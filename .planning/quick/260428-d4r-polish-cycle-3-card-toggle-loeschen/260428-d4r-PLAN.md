---
phase: quick-260428-d4r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/nerve.css
  - templates/profile_editor.html
  - static/profile_editor.js
autonomous: true
requirements: [POLISH-C3]
must_haves:
  truths:
    - "Save-Button zeigt schwarze Schrift (#000000)"
    - "'Vorschläge laden' zeigt schwarze Schrift, auch disabled (opacity 0.6, nicht grau)"
    - "Alle '+ Hinzufügen' Buttons haben margin-top: 18px (konsistent, kein inline-style override)"
    - "Card-Toggle: nur Chevron-Button triggert collapse, Name-Input-Click öffnet kein Toggle mehr"
    - "Chevron-Icons sind 18px, teal (#00D4AA), rotation-animation bleibt"
    - "Kein 'Löschen'-Text sichtbar — alle crud-del sind Lucide trash-2 Icon-Buttons"
    - ".tip-icon hat teal Hintergrund, schwarze Schrift, 18x18px"
    - "Tabu-Zeilen-Delete ist Lucide trash-2 (nicht '×'-Zeichen)"
    - "crud-hd hat border-bottom Separator, crud-body hat dunkleren Background"
  artifacts:
    - path: "static/nerve.css"
      provides: "CSS-Regeln für alle 9 Items"
    - path: "templates/profile_editor.html"
      provides: "Chevron-Button-Refactoring, crud-del → btn-trash, Wisdom-disabled-fix"
    - path: "static/profile_editor.js"
      provides: "Toggle-Logik nur auf Chevron-Button, tabu-del-btn → btn-trash"
  key_links:
    - from: ".crud-hd onclick"
      to: "nur .crud-chevron button"
      via: "onclick entfernt von hd.addEventListener, Button-click-handler auf chevron-btn"
    - from: ".crud-del"
      to: ".btn-trash mit Lucide SVG"
      via: "JS renderItem() + template/inline HTML"
---

<objective>
Polish-Cycle 3 für Phase 08.19.2: 9 UAT-Items, alle Frontend-only.

Purpose: Visuell konsistente CRUD-Cards, einheitliche Farben, saubere Toggle-Isolation, Löschen-Button-Vereinheitlichung mit Lucide trash-2 Icon.
Output: Aktualisierte nerve.css, profile_editor.html, profile_editor.js ohne Backend-Änderungen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md

<!-- Aktuelle CSS-Patterns (aus nerve.css) -->
<interfaces>
<!-- .save-btn (inline <style> in profile_editor.html, Zeile 26-31):
  background: #00D4AA; color: var(--page-bg); border-radius: 8px;
  PROBLEM: color: var(--page-bg) = dunkel/schwarz auf teal => FIXIEREN auf color: #000000

  .btn-primary (nerve.css Zeile 402-426):
  color: var(--btn-primary-text) = #0D1117 (aus Variable, bereits fast schwarz)
  .btn-primary:disabled { opacity: 0.5; } => muss 0.6 + color: #000000 explizit sein

  .crud-hd (inline <style> Zeile 207-214):
  display: flex; align-items: center; gap: 8px; padding: 10px 14px; cursor: pointer;
  => cursor: pointer muss weg (nur Chevron-Button ist klickbar)

  .crud-chevron (inline <style> Zeile 215):
  flex-shrink: 0; color: var(--page-text-muted); font-size: 12px;
  => wird zu Button mit width/height 18px, color: #00D4AA

  .acc-chevron (inline in JS/HTML, style="font-size:12px;color:var(--page-text-muted)"):
  Alle Stellen: block-hd onclick=toggleBlock/toggleEW — toggle BLEIBT auf dem div
  (block-hd hat kein name-input, kein Konflikt) => nur CRUD-Cards betroffen

  .tip-icon (nerve.css Zeile 2527-2553):
  background: #F3F4F6; color: #00D4AA; width: 16px; height: 16px; font-size: 12px;
  => muss: background: #00D4AA; color: #000000; width: 18px; height: 18px; font-size: 14px;

  .btn-trash (nerve.css Zeile 474-498):
  Bereits vorhanden aus C2X: 16px SVG, color: var(--page-text-muted), hover: #EF4444

  tabu-del-btn (profile_editor.js Zeile 335-337):
  delBtn.className = 'tabu-del-btn';
  delBtn.textContent = '×';  (= × Zeichen)
  inline style: font-size:18px; color:#9CA3AF; => ersetzen durch .btn-trash + Lucide SVG
-->
</interfaces>
</context>

<tasks>

<!-- ══════════════════════════════════════════════════════════════════════
     TASK 1: CSS-Fixes (Items 1, 2, 3, 5, 7, 9) — alle in nerve.css
     + inline <style>-Block in profile_editor.html
     ══════════════════════════════════════════════════════════════════════ -->
<task type="auto">
  <name>Task 1: CSS — Save-Button, Vorschläge-disabled, Add-Spacing, Chevron-Style, tip-icon, Separator</name>
  <files>static/nerve.css, templates/profile_editor.html</files>
  <action>
**A) profile_editor.html — inline &lt;style&gt;-Block (oben, Zeile 26-31):**

Ändere `.save-btn`:
```css
.save-btn {
  background: #00D4AA; color: #000000; border: none; border-radius: 8px;
  padding: 8px 20px; font-size: 13px; font-weight: 700; cursor: pointer;
  font-family: inherit; white-space: nowrap; margin-left: auto;
}
```
(color: var(--page-bg) → color: #000000)

Ändere `.crud-hd` (Zeile 207-210): Entferne `cursor: pointer` aus der Klasse — Cursor soll nur auf dem Chevron-Button liegen (Item 4 folgt in Task 2).
```css
.crud-hd {
  display: flex; align-items: center; gap: 8px; padding: 10px 14px;
  user-select: none;
}
```

Ändere `.crud-chevron` (Zeile 215): Von reinem `&lt;span&gt;`-Style zu Button-freundlichem Style:
```css
.crud-chevron {
  flex-shrink: 0; background: none; border: none; padding: 0;
  color: #00D4AA; font-size: 18px; cursor: pointer; line-height: 1;
  width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center;
  border-radius: 4px; transition: opacity 0.15s;
}
.crud-chevron:hover { opacity: 0.75; }
```

Füge am Ende des inline `&lt;style&gt;`-Blocks hinzu (Item 9 — Separator):
```css
/* C3: Separator Name-Header / Body */
.crud-hd {
  border-bottom: 1px solid #E2E8F0;
}
.crud-body {
  background: #161B22;
}
```
WICHTIG: Schreibe stattdessen `.crud-hd` und `.crud-body` als separate Regel an, damit keine Dopplung mit dem bestehenden `.crud-hd`-Block entsteht. Füge `border-bottom: 1px solid #E2E8F0;` direkt in den bestehenden `.crud-hd`-Block ein. Füge `background: #161B22;` in den bestehenden `.crud-body`-Block (Zeile 216) ein.

**B) static/nerve.css:**

**Item 2 — .btn-primary:disabled** (aktuell Zeile 422-426, opacity: 0.5):
```css
.btn-primary:disabled {
  opacity: 0.6;
  color: #000000;
  cursor: not-allowed;
  transform: none;
}
```

**Item 3 — Add-Buttons margin-top**: Füge unterhalb der `.btn-primary:disabled`-Regel eine neue Regel ein:
```css
/* C3: Add-Button spacing — alle "+ Hinzufügen" im Profile-Editor */
.btn-primary.btn-add-item {
  margin-top: 18px;
}
```
Anschließend in profile_editor.html: Alle `&lt;button class="btn-primary"&gt;` die "+ ... hinzufügen" oder "+ Hinzufügen" text haben bekommen zusätzlich class `btn-add-item`. Die Stellen sind (Zeilen ca. 412, 427, 433, 438, 444, 457, 472, 478, 492, 577, 591, 603, 616, 628):
- `class="btn-primary"` → `class="btn-primary btn-add-item"`
  Nur die "+" Add-Buttons (nicht "Vorschläge laden", nicht "Standard-Paare einfügen").
  Entferne dabei bestehende `style="margin-top:8px"` inline-Styles von diesen Buttons.

**Item 5 — Chevron teal + bigger** (acc-chevron, die inline-gestylten Spans in block-hd):
Füge in nerve.css eine neue Regel für `.acc-chevron` ein (unterhalb `.card-header` Zeile 838):
```css
/* C3: acc-chevron teal + 18px */
.acc-chevron {
  color: #00D4AA !important;
  font-size: 18px !important;
  flex-shrink: 0;
  transition: color 0.15s;
}
```
(Die inline-Styles verwenden `font-size:12px;color:var(--page-text-muted)` — `!important` überschreibt sie ohne HTML-Änderung.)

**Item 7 — .tip-icon** (nerve.css Zeile 2527-2553): Ändere den Block:
```css
.tip-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 18px; height: 18px; min-width: 18px;
  margin-left: 6px;
  border-radius: 50%;
  background: #00D4AA;
  color: #000000;
  font-size: 14px;
  font-weight: 700;
  cursor: help;
  user-select: none;
  vertical-align: middle;
  font-style: normal;
  text-transform: none;
  letter-spacing: 0;
  transition: background-color .12s, color .12s;
}
.tip-icon:hover,
.tip-icon:focus {
  background: #00BFA0;
  color: #000000;
  outline: none;
}
```
(border: 1px solid entfernt, background von #F3F4F6 → #00D4AA, color von #00D4AA → #000000, size 16→18px, font-size 12→14px)
  </action>
  <verify>
    Lade `/profiles` im Browser (oder Flask dev server). Prüfe:
    - Save-Button (oben rechts) zeigt schwarze Schrift auf tealem Hintergrund
    - "Vorschläge laden" Button hat schwarze Schrift, opacity 0.6 bei disabled
    - "+ Hinzufügen"-Buttons haben sichtbaren Abstand nach oben (~18px)
    - Chevrons in Einwands-Cards / Painpoints sind teal und größer
    - Fragezeichen-Icons (.tip-icon) haben teal Hintergrund und schwarze Schrift, 18px
    - CRUD-Cards (Skripte etc.) haben Trennlinie unter dem Header
    `python -c "import re; html=open('templates/profile_editor.html').read(); css=open('static/nerve.css').read(); print('save-btn color:#000' if 'color: #000000' in html else 'FAIL save-btn'); print('disabled:0.6' if 'opacity: 0.6' in css else 'FAIL disabled'); print('tip-icon bg teal' if 'background: #00D4AA' in css and '.tip-icon' in css else 'FAIL tip-icon')"`
  </verify>
  <done>
    - .save-btn color: #000000 (nicht var(--page-bg))
    - .btn-primary:disabled opacity: 0.6 + color: #000000
    - .btn-primary.btn-add-item { margin-top: 18px } in nerve.css; alle Add-Buttons haben class btn-add-item, keine margin-top inline-styles mehr
    - .acc-chevron in nerve.css: color: #00D4AA !important; font-size: 18px !important
    - .crud-chevron: color: #00D4AA; 32x32px touch target
    - .tip-icon: background: #00D4AA; color: #000000; 18x18px; font-size: 14px
    - .crud-hd hat border-bottom: 1px solid #E2E8F0
    - .crud-body hat background: #161B22
  </done>
</task>

<!-- ══════════════════════════════════════════════════════════════════════
     TASK 2: Card-Toggle-Fix (Item 4) — JS-Änderung in renderItem()
     Chevron als &lt;button&gt; statt &lt;span&gt;, hd.addEventListener entfernen
     ══════════════════════════════════════════════════════════════════════ -->
<task type="auto">
  <name>Task 2: Toggle-Isolation — Chevron-Button statt Header-onclick (CRUD-Cards)</name>
  <files>templates/profile_editor.html</files>
  <action>
In `profile_editor.html`, Funktion `renderItem()` (ab Zeile ~1220 im `&lt;script&gt;`-Block):

**Schritt 1** — Chevron von `&lt;span&gt;` zu `&lt;button&gt;` ändern:
```js
// ALT (Zeile 1230-1232):
var chevron = document.createElement('span');
chevron.className = 'crud-chevron';
chevron.textContent = '▸';

// NEU:
var chevron = document.createElement('button');
chevron.type = 'button';
chevron.className = 'crud-chevron';
chevron.setAttribute('aria-label', 'Abschnitt ein-/ausklappen');
chevron.textContent = '▸';
```

**Schritt 2** — onclick-Handler vom `hd`-div ENTFERNEN, stattdessen auf `chevron`-Button setzen:
```js
// ALT (Zeile 1236-1240): hd.addEventListener('click', function() { ... });
// => KOMPLETT ERSETZEN durch:

chevron.addEventListener('click', function(e) {
  e.stopPropagation();
  var body = card.querySelector('.crud-body');
  var collapsed = body.classList.toggle('collapsed');
  chevron.textContent = collapsed ? '▸' : '▾';
});
```

**Schritt 3** — nameInput click-Stopper bleibt (Zeile 1228, `e.stopPropagation()`): Dieser kann bleiben oder entfernt werden — da hd kein onclick mehr hat, ist er redundant aber nicht schädlich. Entferne ihn der Sauberkeit halber.

**Schritt 4** — Auto-expand bei neuem Item (ab Zeile ~1311-1314): Der Code setzt `chev.textContent = '▾'` — das bleibt identisch, kein Änderungsbedarf.

WICHTIG: Die `block-hd` + `acc-chevron` Stellen (Schmerz, Phasen, Einwände, FAQ) verwenden `onclick="toggleBlock(this.parentElement)"` auf dem `div` — diese bleiben UNVERÄNDERT. Die haben kein Name-Input-Konflikt. Nur CRUD-Cards (`renderItem()`) werden angefasst.
  </action>
  <verify>
    Im Browser: CRUD-Card öffnen (z.B. Skript-Karte).
    - Click auf Name-Input: kein Toggle, Edit-Cursor erscheint
    - Click auf Chevron-Button: Card klappt auf/zu
    - Click auf Card-Header-Bereich (nicht Chevron): KEIN Toggle
    `python -c "import re; src=open('templates/profile_editor.html').read(); m=re.search(r'hd\.addEventListener.*?click.*?body.*?classList', src, re.DOTALL); print('FAIL: hd still has click listener' if m else 'OK: hd listener removed'); print('OK: chevron is button' if 'chevron.type = .button.' in src else 'FAIL: chevron not button')"`
  </verify>
  <done>
    - `chevron` ist ein `&lt;button type="button" class="crud-chevron"&gt;`
    - `hd.addEventListener('click', ...)` existiert nicht mehr
    - `chevron.addEventListener('click', ...)` öffnet/schließt `.crud-body`
    - Name-Input-Click hat kein Toggle-Side-Effect
    - Auto-expand bei neuem Item funktioniert weiterhin
  </done>
</task>

<!-- ══════════════════════════════════════════════════════════════════════
     TASK 3: Löschen-Konsolidierung (Items 6 + 8) — crud-del + tabu-del-btn
     ══════════════════════════════════════════════════════════════════════ -->
<task type="auto">
  <name>Task 3: Löschen-Konsolidierung — crud-del → btn-trash, tabu × → trash-2</name>
  <files>templates/profile_editor.html, static/profile_editor.js</files>
  <action>
**Lucide trash-2 SVG-Snippet** (identisch mit bestehenden btn-trash Stellen):
```html
<svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4h6v2"/></svg>
```

**Item 6 — crud-del in renderItem() (profile_editor.html, Zeile ~1251-1254):**

```js
// ALT:
var delBtn = document.createElement('button');
delBtn.type = 'button';
delBtn.className = 'crud-del';
delBtn.textContent = 'Löschen';

// NEU:
var delBtn = document.createElement('button');
delBtn.type = 'button';
delBtn.className = 'btn-trash';
delBtn.title = 'Löschen';
delBtn.innerHTML = '<svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4h6v2"/></svg>';
```

Der `delBtn.addEventListener('click', ...)` ab Zeile 1275 bleibt unverändert.

Entferne aus dem inline `&lt;style&gt;`-Block die alten Regeln (Zeilen 225-226):
```css
/* WEG: */
.crud-card .crud-del { ... }
.crud-card .crud-del:hover { ... }
```
(nicht mehr benötigt, .btn-trash ist in nerve.css definiert)

**Item 8 — Tabu-Zeilen-Delete (profile_editor.js, Zeile ~333-342):**

```js
// ALT:
var delBtn = document.createElement('button');
delBtn.type = 'button';
delBtn.className = 'tabu-del-btn';
delBtn.textContent = '×';
delBtn.style.cssText = 'background:transparent;border:none;color:#9CA3AF;cursor:pointer;font-size:18px;padding:4px 6px;line-height:1;flex-shrink:0;';
delBtn.addEventListener('click', function () {
  row.remove();
  validateTabuRows();
  syncTabuHidden();
});

// NEU:
var delBtn = document.createElement('button');
delBtn.type = 'button';
delBtn.className = 'btn-trash';
delBtn.title = 'Löschen';
delBtn.innerHTML = '<svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4h6v2"/></svg>';
delBtn.addEventListener('click', function () {
  row.remove();
  validateTabuRows();
  syncTabuHidden();
});
```
(style.cssText entfällt — .btn-trash aus nerve.css übernimmt)

Nach den Änderungen: Suche in beiden Dateien nach verbliebenem "Löschen"-Text in Buttons/Links (nicht in title-Attributen, nicht in Kommentaren — die sind OK):
```bash
grep -n "textContent.*Löschen\|\.crud-del\b" templates/profile_editor.html static/profile_editor.js
```
Ergebnis muss leer sein (außer Kommentare).
  </action>
  <verify>
    Im Browser:
    - CRUD-Card aufklappen: Löschen-Button zeigt trash-2 Icon (kein "Löschen"-Text)
    - Tabu-Zeile: × durch trash-2 Icon ersetzt
    - Delete-Funktion: confirmDelete Dialog erscheint, danach Eintrag entfernt
    `grep -n "textContent.*Löschen\b\|className.*crud-del\b" templates/profile_editor.html static/profile_editor.js | grep -v "//\|/\*" | wc -l` muss 0 sein.
  </verify>
  <done>
    - renderItem() erzeugt `&lt;button class="btn-trash"&gt;` mit Lucide SVG statt Text "Löschen"
    - renderTabuRow() erzeugt `&lt;button class="btn-trash"&gt;` mit Lucide SVG statt ×
    - .crud-del CSS-Regeln aus inline &lt;style&gt; entfernt
    - confirmDelete-Logik beider Buttons funktioniert unverändert
    - Kein sichtbarer "Löschen"-Text in der UI
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Flask | Alle Formulardaten aus profile_editor.html gehen als JSON ans Backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C3-01 | Tampering | crud-del onclick → confirmDelete | accept | Nur UI-Änderung; Backend-Delete-Route validiert Auth bereits via login_required |
| T-C3-02 | Information | tabu-del-btn ohne confirm | accept | Tabu-Zeilen sind Low-Value; Undo über Neuladen; kein Datenverlust ohne expliziten Save |
</threat_model>

<verification>
Nach allen 3 Tasks:
1. Flask starten: `python app.py`
2. `/profiles` öffnen, ein Profil öffnen → Profile-Editor aufrufen
3. Checklist:
   - [ ] Save-Button: schwarze Schrift (#000000) auf teal
   - [ ] "Vorschläge laden": schwarze Schrift, opacity 0.6 (disabled)
   - [ ] "+ Hinzufügen"-Buttons: ~18px Abstand nach oben
   - [ ] CRUD-Card (z.B. Skript): nur Chevron-Icon klappt auf/zu; Name-Input-Click togglet nicht
   - [ ] Chevron-Icons überall: teal (#00D4AA), ~18px
   - [ ] Löschen in CRUD-Cards: trash-2 Icon, kein Text
   - [ ] Fragezeichen-Icons (.tip-icon): teal Hintergrund, schwarze Schrift, 18px
   - [ ] Tabu-Zeilen: trash-2 Icon statt ×
   - [ ] CRUD-Card-Header: Trennlinie sichtbar; Body: etwas dunkler (#161B22)
</verification>

<success_criteria>
- Alle 9 UAT-Items implementiert, keine Regression in bestehenden Interaktionen
- Kein sichtbarer "Löschen"-Text in der UI (nur Icon)
- Toggle-Konflikt zwischen Name-Input und Card-Collapse behoben
- CSS-Änderungen in nerve.css (persistent), nicht als weitere inline-Overrides
- Keine Backend-Änderungen
</success_criteria>

<output>
Nach Completion: `.planning/quick/260428-d4r-polish-cycle-3-card-toggle-loeschen/260428-d4r-SUMMARY.md` erstellen mit:
- Items implemented (1-9)
- Files modified
- Any deviations from plan
</output>
