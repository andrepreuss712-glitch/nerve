---
phase: 260428-c2x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/profile_editor.html
  - static/nerve.css
  - static/profile_editor.js
autonomous: true
requirements:
  - POLISH-C2X-A
  - POLISH-C2X-B
  - POLISH-C2X-C

must_haves:
  truths:
    - "Der 'Sieh dir ein ausgefülltes Beispiel an'-Link ist nicht mehr sichtbar"
    - "Alle '+ Hinzufügen'-Buttons sind Teal-Pille (btn-primary Pattern)"
    - "Der 'Vorschläge laden'-Button ist Teal-Pille (sichtbar auch im disabled-State)"
    - "Alle Lösch-Aktionen zeigen ein Lucide trash-2 Icon (grau → rot-hover)"
    - "Jede Lösch-Aktion öffnet einen Modal-Confirm-Dialog statt browser-native confirm()"
    - "Der confirm-Dialog hat 'Abbrechen' (btn-secondary) + 'Löschen' (btn-destructive)"
    - "Keine info-blue-Verwendung auf Buttons mehr, keine Inline-Styles auf Buttons"
  artifacts:
    - path: "templates/profile_editor.html"
      provides: "Bereinigter Template-Code — Link entfernt, btn-primary auf Add-Buttons, trash-2 Icons auf Delete-Buttons"
    - path: "static/nerve.css"
      provides: "btn-primary, btn-destructive, btn-secondary CSS-Klassen + trash-icon CSS + confirm-modal CSS"
    - path: "static/profile_editor.js"
      provides: "confirmDelete(callback, label) Helper + Modal-Inject-Logik"
  key_links:
    - from: "templates/profile_editor.html (trash-2 Icons)"
      to: "static/profile_editor.js confirmDelete()"
      via: "onclick='confirmDelete(...)' Attribut"
      pattern: "confirmDelete\\("
    - from: "static/profile_editor.js confirmDelete()"
      to: "CSS confirm-modal"
      via: "DOM-inject eines #confirm-modal divs"
      pattern: "confirm-modal"
---

<objective>
Polish-Cycle 2 für Phase 08.19.2: drei UAT-Findings 2026-04-28 schließen.

Purpose: Header-Link-Aufräumung, Button-System-Konsolidierung und einheitliches Lösch-Pattern
erhöhen die visuelle Konsistenz des Profil-Editors und beseitigen inkonsistente UI-Zustände
vor dem Launch.

Output:
- `templates/profile_editor.html` — Link entfernt, Add-Buttons auf btn-primary, Delete-Buttons auf trash-2
- `static/nerve.css` — btn-primary / btn-destructive / btn-secondary Klassen + trash-icon + modal CSS
- `static/profile_editor.js` — confirmDelete() Helper
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/quick/260428-c2x-polish-cycle-2-button-konsolidierung/260428-c2x-PLAN.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/STATE.md
@C:/Users/andre/dev/salesnerve/CLAUDE.md

Stack: Flask + Vanilla JS. Lucide via `static/vendor/lucide.min.js` bereits geladen.
Bestehende Button-Token: `.n-btn-primary` (Teal-Pille), `.n-btn-ghost`, `.n-btn-danger` in nerve.css.
Neue Kurzklassen (ohne n-Prefix) werden als Convenience-Aliases ergänzt — sie müssen das
n-btn-primary-Pattern spiegeln, NICHT duplizieren (Token-Variablen referenzieren).

<interfaces>
<!-- Bestehende CSS-Token in nerve.css (Quelle: Zeilen 39–79) -->
/* Teal-Primary Token */
--btn-primary-bg-from:  #00D4AA;
--btn-primary-bg-to:    #00B894;
--btn-primary-text:     #0D1117;
--btn-primary-radius:   9999px;
--btn-primary-padding:  12px 28px;
--btn-primary-size:     14px;
--btn-primary-weight:   600;
--btn-primary-glow:     0 0 20px rgba(0,212,170,0.15), 0 0 60px rgba(0,212,170,0.06);
--btn-primary-glow-hover: 0 8px 30px rgba(0,212,170,0.25);

/* Bestehende Klassen */
.n-btn-primary  → Teal-Pille (vollständig implementiert)
.n-btn-ghost    → Outline/transparent
.n-btn-danger   → Rot-Hover destructive

/* Bestehende Add-Buttons (nerve.css, Zeile 364 + template) */
--info-blue: #4899c8;  /* btn-add Basis-Farbe — WIRD ENTFERNT von Buttons */
class="btn-add"        → 11 Stellen in profile_editor.html
class="wisdom-stub-btn" → Zeile 319 in profile_editor.html (disabled)
class="n-btn-ghost" id="tabu-add-btn" → Zeile 639 (ebenfalls → btn-primary)

/* Bestehende Delete-Buttons in profile_editor.html */
class="btn-rm faq-delete"              → Zeile 539 (Text "Löschen")
class="btn-rm" onclick="...remove()">✕ → Zeilen 893, 927, 968, 1016, 1042, 1059, 1077, 1097
native confirm() in profile_editor.js  → Zeile 136 (FAQ) und Zeile 1287

/* Confirm-Dialog: bisher */
// profile_editor.js Zeile 136:
if (!confirm('FAQ wirklich löschen?')) return;
// profile_editor.html Zeile 1287:
if (!confirm('Wirklich löschen?')) return;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Header-Link entfernen (profile_editor.html)</name>
  <files>templates/profile_editor.html</files>
  <action>
Entferne exakt die folgenden zwei Zeilen aus `templates/profile_editor.html` (Zeilen 263–264):

```html
    <a href="#" class="beispiel-link" onclick="openBeispiel();return false"
       aria-label="Beispiel-Profil anzeigen">Sieh dir ein ausgefülltes Beispiel an</a>
```

Falls dort ein umgebender Container (z.B. `<div class="topbar-...">` o.ä.) durch das Entfernen
leer wird, diesen ebenfalls entfernen — aber NUR wenn er ausschließlich diesen Link enthielt.
Nicht die `openBeispiel()`-Funktion in JS entfernen (sie kann noch durch das Beispiel-Modal
referenziert werden — Task A betrifft nur das Link-Element im Header).
  </action>
  <verify>
    <automated>grep -n "Sieh dir ein ausgefülltes Beispiel an" templates/profile_editor.html; echo "exit $?"</automated>
  </verify>
  <done>Der Satz "Sieh dir ein ausgefülltes Beispiel an" kommt in templates/profile_editor.html nicht mehr vor. grep gibt keine Treffer zurück.</done>
</task>

<task type="auto" tdd="false">
  <name>Task B: Button-Konsolidierung — CSS + Template</name>
  <files>static/nerve.css, templates/profile_editor.html</files>
  <action>
### 1. nerve.css — neue Convenience-Klassen ergänzen

Füge nach dem bestehenden `.n-btn-primary`-Block (nach ca. Zeile 395) einen neuen Abschnitt ein:

```css
/* ═══════════════════════════════════════════════════
   POLISH C2X: Convenience-Aliases für Profil-Editor
   ═══════════════════════════════════════════════════ */

/* btn-primary: Teal-Pille — identisch mit .n-btn-primary */
.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: linear-gradient(135deg, var(--btn-primary-bg-from), var(--btn-primary-bg-to));
  color: var(--btn-primary-text);
  border: none;
  border-radius: var(--btn-primary-radius);
  padding: 8px 20px;
  font-size: 13px;
  font-weight: var(--btn-primary-weight);
  font-family: var(--page-font);
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4,0,0.2,1);
  box-shadow: var(--btn-primary-glow);
}
.btn-primary:hover {
  transform: translateY(-1px);
  box-shadow: var(--btn-primary-glow-hover);
}
.btn-primary:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

/* btn-destructive: grau normal → rot auf hover */
.btn-destructive {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: rgba(0,0,0,0.04);
  color: var(--page-text-muted);
  border: 1px solid rgba(0,0,0,0.12);
  border-radius: 6px;
  padding: 8px 16px;
  font-size: 13px;
  font-weight: 500;
  font-family: var(--page-font);
  cursor: pointer;
  transition: all 0.2s ease;
}
.btn-destructive:hover {
  background: #EF4444;
  color: #ffffff;
  border-color: #EF4444;
}

/* btn-secondary: outline-Stil */
.btn-secondary {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: transparent;
  color: var(--page-text-muted);
  border: 1px solid rgba(0,0,0,0.2);
  border-radius: 6px;
  padding: 8px 16px;
  font-size: 13px;
  font-weight: 500;
  font-family: var(--page-font);
  cursor: pointer;
  transition: all 0.2s ease;
}
.btn-secondary:hover {
  border-color: var(--page-text-muted);
  background: rgba(0,0,0,0.04);
}
```

### 2. nerve.css — `--info-blue`-Referenz auf Buttons bereinigen

Die `.btn-add`-Klasse in der CSS referenziert `--info-blue`. Ersetze diese Klasse so, dass
sie nur noch als toter Selektor stehen bleibt (oder entferne sie ganz), da nach diesem Task
alle `btn-add`-Elemente im Template zu `btn-primary` werden. Suche nach dem Block der
`.btn-add`-Definition im Template (inline `<style>` oder in nerve.css) und entferne ihn
bzw. setze ihn auf `/* deprecated, replaced by .btn-primary */`.

### 3. templates/profile_editor.html — Add-Buttons ersetzen

Ersetze an allen folgenden Stellen `class="btn-add"` durch `class="btn-primary"`:
- Zeile 425: Painpoint hinzufügen
- Zeile 440: Erlaubnisfrage hinzufügen (id="btn-add-erlaubnis" bleibt erhalten)
- Zeile 446: Pitch hinzufügen (id="btn-add-pitch" bleibt erhalten)
- Zeile 451: Skript hinzufügen (id="btn-add-skripte" bleibt erhalten) — Inline-Style `style="margin-top:8px"` extrahieren: statt Inline-Style die Klasse `btn-primary` behalten, margin-top als separate Utility via `style="margin-top:8px"` ist akzeptabel wenn keine Utility-Klasse vorhanden; alternativ in CSS ergänzen als `.btn-primary + .btn-primary { margin-top: 8px; }` — entscheide nach aktuellem Kontext im Template was sauberer ist.
- Zeile 457: Opener hinzufügen (id="btn-add-opener" bleibt erhalten)
- Zeile 470: Phase hinzufügen
- Zeile 485: Einwand hinzufügen
- Zeile 491: Frage hinzufügen (Skript-Sektion)
- Zeile 505: Frage hinzufügen (FAQ-Sektion, id="btn-faq-add" bleibt erhalten)
- Zeile 588: Kaufsignal hinzufügen
- Zeile 602: Übergang hinzufügen
- Zeile 614: No-Go hinzufügen
- Zeile 627: Wettbewerber hinzufügen

Zeile 639: `class="n-btn-ghost"` auf `class="btn-primary"` ändern (tabu-add-btn).

### 4. templates/profile_editor.html — Vorschläge-laden-Button

Zeile 319: `.wisdom-stub-btn` → `class="btn-primary"` setzen (bleibt disabled).
Den `.wisdom-stub-btn`-CSS-Block in nerve.css als deprecated markieren (kommentieren),
da die Styles nun durch `.btn-primary:disabled` abgedeckt werden.
  </action>
  <verify>
    <automated>grep -c "btn-add\|wisdom-stub-btn\|info-blue.*btn\|n-btn-ghost.*tabu" templates/profile_editor.html; echo "Erwartung: 0"</automated>
  </verify>
  <done>
- Kein `class="btn-add"` mehr in profile_editor.html (alle → btn-primary)
- `tabu-add-btn` hat Klasse btn-primary statt n-btn-ghost
- `wisdom-stub-btn` hat Klasse btn-primary (mit disabled-Attribut)
- CSS enthält .btn-primary, .btn-destructive, .btn-secondary Definitionen
- nerve.css hat keine aktive .btn-add-Klasse mehr die info-blue verwendet
  </done>
</task>

<task type="auto">
  <name>Task C: Lösch-Button-Konsolidierung — trash-2 Icon + confirmDelete Modal</name>
  <files>static/profile_editor.js, templates/profile_editor.html, static/nerve.css</files>
  <action>
### 1. nerve.css — trash-icon CSS + confirm-modal CSS ergänzen

Füge nach den btn-*-Klassen aus Task B folgenden Block ein:

```css
/* ═══════════════════════════════════
   TRASH ICON BUTTON (C2X)
   ═══════════════════════════════════ */
.btn-trash {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: none;
  cursor: pointer;
  color: var(--page-text-muted);
  padding: 4px;
  border-radius: 4px;
  transition: color 0.2s ease, background 0.2s ease;
  line-height: 0;
}
.btn-trash:hover {
  color: #EF4444;
  background: rgba(239,68,68,0.08);
}
.btn-trash svg {
  width: 16px;
  height: 16px;
  stroke: currentColor;
  fill: none;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

/* ═══════════════════════════════════
   CONFIRM DELETE MODAL (C2X)
   ═══════════════════════════════════ */
#confirm-delete-modal {
  display: none;
  position: fixed;
  inset: 0;
  z-index: 9999;
  background: rgba(0,0,0,0.45);
  align-items: center;
  justify-content: center;
}
#confirm-delete-modal.active {
  display: flex;
}
.confirm-delete-box {
  background: var(--card-bg, #fff);
  border: 1px solid var(--card-border, rgba(0,0,0,0.1));
  border-radius: 12px;
  padding: 28px 32px;
  max-width: 360px;
  width: 90%;
  text-align: center;
  box-shadow: 0 20px 60px rgba(0,0,0,0.2);
}
.confirm-delete-box h3 {
  margin: 0 0 8px;
  font-size: 16px;
  font-weight: 600;
  color: var(--page-text, #1a1a1a);
}
.confirm-delete-box p {
  margin: 0 0 24px;
  font-size: 14px;
  color: var(--page-text-muted, #6b7280);
}
.confirm-delete-actions {
  display: flex;
  gap: 12px;
  justify-content: center;
}
```

### 2. profile_editor.js — confirmDelete() Helper

Füge am Anfang von profile_editor.js (nach bestehenden `'use strict'` oder ersten Zeilen)
folgende Funktion ein:

```javascript
/* ── Confirm-Delete Modal (C2X) ───────────────────────── */
(function ensureConfirmModal() {
  if (document.getElementById('confirm-delete-modal')) return;
  const el = document.createElement('div');
  el.id = 'confirm-delete-modal';
  el.innerHTML = `
    <div class="confirm-delete-box">
      <h3>Wirklich löschen?</h3>
      <p id="confirm-delete-label">Dieser Eintrag wird entfernt.</p>
      <div class="confirm-delete-actions">
        <button type="button" class="btn-secondary" id="confirm-delete-cancel">Abbrechen</button>
        <button type="button" class="btn-destructive" id="confirm-delete-ok">Löschen</button>
      </div>
    </div>`;
  document.body.appendChild(el);
  document.getElementById('confirm-delete-cancel').addEventListener('click', () => {
    el.classList.remove('active');
    el._callback = null;
  });
  el.addEventListener('click', (e) => {
    if (e.target === el) { el.classList.remove('active'); el._callback = null; }
  });
})();

/**
 * Zeigt einen Modal-Confirm-Dialog.
 * @param {Function} callback  — wird bei Bestätigung aufgerufen
 * @param {string}   label     — optionaler Beschreibungstext (z.B. "Einwand")
 */
function confirmDelete(callback, label) {
  const modal = document.getElementById('confirm-delete-modal');
  const labelEl = document.getElementById('confirm-delete-label');
  if (labelEl && label) {
    labelEl.textContent = label + ' wird entfernt.';
  } else if (labelEl) {
    labelEl.textContent = 'Dieser Eintrag wird entfernt.';
  }
  modal._callback = callback;
  const okBtn = document.getElementById('confirm-delete-ok');
  // Remove previous listener to avoid stacking
  const newOk = okBtn.cloneNode(true);
  okBtn.parentNode.replaceChild(newOk, okBtn);
  newOk.addEventListener('click', () => {
    modal.classList.remove('active');
    if (modal._callback) modal._callback();
    modal._callback = null;
  });
  modal.classList.add('active');
}
```

Ersetze dann in profile_editor.js alle nativen `confirm()`-Aufrufe:
- Zeile 136 (FAQ): `if (!confirm('FAQ wirklich löschen?')) return;` →
  den umgebenden Code so umstrukturieren, dass der Delete-Code in eine lokale Funktion
  wandert die an `confirmDelete(fn, 'FAQ-Eintrag')` übergeben wird.
- Zeile 1287: `if (!confirm('Wirklich löschen?')) return;` → analog mit `confirmDelete(fn, 'Eintrag')`.

### 3. templates/profile_editor.html — alle btn-rm durch btn-trash ersetzen

**Trash-Icon SVG Snippet** (für alle Stellen verwenden):
```html
<button type="button" class="btn-trash" title="Löschen" onclick="confirmDelete(() => { this.closest('.block').remove(); }, 'Eintrag')">
  <svg viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4h6v2"/></svg>
</button>
```

Ersetze an folgenden Stellen (grep zur Verifikation zuerst ausführen):

- Zeile 539: `<button type="button" class="btn-rm faq-delete">Löschen</button>` →
  btn-trash mit `confirmDelete(() => { this.closest('.faq-entry, .block, li').remove(); }, 'FAQ-Eintrag')`
  Achtung: `this` im onclick-Attribut zeigt auf den Button — korrekt für `.closest()`.

- Zeilen 893, 927, 968, 1016, 1042, 1059, 1077, 1097 — jede `class="btn-rm"` mit `✕` →
  btn-trash; den bisherigen `.remove()`-Inline-Code als Callback in `confirmDelete()` verpacken.
  Für den jeweiligen Kontext passenden Label wählen:
  - Schmerz/Painpoint: `'Painpoint'`
  - Phase: `'Gesprächsphase'`
  - Einwand: `'Einwand'`
  - Frage: `'Frage'`
  - Übergang: `'Übergang'`
  - Wettbewerber: `'Wettbewerber'`
  - NoGo: `'No-Go'`
  - Tabu-Begriff: `'Tabu-Begriff'`

  Vorlage für inline-onclick-Konvertierung:
  ```html
  <!-- vorher -->
  <button type="button" class="btn-rm" onclick="this.closest('.block').remove();renumBlocks('schmerz-list','Painpoint')">✕</button>
  <!-- nachher -->
  <button type="button" class="btn-trash" title="Löschen" onclick="confirmDelete(() => { this.closest('.block').remove();renumBlocks('schmerz-list','Painpoint'); }, 'Painpoint')">
    <svg viewBox="0 0 24 24"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4h6v2"/></svg>
  </button>
  ```

  WICHTIG: `this` im onclick-Attribut zeigt auf den Button-Node. Wenn der Callback als Arrow
  Function in `confirmDelete(() => { ... })` übergeben wird, verliert `this` den Button-Kontext.
  Lösung: Den Element-Bezug VOR dem Aufruf sichern:
  ```html
  onclick="(function(btn){ confirmDelete(() => { btn.closest('.block').remove(); }, 'Einwand'); })(this)"
  ```
  Dieses Pattern durchgängig verwenden für alle inline-onclick-Stellen.

Entferne außerdem die veralteten CSS-Blöcke `.btn-rm` aus nerve.css nach erfolgreicher Migration.

Lucide: Da Lucide bereits via `static/vendor/lucide.min.js` geladen wird, ist kein zusätzlicher
Schritt nötig. Die SVG-Pfade sind inline — kein `lucide.createIcons()` Aufruf erforderlich.
  </action>
  <verify>
    <automated>grep -c "class=\"btn-rm\"\|btn-rm faq-delete\|confirm(" templates/profile_editor.html static/profile_editor.js; echo "Alle sollten 0 sein"</automated>
  </verify>
  <done>
- `confirmDelete()` Helper existiert in profile_editor.js
- Kein nativer `confirm()`-Aufruf mehr in profile_editor.js oder profile_editor.html
- Kein `class="btn-rm"` mehr im Template
- Alle Lösch-Stellen zeigen btn-trash SVG-Button
- Hover auf trash-Button färbt Icon rot
- Klick öffnet Modal mit "Abbrechen" + "Löschen"
- CSS enthält .btn-trash und #confirm-delete-modal Styles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-Input → DOM | Alle Label-Strings in confirmDelete() sind aus dem Template hardcoded — kein User-Input fließt in innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C2X-01 | Tampering | confirmDelete() Modal | accept | Modal-Callback ist closure-gebunden; kein XSS-Vektor da label-String aus Template-Attribut kommt (statisch) |
| T-C2X-02 | Spoofing | Confirm-Dialog Overlay | accept | Dialog ist rein client-seitig; tatsächliches Löschen passiert serverseitig im existierenden API-Call der unverändert bleibt |
</threat_model>

<verification>
Nach Ausführung aller drei Tasks im Browser prüfen:

1. `templates/profile_editor.html` im Browser laden → kein "Sieh dir ein ausgefülltes Beispiel an" sichtbar
2. Einen "+ Hinzufügen"-Button sehen → Teal-Pille-Optik
3. Einen Trash-Icon-Button hovern → Icon wird rot
4. Trash-Icon klicken → Modal öffnet mit "Abbrechen" + "Löschen"
5. "Abbrechen" → Modal schließt, Eintrag bleibt
6. Erneut Trash → "Löschen" → Eintrag wird entfernt
7. "Vorschläge laden"-Button sehen → Teal-Pille, disabled-State (ausgegraut)
</verification>

<success_criteria>
- Kein `btn-add`, `btn-rm`, `wisdom-stub-btn` mehr in templates/profile_editor.html
- Kein nativer `confirm()` in JS-Dateien
- CSS enthält .btn-primary, .btn-destructive, .btn-secondary, .btn-trash, #confirm-delete-modal
- Alle Lösch-Aktionen laufen über confirmDelete()
- Header-Link nicht mehr im DOM
</success_criteria>

<output>
Nach Completion kein SUMMARY erforderlich (Quick-Plan). Änderungen direkt committen:
`git add templates/profile_editor.html static/nerve.css static/profile_editor.js`
`git commit -m "polish(c2x): header-link entfernen, btn-system konsolidieren, trash-icon + confirm-modal"`
</output>
```
