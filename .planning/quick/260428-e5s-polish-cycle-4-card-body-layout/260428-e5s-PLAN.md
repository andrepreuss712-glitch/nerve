---
phase: quick-260428-e5s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/nerve.css
  - templates/profile_editor.html
autonomous: true
requirements:
  - POLISH-CYCLE-4-CARD-BODY-BG
  - POLISH-CYCLE-4-CRUD-LAYOUT
  - POLISH-CYCLE-4-PHASEN-SUBHEADER

must_haves:
  truths:
    - ".crud-body hat keinen schwarzen Hintergrund (#161B22) mehr — passt visuell zu .block-Phasen-Cards"
    - "CRUD-Cards (Skripte/Opener/Erlaubnis/Pitch) sehen aus wie .block-Cards: gleicher Border, Radius, Padding, Hintergrund"
    - "Trash-Icon befindet sich in der crud-hd-Zeile neben Chevron"
    - "Phasen-Bereich hat eine sichtbare Sub-Überschrift 'Gesprächsphasen' vor dem col-header"
    - "Alle 4 CRUD-Listen (skripte-list, opener-list, erlaubnis-list, pitch-list) profitieren gleichmäßig"
  artifacts:
    - path: static/nerve.css
      provides: ".crud-card, .crud-hd, .crud-body, .crud-chevron aktualisiert — kein #161B22"
    - path: templates/profile_editor.html
      provides: "Sub-Header 'Gesprächsphasen' eingebaut; Trash in crud-hd per renderItem-JS"
  key_links:
    - from: ".crud-body"
      to: ".block"
      via: "gleiche background/border/padding Werte"
      pattern: "background: #F8FAFC"
    - from: "renderItem() in crudList()"
      to: "crud-hd"
      via: "delBtn wird in hd statt actions angehängt"
      pattern: "hd.appendChild(delBtn)"
    - from: "sec-gespraechsleitfaden"
      to: "phasen-list"
      via: "<h3 class='subsec-title'> vor col-header"
      pattern: "subsec-title"
---

<objective>
Polish-Cycle 4: Drei visuelle Korrekturen im Profil-Editor.

1. `.crud-body { background: #161B22 }` war Overreach aus Cycle 3 — revertieren auf transparent (konsistent zu .block-Cards).
2. CRUD-Cards (alle 4: Skripte, Opener, Erlaubnisfragen, Pitch) an .block-Phasen-Card-Style angleichen: gleicher Background, Border, Padding, Trash-Icon in der Header-Zeile neben Chevron.
3. Sub-Überschrift "Gesprächsphasen" vor den Phasen-Cards in der Gesprächsleitfaden-Sektion hinzufügen.

Purpose: Visuell kohärente Card-Sprache im gesamten Profil-Editor. Keine JS-Logik-Änderungen, kein Backend.
Output: Angepasstes nerve.css + profile_editor.html (Inline-Style-Block + renderItem JS-Funktion + HTML-Struktur).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende CSS-Klassen (aus templates/profile_editor.html inline <style>) -->

.block {
  background: #F8FAFC; border: 1px solid #E2E8F0; border-radius: 9px;
  padding: 14px; margin-bottom: 10px;
}
.block-hd {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 12px; gap: 8px;
}

<!-- Aktuelle CRUD-Card CSS (inline <style> im HTML, NICHT in nerve.css) -->

.crud-card {
  background: #F8FAFC; border: 1px solid #E2E8F0; border-radius: 8px;
  padding: 0; overflow: hidden;
}
.crud-hd {
  display: flex; align-items: center; gap: 8px; padding: 10px 14px;
  user-select: none; border-bottom: 1px solid #E2E8F0;
}
.crud-hd input {
  background: transparent; border: none; font-weight: 600; font-size: 13px;
  color: #1a1a1a; outline: none; font-family: inherit; flex: 1; cursor: text;
}
.crud-chevron { /* teal toggle button */ }
.crud-body { display: none; padding: 0 14px 10px; display: flex; flex-direction: column; gap: 6px; background: #161B22; }
.crud-body.collapsed { display: none; }
.crud-card textarea { background: transparent; ... min-height: 48px; ... }
.crud-card .crud-actions { display: flex; justify-content: flex-end; }

<!-- Phasen-Struktur (HTML um sec-gespraechsleitfaden) -->
<!-- phasen-Block beginnt bei: <div style="margin-top:24px"> -->
<!--   gefolgt von: <div class="col-header pe-col-grid-skript"> -->
<!--   dann: <div class="skript-hint"> -->
<!--   dann: <div id="phasen-list"> -->

<!-- Sub-Header Pattern der Sektion (existierende Labels in Gesprächsleitfaden) -->
<!-- Erlaubnisfrage: <label class="fl"> in <div class="fg erlaubnis-entry"> -->
<!-- Pitch:          <label class="fl"> in <div class="fg pitch-entry"> -->
<!-- Phasen:         KEIN Label — bisher nur col-header ohne Überschrift -->

<!-- Verfügbare CSS-Klassen für Sub-Header -->
.sec-hint { font-size: 13px; font-weight: 600; color: var(--page-text-color); ... }
/* Kein .subsec-title in nerve.css — neu anlegen oder .fl verwenden -->
/* .fl = <label class="fl"> Pattern für Bereichs-Labels in anderen fg-Divs -->

<!-- renderItem() in crudList() — bestehende Struktur -->
/* hd enthält: nameInput, chevron */
/* body enthält: inhaltTa (textarea), actions (div mit delBtn) */
/* Trash ist aktuell in body > actions, NICHT in hd */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS — .crud-body Background revertieren + .crud-body Padding anpassen</name>
  <files>templates/profile_editor.html</files>
  <action>
Im `<style>`-Block (inline, ca. Zeile 222) die Regel `.crud-body` korrigieren:

VORHER:
```css
.crud-body { display: none; padding: 0 14px 10px; display: flex; flex-direction: column; gap: 6px; background: #161B22; }
```

NACHHER:
```css
.crud-body { padding: 12px 14px 14px; display: flex; flex-direction: column; gap: 6px; background: transparent; }
.crud-body.collapsed { display: none; }
```

Begründungen:
- `background: #161B22` raus — war Cycle-3-Overreach (zu dunkel, fast-schwarz auf weißem Card)
- `background: transparent` — erbt #F8FAFC vom .crud-card, konsistent zu .block
- `padding: 12px 14px 14px` — erhöht von `0 14px 10px` auf 12px top, konsistent mit .block (padding:14px)
- Das doppelte `display: none` / `display: flex` entfernen (Bug aus altem Code) — nur `display: flex` bleibt

WICHTIG: `.crud-body.collapsed { display: none; }` bleibt exakt so (Zeile 223) — nicht anfassen.

Textarea-Mindesthöhe ebenfalls anpassen (Zeile 224-228):
```css
.crud-card textarea { background: transparent; border: 1px solid transparent; border-radius: 4px;
  font-size: 13px; color: var(--page-text-color); resize: vertical;
  min-height: 100px; outline: none; padding: 6px 8px; font-family: inherit; width: 100%; }
```
- `min-height: 48px` → `min-height: 100px` (Item 2: komfortabler Body)
- `font-size: 12px` → `font-size: 13px` (konsistent zu .block-Inputs)
- `color: var(--page-text-secondary)` → `color: var(--page-text-color)` (lesbarerer Text)
- `padding: 4px 6px` → `padding: 6px 8px` (komfortabler)
  </action>
  <verify>
Browser öffnen → Profil-Editor → Skript-Card aufklappen → Body-Hintergrund muss weißlich (#F8FAFC) sein, NICHT dunkelgrau/schwarz. Textarea muss mindestens 100px hoch sein.
  </verify>
  <done>
.crud-body zeigt background: transparent (erscheint weiß durch .crud-card), kein #161B22. Textarea min-height 100px. Kein doppeltes display-Property in der Regel.
  </done>
</task>

<task type="auto">
  <name>Task 2: CRUD-Card Layout — Trash in Header + Phasen Sub-Überschrift</name>
  <files>templates/profile_editor.html</files>
  <action>
Zwei Änderungen im `<script>`-Block und HTML der Gesprächsleitfaden-Sektion.

**A) renderItem() in crudList() — Trash-Icon in crud-hd verschieben**

Die Funktion `renderItem` (ca. ab Zeile 1220) baut die Card per JS. Aktuell liegt `delBtn` in `body > actions`. Umbauen: `delBtn` direkt in `hd` nach `chevron`.

Aktuellen Code suchen (ca. Zeile 1256-1268):
```javascript
var actions = document.createElement('div');
actions.className = 'crud-actions';
var delBtn = document.createElement('button');
// ... delBtn setup ...
actions.appendChild(delBtn);

body.appendChild(inhaltTa);
body.appendChild(actions);
card.appendChild(hd);
card.appendChild(body);
```

Ersetzen durch:
```javascript
var delBtn = document.createElement('button');
delBtn.type = 'button';
delBtn.className = 'btn-trash';
delBtn.title = 'Löschen';
delBtn.innerHTML = '<svg viewBox="0 0 24 24" width="16" height="16" stroke="currentColor" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4h6v2"/></svg>';

hd.appendChild(nameInput);
hd.appendChild(chevron);
hd.appendChild(delBtn);

body.appendChild(inhaltTa);
card.appendChild(hd);
card.appendChild(body);
```

Der `delBtn.addEventListener('click', ...)` Block (Zeile 1283-1289) bleibt unverändert — nur Position im DOM ändert sich.

Die `.crud-actions`-Klasse wird nicht mehr benötigt (div entfernt). Die CSS-Regel `.crud-card .crud-actions` kann bleiben (tut nichts, schadet nicht — kein Aufräumen nötig).

**B) Sub-Überschrift "Gesprächsphasen" in HTML einfügen**

Im HTML-Block `<!-- ehemals sec-phasen -->` (ca. Zeile 451-462):

VORHER:
```html
<!-- ehemals sec-phasen -->
<div style="margin-top:24px">
  <div class="col-header pe-col-grid-skript">
```

NACHHER:
```html
<!-- ehemals sec-phasen -->
<div style="margin-top:24px">
  <label class="fl">Gesprächsphasen <i class="tip-icon" data-tip="Definiere die Phasen deines Gesprächs — NERVE erkennt daran wo du im Gespräch stehst.">?</i></label>
  <div class="col-header pe-col-grid-skript">
```

Warum `<label class="fl">`: Konsistent zum Erlaubnisfrage- und Pitch-Label-Pattern direkt oberhalb in der gleichen Sektion (`.fg > label.fl`). Kein neues CSS nötig.
  </action>
  <verify>
1. Profil-Editor öffnen (Profil mit PROFILE_ID, also eingeloggter User).
2. Skript-Card aufklappen — Trash-Icon muss in der Header-Zeile rechts neben dem Chevron erscheinen (nicht unten im Body).
3. In der Gesprächsleitfaden-Sektion muss "Gesprächsphasen" als Label vor der Spalten-Header-Zeile (NAME / ZIEL/BESCHREIBUNG) erscheinen.
4. Alle 4 CRUD-Listen (Skripte, Opener, Erlaubnisfragen, Pitch) müssen das neue Layout zeigen (alle nutzen dieselbe crudList() Funktion).
  </verify>
  <done>
Trash-Icon sitzt in crud-hd (Header-Zeile: [Name-Input] [Chevron] [Trash]). Label "Gesprächsphasen" erscheint vor dem Phasen-col-header. Delete-Funktion noch immer korrekt per confirmDelete().
  </done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
- `.crud-body` background ist transparent (weißlich durch .crud-card) in allen 4 CRUD-Listen
- Kein `#161B22` mehr in `.crud-body`
- Textarea min-height 100px, font-size 13px
- Trash-Icon in crud-hd-Header-Zeile (nicht im Body)
- Label "Gesprächsphasen" sichtbar vor Phasen-Cards
- Keine JS-Funktions-Logik verändert (nur DOM-Struktur des renderItem)
- Keine Backend-Änderungen
</verification>

<success_criteria>
1. `.crud-body { background: transparent }` — kein #161B22
2. Alle 4 CRUD-Cards: Header = [Name] [Chevron] [Trash], Body = Textarea (100px min-height) ohne Actions-Div
3. Label "Gesprächsphasen" erscheint in Sektion 3 vor den Phasen-Cards
4. Visuell konsistent: CRUD-Cards sehen aus wie .block-Cards (gleicher weißlicher Hintergrund, gleicher Border)
</success_criteria>

<output>
Nach Abschluss: `.planning/quick/260428-e5s-polish-cycle-4-card-body-layout/260428-e5s-SUMMARY.md` erstellen.
</output>
