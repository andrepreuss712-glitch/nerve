---
type: quick
task_count: 4
files_modified:
  - database/models.py
  - routes/profiles.py
  - templates/profile_editor.html
  - routes/app_routes.py
  - static/pip-launcher.js
autonomous: true
---

<objective>
Mehrere Skripte und Opener pro Verkaufsprofil: DB-Modelle, Editor-CRUD, API-Endpoints und PiP-Launcher Step 5 Umbau.

Purpose: Vertriebler sollen pro Profil mehrere Skripte und Opener anlegen, verwalten und im Launcher vor dem Call auswaehlen koennen — statt nur einem einzigen Opener-Textfeld im Profil-JSON.

Output: Zwei neue DB-Tabellen, CRUD im Profil-Editor, API-Endpoints, erweiterter PiP-Launcher Step 5.
</objective>

<context>
@database/models.py (Profile model at line 121, utcnow helper at line 6, Base import, Column patterns)
@database/db.py (get_session pattern at line 46, Base.metadata.create_all in models.init_db)
@routes/profiles.py (Blueprint patterns, login_required, get_session + try-finally, g.org.id filtering)
@routes/app_routes.py (api_launcher_init at line 598, api_launcher_profile at line 631)
@templates/profile_editor.html (sec-leitfaden section at line 318, buildAndSubmit at line 911, init at line 972)
@static/pip-launcher.js (renderStep5 at line 420, state object, escHtml helper)
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Modelle ProfileSkript und ProfileOpener</name>
  <files>database/models.py</files>
  <action>
Fuege zwei neue SQLAlchemy-Modelle in database/models.py ein, direkt NACH der Profile-Klasse (nach Zeile 131):

```python
class ProfileSkript(Base):
    __tablename__ = 'profile_skripte'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)


class ProfileOpener(Base):
    __tablename__ = 'profile_opener'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)
```

Konventionen beachten:
- `utcnow` ist bereits als Helper definiert (Zeile 6)
- `Base` ist importiert aus database.db
- Column-Imports sind bereits vorhanden (Integer, String, Text, DateTime, ForeignKey)
- Tabellen werden automatisch bei App-Start erstellt via `init_db()` → `Base.metadata.create_all(engine_instance)` am Ende der Datei

KEINE weiteren Aenderungen noetig — create_all erstellt neue Tabellen automatisch beim naechsten Start.
  </action>
  <verify>python -c "from database.models import ProfileSkript, ProfileOpener; print('OK:', ProfileSkript.__tablename__, ProfileOpener.__tablename__)"</verify>
  <done>ProfileSkript und ProfileOpener Klassen existieren in models.py, importierbar, Tabellen werden bei App-Start erstellt.</done>
</task>

<task type="auto">
  <name>Task 2: Profil-Editor CRUD fuer Skripte und Opener</name>
  <files>routes/profiles.py, templates/profile_editor.html</files>
  <action>
**A) routes/profiles.py — 4 neue AJAX-Endpoints:**

Importiere zusaetzlich: `from database.models import ProfileSkript, ProfileOpener`

1. `GET /profiles/<int:pid>/skripte` — Liste aller Skripte eines Profils:
```python
@profiles_bp.route('/<int:pid>/skripte')
@login_required
def skripte_liste(pid):
    db = get_session()
    try:
        p = db.query(Profile).filter_by(id=pid, org_id=g.org.id).first()
        if not p:
            return jsonify({'error': 'not found'}), 404
        items = db.query(ProfileSkript).filter_by(profile_id=pid).order_by(ProfileSkript.sortierung, ProfileSkript.id).all()
        return jsonify([{'id': s.id, 'name': s.name, 'inhalt': s.inhalt or '', 'sortierung': s.sortierung} for s in items])
    finally:
        db.close()
```

2. `POST /profiles/<int:pid>/skripte` — Neues Skript anlegen (JSON body: {name, inhalt}):
```python
@profiles_bp.route('/<int:pid>/skripte', methods=['POST'])
@login_required
def skript_erstellen(pid):
    if _rolle() not in ('owner', 'admin'):
        return jsonify({'error': 'Keine Berechtigung'}), 403
    db = get_session()
    try:
        p = db.query(Profile).filter_by(id=pid, org_id=g.org.id).first()
        if not p:
            return jsonify({'error': 'not found'}), 404
        data = request.get_json(force=True)
        max_sort = db.query(ProfileSkript).filter_by(profile_id=pid).count()
        s = ProfileSkript(profile_id=pid, name=data.get('name', '').strip() or 'Neues Skript',
                          inhalt=data.get('inhalt', ''), sortierung=max_sort)
        db.add(s)
        db.commit()
        return jsonify({'id': s.id, 'name': s.name, 'inhalt': s.inhalt or '', 'sortierung': s.sortierung}), 201
    finally:
        db.close()
```

3. `PUT /profiles/<int:pid>/skripte/<int:sid>` — Skript aktualisieren:
```python
@profiles_bp.route('/<int:pid>/skripte/<int:sid>', methods=['PUT'])
@login_required
def skript_bearbeiten(pid, sid):
    if _rolle() not in ('owner', 'admin'):
        return jsonify({'error': 'Keine Berechtigung'}), 403
    db = get_session()
    try:
        p = db.query(Profile).filter_by(id=pid, org_id=g.org.id).first()
        if not p:
            return jsonify({'error': 'not found'}), 404
        s = db.query(ProfileSkript).filter_by(id=sid, profile_id=pid).first()
        if not s:
            return jsonify({'error': 'not found'}), 404
        data = request.get_json(force=True)
        if 'name' in data:
            s.name = data['name'].strip()
        if 'inhalt' in data:
            s.inhalt = data['inhalt']
        if 'sortierung' in data:
            s.sortierung = data['sortierung']
        db.commit()
        return jsonify({'id': s.id, 'name': s.name, 'inhalt': s.inhalt or '', 'sortierung': s.sortierung})
    finally:
        db.close()
```

4. `DELETE /profiles/<int:pid>/skripte/<int:sid>` — Skript loeschen:
```python
@profiles_bp.route('/<int:pid>/skripte/<int:sid>', methods=['DELETE'])
@login_required
def skript_loeschen(pid, sid):
    if _rolle() not in ('owner', 'admin'):
        return jsonify({'error': 'Keine Berechtigung'}), 403
    db = get_session()
    try:
        p = db.query(Profile).filter_by(id=pid, org_id=g.org.id).first()
        if not p:
            return jsonify({'error': 'not found'}), 404
        s = db.query(ProfileSkript).filter_by(id=sid, profile_id=pid).first()
        if s:
            db.delete(s)
            db.commit()
        return jsonify({'ok': True})
    finally:
        db.close()
```

5-8. IDENTISCHE 4 Endpoints fuer Opener (`ProfileOpener`):
- `GET /<int:pid>/opener` → `opener_liste(pid)`
- `POST /<int:pid>/opener` → `opener_erstellen(pid)`
- `PUT /<int:pid>/opener/<int:oid>` → `opener_bearbeiten(pid, oid)`
- `DELETE /<int:pid>/opener/<int:oid>` → `opener_loeschen(pid, oid)`

Exakt gleiche Struktur wie Skript-Endpoints, nur mit `ProfileOpener` statt `ProfileSkript`.

**B) templates/profile_editor.html — Gespraechsleitfaden-Section erweitern:**

Im `sec-leitfaden` Bereich (ab Zeile 318), NACH dem bestehenden Opener-Textarea und VOR dem Erlaubnis-Feld, fuege zwei neue CRUD-Listen ein:

1. **Skripte-Liste** mit Container `id="skripte-list"`:
```html
<div class="fg" style="margin-top:18px">
  <label class="fl">Skripte <i class="tip-icon" data-tip="Erstelle mehrere Gespraechsskripte fuer verschiedene Szenarien. Im Launcher waehlst du vor dem Call das passende aus.">?</i></label>
  <div id="skripte-list" style="display:flex;flex-direction:column;gap:8px"></div>
  <button type="button" class="btn-add" id="btn-add-skript" style="margin-top:8px">+ Skript hinzufuegen</button>
</div>
```

2. **Opener-Liste** mit Container `id="opener-list"`:
```html
<div class="fg" style="margin-top:18px">
  <label class="fl">Opener-Sammlung <i class="tip-icon" data-tip="Sammle verschiedene Opener-Saetze. Im Launcher waehlst du vor dem Call den passenden aus.">?</i></label>
  <div id="opener-list" style="display:flex;flex-direction:column;gap:8px"></div>
  <button type="button" class="btn-add" id="btn-add-opener" style="margin-top:8px">+ Opener hinzufuegen</button>
</div>
```

3. **JavaScript** am Ende des Script-Blocks (vor `init();`):

Variable `PROFILE_ID` aus Template: `var PROFILE_ID = {{ profile.id if profile else 'null' }};`

Helper-Funktion `crudList(containerId, endpoint, label)` die:
- Beim Laden alle Items via GET holt und rendert
- Jedes Item als Inline-Card zeigt: Name (editierbar), Inhalt (Textarea, editierbar), Loeschen-Button
- Aenderungen bei Blur via PUT speichert
- Loeschen mit `confirm('Wirklich loeschen?')` und DELETE
- "Hinzufuegen" via POST mit Default-Name

Initialisierung in `init()`:
```javascript
if (PROFILE_ID) {
  crudList('skripte-list', '/profiles/' + PROFILE_ID + '/skripte', 'Skript');
  crudList('opener-list', '/profiles/' + PROFILE_ID + '/opener', 'Opener');
}
```

Die bestehenden Felder (vi_opener, vi_erlaubnis, vi_pitch) bleiben UNVERAENDERT — sie sind Teil des Profil-JSON (Legacy). Die neuen Skripte/Opener sind separate DB-Entitaeten.

CSS fuer die Inline-Cards — innerhalb `{% block extra_style %}`:
```css
.crud-card {
  background: #F8FAFC; border: 1px solid #E2E8F0; border-radius: 8px;
  padding: 10px 14px; display: flex; flex-direction: column; gap: 6px;
}
.crud-card input { background: transparent; border: none; font-weight: 600; font-size: 13px; color: #1a1a1a; outline: none; font-family: inherit; }
.crud-card textarea { background: transparent; border: 1px solid transparent; border-radius: 4px; font-size: 12px; color: var(--page-text-secondary); resize: vertical; min-height: 48px; outline: none; padding: 4px 6px; font-family: inherit; }
.crud-card textarea:focus { border-color: #00D4AA; }
.crud-card .crud-actions { display: flex; justify-content: flex-end; }
.crud-card .crud-del { background: none; border: none; color: #EF4444; font-size: 11px; cursor: pointer; font-family: inherit; }
.crud-card .crud-del:hover { text-decoration: underline; }
```
  </action>
  <verify>
Start app briefly: python -c "from routes.profiles import profiles_bp; print('Routes OK')" && python -c "from database.models import ProfileSkript, ProfileOpener; print('Models OK')"
  </verify>
  <done>
- 8 neue AJAX-Endpoints (4 Skript, 4 Opener) in routes/profiles.py
- Profil-Editor zeigt Skripte- und Opener-Listen im Gespraechsleitfaden-Bereich
- Inline-CRUD: Hinzufuegen, Name/Inhalt bearbeiten bei Blur, Loeschen mit Confirm
- Nur owner/admin koennen Skripte/Opener erstellen/bearbeiten/loeschen
  </done>
</task>

<task type="auto">
  <name>Task 3: API-Endpoints fuer Launcher (Skripte + Opener im Init)</name>
  <files>routes/app_routes.py</files>
  <action>
In `api_launcher_init()` (Zeile 598): Erweitere die Response um Skripte und Opener des aktiven Profils.

Nach dem Block der `active_profile_daten` Ermittlung (ca. Zeile 618), fuege hinzu:

```python
# Skripte + Opener des aktiven Profils laden
from database.models import ProfileSkript, ProfileOpener
skripte = []
opener_items = []
if active_profile_id:
    skripte = [{'id': s.id, 'name': s.name, 'inhalt': s.inhalt or ''}
               for s in db.query(ProfileSkript).filter_by(profile_id=active_profile_id)
               .order_by(ProfileSkript.sortierung, ProfileSkript.id).all()]
    opener_items = [{'id': o.id, 'name': o.name, 'inhalt': o.inhalt or ''}
                    for o in db.query(ProfileOpener).filter_by(profile_id=active_profile_id)
                    .order_by(ProfileOpener.sortierung, ProfileOpener.id).all()]
```

Erweitere das return-jsonify um die zwei neuen Keys:
```python
return jsonify({
    'profiles': profiles,
    'active_profile_id': active_profile_id,
    'precall_verfuegbar': precall_verfuegbar,
    'profile_daten': active_profile_daten,
    'skripte': skripte,
    'opener': opener_items
})
```

In `api_launcher_profile(pid)` (Zeile 631): Erweitere die Response ebenfalls:

Nach der `daten` Ermittlung, fuege hinzu:
```python
from database.models import ProfileSkript, ProfileOpener
skripte = [{'id': s.id, 'name': s.name, 'inhalt': s.inhalt or ''}
           for s in db.query(ProfileSkript).filter_by(profile_id=pid)
           .order_by(ProfileSkript.sortierung, ProfileSkript.id).all()]
opener_items = [{'id': o.id, 'name': o.name, 'inhalt': o.inhalt or ''}
                for o in db.query(ProfileOpener).filter_by(profile_id=pid)
                .order_by(ProfileOpener.sortierung, ProfileOpener.id).all()]
```

Erweitere das return-jsonify:
```python
return jsonify({'id': p.id, 'name': p.name, 'daten': daten, 'skripte': skripte, 'opener': opener_items})
```
  </action>
  <verify>python -c "from routes.app_routes import app_routes_bp; print('Routes OK')"</verify>
  <done>
- /api/launcher/init gibt skripte[] und opener[] des aktiven Profils zurueck
- /api/launcher/profile/<pid> gibt skripte[] und opener[] des gewaehlten Profils zurueck
  </done>
</task>

<task type="auto">
  <name>Task 4: PiP-Launcher Step 5 Umbau — Skript- und Opener-Dropdowns</name>
  <files>static/pip-launcher.js</files>
  <action>
**A) State erweitern** — Im state-Objekt (am Anfang der IIFE) zwei neue Properties:
```javascript
skripte: [],
openerItems: [],
selectedSkriptId: null,
selectedOpenerId: null,
```

**B) Init-Fetch anpassen** — Wo `/api/launcher/init` Daten zugewiesen werden, erweitere:
```javascript
state.skripte = data.skripte || [];
state.openerItems = data.opener || [];
```

**C) renderStep5() komplett ersetzen** (ab Zeile 420):

Neuer Aufbau:
1. Profil-Dropdown (wie bisher)
2. Skript-Dropdown (aus `state.skripte`) + Skript-Vorschau (aus `inhalt` des gewaehlten Skripts)
3. Opener-Dropdown (aus `state.openerItems`) + Opener-Vorschau (aus `inhalt` des gewaehlten Openers)
4. Buttons: Zurueck, Ueberspringen, Call starten

```javascript
function renderStep5() {
  var c = content();
  if (!c) return;

  var profileOptions = state.profiles.map(function (p) {
    var sel = p.id === state.activeProfileId ? ' selected' : '';
    return '<option value="' + p.id + '"' + sel + '>' + escHtml(p.name) + '</option>';
  }).join('');

  // Skript-Dropdown
  var skriptOptions = '<option value="">-- Kein Skript --</option>' + state.skripte.map(function (s) {
    var sel = s.id === state.selectedSkriptId ? ' selected' : '';
    return '<option value="' + s.id + '"' + sel + '>' + escHtml(s.name) + '</option>';
  }).join('');

  // Opener-Dropdown
  var openerOptions = '<option value="">-- Kein Opener --</option>' + state.openerItems.map(function (o) {
    var sel = o.id === state.selectedOpenerId ? ' selected' : '';
    return '<option value="' + o.id + '"' + sel + '>' + escHtml(o.name) + '</option>';
  }).join('');

  // Vorschauen
  var skriptPreview = '';
  if (state.selectedSkriptId) {
    var sk = state.skripte.find(function (s) { return s.id === state.selectedSkriptId; });
    if (sk) skriptPreview = escHtml(sk.inhalt);
  }
  var openerPreview = '';
  if (state.selectedOpenerId) {
    var op = state.openerItems.find(function (o) { return o.id === state.selectedOpenerId; });
    if (op) openerPreview = escHtml(op.inhalt);
  }
  // Fallback: alten Opener aus Profil-JSON verwenden wenn keine Opener-Items existieren
  var legacyOpener = (state.profileDaten && state.profileDaten.opener) ? state.profileDaten.opener : '';

  c.innerHTML = [
    '<div class="launcher-step">',
    '<div class="nav-live-title">Skript & Opener waehlen</div>',

    // Profil
    state.profiles.length > 0
      ? '<label style="font-size:11px;color:var(--page-text-muted);margin-bottom:2px;display:block">Profil</label><select class="launcher-select" id="lnr-profile-select">' + profileOptions + '</select>'
      : '<div style="color:var(--page-text-muted);font-size:13px">Noch kein Profil angelegt. <a href="/profiles" style="color:#00D4AA">Profil erstellen</a></div>',

    // Skript
    state.skripte.length > 0
      ? '<label style="font-size:11px;color:var(--page-text-muted);margin-top:8px;margin-bottom:2px;display:block">Skript</label><select class="launcher-select" id="lnr-skript-select">' + skriptOptions + '</select>'
      : '',
    skriptPreview
      ? '<div class="launcher-opener-preview" id="lnr-skript-preview" style="white-space:pre-wrap;max-height:80px;overflow-y:auto">' + skriptPreview + '</div>'
      : (state.skripte.length > 0 ? '<div class="launcher-opener-preview" id="lnr-skript-preview" style="color:var(--page-text-muted);font-style:italic">Skript auswaehlen fuer Vorschau</div>' : ''),

    // Opener
    state.openerItems.length > 0
      ? '<label style="font-size:11px;color:var(--page-text-muted);margin-top:8px;margin-bottom:2px;display:block">Opener</label><select class="launcher-select" id="lnr-opener-select">' + openerOptions + '</select>'
      : '',
    openerPreview
      ? '<div class="launcher-opener-preview" id="lnr-opener-preview" style="white-space:pre-wrap">' + openerPreview + '</div>'
      : (state.openerItems.length > 0
          ? '<div class="launcher-opener-preview" id="lnr-opener-preview" style="color:var(--page-text-muted);font-style:italic">Opener auswaehlen fuer Vorschau</div>'
          : (legacyOpener
              ? '<div style="font-size:12px;color:var(--page-text-muted);margin-top:6px;margin-bottom:2px">Opener (aus Profil):</div><div class="launcher-opener-preview" id="lnr-opener-preview">' + escHtml(legacyOpener) + '</div>'
              : '<div class="launcher-opener-preview" id="lnr-opener-preview" style="color:var(--page-text-muted);font-style:italic">Kein Opener hinterlegt</div>')),

    '<div class="launcher-actions">',
    '<button class="launcher-btn-ghost" id="lnr-step5-back">&#8592; Zurueck</button>',
    '<button class="launcher-btn-ghost" id="lnr-step5-skip">Ueberspringen</button>',
    '<button class="launcher-btn-primary" id="lnr-step5-start">Call starten &#9654;</button>',
    '</div>',
    '</div>'
  ].join('');

  // Profile change → reload Skripte + Opener
  var profileSel = document.getElementById('lnr-profile-select');
  if (profileSel) {
    profileSel.onchange = function () {
      var pid = parseInt(profileSel.value);
      if (!pid) return;
      fetch('/api/launcher/profile/' + pid)
        .then(function (r) { return r.json(); })
        .then(function (data) {
          state.activeProfileId = data.id;
          state.profileDaten = data.daten || {};
          state.skripte = data.skripte || [];
          state.openerItems = data.opener || [];
          state.selectedSkriptId = null;
          state.selectedOpenerId = null;
          renderStep5();  // Re-render with new data
        })
        .catch(function () {});
    };
  }

  // Skript change → update preview
  var skriptSel = document.getElementById('lnr-skript-select');
  if (skriptSel) {
    skriptSel.onchange = function () {
      state.selectedSkriptId = parseInt(skriptSel.value) || null;
      var preview = document.getElementById('lnr-skript-preview');
      if (preview) {
        var sk = state.skripte.find(function (s) { return s.id === state.selectedSkriptId; });
        preview.textContent = sk ? sk.inhalt : 'Skript auswaehlen fuer Vorschau';
        preview.style.fontStyle = sk ? 'normal' : 'italic';
        preview.style.color = sk ? '' : 'var(--page-text-muted)';
      }
    };
  }

  // Opener change → update preview
  var openerSel = document.getElementById('lnr-opener-select');
  if (openerSel) {
    openerSel.onchange = function () {
      state.selectedOpenerId = parseInt(openerSel.value) || null;
      var preview = document.getElementById('lnr-opener-preview');
      if (preview) {
        var op = state.openerItems.find(function (o) { return o.id === state.selectedOpenerId; });
        preview.textContent = op ? op.inhalt : 'Opener auswaehlen fuer Vorschau';
        preview.style.fontStyle = op ? 'normal' : 'italic';
        preview.style.color = op ? '' : 'var(--page-text-muted)';
      }
    };
  }

  // Navigation
  document.getElementById('lnr-step5-back').onclick = function () {
    state.step = state.precallVerfuegbar ? 2 : 1;
    renderStep();
  };
  document.getElementById('lnr-step5-skip').onclick = function () {
    startCall(false);
  };
  document.getElementById('lnr-step5-start').onclick = function () {
    var s = document.getElementById('lnr-profile-select');
    if (s && s.value) state.activeProfileId = parseInt(s.value);
    startCall(true);
  };
}
```

**D) Profilwechsel-Response in Init-Fetch:** Wo die `/api/launcher/init` Response verarbeitet wird, stelle sicher dass `state.skripte` und `state.openerItems` gesetzt werden (siehe oben B).

WICHTIG: Legacy-Opener aus `state.profileDaten.opener` als Fallback behalten, wenn keine Opener-Items existieren. So funktionieren bestehende Profile weiterhin.
  </action>
  <verify>Manuell: App starten, PiP-Launcher oeffnen, Step 5 zeigt Profil → Skript → Opener Dropdowns. Profilwechsel laedt neue Skripte/Opener.</verify>
  <done>
- Step 5 zeigt: Profil-Dropdown → Skript-Dropdown + Vorschau → Opener-Dropdown + Vorschau → Buttons
- Profilwechsel laedt Skripte und Opener des neuen Profils dynamisch nach
- Skript/Opener-Wechsel aktualisiert die jeweilige Vorschau
- Legacy-Opener aus Profil-JSON wird als Fallback angezeigt wenn keine Opener-Items existieren
- Ausgewaehltes Profil wird beim Call-Start gesetzt (bestehendes Verhalten)
  </done>
</task>

</tasks>

<verification>
1. App starten: `python app.py` — Tabellen profile_skripte und profile_opener werden automatisch erstellt
2. Profil-Editor oeffnen: Gespraechsleitfaden-Section zeigt Skripte- und Opener-Listen (leer bei bestehenden Profilen)
3. Skript/Opener hinzufuegen, bearbeiten, loeschen im Editor
4. PiP-Launcher Step 5: Profil waehlen → Skript-Dropdown erscheint → Opener-Dropdown erscheint
5. Profilwechsel im Launcher: Skripte und Opener werden dynamisch nachgeladen
</verification>

<success_criteria>
- Zwei neue Tabellen in der DB (profile_skripte, profile_opener)
- Profil-Editor CRUD fuer beide Entitaeten funktioniert
- API-Endpoints liefern Skripte + Opener im Launcher-Init und Profilwechsel
- PiP-Launcher Step 5 zeigt Skript- und Opener-Auswahl mit Vorschau
- Legacy-Opener funktioniert weiterhin als Fallback
</success_criteria>
