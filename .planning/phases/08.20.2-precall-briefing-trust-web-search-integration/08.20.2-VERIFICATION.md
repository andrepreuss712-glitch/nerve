---
phase: 08.20.2-precall-briefing-trust-web-search-integration
verified: 2026-04-30T15:10:00Z
status: human_needed
score: 14/14 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open the pip-launcher modal, navigate to PreCall step, submit a real company name (e.g. SAP SE)"
    expected: "Step 4 shows 3 distinct sections: (A) 2x2 Pflichtfeld-Karte with confidence icons top, (B) Fliesstext middle, (C) Empfehlungen bottom. not_found fields appear dimmed with 'Nicht gefunden', never hidden."
    why_human: "CSS rendering, visual layout order, and confidence icon appearance cannot be verified programmatically without a browser context"
  - test: "Complete a call that started with PreCall research; check the ConversationLog row in DB"
    expected: "ConversationLog.precall_fields contains valid JSON with geschaeftsfuehrer/branche/mitarbeiterzahl/hauptprodukt keys; ConversationLog.precall_briefing still contains Schicht-2 text (no regression)"
    why_human: "End-to-end session flow (api_beenden receiving precall_fields from the JS frontend) requires a real browser session — not testable headless"
  - test: "When briefingText is empty but fields are present, verify the 'Analyse anpassen' button does NOT appear"
    expected: "Button absent; grid still shows"
    why_human: "Requires conditional DOM rendering verification in a live browser"
---

# Phase 08.20.2: PreCall Briefing Trust + Web-Search Integration — Verification Report

**Phase Goal:** Eliminate hallucinations in PreCall briefings through structured Schicht-1 fields with per-field confidence + source URL, hardened Schicht-2 narrative, and a separate Schicht-3 recommendations call via build_profile_context().
**Verified:** 2026-04-30T15:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `_generiere_briefing()` returns a dict with 'fields' key containing all 4 Pflichtfelder | VERIFIED | Line 394-400 precall_service.py: returns {fields, text, firmenname, ansprechpartner, quellen_count}; REQUIRED_FIELDS enforced at lines 366-368 |
| 2 | Each field has EXACTLY value/source_url/confidence keys — extra keys stripped | VERIFIED | Lines 385-386: dict comprehension `{k: v for k, v in fields[key].items() if k in ('value', 'source_url', 'confidence')}`; 7/7 schema contract tests PASS |
| 3 | confidence is always one of: high, medium, not_found | VERIFIED | Line 378: whitelist check `val.get('confidence') not in ('high', 'medium', 'not_found')` resets to NOT_FOUND_FIELD |
| 4 | not_found fields have value=='not_found' and source_url==None | VERIFIED | Lines 382-384: explicit enforcement block; test_schicht1_not_found_enforcement PASS |
| 5 | Optional fields (standorte, gruendungsjahr, usp_positionierung, aktuelle_news) only present when confidence != not_found | VERIFIED | Lines 388-392: optional_keys loop deletes not_found optional fields; test_schicht1_optional_field_not_found_excluded PASS |
| 6 | `_generiere_empfehlungen()` is a separate function that calls build_profile_context() | VERIFIED | Lines 407-490: standalone function; line 423: `from services.prompt_pipeline import build_profile_context` |
| 7 | `_generiere_empfehlungen()` logs observable message when sid=None | VERIFIED | Line 419: `print(f"[PreCall] Empfehlungen: sid=None, Section 8 nicht im Kontext (non-fatal)")` |
| 8 | `recherche_firma()` returns dict with keys: fields, text, empfehlungen, firmenname, ansprechpartner, quellen_count | VERIFIED | Lines 214-218: briefing['empfehlungen'] set; cache stores full 6-key dict; return (briefing, None) |
| 9 | `set_briefing_for_sid()` receives schicht1_summary + newline + newline + text (NOT empfehlungen) | VERIFIED | Lines 201-208: schicht1_summary built from fields, `_sid_text = schicht1_summary + "\n\n" + briefing.get('text', '')` |
| 10 | Cache key uses firmenname_lower + '_' + str(profile_id) | VERIFIED | Line 176: `cache_key = f"{firmenname.strip().lower()}_{profile_id}"` |
| 11 | PRECALL_FIELDS_SYSTEM_PROMPT contains: 3 confidence levels, Primaer/Sekundaer/Ausgeschlossen source hierarchy, 36-month freshness rule, few-shot JSON example, Schicht-2 hardening rules | VERIFIED | Lines 29-150: full prompt present with all required elements |
| 12 | ConversationLog has precall_fields TEXT nullable column; app.py has migration block | VERIFIED | models.py line 302; app.py lines 394-398 migration block present |
| 13 | api_precall_research passes user_id, profile_id, sid to recherche_firma(); api_beenden saves precall_fields JSON to ConversationLog | VERIFIED | app_routes.py lines 954-963 (research route); lines 58-65 + 253-254 (beenden route) |
| 14 | tests/test_precall_schema.py exists with 7 mock-based tests, all passing | VERIFIED | `python -m pytest tests/test_precall_schema.py -v` → 7 passed in 0.78s |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `services/precall_service.py` | Rebuilt 3-layer precall service | VERIFIED | 496 lines; PRECALL_FIELDS_SYSTEM_PROMPT, _generiere_briefing, _generiere_empfehlungen, recherche_firma all present and substantive |
| `database/models.py` | precall_fields column on ConversationLog | VERIFIED | Line 302: `precall_fields = Column(Text, nullable=True)` |
| `app.py` | DB migration for precall_fields | VERIFIED | Lines 394-398: Phase 08.20.2 migration block with silent-fail pattern |
| `routes/app_routes.py` | Updated api_precall_research and api_beenden | VERIFIED | Both routes updated with all required wiring |
| `templates/base.html` | CSS for 3-layer PreCall modal | VERIFIED | Lines 354-367: .precall-fields-grid, .confidence-high, .confidence-medium, .confidence-not-found, @media rule present |
| `static/pip-launcher.js` | renderStep4() rewritten for 3 sections | VERIFIED | Lines 375-460: renderStep4 contains gridHtml (A) + textHtml (B) + empfHtml (C) in DOM order; CONFIDENCE_ICONS and REQUIRED_KEYS present |
| `tests/test_precall_schema.py` | Schema contract tests for Schicht-1 output | VERIFIED | 7 tests, all GREEN |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `recherche_firma()` | `_generiere_briefing()` | first Claude call (Schicht-1+2) | WIRED | Line 192: `briefing = _generiere_briefing(...)` |
| `recherche_firma()` | `_generiere_empfehlungen()` | second Claude call AFTER set_briefing_for_sid() | WIRED | Lines 204-214: set_briefing_for_sid called first (if sid), then _generiere_empfehlungen called |
| `_generiere_empfehlungen()` | `build_profile_context()` | import from services.prompt_pipeline | WIRED | Line 422-423: `from services.prompt_pipeline import build_profile_context` |
| `api_precall_research` | `recherche_firma()` | passes user_id, profile_id, sid | WIRED | Lines 954-963: all three params passed |
| `api_beenden` | `ConversationLog` | saves precall_fields=json.dumps(briefing['fields']) | WIRED | Lines 58-65 (extraction + serialization) + line 254 (constructor arg) |
| `renderStep4()` | `state.precallBriefing.fields` | renders Schicht-1 fields dict as 2x2 grid | WIRED | Lines 377, 404-405: fields extracted, REQUIRED_KEYS.map(buildFieldCell) |
| `renderStep4()` | `state.precallBriefing.empfehlungen` | renders Schicht-3 empfehlungen string | WIRED | Lines 379, 417-420: empfehlungen extracted and rendered via mdToHtml |
| `lnr-step4-accept onclick` | `state.precallBriefing` | saves briefing object including fields for api_beenden | WIRED | pip-launcher.js line 2008: `precall_fields: (state.precallBriefing && state.precallBriefing.fields) ? state.precallBriefing.fields : null` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `renderStep4()` in pip-launcher.js | `state.precallBriefing.fields` | `/api/precall/research` JSON response → `data.briefing` → `state.precallBriefing` (set in step 3 handler) | Yes — briefing dict from Claude API call via recherche_firma() | FLOWING |
| `renderStep4()` in pip-launcher.js | `state.precallBriefing.empfehlungen` | Same API response `briefing.empfehlungen` key (set by _generiere_empfehlungen()) | Yes — second Claude call result | FLOWING |
| `ConversationLog.precall_fields` | `precall_fields_json` | `req_data.get('precall_fields')` from api_beenden POST body (JS sends `state.precallBriefing.fields`) | Yes — real dict from research session | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| _generiere_briefing() schema contract | `python -m pytest tests/test_precall_schema.py -v` | 7 passed in 0.78s | PASS |
| precall_fields column on ConversationLog model | `python -c "from database.models import ConversationLog; c = ConversationLog.__table__.c.precall_fields; print(c.nullable)"` | True | PASS |
| PRECALL_FIELDS_SYSTEM_PROMPT content check | Grep confirmed: `high`, `medium`, `not_found`, `Impressum`, `Handelsregister`, `Bundesanzeiger`, `Reddit`, `Kununu`, `Glassdoor`, `nicht verifiziert` all present | All present | PASS |

### Requirements Coverage

The phase PLANs reference REQ-1 through REQ-9 which are phase-internal requirements defined in the 08.20.2-SPEC.md (not tracked in REQUIREMENTS.md — REQUIREMENTS.md uses a different ID system for v1 milestone requirements). These phase-internal IDs map to SPEC.md requirements 1-9:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| REQ-1 | Plan 01, 04 | Schicht-1-Output-Schema: fields dict with per-field {value, source_url, confidence} | SATISFIED | precall_service.py lines 394-400; 7/7 schema tests pass |
| REQ-2 | Plan 01, 04 | Pflichtfelder hard-required: geschaeftsfuehrer, branche, mitarbeiterzahl, hauptprodukt always present | SATISFIED | Lines 365-368: REQUIRED_FIELDS enforced |
| REQ-3 | Plan 01, 04 | Optionale Felder only when confidence != not_found | SATISFIED | Lines 388-392: not_found optional fields deleted |
| REQ-4 | Plan 01, 04 | Confidence-Algorithmus in system prompt with 3 levels + source types | SATISFIED | PRECALL_FIELDS_SYSTEM_PROMPT lines 63-79 |
| REQ-5 | Plan 01 | Schicht-2-Haertung: anti-hallucination rules in system prompt | SATISFIED | Lines 108-119 of PRECALL_FIELDS_SYSTEM_PROMPT |
| REQ-6 | Plan 01 | Schicht-3 as separate _generiere_empfehlungen() function | SATISFIED | Lines 407-490; called after set_briefing_for_sid |
| REQ-7 | Plan 02 | briefing_dict-Erweiterung: recherche_firma() returns 6-key dict; api_precall_research passes user_id/profile_id/sid | SATISFIED | Lines 153-220 in precall_service.py; lines 954-963 in app_routes.py |
| REQ-8-db | Plan 02 | DB-Persistenz: precall_fields TEXT column + migration + api_beenden wiring | SATISFIED | models.py line 302; app.py lines 394-398; app_routes.py lines 58-65, 254 |
| REQ-9 | Plan 03 | UI 3-Layer-Anzeige: Pflichtfeld-Karte (A) + Fliesstext (B) + Empfehlungen (C) | SATISFIED (automated checks); NEEDS HUMAN for visual rendering | CSS confirmed in base.html; JS logic confirmed in pip-launcher.js; visual rendering needs browser verification |

Note: REQ-1 through REQ-9 are phase-internal requirements from 08.20.2-SPEC.md. No REQUIREMENTS.md orphans — these IDs do not appear in the milestone REQUIREMENTS.md file and are not expected to.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| services/precall_service.py | 478 | Cost tracking logs output_tokens twice (both calls use `per_1k_output_tokens`; input tokens use `per_1k_output_tokens` instead of `per_1k_input_tokens` on second log call for `_generiere_empfehlungen`) | Warning | Minor cost tracking inaccuracy for empfehlungen input tokens — does not affect briefing quality or goal |

No STUB patterns, no TODO/FIXME blockers, no hardcoded empty data paths found in any phase files.

### Human Verification Required

#### 1. 3-Section Visual Layout

**Test:** Open the pip-launcher modal on a live session, navigate to PreCall mode, submit a real company name (e.g. "SAP SE") and wait for the result.
**Expected:** Step 4 shows (A) Pflichtfeld-Karte as 2x2 grid at top with confidence icons (green ✓ for high, yellow ~ for medium, grey — for not_found); (B) Fliesstext block below the grid; (C) Gesprächs-Empfehlungen block at bottom. not_found fields appear dimmed with "Nicht gefunden" text, never hidden.
**Why human:** CSS grid rendering, visual icon colors, and relative DOM section ordering cannot be verified headless.

#### 2. End-to-End DB Persistence via api_beenden

**Test:** Complete a full session: start a call that begins with PreCall research, then click "Beenden". Query the resulting ConversationLog row.
**Expected:** `ConversationLog.precall_fields` contains valid JSON; `json.loads(conv.precall_fields)` yields dict with all 4 Pflichtfelder keys. `ConversationLog.precall_briefing` still holds Schicht-2 text (no regression).
**Why human:** The precall_fields payload is sent from the JS frontend during api_beenden — this path requires a real browser session where state.precallBriefing.fields was populated by a prior PreCall API call.

#### 3. Edit Button Conditional Rendering

**Test:** Trigger a PreCall response where fields are returned but briefing.text is empty (can be simulated by editing the response mock or finding a company with very little web presence).
**Expected:** "Analyse anpassen" button does NOT appear; Pflichtfeld-Karte still renders.
**Why human:** The `(hasAnyData && briefingText)` conditional requires a browser to verify the button is absent when only fields are present.

### Gaps Summary

No blocking gaps found. All 14 must-have truths are verified in the codebase. The minor cost-tracking issue (REQ-6 _generiere_empfehlungen logs output_tokens for both input and output) is a non-blocking warning that does not affect the phase goal.

The 3 human verification items are standard visual/E2E checks that cannot be automated headlessly. The automated evidence strongly indicates correct wiring for all three.

---

_Verified: 2026-04-30T15:10:00Z_
_Verifier: Claude (gsd-verifier)_
