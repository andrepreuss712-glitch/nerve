---
phase: 08.23.2.AUTH-2
plan: "04"
subsystem: onboarding
tags: [routing, onboarding, auth, erstprofil, weiche]
dependency_graph:
  requires: [08.23.2.AUTH-2-03]
  provides: [post_login_destination, onboarding_erstprofil_route, erstprofil_template]
  affects: [routes/auth.py (Plan 05 docks), routes/oauth.py (Plan 05 docks)]
tech_stack:
  added: [services/onboarding_routing.py]
  patterns: [nummerierte-Stufenliste, Rollen-Gate, single-commit-create-activate]
key_files:
  created:
    - services/onboarding_routing.py
    - templates/onboarding_erstprofil.html
    - tests/test_post_login_destination.py
  modified:
    - routes/onboarding.py
    - routes/dashboard.py
decisions:
  - "Finding 2 (D-09): NOT IN ('done','skipped') statt == 'pending' — _DONE_STATES Konstante"
  - "Finding 3: Rollen-Gate (owner/admin) auf POST Submit; Skip-Handler offen fuer alle Rollen"
  - "D-14: Profil-Anlage + active_profile_id + onboarding_state='done' im SELBEN db.commit()"
  - "D-13: EXPLIZITES BRANCHE_ENUM_MAP (8 Keys, Recruiting/Agentur->sonstiges dokumentiert)"
  - "D-04: skip_onboarding != skip_billing — Trennung verbatim als AUTH-3-Slot-Kommentar"
  - "D-20: toter dashboard.py Gate-Rest (profiles.wizard_page) ersatzlos geloescht"
  - "Finding 2 step_1-Test: In-Memory-Attribut-Override statt DB-Persist (CHECK wuerde verletzen)"
metrics:
  duration: "~35 Minuten"
  completed: "2026-07-08"
  tasks_completed: 3
  files_changed: 5
---

# Phase 08.23.2.AUTH-2 Plan 04: Onboarding-Router + Erstprofil-Seite Summary

**One-liner:** Post-Login-Routing-Weiche (4-Stufen, NOT-IN-done/skipped) + Erstprofil-Minimum-Seite (Branchen-Wahl + 1 Feld, Rollen-Gate, single-commit create+activate).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 (TDD RED) | Routing-Matrix-Waechter test_post_login_destination.py | f27f43a | tests/test_post_login_destination.py (neu) |
| 2 | services/onboarding_routing.py — post_login_destination | 1ef540f | services/onboarding_routing.py (neu) |
| 3 | Erstprofil-Seite + stubs ersetzen + dashboard dead-code | 1af1e0f | routes/onboarding.py, templates/onboarding_erstprofil.html, routes/dashboard.py |

## Rot-Beleg (TDD ERST-ROT)

Task 1 (Commit f27f43a) wurde eingespielt BEVOR services/onboarding_routing.py existierte.
Gegen HEAD haette `from services.onboarding_routing import post_login_destination` einen
ImportError geworfen -> deploy.sh-Gate haette den Restart geblockt (ROT). Task 2 (Commit
1ef540f) legt das Modul an und macht den Test gruen.

## Static Acceptance (deploy-gate deferred to Andre)

Da CLAUDE.md HART kein Local-Dev erlaubt, wurde die Akzeptanz statisch per Grep verifiziert
und das deploy.sh-Gate (pytest auf nerve_test) ist Andres naechster Schritt.

### Finding 2 — NOT IN Verifikation (D-09 Tueroefffner)

```
services/onboarding_routing.py:
  18: _DONE_STATES = ('done', 'skipped')
  53: if state not in _DONE_STATES and rolle in ('owner', 'admin'):
```
Beweist: Weiche liest NOT IN _DONE_STATES, NICHT == 'pending'. step_1-Test (Fall 8) prueft
per In-Memory-Override (kein DB-Persist, da CHECK-Constraint nur pending/done/skipped erlaubt;
Override simuliert zukuenftigen step_*-State nach CHECK-Erweiterung, D-09 Tueroeffner-Design).

### Finding 3 — Rollen-Gate auf Submit (T-AUTH2-06)

```
routes/onboarding.py:
  195: if g.user.rolle not in ('owner', 'admin'):
  196:     flash('Keine Berechtigung.', 'error')
  197:     return redirect(url_for('dashboard.index'))
```
Rollen-Gate steht VOR jeder Profil-Anlage. Skip-Handler (/onboarding/skip) hat KEIN Gate.

### BRANCHE_ENUM_MAP — 8 Keys explizit (D-13)

```
routes/onboarding.py:
  BRANCHE_ENUM_MAP = {
      'SaaS':         'saas_b2b',
      'Versicherung': 'versicherung',
      'Consulting':   'beratung',
      'Immobilien':   'immobilien',
      'Industrie':    'maschinenbau',
      'Sonstiges':    'sonstiges',
      'Recruiting':   'sonstiges',   # dokumentiert: kein eigener Whitelist-Wert
      'Agentur':      'sonstiges',   # dokumentiert: kein eigener Whitelist-Wert
  }
```
KEIN _normalize_branche()-Fallback verwendet (grep bestaetigt).

### AUTH-3-Slot verbatim (D-03/D-04)

```
services/onboarding_routing.py:
  40: „kein Abo = Owner UND nicht skip_billing UND Status nicht in (active, past_due)
  41:  → Preisseite; past_due → Portal."
  43: skip_onboarding ueberspringt NUR das Onboarding, NICHT die Kasse.
  44: Das Geld laeuft ueber ein EIGENES organisations.skip_billing pro Firma
```

### D-14 Single-Commit (kein Training-404-Trap)

```
routes/onboarding.py:
  236: u.active_profile_id = p.id
  237: u.onboarding_state = 'done'
  238: db.commit()   # SELBER Commit wie flush() -> active_profile_id sofort gesetzt
```

### Dashboard Dead-Code geloescht (D-20)

```
grep -n "wizard_page|Wizard redirect disabled" routes/dashboard.py -> 0 Treffer
```
Toter Gate-Rest (zeigt auf nicht-existente profiles.wizard_page, Logik invertiert) ist
ersatzlos geloescht.

### url_for-Verifikation

- url_for('onboarding.wizard'): onboarding_bp = Blueprint('onboarding', ...) VERIFIZIERT
  (routes/CLAUDE.md Blueprint-Uebersicht + grep -n "def wizard" routes/onboarding.py -> Z.186)
- url_for('dashboard.index'): dashboard_bp = Blueprint('dashboard', ...) VERIFIZIERT
- url_for('onboarding.skip'): def skip() @ /skip VERIFIZIERT (routes/onboarding.py Z.259)

### Scope-Check: Genau 5 Dateien

```
git diff --name-only HEAD~3 HEAD:
  routes/dashboard.py
  routes/onboarding.py
  services/onboarding_routing.py
  templates/onboarding_erstprofil.html
  tests/test_post_login_destination.py
```
Keine anderen Dateien beruehrt. auth.py, oauth.py, models.py, migrations unveraendert.

## Deviations from Plan

### Auto-fixed Issues

Keine Deviations — Plan exakt wie geschrieben implementiert.

**Routing-Test Fall 8 (step_1):** Der Plan liess dem Executor die Wahl ob der step_1-State
per direktem DB-UPDATE oder per In-Memory-Override getestet wird. Gewaehlt wurde In-Memory-
Attribut-Override (kein DB-Persist), weil der CHECK-Constraint (migration 0032) nur
pending/done/skipped erlaubt und ein CHECK-verletzender DB-INSERT den Test selbst beschaedigen
wuerde. Der Override ist funktional aequivalent zum zukuenftigen step_*-State nach
CHECK-Erweiterung (D-09 Tueroeffner-Design) — dokumentiert via Kommentar im Test.

## Known Stubs

Keine. Die Weiche (post_login_destination) ist vollstaendig implementiert und gibt echte URLs
zurueck. Die Erstprofil-Seite legt echte Profile an. Der Skip-Handler setzt echten State.
KEIN hardcoded leeres Array/Objekt/None das zum UI fliesst und das Plan-Ziel verhindert.

Der einzige bewusste Leer-Slot ist die AUTH-3-Billing-Stufe 2 (Kommentar im Code) — das ist
intentional (kein Billing in AUTH-2), durch den verbatim Slot-Vertrag fuer AUTH-3 gesichert.

## Threat Surface Scan

Keine neuen Netzwerk-Endpunkte ausserhalb des Scope. Neue Routen:
- POST /onboarding/ (Submit) — bereits im Plan-Threat-Modell (T-AUTH2-06 Rollen-Gate)
- POST /onboarding/skip — offen fuer alle eingeloggten User (by design, D-12); CSRF-Token
  in Form vorhanden (T-AUTH2-08)
- services/onboarding_routing.py — kein Netzwerk-Endpunkt, reine Python-Funktion

Kein neuer Trust-Boundary-Crossing ausserhalb des Plan-Threat-Modells.

## ★ NEXT GATE

Andre fuehrt `bash deploy.sh production` aus (Wave-4 deploy gate):
- nerve_test-pytest muss gruen sein: test_post_login_destination.py (alle 8 Matrix-Faelle
  incl. Selbstheilung + Finding-2-step_1 + Finding-3-member-submit-gate)
- Routing-Matrix-Waechter gruen = Wave-4 abgenommen
- Weiche ist NOCH NICHT an Login-Pfade angedockt (Plan 05) -> Login-Verhalten unveraendert
- DO NOT auto-advance to Plan 05 (Andre entscheidet nach deploy.sh-Ergebnis)

---

## Self-Check

### Created files exist

- C:\Users\andre\dev\salesnerve\services\onboarding_routing.py — CREATED (Commit 1ef540f)
- C:\Users\andre\dev\salesnerve\templates\onboarding_erstprofil.html — CREATED (Commit 1af1e0f)
- C:\Users\andre\dev\salesnerve\tests\test_post_login_destination.py — CREATED (Commit f27f43a)

### Modified files

- C:\Users\andre\dev\salesnerve\routes\onboarding.py — MODIFIED (Commit 1af1e0f)
- C:\Users\andre\dev\salesnerve\routes\dashboard.py — MODIFIED (Commit 1af1e0f)

### Commits exist

- f27f43a — test(08.23.2.AUTH-2-04): add failing routing-matrix test (ERST-ROT)
- 1ef540f — feat(08.23.2.AUTH-2-04): add services/onboarding_routing.py
- 1af1e0f — feat(08.23.2.AUTH-2-04): Erstprofil-Seite + onboarding stubs ersetzen + dashboard dead-code loeschen

## Self-Check: PASSED
