---
phase: 08.23.2.AUTH-2
plan: 04
type: execute
wave: 4
complexity: "🟡 mittel (Auth-Weiche + neue UI-Seite + AUTH-3-Vertrags-Naht — Cross-AI Pflicht VOR Execute)"
depends_on: ["08.23.2.AUTH-2-03"]
files_modified:
  - services/onboarding_routing.py
  - routes/onboarding.py
  - templates/onboarding_erstprofil.html
  - routes/dashboard.py
  - tests/test_post_login_destination.py
autonomous: true
requirements: []   # Scope = CONTEXT-Decisions D-01/D-02/D-03/D-04/D-09/D-11/D-12/D-13/D-14/D-15/D-18/D-20
user_setup: []

must_haves:
  truths:
    - "post_login_destination(user) gibt fuer onboarding_state NOT IN ('done','skipped') + Owner/Admin + 0 Firmen-Profile die Erstprofil-URL zurueck; sonst (done/skipped/member/skip_onboarding/profile-vorhanden) None (D-09 Tueroeffner: Finding 2 — nicht state=='pending')"
    - "Bei NOT-done/skipped + bereits vorhandenen Profilen heilt der Router selbst auf onboarding_state='done' (kein Redirect)"
    - "skip_onboarding=True liefert IMMER None (Stufe 1) — Onboarding uebersprungen, Billing unberuehrt (Vertrags-Trennung D-04)"
    - "Ein Member wird NIE zur Erstprofil-Seite umgeleitet (None), auch bei pending + 0 Profilen"
    - "Die Erstprofil-Seite (/onboarding/) legt aus Branchen-Wahl + 1 Produktfeld ein lauffaehiges Profil an, setzt es aktiv (SELBER Commit) und flippt onboarding_state='done'"
    - "★ Der Submit-Handler traegt das gleiche g.user.rolle in ('owner','admin')-Gate wie profiles.py:63 — ein Member kann per POST kein Profil anlegen (403/Redirect); Skip bleibt offen (Finding 3)"
    - "Skip auf der Erstprofil-Seite setzt onboarding_state='skipped' und leitet ins Dashboard mit Pflicht-Banner 'Noch kein Profil'"
    - "Der tote Gate-Rest dashboard.py (auskommentiert, zeigt auf nicht-existente profiles.wizard_page) ist geloescht"
    - "Der AUTH-3-Leer-Slot (Stufe 2) traegt den verbatim Vertragstext (kein Abo/skip_billing/Preisseite/Portal + skip_onboarding≠skip_billing-Trennung)"
  artifacts:
    - path: "services/onboarding_routing.py"
      provides: "post_login_destination(user) als nummerierte Stufenliste (Stufe 1 skip_onboarding / Stufe 2 AUTH-3-Leer-Slot / Stufe 3 Onboarding NOT-IN-done/skipped+Selbstheilung / Stufe 4 default); importierbar von auth.py UND oauth.py"
      exports: ["post_login_destination"]
      min_lines: 40
    - path: "routes/onboarding.py"
      provides: "Erstprofil-GET (/) + POST-Submit (Rollen-Gate+Profil-Anlage+Aktiv+state=done) + POST-Skip (state=skipped); ersetzt die 3 Stubs; EXPLIZITE Branche-Enum-Mapping-Tabelle (D-13)"
      contains: "BRANCHE_ENUM_MAP"
    - path: "templates/onboarding_erstprofil.html"
      provides: "schlanke Erstprofil-Seite: Branchen-Wahl (1 von 8) + 1 Produktfeld (vorbefuellt) + Skip-Button; nerve.css-Komponenten"
      contains: "onboarding"
    - path: "routes/dashboard.py"
      provides: "toter Wizard-Gate-Rest geloescht"
      contains: "index"
    - path: "tests/test_post_login_destination.py"
      provides: "Routing-Matrix-Verhaltenstest (Function-Call-Return) fuer alle Stufen-Faelle + Selbstheilung + Finding-2-step_*-Fall + Finding-3-Member-Submit-Gate; real-PG nerve_test"
      contains: "post_login_destination"
  key_links:
    - from: "services/onboarding_routing.post_login_destination"
      to: "url_for('onboarding.wizard') Erstprofil-URL"
      via: "return bei NOT-done/skipped+owner/admin+0 profiles"
      pattern: "post_login_destination"
    - from: "routes/onboarding.py Erstprofil-Submit"
      to: "Rollen-Gate + profiles-Anlage + active_profile_id + onboarding_state='done'"
      via: "g.user.rolle-Check + Profile(...)+db.add + u.active_profile_id + state-Flip in EINEM commit"
      pattern: "rolle"
---

<objective>
Die Onboarding-Weiche + die Tuer dahinter: (1) `post_login_destination(user)` als nummerierte Stufenliste (nicht verschachteltes if), importierbar von auth.py UND oauth.py — mit dem verbatim AUTH-3-Leer-Slot (Stufe 2); (2) die schlanke Erstprofil-Minimum-Seite (Branche + 1 Feld), die aus BRANCHE_TEMPLATES ein lauffaehiges Erstprofil seedet, aktiv setzt (selber Commit → keine Training-404-Falle) und onboarding_state='done' flippt.

**★ Finding 2 (D-09-Tueroeffner):** Die Weiche liest `onboarding_state NOT IN ('done','skipped')` — NICHT `== 'pending'`. Sonst umgehen spaetere `step_*`-Zwischenzustaende die Weiche (genau was der CHECK-Kommentar in 0032 ausschliessen wollte). Der fail-safe-Default bleibt 'done' (unbekannt/None → nicht umleiten).

**★ Finding 3 (Rollen-Gate auf Submit):** Der POST-Submit-Handler (Profil-Anlage) bekommt dasselbe `g.user.rolle in ('owner','admin')`-Gate wie `profiles.py:63` — sonst umgeht ein Member das owner/admin-Gate der regulaeren Profil-Anlage. Der Skip-Handler darf offen bleiben (jeder darf ueberspringen).

**Keine Verkabelung an die 4 Andock-Stellen in diesem Plan** — der Router wird definiert + getestet, aber noch nicht an auth.py/oauth.py angedockt (das ist Plan 05). So kann der lesende Router-Code hier committet werden, ohne dass ein Login-Pfad ihn live gegen Prod liest, bevor Plan 05 die Docks setzt.

Dieser Plan baut auf Plan 03 (ORM-Spalten onboarding_state/skip_onboarding auf models.py, Prod migriert) → Wave 4.

Purpose: Die Interface-Contracts (Router-Rueckgabe = URL-oder-None) + die Erstprofil-Seite als in sich lauffaehige Einheit, bevor Plan 05 sie an die Login-Pfade haengt.
Output: services/onboarding_routing.py + neue onboarding.py-Bodies (mit Rollen-Gate) + Erstprofil-Template + dashboard.py-Dead-Code-Loeschung + Routing-Matrix-Waechter.

Komplexität: 🟡 mittel — Auth-Weiche + AUTH-3-Vertrags-Naht. **Cross-AI (Gemini + Fable) PFLICHT vor Execute.** Dieser Plan wird NICHT direkt executet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-03-PLAN.md

<interfaces>
<!-- Verifizierte Anker (2026-07-08). ⚠ ALLE Zeilen per INHALT re-ankern (Punkt 20) — vor jedem Edit frisch greppen. -->

Auth-Kontext (VERIFIED routes/auth.py:43-83): `login_required` haengt `g.user` (:55) + `g.org` (:56) frisch aus DB an → post_login_destination(g.user) liest `g.user.onboarding_state`/`.skip_onboarding`/`.rolle`/`.active_profile_id` ohne neue Query. Rolle-Muster (AUTH-1): `g.user.rolle in ('owner','admin')`.

★ Rollen-Gate-Muster (VERIFIED routes/profiles.py:35-36 + :63): Helfer `_rolle()` gibt `g.user.rolle`; die Profil-Anlage (`neu()` :60-63) gated mit `if _rolle() not in ('owner', 'admin'): flash('Keine Berechtigung.','error'); return redirect(url_for('profiles.liste'))`. → Finding 3: der onboarding-Submit-Handler bekommt das GLEICHE Gate (inline `if g.user.rolle not in ('owner','admin')` — onboarding.py importiert nicht profiles._rolle, kleinster Eingriff). Anker per INHALT frisch: `grep -n "rolle" routes/profiles.py`.

Profil-Anlage-Muster (VERIFIED routes/profiles.py:74-83, KOPIEREN):
```python
p = Profile(org_id=g.org.id, name=..., branche=_normalize_branche(...), daten=daten_json, erstellt_von=g.user.id)
db.add(p); db.commit()
```
Aktiv-Setzen-Muster (VERIFIED routes/profiles.py:194-198, im SELBEN commit wie Anlage, D-14):
```python
u = db.get(UserModel, g.user.id)
u.active_profile_id = p.id
db.commit()
flask_session['active_profile_id'] = p.id  # convenience only
```
Branche-Whitelist (VERIFIED routes/profiles.py:17-20): `VALID_BRANCHE = {'saas_b2b','maschinenbau','versicherung','finanzprodukte','immobilien','coaching','beratung','sonstiges',''}`. `_normalize_branche()` (:23-32) mappt Freitext still auf 'sonstiges' — hier NICHT verwenden fuer den Branche-Key (D-13 verlangt EXPLIZITES Mapping).

Profil-Modell (VERIFIED database/models.py:137-148): `Profile(id, org_id, name, branche, daten[JSON-Text], erstellt_von, erstellt_am, consent_text)`. `daten` = JSON-String.

BRANCHE_TEMPLATES (VERIFIED routes/onboarding.py:6-159): 8 Keys — `'SaaS','Versicherung','Consulting','Recruiting','Immobilien','Agentur','Industrie','Sonstiges'`. Jeder hat `basis.{produktbeschreibung, einwaende_detail, phasen}`. Seed-Quelle → das angelegte Profil `daten` = `json.dumps({'basis': BRANCHE_TEMPLATES[key]['basis']})` mit ueberschriebenem produktbeschreibung-Feld.

Onboarding-Stubs (VERIFIED routes/onboarding.py:162/171/178, ERSETZEN — heute alle 302→Dashboard):
- `wizard()` @onboarding_bp.route('/') @login_required  (:162-168) → wird die Erstprofil-GET-Seite + POST-Submit
- `complete()` @route('/complete', POST) (:171-175)
- `create_profile_from_template()` @route('/create_profile', POST) (:178-182)
Blueprint: `onboarding_bp = Blueprint('onboarding', __name__, url_prefix='/onboarding')` (:4). url_for('onboarding.wizard') = '/onboarding/'.

Training-404-Falle (VERIFIED routes/training.py:165-167): `profile = ...filter_by(id=profile_id, org_id=g.org.id).first(); if not profile: return 404`. → aktives Profil MUSS sofort existieren (D-14, selber Commit).

Toter Gate-Rest (VERIFIED routes/dashboard.py:449-453, LOESCHEN, D-20):
```python
        # Wizard redirect disabled — wird in einer späteren Phase neu gebaut
        # if user and user.onboarding_done:
        #     profile_count = db.query(Profile).filter_by(org_id=g.org.id).count()
        #     if profile_count == 0:
        #         return redirect(url_for('profiles.wizard_page'))
```
`profiles.wizard_page` existiert NICHT (grep-belegt) + Logik invertiert. Ersatzlos loeschen.

Dashboard-Banner-Naht (fuer Plan 05 relevant, hier nur Kenntnis): das Skip-Banner 'Noch kein Profil' (D-12) wird in Plan 05 verdrahtet. Dieser Plan setzt beim Skip `onboarding_state='skipped'` + `flash('Noch kein Profil ...', 'warning')` bereits — Plan 05 stellt die Banner-Sichtbarkeit sicher.
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR ueber Production-Pfad. -->
- Deploy-Gate: `bash deploy.sh production` → nerve_test (hat via 0032 die Spalten) + pytest. test_post_login_destination.py auto-collected.
- url_for-Verifikation (routes/CLAUDE.md): VOR url_for('onboarding.wizard') → `grep -rn "def wizard" routes/onboarding.py` + Blueprint-Name 'onboarding' bestaetigt. VOR url_for('dashboard.index') → bestaetigt.
- Live-Test (Plan 05): Browser mit Test-User. In DIESEM Plan kein Login-Andock → Verify ueber Waechter + Route-Existenz.
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Routing-Matrix-Waechter test_post_login_destination.py — Verhaltenstest (Function-Call-Return), ERST-ROT, inkl. Finding-2 + Finding-3</name>
  <read_first>
    - tests/conftest.py — cleanup_rows, nerve_test-DSN, Skip-Guard-Muster (test_schild_guard.py: skip wenn kein Postgres-DSN).
    - tests/test_logs_org_boundary.py (AUTH-LOGS-TENANT) — Muster fuer real-PG-Fixtures + _make_org_and_user (Org+User anlegen mit rolle/onboarding_state).
    - CONTEXT.md D-03 (Stufenliste) + D-02 (done/skipped → None) + D-04 (skip_onboarding≠billing) + D-09 (Tueroeffner).
    - services/onboarding_routing.py existiert noch NICHT → gegen HEAD ImportError = ROT (Erst-Rot-Beweis).
  </read_first>
  <behavior>
    - Verhaltenstest: ruft `post_login_destination(user)` mit echten User-Objekten auf und assertet den RUECKGABEWERT (URL-String oder None). KEIN source-presence. Real-PG nerve_test (Skip-Guard wenn nicht postgresql).
    - Matrix-Faelle (Function-Call-Return-Assertions):
      1. `pending` + rolle 'owner' + 0 Firmen-Profile → return == url_for('onboarding.wizard') ('/onboarding/').
      2. `pending` + rolle 'admin' + 0 Profile → dieselbe URL.
      3. `pending` + rolle 'member' + 0 Profile → return is None (Member NIE umgeleitet, D-03).
      4. `pending` + rolle 'owner' + ≥1 Profil vorhanden → return is None UND onboarding_state danach 'done' (Selbstheilung, D-18) — re-SELECT.
      5. `done` + owner + 0 Profile → return is None (D-02).
      6. `skipped` + owner + 0 Profile → return is None (D-02).
      7. `skip_onboarding=True` + pending + owner + 0 Profile → return is None (Stufe 1 gewinnt VOR Stufe 3; D-04).
      8. ★ FINDING 2: `onboarding_state='step_1'` (hypothetischer step_*-Zwischenzustand) + owner + 0 Profile → return == url_for('onboarding.wizard') (routet wie pending, NICHT wie done). Beweist: die Weiche liest NOT IN ('done','skipped'), nicht == 'pending'. (Der Test setzt onboarding_state per direktem UPDATE auf 'step_1' — der CHECK aus 0032 erlaubt nur pending/done/skipped, daher: entweder der Test dokumentiert dass step_1 (noch) nicht im CHECK ist und nutzt einen Monkeypatch/Objekt-Attribut statt DB-Persist, ODER — sauberer — der Test baut ein User-OBJEKT mit onboarding_state='step_1' in-memory (ohne DB-Persist, da post_login_destination getattr liest) und prueft nur den Routing-Zweig. Executor waehlt den Weg der den NOT-IN-Zweig ohne CHECK-Verletzung testet; Kommentar warum.)
    - Committende Rows (Org/User/ggf. Profile) im Teardown via cleanup_rows raeumen (Reihenfolge Session->User->Org, Profile vor Org).
    - ASCII Test-/Fixture-Namen.
  </behavior>
  <action>
    Erstelle `tests/test_post_login_destination.py`. Skip-Guard oben (nicht-postgresql → skip module-level).

    Import: `from services.onboarding_routing import post_login_destination` (gegen HEAD ImportError → ROT).

    Fixture: App-Test-Request-Context (fuer url_for) + _make_org_and_user(rolle, onboarding_state, skip_onboarding=False, with_profile=False).

    Je Matrix-Fall ein Test. Fall 4 (Selbstheilung): nach `res = post_login_destination(user)` → `assert res is None` UND DB-re-SELECT `onboarding_state == 'done'`. Fall 8 (Finding 2): User-Objekt mit onboarding_state='step_1' → `assert res == url_for('onboarding.wizard')` (routet wie pending). Kommentar: „Finding 2 — Weiche liest NOT IN ('done','skipped'), nicht == 'pending'; ein CHECK-erweiternder step_* darf die Weiche nicht umgehen."

    SEQUENZ (ERST-ROT): Diesen Test zuerst committen+deployen OHNE Task 2 (services/onboarding_routing.py) → gegen HEAD ImportError/ROT. DANN Task 2/3 → gruen. Rot-Beleg ins SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_post_login_destination.py auto-collected. VOR Task 2: ImportError → ROT. NACH Task 2/3: gruen, kein Row-Leak.</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert; grep zeigt `from services.onboarding_routing import post_login_destination` + echte `post_login_destination(...)`-Aufrufe (KEIN source-presence).
    - grep zeigt alle 8 Matrix-Faelle als eigene Test-Funktionen (owner+0→URL, admin+0→URL, member→None, selfheal→None+done, done→None, skipped→None, skip_onboarding→None, ★ step_1→URL [Finding 2]).
    - Fall 4 assertet re-SELECT onboarding_state == 'done'.
    - ★ Fall 8 (Finding 2) assertet step_1 → Erstprofil-URL (routet wie pending) mit Kommentar zur NOT-IN-Semantik.
    - Skip-Guard fuer nicht-postgresql; Cleanup via cleanup_rows.
    - Rot-Beleg dokumentiert.
  </acceptance_criteria>
  <done>test_post_login_destination.py existiert, Verhaltenstest gegen echte User-Objekte, alle 8 Matrix-Faelle inkl. Selbstheilung + Finding-2-step_*-Fall, gegen HEAD nachweislich ROT (ImportError), sauberer Cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: services/onboarding_routing.py — post_login_destination als nummerierte Stufenliste (NOT-IN done/skipped, Finding 2) + verbatim AUTH-3-Leer-Slot</name>
  <read_first>
    - routes/auth.py:43-83 — g.user-Attribute (onboarding_state/skip_onboarding/rolle/active_profile_id nach Plan 03 verfuegbar).
    - routes/profiles.py:53 — Profil-Count-Muster `db.query(Profile).filter_by(org_id=...).count()`.
    - `grep -rn "def wizard" routes/onboarding.py` + routes/CLAUDE.md (Blueprint 'onboarding') → url_for('onboarding.wizard').
    - CONTEXT.md D-03 (4 Stufen) + D-04 (Slot-Vertrag-Trennung) + D-09 (Tueroeffner) — VERBATIM uebernehmen.
    - Claude's Discretion (CONTEXT): Datei-Heimat = neues services/-Modul (importierbar von auth.py UND oauth.py, kein Zirkelimport — services/ importiert NICHT routes/auth).
  </read_first>
  <action>
    Erstelle `services/onboarding_routing.py` mit `post_login_destination(user)`. Nimmt ein User-Objekt (g.user), bekommt bei Bedarf eine DB-Session intern (get_session) fuer den Profil-Count, gibt eine Redirect-URL (str) ODER None zurueck. NUMMERIERTE Stufenliste (D-03, KEIN verschachteltes if):

    ```python
    from flask import url_for
    from database.db import get_session
    from database.models import Profile, User

    _DONE_STATES = ('done', 'skipped')  # Finding 2 / D-09: alles ANDERE (pending, step_*) = Onboarding offen

    def post_login_destination(user):
        """Nummerierte Stufenliste (D-03). Gibt eine Redirect-URL (str) zurueck ODER None
        ('nichts tun' → Aufrufer behaelt sein Standard-Ziel, z.B. Coach-Weiche).
        """
        # ── Stufe 1 — skip_onboarding → nichts tun (Onboarding uebersprungen) ──
        if getattr(user, 'skip_onboarding', False):
            return None

        # ── Stufe 2 — [LEER — AUTH-3-SLOT] ────────────────────────────────────────────────
        # AUTH-3 baut hier seinen Billing-Gate ein, VOR der Onboarding-Stufe.
        # VERTRAG (D-03/D-04, WORTGLEICH — AUTH-3 nicht raten lassen):
        #   „kein Abo = Owner UND nicht skip_billing UND Status nicht in (active, past_due)
        #    → Preisseite; past_due → Portal."
        # ★ ANDRE-TRENNUNG (D-04): skip_onboarding ueberspringt NUR das Onboarding, NICHT die
        #   Kasse. Das Geld laeuft ueber ein EIGENES organisations.skip_billing pro Firma
        #   (AUTH-3/4). Diese zwei Schranken NIEMALS vermischen: Stufe 1 (skip_onboarding, oben)
        #   und der Billing-Gate hier (skip_billing) sind getrennte Spalten mit getrennter Semantik.
        # (In AUTH-2 bewusst LEER — kein Billing verdrahtet.)

        # ── Stufe 3 — Onboarding (D-09 Tueroeffner: NOT IN done/skipped, Finding 2) ────────
        # fail-safe-Default 'done' (unbekannt/None → nicht umleiten).
        state = getattr(user, 'onboarding_state', 'done') or 'done'
        rolle = getattr(user, 'rolle', None)
        if state not in _DONE_STATES and rolle in ('owner', 'admin'):
            db = get_session()
            try:
                profile_count = db.query(Profile).filter_by(org_id=user.org_id).count()
                if profile_count == 0:
                    return url_for('onboarding.wizard')          # Erstprofil-Seite '/onboarding/'
                # Selbstheilung: Profile existieren schon → offener State war stale → auf done, kein Redirect
                u = db.get(User, user.id)
                if u is not None:
                    u.onboarding_state = 'done'
                    db.commit()
                return None
            finally:
                db.close()
        # Member NIE umgeleitet (faellt durch); NOT-done/skipped+member → None.

        # ── Stufe 4 — nichts tun (Standard-Ziel; done/skipped/member landen hier) ──────────
        return None
    ```
    Hinweise:
    - ★ Finding 2: der Zweig prueft `state not in ('done','skipped')` (via `_DONE_STATES`), NICHT `state == 'pending'` — so routen auch spaetere step_*-Zustaende korrekt ins Onboarding, ohne dass die Weiche geaendert werden muss (D-09 Tueroeffner). Der `or 'done'`-Fallback deckt None/leer fail-safe ab.
    - KEIN Import aus routes/auth (Zirkel). url_for ist zur Aufruf-Zeit im Request-Context gueltig.
    - Latenz (Punkt 25): 1 zusaetzlicher COUNT-Query nur im NOT-done/skipped+owner/admin-Zweig (reiner Login, nicht Live-Loop) → vernachlaessigbar.
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_post_login_destination.py jetzt GRUEN (alle 8 Matrix-Faelle inkl. Finding-2-step_1). url_for('onboarding.wizard') aufloesbar (Task 3 liefert die Route).</automated>
  </verify>
  <acceptance_criteria>
    - Datei `services/onboarding_routing.py` existiert; grep zeigt `def post_login_destination`.
    - grep zeigt die 4 Stufen-Marker als Kommentare (`Stufe 1`, `Stufe 2 — [LEER — AUTH-3-SLOT]`, `Stufe 3`, `Stufe 4`).
    - ★ grep zeigt `not in` gegen `('done','skipped')` (bzw. `_DONE_STATES`) im Stufe-3-Zweig — NICHT `== 'pending'` (Finding 2).
    - grep zeigt den VERBATIM Slot-Vertrag: `kein Abo = Owner UND nicht skip_billing UND Status nicht in (active, past_due)` + `past_due → Portal` + die D-04-Trennung `skip_onboarding ueberspringt NUR das Onboarding, NICHT die Kasse` + `organisations.skip_billing`.
    - grep zeigt Stufe 1 `getattr(user, 'skip_onboarding', False)` return None (VOR Stufe 3).
    - grep zeigt Selbstheilung: `onboarding_state = 'done'` im NOT-done/skipped+profile_count>0-Zweig.
    - KEIN Import von routes.auth (grep bestaetigt kein Zirkel).
    - test_post_login_destination.py PASSED gegen nerve_test.
  </acceptance_criteria>
  <done>post_login_destination als nummerierte 4-Stufen-Liste; Stufe 1 skip_onboarding, Stufe 2 verbatim AUTH-3-Leer-Slot, Stufe 3 Onboarding NOT-IN-done/skipped (Finding 2) + Selbstheilung + Member-nie, Stufe 4 default; importierbar ohne Zirkel; Matrix-Waechter gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Erstprofil-Minimum-Seite (onboarding.py-Bodies + Template) mit Rollen-Gate auf Submit (Finding 3) + EXPLIZITEM Branche-Enum-Mapping + dashboard.py Dead-Code loeschen</name>
  <read_first>
    - routes/onboarding.py:6-159 (BRANCHE_TEMPLATES 8 Keys) + :162-182 (die 3 Stubs, per INHALT frisch greppen `grep -n "def wizard\|def complete\|def create_profile" routes/onboarding.py`).
    - ★ routes/profiles.py:35-36 (`_rolle()`) + :60-65 (`neu()`-Rollen-Gate) — das Gate-Muster fuer Finding 3. Frisch: `grep -n "rolle" routes/profiles.py`.
    - routes/profiles.py:74-83 (Anlage) + :194-198 (Aktiv-Setzen) + :17-20 (VALID_BRANCHE) — KOPIER-Vorlage.
    - `grep -n "def wizard_page\|def wizard" routes/profiles.py` → bestaetigen dass wizard_page NICHT existiert (Dead-Code-Beleg D-20).
    - routes/dashboard.py:444-453 (index() + toter Gate-Rest, per INHALT ankern).
    - templates/ — eine bestehende schlanke Seite als nerve.css-Komponenten-Vorlage.
    - CONTEXT.md D-11/D-12/D-13/D-14/D-15/D-18.
  </read_first>
  <action>
    (A) `routes/onboarding.py` — die 3 Stubs ERSETZEN (D-15). Neue Struktur:

    1. EXPLIZITE Enum-Mapping-Tabelle als Modul-Konstante (D-13, KEIN _normalize_branche-Fallback):
    ```python
    # D-13: EXPLIZITES Branche-Enum-Mapping (BRANCHE_TEMPLATES-Key → profiles.VALID_BRANCHE).
    # KEIN stiller _normalize_branche()-Fallback. Recruiting + Agentur haben KEINEN eigenen
    # Whitelist-Wert → bewusst dokumentiert auf 'sonstiges' (nicht still geschluckt).
    BRANCHE_ENUM_MAP = {
        'SaaS':         'saas_b2b',
        'Versicherung': 'versicherung',
        'Consulting':   'beratung',
        'Immobilien':   'immobilien',
        'Industrie':    'maschinenbau',
        'Sonstiges':    'sonstiges',
        'Recruiting':   'sonstiges',   # dokumentiert: kein eigener Whitelist-Wert
        'Agentur':      'sonstiges',   # dokumentiert: kein eigener Whitelist-Wert
    }
    ```

    2. `wizard()` @onboarding_bp.route('/') @login_required (GET): rendert `onboarding_erstprofil.html` mit den 8 Branchen-Keys + vorbefuelltem Produktfeld. Vorbefuellung (D-11): Text aus `BRANCHE_TEMPLATES[<Branche>]['basis']['produktbeschreibung']` (client-JS aus einer ins Template gegebenen Map ODER Default-Branche 'Sonstiges' als Startwert). GET-Anzeige braucht KEIN Rollen-Gate (Member darf die Seite sehen / Skippen). Empfohlen: POST-Submit auf dieselbe '/' + Skip als eigener POST '/onboarding/skip'.

    3. ★ Submit-Handler (POST) — FINDING 3, Rollen-Gate ZUERST: 
       `if g.user.rolle not in ('owner', 'admin'): flash('Keine Berechtigung.','error'); return redirect(url_for('dashboard.index'))` (gleiche Semantik wie profiles.py:63; ERST-ROT-tauglich). DANACH:
       - liest `branche_key` (1 von 8) + `produkt`.
       - `branche_enum = BRANCHE_ENUM_MAP.get(branche_key, 'sonstiges')` (explizit).
       - `daten = {'basis': {**BRANCHE_TEMPLATES[branche_key]['basis']}}`; `daten['basis']['produktbeschreibung'] = produkt.strip() or BRANCHE_TEMPLATES[branche_key]['basis']['produktbeschreibung']` (Durchklicken ohne Aendern erlaubt, D-11).
       - `name = f"{branche_key} — Startprofil"` (D-11).
       - Profil anlegen (profiles.py:74-83-Muster): `p = Profile(org_id=g.org.id, name=name, branche=branche_enum, daten=json.dumps(daten, ensure_ascii=False), erstellt_von=g.user.id); db.add(p); db.flush()`.
       - Aktiv-Setzen + State-Flip im SELBEN commit (D-14/D-18): `u = db.get(User, g.user.id); u.active_profile_id = p.id; u.onboarding_state = 'done'; db.commit(); flask_session['active_profile_id'] = p.id`.
       - `return redirect(url_for('dashboard.index'))`.

    4. Skip-Handler (POST '/onboarding/skip') — KEIN Rollen-Gate (Skip offen fuer alle, Finding 3): `u = db.get(User, g.user.id); u.onboarding_state = 'skipped'; db.commit(); flash('Noch kein Profil — lege eins an, sonst ist Training nicht nutzbar.', 'warning'); return redirect(url_for('dashboard.index'))` (D-12; Plan 05 sichert die Banner-Sichtbarkeit).

    5. `complete()`/`create_profile_from_template()`-Stubs entfernen (kein aktiver Aufrufer, grep-belegen).

    Import ergaenzen: `from flask import render_template, request, flash, g, redirect, url_for, session as flask_session`; `from database.db import get_session`; `from database.models import Profile, User`; `import json`.

    (B) `templates/onboarding_erstprofil.html`: schlanke Seite (nerve.css n-* Komponenten). Branche-Select (8 Optionen, deutsche Labels mit Umlauten wo noetig — Keys ASCII 'SaaS' etc.), 1 Produktfeld „Was verkaufst du?" (vorbefuellt), „Profil anlegen"-Button (POST Submit) + „Ueberspringen"-Button (POST Skip). HTML-IDs/Namen ASCII (`branche_key`, `produkt`); sichtbare Labels/Buttons mit Umlauten. CSRF: bei klassischem form `{{ csrf_token() }}` Hidden-Field (der JS-fetch-Wrapper aus AUTH-1 deckt nur fetch ab).

    (C) `routes/dashboard.py` — den toten Gate-Rest (frisch per INHALT greppen: `grep -n "wizard_page\|Wizard redirect disabled" routes/dashboard.py`) ERSATZLOS loeschen (~:449-453). Keine Ersatz-Logik.

    Umlaute-Regel strikt: JS-Identifier/HTML-IDs/dict-Keys/Route-Namen ASCII; User-facing Labels/Buttons/Flash mit Umlauten.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen. url_for('onboarding.wizard') aufloesbar. test_post_login_destination.py gruen. Danach Live (Plan 05): /onboarding/ mit Test-User → Seite rendert, Submit (owner) legt Profil an + aktiv + done; Member-Submit → 403/Redirect (Finding 3); Skip → Dashboard + Banner.</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/onboarding.py zeigt `BRANCHE_ENUM_MAP` mit allen 8 Keys + „dokumentiert"-Kommentaren bei Recruiting/Agentur.
    - ★ grep zeigt im Submit-Handler `g.user.rolle not in ('owner', 'admin')` (Rollen-Gate, Finding 3) VOR der Profil-Anlage; der Skip-Handler hat KEIN solches Gate.
    - grep zeigt Submit-Handler: Profil-Anlage + `active_profile_id` + `onboarding_state = 'done'` im selben commit-Block (D-14/D-18).
    - grep zeigt Skip-Handler: `onboarding_state = 'skipped'` + `flash(...)` (D-12).
    - grep zeigt Profilname-Muster `— Startprofil` (D-11).
    - Datei `templates/onboarding_erstprofil.html` existiert; Branche-Select + Produktfeld (name='produkt') + Skip-Button; HTML-IDs ASCII, sichtbare Labels mit Umlauten; CSRF-Token im Form.
    - grep von routes/dashboard.py zeigt KEIN `wizard_page` mehr + KEIN „Wizard redirect disabled" (Dead-Code geloescht).
    - `_normalize_branche` wird fuer den Branche-Key NICHT verwendet (grep: onboarding.py nutzt BRANCHE_ENUM_MAP).
    - deploy.sh production Gate gruen.
  </acceptance_criteria>
  <done>Erstprofil-Seite (/onboarding/) ersetzt die 3 Stubs: Submit hat das owner/admin-Rollen-Gate (Finding 3), Skip bleibt offen; Branche + 1 Feld → lauffaehiges Profil aus BRANCHE_TEMPLATES, aktiv im selben Commit (keine Training-404-Falle), state='done'; Skip → 'skipped' + Banner; EXPLIZITES Enum-Mapping; toter dashboard.py-Gate-Rest geloescht.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN: onboarding_state/skip_onboarding/rolle/active_profile_id in post_login_destination; SCHREIBEN: onboarding_state Selbstheilung 'done' + Submit 'done'/Skip 'skipped' + active_profile_id).
- `profiles` (LESEN: Count je org_id in Stufe 3; SCHREIBEN: Erstprofil-Anlage).

### inspect.sh-Beleg (VERBATIM `\d users` Prod-nerve — nach Plan 02/03)
```
users (public) — relevante Spalten (Prod-nerve, nach Plan 02/03):
  id                integer      NOT NULL (PK)
  org_id            integer      NOT NULL  FK→organisations(id)
  rolle             varchar(50)  NULL
  active_profile_id integer      NULL       FK→profiles(id)
  onboarding_state  text         NOT NULL DEFAULT 'pending', CHECK ('pending'/'done'/'skipped')
  skip_onboarding   boolean      NOT NULL DEFAULT false
  onboarding_done   boolean      NULL       ← DEPRECATED (nur Leser)
```
`profiles` (models.py:137-148 + inspect.sh schema profiles): id, org_id (FK), name NOT NULL, branche, daten (JSON-Text), erstellt_von, erstellt_am, consent_text. Erstprofil schreibt eine neue Row + setzt users.active_profile_id.

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg) + kann Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `user.onboarding_state` | gelesen post_login_destination (Stufe 3, NOT-IN done/skipped) | DB-Spalte users.onboarding_state (Plan 02/03) | LIVE, synchron via login_required (g.user frisch, auth.py:51). Kein Voraus-Lauf | ✓ Plan 02/03 + login_required |
| `user.skip_onboarding` | gelesen Stufe 1 | DB-Spalte (Plan 02/03) | LIVE synchron | ✓ |
| `user.rolle` | gelesen Stufe 3 + ★ Submit-Rollen-Gate (Finding 3) | DB-Spalte users.rolle (bestehend) | LIVE synchron (g.user) | ✓ bestehend |
| Profil-Count je org | `db.query(Profile).filter_by(org_id).count()` Stufe 3 | DB-Tabelle profiles | LIVE synchron im Login-Request | ✓ |
| `onboarding_state='done'/'skipped'` | geschrieben Selbstheilung + Submit + Skip | DB-Spalte users.onboarding_state | LIVE synchron (commit im Request) | ✓ |
| `active_profile_id` | geschrieben Submit (selber commit wie Profil) | DB-Spalte users.active_profile_id | LIVE synchron — Aktiv-Setzen im SELBEN commit wie Profil-Anlage (D-14) → Training-404-Falle (training.py:165) kann NICHT auslaufen | ✓ Muster profiles.py:194-198 |

### Punkt 26 Schreib-vor-Lese-Analyse
Alle Reads von `g.user.*` sind LIVE-attached via `login_required` (auth.py:51, frischer DB-Load) — KEIN async/batch-Versatz. Die Training-404-Naht ist entschaerft (Submit schreibt Profil + active_profile_id im SELBEN commit, D-14). Der Prod-Ebenen-Schreib-vor-Lese-Fall (Spalte existiert) ist bereits in Plan 02/03 aufgeloest — hier liest der Router gegen existierende Spalten. Kein Login-Andock in diesem Plan (das ist Plan 05).

### Bei Diskrepanz: STOP + Replan.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| eingeloggter Browser → POST /onboarding/ (Submit) + /onboarding/skip | untrusted Form-Input (branche_key, produkt) + Rollen-Grenze kreuzt Server-Grenze |
| post_login_destination → url_for/Redirect | Weiche entscheidet Redirect-Ziel aus User-State |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH2-05 | Tampering (ungueltiger branche_key unterlaeuft VALID_BRANCHE) | Erstprofil-Submit | mitigate | EXPLIZITE BRANCHE_ENUM_MAP (D-13) → nur Whitelist-Werte in profiles.branche |
| T-AUTH2-06 | ★ Elevation (Member legt per POST Profil an / umgeht owner/admin-Gate) | Erstprofil-Submit-Handler (Finding 3) | mitigate | `g.user.rolle in ('owner','admin')`-Gate auf Submit (wie profiles.py:63); Member → Redirect+Flash; Skip bleibt offen; Waechter/UAT: Member-POST → 403/Redirect |
| T-AUTH2-06b | Elevation (Member erzwingt Onboarding-Redirect / fremde org) | post_login_destination Stufe 3 | mitigate | Stufe 3 nur owner/admin; Member → None; Count strikt filter_by(org_id=user.org_id) |
| T-AUTH2-07 | DoS (Training-404-Falle: aktives Profil referenziert aber fehlt) | Submit Aktiv-Setzen | mitigate | active_profile_id + Profil-Anlage im SELBEN commit (D-14) |
| T-AUTH2-08 | CSRF auf POST /onboarding/ | Submit/Skip | mitigate | CSRF-Token im Form (klassisches POST → csrf_token()-Hidden; AUTH-1 fetch-Wrapper deckt fetch) |
| T-AUTH2-09 | Info-Leak (falscher AUTH-3-Slot-Vertrag → AUTH-3 baut Billing-Bypass) | Stufe-2-Leer-Slot | mitigate | Vertrag VERBATIM im Kommentar (skip_onboarding≠skip_billing D-04) |
</threat_model>

<verification>
- deploy.sh production Gate gruen MIT test_post_login_destination.py PASSED (real-PG, alle 8 Matrix-Faelle inkl. Selbstheilung + Finding-2-step_1).
- test_post_login_destination.py: ROT gegen HEAD (ImportError), gruen nach Task 2/3 — dokumentiert.
- ★ Finding 2: step_1-Fall gruen (routet wie pending, nicht wie done).
- ★ Finding 3: Submit-Handler traegt das Rollen-Gate (grep + Live-UAT Member-POST → 403/Redirect in Plan 05).
- url_for('onboarding.wizard') + url_for('dashboard.index') aufloesbar.
- AUTH-3-Slot-Vertrag VERBATIM vorhanden (grep).
- dashboard.py Dead-Code (wizard_page) geloescht (grep 0 Treffer).
</verification>

<success_criteria>
- post_login_destination als nummerierte 4-Stufen-Liste, NOT-IN-done/skipped (Finding 2), importierbar aus auth.py/oauth.py, verbatim AUTH-3-Leer-Slot inkl. D-04-Trennung.
- Routing-Matrix (8 Faelle inkl. Selbstheilung + Member-nie + skip_onboarding-Stufe-1 + step_1) im Deploy-Gate gruen, gegen HEAD rot gewesen.
- ★ Submit-Handler hat das owner/admin-Rollen-Gate (Finding 3); Skip offen.
- Erstprofil-Seite ersetzt die 3 Stubs, EXPLIZITES Enum-Mapping, Profil-Anlage+aktiv+done im selben Commit, Skip→skipped+Banner.
- Toter dashboard.py-Gate-Rest geloescht (D-20).
- KEIN Login-Andock in diesem Plan (erst Plan 05).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-04-SUMMARY.md` erstellen (inkl. Rot-Beleg der Routing-Matrix, Finding-2-step_1-Beleg, Finding-3-Rollen-Gate-Beleg, Enum-Mapping-Beleg, Dead-Code-Loesch-Beleg, url_for-Verifikation).
</output>
