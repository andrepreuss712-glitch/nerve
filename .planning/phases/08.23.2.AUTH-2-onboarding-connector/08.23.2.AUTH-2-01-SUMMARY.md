---
phase: 08.23.2.AUTH-2
plan: "01"
subsystem: auth/onboarding
tags: [migration, alembic, onboarding, real-pg-guard, expand-contract]
dependency_graph:
  requires: [08.23.2.AUTH-1]
  provides: [alembic/versions/0032, users.onboarding_state, users.skip_onboarding, test_onboarding_state_migration]
  affects: [users table, alembic_version]
tech_stack:
  added: []
  patterns: [alembic-3-step-nullable-backfill-not-null, check-constraint-dooropener, pg-comment-schild]
key_files:
  created:
    - alembic/versions/0032_onboarding_state_skip_onboarding.py
    - tests/test_onboarding_state_migration.py
  modified: []
decisions:
  - "Expand/Contract Schritt 1a: models.py UNANGETASTET — ORM-Leser darf erst live, wenn Spalte auf Prod existiert (Finding 1)"
  - "3-Schritt-Backfill D-07: add nullable -> UPDATE alle done -> ALTER NOT NULL (kein Zwangs-Onboarding fuer Bestand)"
  - "CHECK-Constraint D-09 als Tueroeffner: IN ('pending','done','skipped') erweiterbar ohne Weichen-Aenderung"
  - "onboarding_done EINGEFROREN (DEPRECATED-COMMENT), nicht gedroppt (Zombie-Regel D-06)"
  - "ERST-ROT-Sequenz: Test zuerst committen (Task 1), dann Migration (Task 2)"
metrics:
  duration: "~25 min"
  completed_date: "2026-07-08"
  tasks_completed: 2
  tasks_total: 2
  files_created: 2
  files_modified: 0
---

# Phase 08.23.2.AUTH-2 Plan 01: Onboarding-Fundament-Migration + ERST-ROT-Waechter Summary

**One-liner:** Alembic 0032 legt users.onboarding_state (TEXT NOT NULL Default pending, Backfill done, CHECK-Tueroeffner D-09) + users.skip_onboarding (boolean Default FALSE) an, mit Postgres-Schildern und DEPRECATED-Marker auf onboarding_done; models.py bleibt unangetastet (Expand/Contract Schritt 1a).

---

## Was wurde gebaut

### Task 1 — ERST-ROT-Migrations-Waechter (commit a00661a)

`tests/test_onboarding_state_migration.py` — 4 Test-Funktionen gegen real-PG nerve_test:

- `test_columns_exist` — fragt information_schema.columns, prueft onboarding_state (TEXT, NOT NULL) + skip_onboarding (boolean). Gegen HEAD ohne 0032: **ROT** (Spalten fehlen, deploy.sh-Gate blockt Restart).
- `test_no_null_and_default_pending` — zaehlt NULL-Rows (muss 0 sein) + legt User ohne onboarding_state an, prueft DB-Default 'pending'.
- `test_check_constraint_rejects_bogus` — bogus_value wirft CHECK-Verletzung (pytest.raises via Savepoint); 'skipped' geht durch.
- `test_skip_onboarding_default_false` — neuer User ohne skip_onboarding hat FALSE.

Skip-Guard oben im Modul: `allow_module_level=True`, skippt wenn DATABASE_URL/TEST_DATABASE_URL nicht mit 'postgresql' beginnt (kein False-Green auf SQLite).

Cleanup: `cleanup_rows(db_session, tracker)` mit Reihenfolge users -> organisations -> tenant_orgs.

### Task 2 — Migration 0032 (commit 797404f)

`alembic/versions/0032_onboarding_state_skip_onboarding.py`:

**down_revision = '0031'** (auf Prod-HEAD 0030 kommen 0031+0032 zusammen hoch; 0031 ist idempotenter COMMENT-only, D-22).

**upgrade()-Sequenz (D-07, EIN Schritt):**
1. `op.add_column` onboarding_state TEXT nullable=True server_default='pending'
2. `op.execute("UPDATE users SET onboarding_state = 'done'")` — alle Bestands-User
3. `op.alter_column` onboarding_state nullable=False
4. `op.create_check_constraint('ck_users_onboarding_state', ..., "onboarding_state IN ('pending','done','skipped')")` — D-09 Tueroeffner
5. `op.add_column` skip_onboarding boolean nullable=False server_default=false()
6. 3x `op.execute(COMMENT ON COLUMN users.{onboarding_state,skip_onboarding,onboarding_done})`

**downgrade()-Sequenz:** onboarding_done-Schild zuruecksetzen -> skip_onboarding droppen -> CHECK-Constraint droppen -> onboarding_state droppen.

---

## Static-Acceptance-Grep-Ergebnisse (deploy-gate deferred to Andre)

Da lokales pytest/deploy.sh NICHT ausgefuehrt wird (CLAUDE.md HART: Kein Local-Dev), wurden
alle Acceptance-Criteria statisch via grep verifiziert:

| Kriterium | Grep-Beleg | Status |
|---|---|---|
| `down_revision = '0031'` | Zeile 22+31 in 0032_...py | OK |
| `add_column onboarding_state` nullable + server_default='pending' | Zeile 40 | OK |
| `UPDATE users SET onboarding_state = 'done'` | Zeile 45 | OK |
| `alter_column ... nullable=False` | Zeile 48 | OK |
| `create_check_constraint` mit `IN ('pending','done','skipped')` | Zeilen 54-57 | OK |
| D-09-Tueroeffner-Kommentar verbatim im Code | Zeilen 51-53 | OK |
| `add_column skip_onboarding boolean nullable=False server_default=false()` | Zeilen 62-68 | OK |
| 3x `COMMENT ON COLUMN users.` (onboarding_state, skip_onboarding, onboarding_done) | Zeilen 73, 83, 95 | OK |
| DEPRECATED-Marker auf onboarding_done | Zeile 96 | OK |
| `information_schema.columns` (echte DB-Query, KEIN source-presence-grep) | Test-Zeile 94 | OK |
| `allow_module_level=True` Skip-Guard | Test-Zeile 31 | OK |
| `data_type == 'text'` + `is_nullable == 'NO'` | Test-Zeilen 109/112 | OK |
| `pytest.raises(Exception)` fuer Bogus-CHECK | Test-Zeile 169 | OK |
| `skip_onboarding is False` | Test-Zeile 219 | OK |
| `cleanup_rows` mit tracker | Test-Zeilen 24/48 | OK |
| `database/models.py` NICHT in git diff | `git diff --name-only HEAD~2 HEAD` zeigt nur 2 Dateien | OK |

---

## ERST-ROT-Beleg (dokumentiert, nicht live verifiziert)

Commit-Sequenz:
1. `a00661a` — test_onboarding_state_migration.py (Task 1) **ohne 0032** -> gegen HEAD (nerve_test alembic 0030/0031) MUSS `test_columns_exist` ROT sein, weil die Spalten onboarding_state + skip_onboarding in nerve_test noch nicht existieren. Das deploy.sh-Gate haette den Restart geblockt.
2. `797404f` — 0032_onboarding_state_skip_onboarding.py (Task 2) -> nach alembic upgrade head gegen nerve_test werden die Spalten angelegt -> alle 4 Tests gruen.

Der Rot-Nachweis ist durch die ERST-ROT-Commit-Reihenfolge strukturell belegt. Die tatsaechliche Gruen-Bestaeigung erfolgt durch Andreas deploy.sh-Gate auf nerve_test.

---

## Finding 1 / Expand/Contract Schritt 1a — Bestaetigung

`database/models.py` wurde in DIESEM Plan UNANGETASTET gelassen. Beleg:

```
git diff --name-only HEAD~2 HEAD
alembic/versions/0032_onboarding_state_skip_onboarding.py
tests/test_onboarding_state_migration.py
```

Nur die 2 neuen Dateien, kein models.py, kein app.py.

Begrundung (D-23, Finding 1): routes/auth.py:51 = `db.get(User, session['user_id'])` = ORM SELECT * bei jedem login_required-Request. Wuerde models.py die neuen Spalten kennen UND deploy.sh den Service neustarten, WAEHREND Prod-nerve noch auf alembic 0030 steht (ohne die Spalten) -> Login-Crash. Deshalb: erst die Datei + Guard live (dieser Plan), dann manuelle Prod-Migration (Plan 02), dann ORM-Spalten + app.py (Plan 03).

---

## Deviations from Plan

None — Plan executed exactly as written. Beide Tasks entsprechen exakt der Spec.

Einzige Anpassung: Der Skip-Guard im Test prueft sowohl `DATABASE_URL` als auch `TEST_DATABASE_URL` (OR-Verknuepfung), weil deploy.sh die Umgebung als `TEST_DATABASE_URL` exportiert (Muster aus bestehenden Tests), aber der Plan `DATABASE_URL` erwaehnt. Beide koennen je nach Ausfuehrungs-Kontext gesetzt sein. Semantisch equivalent.

---

## Threat Flags

Keine neuen Threat Surfaces ausserhalb des Plan-Threat-Models: 0032 ist rein additiv auf nerve_test (kein neuer Netzwerk-Endpoint, kein neuer Auth-Pfad). Prod-nerve wird in DIESEM Plan nicht migriert.

---

## Known Stubs

Keine Stubs in diesen Dateien. Die Migration schreibt echte Daten (Backfill). Der Test ist ERST-ROT — er wird nach Task 2 gruen, ist kein Stub.

---

## ★ NEXT GATE: Andre fuehrt `bash deploy.sh production` aus

Das ist der naechste Schritt — NICHT dieser Agent.

`bash deploy.sh production` (auf Andre's Rechner):
1. Provisioniert nerve_test via `pg_dump --schema-only` vom Prod-nerve (alembic 0030)
2. Setzt alembic_version auf Prod-HEAD (0030)
3. Fahrt `alembic upgrade head` gegen nerve_test — wendet 0031 + 0032 an
4. Fahrt `pytest tests/ -m "not live and not perf"` gegen nerve_test — test_onboarding_state_migration.py wird automatisch collected
5. Wenn gruen: kein Restart (models.py kennt onboarding_state nicht -> sicher gegen Prod-0030)

Migration-gruen ist erst auf nerve_test bewiesen, NICHT lokal. Prod-nerve selbst wird in Plan 02 (manueller alembic upgrade head, Schritt 1b) migriert.

---

## Self-Check: PASSED

Erstellte Dateien existieren:
- `tests/test_onboarding_state_migration.py` — erstellt (Write-Tool, bestätigt)
- `alembic/versions/0032_onboarding_state_skip_onboarding.py` — erstellt (Write-Tool, bestätigt)

Commits existieren:
- `a00661a` — test(08.23.2.AUTH-2-01): ERST-ROT test_onboarding_state_migration
- `797404f` — feat(08.23.2.AUTH-2-01): Migration 0032

models.py nicht in git diff (Finding 1 / Schritt 1a intact): bestätigt via grep.
