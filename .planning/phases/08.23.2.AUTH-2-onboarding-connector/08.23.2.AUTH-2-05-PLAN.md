---
phase: 08.23.2.AUTH-2
plan: 05
type: execute
wave: 5
complexity: "🔴-nah (Verkabelung an Auth-Pfad + Live-UAT — Cross-AI Pflicht; autonomous:false am UAT-Checkpoint, Claudian/André fuehrt)"
depends_on: ["08.23.2.AUTH-2-02", "08.23.2.AUTH-2-04"]
files_modified:
  - routes/auth.py
  - routes/oauth.py
  - templates/marketing/landing.html
  - templates/dashboard.html
  - tests/test_login_destination_wiring.py
autonomous: false   # Task 4 = Live-Browser-UAT (Claudian/André), inkl. S1+S4 PFLICHT (Fable-UAT-Tightening)
user_setup: []

must_haves:
  truths:
    - "Ein frisch registrierter Owner (Formular-Register) mit pending + 0 Profilen landet nach Login auf der Erstprofil-Seite (/onboarding/)"
    - "Ein OAuth-Neuuser (Google/Microsoft) mit pending + 0 Profilen landet auf der Erstprofil-Seite (Weiche laeuft VOR dem OAuth-Commit/Redirect, D-19)"
    - "Ein bereits-eingeloggter pending-Owner (S1) wird beim Aufruf von /login zur Erstprofil-Seite geleitet"
    - "Bestandsuser (done) landen unveraendert auf Dashboard bzw. Coach-Weiche"
    - "Der JSON-Register-Response (S3) traegt das Ziel; landing.html nutzt es (window.location aus Response statt hart /dashboard)"
    - "Ein Skip-User sieht im Dashboard das Pflicht-Banner 'Noch kein Profil'"
    - "★ Prod-nerve traegt onboarding_state + skip_onboarding bereits (Plan 02 lief davor) — der Weichen-Code liest gefahrlos; die Prod-Migration ist NICHT mehr Teil dieses Plans (Finding 1: verschoben nach Plan 02)"
  artifacts:
    - path: "routes/auth.py"
      provides: "S1 (already-logged-in) + S2 (JSON-Login next=Ziel) + S3 (JSON-Register Ziel in Response) via post_login_destination"
      contains: "post_login_destination"
    - path: "routes/oauth.py"
      provides: "S4 (Bestand + Neuanlage) via post_login_destination, VOR db.commit()/redirect (D-19)"
      contains: "post_login_destination"
    - path: "templates/marketing/landing.html"
      provides: "S3-JS-Fix (window.location aus data.next/Response statt hart '/dashboard')"
      contains: "data.next"
    - path: "templates/dashboard.html"
      provides: "Skip-Banner 'Noch kein Profil' (D-12) sichtbar bei onboarding_state='skipped' ohne Profil"
      contains: "Noch kein Profil"
    - path: "tests/test_login_destination_wiring.py"
      provides: "E2E-Waechter: Register→Erstprofil-Ziel im Response; done→kein Redirect; ★ Member-Submit-Gate (Finding 3); real-PG nerve_test; ERST-ROT"
      contains: "onboarding"
  key_links:
    - from: "routes/auth.py api_register/api_login/login"
      to: "post_login_destination(user) → next/redirect"
      via: "Import aus services.onboarding_routing"
      pattern: "post_login_destination"
    - from: "routes/oauth.py _oauth_login_or_create"
      to: "post_login_destination VOR db.commit()/redirect"
      via: "Weiche vor den 2 Ruecksprung-Stellen"
      pattern: "post_login_destination"
---

<objective>
Die Verkabelung + die Live-Scharfschaltung: (1) `post_login_destination` an alle 4 Andock-Stellen (S1 already-logged-in, S2 JSON-Login, S3 JSON-Register, S4 OAuth) haengen — mit dem +1-Zeilen-JS-Fix in landing.html (S3) und der Commit-Reihenfolge-Sicherung bei OAuth (D-19: Weiche VOR commit/redirect); (2) das Skip-Banner im Dashboard sichtbar machen (D-12); (3) Live-Browser-UAT mit Test-User (Sicht statt Zusicherung).

**★ Finding 1 — Prod-Migration ist NICHT mehr hier:** Der frühere Plan-03-Task-4 enthielt die manuelle Prod-Migration. Die ist jetzt Plan 02 (Schritt 1b, lief VOR dem models.py-Code in Plan 03). Prod-`nerve` traegt onboarding_state/skip_onboarding also bereits, wenn dieser Plan den Weichen-Code an die Login-Docks haengt → kein Login-Crash. Dieser Plan enthaelt KEINEN Prod-alembic-Schritt mehr — Task 4 ist reine Verifikation + Live-UAT.

**★ Fable-UAT-Tightening — S1 + S4 auf PFLICHT:** Kein automatisierter Waechter deckt S1 (already-logged-in Redirect) und S4 (OAuth login+create, beide Ruecksprünge durch `_oauth_login_or_create`) ab. Beide sind im Live-UAT (Task 4) PFLICHT-Checklisten-Punkte, nicht optional.

Baut auf Plan 02 (Prod migriert) + Plan 04 (Router + Erstprofil-Seite) → Wave 5.

Purpose: Die Weiche geht scharf — Neu-Registrierung/Login/OAuth landen zustandsgerecht — gegen ein bereits migriertes Prod (kein Schreib-vor-Lese-Crash).
Output: auth.py/oauth.py-Andock + landing.html-JS-Fix + dashboard-Banner + Wiring-Waechter + Live-UAT (S1+S4 Pflicht).

Komplexität: 🔴-nah — Auth-Pfad-Verkabelung + Live-UAT. **Cross-AI (Gemini + Fable) PFLICHT vor Execute.** Supervised Execute (André fuehrt Deploy-Gates + UAT, kein Auto-Advance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-02-PLAN.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-04-PLAN.md

<interfaces>
<!-- Verifizierte Anker (2026-07-08). ⚠ ALLE Zeilen per INHALT re-ankern (Punkt 20, 3x Drift) — vor jedem Edit frisch greppen. -->

Router-Contract (aus Plan 04): `from services.onboarding_routing import post_login_destination`; `post_login_destination(user)` → URL-str ODER None ('nichts tun').

S1 — already-logged-in (VERIFIED routes/auth.py:86-96, `login()`):
```python
    nxt = safe_next(request.args.get('next'))
    if 'user_id' in session:
        return redirect(nxt) if nxt else redirect(url_for('dashboard.index'))   # :91
```
→ VOR dem Standard-Redirect die Weiche einschieben: wenn nxt fehlt, User frisch laden (`db.get(User, session['user_id'])`), `dest = post_login_destination(u)` → falls dest, dorthin; sonst dashboard. ⚠ In `login()` ist g.user NICHT gesetzt (kein login_required) → User frisch laden.

S2 — JSON-Login (VERIFIED routes/auth.py:204-219, `api_login`):
```python
    resp = {'ok': True, 'coach': user_info['is_coach']}
    if nxt:
        resp['next'] = nxt
    return jsonify(resp)                                   # :219
```
→ wenn KEIN nxt: `dest = post_login_destination(<user>)`; falls dest, `resp['next'] = dest`. landing.html:862 (`data.next`) konsumiert es bereits. ⚠ `_do_login`/`_login_user` gibt user_info-dict zurueck (`id` in user_info, auth.py:143 / :105) → User frisch via id laden und post_login_destination(user) rufen (kleinster Eingriff, Punkt 27).

S3 — JSON-Register (VERIFIED routes/auth.py:250-258, `api_register`):
```python
        db.commit()
        _login_user(db, user)
        session['welcome_trial'] = True
        ... send_welcome ...
        return jsonify({'ok': True})                       # :258
```
→ `dest = post_login_destination(user)`; `return jsonify({'ok': True, 'next': dest})`. ⚠ user ist hier das frische Objekt (nach _create_org_and_user + commit) → onboarding_state='pending' (DB-Default 0032). Profil-Count=0 → Weiche gibt Erstprofil-URL. Falls das ORM-Objekt onboarding_state nach flush/commit nicht kennt → `db.refresh(user)` vor dem Weichen-Call.

S3-JS-Fix (VERIFIED templates/marketing/landing.html:918-933, `submitRegister`):
```javascript
    const data=await res.json();
    if(data.ok){window.location='/dashboard';}             // :931 — ignoriert next HEUTE
```
→ auf `window.location = safeNextParam(data.next) || '/dashboard';` umstellen (safeNextParam-Guard :838, + data.next aus S3). +1-Zeilen-Fix.

S4 — OAuth (VERIFIED routes/oauth.py:74-165, `_oauth_login_or_create`):
- Bestand-Pfad: `db.commit()` (:107) → `return redirect(url_for('dashboard.index'))` (:112).
- Neuanlage-Pfad: `db.commit()` (:137) → ... → `return redirect(url_for('dashboard.index'))` (:163).
→ ★ D-19 Commit-Falle: nach `_login_user` + commit den `dest` berechnen, `return redirect(dest or url_for('dashboard.index'))` an BEIDEN Ruecksprung-Stellen (:112 + :163). Beide gehen durch dieselbe Funktion → 1 Einbau-Muster, 2 Rueckspruenge. ⚠ die Weiche nutzt eine EIGENE get_session (Plan 04) → stoert die OAuth-Session-Transaktion nicht. Neuanlage: `db.refresh(new_user)` vor dem Weichen-Call falls server_default nicht bekannt.

D-17 (VERIFIED routes/auth.py:189-198, `_create_org_and_user`): setzt `onboarding_done=False` explizit (auth.py:197). onboarding_state kommt via DB-Default 'pending' (0032) — KEIN expliziter Setzer noetig (D-17). Deckt Formular-Register + OAuth-Neuanlage + Invite.

Dashboard-Banner (VERIFIED routes/dashboard.py index() rendert dashboard.html): D-12 Banner 'Noch kein Profil' bei onboarding_state='skipped'. Plan 04 setzt schon `flash('Noch kein Profil ...','warning')` beim Skip. Executor prueft per grep (`grep -n "get_flashed_messages\|flash\|onboarding" templates/dashboard.html templates/base.html`): rendert das Dashboard bereits Flash → Banner sichtbar; sonst Template-Flag `show_no_profile_banner = (g.user.onboarding_state=='skipped')` (persistent, D-12 „Pflicht"). Kleinster Weg (Punkt 27); persistentes Banner empfohlen.
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" + „Verify=Production". -->
- Deploy-Gate: `bash deploy.sh production` (pytest gegen nerve_test, hat 0032). test_login_destination_wiring.py auto-collected.
- ★ Prod-`nerve` ist bereits migriert (Plan 02) → der Weichen-Code liest gegen existierende Spalten. KEIN Prod-alembic-Schritt in diesem Plan.
- Live-Browser-Test mit Test-User (Task 4): Neu-Registrierung → Erstprofil-Seite; Skip → Dashboard+Banner; Bestandsuser → Dashboard/Coach unveraendert; ★ S1 + S4 PFLICHT.
- url_for-Verifikation (routes/CLAUDE.md): onboarding.wizard / dashboard.index / auth.login aufloesbar.
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wiring-Waechter test_login_destination_wiring.py — E2E gegen echte Login-/Register-Pfade + Member-Submit-Gate (Finding 3), ERST-ROT</name>
  <read_first>
    - tests/conftest.py — cleanup_rows, nerve_test-DSN, csrf_client-Muster (AUTH-1 test_signup_journey.py — CSRF via client.application.config).
    - tests/test_signup_journey.py (AUTH-1) — Register-E2E-Vorlage inkl. Cleanup Session->User->Org + @nerve.local-Email (send_welcome-Falle).
    - routes/auth.py:204-258 (api_login/api_register) + services/onboarding_routing.py (Plan 04) + routes/onboarding.py Submit-Handler (Plan 04, Finding-3-Gate).
    - CONTEXT.md D-16 (4 Docks) + D-02 (done→kein Redirect).
    - Gegen HEAD: die Docks sind noch nicht verdrahtet → Register-Response hat kein 'next' auf Erstprofil → Test ROT.
  </read_first>
  <behavior>
    - E2E via test-client gegen echte Routen (API-Response-Assertion, KEIN source-presence). Real-PG nerve_test (Skip-Guard). CSRF im Test AN (client.application.config, AUTH-1-Muster).
    - `test_register_owner_lands_on_erstprofil`: POST /api/register (Email @nerve.local, Owner-Neuanlage) → assert status 200 UND resp.json()['next'] == '/onboarding/'. Cleanup Session->User->Org.
    - `test_login_done_user_no_onboarding_redirect`: User mit onboarding_state='done' anlegen, POST /api/login → resp.json() hat KEIN 'next' auf /onboarding/ (D-02; ok:true). Cleanup.
    - ★ `test_member_submit_rejected` (Finding 3): einen Member-User + Session anlegen, POST /onboarding/ (Submit mit branche_key+produkt) → assert 403 ODER Redirect (nicht 200-mit-Profil-angelegt); Gegenprobe re-SELECT: kein neues Profil in der org (Count unveraendert). Owner-Gegenprobe (optional): owner-Submit legt Profil an. Cleanup Profile->Session->User->Org.
    - ASCII Namen; committende Rows raeumen.
  </behavior>
  <action>
    Erstelle `tests/test_login_destination_wiring.py` (AUTH-1 test_signup_journey.py als Vorlage — csrf_client, cleanup_tracker, @nerve.local-Email).

    `test_register_owner_lands_on_erstprofil`: `assert resp.get_json()['next'] == '/onboarding/'`.
    `test_login_done_user_no_onboarding_redirect`: User done anlegen, POST /api/login → `data=resp.get_json(); assert data['ok']; assert data.get('next') != '/onboarding/'`.
    ★ `test_member_submit_rejected`: Member-Session, `client.post('/onboarding/', data={'branche_key':'SaaS','produkt':'x', ...csrf...})` → `assert resp.status_code in (302, 403)`; danach `SELECT count(*) FROM profiles WHERE org_id=<member-org>` unveraendert (Member konnte kein Profil anlegen — Finding 3).

    SEQUENZ (ERST-ROT): zuerst committen+deployen OHNE Plan-04/05-Docks → gegen HEAD gibt Register-Response kein 'next'==/onboarding/ → `test_register_owner_lands_on_erstprofil` ROT. (test_member_submit_rejected setzt den Plan-04-Submit-Handler voraus — laeuft in dieser Phase erst gruen nachdem Plan 04 executet ist; dokumentiere die Reihenfolge.) DANN Task 2/3 → gruen.
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_login_destination_wiring.py auto-collected. VOR Task 2/3: test_register_owner_lands_on_erstprofil ROT. NACH Task 2/3: gruen, kein Row-Leak.</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert; grep zeigt echte `client.post('/api/register'...)` + `resp.get_json()['next']`-Assertion (KEIN source-presence).
    - grep zeigt `== '/onboarding/'` + den done→kein-Redirect-Test.
    - ★ grep zeigt `test_member_submit_rejected` mit POST /onboarding/ als Member → 403/302 + Profil-Count-unveraendert-Assertion (Finding 3).
    - CSRF via client.application.config; @nerve.local-Email; Cleanup Profile->Session->User->Org; Skip-Guard nicht-postgresql.
    - Rot-Beleg dokumentiert.
  </acceptance_criteria>
  <done>test_login_destination_wiring.py existiert, E2E gegen echte Register-/Login-Pfade + Member-Submit-Gate (Finding 3), gegen HEAD nachweislich ROT, sauberer Cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Andock S1+S2+S3 in auth.py + S3-JS-Fix in landing.html</name>
  <read_first>
    - `grep -n "def login\|def api_login\|def api_register\|user_id' in session\|resp\['next'\]\|return jsonify({'ok': True})" routes/auth.py` (S1 :91, S2 :216-219, S3 :258 per INHALT frisch ankern).
    - `grep -n "window.location='/dashboard'\|data.next\|safeNextParam" templates/marketing/landing.html` (S3-JS :931 + safeNextParam :838 per INHALT).
    - services/onboarding_routing.py (Plan 04 Contract).
    - CONTEXT.md D-16 (S1/S2/S3) + D-17 (kein expliziter Setzer, DB-Default).
  </read_first>
  <action>
    Import oben in auth.py: `from services.onboarding_routing import post_login_destination`.

    S1 (`login()`, ~:86-96): im `if 'user_id' in session:`-Zweig, wenn KEIN nxt: User frisch laden (`db = get_session(); u = db.get(User, session['user_id']); db.close()`), `dest = post_login_destination(u)`; `return redirect(nxt or dest or url_for('dashboard.index'))`.

    S2 (`api_login`, ~:216-219): nach `resp = {'ok': True, 'coach': ...}`, wenn KEIN nxt: User frisch via user_info['id'] laden, `dest = post_login_destination(u)`; `if dest: resp['next'] = dest`. (nxt behaelt Vorrang.)

    S3 (`api_register`, ~:250-258): nach `_login_user(db, user)`, `dest = post_login_destination(user)`; `return jsonify({'ok': True, 'next': dest})`. ⚠ sicherstellen dass user.onboarding_state 'pending' ist (DB-Default 0032) — falls das ORM-Objekt es nicht kennt, `db.refresh(user)` vor dem Weichen-Call. Kein expliziter onboarding_state-Setzer (D-17).

    S3-JS-Fix (landing.html ~:931): `window.location='/dashboard';` ersetzen durch `window.location = safeNextParam(data.next) || '/dashboard';`. +1-Zeilen-Fix; die anderen Landing-Fetches unangetastet.

    Umlaute: JS-Identifier ASCII; keine User-facing Strings geaendert. D-17: KEIN onboarding_state=... in _create_org_and_user (grep-belegen).
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_login_destination_wiring.py::test_register_owner_lands_on_erstprofil GRUEN (S3 gibt next). Danach Live (Task 4): Neu-Registrierung → landet auf /onboarding/.</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/auth.py zeigt `from services.onboarding_routing import post_login_destination` + `post_login_destination(` an S1, S2, S3 (3 Aufrufe).
    - grep zeigt S3: `jsonify({'ok': True, 'next': dest})`.
    - grep zeigt S2: `resp['next'] = dest` nur wenn kein nxt.
    - grep zeigt S1: `redirect(nxt or dest or url_for('dashboard.index'))`.
    - grep von landing.html zeigt `window.location = safeNextParam(data.next) || '/dashboard'`; die anderen Fetches unveraendert.
    - grep bestaetigt: `_create_org_and_user` hat KEINEN `onboarding_state=`-Setzer (D-17).
    - deploy.sh production Gate gruen.
  </acceptance_criteria>
  <done>S1/S2/S3 rufen post_login_destination; S3-Response traegt das Erstprofil-Ziel; landing.html nutzt data.next; kein expliziter onboarding_state-Setzer (DB-Default D-17); Wiring-Waechter gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Andock S4 in oauth.py (VOR commit/redirect, D-19) + Skip-Banner im Dashboard (D-12)</name>
  <read_first>
    - `grep -n "db.commit()\|return redirect(url_for('dashboard.index'))\|_oauth_login_or_create" routes/oauth.py` (Bestand-commit :107 + Redirect :112; Neuanlage-commit :137 + Redirect :163 — per INHALT frisch ankern; D-19 Commit-Falle).
    - `grep -n "get_flashed_messages\|flash\|onboarding" templates/dashboard.html templates/base.html` — pruefen ob Dashboard bereits Flash rendert.
    - routes/dashboard.py index() — wo `render_template('dashboard.html', ...)`; ggf. `show_no_profile_banner` ergaenzen.
    - services/onboarding_routing.py (Contract) + CONTEXT.md D-19 + D-12.
  </read_first>
  <action>
    (A) oauth.py — Import oben: `from services.onboarding_routing import post_login_destination`. An BEIDEN Ruecksprung-Stellen (Bestand ~:112, Neuanlage ~:163) den harten `return redirect(url_for('dashboard.index'))` ersetzen durch:
    ```python
    dest = post_login_destination(user)        # Bestand: user ; Neuanlage: new_user
    return redirect(dest or url_for('dashboard.index'))
    ```
    ⚠ D-19: die Weiche wird NACH `_login_user` + `db.commit()` (:107/:137) berechnet, aber der Redirect nutzt ihr Ergebnis → die Weiche laeuft VOR dem tatsaechlichen Redirect. Eigene get_session (Plan 04) → stoert OAuth-Tx nicht. Neuanlage: `db.refresh(new_user)` vor dem Weichen-Call falls server_default nicht bekannt. 1 Einbau-Muster, 2 Rueckspruenge (D-16 S4).

    (B) Skip-Banner (D-12): den von Plan 04 gesetzten `flash('Noch kein Profil ...','warning')` sichtbar machen. Kleinster Weg (Punkt 27, per grep entscheiden):
      - Wenn dashboard.html/base.html bereits `get_flashed_messages()` rendert → nichts weiter noetig (grep-belegen + Live-Check).
      - Wenn NICHT: in dashboard.py index() `show_no_profile_banner = (getattr(g.user,'onboarding_state',None)=='skipped')` ans Template + in dashboard.html ein persistentes Banner (nerve.css n-* Alert). Text: „Noch kein Profil — lege eins an, sonst ist Training nicht nutzbar." + „Profil anlegen"-Link auf url_for('onboarding.wizard').
    Empfehlung: persistentes Banner (Template-Flag), weil D-12 „Pflicht-Banner" dauerhaft meint bis Profil existiert.

    Umlaute: HTML-IDs/Flags ASCII (`show_no_profile_banner`); Banner-Text mit Umlauten.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen. Live (Task 4): OAuth-Neuuser → Erstprofil; Skip → Dashboard zeigt persistentes Banner.</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/oauth.py zeigt `post_login_destination(` 2x (Bestand + Neuanlage) + `redirect(dest or url_for('dashboard.index'))` an beiden Ruecksprung-Stellen.
    - grep bestaetigt: post_login_destination-Aufruf steht VOR dem finalen redirect (D-19).
    - Skip-Banner: grep von dashboard.html zeigt `Noch kein Profil` + (falls Flag-Weg) `show_no_profile_banner`; Banner-Link auf onboarding.wizard.
    - deploy.sh production Gate gruen.
  </acceptance_criteria>
  <done>S4 (OAuth Bestand + Neuanlage) rufen post_login_destination VOR dem Redirect (D-19); Skip-User sieht persistentes Dashboard-Banner 'Noch kein Profil' (D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: ★ Live-Browser-UAT mit Test-User — S1 + S4 PFLICHT (Fable-UAT-Tightening) + Verifikation dass Prod bereits migriert ist (Plan 02)</name>
  <read_first>
    - Plan-02-SUMMARY (Prod auf 0032, 3 User = 'done') + Plan-03-SUMMARY (ORM-Leser live, kein Login-Crash).
    - inspect.sh (schema users / migrations / logs-errors).
    - CONTEXT.md D-16 (4 Docks) + Fable-Review „Klein — S1 + S4 UAT auf PFLICHT".
  </read_first>
  <what-built>
    Plan 02 migrierte Prod-`nerve` (0032), Plan 03 schaltete den ORM-Leser scharf, Plan 04 baute Router + Erstprofil-Seite, dieser Plan haengt post_login_destination an alle 4 Login-Docks (S1-S4) + macht das Skip-Banner sichtbar. Prod ist bereits migriert → kein Schreib-vor-Lese-Crash (Finding 1 in Plan 02 aufgeloest). Dieser Checkpoint verifiziert das End-zu-End-Verhalten im Browser.
  </what-built>
  <how-to-verify>
    Vorab (kein Prod-alembic-Schritt mehr — der ist Plan 02):
    - `inspect.sh migrations` → alembic 0032 (aus Plan 02). `inspect.sh schema users` → onboarding_state/skip_onboarding da.
    - `inspect.sh logs-errors` → kein `column onboarding_state does not exist` nach dem Deploy dieses Plans (Leser liest gegen existierende Spalte).

    Live-Browser-UAT mit Test-User (Sicht statt Zusicherung) — ★ ALLE PFLICHT, S1 + S4 ausdruecklich nicht optional:
    1. **(S3) Neu-Registrierung (Formular)** → landet auf Erstprofil-Seite (/onboarding/).
    2. **Erstprofil absenden** (Branche + Feld, als Owner) → Dashboard, aktives Profil gesetzt (Training nicht 404).
    3. **(Finding 3) Member-Submit** (falls Member-Test-User verfuegbar): POST auf /onboarding/ als Member → abgewiesen (kein Profil angelegt).
    4. **Skip auf Erstprofil** → Dashboard mit persistentem Banner „Noch kein Profil".
    5. **★ (S1) PFLICHT** — bereits-eingeloggter pending-Owner: mit einem pending-Owner (0 Profile) eingeloggt bleiben, `/login` erneut aufrufen → Redirect auf /onboarding/ (nicht Dashboard). Kein automatisierter Waechter deckt das ab.
    6. **(done-User) Bestehender done-User** (z.B. admin) Login → Dashboard/Coach unveraendert (kein /onboarding/-Redirect).
    7. **★ (S4) PFLICHT** — OAuth login+create: OAuth-Neuuser (Google/Microsoft) → Erstprofil-Seite; bestehender OAuth-done-User → Dashboard. Beide Ruecksprünge durch `_oauth_login_or_create`. Kein automatisierter Waechter deckt das ab.
  </how-to-verify>
  <resume-signal>Tippe „UAT gruen" MIT expliziter Bestaetigung von S1 (Punkt 5) und S4 (Punkt 7) — beide Pflicht — ODER beschreibe Abweichungen. Bei S1/S4-Fehler: die betroffene Andock-Stelle nachbessern (Plan 05 Task 2/3), nicht als „optional" abhaken.</resume-signal>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN via post_login_destination an 4 Docks: onboarding_state/skip_onboarding/rolle/active_profile_id; SCHREIBEN indirekt via Router-Selbstheilung + Erstprofil aus Plan 04). ★ Prod-`nerve` ist bereits auf 0032 (Plan 02) — dieser Plan migriert NICHT.

### inspect.sh-Beleg (VERBATIM `\d users` Prod-nerve — nach Plan 02/03)
```
users (public) — relevante Spalten (Prod-nerve, nach Plan 02/03):
  id                integer      NOT NULL (PK)
  org_id            integer      NOT NULL  FK→organisations(id)
  rolle             varchar(50)  NULL
  active_profile_id integer      NULL       FK→profiles(id)
  onboarding_state  text         NOT NULL DEFAULT 'pending', CHECK  ← aus Plan 02 Migration
  skip_onboarding   boolean      NOT NULL DEFAULT false             ← aus Plan 02 Migration
  onboarding_done   boolean      NULL       ← DEPRECATED (nur Leser)

Prod-Stand: alembic 0032 (Plan 02), 3 users onboarding_state='done'.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg) + kann Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `g.user.onboarding_state` | gelesen in post_login_destination (S1-S4) | DB-Spalte users.onboarding_state (Plan 02) | ★ Prod-Ebene bereits aufgeloest: die Spalte existiert seit Plan 02 (1b) — der Leser (dieser Plan, Docks) laeuft NICHT voraus. Innerhalb Request synchron (login_required auth.py:51) | ✓ Plan 02/03 + login_required |
| `user.skip_onboarding` | gelesen Stufe 1 | DB-Spalte (Plan 02) | wie oben; existiert auf Prod seit Plan 02 | ✓ Plan 02 |
| Register-Response `next` | S3 schreibt Ziel in JSON → landing.html window.location | RAM/Request-scope + Client-JS | synchron pro Request | ✓ landing.html data.next |
| OAuth-Redirect `dest` | S4 berechnet VOR Redirect (D-19) | RAM/Request-scope | synchron; Weiche VOR redirect | ✓ D-19 Reihenfolge |
| `new_user.onboarding_state` | S3/S4 lesen 'pending' nach Anlage (DB-Default) | DB-Spalte via server_default (Plan 02/03) | ★ Request-Ebene: _create_org_and_user flush/commit setzt server_default 'pending'; falls ORM-Objekt es nicht kennt → db.refresh(user) VOR dem Weichen-Call (Task 2/3) → Leser liest garantiert 'pending' | ✓ db.refresh-Absicherung Task 2/3 |

### Punkt 26 Schreib-vor-Lese-Analyse
★ Die Prod-Ebenen-Schreib-vor-Lese-Naht (D-23 / Finding 1) ist NICHT mehr in diesem Plan — sie wurde nach Plan 02 (Prod-Migration, Schritt 1b) verschoben, das VOR Plan 03 (ORM-Leser) und Plan 05 (Docks) lief. Es bleibt nur die Request-Ebene: S3/S4 lesen `new_user.onboarding_state` direkt nach Anlage. Der server_default 'pending' greift auf DB-Ebene; das ORM-Objekt kennt ihn nach flush evtl. nicht → `db.refresh(user)` vor dem Weichen-Call sichert 'pending' (sonst faellt die Weiche faelschlich in Stufe 4). Explizit in Task 2/3 verankert.

### Bei Diskrepanz: STOP + Replan.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anonymer/eingeloggter Browser → /api/register, /api/login, /login, OAuth-Callback | Login-Pfade, die jetzt die Weiche fahren |
| Client-JS landing.html → window.location aus data.next | Redirect-Ziel aus Server-Response |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH2-11 | Elevation/Bypass (OAuth redirectet am Router vorbei) | oauth.py commit/redirect-Reihenfolge | mitigate | D-19: post_login_destination VOR dem finalen redirect an beiden Ruecksprung-Stellen; eigene get_session stoert OAuth-Tx nicht |
| T-AUTH2-12 | Tampering (Open-Redirect via data.next) | landing.html window.location | mitigate | safeNextParam(data.next)-Guard (landing.html:838, AUTH-1) validiert same-origin; dest kommt server-seitig aus url_for (kein User-Input) |
| T-AUTH2-13 | Info-Leak (stale/None onboarding_state → falsche Weiche fuer Neuuser) | S3/S4 Read direkt nach Anlage | mitigate | db.refresh(user) vor Weichen-Call → server_default 'pending' garantiert gelesen |
| T-AUTH2-15 | ★ Bypass (S1/S4 nicht abgedeckt → Weiche stumm auf diesen Pfaden) | S1 already-logged-in + S4 OAuth (kein Waechter) | mitigate | S1 + S4 als PFLICHT-UAT-Checkliste (Task 4, Fable-Tightening); Resume-Signal verlangt explizite S1+S4-Bestaetigung |
</threat_model>

<verification>
- deploy.sh production Gate gruen MIT test_login_destination_wiring.py PASSED (real-PG), inkl. Member-Submit-Gate (Finding 3).
- test_register_owner_lands_on_erstprofil: ROT gegen HEAD, gruen nach Andock — dokumentiert.
- ★ Prod bereits migriert (Plan 02) — inspect.sh migrations 0032, logs-errors kein column-does-not-exist.
- OAuth (S4) redirectet nicht am Router vorbei (D-19).
- ★ Live-UAT: S1 (Punkt 5) + S4 (Punkt 7) PFLICHT bestaetigt; Neu-Register→Erstprofil; Member-Submit abgewiesen; Skip→Dashboard+Banner; done-User→Dashboard/Coach unveraendert.
</verification>

<success_criteria>
- post_login_destination an allen 4 Docks (S1-S4); OAuth VOR commit/redirect (D-19).
- landing.html nutzt data.next (S3-JS-Fix); Register-Response traegt Erstprofil-Ziel.
- Skip-User sieht persistentes Dashboard-Banner (D-12).
- ★ Prod-Migration ist NICHT Teil dieses Plans (Finding 1: in Plan 02) — der Weichen-Code liest gegen bereits migriertes Prod.
- Wiring-Waechter im Deploy-Gate, gegen HEAD rot gewesen; Member-Submit-Gate (Finding 3) getestet.
- ★ Live-UAT S1 + S4 PFLICHT bestaetigt (Fable-Tightening).
- Kein expliziter onboarding_state-Setzer (DB-Default D-17).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-05-SUMMARY.md` erstellen (inkl. Rot-Beleg des Wiring-Waechters, Bestaetigung dass Prod via Plan 02 migriert war, Finding-3-Member-Gate-Beleg, Live-UAT-Beleg inkl. expliziter S1 + S4 PFLICHT-Bestaetigung).
</output>
