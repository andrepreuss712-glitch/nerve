---
phase: 08.23.2.AUTH-2
plan: 01
type: execute
wave: 1
complexity: "🟡 mittel (Schema/Migration + Landmine-Stilllegung — Auth-Fundament; Cross-AI Pflicht VOR Execute, dieser Plan wird NICHT direkt executet)"
depends_on: []
files_modified:
  - alembic/versions/0032_onboarding_state_skip_onboarding.py
  - database/models.py
  - app.py
  - tests/test_onboarding_state_migration.py
autonomous: true
requirements: []   # keine REQ-IDs gemappt; Scope = CONTEXT-Decisions D-05/D-06/D-07/D-08/D-09/D-10
user_setup: []

must_haves:
  truths:
    - "Die Spalte users.onboarding_state existiert auf nerve_test nach 0032 (TEXT, NOT NULL, Default 'pending')"
    - "Alle vor 0032 bestehenden User haben onboarding_state='done' (Backfill — kein Zwangs-Onboarding fuer Bestand, Anti-Block-C)"
    - "Die Spalte users.skip_onboarding existiert (boolean, Default FALSE)"
    - "Ein CHECK-Constraint erlaubt onboarding_state IN ('pending','done','skipped') so, dass 'pending' = alles-ausser-done/skipped bedeutet (D-09 Tueroeffner fuer spaetere step_*-Zustaende)"
    - "onboarding_state + skip_onboarding tragen ein Postgres-Schild (COMMENT); onboarding_done traegt einen DEPRECATED-Marker (eingefroren, nicht gedroppt)"
    - "Der Startup-Flip app.py (_migrate 08.12 UPDATE onboarding_done=1) beruehrt onboarding_state NIE (stillgelegt/kommentiert, nicht portiert)"
    - "Der Waechter test_onboarding_state_migration faengt gegen ein Schema OHNE 0032 (ERST-ROT) und wird gruen nach 0032"
  artifacts:
    - path: "alembic/versions/0032_onboarding_state_skip_onboarding.py"
      provides: "additive Migration: onboarding_state (TEXT NOT NULL Default pending, Backfill done, CHECK) + skip_onboarding (boolean Default FALSE) + Schilder + DEPRECATED-Marker onboarding_done"
      contains: "down_revision = '0031'"
    - path: "database/models.py"
      provides: "User.onboarding_state + User.skip_onboarding ORM-Spalten mit comment= (Schild) + onboarding_done comment= mit [DEPRECATED]-Marker"
      contains: "onboarding_state"
    - path: "app.py"
      provides: "Landmine-Stilllegung: _migrate-08.12-Flip + Legacy-ADD-COLUMN + _seed onboarding_done kommentiert 'NIE auf onboarding_state portieren'"
      contains: "onboarding_state"
    - path: "tests/test_onboarding_state_migration.py"
      provides: "real-PG Migrations-/Backfill-Waechter gegen nerve_test (Spalten existieren, alle Rows done, skip_onboarding Default, CHECK aktiv), ERST-ROT"
      contains: "onboarding_state"
  key_links:
    - from: "alembic/versions/0032"
      to: "users.onboarding_state / users.skip_onboarding"
      via: "op.add_column + UPDATE-Backfill + CHECK-Constraint + COMMENT"
      pattern: "onboarding_state"
    - from: "database/models.py User"
      to: "onboarding_state Schild + onboarding_done DEPRECATED-Marker"
      via: "Column(..., comment=...)"
      pattern: "comment="
---

<objective>
Das Fundament der Onboarding-Weiche: die neue Wahrheitsquelle `users.onboarding_state` (pending/done/skipped) + der Interlock-Blocker `users.skip_onboarding` als additive Alembic-Migration 0032, plus die Stilllegung der Startup-Flip-Landmine (D-10), damit die neue Spalte nicht bei jedem App-Start tot-gesetzt wird.

**Kein Router, keine UI, keine Verkabelung in diesem Plan** — nur Schema + Schild + Landmine + Migrations-Waechter. Dieser Plan liefert NUR SCHREIB-Seite (Spalten anlegen + Backfill); der lesende Weichen-Code kommt in Plan 02/03. Damit ist die Deploy-Sequenz-Falle (D-23: lesender Code vor Spalten-Existenz → Login-Crash) auf Plan-Ebene entschaerft: dieser Plan geht zuerst live (Spalte existiert), erst dann liest Code sie.

Purpose: Neue Truth-Source für `post_login_destination()` bereitstellen ohne den Bestand ins Onboarding zu zwingen (Backfill 'done'), und die Halb-Migration-Falle (Punkt 29) am `onboarding_done` sauber einfrieren.
Output: Migration 0032 + models.py-Schilder + app.py-Landmine-Stilllegung + ERST-ROT-Migrations-Waechter.

Komplexität: 🟡 mittel — Schema/Migration + Auth-Fundament. **Cross-AI (Gemini + Fable) PFLICHT vor Execute** (CLAUDE.md GSD-Workflow-Pflichten: Migrations-Logik-Trigger + Punkt 24). Dieser Plan wird NICHT direkt executet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md

<interfaces>
<!-- Verifizierte Anker (2026-07-08 gegen echten Code + Prod-ssh). ⚠ ALLE Zeilennummern per INHALT re-ankern (Punkt 20, 3x Drift in dieser Phase) — vor jedem Edit frisch greppen. Zeilen = Wegweiser, kein Vertrag. -->

Migrations-Muster (VERIFIED alembic/versions/0031_intent_event_handling_status_schild.py):
```python
from alembic import op
revision = '0031'
down_revision = '0030'
def upgrade():
    op.execute("COMMENT ON COLUMN intent_event.handling_status IS '...'")
def downgrade():
    op.execute("COMMENT ON COLUMN ...")
```
→ 0032 hat `down_revision = '0031'`. Naming-Muster: `0032_<snake>.py`. Prod-`nerve` steht auf alembic 0030; 0031 ist reine COMMENT-Migration (idempotent) und laeuft gefahrlos mit 0032 zusammen hoch (D-22).

User-Modell (VERIFIED database/models.py ~:85):
```python
onboarding_done = Column(Boolean, default=False, comment='Flag: Onboarding abgeschlossen')
```
→ NEUE Spalten daneben ergaenzen. onboarding_done bleibt (D-06, eingefroren) — comment= auf [DEPRECATED]-Marker aktualisieren, KEIN Drop.

★ LANDMINE-BEFUND (VERIFIED 2026-07-08, entschaerft die D-10-Panik, aber Stilllegung bleibt Pflicht):
- `_migrate()` (app.py ~:133) macht bei `db_url.startswith('postgresql')` einen **early return** (app.py ~:140-142: `[DB] _migrate skipped on Postgres`). D.h. der 08.12-Startup-Flip `UPDATE users SET onboarding_done=1` (app.py ~:831-846) UND die Legacy-ADD-COLUMN-Liste (`('onboarding_done', 'BOOLEAN DEFAULT 0')` app.py ~:155) laufen auf Prod-Postgres **NIE**. `_seed()` (app.py ~:1623) setzt `onboarding_done=True` (app.py ~:1643) nur bei `User.count()==0` → auf Prod (3 User) **NIE**.
- FOLGE: die Landmine ist auf Prod bereits inert. ABER D-10 verlangt Stilllegung/Kommentar, damit niemand das Flip-Muster jemals auf `onboarding_state` PORTIERT (dann waere onboarding_state bei jedem SQLite-Start/Seed tot). → Nur Kommentar-Warnung setzen, KEINE Verhaltensaenderung noetig (das Muster ist Postgres-inert).

Prod-Ist-Stand (VERIFIED ssh 2026-07-08): alembic_version=0030; users=3 (onboarding_done f=1/t=2); onboarding_state + skip_onboarding existieren NICHT → 0032 rein additiv.

Deploy-Gate-Mechanik (VERIFIED deploy.sh:140-205): baut `nerve_test` per `pg_dump --schema-only` vom Prod-`nerve`, stampt alembic_version=prod-head (0030), faehrt `alembic upgrade head` (wendet 0031+0032 an), dann `pytest tests/ -m "not live and not perf"` als `nerve_app` gegen `DATABASE_URL=postgresql://nerve_app@/nerve_test`. → Ein neuer test_*.py OHNE live/perf-Marker laeuft automatisch mit, gegen ein nerve_test DAS 0032 SCHON HAT. **Prod-`nerve` selbst migriert deploy.sh NICHT** (nur nerve_test) — der manuelle Prod-Migrations-Schritt kommt in Plan 03 (D-23).
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR ueber Production-Pfad. KEIN lokales pytest, KEIN python app.py, KEIN lokaler Alembic-Trockenlauf. -->
- Deploy-Gate: `bash deploy.sh production` provisioniert nerve_test (pg_dump vom Prod-nerve + alembic upgrade head inkl. 0032) und faehrt pytest dagegen VOR dem Restart. Rot = kein Restart, alter Code bleibt live.
- Live-Schema/Migrations-Stand: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema users'` / `inspect.sh migrations`.
- Prod-`nerve` wird in DIESEM Plan NICHT manuell migriert (das ist Plan 03, gekoppelt mit dem Code-Restart). Dieser Plan liefert nur den Migrations-CODE; der deploy.sh-Gate testet ihn gegen nerve_test.
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations-/Backfill-Waechter test_onboarding_state_migration.py — real-PG gegen nerve_test, ERST-ROT gegen HEAD</name>
  <read_first>
    - tests/conftest.py — `cleanup_rows` (~:238), `_baseline_cleanup_guard` autouse (~:549), das nerve_test-DSN-Muster (`DATABASE_URL=postgresql://nerve_app@/nerve_test`, deploy.sh:195). Real-PG-Test-Muster: grep `DATABASE_URL` in bestehenden tests/test_*.py die gegen nerve_test laufen (z.B. tests/test_logs_org_boundary.py aus AUTH-LOGS-TENANT — Muster fuer real-PG-Query + skip-wenn-nicht-PG).
    - deploy.sh:192-205 — pytest laeuft als nerve_app gegen nerve_test; nerve_test HAT nach `alembic upgrade head` die Spalten 0032. Der Test muss SKIPpen wenn kein Postgres-DSN (kein False-Green auf SQLite, Muster wie test_schild_guard.py).
    - alembic/versions/0031_intent_event_handling_status_schild.py — Struktur-Vorlage (Task 2 baut 0032 danach).
    - CONTEXT.md D-05/D-07/D-08/D-09 (Spalten-Spezifikation + Backfill + CHECK).
  </read_first>
  <behavior>
    - Der Test fragt das ECHTE nerve_test-Schema/Daten ab (Function-Call-/DB-Query-Assertion, KEIN source-presence-grep — CLAUDE.md Test-Qualitaets-Regel). Er skippt, wenn `DATABASE_URL` nicht postgresql ist (kein False-Green lokal/SQLite; Muster test_schild_guard.py).
    - Assertion A (Spalten-Existenz): Query auf `information_schema.columns WHERE table_name='users' AND column_name IN ('onboarding_state','skip_onboarding')` → beide vorhanden; `onboarding_state`.data_type = 'text', is_nullable='NO'; `skip_onboarding`.data_type = 'boolean'.
    - Assertion B (Backfill / Default): der Test legt ueber den echten Registrier-Pfad ODER direkt via INSERT einen User an OHNE onboarding_state zu setzen → nach Insert ist onboarding_state='pending' (DB-Default greift). PLUS: es gibt in nerve_test 0 User mit onboarding_state NULL (Backfill/NOT-NULL). (Bestands-Backfill 'done' wird auf Prod separat in Plan 03 verifiziert — nerve_test ist frisch gedumpt, hat evtl. keine Alt-User; hier reicht Default-Greifen + NOT-NULL + keine NULLs.)
    - Assertion C (CHECK-Constraint): ein INSERT/UPDATE mit onboarding_state='bogus_value' wirft einen IntegrityError (CHECK verbietet Nicht-Whitelist-Werte) — Assertion via `pytest.raises`. Ein INSERT mit 'skipped' geht durch (Whitelist-Wert).
    - Assertion D (skip_onboarding Default): neuer User ohne skip_onboarding → skip_onboarding IS FALSE.
    - Committende Rows raeumen sich im Teardown via cleanup_rows (Baseline-Sauberkeit, Punkt Test-Cleanup-Regel). Cleanup-Reihenfolge Session->User->Organisation falls ueber Registrier-Pfad angelegt (FK sessions.user_id ohne ondelete — Muster AUTH-1 Plan 01).
    - ASCII: Test-/Fixture-Namen + dict-keys ASCII. User-facing gibt es keine.
  </behavior>
  <action>
    Erstelle `tests/test_onboarding_state_migration.py`.

    Skip-Guard oben: wenn `os.environ.get('DATABASE_URL','')` NICHT mit 'postgresql' beginnt → `pytest.skip('real-PG only (nerve_test); kein False-Green auf SQLite', allow_module_level=True)` (Muster test_schild_guard.py).

    `test_columns_exist`: SQLAlchemy `text()`-Query auf `information_schema.columns` fuer table_name='users', column_name IN ('onboarding_state','skip_onboarding'). Assert beide da; onboarding_state data_type=='text' UND is_nullable=='NO'; skip_onboarding data_type=='boolean'.

    `test_no_null_and_default_pending`: (a) `SELECT count(*) FROM users WHERE onboarding_state IS NULL` == 0 (NOT-NULL/Backfill). (b) Insert einer minimalen User+Org-Row OHNE onboarding_state (via echtem Anlage-Pfad `_create_org_and_user` ODER direktes INSERT mit den Pflichtfeldern) → re-SELECT onboarding_state == 'pending' (DB-Default). Row-IDs im cleanup-Tracker (Session->User->Org).

    `test_check_constraint_rejects_bogus`: `with pytest.raises(Exception)` (IntegrityError) → `UPDATE users SET onboarding_state='bogus_value' WHERE id=<test-user>`; rollback danach. Gegenprobe: `UPDATE ... SET onboarding_state='skipped'` geht durch (kein Raise), dann zuruecksetzen.

    `test_skip_onboarding_default_false`: neuer User ohne skip_onboarding → SELECT skip_onboarding IS FALSE.

    SEQUENZ (André „ERST rot, DANN Migration"): Diesen Test ZUERST committen+deployen OHNE 0032 (Task 2) — gegen HEAD (nerve_test hat 0032 noch nicht) MUSS `test_columns_exist` ROT sein (Spalten fehlen → Gate blockt Restart). DANN Task 2 (0032) → gruen. Rot-Beleg ins SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # pytest-Gate auf nerve_test, test_onboarding_state_migration.py auto-collected. VOR Task 2: test_columns_exist MUSS ROT sein (Spalten fehlen, Gate blockt). NACH Task 2: gruen, kein Row-Leak.</automated>
  </verify>
  <acceptance_criteria>
    - Datei `tests/test_onboarding_state_migration.py` existiert; grep zeigt `information_schema.columns` (echte DB-Query, KEIN getsource/read-file/grep-auf-Quelle).
    - grep zeigt Skip-Guard `allow_module_level=True` fuer nicht-postgresql DATABASE_URL.
    - grep zeigt Assertions: data_type == 'text', is_nullable == 'NO', `pytest.raises` fuer den Bogus-CHECK, `skip_onboarding` IS FALSE.
    - Cleanup via cleanup_rows, Reihenfolge Session->User->Organisation (grep zeigt Session-Key vor User-Key falls ueber Anlage-Pfad).
    - Rot-Beleg dokumentiert: gegen HEAD (vor Task 2) blockt `bash deploy.sh production` am roten `test_columns_exist`.
    - Test-/Fixture-Namen ASCII.
  </acceptance_criteria>
  <done>test_onboarding_state_migration.py existiert, real-PG gegen nerve_test, gegen HEAD nachweislich ROT (Spalten fehlen), prueft Existenz+Backfill/Default+CHECK+skip_onboarding-Default via echte DB-Queries, sauberer Cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0032 + models.py-Schilder + onboarding_done DEPRECATED-Marker</name>
  <read_first>
    - `grep -n "class User" database/models.py` + die onboarding_done-Zeile frisch: `grep -n "onboarding_done" database/models.py` (per INHALT ankern, ~:85). Trivial-Spalten-Konvention Punkt 23 (id/created_at/*_id/is_* NICHT kommentieren — onboarding_state/skip_onboarding sind NICHT-trivial → Schild Pflicht).
    - alembic/versions/0031_intent_event_handling_status_schild.py — op.execute(COMMENT)-Muster + revision/down_revision-Kopf.
    - `ls alembic/versions/` — bestaetigen dass 0031 die hoechste ist (0032 folgt).
    - CONTEXT.md D-05 (TEXT pending/done/skipped) / D-07 (EIN Schritt: Default pending + UPDATE alle done + dann NOT NULL) / D-08 (skip_onboarding boolean Default FALSE) / D-09 (CHECK als Tueroeffner) / D-06 (onboarding_done einfrieren).
    - Punkt 23 §0.2 Schild-Inhalt: „<Zweck/Business-Logik>. Status: <lebt|...>. Schreibt <datei:zeile>; liest <datei>."
  </read_first>
  <action>
    (A) Erstelle `alembic/versions/0032_onboarding_state_skip_onboarding.py`.

    Kopf:
    ```python
    revision = '0032'
    down_revision = '0031'
    branch_labels = None
    depends_on = None
    ```

    `upgrade()` — EXAKTE Reihenfolge (D-07, EIN Schritt, Anti-Block-C):
    1. `op.add_column('users', sa.Column('onboarding_state', sa.Text(), nullable=True, server_default='pending'))`  # erst nullable, damit Backfill greift
    2. `op.execute("UPDATE users SET onboarding_state = 'done'")`  # ALLE Bestands-User → done (kein Zwangs-Onboarding, Anti-Block-C, D-07)
    3. `op.alter_column('users', 'onboarding_state', nullable=False)`  # jetzt gefahrlos NOT NULL
    4. CHECK-Constraint (D-09, Tueroeffner): `op.create_check_constraint('ck_users_onboarding_state', 'users', "onboarding_state IN ('pending','done','skipped')")`
       KOMMENTAR im Migrations-File genau ueber der CHECK-Zeile (D-09 Tueroeffner-Semantik, VERBATIM):
       „D-09 Tueroeffner: die Weiche liest 'pending' = alles ausser done/skipped. Der spaetere Voll-Wizard darf step_*-Zwischenzustaende per CHECK-ERWEITERUNG (neue Werte in die IN-Liste) ergaenzen, OHNE die Weiche anzufassen — pending bleibt der Default/Sammelzustand."
    5. `op.add_column('users', sa.Column('skip_onboarding', sa.Boolean(), nullable=False, server_default=sa.false()))`  # D-08 Interlock-Blocker S4
    6. Schilder (Punkt 23, COMMENT ON COLUMN):
       - onboarding_state: `op.execute("COMMENT ON COLUMN users.onboarding_state IS 'Onboarding-Fortschritt der Weiche post_login_destination (pending|done|skipped; CHECK ck_users_onboarding_state, erweiterbar um step_* ohne Weichen-Aenderung, D-09). Neue Wahrheitsquelle statt onboarding_done. Status: lebt (ab AUTH-2). Schreibt routes/onboarding.py (Erstprofil-Submit/Skip) + DB-Default pending bei Anlage; liest routes/auth.py + routes/oauth.py (post_login_destination).'")`
       - skip_onboarding: `op.execute("COMMENT ON COLUMN users.skip_onboarding IS 'Founder/Support-Schalter: ueberspringt NUR das Onboarding (Stufe 1 der Weiche), NICHT das Billing (das laeuft ueber organisations.skip_billing, AUTH-3/4). Status: Foundation — Setz-UI kommt AUTH-4, hier nur Spalte + Leser. Schreibt (spaeter) AUTH-4 Flask-Admin; liest routes/auth.py + routes/oauth.py (post_login_destination Stufe 1).'")`
    7. onboarding_done DEPRECATED-Marker (D-06, COMMENT — einfrieren, NICHT droppen):
       `op.execute("COMMENT ON COLUMN users.onboarding_done IS '[DEPRECATED ab AUTH-2 — EINGEFROREN, nicht droppen] Abgeloest durch users.onboarding_state. Noch aktive LESER (kein neuer Schreiber): routes/auth.py (_login_user, _create_org_and_user setzt False), routes/oauth.py (_oauth_login_or_create). Drop erst nach grep-Beleg 0 Leser (Zombie-Regel Punkt 23/29).'")`

    `downgrade()` — Reihenfolge umgekehrt: onboarding_done-Schild zuruecksetzen (auf 'Flag: Onboarding abgeschlossen'); `op.drop_column('users','skip_onboarding')`; `op.drop_constraint('ck_users_onboarding_state','users')`; `op.drop_column('users','onboarding_state')`.

    Import oben: `from alembic import op` + `import sqlalchemy as sa`.

    (B) `database/models.py` — im User-Modell frisch per grep die onboarding_done-Zeile ankern, dann:
    - onboarding_done comment= aktualisieren auf den [DEPRECATED]-Marker (WORTGLEICH-Kern zum Migrations-COMMENT oben, Single-Source Punkt 23): z.B. `comment='[DEPRECATED ab AUTH-2 — eingefroren, nicht droppen] Abgeloest durch onboarding_state. Nur noch Leser (auth.py/oauth.py), kein neuer Schreiber.'`
    - Neue Spalten direkt darunter ergaenzen (WORTGLEICH-Kern zu den Migrations-COMMENTs):
      ```python
      onboarding_state = Column(Text, nullable=False, server_default='pending',
          comment='Onboarding-Fortschritt der Weiche post_login_destination (pending|done|skipped; erweiterbar um step_*, D-09). Neue Wahrheitsquelle statt onboarding_done. Status: lebt. Schreibt routes/onboarding.py + DB-Default; liest routes/auth.py + routes/oauth.py.')
      skip_onboarding = Column(Boolean, nullable=False, server_default=sa_false_or_text_false,
          comment='Ueberspringt NUR Onboarding, NICHT Billing (organisations.skip_billing, AUTH-3/4). Status: Foundation, Setz-UI AUTH-4. Schreibt (spaeter) AUTH-4; liest routes/auth.py + routes/oauth.py.')
      ```
      (server_default fuer Boolean per Projekt-Muster — grep bestehende Boolean-server_default in models.py; falls kein Muster, `default=False` + die DB-Default kommt aus der Migration. Der ORM-default reicht fuer neue Anlagen; die DB-server_default aus 0032 ist die Persistenz-Wahrheit.)
    - ASCII in comment-Strings (Code-Kommentar-Kontext; Umlaute erlaubt aber ae/oe/ue OK).
  </action>
  <verify>
    <automated>bash deploy.sh production  # nerve_test bekommt via alembic upgrade head die 0032; test_onboarding_state_migration.py jetzt GRUEN (Spalten+CHECK+Default+Backfill). Danach inspect.sh schilder users auf Prod NICHT hier (Prod-Migration ist Plan 03) — nur nerve_test-Gate zaehlt.</automated>
  </verify>
  <acceptance_criteria>
    - Datei `alembic/versions/0032_onboarding_state_skip_onboarding.py` existiert; grep zeigt `down_revision = '0031'`.
    - grep zeigt die 3-Schritt-Sequenz: add_column onboarding_state (server_default 'pending') → `UPDATE users SET onboarding_state = 'done'` → alter_column nullable=False.
    - grep zeigt `create_check_constraint` mit `onboarding_state IN ('pending','done','skipped')` + den D-09-Tueroeffner-Kommentar VERBATIM darueber.
    - grep zeigt `add_column` skip_onboarding Boolean nullable=False server_default false.
    - grep zeigt 3x `COMMENT ON COLUMN users.` (onboarding_state, skip_onboarding, onboarding_done) mit Schild-Inhalt (Zweck+Status+Schreibt/liest, ≥10 Zeichen).
    - grep von database/models.py zeigt `onboarding_state` + `skip_onboarding` Columns mit comment= UND onboarding_done comment= enthaelt `[DEPRECATED`.
    - deploy.sh production Gate gruen; test_onboarding_state_migration.py PASSED (nicht SKIPPED — laeuft gegen nerve_test).
    - test_schild_guard.py bleibt gruen (neue Spalten haben Schild ≥10 Zeichen — sonst rot).
  </acceptance_criteria>
  <done>Migration 0032 (down_revision 0031) legt onboarding_state (TEXT NOT NULL Default pending, Backfill done, CHECK-Tueroeffner) + skip_onboarding (boolean Default FALSE) an, mit Schildern; onboarding_done ist DEPRECATED-markiert (nicht gedroppt); models.py traegt die comment=-Schilder; Migrations-Waechter gruen gegen nerve_test.</done>
</task>

<task type="auto">
  <name>Task 3: Landmine-Stilllegung (D-10) — Startup-Flip + Legacy-ADD-COLUMN + Seed dokumentieren, NIE auf onboarding_state portieren</name>
  <read_first>
    - `grep -n "onboarding_done" app.py` (frisch, per INHALT ankern — ~:155 Legacy-ADD-COLUMN, ~:831-846 08.12-Flip, ~:1643 _seed).
    - app.py ~:133-142 — `_migrate()`-Kopf + `if db_url.startswith('postgresql'): return` (der early-return, der die Landmine auf Prod inert macht — VERIFIED 2026-07-08).
    - app.py ~:1623-1648 — `_seed()`-Guard `if db.query(User).count() == 0` (laeuft auf Prod nie).
    - CONTEXT.md D-10 (Landmine: Flip darf NIE auf onboarding_state portiert werden).
  </read_first>
  <action>
    KEINE Verhaltensaenderung noetig (die 3 Stellen sind alle Postgres-/frisch-DB-inert, VERIFIED). NUR Warn-Kommentare setzen, damit das Muster nie auf onboarding_state wandert (D-10, Punkt 29 Anti-Halb-Migration):

    (a) app.py ~:831-846 (08.12-Flip `UPDATE users SET onboarding_done=1`): direkt ueber dem `try:` einen Kommentar ergaenzen:
    „★ AUTH-2 D-10 LANDMINE-MARKER: Dieser Startup-Flip ist SQLite-only (laeuft nie auf Prod — _migrate() early-returns bei Postgres, app.py ~:140). Er darf NIEMALS auf users.onboarding_state portiert/kopiert werden — ein Start-Flip auf onboarding_state wuerde jeden pending-Neuuser bei naechstem Start tot auf 'done' setzen und die Weiche (post_login_destination) aushebeln. onboarding_state ist ausschliesslich Migration 0032 (Anlage/Backfill) + Weichen-Uebergaenge (routes/onboarding.py). Kein Startup-Schreiber."

    (b) app.py ~:155 (Legacy-ADD-COLUMN-Liste, `('onboarding_done', 'BOOLEAN DEFAULT 0')`): Kommentar am Zeilenende oder darueber:
    „# AUTH-2 D-10: SQLite-Legacy-ADD-COLUMN (Prod via Alembic). KEINE onboarding_state-Zeile hier ergaenzen — die neue Spalte lebt ausschliesslich in Alembic 0032, kein _migrate()-Muster."

    (c) app.py ~:1643 (_seed `onboarding_done=True`): Kommentar darueber:
    „# AUTH-2 D-10: _seed laeuft nur bei User.count()==0 (nie auf Prod). onboarding_state NICHT hier setzen — DB-Default 'pending' (Migration 0032) greift; ein Seed-Setzer auf 'done' wuerde die Weiche fuer den Seed-Owner umgehen."

    Umlaute: reiner Code-Kommentar-Kontext → ASCII-Ersatz erlaubt (ae/oe/ue). KEINE Logik-Zeile aendern.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen (keine Verhaltensaenderung, nur Kommentare). inspect.sh grep 'D-10 LANDMINE' app.py auf Prod bestaetigt deployed Stand.</automated>
  </verify>
  <acceptance_criteria>
    - grep von app.py zeigt `D-10 LANDMINE-MARKER` ueber dem 08.12-Flip-Block.
    - grep von app.py zeigt an der Legacy-ADD-COLUMN-Stelle + der _seed-Stelle je einen `AUTH-2 D-10`-Kommentar mit dem Verbot „KEINE onboarding_state-Zeile / onboarding_state NICHT hier setzen".
    - KEINE Logik-Zeile geaendert (git diff zeigt nur Kommentar-Additionen an den 3 Stellen).
    - deploy.sh production Gate gruen.
  </acceptance_criteria>
  <done>Alle 3 Landmine-Stellen (Flip/Legacy-ADD/Seed) tragen einen D-10-Warn-Kommentar der das Portieren auf onboarding_state verbietet; keine Verhaltensaenderung (Stellen sind Prod-inert); der Flip beruehrt onboarding_state garantiert nie.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (SCHREIBEN: 2 neue Spalten + Backfill via Migration 0032; DEPRECATED-Marker auf onboarding_done).

### inspect.sh-Beleg (VERBATIM `\d users` Prod-nerve, 2026-07-08, Claudian per ssh — nicht zusammengefasst)
```
users (public) — relevante Spalten (Prod-nerve, \d users, 2026-07-08):
  id                integer      NOT NULL (PK, nextval users_id_seq)
  org_id            integer      NOT NULL  FK→organisations(id)
  email             varchar(200) NOT NULL  UNIQUE
  rolle             varchar(50)  NULL
  is_superadmin     boolean      NOT NULL
  is_coach          boolean      NULL
  active_profile_id integer      NULL       FK→profiles(id)
  onboarding_done   boolean      NULL       ← DEPRECATED, einfrieren (nicht droppen)
  is_test_user      boolean      NOT NULL DEFAULT false
  is_trial          boolean      NULL
  trial_ends_at     timestamp    NULL
  (+ ~40 weitere Spalten, für AUTH-2 irrelevant)

AUTH-2-RELEVANTE DELTAS (verifiziert):
  onboarding_state  → EXISTIERT NICHT → Migration 0032 legt an (TEXT, Default 'pending', Backfill alle→'done', dann NOT NULL, CHECK 'pending = alles außer done/skipped')
  skip_onboarding   → EXISTIERT NICHT → Migration 0032 legt an (boolean, Default FALSE, Interlock-Blocker S4)

Prod-Ist-Stand: alembic 0030, 3 users (onboarding_done: 2×t / 1×f — Backfill setzt alle auf 'done'), 4 orgs, 3 profiles.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg) + kann Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `users.onboarding_state` | GESCHRIEBEN von Migration 0032 (add+backfill) | DB-Spalte users.onboarding_state (NEU) | Migrations-Zeitpunkt (einmalig, synchron beim `alembic upgrade`); danach Default bei Anlage. KEIN Leser in diesem Plan → kein Voraus-Lauf moeglich | ✓ 0032 legt an; Waechter Task 1 prueft gegen nerve_test |
| `users.skip_onboarding` | GESCHRIEBEN von 0032 (add, Default FALSE) | DB-Spalte users.skip_onboarding (NEU) | Migrations-Zeitpunkt; Setz-UI erst AUTH-4. Kein Leser hier | ✓ 0032 + Waechter |
| `users.onboarding_done` | nur COMMENT-Update (DEPRECATED-Marker) | DB-Spalte (bestehend) | keine Datenaenderung, nur pg_description | ✓ inspect.sh \d users |

### Punkt 26 Schreib-vor-Lese-Analyse (dieser Plan)
Dieser Plan hat KEINEN Leser der neuen Spalten (Router/Weiche kommt Plan 02/03). Die einzige Schreib-Operation ist die einmalige Migration (synchron beim `alembic upgrade`). Es gibt daher in diesem Plan keinen Leser-laeuft-Schreiber-voraus-Fall. Der kritische Schreib-vor-Lese-Fall (Spalte muss auf Prod existieren BEVOR der lesende Weichen-Code Traffic bekommt, D-23) wird in Plan 03 als geordnete Deploy-Sequenz aufgeloest.

### Bei Diskrepanz: STOP + Replan.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Alembic-Migration → Prod-DB users | Schema-Aenderung + Backfill an einer Bestands-Tabelle mit echten Nutzern |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH2-01 | Denial of Service (Migration bricht/haengt Bestand) | 0032 onboarding_state NOT NULL ohne Backfill | mitigate | 3-Schritt-Reihenfolge (add nullable → UPDATE alle 'done' → NOT NULL); kein NOT-NULL-vor-Backfill-Crash; Waechter Task 1 prueft 0 NULLs |
| T-AUTH2-02 | Tampering (ungueltiger State-Wert unterlaeuft die Weiche) | onboarding_state Freitext | mitigate | CHECK-Constraint IN ('pending','done','skipped'); Waechter test_check_constraint_rejects_bogus (pytest.raises) |
| T-AUTH2-03 | Elevation/Bypass (Startup-Flip setzt Neuuser still auf done → Onboarding uebersprungen) | app.py 08.12-Flip auf onboarding_state portiert | mitigate | D-10-Landmine-Marker verbietet Portierung; Flip ist Postgres-inert (early-return); onboarding_state hat keinen Startup-Schreiber |
| T-AUTH2-04 | Repudiation (Halb-Migration: onboarding_done still weiterbeschrieben) | onboarding_done Zombie | mitigate | DEPRECATED-Marker (Schild + models.py), kein neuer Schreiber; Drop deferred bis 0 Leser (Zombie-Regel) |
</threat_model>

<verification>
- deploy.sh production Gate gruen MIT test_onboarding_state_migration.py auto-collected + PASSED (nicht SKIPPED — real-PG nerve_test).
- test_columns_exist: ROT gegen HEAD (vor 0032), GRUEN nach 0032 — dokumentiert.
- test_check_constraint_rejects_bogus: gruen (CHECK aktiv, Bogus wirft).
- test_schild_guard.py bleibt gruen (neue Spalten haben Schild).
- Kein Row-Leak (cleanup Session->User->Org fuer Test-angelegte Rows).
- git diff app.py zeigt NUR Kommentar-Additionen (keine Logik-Aenderung an der Landmine).
</verification>

<success_criteria>
- Migration 0032 (down_revision 0031) legt onboarding_state (TEXT NOT NULL Default pending, Backfill done, CHECK) + skip_onboarding (boolean Default FALSE) an, mit Schildern.
- onboarding_done ist DEPRECATED-eingefroren (Schild + models.py), nicht gedroppt.
- Startup-Flip/Legacy-ADD/Seed tragen D-10-Warn-Kommentare, onboarding_state hat keinen Startup-Schreiber.
- Migrations-Waechter im Deploy-Gate, gegen HEAD nachweislich rot gewesen, gruen nach 0032.
- KEIN lesender Weichen-Code in diesem Plan (Deploy-Sequenz-Falle D-23 erst in Plan 03 relevant).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-01-SUMMARY.md` erstellen (inkl. Rot-Beleg des Migrations-Waechters, nerve_test-Gate-Beleg dass 0032 sauber hochlaeuft, Bestaetigung dass die Landmine Postgres-inert ist).
</output>
