---
phase: 08.23.2.AUTH-2
plan: 01
type: execute
wave: 1
complexity: "🟡 mittel (Migrations-DATEI + Migrations-Waechter — NUR die Datei, models.py bleibt UNANGETASTET; Auth-Fundament; Cross-AI Pflicht VOR Execute, dieser Plan wird NICHT direkt executet)"
depends_on: []
files_modified:
  - alembic/versions/0032_onboarding_state_skip_onboarding.py
  - tests/test_onboarding_state_migration.py
autonomous: true
requirements: []   # keine REQ-IDs gemappt; Scope = CONTEXT-Decisions D-05/D-07/D-08/D-09 (Migrations-DATEI-Anteil)
user_setup: []

must_haves:
  truths:
    - "Die Migrations-DATEI 0032 existiert im Repo (down_revision 0031) und laeuft gegen nerve_test sauber hoch"
    - "Die Spalte users.onboarding_state existiert auf nerve_test nach 0032 (TEXT, NOT NULL, Default 'pending')"
    - "Alle vor 0032 bestehenden User (nerve_test) haben onboarding_state='done' (Backfill — kein Zwangs-Onboarding, Anti-Block-C)"
    - "Die Spalte users.skip_onboarding existiert (boolean, Default FALSE)"
    - "Ein CHECK-Constraint erlaubt onboarding_state IN ('pending','done','skipped') (D-09 Tueroeffner)"
    - "onboarding_state + skip_onboarding tragen ein Postgres-Schild (COMMENT via Migration); onboarding_done traegt einen DEPRECATED-Marker (COMMENT, eingefroren, nicht gedroppt)"
    - "★ database/models.py bleibt in DIESEM Plan UNANGETASTET (keine ORM-Spalten ergaenzt) — deploy.sh restartet zwar Prod, aber der ORM-SELECT-* liest die neue Spalte NICHT (models.py kennt sie nicht) → kein Login-Crash (Expand/Contract Schritt 1a, Finding 1)"
    - "Der Waechter test_onboarding_state_migration faengt gegen ein Schema OHNE 0032 (ERST-ROT) und wird gruen nach 0032"
  artifacts:
    - path: "alembic/versions/0032_onboarding_state_skip_onboarding.py"
      provides: "additive Migration: onboarding_state (TEXT NOT NULL Default pending, Backfill done, CHECK) + skip_onboarding (boolean Default FALSE) + Schilder + DEPRECATED-Marker onboarding_done"
      contains: "down_revision = '0031'"
    - path: "tests/test_onboarding_state_migration.py"
      provides: "real-PG Migrations-/Backfill-Waechter gegen nerve_test (Spalten existieren, alle Rows done, skip_onboarding Default, CHECK aktiv), ERST-ROT"
      contains: "onboarding_state"
  key_links:
    - from: "alembic/versions/0032"
      to: "users.onboarding_state / users.skip_onboarding"
      via: "op.add_column + UPDATE-Backfill + CHECK-Constraint + COMMENT"
      pattern: "onboarding_state"
    - from: "alembic/versions/0032 COMMENT ON COLUMN onboarding_done"
      to: "DEPRECATED-Marker in pg_description"
      via: "op.execute COMMENT"
      pattern: "DEPRECATED"
---

<objective>
Das Fundament der Onboarding-Weiche — **aber bewusst nur die Migrations-DATEI, NICHT die ORM-Definition**: die additive Alembic-Migration 0032, die `users.onboarding_state` (pending/done/skipped) + `users.skip_onboarding` anlegt, Bestand auf 'done' backfillt, den CHECK-Tueroeffner (D-09) setzt und die Schilder/DEPRECATED-Marker in `pg_description` schreibt.

**★ EXPAND/CONTRACT — SCHRITT 1a (Finding 1, BLOCKER-Fix):** `database/models.py` wird in DIESEM Plan **NICHT** angefasst. Grund (Cross-AI Fable): `models.py` IST der Leser — der ORM macht `db.get(User, ...)` = `SELECT *` bei JEDEM Login (`auth.py:51`). Wuerde dieser Plan die Spalten in `models.py` eintragen UND deployen (deploy.sh:247 restartet Prod), aber Prod-`nerve` steht noch auf 0030 ohne die Spalte → jeder Login-`SELECT *` crasht mit `"column onboarding_state does not exist"` → **Anmeldung tot** bis die Prod-Migration lief. Deshalb: erst die DATEI + der Guard live (alter Code laeuft weiter, ORM kennt die Spalte nicht → sicher). Die ORM-Spalten + app.py-Landmine kommen in Plan 03 (Schritt 1c), NACH der manuellen Prod-Migration (Plan 02, Schritt 1b).

**Chicken-Egg:** die 0032-Datei muss physisch auf dem Server liegen (dieser Plan deployt sie) BEVOR Plan 02 den manuellen `alembic upgrade head` gegen Prod-`nerve` fahren kann.

**Kein Router, keine UI, kein models.py, kein app.py in diesem Plan** — nur die Migrations-Datei + der Migrations-Waechter.

Purpose: Die Migrations-Datei auf den Server bringen (Voraussetzung fuer die manuelle Prod-Migration in Plan 02) und gegen nerve_test beweisen, dass sie sauber hochlaeuft — ohne den ORM-Leser scharfzuschalten.
Output: Migration 0032 (mit Schildern + DEPRECATED-Marker als COMMENT) + ERST-ROT-Migrations-Waechter.

Komplexität: 🟡 mittel — Migrations-Datei + Auth-Fundament. **Cross-AI (Gemini + Fable) PFLICHT vor Execute.** Dieser Plan wird NICHT direkt executet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md

<interfaces>
<!-- Verifizierte Anker (2026-07-08 gegen echten Code + Prod-ssh). ⚠ ALLE Zeilennummern per INHALT re-ankern (Punkt 20, 3x Drift in dieser Phase) — vor jedem Edit frisch greppen. Zeilen = Wegweiser, kein Vertrag. -->

★ FINDING-1-BEFUND (Cross-AI Fable, VERIFIED 2026-07-08): `routes/auth.py:51` = `user = db.get(User, session['user_id'])` → ORM `SELECT *` auf users bei JEDEM `login_required`-Request. `deploy.sh:247` = `sudo systemctl restart $SERVICE_NAME`. models.py:85 traegt heute nur `onboarding_done`. FOLGE: die ORM-Spalten-Definition ist der Leser — sie darf erst live, wenn die Spalte auf Prod existiert. Dieser Plan liefert deshalb NUR die Migrations-Datei, kein models.py.

Migrations-Muster (VERIFIED alembic/versions/0031_intent_event_handling_status_schild.py):
```python
from alembic import op
revision = '0031'
down_revision = '0030'
def upgrade():
    op.execute("COMMENT ON COLUMN intent_event.handling_status IS '...'")
def downgrade():
    op.execute("COMMENT ON COLUMN ...")
```
→ 0032 hat `down_revision = '0031'`. Naming-Muster: `0032_<snake>.py`. Prod-`nerve` steht auf alembic 0030; 0031 ist reine COMMENT-Migration (idempotent) und laeuft gefahrlos mit 0032 zusammen hoch (D-22).

User-Modell (VERIFIED database/models.py:85):
```python
onboarding_done     = Column(Boolean, default=False, comment='Flag: Onboarding abgeschlossen')
```
→ ★ In DIESEM Plan NICHT anfassen. Der onboarding_done-DEPRECATED-Marker wird hier NUR als Postgres-COMMENT (via Migration) gesetzt; die models.py `comment=`-Aktualisierung + die neuen ORM-Spalten kommen in Plan 03 (Schritt 1c), gekoppelt mit dem app.py-Landmine-Kommentar und NACH der Prod-Migration.

Prod-Ist-Stand (VERIFIED ssh 2026-07-08): alembic_version=0030; users=3 (onboarding_done f=1/t=2); onboarding_state + skip_onboarding existieren NICHT → 0032 rein additiv.

Deploy-Gate-Mechanik (VERIFIED deploy.sh:147-205): baut `nerve_test` per `pg_dump --schema-only` vom Prod-`nerve`, stampt alembic_version=prod-head (0030), faehrt `alembic upgrade head` (wendet 0031+0032 an), dann `pytest tests/ -m "not live and not perf"` gegen nerve_test. → Ein neuer test_*.py OHNE live/perf-Marker laeuft automatisch mit, gegen ein nerve_test DAS 0032 SCHON HAT. **Prod-`nerve` selbst migriert deploy.sh NICHT** (nur nerve_test) — der manuelle Prod-Migrations-Schritt ist Plan 02 (D-23, Schritt 1b). deploy.sh:247 restartet den Service — aber weil models.py hier UNANGETASTET bleibt, liest der restartete Code die neue Spalte nicht (Schritt 1a sicher).
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR ueber Production-Pfad. KEIN lokales pytest, KEIN python app.py, KEIN lokaler Alembic-Trockenlauf. -->
- Deploy-Gate: `bash deploy.sh production` provisioniert nerve_test (pg_dump vom Prod-nerve + alembic upgrade head inkl. 0032) und faehrt pytest dagegen VOR dem Restart. Rot = kein Restart, alter Code bleibt live.
- Live-Schema/Migrations-Stand: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema users'` / `inspect.sh migrations`.
- ★ Prod-`nerve` wird in DIESEM Plan NICHT migriert (das ist Plan 02) und NICHT vom ORM gelesen (models.py unangetastet). Dieser Plan liefert nur die Migrations-CODE-Datei; der deploy.sh-Gate testet sie gegen nerve_test; der Restart ist ungefaehrlich (kein neuer Leser).
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations-/Backfill-Waechter test_onboarding_state_migration.py — real-PG gegen nerve_test, ERST-ROT gegen HEAD</name>
  <read_first>
    - tests/conftest.py — `cleanup_rows` (~:238), `_baseline_cleanup_guard` autouse (~:549), das nerve_test-DSN-Muster (`DATABASE_URL=postgresql://nerve_app@/nerve_test`, deploy.sh:~195). Real-PG-Test-Muster: grep `DATABASE_URL` in bestehenden tests/test_*.py die gegen nerve_test laufen (z.B. tests/test_logs_org_boundary.py aus AUTH-LOGS-TENANT — Muster fuer real-PG-Query + skip-wenn-nicht-PG).
    - deploy.sh:~192-205 — pytest laeuft gegen nerve_test; nerve_test HAT nach `alembic upgrade head` die Spalten 0032. Der Test muss SKIPpen wenn kein Postgres-DSN (kein False-Green auf SQLite, Muster wie test_schild_guard.py).
    - alembic/versions/0031_intent_event_handling_status_schild.py — Struktur-Vorlage (Task 2 baut 0032 danach).
    - CONTEXT.md D-05/D-07/D-08/D-09 (Spalten-Spezifikation + Backfill + CHECK).
  </read_first>
  <behavior>
    - Der Test fragt das ECHTE nerve_test-Schema/Daten ab (DB-Query-Assertion, KEIN source-presence-grep — CLAUDE.md Test-Qualitaets-Regel). Er skippt, wenn `DATABASE_URL` nicht postgresql ist (kein False-Green lokal/SQLite; Muster test_schild_guard.py).
    - Assertion A (Spalten-Existenz): Query auf `information_schema.columns WHERE table_name='users' AND column_name IN ('onboarding_state','skip_onboarding')` → beide vorhanden; `onboarding_state`.data_type = 'text', is_nullable='NO'; `skip_onboarding`.data_type = 'boolean'.
    - Assertion B (Backfill / Default): der Test legt via direktem INSERT einen User an OHNE onboarding_state zu setzen → nach Insert ist onboarding_state='pending' (DB-Default greift). PLUS: es gibt in nerve_test 0 User mit onboarding_state NULL (Backfill/NOT-NULL).
    - Assertion C (CHECK-Constraint): ein UPDATE mit onboarding_state='bogus_value' wirft einen IntegrityError — Assertion via `pytest.raises`. Ein UPDATE mit 'skipped' geht durch (Whitelist-Wert), danach zuruecksetzen.
    - Assertion D (skip_onboarding Default): neuer User ohne skip_onboarding → skip_onboarding IS FALSE.
    - Committende Rows raeumen sich im Teardown via cleanup_rows. Cleanup-Reihenfolge Session->User->Organisation falls ueber Anlage-Pfad angelegt (FK sessions.user_id ohne ondelete — Muster AUTH-1 Plan 01).
    - ASCII: Test-/Fixture-Namen + dict-keys ASCII.
  </behavior>
  <action>
    Erstelle `tests/test_onboarding_state_migration.py`.

    Skip-Guard oben: wenn `os.environ.get('DATABASE_URL','')` NICHT mit 'postgresql' beginnt → `pytest.skip('real-PG only (nerve_test); kein False-Green auf SQLite', allow_module_level=True)` (Muster test_schild_guard.py).

    `test_columns_exist`: SQLAlchemy `text()`-Query auf `information_schema.columns` fuer table_name='users', column_name IN ('onboarding_state','skip_onboarding'). Assert beide da; onboarding_state data_type=='text' UND is_nullable=='NO'; skip_onboarding data_type=='boolean'.

    `test_no_null_and_default_pending`: (a) `SELECT count(*) FROM users WHERE onboarding_state IS NULL` == 0 (NOT-NULL/Backfill). (b) Insert einer minimalen User+Org-Row OHNE onboarding_state (via echtem Anlage-Pfad `_create_org_and_user` ODER direktes INSERT mit den Pflichtfeldern) → re-SELECT onboarding_state == 'pending' (DB-Default). Row-IDs im cleanup-Tracker (Session->User->Org).

    `test_check_constraint_rejects_bogus`: `with pytest.raises(Exception)` (IntegrityError) → `UPDATE users SET onboarding_state='bogus_value' WHERE id=<test-user>`; rollback danach. Gegenprobe: `UPDATE ... SET onboarding_state='skipped'` geht durch, dann zuruecksetzen.

    `test_skip_onboarding_default_false`: neuer User ohne skip_onboarding → SELECT skip_onboarding IS FALSE.

    SEQUENZ (André „ERST rot, DANN Migration"): Diesen Test ZUERST committen+deployen OHNE 0032 (Task 2) — gegen HEAD (nerve_test hat 0032 noch nicht) MUSS `test_columns_exist` ROT sein (Spalten fehlen → Gate blockt Restart). DANN Task 2 (0032) → gruen. Rot-Beleg ins SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # pytest-Gate auf nerve_test, test_onboarding_state_migration.py auto-collected. VOR Task 2: test_columns_exist MUSS ROT sein (Spalten fehlen, Gate blockt). NACH Task 2: gruen, kein Row-Leak.</automated>
  </verify>
  <acceptance_criteria>
    - Datei `tests/test_onboarding_state_migration.py` existiert; grep zeigt `information_schema.columns` (echte DB-Query, KEIN getsource/read-file/grep-auf-Quelle).
    - grep zeigt Skip-Guard `allow_module_level=True` fuer nicht-postgresql DATABASE_URL.
    - grep zeigt Assertions: data_type == 'text', is_nullable == 'NO', `pytest.raises` fuer den Bogus-CHECK, `skip_onboarding` IS FALSE.
    - Cleanup via cleanup_rows, Reihenfolge Session->User->Organisation (grep zeigt Session-Key vor User-Key falls ueber Anlage-Pfad).
    - Rot-Beleg dokumentiert: gegen HEAD (vor Task 2) blockt `bash deploy.sh production` am roten `test_columns_exist`.
    - Test-/Fixture-Namen ASCII.
  </acceptance_criteria>
  <done>test_onboarding_state_migration.py existiert, real-PG gegen nerve_test, gegen HEAD nachweislich ROT (Spalten fehlen), prueft Existenz+Backfill/Default+CHECK+skip_onboarding-Default via echte DB-Queries, sauberer Cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0032 — Spalten + Backfill + CHECK + Schilder + onboarding_done DEPRECATED-Marker (NUR die Migrations-Datei, KEIN models.py)</name>
  <read_first>
    - alembic/versions/0031_intent_event_handling_status_schild.py — op.execute(COMMENT)-Muster + revision/down_revision-Kopf.
    - `ls alembic/versions/` — bestaetigen dass 0031 die hoechste ist (0032 folgt).
    - CONTEXT.md D-05 (TEXT pending/done/skipped) / D-07 (EIN Schritt: Default pending + UPDATE alle done + dann NOT NULL) / D-08 (skip_onboarding boolean Default FALSE) / D-09 (CHECK als Tueroeffner) / D-06 (onboarding_done einfrieren).
    - Punkt 23 §0.2 Schild-Inhalt: „<Zweck/Business-Logik>. Status: <lebt|...>. Schreibt <datei:zeile>; liest <datei>."
    - ★ HINWEIS: database/models.py in DIESEM Plan NICHT editieren (Finding 1 / Schritt 1a). Der onboarding_done-Schild-Text kommt hier NUR als Postgres-COMMENT; die models.py-`comment=`-Aenderung + die neuen ORM-Spalten sind Plan 03.
  </read_first>
  <action>
    Erstelle `alembic/versions/0032_onboarding_state_skip_onboarding.py`. **KEINE Aenderung an database/models.py in diesem Plan.**

    Kopf:
    ```python
    revision = '0032'
    down_revision = '0031'
    branch_labels = None
    depends_on = None
    ```

    `upgrade()` — EXAKTE Reihenfolge (D-07, EIN Schritt, Anti-Block-C):
    1. `op.add_column('users', sa.Column('onboarding_state', sa.Text(), nullable=True, server_default='pending'))`  # erst nullable, damit Backfill greift
    2. `op.execute("UPDATE users SET onboarding_state = 'done'")`  # ALLE Bestands-User → done (kein Zwangs-Onboarding, Anti-Block-C, D-07)
    3. `op.alter_column('users', 'onboarding_state', nullable=False)`  # jetzt gefahrlos NOT NULL
    4. CHECK-Constraint (D-09, Tueroeffner): `op.create_check_constraint('ck_users_onboarding_state', 'users', "onboarding_state IN ('pending','done','skipped')")`
       KOMMENTAR im Migrations-File genau ueber der CHECK-Zeile (D-09 Tueroeffner-Semantik, VERBATIM):
       „D-09 Tueroeffner: die Weiche liest 'pending' = alles ausser done/skipped. Der spaetere Voll-Wizard darf step_*-Zwischenzustaende per CHECK-ERWEITERUNG (neue Werte in die IN-Liste) ergaenzen, OHNE die Weiche anzufassen — pending bleibt der Default/Sammelzustand."
    5. `op.add_column('users', sa.Column('skip_onboarding', sa.Boolean(), nullable=False, server_default=sa.false()))`  # D-08 Interlock-Blocker S4
    6. Schilder (Punkt 23, COMMENT ON COLUMN):
       - onboarding_state: `op.execute("COMMENT ON COLUMN users.onboarding_state IS 'Onboarding-Fortschritt der Weiche post_login_destination (pending|done|skipped; CHECK ck_users_onboarding_state, erweiterbar um step_* ohne Weichen-Aenderung, D-09). Neue Wahrheitsquelle statt onboarding_done. Status: lebt (ab AUTH-2). Schreibt routes/onboarding.py (Erstprofil-Submit/Skip) + DB-Default pending bei Anlage; liest routes/auth.py + routes/oauth.py (post_login_destination).'")`
       - skip_onboarding: `op.execute("COMMENT ON COLUMN users.skip_onboarding IS 'Founder/Support-Schalter: ueberspringt NUR das Onboarding (Stufe 1 der Weiche), NICHT das Billing (das laeuft ueber organisations.skip_billing, AUTH-3/4). Status: Foundation — Setz-UI kommt AUTH-4, hier nur Spalte + Leser. Schreibt (spaeter) AUTH-4 Flask-Admin; liest routes/auth.py + routes/oauth.py (post_login_destination Stufe 1).'")`
    7. onboarding_done DEPRECATED-Marker (D-06, COMMENT — einfrieren, NICHT droppen):
       `op.execute("COMMENT ON COLUMN users.onboarding_done IS '[DEPRECATED ab AUTH-2 — EINGEFROREN, nicht droppen] Abgeloest durch users.onboarding_state. Noch aktive LESER (kein neuer Schreiber): routes/auth.py (_login_user liest, _create_org_and_user setzt False), routes/oauth.py. Drop erst nach grep-Beleg 0 Leser (Zombie-Regel Punkt 23/29).'")`

    `downgrade()` — Reihenfolge umgekehrt: onboarding_done-Schild zuruecksetzen (auf 'Flag: Onboarding abgeschlossen'); `op.drop_column('users','skip_onboarding')`; `op.drop_constraint('ck_users_onboarding_state','users')`; `op.drop_column('users','onboarding_state')`.

    Import oben: `from alembic import op` + `import sqlalchemy as sa`.

    ★ WICHTIG (Finding 1): NUR diese Datei anlegen. models.py bleibt unberuehrt — deploy.sh restartet nach dem Gate, aber der ORM `SELECT *` (auth.py:51) referenziert onboarding_state NICHT (models.py kennt es nicht) → kein Crash gegen das noch-nicht-migrierte Prod. Der test_schild_guard.py laeuft gegen nerve_test (hat 0032) → die COMMENTs sind da → Guard gruen.
  </action>
  <verify>
    <automated>bash deploy.sh production  # nerve_test bekommt via alembic upgrade head die 0032; test_onboarding_state_migration.py jetzt GRUEN. Restart ungefaehrlich (models.py kennt onboarding_state nicht → kein ORM-SELECT-Crash gegen Prod-0030).</automated>
  </verify>
  <acceptance_criteria>
    - Datei `alembic/versions/0032_onboarding_state_skip_onboarding.py` existiert; grep zeigt `down_revision = '0031'`.
    - grep zeigt die 3-Schritt-Sequenz: add_column onboarding_state (server_default 'pending') → `UPDATE users SET onboarding_state = 'done'` → alter_column nullable=False.
    - grep zeigt `create_check_constraint` mit `onboarding_state IN ('pending','done','skipped')` + den D-09-Tueroeffner-Kommentar VERBATIM darueber.
    - grep zeigt `add_column` skip_onboarding Boolean nullable=False server_default false.
    - grep zeigt 3x `COMMENT ON COLUMN users.` (onboarding_state, skip_onboarding, onboarding_done) mit Schild-Inhalt (Zweck+Status+Schreibt/liest, ≥10 Zeichen).
    - ★ git diff zeigt KEINE Aenderung an database/models.py (Finding 1 / Schritt 1a) — nur die neue Migrations-Datei + der Test.
    - deploy.sh production Gate gruen; test_onboarding_state_migration.py PASSED (nicht SKIPPED).
    - test_schild_guard.py bleibt gruen (die neuen Spalten haben COMMENT ≥10 Zeichen in pg_description von nerve_test — der Guard prueft die DB, nicht models.py).
  </acceptance_criteria>
  <done>Migration 0032 (down_revision 0031) legt onboarding_state (TEXT NOT NULL Default pending, Backfill done, CHECK-Tueroeffner) + skip_onboarding (boolean Default FALSE) an, mit Schildern; onboarding_done ist per COMMENT DEPRECATED-markiert; models.py bleibt UNANGETASTET (Finding 1); Migrations-Waechter gruen gegen nerve_test.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (SCHREIBEN: 2 neue Spalten + Backfill via Migration 0032; DEPRECATED-COMMENT auf onboarding_done). ★ NUR die DB-Ebene (Migrations-Datei) — die ORM-Ebene (models.py) bleibt in diesem Plan aussen vor (Finding 1 / Schritt 1a).

### inspect.sh-Beleg (VERBATIM `\d users` Prod-nerve, 2026-07-08, Claudian per ssh — nicht zusammengefasst)
```
users (public) — relevante Spalten (Prod-nerve, \d users, 2026-07-08):
  id                integer      NOT NULL (PK, nextval users_id_seq)
  org_id            integer      NOT NULL  FK→organisations(id)
  email             varchar(200) NOT NULL  UNIQUE
  rolle             varchar(50)  NULL
  is_superadmin     boolean      NOT NULL
  is_coach          boolean      NULL
  active_profile_id integer      NULL       FK→profiles(id)
  onboarding_done   boolean      NULL       ← DEPRECATED-COMMENT (Marker), Spalte bleibt (nicht droppen)
  is_test_user      boolean      NOT NULL DEFAULT false
  is_trial          boolean      NULL
  trial_ends_at     timestamp    NULL
  (+ ~40 weitere Spalten, für AUTH-2 irrelevant)

AUTH-2-RELEVANTE DELTAS (verifiziert):
  onboarding_state  → EXISTIERT NICHT → Migration 0032 legt an (TEXT, Default 'pending', Backfill alle→'done', dann NOT NULL, CHECK 'pending = alles außer done/skipped')
  skip_onboarding   → EXISTIERT NICHT → Migration 0032 legt an (boolean, Default FALSE, Interlock-Blocker S4)

Prod-Ist-Stand: alembic 0030, 3 users (onboarding_done: 2×t / 1×f — Backfill setzt alle auf 'done'), 4 orgs, 3 profiles.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg) + kann Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `users.onboarding_state` | GESCHRIEBEN von Migration 0032 (add+backfill) | DB-Spalte users.onboarding_state (NEU) | Migrations-Zeitpunkt (einmalig, synchron beim `alembic upgrade`). ★ KEIN ORM-Leser in diesem Plan (models.py unangetastet) → der Prod-Restart liest die Spalte nicht → kein Crash gegen Prod-0030 (Finding 1) | ✓ 0032 legt an (nerve_test); Waechter Task 1 prueft; models.py-Diff leer |
| `users.skip_onboarding` | GESCHRIEBEN von 0032 (add, Default FALSE) | DB-Spalte users.skip_onboarding (NEU) | Migrations-Zeitpunkt; kein ORM-Leser hier | ✓ 0032 + Waechter |
| `users.onboarding_done` | nur COMMENT-Update (DEPRECATED-Marker via Migration) | DB-Spalte (bestehend) | keine Datenaenderung, nur pg_description | ✓ inspect.sh \d users |

### Punkt 26 Schreib-vor-Lese-Analyse (dieser Plan)
★ FINDING-1-KERN: Der frühere Plan behauptete „nur Schreib-Seite, kein Leser" — FALSCH, weil `models.py` (ORM `SELECT *`, auth.py:51) der Leser ist. Auflösung: dieser Plan fasst models.py NICHT an, also gibt es in diesem Plan keinen ORM-Leser der neuen Spalten. Die einzige Schreib-Operation ist die einmalige Migration gegen nerve_test (synchron). Der Prod-Restart (deploy.sh:247) laeuft mit ALTEM ORM (kennt onboarding_state nicht) → sicher gegen Prod-0030. Die geordnete Prod-Scharfschaltung (1a→1b→1c) ist: (1a) DIESER Plan = Datei+Guard live; (1b) Plan 02 = manuelle Prod-Migration; (1c) Plan 03 = models.py + app.py live NACH 1b.

### Bei Diskrepanz: STOP + Replan.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Alembic-Migration → nerve_test / (spaeter) Prod-DB users | Schema-Aenderung + Backfill an einer Bestands-Tabelle mit echten Nutzern |
| deploy.sh Restart (deploy.sh:247) → laufende App | Restart mit neuem Code — hier ohne neuen ORM-Leser (Finding 1) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH2-01 | Denial of Service (Migration bricht/haengt Bestand) | 0032 onboarding_state NOT NULL ohne Backfill | mitigate | 3-Schritt-Reihenfolge (add nullable → UPDATE alle 'done' → NOT NULL); Waechter Task 1 prueft 0 NULLs |
| T-AUTH2-02 | Tampering (ungueltiger State-Wert unterlaeuft die Weiche) | onboarding_state Freitext | mitigate | CHECK-Constraint IN ('pending','done','skipped'); Waechter test_check_constraint_rejects_bogus (pytest.raises) |
| T-AUTH2-01b | ★ Denial of Service (Login-Crash: ORM-SELECT-* liest Spalte vor Prod-Migration) | models.py-Spalten + Prod-Restart vor 0032-auf-Prod (Finding 1) | mitigate | models.py in DIESEM Plan UNANGETASTET → ORM kennt onboarding_state nicht → Restart liest sie nicht; ORM-Spalten erst Plan 03 NACH Plan-02-Prod-Migration |
| T-AUTH2-04 | Repudiation (Halb-Migration: onboarding_done still weiterbeschrieben) | onboarding_done Zombie | mitigate | DEPRECATED-COMMENT (Migration), kein neuer Schreiber; Drop deferred bis 0 Leser (Zombie-Regel) |
</threat_model>

<verification>
- deploy.sh production Gate gruen MIT test_onboarding_state_migration.py auto-collected + PASSED (nicht SKIPPED — real-PG nerve_test).
- test_columns_exist: ROT gegen HEAD (vor 0032), GRUEN nach 0032 — dokumentiert.
- test_check_constraint_rejects_bogus: gruen (CHECK aktiv, Bogus wirft).
- test_schild_guard.py bleibt gruen (die neuen Spalten haben COMMENT in nerve_test).
- ★ git diff zeigt KEINE Aenderung an database/models.py + app.py (Finding 1 / Schritt 1a) — nur Migrations-Datei + Test.
- Kein Row-Leak (cleanup Session->User->Org fuer Test-angelegte Rows).
</verification>

<success_criteria>
- Migration 0032 (down_revision 0031) legt onboarding_state (TEXT NOT NULL Default pending, Backfill done, CHECK) + skip_onboarding (boolean Default FALSE) an, mit Schildern.
- onboarding_done ist per COMMENT DEPRECATED-markiert (Marker in pg_description), nicht gedroppt.
- ★ database/models.py + app.py bleiben UNANGETASTET (Finding 1 / Schritt 1a) — kein neuer ORM-Leser live, kein Login-Crash gegen Prod-0030.
- Migrations-Waechter im Deploy-Gate, gegen HEAD nachweislich rot gewesen, gruen nach 0032.
- Die 0032-Datei liegt nach dem Deploy auf dem Server (Voraussetzung fuer Plan 02 Chicken-Egg).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-01-SUMMARY.md` erstellen (inkl. Rot-Beleg des Migrations-Waechters, nerve_test-Gate-Beleg dass 0032 sauber hochlaeuft, Bestaetigung dass models.py-Diff leer ist / kein ORM-Leser live, Bestaetigung dass die 0032-Datei auf dem Server liegt).
</output>
