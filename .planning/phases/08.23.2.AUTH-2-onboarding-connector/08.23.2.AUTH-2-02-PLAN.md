---
phase: 08.23.2.AUTH-2
plan: 02
type: execute
wave: 2
complexity: "🔴-nah (manuelle Prod-Schema-Migration an Bestands-Nutzern — Schritt 1b der Expand/Contract-Sequenz, Finding 1; autonomous:false, Claudian fuehrt den ssh-Schritt)"
depends_on: ["08.23.2.AUTH-2-01"]
files_modified: []   # KEIN Code — reiner manueller Prod-Migrations-/Verifikations-Schritt
autonomous: false    # Task 1 = manueller Prod-alembic-upgrade (Claudian, D-23 / Finding 1 Schritt 1b)
requirements: []     # Scope = CONTEXT-Decisions D-21/D-22/D-23 (Prod-Migrations-Naht)
user_setup: []

must_haves:
  truths:
    - "Prod-nerve traegt onboarding_state + skip_onboarding NACH diesem Plan (0030→0031→0032, additiv, alter Code lief waehrenddessen weiter)"
    - "Alle 3 realen Prod-User haben onboarding_state='done' (Backfill) + skip_onboarding=false (Post-Migration-Verifikation, Punkt 13)"
    - "Die Migration lief BEVOR der models.py-lesende Code (Plan 03) live geht — Expand/Contract Schritt 1b zwischen 1a (Plan 01: Datei) und 1c (Plan 03: ORM-Spalten)"
    - "Bei Migrations-Fehler: KEIN Fortschritt zu Plan 03, alter Code (ohne onboarding_state-Read) bleibt live, Rollback via alembic downgrade 0031"
  artifacts: []       # kein Code-Artefakt; das Artefakt ist der migrierte Prod-DB-Zustand + der psql-Beleg im SUMMARY
  key_links:
    - from: "Prod-nerve alembic upgrade head (0030→0032)"
      to: "onboarding_state/skip_onboarding existieren auf Prod VOR dem ORM-Leser (Plan 03)"
      via: "manueller ssh sudo -u postgres alembic upgrade head (D-23, Schritt 1b)"
      pattern: "alembic upgrade head"
---

<objective>
★ EXPAND/CONTRACT — SCHRITT 1b (Finding 1, BLOCKER-Fix): die **manuelle Prod-Schema-Migration**, eingeschoben ZWISCHEN Plan 01 (Migrations-Datei live, Schritt 1a) und Plan 03 (ORM-Spalten + app.py-Landmine live, Schritt 1c).

**Warum ein eigener Plan (autonomous:false):** `deploy.sh` restartet zwar Prod, migriert Prod-`nerve` aber NICHT (nur nerve_test — VERIFIED deploy.sh:147-205). Der onboarding_state-lesende Code (Plan 03: models.py ORM `SELECT *`, auth.py:51) darf NICHT live gehen, bevor die Spalte auf Prod existiert — sonst crasht jeder Login (Finding 1 / D-23). Nachdem Plan 01 die 0032-DATEI auf den Server gebracht hat (Chicken-Egg aufgeloest), faehrt Claudian hier MANUELL `alembic upgrade head` gegen Prod-`nerve` (0030→0031→0032, rein additiv, der noch laufende alte Code kennt die Spalte nicht → stoert nicht). Danach existiert die Spalte auf Prod, und Plan 03 kann die ORM-Definition + den Landmine-Kommentar gefahrlos live schalten.

**Dieser Plan aendert KEINEN Code** — er ist der geordnete, verifizierte Prod-Migrations-Schritt. Das „Artefakt" ist der migrierte Prod-DB-Zustand plus der psql-Beleg (3 reale User = 'done').

Baut auf Plan 01 (0032-Datei liegt auf dem Server) → Wave 2.

Purpose: Prod-`nerve` scharfschalten (Spalten existieren + Bestand korrekt gebackfillt) BEVOR der lesende Code kommt — die Schreib-vor-Lese-Naht (D-23) sauber aufloesen.
Output: migrierte Prod-DB (alembic 0032) + Post-Migrations-Verifikations-Beleg.

Komplexität: 🔴-nah — Prod-Schema-Migration an echten Nutzern. **Cross-AI (Gemini + Fable) PFLICHT vor Execute.** Supervised (André/Claudian fuehrt den ssh-Schritt, kein Auto-Advance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-01-PLAN.md

<interfaces>
<!-- Verifizierte Anker (2026-07-08). ⚠ ALLE Zeilen per INHALT re-ankern (Punkt 20). -->

Manueller Prod-Migrations-Weg (VERIFIED docs/cutover-runbook.md:64-116): `sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/alembic upgrade head"`. Faehrt 0030→0031→0032. 0031 = COMMENT-only (idempotent, D-22) → laeuft gefahrlos mit 0032 zusammen hoch.

Prod-Ist-Stand (VERIFIED ssh 2026-07-08): alembic_version=0030; users=3 (onboarding_done f=1/t=2); onboarding_state + skip_onboarding existieren NICHT → 0032 rein additiv. 0031 auf Prod NICHT angewandt.

Warum vor Plan 03 (Finding 1): Plan 03 traegt onboarding_state in models.py ein → ORM `SELECT *` (auth.py:51) liest die Spalte bei jedem Login. Existiert sie auf Prod nicht → Crash. Deshalb DIESER Plan (Prod-Migration) VOR Plan 03 (ORM-Code).

Sicherheit waehrend 1b: der aktuell live laufende Code ist der ALTE (Plan 01 hat models.py NICHT angefasst) → er liest onboarding_state nicht → die additive Migration stoert den laufenden Betrieb nicht (nur neue Spalten dazu, kein Drop, kein Typ-Wechsel an bestehenden Spalten).
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" + „Verify=Production". -->
- ★ PROD-MIGRATION (D-23 / Finding 1 Schritt 1b, VERBINDLICH VOR Plan 03): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/alembic upgrade head"'`.
- Vorab-/Post-Verifikation: `inspect.sh schema users` / `inspect.sh migrations` + `sudo -u postgres psql -d nerve -c "SELECT id, onboarding_state, skip_onboarding FROM users ORDER BY id"`.
- KEIN Code-Deploy in diesem Plan (deploy.sh laeuft hier NICHT — der Code-Restart mit dem ORM-Leser ist Plan 03).
</verify_environment>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: ★ VERBINDLICH — Prod-nerve MANUELL migrieren (0030→0032) + Post-Migration-Verifikation (Schritt 1b, VOR Plan 03)</name>
  <read_first>
    - docs/cutover-runbook.md:64-116 (manueller Prod-alembic-upgrade-Weg, sudo -u postgres DATABASE_URL=@/nerve).
    - CONTEXT.md D-21/D-22/D-23 (Prod manuell migrieren; 0031 COMMENT-only idempotent; deploy.sh migriert nur nerve_test; Schreib-vor-Lese-Falle).
    - Plan 01 SUMMARY (bestaetigt: 0032-Datei liegt auf dem Server — Chicken-Egg aufgeloest).
    - inspect.sh (schema users / migrations).
  </read_first>
  <what-built>
    Plan 01 lieferte die Migrations-DATEI 0032 auf den Server (models.py UNANGETASTET, kein ORM-Leser live). Prod-`nerve` hat die Spalten NOCH NICHT (deploy.sh migriert nur nerve_test). Dieser Schritt migriert Prod-`nerve` MANUELL (0030→0032), BEVOR der onboarding_state-LESENDE Code (Plan 03: models.py + app.py) live geht — sonst Login-Crash beim ORM `SELECT *` (Finding 1 / D-23). Der aktuell live laufende alte Code kennt die Spalte nicht → die additive Migration stoert ihn nicht.
  </what-built>
  <how-to-verify>
    Claudian fuehrt in dieser exakten Reihenfolge (VERBINDLICH):

    1. **Vorab-Check**: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` → alembic steht auf 0030 (oder 0031). `inspect.sh schema users` → onboarding_state/skip_onboarding NOCH NICHT da. Bestaetigen dass die 0032-Datei auf dem Server liegt (`ls alembic/versions/0032_*` via inspect.sh grep oder `inspect.sh` — Plan 01 hat sie deployt).

    2. **Prod-Migration (0030→0031→0032, 0031 COMMENT-only idempotent, D-22)**:
       ```
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/alembic upgrade head"'
       ```
       Additiv (nur neue Spalten + Backfill + CHECK + COMMENTs) — der noch laufende ALTE Code (ohne onboarding_state-Read) laeuft ununterbrochen weiter.

    3. **Post-Migration-Verifikation (Pflicht, Punkt 13 Real-Daten)**:
       - `inspect.sh schema users` → onboarding_state (text, not null) + skip_onboarding (boolean) EXISTIEREN.
       - `ssh ... 'sudo -u postgres psql -d nerve -c "SELECT id, onboarding_state, skip_onboarding FROM users ORDER BY id"'` → alle 3 realen User: onboarding_state='done' (Backfill), skip_onboarding=false.
       - `inspect.sh migrations` → alembic auf 0032.
       - `inspect.sh schilder users` → onboarding_state/skip_onboarding/onboarding_done tragen die Schilder/DEPRECATED-Marker (aus 0032).
  </how-to-verify>
  <resume-signal>Tippe „migriert+verifiziert" (mit den 3 psql-Zeilen onboarding_state='done' + skip_onboarding=false) ODER beschreibe Abweichungen. Bei Migrations-Fehler: KEIN Fortschritt zu Plan 03, alter Code bleibt live (liest onboarding_state nicht → kein Crash), Rollback via `sudo -u postgres … alembic downgrade 0031` + Analyse.</resume-signal>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (SCHREIBEN auf Prod-`nerve`: die 0032-Migration wird hier manuell angewandt — Spalten + Backfill 'done' + CHECK + COMMENTs). Kein Code, nur der DB-Zustand.

### inspect.sh-Beleg (VERBATIM `\d users` Prod-nerve, 2026-07-08 — nicht zusammengefasst)
```
users (public) — relevante Spalten (Prod-nerve, \d users, 2026-07-08, VOR diesem Plan):
  id                integer      NOT NULL (PK)
  org_id            integer      NOT NULL  FK→organisations(id)
  rolle             varchar(50)  NULL
  active_profile_id integer      NULL       FK→profiles(id)
  onboarding_done   boolean      NULL       ← DEPRECATED-COMMENT nach 0032

AUTH-2-RELEVANTE DELTAS:
  onboarding_state  → EXISTIERT VOR diesem Plan NICHT → nach 1b: TEXT NOT NULL Default 'pending', Backfill alle 3→'done', CHECK
  skip_onboarding   → EXISTIERT VOR diesem Plan NICHT → nach 1b: boolean Default FALSE

Prod-Ist-Stand VOR 1b: alembic 0030, 3 users (onboarding_done 2×t/1×f → Backfill setzt onboarding_state alle auf 'done'), 4 orgs, 3 profiles.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg) + kann Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `users.onboarding_state` (Prod) | GESCHRIEBEN von manueller Migration (add+backfill) | DB-Spalte Prod-nerve.users.onboarding_state | Migrations-Zeitpunkt (synchron, `alembic upgrade`). ★ KEIN Leser waehrend 1b (der live laufende Code ist der ALTE, models.py-Spalte kommt erst Plan 03) → kein Voraus-Lauf; der Leser (Plan 03) kommt NACH diesem Plan | ✓ Task 1 Post-Verifikation (psql 3 User = 'done') |
| `users.skip_onboarding` (Prod) | GESCHRIEBEN von Migration (Default FALSE) | DB-Spalte Prod-nerve.users.skip_onboarding | Migrations-Zeitpunkt; Leser erst Plan 03 | ✓ Task 1 psql (skip_onboarding=false) |

### Punkt 26 Schreib-vor-Lese-Analyse (der Kern dieses Plans)
Dieser Plan IST die Aufloesung der Prod-Ebenen-Schreib-vor-Lese-Naht (D-23 / Finding 1): der onboarding_state-Schreiber (Migration) muss auf Prod VOR dem onboarding_state-Leser (Plan 03: ORM `SELECT *` an auth.py:51) laufen. Sequenz: 1a (Plan 01, Datei live, kein Leser) → **1b (DIESER Plan, Prod-Migration)** → 1c (Plan 03, ORM-Leser live). Waehrend 1b liest kein Code die Spalte (alter ORM ohne die Spalte) → sicher.

### Bei Diskrepanz: STOP + Replan.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Claudian ssh → Prod-nerve alembic upgrade | manuelle Schema-Migration an Prod mit echten Nutzern |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH2-10 | Denial of Service (Login-Crash: lesender Code vor Spalten-Existenz) | Prod-Migration-Reihenfolge relativ zum ORM-Leser (Finding 1 / D-23) | mitigate | DIESER Plan (1b, autonomous:false) migriert Prod VOR Plan 03 (1c, ORM-Leser); bei Migrations-Fehler kein Fortschritt zu Plan 03 |
| T-AUTH2-14 | Repudiation (Migration ohne Verifikation → stiller Teil-Backfill) | Prod-Migration | mitigate | Post-Migration-Verifikation Pflicht: 3 reale User onboarding_state='done' via psql (Punkt 13) |
</threat_model>

<verification>
- Prod-Migration: alembic 0032, inspect.sh schema users zeigt beide Spalten (onboarding_state text not null, skip_onboarding boolean).
- 3 reale User: onboarding_state='done' (Backfill), skip_onboarding=false (psql-Beleg im SUMMARY).
- Schilder/DEPRECATED-Marker in pg_description (inspect.sh schilder users).
- Reihenfolge belegt: Migration lief NACH Plan 01 (Datei da) + VOR Plan 03 (ORM-Leser) — kein Login-Crash.
- Kein Code-Deploy in diesem Plan (deploy.sh lief hier nicht).
</verification>

<success_criteria>
- Prod-nerve auf 0032 migriert (0030→0031→0032, additiv), 3 reale User onboarding_state='done' + skip_onboarding=false verifiziert.
- Migration lief zwischen Plan 01 (Datei, 1a) und Plan 03 (ORM-Leser, 1c) — Schreib-vor-Lese-Naht (D-23 / Finding 1) sauber aufgeloest.
- Kein Login-Crash (alter Code lief waehrend der additiven Migration weiter, kannte die Spalte nicht).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-02-SUMMARY.md` erstellen (inkl. Prod-Migrations-psql-Beleg der 3 User = 'done' + skip_onboarding=false, inspect.sh schema/migrations/schilder-Beleg, Bestaetigung der Reihenfolge 1a→1b→1c, Freigabe fuer Plan 03).
</output>
