# Phase 08.23.2.AUTH-2: Onboarding-Connector - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-08
**Phase:** 08.23.2.AUTH-2-onboarding-connector
**Areas discussed:** Routing-Matrix + Nachbar-Naht, onboarding_state Spalte + Migration, Erstprofil-Minimum-Seite, Verkabelung 3+ Stellen

---

## Gray-Area-Auswahl

Alle 4 vorgeschlagenen Grau-Zonen wurden vom User in EINER Antwort abschließend beantwortet — André hatte sie vorab mit Fable am echten Code durchgearbeitet („Entscheidungen final"). Keine iterative Nachfrage nötig; Claude hat die 4 lastragendsten neuen Anker (Startup-Flip, Branche-Whitelist, Training-404, Coach-Weiche/data.next, Onboarding-Stubs) gegen den echten Code spot-verifiziert (alle bestätigt, +1 Zeilen-Drift bei training.py).

---

## GZ1 — Routing-Matrix + AUTH-3-Naht

| Option | Beschreibung | Gewählt |
|--------|-------------|---------|
| Weg 1 — nur Onboarding-Zweig + Leer-Slot für AUTH-3 | Kein Billing hier; dokumentierter Vertragstext im Router | ✓ |
| Weg 2 — voller Gate-Chain jetzt (Billing→Onboarding→Dashboard) | Referenziert noch nicht existente skip_billing/subscription | |

**User's choice:** Weg 1. Router als nummerierte Stufenliste (Stufe1 skip_onboarding→nichts; Stufe2 [LEER AUTH-3-SLOT] mit wörtlichem Vertragstext; Stufe3 Onboarding pending+Owner/Admin+0 Profile→Erstprofil, pending+Profile→selbstheilend done, Member nie; Stufe4 nichts). done/skipped werden NICHT umgeleitet → Coach-Weiche landing.html:864 bleibt.
**Notes:** ★ André-Entscheidung: skip_onboarding überspringt NUR Onboarding, NICHT die Kasse (Geld über eigenes skip_billing pro Firma) — Trennung in Slot-Vertrag schreiben. Bewusster Interim-Zustand: bis AUTH-3+4 = Neu-Registrierte gratis.

---

## GZ2 — onboarding_state Spalte + Migration

| Option | Beschreibung | Gewählt |
|--------|-------------|---------|
| A — neue onboarding_state TEXT, onboarding_done als [DEPRECATED] einfrieren | Kein Drop (Leser bleiben); Backfill alle→done | ✓ |
| B — onboarding_state aus onboarding_done ableiten + Bool droppen | Bricht aktive Leser | |

**User's choice:** A. Migration 0032 in EINEM Schritt: Default 'pending' + sofort UPDATE alle Bestand→'done' + dann NOT NULL. Legt AUCH users.skip_onboarding an (Default FALSE, Interlock-Blocker S4). CHECK-Constraint so, dass Weiche „pending = alles außer done/skipped" liest (Türöffner für späteren Wizard).
**Notes:** ★ Landmine: Startup-Flip app.py:831-846 (setzt bei jedem Start alle onboarding_done=1) stilllegen/kommentieren — NIE auf neue Spalte portieren. onboarding_done-Leser: auth.py:109/147, oauth.py:100/108.

**Nachschärfung (Claudian, Prod-verifiziert per ssh 2026-07-08, Punkt 13/21/22):** Naht-Fund — Prod-nerve alembic_version=0030, Repo-0031 unangewandt, deploy.sh migriert nur nerve_test nicht Prod-nerve, Auto-Alembic-Startup tot (app.py:1109-1114). → D-21..D-24 (GZ2b) ergänzt: (a) Prod-Migrations-Mechanismus dokumentieren+verifizieren, (b) 0031 bewusst behandeln (0030→0031→0032 zusammen?), (c) verifizierter 0032-Prod-Schritt + Backfill gegen reale 3 Nutzer, (d) EIN Mechanismus (Alembic ODER _migrate, nicht beide). PLAN braucht Persistenz-Schicht-Sektion 5 mit verbatim inspect.sh-Output.

---

## GZ3 — Erstprofil-Minimum-Seite

| Option | Beschreibung | Gewählt |
|--------|-------------|---------|
| A — nur Branchen-Wahl (1 von 8) → Profil seeden | Ein Klick | |
| B — Branchen-Wahl + 1 vorbefülltes Produktfeld „Was verkaufst du?" | Profilname auto, überschreibbar | ✓ |
| C — größeres Mini-Formular (Branche + Produkt + erster Einwand) | Zu nah am Voll-Wizard | |

**User's choice:** ★ Option B. Branche + EIN Produktfeld (Vorlagen-Text vorbefüllt, überschreibbar, Durchklicken erlaubt). Profilname auto „<Branche> — Startprofil". Skip → skipped → Dashboard + Pflicht-Banner „Noch kein Profil" (ohne Profil ist Training tot, training.py:165-167→404).
**Notes:** Enum-Mapping EXPLIZIT: Vorlagen-Keys → profiles.py VALID_BRANCHE; Recruiting+Agentur haben KEINEN Whitelist-Wert → dokumentierte Zeile →sonstiges, kein stiller Fallback. Anlage+Aktiv-Setzen aus profiles.py:74-83/:194-198 (Aktiv-Setzen im selben Commit entschärft 404). Seite an /onboarding/ (ersetzt 3 Attrappen onboarding.py:162-182).

---

## GZ4 — Verkabelung + Übergänge

| Option | Beschreibung | Gewählt |
|--------|-------------|---------|
| 3 Stellen (Login-JSON + OAuth Bestand + OAuth Neu) | Roadmap-Nennung | |
| 4 Stellen (+ S1 already-logged-in auth.py:91) | Fängt „morgen wieder rein" | ✓ |

**User's choice:** 4 Andock-Stellen: S1 auth.py:91 (schon eingeloggt), S2 auth.py:216-219 JSON-Login (+landing.html data.next), S3 auth.py:258 JSON-Register (+1-Zeilen-JS-Fix landing.html, ignoriert next heute), S4 oauth.py:112+:163 (1 Einbaupunkt/2 Rücksprünge). pending via DB-Default an _create_org_and_user (auth.py:171-201) — kein expliziter Setzer. Übergänge: pending→done bei Absenden (EIN Commit), pending→skipped bei Skip, Selbstheilung pending→done bei existierenden Profilen.
**Notes:** Commit-Falle: Weiche VOR oauth.py:107 und :137. Toten Gate-Rest dashboard.py:449-453 (nicht-existente profiles.wizard_page, Logik verkehrt) JETZT mit löschen. ★ ALLE Zeilennummern per INHALT ankern (3× Drift gefunden), vor Bau frisch greppen.

## Claude's Discretion
- Datei-Heimat von post_login_destination (services/ vs. auth.py-Helfer)
- Template-Aufbau Erstprofil-Seite (n-*-Komponenten)
- Deprecated-Marker-Form auf onboarding_done
- GET/POST-Aufteilung der /onboarding/-Route

## Deferred Ideas
- Voll-Onboarding-Wizard (Mehr-Schritt-State) → spätere Phase
- Billing-Gate (Stufe-2-Leer-Slot) → AUTH-3
- Founder-Setz-UI skip_onboarding/skip_billing → AUTH-4
- onboarding_done Drop + Startup-Flip endgültig raus → späterer Cleanup
