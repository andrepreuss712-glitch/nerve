---
phase: 08.23.2.AUTH-2
plan: "03"
subsystem: auth / onboarding
tags: [orm, models, landmine, expand-contract, schild, deprecated]
dependency_graph:
  requires:
    - "08.23.2.AUTH-2-01 (Migrations-Datei 0032 live)"
    - "08.23.2.AUTH-2-02 (Prod-nerve auf 0032 migriert, 3 User = done)"
  provides:
    - "ORM-Leser fuer onboarding_state + skip_onboarding (database/models.py)"
    - "D-10 Landmine-Stilllegung (app.py)"
  affects:
    - "routes/auth.py (auth.py:51 SELECT * liest jetzt onboarding_state sicher)"
    - "routes/oauth.py (ORM SELECT * ebenfalls)"
tech_stack:
  added: []
  patterns:
    - "Column(Text, nullable=False, server_default='pending') fuer onboarding_state"
    - "Column(Boolean, nullable=False, server_default=text('false')) fuer skip_onboarding"
    - "comment= Schild WORTGLEICH-Kern zu Alembic 0032 COMMENTs (Punkt 23)"
key_files:
  created: []
  modified:
    - database/models.py
    - app.py
decisions:
  - "comment=-Strings aus 0032 VERBATIM als Kern uebernommen (Single-Source Punkt 23)"
  - "server_default=text('false') fuer Boolean (bestehendes Projekt-Muster, line 704/714/929)"
  - "app.py: comment-only Additionen an allen 3 D-10-Stellen, kein Logik-Touch"
metrics:
  duration: "~15 Minuten"
  completed: "2026-07-08"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 2
---

# Phase 08.23.2.AUTH-2 Plan 03: ORM-Scharfschaltung + D-10 Landmine-Stilllegung -- Summary

**One-liner:** ORM-Spalten `onboarding_state`/`skip_onboarding` in `models.py` scharf (Schritt 1c nach Prod-Migration), D-10 Landmine an 3 app.py-Stellen mit WARN-Kommentar stillgelegt (kein Portier-Muster auf `onboarding_state`).

---

## Vorbedingung bestaetigt (Finding 1 / Punkt 26)

Plan-02-SUMMARY (Prod-Verifikation 2026-07-08) bestaetigt:
- `alembic_version` = **0032** auf Prod-`nerve`
- `users.onboarding_state` = `text NOT NULL`, `users.skip_onboarding` = `boolean NOT NULL`
- Alle 3 realen User `onboarding_state='done'`, `skip_onboarding=false`
- App nach Migration **healthy** (kein `column onboarding_state does not exist` in journalctl)

Der ORM-Leser (`auth.py:51` `SELECT *`) geht hiermit sicher scharf (Expand/Contract Schritt 1c NACH Schritt 1b). Login-Crash-Risiko = 0.

---

## Task 1 -- models.py ORM-Spalten (Commit 33d6782)

**Datei:** `database/models.py`

### Was geaendert wurde

Zeile 85 (per `grep -n "onboarding_done" database/models.py` frisch verankert):

1. `onboarding_done` `comment=` aktualisiert auf `[DEPRECATED ab AUTH-2 -- EINGEFROREN, nicht droppen]` + Leser-Liste + Zombie-Regel-Verweis.

2. Neue Zeilen 86-89 direkt darunter ergaenzt:
   - `onboarding_state = Column(Text, nullable=False, server_default='pending', comment=...)`
   - `skip_onboarding = Column(Boolean, nullable=False, server_default=text('false'), default=False, comment=...)`

### Import-Check

`Text`, `Boolean`, `text` sind bereits auf Zeile 2 importiert:
```
from sqlalchemy import Column, Integer, ..., Boolean, ..., Text, ..., text, ...
```
Kein neuer Import noetig.

### server_default-Muster

Bestehendes Projekt-Muster fuer Boolean-Falsch-Default: `server_default=text('false')` (z.B. models.py Z.704, Z.714, Z.929). Dieses Muster wurde uebernommen.

### Schild-WORTGLEICH-Kern-Pruefung (Punkt 23 Aktualitaets-Pflicht)

| Spalte | 0032 COMMENT (Kern) | models.py comment= (Kern) | Match |
|---|---|---|---|
| `onboarding_state` | "Onboarding-Fortschritt der Weiche post_login_destination (pending|done|skipped; CHECK ck_users_onboarding_state, erweiterbar um step_* ohne Weichen-Aenderung, D-09). Neue Wahrheitsquelle statt onboarding_done. Status: lebt (ab AUTH-2). Schreibt routes/onboarding.py...; liest routes/auth.py + routes/oauth.py..." | identisch | WORTGLEICH |
| `skip_onboarding` | "Founder/Support-Schalter: ueberspringt NUR das Onboarding (Stufe 1 der Weiche), NICHT das Billing (das laeuft ueber organisations.skip_billing, AUTH-3/4). Status: Foundation -- Setz-UI kommt AUTH-4, hier nur Spalte + Leser. Schreibt (spaeter) AUTH-4 Flask-Admin; liest routes/auth.py + routes/oauth.py (post_login_destination Stufe 1)." | identisch | WORTGLEICH |
| `onboarding_done` | "[DEPRECATED ab AUTH-2 -- EINGEFROREN, nicht gedroppt] Abgeloest durch users.onboarding_state. Noch aktive LESER...: routes/auth.py ..., routes/oauth.py. Drop erst nach grep-Beleg 0 Leser (Zombie-Regel Punkt 23/29)." | identisch (Kern) | WORTGLEICH |

### Static-Acceptance-Grep (models.py)

```
grep "onboarding_state\|skip_onboarding\|DEPRECATED" database/models.py
```

Ergebnis (verifiziert):
- Z.85: `onboarding_done` mit `[DEPRECATED ab AUTH-2 -- EINGEFROREN, nicht droppen]` in comment=
- Z.86-87: `onboarding_state = Column(Text, nullable=False, server_default='pending', comment='Onboarding-Fortschritt...')`
- Z.88-89: `skip_onboarding = Column(Boolean, nullable=False, server_default=text('false'), default=False, comment='Founder/Support-Schalter...')`

---

## Task 2 -- app.py D-10 Landmine-Marker (Commit e49450e)

**Datei:** `app.py`

### 3 Stellen markiert (per `grep -n "onboarding_done" app.py` frisch verankert)

**(a) Z.155 -- Legacy-ADD-COLUMN (inline):**
```python
('onboarding_done', 'BOOLEAN DEFAULT 0'),  # AUTH-2 D-10: SQLite-Legacy-ADD-COLUMN (Prod via Alembic). KEINE onboarding_state-Zeile hier ergaenzen -- die neue Spalte lebt ausschliesslich in Alembic 0032, kein _migrate()-Muster.
```

**(b) Z.836-842 -- 08.12 Startup-Flip (Block-Kommentar darueber):**
```python
        # ★ AUTH-2 D-10 LANDMINE-MARKER: Dieser Startup-Flip ist SQLite-only (laeuft nie
        # auf Prod -- _migrate() early-returns bei Postgres, app.py ~:140). Er darf NIEMALS
        # auf users.onboarding_state portiert/kopiert werden -- ein Start-Flip auf
        # onboarding_state wuerde jeden pending-Neuuser bei naechstem Start tot auf 'done'
        # setzen und die Weiche (post_login_destination) aushebeln. onboarding_state ist
        # ausschliesslich Migration 0032 (Anlage/Backfill) + Weichen-Uebergaenge
        # (routes/onboarding.py). Kein Startup-Schreiber.
        try:
            ...UPDATE users SET onboarding_done = 1...
```

**(c) Z.1650-1652 -- _seed onboarding_done=True (Kommentar darueber):**
```python
                # AUTH-2 D-10: _seed laeuft nur bei User.count()==0 (nie auf Prod). onboarding_state
                # NICHT hier setzen -- DB-Default 'pending' (Migration 0032) greift; ein Seed-Setzer
                # auf 'done' wuerde die Weiche fuer den Seed-Owner umgehen.
                onboarding_done=True,
```

### Bestaetigte Verifikation: git diff app.py = comment-only

Der vollstaendige `git diff app.py` wurde geprueft:
- Z.155: Inline-Kommentar an bestehende Tuple-Zeile angehaengt (keine neue Tuple-Zeile)
- Z.836-842: 7 Kommentar-Zeilen VOR dem bestehenden `try:` eingefuegt
- Z.1650-1652: 3 Kommentar-Zeilen VOR dem bestehenden `onboarding_done=True,` eingefuegt
- **KEIN einziger Logik-Identifier, kein veraendertes Statement, kein neuer Code-Pfad.**

### Postgres-Inert-Bestaetigung (D-10)

Alle 3 Stellen sind auf Prod-Postgres strukturell inert:
- `_migrate()`: `early-return` bei `db_url.startswith('postgresql')` (app.py Z.140-142) -- weder Flip noch Legacy-ADD-COLUMN laufen auf Prod.
- `_seed()`: Guard `if db.query(User).count() == 0` -- auf Prod (3 User) niemals ausgefuehrt.
Die Kommentare verhindern, dass ein kuenftiger Entwickler das Muster auf `onboarding_state` kopiert.

---

## Deviations from Plan

None -- plan executed exactly as written. Static-Verify-Anpassung (kein `bash deploy.sh production` gemaess CLAUDE.md "HART: Kein Local-Dev") war in der Objective bereits vorgesehen.

---

## Static Acceptance (deploy-gate deferred to Andre)

| Check | Ergebnis |
|---|---|
| `grep "onboarding_state" database/models.py` | 2 Treffer (Column Z.86 + comment= Z.87) |
| `grep "skip_onboarding" database/models.py` | 2 Treffer (Column Z.88 + comment= Z.89) |
| `grep "DEPRECATED" database/models.py` | 1 Treffer (onboarding_done comment= Z.85) |
| `grep "D-10" app.py` | 3 Treffer (Z.155, Z.836, Z.1650) |
| `grep "LANDMINE" app.py` | 1 Treffer (Z.836, Startup-Flip) |
| `git diff --name-only` | nur `app.py` + `database/models.py` |
| `git diff app.py` = comment-only | bestaetigt (kein Logic-Statement geaendert) |
| Text importiert in models.py | ja (Z.2) |
| Boolean importiert in models.py | ja (Z.2) |
| text() importiert in models.py | ja (Z.2) |
| server_default Muster Boolean | `text('false')` (Projekt-Konvention Z.704/714/929) |
| Schild WORTGLEICH-Kern zu 0032 | bestaetigt (alle 3 Spalten) |

**Deploy-Gate deferred:** `bash deploy.sh production` laeuft durch Andre (supervised). Das Gate prueft `test_schild_guard.py` (Schild >= 10 Zeichen auf Prod-Postgres) + `test_onboarding_state_migration.py` (Migration korrekt). Restart liest dann `onboarding_state` gegen die seit Plan 02 existierende Prod-Spalte -- kein Login-Crash erwartet.

---

## Known Stubs

Keine. Dieser Plan definiert nur ORM-Spalten und Kommentare -- kein UI, keine Route, kein Daten-Fluss.

---

## Threat Flags

Keine neuen Threat-Surfaces. Die ORM-Spalten-Definition fuegt dem `SELECT *`-Pfad (auth.py:51) keine neue Logik hinzu -- die Spalten werden nur gelesen, weil sie jetzt im ORM bekannt sind. Kein neuer Schreiber, kein neuer Endpunkt, keine neue Trust-Boundary.

---

## ★ NEXT GATE: Andre fuehrt `bash deploy.sh production` aus

**Wave-3-Deploy-Gate -- erstes Deploy wo der ORM SELECT * `onboarding_state` liest:**

```bash
bash deploy.sh production
```

Erwartetes Ergebnis:
- `test_schild_guard.py` PASSED (onboarding_state + skip_onboarding haben Schild >= 10 Zeichen auf Prod-nerve)
- `test_onboarding_state_migration.py` PASSED
- Restart (deploy.sh:247) liest onboarding_state gegen existierende Prod-Spalte (Plan 02) → Login 302 intakt
- `inspect.sh logs-errors` zeigt kein `column onboarding_state does not exist`

**Danach: Plan 04 (Router + Weiche post_login_destination) kann starten.**

**DO NOT AUTO-ADVANCE TO PLAN 04** -- Andre verifiziert Deploy-Gate zuerst.

---

## Self-Check: PASSED

- `database/models.py` Z.85-89: FOUND onboarding_done DEPRECATED + onboarding_state + skip_onboarding
- `app.py` Z.155, Z.836, Z.1650: FOUND alle 3 D-10-Marker
- Commits: 33d6782 (models.py) + e49450e (app.py) -- beide verifiziert via `git log --oneline -3`
- `git diff --name-only` (nach beiden Commits): leer (alles committed)
- Nur 2 Dateien geaendert: bestaetigt
