# Phase 08.23.2.AUTH-2 — Cross-AI Review (Fable)

**Reviewed:** 2026-07-08
**Reviewer:** Fable (Cross-AI, Punkt 24 — 3 Sichten)
**Verdict:** NACHBESSERN — 3 Findings (1 BLOCKER) + 1 UAT-Tightening
**Scope reviewed:** 3 PLAN.md (01 Migration, 02 Router+Erstprofil, 03 Verkabelung) gegen realen Code.

---

## Finding 1 — ★ BLOCKER: Deploy-Sequenz (Schreib-vor-Lese, Punkt 22/26)

**Problem:** Plan 01 trägt die neuen Spalten in `database/models.py` ein UND deployt (`deploy.sh:247` restartet Prod), migriert Prod-`nerve` aber NICHT (nur `nerve_test`). Ab dem Plan-01-Deploy fragt das ORM `users.onboarding_state` bei JEDEM Login (`auth.py:51`, `SELECT *`) → Prod steht auf 0030 ohne Spalte → `"column does not exist"` → **Anmeldung tot bis Plan 03**. Der Plan-01-Claim „nur Schreib-Seite, kein Leser" ist FALSCH — `models.py` IST der Leser (ORM `SELECT *`).

**Fix (Expand/Contract, deploy.sh-kompatibel):** Prod-DB-Migration MUSS laufen BEVOR der `models.py`-lesende Code live geht. Reihenfolge:
- **(1a)** Migrations-Datei 0032 + Migrations-Wächter deployen, `models.py` NOCH OHNE die neuen Spalten → Gate grün (nerve_test hat 0032, Wächter grün) → Restart (alter Code, sicher).
- **(1b)** Claudian fährt manuell `ssh … alembic upgrade head` gegen Prod-`nerve` (0031+0032, additiv, alter Code läuft weiter) → Spalte existiert.
- **(1c)** `models.py`-Spalten + `app.py`-Landmine deployen → Restart (Spalte existiert, sicher).

Danach Plan 02/03 wie gehabt; **Plan 03 Task 4 wird reine Verifikation + UAT** (Migration bereits angewandt). Den fertigen ssh-Befehl / Vorab-Check / Post-Verify aus Plan 03 Task 4 nach **(1b)** ziehen.

**Chicken-Egg:** die 0032-Datei muss auf dem Server sein (via 1a) BEVOR `alembic upgrade` läuft.

---

## Finding 2 — D-09-Türöffner nicht wirksam

**Problem:** Weiche in Plan 02 liest `state == 'pending'`.

**Fix:** ändern zu `state not in ('done','skipped')` (fail-safe-Default `'done'` bleibt). Sonst umgehen spätere `step_*`-Zustände die Weiche — genau was der CHECK-Kommentar (Plan 01 CHECK-Constraint D-09) ausschließen wollte.

---

## Finding 3 — Rollen-Gate fehlt auf Submit-Handler

**Problem:** die neuen POST-Handler `/onboarding/` (Submit) + Profil-Anlage haben kein Rollen-Gate (Plan 02 Task 3) → Member umgeht das owner/admin-Gate von `profiles.py:63`.

**Fix:** Submit-Handler dasselbe `g.user.rolle in ('owner','admin')`-Gate geben (Skip darf offen bleiben).

---

## Klein — S1 + S4 UAT auf PFLICHT

S1 (already-logged-in) + S4 (OAuth) im Live-UAT (Plan 03 Task 4) auf **PFLICHT** stellen, nicht optional — kein automatisierter Wächter deckt sie ab.
