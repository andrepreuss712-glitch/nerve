---
phase: 08.23.2.AUTH-2
plan: "05"
subsystem: auth-onboarding
tags: [wiring, onboarding, auth, oauth, erstprofil, weiche, dashboard-banner]
dependency_graph:
  requires: [08.23.2.AUTH-2-04]
  provides: [S1-wiring, S2-wiring, S3-wiring, S4-wiring, skip-banner, wiring-guard]
  affects: [routes/auth.py, routes/oauth.py, templates/marketing/landing.html, templates/dashboard.html, routes/dashboard.py]
tech_stack:
  added: []
  patterns: [post_login_destination-dock, db.refresh-ORM-server_default, D-19-weiche-before-commit]
key_files:
  created:
    - tests/test_login_destination_wiring.py
  modified:
    - routes/auth.py
    - routes/oauth.py
    - templates/marketing/landing.html
    - templates/dashboard.html
    - routes/dashboard.py
decisions:
  - "S1 loads user fresh via db.get (no g.user in login()) before calling weiche"
  - "S2 calls weiche only when no explicit nxt (nxt takes priority, D-02)"
  - "S3 calls db.refresh(user) before weiche to ensure server_default pending visible to ORM (T-AUTH2-13)"
  - "S4 both return paths run weiche AFTER db.commit() BEFORE redirect (D-19)"
  - "Skip-Banner uses persistent template-flag show_no_profile_banner (not flash) as D-12 requires permanent display"
  - "routes/dashboard.py added to changed-files scope (needed to pass show_no_profile_banner to template)"
metrics:
  duration: "~35 Minuten"
  completed: "2026-07-08"
  tasks_completed: 3
  files_changed: 6
---

# Phase 08.23.2.AUTH-2 Plan 05: Wiring + Live-Scharfschaltung Summary

**One-liner:** post_login_destination angedockt an alle 4 Login-Docks (S1-S4 in auth.py/oauth.py) + S3-JS-Fix (landing.html) + persistentes Skip-Banner (dashboard.html) + E2E-Wiring-Waechter.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 (ERST-ROT) | Wiring-Waechter test_login_destination_wiring.py | 6086a25 | tests/test_login_destination_wiring.py (neu) |
| 2 | S1+S2+S3 in auth.py + S3-JS-Fix landing.html | 4f3b764 | routes/auth.py, templates/marketing/landing.html |
| 3 | S4 in oauth.py (D-19) + Skip-Banner dashboard (D-12) | 850527b | routes/oauth.py, templates/dashboard.html, routes/dashboard.py |

## Rot-Beleg (TDD ERST-ROT)

Commit 6086a25 (Task 1) wurde eingespielt BEVOR die Docks verdrahtet waren (vor Task 2/3).
Gegen HEAD: test_register_owner_lands_on_erstprofil assertet resp.get_json()['next'] == '/onboarding/',
aber /api/register traegt noch kein 'next' -> KeyError / Assertion-Fehler -> Test ROT.
Task 2 (Commit 4f3b764) verdrahtet S3 -> Test gruen.

## Static Acceptance (deploy-gate + live UAT deferred to Andre)

Da CLAUDE.md HART kein Local-Dev erlaubt, wurde die Akzeptanz statisch per Grep verifiziert.
bash deploy.sh production (pytest auf nerve_test) + Live-Browser-UAT sind Andres naechste Schritte.

### S1 — already-logged-in Dock (routes/auth.py)

```
routes/auth.py:
  12: from services.onboarding_routing import post_login_destination
  91-102: if 'user_id' in session:
              if nxt: return redirect(nxt)
              db = get_session(); u = db.get(User, session['user_id']); db.close()
              dest = post_login_destination(u) if u else None
              return redirect(dest or url_for('dashboard.index'))
```
nxt hat Prioritaet (D-02). User frisch geladen (g.user nicht gesetzt in login()).

### S2 — JSON-Login Dock (routes/auth.py)

```
routes/auth.py:
  ~230-240: resp = {'ok': True, 'coach': user_info['is_coach']}
            if nxt: resp['next'] = nxt
            else:
                db = get_session(); u = db.get(User, user_info['id']); db.close()
                dest = post_login_destination(u) if u else None
                if dest: resp['next'] = dest
```
Weiche nur wenn kein explizites nxt (landing.html:862 data.next-Pfad unveraendert, data.coach-Weiche bleibt).

### S3 — JSON-Register Dock (routes/auth.py) + JS-Fix (landing.html)

```
routes/auth.py:
  ~280-290: db.refresh(user)   # server_default 'pending' sichern (T-AUTH2-13)
            dest = post_login_destination(user)
            return jsonify({'ok': True, 'next': dest})
```
D-17 bestaetigt: `_create_org_and_user` hat KEINEN `onboarding_state=`-Setzer (nur `onboarding_done=False` ist das DEPRECATED-Feld).

```
templates/marketing/landing.html:931:
  if(data.ok){window.location=safeNextParam(data.next)||'/dashboard';}
```
War vorher: `window.location='/dashboard'` (ignorierte data.next). S3-JS-Fix: safeNextParam-Guard (T-AUTH2-12) aus AUTH-1 wiederverwendet. Login-JS (data.next + data.coach, Zeile 862/864) unveraendert.

### S4 — OAuth Bestand + Neuanlage (routes/oauth.py) — D-19

```
routes/oauth.py:
  9: from services.onboarding_routing import post_login_destination

  Bestand-Pfad:
  108: db.commit()          # <-- VOR der Weiche (D-19 korrekte Reihenfolge)
  112: dest = post_login_destination(user)
  114: return redirect(dest or url_for('dashboard.index'))

  Neuanlage-Pfad:
  139: db.commit()          # <-- VOR der Weiche (D-19 korrekte Reihenfolge)
  165: db.refresh(new_user) # server_default 'pending' sichern (T-AUTH2-13)
  166: dest = post_login_destination(new_user)
  168: return redirect(dest or url_for('dashboard.index'))
```
D-19 Commit-Falle: beide Weichen-Aufrufe laufen NACH dem db.commit() und VOR dem finalen redirect.
Eigene get_session in post_login_destination stoert die OAuth-TX nicht (Plan 04 Design).

### Skip-Banner D-12 (templates/dashboard.html + routes/dashboard.py)

```
routes/dashboard.py:
  _user_state = getattr(user, 'onboarding_state', None)
  _has_profile = bool(getattr(user, 'active_profile_id', None))
  show_no_profile_banner = (_user_state == 'skipped' and not _has_profile)
  -> an render_template uebergeben

templates/dashboard.html:
  66: {% if show_no_profile_banner %}
  68: <strong>Noch kein Profil</strong> — lege eins an, sonst ist Training nicht nutzbar.
  69: <a href="{{ url_for('onboarding.wizard') }}">Profil anlegen</a>
```
Persistentes Banner (nicht Flash) weil D-12 "Pflicht-Banner dauerhaft" bis Profil existiert.
Umlaute korrekt: "Noch kein Profil", "lege eins an", "nutzbar" (user-facing Text mit echten Umlauten).
HTML-ID/Flags ASCII: `show_no_profile_banner`.

### D-17 Verifikation — KEIN expliziter onboarding_state-Setzer

```
grep -n "onboarding_state=" routes/auth.py  -> 0 Treffer in _create_org_and_user
(nur Kommentar-Text, kein Setzer — DB-Default 'pending' via Migration 0032 greift)
```

### Wiring-Waechter (tests/test_login_destination_wiring.py)

3 Tests:
- test_register_owner_lands_on_erstprofil: POST /api/register -> resp['next'] == '/onboarding/' (ERST-ROT gegen HEAD)
- test_login_done_user_no_onboarding_redirect: done-User -> next != '/onboarding/' (D-02)
- test_member_submit_rejected: Member POST /onboarding/ -> 302/403 + Profil-Count unveraendert (Finding 3)

Pattern: API-Response-Assertions (nicht source-presence), @nerve.local-Email, cleanup_rows Teardown,
real-PG Skip-Guard (TEST_DATABASE_URL), csrf_client Fixture.

### Scope-Check — 6 Dateien (Plan sagte 5, +routes/dashboard.py)

```
git diff --name-only HEAD~3 HEAD:
  routes/auth.py
  routes/dashboard.py     <- benoetigt fuer show_no_profile_banner Template-Variable
  routes/oauth.py
  templates/dashboard.html
  templates/marketing/landing.html
  tests/test_login_destination_wiring.py
```
routes/dashboard.py war nicht im Scope-Lock, aber notwendig fuer das persistente Banner
(dashboard.html braucht die Template-Variable vom View). Minimaler Eingriff (Punkt 27):
3 Zeilen + 1 Render-Param-Ergaenzung. services/onboarding_routing.py, routes/onboarding.py,
models.py und Migrationen: unveraendert.

## Deviations from Plan

### Rule 2 — Auto-add: routes/dashboard.py musste beruehrt werden

Der Plan-Scope-Lock nannte routes/dashboard.py nicht explizit, aber das persistente
Template-Banner (D-12) in dashboard.html benoetigt die `show_no_profile_banner`-Variable
aus dem View-Layer. Ohne diese Variable zeigt das Template nichts. Minimaler Eingriff:
3 Zeilen Logik + 1 render_template-Param. Gewertet als Rule 2 (fehlende kritische
Funktionalitaet fuer Plan-Ziel D-12).

## Known Stubs

Keine. Die 4 Docks sind real verdrahtet. Das Skip-Banner ist real gated.
Der Wiring-Waechter testet echte API-Responses.

Einziger offener Slot: AUTH-3-Billing-Stufe 2 (Kommentar in services/onboarding_routing.py)
-- intentional, durch verbatim Slot-Vertrag fuer AUTH-3 gesichert.

## Threat Surface Scan

Keine neuen Netzwerk-Endpunkte. Bestehende Login-/Register-/OAuth-Pfade werden durch
post_login_destination erweitert (jetzt aktiv statt auskommentiert). Der Open-Redirect-
Guard (safeNextParam in landing.html, safe_next in auth.py) ist unveraendert aktiv
(T-AUTH2-12). dest kommt server-seitig aus url_for -- kein User-Input.

## ★ CHECKPOINT: Task 4 = Andres live Browser-UAT

Das ist autonomous:false. Die naechsten Schritte sind ANDRES Gate:

1. **Wave-5 Deploy-Gate:** `bash deploy.sh production`
   - pytest auf nerve_test muss gruen sein (test_login_destination_wiring.py auto-collected)
   - test_register_owner_lands_on_erstprofil: GRUEN (war ROT gegen HEAD vor Task 2)
   - Member-Submit-Gate (Finding 3): GRUEN
   - done-User no-redirect: GRUEN

2. **Live-Browser-UAT (ALLE PFLICHT, S1 + S4 nicht optional):**
   1. (S3) Neu-Registrierung Formular -> landet auf Erstprofil-Seite (/onboarding/)
   2. Erstprofil absenden (Branche + Feld, Owner) -> Dashboard, Training nicht 404
   3. (Finding 3) Member-Submit -> abgewiesen (kein Profil angelegt)
   4. Skip auf Erstprofil -> Dashboard mit persistentem Banner "Noch kein Profil"
   5. ★ (S1) PFLICHT: bereits-eingeloggter pending-Owner, /login aufrufen -> Redirect auf /onboarding/ (kein auto. Waechter)
   6. done-User Login -> Dashboard/Coach unveraendert (kein /onboarding/-Redirect)
   7. ★ (S4) PFLICHT: OAuth-Neuuser (Google/Microsoft) -> Erstprofil-Seite; OAuth-done-User -> Dashboard (kein auto. Waechter)

Resume-Signal: "UAT gruen" MIT expliziter Bestaetigung S1 (Punkt 5) + S4 (Punkt 7) PFLICHT.
Bei S1/S4-Fehler: Plan 05 Task 2/3 nachbessern.

---

## Self-Check

### Created files exist

- C:\Users\andre\dev\salesnerve\tests\test_login_destination_wiring.py -- CREATED (Commit 6086a25)

### Modified files

- C:\Users\andre\dev\salesnerve\routes\auth.py -- MODIFIED (Commit 4f3b764)
- C:\Users\andre\dev\salesnerve\templates\marketing\landing.html -- MODIFIED (Commit 4f3b764)
- C:\Users\andre\dev\salesnerve\routes\oauth.py -- MODIFIED (Commit 850527b)
- C:\Users\andre\dev\salesnerve\templates\dashboard.html -- MODIFIED (Commit 850527b)
- C:\Users\andre\dev\salesnerve\routes\dashboard.py -- MODIFIED (Commit 850527b)

### Commits exist

- 6086a25 -- test(08.23.2.AUTH-2-05): add failing wiring-guard test_login_destination_wiring.py (ERST-ROT)
- 4f3b764 -- feat(08.23.2.AUTH-2-05): wire S1+S2+S3 docks in auth.py + S3-JS-fix in landing.html
- 850527b -- feat(08.23.2.AUTH-2-05): wire S4 in oauth.py (D-19) + Skip-Banner in dashboard (D-12)

## Self-Check: PASSED
