---
phase: 08.23.2.AUTH-2
plan: 03
type: execute
wave: 3
complexity: "🔴-nah (Verkabelung an Auth-Pfad + Prod-Migrations-Deploy-Sequenz D-23 — Cross-AI Pflicht; autonomous:false am manuellen Prod-Migrations-Schritt, Claudian fuehrt)"
depends_on: ["08.23.2.AUTH-2-01", "08.23.2.AUTH-2-02"]
files_modified:
  - routes/auth.py
  - routes/oauth.py
  - templates/marketing/landing.html
  - templates/dashboard.html
  - tests/test_login_destination_wiring.py
autonomous: false   # Task 4 = manueller Prod-alembic-upgrade-Schritt (Claudian, D-23) + Deploy-Sequenz-Gate
user_setup: []

must_haves:
  truths:
    - "Ein frisch registrierter Owner (Formular-Register) mit pending + 0 Profilen landet nach Login auf der Erstprofil-Seite (/onboarding/)"
    - "Ein OAuth-Neuuser (Google/Microsoft) mit pending + 0 Profilen landet auf der Erstprofil-Seite (Weiche laeuft VOR dem OAuth-Commit/Redirect, D-19)"
    - "Ein bereits-eingeloggter pending-Owner (S1, morgen wieder rein) wird beim Aufruf von /login zur Erstprofil-Seite geleitet"
    - "Bestandsuser (done) landen unveraendert auf Dashboard bzw. Coach-Weiche (kein Verhalten geaendert)"
    - "Der JSON-Register-Response (S3) traegt das Ziel; landing.html:931 nutzt es (window.location aus Response statt hart /dashboard)"
    - "Ein Skip-User sieht im Dashboard das Pflicht-Banner 'Noch kein Profil'"
    - "Prod-nerve traegt onboarding_state + skip_onboarding BEVOR der lesende Weichen-Code Traffic bekommt (Deploy-Sequenz D-23) — kein Login-Crash durch fehlende Spalte"
    - "Alle 3 realen Prod-User haben nach der Migration onboarding_state='done' + skip_onboarding default (Post-Migration-Verifikation, Punkt 13)"
  artifacts:
    - path: "routes/auth.py"
      provides: "S1 (already-logged-in :91) + S2 (JSON-Login :216-219 next=Ziel) + S3 (JSON-Register :258 Ziel in Response) via post_login_destination"
      contains: "post_login_destination"
    - path: "routes/oauth.py"
      provides: "S4 (:112 Bestand + :163 Neuanlage) via post_login_destination, VOR db.commit()/redirect (:107/:137, D-19)"
      contains: "post_login_destination"
    - path: "templates/marketing/landing.html"
      provides: "S3-JS-Fix :931 (window.location aus data.next/Response statt hart '/dashboard')"
      contains: "data.next"
    - path: "templates/dashboard.html"
      provides: "Skip-Banner 'Noch kein Profil' (D-12) sichtbar bei onboarding_state='skipped' ohne Profil"
      contains: "Noch kein Profil"
    - path: "tests/test_login_destination_wiring.py"
      provides: "E2E-Waechter: Register→Erstprofil-Ziel im Response; S1-Redirect; done→kein Redirect; real-PG nerve_test; ERST-ROT"
      contains: "onboarding"
  key_links:
    - from: "routes/auth.py api_register/api_login/login"
      to: "post_login_destination(user) → next/redirect"
      via: "Import aus services.onboarding_routing"
      pattern: "post_login_destination"
    - from: "routes/oauth.py _oauth_login_or_create"
      to: "post_login_destination VOR db.commit()/redirect"
      via: "Weiche vor :107 und :137"
      pattern: "post_login_destination"
    - from: "Prod-nerve alembic upgrade head (0030→0032)"
      to: "onboarding_state/skip_onboarding existieren VOR Code-Restart"
      via: "manueller ssh sudo -u postgres alembic upgrade head (D-23)"
      pattern: "alembic upgrade head"
---

<objective>
Die Verkabelung + die Prod-Scharfschaltung: (1) `post_login_destination` an alle 4 Andock-Stellen (S1 already-logged-in, S2 JSON-Login, S3 JSON-Register, S4 OAuth) haengen — mit dem +1-Zeilen-JS-Fix in landing.html (S3) und der Commit-Reihenfolge-Sicherung bei OAuth (D-19: Weiche VOR commit/redirect); (2) das Skip-Banner im Dashboard sichtbar machen (D-12); (3) die VERBINDLICHE Deploy-Sequenz D-23: Prod-`nerve` MANUELL migrieren (0030→0032) BEVOR / gekoppelt mit dem Code-Restart, sonst crasht der onboarding_state-lesende Code am fehlenden Spalten-Zugriff.

**★ Deploy-Sequenz-Falle (D-23, Punkt 26 Schreib-vor-Lese):** `deploy.sh` startet die App mit neuem Code neu, migriert Prod-`nerve` aber NICHT (nur nerve_test). Geht der lesende Code (post_login_destination an den Login-Docks) live BEVOR 0032 auf Prod lief → jeder Login-Request der `g.user.onboarding_state` liest crasht (Spalte fehlt). Deshalb ist Task 4 (manuelle Prod-Migration + Post-Verifikation) autonomous:false — Claudian fuehrt den ssh-Schritt, und er MUSS VOR dem code-restart-Deploy laufen.

Baut auf Plan 01 (Migration) + Plan 02 (Router + Erstprofil-Seite) → Wave 3.

Purpose: Die Weiche geht scharf — Neu-Registrierung/Login/OAuth landen zustandsgerecht — ohne Login-Crash durch die Schreib-vor-Lese-Naht.
Output: auth.py/oauth.py-Andock + landing.html-JS-Fix + dashboard-Banner + Wiring-Waechter + geordnete Prod-Deploy-Sequenz.

Komplexität: 🔴-nah — Auth-Pfad-Verkabelung + Prod-Migrations-Sequenz. **Cross-AI (Gemini + Fable) PFLICHT vor Execute** (Punkt 24 + Migrations-/Auth-Trigger). Supervised Execute (André fuehrt Deploy-Gates, kein Auto-Advance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-01-PLAN.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-02-PLAN.md

<interfaces>
<!-- Verifizierte Anker (2026-07-08). ⚠ ALLE Zeilen per INHALT re-ankern (Punkt 20, 3x Drift) — vor jedem Edit frisch greppen. -->

Router-Contract (aus Plan 02): `from services.onboarding_routing import post_login_destination`; `post_login_destination(user)` → URL-str ODER None ('nichts tun').

S1 — already-logged-in (VERIFIED routes/auth.py:86-96, `login()`):
```python
    nxt = safe_next(request.args.get('next'))
    if 'user_id' in session:
        return redirect(nxt) if nxt else redirect(url_for('dashboard.index'))   # :91
```
→ VOR dem Standard-Redirect die Weiche einschieben: wenn nxt fehlt, `dest = post_login_destination(g.user or geladener User)` → falls dest, dorthin; sonst dashboard. ⚠ In `login()` ist g.user evtl. nicht gesetzt (kein login_required) → User frisch laden (`db.get(User, session['user_id'])`) fuer die Weiche, ODER die Weiche liest robust via getattr.

S2 — JSON-Login (VERIFIED routes/auth.py:204-219, `api_login`):
```python
    resp = {'ok': True, 'coach': user_info['is_coach']}
    if nxt:
        resp['next'] = nxt
    return jsonify(resp)                                   # :219
```
→ wenn KEIN nxt: `dest = post_login_destination(<user>)`; falls dest, `resp['next'] = dest`. landing.html:862 (`data.next`) konsumiert es bereits (S2-Naht steht). ⚠ `_do_login` gibt user_info-dict zurueck, nicht das User-Objekt → fuer die Weiche entweder das User-Objekt durchreichen ODER post_login_destination so bauen dass es mit user_info-dict-Feldern arbeitet. Sauberer: `_do_login`/`_login_user` das User-Objekt-relevante (onboarding_state/skip_onboarding/rolle/org_id/active_profile_id) in user_info aufnehmen ODER den User frisch laden. Executor waehlt den kleinsten Eingriff (Punkt 27) — empfohlen: User frisch via id laden und post_login_destination(user) rufen.

S3 — JSON-Register (VERIFIED routes/auth.py:250-258, `api_register`):
```python
        db.commit()
        _login_user(db, user)
        session['welcome_trial'] = True
        ... send_welcome ...
        return jsonify({'ok': True})                       # :258
```
→ `dest = post_login_destination(user)`; `return jsonify({'ok': True, 'next': dest})` (dest ist die Erstprofil-URL fuer pending+owner+0 Profile — Neuuser ist genau das). ⚠ user ist hier noch das frische Objekt (nach _create_org_and_user + commit) → onboarding_state='pending' (DB-Default aus 0032). Profil-Count=0 → Weiche gibt Erstprofil-URL.

S3-JS-Fix (VERIFIED templates/marketing/landing.html:918-933, `submitRegister`):
```javascript
    const data=await res.json();
    if(data.ok){window.location='/dashboard';}             // :931 — ignoriert next HEUTE
```
→ auf `window.location = safeNextParam(data.next) || '/dashboard';` umstellen (nutzt den bestehenden safeNextParam-Guard :838, + data.next aus S3). +1-Zeilen-Fix.

S4 — OAuth (VERIFIED routes/oauth.py:74-165, `_oauth_login_or_create`):
- Bestand-Pfad: `db.commit()` (:107) → `return redirect(url_for('dashboard.index'))` (:112).
- Neuanlage-Pfad: `db.commit()` (:137) → ... → `return redirect(url_for('dashboard.index'))` (:163).
→ ★ D-19 Commit-Falle: die Weiche muss VOR :107/:137-commit laufen — genauer: `dest = post_login_destination(user/new_user)` BERECHNEN (der COUNT-Query in der Weiche darf nach dem User-Commit laufen, aber der Redirect muss `dest or url_for('dashboard.index')` sein). Sauber: nach `_login_user` + commit den `dest` berechnen und `return redirect(dest or url_for('dashboard.index'))` an BEIDEN Ruecksprung-Stellen (:112 + :163). Beide gehen durch dieselbe Funktion → 1 Einbau-Muster, 2 Rueckspruenge. ⚠ Die Selbstheilung in der Weiche (Stufe 3, commit) darf die OAuth-Session-Transaktion nicht stoeren → die Weiche nutzt eine EIGENE get_session (Plan 02, tut sie).

D-17 (VERIFIED routes/auth.py:189-198, `_create_org_and_user`): setzt `onboarding_done=False` explizit. onboarding_state kommt via DB-Default 'pending' (0032) — KEIN expliziter Setzer noetig (D-17). Deckt Formular-Register + OAuth-Neuanlage + Invite (alle durch _create_org_and_user). ⚠ Falls das ORM-Objekt nach flush onboarding_state noch nicht kennt (server_default): nach commit `db.refresh(user)` ODER post_login_destination laedt frisch — sicherstellen dass der Weichen-Read 'pending' sieht.

Dashboard-Banner (VERIFIED routes/dashboard.py index() rendert dashboard.html; templates/dashboard.html): D-12 Banner 'Noch kein Profil' bei onboarding_state='skipped' + 0 Profile. Plan 02 setzt schon flash('Noch kein Profil ...','warning') beim Skip — falls das Dashboard Flash-Messages rendert, ist das Banner bereits sichtbar (grep dashboard.html/base.html nach flash-Rendering). Falls kein Flash-Render im Dashboard: ein Template-Flag setzen (index() gibt `show_no_profile_banner = (g.user.onboarding_state=='skipped')` ans Template). Executor prueft per grep welcher Weg der kleinste ist (Punkt 27).
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" + „Verify=Production". -->
- Deploy-Gate: `bash deploy.sh production` (pytest gegen nerve_test, hat 0032). test_login_destination_wiring.py auto-collected.
- ★ PROD-MIGRATION (Task 4, D-23, VERBINDLICH VOR Code-Restart-Deploy): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/alembic upgrade head"'` (Muster docs/cutover-runbook.md:64-116). Faehrt 0030→0031→0032 (0031 COMMENT-only, idempotent).
- Post-Migration-Verifikation: `inspect.sh schema users` (Spalten da) + `sudo -u postgres psql -d nerve -c "SELECT id, onboarding_state, skip_onboarding FROM users"` (alle 3 = 'done' / FALSE).
- Live-Browser-Test mit Test-User: Neu-Registrierung → Erstprofil-Seite; Skip → Dashboard+Banner; Bestandsuser → Dashboard/Coach unveraendert.
- url_for-Verifikation (routes/CLAUDE.md): onboarding.wizard / dashboard.index / auth.login aufloesbar.
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wiring-Waechter test_login_destination_wiring.py — E2E gegen echte Login-/Register-Pfade, ERST-ROT</name>
  <read_first>
    - tests/conftest.py — cleanup_rows, nerve_test-DSN, csrf_client-Muster (AUTH-1 test_signup_journey.py — CSRF via client.application.config, register-only-Grund audit_log/0026).
    - tests/test_signup_journey.py (AUTH-1) — Register-E2E-Vorlage inkl. Cleanup Session->User->Org + @nerve.local-Email (send_welcome-Falle).
    - routes/auth.py:204-258 (api_login/api_register) + services/onboarding_routing.py (Plan 02).
    - CONTEXT.md D-16 (4 Docks) + D-02 (done→kein Redirect).
    - Gegen HEAD: die Docks sind noch nicht verdrahtet → Register-Response hat kein 'next' auf Erstprofil → Test ROT.
  </read_first>
  <behavior>
    - E2E via test-client gegen echte Routen (API-Response-Assertion, KEIN source-presence). Real-PG nerve_test (Skip-Guard). CSRF im Test AN (client.application.config, AUTH-1-Muster). Register-only-Verlaengerung vermeiden (audit_log/0026-Grund als Kommentar).
    - `test_register_owner_lands_on_erstprofil`: POST /api/register (Email @nerve.local, Owner-Neuanlage) → assert resp.status_code==200 UND resp.json()['next'] == '/onboarding/' (S3 gibt Erstprofil-Ziel; Neuuser = pending+owner+0 Profile). Cleanup Session->User->Org.
    - `test_login_done_user_no_onboarding_redirect`: einen User mit onboarding_state='done' anlegen, POST /api/login → resp.json() hat KEIN 'next' auf /onboarding/ (D-02, kein Redirect; ok:true). Cleanup.
    - (Optional, wenn im Test-Client praktikabel) `test_s1_already_logged_in_pending_redirects`: Session mit pending-Owner (0 Profile) setzen, GET /login → 302 auf /onboarding/. Falls Session-Setup im Test zu sperrig → als Live-Browser-Check (Task 4) belegen und hier weglassen (dokumentieren).
    - ASCII Namen; committende Rows raeumen.
  </behavior>
  <action>
    Erstelle `tests/test_login_destination_wiring.py` (AUTH-1 test_signup_journey.py als Vorlage — csrf_client, cleanup_tracker Session->User->Org, @nerve.local-Email, register-only-Kommentar).

    `test_register_owner_lands_on_erstprofil`: wie oben, `assert resp.get_json()['next'] == '/onboarding/'`. (⚠ url_for('onboarding.wizard') = '/onboarding/' — im Test ggf. via app-context bestaetigen statt hart, aber der String ist der Contract.)

    `test_login_done_user_no_onboarding_redirect`: User done anlegen (direktes INSERT/Anlage-Pfad + onboarding_state='done'), POST /api/login mit dessen Credentials → `data=resp.get_json(); assert data['ok']; assert data.get('next') != '/onboarding/'`.

    SEQUENZ (ERST-ROT): zuerst committen+deployen OHNE Task 2/3 (Docks) → gegen HEAD gibt Register-Response kein 'next'==/onboarding/ → `test_register_owner_lands_on_erstprofil` ROT (Gate blockt). DANN Task 2/3 → gruen. Rot-Beleg ins SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_login_destination_wiring.py auto-collected. VOR Task 2/3: test_register_owner_lands_on_erstprofil ROT (kein next). NACH Task 2/3: gruen, kein Row-Leak.</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert; grep zeigt echte `client.post('/api/register'...)` + `resp.get_json()['next']`-Assertion (KEIN source-presence).
    - grep zeigt `== '/onboarding/'` (Erstprofil-Ziel) + den done→kein-Redirect-Test.
    - CSRF via client.application.config; @nerve.local-Email; Cleanup Session->User->Org; Skip-Guard nicht-postgresql.
    - Rot-Beleg dokumentiert (gegen HEAD blockt das Gate am fehlenden 'next').
  </acceptance_criteria>
  <done>test_login_destination_wiring.py existiert, E2E gegen echte Register-/Login-Pfade, gegen HEAD nachweislich ROT (kein Erstprofil-Ziel), sauberer Cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Andock S1+S2+S3 in auth.py + S3-JS-Fix in landing.html</name>
  <read_first>
    - `grep -n "def login\|def api_login\|def api_register\|user_id' in session\|resp\['next'\]\|return jsonify({'ok': True})" routes/auth.py` (S1 :91, S2 :216-219, S3 :258 per INHALT frisch ankern).
    - `grep -n "window.location='/dashboard'\|data.next\|safeNextParam" templates/marketing/landing.html` (S3-JS :931 + safeNextParam :838 per INHALT).
    - services/onboarding_routing.py (Plan 02 Contract).
    - CONTEXT.md D-16 (S1/S2/S3) + D-17 (kein expliziter Setzer, DB-Default).
  </read_first>
  <action>
    Import oben in auth.py: `from services.onboarding_routing import post_login_destination`.

    S1 (`login()`, ~:86-96): im `if 'user_id' in session:`-Zweig, wenn KEIN nxt: User frisch laden (`db = get_session(); u = db.get(User, session['user_id']); db.close()` — oder bestehende Lade-Stelle nutzen), `dest = post_login_destination(u)`; `return redirect(nxt or dest or url_for('dashboard.index'))`. (nxt behaelt Vorrang, dann Weiche, dann Dashboard.)

    S2 (`api_login`, ~:216-219): nach `resp = {'ok': True, 'coach': ...}`, wenn KEIN nxt: den User frisch laden (via user_info['id']) und `dest = post_login_destination(u)`; `if dest: resp['next'] = dest`. (nxt behaelt Vorrang wie bisher.) ⚠ kleinster Eingriff (Punkt 27): User via id laden — user_info-dict hat 'id' (auth.py:143).

    S3 (`api_register`, ~:250-258): nach `_login_user(db, user)` (user ist das frische Objekt), `dest = post_login_destination(user)`; `return jsonify({'ok': True, 'next': dest})`. ⚠ sicherstellen dass user.onboarding_state 'pending' ist (DB-Default 0032) — falls das ORM-Objekt es nach flush/commit nicht kennt, `db.refresh(user)` vor dem Weichen-Call. Kein expliziter onboarding_state-Setzer (D-17).

    S3-JS-Fix (landing.html ~:931, `submitRegister`): `window.location='/dashboard';` ersetzen durch `window.location = safeNextParam(data.next) || '/dashboard';` (nutzt den bestehenden safeNextParam-Guard :838). +1-Zeilen-Fix; die 3 anderen Landing-Fetches unangetastet.

    Umlaute: JS-Identifier ASCII; keine User-facing Strings geaendert. D-17: KEIN onboarding_state=... Zuweisungs-Statement in _create_org_and_user (grep-belegen dass es fehlt — DB-Default traegt).
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_login_destination_wiring.py::test_register_owner_lands_on_erstprofil GRUEN (S3 gibt next). Danach (nach Task 4 Prod-Migration) Live: Neu-Registrierung mit Test-User → landet auf /onboarding/.</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/auth.py zeigt `from services.onboarding_routing import post_login_destination` + `post_login_destination(` an S1, S2, S3 (3 Aufrufe).
    - grep zeigt S3: `jsonify({'ok': True, 'next': dest})` (Register-Response traegt Ziel).
    - grep zeigt S2: `resp['next'] = dest` nur wenn kein nxt (nxt behaelt Vorrang).
    - grep zeigt S1: `redirect(nxt or dest or url_for('dashboard.index'))` (oder aequivalente Reihenfolge nxt→dest→dashboard).
    - grep von landing.html zeigt `window.location = safeNextParam(data.next) || '/dashboard'` (S3-JS-Fix); die anderen 3 Fetches unveraendert.
    - grep bestaetigt: `_create_org_and_user` hat KEINEN `onboarding_state=`-Setzer (D-17, DB-Default).
    - deploy.sh production Gate gruen.
  </acceptance_criteria>
  <done>S1/S2/S3 rufen post_login_destination; S3-Response traegt das Erstprofil-Ziel; landing.html:931 nutzt data.next; kein expliziter onboarding_state-Setzer (DB-Default D-17); Wiring-Waechter gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Andock S4 in oauth.py (VOR commit/redirect, D-19) + Skip-Banner im Dashboard (D-12)</name>
  <read_first>
    - `grep -n "db.commit()\|return redirect(url_for('dashboard.index'))\|_oauth_login_or_create" routes/oauth.py` (Bestand-commit :107 + Redirect :112; Neuanlage-commit :137 + Redirect :163 — per INHALT frisch ankern; D-19 Commit-Falle).
    - `grep -n "get_flashed_messages\|flash\|onboarding" templates/dashboard.html templates/base.html` — pruefen ob Dashboard bereits Flash-Messages rendert (dann traegt der Skip-flash aus Plan 02 das Banner schon) ODER ob ein Template-Flag noetig ist.
    - routes/dashboard.py index() — wo `render_template('dashboard.html', ...)`; ggf. `show_no_profile_banner` ergaenzen.
    - services/onboarding_routing.py (Contract) + CONTEXT.md D-19 + D-12.
  </read_first>
  <action>
    (A) oauth.py — Import oben: `from services.onboarding_routing import post_login_destination`. An BEIDEN Ruecksprung-Stellen (Bestand ~:112, Neuanlage ~:163) den harten `return redirect(url_for('dashboard.index'))` ersetzen durch:
    ```python
    dest = post_login_destination(user)        # Bestand: user ; Neuanlage: new_user
    return redirect(dest or url_for('dashboard.index'))
    ```
    ⚠ D-19: die Weiche wird NACH `_login_user` + `db.commit()` (:107/:137) berechnet, aber der Redirect nutzt ihr Ergebnis — die Weiche laeuft also VOR dem tatsaechlichen Redirect (nicht am Router vorbei). Die Weiche hat eine EIGENE get_session (Plan 02) → stoert die OAuth-Transaktion nicht. Fuer die Neuanlage: new_user.onboarding_state = 'pending' (DB-Default 0032) → falls das ORM-Objekt es nicht kennt, `db.refresh(new_user)` vor dem Weichen-Call (analog S3). 1 Einbau-Muster, 2 Rueckspruenge (D-16 S4).

    (B) Skip-Banner (D-12): den von Plan 02 gesetzten `flash('Noch kein Profil ...','warning')` sichtbar machen. Kleinster Weg (Punkt 27, per grep entscheiden):
      - Wenn dashboard.html/base.html bereits `get_flashed_messages()` rendert → nichts weiter noetig, das Banner erscheint (grep-belegen + Live-Check).
      - Wenn NICHT: in dashboard.py index() `show_no_profile_banner = (getattr(g.user,'onboarding_state',None)=='skipped')` ans Template geben und in dashboard.html ein persistentes Banner-Element (nerve.css n-* Alert) rendern das bei show_no_profile_banner erscheint (persistenter als Flash, D-12 „Pflicht" → besser persistent als einmaliger Flash). Text: „Noch kein Profil — lege eins an, sonst ist Training nicht nutzbar." (User-facing, Umlaute; „Profil anlegen"-Link auf url_for('onboarding.wizard')).
    Empfehlung: persistentes Banner (Template-Flag), weil Flash nur einmal erscheint und D-12 „Pflicht-Banner" dauerhaft meint bis Profil existiert.

    Umlaute: HTML-IDs/Flags ASCII (`show_no_profile_banner`); Banner-Text mit Umlauten.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen. Nach Task 4 (Prod-Migration) Live mit Test-User: OAuth-Neuuser (falls Test-OAuth verfuegbar) → Erstprofil; Skip → Dashboard zeigt persistentes Banner 'Noch kein Profil'.</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/oauth.py zeigt `post_login_destination(` 2x (Bestand + Neuanlage) + `redirect(dest or url_for('dashboard.index'))` an beiden Ruecksprung-Stellen.
    - grep bestaetigt: post_login_destination-Aufruf steht VOR dem finalen redirect (D-19, nicht danach umgangen).
    - Skip-Banner: grep von dashboard.html zeigt `Noch kein Profil` + (falls Flag-Weg) `show_no_profile_banner`; Banner-Link auf onboarding.wizard.
    - deploy.sh production Gate gruen.
  </acceptance_criteria>
  <done>S4 (OAuth Bestand + Neuanlage) rufen post_login_destination VOR dem Redirect (D-19); Skip-User sieht persistentes Dashboard-Banner 'Noch kein Profil' (D-12).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: ★ VERBINDLICHE Deploy-Sequenz (D-23) — Prod-nerve MANUELL migrieren (0030→0032) VOR Code-Restart + Post-Migration-Verifikation + Live-UAT</name>
  <read_first>
    - docs/cutover-runbook.md:64-116 (manueller Prod-alembic-upgrade-Weg, sudo -u postgres DATABASE_URL=@/nerve).
    - CONTEXT.md D-21/D-22/D-23 (Prod manuell migrieren; 0031 COMMENT-only idempotent; deploy.sh migriert nur nerve_test; Schreib-vor-Lese-Falle).
    - inspect.sh (schema users / migrations).
  </read_first>
  <what-built>
    Plan 01 lieferte Migration 0032; Plan 02/03 lieferten den onboarding_state-LESENDEN Weichen-Code (jetzt an den 4 Login-Docks). Der lesende Code ist committet, aber Prod-`nerve` hat die Spalten NOCH NICHT (deploy.sh migriert nur nerve_test). Dieser Schritt migriert Prod-`nerve` MANUELL, BEVOR der lesende Code Traffic bekommt — sonst Login-Crash (D-23).
  </what-built>
  <how-to-verify>
    Claudian fuehrt in dieser exakten Reihenfolge (VERBINDLICH):

    1. **Vorab-Check**: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` → alembic steht auf 0030 (oder 0031). `inspect.sh schema users` → onboarding_state/skip_onboarding NOCH NICHT da.

    2. **Prod-Migration (0030→0031→0032, 0031 COMMENT-only idempotent, D-22)**:
       ```
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/alembic upgrade head"'
       ```
       ⚠ Dieser Schritt MUSS laufen BEVOR der Code-Restart-Deploy (`deploy.sh production`) den lesenden Weichen-Code live schaltet. Reihenfolge: (a) Prod-Migration → (b) Verifikation Schritt 3 → (c) `bash deploy.sh production` (Code-Restart). Wenn der Code bereits live ist bevor migriert wurde: der alte Code (ohne onboarding_state-Read) laeuft weiter bis zum Restart — daher Migration + Restart eng koppeln, Migration zuerst.

    3. **Post-Migration-Verifikation (Pflicht, Punkt 13 Real-Daten)**:
       - `inspect.sh schema users` → onboarding_state (text, not null) + skip_onboarding (boolean) EXISTIEREN.
       - `ssh ... 'sudo -u postgres psql -d nerve -c "SELECT id, onboarding_state, skip_onboarding FROM users ORDER BY id"'` → alle 3 realen User: onboarding_state='done' (Backfill), skip_onboarding=false.
       - `inspect.sh migrations` → alembic auf 0032.

    4. **Code-Restart-Deploy**: `bash deploy.sh production` (pytest-Gate gruen inkl. aller 3 AUTH-2-Waechter, dann Restart). Jetzt liest der Weichen-Code gegen existierende Spalten → kein Crash.

    5. **Live-Browser-UAT mit Test-User (Sicht statt Zusicherung)**:
       - Neu-Registrierung (Formular) → landet auf Erstprofil-Seite (/onboarding/).
       - Erstprofil absenden (Branche + Feld) → Dashboard, aktives Profil gesetzt (Training nicht 404).
       - Skip auf Erstprofil → Dashboard mit persistentem Banner „Noch kein Profil".
       - Bestehender done-User (z.B. admin) Login → Dashboard/Coach unveraendert (kein /onboarding/-Redirect).
       - (falls Test-OAuth verfuegbar) OAuth-Neuuser → Erstprofil.
  </how-to-verify>
  <resume-signal>Tippe „migriert+verifiziert" (mit den 3 psql-Zeilen onboarding_state='done') ODER beschreibe Abweichungen. Bei Migrations-Fehler: KEIN Code-Restart, alter Code bleibt live (liest onboarding_state nicht), Rollback via `alembic downgrade 0031` + Analyse.</resume-signal>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN via post_login_destination an 4 Docks: onboarding_state/skip_onboarding/rolle/active_profile_id; SCHREIBEN nur indirekt via Router-Selbstheilung + Erstprofil aus Plan 02). Prod-`nerve` wird in Task 4 MANUELL auf 0032 migriert (Backfill 'done').

### inspect.sh-Beleg (VERBATIM `\d users` Prod-nerve, 2026-07-08 — nicht zusammengefasst)
```
users (public) — relevante Spalten (Prod-nerve, \d users, 2026-07-08):
  id                integer      NOT NULL (PK, nextval users_id_seq)
  org_id            integer      NOT NULL  FK→organisations(id)
  rolle             varchar(50)  NULL
  active_profile_id integer      NULL       FK→profiles(id)
  onboarding_done   boolean      NULL       ← DEPRECATED (nur Leser: auth.py/oauth.py)

AUTH-2-RELEVANTE DELTAS:
  onboarding_state  → nach Task-4-Prod-Migration: TEXT NOT NULL Default 'pending', Backfill alle 3→'done', CHECK
  skip_onboarding   → nach Task-4-Prod-Migration: boolean Default FALSE

Prod-Ist-Stand VOR Task 4: alembic 0030, 3 users (onboarding_done 2×t/1×f → Backfill setzt onboarding_state alle auf 'done'), 4 orgs, 3 profiles.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg) + kann Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `g.user.onboarding_state` | gelesen in post_login_destination (S1-S4) | DB-Spalte users.onboarding_state (0032) | ★ KRITISCH: der Leser (Login-Code) laeuft NACH dem Schreiber (Migration 0032) NUR WENN Task 4 die Prod-Migration VOR dem Code-Restart fuehrt. Sonst liest der Code gegen eine NICHT-EXISTENTE Spalte → Crash (D-23). Innerhalb eines Requests: g.user ist frisch (login_required auth.py:51), synchron, kein async-Versatz | ✓ Task 4 ordnet Migration VOR Restart + Post-Verifikation |
| `user.skip_onboarding` | gelesen Stufe 1 (S1-S4) | DB-Spalte (0032) | wie oben: existiert erst nach Task-4-Prod-Migration; Setz-UI AUTH-4 | ✓ Task 4 |
| Register-Response `next` | S3 schreibt Ziel in JSON → landing.html:931 window.location | RAM/Request-scope (JSON-Response) + Client-JS | synchron pro Request; Client liest nach Response | ✓ landing.html data.next |
| OAuth-Redirect `dest` | S4 berechnet VOR Redirect (D-19) | RAM/Request-scope | synchron; Weiche VOR redirect, nicht danach umgangen | ✓ D-19 Reihenfolge |
| `new_user.onboarding_state` | S3/S4 lesen 'pending' nach Anlage (DB-Default) | DB-Spalte via server_default 0032 | ★ Schreib-vor-Lese im Request: _create_org_and_user flush/commit setzt server_default 'pending'; falls ORM-Objekt es nicht kennt → db.refresh(user) VOR dem Weichen-Call (Task 2/3) → Leser liest garantiert 'pending', nicht None/stale | ✓ db.refresh-Absicherung in Task 2/3 |

### Punkt 26 Schreib-vor-Lese-Analyse (der Kern dieses Plans)
Zwei Schreib-vor-Lese-Naehte:
1. **Prod-Ebene (D-23, kritisch):** der onboarding_state-lesende Code darf NICHT live gehen bevor die Spalte auf Prod-`nerve` existiert. Aufgeloest durch Task 4 (autonomous:false): manuelle Prod-Migration + Verifikation VOR dem Code-Restart-Deploy.
2. **Request-Ebene (Neuanlage):** S3/S4 lesen `new_user.onboarding_state` direkt nach der Anlage. Der server_default 'pending' (0032) greift auf DB-Ebene; das ORM-Objekt kennt ihn nach flush evtl. nicht → `db.refresh(user)` vor dem Weichen-Call sichert, dass der Leser 'pending' sieht (nicht None → sonst faellt die Weiche faelschlich in Stufe 4). Explizit in Task 2/3 verankert.

### Bei Diskrepanz: STOP + Replan.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anonymer/eingeloggter Browser → /api/register, /api/login, /login, OAuth-Callback | Login-Pfade, die jetzt die Weiche fahren |
| Client-JS landing.html → window.location aus data.next | Redirect-Ziel aus Server-Response |
| Claudian ssh → Prod-nerve alembic upgrade | manuelle Schema-Migration an Prod mit echten Nutzern |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH2-10 | Denial of Service (Login-Crash: lesender Code vor Spalten-Existenz) | onboarding_state-Read live vor Prod-Migration | mitigate | Task 4 (autonomous:false): Prod-Migration + Post-Verifikation VOR Code-Restart (D-23); bei Migrations-Fehler kein Restart |
| T-AUTH2-11 | Elevation/Bypass (OAuth redirectet am Router vorbei) | oauth.py commit/redirect-Reihenfolge | mitigate | D-19: post_login_destination VOR dem finalen redirect an beiden Ruecksprung-Stellen; eigene get_session stoert OAuth-Tx nicht |
| T-AUTH2-12 | Tampering (Open-Redirect via data.next) | landing.html:931 window.location | mitigate | safeNextParam(data.next)-Guard (landing.html:838, AUTH-1) validiert same-origin; dest kommt server-seitig aus url_for (kein User-Input) |
| T-AUTH2-13 | Info-Leak (stale/None onboarding_state → falsche Weiche fuer Neuuser) | S3/S4 Read direkt nach Anlage | mitigate | db.refresh(user) vor Weichen-Call → server_default 'pending' garantiert gelesen (Punkt 26) |
| T-AUTH2-14 | Repudiation (Migration ohne Verifikation → stiller Teil-Backfill) | Task-4-Prod-Migration | mitigate | Post-Migration-Verifikation Pflicht: 3 realen User onboarding_state='done' via psql (Punkt 13) |
</threat_model>

<verification>
- deploy.sh production Gate gruen MIT test_login_destination_wiring.py PASSED (real-PG).
- test_register_owner_lands_on_erstprofil: ROT gegen HEAD (kein 'next', vor Task 2/3), gruen nach Andock — dokumentiert.
- Prod-Migration (Task 4): alembic 0032, inspect.sh schema users zeigt beide Spalten, 3 reale User onboarding_state='done' (psql-Beleg im SUMMARY).
- Deploy-Reihenfolge belegt: Migration + Verifikation VOR Code-Restart (kein Login-Crash).
- Live-Browser-UAT mit Test-User: Neu-Register→Erstprofil; Skip→Dashboard+Banner; done-User→Dashboard/Coach unveraendert (Sicht statt Zusicherung).
- OAuth (S4) redirectet nicht am Router vorbei (D-19).
</verification>

<success_criteria>
- post_login_destination an allen 4 Docks (S1-S4); OAuth VOR commit/redirect (D-19).
- landing.html:931 nutzt data.next (S3-JS-Fix); Register-Response traegt Erstprofil-Ziel.
- Skip-User sieht persistentes Dashboard-Banner (D-12).
- Prod-nerve auf 0032 migriert VOR Code-Restart (D-23), 3 reale User onboarding_state='done' verifiziert.
- Wiring-Waechter im Deploy-Gate, gegen HEAD rot gewesen.
- Kein expliziter onboarding_state-Setzer (DB-Default D-17).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-03-SUMMARY.md` erstellen (inkl. Rot-Beleg des Wiring-Waechters, Prod-Migrations-psql-Beleg der 3 User=done, Deploy-Reihenfolge-Beleg, Live-UAT-Beleg der 4 Faelle).
</output>
