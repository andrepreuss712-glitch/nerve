---
phase: 08.23.2.AUTH-2
plan: 03
type: execute
wave: 3
complexity: "🟡 mittel (ORM-Spalten in models.py + app.py-Landmine-Stilllegung — Schritt 1c der Expand/Contract-Sequenz, geht NACH der Prod-Migration live; Cross-AI Pflicht VOR Execute)"
depends_on: ["08.23.2.AUTH-2-01", "08.23.2.AUTH-2-02"]
files_modified:
  - database/models.py
  - app.py
autonomous: true
requirements: []   # Scope = CONTEXT-Decisions D-06/D-10 (ORM-Schild + Landmine)
user_setup: []

must_haves:
  truths:
    - "database/models.py traegt User.onboarding_state + User.skip_onboarding als ORM-Spalten mit comment= (Schild), WORTGLEICH-Kern zu den Migrations-COMMENTs aus 0032"
    - "onboarding_done comment= traegt in models.py den [DEPRECATED]-Marker (eingefroren, nicht gedroppt)"
    - "Der ORM SELECT * (auth.py:51) liest onboarding_state jetzt gefahrlos — die Spalte existiert auf Prod (Plan 02 lief davor, Schritt 1c NACH 1b)"
    - "Der Startup-Flip app.py (_migrate 08.12 UPDATE onboarding_done=1) + Legacy-ADD-COLUMN + _seed tragen einen D-10-Warn-Kommentar (NIE auf onboarding_state portieren) — keine Verhaltensaenderung (Postgres-inert)"
  artifacts:
    - path: "database/models.py"
      provides: "User.onboarding_state + User.skip_onboarding ORM-Spalten mit comment= (Schild) + onboarding_done comment= mit [DEPRECATED]-Marker"
      contains: "onboarding_state"
    - path: "app.py"
      provides: "Landmine-Stilllegung: _migrate-08.12-Flip + Legacy-ADD-COLUMN + _seed onboarding_done kommentiert 'NIE auf onboarding_state portieren'"
      contains: "onboarding_state"
  key_links:
    - from: "database/models.py User"
      to: "onboarding_state Schild + onboarding_done DEPRECATED-Marker"
      via: "Column(..., comment=...)"
      pattern: "comment="
    - from: "app.py 08.12-Flip / Legacy-ADD / _seed"
      to: "D-10-Landmine-Marker (nie auf onboarding_state portieren)"
      via: "Kommentar-Addition, keine Logik-Aenderung"
      pattern: "D-10 LANDMINE"
---

<objective>
★ EXPAND/CONTRACT — SCHRITT 1c (Finding 1, BLOCKER-Fix): die ORM-Scharfschaltung. Jetzt — NACHDEM Plan 02 Prod-`nerve` auf 0032 migriert hat (Spalten existieren auf Prod) — werden die ORM-Spalten `User.onboarding_state` + `User.skip_onboarding` in `database/models.py` eingetragen (mit Schild, D-06/Punkt 23) und die Startup-Flip-Landmine in `app.py` stillgelegt (D-10).

**Warum jetzt sicher (Finding 1):** `models.py` IST der Leser — der ORM macht `SELECT *` bei jedem Login (`auth.py:51`). Der frühere Plan trug die Spalten in models.py ein BEVOR Prod migriert war → Login-Crash. Jetzt lief Plan 02 (Prod-Migration) davor → die Spalte existiert auf Prod → der ORM `SELECT *` liest sie gefahrlos. Erst dieser Plan darf models.py anfassen.

**Kein Router, keine UI, keine Verkabelung in diesem Plan** — nur die ORM-Definition (holt das nach, was Plan 01 bewusst ausgelassen hat) + die Landmine-Kommentare. Der lesende Weichen-Code (post_login_destination) kommt in Plan 04/05.

Baut auf Plan 01 (Migrations-Datei) + Plan 02 (Prod migriert) → Wave 3.

Purpose: Die ORM-Definition an die real existierende Prod-Spalte nachziehen (Single-Source Schild, Punkt 23) und die Halb-Migration-Falle (Punkt 29) am `onboarding_done` sauber einfrieren — ohne Login-Crash, weil die Spalte jetzt auf Prod da ist.
Output: models.py-Schilder (onboarding_state/skip_onboarding + onboarding_done DEPRECATED) + app.py-Landmine-Stilllegung.

Komplexität: 🟡 mittel — ORM-Spalten (Leser scharf) + Landmine. **Cross-AI (Gemini + Fable) PFLICHT vor Execute.** Dieser Plan wird NICHT direkt executet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-01-PLAN.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-02-PLAN.md

<interfaces>
<!-- Verifizierte Anker (2026-07-08). ⚠ ALLE Zeilennummern per INHALT re-ankern (Punkt 20, 3x Drift). Zeilen = Wegweiser, kein Vertrag. -->

★ FINDING-1 / SCHRITT 1c: Prod-`nerve` traegt onboarding_state/skip_onboarding erst NACH Plan 02. Dieser Plan schaltet den ORM-Leser scharf (models.py `SELECT *`), was jetzt sicher ist (Spalte existiert auf Prod). Voraussetzung: Plan-02-SUMMARY bestaetigt 3 reale User = 'done'.

User-Modell (VERIFIED database/models.py:85):
```python
onboarding_done     = Column(Boolean, default=False, comment='Flag: Onboarding abgeschlossen')
```
→ per grep frisch ankern (`grep -n "onboarding_done" database/models.py`). NEUE Spalten daneben ergaenzen. onboarding_done bleibt (D-06, eingefroren) — comment= auf [DEPRECATED]-Marker aktualisieren, KEIN Drop. Der WORTGLEICH-Kern der comment=-Strings muss mit den 0032-Migrations-COMMENTs (Plan 01) uebereinstimmen (Single-Source, Punkt 23).

Boolean-server_default-Muster: `grep -n "server_default" database/models.py` → bestehendes Boolean-server_default-Muster kopieren (z.B. `sa.text('false')` oder `text('false')`); falls kein Muster, `default=False` (ORM-default reicht fuer neue Anlagen; die DB-server_default aus 0032 ist die Persistenz-Wahrheit).

★ LANDMINE-BEFUND (VERIFIED 2026-07-08, entschaerft die D-10-Panik, aber Stilllegung bleibt Pflicht):
- `_migrate()` (app.py ~:133) macht bei `db_url.startswith('postgresql')` einen **early return** (app.py ~:140-142: `[DB] _migrate skipped on Postgres`). D.h. der 08.12-Startup-Flip `UPDATE users SET onboarding_done=1` (app.py ~:831-846) UND die Legacy-ADD-COLUMN-Liste (`('onboarding_done', 'BOOLEAN DEFAULT 0')` app.py ~:155) laufen auf Prod-Postgres **NIE**. `_seed()` (app.py ~:1623) setzt `onboarding_done=True` (app.py ~:1643) nur bei `User.count()==0` → auf Prod (3 User) **NIE**.
- FOLGE: die Landmine ist auf Prod bereits inert. ABER D-10 verlangt Stilllegung/Kommentar, damit niemand das Flip-Muster jemals auf `onboarding_state` PORTIERT. → Nur Kommentar-Warnung setzen, KEINE Verhaltensaenderung noetig.

Deploy-Gate-Mechanik (VERIFIED deploy.sh:147-247): nerve_test hat via 0032 die Spalten → test_schild_guard.py laeuft gegen nerve_test gruen (die neuen ORM-Spalten haben comment= ≥10 Zeichen). deploy.sh:247 restartet Prod — jetzt sicher, weil Prod (Plan 02) die Spalte hat und der neue ORM sie liest.
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR ueber Production-Pfad. -->
- Deploy-Gate: `bash deploy.sh production` — pytest gegen nerve_test (hat 0032) VOR dem Restart. test_schild_guard.py + test_onboarding_state_migration.py gruen.
- ★ Reihenfolge (Finding 1): dieser Plan (models.py-Leser) geht NUR live NACHDEM Plan 02 (Prod-Migration) bestaetigt ist. Der deploy.sh-Restart (deploy.sh:247) liest dann onboarding_state gegen die existierende Prod-Spalte → kein Crash.
- Live-Schema: `inspect.sh schema users` (Spalten da, aus Plan 02).
- inspect.sh grep 'D-10 LANDMINE' app.py auf Prod bestaetigt deployed Stand.
</verify_environment>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ORM-Spalten in models.py (onboarding_state + skip_onboarding mit Schild) + onboarding_done DEPRECATED-Marker (Schritt 1c, Leser scharf NACH Prod-Migration)</name>
  <read_first>
    - Plan-02-SUMMARY — bestaetigt dass Prod-`nerve` auf 0032 ist (3 User = 'done'). ★ Ohne diese Bestaetigung NICHT starten (Finding 1: models.py-Leser darf erst nach Prod-Migration live).
    - `grep -n "class User" database/models.py` + die onboarding_done-Zeile frisch: `grep -n "onboarding_done" database/models.py` (per INHALT ankern, ~:85). Trivial-Spalten-Konvention Punkt 23 (onboarding_state/skip_onboarding sind NICHT-trivial → Schild Pflicht).
    - `grep -n "server_default" database/models.py` — bestehendes Boolean-server_default-Muster.
    - alembic/versions/0032_onboarding_state_skip_onboarding.py (Plan 01) — die COMMENT-Texte, damit die models.py-comment= WORTGLEICH im Kern uebereinstimmen (Single-Source Punkt 23).
    - CONTEXT.md D-05 (TEXT pending/done/skipped) / D-08 (skip_onboarding boolean) / D-06 (onboarding_done einfrieren).
  </read_first>
  <action>
    ★ VORBEDINGUNG (Finding 1): Plan-02-SUMMARY muss bestaetigen dass Prod-`nerve` auf 0032 migriert ist. Erst dann models.py editieren (sonst crasht der ORM `SELECT *` an auth.py:51 gegen Prod).

    `database/models.py` — im User-Modell frisch per grep die onboarding_done-Zeile ankern, dann:
    - onboarding_done comment= aktualisieren auf den [DEPRECATED]-Marker (WORTGLEICH-Kern zum Migrations-COMMENT in 0032, Single-Source Punkt 23): z.B. `comment='[DEPRECATED ab AUTH-2 — eingefroren, nicht droppen] Abgeloest durch onboarding_state. Nur noch Leser (auth.py/oauth.py), kein neuer Schreiber.'`
    - Neue Spalten direkt darunter ergaenzen (WORTGLEICH-Kern zu den Migrations-COMMENTs):
      ```python
      onboarding_state = Column(Text, nullable=False, server_default='pending',
          comment='Onboarding-Fortschritt der Weiche post_login_destination (pending|done|skipped; erweiterbar um step_*, D-09). Neue Wahrheitsquelle statt onboarding_done. Status: lebt. Schreibt routes/onboarding.py + DB-Default; liest routes/auth.py + routes/oauth.py.')
      skip_onboarding = Column(Boolean, nullable=False, server_default=<Boolean-false-Muster>,
          comment='Ueberspringt NUR Onboarding, NICHT Billing (organisations.skip_billing, AUTH-3/4). Status: Foundation, Setz-UI AUTH-4. Schreibt (spaeter) AUTH-4; liest routes/auth.py + routes/oauth.py.')
      ```
      (server_default fuer Boolean per Projekt-Muster — grep bestehende Boolean-server_default in models.py; falls kein Muster, `default=False`. Import Text/Boolean sicherstellen — `grep -n "from sqlalchemy" database/models.py`, Text ggf. ergaenzen.)
    - ASCII in comment-Strings (Code-Kommentar-Kontext; ae/oe/ue OK).
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_schild_guard.py gruen (neue Spalten haben Schild ≥10 Zeichen); test_onboarding_state_migration.py gruen. Restart liest onboarding_state gegen existierende Prod-Spalte (Plan 02) → kein Crash. Danach inspect.sh: kein Login-Crash im Log.</automated>
  </verify>
  <acceptance_criteria>
    - grep von database/models.py zeigt `onboarding_state` + `skip_onboarding` Columns mit comment= UND onboarding_done comment= enthaelt `[DEPRECATED`.
    - Die comment=-Kerne stimmen WORTGLEICH mit den 0032-COMMENTs ueberein (Single-Source Punkt 23).
    - Text/Boolean sind importiert (grep `from sqlalchemy`).
    - deploy.sh production Gate gruen; test_schild_guard.py PASSED; test_onboarding_state_migration.py PASSED.
    - ★ Reihenfolge-Beleg: Plan-02-SUMMARY (Prod auf 0032) lag vor diesem Deploy — kein Login-Crash nach Restart (inspect.sh logs-errors zeigt kein `column onboarding_state does not exist`).
  </acceptance_criteria>
  <done>models.py traegt onboarding_state + skip_onboarding ORM-Spalten mit Schild (WORTGLEICH-Kern zu 0032); onboarding_done ist [DEPRECATED]-markiert (nicht gedroppt); der ORM-Leser ist scharf und liest gegen die existierende Prod-Spalte (Schritt 1c NACH 1b) → kein Crash.</done>
</task>

<task type="auto">
  <name>Task 2: Landmine-Stilllegung (D-10) — Startup-Flip + Legacy-ADD-COLUMN + Seed dokumentieren, NIE auf onboarding_state portieren</name>
  <read_first>
    - `grep -n "onboarding_done" app.py` (frisch, per INHALT ankern — ~:155 Legacy-ADD-COLUMN, ~:831-846 08.12-Flip, ~:1643 _seed).
    - app.py ~:133-142 — `_migrate()`-Kopf + `if db_url.startswith('postgresql'): return` (der early-return, der die Landmine auf Prod inert macht — VERIFIED 2026-07-08).
    - app.py ~:1623-1648 — `_seed()`-Guard `if db.query(User).count() == 0` (laeuft auf Prod nie).
    - CONTEXT.md D-10 (Landmine: Flip darf NIE auf onboarding_state portiert werden).
  </read_first>
  <action>
    KEINE Verhaltensaenderung noetig (die 3 Stellen sind alle Postgres-/frisch-DB-inert, VERIFIED). NUR Warn-Kommentare setzen, damit das Muster nie auf onboarding_state wandert (D-10, Punkt 29 Anti-Halb-Migration):

    (a) app.py ~:831-846 (08.12-Flip `UPDATE users SET onboarding_done=1`): direkt ueber dem `try:` einen Kommentar ergaenzen:
    „★ AUTH-2 D-10 LANDMINE-MARKER: Dieser Startup-Flip ist SQLite-only (laeuft nie auf Prod — _migrate() early-returns bei Postgres, app.py ~:140). Er darf NIEMALS auf users.onboarding_state portiert/kopiert werden — ein Start-Flip auf onboarding_state wuerde jeden pending-Neuuser bei naechstem Start tot auf 'done' setzen und die Weiche (post_login_destination) aushebeln. onboarding_state ist ausschliesslich Migration 0032 (Anlage/Backfill) + Weichen-Uebergaenge (routes/onboarding.py). Kein Startup-Schreiber."

    (b) app.py ~:155 (Legacy-ADD-COLUMN-Liste, `('onboarding_done', 'BOOLEAN DEFAULT 0')`): Kommentar am Zeilenende oder darueber:
    „# AUTH-2 D-10: SQLite-Legacy-ADD-COLUMN (Prod via Alembic). KEINE onboarding_state-Zeile hier ergaenzen — die neue Spalte lebt ausschliesslich in Alembic 0032, kein _migrate()-Muster."

    (c) app.py ~:1643 (_seed `onboarding_done=True`): Kommentar darueber:
    „# AUTH-2 D-10: _seed laeuft nur bei User.count()==0 (nie auf Prod). onboarding_state NICHT hier setzen — DB-Default 'pending' (Migration 0032) greift; ein Seed-Setzer auf 'done' wuerde die Weiche fuer den Seed-Owner umgehen."

    Umlaute: reiner Code-Kommentar-Kontext → ASCII-Ersatz erlaubt (ae/oe/ue). KEINE Logik-Zeile aendern.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen (keine Verhaltensaenderung, nur Kommentare). inspect.sh grep 'D-10 LANDMINE' app.py auf Prod bestaetigt deployed Stand.</automated>
  </verify>
  <acceptance_criteria>
    - grep von app.py zeigt `D-10 LANDMINE-MARKER` ueber dem 08.12-Flip-Block.
    - grep von app.py zeigt an der Legacy-ADD-COLUMN-Stelle + der _seed-Stelle je einen `AUTH-2 D-10`-Kommentar mit dem Verbot „KEINE onboarding_state-Zeile / onboarding_state NICHT hier setzen".
    - KEINE Logik-Zeile geaendert (git diff zeigt nur Kommentar-Additionen an den 3 Stellen).
    - deploy.sh production Gate gruen.
  </acceptance_criteria>
  <done>Alle 3 Landmine-Stellen (Flip/Legacy-ADD/Seed) tragen einen D-10-Warn-Kommentar der das Portieren auf onboarding_state verbietet; keine Verhaltensaenderung (Stellen sind Prod-inert); der Flip beruehrt onboarding_state garantiert nie.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN: der ORM `SELECT *` an auth.py:51 liest jetzt onboarding_state/skip_onboarding mit — die ORM-Definition wird hier nachgezogen. KEIN Schreiben in diesem Plan; die Prod-Spalte kommt aus Plan 02).

### inspect.sh-Beleg (VERBATIM `\d users` Prod-nerve — NACH Plan 02, vor diesem Plan)
```
users (public) — relevante Spalten (Prod-nerve, \d users, nach Plan 02):
  id                integer      NOT NULL (PK)
  org_id            integer      NOT NULL  FK→organisations(id)
  onboarding_state  text         NOT NULL DEFAULT 'pending'  ← NEU (Plan 02 Migration), CHECK ck_users_onboarding_state
  skip_onboarding   boolean      NOT NULL DEFAULT false       ← NEU (Plan 02 Migration)
  onboarding_done   boolean      NULL       ← DEPRECATED-COMMENT (aus 0032)

Prod-Stand: alembic 0032 (nach Plan 02), 3 users onboarding_state='done'.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg) + kann Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `user.onboarding_state` | gelesen via ORM `SELECT *` (auth.py:51, login_required) | DB-Spalte users.onboarding_state (Plan 02 auf Prod) | ★ FINDING-1-KERN: der ORM-Leser (models.py) geht in DIESEM Plan live. Er liest gefahrlos, WEIL Plan 02 die Spalte auf Prod anlegte (1b vor 1c). Innerhalb Request synchron (g.user frisch) | ✓ Plan-02-SUMMARY (Prod 0032) + inspect.sh schema users |
| `user.skip_onboarding` | gelesen via ORM `SELECT *` | DB-Spalte users.skip_onboarding (Plan 02) | wie oben; existiert auf Prod seit Plan 02 | ✓ Plan-02-SUMMARY |
| `users.onboarding_done` | ORM comment=-Update (DEPRECATED-Marker) | DB-Spalte (bestehend) + pg_description (aus 0032) | keine Datenaenderung | ✓ inspect.sh schilder users |

### Punkt 26 Schreib-vor-Lese-Analyse (dieser Plan)
★ FINDING-1-ABSCHLUSS: dieser Plan schaltet den ORM-Leser (models.py `SELECT *`) scharf. Das ist der kritische Leser aus Finding 1. Er ist sicher, WEIL die Sequenz eingehalten ist: 1a (Plan 01, Datei, kein Leser) → 1b (Plan 02, Prod-Migration) → **1c (DIESER Plan, ORM-Leser live)**. Vorbedingung Task 1: Plan-02-SUMMARY bestaetigt Prod auf 0032. Ohne diese Bestaetigung darf models.py NICHT editiert werden.

### Bei Diskrepanz: STOP + Replan.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ORM `SELECT *` (auth.py:51) → DB users | jeder Login liest die (jetzt existierende) neue Spalte mit |
| deploy.sh Restart (deploy.sh:247) → laufende App | Restart schaltet den neuen ORM-Leser scharf |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH2-01c | ★ Denial of Service (Login-Crash: ORM-Leser scharf) | models.py onboarding_state + Restart (Finding 1 Schritt 1c) | mitigate | geht NUR live nach Plan-02-Prod-Migration (Vorbedingung Task 1); inspect.sh logs-errors bestaetigt kein column-does-not-exist |
| T-AUTH2-03 | Elevation/Bypass (Startup-Flip setzt Neuuser still auf done → Onboarding uebersprungen) | app.py 08.12-Flip auf onboarding_state portiert | mitigate | D-10-Landmine-Marker verbietet Portierung; Flip ist Postgres-inert (early-return); onboarding_state hat keinen Startup-Schreiber |
| T-AUTH2-04 | Repudiation (Halb-Migration: onboarding_done still weiterbeschrieben) | onboarding_done Zombie | mitigate | DEPRECATED-Marker (models.py comment=), kein neuer Schreiber; Drop deferred bis 0 Leser (Zombie-Regel) |
</threat_model>

<verification>
- deploy.sh production Gate gruen MIT test_schild_guard.py + test_onboarding_state_migration.py PASSED.
- ★ Reihenfolge belegt: Plan-02-SUMMARY (Prod 0032) lag vor diesem Deploy; inspect.sh logs-errors zeigt kein `column onboarding_state does not exist` nach Restart (Finding 1 aufgeloest).
- models.py: onboarding_state + skip_onboarding mit Schild; onboarding_done [DEPRECATED].
- git diff app.py zeigt NUR Kommentar-Additionen (keine Logik-Aenderung an der Landmine).
</verification>

<success_criteria>
- models.py traegt onboarding_state + skip_onboarding ORM-Spalten mit Schild (WORTGLEICH-Kern zu 0032); onboarding_done [DEPRECATED]-eingefroren, nicht gedroppt.
- Der ORM-Leser (SELECT *) geht sicher live (Schritt 1c NACH Plan-02-Prod-Migration) — kein Login-Crash.
- Startup-Flip/Legacy-ADD/Seed tragen D-10-Warn-Kommentare, onboarding_state hat keinen Startup-Schreiber.
- KEIN lesender Weichen-Code in diesem Plan (post_login_destination erst Plan 04/05).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-03-SUMMARY.md` erstellen (inkl. Bestaetigung dass Plan 02 davor lief / kein Login-Crash im Log, Schild-Guard-Beleg, Bestaetigung dass die Landmine Postgres-inert ist).
</output>
