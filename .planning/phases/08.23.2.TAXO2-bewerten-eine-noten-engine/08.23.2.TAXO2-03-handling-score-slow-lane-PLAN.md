---
phase: 08.23.2.TAXO2
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - services/slow_lane.py
  - services/handling_markers.py
  - database/models.py
  - alembic/versions/XXXX_abstain_log.py
  - tests/test_handling_score_marker.py
# INTERLOCK I-1 RESOLVED → Option b: handling_status-Spalte wird von TAXO1-01 gebaut (NICHT mehr von TAXO2).
# Die fruehere XXXX_intent_event_handling_status.py-Migration ENTFAELLT. Plan 03 nutzt die Spalte nur.
autonomous: true
requirements: ["SPEC Req 4", "D-03 Tor2", "D-07 (Abstention + Goodhart-Logging)"]
user_setup: []

must_haves:
  truths:
    - "intent_event hat die Spalte handling_status (pending|scored|abstained|failed) — der Wurzel-Fix gegen die dreifach-überladene NULL (F-01/F-05/F-06). INTERLOCK I-1 RESOLVED: die Spalte wird von TAXO1-01 gebaut (Option b, da TAXO1 ohnehin das vollstaendige Schema ab Tag 1 baut + leere Tabelle = kein Backfill); Plan 03 NUTZT sie nur (ORM-Abgleich + Statemachine), baut KEINE eigene Migration."
    - "Die Slow Lane befüllt intent_event.handling_score_numeric (1-3) regel-/marker-basiert mit großzügiger Abstention; Abstention setzt handling_status='abstained' (NICHT nur NULL)"
    - "Dauerhaft scheiternde Einzelbewertung setzt handling_status='failed' (Poison-Pill, F-05) — blockiert den Call-Ende-Merge NICHT"
    - "Das In-Place-Update ist race-frei — KEINE Fast/Medium-Lane liest intent_event-Zeilen zurück (ORM-AWARE grep-Beweis Pflicht-Gate, F-04)"
    - "Kein handling_score-Write aus Fast/Medium Lane (Bau-Regel 1); LLM-Urteil NUR async Slow Lane"
    - "Slow-Lane-Restart re-derived die Arbeitsliste aus der DB (WHERE handling_status='pending') — Multi-Worker-ready (FOR UPDATE SKIP LOCKED vorbereitet, Aktivierung Block M) (F-06)"
    - "Ein Bootstrap-Re-Queue (H-3) ist GEBAUT, nicht nur behauptet: am Consumer-Start UND periodisch über den TAXO1-02-`_periodic_tick()`-Haken werden `WHERE handling_status='pending'`-Events in die Queue nachgefüllt — sonst bleiben pending-Events nach Crash/Deploy für immer liegen (die Queue ist nach Restart leer, der Producer feuert nur für NEUE Events). Idempotent via handling_status-Skip; kein Re-Architecture (dockt am bestehenden Tick an)"
    - "Eine MINIMALE Hook-/Registrierungs-Schicht (FOLD 23.06.) ist angelegt: `_PERIODIC_TICK_HOOKS` + `_CALL_END_MERGE_STEPS` (Modul-Listen) + register-/run-Funktionen — Plan 04/06/07 hängen sich nur ein (append), statt _periodic_tick/den Merge je neu zu schreiben. H-3 ist als erster Tick-Hook registriert; die Call-End-Step-Liste ist hier LEER (Plan 04 baut den Merge-Gate + GUC-Klammer drumherum). Minimal: nur Listen + 3 Funktionen, keine Prioritäten/Klassen (kein Over-Engineering)"
    - "Idempotenz skippt WHERE handling_status != 'pending' (kein doppelter abstain_log bei Consumer-Restart, F-01)"
    - "Jedes Abstain-Event wird mit nachfolgendem Berater-Satz + interaction_id geloggt (D-07 Goodhart); abstain_log hat harten FK CASCADE (F-08)"
    - "DSGVO-Cascade-Kette calls → intent_event → abstain_log ist DURCHGEHEND: Call-Löschung räumt BEIDE Kind-Tabellen (0 Rest-Zeilen), inkl. der TAXO1-Naht intent_event.call_id ON DELETE CASCADE (F-08, REVIEWS)"
  artifacts:
    # INTERLOCK I-1 RESOLVED: handling_status-Spalte = TAXO1-01-Artefakt (Option b). KEINE TAXO2-Migration dafuer.
    - path: "services/handling_markers.py"
      provides: "Anker-Marker-Regeln Label/Frage vs Gegenargument → score 1-3 ODER Abstention (rein, testbar)"
      contains: "def grade_handling"
    - path: "services/slow_lane.py"
      provides: "_persist_event_ref echt + benotend (In-Place-UPDATE handling_score_numeric + handling_status-Statemachine) + _requeue_pending (H-3 Bootstrap am Consumer-Start + gedrosselt im _periodic_tick)"
      contains: "_persist_event_ref"
    - path: "alembic/versions/XXXX_abstain_log.py"
      provides: "abstain_log-Tabelle (D-07 Rider 3: nachfolgender Satz + interaction_id) + harter event_id-FK CASCADE (F-08)"
  key_links:
    - from: "services/slow_lane.py _persist_event_ref"
      to: "intent_event.handling_score_numeric + handling_status"
      via: "In-Place-UPDATE post-call (race-frei, §8.7-enge-Abweichung) + Statemachine pending→scored/abstained/failed"
      pattern: "handling_status"
    - from: "_persist_event_ref"
      to: "abstain_log"
      via: "Goodhart-Logging bei Abstention (D-07 Rider 3)"
      pattern: "abstain"
    - from: "calls (ON DELETE) → intent_event.call_id → abstain_log.event_id"
      to: "DSGVO-Orphan-Cleanup (0 Rest-Zeilen in BEIDEN Kindern)"
      via: "durchgehende ON DELETE CASCADE-Kette — abstain_log→intent_event baut DIESER Plan (F-08), intent_event→calls ist TAXO1-Vertrag (VERIFIZIEREN, nicht annehmen — REVIEWS)"
      pattern: "ON DELETE CASCADE"
    - from: "intent_event.handling_status"
      to: "TAXO1 intent_event-Vertrag"
      via: "ADDITIVE Spalte (CONTEXT erlaubt additive Felder mit Begründung) — INTERLOCK mit TAXO1-Plan-01"
      pattern: "handling_status"
---

<objective>
Mache den Slow-Lane-Consumer aus TAXO1 echt + benotend: er liest offene intent_event-Zeilen (`WHERE handling_status='pending'`), prüft Tor-1-Konfidenz, wendet regel-/marker-basierte Anker-Regeln auf die nächste Berater-Aussage an (Label/Frage vs Gegenargument) und schreibt `handling_score_numeric` 1-3 IN-PLACE — mit großzügiger Abstention (unsicher → kein Score). LLM-Verhaltens-Urteil läuft NUR hier (async, post-call), NIE auf Fast/Medium Lane (Bau-Regel 1, Zirkelschluss-Schutz). Jede Abstention wird mit dem nachfolgenden Satz + interaction_id geloggt (D-07 Goodhart-Schutz).

**Cross-AI-Fold — WURZEL-FIX (REVIEWS §0, klärt F-01/F-05/F-06):** Die aus TAXO1 geerbte Arbeitslisten-Invariante `WHERE handling_score_numeric IS NULL` ist semantisch DREIFACH überladen (noch-nicht-verarbeitet vs. bewusst abgewinkt vs. fehlgeschlagen). Das verursacht einen Deadlock: eine Abstention (NULL) lässt den Call-Ende-Merge NIE feuern (D-07 will Abstention HÄUFIG) → Call hängt ewig "pending". Fix: eine ADDITIVE Spalte `intent_event.handling_status` (varchar: `pending`|`scored`|`abstained`|`failed`). Arbeitsliste wird `WHERE handling_status='pending'`. Merge-Trigger (Plan 04) zählt nur `pending` als offen — `abstained`/`failed` sind ABGESCHLOSSEN.

**Schema-Hoheit (INTERLOCK I-1 RESOLVED → Option b, TAXO1↔TAXO2-Interlock 2026-06-11):** `handling_status` gehört zum `intent_event`-Schema = TAXO1-Vertrag. **Auflösung: Option (b) — TAXO1-01 baut die Spalte** (String(16) NOT NULL server_default='pending', indiziert, ab Tag 1). Begründung gegen die frühere Option a: TAXO1-01 baut ohnehin das vollständige Schema ab Tag 1 ("später ergänzen = Abrieb") und die Tabelle startet LEER → Option b braucht KEIN Backfill auf befüllter Tabelle; Option a hätte TAXO2 eine TAXO1-Tabelle umbauen lassen = genau der Abrieb. Gemini-Interlock bestätigt. **Folge für Plan 03:** Task 0b baut KEINE Migration — sie VERIFIZIERT nur dass TAXO1-01 die Spalte gebaut hat + gleicht das ORM ab. Die _persist_event_ref-Statemachine (Task 3) ist unverändert. F-02-Queue-Timing (Plan 04) interlockt zusätzlich mit TAXO1-Plan-02 (emit live → Merge am Call-Ende — bestätigt konsistent).

Purpose: Liefert den pro-Einwand-Behandlungs-Wert, den die Engine (Plan 02) konsumiert und den objection_events-Leser (Plan 05) als Quelle brauchen. MUSS vor Plan 05 laufen (Reihenfolge-Zwang Req 7).
Output: handling_status-VERIFIKATION (Spalte von TAXO1-01, I-1 Option b — keine TAXO2-Migration) + handling_markers.py (reine Regeln) + echter Slow-Lane-Consumer mit Statemachine + abstain_log-Tabelle (harter FK) + Marker-Test.

KEIN rubric_score/coaching_score-Write (Plan 04). Das In-Place-Update ist die bewusste ENGE §8.7-Abweichung (Begründung unten) — Pflicht-Gate: ORM-aware grep-Beweis Race-Freiheit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** Marker-Test deterministisch (Prod-Pytest-Gate). Diese Welle IST riskant (In-Place-Write, race-sensitiv, additive Schema-Änderung auf TAXO1-Tabelle) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht (RESEARCH §I): Test-Call → `inspect.sh sample intent_event` zeigt befüllte 1-3/NULL + handling_status scored/abstained; Slow-Lane-Restart re-derived Arbeitsliste. KEIN lokales pytest als Acceptance.

**HART Pflicht-Gate (Cross-AI-Interlock, SPEC Constraint + F-04):** VOR dem In-Place-Write der ORM-AWARE grep-Beweis dass KEINE Live-Bahn (Fast/Medium) intent_event-Zeilen ZURÜCKLIEST. Wird ein Live-Leser gefunden → Fallback auf separates Pro-Event-Score-Objekt (Task 0). Abweichung in Nerve-Vault/05 Log.

**LLM-Modell:** Falls LLM-Urteil im Marker genutzt wird → Sonnet/Haiku NUR async Slow Lane, NIE Live-Loop (CLAUDE.md Haiku-only-Live). v1 ist regel-/marker-basiert (Gerüst §4) — LLM-Urteil ist Erweiterung, async.

**Migrations-Revision (HART, DEPENDS-ON-TAXO1):** beide neuen Migrationen (handling_status + abstain_log) folgen der TAXO1-Kette. down_revision = beim Execute via `inspect.sh migrations` lesen, NICHT hardcoden. handling_status-Migration läuft VOR/zusammen mit dem _persist_event_ref-Umbau.

**UTF-8:** Marker-Strings (Deutsch) mit Umlauten OK; DB-Spalten/Status-Werte ASCII (handling_status, pending, scored, abstained, failed).
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- Slow-Lane-Vertrag (TAXO1 Plan 02, DEPENDS-ON-TAXO1) — exakt zum Andocken. -->

services/slow_lane.py (nach TAXO1):
- SENTINEL = object()
- class SlowLaneQueue: put(event_ref) / drain()->list / get(timeout=None)  (event_ref = {'event_id': eid})
- slow_lane = SlowLaneQueue()  (Modul-Singleton)
- _persist_event_ref(event_ref, db) -> None   # No-Op in TAXO1 (# SLOW LANE: No-Op in TAXO1) → TAXO2 macht es echt
- flush_to_db() -> int  (Shutdown: drain + persist je Item + commit)
- slow_lane_consumer() -> None  (Daemon: while True: get(); if SENTINEL: flush+break; _persist_event_ref(...))
- request_shutdown() -> None  (put(SENTINEL))
- Arbeits-Liste re-derivierbar: SELECT id FROM intent_event WHERE handling_status = 'pending'   (NEU — F-01/F-06; ersetzt WHERE handling_score_numeric IS NULL)
- SLOW_LANE_TICK = 5.0  (Modul-Konstante, TAXO1-02 §0.1) + def _periodic_tick() -> None  (No-Op in TAXO1 — der periodische Haken, an den TAXO2 H-3 (Re-Queue-Bootstrap) hängt. Wird bei jedem queue.Empty-Timeout im slow_lane_consumer-Loop gerufen.)
- Consumer-Loop ist timeout-getaktet: item = slow_lane.get(timeout=SLOW_LANE_TICK); except queue.Empty: _periodic_tick(); continue  (TAXO1-02-Struktur — H-3 dockt am _periodic_tick an, NICHT am blockierenden get())

intent_event (TAXO1 Plan 01 + TAXO2-additive Spalte):
- handling_score_numeric (SmallInt, NULL, indiziert) ← TAXO2 schreibt 1-3 oder lässt NULL (Abstention/failed)
- handling_status (varchar(16), von TAXO1-01 gebaut — I-1 Option b, default 'pending', indiziert) ← TAXO2-Statemachine pending→scored/abstained/failed (Wurzel-Fix F-01/F-05/F-06)
- intent_type, confidence (Tor-1-Filter), interaction_id (UUID, Moment-Klammer für Goodhart-Logging)
- payload_jsonb: speaker_role (berater/kunde) → "nächste Berater-Aussage" finden
- **payload_jsonb.triggering_text (FOLD B Punkt 1, neu seit Plan-Lock — Welle 7/TAXO1-07):** anonymisierter Auslöser-Wortlaut des Einwands (`intent_event_writer.py:101`, IMMER gesetzt auch None; Aufrufer anonymisieren via `anonymize_output` VOR dem Emit). **Optional nutzbar in grade_handling als sauberer Auslöser-Bezug** statt den Einwand-Text aus dem Transkript-Fenster zu REKONSTRUIEREN → verbessert die Marker-Genauigkeit (die "nächste Berater-Aussage" wird gegen den bekannten Auslöser geprüft, nicht gegen einen rekonstruierten). KEIN Pflicht-Umbau — additiver Genauigkeits-Hebel.
- call_id (FK auf calls) ← DSGVO-Cascade-Naht: MUSS `REFERENCES calls(id) ON DELETE CASCADE` sein (TAXO1-Vertrag, am Execute VERIFIZIEREN — siehe Task 0; bei LOSE FK → INTERLOCK-Flag, F-08)

Bau-Regel 1 (TAXO1-SPEC Req 3 Acceptance): "intent_event nach Erzeugung read-only für Live-Bahnen;
kein UPDATE auf intent_event aus Fast/Medium Lane". → garantiert Race-Freiheit per Design;
Plan re-verifiziert per ORM-AWARE grep gegen den dann-lebenden TAXO1-Cutover-Code (Plan 04).

Statemachine handling_status (Wurzel-Fix):
- 'pending'   = noch nicht verarbeitet (= Arbeitsliste; ersetzt "IS NULL")
- 'scored'    = handling_score_numeric (1-3) gesetzt
- 'abstained' = bewusst abgewinkt (D-07; handling_score_numeric bleibt NULL, abstain_log geschrieben) — ABGESCHLOSSEN
- 'failed'    = dauerhaft nicht bewertbar (Poison-Pill F-05; handling_score_numeric NULL) — ABGESCHLOSSEN
Merge-Trigger (Plan 04) zählt NUR 'pending' als offen.
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `intent_event` (handling_status-Spalte von TAXO1-01 / I-1 Option b; TAXO2 macht NUR UPDATE handling_score_numeric/handling_status IN-PLACE, KEINE Schema-Änderung) — DEPENDS-ON-TAXO1
- `abstain_log` (CREATE, neu, harter FK CASCADE) — Goodhart-Logging D-07 + DSGVO F-08
### inspect.sh-Beleg
`inspect.sh schema intent_event` (beim Execute, nach TAXO1-Deploy): bestätigt handling_score_numeric SmallInt NULL indiziert + interaction_id UUID + **`handling_status varchar(16) NOT NULL default 'pending'` indiziert (von TAXO1-01, I-1 Option b — TAXO2 baut sie NICHT)**. **Zusätzlich F-08 (I-2 RESOLVED):** dieselbe Ausgabe belegt dass `call_id` als `REFERENCES calls(id) ON DELETE CASCADE` gebaut ist (von TAXO1-01, I-2-Fold — harter Cascade-FK; wenn FEHLT → Interlock-Bruch, STOP). `inspect.sh schema abstain_log` → "Did not find" (neu).
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| handling_status | _persist_event_ref Statemachine + Arbeitsliste | DB intent_event.handling_status (varchar(16), default 'pending', indiziert) — von TAXO1-01 (I-1 Option b) | ⬜ Task 0b Verifikation + Task 3 |
| handling_score_numeric | _persist_event_ref UPDATE in-place | DB intent_event.handling_score_numeric (SmallInt NULL) | ⬜ Task 3 Test-Anruf-Sample |
| Arbeits-Liste | SELECT WHERE handling_status='pending' | DB-Query (kein file-spill) — ersetzt IS NULL (F-01/F-06) | ⬜ Task 3 Restart-Test |
| abstain (Satz+interaction_id) | _persist_event_ref schreibt bei Abstention | DB abstain_log (harter event_id-FK CASCADE, F-08) | ⬜ Task 2 |
| intent_event.call_id (Cascade-Naht) | TAXO1-FK auf calls(id); F-08-Kette calls→intent_event→abstain_log | DB intent_event.call_id — MUSS ON DELETE CASCADE (TAXO1-Vertrag) | ⬜ Task 0 (inspect.sh-Beleg; bei LOSE → INTERLOCK-Flag) |
| "nächste Berater-Aussage" | aus intent_event-Strom (payload_jsonb.speaker_role) | DB intent_event payload_jsonb | ⬜ Task 1 |

## Verbindungs-Karte (Punkt 22)
- **handling_status-Spalte (I-1 RESOLVED → Option b, von TAXO1-01):** intent_event ist TAXO1-Vertrag → die Spalte wird in TAXO1-01 gebaut (String(16) NOT NULL server_default='pending', indiziert, leer ab Tag 1 → KEIN Backfill). TAXO2 verifiziert nur (Task 0b) + schreibt die Statemachine-Werte (Task 3). KEINE TAXO2-Migration, KEIN Backfill (Tabelle war leer).
- **§8.7-In-Place-Race-Beweis (RESEARCH §B, DEPENDS-ON-TAXO1):** Bau-Regel 1 garantiert per Design dass Live-Bahnen intent_event nicht zurücklesen. Pflicht-Gate Task 0 (F-04 ORM-aware): erweiterter grep auf ORM-Lese-Muster gegen den dann-lebenden Code → bestätigen Fast/Medium-Lane ruft nur `emit_intent_event` + KEINE Rück-Lese-Query bestehender Zeilen. Falls Live-Leser → Fallback separates Score-Objekt.
- **abstain_log neu (F-08):** `grep -rn "abstain_log\|AbstainLog" services/ routes/` == 0 vor diesem Plan. event_id = harter FK auf intent_event(id) ON DELETE CASCADE.
- **DSGVO-Cascade-KETTE (F-08, REVIEWS — kritisch):** der Orphan-Cleanup ist eine 3-Glieder-Kette `calls → intent_event → abstain_log`. DIESER Plan baut nur das letzte Glied (`abstain_log.event_id REFERENCES intent_event(id) ON DELETE CASCADE`). Das mittlere Glied (`intent_event.call_id REFERENCES calls(id) ON DELETE CASCADE`) ist ein TAXO1-Vertrag und in RESEARCH §A4 NICHT enumeriert. **Wenn TAXO1s `intent_event.call_id` eine LOSE FK ist (kein ON DELETE CASCADE), bricht die Kette:** Call-Löschung cascaded nicht zu intent_event → intent_event-Zeilen überleben → abstain_log-Zeilen überleben (ihre Eltern-intent_event-Zeile lebt ja) → der F-08-DSGVO-Orphan (Wortlaut-Rest) kommt durch die Hintertür zurück. Task 0 verifiziert die Naht per `inspect.sh schema intent_event`. **INTERLOCK-Auflösung bei LOSE FK:** entweder (i) TAXO1-Plan-01 ergänzt `ON DELETE CASCADE` auf `intent_event.call_id` (additiv) ODER (ii) der Anonymizer-/Lösch-Pfad (services/anonymization.py / Call-Delete) räumt `intent_event` für die call_id explizit mit (dann cascaden die abstain_log-Zeilen sauber). Flag für den TAXO1/2/3-Interlock — Eintrag in der residual-interlock-/05-Log-Note (siehe Task 0).

## 1. Lokaler Kontext (Punkt 14 — _persist_event_ref-Insert)
`_persist_event_ref(event_ref, db)` ist in TAXO1 ein No-Op (Marker `# SLOW LANE: No-Op in TAXO1`). TAXO2 ersetzt den Body. Kein early-return-Problem (eigene Funktion). Aufgerufen aus `slow_lane_consumer`-Loop (eigene DB-Session) + `flush_to_db` (drain-Session).

## 2. Funktions-Skelett
`_persist_event_ref(event_ref, db)`: hole intent_event per event_id → wenn `handling_status != 'pending'` return (idempotent, F-01: NICHT mehr "handling_score_numeric not None" — sonst würde abstained/failed neu verarbeitet) → Tor-1-Konfidenz prüfen → "nächste Berater-Aussage" finden → grade_handling() in try/except:
- Erfolg + score 1-3 → handling_score_numeric=score, handling_status='scored'
- Erfolg + None (Abstention, Tor 2 D-07) → handling_score_numeric NULL, handling_status='abstained', abstain_log-INSERT
- Exception/dauerhaft nicht bewertbar (F-05 Poison-Pill) → handling_status='failed' (handling_score_numeric NULL), optional retry_count++ → blockiert den Merge NICHT
→ commit (vom Consumer/flush gesteuert).

## 3. Cross-File-Awareness
`grep -rnE "query\(IntentEvent|select\(IntentEvent|IntentEvent\.query|FROM intent_event WHERE|handling_status|_persist_event_ref|slow_lane" services/ routes/`: slow_lane.py (Consumer+flush+Statemachine), emit_intent_event/Plan 04 (Producer put), Engine Plan 02 (liest handling_score_numeric/handling_status als Eingang). KEIN Fast/Medium-Lane-Treffer der schreibt (Task-0-Gate beweist auch: keiner der liest, ORM-aware).

## 4. Edge-Case-Analyse + Race-Condition-Audit (Punkt 14)
1. **Gleichzeitiges Feuern:** Mehrere Consumer? TAXO1 = 1 Consumer-Thread → keine parallele Schreibung auf dieselbe Zeile. Fast/Medium schreiben handling_score_numeric/handling_status NIE (Bau-Regel 1). → keine Race auf der Spalte. (F-06 Multi-Worker: erst Block M, dann FOR UPDATE SKIP LOCKED — jetzt nur vorbereitet.)
2. **Verbindungsabbruch mid-Operation:** Consumer-Crash vor commit → Zeile bleibt handling_status='pending' → bei Restart neu in Arbeitsliste (re-derivierbar via 'pending'). Kein halb-fertiger Zustand persistiert.
3. **Wiederverbinden mit altem Zustand:** Idempotenz — _persist_event_ref skippt Zeilen `WHERE handling_status != 'pending'` (F-01: scored/abstained/failed werden NICHT neu verarbeitet → KEIN doppelter abstain_log bei Consumer-Restart).
4. **Tab im Hintergrund:** n/a (Server-seitiger Daemon, kein Browser-Timer).
5. **Race-Beweis-Gate (Task 0, F-04 ORM-aware):** kein Live-Leser von intent_event → das In-Place-Update kann mit keiner Live-Lese-Query kollidieren.
6. **Poison-Pill (F-05):** dauerhaft scheiternde Einzelbewertung → 'failed', blockiert Merge nicht (Plan 04 zählt 'failed' nicht als offen). Call wird mit Marker "N Events nicht bewertbar" trotzdem benotet (D-09-not_gradable-Philosophie konsistent).
7. **DSGVO-Cascade-Bruch (F-08, REVIEWS):** wenn TAXO1s `intent_event.call_id` LOSE FK ist → Call-Löschung räumt intent_event NICHT → abstain_log-Wortlaut-Zeilen überleben → DSGVO-Orphan zurück. Mitigation: Task 0 verifiziert die Naht (inspect.sh); bei LOSE → INTERLOCK-Flag (TAXO1 ergänzt CASCADE additiv ODER Anonymizer räumt intent_event mit). Test (Task 3): End-to-End-Lösch-Assertion über BEIDE Tabellen.

<tasks>

<task type="auto">
  <name>Task 0: Race-Freiheits-Gate (ORM-AWARE, F-04) + handling_status-INTERLOCK-Note + F-08-Cascade-Ketten-Verifikation (HART, Cross-AI-Interlock)</name>
  <read_first>
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §B (Race-Beweis-Argument)
    - .planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-04-live-cutover-taxonomie-k4-PLAN.md (emit_intent_event, dann-lebender Code)
    - .planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-01-*PLAN.md (intent_event-Vertrag — handling_status-Interlock-Abgleich + call_id-FK-Klausel)
    - **F-08-Cascade-Kette (DEPENDS-ON-TAXO1, REVIEWS):** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema intent_event'` (beim Execute, gegen das dann-lebende TAXO1-Schema) — explizit bestätigen ob `call_id` als `REFERENCES calls(id) ON DELETE CASCADE` gebaut ist (NICHT als LOSE FK ohne ON DELETE). Die DSGVO-Orphan-Cleanup-Kette ist `calls → intent_event → abstain_log`; RESEARCH §A4 enumeriert NUR `transcript_segments → conversation_logs`, NICHT diese intent_event→calls-Naht → sie ist ein TAXO1-Vertrag, der hier VERIFIZIERT (nicht angenommen) werden muss. Markiere DEPENDS-ON-TAXO1: am 3-Wege-TAXO1/2/3-Interlock-Check bzw. am Execute gegen die dann-live TAXO1-Schema prüfen. Bei LOSE FK → INTERLOCK-Flag (siehe action).
    - services/einwand_keyword_matcher.py + services/claude_service.py (analyse_loop) — Fast/Medium Lane
  </read_first>
  <action>
    **F-04 — ORM-AWARE Race-Gate (nicht nur rohes SQL):** Das Projekt nutzt SQLAlchemy 2.x → Live-Reads laufen als `db.query(IntentEvent).filter(...)` / `select(IntentEvent)...`, die ein rohes `FROM intent_event WHERE`-Grep NICHT findet (Scheinsicherheit). Erweitere das Gate:
    `grep -rnE "query\(IntentEvent|select\(IntentEvent|IntentEvent\.query|FROM intent_event WHERE" services/ routes/`
    Jeden Treffer klassifizieren: Schreiber (emit_intent_event / INSERT), Lese-Query gegen BESTEHENDE Zeilen (query/select gegen schon-persistierte intent_event), oder Definition. Bestätigen: Fast/Medium-Lane-Code (einwand_keyword_matcher.py, claude_service.py analyse_loop) ruft NUR emit_intent_event auf, KEINE Rück-Lese-Query bestehender Zeilen. Den breiten Hand-Task (alle intent_event-Treffer klassifizieren) zusätzlich behalten.
    **Wenn KEIN Live-Leser:** In-Place-Strategie ist race-frei → weiter mit Task 0b-3. Eintrag in Nerve-Vault/05 Log (bewusste ENGE §8.7-Abweichung: pro-CALL-Note = rubric_score = das von §8.7 verlangte separate Objekt; NUR pro-EVENT-Note in-place; race-frei per Bau-Regel 1).
    **Wenn DOCH ein Live-Leser:** STOP In-Place. Fallback: separates Pro-Event-Score-Objekt (eigene Tabelle event_score(event_id, score, status)). Plan in der SUMMARY dokumentieren + Andre informieren.
    **handling_status-INTERLOCK-Note (I-1 RESOLVED → Option b):** `intent_event.handling_status` ist eine TAXO1↔TAXO2-Naht — der TAXO1↔TAXO2-Interlock (2026-06-11) hat sie auf **Option b** aufgelöst: **TAXO1-01 baut die Spalte**, TAXO2 verifiziert + nutzt sie nur (Task 0b). KEINE TAXO2-Migration. _persist_event_ref unverändert. Beim Execute bestätigen dass TAXO1 sie gebaut hat (sonst Interlock-Bruch, STOP). F-02-Queue-Timing (Plan 04) interlockt zusätzlich mit TAXO1-Plan-02 (bestätigt konsistent: emit live → Merge am Call-Ende).
    **F-08-Cascade-Ketten-Note (INTERLOCK I-2 RESOLVED):** Die DSGVO-Orphan-Cleanup-Kette ist `calls → intent_event → abstain_log`. DIESER Plan baut das letzte Glied (`abstain_log.event_id REFERENCES intent_event(id) ON DELETE CASCADE`, Task 2). Das mittlere Glied (`intent_event.call_id REFERENCES calls(id) ON DELETE CASCADE`) wird durch den **INTERLOCK-I-2-Fold in TAXO1-01 gebaut** (die alte A2-"lose FK"-Entscheidung wurde revidiert — Gemini-Interlock bestätigt "DSGVO-kritisch"). **Verifizieren (NICHT mehr offen flaggen):** `inspect.sh schema intent_event` muss `call_id ... REFERENCES calls(id) ON DELETE CASCADE` zeigen. **Wenn die CASCADE-FK FEHLT** (TAXO1 ohne I-2-Fold deployed) → STOP + Andre-Info (Interlock-Bruch): die Kette ist gebrochen, Call-Löschung würde den abstain_log-Wortlaut als DSGVO-Waise hinterlassen. NICHT eigenmächtig in TAXO2 nachbauen — TAXO1-01 ist die Stelle. Befund in SUMMARY + residual-interlock-Note.
    **Emit-INSERT-only-Check (Gemini-Interlock-Verschärfung):** zusätzlich zum Live-Leser-Gate bestätigen dass `emit_intent_event` (TAXO1) die intent_event-Zeile als ATOMARES INSERT schreibt (kein nachträgliches Live-UPDATE/payload-Append auf dieselbe Zeile). Sonst könnte ein Live-UPDATE mit dem async TAXO2-In-Place-UPDATE (handling_score_numeric/handling_status) konkurrieren. Bau-Regel 1 fordert das bereits ("intent_event read-only für Live-Bahnen") — hier per grep gegen den dann-live emit-Code bestätigen: `grep -A30 "def emit_intent_event" services/*.py` zeigt INSERT/add, KEIN UPDATE/setattr auf eine bereits geschriebene intent_event-Zeile.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "query(IntentEvent\|select(IntentEvent\|IntentEvent.query\|FROM intent_event WHERE"'</automated>
  </verify>
  <acceptance_criteria>
    - grep-Ergebnis dokumentiert: jeder intent_event-Treffer (ORM query/select + rohes SQL) in services/routes klassifiziert (Schreiber/Leser/Def)
    - Fast/Medium-Lane liest KEINE bestehenden intent_event-Zeilen zurück (race-frei) — ODER Fallback auf separates Score-Objekt dokumentiert
    - Nerve-Vault/05 Log trägt den §8.7-Abweichungs-Eintrag UND die handling_status-INTERLOCK-Note (TAXO1↔TAXO2-Naht, Option a + Fallback b)
    - **F-08-Cascade-Naht belegt:** `inspect.sh schema intent_event` zeigt ob `call_id` ON DELETE CASCADE ist; Ergebnis in SUMMARY + residual-interlock-/05-Log-Note. Bei LOSE FK → INTERLOCK-Flag mit Auflösungs-Option (i) TAXO1-CASCADE additiv ODER (ii) Anonymizer räumt intent_event mit (REVIEWS)
    - SUMMARY enthält den 3-Wege-Interlock-Hinweis (handling_status-Vertrag + F-02-Queue-Timing + F-08-Cascade-Kette intent_event.call_id)
  </acceptance_criteria>
  <done>Race-Freiheit ORM-aware bewiesen (oder Fallback); §8.7-Abweichung + handling_status-Interlock + F-08-Cascade-Ketten-Naht (intent_event.call_id) dokumentiert/geflaggt.</done>
</task>

<task type="auto">
  <name>Task 0b: handling_status-Spalte VERIFIZIEREN (von TAXO1-01 gebaut, INTERLOCK I-1 Option b) + ORM-Abgleich — KEINE TAXO2-Migration</name>
  <files>database/models.py</files>
  <read_first>
    - .planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-01-*PLAN.md (intent_event-Schema — handling_status ist DORT gebaut: String(16) NOT NULL server_default='pending', indiziert, INTERLOCK I-1)
    - ssh ... 'bash scripts/inspect.sh schema intent_event' (beim Execute, gegen das dann-live TAXO1-Schema — bestätigt dass handling_status EXISTIERT + ix_intent_event_handling_status)
  </read_first>
  <action>
    **INTERLOCK I-1 RESOLVED → Option b:** Die Spalte `handling_status` wird von **TAXO1-01** gebaut (Migration 0016), NICHT von TAXO2. Grund: TAXO1 baut ohnehin das vollständige intent_event-Schema ab Tag 1 ("später ergänzen = Abrieb"), die Tabelle startet LEER → kein Backfill nötig (server_default='pending' deckt alle ab Welle-4-Inserts). TAXO2 eine TAXO1-Tabelle nachträglich umzubauen wäre genau der Abrieb, den TAXO1 vermeidet (Gemini-Interlock bestätigt).
    **Diese Task baut KEINE Migration.** Stattdessen:
    1. **Verifizieren (Execute-Zeit):** `inspect.sh schema intent_event` zeigt `handling_status varchar(16) NOT NULL default 'pending'` + Index `ix_intent_event_handling_status` (von TAXO1-01 gebaut). Wenn die Spalte FEHLT → STOP: TAXO1 wurde ohne den I-1-Fold deployed → Interlock-Bruch, Andre informieren (NICHT eigenmächtig eine TAXO2-Migration nachschieben — das wäre der vermiedene Abrieb).
    2. **ORM-Abgleich:** sicherstellen dass `class IntentEvent` in models.py die Spalte kennt (TAXO1-01 hat sie ins ORM gesetzt — beim Execute gegen den dann-live Code prüfen, ggf. nur Doku/Kommentar abgleichen). KEINE Duplikat-Definition.
    Kein 05-Log-Eintrag für eine TAXO2-Migration nötig (die additive-Feld-Begründung steht im TAXO1-01-Fold + Interlock-Record).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema intent_event | grep -i handling_status'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh schema intent_event` zeigt `handling_status` varchar(16) NOT NULL default 'pending' + Index ix_intent_event_handling_status (von TAXO1-01, I-1)
    - `grep "handling_status" database/models.py` >= 1 (ORM kennt die Spalte — TAXO1-01-Definition, KEINE TAXO2-Duplikat-Spalte)
    - KEINE TAXO2-Migrationsdatei für handling_status existiert (`ls alembic/versions/*handling_status*` leer — Option b)
    - Wenn handling_status FEHLT auf Prod → Task schlägt fehl + Andre-Info (Interlock-Bruch, NICHT eigenmächtig nachziehen)
  </acceptance_criteria>
  <done>handling_status (von TAXO1-01) verifiziert + ORM abgeglichen; KEINE TAXO2-Migration (Option b, Anti-Abrieb).</done>
</task>

<task type="auto">
  <name>Task 1: Anker-Marker-Regeln grade_handling (services/handling_markers.py, D-03 Tor2 + D-07)</name>
  <files>services/handling_markers.py</files>
  <read_first>
    - Nerve-Vault/04 Entscheidungen/NERVE TAXO-Gerüst (verriegelt).md §4 (handling_score 1-3, Label/Frage vs Gegenargument, großzügige Abstention)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md D-07 (großzügige Abstention)
  </read_first>
  <action>
    `def grade_handling(objection_event, next_advisor_utterance, triggering_text=None) -> int|None` (reine Funktion):
    **FOLD B Punkt 1 (optional, additiv):** `triggering_text` (aus `intent_event.payload_jsonb['triggering_text']`, Welle 7) als sauberer Auslöser-Bezug durchreichen — der Marker prüft die nächste Berater-Aussage gegen den BEKANNTEN Auslöser-Wortlaut statt ihn aus dem Transkript-Fenster zu rekonstruieren (genauere Label/Spiegeln-Erkennung). Default None → Verhalten wie bisher (rein auf next_advisor_utterance). KEIN Pflicht-Umbau; wenn genutzt, in SUMMARY als Genauigkeits-Hebel notieren.
    Prüft die nächste Berater-Aussage nach einem Einwand auf Anker-Verhalten:
    - GUT (Stufe 3): Label/Spiegeln/Isolieren — Marker wie "verstehe ich richtig", "wenn ich Sie richtig verstehe", Rückfrage (Fragewort), "abgesehen davon", Benennen der Emotion.
    - MITTEL (Stufe 2): teils angenommen, teils Gegenargument.
    - SCHLECHT (Stufe 1): sofortiges Gegenargument/Wegargumentieren ohne Anerkennung ("aber", "doch", "trotzdem" als Eröffnung + sofortiger Pitch).
    - ABSTENTION (None, D-07 großzügig): kurz/generisch/unklar → KEINE Note. Default: im Zweifel KEINE Note. Eine falsche Note zerstört Trust; fehlende Note ist verschmerzbar.
    Konkrete Marker-Listen als Konstanten (ASCII-Keys, deutsche Marker-Strings mit Umlauten OK). Wortzahl-Schwelle für "zu kurz" → Abstention.
    KEIN LLM in v1 (regel-/marker; LLM-Urteil ist spätere async-Erweiterung).
    Modul-Docstring: Gerüst §4 + D-07-Großzügigkeits-Richtung.
    **Hinweis (F-05):** grade_handling gibt int|None zurück; die Poison-Pill-Behandlung (Exception → 'failed') liegt im _persist_event_ref-try/except (Task 3), NICHT hier — grade_handling bleibt rein.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_handling_score_marker.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def grade_handling" services/handling_markers.py` == 1
    - Rückgabe ∈ {1,2,3,None}; None bei kurz/generisch/unklar (großzügige Abstention)
    - kein anthropic/claude-Import: `grep -iE "anthropic|claude" services/handling_markers.py` == 0 (v1 regel-basiert)
  </acceptance_criteria>
  <done>Marker-Regeln liefern 1-3 oder Abstention; großzügige Default-Abstention.</done>
</task>

<task type="auto">
  <name>Task 2: abstain_log-Tabelle (D-07 Rider 3 Goodhart-Logging + harter FK CASCADE F-08)</name>
  <files>database/models.py, alembic/versions/XXXX_abstain_log.py</files>
  <read_first>
    - alembic/versions/0010_add_transcript_segments.py:24 (REFERENCES ... ON DELETE CASCADE — F-08-Vorbild) + :40 (OWNER nerve_app) + 0015 (COMMENT)
    - database/models.py:738/757 (CallEvent/TranscriptSegment ondelete='CASCADE' — DD-01-Konvention)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md D-07 Rider 3
    - NERVE DSGVO Analyse (kein wörtliches Mitschneiden default) — Berater-Satz-Speicherung
  </read_first>
  <action>
    Neue Tabelle `public.abstain_log` (Goldstaub für Post-Call-LLM-Nachbewertung / Flywheel):
    Spalten: `id` PK, `event_id UUID` **harter FK auf intent_event(id) ON DELETE CASCADE (F-08)** , `interaction_id UUID NULL` (Moment-Klammer TAXO1), `next_advisor_sentence TEXT NULL`, `intent_type VARCHAR(64) NULL`, `tenant_id UUID NULL`, `created_at`.
    - ORM: `event_id = Column(UUID_TYPE, ForeignKey('intent_event.id', ondelete='CASCADE'), index=True, nullable=False)` — DD-01-Konvention (models.py:738). Migration-DDL entsprechend `REFERENCES public.intent_event(id) ON DELETE CASCADE`.
    **F-08 DSGVO (REVIEWS, KRITISCH):** `next_advisor_sentence` speichert gesprochenen Wortlaut → berührt die NERVE-DSGVO-Linie ("löscht sauber, kein Wortlaut-Rückstand"). Der harte FK CASCADE auf intent_event(id) sorgt dafür, dass beim Call-Löschen (calls → intent_event CASCADE → abstain_log CASCADE) KEINE verwaiste Wortlaut-Zeile bleibt. **Vorbedingung der Kette (siehe Task 0 / Verbindungs-Karte):** das mittlere Glied `intent_event.call_id ON DELETE CASCADE` ist TAXO1-Vertrag und MUSS für den durchgehenden Cleanup CASCADE sein — Task 0 verifiziert das per inspect.sh; bei LOSE FK greift die INTERLOCK-Auflösung (TAXO1-CASCADE additiv ODER Anonymizer räumt intent_event mit). Zusätzlich: der Satz ist die EIGENE Stimme des Beraters (nicht Kunde); falls Kunden-PII drin sein könnte → bestehende anonymize()-Pipeline nutzen (services/anonymization.py, cache=None, ephemerer Modus — Muster app_routes.py:1273). tenant_id für Mandanten-Abschottung. In `Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse` referenzieren (Cross-AI bei diesem Fund Pflicht — DSGVO).
    Schilder: Tabelle "Goodhart-/Bias-Schutz-Log (D-07 Rider 3): jede handling_score-Abstention mit nachfolgendem Berater-Satz + interaction_id. Harter FK event_id ON DELETE CASCADE (F-08, DSGVO-clean). Goldstaub fuer Post-Call-LLM-Nachbewertung (Flywheel). Status: lebt (neu, TAXO2). Schreibt services/slow_lane.py; liest Active-Learning (Post-Launch)." + Spalten-Schilder (next_advisor_sentence/event_id/interaction_id/intent_type/tenant_id).
    Owner nerve_app. ORM-Model `class AbstainLog(Base)`. down_revision = echter HEAD.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh schema abstain_log` zeigt event_id/interaction_id/next_advisor_sentence/intent_type/tenant_id; Owner nerve_app; Guard grün
    - `inspect.sh schema abstain_log` zeigt die FK-Zeile `event_id ... REFERENCES intent_event(id) ON DELETE CASCADE` (F-08 harter FK)
    - `grep "class AbstainLog" database/models.py` == 1 + `grep "ForeignKey('intent_event.id', ondelete='CASCADE')" database/models.py` deckt abstain_log.event_id ab
    - DSGVO-Referenz in Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse vorhanden
  </acceptance_criteria>
  <done>abstain_log live + beschildert + harter FK CASCADE; Berater-Satz-Speicherung DSGVO-bewusst (Cascade-cleanup).</done>
</task>

<task type="auto">
  <name>Task 3: _persist_event_ref echt + benotend (In-Place + handling_status-Statemachine) + Poison-Pill (F-05) + Multi-Worker-Vorsorge (F-06) + Marker-Tests + Test-Anruf-Beweis + F-08-End-to-End-Lösch-Assertion</name>
  <files>services/slow_lane.py, tests/test_handling_score_marker.py</files>
  <read_first>
    - services/slow_lane.py (TAXO1-No-Op-Body + Consumer/flush-Struktur)
    - services/handling_markers.py (Task 1) + services/rubric_dimensions.py (Plan 02, Tor-1-Konfidenz-Logik)
    - .planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-02-drei-bahnen-geruest-PLAN.md (Vertrag)
  </read_first>
  <action>
    Ersetze den No-Op-Body von `_persist_event_ref(event_ref, db)`:
    1. Hole intent_event per event_ref['event_id']. Wenn None → return (Loop überlebt). **Idempotenz (F-01): wenn `handling_status != 'pending'` → return** (scored/abstained/failed werden NICHT neu verarbeitet → KEIN doppelter abstain_log bei Consumer-Restart). NICHT mehr "handling_score_numeric is not None" prüfen (das würde abstained/failed neu verarbeiten).
    2. Tor-1-Konfidenz prüfen (D-03): wenn confidence < Schwelle (mode_weight_config.confidence_gate oder Default 0.70) → KEINE Benotung (Ereignis fragwürdig — getrennt von Abstention!). Setze handling_status='failed' mit reason-Marker ODER lasse 'pending'→behandle als nicht bewertbar — ENTSCHEIDUNG: Tor-1-Fail = `handling_status='failed'` (Ereignis garbage, nicht abstained; Diagnose-Trennung D-03), KEIN abstain_log (das ist Tor-1, nicht Tor-2).
    3. "Nächste Berater-Aussage" finden: aus dem intent_event-Strom desselben Calls (payload_jsonb.speaker_role == 'berater' nach diesem Event) ODER aus transcript_segments.
    4. **try/except um grade_handling (F-05 Poison-Pill):** (FOLD B P1 — `triggering_text=intent_event.payload_jsonb.get('triggering_text')` mit übergeben, sauberer Auslöser-Bezug statt Rekonstruktion; None-safe)
       - score (1-3) → `intent_event.handling_score_numeric = score; handling_status='scored'` (In-Place; race-frei per Task 0).
       - None (Abstention, Tor 2 D-07) → handling_score_numeric bleibt NULL; `handling_status='abstained'` (F-01: NICHT nur NULL lassen!); INSERT abstain_log(event_id, interaction_id, next_advisor_sentence, intent_type, tenant_id).
       - Exception / "nächste Berater-Aussage" nicht findbar / dauerhaft nicht bewertbar → `handling_status='failed'` (handling_score_numeric NULL); optional retry_count (payload_jsonb) für späteres Block-E-Retry; loggen. Der Call-Ende-Merge (Plan 04) zählt 'failed' NICHT als offen → Call kann mit Marker "N Events nicht bewertbar" trotzdem benotet werden.
    5. KEIN commit hier (Consumer/flush committen) — konsistent mit TAXO1-Vertrag.
    **F-06 Multi-Worker-Vorsorge (NICHT bauen, nur vorbereiten):** Die Arbeitsliste ist jetzt `WHERE handling_status='pending'` (statt IS NULL). Dokumentiere im Code-Kommentar + SUMMARY: "Multi-Worker-ready — Aktivierung Block M via `SELECT ... WHERE handling_status='pending' FOR UPDATE SKIP LOCKED`. JETZT 1 Consumer-Thread, KEIN SKIP LOCKED gebaut (Foundation-Register, Anti-Abrieb)." Wo die Arbeitsliste re-derived wird (Consumer-Bootstrap/Re-Queue), den Query auf `handling_status='pending'` umstellen.
    Erweitere `tests/test_handling_score_marker.py`: grade_handling-Cases (Label→3, Gegenargument→1, kurz→None) + _persist_event_ref-Tests via Mock-Session:
    - score → assert handling_score_numeric gesetzt + handling_status='scored'
    - None → assert handling_score_numeric NULL + handling_status='abstained' + abstain_log-add (F-01)
    - Exception in grade_handling → assert handling_status='failed', KEIN abstain_log, kein Crash (F-05)
    - Idempotenz: Zeile mit handling_status='abstained' → _persist_event_ref skippt (KEIN zweiter abstain_log) (F-01)
    Function-Call-Mock, KEIN getsource.
    **Test-Anruf-DATEN-FLUSS (riskante Welle):** nach Deploy 1 Live-Test-Call (Cold-Call mit ≥1 Einwand) → `inspect.sh sample intent_event` zeigt befüllte handling_score_numeric (1-3) mit handling_status='scored' bzw. handling_status='abstained' bei Abstention; simulierter Slow-Lane-Restart (Service-Restart) → Consumer re-derived offene Arbeitsliste aus `WHERE handling_status='pending'`; `inspect.sh sample abstain_log` zeigt bei Abstention die geloggte Zeile.
    **F-08-END-TO-END-Lösch-Assertion (REVIEWS, DSGVO-Cascade-Kette):** Nach dem Test-Call (der ≥1 abstained-Event + abstain_log-Zeile erzeugt hat): notiere die `call_id` des Test-Calls. Lösche den Test-Call (über den regulären Call-Delete-Pfad — NICHT manuell nur abstain_log droppen). Danach per `inspect.sh` belegen: für diese `call_id` bleiben **0 Zeilen** in `intent_event` UND **0 Zeilen** in `abstain_log` (NICHT nur abstain_log→intent_event in Isolation — die GANZE Kette calls→intent_event→abstain_log muss durchgehen). Wenn intent_event-Zeilen überleben (LOSE call_id-FK) → der F-08-DSGVO-Orphan ist NICHT geschlossen → INTERLOCK-Flag aus Task 0 greift (TAXO1-CASCADE additiv ODER Anonymizer räumt intent_event mit), Befund in SUMMARY + 05-Log.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_handling_score_marker.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A40 "def _persist_event_ref" services/slow_lane.py` zeigt: Statemachine handling_status='scored'/'abstained'/'failed' + Idempotenz-Skip `handling_status != 'pending'` + try/except (F-05) + abstain_log-INSERT bei abstained
    - Arbeitsliste umgestellt: `grep -nE "handling_status *== *'pending'|handling_status = 'pending'|WHERE handling_status" services/slow_lane.py` >= 1; `grep "handling_score_numeric IS NULL" services/slow_lane.py` == 0 (alte Invariante ersetzt, F-01/F-06)
    - F-06 Multi-Worker-Kommentar im Code + SUMMARY ("FOR UPDATE SKIP LOCKED, Aktivierung Block M") — KEIN SKIP LOCKED jetzt gebaut
    - grep: KEIN handling_score-Write aus Fast/Medium Lane (`grep -rn "handling_score_numeric\|handling_status" services/einwand_keyword_matcher.py services/claude_service.py` == 0)
    - Test-Anruf: `inspect.sh sample intent_event` zeigt nach Test-Call befüllte handling_score_numeric (1-3) + handling_status scored/abstained — DATEN-FLUSS-Beweis
    - Slow-Lane-Restart re-derived Arbeitsliste (`WHERE handling_status='pending'`) — beim Restart-Test belegt
    - `inspect.sh sample abstain_log` zeigt bei Abstention next_advisor_sentence + interaction_id (D-07 Rider 3)
    - **F-08-END-TO-END (REVIEWS):** nach Löschen des Test-Calls belegt `inspect.sh` für die call_id **0 Zeilen in abstain_log UND 0 Zeilen in intent_event** (durchgehende Cascade-Kette calls→intent_event→abstain_log, NICHT nur abstain_log→intent_event isoliert). Bei überlebenden intent_event-Zeilen → INTERLOCK-Flag (Task 0) + Befund in 05-Log/SUMMARY
    - test: Exception→'failed' ohne Crash, kein abstain_log (F-05); abstained-Zeile wird bei Re-Run übersprungen (F-01)
  </acceptance_criteria>
  <done>Slow Lane befüllt handling_score_numeric in-place + handling_status-Statemachine (scored/abstained/failed); Abstention+Goodhart-Logging; Poison-Pill→failed (kein Deadlock); Multi-Worker-ready vorbereitet; Test-Anruf-Daten-Fluss bewiesen; F-08-Cascade-Kette end-to-end belegt (0 Rest-Zeilen in BEIDEN Kindern) oder INTERLOCK-Flag.</done>
</task>

<task type="auto">
  <name>Task 4: Bootstrap-Re-Queue der pending-Events (H-3) — am Consumer-Start + _periodic_tick-Safety-Net (dockt am TAXO1-02-Haken an, KEINE Re-Architektur)</name>
  <files>services/slow_lane.py, tests/test_handling_score_marker.py</files>
  <read_first>
    - services/slow_lane.py (TAXO1-02-Struktur: slow_lane_consumer timeout-getaktet, `_periodic_tick()` No-Op-Haken, SLOW_LANE_TICK, slow_lane.put)
    - .planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-02-drei-bahnen-geruest-PLAN.md §0.1 (periodische Timer-Komponente — H-3 hängt explizit hier)
    - database/db.py (get_session-Muster für die SELECT-Query)
  </read_first>
  <action>
    **Das Problem (H-3, TAXO-INTERLOCK-FINDINGS):** Nach Crash/Deploy/`systemctl restart` startet der Consumer mit LEERER Queue. Der Producer (emit_intent_event, Plan 04) feuert `slow_lane.put()` nur für NEUE Erkennungen — er füllt die Queue NICHT mit den vor dem Restart geschriebenen, noch nicht benoteten `handling_status='pending'`-Events nach. Ohne Re-Queue bleiben diese Events für immer pending liegen → ihr Call hängt ewig "wird ausgewertet…" (Plan-04-Merge feuert nie, weil pending_events nie 0 wird). Der „re-derivierbar"-Anspruch (TAXO1-02 §0) ist unerfüllt, solange ihn NIEMAND ausführt.

    **Der Fix — dockt am bestehenden Haken an (kein Re-Architecture):**
    1. **`def _requeue_pending(limit=...) -> int`** (neue Funktion in slow_lane.py): eigene `get_session()` → `SELECT id FROM intent_event WHERE handling_status='pending' ORDER BY timestamp LIMIT <limit>` → für jede id `slow_lane.put({'event_id': id})`. try-finally close. Loggt `[SLOW] requeue_pending: {n} re-enqueued`. **Idempotenz (KRITISCH gegen Queue-Bloat):** weil `_persist_event_ref` (Task 3) Zeilen mit `handling_status != 'pending'` skippt, ist ein doppelt einge-queuetes Event harmlos (wird einmal benotet, dann beim zweiten Mal übersprungen). Damit der periodische Safety-Net die Queue nicht unbegrenzt aufbläht: re-queue im Tick NUR wenn die Queue (nahezu) leer ist (z.B. `slow_lane.qsize()==0` falls verfügbar, sonst Tick-Zähler-Drossel alle N Ticks) — der Start-Aufruf ist immer voll.
    2. **Am Consumer-Start (Bootstrap):** `slow_lane_consumer()` ruft `_requeue_pending()` EINMAL vor dem `while True`-Loop (füllt die Queue aus der DB nach jedem Prozess-Start/Restart). Das ist der Haupt-Pfad von H-3.
    3. **Im `_periodic_tick()` (Safety-Net):** den TAXO1-02-No-Op-Body durch den gedrosselten Re-Queue ersetzen — `_requeue_pending()` (gedrosselt, s.o.). Fängt Events ab, die nach dem Bootstrap pending wurden aber deren put() verloren ging (z.B. Producer-Crash zwischen DB-Write und put, oder ein Multi-Worker-Block-M-Szenario). Marker-Kommentar ersetzen: `# H-3 Re-Queue-Bootstrap (Safety-Net): pending-Events nachfüllen, gedrosselt`.
    **Reichweite/Wechselwirkung:** M-4 (tenant_id-GUC) + H-2 (Sweep) hängen am SELBEN `_periodic_tick` — diese Plan-03-Änderung baut NUR den Re-Queue. Der Tick wird in Plan 04 um Sweep (H-2) + GUC-Setzung (M-4) erweitert; die drei dürfen sich nicht gegenseitig überschreiben (Plan 04 ERWEITERT _periodic_tick, ersetzt ihn nicht). SUMMARY-Notiz für den Interlock.
    **Test (tests/test_handling_score_marker.py erweitern):**
    - `test_requeue_pending_enqueues_only_pending`: Mock/In-Memory mit 2 'pending' + 1 'scored' + 1 'abstained' → `_requeue_pending()` legt GENAU 2 Items in die Queue (nur pending). Function-Call-/State-Assertion, kein getsource.
    - `test_consumer_bootstrap_requeues`: beweist, dass der Start-Pfad `_requeue_pending` aufruft (z.B. via monkeypatch-Sink). Kein Source-Presence.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_handling_score_marker.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "def _requeue_pending" services/slow_lane.py` == 1; enthält `handling_status` + `'pending'` + `slow_lane.put`
    - `grep -A3 "def slow_lane_consumer" services/slow_lane.py` zeigt `_requeue_pending()` VOR dem while-Loop (Bootstrap-Pfad)
    - `_periodic_tick` ruft jetzt `_requeue_pending` (gedrosselt) — `grep -A6 "def _periodic_tick" services/slow_lane.py` zeigt den Re-Queue-Aufruf statt purem No-Op; Marker-Kommentar `# H-3 Re-Queue` vorhanden
    - Idempotenz belegt: test_requeue_pending_enqueues_only_pending grün (nur 'pending' → Queue; scored/abstained NICHT)
    - **Test-Anruf-Restart-Beleg (riskante Welle):** nach einem Test-Call mit ≥1 pending Event → `systemctl restart nerve` (Deploy-Pfad) → `inspect.sh logs` zeigt `[SLOW] requeue_pending: N re-enqueued` (N>=1) UND `inspect.sh sample intent_event` zeigt die vormals-pending Events danach 'scored'/'abstained' (NICHT für immer 'pending' liegengeblieben) — das ist der H-3-DATEN-FLUSS-Beweis
    - SUMMARY-Interlock-Notiz: _periodic_tick wird in Plan 04 um H-2/M-4 erweitert (Plan 04 ergänzt, überschreibt nicht)
  </acceptance_criteria>
  <done>Bootstrap-Re-Queue gebaut (Consumer-Start + gedrosselter _periodic_tick-Safety-Net); pending-Events überleben Restart und werden benotet statt liegenzubleiben; idempotent; Plan-04-Interlock notiert.</done>
</task>

<task type="auto">
  <name>Task 5: Minimale Hook-/Registrierungs-Liste für slow_lane.py — EIN Einklink-Punkt statt Viermal-Umschreiben (FOLD 23.06., minimal)</name>
  <files>services/slow_lane.py</files>
  <read_first>
    - services/slow_lane.py (slow_lane_consumer, _periodic_tick, _persist_event_ref — der Stand nach Task 3/4)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-04-coaching-score-cutover-async-PLAN.md (Call-Ende-Merge + H-2-Sweep + M-4-GUC, hängen hier ein)
  </read_first>
  <action>
    **Problem (FOLD 23.06., André-Empfehlung):** `services/slow_lane.py` wird von Plan 03/04/06/07 NACHEINANDER angefasst — `_periodic_tick` (H-3 Plan 03, H-2-Sweep + M-4-GUC Plan 04) und der Call-Ende-Merge (compute_rubric Plan 04, Schatten-Aggregate Plan 06, Aggregat-Cutover Plan 07). Wenn jeder Plan denselben Block neu schreibt, steigt das Abrieb-/Regressions-Risiko (4× am selben Code, Merge-Konflikte, „überschreibt versehentlich den Vorgänger-Hook").
    **Fix — EINE schlanke Registrierungs-Schicht JETZT anlegen (minimal, KEIN Over-Engineering, KEINE Klassen/Plugin-Framework):** zwei Modul-Listen + zwei `register_*`-Funktionen + die zwei Iterations-Punkte. Spätere Pläne HÄNGEN sich nur ein (append einer Funktion), statt den Tick/Merge umzuschreiben.
    1. **Periodischer Tick — Hook-Liste:** `_PERIODIC_TICK_HOOKS: list = []` (Modul-Ebene). `def register_periodic_tick_hook(fn): _PERIODIC_TICK_HOOKS.append(fn)`. `_periodic_tick()` iteriert die Liste: `for hook in _PERIODIC_TICK_HOOKS: try: hook() except Exception as e: print(f"[SLOW] periodic hook {getattr(hook,'__name__','?')} failed: {type(e).__name__}")` (ein Hook-Fehler darf den Tick + die anderen Hooks NICHT killen — Verfügbarkeits-Schutz, konsistent mit H-3). **Task 4 (H-3 _requeue_pending) wird über `register_periodic_tick_hook(_requeue_pending)` registriert** statt direkt im Tick-Body zu stehen → Plan 04 registriert seinen H-2-Sweep + M-4-GUC-Schritt genauso, ohne `_periodic_tick` neu zu schreiben.
    2. **Call-Ende-Arbeit — Schritt-Liste:** `_CALL_END_MERGE_STEPS: list = []` + `def register_call_end_step(fn): _CALL_END_MERGE_STEPS.append(fn)` + ein Helfer `def run_call_end_steps(ctx) -> None:` der die Schritte der Reihe nach mit einem gemeinsamen Kontext-Objekt `ctx` (dict/leichtes Objekt: call, events, speech_stats, db, results) aufruft. **In dieser Welle ist die Liste LEER** (Plan 03 registriert hier KEINEN Merge-Schritt — der Merge-Gate + die M-4-GUC-Klammer selbst baut Plan 04). Plan 04 baut den Merge-Gate (harte Vorbedingung ended+outcome+0 pending, set/clear_current_tenant try/finally) und ruft INNERHALB der GUC-Klammer `run_call_end_steps(ctx)`; Plan 04 registriert den compute_rubric→rubric_score-Schritt, Plan 06 den Schatten-Vergleich, Plan 07 den Aggregat-Write — jeweils via `register_call_end_step(...)`, ohne den Merge-Body umzuschreiben. Reihenfolge-Hinweis im Code-Kommentar: Schritte laufen in Registrierungs-Reihenfolge; falls eine Abhängigkeit besteht (z.B. Note vor Aggregaten), in der SUMMARY der jeweiligen Welle dokumentieren.
    **ANDERE Fehler-Semantik als der Tick (FOLD 23.06., Gemini — KRITISCH):** `run_call_end_steps` swallow-t Schritt-Exceptions NICHT (KEIN per-Schritt try/except wie bei den Tick-Hooks). Ein Schritt-Fehler PROPAGIERT — **innerhalb EINES Calls ist all-or-nothing korrekt** (die rubric_score-/Aggregat-UPSERTs sind idempotent, ein Re-Run läuft sauber neu). Die PRO-CALL-Isolierung (eine fehlerhafte Call-Verarbeitung darf NICHT die Schleife für ANDERE Calls abreißen) + der gedeckelte Re-Queue liegen im **Consumer/Merge-Gate (Plan 04)**, NICHT hier. Begründung der Asymmetrie: Tick-Hooks sind unabhängige Wartungsjobs (einer darf den anderen nicht killen → swallow); Call-End-Steps sind EINE Transaktion pro Call (Teil-Erfolg = halb-geschriebene Note = schlimmer als sauberer Re-Run → propagieren).
    **Minimal-Disziplin:** nur die Listen + register-Funktionen + Iteration. KEINE Prioritäten/Gewichte/Dependency-Graphen/Conditional-Dispatch (Over-Engineering). Wenn ein späterer Plan mehr braucht, entscheidet er das dann — nicht jetzt vorbauen.
    3. **IMPORT-FALLE schließen (FOLD 23.06., Gemini — lautlose Leere-Registry):** die `register_call_end_step(...)`/`register_periodic_tick_hook(...)`-Aufrufe von Plan 04/06/07 laufen NUR, wenn deren Module beim Daemon-Start auch IMPORTIERT werden — sonst ist die Registry leer und KEIN Schritt läuft, **lautlos** (Note wird nie geschrieben, niemand merkt es). Darum: EIN zentraler, expliziter Import-Block beim Slow-Lane-Start (im Consumer-Bootstrap, VOR dem Loop — neben dem H-3-`_requeue_pending`-Aufruf) der die Hook-registrierenden Module lädt (Plan 04/06/07 ergänzen hier beim Execute ihren Import; in dieser Welle ist der Block leer/vorbereitet mit Kommentar). **Beleg-Log am Start (Pflicht):** `print(f"[SLOW] call-end-steps registriert: {len(_CALL_END_MERGE_STEPS)} | periodic-hooks: {len(_PERIODIC_TICK_HOOKS)}")` — so ist eine leere Registry sofort an `inspect.sh logs` sichtbar (statt still). Wenn die erwartete Zahl 0 ist obwohl Plan 04+ deployed → Import fehlt → BLOCK.
    **Punkt 14 (Insert in _periodic_tick):** der Tick-Body ruft jetzt die Hook-Liste statt direkt `_requeue_pending` — sicherstellen, dass der Bootstrap-Aufruf am Consumer-Start (Task 4) davon UNBERÜHRT bleibt (der läuft VOR dem Loop, nicht über die Hook-Liste). **Punkt 20 (grep):** `grep -n "_PERIODIC_TICK_HOOKS\|_CALL_END_MERGE_STEPS\|register_periodic_tick_hook\|register_call_end_step\|run_call_end_steps" services/slow_lane.py` == die Definitionen; vor diesem Plan 0 Treffer.
    Test (tests/test_handling_score_marker.py erweitern): `test_periodic_tick_runs_registered_hooks` (zwei Dummy-Hooks registriert → beide bei _periodic_tick gerufen; ein werfender Hook killt den anderen NICHT). `test_call_end_steps_registry_empty_and_callable` (run_call_end_steps mit leerer Liste = No-Op; ein registrierter Dummy-Schritt wird mit ctx gerufen).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_handling_score_marker.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "def register_periodic_tick_hook|def register_call_end_step|def run_call_end_steps" services/slow_lane.py` == 3; `_PERIODIC_TICK_HOOKS`/`_CALL_END_MERGE_STEPS` als Modul-Listen vorhanden
    - `_periodic_tick` iteriert `_PERIODIC_TICK_HOOKS` mit per-Hook try/except (ein Fehler killt Tick/andere Hooks nicht); H-3 `_requeue_pending` ist via register_periodic_tick_hook registriert (NICHT mehr hart im Tick-Body)
    - `_CALL_END_MERGE_STEPS` ist in dieser Welle LEER (Plan 03 registriert keinen Merge-Schritt); run_call_end_steps mit leerer Liste = No-Op
    - **run_call_end_steps swallow-t Schritt-Exceptions NICHT (all-or-nothing pro Call): `grep -A6 "def run_call_end_steps" services/slow_lane.py` zeigt KEIN per-Schritt try/except (anders als _periodic_tick); Test `test_call_end_step_exception_propagates` (ein werfender Schritt → run_call_end_steps wirft, swallow-t nicht)**
    - **IMPORT-FALLE (FOLD 23.06.): zentraler Hook-Import-Block im Consumer-Bootstrap (vor dem Loop) vorhanden + Start-Log `[SLOW] call-end-steps registriert: N` — `grep -n "call-end-steps registriert" services/slow_lane.py` == 1; in Plan 03 ist der Import-Block leer/vorbereitet (Kommentar), 04/06/07 ergänzen ihren Import**
    - KEINE Klassen/Prioritäten/Dependency-Graphen (Minimal-Beleg: nur Listen + 3 Funktionen + 2 Iterationen)
    - test_periodic_tick_runs_registered_hooks + test_call_end_steps_registry_empty_and_callable + test_call_end_step_exception_propagates grün
    - SUMMARY-Notiz: Plan 04 baut den Merge-Gate + GUC-Klammer und ruft run_call_end_steps darin; 04/06/07 registrieren ihre Schritte via register_call_end_step (kein Umschreiben von Tick/Merge)
  </acceptance_criteria>
  <done>Schlanke Hook-/Registrierungs-Schicht (2 Listen + 3 Funktionen + 2 Iterationen); H-3 als erster Tick-Hook registriert; Call-End-Step-Liste leer & bereit; 04/06/07 hängen sich nur ein.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Slow-Lane-Consumer → intent_event (In-Place-UPDATE + Statemachine) | einziger Schreiber von handling_score_numeric/handling_status |
| abstain_log (Berater-Saetze) | DSGVO — gespeicherte Aussagen |
| Call-Löschung → intent_event → abstain_log | Wortlaut-Reste müssen cascaden (durchgehende Kette; intent_event.call_id-Naht = TAXO1-Vertrag, verifizieren) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-03-01 | T (Race/Tampering) | In-Place-UPDATE kollidiert mit Live-Lese | mitigate | Task-0-ORM-AWARE-grep-Gate (F-04): kein Live-Leser von intent_event (Bau-Regel 1); 1 Consumer-Thread; idempotenter Skip `handling_status != 'pending'`. Fallback separates Objekt falls Leser gefunden. |
| T-TAXO2-03-02 | D (Deadlock/Hänger) | Abstention/Fehler als NULL → Call-Ende-Merge feuert nie → Call hängt ewig | mitigate | **Wurzel-Fix (F-01/F-05): handling_status='abstained'/'failed' sind ABGESCHLOSSEN; Merge zählt nur 'pending' als offen. D-07 großzügige Abstention (häufig) hängt keinen Call mehr.** |
| T-TAXO2-03-03 | R (Repudiation/Bias) | falsche Abstentions verzerren Score (Goodhart) | mitigate | D-07 Rider 3: jede Abstention mit Satz+interaction_id geloggt → Post-Call-Review fängt biased Abstentions; großzügige Default-Abstention. |
| T-TAXO2-03-04 | I (Info Disclosure / DSGVO) | abstain_log speichert Gespräch-Saetze (Wortlaut); Call-Löschung räumt sie evtl. NICHT (Cascade-Kette bricht bei loser intent_event.call_id-FK) | mitigate | **F-08: harter FK abstain_log.event_id ON DELETE CASCADE** (Task 2, baut DIESER Plan). **Mittleres Kettenglied intent_event.call_id ON DELETE CASCADE = TAXO1-Vertrag → Task 0 verifiziert per inspect.sh; bei LOSE FK INTERLOCK-Flag (TAXO1-CASCADE additiv ODER Anonymizer räumt intent_event mit).** End-to-End-Lösch-Test (Task 3): 0 Rest-Zeilen in intent_event UND abstain_log. Nur Berater-eigene Aussage, anonymisiert falls Kunden-PII; tenant_id-Abschottung; kein wörtliches Kunden-Mitschneiden (DSGVO-Default). |
| T-TAXO2-03-05 | A (Availability) | dauerhaft scheiternde Einzelbewertung blockiert ganzen Call (Poison-Pill) | mitigate | **F-05: handling_status='failed' (try/except) — Merge zählt 'failed' nicht als offen; Call mit "N Events nicht bewertbar"-Marker benotbar.** |
| T-TAXO2-03-07 | A (Verfügbarkeit / H-3) | nach Crash/Deploy startet Consumer mit leerer Queue; pending-Events vom Producer nie nachgefüllt → Call hängt ewig "wird ausgewertet…" | mitigate | **H-3: `_requeue_pending` am Consumer-Start (Bootstrap) + gedrosselt im `_periodic_tick` (TAXO1-02-Haken) füllt `WHERE handling_status='pending'` nach. Idempotent (Skip via handling_status). Restart-Test belegt `[SLOW] requeue_pending` + Events danach scored.** |
| T-TAXO2-03-06 | E | LLM-Scoring rutscht in Live-Loop (Zirkelschluss) | mitigate | v1 regel-/marker (kein LLM-Import in handling_markers.py); LLM-Urteil NUR async Slow Lane (Bau-Regel 1). |
</threat_model>

<verification>
- handling_status live + verifiziert (von TAXO1-01, I-1 Option b — default 'pending', indiziert, beschildert) — Wurzel-Fix (F-01/F-05/F-06); KEINE TAXO2-Migration.
- Test-Anruf-DATEN-FLUSS: intent_event.handling_score_numeric + handling_status befüllt nach Live-Call.
- Idempotenz: handling_status != 'pending' wird übersprungen → kein doppelter abstain_log (F-01).
- Poison-Pill: Exception → 'failed', kein Crash, kein Deadlock (F-05).
- grep: kein handling_score/handling_status-Write aus Fast/Medium Lane (Bau-Regel 1, ORM-aware F-04).
- Slow-Lane-Restart re-derived Arbeitsliste aus DB `WHERE handling_status='pending'` (Multi-Worker-ready, F-06).
- H-3: `_requeue_pending` am Consumer-Start + gedrosselt im _periodic_tick GEBAUT (nicht nur behauptet); Restart-Test belegt `[SLOW] requeue_pending` + pending→scored; idempotent. Plan-04 erweitert _periodic_tick um H-2/M-4 (Interlock-Notiz).
- abstain_log trägt Satz + interaction_id (D-07) + harter FK CASCADE (F-08).
- **F-08-Cascade-Kette end-to-end: Test-Call-Löschung → 0 Rest-Zeilen in intent_event UND abstain_log (durchgehend calls→intent_event→abstain_log); intent_event.call_id-Naht per inspect.sh belegt, bei LOSE FK INTERLOCK-Flag (REVIEWS).**
- Race-Gate (Task 0, ORM-aware): kein Live-Leser → In-Place race-frei.
- handling_status-INTERLOCK-Note (TAXO1↔TAXO2) + F-08-Cascade-Ketten-Note (intent_event.call_id) im 05-Log + SUMMARY.
</verification>

<success_criteria>
- handling_status-Wurzel-Fix live; Arbeitsliste = WHERE handling_status='pending'.
- handling_score_numeric 1-3/NULL regel-/marker-basiert, großzügige Abstention → 'abstained'; Fehler → 'failed' (kein Deadlock).
- In-Place-Write race-frei ORM-aware bewiesen (oder Fallback gewählt); §8.7-Abweichung + handling_status-Interlock im 05-Log.
- Goodhart-Logging vollständig (abstain_log harter FK CASCADE); LLM NUR async; Multi-Worker-ready vorbereitet (Block M).
- **F-08-DSGVO-Cascade-Kette durchgehend belegt (calls→intent_event→abstain_log, 0 Rest-Zeilen) ODER INTERLOCK-Flag für die intent_event.call_id-Naht gesetzt (REVIEWS).**
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-03-SUMMARY.md` (inkl. ORM-aware Race-Gate-grep-Ergebnis (F-04), handling_status-Verifikations-Beleg (von TAXO1-01, I-1 Option b — keine TAXO2-Migration), Test-Anruf-intent_event-Sample mit handling_status, Restart-Re-Derivation `WHERE handling_status='pending'` (F-06), **H-3 Bootstrap-Re-Queue-Beleg (`[SLOW] requeue_pending`-Log + pending→scored nach Restart) + Plan-04-_periodic_tick-Erweiterungs-Interlock-Notiz (H-2/M-4)**, abstain_log-Sample + FK-CASCADE-Beleg (F-08), F-08-End-to-End-Lösch-Assertion (0 Rest-Zeilen in intent_event UND abstain_log; intent_event.call_id-Cascade von TAXO1-01/I-2), Poison-Pill-Test (F-05), §8.7-Abweichungs-Notiz + handling_status-INTERLOCK-Note (I-1 Option b) + F-08-Cascade-Ketten-Note (I-2 resolved)).
</output>
</invoke>
