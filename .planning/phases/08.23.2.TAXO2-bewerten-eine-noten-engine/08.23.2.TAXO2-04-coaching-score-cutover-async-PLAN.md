---
phase: 08.23.2.TAXO2
plan: 04
type: execute
wave: 4
depends_on: ["02", "03"]
files_modified:
  - routes/app_routes.py
  - routes/dashboard.py
  - templates/   # session_detail-Template: additives "Neu — Vorschau"-Panel (Template-Name beim Execute bestaetigen)
  - services/slow_lane.py
  - config.py
  - tests/test_coaching_score_cutover.py
# RE-SCOPE (FOLD 23.06.): static/pip-launcher.js NICHT mehr angefasst (kein _calcScore-Loeschen, kein Display-Cutover — das ist SCORE-UI).
# RE-SCOPE (FOLD 26.06., Option B / 3-Sichten): F-09 (outcome-Vorbedingung) GESTRICHEN (Engine ergebnis-blind); H-2-Sweep KOMPLETT GESTRICHEN; outcome = reine ANZEIGE-Sperre.
autonomous: false
requirements: ["SPEC Req 2 (NUR Engine-Compute+Store, KEIN Formel-Loeschen — re-scoped FOLD 23.06.)", "SPEC Req 5 (Anzeige unveraendert + Vorschau-Panel)", "SPEC Req 6 (NULL-Edge im Vorschau-Panel)", "SPEC Req 9 (Speech-Stats-Verkabelung)", "D-09", "D-10"]
user_setup: []

must_haves:
  truths:
    - "RE-SCOPE (FOLD 23.06., André + Design-Brief): Plan 04 ist NUR noch 'rechnen + speichern + roh als Vorschau zeigen + EINMAL end-to-end verifizieren' — KEIN Display-Cutover. Das LÖSCHEN der alten Formel + das UMSCHALTEN der angezeigten Score-Leser wandert in die eigene Phase NACH TAXO (08.23.2.SCORE-UI). Die alte Anzeige bleibt während TAXO komplett unverändert stehen."
    - "Die Engine rechnet die neue Note + schreibt sie ins Daten-Fundament: NUR `rubric_score` (volle Aufschlüsselung inkl. coaching_score-Feld, Vorschau-Quelle) — async (Slow Lane), idempotenter UPSERT, M-4-Tenant-GUC. **Option B (Audit RESOLVED 23.06., Grep-Beleg: 0 Anzeige-Leser von calls.coaching_score): die Engine schreibt NICHT `calls.coaching_score`** — die Spalte behält ihren EINEN alten Schreiber (correct_outcome:2011, unverändert); der Spiegel wandert nach 08.23.2.SCORE-UI (sonst zwei Schreiber + Anzeige-Änderung)."
    - "Die alte Live-Formel (_calc_process_score/_calc_call_score/_apply_outcome_modifier/_OUTCOME_MODIFIERS/_calc_coaching_score + client _calcScore) bleibt STEHEN — NICHT gelöscht; die heutigen Anzeige-Leser (dashboard session_detail, pip-launcher) bleiben auf ihrem heutigen Pfad (NICHT umgestellt)"
    - "Ein rohes, klar als 'Neu — Vorschau' markiertes, read-only Panel in session_detail zeigt die neue coaching_score + die 7 rubric-Dimensionen (Wert/Gewicht/messbar?) + die 'nicht messbar weil…'-Gründe — NEBEN der alten Anzeige, bewusst ungestylt (Zahlen + Liste + Stempel). Zweck: die ganze Kette (Rechnen→Speichern→RLS→Lesen→Anzeigen) an einem echten Test-Anruf EINMAL verifizieren ('getestet ≠ eingebunden', Welle-4-Lehre)"
    - "Ein Live-Test-Call erzeugt eine rubric_score-Zeile, die im Vorschau-Panel sichtbar wird"
    - "Score-Timing ist async (Slow Lane) — Call-Ende ist NICHT auf die Note blockiert, Interim 'wird ausgewertet…'"
    - "Der Call-Ende-Merge feuert NUR bei HARTER Vorbedingung: call_status=='ended' (aus /api/beenden) UND pending_events(call_id)==0 (F-02) — scored/abstained/failed sind TERMINAL, zählen NICHT als pending (F-01/F-05; 'failed' terminal verifiziert slow_lane.py:236/248/267 → pending draint sicher auf 0). **KEINE outcome-Vorbedingung (F-09 GESTRICHEN 26.06., 3-Sichten):** die Engine ist ergebnis-blind (rubric_engine.py:30-31/120 — compute_rubric liest calls.outcome NICHT für die Benotung), darum braucht der Merge calls.outcome nicht. **Scoring-Anstoß = api_beenden (Call-ended-Signal) → slow_lane.put, NICHT mehr correct_outcome (entkoppelt).** outcome wird zur reinen ANZEIGE-Sperre (Punkt unten), nicht zum Trigger."
    - "ANZEIGE-Sperre (André 'nachdem'-Wunsch, 26.06.): die Note wird FRÜH berechnet + in rubric_score gespeichert (sobald ended + 0 pending), aber im 'Neu — Vorschau'-Panel erst ANGEZEIGT, wenn `calls.outcome` bestätigt (NOT NULL). Solange outcome NULL → das Panel zeigt 'Ergebnis bestätigen, dann erscheint die Note' (statt der Note). Die Sperre ist ein DISPLAY-Gate via call_id-Join auf `calls.outcome` — KEIN Compute-/Merge-Gate. **rubric_score speichert das outcome NICHT redundant (Gemini)** — es bleibt in `calls`, die Sperre liest es per Join."
    - "Kein H-2-Sweep (GESTRICHEN 26.06.): der Merge wartet NICHT mehr aufs bestätigte Ergebnis → es entsteht kein 'wird ausgewertet…'-Hänger durch ein nie gesetztes outcome. Festhängende Momente (Consumer-Restart, pending nie nachgefüllt) deckt Plan-03-H-3 (`_requeue_pending`) bereits ab. Es gibt KEINEN SCORE_SWEEP_AFTER_S, KEINEN _periodic_tick-Sweep, KEINE outcome='unknown'-Daemon-Schreibung, KEINE Request-Context-Falle und KEINE H-2-Tests mehr in Plan 04."
    - "<50%-Call (Proration) oder poor-audio (D-09) → coaching_score=NULL → das Vorschau-Panel zeigt 'nicht genug Daten' ohne Crash/leere 0"
    - "Audio-Gate D-09 läuft VOR dem Scoring (audio_health_score < Schwelle ODER NULL ODER < N hoch-konfidente Events → not_gradable, gespeichert+geflaggt). **Die hoch-konfidenten Events zählt das Gate SELBST aus der geladenen events-Liste (confidence ≥ Tor-1-Schwelle) — NICHT aus dem compute_rubric-Rückgabe-Dict (das Feld `n_high_confidence_events` existiert dort NICHT — Detail-Drift-Fix 26.06.).** N ist ein Config-Default (F-07)"
    - "rubric_score-UPSERT referenziert den partiellen Unique-Index aus Plan 01: ON CONFLICT (call_id) WHERE origin='live' DO UPDATE (F-03)"
    - "M-4 (HÖCHSTES STILLES RISIKO): der Slow-Lane-Daemon setzt VOR jedem rubric_score-Write `set_current_tenant(str(call.tenant_id))` (database/db.py:43, app.py:2200-Muster) — sonst ist im Daemon-Thread (kein Request-Context) der app.tenant_id-GUC leer → FORCE-RLS WITH CHECK fail-closed → INSERT abgelehnt → coaching_score ewig NULL, lautlos. `clear_current_tenant()` steht ZWINGEND im `finally`-Block (läuft auch bei Exception in compute_rubric/commit) — sonst leakt die tenant_id im Endlos-Daemon-Thread in die nächste Iteration → Cross-Tenant-Schreib-Risiko"
    - "ROBUSTHEIT (FOLD 23.06., Gemini-Schluss-Review): (1) MAX-RETRY+DEAD-LETTER — der Merge-Job hat einen Versuchs-Zähler, scheitert er → gedeckelter Re-Queue (SCORE_MAX_RETRIES, Default 3), danach dead-letter (laut loggen/alert, Job aus Queue, optional rubric_score status='failed'); (2) DAEMON ÜBERLEBT — ein Fehler failt NUR DEN Call (pro-Call try/except → re-queue-mit-Cap → continue), der Consumer-Thread stirbt NIE (sonst stehen alle Calls); (3) Vorschau-Panel fängt ROW-ABSENT (Zeile noch nicht da) UND field-NULL UND status='failed' (Dead-Letter) ab → 'Score wird im Hintergrund berechnet…' bzw. 'Berechnung fehlgeschlagen', Stempel 'Beta'"
    - "M-4-VERSCHÄRFUNG (FOLD 23.06., Gemini): scheitert der rubric_score-Write am RLS (Tenant-GUC fehlt/leer ODER permission-denied) → der Daemon CRASHT HART + loggt LAUT (`[SLOW] FATAL rubric_score write rejected (RLS/tenant)`), und der Job/das Event wird NICHT als 'erledigt' markiert (kein Merge-Done-Flag) → der Re-Queue/Retry greift. KEIN Silent-Drop, KEIN 'als ob es lief'-Weitermachen — sonst ist die Note für immer weg und niemand merkt es (genau die M-4-Stille, die wir hassen)"
    - "H-1: MOOT unter Re-Scope (nichts gelöscht → kein NameError-Risiko; der Stash-Block bleibt unverändert). Das Löschen + die H-1-Einheits-Disziplin wandern mit der Formel-Löschung in die SCORE-UI-Phase."
  artifacts:
    - path: "services/slow_lane.py"
      provides: "Call-Ende-Merge: harte Vorbedingung (call ended + 0 pending, KEIN outcome) + compute_rubric → UPSERT rubric_score (async; Option B: KEIN calls.coaching_score-Write); KEIN H-2-Sweep"
      contains: "compute_rubric"
    - path: "routes/app_routes.py"
      provides: "alte Formel BLEIBT (Re-Scope); /api/beenden markiert Call ended UND stößt den async Engine-Merge an (slow_lane.put); correct_outcome bleibt UNVERÄNDERT (entkoppelt — kein neuer slow_lane.put)"
    - path: "config.py"
      provides: "D-09 Audio-Gate-Defaults: AUDIO_HEALTH_GATE_THRESHOLD + MIN_HIGH_CONFIDENCE_EVENTS (F-07) + SCORE_MAX_RETRIES (Robustheit). KEIN SCORE_SWEEP_AFTER_S (H-2 gestrichen)."
    - path: "routes/dashboard.py"
      provides: "session_detail gibt rubric_preview (rubric_score-Zeile) + outcome_confirmed (calls.outcome IS NOT NULL) ADDITIV ans Template; altes Score-Hero unverändert"
  key_links:
    - from: "services/slow_lane.py (Call-Ende-Merge)"
      to: "rubric_score (Option B: NUR diese Tabelle)"
      via: "compute_rubric → UPSERT rubric_score (einziger Engine-Write), async (D-10), harte Vorbedingung (F-02: ended + 0 pending, KEIN outcome), partieller Unique-Index (F-03); KEIN calls.coaching_score-Write; KEIN H-2-Sweep"
      pattern: "rubric_score"
    - from: "routes/dashboard.py session_detail (Vorschau-Panel)"
      to: "rubric_score (NICHT calls.coaching_score) + calls.outcome (Anzeige-Sperre)"
      via: "rubric_preview read-only; ANZEIGE-Sperre via calls.outcome-Join (Note erst bei outcome NOT NULL); NULL/not_gradable/failed-Edge im Panel"
      pattern: "rubric_preview"
---

<objective>
> ## ⬛ RE-SCOPE (FOLD 23.06.2026 — André-Entscheidung + Design-Brief `Nerve-Vault/03 Planung/Scoreboard + Auswertung Redesign`)
>
> **Plan 04 ist jetzt „rechnen + speichern + roh als Vorschau zeigen + EINMAL end-to-end verifizieren" — KEIN Display-Cutover.** Das Umschalten der Anzeige + das komplette Redesign sind eine **eigene Phase NACH TAXO (08.23.2.SCORE-UI)**. TAXO baut nur das Fundament + verifiziert es einmal an einem echten Anruf.
>
> **BEHALTEN (in Plan 04):** Engine rechnet (compute_rubric, Plan 02) + schreibt ins Daten-Fundament — **NUR `rubric_score`** (volle Aufschlüsselung inkl. coaching_score-Feld als Kopf-Zahl), async, idempotenter UPSERT (F-03), M-4-Tenant-GUC, Audio-Gate (D-09), harte Merge-Vorbedingung (F-02). **KEIN `calls.coaching_score`-Write (Option B, siehe Audit unten).** Alles wie unten in Task 1.
>
> **RAUS aus Plan 04 → wandert nach SCORE-UI:** (a) das **LÖSCHEN** der alten Formel (`_calc_call_score`/`_calc_coaching_score`/`_calc_process_score`/`_apply_outcome_modifier`/`_OUTCOME_MODIFIERS` + client `_calcScore`); (b) das **UMSCHALTEN** der angezeigten Score-Leser (dashboard.py session_detail :774, pip-launcher). **Die alte Anzeige + die alte Formel bleiben während TAXO komplett unverändert stehen.**
>
> **NEU in Plan 04:** ein rohes, read-only, klar als **„Neu — Vorschau"** markiertes Panel in session_detail, das die neue coaching_score + die 7 rubric-Dimensionen (Wert/Gewicht/messbar?) + die „nicht messbar weil…"-Gründe rendert — **NEBEN** der alten Anzeige. Bewusst ungestylt (Zahlen + Liste + Stempel), kein Layout-Aufwand (das ist SCORE-UI). Zweck: die ganze Kette (Rechnen→Speichern→RLS→Lesen→Anzeigen) an einem echten Test-Anruf EINMAL verifizieren (gegen „getestet ≠ eingebunden", Welle-4-Lehre „blieb leer").
>
> **✅ AUDIT-PUNKT RESOLVED = Option B (Pre-Execute-Audit 23.06., Claudian + Grep-Beleg):** `grep -rn "coaching_score" routes/dashboard.py routes/performance.py static/pip-launcher.js templates/` == **0 Treffer** → KEIN heutiger Anzeige-Pfad liest `calls.coaching_score` direkt (das Score-Hero re-rechnet via `_calc_call_score`, der PiP nutzt `final_score` aus der correct_outcome-Antwort). `calls.coaching_score` hat GENAU EINEN lebenden Schreiber (correct_outcome:2011), und der BLEIBT (Re-Scope: alte Welt unangetastet). **Entscheidung (gelockt):** die Engine schreibt in TAXO **NUR `rubric_score`**, NICHT `calls.coaching_score`. Der `calls.coaching_score`-Spiegel wandert nach **08.23.2.SCORE-UI**. Das Vorschau-Panel liest `rubric_score` direkt. M-4-Tenant-GUC + Hard-Crash-bei-RLS-Fehler gelten weiter für den `rubric_score`-Write.

> ## ⬛ RE-SCOPE 2 (FOLD 26.06.2026 — 3-Sichten-Entscheidung: Claudian-Audit + Gemini + Code-Verifikation → „Option B: Compute/Display entkoppeln")
>
> **Anlass:** die Engine ist **ergebnis-blind** (am Code verifiziert: `services/rubric_engine.py:30-31` „compute_rubric liest calls.outcome NICHT mehr für die Benotung", `:120` neutrales `reason='na'` statt outcome-getriebenem Straf-Grund). Damit ist die **F-09-Begründung obsolet** — der ursprüngliche F-09-Fold verlangte `calls.outcome IS NOT NULL` als Merge-Vorbedingung, weil compute_rubric angeblich outcome braucht (D-08-Guard + outcome_progression). Das stimmt für die heutige Engine NICHT mehr.
>
> **Umbau (6 Punkte, je am Code/Audit belegt):**
> 1. **F-09 GESTRICHEN.** Merge-Vorbedingung = NUR `call_status=='ended'` UND `pending_events(call_id)==0`. Die Bedingung `calls.outcome IS NOT NULL` ist RAUS. Der Scoring-Anstoß ist NICHT mehr an `correct_outcome` gekoppelt — der Merge feuert, sobald der Call beendet ist UND alle Momente terminal sind (scored/abstained/failed; `'failed'` terminal verifiziert `slow_lane.py:236/248/267`, draint pending sicher auf 0).
> 2. **H-2-Sweep KOMPLETT GESTRICHEN** (`SCORE_SWEEP_AFTER_S`, der `_periodic_tick`-Sweep, die Request-Context-Falle, ALLE H-2-Tests). Kein Warten aufs Ergebnis = kein Hänger. Festhängende Momente deckt der Plan-03-H-3-`_requeue_pending` bereits ab.
> 3. **Der „nachdem"-Wunsch (André) ist eine ANZEIGE-Sperre, KEIN Trigger:** das „Neu — Vorschau"-Panel (Task 3) zeigt die Note NUR, wenn `calls.outcome` bestätigt ist (NOT NULL); solange outcome NULL → „Ergebnis bestätigen, dann erscheint die Note" (statt der Note). Der Score wird FRÜH berechnet + in `rubric_score` gespeichert, aber erst nach Outcome-Bestätigung gerendert.
> 4. **`rubric_score` speichert das outcome NICHT redundant (Gemini):** es bleibt in `calls`; die Anzeige-Sperre liest es per `call_id`-Join auf `calls.outcome`.
> 5. **Detail-Drift-Fix:** `compute_rubric` liefert KEIN `n_high_confidence_events` (verifiziert: Return-Dict = `coaching_score, dimensions, is_provisional, measured_weight_pct, unmeasured_dimensions, status, mode_key`). Das D-09-Audio-Gate zählt die hoch-konfidenten Events SELBST aus der `events`-Liste (confidence ≥ Tor-1-Schwelle), NICHT aus dem Engine-Rückgabe-Dict.
> 6. **Stale-Text bereinigt:** `'not_reached'` gibt es in der Engine nicht mehr (Status = `scored|insufficient_data|no_weight_set`, verifiziert `rubric_engine.py:78`). Alle `'not_reached'`-Erwähnungen in Plan 04 sind entfernt/angepasst.
>
> **BEHALTEN (unverändert gut):** M-4-Tenant-GUC-Klammer (set/clear_current_tenant try/finally) für JEDEN rubric_score-Write; F-02-Schutz gegen verfrühten Merge (ended + 0 pending); F-03 idempotenter UPSERT; Audio-Gate D-09 (`status='not_gradable'`); Retry/Dead-Letter für den Merge-Job; Daemon überlebt; Hook-Registry-Einhängung; Re-Scope (alte Formel + Anzeige bleiben); Option B (kein calls.coaching_score-Write).

Die Engine (Plan 02) rechnet am Call-Ende die rubric_score-Aufschlüsselung UND die Kopf-Zahl (coaching_score-Feld) und schreibt sie ins Daten-Fundament — **NUR `rubric_score` (Option B, KEIN `calls.coaching_score`-Write; die Spalte behält ihren alten Schreiber correct_outcome:2011)**. Das Scoring läuft ASYNC auf der Slow Lane (D-10) — der Call-Ende-Moment ist nicht auf die Note blockiert (Interim "wird ausgewertet…"). Vor dem Scoring greift das Audio-Gate (D-09). **Die alte Live-Formel + die alten Anzeige-Leser bleiben STEHEN (Re-Scope oben);** das neue „Neu — Vorschau"-Panel in session_detail fängt `coaching_score=NULL`/not_gradable/failed sauber ab ("nicht genug Daten"/"Berechnung fehlgeschlagen"/"wird ausgewertet…", kein Crash/leere 0) UND hält die Note hinter der ANZEIGE-Sperre zurück, bis `calls.outcome` bestätigt ist.

**Cross-AI-Fold (REVIEWS F-02, HIGH):** Der Call-Ende-Merge braucht eine HARTE Call-Ende-Vorbedingung. Heute würde "alle Events benotet" mitten im laufenden Call transient WAHR werden (Slow-Lane benotet live aus der Queue) → vorzeitiger Merge → falscher/unvollständiger Score. Fix: Merge feuert NUR wenn `call_status == 'ended'` (Call ist via `/api/beenden` beendet) **UND** `pending_events(call_id) == 0`. **Queue-Timing explizit:** Events gelangen bei EMIT (live, während des Calls) in die Slow-Lane-Queue und werden live benotet (handling_score) — ABER der Call-Ende-MERGE (rubric_score) wird erst durch das `/api/beenden`-ended-Signal freigeschaltet (das ist jetzt auch der Scoring-Anstoß — FOLD 26.06.). `pending_events` zählt NUR `handling_status='pending'` — NICHT `failed`/`abstained`/`scored` (F-01/F-05: die sind terminal, `'failed'` verifiziert `slow_lane.py:236/248/267`). INTERLOCK mit TAXO1-Plan-02 (Slow-Lane-Gerüst): falls TAXO1 das Queue-Timing anders fixt → im 3-Wege-Interlock abgleichen.

**Cross-AI-Fold (REVIEWS F-03):** Der idempotente `rubric_score`-Write nutzt `ON CONFLICT (call_id) WHERE origin='live' DO UPDATE` — referenziert exakt den partiellen Unique-Index `ux_rubric_score_live_call_id` aus Plan 01 (Postgres verlangt einen passenden Index).

**Cross-AI-Fold (REVIEWS F-07):** Das D-09-Audio-Gate-Kriterium "zu wenig hoch-konfidente Events" bekommt einen konkreten Config-Default: `< MIN_HIGH_CONFIDENCE_EVENTS` (Default 3) Events über der Tor-1-Konfidenzschwelle → `not_gradable`. **Die Zählung passiert über die geladene `events`-Liste (confidence ≥ Tor-1-Schwelle), NICHT über das Engine-Rückgabe-Dict (FOLD 26.06., Detail-Drift).** In config.py/ENV (Punkt 12), post-launch tunbar.

**⬛ F-09 GESTRICHEN (FOLD 26.06., 3-Sichten — ersetzt den alten F-09-Fold):** Der ursprüngliche F-09-Fold (Claudian Runde 2) verlangte zusätzlich `calls.outcome IS NOT NULL` als Merge-Vorbedingung, mit der Begründung, `compute_rubric` brauche outcome (D-08-vergeigt-Guard + Dimension `outcome_progression`). **Am Code widerlegt:** die heutige Engine ist ergebnis-blind (`rubric_engine.py:30-31` — „liest calls.outcome NICHT mehr für die Benotung"; `:120` neutrales `reason='na'` statt eines outcome-Straf-Grunds; Status-Enum `scored|insufficient_data|no_weight_set`, KEIN `not_reached`). Ein Merge ohne bestätigtes outcome erzeugt also KEINE D-08-False-Green — die Note misst reines Verhalten, das Ergebnis zieht sie nie hoch oder runter. **Konsequenz:** die outcome-Vorbedingung entfällt; der Merge feuert auf `ended` + `0 pending`. Der „nachdem"-Wunsch (André: die Note erst NACH der Ergebnis-Bestätigung zeigen) wird als **ANZEIGE-Sperre** im Vorschau-Panel umgesetzt (Task 3, Display-Gate auf `calls.outcome`), NICHT als Compute-/Merge-Gate. **Scoring-Anstoß:** `api_beenden` (Call-ended-Signal) macht `slow_lane.put({'call_id': ...})`; `correct_outcome` bleibt UNVERÄNDERT (entkoppelt, kein neuer Anstoß dort). Kein Hang-Edge mehr (der Merge wartet nicht aufs outcome) → **H-2-Sweep gestrichen** (s.u.).

**Interlock-Fold (TAXO-INTERLOCK-FINDINGS M-4, HÖCHSTES STILLES RISIKO — code+prod-verifiziert):** rubric_score hat `FORCE ROW LEVEL SECURITY` (Plan 01). Der `app.tenant_id`-GUC wird NUR im `after_begin`-Hook gesetzt und NUR wenn der `_current_tenant_id`-Contextvar gefüllt ist (database/db.py:62-78, live auf Prod). Der Call-Ende-Merge läuft im **Slow-Lane-Daemon-Thread OHNE Request-Context** → Contextvar leer → GUC NULL → `nullif(current_setting('app.tenant_id', true),'')::uuid` = NULL → WITH CHECK `tenant_id = NULL` = false → der rubric_score-INSERT wird **fail-closed ABGELEHNT** → die Note bleibt **ewig NULL, ohne Fehler im UI**. **Fix:** vor dem Write `set_current_tenant(str(call.tenant_id))` (database/db.py:43 — exakt das blessed Pattern aus app.py:2200 im Request-Pfad; explizit auch für Worker-Threads dokumentiert, db.py:46-47), `clear_current_tenant()` ZWINGEND im `finally` (Gemini-Gegencheck: der Endlos-Daemon-Thread leakt sonst bei einer Exception in compute_rubric/commit die tenant_id in die nächste Iteration → Cross-Tenant-Write; finally garantiert das Räumen auf jedem Pfad). KEINE neue Mechanik — der Daemon nutzt die bestehende.

**⬛ H-2-Sweep GESTRICHEN (FOLD 26.06., 3-Sichten — ersetzt den alten H-2-Fold):** Der alte H-2-Fold baute einen zeitbasierten `_periodic_tick`-Sweep, der hängende „ended + outcome NULL"-Calls nach `SCORE_SWEEP_AFTER_S` auf `outcome='unknown'` setzte — als Auflösung des F-09-Hang-Edges (Merge wartet aufs outcome, das nie kommt). **Da F-09 gestrichen ist (der Merge wartet NICHT mehr aufs outcome), gibt es diesen Hang-Edge nicht mehr** → der Sweep ist überflüssig. **Komplett raus:** `SCORE_SWEEP_AFTER_S`-Config, der Sweep-Block im `_periodic_tick`, die `calls.outcome='unknown'`-Daemon-Schreibung, die Request-Context-/CHECK-Constraint-Fallen und alle H-2-Tests. `_periodic_tick` trägt damit in Plan 04 NUR noch das Plan-03-H-3 (`_requeue_pending`) — Plan 04 fasst `_periodic_tick` gar nicht mehr an. Festhängende Momente (Consumer-Restart) deckt H-3 ab.

Purpose: Macht die Engine live + tötet K2 im Produktiv-Pfad. RISKANTE Welle (Live-Score-Umbau + sync→async-Control-Flow) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht.
Output (RE-SCOPE + Option B + FOLD 26.06.): Daten-Fundament — async Engine-Verkabelung (NUR rubric_score, KEIN calls.coaching_score) mit harter Merge-Vorbedingung (F-02: ended + 0 pending, KEIN outcome), D-09-Gate (F-07, high-conf-Zählung aus events-Liste), idempotenter UPSERT (F-03), M-4-GUC + Hard-Crash-bei-RLS-Fehler; „Neu — Vorschau"-Panel in session_detail (NULL/not_gradable/failed abgefangen + ANZEIGE-Sperre auf calls.outcome); alte Formel + Anzeige + calls.coaching_score-Schreiber unverändert; Test-Anruf-Beweis. KEIN H-2-Sweep.

Bestehende PRODUKT-Anzeige (Call-Ende-Screen, session_detail Score-Hero) bleibt unverändert (Req 5 Rider — kein neuer „nackte-Zahl"-Schaden im Produkt). Das „Neu — Vorschau"-Panel ist davon ausgenommen: es ist ein klar gestempeltes, read-only Verifikations-Panel NEBEN der alten Anzeige (kein Produkt-Feedback-Element) — explizit vom Design-Brief autorisiert. Das gestylte Breakdown-Redesign bleibt die spätere SCORE-UI-Phase (vormals 999.2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** RISKANTE Welle (RESEARCH §I) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht (Bildschirme rendern korrekt aus neuer Quelle, NICHT nur "Note gerechnet" — K1/K8-Lehre). `bash deploy.sh production` + `inspect.sh` + Live-Test als Test-User. KEIN lokales pytest/git pull.

**autonomous:false** — Human-Verify-Checkpoint (Test-Anruf: „Neu — Vorschau"-Panel rendert Note+Dimensionen NACH Outcome-Bestätigung + alte Anzeige unverändert + <50%-Call + poor-audio-Call + outcome-noch-nicht-bestätigt (Anzeige-Sperre) + Dauer-Korrektheit). André testet.

**UTF-8:** "wird ausgewertet…" / "Ergebnis bestätigen, dann erscheint die Note" user-facing mit Umlaut/Ellipse OK. Config-Identifier ASCII.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- Alte Formel (bleibt — Re-Scope) + Schreiber/Leser — grep file:line aus RESEARCH §A2/§H. -->

ALT (routes/app_routes.py — BLEIBT, NICHT löschen; Löschung = SCORE-UI):
- :743 _calc_call_score(conv)  (40/30/20/10-Formel, rede_score Fixwert)
- :757 _OUTCOME_MODIFIERS (dict)
- :768 _calc_process_score(conv)  (frage_qualitaet=0.0 :785 K2, redeanteil Fixwert :782-783)
- :806 _apply_outcome_modifier(...)
- :829 _calc_coaching_score(conv, outcome)  (Wrapper)
Aufruf-Stellen:
- :659 _calc_process_score → score_breakdown (:660), coaching_score bleibt NULL (:652)
- :2005-2011 correct_outcome: _calc_process_score + _apply_outcome_modifier → SCHREIBT calls.coaching_score (:2011) SYNCHRON — **BLEIBT UNVERÄNDERT (Option B + Entkopplung FOLD 26.06.: KEIN neuer slow_lane.put hier)**
- :1111 _calc_call_score (Trend api_postcall_trend)

ALT (static/pip-launcher.js — BLEIBT; Löschung = SCORE-UI):
- :3183 function _calcScore(postcall)  (bereits aus Render-Pfad raus :3248/:3251; Render nutzt json.final_score :4181/4185/4197)

HEIMTÜCKISCHE Leser (dashboard.py — UNTER RE-SCOPE NICHT umstellen, RESEARCH §H/finding 3):
- :724 from routes.app_routes import _calc_call_score
- :774 score_total = _calc_call_score(conv)  ← rechnet HEUTE neu; bleibt unverändert (Umstellen = SCORE-UI)
- :795 sum(_calc_call_score(c) for c in recent)  (Trend-Avg) — bleibt

Single-Writer-Befund (RESEARCH §A2) + Option B: calls.coaching_score hat GENAU EINE lebende Schreib-Stelle (:2011) — die BLEIBT. TAXO2 biegt sie NICHT um und legt KEINE zweite an (die neue Note lebt in rubric_score). Der calls.coaching_score-Umzug auf die Engine ist SCORE-UI.

calls (inspect.sh §A1): coaching_score (double NULL), audio_health_score (double NULL, 0.0-1.0-Skala!), score_breakdown (jsonb), call_mode, outcome (Anzeige-Sperre-Quelle, FOLD 26.06.), tenant_id, ended_at

Call-Ende-Signal (F-02) + Scoring-Anstoß (FOLD 26.06.): /api/beenden ist der Call-Ende-Punkt. Es (a) markiert den Call beendet (ended_at/Status — beim Execute via `inspect.sh schema calls` bestätigen, welches Feld 'ended' signalisiert) UND (b) macht `slow_lane.put({'call_id': ...})` (Scoring-Anstoß). pending_events(call_id) = COUNT intent_event WHERE call_id=X AND handling_status='pending'.

handling_status (Plan 03, intent_event): pending|scored|abstained|failed.
  pending_events zählt NUR 'pending' (scored/abstained/failed = terminal, F-01/F-05; 'failed'-Setzpunkte slow_lane.py:236/248/267).

rubric_score (Plan 01): partieller Unique-Index ux_rubric_score_live_call_id (call_id, WHERE origin='live')
  → UPSERT: INSERT ... ON CONFLICT (call_id) WHERE origin='live' DO UPDATE SET ... (F-03). **Speichert KEIN outcome (FOLD 26.06.) — outcome bleibt in calls.**

Engine (Plan 02, services/rubric_engine.py): compute_rubric(events, speech_stats, call, mode_config) -> dict
  {coaching_score, dimensions, is_provisional, measured_weight_pct, unmeasured_dimensions, status, mode_key}
  — **ergebnis-blind (rubric_engine.py:30-31): liest calls.outcome NICHT für die Benotung.**
  — **KEIN `n_high_confidence_events` im Return (FOLD 26.06.) — das D-09-Gate zählt selbst aus events.**
  — status ∈ {scored, insufficient_data, no_weight_set} (rubric_engine.py:78); KEIN 'not_reached'.
Speech-Stats: services/live_session.py get_speech_stats(sid) (K1 per-SID — Req 9 Verkabelung)

config.py (F-07, NEU): AUDIO_HEALTH_GATE_THRESHOLD = float(os.getenv('AUDIO_HEALTH_GATE_THRESHOLD', '0.5'))  # 0.0-1.0-Skala
                       MIN_HIGH_CONFIDENCE_EVENTS = int(os.getenv('MIN_HIGH_CONFIDENCE_EVENTS', '3'))  # D-09 "zu wenig hoch-konfidente Events"
                       SCORE_MAX_RETRIES = int(os.getenv('SCORE_MAX_RETRIES', '3'))  # Merge-Job-Retry-Cap → dead-letter
                       # KEIN SCORE_SWEEP_AFTER_S (H-2 gestrichen, FOLD 26.06.)

M-4 GUC-Vertrag (database/db.py, LIVE auf Prod — verifiziert HEAD=0015):
- set_current_tenant(tid)  (db.py:43): publiziert die Tenant-UUID in den _current_tenant_id-Contextvar; der after_begin-Hook (db.py:62-78) setzt daraus transaction-local app.tenant_id. "Called ... by any worker thread before its session work" (db.py:46-47) — ausdrücklich für Daemon-Threads gesegnet.
- clear_current_tenant()  (db.py:52): Hygiene nach dem Write (Contextvar leeren).
- Request-Pfad-Muster (app.py:2200): `_set_tenant(_sess.get('tenant_id'))` — der Daemon spiegelt das mit `set_current_tenant(str(call.tenant_id))`.
- Reihenfolge KRITISCH: set_current_tenant MUSS vor dem Transaktions-Beginn des rubric_score-Writes stehen (der GUC wird bei after_begin gesetzt). calls.tenant_id wird VORHER gelesen (calls ist nicht FORCE-RLS → Lesen ohne GUC ok); dann GUC setzen; dann rubric_score INSERT/UPSERT.

Tick-Vertrag (services/slow_lane.py, TAXO1-02 §0.1 + Plan 03) — FOLD 26.06.:
- def _periodic_tick(): bei queue.Empty-Timeout (alle SLOW_LANE_TICK=5s) gerufen. Plan 03 hat ihn um H-3 (_requeue_pending) erweitert. **Plan 04 fasst `_periodic_tick` NICHT mehr an (H-2-Sweep gestrichen).**
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `calls` (LESE call-ended-Signal F-02 + tenant_id/audio_health/call_mode/outcome; **KEIN Engine-Write — Option B + FOLD 26.06.: auch KEIN `calls.outcome='unknown'`-Sweep-Write mehr (H-2 gestrichen)**; calls ist für Plan 04 read-only) — Schema §A1, KEINE Schema-Änderung
- `rubric_score` (UPSERT auf partiellem Unique-Index, Plan-01-Tabelle) — **die EINZIGE Engine-Schreib-Tabelle der Note** (Aufschlüsselung + coaching_score-Feld + status). **Speichert KEIN outcome (FOLD 26.06.).**
- `intent_event` (LESE Aggregations-Eingang + COUNT handling_status='pending' für F-02 + confidence für die D-09-high-conf-Zählung) — DEPENDS-ON-TAXO1 + Plan 03 handling_status
- `conversation_logs` (LESE für dauer_sekunden/D-08; KEIN Aggregat-Write hier — das ist Plan 06/07)
### inspect.sh-Beleg (VERBATIM §A1, beim Execute neu ziehen)
```
coaching_score        | double precision |   (NULL-fähig → kein Schema-Change für NULL-Edge)
audio_health_score    | double precision |   (0.0-1.0-Skala — D-09-Schwelle auf dieser Skala!)
outcome               | text             |   (Anzeige-Sperre-Quelle, FOLD 26.06. — NICHT Merge-Gate)
ended_at              | (Call-Ende-Signal für F-02 — beim Execute bestätigen welches Feld 'ended' markiert)
```
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| call.coaching_score | **KEIN Engine-Write (Option B)** — bleibt alter Schreiber correct_outcome:2011 | DB calls.coaching_score (double NULL, unverändert) | ✓ Audit RESOLVED (grep 0 Anzeige-Leser) |
| rubric_score.coaching_score (Kopf-Zahl) | Engine UPSERT (einziger Schreiber der Note) | DB rubric_score (Plan 01) | ⬜ Task 1 Test-Anruf |
| call_status == 'ended' (F-02) | Merge-Vorbedingung LESE | DB calls.ended_at/status (beim Execute bestätigen) | ⬜ Task 1 |
| pending_events(call_id) (F-02) | COUNT handling_status='pending' | DB intent_event.handling_status (Plan 03) | ⬜ Task 1 |
| calls.outcome (ANZEIGE-Sperre, FOLD 26.06.) | dashboard LESE (Display-Gate, NICHT Merge) | DB calls.outcome (unverändert, alter Schreiber) | ⬜ Task 3 |
| call.audio_health_score | D-09-Gate LESE (0.0-1.0) | DB calls.audio_health_score | ✓ §A1 (Skala 0-1, NICHT 0-100) |
| n_high_confidence (F-07) | D-09-Gate Zählung **aus events-Liste (confidence ≥ Tor-1), NICHT Engine-Dict** | RAM (Aggregation aus intent_event.confidence) | ⬜ Task 1 (FOLD 26.06.) |
| status='not_gradable' | D-09 SCHREIBE | DB **rubric_score.status** (Option B — NICHT calls.score_breakdown) | ⬜ Task 1 |
| rubric_score-Zeile | Engine UPSERT (ON CONFLICT partieller Index, F-03) | DB rubric_score (Plan 01) | ⬜ Task 1 Test-Anruf |
| speech_stats (redeanteil/tempo/monolog) | get_speech_stats(sid) per-SID (NICHT tote Globale, K1) | per-SID-State RAM | ✓ RESEARCH §A4 |

## Verbindungs-Karte (Punkt 22)
- **Single-Writer bewiesen (RESEARCH §A2) + Option B (Audit RESOLVED 23.06.):** calls.coaching_score = GENAU EINE Schreib-Stelle app_routes.py:2011, die BLEIBT (Re-Scope). Die Engine schreibt NICHT in calls.coaching_score. grep-Beleg: `grep -rn "coaching_score" routes/dashboard.py routes/performance.py static/pip-launcher.js templates/` == 0 (kein Anzeige-Leser der Spalte) → der Engine-Wert lebt in rubric_score, die alte Anzeige bleibt unverändert. Nach Bau: `grep -rn "coaching_score" services/slow_lane.py` zeigt KEINEN calls.coaching_score-Write (nur rubric_score.coaching_score im UPSERT).
- **calls IST in Plan 04 read-only (FOLD 26.06.):** mit dem Streichen des H-2-Sweeps gibt es KEINEN Engine-`calls`-Write mehr (auch kein `calls.outcome='unknown'`). `grep -nE "\.outcome *=|UPDATE calls" services/slow_lane.py` == 0.
- **Heimtückischer Leser (RESEARCH §H finding 3) — unter Re-Scope HARMLOS:** dashboard.py:774 rechnet `_calc_call_score` neu (liest NICHT coaching_score) → BLEIBT unverändert (nichts wird gelöscht/umgestellt in TAXO). Das Umstellen wandert nach SCORE-UI.
- **Audio-Gate-Andock (RESEARCH §F):** Call.audio_health_score existiert (models.py:698, 0.0-1.0). KEINE neue Spalte. IS NULL = kein Buffer = auch not_gradable. Die high-conf-Event-Zählung kommt aus den geladenen intent_event-Rows (confidence ≥ Tor-1), NICHT aus dem Engine-Dict (FOLD 26.06.).
- **Call-Ende-Signal + Anstoß (F-02, FOLD 26.06.):** beim Execute via `inspect.sh schema calls` bestätigen, welches Feld den beendeten Call markiert (ended_at IS NOT NULL / status). pending_events nutzt intent_event.handling_status (Plan 03). Der Scoring-Anstoß sitzt in api_beenden (`slow_lane.put`). INTERLOCK TAXO1-Plan-02 (Queue-Timing).
- **ANZEIGE-Sperre (FOLD 26.06.):** das Vorschau-Panel liest `calls.outcome` per call_id-Join und zeigt die rubric_score-Note NUR bei `outcome IS NOT NULL`. Reines Display-Gate, kein Merge-Gate; outcome wird NICHT in rubric_score dupliziert.
- **UPSERT-Index (F-03):** Plan 01 baut ux_rubric_score_live_call_id (call_id, WHERE origin='live') → ON CONFLICT referenziert genau diesen.

## 1. Lokaler Kontext (Punkt 14 — api_beenden + correct_outcome + Call-Ende-Merge)
**RE-SCOPE + Option B + FOLD 26.06.:** `correct_outcome` behält seine HEUTIGE sync-Logik (inkl. `row.coaching_score=...` :2011) **vollständig UNVERÄNDERT** (alte Anzeige bleibt) — und bekommt **KEINEN** neuen slow_lane.put (entkoppelt). Der Scoring-Anstoß wandert nach `api_beenden`: es markiert den Call ended UND macht `slow_lane.put({'call_id': ...})`. Response/early returns/Ownership-Checks unangetastet.
**F-02 Merge-Vorbedingung:** der Call-Ende-Merge im Consumer feuert NUR wenn der Call ended ist (call_status/ended_at) UND pending_events==0. **KEINE outcome-Bedingung (F-09 gestrichen, FOLD 26.06.).** Das verhindert den transient-WAHR-Merge mitten im Call; das outcome ist für den Merge irrelevant (Engine ergebnis-blind).

## 2. Funktions-Skelett
api_beenden (:659ff): markiert den Call als beendet (ended_at/status) → das ist das F-02-Call-Ende-Signal → **[NEU additiv] `slow_lane.put({'call_id': ...})` (Scoring-Anstoß, FOLD 26.06.).** Bestehenden _calc_process_score-Stash NICHT entfernen (Re-Scope — alte Welt unangetastet).
correct_outcome: Ownership-Check → outcome/followup persistieren → **bestehende sync-Anzeige-Logik (row.coaching_score=… via _calc_process_score) BLEIBT** → bestehende Response. **KEIN slow_lane.put hier (entkoppelt, FOLD 26.06.). H-1 MOOT** (nichts gelöscht → kein NameError; der Stash-Block bleibt).
Slow-Lane Call-Ende-Merge (slow_lane.py):
  WENN call_status=='ended' UND pending_events(call_id)==0 (NUR handling_status='pending' zählt, NICHT scored/failed/abstained — F-01/F-05):
    → **M-4: set_current_tenant(str(call.tenant_id)) (VOR dem rubric_score-Write — sonst FORCE-RLS fail-closed → rubric_score-Note fehlt ewig, Vorschau leer)**
    → Audio-Gate D-09 (F-07: audio_health < AUDIO_HEALTH_GATE_THRESHOLD ODER NULL ODER n_high_confidence_events < MIN_HIGH_CONFIDENCE_EVENTS — **n_high_confidence selbst aus der events-Liste gezählt (confidence ≥ Tor-1), NICHT aus dem Engine-Dict**)
    → wenn not_gradable: **rubric_score mit status='not_gradable' + coaching_score=NULL** (Option B — NICHT calls)
    → sonst compute_rubric (mode_key aus calls.call_mode, N-4) → **UPSERT rubric_score (ON CONFLICT call_id WHERE origin='live', F-03) — KEIN UPDATE calls.coaching_score (Option B), KEIN outcome in rubric_score (FOLD 26.06.)**
    → clear_current_tenant() (M-4 Hygiene, finally) → optional SocketIO 'score_ready'
_periodic_tick (slow_lane.py): **Plan 04 fasst es NICHT an (H-2 gestrichen, FOLD 26.06.) — es trägt nur Plan-03-H-3 (_requeue_pending).**

## 3. Cross-File-Awareness
`grep -rn "_calc_process_score\|_calc_call_score\|_apply_outcome_modifier\|_calc_coaching_score\|_OUTCOME_MODIFIERS\|_calcScore" routes/ services/ static/`: app_routes.py (Def :743-839 + Aufrufe :659 Stash-WRITE / :1111 Trend / :1992-2013 correct_outcome Stash-READ-Block), dashboard.py (:724/:774/:795), pip-launcher.js (:3183). **ALLE BLEIBEN unverändert (Re-Scope — Löschung/Umstellung = SCORE-UI).** Plan 04 fügt nur additiv hinzu: api_beenden→slow_lane.put, dashboard→rubric_preview+outcome_confirmed, slow_lane→Merge.

## 4. Edge-Case-Analyse + Race-Condition-Audit (Punkt 14, D-10 sync→async + F-02)
1. **Vorzeitiger Merge (F-02, HIGH):** Slow-Lane benotet Events live aus der Queue → "keine offenen Events" wird transient WAHR mitten im laufenden Call. **Fix:** Merge-Vorbedingung call_status=='ended' (aus /api/beenden) UND pending_events==0. Der Merge feuert NICHT bevor der Call beendet ist. Trigger-Punkt: nach jedem _persist_event_ref UND auf den api_beenden-Anstoß (`{'call_id':...}`) prüfen "ist Call ended + 0 pending?" → erst dann mergen.
2. **Poison-Pill / Abstention zählt als offen (F-01/F-05):** pending_events zählt NUR handling_status='pending'. failed/abstained/scored = terminal → blockieren den Merge NICHT. Call mit ≥1 failed wird mit Marker "N Events nicht bewertbar" benotet.
3. **Verbindungsabbruch:** Scoring-Job crasht → rubric_score-Zeile fehlt → re-derivierbar (Call ended + rubric_score fehlt = offener Merge, via Re-Queue/Retry). Kein halb-fertiger Score.
4. **Doppeltes api_beenden / Doppelklick:** idempotenter UPSERT keyed call_id (ON CONFLICT partieller Index, F-03) → genau 1 Live-rubric_score-Zeile.
5. **Tab-Hintergrund:** Score kommt async; Frontend pollt/SocketIO 'score_ready' (999.2-Detail); Interim "wird ausgewertet…" statt stale Zahl.
6. **M-4 GUC-Leere + GUC-Leak im Daemon (HÖCHSTES STILLES RISIKO):** (a) Daemon-Thread ohne Request-Context → app.tenant_id-GUC leer → FORCE-RLS lehnt rubric_score-INSERT fail-closed ab → die Note ewig NULL, lautlos. (b) **GUC-Leak (Gemini-Gegencheck):** wird clear_current_tenant() nach einer Exception in compute_rubric/commit übersprungen, behält der Endlos-Daemon-Thread die tenant_id für die nächste Iteration (anderer Call/Tenant) → Cross-Tenant-Write. **Fix:** set_current_tenant(str(call.tenant_id)) vor JEDEM Write (Merge + not_gradable-Write); **clear_current_tenant() ZWINGEND im `finally`** (läuft auf jedem Pfad). call.tenant_id zuerst lesen (calls nicht FORCE-RLS). call.tenant_id NULL → loggen, kein stiller Abbruch.
7. **outcome nie bestätigt (FOLD 26.06. — KEIN Hang mehr):** der Merge wartet NICHT aufs outcome (F-09 gestrichen) → die rubric_score-Zeile wird trotzdem geschrieben (ended + 0 pending). Folge ist KEIN „wird ausgewertet…"-Hang, sondern nur die **ANZEIGE-Sperre**: das Vorschau-Panel zeigt „Ergebnis bestätigen, dann erscheint die Note", bis `calls.outcome` gesetzt ist. Kein Sweep, kein Daemon-outcome-Write, keine Request-Context-Falle.
8. **H-1 halb-gelöschter Stash-Pfad:** MOOT — es wird nichts gelöscht (Re-Scope), kein NameError-Risiko.

<tasks>

<task type="auto">
  <name>Task 1: Call-Ende-Merge in Slow Lane — HARTE Vorbedingung (F-02: ended + 0 pending, KEIN outcome) + Audio-Gate D-09 (F-07-Config, high-conf-Zählung aus events) + idempotenter UPSERT rubric_score (F-03) — Option B: EINZIGER Engine-Write ist rubric_score (KEIN calls.coaching_score, KEIN outcome), async D-10. KEIN H-2-Sweep.</name>
  <files>services/slow_lane.py, config.py</files>
  <read_first>
    - services/slow_lane.py (Consumer + _persist_event_ref aus Plan 03, handling_status-Statemachine; 'failed'-Setzpunkte :236/:248/:267 = terminal)
    - services/rubric_engine.py (compute_rubric, Plan 02 — ergebnis-blind :30-31, Return-Dict :71-80 OHNE n_high_confidence_events, status :78) + services/live_session.py get_speech_stats
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §F (Audio-Gate 0.0-1.0) + §C (Merge)
    - ssh ... 'bash scripts/inspect.sh schema calls' (coaching_score/audio_health_score + ended_at/status = Call-Ende-Signal F-02; outcome NUR für die Anzeige-Sperre, NICHT Merge)
    - config.py (bestehende Config-Defaults als Muster, Punkt 12)
  </read_first>
  <action>
    **Hook-Registry-Nutzung (FOLD 23.06., Plan 03 Task 5):** Plan 04 baut den Call-Ende-Merge-GATE (harte Vorbedingung ended + 0 pending) + die M-4-GUC-Klammer (set/clear_current_tenant try/finally) und ruft INNERHALB der GUC-Klammer `run_call_end_steps(ctx)` (aus Plan 03). Den eigenen compute_rubric→rubric_score-Schritt (Option B: NUR rubric_score) registriert Plan 04 via `register_call_end_step(...)` — Plan 06/07 registrieren später EBENSO. **Plan 04 hängt NICHTS mehr in `_periodic_tick` ein (H-2 gestrichen, FOLD 26.06.).** Minimal halten — die Registry existiert schon (Plan 03).

    **F-07 Config-Defaults (config.py):** `AUDIO_HEALTH_GATE_THRESHOLD = float(os.getenv('AUDIO_HEALTH_GATE_THRESHOLD', '0.5'))` (0.0-1.0-Skala) + `MIN_HIGH_CONFIDENCE_EVENTS = int(os.getenv('MIN_HIGH_CONFIDENCE_EVENTS', '3'))` (D-09 "zu wenig hoch-konfidente Events" = < 3 Events über Tor-1-Schwelle) + **`SCORE_MAX_RETRIES = int(os.getenv('SCORE_MAX_RETRIES', '3'))`** (Merge-Job-Retry-Cap → dann dead-letter). Kommentar: post-launch tunbar (Punkt 12). **KEIN `SCORE_SWEEP_AFTER_S` (H-2 gestrichen, FOLD 26.06.).**

    Call-Ende-Merge in slow_lane.py — **F-02 HARTE Vorbedingung (KRITISCH), KEINE outcome-Bedingung (F-09 gestrichen):**
    Der Merge feuert NUR wenn BEIDE gelten:
    - `call_status == 'ended'` — der Call ist via /api/beenden beendet (Feld beim Execute bestätigt: ended_at IS NOT NULL bzw. status). KEIN Merge solange der Call läuft.
    - `pending_events(call_id) == 0` — `SELECT COUNT(*) FROM intent_event WHERE call_id=X AND handling_status='pending'` == 0. **NUR 'pending' zählt** — `scored`/`failed`/`abstained` sind terminal (F-01/F-05; 'failed' verifiziert :236/:248/:267), zählen NICHT als offen.
    **KEIN `calls.outcome IS NOT NULL` (F-09 GESTRICHEN, FOLD 26.06.):** die Engine ist ergebnis-blind (rubric_engine.py:30-31) → compute_rubric braucht outcome nicht → keine D-08-False-Green-Gefahr durch frühen Merge. Der „nachdem"-Wunsch wird als ANZEIGE-Sperre (Task 3) gelöst, NICHT als Merge-Gate.
    Trigger-Punkt (FOLD 26.06.): die Vorbedingung wird nach jedem _persist_event_ref (letzte Event-Note drainiert pending auf 0) UND auf den **api_beenden-Anstoß** (`{'call_id':...}` aus der Queue) geprüft → erst bei Erfüllung beider mergen. **`correct_outcome` stößt NICHTS an (entkoppelt).** Im SUMMARY den Anstoß-Datenfluss belegen (api_beenden → put → Consumer-Merge bei ended+0pending).
    **Queue-Timing explizit (F-02):** Events gehen bei EMIT (live) in die Queue + werden live benotet (handling_score, Plan 03); der MERGE wird erst durch ended (api_beenden-Anstoß) freigeschaltet. INTERLOCK TAXO1-Plan-02 — falls TAXO1 das Timing anders fixt, im 3-Wege-Check abgleichen (SUMMARY-Notiz).

    Bei erfüllter Vorbedingung:
    1. **Audio-Gate D-09 (VOR Scoring, F-07):** lade call + die zugehörigen events (intent_event-Rows des Calls). Wenn `audio_health_score < AUDIO_HEALTH_GATE_THRESHOLD` (0.0-1.0!) ODER `audio_health_score IS NULL` (kein Buffer) ODER **`n_high_conf < MIN_HIGH_CONFIDENCE_EVENTS`, wobei `n_high_conf` SELBST aus der events-Liste gezählt wird** (`sum(1 for e in events if e.confidence is not None and e.confidence >= TOR1_SCHWELLE)` — die Tor-1-Konfidenzschwelle aus dem bestehenden Slow-Lane-`_confidence_gate_for`/Config; **NICHT aus einem compute_rubric-Rückgabefeld — `n_high_confidence_events` existiert dort NICHT, FOLD 26.06.**) → Call "nicht bewertbar": eine **rubric_score-Zeile mit `status='not_gradable'`, `coaching_score=NULL`, reason ("poor_audio_health" bzw. "too_few_high_confidence_events") in dimensions/payload** schreiben (Option B: in rubric_score, NICHT in calls). SPEICHERN+FLAGGEN, nicht verwerfen. **(Auch dieser not_gradable-Write geht unter Tenant-GUC — M-4 gilt für JEDEN rubric_score-Write.)**
    2. Sonst: events + speech_stats (get_speech_stats per-SID, K1, Req 9) + call + mode_config (Lookup-Schlüssel = `calls.call_mode`, N-4) laden → `compute_rubric(...)`.
    3. **M-4 Tenant-GUC SETZEN + ZWINGENDES try/finally (KRITISCH gegen Cross-Tenant-Leak):** Lies zuerst `call.tenant_id` (calls ist nicht FORCE-RLS → Lesen ohne GUC ok). Dann `set_current_tenant(str(call.tenant_id))` (database/db.py:43) — der after_begin-Hook setzt daraus transaction-local `app.tenant_id`, sodass FORCE-RLS WITH CHECK den Daemon-INSERT DURCHLÄSST. **Ohne diesen Schritt: rubric_score-INSERT fail-closed abgelehnt → die neue Note fehlt ewig, das Vorschau-Panel bleibt leer, lautlos (M-4).** **MUSTER-PFLICHT — `clear_current_tenant()` gehört in den `finally`-Block:** der Slow-Lane-Consumer ist ein ENDLOS-Daemon-Thread → ein nach Exception NICHT geräumter Contextvar leakt in die NÄCHSTE Iteration → der nächste Call (evtl. anderer Tenant) schreibt unter STALE tenant_id → **Cross-Tenant-Schreib-Risiko**. Skelett:
       ```
       set_current_tenant(str(call.tenant_id))   # vor Txn-Beginn (after_begin liest den Contextvar)
       try:
           # Schritt 4: UPSERT rubric_score (volle Aufschluesselung + status) + commit
           #            (Option B: KEIN calls.coaching_score-Write; KEIN outcome in rubric_score)
       finally:
           clear_current_tenant()                 # LÄUFT IMMER — auch bei Exception
       ```
       Reihenfolge: set_current_tenant MUSS vor dem Transaktions-Beginn des Writes stehen. Wenn `call.tenant_id` NULL ist (Alt-Call ohne Tenant) → loggen + behandeln (kein stiller Abbruch). Gilt identisch für den not_gradable-Write (Schritt 1).
       **M-4-VERSCHÄRFUNG + MAX-RETRY/DEAD-LETTER (FOLD 23.06., Gemini — KEIN Silent-Drop, ABER auch kein Endlos-Block):** schlägt der Merge fehl (IntegrityError/permission-denied/RLS-Ablehnung/compute_rubric-Fehler) → der Call wird NICHT als 'erledigt' markiert, sondern:
       - **GEDECKELTER Re-Queue:** der Call-Ende-Job trägt einen Versuchs-Zähler (`{'call_id':..., 'attempts': N}`). Bei Fehler: LAUT loggen (`[SLOW] merge failed call=<id> attempt=N/{MAX}: <err>`) → `slow_lane.put({'call_id':..., 'attempts': N+1})`. **Cap: `SCORE_MAX_RETRIES` (Default 3).**
       - **DEAD-LETTER nach Cap:** `attempts >= SCORE_MAX_RETRIES` → KEIN weiterer Re-Queue + LAUT als dead-letter loggen/alerten (`[SLOW] DEAD-LETTER call=<id> after {N} attempts: <err>`). Best-effort: rubric_score-Zeile mit `status='failed'` (falls der Write geht). So blockiert ein Poison-Call die Queue NICHT ewig, geht aber auch NICHT still verloren.
       - **DAEMON ÜBERLEBT (Call-Isolierung, KRITISCH):** der Fehler failt NUR DIESEN Call. Pro-Call try/except auf Consumer-Ebene fängt die propagierte Exception → re-queue-mit-Cap → `continue`. **Der Daemon-Thread darf NIE sterben.** Innerhalb EINES Calls all-or-nothing (UPSERTs idempotent → sauberer Re-Run).
       Tests: `test_daemon_write_failure_requeues_with_cap` + `test_daemon_survives_step_exception` + `test_daemon_write_failure_not_marked_done`.
    4. (im try) **UPSERT rubric_score-Zeile (F-03 idempotent) — DER EINZIGE Engine-Write (Option B):** `INSERT ... ON CONFLICT (call_id) WHERE origin='live' DO UPDATE SET ...` — referenziert ux_rubric_score_live_call_id (Plan 01). Volle Aufschlüsselung: coaching_score (kann NULL bei <50%), dimensions, is_provisional, measured_weight_pct, unmeasured_dimensions, status + tenant_id aus calls.tenant_id (D-11) + session_mode = aus calls.call_mode abgeleitet (N-4). **KEIN outcome-Feld (FOLD 26.06. — outcome bleibt in calls).** Bei ≥1 failed-Event: Marker "N Events nicht bewertbar" (payload/status, F-05). commit.
    **Option B: KEIN `UPDATE calls.coaching_score`.** Die neue Note lebt in TAXO ausschließlich in rubric_score (Vorschau-Panel liest sie). Der calls.coaching_score-Spiegel wandert nach **08.23.2.SCORE-UI**.
    6. **`clear_current_tenant()` im `finally` (M-4 Hygiene, IMMER).**
    7. Optional SocketIO 'score_ready' room-targeted (D-10 Pending→ready; Display-Mechanik = 999.2, hier nur Event) — NACH dem finally.
    Alles async im Slow-Lane-Consumer (D-10 — NIE synchron im Call-Ende).

    **KEIN H-2-Sweep (FOLD 26.06., GESTRICHEN):** Plan 04 baut KEINEN `_periodic_tick`-Sweep, KEINE `SCORE_SWEEP_AFTER_S`-Frist, KEINE `calls.outcome='unknown'`-Daemon-Schreibung. Begründung: der Merge wartet nicht mehr aufs outcome (F-09 gestrichen) → es gibt keinen outcome-Hang aufzulösen. `_periodic_tick` bleibt unangetastet (trägt nur Plan-03-H-3 `_requeue_pending`).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_coaching_score_cutover.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - **F-02:** Merge-Vorbedingung im Code: `grep -nE "ended|pending_events|handling_status *== *'pending'" services/slow_lane.py` belegt call_status=='ended' UND pending_events==0; Test belegt: Merge feuert NICHT solange call nicht ended (transient-WAHR-Schutz)
    - **F-09 GESTRICHEN (FOLD 26.06.):** `grep -niE "outcome IS NOT NULL|outcome is not None|\.outcome" services/slow_lane.py` zeigt KEINE outcome-Vorbedingung im Merge (calls.outcome ist NICHT Teil des Merge-Gates). Anstoß sitzt in api_beenden, NICHT correct_outcome: `grep -A40 "def api_beenden\|def beenden" routes/app_routes.py` zeigt `slow_lane.put`; `grep -A45 "def correct_outcome" routes/app_routes.py` zeigt KEINEN `slow_lane.put` (entkoppelt).
    - **F-01/F-05:** pending_events zählt NUR handling_status='pending' (`grep -n "handling_status" services/slow_lane.py` zeigt Filter auf 'pending'); failed/abstained/scored zählen nicht — Test: Call mit 1 abstained + 1 failed + Rest scored → Merge feuert
    - **F-07 + high-conf-Zählung aus events (FOLD 26.06.):** `grep -E "AUDIO_HEALTH_GATE_THRESHOLD|MIN_HIGH_CONFIDENCE_EVENTS" config.py` == 2 (Defaults 0.5 / 3); das Gate zählt die hoch-konfidenten Events aus der events-Liste (`grep -nE "confidence" services/slow_lane.py` im Gate-Pfad), NICHT aus einem compute_rubric-Feld — `grep -n "n_high_confidence_events" services/slow_lane.py` == 0 (das Engine-Dict hat das Feld nicht). Test: ein Call mit < 3 hoch-konfidenten Events → not_gradable.
    - **F-03:** `grep -iE "ON CONFLICT.*call_id.*origin.*live|on_conflict" services/slow_lane.py` belegt UPSERT auf dem partiellen Index
    - **M-4 (KRITISCH):** `grep -nE "set_current_tenant|clear_current_tenant" services/slow_lane.py` >= 2; `clear_current_tenant()` steht im `finally`-Block (`grep -nB6 "clear_current_tenant" services/slow_lane.py` zeigt ein `finally:` direkt darüber). Test `test_daemon_write_sets_tenant_guc` (Spy: set_current_tenant(call.tenant_id) VOR dem Write) + Integrations-Test (Postgres): Write OHNE GUC fail-closed, MIT GUC ok. **Leak-Test `test_daemon_clears_tenant_on_exception`: Write wirft → Contextvar danach GELEERT.** Test-Anruf-Beleg: `inspect.sh sample rubric_score` zeigt eine Zeile MIT tenant_id + gesetztem coaching_score-Feld (GUC greift live). calls.coaching_score NICHT geprüft (Option B).
    - **KEIN outcome in rubric_score (FOLD 26.06.):** der UPSERT setzt KEIN outcome-Feld — `grep -niE "outcome" services/slow_lane.py` zeigt keinen rubric_score-outcome-Write (outcome bleibt in calls).
    - Audio-Gate D-09: audio_health_score < Schwelle ODER IS NULL ODER < N high-conf → **rubric_score-Zeile mit status='not_gradable' + coaching_score=NULL** (0.0-1.0-Skala; Option B: in rubric_score, NICHT calls)
    - speech_stats kommt aus get_speech_stats(sid) per-SID (Req 9): `grep "get_speech_stats" services/slow_lane.py` >= 1
    - **Robustheit (FOLD 23.06.): `SCORE_MAX_RETRIES` in config.py; test_daemon_write_failure_requeues_with_cap (Fehler → attempts+1 bis Cap, dann dead-letter + `[SLOW] DEAD-LETTER`-Log) + test_daemon_survives_step_exception + test_daemon_write_failure_not_marked_done grün. KEIN Silent-Drop, KEIN Endlos-Block, Daemon stirbt nie.**
    - **KEIN H-2-Sweep (FOLD 26.06.): `grep -nE "SCORE_SWEEP_AFTER_S|outcome *= *'unknown'|outcome=\"unknown\"" config.py services/slow_lane.py` == 0; `_periodic_tick` von Plan 04 NICHT angefasst (nur Plan-03-H-3 darin). `grep -nE "\.outcome *=|UPDATE calls" services/slow_lane.py` == 0 (calls read-only in Plan 04).**
    - **Option B: `grep -n "coaching_score" services/slow_lane.py` zeigt KEIN `calls`-Objekt-Attribut-Write (kein `<call>.coaching_score =` / kein `UPDATE calls SET coaching_score`).** SUMMARY: Grep-Beleg (0 Anzeige-Leser von calls.coaching_score) + Spiegel-Verschiebung nach SCORE-UI.
  </acceptance_criteria>
  <done>Call-Ende-Merge mit harter Vorbedingung (F-02: ended + 0 pending, KEIN outcome), Audio-Gate (F-07, high-conf aus events), idempotenter UPSERT (F-03, KEIN outcome), failed/abstained-aware (F-01/F-05), M-4-GUC + Hard-Crash-bei-RLS-Fehler, async; via Hook-Registry eingehängt; KEIN H-2-Sweep.</done>
</task>

<task type="auto">
  <name>Task 2 (RE-SCOPED FOLD 23.06. + FOLD 26.06.): Alte Formel + Anzeige UNVERÄNDERT — api_beenden setzt Call-ended-Signal (F-02) UND stößt async die Engine an (slow_lane.put); correct_outcome bleibt UNVERÄNDERT (entkoppelt). KEIN Löschen, KEIN Umschalten.</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py:743-839 (alle 5 alten Funktionen — BLEIBEN) + :659 (Stash-WRITE) + :1111 (Trend) + :1992-2013 (correct_outcome Stash-READ-Block — BLEIBT VERBATIM)
    - routes/app_routes.py: api_beenden (:98ff bzw. die /api/beenden-Route — beim Execute Zeile bestätigen)
    - Task 1 (Slow-Lane-Merge-Vorbedingung) + services/slow_lane.py (slow_lane.put)
    - ssh ... 'bash scripts/inspect.sh schema calls' (welches Feld markiert ended — F-02)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §A2
  </read_first>
  <action>
    **RE-SCOPE: NICHTS LÖSCHEN, NICHTS UMSCHALTEN.** Die 5 alten Funktionen (`_calc_call_score` :743, `_OUTCOME_MODIFIERS` :757, `_calc_process_score` :768 inkl. frage_qualitaet=0.0, `_apply_outcome_modifier` :806, `_calc_coaching_score` :829) BLEIBEN STEHEN. Der heutige Stash-Write (api_beenden:659) + Stash-Read-Block (correct_outcome:1992-2013) + die alte sync `row.coaching_score=...`-Schreibung BLEIBEN UNVERÄNDERT. **H-1 hinfällig** (nichts gelöscht).
    Nur ZWEI additive Änderungen — beide in **api_beenden** (FOLD 26.06.: der Anstoß wandert hierher):
    1. **F-02 Call-ended-Signal:** api_beenden markiert den Call als beendet (ended_at/status — das Call-Ende-Signal für den Merge), falls das Feld nicht ohnehin schon gesetzt wird. Beim Execute via `inspect.sh schema calls` bestätigen, welches Feld 'ended' markiert.
    2. **F-02/FOLD-26.06. async Engine-Anstoß in api_beenden (ADDITIV):** NACHDEM api_beenden den Call beendet → ZUSÄTZLICH `slow_lane.put({'call_id': ...})` (Scoring-Anstoß). Der Consumer prüft dann ended + 0 pending und mergt (KEINE outcome-Bedingung — F-09 gestrichen). Die bestehende Response/das bestehende Verhalten von api_beenden NICHT ändern.
    **correct_outcome bleibt VOLLSTÄNDIG UNVERÄNDERT (FOLD 26.06., Entkopplung):** KEIN neuer `slow_lane.put`, kein Umbau. Es behält seine sync-Anzeige-Logik (inkl. `row.coaching_score=…` :2011) wie heute — die alte Anzeige läuft exakt wie bisher. (Der Scoring-Anstoß ist NICHT mehr an die Outcome-Bestätigung gekoppelt; outcome ist nur noch die ANZEIGE-Sperre in Task 3.)
    KEINE Löschung von _calcScore (Client) in dieser Welle (SCORE-UI). Bestehende Anzeige + Interim-Verhalten unverändert.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "slow_lane.put\|_calc_call_score"'</automated>
  </verify>
  <acceptance_criteria>
    - **RE-SCOPE: die 5 alten Funktionen BLEIBEN — `grep -E "_calc_process_score|_calc_call_score|_apply_outcome_modifier|_calc_coaching_score|_OUTCOME_MODIFIERS" routes/app_routes.py` >= die heutigen Treffer (NICHT 0); frage_qualitaet=0.0 bleibt (→ SCORE-UI)**
    - **api_beenden stößt async an (FOLD 26.06.): `grep -A30 "def api_beenden\|def beenden" routes/app_routes.py` zeigt das ended-Signal UND `slow_lane.put`**
    - **correct_outcome UNVERÄNDERT: `grep -A45 "def correct_outcome" routes/app_routes.py` zeigt KEINEN `slow_lane.put` (entkoppelt) — die alte `.coaching_score =`-Schreibung bleibt unangetastet**
    - KEINE Anzeige-Leser umgestellt, KEIN _calcScore gelöscht (das ist SCORE-UI)
    - Option B: correct_outcome behält die alte `.coaching_score =`-Schreibung; die Engine schreibt NICHT in calls.coaching_score (kein zweiter Schreiber). SUMMARY: grep-Beleg + Spiegel-Verschiebung nach SCORE-UI.
  </acceptance_criteria>
  <done>Alte Formel + Anzeige unverändert; api_beenden setzt Call-ended-Signal (F-02) UND stößt async die Engine an (FOLD 26.06.); correct_outcome unverändert/entkoppelt; kein Löschen/Umschalten.</done>
</task>

<task type="auto">
  <name>Task 3 (RE-SCOPED FOLD 23.06. + FOLD 26.06.): „Neu — Vorschau"-Panel in session_detail (read-only, roh, gestempelt) + ANZEIGE-Sperre auf calls.outcome + NULL/not_gradable/failed-Edge IM PANEL. Alte Anzeige + Leser UNANGETASTET.</name>
  <files>routes/dashboard.py, templates/ (session_detail-Template)</files>
  <read_first>
    - routes/dashboard.py:720-795 (session_detail — Kontext-Aufbau; das alte Score-Hero via _calc_call_score BLEIBT, NICHT anfassen)
    - `grep -n "render_template" routes/dashboard.py` → exakter Template-Name von session_detail (beim Execute bestätigen)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-01-rubric-score-tabelle-PLAN.md (rubric_score-Spalten: coaching_score, dimensions JSON, measured_weight_pct, unmeasured_dimensions, is_provisional, status)
    - ssh ... 'bash scripts/inspect.sh schema calls' (outcome-Feld für die Anzeige-Sperre)
    - static/nerve.css (existierende .n-*-Klassen/CSS-Variablen — KEINE hardcoded Farben, CLAUDE.md-Regel)
  </read_first>
  <action>
    **Ziel (Design-Brief Abschnitt 6):** ein rohes, klar als „Neu — Vorschau" gestempeltes, READ-ONLY Panel NEBEN der bestehenden session_detail-Anzeige. Zweck = die ganze Kette (Rechnen→Speichern→RLS→Lesen→Anzeigen) an echten Calls EINMAL verifizieren. Bewusst ungestylt.
    1. **dashboard.py session_detail — ADDITIV:** die zum Call gehörende `rubric_score`-Zeile (origin='live', via call_id/conversation_log_id) lesen und als `rubric_preview` ans Template geben; **zusätzlich `outcome_confirmed = (call.outcome is not None)` ans Template geben (FOLD 26.06., ANZEIGE-Sperre, Join auf dieselbe calls-Zeile).** Die bestehende Score-/Anzeige-Logik (_calc_call_score-Recompute :774, Trend :795, Import :724) NICHT anfassen.
    2. **Template — neues Panel** (eigener Block, NEBEN/unter dem bestehenden): Überschrift mit Stempel **„🧪 Neu — Vorschau (Beta)"** + Mini-Hinweis „neues Scoring, weicht bewusst von der alten Zahl ab". **ANZEIGE-Sperre (FOLD 26.06., André 'nachdem'-Wunsch):** ist `outcome_confirmed` falsch (calls.outcome NULL) → das Panel zeigt **„Ergebnis bestätigen, dann erscheint die Note"** STATT der Note (auch wenn die rubric_score-Zeile schon existiert). Erst wenn `outcome_confirmed` wahr → rendert es read-only aus `rubric_preview`:
       - die neue `coaching_score` (Kopf-Zahl) — **Quelle = `rubric_preview.coaching_score`, NICHT `calls.coaching_score`** (Option B) — bei NULL/not_gradable: Klartext-Hinweis statt Zahl.
       - die 7 Dimensionen aus `rubric_preview.dimensions` (JSON): pro Dimension Name + Wert + Gewicht + „messbar? ja/nein".
       - die „nicht messbar weil…"-Gründe aus `unmeasured_dimensions` (+ is_provisional/measured_weight_pct als „vorläufig, X% Gewicht messbar").
    3. **PANEL-EDGE-PRÜFREIHENFOLGE (Req 6 + FOLD 23.06./26.06.):** **(0) ANZEIGE-Sperre zuerst: `outcome_confirmed` falsch → „Ergebnis bestätigen, dann erscheint die Note" (FOLD 26.06.).** Sonst, in dieser Reihenfolge: **(a) status='failed'** (Dead-Letter) → „Berechnung fehlgeschlagen (Admin benachrichtigt)". **(b) status='not_gradable'** → „Audio zu schlecht zum Bewerten" bzw. „zu wenig auswertbare Momente". **(c) coaching_score NULL** (<50%) → „nicht genug Daten". **(d) ROW-ABSENT** (rubric_preview None — async-Merge noch nicht durch) → „Score wird im Hintergrund berechnet…". **(e)** sonst die Score + Dimensionen. **Alle Fälle explizit abfangen — KEIN Crash, KEINE leere 0, kein .toFixed/Division auf None/NULL.**
    4. **Styling minimal:** nur falls nötig eine schlichte `.n-*`-Klasse aus nerve.css (CSS-Variablen, KEINE hardcoded Farben — CLAUDE.md HART). Der „Vorschau"-Stempel muss eindeutig signalisieren: das ist NICHT die echte Note.
    **NICHT in dieser Welle:** kein _calcScore-Löschen, keine pip-launcher/performance/dashboard-Reader-Umstellung, kein Redesign — alles SCORE-UI.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "rubric_preview"'</automated>
  </verify>
  <acceptance_criteria>
    - session_detail gibt `rubric_preview` (rubric_score-Zeile) UND `outcome_confirmed` (calls.outcome IS NOT NULL) ADDITIV ans Template; `grep -nE "rubric_preview|outcome_confirmed" routes/dashboard.py` >= 2
    - **alte Anzeige unangetastet: `grep -n "_calc_call_score" routes/dashboard.py` >= die heutigen Treffer (NICHT entfernt); `grep "function _calcScore" static/pip-launcher.js` == heutiger Stand (NICHT gelöscht)**
    - Template hat einen klar gestempelten „Neu — Vorschau"-Block, read-only, mit ANZEIGE-Sperre (outcome NULL → „Ergebnis bestätigen, dann erscheint die Note") und rendert sonst coaching_score + 7 Dimensionen + „nicht messbar weil…"-Gründe
    - Edge-Reihenfolge im Panel: Sperre → failed → not_gradable → NULL → row-absent → Score; jeder Fall Klartext, kein Crash (Test-Anruf belegt)
    - kein hardcoded Farb-Hex im neuen Markup (CLAUDE.md): `grep -nE "#[0-9a-fA-F]{6}" <neuer Template-Block>` == 0
  </acceptance_criteria>
  <done>„Neu — Vorschau"-Panel zeigt die neue Note + Dimensionen read-only neben der unveränderten alten Anzeige; ANZEIGE-Sperre auf calls.outcome (Note erst nach Bestätigung); NULL/not_gradable/failed/row-absent abgefangen; nichts Bestehendes umgestellt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>RE-SCOPED (FOLD 23.06. + 26.06.) + Option B: Engine rechnet die neue Note async + speichert sie NUR in rubric_score (KEIN calls.coaching_score, KEIN outcome) — Call-Ende-Merge mit harter Vorbedingung (F-02: ended + 0 pending, KEIN outcome; F-09 gestrichen), Audio-Gate D-09 (F-07, high-conf aus events), idempotenter UPSERT (F-03), M-4-GUC + Hard-Crash-bei-RLS-Fehler. KEIN H-2-Sweep. Alte Formel + Anzeige + calls.coaching_score-Schreiber UNVERÄNDERT; neues „Neu — Vorschau"-Panel mit ANZEIGE-Sperre auf calls.outcome. KEIN Display-Cutover (→ SCORE-UI).</what-built>
  <how-to-verify>
    Nach `bash deploy.sh production`:
    1. **Normaler Test-Anruf** (Cold-Call mit ≥1 Einwand, gutes Audio) als Test-User → Call beenden → Outcome bestätigen. Erwartung: die ALTE Anzeige bleibt exakt wie heute. Claudian: `inspect.sh sample rubric_score` zeigt EINE neue Zeile (origin='live') mit Aufschlüsselung; das **„Neu — Vorschau"-Panel** zeigt NACH der Outcome-Bestätigung die neue Note + die 7 Dimensionen + „nicht messbar weil…"-Gründe (DATEN-FLUSS durch die ganze Kette — der Kern dieser Welle).
    2. **F-02 vorzeitiger-Merge-Schutz:** Während eines laufenden Calls (BEVOR /api/beenden) → `inspect.sh sample rubric_score` zeigt KEINE Zeile für diesen Call (Merge feuert erst nach ended).
    2b. **ANZEIGE-Sperre (FOLD 26.06., F-09-Ersatz):** Call beenden (api_beenden), aber Outcome NOCH NICHT bestätigen → `inspect.sh sample rubric_score` zeigt für diesen Call BEREITS eine Zeile (der Merge feuert auf ended+0pending, ergebnis-blind), ABER das Vorschau-Panel zeigt **„Ergebnis bestätigen, dann erscheint die Note"** (Note versteckt). Erst NACH Outcome-Bestätigung rendert das Panel die Note (rubric_score-Zeile war schon da). Beleg: die Note ist FRÜH berechnet, aber erst nach Bestätigung sichtbar.
    3. **Re-Scope-Beleg — alte Anzeige unverändert:** session_detail-Score-Hero + Dashboard + Performance zeigen GENAU wie vor dem Deploy — NUR das gestempelte Vorschau-Panel ist neu. (Ändert sich eine ALTE Anzeige → Engine hat fälschlich calls.coaching_score geschrieben (Option-B-Verstoß) → BLOCK. `grep -n "coaching_score" services/slow_lane.py` zeigt KEINEN calls-Write.)
    4. **<50%-Szenario** (sehr kurzer Cold-Call) → coaching_score=NULL → das **Vorschau-Panel** zeigt „nicht genug Daten" OHNE Crash/leere 0 (alte Anzeige unberührt; Outcome bestätigt, damit die Sperre nicht greift).
    5. **Poor-Audio-Szenario** (D-09: schlechtes/leeres Audio ODER < 3 hoch-konfidente Events, F-07 — high-conf aus events gezählt) → Call gespeichert, rubric_score.status='not_gradable', das **Vorschau-Panel** zeigt „Audio zu schlecht zum Bewerten" bzw. „zu wenig auswertbare Momente".
    5b. **Dauer-Korrektheit (FOLD 23.06., Gemini-Blindfleck):** ein Test-Call mit langem Schweigen am Ende → die angezeigte Call-Dauer (`dauer_sek`) entspricht der echten Gesprächsdauer (am /api/beenden festgehalten), NICHT der Zeit bis zum async-Merge. Claudian belegt: Dauer nicht aufgebläht.
    6. **M-4 GUC live (HÖCHSTES STILLES RISIKO):** beim normalen Test-Anruf prüft Claudian `inspect.sh sample rubric_score` — die Zeile MUSS `tenant_id` + ein gesetztes `coaching_score`-Feld haben. Wenn KEINE rubric_score-Zeile existiert (Vorschau-Panel bleibt auch nach Outcome-Bestätigung leer / „wird im Hintergrund berechnet") → M-4 durchgeschlagen (GUC nicht gesetzt) → BLOCK. `inspect.sh logs-errors` auf RLS/permission-denied + `[SLOW] FATAL`-Log prüfen. (`calls.coaching_score` NICHT geprüft — Option B.)
    7. **KEIN H-2-Sweep / KEIN outcome-Hang (FOLD 26.06.):** einen Call beenden und das Outcome bewusst NIE bestätigen → `inspect.sh sample calls` zeigt für den Call `outcome` weiterhin NULL (KEIN automatisches `outcome='unknown'` — der Sweep ist gestrichen); `inspect.sh sample rubric_score` zeigt für den Call TROTZDEM eine Zeile (Merge ergebnis-blind). Das Vorschau-Panel bleibt für ihn auf „Ergebnis bestätigen, dann erscheint die Note". `inspect.sh logs` zeigt KEINEN Sweep-Lauf, KEINEN `RuntimeError: working outside of request context`, KEIN `outcome='unknown'`.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe Abweichungen (welcher Screen, welches Szenario).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| api_beenden (Client) → Scoring-Anstoß | User beendet Call → async Job (FOLD 26.06.) |
| Slow-Lane-Merge → rubric_score | einziger Engine-Write der Note (Option B; KEIN calls.coaching_score, KEIN outcome) |
| laufender Call → Merge-Trigger | vorzeitiger Merge mitten im Call (F-02) |
| poor-audio STT → Score | halluzinierter selbstbewusster Müll-Score |
| calls.outcome → Vorschau-Panel | Anzeige-Sperre (Display-Gate, FOLD 26.06.) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-04-01 | I (False confidence) | poor-audio → halluzinierter Score | mitigate | D-09 Audio-Gate VOR Scoring (audio_health < Schwelle ODER NULL ODER < MIN_HIGH_CONFIDENCE_EVENTS → not_gradable, gespeichert+geflaggt, F-07). **High-conf-Events aus der events-Liste gezählt (FOLD 26.06.), nicht aus dem Engine-Dict.** |
| T-TAXO2-04-02 | D (DoS/Crash) | NULL coaching_score crasht das Vorschau-Panel | mitigate | NULL/not_gradable/failed/row-absent-Edge IM „Neu — Vorschau"-Panel ("nicht genug Daten"/"wird ausgewertet…"/"fehlgeschlagen" statt .toFixed(NULL)). Alte Screens unberührt. |
| T-TAXO2-04-03 | T (Double-write) | doppeltes api_beenden → 2 rubric_score-Zeilen | mitigate | **F-03 idempotenter UPSERT ON CONFLICT (call_id) WHERE origin='live'** → genau 1 Live-Zeile. |
| T-TAXO2-04-04 | I (Tenant leak) | rubric_score-Zeile ohne tenant_id | mitigate | tenant_id aus calls.tenant_id gestampt (D-11, RLS Plan 01). |
| T-TAXO2-04-05 | T (Premature merge) | Merge feuert mitten im Call → falscher Score | mitigate | **F-02 harte Vorbedingung: call_status=='ended' (aus /api/beenden) UND pending_events(handling_status='pending')==0. failed/abstained/scored terminal.** |
| T-TAXO2-04-06 | E | LLM rutscht in Live-Call-Ende (sync) | mitigate | D-10 async — Scoring NUR Slow Lane, api_beenden stößt nur an. grep: kein compute_rubric im Request-Sync-Pfad. |
| T-TAXO2-04-08 | D (Stille Verfügbarkeits-Regression / M-4, HÖCHSTES STILLES RISIKO) | Daemon-Write ohne Tenant-GUC → FORCE-RLS fail-closed → rubric_score-Note fehlt ewig (Vorschau leer), lautlos | mitigate | **M-4: set_current_tenant(str(call.tenant_id)) vor JEDEM rubric_score-Write, clear_current_tenant() im `finally`. M-4-Hardening: RLS-Fehler → Hard-Crash + lautes Log, Call NICHT als erledigt markiert (re-queue), kein Silent-Drop. Test-Anruf belegt: rubric_score-Zeile mit tenant_id + coaching_score-Feld.** |
| T-TAXO2-04-11 | I (Cross-Tenant-Write / M-4-Leak) | Exception in compute_rubric/commit überspringt clear_current_tenant → stale tenant_id leakt in nächste Daemon-Iteration | mitigate | **clear_current_tenant() ZWINGEND im `finally`-Block. Test test_daemon_clears_tenant_on_exception.** |
| T-TAXO2-04-13 | A (Daemon-Tod / Poison-Call) | ein Merge-Fehler reißt den Consumer ab; ODER Endlos-Re-Queue blockiert die Queue | mitigate | **pro-Call try/except (Daemon stirbt nie) + MAX-RETRY-Cap (SCORE_MAX_RETRIES) → dead-letter. Tests: test_daemon_survives_step_exception + test_daemon_write_failure_requeues_with_cap.** |
| T-TAXO2-04-14 | I (Verfrühte Anzeige / „nachdem"-Wunsch) | die neue Note erscheint, BEVOR der User das Ergebnis bestätigt hat (André will sie erst „nachdem") | mitigate | **ANZEIGE-Sperre (FOLD 26.06.): das Vorschau-Panel rendert die Note NUR bei `calls.outcome IS NOT NULL` (call_id-Join), sonst „Ergebnis bestätigen, dann erscheint die Note". Display-Gate, kein Merge-Gate — der Score wird früh berechnet, aber spät gezeigt.** |
| T-TAXO2-04-07 | — (F-09 entfällt) | (alt: Merge vor outcome → D-08-False-Green) | **MOOT (FOLD 26.06.)** | Die Engine ist ergebnis-blind (rubric_engine.py:30-31) → ein Merge ohne outcome erzeugt KEINE D-08-False-Green (die Note misst nur Verhalten). F-09-Vorbedingung gestrichen; outcome wird reine Anzeige-Sperre. |
| T-TAXO2-04-09 | — (H-2 entfällt) | (alt: outcome nie bestätigt → Hang „offen ohne Ergebnis") | **MOOT (FOLD 26.06.)** | Der Merge wartet NICHT mehr aufs outcome → es entsteht kein Hang. Der Sweep ist gestrichen. Festhängende Momente (Consumer-Restart) deckt Plan-03-H-3 ab. |
| T-TAXO2-04-12 | — (H-2 entfällt) | (alt: Sweep ruft Request-Pfad → RuntimeError tötet den Tick) | **MOOT (FOLD 26.06.)** | Kein Sweep mehr → kein Request-Context-Aufruf aus dem Daemon → keine Tick-Crash-Gefahr. |
| T-TAXO2-04-10 | D (Crash / H-1) | _calc_process_score gelöscht, Stash-Read-Fallback bleibt → NameError | **MOOT unter RE-SCOPE** | Es wird NICHTS gelöscht (alte Formel + Stash bleiben). Das Löschen + H-1-Disziplin → SCORE-UI. |
</threat_model>

<verification>
- Test-Anruf-DATEN-FLUSS (riskante Welle): „Neu — Vorschau"-Panel rendert Note + 7 Dimensionen NACH Outcome-Bestätigung; <50%→NULL ohne Crash; poor-audio/zu-wenig-Events→not_gradable (F-07). Alte Anzeige unverändert (Re-Scope).
- F-02: Merge feuert NICHT vor call ended; pending_events zählt nur 'pending' (scored/failed/abstained terminal). **KEINE outcome-Vorbedingung (F-09 gestrichen).**
- ANZEIGE-Sperre (FOLD 26.06.): Note erscheint erst bei calls.outcome NOT NULL; outcome NICHT in rubric_score dupliziert (Display-Gate via Join).
- F-03: idempotenter UPSERT ON CONFLICT auf partiellem Index (Doppelklick → 1 Zeile).
- RE-SCOPE: alte Formel + _calcScore + frage_qualitaet=0.0 BLEIBEN (NICHT gelöscht — SCORE-UI). api_beenden hat additiv slow_lane.put; correct_outcome UNVERÄNDERT (entkoppelt).
- M-4-Hardening: RLS-Write-Fehler → Hard-Crash + lautes Log, Call NICHT als erledigt markiert (re-queue), kein Silent-Drop (test_daemon_write_failure_not_marked_done).
- rubric_score-Zeile (origin='live') pro Test-Call (inspect.sh sample) → im Vorschau-Panel sichtbar (nach Outcome-Bestätigung).
- D-10: Engine-Merge async; Vorschau-Panel zeigt „wird ausgewertet…"/„Ergebnis bestätigen…" bis Note da + Outcome bestätigt.
- Req 9: speech_stats aus get_speech_stats per-SID verkabelt.
- F-07: AUDIO_HEALTH_GATE_THRESHOLD + MIN_HIGH_CONFIDENCE_EVENTS in config.py; high-conf-Zählung aus events-Liste (kein n_high_confidence_events aus dem Engine-Dict).
- M-4: set_current_tenant vor jedem rubric_score-Daemon-Write + clear_current_tenant im `finally`; test_daemon_clears_tenant_on_exception grün; Test-Anruf-rubric_score-Zeile trägt tenant_id + gesetztes coaching_score-Feld.
- **KEIN H-2-Sweep (FOLD 26.06.): kein SCORE_SWEEP_AFTER_S, kein _periodic_tick-Sweep, kein calls.outcome='unknown'-Write, calls read-only in slow_lane.py (grep == 0); _periodic_tick trägt nur Plan-03-H-3.**
- H-1: MOOT (nichts gelöscht).
- Hook-Registry (Plan 03): Plan 04 hängt compute_rubric-Schritt via register_call_end_step ein (kein _periodic_tick-Hook mehr).
</verification>

<success_criteria>
- RE-SCOPE (FOLD 23.06. + 26.06.) + Option B: Daten-Fundament gebaut (Engine rechnet + speichert NUR rubric_score; calls.coaching_score behält alten Schreiber; KEIN outcome in rubric_score), KEIN Display-Cutover. Alte Formel + Anzeige unverändert; nicht gelöscht.
- Async Score (D-10), Audio-Gate (D-09, F-07, high-conf aus events), harte Merge-Vorbedingung (F-02: ended + 0 pending, KEIN outcome — F-09 gestrichen), idempotenter UPSERT (F-03), M-4-GUC + Hard-Crash-bei-RLS-Fehler, via Hook-Registry (Plan 03) eingehängt. KEIN H-2-Sweep.
- „Neu — Vorschau"-Panel in session_detail zeigt Note + 7 Dimensionen read-only NACH Outcome-Bestätigung (ANZEIGE-Sperre); NULL/not_gradable/failed/row-absent abgefangen (Req 6); kein Produkt-Display geändert (Req 5).
- Dauer am /api/beenden festgehalten (nicht durch async-Wartezeit verfälscht).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-04-SUMMARY.md` (inkl. RE-SCOPE-Beleg (alte Formel + Anzeige + calls.coaching_score-Schreiber unverändert; Vorschau-Panel zeigt Note+Dimensionen), **FOLD-26.06.-Beleg (F-09 gestrichen: Engine ergebnis-blind rubric_engine.py:30-31, Merge ohne outcome; H-2-Sweep gestrichen: grep SCORE_SWEEP_AFTER_S/outcome='unknown' == 0, _periodic_tick unangetastet; ANZEIGE-Sperre: Note erst bei calls.outcome NOT NULL, outcome nicht in rubric_score; Anstoß in api_beenden statt correct_outcome; high-conf-Zählung aus events-Liste statt Engine-Dict; kein 'not_reached')**, **Option-B-Beleg (Engine schreibt NUR rubric_score, kein calls.coaching_score-Write in slow_lane.py; Spiegel → SCORE-UI)**, M-4-Hardening-Beleg (Hard-Crash + nicht-erledigt bei RLS-Fehler, test_daemon_write_failure_not_marked_done), F-02-Merge-Vorbedingungs-Beleg + vorzeitiger-Merge-Test, F-03-UPSERT-Beleg, F-07-Config-Defaults, Test-Anruf-rubric_score-Sample (origin='live') im Vorschau-Panel, Re-Scope-Beleg „alte Anzeige unverändert", <50%+poor-audio+zu-wenig-Events-Edge-Belege (im Panel), ANZEIGE-Sperre-Beleg (Note erst nach Outcome-Bestätigung sichtbar), Dauer-Korrektheit-Beleg, F-02-Queue-Timing-INTERLOCK-Notiz mit TAXO1-Plan-02, **M-4-Beleg (set_current_tenant vor jedem rubric_score-Write + clear_current_tenant im `finally` + test_daemon_clears_tenant_on_exception + Test-Anruf-rubric_score-Zeile mit tenant_id + coaching_score-Feld), Hook-Registry-Einhängung (register_call_end_step; KEIN register_periodic_tick_hook mehr), N-4-Beleg (session_mode-Stamp aus calls.call_mode)**, 05-Log Re-Scope-Notiz (FOLD 23.06. + 26.06.)).
</output>
