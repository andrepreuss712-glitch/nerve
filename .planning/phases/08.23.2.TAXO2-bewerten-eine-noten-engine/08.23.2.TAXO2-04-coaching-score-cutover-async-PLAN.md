---
phase: 08.23.2.TAXO2
plan: 04
type: execute
wave: 4
depends_on: ["02", "03"]
files_modified:
  - routes/app_routes.py
  - routes/dashboard.py
  - services/slow_lane.py
  - static/pip-launcher.js
  - tests/test_coaching_score_cutover.py
autonomous: false
requirements: ["SPEC Req 2 (Live-Cutover)", "SPEC Req 5 (Anzeige unveraendert)", "SPEC Req 6", "SPEC Req 9 (Speech-Stats-Verkabelung)", "D-09", "D-10"]
user_setup: []

must_haves:
  truths:
    - "Die Engine schreibt die Kopf-Zahl in calls.coaching_score — GENAU EIN Schreiber, alte Formel gelöscht"
    - "Ein Live-Test-Call erzeugt eine rubric_score-Zeile + die alte Live-Formel (_calc_process_score/_calc_call_score/_calcScore) existiert nicht mehr"
    - "Score-Timing ist async (Slow Lane) — Call-Ende ist NICHT auf die Note blockiert, Interim 'wird ausgewertet…'"
    - "<50%-Call (Proration) oder poor-audio (D-09) → coaching_score=NULL → Dashboard/Coaching/Performance zeigen 'nicht genug Daten' ohne Crash/leere 0"
    - "Audio-Gate D-09 läuft VOR dem Scoring (audio_health_score < Schwelle ODER NULL → not_gradable, gespeichert+geflaggt)"
  artifacts:
    - path: "services/slow_lane.py"
      provides: "Call-Ende-Merge: rubric_score-Zeile rechnen (compute_rubric) + calls.coaching_score schreiben (async)"
      contains: "compute_rubric"
    - path: "routes/app_routes.py"
      provides: "alte Formel gelöscht; correct_outcome stößt async Slow-Lane-Scoring an statt sync zu rechnen"
    - path: "routes/dashboard.py"
      provides: "session_detail liest persistierten Wert statt _calc_call_score; NULL-Edge abgefangen"
  key_links:
    - from: "services/slow_lane.py (Call-Ende-Merge)"
      to: "calls.coaching_score + rubric_score"
      via: "compute_rubric → einziger Schreiber, async (D-10)"
      pattern: "coaching_score"
    - from: "routes/dashboard.py session_detail"
      to: "calls.coaching_score / rubric_score"
      via: "persistierter Wert statt Neuberechnung; NULL-Edge"
      pattern: "coaching_score"
---

<objective>
Schneide den Live-Score-Pfad sauber um: die Engine (Plan 02) rechnet am Call-Ende die rubric_score-Aufschlüsselung UND schreibt die Kopf-Zahl in `calls.coaching_score` — GENAU EIN Schreiber. Die alte Live-Formel (`_calc_process_score`/`_calc_call_score`/`_apply_outcome_modifier`/`_OUTCOME_MODIFIERS` + Client-`_calcScore`) wird GELÖSCHT, inklusive der heimtückischen `dashboard.py:774`-Stelle die heute via `_calc_call_score` neu rechnet. Das Scoring läuft ASYNC auf der Slow Lane (D-10) — der Call-Ende-Moment ist nicht auf die Note blockiert (Interim "wird ausgewertet…"). Vor dem Scoring greift das Audio-Gate (D-09). Die drei Leser-Bildschirme fangen `coaching_score=NULL` sauber ab ("nicht genug Daten", kein Crash/leere 0).

Purpose: Macht die Engine live + tötet K2 im Produktiv-Pfad. RISKANTE Welle (Live-Score-Umbau + sync→async-Control-Flow) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht.
Output: alte Formel weg, async Engine-Verkabelung, D-09-Gate, NULL-Edge in 3 Screens, Test-Anruf-Beweis.

KEINE neue nackte Zahl im Übergang (Req 5 Rider) — bestehende Call-Ende-Anzeige bleibt, Interim "wird ausgewertet…". Breakdown-UI → 999.2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** RISKANTE Welle (RESEARCH §I) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht (Bildschirme rendern korrekt aus neuer Quelle, NICHT nur "Note gerechnet" — K1/K8-Lehre). `bash deploy.sh production` + `inspect.sh` + Live-Test als Test-User. KEIN lokales pytest/git pull.

**autonomous:false** — Human-Verify-Checkpoint (Test-Anruf: 3 Screens + <50%-Call + poor-audio-Call). André testet.

**UTF-8:** "wird ausgewertet…" user-facing mit Umlaut/Ellipse OK.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- Alte Formel (zu LÖSCHEN) + Schreiber/Leser — grep file:line aus RESEARCH §A2/§H. -->

ALT (routes/app_routes.py — LÖSCHEN):
- :743 _calc_call_score(conv)  (40/30/20/10-Formel, rede_score Fixwert)
- :757 _OUTCOME_MODIFIERS (dict)
- :768 _calc_process_score(conv)  (frage_qualitaet=0.0 :785 K2, redeanteil Fixwert :782-783)
- :806 _apply_outcome_modifier(...)
- :829 _calc_coaching_score(conv, outcome)  (Wrapper)
Aufruf-Stellen:
- :659 _calc_process_score → score_breakdown (:660), coaching_score bleibt NULL (:652)
- :2005-2011 correct_outcome: _calc_process_score + _apply_outcome_modifier → SCHREIBT calls.coaching_score (:2011) SYNCHRON
- :1111 _calc_call_score (Trend api_postcall_trend)

ALT (static/pip-launcher.js — LÖSCHEN als tote Funktion):
- :3183 function _calcScore(postcall)  (bereits aus Render-Pfad raus :3248/:3251; Render nutzt json.final_score :4181/4185/4197)

HEIMTÜCKISCHE Leser (dashboard.py — UMSTELLEN, RESEARCH §H/finding 3):
- :724 from routes.app_routes import _calc_call_score
- :774 score_total = _calc_call_score(conv)  ← rechnet HEUTE neu, liest NICHT coaching_score; nach Löschung umstellen auf persistierten Wert
- :795 sum(_calc_call_score(c) for c in recent)  (Trend-Avg)

Single-Writer-Befund (RESEARCH §A2): calls.coaching_score hat GENAU EINE lebende Schreib-Stelle (:2011). TAXO2 biegt sie auf die Engine um (NICHT zweite Stelle anlegen).

coaching_score-Reader-Screens (RESEARCH §H — NULL-Edge):
- routes/dashboard.py (api_dashboard + session_detail)
- routes/performance.py (Aggregat-Trends)
- static/pip-launcher.js:4181-4197 (Call-Ende, rendert json.final_score)

calls (inspect.sh §A1): coaching_score (double NULL), audio_health_score (double NULL, 0.0-1.0-Skala!), score_breakdown (jsonb, D-09 not_gradable-Träger), call_mode, outcome, tenant_id

Engine (Plan 02): compute_rubric(events, speech_stats, call, mode_config) -> dict {coaching_score, dimensions, is_provisional, measured_weight_pct, unmeasured_dimensions, status}
Speech-Stats: services/live_session.py get_speech_stats(sid) (K1 per-SID — Req 9 Verkabelung)
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `calls` (UPDATE coaching_score + score_breakdown) — Schema §A1, KEINE Schema-Änderung (NULL-fähig)
- `rubric_score` (INSERT, Plan-01-Tabelle) — Engine schreibt die Aufschlüsselung
- `intent_event` (LESE, Aggregations-Eingang) — DEPENDS-ON-TAXO1
- `conversation_logs` (LESE für dauer_sekunden/D-08; KEIN Aggregat-Write hier — das ist Plan 06/07)
### inspect.sh-Beleg (VERBATIM §A1, beim Execute neu ziehen)
```
coaching_score        | double precision |   (NULL-fähig → kein Schema-Change für NULL-Edge)
audio_health_score    | double precision |   (0.0-1.0-Skala — D-09-Schwelle auf dieser Skala!)
score_breakdown       | jsonb            |   (status="not_gradable"/reason-Träger)
```
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| call.coaching_score | Engine UPDATE (einziger Schreiber) | DB calls.coaching_score (double NULL) | ⬜ Task 4 grep EIN Schreiber + Test-Anruf |
| call.audio_health_score | D-09-Gate LESE (0.0-1.0) | DB calls.audio_health_score | ✓ §A1 (Skala 0-1, NICHT 0-100) |
| score_breakdown.status='not_gradable' | D-09 SCHREIBE | DB calls.score_breakdown (jsonb) | ⬜ Task 1 |
| rubric_score-Zeile | Engine INSERT | DB rubric_score (Plan 01) | ⬜ Task 1 Test-Anruf |
| speech_stats (redeanteil/tempo/monolog) | get_speech_stats(sid) per-SID (NICHT tote Globale, K1) | per-SID-State RAM | ✓ RESEARCH §A4 |

## Verbindungs-Karte (Punkt 22)
- **Single-Writer bewiesen (RESEARCH §A2):** calls.coaching_score = GENAU EINE Schreib-Stelle app_routes.py:2011. → umbiegen, nicht duplizieren. grep nach Cutover: `grep -rn "\.coaching_score *=" routes/ services/` == GENAU 1 (Engine).
- **Heimtückischer Leser (RESEARCH §H finding 3):** dashboard.py:774 rechnet `_calc_call_score` neu (liest NICHT coaching_score) → nach Löschung MUSS umgestellt werden, sonst rechnet es mit gelöschter Formel (ImportError/Crash).
- **Audio-Gate-Andock (RESEARCH §F):** Call.audio_health_score existiert (models.py:698, 0.0-1.0). KEINE neue Spalte. IS NULL = kein Buffer = auch not_gradable.

## 1. Lokaler Kontext (Punkt 14 — correct_outcome :1985-2015)
correct_outcome schreibt HEUTE SYNCHRON coaching_score (:2011). D-10 verlangt async. Insert/Umbau: statt `_calc_process_score+_apply_outcome_modifier+row.coaching_score=...` → `slow_lane.put({'call_id': ...})` (Scoring-Anstoß) + Response liefert "pending"/"wird ausgewertet…" statt sofortiger Zahl. early returns im Block prüfen (Ownership-Check :ASVS).

## 2. Funktions-Skelett
correct_outcome: Ownership-Check → outcome/followup persistieren → [NEU] Audio-Gate prüfen → wenn ok: Slow-Lane-Scoring anstoßen (async) → Response {ok, outcome, score_pending:true}. KEIN sync coaching_score mehr.
Slow-Lane Call-Ende-Merge (slow_lane.py): wenn alle Events eines Calls benotet (handling_score_numeric not NULL oder abstained) → Audio-Gate (D-09) → compute_rubric → INSERT rubric_score + UPDATE calls.coaching_score (oder NULL bei <50%/not_gradable) → optional SocketIO 'score_ready'.

## 3. Cross-File-Awareness
`grep -rn "_calc_process_score\|_calc_call_score\|_apply_outcome_modifier\|_calc_coaching_score\|_OUTCOME_MODIFIERS\|_calcScore" routes/ services/ static/`: app_routes.py (Def+Aufrufe :659/:1111/:2005-2011), dashboard.py (:724/:774/:795), pip-launcher.js (:3183). ALLE müssen weg/umgestellt sein.

## 4. Edge-Case-Analyse + Race-Condition-Audit (Punkt 14, D-10 sync→async)
1. **Gleichzeitiges Feuern:** Call endet bevor alle Events benotet → Call-Ende-Merge wartet bzw. wird vom Consumer getriggert wenn letzte Event-Note gesetzt; Arbeitsliste re-derivierbar. Doppeltes correct_outcome → idempotenter rubric_score-Write (keyed call_id, upsert).
2. **Verbindungsabbruch:** Scoring-Job crasht → coaching_score bleibt NULL → re-derivierbar (offene Calls = rubric_score fehlt). Kein halb-fertiger Score.
3. **Wiederverbinden:** alte SID/Call-Ende-Event → idempotenter Score-Write (keyed call_id).
4. **Tab-Hintergrund:** Score kommt async; Frontend pollt/SocketIO 'score_ready' (999.2-Detail); Interim "wird ausgewertet…" statt stale Zahl.

<tasks>

<task type="auto">
  <name>Task 1: Call-Ende-Merge in Slow Lane — compute_rubric + Audio-Gate D-09 + EIN Schreiber calls.coaching_score (async D-10)</name>
  <files>services/slow_lane.py</files>
  <read_first>
    - services/slow_lane.py (Consumer + _persist_event_ref aus Plan 03)
    - services/rubric_engine.py (compute_rubric, Plan 02) + services/live_session.py get_speech_stats
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §F (Audio-Gate 0.0-1.0) + §C (Merge)
    - ssh ... 'bash scripts/inspect.sh schema calls' (coaching_score/audio_health_score/score_breakdown)
  </read_first>
  <action>
    Call-Ende-Merge in slow_lane.py: wenn alle intent_events eines Calls benotet sind (kein `handling_score_numeric IS NULL` mehr für diesen Call ODER abstained):
    1. **Audio-Gate D-09 (VOR Scoring):** lade call. Wenn `audio_health_score < Schwelle` (Schwelle auf 0.0-1.0-Skala! Config/ENV-Default z.B. 0.5) ODER `audio_health_score IS NULL` (kein Buffer) ODER zu wenig hoch-konfidente Events → Call "nicht bewertbar": `calls.coaching_score=NULL` + `score_breakdown` = {status:"not_gradable", reason:"poor_audio_health"}. SPEICHERN+FLAGGEN, nicht verwerfen (Anti-Training-Poisoning + Headset-Telemetrie). KEIN rubric_score-Score (oder rubric_score.status='not_gradable').
    2. Sonst: events + speech_stats (get_speech_stats per-SID, K1, Req 9) + call + mode_config laden → `compute_rubric(...)`.
    3. INSERT/UPSERT rubric_score-Zeile (keyed call_id, idempotent) mit voller Aufschlüsselung + tenant_id aus calls.tenant_id (D-11).
    4. UPDATE `calls.coaching_score` = rubric['coaching_score'] (kann NULL sein bei <50%) — GENAU EIN Schreiber. score_breakdown spiegelt is_provisional/measured_weight_pct/unmeasured_dimensions (D-08).
    5. Optional SocketIO 'score_ready' room-targeted (D-10 Pending→ready; Display-Mechanik = 999.2, hier nur Event).
    Alles async im Slow-Lane-Consumer (D-10 — NIE synchron im Call-Ende).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_coaching_score_cutover.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - Call-Ende-Merge ruft compute_rubric + schreibt calls.coaching_score + INSERT rubric_score
    - Audio-Gate D-09: audio_health_score < Schwelle ODER IS NULL → coaching_score=NULL + score_breakdown.status='not_gradable' (0.0-1.0-Skala, NICHT 0-100)
    - speech_stats kommt aus get_speech_stats(sid) per-SID (Req 9): `grep "get_speech_stats" services/slow_lane.py` >= 1
    - rubric_score-Write idempotent (keyed call_id)
  </acceptance_criteria>
  <done>Engine async verkabelt, Audio-Gate vor Scoring, EIN coaching_score-Schreiber.</done>
</task>

<task type="auto">
  <name>Task 2: Alte Live-Formel löschen (app_routes.py) + correct_outcome auf async umbauen (D-10)</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py:743-839 (alle 5 alten Funktionen) + :659/:1111/:2005-2011 (Aufrufe)
    - Task 1 (Slow-Lane-Scoring-Anstoß) + services/slow_lane.py (slow_lane.put)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §A2
  </read_first>
  <action>
    1. LÖSCHE `_calc_call_score` (:743), `_OUTCOME_MODIFIERS` (:757), `_calc_process_score` (:768, inkl. frage_qualitaet=0.0-Hardcode), `_apply_outcome_modifier` (:806), `_calc_coaching_score` (:829).
    2. :659-661 (api_beenden Score-Stash): die alte _calc_process_score-Zeile + score_breakdown-Stash raus. api_beenden lässt coaching_score weiter NULL (Score kommt async). Stoße ggf. hier den Slow-Lane-Scoring-Job an (`slow_lane.put({'call_id': ...})`) wenn das der natürliche Call-Ende-Punkt ist.
    3. :2005-2011 (correct_outcome): statt sync `_calc_process_score + _apply_outcome_modifier + row.coaching_score=val` → outcome/followup persistieren, dann Slow-Lane-Scoring anstoßen (async). Response: {ok, outcome, score_pending:true} (KEINE sofortige Zahl, D-10). Punkt-14-Race-Audit (oben) beachten: Ownership-Check bleibt.
    4. :1111 (api_postcall_trend): `_calc_call_score` weg → liest persistierte calls.coaching_score der recent Calls (NULL-safe Aggregation).
    KEINE neue nackte Zahl (Req 5 Rider). Bestehende Call-Ende-Anzeige bleibt, Interim "wird ausgewertet…".
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "_calc_process_score\|_calc_call_score\|_apply_outcome_modifier\|_calc_coaching_score"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "_calc_process_score|_calc_call_score|_apply_outcome_modifier|_calc_coaching_score|_OUTCOME_MODIFIERS" routes/app_routes.py` == 0 (alle gelöscht)
    - `grep "frage_qualitaet *= *0.0" routes/app_routes.py` == 0 (K2-Hardcode weg)
    - correct_outcome schreibt coaching_score NICHT mehr synchron (`grep -A40 "def correct_outcome" routes/app_routes.py` zeigt kein `.coaching_score =` mehr, stattdessen slow_lane-Anstoß)
    - `grep -rn "\.coaching_score *=" routes/ services/` == GENAU 1 (nur Engine/slow_lane.py — EIN Schreiber)
  </acceptance_criteria>
  <done>Alte Formel weg, correct_outcome async, EIN Schreiber.</done>
</task>

<task type="auto">
  <name>Task 3: dashboard.py session_detail + Performance + pip-launcher.js — Leser umstellen + NULL-Edge (Req 6)</name>
  <files>routes/dashboard.py, static/pip-launcher.js</files>
  <read_first>
    - routes/dashboard.py:720-795 (session_detail :774 _calc_call_score-Neuberechnung + :795 Trend + :724 Import)
    - routes/performance.py (coaching_score-Leser, Aggregat)
    - static/pip-launcher.js:3183 (_calcScore tote Funktion) + :4181-4197 (Render json.final_score)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §H
  </read_first>
  <action>
    1. dashboard.py:724 Import `_calc_call_score` ENTFERNEN. :774 `score_total = _calc_call_score(conv)` → persistierten Wert lesen (`call.coaching_score` bzw. rubric_score). :795 Trend-Avg → persistierte Werte (NULL-safe).
    2. **NULL-Edge (Req 6, D-08/D-09):** wenn coaching_score IS NULL → Template/Response zeigt "nicht genug Daten" (user-facing, Umlaut OK) statt Crash/leere 0. Gilt für Dashboard, session_detail, Performance. Klartext-Hinweis (kein IT-Sprech).
    3. performance.py: coaching_score-Aggregation NULL-safe (NULL-Calls aus dem Durchschnitt ausschließen, nicht als 0 mitzählen).
    4. pip-launcher.js: tote Funktion `_calcScore` (:3183) LÖSCHEN (SPEC Acceptance: client-_calcScore entfernt/server-gespeist). Render-Pfad nutzt schon json.final_score (server) — Interim bei pending: "wird ausgewertet…" statt Zahl (D-10). KEINE neue nackte Zahl.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "_calc_call_score\|_calcScore"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "_calc_call_score" routes/dashboard.py` == 0 (Import + Aufrufe weg)
    - `grep "function _calcScore" static/pip-launcher.js` == 0 (tote Funktion gelöscht)
    - session_detail/Dashboard/Performance zeigen bei coaching_score IS NULL "nicht genug Daten" (Klartext) — kein .toFixed/Division auf NULL
    - keine neue nackte Zahl: bestehende Call-Ende-Anzeige unverändert, Interim "wird ausgewertet…"
  </acceptance_criteria>
  <done>Alle 3 Leser auf persistierten Wert umgestellt, NULL-Edge abgefangen, tote Client-Funktion weg.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Engine schreibt calls.coaching_score async (EIN Schreiber), alte Formel gelöscht, Audio-Gate D-09, NULL-Edge in 3 Screens, Test-Anruf-Pfad.</what-built>
  <how-to-verify>
    Nach `bash deploy.sh production`:
    1. **Normaler Test-Anruf** (Cold-Call mit ≥1 Einwand, gutes Audio) als Test-User → Call beenden + Outcome bestätigen. Erwartung: Call-Ende zeigt "wird ausgewertet…" (KEINE sofortige Zahl), kurz danach Note. Claudian: `inspect.sh sample rubric_score` zeigt EINE neue Zeile mit Aufschlüsselung + `inspect.sh sample calls` zeigt coaching_score gesetzt.
    2. **3 Screens** öffnen: Dashboard (/dashboard), Coaching/Session-Detail (/session/<id>), Performance — alle rendern die neue Note korrekt (DATEN-FLUSS, nicht nur "gerechnet").
    3. **<50%-Szenario** (sehr kurzer Cold-Call, kaum messbare Dimensionen) → coaching_score=NULL → alle 3 Screens zeigen "nicht genug Daten" OHNE Crash/leere 0.
    4. **Poor-Audio-Szenario** (D-09: schlechtes/leeres Audio) → Call gespeichert, score_breakdown.status='not_gradable', Dashboard spiegelt "Audio zu schlecht zum Bewerten".
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe Abweichungen (welcher Screen, welches Szenario).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| correct_outcome (Client) → Scoring-Anstoß | User triggert async Job |
| Slow-Lane-Merge → calls.coaching_score | einziger Schreiber |
| poor-audio STT → Score | halluzinierter selbstbewusster Müll-Score |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-04-01 | I (False confidence) | poor-audio → halluzinierter Score | mitigate | D-09 Audio-Gate VOR Scoring (audio_health < Schwelle ODER NULL → not_gradable, gespeichert+geflaggt). Fängt was <50%-Tor nicht fängt. |
| T-TAXO2-04-02 | D (DoS/Crash) | NULL coaching_score crasht Leser-Screen | mitigate | NULL-Edge in 3 Screens ("nicht genug Daten" statt .toFixed(NULL)/Division). Test-Anruf <50%-Szenario beweist. |
| T-TAXO2-04-03 | T (Double-write) | doppeltes correct_outcome → 2 rubric_score-Zeilen | mitigate | idempotenter UPSERT keyed call_id; EIN coaching_score-Schreiber (grep==1). |
| T-TAXO2-04-04 | I (Tenant leak) | rubric_score-Zeile ohne tenant_id | mitigate | tenant_id aus calls.tenant_id gestampt (D-11, RLS Plan 01). |
| T-TAXO2-04-05 | E | LLM rutscht in Live-Call-Ende (sync) | mitigate | D-10 async — Scoring NUR Slow Lane, Call-Ende stößt nur an. grep: kein compute_rubric im correct_outcome-Sync-Pfad. |
</threat_model>

<verification>
- Test-Anruf-DATEN-FLUSS (riskante Welle): 3 Screens rendern; <50%→NULL ohne Crash; poor-audio→not_gradable.
- grep: GENAU EIN Schreiber calls.coaching_score; alte Formel + _calcScore gelöscht; frage_qualitaet=0.0 weg.
- rubric_score-Zeile pro Test-Call (inspect.sh sample).
- D-10: kein sync coaching_score in correct_outcome; Interim "wird ausgewertet…".
- Req 9: speech_stats aus get_speech_stats per-SID verkabelt.
</verification>

<success_criteria>
- EINE Engine, EIN Schreiber, alte Formel tot (Live-Pfad).
- Async Score (D-10), Audio-Gate (D-09), NULL-Edge in 3 Screens (Req 6).
- Keine neue nackte Zahl; bestehende Anzeige unverändert (Req 5).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-04-SUMMARY.md` (inkl. grep EIN-Schreiber-Beleg, Test-Anruf-rubric_score-Sample, 3-Screen-Render-Bestätigung, <50%+poor-audio-Edge-Belege, 05-Log Req-2-Abweichungs-Notiz falls noch offen).
</output>
