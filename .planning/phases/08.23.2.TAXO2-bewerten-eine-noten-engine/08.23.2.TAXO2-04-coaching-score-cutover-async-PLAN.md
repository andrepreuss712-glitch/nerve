---
phase: 08.23.2.TAXO2
plan: 04
type: execute
wave: 4
depends_on: ["02", "03"]
files_modified:
  - routes/app_routes.py
  - routes/dashboard.py
  - templates/   # session_detail-Template: additives "Neu — Vorschau"-Panel (Template-Name beim Execute bestaetigen)
  - services/slow_lane.py
  - config.py
  - tests/test_coaching_score_cutover.py
# RE-SCOPE (FOLD 23.06.): static/pip-launcher.js NICHT mehr angefasst (kein _calcScore-Loeschen, kein Display-Cutover — das ist SCORE-UI).
autonomous: false
requirements: ["SPEC Req 2 (NUR Engine-Compute+Store, KEIN Formel-Loeschen — re-scoped FOLD 23.06.)", "SPEC Req 5 (Anzeige unveraendert + Vorschau-Panel)", "SPEC Req 6 (NULL-Edge im Vorschau-Panel)", "SPEC Req 9 (Speech-Stats-Verkabelung)", "D-09", "D-10"]
user_setup: []

must_haves:
  truths:
    - "RE-SCOPE (FOLD 23.06., André + Design-Brief): Plan 04 ist NUR noch 'rechnen + speichern + roh als Vorschau zeigen + EINMAL end-to-end verifizieren' — KEIN Display-Cutover. Das LÖSCHEN der alten Formel + das UMSCHALTEN der angezeigten Score-Leser wandert in die eigene Phase NACH TAXO (08.23.2.SCORE-UI). Die alte Anzeige bleibt während TAXO komplett unverändert stehen."
    - "Die Engine rechnet die neue Note + schreibt sie ins Daten-Fundament: NUR `rubric_score` (volle Aufschlüsselung inkl. coaching_score-Feld, Vorschau-Quelle) — async (Slow Lane), idempotenter UPSERT, M-4-Tenant-GUC. **Option B (Audit RESOLVED 23.06., Grep-Beleg: 0 Anzeige-Leser von calls.coaching_score): die Engine schreibt NICHT `calls.coaching_score`** — die Spalte behält ihren EINEN alten Schreiber (correct_outcome:2011, unverändert); der Spiegel wandert nach 08.23.2.SCORE-UI (sonst zwei Schreiber + Anzeige-Änderung)."
    - "Die alte Live-Formel (_calc_process_score/_calc_call_score/_apply_outcome_modifier/_OUTCOME_MODIFIERS/_calc_coaching_score + client _calcScore) bleibt STEHEN — NICHT gelöscht; die heutigen Anzeige-Leser (dashboard session_detail, pip-launcher) bleiben auf ihrem heutigen Pfad (NICHT umgestellt)"
    - "Ein rohes, klar als 'Neu — Vorschau' markiertes, read-only Panel in session_detail zeigt die neue coaching_score + die 7 rubric-Dimensionen (Wert/Gewicht/messbar?) + die 'nicht messbar weil…'-Gründe — NEBEN der alten Anzeige, bewusst ungestylt (Zahlen + Liste + Stempel). Zweck: die ganze Kette (Rechnen→Speichern→RLS→Lesen→Anzeigen) an einem echten Test-Anruf EINMAL verifizieren ('getestet ≠ eingebunden', Welle-4-Lehre)"
    - "Ein Live-Test-Call erzeugt eine rubric_score-Zeile, die im Vorschau-Panel sichtbar wird"
    - "Score-Timing ist async (Slow Lane) — Call-Ende ist NICHT auf die Note blockiert, Interim 'wird ausgewertet…'"
    - "Der Call-Ende-Merge feuert NUR bei HARTER Vorbedingung: call_status=='ended' (aus /api/beenden) UND calls.outcome IS NOT NULL (Ergebnis bestätigt, F-09) UND pending_events(call_id)==0 (F-02) — failed/abstained zählen NICHT als pending (F-01/F-05). Scoring-Anstoß aus correct_outcome (outcome-aware), NICHT aus api_beenden (sonst läuft compute_rubric mit outcome=NULL → D-08-Guard + outcome_progression brechen, F-09)"
    - "<50%-Call (Proration) oder poor-audio (D-09) → coaching_score=NULL → Dashboard/Coaching/Performance zeigen 'nicht genug Daten' ohne Crash/leere 0"
    - "Audio-Gate D-09 läuft VOR dem Scoring (audio_health_score < Schwelle ODER NULL ODER < N hoch-konfidente Events → not_gradable, gespeichert+geflaggt); N ist ein Config-Default (F-07)"
    - "rubric_score-UPSERT referenziert den partiellen Unique-Index aus Plan 01: ON CONFLICT (call_id) WHERE origin='live' DO UPDATE (F-03)"
    - "M-4 (HÖCHSTES STILLES RISIKO): der Slow-Lane-Daemon setzt VOR jedem rubric_score-Write `set_current_tenant(str(call.tenant_id))` (database/db.py:43, app.py:2200-Muster) — sonst ist im Daemon-Thread (kein Request-Context) der app.tenant_id-GUC leer → FORCE-RLS WITH CHECK fail-closed → INSERT abgelehnt → coaching_score ewig NULL, lautlos. `clear_current_tenant()` steht ZWINGEND im `finally`-Block (läuft auch bei Exception in compute_rubric/commit) — sonst leakt die tenant_id im Endlos-Daemon-Thread in die nächste Iteration → Cross-Tenant-Schreib-Risiko"
    - "ROBUSTHEIT (FOLD 23.06., Gemini-Schluss-Review): (1) MAX-RETRY+DEAD-LETTER — der Merge-Job hat einen Versuchs-Zähler, scheitert er → gedeckelter Re-Queue (SCORE_MAX_RETRIES, Default 3), danach dead-letter (laut loggen/alert, Job aus Queue, optional rubric_score status='failed'); (2) DAEMON ÜBERLEBT — ein Fehler failt NUR DEN Call (pro-Call try/except → re-queue-mit-Cap → continue), der Consumer-Thread stirbt NIE (sonst stehen alle Calls); (3) H-2-Sweep SCORT NICHT — er setzt nur calls.outcome='unknown' (Entsperrung), löst KEINEN Merge aus (kein slow_lane.put); Scoring bleibt allein an correct_outcome gebunden; (4) Vorschau-Panel fängt ROW-ABSENT (Zeile noch nicht da) UND field-NULL ab → 'Score wird im Hintergrund berechnet…', Stempel 'Beta'"
    - "M-4-VERSCHÄRFUNG (FOLD 23.06., Gemini): scheitert der rubric_score/calls-Write am RLS (Tenant-GUC fehlt/leer ODER permission-denied) → der Daemon CRASHT HART + loggt LAUT (`[SLOW] FATAL rubric_score write rejected (RLS/tenant)`), und der Job/das Event wird NICHT als 'erledigt' markiert (handling_status NICHT auf scored, kein Merge-Done-Flag) → der Re-Queue/Retry greift. KEIN Silent-Drop, KEIN 'als ob es lief'-Weitermachen — sonst ist die Note für immer weg und niemand merkt es (genau die M-4-Stille, die wir hassen)"
    - "H-1: der Stash→Read-Vertrag (api_beenden:659 stasht score_breakdown ↔ correct_outcome:2001-2005 liest Stash + Fallback _calc_process_score) wird als EINE Einheit gelöscht — kein halb-gelöschter Pfad (NameError bei Stash-Miss nach _calc_process_score-Löschung)"
    - "H-2: der Hang-Fix (outcome nie gesetzt) ist ein ZEITBASIERTER Sweep am TAXO1-02-`_periodic_tick`-Haken (NICHT am blockierenden queue.get()) — setzt outcome='unknown' DIREKT im Daemon (eigene DB-Session) → D-08 not_reached, KEIN benigner Drop. KEIN Aufruf des correct_outcome/ai_auto-Request-Pfads (g.user.id app_routes.py:1958 → RuntimeError outside request context), sonst stirbt der Tick. Sonst hängt der Call ewig 'wird ausgewertet…'"
  artifacts:
    - path: "services/slow_lane.py"
      provides: "Call-Ende-Merge: harte Vorbedingung (call ended + outcome + 0 pending) + compute_rubric → UPSERT rubric_score (async; Option B: KEIN calls.coaching_score-Write)"
      contains: "compute_rubric"
    - path: "routes/app_routes.py"
      provides: "alte Formel BLEIBT (Re-Scope); /api/beenden markiert Call ended; correct_outcome stößt ZUSÄTZLICH async Slow-Lane-Scoring an (additiv, bestehende sync-Logik unangetastet)"
    - path: "config.py"
      provides: "D-09 Audio-Gate-Defaults: AUDIO_HEALTH_GATE_THRESHOLD + MIN_HIGH_CONFIDENCE_EVENTS (F-07)"
    - path: "routes/dashboard.py"
      provides: "session_detail gibt rubric_preview (rubric_score-Zeile) ADDITIV ans Template; altes Score-Hero unverändert"
  key_links:
    - from: "services/slow_lane.py (Call-Ende-Merge)"
      to: "rubric_score (Option B: NUR diese Tabelle)"
      via: "compute_rubric → UPSERT rubric_score (einziger Engine-Write), async (D-10), harte Vorbedingung (F-02/F-09), partieller Unique-Index (F-03); KEIN calls.coaching_score-Write"
      pattern: "rubric_score"
    - from: "routes/dashboard.py session_detail (Vorschau-Panel)"
      to: "rubric_score (NICHT calls.coaching_score)"
      via: "rubric_preview read-only; NULL/not_gradable-Edge im Panel"
      pattern: "rubric_preview"
---

<objective>
> ## ⬛ RE-SCOPE (FOLD 23.06.2026 — André-Entscheidung + Design-Brief `Nerve-Vault/03 Planung/Scoreboard + Auswertung Redesign`)
>
> **Plan 04 ist jetzt „rechnen + speichern + roh als Vorschau zeigen + EINMAL end-to-end verifizieren" — KEIN Display-Cutover.** Das Umschalten der Anzeige + das komplette Redesign sind eine **eigene Phase NACH TAXO (08.23.2.SCORE-UI)**. TAXO baut nur das Fundament + verifiziert es einmal an einem echten Anruf.
>
> **BEHALTEN (in Plan 04):** Engine rechnet (compute_rubric, Plan 02) + schreibt ins Daten-Fundament — **NUR `rubric_score`** (volle Aufschlüsselung inkl. coaching_score-Feld als Kopf-Zahl), async, idempotenter UPSERT (F-03), M-4-Tenant-GUC, Audio-Gate (D-09), harte Merge-Vorbedingung (F-02/F-09), H-2-Sweep. **KEIN `calls.coaching_score`-Write (Option B, siehe Audit unten).** Alles wie unten in Task 1.
>
> **RAUS aus Plan 04 → wandert nach SCORE-UI:** (a) das **LÖSCHEN** der alten Formel (`_calc_call_score`/`_calc_coaching_score`/`_calc_process_score`/`_apply_outcome_modifier`/`_OUTCOME_MODIFIERS` + client `_calcScore`); (b) das **UMSCHALTEN** der angezeigten Score-Leser (dashboard.py session_detail :774, pip-launcher). **Die alte Anzeige + die alte Formel bleiben während TAXO komplett unverändert stehen.**
>
> **NEU in Plan 04:** ein rohes, read-only, klar als **„Neu — Vorschau"** markiertes Panel in session_detail, das die neue coaching_score + die 7 rubric-Dimensionen (Wert/Gewicht/messbar?) + die „nicht messbar weil…"-Gründe rendert — **NEBEN** der alten Anzeige. Bewusst ungestylt (Zahlen + Liste + Stempel), kein Layout-Aufwand (das ist SCORE-UI). Zweck: die ganze Kette (Rechnen→Speichern→RLS→Lesen→Anzeigen) an einem echten Test-Anruf EINMAL verifizieren (gegen „getestet ≠ eingebunden", Welle-4-Lehre „blieb leer").
>
> **✅ AUDIT-PUNKT RESOLVED = Option B (Pre-Execute-Audit 23.06., Claudian + Grep-Beleg):** `grep -rn "coaching_score" routes/dashboard.py routes/performance.py static/pip-launcher.js templates/` == **0 Treffer** → KEIN heutiger Anzeige-Pfad liest `calls.coaching_score` direkt (das Score-Hero re-rechnet via `_calc_call_score`, der PiP nutzt `final_score` aus der correct_outcome-Antwort). `calls.coaching_score` hat GENAU EINEN lebenden Schreiber (correct_outcome:2011), und der BLEIBT (Re-Scope: alte Welt unangetastet). **Entscheidung (gelockt):** die Engine schreibt in TAXO **NUR `rubric_score`**, NICHT `calls.coaching_score` (sonst zwei Schreiber → Kollision mit dem bleibenden correct_outcome-Write). Der `calls.coaching_score`-Spiegel wandert nach **08.23.2.SCORE-UI**. Das Vorschau-Panel liest `rubric_score` direkt. M-4-Tenant-GUC + Hard-Crash-bei-RLS-Fehler gelten weiter für den `rubric_score`-Write.
>
> **Die zwei Sätze:** (1) `kb_*` + `skript_abdeckung` bleiben beim alten Schreiber (Plan 06/07-Fold, Engine kann sie nicht). (2) Alte Anzeige unangetastet — nur das Vorschau-Panel ist neu.

Die Engine (Plan 02) rechnet am Call-Ende die rubric_score-Aufschlüsselung UND die Kopf-Zahl (coaching_score-Feld) und schreibt sie ins Daten-Fundament — **NUR `rubric_score` (Option B, KEIN `calls.coaching_score`-Write; die Spalte behält ihren alten Schreiber correct_outcome:2011)**. Das Scoring läuft ASYNC auf der Slow Lane (D-10) — der Call-Ende-Moment ist nicht auf die Note blockiert (Interim "wird ausgewertet…"). Vor dem Scoring greift das Audio-Gate (D-09). **Die alte Live-Formel + die alten Anzeige-Leser bleiben STEHEN (Re-Scope oben);** das neue „Neu — Vorschau"-Panel in session_detail fängt `coaching_score=NULL`/not_gradable sauber ab ("nicht genug Daten", kein Crash/leere 0).

**Cross-AI-Fold (REVIEWS F-02, HIGH):** Der Call-Ende-Merge braucht eine HARTE Call-Ende-Vorbedingung. Heute würde "alle Events benotet" mitten im laufenden Call transient WAHR werden (Slow-Lane benotet live aus der Queue) → vorzeitiger Merge → falscher/unvollständiger Score. Fix: Merge feuert NUR wenn `call_status == 'ended'` (Call ist via `/api/beenden` beendet) **UND** `pending_events(call_id) == 0`. **Queue-Timing explizit:** Events gelangen bei EMIT (live, während des Calls) in die Slow-Lane-Queue und werden live benotet (handling_score) — ABER der Call-Ende-MERGE (rubric_score + coaching_score) wird erst durch das `/api/beenden`-ended-Signal freigeschaltet. `pending_events` zählt NUR `handling_status='pending'` — NICHT `failed`/`abstained` (F-01/F-05: die sind abgeschlossen). INTERLOCK mit TAXO1-Plan-02 (Slow-Lane-Gerüst): falls TAXO1 das Queue-Timing anders fixt → im 3-Wege-Interlock abgleichen.

**Cross-AI-Fold (REVIEWS F-03):** Der idempotente `rubric_score`-Write nutzt `ON CONFLICT (call_id) WHERE origin='live' DO UPDATE` — referenziert exakt den partiellen Unique-Index `ux_rubric_score_live_call_id` aus Plan 01 (Postgres verlangt einen passenden Index).

**Cross-AI-Fold (REVIEWS F-07):** Das D-09-Audio-Gate-Kriterium "zu wenig hoch-konfidente Events" bekommt einen konkreten Config-Default: `< MIN_HIGH_CONFIDENCE_EVENTS` (Default 3) Events über der Tor-1-Konfidenzschwelle → `not_gradable`. In config.py/ENV (Punkt 12), post-launch tunbar.

**Pre-Execute-Fold (REVIEWS F-09, MED-HIGH — Claudian Runde 2, code-verifiziert):** Die F-02-Vorbedingung "call ended + 0 pending" reicht NICHT — sie muss zusätzlich `calls.outcome IS NOT NULL` (Ergebnis bestätigt) verlangen. Belegt am lebenden Code: `api_beenden` (app_routes.py:98) beendet den Call, setzt aber KEIN outcome; `row.outcome` wird erst in `correct_outcome` (:1961) gesetzt — ein separater, späterer Schritt. Der ALTE Code rechnete die Note bewusst in `correct_outcome` (:2011, NACHDEM outcome bekannt war). Wenn der Merge schon bei `api_beenden` (ended, 0 pending — oft sofort wahr) feuert, läuft `compute_rubric` mit `outcome=NULL` → (a) Dimension `outcome_progression` fällt aus (D-04: messbar nur wenn outcome != NULL) UND (b) der D-08-N/A-vs-vergeigt-Guard kann den Abbruch NICHT erkennen (er prüft `outcome in {no_interest, lost}`) → ein Totalausfall bekommt einen falsch-hohen vorläufigen Score = exakt die D-08-False-Green, die wir gehärtet haben. **Fix:** Merge-Vorbedingung = `call ended` UND `calls.outcome IS NOT NULL` UND `pending_events==0`; Scoring-Anstoß-Punkt ist `correct_outcome` (outcome-aware), NICHT `api_beenden`. **Hang-Edge (Pflicht mitdenken):** falls outcome nie gesetzt wird (User bricht das Outcome-Fenster ab) → Merge feuert nie → Call hängt "wird ausgewertet…". Auflösung = der H-2-Sweep (siehe Task 1): der Daemon setzt nach Frist `outcome='unknown'` DIREKT (eigene DB-Session) und scort als D-08-`not_reached` (KEIN benigner Drop). **NICHT** über den `ai_auto`-correct_outcome-Request-Pfad (Gemini-Gegencheck, code-verifiziert): `correct_outcome`/der ai_auto-Pfad liest `g.user.id` (app_routes.py:1958, Flask-Request-Kontext) → aus dem Daemon-Thread = `RuntimeError: working outside of request context`. Der Hang darf nicht entstehen, aber die Auflösung läuft komplett im Daemon, request-context-frei.

**Interlock-Fold (TAXO-INTERLOCK-FINDINGS M-4, HÖCHSTES STILLES RISIKO — code+prod-verifiziert):** rubric_score hat `FORCE ROW LEVEL SECURITY` (Plan 01). Der `app.tenant_id`-GUC wird NUR im `after_begin`-Hook gesetzt und NUR wenn der `_current_tenant_id`-Contextvar gefüllt ist (database/db.py:62-78, live auf Prod). Der Call-Ende-Merge läuft im **Slow-Lane-Daemon-Thread OHNE Request-Context** → Contextvar leer → GUC NULL → `nullif(current_setting('app.tenant_id', true),'')::uuid` = NULL → WITH CHECK `tenant_id = NULL` = false → der rubric_score-INSERT wird **fail-closed ABGELEHNT** → `coaching_score` bleibt **ewig NULL, ohne Fehler im UI**. **Fix:** vor dem Write `set_current_tenant(str(call.tenant_id))` (database/db.py:43 — exakt das blessed Pattern aus app.py:2200 im Request-Pfad; explizit auch für Worker-Threads dokumentiert, db.py:46-47), `clear_current_tenant()` ZWINGEND im `finally` (Gemini-Gegencheck: der Endlos-Daemon-Thread leakt sonst bei einer Exception in compute_rubric/commit die tenant_id in die nächste Iteration → Cross-Tenant-Write; finally garantiert das Räumen auf jedem Pfad). Prod-Grund: HEAD=0015, set_current_tenant bereits deployed (db.py:43, app.py:2200); rubric_score+FORCE kommt mit Plan 01. KEINE neue Mechanik — der Daemon nutzt die bestehende.

**Interlock-Fold (TAXO-INTERLOCK-FINDINGS H-2, Sweep-Owner — dockt am TAXO1-02-`_periodic_tick` an):** Der F-09-Hang-Edge (User bestätigt das Outcome nie → Merge-Vorbedingung `outcome IS NOT NULL` nie wahr → Call hängt ewig "wird ausgewertet…") braucht einen ZEITBASIERTEN Sweep. Der blockierende `slow_lane.get()` erwacht nur bei neuen Items, NIE periodisch — er kann den Sweep nicht tragen. TAXO1-02 §0.1 hat dafür die periodische Timer-Komponente gebaut: `slow_lane.get(timeout=SLOW_LANE_TICK)` + `_periodic_tick()`-Haken. **Der Sweep hängt HIER an** (kein Re-Architecture): `_periodic_tick` findet Calls die `call ended` + `outcome IS NULL` + Frist überschritten sind → setzt `outcome='unknown'` DIREKT im Daemon (eigene DB-Session) und stößt den Merge an, der in compute_rubric als D-08 `not_reached` behandelt wird (KEIN benigner Drop, KEIN hoher Score). **KEIN Aufruf des `ai_auto`/correct_outcome-Request-Pfads** (Gemini-Gegencheck): der liest `g.user.id` (app_routes.py:1958) → aus dem Daemon-Thread `RuntimeError` (outside request context). Plan 03 hat denselben `_periodic_tick` bereits um H-3 (Re-Queue) erweitert — Plan 04 ERGÄNZT Sweep (H-2) + GUC (M-4), überschreibt H-3 NICHT.

Purpose: Macht die Engine live + tötet K2 im Produktiv-Pfad. RISKANTE Welle (Live-Score-Umbau + sync→async-Control-Flow) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht.
Output (RE-SCOPE + Option B): Daten-Fundament — async Engine-Verkabelung (NUR rubric_score, KEIN calls.coaching_score) mit harter Merge-Vorbedingung (F-02/F-09), D-09-Gate (F-07), idempotenter UPSERT (F-03), M-4-GUC + Hard-Crash-bei-RLS-Fehler; „Neu — Vorschau"-Panel in session_detail (NULL/not_gradable abgefangen); alte Formel + Anzeige + calls.coaching_score-Schreiber unverändert; Test-Anruf-Beweis.

Bestehende PRODUKT-Anzeige (Call-Ende-Screen, session_detail Score-Hero) bleibt unverändert (Req 5 Rider — kein neuer „nackte-Zahl"-Schaden im Produkt). Das „Neu — Vorschau"-Panel ist davon ausgenommen: es ist ein klar gestempeltes, read-only Verifikations-Panel NEBEN der alten Anzeige (kein Produkt-Feedback-Element) — explizit vom Design-Brief autorisiert. Das gestylte Breakdown-Redesign bleibt die spätere SCORE-UI-Phase (vormals 999.2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** RISKANTE Welle (RESEARCH §I) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht (Bildschirme rendern korrekt aus neuer Quelle, NICHT nur "Note gerechnet" — K1/K8-Lehre). `bash deploy.sh production` + `inspect.sh` + Live-Test als Test-User. KEIN lokales pytest/git pull.

**autonomous:false** — Human-Verify-Checkpoint (Test-Anruf: „Neu — Vorschau"-Panel rendert Note+Dimensionen + alte Anzeige unverändert + <50%-Call + poor-audio-Call + vorzeitig-abgebrochener-Call für F-02/F-09 + Dauer-Korrektheit). André testet.

**UTF-8:** "wird ausgewertet…" user-facing mit Umlaut/Ellipse OK. Config-Identifier ASCII.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- Alte Formel (zu LÖSCHEN) + Schreiber/Leser — grep file:line aus RESEARCH §A2/§H. -->

ALT (routes/app_routes.py — LÖSCHEN):
- :743 _calc_call_score(conv)  (40/30/20/10-Formel, rede_score Fixwert)
- :757 _OUTCOME_MODIFIERS (dict)
- :768 _calc_process_score(conv)  (frage_qualitaet=0.0 :785 K2, redeanteil Fixwert :782-783)
- :806 _apply_outcome_modifier(...)
- :829 _calc_coaching_score(conv, outcome)  (Wrapper)
Aufruf-Stellen:
- :659 _calc_process_score → score_breakdown (:660), coaching_score bleibt NULL (:652)
- :2005-2011 correct_outcome: _calc_process_score + _apply_outcome_modifier → SCHREIBT calls.coaching_score (:2011) SYNCHRON
- :1111 _calc_call_score (Trend api_postcall_trend)

ALT (static/pip-launcher.js — LÖSCHEN als tote Funktion):
- :3183 function _calcScore(postcall)  (bereits aus Render-Pfad raus :3248/:3251; Render nutzt json.final_score :4181/4185/4197)

HEIMTÜCKISCHE Leser (dashboard.py — UMSTELLEN, RESEARCH §H/finding 3):
- :724 from routes.app_routes import _calc_call_score
- :774 score_total = _calc_call_score(conv)  ← rechnet HEUTE neu, liest NICHT coaching_score; nach Löschung umstellen auf persistierten Wert
- :795 sum(_calc_call_score(c) for c in recent)  (Trend-Avg)

Single-Writer-Befund (RESEARCH §A2) + Option B: calls.coaching_score hat GENAU EINE lebende Schreib-Stelle (:2011) — die BLEIBT. TAXO2 biegt sie NICHT um und legt KEINE zweite an (die neue Note lebt in rubric_score). Der calls.coaching_score-Umzug auf die Engine ist SCORE-UI.

coaching_score-Reader-Screens (RESEARCH §H — NULL-Edge):
- routes/dashboard.py (api_dashboard + session_detail)
- routes/performance.py (Aggregat-Trends)
- static/pip-launcher.js:4181-4197 (Call-Ende, rendert json.final_score)

calls (inspect.sh §A1): coaching_score (double NULL), audio_health_score (double NULL, 0.0-1.0-Skala!), score_breakdown (jsonb, D-09 not_gradable-Träger), call_mode, outcome, tenant_id

Call-Ende-Signal (F-02): /api/beenden ist der Call-Ende-Punkt. Markierung dass der Call beendet ist —
Quelle prüfen: calls.outcome gesetzt / calls.ended_at / ein explizites Status-Feld. Beim Execute via
`inspect.sh schema calls` bestätigen welches Feld 'ended' signalisiert (ended_at IS NOT NULL ODER ein
status-Feld). pending_events(call_id) = COUNT intent_event WHERE call_id=X AND handling_status='pending'.

handling_status (Plan 03, intent_event): pending|scored|abstained|failed.
  pending_events zählt NUR 'pending' (abstained/failed = abgeschlossen, F-01/F-05).

rubric_score (Plan 01): partieller Unique-Index ux_rubric_score_live_call_id (call_id, WHERE origin='live')
  → UPSERT: INSERT ... ON CONFLICT (call_id) WHERE origin='live' DO UPDATE SET ... (F-03)

Engine (Plan 02): compute_rubric(events, speech_stats, call, mode_config) -> dict {coaching_score, dimensions, is_provisional, measured_weight_pct, unmeasured_dimensions, status, n_high_confidence_events}
Speech-Stats: services/live_session.py get_speech_stats(sid) (K1 per-SID — Req 9 Verkabelung)

config.py (F-07, NEU): AUDIO_HEALTH_GATE_THRESHOLD = float(os.getenv('AUDIO_HEALTH_GATE_THRESHOLD', '0.5'))  # 0.0-1.0-Skala
                       MIN_HIGH_CONFIDENCE_EVENTS = int(os.getenv('MIN_HIGH_CONFIDENCE_EVENTS', '3'))  # D-09 "zu wenig hoch-konfidente Events"

M-4 GUC-Vertrag (database/db.py, LIVE auf Prod — verifiziert HEAD=0015):
- set_current_tenant(tid)  (db.py:43): publiziert die Tenant-UUID in den _current_tenant_id-Contextvar; der after_begin-Hook (db.py:62-78) setzt daraus transaction-local app.tenant_id. "Called ... by any worker thread before its session work" (db.py:46-47) — ausdrücklich für Daemon-Threads gesegnet.
- clear_current_tenant()  (db.py:52): Hygiene nach dem Write (Contextvar leeren).
- Request-Pfad-Muster (app.py:2200): `_set_tenant(_sess.get('tenant_id'))` — der Daemon spiegelt das mit `set_current_tenant(str(call.tenant_id))`.
- Reihenfolge KRITISCH: set_current_tenant MUSS vor dem Transaktions-Beginn des rubric_score-Writes stehen (der GUC wird bei after_begin gesetzt). calls.tenant_id wird VORHER gelesen (calls ist nicht FORCE-RLS → Lesen ohne GUC ok); dann GUC setzen; dann rubric_score INSERT/UPSERT.

H-2/H-3/M-4 Tick-Vertrag (services/slow_lane.py, TAXO1-02 §0.1 + Plan 03):
- def _periodic_tick(): bei queue.Empty-Timeout (alle SLOW_LANE_TICK=5s) gerufen. Plan 03 hat ihn um H-3 (_requeue_pending) erweitert; Plan 04 ERGÄNZT hier H-2 (Sweep nie-gesetzter outcomes) — die Erweiterungen koexistieren, KEIN Überschreiben.
- Sweep-Frist: Config-Default (z.B. SCORE_SWEEP_AFTER_S), beim Execute in config.py anlegen (post-launch tunbar, Punkt 12).
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `calls` (LESE call-ended-Signal F-02 + tenant_id/audio_health/call_mode/outcome; **KEIN Engine-Write von coaching_score/score_breakdown — Option B**; einziger Engine-`calls`-Write ist `calls.outcome='unknown'` im Hung-Call-Sweep, das ist nicht der Score) — Schema §A1, KEINE Schema-Änderung
- `rubric_score` (UPSERT auf partiellem Unique-Index, Plan-01-Tabelle) — **die EINZIGE Engine-Schreib-Tabelle der Note** (Aufschlüsselung + coaching_score-Feld + status)
- `intent_event` (LESE Aggregations-Eingang + COUNT handling_status='pending' für F-02) — DEPENDS-ON-TAXO1 + Plan 03 handling_status
- `conversation_logs` (LESE für dauer_sekunden/D-08; KEIN Aggregat-Write hier — das ist Plan 06/07)
### inspect.sh-Beleg (VERBATIM §A1, beim Execute neu ziehen)
```
coaching_score        | double precision |   (NULL-fähig → kein Schema-Change für NULL-Edge)
audio_health_score    | double precision |   (0.0-1.0-Skala — D-09-Schwelle auf dieser Skala!)
score_breakdown       | jsonb            |   (status="not_gradable"/reason-Träger)
ended_at / outcome    | (Call-Ende-Signal für F-02 — beim Execute bestätigen welches Feld 'ended' markiert)
```
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| call.coaching_score | **KEIN Engine-Write (Option B)** — bleibt alter Schreiber correct_outcome:2011 | DB calls.coaching_score (double NULL, unverändert) | ✓ Audit RESOLVED (grep 0 Anzeige-Leser) |
| rubric_score.coaching_score (Kopf-Zahl) | Engine UPSERT (einziger Schreiber der Note) | DB rubric_score (Plan 01) | ⬜ Task 1 Test-Anruf |
| call_status == 'ended' (F-02) | Merge-Vorbedingung LESE | DB calls.ended_at/outcome (beim Execute bestätigen) | ⬜ Task 1 |
| pending_events(call_id) (F-02) | COUNT handling_status='pending' | DB intent_event.handling_status (Plan 03) | ⬜ Task 1 |
| call.audio_health_score | D-09-Gate LESE (0.0-1.0) | DB calls.audio_health_score | ✓ §A1 (Skala 0-1, NICHT 0-100) |
| n_high_confidence_events (F-07) | D-09-Gate Zählung | aus intent_event confidence ≥ Tor-1 | ⬜ Task 1 |
| status='not_gradable' | D-09 SCHREIBE | DB **rubric_score.status** (Option B — NICHT calls.score_breakdown) | ⬜ Task 1 |
| rubric_score-Zeile | Engine UPSERT (ON CONFLICT partieller Index, F-03) | DB rubric_score (Plan 01) | ⬜ Task 1 Test-Anruf |
| speech_stats (redeanteil/tempo/monolog) | get_speech_stats(sid) per-SID (NICHT tote Globale, K1) | per-SID-State RAM | ✓ RESEARCH §A4 |

## Verbindungs-Karte (Punkt 22)
- **Single-Writer bewiesen (RESEARCH §A2) + Option B (Audit RESOLVED 23.06.):** calls.coaching_score = GENAU EINE Schreib-Stelle app_routes.py:2011, die BLEIBT (Re-Scope). Die Engine schreibt NICHT in calls.coaching_score (sonst zweiter Schreiber). grep-Beleg: `grep -rn "coaching_score" routes/dashboard.py routes/performance.py static/pip-launcher.js templates/` == 0 (kein Anzeige-Leser der Spalte) → der Engine-Wert lebt in rubric_score, die alte Anzeige bleibt unverändert. Nach Bau: `grep -rn "coaching_score" services/slow_lane.py` zeigt KEINEN calls.coaching_score-Write (nur rubric_score.coaching_score im UPSERT).
- **Heimtückischer Leser (RESEARCH §H finding 3) — unter Re-Scope HARMLOS:** dashboard.py:774 rechnet `_calc_call_score` neu (liest NICHT coaching_score) → BLEIBT unverändert (nichts wird gelöscht/umgestellt in TAXO). Das Umstellen wandert nach SCORE-UI.
- **Audio-Gate-Andock (RESEARCH §F):** Call.audio_health_score existiert (models.py:698, 0.0-1.0). KEINE neue Spalte. IS NULL = kein Buffer = auch not_gradable.
- **Call-Ende-Signal (F-02):** beim Execute via `inspect.sh schema calls` bestätigen welches Feld den beendeten Call markiert (ended_at IS NOT NULL / outcome gesetzt). pending_events nutzt intent_event.handling_status (Plan 03). INTERLOCK TAXO1-Plan-02 (Queue-Timing).
- **UPSERT-Index (F-03):** Plan 01 baut ux_rubric_score_live_call_id (call_id, WHERE origin='live') → ON CONFLICT referenziert genau diesen.

## 1. Lokaler Kontext (Punkt 14 — correct_outcome :1985-2015 + Call-Ende-Merge)
**RE-SCOPE + Option B:** correct_outcome behält seine HEUTIGE sync-Logik (inkl. `row.coaching_score=...` :2011) UNVERÄNDERT (alte Anzeige bleibt). Einzige additive Änderung: NACH dem bestehenden Code ein `slow_lane.put({'call_id': ...})` (async Scoring-Anstoß für den Engine-Merge). Response NICHT umbauen (kein score_pending — die alte Anzeige läuft wie heute). early returns / Ownership-Check unangetastet.
**F-02 Merge-Vorbedingung:** der Call-Ende-Merge im Consumer feuert NUR wenn der Call ended ist (call_status/ended_at) UND outcome IS NOT NULL (F-09) UND pending_events==0. Das verhindert den transient-WAHR-Merge mitten im Call.

## 2. Funktions-Skelett
correct_outcome: Ownership-Check → outcome/followup persistieren → **bestehende sync-Anzeige-Logik (row.coaching_score=… via _calc_process_score) BLEIBT** → [NEU additiv] `slow_lane.put({'call_id': ...})` (async Engine-Anstoß) → bestehende Response. **H-1 MOOT** (nichts gelöscht → kein NameError; der Stash-Block bleibt).
/api/beenden (:659ff): markiert den Call als beendet (ended_at/status) → das ist das F-02-Call-Ende-Signal. **Bestehenden _calc_process_score-Stash NICHT entfernen (Re-Scope — alte Welt unangetastet).**
Slow-Lane Call-Ende-Merge (slow_lane.py):
  WENN call_status=='ended' UND calls.outcome IS NOT NULL (F-09) UND pending_events(call_id)==0 (NUR handling_status='pending' zählt, NICHT failed/abstained — F-01/F-05):
    → **M-4: set_current_tenant(str(call.tenant_id)) (VOR dem rubric_score-Write — sonst FORCE-RLS fail-closed → rubric_score-Note fehlt ewig, Vorschau leer)**
    → Audio-Gate D-09 (F-07: audio_health < AUDIO_HEALTH_GATE_THRESHOLD ODER NULL ODER n_high_confidence_events < MIN_HIGH_CONFIDENCE_EVENTS)
    → wenn not_gradable: **rubric_score mit status='not_gradable' + coaching_score=NULL** (Option B — NICHT calls)
    → sonst compute_rubric (mode_key aus calls.call_mode, N-4) → **UPSERT rubric_score (ON CONFLICT call_id WHERE origin='live', F-03) — KEIN UPDATE calls.coaching_score (Option B)**
    → clear_current_tenant() (M-4 Hygiene) → optional SocketIO 'score_ready'
_periodic_tick (slow_lane.py, TAXO1-02 §0.1-Haken — ERGÄNZT Plan-03-H-3, überschreibt NICHT):
  → H-3 (Plan 03): _requeue_pending (pending-Events nachfüllen)
  → H-2 (dieser Plan): Sweep ended+outcome-NULL-Calls nach SCORE_SWEEP_AFTER_S → outcome='unknown' DIREKT im Daemon (KEIN ai_auto/correct_outcome-Request-Pfad: g.user → RuntimeError) → D-08 not_reached (Write unter M-4-GUC try/finally)

## 3. Cross-File-Awareness
`grep -rn "_calc_process_score\|_calc_call_score\|_apply_outcome_modifier\|_calc_coaching_score\|_OUTCOME_MODIFIERS\|_calcScore" routes/ services/ static/`: app_routes.py (Def :743-839 + Aufrufe :659 Stash-WRITE / :1111 Trend / :1992-2013 correct_outcome Stash-READ-Block H-1), dashboard.py (:724/:774/:795), pip-launcher.js (:3183). ALLE müssen weg/umgestellt sein. Zusätzlich H-1-grep: `process_score\|base_bd` in correct_outcome == 0 nach Edit.

## 4. Edge-Case-Analyse + Race-Condition-Audit (Punkt 14, D-10 sync→async + F-02)
1. **Vorzeitiger Merge (F-02, HIGH):** Slow-Lane benotet Events live aus der Queue → "keine offenen Events" wird transient WAHR mitten im laufenden Call. **Fix:** Merge-Vorbedingung call_status=='ended' (aus /api/beenden) UND pending_events==0. Der Merge feuert NICHT bevor der Call beendet ist. Trigger-Punkt: nach jedem _persist_event_ref UND nach /api/beenden prüfen "ist Call ended + 0 pending?" → erst dann mergen.
2. **Poison-Pill / Abstention zählt als offen (F-01/F-05):** pending_events zählt NUR handling_status='pending'. failed/abstained = abgeschlossen → blockieren den Merge NICHT. Call mit ≥1 failed wird mit Marker "N Events nicht bewertbar" benotet.
3. **Verbindungsabbruch:** Scoring-Job crasht → coaching_score bleibt NULL → re-derivierbar (Call ended + rubric_score fehlt = offener Merge). Kein halb-fertiger Score.
4. **Doppeltes correct_outcome / Doppelklick:** idempotenter UPSERT keyed call_id (ON CONFLICT partieller Index, F-03) → genau 1 Live-rubric_score-Zeile.
5. **Tab-Hintergrund:** Score kommt async; Frontend pollt/SocketIO 'score_ready' (999.2-Detail); Interim "wird ausgewertet…" statt stale Zahl.
6. **M-4 GUC-Leere + GUC-Leak im Daemon (HÖCHSTES STILLES RISIKO):** (a) Daemon-Thread ohne Request-Context → app.tenant_id-GUC leer → FORCE-RLS lehnt rubric_score-INSERT fail-closed ab → coaching_score ewig NULL, lautlos. (b) **GUC-Leak (Gemini-Gegencheck):** wird clear_current_tenant() nach einer Exception in compute_rubric/commit übersprungen, behält der Endlos-Daemon-Thread die tenant_id für die nächste Iteration (anderer Call/Tenant) → Cross-Tenant-Write. **Fix:** set_current_tenant(str(call.tenant_id)) vor JEDEM Write (Merge + Sweep + not_gradable-Write); **clear_current_tenant() ZWINGEND im `finally`** (läuft auf jedem Pfad). call.tenant_id zuerst lesen (calls nicht FORCE-RLS). call.tenant_id NULL → loggen, kein stiller Abbruch.
7. **H-2 nie-gesetztes outcome (Hang):** User bestätigt Outcome nie → outcome IS NULL bleibt → Merge feuert nie. **Fix:** zeitbasierter Sweep im _periodic_tick (nicht queue.get()) nach SCORE_SWEEP_AFTER_S setzt outcome='unknown' DIREKT im Daemon → D-08 not_reached. **KEIN ai_auto/correct_outcome-Request-Pfad (g.user.id app_routes.py:1958 → RuntimeError outside request context).** Kein ewiges "wird ausgewertet…".
8. **H-1 halb-gelöschter Stash-Pfad:** _calc_process_score gelöscht, aber correct_outcome-Stash-Read-Fallback bleibt → NameError bei Stash-Miss. **Fix:** ganzer Block :1992-2013 als Einheit ersetzt; grep process_score==0.

<tasks>

<task type="auto">
  <name>Task 1: Call-Ende-Merge in Slow Lane — HARTE Vorbedingung (F-02/F-09) + Audio-Gate D-09 (F-07-Config) + idempotenter UPSERT rubric_score (F-03) — Option B: EINZIGER Engine-Write ist rubric_score (KEIN calls.coaching_score), async D-10</name>
  <files>services/slow_lane.py, config.py</files>
  <read_first>
    - services/slow_lane.py (Consumer + _persist_event_ref aus Plan 03, handling_status-Statemachine)
    - services/rubric_engine.py (compute_rubric, Plan 02) + services/live_session.py get_speech_stats
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §F (Audio-Gate 0.0-1.0) + §C (Merge)
    - ssh ... 'bash scripts/inspect.sh schema calls' (coaching_score/audio_health_score/score_breakdown + ended_at/outcome = Call-Ende-Signal F-02)
    - config.py (bestehende Config-Defaults als Muster, Punkt 12)
  </read_first>
  <action>
    **Hook-Registry-Nutzung (FOLD 23.06., Plan 03 Task 5):** Plan 04 baut den Call-Ende-Merge-GATE (harte Vorbedingung ended+outcome+0 pending) + die M-4-GUC-Klammer (set/clear_current_tenant try/finally) und ruft INNERHALB der GUC-Klammer `run_call_end_steps(ctx)` (aus Plan 03). Den eigenen compute_rubric→rubric_score-Schritt (Option B: NUR rubric_score, KEIN calls.coaching_score) registriert Plan 04 via `register_call_end_step(...)` — Plan 06 (Schatten) + Plan 07 (Aggregat-Write) registrieren später ihre Schritte EBENSO, ohne den Merge-Body neu zu schreiben. Den H-2-Sweep registriert Plan 04 via `register_periodic_tick_hook(...)` (koexistiert mit Plan-03-H-3, überschreibt `_periodic_tick` NICHT). Minimal halten — die Registry existiert schon (Plan 03), hier nur einhängen.

    **F-07 Config-Defaults (config.py):** `AUDIO_HEALTH_GATE_THRESHOLD = float(os.getenv('AUDIO_HEALTH_GATE_THRESHOLD', '0.5'))` (0.0-1.0-Skala) + `MIN_HIGH_CONFIDENCE_EVENTS = int(os.getenv('MIN_HIGH_CONFIDENCE_EVENTS', '3'))` (D-09 "zu wenig hoch-konfidente Events" = < 3 Events über Tor-1-Schwelle) + **`SCORE_SWEEP_AFTER_S = int(os.getenv('SCORE_SWEEP_AFTER_S', '900'))`** (H-2 Hang-Sweep-Frist: ein beendeter Call ohne bestätigtes outcome wird nach so vielen Sekunden NUR auf outcome='unknown' geschlossen — NICHT benotet) + **`SCORE_MAX_RETRIES = int(os.getenv('SCORE_MAX_RETRIES', '3'))`** (Merge-Job-Retry-Cap → dann dead-letter). Kommentar: post-launch tunbar (Punkt 12).

    Call-Ende-Merge in slow_lane.py — **F-02 + F-09 HARTE Vorbedingung (KRITISCH):**
    Der Merge feuert NUR wenn ALLE DREI gelten:
    - `call_status == 'ended'` — der Call ist via /api/beenden beendet (Feld beim Execute bestätigt: ended_at IS NOT NULL bzw. status). KEIN Merge solange der Call läuft.
    - **`calls.outcome IS NOT NULL` (F-09, KRITISCH) — das Ergebnis ist bestätigt.** `outcome` wird NICHT in api_beenden gesetzt, sondern erst in `correct_outcome` (app_routes.py:1961). compute_rubric BRAUCHT outcome: D-08-vergeigt-Guard prüft `outcome in {no_interest, lost}`, Dimension outcome_progression ist nur messbar wenn outcome != NULL. Merge VOR outcome → D-08-False-Green (Abbruch kriegt hohen Score) + outcome_progression fällt aus. Darum: outcome-confirmed ist Pflicht-Vorbedingung.
    - `pending_events(call_id) == 0` — `SELECT COUNT(*) FROM intent_event WHERE call_id=X AND handling_status='pending'` == 0. **NUR 'pending' zählt** — `failed`/`abstained` sind abgeschlossen (F-01/F-05), zählen NICHT als offen.
    Trigger-Punkt (F-09): die Vorbedingung wird nach jedem _persist_event_ref (letzte Event-Note) UND vor allem in `correct_outcome` (wo outcome gesetzt wird — typisch die zuletzt wahr werdende Bedingung) geprüft → erst bei Erfüllung aller drei mergen. (api_beenden allein darf NICHT zum Score führen — outcome fehlt dort noch.)
    **Hang-Edge (F-09) → H-2 Sweep ist jetzt KONKRET gebaut, NICHT mehr Executor-Discretion:** falls outcome nie gesetzt wird (User bricht das Outcome-Fenster ab) → Merge feuert nie → Call hängt "wird ausgewertet…". **Auflösung = der H-2 ZEITBASIERTE Sweep im `_periodic_tick` (siehe Sweep-Block unten):** nach `SCORE_SWEEP_AFTER_S` setzt der Daemon `outcome='unknown'` DIREKT (eigene DB-Session) und scort als D-08 `not_reached` (KEIN benigner Drop). **KEIN `ai_auto`/correct_outcome-Request-Pfad** (Gemini-Gegencheck): app_routes.py:1958 liest `g.user.id` → aus dem Daemon `RuntimeError: outside request context`. Der Sweep MUSS am periodischen Tick hängen (TAXO1-02 §0.1) — der blockierende `queue.get()` erwacht nie periodisch und kann den Sweep nicht tragen. Im SUMMARY den Sweep-Daten-Fluss belegen (hängender Call wird nach Frist benotet).
    **Queue-Timing explizit (F-02):** Events gehen bei EMIT (live) in die Queue + werden live benotet (handling_score, Plan 03); der MERGE wird erst durch ended+outcome freigeschaltet. INTERLOCK TAXO1-Plan-02 — falls TAXO1 das Timing anders fixt, im 3-Wege-Check abgleichen (SUMMARY-Notiz).

    Bei erfüllter Vorbedingung:
    1. **Audio-Gate D-09 (VOR Scoring, F-07):** lade call. Wenn `audio_health_score < AUDIO_HEALTH_GATE_THRESHOLD` (0.0-1.0!) ODER `audio_health_score IS NULL` (kein Buffer) ODER `n_high_confidence_events < MIN_HIGH_CONFIDENCE_EVENTS` (zu wenig Events über Tor-1-Schwelle, F-07) → Call "nicht bewertbar": eine **rubric_score-Zeile mit `status='not_gradable'`, `coaching_score=NULL`, reason ("poor_audio_health" bzw. "too_few_high_confidence_events") in dimensions/payload** schreiben (Option B: in rubric_score, NICHT in calls.coaching_score/calls.score_breakdown — calls bleibt dem alten Schreiber überlassen). SPEICHERN+FLAGGEN, nicht verwerfen (Anti-Training-Poisoning + Headset-Telemetrie). **(Auch dieser not_gradable-rubric_score-Write geht unter Tenant-GUC — M-4 gilt für JEDEN rubric_score-Write, nicht nur den Erfolgsfall.)**
    2. Sonst: events + speech_stats (get_speech_stats per-SID, K1, Req 9) + call + mode_config (Lookup-Schlüssel = `calls.call_mode`, N-4) laden → `compute_rubric(...)`.
    3. **M-4 Tenant-GUC SETZEN + ZWINGENDES try/finally (KRITISCH gegen Cross-Tenant-Leak):** Lies zuerst `call.tenant_id` (calls ist nicht FORCE-RLS → Lesen ohne GUC ok). Dann `set_current_tenant(str(call.tenant_id))` (database/db.py:43) — der after_begin-Hook setzt daraus transaction-local `app.tenant_id`, sodass FORCE-RLS WITH CHECK den Daemon-INSERT DURCHLÄSST. **Ohne diesen Schritt: rubric_score-INSERT fail-closed abgelehnt → die neue Note fehlt ewig, das Vorschau-Panel bleibt leer, lautlos (M-4).** **MUSTER-PFLICHT — `clear_current_tenant()` gehört in den `finally`-Block, NICHT hinter den Write:** weil der Slow-Lane-Consumer ein ENDLOS-Daemon-Thread ist (lebt über alle Calls), bleibt ein nach Exception (in compute_rubric ODER db.commit) NICHT geräumter Contextvar in die NÄCHSTE Schleifen-Iteration stehen → der nächste Call (evtl. anderer Tenant) schreibt unter der STALE tenant_id → **Cross-Tenant-Schreib-Risiko**. Skelett:
       ```
       set_current_tenant(str(call.tenant_id))   # vor Txn-Beginn (after_begin liest den Contextvar)
       try:
           # Schritt 4: UPSERT rubric_score (volle Aufschluesselung + status) + commit
           #            (Option B: KEIN calls.coaching_score-Write — das bleibt der alte Schreiber)
       finally:
           clear_current_tenant()                 # LÄUFT IMMER — auch bei Exception
       ```
       Reihenfolge: set_current_tenant MUSS vor dem Transaktions-Beginn des Writes stehen (GUC wird bei after_begin gesetzt). Wenn `call.tenant_id` NULL ist (Alt-Call ohne Tenant) → loggen + behandeln (kein stiller Abbruch). Gilt identisch für den not_gradable-Write (Schritt 1) UND den H-2-Sweep-Write (jeder pro-Call ein eigenes set→try→finally).
       **M-4-VERSCHÄRFUNG + MAX-RETRY/DEAD-LETTER (FOLD 23.06., Gemini — KEIN Silent-Drop, ABER auch kein Endlos-Block):** schlägt der Merge fehl (IntegrityError/permission-denied/RLS-Ablehnung/compute_rubric-Fehler) → der Call wird NICHT als 'erledigt' markiert (KEIN benigner `continue`/Done-Flag), sondern:
       - **GEDECKELTER Re-Queue:** der Call-Ende-Job trägt einen Versuchs-Zähler (`{'call_id':..., 'attempts': N}` in der Queue). Bei Fehler: LAUT loggen (`[SLOW] merge failed call=<id> attempt=N/{MAX}: <err>`) → `slow_lane.put({'call_id':..., 'attempts': N+1})`. **Cap: `SCORE_MAX_RETRIES` (config.py-Default 3).**
       - **DEAD-LETTER nach Cap:** `attempts >= SCORE_MAX_RETRIES` → KEIN weiterer Re-Queue (Job aus der Queue genommen) + LAUT als dead-letter loggen/alerten (`[SLOW] DEAD-LETTER call=<id> after {N} attempts: <err>`). Best-effort: eine rubric_score-Zeile mit `status='failed'` schreiben (falls der Write geht — bei RLS-Wurzelfehler geht auch der nicht, dann reicht das Log). So blockiert ein Poison-Call die Queue NICHT ewig, geht aber auch NICHT still verloren (versöhnt M-4 mit Verfügbarkeit).
       - **DAEMON ÜBERLEBT (Schritt-/Call-Isolierung, KRITISCH):** der Fehler failt NUR DIESEN Call. Pro-Call try/except auf Consumer-Ebene fängt die propagierte Exception (run_call_end_steps swallow-t NICHT, Plan 03) → re-queue-mit-Cap → `continue` zum nächsten Job. **Der Daemon-Thread darf NIE sterben** (sonst stehen ALLE Calls). Innerhalb EINES Calls bleibt es all-or-nothing (UPSERTs idempotent → sauberer Re-Run).
       Tests: `test_daemon_write_failure_requeues_with_cap` (Fehler → attempts+1 re-queued bis Cap, dann dead-letter, kein weiterer Re-Queue) + `test_daemon_survives_step_exception` (ein werfender Call-End-Step killt den Consumer NICHT, der nächste Call wird verarbeitet) + `test_daemon_write_failure_not_marked_done` (Call vor Cap NICHT als erledigt markiert).
    4. (im try) **UPSERT rubric_score-Zeile (F-03 idempotent) — DER EINZIGE Engine-Write (Option B):** `INSERT ... ON CONFLICT (call_id) WHERE origin='live' DO UPDATE SET ...` — referenziert den partiellen Unique-Index ux_rubric_score_live_call_id aus Plan 01. Volle Aufschlüsselung: coaching_score (kann NULL sein bei <50%), dimensions, is_provisional, measured_weight_pct, unmeasured_dimensions, status + tenant_id aus calls.tenant_id (D-11) + session_mode = aus calls.call_mode abgeleiteter Modus-Schlüssel (N-4, Spiegel). Bei ≥1 failed-Event: Marker "N Events nicht bewertbar" in rubric_score (payload/status, F-05). commit. (Doppelklick → genau 1 Live-Zeile.)
    **Option B (Audit RESOLVED 23.06.): KEIN `UPDATE calls.coaching_score`.** Grep-Beleg: kein heutiger Anzeige-Pfad liest calls.coaching_score direkt → die Spalte behält ihren EINEN alten Schreiber (correct_outcome:2011, unverändert). Ein Engine-Write dorthin wäre ein ZWEITER Schreiber (Kollision) + würde die alte Anzeige verändern (Re-Scope-Verstoß). Der calls.coaching_score-Spiegel wandert nach **08.23.2.SCORE-UI**. Die neue Note lebt in TAXO ausschließlich in rubric_score (Vorschau-Panel liest sie).
    6. **`clear_current_tenant()` im `finally` (M-4 Hygiene, IMMER):** der transaction-local GUC selbst clear-t bei COMMIT/ROLLBACK automatisch (db.py:73-75); clear_current_tenant räumt den Contextvar — und MUSS via finally auch laufen wenn der Write exception-t (sonst Leak in die nächste Daemon-Iteration, s.o.).
    7. Optional SocketIO 'score_ready' room-targeted (D-10 Pending→ready; Display-Mechanik = 999.2, hier nur Event) — NACH dem finally.
    Alles async im Slow-Lane-Consumer (D-10 — NIE synchron im Call-Ende).

    **H-2 ZEITBASIERTER HANG-SWEEP (im `_periodic_tick`, dockt am TAXO1-02 §0.1-Haken an — ERGÄNZT Plan-03-H-3, überschreibt es NICHT):**
    Erweitere `_periodic_tick()` (Plan 03 baute dort `_requeue_pending`): zusätzlich einen Sweep für hängende Calls. Pro Tick (gedrosselt, z.B. nicht jeden 5s-Tick — ein Zähler/Intervall reicht):
    `SELECT id FROM calls WHERE call_status='ended' AND outcome IS NULL AND ended_at < now() - SCORE_SWEEP_AFTER_S` → für jeden Treffer setzt der Daemon **NUR `row.outcome='unknown'` DIREKT über die eigene Daemon-DB-Session** (Entsperrung — der Call ist nicht mehr ewig „offen ohne Ergebnis"). Sobald outcome='unknown' gesetzt ist, matcht der Call die Sweep-Query nicht mehr (Re-Sweep-Schutz inhärent).
    **HART (FOLD 23.06., Gemini — der Sweep SCORT NICHT):** der Sweep löst **KEINEN** Scoring-Merge aus — **KEIN `slow_lane.put`, KEIN `run_call_end_steps`, KEIN compute_rubric, KEINE rubric_score-Zeile.** Ein abgebrochener Call ohne bestätigtes Ergebnis (oft ohne Ton/Transkript) soll NICHT benotet werden — er wird nur als „erledigt/unknown" geschlossen. **Der Scoring-Anstoß bleibt AUSSCHLIESSLICH an `correct_outcome` gebunden** (echte Outcome-Bestätigung durch den User). Folge: ein Sweep-Call bekommt KEINE rubric_score-Zeile → das Vorschau-Panel zeigt für ihn keinen Score (Row-absent-Handling, Task 3).
    **calls.outcome-Write ist erlaubt (NICHT der Score):** Option B verbietet den Engine-Write von `calls.coaching_score` — das `calls.outcome='unknown'` des Sweeps ist davon NICHT betroffen (nötige Hung-Call-Entsperrung; `calls.outcome` ist nicht der Score und nicht FORCE-RLS). Der Sweep triggert NICHT den alten correct_outcome-coaching_score-Write (er setzt outcome direkt im Daemon, ruft den Request-Handler nicht) → calls.coaching_score bleibt für Sweep-Calls beim alten Schreiber (NULL, da nie bestätigt).
    **CHECK-Constraint-Falle (Punkt 21, KRITISCH — sonst crasht der Sweep selbst):** `calls` hat `ck_calls_outcome` (erlaubt `'unknown'` — models.py:717, von outcome_service.py bereits genutzt → `outcome='unknown'` ist SAFE) UND `ck_calls_outcome_source` (erlaubt NUR `'ai_auto'|'ai_auto_unsicher'|'user_corrected'|NULL` — models.py:721). **Der Sweep lässt `outcome_source = NULL` (CHECK-erlaubt) — KEIN neuer Wert wie 'sweep_timeout' (würde die CHECK verletzen → Sweep-Crash).** Da der Sweep KEINE rubric_score-Zeile erzeugt (er scort nicht), gibt es auch keinen sweep_timeout-Marker dort; die Sweep-Herkunft steht im `[SLOW]`-Log. Beim Execute `inspect.sh constraints calls` gegen die Werte prüfen.
    **KEIN Aufruf des `ai_auto`/`correct_outcome`-Request-Pfads (Gemini-Gegencheck, code-verifiziert — KRITISCH):** `correct_outcome` liest `Call.user_id == g.user.id` (app_routes.py:1958, Flask-`g`) → aus dem Daemon-Thread (kein Request) wirft das `RuntimeError: working outside of request context` und der Tick stirbt. Der Sweep repliziert NUR das Daten-Ergebnis (outcome-Spalte setzen) im Daemon, NICHT den Request-Handler. (Ownership-Check entfällt im Sweep — der Daemon ist system-intern, nicht user-getriggert.)
    **Der Sweep-Write braucht KEINEN M-4-GUC** — er schreibt nur `calls.outcome` (calls ist NICHT FORCE-RLS, kein GUC nötig); er schreibt KEINE rubric_score-Zeile (FORCE-RLS) mehr. Per-Call in try/except, ein Fehler killt den Tick NICHT (andere hängende Calls werden weiter gesweept). So wird KEIN Call ewig „offen ohne Ergebnis" — und der Tick crasht nicht am Request-Kontext, ohne den Call zu benoten.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_coaching_score_cutover.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - **F-02:** Merge-Vorbedingung im Code: `grep -nE "ended|pending_events|handling_status *== *'pending'" services/slow_lane.py` belegt call_status=='ended' UND pending_events==0; ein Test belegt: Merge feuert NICHT solange call nicht ended (transient-WAHR-Schutz)
    - **F-09:** Merge-Vorbedingung enthält `outcome IS NOT NULL`: `grep -nE "outcome" services/slow_lane.py` belegt den outcome-confirmed-Check; ein Test belegt: Merge feuert NICHT solange outcome NULL (Call ended aber outcome offen → kein Score). Scoring-Anstoß sitzt in correct_outcome, NICHT in api_beenden (`grep -A40 "def correct_outcome" routes/app_routes.py` zeigt slow_lane-Merge-Anstoß; `grep -A30 "def api_beenden" routes/app_routes.py` zeigt KEINEN Merge-Anstoß). Hang-Edge-Auflösung (ai_auto/Sweep) im SUMMARY dokumentiert.
    - **F-01/F-05:** pending_events zählt NUR handling_status='pending' (`grep -n "handling_status" services/slow_lane.py` zeigt Filter auf 'pending'); failed/abstained zählen nicht — Test: Call mit 1 abstained + 1 failed + Rest scored → Merge feuert
    - **F-07:** `grep -E "AUDIO_HEALTH_GATE_THRESHOLD|MIN_HIGH_CONFIDENCE_EVENTS" config.py` == 2 (Defaults 0.5 / 3); Gate nutzt beide
    - **F-03:** `grep -iE "ON CONFLICT.*call_id.*origin.*live|on_conflict" services/slow_lane.py` belegt UPSERT auf dem partiellen Index
    - **M-4 (KRITISCH):** `grep -nE "set_current_tenant|clear_current_tenant" services/slow_lane.py` >= 2 (vor JEDEM rubric_score-Write gesetzt, danach geleert); `grep -n "from database.db import" services/slow_lane.py` deckt set_current_tenant/clear_current_tenant ab. **`clear_current_tenant()` steht im `finally`-Block jedes Write-Pfads — `grep -nB6 "clear_current_tenant" services/slow_lane.py` zeigt ein `finally:` direkt darüber (NICHT nur hinter dem Write).** Test `test_daemon_write_sets_tenant_guc`: der Merge-Pfad ruft set_current_tenant(call.tenant_id) VOR dem Write (Function-Call-Mock/Spy) — und ein Integrations-Test (server-seitig, Postgres) beweist, dass der Daemon-Write OHNE GUC fail-closed wäre, MIT GUC durchgeht (komplementär zu Plan-01 test_rubric_score_rls_requires_tenant_guc). **Leak-Test `test_daemon_clears_tenant_on_exception` (Gemini-Gegencheck, Pflicht): ein Write der eine Exception wirft (compute_rubric/commit monkeypatched auf raise) → nach dem Merge ist der Contextvar GELEERT (kein tenant_id-Leak in die nächste Iteration).** **Test-Anruf-Beleg (Pflicht): nach einem Live-Test-Call zeigt `inspect.sh sample rubric_score` eine Zeile MIT korrektem tenant_id + gesetztem coaching_score-Feld (in der rubric_score-Zeile) — beweist live, dass der GUC im Daemon greift (M-4 nicht durchgeschlagen). calls.coaching_score wird NICHT geprüft (Option B: bleibt alter Schreiber).**
    - **H-2:** `grep -A8 "def _periodic_tick" services/slow_lane.py` zeigt den Sweep (call_status='ended' AND outcome IS NULL AND ended_at-Frist) ZUSÄTZLICH zum H-3-_requeue_pending (Plan 03 koexistiert, nicht überschrieben); `grep -E "SCORE_SWEEP_AFTER_S" config.py services/slow_lane.py` >= 2 (Config + Nutzung); Sweep-Write läuft unter set_current_tenant (M-4, try/finally). **Kein Request-Pfad: `grep -nE "correct_outcome\(|\bg\.user|from flask import.*\bg\b" services/slow_lane.py` == 0 (der Sweep setzt outcome direkt über die Daemon-DB-Session, ruft NICHT correct_outcome/ai_auto — sonst RuntimeError outside request context).** Test test_sweep_no_request_context: Sweep läuft ohne Flask-Request-Context fehlerfrei. Test: ein Call ended+outcome=NULL+Frist überschritten → Sweep setzt outcome='unknown' → D-08 not_reached (kein hoher Score), Call nicht mehr "pending". **CHECK-Constraint-Safety: der Sweep setzt KEINEN neuen outcome_source-Wert — `grep -n "outcome_source" services/slow_lane.py` zeigt entweder nichts oder NULL/einen der ck_calls_outcome_source-erlaubten Werte (ai_auto/ai_auto_unsicher/user_corrected); Sweep-Herkunft steht in rubric_score.payload_jsonb.sweep_timeout (Option B — NICHT calls.score_breakdown). Test-Call-Sweep wirft KEINE CHECK-Verletzung (`inspect.sh logs-errors` ohne ck_calls_outcome_source-Fehler).**
    - Audio-Gate D-09: audio_health_score < Schwelle ODER IS NULL ODER < N high-conf → **rubric_score-Zeile mit status='not_gradable' + coaching_score=NULL** (0.0-1.0-Skala, NICHT 0-100; Option B: in rubric_score, NICHT calls)
    - speech_stats kommt aus get_speech_stats(sid) per-SID (Req 9): `grep "get_speech_stats" services/slow_lane.py` >= 1
    - **M-4-Hardening + Robustheit (FOLD 23.06.): `SCORE_MAX_RETRIES` in config.py; test_daemon_write_failure_requeues_with_cap (Fehler → attempts+1 bis Cap, dann dead-letter, kein weiterer Re-Queue + `[SLOW] DEAD-LETTER`-Log) + test_daemon_survives_step_exception (werfender Schritt killt Consumer NICHT) + test_daemon_write_failure_not_marked_done grün. KEIN Silent-Drop, KEIN Endlos-Block, Daemon stirbt nie.**
    - **H-2-Sweep SCORT NICHT (FOLD 23.06.): `grep -A8 "def _periodic_tick"`/Sweep-Block zeigt KEIN `slow_lane.put`/`run_call_end_steps`/`compute_rubric` im Sweep-Pfad — nur `calls.outcome='unknown'`; Test test_sweep_does_not_score (Sweep-Call bekommt KEINE rubric_score-Zeile)**
    - **Option B (Audit RESOLVED 23.06.): die Engine schreibt NUR rubric_score — `grep -n "coaching_score" services/slow_lane.py` zeigt KEIN `calls`-Objekt-Attribut-Write (kein `<call>.coaching_score =` / kein `UPDATE calls SET coaching_score`); calls.coaching_score behält seinen EINEN alten Schreiber (correct_outcome:2011, unverändert). Der einzige Engine-`calls`-Write ist `calls.outcome='unknown'` im Hung-Call-Sweep (nicht der Score).** SUMMARY: Grep-Beleg (0 Anzeige-Leser von calls.coaching_score) + Verschiebung des Spiegels nach SCORE-UI dokumentiert.
  </acceptance_criteria>
  <done>Call-Ende-Merge mit harter Vorbedingung (F-02/F-09), Audio-Gate (F-07), idempotenter UPSERT (F-03), failed/abstained-aware (F-01/F-05), M-4-GUC + Hard-Crash-bei-RLS-Fehler (kein Silent-Drop), async; via Hook-Registry eingehängt.</done>
</task>

<task type="auto">
  <name>Task 2 (RE-SCOPED, FOLD 23.06.): Alte Formel + Anzeige UNVERÄNDERT lassen — NUR /api/beenden Call-ended-Signal (F-02) + correct_outcome stößt zusätzlich async die Engine an (F-09). KEIN Löschen, KEIN Umschalten.</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py:743-839 (alle 5 alten Funktionen) + :659 (Stash-WRITE) + :1111 (Trend) + **:1992-2013 (correct_outcome Stash-READ-Block: base_bd['process_score'] + _calc_process_score-Fallback + _apply_outcome_modifier + coaching_score-Write + try/except — H-1, GANZER Block VERBATIM lesen vor Edit)**
    - Task 1 (Slow-Lane-Scoring-Anstoß + Merge-Vorbedingung) + services/slow_lane.py (slow_lane.put)
    - ssh ... 'bash scripts/inspect.sh schema calls' (welches Feld markiert ended — F-02)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §A2
  </read_first>
  <action>
    **RE-SCOPE (FOLD 23.06.): NICHTS LÖSCHEN, NICHTS UMSCHALTEN.** Die 5 alten Funktionen (`_calc_call_score` :743, `_OUTCOME_MODIFIERS` :757, `_calc_process_score` :768 inkl. frage_qualitaet=0.0, `_apply_outcome_modifier` :806, `_calc_coaching_score` :829) BLEIBEN STEHEN. Der heutige Stash-Write (api_beenden:659) + Stash-Read-Block (correct_outcome:1992-2013) + die alte sync `row.coaching_score=...`-Schreibung BLEIBEN UNVERÄNDERT — damit die alte Anzeige exakt wie heute weiterläuft (das ist die Re-Scope-Regel). **H-1 (Stash→Read-Einheit) ist hinfällig** — wir löschen die Funktionen nicht, also kann kein NameError entstehen.
    Nur ZWEI additive Änderungen (kein Umbau des Bestehenden):
    1. **F-02 Call-ended-Signal:** api_beenden markiert den Call als beendet (ended_at/status — das Call-Ende-Signal für den Merge), falls das Feld nicht ohnehin schon gesetzt wird. Beim Execute via `inspect.sh schema calls` bestätigen, welches Feld 'ended' markiert (ended_at IS NOT NULL bzw. status). **api_beenden stößt das Merge-Scoring NICHT an** (outcome hier noch NULL — F-09).
    2. **F-09 async Engine-Anstoß in correct_outcome (ADDITIV, NACH dem bestehenden Code):** NACHDEM correct_outcome outcome/followup persistiert (und die alte sync-Anzeige-Logik wie gehabt gelaufen ist) → ZUSÄTZLICH `slow_lane.put({'call_id': ...})` (Scoring-Anstoß für den Engine-Merge — jetzt ist outcome gesetzt, alle drei F-02/F-09-Vorbedingungen können erfüllt sein). Die bestehende Response/das bestehende Verhalten NICHT ändern (kein „score_pending"-Umbau der alten Anzeige) — der Engine-Merge läuft still im Hintergrund und befüllt rubric_score (+ ggf. calls.coaching_score, siehe ⚠-Audit-Punkt im objective). Ownership-Check + Control-Flow von correct_outcome unangetastet (Punkt 14).
    **⚠ calls.coaching_score-Schreibe-Ziel (Audit-gated, siehe objective):** Default in TAXO = die Engine schreibt die NEUE Note in **rubric_score** (Vorschau-Quelle); `calls.coaching_score` wird vom Engine-Merge NUR dann zusätzlich gespiegelt, wenn der Pre-Execute-grep belegt, dass KEIN heutiger Anzeige-Pfad die Spalte direkt rendert (sonst änderte sich die alte Anzeige → Re-Scope-Verstoß; dann bleibt der alte sync-Write die einzige calls.coaching_score-Quelle und der Spiegel wandert nach SCORE-UI). Diese Entscheidung fällt im Audit, NICHT hier blind.
    KEINE Löschung von _calcScore (Client) in dieser Welle (das ist SCORE-UI). Bestehende Anzeige + Interim-Verhalten unverändert.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "slow_lane.put\|_calc_call_score"'</automated>
  </verify>
  <acceptance_criteria>
    - **RE-SCOPE: die 5 alten Funktionen BLEIBEN — `grep -E "_calc_process_score|_calc_call_score|_apply_outcome_modifier|_calc_coaching_score|_OUTCOME_MODIFIERS" routes/app_routes.py` >= die heutigen Treffer (NICHT 0); frage_qualitaet=0.0 bleibt (wird in SCORE-UI gelöscht)**
    - correct_outcome behält seine bestehende sync-Logik UND hat ZUSÄTZLICH einen async Engine-Anstoß: `grep -A45 "def correct_outcome" routes/app_routes.py` zeigt `slow_lane.put` (additiv, nach dem bestehenden outcome-Persist) — die alte `.coaching_score =`-Schreibung bleibt unangetastet (kein Entfernen)
    - F-02: api_beenden markiert den Call als ended (ended_at/status) — `grep -A30 "def api_beenden\|def beenden" routes/app_routes.py` zeigt das ended-Signal; KEIN Merge-Anstoß in api_beenden
    - KEINE Anzeige-Leser umgestellt, KEIN _calcScore gelöscht (das ist SCORE-UI) — Tasks 3 baut nur das Vorschau-Panel daneben
    - Option B (Audit RESOLVED): correct_outcome behält die alte `.coaching_score =`-Schreibung (unverändert) — die Engine schreibt NICHT in calls.coaching_score (kein zweiter Schreiber). SUMMARY: grep-Beleg (0 Anzeige-Leser) + Spiegel-Verschiebung nach SCORE-UI.
  </acceptance_criteria>
  <done>Alte Formel + Anzeige unverändert; api_beenden setzt Call-ended-Signal (F-02); correct_outcome stößt zusätzlich async die Engine an (F-09); kein Löschen/Umschalten.</done>
</task>

<task type="auto">
  <name>Task 3 (RE-SCOPED, FOLD 23.06.): „Neu — Vorschau"-Panel in session_detail (read-only, roh, gestempelt) + NULL/not_gradable-Edge IM PANEL. Alte Anzeige + Leser UNANGETASTET.</name>
  <files>routes/dashboard.py, templates/ (session_detail-Template)</files>
  <read_first>
    - routes/dashboard.py:720-795 (session_detail — Kontext-Aufbau; das alte Score-Hero via _calc_call_score BLEIBT, NICHT anfassen)
    - `grep -n "render_template" routes/dashboard.py` → exakter Template-Name von session_detail (beim Execute bestätigen, z.B. templates/session_detail.html)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-01-rubric-score-tabelle-PLAN.md (rubric_score-Spalten: coaching_score, dimensions JSON, measured_weight_pct, unmeasured_dimensions, is_provisional, status)
    - static/nerve.css (existierende .n-*-Klassen/CSS-Variablen — falls überhaupt minimal Styling nötig; KEINE hardcoded Farben, CLAUDE.md-Regel)
  </read_first>
  <action>
    **Ziel (Design-Brief Abschnitt 6):** ein rohes, klar als „Neu — Vorschau" gestempeltes, READ-ONLY Panel NEBEN der bestehenden session_detail-Anzeige. Zweck = die ganze Kette (Rechnen→Speichern→RLS→Lesen→Anzeigen) an echten Calls EINMAL verifizieren. Bewusst ungestylt — Zahlen + Liste + Stempel, KEIN Layout/Redesign (das ist SCORE-UI).
    1. **dashboard.py session_detail — ADDITIV:** die zum Call gehörende `rubric_score`-Zeile (origin='live', via call_id/conversation_log_id) lesen und als separates Kontext-Objekt `rubric_preview` an das Template geben. **Die bestehende Score-/Anzeige-Logik (_calc_call_score-Recompute :774, Trend :795, Import :724) NICHT anfassen** — sie bleibt exakt wie heute (das alte Score-Hero zeigt unverändert die alte Zahl). KEINE Umstellung bestehender Leser.
    2. **Template — neues Panel** (eigener, klar abgegrenzter Block, NEBEN/unter dem bestehenden): Überschrift mit sichtbarem Stempel **„🧪 Neu — Vorschau (Beta)"** + Mini-Hinweis „neues Scoring, weicht bewusst von der alten Zahl ab". **(FOLD 23.06., Gemini — KRITISCH:** ohne Stempel meldet André/ein Tester die abweichende Engine-Zahl als Bug. Die Engine misst anderes als die alte Formel → Divergenz ist GEWOLLT, muss sichtbar markiert sein.) Rendert NUR lesend aus `rubric_preview`:
       - die neue `coaching_score` (Kopf-Zahl) — **Quelle = `rubric_preview.coaching_score` (das coaching_score-Feld der rubric_score-Zeile), NICHT `calls.coaching_score`** (Option B) — bei NULL/not_gradable: Klartext-Hinweis statt Zahl.
       - die 7 Dimensionen aus `rubric_preview.dimensions` (JSON): pro Dimension Name + Wert + Gewicht + „messbar? ja/nein".
       - die „nicht messbar weil…"-Gründe aus `unmeasured_dimensions` (+ is_provisional/measured_weight_pct als Hinweis „vorläufig, X% Gewicht messbar").
    3. **ROW-ABSENT + NULL/not_gradable-Edge IM PANEL (Req 6 + FOLD 23.06., Gemini — beide Fälle, nur hier):** **(a) ROW-ABSENT (KRITISCH): die `rubric_score`-ZEILE existiert noch GAR NICHT** — beim ersten Öffnen direkt nach Call-Ende ist der async-Merge fast sicher noch nicht durch (`rubric_preview` ist `None`/keine Zeile) → Panel zeigt „Score wird im Hintergrund berechnet…" (Klartext). **(b) ZEILE da, `coaching_score` NULL** bei <50%-Gewicht → „nicht genug Daten". **(c) status='not_gradable'** → „Audio zu schlecht zum Bewerten" bzw. „zu wenig auswertbare Momente" (F-07-Variante). **(d) status='failed' (Dead-Letter nach SCORE_MAX_RETRIES, Task 1 :274 — FOLD 23.06., Gemini-Bestätigungs-Review):** Zeile existiert MIT status='failed' (coaching_score NULL) → Panel zeigt **„Berechnung fehlgeschlagen (Admin benachrichtigt)"** — NICHT „wird im Hintergrund berechnet…" (sonst hängt der Hinweis für einen tot-gelegten Job ewig) und NICHT „nicht genug Daten" (irreführend). Reihenfolge der Panel-Prüfung: failed → not_gradable → coaching_score-NULL → row-absent → Score. **Alle Fälle (Row-absent UND field-NULL UND failed) explizit abfangen — KEIN Crash, KEINE leere 0, kein .toFixed/Division auf None/NULL.** (Abgebrochene Sweep-Calls bekommen nie eine Zeile → bleiben in (a); akzeptabel im Beta-Panel, der saubere „abgebrochen"-Status kommt in SCORE-UI.)
    4. **Styling minimal:** nur falls nötig eine schlichte `.n-*`-Klasse aus nerve.css (CSS-Variablen, KEINE hardcoded Farben — CLAUDE.md HART). Lieber ganz roh (Standard-Tabellen/Liste) als neue Farben erfinden. Der „Vorschau"-Stempel muss eindeutig signalisieren: das ist NICHT die echte Note (kein Kluger&DeNisi-Produkt-Feedback).
    **NICHT in dieser Welle:** kein _calcScore-Löschen, keine pip-launcher/performance/dashboard-Reader-Umstellung, kein Redesign — alles SCORE-UI.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "rubric_preview"'</automated>
  </verify>
  <acceptance_criteria>
    - session_detail gibt `rubric_preview` (rubric_score-Zeile des Calls) ADDITIV ans Template; `grep -n "rubric_preview" routes/dashboard.py` >= 1
    - **alte Anzeige unangetastet: `grep -n "_calc_call_score" routes/dashboard.py` >= die heutigen Treffer (NICHT entfernt); `grep "function _calcScore" static/pip-launcher.js` == heutiger Stand (NICHT gelöscht)**
    - Template hat einen klar gestempelten „Neu — Vorschau"-Block, read-only, der coaching_score + 7 Dimensionen (Wert/Gewicht/messbar?) + „nicht messbar weil…"-Gründe rendert
    - NULL/pending/not_gradable im Panel → Klartext-Hinweis, kein Crash (Test-Anruf belegt)
    - kein hardcoded Farb-Hex im neuen Markup (CLAUDE.md): `grep -nE "#[0-9a-fA-F]{6}" <neuer Template-Block>` == 0
  </acceptance_criteria>
  <done>„Neu — Vorschau"-Panel zeigt die neue Note + Dimensionen read-only neben der unveränderten alten Anzeige; NULL/not_gradable abgefangen; nichts Bestehendes umgestellt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>RE-SCOPED (FOLD 23.06.) + Option B: Engine rechnet die neue Note async + speichert sie NUR in rubric_score (KEIN calls.coaching_score — Audit RESOLVED) — Call-Ende-Merge mit harter Vorbedingung (F-02/F-09), Audio-Gate D-09 (F-07), idempotenter UPSERT (F-03), M-4-GUC + Hard-Crash-bei-RLS-Fehler. Alte Formel + Anzeige + calls.coaching_score-Schreiber UNVERÄNDERT; neues „Neu — Vorschau"-Panel in session_detail zeigt Note + 7 Dimensionen read-only (aus rubric_score). KEIN Display-Cutover (→ SCORE-UI).</what-built>
  <how-to-verify>
    Nach `bash deploy.sh production`:
    1. **Normaler Test-Anruf** (Cold-Call mit ≥1 Einwand, gutes Audio) als Test-User → Call beenden + Outcome bestätigen. Erwartung: die ALTE Anzeige bleibt exakt wie heute (unverändert). Claudian: `inspect.sh sample rubric_score` zeigt EINE neue Zeile (origin='live') mit Aufschlüsselung; das **„Neu — Vorschau"-Panel in session_detail** zeigt kurz danach die neue Note + die 7 Dimensionen (Wert/Gewicht/messbar?) + „nicht messbar weil…"-Gründe (DATEN-FLUSS durch die ganze Kette — der Kern dieser Welle).
    2. **F-02 vorzeitiger-Merge-Schutz:** Während eines laufenden Calls (BEVOR /api/beenden) → `inspect.sh sample rubric_score` zeigt KEINE Zeile für diesen Call (Merge feuert erst nach ended).
    2b. **F-09 outcome-vor-Score-Schutz:** Call beenden (api_beenden), aber Outcome-Fenster NOCH NICHT bestätigen → `inspect.sh sample rubric_score` zeigt für diesen Call KEINE Zeile, solange outcome NULL ist. Erst NACH Outcome-Bestätigung erscheint die rubric_score-Zeile (Vorschau-Panel füllt sich). Plus: ein bewusst-vergeigter Kurz-Call (Abbruch, Outcome „kein Interesse") → KEIN hoher Score im Panel (D-08 not_reached).
    3. **Re-Scope-Beleg — alte Anzeige unverändert:** das bestehende session_detail-Score-Hero + Dashboard + Performance zeigen GENAU wie vor dem Deploy (keine geänderte Zahl, kein „nicht genug Daten" in den ALTEN Bereichen) — NUR das gestempelte Vorschau-Panel ist neu. (Wenn eine ALTE Anzeige sich ändert → die Engine hat fälschlich calls.coaching_score geschrieben (Option-B-Verstoß) ODER einen alten Pfad angefasst → BLOCK. Gegencheck: `grep -n "coaching_score" services/slow_lane.py` zeigt KEINEN calls.coaching_score-Write.)
    4. **<50%-Szenario** (sehr kurzer Cold-Call, kaum messbare Dimensionen) → coaching_score=NULL → das **Vorschau-Panel** zeigt „nicht genug Daten" OHNE Crash/leere 0 (alte Anzeige unberührt).
    5. **Poor-Audio-Szenario** (D-09: schlechtes/leeres Audio ODER < 3 hoch-konfidente Events, F-07) → Call gespeichert, score_breakdown.status='not_gradable', das **Vorschau-Panel** zeigt „Audio zu schlecht zum Bewerten" bzw. „zu wenig auswertbare Momente".
    5b. **Dauer-Korrektheit (FOLD 23.06., Gemini-Blindfleck):** ein Test-Call mit langem Schweigen am Ende ODER spät bestätigtem Outcome → die angezeigte Call-Dauer (`dauer_sek`) entspricht der echten Gesprächsdauer (am /api/beenden festgehalten), NICHT der Zeit bis zum async-Merge/Sweep. Claudian belegt: Dauer nicht durch Wartezeit aufgebläht.
    6. **M-4 GUC live (HÖCHSTES STILLES RISIKO):** Beim normalen Test-Anruf (Schritt 1) prüft Claudian `inspect.sh sample rubric_score` — die Zeile MUSS `tenant_id` + ein gesetztes `coaching_score`-Feld (in der rubric_score-Zeile) haben. Wenn NACH Outcome-Bestätigung KEINE rubric_score-Zeile existiert (Vorschau-Panel bleibt leer) → M-4 ist durchgeschlagen (GUC nicht gesetzt, RLS hat den Daemon-rubric_score-Write fail-closed abgelehnt) → BLOCK. `inspect.sh logs-errors` auf RLS/permission-denied + das `[SLOW] FATAL`-Log prüfen (M-4-Hardening: bei RLS-Fehler crasht der Daemon laut, markiert NICHT als erledigt). (Das ist der teuerste stille Fehler — explizit live verifizieren, nicht nur "Test grün". `calls.coaching_score` wird NICHT geprüft — Option B, bleibt alter Schreiber.)
    7. **H-2 Hang-Sweep (SCORT NICHT, FOLD 23.06.):** einen Call beenden und das Outcome-Fenster bewusst NIE bestätigen (wegklicken/Tab schließen). Nach Ablauf von SCORE_SWEEP_AFTER_S (für den Test ggf. via ENV kurz setzen) → `inspect.sh sample calls` zeigt für den Call `outcome='unknown'` (geschlossen/entsperrt), aber `inspect.sh sample rubric_score` zeigt **KEINE Zeile** für diesen Call (der Sweep benotet NICHT — abgebrochener Call ohne Ergebnis bekommt keinen Score). `inspect.sh logs` zeigt den Sweep-Lauf **OHNE `RuntimeError: working outside of request context`** und OHNE Scoring-Anstoß für den Call.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe Abweichungen (welcher Screen, welches Szenario).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| correct_outcome (Client) → Scoring-Anstoß | User triggert async Job |
| Slow-Lane-Merge → rubric_score | einziger Engine-Write der Note (Option B; KEIN calls.coaching_score) |
| laufender Call → Merge-Trigger | vorzeitiger Merge mitten im Call (F-02) |
| poor-audio STT → Score | halluzinierter selbstbewusster Müll-Score |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-04-01 | I (False confidence) | poor-audio → halluzinierter Score | mitigate | D-09 Audio-Gate VOR Scoring (audio_health < Schwelle ODER NULL ODER < MIN_HIGH_CONFIDENCE_EVENTS → not_gradable, gespeichert+geflaggt, F-07). Fängt was <50%-Tor nicht fängt. |
| T-TAXO2-04-02 | D (DoS/Crash) | NULL coaching_score crasht das Vorschau-Panel | mitigate | NULL/not_gradable-Edge IM „Neu — Vorschau"-Panel ("nicht genug Daten"/"wird ausgewertet…" statt .toFixed(NULL)/Division). Alte Screens unberührt. Test-Anruf <50%-Szenario beweist. |
| T-TAXO2-04-03 | T (Double-write) | doppeltes correct_outcome → 2 rubric_score-Zeilen | mitigate | **F-03 idempotenter UPSERT ON CONFLICT (call_id) WHERE origin='live' (partieller Index Plan 01)** → genau 1 Live-rubric_score-Zeile. (calls.coaching_score: Option B, kein Engine-Write → keine Doppel-Schreiber-Gefahr dort.) |
| T-TAXO2-04-04 | I (Tenant leak) | rubric_score-Zeile ohne tenant_id | mitigate | tenant_id aus calls.tenant_id gestampt (D-11, RLS Plan 01). |
| T-TAXO2-04-05 | T (Premature merge) | Merge feuert mitten im Call → falscher Score | mitigate | **F-02 harte Vorbedingung: call_status=='ended' (aus /api/beenden) UND pending_events(handling_status='pending')==0. Queue-Timing explizit (Emit live, Merge erst ended). failed/abstained zählen nicht als pending.** |
| T-TAXO2-04-07 | I (False-Green / D-08-Umgehung) | Merge feuert nach api_beenden aber VOR outcome-Bestätigung → compute_rubric ohne outcome → D-08-vergeigt-Guard greift nicht, outcome_progression fällt aus → Abbruch-Call kriegt falsch-hohen Score | mitigate | **F-09: Merge-Vorbedingung verlangt zusätzlich `calls.outcome IS NOT NULL`; Scoring-Anstoß aus correct_outcome (outcome-aware, :1961), NICHT api_beenden (:98). Hang-Edge: ai_auto-outcome/Sweep, kein ewiges pending.** Code-verifiziert: outcome wird erst in correct_outcome gesetzt. |
| T-TAXO2-04-06 | E | LLM rutscht in Live-Call-Ende (sync) | mitigate | D-10 async — Scoring NUR Slow Lane, Call-Ende stößt nur an. grep: kein compute_rubric im correct_outcome-Sync-Pfad. |
| T-TAXO2-04-08 | D (Stille Verfügbarkeits-Regression / M-4, HÖCHSTES STILLES RISIKO) | Daemon-Write ohne Tenant-GUC → FORCE-RLS fail-closed → rubric_score-Note fehlt ewig (Vorschau leer), lautlos | mitigate | **M-4: set_current_tenant(str(call.tenant_id)) vor JEDEM rubric_score-Write (db.py:43, app.py:2200-Muster), clear_current_tenant() im `finally`. M-4-Hardening (FOLD 23.06.): RLS-Fehler → Hard-Crash + lautes Log, Call NICHT als erledigt markiert (re-queue), kein Silent-Drop. Test (Plan 01 + hier) beweist fail-closed ohne GUC / ok mit GUC. Test-Anruf belegt live: rubric_score-Zeile mit tenant_id + gesetztem coaching_score-Feld.** |
| T-TAXO2-04-11 | I (Cross-Tenant-Write / M-4-Leak) | Exception in compute_rubric/commit überspringt clear_current_tenant → stale tenant_id leakt in nächste Daemon-Iteration → nächster Call schreibt unter fremdem Tenant | mitigate | **clear_current_tenant() ZWINGEND im `finally`-Block (läuft auf jedem Pfad, auch Exception). Test test_daemon_clears_tenant_on_exception: ein Write der wirft → Contextvar danach leer (kein Leak).** Gemini-Gegencheck am Code verifiziert. |
| T-TAXO2-04-09 | A (Hang / H-2) | outcome nie bestätigt → Call bleibt ewig „offen ohne Ergebnis" | mitigate | **H-2: zeitbasierter Sweep im _periodic_tick (TAXO1-02 §0.1-Haken, NICHT queue.get()) nach SCORE_SWEEP_AFTER_S setzt NUR `outcome='unknown'` (Entsperrung) — SCORT NICHT (kein slow_lane.put/Merge/rubric_score; abgebrochener Call ohne Ergebnis wird nicht benotet, FOLD 23.06.). calls.outcome-Write braucht keinen GUC (nicht FORCE-RLS). Scoring bleibt allein an correct_outcome.** |
| T-TAXO2-04-13 | A (Daemon-Tod / Poison-Call) | ein Merge-Fehler reißt den Consumer-Thread ab → ALLE Calls stehen; ODER Endlos-Re-Queue blockiert die Queue | mitigate | **FOLD 23.06.: pro-Call try/except (Daemon stirbt nie) + MAX-RETRY-Cap (SCORE_MAX_RETRIES, Default 3) → dead-letter (laut loggen/alert, Job raus). run_call_end_steps propagiert (all-or-nothing/Call), Consumer fängt + re-queued-mit-Cap. Tests: test_daemon_survives_step_exception + test_daemon_write_failure_requeues_with_cap.** |
| T-TAXO2-04-12 | A (Tick-Crash / H-2) | Sweep ruft correct_outcome/ai_auto-Request-Pfad → `g.user.id` (app_routes.py:1958) im Daemon → RuntimeError outside request context → _periodic_tick stirbt (auch H-3-Re-Queue fällt mit aus) | mitigate | **Sweep setzt outcome DIREKT über die Daemon-DB-Session (kein Request-Handler-Aufruf). grep: kein `correct_outcome(`/kein `g.user` in services/slow_lane.py. Test test_sweep_no_request_context: Sweep läuft ohne Flask-Request-Context ohne RuntimeError.** Gemini-Gegencheck. |
| T-TAXO2-04-10 | D (Crash / H-1) | _calc_process_score gelöscht, Stash-Read-Fallback bleibt → NameError | **MOOT unter RE-SCOPE (FOLD 23.06.)** | Es wird NICHTS gelöscht (alte Formel + Stash bleiben unverändert). Kein halb-gelöschter Pfad → kein NameError-Risiko. Das Löschen + die H-1-Einheits-Disziplin wandern mit der Formel-Löschung in die SCORE-UI-Phase. |
</threat_model>

<verification>
- Test-Anruf-DATEN-FLUSS (riskante Welle): „Neu — Vorschau"-Panel rendert Note + 7 Dimensionen; <50%→NULL ohne Crash; poor-audio/zu-wenig-Events→not_gradable (F-07). Alte Anzeige unverändert (Re-Scope).
- F-02/F-09: Merge feuert NICHT vor call ended + outcome bestätigt; pending_events zählt nur 'pending' (failed/abstained abgeschlossen).
- F-03: idempotenter UPSERT ON CONFLICT auf partiellem Index (Doppelklick → 1 Zeile).
- RE-SCOPE: alte Formel + _calcScore + frage_qualitaet=0.0 BLEIBEN (NICHT gelöscht — SCORE-UI). correct_outcome hat additiv slow_lane.put (bestehende sync-Logik unangetastet).
- M-4-Hardening: RLS-Write-Fehler → Hard-Crash + lautes Log, Call NICHT als erledigt markiert (re-queue), kein Silent-Drop (test_daemon_write_failure_not_marked_done).
- rubric_score-Zeile (origin='live') pro Test-Call (inspect.sh sample) → im Vorschau-Panel sichtbar.
- D-10: Engine-Merge async; Vorschau-Panel zeigt „wird ausgewertet…" bis die Note da ist.
- Req 9: speech_stats aus get_speech_stats per-SID verkabelt.
- F-07: AUDIO_HEALTH_GATE_THRESHOLD + MIN_HIGH_CONFIDENCE_EVENTS in config.py.
- M-4: set_current_tenant vor jedem rubric_score-Daemon-Write + clear_current_tenant im `finally` (grep zeigt finally direkt über clear); test_daemon_clears_tenant_on_exception grün (kein Leak bei Write-Exception); M-4-Hardening: RLS-Fehler → Hard-Crash + `[SLOW] FATAL` + nicht-erledigt (test_daemon_write_failure_not_marked_done); Test-Anruf-rubric_score-Zeile trägt tenant_id + gesetztes coaching_score-Feld (GUC greift live).
- H-2: Sweep im _periodic_tick (SCORE_SWEEP_AFTER_S, ended+outcome NULL+Frist → outcome='unknown' DIREKT im Daemon, D-08 not_reached), koexistiert mit Plan-03-H-3; KEIN correct_outcome/ai_auto-Request-Pfad (g.user→RuntimeError); grep g.user/correct_outcome in slow_lane.py == 0; kein Call hängt ewig.
- H-1: MOOT unter Re-Scope (nichts gelöscht → kein NameError-Risiko; Löschung + H-1-Disziplin → SCORE-UI).
- Hook-Registry (Plan 03): Plan 04 hängt compute_rubric-Schritt via register_call_end_step + Sweep via register_periodic_tick_hook ein (kein Umschreiben von Merge/Tick).
</verification>

<success_criteria>
- RE-SCOPE (FOLD 23.06.) + Option B: Daten-Fundament gebaut (Engine rechnet + speichert NUR rubric_score; calls.coaching_score behält alten Schreiber — Audit RESOLVED), KEIN Display-Cutover. Alte Formel + Anzeige unverändert; alte Formel NICHT gelöscht.
- Async Score (D-10), Audio-Gate (D-09, F-07-Config), harte Merge-Vorbedingung (F-02/F-09), idempotenter UPSERT (F-03), M-4-GUC + Hard-Crash-bei-RLS-Fehler (kein Silent-Drop), H-2-Sweep, via Hook-Registry (Plan 03) eingehängt.
- „Neu — Vorschau"-Panel in session_detail zeigt Note + 7 Dimensionen read-only; NULL/not_gradable im Panel abgefangen (Req 6); kein Produkt-Display geändert (Req 5).
- Dauer am /api/beenden festgehalten (nicht durch async-Wartezeit verfälscht).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-04-SUMMARY.md` (inkl. RE-SCOPE-Beleg (alte Formel + Anzeige + calls.coaching_score-Schreiber unverändert; Vorschau-Panel zeigt Note+Dimensionen), **Option-B-Beleg (Audit RESOLVED: grep `coaching_score` in dashboard/performance/pip/templates == 0 → kein Anzeige-Leser; Engine schreibt NUR rubric_score, kein calls.coaching_score-Write in slow_lane.py; Spiegel → SCORE-UI)**, M-4-Hardening-Beleg (Hard-Crash + nicht-erledigt bei RLS-Fehler, test_daemon_write_failure_not_marked_done), F-02/F-09-Merge-Vorbedingungs-Beleg + vorzeitiger-Merge-Test, F-03-UPSERT-Beleg, F-07-Config-Defaults, Test-Anruf-rubric_score-Sample (origin='live') im Vorschau-Panel, Re-Scope-Beleg „alte Anzeige unverändert", <50%+poor-audio+zu-wenig-Events-Edge-Belege (im Panel), Dauer-Korrektheit-Beleg, F-02-Queue-Timing-INTERLOCK-Notiz mit TAXO1-Plan-02, **M-4-Beleg (set_current_tenant vor jedem rubric_score-Write + clear_current_tenant im `finally` (grep finally über clear) + test_daemon_clears_tenant_on_exception (kein Leak) + Test-Anruf-rubric_score-Zeile mit tenant_id + gesetztem coaching_score-Feld — GUC greift live), H-2-Beleg (Sweep im _periodic_tick setzt outcome='unknown' DIREKT im Daemon, sweep_timeout in rubric_score.payload — grep g.user/correct_outcome in slow_lane.py==0, test_sweep_no_request_context; hängender Call nach Frist zwangsbenotet, koexistiert mit Plan-03-H-3 ohne Überschreiben), Hook-Registry-Einhängung (register_call_end_step/register_periodic_tick_hook), N-4-Beleg (session_mode-Stamp aus calls.call_mode)**, 05-Log Re-Scope-Notiz).
</output>
