# 08.23.2.TAXO2-09 — Anon Live-vs-Stored — SUMMARY

**Status:** ✅ CODE-COMPLETE + auf Prod deployed (deploy.sh production GRÜN, Service active), Test-Gate
672 passed/6 skipped — **WARTET auf André's Live-Test-Anruf** (autonomous:false, Human-Verify-Checkpoint).
**NICHT auto-advanced.** Plan 08 + Scoring-Kette kommen ERST nach André's Live-Test.

Commits: `58c5124` (Bau Task 1-4) + `b6dd32b` (Anzeige-Payload-Härtung, Test-Gate-Fund) — beide gepusht.
Verzeichnis hardcoded `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` (Multi-Segment-Gotcha;
gsd-tools/gsd-code-review/gsd-verifier umgangen, STATE/ROADMAP hand-editiert).
**KEINE Migration** (reiner Code: claude_service / anonymization / deepgram).

---

## Was umgesetzt wurde (FOLD A-2 / SPEC Req 11, ANON-LIVE-ANSWER)

### Task 1 — Auto-Variante: Anzeige roh, Storage anonymisiert (Entkopplung)
`services/claude_service.py streame_auto_variante` (~:674-694): die frühere Zeile, die den Anzeige-Text
via `anonymize_output` reassignte (Anzeige wurde `[PERSON_A]` = Bug), ist entfernt.
- **Anzeige (pip_token_done):** eigenes `display_result`-Dict mit `cleaned_display` = `full_text.strip()`
  (ROHE echte Namen, Live-Nutzen). Wird ZUERST emittiert (Latenz Punkt 25: Berater hat die Antwort, bevor
  die Storage berechnet wird).
- **Storage-Version:** separates Rückgabe-`result` mit Feld `_storage_text` = `anonymize_for_storage(cleaned_display, sid)`.
  **Das ist der Vertrag, auf den Plan 08 (`record_suggestion_offer`) `depends_on`.**
- **DSGVO-Härtung (Test-Gate-Fund, Commit b6dd32b):** Anzeige- und Rückgabe-Dict sind GETRENNTE Objekte.
  Ursprünglich wurde `_storage_text` an dasselbe emittierte `result` angehängt → bei deferred SocketIO-
  Serialisierung hätte die anonymisierte Version ans Client-Payload leaken können. Jetzt unmöglich.
  Test `test_auto_variante_display_keeps_real_names` prüft explizit `'_storage_text' not in` Anzeige-Payload.

### Task 2 — Gemeinsamer Storage-Anon-Helper `anonymize_for_storage` (nie roh / nie verloren / geloggt)
`services/anonymization.py:640` — EINZIGE Quelle der Wahrheit fürs "Text für die Speicherung säubern":
- (a) Per-SID-Cache da → `anonymize_output(text, cache)` (konsistente Tokens wie Transkript).
- (b) Cache None / (a) wirft → FRISCHE `anonymize(text, cache_or_None)` (liefert nie rohe Namen:
  `[PERSON_X]`/Sentinel, auch ohne Mapping-Persistenz).
- (c) auch (b) wirft → `[ANON_FEHLER]` (geschwärzte Minimal-Form, Zeile bleibt schreibbar).
- (d) jeder Notweg (b/c) wird geloggt (`[ANON] storage-fallback ...`).
- Gibt NIE den rohen Eingabetext zurück. Lazy-Import von `live_session` (vermeidet Modul-Zyklus).

#### 3-Quellen-Storage-Vertrags-Tabelle (alle über `anonymize_for_storage`)
| Quelle | Pfad | Status in diesem Plan |
|---|---|---|
| **Auto-Variante** | `claude_service.py streame_auto_variante` → `_storage_text` | ✅ umgestellt (Task 1+2) |
| **Knopf-Pfad** | `deepgram_service.py:954` vor `record_ewb_click` | ✅ umgestellt — fail-OPEN ersetzt (früher blieb `_antwort` bei Anon-Fehler ROH; "_antwort bleibt unveraendert" entfernt). André-Entscheidung 22.06.: beide Wege gleich, nie roh. |
| **Keyword (Slot A)** | profil-statischer Vorschlag, PII-Risiko niedrig | ⏳ Code-Naht entsteht erst in **Plan 08** (`record_suggestion_offer`); dort `anonymize_for_storage(text, sid)` zur Konsistenz. Heute KEIN Keyword-Storage-Write vorhanden → nichts zu ändern (kein Bau über den Scope hinaus). |

Live-Anzeige bleibt überall roh; JEDE Storage-Schreibung geht durch `anonymize_for_storage`.

### Task 3 — NER-Über-Schärfe entschärft (PRONOMEN_WHITELIST ergänzt)
`services/anonymization.py:127` — die fehlenden Funktionswörter zur BESTEHENDEN `PRONOMEN_WHITELIST` ergänzt:
`'ihnen', 'euch', 'euer', 'eure', 'sich', 'einander'`. Wurzel: die Liste hatte `ihr/ihre/.../ihres` aber
nicht die Dativform `ihnen` → `Ihnen` wurde als PERSON → `[PERSON_A]` über-geschwärzt.
- **KEIN neuer Mechanismus, KEINE neue Konstante** (`STOPWORD_FUNCTION_TOKENS` == 0). Da `_is_whitelisted`
  schon lowercased vergleicht + an allen 6 NER-Stellen aufgerufen wird (:365/392/442/471/699/727), deckt
  diese EINE Listen-Änderung automatisch alle Pfade (sequenziell + parallel, spaCy + GLiNER).
- **DSGVO:** keine realen Nachnamen aufgenommen, keine Heuristik gelockert. `GENERIC_BERUF_WHITELIST`/LOC/
  ORG/Art-9/Regex unverändert. Mehrwort-`all()`-Logik kippt weiter bei einem echten Namen im Span.

### Task 4 — Tests `tests/test_anon_live_vs_stored.py` (5, alle GRÜN server-seitig)
Runtime-Behavior (Function-Call/Spy/Return), kein `getsource`:
1. `test_auto_variante_display_keeps_real_names` — Live-Anzeige (pip_token_done-Spy) trägt echten Namen,
   kein `[PERSON_*]`, und das Anzeige-Payload leakt `_storage_text` NICHT.
2. `test_auto_variante_storage_is_anonymized` — `_storage_text` trägt `[PERSON_A]`, kein roher Name.
3. `test_storage_fallback_no_raw_no_loss` — `anonymize_for_storage` mit cache=None: kein roher Name,
   nicht leer-verworfen, Notweg-Log emittiert (beweist: nie roh, nie verloren).
4. `test_ner_stopword_keeps_function_words` — "Wie kann ich Ihnen helfen?" → "Ihnen" bleibt.
5. `test_ner_stopword_still_redacts_real_name` — "Herr Müller, wie kann ich Ihnen helfen?" → "Müller"
   geschwärzt UND "Ihnen" bleibt (DSGVO-Balance beide Richtungen).

---

## Verifikation (Production-only, CLAUDE.md HART)
- `bash deploy.sh production` → server-seitiges Test-Gate **672 passed/6 skipped/5 deselected** (inkl. alle 5
  neuen Tests), POST-SUITE-Baseline-Check OK, Schild-Guard lief mit, Service `active (running)`. `[deploy] Done.`
- `inspect.sh grep` gegen Prod bestätigt: `cleaned_display`/`cleaned_storage`/`display_result` (claude:674-694),
  `def anonymize_for_storage` (anon:640) + Nutzung in deepgram:954 + claude:686, `'ihnen'` in Whitelist (anon:127).
- Erster Deploy-Versuch wurde vom Gate korrekt GEBLOCKT (Shared-Reference-Leak in einem Test) → kein Restart,
  alter Code blieb live; nach Härtung b6dd32b grün durchgelaufen.

## ⏳ OFFEN — André's Live-Test-DATEN-FLUSS (Human-Verify-Checkpoint, blocking)
Test-Anruf (Cold-Call + Meeting) mit echtem Test-Namen:
1. Auto-Varianten-Antwort auf dem Schirm zeigt den **ECHTEN Namen** (kein `[PERSON_A]`).
2. `inspect.sh sample` Transkript (+ suggestion_reactions sobald Plan 08 live) → gespeicherte Kopie **anonymisiert**.
3. "Wie kann ich Ihnen helfen?" → Storage-Kopie behält **"Ihnen"** (kein `[PERSON_A]`); echter Name im selben
   Satz weiter geschwärzt.
4. Beide Modi (Cold/Meeting) gleich.
**Resume-Signal:** "approved" oder beschreiben, welche Stelle (Live-Anzeige / Storage / Über-Schärfe) abweicht.

## DSGVO-Cross-AI-Notiz
Plan war 🔴 (DSGVO, Live-vs-Stored-Split + NER-Stopliste); die Cross-AI-Pflicht wurde bei der Plan-Erstellung
erfüllt (FOLD 23.06., "bau-fertig" inkl. Gemini-3-Sicht-Bestätigung der konservativen Whitelist-Wörter — alle
provably-harmlos, kein realer Nachname). Dieser Lauf = reine Execution des verriegelten Plans. Die Stopliste
ließ in den Tests keinen echten Namen durch (Balance Task 4 beide Richtungen).
