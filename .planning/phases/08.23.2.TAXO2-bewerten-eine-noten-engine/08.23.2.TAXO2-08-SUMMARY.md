---
phase: 08.23.2.TAXO2
plan: 08
subsystem: database
tags: [postgres, alembic, rls, dsgvo, anonymization, latency, suggestion_reactions, taxo2]

# Dependency graph
requires:
  - phase: 08.23.2.TAXO2-09 (anon-live-vs-stored)
    provides: "anonymisierte Storage-Version pro Antwort am Erfassen — claude_service result['_storage_text'] + anonymize_for_storage(text, sid)"
  - phase: 08.23.2.TAXO1
    provides: "intent_event.interaction_id (Moment-Klammer) + get_or_open_moment (RAM-Lookup) + calls(id) UUID-PK + tenant_orgs"
provides:
  - "public.suggestion_reactions (Roh-Erfassung jedes NERVE-Vorschlags pro Call, insert-only, anonymisiert) — CODE GEBAUT, Tabelle PENDING-SUPERVISED-DEPLOY"
  - "RAM-Puffer state['suggestion_offers'] + record_suggestion_offer (latenz-neutral) + drei Capture-Hooks (Auto/Knopf/Keyword)"
  - "flush_suggestion_offers (Call-Ende, insert-only, idempotent org+call_id-scoped, KEINE Flush-Anon)"
affects: [TAXO3, post-launch-uebernahme-scoring, suggestion-adoption]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Capture-am-Erfassen (anonymisierte Storage-Version Plan 09) + Call-Ende-Flush (KEIN Live-DB-Write, Punkt 25)"
    - "DELETE-then-INSERT Idempotenz strikt org+call_id-scoped (B3, strenger als objection_bridge)"
    - "FORCE RLS + OWNER nerve_app gemeinsam (FORCE greift auch fuer Owner -> kein Bypass)"

key-files:
  created:
    - alembic/versions/0019_suggestion_reactions_table.py
    - services/suggestion_capture.py
    - tests/test_suggestion_reactions.py
  modified:
    - database/models.py
    - services/live_session.py
    - services/claude_service.py
    - services/deepgram_service.py
    - services/einwand_keyword_matcher.py
    - routes/app_routes.py

key-decisions:
  - "Auto-Variante-Capture INNERHALB streame_auto_variante (cleaned_storage/_storage_text + _model_autovar in scope) statt am Call-Site — der Storage-Text entsteht dort"
  - "Knopf-model = config.MODEL_PIP_VARIANTE (streame_manual_ewb_variante:831), Auto-model = _model_autovar (config.MODEL_PIP_AUTOVAR bzw. Haiku-Fallback)"
  - "Keyword-suggestion_text = _profile_gegenargument(profile_einwand) via anonymize_for_storage(text, sid) (profil-statisch/niedrig-PII, aber Vertrag eingehalten)"
  - "ts_offered: ISO-String aus RAM via _parse_ts() -> datetime (DateTime-Spalte, SQLite/PG-konsistent)"
  - "tenant_id im Flush light aus der Call-Row resolved (Call-Ende-Request-Kontext, kein Live-Loop)"

patterns-established:
  - "record_suggestion_offer = thread-safe list-append unter state_lock (Muster record_ewb_click), KEIN DB/Netz/LLM"
  - "B1: interaction_id an allen 3 Hooks via get_or_open_moment IMMER gesetzt"

requirements-completed: ["SPEC Req 10 (FOLD A 2026-06-22)"]

# Metrics
duration: ~40min
completed: 2026-06-24
---

# Phase 08.23.2.TAXO2 Plan 08: Suggestion-Offer-Capture Summary

**Roh-Erfassung jedes NERVE-Vorschlags (Auto-Variante Slot B + Manueller Knopf + Keyword Slot A) pro Call in `public.suggestion_reactions` — insert-only, anonymisierte Storage-Version (Plan 09) am Erfassen, latenz-neutraler RAM-Puffer + EIN Call-Ende-Flush; Code gebaut + gepusht, Prod-Tabelle wartet auf supervidierten Deploy.**

> **STATUS: CODE GEBAUT + GEPUSHT — Tasks 1-4 fertig, jeder Task atomar committet. Plan steht am `checkpoint:human-verify` (Task 5). KEIN Deploy ausgefuehrt (André-Direktive: Prod-DB-Migration laeuft supervidiert). Die live-DB-Verifikationen (inspect.sh schema, SCHILD-Guard, server-seitiges pytest, Test-Anruf-Daten-Fluss, count=0) sind unten als PENDING-SUPERVISED-DEPLOY markiert.**

## Performance
- **Duration:** ~40 min
- **Tasks:** 4/4 auto-Tasks gebaut (Task 5 = Checkpoint, NICHT ausgefuehrt)
- **Files created:** 3 | **Files modified:** 6

## Accomplishments
- `SuggestionReaction` ORM-Model (Roh-Angebot-Spalten + nullable DEFERRED-Reaktions-Spalten + harter `call_id`-FK CASCADE + Schilder an Tabelle/allen nicht-trivialen Spalten).
- RAM-Puffer `state['suggestion_offers']` + `record_suggestion_offer` (thread-safe Append, latenz-neutral) + Reset; drei Live-Capture-Hooks (Auto/Knopf/Keyword) mit IMMER gesetzter `interaction_id` (B1) und bereits-anonymisierter Storage-Version (Plan 09).
- `flush_suggestion_offers` (Call-Ende, insert-only, idempotent strikt `org_id AND call_id`-scoped/B3, KEINE Flush-Anon) + Hook in der Finalisierungs-Transaktion neben der objection_bridge.
- Alembic-Migration `0019` (down_revision=0018, harter FK CASCADE, OWNER nerve_app, RLS ENABLE+FORCE+tenant_isolation, Schilder) + 8 Runtime-Behavior-Tests.

## Slot/Source-Mapping (aus dem lebenden Code abgeleitet)
| source | slot | Hook (Datei:Zeile) | model | suggestion_text (Anon-Quelle) | interaction_id (B1) |
|---|---|---|---|---|---|
| `auto_variante` | B | services/claude_service.py (in `streame_auto_variante`, nach `return result`-Block) | `_model_autovar` (config.MODEL_PIP_AUTOVAR / Haiku-Fallback) | `cleaned_storage` = `result['_storage_text']` (anonymize_for_storage, claude_service.py:686/693) | `get_or_open_moment(sid, mode, now)` neu |
| `manual_button` | B | services/deepgram_service.py (in `_run` nach record_ewb_click) | `config.MODEL_PIP_VARIANTE` | `_antwort` (anonymize_for_storage, deepgram_service.py:954) | `_btn_iid` (get_or_open_moment, :860) |
| `keyword` | A | services/einwand_keyword_matcher.py (nach emit_intent_event) | `None` | `anonymize_for_storage(_profile_gegenargument(profile_einwand), sid)` | `_kw_iid` (get_or_open_moment, :292) |

## Resolved Anon-Contract-Symbole (Plan 09 — VERIFIZIERT per grep, KEIN Drift)
- **Auto-Variante:** `services/claude_service.py:686` `cleaned_storage = anonymize_for_storage(cleaned_display, sid)` -> `:693` `result = {... '_storage_text': cleaned_storage}`. Plan-08-Hook reicht `cleaned_storage` direkt durch (kein erneuter `result`-Lookup noetig, da im selben Scope).
- **Knopf:** `services/deepgram_service.py:953-954` `from services.anonymization import anonymize_for_storage; _antwort = anonymize_for_storage(_antwort, _sid)`. Plan-08-Hook nutzt `_antwort` (schon anonymisiert) + `_btn_iid`.
- **Keyword:** `services/anonymization.py:640` `def anonymize_for_storage(text, sid)` — gemeinsamer Storage-Helper (nie roh, nie cache=None). Plan-08-Hook ruft ihn fuer den Profil-Vorschlag.
- **Drift:** KEINER. Alle drei Symbole live wie im Plan beschrieben. Der dem Berater LIVE angezeigte Text bleibt roh (Plan 09, `cleaned_display`/`display_result`) — Storage ist anonymisiert.

## Migration
- **Datei:** `alembic/versions/0019_suggestion_reactions_table.py`
- **revision:** `0019` | **down_revision:** `0018` (live Prod-HEAD via `ssh ... inspect.sh migrations` = `0018`; KEINE TAXO2-01/03/05-Migrationen deployed -> 0019 ist die naechste Nummer ueber HEAD).
- DDL-Paritaet zu `models.py`: alle Roh-Spalten + nullable DEFERRED-Spalten; `call_id UUID REFERENCES calls(id) ON DELETE CASCADE` (F-08); `tenant_id UUID REFERENCES tenant_orgs(id)`; `payload_jsonb JSONB NOT NULL DEFAULT '{}'`; 7 Indizes; `ALTER TABLE ... OWNER TO nerve_app`; RLS ENABLE+FORCE+`tenant_isolation` (nullif-fail-closed, 0014-Muster); COMMENT-Schilder. `downgrade()` symmetrisch (DROP POLICY -> DROP TABLE).

## Task Commits
1. **Task 1: SuggestionReaction ORM-Model** — `61674cf` (feat)
2. **Task 2: RAM-Puffer + 3 Capture-Hooks + Reset** — `abb24c5` (feat)
3. **Task 3: flush_suggestion_offers + Call-Ende-Hook** — `5f00e31` (feat)
4. **Task 4: Alembic 0019 + Tests** — `62c0428` (feat)

_Hinweis: Task 1 ist im Plan `tdd="true"`; da lokales pytest als Acceptance VERBOTEN ist (CLAUDE.md HART, no local-dev) und die DB-Tabelle erst beim supervidierten Deploy existiert, wurde Model + Test gebaut, aber die RED/GREEN-Gate-Ausfuehrung laeuft server-seitig beim Deploy-Gate (PENDING-SUPERVISED-DEPLOY). Siehe TDD Gate Compliance unten._

## Files Created/Modified
- `database/models.py` — `class SuggestionReaction(Base)` (neu, vor TranscriptSegment)
- `services/live_session.py` — `record_suggestion_offer` + `state['suggestion_offers']` init/Reset
- `services/claude_service.py` — Slot-B-Auto-Variante-Capture in `streame_auto_variante`
- `services/deepgram_service.py` — Knopf-Capture in `_run`
- `services/einwand_keyword_matcher.py` — Keyword/Slot-A-Capture nach emit
- `services/suggestion_capture.py` (neu) — `flush_suggestion_offers` + `_parse_ts`
- `routes/app_routes.py` — Call-Ende-Flush-Hook (db_conv-Transaktion, try/except)
- `alembic/versions/0019_suggestion_reactions_table.py` (neu)
- `tests/test_suggestion_reactions.py` (neu, 8 Runtime-Behavior-Tests)

## Acceptance — JETZT erfuellbar (grep-basiert, ohne Tabelle)
- `grep -c "class SuggestionReaction" database/models.py` == 1; `__tablename__='suggestion_reactions'` == 1; FK CASCADE deckt call_id; alle Roh-/DEFERRED-Spalten vorhanden, DEFERRED nullable=True; alle Spaltennamen ASCII; Schilder an allen nicht-trivialen Spalten. ✓
- `def record_suggestion_offer` == 1; Append-Muster (state_lock + setdefault); Reset `state['suggestion_offers'] = []` == 1; Hook in allen 3 Capture-Files (4 grep-Treffer inkl. Definition). ✓
- **Latenz (Punkt 25):** `record_suggestion_offer`-Body enthaelt KEIN get_session/commit/requests/anthropic/claude_client — nur list-append. ✓ (Test `test_record_suggestion_offer_does_no_db_write` mit get_session-Spy belegt es server-seitig.)
- **B1:** alle 3 Hooks rufen get_or_open_moment (Auto neu; Knopf `_btn_iid`; Keyword `_kw_iid`). ✓
- **Anon-Vertrag:** uebergebener suggestion_text = anonymisierte Storage-Version, KEIN cache=None im Capture-Pfad. ✓
- `def flush_suggestion_offers` == 1; DELETE org+call_id-scoped + leer->return 0; DEFERRED-Felder =None; KEINE Anon im Flush (`grep -E "anonymize_output|anonymize\(|anonymize_for_storage"` == 0 in suggestion_capture.py). ✓
- Migration: revision 0019 / down_revision 0018 / CASCADE FK / OWNER nerve_app / FORCE RLS / tenant_isolation. ✓
- Tests: KEINE Source-Presence-Assertions (kein getsource/hasattr/open().read()/subprocess). ✓

## Acceptance — PENDING-SUPERVISED-DEPLOY (brauchen die Prod-Tabelle)
> Diese laufen NACH dem supervidierten Deploy (manuelle Migration als postgres -> ALTER OWNER -> RLS/Schild-Verify -> dann deploy.sh). HEUTE NICHT ausgefuehrt.
- `inspect.sh schema suggestion_reactions` — alle Roh-Spalten + nullable DEFERRED + `call_id ... REFERENCES calls(id) ON DELETE CASCADE`; Owner nerve_app; RLS aktiv. **PENDING** (heute: "Did not find any relation named suggestion_reactions").
- `inspect.sh count suggestion_reactions` == 0 nach Test-Cleanup. **PENDING**.
- SCHILD-Guard GRUEN (suggestion_reactions + alle nicht-trivialen Spalten >= 10 Zeichen, server-seitig als nerve_app). **PENDING** (laeuft im deploy.sh-Postgres-Gate gegen nerve_test mit, sobald 0019 angewandt).
- Server-seitiges pytest `tests/test_suggestion_reactions.py` (insert_roundtrip + cascade F-08 + flush_writes_storage_text_verbatim + flush_delete_scoped_to_call + B1 + Punkt-25-Latenz + idempotent + empty-noop). **PENDING** (Gate gegen nerve_test).
- Test-Anruf-DATEN-FLUSS (autonomous:false): >=1 Auto-Variante + >=1 Knopf -> je 1 anonymisierte Zeile mit interaction_id != NULL, DEFERRED=NULL; Live-Antwort-Latenz subjektiv unveraendert; Live-Anzeige echte Namen (Plan 09). **PENDING** (human-verify Task 5).

## DEPLOY-Reihenfolge-Beleg (FOLD B P3)
`deploy.sh production` faehrt `alembic upgrade head` gegen **nerve_test** (Dump-Clone, Test-Gate, deploy.sh:163-164) und macht am Ende `systemctl restart` (deploy.sh:237). Die **Prod-`nerve`-Migration ist NICHT auto-im-deploy.sh** — sie laeuft SUPERVIDIERT (manuell als postgres -> ALTER OWNER nerve_app -> RLS/Schild-Verify) VOR `deploy.sh`, exakt wie TAXO1-01/0016. Grund: laeuft die Tabelle beim Gunicorn-Restart noch nicht in Prod, baut `Base.metadata.create_all()` (models.py-Init) eine NACKTE Tabelle (Owner postgres, KEINE RLS, KEIN Schild). Reihenfolge HART: **Migration(prod) zuerst, dann deploy.sh/Restart.**

## Decisions Made
Siehe `key-decisions` (Frontmatter). Kern: Capture am Entstehungsort des Storage-Texts (Auto: in `streame_auto_variante`); model-Quellen pro Pfad festgeklopft; ts-Parse fuer DateTime-Spalte; tenant_id light aus Call-Row im Flush.

## Deviations from Plan
**Keine inhaltlichen Abweichungen** — Plan 1:1 umgesetzt. Zwei begruendete Praezisierungen (kein Scope-Creep):
1. **Auto-Variante-Capture-Ort:** Der Plan nennt "claude_service.py:572 streame_auto_variante ... nach dem Streamen". Umgesetzt INNERHALB der Funktion direkt vor `return result`, weil dort `cleaned_storage` (= `_storage_text`), `_model_autovar`, `sid`, `slot` im Scope sind — der Storage-Text wird genau dort erzeugt. Kein Call-Site-Lookup noetig.
2. **`ts_offered`-Typ-Parse:** RAM-`ts` ist ein ISO-String (`utcnow().isoformat()`), Spalte ist `DateTime`. `_parse_ts()` parst defensiv (ValueError/None -> None). Korrektheits-Absicherung fuer den verbatim-Roundtrip-Test (Rule 1/korrekt).

## DSGVO-Anonymisierungs-Beleg
Der Flush schreibt `suggestion_text` 1:1 ohne erneute Anon (`grep anonymize* services/suggestion_capture.py` == 0). Die Anonymisierung passiert AM ERFASSEN mit dem lebenden Per-SID-Cache (Plan 09: `anonymize_for_storage` an Auto-/Knopf-/Keyword-Pfad; `anonymize_output(text, cache=None)` waere ein No-Op, anonymization.py:612 -> bewusst NICHT genutzt). DSGVO-Beweis liegt in Plan 09 (`test_anon_live_vs_stored.py`). Vault-Referenz `Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse` (Vault liegt ausserhalb dieses Repos) — die in-Repo-Referenz steht im Modul-Docstring von `services/suggestion_capture.py`.

## Known Stubs
Bewusste DEFERRED-Spalten (KEINE Stubs, Soll-Verhalten §6): `adoption_value`/`following_utterance_ref`/`reaction_class` werden in TAXO2 NICHT befuellt (post-Launch Uebernahme-Scoring, TAXO3). Im Model/Migration als nullable angelegt, im Flush explizit `None`. Test `*_insert_roundtrip` belegt NULL.

## TDD Gate Compliance
Plan-Frontmatter `type: execute` (nicht plan-level `tdd`); Task 1 ist `tdd="true"`. Wegen CLAUDE.md-HART (kein local-dev/pytest als Acceptance) konnte das RED/GREEN-Gate NICHT lokal gefahren werden. Test (`tests/test_suggestion_reactions.py`) + Model (`database/models.py`) sind gebaut; die Gate-Ausfuehrung (RED faellt ohne Tabelle, GREEN nach Migration) laeuft server-seitig im deploy.sh-Postgres-Gate gegen nerve_test. **WARNUNG:** keine separaten `test(...)`/`feat(...)`-RED/GREEN-Commits — Model und Test wurden je in einem feat-Commit gebaut (61674cf Model, 62c0428 Test), da die Tabelle erst beim supervidierten Deploy existiert.

## SUPERVISED-DEPLOY + Human-Verify (verbleibende Schritte — NACH diesem Plan)
1. **Prod-Migration supervidiert** (wie TAXO1-01/0016): als `postgres` `alembic upgrade head` gegen Prod `nerve` (wendet 0019 an) -> `ALTER TABLE public.suggestion_reactions OWNER TO nerve_app` ist in der Migration; verifizieren via `inspect.sh schema suggestion_reactions` (FK CASCADE, Owner, RLS) + SCHILD-Guard als nerve_app (peer-auth).
2. **Dann** `bash deploy.sh production` (Test-Gate gegen nerve_test laeuft test_suggestion_reactions.py + Schild-Guard; bei gruen Restart). NICHT vorher (sonst create_all-Race).
3. **Test-Anruf** als Test-User: >=1 Auto-Variante + >=1 Knopf -> nach Call-Ende `inspect.sh sample suggestion_reactions` (je 1 Zeile, interaction_id != NULL, suggestion_text anonymisiert, DEFERRED NULL); Latenz subjektiv unveraendert; Live-Anzeige echte Namen.
4. **F-08 (Claudian):** Test-Call loeschen -> `inspect.sh count suggestion_reactions` fuer die call_id == 0.
5. **Resume-Signal:** "approved" oder Abweichung beschreiben.

## Next Phase Readiness
Roh-Fundament fuer post-Launch Uebernahme-Scoring (TAXO3) gebaut: `interaction_id`-Korrelation zu intent_event gelegt, Reaktions-Spalten als nullable Reserve. Blocker: supervidierter Prod-Deploy + human-verify offen (Plan steht am Checkpoint).

## Self-Check: PASSED
- Alle 10 created/modified Dateien existieren (Write/Edit ohne Fehler).
- Alle 4 Task-Commits in git log: 61674cf, abb24c5, 5f00e31, 62c0428.

---
*Phase: 08.23.2.TAXO2-bewerten-eine-noten-engine*
*Plan: 08 — gebaut + gepusht, wartet auf supervidierten Deploy + human-verify*
*Completed (Code): 2026-06-24*
