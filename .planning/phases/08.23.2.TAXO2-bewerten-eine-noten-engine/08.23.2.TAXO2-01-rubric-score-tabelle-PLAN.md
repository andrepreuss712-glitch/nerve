---
phase: 08.23.2.TAXO2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - alembic/versions/XXXX_rubric_score_table.py
  - tests/test_rubric_score_schema.py
autonomous: true
requirements: ["SPEC Req 1", "SPEC Req 5 (Schema-Anteil)", "SPEC Req 8 (FK-Beleg)", "D-08", "D-11"]
user_setup: []

must_haves:
  truths:
    - "public.rubric_score existiert auf Production mit Kern-Spalten + Dimensions-Daten + payload_jsonb, Owner nerve_app, RLS ENABLE+FORCE"
    - "Jedes Trainings-Datum (Gerüst §4) landet belegt in einer festen Spalte ODER einem payload_jsonb-Key (Training-Fit-Pass)"
    - "SCHILD-Guard läuft server-seitig GRÜN für rubric_score + alle nicht-trivialen Spalten"
    - "rubric_score wird von keinem Live-Pfad geschrieben (nur Engine ab Plan 02/04 schreibt)"
    - "rubric_score hat einen PARTIELLEN Unique-Index UNIQUE(call_id) WHERE origin='live' (F-03) — macht Plan 04 ON CONFLICT valide, Training-Zeilen (origin='training', call_id NULL) bleiben frei"
    - "rubric_score.call_id ist ein HARTER FK auf calls(id) ON DELETE CASCADE (F-08 Variante a, DD-01-Konvention) — ein gelöschter Call hinterlässt 0 Waisen-Zeilen (DSGVO)"
  artifacts:
    - path: "database/models.py"
      provides: "class RubricScore ORM-Model (hybrid, indizierte Kern-Spalten + payload_jsonb + partieller Unique-Index + harter call_id-FK CASCADE)"
      contains: "class RubricScore"
    - path: "alembic/versions/XXXX_rubric_score_table.py"
      provides: "CREATE TABLE public.rubric_score + harter FK call_id ON DELETE CASCADE + partieller Unique-Index + OWNER nerve_app + RLS FORCE + COMMENTs"
      contains: "op.create_table"
    - path: "tests/test_rubric_score_schema.py"
      provides: "Real-Daten-/Schema-Smoke gegen rubric_score (Punkt 13) + F-08 Cascade-Test (gelöschter Call → 0 Waisen) + F-03 Unique-Konflikt-Test"
  key_links:
    - from: "database/models.py RubricScore"
      to: "public.rubric_score"
      via: "Alembic-Migration"
      pattern: "rubric_score"
    - from: "rubric_score.tenant_id"
      to: "public.tenant_orgs(id)"
      via: "RLS tenant_isolation policy (FORCE, nullif-fail-closed)"
      pattern: "tenant_isolation"
    - from: "rubric_score.call_id"
      to: "public.calls(id)"
      via: "harter FK ON DELETE CASCADE (F-08 Variante a, DD-01-Konvention wie CallEvent/TranscriptSegment)"
      pattern: "ondelete='CASCADE'"
    - from: "rubric_score (origin='live')"
      to: "Plan 04 ON CONFLICT(call_id)"
      via: "partieller Unique-Index ux_rubric_score_live_call_id (F-03)"
      pattern: "WHERE.*origin.*live"
---

<objective>
Lege die neue Tabelle `public.rubric_score` an — die EINE Quelle der Benotung, ab Tag 1 aufnahmefähig für Live UND Training (SPEC Req 1). Hybrid: indizierte feste Kern-Spalten + `payload_jsonb`-Reserve. Owner `nerve_app`, aber RLS mit `FORCE` aktiviert (D-11), damit die spätere Sichtbarkeits-Policy (COACH-Phase) ohne Schema-Umbau greift. Schild (Punkt 23) auf Tabelle + jede nicht-triviale Spalte.

**Cross-AI-Fold (REVIEWS F-03):** partieller Unique-Index `UNIQUE(call_id) WHERE origin='live'` — Plan 04 braucht ihn für ein valides `ON CONFLICT (call_id) DO UPDATE` (Postgres verlangt einen passenden Unique-Index, `index=True` allein reicht NICHT). Partiell, damit Training-Zeilen (`origin='training'`, `call_id` evtl. NULL/mehrfach) NICHT kollidieren.

**Cross-AI-Fold (REVIEWS F-08, DSGVO):** `rubric_score.call_id` ist ein HARTER FK `ON DELETE CASCADE` (Variante a). Beweis-basiert gewählt: die DD-01-Hauskonvention für neue calls-/conversation_logs-Kinder IST der harte Cascade-FK (`CallEvent.call_id` models.py:738 `ForeignKey('calls.id', ondelete='CASCADE')`, `TranscriptSegment` models.py:757, `0010:24 REFERENCES conversation_logs(id) ON DELETE CASCADE`); die Art.17-Lösch-Pipeline (`settings.py:217-219`) verlässt sich auf genau diese Cascade ("DD-01 live aber schlafend"). Ein loser FK wäre hier die Abweichung, nicht die Regel. Cascade = DSGVO-sauber by-design, kein Waisen-Rest.

Purpose: Ohne diese Tabelle kann keine Engine schreiben (Plan 02/04). Risk-low Schema-Addition ZUERST, bevor irgendetwas hineinschreibt (Wellen-Schnitt, TAXO1-D-02).
Output: `rubric_score`-Tabelle live auf Prod (leer), ORM-Model, Migration, Schema-Smoke-Test, belegter Training-Fit-Pass.

KEIN Schreiber in dieser Welle. KEINE Engine-Logik (Plan 02). KEIN Live-Pfad berührt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Phase-Verzeichnis ist EXAKT
`.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen (lesen sonst `08.23.2`). STATE/ROADMAP hand-editieren. SUMMARY in das hardcodierte Verzeichnis.

**Verify = Production only (CLAUDE.md HART):** `bash deploy.sh production` + Prod-Pytest-Gate (server-seitig) + `inspect.sh` gegen Prod. KEIN lokales pytest als Acceptance, KEIN `git pull` auf Prod. Migration als `postgres`-User, dann `ALTER TABLE ... OWNER TO nerve_app`. SCHILD-Guard MANUELL server-seitig (`tests/test_schild_guard.py`, OS-User nerve_app peer-auth).

**Migrations-Revision (HART, DEPENDS-ON-TAXO1):** Prod-HEAD = 0015. TAXO1 legt 0016/0017/0018 an. NICHT die Revision hardcoden — beim Execute den dann-aktuellen Alembic-HEAD lesen (`inspect.sh migrations`) und die rubric_score-Migration als nächste Nummer DARÜBER anlegen (`down_revision` = dann-HEAD). Datei umbenennen auf die echte Nummer.

**UTF-8-Regel:** Umlaute nur in user-facing Text. DB-Spalten/dict-keys/COMMENT-Identifier ASCII (ae/oe/ue/ss). COMMENT-Text-Strings dürfen Umlaute tragen (UTF-8 DB).
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- Muster zum Kopieren — bewiesen aus dem Code. KEINE Codebase-Exploration nötig. -->

RLS-Muster (alembic/versions/0014_crm_user_preferences_rls.py:49-55) — ABER ACHTUNG ABWEICHUNG:
0014 setzt KEINEN OWNER auf nerve_app (sonst BYPASS RLS). SPEC Req 1 verlangt aber OWNER nerve_app.
Auflösung: OWNER nerve_app SETZEN + zusätzlich `FORCE ROW LEVEL SECURITY` — FORCE erzwingt RLS
AUCH für den Tabellen-Eigentümer (0014:50 nennt FORCE "belt-and-suspenders"). So bleibt Req 1
(Owner nerve_app) UND D-11 (RLS-ready) gleichzeitig erfüllt.

```
ALTER TABLE public.rubric_score ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.rubric_score FORCE  ROW LEVEL SECURITY;   -- greift auch fuer OWNER nerve_app
CREATE POLICY tenant_isolation ON public.rubric_score
  USING (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
  WITH CHECK (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid);
```
GUC `app.tenant_id` wird transaction-local via after_begin-Hook gesetzt (0012-Amendment, existiert).

public-Tabelle als postgres anlegen, dann Owner nerve_app (alembic/versions/0010_add_transcript_segments.py:40):
```
op.execute("ALTER TABLE public.rubric_score OWNER TO nerve_app")
```

HARTER FK ON DELETE CASCADE — DD-01-Hauskonvention (bewiesen, F-08):
- models.py:738 CallEvent.call_id = Column(UUID_TYPE, ForeignKey('calls.id', ondelete='CASCADE'), nullable=False)
- models.py:757 TranscriptSegment.conversation_log_id = ForeignKey('conversation_logs.id', ondelete='CASCADE')
- 0010:24 alembic: "REFERENCES conversation_logs(id) ON DELETE CASCADE"
- settings.py:217-219: Art.17-Hard-Purge verlaesst sich auf "ON-DELETE-CASCADE (DD-01) live aber schlafend"
→ rubric_score.call_id = Column(UUID_TYPE, ForeignKey('calls.id', ondelete='CASCADE'), index=True, nullable=True)
  (nullable=True, weil Training-Zeilen call_id NULL haben; FK greift nur fuer gesetzte call_id)

PARTIELLER Unique-Index (F-03, fuer valides ON CONFLICT in Plan 04):
ORM (__table_args__):
  Index('ux_rubric_score_live_call_id', 'call_id', unique=True, postgresql_where=text("origin = 'live'"))
DDL (Migration):
  CREATE UNIQUE INDEX IF NOT EXISTS ux_rubric_score_live_call_id ON public.rubric_score (call_id) WHERE origin = 'live';
→ Plan 04 UPSERT referenziert genau diesen Index: ON CONFLICT (call_id) WHERE origin='live' DO UPDATE ...

COMMENT-Schild-Muster (Migration 0015, hauseigenes op.execute):
```
op.execute("COMMENT ON TABLE public.rubric_score IS '<Zweck>. Status: lebt. Schreibt services/...; liest routes/...'")
op.execute("COMMENT ON COLUMN public.rubric_score.<col> IS '<...>'")
```

intent_event-Vertrag (TAXO1 Plan 01 — DEPENDS-ON-TAXO1, payload_jsonb-Keys die rubric_score aggregiert):
- intent_event.intent_type (varchar64): vorwand/kaufsignal/aufschub/... (Taxonomie)
- intent_event.confidence (Float): Tor-1-Filter D-03 (>= ~0.70)
- intent_event.handling_score_numeric (SmallInt, NULL): pro-Event-Note 1-3 (Plan 03 füllt)
- intent_event.handling_status (varchar, F-01-Wurzelfix, ADDITIV in Plan 03): pending|scored|abstained|failed
- intent_event payload_jsonb-Keys: was_correct, is_simulation, origin_type, source_context, speaker_role, dimension_available

calls-Spalten (inspect.sh §A1, existieren, NULL-fähig — KEINE calls-Änderung in TAXO2):
- calls.id (UUID, PK) → FK-Ziel von rubric_score.call_id (ON DELETE CASCADE)
- calls.tenant_id (UUID, nullable) → rubric_score.tenant_id aus dem Call ableiten
- calls.coaching_score (double, NULL) → Kopf-Zahl-Ziel (Plan 04)
- calls.audio_health_score (double, NULL) → D-09-Gate (Plan 04)
- calls.call_mode (text): 'cold_call' | 'meeting_consented'
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `rubric_score` (CREATE) — existiert NICHT auf Prod (Beleg unten)
- `calls` (FK-ZIEL von rubric_score.call_id ON DELETE CASCADE; ansonsten nur LESE-Referenz fürs Schema-Design, KEINE calls-Änderung) — Beleg RESEARCH §A1
- `tenant_orgs` (FK-Ziel von rubric_score.tenant_id) — existiert (G/MEET Welle 1)

### inspect.sh-Beleg (VERBATIM aus RESEARCH §A1, 2026-06-11)
```
$ inspect.sh schema rubric_score
Did not find any relation named "rubric_score".

$ inspect.sh migrations
 version_num
-------------
 0015
(1 row)
```
**Beim Execute neu ziehen** (TAXO1 hat dann 0016+ angelegt): `inspect.sh migrations` → echter HEAD = down_revision.

**F-08 DSGVO-Beleg (verbatim, gegen lokalen git-Stand = Prod-Code):** Hauskonvention für neue calls-Kinder = harter FK ON DELETE CASCADE:
```
$ grep -n "ondelete='CASCADE'" database/models.py
738:    call_id = Column(UUID_TYPE, ForeignKey('calls.id', ondelete='CASCADE'), nullable=False)   # CallEvent
757:    conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id', ondelete='CASCADE'), nullable=False)  # TranscriptSegment
$ grep -n "ON DELETE CASCADE\|DD-01" routes/settings.py alembic/versions/0010_add_transcript_segments.py
settings.py:219:  # Die ON-DELETE-CASCADE (DD-01) ist live aber schlafend
0010:24:           REFERENCES conversation_logs(id) ON DELETE CASCADE
```
→ rubric_score.call_id folgt DD-01 (harter FK CASCADE), NICHT dem losen intent_event-Muster.

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Schreib-/Lese-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `RubricScore.call_id` | Engine schreibt (Plan 04) | DB rubric_score.call_id (UUID, **harter FK calls(id) ON DELETE CASCADE**, F-08) | ⬜ Task 3 Schema-Beleg + Cascade-Test |
| `RubricScore.tenant_id` | aus calls.tenant_id abgeleitet (Plan 04) | DB rubric_score.tenant_id (UUID) + RLS | ⬜ Task 3 Schema-Beleg |
| `RubricScore.coaching_score` | Engine (Plan 04, = Kopf-Zahl Spiegel) | DB rubric_score.coaching_score (double, NULL) | ⬜ Task 3 |
| `RubricScore.is_provisional / measured_weight_pct / unmeasured_dimensions` | Engine (Plan 02 D-08) | feste Spalten (bool/float/jsonb) | ⬜ Task 3 |
| `RubricScore.dimensions` | Engine (Plan 02) | payload_jsonb ODER feste jsonb-Spalte | ⬜ Task 3 |
| Unique(call_id) WHERE origin='live' | Plan 04 ON CONFLICT (F-03) | partieller Unique-Index ux_rubric_score_live_call_id | ⬜ Task 3 Konflikt-Test |
| `was_correct` (Training-GT) | Training-Pfad später (DEFERRED) | payload_jsonb-Key | Fit-Pass Task 0 |

## Verbindungs-Karte (Punkt 22)
- **rubric_score** existiert nicht (inspect.sh oben) → reine Neu-Tabelle, kein bestehender Schreiber/Leser. grep `grep -rn "rubric_score\|RubricScore" services/ routes/ database/` == 0 vor diesem Plan (Beleg in Task 0).
- **tenant_id-Anker:** calls.tenant_id existiert (UUID, RESEARCH §A1) → rubric_score.tenant_id wird daraus abgeleitet (Plan 04), FK auf tenant_orgs erlaubt (0014:40-Muster).
- **call_id-FK (F-08):** calls.id existiert (UUID PK, §A1). rubric_score.call_id = harter FK ON DELETE CASCADE — bewiesene DD-01-Konvention (models.py:738/757, 0010:24, settings.py:219). Loser FK wäre die Abweichung. nullable=True (Training-Zeilen ohne call_id), FK greift nur für gesetzte Werte.
- **Owner-Entscheidung bewiesen:** 0014 setzt KEINEN nerve_app-Owner um RLS zu halten; 0010 setzt nerve_app-Owner für public-Tabellen. SPEC Req 1 verlangt Owner nerve_app → Konflikt aufgelöst via FORCE RLS (0014:50-Muster greift auch für Owner). Beleg: 0014:17-18 + 0014:50.

<tasks>

<task type="auto">
  <name>Task 0: Training-Scoring-Fit-Pass belegen (SPEC Req 1 Pflicht-Artefakt)</name>
  <read_first>
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §D (Fit-Tabelle)
    - Nerve-Vault/04 Entscheidungen/NERVE TAXO-Gerüst (verriegelt).md §4 (7 Dimensionen, Training-Ground-Truth, BARS)
    - services/training_service.py:1122 + :1173 (heutiger Training-Scorer-Shape, NUR lesen, NICHT ändern)
  </read_first>
  <action>
    Schreibe in die Plan-SUMMARY (Task 0) die belegte Fit-Tabelle: jedes Trainings-Datum aus Gerüst §4 → konkrete rubric_score-Spalte ODER payload_jsonb-Key. Verbatim aus RESEARCH §D übernehmen + gegen das finale Schema (Task 1/2) prüfen. Pflicht-Zeilen:
    - 7 geteilte Dimensionen (je: score, weight, available, sample_size, beleg_ref) → feste jsonb-Spalte `dimensions` (Live+Training teilen das Set).
    - Gesamt-Score → feste Spalte `coaching_score`.
    - origin (live/training) → feste Spalte `origin`. session_mode → feste Spalte `session_mode`.
    - Ground-Truth gespielt-vs-erkannt (was_correct) → payload_jsonb-Key (training-only).
    - Szenario-Bezug (scenario_id) → payload_jsonb-Key (training-only).
    - Ground-Truth-Erkennungs-Score (D-04 TRAINING) → payload_jsonb-Key ODER hoechstens EINE additive Spalte.
    - unvorhergesehene Trainings-Felder → payload_jsonb (Reserve).
    Beweis-Aussage: spätere Training-Umstellung = NUR Verkabelung (generate_scoring schreibt dieselbe Tabelle), höchstens EINE additive Spalte, NIE Maschinen-Neubau.
    **F-03-Hinweis im Fit-Pass:** der partielle Unique-Index `WHERE origin='live'` darf den Training-Fit NICHT brechen — Training-Zeilen (origin='training') sind vom Index AUSGENOMMEN, dürfen also mehrfach/ohne call_id existieren (Training keyt auf conversation_log_id/Session, nicht call_id). Belegen, dass dies die Training-Idempotenz NICHT verhindert (Training-Verkabelung definiert ihren eigenen Schlüssel später).
    Schreibe ZUSÄTZLICH einen Eintrag in `Nerve-Vault/05 Log` (bewusste Gerüst-§4-Abweichung: erst Live verkabeln, Training DEFERRED).
  </action>
  <verify>
    <automated>grep -c "Training-Scoring-Fit" .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY enthält eine Fit-Tabelle mit ≥8 Zeilen (jedes Trainings-Datum → Spalte/JSONB-Key)
    - Jede Zeile nennt eine konkrete Spalte ODER einen konkreten payload_jsonb-Key (nicht "irgendwo")
    - F-03-Begründung dokumentiert: partieller Unique-Index (origin='live') bricht Training-Fit NICHT
    - Nerve-Vault/05 Log trägt den Abweichungs-Eintrag (Gerüst §4 "beide auf einmal" → erst Live)
  </acceptance_criteria>
  <done>Belegter Fit-Pass in SUMMARY + 05-Log-Abweichungs-Eintrag + F-03-Training-Kompat-Begründung.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: ORM-Model RubricScore in database/models.py (hybrid + harter FK CASCADE + partieller Unique-Index + Schilder)</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py (Call-Model + transcript_segments-Stil als Vorbild; class IntentEvent falls TAXO1 deployed; **models.py:738 CallEvent.call_id ondelete='CASCADE' als FK-Vorbild — F-08**)
    - ssh ... 'bash scripts/inspect.sh schema calls' (tenant_id/coaching_score/audio_health_score-Typen + calls.id UUID-PK fuer FK)
    - alembic/versions/0014_crm_user_preferences_rls.py:38-56 (RLS+tenant_id-Muster)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §F (is_provisional-Schema) + §D (Fit)
  </read_first>
  <behavior>
    - Model instanziierbar mit Pflicht-Kern + payload leer; ASCII-Spaltennamen
    - is_provisional default False, measured_weight_pct nullable, unmeasured_dimensions default-leer
    - dimensions als JSON-Spalte (kein NOT NULL ohne server_default)
    - call_id ist harter FK ondelete='CASCADE' (F-08), nullable=True
    - partieller Unique-Index ux_rubric_score_live_call_id (call_id, WHERE origin='live') existiert (F-03)
  </behavior>
  <action>
    NEUE Klasse `class RubricScore(Base)` (neue Klasse → Punkt-14-Control-Flow nicht anwendbar; Punkt 21 erfüllt oben). `__tablename__ = 'rubric_score'`.
    Indizierte Kern-Spalten (feste relationale Spalten):
    - `id = Column(UUID_TYPE, primary_key=True, default=...)` (UUID-PK wie calls; nutze das vorhandene UUID_TYPE/Default-Muster aus models.py).
    - `call_id = Column(UUID_TYPE, ForeignKey('calls.id', ondelete='CASCADE'), index=True, nullable=True, comment="Bezug zum Call. HARTER FK ON DELETE CASCADE (F-08/DD-01-Konvention wie CallEvent models.py:738) — geloeschter Call raeumt die Note DSGVO-sauber mit. nullable: Training-Zeilen ohne call_id.")` (**F-08: harter FK CASCADE, NICHT loser FK** — DD-01-Beweis siehe Verbindungs-Karte).
    - `conversation_log_id = Column(Integer, index=True, nullable=True, comment="Bezug zur Session/conversation_logs. Live=aus Call, Training=aus Trainings-Session.")`
    - `session_mode = Column(String(32), index=True, nullable=False, comment="Modus der Bewertung: cold_call|meeting_consented|training. Bestimmt den Gewichtssatz (D-01/D-04).")`
    - `origin = Column(String(16), index=True, nullable=False, comment="Herkunft der Note: live|training. SPEC Req 1 — eine Tabelle fuer beide Welten. Steuert den partiellen Unique-Index (F-03).")`
    - `coaching_score = Column(Float, nullable=True, comment="Gesamt-Kopf-Zahl (0-100). NULL wenn <50% Gewicht messbar (Proration, D-02) oder not_gradable (D-09). Spiegel von calls.coaching_score (Plan 04).")`
    - `is_provisional = Column(Boolean, nullable=False, default=False, comment="Vorlaeufig-Marker (D-08): Score ueber der 50%-Schwelle aber mit weggeprorateten Dimensionen. Anzeige 999.2.")` (is_*-Suffix → L-04 trivial, aber bewusst beschildert wegen D-08-Bedeutung)
    - `measured_weight_pct = Column(Float, nullable=True, comment="Anteil messbaren Gewichts am modus-konfigurierten Maximum (D-02/D-08). <0.5 → coaching_score NULL.")`
    - `unmeasured_dimensions = Column(JSON, nullable=True, comment="Liste der nicht gewerteten Dimensionen + Grund (n/a vs vergeigt, D-08). Goldstaub fuer 999.2-Erklaerung + ML.")`
    - `dimensions = Column(JSON, nullable=True, comment="Volle Aufschluesselung pro Dimension (D-05/Req 5): je Dim {score, weight, available, sample_size, beleg_ref, marker[]}. Beleg-Referenz = Transkript-/intent_event-Verweis, KEIN freier LLM-Text.")`
    - `status = Column(String(24), nullable=True, comment="Bewertungs-Status: scored|pending|not_gradable (D-09 poor_audio_health). NULL = noch nicht gelaufen.")`
    - `tenant_id = Column(UUID_TYPE, index=True, nullable=True, comment="Mandanten-Abschottung (D-11, RLS-ready). Abgeleitet aus calls.tenant_id. Policy erst COACH.")`
    - `payload_jsonb = Column(JSON, nullable=False, server_default='{}', comment="Reserve + Training-only-Felder (was_correct, scenario_id, ground_truth_score) ohne spaetere Migration. SPEC Req 1.")`
    - `score_schema_version = Column(SmallInteger, nullable=False, default=1, comment="Format-Version der Aufschluesselung fuer spaetere Bumps.")`
    - `created_at = Column(DateTime, default=utcnow)` (trivial, kein Schild).
    `__table_args__` (Tupel):
    - `Index('ux_rubric_score_live_call_id', 'call_id', unique=True, postgresql_where=text("origin = 'live'"))` — **F-03 partieller Unique-Index** (Plan 04 ON CONFLICT). `text` aus sqlalchemy importieren (Modul-Ebene, nicht von Spalte ueberdeckt — vgl. TranscriptSegment-Hinweis models.py:761).
    - Tabellen-Schild-comment:
      "Single Source der Benotung (BARS + Proration), Live + Training. Eine Zeile pro bewerteter Call/Session. Hybrid: indizierte Kern-Spalten + payload_jsonb. call_id harter FK CASCADE (F-08/DD-01). Partieller Unique-Index (call_id, origin=live) fuer idempotenten Upsert (F-03). Status: lebt (neu, TAXO2). Schreibt services/slow_lane.py (Engine, Plan 02/04); liest routes/dashboard.py + performance.py (999.2)."
    Importiere fehlende Typen (Boolean/Float/JSON/SmallInteger/UUID_TYPE/ForeignKey/Index/text) konsistent zum models.py-Stil.
  </action>
  <verify>
    <automated>grep -c "class RubricScore" database/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class RubricScore" database/models.py` == 1; `grep "__tablename__ = 'rubric_score'" database/models.py` == 1
    - `grep -E "ForeignKey\('calls.id', ondelete='CASCADE'\)" database/models.py` deckt rubric_score.call_id ab (F-08 harter FK CASCADE)
    - `grep "ux_rubric_score_live_call_id" database/models.py` == 1 + `grep -E "postgresql_where=text\(\"origin = 'live'\"\)" database/models.py` vorhanden (F-03 partieller Unique-Index)
    - Alle festen Spalten als grep-Treffer: `grep -E "session_mode|origin|coaching_score|is_provisional|measured_weight_pct|unmeasured_dimensions|dimensions|payload_jsonb|tenant_id" database/models.py` deckt alle ab
    - `grep -c "comment=" database/models.py` deckt call_id/session_mode/origin/coaching_score/is_provisional/measured_weight_pct/unmeasured_dimensions/dimensions/status/tenant_id/payload_jsonb/score_schema_version ab (nicht-trivial = Schild)
    - Alle Spaltennamen ASCII (kein Umlaut): manuelle Sicht — keine ä/ö/ü/ß in Identifiern
  </acceptance_criteria>
  <done>RubricScore-Model vollständig (hybrid, harter call_id-FK CASCADE, partieller Unique-Index), alle nicht-trivialen Schilder gesetzt, keine Schreiber.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic-Migration rubric_score (CREATE als postgres + harter FK CASCADE + partieller Unique-Index + OWNER nerve_app + RLS FORCE + COMMENTs)</name>
  <files>alembic/versions/XXXX_rubric_score_table.py</files>
  <read_first>
    - alembic/versions/0014_crm_user_preferences_rls.py (RLS ENABLE+FORCE+tenant_isolation-Policy, nullif-fail-closed)
    - alembic/versions/0010_add_transcript_segments.py:24+:40 (REFERENCES ... ON DELETE CASCADE — F-08-Vorbild + ALTER OWNER nerve_app fuer public-Tabelle)
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-op.execute-Stil)
    - ssh ... 'bash scripts/inspect.sh migrations' (echter HEAD beim Execute = down_revision)
  </read_first>
  <action>
    Neue Migration. `down_revision` = aktueller Alembic-HEAD (beim Execute via `inspect.sh migrations` lesen — TAXO1 hat 0016/0017/0018 angelegt; NICHT hardcoden). Datei auf die echte nächste Nummer benennen (z.B. 0019/0020).
    upgrade():
    1. `op.create_table('rubric_score', ...)` ODER `op.execute("CREATE TABLE IF NOT EXISTS public.rubric_score (...)")` — alle Spalten aus Task 1 (DDL-Parität).
       - **call_id `UUID REFERENCES public.calls(id) ON DELETE CASCADE`** (F-08, harter FK; DD-01-Konvention wie 0010:24). NICHT loser FK.
       - tenant_id `UUID REFERENCES public.tenant_orgs(id)` (FK erlaubt). payload_jsonb `JSONB NOT NULL DEFAULT '{}'`.
    2. Indizes: call_id, conversation_log_id, session_mode, origin, tenant_id (`CREATE INDEX IF NOT EXISTS ix_rubric_score_<col> ...`).
    3. **F-03 partieller Unique-Index:** `CREATE UNIQUE INDEX IF NOT EXISTS ux_rubric_score_live_call_id ON public.rubric_score (call_id) WHERE origin = 'live';` (Plan 04 ON CONFLICT — Postgres verlangt diesen Index; partiell, damit Training-Zeilen frei bleiben).
    4. `op.execute("ALTER TABLE public.rubric_score OWNER TO nerve_app")` (public, postgres legt an; 0010-Muster).
    5. RLS (D-11): `ENABLE ROW LEVEL SECURITY` + `FORCE ROW LEVEL SECURITY` (FORCE greift auch fuer Owner nerve_app — Auflösung Req1+D11-Konflikt) + `CREATE POLICY tenant_isolation ... USING/WITH CHECK (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)` (0014:51-55 verbatim, nullif-fail-closed).
    6. Schilder: `COMMENT ON TABLE` + `COMMENT ON COLUMN` für jede nicht-triviale Spalte (Texte aus Task-1-comment=). op.execute-Stil (0015). Umlaute in COMMENT-Strings OK.
    downgrade(): symmetrisch (DROP POLICY, DROP INDEX inkl. ux_rubric_score_live_call_id, DROP TABLE; OWNER muss nicht zurückgesetzt werden; der FK fällt mit DROP TABLE).
    **F-08:** call_id IST jetzt ein harter FK ON DELETE CASCADE (NICHT mehr "lose FK Konsistenz intent_event"). KEIN CHECK auf session_mode/origin in der DB (Validierung Python-seitig in der Engine).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "CREATE TABLE.*rubric_score|op.create_table\('rubric_score'" alembic/versions/*rubric_score*.py` == 1
    - `grep -E "REFERENCES (public\.)?calls\(id\) ON DELETE CASCADE|ForeignKey.*calls.id.*ondelete='CASCADE'" alembic/versions/*rubric_score*.py` == 1 (**F-08 harter FK CASCADE — diesmal MUSS vorhanden sein**)
    - `grep "ux_rubric_score_live_call_id" alembic/versions/*rubric_score*.py` vorhanden + `grep -i "WHERE origin = 'live'" ...` vorhanden (F-03 partieller Unique-Index)
    - `grep "FORCE ROW LEVEL SECURITY" alembic/versions/*rubric_score*.py` == 1 (greift fuer Owner)
    - `grep "ALTER TABLE public.rubric_score OWNER TO nerve_app" alembic/versions/*rubric_score*.py` == 1
    - `grep "tenant_isolation" alembic/versions/*rubric_score*.py` == 1; `grep "nullif" ...` == vorhanden
    - `grep -c "COMMENT ON COLUMN public.rubric_score" alembic/versions/*rubric_score*.py` >= 10 + 1 COMMENT ON TABLE
    - Nach Migration als postgres: `inspect.sh schema rubric_score` zeigt alle Kern-Spalten + payload_jsonb + die FK-Zeile `call_id ... REFERENCES calls(id) ON DELETE CASCADE`; Owner nerve_app
  </acceptance_criteria>
  <done>Migration reversibel, rubric_score live auf Prod (leer), harter call_id-FK CASCADE, partieller Unique-Index, Owner nerve_app, RLS FORCE, Schilder gesetzt.</done>
</task>

<task type="auto">
  <name>Task 3: Schema-Smoke-Test + F-08-Cascade-Test + F-03-Unique-Test + SCHILD-Guard GRÜN (Punkt 13 + Punkt 23)</name>
  <files>tests/test_rubric_score_schema.py</files>
  <read_first>
    - tests/test_schild_guard.py + tests/conftest.py (Guard-Mechanik, pg_description, NERVE_SCHILD_TEST_DSN)
    - tests/test_api_beenden_calls_update.py:49-50 (Call/CallEvent-Cleanup-Muster fuer den Cascade-Test)
    - .planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/ (Guard-Lauf-Muster, manuell server-seitig)
  </read_first>
  <action>
    Schreibe `tests/test_rubric_score_schema.py` (Runtime-Behavior, KEIN Source-Presence):
    - `test_rubric_score_insert_roundtrip`: legt eine RubricScore-Zeile an (minimal: session_mode, origin, payload_jsonb), commit, query zurück, assert Feldwerte (session_mode/origin/is_provisional default False). Gegen die echte Prod-DB-Session server-seitig (deploy-Gate). Nutzt einen Test-Tenant + räumt auf (DELETE) — keine Daten-Vergiftung.
    - `test_rubric_score_payload_jsonb_accepts_training_keys`: schreibt payload_jsonb mit was_correct/scenario_id/ground_truth_score, liest zurück, assert Keys vorhanden (belegt Training-Fit-Reserve aus Task 0).
    - **`test_rubric_score_cascade_on_call_delete` (F-08 DSGVO, Pflicht):** lege einen Test-Call (calls-Zeile, Test-Tenant) + eine rubric_score-Zeile mit `origin='live', call_id=<call>` an → committe → DELETE den Call → assert `db.query(RubricScore).filter_by(call_id=<call>).count() == 0` (Cascade hat die Note mitgeloescht, 0 Waisen). Räumt selbst auf.
    - **`test_rubric_score_live_unique_conflict` (F-03, Pflicht):** zwei rubric_score-Zeilen mit demselben call_id + origin='live' → die zweite muss einen IntegrityError/Unique-Verletzung werfen (partieller Index greift). Gegencheck: zwei Zeilen mit origin='training' und gleichem conversation_log_id (call_id NULL) sind ERLAUBT (Index ausgenommen → Training-Fit unverletzt). Cleanup.
    SCHILD-Guard: nach dem Deploy server-seitig MANUELL laufen lassen (OS-User nerve_app, NERVE_SCHILD_TEST_DSN) — muss rubric_score + alle nicht-trivialen Spalten GRÜN melden.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u nerve_app NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest tests/test_schild_guard.py tests/test_rubric_score_schema.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh schema rubric_score` zeigt alle Kern-Spalten + payload_jsonb + `call_id ... REFERENCES calls(id) ON DELETE CASCADE`; Owner-Zeile nerve_app
    - SCHILD-Guard GRÜN: rubric_score-Tabelle + alle nicht-trivialen Spalten haben Schild >= 10 Zeichen (pg_description)
    - test_rubric_score_insert_roundtrip + payload_jsonb_accepts_training_keys grün server-seitig
    - **test_rubric_score_cascade_on_call_delete grün (F-08): gelöschter Call → 0 verwaiste rubric_score-Zeilen (DSGVO-Beweis)**
    - **test_rubric_score_live_unique_conflict grün (F-03): zweite origin='live'-Zeile mit gleichem call_id → IntegrityError; zwei origin='training'-Zeilen erlaubt**
    - `inspect.sh count rubric_score` == 0 nach Test-Cleanup (kein Daten-Residuum)
  </acceptance_criteria>
  <done>rubric_score schema-verifiziert, Cascade-Cleanup (F-08) + Unique-Konflikt (F-03) bewiesen, Guard grün, Training-Fit-Reserve real bewiesen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App (nerve_app-Rolle) → rubric_score | Mandanten-Daten kreuzen hier; ohne RLS Cross-Org-Noten-Leck |
| Migration (postgres) → public-Schema | privilegierter DDL-Pfad |
| Call-Löschung (DSGVO Art.17) → rubric_score | Note-Reste müssen mit dem Call verschwinden |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-01-01 | I (Info Disclosure) | rubric_score Cross-Org-Lese-Leck (Noten anderer Firma) | mitigate | RLS ENABLE+FORCE + tenant_isolation-Policy (nullif-fail-closed); FORCE greift auch fuer Owner nerve_app (sonst BYPASS). tenant_id NOT NULL fuer geschriebene Zeilen (Plan 04 stampt aus calls.tenant_id). |
| T-TAXO2-01-02 | T (Tampering) | session_mode/origin nehmen beliebigen String | accept | Validierung Python-seitig in der Engine (Plan 02), varchar ist Datenfeld kein SQL; ORM parametrisiert. Niedriges Risiko (kein Live-User-Input direkt). |
| T-TAXO2-01-03 | E (Elevation) | OWNER nerve_app könnte RLS bypassen | mitigate | FORCE ROW LEVEL SECURITY erzwingt Policy auch fuer Owner (0014:50-Muster). Beleg im Acceptance. |
| T-TAXO2-01-04 | I (Info Disclosure / DSGVO) | verwaiste rubric_score-Zeilen nach Call-Löschung (F-08) | mitigate | **F-08 Variante a: harter FK call_id ON DELETE CASCADE (DD-01-Konvention).** Gelöschter Call räumt die Note automatisch mit → 0 Waisen, DSGVO-clean by-design; Art.17-Hard-Purge (settings.py) deckt rubric_score ohne Worker-Erweiterung mit ab. Test: test_rubric_score_cascade_on_call_delete. |
| T-TAXO2-01-05 | T (Doppel-Insert) | Doppelklick/Race erzeugt 2 Live-Noten je Call (F-03) | mitigate | **F-03: partieller Unique-Index UNIQUE(call_id) WHERE origin='live'** — DB lehnt die zweite Live-Zeile ab; Plan 04 ON CONFLICT DO UPDATE wird valide. Test: test_rubric_score_live_unique_conflict. |
| T-TAXO2-01-06 | I | payload_jsonb speichert spaeter Training-Ground-Truth/Berater-Saetze | accept | In dieser Welle leer; DSGVO-No-verbatim-Default gilt fuer Schreiber (Plan 03 D-07). Hier nur Schema. |
</threat_model>

<verification>
- `inspect.sh schema rubric_score` (verbatim in SUMMARY): Kern-Spalten + payload_jsonb + harter call_id-FK CASCADE (F-08) + partieller Unique-Index (F-03), Owner nerve_app, RLS aktiv.
- SCHILD-Guard GRÜN server-seitig (rubric_score + Spalten beschildert).
- F-08: test_rubric_score_cascade_on_call_delete grün (gelöschter Call → 0 Waisen).
- F-03: test_rubric_score_live_unique_conflict grün (zweite Live-Zeile abgelehnt, Training frei).
- `inspect.sh count rubric_score` == 0 (kein Schreiber in dieser Welle).
- `grep -rn "rubric_score\|RubricScore" services/ routes/` == 0 aktive Schreiber/Leser (Engine kommt Plan 02/04).
- Training-Fit-Pass + 05-Log-Eintrag vorhanden.
</verification>

<success_criteria>
- public.rubric_score live (leer), Owner nerve_app, RLS ENABLE+FORCE+tenant_isolation, harter call_id-FK CASCADE (F-08), partieller Unique-Index (F-03), alle nicht-trivialen Spalten beschildert (Guard grün).
- Belegter Training-Scoring-Fit-Pass (jedes Trainings-Datum → konkrete Spalte/JSONB-Key); F-03 bricht Training-Fit nicht.
- Risk-low Auto-Check-Welle: KEIN Test-Anruf nötig (reine Schema-Addition, RESEARCH §I); F-08/F-03 via server-seitigem pytest bewiesen.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-01-SUMMARY.md` (inkl. verbatim `inspect.sh schema rubric_score` mit FK-CASCADE-Zeile + count=0 + Guard-GRÜN-Output + Training-Fit-Tabelle + Cascade-Test-Beleg (F-08) + Unique-Test-Beleg (F-03) + echter Migrations-Revision).
</output>
