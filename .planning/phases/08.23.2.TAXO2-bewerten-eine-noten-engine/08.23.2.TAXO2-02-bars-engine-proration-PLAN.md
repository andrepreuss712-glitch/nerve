---
phase: 08.23.2.TAXO2
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - services/rubric_engine.py
  - services/rubric_dimensions.py
  - database/models.py
  - alembic/versions/XXXX_mode_weight_config.py
  - tests/test_proration.py
  - tests/test_rubric_engine.py
autonomous: true
requirements: ["SPEC Req 2 (Engine + Proration-Kern)", "SPEC Req 3", "SPEC Req 5 (Aufschluesselung)", "SPEC Req 9", "D-01", "D-02", "D-03 Tor1", "D-04", "D-05", "D-08"]
user_setup: []

must_haves:
  truths:
    - "Eine BARS-Engine berechnet aus intent_event-Strom + Speech-Stats eine rubric_score-Aufschluesselung (7 Dimensionen, je 3 Stufen als DB-Daten)"
    - "Nicht-messbare Dimensionen fallen via available=false raus, Restgewichte renormalisieren (Proration tötet K2)"
    - "Modus-Gate ist zweistufig: Config-Gewichtssatz (welche Dim gelten) UND per-Call-Verfügbarkeit (zwei getrennte Ausschluss-Gründe)"
    - "<50%-Schwelle rechnet gegen den MODUS-reduzierten Satz, nicht gegen volle 7 — ein schlanker Modus kann trotzdem scoren"
    - "N/A vs vergeigt wird unterschieden (Abbruch-Call kein hoher Score, D-08)"
    - "Der Modus-Lookup-Schluessel kommt aus `calls.call_mode` (die ECHTE Spalte, Werte {cold_call|meeting_consented}) bzw. origin='training' — NICHT aus einem nicht-existenten `calls.session_mode`; mode_weight_config.session_mode traegt EXAKT dieselben Wertstrings (kein 'meeting'-Kurzform). Sonst leerer mode_config-Lookup → alle Dimensionen proration-drop → coaching_score IMMER NULL (N-4)"
  artifacts:
    - path: "services/rubric_engine.py"
      provides: "compute_rubric(events, speech_stats, call, mode_config) -> rubric-dict (reine Funktion, kein Live-Loop, kein LLM)"
      contains: "def compute_rubric"
    - path: "services/rubric_dimensions.py"
      provides: "7 Dimensionen + 3 BARS-Stufen je Dim + Messbarkeits-Regeln (D-05), als ladbare Daten"
      contains: "DIMENSIONS"
    - path: "tests/test_proration.py"
      provides: "D-02 Pflicht-Tests test_mode_can_never_score + test_proration_drops_below_mode_threshold"
  key_links:
    - from: "services/rubric_engine.py compute_rubric"
      to: "mode_weight_config (DB)"
      via: "Modus-Gewichtssatz laden (D-01/D-04, laufzeit-tunbar Punkt 12)"
      pattern: "mode_weight"
    - from: "compute_rubric"
      to: "intent_event.confidence"
      via: "Tor-1-Konfidenzfilter (D-03, >= tunbare Schwelle)"
      pattern: "confidence"
---

<objective>
Baue die EINE rubrik-basierte Noten-Engine als reine, testbare Funktion (KEIN Live-Loop, KEIN LLM, KEINE DB-Schreibseite hier — das verkabelt Plan 04). Sie nimmt den intent_event-Strom eines Calls + die per-SID-Speech-Stats + den Modus-Gewichtssatz und liefert die volle Aufschlüsselung (7 Dimensionen, je 3 BARS-Stufen) mit Proration: nicht-messbare Dimensionen fallen via `available=false` raus, Restgewichte renormalisieren. Sie tötet K2 (kein `frage_qualitaet=0.0`-Hardcode mehr, kein Redeanteil-Fixwert-Artefakt) an der Wurzel.

Purpose: Das Herz von TAXO2. Alle Vertrauens-Regeln (Gerüst §4) + Modus-Fairness (D-01/D-02) + N/A-vs-vergeigt (D-08) leben hier. Als reine Funktion deterministisch testbar (D-02 Pflicht-Tests).
Output: rubric_engine.py + rubric_dimensions.py + mode_weight_config-Tabelle + 2 D-02-Pflicht-Tests + Engine-Tests.

KEIN Schreiben in rubric_score/calls (Plan 04). KEIN handling_score-Marker (Plan 03 — die Engine konsumiert handling_score_numeric, berechnet es nicht). KEIN LLM (Bau-Regel 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** Die 2 D-02-Pflicht-Tests + Engine-Tests sind reine deterministische Logik → laufen server-seitig im Prod-Pytest-Gate (`bash deploy.sh production` startet das Gate). KEIN lokales pytest als Acceptance. Diese Welle hat KEINEN Test-Anruf (RESEARCH §I: reine Proration-Logik deterministisch, Auto-Check). Die Daten-Fluss-Verifikation kommt in Plan 04 (wenn die Engine verkabelt wird).

**UTF-8:** BARS-Stufen-Texte (user-facing Beschreibungen) mit Umlauten; dict-Keys/Spalten ASCII.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- Eingangs-Daten der Engine — bewiesen aus Code/Verträgen. -->

intent_event (TAXO1-Vertrag, DEPENDS-ON-TAXO1):
- intent_type (str): 'vorwand' | 'kaufsignal' | 'aufschub' | ... (Taxonomie)
- confidence (float): Tor-1-Filter D-03 — Ereignis zählt nur als messbar wenn confidence >= Schwelle (Default 0.70, Config)
- handling_score_numeric (int 1-3 | None): pro-Event-Behandlungs-Note (Plan 03 füllt; Engine konsumiert)
- phase (int 1-6 | None): Gesprächs-Phase
- payload_jsonb: speaker_role, was_correct, dimension_available, outcome.resolution

Speech-Stats (services/live_session.py get_speech_stats(sid), K1 per-SID, READ gefixt):
- redeanteil (int %), tempo (int), monolog/laengster_monolog (float sec)
- Single-Speaker-Cold-Call: Talk-Share nicht messbar → Proration-Drop

calls (inspect.sh §A1):
- outcome (text|NULL): meeting_booked/callback/.../no_interest/contract_signed | NULL
- call_mode (text): cold_call | meeting_consented
- audio_health_score (double|NULL): D-09 (Gate-Entscheidung in Plan 04, hier nur Eingangs-Signal für D-08)
- started_at/ended_at bzw. conversation_logs.dauer_sekunden: Call-Dauer (D-08 Abbruch-Erkennung, D-05 Phasen-Mindestdauer)

RubricScore-Schema (Plan 01): dimensions(JSON), coaching_score, is_provisional, measured_weight_pct, unmeasured_dimensions(JSON), session_mode, origin

<!-- N-4 Naming-Vertrag (TAXO-INTERLOCK-FINDINGS, KRITISCH gegen leeren Lookup):
     QUELLE DER WAHRHEIT für den Call-Modus = `calls.call_mode` (text, Werte {cold_call | meeting_consented}).
     Es gibt KEINE Spalte `calls.session_mode` (inspect.sh §A1 bestätigt: call_mode). Wer `call.session_mode`
     liest, bekommt None → mode_weight_config-Lookup leer → JEDE Dimension fällt config_off raus → measured_weight
     immer <50% → coaching_score IMMER NULL (stiller Totalausfall).
     KONTRAKT:
       - mode_weight_config.session_mode (Task 1) UND rubric_score.session_mode (Plan 01) tragen EXAKT die
         calls.call_mode-Wertstrings: 'cold_call', 'meeting_consented' — PLUS 'training' (origin='training').
       - KEIN 'meeting'-Kurzform irgendwo. Immer 'meeting_consented'.
       - Engine-Lookup-Schlüssel = aus calls.call_mode abgeleitet (live) bzw. 'training' (origin='training').
         compute_rubric nimmt diesen Schlüssel als Parameter (mode_key), liest NICHT eigenmächtig call.session_mode.
       - Plan 04 stampt rubric_score.session_mode = derselbe mode_key (Spiegel von calls.call_mode), damit Leser
         (Plan 05/06/07) konsistent gruppieren. -->
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `mode_weight_config` (CREATE, neue Config-Tabelle, laufzeit-tunbar Punkt 12) — existiert nicht
- `rubric_dimensions` ODER DB-Daten in mode_weight_config — Dimensionen+BARS als Daten (siehe Task 2 Entscheidung)
- LESE-Eingang: intent_event/calls/conversation_logs — KEINE Änderung (Engine ist reine Funktion; Plan 04 liest/schreibt)
### inspect.sh-Beleg
`inspect.sh schema mode_weight_config` → "Did not find any relation" (neu). Beim Execute belegen.
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| mode_weight (pro Modus/Dim) | Engine liest beim compute | DB mode_weight_config | ⬜ Task 1 Schema-Beleg |
| DIMENSIONS + BARS-Stufen | Engine liest | DB-Daten (mode_weight_config payload ODER rubric_dimensions) | ⬜ Task 2 |
| events[].confidence | Engine-Eingang (RAM, aus DB-Query in Plan 04) | intent_event.confidence | ✓ TAXO1-Vertrag |
| speech_stats.redeanteil | Engine-Eingang (RAM, get_speech_stats) | per-SID-State (NICHT tote Globale, K1) | ✓ RESEARCH §A4 |

## Verbindungs-Karte (Punkt 22)
- **K2-Wurzel bewiesen (RESEARCH §A2):** `frage_qualitaet=0.0` HARDCODE app_routes.py:785; `redeanteil_score=max(0,100-abs(redeanteil-40)*2)` :783 mit Fixwert-50-Default :782. Diese Engine ERSETZT die Formel-Logik (Proration statt 0/Fixwert) — gelöscht wird die alte Formel in Plan 04, hier wird die korrekte Berechnung gebaut.
- **Speech-Stats-Quelle (RESEARCH §A4):** `live_session.get_speech_stats(sid)` per-SID (K1-READ gefixt), NICHT tote Modul-Globale. Engine konsumiert die übergebenen Werte (Plan 04 liest sie aus der echten Quelle).
- **mode_weight_config neu:** `grep -rn "mode_weight_config\|ModeWeightConfig" services/ routes/` == 0 vor diesem Plan (Beleg Task 1).

<tasks>

<task type="auto">
  <name>Task 1: mode_weight_config-Tabelle + Default-Gewichtssätze (D-01/D-04, laufzeit-tunbar)</name>
  <files>database/models.py, alembic/versions/XXXX_mode_weight_config.py</files>
  <read_first>
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md D-01 + D-04 (Gewichtssätze)
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-Stil) + 0010 (OWNER nerve_app public)
    - ssh ... 'bash scripts/inspect.sh migrations' (HEAD beim Execute)
  </read_first>
  <action>
    Neue Tabelle `public.mode_weight_config` (laufzeit-tunbar, Punkt-12-Override): pro (session_mode, dimension) ein Gewicht + ein "config-an"-Flag.
    Spalten: `id` PK, `session_mode VARCHAR(32) NOT NULL`, `dimension VARCHAR(48) NOT NULL`, `weight FLOAT NOT NULL` (config-Gewicht; 0 = config-AUS = D-01(a)), `enabled BOOLEAN NOT NULL DEFAULT true`, `partial_marker VARCHAR(48) NULL` (z.B. 'sprechdisziplin' fuer Kaltakquise-Gesprächsführung D-04), `indirekt_erkannt BOOLEAN NOT NULL DEFAULT false` (D-04 "(indirekt erkannt)"-Marker), `confidence_gate FLOAT NULL` (Tor-1-Schwelle D-03, Default 0.70 falls NULL), `updated_at`. UNIQUE(session_mode, dimension).
    Tabellen-Schild: "Modus-Gewichtssatz fuer die Noten-Engine (D-01/D-04, laufzeit-tunbar Punkt 12). Pro Modus+Dimension: Gewicht, config-an/aus, Tor-1-Konfidenzschwelle. Status: lebt (neu, TAXO2). Liest services/rubric_engine.py; schreibt Admin/Seed."
    Spalten-Schilder fuer weight/enabled/partial_marker/indirekt_erkannt/confidence_gate.
    Seed (in der Migration, idempotent): Default-Sätze aus D-04:
    - KALTAKQUISE (session_mode='cold_call'): vorwand_behandlung/aufschub_behandlung/kaufsignal_nutzung/phasen_technik/fragen_qualitaet/outcome_progression = enabled weight>0; gespraechsfuehrung = enabled weight>0 partial_marker='sprechdisziplin' (nur Monolog/Tempo, Talk-Share aus); vorwand/aufschub/kaufsignal indirekt_erkannt=true.
    - MEETING (session_mode='meeting_consented'): alle 7 enabled weight>0, indirekt_erkannt=false.
    - TRAINING (session_mode='training'): alle 7 enabled + Ground-Truth-Erkennung (als 8. Dim oder payload — DEFERRED-Verkabelung, hier nur Gewichtszeile angelegt).
    Owner nerve_app (postgres legt an). down_revision = echter HEAD.
    ORM-Model `class ModeWeightConfig(Base)` mit comment= je nicht-trivialer Spalte.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh schema mode_weight_config` zeigt alle Spalten; Owner nerve_app; Guard grün (Tabelle+Spalten beschildert)
    - `inspect.sh sample mode_weight_config 30` zeigt Default-Sätze: cold_call hat 7 Zeilen (gespraechsfuehrung partial_marker='sprechdisziplin'), meeting hat 7 (kein partial), je vorwand/aufschub/kaufsignal cold_call indirekt_erkannt=true
    - `grep "class ModeWeightConfig" database/models.py` == 1
    - UNIQUE(session_mode, dimension) constraint vorhanden
  </acceptance_criteria>
  <done>Modus-Gewichtssätze als DB-Daten live (laufzeit-tunbar), Defaults geseedet, beschildert.</done>
</task>

<task type="auto">
  <name>Task 2: 7 Dimensionen + 3 BARS-Stufen + Messbarkeits-Regeln (services/rubric_dimensions.py, D-05)</name>
  <files>services/rubric_dimensions.py</files>
  <read_first>
    - Nerve-Vault/04 Entscheidungen/NERVE TAXO-Gerüst (verriegelt).md §4 (7 Dimensionen + BARS + Vertrauens-Regeln)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md D-05 (Messbarkeit) + RESEARCH §E (Mess-Inputs)
  </read_first>
  <action>
    Definiere die 7 Dimensionen als ladbare Daten (Konstante `DIMENSIONS` + je 3 BARS-Stufen + eine Messbarkeits-Funktion pro Dim). 7 Dim-Keys (ASCII): vorwand_behandlung, kaufsignal_nutzung, aufschub_behandlung, phasen_technik, fragen_qualitaet, gespraechsfuehrung, outcome_progression.
    Pro Dimension:
    - `bars` = 3 Stufen (1/2/3) mit user-facing deutschem Text (Umlaute OK), aus Gerüst §4 abgeleitet (z.B. vorwand_behandlung Stufe 1 "Sofort weggebuegelt/Gegenargument", Stufe 2 "Teilweise angenommen", Stufe 3 "Gelabelt+Frage, isoliert").
    - `is_measurable(events, speech_stats, call, mode_cfg) -> bool` (D-05 Regeln):
      - Ereignis-Dims (vorwand/kaufsignal/aufschub): messbar wenn >=1 Ereignis dieses Typs mit confidence >= confidence_gate (Tor 1, D-03). NICHT >=2.
      - fragen_qualitaet: messbar wenn >=1 Berater-Frage erkannt ueber FRAGEWOERTER (W-Fragen: wer/was/wann/wo/warum/wie/welch/wieso/inwiefern...), NICHT ueber '?' (STT unzuverlässig, D-05). Wortzahl als EIN Qualitäts-Input.
      - phasen_technik: messbar wenn Call-Dauer >= plausible Mindestlänge (Config, z.B. >= 60s). Ergebnis = Progression ODER explizites Steckenbleiben (Festhängen = messbares NEGATIV, D-05). "Sehr kurze Phasen" harter Schwellenwert (<3s).
      - gespraechsfuehrung: MODUS-ABHÄNGIG (D-05): meeting → messbar bei Talk-Share (Zwei-Sprecher); cold_call → messbar bei Berater-Speech-Stats (Monolog/Tempo/Pausen, K1, Einzelsprecher; partial_marker='sprechdisziplin').
      - outcome_progression: messbar wenn calls.outcome klassifiziert (nicht NULL/unsicher).
    - `score(events, speech_stats, call) -> int 1-3` pro messbarer Dim (Ereignis-Dims nutzen aggregierten handling_score_numeric; Sprechdisziplin gegen Baselines — Baselines als Config-Konstante, post-launch tunbar D-05-Plan-Flag).
    Modul-Docstring nennt Gerüst-§4-Quelle + D-05.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/python -c "from services.rubric_dimensions import DIMENSIONS; assert len(DIMENSIONS)==7; assert all(len(d[\"bars\"])==3 for d in DIMENSIONS.values())"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DIMENSIONS" services/rubric_dimensions.py` >= 1; genau 7 Dimensionen mit je 3 BARS-Stufen
    - `grep -E "wer|was|wann|wo|warum|wie|welch" services/rubric_dimensions.py` vorhanden (Fragewörter, NICHT '?')
    - `grep "'?'" services/rubric_dimensions.py` für Fragenerkennung == 0 (Satzzeichen-Erkennung verboten, D-05)
    - is_measurable fuer gespraechsfuehrung verzweigt nach session_mode (cold_call vs meeting)
    - phasen_technik unterscheidet Progression vs Steckenbleiben (Negativ-Signal)
  </acceptance_criteria>
  <done>7 Dimensionen + BARS + D-05-Messbarkeits-Regeln als ladbare Daten/Funktionen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Proration-Engine compute_rubric (D-01/D-02/D-08) + 2 D-02-Pflicht-Tests</name>
  <files>services/rubric_engine.py, tests/test_proration.py, tests/test_rubric_engine.py</files>
  <read_first>
    - services/rubric_dimensions.py (Task 2) + Task-1-Gewichtssätze
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md D-01/D-02/D-08
    - RESEARCH §E (Zwei-Stufen-Gate) + §F (N/A-vs-vergeigt, is_provisional-Schema)
    - app_routes.py:768-806 (alte Formel — als Referenz WAS sie tat, NICHT als Vorlage; wird Plan 04 gelöscht)
  </read_first>
  <behavior>
    - test_mode_can_never_score: schlanker Modus (Gesamtgewicht < 50% des globalen Maximums) erzeugt bei VOLLER Datenlage für SEINE Dimensionen trotzdem einen coaching_score != None
    - test_proration_drops_below_mode_threshold: Modus der durch Proration unter 50% SEINES EIGENEN Maximalgewichts fällt → coaching_score == None (nur Teil-Dimensionen)
    - test: Cold-Call-Redeanteil nicht messbar → gespraechsfuehrung available=false, renormalisiert (kein Fixwert-20, kein Dauer-0)
    - test: vorzeitig abgebrochener Call (outcome=no_interest + kurze Dauer) → KEIN selbstbewusster hoher Score (D-08 vergeigt-Guard)
    - test: keine messbare Dimension wird je als 0 gewertet (nur available=false ODER 1-3)
    - test_mode_key_from_call_mode (N-4): Call mit call_mode='meeting_consented' → meeting-Gewichtssatz geladen (Lookup nicht-leer, nicht alles config_off); ein erfundenes call.session_mode würde None liefern und ist verboten
  </behavior>
  <action>
    `def compute_rubric(events, speech_stats, call, mode_config) -> dict` (reine Funktion, kein DB-Write, kein LLM):
    1. **Zwei-Stufen-Mode-Gate (D-01) + N-4 Modus-Schlüssel-Vertrag (KRITISCH):** Der Modus-Lookup-Schlüssel `mode_key` kommt aus **`calls.call_mode`** (die ECHTE Spalte, Werte {cold_call|meeting_consented}) bzw. ist `'training'` wenn origin='training' — **NICHT** aus `call.session_mode` (diese Spalte EXISTIERT NICHT auf calls; ein Read davon liefert None → leerer mode_config-Lookup → alle Dimensionen config_off → coaching_score IMMER NULL, stiller Totalausfall, N-4). Reichweite: `compute_rubric` nimmt `mode_key` als expliziten Parameter ODER liest ausschließlich `call.call_mode` (kein `getattr(call, 'session_mode')`). mode_config wird über DIESEN Schlüssel geladen (mode_weight_config.session_mode == mode_key, identische Wertstrings, kein 'meeting'-Kurzform). Wenn der Lookup leer zurückkommt (unbekannter Modus) → loggen + behandeln wie "kein Gewichtssatz" (NICHT still 0/NULL ohne Spur). Pro Dimension: (a) config-an? (enabled & weight>0) — sonst raus mit Grund 'config_off'. (b) wenn config-an: is_measurable()? — sonst available=false mit Grund. Zwei Gründe SAUBER getrennt im Ergebnis (unmeasured_dimensions traegt {dim, reason: 'config_off'|'no_data'|'na'|'not_reached'}).
    2. **Proration:** messbare Dim → score(1-3) + Gewicht. Restgewichte der messbaren Dims renormalisieren auf 100%. NIE eine config-an-aber-nicht-messbare Dim als 0 werten (available=false → fällt raus).
    3. **<50%-Schwelle MODUS-relativ (D-02):** `measured_weight_pct = sum(weight messbarer Dims) / sum(weight aller config-an Dims des Modus)`. Wenn < 0.5 → coaching_score = None (nur Teil-Dimensionen, kein erfundener Wert). Wenn >= 0.5 aber Dims weggeprorated → is_provisional=True (D-08).
    4. **N/A-vs-vergeigt-Guard (D-08, KRITISCH):** unterscheide (a) Dimension N/A (Situation verlangte sie nicht → fair raus, reason='na') vs (b) nicht erreicht/vergeigt (call.outcome in {no_interest, lost} UND kurze Dauer → reason='not_reached', NEGATIVES Signal). Konkret: bei vorzeitigem Abbruch (outcome=no_interest/aufgelegt + Dauer < Schwelle) liefert die Engine KEINEN selbstbewussten hohen Gesamtscore — entweder coaching_score=None ODER die vergeigten Dimensionen zählen als niedrige Stufe, NICHT als benigner Proration-Drop. Konsistent mit phasen_technik-Steckenbleiben=Negativ.
    5. **Volle Aufschlüsselung (Req 5):** Rückgabe-dict: {coaching_score, dimensions:[{dim, score, weight, available, sample_size, beleg_ref, marker[]}], is_provisional, measured_weight_pct, unmeasured_dimensions:[{dim, reason}], status}. beleg_ref = Transkript-/intent_event-Verweis (KEIN freier LLM-Text). KALTAKQUISE-indirekt-Dims tragen marker '(indirekt erkannt)' (D-04).
    Schreibe `tests/test_proration.py` mit den 2 D-02-PFLICHT-Tests (test_mode_can_never_score + test_proration_drops_below_mode_threshold) + Cold-Call-Renormalisierungs-Test + D-08-Abbruch-Test. `tests/test_rubric_engine.py` mit Aufschluesselungs-Form-Tests. Reine deterministische Tests (Eingangs-Dicts, kein DB/LLM).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_proration.py::test_mode_can_never_score tests/test_proration.py::test_proration_drops_below_mode_threshold tests/test_rubric_engine.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def compute_rubric" services/rubric_engine.py` == 1; reine Funktion (kein anthropic-Import, kein db.commit, kein setattr auf DB-Objekt)
    - `grep "def test_mode_can_never_score" tests/test_proration.py` == 1 UND grün server-seitig
    - `grep "def test_proration_drops_below_mode_threshold" tests/test_proration.py` == 1 UND grün server-seitig
    - measured_weight_pct rechnet gegen Modus-Maximum (nicht gegen volle 7) — im Test bewiesen
    - unmeasured_dimensions traegt getrennte reasons (config_off/no_data/na/not_reached) — D-01+D-08 sauber getrennt
    - **N-4:** `grep -n "session_mode" services/rubric_engine.py` zeigt KEINEN Read von `call.session_mode` (die Spalte existiert nicht); der Modus-Schlüssel kommt aus `call.call_mode`/origin='training' (`grep -E "call_mode|mode_key" services/rubric_engine.py` >= 1). Test `test_mode_key_from_call_mode`: ein Call mit `call_mode='meeting_consented'` lädt den meeting-Gewichtssatz (nicht-leerer Lookup, NICHT alles config_off) — beweist dass der Lookup nicht still leer läuft.
    - kein anthropic/Haiku/Sonnet-Import in rubric_engine.py: `grep -iE "anthropic|claude|haiku|sonnet" services/rubric_engine.py` == 0 (Bau-Regel 1: kein LLM)
  </acceptance_criteria>
  <done>Engine berechnet Aufschlüsselung mit korrekter Proration + Modus-relativer Schwelle + N/A-vs-vergeigt-Guard; D-02-Pflicht-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| intent_event-Daten → Engine | Konfidenz-gefilterte Ereignisse; garbage-in via Low-Confidence-FP |
| mode_weight_config → Engine | tunbare Gewichte; Fehl-Config kann Scoring verzerren |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-02-01 | T (Tampering/Garbage) | Low-Confidence-FP-Ereignis verzerrt Dimension | mitigate | Tor-1-Konfidenzfilter D-03 (confidence_gate, Default 0.70) VOR Messbarkeit; getrennt von handling_score-Abstention (Diagnose-Fähigkeit). |
| T-TAXO2-02-02 | I (False-Green) | Isoliert gute Begrüßung skaliert auf 95% bei Totalausfall | mitigate | N/A-vs-vergeigt-Guard D-08: Abbruch-Call (outcome=lost+kurz) liefert keinen hohen Score; vergeigte Dim = Negativ, kein benigner Drop. |
| T-TAXO2-02-03 | D (Score-Manipulation) | Score wird gamebares "Marker-Bingo" | accept (überwacht) | Engine deterministisch + Beleg-Referenz pro Dim; Goodhart-Monitoring kommt Plan 03 (Abstain-Logging). Hier: keine Live-Manipulation möglich (async, Plan 04). |
| T-TAXO2-02-04 | E | Engine-Funktion ruft LLM (Zirkelschluss) | mitigate | grep-Gate: kein anthropic/claude-Import in rubric_engine.py (Bau-Regel 1). |
</threat_model>

<verification>
- 2 D-02-Pflicht-Tests + Engine-Tests grün server-seitig (Prod-Pytest-Gate).
- N-4: Modus-Schlüssel aus calls.call_mode/origin (kein call.session_mode-Read); mode_weight_config.session_mode-Werte == {cold_call, meeting_consented, training} (kein 'meeting'); test_mode_key_from_call_mode grün.
- `inspect.sh sample mode_weight_config` zeigt Default-Gewichtssätze (D-04).
- grep: kein LLM-Import in rubric_engine.py (Bau-Regel 1).
- Auto-Check-Welle (RESEARCH §I): KEIN Test-Anruf (reine Logik; Daten-Fluss-Beweis in Plan 04).
</verification>

<success_criteria>
- compute_rubric als reine, deterministische Funktion (7 Dim, 3 BARS-Stufen, Proration, Modus-relative <50%-Schwelle, N/A-vs-vergeigt).
- K2 an der Wurzel tot: keine messbar-aber-0-Wertung, kein Redeanteil-Fixwert-Artefakt.
- D-02 zwei Pflicht-Tests beweisen Modus-Fairness.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-02-SUMMARY.md` (inkl. Engine-API-Signatur, D-04-Gewichtssatz-Sample, beide D-02-Test-Ergebnisse, Bestätigung kein LLM-Import).
</output>
