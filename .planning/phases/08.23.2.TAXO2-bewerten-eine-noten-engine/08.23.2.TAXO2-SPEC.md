# Phase 08.23.2.TAXO2: Bewerten — EINE Noten-Engine — Specification

**Created:** 2026-06-11
**Ambiguity score:** 0.18 (gate: ≤ 0.20)
**Requirements:** 9 locked
**Komplexität:** 🔴 — Schema (neue `rubric_score`-Tabelle) + Scoring-Logik die ZWEI Alt-Systeme ersetzt + Rückgrat-Aggregat-Konsolidierung + Zombie-Migration. Cross-AI **Pflicht** vor Execute. Real-Daten-Validation Pflicht.
**Depends on:** 08.23.2.TAXO1 (`intent_event`-Schema + Slow-Lane-Gerüst + objection_events-Dual-Write-Brücke). TAXO2 wird NACH TAXO1 ausgeführt (Andre-Direktive 2026-06-10: TAXO1→TAXO2→TAXO3).
**Pflicht-Pre-Read:** `Nerve-Vault/04 Entscheidungen/NERVE TAXO-Gerüst (verriegelt).md` §4 (Scoring: EINE Rubrik, Proration, BARS) + §5 (Slow Lane / 3 Bau-Regeln) + §0.1 (Single-Source) · `Nerve-Vault/03 Planung/TAXO Aufräum-Inventur (Verständnis + Scoring).md` D1/K1/K2/K8 · TAXO1-Pläne 01 (intent_event-Vertrag), 02 §0 (Slow-Lane re-derivierbar `WHERE handling_score_numeric IS NULL`), 05 (objection_events Dual-Write-Brücke `# SUNSET: TAXO2`).

## Goal

NERVE bekommt EINE rubrik-basierte Noten-Engine (BARS + Proration), die die ZWEI driftenden Alt-Systeme (Live-Formel `app_routes.py:_calc_process_score`/`_calc_call_score` + Training-7-Kategorien-Sonnet-Bewerter `training_service.py:1122`) ablöst. Sie schreibt in EINE neue `rubric_score`-Tabelle, tötet den Redeanteil-0%/`frage_qualitaet=0.0`-Bug (K2) per Proration an der Wurzel, vergibt `handling_score` 1-3 regel-/marker-basiert (LLM-Verhaltens-Urteil NUR async auf der Slow Lane), und übernimmt die aus TAXO1 geerbten Schulden: echte Zombifizierung von `objection_events` (Brücke ablösen, alle Leser umziehen) + tief-gestaffelte Konsolidierung des `conversation_logs`-Aggregats (EINE Engine ist einziger Schreiber aller Call-Aggregate). **TAXO2 verkabelt den Live-Pfad; die Training-Verkabelung ist DEFERRED (TRAINING-REVISIT), das Schema wird aber JETZT für beide entworfen.**

## Background

Grounded gegen Code + Production (inspect.sh / psql, 2026-06-11):

- **`rubric_score` existiert nicht** (0 Tabellen-Treffer auf Prod) — komplette Neu-Tabelle.
- **ZWEI getrennte Scoring-Systeme (D1, verifiziert):**
  - **Live:** `_calc_process_score(conv)` (`routes/app_routes.py:768`) = gewichtete Formel `kb*0.30 + behandelt*0.30 + redeanteil*0.20 + skript*0.10 + frage*0.10`, `frage_qualitaet=0.0` hardcoded (K2 → 10% Dauer-0), `redeanteil_score=max(0,100-abs(redeanteil-40)*2)`. Plus `_apply_outcome_modifier` (`:806`, `_OUTCOME_MODIFIERS`-Tabelle). Spiegelt die client-seitige `_calcScore()` in `static/pip-launcher.js`. Schreibt `calls.coaching_score` (double), `calls.score_breakdown` (jsonb), `calls.score_schema_version` (smallint), `calls.final_score`.
  - **Training:** Sonnet-Prompt (`services/training_service.py:1173`, `config.MODEL_TRAINING_SCORING`) mit 5-7 benannten Kategorien à 1-10 (Gesprächseröffnung, Bedarfsanalyse, Einwandbehandlung, Gesprächsführung, Abschluss, optional Beziehungsaufbau + Sekretärin) + `gesamt_score`. Keine geteilte Rubrik mit Live.
- **`conversation_logs` (RÜCKGRAT, 36 Zeilen, 6 FK-Kinder):** trägt die denormalisierten Call-Aggregate als feste Spalten — `einwaende_gesamt/behandelt/fehlgeschlagen/ignoriert`, `vorwaende_erkannt`, `kb_start/end/min/max`, `redeanteil_avg`, `tempo_avg`, `laengster_monolog`, `skript_abdeckung`. FK-Kinder: `calls`, `objection_events`, `ewb_ratings`/`zombie_ewb_ratings`, `transcript_segments`, `learning_cards`, `phrases`. Heute schreiben mehrere Pfade in diese Aggregate.
- **K1 (Speech-Stats) READ-Seite ist BEREITS gefixt:** `live_session.py:get_speech_stats(sid)` liest per-SID (`_session_state[sid]`), die toten Modul-Globalen wurden entfernt (verifiziert im aktuellen Code). TAXO2 fixt K1 NICHT erneut — TAXO2 sorgt dafür, dass die Engine die echten Werte (Redeanteil/Tempo/Monolog) als messbare Dimension nutzt bzw. via Proration korrekt abschaltet.
- **K2 (`frage_qualitaet=0.0`):** lebt in beiden Live-Score-Funktionen als 10%-Dauer-0 + die Redeanteil-Formel kollabiert auf einen Fixwert wenn keine echten Stats fließen. Proration löst beides.
- **`intent_event` (aus TAXO1):** hybrides Schema, Spalte `handling_score_numeric` (SmallInteger, NULL in TAXO1) ist der Andock-Punkt für TAXO2-Benotung; Slow-Lane-Arbeitsliste re-derivierbar `WHERE handling_score_numeric IS NULL`. payload_jsonb trägt `dimension_available`, `was_correct`, `cue_fired`, `outcome.resolution`, `speaker_role` etc.
- **`objection_events` (8 Zeilen, lebt via TAXO1-Brücke):** `services/objection_bridge.py` (`# SUNSET: TAXO2`) spiegelt bei Call-Ende `intent_event`-EWB-Events nach `objection_events`. Leser (dürfen nicht brechen): `dashboard.py:736` (Einwand-Zähler), `integration_engine.py:43`, `admin_views.py:9`, `app_routes.py:446/449/471/473/1389` (Re-Aggregation/Rating-Detail).
- **Slow-Lane-Gerüst (aus TAXO1):** `services/slow_lane.py` Adapter + 1 Daemon-Consumer + Graceful-Flush existieren, laufen aber LEER (`_persist_event_ref` = No-Op). TAXO2 macht den Consumer echt + benotend.

## Requirements

1. **`rubric_score`-Tabelle (hybrid, für Live + Training entworfen)**: EINE neue Tabelle als Single Source der Benotung, ab Tag 1 aufnahmefähig für beide Welten — Training-Verkabelung folgt später ohne Maschinen-Neubau.
   - Current: keine `rubric_score`-Tabelle; Live-Note lebt in `calls.score_breakdown` (jsonb), Training-Note nur im Sonnet-JSON-Output (kein geteiltes Schema).
   - Target: `public.rubric_score` mit indizierten Kern-Spalten (mind. `call_id`/`conversation_log_id`-Bezug, `session_mode`, `origin`-Marker `live`/`training`, Gesamt-/Teil-Score-Felder, `dimensions_available_count`/`total_weight_available`) + Teil-Dimensions-Daten (je Dimension: Score, Gewicht, `available`-Flag, Beleg-Referenz) + `payload_jsonb` als Reserve (Training-Ground-Truth `was_correct`/Szenario-Bezug + unvorhergesehene Trainings-Felder ohne spätere Migration). Owner `nerve_app`. Schild (Punkt 23) gesetzt. **Pflicht-"Training-Scoring-Fit"-Pass (Plan-Phase):** aus Gerüst §4/§5 ableiten was eine Trainings-Note braucht (geteilte Dimensionen, Ground-Truth gespielt-vs-erkannt, Szenario-Bezug, objektive ja/nein-Erkennung) und **belegen** dass `rubric_score` (feste Spalte ODER JSONB) jedes davon aufnimmt — Fit beweisen, nicht hoffen.
   - Acceptance: `inspect.sh schema rubric_score` zeigt die Kern-Spalten + Dimensions-Daten + `payload_jsonb`; Owner `nerve_app`; Guard grün (Tabelle + nicht-triviale Spalten beschildert); Plan enthält belegten "Training-Scoring-Fit"-Pass (jedes Trainings-Datum → konkrete Spalte/JSONB-Key).

2. **EINE Rubrik-Engine ersetzt beide Alt-Systeme — Live verkabelt, Training DEFERRED**: Eine BARS-basierte Engine berechnet die Note; der Live-Pfad wird vollständig auf sie umgestellt, der alte Sonnet-7-Kategorien-Training-Bewerter bleibt als markierter Platzhalter.
   - Current: zwei getrennte Systeme (Live-Formel `app_routes.py` + Training-Sonnet `training_service.py:1122`).
   - Target: EINE Engine (Dimensionen aus der Taxonomie abgeleitet, als DB-Daten mit je 3 BARS-Stufen — Gerüst §4: Vorwand-Behandlung, Kaufsignal-Nutzung, Aufschub-Behandlung, Phasen-Technik-Passung, Fragen-Qualität, Gesprächsführung [nur wo messbar], Outcome-Progression). Der **Live-Telefonat-Pfad** ist vollständig auf die Engine verkabelt; die alte Live-Formel (`_calc_process_score`/`_calc_call_score` + client-`_calcScore`) ist GELÖSCHT. Der **Training-Sonnet-Bewerter** (`training_service.py:1122`) bleibt mit `[TO-REPLACE: TRAINING-REVISIT]`-Marker stehen — KEINE Umstellung in TAXO2. **Ziel-Garantie:** die spätere Training-Umstellung ist NUR Verkabelung (höchstens eine additive Spalte), NIE ein Maschinen-Neubau. Bewusste, begründete Abweichung von Gerüst §4 ("beide auf einmal") → Eintrag in `Nerve-Vault/05 Log`.
   - Acceptance: grep belegt — alte Live-Score-Formel-Funktionen gelöscht (kein `_calc_process_score`/`_calc_call_score`-Aufruf im Live-Pfad), client-`_calcScore` entfernt/auf Server-Wert umgestellt; die Engine erzeugt für einen Live-Test-Call eine `rubric_score`-Zeile; `training_service.py:1122` trägt sichtbaren `[TO-REPLACE: TRAINING-REVISIT]`-Marker und ist sonst unverändert; 05-Log-Eintrag zur Abweichung existiert.

3. **Proration statt Null-Strafe (tötet K2 + Redeanteil-0%-Bug an der Wurzel)**: Nicht-messbare Dimensionen fallen raus statt als 0 zu zählen; Restgewichte renormalisieren.
   - Current: `frage_qualitaet=0.0` (10% Dauer-0); `redeanteil` kollabiert auf Fixwert ohne echte Stats → strukturelle Unter-Benotung (K2 / Block J).
   - Target: pro Dimension ein `available`-Flag. Nicht-messbar (z.B. Redeanteil im Single-Speaker-Cold-Call, fehlende Fragen-Daten) → `available=false` → Dimension fällt raus → Restgewichte renormalisieren auf 100%. Mindest-Schwelle (Gerüst §4): **<50% Gewicht verfügbar → KEIN Gesamtscore, nur Teil-Dimensionen.** NIE eine messbare-aber-nicht-vorhandene Dimension als 0 werten.
   - Acceptance: Test mit einem Cold-Call-Szenario (Redeanteil nicht messbar) zeigt: die Redeanteil-Dimension ist `available=false`, das Gesamt-Ergebnis renormalisiert (kein Fixwert-20-Artefakt, kein Dauer-0-Abzug); ein Szenario mit <50% verfügbarem Gewicht liefert KEINEN Gesamtscore (nur Teil-Dimensionen, kein erfundener Wert); `frage_qualitaet=0.0`-Hardcode existiert nicht mehr.

4. **`handling_score` 1-3 — regel-/marker-basiert + Abstention, LLM NUR Slow Lane**: Pro Einwand-Behandlung eine Verhaltens-Note, ohne Live-LLM-Scoring (Zirkelschluss-Schutz).
   - Current: `intent_event.handling_score_numeric` existiert (aus TAXO1), ist NULL, kein Code befüllt es.
   - Target: die Slow Lane (echter Consumer, kein No-Op mehr) befüllt `intent_event.handling_score_numeric` (1-3) **regel-/marker-basiert** (prüft die nächste Berater-Aussage auf Anker-Verhalten: Label/Frage vs. Gegenargument) **mit großzügiger Abstention** (unsicher → kein Score, kein Raten). **LLM-Verhaltens-Urteil läuft NUR async auf der Slow Lane (post-call), NIE Fast/Medium Lane** (Gerüst §5 Bau-Regel 1: intent_event read-only für Live-Bahnen; Score als separates Objekt, Merge bei Call-Ende). Arbeitsliste re-derivierbar `WHERE handling_score_numeric IS NULL` (TAXO1-Vertrag). **Schreibstrategie (Cross-AI-Interlock-Auflösung 2026-06-11):** `handling_score_numeric` (pro Einwand) wird von der Slow Lane POST-CALL **IN-PLACE** auf der `intent_event`-Zeile aktualisiert — Spalte bleibt wie in TAXO1 Plan 01 gebaut, Arbeits-Liste `WHERE handling_score_numeric IS NULL`. Dies ist eine **bewusste, ENGE Abweichung von Gerüst §8.7** ("separates Score-Objekt, NICHT In-Place-Update"). Begründung: die Pro-CALL-Note (`rubric_score`-Tabelle) IST das von §8.7 verlangte separate Objekt; NUR die Pro-EVENT-Note ist in-place. **Plan-Pflicht-Bedingung:** grep-Beweis dass KEINE Live-Bahn (Fast/Medium) `intent_event`-Zeilen zurückliest → keine Race (die §8.7 fürchtete). Falls ein Live-Leser gefunden wird → Fallback auf separates Pro-Event-Score-Objekt. Abweichung in `Nerve-Vault/05 Log` dokumentiert.
   - Acceptance: nach einem Live-Test-Call befüllt die Slow Lane `handling_score_numeric` (1-3 oder NULL bei Abstention) — `inspect.sh sample intent_event` zeigt befüllte Scores; grep belegt KEIN handling_score-Write aus Fast/Medium Lane (Bau-Regel 1); ein simulierter Slow-Lane-Restart re-derived die offene Arbeitsliste aus der DB (`WHERE handling_score_numeric IS NULL`).
   - Acceptance (Schreibstrategie): grep-Beleg — KEINE Live-Bahn (Fast/Medium) liest `intent_event`-Zeilen zurück (Race-Freiheit für das In-Place-`handling_score`-Update); falls doch ein Live-Leser existiert → Fallback auf separates Pro-Event-Score-Objekt.

5. **Volle Aufschlüsselung gespeichert — Bildschirm DEFERRED (999.2), kein Nackte-Zahl-Schaden**: Die rückwirkend-nicht-rekonstruierbaren Vertrauens-Daten (Kluger & DeNisi) werden ab Tag 1 erfasst; die Anzeige kommt später.
   - Current: `calls.score_breakdown` trägt nur die alten Formel-Keys; keine Beleg-Referenzen, keine `dimension_available`-Karte.
   - Target: `rubric_score` speichert pro Call die VOLLE Aufschlüsselung — Teil-Noten pro Dimension, **Beleg-Referenz pro Dimension** (Transkript-/`intent_event`-Verweis, NICHT freier LLM-Text), `dimension_available`-Karte ("was war nicht messbar", z.B. Redeanteil im Cold Call). Der **trust-bewusste Bildschirm** (Aufschlüsselung + Beleg + "nicht gewertet"-Hinweis + "vorläufig" bei Low-Confidence + ein Ziel pro Call) ist eine **eigene spätere UI-Phase (Backlog 999.2)** mit voller UI-Token-Disziplin (UI-SPEC, keine hardcoded Farben, Brand-Check) — NICHT in TAXO2. **Rider:** TAXO2 schaltet KEINE neue nackte Zahl ohne Beleg live; die bestehende Anruf-Ende-Anzeige bleibt unverändert (keine Regression), bis 999.2 den Aufschlüsselungs-Bildschirm baut.
   - Acceptance: `rubric_score` einer Test-Call-Zeile enthält Teil-Noten + Beleg-Referenzen + `dimension_available`-Karte (`inspect.sh sample`); grep/UI-Review belegt: keine neue nackte-Zahl-Anzeige in TAXO2 hinzugefügt, bestehende Call-Ende-Anzeige unverändert; ein Backlog/Plant-Seed-Eintrag verweist die Breakdown-UI auf 999.2.

6. **`calls.coaching_score` — sauberer Schnitt, EIN Schreiber, Proration-NULL-Edge abgefangen**: Die neue Engine liefert die Kopf-Zahl ins bestehende Feld; alte Formel gelöscht; Bildschirme unverändert.
   - Current: alte Live-Formel schreibt `calls.coaching_score`/`score_breakdown`/`final_score`; mehrere Bildschirme (Dashboard, Coaching, Performance) lesen `coaching_score` und nehmen an, es ist immer eine Zahl.
   - Target: die Engine rechnet die Detail-Noten (`rubric_score`) UND schreibt die Kopf-Zahl in `calls.coaching_score` (das Feld, das die Bildschirme schon lesen). Alte Formel-Schreibstelle GELÖSCHT — **GENAU EIN Schreiber** auf `calls.coaching_score`. **Proration-NULL-Edge (Gerüst §4):** wenn <50% Dimensionen messbar → keine Gesamtnote → `calls.coaching_score` = NULL/Sentinel; die lesenden Bildschirme MÜSSEN das sauber abfangen ("nicht genug Daten" statt Crash/leere 0) — Punkt-14-Edge-Case-Pflicht beim Bau.
   - Acceptance: grep belegt GENAU EINEN Schreiber auf `calls.coaching_score` (alte Formel-Schreibstelle weg); Test-Anruf → Dashboard/Coaching/Performance rendern mit der neuen Note korrekt (DATEN-FLUSS-Beweis, nicht nur "Note gerechnet" — K1/K8-Lehre); ein konstruierter <50%-Szenario-Call setzt `coaching_score=NULL` und die drei Bildschirme zeigen "nicht genug Daten" ohne Crash/leere 0.

7. **`objection_events` voll zombifiziert — alle Leser umgezogen, Brücke entfernt**: Die aus TAXO1 geerbte Dual-Write-Brücke wird abgelöst; die Einwand-Quelle ist `intent_event`(+`handling_score`).
   - Current: `objection_events` lebt; `services/objection_bridge.py` (`# SUNSET: TAXO2`) spiegelt bei Call-Ende; Leser: `dashboard.py:736`, `integration_engine.py:43`, `admin_views.py:9`, `app_routes.py:446/449/471/473/1389`.
   - Target: **(Reihenfolge zwingend)** (a) Rubrik-Engine befüllt `handling_score` ZUERST (Req 4) → (b) ALLE `objection_events`-Leser auf `intent_event`(+`handling_score`) umgezogen, mit dokumentiertem **Semantik-Match** pro Leser (wie bildet die alte Abfrage auf die neue ab; bewusste Bedeutungs-Verbesserungen — z.B. alte zählte kaputte Daten mit — festhalten, nicht still ändern) → (c) Brücke `services/objection_bridge.py` GELÖSCHT + `objection_events` Zombie-Schild (`zombie_`/`[ZOMBIE]`, NICHT droppen). Vor (c): `grep`==0 verbleibende aktive `objection_events`-Leser (Punkt 20; ein übersehener Leser = Crash-by-design). Seit TAXO1 evtl. neu dazugekommene Leser mitprüfen.
   - Acceptance: `grep -rn "objection_events\|ObjectionEvent" services/ routes/` == 0 aktive Leser (außer Zombie-Definition); `objection_bridge.py` gelöscht (`# SUNSET: TAXO2`-Marker weg); `objection_events` trägt `[ZOMBIE]`-Schild; Test-Anruf → ALLE 4 Oberflächen (Dashboard-Einwand-Zähler, Admin-Ansicht, interne Auswertung, Call-Ende-Nachrechnung) rendern korrekt aus der neuen Quelle; Semantik-Match pro Leser im Plan/SUMMARY dokumentiert.

8. **`conversation_logs`-Aggregat tief konsolidiert — risiko-gestaffelt (Schatten → Cutover), EIN Schreiber je Feld**: Die Engine wird einziger Schreiber aller Call-Aggregate; das Rückgrat-Schema/FK bleibt unangetastet.
   - Current: mehrere Pfade schreiben die denormalisierten Aggregat-Spalten (`einwaende_*`, `redeanteil_avg`, `tempo_avg`, `kb_*`, `skript_abdeckung`) auf `conversation_logs` (RÜCKGRAT, 6 FK-Kinder).
   - Target: die Engine rechnet am Call-Ende ALLE `conversation_logs`-Aggregate aus dem `intent_event`-Strom + per-SID-State (TAXO1) und ist **einziger Schreiber** (§0.1). Cache-Spalten bleiben (Performance unverändert), **FK-Struktur unangetastet** (kein Rename/Zombie von `conversation_logs`). **Risiko-Staffelung INNERHALB TAXO2 (kein permanentes Patchwork):** Welle "Engine + Schatten" — Engine rechnet alle Aggregate, schreibt aber NUR `coaching_score`; die übrigen werden im Schatten mitgerechnet + Diskrepanzen gegen die heutigen Schreiber GELOGGT (nicht geschrieben). Welle "Cutover" — erst wenn Schatten sauber → alte Aggregat-Schreiber raus, Engine schreibt alle Aggregate; `grep`-Beleg GENAU EIN Schreiber je Aggregat-Feld. **Semantik-Match-Pflicht** pro Aggregat (alte→neue Berechnung; bewusste Abweichungen dokumentieren).
   - Acceptance: Schatten-Welle: `inspect.sh logs` zeigt Diskrepanz-Logs (Schatten vs. heutige Werte) für einen Test-Call, Abweichungen erklärbar/dokumentiert; Cutover-Welle: `grep` belegt GENAU EINEN Schreiber je Aggregat-Feld (`einwaende_*`, `redeanteil_avg`, `tempo_avg`, `kb_*`, `skript_abdeckung`), alte Schreiber weg; Test-Anruf → Dashboard/Coaching/Auswertung rendern korrekt; `conversation_logs`-FK/Spalten-Struktur unverändert (`inspect.sh schema` Diff = nur ggf. additive Felder, kein Rename).

9. **Speech-Stats K1 speist die Note (Read bereits gefixt) — ambienter Tempo-Regler OUT**: Die echten per-SID-Speech-Stats werden Score-Input bzw. via Proration korrekt abgeschaltet; der ambiente Visual-Kanal bleibt Post-Launch.
   - Current: `get_speech_stats(sid)` liest bereits per-SID (K1 READ-Seite gefixt); die Werte fließen heute in `redeanteil_avg`/`tempo_avg`, aber die alte Score-Formel verarbeitet sie unfair (K2).
   - Target: die Engine nutzt die echten Speech-Stats (Redeanteil/Tempo/Monolog) als messbare Dimension(en) **oder** schaltet sie via Proration ab wo nicht messbar (Cold-Call-Single-Speaker). **OUT (Gerüst §5 DESIGN-ENTSCHEIDUNG, Post-Launch):** der ambiente Visual-Kanal (aufleuchtende Symbole, live Sprechgeschwindigkeits-Regler) wird NICHT gebaut — TAXO2 berührt nur die Note, nicht den Live-Visual-Kanal.
   - Acceptance: Engine-Test belegt: bei vorhandenen Speech-Stats fließt Redeanteil als messbare Dimension in die Note; bei Single-Speaker-Cold-Call ist sie `available=false` (Proration, Req 3); grep belegt KEIN ambienter-Visual-Kanal-Code (Symbole/Tempo-Regler) in TAXO2.

## Boundaries

**In scope:**
- Neue `public.rubric_score`-Tabelle (hybrid: feste Spalten + JSONB-Reserve), für Live + Training entworfen, Owner `nerve_app`, Schild + belegter "Training-Scoring-Fit"-Pass
- EINE Rubrik-Engine (BARS, 7 Dimensionen als DB-Daten, je 3 Stufen) — **Live-Pfad verkabelt**, alte Live-Formel gelöscht
- Proration statt Null-Strafe (`available`-Flags, Renormalisierung, <50%-Schwelle → kein Gesamtscore)
- `handling_score` 1-3 auf der echten Slow Lane (regel-/marker-basiert + Abstention; LLM-Urteil NUR async Slow Lane)
- Volle Aufschlüsselung gespeichert (Teil-Noten + Beleg-Referenzen + `dimension_available`-Karte)
- `calls.coaching_score`: sauberer Schnitt, EIN Schreiber, Proration-NULL-Edge in den lesenden Bildschirmen abgefangen
- `objection_events`: alle Leser auf `intent_event`/`handling_score` umgezogen + Brücke (`objection_bridge.py`) gelöscht + Zombie-Schild (Reihenfolge: handling_score zuerst)
- `conversation_logs`-Aggregat: tief konsolidiert, risiko-gestaffelt (Schatten → Cutover), EIN Schreiber je Feld, FK unangetastet, Semantik-Match-Doku
- Speech-Stats K1 als Score-Input bzw. Proration-Abschaltung
- Schild (Punkt 23) für `rubric_score` + `zombie_objection_events` + neue Spalten; SCHILD-Guard MANUELL gegen Prod

**Out of scope:**
- **Training-Scoring-Umstellung** (Sonnet-7-Kategorien `training_service.py:1122`) — DEFERRED (TRAINING-REVISIT); nur `[TO-REPLACE]`-Marker, Schema aber JETZT mit-entworfen
- **Trust-bewusster Breakdown-Bildschirm** (Aufschlüsselung + Beleg + "nicht gewertet"-Anzeige) — eigene UI-Phase, Backlog **999.2** (Session-Detail Einzel-Bewertungen)
- **Ambienter Visual-Kanal** (aufleuchtende Symbole, live Sprechgeschwindigkeits-Regler) — Post-Launch (Gerüst §5/§6)
- **Neue nackte-Zahl-Anzeige im Übergang** — verboten (Kluger&DeNisi-Schaden); bestehende Call-Ende-Anzeige bleibt unverändert bis 999.2
- **LLM-Verhaltens-Scoring auf Fast/Medium Lane** — verboten (Bau-Regel 1, Zirkelschluss); LLM-Urteil NUR async Slow Lane
- **Rename/Zombie/FK-Umbau von `conversation_logs`** — Rückgrat bleibt strukturell unangetastet (nur Schreiber-Konsolidierung)
- **K1 READ-Seite erneut fixen** — bereits gefixt; TAXO2 nutzt nur das Ergebnis
- **`intent_event`-Schema-Änderung** — TAXO1-Vertrag bleibt; TAXO2 befüllt `handling_score_numeric`, ändert das Schema nicht (höchstens additive Felder mit Begründung)
- **TAXO3 (`build_answer_context`, Antwort-Wissensversorgung, Slot-B-line_id-Dedup D3)** — separate Phase
- **FSM-Simulator, Coaching-Hub-Oberfläche, Active-Learning-Batch** — Post-Launch (Gerüst §6/§7)

**Offene Discuss-Punkte (in TAXO2-Discuss zu klären, NICHT im Spec verriegelt):**
- **`zombie_ewb_ratings` + `admin_ewb.py` (manuelle Einwand-Bewertung) — Schicksal in TAXO2-Discuss klären.** Lean: durch die automatische `handling_score` (Req 4) ersetzt → `admin_ewb` entfernen — AUSSER ein manuelles Override (Mensch korrigiert die automatische Note) ist gewünscht. Entscheidung in Discuss, dann ggf. Boundary nachziehen.

## Constraints

- **Verify = Production only** (`bash deploy.sh production` + `inspect.sh` gegen Prod, `ssh -i ~/.ssh/nerve_vps root@178.104.82.166`). KEIN Local-Dev, kein lokales pytest als Acceptance, kein `git pull` auf Prod (tar-deployed).
- **Postgres** ist die Wahrheit; neue Tabelle `rubric_score` als `postgres`-User anlegen, dann `OWNER TO nerve_app`.
- **Gerüst §4 verriegelt:** 7 Dimensionen, Proration (<50% → kein Gesamtscore), `handling_score` 1-3 regel-/marker + Abstention, LLM NUR Slow Lane, Vertrauens-Regeln (Breakdown + Beleg, "nicht gewertet", "vorläufig", ein Ziel/Call). Diese sind WAS-Lock — die Pro-Modus-Dimensions-Verfügbarkeit (welche im Cold Call abgeschaltet) ist WIE/Config (DB-Daten, laufzeit-tunbar) → Discuss-Phase, NICHT Spec.
- **Bau-Regel 1 (Gerüst §5):** `intent_event` read-only für Live-Bahnen; Scoring auf separatem Objekt, Merge bei Call-Ende. **Bau-Regel 2:** Graceful-Shutdown-Flush; Slow-Lane-Arbeitsliste re-derivierbar `WHERE handling_score_numeric IS NULL` (kein file-spill).
- **Slow-Lane-Schreibstrategie (Cross-AI-Interlock-Auflösung 2026-06-11):** `handling_score_numeric` (pro Einwand) wird von der Slow Lane POST-CALL **IN-PLACE** auf der `intent_event`-Zeile aktualisiert — Spalte bleibt wie in TAXO1 Plan 01 gebaut, Arbeits-Liste `WHERE handling_score_numeric IS NULL`. Dies ist eine **bewusste, ENGE Abweichung von Gerüst §8.7** ("separates Score-Objekt, NICHT In-Place-Update"). Begründung: die Pro-CALL-Note (`rubric_score`-Tabelle) IST das von §8.7 verlangte separate Objekt; NUR die Pro-EVENT-Note ist in-place. **Plan-Pflicht-Bedingung:** grep-Beweis dass KEINE Live-Bahn (Fast/Medium) `intent_event`-Zeilen zurückliest → keine Race (die §8.7 fürchtete). Falls ein Live-Leser gefunden wird → Fallback auf separates Pro-Event-Score-Objekt. Abweichung in `Nerve-Vault/05 Log` dokumentiert.
- **Single-Source §0.1:** GENAU EIN Schreiber je Feld (`calls.coaching_score`, jedes `conversation_logs`-Aggregat); alte Schreiber GELÖSCHT, grep-belegt. Kein Parallel-System als Dauerlösung.
- **Schreiber-Cutover ist gestaffelt, NICHT patchwork:** die Schatten-Welle ist temporär und endet im selben Phasen-Lauf mit dem Cutover.
- **Test-Anruf-Pflicht (riskante Wellen):** DATEN-FLUSS-Beweis (Bildschirme rendern korrekt), nicht nur "Migration lief"/"Note gerechnet" (K1/K8-deployed-aber-tot-Lehre).
- **Haiku-only im Live-Loop** (Sonnet nur Post-Call); jegliches LLM-Verhaltens-Urteil läuft async auf der Slow Lane.
- **SCHILD-Guard (Punkt 23):** `rubric_score` + `zombie_objection_events` + neue/nicht-triviale Spalten brauchen ein Schild; Guard MANUELL server-seitig gegen Prod-DSN laufen lassen (Automatik inert bis 08.23.2.STAGING).
- **Multi-Segment-ID-Gotcha:** Pfade auf `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hartkodieren; `gsd-tools`/`gsd-code-review`/`gsd-verifier` umgehen (lesen sonst `08.23.2`); STATE/ROADMAP hand-editieren.
- **Plan-Pflicht-Sektionen:** Punkt 14 (Control-Flow-Audit, inkl. Race-Fragen), Punkt 21 (Persistenz-Schicht-Verifikation mit `inspect.sh`-Output), Punkt 22 (Verbindungs-Karte: jeder Aggregat-/Leser-Schreib-/Lese-Pfad grep+inspect-belegt) bei jedem Code-Insert/Schema-Entscheidung; Cross-AI **Pflicht** (🔴) vor Execute; Real-Daten-Validation (Punkt 13).

## Acceptance Criteria

- [ ] `inspect.sh schema rubric_score` zeigt Kern-Spalten + Teil-Dimensions-Daten + `payload_jsonb`; Owner `nerve_app`; SCHILD-Guard grün; Plan enthält belegten "Training-Scoring-Fit"-Pass.
- [ ] Alte Live-Score-Formel gelöscht (`_calc_process_score`/`_calc_call_score`-Aufruf weg, client-`_calcScore` entfernt/server-gespeist); Live-Test-Call erzeugt eine `rubric_score`-Zeile; `training_service.py:1122` trägt `[TO-REPLACE: TRAINING-REVISIT]`-Marker, sonst unverändert; 05-Log-Abweichungs-Eintrag existiert.
- [ ] Proration nachgewiesen: Cold-Call-Redeanteil `available=false` (kein Fixwert-Artefakt, kein Dauer-0); <50%-Szenario → KEIN Gesamtscore; `frage_qualitaet=0.0`-Hardcode existiert nicht mehr.
- [ ] Slow Lane befüllt `intent_event.handling_score_numeric` (1-3 / NULL bei Abstention) nach Live-Call (`inspect.sh sample`); grep: kein handling_score-Write aus Fast/Medium Lane (Bau-Regel 1); Slow-Lane-Restart re-derived Arbeitsliste `WHERE handling_score_numeric IS NULL`.
- [ ] `rubric_score` trägt Teil-Noten + Beleg-Referenzen + `dimension_available`-Karte; keine neue nackte-Zahl-Anzeige in TAXO2 (bestehende Call-Ende-Anzeige unverändert); Breakdown-UI auf 999.2 verwiesen.
- [ ] grep belegt GENAU EINEN Schreiber auf `calls.coaching_score`; Test-Anruf → Dashboard/Coaching/Performance rendern korrekt; <50%-Call setzt `coaching_score=NULL` und alle drei Bildschirme zeigen "nicht genug Daten" ohne Crash/leere 0.
- [ ] `grep` == 0 aktive `objection_events`-Leser (außer Zombie-Def); `objection_bridge.py` gelöscht; `objection_events` `[ZOMBIE]`-Schild; Test-Anruf → alle 4 Oberflächen (Dashboard-Zähler, Admin, interne Auswertung, Call-Ende-Nachrechnung) korrekt aus neuer Quelle; Semantik-Match pro Leser dokumentiert.
- [ ] Schatten-Welle: Diskrepanz-Logs (Schatten vs. heute) für Test-Call, Abweichungen dokumentiert; Cutover-Welle: grep GENAU EIN Schreiber je `conversation_logs`-Aggregat-Feld (alte weg); Test-Anruf → Bildschirme korrekt; `conversation_logs`-FK/Struktur unverändert (kein Rename/Zombie).
- [ ] Speech-Stats: Redeanteil fließt als messbare Dimension bzw. Proration-Abschaltung im Cold Call; grep: kein ambienter-Visual-Kanal-Code (Symbole/Tempo-Regler) in TAXO2.
- [ ] SCHILD-Guard läuft GRÜN server-seitig (`rubric_score` + `zombie_objection_events` + neue Spalten beschildert).

## Ambiguity Report

| Dimension          | Score | Min  | Status | Notes                                                                      |
|--------------------|-------|------|--------|----------------------------------------------------------------------------|
| Goal Clarity       | 0.88  | 0.75 | ✓      | EINE Engine ersetzt 2 Systeme; Gerüst §4 verriegelt; Live-Pfad-Lock        |
| Boundary Clarity   | 0.85  | 0.70 | ✓      | Training OUT, Breakdown-UI→999.2, headline clean-cut, objection_events voll, conversation_logs tief-gestaffelt |
| Constraint Clarity | 0.78  | 0.65 | ✓      | Schatten→Cutover, ein-Schreiber-je-Feld, Bau-Regeln, Proration-NULL-Edge, Test-Anruf-Pflicht |
| Acceptance Criteria| 0.72  | 0.70 | ✓      | 10 pass/fail-Kriterien, gegen Prod verifizierbar (Daten-Fluss, nicht nur "lief") |
| **Ambiguity**      | 0.18  | ≤0.20| ✓      |                                                                            |

Status: ✓ = met minimum, ⚠ = below minimum (planner treats as assumption)

## Interview Log

| Runde | Perspektive         | Frage                                                  | Entscheidung verriegelt                                                                                          |
|-------|---------------------|--------------------------------------------------------|------------------------------------------------------------------------------------------------------------------|
| 1     | Boundary/Simplifier | Umbau-Tiefe: Live + Training, oder erst Live?          | **Erst Live verkabeln, Training DEFERRED (TRAINING-REVISIT)**; `rubric_score` aber JETZT für beide entworfen (hybrid), belegter Training-Fit-Pass; spätere Umstellung = nur Verkabelung. Abweichung Gerüst §4 → 05-Log |
| 1     | Boundary Keeper     | Breakdown-Bildschirm in TAXO2?                         | **Nur Daten** (Teil-Noten + Beleg + dimension_available), Bildschirm → Backlog 999.2; kein neuer Nackte-Zahl-Schaden, bestehende Anzeige unverändert |
| 1     | Failure Analyst     | Kompat für die Gesamtnote (calls.coaching_score)?      | **Sauberer Schnitt** — neue Engine schreibt dieselbe Stelle, alte Formel gelöscht, EIN Schreiber; Proration-NULL-Edge in Lesern abfangen |
| 2     | Boundary Keeper     | objection_events-Zombifizierung: alle Leser oder Rest später? | **Alle Leser umziehen** + Brücke weg + Zombie-Schild; Reihenfolge handling_score→Leser→Brücke; grep==0 + Semantik-Match + Test-Anruf alle 4 Oberflächen |
| 2     | Boundary/Failure    | conversation_logs-Rückgrat: minimal oder tief?         | **Tief, aber risiko-gestaffelt** (Gemini-Cross-AI-Reversal): Engine = einziger Aggregat-Schreiber; Welle Schatten (loggt Diskrepanzen) → Welle Cutover; EIN Schreiber/Feld; FK unangetastet |

---

*Phase: 08.23.2.TAXO2-bewerten-eine-noten-engine*
*Spec created: 2026-06-11*
*Next step: /gsd-discuss-phase 08.23.2.TAXO2 — Implementierungs-Entscheidungen (WIE: rubric_score-Schema-Form, Pro-Modus-Dimensions-Verfügbarkeit/Proration-Config, Slow-Lane-Consumer-Verdrahtung, Schatten-Diskrepanz-Logging-Mechanik). Danach Plan + Cross-AI (🔴 Pflicht); Execute erst nachdem TAXO1/2/3 alle bis kurz vor Execute gebracht + auf sauberes Ineinandergreifen geprüft sind, dann TAXO1→TAXO2→TAXO3 (Andre-Direktive 2026-06-10).*
