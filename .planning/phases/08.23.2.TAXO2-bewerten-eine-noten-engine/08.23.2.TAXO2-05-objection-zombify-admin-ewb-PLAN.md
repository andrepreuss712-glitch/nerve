---
phase: 08.23.2.TAXO2
plan: 05
type: execute
wave: 5
depends_on: ["03", "04"]
files_modified:
  - routes/dashboard.py
  - routes/app_routes.py
  - routes/admin_ewb.py
  - app.py
  - database/models.py
  - templates/admin/ewb_quality.html
  - templates/admin/ewb_rating_template.html
  - routes/CLAUDE.md
  - alembic/versions/XXXX_zombie_schilder.py
autonomous: false
requirements: ["SPEC Req 7", "D-06"]
user_setup: []

must_haves:
  truths:
    - "Alle 4 lebenden objection_events-Leser sind auf intent_event(+handling_score) umgezogen, mit dokumentiertem Semantik-Match pro Leser"
    - "Einwand-ZÄHLER zählen COUNT(DISTINCT interaction_id) (ODER filtern source='ui_asserted'), NICHT rohe Zeilen — sonst verdreifacht die Moment-Klammer (Fast+Medium+Button emittieren je eine intent_event-Zeile pro Kundenmoment) die Einwand-Zahl (M-3)"
    - "services/objection_bridge.py ist gelöscht (# SUNSET: TAXO2-Marker weg)"
    - "objection_events trägt ein [ZOMBIE]-Schild (NICHT umbenannt/gedroppt)"
    - "grep == 0 aktive objection_events-Leser (außer Zombie-Definition)"
    - "admin_ewb ist ersatzlos entfernt (Datei + 2 Templates + app.py Import & register + EwbRating + Tests + routes/CLAUDE.md-Zeile)"
  artifacts:
    - path: "routes/dashboard.py"
      provides: "Einwand-Zähler aus intent_event statt objection_events (Semantik-Match)"
    - path: "alembic/versions/XXXX_zombie_schilder.py"
      provides: "[ZOMBIE]-Schild objection_events + zombie_ewb_ratings"
  key_links:
    - from: "routes/dashboard.py + app_routes.py"
      to: "intent_event (+handling_score_numeric)"
      via: "Einwand-Quelle nach Umzug (Tor-1-confidence-gefiltert)"
      pattern: "IntentEvent"
    - from: "app.py"
      to: "(admin_ewb entfernt)"
      via: "Import + register_blueprint BEIDE raus (toter Import crasht Gunicorn)"
      pattern: "admin_ewb"
---

<objective>
Zahle die aus TAXO1 geerbte Schuld ab: löse die `objection_events`-Dual-Write-Brücke ab. Reihenfolge ZWINGEND (Req 7): (a) handling_score ist befüllt (Plan 03, depends_on) → (b) die 4 ECHTEN lebenden objection_events-Leser ziehen auf `intent_event`(+`handling_score`) um, mit dokumentiertem Semantik-Match pro Leser → (c) `services/objection_bridge.py` GELÖSCHT + `objection_events` bekommt das `[ZOMBIE]`-Schild (NICHT droppen). Zusätzlich (D-06): `admin_ewb` ersatzlos entfernt (zwei Founder-Tools auf der schon-zombifizierten ewb_ratings-Tabelle) + Plant-Seed Anti-Regression.

Purpose: Eine Quelle für Einwände (intent_event), Brücke weg, sauberer Schnitt. RISKANTE Welle (übersehener Leser = Crash-by-design) → grep==0 + Test-Anruf-DATEN-FLUSS Pflicht.
Output: 4 Leser umgezogen + Semantik-Match-Doku, Brücke gelöscht, [ZOMBIE]-Schild, admin_ewb komplett raus.

REIHENFOLGE HART: handling_score (Plan 03) ZUERST, dann Leser, dann Brücke (sonst Crash).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** RISKANTE Welle (RESEARCH §I) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht (alle 4 Oberflächen rendern aus neuer Quelle + grep==0 Leser). `bash deploy.sh production` + `inspect.sh`. **app.py-Import-Falle (D-06):** toter `from routes.admin_ewb import admin_ewb_bp` crasht den Gunicorn-Start → BEIDE Zeilen (Import + register_blueprint) müssen weg, sonst startet der Service nicht. KEIN lokales pytest.

**autonomous:false** — Human-Verify (Test-Anruf 4 Oberflächen + Service startet sauber nach admin_ewb-Removal).

**url_for-Falle (routes/CLAUDE.md):** admin_ewb-Blueprint entfernt → prüfen ob irgendwo `url_for('admin_ewb.*')` referenziert wird (Navigation/Templates) — sonst BuildError. grep Pflicht.

**Migrations-Revision:** down_revision = echter HEAD beim Execute. DEPENDS-ON-TAXO1: prüfen ob TAXO1 das objection_events-Schild schon gesetzt hat (Migration 0015 = "Status: lebt"); TAXO2 ändert auf [ZOMBIE]. zombie_ewb_ratings-Schild ggf. schon von TAXO1 — dann nur ergänzen.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- objection_events-Leser (RESEARCH §A3) — korrigiert: 4 ECHTE Leser, NICHT 7. -->

ECHTE lebende Leser (umziehen + Semantik-Match):
- dashboard.py:736  db.query(ObjectionEvent).filter(conversation_log_id==sid).order_by(id).all()
    → session_detail: Liste der Einwände + success einer Session
- app_routes.py:446/449  count(ObjectionEvent.id) + sum(case(success==True)) filter conv.id
    → POLISH-38 Re-Aggregation: einwaende_gesamt=count, einwaende_behandelt=sum(success) → conversation_logs (:455-456)
- app_routes.py:471/473  query(ObjectionEvent).filter(conv.id).order_by(created_at)
    → POLISH-54: mergt OE-Rows in postcall['einwaende'] (Dedup typ+ts) für Cold-Call-Postcall-Kachel
- app_routes.py:1389  query(ObjectionEvent).filter_by(id=event_id).first()
    → EWB-rate-success: setzt ev.success (User-Rating eines Events)

NICHT-Leser (kein Umzug):
- admin_views.py:9  NUR import ObjectionEvent, KEIN ModelView → toter Import, nur entfernen
- integration_engine.py:43  NUR Kommentar → ignorieren
- admin_ewb.py:126  raw SQL LEFT JOIN objection_events → verschwindet mit D-06-Removal

intent_event-Ersatz-Quelle (TAXO1):
- intent_event.intent_type ('vorwand'/etc.), confidence (Tor-1 D-03), handling_score_numeric (Plan 03), interaction_id (UUID, Moment-Klammer), payload_jsonb.source (z.B. 'ui_asserted')
- **Einwand-Zähler = COUNT(DISTINCT interaction_id) WHERE intent_type vorwand-artig AND confidence>=0.70 (M-3 — NICHT roher COUNT(*))**
- "behandelt" = COUNT(DISTINCT interaction_id) der Momente mit handling_score_numeric IS NOT NULL (oder >= Stufe-Schwelle)

<!-- M-3 Moment-Klammer (TAXO-INTERLOCK-FINDINGS + TAXO1-01): interaction_id klammert ALLE Emits (Fast-Lane
     Keyword + Medium-Lane Haiku + Button/ui_asserted) EINES Kundenmoments. Ein einziger Einwand erzeugt
     darum MEHRERE intent_event-Zeilen mit gleichem interaction_id. Ein naiver COUNT(*) über die Zeilen
     VERDREIFACHT die Einwand-Zahl (Dashboard zeigt 9 statt 3). Fix: COUNT(DISTINCT interaction_id) — EIN
     Einwand pro Moment-Klammer. Alternative (gröber): WHERE payload_jsonb->>'source'='ui_asserted' (nur die
     Button-Emit-Zeile zählt). DISTINCT interaction_id ist robuster (zählt auch rein erkannte Einwände ohne
     Button). Gilt für BEIDE Zähl-Leser: dashboard.py:736 (session_detail) + app_routes.py:446/449 (POLISH-38
     einwaende_gesamt/einwaende_behandelt). interaction_id ist NULL-fähig (TAXO1-01) → NULL-Momente separat
     behandeln (jede NULL-Zeile = eigener Moment ODER ausschließen — beim Execute entscheiden + dokumentieren). -->


objection_bridge.py (TAXO1 Plan 05, # SUNSET: TAXO2): spiegelt intent_event-EWB → objection_events. ZU LÖSCHEN.

admin_ewb-Removal-Liste (RESEARCH §A5, D-06, grep-isoliert):
- routes/admin_ewb.py (Blueprint admin_ewb_bp :24, Routes :54/:106/:155)
- templates/admin/ewb_quality.html + templates/admin/ewb_rating_template.html
- app.py:2155 (Import) + :2161 (register_blueprint) — BEIDE
- EwbRating-Model (models.py:422)
- tests/test_admin_ewb_datetime.py + tests/test_ab_stats.py
- routes/CLAUDE.md:57 (admin_ewb-Blueprint-Zeile)
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `objection_events` (Leser umziehen WEG davon; [ZOMBIE]-Schild) — 8 Zeilen, kein handling_score
- `intent_event` (neue LESE-Quelle) — DEPENDS-ON-TAXO1
- `conversation_logs` (POLISH-38 schreibt einwaende_* — bleibt, Aggregat-Konsolidierung ist Plan 06/07; HIER nur Leser-Quelle wechseln)
- `ewb_ratings`/`zombie_ewb_ratings` (EwbRating-Model entfernen; Schild) — 0 Zeilen
### inspect.sh-Beleg (VERBATIM §A3)
```
$ inspect.sh count objection_events  → 8
objection_events: id, user_id, org_id, conversation_log_id, einwand_typ, success(3-state), created_at, antwort_text, einwand_text  (KEINE handling_score-Spalte)
```
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Alt-Pfad | Neu-Pfad | Schicht | Verifiziert? |
|---|---|---|---|---|
| einwaende_gesamt | count(ObjectionEvent) | **COUNT(DISTINCT interaction_id)** intent_event vorwand-artig, conf>=0.70 (M-3) | DB | ⬜ Task 1 (Semantik-Match: alt zählte kaputte Daten mit; M-3: DISTINCT statt roher COUNT) |
| einwaende_behandelt | sum(OE.success) | COUNT(DISTINCT interaction_id) mit handling_score_numeric IS NOT NULL | DB | ⬜ Task 1 |
| Einwand-Liste (session_detail) | OE.filter(conv) | intent_event.filter(call/conv) | DB | ⬜ Task 1 |
| ev.success (rate-Endpoint) | OE.filter_by(id) | intent_event/handling_score (oder Endpoint deprecaten) | DB | ⬜ Task 1 (Semantik-Match) |

## Verbindungs-Karte (Punkt 22)
- **4 ECHTE Leser bewiesen (RESEARCH §A3, korrigiert 7→4):** dashboard.py:736, app_routes:446/449, app_routes:471/473, app_routes:1389. admin_views.py:9 = toter Import (nur entfernen), integration_engine.py:43 = Kommentar (ignorieren), admin_ewb.py:126 = verschwindet mit D-06.
- **Semantik-Match-Pflicht-Beispiel (RESEARCH §A3):** app_routes:446/449 zählt heute count(*) über ObjectionEvent (jeder EWB-Klick, auch ohne valides Ereignis). Neue Quelle intent_event mit Tor-1-confidence>=0.70 ändert die Bedeutung BEWUSST (alte zählte kaputte Daten mit) → dokumentieren, nicht still ändern.
- **admin_ewb isoliert (RESEARCH §A5):** Live-Refs NUR eigene Routes + app.py-Reg + EwbRating-Model. Punkt-20-Re-grep Pflicht (seit TAXO1 evtl. neue Refs).

## 1.-4. Control-Flow + Cross-File (Punkt 14)
**Cross-File-grep (Pflicht vor jeder Änderung):**
`grep -rn "objection_events\|ObjectionEvent" services/ routes/ database/` → muss am Ende == 0 aktive Leser (außer Zombie-Def).
`grep -rn "admin_ewb\|admin_ewb_bp\|EwbRating\|url_for('admin_ewb" services/ routes/ templates/ app.py` → alle Refs identifizieren vor Removal.
**Edge:** Reihenfolge — handling_score MUSS befüllt sein (Plan 03 depends_on) bevor "behandelt"-Zähler auf intent_event.handling_score_numeric umzieht, sonst zeigt der Zähler 0. Beleg: depends_on=[03,04].

<tasks>

<task type="auto">
  <name>Task 1: 4 objection_events-Leser auf intent_event umziehen + Semantik-Match-Doku</name>
  <files>routes/dashboard.py, routes/app_routes.py</files>
  <read_first>
    - routes/dashboard.py:736 (session_detail OE-Liste)
    - routes/app_routes.py:446/449 (Re-Aggregation), :471/473 (POLISH-54 Postcall-Merge), :1389 (rate-success)
    - intent_event-Vertrag (TAXO1 Plan 01: intent_type/confidence/handling_score_numeric)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §A3 (Semantik-Match-Beispiel)
  </read_first>
  <action>
    Ziehe die 4 Leser auf intent_event(+handling_score) um. Pro Leser ein dokumentierter Semantik-Match (in SUMMARY):
    1. **dashboard.py:736** (session_detail Einwand-Liste): statt ObjectionEvent → intent_event-Query (einwand-artige intent_type, confidence>=Tor-1, filter call/conversation_log). Liste der Einwände + handling_score_numeric (statt success). **M-3: die Liste/Zählung gruppiert nach `interaction_id` (EIN Eintrag pro Moment-Klammer) — NICHT eine Zeile pro Emit, sonst erscheint derselbe Einwand 3× (Fast+Medium+Button).** Pro Moment den repräsentativen Emit wählen (z.B. höchste confidence ODER source='ui_asserted'). Semantik-Match: success(3-state) → handling_score_numeric(1-3/NULL); dokumentieren.
    2. **app_routes.py:446/449** (POLISH-38 Re-Aggregation): **einwaende_gesamt = COUNT(DISTINCT interaction_id) WHERE intent_event einwand-artig AND confidence>=0.70 (M-3 — NICHT roher COUNT(*), sonst verdreifacht die Moment-Klammer den Wert)**; einwaende_behandelt = COUNT(DISTINCT interaction_id) der Momente mit handling_score_numeric IS NOT NULL (bzw. >= Stufe). Semantik-Match (zwei bewusste Änderungen dokumentieren): (a) alte count(*) zählte JEDEN EWB-Klick (auch kaputte Daten), neue ist Tor-1-gefiltert; (b) M-3: alte Quelle = 1 Zeile/Einwand, neue Quelle = mehrere Emits/Moment → DISTINCT interaction_id korrigiert die Zähl-Einheit. (Diese Stelle schreibt conversation_logs :455-456 — bleibt vorerst; die volle Aggregat-Konsolidierung ist Plan 06/07. Hier nur die LESE-Quelle der Zahl wechseln.)
    3. **app_routes.py:471/473** (POLISH-54 Postcall-Kachel): intent_event-Merge in postcall['einwaende'] (Dedup typ+ts). Semantik-Match dokumentieren.
    4. **app_routes.py:1389** (rate-success-Endpoint): setzt heute OE.success. Neue Quelle hat keine User-Rating-Spalte (handling_score ist auto). Entscheidung: Endpoint deprecaten (404/no-op mit Klartext) ODER auf abstain_log/manuelles-Review-Plant-Seed verweisen. D-06 Plant-Seed: manuelles Review-Tool kommt später nativ auf intent_event/rubric_score. Dokumentieren.
    KEINE objection_events-Schreibung mehr (Brücke kommt Task 3 raus).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ObjectionEvent"'</automated>
  </verify>
  <acceptance_criteria>
    - Alle 4 Leser nutzen intent_event statt ObjectionEvent
    - **M-3: die Zähl-Leser (dashboard.py:736, app_routes.py:446/449) nutzen `COUNT(DISTINCT interaction_id)` bzw. gruppieren nach interaction_id (ODER filtern source='ui_asserted') — `grep -nE "distinct.*interaction_id|interaction_id" routes/dashboard.py routes/app_routes.py` belegt es; KEIN roher count(*) über intent_event-Zeilen für Einwand-Zahlen**
    - Test-Anruf-Beleg (M-3): ein Cold-Call mit 1 Einwand, der Fast+Medium+Button auslöst (mehrere intent_event-Zeilen, 1 interaction_id) → Dashboard zeigt **1** Einwand, NICHT 3
    - Semantik-Match pro Leser in SUMMARY dokumentiert (insb. :446/449 Tor-1-Filter = bewusste Bedeutungs-Verbesserung + M-3 Zähl-Einheit interaction_id)
    - "behandelt"-Zähler liest handling_score_numeric (Plan-03-Quelle) — nicht 0 (depends_on 03 erfüllt)
    - rate-success-Endpoint: deprecaten/no-op-Entscheidung dokumentiert (kein Crash)
  </acceptance_criteria>
  <done>4 Leser auf intent_event umgezogen, Semantik-Match pro Leser dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 2: admin_ewb ersatzlos entfernen (D-06, vollständige Liste) + Plant-Seed</name>
  <files>routes/admin_ewb.py, app.py, database/models.py, templates/admin/ewb_quality.html, templates/admin/ewb_rating_template.html, routes/CLAUDE.md</files>
  <read_first>
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §A5 (Removal-Liste grep-belegt)
    - app.py (Zeilen mit admin_ewb Import + register_blueprint — beim Execute exakte Zeilen greppen, :2155/:2161 sind RESEARCH-Stand)
    - database/models.py (EwbRating-Model)
  </read_first>
  <action>
    Punkt-20-Re-grep ZUERST: `grep -rn "admin_ewb\|admin_ewb_bp\|EwbRating\|url_for('admin_ewb" services/ routes/ templates/ app.py` — bestätige Isolation (seit TAXO1 evtl. neue Refs). Dann ersatzlos entfernen:
    1. `routes/admin_ewb.py` löschen.
    2. `templates/admin/ewb_quality.html` + `templates/admin/ewb_rating_template.html` löschen.
    3. `app.py`: BEIDE Zeilen raus — `from routes.admin_ewb import admin_ewb_bp` UND `register_blueprint(admin_ewb_bp)` (toter Import crasht Gunicorn-Start, D-06).
    4. `database/models.py`: `EwbRating`-Model entfernen (Leser nur admin_ewb).
    5. `tests/test_admin_ewb_datetime.py` + `tests/test_ab_stats.py` löschen (sonst CI rot).
    6. `routes/CLAUDE.md:57` (admin_ewb-Blueprint-Zeile) entfernen.
    7. Falls `url_for('admin_ewb.*')` irgendwo (Nav/Template) → Link entfernen (sonst BuildError).
    **Plant-Seed (Anti-Regression, D-06):** Eintrag in Nerve-Vault/05 Log + Foundation-Code-Register: ein manuelles Review-/Korrektur-Tool kommt später als Teil des Active-Learning-Flywheels — MUSS nativ auf intent_event/rubric_score + DPO/RLHF-Labeling bauen, KEIN Rollback zum alten 3-Checkbox-UI.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "admin_ewb\|EwbRating"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "admin_ewb\|admin_ewb_bp" app.py routes/ templates/` == 0
    - `grep -rn "EwbRating" services/ routes/ database/` == 0
    - `grep "url_for('admin_ewb" templates/ routes/` == 0
    - routes/admin_ewb.py + beide Templates + 2 Tests gelöscht
    - Service startet nach Deploy sauber (Gunicorn kein ImportError — Health-Check ok)
    - Plant-Seed-Eintrag (05 Log + Foundation-Register) vorhanden
  </acceptance_criteria>
  <done>admin_ewb komplett raus, Service startet, Plant-Seed gesetzt.</done>
</task>

<task type="auto">
  <name>Task 3: Brücke löschen + grep==0 Leser + [ZOMBIE]-Schilder (Reihenfolge: NACH Task 1)</name>
  <files>services/objection_bridge.py, database/models.py, alembic/versions/XXXX_zombie_schilder.py</files>
  <read_first>
    - services/objection_bridge.py (# SUNSET: TAXO2)
    - .planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-05-zombie-rename-dual-write-PLAN.md (Brücken-Vertrag + objection_events/ewb_ratings-Schild-Stand)
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-Stil) + ssh ... inspect.sh schilder objection_events
  </read_first>
  <action>
    NUR ausführen NACHDEM Task 1 alle Leser umgezogen hat (Reihenfolge-Zwang Req 7c).
    1. Pflicht-Gate: `grep -rn "objection_events\|ObjectionEvent" services/ routes/ database/` == 0 aktive Leser (außer der Zombie-Model-Definition in models.py). Wenn >0 → STOP (übersehener Leser = Crash-by-design).
    2. `services/objection_bridge.py` LÖSCHEN (# SUNSET: TAXO2-Marker damit weg). Prüfen wo die Brücke aufgerufen wird (Call-Ende-Spiegelung) → Aufruf entfernen.
    3. ObjectionEvent-Model in models.py auf Zombie-Status: NICHT droppen, [ZOMBIE]-Schild im Tabellen-comment ("... Status: write-only [ZOMBIE]. Ersetzt durch intent_event+handling_score (TAXO2). NICHT droppen.").
    4. Migration: `op.execute("COMMENT ON TABLE public.objection_events IS '... [ZOMBIE] ...'")` (DEPENDS-ON-TAXO1: TAXO1-Stand "lebt via Brücke" → auf [ZOMBIE] ändern). Plus `zombie_ewb_ratings`-[ZOMBIE]-Schild falls TAXO1 es noch nicht gesetzt hat (sonst nur bestätigen). down_revision = echter HEAD.
    objection_events-Tabelle + Daten bleiben (Zombie-Regel: nur Schild, nicht droppen).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "objection_events\|ObjectionEvent" && bash scripts/inspect.sh schilder objection_events'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "objection_events\|ObjectionEvent" services/ routes/ database/` == 0 aktive Leser (nur Zombie-Model-Def)
    - `services/objection_bridge.py` existiert nicht mehr; `grep -rn "objection_bridge\|# SUNSET: TAXO2" services/ routes/` == 0
    - `inspect.sh schilder objection_events` zeigt [ZOMBIE]-Schild
    - objection_events-Tabelle existiert weiter (NICHT gedroppt, `inspect.sh count objection_events` läuft)
    - zombie_ewb_ratings trägt [ZOMBIE]-Schild
  </acceptance_criteria>
  <done>Brücke gelöscht, grep==0 Leser, [ZOMBIE]-Schilder gesetzt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>4 objection_events-Leser auf intent_event umgezogen, Brücke gelöscht, [ZOMBIE]-Schild, admin_ewb ersatzlos raus.</what-built>
  <how-to-verify>
    Nach `bash deploy.sh production`:
    1. Service startet sauber (kein Gunicorn-ImportError nach admin_ewb-Removal) — `inspect.sh health`.
    2. **Test-Anruf** (Cold-Call mit ≥1 Einwand) als Test-User → alle 4 Oberflächen rendern korrekt aus der NEUEN Quelle (intent_event):
       - Dashboard-Einwand-Zähler (zeigt gefilterte Anzahl, Semantik-Match; **M-3: 1 echter Einwand = 1 gezählt, NICHT 3 — auch wenn Fast+Medium+Button gefeuert haben**)
       - session_detail Einwand-Liste (mit handling_score statt success)
       - interne Auswertung/Postcall-Kachel
       - Call-Ende-Nachrechnung
    3. admin/ewb-Routen sind weg (404 — kein Crash, keine Nav-Links die brechen).
    Claudian: `inspect.sh grep "objection_events"` == 0 aktive Leser; `inspect.sh schilder objection_events` zeigt [ZOMBIE].
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe welche Oberfläche/Route bricht.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Leser-Umzug → intent_event | falsche Query → falsche Einwand-Zahlen |
| admin_ewb-Removal → app.py | toter Import crasht Gunicorn |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-05-01 | D (Crash-by-design) | übersehener objection_events-Leser nach Brücken-Delete | mitigate | grep==0-Pflicht-Gate VOR Brücken-Delete (Task 3); Reihenfolge handling_score→Leser→Brücke (depends_on 03). |
| T-TAXO2-05-02 | D (Crash) | toter admin_ewb-Import crasht Gunicorn-Start | mitigate | BEIDE app.py-Zeilen raus (Import + register); Health-Check nach Deploy. |
| T-TAXO2-05-03 | T (Data semantics) | neue Zähler bedeuten was anderes (stille Drift) | mitigate | Semantik-Match pro Leser dokumentiert (Tor-1-Filter = bewusste Verbesserung, nicht still). |
| T-TAXO2-05-05 | I (Falsche Kennzahl / M-3) | Moment-Klammer (Fast+Medium+Button = mehrere intent_event-Zeilen/Einwand) → roher COUNT(*) verdreifacht die Einwand-Zahl | mitigate | **M-3: COUNT(DISTINCT interaction_id) (ODER source='ui_asserted') statt roher Zeilen-COUNT für beide Zähl-Leser. Test-Anruf belegt: 1 Einwand → Dashboard zeigt 1, nicht 3.** |
| T-TAXO2-05-04 | I | objection_events-Daten bleiben (Zombie) | accept | Zombie-Regel: nur [ZOMBIE]-Schild, nicht droppen (Daten heilig, Cleanup = eigener Bau). |
</threat_model>

<verification>
- Test-Anruf-DATEN-FLUSS (riskante Welle): 4 Oberflächen rendern aus intent_event.
- M-3: Zähl-Leser nutzen COUNT(DISTINCT interaction_id); Test-Anruf mit 1 Einwand (Fast+Medium+Button) → Dashboard zeigt 1, nicht 3.
- grep==0 aktive objection_events-Leser; objection_bridge.py weg; [ZOMBIE]-Schild.
- admin_ewb komplett raus; Service startet; kund url_for-BuildError.
- Semantik-Match pro Leser dokumentiert.
</verification>

<success_criteria>
- objection_events voll zombifiziert (Leser umgezogen, Brücke weg, [ZOMBIE]-Schild).
- admin_ewb ersatzlos entfernt + Plant-Seed.
- Reihenfolge eingehalten (handling_score→Leser→Brücke).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-05-SUMMARY.md` (inkl. Semantik-Match pro 4 Leser, **M-3-Beleg (COUNT DISTINCT interaction_id, Test-Anruf 1≠3) + NULL-interaction_id-Behandlungs-Entscheidung**, grep==0-Beleg, [ZOMBIE]-Schild-Output, admin_ewb-Removal-Beleg + Health-Check, Plant-Seed-Eintrag).
</output>
