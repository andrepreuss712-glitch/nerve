---
phase: 08.23.2.TAXO2
plan: 09
type: execute
wave: 1
depends_on: []
# FOLD A-2 (André 22.06.): ANON-LIVE-ANSWER von TAXO3 nach TAXO2 gezogen (Anti-Abrieb — Plan 08
# fasst die Anonymisierung sowieso an). Eigener Plan (NICHT in Plan 08 gequetscht): beruehrt
# BESTEHENDEN Live-Antwort-Code, eigenes DSGVO-Threat-Model (Live-vs-Stored, Ueber-Schaerfe, Mapping-Leben).
# Laeuft VOR Plan 08 (Plan 08 depends_on 09 — konsumiert die Storage-Version).
files_modified:
  - services/claude_service.py
  - services/anonymization.py
  - services/deepgram_service.py
  - tests/test_anon_live_vs_stored.py
autonomous: false
requirements: ["SPEC Req 11 (FOLD A-2)"]
user_setup: []

must_haves:
  truths:
    - "Die dem Berater LIVE angezeigte NERVE-Antwort (Auto-Variante pip_token_done) traegt ECHTE Namen — KEIN [PERSON_A]. Die WR-01-Anonymisierung des angezeigten Endergebnisses (claude_service.py:675-679) wird auf die STORAGE-Version umgelenkt, NICHT auf die Anzeige."
    - "Pro NERVE-Antwort existiert EINE anonymisierte STORAGE-Version ueber den gemeinsamen Helper anonymize_for_storage(text, sid): Cache-Replay wenn der Per-SID-Merkzettel da ist (konsistente Tokens wie Transkript), sonst FRISCHE Saeuberung anonymize() (nie roh — '[PERSON_X]'/Sentinel), schlimmstenfalls geschwaerzt+markiert. NIE roh, NIE verworfen, jeder Notweg geloggt. Diese Version ist der Vertrag, den Plan 08 (suggestion_reactions) + record_ewb_click + Transkript konsumieren."
    - "Der Manuelle-Knopf-Pfad zeigt schon roh (streame_manual_ewb_variante:823). DIESER Plan bringt die Auto-Variante auf dasselbe Anzeige-Muster UND macht beide Storage-Pfade konsistent: der heute fail-OPENE Knopf-Storage (deepgram_service.py:949-953 — bei Anon-Fehler bleibt _antwort ROH) wird auf anonymize_for_storage umgestellt (Andre-Entscheidung 22.06.: beide Wege gleich, nie roh, nie verloren)."
    - "Die NER-Ueber-Schaerfe ('Ihnen'->[PERSON_A]) wird durch ERGAENZEN der BESTEHENDEN PRONOMEN_WHITELIST (anonymization.py:116) um die fehlende Dativform 'ihnen' (+ 'euch'/'euer'/'eure'/'sich'/'einander') behoben — KEIN neuer Mechanismus. Da _is_whitelisted an allen 6 NER-Stellen aufgerufen wird, deckt diese EINE Listen-Aenderung alle Pfade (sequenziell+parallel, spaCy+GLiNER). DSGVO: keine realen Nachnamen, kein Heuristik-Lockern."
    - "Cold-Call + Meeting-Modus konsistent (gleiches Live-roh / Storage-anon-Verhalten)"
    - "Latenz (Punkt 25): die Anzeige (pip_token_done, roh) wird VOR der Storage-Berechnung emittiert (Berater hat die Antwort schon); im Normalfall ist Storage = Cache-Replay (String-Replace), der seltene frische Fallback verzoegert nur die Hintergrund-Storage, NIE die Anzeige — kein spuerbarer Live-Antwort-Verzug"
  artifacts:
    - path: "services/claude_service.py"
      provides: "streame_auto_variante: Anzeige roh (echte Namen) + separate anonymisierte Storage-Version (Vertrag fuer Plan 08)"
      contains: "pip_token_done"
    - path: "services/anonymization.py"
      provides: "PRONOMEN_WHITELIST um fehlende Funktionswoerter ('ihnen' etc.) ergaenzt (Ueber-Schaerfe-Fix, bestehende _is_whitelisted-Single-Source — kein neuer Mechanismus) + NEUER Helper anonymize_for_storage (nie roh, nie verloren, geloggt)"
      contains: "def anonymize_for_storage"
    - path: "tests/test_anon_live_vs_stored.py"
      provides: "Live-roh-vs-Storage-anon-Test + Ueber-Schaerfe-Balance-Test (beide Richtungen: Funktionswort bleibt, echter Name geschwaerzt)"
  key_links:
    - from: "services/claude_service.py streame_auto_variante (Anzeige)"
      to: "sio.emit('pip_token_done', ... roh ...)"
      via: "Live-Display behaelt echte Namen (WR-01-Anon umgelenkt auf Storage)"
      pattern: "pip_token_done"
    - from: "services/claude_service.py streame_auto_variante (Storage-Version)"
      to: "Plan 08 record_suggestion_offer + (kuenftig) Transkript/record_ewb_click"
      via: "anonymize_output(raw, get_anonymisierer(sid)) — lebender Cache, am Erfassen"
      pattern: "anonymize_output"
    - from: "services/anonymization.py _is_whitelisted (an allen 6 NER-Stellen)"
      to: "PRONOMEN_WHITELIST (+ 'ihnen' etc.)"
      via: "PERSON-Tagging ueberspringt die bestehende Whitelist — EINE Listen-Aenderung deckt alle NER-Pfade (Ueber-Schaerfe-Fix)"
      pattern: "PRONOMEN_WHITELIST"
    - from: "services/claude_service.py + services/deepgram_service.py (Storage-Schreibungen)"
      to: "anonymize_for_storage(text, sid)"
      via: "EIN gemeinsamer Storage-Anon-Helper: Replay->frische Saeuberung->blank, nie roh/nie verloren, geloggt"
      pattern: "anonymize_for_storage"
---

<objective>
Setze SPEC Req 11 (FOLD A-2, ANON-LIVE-ANSWER) um: die dem Berater LIVE gezeigte NERVE-Antwort behält die ECHTEN Namen (anonymisierter Live-Text „[PERSON_A]" ist für den Berater unbrauchbar), und NUR die gespeicherte Kopie wird anonymisiert — am Erfassen, solange das Per-SID-Namens-Mapping lebt.

**Drei bewiesene Code-Befunde (gegen git-Stand = Prod, 22.06.):**
1. **Live-Display-Bug (Auto-Variante):** `streame_auto_variante` reassignt `cleaned = anonymize_output(cleaned, cache)` (claude_service.py:675) und emittiert genau dieses anonymisierte `cleaned` ins finale `pip_token_done` (`:679`) → der Berater sieht „[PERSON_A]" statt „Herr Müller". Der Knopf-Pfad ist demgegenüber schon korrekt: `streame_manual_ewb_variante` emittiert roh (`:823`), die Anonymisierung passiert NUR für die Storage im Caller (`deepgram_service.py:953` vor `record_ewb_click`).
2. **cache=None ist ein No-Op:** `anonymize_output(text, cache=None)` gibt den Text UNVERÄNDERT zurück (anonymization.py:612-613 — reiner Cache-Reverse-Lookup, KEIN NER). → Storage-Anonymisierung MUSS mit dem lebenden Per-SID-Cache laufen, NIE ephemer beim Flush.
3. **Über-Schärfe:** der NER-Input-Pfad (`anonymize`/`_apply_ner`) taggt harmlose Funktionswörter („Ihnen") als PERSON → `[PERSON_A]`.

**Was dieser Plan tut:** (a) die Auto-Variante auf das (schon korrekte) Knopf-Muster bringen — Anzeige roh, separate anonymisierte Storage-Version; (b) die Storage-Version als Vertrag bereitstellen, den Plan 08 (`suggestion_reactions`) konsumiert; (c) die NER-Über-Schärfe konservativ entschärfen, OHNE echte PII durchzulassen.

Purpose: Live-Nutzen (echte Namen) + DSGVO (Storage anonymisiert) gleichzeitig; Fundament für Plan 08s anonymisierte `suggestion_text`. Plan 08 `depends_on: [09]`.
Output: Auto-Varianten-Display-roh + Storage-Vertrag; NER-Funktionswort-Stopliste; Live-vs-Storage- + Über-Schärfe-Tests.

KEINE Änderung am Knopf-Display (schon korrekt). Beide Storage-Pfade (Auto + Knopf) auf den gemeinsamen Helper anonymize_for_storage (nie roh/nie verloren). KEIN Lockern der Namens-Erkennung — nur Ergaenzen der bestehenden PRONOMEN_WHITELIST um fehlende Funktionswoerter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only (CLAUDE.md HART):** `bash deploy.sh production` + `inspect.sh` gegen Prod. KEIN lokales pytest als Acceptance. RISKANTE + DSGVO-Welle (Live-Antwort-Pfad + PII) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht: Live-Antwort zeigt echten (Test-)Namen, gespeicherte Kopie anonymisiert.

**autonomous:false** — Human-Verify Pflicht (DSGVO): Test-Anruf mit echtem Test-Namen → Berater sieht echten Namen LIVE, Storage anonymisiert; Über-Schärfe-Gegenprobe.

**DSGVO (HART, 🔴):** Cross-AI Pflicht bei diesem Plan (Live-vs-Stored-Split + NER-Stopliste). Die Stopliste ist die heikelste Stelle — sie darf NIE einen echten Namen durchlassen. Referenz: `Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse`.

**Latenz (Punkt 25):** Die Anzeige emittiert roh (sofort, kein zusätzlicher Schritt). Die Storage-Anonymisierung (`anonymize_output`, String-Replace, kein NER/LLM) ist der bestehende erprobte Aufruf — nur sein ZIEL (Storage statt Anzeige) ändert sich. Kein spürbarer Verzug.

**UTF-8:** Stopwort-Liste enthält deutsche Funktionswörter mit Umlauten — das sind Daten-Strings (Vergleichswerte), KEINE Identifier → Umlaute OK (z.B. „für"); Code-Identifier (STOPWORD_*) ASCII.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-FOLD-2026-06-22.md

<interfaces>
<!-- Bewiesen aus dem Code, 22.06. -->

streame_auto_variante (claude_service.py:572, Slot-B Auto-Variante):
- :668  sio.emit('pip_token', {'token': token, 'raw_text': True})   # Streaming roh — bleibt
- :669  cleaned = full_text.strip()
- :672-675  cleaned = anonymize_output(cleaned, get_anonymisierer(sid))   # ← REASSIGN: macht die Anzeige anon (BUG)
- :678  result = {'einwand': True, 'typ': 'AUTO', 'gegenargument_1': cleaned}
- :679  sio.emit('pip_token_done', {'result': result, 'raw_text': True})   # ← zeigt anon (BUG)
→ FIX: cleaned_display = full_text.strip() (ROH, an pip_token_done); cleaned_storage =
  anonymize_output(cleaned_display, cache) (separat, fuer Plan 08 / kuenftige Storage). Anzeige = roh.

streame_manual_ewb_variante (claude_service.py:719, SCHON KORREKT — Vorbild):
- :823  sio.emit('pip_token_done', {'result': {'gegenargument_1': cleaned}})   # cleaned ROH → Anzeige roh ✓
- Caller deepgram_service.py:949-953: _antwort = anonymize_output(_antwort, get_anonymisierer(_sid))   # NUR Storage ✓
→ Auto-Variante auf GENAU dieses Muster bringen.

anonymize_output (anonymization.py:597): Cache-Reverse-Lookup; cache=None → No-Op (return text, :612-613).
anonymize (anonymization.py:534): INPUT-Pfad; Schritt 2 = _apply_ner (PER/LOC/ORG → Tokens). Die Ueber-Schaerfe
  ('Ihnen'→PERSON) sitzt im NER-Schritt → dort die Funktionswort-Stopliste einhaengen.

get_anonymisierer(sid) (live_session.py:292): gibt den Per-SID-Cache (lebt waehrend des Calls,
  _session_state[sid]['anonymisierer'], erstellt :288). Waehrend des Calls IMMER lebendig.
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen / Schichten
- KEINE Schema-Aenderung. Betroffen sind Live-Pfade (SocketIO-Emit) + die Anonymisierungs-Funktion + die Storage-Versionen (Transkript-Append / record_ewb_click / Plan-08-record_suggestion_offer).
- Storage-Schichten (Lese-Referenz, NICHT in diesem Plan geschrieben): `conversation_logs`-Transkript (anonymisiert am Input deepgram_service.py:119), `ewb_clicks` (RAM, anonymisiert deepgram_service.py:953), `suggestion_reactions` (Plan 08). Dieser Plan stellt die Storage-VERSION bereit, schreibt sie nicht selbst.
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| `cleaned_display` (roh) | streame_auto_variante → pip_token_done | SocketIO-Emit (Anzeige, fluechtig) | ⬜ Task 1 + Test-Anruf |
| `cleaned_storage` (anon) | anonymize_output(roh, get_anonymisierer(sid)) | Vertrag → Plan 08 record_suggestion_offer | ⬜ Task 1+2 |
| NER-Funktionswort-Skip | anonymize → _apply_ner | reine Logik (kein Persist), wirkt auf alle Storage-Kopien | ⬜ Task 3+4 |

## Verbindungs-Karte (Punkt 22)
- **Live-Display-Anon-Bug (bewiesen):** `grep -n "anonymize_output\|pip_token_done" services/claude_service.py` → :675 reassign + :679 emit (Auto-Variante zeigt anon); :823 emit roh (Knopf zeigt roh, korrekt). Der Fix entkoppelt Anzeige (roh) von Storage (anon) in der Auto-Variante.
- **cache=None-No-Op (bewiesen):** `anonymization.py:612-613` → kein Storage-Anon mit cache=None. Storage-Anon nutzt get_anonymisierer(sid) (lebt waehrend Call).
- **NER-Ueber-Schaerfe (André-Produktbefund, Audit-Wurzel):** der NER-Schritt taggt 'Ihnen' als PERSON, WEIL die bestehende `PRONOMEN_WHITELIST` (:116) zwar 'ihr/ihre/ihrer/...' enthaelt, aber die Dativform 'ihnen' vergisst. Fix = die fehlenden Woerter zur Liste ergaenzen — `_is_whitelisted` (:158) wird an allen 6 NER-Stellen (`grep -n "_is_whitelisted" services/anonymization.py` → :365,392,442,471,699,727) aufgerufen, EINE Aenderung deckt alle Pfade. KEIN neuer Mechanismus (vermeidet Gatekeeper-Duplikat).
- **Knopf-Pfad unberuehrt:** streame_manual_ewb_variante:823 + deepgram_service.py:953 bleiben — DIESER Plan aendert NUR die Auto-Variante + die NER-Stopliste (die ALLEN Storage-Kopien zugutekommt).

## 1.-4. Control-Flow + Edge-Cases (Punkt 14)
1. **Anzeige roh, Storage anon — Entkopplung:** in streame_auto_variante NICHT mehr `cleaned` reassignen; zwei Variablen (display roh / storage anon). pip_token_done bekommt display. Plan 08 (depends_on) bekommt storage.
2. **cache None (Ghost-SID, Pitfall 3):** get_anonymisierer(sid) kann None liefern (Race) → Cache-Replay waere No-Op/roh. anonymize_for_storage faengt das: bei None-Cache FRISCHE Saeuberung statt Replay (nie roh), Zeile bleibt (nie verwerfen), Fallback geloggt. Die Anzeige ist ohnehin roh.
3. **Stopliste vs. echter Name:** ein Funktionswort darf nie geschwaerzt werden, ein echter Name nie durchrutschen. Die Stopliste ist eine geschlossene Whitelist von Funktionswoertern, die das NER-PERSON-Tagging ueberspringt — sie LOCKERT NICHT die Namens-Heuristik (ein als PERSON erkannter echter Name bleibt geschwaerzt). Test beide Richtungen (Task 4).
4. **Cold vs Meeting:** beide Modi nutzen denselben Anzeige-/Storage-Pfad → Fix gilt fuer beide. Test-Anruf in beiden Modi (oder Beleg dass der Pfad modus-agnostisch ist).
5. **Art-9 / Sentinel:** anonymize_output gibt keine Sentinel zurueck (Finding 4, claude_service.py:1084) — kein Skip-Check noetig fuer die Output-Version. Die NER-Stopliste beruehrt den Art-9-Filter (Schritt 0) NICHT.

<tasks>

<task type="auto">
  <name>Task 1: streame_auto_variante — Anzeige roh, separate anonymisierte Storage-Version (auf das Knopf-Muster bringen)</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py:668-680 (Auto-Variante: pip_token + cleaned-reassign + pip_token_done)
    - services/claude_service.py:820-823 (streame_manual_ewb_variante — Vorbild: pip_token_done ROH)
    - services/deepgram_service.py:949-953 (Knopf-Storage-Anon — Vorbild: anonymize_output NUR fuer record_ewb_click)
    - services/anonymization.py:597-613 (anonymize_output + cache=None-No-Op)
  </read_first>
  <action>
    In `streame_auto_variante` (claude_service.py:~669-679) die Anzeige von der Storage entkoppeln:
    1. `cleaned_display = full_text.strip()` — der ROHE Text (echte Namen).
    2. `result = {'einwand': True, 'typ': 'AUTO', 'gegenargument_1': cleaned_display}`; `sio.emit('pip_token_done', {'slot': slot, 'result': result, 'raw_text': True}, room=sid)` — Anzeige ROH (echte Namen). **Die bisherige Zeile `cleaned = anonymize_output(cleaned, cache)` VOR dem Emit ENTFERNEN** (sie machte die Anzeige anon — der WR-01-Zweck wandert in die Storage-Version unten).
    3. Separate **Storage-Version** ueber den NEUEN gemeinsamen Helper (Task 2): `cleaned_storage = anonymize_for_storage(cleaned_display, sid)`. Der Helper garantiert "nie roh, nie verloren": (a) Merkzettel da -> Cache-Replay (konsistente Tokens wie Transkript); (b) Merkzettel fehlt/Replay wirft -> FRISCHE Saeuberung `anonymize(text, cache_or_None)` (liefert auch ohne Cache `[PERSON_X]`/Sentinel, NIE rohe Namen — Audit-belegt anonymization.py:534/:574); (c) auch das wirft -> geschwaerzte/markierte Minimal-Form (Zeile bleibt); (d) jeder Notweg (b/c) wird geloggt. Die ANZEIGE ist davon unberührt (immer roh).
    **Latenz (Punkt 25):** die Anzeige (`pip_token_done`, roh) wird VOR der Storage-Berechnung emittiert — der Berater hat seine Antwort schon; der (seltene) frische Fallback verzoegert nur die Hintergrund-Storage, nicht die Anzeige.
    4. `cleaned_storage` als Rückgabe/Feld bereitstellen, das Plan 08 (record_suggestion_offer) konsumiert (z.B. result-internes Feld `_storage_text` oder Rückgabewert-Erweiterung — beim Execute die schmalste Naht wählen + in SUMMARY dokumentieren). **Das ist der Vertrag, auf den Plan 08 depends_on.** Da der Helper IMMER einen sauberen String liefert (nie None/roh), bekommt Plan 08 immer eine schreibbare anonymisierte Version (vereinfacht den Plan-08-Flush: kein None-Handling, kein Re-Anon).
    Kommentar setzen: „FOLD A-2 / Req 11: Anzeige roh (echte Namen, Live-Nutzen), Storage anonymisiert (DSGVO) — analog Knopf-Pfad (streame_manual_ewb_variante:823 + deepgram_service.py:953)."
    KEIN anderes pip_token_done anfassen (Knopf :823 ist schon roh).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "cleaned_storage\|cleaned_display"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "cleaned_display|cleaned_storage" services/claude_service.py` belegt die Entkopplung
    - pip_token_done der Auto-Variante emittiert die ROHE Version: `grep -A2 "result = .*AUTO" services/claude_service.py` zeigt cleaned_display (kein anonymize_output davor)
    - die alte „cleaned = anonymize_output(cleaned, ...)"-Reassign-Zeile VOR dem Auto-Variante-Emit ist weg (`grep -n "cleaned = anonymize_output" services/claude_service.py` trifft NICHT mehr die Auto-Variante-Stelle :675)
    - cleaned_storage wird ueber anonymize_for_storage(text, sid) erzeugt (Task 2) + als Vertrag fuer Plan 08 bereitgestellt
    - Ghost-SID-Edge (None-Cache): Helper faellt auf frische Saeuberung zurueck (nie roh, nie verworfen) + loggt — dokumentiert
  </acceptance_criteria>
  <done>Auto-Variante: Anzeige roh, Storage-Version separat ueber anonymize_for_storage (nie roh/nie verloren); Vertrag fuer Plan 08 da.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gemeinsamer Storage-Anon-Helper anonymize_for_storage (nie roh / nie verloren / geloggt) + auf Auto- UND Knopf-Pfad anwenden — Konsistenz (Andre-Entscheidung 22.06.)</name>
  <files>services/anonymization.py, services/claude_service.py, services/deepgram_service.py</files>
  <read_first>
    - services/anonymization.py:597-613 (anonymize_output — Cache-Replay; cache=None → No-Op/roh) + :534-595 (anonymize — frische NER; cache=None → '[PERSON_X]'/Sentinel, NIE roh; Audit-belegt)
    - services/deepgram_service.py:948-957 (Knopf-Storage: HEUTE fail-OPEN — "Fallback: _antwort bleibt unveraendert" laesst rohen Text bei Anon-Fehler durch; genau das wird konsistent gemacht)
    - services/einwand_keyword_matcher.py:300-325 (Keyword: profil-statischer Vorschlag — PII-Risiko niedrig)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-08-suggestion-offer-capture-PLAN.md Task 2 (record_suggestion_offer-Hooks)
  </read_first>
  <action>
    1. **NEUER Helper in services/anonymization.py:** `def anonymize_for_storage(text, sid) -> str` — EINE Quelle der Wahrheit fuers "Text fuer die Speicherung saeubern" (Single-Source wie _is_whitelisted; verhindert Drift zwischen den Storage-Stellen). Logik:
       (a) Cache holen (`live_session.get_anonymisierer(sid)`). Cache vorhanden → `anonymize_output(text, cache)` (konsistente Tokens wie Transkript). Nur nutzen wenn der Cache wirklich da war (cache=None-No-Op-Falle vermeiden).
       (b) Cache None ODER (a) wirft → FRISCHE Saeuberung `anonymize(text, cache_or_None)` (Audit: liefert nie rohe Namen — '[PERSON_X]'/Sentinel). Tuple-Return → Text-Teil nehmen; Sentinel ('[ART9_REDACTED]'/'[ANON_FEHLER]') ist KEIN roher Name → akzeptabel als Storage-Wert (oder leeren + markieren).
       (c) auch (b) wirft → return geschwaerzte/markierte Minimal-Form (z.B. '' + Marker) — Zeile bleibt schreibbar, kein roher Text.
       (d) bei (b)/(c) → `print('[ANON] storage-fallback genutzt sid=... grund=...')` (Gemini-Blindfleck: Notweg nie still).
       NIE den rohen Eingabetext zurueckgeben. Modul-Docstring: FOLD A-2 / Req 11, "nie roh, nie verloren, geloggt".
    2. **Auto-Variante (claude_service.py):** Task 1 nutzt `anonymize_for_storage(cleaned_display, sid)`.
    3. **Knopf-Pfad (deepgram_service.py:949-953) konsistent machen:** den heutigen fail-open-Block (`_antwort = anonymize_output(_antwort, _anon_cache)` mit "Fallback: _antwort bleibt unveraendert") ERSETZEN durch `_antwort = anonymize_for_storage(_antwort, _sid)`. Kein roher Text mehr bei Anon-Fehler in record_ewb_click. (Andre-Entscheidung 22.06.: beide Wege gleich, nie roh, nie verloren.)
    4. **Keyword (Slot A):** profil-statischer Text, PII-Risiko niedrig — zur Konsistenz beim Erfassen (Plan 08) ebenfalls `anonymize_for_storage(text, sid)`. Dokumentieren.
    Gemeinsame Invariante (SUMMARY + Code-Kommentar): "Live-Anzeige roh; JEDE Storage-Schreibung geht durch anonymize_for_storage → nie roh, nie verloren, Notweg geloggt." Plan 08 konsumiert pro Quelle die so erzeugte Version (immer ein sauberer String).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "anonymize_for_storage"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def anonymize_for_storage" services/anonymization.py` == 1
    - Helper nie roh: `grep -A25 "def anonymize_for_storage" services/anonymization.py` zeigt frischen anonymize-Fallback + Blank-Last-Resort + Log; KEIN `return text` des Roh-Eingangs
    - Auto + Knopf nutzen den Helper: `grep -l "anonymize_for_storage" services/claude_service.py services/deepgram_service.py` == 2 Dateien
    - Knopf-fail-open weg: `grep -n "_antwort bleibt unveraendert" services/deepgram_service.py` == 0
    - SUMMARY enthält die 3-Quellen-Storage-Vertrags-Tabelle (Auto/Knopf/Keyword alle ueber anonymize_for_storage)
  </acceptance_criteria>
  <done>Ein gemeinsamer Storage-Anon-Helper (nie roh/nie verloren/geloggt); Auto+Knopf konsistent darauf umgestellt; Keyword+Plan-08-Naht dokumentiert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NER-Über-Schärfe entschärfen — konservative Funktionswort-Stopliste (DSGVO-sicher)</name>
  <files>services/anonymization.py</files>
  <read_first>
    - services/anonymization.py:116-138 (PRONOMEN_WHITELIST — HIER die fehlenden Funktionswoerter ergaenzen) + :158-180 (_is_whitelisted — zentrale Whitelist, low-Vergleich, an allen 6 NER-Stellen aufgerufen)
    - services/anonymization.py:365,392,442,471,699,727 (die 6 _is_whitelisted-Aufrufstellen — Beleg dass EINE Listen-Aenderung alle Pfade deckt: _apply_ner + _apply_ner_parallel + Detektions-Funktion, je spaCy+GLiNER)
    - services/anonymization.py Art-9-Filter (Schritt 0 — DARF NICHT beruehrt werden)
  </read_first>
  <behavior>
    - Ein Satz „Wie kann ich Ihnen helfen?" → kein [PERSON_*] („Ihnen" bleibt)
    - Ein Satz „Herr Müller, wie kann ich Ihnen helfen?" → „Müller" geschwaerzt, „Ihnen" bleibt
    - Die Aenderung ist EINE Liste (PRONOMEN_WHITELIST), KEIN Lockern der Namens-Heuristik
  </behavior>
  <action>
    WURZEL (Pre-Execute-Audit am Code verifiziert): die Ueber-Schaerfe ist KEIN fehlender Mechanismus, sondern EIN vergessenes Wort. anonymization.py hat BEREITS `PRONOMEN_WHITELIST` (frozenset, :116) + die zentrale Funktion `_is_whitelisted(ent_text, token_type)` (:158, macht `low = ent_text.strip().lower()`), die an ALLEN SECHS NER-Entscheidungsstellen aufgerufen wird (`_apply_ner` spaCy+GLiNER, `_apply_ner_parallel` spaCy+GLiNER, Detektions-Funktion). PRONOMEN_WHITELIST enthaelt 'sie','ihr','ihre','ihrer','ihrem','ihren','ihres' — ABER NICHT die Dativform 'ihnen'. Genau deshalb wird 'Ihnen' geschwaerzt.
    **KEINE neue Konstante, KEIN neuer Skip-Mechanismus** — das wuerde die bestehende Single-Source-of-Truth duplizieren (genau die "Gatekeeper-Inkonsistenz", vor der der `_is_whitelisted`-Docstring warnt) und nur `_apply_ner` treffen, nicht den parallelen Pfad.
    **FIX:** die FEHLENDEN Funktionswoerter zu `PRONOMEN_WHITELIST` (:116) ergaenzen. Da `_is_whitelisted` schon lowercased vergleicht + an allen 6 Stellen aufgerufen wird, deckt diese EINE Aenderung automatisch alle NER-Pfade (sequenziell + parallel, spaCy + GLiNER) ab.
    Konkret zu ergaenzen (konservativ; Pre-Execute-Audit + Gemini-3-Sicht bestaetigen DSGVO-sicher — keines ist ein realer Nachname): 'ihnen', 'euch', 'euer', 'eure', 'sich', 'einander'. Beim Execute gegen die bestehende Liste diffen und NUR die wirklich fehlenden + provably-harmlosen aufnehmen.
    **DSGVO-Leitplanke (HART):**
    - NUR exakte Funktionswort-Matches (macht `_is_whitelisted` schon: low-Vergleich gegen die Sets). Mehrteilige Spans („Herr Müller") bleiben geschwaerzt (die bestehende all()-Mehrwort-Logik kippt bei einem echten Namen im Span auf False).
    - Aenderung betrifft AUSSCHLIESSLICH PRONOMEN_WHITELIST (Pronomen typ-unabhaengig); GENERIC_BERUF_WHITELIST / LOC / ORG / Art-9 / Regex unveraendert.
    - Im Zweifel ob ein Wort je ein Name sein kann → NICHT aufnehmen (lieber über-schwärzen als PII durchlassen).
    - KEIN Lockern von NER-Schwellen, KEINE Heuristik.
    Kommentar an der ergaenzten Stelle: „Ueber-Schaerfe-Fix (Req 11/FOLD A-2): fehlende Dativ-/Reflexiv-Funktionswoerter ('ihnen' etc.) nachgetragen; deckt via _is_whitelisted automatisch alle NER-Pfade. DSGVO: keine realen Nachnamen, kein Heuristik-Lockern."
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_anon_live_vs_stored.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - die fehlenden Funktionswoerter ('ihnen' etc.) sind in PRONOMEN_WHITELIST ergaenzt: `grep -n "ihnen" services/anonymization.py` trifft die Whitelist
    - KEINE neue Konstante: `grep -c "STOPWORD_FUNCTION_TOKENS" services/anonymization.py` == 0 (bestehender _is_whitelisted-Mechanismus genutzt, nicht dupliziert)
    - `_is_whitelisted` weiterhin an allen 6 NER-Stellen aufgerufen (Struktur unveraendert) — nur die Liste waechst
    - GENERIC_BERUF_WHITELIST / LOC / ORG / Art-9 / Regex unveraendert (grep/Sicht-Beleg)
    - Kommentar begruendet die ergaenzten Woerter + DSGVO-Leitplanke
  </acceptance_criteria>
  <done>Fehlende Funktionswoerter zur bestehenden PRONOMEN_WHITELIST ergaenzt; deckt via _is_whitelisted alle NER-Pfade; konservativ, DSGVO-sicher; kein neuer Mechanismus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Tests (Live-roh-vs-Storage-anon + Über-Schärfe-Balance) + Test-Anruf-DATEN-FLUSS</name>
  <files>tests/test_anon_live_vs_stored.py</files>
  <read_first>
    - services/anonymization.py (anonymize/anonymize_output + neue Stopliste)
    - tests/ (bestehende Anonymisierungs-Test-Muster, z.B. test_anonymization_wiring.py)
  </read_first>
  <action>
    `tests/test_anon_live_vs_stored.py` (Runtime-Behavior, KEIN Source-Presence):
    - `test_auto_variante_display_keeps_real_names`: monkeypatch/Spy auf den sio.emit('pip_token_done')-Pfad ODER Funktions-Rueckgabe von streame_auto_variante → assert die ANZEIGE-Version traegt den echten (Test-)Namen, NICHT [PERSON_*]. (Function-Call-/State-Assertion.)
    - `test_auto_variante_storage_is_anonymized`: assert die Storage-Version (cleaned_storage / Plan-08-Vertrag) traegt [PERSON_*] (anonymisiert mit gefuelltem Test-Cache).
    - `test_storage_fallback_no_raw_no_loss`: `anonymize_for_storage("Herr Mueller hat angerufen", sid)` mit cache=None → Ergebnis enthaelt KEINEN rohen Namen ('Mueller' nicht im Klartext), ist aber NICHT leer-verworfen (schreibbarer String, frische Saeuberung griff); ein Fallback-Log wurde emittiert. (Beweist: nie roh, nie verloren.)
    - `test_ner_stopword_keeps_function_words`: `anonymize("Wie kann ich Ihnen helfen?", cache)` → „Ihnen" bleibt (kein [PERSON_*]).
    - `test_ner_stopword_still_redacts_real_name` (DSGVO, beide Richtungen): `anonymize("Herr Müller, wie kann ich Ihnen helfen?", cache)` → „Müller" geschwaerzt UND „Ihnen" bleibt. Beweist die Balance.
    Function-Call-Mock gegen die echten Funktionen; KEIN getsource.
    **Test-Anruf-DATEN-FLUSS (riskante DSGVO-Welle, autonomous:false):** nach Deploy 1 Live-Test-Call mit einem echten (Test-)Namen → (a) die dem Berater LIVE angezeigte Auto-Varianten-Antwort zeigt den ECHTEN Namen; (b) `inspect.sh sample` auf Transkript + (sobald Plan 08 live) suggestion_reactions zeigt die ANONYMISIERTE Form; (c) ein Satz mit „Ihnen"/„Sie" → die Storage-Kopie behaelt diese Funktionswoerter (kein [PERSON_*]). In SUMMARY dokumentieren.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_anon_live_vs_stored.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - alle 5 Tests gruen server-seitig (display-roh / storage-anon / storage-fallback-no-raw-no-loss / stopword-keeps / stopword-still-redacts)
    - Test-Anruf-Beleg: Live-Anzeige echter Name; Storage anonymisiert; „Ihnen"/„Sie" bleiben in der Storage-Kopie
    - DSGVO-Balance beide Richtungen bewiesen (Funktionswort bleibt, echter Name geschwaerzt)
  </acceptance_criteria>
  <done>Live-roh-vs-Storage-anon + Ueber-Schaerfe-Balance per Test + Test-Anruf bewiesen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Auto-Variante zeigt dem Berater echte Namen LIVE; Storage anonymisiert (mit lebendem Cache); NER-Über-Schärfe konservativ entschärft.</what-built>
  <how-to-verify>
    Nach `bash deploy.sh production`, Test-Anruf (Cold-Call + Meeting) als Test-User mit einem echten Test-Namen im Gespräch:
    1. Die NERVE-Auto-Varianten-Antwort auf dem Schirm zeigt den ECHTEN Namen (kein „[PERSON_A]") — Live-Nutzen wiederhergestellt.
    2. `inspect.sh sample` auf das Transkript (+ suggestion_reactions sobald Plan 08 live) → die GESPEICHERTE Kopie ist anonymisiert (kein Klar-Name).
    3. Sag „Wie kann ich Ihnen helfen?" → die gespeicherte Kopie behält „Ihnen" (kein „[PERSON_A]"); ein echter Name im selben Satz ist weiterhin geschwärzt.
    4. Beide Modi (Cold/Meeting) gleich.
    DSGVO-Check (Claudian/Cross-AI): die Stopliste lässt keinen echten Namen durch.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe welche Stelle (Live-Anzeige / Storage / Über-Schärfe) abweicht.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| NERVE-Antwort → Berater-Anzeige (SocketIO) | echte Namen erlaubt (flüchtig, Live-Nutzen) |
| NERVE-Antwort → Storage (Transkript/suggestion_reactions/ewb_clicks) | MUSS anonymisiert (DSGVO) |
| NER-Stopliste → PERSON-Tagging | ein durchgelassener Name = DSGVO-Verstoß |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-09-01 | I (DSGVO) | Live-Anzeige roh → Name landet versehentlich in Storage | mitigate | Entkopplung display/storage (Task 1): pip_token_done bekommt display (roh); JEDE Storage-Schreibung nutzt die separate anonymisierte Version (mit lebendem Cache). Test storage_is_anonymized. |
| T-TAXO2-09-02 | I (DSGVO / No-Op-Falle) | Storage-Anon mit cache=None speichert rohe Namen | mitigate | anonymize_for_storage: Cache-Replay nur wenn Cache da; sonst frische Saeuberung (nie roh), sonst geschwaerzt+markiert; nie roh, nie verworfen, geloggt. Test storage_fallback_no_raw_no_loss. |
| T-TAXO2-09-03 | I (DSGVO) | ergaenzte Whitelist laesst echten Namen durch | mitigate | NUR exakte Funktionswort-Matches in PRONOMEN_WHITELIST (kein Substring/Heuristik); Mehrwort-all()-Logik kippt bei echtem Namen; im Zweifel nicht aufnehmen (über-schwärzen). Test still_redacts_real_name (beide Richtungen). Cross-AI-DSGVO bestaetigt. |
| T-TAXO2-09-07 | A (stiller Datenverlust) | Storage-Fallback verwirft/blankt still → unbemerkter Daten-/Qualitaetsverlust | mitigate | anonymize_for_storage verwirft NIE die Zeile (blank+Marker als letzter Schritt) + loggt jeden Fallback ('[ANON] storage-fallback') → diagnostizierbar statt blind (Gemini-Blindfleck). |
| T-TAXO2-09-04 | F (Funktion / Live-Nutzen) | Berater sieht anonymisierte Antwort (unbrauchbar) | mitigate | Anzeige roh (Task 1); Test display_keeps_real_names + Test-Anruf. |
| T-TAXO2-09-05 | D (Latenz / Punkt 25) | zusätzlicher Anon-Schritt verzögert Live-Antwort | mitigate | Anzeige emittiert roh (kein Schritt davor); Storage-Anon = bestehender anonymize_output (String-Replace), nur Ziel geändert. Kein neuer LLM/NER im Anzeige-Pfad. |
| T-TAXO2-09-06 | T (Regression Knopf-Pfad) | Auto-Fix bricht den schon-korrekten Knopf-Pfad | mitigate | Knopf-Pfad (streame_manual_ewb_variante:823 + deepgram:953) NICHT angefasst; nur Auto-Variante + NER-Stopliste. grep-Beleg. |
</threat_model>

<verification>
- Auto-Variante: pip_token_done zeigt rohe Version (echte Namen); separate anonymisierte Storage-Version (mit lebendem Cache) als Plan-08-Vertrag.
- Knopf-Pfad unveraendert (schon korrekt); Storage-Vertrag fuer alle drei Quellen dokumentiert.
- PRONOMEN_WHITELIST um 'ihnen' etc. ergaenzt: „Ihnen"/„Sie" bleiben, echter Name weiter geschwaerzt (DSGVO-Balance beide Richtungen); deckt via _is_whitelisted alle 6 NER-Stellen.
- cache=None-No-Op entschaerft: anonymize_for_storage faellt auf frische Saeuberung zurueck (nie roh, nie verworfen, geloggt) — Auto + Knopf konsistent.
- Latenz (Punkt 25): Anzeige roh sofort; Storage-Anon = bestehender Aufruf, Ziel umgelenkt.
- Test-Anruf-DATEN-FLUSS (Cold+Meeting, autonomous:false): Live echte Namen, Storage anonymisiert, Funktionswoerter erhalten.
- DSGVO: Cross-AI-Pflicht + Referenz NERVE DSGVO Analyse.
</verification>

<success_criteria>
- Live-Antwort an den Berater traegt echte Namen (Auto-Variante auf Knopf-Muster gebracht).
- Storage-Kopie (Transkript/suggestion_reactions/ewb_clicks) anonymisiert ueber anonymize_for_storage am Erfassen: nie roh, nie verloren, Notweg geloggt; Auto + Knopf konsistent.
- NER-Ueber-Schaerfe konservativ entschaerft (PRONOMEN_WHITELIST ergaenzt) ohne echte PII durchzulassen (Balance beide Richtungen bewiesen).
- Plan-08-Vertrag (anonymisierte Storage-Version pro Quelle) bereitgestellt; Knopf-Pfad unberuehrt; Latenz unveraendert.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-09-SUMMARY.md` (inkl. Auto-Variante display/storage-Entkopplungs-Beleg, 3-Quellen-Storage-Vertrags-Tabelle, NER-Stopliste + kuratierte Begruendung, 5 Test-Belege, Test-Anruf-DATEN-FLUSS (Live echte Namen / Storage anon / Funktionswoerter erhalten, Cold+Meeting), DSGVO-Cross-AI-Notiz).
</output>
