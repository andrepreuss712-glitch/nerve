# TAXO2/TAXO3 Einarbeitungs-Fold — 2026-06-22 (vor Execute)

**Quelle:** Verifikations-Runde Claudian am echten SPEC (`TAXO2-SPEC.md`, 11.06.) + echtem Code, NACH den Detours (MEDFIX 18.06., TAXO1 Welle 5/6/7) + den neuen Entscheidungen (Vorschlags-Übernahme 22.06., Soll-Verhalten §6).
**Drei-Sichten-Stand der Idee:** Claudian + frischer Claude + Gemini 3.1 Pro deckungsgleich (raw-now / Auswertung-später).
**Zweck:** das Delta zwischen dem am 11.–13.06. verriegelten TAXO2-Plan und dem heutigen Stand schließen, BEVOR Execute.

---

## FOLD A (TAXO2, 🔴 — NEUE Anforderung Req 10): Vorschlags-Angebot roh erfassen (insert-only, nicht nachholbar)

**Code-Befund 22.06 (bewiesen, nicht angenommen):**
- NERVEs Ausgabe (`antwort_text`) liegt in `ewb_clicks` (RAM, `live_session.py:949 record_ewb_click`) — OHNE `interaction_id`.
- `interaction_id` liegt im `intent_event` (`deepgram_service.py:881`) — OHNE Antwort-Text.
- Die **Auto-Variante (Slot B, gestreamt)** wird **gar nicht** persistiert — nur die Knopf-Antwort.
- `grep "NERVE-Ausgabe MIT interaction_id"` == 0 Treffer.
→ Die Vorschlags-Übernahme (Soll-Verhalten §6) ist **heute nicht baubar**; pro Call geht das Roh-Material **für immer** verloren (insert-only / nicht nachholbar — Türöffner-Regel Leitplanke 4).

**Was TAXO2 schon hat (kein Konflikt):** Req 4 (`handling_score`, marker-basiert auf der nächsten Berater-Aussage = „behandelt ≠ Knopf"-Kern) + Req 3 (Proration tötet Redeanteil-Cold-Call-Artefakt). Req 10 ergänzt die **fehlende Roh-Erfassung von NERVEs AUSGABE** — eine andere Achse als das Marker-Scoring.

**Anforderung Req 10 (Vorschlag):**
> Jeder NERVE-Vorschlag (Auto-Variante Slot B + Manueller Knopf + Keyword-Slot) wird **insert-only** erfasst, verknüpft mit `interaction_id` + `call_id`. NUR das ANGEBOT (was NERVE ausgab) — die Übernahme-EINSTUFUNG (1:1/90%/ignoriert) ist DEFERRED (post-Launch, neu-berechenbare Schicht). Anonymisiert (DSGVO, wie das Transkript). Latenz-neutral (RAM-Puffer + Call-Ende-Flush, KEIN Live-Pfad-DB-Write).

**Türöffner-Validierung (Leitplanke 4 — das Roh-Schema trägt diese späteren Features):**

| Roh-Feld | trägt welches spätere Feature |
|---|---|
| `suggestion_text` + `slot` (A/B) + `model` | Selbst-Verbesserung (welcher Vorschlags-Typ wird systematisch ignoriert → TAXO3-Treibstoff); A/B-Test der Antwort-Engine |
| `interaction_id` | Verknüpfung zu Auslöser (`triggering_text`) + Einwand-Typ (Moment-Klammer) |
| folgende Berater-Äußerung (via `interaction_id`→Transkript-Fenster, post-call) | die Übernahme-Skala (1:1 / ~90% / ignoriert) |
| `call_id` → Outcome-Join | Vier-Felder-Tafel (Übernahme × Ergebnis), Churn-Frühwarnung, persönlicher ROI |
| `ts_offered` | „ignoriert-weil-zu-spät" vs. „weil-schlecht" (echte Live-Latenz) |

**Schema (neue Tabelle `suggestion_reactions`, public, owner `nerve_app`, Schild Punkt 23 — Angebots-Hälfte JETZT, Reaktions-Hälfte NULL/deferred):**
- `id` PK · `call_id` FK→calls CASCADE · `conversation_log_id` · `interaction_id` (UUID, Moment-Klammer) · `org_id` · `user_id` (RLS/per-Berater)
- `slot` ('A'=Profil-Stichwort-instant / 'B'=KI-gestreamt) · `source` ('auto_variante'/'manual_button'/'keyword') · `model`
- `suggestion_text` (ANONYMISIERT)
- `ts_offered`
- `payload_jsonb` (Reserve: einwand_typ, confidence, künftige Felder ohne Migration)
- **[DEFERRED, nullable, post-Launch befüllt]** `adoption_value` (kontinuierlich 0-1) · `following_utterance_ref` · `reaction_class` — neu-berechenbare Schicht, NICHT jetzt befüllen

**Architektur (Latenz-sicher, Bau-Regel-1-konform):** Pro-SID-RAM-Puffer (wie heute `ewb_clicks` fürs Knopf-`antwort_text`) um JEDEN Vorschlag erweitern (Auto + Knopf + Keyword) mit `slot`/`model`/`interaction_id`/`ts`. Bei Call-Ende EIN insert-only Flush nach `suggestion_reactions` (anonymisiert). **KEIN Live-Pfad-DB-Write** → kein Latenz-Risiko (Latenz-Dealbreaker-Regel). Die Übernahme-BERECHNUNG (Vergleich Angebot↔folgende Äußerung, asynchroner KI-Richter, gebündelt) läuft SPÄTER post-Launch.

**Wohin im Plan:** **NEUER Plan 08 „suggestion-offer-capture"** (Schema-Anlage + RAM-Puffer-Erweiterung + Call-Ende-Flush + Anonymisierung), klar abgegrenzt von den 7 reviewten Plänen. Pflicht-Sektionen Punkt 14/20/21. + Req 10 in `TAXO2-SPEC.md` ergänzen (Boundary: Roh-Erfassung IN scope, Übernahme-Scoring OUT/post-Launch). **[KORRIGIERT durch FOLD A-2 unten: die Anonymisierung wird NICHT in Plan 08 gequetscht, sondern in den eigenen Plan 09 ausgelagert (am Erfassen mit lebendem Cache, NICHT beim Flush — cache=None ist ein No-Op); Plan 08 `depends_on: [09]`.]**

---

## FOLD A-2 (Korrektur nach Gemini-Cross-AI + André-Review, 22.06. — vor Execute)

Gemini hat Plan 08 + die SPEC-Req-10 review't; André hat die Funde gesichtet. Drei Korrekturen + eine Scope-Verschiebung:

### A — ANON-LIVE-ANSWER ZIEHT NACH TAXO2 (André-Entscheidung 22.06., war FOLD C #2 / TAXO3)
Grund: Plan 08 fasst die Anonymisierung sowieso an → Anti-Abrieb (nicht zweimal dieselbe Stelle). Drei Anforderungen:
1. **Live-Antwort behält ECHTE Namen.** Code-Befund (bewiesen): der Auto-Varianten-Pfad emittiert die FINALE Antwort ANONYMISIERT an den Berater (`claude_service.py:675-679` — `cleaned = anonymize_output(...)` → `sio.emit('pip_token_done', {'result': {'gegenargument_1': cleaned}})`). Der Berater liest also „[PERSON_A]" statt „Herr Müller" — unbrauchbar. **Fix: Live-Display roh (echte Namen), nur die GESPEICHERTE Kopie anonymisiert.**
2. **Stored-copy WÄHREND des Calls säubern (Mapping lebt).** Code-Befund (bewiesen, KRITISCH): `anonymize_output(text, cache=None)` gibt den Text UNVERÄNDERT zurück (`anonymization.py:612-613`) — es ist ein Cache-Reverse-Lookup, KEIN NER. Plan 08s ursprünglicher „ephemerer Modus cache=None beim Call-Ende-Flush" hätte deshalb ROHE Namen gespeichert (DSGVO-Loch). **Auflösung: am ERFASSEN anonymisieren mit dem lebenden Per-SID-Cache (`get_anonymisierer(sid)`), NICHT beim Flush.** Der Knopf-Pfad macht das heute schon korrekt (`deepgram_service.py:953`, anonymisiert mit Live-Cache vor `record_ewb_click`) → latenz-erprobt (anonymize_output = String-Replace, kein NER/LLM).
3. **Über-Schärfe (ANON-OVER-AGGRESSIVE):** der NER-Input-Pfad schwärzt harmlose Funktionswörter („Ihnen" → [PERSON_A]). Entschärfen — ABER DSGVO-Leitplanke: weniger aggressiv darf NIE echte PII in der gespeicherten Kopie durchlassen (konservative Stopliste nur provably-harmloser Funktionswörter, NIE Namen).

**Wohin:** 🔴 + DSGVO + berührt BESTEHENDEN Live-Antwort-Code (orthogonal zu Plan 08s Capture-Mechanik) + eigenes Threat-Model (Live-vs-Stored-Split, Über-Schärfe-Rebalance, Mapping-Lebenszeit) → **eigener NEUER Plan 09 „anon-live-vs-stored"**, NICHT in Plan 08 gequetscht. Plan 08 KONSUMIERT den Vertrag (Plan 09 produziert pro Vorschlag die anonymisierte Storage-Version; Plan 08 erfasst genau die). Plan 08 `depends_on: [09]`. + SPEC-Req 11.

### B — drei verbleibende Gemini-Funde (valide, eingearbeitet)
1. **`interaction_id` NIE leer speichern:** für JEDEN erfassten Vorschlag ein Moment-Fenster öffnen (`get_or_open_moment`, Muster `deepgram_service.py:860`), damit `interaction_id` immer gesetzt ist — statt nullable-NULL als „über call_id korrelierbar" zu akzeptieren. Plan 08 angepasst (Auto + Keyword öffnen/holen das Fenster wie der Knopf-Pfad).
2. **Daten-Verlust bei abruptem Call-Ende** (RAM-Puffer weg vor Flush) explizit als Known-Issue dokumentiert (Plan 08 T-TAXO2-08-07 „accept" — bewusste Eigenschaft insert-only/nicht-nachholbar, NICHT still geschluckt).
3. **DELETE-then-INSERT strikt auf DIESEN Call scopen:** `org_id AND call_id` (Plan 08 scopte nur `conversation_log_id + org_id`) — nie Zeilen anderer Calls anfassen. Plan 08 Flush-Idempotenz angepasst.

### C — GESTRICHEN (NICHT eingearbeitet)
Gemini-Fund „Vorschlag nicht gesehen weil überschrieben" — GESTRICHEN (André-Produktwissen: bei NERVE kommt ein neuer Vorschlag erst NACH der Gesprächs-Reaktion; das Szenario existiert nicht).

---

## FOLD B (TAXO2, kleinere Punkte)

1. **`triggering_text` nutzen (Welle 7, neu seit Plan-Lock):** Req 4 (`handling_score` marker-basiert auf der „nächsten Berater-Aussage") kann jetzt `triggering_text` als sauberen Auslöser-Bezug nutzen, statt ihn zu rekonstruieren. Optional, verbessert Marker-Genauigkeit → Plan 03 prüfen.
2. **Zombify-Stand (Plan 05):** verifizieren, dass Plan 05 den TATSÄCHLICH von Welle 5 gebauten `objection_bridge.py` + die aktuelle Leser-Liste trifft (seit 13.06. evtl. neue `objection_events`-Leser dazugekommen → Punkt-20-grep frisch).
3. **DEPLOY-CREATE-ALL-CRASH:** `rubric_score` + `suggestion_reactions` = neue Tabellen → Migration VOR `deploy.sh`-Restart (Backlog-Lehre 18.06.), NICHT via `create_all`.

---

## FOLD C (TAXO3 — kommt nach TAXO2, hier nur vermerkt)

1. **Cross-AI-Plan-Review fehlt:** TAXO3-Pläne 01-05 haben (anders als TAXO2) noch keinen Gemini-Plan-Review → nachholen (agy-Draht steht). Pflicht bei 🔴.
2. **ANON-LIVE-ANSWER — VERSCHOBEN NACH TAXO2 (André 22.06., siehe FOLD A-2/A + Plan 09).** ~~verifizieren, dass der TAXO3-Plan adressiert: echte Namen flüchtig IN der Live-Antwort, nur Storage anonymisiert~~. Nicht mehr TAXO3-Scope; TAXO3 erbt nur die Konsistenz-Prüfung (Cold/Meeting) auf dem bereits in TAXO2/Plan 09 gebauten Fundament.
3. **I-4-Tor:** TAXO3-05 (Slot-B-Dedup) war auf TAXO1-04/I-4 gesperrt → TAXO1 ist komplett, bestätigen dass gelöst.

---

## Reihenfolge
TAXO2 Fold A (Req 10 + Plan 08) + Fold A-2 (Req 11 + Plan 09, Plan-08-Anpassung B1/B2/B3 + Anon-Vertrag) + Fold B → Claudian Pre-Execute-Audit → Gemini re-review der geänderten/neuen Stellen (insb. Plan 09 NER-Stopliste 🔴 DSGVO + Plan 08 depends_on 09) → TAXO2 Execute (Wellen-Ordnung: Plan 09 vor Plan 08). Danach TAXO3 Fold C (Gemini-Review + I-4-Bestätigung; ANON-LIVE-ANSWER ist nicht mehr TAXO3-Scope, siehe A-2) → TAXO3 Execute.
