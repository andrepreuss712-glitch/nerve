---
phase: 08.23.2.TAXO2
plan: 08
type: execute
wave: 2
depends_on: ["09"]
# UNABHAENGIG von der Scoring-Engine (Plan 01-07) — teilt KEIN Schema/Feld mit rubric_score/handling_score.
# ABER depends_on Plan 09 (FOLD A-2): Plan 09 (anon-live-vs-stored) produziert pro Antwort die
# anonymisierte STORAGE-Version (am Erfassen, mit lebendem Per-SID-Cache); Plan 08 erfasst genau die.
# Grund: anonymize_output(text, cache=None) ist ein No-Op (anonymization.py:612) — Flush-Zeit-Anon
# mit ephemerem Cache wuerde ROHE Namen speichern. Anon MUSS am Erfassen (Mapping lebt) laufen → Plan 09.
# DEPENDS-ON-TAXO1 fuer intent_event.interaction_id / get_or_open_moment.
files_modified:
  - database/models.py
  - alembic/versions/XXXX_suggestion_reactions_table.py
  - services/suggestion_capture.py
  - services/live_session.py
  - services/claude_service.py
  - services/einwand_keyword_matcher.py
  - services/deepgram_service.py
  - routes/app_routes.py
  - tests/test_suggestion_reactions.py
autonomous: false
requirements: ["SPEC Req 10 (FOLD A 2026-06-22)"]
user_setup: []

must_haves:
  truths:
    - "public.suggestion_reactions existiert auf Production (Owner nerve_app, RLS ENABLE+FORCE) mit den Roh-Angebot-Feldern (suggestion_text/slot/source/model/interaction_id/call_id/conversation_log_id/org_id/user_id/ts_offered/payload_jsonb) + den nullable DEFERRED-Reaktions-Feldern (adoption_value/following_utterance_ref/reaction_class)"
    - "JEDER ausgegebene Vorschlag (Auto-Variante Slot B + Manueller Knopf + Keyword-Slot) wird im Pro-SID-RAM-Puffer state['suggestion_offers'] erfasst — mit slot/source/model/interaction_id/ts; KEIN Live-Pfad-DB-Write (Latenz-Dealbreaker, Punkt 25)"
    - "interaction_id ist IMMER gesetzt (FOLD A-2/B1): pro Vorschlag wird ein Moment-Fenster geoeffnet/geholt (get_or_open_moment, Muster deepgram_service.py:860) — NIE leer-NULL gespeichert (Spalte bleibt nullable als Defense, aber der Capture-Pfad fuellt sie immer)"
    - "Der gespeicherte suggestion_text ist die ANONYMISIERTE Storage-Version (Plan 09, FOLD A-2): am ERFASSEN mit lebendem Per-SID-Cache (get_anonymisierer(sid)) gesaeubert — NIE mit cache=None (No-Op-Falle anonymization.py:612). Der Flush schreibt den schon-anonymisierten Text 1:1, KEINE zweite Anon im Flush"
    - "Bei Call-Ende EIN insert-only Flush nach suggestion_reactions; idempotent — DELETE-then-INSERT strikt auf org_id AND call_id gescoped (FOLD A-2/B3), NIE Zeilen anderer Calls anfassen; Doppel-/api/beenden konvergiert"
    - "Known-Issue dokumentiert (FOLD A-2/B2): Prozess-Crash vor Call-Ende verliert den RAM-Puffer → Vorschlaege dieses Calls nicht erfasst (insert-only/nicht-nachholbar = bewusste Eigenschaft, kein Backfill) — explizit, nicht still"
    - "NUR das ANGEBOT wird befuellt — adoption_value/following_utterance_ref/reaction_class bleiben NULL (DEFERRED, post-Launch neu-berechenbare Schicht); KEIN Uebernahme-Scoring in TAXO2"
    - "suggestion_reactions.call_id ist HARTER FK auf calls(id) ON DELETE CASCADE (F-08/DD-01) — geloeschter Call hinterlaesst 0 Waisen (DSGVO; suggestion_text = potenziell PII)"
    - "Migration laeuft VOR dem Gunicorn-Restart/create_all (DEPLOY-CREATE-ALL-Lehre 18.06) — sonst baut Base.metadata.create_all() beim App-Start eine NACKTE Tabelle ohne RLS/Owner/Schild"
    - "SCHILD-Guard server-seitig GRUEN fuer suggestion_reactions + alle nicht-trivialen Spalten"
    - "Latenz-Budget eingehalten: die Live-Pfad-Aenderung ist NUR ein thread-safe list-append (record_suggestion_offer, wie record_ewb_click) — kein DB-/Netz-/LLM-Aufruf im Live-Loop"
  artifacts:
    - path: "database/models.py"
      provides: "class SuggestionReaction ORM-Model (Roh-Angebot-Spalten + nullable DEFERRED-Reaktions-Spalten + harter call_id-FK CASCADE)"
      contains: "class SuggestionReaction"
    - path: "alembic/versions/XXXX_suggestion_reactions_table.py"
      provides: "CREATE TABLE public.suggestion_reactions + harter FK CASCADE + OWNER nerve_app + RLS FORCE + COMMENTs"
      contains: "op.create_table"
    - path: "services/suggestion_capture.py"
      provides: "flush_suggestion_offers(...) — Call-Ende-Flush insert-only + Anonymisierung (KEIN Live-Write)"
      contains: "def flush_suggestion_offers"
    - path: "services/live_session.py"
      provides: "record_suggestion_offer(...) — thread-safe RAM-Append (wie record_ewb_click) + Reset von state['suggestion_offers']"
      contains: "def record_suggestion_offer"
    - path: "tests/test_suggestion_reactions.py"
      provides: "Schema-/Insert-Roundtrip + F-08-Cascade-Test + Anonymisierungs-Test + Latenz-Beleg (kein DB-Write im record_-Pfad)"
  key_links:
    - from: "services/claude_service.py streame_auto_variante (Slot B) + deepgram_service.py Knopf-Pfad + einwand_keyword_matcher.py (Keyword)"
      to: "live_session.record_suggestion_offer → state['suggestion_offers'] (RAM)"
      via: "thread-safe list-append im Live-Loop (KEIN DB-Write)"
      pattern: "record_suggestion_offer"
    - from: "routes/app_routes.py (Call-Ende-Finalisierung, neben mirror_ewb_clicks_to_objection_events :392)"
      to: "services.suggestion_capture.flush_suggestion_offers → suggestion_reactions"
      via: "EIN insert-only Flush in der Finalisierungs-Transaktion (suggestion_text schon anonymisiert/Plan 09; DELETE org+call_id-scoped/B3)"
      pattern: "flush_suggestion_offers"
    - from: "suggestion_reactions.interaction_id"
      to: "intent_event.interaction_id (Moment-Klammer, TAXO1)"
      via: "Korrelations-Spalte (KEIN harter FK — interaction_id ist kein PK; Tuermoeffner-Naht models.py:774 / 0016:76)"
      pattern: "interaction_id"
    - from: "suggestion_reactions.call_id"
      to: "public.calls(id)"
      via: "harter FK ON DELETE CASCADE (F-08/DD-01)"
      pattern: "ondelete='CASCADE'"
---

<objective>
Schließe die in FOLD A (2026-06-22) bewiesene Roh-Lücke: NERVEs AUSGABE (was die Engine dem Berater vorschlug) wird heute NIRGENDS durable + korreliert erfasst — der Knopf-`antwort_text` liegt nur im RAM (`ewb_clicks`, ohne `interaction_id`), die Auto-Variante (Slot B) wird GAR NICHT persistiert, der Keyword-Slot hat keinen Ausgabe-Speicher. Pro Call geht das Material für immer verloren (insert-only / nicht nachholbar, Türöffner-Leitplanke 4). Dieser Plan legt die neue Tabelle `public.suggestion_reactions` an, erweitert den Pro-SID-RAM-Puffer um JEDEN Vorschlag (Auto + Knopf + Keyword, je mit `slot`/`source`/`model`/`interaction_id`/`ts`) und flusht bei Call-Ende EINMAL insert-only in die Tabelle.

**FOLD A-2 (Korrektur, depends_on Plan 09):** Die Anonymisierung des gespeicherten `suggestion_text` passiert NICHT beim Flush (dort wäre `anonymize_output(..., cache=None)` ein No-Op — anonymization.py:612 — und würde rohe Namen speichern), sondern AM ERFASSEN mit dem lebenden Per-SID-Cache. Plan 09 (anon-live-vs-stored) produziert pro Antwort die anonymisierte Storage-Version (Live-Display bleibt roh/echte Namen); Plan 08 erfasst genau die. Zusätzlich: `interaction_id` immer via `get_or_open_moment` (B1), Flush-DELETE strikt `org_id AND call_id` (B3), Crash-vor-Flush als Known-Issue (B2).

**Boundary (HART):** NUR das ANGEBOT wird erfasst. Die Übernahme-EINSTUFUNG (1:1 / ~90% / ignoriert — Soll-Verhalten §6) ist DEFERRED (post-Launch, neu-berechenbare Schicht): die Reaktions-Spalten (`adoption_value`/`following_utterance_ref`/`reaction_class`) werden JETZT als nullable angelegt, aber NICHT befüllt. KEIN asynchroner KI-Richter, KEIN Vergleich Angebot↔folgende Äußerung in TAXO2.

**Latenz (HART, Punkt 25):** Die einzige Live-Pfad-Änderung ist ein thread-safe `list.append` (`record_suggestion_offer`, exakt das Muster von `record_ewb_click` live_session.py:949) — KEIN DB-/Netz-/LLM-Aufruf im Live-Loop. Der einzige DB-Write ist der EINE Call-Ende-Flush in der bestehenden Finalisierungs-Transaktion. Antwort-Latenz bleibt unverändert.

Purpose: Liefert das Roh-Fundament, auf dem die spätere Vorschlags-Übernahme (TAXO3/post-Launch) ohne Maschinen-Neubau aufsetzt. Türöffner-Beleg: `intent_event.interaction_id` ist bereits als "FK-Ziel für spätere suggestion_reactions (Phase H)" beschildert (models.py:774 / 0016:76).
Output: suggestion_reactions live (leer), ORM-Model, Migration, RAM-Puffer + drei Capture-Hooks, Call-Ende-Flush, Anonymisierung, Schema-/Cascade-/Anonymisierungs-/Latenz-Test.

KEIN Übernahme-Scoring. KEIN Live-Pfad-DB-Write. KEINE Berührung der Scoring-Engine (Plan 01-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Phase-Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen (lesen sonst `08.23.2`). STATE/ROADMAP hand-editieren. SUMMARY ins hardcodierte Verzeichnis.

**Verify = Production only (CLAUDE.md HART):** `bash deploy.sh production` + Prod-Pytest-Gate + `inspect.sh` gegen Prod (`ssh -i ~/.ssh/nerve_vps root@178.104.82.166`). KEIN lokales pytest als Acceptance, KEIN `git pull` auf Prod. Migration als `postgres`-User, dann `ALTER TABLE ... OWNER TO nerve_app`. SCHILD-Guard MANUELL server-seitig (OS-User nerve_app peer-auth).

**autonomous:false** — Human-Verify Pflicht: riskante Welle (Live-Loop-Capture-Pfad berührt + DSGVO-Wortlaut-Speicherung + neue Tabelle). Test-Anruf-DATEN-FLUSS-Beweis: ≥1 Auto-Variante + ≥1 Knopf → je EINE anonymisierte suggestion_reactions-Zeile nach Call-Ende; Live-Antwort-Latenz subjektiv unverändert.

**DEPLOY-REIHENFOLGE (HART, FOLD B Punkt 3 / DEPLOY-CREATE-ALL-Lehre 18.06):** Die neue Tabelle wird in models.py als ORM-Model definiert → `Base.metadata.create_all()` läuft beim App-Startup (models.py:677, app.py-Init) und würde die Tabelle als NACKTE Variante anlegen (Owner postgres, KEINE RLS, KEIN Schild), falls sie beim Restart noch nicht existiert. Die Alembic-Migration (mit OWNER nerve_app + RLS FORCE + COMMENTs) MUSS deshalb VOR dem Gunicorn-Restart laufen. `deploy.sh production` führt die Migration server-seitig vor dem Restart aus — verifizieren, dass die Reihenfolge eingehalten ist (sonst: nackte Tabelle, SCHILD-Guard rot, RLS fehlt). NICHT auf create_all verlassen.

**Migrations-Revision (HART, DEPENDS-ON-TAXO1):** lokaler/Prod-HEAD = TAXO1-Kette (0016 intent_event, 0017 zombie_ewb_ratings, 0018 drop_ewb_prompt_versions). TAXO2-Pläne 01/03/05 legen weitere Migrationen an. NICHT die Revision hardcoden — beim Execute den dann-aktuellen Alembic-HEAD lesen (`inspect.sh migrations`) und die suggestion_reactions-Migration als nächste Nummer DARÜBER anlegen (`down_revision` = dann-HEAD). Datei auf die echte Nummer benennen.

**UTF-8-Regel:** Umlaute nur in user-facing Text. DB-Spalten/dict-keys/COMMENT-Identifier ASCII (slot/source/model/auto_variante/manual_button/keyword). COMMENT-Text-Strings dürfen Umlaute tragen.

**DSGVO (HART, FOLD A-2):** `suggestion_text` ist NERVEs eigene Ausgabe, kann aber Kunden-Wortlaut zitieren → wird AM ERFASSEN mit dem lebenden Per-SID-Cache anonymisiert (Plan 09, `depends_on`), NICHT beim Flush. `anonymize_output(text, cache=None)` ist ein No-Op (anonymization.py:612-613) → ephemerer Flush-Modus würde rohe Namen speichern (verworfen). Plan 08 erfasst die schon-anonymisierte Storage-Version (Live-Display bleibt roh/echte Namen, Plan 09). Cross-AI bei diesem Fund Pflicht (DSGVO). Referenz in `Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse`.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-CONTEXT.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-FOLD-2026-06-22.md

<interfaces>
<!-- Muster zum Kopieren — bewiesen aus dem Code (FOLD-A-Verifikation 22.06). -->

RAM-Puffer-Vorbild (live_session.py:949-961, record_ewb_click — exaktes thread-safe Append-Muster):
```
def record_ewb_click(einwand_typ, success=False, antwort_text=None, einwand_text=None):
    entry = {'einwand_typ': ..., 'success': ..., 'ts': _dt.utcnow().isoformat(),
             'antwort_text': ..., 'einwand_text': ...}
    with state_lock:
        state.setdefault('ewb_clicks', []).append(entry)
```
→ record_suggestion_offer(slot, source, model, suggestion_text, interaction_id, einwand_typ=None)
  baut analog ein entry-dict + `with state_lock: state.setdefault('suggestion_offers', []).append(entry)`.
Reset: live_session.py:932 `state['ewb_clicks'] = []` → DANEBEN `state['suggestion_offers'] = []` ergaenzen.

Drei Capture-Punkte (alle haben slot/interaction_id zur Hand — FOLD-A-Verifikation):
- **Slot B Auto-Variante:** services/claude_service.py:572 `streame_auto_variante(neuer_text, einwaende, kontext, sid, slot=1, trigger=...)`
  — gestreamte KI-Antwort, HEUTE NICHT persistiert. interaction_id via live_session.get_or_open_moment(sid, ...)
  (Muster deepgram_service.py:860). model aus config (MODEL_* fuer den Auto-Variante-Pfad).
- **Manueller Knopf:** services/deepgram_service.py:877-883 (intent_event-Emit-Block, source='ui_asserted')
  — hat `_btn_iid` (interaction_id, via get_or_open_moment :860) + den Knopf-Antwort-Text zur Hand. slot/model dort.
- **Keyword-Slot:** services/einwand_keyword_matcher.py:321 (emit_intent_event mit triggering_text=_kw_trig)
  — Fast-Lane Slot-A-Sofortnetz; hat den gematchten Vorschlags-Text + interaction_id.

Slot/Source-Mapping (FOLD-Schema; EXAKTE Zuordnung beim Execute aus dem Live-Code ableiten + dokumentieren):
- slot: 'A' = Profil-Stichwort-instant (Keyword/Fast-Lane) | 'B' = KI-gestreamt (Auto-Variante + Knopf-generierte Antwort)
- source: 'auto_variante' | 'manual_button' | 'keyword'

Call-Ende-Flush-Hook (app_routes.py:392 — Vorbild objection_bridge, gleiche Finalisierungs-Transaktion):
```
from services.objection_bridge import mirror_ewb_clicks_to_objection_events
mirror_ewb_clicks_to_objection_events(conversation_log_id=..., user_id=..., org_id=..., ewb_clicks=..., db=...)
```
→ DANEBEN: flush_suggestion_offers(conversation_log_id=..., call_id=..., user_id=..., org_id=...,
   tenant_id=..., suggestion_offers=state['suggestion_offers'], db=db_conv). Caller committet (wie die Bruecke).

Idempotenz-Vorbild (objection_bridge.py:51-77, DELETE-then-INSERT) — ABER FOLD A-2/B3: STRIKTER scopen:
- Leerer Puffer → No-Op (return 0, KEIN DELETE).
- Nicht-leer → eigene Zeilen **dieses Calls** loeschen: Filter `org_id == ... AND call_id == ...` (NICHT nur
  conversation_log_id+org wie die Bruecke) — nie Zeilen anderer Calls anfassen. Dann frisch insert-only re-schreiben.

ANONYMISIERUNG (FOLD A-2 — KORRIGIERT gegen die cache=None-No-Op-Falle):
```
# FALSCH (urspruengliche Plan-08-Annahme): anonymize_output(text, cache=None) ist ein NO-OP
#   (anonymization.py:612-613 — reiner Cache-Reverse-Lookup) → wuerde ROHE Namen speichern (DSGVO-Loch).
# RICHTIG (Plan 09 / FOLD A-2): die anonymisierte STORAGE-Version wird am ERFASSEN erzeugt, mit dem
#   LEBENDEN Per-SID-Cache — Plan 09 produziert sie ohnehin (Live-Display roh, Storage anonymisiert).
#   Plan 08 ERFASST genau diese schon-anonymisierte Version; der Flush schreibt sie 1:1 (KEINE Anon im Flush).
```
Konkret: die drei Capture-Punkte haben die anonymisierte Storage-Version bereits zur Hand
(Auto-Variante: `cleaned_storage` aus Plan 09 Task 1/anonymize_for_storage; Knopf: das via anonymize_for_storage
gesäuberte `_antwort` deepgram_service.py:949-953; Keyword: profil-statischer Text, niedrig-PII, ebenfalls anonymize_for_storage mit Live-Cache).
record_suggestion_offer bekommt diese Version → KEIN zusaetzlicher Anon-Aufruf im Flush, KEIN cache=None.

Schema-/RLS-/FK-/Schild-Muster (Plan 01 rubric_score — 1:1 uebernehmen):
- HARTER FK: `call_id = Column(UUID_TYPE, ForeignKey('calls.id', ondelete='CASCADE'), index=True, nullable=True)` (F-08/DD-01, models.py:738-Vorbild)
- RLS: ENABLE + `FORCE ROW LEVEL SECURITY` + tenant_isolation-Policy `tenant_id = nullif(current_setting('app.tenant_id', true),'')::uuid` (0014:49-55)
- Owner: `op.execute("ALTER TABLE public.suggestion_reactions OWNER TO nerve_app")` (0010:40)
- Schild: `op.execute("COMMENT ON TABLE/COLUMN ...")` (0015-Stil)

intent_event.interaction_id (Tuermoeffner-Beleg, models.py:774 / 0016:76):
COMMENT == "... FK-Ziel fuer spaetere suggestion_reactions (Phase H) ..." → Naht bewusst gelegt; interaction_id
ist Korrelations-Spalte (KEIN harter FK, da kein PK).
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `suggestion_reactions` (CREATE) — existiert NICHT (grep-Beleg unten)
- `calls` (FK-ZIEL von suggestion_reactions.call_id ON DELETE CASCADE; sonst nur Lese-Referenz fuers Design)
- `intent_event` (Korrelations-Quelle der interaction_id — READ-ONLY, KEINE Aenderung; TAXO1-Vertrag)
- `state['ewb_clicks']` / `state['suggestion_offers']` (RAM, live_session — Quelle des Flushs, keine DB-Schicht)

### inspect.sh-Beleg (beim Execute neu ziehen)
```
$ inspect.sh schema suggestion_reactions   → "Did not find any relation named suggestion_reactions" (neu)
$ inspect.sh migrations                     → dann-HEAD (TAXO1 0018 + TAXO2 01/03/05) = down_revision
```
F-08 DSGVO-Beleg (Hauskonvention harter FK CASCADE, verbatim gegen git-Stand = Prod-Code):
```
$ grep -n "ondelete='CASCADE'" database/models.py
738: call_id = Column(UUID_TYPE, ForeignKey('calls.id', ondelete='CASCADE'), nullable=False)   # CallEvent
757: conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id', ondelete='CASCADE'), nullable=False)  # TranscriptSegment
```
Tuermoeffner-Beleg (interaction_id-Naht bereits gelegt):
```
$ grep -n "suggestion_reactions" database/models.py alembic/versions/0016_intent_event_table.py
models.py:774:  interaction_id = Column(..., comment="... FK-Ziel fuer spaetere suggestion_reactions (Phase H) ...")
0016_intent_event_table.py:76: COMMENT ... 'FK-Ziel fuer spaetere suggestion_reactions (Phase H)'
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Schreib-/Lese-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `entry` (slot/source/model/suggestion_text/interaction_id/ts) | record_suggestion_offer (Live-Loop) | RAM state['suggestion_offers'] (KEINE DB im Live-Pfad); suggestion_text = anonymisierte Storage-Version (Plan 09), interaction_id immer gesetzt (B1) | ⬜ Task 2 + Latenz-Test |
| `SuggestionReaction.call_id` | flush_suggestion_offers (Call-Ende) | DB suggestion_reactions.call_id (harter FK calls(id) CASCADE, F-08) | ⬜ Task 1+4 Cascade-Test |
| `SuggestionReaction.suggestion_text` | flush schreibt 1:1 (Anon am Erfassen, NICHT im Flush — Plan 09) | DB suggestion_reactions.suggestion_text (TEXT, schon anonymisiert) | ⬜ Task 3+4 verbatim-Test |
| `SuggestionReaction.interaction_id` | flush (aus RAM-entry; immer gesetzt, B1) | DB suggestion_reactions.interaction_id (UUID, Korrelation zu intent_event) | ⬜ Task 1+2 |
| `adoption_value/following_utterance_ref/reaction_class` | NICHT geschrieben (DEFERRED) | DB nullable Spalten — post-Launch | ⬜ Task 1 (nullable-Beleg) |
| `tenant_id` | flush (aus calls.tenant_id) | DB suggestion_reactions.tenant_id + RLS FORCE | ⬜ Task 1+4 |

## Verbindungs-Karte (Punkt 22)
- **suggestion_reactions existiert nicht:** `grep -rn "suggestion_reactions\|SuggestionReaction" services/ routes/ database/ alembic/` == 0 ausser dem COMMENT-Hinweis in models.py:774 + 0016:76 (= Tuermoeffner-Naht, KEIN Leser/Schreiber). Reine Neu-Tabelle.
- **Drei Capture-Punkte bewiesen (FOLD-A-Verifikation 22.06, gegen git-Stand = Prod):**
  - Slot B Auto-Variante: `claude_service.py:572 streame_auto_variante` — heute KEIN Persist (grep "streame_auto_variante" zeigt nur Stream-Logik). Neuer record_suggestion_offer-Aufruf.
  - Knopf: `deepgram_service.py:877-883` (intent_event-Emit, `_btn_iid` interaction_id vorhanden :860). Neuer record-Aufruf daneben.
  - Keyword: `einwand_keyword_matcher.py:321` (emit_intent_event, triggering_text). Neuer record-Aufruf daneben.
- **interaction_id-Korrelation (KEIN harter FK):** interaction_id ist im intent_event eine indizierte UUID-Spalte (KEIN PK) → suggestion_reactions.interaction_id ist eine indizierte Korrelations-Spalte, KEIN FK (kann nicht referenzieren, da kein eindeutiger PK). Die Tuermoeffner-Naht (models.py:774) meint genau diese Korrelation. Beim spaeteren Uebernahme-Scoring wird ueber interaction_id gejoint.
- **call_id-FK (F-08):** calls.id existiert (UUID PK). suggestion_reactions.call_id = harter FK ON DELETE CASCADE — DD-01-Konvention (models.py:738/757). nullable=True (Live-Call setzt es; Edge: kein call_id ermittelbar → NULL, Zeile bleibt via conversation_log_id korrelierbar). DSGVO: Cascade raeumt suggestion_text mit.
- **Flush-Hook bewiesen:** app_routes.py:392 ruft `mirror_ewb_clicks_to_objection_events` in der Call-Ende-Finalisierung (db_conv-Transaktion, Caller committet). flush_suggestion_offers dockt EXAKT hier an (gleiche Transaktion, gleicher Commit). KEIN zweiter Commit, KEIN zweiter DB-Roundtrip im Live-Pfad.
- **Latenz-Naht (Punkt 25):** der einzige Live-Loop-Eingriff ist `state.setdefault('suggestion_offers', []).append(entry)` unter state_lock — identisch zu record_ewb_click (live_session.py:949, seit Monaten latenz-neutral). KEIN DB/Netz/LLM. Beleg: Task-2-Latenz-Test (record_suggestion_offer macht keinen DB-Call).
- **RLS-Daemon-Falle (M-4) NICHT relevant hier:** der Flush laeuft im Call-Ende-REQUEST-Kontext (app_routes /api/beenden), NICHT im Slow-Lane-Daemon-Thread → der tenant_id-GUC ist via after_begin-Hook gesetzt (db.py:62-78). Anders als rubric_score (Plan 04, Daemon). Trotzdem RLS FORCE + tenant_id fuer Konsistenz/Abschottung. Im Test (Task 4) GUC-Positiv-Probe.

## 1. Lokaler Kontext (Punkt 14 — record_suggestion_offer + flush_suggestion_offers Inserts)
- `record_suggestion_offer` ist eine NEUE Funktion (eigener thread-safe Append, kein early-return-Problem). Aufgerufen aus drei Live-Pfaden (Auto/Knopf/Keyword) — jeder in try/except gekapselt (Live-Loop crasht NIE, Muster deepgram_service.py:884 `[PiP] ... skip`).
- `flush_suggestion_offers` ist NEU (eigene Funktion in services/suggestion_capture.py). Aufgerufen aus der Call-Ende-Finalisierung (app_routes.py, neben der Bruecke). try/except: Flush-Fehler bricht die Call-Finalisierung NIE (Muster app_routes.py:407 "Ein Fehler hier bricht die Call-Finalisierung NIE wie ObjectionEvent").

## 2. Funktions-Skelett
- `record_suggestion_offer(slot, source, model, suggestion_text, interaction_id, einwand_typ=None)`: baut entry-dict {slot, source, model, suggestion_text(**schon anonymisierte Storage-Version/Plan 09**), interaction_id(**immer gesetzt/B1**), einwand_typ, ts} → `with state_lock: state.setdefault('suggestion_offers', []).append(entry)`. KEIN DB.
- `flush_suggestion_offers(*, conversation_log_id, call_id, user_id, org_id, tenant_id, suggestion_offers, db) -> int`: leer → return 0 (No-Op, kein DELETE). Nicht-leer → DELETE eigene Zeilen (**org_id AND call_id-scoped/B3**, Idempotenz) → pro offer: `db.add(SuggestionReaction(suggestion_text=offer['suggestion_text'] schon-anon, ... adoption_value=None, following_utterance_ref=None, reaction_class=None ...))`. **KEINE Anon im Flush (kein cache=None).** return len. KEIN commit (Caller).

## 3. Cross-File-Awareness (Pflicht-grep, Punkt 20)
`grep -rn "suggestion_reactions\|SuggestionReaction\|record_suggestion_offer\|flush_suggestion_offers\|suggestion_offers" services/ routes/ database/` → vor diesem Plan == 0 (ausser Tuermoeffner-COMMENT). Nach Bau: record-Aufrufe in claude_service.py + deepgram_service.py + einwand_keyword_matcher.py; flush in app_routes.py; Definitionen in live_session.py + suggestion_capture.py + models.py.
`grep -rn "streame_auto_variante\|record_ewb_click\|get_or_open_moment" services/` → die drei Capture-Punkte + interaction_id-Quelle gegen den dann-lebenden Code bestaetigen (FOLD-A-Stand 22.06 re-verifizieren — evtl. Zeilen verschoben).

## 4. Edge-Case-Analyse + Latenz-/Race-Audit (Punkt 14)
1. **Latenz (Punkt 25, KRITISCH):** record_suggestion_offer macht NUR list.append unter state_lock → kein DB/Netz/LLM im Live-Loop. Test (Task 2) beweist: kein get_session/commit im record-Pfad. Wenn doch ein DB-Call reinrutscht → Dealbreaker, umbauen.
2. **Doppel-/api/beenden (Idempotenz, B3):** DELETE-then-INSERT strikt `org_id AND call_id`-scoped → Doppel-Klick/Reload am Call-Ende konvergiert auf denselben Zeilen-Satz, keine Duplikate, KEINE Fremd-Call-Zeilen angefasst (Gemini-Fund).
3. **Leerer Puffer:** kein Vorschlag im Call → flush = No-Op (return 0, kein DELETE) → bestehende Zeilen unberuehrt (zweiter Aufruf nach Reset darf nichts loeschen).
4. **interaction_id (B1, Gemini-Fund):** alle drei Capture-Hooks oeffnen ein Moment-Fenster (get_or_open_moment) → interaction_id IMMER gesetzt, NIE leer-NULL. (Spalte bleibt nullable als Defense, aber der Pfad fuellt sie immer.) Loest die urspruengliche "NULL ist via call_id korrelierbar"-Annahme auf.
5. **Anonymisierung (FOLD A-2, KRITISCH):** NICHT beim Flush — `anonymize_output(text, cache=None)` ist ein No-Op (anonymization.py:612) und wuerde rohe Namen speichern. Die Anon laeuft am ERFASSEN mit dem lebenden Per-SID-Cache (Plan 09 produziert die Storage-Version, der Live-Display bleibt roh). Plan 08 erfasst die schon-anonymisierte Version; der Flush schreibt sie 1:1. Damit ist die "Anonymisierer-disposed-beim-Flush"-Falle hinfaellig (Anon passiert waehrend der Call laeuft, Mapping garantiert lebendig).
6. **DSGVO-Cascade (F-08):** geloeschter Call → CASCADE raeumt suggestion_reactions (suggestion_text = potenzieller Wortlaut) → 0 Waisen. Test (Task 4).
7. **create_all-Race (DEPLOY-Lehre):** Migration VOR Restart, sonst nackte Tabelle ohne RLS/Owner/Schild. Verify im Deploy-Schritt.
8. **Crash mid-Call:** Puffer ist RAM → Prozess-Crash vor Call-Ende verliert den Puffer (kein Flush). Akzeptiert (insert-only, nicht-nachholbar IST die bewusste Eigenschaft; kein Backfill-Anspruch). Dokumentieren — NICHT als Bug behandeln.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ORM-Model SuggestionReaction (Roh-Angebot + nullable DEFERRED-Reaktion + harter FK CASCADE + Schilder)</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py:738 (CallEvent.call_id ondelete='CASCADE' — F-08-FK-Vorbild) + :774 (intent_event.interaction_id Tuermoeffner-COMMENT)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-01-rubric-score-tabelle-PLAN.md Task 1 (RLS/FK/Schild-Spalten-Muster — 1:1 Vorbild)
    - ssh ... 'bash scripts/inspect.sh schema calls' (calls.id UUID-PK + calls.tenant_id-Typ fuer FK)
  </read_first>
  <behavior>
    - Model instanziierbar mit Roh-Pflichtfeldern + leerem payload; ASCII-Spaltennamen
    - adoption_value/following_utterance_ref/reaction_class nullable (DEFERRED — kein server_default-Zwang, NICHT befuellt)
    - call_id harter FK ondelete='CASCADE' (F-08), nullable=True
    - interaction_id indizierte UUID-Spalte (KEIN FK)
  </behavior>
  <action>
    NEUE Klasse `class SuggestionReaction(Base)`, `__tablename__ = 'suggestion_reactions'`.
    Roh-Angebot-Spalten (JETZT befuellt):
    - `id = Column(UUID_TYPE, primary_key=True, default=...)` (UUID-PK-Muster wie rubric_score/calls)
    - `call_id = Column(UUID_TYPE, ForeignKey('calls.id', ondelete='CASCADE'), index=True, nullable=True, comment="Bezug zum Call. HARTER FK ON DELETE CASCADE (F-08/DD-01) — geloeschter Call raeumt das Angebot (suggestion_text=potenzieller Wortlaut) DSGVO-sauber mit. nullable: Edge ohne ermittelbare call_id.")`
    - `conversation_log_id = Column(Integer, index=True, nullable=True, comment="Bezug zur Session. Korrelations-/Gruppier-Schluessel des Flushs.")`
    - `interaction_id = Column(UUID_TYPE, index=True, nullable=True, comment="Moment-Klammer (Korrelation zu intent_event.interaction_id, TAXO1). Vom Capture-Pfad IMMER gesetzt (get_or_open_moment, FOLD A-2/B1); nullable nur als Defense. KEIN FK (kein PK). Naht fuer spaeteres Uebernahme-Scoring.")`
    - `org_id = Column(Integer, index=True, nullable=True, comment="Mandant (per-Berater-/Org-Filter, RLS-Ergaenzung).")`
    - `user_id = Column(Integer, index=True, nullable=True, comment="Berater (per-Berater-Auswertung, DEFERRED).")`
    - `slot = Column(String(8), nullable=True, comment="Liefer-Kanal: A=Profil-Stichwort-instant (Keyword/Fast-Lane) | B=KI-gestreamt (Auto-Variante/Knopf-Antwort).")`
    - `source = Column(String(24), index=True, nullable=True, comment="Ausloeser des Angebots: auto_variante | manual_button | keyword. Fuer A/B-Test der Antwort-Engine + systematisch-ignoriert-Analyse (TAXO3).")`
    - `model = Column(String(48), nullable=True, comment="Antwort-Modell (z.B. haiku/sonnet) — A/B-Test + Selbst-Verbesserung.")`
    - `suggestion_text = Column(Text, nullable=True, comment="Was NERVE ausgab — ANONYMISIERTE Storage-Version (Plan 09, am Erfassen mit lebendem Per-SID-Cache; NIE cache=None). DSGVO: Cascade-clean via call_id-FK.")`
    - `einwand_typ = Column(String(64), nullable=True, comment="Einwand-Typ-Kontext des Angebots (Korrelation).")`
    - `ts_offered = Column(DateTime, nullable=True, comment="Zeitpunkt des Angebots (Live-Latenz-Diagnose: ignoriert-weil-zu-spaet vs weil-schlecht).")`
    - `tenant_id = Column(UUID_TYPE, index=True, nullable=True, comment="Mandanten-Abschottung (RLS FORCE, abgeleitet aus calls.tenant_id).")`
    - `payload_jsonb = Column(JSON, nullable=False, server_default='{}', comment="Reserve fuer kuenftige Felder (confidence, einwand_typ-Detail) ohne Migration. FOLD A.")`
    - `created_at = Column(DateTime, default=utcnow)` (trivial, kein Schild)
    DEFERRED-Reaktions-Spalten (nullable, JETZT NICHT befuellt — post-Launch neu-berechenbar):
    - `adoption_value = Column(Float, nullable=True, comment="[DEFERRED, post-Launch] Uebernahme-Grad 0-1 (1:1 / ~90% / ignoriert). In TAXO2 NICHT befuellt (Soll-Verhalten §6).")`
    - `following_utterance_ref = Column(String(128), nullable=True, comment="[DEFERRED, post-Launch] Verweis auf die folgende Berater-Aeusserung (Uebernahme-Skala). NICHT in TAXO2.")`
    - `reaction_class = Column(String(24), nullable=True, comment="[DEFERRED, post-Launch] Klassifikation der Reaktion. NICHT in TAXO2.")`
    `__table_args__`: Index auf (interaction_id) + (call_id) ggf. schon via index=True. Tabellen-Schild-comment:
      "Roh-Erfassung jedes NERVE-Vorschlags pro Call (Auto-Variante Slot B + Manueller Knopf + Keyword), insert-only + anonymisiert, Call-Ende-Flush (KEIN Live-Write). NUR das ANGEBOT befuellt; Reaktions-Haelfte (adoption_value/...) DEFERRED post-Launch. call_id harter FK CASCADE (F-08). Status: lebt (neu, TAXO2 FOLD A). Schreibt services/suggestion_capture.py (Flush) + services/live_session.py (RAM); liest Uebernahme-Scoring (Post-Launch)."
    Importiere fehlende Typen (Text/Float/JSON/UUID_TYPE/ForeignKey/Index) konsistent zum models.py-Stil.
  </action>
  <verify>
    <automated>grep -c "class SuggestionReaction" database/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class SuggestionReaction" database/models.py` == 1; `grep "__tablename__ = 'suggestion_reactions'" database/models.py` == 1
    - `grep -E "ForeignKey\('calls.id', ondelete='CASCADE'\)" database/models.py` deckt suggestion_reactions.call_id ab (F-08)
    - Roh-Spalten als grep-Treffer: `grep -E "slot|source|model|suggestion_text|ts_offered|interaction_id|payload_jsonb" database/models.py` deckt alle ab
    - DEFERRED-Spalten nullable: `grep -E "adoption_value|following_utterance_ref|reaction_class" database/models.py` vorhanden + jeweils nullable=True (kein NOT NULL)
    - `grep -c "comment=" database/models.py` deckt alle nicht-trivialen Spalten ab (Schild Punkt 23)
    - Alle Spaltennamen ASCII (kein Umlaut)
  </acceptance_criteria>
  <done>SuggestionReaction-Model vollstaendig (Roh + DEFERRED-nullable, harter call_id-FK CASCADE), Schilder gesetzt, keine Schreiber.</done>
</task>

<task type="auto">
  <name>Task 2: RAM-Puffer record_suggestion_offer + drei Live-Capture-Hooks (Auto/Knopf/Keyword) + Reset — Latenz-neutral</name>
  <files>services/live_session.py, services/claude_service.py, services/deepgram_service.py, services/einwand_keyword_matcher.py</files>
  <read_first>
    - services/live_session.py:949-961 (record_ewb_click — exaktes Append-Muster) + :932 (state['ewb_clicks']=[] Reset)
    - services/claude_service.py:572 (streame_auto_variante — Slot B, heute NICHT persistiert)
    - services/deepgram_service.py:847-885 (Knopf-Block, _btn_iid interaction_id via get_or_open_moment :860)
    - services/einwand_keyword_matcher.py:300-325 (Keyword-emit, triggering_text/interaction_id-Kontext)
  </read_first>
  <action>
    1. **live_session.py — record_suggestion_offer:** NEUE Funktion analog record_ewb_click (thread-safe Append, KEIN DB):
       `def record_suggestion_offer(slot, source, model, suggestion_text, interaction_id, einwand_typ=None)`:
       entry = {slot, source, model, suggestion_text (**bereits anonymisierte Storage-Version aus Plan 09** — NICHT roh, NICHT cache=None), interaction_id, einwand_typ, ts=utcnow().isoformat()};
       `with state_lock: state.setdefault('suggestion_offers', []).append(entry)`.
       Reset ergaenzen (:932-Block): `state['suggestion_offers'] = []` daneben.
    2. **claude_service.py:572 (Slot B Auto-Variante):** nach dem Streamen capturen → `record_suggestion_offer(slot='B', source='auto_variante', model=<config-Modell>, suggestion_text=<die durch Plan 09 anonymisierte Storage-Version, NICHT der roh angezeigte Text>, interaction_id=<get_or_open_moment(sid)>)`. **B1: get_or_open_moment IMMER aufrufen → interaction_id nie None.** try/except (Live-Loop crasht nie).
    3. **deepgram_service.py Knopf-Block:** neben dem intent_event-Emit (:877) → `record_suggestion_offer(slot='B', source='manual_button', model=<config>, suggestion_text=<das anonymisierte _antwort, deepgram_service.py:953>, interaction_id=_btn_iid, einwand_typ=typ)`. `_btn_iid` ist via get_or_open_moment schon gesetzt (B1 erfuellt). try/except.
    4. **einwand_keyword_matcher.py:321 (Keyword/Slot A):** neben dem emit → `record_suggestion_offer(slot='A', source='keyword', model=None, suggestion_text=<gematchter Vorschlag, ggf. anonymize_output mit Live-Cache wenn PII moeglich>, interaction_id=<get_or_open_moment(sid)>, einwand_typ=...)`. **B1: get_or_open_moment IMMER → interaction_id nie None.** try/except.
    **B1 (FOLD A-2) HART:** an ALLEN drei Hooks wird das Moment-Fenster via `get_or_open_moment(sid, mode=..., now=...)` geoeffnet/geholt (Muster deepgram_service.py:860) → `interaction_id` ist IMMER gesetzt, NIE leer-NULL. (Plan-08-Urfassung akzeptierte NULL als "ueber call_id korrelierbar" — Gemini-Fund: stattdessen immer eine ID.)
    **Anon-Vertrag (FOLD A-2 / Plan 09 depends_on):** `suggestion_text` ist die ANONYMISIERTE Storage-Version, die Plan 09 am Erfassen mit dem lebenden Per-SID-Cache erzeugt — Plan 08 reicht sie nur durch (kein eigener Anon-Aufruf, KEIN cache=None). Der dem Berater LIVE angezeigte Text bleibt roh (Plan 09).
    **Slot/Source-Mapping beim Execute final festklopfen** (aus dem lebenden Code; FOLD-Definition als Leitplanke) + in SUMMARY dokumentieren.
    **HART (Punkt 25):** in record_suggestion_offer + an allen drei Hooks KEIN get_session/commit/Netz/LLM — nur das Append + den schon-vorhandenen (anonymisierten) Text/interaction_id durchreichen. get_or_open_moment ist ein RAM-Lookup (kein DB/Netz). Wenn der anonymisierte Text an einem Hook nur via zusaetzlichem Aufruf beschaffbar waere, der spuerbar Latenz kostet → NICHT bauen, in SUMMARY flaggen + Andre fragen.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "record_suggestion_offer"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def record_suggestion_offer" services/live_session.py` == 1; Append-Muster (state_lock + setdefault) wie record_ewb_click
    - `grep "state\['suggestion_offers'\] = \[\]" services/live_session.py` == 1 (Reset)
    - record_suggestion_offer-Aufruf in allen drei Capture-Files: `grep -l "record_suggestion_offer" services/claude_service.py services/deepgram_service.py services/einwand_keyword_matcher.py` == 3 Dateien
    - **Latenz-Beleg:** `grep -A12 "def record_suggestion_offer" services/live_session.py` zeigt KEIN get_session/commit/requests/anthropic — nur list-append (Punkt 25)
    - **B1 interaction_id immer:** alle drei Hooks rufen get_or_open_moment (`grep -nE "get_or_open_moment" services/claude_service.py services/deepgram_service.py services/einwand_keyword_matcher.py` deckt die Auto/Knopf/Keyword-Stellen ab); Test-Anruf → suggestion_reactions-Zeilen haben interaction_id NICHT NULL
    - **Anon-Vertrag:** der an record_suggestion_offer uebergebene suggestion_text ist die anonymisierte Storage-Version (Plan 09), NICHT der roh angezeigte Live-Text; KEIN cache=None im Capture-Pfad
    - jeder Hook in try/except (Live-Loop crasht nie)
    - Slot/Source-Mapping in SUMMARY dokumentiert
  </acceptance_criteria>
  <done>RAM-Puffer + drei latenz-neutrale Capture-Hooks; Reset; KEIN Live-DB-Write.</done>
</task>

<task type="auto">
  <name>Task 3: services/suggestion_capture.py flush_suggestion_offers (insert-only + Idempotenz org+call_id-scoped) + Call-Ende-Hook — KEINE Flush-Zeit-Anon</name>
  <files>services/suggestion_capture.py, routes/app_routes.py</files>
  <read_first>
    - services/objection_bridge.py (DELETE-then-INSERT Idempotenz-Muster; No-Op bei leer; Caller-Commit) — ABER strikter scopen (B3)
    - routes/app_routes.py:380-407 (Call-Ende-Finalisierung: mirror_ewb_clicks_to_objection_events-Aufruf :392 + try/except-Muster :407)
    - **anonymization.py:612-613** (BELEG: anonymize_output(text, cache=None) ist ein No-Op → NICHT im Flush nutzen; die Anon liegt am Erfassen, Plan 09/Task 2)
  </read_first>
  <action>
    1. **services/suggestion_capture.py — flush_suggestion_offers** (Vorbild objection_bridge, ABER FOLD A-2):
       `def flush_suggestion_offers(*, conversation_log_id, call_id, user_id, org_id, tenant_id, suggestion_offers, db) -> int`:
       - leer → return 0 (No-Op, KEIN DELETE).
       - nicht-leer → **DELETE strikt auf DIESEN Call (FOLD A-2/B3): `SuggestionReaction.org_id == org_id AND SuggestionReaction.call_id == call_id`** (synchronize_session=False) — NIE nur conversation_log_id, NIE Zeilen anderer Calls. Idempotenz gegen Doppel-/api/beenden.
       - pro offer: `db.add(SuggestionReaction(call_id=..., conversation_log_id=..., interaction_id=offer['interaction_id'], org_id, user_id, tenant_id, slot=offer['slot'], source=offer['source'], model=offer.get('model'), suggestion_text=offer['suggestion_text'] (**bereits anonymisiert aus Task 2/Plan 09 — KEINE Anon hier, KEIN cache=None**), einwand_typ=offer.get('einwand_typ'), ts_offered=<offer ts>, payload_jsonb={}, adoption_value=None, following_utterance_ref=None, reaction_class=None))`.
       - **KEINE Anonymisierung im Flush** (der Text ist schon die Storage-Version; eine Flush-Zeit-Anon mit cache=None waere ein No-Op und ein DSGVO-Loch — anonymization.py:612). KEIN commit (Caller, db_conv-Transaktion). return len.
       Modul-Docstring: FOLD A/A-2, insert-only, NUR Angebot, Reaktion DEFERRED, KEIN Live-Write, suggestion_text schon anonymisiert (Plan 09), DELETE org+call_id-scoped.
    2. **app_routes.py Call-Ende-Hook:** neben dem Bruecken-Aufruf (:392) → `from services.suggestion_capture import flush_suggestion_offers; flush_suggestion_offers(conversation_log_id=..., call_id=..., user_id=..., org_id=..., tenant_id=<aus call>, suggestion_offers=<state['suggestion_offers']>, db=db_conv)` in try/except (Fehler bricht Finalisierung NIE, :407-Muster). Den suggestion_offers-Puffer aus dem live_session-State dieser Session ziehen (Muster wie ewb_clicks ans mirror_ewb_clicks gereicht wird).
    **DSGVO (Cross-AI Pflicht):** belegen dass suggestion_text bereits anonymisiert ankommt (Plan-09-Vertrag) + in Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse referenzieren.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "flush_suggestion_offers"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def flush_suggestion_offers" services/suggestion_capture.py` == 1
    - Idempotenz: DELETE-then-INSERT org-scoped (`grep -A20 "def flush_suggestion_offers" services/suggestion_capture.py` zeigt .delete( + org-Filter + db.add); leer → return 0 (kein DELETE)
    - KEINE Anon im Flush (FOLD A-2): `grep -E "anonymize_output|anonymize\(" services/suggestion_capture.py` == 0 — suggestion_text kommt schon-anonymisiert aus Task 2/Plan 09; der Flush schreibt 1:1 (kein cache=None-No-Op). Beleg: test_flush_writes_storage_text_verbatim.
    - DEFERRED-Felder NICHT befuellt: adoption_value/following_utterance_ref/reaction_class = None im add() (grep-Beleg)
    - Call-Ende-Hook: `grep "flush_suggestion_offers" routes/app_routes.py` == 1, in try/except, db_conv-Transaktion (KEIN eigener commit)
    - DSGVO-Referenz in Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse vorhanden (DSGVO-Beweis liegt in Plan 09)
  </acceptance_criteria>
  <done>flush insert-only (schreibt schon-anonymisierte Storage-Version 1:1, KEINE Flush-Anon) + idempotent; Call-Ende-Hook in der Finalisierungs-Transaktion; KEIN Uebernahme-Scoring.</done>
</task>

<task type="auto">
  <name>Task 4: Alembic-Migration (VOR create_all/Restart) + Schema-/Cascade-/Anonymisierungs-/Latenz-Test + SCHILD-Guard GRUEN</name>
  <files>alembic/versions/XXXX_suggestion_reactions_table.py, tests/test_suggestion_reactions.py</files>
  <read_first>
    - alembic/versions/0014_crm_user_preferences_rls.py (RLS ENABLE+FORCE+tenant_isolation nullif-fail-closed)
    - alembic/versions/0010_add_transcript_segments.py:24+:40 (REFERENCES ... ON DELETE CASCADE + ALTER OWNER nerve_app)
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-op.execute-Stil)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-01-rubric-score-tabelle-PLAN.md Task 2+3 (Migration + Schema-Test-Muster 1:1)
    - ssh ... 'bash scripts/inspect.sh migrations' (echter HEAD = down_revision)
  </read_first>
  <action>
    1. **Migration** (`down_revision` = aktueller HEAD via inspect.sh, NICHT hardcoden; Datei auf echte Nummer benennen):
       upgrade(): `op.create_table('suggestion_reactions', ...)` ODER `CREATE TABLE IF NOT EXISTS public.suggestion_reactions (...)` — alle Spalten aus Task 1 (DDL-Paritaet).
       - call_id `UUID REFERENCES public.calls(id) ON DELETE CASCADE` (F-08, harter FK).
       - tenant_id `UUID REFERENCES public.tenant_orgs(id)` (FK erlaubt). payload_jsonb `JSONB NOT NULL DEFAULT '{}'`. adoption_value/following_utterance_ref/reaction_class nullable.
       - Indizes: call_id, conversation_log_id, interaction_id, source, org_id, user_id, tenant_id.
       - `ALTER TABLE public.suggestion_reactions OWNER TO nerve_app` (0010-Muster).
       - RLS: ENABLE + FORCE ROW LEVEL SECURITY + tenant_isolation-Policy (0014:51-55 nullif-fail-closed).
       - Schilder: COMMENT ON TABLE + COMMENT ON COLUMN je nicht-triviale Spalte (Texte aus Task-1-comment=).
       downgrade(): symmetrisch (DROP POLICY, DROP INDEX, DROP TABLE).
       **KEIN CHECK auf slot/source (Validierung Python-seitig).**
    2. **DEPLOY-REIHENFOLGE verifizieren (FOLD B Punkt 3):** im SUMMARY belegen, dass `deploy.sh production` die Migration VOR dem Gunicorn-Restart fahrt (sonst create_all → nackte Tabelle ohne RLS/Owner/Schild). Falls deploy.sh die Reihenfolge nicht garantiert → die Migration manuell als postgres VOR dem Restart fahren + flaggen.
    3. **tests/test_suggestion_reactions.py** (Runtime-Behavior, KEIN Source-Presence):
       - `test_suggestion_reactions_insert_roundtrip`: minimaler insert (slot/source/suggestion_text/payload_jsonb) → commit → query → assert Feldwerte; DEFERRED-Felder NULL. Server-seitig, Test-Tenant, raeumt auf.
       - `test_suggestion_reactions_cascade_on_call_delete` (F-08): Test-Call + suggestion_reactions-Zeile mit call_id → DELETE Call → assert count==0 (0 Waisen). Cleanup.
       - `test_flush_writes_storage_text_verbatim` (FOLD A-2): offer mit bereits-anonymisiertem suggestion_text (z.B. '[PERSON_A] ist...') → flush → assert die DB-Zeile traegt EXAKT diesen Text (Flush anonymisiert NICHT erneut, kein Doppel-Token, kein cache=None-No-Op). DSGVO-Beleg liegt in Plan 09 (Anon am Erfassen).
       - `test_flush_delete_scoped_to_call` (FOLD A-2/B3): zwei Calls (callA, callB) desselben org → flush fuer callA loescht/ersetzt NUR callA-Zeilen, callB-Zeilen bleiben unberuehrt. Beweist org+call_id-Scope (nicht nur conversation_log_id).
       - `test_record_suggestion_offer_always_sets_interaction_id` (FOLD A-2/B1): record_suggestion_offer mit gesetztem interaction_id → entry traegt es; (Capture-Hook-Test: get_or_open_moment wird gerufen — via Spy/monkeypatch belegen, dass interaction_id nie None ist).
       - `test_record_suggestion_offer_does_no_db_write` (Punkt 25): monkeypatch/Spy auf get_session → record_suggestion_offer ruft KEINE DB; assert der RAM-Puffer waechst um 1, get_session NICHT aufgerufen. (Latenz-Beleg.)
       - `test_flush_idempotent_on_double_call`: zweimaliger flush derselben Offers (gleicher call_id) → genau N Zeilen (kein Duplikat, B3-Scope).
    4. **SCHILD-Guard** server-seitig MANUELL (OS-User nerve_app, NERVE_SCHILD_TEST_DSN) → suggestion_reactions + nicht-triviale Spalten GRUEN.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u nerve_app NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest tests/test_schild_guard.py tests/test_suggestion_reactions.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh schema suggestion_reactions` zeigt alle Roh-Spalten + nullable DEFERRED-Spalten + `call_id ... REFERENCES calls(id) ON DELETE CASCADE`; Owner nerve_app; RLS aktiv
    - SCHILD-Guard GRUEN: suggestion_reactions + alle nicht-trivialen Spalten Schild >= 10 Zeichen
    - test_suggestion_reactions_insert_roundtrip + cascade_on_call_delete (F-08) + flush_writes_storage_text_verbatim (FOLD A-2 Anon-Vertrag) + flush_delete_scoped_to_call (B3) + record_suggestion_offer_always_sets_interaction_id (B1) + record_suggestion_offer_does_no_db_write (Punkt 25) + flush_idempotent_on_double_call gruen server-seitig
    - DEPLOY-Reihenfolge belegt: Migration VOR Restart (kein nackte-Tabelle-Risiko); SUMMARY-Notiz
    - `inspect.sh count suggestion_reactions` == 0 nach Test-Cleanup
  </acceptance_criteria>
  <done>Migration reversibel + VOR create_all; Tabelle live (leer), harter FK CASCADE, Owner nerve_app, RLS FORCE, Schilder; Cascade/Anon/Latenz/Idempotenz bewiesen; Guard gruen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>suggestion_reactions-Tabelle + RAM-Puffer (Auto/Knopf/Keyword) + Call-Ende-Flush insert-only anonymisiert. NUR Angebot, Reaktion DEFERRED.</what-built>
  <how-to-verify>
    Nach `bash deploy.sh production`:
    1. Service startet sauber (kein ImportError) — `inspect.sh health`.
    2. **Test-Anruf** (Cold-Call) als Test-User mit ≥1 Auto-Variante (Slot B gestreamt) + ≥1 Knopf-Klick:
       - nach Call-Ende: `inspect.sh sample suggestion_reactions` zeigt je EINE Zeile pro Vorschlag mit slot/source/model/ts_offered;
       - **interaction_id ist BEFUELLT (nicht NULL) — B1**;
       - `suggestion_text` ist ANONYMISIERT (kein Klar-Name/PII) — die Anon kommt am Erfassen aus Plan 09, NICHT aus dem Flush;
       - adoption_value/following_utterance_ref/reaction_class sind NULL (DEFERRED).
    3. **Latenz (Punkt 25):** die Live-Antworten (Auto-Variante/Knopf) erscheinen subjektiv genauso schnell wie vorher (kein spuerbarer Verzug); die dem Berater angezeigte Antwort traegt ECHTE Namen (Plan 09 — Live roh, Storage anonymisiert).
    4. `inspect.sh grep "suggestion_reactions"` zeigt KEINEN Live-Loop-INSERT (nur Call-Ende-Flush).
    Claudian: F-08 — Test-Call loeschen → `inspect.sh count suggestion_reactions` fuer die call_id == 0.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe welche Zeile/Latenz/Anonymisierung abweicht.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| App (nerve_app) → suggestion_reactions | Mandanten-/Berater-Daten (Wortlaut) kreuzen; ohne RLS Cross-Org-Leck |
| Live-Loop → state['suggestion_offers'] (RAM) | Latenz-kritischer Pfad — nur Append erlaubt |
| Call-Loeschung (DSGVO Art.17) → suggestion_reactions | suggestion_text (Wortlaut) muss mit dem Call verschwinden |
| Capture (Anonymisierung am Erfassen, Plan 09) → RAM/DB | Kunden-PII darf nicht im Klartext landen; Live-Display bleibt roh |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-08-01 | D (Latenz / Punkt 25) | DB-Write im Live-Loop → spuerbarer Antwort-Verzug | mitigate | record_suggestion_offer = NUR list-append unter state_lock (record_ewb_click-Muster); EINZIGER DB-Write = Call-Ende-Flush. Test test_record_suggestion_offer_does_no_db_write beweist kein get_session im Live-Pfad. |
| T-TAXO2-08-02 | I (Info Disclosure / DSGVO) | suggestion_text speichert Kunden-Wortlaut im Klartext | mitigate | **FOLD A-2: Anon am ERFASSEN mit lebendem Per-SID-Cache (Plan 09), NIE cache=None im Flush (No-Op-Falle anonymization.py:612). Plan 08 erfasst die Storage-Version, Flush schreibt sie 1:1. Test flush_writes_storage_text_verbatim; DSGVO-Beweis liegt in Plan 09.** |
| T-TAXO2-08-03 | I (DSGVO Art.17) | verwaiste suggestion_reactions nach Call-Loeschung | mitigate | F-08 harter FK call_id ON DELETE CASCADE (DD-01); Art.17-Hard-Purge deckt die Tabelle ohne Worker-Erweiterung mit ab. Test cascade_on_call_delete. |
| T-TAXO2-08-04 | I (Cross-Org-Leck) | Berater sieht Angebote anderer Firma | mitigate | RLS ENABLE+FORCE + tenant_isolation (nullif-fail-closed); FORCE greift auch fuer Owner nerve_app. org_id/user_id zusaetzlich. |
| T-TAXO2-08-05 | T (Doppel-Insert / Fremd-Call-Loeschung) | Doppel-/api/beenden dupliziert Angebote ODER ein zu breiter DELETE loescht Zeilen anderer Calls | mitigate | **FOLD A-2/B3: Idempotenz-DELETE strikt auf `org_id AND call_id` gescoped (NICHT nur conversation_log_id) — nie Fremd-Call-Zeilen. leer → No-Op. Test flush_delete_scoped_to_call + flush_idempotent.** |
| T-TAXO2-08-06 | D (Deploy / create_all) | create_all baut nackte Tabelle (kein RLS/Owner/Schild) | mitigate | Migration VOR Gunicorn-Restart (FOLD B P3); Verify im Deploy-Schritt; SCHILD-Guard fangt nackte Tabelle (rot). |
| T-TAXO2-08-07 | A (Datenverlust mid-Call, KNOWN-ISSUE) | Prozess-Crash vor Call-Ende verliert RAM-Puffer → Vorschlaege dieses Calls nicht erfasst | accept | **FOLD A-2/B2: explizit dokumentiertes Known-Issue (nicht still). Insert-only / nicht-nachholbar IST die bewusste Eigenschaft (Tuermoeffner-Regel); kein Backfill-Anspruch.** |
| T-TAXO2-08-08 | T (Scope-Creep) | Uebernahme-Scoring rutscht in TAXO2 rein | mitigate | Boundary HART: adoption_value/following_utterance_ref/reaction_class NULL; kein KI-Richter; Test belegt DEFERRED-Felder NULL. |
| T-TAXO2-08-09 | I (Korrelations-Verlust / B1) | interaction_id leer → Vorschlag nicht zum Moment korrelierbar | mitigate | **FOLD A-2/B1: alle drei Capture-Hooks oeffnen get_or_open_moment → interaction_id immer gesetzt. Test record_suggestion_offer_always_sets_interaction_id; Test-Anruf belegt nicht-NULL.** |
</threat_model>

<verification>
- suggestion_reactions live (leer), Owner nerve_app, RLS ENABLE+FORCE+tenant_isolation, harter call_id-FK CASCADE (F-08), Schilder (Guard gruen).
- Drei Live-Capture-Hooks (Auto Slot B / Knopf / Keyword) → record_suggestion_offer (RAM, latenz-neutral); Latenz-Test beweist kein DB-Write im Live-Pfad (Punkt 25).
- B1: alle drei Hooks oeffnen get_or_open_moment → interaction_id IMMER gesetzt (Test + Test-Anruf nicht-NULL).
- Anon-Vertrag (Plan 09): suggestion_text ist die am Erfassen anonymisierte Storage-Version; Flush schreibt sie 1:1, KEINE Flush-Anon, KEIN cache=None (Test flush_writes_storage_text_verbatim).
- Call-Ende-Flush insert-only + idempotent, DELETE strikt org+call_id-scoped (B3) in der Finalisierungs-Transaktion (KEIN zweiter Commit/Roundtrip).
- NUR Angebot befuellt; adoption_value/following_utterance_ref/reaction_class NULL (DEFERRED) — Test belegt.
- F-08: Test-Call-Loeschung → 0 verwaiste suggestion_reactions-Zeilen.
- DSGVO: Live-Display roh (echte Namen, Plan 09), Storage anonymisiert; Cross-AI-Pflicht + DSGVO-Analyse-Referenz.
- DEPLOY-Reihenfolge: Migration VOR create_all/Restart (FOLD B P3) belegt.
- Known-Issue (B2): Crash-vor-Flush → Datenverlust dieses Calls explizit dokumentiert (accept).
- Tuermoeffner: interaction_id-Korrelation zu intent_event (Naht models.py:774/0016:76) genutzt.
- Test-Anruf-DATEN-FLUSS (riskante Welle, autonomous:false): Zeilen erscheinen, interaction_id befuellt, suggestion_text anonymisiert, Live-Antwort echte Namen, Latenz unveraendert.
</verification>

<success_criteria>
- public.suggestion_reactions live (Owner nerve_app, RLS FORCE, harter FK CASCADE, Schilder); Migration VOR create_all.
- Jeder Vorschlag (Auto/Knopf/Keyword) erfasst mit IMMER gesetzter interaction_id (B1) + bereits anonymisierter Storage-Version (Plan 09) im RAM-Puffer + Call-Ende insert-only geflusht (DELETE org+call_id-scoped, B3) — KEIN Live-DB-Write, Latenz unveraendert (Punkt 25).
- NUR das ANGEBOT; Uebernahme-Scoring DEFERRED (nullable Reaktions-Spalten, NICHT befuellt).
- F-08-Cascade + Anon-Vertrag (verbatim) + B1 + B3 + Idempotenz + Latenz per server-seitigem pytest bewiesen; Guard gruen; Test-Anruf-Daten-Fluss human-verifiziert.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-08-SUMMARY.md` (inkl. verbatim `inspect.sh schema suggestion_reactions` mit FK-CASCADE-Zeile + count=0 + Guard-GRUEN-Output, Slot/Source-Mapping-Doku, Latenz-Beleg (kein DB-Write im record-Pfad), DEPLOY-Reihenfolge-Beleg (Migration vor create_all), DSGVO-Anonymisierungs-Beleg + DSGVO-Analyse-Referenz, F-08-Cascade-Test-Beleg, Test-Anruf-Daten-Fluss-Beleg, echter Migrations-Revision).
</output>
