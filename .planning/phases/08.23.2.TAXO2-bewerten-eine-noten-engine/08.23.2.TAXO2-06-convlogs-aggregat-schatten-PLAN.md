---
phase: 08.23.2.TAXO2
plan: 06
type: execute
wave: 6
depends_on: ["04", "05"]
files_modified:
  - services/rubric_engine.py
  - services/slow_lane.py
  - tests/test_aggregat_shadow.py
autonomous: true
requirements: ["SPEC Req 8 (Schatten-Welle)"]
user_setup: []

must_haves:
  truths:
    - "Die Engine rechnet die ENGINE-HERLEITBAREN conversation_logs-Aggregate aus dem intent_event-Strom + per-SID-Speech-Stats — schreibt aber NUR coaching_score (Plan 04). HERLEITBAR (FOLD 23.06., am Code verifiziert): einwaende_gesamt/behandelt/fehlgeschlagen/ignoriert, vorwaende_erkannt, redeanteil_avg, tempo_avg, laengster_monolog."
    - "kb_start/end/min/max + skript_abdeckung sind NICHT engine-herleitbar (compute_aggregates sieht nur events + speech_stats, KEINE Kaufbereitschafts- oder Skript-Daten) → sie werden NICHT im Schatten mitgerechnet und bleiben dauerhaft beim Live-Schreiber (app_routes.py:341-374). Im Schatten-Vergleich als 'bleibt live, nicht engine-owned' markiert. (kb_verlauf war eh nie Teil des Cutovers — bleibt.)"
    - "Die übrigen (herleitbaren) Aggregate werden im Schatten mitgerechnet + Diskrepanzen gegen die heutigen Schreiber geLOGGT (NICHT geschrieben)"
    - "Die Diskrepanz-Logs sind via inspect.sh logs für einen Test-Call sichtbar und erklärbar"
    - "Die heutigen Aggregat-Schreiber bleiben in dieser Welle unverändert (kein Cutover hier)"
  artifacts:
    - path: "services/rubric_engine.py"
      provides: "compute_aggregates(events, speech_stats) -> dict (alle conversation_logs-Aggregat-Felder, reine Funktion)"
      contains: "def compute_aggregates"
    - path: "services/slow_lane.py"
      provides: "Schatten-Berechnung + Diskrepanz-Logging gegen heutige conversation_logs-Werte"
      contains: "shadow"
  key_links:
    - from: "services/slow_lane.py (Schatten)"
      to: "conversation_logs (LESE heutige Werte)"
      via: "Diskrepanz-Vergleich Engine-vs-heute (nur LOGGEN, nicht schreiben)"
      pattern: "discrepanc"
---

<objective>
Erste Stufe der risiko-gestaffelten conversation_logs-Konsolidierung (Req 8, Gemini-Cross-AI-Reversal: tief aber gestaffelt). Die Engine rechnet am Call-Ende die ENGINE-HERLEITBAREN denormalisierten Aggregate (`einwaende_*`, `vorwaende_erkannt`, `redeanteil_avg`, `tempo_avg`, `laengster_monolog`) aus dem intent_event-Strom + per-SID-Speech-Stats — schreibt aber in dieser Welle NUR `coaching_score` (das macht Plan 04). Die übrigen (herleitbaren) Aggregate werden im SCHATTEN mitgerechnet und die Diskrepanz gegen die heutigen Schreiber-Werte GELOGGT (NICHT geschrieben). So sehen wir vor dem Cutover ob die Engine-Berechnung die heutigen Werte trifft.

**FOLD 23.06. (am Code verifiziert) — `kb_*` + `skript_abdeckung` sind NICHT engine-herleitbar:** `compute_aggregates(events, speech_stats)` bekommt NUR den Einwand-Strom + Sprech-Stats — WEDER Kaufbereitschafts-Daten (kb kommt aus dem Live-Analyse-Loop / `kb_verlauf`) NOCH Skript-Daten (aus der Teleprompter-Verfolgung). Die Engine kann `kb_start/end/min/max` + `skript_abdeckung` strukturell nicht rechnen (Plan-Z.116 „falls verfügbar" / Z.117 „int(...)" = unerfüllbar). → Sie werden NICHT im Schatten mitgerechnet und bleiben DAUERHAFT beim Live-Schreiber (app_routes.py:341-374); im Schatten-Vergleich als „bleibt live, nicht engine-owned" markiert. (Design-Brief Abschnitt 3.)

Purpose: De-Risk vor dem Single-Writer-Cutover (Plan 07). Wellen-Schnitt (TAXO1-D-02). FK/Struktur unangetastet.
Output: compute_aggregates (reine Funktion) + Schatten-Berechnung + Diskrepanz-Logging + Test.

KEIN Cutover hier (Plan 07). Die heutigen Schreiber (app_routes.py:350-364 + :455-456 Reconcile) bleiben unverändert. Engine schreibt KEINE Aggregate (nur Schatten-Log).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** Schatten-Welle (RESEARCH §I) → Test-Anruf nötig, aber DATEN-FLUSS = Diskrepanz-Logs sichtbar (`inspect.sh logs`), NICHT Screen-Render (es wird ja nichts geschrieben). `bash deploy.sh production` + `inspect.sh logs`. KEIN lokales pytest.

**Anti-K1-Falle (Punkt 21):** Engine liest dieselbe per-SID-Quelle `get_speech_stats(sid)` (NICHT tote Globale). `redeanteil_avg`/`tempo_avg` sind `integer`-Spalten, `laengster_monolog` `double` — Engine-Werte entsprechend casten.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- conversation_logs-Aggregat-Schreiber (RESEARCH §A4) — heutige Quelle. -->

Heutige Live-Aggregat-Schreiber (BLEIBEN in dieser Welle):
- app_routes.py:341-374 (CREATE in api_beenden):
    einwaende_gesamt(:350=len(ewb_clicks)), einwaende_behandelt(:351 ga_details erfolgreich),
    einwaende_fehlgeschlagen(:352), einwaende_ignoriert(:353), vorwaende_erkannt(:354 ga_details ist_vorwand),
    kb_start/end/min/max(:355-358), redeanteil_avg(:359=stats.redeanteil), tempo_avg(:360=stats.tempo),
    laengster_monolog(:361=stats.monolog), skript_abdeckung(:364)
    Quelle heute: per-SID stats (get_speech_stats), ewb_clicks, ga_details, skript_pct
- app_routes.py:455-456 (UPDATE POLISH-38): einwaende_gesamt/behandelt Reconcile

conversation_logs Aggregat-Spalten + Typen (inspect.sh §A1):
  einwaende_gesamt/behandelt/fehlgeschlagen/ignoriert (int), vorwaende_erkannt (int),
  kb_start/end/min/max, redeanteil_avg (INTEGER), tempo_avg (INTEGER), laengster_monolog (DOUBLE), skript_abdeckung (INTEGER)

intent_event-Strom (TAXO1): intent_type/confidence/handling_score_numeric (→ einwaende_* re-derivieren)
Speech-Stats: get_speech_stats(sid) per-SID (→ redeanteil_avg/tempo_avg/laengster_monolog, K1)
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `conversation_logs` (NUR LESE der heutigen Werte für Diskrepanz-Vergleich; KEIN Write) — Schema §A1
- `intent_event` (LESE, Aggregat-Quelle) — DEPENDS-ON-TAXO1
### inspect.sh-Beleg (§A1, beim Execute neu)
conversation_logs Aggregat-Spalten + Typen (redeanteil_avg/tempo_avg/skript_abdeckung = INTEGER, laengster_monolog = DOUBLE).
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| compute_aggregates() Ergebnis | Engine rechnet (RAM, NICHT geschrieben) | RAM (Schatten) | ⬜ Task 1 |
| heutige conversation_logs-Werte | LESE für Vergleich | DB conversation_logs (int/double) | ⬜ Task 2 (Typ-Cast beachten) |
| Diskrepanz-Log | print/log [SHADOW] | Prod-Logs (inspect.sh logs) | ⬜ Task 2 Test-Anruf |
| speech_stats | get_speech_stats(sid) per-SID (K1, NICHT Globale) | per-SID RAM | ✓ RESEARCH §A4 |

## Verbindungs-Karte (Punkt 22)
- **Aggregat-Schreiber bewiesen (RESEARCH §A4):** Live-Aggregate aus app_routes.py:341-374 (CREATE) + :455-456 (POLISH-38 Reconcile). learning.py/coaching_service = Training/Read-only, KEIN Live-Writer. → In dieser Welle BLEIBEN sie; Engine rechnet im Schatten parallel.
- **Typ-Falle (Anti-K1/K8):** redeanteil_avg/tempo_avg/skript_abdeckung INTEGER, laengster_monolog DOUBLE → compute_aggregates castet entsprechend, sonst Schein-Diskrepanz (float vs int).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_aggregates (reine Funktion, alle conversation_logs-Aggregate aus intent_event + Speech-Stats)</name>
  <files>services/rubric_engine.py, tests/test_aggregat_shadow.py</files>
  <read_first>
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §A4 (Schreiber-Karte + Quellen + Typen)
    - app_routes.py:341-374 (heutige Berechnung — als Referenz WAS sie rechnet, Semantik-Match)
    - services/live_session.py get_speech_stats (per-SID-Quelle)
  </read_first>
  <behavior>
    - compute_aggregates(events, speech_stats) liefert dict mit allen Aggregat-Keys
    - int-Felder als int gecastet (redeanteil_avg/tempo_avg/skript_abdeckung), laengster_monolog float
    - einwaende_gesamt aus intent_event (Tor-1-gefiltert), einwaende_behandelt aus handling_score_numeric
  </behavior>
  <action>
    `def compute_aggregates(events, speech_stats) -> dict` (reine Funktion, kein DB-Write):
    Berechnet die ENGINE-HERLEITBAREN conversation_logs-Aggregate aus dem intent_event-Strom + per-SID-Speech-Stats:
    - einwaende_gesamt = count(intent_event einwand-artig, confidence>=Tor-1); einwaende_behandelt = count(handling_score_numeric IS NOT NULL bzw. >= Stufe); einwaende_fehlgeschlagen/ignoriert analog; vorwaende_erkannt = count(intent_type vorwand).
    - redeanteil_avg = int(speech_stats.redeanteil); tempo_avg = int(speech_stats.tempo); laengster_monolog = float(speech_stats.monolog).
    - **NICHT rechnen (FOLD 23.06.): kb_start/end/min/max + skript_abdeckung** — die Eingänge fehlen (kein kb_verlauf, kein skript_pct in der Signatur). Diese Felder bleiben beim Live-Schreiber (app_routes.py:341-374); compute_aggregates gibt sie NICHT zurück (kein leerer/0-Platzhalter, der eine Schein-Diskrepanz erzeugt). Wenn ein späterer Bedarf entsteht, müsste die Engine zusätzliche Eingänge bekommen — das ist NICHT TAXO2-Scope (Design-Brief Abschnitt 3 + Redesign-Phase).
    Pro Aggregat ein dokumentierter Semantik-Match (in SUMMARY): alt→neu (z.B. einwaende_gesamt alt=len(ewb_clicks) inkl. kaputte, neu=Tor-1-gefiltert → bewusste Verbesserung).
    Typ-Casts exakt zur DB-Spalte (Anti-K1-Falle).
    Tests: deterministische Eingangs-Dicts → erwartete Aggregate; Typ-Asserts (int/float); Test belegt, dass kb_*/skript_abdeckung NICHT im Rückgabe-dict sind (engine-owned-Grenze).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_aggregat_shadow.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def compute_aggregates" services/rubric_engine.py` == 1; reine Funktion (kein db.commit/setattr)
    - die HERLEITBAREN Aggregat-Felder im Rückgabe-dict (einwaende_*, vorwaende_erkannt, redeanteil_avg, tempo_avg, laengster_monolog); korrekte Typen (int/float zur DB-Spalte)
    - **kb_start/end/min/max + skript_abdeckung sind NICHT im Rückgabe-dict (FOLD 23.06., nicht engine-herleitbar) — Test belegt deren Abwesenheit**
    - Semantik-Match pro Aggregat in SUMMARY (alt→neu)
  </acceptance_criteria>
  <done>compute_aggregates rechnet alle Aggregate (reine Funktion), Typ-konsistent.</done>
</task>

<task type="auto">
  <name>Task 2: Schatten-Berechnung + Diskrepanz-Logging (NUR loggen, nicht schreiben) + Test-Anruf</name>
  <files>services/slow_lane.py</files>
  <read_first>
    - services/slow_lane.py (Call-Ende-Merge aus Plan 04 — hier erweitern)
    - services/rubric_engine.py compute_aggregates (Task 1)
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md §I (Schatten-Logging-Mechanik)
  </read_first>
  <action>
    Im Call-Ende-Merge (slow_lane.py, Plan 04): nach compute_rubric ZUSÄTZLICH compute_aggregates rechnen. Die heutigen conversation_logs-Werte des Calls LESEN. Pro HERLEITBAREM Aggregat-Feld die Diskrepanz Engine-vs-heute vergleichen und LOGGEN (NICHT schreiben):
    `print(f"[SHADOW] call={call_id} {feld}: engine={e} heute={h} diff={e-h}")` (sichtbar via inspect.sh logs).
    **NUR die herleitbaren Felder vergleichen** (einwaende_*, vorwaende_erkannt, redeanteil_avg, tempo_avg, laengster_monolog). **kb_*/skript_abdeckung NICHT im Schatten vergleichen** (FOLD 23.06.) — einmalig als `[SHADOW] {feld}: bleibt live (nicht engine-owned)` markieren, damit der Cutover-Plan (07) sie definitiv NICHT anfasst.
    NUR coaching_score wird geschrieben (Plan 04). Alle anderen Aggregate bleiben unberührt — die heutigen Schreiber (app_routes:341-374 + :455-456) laufen weiter.
    Schwelle/Form des Diskrepanz-Logs (Discretion): jede Abweichung != 0 loggen; bei Strings/Counts exakt; bei float-Aggregaten Toleranz dokumentieren.
    **VERIFY Zeitstempel/Dauer (FOLD 23.06., Gemini-Blindfleck — Pflicht im SUMMARY):** welchen Zeitstempel nimmt der async-Worker fürs Call-Ende bzw. die Dauer (`dauer_sek`)? Belegen, dass die angezeigte Dauer am `/api/beenden`-Moment festgehalten wird (Call-Ende), NICHT zum (evtl. viel späteren, bis zum H-2-Sweep nach `SCORE_SWEEP_AFTER_S`) Merge-Zeitpunkt neu gerechnet wird — sonst verfälscht langes Schweigen am Call-Ende / ein hängendes Outcome-Fenster die Dauer massiv. Im Schatten loggen falls eine Dauer-bezogene Größe berührt wird; Befund in SUMMARY (Cutover-Freigabe-relevant).
    **Test-Anruf:** nach Deploy 1 Live-Test-Call → `inspect.sh logs` zeigt [SHADOW]-Diskrepanz-Zeilen für den Call (herleitbare Felder) + die „bleibt live"-Markierung für kb_*/skript; Abweichungen erklärbar/dokumentiert (z.B. einwaende_gesamt-Diff = Tor-1-Filter-Effekt, erwartet).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 300'</automated>
  </verify>
  <acceptance_criteria>
    - Call-Ende-Merge loggt [SHADOW]-Diskrepanz pro Aggregat-Feld (engine vs heute)
    - KEIN Aggregat-Write aus der Engine in dieser Welle: `grep -rn "conversation_logs\|ConversationLog" services/slow_lane.py` zeigt nur LESE (für Vergleich), kein setattr/UPDATE der Aggregate
    - Test-Anruf: `inspect.sh logs` zeigt [SHADOW]-Zeilen für den Test-Call; Abweichungen in SUMMARY erklärt
    - heutige Schreiber (app_routes:350-364/:455-456) unverändert
  </acceptance_criteria>
  <done>Schatten-Berechnung + Diskrepanz-Logs sichtbar; nichts geschrieben außer coaching_score.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Engine-Schatten → conversation_logs (LESE) | nur Vergleich, kein Write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-06-01 | T (Schein-Diskrepanz) | float-vs-int-Cast erzeugt falsche Diff | mitigate | compute_aggregates castet exakt zur DB-Spalte (int/double, Anti-K1-Falle). |
| T-TAXO2-06-02 | I (versehentlicher Write) | Schatten schreibt doch ein Aggregat | mitigate | grep-Gate: kein UPDATE/setattr der Aggregate in slow_lane.py (nur coaching_score, Plan 04). |
</threat_model>

<verification>
- Test-Anruf: [SHADOW]-Diskrepanz-Logs sichtbar (inspect.sh logs), erklärbar/dokumentiert.
- grep: kein Engine-Aggregat-Write (nur coaching_score); heutige Schreiber unverändert.
- compute_aggregates Typ-konsistent (int/double).
</verification>

<success_criteria>
- Engine rechnet alle Aggregate im Schatten, loggt Diskrepanzen, schreibt nichts (außer coaching_score).
- Semantik-Match pro Aggregat dokumentiert (Cutover-Vorbereitung).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-06-SUMMARY.md` (inkl. Test-Anruf-[SHADOW]-Diskrepanz-Sample, Semantik-Match pro Aggregat, Abweichungs-Erklärung als Cutover-Freigabe-Beleg).
</output>
