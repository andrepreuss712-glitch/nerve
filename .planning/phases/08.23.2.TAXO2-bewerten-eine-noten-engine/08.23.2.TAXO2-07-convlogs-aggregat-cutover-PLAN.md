---
phase: 08.23.2.TAXO2
plan: 07
type: execute
wave: 7
depends_on: ["06"]
files_modified:
  - routes/app_routes.py
  - services/slow_lane.py
  - tests/test_aggregat_cutover.py
autonomous: false
requirements: ["SPEC Req 8 (Cutover-Welle)"]
user_setup: []

must_haves:
  truths:
    - "Die Engine ist GENAU EIN Schreiber je ENGINE-HERLEITBAREM conversation_logs-Aggregat-Feld — die alten Schreiber DIESER Felder sind raus. HERLEITBAR (FOLD 23.06.): einwaende_gesamt/behandelt/fehlgeschlagen/ignoriert, vorwaende_erkannt, redeanteil_avg, tempo_avg, laengster_monolog."
    - "kb_start/end/min/max + skript_abdeckung BLEIBEN beim Live-Schreiber (app_routes.py:341-374) — NICHT entfernt, NICHT der Engine zugewiesen (FOLD 23.06., am Code verifiziert: compute_aggregates kann sie nicht herleiten). Für diese Felder ändert der Cutover NICHTS. (kb_verlauf war nie im Cutover — bleibt.)"
    - "Die Engine schreibt die Aggregate NUR per UPDATE WHERE conversation_log_id (NIE INSERT — der sync-CREATE in api_beenden legt die Zeile an + ankert die FK-Kinder; Ordering sicher: CREATE vor async-Merge). UPDATE ohne Treffer → loggen, kein Crash (FOLD 23.06., gespaltene Schreiber bestätigt)"
    - "Der Cutover passiert erst NACHDEM die Schatten-Welle (Plan 06) sauber war (Diskrepanzen erklärt)"
    - "Ein Test-Anruf rendert Dashboard/Coaching/Auswertung korrekt aus den Engine-geschriebenen Aggregaten (herleitbar) UND den weiter live geschriebenen kb_*/skript_abdeckung"
    - "conversation_logs-FK/Spalten-Struktur ist unverändert (kein Rename/Zombie, nur Schreiber-Konsolidierung)"
  artifacts:
    - path: "services/slow_lane.py"
      provides: "Engine schreibt alle conversation_logs-Aggregate (einziger Schreiber je Feld)"
    - path: "routes/app_routes.py"
      provides: "alte Aggregat-Schreiber (CREATE :350-364 + POLISH-38 Reconcile :455-456) entfernt"
  key_links:
    - from: "services/slow_lane.py (Cutover)"
      to: "conversation_logs (alle Aggregate)"
      via: "Engine = EIN Schreiber je Feld (§0.1)"
      pattern: "conversation_logs"
---

<objective>
Zweite Stufe (Cutover) der conversation_logs-Konsolidierung (Req 8). NACHDEM die Schatten-Welle (Plan 06) sauber war (Diskrepanzen erklärt/dokumentiert), werden die alten Schreiber der ENGINE-HERLEITBAREN Aggregat-Felder entfernt und die Engine wird GENAU EIN Schreiber je herleitbarem Feld (`einwaende_*`, `vorwaende_erkannt`, `redeanteil_avg`, `tempo_avg`, `laengster_monolog`). Cache-Spalten bleiben (Performance unverändert), FK-Struktur unangetastet (kein Rename/Zombie von conversation_logs).

**FOLD 23.06. (am Code verifiziert) — `kb_*` + `skript_abdeckung` RAUS aus dem Cutover:** `compute_aggregates(events, speech_stats)` hat WEDER Kaufbereitschafts- NOCH Skript-Daten als Eingang → die Engine kann `kb_start/end/min/max` + `skript_abdeckung` strukturell nicht herleiten. Diese Felder BLEIBEN beim Live-Schreiber (app_routes.py:341-374) — NICHT entfernen, NICHT der Engine zuweisen. Der Cutover gilt NUR für die fünf herleitbaren Feld-Gruppen oben. (Design-Brief Abschnitt 3: „Kaufbereitschaft + Skript bleiben beim alten Schreiber".)

Purpose: §0.1 Single-Source vollendet — eine Engine, ein Schreiber je Feld, kein Parallel-System als Dauerlösung. Im selben Phasen-Lauf wie die Schatten-Welle abgeschlossen (kein permanentes Patchwork). RISKANTE Welle (EIN-Schreiber-Cutover am Rückgrat) → Test-Anruf-DATEN-FLUSS Pflicht.
Output: alte Aggregat-Schreiber raus, Engine = einziger Schreiber je Feld, grep-Beleg, Test-Anruf.

NUR ausführen wenn Plan 06 Schatten sauber war. FK/Struktur NICHT anfassen (Rückgrat bleibt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/` hardcoden. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen.

**Verify = Production only:** RISKANTE Welle (RESEARCH §I) → Test-Anruf-DATEN-FLUSS-Beweis Pflicht (Dashboard/Coaching/Auswertung rendern korrekt aus Engine-geschriebenen Aggregaten + grep EIN Schreiber je Feld). `bash deploy.sh production` + `inspect.sh`. KEIN lokales pytest.

**autonomous:false** — Human-Verify (Test-Anruf: Screens rendern aus Engine-Aggregaten + FK-Struktur-Diff = nur additiv).

**Pre-Cutover-Gate:** Plan-06-SUMMARY lesen — Schatten-Diskrepanzen müssen erklärt/sauber sein. Wenn unerklärte Diskrepanz → STOP, erst Engine-Berechnung fixen.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-SPEC.md
@.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-RESEARCH.md

<interfaces>
<!-- Alte Aggregat-Schreiber (zu ENTFERNEN, RESEARCH §A4). -->

ALT (routes/app_routes.py — ENTFERNEN/auf Engine umbiegen):
- :341-374 (CREATE in api_beenden): einwaende_gesamt/behandelt/fehlgeschlagen/ignoriert, vorwaende_erkannt,
    kb_start/end/min/max, redeanteil_avg, tempo_avg, laengster_monolog, skript_abdeckung
- :455-456 (UPDATE POLISH-38 Reconcile): einwaende_gesamt/behandelt

NEU (services/slow_lane.py, aus Plan 06): compute_aggregates(events, speech_stats) — schon gerechnet im Schatten;
hier wird das Ergebnis GESCHRIEBEN statt nur geloggt.

conversation_logs (inspect.sh §A1): Aggregat-Spalten + 6 FK-Kinder. FK/Struktur UNANGETASTET.
Cache-Spalten bleiben (Performance). redeanteil_avg/tempo_avg/skript_abdeckung INTEGER, laengster_monolog DOUBLE.
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `conversation_logs` (Aggregat-WRITE wandert von app_routes zu slow_lane/Engine) — Schema §A1, FK unangetastet
- `intent_event` (LESE, Aggregat-Quelle) — DEPENDS-ON-TAXO1
### inspect.sh-Beleg
`inspect.sh schema conversation_logs` vor + nach Cutover → Diff = KEIN Rename/Spalten-Drop (nur ggf. additiv). FK-Kinder (6) unverändert.
### Cross-Layer-Konsistenz-Tabelle
| Code-Variable | Pfad alt | Pfad neu | Schicht | Verifiziert? |
|---|---|---|---|---|
| einwaende_* etc. | app_routes:350-364/:455-456 schreibt | slow_lane/Engine schreibt (einziger) | DB conversation_logs | ⬜ Task 1+2 grep EIN Schreiber |
| redeanteil_avg/tempo_avg | app_routes:359-360 | Engine (int-Cast) | DB (INTEGER) | ⬜ Task 1 |

## Verbindungs-Karte (Punkt 22)
- **Heutige Schreiber bewiesen (RESEARCH §A4):** CREATE app_routes:341-374 + Reconcile :455-456. Für Live-Aggregate sind das die EINZIGEN Live-Schreiber (learning.py=Training, coaching_service=Read-only, migrate_polish_38=Einmal-Script). → nach Cutover: Engine einziger Schreiber.
- **grep-Ziel nach Cutover (NUR herleitbare Felder, FOLD 23.06.):** `grep -rn "\.einwaende_gesamt *=\|\.redeanteil_avg *=\|\.tempo_avg *=\|\.laengster_monolog *=\|\.vorwaende_erkannt *=" routes/ services/` == GENAU 1 Schreib-Stelle je Feld (Engine/slow_lane.py). **`kb_*` + `skript_abdeckung` bleiben in app_routes.py:341-374 (Live-Schreiber, NICHT engine-herleitbar) — für sie KEIN „EIN Schreiber=Engine"-Ziel.**

## 1.-4. Control-Flow + Race (Punkt 14)
**Insert/Entfernung:** app_routes.py:341-374 CREATE legt conversation_logs an (ConvLog-Save). Der Record muss weiter entstehen (FK-Anker für Kinder) — aber die AGGREGAT-FELDER werden nicht mehr hier gesetzt, sondern async von der Engine nachgetragen (UPDATE). Race: ConvLog wird bei Call-Ende angelegt (sync), Aggregate kommen async (Engine) — Edge: Screens die VOR dem Engine-Update lesen sehen NULL-Aggregate → "wird ausgewertet…" konsistent mit D-10. :455-456 Reconcile entfällt (Engine rechnet einwaende_* korrekt aus intent_event).
**Edge:** ConvLog-CREATE darf NICHT die Aggregat-Spalten mit Alt-Werten füllen (sonst zwei Schreiber). Entweder NULL lassen (Engine trägt nach) oder weglassen.

<tasks>

<task type="auto">
  <name>Task 1: Engine schreibt alle conversation_logs-Aggregate (einziger Schreiber, slow_lane.py)</name>
  <files>services/slow_lane.py, tests/test_aggregat_cutover.py</files>
  <read_first>
    - .planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-06-SUMMARY.md (Schatten sauber? Diskrepanzen erklärt?)
    - services/slow_lane.py (Schatten-Berechnung Plan 06) + services/rubric_engine.py compute_aggregates
    - ssh ... 'bash scripts/inspect.sh schema conversation_logs' (Aggregat-Typen)
  </read_first>
  <action>
    Pre-Gate: Plan-06-SUMMARY prüfen — Schatten-Diskrepanzen erklärt/sauber. Wenn unerklärt → STOP.
    Im Call-Ende-Merge (slow_lane.py): das compute_aggregates-Ergebnis (bisher nur geloggt, Plan 06) jetzt SCHREIBEN — **NUR per `UPDATE conversation_logs SET ... WHERE conversation_log_id = <id>`, NIE INSERT (FOLD 23.06., Gemini):** die conversation_logs-Zeile wird vom sync-CREATE in api_beenden angelegt (+ ankert die 6 FK-Kinder) — der async-Engine-Merge trägt NUR die herleitbaren Aggregat-Felder per UPDATE nach. **Findet das UPDATE keine Zeile (rowcount==0) → loggen (`[SLOW] aggregate UPDATE: conversation_log <id> not found`), KEIN Crash, KEIN INSERT** (ein INSERT würde eine zweite/verwaiste Zeile ohne FK-Kinder erzeugen). Ordering ist sicher: api_beenden-CREATE läuft IMMER vor dem async-Merge (Call-Ende-Signal kommt aus genau diesem Pfad). Gesetzte Felder — einwaende_gesamt/behandelt/fehlgeschlagen/ignoriert, vorwaende_erkannt, redeanteil_avg=int(...), tempo_avg=int(...), laengster_monolog=float(...) (Typ-Casts exakt). Idempotent (keyed conversation_log_id). FK/Struktur NICHT anfassen.
    **NICHT schreiben (FOLD 23.06.): `kb_start/end/min/max` + `skript_abdeckung`** — die Engine hat sie nicht (compute_aggregates gibt sie nicht zurück). Sie bleiben beim Live-Schreiber (app_routes.py:341-374). Das UPDATE darf diese Spalten NICHT anfassen (sonst überschreibt es die Live-Werte mit NULL/Müll).
    Entferne das [SHADOW]-Logging (oder belasse es als optionalen Debug-Log hinter ENV-Flag).
    Tests: compute_aggregates-Ergebnis → UPDATE-Assertion via Mock-Session (richtige Felder, richtige Typen); Test belegt, dass das UPDATE kb_*/skript_abdeckung NICHT in der SET-Liste hat.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_aggregat_cutover.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - slow_lane.py schreibt die herleitbaren Aggregat-Felder per **UPDATE WHERE conversation_log_id** (NIE INSERT — `grep -nE "INSERT INTO conversation_logs|ConversationLog\(" services/slow_lane.py` == 0), Typ-konsistent (int/double)
    - UPDATE-rowcount==0 → Log, kein Crash, kein INSERT (Test: Merge auf nicht-existente conversation_log_id → loggt, wirft nicht)
    - idempotent (keyed conversation_log_id)
    - Plan-06-Schatten-Sauberkeit als Pre-Gate dokumentiert
  </acceptance_criteria>
  <done>Engine schreibt alle Aggregate (Typ-konsistent, idempotent).</done>
</task>

<task type="auto">
  <name>Task 2: Alte Aggregat-Schreiber entfernen (app_routes.py :341-374 CREATE-Werte + :455-456 Reconcile)</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py:341-374 (CREATE) + :455-456 (POLISH-38 Reconcile)
    - Task 1 (Engine schreibt jetzt) + .planning/.../RESEARCH.md §A4
  </read_first>
  <action>
    1. :341-374: der ConvLog-Record muss weiter angelegt werden (FK-Anker für 6 Kinder). NUR die HERLEITBAREN Aggregat-Spalten NICHT mehr hier setzen (Engine trägt sie async nach). Entweder NULL lassen oder die Zuweisungen entfernen — KEINE Alt-Werte (sonst zwei Schreiber). **Raus aus der CREATE-Zuweisung NUR: einwaende_* (gesamt/behandelt/fehlgeschlagen/ignoriert), vorwaende_erkannt, redeanteil_avg, tempo_avg, laengster_monolog.**
    **HART (FOLD 23.06.): `kb_start/end/min/max` + `skript_abdeckung` BLEIBEN in der CREATE-Zuweisung (:355-358 + :364) — NICHT entfernen.** Sie sind nicht engine-herleitbar; app_routes.py bleibt ihr einziger Schreiber. (Sonst werden sie blank.)
    2. :455-456 (POLISH-38 Reconcile): entfernen (Engine rechnet einwaende_* korrekt aus intent_event, kein Reconcile mehr nötig).
    Punkt-14: ConvLog-CREATE-Control-Flow beachten — Record + FK-Kinder + kb_*/skript_abdeckung entstehen weiter live; nur die herleitbaren Aggregat-Felder kommen async von der Engine.
    KEINE FK/Struktur-Änderung an conversation_logs.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "redeanteil_avg\|einwaende_gesamt\|tempo_avg\|skript_abdeckung\|laengster_monolog"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\.einwaende_gesamt *=\|\.einwaende_behandelt *=\|\.einwaende_fehlgeschlagen *=\|\.einwaende_ignoriert *=\|\.vorwaende_erkannt *=\|\.redeanteil_avg *=\|\.tempo_avg *=\|\.laengster_monolog *=" routes/ services/` == GENAU 1 Schreib-Stelle je Feld (nur slow_lane.py/Engine)
    - **FOLD 23.06.: `grep -rn "\.kb_start *=\|\.kb_end *=\|\.kb_min *=\|\.kb_max *=\|\.skript_abdeckung *=" routes/ services/` zeigt die Schreib-Stelle WEITER in app_routes.py:341-374 (Live-Schreiber bleibt) — diese Felder sind NICHT zur Engine gewandert, kein „EIN Schreiber=Engine" für sie**
    - app_routes.py:341-374 setzt KEINE herleitbaren Aggregat-Werte mehr, ABER kb_*/skript_abdeckung weiter (CREATE legt Record + FK + kb_*/skript an); :455-456 Reconcile entfernt
    - ConvLog-Record + 6 FK-Kinder entstehen weiter (Test-Anruf belegt)
  </acceptance_criteria>
  <done>Alte Schreiber raus, Engine einziger Schreiber je Feld.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Engine = einziger Schreiber aller conversation_logs-Aggregate; alte Schreiber raus; FK/Struktur unangetastet.</what-built>
  <how-to-verify>
    Nach `bash deploy.sh production`:
    1. **Test-Anruf** (Cold-Call mit ≥1 Einwand) als Test-User → Call beenden. Kurz danach (async Engine) → Dashboard + Coaching/Session-Detail + Auswertung rendern die Einwand-Zähler/Redeanteil/Tempo korrekt aus den ENGINE-geschriebenen Aggregaten (DATEN-FLUSS, nicht nur "gerechnet").
       Claudian: `inspect.sh sample conversation_logs` zeigt die herleitbaren Aggregat-Spalten beim Test-Call von der Engine gesetzt (korrekte Werte, Tor-1-gefiltert).
    1b. **kb_*/skript bleiben live (FOLD 23.06.):** `inspect.sh sample conversation_logs` zeigt für den Test-Call kb_start/end/min/max + skript_abdeckung WEITER gefüllt (vom Live-Schreiber) — NICHT blank/NULL geworden durch den Cutover.
    1c. **Dauer-Korrektheit (FOLD 23.06., Gemini-Blindfleck):** ein Test-Call, bei dem am Ende bewusst lange geschwiegen wird ODER das Outcome-Fenster erst spät bestätigt wird → die angezeigte `dauer_sek`/Call-Länge entspricht der echten Gesprächsdauer (am `/api/beenden` festgehalten), NICHT der Zeit bis zum async-Merge. Claudian belegt: Dauer plausibel, nicht durch Schweigen/Wartezeit aufgebläht.
    2. **FK/Struktur-Check:** `inspect.sh schema conversation_logs` vor/nach = nur additiv, kein Rename/Drop; 6 FK-Kinder unverändert.
    3. **grep EIN Schreiber je Feld:** Claudian belegt GENAU 1 Schreib-Stelle je HERLEITBAREM Aggregat-Feld (slow_lane/Engine); kb_*/skript_abdeckung weiter NUR in app_routes.py.
  </how-to-verify>
  <resume-signal>Type "approved" oder beschreibe welches Aggregat/Screen abweicht.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Engine → conversation_logs (Aggregat-WRITE) | einziger Schreiber je Feld |
| async Aggregat-Nachtrag vs Screen-LESE | Screen liest evtl. vor Engine-Update |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO2-07-01 | T (Two-writer) | alter + neuer Schreiber gleichzeitig | mitigate | alte Schreiber entfernt (grep EIN Schreiber je Feld); CREATE setzt keine Aggregat-Werte mehr. |
| T-TAXO2-07-02 | D (Stale/empty) | Screen liest Aggregat vor Engine-Update | mitigate | async-Edge wie D-10: NULL-Aggregat → "wird ausgewertet…"; konsistent mit Plan 04 NULL-Edge. |
| T-TAXO2-07-03 | I (FK-Bruch) | Cutover ändert versehentlich FK-Struktur | mitigate | FK/Struktur NICHT angefasst; inspect.sh schema-Diff = nur additiv (Human-Verify). |
| T-TAXO2-07-04 | I (unerklärte Drift) | Engine-Wert weicht still von heute ab | mitigate | Pre-Gate: Plan-06-Schatten-Diskrepanzen erklärt VOR Cutover; sonst STOP. |
</threat_model>

<verification>
- Test-Anruf-DATEN-FLUSS (riskante Welle): Screens rendern aus Engine-Aggregaten.
- grep: GENAU EIN Schreiber je Aggregat-Feld (alte weg).
- conversation_logs FK/Struktur unverändert (inspect.sh schema-Diff additiv).
- Pre-Gate: Schatten (Plan 06) sauber.
</verification>

<success_criteria>
- Engine = einziger Schreiber aller conversation_logs-Aggregate (§0.1).
- Schatten→Cutover im selben Phasen-Lauf abgeschlossen (kein Patchwork).
- Rückgrat strukturell unangetastet (FK/Spalten).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-07-SUMMARY.md` (inkl. grep-EIN-Schreiber-je-Feld-Beleg, Test-Anruf-conversation_logs-Sample, FK-Struktur-Diff-Bestätigung, Pre-Gate-Schatten-Sauberkeits-Notiz).
</output>
