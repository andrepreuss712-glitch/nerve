---
phase: 08.23.2.AUTH-EMAIL-VERIFY
plan: "02"
subsystem: auth-email-verify
tags: [auth, email-verify, migration, orm-default, schild, test-fixture, wave-3, no-op]
dependency_graph:
  requires: [08.23.2.AUTH-EMAIL-VERIFY-01, 08.23.2.AUTH-EMAIL-VERIFY-03]
  provides: [db-default-false, orm-default-false, email-confirmed-schild, test-default-fixture]
  affects: [alembic/versions/0033_email_confirmed_default_false.py, database/models.py, tests/conftest.py]
tech_stack:
  added: []
  patterns: [ddl-only-default-flip-no-backfill, before_insert-test-default-listener, punkt23-schild]
key_files:
  created:
    - alembic/versions/0033_email_confirmed_default_false.py
  modified:
    - database/models.py
    - tests/conftest.py
decisions:
  - "Migration 0033: ALTER COLUMN server_default=false (DDL only, KEIN Backfill — 0 NULL-Rows, 3 Konten=true bleiben)"
  - "models.py ORM-Default True→False; nullable=True bleibt (kein NOT-NULL, Bestand nicht angefasst)"
  - "Schild (Punkt 23) in Migration + models.py identisch: deckt alle Schreiber (oauth/auth/seed/DB-Default) + Leser (Gate)"
  - "conftest before_insert-Listener (Test-Scope, is None-Guard) defaultet test-gebaute User auf True — haelt die 8 @login_required-Test-Dateien nach dem Default-Flip gruen (Finding 2)"
  - "Base-Seed User(id=1) zusaetzlich explizit email_confirmed=True (belt-and-suspenders)"
  - "Flip ist ein garantierter No-Op (Wave 3): Plan 03 setzt jeden Anlage-Pfad explizit → Default nie wirksam"
  - "Migration manuell auf Prod als postgres gefahren (Deploy-Order-Checkpoint, autonomous:false)"
metrics:
  completed: "2026-07-10"
  tasks_completed: 4
  files_changed: 3
---

# Phase 08.23.2.AUTH-EMAIL-VERIFY Plan 02: DB-/ORM-Default-Flip (belt-and-suspenders) Summary

**One-liner:** email_confirmed DB-Default + ORM-Default auf False geflippt (hinterste Verteidigungslinie unter Gate + expliziten Settern), Schild aktualisiert, zentraler Test-Helfer haelt das Deploy-Gate gruen — reiner No-Op, weil jeder Creator (Plan 03) schon explizit setzt.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Alembic-Migration 0033 (DB-Default False + Schild) | a2eae8c | alembic/versions/0033_email_confirmed_default_false.py (neu) |
| 2 | models.py ORM-Default False + Schild | c836382 | database/models.py |
| 3 | Zentraler Test-Helfer (Finding 2) | 0802176 | tests/conftest.py |
| 4 (checkpoint) | Prod-Migration 0033 manuell (postgres@) | — (DDL auf Prod) | — |

## Was gebaut wurde

- **Migration 0033:** `op.alter_column('users','email_confirmed', server_default=sa.false())` + `COMMENT ON COLUMN` (Schild). Down_revision 0032. KEIN Backfill-UPDATE (dokumentiert warum: 3 true-Konten dürfen nicht geflippt werden, 0 NULL-Rows). downgrade() setzt Default+Schild zurück.
- **models.py:** ORM `default=True` → `default=False`, `nullable=True` unverändert; `comment=` auf den aktuellen Schild-Text (identisch zur Migration).
- **conftest.py (Finding 2):** `@event.listens_for(User, "before_insert")` mit `is None`-Guard → test-gebaute User ohne expliziten Wert bekommen True. Deckt alle ~8 `@login_required`-Test-Dateien + Base-Seed in EINER Stelle (Punkt 27). Base-Seed `User(id=1)` zusätzlich explizit True.

## Verifikation (Verify=Production)

- **Grün-Gate:** 941 passed, Restart, gepusht (`0802176`). Finding 2 bestätigt — die conftest-Fixture hält die Tests nach dem ORM-Default-Flip grün (ohne sie wären ~8 Dateien rot).
- **Prod-Migration:** 0033 manuell als `postgres` gefahren (alembic 0032→0033). `email_confirmed` DB-Default = `false`; die 3 Bestandskonten unverändert `true` (kein Backfill); 0 NULL-Rows.
- **No-Op-Beleg:** Register-Flow unverändert — `api_register` setzt False explizit, unabhängig vom Default. App healthy.

## Self-Check: PASSED

- Alle Task-1/2/3-Acceptance-Kriterien grep-belegt grün.
- Migration DDL-only, kein Backfill (grep: `UPDATE users SET email_confirmed` nur in Kommentaren).
- Schild aktuell + deckt alle Leser/Schreiber (Punkt 23 Aktualitäts-Pflicht).
- Prod verifiziert: DB-Default=false, alembic=0033, Bestand=true, 0 NULL.

## Dreifach-Absicherung (Phasen-Abschluss)

email_confirmed ist jetzt dreifach fail-closed abgesichert:
1. **Gate (Plan 01):** `login_required` gatet NULL/False/unset — nur True passiert.
2. **Explizite Setter (Plan 03):** jeder Anlage-Pfad setzt email_confirmed selbst.
3. **DB-/ORM-Default False (Plan 02):** ein vergessener künftiger Creator landet False (gegatet), nicht NULL (fail-open).
