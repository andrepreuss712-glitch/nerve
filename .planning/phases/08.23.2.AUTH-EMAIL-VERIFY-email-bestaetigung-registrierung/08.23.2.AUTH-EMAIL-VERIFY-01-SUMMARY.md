---
phase: 08.23.2.AUTH-EMAIL-VERIFY
plan: "01"
subsystem: auth-email-verify
tags: [auth, security-gate, fail-closed, email-confirmed, erst-rot, wave-1]
dependency_graph:
  requires: []
  provides: [fail-closed-gate, email-verify-guard]
  affects: [routes/auth.py, tests/test_email_verify_gate.py]
tech_stack:
  added: []
  patterns: [fail-closed-is-not-True, erst-rot-behavior-guard, session_transaction-login, cleanup_rows-teardown]
key_files:
  created:
    - tests/test_email_verify_gate.py
  modified:
    - routes/auth.py
decisions:
  - "Gate von `getattr(g.user,'email_confirmed',True) is False` auf `is not True` — NULL/unset/False gaten, nur True passiert (D-01b)"
  - "getattr-Default bleibt True (Fallback fuer den seltenen Fall dass g.user das Attribut nicht traegt); entscheidend ist dass NULL-Werte jetzt gaten"
  - "Ausnahme-Pfade confirm_email/logout unveraendert — kein Redirect-Loop"
  - "Test A (NULL gatet) als ERST-ROT-Kern: None ist NICHT `is False` → rutschte durch das alte Gate; beweist der Fix beisst"
  - "Test D exerziert den Gate-Ausnahmezweig NICHT (keine @login_required-Route mit /auth/confirm_email-Praefix) — FINDING-4-Kommentar verankert; beweist nur Nicht-Sperre"
  - "VALID Runtime-Verhalten (Request gegen echte PG nerve_test), KEIN Source-Presence"
metrics:
  completed: "2026-07-09"
  tasks_completed: 2
  files_changed: 2
---

# Phase 08.23.2.AUTH-EMAIL-VERIFY Plan 01: Fail-Closed login_required-Gate Summary

**One-liner:** Fail-Open-Leck im login_required-Gate geschlossen — Gate liest jetzt `email_confirmed is not True` (NULL/unset/False gaten, nur True passiert), abgesichert durch einen ERST-ROT-Verhaltens-Guard.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 (ERST-ROT) | Fail-Closed-Verhaltens-Guard (NULL/False/True/Ausnahme) | 1e806cb | tests/test_email_verify_gate.py (neu) |
| 2 | Gate fail-closed umstellen (`is not True`) + D-01b-Naht-Kommentar | de0dabb | routes/auth.py |

## Was gebaut wurde

- `routes/auth.py:81` — Gate-Ausdruck von `is False` auf `is not True` umgestellt. Ein User mit `email_confirmed IS NULL` (Python `None`) wird jetzt gegatet; vorher passierte er, weil `None is False == False`. Wurzel des freien Register-Zugangs (D-01, GZ0-korrigiert).
- `tests/test_email_verify_gate.py` — 4 Verhaltens-Tests gegen echte PG nerve_test:
  - `test_null_email_confirmed_is_gated` (ERST-ROT-Kern): NULL → 302 auf `/auth/confirm-email-pending`.
  - `test_false_email_confirmed_is_gated`: False → 302 Warteseite.
  - `test_true_email_confirmed_passes`: True → KEIN Redirect auf die Warteseite.
  - `test_confirm_email_route_not_gated_for_null_user` (FINDING 4): NULL-User auf `/auth/confirm_email` → nicht auf die Warteseite gesperrt.
  - NULL via raw `UPDATE users SET email_confirmed = NULL` (ORM-Default greift sonst); Rows via `cleanup_rows` im POST-yield-Teardown; uuid-suffixed Emails.

## Verifikation (Verify=Production, deploy.sh-Pytest-Gate)

- **Pre-Deploy-Live-Check (read-only, Prod-`nerve`):** `users WHERE email_confirmed IS NULL` = **0** ✓; `alembic_version` = **0032** ✓ → kein Bestandskonto wird ueberrascht-gegatet.
- **Zwei-Tore-Beweis:**
  - Erst-Rot: genau `test_null_email_confirmed_is_gated` ROT gegen das alte `is False`-Gate (935 andere gruen, kein Restart, Prod unberuehrt) → Test A beisst.
  - Gruen-Deploy: **936 passed**, Restart, live git_head `de0dabb`, gepusht (`944630f..de0dabb`).
- **Live healthy:** login 302, journalctl seit Restart kein Fehler, keine falschen `confirm-email-pending`-Redirects.

## Self-Check: PASSED

- Gate-Ausdruck `email_confirmed', True) is not True` grep-belegt; altes `is False` = 0 Treffer im Gate.
- Ausnahme-Pfade confirm_email/logout intakt (auth.py:82-83).
- 4 Guard-Tests gruen im Deploy-Gate; ERST-ROT-Disziplin bewiesen (rot→gruen).
- Kein Source-Presence-Test.

## Naht fuer Wave 2 (Plan 03)

Das Gate ist jetzt fail-closed scharf. Ab hier gatet es JEDEN NULL/False-Lander — daher muessen ALLE Anlage-Pfade (Form-Register, Google-/Microsoft-OAuth, Invite-Accept) `email_confirmed` explizit setzen (Plan 03), BEVOR der Default-Flip (Plan 02) zuletzt als No-Op laeuft.
