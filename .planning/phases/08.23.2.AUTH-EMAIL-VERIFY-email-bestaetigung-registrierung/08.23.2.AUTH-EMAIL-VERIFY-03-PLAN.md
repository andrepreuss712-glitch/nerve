---
phase: 08.23.2.AUTH-EMAIL-VERIFY
plan: 03
type: execute
wave: 2
depends_on: ["08.23.2.AUTH-EMAIL-VERIFY-01", "08.23.2.AUTH-EMAIL-VERIFY-02"]
files_modified:
  - routes/auth.py
  - routes/oauth.py
  - app.py
  - scripts/seed_test_user.py
  - services/email_service.py
  - templates/confirm_email_pending.html
  - tests/test_email_verify_creation_paths.py
autonomous: false
requirements: [D-02, D-03, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
complexity: "🔴 komplex (Auth-Naht + Anlage-Pfad-Inventur + Live-UAT — Cross-AI Pflicht)"

must_haves:
  truths:
    - "Ein Form-Registrierter wird als email_confirmed=False angelegt und auf die Warteseite geleitet (nicht /onboarding)"
    - "Bei Register wird NUR send_confirmation_email versendet (kein send_welcome an unbestätigte Adresse)"
    - "Nach Klick auf den Confirm-Link wird email_confirmed=True, send_welcome gesendet und zur AUTH-2-Weiche gedockt"
    - "Kein User-Anlage-Pfad hinterlässt email_confirmed IS NULL"
    - "Die Bestätigungs-Mail und die Warteseite sind US-Englisch"
  artifacts:
    - path: "routes/auth.py"
      provides: "api_register email_confirmed=False + Warteseite-Landing + welcome-move in confirm_email"
      contains: "confirm_email_pending"
    - path: "services/email_service.py"
      provides: "EN send_confirmation_email"
      contains: "confirm"
    - path: "templates/confirm_email_pending.html"
      provides: "EN Warteseite"
      contains: "email"
    - path: "tests/test_email_verify_creation_paths.py"
      provides: "Guard: kein Creator lässt email_confirmed NULL"
      contains: "def test_"
  key_links:
    - from: "api_register"
      to: "url_for('auth.confirm_email_pending')"
      via: "return next für Unbestätigte"
      pattern: "confirm_email_pending"
    - from: "confirm_email (email_confirmed=True)"
      to: "send_welcome + session welcome_trial"
      via: "welcome-timing move"
      pattern: "send_welcome"
---

<objective>
Verdrahtet die Form-Registrierung (`api_register`) an den bestehenden (~80%, H-18)
E-Mail-Bestätigungs-Flow und schließt die Anlage-Pfad-Inventur ab:

1. **D-02:** `email_confirmed=False` explizit in `api_register` (NICHT im geteilten
   `_create_org_and_user` — OAuth-Naht Punkt 14, Google muss True bleiben).
2. **D-03 (Verbindungs-Karte, Punkt 22):** jeder User-Creation-Pfad setzt email_confirmed
   explizit, grep-belegt — kein Pfad lässt NULL. Invite-Accept → True (Mail-Besitz via
   Invite bewiesen); OAuth Google→True/Microsoft→False (verifizieren, Bestand); seed/admin→explizit.
3. **D-11 Register-Sequenz:** create→commit→email_confirmed=False→_login_user→send_confirmation_email
   →return {ok, next: '/auth/confirm-email-pending'} (D-09, überschreibt AUTH-2-Onboarding-Weiche
   für Unbestätigte). Auto-Login bleibt (D-10). KEIN welcome_trial hier.
4. **D-12/D-13:** send_welcome + session['welcome_trial'] wandern in confirm_email (nach
   email_confirmed=True). welcome_trial best-effort same-browser (D-14, Punkt 27 — nicht über-engineeren).
5. **D-15:** confirm_email redirect→/login docked an AUTH-2 S1 → post_login_destination → /onboarding.
   Bewusst so lassen + im Live-Test verifizieren.
6. **D-08:** Bestätigungs-Mail + Warteseite auf US-Englisch (RESEND_BASE_URL ungesetzt lassen).
7. **D-16:** End-to-End-Live-Test (Flow nie live gefeuert) — MANDATORY human checkpoint.

Purpose: das ~80%-Fundament an den Form-Register-Pfad anschließen (Punkt 27, kein Neubau).
Output: Naht-Edits in auth.py + Creation-Path-Setter + EN-Texte + Anlage-Pfad-Guard + Live-UAT.

★ Depends_on Plan 01 (Gate muss fail-closed sein, sonst gatet email_confirmed=False nicht) +
Plan 02 (DB/ORM-Default False konsistent). File-Overlap mit Plan 01 auf routes/auth.py → Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

★ Multi-Segment-Phase-ID-Gotcha: phase_dir HART =
`.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung`.
SUMMARY = `08.23.2.AUTH-EMAIL-VERIFY-03-SUMMARY.md`. KEIN gsd-tools.

★ HART: Kein Local-Dev. Verify=Production. Automatisierte Tests im deploy.sh-Gate auf
nerve_test. Live-UAT (echte Confirm-Mail klicken) = human checkpoint (autonomous:false).
★ url_for-Verifikation (routes/CLAUDE.md): auth.confirm_email_pending / auth.confirm_email
existieren (Blueprint 'auth', grep-belegt: def confirm_email_pending auth.py:358, def confirm_email :365).
</execution_context>

<context>
@.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-CONTEXT.md
@.planning/phases/08.23.2.AUTH-2-onboarding-connector/08.23.2.AUTH-2-CONTEXT.md
@salesnerve/CLAUDE.md
@routes/CLAUDE.md

<interfaces>
<!-- Alle per INHALT re-ankern (grep frisch vor jedem Edit, André-Direktive). Anker 2026-07-09: -->

<!-- api_register (routes/auth.py:246), heutige Sequenz Body ~:262-287: -->
```python
# _create_org_and_user(...) -> db.commit() -> _login_user(db, user)
# -> session['welcome_trial']=True -> send_welcome(...) -> db.refresh(user)
# -> dest = post_login_destination(user) -> return {'ok': True, 'next': dest}
```

<!-- _create_org_and_user (auth.py:182-212) — GETEILT, email_confirmed NICHT hier setzen (D-02/Punkt 14). -->
<!-- User(...)-Konstruktor darin setzt email_confirmed NICHT -> ORM-Default (nach Plan 02 = False). -->

<!-- OAuth-Neuanlage (oauth.py:118-160): _create_org_and_user -> if provider=='microsoft': new_user.email_confirmed=False (oauth.py:133). Google setzt NICHTS -> ORM-Default. -->
<!--   ★ ACHTUNG Plan-02-Interaktion: nach Plan 02 ist ORM-Default False -> Google-User bekaeme jetzt False statt True! -->
<!--   D-07: Google MUSS True bleiben. Also Google-Pfad in oauth.py explizit auf True setzen (siehe Task 2). -->

<!-- Invite-register (auth.py:291-329): User(org_id, email, passwort_hash, rolle='member') bei :316, email_confirmed NICHT gesetzt -> ORM-Default (jetzt False!). D-03: Invite beweist Mail-Besitz -> explizit True. -->

<!-- _seed owner (app.py:1643): User(...) email_confirmed NICHT gesetzt -> ORM-Default. Laeuft nur bei User.count()==0 (nie Prod). D-03: explizit True (Seed-Owner soll rein). -->

<!-- seed_test_user.py:33: User(...) email_confirmed NICHT gesetzt -> ORM-Default. D-03: explizit True (Test-User soll App nutzen koennen). -->

<!-- confirm_email (auth.py:365-387): setzt user.email_confirmed=True :375, redirect url_for('auth.login') :387. -->
<!-- resend_confirm (auth.py:394): @limiter 3/10min, send_confirmation_email. -->
<!-- Gate (auth.py:79, NACH Plan 01): email_confirmed is not True -> redirect confirm_email_pending. -->

<!-- send_confirmation_email (email_service.py:55-69) heute DEUTSCH. send_welcome (:42-52) DEUTSCH (bleibt DE — nur confirmation + Warteseite EN, D-08). -->
<!-- confirm_email_pending.html — DEUTSCH, resend-Button fetch('/auth/resend-confirm'). -->

<!-- Register-Rate-Limit auth.py:245 @limiter.limit("5 per minute") — UNVERAENDERT (D-06). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api_register-Naht (D-02/D-09/D-10/D-11) + welcome-timing-move (D-12/D-13) + Creation-Path-Setter (D-03) + Anlage-Pfad-Guard</name>
  <read_first>
    - routes/auth.py api_register (per INHALT ankern JETZT: `grep -n "def api_register" routes/auth.py` ~:246, Body ~:262-287)
    - routes/auth.py _create_org_and_user :182-212 (GETEILT — NICHT anfassen fuer email_confirmed)
    - routes/auth.py confirm_email :365-387 (welcome-move-Ziel, nach email_confirmed=True :375)
    - routes/auth.py register-invite :291-329 (User(...) :316 — Invite-Pfad → True)
    - app.py _seed owner (`grep -n "owner = User" app.py` ~:1643)
    - scripts/seed_test_user.py :33 (Test-User → True)
    - tests/conftest.py:792-838 (client-Fixture PG), :240-310 (cleanup_rows)
  </read_first>
  <behavior>
    VALID Runtime-Verhalten (API-Response + DB-Read gegen echte PG nerve_test, KEIN Source-Presence):
    - Test A (Register→False+Warteseite): POST /api/register mit gueltigen Feldern → response.json['next']
      == '/auth/confirm-email-pending' (D-09); DB-Read des neuen Users: email_confirmed IS False (D-02, nicht NULL).
    - Test B (Register sendet KEIN welcome): send_welcome monkeypatched/gespäht → bei Register NICHT
      aufgerufen; send_confirmation_email AUFGERUFEN (D-12).
    - Test C (confirm_email→True+welcome): confirm_email mit gueltigem Token → DB email_confirmed True;
      send_welcome AUFGERUFEN (D-13); (welcome_trial-Session-Flag best-effort, nicht hart asserten — D-14).
    - Test D (Anlage-Pfad-Inventur-Guard, D-03): fuer JEDEN Creator ein Test dass email_confirmed
      NICHT NULL ist nach Anlage — api_register→False, Invite-register→True, seed_test_user→True.
      (OAuth-Pfade in Task 2 separat.) real-PG DB-Read, assert `IS NOT NULL` + korrekter Wert.
    Alle committenden Tests raeumen Rows via cleanup_rows (Test-Cleanup-Regel), uuid-suffixed Emails.
  </behavior>
  <action>
    Datei tests/test_email_verify_creation_paths.py (Test A–D oben) — ERST-ROT wo sinnvoll (Test A
    'next' ist heute post_login_destination, nicht Warteseite → rot vor dem Fix). Dann die Edits:

    (a) routes/auth.py api_register (D-11-Sequenz): NACH `db.commit()` (Org+User angelegt) explizit
        `user.email_confirmed = False` setzen + `db.commit()` (D-02 — NUR hier, NICHT in _create_org_and_user).
        `_login_user(db, user)` BEHALTEN (D-10). `session['welcome_trial'] = True` + den `send_welcome`-Block
        ENTFERNEN (wandern in confirm_email, D-13). STATTDESSEN send_confirmation_email versenden:
        Token wie in resend_confirm/oauth: `URLSafeTimedSerializer(SECRET_KEY, salt='nerve-email-confirm').dumps(user.email)`,
        `confirm_url = url_for('auth.confirm_email', token=..., _external=True)`, `send_confirmation_email(user.email, confirm_url, vorname)`
        in try/except (Muster wie oauth.py:150-160). Response: `return jsonify({'ok': True, 'next': url_for('auth.confirm_email_pending')})`
        (D-09 — ueberschreibt post_login_destination; db.refresh/dest-Berechnung entfaellt fuer den Unbestaetigt-Pfad).
    (b) routes/auth.py confirm_email (D-13): NACH `user.email_confirmed = True` + `db.commit()` (:375-376)
        `send_welcome(user.email, getattr(user,'vorname','') or '')` in try/except einfuegen +
        `session['welcome_trial'] = True` setzen (best-effort same-browser, D-14). Import send_welcome lokal
        (Muster bestehende lokale Imports). Redirect→/login (:387) UNVERAENDERT (D-15).
    (c) routes/auth.py register-invite (:316, D-03): dem `User(...)`-Konstruktor `email_confirmed=True`
        hinzufuegen — Invite-Link beweist Mail-Besitz; sonst wuerde das eingeladene Mitglied nach Plan 02
        (ORM-Default False) grundlos gegatet. Kommentar: "D-03: Invite beweist Mail-Besitz → True".
    (d) app.py _seed owner (:1643, D-03): `email_confirmed=True` zum `User(...)` — Seed-Owner soll rein
        (laeuft nur bei count==0, nie Prod). Kommentar "D-03: Seed-Owner explizit bestaetigt".
    (e) scripts/seed_test_user.py (:33, D-03): `email_confirmed=True` zum `User(...)` — Test-User soll
        App nutzen. Kommentar "D-03: Test-User explizit bestaetigt".
    ★ _create_org_and_user (:200 User(...)) UNANGETASTET lassen (D-02/Punkt 14 — OAuth teilt es).
    Committe test rot zuerst, dann Fix: `feat(08.23.2.AUTH-EMAIL-VERIFY-03): api_register→confirm-flow + welcome-move + creation-path setters (D-02/D-03/D-09/D-11/D-13)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: test_email_verify_creation_paths.py A–D gruen</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "user.email_confirmed = False" routes/auth.py` vorhanden im api_register-Body (D-02)
    - `grep -n "next.*confirm_email_pending\|confirm_email_pending.*next" routes/auth.py` — api_register returnt Warteseite (D-09)
    - `grep -n "send_confirmation_email" routes/auth.py` im api_register-Body vorhanden
    - api_register-Body enthaelt KEIN `send_welcome` mehr; `grep -n "send_welcome" routes/auth.py` steht NUR in confirm_email-Naehe (D-13)
    - `grep -n "email_confirmed=True" routes/auth.py` im register-invite User(...) vorhanden (D-03)
    - `grep -n "email_confirmed=True" app.py` im _seed owner vorhanden; `grep -n "email_confirmed=True" scripts/seed_test_user.py` vorhanden
    - `grep -n "email_confirmed" routes/auth.py` zeigt _create_org_and_user (:182-212) UNVERAENDERT ohne email_confirmed-Setz (D-02-Beleg)
    - KEIN Source-Presence-Test in test_email_verify_creation_paths.py
  </acceptance_criteria>
  <done>Register→False+Warteseite, welcome in confirm, alle Nicht-OAuth-Creator setzen explizit (kein NULL), _create_org_and_user untouched.</done>
</task>

<task type="auto">
  <name>Task 2: OAuth-Pfade verifizieren + Google-Default-Naht (D-03/D-07, Plan-02-Interaktion)</name>
  <read_first>
    - routes/oauth.py Neuanlage (`grep -n "_create_org_and_user\|email_confirmed" routes/oauth.py` — ~:118-160)
    - oauth.py:132-133 (Microsoft email_confirmed=False), :147 (Kommentar Google=verified)
    - database/models.py:135 (ORM-Default NACH Plan 02 = False — kritisch fuer Google)
  </read_first>
  <action>
    ★ Plan-02-Interaktion (kritisch, Punkt 22/29 — Halb-Migration-Falle): NACH Plan 02 ist der
    ORM-Default False. Der Google-OAuth-Pfad setzt email_confirmed heute NICHT explizit (verlaesst
    sich auf den alten Default True) → nach Plan 02 bekaeme Google-Neuanlage FALSE → Google-User
    wuerde faelschlich gegatet (verletzt D-07 "Google bleibt bestaetigt").
    FIX: In routes/oauth.py, im Neuanlage-Block (~:130-133), den bestehenden Microsoft-Zweig zu
    einem expliziten if/else erweitern:
    ```
    if provider == 'microsoft':
        new_user.email_confirmed = False
    else:  # google (+ kuenftige verified-Provider): Mail vom Provider verifiziert (D-07)
        new_user.email_confirmed = True
    ```
    (statt heute nur `if provider=='microsoft': ...=False`). Damit setzt JEDER OAuth-Pfad email_confirmed
    explizit → kein Verlass mehr auf den ORM-Default (D-03: kein Pfad laesst NULL, kein stiller Default-Drift).
    Kommentar: "D-03/D-07: Google explizit True (Provider-verified), nicht mehr auf ORM-Default verlassen (der ist ab AUTH-EMAIL-VERIFY False)."
    Microsoft-Confirmation-Versand (oauth.py:149-160) UNVERAENDERT (D-07 Zwang behalten).
    Committe: `fix(08.23.2.AUTH-EMAIL-VERIFY-03): oauth Google explizit email_confirmed=True — kein ORM-Default-Drift (D-03/D-07)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate gruen (falls OAuth-Neuanlage-Test existiert; sonst grep-Beleg)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "new_user.email_confirmed = True" routes/oauth.py` vorhanden (Google explizit)
    - `grep -n "new_user.email_confirmed = False" routes/oauth.py` vorhanden (Microsoft, Bestand)
    - `grep -n "else:" routes/oauth.py` im Neuanlage-Block (explizites if/else statt nur if)
    - `grep -n "D-07\|D-03" routes/oauth.py` Naht-Kommentar vorhanden
  </acceptance_criteria>
  <done>Jeder OAuth-Pfad setzt email_confirmed explizit (Google=True, Microsoft=False); kein Default-Drift.</done>
</task>

<task type="auto">
  <name>Task 3: EN-Übersetzung Bestätigungs-Mail + Warteseite (D-08)</name>
  <read_first>
    - services/email_service.py:55-69 (send_confirmation_email — heute DE)
    - services/email_service.py:1-11 (RESEND_BASE_URL — NICHT setzen, D-08)
    - templates/confirm_email_pending.html (heute DE, resend-Button)
  </read_first>
  <action>
    (a) services/email_service.py send_confirmation_email (:55-69): den HTML-Body + subject auf
        US-Englisch. Ton: US-B2B-SaaS-Register (Claude's Discretion, D-08). Beispiel:
        subject 'Confirm your email for NERVE'; Body: "Hi {name}, please confirm your email address
        for NERVE: [Confirm email] (link valid 24 hours). If you didn't sign up for NERVE, ignore this email. — André, Founder NERVE".
        Link-Text 'Confirm email'. Der `name = vorname or 'da'`-Fallback → 'there' (EN). send_welcome
        (:42-52) BLEIBT Deutsch (D-08 nennt nur confirmation + Warteseite). RESEND_BASE_URL-Logik
        (:6-11) UNVERAENDERT (ungesetzt = US-Standard).
    (b) templates/confirm_email_pending.html: sichtbaren Text EN. "Confirm your email" (h2);
        "Please confirm your email address. We've sent you a confirmation link." ; Button "Didn't get
        the link? [Resend]"; "Log out". Die fetch-Logik (/auth/resend-confirm, X-CSRFToken) + alert-Texte
        EN ("Email resent!" / "Error: " / "Failed to send. Please try again."). ASCII-Pflicht bleibt fuer
        Code-Identifier; sichtbarer EN-Text braucht keine Umlaute.
    Committe: `feat(08.23.2.AUTH-EMAIL-VERIFY-03): EN confirmation mail + waiting page (D-08)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate gruen (Text-Change, kein Test-Bruch)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ni "confirm your email\|Confirm email" services/email_service.py` vorhanden
    - `grep -ni "bitte bestätige\|E-Mail bestätigen" services/email_service.py` liefert 0 Treffer in send_confirmation_email (DE weg)
    - `grep -ni "confirm your email\|Resend\|Log out" templates/confirm_email_pending.html` vorhanden
    - `grep -ni "bestätige\|Erneut senden\|Abmelden" templates/confirm_email_pending.html` liefert 0 Treffer (DE weg)
    - `grep -n "RESEND_BASE_URL" services/email_service.py` unveraendert (nicht gesetzt)
  </acceptance_criteria>
  <done>Bestätigungs-Mail + Warteseite EN; send_welcome bleibt DE; RESEND_BASE_URL ungesetzt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-End Live-UAT (D-16) — MANDATORY (Flow nie live gefeuert)</name>
  <what-built>
    Form-Register ist an den Confirm-Flow verdrahtet (email_confirmed=False, Warteseite, EN-Mail),
    welcome wandert in confirm, Gate ist fail-closed (Plan 01), DB-Default False (Plan 02).
    Alles committet + auf Prod deployed (bash deploy.sh production; Pytest-Gate gruen vor Restart).
  </what-built>
  <how-to-verify>
    ★ Verify=Production (HART). Mit echter (routbarer, NICHT @nerve.local) Test-Adresse auf getnerve.app:
    1. **Register→Mail→Confirm→Zugang:** Landingpage-Formular ausfuellen + absenden → landet auf
       /auth/confirm-email-pending (EN Warteseite), NICHT /onboarding. Bestaetigungs-Mail (EN, RESEND
       live) im Postfach empfangen. Confirm-Link klicken → email_confirmed=True → via /login → AUTH-2-Weiche
       → /onboarding/ (D-15-Kette). welcome-Mail (DE) empfangen.
    2. **Unbestaetigt = gesperrt:** frisch registrieren, NICHT bestaetigen, versuchen /dashboard oder /live
       aufzurufen → Redirect auf die Warteseite (Gate greift). Kasse/Checkout unerreichbar (D-05).
    3. **Resend:** auf der Warteseite "Resend" klicken → neue Mail; 4. Klick in <10min → Rate-Limit (3/10min, D-06).
    4. **24h-Link:** (optional/dokumentiert) Link bleibt 24h gueltig (max_age=86400) — nicht live abwartbar,
       aber im Confirm-Code belegt.
    5. **MS-OAuth-Confirm-Pfad einmal:** mit Microsoft-Login neu registrieren → email_confirmed=False →
       Confirmation-Mail → Link → Zugang (H-18-Pfad, auch nie live gefeuert). Google-Login → SOFORT rein
       (email_confirmed=True, kein Confirm noetig, D-07).
    6. **Bestand unberuehrt:** die 3 Bestandskonten (email_confirmed=true) loggen weiter normal ein.
    inspect.sh read-only zur Belegkontrolle: `bash scripts/inspect.sh sample users 10` (neuer User False→True nach Confirm).
    André bestaetigt jeden Punkt oder beschreibt Abweichungen.
  </how-to-verify>
  <resume-signal>Type "uat-pass" wenn Register→Confirm→Onboarding-Kette + Sperre + Resend + MS-OAuth live gruen — oder beschreibe die Bugs.</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `users` (SCHREIBEN: email_confirmed in api_register=False, confirm_email=True, register-invite=True,
  seed=True, oauth Google=True/Microsoft=False; LESEN in confirm_email/resend). `organisations` (via _create_org_and_user, unveraendert).

### Beleg (verbatim Prod-`nerve`, Claudian 2026-07-09 — NICHT zusammenfassen)
```
users.email_confirmed (Prod-nerve, \d users / information_schema.columns, 2026-07-09):
  data_type   = boolean
  is_nullable = YES
  column_default = (leer / kein DB-Default)   ← Fail-Open-Wurzel: Insert ohne Wert → NULL
Real-Daten: id=1 true, id=2 true, id=3 true (alle 3 Bestandskonten bestätigt; 0 NULL-Rows aktuell).
models.py:135 = Column(Boolean, default=True, nullable=True)  ← ORM-Default True driftet zur Prod-Realität (kein DB-Default).
```
(★ Nach Plan 02 ist der DB-Default + ORM-Default False — dieser Plan setzt in JEDEM Creator explizit,
verlaesst sich also NICHT mehr auf den Default. Das ist die D-03-Absicherung.)

### Cross-Layer-Konsistenz-Tabelle (Verbindungs-Karte, Punkt 22 — jeder Creator × Wert × Beleg)
| Creator (Code-Pfad) | email_confirmed-Wert | Begründung | Schreib-Zeitpunkt / Leser-Voraus? | grep-Beleg |
|---|---|---|---|---|
| `api_register` (auth.py:246) | **False** (explizit, NEU) | Form-Register: Mail-Besitz unbewiesen → gaten bis Confirm (D-02) | Live bei Register, ein commit vor Response. Kein Voraus. | `grep "user.email_confirmed = False" routes/auth.py` |
| `_create_org_and_user` (auth.py:182, GETEILT) | NICHT gesetzt (ORM-Default False n. Plan 02) | Punkt 14: OAuth teilt es; api_register/OAuth setzen SELBST danach explizit | flush; danach Caller-Override | UNVERAENDERT (D-02-Beleg) |
| OAuth Google (oauth.py) | **True** (explizit, NEU Task 2) | Provider verifiziert Mail (D-07) — NICHT auf Default verlassen (Drift-Schutz) | live bei Neuanlage | `grep "new_user.email_confirmed = True" routes/oauth.py` |
| OAuth Microsoft (oauth.py:133) | **False** (Bestand) | H-18: MS verifiziert nicht → Confirm-Zwang (D-07) | live bei Neuanlage | `grep "new_user.email_confirmed = False" routes/oauth.py` |
| confirm_email (auth.py:375) | **True** (Bestand) | User hat Mail bewiesen (Link geklickt) | live bei Confirm | `grep "user.email_confirmed = True" routes/auth.py` |
| Invite-register (auth.py:316) | **True** (explizit, NEU) | Invite-Link beweist Mail-Besitz (D-03) — sonst grundlos gegatet | live bei Accept | `grep "email_confirmed=True" routes/auth.py` (invite) |
| _seed owner (app.py:1643) | **True** (explizit, NEU) | Seed-Owner soll rein (nur count==0, nie Prod, D-03) | Seed-Zeit | `grep "email_confirmed=True" app.py` |
| seed_test_user.py:33 | **True** (explizit, NEU) | Test-User soll App nutzen (D-03) | Seed-Zeit | `grep "email_confirmed=True" scripts/seed_test_user.py` |

**Ergebnis: KEIN Pfad lässt email_confirmed NULL** (D-03 erfüllt). Die NULL-vs-False-Subtilität (Plan 01)
ist doppelt abgesichert: Gate gatet NULL sowieso (Plan 01), Default ist False (Plan 02), jeder Creator
setzt explizit (dieser Plan).

### Async-Naht (Punkt 26)
Kein Leser laeuft einem Schreiber voraus: email_confirmed wird bei Anlage/Confirm synchron im selben
Request geschrieben; das Gate liest g.user live pro Request (db.get). Keine Batch-/BG-Thread-Quelle.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Anonymer Browser → POST /api/register | Untrusted: beliebige (auch fremde) E-Mail-Adresse. |
| E-Mail-Postfach → Confirm-Link-Klick | Nur der echte Mail-Besitzer kann den signierten Link klicken. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-EMAILVERIFY-07 | Spoofing | Register mit fremder E-Mail | mitigate | email_confirmed=False + fail-closed Gate (Plan 01): kein App-Zugang bis signierter Link (nur echter Besitzer) geklickt. |
| T-EMAILVERIFY-08 | Elevation of Privilege | api_register lässt email_confirmed NULL/True | mitigate | Explizit False (Task 1); grep-Guard; _create_org_and_user untouched (kein OAuth-Google-Regression). |
| T-EMAILVERIFY-09 | Elevation of Privilege | Google-User nach Plan-02-Default-Flip gegatet (Regression) | mitigate | Task 2: Google explizit True (kein Verlass auf ORM-Default) — Halb-Migration-Falle (Punkt 29) vermieden. |
| T-EMAILVERIFY-10 | Information Disclosure | welcome-Mail an unbestätigte (fremde) Adresse | mitigate | send_welcome wandert in confirm_email (D-13) — feuert erst nach bewiesenem Mail-Besitz. |
| T-EMAILVERIFY-11 | Denial of Service | Register/Resend-Spam | accept | Rate-Limits Bestand: register 5/min, resend 3/10min (D-06, unverändert). |
</threat_model>

<verification>
- api_register: email_confirmed=False (explizit), next=Warteseite, send_confirmation_email (kein send_welcome).
- confirm_email: send_welcome + welcome_trial nach email_confirmed=True.
- Jeder Creator setzt email_confirmed explizit (Verbindungs-Karte grep-belegt); _create_org_and_user untouched.
- OAuth Google explizit True (kein Default-Drift nach Plan 02).
- Mail + Warteseite EN; RESEND_BASE_URL ungesetzt.
- Live-UAT (D-16) gruen: Register→Confirm→/onboarding, Sperre, Resend, MS-OAuth.
</verification>

<success_criteria>
- Form-Registrierter landet auf Warteseite, ist gesperrt bis Confirm, kommt danach via AUTH-2-Weiche in die App.
- Kein Creation-Pfad hinterlaesst NULL; kein welcome an unbestaetigte Adresse.
- Cross-AI (Fable+Gemini) PFLICHT vor Execute (Auth-Gate, Punkt 24) — bereits erfolgt.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-03-SUMMARY.md`
</output>
