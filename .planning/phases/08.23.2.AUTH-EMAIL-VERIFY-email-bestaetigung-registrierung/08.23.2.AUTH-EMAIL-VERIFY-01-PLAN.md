---
phase: 08.23.2.AUTH-EMAIL-VERIFY
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/auth.py
  - tests/test_email_verify_gate.py
autonomous: true
requirements: [D-01, D-01b, D-16b]
complexity: "🔴 komplex (Auth/Sicherheits-Gate — Cross-AI Pflicht vor Execute)"

must_haves:
  truths:
    - "Ein User mit email_confirmed IS NULL wird vom login_required-Gate auf die Warteseite umgeleitet (heute NICHT — Fail-Open-Leck)"
    - "Ein User mit email_confirmed = False wird gegatet"
    - "Ein User mit email_confirmed = True passiert das Gate ungehindert"
    - "Die Ausnahme-Pfade /auth/confirm_email und /auth/logout gaten NIE (kein Redirect-Loop)"
  artifacts:
    - path: "routes/auth.py"
      provides: "Fail-closed Gate-Ausdruck email_confirmed is not True in login_required"
      contains: "is not True"
    - path: "tests/test_email_verify_gate.py"
      provides: "ERST-ROT Verhaltens-Guard: NULL+False gaten, True passiert"
      contains: "def test_"
  key_links:
    - from: "routes/auth.py login_required Gate"
      to: "url_for('auth.confirm_email_pending')"
      via: "redirect bei nicht-True email_confirmed"
      pattern: "email_confirmed.*is not True"
---

<objective>
Schließt das Fail-Open-Leck im login_required-Gate: heute liest `auth.py:79`
`getattr(g.user, 'email_confirmed', True) is False` — ein User mit
`email_confirmed IS NULL` ist NICHT `is False` und passiert das Gate UNGATED.
Das ist die Wurzel des freien Register-Zugangs (D-01, GZ0-korrigiert).

Der Fix: Gate auf **fail-closed** umstellen (`email_confirmed is not True`) — nur
explizit `True` passiert, NULL/unset/False gaten alle (D-01b). Die Ausnahme-Pfade
confirm_email/logout bleiben unverändert.

Guard-first (ERST-ROT, D-16b): Ein VALID Verhaltens-Guard (Function-Call/Request
gegen echte PG, kein Source-Presence) beweist NULL+False→gegatet, True→durch. Rot
gegen das heutige `is False`-Gate (NULL-User rutscht durch), grün nach dem Fix.

Purpose: Auth-Sicherheits-Gate fail-closed machen, damit unbestätigte/NULL-User
keinen App-Zugang bekommen — Voraussetzung für die gesamte E-Mail-Verify-Phase.
Output: 1 Gate-Ausdruck-Edit + 1 ERST-ROT-Guard-Testdatei.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

★ Multi-Segment-Phase-ID-Gotcha (memory project_nerve_multisegment_phase_ids):
gsd-tools/gsd-sdk MISRESOLVEN `08.23.2.AUTH-EMAIL-VERIFY`. HART kodiert:
- phase_dir = `.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung`
- SUMMARY = `.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-01-SUMMARY.md`
KEIN gsd-tools für Phase-Resolution.

★ HART: Kein Local-Dev (CLAUDE.md). Verify=Production: automatisierte Tests laufen
im deploy.sh-Pytest-Gate auf nerve_test (SSH). KEIN lokales pytest als Acceptance.
</execution_context>

<context>
@.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-CONTEXT.md
@salesnerve/CLAUDE.md
@routes/CLAUDE.md

<interfaces>
<!-- Gate-Ausdruck heute (routes/auth.py, per INHALT re-ankern — Zeilen driften): -->
```python
# routes/auth.py, login_required decorator (~Z.79-83):
if getattr(g.user, 'email_confirmed', True) is False:
    if (not request.path.startswith('/auth/confirm_email') and
            not request.path.startswith('/auth/logout')):
        return redirect(url_for('auth.confirm_email_pending'))
return f(*args, **kwargs)
```

<!-- Test-Client-Fixture (tests/conftest.py:792) — echte PG nerve_test, kein SQLite-Fallback: -->
- `client` (Flask test_client gegen nerve_test); `client._test_session` = MODUL-SessionLocal (PG-gebunden, hook-tragend); `client._test_engine`.
- Skippt wenn `TEST_DATABASE_URL` nicht gesetzt.
- `cleanup_rows(conn_or_session, {Model: [ids]})` aus tests/conftest.py — POST-yield reverse-FK-Cleanup (public.* ohne Tenant-GUC).
- Session-Login im Test: `with client.session_transaction() as sess: sess['user_id']=...; sess['org_id']=...`.

<!-- confirm_email_pending Route (Ziel des Redirects): -->
- `url_for('auth.confirm_email_pending')` -> `/auth/confirm-email-pending` (auth.py:354, Blueprint 'auth', KEIN @login_required).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ERST-ROT Fail-Closed-Verhaltens-Guard (D-16b)</name>
  <read_first>
    - tests/conftest.py:792-838 (client-Fixture: PG nerve_test, _test_session, session_transaction-Login-Muster)
    - tests/conftest.py:240-310 (cleanup_rows-Contract: {Model:[ids]}, public.* reverse-FK)
    - tests/test_auth_next_redirect.py:28-34 (cleanup_tracker-Fixture-Muster, uuid-suffixed Email gegen UNIQUE)
    - routes/auth.py Gate ~:79-83 (per INHALT ankern: `grep -n "email_confirmed" routes/auth.py`)
    - database/models.py:65-135 (User-Modell, email_confirmed Spalte)
  </read_first>
  <behavior>
    VALID Runtime-Verhalten (Request gegen echte PG, KEIN Source-Presence, CLAUDE.md Test-Qualitaets-Regel):
    - Test A (NULL gatet — ERST-ROT-Kern): User mit email_confirmed=NULL angelegt (via
      raw SQL UPDATE users SET email_confirmed=NULL, da ORM-Default sonst greift), eingeloggt
      via session_transaction, GET auf eine @login_required-Route (z.B. '/dashboard') →
      erwartet Redirect (302) auf '/auth/confirm-email-pending'. HEUTE ROT (NULL is not `is False` → durch).
    - Test B (False gatet): User email_confirmed=False, eingeloggt, GET @login_required-Route →
      302 auf '/auth/confirm-email-pending'.
    - Test C (True passiert): User email_confirmed=True, eingeloggt, GET @login_required-Route →
      KEIN Redirect auf confirm-email-pending (200 oder Redirect auf etwas anderes, NICHT die Warteseite).
    - Test D (Ausnahme confirm_email): User email_confirmed=NULL, eingeloggt, GET '/auth/confirm_email?token=x'
      → KEIN Redirect auf confirm-email-pending (Ausnahme-Pfad, kein Loop).
    Jeder Test raeumt seine committeten Rows via cleanup_rows im POST-yield-Teardown weg (Test-Cleanup-Regel).
    Emails uuid-suffixed gegen UNIQUE.
  </behavior>
  <action>
    Neue Datei tests/test_email_verify_gate.py. Nutze die `client`-Fixture (PG nerve_test).
    Lege pro Test einen User (+ Org falls noetig, via _test_session commit) an, setze email_confirmed
    auf NULL/False/True (fuer NULL: nach ORM-Insert ein raw `UPDATE users SET email_confirmed = NULL
    WHERE id = :id` auf _test_session ausfuehren + commit, weil der ORM-Default sonst True/False setzt).
    Logge via `with client.session_transaction() as sess: sess['user_id']=uid; sess['org_id']=org_id`.
    Feuere GET auf eine echte @login_required-Route (grep eine minimale: `grep -rn "@login_required" routes/ | head`
    — z.B. dashboard.index '/dashboard'). Assertion auf response.status_code == 302 UND
    '/auth/confirm-email-pending' in response.headers['Location'] (bzw. NICHT-darin fuer True/Ausnahme).
    cleanup_tracker-Fixture (Muster test_auth_next_redirect.py:28-34) fuer POST-yield cleanup_rows.
    ★ Dieser Test MUSS ROT sein BEVOR Task 2 den Gate-Fix macht (Test A faellt gegen das `is False`-Gate).
    Committe zuerst rot: `test(08.23.2.AUTH-EMAIL-VERIFY-01): ERST-ROT fail-closed gate guard`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate auf nerve_test; NACH Task 2 gruen. Vor Task 2: Test A MUSS lokal-im-Gate rot sein (dokumentiert im Commit).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_" tests/test_email_verify_gate.py` zeigt ≥4 Tests (NULL/False/True/Ausnahme)
    - `grep -n "confirm-email-pending" tests/test_email_verify_gate.py` vorhanden (Redirect-Ziel-Assertion)
    - `grep -n "UPDATE users SET email_confirmed" tests/test_email_verify_gate.py` vorhanden (NULL-Setz-Pfad)
    - `grep -n "cleanup_rows\|cleanup_tracker" tests/test_email_verify_gate.py` vorhanden (Row-Cleanup)
    - KEIN `getsource`/`open(`/`hasattr`/`grep` auf Quelldateien im Test (Source-Presence verboten)
  </acceptance_criteria>
  <done>NULL/False→302-auf-Warteseite, True/Ausnahme→nicht-Warteseite; Test A rot gegen heutiges Gate, alle raeumen Rows.</done>
</task>

<task type="auto">
  <name>Task 2: Gate fail-closed umstellen (D-01b)</name>
  <read_first>
    - routes/auth.py Gate (per INHALT re-ankern JETZT: `grep -n "email_confirmed', True) is False" routes/auth.py` — ~Z.79)
    - routes/auth.py:73-83 (Kommentar-Block H-18 + Ausnahme-Pfade confirm_email/logout ~:80-82)
  </read_first>
  <action>
    In routes/auth.py, im login_required-Decorator, den Gate-Ausdruck von
    `if getattr(g.user, 'email_confirmed', True) is False:` umstellen auf **fail-closed**:
    `if getattr(g.user, 'email_confirmed', True) is not True:`
    (NULL/None/False gaten alle; nur explizit True passiert). Der `getattr(..., True)`-Default
    bleibt bewusst True als Fallback fuer den seltenen Fall dass g.user das Attribut nicht traegt —
    das ist ok, weil jeder echte User-Row die Spalte hat; entscheidend ist dass NULL-Werte jetzt gaten.
    Die inneren Ausnahme-Pfade (`request.path.startswith('/auth/confirm_email')` und `/auth/logout`,
    ~:80-82) UNVERAENDERT lassen — kein Redirect-Loop. Den Kommentar-Block ~:73-78 um einen Satz
    ergaenzen: "AUTH-EMAIL-VERIFY D-01b: fail-closed — NULL/unset/False gaten, nur True passiert
    (Form-Register-Leck-Fix, Fail-Open-Wurzel war `is False` das NULL durchliess)."
    Committe: `fix(08.23.2.AUTH-EMAIL-VERIFY-01): fail-closed email_confirmed gate (is not True) — schliesst NULL-Bypass`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: test_email_verify_gate.py Test A/B/C/D jetzt alle GRUEN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "email_confirmed', True) is not True" routes/auth.py` vorhanden (exakter fail-closed Ausdruck)
    - `grep -n "email_confirmed', True) is False" routes/auth.py` liefert 0 Treffer im Gate (alter Ausdruck weg)
    - `grep -n "confirm_email'" routes/auth.py` + `logout'` — Ausnahme-Pfade unveraendert vorhanden (~:80-82)
    - `grep -n "D-01b" routes/auth.py` vorhanden (Naht-Kommentar)
  </acceptance_criteria>
  <done>Gate liest `is not True`; Ausnahme-Pfade intakt; alle 4 Guard-Tests gruen im Deploy-Gate.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `users` (LESEN, Spalte `email_confirmed`) — via `g.user` im login_required-Gate.

### Beleg (verbatim Prod-`nerve`, Claudian 2026-07-09 — NICHT zusammenfassen)
```
users.email_confirmed (Prod-nerve, \d users / information_schema.columns, 2026-07-09):
  data_type   = boolean
  is_nullable = YES
  column_default = (leer / kein DB-Default)   ← Fail-Open-Wurzel: Insert ohne Wert → NULL
Real-Daten: id=1 true, id=2 true, id=3 true (alle 3 Bestandskonten bestätigt; 0 NULL-Rows aktuell).
models.py:135 = Column(Boolean, default=True, nullable=True)  ← ORM-Default True driftet zur Prod-Realität (kein DB-Default).
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt / Leser-Voraus? | Verifiziert? |
|---|---|---|---|---|
| `g.user.email_confirmed` | GELESEN in login_required (auth.py:79) | DB-Spalte `users.email_confirmed` (boolean, nullable, KEIN DB-Default) | Live: g.user via `db.get(User, session['user_id'])` je Request geladen (auth.py:51). Kein async-Lag (Punkt 26): der Wert existiert zum Lese-Zeitpunkt schon (bei Anlage geschrieben). Leser laeuft NIE dem Schreiber voraus. | ✓ CONTEXT-Beleg |

### NULL-vs-False Layer-Subtilität (Kern dieser Phase)
`is False` unterscheidet in Python NULL(None)/False/True STRIKT: `None is False` == `False`.
Darum passiert ein `email_confirmed IS NULL`-User das heutige Gate. `is not True` behandelt
None UND False identisch als "nicht bestaetigt" → fail-closed. Das ist eine reine Python-
Vergleichs-Semantik-Aenderung, KEINE Schema-Aenderung (die kommt in Plan 02, unabhaengig).
Kein neuer Spalten-Read der crashen koennte (die Spalte existiert seit H-18).

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser (eingeloggter, unbestätigter User) → login_required-Gate | Untrusted: ein Angreifer registriert sich, wird auto-eingeloggt, versucht App-Zugang OHNE E-Mail-Besitz-Nachweis. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-EMAILVERIFY-01 | Elevation of Privilege | login_required Gate (auth.py:79) | mitigate | Fail-closed `is not True`: NULL/unset/False bekommen KEINEN App-Zugang. Genau der Fix dieses Plans (D-01b). ERST-ROT-Guard (Task 1) verhindert Regression. |
| T-EMAILVERIFY-02 | Spoofing | E-Mail-Besitz-Vortäuschung (fremde Adresse registrieren) | mitigate | Signierter 24h-Confirm-Link (Bestand, H-18) beweist Mail-Besitz; Gate hält bis Klick. Dieser Plan macht das Gate erst wirksam (vorher fail-open). |
| T-EMAILVERIFY-03 | Denial of Service | Redirect-Loop confirm_email/logout | accept | Ausnahme-Pfade (auth.py:80-82) unverändert — kein Loop. Bewusst kein @login_required auf confirm_email_pending/confirm_email/resend. |
</threat_model>

<verification>
- Gate-Ausdruck ist `email_confirmed', True) is not True` (grep-belegt).
- 4 Verhaltens-Tests (NULL/False/True/Ausnahme) gruen im deploy.sh-Pytest-Gate auf nerve_test.
- Ausnahme-Pfade confirm_email/logout unveraendert.
- KEIN Source-Presence-Test (VALID Runtime-Verhalten).
</verification>

<success_criteria>
- login_required gatet NULL, False; passiert nur True.
- ERST-ROT-Guard war rot gegen `is False`, gruen nach `is not True`.
- Deploy-Gate (bash deploy.sh production) gruen; alter Code bleibt live bei Test-Fehler.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-01-SUMMARY.md`
</output>
