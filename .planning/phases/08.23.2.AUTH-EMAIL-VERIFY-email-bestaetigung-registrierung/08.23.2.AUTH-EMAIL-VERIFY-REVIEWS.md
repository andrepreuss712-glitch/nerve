# Phase 08.23.2.AUTH-EMAIL-VERIFY — Cross-AI Review (Fable + Gemini)

**Reviewed:** 2026-07-09
**Reviewers:** Fable + Gemini (3 Sichten, Punkt 24 — Auth-Gate + Migration)
**Verdict:** NACHBESSERN — 2 BLOCKER + 1 Fund + 1 Minor. (Bestätigt + verbreitert den GSD-internen Deploy-Ordnungs-Vorbehalt.)

---

## Finding 1 — ★ BLOCKER: Deploy-Reihenfolge (Fenster BREITER als GSD-Fund)

Der Default-Flip (Plan 02) darf NICHT vor den expliziten Settern (Plan 03) live gehen. **DREI Pfade betroffen, nicht nur Google:**
- Google-OAuth (`oauth.py:132-133` setzt nur MS) → im Fenster `False` → fälschlich gegatet.
- Invite-Accept (`auth.py:316` setzt `email_confirmed` nicht) → im Fenster `False` → eingeladenes Mitglied fälschlich gegatet.
- Form-Register → im Fenster gegatet OHNE Confirm-Mail (Plan-03-Naht noch nicht live).

**FIX:** Wave-Reihenfolge **01 → 03 → 02** (Default-Flip **zuletzt** = garantierter No-Op, weil dann alle Pfade schon explizit setzen). Plan 03 hängt funktional NICHT an Plan 02 (alle Plan-03-Tests asserten explizit gesetzte Werte, gelten unter altem UND neuem Default) → nur `depends_on` in Plan 03 (Z.6) ändern + die „NACH Plan 02"-Interface-Kommentare anpassen. (Gemini + Fable einig; „Google-True in Plan 02 koppeln" verworfen — schließt Invite + Form nicht.)

## Finding 2 — ★ BLOCKER: Plan 02 bricht sein EIGENES Deploy-Gate

`grep email_confirmed tests/` = **0 Treffer**. Nach dem ORM-Default-Flip bekommen alle testgebauten User `False` → ~8 Test-Dateien mit `@login_required`-Requests laufen in 302/Warteseite:
`tests/conftest.py:95` (Base-Seed User id=1), `test_admin_dashboard_auth.py:32-34`, `test_ewb_rate_api.py:94`, `test_waitlist_admin_auth.py:52`, `test_logs_org_boundary.py:66`, `test_logs_founder_access.py:64`, `test_profile_editor_validation.py:73`, `test_login_destination_wiring.py:131/199`.

**FIX:** Plan 02 braucht eine ZUSATZ-Task: `email_confirmed=True` **zentral** in den Test-Helfern setzen (bevorzugt zentral in `conftest.py`, sodass alle Fixtures abgedeckt sind). Muss im SELBEN Deploy wie der Default-Flip (Plan 02) shippen.

## Finding 3 — Doppel-Welcome (Plan 03)

- **(a)** `oauth.py:140-144` sendet `send_welcome` bei JEDER OAuth-Neuanlage — auch an MS-User (unbestätigt) → nach dem Welcome-Move zweites Welcome beim MS-Confirm. **Fix:** `send_welcome` nur im Google/else-Zweig (MS bekommt es via `confirm_email`).
- **(b)** `confirm_email` (`auth.py:372-376`) ist nicht idempotent → Mehrfach-Klick = Mehrfach-Welcome. **Fix:** Guard `if not user.email_confirmed:` um den Welcome-Block.

## Finding 4 — Minor

- Plan-01 Test D testet die **undecorierte** Route `/auth/confirm_email` → exerziert den Gate-Ausnahmezweig nie. Entweder eine **decorierte** Route testen ODER Kommentar, dass D nur die Nicht-Sperre beweist.
- `app.py:933` Legacy-Inline-Migration (`DEFAULT 1`) mit Aufräum-Kommentar versehen (widerspricht neuem False-Default; kein Blocker).

## Bleibt
Persistenz/Live-Checks unverändert. Claudian fährt VOR Plan-01-Deploy: `SELECT count(*) WHERE email_confirmed IS NULL = 0`, `alembic = 0032`.
