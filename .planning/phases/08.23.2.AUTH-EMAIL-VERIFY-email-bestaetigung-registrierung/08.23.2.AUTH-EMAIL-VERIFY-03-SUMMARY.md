---
phase: 08.23.2.AUTH-EMAIL-VERIFY
plan: "03"
subsystem: auth-email-verify
tags: [auth, email-verify, confirm-flow, oauth, creation-paths, welcome-move, en-i18n, wave-2]
dependency_graph:
  requires: [08.23.2.AUTH-EMAIL-VERIFY-01]
  provides: [confirm-first-register, idempotent-welcome, explicit-creation-setters, en-confirmation-mail]
  affects: [routes/auth.py, routes/oauth.py, app.py, scripts/seed_test_user.py, services/email_service.py, templates/confirm_email_pending.html, tests/test_login_destination_wiring.py]
tech_stack:
  added: []
  patterns: [confirm-first-landing, welcome-move-to-confirm, idempotency-guard-before-set, explicit-email_confirmed-per-creator, provider-verified-google-true]
key_files:
  created:
    - tests/test_email_verify_creation_paths.py
  modified:
    - routes/auth.py
    - routes/oauth.py
    - app.py
    - scripts/seed_test_user.py
    - services/email_service.py
    - templates/confirm_email_pending.html
    - tests/test_login_destination_wiring.py
decisions:
  - "api_register setzt email_confirmed=False EXPLIZIT (nur hier, nicht im geteilten _create_org_and_user — OAuth-Naht Punkt 14)"
  - "api_register returnt next=/auth/confirm-email-pending (D-09 — ueberschreibt die AUTH-2-Onboarding-Weiche fuer Unbestaetigte); Auto-Login bleibt (D-10)"
  - "send_welcome + welcome_trial wandern in confirm_email, IDEMPOTENT via `if not user.email_confirmed` VOR dem =True-Setzen (Finding 3b)"
  - "OAuth: Google/else=True explizit (Provider-verified, D-07), Microsoft=False; send_welcome nur im Nicht-Microsoft-Zweig (Finding 3a, kein Doppel-Welcome an MS)"
  - "Invite-register/_seed-owner/seed_test_user: email_confirmed=True explizit → jeder Creator setzt, Plan-02-Default-Flip wird No-Op (D-03)"
  - "app.py:933 Legacy-SQLite-Migration DEFAULT 1: nur Punkt-29-DEPRECATED-Marker, kein Code-Change (Finding 4b)"
  - "Bestaetigungs-Mail + Warteseite EN (D-08); send_welcome bleibt DE; RESEND_BASE_URL ungesetzt"
  - "Stale AUTH-2-Wiring-Test test_register_owner_lands_on_erstprofil an den Confirm-First-Vertrag angepasst (retarget /auth/confirm-email-pending, umbenannt) — kein Prod-Code-Change"
metrics:
  completed: "2026-07-10"
  tasks_completed: 4
  files_changed: 7
---

# Phase 08.23.2.AUTH-EMAIL-VERIFY Plan 03: Confirm-Flow-Naht + Anlage-Pfad-Inventur Summary

**One-liner:** Form-Register an den Confirm-Flow verdrahtet (email_confirmed=False → EN-Warteseite → EN-Confirm-Mail → Klick → True + idempotenter Welcome → AUTH-2-Weiche), jeder Anlage-Pfad setzt email_confirmed explizit (Google=True/MS=False, Invite/Seed=True), kein NULL mehr.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| — (ERST-ROT) | Creation-path + confirm-flow guard | 9fce3cf | tests/test_email_verify_creation_paths.py (neu) |
| 1 | api_register→confirm-flow + welcome-move + creation-path setters | d1ed793 | routes/auth.py, app.py, scripts/seed_test_user.py |
| 2 | oauth Google=True/MS=False + send_welcome Google-only | 3fff0c1 | routes/oauth.py |
| 3 | EN confirmation mail + waiting page | 4a05ae0 | services/email_service.py, templates/confirm_email_pending.html |
| Fix | Stale AUTH-2-Wiring-Test → Confirm-First-Vertrag (D-09) | 4537df0 | tests/test_login_destination_wiring.py |

## Was gebaut wurde

- **api_register (auth.py):** nach create+commit `email_confirmed=False`, Auto-Login bleibt, der alte send_welcome-Block ersetzt durch `send_confirmation_email` (signierter Token, Muster wie oauth), Response `next=url_for('auth.confirm_email_pending')`. Unbestaetigte landen auf der Warteseite; das fail-closed Gate (Plan 01) sperrt sie bis Confirm.
- **confirm_email (auth.py):** Welcome-Move mit Idempotenz-Guard — `if not user.email_confirmed:` VOR dem `=True`. Mehrfach-Klick/Reload → kein zweites Welcome.
- **OAuth (oauth.py):** explizites if/else Microsoft=False / Google(+else)=True; send_welcome nur im Nicht-Microsoft-Zweig. MS bekommt genau EIN Welcome (via confirm_email), Google sofort.
- **Creation-Setter:** Invite-register + _seed-owner + seed_test_user → `email_confirmed=True`. `_create_org_and_user` UNANGETASTET (OAuth teilt es, D-02/Punkt 14).
- **EN-i18n:** `send_confirmation_email` + `confirm_email_pending.html` US-Englisch; send_welcome DE; RESEND_BASE_URL ungesetzt.
- **app.py:933:** Punkt-29-DEPRECATED-Marker auf der Legacy-SQLite-Inline-Migration (kein Code-Change).

## Verbindungs-Karte (Punkt 22) — jeder Creator setzt explizit

| Creator | Wert | Beleg |
|---|---|---|
| api_register | **False** | `user.email_confirmed = False` (auth.py:278) |
| OAuth Google/else | **True** | `new_user.email_confirmed = True` (oauth.py:138) |
| OAuth Microsoft | **False** | `new_user.email_confirmed = False` (oauth.py:136) |
| confirm_email | **True** (idempotent) | `if not user.email_confirmed` + `= True` (auth.py:388/398) |
| Invite-register | **True** | `email_confirmed=True` (auth.py:330) |
| _seed owner | **True** | `email_confirmed=True` (app.py:1657) |
| seed_test_user | **True** | `email_confirmed=True` (scripts/seed_test_user.py:43) |
| _create_org_and_user | NICHT gesetzt (Caller-Override) | UNVERAENDERT (D-02) |

→ Kein Pfad laesst NULL. Plan-02-Default-Flip (Wave 3) ist damit ein garantierter No-Op.

## Verifikation (Verify=Production)

- **Grün-Gate:** 941 passed, Restart, live git_head `4537df0`, gepusht.
- **Stale-Test-Fix:** der Grün-Gate war zunaechst rot (1 failed / 940) — `test_register_owner_lands_on_erstprofil` assertete den Vor-EMAIL-VERIFY Register→/onboarding-Vertrag. D-09 ist gewollt; Test an den Confirm-First-Vertrag angepasst (retarget + umbenannt), Datei ganz durchgeprueft (nur diese eine Assertion stale). Klasse: Stale-Test-Contract (Punkt 18).
- **Live-UAT (D-16):** ✓ Register→Confirm→Onboarding + Idempotenz; ✓ unbestaetigt=gesperrt; ✓ Resend + Rate-Limit; ✓ Bestandskonten unberuehrt. DB-Beleg: neuer Nutzer id=6 register(False)→confirm(True)→onboarding_state=done; 0 NULL-Nutzer.
- **UAT-Gap (dokumentiert):** MS-OAuth / Google-Neuanlage (Punkt 5 der Checkliste) nicht live pruefbar — André hat kein Microsoft-Konto + kein zweites Google-Konto. Abgedeckt durch die expliziten Setter (grep-belegt, oauth.py:136/138) + Code-Review + Finding-3a-Guard (send_welcome nur Google-Zweig). Kein offener Verifikations-Rest fuer die Form-/Confirm-Pfade.

## Self-Check: PASSED

- Alle Task-1/2/3-Acceptance-Kriterien grep-belegt gruen (siehe Commits).
- Kein Source-Presence-Test.
- _create_org_and_user untouched (D-02-Beleg).
- Deploy-Gate gruen (941), Live-UAT der Form-/Confirm-Pfade gruen.

## Naht fuer Wave 3 (Plan 02)

Jeder Creator setzt email_confirmed jetzt explizit → der DB-Default-Flip auf False (Migration 0033) + ORM-Default-Flip ist ein garantierter No-Op. Plan 02 braucht zusaetzlich den zentralen conftest-Test-Helfer (Finding 2), damit test-gebaute User nach dem Default-Flip nicht auf die Warteseite laufen. Enthaelt eine Migration → Prod-Migrations-Checkpoint (manueller alembic-Schritt, autonomous:false).
