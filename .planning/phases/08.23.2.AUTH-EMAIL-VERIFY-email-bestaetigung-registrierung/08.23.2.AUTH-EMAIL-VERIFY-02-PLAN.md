---
phase: 08.23.2.AUTH-EMAIL-VERIFY
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0033_email_confirmed_default_false.py
  - database/models.py
autonomous: false
requirements: [D-03b]
complexity: "🟡 mittel (kleine Migration + Schema-Default-Flip + Prod-Deploy-Order)"

must_haves:
  truths:
    - "Ein INSERT in users OHNE explizites email_confirmed landet als False, nicht NULL (belt-and-suspenders)"
    - "Die 3 Bestandskonten (email_confirmed=true) bleiben unangetastet — kein Backfill"
    - "Die Spalte users.email_confirmed trägt ein aktuelles Schild (Punkt 23)"
  artifacts:
    - path: "alembic/versions/0033_email_confirmed_default_false.py"
      provides: "DB-Column-Default False + COLUMN-COMMENT (Schild)"
      contains: "email_confirmed"
    - path: "database/models.py"
      provides: "ORM-Default False + aktualisiertes Schild-Comment"
      contains: "default=False"
  key_links:
    - from: "alembic 0033 upgrade"
      to: "users.email_confirmed DB-Default"
      via: "ALTER COLUMN SET DEFAULT false"
      pattern: "SET DEFAULT"
---

<objective>
Belt-and-suspenders (D-03b): setzt den ORM-Default (`models.py:135`) UND den
DB-Column-Default von `email_confirmed` auf **False**, damit kein Insert-Pfad still
auf NULL fällt (Fail-Open-Wurzel: Prod-Spalte ist nullable OHNE DB-Default → Insert
ohne Wert = NULL). Die alte "Default NICHT flippen"-Annahme galt unter der falschen
Default-True-Prämisse (GZ0-Korrektur) und ist hinfällig.

**KEIN NULL→False-Backfill nötig:** 0 NULL-Rows aktuell; die 3 Bestandskonten sind
explizit `true` und bleiben unangetastet. Schild (Punkt 23) wird nachgezogen.

Purpose: zweite Verteidigungslinie unter dem fail-closed Gate (Plan 01) — auch wenn
ein künftiger Creator email_confirmed vergisst, landet False (gegatet) statt NULL.
Output: 1 Alembic-Migration `0033` + models.py ORM-Default-Flip + Schild.

★ Unabhängig von Plan 01 (andere Dateien: alembic/, models.py vs auth.py-Gate).
Beide Wave 1. Der DB-Default-Flip ändert KEINE Leser (nullable bleibt, kein neuer
Spalten-Read) → niedrig-Risiko, aber Deploy-Order dennoch explizit (Punkt 26).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

★ Multi-Segment-Phase-ID-Gotcha: phase_dir HART =
`.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung`.
SUMMARY = `08.23.2.AUTH-EMAIL-VERIFY-02-SUMMARY.md`. KEIN gsd-tools.

★ HART: Kein Local-Dev. Verify=Production. Migration wird MANUELL auf Prod als
`postgres` gefahren (memory project_nerve_prod_deploy: Migrationen als postgres),
Deploy-Order-Checkpoint unten (autonomous:false).
</execution_context>

<context>
@.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-CONTEXT.md
@salesnerve/CLAUDE.md
@alembic/versions/0032_onboarding_state_skip_onboarding.py

<interfaces>
<!-- Alembic-Head heute = 0032 (verifiziert: alembic/versions/, down_revision-Kette). -->
<!-- Neue Migration: revision='0033', down_revision='0032'. -->

<!-- Migrations-Stil (Hausmuster, wie 0032): op.execute("COMMENT ON COLUMN ...") + op.alter_column. -->
<!-- KEIN autogenerate-Round-Trip (Punkt 23 Cross-AI-Finding 1). -->

<!-- models.py:135 heute (per INHALT re-ankern): -->
```python
email_confirmed = Column(Boolean, default=True, nullable=True,
    comment='Email bestaetigt (Microsoft-OAuth Hijacking-Mitigation, H-18)')
```

<!-- Schild-Regel Punkt 23: Zweck. Status. Schreibt <datei.py>; liest <datei.py>. -->
<!-- Leser: login_required-Gate (auth.py:79). Schreiber: OAuth (oauth.py) + confirm_email (auth.py:375) + api_register NEU=False (Plan 03) + DB-Default False (diese Migration). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic-Migration 0033 — DB-Default False + Schild</name>
  <read_first>
    - alembic/versions/0032_onboarding_state_skip_onboarding.py (Hausmuster: op.alter_column, op.execute COMMENT, upgrade/downgrade)
    - alembic/versions/ (`ls` — bestaetigen dass 0032 der Head ist, kein 0033 existiert)
    - database/models.py:135 (aktueller Spalten-Zustand + Comment — per INHALT ankern)
    - CONTEXT.md persistenz_beleg (nullable=YES, column_default leer, 0 NULL-Rows)
  </read_first>
  <action>
    Neue Datei alembic/versions/0033_email_confirmed_default_false.py, Muster wie 0032:
    - `revision = '0033'`, `down_revision = '0032'`, branch_labels=None, depends_on=None.
    - Docstring: erklaert D-03b (Default-Flip True→False belt-and-suspenders, Fail-Open-Wurzel
      war nullable ohne DB-Default), + Prod-Ist-Stand (nullable=YES, column_default leer,
      3 Konten=true, 0 NULL-Rows → KEIN Backfill).
    upgrade():
    - `op.alter_column('users', 'email_confirmed', server_default=sa.false())` (setzt DB-Default False).
      ODER explizit `op.execute("ALTER TABLE users ALTER COLUMN email_confirmed SET DEFAULT false")`.
    - KEIN `UPDATE users SET email_confirmed=False` (kein Backfill — 0 NULL-Rows, Bestand=true bleibt).
      Als Kommentar dokumentieren WARUM kein Backfill (die 3 true-Konten duerfen NICHT geflippt werden).
    - Schild (Punkt 23) via op.execute COMMENT ON COLUMN users.email_confirmed IS
      '<Zweck: Email bestaetigt — fail-closed Gate liest, Form-Register/OAuth-Microsoft setzen False>.
      Status: lebt. Schreibt routes/oauth.py (Microsoft=False/Google=True), routes/auth.py
      (api_register=False, confirm_email=True), DB-Default False (Migration 0033);
      liest routes/auth.py login_required-Gate.'
    downgrade():
    - `op.alter_column('users','email_confirmed', server_default=sa.true())` (zurueck auf True-Default).
    - Schild-COMMENT zuruecksetzen auf den H-18-Text.
    Committe: `feat(08.23.2.AUTH-EMAIL-VERIFY-02): migration 0033 email_confirmed DB-default False + Schild (D-03b)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate + SCHILD-Guard (test_schild_guard.py) gruen; Migration selbst wird MANUELL gefahren (Task 2 Checkpoint)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "revision = '0033'" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -n "down_revision = '0032'" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -niE "SET DEFAULT false|server_default=sa.false" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -ni "COMMENT ON COLUMN users.email_confirmed" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -niE "backfill|UPDATE users SET email_confirmed" alembic/versions/0033_email_confirmed_default_false.py` — nur im Kommentar (kein echtes UPDATE-Statement gegen Bestand)
  </acceptance_criteria>
  <done>Migration 0033 flippt DB-Default auf False, kein Backfill, Schild aktuell.</done>
</task>

<task type="auto">
  <name>Task 2: models.py ORM-Default False + Schild-Comment</name>
  <read_first>
    - database/models.py:132-135 (email_confirmed Column + H-18-Kommentar — per INHALT ankern)
  </read_first>
  <action>
    In database/models.py die email_confirmed-Column-Definition (~:135) aendern:
    `default=True` → `default=False` (ORM-Default; `nullable=True` bleibt — kein NOT-NULL,
    weil Bestand-Rows nicht angefasst werden). Das `comment=`-Argument auf den Schild-Text
    aus Task 1 aktualisieren (identisch zur Migration, Punkt 23 Aktualitaets-Pflicht:
    Schreibt oauth.py/auth.py + DB-Default False; liest login_required-Gate).
    Die Kommentar-Zeilen ~:132-134 ("Default=True: bestehende User gelten...") auf die neue
    Semantik anpassen: "Default=False (AUTH-EMAIL-VERIFY D-03b): neue Inserts unbestaetigt bis
    Confirm; fail-closed Gate. Bestand-Rows unveraendert (kein Backfill)."
    ★ Naht (Punkt 14): NUR den Default aendern. `_create_org_and_user` (auth.py:200) setzt
    email_confirmed NICHT explizit — der ORM-Default greift dort jetzt False; Plan 03 setzt es
    in api_register explizit + Google/Invite explizit True. Kein Edit an models.py User(...)-Callern hier.
    Committe: `feat(08.23.2.AUTH-EMAIL-VERIFY-02): ORM-default email_confirmed False + Schild (D-03b)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate + SCHILD-Guard gruen</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "email_confirmed.*default=False" database/models.py` vorhanden
    - `grep -n "email_confirmed.*default=True" database/models.py` liefert 0 Treffer
    - `grep -n "D-03b" database/models.py` vorhanden (Naht-Kommentar)
    - `grep -ni "Schreibt routes/oauth.py\|liest routes/auth.py" database/models.py` vorhanden (Schild deckt Leser+Schreiber)
  </acceptance_criteria>
  <done>ORM-Default False, Schild aktuell + deckt alle Leser/Schreiber, nullable unveraendert.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Prod-Migration 0033 fahren (Deploy-Order, Punkt 26)</name>
  <what-built>
    Migration 0033 (DB-Default False) + models.py ORM-Default False. Beide committet + gepusht.
  </what-built>
  <how-to-verify>
    ★ Deploy-Order (Punkt 26 — niedrig-Risiko, weil KEIN neuer Spalten-Read und nullable bleibt,
    aber dennoch Order explizit): Die Migration aendert NUR den DB-Default fuer KUENFTIGE Inserts —
    sie bricht keinen bestehenden Leser (kein `column does not exist`-Risiko wie bei AUTH-2, weil
    email_confirmed seit H-18 existiert). Reihenfolge daher unkritisch, empfohlen:
    1. Code deployen (models.py ORM-Default False) via `bash deploy.sh production` — der Pytest-Gate
       laeuft auf nerve_test VOR dem Restart; bei Test-Fehler kein Restart, alter Code bleibt live.
    2. Migration manuell auf Prod als postgres fahren (memory project_nerve_prod_deploy):
       `ssh -i ~/.ssh/nerve_vps root@178.104.82.166` → als postgres alembic upgrade head (0032→0033).
    3. Verifizieren via inspect.sh (read-only): `bash scripts/inspect.sh schema users` →
       email_confirmed column_default sollte jetzt `false` sein; `bash scripts/inspect.sh count users`
       + Bestand-Check: die 3 Konten weiterhin email_confirmed=true (KEIN Backfill hat sie geflippt).
    4. `bash scripts/inspect.sh migrations` → alembic_version = 0033.
    André klickt/bestaetigt: Migration lief, 3 Bestandskonten unveraendert true, DB-Default=false.
  </how-to-verify>
  <resume-signal>Type "migration-live" wenn 0033 auf Prod, Bestand unveraendert, DB-Default=false — oder beschreibe Fehler.</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `users` (SCHREIBEN: DB-Default-Aenderung der Spalte `email_confirmed`; KEIN Row-Backfill).

### Beleg (verbatim Prod-`nerve`, Claudian 2026-07-09 — NICHT zusammenfassen)
```
users.email_confirmed (Prod-nerve, \d users / information_schema.columns, 2026-07-09):
  data_type   = boolean
  is_nullable = YES
  column_default = (leer / kein DB-Default)   ← Fail-Open-Wurzel: Insert ohne Wert → NULL
Real-Daten: id=1 true, id=2 true, id=3 true (alle 3 Bestandskonten bestätigt; 0 NULL-Rows aktuell).
models.py:135 = Column(Boolean, default=True, nullable=True)  ← ORM-Default True driftet zur Prod-Realität (kein DB-Default).
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt / Leser-Voraus? | Verifiziert? |
|---|---|---|---|---|
| `email_confirmed` DB-Default | GESCHRIEBEN via ALTER COLUMN SET DEFAULT (Migration 0033) | DB-Spalte `users.email_confirmed` (boolean, nullable) | Migration einmalig (DDL). Betrifft nur KUENFTIGE Inserts, nicht Bestand-Rows. Kein Leser-Voraus-Problem (Punkt 26): DDL vor jedem neuen Insert. | ✓ CONTEXT-Beleg |
| `email_confirmed` ORM-Default | GESCHRIEBEN bei `User(...)` ohne expliziten Wert (SQLAlchemy) | dito (Python-seitig gesetzt beim flush) | Insert-Zeit; deterministisch. | ✓ models.py:135 |
| 3 Bestand-Rows (true) | UNBERUEHRT (kein UPDATE) | dito | — | ✓ CONTEXT: 0 NULL-Rows, kein Backfill noetig |

### NULL-vs-False Layer-Subtilität
Die Migration eliminiert die NULL-QUELLE fuer neue Inserts (DB-Default False statt kein-Default).
Zusammen mit dem fail-closed Gate (Plan 01, das NULL SOWIESO gatet) doppelte Absicherung:
selbst wenn ein Creator email_confirmed vergisst, landet jetzt False (gegatet) statt NULL.
Kein neuer Spalten-Read → kein Deploy-Crash-Risiko (anders als AUTH-2 onboarding_state).

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Künftiger Code-Pfad (neuer User-Creator) → users-INSERT | Ein neuer Creator könnte email_confirmed vergessen → ohne DB-Default = NULL. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-EMAILVERIFY-04 | Elevation of Privilege | users-INSERT ohne email_confirmed | mitigate | DB-Default False (0033) + ORM-Default False: vergessener Setter landet False (gegatet), nicht NULL (fail-open). Zweite Linie unter Plan 01. |
| T-EMAILVERIFY-05 | Tampering | versehentliches Flippen der 3 Bestandskonten | mitigate | KEIN Backfill-UPDATE in der Migration; DDL-only. inspect.sh-Verifikation im Checkpoint dass Bestand=true bleibt. |
| T-EMAILVERIFY-06 | Denial of Service | Migration bricht Prod-Login | accept | Kein neuer Spalten-Read; nullable bleibt; email_confirmed existiert seit H-18. Kein `column does not exist`-Risiko. Deploy-Order dennoch explizit (Checkpoint). |
</threat_model>

<verification>
- 0033 hat down_revision 0032, ALTER SET DEFAULT false, COMMENT ON COLUMN, KEIN Backfill-UPDATE.
- models.py default=False, Schild deckt alle Leser/Schreiber (grep).
- SCHILD-Guard (test_schild_guard.py) gruen im Deploy-Gate.
- Prod nach Migration: DB-Default=false, alembic_version=0033, 3 Konten weiterhin true (Checkpoint).
</verification>

<success_criteria>
- Neue Inserts ohne email_confirmed = False (nicht NULL).
- Bestand (3× true) unveraendert.
- Schild aktuell + vollstaendig (Punkt 23).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-02-SUMMARY.md`
</output>
