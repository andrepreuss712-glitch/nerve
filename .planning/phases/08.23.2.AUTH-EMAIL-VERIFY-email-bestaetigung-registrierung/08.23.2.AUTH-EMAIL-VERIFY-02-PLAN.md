---
phase: 08.23.2.AUTH-EMAIL-VERIFY
plan: 02
type: execute
wave: 3
depends_on: ["08.23.2.AUTH-EMAIL-VERIFY-01", "08.23.2.AUTH-EMAIL-VERIFY-03"]
files_modified:
  - alembic/versions/0033_email_confirmed_default_false.py
  - database/models.py
  - tests/conftest.py
autonomous: false
requirements: [D-03b]
complexity: "🟡 mittel (kleine Migration + Schema-Default-Flip + Prod-Deploy-Order — laeuft ZULETZT als No-Op)"

must_haves:
  truths:
    - "Ein INSERT in users OHNE explizites email_confirmed landet als False, nicht NULL (belt-and-suspenders)"
    - "Die 3 Bestandskonten (email_confirmed=true) bleiben unangetastet — kein Backfill"
    - "Die Spalte users.email_confirmed trägt ein aktuelles Schild (Punkt 23)"
    - "Der Default-Flip bricht das eigene Deploy-Gate NICHT: test-gebaute User bekommen zentral email_confirmed=True (Finding 2)"
  artifacts:
    - path: "alembic/versions/0033_email_confirmed_default_false.py"
      provides: "DB-Column-Default False + COLUMN-COMMENT (Schild)"
      contains: "email_confirmed"
    - path: "database/models.py"
      provides: "ORM-Default False + aktualisiertes Schild-Comment"
      contains: "default=False"
    - path: "tests/conftest.py"
      provides: "Zentraler Test-Helfer: email_confirmed=True fuer test-gebaute User (Finding 2)"
      contains: "email_confirmed"
  key_links:
    - from: "alembic 0033 upgrade"
      to: "users.email_confirmed DB-Default"
      via: "ALTER COLUMN SET DEFAULT false"
      pattern: "SET DEFAULT"
---

<objective>
Belt-and-suspenders (D-03b): setzt den ORM-Default (`models.py:135`) UND den
DB-Column-Default von `email_confirmed` auf **False**, damit kein Insert-Pfad still
auf NULL fällt (Fail-Open-Wurzel: Prod-Spalte ist nullable OHNE DB-Default → Insert
ohne Wert = NULL). Die alte "Default NICHT flippen"-Annahme galt unter der falschen
Default-True-Prämisse (GZ0-Korrektur) und ist hinfällig.

**KEIN NULL→False-Backfill nötig:** 0 NULL-Rows aktuell; die 3 Bestandskonten sind
explizit `true` und bleiben unangetastet. Schild (Punkt 23) wird nachgezogen.

Purpose: zweite Verteidigungslinie unter dem fail-closed Gate (Plan 01) — auch wenn
ein künftiger Creator email_confirmed vergisst, landet False (gegatet) statt NULL.
Output: 1 Alembic-Migration `0033` + models.py ORM-Default-Flip + Schild + zentraler
Test-Helfer (Finding 2).

★ WAVE 3, LÄUFT ZULETZT (Finding-1-REORDER 2026-07-09): depends_on = Plan 01 + Plan 03.
Dieser Plan ist jetzt ein **garantierter No-Op** — weil Plan 03 (Wave 2) BEREITS jeden
Anlage-Pfad explizit gesetzt hat (api_register=False, OAuth Google=True/MS=False, Invite=True,
seed=True), verlaesst sich KEIN Produktiv-Pfad mehr auf den Default. Der Flip aendert also
kein Laufzeit-Verhalten eines existierenden Pfades; er ist reine Zukunfts-Absicherung
(vergessener kuenftiger Creator → False statt NULL). Wuerde dieser Plan ZUERST laufen
(alte Reihenfolge), fielen Google-OAuth/Invite/Form im Fenster auf False → faelschlich
gegatet (Finding 1). Darum: ZULETZT.

★ FINDING 2 (Selbst-Gate-Bruch): Nach dem ORM-Default-Flip bekommen test-gebaute User
(die email_confirmed NICHT explizit setzen) False → ~8 Test-Dateien mit @login_required-
Requests laufen in 302/Warteseite und FAILEN, was das EIGENE Deploy-Gate dieses Plans
rot macht. Fix: eine ZENTRALE Test-Helfer-Setzung (email_confirmed=True fuer test-gebaute
User) in conftest.py — im SELBEN Deploy wie der Flip. Details Task 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

★ Multi-Segment-Phase-ID-Gotcha: phase_dir HART =
`.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung`.
SUMMARY = `08.23.2.AUTH-EMAIL-VERIFY-02-SUMMARY.md`. KEIN gsd-tools.

★ HART: Kein Local-Dev. Verify=Production. Migration wird MANUELL auf Prod als
`postgres` gefahren (memory project_nerve_prod_deploy: Migrationen als postgres),
Deploy-Order-Checkpoint unten (autonomous:false).
</execution_context>

<context>
@.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-CONTEXT.md
@salesnerve/CLAUDE.md
@alembic/versions/0032_onboarding_state_skip_onboarding.py

<interfaces>
<!-- Alembic-Head heute = 0032 (verifiziert: alembic/versions/, down_revision-Kette). -->
<!-- Neue Migration: revision='0033', down_revision='0032'. -->

<!-- Migrations-Stil (Hausmuster, wie 0032): op.execute("COMMENT ON COLUMN ...") + op.alter_column. -->
<!-- KEIN autogenerate-Round-Trip (Punkt 23 Cross-AI-Finding 1). -->

<!-- models.py:135 heute (per INHALT re-ankern): -->
```python
email_confirmed = Column(Boolean, default=True, nullable=True,
    comment='Email bestaetigt (Microsoft-OAuth Hijacking-Mitigation, H-18)')
```

<!-- Schild-Regel Punkt 23: Zweck. Status. Schreibt <datei.py>; liest <datei.py>. -->
<!-- Leser: login_required-Gate (auth.py:79). Schreiber: OAuth (oauth.py Google=True/MS=False) + confirm_email (auth.py:375) + api_register False + Invite True (auth.py) + seed True + DB-Default False (diese Migration). -->

<!-- FINDING 2 Anker (grep-verifiziert 2026-07-09): -->
<!--   `grep email_confirmed tests/` = 0 Treffer (kein Test setzt es heute). -->
<!--   Base-Seed-Fixture: tests/conftest.py:95 `User(id=1, org_id=1, email="pgtest-base@nerve.local")` (session-autouse, id=1) — setzt email_confirmed NICHT. -->
<!--   ~8 Test-Dateien bauen EIGENE User inline (NICHT ueber einen shared Helper): test_admin_dashboard_auth.py:32, test_ewb_rate_api.py:94, test_waitlist_admin_auth.py:52, test_logs_org_boundary.py:66, test_logs_founder_access.py:64, test_profile_editor_validation.py:73, test_login_destination_wiring.py:131/199. -->
<!--   → EINE zentrale Setzung, die ALLE inline-User + die Base-Seed abdeckt: ein SQLAlchemy before_insert-Event-Listener auf dem User-Mapper (in conftest.py registriert), der email_confirmed=True defaultet WENN der Test es nicht explizit gesetzt hat. Deckt alle 8 Dateien in EINER Stelle (Punkt 27 — nicht 8 Dateien anfassen). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic-Migration 0033 — DB-Default False + Schild</name>
  <read_first>
    - alembic/versions/0032_onboarding_state_skip_onboarding.py (Hausmuster: op.alter_column, op.execute COMMENT, upgrade/downgrade)
    - alembic/versions/ (`ls` — bestaetigen dass 0032 der Head ist, kein 0033 existiert)
    - database/models.py:135 (aktueller Spalten-Zustand + Comment — per INHALT ankern)
    - CONTEXT.md persistenz_beleg (nullable=YES, column_default leer, 0 NULL-Rows)
  </read_first>
  <action>
    Neue Datei alembic/versions/0033_email_confirmed_default_false.py, Muster wie 0032:
    - `revision = '0033'`, `down_revision = '0032'`, branch_labels=None, depends_on=None.
    - Docstring: erklaert D-03b (Default-Flip True→False belt-and-suspenders, Fail-Open-Wurzel
      war nullable ohne DB-Default), + Prod-Ist-Stand (nullable=YES, column_default leer,
      3 Konten=true, 0 NULL-Rows → KEIN Backfill). + Hinweis: laeuft ZULETZT (Wave 3), reiner
      No-Op weil Plan 03 jeden Pfad schon explizit setzt.
    upgrade():
    - `op.alter_column('users', 'email_confirmed', server_default=sa.false())` (setzt DB-Default False).
      ODER explizit `op.execute("ALTER TABLE users ALTER COLUMN email_confirmed SET DEFAULT false")`.
    - KEIN `UPDATE users SET email_confirmed=False` (kein Backfill — 0 NULL-Rows, Bestand=true bleibt).
      Als Kommentar dokumentieren WARUM kein Backfill (die 3 true-Konten duerfen NICHT geflippt werden).
    - Schild (Punkt 23) via op.execute COMMENT ON COLUMN users.email_confirmed IS
      '<Zweck: Email bestaetigt — fail-closed Gate liest, Form-Register/OAuth-Microsoft setzen False>.
      Status: lebt. Schreibt routes/oauth.py (Microsoft=False/Google=True), routes/auth.py
      (api_register=False, confirm_email=True, invite=True), app.py+scripts/seed (seed=True),
      DB-Default False (Migration 0033); liest routes/auth.py login_required-Gate.'
    downgrade():
    - `op.alter_column('users','email_confirmed', server_default=sa.true())` (zurueck auf True-Default).
    - Schild-COMMENT zuruecksetzen auf den H-18-Text.
    Committe: `feat(08.23.2.AUTH-EMAIL-VERIFY-02): migration 0033 email_confirmed DB-default False + Schild (D-03b)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate + SCHILD-Guard (test_schild_guard.py) gruen; Migration selbst wird MANUELL gefahren (Task 4 Checkpoint)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "revision = '0033'" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -n "down_revision = '0032'" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -niE "SET DEFAULT false|server_default=sa.false" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -ni "COMMENT ON COLUMN users.email_confirmed" alembic/versions/0033_email_confirmed_default_false.py` vorhanden
    - `grep -niE "backfill|UPDATE users SET email_confirmed" alembic/versions/0033_email_confirmed_default_false.py` — nur im Kommentar (kein echtes UPDATE-Statement gegen Bestand)
  </acceptance_criteria>
  <done>Migration 0033 flippt DB-Default auf False, kein Backfill, Schild aktuell (deckt alle Schreiber inkl. invite/seed).</done>
</task>

<task type="auto">
  <name>Task 2: models.py ORM-Default False + Schild-Comment</name>
  <read_first>
    - database/models.py:132-135 (email_confirmed Column + H-18-Kommentar — per INHALT ankern)
  </read_first>
  <action>
    In database/models.py die email_confirmed-Column-Definition (~:135) aendern:
    `default=True` → `default=False` (ORM-Default; `nullable=True` bleibt — kein NOT-NULL,
    weil Bestand-Rows nicht angefasst werden). Das `comment=`-Argument auf den Schild-Text
    aus Task 1 aktualisieren (identisch zur Migration, Punkt 23 Aktualitaets-Pflicht:
    Schreibt oauth.py/auth.py(api_register/confirm/invite)/seed + DB-Default False; liest login_required-Gate).
    Die Kommentar-Zeilen ~:132-134 auf die neue Semantik anpassen: "Default=False (AUTH-EMAIL-VERIFY
    D-03b): neue Inserts unbestaetigt bis Confirm; fail-closed Gate. Bestand-Rows unveraendert (kein
    Backfill). Laeuft ZULETZT (Wave 3) — reiner No-Op, weil jeder Creator (Plan 03) schon explizit setzt."
    ★ Naht (Punkt 14): NUR den Default aendern. `_create_org_and_user` (auth.py:182) setzt
    email_confirmed NICHT explizit — der ORM-Default greift dort jetzt False; aber jeder REALE Caller
    (api_register, OAuth, Invite — alle aus Plan 03) setzt danach explizit → der Default ist nie wirksam.
    Kein Edit an models.py User(...)-Callern hier.
    Committe: `feat(08.23.2.AUTH-EMAIL-VERIFY-02): ORM-default email_confirmed False + Schild (D-03b)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate + SCHILD-Guard gruen (nach Task 3 conftest-Helfer)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "email_confirmed.*default=False" database/models.py` vorhanden
    - `grep -n "email_confirmed.*default=True" database/models.py` liefert 0 Treffer
    - `grep -n "D-03b" database/models.py` vorhanden (Naht-Kommentar)
    - `grep -ni "Schreibt routes/oauth.py\|liest routes/auth.py" database/models.py` vorhanden (Schild deckt Leser+Schreiber)
  </acceptance_criteria>
  <done>ORM-Default False, Schild aktuell + deckt alle Leser/Schreiber, nullable unveraendert.</done>
</task>

<task type="auto">
  <name>Task 3: Zentraler Test-Helfer — email_confirmed=True fuer test-gebaute User (Finding 2)</name>
  <read_first>
    - tests/conftest.py:64-109 (Base-Seed-Fixture, User(id=1) :95)
    - tests/conftest.py:1-30 (Imports: User-Model, event? — pruefen ob `from sqlalchemy import event` noetig)
    - `grep -rn "email_confirmed" tests/` (= 0 heute — bestaetigen; nach diesem Task nur die zentrale Stelle)
    - `grep -n "User(" tests/test_admin_dashboard_auth.py tests/test_login_destination_wiring.py` (Stichprobe: inline-gebaute User setzen email_confirmed NICHT → sollen True defaulten)
  </read_first>
  <action>
    ★ Ziel (Punkt 27 — EINE zentrale Stelle statt 8 Dateien): nach dem ORM-Default-Flip (Task 2)
    bekommen test-gebaute User email_confirmed=False → @login_required-Requests in ~8 Test-Dateien
    laufen in 302/Warteseite → Deploy-Gate rot. Zentral loesen in tests/conftest.py.

    EMPFOHLEN (deckt ALLE inline-gebauten User + Base-Seed in einer Stelle): ein SQLAlchemy
    `before_insert`-Event-Listener auf dem User-Mapper, in conftest.py auf Modul-Ebene registriert:
    ```python
    from sqlalchemy import event
    from database.models import User

    @event.listens_for(User, "before_insert")
    def _test_default_email_confirmed(mapper, connection, target):
        # Finding 2 (AUTH-EMAIL-VERIFY): nach dem Prod-Default-Flip (False) wuerden test-gebaute
        # User gegatet werden und @login_required-Tests brechen. Test-Umgebung: unbestaetigt-Default
        # ist hier irrelevant; wir defaulten auf True, WENN der Test nichts explizit gesetzt hat.
        # Tests, die das Gate-/Creation-Verhalten pruefen (Plan 01/03), setzen email_confirmed
        # EXPLIZIT (NULL/False/True) → dieser Default greift dort NICHT (nur bei None).
        if target.email_confirmed is None:
            target.email_confirmed = True
    ```
    Der `is None`-Guard ist entscheidend (Punkt-27-Korrektheit): er ueberschreibt NIE einen
    explizit gesetzten Wert — die Fail-Closed-Guard-Tests (Plan 01: NULL/False/True) und die
    Creation-Path-Tests (Plan 03: api_register=False etc.) setzen explizit und bleiben unberuehrt.
    NUR test-gebaute User, die gar nichts setzen (die ~8 @login_required-Dateien + Base-Seed),
    bekommen True.

    ★ WICHTIG: Der Listener wird NUR in tests/conftest.py registriert (Test-Scope), NICHT in
    database/models.py — Prod-Verhalten (Default False) bleibt unberuehrt. Das ist kein
    Source-Presence-Umgehen: es ist eine Test-Fixture-Konvention, kein App-Code.

    Zusaetzlich (Guertel): die Base-Seed-Fixture (conftest.py:95) explizit `email_confirmed=True`
    an `User(id=1, ...)` geben — belt-and-suspenders, falls der Listener-Scope je enger wird.

    ALTERNATIVE (falls der Event-Listener in der PGTEST-Fixture-Architektur Nebenwirkungen zeigt,
    z.B. mit dem before_insert-Hook fuer tenant_orgs kollidiert): stattdessen die Base-Seed +
    jede der 8 inline-Stellen minimal patchen — ABER das ist 8 Dateien (Anti-Punkt-27), nur
    Fallback. Zuerst den zentralen Listener versuchen.

    Committe: `test(08.23.2.AUTH-EMAIL-VERIFY-02): central email_confirmed=True default for test-built users (Finding 2, keeps deploy gate green after default-flip)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: die ~8 @login_required-Test-Dateien wieder gruen NACH dem Default-Flip; Plan-01/03-Guards weiterhin gruen (explizite Werte unberuehrt)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "email_confirmed" tests/conftest.py` zeigt die zentrale Setzung (Listener + `is None`-Guard, ODER Base-Seed True)
    - `grep -n "before_insert\|email_confirmed = True\|email_confirmed=True" tests/conftest.py` vorhanden
    - `grep -n "is None" tests/conftest.py` im email_confirmed-Kontext (kein Ueberschreiben expliziter Werte)
    - `grep -rn "email_confirmed" tests/` zeigt: die Plan-01-Gate-Tests + Plan-03-Creation-Tests setzen weiterhin EXPLIZIT (nicht vom Default abhaengig)
    - Deploy-Gate gruen: die 8 @login_required-Dateien (admin_dashboard_auth, ewb_rate_api, waitlist_admin_auth, logs_org_boundary, logs_founder_access, profile_editor_validation, login_destination_wiring) passieren
  </acceptance_criteria>
  <done>Test-gebaute User bekommen zentral email_confirmed=True (ein Ort); explizite Werte (Plan 01/03) unberuehrt; Deploy-Gate gruen nach Default-Flip.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Prod-Migration 0033 fahren (Deploy-Order, Punkt 26)</name>
  <what-built>
    Migration 0033 (DB-Default False) + models.py ORM-Default False + zentraler Test-Helfer (Finding 2).
    Alle committet + gepusht. ★ Laeuft ZULETZT (Wave 3): Plan 01 (Gate) + Plan 03 (explizite Setter)
    sind bereits live → dieser Flip ist ein garantierter No-Op fuer alle existierenden Pfade.
  </what-built>
  <how-to-verify>
    ★ Deploy-Order (Punkt 26 — niedrig-Risiko, weil KEIN neuer Spalten-Read und nullable bleibt,
    UND weil Plan 03 jeden Pfad schon explizit setzt → der Flip aendert kein Laufzeit-Verhalten):
    Die Migration aendert NUR den DB-Default fuer KUENFTIGE Inserts ohne expliziten Wert — sie bricht
    keinen bestehenden Leser (kein `column does not exist`-Risiko, email_confirmed existiert seit H-18).
    Reihenfolge:
    1. Code deployen (models.py ORM-Default False + conftest-Helfer) via `bash deploy.sh production` —
       der Pytest-Gate laeuft auf nerve_test VOR dem Restart; bei Test-Fehler kein Restart, alter Code
       bleibt live. ★ Genau hier greift Finding 2: ohne den conftest-Helfer waere der Gate rot.
    2. Migration manuell auf Prod als postgres fahren (memory project_nerve_prod_deploy):
       `ssh -i ~/.ssh/nerve_vps root@178.104.82.166` → als postgres alembic upgrade head (0032→0033).
    3. Verifizieren via inspect.sh (read-only): `bash scripts/inspect.sh schema users` →
       email_confirmed column_default sollte jetzt `false` sein; `bash scripts/inspect.sh count users`
       + Bestand-Check: die 3 Konten weiterhin email_confirmed=true (KEIN Backfill hat sie geflippt).
    4. `bash scripts/inspect.sh migrations` → alembic_version = 0033.
    5. Regressions-Sicht: ein bereits (in Plan 03) live-getesteter Register→Confirm-Flow funktioniert
       weiter identisch (Flip ist No-Op — api_register setzt False explizit, unabhaengig vom Default).
    André klickt/bestaetigt: Migration lief, 3 Bestandskonten unveraendert true, DB-Default=false,
    Register-Flow unveraendert.
  </how-to-verify>
  <resume-signal>Type "migration-live" wenn 0033 auf Prod, Bestand unveraendert, DB-Default=false, Register-Flow unveraendert — oder beschreibe Fehler.</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `users` (SCHREIBEN: DB-Default-Aenderung der Spalte `email_confirmed`; KEIN Row-Backfill).

### Beleg (verbatim Prod-`nerve`, Claudian 2026-07-09 — NICHT zusammenfassen)
```
users.email_confirmed (Prod-nerve, \d users / information_schema.columns, 2026-07-09):
  data_type   = boolean
  is_nullable = YES
  column_default = (leer / kein DB-Default)   ← Fail-Open-Wurzel: Insert ohne Wert → NULL
Real-Daten: id=1 true, id=2 true, id=3 true (alle 3 Bestandskonten bestätigt; 0 NULL-Rows aktuell).
models.py:135 = Column(Boolean, default=True, nullable=True)  ← ORM-Default True driftet zur Prod-Realität (kein DB-Default).
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt / Leser-Voraus? | Verifiziert? |
|---|---|---|---|---|
| `email_confirmed` DB-Default | GESCHRIEBEN via ALTER COLUMN SET DEFAULT (Migration 0033) | DB-Spalte `users.email_confirmed` (boolean, nullable) | Migration einmalig (DDL). Betrifft nur KUENFTIGE Inserts OHNE expliziten Wert. Kein Leser-Voraus-Problem (Punkt 26): DDL vor jedem neuen Insert. Kein realer Pfad verlaesst sich darauf (Plan 03 setzt alle explizit). | ✓ CONTEXT-Beleg |
| `email_confirmed` ORM-Default | GESCHRIEBEN bei `User(...)` ohne expliziten Wert (SQLAlchemy) | dito (Python-seitig gesetzt beim flush) | Insert-Zeit; deterministisch; nur wenn Caller nichts setzt (kein realer Prod-Caller mehr, Plan 03). | ✓ models.py:135 |
| 3 Bestand-Rows (true) | UNBERUEHRT (kein UPDATE) | dito | — | ✓ CONTEXT: 0 NULL-Rows, kein Backfill noetig |
| Test-gebaute User (conftest) | GESCHRIEBEN via before_insert-Listener email_confirmed=True (Finding 2) | dito (Test-Scope, nerve_test) | Test-Insert-Zeit; nur wenn Test nichts explizit setzt (`is None`) | ✓ Task 3 grep-Beleg |

### NULL-vs-False Layer-Subtilität
Die Migration eliminiert die NULL-QUELLE fuer neue Inserts (DB-Default False statt kein-Default).
Zusammen mit dem fail-closed Gate (Plan 01, das NULL SOWIESO gatet) + den expliziten Settern (Plan 03,
jeder Pfad setzt selbst) DREIFACHE Absicherung. Dieser Flip ist die HINTERSTE Linie (Wave 3) und ein
No-Op fuer alle heute existierenden Pfade — reine Zukunfts-Absicherung gegen vergessene kuenftige Creator.
Kein neuer Spalten-Read → kein Deploy-Crash-Risiko (anders als AUTH-2 onboarding_state).

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Künftiger Code-Pfad (neuer User-Creator) → users-INSERT | Ein neuer Creator könnte email_confirmed vergessen → ohne DB-Default = NULL. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-EMAILVERIFY-04 | Elevation of Privilege | users-INSERT ohne email_confirmed | mitigate | DB-Default False (0033) + ORM-Default False: vergessener Setter landet False (gegatet), nicht NULL (fail-open). Hinterste Linie (Wave 3) unter Plan 01+03. |
| T-EMAILVERIFY-05 | Tampering | versehentliches Flippen der 3 Bestandskonten | mitigate | KEIN Backfill-UPDATE in der Migration; DDL-only. inspect.sh-Verifikation im Checkpoint dass Bestand=true bleibt. |
| T-EMAILVERIFY-06 | Denial of Service | Migration bricht Prod-Login | accept | Kein neuer Spalten-Read; nullable bleibt; email_confirmed existiert seit H-18. Kein `column does not exist`-Risiko. No-Op weil Plan 03 alle Pfade explizit setzt. Deploy-Order dennoch explizit (Checkpoint). |
| T-EMAILVERIFY-06b | Denial of Service | Default-Flip bricht das eigene Deploy-Gate (test-User → False → 302) | mitigate | Finding 2: zentraler conftest-Helfer (email_confirmed=True fuer test-gebaute User, `is None`-Guard) im SELBEN Deploy → Gate bleibt gruen; Plan-01/03-Guards (explizite Werte) unberuehrt. |
</threat_model>

<verification>
- 0033 hat down_revision 0032, ALTER SET DEFAULT false, COMMENT ON COLUMN, KEIN Backfill-UPDATE.
- models.py default=False, Schild deckt alle Leser/Schreiber inkl. invite/seed (grep).
- conftest-Helfer setzt email_confirmed=True fuer test-gebaute User zentral (`is None`-Guard); Plan-01/03-Guards unberuehrt.
- SCHILD-Guard (test_schild_guard.py) + die 8 @login_required-Test-Dateien gruen im Deploy-Gate.
- Prod nach Migration: DB-Default=false, alembic_version=0033, 3 Konten weiterhin true, Register-Flow unveraendert (Checkpoint).
</verification>

<success_criteria>
- Neue Inserts ohne email_confirmed = False (nicht NULL).
- Bestand (3× true) unveraendert; Flip ist No-Op fuer alle existierenden Pfade (Plan 03 setzt explizit).
- Schild aktuell + vollstaendig (Punkt 23).
- Eigenes Deploy-Gate bleibt gruen (Finding 2 conftest-Helfer).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-EMAIL-VERIFY-email-bestaetigung-registrierung/08.23.2.AUTH-EMAIL-VERIFY-02-SUMMARY.md`
</output>
