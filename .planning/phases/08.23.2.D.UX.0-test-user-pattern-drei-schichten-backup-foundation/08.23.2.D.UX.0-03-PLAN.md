---
phase: 08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation
plan: 03
type: execute
wave: 2
depends_on: [01]   # braucht training-Schema + nerve_anon_worker-Rolle + pg_hba host-Auth (pg_dump -h 127.0.0.1 --schema=training)
files_modified:
  - scripts/backup_training_to_s3.sh
  - scripts/restore_test_s3.sh
  - deploy/nerve-backup-training.service
  - deploy/nerve-backup-training.timer
  - deploy/nerve-restore-test.service
  - deploy/nerve-restore-test.timer
  - routes/app_routes.py
autonomous: false   # IONOS account/AVV/bucket setup is human-action gated
covers: [C-01, C-02, C-03, C-04, C-05, C-06, C-07, C-08, C-09, P-01, P-03, P-04, P-05]
requirements: [C-01, C-02, C-03, C-04, C-05, C-06, C-07, C-08, C-09]
user_setup:
  - service: ionos-s3
    why: "Backup-Schicht 3 WORM-Ziel (deutscher Anbieter, DSGVO-eindeutig, kein Drittland — C-01)"
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "IONOS Console → Object Storage → Key Management"
      - name: AWS_SECRET_ACCESS_KEY
        source: "IONOS Console → Object Storage → Key Management"
    dashboard_config:
      - task: "IONOS-Account anlegen + AVV abschliessen (deutsch, kostenlos)"
        location: "IONOS Cloud → Vertrag / DSGVO"
      - task: "Bucket nerve-training-backup MIT Object-Lock erstellen (--object-lock-enabled-for-bucket — NUR bei Erstellung moeglich, NICHT nachruestbar). Default-Retention COMPLIANCE 30d wird ERST nach erfolgreichem 1-Tages-Smoke-Restore gesetzt (staged WORM bring-up, C-BLOCKER-4)"
        location: "aws s3 mb ... --object-lock-enabled-for-bucket (RESEARCH Pattern 3)"

must_haves:
  truths:
    - "backup_training_to_s3.sh dumpt training-Schema als nerve_anon_worker (pg_dump -h 127.0.0.1, TCP) und laedt es per aws s3api put-object mit Object-Lock COMPLIANCE +30d auf IONOS hoch"
    - "Der IONOS-Bucket wurde mit Object-Lock enabled erstellt; 30d-COMPLIANCE-Default wird ERST nach erfolgreichem 1-Tages-Smoke-Restore aktiviert (staged WORM, C-BLOCKER-4)"
    - "Bucket-Versioning ist Enabled (Object-Lock-Voraussetzung, C-WARN-3)"
    - "aws-cli ist v2 (aws --version meldet aws-cli/2., C-WARN-1)"
    - "Server-side Encryption at rest ist verifiziert (get-bucket-encryption)"
    - "Ein monatlicher Restore-Test zieht ein Objekt, prueft Integritaet und schreibt /opt/nerve/.s3_restore_status"
    - "/api/health gibt backup_s3_restore_ok + backup_s3_restore_checked_at zurueck"
    - "Objekte aelter als 365 Tage (Retention abgelaufen) werden rotiert"
  artifacts:
    - path: "scripts/backup_training_to_s3.sh"
      provides: "Schicht-3 WORM-Upload (TCP-pg_dump) + 365d-Rotation"
      contains: "object-lock-mode COMPLIANCE"
    - path: "scripts/restore_test_s3.sh"
      provides: "monatlicher Restore-Integritaets-Test → .s3_restore_status"
      contains: "S3_RESTORE_OK"
    - path: "routes/app_routes.py"
      provides: "backup_s3_restore_ok in /api/health"
      contains: "backup_s3_restore_ok"
  key_links:
    - from: "scripts/backup_training_to_s3.sh"
      to: "IONOS S3 Bucket (WORM)"
      via: "aws s3api put-object --object-lock-mode COMPLIANCE"
      pattern: "object-lock-mode COMPLIANCE"
    - from: "scripts/restore_test_s3.sh"
      to: "/opt/nerve/.s3_restore_status"
      via: "write S3_RESTORE_OK=true/false"
      pattern: "S3_RESTORE_OK="
    - from: "routes/app_routes.py api_health"
      to: "/opt/nerve/.s3_restore_status"
      via: "flat-file read (analog deploy_meta)"
      pattern: "s3_restore_status"
---

<objective>
Backup-Schicht 3: WORM-Backup des training-Schemas auf IONOS S3 mit Object-Lock COMPLIANCE
(30d), eigener systemd-Timer, monatlicher automatischer Restore-Test mit /api/health-Metrik,
365d-Rotation, Encryption-at-rest-Verifikation. Eigenes Skript (NICHT backup_postgres.sh
erweitern — andere Kadenz, Rolle, Credentials, C-04).

Purpose: Existenz-Versicherung gegen Ransomware / Hetzner-Account-Kompromittierung. Anderer
Anbieter (IONOS, DE), WORM = niemand kann vor Ablauf loeschen (auch nicht mit Root-Key, C-02).
Nur training.* (anonymisiert → kein DSGVO-Issue beim Cross-Anbieter-Transfer, C-03).
Output: backup_training_to_s3.sh + restore_test_s3.sh + 4 systemd-Units + /api/health-Erweiterung.

⚠️ STAGED WORM BRING-UP (C-BLOCKER-4): COMPLIANCE ist irreversibel — niemand (auch nicht
Root/Master-Key) kann ein Objekt vor Ablauf loeschen. Eine Fehlkonfiguration des ERSTEN Uploads
= 30 Tage un-loeschbarer Muell. Deshalb wird der 30d-COMPLIANCE-Default NICHT bei Bucket-
Erstellung scharf geschaltet, sondern erst nachdem ein 1-Tages-Smoke-Restore-Roundtrip
erfolgreich war (≤1 Tag Risiko statt 30). C-02 (30d-COMPLIANCE) bleibt der finale End-Zustand —
nur der Rollout ist gestaffelt.

⚠️ WICHTIG (C-03): Backup ist VOLLER `pg_dump --schema=training` OHNE Filter. Der is_test_user-
Filter greift WRITE-TIME im D.UX.1-Background-Job (Test-Calls landen nie in training.*), NICHT
beim Backup. Die Roadmap-Skizze `WHERE source_call_hash NOT IN (...)` wird NICHT umgesetzt (haette
FK-Bruecke training.*→calls gebraucht = ADR-Verletzung). D.UX.0 dokumentiert die Write-Time-Regel
als Contract (siehe unten), D.UX.1 implementiert sie.

⚠️ KEINE UI-Aenderungen (P-05). /api/health-Feld ist JSON, keine UI. KEINE lokalen Tests (P-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-CONTEXT.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-RESEARCH.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-PATTERNS.md

<interfaces>
<!-- routes/app_routes.py api_health() — bestehender return (Z.1632-1640, VERIFIZIERT durch Read): -->
```python
    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
        'pipeline_status': pipeline_status,
        'pipeline_error_count_10min': pipeline_error_count,
        'git_head': git_head,
        'deployed_at': deployed_at,
    })
```
<!-- Flat-file-read-Pattern fuer deploy_meta (Z.~1582-1595) ist die Vorlage fuer .s3_restore_status -->
<!-- IONOS-Credentials-Template /etc/nerve/ionos-s3.env (RESEARCH.md Z.629-637): -->
<!--   AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / IONOS_S3_ENDPOINT / IONOS_S3_BUCKET / NERVE_ANON_WORKER_DB_PASSWORD -->
<!-- KANONISCHES IONOS Endpoint/Region-Paar (C-WARN-2, gegen IONOS-Docs verifiziert 2026-05-29): -->
<!--   User-owned Bucket Frankfurt: Endpoint https://s3.eu-central-1.ionoscloud.com  ↔  --region de -->
<!--   (IONOS docs: user-owned buckets nutzen Region-String "de" mit dem eu-central-1-Host. -->
<!--    NICHT "eu-central-1" als Region-String. Contract-owned waere s3.eu-central-4 ↔ eu-central-4 — -->
<!--    hier NICHT verwenden. Dieses Paar EINHEITLICH ueberall: mb / put-object / restore / ionos-s3.env.) -->
<!-- systemd-Vorlage: docs/systemd-backup-setup.md (nerve-backup.service/.timer) -->
<!-- backup-Skript-Vorlage: scripts/backup_postgres.sh + PATTERNS.md Z.353-430 (Vollvorlage backup_training_to_s3.sh) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 [BLOCKING]: IONOS-Account + AVV + Object-Lock-Bucket (staged WORM) + Versioning + aws-cli v2 + ionos-s3.env</name>
  <read_first>
    - CONTEXT.md C-01 (Account+AVV, Andre) / C-02 (Object-Lock COMPLIANCE 30d + Versioning, finaler End-Zustand) / C-06 (ionos-s3.env chmod 600) / C-09 (Encryption verifizieren)
    - RESEARCH.md Pattern 3 (Bucket-Erstellung mit Object-Lock — exakte aws-Befehle + Endpoint) + Pitfall 1 (Object-Lock NUR bei Erstellung, nicht nachruestbar) + Open Question 3/4 (Region + aws-cli-Install)
    - RESEARCH.md Code Examples Z.626-637 (ionos-s3.env-Template)
    - <interfaces> oben: KANONISCHES Endpoint/Region-Paar (C-WARN-2)
  </read_first>
  <what-built>
Skript- + systemd- + /api/health-Verdrahtung (Tasks 2-3) ist autonom. Aber WORM-Upload braucht
einen existierenden Object-Lock-Bucket + Credentials. Account/AVV/Bucket-Erstellung sind manuell
(IONOS-Console + Credential-Generierung). Object-Lock ist NICHT nachruestbar → Bucket MUSS direkt
mit Object-Lock-enabled erstellt werden (Pitfall 1). Der scharfe 30d-COMPLIANCE-Default wird
GESTAFFELT scharf geschaltet (C-BLOCKER-4): erst Bucket+Versioning, dann 1-Tages-Smoke-Roundtrip,
dann — und nur dann — 30d-COMPLIANCE.
  </what-built>
  <how-to-verify>
**Reihenfolge ist Pflicht (staged WORM bring-up, C-BLOCKER-4). 30d-COMPLIANCE NICHT vor dem
1-Tages-Smoke-Restore aktivieren.**

1. IONOS-Cloud-Account anlegen + AVV abschliessen (deutsch, kostenlos, C-01). Object-Storage-
   Access-Key + Secret in der Console generieren.

2. **aws-cli v2 installieren (C-WARN-1).** `apt-get install awscli` liefert v1 — NICHT verwenden
   (`s3api put-object --object-lock-*` ist nur auf v2 zuverlaessig). Offizieller v2-Zip-Installer:
   ```
   ssh -i ~/.ssh/id_ed25519_nerve root@<prod> '\
     curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o /tmp/awscliv2.zip \
     && unzip -o /tmp/awscliv2.zip -d /tmp \
     && /tmp/aws/install --update \
     && aws --version'
   ```
   → `aws --version` MUSS `aws-cli/2.` melden (nicht `aws-cli/1.`). Falls v1 vorhanden war:
   `--update` ueberschreibt. In Persistenz-Sektion zitieren.

3. **Kanonisches Endpoint/Region-Paar setzen (C-WARN-2)** — EINHEITLICH ueberall verwenden:
   ```
   export AWS_ACCESS_KEY_ID=...; export AWS_SECRET_ACCESS_KEY=...
   IONOS_ENDPOINT="https://s3.eu-central-1.ionoscloud.com"
   IONOS_REGION="de"   # user-owned Bucket Frankfurt → Region "de" zum eu-central-1-Host (IONOS docs, C-WARN-2)
   ```
   Connectivity-/Paar-Verifikation BEVOR irgendwas Irreversibles passiert:
   ```
   aws s3api list-buckets --region "$IONOS_REGION" --endpoint-url "$IONOS_ENDPOINT"
   ```
   → muss ohne Auth/Region-Fehler durchlaufen (leere oder vorhandene Bucket-Liste). Schlaegt das
   fehl → Endpoint/Region-Paar oder Credentials falsch → STOP, NICHT weitermachen (kein Bucket
   anlegen bevor das Paar bestaetigt ist).

4. **Bucket MIT Object-Lock erstellen — ABER noch OHNE 30d-COMPLIANCE-Default** (Pitfall 1 +
   C-BLOCKER-4 Schritt 1):
   ```
   aws s3api create-bucket --bucket nerve-training-backup \
     --object-lock-enabled-for-bucket \
     --region "$IONOS_REGION" --endpoint-url "$IONOS_ENDPOINT"
   ```
   (Object-Lock-enabled aktiviert implizit Versioning. KEINE put-object-lock-configuration mit
   30d hier — die kommt erst in Schritt 8.)

5. **Versioning verifizieren (C-WARN-3)** — Object-Lock setzt Versioning voraus:
   ```
   aws s3api get-bucket-versioning --bucket nerve-training-backup \
     --endpoint-url "$IONOS_ENDPOINT"
   ```
   → `Status: Enabled`. Falls nicht Enabled: explizit setzen
   (`aws s3api put-bucket-versioning --versioning-configuration Status=Enabled ...`) und erneut
   pruefen. In Persistenz-Sektion zitieren.

6. **1-Tages-Smoke put-object (C-BLOCKER-4 Schritt 2)** — COMPLIANCE, aber nur 1 Tag Risiko:
   ```
   echo "smoke-$(date -u +%Y%m%dT%H%M%SZ)" | gzip > /tmp/s3-smoke.gz
   RETAIN_1D=$(date -u -d '+1 day' +%Y-%m-%dT%H:%M:%SZ)
   aws s3api put-object --bucket nerve-training-backup --key smoke-test.sql.gz \
     --body /tmp/s3-smoke.gz \
     --object-lock-mode COMPLIANCE --object-lock-retain-until-date "$RETAIN_1D" \
     --endpoint-url "$IONOS_ENDPOINT"
   ```

7. **Smoke-Roundtrip verifizieren (C-BLOCKER-4 Schritt 3)** — Download + Integritaet + Lock-Status:
   ```
   aws s3api get-object --bucket nerve-training-backup --key smoke-test.sql.gz \
     /tmp/s3-smoke-dl.gz --endpoint-url "$IONOS_ENDPOINT"
   gunzip -t /tmp/s3-smoke-dl.gz && echo "SMOKE-RESTORE-OK"
   aws s3api head-object --bucket nerve-training-backup --key smoke-test.sql.gz \
     --endpoint-url "$IONOS_ENDPOINT"   # zeigt ObjectLockMode=COMPLIANCE + RetainUntilDate (~+1d)
   ```
   → `SMOKE-RESTORE-OK` UND head-object zeigt COMPLIANCE mit RetainUntil ~+1 Tag. Erst wenn das
   gruen ist, weiter zu Schritt 8. (Das Smoke-Objekt ist max. 1 Tag un-loeschbar — akzeptables
   Risiko statt 30 Tagen.)

8. **ERST JETZT: 30d-COMPLIANCE-Default scharf schalten (C-BLOCKER-4 Schritt 4 / C-02 End-Zustand):**
   ```
   aws s3api put-object-lock-configuration --bucket nerve-training-backup \
     --object-lock-configuration '{"ObjectLockEnabled":"Enabled","Rule":{"DefaultRetention":{"Mode":"COMPLIANCE","Days":30}}}' \
     --endpoint-url "$IONOS_ENDPOINT"
   ```

9. /etc/nerve/ionos-s3.env anlegen (chmod 600, root:root, C-06) mit dem KANONISCHEN Paar:
   AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / IONOS_S3_ENDPOINT=https://s3.eu-central-1.ionoscloud.com /
   IONOS_S3_REGION=de / IONOS_S3_BUCKET=nerve-training-backup /
   NERVE_ANON_WORKER_DB_PASSWORD=<aus Plan 01 Task 2>.
   `chmod 600 /etc/nerve/ionos-s3.env && chown root:root /etc/nerve/ionos-s3.env`. NIEMALS in git.

10. **Object-Lock-Default-Verifikation (C-02):**
    `aws s3api get-object-lock-configuration --bucket nerve-training-backup --endpoint-url "$IONOS_ENDPOINT"`
    → ObjectLockEnabled: Enabled, Mode: COMPLIANCE, Days: 30.

11. **Encryption-at-rest-Verifikation (C-09):**
    `aws s3api get-bucket-encryption --bucket nerve-training-backup --endpoint-url "$IONOS_ENDPOINT"`
    → SSE-Konfiguration (AES256). Falls leer/Fehler: Finding notieren, ggf. --server-side-encryption AES256 beim put-object (Assumption A2).
  </how-to-verify>
  <resume-signal>Type "ionos ready endpoint=https://s3.eu-central-1.ionoscloud.com region=de bucket=nerve-training-backup awscli=2.x versioning=Enabled smoke-restore=OK objectlock=COMPLIANCE/30d sse=<status>" — d.h. NUR bestaetigen wenn der 1-Tages-Smoke-Restore erfolgreich war BEVOR der 30d-COMPLIANCE-Default scharf geschaltet wurde. SSE-Status (verifiziert/Finding) beschreiben.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: backup_training_to_s3.sh (TCP-pg_dump + WORM-Upload + 365d-Rotation) + Write-Time-Filter-Contract dokumentieren</name>
  <files>scripts/backup_training_to_s3.sh</files>
  <read_first>
    - scripts/backup_postgres.sh (Struktur-Analog: set -e, Konstanten, pg_dump|gzip, Size-Check, Rotation)
    - PATTERNS.md Z.353-430 (backup_training_to_s3.sh Vollvorlage)
    - RESEARCH.md Pattern 4 (put-object mit COMPLIANCE) + Pitfall 2 (aws s3 cp kann KEIN Object-Lock → put-object Pflicht) + C-03 (voller Dump, kein Filter) + C-05 (365d) + C-07 (aws-cli)
    - CONTEXT.md C-03 (Write-Time-Filter-Contract — D.UX.0 dokumentiert, D.UX.1 implementiert)
    - Plan 01 Task 2 (G-2): pg_hba host-Auth-Zeile fuer nerve_anon_worker MUSS gesetzt sein → pg_dump nutzt -h 127.0.0.1 (TCP, scram/md5), NICHT den Unix-Socket (peer auth wuerde PGPASSWORD ignorieren)
    - Resume-Signal aus Task 1 (Endpoint/Region/Bucket)
  </read_first>
  <action>
**scripts/backup_training_to_s3.sh NEU** (PATTERNS.md-Vollvorlage). Kern-Punkte:
- `set -euo pipefail`
- `source /etc/nerve/ionos-s3.env` (Fehler-Exit wenn fehlt)
- pg_dump als nerve_anon_worker via PGPASSWORD UEBER TCP (G-2):
  `pg_dump -h 127.0.0.1 -U nerve_anon_worker -d nerve --schema=training --no-owner --no-acl | gzip > $TMP_FILE`
  **WICHTIG (G-2): `-h 127.0.0.1` ist Pflicht.** Der systemd-Service laeuft als `User=root`. Ohne
  `-h` connectet pg_dump ueber den lokalen Unix-Socket → Postgres matcht `peer`-Auth (OS-User root
  vs DB-User nerve_anon_worker) → PGPASSWORD wird ignoriert → Connection abgelehnt. Mit
  `-h 127.0.0.1` greift die `host`-Regel aus pg_hba.conf (scram-sha-256, Plan 01 Task 2) und
  PGPASSWORD wird korrekt genutzt. (VOLLER Dump, KEIN Filter — C-03)
- Size-Check (MIN_SIZE_BYTES=512 — training startet leer)
- Upload mit `aws s3api put-object --object-lock-mode COMPLIANCE --object-lock-retain-until-date $(date -u -d '+30 days' +%Y-%m-%dT%H:%M:%SZ)` (Pitfall 2: KEIN `aws s3 cp`)
- Cleanup Temp-File
- 365d-Rotation mit grep-Guard (G-3): `aws s3 ls` → `(grep '^training-' || true)` → Dateinamen-Datum parsen → `aws s3api delete-object` fuer abgelaufene (Retention vorbei → delete erlaubt, C-05)

Exakter Inhalt:
```bash
#!/bin/bash
# scripts/backup_training_to_s3.sh — NERVE Training Schema Backup (Schicht 3)
# Phase 08.23.2.D.UX.0. systemd nerve-backup-training.timer (woechentlich). User: root.
#
# C-03 WRITE-TIME-FILTER-CONTRACT (Dokumentation, NICHT hier implementiert):
#   Dieser Dump ist VOLL (--schema=training, kein WHERE). Test-Calls duerfen NIE in training.*
#   landen — der D.UX.1-Background-Job prueft users.is_test_user BEVOR er in training.* schreibt.
#   KEIN dump-time-Filter (haette FK training.*→calls gebraucht = ADR-Verletzung Re-ID).
set -euo pipefail

SECRETS_FILE="/etc/nerve/ionos-s3.env"
if [ ! -f "$SECRETS_FILE" ]; then
    echo "[BACKUP-S3] FEHLER: $SECRETS_FILE nicht gefunden" >&2
    exit 1
fi
source "$SECRETS_FILE"

TIMESTAMP=$(date +%Y-%m-%d_%H%M%S)
BACKUP_KEY="training-${TIMESTAMP}.sql.gz"
TMP_FILE="/tmp/${BACKUP_KEY}"
MIN_SIZE_BYTES=512
IONOS_ENDPOINT="${IONOS_S3_ENDPOINT}"
BUCKET="${IONOS_S3_BUCKET}"

echo "[BACKUP-S3] Start: $TIMESTAMP"

# G-2: -h 127.0.0.1 erzwingt TCP → pg_hba host-Regel (scram/md5) → PGPASSWORD greift.
# Ohne -h: Unix-Socket → peer auth (root vs nerve_anon_worker) → Connection abgelehnt.
PGPASSWORD="$NERVE_ANON_WORKER_DB_PASSWORD" \
    pg_dump -h 127.0.0.1 -U nerve_anon_worker -d nerve \
    --schema=training --no-owner --no-acl \
    | gzip > "$TMP_FILE"

ACTUAL_SIZE=$(stat -c%s "$TMP_FILE" 2>/dev/null || echo 0)
if [ "$ACTUAL_SIZE" -lt "$MIN_SIZE_BYTES" ]; then
    echo "[BACKUP-S3] FEHLER: Dump zu klein ($ACTUAL_SIZE bytes < $MIN_SIZE_BYTES)" >&2
    rm -f "$TMP_FILE"
    exit 1
fi
echo "[BACKUP-S3] pg_dump OK: $ACTUAL_SIZE bytes"

# WORM-Upload (COMPLIANCE +30d). WICHTIG: aws s3 cp unterstuetzt kein --object-lock-* → put-object.
RETAIN_UNTIL=$(date -u -d '+30 days' +%Y-%m-%dT%H:%M:%SZ)
aws s3api put-object \
    --bucket "$BUCKET" \
    --key "$BACKUP_KEY" \
    --body "$TMP_FILE" \
    --object-lock-mode COMPLIANCE \
    --object-lock-retain-until-date "$RETAIN_UNTIL" \
    --endpoint-url "$IONOS_ENDPOINT"
echo "[BACKUP-S3] OK: s3://$BUCKET/$BACKUP_KEY (WORM until $RETAIN_UNTIL)"
rm -f "$TMP_FILE"

# 365d-Rotation: Objekte mit abgelaufener Retention loeschen.
# G-3: (grep '^training-' || true) — leeres grep darf die Pipeline unter set -euo pipefail nicht abbrechen.
CUTOFF_DATE=$(date -u -d '-365 days' +%Y-%m-%d)
aws s3 ls "s3://${BUCKET}/" --endpoint-url "$IONOS_ENDPOINT" \
    | awk '{print $4}' \
    | (grep '^training-' || true) \
    | while read -r obj; do
        obj_date=$(echo "$obj" | grep -oP '\d{4}-\d{2}-\d{2}' | head -1)
        if [ -n "$obj_date" ] && [ "$obj_date" \< "$CUTOFF_DATE" ]; then
            aws s3api delete-object --bucket "$BUCKET" --key "$obj" --endpoint-url "$IONOS_ENDPOINT" 2>/dev/null \
                && echo "[BACKUP-S3] Rotation: $obj geloescht (>365d)"
        fi
      done

echo "[BACKUP-S3] Abgeschlossen: $(date)"
```
HINWEIS Punkt 21: Vor dem Test verifizieren dass training.* via `pg_dump -h 127.0.0.1 -U
nerve_anon_worker` zugaenglich ist — das setzt die pg_hba host-Auth-Zeile aus Plan 01 Task 2
(G-2) voraus. Falls Connection-Refused / peer-auth-Fehler: pg_hba host-Zeile fehlt → Plan 01
Task 2 nicht vollstaendig → STOP, dort nachholen (Plan 03 depends_on [01]).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && bash -n scripts/backup_training_to_s3.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep `object-lock-mode COMPLIANCE` in scripts/backup_training_to_s3.sh → 1 Treffer (C-02/C-07)
    - grep `aws s3api put-object` in Skript → 1 Treffer; grep `aws s3 cp` → 0 Treffer (Pitfall 2)
    - grep `pg_dump -h 127.0.0.1` in Skript → 1 Treffer (G-2: TCP erzwingen, peer-auth vermeiden)
    - grep `--schema=training` in Skript → 1 Treffer; grep `WHERE` in Skript → 0 Treffer (C-03 voller Dump, kein Filter)
    - grep `nerve_anon_worker` in Skript → mind. 1 Treffer (Rolle, C-04/C-06)
    - grep `WRITE-TIME-FILTER-CONTRACT` in Skript → 1 Treffer (C-03 Contract dokumentiert)
    - grep `(grep '^training-' || true)` in Skript → 1 Treffer (G-3: leeres grep bricht Pipeline nicht ab)
    - grep `-365 days` in Skript → 1 Treffer (C-05)
    - `bash -n` Exit 0
  </acceptance_criteria>
  <done>backup_training_to_s3.sh schreibt voller training-Dump als nerve_anon_worker ueber TCP (-h 127.0.0.1, G-2), laedt mit COMPLIANCE-WORM hoch, 365d-Rotation mit grep-Guard (G-3), Write-Time-Contract als Kommentar dokumentiert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: restore_test_s3.sh + 4 systemd-Units + /api/health-Erweiterung</name>
  <files>scripts/restore_test_s3.sh, deploy/nerve-backup-training.service, deploy/nerve-backup-training.timer, deploy/nerve-restore-test.service, deploy/nerve-restore-test.timer, routes/app_routes.py</files>
  <read_first>
    - PATTERNS.md Z.434-524 (systemd-Units Vollvorlage) + Z.528-591 (/api/health-Erweiterung Vollvorlage)
    - routes/app_routes.py Z.1565-1640 (api_health — exakte return-Struktur, Code-Lesen lokal OK)
    - docs/systemd-backup-setup.md (nerve-backup.service/.timer Analog + Aktivierungs-Befehle)
    - CONTEXT.md C-08 (Restore-Test initial + monatlich → /api/health-Metrik)
    - PATTERNS.md Critical Planner Note 4 (nerve-backup-training.service nutzt User=root, nicht postgres)
  </read_first>
  <action>
**Schritt 1 — scripts/restore_test_s3.sh NEU** (C-08): zieht das neueste Objekt, prueft Integritaet,
schreibt /opt/nerve/.s3_restore_status im key=value-Format (analog .deploy_meta):
```bash
#!/bin/bash
# scripts/restore_test_s3.sh — Monatlicher S3-Restore-Integritaets-Test (Schicht 3, C-08)
set -uo pipefail
SECRETS_FILE="/etc/nerve/ionos-s3.env"
[ -f "$SECRETS_FILE" ] && source "$SECRETS_FILE"
STATUS_FILE="/opt/nerve/.s3_restore_status"
CHECKED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)

write_status() {  # $1 = true|false
    echo "S3_RESTORE_OK=$1" > "$STATUS_FILE"
    echo "S3_RESTORE_CHECKED_AT=$CHECKED_AT" >> "$STATUS_FILE"
}

# G-3: (grep '^training-' || true) — leeres grep darf unter pipefail nicht abbrechen.
LATEST=$(aws s3 ls "s3://${IONOS_S3_BUCKET}/" --endpoint-url "$IONOS_S3_ENDPOINT" 2>/dev/null \
    | awk '{print $4}' | (grep '^training-' || true) | sort | tail -1)
if [ -z "$LATEST" ]; then
    echo "[RESTORE-TEST] Kein Objekt im Bucket — noch kein Backup gelaufen"
    write_status false
    exit 0
fi
TMP="/tmp/restore-test-${LATEST}"
aws s3api get-object --bucket "$IONOS_S3_BUCKET" --key "$LATEST" "$TMP" --endpoint-url "$IONOS_S3_ENDPOINT" >/dev/null 2>&1
if gunzip -t "$TMP" 2>/dev/null; then
    echo "[RESTORE-TEST] OK: $LATEST integer"
    write_status true
else
    echo "[RESTORE-TEST] FEHLER: $LATEST korrupt"
    write_status false
fi
rm -f "$TMP"
```

**Schritt 2 — 4 systemd-Units** in deploy/ anlegen (PATTERNS.md Z.464-514). User=root,
EnvironmentFile=/etc/nerve/ionos-s3.env:
- deploy/nerve-backup-training.service: Type=oneshot, User=root, EnvironmentFile, ExecStart=/opt/nerve/app/scripts/backup_training_to_s3.sh
- deploy/nerve-backup-training.timer: OnCalendar=Sun *-*-* 02:00:00, Persistent=true, RandomizedDelaySec=30min, WantedBy=timers.target
- deploy/nerve-restore-test.service: Type=oneshot, User=root, EnvironmentFile, ExecStart=/opt/nerve/app/scripts/restore_test_s3.sh
- deploy/nerve-restore-test.timer: OnCalendar=monthly, Persistent=true, WantedBy=timers.target

**Schritt 3 — routes/app_routes.py api_health()** (P-05: JSON, keine UI). VOR `return jsonify({`
(Z.1632) den Flat-File-Read-Block einfuegen (analog deploy_meta-Pattern):
```python
    # Phase 08.23.2.D.UX.0: Schicht 3 S3 Restore-Test Status
    s3_restore_ok = None
    s3_restore_checked_at = None
    try:
        with open('/opt/nerve/.s3_restore_status', 'r') as _f:
            for _line in _f:
                _line = _line.strip()
                if _line.startswith('S3_RESTORE_OK='):
                    s3_restore_ok = _line[len('S3_RESTORE_OK='):] == 'true'
                elif _line.startswith('S3_RESTORE_CHECKED_AT='):
                    s3_restore_checked_at = _line[len('S3_RESTORE_CHECKED_AT='):]
    except (OSError, IOError):
        pass  # Datei fehlt = noch kein Restore-Test / Schicht 3 nicht aktiv
```
Dann den return jsonify um zwei Keys erweitern (nach `deployed_at`):
```python
        'backup_s3_restore_ok': s3_restore_ok,                 # True | False | None
        'backup_s3_restore_checked_at': s3_restore_checked_at, # ISO-8601 | None
```

**Schritt 4 — Deploy + Production-Verifikation (P-04 HART):**
`git add -A && git commit && git push && bash deploy.sh production`
Units installieren + initialen Lauf erzwingen (C-08 initial):
```
ssh ... "cp /opt/nerve/app/deploy/nerve-backup-training.* /opt/nerve/app/deploy/nerve-restore-test.* /etc/systemd/system/ \
  && systemctl daemon-reload \
  && systemctl enable --now nerve-backup-training.timer nerve-restore-test.timer \
  && systemctl start nerve-backup-training.service && journalctl -u nerve-backup-training.service -n 40 \
  && systemctl start nerve-restore-test.service && journalctl -u nerve-restore-test.service -n 20"
```
/api/health pruefen:
`ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh health'` → enthaelt backup_s3_restore_ok.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && bash -n scripts/restore_test_s3.sh && python -m py_compile routes/app_routes.py</automated>
  </verify>
  <acceptance_criteria>
    - grep `S3_RESTORE_OK=` in scripts/restore_test_s3.sh → mind. 2 Treffer (write true/false)
    - grep `gunzip -t` in scripts/restore_test_s3.sh → 1 Treffer (Integritaet)
    - grep `(grep '^training-' || true)` in scripts/restore_test_s3.sh → 1 Treffer (G-3)
    - grep `backup_s3_restore_ok` in routes/app_routes.py → 2 Treffer (Read + jsonify-Key)
    - grep `.s3_restore_status` in routes/app_routes.py → 1 Treffer
    - 4 Units in deploy/: grep `OnCalendar=monthly` in nerve-restore-test.timer → 1; grep `User=root` in nerve-backup-training.service → 1; grep `EnvironmentFile=/etc/nerve/ionos-s3.env` in beiden .service → je 1
    - `bash -n scripts/restore_test_s3.sh` Exit 0 + py_compile routes/app_routes.py Exit 0
    - Production: `inspect.sh health` JSON enthaelt `backup_s3_restore_ok` (in SUMMARY zitieren)
    - Production journalctl nerve-backup-training: `[BACKUP-S3] OK` (in SUMMARY zitieren)
    - aws s3api get-object-lock-configuration zeigt COMPLIANCE/30d (aus Task 1, in SUMMARY zitieren)
  </acceptance_criteria>
  <done>restore_test_s3.sh schreibt .s3_restore_status (grep-Guard G-3), 4 Units installiert+aktiv, /api/health gibt backup_s3_restore_ok, Production-Laeufe gruen, Object-Lock COMPLIANCE verifiziert.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen / Persistenz-Pfade

- `training.*` (lesen via pg_dump als nerve_anon_worker, TCP -h 127.0.0.1) — Schema/Tabelle aus Plan 01
- IONOS S3 Bucket nerve-training-backup (schreiben — WORM-Objekte)
- /opt/nerve/.s3_restore_status (schreiben Restore-Test, lesen /api/health) — Flat-File
- /api/health JSON (kein DB — liest Flat-File)

### inspect.sh / Beleg (Executor MUSS verbatim einfuegen)

```
# aws-cli v2 (C-WARN-1):
$ ssh ... 'aws --version'
[VERBATIM — muss aws-cli/2. zeigen, NICHT aws-cli/1.]

# Endpoint/Region-Paar funktioniert (C-WARN-2, vor jedem irreversiblen Schritt):
$ aws s3api list-buckets --region de --endpoint-url https://s3.eu-central-1.ionoscloud.com
[VERBATIM — laeuft ohne Auth/Region-Fehler]

# Versioning Enabled (C-WARN-3, Object-Lock-Voraussetzung):
$ aws s3api get-bucket-versioning --bucket nerve-training-backup --endpoint-url <ionos>
[VERBATIM — Status: Enabled]

# 1-Tages-Smoke-Restore (C-BLOCKER-4) — BEVOR 30d scharf:
$ aws s3api head-object --bucket nerve-training-backup --key smoke-test.sql.gz --endpoint-url <ionos>
[VERBATIM — ObjectLockMode=COMPLIANCE, RetainUntilDate ~ now+1d]
$ gunzip -t /tmp/s3-smoke-dl.gz && echo SMOKE-RESTORE-OK
[VERBATIM — SMOKE-RESTORE-OK]

# training-Schema lesbar als nerve_anon_worker UEBER TCP (G-2, Vorbedingung pg_dump):
$ ssh ... "PGPASSWORD='***' psql -h 127.0.0.1 -U nerve_anon_worker -d nerve -c '\dn training'"
[VERBATIM — training-Schema sichtbar als diese Rolle ueber TCP. Falls peer-auth/Refused → Plan 01 Task 2 G-2-Finding]

# Object-Lock-Default-Config (C-02) — NACH staged bring-up:
$ aws s3api get-object-lock-configuration --bucket nerve-training-backup --endpoint-url <ionos>
[VERBATIM — Enabled / COMPLIANCE / 30]

# Encryption (C-09):
$ aws s3api get-bucket-encryption --bucket nerve-training-backup --endpoint-url <ionos>
[VERBATIM — SSE AES256 oder Finding]

# Objekt im Bucket nach Lauf:
$ aws s3 ls s3://nerve-training-backup/ --endpoint-url <ionos>
[VERBATIM — training-*.sql.gz]

# Restore-Status-Datei + /api/health:
$ ssh ... 'cat /opt/nerve/.s3_restore_status'
[VERBATIM — S3_RESTORE_OK=true]
$ ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh health'
[VERBATIM — backup_s3_restore_ok im JSON]
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `pg_dump -h 127.0.0.1 --schema=training` | gelesen (als nerve_anon_worker, TCP G-2) | DB-Schema training.* | ❓ psql -h 127.0.0.1 \dn training als Rolle |
| WORM-Objekt | geschrieben | IONOS S3 (Object-Lock COMPLIANCE, staged C-BLOCKER-4) | ❓ aws s3 ls + get-object-lock-configuration + Versioning |
| `S3_RESTORE_OK` | geschrieben (restore_test) / gelesen (api_health) | Flat-File /opt/nerve/.s3_restore_status | ❓ cat + inspect.sh health |
| `backup_s3_restore_ok` | jsonify-Output | KEINE DB — Flat-File-Read (analog deploy_meta) | ✓ Pattern-Praezedenz git_head/deployed_at |

### Bei Diskrepanz: STOP + Replan
Wenn nerve_anon_worker training.* nicht ueber TCP lesen kann (peer-auth → G-2/Plan 01 Task 2
unvollstaendig) → STOP, Plan 01 Task 2 pg_hba-Zeile nachholen. Wenn der 1-Tages-Smoke-Restore
fehlschlaegt → 30d-COMPLIANCE NICHT scharf schalten (C-BLOCKER-4), Endpoint/Region/Format pruefen.
Wenn Versioning NICHT Enabled (C-WARN-3) → Object-Lock greift nicht → Finding. Wenn aws-cli v1
(C-WARN-1) → v2 nachinstallieren. Wenn Object-Lock-Config NICHT COMPLIANCE zeigt → Bucket falsch
erstellt (Pitfall 1, nicht nachruestbar) → Bucket neu anlegen. Wenn /api/health das Feld nicht
zeigt → Code-Pfad pruefen.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production-Server → IONOS S3 | aws-cli v2 mit Access-Key/Secret aus /etc/nerve/ionos-s3.env |
| Hetzner-Account-Kompromittierung → training-Backup | WORM verhindert Loeschung (das Kern-Threat-Modell der Phase) |
| nerve_anon_worker → training.* | nur SELECT (pg_dump ueber TCP) — kein Write-Pfad im Backup |
| Cross-Anbieter-Transfer (DE→IONOS DE) | nur anonymisierte training.*-Daten (C-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DUX0-10 | Tampering / Denial | Ransomware/Account-Kompromittierung loescht Backups | mitigate | Object-Lock COMPLIANCE 30d (C-02): NIEMAND kann vor Ablauf loeschen/ueberschreiben — auch nicht mit Root/Master-Key. KEIN Governance-Mode (Bypass-Loch). Versioning Enabled (Voraussetzung, C-WARN-3). get-object-lock-configuration-Beleg. |
| T-DUX0-10b | Denial (Self-inflicted) | Fehlkonfig erster Upload = 30d un-loeschbarer Muell | mitigate | Staged WORM bring-up (C-BLOCKER-4): Bucket erst nur Object-Lock-enabled, dann 1-Tages-Smoke-Roundtrip (≤1d Risiko), 30d-COMPLIANCE-Default ERST nach erfolgreichem Smoke-Restore. Fehlkonfig kostet ≤1 Tag statt 30. |
| T-DUX0-11 | Information Disclosure | IONOS S3 Credentials | mitigate | /etc/nerve/ionos-s3.env chmod 600 root:root (C-06), getrennt von App-.env (kleinerer Blast-Radius), nie in git. |
| T-DUX0-12 | Information Disclosure | Cross-Anbieter-Transfer von PII | mitigate | Nur training.* (anonymisiert, C-03) verlaesst Hetzner. Klartext-/Produktionsdaten bleiben in DE/Hetzner. Write-Time-Filter (D.UX.1) haelt Test-Daten draussen. |
| T-DUX0-13 | Tampering (Re-Identifikation) | training-Dump | mitigate | KEINE FK training.*→public.*. source_call_hash=HMAC (D-05, kein Brute-Force ohne Key), ts_offset_ms-Salt (D-04). Voller Dump ohne Cross-Schema-Bruecke (C-03 korrigiert Roadmap-Skizze). |
| T-DUX0-14 | Repudiation / Denial | Backup nie getestet | mitigate | Monatlicher Restore-Test (C-08) schreibt .s3_restore_status → /api/health-Metrik. Backup das nie getestet wird ist kein Backup. grep-Guard (G-3) verhindert Pipeline-Abbruch. |
| T-DUX0-15 | Information Disclosure | Encryption at rest | mitigate | C-09: get-bucket-encryption verifiziert SSE-S3 (AES256). Falls nicht default: --server-side-encryption AES256 beim put-object. |
</threat_model>

<verification>
- aws-cli v2 (C-WARN-1: aws --version = aws-cli/2.)
- Endpoint/Region-Paar de↔s3.eu-central-1 konsistent + list-buckets laeuft (C-WARN-2)
- Versioning Enabled (C-WARN-3)
- Staged WORM: 1-Tages-Smoke-Restore VOR 30d-COMPLIANCE-Default (C-BLOCKER-4)
- Object-Lock COMPLIANCE/30d auf Bucket (get-object-lock-configuration)
- Encryption verifiziert (get-bucket-encryption oder Finding)
- backup_training_to_s3.sh dumpt ueber TCP (-h 127.0.0.1, G-2) + laedt mit put-object COMPLIANCE hoch (journalctl: OK)
- grep-Guards (G-3) in beiden Skripten
- restore_test_s3.sh schreibt .s3_restore_status; /api/health gibt backup_s3_restore_ok
- 4 systemd-Units installiert + aktiv
- Write-Time-Filter-Contract als Kommentar im Skript dokumentiert (C-03)
- Persistenz-Sektion mit verbatim Output befuellt
</verification>

<success_criteria>
- WORM-Backup des training-Schemas auf IONOS (COMPLIANCE, 30d, voller Dump, TCP-pg_dump)
- Staged WORM bring-up (C-BLOCKER-4): 30d erst nach 1-Tages-Smoke-Restore scharf
- aws-cli v2 (C-WARN-1), kanonisches de↔eu-central-1-Paar (C-WARN-2), Versioning Enabled (C-WARN-3)
- 365d-Rotation (C-05) mit grep-Guard (G-3), monatlicher Restore-Test (C-08), /api/health-Metrik
- Credentials in /etc/nerve/ionos-s3.env (chmod 600), nie in git (C-06)
- Encryption verifiziert (C-09)
- C-03 Contract dokumentiert für D.UX.1
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-03-SUMMARY.md`
Im SUMMARY: aws-cli-Version (C-WARN-1), Endpoint/Region-Paar (C-WARN-2, kein Secret), Versioning-Status (C-WARN-3), Smoke-Restore-Beleg + Reihenfolge-Bestaetigung (C-BLOCKER-4), Object-Lock-Config-Output, Encryption-Status, TCP-pg_dump-Beleg (G-2), journalctl-Outputs, /api/health-Feld.
</output>
</content>
