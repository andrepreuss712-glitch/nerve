---
phase: 08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backup_postgres.sh
autonomous: false   # Hetzner Storage Box order + SSH-Key upload are human-action gated
covers: [B-01, B-02, B-03, B-04, B-05, P-01, P-03, P-04, P-05]
requirements: [B-01, B-02, B-03, B-04, B-05]
user_setup:
  - service: hetzner-storagebox
    why: "Backup-Schicht 2 Ziel (~3 EUR/Mon, 100 GB, gleicher Hetzner-AVV — kein neuer AVV noetig, B-01)"
    dashboard_config:
      - task: "Storage Box BX11 (100 GB) bestellen"
        location: "Hetzner Robot / Console → Storage Box"
      - task: "Dedizierten SSH-Public-Key id_storagebox.pub hinterlegen (Key-Auth, kein Passwort)"
        location: "Storage Box → Settings → SSH Keys"

must_haves:
  truths:
    - "backup_postgres.sh pusht nach lokalem pg_dump+Size-Check den Dump via rsync auf die Storage Box (Port 23, Key-Auth)"
    - "Auf der Storage Box liegt nach einem Lauf eine nerve-*.sql.gz-Datei"
    - "Dumps aelter als 90 Tage werden script-seitig von der Box geloescht"
    - "Ein von der Box gezogener Dump besteht die Integritaetspruefung (gunzip -t)"
  artifacts:
    - path: "scripts/backup_postgres.sh"
      provides: "rsync-Push-Block + 90d-Rotation (Schicht 2)"
      contains: "your-storagebox.de"
  key_links:
    - from: "scripts/backup_postgres.sh pg_dump"
      to: "Hetzner Storage Box"
      via: "rsync -e 'ssh -p23 -i /root/.ssh/id_storagebox'"
      pattern: "ssh -p23"
---

<objective>
Backup-Schicht 2: erweitert das bestehende scripts/backup_postgres.sh um einen rsync-Push auf
eine Hetzner Storage Box (nach dem lokalen pg_dump + Size-Check), mit 90-Tage-Rotation und
Test-Restore-Verifikation.

Purpose: Defense gegen Server-Crash / Disk-Failure — der lokale Dump (Schicht 1) liegt sonst auf
derselben Maschine. Schicht 2 ist Off-Server, aber gleicher Anbieter/AVV (kein DSGVO-Mehraufwand).
Output: erweitertes backup_postgres.sh + Storage-Box-Bestellung + Restore-Test.

⚠️ KEINE UI-Aenderungen (P-05). KEINE lokalen Tests (P-04 HART) — Backup-Tests laufen auf
Production via SSH.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-CONTEXT.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-RESEARCH.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-PATTERNS.md

<interfaces>
<!-- scripts/backup_postgres.sh bestehende Struktur (PATTERNS.md Z.288-299): -->
<!-- Z.1-8 shebang+header+set -e | Z.8-11 CONSTANTS (BACKUP_DIR/TIMESTAMP/BACKUP_FILE/MIN_SIZE_BYTES) -->
<!-- Z.13 mkdir -p | Z.15-16 pg_dump | Z.18-24 SIZE-CHECK | Z.26 [BACKUP] OK | Z.28-29 find -mtime +30 -delete | Z.31/33 Logs -->
<!-- Dateinamensschema: nerve-YYYY-MM-DD_HHMMSS.sql.gz (ermoeglicht dateinamenbasierte Rotation) -->
<!-- HINWEIS: Executor MUSS scripts/backup_postgres.sh zuerst vollstaendig lesen — die exakten Zeilennummern oben sind aus PATTERNS.md, vor Edit verifizieren. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 [BLOCKING]: Hetzner Storage Box bestellen + SSH-Key generieren/hinterlegen</name>
  <read_first>
    - CONTEXT.md B-01 (Box bestellen, ~3 EUR/Mon, gleicher AVV) + B-04 (dedizierter Key /root/.ssh/id_storagebox)
    - RESEARCH.md Pattern 5 + Pitfall 5 (Port 23, kein Shell auf Box, Key-Format)
    - RESEARCH.md Environment Availability (Box noch nicht bestellt — blocking fuer Schicht 2)
  </read_first>
  <what-built>
Skript-Verdrahtung (Task 2) ist autonom — aber der rsync-Push braucht eine existierende Box +
hinterlegten Key. Box-Bestellung + Key-Upload sind manuell (Hetzner-Console, kein CLI im Scope).
  </what-built>
  <how-to-verify>
1. Storage Box (100 GB, BX11) im Hetzner-Account bestellen — gleicher AVV wie Hauptsystem
   (B-01, KEIN neuer AVV). Box-User notieren (Format uXXXXX).
2. Dedizierten SSH-Key auf Production generieren (Key-Auth, kein Passwort):
   `ssh -i ~/.ssh/id_ed25519_nerve root@<prod> "ssh-keygen -t ed25519 -N '' -f /root/.ssh/id_storagebox"`
3. Public-Key auf der Box hinterlegen (Hetzner-Methode — Box hat keine normale Shell):
   `ssh ... "cat /root/.ssh/id_storagebox.pub"` → in Hetzner-Console → Storage Box → SSH Keys einfuegen.
   (Alternativ Hetzner-dokumentierte ssh-copy-id-via-Port-23-Methode.)
4. Ziel-Verzeichnis auf der Box anlegen (via SFTP/Port 23):
   `ssh -p23 -i /root/.ssh/id_storagebox uXXXXX@uXXXXX.your-storagebox.de "mkdir -p /backups/nerve"`
   (Storage Box unterstuetzt einzelne SSH-Kommandos wie mkdir; KEIN find/awk-Scripting.)
5. Connectivity-Smoke (Key-Auth funktioniert):
   `ssh -p23 -i /root/.ssh/id_storagebox -o BatchMode=yes uXXXXX@uXXXXX.your-storagebox.de "ls /backups/nerve"`
   → kein Passwort-Prompt, Exit 0.
  </how-to-verify>
  <resume-signal>Type "box ready uXXXXX=<deine-box-user-id>" — der Executor traegt diese ID in Task 2 als BOX_USER ein.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: rsync-Push-Block + 90d-Rotation in backup_postgres.sh + Production-Restore-Test</name>
  <files>scripts/backup_postgres.sh</files>
  <read_first>
    - scripts/backup_postgres.sh KOMPLETT (das exakte zu erweiternde Skript — Code-Lesen lokal OK via git-Sync)
    - PATTERNS.md Z.284-349 (rsync-Konstanten-Block + Push-Block + 90d-Rotation Vollvorlage)
    - RESEARCH.md Pattern 5 (rsync-Flags, Port 23) + Pitfall 5 (kein remote find — script-side Rotation) + Anti-Pattern "rsync --delete fuer Rotation" (verboten)
    - CONTEXT.md B-02 (Push nach Size-Check) / B-03 (90d) / B-04 (Key, Host als Konstante) / B-05 (Restore-Test)
    - Resume-Signal aus Task 1 (BOX_USER-ID)
  </read_first>
  <action>
**Schritt 1 — Konstanten-Block** in scripts/backup_postgres.sh einfuegen NACH dem set -e /
Header (vor dem bestehenden BACKUP_DIR-Block). BOX_USER = die ID aus Task-1-Resume-Signal:
```bash
# ── Schicht 2: Hetzner Storage Box (Phase 08.23.2.D.UX.0) ────────────────────
BOX_USER="uXXXXX"          # ← ID aus Task 1 eintragen (Format uXXXXX)
BOX_HOST="${BOX_USER}.your-storagebox.de"
BOX_PATH="/backups/nerve"
BOX_KEY="/root/.ssh/id_storagebox"
BOX_RETENTION_DAYS=90      # 3x laenger als lokale 30d (B-03)
```

**Schritt 2 — Push + Rotation-Block** einfuegen NACH dem bestehenden `[BACKUP] OK`-Log
(also nach erfolgreichem Size-Check, B-02), VOR der bestehenden lokalen find-mtime+30-Rotation.
Der Guard `[ "$BOX_USER" != "uXXXXX" ]` verhindert dass der Block ohne konfigurierte Box laeuft:
```bash
# ── Schicht 2: rsync Push → Hetzner Storage Box ──────────────────────────────
if [ -f "$BOX_KEY" ] && [ -n "$BOX_USER" ] && [ "$BOX_USER" != "uXXXXX" ]; then
    echo "[BACKUP] Schicht 2: rsync Push → ${BOX_HOST}${BOX_PATH}/"
    rsync \
        --archive \
        --compress \
        -e "ssh -p23 -i ${BOX_KEY} -o StrictHostKeyChecking=no -o BatchMode=yes" \
        "$BACKUP_FILE" \
        "${BOX_USER}@${BOX_HOST}:${BOX_PATH}/"
    echo "[BACKUP] Schicht 2: Push OK"

    # 90d-Rotation: Storage Box hat KEINE volle Shell (Pitfall 5) — kein remote find.
    # Dateinamen-Timestamp parsen (nerve-YYYY-MM-DD_HHMMSS.sql.gz), Alter script-seitig pruefen.
    CUTOFF_DATE=$(date -d "-${BOX_RETENTION_DAYS} days" +%Y-%m-%d)
    ssh -p23 -i "${BOX_KEY}" -o BatchMode=yes "${BOX_USER}@${BOX_HOST}" \
        "ls ${BOX_PATH}/" 2>/dev/null \
        | grep '^nerve-' \
        | while read -r fname; do
            file_date=$(echo "$fname" | grep -oP '\d{4}-\d{2}-\d{2}' | head -1)
            if [ -n "$file_date" ] && [ "$file_date" \< "$CUTOFF_DATE" ]; then
                ssh -p23 -i "${BOX_KEY}" -o BatchMode=yes "${BOX_USER}@${BOX_HOST}" \
                    "rm ${BOX_PATH}/${fname}" 2>/dev/null \
                    && echo "[BACKUP] Schicht 2: Rotation geloescht: ${fname} (>${BOX_RETENTION_DAYS}d)"
            fi
          done
else
    echo "[BACKUP] Schicht 2: BOX_KEY fehlt oder BOX_USER nicht konfiguriert — skip"
fi
```
WICHTIG: KEIN `rsync --delete` (loescht sonst nicht-gematchte Dateien — Anti-Pattern). Port 23
(nicht 22). `-o BatchMode=yes` damit der systemd-Timer nicht an einem Passwort-Prompt haengt.

**Schritt 3 — Deploy + Production-Test (P-04 HART):**
`git add -A && git commit && git push && bash deploy.sh production`
Einen Backup-Lauf erzwingen + verifizieren:
```
ssh -i ~/.ssh/id_ed25519_nerve root@<prod> "sudo systemctl start nerve-backup.service && journalctl -u nerve-backup.service -n 40"
```
→ Logs zeigen `[BACKUP] Schicht 2: Push OK`.
Box-Inhalt pruefen (B-02-Beleg):
```
ssh -p23 -i /root/.ssh/id_storagebox uXXXXX@uXXXXX.your-storagebox.de "ls -la /backups/nerve/"
```
→ zeigt nerve-*.sql.gz.

**Schritt 4 — Restore-Test (B-05):** Dump von der Box ziehen + Integritaet pruefen:
```
ssh ... "rsync -e 'ssh -p23 -i /root/.ssh/id_storagebox -o BatchMode=yes' \
  uXXXXX@uXXXXX.your-storagebox.de:/backups/nerve/<neuester-dump>.sql.gz /tmp/restore-test.sql.gz \
  && gunzip -t /tmp/restore-test.sql.gz && echo 'RESTORE-OK' && rm /tmp/restore-test.sql.gz"
```
→ Ausgabe `RESTORE-OK` (gunzip -t Exit 0 = Archiv intakt).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && bash -n scripts/backup_postgres.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep `ssh -p23` in scripts/backup_postgres.sh → mind. 1 Treffer (Port 23, nicht 22)
    - grep `your-storagebox.de` in scripts/backup_postgres.sh → 1 Treffer
    - grep `BOX_RETENTION_DAYS=90` in scripts/backup_postgres.sh → 1 Treffer (B-03)
    - grep `id_storagebox` in scripts/backup_postgres.sh → mind. 1 Treffer (B-04)
    - grep `rsync --delete` in scripts/backup_postgres.sh → 0 Treffer (Anti-Pattern)
    - Push-Block steht NACH dem `[BACKUP] OK`-Log, VOR der lokalen find-mtime-Rotation (visuell pruefen)
    - `bash -n scripts/backup_postgres.sh` Exit 0 (Syntax)
    - Production journalctl zeigt `[BACKUP] Schicht 2: Push OK` (in SUMMARY zitieren)
    - Box-`ls` zeigt nerve-*.sql.gz (in SUMMARY zitieren)
    - Restore-Test gibt `RESTORE-OK` (gunzip -t, in SUMMARY zitieren)
  </acceptance_criteria>
  <done>backup_postgres.sh pusht nach Box (Port 23, Key-Auth), 90d-Rotation script-seitig, Production-Lauf gruen, Restore-Test RESTORE-OK.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen / Persistenz-Pfade

- KEINE DB-Tabellen geschrieben/gelesen — der Plan liest den fertigen pg_dump (Datei) und schreibt
  auf eine externe Storage Box (Datei). Persistenz-Schicht ist Dateisystem (lokal + Box).

### inspect.sh / Datei-Beleg (Executor MUSS verbatim einfuegen)

```
# Lokaler Dump existiert nach Lauf (Schicht 1, Vorbedingung fuer Push):
$ ssh ... 'ls -la /opt/nerve/backups/postgres/'
[VERBATIM — neueste nerve-*.sql.gz]

# Box-Inhalt (Schicht 2 Ziel):
$ ssh -p23 -i /root/.ssh/id_storagebox uXXXXX@uXXXXX.your-storagebox.de 'ls -la /backups/nerve/'
[VERBATIM — nerve-*.sql.gz auf der Box]

# Restore-Integritaet:
$ ssh ... 'gunzip -t /tmp/restore-test.sql.gz && echo RESTORE-OK'
[VERBATIM — RESTORE-OK]
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `$BACKUP_FILE` | gelesen (rsync-Quelle) | lokales Dateisystem /opt/nerve/backups/postgres/ | ❓ ls (Datei existiert) |
| Box-Zieldatei | geschrieben (rsync) | Hetzner Storage Box /backups/nerve/ | ❓ ssh -p23 ls |
| Rotation-Cutoff | dateinamenbasiert (kein DB/mtime) | Dateinamen-Timestamp nerve-YYYY-MM-DD | ✓ Pattern im Skript |

### Bei Diskrepanz: STOP + Replan
Wenn der Push fehlschlaegt (Box-Inhalt leer) oder gunzip -t fehlschlaegt → Finding, nicht
weiterbauen. Pruefen: Port 23? Key hinterlegt? /backups/nerve existiert auf Box?

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production-Server → Storage Box | rsync ueber SSH-Key (Port 23), kein Passwort |
| systemd-Timer → SSH | non-interaktiv (BatchMode) — kein Passwort-Prompt-Hang |
| Backup-Dateien | enthalten Klartext-Produktionsdaten — Box-Zugriff = Daten-Zugriff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DUX0-06 | Information Disclosure | SSH-Key id_storagebox | mitigate | Dedizierter Key (nur fuer die Box, B-04), /root/.ssh/, chmod 600 durch ssh-keygen. Kein Passwort-Auth. |
| T-DUX0-07 | Tampering | Backup-Rotation | mitigate | KEIN `rsync --delete` (loescht nicht-gematchte Dateien blind). Rotation ausschliesslich dateinamenbasiert mit explizitem rm pro alter Datei (Anti-Pattern vermieden). |
| T-DUX0-08 | Denial of Service | systemd-Timer-Hang | mitigate | `-o BatchMode=yes` + `-o StrictHostKeyChecking=no` verhindern interaktiven Hang im Timer-Kontext. |
| T-DUX0-09 | Information Disclosure | Box gleicher Anbieter (Hetzner) | accept | B-01: gleicher AVV, gleiche Jurisdiktion (DE). Ransomware-Schutz gegen Account-Kompromittierung liefert Schicht 3 (Plan 03, WORM), nicht Schicht 2. Bewusst akzeptiert. |
</threat_model>

<verification>
- backup_postgres.sh syntaktisch valide (bash -n)
- Production-Lauf pusht auf Box (journalctl: Push OK)
- Box enthaelt nerve-*.sql.gz
- 90d-Rotation im Skript (dateinamenbasiert, kein rsync --delete)
- Restore-Test RESTORE-OK
</verification>

<success_criteria>
- Storage Box bestellt, Key hinterlegt, /backups/nerve angelegt
- rsync-Push nach Size-Check (Port 23, Key-Auth, BatchMode)
- 90d script-seitige Rotation (B-03)
- Restore-Test bestanden (B-05)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-02-SUMMARY.md`
Im SUMMARY: BOX_USER-ID (kein Secret), verbatim journalctl/ls/gunzip-Outputs, Restore-Test-Ergebnis.
</output>
