---
phase: 08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - alembic/versions/0008_training_schema_foundation.py
  - services/email_service.py
  - scripts/seed_test_user.py
autonomous: false   # contains [BLOCKING] runbook (CREATE ROLE) + production upgrade + human-action gate
covers: [A-01, A-02, A-03, A-04, A-05, D-01, D-02, D-03, D-04, D-05, D-06, D-07, P-01, P-04, P-05]
requirements: [A-01, A-02, A-03, A-04, A-05, D-01, D-02, D-03, D-04, D-05, D-06, D-07]
user_setup:
  - service: postgresql-role
    why: "nerve_anon_worker DB-Rolle (Passwort darf nicht in VCS/Migration — CLAUDE.md HART Secrets-Regel, D-01)"
    env_vars:
      - name: NERVE_ANON_WORKER_DB_PASSWORD
        source: "openssl rand -base64 32 → eintragen in /etc/nerve/ionos-s3.env (chmod 600, root)"

must_haves:
  truths:
    - "users-Tabelle hat Spalte is_test_user BOOLEAN NOT NULL DEFAULT FALSE auf Production"
    - "Schema training existiert auf Production mit Tabelle training.transcript_archive"
    - "Rolle nerve_anon_worker hat USAGE+INSERT/SELECT auf training.* und SELECT auf public.calls/users/conversation_logs — und KEIN Grant auf training.* fuer die App-Rolle"
    - "Rolle nerve_anon_worker hat einen pg_hba.conf host-Eintrag (TCP, scram-sha-256) und kann sich ueber 127.0.0.1 verbinden (Vorbedingung Plan 03 pg_dump -h 127.0.0.1)"
    - "Email an @nerve.local wird in _send() geblockt mit Log [EMAIL] test-user recipient — auch wenn `to` ein bare-String ist"
    - "Test-Account andre-test@nerve.local existiert mit is_test_user=TRUE"
    - "Es ist tief dokumentiert als welche DB-Rolle die App connectet UND ob diese Rolle Superuser/Owner von training ist (D-07 Finding, \\du + Owner-Query)"
  artifacts:
    - path: "alembic/versions/0008_training_schema_foundation.py"
      provides: "Migration: CREATE SCHEMA training + transcript_archive + GRANTs + users.is_test_user"
      contains: "CREATE SCHEMA IF NOT EXISTS training"
    - path: "database/models.py"
      provides: "is_test_user Column auf User"
      contains: "is_test_user"
    - path: "services/email_service.py"
      provides: "Email-Guard @nerve.local in _send() (mit String-Normalisierung)"
      contains: "isinstance(recipients, str)"
    - path: "scripts/seed_test_user.py"
      provides: "idempotenter Test-User-Seed"
      contains: "andre-test@nerve.local"
  key_links:
    - from: "alembic/versions/0008_training_schema_foundation.py"
      to: "users.is_test_user"
      via: "batch_alter_table add_column"
      pattern: "is_test_user"
    - from: "services/email_service.py _send()"
      to: "alle 6 Email-Call-Sites (auth/oauth/admin_views)"
      via: "Chokepoint Domain-Check (String-normalisiert)"
      pattern: "endswith\\('@nerve.local'\\)"
    - from: "scripts/seed_test_user.py"
      to: "users.is_test_user Spalte"
      via: "User(is_test_user=True)"
      pattern: "is_test_user=True"
---

<objective>
Test-User-Pattern + PostgreSQL training-Schema-Foundation in EINER Migration (0008).

Legt die Daten-Foundation an die D.UX.1 und Phase E konsumieren: die is_test_user-Spalte
(Single Source of Truth fuer Test-Call-Filterung via JOIN), das isolierte training-Schema
mit transcript_archive-Tabelle, die nerve_anon_worker-Rolle mit minimalen GRANTs, den
Email-Guard gegen Test-Account-Mails, und den idempotenten Test-User-Seed.

Purpose: VOR D.UX.1 muss Schema/Tabelle/Rolle/Contract stehen, sonst landen Test-Daten in
der Cloud-Backup-Foundation (Andre-Quote 2026-05-28).
Output: Migration 0008, is_test_user-Spalte, training.transcript_archive, nerve_anon_worker,
Email-Guard, seed_test_user.py — alle auf Production verifiziert.

⚠️ KEINE UI-Aenderungen (P-05). KEINE lokalen Tests (P-04 HART) — alles via SSH/inspect.sh
auf Production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-CONTEXT.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-RESEARCH.md
@.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-PATTERNS.md

<interfaces>
<!-- Aus Codebase extrahiert. Executor nutzt diese direkt — keine Exploration noetig. -->

services/email_service.py — bestehender _send() Chokepoint (Z.17-26, VERIFIZIERT durch Read):
```python
def _send(payload):
    if not resend.api_key:
        print('[EMAIL] RESEND_API_KEY missing — skip send')
        return False
    try:
        resend.Emails.send(payload)
        return True
    except Exception as e:
        print(f'[EMAIL] send failed: {e}')
        return False
```

database/models.py — bestehendes Boolean-Flag-Pattern auf User (PATTERNS.md Z.30-36):
```python
is_superadmin = Column(Boolean, default=False, nullable=False)
aktiv         = Column(Boolean, default=True)
is_trial      = Column(Boolean, default=False)
is_coach      = Column(Boolean, default=False)
```
Boolean ist bereits importiert. Neue Spalte direkt nach is_coach einfuegen.

alembic/versions/0007_add_score_fields_to_calls.py — letzte Migration (head). down_revision='0007'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: is_test_user Column + Migration 0008 (Schema + Tabelle + GRANTs) schreiben</name>
  <files>database/models.py, alembic/versions/0008_training_schema_foundation.py</files>
  <read_first>
    - database/models.py Z.64-132 (User-Klasse, Boolean-Flag-Block — Code-Lesen lokal OK via git-Sync)
    - alembic/versions/0007_add_score_fields_to_calls.py (Header/Imports/batch_alter_table-Pattern — der genaue Stil den 0008 kopiert)
    - PATTERNS.md Z.51-151 (Migration-0008-Vollvorlage mit exaktem upgrade/downgrade-Body)
    - RESEARCH.md Pitfall 4 + Pitfall 7 + Critical Planner Notes 2/3 (transcript_segments NICHT granten in 0008; training.* NICHT in models.py)
    - CONTEXT.md D-03 (finale Spaltenliste transcript_archive) + D-06 (GRANT-Liste)
    - ssh -i ~/.ssh/id_ed25519_nerve root@<prod> 'cd /opt/nerve/app && bash scripts/inspect.sh schema users' (Live-Schema VOR Migration — Beleg dass is_test_user noch NICHT existiert; in Persistenz-Sektion zitieren)
    - ssh -i ~/.ssh/id_ed25519_nerve root@<prod> 'cd /opt/nerve/app && bash scripts/inspect.sh migrations' (Beleg: 0007 ist head)
  </read_first>
  <action>
**Schritt 1 — database/models.py:** In der User-Klasse direkt NACH der Zeile
`is_coach = Column(Boolean, default=False)` (Z.~78) EXAKT diese Zeile einfuegen:
```python
is_test_user        = Column(Boolean, default=False, nullable=False)
```
Kein Import noetig (Boolean bereits importiert). KEINE training.*-Tabelle in models.py
hinzufuegen (D-03/Pitfall 4 — Alembic-Autogenerate darf training.* nicht sehen).

**Schritt 2 — alembic/versions/0008_training_schema_foundation.py NEU anlegen.**
Header-Docstring exakt:
```python
"""Add training schema foundation, transcript_archive table, nerve_anon_worker grants,
and users.is_test_user column.

Revision ID: 0008
Revises: 0007
Create Date: 2026-05-29

Phase 08.23.2.D.UX.0 — Wave 1 (Migration).

NOTE: CREATE ROLE nerve_anon_worker is NOT in this migration — see Task 2 Runbook.
Passwords must not appear in VCS (CLAUDE.md HART Secrets-Regel).
Roles are cluster-global and belong outside schema migrations.

NOTE: GRANT on public.transcript_segments is NOT here — that table is created
in migration 0009 (D.UX.1). Add that GRANT in 0009.
"""
```
Imports + Revision-Header:
```python
from alembic import op
import sqlalchemy as sa

revision = '0008'
down_revision = '0007'
branch_labels = None
depends_on = None
```
upgrade() — EXAKT diese Reihenfolge (CREATE SCHEMA vor CREATE TABLE, GRANTs nach Tabelle):
```python
def upgrade() -> None:
    op.execute("CREATE SCHEMA IF NOT EXISTS training")
    op.execute("""
        CREATE TABLE IF NOT EXISTS training.transcript_archive (
            id               BIGSERIAL   PRIMARY KEY,
            source_call_hash TEXT        NOT NULL,
            segment_index    INTEGER     NOT NULL,
            speaker          TEXT        NOT NULL
                CHECK (speaker IN ('berater', 'kunde', 'system')),
            text             TEXT        NOT NULL,
            ts_offset_ms     INTEGER     NOT NULL,
            schema_version   SMALLINT    NOT NULL DEFAULT 1,
            archived_at      TIMESTAMPTZ DEFAULT now()
        )
    """)
    op.execute("GRANT USAGE ON SCHEMA training TO nerve_anon_worker")
    op.execute("GRANT INSERT, SELECT ON training.transcript_archive TO nerve_anon_worker")
    op.execute("GRANT SELECT ON public.calls TO nerve_anon_worker")
    op.execute("GRANT SELECT ON public.users TO nerve_anon_worker")
    op.execute("GRANT SELECT ON public.conversation_logs TO nerve_anon_worker")
    # NOTE: public.transcript_segments existiert noch NICHT (kommt in 0009/D.UX.1) — kein GRANT hier
    with op.batch_alter_table('users') as batch_op:
        batch_op.add_column(
            sa.Column('is_test_user', sa.Boolean(), nullable=False,
                      server_default='false')
        )
```
downgrade() — exakt umgekehrt:
```python
def downgrade() -> None:
    with op.batch_alter_table('users') as batch_op:
        batch_op.drop_column('is_test_user')
    op.execute("REVOKE SELECT ON public.conversation_logs FROM nerve_anon_worker")
    op.execute("REVOKE SELECT ON public.users FROM nerve_anon_worker")
    op.execute("REVOKE SELECT ON public.calls FROM nerve_anon_worker")
    op.execute("REVOKE INSERT, SELECT ON training.transcript_archive FROM nerve_anon_worker")
    op.execute("REVOKE USAGE ON SCHEMA training FROM nerve_anon_worker")
    op.execute("DROP TABLE IF EXISTS training.transcript_archive")
    op.execute("DROP SCHEMA IF EXISTS training")
```
Begruendung Decision-Refs: D-01 (Hybrid: Migration macht Schema+Tabelle+GRANTs, ROLE separat),
D-02 (Nummer 0008, D.UX.1 wird 0009), D-03 (per-Segment-Row, kein FK zu public.*), D-04
(ts_offset_ms-Spalte bereitgestellt, Befuellung erst D.UX.1), D-05 (source_call_hash-Spalte als
TEXT, HMAC-Berechnung erst D.UX.1), D-06 (GRANTs nur auf existierende public.*-Tabellen).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -m py_compile alembic/versions/0008_training_schema_foundation.py database/models.py</automated>
  </verify>
  <acceptance_criteria>
    - grep `is_test_user` in database/models.py → genau 1 Treffer, mit `nullable=False`
    - grep `down_revision = '0007'` in 0008-Datei → 1 Treffer
    - grep `CREATE SCHEMA IF NOT EXISTS training` in 0008 → 1 Treffer
    - grep `transcript_segments` in 0008 → 0 Treffer (NICHT in 0008 granten — Pitfall 7)
    - grep `CREATE ROLE` in 0008 → 0 Treffer (ROLE ist Runbook, nicht Migration — D-01)
    - grep `PASSWORD` in 0008 → 0 Treffer (Secrets-Regel)
    - py_compile beider Dateien Exit 0
    - 0008 enthaelt GRANT-Zeilen fuer genau: training (USAGE), training.transcript_archive (INSERT,SELECT), public.calls (SELECT), public.users (SELECT), public.conversation_logs (SELECT)
  </acceptance_criteria>
  <done>0008-Migration + models.py-Spalte geschrieben, py_compile gruen, kein Secret/CREATE ROLE/transcript_segments-GRANT in der Migration.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: nerve_anon_worker Runbook-Step (CREATE ROLE + pg_hba host-Auth) auf Production + Migration 0008 upgrade auf Production</name>
  <read_first>
    - RESEARCH.md Pattern 6 (Runbook CREATE ROLE) + Anti-Pattern "CREATE ROLE ohne IF NOT EXISTS"
    - CONTEXT.md D-01 (ROLE als separater dokumentierter psql-Runbook-Step)
    - RESEARCH.md Open Question 2 (pg_hba.conf nerve_anon_worker Passwort-Auth — jetzt HARTE Vorbedingung, nicht mehr deferred)
    - CLAUDE.md "HART: Kein Local-Dev" + "deploy.sh production"
  </read_first>
  <what-built>
Migration 0008 ist geschrieben (Task 1). Dieser Schritt ist BLOCKING weil:
(a) Build/Import-Checks bestehen OHNE upgrade (Typen kommen aus models.py, nicht aus der Live-DB)
    → False-Positive-Risiko. Verifikation in Task 3/Persistenz-Sektion braucht die echten
    Spalten/Schema auf Production.
(b) Migration 0008 referenziert `nerve_anon_worker` in GRANTs → die Rolle MUSS VOR dem
    `alembic upgrade head` existieren, sonst bricht das GRANT-Statement ab.
(c) Plan 03 (S3-Backup) ruft `pg_dump -h 127.0.0.1 -U nerve_anon_worker` ueber TCP auf
    (G-2). Damit das funktioniert MUSS pg_hba.conf eine `host`-Auth-Zeile fuer die Rolle haben
    (scram-sha-256). Das ist KEIN deferred Finding mehr — es ist eine HARTE, geordnete
    Vorbedingung die hier (vor Plan 03) erledigt + verifiziert wird.
  </what-built>
  <how-to-verify>
Folgende manuellen Schritte auf Production ausfuehren (Andre / Operator, NICHT automatisierbar
weil Passwort-Generierung + Secret-Ablage + pg_hba.conf-Edit):

1. Passwort generieren:
   `openssl rand -base64 32`
2. CREATE ROLE als Runbook-Step (idempotent):
   ```
   ssh -i ~/.ssh/id_ed25519_nerve root@<prod> \
     "sudo -u postgres psql -d nerve -c \"DO \\$\\$ BEGIN IF NOT EXISTS (SELECT FROM pg_roles WHERE rolname='nerve_anon_worker') THEN CREATE ROLE nerve_anon_worker LOGIN PASSWORD '<GENERATED>'; END IF; END \\$\\$;\""
   ```
   (Alternativ schlichter: `CREATE ROLE nerve_anon_worker LOGIN PASSWORD '<GENERATED>';` — nur falls Rolle sicher noch nicht existiert.)
3. Passwort in /etc/nerve/ionos-s3.env ablegen (wird in Plan 03 angelegt; falls noch nicht da:
   vormerken als NERVE_ANON_WORKER_DB_PASSWORD=<GENERATED>). NIEMALS in git.
4. **[HARTE VORBEDINGUNG fuer Plan 03 — G-2] pg_hba.conf host-Auth-Zeile fuer nerve_anon_worker
   ueber TCP ergaenzen.** Der S3-Backup ruft `pg_dump -h 127.0.0.1` auf → Postgres matcht eine
   `host`-Regel (nicht `peer`/`local`). Ohne diese Zeile wird PGPASSWORD ignoriert und die
   Connection abgelehnt. Diese Zeile MUSS vor Plan 03 stehen.
   a. Pruefen ob bereits vorhanden:
      `ssh ... 'grep nerve_anon_worker /etc/postgresql/*/main/pg_hba.conf'`
   b. Falls leer: Zeile ergaenzen (vor evtl. breiteren reject/all-Regeln, scram-sha-256):
      ```
      ssh ... "echo 'host    nerve    nerve_anon_worker    127.0.0.1/32    scram-sha-256' \
        | sudo tee -a /etc/postgresql/*/main/pg_hba.conf"
      ```
      (Falls der Cluster md5 statt scram nutzt: `md5` verwenden — gegen die bestehende
       Datei pruefen, nicht raten.)
   c. Reload (kein Restart noetig fuer pg_hba):
      `ssh ... 'sudo systemctl reload postgresql'`
   d. **Connectivity-Verifikation ueber TCP als die Rolle** (genau der Pfad den Plan 03 nutzt):
      ```
      ssh ... "PGPASSWORD='<GENERATED>' psql -h 127.0.0.1 -U nerve_anon_worker -d nerve -c 'SELECT 1'"
      ```
      → muss `1` zurueckgeben (kein "peer authentication failed", kein "connection refused").
      Diesen Output in Plan 01 Persistenz-Sektion zitieren.
5. Verifikation Rolle existiert:
   `ssh ... "sudo -u postgres psql -d nerve -c '\du nerve_anon_worker'"` → zeigt die Rolle.
6. Migration deployen + upgrade auf Production:
   `git add -A && git commit && git push && bash deploy.sh production`
   Falls deploy.sh-Gate durch pre-existing test_ft_seed blockt (siehe STATE.md Plan-01..08-Deviation):
   Alembic manuell als nerve_app-User upgraden + .deploy_meta patchen (identisches Workaround).
7. upgrade-Verifikation:
   `ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` → head ist 0008.
  </how-to-verify>
  <resume-signal>Type "role+migration done" wenn nerve_anon_worker existiert, die pg_hba host-Zeile gesetzt ist und `psql -h 127.0.0.1 -U nerve_anon_worker` Erfolg (SELECT 1 = 1) liefert, 0008 auf Production = head, und Passwort in Secrets abgelegt ist.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Email-Guard in _send() (String-normalisiert) + idempotenter seed_test_user.py + Production-Verifikation</name>
  <files>services/email_service.py, scripts/seed_test_user.py</files>
  <read_first>
    - services/email_service.py Z.17-26 (exakter _send()-Body, Insertion-Point) — siehe <interfaces>
    - PATTERNS.md Z.154-280 (Email-Guard-Zielzustand + seed_test_user.py-Vollvorlage)
    - CONTEXT.md A-02 (Domain-Check @nerve.local, kein DB-Lookup, kein ENV-Schalter) + A-04 (idempotenter Seed)
    - scripts/migrate_polish_38_counters.py (sys.path-Bootstrap + get_session + try/finally Analog)
    - Call-Site-Shapes pruefen (G-1): routes/auth.py:239,356 / routes/oauth.py:139,149 / routes/admin_views.py:69 — bestaetigen ob `to` als String oder Liste uebergeben wird (Code-Lesen lokal OK via git-Sync). Der Guard muss BEIDE Formen abfangen.
    - ssh -i ~/.ssh/id_ed25519_nerve root@<prod> 'cd /opt/nerve/app && bash scripts/inspect.sh schema users' (NACH 0008 — Beleg dass is_test_user jetzt existiert; in Persistenz-Sektion zitieren)
  </read_first>
  <action>
**Schritt 1 — services/email_service.py:** In _send() den Guard-Block einfuegen NACH dem
api_key-Check, VOR dem try-Block. Exakter Zielzustand:
```python
def _send(payload):
    if not resend.api_key:
        print('[EMAIL] RESEND_API_KEY missing — skip send')
        return False
    # Test-User-Guard (A-02): @nerve.local ist nicht-routbar (.local IANA-reserviert).
    # Domain-Check, kein DB-Lookup — alle @nerve.local-Empfaenger sind Test-Accounts.
    # WICHTIG (G-1): `to` zuerst zu einer Liste normalisieren. Resend akzeptiert sowohl
    # einen String als auch eine Liste. Ohne isinstance-Normalisierung wuerde
    # `any(... for r in recipients)` bei einem bare-String ueber EINZELZEICHEN iterieren
    # ('a','n','d',...), der endswith-Check waere immer False → Test-Mail wuerde
    # gesendet → A-02 ausgehebelt.
    recipients = payload.get('to', [])
    if isinstance(recipients, str):
        recipients = [recipients]
    if any(r.endswith('@nerve.local') for r in recipients):
        print(f'[EMAIL] test-user recipient {recipients!r}, skip')
        return False
    try:
        resend.Emails.send(payload)
        return True
    except Exception as e:
        print(f'[EMAIL] send failed: {e}')
        return False
```
Damit sind alle 6 Call-Sites (routes/auth.py:239,356; routes/oauth.py:139,149;
routes/admin_views.py:69) durch den EINEN Chokepoint abgedeckt (A-02/A-03) — egal ob die
Call-Site `to` als Liste ODER als bare-String uebergibt. KEIN ENV-Schalter, KEINE
per-Call-Site-Checks.

**Schritt 2 — scripts/seed_test_user.py NEU.** Idempotent via Existenz-Check (A-04). Inhalt
exakt (PATTERNS.md-Vorlage):
```python
#!/usr/bin/env python3
"""seed_test_user.py — Idempotentes Test-User-Seed (Phase 08.23.2.D.UX.0)
Legt andre-test@nerve.local mit is_test_user=True an.
Ausfuehren (einmalig auf Production via SSH, nach Migration 0008):
    cd /opt/nerve/app && source venv/bin/activate && \
    TEST_USER_PASSWORD='<aus-secrets>' python scripts/seed_test_user.py
"""
import os
import sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from database.db import get_session
from database.models import User, Organisation
from werkzeug.security import generate_password_hash

EMAIL = 'andre-test@nerve.local'
PASSWORD = os.environ.get('TEST_USER_PASSWORD', '')

if not PASSWORD:
    print('[SEED] FEHLER: TEST_USER_PASSWORD Env-Variable nicht gesetzt')
    sys.exit(1)

db = get_session()
try:
    existing = db.query(User).filter_by(email=EMAIL).first()
    if existing:
        print(f'[SEED] Test-User {EMAIL} existiert bereits (id={existing.id}) — skip')
        sys.exit(0)
    org = db.query(Organisation).first()
    if not org:
        print('[SEED] FEHLER: Keine Organisation in DB — Seed nicht moeglich')
        sys.exit(1)
    user = User(
        email=EMAIL,
        passwort_hash=generate_password_hash(PASSWORD),
        org_id=org.id,
        rolle='member',
        is_test_user=True,
        is_superadmin=False,
        aktiv=True,
    )
    db.add(user)
    db.commit()
    print(f'[SEED] Test-User {EMAIL} angelegt (id={user.id}, is_test_user=True)')
finally:
    db.close()
```
HINWEIS fuer Executor: VOR dem Schreiben den User-Konstruktor gegen models.py pruefen — Felder
email/passwort_hash/org_id/rolle/aktiv muessen exakt den Spalten-/Attributnamen entsprechen
(Cross-Layer-Naming, Punkt 21). Falls ein Attribut anders heisst (z.B. `password_hash` statt
`passwort_hash`), an models.py anpassen, nicht raten.

**Schritt 3 — Deploy + Production-Verifikation (P-04 HART):**
`git add -A && git commit && git push && bash deploy.sh production`
Dann seed einmalig auf Production:
```
ssh -i ~/.ssh/id_ed25519_nerve root@<prod> \
  "cd /opt/nerve/app && source venv/bin/activate && TEST_USER_PASSWORD='<aus-secrets>' python scripts/seed_test_user.py"
```
Email-Guard auf Production smoke-testen — BEIDE Formen (G-1):
```
# (a) `to` als LISTE:
ssh ... "cd /opt/nerve/app && source venv/bin/activate && python -c \"from services.email_service import _send; print(_send({'to':['andre-test@nerve.local'],'from':'x','subject':'s','html':'h'}))\""
# (b) `to` als BARE STRING (genau der Bypass den G-1 fixt):
ssh ... "cd /opt/nerve/app && source venv/bin/activate && python -c \"from services.email_service import _send; print(_send({'to':'andre-test@nerve.local','from':'x','subject':'s','html':'h'}))\""
```
→ BEIDE muessen `False` zurueckgeben + Log `[EMAIL] test-user recipient`. Der bare-String-Fall
ist der entscheidende Beleg dass G-1 gefixt ist (vor dem Fix waere er `True`/gesendet).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -m py_compile services/email_service.py scripts/seed_test_user.py</automated>
  </verify>
  <acceptance_criteria>
    - grep `isinstance(recipients, str)` in services/email_service.py → 1 Treffer (G-1 String-Normalisierung vorhanden)
    - grep `@nerve.local` in services/email_service.py → 1 Treffer im any()-Check
    - grep `endswith('@nerve.local')` in services/email_service.py → 1 Treffer
    - grep `[EMAIL] test-user recipient` in services/email_service.py → 1 Treffer
    - Reihenfolge im Code: `isinstance(recipients, str)` steht VOR dem `any(... endswith ...)`-Check (visuell pruefen)
    - grep `is_test_user=True` in scripts/seed_test_user.py → 1 Treffer
    - grep `andre-test@nerve.local` in scripts/seed_test_user.py → 1 Treffer
    - Production-REPL (Liste): `_send({'to':['andre-test@nerve.local'],...})` gibt `False` zurueck + Log `[EMAIL] test-user recipient` (in SUMMARY zitieren)
    - Production-REPL (BARE STRING): `_send({'to':'andre-test@nerve.local',...})` gibt `False` zurueck + Log `[EMAIL] test-user recipient` (in SUMMARY zitieren — kritischer G-1-Beleg)
    - Production: `inspect.sh sample users 200` zeigt eine Zeile mit email=andre-test@nerve.local und is_test_user=t (oder `inspect.sh count users` vor/nach +1; in SUMMARY zitieren)
    - py_compile beider Dateien Exit 0
  </acceptance_criteria>
  <done>Email-Guard blockt @nerve.local auf Production (False + Log) sowohl bei `to`=Liste als auch bei `to`=bare-String (G-1), Test-Account existiert mit is_test_user=TRUE, beide Dateien py_compile-gruen.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen

- `public.users` (schreiben: neue Spalte is_test_user; INSERT Test-Account via Seed)
- `training.transcript_archive` (anlegen — neue Tabelle, in dieser Phase kein Daten-Write; D.UX.1 schreibt)
- DB-Rolle `nerve_anon_worker` (cluster-global, Runbook) + GRANTs auf training.* und public.calls/users/conversation_logs + pg_hba host-Auth-Zeile

### inspect.sh-Beleg (Executor MUSS verbatim einfuegen)

```
# VOR Migration (Beleg: is_test_user existiert noch nicht):
$ ssh -i ~/.ssh/id_ed25519_nerve root@<prod> 'cd /opt/nerve/app && bash scripts/inspect.sh schema users'
[VERBATIM Output hier einfuegen — NICHT zusammenfassen]

# Migration-Stand vor upgrade (Beleg: 0007 head):
$ ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
[VERBATIM Output hier einfuegen]

# NACH Migration 0008:
$ ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh schema users'
[VERBATIM — muss is_test_user BOOLEAN NOT NULL DEFAULT false zeigen]

$ ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
[VERBATIM — muss 0008 als head zeigen]

# training-Schema + Tabelle (inspect.sh schema nutzt \d ohne Schema-Qualifier → direkt psql, Pitfall 6):
$ ssh ... "sudo -u nerve_app psql -d nerve -c '\dn training'"
[VERBATIM — Schema training muss existieren]
$ ssh ... "sudo -u nerve_app psql -d nerve -c '\d training.transcript_archive'"
[VERBATIM — alle 8 Spalten + speaker-CHECK]

# GRANTs (D-06 Beleg):
$ ssh ... "sudo -u postgres psql -d nerve -c '\dp training.transcript_archive'"
[VERBATIM — INSERT/SELECT fuer nerve_anon_worker, KEIN Grant fuer App-Rolle]

# nerve_anon_worker TCP-Connectivity (G-2 Vorbedingung Plan 03, aus Task 2):
$ ssh ... "PGPASSWORD='***' psql -h 127.0.0.1 -U nerve_anon_worker -d nerve -c 'SELECT 1'"
[VERBATIM — muss 1 zeigen; Beleg dass pg_hba host-Auth greift, kein peer-Fehler]

# D-07 App-Rollen-Isolation — TIEF (C-BLOCKER-3): NICHT nur der Name aus env-keys.
# (a) Welcher Rollen-Name connectet die App?
$ ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh env-keys'   # DATABASE_URL-Name sichtbar
$ ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh grep "DB_USER"'  # bzw. Rolle aus DATABASE_URL
[VERBATIM — App-Rollen-Name, z.B. nerve_app]

# (b) Hat die App-Rolle Superuser-Attribut? (Superuser umgeht GRANTs komplett → Isolation void)
$ ssh ... "sudo -u postgres psql -d nerve -c '\du nerve_app'"
[VERBATIM — Attribut-Liste der App-Rolle. Darf NICHT 'Superuser' enthalten.
 Falls Superuser → D-07 Finding: Isolation D-06 ist void (Superuser ignoriert GRANTs).]

# (c) Ist die App-Rolle OWNER des training-Schemas? (Owner umgeht GRANTs auf eigenes Schema)
$ ssh ... "sudo -u postgres psql -d nerve -c \"SELECT n.nspname, r.rolname AS owner FROM pg_namespace n JOIN pg_roles r ON n.nspowner=r.oid WHERE n.nspname='training';\""
[VERBATIM — owner darf NICHT die App-Rolle sein (sollte postgres / Migration-Runner sein).
 Falls owner = App-Rolle → D-07 Finding: Isolation D-06 ist void (Owner ignoriert GRANTs).]
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `User.is_test_user` | geschrieben (Migration + Seed), gelesen (D.UX.1/E JOIN) | DB-Spalte public.users.is_test_user | ❓ inspect.sh schema users NACH 0008 |
| `payload['to']` @nerve.local | gelesen in _send() (String→Liste normalisiert, G-1) | KEINE DB — reiner Domain-String-Check (A-02, kein DB-Lookup) | ✓ Code-Pfad, kein Persistenz-Layer |
| `training.transcript_archive.*` | angelegt (kein Write in D.UX.0) | DB-Tabelle training.transcript_archive | ❓ \d training.transcript_archive |
| `nerve_anon_worker` GRANTs | gesetzt (Migration) | pg_catalog (Rollen-Privilegien) | ❓ \dp training.transcript_archive |
| `nerve_anon_worker` TCP-Auth | host-Zeile (Task 2, G-2) | pg_hba.conf | ❓ psql -h 127.0.0.1 -U nerve_anon_worker SELECT 1 |
| App-Rolle: Name + Superuser + Owner (D-07) | App connectet als ? | DATABASE_URL + pg_authid + pg_namespace.nspowner | ❓ env-keys + \du <app-role> + Owner-Query (C-BLOCKER-3: TIEF) |

### Bei Diskrepanz: STOP + Replan
Wenn is_test_user nach 0008 NICHT in inspect.sh erscheint, oder training-Schema fehlt, oder
`psql -h 127.0.0.1 -U nerve_anon_worker` NICHT verbindet (pg_hba/G-2), oder die App-Rolle
Superuser ist ODER Owner von training (D-07/C-BLOCKER-3 — Isolation greift dann NICHT) →
Finding dokumentieren, NICHT weiterbauen. D-07 erzwingt KEINE Umstellung der App-Rolle (das ist
G/MEET Wave 1), aber die Verifikation MUSS tief sein (Name + Superuser + Owner) und jeder Finding
MUSS im SUMMARY stehen. Die pg_hba-Vorbedingung (G-2) ist dagegen HART und blockiert Plan 03.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App (nerve_app) → DB | App-Rolle darf NICHT auf training.* zugreifen (Isolation D-06) — nur gueltig wenn App-Rolle NICHT Superuser/Owner (D-07 tief verifiziert) |
| nerve_anon_worker → DB | Background-Job-Rolle (D.UX.1): read public.*, write training.* — kein DDL/DELETE; verbindet ueber TCP (pg_hba host, G-2) |
| Test-Account → Email-Provider (Resend) | @nerve.local-Mails duerfen Resend nie erreichen (auch nicht bei `to`=bare-String, G-1) |
| VCS (git) → Secrets | Passwoerter/Keys duerfen nie in Migration/Repo landen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DUX0-01 | Information Disclosure | nerve_anon_worker DB-Passwort | mitigate | CREATE ROLE als Runbook-Step (Task 2), Passwort via `openssl rand`, Ablage in /etc/nerve/ionos-s3.env chmod 600 — NICHT in Migration/VCS (D-01). grep PASSWORD in 0008 = 0 Treffer (acceptance). |
| T-DUX0-02 | Elevation of Privilege | nerve_anon_worker | mitigate | Rolle bekommt nur USAGE+INSERT/SELECT auf training.* und SELECT auf 3 public.*-Tabellen. Kein DDL, kein DELETE, kein GRANT auf andere Schemas (D-06). pg_hba host-Zeile nur 127.0.0.1/32 (G-2, kein Remote-Zugriff). |
| T-DUX0-03 | Spoofing/Information Disclosure | Test-Account-Emails | mitigate | _send()-Guard blockt jeden @nerve.local-Empfaenger VOR Resend-API-Call (A-02). String-Normalisierung (G-1) verhindert Bypass via bare-String. Production-REPL-Beleg (Liste UND String) in acceptance. |
| T-DUX0-04 | Tampering (Re-Identifikation) | training.transcript_archive | mitigate | KEINE FK zu public.*/crm.* (ADR-Pflicht). source_call_hash ist TEXT fuer HMAC (D-05, befuellt in D.UX.1), ts_offset_ms-Spalte fuer per-Call-Salt (D-04). D.UX.0 stellt nur die FK-freie Struktur bereit. |
| T-DUX0-05 | Elevation of Privilege | App-Rolle = Superuser/Owner? | accept (Phase) | D-07: TIEF verifizieren (\du + Owner-Query, C-BLOCKER-3) + Finding, KEINE Erzwingung. Volle public.*-only-Restriktion ist G/MEET-Wave-1-Scope. Risiko bewusst akzeptiert fuer diese Phase, aber Verifikation ist deep (nicht name-only). |
</threat_model>

<verification>
- Migration 0008 auf Production = head (inspect.sh migrations)
- users.is_test_user existiert auf Production (inspect.sh schema users)
- training-Schema + transcript_archive + GRANTs existieren (psql \dn / \d / \dp)
- nerve_anon_worker verbindet ueber TCP (psql -h 127.0.0.1, G-2 — Vorbedingung Plan 03)
- Email-Guard blockt @nerve.local (Production-REPL: False + Log) — BEIDE Formen (Liste + bare-String, G-1)
- Test-Account existiert mit is_test_user=TRUE (inspect.sh sample/count users)
- D-07 TIEF dokumentiert: App-Rollen-Name + Superuser-Attribut (\du) + training-Owner (Owner-Query) — C-BLOCKER-3
- Persistenz-Schicht-Verifikation-Sektion mit verbatim inspect.sh-Output befuellt
</verification>

<success_criteria>
- is_test_user BOOLEAN NOT NULL DEFAULT FALSE auf Production
- training.transcript_archive mit 8 Spalten + speaker-CHECK + KEIN FK
- nerve_anon_worker mit exakt den GRANTs aus D-06 (kein transcript_segments-GRANT) + pg_hba host-Auth (G-2)
- _send() blockt @nerve.local; alle 6 Call-Sites abgedeckt; bare-String-Bypass geschlossen (G-1)
- andre-test@nerve.local mit is_test_user=TRUE, idempotent re-runnable
- Kein Secret/CREATE ROLE in der Migration
- D-07 TIEF verifiziert (Superuser + Owner, nicht name-only — C-BLOCKER-3)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation/08.23.2.D.UX.0-01-SUMMARY.md`
Im SUMMARY: alle verbatim inspect.sh-Outputs, der bare-String-REPL-Beleg (G-1), der TCP-Connect-Beleg (G-2), der tiefe D-07-Finding (\du + Owner-Query, C-BLOCKER-3), Bestaetigung dass D.UX.1-Migration auf 0009 umnummeriert werden muss.
</output>
</content>
</invoke>
