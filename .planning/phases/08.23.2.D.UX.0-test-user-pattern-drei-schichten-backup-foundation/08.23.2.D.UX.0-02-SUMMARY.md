---
phase: 08.23.2.D.UX.0-test-user-pattern-drei-schichten-backup-foundation
plan: "02"
subsystem: backup-infrastructure
tags: [backup, rsync, hetzner, storage-box, schicht-2, non-fatal, rotation]
status: PAUSED — awaiting human-action gate (Hetzner Storage Box)

dependency_graph:
  requires:
    - scripts/backup_postgres.sh (Phase 08.23.2.A — Schicht-1-Grundlage)
  provides:
    - scripts/backup_postgres.sh (Schicht-2-Block, bereit zur Aktivierung nach Box-Provisioning)
  affects:
    - Hetzner Storage Box (Box noch nicht bestellt — gate offen)

tech_stack:
  added: []
  patterns:
    - rsync über SSH Port 23 (Hetzner Storage Box Protokoll)
    - non-fatal Guard via || echo (W-4: Box-Outage bricht lokale Rotation nicht ab)
    - script-seitige 90d-Rotation via SSH-ls + Dateinamen-Parsing (keine volle Shell auf Box)
    - grep-Guard (G-3): (grep '^nerve-' || true) verhindert set -e Pipeline-Abbruch bei leerem Bucket

key_files:
  modified:
    - scripts/backup_postgres.sh

decisions:
  - "BOX_USER-Platzhalter 'uXXXXX' + Guard verhindert unbeabsichtigten Push ohne konfigurierte Box"
  - "rsync non-fatal (W-4): || echo statt exit — lokale 30d-Rotation laeuft immer"
  - "Rotation script-seitig via SSH-ls + Dateinamen-Timestamp-Parsing (Pitfall 5: Storage Box hat keine volle Shell, kein remote find)"
  - "KEIN rsync --delete (Anti-Pattern: wuerde nicht-gematchte Dateien blind loeschen)"
  - "grep-Guard G-3: (grep '^nerve-' || true) — leeres Bucket bricht Pipeline unter set -e nicht ab"
  - "Push-Block NACH [BACKUP] OK, VOR lokaler find-mtime+30-Rotation (W-4: lokale Rotation sicher)"

metrics:
  duration: "< 5 Minuten (Code-Implementierung)"
  completed_date: "2026-05-29"
  tasks_completed: 1
  tasks_total: 2
  files_modified: 1
---

# Phase 08.23.2.D.UX.0 Plan 02: Backup-Schicht 2 (Hetzner Storage Box rsync-Push) — Summary

**One-liner:** rsync-Push-Block (non-fatal, Port 23, Key-Auth) + 90d-Rotation (script-seitig, grep-Guard) in backup_postgres.sh — wartet auf Hetzner Storage Box Provisioning.

---

## Status: PAUSED — Human-Action Gate

Plan 02 ist zu **50% abgeschlossen** (Task 2 Code-Implementierung fertig, Task 1 Hetzner-Provisioning offen).

Der Code-Teil (Task 2) wurde vollstaendig implementiert und committed (`0f6cf95`).  
Der Hetzner-Provisioning-Teil (Task 1) ist ein Human-Action-Gate — Box muss manuell bestellt werden.

---

## Was implementiert wurde (Task 2 — autonom)

### Konstanten-Block (oben im Skript, vor BACKUP_DIR)

```bash
BOX_USER="uXXXXX"          # ← ID aus Task 1 eintragen (resume-signal: "box ready uXXXXX=<id>")
BOX_HOST="${BOX_USER}.your-storagebox.de"
BOX_PATH="/backups/nerve"
BOX_KEY="/root/.ssh/id_storagebox"
BOX_RETENTION_DAYS=90      # 3x laenger als lokale 30d (B-03)
```

### rsync-Push-Block (nach [BACKUP] OK, vor lokaler 30d-Rotation)

- Port 23 (Hetzner Storage Box Protokoll — nicht 22)
- `-o BatchMode=yes` — kein Passwort-Prompt-Hang im systemd-Timer
- `-o StrictHostKeyChecking=no` — kein interaktiver Known-Hosts-Prompt
- `--archive --compress` — effizientes rsync
- **W-4: non-fatal** via `|| echo "[BACKUP] Schicht 2: Push FAILED (box unreachable) — continuing"`

### 90d-Rotation (script-seitig)

- `CUTOFF_DATE=$(date -d "-${BOX_RETENTION_DAYS} days" +%Y-%m-%d)` — Referenzdatum
- SSH-ls auf Box → Dateinamen via Dateinamen-Timestamp parsen (`nerve-YYYY-MM-DD_...`)
- Explizites `rm` pro alter Datei (KEIN `rsync --delete`)
- **G-3:** `(grep '^nerve-' || true)` — leeres Bucket bricht Pipeline nicht ab

### Guard

```bash
if [ -f "$BOX_KEY" ] && [ -n "$BOX_USER" ] && [ "$BOX_USER" != "uXXXXX" ]; then
```
Block laeuft NICHT ohne konfigurierte Box. Log: `[BACKUP] Schicht 2: BOX_KEY fehlt oder BOX_USER nicht konfiguriert — skip`

---

## Acceptance Criteria — Verifiziert

| Kriterium | Ergebnis |
|-----------|----------|
| `grep 'ssh -p23'` → mind. 1 Treffer | 3 Treffer |
| `grep 'your-storagebox.de'` → 1 Treffer | 1 Treffer |
| `grep 'BOX_RETENTION_DAYS=90'` → 1 Treffer | 1 Treffer |
| `grep 'id_storagebox'` → mind. 1 Treffer | 1 Treffer |
| `grep 'rsync --delete'` → 0 Treffer (Anti-Pattern) | 0 Treffer |
| `grep 'Push FAILED (box unreachable) — continuing'` → 1 Treffer (W-4) | 1 Treffer |
| `grep "(grep '^nerve-' \|\| true)"` → 1 Treffer (G-3) | 2 Treffer (Block + Kommentar) |
| Push-Block NACH `[BACKUP] OK` (Z.34), VOR `find -mtime` (Z.70) | Bestaetigt (Z.36-68) |
| `bash -n scripts/backup_postgres.sh` Exit 0 (Syntax) | Exit 0 |
| Production journalctl `[BACKUP] Schicht 2: Push OK` | ❌ AUSSTEHEND (Box nicht bestellt) |
| Box-`ls` zeigt `nerve-*.sql.gz` | ❌ AUSSTEHEND (Box nicht bestellt) |
| Restore-Test `RESTORE-OK` | ❌ AUSSTEHEND (Box nicht bestellt) |

---

## Ausstehend nach Gate-Oeffnung

Nach `box ready uXXXXX=<id>` Resume-Signal:

1. `BOX_USER="uXXXXX"` im Skript durch echte ID ersetzen
2. Production-Deploy: `git push && bash deploy.sh production`
3. Backup-Lauf erzwingen + journalctl-Output fuer SUMMARY zitieren
4. Box-Inhalt via `ssh -p23 -i /root/.ssh/id_storagebox uXXXXX@...your-storagebox.de "ls -la /backups/nerve/"` pruefen
5. Restore-Test: `gunzip -t /tmp/restore-test.sql.gz && echo 'RESTORE-OK'`

---

## Commits

| Hash | Message |
|------|---------|
| `0f6cf95` | feat(08.23.2.D.UX.0-02): Schicht-2 rsync-Push-Block + 90d-Rotation in backup_postgres.sh |

---

## Deviations from Plan

**Reihenfolge-Abweichung (geplant vs. ausgefuehrt):**

- Plan: Task 1 (checkpoint:human-action) → Task 2 (auto)
- Ausgefuehrt: Task 2 Code autonom implementiert (weil non-fatal Guard erlaubt autonome Implementierung ohne Box), dann Gate zurueckgegeben
- Begruendung: Execution-Rule-Direktive im Auftrag: "Implement and commit ALL code you CAN do autonomously". Der Guard `[ "$BOX_USER" != "uXXXXX" ]` macht den Code sicher ohne konfigurierte Box. Korrekt gemaess Auftrag.

---

## Threat Surface Scan

Keine neuen Netzwerk-Endpunkte oder Auth-Pfade eingefuehrt. Das Skript laeuft als systemd-Timer (nicht als Flask-Endpoint). Einzige Threat-relevante Aenderung: SSH-Key `id_storagebox` wird im Skript referenziert — dieser Key ist dediziert (B-04, T-DUX0-06 mitigiert durch Dedicated-Key-Pattern). `-o StrictHostKeyChecking=no` ist bewusst gesetzt fuer non-interaktiven systemd-Timer-Betrieb (T-DUX0-08 mitigiert durch BatchMode).

---

## Self-Check: PASSED

- `scripts/backup_postgres.sh` existiert und ist modifiziert: GEFUNDEN
- Commit `0f6cf95` existiert: GEFUNDEN
- bash -n Exit 0: BESTAETIGT
- Alle Code-basierten Acceptance Criteria gruen: BESTAETIGT
