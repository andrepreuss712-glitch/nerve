---
phase: 08.23.2.D.UX.0
slug: test-user-pattern-drei-schichten-backup-foundation
status: verified
threats_open: 0
asvs_level: 1
created: 2026-05-30
---

# Phase 08.23.2.D.UX.0 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Aggregated from the `<threat_model>` blocks of plans 01–04 and verified against each plan's SUMMARY "Threat Surface Scan".

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| App (nerve_app) → DB | App-Rolle darf NICHT auf `training.*` zugreifen (Isolation D-06) — nur gueltig wenn App-Rolle NICHT Superuser/Owner (D-07 tief verifiziert) | Produktions-PII (public.*) vs. anonymisierte training.* |
| nerve_anon_worker → DB | Background-Job-Rolle (D.UX.1): read `public.*`, write `training.*` — kein DDL/DELETE; TCP via pg_hba host (G-2, 127.0.0.1/32) | Lese-PII + Schreib-anonymisiert |
| Test-Account → Email-Provider (Resend) | `@nerve.local`-Mails duerfen Resend nie erreichen (auch nicht bei `to`=bare-String, G-1) | Test-Account-Emails |
| Production-Server → Hetzner Storage Box | rsync ueber dediziertem SSH-Key (Port 23), kein Passwort, BatchMode | Klartext-Produktions-Backup |
| Production-Server → IONOS S3 | aws-cli v2 mit Access-Key/Secret aus `/etc/nerve/ionos-s3.env`; WORM gegen Account-Kompromittierung | Nur anonymisierte training.*-Daten (C-03) |
| Cross-Anbieter-Transfer (DE→IONOS DE) | nur anonymisierte training.*-Daten verlassen Hetzner | Anonymisierte training.* |
| VCS (git) / Doku → Secrets | Passwoerter/Keys/Access-Keys duerfen nie in Migration/Repo/DSGVO-Doku landen | Credentials |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-DUX0-01 | Information Disclosure | nerve_anon_worker DB-Passwort | mitigate | CREATE ROLE als Runbook-Step, Passwort via `openssl rand`, Ablage `/etc/nerve/ionos-s3.env` chmod 600 — nicht in Migration/VCS (D-01). grep PASSWORD in 0008 = 0 Treffer | closed |
| T-DUX0-02 | Elevation of Privilege | nerve_anon_worker | mitigate | Nur USAGE+INSERT/SELECT auf `training.*` + SELECT auf 3 public.*-Tabellen. Kein DDL/DELETE/GRANT (D-06). pg_hba host nur 127.0.0.1/32 (G-2) | closed |
| T-DUX0-03 | Spoofing / Information Disclosure | Test-Account-Emails | mitigate | `_send()`-Guard blockt jeden `@nerve.local`-Empfaenger VOR Resend-API-Call (A-02). String-Normalisierung (G-1) verhindert bare-String-Bypass. Production-REPL-Beleg | closed |
| T-DUX0-04 | Tampering (Re-Identifikation) | training.transcript_archive | mitigate | KEINE FK zu public.*/crm.* (ADR-Pflicht). `source_call_hash` TEXT fuer HMAC (D-05), `ts_offset_ms`-Spalte fuer per-Call-Salt (D-04). FK-freie Struktur bereitgestellt | closed |
| T-DUX0-05 | Elevation of Privilege | App-Rolle = Superuser/Owner? | accept (Phase) | D-07 TIEF verifiziert (C-BLOCKER-3): `nerve_app` ist KEIN Superuser und NICHT Owner von `training` → Isolation D-06 aktiv. Volle public.*-only-Restriktion ist G/MEET-Wave-1-Scope. Bewusst akzeptiert fuer diese Phase | closed |
| T-DUX0-06 | Information Disclosure | SSH-Key id_storagebox | mitigate | Dedizierter Key (nur fuer die Box, B-04), `/root/.ssh/`, chmod 600. Kein Passwort-Auth | closed |
| T-DUX0-07 | Tampering | Backup-Rotation | mitigate | KEIN `rsync --delete`. Rotation ausschliesslich dateinamenbasiert mit explizitem `rm` pro alter Datei. grep-Guard (G-3) verhindert Pipeline-Abbruch bei leerem Bucket | closed |
| T-DUX0-08 | Denial of Service | systemd-Timer-Hang / Box-Outage | mitigate | `-o BatchMode=yes` + `StrictHostKeyChecking=no` verhindern interaktiven Hang. `|| echo` (W-4) macht rsync non-fatal → lokale 30d-Rotation laeuft auch bei Box-Outage | closed |
| T-DUX0-09 | Information Disclosure | Box gleicher Anbieter (Hetzner) | accept | B-01: gleicher AVV, gleiche Jurisdiktion (DE). Ransomware-Schutz gegen Account-Kompromittierung liefert Schicht 3 (Plan 03, WORM). Bewusst akzeptiert | closed |
| T-DUX0-10 | Tampering / Denial | Ransomware / Account-Kompromittierung loescht Backups | mitigate | Object-Lock COMPLIANCE 30d (C-02): niemand kann vor Ablauf loeschen/ueberschreiben — auch nicht mit Root/Master-Key. KEIN Governance-Mode. Versioning Enabled. get-object-lock-configuration-Beleg | closed |
| T-DUX0-10b | Denial (Self-inflicted) | Fehlkonfig erster Upload = 30d un-loeschbarer Muell | mitigate | Staged WORM bring-up (C-BLOCKER-4): erst Object-Lock-enabled, dann 1-Tages-Smoke-Roundtrip, 30d-COMPLIANCE-Default ERST nach erfolgreichem Smoke-Restore. Fehlkonfig kostet ≤1 Tag | closed |
| T-DUX0-11 | Information Disclosure | IONOS S3 Credentials | mitigate | `/etc/nerve/ionos-s3.env` chmod 600 root:root (C-06), getrennt von App-.env, nie in git | closed |
| T-DUX0-12 | Information Disclosure | Cross-Anbieter-Transfer von PII | mitigate | Nur `training.*` (anonymisiert, C-03) verlaesst Hetzner. Klartext-/Produktionsdaten bleiben in DE/Hetzner. Write-Time-Filter (D.UX.1) haelt Test-Daten draussen | closed |
| T-DUX0-13 | Tampering (Re-Identifikation) | training-Dump | mitigate | KEINE FK `training.*→public.*`. `source_call_hash`=HMAC (D-05), `ts_offset_ms`-Salt (D-04). Voller Dump ohne Cross-Schema-Bruecke (C-03) | closed |
| T-DUX0-14 | Repudiation / Denial | Backup nie getestet | mitigate | Monatlicher Restore-Test (C-08) schreibt `.s3_restore_status` → `/api/health`-Metrik. grep-Guard (G-3) verhindert Pipeline-Abbruch | closed |
| T-DUX0-15 | Information Disclosure | Encryption at rest | mitigate | C-09: get-bucket-encryption verifiziert SSE-S3 (AES256). Fallback `--server-side-encryption AES256` beim put-object | closed |
| T-DUX0-16 | Information Disclosure | DSGVO-Doku enthaelt Secrets | mitigate | Acceptance-Grep: 0 Treffer fuer Access-Key/Secret/Passwort. Nur Anbieter/Zweck/Jurisdiktion dokumentiert | closed |
| T-DUX0-17 | Repudiation | Falsche AVV-Angaben | mitigate | Kein erfundenes AVV-Datum — Status aus Plan-03-SUMMARY; falls offen "in Abschluss" | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-DUX0-01 | T-DUX0-05 | App-Rolle volle public.*-only-Restriktion ist G/MEET-Wave-1-Scope, nicht D.UX.0. D-07 wurde tief verifiziert (nerve_app KEIN Superuser, NICHT Owner von training → Isolation D-06 greift bereits), daher Restrisiko niedrig. Erzwingung bewusst auf Wave 1 verschoben. | André Preuß | 2026-05-30 |
| AR-DUX0-02 | T-DUX0-09 | Storage Box (Schicht 2) liegt beim selben Anbieter (Hetzner) wie Production — gleicher AVV, gleiche Jurisdiktion (DE). Ransomware-/Account-Kompromittierungs-Schutz liefert bewusst Schicht 3 (IONOS WORM, T-DUX0-10), nicht Schicht 2. | André Preuß | 2026-05-30 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-05-30 | 17 | 17 | 0 | gsd-secure-phase (artifact verification, State B) |

**Verification basis:** All 17 threats carry a disposition in the plan threat registers. Each plan's SUMMARY "Threat Surface Scan" confirms execution-time mitigation:
- Plan 01 (T-DUX0-01..05): `\du nerve_app` = kein Superuser, Owner-Query = postgres → D-07 Isolation aktiv; `is_test_user` + `@nerve.local`-Guard verifiziert.
- Plan 02 (T-DUX0-06..09): dedizierter SSH-Key, BatchMode, kein `rsync --delete`.
- Plan 03 (T-DUX0-10..15): WORM COMPLIANCE 30d (kein Governance-Bypass), staged bring-up, env chmod 600, nur training.*, SSE AES256, Restore-Test live.
- Plan 04 (T-DUX0-16..17): Acceptance-Grep 0 Secrets, kein erfundenes AVV-Datum.

No auditor subagent was spawned: `threats_open` was 0 after classification (workflow Step 3 → skip to write).

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-05-30
