---
phase: 08.19.5.6.3
plan: "01"
subsystem: pip-launcher / nerve.css
tags: [frontend, pip, cheat-sheet, css]
dependency_graph:
  requires: []
  provides: [pip-cheat-sheet-render, pip-analyse-toggle-handler]
  affects: [static/pip-launcher.js, static/nerve.css]
tech_stack:
  added: []
  patterns: [maxHeight-toggle, event-delegation, CSS-variable-only-styling]
key_files:
  created: []
  modified:
    - static/nerve.css
    - static/pip-launcher.js
decisions:
  - "D-01: data-analyse-toggle via event-delegation (analog data-briefing-toggle) — kein onclick-Inline-Attribut"
  - "D-02: maxHeight '0' / '999px' toggle fuer pip-analyse-body — analog _expandBriefingTab Pattern"
  - "D-03: empf.trim() Leer-Check (String, kein Array) — SPEC Constraint eingehalten"
metrics:
  duration: "~15 min"
  completed: "2026-05-07"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 2
---

# Phase 08.19.5.6.3 Plan 01: PiP-Briefing-Tab Cheat-Sheet-Format Summary

**One-liner:** 3-Sektions-Cheat-Sheet im PiP-Briefing-Tab — Eckdaten-Block (GF/Branche/MA/Produkt mit confidence-Guard), konditionaler Empfehlungen-Block via mdToHtml(), und kollabierter Fliesstext hinter pip-analyse-toggle Button.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | nerve.css — 4 neue pip-cheat-*/pip-analyse-toggle Klassen | 73ef6e1 | static/nerve.css |
| 2 | pip-launcher.js — Cheat-Sheet render + data-analyse-toggle Event-Delegation | d9d47a1 | static/pip-launcher.js |

## What Was Built

### Task 1: nerve.css (R-04)
Vier neue CSS-Klassen am Ende von nerve.css (nach Zeile 2989) hinzugefuegt:
- `.pip-cheat-row` — Flex-Zeile fuer Label:Wert-Paare, CSS-Variablen fuer Farben
- `.pip-cheat-sep` — HR-Trenner mit `var(--glass-border)`
- `.pip-cheat-heading` — Uppercase-Ueberschrift fuer Sektionen
- `.pip-analyse-toggle` — Toggle-Button ohne hardcodierte Farben

### Task 2: pip-launcher.js (R-01..R-05)
**Aenderung 1 — _startCall() Render-Block (Zeilen 1927-1932 ersetzt):**
- Sektion 1: Eckdaten-Block — iteriert fieldDefs [GF, Branche, MA, Produkt], rendert nur wenn `f.confidence !== 'not_found' && f.value`
- Sektion 2: Empfehlungen — `if (empf && empf.trim())` Guard, mdToHtml() fuer Markdown-Bullet-Liste
- Sektion 3: Fliesstext kollabiert — `pip-analyse-body` mit `max-height:0;overflow:hidden;transition:max-height 200ms ease;` initial

**Aenderung 2 — Event-Delegation Block (nach data-briefing-toggle Handler):**
- `data-analyse-toggle` Handler: togglet `pip-analyse-body.style.maxHeight` zwischen `'0'` und `'999px'`
- Icon-Text: `▶` / `▼` via `pipEl('pip-analyse-icon')`

## Verification Results

| Check | Result |
|-------|--------|
| `grep "\.pip-cheat-row" nerve.css` | 1+ Treffer |
| `grep "\.pip-cheat-sep" nerve.css` | 1+ Treffer |
| `grep "\.pip-cheat-heading" nerve.css` | 1+ Treffer |
| `grep "\.pip-analyse-toggle" nerve.css` | 1+ Treffer |
| `grep "data-analyse-toggle" pip-launcher.js` | 2 Treffer (render + handler) |
| `grep "mdToHtml(state.precallBriefing.text"` | 0 Treffer (alter Einzeiler entfernt) |
| `grep "not_found" pip-launcher.js` (neu) | 1 Treffer (`confidence !== 'not_found'`) |
| `grep "empf.trim"` | 1 Treffer |
| Python-Dateien in git diff | 0 |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all sections are wired to real data from `state.precallBriefing`.

## Threat Flags

None — `fields[*].value` strings flow into `contentEl.innerHTML` without escaping, but this matches the accepted risk T-08.19.5.6.3-01 documented in the plan's threat model (internal-tool context, values are short fact-fields from own backend).

## Self-Check: PASSED

- `static/nerve.css` modified: FOUND (42 lines added)
- `static/pip-launcher.js` modified: FOUND (54 insertions, 3 deletions)
- Commit 73ef6e1: FOUND (nerve.css)
- Commit d9d47a1: FOUND (pip-launcher.js)
- No Python files changed: CONFIRMED
- Old `mdToHtml(state.precallBriefing.text` one-liner: REMOVED (0 grep hits)
