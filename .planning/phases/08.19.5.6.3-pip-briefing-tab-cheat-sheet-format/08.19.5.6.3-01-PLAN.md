---
phase: 08.19.5.6.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/nerve.css
  - static/pip-launcher.js
autonomous: true
requirements:
  - R-01
  - R-02
  - R-03
  - R-04
  - R-05

must_haves:
  truths:
    - "pip-briefing-tab-content zeigt GF/Branche/MA/Produkt als Kurz-Label:Wert-Zeilen wenn confidence != not_found"
    - "Felder mit confidence === not_found erscheinen nicht im gerenderten HTML"
    - "Empfehlungen-Sektion ist sichtbar wenn empfehlungen.trim() nicht leer ist"
    - "Empfehlungen-Sektion fehlt komplett wenn empfehlungen leer oder Whitespace"
    - "Fließtext (text) ist initial nicht sichtbar — maxHeight: 0"
    - "Toggle-Button data-analyse-toggle klappt Fließtext auf und wieder zu"
    - "Keine hardcodierten Farben in neu hinzugefügten Zeilen"
    - "Keine Python-Dateien geändert"
  artifacts:
    - path: "static/nerve.css"
      provides: "pip-cheat-row, pip-cheat-sep, pip-cheat-heading, pip-analyse-toggle Klassen"
      contains: ".pip-cheat-row"
    - path: "static/pip-launcher.js"
      provides: "3-Sektions-Cheat-Sheet Render-Logik in _startCall() + data-analyse-toggle Event-Delegation"
      contains: "data-analyse-toggle"
  key_links:
    - from: "static/pip-launcher.js (_startCall)"
      to: "pip-briefing-tab-content"
      via: "contentEl.innerHTML = cheat-sheet HTML string"
      pattern: "pip-analyse-body"
    - from: "Event-Delegation Block (~line 1827)"
      to: "pip-analyse-body"
      via: "data-analyse-toggle click → pipEl('pip-analyse-body').style.maxHeight toggle"
      pattern: "data-analyse-toggle"
---

<objective>
PiP-Briefing-Tab rendert statt reinem Fließtext ein 3-Sektions-Cheat-Sheet: Eckdaten-Block (GF/Branche/MA/Produkt aus fields), Empfehlungen-Block (via mdToHtml), und den bisherigen Fließtext kollabiert hinter einem "Analyse aufklappen ▼"-Toggle.

Purpose: Im Live-Call müssen Vertriebler wichtige Daten auf einen Blick sehen — der 250-Wort-Fließtext ist dafür ungeeignet.
Output: pip-launcher.js mit neuem contentEl.innerHTML-Block + Event-Delegation; nerve.css mit 4 neuen pip-cheat-* / pip-analyse-toggle Klassen.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.19.5.6.3-pip-briefing-tab-cheat-sheet-format/08.19.5.6.3-SPEC.md
@.planning/phases/08.19.5.6.3-pip-briefing-tab-cheat-sheet-format/08.19.5.6.3-CONTEXT.md

<interfaces>
<!-- Extracted from static/pip-launcher.js — relevant to both tasks -->

Key utilities (read from file before implementing):
```javascript
// pipEl(id) — PiP-DOM-Lookup, line ~30
function pipEl(id) { ... }

// mdToHtml(str) — XSS-sichere Markdown-Konvertierung, line ~85
function mdToHtml(str) { ... }

// _expandBriefingTab() / _collapseBriefingTab() — maxHeight-Toggle-Pattern, lines 1955–1967
function _expandBriefingTab() {
  var body = pipEl('pip-briefing-tab-body');
  var icon = pipEl('pip-briefing-tab-icon');
  if (body) { body.style.maxHeight = '300px'; }
  if (icon) { icon.textContent = '▼'; }
}
function _collapseBriefingTab() {
  var body = pipEl('pip-briefing-tab-body');
  var icon = pipEl('pip-briefing-tab-icon');
  if (body) { body.style.maxHeight = '0'; }
  if (icon) { icon.textContent = '▶'; }
}
```

Current render block to REPLACE (lines 1928–1932 in _startCall()):
```javascript
var contentEl = pipEl('pip-briefing-tab-content');
if (contentEl) {
  var briefingHtml = mdToHtml(state.precallBriefing.text || '');
  contentEl.innerHTML = briefingHtml;
}
```

Event-Delegation block (line ~1827) — ADD new case after existing data-briefing-toggle handler:
```javascript
// Phase 08.20.3: Briefing Tab toggle
if (ev.target.closest('[data-briefing-toggle]')) {
  // ... existing handler (do not touch)
  ev.stopPropagation();
  return;
}
// ADD data-analyse-toggle handler AFTER this block
```

precallBriefing object schema (from SPEC.md):
```javascript
state.precallBriefing = {
  firmenname: string,
  fields: {
    geschaeftsfuehrer: { value: string, confidence: string },  // confidence === 'not_found' → skip
    branche:           { value: string, confidence: string },
    mitarbeiterzahl:   { value: string, confidence: string },
    hauptprodukt:      { value: string, confidence: string }
  },
  empfehlungen: string,  // Markdown-Bullet-Liste oder ''
  text: string           // Schicht-2-Fließtext
}
```

CSS variables available in nerve.css:
```css
--glass-border: #E2E8F0
--page-text-color: #1a1a1a
--page-text-secondary: #6B7280
--page-text-muted: #9CA3AF
```

launcher-btn-ghost (from base.html line 341):
```css
.launcher-btn-ghost {
  background: transparent;
  border: 1.5px solid var(--glass-border);
  border-radius: 8px;
  padding: 10px 16px;
  font-size: 13px;
  font-weight: 600;
  color: var(--page-text-muted);
  cursor: pointer;
  font-family: inherit;
  transition: border-color .15s, color .15s
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: nerve.css — 4 neue pip-cheat-* / pip-analyse-toggle Klassen (R-04)</name>
  <read_first>
    - static/nerve.css (letzten 30 Zeilen — wo neue Klassen angefügt werden, EOF ab Zeile ~2960)
    - static/nerve.css Zeile 2910–2940 (pip-anrede-btn als Referenz für pip-Prefix-Stil)
  </read_first>
  <files>static/nerve.css</files>
  <action>
Füge am Ende von static/nerve.css (nach Zeile 2989) einen neuen Abschnitt ein:

```css
/* ── Phase 08.19.5.6.3: PiP Cheat-Sheet ──────────────────────────────── */
.pip-cheat-row {
  display: flex;
  gap: 6px;
  font-size: 12px;
  line-height: 1.6;
  color: var(--page-text-color);
}
.pip-cheat-row span:first-child {
  color: var(--page-text-secondary);
  flex-shrink: 0;
  min-width: 54px;
}
.pip-cheat-sep {
  border: none;
  border-top: 1px solid var(--glass-border);
  margin: 8px 0;
}
.pip-cheat-heading {
  font-size: 11px;
  font-weight: 700;
  color: var(--page-text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  margin: 0 0 6px 0;
}
.pip-analyse-toggle {
  background: transparent;
  border: none;
  padding: 4px 0;
  font-size: 12px;
  color: var(--page-text-muted);
  cursor: pointer;
  font-family: inherit;
  display: flex;
  align-items: center;
  gap: 4px;
}
.pip-analyse-toggle:hover {
  color: var(--page-text-secondary);
}
```

KEINE `color:#...`, `background:#...` oder `rgba(...)` in diesen Klassen — ausschließlich `var(--...)`.
  </action>
  <verify>
    <automated>grep -n "color:#\|background:#\|rgba(" C:\Users\andre\dev\salesnerve\static\nerve.css | grep -A0 "pip-cheat\|pip-analyse-toggle"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.pip-cheat-row" static/nerve.css` liefert mindestens 1 Treffer
    - `grep -n "\.pip-cheat-sep" static/nerve.css` liefert mindestens 1 Treffer
    - `grep -n "\.pip-cheat-heading" static/nerve.css` liefert mindestens 1 Treffer
    - `grep -n "\.pip-analyse-toggle" static/nerve.css` liefert mindestens 1 Treffer
    - `grep -n "color:#\|background:#\|rgba(" static/nerve.css | tail -20` zeigt KEINEN Treffer in den neu hinzugefügten Klassen (pip-cheat-* / pip-analyse-toggle)
    - Alle neuen Klassen stehen nach Zeile 2989 (EOF vor der Änderung)
  </acceptance_criteria>
  <done>nerve.css enthält 4 neue pip-Klassen ohne hardcodierte Farben. Jede Klasse ist per grep auffindbar.</done>
</task>

<task type="auto">
  <name>Task 2: pip-launcher.js — Cheat-Sheet render in _startCall() + data-analyse-toggle Event-Delegation (R-01, R-02, R-03, R-04, R-05)</name>
  <read_first>
    - static/pip-launcher.js Zeile 1918–1952 (aktueller _startCall()-Briefing-Block — REPLACE-Ziel ist 1928–1932)
    - static/pip-launcher.js Zeile 1820–1850 (Event-Delegation Block — ADD nach data-briefing-toggle Handler)
    - static/pip-launcher.js Zeile 1954–1967 (_expandBriefingTab / _collapseBriefingTab Pattern)
  </read_first>
  <files>static/pip-launcher.js</files>
  <action>
**Änderung 1 — REPLACE lines 1928–1932 (contentEl.innerHTML) in _startCall():**

Ersetze den aktuellen Block:
```javascript
        var contentEl = pipEl('pip-briefing-tab-content');
        if (contentEl) {
          var briefingHtml = mdToHtml(state.precallBriefing.text || '');
          contentEl.innerHTML = briefingHtml;
        }
```

durch folgenden Block (exakt diesen Code einfügen):
```javascript
        // Phase 08.19.5.6.3: Cheat-Sheet render (Eckdaten + Empfehlungen + kollabierter Fließtext)
        var contentEl = pipEl('pip-briefing-tab-content');
        if (contentEl) {
          var cheatHtml = '';
          // ── Sektion 1: Eckdaten-Block (R-01) ──
          var briefingFields = state.precallBriefing.fields || {};
          var fieldDefs = [
            { key: 'geschaeftsfuehrer', label: 'GF' },
            { key: 'branche',           label: 'Branche' },
            { key: 'mitarbeiterzahl',   label: 'MA' },
            { key: 'hauptprodukt',      label: 'Produkt' }
          ];
          var eckdatenRows = '';
          for (var fi = 0; fi < fieldDefs.length; fi++) {
            var fd = fieldDefs[fi];
            var f = briefingFields[fd.key];
            if (f && f.confidence !== 'not_found' && f.value) {
              eckdatenRows += '<div class="pip-cheat-row"><span>' + fd.label + ':</span><span>' + f.value + '</span></div>';
            }
          }
          if (eckdatenRows) {
            cheatHtml += eckdatenRows;
          }
          // ── Sektion 2: Empfehlungen-Block (R-02) ──
          var empf = state.precallBriefing.empfehlungen || '';
          if (empf && empf.trim()) {
            cheatHtml += '<hr class="pip-cheat-sep">';
            cheatHtml += '<div class="pip-cheat-heading">Empfehlungen</div>';
            cheatHtml += mdToHtml(empf);
          }
          // ── Sektion 3: Fließtext kollabiert (R-03) ──
          var analyseText = state.precallBriefing.text || '';
          cheatHtml += '<hr class="pip-cheat-sep">';
          cheatHtml += '<button class="pip-analyse-toggle" data-analyse-toggle="1"><span id="pip-analyse-icon">▶</span> Analyse aufklappen</button>';
          cheatHtml += '<div id="pip-analyse-body" style="max-height:0;overflow:hidden;transition:max-height 200ms ease;">';
          cheatHtml += mdToHtml(analyseText);
          cheatHtml += '</div>';
          contentEl.innerHTML = cheatHtml;
        }
```

**Änderung 2 — ADD data-analyse-toggle Handler im Event-Delegation Block:**

Direkt NACH dem bestehenden `data-briefing-toggle`-Handler-Block (nach der `return;`-Zeile, ca. Zeile 1846), füge folgenden Block ein:

```javascript
      // Phase 08.19.5.6.3: Analyse-Fließtext toggle
      if (ev.target.closest('[data-analyse-toggle]')) {
        var analyseBody = pipEl('pip-analyse-body');
        var analyseIcon = pipEl('pip-analyse-icon');
        if (analyseBody) {
          var analyseExpanded = analyseBody.style.maxHeight !== '0' && analyseBody.style.maxHeight !== '';
          if (analyseExpanded) {
            analyseBody.style.maxHeight = '0';
            if (analyseIcon) { analyseIcon.textContent = '▶'; }
          } else {
            analyseBody.style.maxHeight = '999px';
            if (analyseIcon) { analyseIcon.textContent = '▼'; }
          }
        }
        ev.stopPropagation();
        return;
      }
```

WICHTIG (R-04): Kein `color:#...`, kein `background:#...`, kein `rgba(...)` in neu hinzugefügten Zeilen.
WICHTIG (R-05): Keine Python-Dateien anfassen.

Reihenfolge der Sektionen: Eckdaten → Empfehlungen → Toggle (analog CONTEXT.md Discretion).
Toggle initial collapsed: `max-height: 0` (R-03 Acceptance: Fließtext im DOM aber nicht sichtbar nach _startCall()).
Leer-Check für empfehlungen: `empf && empf.trim()` (kein `.length` — SPEC Constraints: String-Typ).
  </action>
  <verify>
    <automated>grep -n "data-analyse-toggle\|pip-analyse-body\|pip-analyse-icon\|pip-cheat-row\|not_found\|pip-cheat-heading\|Empfehlungen\|Analyse aufklappen" C:\Users\andre\dev\salesnerve\static\pip-launcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "data-analyse-toggle" static/pip-launcher.js` liefert mindestens 2 Treffer (einmal im HTML-String, einmal im Event-Handler)
    - `grep -n "pip-analyse-body" static/pip-launcher.js` liefert mindestens 2 Treffer (im HTML-String + im Event-Handler via pipEl)
    - `grep -n "not_found" static/pip-launcher.js` liefert mindestens 1 Treffer im neuen Block (`confidence !== 'not_found'`)
    - `grep -n "empf.trim\|empfehlungen.trim" static/pip-launcher.js` liefert mindestens 1 Treffer (Leer-Check per SPEC Constraints)
    - `grep -n "pip-cheat-heading" static/pip-launcher.js` liefert mindestens 1 Treffer ("Empfehlungen"-Überschrift)
    - `grep -n "Analyse aufklappen" static/pip-launcher.js` liefert mindestens 1 Treffer
    - `grep -n "color:#\|background:#\|rgba(" static/pip-launcher.js | tail -20` zeigt KEINE neuen Treffer in den hinzugefügten Zeilen dieses Render-Blocks
    - `git diff --name-only` zeigt KEINE Python-Dateien (.py) als geändert
    - Bestehender `mdToHtml(state.precallBriefing.text || '')` Einzeiler in _startCall() ist NICHT mehr vorhanden (ersetzt durch neuen Block)
    - `pip-analyse-body` im neuen HTML-String hat `style="max-height:0;overflow:hidden;transition:max-height 200ms ease;"` (initial collapsed)
  </acceptance_criteria>
  <done>pip-launcher.js: _startCall() rendert Cheat-Sheet mit 3 Sektionen; Event-Delegation fängt data-analyse-toggle ab und togglet maxHeight zwischen '0' und '999px'. Kein Python-File geändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Frontend (precallBriefing) | `fields[*].value` und `empfehlungen` kommen vom Backend — werden via mdToHtml() und direkt in innerHTML eingesetzt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.5.6.3-01 | Injection (XSS) | contentEl.innerHTML mit fields[].value | mitigate | `fields[*].value` kommt vom eigenen Backend (precall_service.py, Claude-generiert). `empfehlungen` und `text` werden via bestehende `mdToHtml()` sanitized. `fields[*].value`-Strings sind kurze Fakten-Felder (GF-Name, Branche) — kein HTML-Markup erwartet. Akzeptables Risiko für internal-tool-Kontext; Escaping optional in späterer Phase. |
| T-08.19.5.6.3-02 | Tampering | pip-analyse-body maxHeight via JS | accept | Nur PiP-eigenes JS kann den Wert ändern. Kein externer Einfluss auf maxHeight-Toggle im PiP-Window-Kontext. |
</threat_model>

<verification>
Nach Ausführung beider Tasks:

1. `grep -n "pip-cheat-row\|pip-cheat-sep\|pip-cheat-heading\|pip-analyse-toggle" static/nerve.css` — alle 4 Klassen vorhanden
2. `grep -n "data-analyse-toggle" static/pip-launcher.js` — 2+ Treffer (render + event handler)
3. `grep -n "color:#\|background:#\|rgba(" static/pip-launcher.js | grep -v "^Binary"` — keine neuen Treffer im Cheat-Sheet-Block
4. `git diff --name-only` — keine .py-Dateien
5. `grep -n "mdToHtml(state.precallBriefing.text" static/pip-launcher.js` — sollte 0 Treffer liefern (alter Einzeiler entfernt)
6. `grep -n "confidence.*not_found\|not_found.*confidence" static/pip-launcher.js` — mindestens 1 Treffer
</verification>

<success_criteria>
- pip-briefing-tab-content zeigt GF/Branche/MA/Produkt-Kurz-Labels wenn confidence !== 'not_found'
- Felder mit confidence === 'not_found' erscheinen nicht (kein Leer-Label)
- Empfehlungen-Sektion sichtbar wenn empfehlungen.trim() nicht leer; fehlt wenn leer
- Fließtext initial maxHeight: 0 (nicht sichtbar); Toggle-Button macht ihn sichtbar/wieder unsichtbar
- Keine hardcodierten Farben in neuen Zeilen
- Git diff zeigt 2 geänderte Dateien: nerve.css + pip-launcher.js (keine .py-Dateien)
</success_criteria>

<output>
Nach Completion erstelle `.planning/phases/08.19.5.6.3-pip-briefing-tab-cheat-sheet-format/08.19.5.6.3-01-SUMMARY.md`
</output>
```

## Source Audit

| Source | Item | Plan Coverage |
|--------|------|---------------|
| GOAL | 3-Sektions-Cheat-Sheet im PiP-Briefing-Tab | Task 2 (COVERED) |
| R-01 | Eckdaten-Block: 4 Pflichtfelder mit confidence-Guard | Task 2 (COVERED) |
| R-02 | Empfehlungen-Block via mdToHtml(), konditionell | Task 2 (COVERED) |
| R-03 | Fließtext kollabiert initial, Toggle aufklappbar | Task 2 (COVERED) |
| R-04 | nerve.css-Tokens, keine hardcodierten Farben | Task 1 (nerve.css), Task 2 guard (COVERED) |
| R-05 | Kein Backend-Eingriff | Task 2 acceptance_criteria (git diff check) (COVERED) |
| D-01 | Event-Delegation, data-analyse-toggle, pip-analyse-body, maxHeight '0'/'999px' | Task 2 (COVERED) |
| D-02 | Neue Klassen: pip-cheat-row, pip-cheat-sep, pip-cheat-heading, pip-analyse-toggle | Task 1 (COVERED) |
| D-03 | base.html unangetastet | Not planned (out-of-scope: JS-only approach confirmed by context) |
</output>
