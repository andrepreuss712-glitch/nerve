---
phase: 08.8-stabilisierung-block-i-dead-code-prune
plan: "02"
type: execute
wave: 2
depends_on: ["08.8-01"]
files_modified:
  - routes/coach.py
  - routes/training.py
  - services/live_session.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "/coach/live_tipp Route existiert nicht mehr"
    - "/coach/api/tipps Route existiert nicht mehr"
    - "coach_tipps und coach_tipps_lock in live_session.py entfernt"
    - "/api/training/personalities/save Route existiert nicht mehr"
    - "pytest: 268 passing, 0 failures nach jedem Commit"
  artifacts:
    - path: routes/coach.py
      provides: "Coach-Blueprint ohne dead Live-Tipp-Routes"
    - path: routes/training.py
      provides: "Training-Blueprint ohne dead Personality-Save-Route"
    - path: services/live_session.py
      provides: "Live-Session ohne coach_tipps Queue (Memory-Leak beseitigt)"
  key_links:
    - from: routes/coach.py
      to: live_session.coach_tipps
      via: "Beide entfernt — keine Abhaengigkeit mehr"
---

<objective>
H-27: /coach/live_tipp + /coach/api/tipps entfernen — 0 Frontend-Caller, Memory-Leak in ls.coach_tipps-Queue.
H-28: /api/training/personalities/save entfernen — Frontend-Caller wurde in Phase 07.2 Wave 3 entfernt, Backend-Route blieb stehen.

Purpose: ~80-100 Zeilen Dead-Code aus routes/coach.py und routes/training.py entfernen. Memory-Leak (unbounded coach_tipps List) beseitigen.
Output: Saubere Blueprint-Dateien ohne Zombie-Routes.
</objective>

<execution_context>
@/c/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: H-27 — Coach Live-Tipp-Routes + coach_tipps entfernen</name>
  <files>routes/coach.py, services/live_session.py</files>
  <read_first>
    - routes/coach.py komplett lesen (Datei ist ueberschaubar)
    - services/live_session.py Z.50-60 (coach_tipps_lock + coach_tipps Definitionen)
    - Ausfuehren: grep -rn "coach_tipps\|live_tipp\|coach/api/tipps\|coach/live_tipp" routes/ services/ templates/ static/ tests/ (ausser __pycache__) — alle Treffer dokumentieren vor Loeschung
    - Ausfuehren: grep -rn "live_tipp\|coach/api/tipps" templates/ static/ --include="*.html" --include="*.js" — Frontend-Caller-Check (muss 0 ergeben)
  </read_first>
  <action>
VORBEDINGUNG: grep -rn "live_tipp\|coach/api/tipps" templates/ static/ --include="*.html" --include="*.js"
Falls Treffer: STOPPEN, melden.
Falls 0 Treffer: Weitermachen.

SCHRITT 1 — routes/coach.py:
Loeschen der Routen-Funktionen:
- Route @coach_bp.route('/live_tipp', methods=['POST']) + def live_tipp(): (ca. Z.199-248) — die gesamte Funktion inkl. Decorator
- Route @coach_bp.route('/api/tipps') + def api_tipps(): (ca. Z.240-260) — die gesamte Funktion inkl. Decorator

Vor dem Loeschen genau lesen wo die Funktionen anfangen und enden. Sicherstellen dass keine folgenden Funktionen abgeschnitten werden.

SCHRITT 2 — services/live_session.py:
Loeschen:
- Z.53: "coach_tipps_lock = threading.Lock()"
- Z.54: "coach_tipps      = []"

Auch reset()-Aufruf oder set_active_profile()-Block pruefen ob coach_tipps dort zurueckgesetzt wird:
- grep "coach_tipps" services/live_session.py — alle verbleibenden Treffer loeschen

SCHRITT 3 — Import-Cleanup in routes/coach.py:
Nach Loeschen der Funktionen pruefen ob live_session-Imports (coach_tipps_lock, coach_tipps) noch gebraucht werden. Nicht mehr benoetigte Imports entfernen.

VORSICHT: Die anderen Coach-Routen (Coach-Dashboard, Firma, Methodik, my_profiles) behalten!
Nur live_tipp und api_tipps loeschen.

Commit: feat(08.8-02): H-27 delete coach live_tipp routes + coach_tipps queue — memory leak removed
  </action>
  <behavior>
    - grep routes/coach.py for "live_tipp" -> 0 Treffer
    - grep routes/coach.py for "api/tipps" -> 0 Treffer (nur die dead route, nicht andere /tipps-artige)
    - grep services/live_session.py for "coach_tipps" -> 0 Treffer
    - pytest 268 passing
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "live_tipp\|coach_tipps" routes/coach.py services/live_session.py && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>Routes live_tipp + api_tipps geloescht. coach_tipps/coach_tipps_lock aus live_session.py entfernt. pytest 268 passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: H-28 — Personality-Save-Route entfernen</name>
  <files>routes/training.py</files>
  <read_first>
    - routes/training.py Z.980-1010 (Personality-Save-Route lesen)
    - Ausfuehren: grep -rn "personalities/save\|saveGeneratedPersonality\|save-personality" routes/ services/ templates/ static/ tests/ (ausser __pycache__) — alle Treffer
    - templates/training.html: grep "personalities/save\|saveGeneratedPersonality" — muss 0 aktive Caller zeigen (nur auskommentierte Kommentare erlaubt)
    - static/app.js: grep "personalities/save" — muss 0 Treffer zeigen
    - static/pip-launcher.js: grep "personalities/save" — muss 0 Treffer zeigen
  </read_first>
  <action>
VORBEDINGUNG: grep -rn "personalities/save" templates/ static/ --include="*.html" --include="*.js"
Falls aktive (nicht-kommentierte) Treffer: STOPPEN, melden.
Falls 0 aktive Treffer: Weitermachen.

Hinweis aus training.html Z.1633: "// ENTFERNT in 07.2 Wave 3: saveGeneratedPersonality() war nur aus dem Post-Call-..." — das ist nur ein Kommentar, kein aktiver Caller.

LOESCHEN in routes/training.py:
- @training_bp.route('/api/training/personalities/save', methods=['POST']) Decorator
- def api_training_save_personality(): (oder wie die Funktion heisst — lesen) + den kompletten Funktionskoerper

Vor dem Loeschen: Funktion komplett lesen (Anfang + Ende bestimmen), sicherstellen dass die naechste Route nicht angeschnitten wird.

Nach dem Loeschen: grep "personalities/save" routes/training.py → 0 Treffer.

Commit: feat(08.8-02): H-28 delete training personalities/save route — frontend caller removed in 07.2
  </action>
  <behavior>
    - grep routes/training.py for "personalities/save" -> 0 Treffer
    - pytest 268 passing
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "personalities/save" routes/training.py && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>Route /api/training/personalities/save aus routes/training.py entfernt. pytest 268 passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route removal | Entfernen nicht-aufgerufener Routes — kein Security-Impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.8-02-01 | Denial of Service | coach_tipps Memory-Leak | mitigate | Durch Loeschen der unbounded List behoben |
| T-08.8-02-02 | Tampering | Route-Loeschung | accept | Grep-Vorbedingung verifiziert 0 Frontend-Caller |
</threat_model>

<verification>
1. grep -rn "live_tipp\|coach/api/tipps" routes/ services/ templates/ static/ tests/ --include="*.py" --include="*.html" --include="*.js" → 0 Treffer
2. grep -rn "coach_tipps" services/live_session.py routes/ --include="*.py" → 0 Treffer
3. grep -rn "personalities/save" routes/ templates/ static/ --include="*.py" --include="*.html" --include="*.js" → 0 Treffer
4. pytest --ignore=tests/test_ft_seed.py → 268 passed
</verification>

<success_criteria>
3 dead Routes + 1 Memory-Leak entfernt. 2 atomare Commits auf main. pytest 268 passing.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-02-SUMMARY.md` erstellen.
</output>
