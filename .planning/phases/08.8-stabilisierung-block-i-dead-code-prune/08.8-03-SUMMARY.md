---
phase: 08.8-stabilisierung-block-i-dead-code-prune
plan: "03"
subsystem: app_routes, feedback, training, learning
tags: [dead-code-prune, refactor, orphan-routes]
dependency_graph:
  requires: [08.8-02]
  provides: [app-routes-ohne-swap-roles-status-skripte, feedback-ohne-quick, training-ohne-ping, learning-ohne-training-postcall]
  affects:
    - routes/app_routes.py
    - routes/feedback.py
    - routes/training.py
    - routes/learning.py
tech_stack:
  added: []
  patterns: [dead-code-removal, caller-verification-before-deletion]
key_files:
  modified:
    - routes/app_routes.py
    - routes/feedback.py
    - routes/training.py
    - routes/learning.py
decisions:
  - "swap_roles geloescht — 0 Frontend-Caller in templates/ + static/ verifiziert"
  - "api_status geloescht — 0 Frontend-Caller, unterschiedlich von /api/ergebnis (live Polling-Endpoint, behalten)"
  - "api_skripte geloescht — Duplikat zu /api/launcher/*, 0 Frontend-Caller in templates/ + static/"
  - "api_feedback_quick geloescht — 0 Frontend-Caller verifiziert"
  - "training_ping geloescht — Deploy-Verifikations-Endpoint, 0 Frontend-Caller"
  - "api_training_postcall_analysis (M-NEW-7) geloescht — /api/postcall_analysis (live) bleibt erhalten"
metrics:
  duration: "~5 Minuten"
  completed: "2026-04-25"
  tasks_completed: 2
  files_modified: 4
---

# Phase 08.8 Plan 03: Orphan-Routes Prune Summary

**One-liner:** 6 verwaiste Routes aus 4 Blueprint-Dateien entfernt (~118 Zeilen) — alle per 0-Frontend-Caller-Grep vor Loeschung verifiziert, pytest 268 passing nach jedem Commit.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Orphan-Routes in app_routes.py loeschen (swap_roles + status + skripte) | 8985073 | routes/app_routes.py |
| 2a | Orphan-Route api_feedback_quick aus feedback.py | 99d8000 | routes/feedback.py |
| 2b | Orphan-Route training_ping aus training.py | 43d8839 | routes/training.py |
| 2c | Orphan-Route api_training_postcall_analysis aus learning.py | 7100cad | routes/learning.py |

## What Was Done

### Task 1 — app_routes.py (8985073)

**Vorbedingung verifiziert:** 0 Frontend-Caller fuer alle drei Routes in templates/ + static/.

- `@app_routes_bp.route('/api/swap_roles', methods=['POST'])` + `def api_swap_roles()` entfernt (8 Zeilen)
- `@app_routes_bp.route('/api/status')` + `def api_status()` entfernt (5 Zeilen)
- `@app_routes_bp.route('/api/skripte')` + `def api_skripte()` + Sektion-Header entfernt (26 Zeilen inkl. Kommentar)

**Behalten:** `/api/ergebnis` (live Polling), `/api/keepalive` (app.html + base.html Caller), `/api/launcher/*` (PiP).

### Task 2a — feedback.py (99d8000)

**Vorbedingung verifiziert:** 0 Frontend-Caller fuer `feedback/quick`.

- `@feedback_bp.route('/api/feedback/quick', methods=['POST'])` + `def api_feedback_quick()` entfernt (25 Zeilen)

### Task 2b — training.py (43d8839)

**Vorbedingung verifiziert:** 0 Frontend-Caller fuer `training/ping`.

- `@training_bp.route('/training/ping')` + `def training_ping()` entfernt (4 Zeilen)

### Task 2c — learning.py (7100cad)

**Vorbedingung verifiziert:** 0 Frontend-Caller fuer `training/postcall-analysis`.

- `@learning_bp.route('/api/training/postcall-analysis', methods=['POST'])` + `def api_training_postcall_analysis()` entfernt (45 Zeilen)
- `/api/postcall_analysis` (Zeile 8, live Route) bleibt unveraendert erhalten — hat aktive Caller in app.js

## Behalten-Liste (mit Begruendung)

| Route | Datei | Begruendung |
|-------|-------|-------------|
| `/api/keepalive` | routes/app_routes.py | Caller in templates/app.html + templates/base.html (fetch) |
| `/coach/api/my_profiles` | routes/coach.py | Caller in templates/coach_firma.html |
| `/api/learning_cards/<id>/regenerate` + `/status` | routes/learning.py / routes/app_routes.py | Caller in static/app.js + templates/dashboard.html |
| `/api/training/phrases` | routes/training.py | Caller in templates/training.html |
| `/api/postcall_analysis` | routes/learning.py | Caller in static/app.js (live postcall overlay) |

## Verification Results

| Check | Result |
|-------|--------|
| grep swap_roles\|api_status\|api_skripte\|feedback/quick\|training/ping\|training/postcall-analysis in routes/ | 0 Treffer |
| grep gleiche Strings in templates/ + static/ | 0 Treffer |
| /api/keepalive in routes/app_routes.py | vorhanden (Zeile 669) |
| /coach/api/my_profiles in routes/coach.py | vorhanden (Zeile 201) |
| /api/postcall_analysis in routes/learning.py | vorhanden (Zeile 10) |
| pytest --ignore=test_ft_seed.py nach jedem Commit | 268 passed |

## Deviations from Plan

Keine — Plan wurde exakt ausgefuehrt. Alle 6 Routes hatten 0 Frontend-Caller wie in der Pre-Verifikation (08.8-01 MASTER-AUDIT) erwartet.

## Known Stubs

None — reine Dead-Code-Entfernung.

## Threat Flags

None — Route-Loeschung erzeugt keine neuen Angriffsflächen. Threat-Register-Mitigation T-08.8-03-01 eingehalten: jede Route einzeln per Grep auf 0-Caller verifiziert vor Loeschung.

## Self-Check: PASSED

- routes/app_routes.py vorhanden: ja
- routes/feedback.py vorhanden: ja
- routes/training.py vorhanden: ja
- routes/learning.py vorhanden: ja
- Commit 8985073 vorhanden: ja
- Commit 99d8000 vorhanden: ja
- Commit 43d8839 vorhanden: ja
- Commit 7100cad vorhanden: ja
- grep swap_roles routes/app_routes.py: 0 Treffer
- grep feedback/quick routes/feedback.py: 0 Treffer
- grep training/ping routes/training.py: 0 Treffer
- grep training/postcall-analysis routes/learning.py: 0 Treffer
- pytest 268 passed: ja
