---
phase: 08.8-stabilisierung-block-i-dead-code-prune
plan: "04"
type: execute
wave: 4
depends_on: ["08.8-03"]
files_modified:
  - templates/login.html
  - templates/admin/feedback_notification.html
  - routes/app_routes.py
  - services/live_session.py
  - static/app.js
autonomous: false
requirements: []
must_haves:
  truths:
    - "templates/login.html ist geloescht (kein render_template-Caller gefunden)"
    - "templates/admin/feedback_notification.html ist geloescht (kein render_template-Caller)"
    - "_tooltip.html ist BEHALTEN (wird in profile_editor.html gejinja-importiert)"
    - "ewb_top2-Writer ist verifiziert (rank_ewb entfernt in Phase 04.8 D-08) — Reader in app.js und app_routes.py entfernt wenn Writer nachweislich 0"
    - "opener vs openerItems: Legacy-Kompatibilitaet in pip-launcher.js behalten, Legacy-opener in app.js Classic-Pfad entfernt (Entscheidung dokumentiert)"
    - "pytest: 268 passing, 0 failures nach jedem Commit"
  artifacts:
    - path: templates/
      provides: "templates/login.html geloescht"
    - path: templates/admin/
      provides: "templates/admin/feedback_notification.html geloescht"
    - path: static/app.js
      provides: "ewb_top2 Classic-Render-Branch entfernt (wenn Writer = 0)"
    - path: routes/app_routes.py
      provides: "ewb_top2 aus /api/ergebnis Response entfernt (wenn Writer = 0)"
  key_links:
    - from: templates/profile_editor.html
      to: templates/_tooltip.html
      via: "jinja2 import — _tooltip.html BEHALTEN"
---

<objective>
Orphan-Templates loeschen: login.html (kein render_template-Caller), admin/feedback_notification.html (kein render_template-Caller). _tooltip.html wird in profile_editor.html importiert — BEHALTEN.

ewb_top2-Cleanup (F-8/H-36): rank_ewb() wurde in Phase 04.8 D-08 entfernt (kein Writer mehr). Reader in app.js:798-799 (Classic-Pfad) und Response-Key in app_routes.py:145 entfernen.

Legacy-opener vs. openerItems: pip-launcher.js hat bereits beide Pfade (openerItems + legacyOpener Fallback). Die app.js Classic-opener-Referenzen koennen entfernt werden (Classic-View wird in Block F deprecated). Entscheidung dokumentieren.

Purpose: 2 Template-Dateien loeschen, ~20 Zeilen Dead-Code in app.js + app_routes.py entfernen.
Output: Saubere Templates-Struktur, bereinigter app.js ewb_top2-Branch.
</objective>

<execution_context>
@/c/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orphan-Templates loeschen (login.html + feedback_notification.html)</name>
  <files>templates/login.html, templates/admin/feedback_notification.html</files>
  <read_first>
    - Ausfuehren: grep -rn "render_template.*login\.html\|render_template.*login'" routes/ services/ app.py (ausser __pycache__) — muss 0 Treffer zeigen
    - Ausfuehren: grep -rn "render_template.*feedback_notification\|feedback_notification\.html" routes/ services/ app.py (ausser __pycache__) — muss 0 Treffer zeigen
    - Ausfuehren: grep -rn "extends.*login\|include.*login\|import.*login" templates/ (ausser __pycache__) — auf Jinja-Includes pruefen
    - Ausfuehren: grep -rn "_tooltip\.html" templates/ (ausser __pycache__) — bestaetigen dass _tooltip.html in profile_editor.html importiert wird (dann BEHALTEN)
    - templates/login.html: kurz lesen um zu verstehen was drin ist (evtl. wurde es umbenannt/ersetzt)
    - templates/admin/feedback_notification.html: kurz lesen
  </read_first>
  <action>
VORBEDINGUNG A — login.html:
grep -rn "render_template.*login" routes/ services/ app.py --include="*.py" → muss 0 aktive Treffer zeigen.
Hinweis: Die App nutzt landing.html + register.html als Login-UI (Login-Modal in landing). login.html ist Legacy aus frueherer Phase.
Falls 0 Treffer: templates/login.html loeschen (git rm templates/login.html).

VORBEDINGUNG B — feedback_notification.html:
grep -rn "feedback_notification" routes/ services/ app.py --include="*.py" → muss 0 Treffer zeigen.
Falls 0 Treffer: templates/admin/feedback_notification.html loeschen (git rm templates/admin/feedback_notification.html).

VORBEDINGUNG C — _tooltip.html BEHALTEN:
grep zeigt {% import '_tooltip.html' as tooltip %} in profile_editor.html → _tooltip.html ist LIVE, NICHT loeschen.

Nach Loeschung: 
- ls templates/ → login.html nicht mehr vorhanden
- ls templates/admin/ → feedback_notification.html nicht mehr vorhanden

Commits (zwei atomare):
1. feat(08.8-04): delete orphan template login.html (replaced by landing+register)
2. feat(08.8-04): delete orphan template admin/feedback_notification.html (no render_template caller)
  </action>
  <behavior>
    - ls templates/login.html → File Not Found
    - ls templates/admin/feedback_notification.html → File Not Found
    - templates/_tooltip.html noch vorhanden
    - pytest 268 passing
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && [ ! -f templates/login.html ] && echo "login.html deleted OK" && [ ! -f templates/admin/feedback_notification.html ] && echo "feedback_notification.html deleted OK" && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>Beide Orphan-Templates geloescht. _tooltip.html behalten. pytest 268 passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1 hat login.html und admin/feedback_notification.html geloescht.

Jetzt: ewb_top2-Cleanup-Entscheidung vor der Implementierung.

Befund:
- rank_ewb() wurde in Phase 04.8 D-08 entfernt → kein Writer fuer ewb_top2 mehr (live_session.py:336 setzt es auf None, niemand schreibt einen echten Wert)
- Reader in app.js:798-799: "if(data.ewb_top2 && Array.isArray(data.ewb_top2)...)" — Classic-View-Pfad, rendert ewb_top2
- Reader in app_routes.py:145: "'ewb_top2': ls.state.get('ewb_top2')" in /api/ergebnis Response
- Kommentar in app_routes.py:145: "# legacy (may be None post-04.8)" — bestätigt dass ewb_top2 immer None ist
- Block F (Classic-Deprecation) ist als späterer Block geplant
  </what-built>
  <how-to-verify>
Frage an den Entwickler:

ewb_top2 ist immer None (Writer geloescht in Phase 04.8). Reader in app.js prueft "if(data.ewb_top2)" → Branch tritt nie ein. Soll ich:

Option A: ewb_top2 sofort entfernen (app.js:798-799 + app_routes.py:145) — bereinigt jetzt, vor Block F
Option B: ewb_top2 erst in Block F (Classic-Deprecation) entfernen — alles auf einmal wenn Classic komplett raus geht

Empfehlung: Option A. Der Reader ist Dead-Branch (prueft immer-None-Wert), kein Risiko.

Ebenso: opener vs. openerItems in app.js — Classic-Path in app.js nutzt noch "opener" (Z.2011). pip-launcher.js hat bereits den openerItems+legacyOpener-Dual-Pfad. Soll der Classic-opener-Branch in app.js jetzt entfernt werden (vor Block F)?

Bitte antworten: "A" oder "B" fuer ewb_top2, und "classic-opener-jetzt" oder "classic-opener-block-f" fuer opener.
  </how-to-verify>
  <resume-signal>Antwort mit "A" oder "B" fuer ewb_top2, und Entscheidung zu opener. Oder: "alles in Block F" wenn beide Punkte dort gebuendelt werden sollen.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ewb_top2 Reader entfernen (wenn Option A gewaehlt)</name>
  <files>static/app.js, routes/app_routes.py</files>
  <read_first>
    - static/app.js Z.790-810 (ewb_top2-Reader-Block lesen)
    - routes/app_routes.py Z.140-155 (ewb_top2 in /api/ergebnis Response)
    - services/live_session.py Z.108-112 (ewb_top2 State-Definition lesen)
    - Ausfuehren: grep -rn "ewb_top2\|ewbTop2" routes/ services/ static/ templates/ tests/ (ausser __pycache__) — alle Treffer vor Loeschung
  </read_first>
  <action>
NUR AUSFUEHREN wenn Checkpoint-Antwort "A" (sofort loeschen):

SCHRITT 1 — static/app.js Z.798-799 (ewb_top2-Reader):
Loeschen des if-Blocks:
  if(data.ewb_top2 && Array.isArray(data.ewb_top2) && data.ewb_top2.length >= 2){
    renderPipEwbButtons(data.ewb_top2);
  }
(Exakte Zeilen im File pruefen, koennen nach vorherigen Edits verschoben sein)

SCHRITT 2 — routes/app_routes.py Z.145 (ewb_top2 in /api/ergebnis):
Loeschen der Zeile: 'ewb_top2': ls.state.get('ewb_top2'),  # legacy (may be None post-04.8)

SCHRITT 3 — services/live_session.py:
State-Feld 'ewb_top2': None in reset()-Block kann bleiben ODER entfernt werden — es schadet nicht (leerer State-Key), aber fuers Aufraumen: entfernen wenn keine anderen Leser mehr.
Nur entfernen nach: grep -rn "ewb_top2" services/live_session.py → alle Treffer pruefen.

Falls Option B gewaehlt: Dieser Task wird UEBERSPRUNGEN. In Summary vermerken "ewb_top2-Cleanup verschoben auf Block F (Classic-Deprecation)".

Fuer opener-Legacy: Wenn "classic-opener-jetzt": app.js Z.2008-2013 (opener Classic-Block) loeschen. Wenn "classic-opener-block-f": uebersprungen.

Commit: feat(08.8-04): F-8/H-36 remove ewb_top2 dead reader from app.js + api/ergebnis response
  </action>
  <behavior>
    - grep static/app.js for "ewb_top2" -> 0 Treffer (oder nur kommentierte)
    - grep routes/app_routes.py for "ewb_top2" -> 0 Treffer
    - pytest 268 passing
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "ewb_top2" static/app.js routes/app_routes.py && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>ewb_top2 Reader aus app.js + app_routes.py entfernt. Entscheidung dokumentiert. pytest 268 passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template deletion | Loeschen nicht-gerenderter HTML-Dateien — kein Security-Impact |
| JS branch removal | ewb_top2 Branch prueft immer-None-Wert — kein Funktionsverlust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.8-04-01 | Tampering | Template-Loeschung | accept | render_template-Grep bestaetigt 0 Caller vor Loeschung |
| T-08.8-04-02 | Tampering | ewb_top2 Reader-Entfernung | accept | Writer ist seit Phase 04.8 D-08 entfernt — Wert ist immer None, Branch tritt nie ein |
</threat_model>

<verification>
1. templates/login.html existiert nicht mehr
2. templates/admin/feedback_notification.html existiert nicht mehr
3. templates/_tooltip.html existiert noch (behalten)
4. ewb_top2 (wenn Option A): grep routes/ static/ --include="*.py" --include="*.js" for "ewb_top2" → 0 Treffer (ausser evtl. live_session.py State-Reset der harm-neutral ist)
5. opener-Entscheidung in Summary dokumentiert
6. pytest --ignore=tests/test_ft_seed.py → 268 passed
</verification>

<success_criteria>
2 Orphan-Templates geloescht. ewb_top2-Entscheidung getroffen und umgesetzt (oder documented fuer Block F). opener-Entscheidung dokumentiert. pytest 268 passing.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-04-SUMMARY.md` erstellen. Checkpoint-Entscheidungen dokumentieren.
</output>
