---
phase: 08.8-stabilisierung-block-i-dead-code-prune
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/claude_service.py
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "analysiere_mit_claude_streaming (Z.704-805) existiert nicht mehr in claude_service.py"
    - "_build_system_prompt (Z.265-380) existiert nicht mehr in claude_service.py"
    - "_get_erfolgsquoten (Z.206-264) existiert nicht mehr in claude_service.py"
    - "analyse_loop prueft nicht mehr auf active_sid (if/else-Duplikat entfernt)"
    - "CONCERNS.md nennt ANALYSE_INTERVALL korrekt als 4s (nicht 2s)"
    - "pytest: 268 passing, 0 failures nach Commit"
  artifacts:
    - path: services/claude_service.py
      provides: "Bereinigter Service ohne dead streaming function und legacy prompt builders"
    - path: .planning/codebase/CONCERNS.md
      provides: "Korrekte ANALYSE_INTERVALL-Dokumentation (4s)"
  key_links:
    - from: deepgram_service.py
      to: claude_service.streame_auto_variante
      via: "import + call (live, behalten)"
    - from: analyse_loop
      to: analysiere_mit_claude
      via: "direkter Call (kein Duplikat-Branch)"
---

<objective>
H-3: analysiere_mit_claude_streaming (102 Zeilen, Z.704-805) loeschen — dead, 0 Caller.
H-4: _build_system_prompt (116 Zeilen, Z.265-380) + _get_erfolgsquoten (58 Zeilen, Z.206-264) loeschen — dead/zombie, replaced by resolve_prompt_version + build_ewb_prompt.
H-11: if/else-Branch in analyse_loop entfernen (Z.1072-1079) — beide Zweige rufen identisch analysiere_mit_claude() auf, active_sid-Check ist sinnloses Duplikat. CONCERNS.md ANALYSE_INTERVALL von "2s" auf "4s" korrigieren.

Purpose: ~275 Zeilen Dead-Code aus dem Hot-Path-Service entfernen. Tests-Blockade (Phase 08.7) bereits aufgehoben.
Output: services/claude_service.py mit ~275 Zeilen weniger. CONCERNS.md korrekt.
</objective>

<execution_context>
@/c/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/audits/MASTER-AUDIT-v2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vorbedingung — H-3/H-4 Test-Protection-Check</name>
  <files>tests/test_claude_service_phase08.py</files>
  <read_first>
    - tests/test_claude_service_phase08.py (lesen, pruefen ob inspect.getsource-Tests weg sind)
    - Ausfuehren: grep -n "analysiere_mit_claude_streaming\|_build_system_prompt\|inspect.getsource" tests/test_claude_service_phase08.py
    - Ausfuehren: grep -rn "analysiere_mit_claude_streaming" tests/ routes/ templates/ static/ (ausser __pycache__)
    - Ausfuehren: grep -rn "_build_system_prompt\|_get_erfolgsquoten" tests/ routes/ templates/ static/ (ausser __pycache__)
  </read_first>
  <action>
REINE VERIFIKATION — keine Aenderungen.

Pruefen ob Phase 08.7 die Test-Schutzwall entfernt hat:

1. grep -n "analysiere_mit_claude_streaming" tests/ -r — muss 0 Treffer in .py-Dateien ergeben (nur __pycache__ darf matchen)
2. grep -n "_build_system_prompt" tests/ -r — muss 0 Treffer in .py-Dateien ergeben
3. grep -n "inspect.getsource" tests/test_claude_service_phase08.py — muss 0 Treffer ergeben

Falls IRGENDEIN Treffer in .py-Dateien: STOPPEN, melden. Nicht mit Task 2 fortfahren.

Falls alle 3 Checks sauber (0 .py-Treffer): Freigabe fuer Task 2.
  </action>
  <verify>
    <automated>grep -rn "analysiere_mit_claude_streaming" /c/Users/andre/dev/salesnerve/tests/ --include="*.py" | wc -l</automated>
  </verify>
  <done>Alle 3 Grepp-Checks liefern 0 Treffer in .py-Dateien. Freigabe erteilt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: H-3 loeschen (analysiere_mit_claude_streaming, Z.704-805)</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py Z.704-830 (die ganze Funktion + Abschluss-Kommentar lesen)
    - services/claude_service.py Z.1-20 (Imports — pruefen ob re, re as _re nur in dieser Funktion importiert wird)
    - Ausfuehren: grep -n "analysiere_mit_claude_streaming" services/ routes/ templates/ static/ tests/ -r (ausser __pycache__) — muss danach 0 Treffer zeigen
    - Ausfuehren: grep -n "from extensions import socketio as sio" services/claude_service.py — pruefen ob dieser Import nur in der zu loeschenden Funktion steht oder auch woanders
  </read_first>
  <action>
Losche die Funktion analysiere_mit_claude_streaming (Z.704-805) vollstaendig aus services/claude_service.py.

Exakte Grenzen:
- Beginn: Zeile 704 ("def analysiere_mit_claude_streaming(neuer_text: str, kontext: str, sid: str, slot_id: int, on_einwand_detected=None) -> dict:")
- Ende: Zeile 805 ("return {}") + Leerzeile
- Die Funktion endet bei der leeren Zeile nach dem "return {}" im except-Block (vor "def streame_auto_variante")

VORSICHT — streame_auto_variante (ab Z.808) ist LIVE und darf NICHT geloescht werden.
streame_auto_variante wird aufgerufen in deepgram_service.py — behalten.

Nach dem Loeschen:
- grep "analysiere_mit_claude_streaming" services/claude_service.py — muss 0 Treffer zeigen
- Kommentar in claude_service.py Z.820 "Laeuft parallel zu analysiere_mit_claude_streaming" anpassen oder entfernen

Commit: feat(08.8-01): H-3 delete analysiere_mit_claude_streaming — 102 dead lines removed
  </action>
  <behavior>
    - Nach Loeschung: grep services/claude_service.py for "analysiere_mit_claude_streaming" -> 0 Treffer
    - pytest 268 passing nach Commit
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "analysiere_mit_claude_streaming" services/claude_service.py && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>analysiere_mit_claude_streaming nicht mehr in claude_service.py. pytest 268 passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: H-4 + H-11 loeschen (_build_system_prompt, _get_erfolgsquoten, if/else-Branch)</name>
  <files>services/claude_service.py, .planning/codebase/CONCERNS.md</files>
  <read_first>
    - services/claude_service.py Z.200-400 (beide zu loeschenden Funktionen lesen)
    - services/claude_service.py Z.1070-1085 (if/else-Branch in analyse_loop lesen)
    - services/claude_service.py Z.1-15 (Kommentar zu _build_system_prompt am Dateiende pruefen)
    - Ausfuehren: grep -n "_build_system_prompt\|_get_erfolgsquoten" services/claude_service.py — alle Treffer identifizieren
    - Ausfuehren: grep -n "_build_system_prompt\|_get_erfolgsquoten" services/ routes/ templates/ static/ tests/ -r (ausser __pycache__) — muss danach 0 Treffer in anderen Dateien zeigen
    - .planning/codebase/CONCERNS.md Z.175-200 (ANALYSE_INTERVALL Abschnitt)
  </read_first>
  <action>
TEIL A — _get_erfolgsquoten loeschen (Z.206-264):
Komplette Funktion _get_erfolgsquoten entfernen.
Exakte Grenzen: Beginn Z.206 ("def _get_erfolgsquoten() -> str:"), Ende: letzte Zeile der Funktion vor der naechsten def.
Aufruf-Stelle: Z.383 "lerndaten = _get_erfolgsquoten()" — diese Zeile und Z.384-385 (if lerndaten: lines.append(lerndaten)) ebenfalls loeschen.

TEIL B — _build_system_prompt loeschen (Z.265-380):
Komplette Funktion _build_system_prompt entfernen.
ACHTUNG: _get_erfolgsquoten wurde IN _build_system_prompt aufgerufen (Z.383). Nach Loeschen von TEIL A ist dieser Call bereits weg. _build_system_prompt selbst hat keine Caller mehr (verifiziert per Grep in Task 1).
Auch den Datei-Kopf-Kommentar Z.10 anpassen: "# bleiben bewusst auf _ACTIVE_PROMPT_CACHE + _build_system_prompt (Legacy)." — Klammerzusatz "(Legacy)" entfernen oder Zeile anpassen da _build_system_prompt jetzt weg ist.

TEIL C — H-11 if/else-Branch vereinfachen (analyse_loop Z.1072-1079):
Aktueller Code (vereinfacht):
  if active_sid:
      ergebnis = analysiere_mit_claude(neuer_text, kontext)
  else:
      ergebnis = analysiere_mit_claude(neuer_text, kontext)

Beide Zweige sind identisch. Das if/else loeschen, ersetzen durch:
  ergebnis = analysiere_mit_claude(neuer_text, kontext)
Den active_sid-Lese-Block (Z.1070-1071 "with ls.state_lock: active_sid = ls.state.get('active_sid')") ebenfalls entfernen wenn active_sid danach nicht mehr gebraucht wird.
Kommentar Z.1073-1076 ueber Phase 06.3 stehen lassen (erklaert warum kein PiP-Slot-Render).

TEIL D — CONCERNS.md korrigieren:
In .planning/codebase/CONCERNS.md: "ANALYSE_INTERVALL (2 seconds per config.py)" → "ANALYSE_INTERVALL (4 seconds per config.py — Phase 06.3: raised from 2s to reduce 529-Risk)"
Betroffene Zeile(n): ~Z.181, ~Z.191 (beide aktualisieren)

Commits (zwei atomare Commits):
1. feat(08.8-01): H-4 delete _build_system_prompt + _get_erfolgsquoten — 195 dead lines removed
2. docs(08.8-01): H-11 collapse if/else duplikat in analyse_loop + fix CONCERNS.md ANALYSE_INTERVALL 4s
  </action>
  <behavior>
    - grep services/claude_service.py for "_build_system_prompt" -> 0 Treffer
    - grep services/claude_service.py for "_get_erfolgsquoten" -> 0 Treffer  
    - analyse_loop hat kein if active_sid / else Duplikat mehr
    - CONCERNS.md nennt 4s
    - pytest 268 passing nach jedem Commit
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "_build_system_prompt\|_get_erfolgsquoten" services/claude_service.py && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>Beide Funktionen geloescht, if/else-Duplikat entfernt, CONCERNS.md korrigiert. pytest 268 passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File edits | Dead code removal — kein Security-Impact, nur Correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.8-01-01 | Tampering | claude_service.py edit | accept | Grep-Vorbedingung verifiziert 0 Caller vor Loeschung. Git Rollback moeglich. |
</threat_model>

<verification>
1. grep -rn "analysiere_mit_claude_streaming" services/ routes/ templates/ static/ tests/ --include="*.py" → 0 Treffer
2. grep -rn "_build_system_prompt" services/ routes/ templates/ static/ tests/ --include="*.py" → 0 Treffer
3. grep -rn "_get_erfolgsquoten" services/ routes/ templates/ static/ tests/ --include="*.py" → 0 Treffer
4. analyse_loop (Z.1070ff) enthaelt kein doppeltes if/else mehr
5. CONCERNS.md nennt ANALYSE_INTERVALL = 4s
6. pytest --ignore=tests/test_ft_seed.py → 268 passed
</verification>

<success_criteria>
~275 Zeilen toter Code geloescht. claude_service.py schlank und korrekt. pytest 268 passing. 3 atomare Commits auf main.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-01-SUMMARY.md` erstellen.
</output>
