---
phase: 08.8-stabilisierung-block-i-dead-code-prune
plan: "05"
type: execute
wave: 5
depends_on: ["08.8-04"]
files_modified:
  - services/prompt_pipeline.py
  - services/qa_pipeline.py
  - services/training_service.py
  - tests/test_08_5_05_training_pipeline_t1.py
  - tests/test_08_5_05_training_pipeline_t2.py
  - tests/test_prompt_pipeline.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "services/finetune_logging.py existiert NICHT (war bereits nicht vorhanden — H-1 bestaetigt)"
    - "log_pipeline_event in prompt_pipeline.py entfernt ODER zu harmlosem Stub vereinfacht"
    - "log_pipeline_event Aufrufe in qa_pipeline.py und training_service.py entfernt"
    - "Alle Tests die log_pipeline_event aufrufen/testen angepasst (nicht geloescht wenn sie echte Integration testen)"
    - "Keine FtPipelineEvent-Tabelle in models.py vorhanden — keine DB-Migration noetig (Tabelle existiert nicht)"
    - "pytest: 268 passing, 0 failures nach jedem Commit"
    - "git push origin main ausgefuehrt nach Abschluss"
  artifacts:
    - path: services/prompt_pipeline.py
      provides: "log_pipeline_event entfernt oder zu No-Op stub vereinfacht"
    - path: services/qa_pipeline.py
      provides: "log_pipeline_event Aufrufe entfernt"
    - path: services/training_service.py
      provides: "log_pipeline_event Aufrufe entfernt"
  key_links:
    - from: services/prompt_pipeline.log_pipeline_event
      to: services/finetune_logging.log_ft_event
      via: "try-import (graceful skip wenn nicht vorhanden) — ganzer Pfad entfernen"
---

<objective>
H-1: finetune_logging.py Entfernung abschliessen.

Situation (verifiziert waehrend Planung):
- services/finetune_logging.py existiert BEREITS NICHT (H-1 bestaetigt: das File wurde nie erstellt)
- FtPipelineEvent Tabelle existiert NICHT in database/models.py → keine DB-Migration noetig
- log_pipeline_event() in prompt_pipeline.py ist ein Wrapper der versucht finetune_logging zu importieren (try/except ImportError → silent skip). Weil das File nie existierte, laeuft er immer in den except-Pfad → de facto No-Op
- log_pipeline_event wird aufgerufen in: qa_pipeline.py (2 Stellen), training_service.py (4 Stellen), + Tests pruefen ob es importierbar ist

Aufgabe: log_pipeline_event-Wrapper + alle Aufrufe entfernen da sie dauerhaft No-Ops sind (das eigentliche logging-Backend existiert nie). Tests anpassen die log_pipeline_event importieren/pruefen.

Sicherheitsnetz: Kein DB-Migration-Script noetig (Tabelle existiert nicht). Kein Down-Migration-Script noetig.

Purpose: ~60 Zeilen No-Op-Code entfernen + Tests bereinigen.
Output: Saubere Pipeline-Services ohne toten Logging-Wrapper.
</objective>

<execution_context>
@/c/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vorbedingung und Bestandsaufnahme finetune_logging</name>
  <files>database/models.py, services/prompt_pipeline.py</files>
  <read_first>
    - Ausfuehren: ls services/finetune_logging.py → erwartet "No such file" (bestaetigt H-1)
    - Ausfuehren: grep -n "FtPipelineEvent\|ft_pipeline_events" database/models.py → muss 0 Treffer zeigen
    - Ausfuehren: grep -rn "FtPipelineEvent\|ft_pipeline" database/ services/ routes/ tests/ (ausser __pycache__) → alle Treffer dokumentieren
    - services/prompt_pipeline.py Z.225-260 lesen (log_pipeline_event vollstaendig lesen)
    - services/qa_pipeline.py: grep -n "log_pipeline_event" services/qa_pipeline.py — alle Aufruf-Zeilen
    - services/training_service.py: grep -n "log_pipeline_event" services/training_service.py — alle Aufruf-Zeilen
    - tests/test_08_5_05_training_pipeline_t1.py Z.135-150 lesen
    - tests/test_08_5_05_training_pipeline_t2.py Z.50-110 lesen
    - tests/test_prompt_pipeline.py Z.188-210 lesen
  </read_first>
  <action>
REINE VERIFIKATION — keine Aenderungen.

Checkliste:
1. ls services/finetune_logging.py → "No such file" → BESTAETIGT: kein echtes logging-Backend
2. grep -n "FtPipelineEvent" database/models.py → 0 Treffer → BESTAETIGT: keine DB-Migration noetig
3. Alle log_pipeline_event-Aufrufstellen in qa_pipeline.py + training_service.py notieren (Zeilennummern)
4. Alle Test-Stellen notieren die log_pipeline_event importieren oder testen

Falls FtPipelineEvent in models.py existiert: STOPPEN, melden. DB-Migration-Plan erstellen bevor weiter.
Falls finetune_logging.py existiert: STOPPEN, melden. Aufrufstellen und Caller analysieren.

Falls alles bestaetigt: Freigabe fuer Task 2.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && [ ! -f services/finetune_logging.py ] && echo "CONFIRMED: finetune_logging.py does not exist" && grep -c "FtPipelineEvent" database/models.py || echo "0"</automated>
  </verify>
  <done>Bestandsaufnahme abgeschlossen. Keine DB-Migration noetig. Aufrufstellen dokumentiert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: log_pipeline_event aus prompt_pipeline.py entfernen</name>
  <files>services/prompt_pipeline.py</files>
  <read_first>
    - services/prompt_pipeline.py Z.1-15 (Datei-Kopf + Kommentar zu log_pipeline_event)
    - services/prompt_pipeline.py Z.225-260 (log_pipeline_event Implementierung vollstaendig)
    - Ausfuehren: grep -rn "from services.prompt_pipeline import.*log_pipeline_event\|from services import prompt_pipeline" services/ routes/ tests/ (ausser __pycache__) — alle Import-Stellen
  </read_first>
  <action>
SCHRITT 1 — log_pipeline_event Funktion loeschen:
In services/prompt_pipeline.py: die gesamte Funktion log_pipeline_event (Z.228-254 circa) entfernen:
- Kommentar-Block "# -- Modul-agnostisches Logging: log_pipeline_event" (Z.226-227)
- def log_pipeline_event(event_type: str, module: str, data: dict) -> None:
- den gesamten Funktionskoerper (try-import + call + except-Blöcke)

SCHRITT 2 — Kommentar im Datei-Kopf anpassen:
services/prompt_pipeline.py Z.9: "- log_pipeline_event(event_type, module, data) → modul-agnostisches FT-Logging" — diese Zeile aus dem Modul-Docstring/Kommentar entfernen

SCHRITT 3 — Verifikation:
grep "log_pipeline_event" services/prompt_pipeline.py → 0 Treffer

SCHRITT 4 — services/qa_pipeline.py Import-Fix:
services/qa_pipeline.py Z.41: "build_profile_context, resolve_prompt_version, log_pipeline_event" — log_pipeline_event aus dem Import entfernen:
  from services.prompt_pipeline import (
      build_profile_context, resolve_prompt_version
  )
(log_pipeline_event streichen)

Dann alle Aufrufstellen in qa_pipeline.py loeschen:
- Z.331-338 (log_pipeline_event classifier call + except)
- Z.449-458 (log_pipeline_event qa_response call + except)
Beide try/except-Bloecke vollstaendig loeschen.

Commit: feat(08.8-05): H-1 remove log_pipeline_event from prompt_pipeline + qa_pipeline — no-op since finetune_logging.py never existed
  </action>
  <behavior>
    - grep services/prompt_pipeline.py for "log_pipeline_event" -> 0 Treffer
    - grep services/qa_pipeline.py for "log_pipeline_event" -> 0 Treffer
    - pytest 268 passing
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "log_pipeline_event" services/prompt_pipeline.py services/qa_pipeline.py && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>log_pipeline_event aus prompt_pipeline.py und qa_pipeline.py entfernt. pytest 268 passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: log_pipeline_event aus training_service.py + Tests entfernen</name>
  <files>services/training_service.py, tests/test_08_5_05_training_pipeline_t1.py, tests/test_08_5_05_training_pipeline_t2.py, tests/test_prompt_pipeline.py</files>
  <read_first>
    - services/training_service.py Z.1-10 (Import-Block)
    - grep -n "log_pipeline_event" services/training_service.py — alle 4 Aufruf-Stellen
    - tests/test_08_5_05_training_pipeline_t1.py Z.135-150 lesen
    - tests/test_08_5_05_training_pipeline_t2.py Z.50-110 lesen (log_pipeline_event mock + Assertions)
    - tests/test_prompt_pipeline.py Z.188-220 lesen (log_pipeline_event swallow-errors Test)
  </read_first>
  <action>
SCHRITT 1 — services/training_service.py:
Import loeschen: services/training_service.py Z.8: "from services.prompt_pipeline import resolve_prompt_version, log_pipeline_event"
→ anpassen auf: "from services.prompt_pipeline import resolve_prompt_version"

Alle 4 Aufruf-Stellen loeschen:
- Z.829-834 (log_pipeline_event training call + except print)
- Z.901-907 (log_pipeline_event mood call + except print)
- Z.1225-1230 (log_pipeline_event scoring call + except print)
- (ggf. weitere, Grep-Ergebnis aus Task 1 nutzen)
Jeweils den gesamten try/except-Block fuer log_pipeline_event entfernen.

SCHRITT 2 — tests/test_08_5_05_training_pipeline_t1.py:
Test 8 (Z.137-147): "resolve_prompt_version and log_pipeline_event are importable"
Dieser Test prueft via inspect.getsource ob der Import-String in training_service.py vorkommt — das ist ein Source-Presence-False-Green per CLAUDE.md Regel.
Loeschen: def test_08_5_t1_imports_pipeline... (gesamter Test)
Auch Z.147: "assert callable(ts.log_pipeline_event)" loeschen.

SCHRITT 3 — tests/test_08_5_05_training_pipeline_t2.py:
Tests die log_pipeline_event mocken (Z.57 + Z.90):
monkeypatch.setattr(ts, 'log_pipeline_event', ...) wird failing wenn log_pipeline_event nicht mehr in ts existiert.
Optionen: 
a) Diese Mock-Zeile + zugehoerige Assertion entfernen wenn der Test dadurch noch sinnvoll bleibt (er testet generate_response / generate_response_with_mood, nicht nur logging)
b) monkeypatch-Zeile kommentieren + Assertion loeschen

Strategie: Die Kern-Assertion (dass generate_response ein Ergebnis liefert) behalten, nur logging-bezogene Assertions entfernen.

SCHRITT 4 — tests/test_prompt_pipeline.py:
Z.192+ "log_pipeline_event: MUST swallow errors" Test lesen.
Falls dieser Test TYLKO log_pipeline_event testet: loeschen.
Falls er andere Pipeline-Aspekte prueft: nur log_pipeline_event-Teil entfernen.

SCHRITT 5 — Finaler pytest-Lauf:
python -m pytest tests/ -q --ignore=tests/test_ft_seed.py → muss 268 passed zeigen

SCHRITT 6 — git push:
Nach gruenem pytest: git push origin main (Ende der gesamten Phase 08.8)

Commits (zwei atomare):
1. feat(08.8-05): H-1 remove log_pipeline_event from training_service.py
2. test(08.8-05): H-1 remove source-presence tests for log_pipeline_event
  </action>
  <behavior>
    - grep services/training_service.py for "log_pipeline_event" -> 0 Treffer
    - grep tests/ for "log_pipeline_event" (*.py) -> 0 Treffer (ausser archivierte Tests)
    - pytest 268 passing
    - git push origin main ausgefuehrt
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "log_pipeline_event" services/training_service.py && grep -rn "log_pipeline_event" tests/ --include="*.py" | grep -v archive | wc -l && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | tail -3</automated>
  </verify>
  <done>log_pipeline_event vollstaendig aus codebase entfernt. Tests bereinigt. pytest 268 passing. git push origin main ausgefuehrt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No-Op code removal | log_pipeline_event ist seit Beginn ein No-Op (Backend nie implementiert) — kein Datenverlust |
| No DB migration | FtPipelineEvent-Tabelle existiert nicht → keine Migration noetig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.8-05-01 | Information Disclosure | FT-Logging Entfernung | accept | Logging war nie aktiv (finetune_logging.py existiert nicht). Keine echten Daten verloren. |
| T-08.8-05-02 | Tampering | Training-Service-Edit | mitigate | Nur log_pipeline_event try/except Bloecke entfernt. generate_response + generate_response_with_mood + generate_scoring Kernlogik unberuehrt. |
</threat_model>

<verification>
1. ls services/finetune_logging.py → "No such file" (war schon so)
2. grep -rn "log_pipeline_event" services/ routes/ templates/ static/ tests/ --include="*.py" --include="*.js" --include="*.html" → 0 Treffer (ausser tests/archive/)
3. grep -rn "FtPipelineEvent" database/ services/ → 0 Treffer
4. python -m pytest tests/ -q --ignore=tests/test_ft_seed.py → 268 passed
5. git log --oneline -5 → zeigt alle Phase 08.8 Commits
6. git push origin main → ausgefuehrt
</verification>

<success_criteria>
H-1 vollstaendig abgeschlossen. ~60 Zeilen No-Op-Code entfernt. Tests bereinigt. 268 passing. git push auf main. Phase 08.8 (Block I) komplett.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-05-SUMMARY.md` erstellen.

Phase-08.8-Abschluss-Notiz:
- Block I vollstaendig: H-1, H-3, H-4, H-11, H-27, H-28, 6 Orphan-Routes, 2 Orphan-Templates, F-8/H-36 (wenn Option A gewaehlt)
- Geschaetzter Zeilenabbau: 500-800 Zeilen (H-3: 102, H-4: 195, H-11: ~10, H-27: ~50, H-28: ~30, Orphan-Routes: ~120, Templates: ~80, log_pipeline_event: ~60)
- Naechster Block: Block B (Auth-Haertung) oder Block C (Schema-Drift) — per MASTER-AUDIT-v2 Priorisierung
</output>
