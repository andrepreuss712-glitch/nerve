---
phase: 08.8-stabilisierung-block-i-dead-code-prune
plan: "03"
type: execute
wave: 3
depends_on: ["08.8-02"]
files_modified:
  - routes/app_routes.py
  - routes/feedback.py
  - routes/training.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "/api/swap_roles existiert nicht mehr"
    - "/api/status existiert nicht mehr (app_routes)"
    - "/api/skripte existiert nicht mehr (Duplikat zu /api/launcher/*)"
    - "/api/feedback/quick existiert nicht mehr"
    - "/api/training/postcall-analysis existiert nicht mehr"
    - "/training/ping existiert nicht mehr"
    - "/api/keepalive ist BEHALTEN (hat echte Caller in app.html + base.html)"
    - "/coach/api/my_profiles ist BEHALTEN (hat Caller in coach_firma.html)"
    - "/api/learning_cards/<id>/regenerate + /status sind BEHALTEN (lernen.py hat Caller in dashboard.html und app.js)"
    - "/api/phrases (training/phrases) ist BEHALTEN (Caller in training.html)"
    - "pytest: 268 passing, 0 failures nach jedem Commit"
  artifacts:
    - path: routes/app_routes.py
      provides: "Bereinigt: swap_roles + status + skripte entfernt"
    - path: routes/feedback.py
      provides: "Bereinigt: feedback/quick entfernt"
    - path: routes/training.py
      provides: "Bereinigt: postcall-analysis + ping entfernt"
  key_links:
    - from: templates/app.html + templates/base.html
      to: routes/app_routes.api_keepalive
      via: "fetch('/api/keepalive') — LIVE, behalten"
    - from: templates/coach_firma.html
      to: routes/coach.api_my_profiles
      via: "fetch('/coach/api/my_profiles') — LIVE, behalten"
---

<objective>
Orphan-Routes prunen: 6 von 9 aus dem MASTER-AUDIT — nur die wo Grep 0 Frontend-Caller bestaetigt.
BEHALTEN (weil echte Caller gefunden): /api/keepalive (app.html + base.html), /coach/api/my_profiles (coach_firma.html), /api/learning_cards/* (app.js + dashboard.html), /api/phrases (training.html).

Zu loeschen (verifiziert 0 Caller):
- /api/swap_roles (routes/app_routes.py)
- /api/status (routes/app_routes.py)
- /api/skripte (routes/app_routes.py — Duplikat zu /api/launcher/*)
- /api/feedback/quick (routes/feedback.py)
- /api/training/postcall-analysis (routes/learning.py — M-NEW-7)
- /training/ping (routes/training.py)

Purpose: ~120 Zeilen Dead-Routes entfernen.
Output: Schlankere Route-Blueprints.
</objective>

<execution_context>
@/c/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orphan-Routes in app_routes.py loeschen (swap_roles + status + skripte)</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py: lesen um Zeilenbereiche fuer alle 3 Routen zu identifizieren
      - grep -n "def api_swap_roles\|def api_status\|def api_skripte\|route.*swap_roles\|route.*api/status\|route.*api/skripte" routes/app_routes.py
    - Ausfuehren: grep -rn "swap_roles\|api/status\|api/skripte" templates/ static/ tests/ routes/ services/ (ausser __pycache__) — Frontend-Caller-Check
    - ACHTUNG /api/status: Pruefen ob /api/status ANDERS als /api/ergebnis ist. Nur /api/status in app_routes.py loeschen wenn 0 Caller.
    - Ausfuehren: grep -rn "fetch.*api/status\|url.*api/status\|href.*api/status" templates/ static/ --include="*.html" --include="*.js"
    - Ausfuehren: grep -rn "fetch.*api/skripte\|url.*api/skripte" templates/ static/ --include="*.html" --include="*.js"
    - Ausfuehren: grep -rn "fetch.*swap_roles\|url.*swap_roles" templates/ static/ --include="*.html" --include="*.js"
  </read_first>
  <action>
VORBEDINGUNG fuer jede Route — grep in templates/ static/ --include="*.html" --include="*.js":
- /api/swap_roles → 0 Caller erwartet → loeschen
- /api/status → 0 Caller erwartet → loeschen
- /api/skripte → 0 Caller erwartet (PiP nutzt /api/launcher/init) → loeschen

Falls fuer IRGENDEINE Route ein aktiver Frontend-Caller gefunden wird: Diese Route BEHALTEN, Ergebnis in Summary dokumentieren.

LOESCHEN (nach Einzelpruefung):
1. api_swap_roles-Route + Funktion (ca. Z.219-228): @app_routes_bp.route('/api/swap_roles', methods=['POST']) + def api_swap_roles(): + Koerper
2. api_status-Route + Funktion (ca. Z.229-XX): @app_routes_bp.route('/api/status') + def api_status(): + Koerper  
3. api_skripte-Route + Funktion (ca. Z.1358-XX): @app_routes_bp.route('/api/skripte') + def api_skripte(): + Koerper

Fuer jede: Anfang + Ende der Funktion lesen, dann loeschen.

WICHTIG: /api/ergebnis NICHT loeschen — das ist der live Polling-Endpoint.
WICHTIG: /api/launcher/init NICHT anfassen.

Commit: feat(08.8-03): prune orphan routes swap_roles + api_status + api_skripte from app_routes.py
  </action>
  <behavior>
    - grep routes/app_routes.py for "def api_swap_roles\|def api_status\|def api_skripte" -> 0 Treffer
    - grep routes/app_routes.py for "route.*swap_roles\|route.*api/status\|route.*api/skripte" -> 0 Treffer
    - pytest 268 passing
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "api_swap_roles\|api_status\|def api_skripte" routes/app_routes.py && python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>3 Orphan-Routes aus app_routes.py entfernt. pytest 268 passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orphan-Routes in feedback.py + training.py + learning.py loeschen</name>
  <files>routes/feedback.py, routes/training.py, routes/learning.py</files>
  <read_first>
    - routes/feedback.py: grep -n "def api_feedback_quick\|route.*feedback/quick" routes/feedback.py
    - routes/training.py: grep -n "def.*ping\|route.*training/ping" routes/training.py
    - routes/learning.py: grep -n "def api_training_postcall_analysis\|route.*training/postcall-analysis" routes/learning.py
    - Ausfuehren: grep -rn "feedback/quick" templates/ static/ tests/ routes/ services/ --include="*.html" --include="*.js" --include="*.py" (ausser __pycache__) — Frontend-Caller-Check
    - Ausfuehren: grep -rn "training/ping\|training/postcall-analysis" templates/ static/ tests/ routes/ services/ --include="*.html" --include="*.js" --include="*.py" (ausser __pycache__)
  </read_first>
  <action>
VORBEDINGUNG fuer jede Route:

A) /api/feedback/quick in routes/feedback.py:
grep -rn "feedback/quick" templates/ static/ --include="*.html" --include="*.js" → muss 0 Treffer zeigen
Falls 0 Treffer: Loeschen. @feedback_bp.route('/api/feedback/quick', methods=['POST']) + def api_feedback_quick(): + Koerper

B) /training/ping in routes/training.py:
grep -rn "training/ping\|/ping" templates/ static/ --include="*.html" --include="*.js" → muss 0 Treffer zeigen
Falls 0 Treffer: Loeschen. @training_bp.route('/training/ping') + def training_ping(): (oder wie auch immer die Funktion heisst) + Koerper

C) /api/training/postcall-analysis in routes/learning.py (M-NEW-7):
grep -rn "training/postcall-analysis" templates/ static/ --include="*.html" --include="*.js" → muss 0 Treffer zeigen
WICHTIG: /api/postcall_analysis (ohne /training/ prefix) in app.js:1121 ist LIVE → NICHT loeschen
Nur /api/training/postcall-analysis loeschen.
Falls 0 Treffer fuer training/postcall-analysis: Loeschen. @learning_bp.route('/api/training/postcall-analysis', ...) + def api_training_postcall_analysis(): + Koerper (Z.235-280 circa)

Falls fuer IRGENDEINE Route aktive Caller gefunden: Diese Route BEHALTEN, in Summary dokumentieren.

Nach Loeschung Import-Cleanup in jeweiliger Datei: pruefen ob Imports (generate_postcall_analysis etc.) noch von anderen Funktionen gebraucht werden.

Commits (drei atomare):
1. feat(08.8-03): prune orphan route api_feedback_quick from feedback.py
2. feat(08.8-03): prune orphan route training_ping from training.py
3. feat(08.8-03): prune orphan route api_training_postcall_analysis (M-NEW-7) from learning.py
  </action>
  <behavior>
    - grep routes/feedback.py for "feedback/quick" -> 0 Treffer
    - grep routes/training.py for "training/ping" -> 0 Treffer
    - grep routes/learning.py for "training/postcall-analysis" -> 0 Treffer
    - /api/postcall_analysis (live route) ist weiterhin in routes/learning.py vorhanden
    - pytest 268 passing nach jedem Commit
  </behavior>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "feedback/quick" routes/feedback.py; grep -c "training/ping" routes/training.py; grep -c "training/postcall-analysis" routes/learning.py; python -m pytest tests/ -q --ignore=tests/test_ft_seed.py 2>&1 | grep "268 passed"</automated>
  </verify>
  <done>3 weitere Orphan-Routes entfernt. pytest 268 passing nach jedem Commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route removal | Entfernen von Routes ohne Frontend-Caller — kein Security-Impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.8-03-01 | Tampering | Route-Loeschung | mitigate | Jede Route einzeln per Grep auf 0-Caller verifiziert vor Loeschung. Keepalive + my_profiles + learning_cards + phrases BEHALTEN. |
</threat_model>

<verification>
1. grep -rn "swap_roles\|api_status\|api_skripte\|feedback/quick\|training/ping\|training/postcall-analysis" routes/ --include="*.py" → 0 Treffer
2. /api/keepalive in routes/app_routes.py noch vorhanden (behalten)
3. /coach/api/my_profiles in routes/coach.py noch vorhanden (behalten)
4. /api/postcall_analysis (live) in routes/learning.py noch vorhanden (behalten)
5. /api/learning_cards/* routes noch vorhanden (behalten)
6. /api/phrases (/api/training/phrases) noch vorhanden (behalten)
7. pytest --ignore=tests/test_ft_seed.py → 268 passed nach jedem Commit
</verification>

<success_criteria>
6 Orphan-Routes entfernt (in 5 atomaren Commits). 3 Dateien bereinigt. pytest 268 passing. Behalten-Liste dokumentiert.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.8-stabilisierung-block-i-dead-code-prune/08.8-03-SUMMARY.md` erstellen. Behalten-Liste (mit Begruendung) dokumentieren.
</output>
