# Phase 08.23.2.AUTH-1: Eingangstür + Netz — Research

**Researched:** 2026-07-04
**Domain:** Flask Auth / CSRF / Role-sweep / Deploy-gate tests
**Confidence:** HIGH — all claims verified via local grep (git-synced code) or live Prod SSH

> **Cross-AI (Gemini) PFLICHT vor Execute** — Phase ist 🟡 Auth/Security.
> Nach Planung: `/gsd-review --phase 08.23.2.AUTH-1 --all` BEVOR `/gsd-execute-phase`.

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions
- D-01: CSRF-Fix Weg A1 — Meta-Token in landing.html + X-CSRFToken in 3 state-changing Fetches
- D-02: landing.html ist eigenständige HTML, kein base.html-Erbe
- D-03: 3 Fetches zu behandeln: /api/login, /api/register, /waitlist/join. GET /waitlist/stats bleibt tokenlos
- D-04: WTF_CSRF_TIME_LIMIT=None in app.py config (nach CSRFProtect(app):54)
- D-05: test_signup_journey.py — E2E mit CSRF AN, ERST ROT gegen HEAD
- D-06: test_csrf_fetch_guard.py — statischer Sweep templates/**+static/*.js
- D-07: Template-Existenz-Wächter (render_template-String → Datei muss existieren)
- D-08: Andrés Konto ist OAuth-only (kein passwort_hash), auth.py:158 blockiert Email-Login bereits
- D-09: Prod-Vorab-Verifikation (SUPERADMIN_EMAIL, passwort_hash leer, is_superadmin=True)
- D-10: ALLE session['rolle']-Leser auf g.user.rolle umstellen
- D-11: Vollständiger Sweep — Routes UND Templates
- D-12: Setzstelle auth.py:130 bleibt (session['rolle'] = user_rolle), nur Leser umstellen
- D-13: Zwei Löcher — waitlist.py:138-141 (admin) + :96-99 (invite) — schwacher Owner-Check
- D-14: Fix: @login_required + @superadmin_required auf BEIDE Waitlist-Routen
- D-15: test_waitlist_admin_auth.py — ERST ROT gegen HEAD; Owner→403, Superadmin→200, anon→Redirect
- D-16: Prod-Vorab identisch zu D-09

### Claude's Discretion
- Genaue Datei-Aufteilung der drei Wächter (D-05/06/07)
- Exakte Token-Extraktions-Mechanik im JS (App-Muster aus base.html-Templates übernehmen)
- CSRF-Guard-Whitelist-Repräsentation (Liste im Test vs. Marker-Kommentar)

### Deferred Ideas (OUT OF SCOPE)
- Onboarding-Connector → AUTH-2
- Pricing/Checkout/Billing → AUTH-3
- Founder-Maske/skip-Flags → AUTH-4
- Invite-Mail/max_users/Team-Verwaltung → AUTH-5
- Password-Reset/Email-Bestätigung/Hard-Delete/Export → DSGVO-Paket
</user_constraints>

---

## Section 1: Prod-Vorab-Verifikation (D-09 / D-16) — SELBST-AUSSPERR-RISIKO

**Status: VOLLSTÄNDIG VERIFIZIERT via SSH gegen Production (2026-07-04)**

SSH-Verbindung `ssh -i ~/.ssh/nerve_vps root@178.104.82.166` war erreichbar und alle Checks wurden live ausgeführt.

### Ergebnisse

**Check (a): SUPERADMIN_EMAIL auf Prod gesetzt?**
```
inspect.sh env-keys
```
Ausgabe enthält: `SUPERADMIN_EMAIL` — KEY EXISTIERT. [VERIFIED: SSH inspect.sh env-keys]

*Note: inspect.sh gibt NUR den Key-Namen zurück, niemals den Wert (Secret-Safety). Der Wert muss durch den Auth-Seed-Mechanismus (auth.py:137-139) beim nächsten Login von andrepreuss712@gmail.com bestätigt werden. Da is_superadmin=True bereits gesetzt ist (s.u.), ist der Seed bereits gelaufen — Beweis dass SUPERADMIN_EMAIL beim letzten Login der korrekten Adresse entsprochen hat.*

**Check (b): Andrés passwort_hash leer/NULL?**
```
inspect.sh sample users 5
```
Row für `andrepreuss712@gmail.com`:
- `passwort_hash`: **leer (leerer String, nicht NULL)** [VERIFIED: SSH inspect.sh sample users]
- `is_superadmin`: **t (TRUE)** [VERIFIED: SSH inspect.sh sample users]
- `oauth_provider`: `google` [VERIFIED: SSH inspect.sh sample users]
- `oauth_id`: `101574715284444269707` [VERIFIED: SSH inspect.sh sample users]

**Check (c): Kolonnenstruktur users-Tabelle:**
```
inspect.sh columns users
```
Relevante Spalten bestätigt: [VERIFIED: SSH inspect.sh columns users]
- `passwort_hash` (character varying, nullable=YES) — kann NULL oder leer sein
- `is_superadmin` (boolean, nullable=NO)
- `oauth_provider` (character varying, nullable=YES)

### PASS-Bedingungen — ALLE ERFÜLLT

| Check | Bedingung | Ergebnis |
|-------|-----------|---------|
| SUPERADMIN_EMAIL gesetzt | Key in env-keys vorhanden | PASS |
| passwort_hash leer | Leer/NULL → OAuth-only | PASS (leerer String) |
| is_superadmin=True | Seed bereits gelaufen | PASS |
| oauth_provider=google | Einziger Zugangspfad | PASS |

**Fazit: Kein Selbst-Aussperr-Risiko. Superadmin-Härtung und Waitlist-Fix können sicher deployed werden, ohne dass André den Admin-Zugang verliert.**

### Hinweis zur auth.py:158 Logik

Code-Verifikation [VERIFIED: grep routes/auth.py:158]:
```python
if not user or not user.passwort_hash:
    return None, 'E-Mail oder Passwort falsch.'
```
Ein leerer String `''` ist in Python falsy → der Email-Password-Login-Block ist für Andrés Account auch bei leerem String (statt NULL) aktiv. D-08-Fundament hält.

---

## Section 2: Rollen-Sweep grep-Beleg — session['rolle'] → g.user.rolle

### Vollständige Reader-Liste (Python-Routes)

**SETTER (bleibt — D-12):**
- `routes/auth.py:130`: `session['rolle'] = user_rolle` — SETTER, wird NICHT umgestellt

**READER via `_rolle()` Helper-Funktion (KRITISCH — CONTEXT D-11 unterschätzt Umfang):**

`routes/profiles.py:35-36`:
```python
def _rolle():
    return flask_session.get('rolle', 'member')
```

Diese Helper-Funktion wird von **18 Call-Sites** in profiles.py aufgerufen: [VERIFIED: grep routes/profiles.py]
- :63, :106, :186, :208, :248, :269, :295, :366, :387, :413, :448, :469, :495, :530, :551, :577, :662, :751

**Fix-Mechanik:** Den `_rolle()`-Helper auf `g.user.rolle` umstellen — alle 18 Call-Sites werden in einem Schlag gefixt ohne jede Zeile einzeln anzufassen:
```python
def _rolle():
    return g.user.rolle  # war: flask_session.get('rolle', 'member')
```

**READER direkt (ohne Helper):**

| Datei | Zeile | Kontext | Umstellung |
|-------|-------|---------|------------|
| `routes/organisations.py` | :16 | `_require_admin`-Decorator | `session.get('rolle')` → `g.user.rolle` |
| `routes/dashboard.py` | :526 | `flask_session.get('rolle', 'member')` | → `g.user.rolle` |
| `routes/logs_routes.py` | :14 | `flask_session.get('rolle', 'member')` in `liste()` | → `g.user.rolle` |
| `routes/logs_routes.py` | :36 | `flask_session.get('rolle', 'member')` in `download()` | → `g.user.rolle` |
| `routes/settings.py` | :63 | `flask_session.get('rolle')` | → `g.user.rolle` |
| `routes/settings.py` | :82 | `flask_session.get('rolle')` | → `g.user.rolle` |
| `routes/settings.py` | :105 | `flask_session.get('rolle')` (= update_privacy, zeile 122 in aktuell gelesen) | → `g.user.rolle` |
| `routes/settings.py` | :122 | `flask_session.get('rolle')` | → `g.user.rolle` |
| `routes/settings.py` | :191 | `flask_session.get('rolle') != 'owner'` | → `g.user.rolle` |
| `routes/changelog.py` | :84 | `flask_session.get('rolle') != 'owner'` | → `g.user.rolle` (Sibling-Loch, Deferred-Scope-Frage) |
| `routes/waitlist.py` | :98 | `flask_session.get('rolle') != 'owner'` | ERSETZT durch `@superadmin_required` (D-14) |
| `routes/waitlist.py` | :140 | `flask_session.get('rolle') != 'owner'` | ERSETZT durch `@superadmin_required` (D-14) |

**Anmerkung zu settings.py-Zeilennummern:** Grep bestätigt 5 Reader in settings.py bei :63, :82, :105, :122, :191. CONTEXT nannte :63/:82/:105/:122/:191 — alle stimmen überein. [VERIFIED: grep routes/settings.py]

### Changelog-Sibling-Loch (Deferred-Scope-Frage)

`routes/changelog.py:84` (CONTEXT sagte ~:87 — **DRIFTED, aktuell Zeile 84**):
```python
if flask_session.get('rolle') != 'owner':
    return jsonify({'error': 'Keine Berechtigung'}), 403
```
Diese Zeile MUSS mechanisch im Rollen-Sweep auf `g.user.rolle` umgestellt werden (Punkt 29, kein Halb-Zustand). [VERIFIED: grep routes/changelog.py:84]

**Mini-Entscheidung für André (Planer muss hochziehen):** Soll `POST /changelog/admin` zusätzlich `@superadmin_required` bekommen (wie die Waitlist-Routen), oder reicht `g.user.rolle != 'owner'`? André hat explizit NUR die zwei Waitlist-Löcher als Security-Risiko benannt. Der Changelog-Admin ist aktuell owner-only (jeder Firmen-Owner kann Changelog-Einträge schreiben) — ob das ein Geschäftsrisiko ist, muss André entscheiden. **NICHT auto-entscheiden.**

### g.user in before_request — Anker-Verifikation

`routes/auth.py:48-55` (Kommentar + Code): [VERIFIED: grep routes/auth.py]
```python
# Attach user to g — read all needed attributes BEFORE db.close()
...
g.user = user
g.org  = db.get(Organisation, user.org_id)
```
`g.user.rolle` ist via `database/models.py:72` definiert: [VERIFIED: grep database/models.py]
```python
rolle = Column(String(50), default='member', comment='Rolle: owner/admin/member')
```

### Template-Reader (Jinja) — alle hinter Login

| Datei | Zeilen | Login-geschützt? |
|-------|--------|-----------------|
| `templates/base.html` | :79 | Ja — nur eingebunden wenn @login_required |
| `templates/logs_page.html` | :25 | Ja — Route hat @login_required |
| `templates/profiles_list.html` | :30, :69, :81 | Ja — @login_required |
| `templates/settings.html` | :115, :236 | Ja — @login_required |

[VERIFIED: grep templates/*.html]

**None-Guard:** Alle betroffenen Templates werden nur hinter @login_required-Routen gerendert. Der login_required-Decorator setzt g.user immer (oder redirectet). `g.user` ist nie None in diesen Templates. Kein separater `{% if g.user %}` Guard nötig, aber der Planer sollte dies als Kommentar in den Plan aufnehmen.

**Flask-Jinja-Kontext:** `g` ist standardmäßig im Jinja2-Template-Kontext verfügbar (Flask injiziert `g` automatisch). `{{ g.user.rolle }}` und `{% if g.user.rolle in ('owner', 'admin') %}` funktionieren ohne weitere Konfiguration. [ASSUMED: Flask 3.x-Standard; in app.py kein custom context processor der dies blockiert]

---

## Section 3: deploy.sh pytest-Gate-Mechanik

### Verifizierte Gate-Zeilen

`deploy.sh:186-194` (CONTEXT sagte ~186-198 — stimmt überein): [VERIFIED: Read deploy.sh:180-210]

```bash
sudo -u nerve_app env ANON_PW="$ANON_PW" TEST_DB="$TEST_DB" bash -c '
  cd /opt/nerve/app && \
  DATABASE_URL="postgresql://nerve_app@/${TEST_DB}" \
  TEST_DATABASE_URL="postgresql://nerve_app@/${TEST_DB}" \
  NERVE_APP_TEST_DSN="postgresql://nerve_app@/${TEST_DB}" \
  NERVE_SCHILD_TEST_DSN="postgresql://nerve_app@/${TEST_DB}" \
  ANON_WORKER_TEST_DSN="postgresql://nerve_anon_worker:${ANON_PW}@127.0.0.1:5432/${TEST_DB}" \
  /opt/nerve/venv/bin/pytest tests/ --tb=short -q -m "not live and not perf"
' || { echo "[deploy] FEHLER: pytest gegen nerve_test ROT — kein Restart, kein Deploy"; exit 1; }
```

**Marker-Ausdruck:** `-m "not live and not perf"` — neue Tests ohne diese Marker werden automatisch mitgezogen. [VERIFIED: Read deploy.sh:193]

**DB-Target:** `nerve_test` (variable `$TEST_DB`), nicht Production-DB. Tests committen gegen `nerve_test`, nicht live. [VERIFIED: Read deploy.sh:188]

**Red-Blocks-Restart:** `|| { ... exit 1; }` — bei Test-Fehler kein Restart, alter Code bleibt live. [VERIFIED: Read deploy.sh:194]

**Auto-Collection:** `pytest tests/` sammelt alle `tests/test_*.py` ohne explizite Registrierung. Neue `test_signup_journey.py`, `test_csrf_fetch_guard.py`, `test_waitlist_admin_auth.py` laufen automatisch mit, solange sie NICHT mit `@pytest.mark.live` oder `@pytest.mark.perf` markiert sind.

### conftest.py cleanup_rows + _baseline_cleanup_guard

[VERIFIED: grep tests/conftest.py]:
- `cleanup_rows` ist in `tests/conftest.py:238` definiert — gemeinsamer Teardown-Helfer
- `_baseline_cleanup_guard` ist ein `autouse`-Fixture in `tests/conftest.py:549`
- Nutzungsmuster (aus test_admin_dashboard_auth.py als Referenz):
  ```python
  @pytest.fixture
  def cleanup_tracker(db_from_client):
      ids = {User: [], Organisation: []}
      yield ids
      cleanup_rows(db_from_client, ids)
  ```
- Tests die Rows committen MÜSSEN dieses Muster (oder äquivalent) verwenden; sonst färbt `_baseline_cleanup_guard` den Test rot
- Für `test_signup_journey.py` (E2E, committet User+Org): cleanup_tracker-Fixture mit User+Organisation-Keys ist Pflicht

---

## Section 4: Wächter-Vorlagen — Muster zum Kopieren

### 4.1 test_waitlist_admin_auth.py — Muster: test_admin_dashboard_auth.py

[VERIFIED: Read tests/test_admin_dashboard_auth.py]

**Strukturmuster:**
```python
from tests.conftest import cleanup_rows
from database.models import User, Organisation

@pytest.fixture
def cleanup_tracker(db_from_client):
    ids = {User: [], Organisation: []}
    yield ids
    cleanup_rows(db_from_client, ids)

def _make_org_and_user(session, email, is_superadmin=False, tracker=None):
    org = Organisation(name='TestCo', plan='starter', subscription_status='active')
    session.add(org)
    session.flush()
    u = User(email=email, passwort_hash=generate_password_hash('pw'),
             rolle='owner', org_id=org.id, is_superadmin=is_superadmin,
             aktiv=True, onboarding_done=True)
    session.add(u)
    session.commit()
    if tracker: tracker[Organisation].append(org.id); tracker[User].append(u.id)
    return u

def test_unauthenticated_redirects_to_login(client):
    r = client.get('/waitlist/admin', follow_redirects=False)
    assert r.status_code in (302, 401)

def test_normal_user_gets_403(client, db_from_client, cleanup_tracker):
    # Auch Owner einer fremden Org → 403 (D-15)
    u = _make_org_and_user(db_from_client, 'owner@example.com',
                           is_superadmin=False, tracker=cleanup_tracker)
    with client.session_transaction() as s:
        s['user_id'] = u.id
    r = client.get('/waitlist/admin')
    assert r.status_code == 403

def test_superadmin_gets_200(client, db_from_client, cleanup_tracker):
    u = _make_org_and_user(db_from_client, 'super@example.com',
                           is_superadmin=True, tracker=cleanup_tracker)
    with client.session_transaction() as s:
        s['user_id'] = u.id
    r = client.get('/waitlist/admin')
    assert r.status_code == 200
```

**Wichtig für D-15:** Test muss auch `POST /waitlist/invite/<wid>` abdecken (Owner→403, Superadmin→200/erfordert einen Waitlist-Eintrag in der DB). cleanup_tracker muss ggf. Waitlist-Rows mitberücksichtigen.

**ERST ROT gegen HEAD:** Beide Waitlist-Routen haben aktuell nur den schwachen `flask_session.get('rolle') != 'owner'`-Check ohne `@login_required`. Test mit `is_superadmin=False` muss bei aktuellem Code 200 liefern (weil Owner-Check passiert) und nach Fix 403. Der Test für `anon` muss aktuell 200-or-redirect liefern (kein @login_required!) und nach Fix 302.

### 4.2 test_csrf_fetch_guard.py — Muster: test_no_live_global_state.py

[VERIFIED: Read tests/test_no_live_global_state.py]

**Strukturprinzip (Verbotenes-Muster-Sweep):**
```python
import re
from pathlib import Path

_ROOT = Path(__file__).parent.parent

# Whitelist: bewusst exempted fetch()-Aufrufe
_CSRF_EXEMPT = frozenset({
    '/stripe/webhook',           # Stripe-Webhook (externe Signatur statt CSRF)
    'accounts.google.com',       # Google OAuth callback
    'login.microsoftonline.com', # Microsoft OAuth callback
    # GET-only: /waitlist/stats — kein Token nötig (GET, nicht state-changing)
})

def _collect_stateful_fetches_without_csrf(path):
    src = path.read_text(encoding='utf-8', errors='replace')
    violations = []
    # Muster: fetch( ... method:'POST'|'PUT'|'PATCH'|'DELETE' ... ohne X-CSRFToken
    # Präzise Regex-Logik ist Claude's Discretion — Whitelist als frozenset
    ...
    return violations

def test_all_stateful_fetches_have_csrf_token():
    scan_dirs = [_ROOT / 'templates', _ROOT / 'static']
    violations = []
    ...
    assert not violations, f"fetch() ohne X-CSRFToken:\n  " + "\n  ".join(violations)
```

**Weißlisten-Mechanik:** Analog `_WHITELIST` frozenset in test_no_live_global_state.py — nicht als Kommentar im Code, sondern als frozenset im Test-File. Der Planer entscheidet die genaue Regex-Logik.

**Nicht-Source-Presence-False-Green:** Sweep prüft VERBOTENES Muster (fetch ohne Token). Wenn ein Token hinzugefügt wird → Test grüner. Wenn neuer tokenloser Fetch hinzukommt → rot. Entspricht CLAUDE.md Grenzfall-Dokumentation.

### 4.3 Template-Existenz-Wächter — Muster: test_schild_guard.py (Struktur) + test_no_live_global_state.py (AST/grep-Sweep)

[VERIFIED: Read tests/test_schild_guard.py:1-60]

**Strukturprinzip:**
```python
import re
from pathlib import Path

_ROOT = Path(__file__).parent.parent

def test_render_template_files_exist():
    """Jeder render_template('...') String muss als Datei in templates/ existieren."""
    violations = []
    scan_dirs = [_ROOT / 'routes', _ROOT / 'app.py']
    for src_file in ...:
        src = src_file.read_text(encoding='utf-8', errors='replace')
        for m in re.finditer(r"render_template\(['\"]([^'\"]+)['\"]", src):
            tmpl = m.group(1)
            if not (_ROOT / 'templates' / tmpl).exists():
                violations.append(f"{src_file.relative_to(_ROOT)}  → '{tmpl}' fehlt")
    assert not violations, "\n  ".join(violations)
```

**Planer-Entscheidung:** Dieser Wächter kann als eigenständige Datei `tests/test_template_existence_guard.py` ODER als Funktion in `test_csrf_fetch_guard.py` integriert werden (Claude's Discretion D-52 / CONTEXT Discretion erster Punkt).

### 4.4 test_signup_journey.py — Muster für E2E mit CSRF

**CSRF-aktiv machen im Test:** conftest.py setzt `WTF_CSRF_ENABLED=False` für lokale Tests. Für diesen Test muss CSRF explizit AN sein — Muster aus conftest.py oder per:
```python
@pytest.fixture
def csrf_client(app):
    app.config['WTF_CSRF_ENABLED'] = True
    with app.test_client() as c:
        yield c
    app.config['WTF_CSRF_ENABLED'] = False
```

**Token-Holen-Muster:** Landing-Page GET → Response-Body nach `meta[name="csrf-token"]` scannen (oder `/` GET wenn csrf_token() dort gerendert wird). Da `landing.html` nach A1-Fix das Meta-Tag haben wird, kann der E2E-Test die Landing-Page GETten und den Token extrahieren.

**Kein existierender signup-Test** — `tests/test_auth_next_redirect.py` ist der nächste Verwandte (auth-flows). Für E2E-signup-with-csrf muss ein neuer Test gebaut werden.

**ERST ROT gegen HEAD:** Der Test muss gegen `current HEAD` einen CSRF-400 für `/api/register` und `/api/login` produzieren — weil landing.html noch kein `X-CSRFToken`-Header mitschickt. Erst nach A1-Fix wird er grün.

---

## Section 5: CSRF-Fix A1 — Anker-Verifikation

### 5.1 base.html CSRF-Meta-Tag (Kopier-Quelle)

`templates/base.html:6`: [VERIFIED: Read templates/base.html:1-10]
```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```
**Exakte Kopier-Quelle** für den Einbau in `landing.html <head>`.

### 5.2 landing.html — Standalone-Bestätigung + Fetch-Zeilen

`templates/marketing/landing.html:1-8`: [VERIFIED: Read templates/marketing/landing.html:1-15]
- Keine `{% extends %}` Direktive → standalone, kein base.html-Erbe ✓
- Kein `<meta name="csrf-token">` im aktuellen `<head>` ✓ (muss hinzugefügt werden)

**Fetch-Zeilen (CONTEXT vs. aktuell):**

| CONTEXT-Angabe | Tatsächliche Zeile | Fetch-Ziel | Aktion |
|---------------|-------------------|-----------|--------|
| :829 | **:829** | `fetch('/api/login', {method:'POST', ...})` | Token-Header hinzufügen |
| :897 | **:897** | `fetch('/api/register', {method:'POST', ...})` | Token-Header hinzufügen |
| :921 | **:921** | `fetch('/waitlist/join', {method:'POST', ...})` | Token-Header hinzufügen |
| :952 | **:952** | `fetch('/waitlist/stats')` GET | NICHT anfassen |

[VERIFIED: Read templates/marketing/landing.html:820-960]

Alle vier Zeilen stimmen exakt mit CONTEXT-Angaben überein — kein Drift.

### 5.3 app.py CSRF-Config

[VERIFIED: Read app.py:49-61]

- `app.py:54`: `csrf = CSRFProtect(app)` — aktiv ✓
- `app.py:58-60`: `WTF_CSRF_ENABLED=False`-Override für lokales Dev — bleibt unangetastet ✓
- `WTF_CSRF_TIME_LIMIT`: **NICHT gesetzt** im aktuellen Code — muss hinzugefügt werden

**Exakte Einfügestelle für WTF_CSRF_TIME_LIMIT=None:**
Nach Zeile 60 (Ende des WTF_CSRF_ENABLED-Override-Blocks), vor Zeile 62 (H-20 Kommentar):
```python
# Nach app.py:60 einfügen:
app.config['WTF_CSRF_TIME_LIMIT'] = None   # D-04: kein Timeout (Seite >1h offen)
```

Alternativ direkt nach `csrf = CSRFProtect(app)` (Zeile 54) als Teil der CSRF-Konfig-Sektion — Planer entscheidet. Beide Positionen sind funktional gleichwertig.

### 5.4 JS Token-Extraktions-Idiom (App-Muster)

Standard-Muster im Projekt (aus `static/feedback.js:2-5` und `static/pip-launcher.js:9-11`): [VERIFIED: Read static/feedback.js:1-5, grep static/]

```javascript
function getCsrfToken() {
    var m = document.querySelector('meta[name="csrf-token"]');
    return m ? m.getAttribute('content') : '';
}
```

Verwendung:
```javascript
headers: {'Content-Type': 'application/json', 'X-CSRFToken': getCsrfToken()}
```

`landing.html` soll diese Funktion INLINE definieren (kein externes JS) — da es eine Standalone-HTML ist und `feedback.js`/`pip-launcher.js` nicht eingebunden sind. Das Inline-Muster ist identisch mit dem in den `.js`-Dateien.

---

## Common Pitfalls

### Pitfall 1: profiles.py _rolle()-Helper-Umfang unterschätzt
**Was geht schief:** Planer ändert nur `profiles.py:36` direkt, aber die 18 Call-Sites bleiben unberührt.
**Warum:** CONTEXT D-11 nannte `:36` als Anker, aber `:36` IST die Helper-Definition — alle 18 Caller werden mit einer einzigen Änderung am Helper gefixt.
**Lösung:** `_rolle()`-Funktion einzeilig auf `g.user.rolle` umstellen → alle Call-Sites automatisch mitgefixed.

### Pitfall 2: changelog.py:84 — Scope-Frage nicht auto-entscheiden
**Was geht schief:** Executor ändert changelog.py:84 auf `g.user.rolle` UND fügt zusätzlich `@superadmin_required` hinzu, ohne André zu fragen.
**Lösung:** Mechanisch auf `g.user.rolle` umstellen (Pflicht, kein Halb-Zustand), ABER die superadmin-Frage als explizite Mini-Entscheidung im Plan markieren. André hat nur die zwei Waitlist-Löcher als Geschäftsrisiko benannt.

### Pitfall 3: changelog.py hat kein @login_required
**Was geht schief:** `POST /changelog/admin` hat kein `@login_required` — nach Umstellung auf `g.user.rolle` würde `g.user` None sein für nicht-eingeloggte User → AttributeError.
**Lösung:** Beim Umstellen von changelog.py:84 MUSS gleichzeitig `@login_required` (oder mindestens ein `if not g.get('user'):` Guard) hinzugefügt werden. [VERIFIED: Read routes/changelog.py — keine @login_required Decorator]

### Pitfall 4: waitlist.py:138 (admin-Route) hat kein @login_required — anon bekommt 200 nicht 302
**Was geht schief:** Aktuell: `GET /waitlist/admin` hat nur den schwachen Owner-Check, kein `@login_required`. Der Test `test_unauthenticated_redirects_to_login` würde aktuell NICHT mit 302 antworten — anon User kämen durch den `flask_session.get('rolle') != 'owner'` → None != 'owner' → redirect(url_for('dashboard.index')) → 302. Also tatsächlich 302, aber zum Dashboard, nicht zum Login.
**Nach Fix:** `@login_required` + `@superadmin_required` → anon bekommt 302 zur Login-Seite.

### Pitfall 5: settings.py delete_account — Zeile 191
**Exakte Zeile:** grep bestätigt `routes/settings.py:191` mit `flask_session.get('rolle') != 'owner'`. CONTEXT sagte :191 — stimmt überein. [VERIFIED]

---

## Rollen-Sweep — Vollständigkeits-Zusammenfassung

**Python-Routes-Leser (müssen auf g.user.rolle umgestellt werden):**

| Datei | Art | Zeilen | Fix-Typ |
|-------|-----|--------|---------|
| routes/profiles.py | `_rolle()` Helper | :35-36 (+ 18 Caller) | Helper-Body ändern |
| routes/organisations.py | `_require_admin` Decorator | :16 | Direkte Änderung |
| routes/dashboard.py | Inline | :526 | Direkte Änderung |
| routes/logs_routes.py | Inline | :14, :36 | Direkte Änderung |
| routes/settings.py | Inline | :63, :82, :105, :122, :191 | Direkte Änderung |
| routes/changelog.py | Inline | :84 | Direkte Änderung + @login_required |
| routes/waitlist.py | Inline | :98, :140 | ERSATZ durch @superadmin_required |

**Template-Leser (müssen auf g.user.rolle umgestellt werden):**

| Template | Zeilen |
|----------|--------|
| templates/base.html | :79 |
| templates/logs_page.html | :25 |
| templates/profiles_list.html | :30, :69, :81 |
| templates/settings.html | :115, :236 |

---

## Environment Availability

| Dependency | Status | Version | Notes |
|------------|--------|---------|-------|
| SSH zu Production | VERFÜGBAR | nerve_vps key | Verifiziert während Research |
| Production DB | VERFÜGBAR | PostgreSQL (peer-auth via inspect.sh) | Alle Checks gelaufen |
| Flask-WTF / CSRFProtect | BEREITS AKTIV | app.py:54 | Keine Neuinstallation |
| superadmin_required | BEREITS VORHANDEN | services/auth_decorators.py:5 | Import bereit |

---

## Validation Architecture

### Test Framework
| Property | Value |
|----------|-------|
| Framework | pytest (via deploy.sh:193) |
| Config | tests/conftest.py |
| Gate-Kommando | `pytest tests/ --tb=short -q -m "not live and not perf"` |
| Deploy-Gate | `bash deploy.sh production` führt pytest vor Restart aus |

### Phase Requirements → Test Map

| Req | Verhalten | Test-Typ | Datei | Existiert? |
|-----|-----------|----------|-------|------------|
| D-05 | Signup-Flow mit CSRF AN → kein 400 nach Fix | E2E (Flask-Client + CSRF an) | `tests/test_signup_journey.py` | NEIN — Wave 0 |
| D-06 | Kein state-changing fetch ohne X-CSRFToken | Statischer Sweep | `tests/test_csrf_fetch_guard.py` | NEIN — Wave 0 |
| D-07 | Jeder render_template-String → Datei existiert | Statischer Sweep | `tests/test_template_existence_guard.py` ODER in D-06 integriert | NEIN — Wave 0 |
| D-15 | waitlist anon→302, owner→403, superadmin→200 | Integration (Flask-Client) | `tests/test_waitlist_admin_auth.py` | NEIN — Wave 0 |

### ERST ROT-Disziplin (Test-Netz-Ratsche)
Alle vier neuen Tests MÜSSEN gegen aktuellen HEAD rot sein:
- `test_signup_journey.py` → rot weil CSRF-400 bei /api/register (kein Token in landing.html)
- `test_csrf_fetch_guard.py` → rot weil landing.html:829/897/921 fehlt X-CSRFToken
- `test_waitlist_admin_auth.py` → rot weil anon/owner beide 302-zum-Dashboard (kein @login_required + kein @superadmin_required)
- `test_template_existence_guard.py` → möglicherweise schon grün (kein offensichtlich fehlender Template bekannt — außer pricing.html falls noch referenziert)

### Wave 0 Gaps
- [ ] `tests/test_signup_journey.py` — CSRF-aktiviertes E2E
- [ ] `tests/test_csrf_fetch_guard.py` — tokenloser Fetch-Sweep
- [ ] `tests/test_template_existence_guard.py` (oder in test_csrf_fetch_guard integrieren)
- [ ] `tests/test_waitlist_admin_auth.py` — Waitlist-Auth-Wächter

---

## Security Domain

### ASVS-Kategorien

| ASVS Kategorie | Betrifft Phase | Kontrolle |
|----------------|---------------|-----------|
| V4 Access Control | Ja — Waitlist-Löcher, Rollen-Härtung | superadmin_required + g.user.rolle |
| V5 Input Validation | Ja — CSRF-Token-Validierung | Flask-WTF CSRFProtect |
| V3 Session Management | Ja — session['rolle'] → g.user.rolle (frische Quelle) | DB-Lesung per Request |
| V2 Authentication | Ja (Prod-Verify) — OAuth-only für Founder | passwort_hash leer = Blockade |

### Bekannte Threat-Patterns

| Pattern | STRIDE | Mitigation |
|---------|--------|-----------|
| CSRF auf Login/Register/Waitlist | Spoofing | X-CSRFToken Header (D-01/A1) |
| Stale-Role-Session (Rolle-Änderung wirkt verzögert) | Elevation of Privilege | g.user.rolle (D-10, frisch aus DB) |
| Waitlist-PII-Leak + Gratis-EA-Plätze | Information Disclosure / Spoofing | @superadmin_required auf beiden Routen (D-14) |
| OAuth-only Founder + SUPERADMIN_EMAIL falsch | Denial of Service (Selbst-Aussperr) | Prod-Vorab-Check (D-09, VERIFIZIERT GRÜN) |

---

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | Flask 3.x injiziert `g` automatisch in Jinja2-Kontext (kein custom processor nötig) | Section 2, Template-Teil | Templates könnten g.user.rolle nicht finden → 500-Fehler |
| A2 | SUPERADMIN_EMAIL-Wert stimmt mit andrepreuss712@gmail.com überein (Key vorhanden + is_superadmin=True als Beweis für erfolgreichen Seed) | Section 1 | Wert könnte auf anderen Account zeigen — unwahrscheinlich da is_superadmin=True bereits korrekt gesetzt |

**Niedrig-Risiko:** A1 ist Flask-Standard seit 0.10 (vor Flask 3.x). A2 ist durch is_superadmin=True auf Prod implizit bewiesen.

---

## Open Questions (RESOLVED)

1. **changelog.py @login_required fehlt** — ✅ RESOLVED (André-Entscheidung 2026-07-04, in Plan 03)
   - Was wir wissen: `POST /changelog/admin` hat kein `@login_required`-Decorator
   - Was unklar war: Soll @login_required ODER @superadmin_required hinzugefügt werden? André hat nur die Waitlist-Löcher als Scope benannt.
   - **Auflösung:** André entschied: `@login_required` + `@superadmin_required` + `g.user.rolle` (verschärfen wie Waitlist) PLUS Vollständigkeits-Sweep der ganzen Muster-Klasse. Umgesetzt in **Plan 03** (Task 0b Mini-Entscheidungs-Checkpoint + `<completeness_sweep>`-Tabelle + Guard-Test Owner→403).

2. **Template-Existenz-Guard: pricing.html fehlt?** — ✅ RESOLVED (in Plan 02)
   - Was wir wissen: CONTEXT erwähnt "hätte die fehlende pricing.html gefangen"
   - Was unklar war: Ob render_template('pricing.html') noch im Code existiert
   - **Auflösung:** Adressiert in **Plan 02 Task 2** (Template-Existenz-Wächter) — der Guard prüft JEDEN `render_template('…')`-String gegen die reale Datei und findet ein fehlendes Template selbst (ERST-ROT gegen seeded Verletzung bewiesen).

---

## Sources

### Verified (HIGH confidence)
- SSH inspect.sh gegen Production 178.104.82.166 (2026-07-04) — SUPERADMIN_EMAIL, passwort_hash, is_superadmin, users-Spalten
- `grep routes/*.py` + `grep templates/*.html` — alle session['rolle']-Reader/Setter
- `Read routes/auth.py:1-170` — Setzstelle :130, login_required :43-83, _do_login :158
- `Read routes/organisations.py:1-20` — _require_admin :12-19
- `Read routes/changelog.py:82-100` — flask_session.get('rolle') an :84 (CONTEXT: ~:87)
- `Read routes/settings.py:58-200` — 5 Reader bei :63/:82/:105/:122/:191
- `Read routes/waitlist.py:90-155` — beide Löcher :98/:140
- `Read routes/logs_routes.py:1-47` — Reader :14/:36
- `Read services/auth_decorators.py` — superadmin_required :5
- `Read deploy.sh:180-215` — pytest gate :186-194
- `Read tests/test_admin_dashboard_auth.py` — Wächter-Vorlage vollständig
- `Read tests/conftest.py (grep)` — cleanup_rows :238, _baseline_cleanup_guard :549
- `Read templates/base.html:1-20` — csrf-token :6
- `Read templates/marketing/landing.html:820-960` — Fetch-Zeilen :829/:897/:921/:952
- `Read app.py:1-70` — CSRFProtect :54, WTF_CSRF_ENABLED override :58-60
- `Read static/feedback.js:1-5` — getCsrfToken()-Muster
- `Read database/models.py (grep)` — rolle Column :72, is_superadmin :73

---

## Metadata

**Confidence breakdown:**
- Prod-Vorab-Verifikation: HIGH — live SSH-Checks gelaufen, alle Pass-Bedingungen erfüllt
- Rollen-Sweep: HIGH — vollständige grep-Liste, Zeilen verifiziert
- deploy.sh Gate: HIGH — direkte Code-Lesung
- Wächter-Vorlagen: HIGH — Quell-Code vollständig gelesen
- CSRF-Anker: HIGH — alle Zeilennummern bestätigt, kein Drift

**Research date:** 2026-07-04
**Valid until:** 2026-08-04 (stabiles Codebase-Segment, kein externes Dependency-Risiko)

**Zeilen-Drift-Zusammenfassung:**
- `changelog.py` flask_session.get('rolle'): CONTEXT sagte ~:87, **tatsächlich :84** — Planer muss :84 verwenden
- Alle anderen Zeilenanker aus CONTEXT stimmen exakt überein (landing.html :829/:897/:921/:952, app.py :54/:58-60, auth.py :130/:137-139/:158, organisations.py :16, settings.py :63/:82/:105/:122/:191, waitlist.py :98/:140)

**Cross-AI-Pflicht:** Phase ist 🟡 Auth/Security → Gemini-Review PFLICHT vor Execute.
Nach Planung: `/gsd-review --phase 08.23.2.AUTH-1 --all`
