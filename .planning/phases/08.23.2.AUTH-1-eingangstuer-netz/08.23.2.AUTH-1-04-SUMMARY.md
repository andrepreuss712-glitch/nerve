---
phase: 08.23.2.AUTH-1
plan: 04
subsystem: auth
tags: [authorization, rbac, stale-role, g.user, jinja, ratchet]

requires:
  - phase: 08.23.2.AUTH-1-03
    provides: "waitlist.py/changelog.py rolle-Reader bereits via @superadmin_required entfernt (aus dem Sweep raus)"
provides:
  - "Alle 17 Rollen-Leser (10 Routes + 7 Template-Zeilen) lesen g.user.rolle (frisch aus DB pro Request)"
  - "base.html:106 mit g.user-None-Guard (kein 500 auf public Seiten)"
  - "test_rolle_source_sweep.py (statischer Ratchet über routes/** UND templates/**)"
affects: [AUTH-5 (Team-Menü-Freischaltung → dann visuell testbar), spätere is_coach-Härtung]

tech-stack:
  added: []
  patterns:
    - "Rollen-Quelle = DB (g.user.rolle) statt Session-Merk-Notiz → sofort-greifende Rollen-Änderung"
    - "Statischer Reader-Sweep-Ratchet über routes UND templates (Halb-Migration-Schutz, Punkt 29)"

key-files:
  created:
    - tests/test_rolle_source_sweep.py
  modified:
    - routes/profiles.py
    - routes/organisations.py
    - routes/dashboard.py
    - routes/logs_routes.py
    - routes/settings.py
    - templates/base.html
    - templates/settings.html
    - templates/profiles_list.html
    - templates/logs_page.html

key-decisions:
  - "session['is_coach'] bewusst NICHT mit-gesweept (out of AUTH-1 scope, registrierter Defer)"
  - "base.html:106 g.user-None-Guard (Fable BLOCKER 2, public-500-Schutz)"

patterns-established:
  - "Reader-Ratchet scannt routes/** UND templates/** — ein Template-session.get('rolle')-Regress wird auch rot"

requirements-completed: []

duration: ~1 Session (supervised, Gate G RED + Gate H GREEN)
completed: 2026-07-06
---

# Phase 08.23.2.AUTH-1 Plan 04: Rollen-Quellen-Sweep Summary

**Alle 17 Rollen-Leser (10 Routes + 7 Template-Zeilen) von der Session-Merk-Notiz auf `g.user.rolle` (frisch aus DB pro Request) umgestellt → Stale-Role-Elevation-Klasse geschlossen (Rollen-Änderung greift sofort); base.html mit None-Guard gegen public-500; ein Ratchet-Wächter über routes + templates hält es fest.**

## Performance

- **Modus:** supervised inline (André fuhr Gate G + Gate H); autonomous:true (keine Checkpoints)
- **Deploy-Gates:** G (ERST-ROT, 17 Reader rot, Gate blockte) → H (grün, 897 passed, live 09:43:21 UTC)
- **Tasks:** 3 (Sweep-Wächter, 5 Routes, 4 Templates)
- **Pre-Start-Gate:** Plan-03-Abhängigkeit verifiziert (waitlist/changelog 0 Reader, Plan-03-SUMMARY existiert)

## Accomplishments
- **Stale-Role-Klasse geschlossen:** `_rolle()`-Helper (1 Zeile → 18 Caller) + organisations/dashboard/logs_routes/settings + 4 Templates lesen `g.user.rolle` frisch aus DB. Setter `auth.py:130` unverändert (D-12).
- **Fable BLOCKER 2 (public-500) geschlossen:** base.html:106 `{% if g.user and g.user.rolle in (...) %}` — live verifiziert: `/impressum`,`/agb`,`/datenschutz` = 200, `/changelog` 308→/changelog/→200, kein Traceback (ausgeloggt).
- **Ratchet-Wächter:** `test_rolle_source_sweep.py` scannt routes/** UND templates/** (Fable-Erweiterung) — ERST-ROT bewiesen (17 Reader), grün nach Umstellung.

## Task Commits

1. **Task 1: test_rolle_source_sweep.py (ERST-ROT gegen HEAD)** — `f3d4ab3` (test)
2. **Task 2: 5 Routes auf g.user.rolle** — `4151721` (feat)
3. **Task 3: 4 Templates auf g.user.rolle + base.html None-Guard** — `666070b` (feat)

## Files Created/Modified
- `tests/test_rolle_source_sweep.py` — statischer Ratchet (routes + templates), Whitelist nur Setter
- `routes/profiles.py`, `organisations.py`, `dashboard.py`, `logs_routes.py`, `settings.py` — Rollen-Leser → g.user.rolle (10 Sites)
- `templates/base.html` (None-Guard), `settings.html`, `profiles_list.html`, `logs_page.html` — Rollen-Leser → g.user.rolle (7 Zeilen)

## ERST-ROT Rot-Beleg (Gate G, verbatim — André 2026-07-06, Gate blockte, auth.py:130 NICHT dabei)
`test_rolle_source_sweep.py::test_no_session_rolle_readers` FAILED — 17 Reader: dashboard.py:526, logs_routes.py:14/:36, organisations.py:16, profiles.py:36, settings.py:63/:82/:105/:122/:191 + base.html:105, logs_page.html:25, profiles_list.html:30/:69/:81, settings.html:115/:236. Nach Task 2/3: 0 Reader → grün (Gate H).

## Deviations from Plan
None im Code — Plan exakt umgesetzt. Einzige Anpassung: base.html-Reader war **:105** statt der im Plan genannten :79 (durch die ~25 Wrapper-Zeilen aus Plan 01 nach unten verschoben) — Anker vor Edit re-verifiziert (Punkt 20). `from flask import g` war in profiles.py bereits vorhanden (multi-line Import) — keine Ergänzung nötig.

## Issues Encountered

**1. [Test-Naht-Befund] Visueller „Menü-Sichtbarkeit pro Rolle"-Test heute NICHT prüfbar**
- Der einzige rolle-abhängige Menüeintrag (base.html:106, owner/admin) ist **„Team"** — der liegt im `display:none`-Legacy-Block (D-31, erst AUTH-5 schaltet ihn frei), also unsichtbar. Die *sichtbaren* Admin-Einträge (Admin/CRM/EWB) hängen an `is_superadmin`, NICHT an `rolle`. Ein Rollen-Umstell-Test an Andrés Konto (owner→member) würde visuell nichts zeigen (Superadmin-Häkchen bliebe) → kein sauberer Test, bewusst NICHT gefahren.
- **Stattdessen technisch belegt:** die Sofort-Wirkung (Rolle frisch pro Request aus g.user statt Session) ist durch den grünen `test_no_session_rolle_readers` bewiesen — alle 17 Leser lesen jetzt frisch. **Visuell testbar erst mit AUTH-5** (Team-Eintrag freigeschaltet). → Vermerk für AUTH-5.

## Deferred (registriert, nicht gebaut)
- **session['is_coach']** (coach.py:17, base.html) — identische Stale-Session-Schwäche wie rolle, bewusst out of AUTH-1 scope (André scopte nur `rolle`). Kandidat für spätere Coach-Härtung.

## Next Phase Readiness
- **AUTH-1 baulich durch** (alle 4 Plans live). Phase-Verifikation + Abschluss folgt (supervised, kein Auto-Advance).
- **AUTH-5-Vermerk:** visueller Rollen-Menü-Test wird prüfbar, sobald der Team-Legacy-Block (D-31) freigeschaltet ist.

---
*Phase: 08.23.2.AUTH-1*
*Plan: 04*
*Completed: 2026-07-06*
