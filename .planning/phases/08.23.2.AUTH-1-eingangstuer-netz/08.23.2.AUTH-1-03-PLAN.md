---
phase: 08.23.2.AUTH-1
plan: 03
type: execute
wave: 1
complexity: "🟡 mittel (Auth/Security — Cross-AI Gemini PFLICHT vor Execute)"
depends_on: []
files_modified:
  - routes/waitlist.py
  - routes/changelog.py
  - tests/test_waitlist_admin_auth.py
autonomous: false   # enthält BLOCKING Prod-Vorab-Verify + eine Mini-Entscheidung-Checkpoint (changelog superadmin-Scope)
requirements: []   # Scope = CONTEXT-Decisions D-13, D-14, D-15, D-16 + André-changelog-Direktive
user_setup: []

must_haves:
  truths:
    - "GET /waitlist/admin ist NUR für Superadmin erreichbar — jeder Firmen-Owner bekommt 403 (keine fremden Interessenten-PII mehr)"
    - "POST /waitlist/invite/<wid> ist NUR für Superadmin erreichbar — kein Firmen-Owner kann fremde einladen / Gratis-EA-Plätze verschenken"
    - "POST /changelog/admin ist NUR für Superadmin erreichbar (André-Direktive) und hat @login_required (kein AttributeError für anon)"
    - "Ein anonymer Zugriff auf alle drei Routen wird zur Login-Seite umgeleitet (nicht mehr 302-zum-Dashboard über den schwachen Owner-Check)"
    - "Andrés Superadmin-Zugang bleibt intakt (Prod-Vorab bewiesen: SUPERADMIN_EMAIL gesetzt, is_superadmin=True)"
    - "Der Waechter test_waitlist_admin_auth.py ist gegen HEAD nachweislich ROT (Owner kommt aktuell durch), grün nach Fix"
  artifacts:
    - path: "routes/waitlist.py"
      provides: "@login_required + @superadmin_required auf admin_waitlist + invite_from_waitlist; schwache flask_session-Owner-Checks entfernt"
      contains: "superadmin_required"
    - path: "routes/changelog.py"
      provides: "@login_required + @superadmin_required auf add_entry; schwacher Owner-Check entfernt"
      contains: "superadmin_required"
    - path: "tests/test_waitlist_admin_auth.py"
      provides: "Waechter: anon→Redirect, Owner→403, Superadmin→200/erlaubt für admin + invite"
      contains: "is_superadmin"
  key_links:
    - from: "routes/waitlist.py admin_waitlist + invite_from_waitlist"
      to: "services.auth_decorators.superadmin_required (g.user.is_superadmin)"
      via: "@superadmin_required Decorator + @login_required (routes.auth)"
      pattern: "@superadmin_required"
---

<objective>
Zwei bewiesene Sicherheits-Löcher (schwacher `flask_session.get('rolle') != 'owner'`-Check → JEDER Firmen-Owner erreicht fremde Interessenten-Daten) plus das identische Sibling-Loch im Changelog-Admin werden auf `@login_required` + `@superadmin_required` gehärtet — als EINE, in einem Durchgang entschiedene Vollständigkeits-Runde (André-Direktive „kein viertes Loch als Überraschung"). Ein Waechter (test_waitlist_admin_auth.py, ERST ROT gegen HEAD) nagelt die Berechtigung für immer fest.

Purpose: PII-Leck fremder Interessenten schließen + Gratis-EA-Platz-Verschenken (Geschäftsschaden) verhindern (D-13/D-14) + das strukturgleiche Changelog-Loch mitfixen (André 2026-07-04).
Output: gehärtete waitlist.py + changelog.py, test_waitlist_admin_auth.py, plus die dokumentierte Vollständigkeits-Entscheidungs-Tabelle.

Komplexität: 🟡 mittel — Auth/Security → Cross-AI Gemini PFLICHT vor Execute. `autonomous: false` wegen BLOCKING Prod-Vorab-Verify (Selbst-Aussperr-Schutz) + einer Mini-Entscheidung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md
@routes/CLAUDE.md

<interfaces>
<!-- KRITISCHER FUND (live gegen echten Code verifiziert, korrigiert CONTEXT/RESEARCH): -->
<!-- login_required lebt in routes/auth.py:43, NICHT in services/auth_decorators.py. -->
<!-- services/auth_decorators.py enthält NUR superadmin_required. -->

Import-Pattern (VERIFIED gegen bestehende Nutzung in admin_dashboard.py:11-12, admin_ewb.py:20-21):
```python
from routes.auth import login_required
from services.auth_decorators import superadmin_required
```

superadmin_required (services/auth_decorators.py:5, VERIFIED):
```python
def superadmin_required(f):
    @wraps(f)
    def wrapper(*args, **kwargs):
        if not getattr(g, 'user', None) or not getattr(g.user, 'is_superadmin', False):
            abort(403)
        return f(*args, **kwargs)
    return wrapper
```
→ 403 für Nicht-Superadmin. Setzt g.user voraus → @login_required MUSS davor stehen (setzt g.user oder redirectet).

login_required (routes/auth.py:43, VERIFIED): prüft `'user_id' in session`, setzt g.user/g.org, redirectet zur Login-Seite (via _login_redirect_with_next) wenn nicht eingeloggt.

Ziel-Routen (VERIFIED gegen echten Code 2026-07-04):
- routes/waitlist.py:96-99  `invite_from_waitlist(wid)` — POST /waitlist/invite/<int:wid> — HAT KEIN @login_required, nur `if flask_session.get('rolle') != 'owner': return jsonify(...), 403`
- routes/waitlist.py:138-141 `admin_waitlist()` — GET /waitlist/admin — HAT KEIN @login_required, nur `if flask_session.get('rolle') != 'owner': return redirect(url_for('dashboard.index'))`
- routes/changelog.py:82-85  `add_entry()` — POST /changelog/admin — HAT KEIN @login_required, nur `if flask_session.get('rolle') != 'owner': return jsonify(...), 403`

Blueprint-Namen (routes/CLAUDE.md, für url_for in Tests): waitlist='waitlist', changelog='changelog', auth='auth', dashboard='dashboard'.
</interfaces>

<completeness_sweep>
## Vollständigkeits-Sweep (Punkt 20/22 — André-Direktive „kein viertes Loch")

**Klasse (a): schwacher `flask_session/session.get('rolle') !=/== 'owner'`-Auth-Check** (grep-verifiziert 2026-07-04, `routes/`):

| Datei:Zeile | Route | Aktuell | @login_required? | VERDIKT | Begründung |
|---|---|---|---|---|---|
| waitlist.py:98 | POST /waitlist/invite/<wid> | `!= 'owner'` → 403 | NEIN | **@login_required + @superadmin_required** | D-14 — Gratis-EA-Platz-Verschenken (Geschäftsschaden) |
| waitlist.py:140 | GET /waitlist/admin | `!= 'owner'` → redirect | NEIN | **@login_required + @superadmin_required** | D-14 — fremde Interessenten-PII |
| changelog.py:84 | POST /changelog/admin | `!= 'owner'` → 403 | NEIN | **@login_required + @superadmin_required** | André-Direktive 2026-07-04 — strukturgleiche Klasse; Changelog-Einträge sind Founder-Arbeit. (Mini-Entscheidung-Checkpoint bestätigt Scope.) |
| settings.py:63,82,105,122 | diverse settings-POST | `not in ('owner','admin')` | ja (settings.py:2 importiert login_required; auf @login_required-Routen) | **keep (owner/admin) — nur session→g.user in Plan 04** | behaviour-preserving; kein Superadmin-only-Bedarf (org-scoped, nicht cross-tenant) |
| settings.py:191 | delete_account | `!= 'owner'` | ja | **keep (owner) — nur session→g.user in Plan 04** | org-eigene Aktion, kein cross-tenant-Leck |
| organisations.py:16 | _require_admin decorator | `not in ('owner','admin')` | ja | **keep (owner/admin) — nur session→g.user in Plan 04** | org-scoped Admin, kein Superadmin-Bedarf |

**Klasse (b): state-changing Route (POST/PUT/PATCH/DELETE) OHNE @login_required** (grep `methods=[...POST/PUT/PATCH/DELETE]` + @login_required-Abwesenheit, `routes/waitlist.py` + `routes/changelog.py` vollständig geprüft):

| Datei:Zeile | Route | @login_required? | Selbst-Schutz? | VERDIKT |
|---|---|---|---|---|
| waitlist.py:10 | POST /waitlist/join | NEIN | — | **keep public** — bewusst öffentlich (Landing-Signup, D-03-Kontext) |
| waitlist.py:96 | POST /waitlist/invite/<wid> | NEIN | schwacher Owner-Check | **FIX (siehe Klasse a)** |
| changelog.py:65 | POST /changelog/seen | NEIN | `if 'user_id' not in flask_session: return {'ok':True}` (inline Guard, VERIFIED :67) | **keep** — selbst-geschützt, kein Schreib-Risiko für anon |
| changelog.py:82 | POST /changelog/admin | NEIN | schwacher Owner-Check | **FIX (siehe Klasse a)** |

**FAZIT:** Die drei Tighten-Targets (waitlist admin+invite, changelog admin) sind die VOLLSTÄNDIGE Menge der Pattern-Klasse. Kein viertes unprotected Schreib/Admin-Loch derselben Klasse gefunden. `/waitlist/join` (öffentlich by design) und `/changelog/seen` (inline-selbstgeschützt) sind korrekt und bleiben. Die settings/profiles/organisations owner-admin-Reader sind behaviour-preserving und werden NUR in Plan 04 mechanisch session→g.user umgestellt (kein Scope-Wechsel).
</completeness_sweep>

<verify_environment>
- `bash deploy.sh production` pytest-Gate (deploy.sh:186-194). test_waitlist_admin_auth.py auto-collected.
- Prod-Live-Zustand read-only: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'`
</verify_environment>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: [BLOCKING] Prod-Vorab-Verify — Selbst-Aussperr-Schutz (D-09/D-16)</name>
  <what-built>Noch nichts geändert. Dieser Gate stellt VOR jedem auth-sperrenden Edit sicher, dass die Härtung André nicht selbst aussperrt (Sicht statt Zusicherung). Prior-Run in RESEARCH.md Section 1 war GRÜN — dieser Re-Check ist billig und Pflicht direkt vor dem Edit.</what-built>
  <how-to-verify>
    Führe read-only gegen Production aus:
    ```bash
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh env-keys' | grep SUPERADMIN_EMAIL
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample users 5'
    ```
    PASS-Bedingungen (ALLE müssen erfüllt sein, sonst STOP):
    1. `SUPERADMIN_EMAIL` erscheint in env-keys (Key vorhanden — inspect.sh gibt nur Namen, nie Wert).
    2. Row für `andrepreuss712@gmail.com`: `is_superadmin` = t (TRUE).
    3. Selbe Row: `oauth_provider` = google (Zugangspfad intakt).
    (passwort_hash-leer ist für DIESEN Fix nicht relevant — betrifft Plan-01-Fundament E3, hier geht es NUR um is_superadmin für den Superadmin-Zugang zu Waitlist/Changelog.)
  </how-to-verify>
  <resume-signal>Tippe "verified" wenn is_superadmin=t + SUPERADMIN_EMAIL vorhanden + oauth_provider=google bestätigt. Bei Abweichung: STOP, NICHT fixen, an André eskalieren (sonst Aussperrung).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0b: [DECISION] changelog.py Scope-Bestätigung (Mini-Entscheidung)</name>
  <decision>POST /changelog/admin (changelog.py:84) auf @superadmin_required härten (wie Waitlist) — Bestätigung des Scope.</decision>
  <context>
    André hat am 2026-07-04 explizit direktivt: changelog.py:84 → @superadmin_required + @login_required + g.user.rolle-Konvertierung, gecovered vom Guard (Owner→403). RESEARCH Open Question 1 hatte das als offene Scope-Frage markiert (André scopte ursprünglich NUR die zwei Waitlist-Löcher als Security-Risiko). Die Executor-Direktive im planning_context löst das bereits auf zu superadmin_required. Dieser Checkpoint dokumentiert die Entscheidung explizit für Audit-Traceability (Punkt 24, 3-Sichten).
  </context>
  <options>
    <option id="superadmin">
      <name>@login_required + @superadmin_required (André-Direktive, DEFAULT)</name>
      <pros>Strukturgleich zu Waitlist-Fix; Changelog-Einträge sind Founder-Arbeit; schließt „jeder Firmen-Owner schreibt Changelog"-Loch</pros>
      <cons>Leicht strenger als vorher (nur noch Superadmin statt jeder Owner)</cons>
    </option>
    <option id="login-owner">
      <name>@login_required + g.user.rolle == 'owner' (minimal)</name>
      <pros>Behält Owner-Scope</pros>
      <cons>Widerspricht André-Direktive; lässt das Loch teilweise offen (jeder Firmen-Owner)</cons>
    </option>
  </options>
  <resume-signal>Bestätige "superadmin" (Default, André-Direktive) oder wähle "login-owner". Bei "superadmin": weiter mit Task 1-3 wie geplant.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: test_waitlist_admin_auth.py — Waechter, ERST ROT gegen HEAD</name>
  <read_first>
    - tests/test_admin_dashboard_auth.py — VOLLSTÄNDIGE Vorlage (cleanup_tracker, _make_org_and_user, anon→redirect / user→403 / superadmin→200) (RESEARCH Section 4.1)
    - tests/conftest.py — cleanup_rows (:238), _baseline_cleanup_guard (:549), db_from_client-Fixture
    - routes/waitlist.py:96-141 — beide Ziel-Routen (Ist-Zustand: schwacher Owner-Check, kein @login_required)
    - RESEARCH.md Section 4.1 + Pitfall 4 (anon bekommt aktuell 302-zum-Dashboard, nach Fix 302-zur-Login-Seite)
  </read_first>
  <behavior>
    - cleanup_tracker-Fixture: `{User: [], Organisation: [], Waitlist: []}` (invite-Test braucht einen Waitlist-Eintrag).
    - `_make_org_and_user(session, email, is_superadmin, tracker)` analog Vorlage.
    - GET /waitlist/admin: `test_admin_anon_redirects` (kein Login → 302), `test_admin_normal_owner_gets_403` (Owner is_superadmin=False → 403), `test_admin_superadmin_gets_200` (is_superadmin=True → 200).
    - POST /waitlist/invite/<wid>: `test_invite_owner_gets_403` (Owner → 403), `test_invite_superadmin_allowed` (Superadmin + existierender Waitlist-Eintrag → nicht-403; 200/erwartete Antwort). Superadmin-invite committet Org+Invitation → cleanup_tracker muss diese auch tracken (oder Waitlist-Eintrag mit Status prüfen ohne echte Org-Erstellung, Discretion; falls Org erstellt wird, ID tracken).
    - ERST-ROT gegen HEAD: aktuell hat Owner (rolle='owner' in session) durch den schwachen Check ZUGANG → `test_admin_normal_owner_gets_403` liefert gegen HEAD 200/redirect statt 403 → ROT. Nach Fix → 403 → grün. (Test-Netz-Ratsche.)
    - Committende Rows: cleanup_rows im Teardown (CLAUDE.md Test-Cleanup-Regel).
    - ASCII: Test-/Fixture-Namen + dict-keys ASCII.
  </behavior>
  <action>
    Erstelle `tests/test_waitlist_admin_auth.py` nach dem Muster test_admin_dashboard_auth.py. Import `from tests.conftest import cleanup_rows`, `from database.models import User, Organisation, Waitlist`. Session setzen via `with client.session_transaction() as s: s['user_id'] = u.id` (und `s['rolle'] = 'owner'` für die HEAD-Rot-Reproduktion des schwachen Checks — der Fix ignoriert diesen dann).

    Fälle (siehe behavior). Für invite: vorab einen Waitlist-Eintrag mit status='waiting' anlegen (tracker[Waitlist].append(id)), dann `client.post(f'/waitlist/invite/{wid}')`.

    SEQUENZ „ERST rot, DANN fixen": Diesen Test zuerst committen+deployen; gegen HEAD MUSS `test_admin_normal_owner_gets_403` rot sein (Owner kommt durch). deploy.sh-Gate blockt → Rot-Beleg. DANN Task 2+3 anwenden → grün. Rot-Beleg im SUMMARY dokumentieren.
  </action>
  <verify>
    <automated>bash deploy.sh production  # VOR Task 2/3: test_admin_normal_owner_gets_403 ROT (Gate blockt). NACH Task 2/3: alle Waechter-Fälle grün.</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_waitlist_admin_auth.py existiert; grep zeigt Fälle für anon-redirect, owner-403, superadmin-200 auf admin UND invite.
    - grep zeigt `is_superadmin` + `cleanup_rows`.
    - Rot-Beleg gegen HEAD dokumentiert (Owner kommt aktuell durch → 403-Test rot).
  </acceptance_criteria>
  <done>Waechter existiert, gegen HEAD rot (Owner-Zugang), deckt admin + invite mit anon/owner/superadmin ab.</done>
</task>

<task type="auto">
  <name>Task 2: routes/waitlist.py — @login_required + @superadmin_required auf beide Löcher</name>
  <read_first>
    - routes/waitlist.py:1-7 (Imports — flask_session, g bereits importiert), :96-141 (beide Ziel-Routen)
    - routes/admin_dashboard.py:11-12 — exaktes Doppel-Import-Muster für login_required + superadmin_required
    - <interfaces> oben — login_required aus routes.auth, superadmin_required aus services.auth_decorators
  </read_first>
  <action>
    (a) Imports oben in routes/waitlist.py ergänzen:
    ```python
    from routes.auth import login_required
    from services.auth_decorators import superadmin_required
    ```

    (b) `invite_from_waitlist` (:96): Decorator-Stack setzen (Reihenfolge: route → login_required → superadmin_required, damit login_required g.user zuerst setzt):
    ```python
    @waitlist_bp.route('/invite/<int:wid>', methods=['POST'])
    @login_required
    @superadmin_required
    def invite_from_waitlist(wid):
    ```
    Die schwache Zeile `if flask_session.get('rolle') != 'owner': return jsonify({'error': 'Keine Berechtigung'}), 403` (:98-99) ENTFERNEN.

    (c) `admin_waitlist` (:138): analog:
    ```python
    @waitlist_bp.route('/admin')
    @login_required
    @superadmin_required
    def admin_waitlist():
    ```
    Die schwache Zeile `if flask_session.get('rolle') != 'owner': return redirect(url_for('dashboard.index'))` (:140-141) ENTFERNEN.

    KEINE UI-Änderung (Weg a, Wurzel-Lösung — die /waitlist/admin-Seite IST inhaltlich schon die Founder-Ansicht, D-14). `flask_session`/`redirect`/`url_for`-Imports bleiben (evtl. anderweitig genutzt — grep vor Entfernen falls ungenutzt; im Zweifel lassen).
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_waitlist_admin_auth.py grün (owner→403, superadmin→200, anon→redirect). Danach Browser-Live-Test: als Nicht-Superadmin-Owner /waitlist/admin → 403; als Superadmin → Seite lädt.</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/waitlist.py zeigt `@login_required` + `@superadmin_required` über BEIDEN Funktionen (invite_from_waitlist, admin_waitlist).
    - grep zeigt KEIN `flask_session.get('rolle')` mehr in waitlist.py (beide schwachen Checks entfernt).
    - deploy.sh Gate grün mit Waechter; Browser-Live-Test: Owner→403, Superadmin→Zugang (Sicht statt Zusicherung).
  </acceptance_criteria>
  <done>Beide Waitlist-Routen nur noch für Superadmin; schwache Owner-Checks weg; kein PII-Leck / kein Gratis-EA-Verschenken.</done>
</task>

<task type="auto">
  <name>Task 3: routes/changelog.py — @login_required + @superadmin_required auf add_entry</name>
  <read_first>
    - routes/changelog.py:1-6 (Imports — flask_session, g importiert; login_required/superadmin_required NICHT), :82-99 (add_entry)
    - Task 0b Entscheidung (DEFAULT: superadmin)
    - RESEARCH Pitfall 3 (changelog hat kein @login_required → nach g.user-Nutzung AttributeError für anon → @login_required MUSS mit dazu)
  </read_first>
  <action>
    (a) Imports oben in routes/changelog.py ergänzen:
    ```python
    from routes.auth import login_required
    from services.auth_decorators import superadmin_required
    ```

    (b) `add_entry` (:82): Decorator-Stack:
    ```python
    @changelog_bp.route('/admin', methods=['POST'])
    @login_required
    @superadmin_required
    def add_entry():
    ```
    Die schwache Zeile `if flask_session.get('rolle') != 'owner': return jsonify({'error': 'Keine Berechtigung'}), 403` (:84-85) ENTFERNEN.

    Damit ist changelog.py:84 aus dem Rollen-Sweep (Plan 04) heraus — es liest keine session-Rolle mehr, sondern nutzt @superadmin_required (g.user.is_superadmin). Plan 04 muss changelog.py:84 NICHT mehr anfassen (Notiz an Plan 04). @login_required verhindert den AttributeError-Pfad (Pitfall 3).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate grün. Browser-Live-Test: als Nicht-Superadmin POST /changelog/admin → 403; als anon → Redirect zur Login-Seite (kein AttributeError/500).</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/changelog.py zeigt `@login_required` + `@superadmin_required` über add_entry.
    - grep zeigt KEIN `flask_session.get('rolle')` mehr in changelog.py.
    - deploy.sh Gate grün; anon-Zugriff auf /changelog/admin → Redirect (kein 500/AttributeError).
  </acceptance_criteria>
  <done>POST /changelog/admin nur noch Superadmin, mit @login_required (kein anon-AttributeError); schwacher Owner-Check weg.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN) — @login_required liest users-Row (via session['user_id']) und setzt g.user; @superadmin_required liest `g.user.is_superadmin`. Bestehende Spalten, KEINE Schema-Änderung.
- `organisations`, `waitlist`, `invitations` (schreiben, nur im Test via Superadmin-invite-Pfad) — bestehende Tabellen, keine neuen Felder.

### inspect.sh-Beleg (Prod, VERIFIED RESEARCH Section 1, 2026-07-04)
`inspect.sh columns users`: `is_superadmin` boolean NOT NULL — bestätigt, existiert. `inspect.sh sample users 5`: andrepreuss712@gmail.com → is_superadmin=t. Damit ist der von @superadmin_required gelesene Wert real vorhanden und für Andrés Account TRUE (Selbst-Aussperr-Schutz, Task 0).

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Leser läuft voraus? | Verifiziert? |
|---|---|---|---|---|---|
| g.user.is_superadmin | gelesen in superadmin_required | DB-Spalte users.is_superadmin | synchron (login_required setzt g.user pro Request) | Nein — synchron request-scope | ✓ inspect.sh columns/sample |
| g.user (Objekt) | gesetzt in login_required (auth.py:55) | DB-Spalte users.* | synchron pro Request | Nein | ✓ auth.py:48-55 |

Kein async Schreib/Lese-Versatz (Punkt 26): g.user wird synchron in before_request/login_required gesetzt, VOR dem Decorator-Body gelesen. Kein Hintergrund-Thread, keine Batch-Write-Naht.
</persistenz_schicht_verifikation>

<verbindungs_karte>
## Verbindungs-Karte (Punkt 22)

- **superadmin_required** (services/auth_decorators.py:5) — 1 Definition; Leser (Nutzer): admin_dashboard.py, admin_ewb.py, admin_views.py (grep-belegt), NEU: waitlist.py + changelog.py. LEBT (aktive Produktiv-Nutzung). Kein neuer Auth-Code — nur Decorator-Anhängen.
- **login_required** (routes/auth.py:43) — 1 Definition; importiert von 18 route-Modulen (grep-belegt: payments, crm_export, organisations, admin_ewb, coach, dashboard, onboarding, admin_dashboard, profiles, settings, performance, learning, app_routes, logs_routes, training). LEBT. NEU: waitlist.py + changelog.py.
- **is_superadmin** (database/models.py, users-Tabelle) — geschrieben vom SUPERADMIN_EMAIL-Seed (auth.py:137-139); gelesen von superadmin_required. LEBT + auf Prod TRUE für André (inspect.sh-belegt).
- Beweis „kein viertes Loch": completeness_sweep oben (Klasse a + b vollständig gegrept in waitlist.py/changelog.py; settings/organisations sind org-scoped keep).
</verbindungs_karte>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eingeloggter Firmen-Owner (fremde Org) → /waitlist/admin, /waitlist/invite, /changelog/admin | ein legitim eingeloggter, aber nicht-Superadmin User versucht Founder-Funktionen zu erreichen |
| anonymer Browser → dieselben Routen | unauthenticated Zugriff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-07 | Information Disclosure (fremde Interessenten-PII) | GET /waitlist/admin | mitigate | @login_required + @superadmin_required; Owner→403 (D-14); Waechter beweist |
| T-AUTH1-08 | Spoofing/Elevation (Gratis-EA-Platz verschenken → Geschäftsschaden) | POST /waitlist/invite/<wid> | mitigate | @login_required + @superadmin_required; kein Owner kann fremde einladen (D-14) |
| T-AUTH1-09 | Elevation (jeder Firmen-Owner schreibt Changelog) | POST /changelog/admin | mitigate | @login_required + @superadmin_required (André-Direktive) |
| T-AUTH1-10 | Denial of Service (AttributeError/500 für anon nach g.user-Nutzung) | changelog add_entry | mitigate | @login_required VOR superadmin_required → anon redirected, g.user nie None im Body (Pitfall 3) |
| T-AUTH1-11 | Denial of Service (Selbst-Aussperrung des Founders) | Superadmin-Zugang | mitigate | BLOCKING Prod-Vorab-Verify Task 0 (is_superadmin=t bewiesen); OAuth-Zugang intakt |
</threat_model>

<verification>
- Task 0 Prod-Vorab: is_superadmin=t + SUPERADMIN_EMAIL vorhanden + oauth_provider=google (BLOCKING, bestätigt).
- Task 0b Scope-Entscheidung dokumentiert (DEFAULT superadmin).
- test_waitlist_admin_auth.py: rot gegen HEAD (Owner-Zugang), grün nach Fix.
- Browser-Live-Test: Nicht-Superadmin-Owner → 403 auf allen drei; Superadmin → Zugang; anon → Redirect zur Login-Seite (Sicht statt Zusicherung).
- grep: kein `flask_session.get('rolle')` mehr in waitlist.py + changelog.py.
</verification>

<success_criteria>
- Drei Löcher (waitlist admin+invite, changelog admin) auf @login_required + @superadmin_required gehärtet.
- Vollständigkeits-Tabelle dokumentiert; kein viertes Loch derselben Klasse offen.
- Waechter im Deploy-Gate, ERST-ROT bewiesen.
- Founder-Zugang intakt (Prod-Vorab grün).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-03-SUMMARY.md` (inkl. Task-0-Prod-Beleg, Task-0b-Entscheidung, Waechter-Rot-Beleg, Browser-Live-Test, Notiz an Plan 04: changelog.py:84 ist erledigt/nicht mehr anfassen).
</output>
