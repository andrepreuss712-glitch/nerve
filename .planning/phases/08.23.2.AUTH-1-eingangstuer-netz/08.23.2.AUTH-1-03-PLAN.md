---
phase: 08.23.2.AUTH-1
plan: 03
type: execute
wave: 3
complexity: "🟡 mittel (Auth/Security — Cross-AI Fable erfolgt; dieser Replan IST die Incorporation)"
depends_on: [02]
files_modified:
  - routes/waitlist.py
  - routes/changelog.py
  - tests/test_waitlist_admin_auth.py
autonomous: false   # enthaelt BLOCKING Prod-Vorab-Verify + eine Mini-Entscheidung-Checkpoint (changelog superadmin-Scope)
requirements: []   # Scope = CONTEXT-Decisions D-13, D-14, D-15, D-16 + André-changelog-Direktive
user_setup: []

must_haves:
  truths:
    - "GET /waitlist/admin ist NUR für Superadmin erreichbar — jeder Firmen-Owner bekommt 403 (keine fremden Interessenten-PII mehr)"
    - "POST /waitlist/invite/<wid> ist NUR für Superadmin erreichbar — kein Firmen-Owner kann fremde einladen / Gratis-EA-Plätze verschenken"
    - "POST /changelog/admin ist NUR für Superadmin erreichbar (André-Direktive) und hat @login_required (kein AttributeError für anon)"
    - "Der Waechter deckt explizit den Fall POST /changelog/admin Owner→403 ab (Fable should-include)"
    - "Ein anonymer Zugriff auf alle drei Routen wird zur Login-Seite umgeleitet (nicht mehr 302-zum-Dashboard über den schwachen Owner-Check)"
    - "Andrés Superadmin-Zugang bleibt intakt (Prod-Vorab bewiesen: SUPERADMIN_EMAIL gesetzt, is_superadmin=True)"
    - "Der Waechter test_waitlist_admin_auth.py ist gegen HEAD nachweislich ROT (Owner kommt aktuell durch), grün nach Fix; committende Rows (inkl. Invitation + invite-erzeugte Org) werden sauber weggeraeumt"
  artifacts:
    - path: "routes/waitlist.py"
      provides: "@login_required + @superadmin_required auf admin_waitlist + invite_from_waitlist; schwache flask_session-Owner-Checks entfernt"
      contains: "superadmin_required"
    - path: "routes/changelog.py"
      provides: "@login_required + @superadmin_required auf add_entry; schwacher Owner-Check entfernt"
      contains: "superadmin_required"
    - path: "tests/test_waitlist_admin_auth.py"
      provides: "Waechter: anon→Redirect, Owner→403, Superadmin→erlaubt für admin + invite; PLUS changelog/admin Owner→403; Cleanup inkl. Invitation + invite-Org"
      contains: "is_superadmin"
  key_links:
    - from: "routes/waitlist.py admin_waitlist + invite_from_waitlist + routes/changelog.py add_entry"
      to: "services.auth_decorators.superadmin_required (g.user.is_superadmin)"
      via: "@superadmin_required Decorator + @login_required (routes.auth)"
      pattern: "@superadmin_required"
---

<objective>
Zwei bewiesene Sicherheits-Löcher (schwacher `flask_session.get('rolle') != 'owner'`-Check → JEDER Firmen-Owner erreicht fremde Interessenten-Daten) plus das identische Sibling-Loch im Changelog-Admin werden auf `@login_required` + `@superadmin_required` gehärtet — als EINE, in einem Durchgang entschiedene Vollständigkeits-Runde (André-Direktive „kein viertes Loch als Überraschung"). Ein Waechter (test_waitlist_admin_auth.py, ERST ROT gegen HEAD) nagelt die Berechtigung für immer fest — inkl. dem Fall changelog/admin Owner→403 (Fable should-include).

Purpose: PII-Leck fremder Interessenten schließen + Gratis-EA-Platz-Verschenken (Geschäftsschaden) verhindern (D-13/D-14) + das strukturgleiche Changelog-Loch mitfixen (André 2026-07-04).
Output: gehärtete waitlist.py + changelog.py, test_waitlist_admin_auth.py, plus die dokumentierte Vollständigkeits-Entscheidungs-Tabelle.

Komplexität: 🟡 mittel — Auth/Security. Cross-AI (Fable) mit diesem Replan eingearbeitet. `autonomous: false` wegen BLOCKING Prod-Vorab-Verify (Selbst-Aussperr-Schutz) + einer Mini-Entscheidung.

**HINWEIS (Serialisierung, Fable BLOCKER 4):** `depends_on: [02]` + `wave: 3` — die Plaene teilen das serielle Prod-Deploy-Gate + haben ueberlappende ERST-ROT-Fenster; strikt 01→02→03→04, damit ein rotes Gate attribuierbar bleibt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-FABLE-REVIEW.md
@routes/CLAUDE.md

<interfaces>
<!-- KRITISCHER FUND (live gegen echten Code verifiziert, korrigiert CONTEXT/RESEARCH): -->
<!-- login_required lebt in routes/auth.py:43, NICHT in services/auth_decorators.py. -->
<!-- services/auth_decorators.py enthaelt NUR superadmin_required. -->

Import-Pattern (VERIFIED gegen bestehende Nutzung in admin_dashboard.py:11-12, admin_ewb.py:20-21):
```python
from routes.auth import login_required
from services.auth_decorators import superadmin_required
```

superadmin_required (services/auth_decorators.py:5, VERIFIED):
```python
def superadmin_required(f):
    @wraps(f)
    def wrapper(*args, **kwargs):
        if not getattr(g, 'user', None) or not getattr(g.user, 'is_superadmin', False):
            abort(403)
        return f(*args, **kwargs)
    return wrapper
```
→ 403 fuer Nicht-Superadmin. Setzt g.user voraus → @login_required MUSS davor stehen.

login_required (routes/auth.py:43, VERIFIED): prueft `'user_id' in session`, setzt g.user/g.org, redirectet zur Login-Seite wenn nicht eingeloggt.

Ziel-Routen (VERIFIED gegen echten Code 2026-07-04):
- routes/waitlist.py:96-99  `invite_from_waitlist(wid)` — POST /waitlist/invite/<int:wid> — KEIN @login_required, nur `if flask_session.get('rolle') != 'owner': return jsonify(...), 403`
- routes/waitlist.py:138-141 `admin_waitlist()` — GET /waitlist/admin — KEIN @login_required, nur `if flask_session.get('rolle') != 'owner': return redirect(url_for('dashboard.index'))`
- routes/changelog.py:82-85  `add_entry()` — POST /changelog/admin — KEIN @login_required, nur `if flask_session.get('rolle') != 'owner': return jsonify(...), 403`

Invite-Nebenwirkung (Fable should-include): der Superadmin-invite-Pfad erzeugt eine `Invitation`-Row UND (als Side-Effect) eine neue Organisation. Der Test muss beide tracken + im Teardown raeumen.

Blueprint-Namen (routes/CLAUDE.md): waitlist='waitlist', changelog='changelog', auth='auth', dashboard='dashboard'.
</interfaces>

<completeness_sweep>
## Vollständigkeits-Sweep (Punkt 20/22 — André-Direktive „kein viertes Loch")

**Klasse (a): schwacher `flask_session/session.get('rolle') !=/== 'owner'`-Auth-Check** (grep-verifiziert 2026-07-04, `routes/`):

| Datei:Zeile | Route | Aktuell | @login_required? | VERDIKT | Begründung |
|---|---|---|---|---|---|
| waitlist.py:98 | POST /waitlist/invite/<wid> | `!= 'owner'` → 403 | NEIN | **@login_required + @superadmin_required** | D-14 — Gratis-EA-Platz-Verschenken (Geschäftsschaden) |
| waitlist.py:140 | GET /waitlist/admin | `!= 'owner'` → redirect | NEIN | **@login_required + @superadmin_required** | D-14 — fremde Interessenten-PII |
| changelog.py:84 | POST /changelog/admin | `!= 'owner'` → 403 | NEIN | **@login_required + @superadmin_required** | André-Direktive 2026-07-04 — strukturgleiche Klasse; Changelog-Einträge sind Founder-Arbeit. (Mini-Entscheidung-Checkpoint bestätigt Scope.) |
| settings.py:63,82,105,122 | diverse settings-POST | `not in ('owner','admin')` | ja | **keep (owner/admin) — nur session→g.user in Plan 04** | behaviour-preserving; org-scoped |
| settings.py:191 | delete_account | `!= 'owner'` | ja | **keep (owner) — nur session→g.user in Plan 04** | org-eigene Aktion, kein cross-tenant-Leck |
| organisations.py:16 | _require_admin decorator | `not in ('owner','admin')` | ja | **keep (owner/admin) — nur session→g.user in Plan 04** | org-scoped Admin |

**Klasse (b): state-changing Route OHNE @login_required** (grep vollstaendig geprueft, `routes/waitlist.py` + `routes/changelog.py`):

| Datei:Zeile | Route | @login_required? | Selbst-Schutz? | VERDIKT |
|---|---|---|---|---|
| waitlist.py:10 | POST /waitlist/join | NEIN | — | **keep public** — bewusst oeffentlich (Landing-Signup) |
| waitlist.py:96 | POST /waitlist/invite/<wid> | NEIN | schwacher Owner-Check | **FIX (Klasse a)** |
| changelog.py:65 | POST /changelog/seen | NEIN | `if 'user_id' not in flask_session: return {'ok':True}` (inline Guard, VERIFIED :67) | **keep** — selbst-geschuetzt |
| changelog.py:82 | POST /changelog/admin | NEIN | schwacher Owner-Check | **FIX (Klasse a)** |

**FAZIT:** Die drei Tighten-Targets (waitlist admin+invite, changelog admin) sind die VOLLSTÄNDIGE Menge der Pattern-Klasse. Kein viertes unprotected Schreib/Admin-Loch derselben Klasse. `/waitlist/join` (oeffentlich by design) und `/changelog/seen` (inline-selbstgeschuetzt) bleiben. Die settings/profiles/organisations owner-admin-Reader werden NUR in Plan 04 mechanisch session→g.user umgestellt.
</completeness_sweep>

<verify_environment>
- `bash deploy.sh production` pytest-Gate (deploy.sh:186-194). test_waitlist_admin_auth.py auto-collected.
- Prod-Live-Zustand read-only: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'`
</verify_environment>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: [BLOCKING] Prod-Vorab-Verify — Selbst-Aussperr-Schutz (D-09/D-16)</name>
  <what-built>Noch nichts geaendert. Dieser Gate stellt VOR jedem auth-sperrenden Edit sicher, dass die Haertung André nicht selbst aussperrt (Sicht statt Zusicherung). Prior-Run in RESEARCH.md Section 1 war GRUEN — dieser Re-Check ist billig und Pflicht direkt vor dem Edit (Fable PRESERVE: Task 0 bleibt BLOCKING).</what-built>
  <how-to-verify>
    Fuehre read-only gegen Production aus:
    ```bash
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh env-keys' | grep SUPERADMIN_EMAIL
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample users 5'
    ```
    PASS-Bedingungen (ALLE, sonst STOP):
    1. `SUPERADMIN_EMAIL` erscheint in env-keys.
    2. Row fuer `andrepreuss712@gmail.com`: `is_superadmin` = t (TRUE).
    3. Selbe Row: `oauth_provider` = google.
    (passwort_hash-leer ist fuer DIESEN Fix nicht relevant — betrifft Plan-01-Fundament E3.)
  </how-to-verify>
  <resume-signal>Tippe "verified" wenn is_superadmin=t + SUPERADMIN_EMAIL vorhanden + oauth_provider=google bestaetigt. Bei Abweichung: STOP, NICHT fixen, an André eskalieren.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0b: [DECISION] changelog.py Scope-Bestätigung (Mini-Entscheidung)</name>
  <decision>POST /changelog/admin (changelog.py:84) auf @superadmin_required haerten (wie Waitlist) — Bestaetigung des Scope.</decision>
  <context>
    André hat am 2026-07-04 explizit direktivt: changelog.py:84 → @superadmin_required + @login_required, gecovered vom Guard (Owner→403). RESEARCH Open Question 1 hatte das als offene Scope-Frage markiert. Dieser Checkpoint dokumentiert die Entscheidung explizit fuer Audit-Traceability (Punkt 24).
  </context>
  <options>
    <option id="superadmin">
      <name>@login_required + @superadmin_required (André-Direktive, DEFAULT)</name>
      <pros>Strukturgleich zu Waitlist-Fix; Changelog-Eintraege sind Founder-Arbeit; schliesst „jeder Firmen-Owner schreibt Changelog"-Loch</pros>
      <cons>Leicht strenger als vorher (nur noch Superadmin statt jeder Owner)</cons>
    </option>
    <option id="login-owner">
      <name>@login_required + g.user.rolle == 'owner' (minimal)</name>
      <pros>Behaelt Owner-Scope</pros>
      <cons>Widerspricht André-Direktive; laesst das Loch teilweise offen</cons>
    </option>
  </options>
  <resume-signal>Bestaetige "superadmin" (Default, André-Direktive) oder waehle "login-owner". Bei "superadmin": weiter mit Task 1-3.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: test_waitlist_admin_auth.py — Waechter (inkl. changelog/admin Owner→403), ERST ROT gegen HEAD</name>
  <read_first>
    - tests/test_admin_dashboard_auth.py — VOLLSTAENDIGE Vorlage (cleanup_tracker, _make_org_and_user, anon→redirect / user→403 / superadmin→200) (RESEARCH Section 4.1)
    - tests/conftest.py — cleanup_rows (:238), _baseline_cleanup_guard (:549), db_from_client-Fixture
    - routes/waitlist.py:96-141 — beide Ziel-Routen (Ist: schwacher Owner-Check, kein @login_required)
    - routes/changelog.py:82-85 — add_entry (Ist: schwacher Owner-Check) — fuer den changelog/admin Owner→403-Fall (Fable should-include)
    - database/models.py — `grep -n "class Invitation" / "class Waitlist"` (Model-Namen fuer Cleanup-Tracker bestaetigen)
    - RESEARCH.md Section 4.1 + Pitfall 4 (anon bekommt aktuell 302-zum-Dashboard, nach Fix 302-zur-Login-Seite)
  </read_first>
  <behavior>
    - cleanup_tracker-Fixture: `{User: [], Organisation: [], Waitlist: [], Invitation: []}` — **Invitation MIT im Tracker** (Fable should-include: der Superadmin-invite-Pfad erzeugt eine Invitation-Row UND eine neue Org als Side-Effect → beide raeumen). Model-Namen per grep bestaetigen.
    - `_make_org_and_user(session, email, is_superadmin, tracker)` analog Vorlage.
    - GET /waitlist/admin: `test_admin_anon_redirects` (kein Login → 302), `test_admin_normal_owner_gets_403` (Owner is_superadmin=False → 403), `test_admin_superadmin_gets_200` (is_superadmin=True → 200).
    - POST /waitlist/invite/<wid>: `test_invite_owner_gets_403` (Owner → 403), `test_invite_superadmin_allowed` (Superadmin + existierender Waitlist-Eintrag → nicht-403). Der Superadmin-invite committet Invitation + eine neue Org → **beide IDs in tracker[Invitation]/tracker[Organisation] aufnehmen** und im Teardown raeumen.
    - POST /changelog/admin (Fable should-include): `test_changelog_admin_owner_gets_403` (Owner is_superadmin=False → 403 nach Fix; gegen HEAD kommt Owner durch → ROT).
    - ERST-ROT gegen HEAD: aktuell hat Owner (rolle='owner' in session) durch den schwachen Check ZUGANG → die 403-Faelle liefern gegen HEAD 200/redirect statt 403 → ROT. Nach Fix → 403 → gruen.
    - Committende Rows: cleanup_rows im Teardown (CLAUDE.md Test-Cleanup-Regel).
    - ASCII: Test-/Fixture-Namen + dict-keys ASCII.
  </behavior>
  <action>
    Erstelle `tests/test_waitlist_admin_auth.py` nach dem Muster test_admin_dashboard_auth.py. Import `from tests.conftest import cleanup_rows`, `from database.models import User, Organisation, Waitlist, Invitation` (Model-Namen per grep bestaetigen). Session setzen via `with client.session_transaction() as s: s['user_id'] = u.id` (und `s['rolle'] = 'owner'` fuer die HEAD-Rot-Reproduktion — der Fix ignoriert diesen dann).

    Faelle (siehe behavior), inklusive `test_changelog_admin_owner_gets_403` (POST /changelog/admin als Owner → 403). Fuer invite: vorab einen Waitlist-Eintrag mit status='waiting' anlegen (tracker[Waitlist].append(id)), dann `client.post(f'/waitlist/invite/{wid}')`; nach dem Superadmin-invite die erzeugte Invitation-Row + die neu erzeugte Org holen und `tracker[Invitation].append(...)` / `tracker[Organisation].append(...)`.

    SEQUENZ „ERST rot, DANN fixen": Diesen Test zuerst committen+deployen; gegen HEAD MUESSEN `test_admin_normal_owner_gets_403` + `test_changelog_admin_owner_gets_403` rot sein (Owner kommt durch). deploy.sh-Gate blockt → Rot-Beleg. DANN Task 2+3 anwenden → gruen. Rot-Beleg im SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # VOR Task 2/3: test_admin_normal_owner_gets_403 + test_changelog_admin_owner_gets_403 ROT (Gate blockt). NACH Task 2/3: alle Faelle gruen; Cleanup (inkl. Invitation + invite-Org) sauber, kein Row-Leak.</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_waitlist_admin_auth.py existiert; grep zeigt Faelle fuer anon-redirect, owner-403, superadmin-200 auf admin UND invite, PLUS `test_changelog_admin_owner_gets_403`.
    - grep zeigt `is_superadmin` + `cleanup_rows` + `Invitation` im Tracker.
    - Rot-Beleg gegen HEAD dokumentiert (Owner kommt aktuell durch → 403-Tests rot, inkl. changelog).
    - Cleanup raeumt Invitation + invite-erzeugte Org (kein Row-Leak).
  </acceptance_criteria>
  <done>Waechter existiert, gegen HEAD rot (Owner-Zugang inkl. changelog), deckt admin + invite + changelog/admin mit anon/owner/superadmin ab, Cleanup vollstaendig (inkl. Invitation + invite-Org).</done>
</task>

<task type="auto">
  <name>Task 2: routes/waitlist.py — @login_required + @superadmin_required auf beide Löcher</name>
  <read_first>
    - routes/waitlist.py:1-7 (Imports — flask_session, g bereits importiert), :96-141 (beide Ziel-Routen)
    - routes/admin_dashboard.py:11-12 — exaktes Doppel-Import-Muster
    - <interfaces> oben — login_required aus routes.auth, superadmin_required aus services.auth_decorators
  </read_first>
  <action>
    (a) Imports oben in routes/waitlist.py ergaenzen:
    ```python
    from routes.auth import login_required
    from services.auth_decorators import superadmin_required
    ```

    (b) `invite_from_waitlist` (:96): Decorator-Stack (Reihenfolge: route → login_required → superadmin_required):
    ```python
    @waitlist_bp.route('/invite/<int:wid>', methods=['POST'])
    @login_required
    @superadmin_required
    def invite_from_waitlist(wid):
    ```
    Die schwache Zeile `if flask_session.get('rolle') != 'owner': return jsonify({'error': 'Keine Berechtigung'}), 403` (:98-99) ENTFERNEN.

    (c) `admin_waitlist` (:138): analog:
    ```python
    @waitlist_bp.route('/admin')
    @login_required
    @superadmin_required
    def admin_waitlist():
    ```
    Die schwache Zeile `if flask_session.get('rolle') != 'owner': return redirect(url_for('dashboard.index'))` (:140-141) ENTFERNEN.

    KEINE UI-Aenderung (Weg a, Wurzel-Loesung — D-14). `flask_session`/`redirect`/`url_for`-Imports bleiben falls anderweitig genutzt (grep vor Entfernen; im Zweifel lassen).
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_waitlist_admin_auth.py gruen (owner→403, superadmin→200, anon→redirect). Danach Browser-Live-Test: Nicht-Superadmin-Owner /waitlist/admin → 403; Superadmin → Seite laedt.</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/waitlist.py zeigt `@login_required` + `@superadmin_required` ueber BEIDEN Funktionen.
    - grep zeigt KEIN `flask_session.get('rolle')` mehr in waitlist.py.
    - deploy.sh Gate gruen; Browser-Live-Test: Owner→403, Superadmin→Zugang.
  </acceptance_criteria>
  <done>Beide Waitlist-Routen nur noch fuer Superadmin; schwache Owner-Checks weg; kein PII-Leck / kein Gratis-EA-Verschenken.</done>
</task>

<task type="auto">
  <name>Task 3: routes/changelog.py — @login_required + @superadmin_required auf add_entry</name>
  <read_first>
    - routes/changelog.py:1-6 (Imports — flask_session, g importiert; login_required/superadmin_required NICHT), :82-99 (add_entry)
    - Task 0b Entscheidung (DEFAULT: superadmin)
    - RESEARCH Pitfall 3 (changelog hat kein @login_required → nach g.user-Nutzung AttributeError fuer anon → @login_required MUSS mit dazu)
  </read_first>
  <action>
    (a) Imports oben in routes/changelog.py ergaenzen:
    ```python
    from routes.auth import login_required
    from services.auth_decorators import superadmin_required
    ```

    (b) `add_entry` (:82): Decorator-Stack:
    ```python
    @changelog_bp.route('/admin', methods=['POST'])
    @login_required
    @superadmin_required
    def add_entry():
    ```
    Die schwache Zeile `if flask_session.get('rolle') != 'owner': return jsonify({'error': 'Keine Berechtigung'}), 403` (:84-85) ENTFERNEN.

    Damit ist changelog.py:84 aus dem Rollen-Sweep (Plan 04) heraus — es liest keine session-Rolle mehr, sondern nutzt @superadmin_required. Plan 04 muss changelog.py:84 NICHT mehr anfassen (Notiz an Plan 04). @login_required verhindert den AttributeError-Pfad (Pitfall 3).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen. Browser-Live-Test: Nicht-Superadmin POST /changelog/admin → 403; anon → Redirect zur Login-Seite (kein AttributeError/500).</automated>
  </verify>
  <acceptance_criteria>
    - grep von routes/changelog.py zeigt `@login_required` + `@superadmin_required` ueber add_entry.
    - grep zeigt KEIN `flask_session.get('rolle')` mehr in changelog.py.
    - deploy.sh Gate gruen; anon-Zugriff auf /changelog/admin → Redirect (kein 500/AttributeError).
  </acceptance_criteria>
  <done>POST /changelog/admin nur noch Superadmin, mit @login_required (kein anon-AttributeError); schwacher Owner-Check weg.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN) — @login_required liest users-Row und setzt g.user; @superadmin_required liest `g.user.is_superadmin`. Bestehende Spalten, KEINE Schema-Aenderung.
- `organisations`, `waitlist`, `invitations` (schreiben, nur im Test via Superadmin-invite-Pfad) — bestehende Tabellen, keine neuen Felder. Test-Cleanup raeumt Invitation + invite-Org.

### inspect.sh-Beleg (Prod, VERIFIED RESEARCH Section 1, 2026-07-04)
`inspect.sh columns users`: `is_superadmin` boolean NOT NULL. `inspect.sh sample users 5`: andrepreuss712@gmail.com → is_superadmin=t. Selbst-Aussperr-Schutz (Task 0).

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Leser laeuft voraus? | Verifiziert? |
|---|---|---|---|---|---|
| g.user.is_superadmin | gelesen in superadmin_required | DB-Spalte users.is_superadmin | synchron (login_required setzt g.user pro Request) | Nein | ✓ inspect.sh columns/sample |
| g.user (Objekt) | gesetzt in login_required (auth.py:55) | DB-Spalte users.* | synchron pro Request | Nein | ✓ auth.py:48-55 |
| Invitation-Row (Test) | Superadmin-invite committet | DB-Tabelle invitations (bestehend) | synchron im Test-Request | N/A | ✓ Test-Cleanup raeumt |

Kein async Schreib/Lese-Versatz (Punkt 26): g.user synchron in before_request/login_required gesetzt.
</persistenz_schicht_verifikation>

<verbindungs_karte>
## Verbindungs-Karte (Punkt 22)

- **superadmin_required** (services/auth_decorators.py:5) — 1 Definition; Nutzer: admin_dashboard.py, admin_ewb.py, admin_views.py, NEU: waitlist.py + changelog.py. LEBT. Kein neuer Auth-Code.
- **login_required** (routes/auth.py:43) — 1 Definition; importiert von 18 route-Modulen. LEBT. NEU: waitlist.py + changelog.py.
- **is_superadmin** (database/models.py, users) — geschrieben vom SUPERADMIN_EMAIL-Seed (auth.py:137-139); gelesen von superadmin_required. LEBT + auf Prod TRUE fuer André.
- Beweis „kein viertes Loch": completeness_sweep oben.
</verbindungs_karte>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eingeloggter Firmen-Owner (fremde Org) → /waitlist/admin, /waitlist/invite, /changelog/admin | legitim eingeloggt, aber nicht-Superadmin → Founder-Funktionen |
| anonymer Browser → dieselben Routen | unauthenticated Zugriff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-07 | Information Disclosure (fremde Interessenten-PII) | GET /waitlist/admin | mitigate | @login_required + @superadmin_required; Owner→403 (D-14); Waechter beweist |
| T-AUTH1-08 | Spoofing/Elevation (Gratis-EA-Platz verschenken) | POST /waitlist/invite/<wid> | mitigate | @login_required + @superadmin_required; kein Owner kann fremde einladen (D-14) |
| T-AUTH1-09 | Elevation (jeder Firmen-Owner schreibt Changelog) | POST /changelog/admin | mitigate | @login_required + @superadmin_required (André-Direktive); Guard Owner→403 |
| T-AUTH1-10 | Denial of Service (AttributeError/500 fuer anon nach g.user-Nutzung) | changelog add_entry | mitigate | @login_required VOR superadmin_required → anon redirected (Pitfall 3) |
| T-AUTH1-11 | Denial of Service (Selbst-Aussperrung des Founders) | Superadmin-Zugang | mitigate | BLOCKING Prod-Vorab-Verify Task 0 (is_superadmin=t bewiesen); OAuth-Zugang intakt |
</threat_model>

<verification>
- Task 0 Prod-Vorab: is_superadmin=t + SUPERADMIN_EMAIL vorhanden + oauth_provider=google (BLOCKING).
- Task 0b Scope-Entscheidung dokumentiert (DEFAULT superadmin).
- test_waitlist_admin_auth.py: rot gegen HEAD (Owner-Zugang inkl. changelog), gruen nach Fix; Cleanup inkl. Invitation + invite-Org sauber.
- Browser-Live-Test: Nicht-Superadmin-Owner → 403 auf allen drei; Superadmin → Zugang; anon → Redirect zur Login-Seite.
- grep: kein `flask_session.get('rolle')` mehr in waitlist.py + changelog.py.
</verification>

<success_criteria>
- Drei Löcher (waitlist admin+invite, changelog admin) auf @login_required + @superadmin_required gehärtet.
- Vollständigkeits-Tabelle dokumentiert; kein viertes Loch derselben Klasse offen.
- Waechter im Deploy-Gate, ERST-ROT bewiesen, inkl. changelog/admin Owner→403; Cleanup vollstaendig (Invitation + invite-Org).
- Founder-Zugang intakt (Prod-Vorab gruen).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-03-SUMMARY.md` (inkl. Task-0-Prod-Beleg, Task-0b-Entscheidung, Waechter-Rot-Beleg inkl. changelog-Fall, Browser-Live-Test, Notiz an Plan 04: changelog.py:84 ist erledigt/nicht mehr anfassen).
</output>
