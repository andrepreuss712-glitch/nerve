---
phase: 08.23.2.AUTH-1
plan: 04
type: execute
wave: 4
complexity: "🟡 mittel (Auth/Security — Cross-AI Fable erfolgt; dieser Replan IST die Incorporation)"
depends_on: [03]   # Plan 03 haertet changelog.py:84 + waitlist.py:98/:140 auf Decorator → sie fallen aus diesem Sweep raus. Strikte Serialisierung 01→02→03→04 (Fable BLOCKER 4). ACHTUNG: Plan 03 ist autonomous:false (BLOCKING-Checkpoints) — siehe <dependency_note>.
files_modified:
  - routes/profiles.py
  - routes/organisations.py
  - routes/dashboard.py
  - routes/logs_routes.py
  - routes/settings.py
  - templates/base.html
  - templates/settings.html
  - templates/profiles_list.html
  - templates/logs_page.html
  - tests/test_rolle_source_sweep.py
autonomous: true
requirements: []   # Scope = CONTEXT-Decisions D-10, D-11, D-12

must_haves:
  truths:
    - "Alle Rollen-Leser (Routes + Templates) lesen g.user.rolle (frisch aus DB pro Request) statt der Session-Merk-Notiz"
    - "Eine Rollen-Änderung (Owner macht Mitarbeiter zum Admin) greift SOFORT — nicht erst nach Neu-Anmeldung"
    - "Die Setzstelle auth.py:130 (session['rolle'] = user_rolle) bleibt unverändert (D-12) — nur Leser umgestellt"
    - "Kein Halb-Zustand: der Sweep ist vollständig über Server-Schranken UND Menü-Sichtbarkeit (D-11)"
    - "Der _rolle()-Helper in profiles.py ist einzeilig umgestellt → alle 18 Call-Sites gefixt"
    - "base.html:79 nutzt den None-Guard `g.user and g.user.rolle in (...)` — kein 500 auf oeffentlichen Seiten (/impressum, /agb, /datenschutz, /changelog), wo g.user None ist"
    - "Der Reader-Waechter scannt routes/** UND templates/** — ein zukuenftiger session.get('rolle')-Reader in einem Template wird ebenfalls ROT gemeldet"
  artifacts:
    - path: "routes/profiles.py"
      provides: "_rolle()-Helper liest g.user.rolle"
      contains: "g.user.rolle"
    - path: "templates/base.html"
      provides: "base.html:79 auf `g.user and g.user.rolle in (...)` (None-Guard fuer public pages)"
      contains: "g.user and"
    - path: "tests/test_rolle_source_sweep.py"
      provides: "Statischer Waechter: kein flask_session/session.get('rolle')-Reader mehr in routes/** UND templates/** (ausser Setter auth.py:130)"
      contains: "rolle"
  key_links:
    - from: "routes/profiles.py _rolle()"
      to: "g.user.rolle"
      via: "einzeilige Helper-Body-Änderung"
      pattern: "g\\.user\\.rolle"
---

<objective>
Alle `session['rolle']` / `flask_session.get('rolle')`-Leser werden auf `g.user.rolle` umgestellt (frisch aus DB pro Request) — damit eine Rollen-Änderung SOFORT greift statt erst nach Neu-Anmeldung (B-8). Vollständiger Sweep über Server-Schranken UND Menü-Sichtbarkeit (D-11, kein Halb-Zustand). Die Setzstelle auth.py:130 bleibt (D-12) — nur die Leser wandern. **Kritischer Fable-Fix (BLOCKER 2):** base.html:79 bekommt den `g.user and`-None-Guard, weil base.html auf PUBLIC-Seiten (/impressum, /agb, /datenschutz, /changelog) gerendert wird, wo `g.user` None ist → sonst Jinja UndefinedError → **500** (Impressum-500 = zusaetzlich Rechtsproblem).

Purpose: Stale-Role-Session-Klasse (Elevation of Privilege durch verzoegerte Rollen-Wirkung) schliessen; einheitliche frische Quelle g.user.rolle — ohne 500 auf oeffentlichen Seiten.
Output: 5 Route-Dateien + 4 Templates auf g.user.rolle (base.html:79 mit None-Guard), plus ein statischer Waechter test_rolle_source_sweep.py (scannt routes/** UND templates/**).

Komplexität: 🟡 mittel — Auth/Security. Cross-AI (Fable) mit diesem Replan eingearbeitet. NICHT direkt executen.

**HINWEIS (Abhängigkeit zu Plan 03):** waitlist.py:98/:140 und changelog.py:84 werden von Plan 03 auf @superadmin_required umgestellt → sie sind in DIESEM Plan NICHT mehr enthalten. Vor Execute grep-bestaetigen, dass Plan 03 gelaufen ist.
</objective>

<dependency_note>
## Abhängigkeit zu Plan 03 (Checkpoint-Blockade — erwartetes Verhalten) + Serialisierung (Fable BLOCKER 4)

Strikte Wellen-Serialisierung 01→02→03→04 (Fable BLOCKER 4): die Plaene teilen das serielle Prod-Deploy-Gate + ueberlappende ERST-ROT-Fenster → ein rotes Gate muss attribuierbar bleiben. Dieser Plan hat `depends_on: [03]` / `wave: 4`, und **Plan 03 ist `autonomous: false`** (BLOCKING-Checkpoints: Task 0 Prod-Vorab-Verify + Mini-Entscheidung). Konsequenz:

- **Wenn Plan 03 Task 0 fehlschlaegt**, HAELT Plan 03 an einem Checkpoint an. Plan 04 darf dann NICHT starten — seine Grundannahme (waitlist.py/changelog.py bereits per @superadmin_required aus dem Rolle-Reader-Sweep raus) ist nicht erfuellt.
- **Das ist erwartetes Verhalten — kein Bug.** Der Executor soll die Blockade SICHTBAR an André melden, NICHT still auf Plan 04 warten oder es „trotzdem" anlaufen lassen.
- **Vor Plan-04-Start Pflicht-Gate:** `grep -n "get('rolle'" routes/waitlist.py routes/changelog.py` → 0 Reader; Plan-03-SUMMARY existiert. Fehlt der Beleg → STOP.
</dependency_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-FABLE-REVIEW.md

<interfaces>
<!-- g.user in before_request (auth.py:48-55, VERIFIED): g.user = user (frische DB-Row). -->
<!-- g.user.rolle: database/models.py:72 Column(String(50), default='member'). -->
<!-- Flask injiziert g automatisch in Jinja-Kontext → {{ g.user.rolle }} funktioniert (RESEARCH A1). -->
<!-- ABER: auf PUBLIC-Seiten (base.html gerendert ohne Login) ist g.user None → None-Guard noetig (Fable BLOCKER 2). base.html guardet an :58/:98 heute schon so (`g.user and ...`). -->

Vollständige Reader-Liste (RESEARCH Section 2, grep-verifiziert 2026-07-04) — NACH Plan-03-Abzug:

Python-Routes (umstellen auf g.user.rolle):
| Datei:Zeile | Ist | Neu |
|---|---|---|
| profiles.py:36 | `def _rolle(): return flask_session.get('rolle', 'member')` (Helper, 18 Call-Sites) | `return g.user.rolle` (EINE Zeile → alle 18) |
| organisations.py:16 | `if session.get('rolle') not in ('owner','admin'):` (_require_admin) | `g.user.rolle not in ('owner','admin')` |
| dashboard.py:526 | `..., flask_session.get('rolle','member'))` | `..., g.user.rolle)` |
| logs_routes.py:14 | `rolle = flask_session.get('rolle','member')` (liste) | `rolle = g.user.rolle` |
| logs_routes.py:36 | `rolle = flask_session.get('rolle','member')` (download) | `rolle = g.user.rolle` |
| settings.py:63 | `if flask_session.get('rolle') not in ('owner','admin'):` | `g.user.rolle not in ('owner','admin')` |
| settings.py:82 | dito | dito |
| settings.py:105 | dito | dito |
| settings.py:122 | dito | dito |
| settings.py:191 | `if flask_session.get('rolle') != 'owner':` | `g.user.rolle != 'owner'` |

SETTER bleibt (D-12): auth.py:130 `session['rolle'] = user_rolle` — NICHT anfassen.
organisations.py:96 `user.rolle != 'owner'` liest bereits fresh user.rolle → schon korrekt, NICHT anfassen (nur verifizieren).

Templates (umstellen auf g.user.rolle):
| Datei:Zeile | None-Guard? |
|---|---|
| base.html:79 | **JA — `g.user and g.user.rolle in (...)`** (Fable BLOCKER 2: base.html rendert auf public /impressum/agb/datenschutz/changelog → g.user None → sonst 500). Spiegelt bestehenden Guard-Stil base.html:58/:98. |
| settings.html:115, :236 | Nein noetig — login-only Route (g.user nie None). Explizit als Kommentar vermerken. |
| profiles_list.html:30, :69, :81 | Nein noetig — login-only. |
| logs_page.html:25 | Nein noetig — login-only. |
</interfaces>

<verify_environment>
- `bash deploy.sh production` pytest-Gate (deploy.sh:186-194); test_rolle_source_sweep.py auto-collected.
- Live read-only: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'`
- Browser-Live-Test: Rollen-Menü-Sichtbarkeit + Admin-Schranken mit Test-User PLUS oeffentliche Seiten (/impressum, /agb, /datenschutz, /changelog) → kein 500.
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_rolle_source_sweep.py — statischer Waechter (routes/** UND templates/**), ERST ROT gegen HEAD</name>
  <read_first>
    - tests/test_no_live_global_state.py — Verbotenes-Muster-Sweep + frozenset-Whitelist (Vorlage)
    - RESEARCH Section 2 — vollstaendige Reader-Liste + Setter-Ausnahme auth.py:130
    - FABLE-REVIEW Pruefpunkt 4 (Ratchet-Luecke: Waechter muss AUCH templates/ scannen, sonst schluepft ein zukuenftiger Template-`session.get('rolle')`-Regression durch)
    - CLAUDE.md „Test-Qualitaets-Regel" — statischer Reader-Sweep ist Grenzfall (Grund als Kommentar)
  </read_first>
  <behavior>
    - `test_no_session_rolle_readers`: Sweep ueber `routes/*.py` **UND** `templates/**` (rglob '*.html') — Fable-Erweiterung. Verbotenes Muster: `flask_session.get('rolle'` / `session.get('rolle'` (Python) bzw. `session.get('rolle'` / `session['rolle']` als Lese-Zugriff (Jinja). Whitelist (frozenset): `routes/auth.py` (Setzstelle :130, D-12). Jeder Nicht-Whitelist-Treffer → Verletzung mit datei:zeile.
    - Gegen HEAD (vor Task 2/3): profiles.py:36, organisations.py:16, dashboard.py:526, logs_routes.py:14/:36, settings.py (5×) + die 4 Templates sind noch Reader → Waechter ROT. Nach Umstellung → gruen.
    - Achtung Setter-Abgrenzung (Python): `session['rolle'] =` (Zuweisung, auth.py:130) darf NICHT als Reader gematcht werden — Python-Muster gezielt auf `.get('rolle'` beschraenken. (Bei Templates ist `session['rolle']` als LESE-Ausdruck moeglich → dort auch matchen; auth.py ist eine .py-Datei, kollidiert nicht mit dem Template-Muster.)
    - Whitelist als frozenset; ASCII-Identifier.
  </behavior>
  <action>
    Erstelle `tests/test_rolle_source_sweep.py`. `_ROOT = Path(__file__).parent.parent`. `_ROLLE_READER_WHITELIST = frozenset({'routes/auth.py'})` (Setzstelle D-12).

    Python-Reader-Regex: `r"(flask_session|session)\.get\(\s*['\"]rolle['\"]"`.
    Template-Reader-Regex: `r"session(\.get\(\s*['\"]rolle['\"]|\[\s*['\"]rolle['\"]\s*\])"` (deckt `session.get('rolle')` UND `session['rolle']` als Jinja-Lese-Ausdruck).

    Sweep: `py_files = list((_ROOT/'routes').glob('*.py'))`; `tpl_files = list((_ROOT/'templates').rglob('*.html'))`. Pro Datei Treffer (datei:zeile) sammeln; Whitelist-Dateien (relativer Pfad in `_ROLLE_READER_WHITELIST`) ueberspringen. `assert not violations, "session-Rolle-Leser (nutze g.user.rolle):\n  " + "\n  ".join(violations)`.

    File-Kopf-Kommentar: Grund (Runtime-Constraint „Rolle IMMER frisch aus DB, nie aus Session gelesen" — routes UND templates; kein Function-Mock testet den ganzen Sweep — CLAUDE.md Grenzfall).

    SEQUENZ „ERST rot, DANN fixen": Test zuerst committen+deployen → gegen HEAD ROT (10 Python-Reader + 4 Template-Reader) → deploy.sh-Gate blockt → Rot-Beleg. DANN Task 2+3 → gruen. Rot-Beleg im SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # VOR Task 2/3: ROT (Python- + Template-Reader). NACH Task 2/3: gruen (nur auth.py:130 Setter whitelisted).</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_rolle_source_sweep.py existiert; grep zeigt Whitelist-frozenset mit routes/auth.py.
    - grep zeigt Scan ueber `routes` (glob '*.py') UND `templates` (rglob '*.html') — Fable-Erweiterung.
    - Python-Regex matcht `.get('rolle'`-Reader, NICHT `['rolle']`-Zuweisung (Setter bleibt unerkannt); Template-Regex matcht `session.get('rolle')` UND `session['rolle']`.
    - Rot-Beleg gegen HEAD dokumentiert (Gate blockt an den Readern in routes UND templates).
  </acceptance_criteria>
  <done>Reader-Waechter existiert, scannt routes/** UND templates/**, gegen HEAD rot, whitelisted nur den Setter.</done>
</task>

<task type="auto">
  <name>Task 2: Routes umstellen — profiles/organisations/dashboard/logs_routes/settings auf g.user.rolle</name>
  <read_first>
    - <interfaces> oben — exakte Datei:Zeile-Liste
    - routes/profiles.py:35-36 (Helper) + die 18 Call-Sites (nur Helper aendern)
    - routes/organisations.py:16 (_require_admin) + :96 (schon korrekt, nur verifizieren)
    - routes/dashboard.py:526, routes/logs_routes.py:14/:36, routes/settings.py:63/:82/:105/:122/:191
    - RESEARCH Pitfall 1 (Helper-Umfang: 1 Zeile → 18 Caller), Pitfall 5 (settings.py:191 delete_account)
    - Pruefen dass `from flask import g` in jeder Datei importiert ist (falls nicht: ergaenzen)
  </read_first>
  <action>
    Pflicht-grep VOR jedem Edit (Punkt 20): `grep -n "get('rolle'" routes/<datei>.py`. Dann pro Datei:

    - **profiles.py:36**: `def _rolle(): return flask_session.get('rolle', 'member')` → `return g.user.rolle`. (`from flask import g` sicherstellen.) Fixt alle 18 Call-Sites — Caller NICHT anfassen.
    - **organisations.py:16**: `if session.get('rolle') not in ('owner', 'admin'):` → `if g.user.rolle not in ('owner', 'admin'):`. organisations.py:96 NUR verifizieren.
    - **dashboard.py:526**: dritter Arg `flask_session.get('rolle', 'member')` → `g.user.rolle`.
    - **logs_routes.py:14 + :36**: `rolle = flask_session.get('rolle', 'member')` → `rolle = g.user.rolle`.
    - **settings.py:63/:82/:105/:122**: `flask_session.get('rolle') not in ('owner', 'admin')` → `g.user.rolle not in ('owner', 'admin')`. **settings.py:191**: `flask_session.get('rolle') != 'owner'` → `g.user.rolle != 'owner'`.

    Alle diese Routen sind @login_required (g.user gesetzt) → kein None-Risiko. `flask_session`-Import lassen falls anderweitig genutzt (grep). KEINE Verhaltensaenderung an der Allow-Menge (behaviour-preserving).
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_rolle_source_sweep.py Python-Seite gruen. Browser-Live-Test: Admin-Schranken (settings/organisations/logs) identisch; Rollen-Aenderung greift ohne Re-Login.</automated>
  </verify>
  <acceptance_criteria>
    - grep zeigt KEIN `.get('rolle'` mehr in profiles.py, organisations.py, dashboard.py, logs_routes.py, settings.py.
    - grep zeigt `g.user.rolle` in jeder der 5 Dateien.
    - organisations.py:96 unveraendert (nur verifiziert).
    - deploy.sh Gate gruen; Admin-Schranken verhalten sich identisch.
  </acceptance_criteria>
  <done>Alle Python-Rollen-Leser lesen g.user.rolle; _rolle()-Helper einzeilig umgestellt; Verhalten identisch.</done>
</task>

<task type="auto">
  <name>Task 3: Templates umstellen — base(mit None-Guard)/settings/profiles_list/logs_page auf g.user.rolle</name>
  <read_first>
    - templates/base.html:79 + die bestehenden Guards base.html:58/:98 (`g.user and ...` — Stil-Vorlage fuer BLOCKER 2)
    - templates/settings.html:115/:236, templates/profiles_list.html:30/:69/:81, templates/logs_page.html:25
    - RESEARCH Section 2 Template-Teil + A1 (g im Jinja-Kontext)
    - FABLE-REVIEW BLOCKER 2 (base.html:79 braucht `g.user and`-Guard — public pages, sonst 500)
    - CLAUDE.md Punkt 14 Schicht 4 (Edge-Case None-Guard-Kommentar) + Umlaute-Regel (Jinja-Expression = Code-Identifier → g.user.rolle ASCII)
  </read_first>
  <action>
    Pflicht-grep VOR Edit: `grep -n "rolle" templates/<datei>.html` (Anti-Drift). Pro Template die Jinja-Rolle-Leser von `session.get('rolle')` / `session['rolle']` auf `g.user.rolle` umstellen:

    - **base.html:79** — **MIT None-Guard (BLOCKER 2)**: `{% if g.user and g.user.rolle in ('owner', 'admin') %}` (gleiche Allow-Menge wie der Ist-Check; `g.user and`-Vorsatz spiegelt base.html:58/:98). Grund: base.html rendert auf PUBLIC-Seiten (/impressum, /agb, /datenschutz, /changelog) ohne g.user → ohne den Guard Jinja UndefinedError → 500 (Impressum-500 = Rechtsproblem).
    - **settings.html:115, :236** — `{% if g.user.rolle in (...) %}` (login-only, kein None-Guard noetig).
    - **profiles_list.html:30, :69, :81** — dito (login-only).
    - **logs_page.html:25** — dito (login-only).

    Setze ueber base.html:79 den Kommentar: `{# g.user and — base.html rendert auf public Seiten (impressum/agb/datenschutz/changelog), g.user ist dort None (Fable BLOCKER 2, spiegelt :58/:98) #}`. Ueber die anderen drei je ein kurzer Kommentar: `{# g.user hier nie None — Route ist @login_required (CLAUDE.md Punkt 14 Schicht 4) #}`. KEINE Aenderung der User-facing Menü-Texte (Umlaute erhalten). Allow-Menge unveraendert.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen. Browser-Live-Test: (a) Menü-Eintraege (owner/admin-only) identisch fuer Owner/Admin, fehlen fuer member; (b) PUBLIC-Seiten /impressum, /agb, /datenschutz, /changelog laden OHNE 500 (g.user None → Guard greift).</automated>
  </verify>
  <acceptance_criteria>
    - grep zeigt KEIN `session.get('rolle')` / `session['rolle']` mehr in den 4 Templates.
    - grep zeigt `g.user.rolle` in base.html, settings.html, profiles_list.html, logs_page.html.
    - **base.html enthaelt den `g.user and `-Guard** (grep `grep -n "g.user and" templates/base.html` liefert die :79-Zeile) — Fable-Acceptance BLOCKER 2.
    - Je Template ein None-Guard-/Kommentar-Vermerk vorhanden.
    - Browser-Live-Test: Menü-Sichtbarkeit identisch pro Rolle; PUBLIC-Seiten kein 500.
  </acceptance_criteria>
  <done>Alle Template-Rollen-Leser nutzen g.user.rolle; base.html:79 mit `g.user and`-None-Guard (kein Public-500); Menü-Sichtbarkeit identisch; Edge dokumentiert.</done>
</task>

</tasks>

<deferred_registration>
## Bewusster Defer — session['is_coach'] (Fable Pruefpunkt 4, should-include)

`session['is_coach']` (coach.py:17, base.html:82) hat die IDENTISCHE Stale-Session-Schwaeche wie `session['rolle']` (wirkt erst nach Neu-Anmeldung; eine Coach-Rechte-Aenderung greift verzoegert). **Bewusster DEFER, NICHT in AUTH-1:** André scopte AUTH-1 explizit nur auf `rolle`. Hier NICHT mit-sweepen — nur registrieren, damit es keine spaetere Ueberraschung ist. Der Reader-Waechter (Task 1) matcht bewusst NUR `rolle`, nicht `is_coach`. Kandidat fuer eine spaetere Rollen-/Coach-Haertungsphase (analog D-10 fuer is_coach).
</deferred_registration>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN, Spalte rolle) — g.user.rolle liest die frische users-Row (via login_required/before_request, auth.py:48-55). Bestehende Spalte, KEINE Schema-Aenderung.

### inspect.sh-Beleg (Prod)
`inspect.sh columns users` (VERIFIED): `rolle` character varying(50), default 'member' (database/models.py:72). Existiert und befuellt (owner/admin/member).

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Leser laeuft voraus? | Verifiziert? |
|---|---|---|---|---|---|
| g.user.rolle | gelesen in Routes + Templates | DB-Spalte users.rolle (frisch pro Request) | synchron: login_required setzt g.user aus DB VOR Route-Body/Template-Render | Nein — synchron request-scope | ✓ inspect.sh columns + auth.py:48-55 |
| session['rolle'] | NICHT mehr gelesen (nur noch auth.py:130 setzt) | Flask-Session-Cookie | beim Login | — (kein Leser mehr) | ✓ Waechter Task 1 beweist 0 Leser (routes + templates) |

Punkt 26 (async-Naht): NICHT anwendbar — g.user.rolle synchron request-scoped. Der Kernvorteil IST gerade die Frische (kein Stale-Session-Versatz).
</persistenz_schicht_verifikation>

<verbindungs_karte>
## Verbindungs-Karte (Punkt 22)

- **Setter (LEBT, bleibt):** auth.py:130 `session['rolle'] = user_rolle` — einzige Schreibstelle (D-12). Nach dem Sweep von 0 Routen/Templates gelesen (Waechter Task 1 beweist).
- **Neue Quelle (LEBT):** g.user (auth.py:55) → g.user.rolle (models.py:72). Frisch aus DB pro Request.
- **Reader-Migration vollstaendig (Punkt 28/29):** 10 Python-Reader + 4 Templates (base.html:79 mit None-Guard). waitlist/changelog bereits via Plan 03 raus. Waechter Task 1 erzwingt 0 verbleibende session-Reader (routes + templates, ausser Setter-Whitelist).
- **Bereits korrekt (nicht angefasst):** organisations.py:96 `user.rolle` (fresh), auth.py:130 (Setter).
- **Bewusst NICHT angefasst (Defer):** session['is_coach'] (coach.py:17, base.html:82) — registriert, out of AUTH-1 scope.
</verbindungs_karte>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eingeloggter User mit kuerzlich geaenderter Rolle → Admin-Schranken + Menü | Session-Rolle koennte stale sein |
| anonymer Browser → PUBLIC-Seiten (impressum/agb/datenschutz/changelog) rendern base.html | g.user ist None → Rolle-Check darf nicht crashen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-12 | Elevation of Privilege (Stale-Role-Session) | alle rolle-Reader (Routes + Templates) | mitigate | g.user.rolle frisch aus DB pro Request (D-10); Waechter erzwingt Vollstaendigkeit (routes + templates); Setter auth.py:130 harmlos |
| T-AUTH1-13 | Tampering (Halb-Migration — neuer Code kopiert alt-Muster) | zukuenftige route/template-Edits | mitigate | test_rolle_source_sweep blockt jeden neuen session-Rolle-Reader in routes/** UND templates/** (Punkt 29) |
| T-AUTH1-14 | Denial of Service (g.user None auf public Seite → 500) | base.html:79 | mitigate | `g.user and g.user.rolle in (...)` None-Guard (Fable BLOCKER 2); spiegelt base.html:58/:98; Browser-Test /impressum etc. |
</threat_model>

<verification>
- test_rolle_source_sweep.py: rot gegen HEAD (10 Python- + 4 Template-Reader), gruen nach Umstellung (nur auth.py:130 whitelisted).
- grep: 0 `.get('rolle'`-Reader in routes/ ausser auth.py; 0 `session.get('rolle')`/`session['rolle']` in den 4 Templates.
- base.html:79 hat den `g.user and`-Guard (grep-belegt).
- Browser-Live-Test: Admin-Schranken + Menü-Sichtbarkeit identisch pro Rolle; Rollen-Aenderung greift ohne Re-Login; PUBLIC-Seiten (impressum/agb/datenschutz/changelog) laden ohne 500.
- organisations.py:96 unveraendert verifiziert.
- session['is_coach'] bewusst NICHT umgestellt (registrierter Defer).
</verification>

<success_criteria>
- Vollstaendiger Sweep (Routes + Templates) auf g.user.rolle; kein Halb-Zustand.
- base.html:79 mit `g.user and`-None-Guard → kein 500 auf public Seiten (BLOCKER 2 geschlossen).
- Setzstelle auth.py:130 unveraendert.
- Waechter im Deploy-Gate (routes/** UND templates/**), ERST-ROT bewiesen; blockt zukuenftige session-Rolle-Reader in beiden Baeumen.
- Verhalten (Allow-Mengen, Menü-Sichtbarkeit) identisch; Rollen-Aenderung greift sofort.
- session['is_coach']-Defer registriert.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-04-SUMMARY.md` (inkl. Waechter-Rot-Beleg fuer routes UND templates, grep-0-Belege, base.html `g.user and`-Beleg, Browser-Live-Test der Menü-Sichtbarkeit + public-Seiten-kein-500 + sofort-greifender Rollen-Aenderung, is_coach-Defer-Notiz).
</output>
