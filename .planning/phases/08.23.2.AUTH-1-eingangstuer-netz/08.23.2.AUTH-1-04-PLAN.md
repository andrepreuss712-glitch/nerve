---
phase: 08.23.2.AUTH-1
plan: 04
type: execute
wave: 2
complexity: "🟡 mittel (Auth/Security — Cross-AI Gemini PFLICHT vor Execute)"
depends_on: [03]   # Plan 03 härtet changelog.py:84 + waitlist.py:98/:140 auf Decorator → sie fallen aus diesem Sweep raus; sequenziert nach Plan 03 damit dessen Guard nicht die CSRF-Arbeit maskiert. ACHTUNG: Plan 03 ist autonomous:false (BLOCKING-Checkpoints) — siehe <dependency_note> unten.
files_modified:
  - routes/profiles.py
  - routes/organisations.py
  - routes/dashboard.py
  - routes/logs_routes.py
  - routes/settings.py
  - templates/base.html
  - templates/settings.html
  - templates/profiles_list.html
  - templates/logs_page.html
  - tests/test_rolle_source_sweep.py
autonomous: true
requirements: []   # Scope = CONTEXT-Decisions D-10, D-11, D-12 (+ D-09 re-verify aus Plan 03 Task 0)

must_haves:
  truths:
    - "Alle Rollen-Leser (Routes + Templates) lesen g.user.rolle (frisch aus DB pro Request) statt der Session-Merk-Notiz"
    - "Eine Rollen-Änderung (Owner macht Mitarbeiter zum Admin) greift SOFORT — nicht erst nach Neu-Anmeldung"
    - "Die Setzstelle auth.py:130 (session['rolle'] = user_rolle) bleibt unverändert (D-12) — nur Leser umgestellt"
    - "Kein Halb-Zustand: der Sweep ist vollständig über Server-Schranken UND Menü-Sichtbarkeit (D-11)"
    - "Der _rolle()-Helper in profiles.py ist einzeilig umgestellt → alle 18 Call-Sites gefixt"
    - "Betroffene Templates rendern nur hinter @login_required (g.user nie None); ein None-Guard-Kommentar dokumentiert den Edge-Case (Punkt 14 Schicht 4)"
  artifacts:
    - path: "routes/profiles.py"
      provides: "_rolle()-Helper liest g.user.rolle"
      contains: "g.user.rolle"
    - path: "routes/settings.py"
      provides: "5 Reader auf g.user.rolle umgestellt"
      contains: "g.user.rolle"
    - path: "tests/test_rolle_source_sweep.py"
      provides: "Statischer Wächter: kein flask_session/session.get('rolle')-Reader mehr in routes/ (außer Setter auth.py:130)"
      contains: "rolle"
  key_links:
    - from: "routes/profiles.py _rolle()"
      to: "g.user.rolle"
      via: "einzeilige Helper-Body-Änderung"
      pattern: "g\\.user\\.rolle"
---

<objective>
Alle `session['rolle']` / `flask_session.get('rolle')`-Leser werden auf `g.user.rolle` umgestellt (frisch aus DB pro Request) — damit eine Rollen-Änderung (z.B. Owner macht Mitarbeiter zum Admin) SOFORT greift statt erst nach Neu-Anmeldung (B-8). Vollständiger Sweep über Server-Schranken UND Menü-Sichtbarkeit (D-11, kein Halb-Zustand). Die Setzstelle auth.py:130 bleibt (D-12) — nur die Leser wandern.

Purpose: Stale-Role-Session-Klasse (Elevation of Privilege durch verzögerte Rollen-Wirkung) schließen; einheitliche frische Quelle g.user.rolle.
Output: 5 Route-Dateien + 4 Templates auf g.user.rolle, plus ein statischer Wächter test_rolle_source_sweep.py.

Komplexität: 🟡 mittel — Auth/Security → Cross-AI Gemini PFLICHT vor Execute. NICHT direkt executen.

**HINWEIS (Abhängigkeit zu Plan 03):** waitlist.py:98/:140 und changelog.py:84 werden von Plan 03 auf @superadmin_required umgestellt (kein session-Rolle-Reader mehr) → sie sind in DIESEM Plan NICHT mehr enthalten. Vor Execute grep-bestätigen, dass Plan 03 gelaufen ist (kein flask_session.get('rolle') mehr in waitlist.py/changelog.py).
</objective>

<dependency_note>
## Abhängigkeit zu Plan 03 (Checkpoint-Blockade — erwartetes Verhalten)

Dieser Plan hat `depends_on: [03]`, und **Plan 03 ist `autonomous: false`** — er enthält BLOCKING-Checkpoints (Task 0 „Prod-Vorab-Verify" + eine Mini-Entscheidung). Konsequenz für den Executor:

- **Wenn Plan 03 Task 0 (Prod-Vorab-Verify) fehlschlägt**, HÄLT Plan 03 an einem Checkpoint an und läuft NICHT durch. Plan 04 darf dann NICHT starten — seine Grundannahme (waitlist.py/changelog.py sind bereits per @superadmin_required aus dem Rolle-Reader-Sweep raus) ist nicht erfüllt.
- **Das ist erwartetes, korrektes Verhalten — kein Bug.** Der Executor soll die Checkpoint-Blockade in Plan 03 SICHTBAR an André melden (surface it), NICHT still auf Plan 04 warten oder Plan 04 „trotzdem" anlaufen lassen.
- **Vor Plan-04-Start Pflicht-Gate:** grep-bestätigen, dass Plan 03 vollständig durchgelaufen ist (`grep -n "get('rolle'" routes/waitlist.py routes/changelog.py` → 0 Reader; Plan-03-SUMMARY existiert). Erst dann Plan 04 executen. Fehlt der Beleg → STOP, Plan 03 zuerst zu Ende bringen.
</dependency_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md

<interfaces>
<!-- g.user in before_request (auth.py:48-55, VERIFIED): g.user = user (frische DB-Row). -->
<!-- g.user.rolle: database/models.py:72 Column(String(50), default='member'). -->
<!-- Flask injiziert g automatisch in Jinja-Kontext → {{ g.user.rolle }} funktioniert (RESEARCH A1). -->

Vollständige Reader-Liste (RESEARCH Section 2, grep-verifiziert 2026-07-04) — NACH Plan-03-Abzug:

Python-Routes (umstellen auf g.user.rolle):
| Datei:Zeile | Ist | Neu |
|---|---|---|
| profiles.py:36 | `def _rolle(): return flask_session.get('rolle', 'member')` (Helper, 18 Call-Sites :63,:106,:186,:208,:248,:269,:295,:366,:387,:413,:448,:469,:495,:530,:551,:577,:662,:751) | `return g.user.rolle` (EINE Zeile → alle 18 gefixt) |
| organisations.py:16 | `if session.get('rolle') not in ('owner','admin'):` (_require_admin) | `g.user.rolle not in ('owner','admin')` |
| dashboard.py:526 | `get_recent_logs(g.user.id, g.org.id, flask_session.get('rolle','member'))` | `..., g.user.rolle)` |
| logs_routes.py:14 | `rolle = flask_session.get('rolle','member')` (liste) | `rolle = g.user.rolle` |
| logs_routes.py:36 | `rolle = flask_session.get('rolle','member')` (download) | `rolle = g.user.rolle` |
| settings.py:63 | `if flask_session.get('rolle') not in ('owner','admin'):` | `g.user.rolle not in ('owner','admin')` |
| settings.py:82 | dito | dito |
| settings.py:105 | dito | dito |
| settings.py:122 | dito | dito |
| settings.py:191 | `if flask_session.get('rolle') != 'owner':` | `g.user.rolle != 'owner'` |

SETTER bleibt (D-12): auth.py:130 `session['rolle'] = user_rolle` — NICHT anfassen.
organisations.py:96 `user.rolle != 'owner'` liest bereits fresh user.rolle → schon korrekt, NICHT anfassen (nur verifizieren).

Templates (umstellen auf g.user.rolle — alle hinter @login_required, g.user nie None, RESEARCH Section 2):
| Datei:Zeile |
|---|
| base.html:79 |
| settings.html:115, :236 |
| profiles_list.html:30, :69, :81 |
| logs_page.html:25 |

g ist im Flask-Jinja-Kontext verfügbar → `{% if g.user.rolle in ('owner','admin') %}` / `{{ g.user.rolle }}` direkt nutzbar.
</interfaces>

<verify_environment>
- `bash deploy.sh production` pytest-Gate (deploy.sh:186-194); test_rolle_source_sweep.py auto-collected.
- Live read-only: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'`
- Browser-Live-Test: Rollen-Menü-Sichtbarkeit + Admin-Schranken mit Test-User.
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_rolle_source_sweep.py — statischer Wächter, ERST ROT gegen HEAD</name>
  <read_first>
    - tests/test_no_live_global_state.py — Verbotenes-Muster-Sweep + frozenset-Whitelist (Vorlage)
    - RESEARCH Section 2 — vollständige Reader-Liste (was verboten wird) + Setter-Ausnahme auth.py:130
    - CLAUDE.md „Test-Qualitaets-Regel" — statischer Reader-Sweep ist Grenzfall (sichert Runtime-Constraint „keine session-Rolle-Leser"; Grund als Kommentar)
  </read_first>
  <behavior>
    - `test_no_session_rolle_readers`: Sweep über `routes/*.py`. Verbotenes Muster: `flask_session.get('rolle'` ODER `session.get('rolle'` als LESE-Zugriff. Whitelist (frozenset): `routes/auth.py` (Setzstelle :130, D-12). Jeder Nicht-Whitelist-Treffer → Verletzung mit datei:zeile.
    - Gegen HEAD (vor Task 2/3): profiles.py:36, organisations.py:16, dashboard.py:526, logs_routes.py:14/:36, settings.py (5×) sind noch Reader → Wächter ROT. Nach Umstellung → grün. (Und waitlist.py/changelog.py sind bereits durch Plan 03 raus.)
    - Achtung Setter-Abgrenzung: `session['rolle'] =` (Zuweisung, auth.py:130) darf NICHT als Reader gematcht werden — Muster gezielt auf `.get('rolle'` beschränken, nicht auf `['rolle']`-Zuweisung.
    - Whitelist als frozenset; ASCII-Identifier.
  </behavior>
  <action>
    Erstelle `tests/test_rolle_source_sweep.py`. `_ROOT = Path(__file__).parent.parent`. `_ROLLE_READER_WHITELIST = frozenset({'routes/auth.py'})` (Setzstelle D-12). Regex für Reader: `r"(flask_session|session)\.get\(\s*['\"]rolle['\"]"`. Sweep `list((_ROOT/'routes').glob('*.py'))`, pro Datei Treffer sammeln (datei:zeile), Whitelist-Dateien überspringen. `assert not violations, "session-Rolle-Leser (nutze g.user.rolle):\n  " + "\n  ".join(violations)`.

    File-Kopf-Kommentar: Grund (Runtime-Constraint „Rolle IMMER frisch aus DB, nie aus Session gelesen"; kein Function-Mock testet den ganzen Sweep — CLAUDE.md Grenzfall).

    SEQUENZ „ERST rot, DANN fixen": Test zuerst committen+deployen → gegen HEAD ROT (die 10 Reader) → deploy.sh-Gate blockt → Rot-Beleg. DANN Task 2+3 → grün. Rot-Beleg im SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # VOR Task 2/3: ROT (10 Reader). NACH Task 2/3: grün (nur auth.py:130 Setter whitelisted).</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_rolle_source_sweep.py existiert; grep zeigt Whitelist-frozenset mit routes/auth.py.
    - Regex matcht `.get('rolle'`-Reader, NICHT `['rolle']`-Zuweisung (Setter bleibt unerkannt).
    - Rot-Beleg gegen HEAD dokumentiert (Gate blockt an den Readern).
  </acceptance_criteria>
  <done>Reader-Wächter existiert, gegen HEAD rot, whitelisted nur den Setter.</done>
</task>

<task type="auto">
  <name>Task 2: Routes umstellen — profiles/organisations/dashboard/logs_routes/settings auf g.user.rolle</name>
  <read_first>
    - <interfaces> oben — exakte Datei:Zeile-Liste
    - routes/profiles.py:35-36 (Helper) + die 18 Call-Sites (nur Helper ändern, Caller NICHT einzeln)
    - routes/organisations.py:16 (_require_admin) + :96 (schon korrekt, nur verifizieren)
    - routes/dashboard.py:526, routes/logs_routes.py:14/:36, routes/settings.py:63/:82/:105/:122/:191
    - RESEARCH Pitfall 1 (Helper-Umfang: 1 Zeile → 18 Caller), Pitfall 5 (settings.py:191 delete_account)
    - Prüfen dass `from flask import g` in jeder Datei importiert ist (falls nicht: ergänzen)
  </read_first>
  <action>
    Pflicht-grep VOR jedem Edit (Punkt 20): `grep -n "get('rolle'" routes/<datei>.py` um die aktuelle Zeile zu bestätigen (Anti-Drift). Dann pro Datei:

    - **profiles.py:36**: `def _rolle(): return flask_session.get('rolle', 'member')` → `return g.user.rolle`. (Sicherstellen `from flask import g` importiert.) Das fixt alle 18 Call-Sites in einem Schlag — Caller NICHT anfassen.
    - **organisations.py:16**: `if session.get('rolle') not in ('owner', 'admin'):` → `if g.user.rolle not in ('owner', 'admin'):`. organisations.py:96 (`user.rolle != 'owner'`) NUR verifizieren, NICHT ändern (schon fresh).
    - **dashboard.py:526**: dritter Arg `flask_session.get('rolle', 'member')` → `g.user.rolle`.
    - **logs_routes.py:14 + :36**: `rolle = flask_session.get('rolle', 'member')` → `rolle = g.user.rolle`.
    - **settings.py:63/:82/:105/:122**: `flask_session.get('rolle') not in ('owner', 'admin')` → `g.user.rolle not in ('owner', 'admin')`. **settings.py:191**: `flask_session.get('rolle') != 'owner'` → `g.user.rolle != 'owner'`.

    Alle diese Routen sind @login_required (g.user gesetzt) → kein None-Risiko. `flask_session`-Import in jeder Datei lassen falls anderweitig genutzt (grep); nur die Rolle-Reader-Zeilen ändern. KEINE Verhaltensänderung an der Allow-Menge (owner/admin bleibt owner/admin — behaviour-preserving, completeness_sweep Plan 03).
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_rolle_source_sweep.py grün (Python-Seite). Browser-Live-Test: Admin-Schranken (settings/organisations/logs) funktionieren identisch; Rollen-Änderung greift ohne Re-Login.</automated>
  </verify>
  <acceptance_criteria>
    - grep zeigt KEIN `.get('rolle'` mehr in profiles.py, organisations.py, dashboard.py, logs_routes.py, settings.py.
    - grep zeigt `g.user.rolle` in jeder der 5 Dateien.
    - organisations.py:96 unverändert (nur verifiziert).
    - deploy.sh Gate grün; Admin-Schranken verhalten sich identisch (Allow-Menge unverändert).
  </acceptance_criteria>
  <done>Alle Python-Rollen-Leser lesen g.user.rolle; _rolle()-Helper einzeilig umgestellt; Verhalten identisch.</done>
</task>

<task type="auto">
  <name>Task 3: Templates umstellen — base/settings/profiles_list/logs_page auf g.user.rolle</name>
  <read_first>
    - templates/base.html:79, templates/settings.html:115/:236, templates/profiles_list.html:30/:69/:81, templates/logs_page.html:25
    - RESEARCH Section 2 Template-Teil (alle hinter @login_required, g.user nie None) + A1 (g im Jinja-Kontext)
    - CLAUDE.md Punkt 14 Schicht 4 (Edge-Case None-Guard-Kommentar) + Umlaute-Regel (Jinja-Expression = Code-Identifier → g.user.rolle ASCII; User-facing Menü-Texte behalten Umlaute)
  </read_first>
  <action>
    Pflicht-grep VOR Edit: `grep -n "rolle" templates/<datei>.html` (Anti-Drift). Pro Template die Jinja-Rolle-Leser von `session.get('rolle')` / `session['rolle']` auf `g.user.rolle` umstellen:
    - **base.html:79** — Menü-Sichtbarkeits-Check → `{% if g.user.rolle in ('owner', 'admin') %}` (oder was der Ist-Check ist, gleiche Allow-Menge).
    - **settings.html:115, :236** — dito.
    - **profiles_list.html:30, :69, :81** — dito.
    - **logs_page.html:25** — dito.

    Setze über den ersten geänderten Block je Template einen kurzen Jinja-Kommentar: `{# g.user ist hier nie None — Route ist @login_required (CLAUDE.md Punkt 14 Schicht 4) #}`. KEINE Änderung der User-facing Menü-Texte (Umlaute erhalten). Allow-Menge unverändert (behaviour-preserving).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate grün. Browser-Live-Test: Menü-Einträge (owner/admin-only) erscheinen identisch für Owner/Admin, fehlen für member; kein 500 (g.user.rolle auflösbar).</automated>
  </verify>
  <acceptance_criteria>
    - grep zeigt KEIN `session.get('rolle')` / `session['rolle']` mehr in den 4 Templates.
    - grep zeigt `g.user.rolle` in base.html, settings.html, profiles_list.html, logs_page.html.
    - Je Template ein None-Guard-Kommentar vorhanden.
    - Browser-Live-Test: Menü-Sichtbarkeit identisch pro Rolle, kein 500 (Sicht statt Zusicherung).
  </acceptance_criteria>
  <done>Alle Template-Rollen-Leser nutzen g.user.rolle; Menü-Sichtbarkeit identisch; None-Edge dokumentiert.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (LESEN, Spalte rolle) — g.user.rolle liest die frische users-Row (via login_required/before_request, auth.py:48-55). Bestehende Spalte, KEINE Schema-Änderung.

### inspect.sh-Beleg (Prod)
`inspect.sh columns users` (VERIFIED): `rolle` character varying(50), default 'member' (database/models.py:72). Die von g.user.rolle gelesene Spalte existiert und ist befüllt (owner/admin/member).

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Leser läuft voraus? | Verifiziert? |
|---|---|---|---|---|---|
| g.user.rolle | gelesen in Routes + Templates | DB-Spalte users.rolle (frisch pro Request) | synchron: login_required setzt g.user aus DB VOR Route-Body/Template-Render | Nein — synchron request-scope, KEIN async Versatz | ✓ inspect.sh columns + auth.py:48-55 |
| session['rolle'] | NICHT mehr gelesen (nur noch auth.py:130 setzt) | Flask-Session-Cookie | beim Login | — (kein Leser mehr) | ✓ Wächter Task 1 beweist 0 Leser |

Punkt 26 (async-Bereitschafts-Naht): NICHT anwendbar — g.user.rolle ist synchron request-scoped (login_required setzt g.user aus der DB, unmittelbar vor jedem Leser). Kein Hintergrund-Thread, keine Batch-Write-Naht. Der Kernvorteil der Umstellung IST gerade die Frische (kein Stale-Session-Versatz mehr).
</persistenz_schicht_verifikation>

<verbindungs_karte>
## Verbindungs-Karte (Punkt 22)

- **Setter (LEBT, bleibt):** auth.py:130 `session['rolle'] = user_rolle` — einzige Schreibstelle der Session-Rolle (grep-belegt, D-12). Wird NACH dem Sweep von 0 Routen/Templates gelesen (Wächter Task 1 beweist).
- **Neue Quelle (LEBT):** g.user (auth.py:55, before_request/login_required) → g.user.rolle (models.py:72). Frisch aus DB pro Request.
- **Reader-Migration vollständig (kein Halb-Zustand, Punkt 28/29):** 10 Python-Reader (profiles-Helper=1 Zeile→18 Caller, organisations, dashboard, logs×2, settings×5) + 4 Templates. waitlist/changelog bereits via Plan 03 raus. Wächter Task 1 erzwingt 0 verbleibende session-Reader (außer Setter-Whitelist).
- **Bereits korrekt (nicht angefasst):** organisations.py:96 `user.rolle` (fresh), auth.py:130 (Setter).
</verbindungs_karte>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eingeloggter User mit kürzlich geänderter Rolle → Admin-Schranken + Menü | die Session-Rolle könnte stale sein (Rollen-Änderung noch nicht durch Re-Login übernommen) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-12 | Elevation of Privilege (Stale-Role-Session — herabgestufter User behält Admin-Zugang bis Re-Login) | alle rolle-Reader (Routes + Templates) | mitigate | g.user.rolle frisch aus DB pro Request (D-10); Wächter erzwingt Vollständigkeit; Setter auth.py:130 harmlos (kein Leser mehr) |
| T-AUTH1-13 | Tampering (Halb-Migration — neuer Code kopiert alt-Muster session.get('rolle')) | zukünftige route/template-Edits | mitigate | test_rolle_source_sweep blockt jeden neuen session-Rolle-Reader (Punkt 29) |
| T-AUTH1-14 | Denial of Service (g.user None auf öffentlicher Seite → 500) | Templates | mitigate | alle 4 Templates nur hinter @login_required (g.user gesetzt); None-Guard-Kommentar dokumentiert Edge (Punkt 14 Schicht 4) |
</threat_model>

<verification>
- test_rolle_source_sweep.py: rot gegen HEAD (10 Reader), grün nach Umstellung (nur auth.py:130 whitelisted).
- grep: 0 `.get('rolle'`-Reader in routes/ außer auth.py; 0 `session['rolle']`/`session.get('rolle')` in den 4 Templates.
- Browser-Live-Test: Admin-Schranken + Menü-Sichtbarkeit identisch pro Rolle; eine Rollen-Änderung greift ohne Re-Login (Sicht statt Zusicherung).
- organisations.py:96 unverändert verifiziert.
</verification>

<success_criteria>
- Vollständiger Sweep (Routes + Templates) auf g.user.rolle; kein Halb-Zustand.
- Setzstelle auth.py:130 unverändert.
- Wächter im Deploy-Gate, ERST-ROT bewiesen; blockt zukünftige session-Rolle-Reader.
- Verhalten (Allow-Mengen, Menü-Sichtbarkeit) identisch; Rollen-Änderung greift sofort.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-04-SUMMARY.md` (inkl. Wächter-Rot-Beleg, grep-0-Belege, Browser-Live-Test der Menü-Sichtbarkeit + sofort-greifender Rollen-Änderung).
</output>
</content>
