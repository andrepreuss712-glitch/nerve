# Fable-Review AUTH-1 (Pläne 01-04 gegen echten Code) — 2026-07-04

> 3. Sicht (Fable, read-only am echten Code) + Claudian-Prod-Verifikation. Ersetzt den Gemini-`/gsd-review`-Weg (bei uns unzuverlässig). Cross-AI-Pflicht bei Auth/Security.
> **Gesamturteil:** Plan 01 (CSRF-Fix) + Plan 03 (Rechte-Härtung) solide, fast baubereit (Detail-Fixes). **Plan 02 muss neu geschnitten werden** (Wächter deckt 39 echte Bestand-Löcher auf → Scope-Entscheidung für André). **Plan 04 hat einen 500er-Blocker.** Selbst-Aussperr-Risiko: OK (Prod-Stand SSH-belegt in RESEARCH: passwort_hash leer, is_superadmin=t).

## ★★ BLOCKER 1 — Der CSRF-Bruch ist APP-WEIT, nicht nur die Landing (Plan 02, 🟢 Scope-Entscheidung)
Fable hat den geplanten statischen CSRF-Wächter als Dry-Run gegen HEAD laufen lassen: **42 token-lose state-changing Fetches, 39 AUSSERHALB landing.html** in 16 Dateien. Kein globaler fetch-Wrapper existiert; keine dieser Dateien schickt den Token anderweitig. Betroffen u.a.: `settings.html:619-705` (billing, cancel, reactivate, privacy, **delete_account**), `training.html` (9×, inkl. transcribe :1327), `base.html:657` (**keepalive**, Fehler im `.catch` still geschluckt), `:700` (changelog/seen), `team.html:155`, `waitlist_admin.html:113` (**André's Invite-Knopf**), `onboarding.html`, `admin/_tab_ausgaben.html` (4×), coach_*, session_detail, dashboard (3×).
**★ Claudian-PROD-VERIFIKATION (curl token-los gegen getnerve.app):** `/api/keepalive` → **400**, `/settings/cancel` → **400**, `/settings/delete_account` → **400**. Bestätigt: CSRF greift vor Login → diese Vorgänge sind auf Prod für ALLE real tot. Nicht Inferenz, gemessen.
**Konsequenz:** Die D-01-Entscheidung (A1 nur landing, A3-globaler-Wrapper abgelehnt „zu groß") ist auf unvollständiger Bestandskenntnis gefallen. Bei 39 Löchern in 16 Dateien ist der **globale Wrapper der KLEINERE + vollständige Eingriff.** → André-Entscheidung: (A) globaler fetch-Wrapper (fix alle 39 + Eingang in EINER Stelle) vs. (B) landing-only jetzt + dokumentierte Whitelist + Folgephase. **Claudian-Empfehlung: (A) Wrapper** — Kernfeatures (Konto-Löschen=DSGVO, Abo-Kündigen=Billing, Training) sind JETZT tot, „fix at root" (André-Linie), Leitsatz 2/3.

## BLOCKER 2 — Plan 04 Task 3 / base.html:79 → 500 auf öffentlichen Seiten
`{% if g.user.rolle in (...) %}` ohne `g.user and`-Guard. base.html ist das Layout, wird auf `/impressum`, `/agb`, `/datenschutz`, `/changelog` (public, kein g.user) gerendert → Jinja UndefinedError → **500** (Impressum-500 = zusätzlich Rechtsproblem). base.html guardet an :58/:98 heute schon so. Fix: `{% if g.user and g.user.rolle in ('owner','admin') %}` (eine Zeile). Die anderen 3 Templates (settings/profiles_list/logs_page) sind login-only → dort ok.

## BLOCKER 3 — Plan 01 Test-Fallen
(a) Cleanup-Spec `{User, Organisation}` reicht nicht — echter register-Pfad ruft `_login_user`→`db.add(DbSession)` (auth.py:134), `sessions.user_id` FK ohne ondelete → users-Delete scheitert, sessions-Row leakt → **Gate rot auch NACH Fix**. `Session` (vor User) in den Tracker. (b) Register triggert `send_welcome` → echte Resend-Mail außer Empfänger endet auf `@nerve.local` → Payload-Email hart `@nerve.local`. (c) Assert `== 200` statt `!= 400` (sonst Validierungs-400 ununterscheidbar). (d) `csrf_client`-Fixture referenziert nicht-existentes `app`-Fixture → über `client.application.config` togglen.

## BLOCKER 4 — Serialisierung
Pläne 01/02/03 alle `wave:1 depends_on:[]`, aber teilen dasselbe serielle Prod-Deploy-Gate + überlappende ERST-ROT-Fenster → Rot nicht attribuierbar. Strikt 01→02→03→04.

## Prüfpunkt 3 — Selbst-Aussperr: OK
RESEARCH SSH-belegt: André-Konto `passwort_hash`=leerer String (falsy, `auth.py:158` blockt korrekt), `is_superadmin`=t (persistent in DB), `SUPERADMIN_EMAIL` gesetzt, oauth=google. **KEIN „Hash entfernen"-Schritt** in den Plänen (E3 = reine Verifikation) → gefährlicher Blind-Lösch-Fall tritt nicht ein. Plan 03 Task 0 re-checkt BLOCKING. Notausgang (DB via SSH) in D-08.

## Prüfpunkt 4 — Rollen-Migration: sonst vollständig
grep-Beleg deckt sich exakt mit eigenem Sweep (10 Python-Leser hinter @login_required + waitlist×2/changelog×1 + 7 Template-Leser). Kein VIERTES Loch der Klasse. **Aber:** `session['is_coach']` (coach.py:17, base.html:82) hat dieselbe Stale-Schwäche → als bewusstes Defer notieren. Ratchet-Lücke: `test_rolle_source_sweep` scannt nur `routes/*.py` → auch `templates/` scannen.

## SOLLTE REIN
- `waitlist_admin.html:113` (Invite-POST token-los) + `base.html:657` (keepalive) im Fix priorisieren (sonst „Warteliste/App funktioniert wieder" nur halb wahr) — mit dem Wrapper (A) automatisch erledigt.
- Guard-Testfall Owner→403 für `POST /changelog/admin` (must_have verspricht ihn, Task baut ihn nicht).
- `Invitation` in Plan-03-Tracker + Teardown der invite-erzeugten Org (Rot-Phase-Nebenwirkung).
- Audit-Log-Warn-Kommentar in test_signup_journey: D-05-Journey (…→Login) NIE vervollständigen — Login schreibt audit_log, Migration 0026 blockt UPDATE+DELETE → uncleanbar → Gate dauerhaft rot. Plan 01 hat richtig register-only, nennt den Grund nicht.
- CSRF-Guard: static rekursiv + vendor-Ausschluss; bekannte Umgehungen (fetch mit Options-Variable, XHR/sendBeacon) dokumentieren.

## NICHT verifiziert
Prod-Live-Zustand Konto (aus RESEARCH-SSH-Protokoll übernommen, Task 0 re-checkt); deploy.sh-Gate aus Code gelesen; Jinja-Undefined als reine Library-Semantik belegt. Die 39-Fetch-400-These ist für 3 Endpunkte von Claudian gemessen (keepalive/cancel/delete), Rest statisch zwingend abgeleitet.
