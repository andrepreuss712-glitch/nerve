---
status: passed
phase: 08.23.2.AUTH-1-eingangstuer-netz
verified: 2026-07-06
method: inline goal-backward (Multi-Segment-Gotcha — gsd-verifier/gsd-code-review umgangen, Pfade hardcoded; jeder Plan live an einem supervised deploy.sh-production-Gate von André verifiziert)
plans_complete: 4/4
---

# Phase 08.23.2.AUTH-1 „Eingangstür + Netz" — Verification

**Ziel (ROADMAP):** Die CSRF-„Eingangstür" (Register/Login/Waitlist + 39 app-weite Bestand-Löcher) wieder öffnen OHNE CSRF zu schwächen, ein statisches Wächter-„Netz" im Deploy-Gate spannen, die Waitlist/Changelog-Admin-Löcher (fremde PII / Gratis-EA / fremd-Changelog) schließen, und die Rolle frisch aus der DB lesen (Stale-Role-Elevation zu). Block-AUTH Pre-Launch-Blocker.

## Goal-Backward Must-Have-Prüfung (live-belegt)

| # | Must-Have (goal-level) | Beleg | Status |
|---|---|---|---|
| 1 | Register/Login/Waitlist funktionieren wieder (kein CSRF-400) | Gate B grün + Browser (Register-Wizard erreicht Server, „E-Mail bereits registriert" = Durchkommen) | ✅ |
| 2 | Die 39 app-weiten Bestand-Löcher (delete_account/cancel/training …) schicken auto X-CSRFToken | globaler window.fetch-Wrapper base.html+landing.html live; im pytest-Gate + Struktur-Wächter grün | ✅ |
| 3 | CSRF bleibt AKTIV (kein exempt/disable) | `test_register_without_token_is_400` grün; nur WTF_CSRF_TIME_LIMIT=None | ✅ |
| 4 | 3 Deploy-Gate-Wächter scharf (E2E-Signup + CSRF-Fetch + Template-Existenz) | Gate D 890 passed, alle 3 auto-collected + ERST-ROT bewiesen | ✅ |
| 5 | Waitlist admin+invite + Changelog admin nur Superadmin (Owner→403) | Gate F grün; Browser: Superadmin lädt /waitlist/admin (0/50), Owner→403 (technisch belegt) | ✅ |
| 6 | Selbst-Aussperr-Schutz (André kommt rein) | Task 0 Prod-Vorab (is_superadmin=t, SUPERADMIN_EMAIL, oauth_provider=google); Gate F live bestätigt | ✅ |
| 7 | Alle Rollen-Leser lesen g.user.rolle (frisch, sofort-greifend) | Gate H: `test_no_session_rolle_readers` grün (0 Reader in routes+templates ausser Setter) | ✅ |
| 8 | Kein 500 auf public Seiten (base.html None-Guard, Fable BLOCKER 2) | Gate H live curl (ausgeloggt): /impressum,/agb,/datenschutz=200, /changelog=308→200, kein Traceback | ✅ |
| 9 | Setzstelle auth.py:130 unverändert (D-12) | grep bestätigt; Sweep whitelisted nur den Setter | ✅ |

## CONTEXT-Decision-Abdeckung
- **D-01 (CSRF, A3-OVERRIDE):** ✅ globaler Wrapper (Plan 01)
- **D-04 (TIME_LIMIT=None):** ✅ (Plan 01)
- **D-06/D-07 (2 statische Wächter):** ✅ (Plan 02)
- **E3 (Superadmin nur Google):** ✅ reine Verifikation via Task 0 Prod-Vorab (kein passwort_hash-Lösch-Schritt gebaut — bewusst)
- **D-10/D-11/D-12 (Rollen-Sweep vollständig, Setter bleibt):** ✅ (Plan 04)
- **D-13/D-14/D-15/D-16 (Waitlist/Changelog-Härtung, Selbst-Aussperr):** ✅ (Plan 03)

## Bewusste Defers (kein Gap)
- **session['is_coach']** — identische Stale-Session-Schwäche, out of AUTH-1 scope (nur `rolle`). Registriert für spätere Coach-Härtung.
- **Visueller Menü-per-Rolle-Test** — heute nicht am Auge prüfbar: der einzige rolle-abhängige Eintrag (Team, base.html:106) liegt im `display:none`-Legacy-Block (D-31, bis AUTH-5); sichtbare Admin-Einträge hängen an `is_superadmin`, nicht `rolle`. Sofort-Wirkung technisch via grünem Sweep belegt. **Visuell testbar erst mit AUTH-5.**

## Findings → Backlog (aus Bau aufgedeckt, nicht Gaps von AUTH-1)
- **999.4** Flask-Admin-CSRF (admin/master.html-Familie ausserhalb Wrapper; crm_overview:155 POST vermutlich CSRF-kaputt) — dokumentiert exempt in `_STANDALONE_WRAPPER_EXEMPT`.
- **999.5** Deploy prunt gelöschte Dateien nicht (3 tote Ghost-Templates + ERST-ROT-Seeds persistierten server-seitig) — beim Bau server-seitig bereinigt.
- **999.6** Login-Cookie-Domain apex `getnerve.app` vs `app.getnerve.app` (Cookie subdomain-gebunden).
- **audit_log-append-only-Leak** (Migration 0026) — bekannt/non-breaking, getrackt in der Test-Cleanup-Härtungs-Phase.

## Regression
Keine: die Suite wuchs von 884 (Baseline) → 897 passed über die Phase, 0 Fehler an jedem Grün-Gate. Jeder Wächter ERST-ROT-bewiesen (kein blind-grüner Guard).

## Prod-Regression während des Baus (gefangen + behoben)
Plan 01: landing.html war zwischenzeitlich zerschossen (Jinja verarbeitete `{% extends %}` in einem JS-Kommentar) → Struktur-Wächter `test_landing_renders_valid_structure` ergänzt (prüft ausgelieferte Struktur), Debug-Session resolved. Kein Rest-Risiko.

## Verdikt
**PASSED.** Alle 4 Plans live-verifiziert an supervised Prod-Deploy-Gates; alle goal-level Must-Haves erfüllt + live belegt; 3 Wächter im Deploy-Gate scharf; keine offenen Gaps (2 bewusste Defers, 4 Backlog-Findings). Block-AUTH CSRF-/Auth-Eingangstür-Teil geschlossen.
