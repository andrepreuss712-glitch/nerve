---
phase: 08.23.2.AUTH-1
plan: 01
type: execute
wave: 1
complexity: "🟡 mittel (Auth/Security — Cross-AI Gemini PFLICHT vor Execute)"
depends_on: []
files_modified:
  - templates/marketing/landing.html
  - app.py
  - tests/test_signup_journey.py
autonomous: true
requirements: []   # keine REQ-IDs in REQUIREMENTS.md gemappt; Scope = CONTEXT-Decisions D-01..D-05
user_setup: []

must_haves:
  truths:
    - "Ein anonymer Besucher kann sich auf der Landing-Page registrieren (POST /api/register) ohne HTTP 400 CSRF-Fehler"
    - "Ein anonymer Besucher kann sich einloggen (POST /api/login) ohne HTTP 400 CSRF-Fehler"
    - "Ein anonymer Besucher kann sich in die Warteliste eintragen (POST /waitlist/join) ohne HTTP 400 CSRF-Fehler"
    - "Der CSRF-Schutz bleibt AKTIV (kein exempt, kein globaler Disable) — nur der Token wird jetzt mitgeschickt"
    - "Eine >1h offene Anmelde-Seite produziert keinen CSRF-Timeout-400 mehr"
    - "Der neue Waechter test_signup_journey.py faengt einen erneuten token-losen Register/Login-Fetch (rot-vor-Fix bewiesen)"
  artifacts:
    - path: "templates/marketing/landing.html"
      provides: "csrf-token Meta-Tag im <head> + X-CSRFToken-Header in den 3 state-changing Fetches + inline getCsrfToken()"
      contains: "meta name=\"csrf-token\""
    - path: "app.py"
      provides: "WTF_CSRF_TIME_LIMIT=None Config nach CSRFProtect(app)"
      contains: "WTF_CSRF_TIME_LIMIT"
    - path: "tests/test_signup_journey.py"
      provides: "E2E-Signup-Waechter MIT CSRF AN (kein WTF_CSRF_ENABLED=False)"
      contains: "WTF_CSRF_ENABLED"
  key_links:
    - from: "templates/marketing/landing.html fetch('/api/register')"
      to: "Flask CSRFProtect Validierung"
      via: "X-CSRFToken-Header aus dem Meta-Tag"
      pattern: "X-CSRFToken.*getCsrfToken"
---

<objective>
Die reguläre Registrierung, Anmeldung und Warteliste funktionieren wieder — heute prallen alle drei live mit HTTP 400 „CSRF token missing" ab (auf Prod bewiesen), weil `templates/marketing/landing.html` eine eigenständige HTML-Datei ist (kein base.html-Erbe) und deshalb weder das `<meta name="csrf-token">`-Tag noch den `X-CSRFToken`-Header mitschickt. Gewählter Weg: **A1 (Token mitschicken)** — der existierende CSRFProtect-Schutz bleibt vollständig aktiv, nur der Token wird clientseitig ergänzt.

Purpose: Die „Eingangstür" wieder aufmachen, ohne den Login-Schutz zu schwächen (NICHT A2 Endpunkt-exempt, NICHT A3 globaler Wrapper — D-01).
Output: landing.html mit Meta-Token + 3 Header-Ergänzungen, app.py mit WTF_CSRF_TIME_LIMIT=None, plus der E2E-Waechter test_signup_journey.py (ERST ROT gegen HEAD).

Komplexität: 🟡 mittel — Auth/Security → Cross-AI Gemini PFLICHT vor Execute (CLAUDE.md Punkt 24, GSD-Workflow-Pflichten). Dieser Plan wird NICHT direkt executet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md

<interfaces>
<!-- Verifizierte Anker (RESEARCH.md Section 5, alle Zeilen live gegen echten Code geprüft). Executor arbeitet aus diesen Werten — kein Codebase-Scan nötig. -->

Kopier-Quelle Meta-Tag — templates/base.html:6 (VERIFIED):
```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```

JS Token-Extraktions-Idiom (Projekt-Muster, static/feedback.js:2-5, VERIFIED — INLINE in landing.html definieren, da standalone HTML feedback.js nicht einbindet):
```javascript
function getCsrfToken() {
    var m = document.querySelector('meta[name="csrf-token"]');
    return m ? m.getAttribute('content') : '';
}
```

Verwendung im Fetch-Header:
```javascript
headers: {'Content-Type': 'application/json', 'X-CSRFToken': getCsrfToken()}
```

app.py CSRF-Config (VERIFIED app.py:49-61):
- app.py:54  `csrf = CSRFProtect(app)`  — aktiv, bleibt
- app.py:58-60  lokaler `WTF_CSRF_ENABLED=False`-Override für Dev — bleibt UNANGETASTET
- `WTF_CSRF_TIME_LIMIT` ist NICHT gesetzt → muss ergänzt werden (Einfügestelle: nach Zeile 60)

landing.html Fetch-Zeilen (VERIFIED landing.html:820-960, kein Drift zu CONTEXT):
- :829  `fetch('/api/login', {method:'POST', ...})`   → Header ergänzen
- :897  `fetch('/api/register', {method:'POST', ...})` → Header ergänzen
- :921  `fetch('/waitlist/join', {method:'POST', ...})` → Header ergänzen
- :952  `fetch('/waitlist/stats')` GET                 → NICHT anfassen (GET, kein state-change, D-03)
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR über Production-Pfad. KEIN lokales pytest, KEIN python app.py. -->
- Deploy-Gate: `bash deploy.sh production` führt `pytest tests/ --tb=short -q -m "not live and not perf"` auf `nerve_test` VOR dem Restart aus (deploy.sh:186-194, VERIFIED). Rot = kein Restart, alter Code bleibt live.
- Live-Zustand (Routes/Env): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'`
- Neue tests/test_*.py ohne live/perf-Marker werden automatisch mitgezogen.
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E-Signup-Waechter test_signup_journey.py — ERST ROT gegen HEAD</name>
  <read_first>
    - tests/conftest.py — cleanup_rows (:238), _baseline_cleanup_guard autouse (:549), WTF_CSRF_ENABLED=False-Default für lokale Tests (RESEARCH Section 3)
    - tests/test_admin_dashboard_auth.py — cleanup_tracker-Fixture-Muster + _make_org_and_user (RESEARCH Section 4.1)
    - tests/test_auth_next_redirect.py — nächster verwandter Auth-Flow-Test (Referenz)
    - RESEARCH.md Section 4.4 (csrf_client-Fixture-Muster, Token-Holen aus Landing-GET)
    - templates/marketing/landing.html:820-960 (die Fetch-Ziele, die der E2E durchläuft)
  </read_first>
  <behavior>
    - Test-Fixture `csrf_client`: setzt `app.config['WTF_CSRF_ENABLED'] = True` VOR dem test_client, restauriert `False` im Teardown. (Sonst blind-grün wie der Bug, der durchrutschte — D-05.)
    - Test `test_register_flow_with_csrf`: GET Landing-Page → Response-Body nach `meta[name="csrf-token"]` scannen → Token extrahieren → POST /api/register MIT `X-CSRFToken`-Header → erwartet KEINEN 400. (Gegen aktuellem HEAD: landing.html hat noch KEIN Meta-Tag → Token nicht extrahierbar/leer → 400 → Test ROT. Nach A1-Fix: grün.)
    - Test `test_register_without_token_is_400`: POST /api/register OHNE X-CSRFToken → erwartet 400 (beweist, dass CSRF aktiv IST und der Test beißt).
    - Committende Rows (User+Org aus erfolgreicher Registrierung): cleanup_tracker-Fixture mit `{User: [], Organisation: []}` + cleanup_rows im Teardown (CLAUDE.md Test-Cleanup-Regel, sonst färbt _baseline_cleanup_guard rot).
    - ASCII-Regel: Test-Funktions-/Fixture-Namen + dict-keys ASCII (Umlaute-Regel — Code-Identifier).
  </behavior>
  <action>
    Erstelle `tests/test_signup_journey.py`. Fixture `csrf_client(app)`: `app.config['WTF_CSRF_ENABLED'] = True`, `yield app.test_client()`, im Finally `app.config['WTF_CSRF_ENABLED'] = False`. Fixture `cleanup_tracker(db_from_client)`: `ids = {User: [], Organisation: []}; yield ids; cleanup_rows(db_from_client, ids)` (Import `from tests.conftest import cleanup_rows`, `from database.models import User, Organisation`).

    Token-Extraktion aus Landing-GET: `r = csrf_client.get('/')` (oder der Route, die landing.html rendert — via grep `render_template('marketing/landing.html')` verifizieren welche Route das ist), dann `import re; m = re.search(r'name="csrf-token"\s+content="([^"]+)"', r.get_data(as_text=True)); token = m.group(1) if m else None`.

    `test_register_flow_with_csrf`: extrahierten Token via Header `{'X-CSRFToken': token}` an `csrf_client.post('/api/register', json={...gültige Registrier-Payload...}, headers={'X-CSRFToken': token})` → `assert r.status_code != 400`. WICHTIG: Gegen aktuellem HEAD ist `m` None (kein Meta-Tag) → token None → 400 → Test ROT. Genau gewünscht (Test-Netz-Ratsche).

    `test_register_without_token_is_400`: `csrf_client.post('/api/register', json={...})` OHNE Header → `assert r.status_code == 400`.

    SEQUENZ (André-Direktive „ERST rot, DANN fixen"): Diesen Test committen + deployen, mit Task 2/3 NICHT fixen — beweisen dass `test_register_flow_with_csrf` gegen HEAD ROT ist (deploy.sh-Gate blockt), DANN Task 2+3 anwenden → grün. Dokumentiere den Rot-Beleg im SUMMARY.
  </action>
  <verify>
    <automated>bash deploy.sh production  # pytest-Gate auf nerve_test; test_signup_journey.py wird auto-collected. VOR Task 2/3: test_register_flow_with_csrf MUSS ROT sein (Gate blockt Restart). NACH Task 2/3: grün.</automated>
  </verify>
  <acceptance_criteria>
    - Datei `tests/test_signup_journey.py` existiert; grep zeigt `WTF_CSRF_ENABLED` = True im csrf_client-Fixture (NICHT nur False).
    - grep zeigt `cleanup_rows` + `cleanup_tracker` im File (Committende-Test-Cleanup).
    - Rot-Beleg dokumentiert: gegen HEAD (vor Task 2/3) blockt `bash deploy.sh production` am roten `test_register_flow_with_csrf`.
    - Test-Namen/Fixture-Namen/dict-keys sind ASCII.
  </acceptance_criteria>
  <done>test_signup_journey.py existiert, ist gegen HEAD nachweislich rot (CSRF-400 im Register-Flow), CSRF ist im Test explizit AN.</done>
</task>

<task type="auto">
  <name>Task 2: landing.html — Meta-Token + inline getCsrfToken() + 3 Fetch-Header (A1-Fix)</name>
  <read_first>
    - templates/base.html:6 — exakte Meta-Tag-Kopier-Quelle: `<meta name="csrf-token" content="{{ csrf_token() }}">`
    - templates/marketing/landing.html:1-15 — bestätigt: KEIN {% extends %}, standalone, kein Meta-Tag vorhanden; `<head>`-Position
    - templates/marketing/landing.html:820-960 — die 4 Fetch-Blöcke (:829 login, :897 register, :921 waitlist/join zu ändern; :952 stats GET NICHT)
    - static/feedback.js:2-5 — getCsrfToken()-Idiom (INLINE übernehmen)
  </read_first>
  <action>
    (a) Ins `<head>` von `templates/marketing/landing.html` das Meta-Tag exakt aus base.html:6 einfügen: `<meta name="csrf-token" content="{{ csrf_token() }}">`.

    (b) Im Inline-`<script>` von landing.html (z.B. am Kopf der bestehenden JS-Logik) die Funktion definieren:
    ```javascript
    function getCsrfToken() {
        var m = document.querySelector('meta[name="csrf-token"]');
        return m ? m.getAttribute('content') : '';
    }
    ```

    (c) In den 3 state-changing Fetches den Header ergänzen. Vorhandene `headers`-Objekte um `'X-CSRFToken': getCsrfToken()` erweitern (falls kein headers-Objekt existiert, eines mit `'Content-Type': 'application/json', 'X-CSRFToken': getCsrfToken()` anlegen):
    - :829 `fetch('/api/login', {method:'POST', headers:{..., 'X-CSRFToken': getCsrfToken()}, body:...})`
    - :897 `fetch('/api/register', {method:'POST', headers:{..., 'X-CSRFToken': getCsrfToken()}, body:...})`
    - :921 `fetch('/waitlist/join', {method:'POST', headers:{..., 'X-CSRFToken': getCsrfToken()}, body:...})`

    (d) `fetch('/waitlist/stats')` bei :952 NICHT anfassen (GET, kein Token — D-03).

    Umlaute-Regel: JS-Identifier (`getCsrfToken`, `m`) ASCII; falls in dem Bereich User-facing Strings stehen, deren Umlaute erhalten.
  </action>
  <verify>
    <automated>bash deploy.sh production  # test_signup_journey.py::test_register_flow_with_csrf jetzt GRÜN. Danach Browser-Live-Test: registrieren + login + Warteliste mit Test-User auf getnerve.app → kein 400.</automated>
  </verify>
  <acceptance_criteria>
    - grep von templates/marketing/landing.html zeigt `<meta name="csrf-token"` im `<head>`.
    - grep zeigt `function getCsrfToken()` inline im File.
    - grep zeigt `'X-CSRFToken': getCsrfToken()` in exakt 3 Fetch-Blöcken (/api/login, /api/register, /waitlist/join).
    - `fetch('/waitlist/stats')` hat KEINEN X-CSRFToken-Header (unverändert).
    - deploy.sh production Gate grün MIT test_signup_journey collected; Browser-Live-Test grün (Sicht statt Zusicherung, CLAUDE.md).
  </acceptance_criteria>
  <done>Registrierung, Login und Warteliste liefern live keinen CSRF-400; test_register_flow_with_csrf grün; CSRFProtect weiterhin aktiv.</done>
</task>

<task type="auto">
  <name>Task 3: app.py — WTF_CSRF_TIME_LIMIT = None</name>
  <read_first>
    - app.py:49-61 — CSRFProtect(app) bei :54, WTF_CSRF_ENABLED=False-Override :58-60; WTF_CSRF_TIME_LIMIT ist NICHT gesetzt
  </read_first>
  <action>
    Füge nach app.py:60 (Ende des WTF_CSRF_ENABLED-Override-Blocks, vor dem H-20-Kommentar bei :62) ein:
    ```python
    app.config['WTF_CSRF_TIME_LIMIT'] = None   # D-04: kein Timeout — Anmelde-Seite darf >1h offen stehen ohne CSRF-400
    ```
    Der lokale `WTF_CSRF_ENABLED=False`-Override (:58-60) bleibt UNANGETASTET (D-04). KEIN Neu-Verdrahten von CSRFProtect.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate grün; danach inspect.sh grep 'WTF_CSRF_TIME_LIMIT' app.py auf Prod zur Bestätigung des deployed Stands.</automated>
  </verify>
  <acceptance_criteria>
    - grep von app.py zeigt genau eine Zeile `app.config['WTF_CSRF_TIME_LIMIT'] = None`.
    - Die Zeile steht NACH dem WTF_CSRF_ENABLED-Override-Block (:58-60 unverändert).
    - deploy.sh production Gate grün.
  </acceptance_criteria>
  <done>WTF_CSRF_TIME_LIMIT=None ist gesetzt; eine >1h offene Seite produziert keinen Timeout-400 mehr; Dev-Disable-Override unverändert.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/26)

### Angefasste Tabellen
- `users` (schreiben, indirekt) — der E2E-Test in Task 1 committet einen User+Org über den echten /api/register-Pfad. KEINE Schema-Änderung, KEINE neue Spalte.
- `organisations` (schreiben, indirekt) — dito via Registrierung.

### inspect.sh-Beleg (bestehende Spalten, aus RESEARCH Section 1 verifiziert)
`inspect.sh columns users` (Prod, VERIFIED 2026-07-04): u.a. `passwort_hash` (varchar, nullable), `is_superadmin` (bool, not null), `oauth_provider` (varchar, nullable). Registrierung schreibt neue Row mit bestehendem Schema — keine neuen Felder.

### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Leser läuft voraus? | Verifiziert? |
|---|---|---|---|---|---|
| User-Row (register) | Task-1-E2E committet via /api/register | DB-Spalte users.* (bestehend) | synchron im Request | N/A (kein async Leser) | ✓ Schema bestehend |
| csrf_token() Wert | server-side im GET-Render → Meta-Tag → JS liest → Header | RAM/Request-scope (Flask-WTF Session-Token) | synchron pro Request | Nein | ✓ Flask-WTF Standard |

Kein async Schreib/Lese-Versatz (Punkt 26): alle Schreib- und Lese-Vorgänge sind synchron request-scoped. Kein Hintergrund-Thread involviert.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymer Browser → /api/login, /api/register, /waitlist/join | untrusted state-changing POST-Requests kreuzen hier die Server-Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-01 | Spoofing (CSRF) | POST /api/login, /api/register, /waitlist/join | mitigate | X-CSRFToken-Header aus csrf_token()-Meta-Tag (D-01/A1); CSRFProtect bleibt aktiv, KEIN exempt |
| T-AUTH1-02 | Elevation (CSRF-Schutz versehentlich abgeschaltet) | app.py CSRF-Config | mitigate | A1 statt A2/A3 gewählt; nur TIME_LIMIT entschärft, ENABLED bleibt True in Prod; test_register_without_token_is_400 beweist Schutz aktiv |
| T-AUTH1-03 | Denial of Service (blind-grüner Test lässt Regression durch) | Deploy-Gate | mitigate | test_signup_journey MIT WTF_CSRF_ENABLED=True (nicht False); ERST-ROT-gegen-HEAD bewiesen |
</threat_model>

<verification>
- deploy.sh production Gate grün MIT test_signup_journey.py auto-collected.
- test_register_flow_with_csrf: rot gegen HEAD (vor Task 2/3), grün nach Fix — dokumentiert.
- test_register_without_token_is_400: grün (CSRF aktiv).
- Browser-Live-Test auf getnerve.app mit Test-User: registrieren + login + Warteliste eintragen — alle ohne 400 (Sicht statt Zusicherung).
</verification>

<success_criteria>
- Alle 3 state-changing Landing-Fetches schicken X-CSRFToken; kein 400 mehr.
- CSRFProtect aktiv (test beweist 400 ohne Token).
- WTF_CSRF_TIME_LIMIT=None gesetzt.
- E2E-Waechter im Deploy-Gate, gegen HEAD nachweislich rot gewesen.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-01-SUMMARY.md` erstellen (inkl. Rot-Beleg des E2E-Waechters, Browser-Live-Test-Beleg).
</output>
