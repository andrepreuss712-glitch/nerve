# Phase 08.23.2.AUTH-1: Eingangstür + Netz - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-04
**Phase:** 08.23.2.AUTH-1-eingangstuer-netz
**Areas discussed:** Founder-Konto (Superadmin E3), Rollen-Umbau-Reichweite, Warteliste-Berechtigungs-Loch

**Pflicht-Pre-Reads gelesen:** beide Vault-03-Planung-Pläne (Anmelde-/Team-/Billing-Plan + Team-Registrierung-Design, Fable 2026-07-04). Code-Anker verifiziert gegen echten Code (landing.html, app.py, auth.py, organisations.py, waitlist.py, changelog.py, auth_decorators.py, deploy.sh, Test-Muster).

---

## Founder-Konto (Superadmin-Härtung E3)

| Option | Description | Selected |
|--------|-------------|----------|
| Nur Google | Konto ohne Passwort, Zugang ausschließlich OAuth; Notausgang = DB-Handgriff | ✓ |
| Passwort als Notausgang | Zusätzlicher Passwort-Login als Rückfalltür | |

**User's choice:** Nur Google (Empfohlen) — E3 gelockt.
**Notes:** Bewusst kein Passwort-Rückfall; André hat direkten Server-Zugang für den Notfall. `auth.py:158` blockt Empty-Hash-Login bereits strukturell.

---

## Rollen-Umbau (session['rolle'] → g.user.rolle) — Reichweite

| Option | Description | Selected |
|--------|-------------|----------|
| Alles umstellen | Server-Schranken UND Menü-Sichtbarkeit gemeinsam; kein Halb-Zustand | ✓ |
| Nur Server-Schranken | Nur harte Zugriffs-Sperren, Menüs später | |

**User's choice:** Alles umstellen (Empfohlen).
**Notes:** Verhindert Punkt-29-Halb-Migration; Rollen-Änderungen wirken sofort statt erst nach Neu-Login. Betrifft Routes (organisations/dashboard/profiles/logs_routes/settings) + Templates (base/settings/profiles_list/logs_page).

---

## Warteliste-Berechtigungs-Loch (aus CONCERNS.md)

| Option | Description | Selected |
|--------|-------------|----------|
| Jetzt mitfixen | Warteliste-Verwaltung auf Superadmin einschränken | ✓ (verschärft) |
| Parken | Ins DSGVO/Auth-Paket verschieben | |

**User's choice:** NICHT parken — direkt in AUTH-1 fixen (Fable-geprüft).
**Notes:** André lieferte präzise Fable-geprüfte Vorgabe — ZWEI Löcher: `GET /waitlist/admin` (waitlist.py:138-141, fremde PII) + `POST /waitlist/invite/<id>` (waitlist.py:96-99, Gratis-EA-Plätze verschenkbar = Geschäftsschaden). Fix: `@login_required` + `@superadmin_required` (auth_decorators.py:5) auf beide, schwache flask_session-Zeilen raus. Wächter-Test nach Muster `test_admin_dashboard_auth.py`, ERST rot. Prod-Vorab: SUPERADMIN_EMAIL gesetzt + is_superadmin=True (sonst sperrt der Fix André selbst aus).

---

## Claude's Discretion
- Datei-Aufteilung der drei Wächter, JS-Token-Extraktions-Mechanik, CSRF-Guard-Whitelist-Repräsentation.

## Deferred Ideas
- Sibling-Loch `changelog.py:87` (identischer schwacher Check) — beim Rollen-Sweep mitbewerten, Superadmin-Scope-Frage im Plan an André hochziehen.
- AUTH-2..5 (Onboarding, Kassentür, Founder-Maske, Team/Sitze), DSGVO/Auth-Kunden-Rechte-Paket.

## Locked upstream (nicht diskutiert — Roadmap/Plan-fixiert)
- CSRF-Fix Weg A1, WTF_CSRF_TIME_LIMIT=None, „ERST rot gegen HEAD"-Disziplin, drei Wächter, kein Billing/Trial in dieser Phase.
