---
phase: 08.23.2.AUTH-1
plan: 03
subsystem: auth
tags: [authorization, superadmin, waitlist, changelog, pii, decorators]

requires:
  - phase: 08.23.2.AUTH-1-02
    provides: "serielles Deploy-Gate + scharfe Wächter (Plan-Kette 01→02→03)"
provides:
  - "GET /waitlist/admin + POST /waitlist/invite/<wid> + POST /changelog/admin nur noch für Superadmin (@login_required + @superadmin_required)"
  - "test_waitlist_admin_auth.py (anon→Redirect / Owner→403 / Superadmin→erlaubt, inkl. changelog/admin Owner→403)"
affects: [08.23.2.AUTH-1-04 (changelog.py:84 fällt aus dem Rollen-Sweep), AUTH-2]

tech-stack:
  added: []
  patterns:
    - "Auth-Härtung via Decorator-Stack @login_required + @superadmin_required (Wurzel statt Inline-Check)"

key-files:
  created:
    - tests/test_waitlist_admin_auth.py
  modified:
    - routes/waitlist.py
    - routes/changelog.py
    - .planning/ROADMAP.md

key-decisions:
  - "Task 0b: changelog/admin auf @superadmin_required (André-Direktive, DEFAULT gewählt)"
  - "Task 0 BLOCKING Prod-Vorab-Verify PASSED (is_superadmin=t, SUPERADMIN_EMAIL, oauth_provider=google) — kein Selbst-Aussperren"
  - "audit_log-append-only-Leak (bekannt, Migration 0026) als non-breaking toleriert; Backlog"

patterns-established:
  - "Selbst-Aussperr-Schutz: BLOCKING Prod-Vorab-Verify VOR jedem auth-sperrenden Edit"

requirements-completed: []

duration: ~1 Session (supervised, Task 0/0b Checkpoints + Gate E RED + Gate F GREEN)
completed: 2026-07-06
---

# Phase 08.23.2.AUTH-1 Plan 03: Waitlist/Changelog-Admin-Härtung Summary

**Drei bewiesene Auth-Löcher (schwacher `flask_session.get('rolle') != 'owner'`-Check → jeder Firmen-Owner erreichte fremde Interessenten-PII / konnte Gratis-EA-Plätze verschenken / fremd Changelog schreiben) auf `@login_required + @superadmin_required` gehärtet; ein Wächter nagelt Owner→403 fest — Superadmin-Zugang vorab bewiesen intakt.**

## Performance

- **Modus:** supervised inline (André fuhr Task-0-ssh-Verify + Gate E + Gate F)
- **Checkpoints (autonomous:false):** Task 0 [BLOCKING] Prod-Vorab-Verify → „verified"; Task 0b [DECISION] → „superadmin"
- **Deploy-Gates:** E (ERST-ROT, 3 Owner→403-Fälle rot, Gate blockte) → F (grün + Browser-Live-Test)
- **Tasks:** 3 (Wächter, waitlist.py, changelog.py)

## Accomplishments
- **PII-Leck + Geschäftsschaden geschlossen:** `/waitlist/admin` (fremde Interessenten-Liste) + `/waitlist/invite` (Gratis-EA-Platz) nur noch Superadmin.
- **Changelog-Sibling-Loch mitgefixt** (André-Direktive, Task 0b): `/changelog/admin` @login_required + @superadmin_required — inkl. anon-AttributeError-Schutz (Pitfall 3).
- **Selbst-Aussperr-Schutz bewiesen:** Task 0 Prod-Vorab (is_superadmin=t, SUPERADMIN_EMAIL, oauth_provider=google) VOR dem Edit → André kommt garantiert rein.
- **ERST-ROT bewiesen** (Gate E, 3 Owner→403 rot), grün nach Fix (Gate F), Live verifiziert (Superadmin lädt /waitlist/admin sauber, 0/50).

## Task Commits

1. **Task 1: test_waitlist_admin_auth.py (ERST-ROT gegen HEAD)** — `1fa89b0` (test)
2. **Task 2: waitlist.py admin+invite härten** — `4b25abe` (feat)
3. **Task 3: changelog.py add_entry härten** — `c4ffe07` (feat)

## Files Created/Modified
- `tests/test_waitlist_admin_auth.py` — Wächter (anon/owner/superadmin × admin/invite/changelog), Cleanup inkl. invite-Side-Effect
- `routes/waitlist.py` — @login_required + @superadmin_required auf beide Routen, schwacher Owner-Check raus
- `routes/changelog.py` — @login_required + @superadmin_required auf add_entry, schwacher Owner-Check raus
- `.planning/ROADMAP.md` — Backlog 999.6 (Login-Cookie-Domain)

## ERST-ROT Rot-Belege (Gate E, verbatim — André 2026-07-06, 3 failed / 893 passed, Gate blockte)

- `test_waitlist_admin_auth.py::test_admin_normal_owner_gets_403` FAILED (Owner kommt durch den schwachen Check → 200 statt 403)
- `test_waitlist_admin_auth.py::test_invite_owner_gets_403` FAILED (Owner darf einladen)
- `test_waitlist_admin_auth.py::test_changelog_admin_owner_gets_403` FAILED (Owner schreibt Changelog)

Superadmin/anon-Tests grün in beiden Ständen. Nach Task 2/3: alle grün (Gate F).

## Deviations from Plan
None im Code — Plan exakt umgesetzt (Decorator-Stack, schwache Checks entfernt, Imports wie admin_dashboard.py-Muster).

## Issues Encountered

**1. [Bekannter Leak — non-breaking] audit_log-append-only-Cleanup-Warnung (Gate E)**
- **Symptom:** `[BASELINE-AUTO-FIX]`-Warnung auf allen 3 Owner-Tests: `audit_log [3-28]` + `organisations` + `users` „nach Retry-Loop nicht loeschbar".
- **Diagnose (verifiziert, nicht angenommen):** NICHT von Plan 03 eingeschleppt. Meine Tests loggen sich NIE ein (setzen Session via `session_transaction`), und weder die Ziel-Routen (admin/invite/changelog) noch der globale `before_request` (`app.py:2091 _load_user`, nur READ) schreiben audit_log. Der Leak ist die bekannte append-only-Klasse: `services/audit.py` schreibt audit_log in LOGIN-Flow-Tests der Suite; Migration 0026 sperrt DELETE → diese Rows + ihre FK-Eltern (users/orgs) sind uncleanbar; der kumulative Baseline-Guard meldet sie auf jedem Folge-Test-Teardown. Meine EIGENEN Rows (waitlist/invitations/changelog/tenant_orgs) wurden sauber geräumt (NICHT in der Warnung) — mein defensiver Cleanup funktioniert.
- **Impact:** keiner (893 passed; Baseline-Guard *warnt* nur bei leaked, hard-failt nur bei missing/mutated). Konsistent mit Plan 01 D-05 (audit_log by-design uncleanbar → dort register-only). Getrackt in der Test-Cleanup-Härtungs-Folge-Phase.

**2. [Kein Bug] „Login-Loop" beim Live-Test = Apex-vs-Subdomain-Cookie**
- Beim Live-Test schien `getnerve.app/waitlist/admin` als Superadmin auszuloggen. Ursache: der Session-Cookie ist subdomain-gebunden (`app.getnerve.app`); die **Apex**-Adresse `getnerve.app` ist ausgeloggt → 302. Auf `app.getnerve.app` lädt alles korrekt. → Backlog **999.6** (SESSION_COOKIE_DOMAIN `.getnerve.app` prüfen vs. Apex-Redirect).

## Next Phase Readiness
- **Plan 04 bereit (letzter AUTH-1-Plan, autonomous:true):** Rollen-Sweep `session['rolle']`→`g.user.rolle` (5 Routes + 4 Templates, base.html:79 None-Guard) + `test_rolle_source_sweep.py`. **Notiz:** `changelog.py:84` ist durch diesen Plan aus dem Sweep heraus (liest keine session-Rolle mehr) — Plan 04 muss changelog.py NICHT mehr anfassen.
- **Backlog:** 999.4 (Flask-Admin-CSRF), 999.5 (deploy-prune), 999.6 (Cookie-Domain), audit_log-Cleanup (Test-Härtungs-Phase).

---
*Phase: 08.23.2.AUTH-1*
*Plan: 03*
*Completed: 2026-07-06*
