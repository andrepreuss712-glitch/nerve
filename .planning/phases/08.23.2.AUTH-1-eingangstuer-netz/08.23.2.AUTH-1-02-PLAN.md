---
phase: 08.23.2.AUTH-1
plan: 02
type: execute
wave: 1
complexity: "🟡 mittel (Auth/Security — Cross-AI Gemini PFLICHT vor Execute)"
depends_on: []
files_modified:
  - tests/test_csrf_fetch_guard.py
  - tests/test_template_existence_guard.py
autonomous: true
requirements: []   # Scope = CONTEXT-Decisions D-06, D-07
user_setup: []

must_haves:
  truths:
    - "Jeder zukünftige state-changing fetch() in templates/** oder static/*.js ohne X-CSRFToken wird vom Deploy-Gate ROT gemeldet (mit Datei:Zeile)"
    - "Jeder render_template('...')-String im Code, dessen Template-Datei fehlt, wird vom Deploy-Gate ROT gemeldet"
    - "Beide Waechter sind gegen eine bewusst eingeschleuste Verletzung nachweislich ROT (beweist, dass sie beißen)"
    - "Die bewusst exempten Fetches (Stripe-Webhook, OAuth-Callbacks, GET-only /waitlist/stats) sind als Whitelist-frozenset (kein Kommentar-Marker) erfasst"
  artifacts:
    - path: "tests/test_csrf_fetch_guard.py"
      provides: "Statischer Sweep templates/**+static/*.js: state-changing fetch ohne X-CSRFToken → rot"
      contains: "X-CSRFToken"
    - path: "tests/test_template_existence_guard.py"
      provides: "Sweep render_template-Strings → Template-Datei muss existieren"
      contains: "render_template"
  key_links:
    - from: "tests/test_csrf_fetch_guard.py"
      to: "templates/** + static/*.js"
      via: "Regex-Sweep über fetch()-Blöcke mit POST/PUT/PATCH/DELETE"
      pattern: "fetch\\("
---

<objective>
Zwei statische Deploy-Gate-Waechter, die die Fehler-Klassen aus Plan 01 für immer festnageln — damit nie wieder ein token-loser Schreib-Fetch (der die Registrierung tötete) oder ein render_template auf eine fehlende Datei (Klasse „fehlende pricing.html") unbemerkt live geht. Beide laufen im deploy.sh-pytest-Gate mit (`-m "not live and not perf"`) und blocken den Restart bei Verletzung.

Purpose: Test-Netz spannen (D-06 CSRF-Sweep, D-07 Template-Existenz). Muster: `tests/test_no_live_global_state.py` (Verbotenes-Muster-Sweep + frozenset-Whitelist) + `tests/test_schild_guard.py` (Struktur).
Output: test_csrf_fetch_guard.py + test_template_existence_guard.py, beide ERST ROT gegen eine seeded Verletzung.

Komplexität: 🟡 mittel — Auth/Security → Cross-AI Gemini PFLICHT vor Execute. NICHT direkt executen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md

<interfaces>
<!-- Muster-Quellen (RESEARCH.md Section 4.2/4.3, verifiziert). -->

Whitelist-Muster (frozenset, wie _WHITELIST in test_no_live_global_state.py — NICHT Kommentar-Marker):
```python
_CSRF_EXEMPT = frozenset({
    '/stripe/webhook',            # Stripe-Webhook: externe Signatur-Prüfung statt CSRF
    'accounts.google.com',        # Google OAuth callback
    'login.microsoftonline.com',  # Microsoft OAuth callback
})
# GET-only wie /waitlist/stats sind KEIN Eintrag — der Sweep prüft nur POST/PUT/PATCH/DELETE-Fetches.
```

Sweep-Root-Idiom (test_no_live_global_state.py / test_schild_guard.py):
```python
from pathlib import Path
_ROOT = Path(__file__).parent.parent
```

Template-Existenz-Idiom (RESEARCH Section 4.3):
```python
for m in re.finditer(r"render_template\(['\"]([^'\"]+)['\"]", src):
    tmpl = m.group(1)
    if not (_ROOT / 'templates' / tmpl).exists():
        violations.append(f"{src_file.relative_to(_ROOT)} -> '{tmpl}' fehlt")
```
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR Production-Pfad. KEIN lokales pytest. -->
- `bash deploy.sh production` führt pytest auf nerve_test aus (deploy.sh:186-194). Beide neuen Tests werden auto-collected (kein live/perf-Marker).
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_csrf_fetch_guard.py — statischer Sweep, ERST ROT gegen seeded Verletzung</name>
  <read_first>
    - tests/test_no_live_global_state.py — Verbotenes-Muster-Sweep-Struktur + frozenset-Whitelist (VERIFIED Muster-Quelle)
    - tests/test_schild_guard.py:1-60 — _ROOT-Idiom + skip-Konvention
    - templates/marketing/landing.html:820-960 — die (nach Plan 01 gefixten) 3 Token-Fetches + der GET-Stats-Fetch; dienen als Positiv-Beispiele die grün sein müssen
    - RESEARCH.md Section 4.2 (Whitelist-Mechanik, Nicht-Source-Presence-False-Green-Hinweis)
    - CLAUDE.md „Test-Qualitaets-Regel" — dieser Guard ist ein Grenzfall (Muster-Sweep über Templates ist erlaubt, weil er ein Runtime-Constraint absichert das kein Function-Mock direkt testbar macht — analog test_no_live_global_state.py; Grund als Kommentar dokumentieren)
  </read_first>
  <behavior>
    - `_collect_stateful_fetches_without_csrf(path)`: liest die Datei, findet `fetch(`-Aufrufe mit `method:` in ('POST','PUT','PATCH','DELETE') (case-insensitiv, Quotes egal), prüft ob im selben Fetch-Options-Objekt ein `X-CSRFToken` vorkommt. Fehlt er UND das Fetch-Ziel ist nicht in `_CSRF_EXEMPT` → Verletzung mit `datei:zeile`.
    - `test_all_stateful_fetches_have_csrf_token`: scannt `templates/**` + `static/*.js`, sammelt Verletzungen, `assert not violations` mit Datei:Zeile-Liste in der Meldung.
    - Nach Plan 01: die 3 landing.html-Fetches haben X-CSRFToken → grün. Der GET-Stats-Fetch wird nicht als POST erkannt → ignoriert.
    - ERST-ROT-Beleg: eine temporäre seeded Verletzung (ein token-loser POST-fetch in einer Test-Fixture-HTML ODER kurzzeitig in landing.html) muss den Test rot färben; danach entfernen. Rot-Beleg im SUMMARY.
    - Whitelist als `frozenset` (kein Kommentar-Marker) — D-06/CONTEXT-Discretion.
    - ASCII: Funktions-/Variablen-Namen ASCII.
  </behavior>
  <action>
    Erstelle `tests/test_csrf_fetch_guard.py`. `_ROOT = Path(__file__).parent.parent`. `_CSRF_EXEMPT = frozenset({'/stripe/webhook', 'accounts.google.com', 'login.microsoftonline.com'})`.

    `_collect_stateful_fetches_without_csrf(path)`: `src = path.read_text(encoding='utf-8', errors='replace')`. Regex-Ansatz (Claude's Discretion für Präzision): pro `fetch(`-Vorkommen den umgebenden Options-Block (~bis zur schließenden Klammer / nächsten `fetch(`) betrachten; wenn `method` in POST/PUT/PATCH/DELETE (regex case-insensitiv) UND kein `X-CSRFToken` im Block UND das URL-Argument nicht in `_CSRF_EXEMPT` → Verletzung `f\"{path.relative_to(_ROOT)}:{lineno}\"` sammeln. Zeilennummer über `src[:match.start()].count('\\n')+1`.

    `test_all_stateful_fetches_have_csrf_token()`: `scan = list((_ROOT/'templates').rglob('*.html')) + list((_ROOT/'static').glob('*.js'))`; Verletzungen über alle sammeln; `assert not violations, \"fetch() ohne X-CSRFToken:\\n  \" + \"\\n  \".join(violations)`.

    Kommentar am File-Kopf: Grund warum Muster-Sweep hier erlaubt ist (Runtime-Constraint „kein token-loser Schreib-Fetch", kein Function-Mock testet das direkt — CLAUDE.md Grenzfall).

    ERST-ROT: Vor dem finalen Grün eine seeded Verletzung einbauen (temporär, z.B. in einer wegwerf tests/_fixtures HTML), deploy.sh-Gate rot beobachten, entfernen. Rot-Beleg dokumentieren.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate grün mit test_csrf_fetch_guard collected; seeded-Verletzung-Variante war zuvor rot (dokumentiert). Voraussetzung: Plan 01 hat landing.html gefixt (sonst legitim rot).</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_csrf_fetch_guard.py existiert; grep zeigt `_CSRF_EXEMPT = frozenset(` (kein Kommentar-Marker-Ansatz).
    - grep zeigt Scan über `templates` UND `static`.
    - Rot-Beleg: mit einer seeded token-losen POST-fetch-Verletzung blockt deploy.sh (dokumentiert im SUMMARY).
    - Nach Entfernen der Verletzung + Plan-01-Fix: Gate grün.
    - File-Kopf-Kommentar begründet den Muster-Sweep-Grenzfall.
  </acceptance_criteria>
  <done>CSRF-Fetch-Guard fängt jeden zukünftigen token-losen Schreib-Fetch, ist gegen seeded Verletzung nachweislich rot, exempt-Liste als frozenset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_template_existence_guard.py — render_template-String → Datei existiert, ERST ROT gegen seeded Verletzung</name>
  <read_first>
    - tests/test_schild_guard.py:1-60 — Struktur (Sweep + assert-Liste)
    - RESEARCH.md Section 4.3 (render_template-Regex-Idiom)
    - RESEARCH.md Open Question 2 (pricing.html — der Guard läuft und findet selbst, ob ein toter render_template-String existiert)
  </read_first>
  <behavior>
    - `test_render_template_files_exist`: scannt `routes/*.py` + `app.py` nach `render_template('...')`-Strings; für jeden String prüft, ob `templates/<string>` existiert; fehlende → Verletzung mit `datei -> 'tmpl' fehlt`.
    - Läuft gegen aktuellem HEAD: wenn ein toter render_template('pricing.html') o.ä. existiert, meldet der Guard das (dann ist es ein echter Fund → im SUMMARY als Mini-Entscheidung hochziehen, NICHT eigenmächtig Template anlegen). Falls kein Fund: Guard grün gegen HEAD.
    - ERST-ROT-Beleg: eine seeded `render_template('does_not_exist.html')`-Zeile temporär → Guard rot → entfernen. Rot-Beleg im SUMMARY.
    - ASCII: Identifier ASCII.
  </behavior>
  <action>
    Erstelle `tests/test_template_existence_guard.py`. `_ROOT = Path(__file__).parent.parent`. `test_render_template_files_exist()`: iteriere `list((_ROOT/'routes').glob('*.py')) + [_ROOT/'app.py']`; pro Datei `for m in re.finditer(r\"render_template\\(['\\\"]([^'\\\"]+)['\\\"]\", src): tmpl = m.group(1); if not (_ROOT/'templates'/tmpl).exists(): violations.append(f\"{src_file.relative_to(_ROOT)} -> '{tmpl}' fehlt\")`. `assert not violations, \"render_template auf fehlende Datei:\\n  \" + \"\\n  \".join(violations)`.

    Discretion (D-07/CONTEXT): eigenständige Datei ODER Funktion in test_csrf_fetch_guard.py — hier als eigenständige Datei umgesetzt (klarere Trennung, gewählt).

    ERST-ROT: seeded `render_template('does_not_exist.html')` temporär in einer Test-Route/Fixture, Gate rot beobachten, entfernen. Rot-Beleg dokumentieren. Falls der Guard gegen HEAD einen ECHTEN fehlenden Template findet → NICHT auto-fixen, im SUMMARY als Fund an André hochziehen.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate grün mit test_template_existence_guard collected (falls kein echter fehlender Template auf HEAD); seeded-Variante war rot (dokumentiert).</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_template_existence_guard.py existiert; grep zeigt `render_template\(` Regex + Scan über routes + app.py.
    - Rot-Beleg: seeded render_template('does_not_exist.html') blockt deploy.sh (dokumentiert).
    - Falls echter fehlender Template auf HEAD gefunden: als Fund im SUMMARY, KEIN eigenmächtiges Template-Anlegen.
  </acceptance_criteria>
  <done>Template-Existenz-Guard fängt jeden render_template auf fehlende Datei, gegen seeded Verletzung nachweislich rot.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation
N/A — beide Waechter sind rein statische Datei-Sweeps (templates/**, static/*.js, routes/*.py, app.py). KEIN DB-Lesen/Schreiben, keine Tabelle angefasst, kein async Schreib/Lese-Versatz (Punkt 26). Damit ist die Persistenz-Schicht-Sektion nicht anwendbar.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zukünftiger Entwickler-Commit → Deploy-Gate | neue Templates/JS könnten die CSRF-Regression oder fehlende-Template-Klasse wieder einschleppen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-04 | Spoofing (CSRF-Regression durch neuen token-losen Fetch) | templates/**, static/*.js | mitigate | test_csrf_fetch_guard: statischer Sweep blockt Deploy bei token-losem Schreib-Fetch; ERST-ROT bewiesen |
| T-AUTH1-05 | Denial of Service (500/Broken-Page durch render_template auf fehlende Datei) | routes/*.py, app.py | mitigate | test_template_existence_guard blockt Deploy bei fehlendem Template |
| T-AUTH1-06 | Tampering (Waechter blind-grün, fängt nichts) | die Waechter selbst | mitigate | ERST-ROT-gegen-seeded-Verletzung für BEIDE Guards; Whitelist als frozenset explizit |
</threat_model>

<verification>
- deploy.sh production Gate grün mit beiden Guards auto-collected.
- Jeder Guard: gegen seeded Verletzung nachweislich rot (dokumentiert), danach grün.
- Falls test_template_existence_guard einen echten fehlenden Template findet → im SUMMARY als Fund hochgezogen (nicht auto-fixed).
</verification>

<success_criteria>
- CSRF-Fetch-Guard + Template-Existenz-Guard im Deploy-Gate.
- Beide gegen seeded Verletzung rot bewiesen.
- Whitelist als frozenset; keine Source-Presence-False-Green-Fallen.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-02-SUMMARY.md` (inkl. Rot-Belege beider Guards, ggf. echter Template-Fund).
</output>
