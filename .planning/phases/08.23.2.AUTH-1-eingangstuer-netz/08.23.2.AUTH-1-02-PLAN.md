---
phase: 08.23.2.AUTH-1
plan: 02
type: execute
wave: 2
complexity: "🟡 mittel (Auth/Security — Cross-AI Fable erfolgt; dieser Replan IST die Incorporation der Fable-Seam-Audit-Funde Z1/Z2 + der 2. Fable-Ground-Truth-Recheck-Funde: Multiline-render_template, _-Prefix-Include-Snippets, tote onboarding.html)"
depends_on: [01]
files_modified:
  - tests/test_csrf_fetch_guard.py
  - tests/test_template_existence_guard.py
  - templates/onboarding.html   # ENTFERNT (git rm) — tote standalone-Datei, siehe Task 3 (FACT B)
autonomous: false   # Task 3 (onboarding.html-Loeschung) STOPPT, falls Re-Grep einen Live-Caller findet
requirements: []   # Scope = CONTEXT-Decisions D-06, D-07

must_haves:
  truths:
    - "Der Wrapper-Mechanismus (Plan 01: `window.fetch =`) ist im Deploy-Gate nachweisbar in base.html VORHANDEN — fehlt der Wrapper-Marker in base.html, ist das Gate ROT (jedes base-erbende Template verliert sonst still seine CSRF-Abdeckung)"
    - "Jedes standalone-Template (kein `{% extends %}`) mit einem state-changing fetch (POST/PUT/PATCH/DELETE) traegt SELBST den `<meta name=\"csrf-token\"`-Tag UND den `window.fetch =`-Wrapper-Marker — sonst Gate ROT (landing.html ist der bekannte standalone-Fall). Include-Snippets (Dateiname beginnt mit `_`, z.B. `templates/admin/_tab_ausgaben.html`) sind AUSGENOMMEN: sie laufen im Kontext einer base-erbenden Wrapper-Seite, ihre Abdeckung kommt vom base.html-Wrapper"
    - "Jeder raw XMLHttpRequest-Write (`.open('POST'|'PUT'|'PATCH'|'DELETE'`) und jedes `navigator.sendBeacon(` in templates/** ODER static/** (rekursiv, Vendor ausgeschlossen) umgeht den window.fetch-Wrapper und wird vom Deploy-Gate ROT gemeldet (mit Datei:Zeile), ausser es zielt auf einen `_CSRF_EXEMPT`-Endpunkt"
    - "Nur die bewusst exempten Endpunkte (Stripe-Webhook, Google/Microsoft-OAuth-Callbacks) sind als Whitelist-frozenset erfasst"
    - "Jeder render_template('...')-String im Code — AUCH die MULTILINE-Form (Template-String auf der NAECHSTEN Zeile nach `render_template(`, wie in admin_dashboard.py:70, admin_ewb.py:94, dashboard.py:965, payments.py:140) — dessen Template-Datei fehlt, wird vom Deploy-Gate ROT gemeldet — AUSSER die bewusst noch nicht existierende `pricing.html` (in `_TEMPLATE_EXEMPT` mit selbst-loeschendem AUTH-3-Marker)"
    - "Die tote standalone-Datei `templates/onboarding.html` ist entfernt (git rm) — sie traegt state-changing fetch(es) ohne Wrapper, wird aber nirgends gerendert (kein render_template/include; onboarding.py-Routen sind redirect-Stubs), also geloescht statt exempt; AUTH-2 baut Onboarding neu"
    - "Beide Waechter sind gegen eine bewusst eingeschleuste Verletzung nachweislich ROT (beweist, dass sie beißen) — der Template-Guard zusaetzlich gegen eine MULTILINE-formulierte Seed-Verletzung (beweist, dass die Multiline-Regex-Erweiterung beißt)"
  artifacts:
    - path: "tests/test_csrf_fetch_guard.py"
      provides: "Drei-Check-Guard: (1) Wrapper-Marker in base.html vorhanden, (2) standalone-Template-Eigenabdeckung (meta+wrapper), _-Prefix-Include-Snippets ausgenommen, (3) rekursiver Forbidden-Bypass-Sweep (XHR-Write/sendBeacon) templates/** + static/** ohne Vendor"
      contains: "window.fetch"
    - path: "tests/test_template_existence_guard.py"
      provides: "Sweep render_template-Strings (single-line UND multiline) → Template-Datei muss existieren; `_TEMPLATE_EXEMPT` frozenset fuer noch-nicht-existierende pricing.html (AUTH-3)"
      contains: "_TEMPLATE_EXEMPT"
  key_links:
    - from: "tests/test_csrf_fetch_guard.py"
      to: "templates/base.html (Wrapper-Marker) + standalone-Templates (ohne _-Prefix-Snippets) + templates/** + static/** (rekursiv, ohne Vendor)"
      via: "Wrapper-Praesenz-Grep + standalone-Eigenabdeckung + Forbidden-Bypass-Regex (XHR-Write/sendBeacon)"
      pattern: "window\\.fetch ="
    - from: "tests/test_template_existence_guard.py"
      to: "routes/*.py + app.py render_template-Strings (single-line + multiline) → templates/**"
      via: "Whitespace-tolerante render_template-Regex (`\\s*` zwischen ( und Quote) + _TEMPLATE_EXEMPT-Skip"
      pattern: "render_template\\(\\s*['\"]"
---

<objective>
Zwei statische Deploy-Gate-Waechter, die die Fehler-Klassen aus Plan 01 für immer festnageln — **nach dem Fable-Seam-Audit umgebaut** (Z1/Z2) und **nach dem 2. Fable-Ground-Truth-Recheck** gegen die REALEN Templates/Dateien nachgeschaerft (Multiline-render_template, `_`-Prefix-Include-Snippets, tote onboarding.html), damit sie das Gate NICHT dauerhaft ROT machen UND keine echte Verletzung durch die Maschen rutscht.

**Kern-Umbau Z1 (CSRF-Guard):** Der alte Guard prüfte per-Call-Token-Text („jeder state-changing fetch braucht X-CSRFToken im Quelltext"). Das war falsch: Plan 01 lässt die 42 bestehenden Fetches im Quelltext UNVERÄNDERT — der globale `window.fetch`-Wrapper injiziert den Token zur LAUFZEIT, der Token steht nie im Source. Der alte Guard hätte 42 token-lose-im-Text-Fetches als Verletzung gewertet → permanent ROT → jeder Deploy der gesamten AUTH-Kette blockiert. Der neue Guard verifiziert stattdessen den **Wrapper-Mechanismus** in drei Checks: (1) Wrapper-Marker `window.fetch =` in base.html vorhanden, (2) standalone-Templates tragen Meta-Tag + Wrapper selbst, (3) rekursiver Forbidden-Bypass-Sweep fängt raw `XMLHttpRequest`-Writes und `navigator.sendBeacon(` — genau die Requests, die den window.fetch-Wrapper umgehen. XHR/sendBeacon sind damit AKTIV gefangen (stärker als vorher, wo sie nur als „akzeptiertes Rest-Risiko" dokumentiert waren).

**Ground-Truth-Nachschaerfung Check 2 (FACT C — `_`-Prefix-Include-Snippets):** Es gibt ~8 Dateien mit `_`-Prefix im Basename (`templates/admin/_tab_ausgaben.html`, `templates/_tooltip.html`, `templates/_beispiel_profil_modal.html`, ...). Das sind `{% include %}`-Fragmente, die IM Kontext einer base.html-erbenden Seite laufen — sie tragen bewusst kein `{% extends %}`. Ein naiver standalone-Check (kein `{% extends %}` = standalone) würde sie fälschlich als uncovered standalone flaggen → permanent ROT. Fix: Check 2 überspringt jede Datei, deren Basename mit `_` beginnt (Projekt-Konvention: Include-Snippet, base-Wrapper deckt sie zur Laufzeit ab).

**Kern-Umbau Z2 + Ground-Truth-Nachschaerfung (Template-Guard):** `payments.py:140-141` referenziert `render_template('pricing.html')` — aber als MULTILINE-Aufruf (`render_template(` auf Z.140, `'pricing.html',` auf Z.141). Die alte Regex `render_template\(['"]` matcht nur SINGLE-LINE → sie hätte diesen (und 3 weitere) Multiline-Calls VERPASST. Zwei Folgen: (a) die `pricing.html`-Exemption hätte NIE gefeuert (Guard bleibt fälschlich grün auf einem realen Miss), (b) jedes künftige fehlende, multiline-gerenderte Template rutscht durch. Fix: Regex `render_template\(\s*['"]` (Whitespace/Newline zwischen `(` und Quote erlaubt — Python `\s` matcht `\n`, Suche über den GESAMTEN Datei-Text, nicht zeilenweise). Damit werden die 4 realen Multiline-Calls (admin_dashboard.py:70, admin_ewb.py:94, dashboard.py:965, payments.py:140) entdeckt, die pricing.html-Exemption feuert TATSÄCHLICH, und `_TEMPLATE_EXEMPT = frozenset({'pricing.html'})` mit selbst-loeschendem AUTH-3-Marker verhindert falsches ROT auf HEAD.

**Tote onboarding.html löschen (FACT B):** `templates/onboarding.html` ist eine standalone-Datei mit state-changing fetch(es) OHNE Wrapper → Check 2 würde sie flaggen. Sie ist aber verifiziert TOT: kein `render_template`/`{% include %}` rendert sie (grep über routes/ + templates/ = 0 Live-Treffer; nur `_design_export/` + Planungs-Docs), und `routes/onboarding.py` (wizard/complete/create_profile) sind reine `redirect(url_for('dashboard.index'))`-Stubs. Daher: LÖSCHEN (git rm) statt exempt. AUTH-2 baut Onboarding neu. Die Löschung passiert VOR dem Grün-Lauf des CSRF-Guards (gleicher Plan/gleiche Welle).

Beide Guards laufen im deploy.sh-pytest-Gate (`-m "not live and not perf"`) und blocken den Restart bei Verletzung.

Purpose: Test-Netz spannen (D-06 CSRF-Wrapper-Verifikation app-weit, D-07 Template-Existenz inkl. Multiline-Calls) OHNE das Gate an HEAD dauerhaft ROT zu färben (Multiline-Coverage + `_`-Prefix-Ausschluss + tote-onboarding.html-Löschung sind die Ground-Truth-Fixes). Muster: `tests/test_no_live_global_state.py` (Forbidden-Muster-Sweep + frozenset-Whitelist) + `tests/test_schild_guard.py` (Struktur).
Output: test_csrf_fetch_guard.py (Drei-Check, Include-Snippets ausgenommen) + test_template_existence_guard.py (whitespace-tolerante Regex, `_TEMPLATE_EXEMPT` fuer pricing.html) + `git rm templates/onboarding.html`. Beide Guards ERST ROT gegen seeded Verletzungen (Template-Guard zusaetzlich gegen eine MULTILINE-Seed).

Komplexität: 🟡 mittel — Auth/Security. Cross-AI (Fable-Seam-Audit + 2. Ground-Truth-Recheck) mit diesem Replan eingearbeitet. NICHT direkt executen — dieser Replan geht zurück in die Verifikation von Plan 02.

**HINWEIS (Abhängigkeit zu Plan 01):** Der CSRF-Guard ist gruen nur, WEIL Plan 01 den Wrapper-Marker `window.fetch =` in base.html + landing.html eingebaut hat. `depends_on: [01]` + `wave: 2` — Plan 01 muss zuerst deployed sein, sonst faellt Check (1) legitim an der fehlenden Wrapper-Praesenz in base.html (Rot attribuierbar auf „Wrapper noch nicht da").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-FABLE-REVIEW.md

<interfaces>
<!-- Muster-Quellen (RESEARCH.md Section 4.2/4.3, verifiziert) + Wrapper-Marker aus Plan 01 Task 2 + Ground-Truth-Fakten aus dem 2. Fable-Recheck (grep-belegt, siehe unten). -->

**Wrapper-Marker aus Plan 01 (VERIFIED aus 08.23.2.AUTH-1-01-PLAN.md Task 2):**
Plan 01 installiert den globalen Wrapper in base.html UND landing.html mit der Override-Zeile:
```javascript
window.fetch = function (input, init) {
```
Der grep-verifizierbare Literal-Marker ist die Zeichenkette `window.fetch =`. Plan 01 Task 2 acceptance_criteria greppt SELBST nach exakt diesem String. Der Guard greppt daher nach DEMSELBEN Literal — kein erfundener Name.

**Meta-Tag-Marker aus Plan 01 (VERIFIED base.html:6 + landing.html-Ergaenzung):**
```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```
Grep-Literal fuer standalone-Eigenabdeckung: `<meta name="csrf-token"`.

**GROUND-TRUTH FACT A — 4 MULTILINE render_template-Calls (grep-belegt 2026-07-05):**
```
routes/admin_dashboard.py:70   render_template(
routes/admin_dashboard.py:71       'admin/dashboard.html',
routes/admin_ewb.py:94         render_template(
routes/admin_ewb.py:95             'admin/ewb_quality.html',
routes/dashboard.py:965        render_template(
routes/dashboard.py:966            'session_detail.html',
routes/payments.py:140         render_template(
routes/payments.py:141             'pricing.html',      <- DIESER Call ist der, den die pricing.html-Exemption matchen MUSS
```
Die alte Single-Line-Regex `render_template\(['"]` verpasst ALLE VIER. Die whitespace-tolerante Regex `render_template\(\s*['"]` (ueber den ganzen Datei-Text, `\s` matcht `\n`) faengt sie.

**GROUND-TRUTH FACT B — templates/onboarding.html ist TOT (grep-belegt 2026-07-05):**
- `grep -rn onboarding.html routes/` → 0 Treffer (kein render_template)
- `grep -rn "include.*onboarding" templates/` → 0 Treffer (kein {% include %})
- Nur-Referenzen: `_design_export/08_auth_onboarding.html`, `_design_export/README.md`, Planungs-Docs (KEIN Live-Code)
- `routes/onboarding.py` wizard()/complete()/create_profile_from_template() → alle `return redirect(url_for('dashboard.index'))` (Z.168/175/182, Stubs seit 08.19.5.2)
→ KEIN Live-Caller → Loeschen statt exempt. AUTH-2 baut Onboarding neu.

**GROUND-TRUTH FACT C — `_`-Prefix-Include-Snippets (Glob-belegt 2026-07-05):**
```
templates/admin/_tab_ausgaben.html   templates/admin/_tab_einnahmen.html
templates/admin/_tab_eur.html        templates/admin/_tab_export.html
templates/admin/_tab_kunden.html     templates/admin/_tab_uebersicht.html
templates/_tooltip.html              templates/_beispiel_profil_modal.html
```
Projekt-Konvention: Basename mit `_`-Prefix = `{% include %}`-Snippet, laeuft im Kontext einer base-erbenden Seite (der base.html-Wrapper deckt es zur Laufzeit ab). Check 2 MUSS solche Dateien ueberspringen, sonst falsches ROT (kein `{% extends %}` → faelschlich als standalone gewertet).

Whitelist-Muster (frozenset, wie _WHITELIST in test_no_live_global_state.py — NICHT Kommentar-Marker):
```python
_CSRF_EXEMPT = frozenset({
    '/stripe/webhook',            # Stripe-Webhook: externe Signatur-Pruefung statt CSRF
    'accounts.google.com',        # Google OAuth callback
    'login.microsoftonline.com',  # Microsoft OAuth callback
})
# GET-only wie /waitlist/stats sind KEIN Eintrag — der Sweep prueft nur POST/PUT/PATCH/DELETE.
```

Template-Exempt-Muster (frozenset, mirror-Style zur CSRF-Whitelist — NICHT loser Kommentar):
```python
_TEMPLATE_EXEMPT = frozenset({
    'pricing.html',  # 'pricing.html': AUTH-3 Welle 3 liefert die Datei und LOESCHT diesen Eintrag
})
```

Sweep-Root-Idiom (test_no_live_global_state.py / test_schild_guard.py):
```python
from pathlib import Path
_ROOT = Path(__file__).parent.parent
```

Vendor-Ausschluss (bundled 3rd-party JS NICHT scannen — Fable should-include):
```python
_VENDOR_DIRS = ('vendor', 'lib', 'libs', 'min', 'node_modules', 'dist')
# Ausschluss ueber Pfad-Teile ODER Dateiname-Suffix '.min.js'.
```

Standalone-Erkennung (kein base.html-Erbe), tolerant gegen `{%-`-Trim-Variante (FIX 4):
```python
import re
# Ein Template ist "standalone" wenn es KEIN {% extends %} enthaelt.
# Tolerant gegen die Whitespace-Trim-Schreibweise {%- extends %} / {%-extends%}
# (sonst wuerde ein getrimmtes extends faelschlich als standalone gewertet).
_EXTENDS_RE = re.compile(r"\{%-?\s*extends")
def _is_standalone(src: str) -> bool:
    return _EXTENDS_RE.search(src) is None
```

`_`-Prefix-Include-Snippet-Ausschluss (FIX 2):
```python
# Projekt-Konvention: Basename mit '_'-Prefix = {% include %}-Fragment, laeuft im
# Kontext einer base-erbenden Seite -> base.html-Wrapper deckt es zur Laufzeit ab.
# Beispiele: templates/admin/_tab_ausgaben.html, templates/_tooltip.html.
def _is_include_snippet(path: Path) -> bool:
    return path.name.startswith('_')
```

Forbidden-Bypass-Muster (Z1 Check 3 — raw request APIs, die window.fetch umgehen):
```python
# XHR-Write: .open( mit state-changing Methode (case-insensitiv)
_XHR_WRITE = re.compile(r"""\.open\(\s*['"](POST|PUT|PATCH|DELETE)['"]""", re.IGNORECASE)
# sendBeacon: IMMER POST
_SENDBEACON = re.compile(r"navigator\.sendBeacon\(")
```

Template-Existenz-Idiom (RESEARCH Section 4.3) — WHITESPACE-TOLERANT (FIX 1, faengt Multiline):
```python
# \s* zwischen ( und Quote -> faengt die 4 Multiline-Calls (payments.py:140 etc.)
for m in re.finditer(r"render_template\(\s*['\"]([^'\"]+)['\"]", src):
    tmpl = m.group(1)
    if Path(tmpl).name in _TEMPLATE_EXEMPT:
        continue
    if not (_ROOT / 'templates' / tmpl).exists():
        violations.append(f"{src_file.relative_to(_ROOT)} -> '{tmpl}' fehlt")
```
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR Production-Pfad. KEIN lokales pytest. -->
- `bash deploy.sh production` fuehrt pytest auf nerve_test aus (deploy.sh:186-194). Beide neuen Tests werden auto-collected (kein live/perf-Marker).
- Voraussetzung fuer gruen: Plan 01 (globaler Wrapper `window.fetch =` in base.html + landing.html) ist bereits deployed (depends_on: [01]).
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_csrf_fetch_guard.py — Drei-Check-Guard (Wrapper-Praesenz + standalone-Eigenabdeckung MIT _-Prefix-Include-Ausschluss + Forbidden-Bypass-Sweep), ERST ROT gegen seeded sendBeacon-Bypass</name>
  <read_first>
    - tests/test_no_live_global_state.py — Forbidden-Muster-Sweep-Struktur + frozenset-Whitelist + „KEIN Source-Presence-False-Green"-Docstring-Muster (VERIFIED Muster-Quelle)
    - tests/test_schild_guard.py:1-52 — _ROOT-Idiom + skip-Konvention + Docstring-Design-Block
    - 08.23.2.AUTH-1-01-PLAN.md Task 2 — der Wrapper-Marker-Literal `window.fetch =`, den Plan 01 in base.html + landing.html einbaut (GREP-ZIEL des Guards; kein erfundener Name — Plan 01 greppt selbst nach diesem String)
    - templates/base.html + templates/marketing/landing.html — nach Plan 01: base.html traegt `window.fetch =` (Check 1 gruen); landing.html ist standalone (kein `{% extends %}`) und traegt `<meta name="csrf-token"` + `window.fetch =` selbst (Check 2 gruen)
    - GROUND-TRUTH FACT C — `_`-Prefix-Include-Snippets. RE-VERIFIZIEREN vor Bau: `ls templates/**/_*.html` bzw. glob `templates/**/_*.html` → muss `templates/admin/_tab_ausgaben.html`, `templates/_tooltip.html`, `templates/_beispiel_profil_modal.html` u.a. zeigen. Diese laufen als `{% include %}` im base-Kontext → Check 2 MUSS sie ueberspringen (Basename beginnt mit `_`).
    - RESEARCH.md Section 4.2 (Whitelist-Mechanik, Nicht-Source-Presence-False-Green-Hinweis)
    - FABLE-REVIEW Seam-Audit Z1 (Guard prueft WRAPPER-Mechanismus, nicht per-Call-Token-Text; XHR/sendBeacon aktiv fangen statt nur dokumentieren)
    - CLAUDE.md „Test-Qualitaets-Regel" — dieser Guard ist ein VALIDER Muster-Sweep-Grenzfall: er prueft (a) Wrapper-Praesenz (ein Runtime-Constraint „jeder base-erbende fetch ist token-abgedeckt", kein Function-Mock testet das direkt) + (b) ein VERBOTENES Muster (raw XHR-Write/sendBeacon umgeht den Wrapper) — analog test_no_live_global_state.py. KEIN Source-Presence-False-Green: entfernt jemand den Wrapper-Marker → rot; fuegt jemand einen Bypass hinzu → rot. Grund als File-Kopf-Kommentar dokumentieren.
  </read_first>
  <behavior>
    Drei Checks, jeder als eigene assert-sammelnde Logik, alle Verletzungen in EINE Liste, EINE assert-Message pro Test-Funktion.

    - **Check 1 — Wrapper-Praesenz in base.html:** `test_base_html_carries_fetch_wrapper`: liest `templates/base.html`, assertiert dass der Literal-Marker `window.fetch =` (aus Plan 01) enthalten ist. Fehlt er → Verletzung (jedes base-erbende Template verliert sonst still die CSRF-Abdeckung). Assertion-Message EXAKT `"base.html ohne window.fetch-Wrapper (Plan 01 Marker fehlt): templates/base.html"`.

    - **Check 2 — standalone-Eigenabdeckung (MIT `_`-Prefix-Include-Ausschluss, FIX 2):** `test_standalone_templates_self_cover_csrf`: fuer jedes `.html` in `templates/**` (rglob, Vendor ausgeschlossen, UND `_`-Prefix-Include-Snippets ausgeschlossen via `_is_include_snippet`), das (a) standalone ist (`_is_standalone`: kein `{% extends %}`, tolerant gegen `{%-`-Trim-Variante) UND (b) einen state-changing `fetch(` mit `method:` in POST/PUT/PATCH/DELETE enthaelt → assertiere dass die Datei SELBST BEIDE Marker traegt: `<meta name="csrf-token"` UND `window.fetch =`. Fehlt einer → Verletzung `datei (kein meta-token / kein wrapper)`. landing.html ist der bekannte standalone-Fall und MUSS beide tragen → gruen. `_`-Prefix-Dateien (z.B. `templates/admin/_tab_ausgaben.html`) werden UEBERSPRUNGEN (Include-Snippet, base-Wrapper deckt sie ab) — mit Justification-Kommentar am Skip. Assertion-Message EXAKT `"standalone-Template mit state-changing fetch ohne Eigenabdeckung (meta-token + window.fetch-Wrapper):\n  " + "\n  ".join(violations)`.

    - **Check 3 — Forbidden-Bypass-Sweep:** `test_no_raw_request_bypass`: scannt **rekursiv** `templates/**` (rglob '*.html') + `static/**` (rglob '*.js'), MIT Vendor-Ausschluss (`_VENDOR_DIRS`-Pfadteile + `.min.js`-Suffix uebersprungen), sucht (a) `.open('POST'|'PUT'|'PATCH'|'DELETE'` (case-insensitiv, XHR-Write) und (b) `navigator.sendBeacon(` (immer POST). Jeder Treffer, dessen umgebendes URL-Argument NICHT in `_CSRF_EXEMPT` liegt → Verletzung mit `datei:zeile`. Diese Requests umgehen den window.fetch-Wrapper → aktiv gefangen. Assertion-Message EXAKT `"raw XHR-Write / sendBeacon umgeht den window.fetch-Wrapper:\n  " + "\n  ".join(violations)` — dieser String IST der Rot-Beleg-Anker fuer den Seed.

    - Whitelist als `frozenset` (kein Kommentar-Marker) — D-06.
    - **File-Kopf-Kommentar MUSS aktualisiert sein:** Der Guard verifiziert (1) Wrapper-Praesenz + (2) standalone-Coverage (Include-Snippets `_`-Prefix ausgenommen) + (3) Forbidden-Bypass (XHR-Write/sendBeacon). Die RESIDUALEN, statisch NICHT fangbaren Loecher (FIX 4, explizit aufgelistet): (a) `fetch` ueber eine aliasierte/Options-Variable, die der statische Scan nicht aufloest; (b) ein GEFAELSCHTER `window.fetch =`-Marker INNERHALB eines HTML/JS-KOMMENTARS wuerde den naiven Grep befriedigen (marker-fake); (c) die `{%- extends %}`/`{%-extends%}`-Whitespace-Trim-Variante — hier bereits toleriert durch `_EXTENDS_RE = \{%-?\s*extends`, damit ein getrimmtes extends nicht faelschlich als standalone gilt. XMLHttpRequest + navigator.sendBeacon sind AKTIV gefangen (Check 3) → sie stehen NICHT in der „akzeptiert"-Liste.
    - ASCII: Funktions-/Variablen-Namen ASCII.
  </behavior>
  <action>
    Erstelle `tests/test_csrf_fetch_guard.py`. `_ROOT = Path(__file__).parent.parent`. `_CSRF_EXEMPT = frozenset({'/stripe/webhook', 'accounts.google.com', 'login.microsoftonline.com'})`. `_VENDOR_DIRS = ('vendor', 'lib', 'libs', 'min', 'node_modules', 'dist')`.

    Konstanten fuer die Marker (grep-verifizierbar, exakt aus Plan 01):
    ```python
    _WRAPPER_MARKER = 'window.fetch ='        # Plan 01 Task 2 Override-Zeile
    _META_TOKEN_MARKER = '<meta name="csrf-token"'
    ```

    Vendor-Ausschluss-Helfer `_is_vendor(path)`: True wenn ein Pfad-Teil in `_VENDOR_DIRS` liegt ODER der Dateiname auf `.min.js` endet → solche Dateien NICHT scannen (bundled 3rd-party JS).

    `_`-Prefix-Include-Snippet-Helfer (FIX 2):
    ```python
    def _is_include_snippet(path: Path) -> bool:
        # Projekt-Konvention: '_'-Prefix-Basename = {% include %}-Fragment, laeuft im
        # Kontext einer base-erbenden Seite -> base.html-Wrapper deckt es zur Laufzeit ab.
        # Beispiele: templates/admin/_tab_ausgaben.html, templates/_tooltip.html.
        return path.name.startswith('_')
    ```

    Standalone-Helfer (FIX 4 — tolerant gegen `{%-`-Trim-Variante):
    ```python
    _EXTENDS_RE = re.compile(r"\{%-?\s*extends")
    def _is_standalone(src: str) -> bool:
        return _EXTENDS_RE.search(src) is None
    ```

    Forbidden-Bypass-Regexe:
    ```python
    _XHR_WRITE = re.compile(r"""\.open\(\s*['"](POST|PUT|PATCH|DELETE)['"]""", re.IGNORECASE)
    _SENDBEACON = re.compile(r"navigator\.sendBeacon\(")
    ```

    Helfer `_line_no(src, pos)`: `return src[:pos].count('\n') + 1`.

    Helfer `_target_is_exempt(src, match)`: extrahiere (best-effort) das erste String-Literal nach der Match-Position (die Ziel-URL); gib True zurueck wenn ein `_CSRF_EXEMPT`-Eintrag als Substring darin vorkommt.

    **Check 1** — `test_base_html_carries_fetch_wrapper()`:
    ```python
    base = _ROOT / 'templates' / 'base.html'
    src = base.read_text(encoding='utf-8', errors='replace')
    assert _WRAPPER_MARKER in src, \
        "base.html ohne window.fetch-Wrapper (Plan 01 Marker fehlt): templates/base.html"
    ```

    **Check 2** — `test_standalone_templates_self_cover_csrf()`: iteriere `[p for p in (_ROOT/'templates').rglob('*.html') if not _is_vendor(p) and not _is_include_snippet(p)]` (Kommentar am `_is_include_snippet`-Skip: `# _-Prefix = {% include %}-Snippet, base-Wrapper deckt es zur Laufzeit ab (FACT C)`); pro Datei `src = p.read_text(...)`; wenn `_is_standalone(src)` UND ein state-changing fetch vorkommt (regex `fetch\(` mit `method:` POST/PUT/PATCH/DELETE im Options-Block, case-insensitiv) → pruefe `_WRAPPER_MARKER in src` UND `_META_TOKEN_MARKER in src`; fehlt einer → `violations.append(f"{p.relative_to(_ROOT)} (fehlt: {'meta-token ' if _META_TOKEN_MARKER not in src else ''}{'wrapper' if _WRAPPER_MARKER not in src else ''})")`. `assert not violations, "standalone-Template mit state-changing fetch ohne Eigenabdeckung (meta-token + window.fetch-Wrapper):\n  " + "\n  ".join(violations)`.

    **Check 3** — `test_no_raw_request_bypass()`: `scan = [p for p in list((_ROOT/'templates').rglob('*.html')) + list((_ROOT/'static').rglob('*.js')) if not _is_vendor(p)]`; pro Datei `src = p.read_text(...)`; fuer jeden `_XHR_WRITE`- und `_SENDBEACON`-Match: wenn NICHT `_target_is_exempt(src, m)` → `violations.append(f"{p.relative_to(_ROOT)}:{_line_no(src, m.start())}")`. `assert not violations, "raw XHR-Write / sendBeacon umgeht den window.fetch-Wrapper:\n  " + "\n  ".join(violations)`.

    File-Kopf-Kommentar (aktualisiert): (a) Grund warum Muster-Sweep hier VALIDE ist (Wrapper-Praesenz = Runtime-Constraint „jeder base-erbende Schreib-fetch ist token-abgedeckt"; Forbidden-Bypass = verbotenes Muster; kein Function-Mock testet das direkt — CLAUDE.md-Grenzfall, KEIN Source-Presence-False-Green da Wrapper-Entfernen ODER Bypass-Hinzufuegen den Test roetet); (b) drei Checks aufgelistet: Wrapper-Praesenz base.html, standalone-Eigenabdeckung (Include-Snippets `_`-Prefix ausgenommen — FACT C), Forbidden-Bypass XHR-Write/sendBeacon; (c) RESIDUALE Loecher (FIX 4, explizit): (i) `fetch` ueber eine aliasierte/Options-Variable, die der statische Scan nicht aufloest; (ii) ein GEFAELSCHTER `window.fetch =`-Marker innerhalb eines HTML/JS-KOMMENTARS befriedigt den naiven Grep (marker-fake); (iii) `{%-`-Trim-extends bereits toleriert (`_EXTENDS_RE`). **XMLHttpRequest + navigator.sendBeacon sind AKTIV GEFANGEN (Check 3), NICHT mehr akzeptiert.**

    **WICHTIG — Scan-Root:** Check 3 scannt `templates/**` + `static/**` rekursiv. Der seeded Fixture-Pfad MUSS im Scan-Bereich liegen und NICHT in einem `_VENDOR_DIRS`-Pfad und NICHT auf `_CSRF_EXEMPT` zielen. (Der `static/_seed_bypass.js`-Seed hat `_`-Prefix, aber Check 3 hat KEINEN `_is_include_snippet`-Ausschluss — nur Check 2 hat den — also wird der Seed in Check 3 gescannt. Nur NICHT `.min.js`, nicht in `_VENDOR_DIRS`.)

    ERST-ROT-Workflow (Test-Netz-Ratsche) — Seed trippt die NEUE Logik (ein token-loser-im-Text fetch ist KEINE Verletzung mehr):
    1. Seede eine temporaere Bypass-Verletzung an EXAKT `static/_seed_bypass.js` (im static-Baum, damit rglob('*.js') sie fangt; NICHT `.min.js`, NICHT in `_VENDOR_DIRS`):
       ```javascript
       navigator.sendBeacon('/api/seed_probe', JSON.stringify({}));
       ```
       Ziel `/api/seed_probe` ist NICHT in `_CSRF_EXEMPT` → garantierte Verletzung in Check 3 (sendBeacon = POST, umgeht window.fetch-Wrapper).
    2. Gate ausfuehren: `bash deploy.sh production`.
    3. RED bestaetigen: Output MUSS `test_csrf_fetch_guard.py::test_no_raw_request_bypass` FAILED zeigen, mit `raw XHR-Write / sendBeacon umgeht den window.fetch-Wrapper:` gefolgt von `static/_seed_bypass.js:1`. Gate blockt Restart.
    4. (Optional, planner's discretion — mindestens der sendBeacon-Seed ist Pflicht) Zusaetzlich pruefen ob Check 2 beisst: einen standalone-Test-Template `templates/_seed_standalone.html` OHNE `{% extends %}`, MIT `fetch('/x', {method:'POST'})` und OHNE `window.fetch =`/`<meta name="csrf-token"` seeden. ACHTUNG: dieser Seed hat `_`-Prefix → wuerde von `_is_include_snippet` UEBERSPRUNGEN und NICHT rot. Um Check 2 zu beweisen, den Seed OHNE `_`-Prefix ablegen, z.B. `templates/zzz_seed_standalone.html` → `test_standalone_templates_self_cover_csrf` MUSS rot werden mit `templates/zzz_seed_standalone.html (fehlt: meta-token wrapper)`. Rot-Output ebenfalls verbatim ins SUMMARY.
    5. Rot-Output(s) verbatim in `08.23.2.AUTH-1-02-SUMMARY.md`.
    6. Seed(s) entfernen: `static/_seed_bypass.js` (und ggf. `templates/zzz_seed_standalone.html`) loeschen. KEIN Seed im finalen Commit.
    7. Gate erneut: `bash deploy.sh production` → GRUEN (Voraussetzung: Plan 01 deployed, base.html+landing.html tragen `window.fetch =`; onboarding.html geloescht — Task 3).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen mit test_csrf_fetch_guard collected (3 Checks: Wrapper-Praesenz base.html, standalone-Eigenabdeckung mit _-Prefix-Include-Ausschluss, Forbidden-Bypass XHR/sendBeacon rekursiv, Vendor ausgeschlossen); seeded-Variante (static/_seed_bypass.js: sendBeacon) war zuvor rot mit 'raw XHR-Write / sendBeacon umgeht den window.fetch-Wrapper: static/_seed_bypass.js:1' (dokumentiert). Voraussetzung: Plan 01 (window.fetch = in base.html + landing.html) deployed + onboarding.html geloescht (Task 3).</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_csrf_fetch_guard.py existiert; grep zeigt `_CSRF_EXEMPT = frozenset(` (kein Kommentar-Marker-Ansatz).
    - grep zeigt Check 1 grept nach dem Plan-01-Wrapper-Literal: `_WRAPPER_MARKER = 'window.fetch ='` UND ein `assert _WRAPPER_MARKER in src` gegen `templates/base.html`.
    - grep zeigt Check 2 standalone-Eigenabdeckung: `_is_standalone` (mit `_EXTENDS_RE = \{%-?\s*extends`) + `_is_include_snippet` (Basename `_`-Prefix-Skip) + Pruefung auf BEIDE Marker (`_WRAPPER_MARKER` + `_META_TOKEN_MARKER = '<meta name="csrf-token"'`).
    - grep zeigt der `_is_include_snippet`-Skip hat einen Justification-Kommentar (FACT C, base-Wrapper deckt Include-Snippets ab).
    - grep zeigt Check 3 Forbidden-Bypass: `_XHR_WRITE` (`.open(` mit POST/PUT/PATCH/DELETE, IGNORECASE) UND `_SENDBEACON` (`navigator.sendBeacon(`), REKURSIV (`rglob('*.html')` + `rglob('*.js')`) ueber `templates` UND `static`, plus Vendor-Ausschluss (`_VENDOR_DIRS` / `.min.js`).
    - File-Kopf-Kommentar nennt die DREI Checks + die residualen Loecher (aliasierte/Options-Variable-fetch, marker-fake im Kommentar, `{%-`-Trim toleriert); XHR/sendBeacon stehen als AKTIV GEFANGEN, NICHT als akzeptiert.
    - Rot-Beleg: SUMMARY.md enthaelt die verbatim RED-Output-Zeile fuer den Seed (`raw XHR-Write / sendBeacon umgeht den window.fetch-Wrapper:` + `static/_seed_bypass.js:1`), deploy.sh blockte daran.
    - Kein Seed-Rest: `ls static/_seed_bypass.js` (und ggf. `templates/zzz_seed_standalone.html`) zeigt die Datei(en) sind entfernt.
    - Nach Seed-Entfernen + Plan-01-Deploy + onboarding.html-Loeschung (Task 3): Gate gruen (die `_`-Prefix-Snippets loesen KEIN falsches ROT in Check 2 aus).
  </acceptance_criteria>
  <done>CSRF-Fetch-Guard verifiziert den Wrapper-Mechanismus in drei Checks (Wrapper-Praesenz base.html, standalone-Eigenabdeckung meta+wrapper mit `_`-Prefix-Include-Ausschluss, Forbidden-Bypass XHR-Write/sendBeacon rekursiv mit Vendor-Ausschluss), faengt aktiv jeden wrapper-umgehenden raw Request, ist gegen seeded sendBeacon-Verletzung nachweislich rot, Seed entfernt, exempt-Liste als frozenset, residuale Loecher (aliasierter fetch, marker-fake, `{%-`-Trim) dokumentiert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_template_existence_guard.py — render_template-String (SINGLE-LINE + MULTILINE) → Datei existiert, `_TEMPLATE_EXEMPT` fuer pricing.html (AUTH-3), ERST ROT gegen seeded single-line UND multiline Verletzung</name>
  <read_first>
    - tests/test_schild_guard.py:1-52 — Struktur (Sweep + assert-Liste) + EXCLUDED_TABLES-frozenset-Muster (analog fuer `_TEMPLATE_EXEMPT`)
    - tests/test_no_live_global_state.py:98-183 — frozenset-Whitelist-Style mit begruendenden Inline-Kommentaren (Muster fuer `_TEMPLATE_EXEMPT`)
    - GROUND-TRUTH FACT A — die 4 MULTILINE render_template-Calls. RE-VERIFIZIEREN vor Bau die EXAKTEN Zeilen (koennen sich verschoben haben): `grep -rn -U -A1 "render_template($" routes/` bzw. eine Multiline-Grep, die `render_template(` am Zeilenende + Template-String auf der Folgezeile findet. Erwartet: admin_dashboard.py (~70/71 'admin/dashboard.html'), admin_ewb.py (~94/95 'admin/ewb_quality.html'), dashboard.py (~965/966 'session_detail.html'), payments.py (~140/141 'pricing.html'). Die aktuellen Zeilen als Beleg im read_first/acceptance zitieren. DER payments.py-Call IST der, den die pricing.html-Exemption matchen muss.
    - RESEARCH.md Section 4.3 (render_template-Regex-Idiom — HIER whitespace-tolerant erweitert)
    - FABLE-REVIEW Seam-Audit Z2 (pricing.html: `payments.py` referenziert render_template('pricing.html') MULTILINE, Datei kommt erst AUTH-3 → Guard sonst permanent rot ODER — mit alter Single-Line-Regex — falsch gruen weil der Call nicht entdeckt; Loesung: whitespace-tolerante Regex + `_TEMPLATE_EXEMPT` mit selbst-loeschendem Marker)
  </read_first>
  <behavior>
    - `test_render_template_files_exist`: scannt `routes/*.py` + `app.py` nach `render_template(...)`-Strings — SINGLE-LINE UND MULTILINE (Regex `render_template\(\s*['\"]([^'\"]+)['\"]`, Suche ueber den GESAMTEN Datei-Text, `\s` matcht `\n`); fuer jeden String: wenn der Datei-Basename in `_TEMPLATE_EXEMPT` steht → ueberspringen; sonst pruefen, ob `templates/<string>` existiert; fehlende → Verletzung `datei -> 'tmpl' fehlt`. Assertion-Message EXAKT `"render_template auf fehlende Datei:\n  " + "\n  ".join(violations)`.
    - **Multiline-Coverage (FIX 1):** Die `\s*`-Erweiterung MUSS die 4 realen Multiline-Calls (admin_dashboard.py, admin_ewb.py, dashboard.py, payments.py) entdecken. Insbesondere feuert dadurch die pricing.html-Exemption TATSAECHLICH (der payments.py-Multiline-Call wird jetzt gesehen). Ohne diese Erweiterung waere der Guard falsch gruen auf einem realen Miss.
    - `_TEMPLATE_EXEMPT = frozenset({'pricing.html'})` mit MANDATORY Inline-Kommentar `# 'pricing.html': AUTH-3 Welle 3 liefert die Datei und LOESCHT diesen Eintrag` — die Datei existiert HEUTE bewusst nicht (kommt in AUTH-3), also ist das kein „fehlender-Template"-Fund, sondern eine bewusste, selbst-loeschende Ausnahme.
    - Laeuft gegen HEAD: findet der Guard einen ANDEREN (nicht-exempten) toten render_template-String → im SUMMARY als Mini-Entscheidung an André hochziehen (NICHT eigenmaechtig Template anlegen). Fuer pricing.html ist die Entscheidung GEMACHT (exempt) — KEINE Mini-Entscheidung mehr dafuer.
    - ASCII-Identifier + ASCII in Kommentar-Schluesselwoertern (`LOESCHT`, nicht `LÖSCHT`).
  </behavior>
  <action>
    Erstelle `tests/test_template_existence_guard.py`. `_ROOT = Path(__file__).parent.parent`.

    `_TEMPLATE_EXEMPT` als frozenset (mirror-Style zu `_CSRF_EXEMPT` / `EXCLUDED_TABLES`), MIT dem verbatim Pflicht-Kommentar auf der pricing.html-Zeile:
    ```python
    _TEMPLATE_EXEMPT = frozenset({
        'pricing.html',  # 'pricing.html': AUTH-3 Welle 3 liefert die Datei und LOESCHT diesen Eintrag
    })
    ```

    Whitespace-tolerante Regex (FIX 1 — `\s*` zwischen `(` und Quote faengt Multiline; `\s` matcht `\n`):
    ```python
    _RENDER_RE = re.compile(r"render_template\(\s*['\"]([^'\"]+)['\"]")
    ```

    `test_render_template_files_exist()`: iteriere `list((_ROOT/'routes').glob('*.py')) + [_ROOT/'app.py']`; pro Datei den GESAMTEN Text lesen (`src = f.read_text(...)`, NICHT zeilenweise — sonst matcht die Multiline-Regex nicht):
    ```python
    for m in _RENDER_RE.finditer(src):
        tmpl = m.group(1)
        if Path(tmpl).name in _TEMPLATE_EXEMPT:
            continue  # bewusst-noch-nicht-existierend (pricing.html: AUTH-3)
        if not (_ROOT / 'templates' / tmpl).exists():
            violations.append(f"{src_file.relative_to(_ROOT)} -> '{tmpl}' fehlt")
    ```
    `assert not violations, "render_template auf fehlende Datei:\n  " + "\n  ".join(violations)`.

    Kommentar an der Regex: `# \s* faengt die 4 MULTILINE-Calls (payments.py:140-141 pricing.html, admin_dashboard.py:70-71, admin_ewb.py:94-95, dashboard.py:965-966); Suche ueber den ganzen Datei-Text, \s matcht \n.` — die konkreten Zeilen vor dem Schreiben per grep RE-VERIFIZIEREN und ggf. anpassen.

    Discretion (D-07): eigenstaendige Datei (gewaehlt, klarere Trennung).

    **WICHTIG — Seed-Ort im Scan-Root (routes/):** ZWEI temporaere Wegwerf-Route-Dateien (single-line + multiline), beide auf ein NICHT-exemptes Template zielend, damit sie trotz `_TEMPLATE_EXEMPT` beissen.

    ERST-ROT-Workflow:
    1a. Seede SINGLE-LINE an EXAKT `routes/_seed_missing_template.py`:
       ```python
       from flask import render_template
       def _seed_probe():
           return render_template('does_not_exist_seed.html')
       ```
    1b. Seede MULTILINE an EXAKT `routes/_seed_missing_template_multiline.py` (String auf der NAECHSTEN Zeile nach `render_template(` — beweist die Multiline-Regex beisst):
       ```python
       from flask import render_template
       def _seed_probe_ml():
           return render_template(
               'does_not_exist_seed_ml.html',
               foo='bar',
           )
       ```
       Beide Ziel-Templates existieren nicht UND sind NICHT in `_TEMPLATE_EXEMPT` → garantierte Verletzungen. (Statischer String-Sweep — die Dateien werden nicht importiert, kein App-Bruch.)
    2. `bash deploy.sh production`.
    3. RED bestaetigen: `test_template_existence_guard.py::test_render_template_files_exist` FAILED mit `render_template auf fehlende Datei:` + BEIDEN Zeilen:
       - `routes/_seed_missing_template.py -> 'does_not_exist_seed.html' fehlt` (single-line-Pfad)
       - `routes/_seed_missing_template_multiline.py -> 'does_not_exist_seed_ml.html' fehlt` (MULTILINE-Pfad — beweist FIX 1 beisst)
       Gate blockt.
    4. BEIDE Rot-Output-Zeilen verbatim in `08.23.2.AUTH-1-02-SUMMARY.md` (single-line + multiline getrennt ausweisen).
    5. Seeds entfernen: `routes/_seed_missing_template.py` UND `routes/_seed_missing_template_multiline.py` loeschen. KEIN Seed im finalen Commit.
    6. `bash deploy.sh production` → GRUEN (pricing.html ist exempt → kein falscher Rot-Fund auf HEAD; die whitespace-tolerante Regex ENTDECKT den payments.py-Multiline-Call, aber pricing.html ist exempt → kein ROT).

    **pricing.html-Exemption-Beweis (acceptance, FIX 1):** Im SUMMARY dokumentieren, dass der Guard gegen HEAD NICHT auf pricing.html rot ist (weil exempt), UND dass der payments.py:140-141-Multiline-Call vom Guard ENTDECKT wird (belegbar: entfernte man `pricing.html` aus `_TEMPLATE_EXEMPT`, wuerde der Guard auf `routes/payments.py -> 'pricing.html' fehlt` rot — d.h. der Multiline-Call ist im Scan-Bereich). Diesen Beweis als kurze verbatim-Notiz (z.B. ein einmaliger lokaler Gedanken-Check ODER — sauberer im Kein-Local-Dev-Rahmen — ein Deploy-Lauf mit temporaer aus `_TEMPLATE_EXEMPT` entferntem pricing.html, der auf `routes/payments.py -> 'pricing.html' fehlt` rot geht, dann Eintrag wieder rein) im SUMMARY festhalten. Falls dieser Zusatz-Lauf zu teuer ist: mindestens der MULTILINE-Seed (1b) beweist die Regex-Erweiterung — das ist Pflicht.

    Falls der Guard gegen HEAD einen ECHTEN fehlenden Template findet, der NICHT in `_TEMPLATE_EXEMPT` steht → NICHT auto-fixen, im SUMMARY als Fund an André hochziehen (Mini-Entscheidung). Fuer pricing.html gilt das NICHT — die Entscheidung ist mit dem Exempt-Eintrag GEMACHT.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen mit test_template_existence_guard collected; whitespace-tolerante Regex entdeckt die 4 Multiline-Calls (payments.py:140-141 pricing.html u.a.); pricing.html via _TEMPLATE_EXEMPT uebersprungen (AUTH-3); seeded single-line (routes/_seed_missing_template.py) UND multiline (routes/_seed_missing_template_multiline.py) waren rot mit 'render_template auf fehlende Datei: ... does_not_exist_seed.html fehlt / ... does_not_exist_seed_ml.html fehlt' (beide dokumentiert).</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_template_existence_guard.py existiert; grep zeigt `_RENDER_RE = re.compile(r"render_template\(\s*['\"]` (whitespace-tolerant, `\s*` zwischen `(` und Quote) + Scan ueber routes + app.py mit GESAMT-Text-Read (nicht zeilenweise).
    - grep-Beleg: die 4 realen Multiline-Calls (admin_dashboard.py, admin_ewb.py, dashboard.py, payments.py) liegen im Scan-Bereich; Regex-Kommentar zitiert die (re-verifizierten) Zeilen.
    - grep zeigt `_TEMPLATE_EXEMPT = frozenset(` enthaelt `pricing.html` UND den verbatim Pflicht-Kommentar `# 'pricing.html': AUTH-3 Welle 3 liefert die Datei und LOESCHT diesen Eintrag` auf derselben Zeile.
    - grep zeigt die Existenz-Pruefung ueberspringt Basenames in `_TEMPLATE_EXEMPT` (`if Path(tmpl).name in _TEMPLATE_EXEMPT: continue`).
    - Rot-Beleg (BEIDE): SUMMARY.md enthaelt die verbatim RED-Output-Zeilen fuer BEIDE Seeds — single-line (`... does_not_exist_seed.html fehlt`) UND multiline (`... does_not_exist_seed_ml.html fehlt`), deploy.sh blockte daran. Der Multiline-Seed-Rot IST der Beweis, dass FIX 1 beisst.
    - pricing.html-Beweis: SUMMARY dokumentiert, dass der Guard pricing.html NICHT rot faerbt (exempt) UND dass der payments.py-Multiline-Call im Scan-Bereich liegt (WUERDE rot ohne Exemption).
    - Kein Seed-Rest: `ls routes/_seed_missing_template.py routes/_seed_missing_template_multiline.py` zeigt beide Dateien sind entfernt.
    - pricing.html loest KEINEN Rot-Fund aus (exempt); andere unerwartete fehlende Templates auf HEAD → als Fund im SUMMARY, KEIN eigenmaechtiges Template-Anlegen.
  </acceptance_criteria>
  <done>Template-Existenz-Guard faengt jeden render_template (SINGLE-LINE UND MULTILINE via `\s*`-Regex) auf fehlende NICHT-exempte Datei, entdeckt die 4 realen Multiline-Calls (payments.py:140-141 pricing.html u.a.), pricing.html via `_TEMPLATE_EXEMPT` mit selbst-loeschendem AUTH-3-Marker bewusst ausgenommen, gegen seeded single-line UND multiline Verletzung nachweislich rot, beide Seeds entfernt.</done>
</task>

<task type="auto">
  <name>Task 3: Tote templates/onboarding.html loeschen (git rm) — VOR dem CSRF-Guard-Gruen-Lauf; STOP falls Re-Grep einen Live-Caller findet (FACT B)</name>
  <read_first>
    - GROUND-TRUTH FACT B — templates/onboarding.html ist verifiziert TOT. RE-VERIFIZIEREN vor der Loeschung mit den EXAKTEN grep-Befehlen (Deadness-Beweis, Punkt 22 „Beweis statt Annahme"):
      1. `grep -rn "onboarding.html" routes/` → MUSS 0 Treffer (kein render_template)
      2. `grep -rn "onboarding.html" templates/` + `grep -rn "include.*onboarding" templates/` → MUSS 0 Treffer (kein {% include %})
      3. `grep -rn "onboarding.html" static/` → MUSS 0 Live-Treffer
      4. `routes/onboarding.py` Z.162-183 (wizard/complete/create_profile_from_template) → alle drei sind `return redirect(url_for('dashboard.index'))`-Stubs (verifiziert Z.168/175/182, Stub seit 08.19.5.2)
    - Bekannte NICHT-Live-Referenzen (kein Blocker): `_design_export/08_auth_onboarding.html`, `_design_export/README.md`, `.planning/`-Docs, `03 Planung/UI-Audit-Ergebnis-2026-05-03.md` — allesamt Doku/Export, KEIN Live-Render-Pfad.
    - CLAUDE.md „Kein-Local-Dev" — Verify auf `bash deploy.sh production`.
    - Begruendung fuer LOESCHEN statt EXEMPT: onboarding.html ist ein standalone-Template (kein `{% extends %}`) mit state-changing fetch(es) und OHNE window.fetch-Wrapper → Check 2 (Task 1) wuerde es flaggen. Da es verifiziert TOT ist, ist Loeschen die richtige Aufloesung (kein Guard-Exempt fuer toten Code). AUTH-2 baut Onboarding neu.
  </read_first>
  <action>
    **SICHERHEITS-GATE (STOP-Bedingung, Punkt 22):** Fuehre ZUERST die vier Re-Grep-Befehle aus `<read_first>` aus. Wenn IRGENDEINER von (1)/(2)/(3) einen Live-Treffer liefert — d.h. ein `render_template(... 'onboarding.html' ...)` in routes/ ODER ein `{% include 'onboarding.html' %}`/`{% include "onboarding.html" %}` in templates/ ODER ein aktiver static/-Referenz-Pfad — dann ist onboarding.html NICHT tot:
    - NICHT loeschen.
    - Den Fund (Datei:Zeile + Kontext) im SUMMARY als BLOCKER an André hochziehen.
    - Die Loeschung stoppen (dieser Task bleibt offen; `autonomous: false` — der Executor pausiert hier statt blind zu loeschen).

    NUR wenn alle drei Live-Grep 0 Treffer zeigen UND onboarding.py-Stubs bestaetigt sind:

    1. `git rm templates/onboarding.html` (aus dem Repo entfernen, nicht nur vom Dateisystem).
    2. Commit (im normalen Execute-Commit-Fluss): Message-Kern `chore(AUTH-1): remove dead templates/onboarding.html (0 render_template/include callers; onboarding.py stubs redirect to dashboard; AUTH-2 rebuilds). Verhindert falsches Check-2-ROT (standalone + state-changing fetch ohne Wrapper).`
    3. Finale Re-Grep als Beleg: `grep -rn "onboarding.html" routes/ templates/ static/` → weiterhin 0 Live-render/include-Treffer; `ls templates/onboarding.html` → Datei existiert nicht mehr.

    **Sequenzierung:** Diese Loeschung MUSS VOR dem finalen Gruen-Lauf des CSRF-Guards (Task 1 Schritt 7) passieren — Check 2 wird erst gruen, wenn die tote standalone-Datei weg ist. Reihenfolge im Plan/in der Welle: Task 3 (Loeschung) vor Task 1 Gruen-Lauf. (Task 1 + Task 3 liegen in derselben Welle/demselben Plan; der Executor loescht onboarding.html, BEVOR er den `deploy.sh production`-GRUEN-Lauf des CSRF-Guards macht.)
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen; test_csrf_fetch_guard Check 2 faerbt NICHT rot auf onboarding.html (Datei geloescht via git rm). Vorher-Beleg im SUMMARY: grep -rn 'onboarding.html' routes/ templates/ static/ zeigt 0 Live-render/include-Treffer (nur _design_export/Docs).</automated>
  </verify>
  <acceptance_criteria>
    - Re-Grep-Beleg im SUMMARY: `grep -rn "onboarding.html" routes/` = 0, `grep -rn "include.*onboarding" templates/` = 0, `grep -rn "onboarding.html" static/` = 0 Live-Treffer (nur _design_export/Docs) — VOR der Loeschung dokumentiert (Deadness-Beweis).
    - onboarding.py-Stub-Beleg im SUMMARY: wizard/complete/create_profile_from_template alle `redirect(url_for('dashboard.index'))` (Z.~168/175/182).
    - `ls templates/onboarding.html` → Datei ist entfernt (git rm, nicht mehr im Repo).
    - Finale Re-Grep: `grep -rn "onboarding.html" routes/ templates/ static/` zeigt keinen `render_template`/`include`, der die (jetzt geloeschte) Datei referenziert.
    - SICHERHEIT: Falls ein Live-Caller gefunden wurde → Datei NICHT geloescht, Fund als BLOCKER im SUMMARY, Task pausiert (autonomous:false).
    - Sequenz: onboarding.html geloescht VOR dem CSRF-Guard-Gruen-Lauf (Task 1) — im SUMMARY als Reihenfolge belegt.
  </acceptance_criteria>
  <done>templates/onboarding.html ist als verifiziert toter standalone-Code (0 render_template/include-Caller, onboarding.py redirect-Stubs) via `git rm` entfernt — verhindert falsches Check-2-ROT; Deadness vor Loeschung re-gegreppt; bei Live-Caller-Fund haette der Task gestoppt statt geloescht; AUTH-2 baut Onboarding neu.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation
N/A — beide Waechter sind rein statische Datei-Sweeps (templates/**, static/**, routes/*.py, app.py) + Marker-Grep (base.html/standalone-Templates), Task 3 ist ein `git rm` einer toten Template-Datei. KEIN DB-Lesen/Schreiben, keine Tabelle angefasst, kein async Schreib/Lese-Versatz (Punkt 26). onboarding.html ist eine Template-Datei (Praesentations-Schicht), keine Persistenz.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zukünftiger Entwickler-Commit → Deploy-Gate | neue Templates/JS koennten (a) den globalen window.fetch-Wrapper entfernen/nicht-erben, (b) per raw XHR-Write/sendBeacon den Wrapper umgehen, oder (c) einen render_template (single-line ODER multiline) auf eine fehlende Datei einschleppen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-04 | Spoofing (CSRF-Regression durch fehlenden/nicht-geerbten Wrapper) | templates/base.html (Wrapper-Praesenz) + standalone-Templates (Eigenabdeckung) | mitigate | test_csrf_fetch_guard Check 1 (base.html traegt `window.fetch =`) + Check 2 (standalone-Templates tragen meta-token + Wrapper selbst; `_`-Prefix-Include-Snippets ausgenommen — base-Wrapper deckt sie ab); ERST-ROT bewiesen (Check 2 optional-Seed OHNE `_`-Prefix) |
| T-AUTH1-04b | Spoofing (raw XHR-Write / navigator.sendBeacon umgeht den window.fetch-Wrapper) | templates/**, static/** (rekursiv, ohne Vendor) | mitigate | test_csrf_fetch_guard Check 3 faengt `.open('POST'|'PUT'|'PATCH'|'DELETE'` (IGNORECASE) + `navigator.sendBeacon(` app-weit; ERST-ROT via seeded sendBeacon-Bypass (static/_seed_bypass.js) bewiesen. (Zuvor akzeptiert — jetzt AKTIV gefangen.) |
| T-AUTH1-04c | Spoofing (residuale Loecher: aliasierter/Options-Variable-fetch; marker-fake im Kommentar; `{%-`-Trim-extends) | statisch nicht (voll) fangbare fetch-Aliase + Kommentar-getarnter Marker | accept (mit Teil-Mitigation) | dokumentiert im Guard-File-Kopf: (a) aliasierter fetch statisch nicht aufloesbar (window.fetch-Wrapper deckt Runtime-Pfad); (b) marker-fake in HTML/JS-Kommentar befriedigt naiven Grep — Rest-Risiko registriert; (c) `{%-`-Trim-extends ist BEREITS gefixt (`_EXTENDS_RE = \{%-?\s*extends`), kein Miss. Nur (a)+(b) bleiben Rest-Risiko |
| T-AUTH1-05 | Denial of Service (500/Broken-Page durch render_template auf fehlende Datei — auch MULTILINE-Call) | routes/*.py, app.py | mitigate | test_template_existence_guard mit whitespace-toleranter Regex (`\s*`) faengt SINGLE-LINE UND MULTILINE-Calls (4 reale Multiline-Sites: admin_dashboard/admin_ewb/dashboard/payments); blockt Deploy bei fehlendem NICHT-exemptem Template; pricing.html bewusst exempt (AUTH-3, selbst-loeschender Marker); ERST-ROT via single-line UND multiline Seed bewiesen |
| T-AUTH1-06 | Tampering (Waechter blind-gruen, faengt nichts) | die Waechter selbst | mitigate | ERST-ROT-gegen-seeded-Verletzung fuer BEIDE Guards (CSRF: sendBeacon-Bypass static/_seed_bypass.js; Template: nicht-exempter fehlender Template single-line UND multiline Seed; dokumentierter Red-Output); Whitelist + Exempt als frozenset explizit. Zusaetzlich: die Multiline-Regex-Luecke (falsch-gruen auf realem payments.py-Miss) IST der konkrete Blind-Gruen-Fall, den FIX 1 schliesst |
| T-AUTH1-07 | Information Disclosure / False-Green durch toten standalone Code (onboarding.html) | templates/onboarding.html | mitigate | Task 3 loescht die verifiziert tote Datei (git rm) statt sie zu exempten — kein toter standalone-Code, der Check 2 dauerhaft rot faerben oder per Exempt-Eintrag getarnt werden muesste; Deadness vor Loeschung re-gegreppt (0 render/include-Caller); STOP-Gate falls Live-Caller |
</threat_model>

<verification>
- deploy.sh production Gate gruen mit beiden Guards auto-collected (nach Plan 01: base.html + landing.html tragen `window.fetch =`; nach Task 3: onboarding.html geloescht).
- CSRF-Guard: Check 1 (Wrapper-Praesenz base.html) + Check 2 (standalone-Eigenabdeckung, `_`-Prefix-Include-Snippets ausgenommen) + Check 3 (Forbidden-Bypass XHR-Write/sendBeacon rekursiv ueber templates/** + static/** mit Vendor-Ausschluss); gegen seeded sendBeacon-Bypass rot (verbatim Red-Output), danach Seed entfernt, danach gruen. Die `_`-Prefix-Snippets (`templates/admin/_tab_ausgaben.html` u.a.) loesen KEIN falsches ROT.
- Template-Guard: whitespace-tolerante Regex entdeckt SINGLE-LINE UND die 4 realen MULTILINE-Calls; gegen seeded single-line UND multiline (nicht-exempte) Verletzung rot, danach gruen; pricing.html via `_TEMPLATE_EXEMPT` bewusst uebersprungen (der payments.py:140-141-Multiline-Call ist im Scan-Bereich). Anderer echter Template-Fund → im SUMMARY hochgezogen (nicht auto-fixed).
- onboarding.html: via git rm entfernt (verifiziert tot, 0 render/include-Caller, onboarding.py redirect-Stubs); bei Live-Caller-Fund waere der Task gestoppt statt geloescht.
</verification>

<success_criteria>
- CSRF-Fetch-Guard (Drei-Check: Wrapper-Praesenz base.html + standalone-Eigenabdeckung mit `_`-Prefix-Include-Ausschluss + Forbidden-Bypass XHR-Write/sendBeacon, rekursiv, Vendor-Ausschluss, residuale Loecher dokumentiert) + Template-Existenz-Guard (whitespace-tolerante Regex fuer single-line + multiline, `_TEMPLATE_EXEMPT` fuer pricing.html) im Deploy-Gate.
- Beide gegen seeded Verletzung rot bewiesen (Template zusaetzlich gegen MULTILINE-Seed), Seeds wieder entfernt.
- Tote templates/onboarding.html via git rm entfernt (verifiziert tot), NICHT als Guard-Exempt getarnt.
- Whitelist + Exempt als frozenset; keine Source-Presence-False-Green-Fallen (Wrapper-Entfernen ODER Bypass-Hinzufuegen roetet → valider Muster-Sweep, im File-Kopf begruendet).
- Guard macht das Gate an HEAD NICHT dauerhaft rot: die 42 token-losen-im-Text Fetches sind KEINE Verletzung (Wrapper injiziert zur Laufzeit); pricing.html ist exempt (und der Multiline-Call wird jetzt korrekt entdeckt); `_`-Prefix-Include-Snippets sind ausgenommen; onboarding.html geloescht.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-02-SUMMARY.md` (inkl. verbatim Rot-Belege beider Guards — CSRF sendBeacon-Bypass + ggf. standalone-Check-2-Seed OHNE `_`-Prefix; Template single-line UND multiline Seed; pricing.html-Exemption-Beweis; onboarding.html Deadness-Re-Grep + git-rm-Beleg; ggf. anderer echter Template-Fund).
</output>
