---
phase: 08.23.2.AUTH-1
plan: 02
type: execute
wave: 2
complexity: "🟡 mittel (Auth/Security — Cross-AI Fable erfolgt; dieser Replan IST die Incorporation)"
depends_on: [01]
files_modified:
  - tests/test_csrf_fetch_guard.py
  - tests/test_template_existence_guard.py
autonomous: true
requirements: []   # Scope = CONTEXT-Decisions D-06, D-07
user_setup: []

must_haves:
  truths:
    - "Jeder zukünftige state-changing fetch() irgendwo in templates/** ODER static/** (rekursiv, ohne Vendor-Bundles) ohne X-CSRFToken-Abdeckung wird vom Deploy-Gate ROT gemeldet (mit Datei:Zeile)"
    - "Der Guard passt gegen HEAD, WEIL der globale Wrapper (Plan 01) jeden state-changing fetch abdeckt — er faengt einen NEUEN raw XHR/sendBeacon oder einen explizit token-gestrippten fetch, der den Wrapper-Mechanismus umgeht"
    - "Nur die bewusst exempten Endpunkte (Stripe-Webhook, Google/Microsoft-OAuth-Callbacks) sind als Whitelist-frozenset erfasst"
    - "Jeder render_template('...')-String im Code, dessen Template-Datei fehlt, wird vom Deploy-Gate ROT gemeldet"
    - "Beide Waechter sind gegen eine bewusst eingeschleuste Verletzung nachweislich ROT (beweist, dass sie beißen)"
  artifacts:
    - path: "tests/test_csrf_fetch_guard.py"
      provides: "Rekursiver statischer Sweep templates/** + static/** (Vendor-Ausschluss): state-changing fetch ohne X-CSRFToken → rot; dokumentierte bekannte Bypasses"
      contains: "X-CSRFToken"
    - path: "tests/test_template_existence_guard.py"
      provides: "Sweep render_template-Strings → Template-Datei muss existieren"
      contains: "render_template"
  key_links:
    - from: "tests/test_csrf_fetch_guard.py"
      to: "templates/** + static/** (rekursiv, ohne Vendor)"
      via: "Regex-Sweep über fetch()-Blöcke mit POST/PUT/PATCH/DELETE"
      pattern: "fetch\\("
---

<objective>
Zwei statische Deploy-Gate-Waechter, die die Fehler-Klassen aus Plan 01 für immer festnageln. Mit dem globalen `window.fetch`-Wrapper (Plan 01) sind ALLE state-changing Fetches app-weit abgedeckt — der CSRF-Guard prüft daher **app-weit rekursiv** (nicht mehr nur landing.html, Fable BLOCKER 1 Guard-Seite): dass kein Template/Script einen state-changing Request macht, der den Wrapper-Mechanismus umgeht (ein NEUER raw `XMLHttpRequest`/`sendBeacon` oder ein explizit token-gestripptes `fetch`). Der zweite Guard nagelt die Klasse „render_template auf fehlende Datei" (fehlende pricing.html) fest. Beide laufen im deploy.sh-pytest-Gate (`-m "not live and not perf"`) und blocken den Restart bei Verletzung.

Purpose: Test-Netz spannen (D-06 CSRF-Sweep app-weit, D-07 Template-Existenz). Muster: `tests/test_no_live_global_state.py` (Verbotenes-Muster-Sweep + frozenset-Whitelist) + `tests/test_schild_guard.py` (Struktur).
Output: test_csrf_fetch_guard.py (rekursiv, Vendor-Ausschluss, dokumentierte Bypasses) + test_template_existence_guard.py, beide ERST ROT gegen eine seeded Verletzung.

Komplexität: 🟡 mittel — Auth/Security. Cross-AI (Fable) mit diesem Replan eingearbeitet. NICHT direkt executen.

**HINWEIS (Abhängigkeit zu Plan 01):** Der CSRF-Guard ist gruen nur, WEIL Plan 01 den globalen Wrapper eingebaut hat (jeder state-changing fetch ist abgedeckt). `depends_on: [01]` + `wave: 2` — Plan 01 muss zuerst deployed sein, sonst faellt der Guard legitim an den bislang token-losen Fetches (Rot nicht attribuierbar, Fable BLOCKER 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-CONTEXT.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-RESEARCH.md
@.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-FABLE-REVIEW.md

<interfaces>
<!-- Muster-Quellen (RESEARCH.md Section 4.2/4.3, verifiziert). -->

Whitelist-Muster (frozenset, wie _WHITELIST in test_no_live_global_state.py — NICHT Kommentar-Marker):
```python
_CSRF_EXEMPT = frozenset({
    '/stripe/webhook',            # Stripe-Webhook: externe Signatur-Pruefung statt CSRF
    'accounts.google.com',        # Google OAuth callback
    'login.microsoftonline.com',  # Microsoft OAuth callback
})
# GET-only wie /waitlist/stats sind KEIN Eintrag — der Sweep prueft nur POST/PUT/PATCH/DELETE.
```

Sweep-Root-Idiom (test_no_live_global_state.py / test_schild_guard.py):
```python
from pathlib import Path
_ROOT = Path(__file__).parent.parent
```

Vendor-Ausschluss (bundled 3rd-party JS NICHT scannen — Fable should-include):
```python
_VENDOR_DIRS = ('vendor', 'lib', 'libs', 'min', 'node_modules', 'dist')
# Ausschluss ueber Pfad-Teile ODER Dateiname-Suffix '.min.js'.
```

Template-Existenz-Idiom (RESEARCH Section 4.3):
```python
for m in re.finditer(r"render_template\(['\"]([^'\"]+)['\"]", src):
    tmpl = m.group(1)
    if not (_ROOT / 'templates' / tmpl).exists():
        violations.append(f"{src_file.relative_to(_ROOT)} -> '{tmpl}' fehlt")
```
</interfaces>

<verify_environment>
<!-- CLAUDE.md „HART: Kein Local-Dev" — Verify NUR Production-Pfad. KEIN lokales pytest. -->
- `bash deploy.sh production` fuehrt pytest auf nerve_test aus (deploy.sh:186-194). Beide neuen Tests werden auto-collected (kein live/perf-Marker).
- Voraussetzung fuer gruen: Plan 01 (globaler Wrapper) ist bereits deployed (depends_on: [01]).
</verify_environment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_csrf_fetch_guard.py — rekursiver app-weiter Sweep (Vendor-Ausschluss), ERST ROT gegen seeded Verletzung</name>
  <read_first>
    - tests/test_no_live_global_state.py — Verbotenes-Muster-Sweep-Struktur + frozenset-Whitelist (VERIFIED Muster-Quelle)
    - tests/test_schild_guard.py:1-60 — _ROOT-Idiom + skip-Konvention
    - templates/base.html + templates/marketing/landing.html — nach Plan 01: globaler window.fetch-Wrapper deckt alle state-changing Fetches ab (Positiv-Beispiele die gruen sein muessen)
    - RESEARCH.md Section 4.2 (Whitelist-Mechanik, Nicht-Source-Presence-False-Green-Hinweis)
    - FABLE-REVIEW BLOCKER 1 (Guard-Seite: app-weit, nicht nur landing) + should-include (rekursiv + Vendor-Ausschluss + bekannte Bypasses dokumentieren)
    - CLAUDE.md „Test-Qualitaets-Regel" — dieser Guard ist ein Grenzfall (Muster-Sweep ueber Templates ist erlaubt, weil er ein Runtime-Constraint absichert das kein Function-Mock direkt testbar macht — analog test_no_live_global_state.py; Grund als Kommentar dokumentieren)
  </read_first>
  <behavior>
    - `_collect_stateful_fetches_without_csrf(path)`: liest die Datei, findet `fetch(`-Aufrufe mit `method:` in ('POST','PUT','PATCH','DELETE') (case-insensitiv), prueft ob im selben Fetch-Options-Objekt ein `X-CSRFToken` vorkommt. Fehlt er UND das Ziel ist nicht in `_CSRF_EXEMPT` → Verletzung mit `datei:zeile`. (Der globale Wrapper deckt state-changing Fetches ab, aber ein explizit token-gestripptes fetch mit hartem `headers`-Objekt ohne Token IST eine bewusste Umgehung → soll gefangen werden.)
    - `test_all_stateful_fetches_have_csrf_token`: scannt **rekursiv** `templates/**` (rglob '*.html') + `static/**` (rglob '*.js'), MIT Vendor-Ausschluss (`_VENDOR_DIRS`-Pfadteile + `.min.js`-Suffix uebersprungen), sammelt Verletzungen, `assert not violations` mit Datei:Zeile-Liste. Assertion-Message EXAKT `"fetch() ohne X-CSRFToken:\n  " + "\n  ".join(violations)` — dieser String IST der Rot-Beleg-Anker.
    - Nach Plan 01: kein produktiver state-changing fetch ohne Token-Abdeckung → gruen. GET-Fetches werden nicht als POST erkannt → ignoriert.
    - Whitelist als `frozenset` (kein Kommentar-Marker) — D-06.
    - **File-Kopf-Kommentar (Fable should-include) MUSS die bekannten, statisch NICHT fangbaren Bypasses nennen:** (1) `fetch` ueber eine aliasierte/Options-Variable, (2) `XMLHttpRequest`, (3) `navigator.sendBeacon`. Der Guard + der Plan-01-Wrapper decken window.fetch ab; diese drei bleiben ausserhalb der statischen Reichweite und sind bewusst als Rest-Risiko dokumentiert.
    - ASCII: Funktions-/Variablen-Namen ASCII.
  </behavior>
  <action>
    Erstelle `tests/test_csrf_fetch_guard.py`. `_ROOT = Path(__file__).parent.parent`. `_CSRF_EXEMPT = frozenset({'/stripe/webhook', 'accounts.google.com', 'login.microsoftonline.com'})`. `_VENDOR_DIRS = ('vendor', 'lib', 'libs', 'min', 'node_modules', 'dist')`.

    Vendor-Ausschluss-Helfer `_is_vendor(path)`: True wenn ein Pfad-Teil in `_VENDOR_DIRS` liegt ODER der Dateiname auf `.min.js` endet → solche Dateien NICHT scannen (bundled 3rd-party JS).

    `_collect_stateful_fetches_without_csrf(path)`: `src = path.read_text(encoding='utf-8', errors='replace')`. Regex-Ansatz (Claude's Discretion fuer Praezision): pro `fetch(`-Vorkommen den umgebenden Options-Block betrachten; wenn `method` in POST/PUT/PATCH/DELETE (regex case-insensitiv) UND kein `X-CSRFToken` im Block UND das URL-Argument nicht in `_CSRF_EXEMPT` → Verletzung `f"{path.relative_to(_ROOT)}:{lineno}"`. Zeilennummer ueber `src[:match.start()].count('\n')+1`.

    `test_all_stateful_fetches_have_csrf_token()`: `scan = [p for p in list((_ROOT/'templates').rglob('*.html')) + list((_ROOT/'static').rglob('*.js')) if not _is_vendor(p)]`; Verletzungen ueber alle sammeln; `assert not violations, "fetch() ohne X-CSRFToken:\n  " + "\n  ".join(violations)`.

    File-Kopf-Kommentar: (a) Grund warum Muster-Sweep hier erlaubt ist (Runtime-Constraint „kein token-loser Schreib-Fetch", kein Function-Mock testet das direkt — CLAUDE.md Grenzfall); (b) die drei bekannten NICHT fangbaren Bypasses (aliasierte/Options-Variable-fetch, XMLHttpRequest, navigator.sendBeacon).

    **WICHTIG — Scan-Root:** Der Sweep scannt `templates/**` + `static/**` rekursiv. Der seeded Fixture-Pfad MUSS im Scan-Bereich liegen. Seede unter `templates/_seed_csrf_violation.html` (im templates-Baum, damit rglob('*.html') sie fangt), NICHT unter `tests/_fixtures/` und NICHT in einem `_VENDOR_DIRS`-Pfad.

    ERST-ROT-Workflow (Test-Netz-Ratsche):
    1. Seede eine temporaere Verletzung an EXAKT `templates/_seed_csrf_violation.html`:
       ```html
       <script>
       fetch('/api/seed_probe', {method: 'POST', headers: {'Content-Type': 'application/json'}, body: '{}'});
       </script>
       ```
       Ziel `/api/seed_probe` ist NICHT in `_CSRF_EXEMPT` → garantierte Verletzung. (Whitelist enthaelt nur Fetch-URL-Ziele, keine Dateipfade → keine Kollision.)
    2. Gate ausfuehren: `bash deploy.sh production`.
    3. RED bestaetigen: Output MUSS `test_csrf_fetch_guard.py::test_all_stateful_fetches_have_csrf_token` FAILED zeigen, mit `fetch() ohne X-CSRFToken:` gefolgt von `templates/_seed_csrf_violation.html:2`. Gate blockt Restart.
    4. Rot-Output verbatim in `08.23.2.AUTH-1-02-SUMMARY.md`.
    5. Seed entfernen: `templates/_seed_csrf_violation.html` loeschen. KEIN Seed im finalen Commit.
    6. Gate erneut: `bash deploy.sh production` → GRUEN (Voraussetzung: Plan 01 deployed).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen mit test_csrf_fetch_guard collected (rekursiv, Vendor ausgeschlossen); seeded-Variante (templates/_seed_csrf_violation.html) war zuvor rot mit 'fetch() ohne X-CSRFToken: templates/_seed_csrf_violation.html:2' (dokumentiert). Voraussetzung: Plan 01 (globaler Wrapper) deployed.</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_csrf_fetch_guard.py existiert; grep zeigt `_CSRF_EXEMPT = frozenset(` (kein Kommentar-Marker-Ansatz).
    - grep zeigt REKURSIVEN Scan (`rglob('*.html')` + `rglob('*.js')`) ueber `templates` UND `static`, plus Vendor-Ausschluss (`_VENDOR_DIRS` / `.min.js`).
    - File-Kopf-Kommentar nennt die drei bekannten Bypasses (aliasierte/Options-Variable-fetch, XMLHttpRequest, navigator.sendBeacon).
    - Rot-Beleg: SUMMARY.md enthaelt die verbatim RED-Output-Zeile fuer den Seed (`fetch() ohne X-CSRFToken:` + `templates/_seed_csrf_violation.html:2`), deploy.sh blockte daran.
    - Kein Seed-Rest: `ls templates/_seed_csrf_violation.html` zeigt die Datei ist entfernt.
    - Nach Seed-Entfernen + Plan-01-Deploy: Gate gruen.
  </acceptance_criteria>
  <done>CSRF-Fetch-Guard scannt app-weit rekursiv (Vendor ausgeschlossen), faengt jeden zukuenftigen token-losen Schreib-Fetch, ist gegen seeded Verletzung nachweislich rot, Seed entfernt, exempt-Liste als frozenset, bekannte Bypasses dokumentiert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_template_existence_guard.py — render_template-String → Datei existiert, ERST ROT gegen seeded Verletzung</name>
  <read_first>
    - tests/test_schild_guard.py:1-60 — Struktur (Sweep + assert-Liste)
    - RESEARCH.md Section 4.3 (render_template-Regex-Idiom)
    - RESEARCH.md Open Question 2 (pricing.html — der Guard laeuft und findet selbst, ob ein toter render_template-String existiert)
  </read_first>
  <behavior>
    - `test_render_template_files_exist`: scannt `routes/*.py` + `app.py` nach `render_template('...')`-Strings; fuer jeden String prueft, ob `templates/<string>` existiert; fehlende → Verletzung `datei -> 'tmpl' fehlt`. Assertion-Message EXAKT `"render_template auf fehlende Datei:\n  " + "\n  ".join(violations)`.
    - Laeuft gegen HEAD: wenn ein toter render_template existiert, meldet der Guard das (echter Fund → im SUMMARY als Mini-Entscheidung hochziehen, NICHT eigenmaechtig Template anlegen). Falls kein Fund: Guard gruen gegen HEAD.
    - ASCII-Identifier.
  </behavior>
  <action>
    Erstelle `tests/test_template_existence_guard.py`. `_ROOT = Path(__file__).parent.parent`. `test_render_template_files_exist()`: iteriere `list((_ROOT/'routes').glob('*.py')) + [_ROOT/'app.py']`; pro Datei `for m in re.finditer(r"render_template\(['\"]([^'\"]+)['\"]", src): tmpl = m.group(1); if not (_ROOT/'templates'/tmpl).exists(): violations.append(f"{src_file.relative_to(_ROOT)} -> '{tmpl}' fehlt")`. `assert not violations, "render_template auf fehlende Datei:\n  " + "\n  ".join(violations)`.

    Discretion (D-07): eigenstaendige Datei (gewaehlt, klarere Trennung).

    **WICHTIG — Seed-Ort im Scan-Root (routes/):** Seede in einer temporaeren Wegwerf-Route-Datei `routes/_seed_missing_template.py`.

    ERST-ROT-Workflow:
    1. Seede an EXAKT `routes/_seed_missing_template.py`:
       ```python
       from flask import render_template
       def _seed_probe():
           return render_template('does_not_exist_seed.html')
       ```
       `templates/does_not_exist_seed.html` existiert nicht → garantierte Verletzung. (Statischer String-Sweep — die Datei wird nicht importiert, kein App-Bruch.)
    2. `bash deploy.sh production`.
    3. RED bestaetigen: `test_template_existence_guard.py::test_render_template_files_exist` FAILED mit `render_template auf fehlende Datei:` + `routes/_seed_missing_template.py -> 'does_not_exist_seed.html' fehlt`. Gate blockt.
    4. Rot-Output verbatim in `08.23.2.AUTH-1-02-SUMMARY.md`.
    5. Seed entfernen: `routes/_seed_missing_template.py` loeschen. KEIN Seed im finalen Commit.
    6. `bash deploy.sh production` → GRUEN (falls kein echter fehlender Template auf HEAD).

    Falls der Guard gegen HEAD einen ECHTEN fehlenden Template findet → NICHT auto-fixen, im SUMMARY als Fund an André hochziehen (Mini-Entscheidung).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Gate gruen mit test_template_existence_guard collected (falls kein echter fehlender Template auf HEAD); seeded-Variante (routes/_seed_missing_template.py) war rot mit 'render_template auf fehlende Datei: routes/_seed_missing_template.py -> does_not_exist_seed.html fehlt' (dokumentiert).</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/test_template_existence_guard.py existiert; grep zeigt `render_template\(` Regex + Scan ueber routes + app.py.
    - Rot-Beleg: SUMMARY.md enthaelt die verbatim RED-Output-Zeile fuer den Seed, deploy.sh blockte daran.
    - Kein Seed-Rest: `ls routes/_seed_missing_template.py` zeigt die Datei ist entfernt.
    - Falls echter fehlender Template auf HEAD gefunden: als Fund im SUMMARY, KEIN eigenmaechtiges Template-Anlegen.
  </acceptance_criteria>
  <done>Template-Existenz-Guard faengt jeden render_template auf fehlende Datei, gegen seeded Verletzung nachweislich rot, Seed entfernt.</done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation
N/A — beide Waechter sind rein statische Datei-Sweeps (templates/**, static/**, routes/*.py, app.py). KEIN DB-Lesen/Schreiben, keine Tabelle angefasst, kein async Schreib/Lese-Versatz (Punkt 26).
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zukünftiger Entwickler-Commit → Deploy-Gate | neue Templates/JS koennten die CSRF-Regression (raw XHR/sendBeacon/token-gestripptes fetch) oder fehlende-Template-Klasse wieder einschleppen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUTH1-04 | Spoofing (CSRF-Regression durch neuen token-losen/gestrippten Fetch) | templates/**, static/** (rekursiv) | mitigate | test_csrf_fetch_guard: rekursiver Sweep (Vendor ausgeschlossen) blockt Deploy; ERST-ROT bewiesen |
| T-AUTH1-04b | Spoofing (raw XHR/sendBeacon/aliasierter fetch umgeht Wrapper+Guard) | statisch nicht fangbare Requests | accept | dokumentiert als bekannte Nicht-Abdeckung im Guard-File-Kopf; keine bekannte state-changing XHR/sendBeacon-Nutzung heute; Rest-Risiko registriert |
| T-AUTH1-05 | Denial of Service (500/Broken-Page durch render_template auf fehlende Datei) | routes/*.py, app.py | mitigate | test_template_existence_guard blockt Deploy bei fehlendem Template |
| T-AUTH1-06 | Tampering (Waechter blind-gruen, faengt nichts) | die Waechter selbst | mitigate | ERST-ROT-gegen-seeded-Verletzung fuer BEIDE Guards (seed im Scan-Root, dokumentierter Red-Output); Whitelist als frozenset explizit |
</threat_model>

<verification>
- deploy.sh production Gate gruen mit beiden Guards auto-collected (nach Plan 01).
- CSRF-Guard: rekursiv ueber templates/** + static/** mit Vendor-Ausschluss; gegen seeded Verletzung rot (verbatim Red-Output), danach Seed entfernt, danach gruen.
- Template-Guard: gegen seeded Verletzung rot, danach gruen. Echter Template-Fund → im SUMMARY hochgezogen (nicht auto-fixed).
</verification>

<success_criteria>
- CSRF-Fetch-Guard (app-weit rekursiv, Vendor-Ausschluss, bekannte Bypasses dokumentiert) + Template-Existenz-Guard im Deploy-Gate.
- Beide gegen seeded Verletzung rot bewiesen, Seeds wieder entfernt.
- Whitelist als frozenset; keine Source-Presence-False-Green-Fallen.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.AUTH-1-eingangstuer-netz/08.23.2.AUTH-1-02-SUMMARY.md` (inkl. verbatim Rot-Belege beider Guards, ggf. echter Template-Fund).
</output>
