---
phase: 04.6.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/landing.html
autonomous: true
requirements:
  - D-02
  - D-03
must_haves:
  truths:
    - "Klick auf 'Einloggen' im Header der Marketing-Site führt nach app.getnerve.app (nicht lokales Modal mit lokalem POST)"
    - "Alle Microsoft-OAuth-Buttons im Login-Kontext zeigen exakt 'Mit Microsoft 365 anmelden'"
    - "Microsoft-Buttons im Register-Kontext zeigen 'Mit Microsoft 365 registrieren' (Konsistenz, kein anmelden im Signup)"
  artifacts:
    - path: "templates/landing.html"
      provides: "Header-Login redirected zu app-Subdomain, Button-Labels normalisiert"
  key_links:
    - from: "templates/landing.html Einloggen-Button"
      to: "https://app.getnerve.app/?modal=login"
      via: "href oder window.location"
      pattern: "app\\.getnerve\\.app"
---

<objective>
Marketing-vs-App-Trennung im UI vollziehen und Microsoft-Branding vereinheitlichen. Implementiert D-02 (Login-Modal → app.getnerve.app) und D-03 (Microsoft 365 Button-Label).

Purpose: Nutzer sollen vom Marketing-Auftritt (getnerve.app) sauber in die App (app.getnerve.app) übergeben werden, statt auf der Landing-Page lokal einzuloggen. Microsoft-Branding klärt dass Business-Accounts gemeint sind.

Output: Aktualisiertes templates/landing.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6.2-deploy-hardening-and-oauth-polish/04.6.2-CONTEXT.md
@templates/landing.html

<interfaces>
Aktueller Stand templates/landing.html (Stand 2026-04-07):
- Zeile 258: `<button class="btn-outline" onclick="openLogin()">Einloggen</button>` — öffnet lokales Modal
- Zeile 602: `<a href="https://app.getnerve.app/auth/google" ...>` — bereits korrekt
- Zeile 611: `<a href="https://app.getnerve.app/auth/microsoft" ...>`
- Zeile 618: `<span>Mit Microsoft 365 anmelden</span>` — Login-Modal Variante (bereits korrekt)
- Zeile 665: `<a href="https://app.getnerve.app/auth/microsoft" ...>` (Register-Modal)
- Zeile 672: `<span>Mit Microsoft 365 registrieren</span>` — Register-Modal (Wortlaut "registrieren")
- Zeile 768: `function openLogin()` — JS-Funktion die das lokale Modal öffnet
- Zeile 815: `await fetch('/api/login', ...)` — Password-Login POSTed lokal

Nicht-triviale Beobachtung: Die OAuth-Buttons im Modal verlinken bereits korrekt nach app.getnerve.app. Der Bug aus Sicht des Users ist der Header-Button, der das lokale Modal öffnet — auf der Marketing-Domain (getnerve.app) hat ein Password-Login dort nichts zu suchen, weil die Session-Cookies für app.getnerve.app gesetzt werden müssen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header-Einloggen-Button auf app.getnerve.app umleiten (D-02)</name>
  <files>templates/landing.html</files>
  <action>
Umsetzung von D-02. Ziel: Wenn der User auf der Marketing-Site (getnerve.app) auf "Einloggen" klickt, soll er zur App-Subdomain (app.getnerve.app) weitergeleitet werden, statt das lokale Modal zu öffnen.

Konkrete Änderungen:

1. Zeile 258 (Header-Button "Einloggen"):
   - Vorher: `<button class="btn-outline" onclick="openLogin()">Einloggen</button>`
   - Nachher: `<a class="btn-outline" href="https://app.getnerve.app/?modal=login" role="button">Einloggen</a>`
   - Begründung: Direkter Redirect statt lokales Modal — die App-Subdomain hat denselben Modal-Code (gleiches Template) und öffnet das Modal automatisch via `?modal=login` (siehe Zeile 801).

2. Lokales Login-Modal (Zeilen ~595-640) NICHT entfernen — wird auf app.getnerve.app weiterhin gebraucht (gleiches Template). Auf der Marketing-Domain wird es nur nicht mehr getriggert vom Header.

3. Falls es noch andere `openLogin()`-Aufrufe im Marketing-Bereich gibt (z.B. CTA-Sektionen), diese ebenfalls auf den App-Link umstellen. Mit Grep prüfen: `grep -n "openLogin()" templates/landing.html` — alle Treffer im Marketing-Kontext (außerhalb des Modal-internen "Bereits Account?" Links Zeile 684 und Escape-Handler Zeile 780) auf den Anker umstellen.

4. Der "Bereits Account? Einloggen"-Link IM Register-Modal (Zeile 684) bleibt unverändert — der wechselt nur zwischen den zwei lokal vorhandenen Modals (Login ↔ Register), das ist auf der App-Subdomain korrekt.

Wichtig: Da templates/landing.html sowohl auf getnerve.app als auch auf app.getnerve.app gerendert wird, ist der harte Link zu app.getnerve.app auf der App-Subdomain ein Self-Redirect. Das ist ok (Browser folgt sofort), aber sauberer wäre relative Logik. Per CONTEXT-Discretion: harte URL ist ok für jetzt.
  </action>
  <verify>
    <automated>grep -c 'href="https://app.getnerve.app/?modal=login"' templates/landing.html | grep -q "^[1-9]"</automated>
  </verify>
  <done>
- Header-Einloggen ist ein `<a href="https://app.getnerve.app/?modal=login">`
- Keine `onclick="openLogin()"` Aufrufe mehr außerhalb des Modal-internen Wechsels
- Modal-Code bleibt im Template (für app-Subdomain)
  </done>
</task>

<task type="auto">
  <name>Task 2: Microsoft 365 Button-Labels normalisieren (D-03)</name>
  <files>templates/landing.html</files>
  <action>
Umsetzung von D-03: Microsoft-Buttons sollen explizit "Microsoft 365" sagen, nicht nur "Microsoft".

1. Login-Modal Microsoft-Button (Zeile 618):
   - Bereits korrekt: `<span>Mit Microsoft 365 anmelden</span>`
   - Verifizieren, nichts ändern.

2. Register-Modal Microsoft-Button (Zeile 672):
   - Vorher: `<span>Mit Microsoft 365 registrieren</span>`
   - Bleibt: `<span>Mit Microsoft 365 registrieren</span>`
   - Begründung: D-03 sagt "Mit Microsoft 365 anmelden" für Login-Kontexte. Im Register-Kontext ist "registrieren" semantisch korrekt und konsistent mit dem Google-Button daneben (Zeile 661 prüfen, sollte "Mit Google registrieren" sein). NICHT auf "anmelden" umstellen — das wäre verwirrend im Signup-Flow.

3. Fehler-Banner-Texte und JS-Strings (Zeilen 788-795) prüfen: enthalten "Microsoft" — dort kontextuell lassen wie sie sind (Fehlermeldung-Body, kein Button-Label).

4. Im gesamten templates/-Ordner per Grep prüfen, ob es weitere Microsoft-Login-Buttons gibt:
   - `grep -rn "Microsoft anmelden\\|Mit Microsoft" templates/`
   - Jeden Login-Button-Treffer (kein Register, kein Fehlertext) auf exakt `Mit Microsoft 365 anmelden` setzen.
   - Falls keine weiteren existieren: ok, diese Sub-Action ist no-op.

5. Aria-Labels und alt-Attribute der Microsoft-Buttons (falls vorhanden, Zeilen 611-620 und 665-674) konsistent halten.
  </action>
  <verify>
    <automated>grep -c "Mit Microsoft 365 anmelden" templates/landing.html | grep -qv "^0$"</automated>
  </verify>
  <done>
- Login-Kontext Microsoft-Button = "Mit Microsoft 365 anmelden"
- Register-Kontext Microsoft-Button = "Mit Microsoft 365 registrieren"
- Keine alten "Mit Microsoft anmelden" (ohne 365) Strings in Login-Buttons
  </done>
</task>

</tasks>

<verification>
- `grep -n "openLogin()" templates/landing.html` zeigt nur Modal-internen Wechsel + Escape-Handler
- `grep -n "Mit Microsoft 365" templates/landing.html` zeigt 2 Treffer (Login: anmelden, Register: registrieren)
- Lokal `python app.py` und Browser auf `/`: Header-Klick "Einloggen" navigiert zu app.getnerve.app
</verification>

<success_criteria>
- D-02 erfüllt: Header-Login leitet auf app-Subdomain
- D-03 erfüllt: Microsoft-Branding sagt "Microsoft 365" überall im Login-Kontext
- Keine Regression: Modal funktioniert auf app.getnerve.app weiterhin (gleiches Template)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04.6.2-deploy-hardening-and-oauth-polish/04.6.2-02-SUMMARY.md`
</output>
</content>
</invoke>