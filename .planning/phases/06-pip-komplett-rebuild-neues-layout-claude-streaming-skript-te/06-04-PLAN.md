---
phase: 06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - templates/app.html
  - static/app.js
autonomous: false
requirements:
  - PIP-01
  - PIP-02
  - PIP-03
  - PIP-04
  - PIP-05

must_haves:
  truths:
    - "Skript dropdown appears in Setup Step 3 and populates from /api/skripte"
    - "Full PiP flow works end-to-end: Setup -> Consent (meeting) -> Live with streaming"
    - "Teleprompter shows script blocks with correct highlighting"
    - "Background opacity slider works without affecting text readability"
  artifacts:
    - path: "templates/app.html"
      provides: "Skript dropdown in setup step 3"
      contains: "pip-skript-select"
  key_links:
    - from: "templates/app.html #pip-skript-select"
      to: "static/app.js pipPopulateSkripte()"
      via: "onchange or JS wiring"
      pattern: "pip-skript-select"
---

<objective>
Add skript dropdown HTML to Setup Step 3 and perform full E2E verification of the PiP rebuild.

Purpose: Complete the last HTML element (skript select) and verify the entire PiP flow works correctly in a real browser session.
Output: Skript dropdown in setup, verified working PiP.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-CONTEXT.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-UI-SPEC.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-01-SUMMARY.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-02-SUMMARY.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skript dropdown HTML to Setup Step 3</name>
  <files>templates/app.html, static/app.js</files>
  <read_first>templates/app.html, static/app.js</read_first>
  <action>
In `templates/app.html`, find Setup Step 3 (the profile/script selection step in the PiP setup flow — look for `pip-setup-step-3`). Add a script dropdown element after the profile select:

```html
<label class="pip-setup-label" style="margin-top:8px">Skript (optional)</label>
<select id="pip-skript-select" class="pip-setup-select">
  <option value="">Kein Skript</option>
</select>
```

Style the select with the same `.pip-setup-select` class used by the profile dropdown.

In `static/app.js`, wire the script dropdown population into the profile selection handler. Find where the profile is selected in Step 3 (likely in `pipPopulateProfiles()` or a profile change handler). After profile selection, call:

```javascript
pipPopulateSkripte(selectedProfileId);
```

Where `selectedProfileId` is the value from the profile dropdown.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && grep -c "pip-skript-select" templates/app.html && grep -c "pipPopulateSkripte" static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - templates/app.html contains `id="pip-skript-select"`
    - templates/app.html contains `Skript (optional)` label text
    - static/app.js calls `pipPopulateSkripte(` when profile is selected in Step 3
  </acceptance_criteria>
  <done>Skript dropdown appears in Setup Step 3, populated from /api/skripte when a profile is selected</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full E2E verification of PiP rebuild</name>
  <files>none</files>
  <action>Human verifies the complete PiP rebuild by testing all features in a live browser session.</action>
  <what-built>Complete PiP rebuild: split layout (KI+EWB upper 55%, teleprompter lower 45%), Claude word-by-word streaming via Socket.IO, dual-slot KI answers, consent flow for meeting mode, background-only opacity slider, teleprompter with auto-scroll and manual override, script dropdown in setup</what-built>
  <how-to-verify>
    1. Start the app: `python app.py` (or however the dev server runs)
    2. Log in and navigate to /live
    3. Open the PiP window (click the PiP launcher button)
    4. **Setup Flow:** Verify 3 steps work: Mode selection -> Kundendaten -> Profile+Skript selection
    5. **Skript Dropdown:** In Step 3, after selecting a profile, verify the "Skript" dropdown populates (if the profile has scripts)
    6. **Meeting Mode Consent:** Select "Meeting" mode, complete setup, click Start -> Verify the consent screen appears with the read-aloud text and two buttons
    7. **Consent Granted:** Click "Stattgegeben" -> Verify live split layout appears with two KI slots and teleprompter below
    8. **Cold Call Direct:** Restart, select "Cold Call" mode -> Verify NO consent screen, goes directly to live layout
    9. **Streaming:** Speak into microphone with an objection (e.g., "Das ist mir zu teuer") -> Verify tokens appear word-by-word in Slot A with blinking teal cursor
    10. **Dual Slots:** Verify both slots show content (einwand in one, proactive/coaching in other)
    11. **Opacity Slider:** Move the "BG" slider in the header -> Verify ONLY the background becomes transparent, ALL text/buttons remain fully readable at 100% opacity
    12. **Teleprompter:** If a script was selected, verify blocks render with the active block highlighted (teal left border, full brightness) and inactive blocks dimmed to ~40%
    13. **Manual Override:** Scroll the teleprompter manually -> Verify it stays at your position for ~8 seconds before auto-scroll resumes
    14. **Beenden:** Click "Beenden" -> Verify postcall screen appears
  </how-to-verify>
  <verify>Human confirms all 14 verification steps pass</verify>
  <done>All PiP features verified working in live browser session</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan adds minimal HTML + verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | T (Tampering) | pip-skript-select option values | accept | Values are integers from trusted /api/skripte endpoint (which filters by org_id) |
</threat_model>

<verification>
- Skript dropdown HTML exists in app.html
- Full E2E flow verified by human tester
</verification>

<success_criteria>
PiP rebuild is complete and verified working: split layout, streaming, consent, opacity, teleprompter, script selection. Human has approved the implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-04-SUMMARY.md`
</output>
