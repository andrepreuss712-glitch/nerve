---
phase: 06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - templates/app.html
autonomous: true
requirements:
  - PIP-01
  - PIP-05

must_haves:
  truths:
    - "PiP live section shows a 55/45 split layout instead of tabs"
    - "Two KI slots are visible side by side in the upper zone"
    - "Teleprompter container exists in the lower zone with scroll"
    - "Opacity slider is in the PiP header"
    - "Consent section HTML exists as a separate pip-section"
    - "EWB button row sits inside the KI zone below the slots"
  artifacts:
    - path: "templates/app.html"
      provides: "New PiP HTML structure and CSS"
      contains: "pip-ki-zone"
    - path: "templates/app.html"
      provides: "Consent section"
      contains: "pip-section-consent"
    - path: "templates/app.html"
      provides: "Opacity slider"
      contains: "pip-opacity-slider"
  key_links:
    - from: "templates/app.html #pip-section-live"
      to: "static/app.js setPipState"
      via: "id='pip-section-live' toggled by setPipState()"
      pattern: "pip-section-live"
---

<objective>
Replace the PiP tab-based HTML/CSS with the new split-layout structure, consent section, and opacity slider.

Purpose: Provide the DOM structure and styling that Plan 03 (JS logic) will wire up. After this plan, the HTML skeleton is complete — but no JS behavior changes yet.
Output: Rebuilt pip-section-live HTML, new pip-section-consent HTML, all new CSS classes per 06-UI-SPEC.md.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-CONTEXT.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-UI-SPEC.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-01-SUMMARY.md

<interfaces>
<!-- Existing PiP HTML structure to be replaced (templates/app.html lines 982-1049) -->

Current structure (REPLACE):
```html
<div id="pip-section-live" class="pip-section" style="display:none;flex-direction:column;height:100%">
  <!-- Mini Header (KEEP, modify) -->
  <div class="pip-header">...</div>
  <!-- Tabs (REMOVE) -->
  <div class="pip-tabs">...</div>
  <!-- Content panels (REMOVE) -->
  <div class="pip-content">
    <div class="pip-panel" id="pip-panel-opener">...</div>
    <div class="pip-panel" id="pip-panel-einwand">...</div>
    <div class="pip-panel" id="pip-panel-coaching">...</div>
    <div class="pip-panel" id="pip-panel-ewb">...</div>
  </div>
  <!-- EWB row (KEEP, reposition) -->
  <div class="pip-ewb-row" id="pip-ewb-row"></div>
  <!-- Beenden button (KEEP) -->
  <button class="pip-btn-beenden" id="pip-btn-beenden">Beenden</button>
</div>
```

CSS classes from 06-UI-SPEC.md that must be implemented:
- .pip-ki-zone (55% flex), .pip-ki-slot, .pip-slot-label, .pip-slot-body, .pip-slot-result
- .pip-streaming::after (cursor blink), @keyframes pip-cursor-blink
- .tp-block, .tp-block-active, .tp-empty
- #pip-teleprompter (45% flex, overflow-y scroll, custom scrollbar)
- #pip-section-consent with consent buttons
- Opacity slider styling
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PiP live section HTML with split layout + consent section</name>
  <files>templates/app.html</files>
  <read_first>templates/app.html, .planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-UI-SPEC.md</read_first>
  <action>
In `templates/app.html`, replace the entire `#pip-section-live` div (lines ~985-1035) with the new split-layout structure. Also add `#pip-section-consent` as a new section. Keep the header but add the opacity slider. Remove all tab-related HTML.

**Replace `#pip-section-live` with:**

```html
  <!-- Section: LIVE (Split Layout per D-01) -->
  <div id="pip-section-live" class="pip-section" style="display:none;flex-direction:column;height:100%">

    <!-- Mini Header (modified: added opacity slider per D-15) -->
    <div class="pip-header">
      <div style="display:flex;align-items:center;gap:8px">
        <span class="pip-mode-badge" id="pip-mode-badge">Cold Call</span>
        <span class="pip-timer" id="pip-timer">00:00</span>
      </div>
      <div style="display:flex;align-items:center;gap:8px">
        <span class="pip-opacity-label" id="pip-opacity-label">BG</span>
        <input type="range" id="pip-opacity-slider" min="10" max="100" step="5" value="100">
        <a href="javascript:void(0)" onclick="closePipToVollbild()" class="pip-to-fullscreen" aria-label="Vollbild oeffnen">
          <i data-lucide="maximize-2" style="width:16px;height:16px"></i>
          <span>Vollbild</span>
        </a>
      </div>
    </div>

    <!-- KI Upper Zone (55% per D-01) -->
    <div class="pip-ki-zone" id="pip-ki-zone">
      <div class="pip-ki-slots">
        <div class="pip-ki-slot" id="pip-slot-0">
          <div class="pip-slot-label">ANTWORT A</div>
          <div class="pip-slot-body" id="pip-slot-body-0">Warte auf Gespraechsinhalt...</div>
          <div class="pip-slot-result" id="pip-slot-result-0" style="display:none"></div>
        </div>
        <div class="pip-ki-slot" id="pip-slot-1">
          <div class="pip-slot-label">ANTWORT B</div>
          <div class="pip-slot-body" id="pip-slot-body-1">Warte auf Gespraechsinhalt...</div>
          <div class="pip-slot-result" id="pip-slot-result-1" style="display:none"></div>
        </div>
      </div>
      <!-- EWB row inside KI zone per UI-SPEC note 6 -->
      <div class="pip-ewb-row" id="pip-ewb-row"></div>
    </div>

    <!-- Divider -->
    <div class="pip-zone-divider"></div>

    <!-- Teleprompter Lower Zone (45% per D-01) -->
    <div id="pip-teleprompter" class="pip-teleprompter">
      <div class="tp-empty">Kein Skript hinterlegt -- Profil bearbeiten</div>
    </div>

    <!-- Beenden button -->
    <button class="pip-btn-beenden" id="pip-btn-beenden" onclick="pipBeendenCall()">Beenden</button>

    <!-- PiP fallback warning -->
    <div id="pip-fallback-warning" style="display:none;padding:8px;background:rgba(248,113,113,0.1);border:1px solid rgba(248,113,113,0.3);border-radius:6px;font-size:12px;color:#f87171;text-align:center;margin:8px">
      PiP nicht unterstuetzt. Chrome 116+ erforderlich.
    </div>
  </div>

  <!-- Section: CONSENT (Meeting-only per D-05) -->
  <div id="pip-section-consent" class="pip-section" style="display:none;flex-direction:column;align-items:center;justify-content:center;height:100%;padding:24px">
    <div class="pip-consent-heading">Hinweis an Kunden vorlesen</div>
    <div class="pip-consent-subtext">Lesen Sie dem Kunden den folgenden Text vor:</div>
    <div class="pip-consent-text" id="pip-consent-text">
      Herr/Frau [Name], kurzer Hinweis -- ich mache mir waehrend unseres Gespraechs digitale Notizen. Ist das fuer Sie in Ordnung?
    </div>
    <div class="pip-consent-buttons">
      <button class="pip-consent-btn-granted" id="pip-consent-granted" onclick="pipConsentGranted()">Stattgegeben -- Meeting-Modus starten</button>
      <button class="pip-consent-btn-denied" id="pip-consent-denied" onclick="pipConsentDenied()">Abgelehnt -- Weiter als Cold Call</button>
    </div>
  </div>
```

**Add the consent section BEFORE the existing `#pip-section-postcall` div** (so section order is: setup, live, consent, postcall).

**CSS Changes — In the existing `<style>` block** (around lines 530-570 in app.html), remove all `.pip-tabs`, `.pip-tab`, `.pip-tab-active`, `.pip-tab-locked::after`, `.pip-content`, `.pip-panel` CSS rules. Replace with the new split-layout CSS per 06-UI-SPEC.md:

```css
  /* ── Phase 06: Split Layout ──────────────────────────────────────── */
  .pip-ki-zone { flex: 0 0 55%; display: flex; flex-direction: column; padding: 0 16px; overflow: hidden; }
  .pip-ki-slots { display: flex; gap: 8px; flex: 1; min-height: 0; }
  .pip-ki-slot { flex: 1; border: 1px solid rgba(255,255,255,0.08); border-radius: 12px;
    padding: 12px; background: rgba(255,255,255,0.04); display: flex; flex-direction: column;
    overflow-y: auto; }
  .pip-ki-slot.pip-slot-streaming { border-color: rgba(0,212,170,0.3); }
  .pip-slot-label { font-size: 12px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase;
    color: rgba(255,255,255,0.4); margin-bottom: 8px; }
  .pip-slot-body { font-size: 14px; font-weight: 400; line-height: 1.5; color: #e8ecf4; flex: 1; }
  .pip-slot-body.pip-streaming::after { content: '\u258C'; animation: pip-cursor-blink 0.7s step-end infinite;
    color: #00D4AA; margin-left: 2px; }
  @keyframes pip-cursor-blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }
  .pip-slot-result { font-size: 14px; font-weight: 400; line-height: 1.5; color: #e8ecf4; }
  .pip-slot-typ-badge { display: inline-block; font-size: 12px; font-weight: 700; padding: 2px 8px;
    border-radius: 9999px; margin-bottom: 4px; }

  .pip-zone-divider { height: 1px; background: rgba(255,255,255,0.08); flex-shrink: 0; }

  /* ── Teleprompter ──────────────────────────────────────────────── */
  .pip-teleprompter { flex: 1; min-height: 0; overflow-y: scroll; padding: 0; }
  .pip-teleprompter::-webkit-scrollbar { width: 4px; }
  .pip-teleprompter::-webkit-scrollbar-track { background: transparent; }
  .pip-teleprompter::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.15); border-radius: 2px; }
  .tp-block { padding: 12px 16px; border-left: 4px solid transparent; border-radius: 0 8px 8px 0;
    font-size: 14px; font-weight: 400; line-height: 1.5; color: rgba(255,255,255,0.4);
    cursor: pointer; transition: opacity 200ms, border-color 200ms; }
  .tp-block-active { border-left-color: #00D4AA; color: #e8ecf4; background: rgba(0,212,170,0.06); }
  .tp-empty { font-size: 14px; font-weight: 400; color: rgba(255,255,255,0.3); text-align: center;
    padding: 24px 16px; }

  /* ── Opacity Slider ────────────────────────────────────────────── */
  .pip-opacity-label { font-size: 12px; font-weight: 700; color: rgba(255,255,255,0.4); }
  #pip-opacity-slider { width: 80px; height: 4px; -webkit-appearance: none; appearance: none;
    background: rgba(255,255,255,0.12); border-radius: 2px; outline: none; cursor: pointer; }
  #pip-opacity-slider::-webkit-slider-thumb { -webkit-appearance: none; width: 16px; height: 16px;
    border-radius: 50%; background: #ffffff; border: none; cursor: pointer; }

  /* ── Consent Screen ────────────────────────────────────────────── */
  .pip-consent-heading { font-size: 14px; font-weight: 700; color: #e8ecf4; margin-bottom: 8px; }
  .pip-consent-subtext { font-size: 12px; font-weight: 700; color: rgba(255,255,255,0.4); margin-bottom: 16px; }
  .pip-consent-text { font-size: 14px; font-weight: 400; line-height: 1.6; color: rgba(255,255,255,0.7);
    background: rgba(255,255,255,0.05); border-radius: 8px; padding: 12px 16px; margin-bottom: 16px; }
  .pip-consent-buttons { display: flex; flex-direction: column; gap: 8px; width: 100%; }
  .pip-consent-btn-granted { background: #00D4AA; color: #06060a; font-weight: 700; font-size: 14px;
    border: none; border-radius: 8px; padding: 12px 20px; cursor: pointer; font-family: inherit; }
  .pip-consent-btn-denied { background: rgba(248,113,113,0.08); color: #f87171;
    border: 1px solid rgba(248,113,113,0.2); font-weight: 700; font-size: 14px;
    border-radius: 8px; padding: 12px 20px; cursor: pointer; font-family: inherit; }
```
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && grep -c "pip-ki-zone" templates/app.html && grep -c "pip-section-consent" templates/app.html && grep -c "pip-teleprompter" templates/app.html && grep -c "pip-opacity-slider" templates/app.html && grep -c "pip-tab" templates/app.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/app.html contains `id="pip-ki-zone"` (split layout upper zone)
    - templates/app.html contains `id="pip-slot-0"` and `id="pip-slot-1"` (dual slots)
    - templates/app.html contains `id="pip-slot-body-0"` and `id="pip-slot-body-1"`
    - templates/app.html contains `id="pip-teleprompter"` (lower zone)
    - templates/app.html contains `id="pip-section-consent"` (consent screen)
    - templates/app.html contains `id="pip-consent-text"` (consent text block)
    - templates/app.html contains `id="pip-opacity-slider"` (opacity range input)
    - templates/app.html contains `pip-consent-btn-granted` class
    - templates/app.html contains `pip-consent-btn-denied` class
    - templates/app.html contains `.pip-streaming::after` CSS rule with `pip-cursor-blink` animation
    - templates/app.html contains `.tp-block-active` CSS rule with `border-left-color: #00D4AA`
    - templates/app.html contains `.pip-zone-divider` CSS rule
    - templates/app.html does NOT contain `pip-tab-active` (old tab CSS removed)
    - templates/app.html does NOT contain `handlePipTabClick` in any onclick attribute (old tab buttons removed)
    - grep for "pip-tab" returns 0 matches (all tab HTML and CSS removed)
  </acceptance_criteria>
  <done>PiP HTML has split layout with dual KI slots, teleprompter container, opacity slider in header, consent section, and all old tab HTML/CSS removed. All CSS classes match 06-UI-SPEC.md specifications exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PiP document boundary | CSS/JS must target PiP document, not main tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | S (Spoofing) | Consent screen | accept | Consent is informational UX — not a security boundary. The actual Deepgram mode decision happens server-side |
| T-06-06 | T (Tampering) | onclick handlers in HTML | accept | All onclick handlers call existing trusted JS functions, no user-supplied data in onclick attributes |
</threat_model>

<verification>
- `grep -c "pip-ki-zone" templates/app.html` returns >= 1
- `grep -c "pip-section-consent" templates/app.html` returns >= 2 (div + CSS)
- `grep -c "pip-tab" templates/app.html` returns 0 (tabs fully removed)
- `grep "pip-opacity-slider" templates/app.html` returns match
- `grep "tp-block-active" templates/app.html` returns match
</verification>

<success_criteria>
PiP HTML skeleton is complete with split layout, consent section, and all CSS per UI-SPEC. Old tab system is fully removed. No JS logic changes in this plan — that is Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-02-SUMMARY.md`
</output>
