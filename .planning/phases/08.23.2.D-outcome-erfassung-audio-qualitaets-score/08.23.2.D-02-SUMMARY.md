---
phase: 08.23.2.D
plan: "02"
subsystem: services
tags: [claude, haiku, classifier, audio-health, service, tdd]
dependency_graph:
  requires:
    - calls.outcome_confidence (08.23.2.D-01 provides)
    - services/claude_service.claude_client
    - config.MODEL_ANALYSE (Haiku)
  provides:
    - services/outcome_service.classify() (REQ-D-3)
    - services/outcome_service.calculate_audio_health() (REQ-D-6)
  affects:
    - services/coaching_service.py (D-03 Einhängepunkt für classify()-Aufruf — Plan 04)
    - routes/app_routes.py (D-06d Audio-Health-Thread-Einhängepunkt — Plan 04)
tech_stack:
  added: []
  patterns:
    - Pure-Logik-Service (kein Emit, kein DB-Write — D-01b/c)
    - TDD RED→GREEN (10 Tests zuerst, dann Implementation)
    - statistics-stdlib fuer deterministische Metriken (REQ-D-6)
    - Haiku-Fallback-Chain via getattr(config, ...) fuer fehlende Konstanten
key_files:
  created:
    - services/outcome_service.py
    - tests/test_outcome_service.py
  modified: []
decisions:
  - "MODEL_POSTCALL_HAIKU existiert nicht in config.py — Fallback-Chain auf MODEL_ANALYSE (bestehende Haiku-Konstante claude-haiku-4-5-20251001)"
  - "10 Tests statt 9 aus Plan-Vorlage: test_classify_handles_malformed_json als eigenstaendiger Test (wichtiger Edge-Case)"
metrics:
  duration: "3 Minuten"
  completed: "2026-05-26T17:13:01Z"
  tasks_completed: 2
  files_modified: 2
---

# Phase 08.23.2.D Plan 02: outcome_service.py — Haiku-Classifier + Audio-Health-Score Summary

TDD-implementiertes `services/outcome_service.py` mit `classify()` (Claude-Haiku Outcome-Klassifikation, crash-safe, 6 Enum-Werte + confidence 0.0-1.0) und `calculate_audio_health()` (5 deterministische Metriken aus Word-Confidence-Buffer + gewichteter Composite-Score).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 2.1 | Test-Suite RED | 3ba6215 | tests/test_outcome_service.py |
| 2.2 | outcome_service.py GREEN | 1df87fd | services/outcome_service.py |

## Verification Results

- `pytest tests/test_outcome_service.py -x -v` -> 10/10 PASSED
- `python -c "from services.outcome_service import classify, calculate_audio_health; print('ok')"` -> ok
- Pure-Logik: grep `socketio.emit|sio.emit` in services/outcome_service.py = 0 Matches
- Pure-Logik: grep `db.commit|session.commit` in services/outcome_service.py = 0 Matches
- REQ-D-3: classify() gibt valides Enum-outcome + confidence ∈ [0,1] zurueck, crash-safe
- REQ-D-6: 5 deterministische Audio-Health-Metriken aus Buffer berechnet

## TDD Gate Compliance

- RED gate commit: 3ba6215 `test(08.23.2.D-02): add failing tests for outcome_service (RED)`
- GREEN gate commit: 1df87fd `feat(08.23.2.D-02): implement outcome_service.py — Haiku classifier + audio health (GREEN)`
- REFACTOR: nicht noetig — Implementation bereits sauber strukturiert

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Config Constant] MODEL_POSTCALL_HAIKU nicht in config.py**

- **Found during:** Task 2.2 Implementation
- **Issue:** Der Plan referenziert `config.MODEL_POSTCALL_HAIKU` und `config.MODEL_LIVE_HAIKU` — beide existieren nicht in `config.py`. Verfuegbare Haiku-Konstante ist `MODEL_ANALYSE = "claude-haiku-4-5-20251001"`.
- **Fix:** Fallback-Chain via `getattr()`: `MODEL_POSTCALL_HAIKU` → `MODEL_LIVE_HAIKU` → `MODEL_ANALYSE` → Default-String. Das macht den Service robust gegen kuenftige Config-Aenderungen. Keine neue Config-Konstante hinzugefuegt (wuerde Deployment-Koordination erfordern — Rule 4-Grenze).
- **Files modified:** services/outcome_service.py (Zeile `_HAIKU_MODEL`)
- **Commit:** 1df87fd

**2. [Rule 2 - Enhancement] 10 statt 9 Tests**

- **Found during:** Task 2.1 Test-Erstellung
- **Issue:** Plan-Vorlage enthielt `test_classify_handles_malformed_json` bereits im Quellcode-Block, aber nicht explizit in der behavior-Liste gezaehlt.
- **Fix:** Alle 10 Tests aus dem Plan-Quellcode-Block implementiert — kein Test weggelassen.
- **Files modified:** tests/test_outcome_service.py
- **Commit:** 3ba6215

## Known Stubs

Keine. `classify()` und `calculate_audio_health()` sind vollstaendig implementiert. Der `_HAIKU_MODEL`-Wert ist deterministisch aus config.py geladen (kein Placeholder).

## Threat Flags

Keine neuen unerwarteten Security-relevanten Surfaces. `classify()` erhält nur strukturierte Daten aus bestehenden DB-Feldern — kein neuer Netzwerk-Endpoint, kein neuer Auth-Pfad. Haiku-Call ist identisch zum bestehenden Pattern in `coaching_service.py`.

## Self-Check: PASSED

- `services/outcome_service.py` existiert: ✓
- `tests/test_outcome_service.py` existiert: ✓
- Commits 3ba6215, 1df87fd in git log: ✓
- `pytest tests/test_outcome_service.py -x` -> 10 passed: ✓
- `python -c "from services.outcome_service import classify, calculate_audio_health; print('ok')"` -> ok: ✓
- Pure-Logik (kein Emit, kein DB-Write): ✓
