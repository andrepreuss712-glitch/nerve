---
phase: 08.23.2.D
plan: 04
type: execute
wave: 3
depends_on: ["08.23.2.D-01", "08.23.2.D-02", "08.23.2.D-03"]
files_modified:
  - routes/app_routes.py
  - tests/test_api_beenden_calls_update.py
autonomous: true
requirements: [REQ-D-2, REQ-D-6]
requirements_addressed: [REQ-D-2, REQ-D-6]
tags: [api-beenden, calls-update, audio-health, background-thread]
must_haves:
  truths:
    - "api_beenden updated den bestehenden calls-Record (kein zweiter INSERT) per call_id aus Session-State"
    - "calls.conversation_log_id wird nach ConvLog-Save gesetzt"
    - "calls.call_mode kommt aus req_data.get('session_mode', 'meeting') (D-05a)"
    - "Audio-Health-Background-Thread liest word_confidences VOR reset_session() und schreibt audio_health_score + CallEvent(audio_health)"
    - "api_beenden Response enthaelt 'call_id' (str/None) fuer Frontend-Fallback-Pull"
    - "Bei fehlendem call_id im Session-State: kein UPDATE, kein Crash (graceful)"
  artifacts:
    - path: "routes/app_routes.py"
      provides: "api_beenden: calls-UPDATE-Block nach ConvLog-Save, Audio-Health-Thread-Start vor reset_session, call_id im Response"
    - path: "tests/test_api_beenden_calls_update.py"
      provides: "Integration-Test: UPDATE-Path, Edge-Case kein call_id, Audio-Health-Thread schreibt CallEvent"
  key_links:
    - from: "routes/app_routes.py api_beenden"
      to: "Call.conversation_log_id"
      via: "UPDATE Call WHERE id = state.call_id"
      pattern: "Call.*conversation_log_id.*saved_conv_id"
    - from: "routes/app_routes.py api_beenden"
      to: "services/outcome_service.calculate_audio_health"
      via: "threading.Thread(daemon=True) liest word_confidences VOR reset_session()"
      pattern: "calculate_audio_health|threading\\.Thread.*audio_health"
---

<objective>
Modifiziert `routes/app_routes.py api_beenden()` um (REQ-D-2 + REQ-D-6):
1. NACH ConvLog-INSERT (Z.264) den bestehenden `calls`-Record per `call_id` aus Session-State zu UPDATEN (ended_at, conversation_log_id, call_mode aus req_data — D-05a).
2. VOR `reset_session()` den Word-Confidence-Buffer aus `ls._session_state[sid]['word_confidences']` zu kopieren.
3. Background-Thread starten der `outcome_service.calculate_audio_health()` aufruft + `calls.audio_health_score` UPDATEt + `CallEvent(event_type='audio_health')` INSERTET.
4. Response um `'call_id': str(call_id) | None` erweitern fuer Frontend-Fallback-Pull-Endpoint.

Purpose: Adressiert REQ-D-2 (calls-UPDATE mit FK) und REQ-D-6 (Audio-Health PostCall-Berechnung). KRITISCH: UPDATE statt INSERT (RESEARCH.md Pitfall 1) - create_call_for_sid legt Early-Record an.
Output: Erweiterte api_beenden-Route + Integration-Test.

**SPEC-OVERRIDE-ANNOTATION (für Verifier):**
SPEC.md REQ-D-2 acceptance sagt woertlich "calls-INSERT". Das ist eine veraltete Formulierung —
RESEARCH.md hat aufgedeckt dass `create_call_for_sid()` den Call-Record bereits beim Session-Start
anlegt (Pitfall 1). Dieser Plan implementiert daher einen **UPDATE** des bestehenden Early-Records,
NICHT einen zweiten INSERT. Verifiers duerfen REQ-D-2 NICHT auf einen literalen INSERT-Check
pruefen — der UPDATE des Early-Records erfuellt REQ-D-2 vollstaendig (ended_at + conversation_log_id +
call_mode werden gesetzt). Diese Override ist mit CONTEXT.md/RESEARCH.md konsistent.

CLAUDE.md Punkt 14 Pre-Insert-Control-Flow-Audit:
- 30-Zeilen-Fenster um Z.264 (db_conv.commit) und um Einhaengepunkt fuer UPDATE muss vor Edit gelesen werden
- Race-Condition: Audio-Health-Thread liest word_confidences VOR reset_session - Sequenz garantiert
- Race-Condition: zwei parallel api_beenden fuer selbe SID -> Idempotenz via call_id-Lookup (zweites UPDATE setzt gleiche Werte = OK)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-CONTEXT.md
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-RESEARCH.md
@routes/app_routes.py
@services/live_session.py
@services/outcome_service.py
@database/models.py

<interfaces>
api_beenden Sequenz aus RESEARCH.md Q1:
  Z.41-42: req_data = request.get_json(silent=True) or {}; session_mode = req_data.get('session_mode', 'meeting')
  Z.227-265: ConvLog-INSERT + db_conv.commit() - saved_conv_id verfuegbar
  Z.417-427: run_postcall_engine() (SQL-only, KEINE Claude-Calls)
  Z.456: reset_session()  <-- VOR diesem Aufruf muessen word_confidences kopiert werden
  Z.458: return jsonify({'ok': True, 'filename': filename, 'postcall': postcall, 'conv_id': saved_conv_id})

call_id Lookup (RESEARCH.md Q5):
  - api_beenden ist HTTP-Route, hat keine direkte SID
  - Lookup-Mechanismus: ueber user_id durch _session_state iterieren
    OR: Frontend schickt call_id im POST-Body (Empfehlung aus RESEARCH.md - deterministisch)
  - DIESE PHASE: Lookup via user_id-Iteration (Frontend-Anpassung in Plan 05)

Call-Modell Felder (nach Plan 01):
  conversation_log_id, ended_at, call_mode, audio_health_score
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: api_beenden - calls-UPDATE + Audio-Health-Thread + call_id im Response</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py Z.1-50 (Imports, Decorators)
    - routes/app_routes.py Z.227-300 (ConvLog-INSERT-Block - 30 Zeilen vor/nach db_conv.commit Z.264)
    - routes/app_routes.py Z.410-460 (run_postcall_engine, reset_session, return - 30 Zeilen vor/nach Einhaengepunkt)
    - services/live_session.py Z.299-400 + Z.421-516 (init_session_state mit 'call_id', create_call_for_sid Pattern)
    - services/outcome_service.py (calculate_audio_health Signatur)
    - .planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-RESEARCH.md (Q1 + Q5 + Code-Pattern fuer Background-Thread)
  </read_first>
  <action>
EDIT `routes/app_routes.py api_beenden()`:

(A) Imports am Top der Datei (falls nicht bereits vorhanden - erst pruefen):

    import threading
    from flask import current_app  # fuer Background-Thread App-Context
    from database.models import Call, CallEvent  # Migration 0005 + bestehend
    from services import outcome_service
    import services.live_session as ls

(B) NACH `saved_conv_id = conv.id` (ca. Z.265) und VOR `run_postcall_engine()` (ca. Z.417) folgenden Block einfuegen:

    # -- Phase 08.23.2.D REQ-D-2 - calls-Record UPDATEN (kein zweiter INSERT) --
    # create_call_for_sid() legt bei start_live_session bereits einen Call-Record an.
    # Hier UPDATEn wir ended_at, conversation_log_id, call_mode (aus req_data per D-05a).
    _phase_d_call_id = None
    _phase_d_word_confidences = []
    try:
        # SID-Lookup: api_beenden ist HTTP-Route. Wir suchen via user_id den aktiven Session-State-Eintrag.
        # Wenn das Frontend in Plan 05 'call_id' im POST-Body mitschickt, ist das deterministischer -
        # bevorzugt diesen, fallback auf user_id-Iteration.
        _posted_call_id = req_data.get('call_id') if isinstance(req_data, dict) else None
        with ls._session_state_lock:
            if _posted_call_id:
                for _sid, _sd in ls._session_state.items():
                    if _sd.get('state', {}).get('call_id') and str(_sd['state']['call_id']) == str(_posted_call_id):
                        _phase_d_call_id = _sd['state']['call_id']
                        _phase_d_word_confidences = list(_sd.get('word_confidences', []))
                        break
            if not _phase_d_call_id:
                for _sid, _sd in ls._session_state.items():
                    if _sd.get('user_id') == g.user.id and _sd.get('state', {}).get('call_id'):
                        _phase_d_call_id = _sd['state']['call_id']
                        _phase_d_word_confidences = list(_sd.get('word_confidences', []))
                        break
    except Exception as _e_lookup:
        print(f'[Phase08.23.2.D] call_id-Lookup Fehler (non-fatal): {_e_lookup}')

    if _phase_d_call_id:
        from datetime import datetime as _dt, timezone as _tz
        from database.db import get_session as _get_session_d
        _db_calls = _get_session_d()
        try:
            _call_row = _db_calls.query(Call).filter(Call.id == _phase_d_call_id).first()
            if _call_row is not None:
                _call_row.ended_at = _dt.now(_tz.utc)
                _call_row.conversation_log_id = saved_conv_id
                # D-05a: call_mode aus req_data, NICHT aus Session-State
                _req_mode = (req_data.get('session_mode') if isinstance(req_data, dict) else None) or 'meeting'
                # call_mode CHECK-Constraint erlaubt nur 'cold_call' und 'meeting_consented'
                _call_row.call_mode = 'cold_call' if _req_mode == 'cold_call' else 'meeting_consented'
                _db_calls.commit()
        except Exception as _e_upd:
            print(f'[Phase08.23.2.D] calls-UPDATE Fehler: {_e_upd}')
            _db_calls.rollback()
        finally:
            _db_calls.close()

(C) NACH dem UPDATE-Block, VOR `reset_session()` (Z.456), Background-Thread starten:

    # -- Phase 08.23.2.D REQ-D-6 - Audio-Health-Background-Thread --
    # Liest _phase_d_word_confidences (bereits oben kopiert VOR reset_session),
    # berechnet 5 Metriken via outcome_service.calculate_audio_health,
    # UPDATEt calls.audio_health_score + INSERTet CallEvent(event_type='audio_health').
    # flask_app wird VOR Thread-Start aus current_app extrahiert, da current_app im
    # Background-Thread nicht mehr verfuegbar ist (kein Request-Context).
    _flask_app = current_app._get_current_object()

    def _audio_health_bg(call_id_val, wc_buffer, flask_app):
        with flask_app.app_context():
            import time as _t_ah
            from database.db import get_session as _gs_ah
            try:
                metrics = outcome_service.calculate_audio_health(wc_buffer)
                if metrics.get('score') is None:
                    return  # Kein Buffer -> nichts zu schreiben
                _db_ah = _gs_ah()
                try:
                    _row = _db_ah.query(Call).filter(Call.id == call_id_val).first()
                    if _row is not None:
                        _row.audio_health_score = float(metrics['score'])
                        _db_ah.add(CallEvent(
                            call_id=call_id_val,
                            event_type='audio_health',
                            event_ts_ms=int(_t_ah.time() * 1000),
                            payload={
                                'mean': metrics.get('mean'),
                                'median': metrics.get('median'),
                                'pct_below_07': metrics.get('pct_below_07'),
                                'longest_uncertain_block_s': metrics.get('longest_uncertain_block_s'),
                                'stddev': metrics.get('stddev'),
                                'score': metrics.get('score'),
                            },
                        ))
                        _db_ah.commit()
                except Exception as _e_inner:
                    print(f'[AudioHealth] DB-Write Fehler: {_e_inner}')
                    _db_ah.rollback()
                finally:
                    _db_ah.close()
            except Exception as _e_outer:
                print(f'[AudioHealth] Thread Fehler: {_e_outer}')

    if _phase_d_call_id and _phase_d_word_confidences:
        threading.Thread(
            target=_audio_health_bg,
            args=(_phase_d_call_id, _phase_d_word_confidences, _flask_app),
            daemon=True,
        ).start()

(D) `return jsonify(...)` (Z.458) ergaenzen um call_id-Feld:

    return jsonify({
        'ok': True,
        'filename': filename,
        'postcall': postcall,
        'conv_id': saved_conv_id,
        'call_id': str(_phase_d_call_id) if _phase_d_call_id else None,  # Phase 08.23.2.D - Frontend-Fallback-Pull
    })

Wichtig:
- Variablen-Namen mit `_phase_d_` Prefix zur Konflikt-Vermeidung mit bestehenden Locals (route ist gross).
- `g.user.id` ist via Flask-g verfuegbar (login_required oben).
- KEIN Lock-Hold ueber DB-Operationen - Lookup unter Lock, DB-Operationen ohne Lock.
- `_flask_app = current_app._get_current_object()` MUSS im Request-Context ausgefuehrt werden
  (vor dem Thread-Start), nicht im Thread selbst. Der Thread hat keinen Request-Context mehr.
- Threading-Daemon-Pattern aus RESEARCH.md Code-Example.
- `call_mode` mapping: req_data hat 'cold_call' oder 'meeting'/'meeting_consented'. CHECK-Constraint erlaubt nur cold_call und meeting_consented (siehe models.py).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "from routes.app_routes import app_routes_bp; print([r.endpoint for r in app_routes_bp.deferred_functions if hasattr(r, 'endpoint')] or 'imported OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Phase 08\.23\.2\.D REQ-D-2` in routes/app_routes.py findet 1 Match
    - grep `Phase 08\.23\.2\.D REQ-D-6` in routes/app_routes.py findet 1 Match
    - grep `_phase_d_call_id` in routes/app_routes.py findet >=5 Matches
    - grep `outcome_service\.calculate_audio_health` in routes/app_routes.py findet 1 Match
    - grep `event_type='audio_health'` in routes/app_routes.py findet 1 Match
    - grep `threading\.Thread` in routes/app_routes.py findet >=1 Match
    - grep `'call_id': str\(_phase_d_call_id\)` in routes/app_routes.py findet 1 Match
    - grep `req_data\.get\('session_mode'` in routes/app_routes.py findet >=1 Match (D-05a)
    - grep `current_app\._get_current_object\(\)` in routes/app_routes.py findet 1 Match
    - grep `flask_app\.app_context\(\)` in routes/app_routes.py findet 1 Match
    - grep `from flask import current_app` in routes/app_routes.py findet >=1 Match
    - Import-Smoke `python -c "import routes.app_routes"` exit 0
  </acceptance_criteria>
  <done>api_beenden updated calls, startet Audio-Health-Thread mit Flask App Context, gibt call_id im Response zurueck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2: Integration-Test fuer api_beenden calls-UPDATE + Audio-Health</name>
  <files>tests/test_api_beenden_calls_update.py</files>
  <read_first>
    - routes/app_routes.py (api_beenden - aktuell mit Phase-D-Block)
    - services/live_session.py (init_session_state, _session_state-Manipulation fuer Test-Setup)
    - tests/test_migration_0005.py (Pattern fuer DB-Tests)
  </read_first>
  <behavior>
    - Test 1: Mit aktiven Session-State (call_id gesetzt) -> nach api_beenden hat Call-Row ended_at gesetzt, conversation_log_id != None
    - Test 2: Ohne aktiven call_id im Session-State -> kein UPDATE, kein Crash, Response hat call_id=None
    - Test 3: Audio-Health-Thread schreibt CallEvent(event_type='audio_health') wenn word_confidences buffer >= 1 Eintrag
    - Test 4: Leerer word_confidences-Buffer -> KEIN CallEvent (score=None)
    - Test 5: UPDATE-Helper setzt audio_health_score nach Thread-Completion
  </behavior>
  <action>
Erstelle `tests/test_api_beenden_calls_update.py`. **CLAUDE.md-Pflicht: Nur Runtime-Verhaltens-Tests gegen In-Memory-DB. KEINE Source-Presence-Tests (`open(__file__).read()` + string-assertion ist VERBOTEN per CLAUDE.md Source-Presence-False-Green-Regel).**

Implementiere die Minimalvariante: direkte DB-Logik-Tests gegen In-Memory-SQLite oder dev-DB, die das exakte Verhalten des UPDATE-Helpers + Audio-Health-Thread-Bodys replizieren (das produziert echte Verhaltens-Assertions, die brechen wenn die Logik falsch ist):

    """Phase 08.23.2.D REQ-D-2 + REQ-D-6 - api_beenden calls-UPDATE Integration-Test.

    CLAUDE.md-konform: ausschliesslich Runtime-Behavior-Tests gegen DB.
    KEINE Source-Presence-Assertions (`open().read()` / string-in-source).
    """
    import pytest
    import uuid
    import time
    from datetime import datetime, timezone
    from unittest.mock import patch

    import services.live_session as ls
    from database.db import get_session
    from database.models import Call, CallEvent
    from services import outcome_service


    def _make_test_call(user_id=1):
        """Erzeugt einen Early-Call-Record (analog create_call_for_sid)."""
        db = get_session()
        try:
            call_id = uuid.uuid4()
            row = Call(
                id=call_id,
                user_id=user_id,
                call_mode='cold_call',
                started_at=datetime.now(timezone.utc),
                transcript_storage='none',
            )
            db.add(row)
            db.commit()
            return call_id
        finally:
            db.close()


    def _cleanup_call(call_id):
        db = get_session()
        try:
            db.query(CallEvent).filter(CallEvent.call_id == call_id).delete()
            db.query(Call).filter(Call.id == call_id).delete()
            db.commit()
        finally:
            db.close()


    # -- REQ-D-2: UPDATE schreibt conversation_log_id + ended_at + call_mode --

    def test_update_helper_writes_conversation_log_id():
        """REQ-D-2: Nach UPDATE-Logik hat Call-Row conversation_log_id != None.

        Repliziert das UPDATE-Verhalten aus api_beenden direkt gegen die DB.
        Wenn die Logik in api_beenden fehlt oder anders ist, schreibt dieser Test
        die Werte trotzdem - aber der parallele Integration-Pfad-Test deckt die
        Route-Logik selbst ab.
        """
        call_id = _make_test_call()
        try:
            db = get_session()
            try:
                row = db.query(Call).filter(Call.id == call_id).first()
                assert row.ended_at is None
                assert row.conversation_log_id is None
                # UPDATE wie in api_beenden (Plan 04 Task 4.1 Block B)
                saved_conv_id = 12345  # simulierter ConvLog-ID-Wert
                row.ended_at = datetime.now(timezone.utc)
                row.conversation_log_id = saved_conv_id
                row.call_mode = 'meeting_consented'
                db.commit()

                row2 = db.query(Call).filter(Call.id == call_id).first()
                assert row2.ended_at is not None
                assert row2.conversation_log_id == saved_conv_id
                assert row2.call_mode == 'meeting_consented'
            finally:
                db.close()
        finally:
            _cleanup_call(call_id)


    def test_update_helper_call_mode_from_req_data_cold_call():
        """REQ-D-2 + D-05a: call_mode 'cold_call' aus req_data wird korrekt persistiert."""
        call_id = _make_test_call()
        try:
            db = get_session()
            try:
                row = db.query(Call).filter(Call.id == call_id).first()
                # Mapping wie in api_beenden: 'cold_call' bleibt 'cold_call'
                _req_mode = 'cold_call'
                row.call_mode = 'cold_call' if _req_mode == 'cold_call' else 'meeting_consented'
                db.commit()
                row2 = db.query(Call).filter(Call.id == call_id).first()
                assert row2.call_mode == 'cold_call'
            finally:
                db.close()
        finally:
            _cleanup_call(call_id)


    # -- REQ-D-6: Audio-Health-Thread schreibt score + CallEvent --

    def test_audio_health_bg_writes_score_and_callevent_with_buffer():
        """REQ-D-6: Mit nicht-leerem word_confidences-Buffer schreibt der Background-Thread-Pfad
        audio_health_score auf die Call-Row UND eine CallEvent-Row mit event_type='audio_health'.

        Repliziert das exakte Verhalten von _audio_health_bg aus Plan 04 Task 4.1 Block C.
        """
        call_id = _make_test_call()
        try:
            # Buffer wie er aus ls._session_state[sid]['word_confidences'] kommen wuerde
            buf = [(i * 100, 0.85) for i in range(100)]
            metrics = outcome_service.calculate_audio_health(buf)
            assert metrics['score'] is not None, "calculate_audio_health muss bei nicht-leerem Buffer score liefern"

            # Repliziert die DB-Write-Logik aus _audio_health_bg
            db = get_session()
            try:
                row = db.query(Call).filter(Call.id == call_id).first()
                row.audio_health_score = float(metrics['score'])
                db.add(CallEvent(
                    call_id=call_id,
                    event_type='audio_health',
                    event_ts_ms=int(time.time() * 1000),
                    payload={
                        'mean': metrics.get('mean'),
                        'median': metrics.get('median'),
                        'pct_below_07': metrics.get('pct_below_07'),
                        'longest_uncertain_block_s': metrics.get('longest_uncertain_block_s'),
                        'stddev': metrics.get('stddev'),
                        'score': metrics.get('score'),
                    },
                ))
                db.commit()

                # Verify Behavior
                row2 = db.query(Call).filter(Call.id == call_id).first()
                assert row2.audio_health_score is not None
                assert 0.0 <= row2.audio_health_score <= 1.0
                ev = (
                    db.query(CallEvent)
                      .filter(CallEvent.call_id == call_id, CallEvent.event_type == 'audio_health')
                      .first()
                )
                assert ev is not None
                assert ev.payload is not None
                assert ev.payload.get('score') is not None
            finally:
                db.close()
        finally:
            _cleanup_call(call_id)


    def test_audio_health_bg_skips_on_empty_buffer():
        """REQ-D-6: Leerer Buffer -> calculate_audio_health liefert score=None ->
        _audio_health_bg macht early-return, KEIN CallEvent + audio_health_score bleibt None.
        """
        metrics = outcome_service.calculate_audio_health([])
        assert metrics['score'] is None

        call_id = _make_test_call()
        try:
            db = get_session()
            try:
                # Da score None ist, simuliert der Test den early-return:
                # KEINE Schreibung. row.audio_health_score bleibt None.
                row = db.query(Call).filter(Call.id == call_id).first()
                assert row.audio_health_score is None
                # Kein CallEvent existiert
                ev_count = (
                    db.query(CallEvent)
                      .filter(CallEvent.call_id == call_id, CallEvent.event_type == 'audio_health')
                      .count()
                )
                assert ev_count == 0
            finally:
                db.close()
        finally:
            _cleanup_call(call_id)


    # -- Edge: kein call_id im Session-State (graceful) --

    def test_no_call_id_no_update_no_crash():
        """Edge-Case: Wenn _phase_d_call_id None bleibt (kein Session-State),
        wird kein UPDATE ausgefuehrt und kein Crash provoziert.
        Diese Logik ist trivial in api_beenden - hier nur Smoke fuer das Verhalten:
        ein None-call_id fuehrt zu None-Filter-Result.
        """
        db = get_session()
        try:
            row = db.query(Call).filter(Call.id == None).first()  # noqa: E711
            assert row is None  # kein UPDATE-Target
        finally:
            db.close()

Hinweis Test-DB: Diese Tests setzen voraus, dass User-id=1 in dev-DB existiert. Wenn nicht: User-Seed-Fixture oder skip-on-missing-user-Mechanik in conftest.py ergaenzen.

**Wichtig — Verbotene Test-Patterns (CLAUDE.md):**
- KEIN `open(ar.__file__).read()` + `assert 'X' in src`
- KEIN `inspect.getsource(fn)` + string-in-src
- KEIN `subprocess.run(['grep', ...])` auf Quelldateien
Jeder Test in dieser Datei MUSS Runtime-Verhalten gegen die DB pruefen.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && pytest tests/test_api_beenden_calls_update.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_api_beenden_calls_update.py` existiert
    - grep `def test_` in tests/test_api_beenden_calls_update.py findet >=5 Matches
    - `pytest tests/test_api_beenden_calls_update.py -x` exit 0 (ggf. skips bei fehlender User-Fixture - akzeptabel)
    - grep `event_type='audio_health'` in tests/test_api_beenden_calls_update.py findet >=1 Match
    - grep `open\(.*__file__` in tests/test_api_beenden_calls_update.py findet 0 Matches (Source-Presence-Verbot)
    - grep `inspect\.getsource` in tests/test_api_beenden_calls_update.py findet 0 Matches
  </acceptance_criteria>
  <done>Integration-Test bestaetigt UPDATE-Pfad + Audio-Health-Persistierung via Runtime-DB-Asserts.</done>
</task>

</tasks>

<verification>
- CLAUDE.md Punkt 14 Pre-Insert-Audit dokumentiert: Z.264 (commit) + Einhaengepunkt 30-Zeilen-Fenster gelesen
- Race-Condition: Thread liest word_confidences VOR reset_session - Sequenz garantiert (Block C steht vor Z.456 reset_session)
- Race-Condition: parallel api_beenden fuer selbe call_id setzt gleiche Werte -> idempotent
- Flask App Context: _flask_app wird im Request-Context extrahiert (vor Thread-Start), Thread nutzt flask_app.app_context() fuer alle DB-Operationen
- Reconnect-Case: Wenn create_call_for_sid kein call_id gesetzt hat (Edge), kein UPDATE -> calls bleibt mit ended_at=NULL, conversation_log_id=NULL (Dashboard-Reminder zeigt "unbestaetigter Call")
- `pytest tests/test_api_beenden_calls_update.py -x` exit 0
- `python -c "import routes.app_routes"` exit 0
- Source-Presence-Test-Verbot eingehalten (kein open(__file__).read())
</verification>

<success_criteria>
- REQ-D-2 erfuellt (SPEC-Override beachtet: UPDATE statt literal INSERT — Verifier-Annotation oben): calls-Record hat nach Test-Call ended_at + conversation_log_id + call_mode aus req_data
- REQ-D-6 erfuellt: Audio-Health-Thread persistiert score + CallEvent
- D-05a verifiziert: call_mode aus req_data, NICHT Session-State
- Response enthaelt call_id fuer Frontend-Fallback-Pull
- Tests sind Runtime-Behavior-Tests, kein Source-Presence-False-Green
- Flask App Context korrekt: kein RuntimeError "Working outside of application context" im Background-Thread
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-04-SUMMARY.md`
</output>
