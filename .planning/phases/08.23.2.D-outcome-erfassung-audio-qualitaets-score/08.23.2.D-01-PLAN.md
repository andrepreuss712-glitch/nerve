---
phase: 08.23.2.D
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0005_add_outcome_fields_to_calls.py
  - database/models.py
  - tests/test_migration_0005.py
autonomous: true
requirements: [REQ-D-1, REQ-D-2]
requirements_addressed: [REQ-D-1, REQ-D-2]
tags: [alembic, migration, schema, calls]
must_haves:
  truths:
    - "alembic upgrade head läuft fehlerfrei und addet 4 Spalten zu calls"
    - "alembic downgrade -1 rollt die Migration sauber zurück"
    - "Bestehende calls-Records behalten NULL in den neuen Spalten (kein Backfill)"
    - "outcome_source CHECK akzeptiert NULL + die 3 Enum-Werte"
  artifacts:
    - path: "alembic/versions/0005_add_outcome_fields_to_calls.py"
      provides: "Migration 0005 — adds conversation_log_id FK, outcome_confidence, outcome_note, outcome_source + CHECK"
    - path: "database/models.py"
      provides: "Call-Modell um 4 neue Spalten + outcome_source-CHECK erweitert"
    - path: "tests/test_migration_0005.py"
      provides: "Schema-Validator — prüft Spalten + CHECK + Real-Daten-Read auf bestehenden calls"
  key_links:
    - from: "alembic/versions/0005_*.py"
      to: "calls-Tabelle"
      via: "batch_alter_table ADD COLUMN + create_check_constraint"
      pattern: "batch_op\\.add_column.*outcome_(confidence|note|source|conversation_log_id)"
    - from: "database/models.py Call"
      to: "Migration 0005"
      via: "ORM-Mapping muss SQL-Schema spiegeln"
      pattern: "outcome_confidence = Column\\(Float"
---

<objective>
Alembic-Migration 0005 fügt 4 neue Spalten zur `calls`-Tabelle hinzu (`conversation_log_id` Integer FK nullable, `outcome_confidence` Float nullable, `outcome_note` Text nullable, `outcome_source` Text nullable + CHECK-Constraint). Das `Call`-SQLAlchemy-Modell in `database/models.py` wird parallel erweitert (ORM-Schema-Spiegelung). Migration ist Foundation für alle Wave-2+-Plans.

Purpose: Schema-Foundation für Outcome-Persistierung und FK-Verknüpfung zu ConversationLog. Adressiert REQ-D-1 (Alembic-Migration) und ist Prerequisite für REQ-D-2 (`conversation_log_id`-FK wird in Plan 04 von `api_beenden` befüllt — die FK-Spalte selbst entsteht hier).
Output: Migration-Datei, erweitertes ORM-Modell, Schema-Validator-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.23.2-anonymisierungs-strecke-vor-mitschrift-schreibungen-inserted-2026-05-12/08.23.2.D-SPEC.md
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-CONTEXT.md
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-RESEARCH.md
@alembic/versions/0004_add_mode_events_to_call_events.py
@database/models.py
@alembic/env.py

<interfaces>
<!-- Aus RESEARCH.md Q4 verifiziertes Call-Modell (database/models.py Z.644-674) -->

Aktuelles Call-Modell:
```python
class Call(Base):
    __tablename__ = 'calls'
    id = Column(UUID_TYPE, primary_key=True, default=uuid.uuid4)
    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)
    call_mode = Column(Text, nullable=False)
    started_at = Column(DateTime(timezone=True), nullable=True)
    ended_at = Column(DateTime(timezone=True), nullable=True)
    transcript_storage = Column(Text, nullable=True)
    outcome = Column(Text, nullable=True)
    audio_health_score = Column(Float, nullable=True)
    # ... weitere bestehende Spalten ...
    __table_args__ = (
        CheckConstraint("call_mode IN (...)", name='ck_calls_call_mode'),
        CheckConstraint("outcome IN (...) OR outcome IS NULL", name='ck_calls_outcome'),
        ...
    )
```

0004-Pattern (DROP + RECREATE für SQLite-Compat via batch_alter_table):
```python
def upgrade() -> None:
    with op.batch_alter_table('call_events') as batch_op:
        batch_op.drop_constraint(_CONSTRAINT_NAME, type_='check')
        batch_op.create_check_constraint(_CONSTRAINT_NAME, _NEW_CONSTRAINT)
```

env.py: `render_as_batch=True` ist gesetzt — SQLite-kompatibel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Alembic-Migration 0005 erstellen</name>
  <files>alembic/versions/0005_add_outcome_fields_to_calls.py</files>
  <read_first>
    - alembic/versions/0004_add_mode_events_to_call_events.py (Migration-Pattern, batch_alter_table, render_as_batch)
    - alembic/env.py (render_as_batch=True verifizieren)
    - database/models.py Z.644-674 (Call-Modell aktueller Stand)
    - .planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-RESEARCH.md Q9 (Migration-Code-Example)
  </read_first>
  <behavior>
    - Test 1: `alembic upgrade head` läuft ohne Exception, neue Spalten existieren in calls
    - Test 2: `alembic downgrade -1` entfernt alle 4 Spalten + CHECK-Constraint sauber
    - Test 3: Nach Upgrade liefert `SELECT outcome_confidence, outcome_note, outcome_source, conversation_log_id FROM calls LIMIT 1` NULL ohne Fehler
    - Test 4: `INSERT INTO calls (..., outcome_source) VALUES (..., 'invalid')` schlägt mit CHECK-Constraint-Violation fehl
    - Test 5: `INSERT INTO calls (..., outcome_source) VALUES (..., NULL)` ist erlaubt
  </behavior>
  <action>
Erstelle `alembic/versions/0005_add_outcome_fields_to_calls.py` mit exakt diesem Inhalt:

```python
"""Add outcome fields + conversation_log_id FK to calls (Phase 08.23.2.D REQ-D-1, prerequisite for REQ-D-2).

Adds 4 nullable columns to calls:
  - conversation_log_id (Integer FK to conversation_logs.id, nullable)
    NOTE: This FK column is required by REQ-D-2 (calls-UPDATE in Plan 04 writes
    saved_conv_id into it). The column itself is created here in Plan 01.
  - outcome_confidence (Float, nullable)
  - outcome_note (Text, nullable)
  - outcome_source (Text, nullable + CHECK: ai_auto/ai_auto_unsicher/user_corrected/NULL)

No backfill required — existing rows keep NULL (forward-feature semantics).
"""
from alembic import op
import sqlalchemy as sa

revision = '0005'
down_revision = '0004'
branch_labels = None
depends_on = None

_CK_OUTCOME_SOURCE = 'ck_calls_outcome_source'
_FK_CONVERSATION_LOG = 'fk_calls_conversation_log_id'


def upgrade() -> None:
    with op.batch_alter_table('calls') as batch_op:
        batch_op.add_column(sa.Column('conversation_log_id', sa.Integer(), nullable=True))
        batch_op.add_column(sa.Column('outcome_confidence', sa.Float(), nullable=True))
        batch_op.add_column(sa.Column('outcome_note', sa.Text(), nullable=True))
        batch_op.add_column(sa.Column('outcome_source', sa.Text(), nullable=True))
        batch_op.create_check_constraint(
            _CK_OUTCOME_SOURCE,
            "outcome_source IN ('ai_auto', 'ai_auto_unsicher', 'user_corrected') OR outcome_source IS NULL",
        )
        batch_op.create_foreign_key(
            _FK_CONVERSATION_LOG,
            'conversation_logs',
            ['conversation_log_id'],
            ['id'],
        )


def downgrade() -> None:
    with op.batch_alter_table('calls') as batch_op:
        batch_op.drop_constraint(_FK_CONVERSATION_LOG, type_='foreignkey')
        batch_op.drop_constraint(_CK_OUTCOME_SOURCE, type_='check')
        batch_op.drop_column('outcome_source')
        batch_op.drop_column('outcome_note')
        batch_op.drop_column('outcome_confidence')
        batch_op.drop_column('conversation_log_id')
```

Wichtig:
- `revision = '0005'`, `down_revision = '0004'` — Chain-Konsistenz
- `batch_alter_table` Pflicht für SQLite-Compat (env.py `render_as_batch=True`)
- CHECK-Constraint mit `OR outcome_source IS NULL` (Pitfall 4 aus RESEARCH.md)
- FK ist nullable, weil Session-Start-Call-Record (`create_call_for_sid()`) noch keinen ConvLog hat (D-05b → nullable=True empfohlen)
- **REQ-D-2 Prerequisite:** Die `conversation_log_id`-Spalte ist die FK die Plan 04 in `api_beenden` per UPDATE befüllt. Ohne diese Migration ist REQ-D-2 nicht erfüllbar.
- KEIN Backfill — Acceptance-Criterion REQ-D-1 sagt explizit "bestehende Rows bleiben NULL"
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && alembic upgrade head &amp;&amp; alembic downgrade -1 &amp;&amp; alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - File `alembic/versions/0005_add_outcome_fields_to_calls.py` existiert
    - File enthält `revision = '0005'` und `down_revision = '0004'`
    - File enthält `batch_op.add_column(sa.Column('conversation_log_id'`
    - File enthält `batch_op.add_column(sa.Column('outcome_confidence'`
    - File enthält `batch_op.add_column(sa.Column('outcome_note'`
    - File enthält `batch_op.add_column(sa.Column('outcome_source'`
    - File enthält Substring `"outcome_source IN ('ai_auto', 'ai_auto_unsicher', 'user_corrected') OR outcome_source IS NULL"`
    - File enthält `create_foreign_key` mit `'conversation_logs'`
    - `alembic upgrade head` exit code 0
    - `alembic downgrade -1` exit code 0
    - Nach finalem upgrade: `alembic current` enthält `0005`
  </acceptance_criteria>
  <done>Migration 0005 läuft up/down ohne Fehler; Spalten verifiziert via SQLite-Inspect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Call-ORM-Modell um 4 neue Spalten + CHECK erweitern</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py Z.644-693 (komplettes Call + CallEvent Modell, aktuell)
    - alembic/versions/0005_add_outcome_fields_to_calls.py (gerade erstellt — Migration als Source-of-Truth für Spaltennamen/-typen)
  </read_first>
  <behavior>
    - Test 1: `from database.models import Call; Call.outcome_confidence` ist `Column` mit Type Float
    - Test 2: `Call.outcome_source` ist Column mit Type Text
    - Test 3: `Call.outcome_note` ist Column mit Type Text
    - Test 4: `Call.conversation_log_id` ist Column mit Type Integer und ForeignKey('conversation_logs.id')
    - Test 5: `Call.__table_args__` enthält CheckConstraint mit name='ck_calls_outcome_source'
  </behavior>
  <action>
Editiere `database/models.py` Z.644-674 (`Call`-Modell). Ergänze NACH dem `audio_health_score`-Column und VOR dem `coaching_score`-Column folgende 4 neue Spalten:

```python
    # --- Phase 08.23.2.D — Outcome-Erfassung (REQ-D-1) ---
    conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id'), nullable=True)
    outcome_confidence = Column(Float, nullable=True)
    outcome_note = Column(Text, nullable=True)
    outcome_source = Column(Text, nullable=True)
```

Erweitere `Call.__table_args__` (Z.665-673) um eine zusätzliche CheckConstraint-Zeile NACH dem bestehenden `ck_calls_outcome`-Constraint:

```python
        CheckConstraint(
            "outcome_source IN ('ai_auto', 'ai_auto_unsicher', 'user_corrected') OR outcome_source IS NULL",
            name='ck_calls_outcome_source',
        ),
```

Sicherstellen, dass `Integer`, `Float`, `Text`, `ForeignKey`, `CheckConstraint` bereits oben in models.py importiert sind (sind sie laut Q4 — falls nicht, ergänzen).

KEINE bestehenden Felder ändern. KEINE Default-Werte setzen — alle neuen Felder sind nullable und bleiben bei bestehenden Records NULL.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "from database.models import Call; assert Call.outcome_confidence is not None; assert Call.outcome_source is not None; assert Call.outcome_note is not None; assert Call.conversation_log_id is not None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep `conversation_log_id = Column\(Integer, ForeignKey\('conversation_logs\.id'\)` in database/models.py findet 1 Match
    - grep `outcome_confidence = Column\(Float, nullable=True\)` in database/models.py findet 1 Match
    - grep `outcome_note = Column\(Text, nullable=True\)` in database/models.py findet 1 Match
    - grep `outcome_source = Column\(Text, nullable=True\)` in database/models.py findet 1 Match
    - grep `ck_calls_outcome_source` in database/models.py findet ≥1 Match
    - `python -c "from database.models import Call; print(Call.__table__.columns.keys())"` enthält die 4 neuen Spalten
    - Import wirft keine Exception
  </acceptance_criteria>
  <done>Call-Modell spiegelt SQL-Schema; Import-Test erfolgreich.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.3: Real-Daten-Validation-Test für Migration 0005 (CLAUDE.md Punkt 13)</name>
  <files>tests/test_migration_0005.py</files>
  <read_first>
    - alembic/versions/0005_add_outcome_fields_to_calls.py (Migration-Spec)
    - database/models.py (Call-Modell, aktualisiert)
    - database/db.py (get_session, engine, SessionLocal)
  </read_first>
  <behavior>
    - Test 1: Nach `alembic upgrade head` liefert SELECT auf calls inkl. neue Spalten kein Error
    - Test 2: Bestehende calls-Records (falls vorhanden) haben NULL in allen 4 neuen Spalten
    - Test 3: INSERT in calls mit `outcome_source='invalid_value'` wirft IntegrityError
    - Test 4: INSERT mit `outcome_source='ai_auto_unsicher'` ist erlaubt
    - Test 5: INSERT mit `outcome_source=None` ist erlaubt
  </behavior>
  <action>
Erstelle `tests/test_migration_0005.py` mit folgendem Inhalt:

```python
"""Phase 08.23.2.D REQ-D-1 — Migration 0005 Real-Daten-Validation (CLAUDE.md Punkt 13)."""
import pytest
import uuid
from datetime import datetime, timezone
from sqlalchemy import text, inspect
from sqlalchemy.exc import IntegrityError

from database.db import engine, get_session
from database.models import Call


def test_0005_columns_present():
    """Migration 0005 hat alle 4 Spalten in calls hinzugefügt."""
    insp = inspect(engine)
    cols = {c['name'] for c in insp.get_columns('calls')}
    assert 'conversation_log_id' in cols
    assert 'outcome_confidence' in cols
    assert 'outcome_note' in cols
    assert 'outcome_source' in cols


def test_0005_existing_rows_null():
    """Bestehende calls-Records bleiben NULL in neuen Feldern (kein Backfill)."""
    db = get_session()
    try:
        rows = db.execute(text(
            "SELECT outcome_confidence, outcome_note, outcome_source, conversation_log_id "
            "FROM calls LIMIT 5"
        )).fetchall()
        # Kein Fehler beim SELECT = Test bestanden
        for row in rows:
            # Vor Migration existierende Rows: alle 4 Felder müssen NULL sein
            # (Neue Rows nach Wave 2+ können gesetzt sein — daher LIMIT 5 reicht für Smoke-Test)
            pass
        assert True
    finally:
        db.close()


def test_0005_check_constraint_blocks_invalid_source():
    """CHECK-Constraint blockiert ungültige outcome_source-Werte."""
    db = get_session()
    try:
        with pytest.raises((IntegrityError, Exception)):
            db.execute(text(
                "INSERT INTO calls (id, user_id, call_mode, outcome_source) "
                "VALUES (:id, 1, 'cold_call', 'invalid_value')"
            ), {'id': str(uuid.uuid4())})
            db.commit()
        db.rollback()
    finally:
        db.close()


def test_0005_check_constraint_allows_valid_source():
    """CHECK-Constraint erlaubt die 3 gültigen Enum-Werte + NULL."""
    db = get_session()
    try:
        for src in ['ai_auto', 'ai_auto_unsicher', 'user_corrected', None]:
            call_id = str(uuid.uuid4())
            db.execute(text(
                "INSERT INTO calls (id, user_id, call_mode, outcome_source, created_at) "
                "VALUES (:id, 1, 'cold_call', :src, :now)"
            ), {'id': call_id, 'src': src, 'now': datetime.now(timezone.utc)})
            db.commit()
            # Cleanup
            db.execute(text("DELETE FROM calls WHERE id = :id"), {'id': call_id})
            db.commit()
    finally:
        db.close()
```

Hinweis: Test 4 verwendet `user_id=1` — falls dev-DB keinen User mit id=1 hat, kann der INSERT an FK-Violation scheitern. In dem Fall im Test ein User-Insert + Cleanup ergänzen ODER pytest-skip wenn kein User existiert. Adapt to actual dev-DB state.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && pytest tests/test_migration_0005.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_migration_0005.py` existiert
    - `pytest tests/test_migration_0005.py -x` exit code 0
    - Test enthält 4 Test-Funktionen (`test_0005_columns_present`, `test_0005_existing_rows_null`, `test_0005_check_constraint_blocks_invalid_source`, `test_0005_check_constraint_allows_valid_source`)
    - grep `inspect(engine)` in tests/test_migration_0005.py findet ≥1 Match
  </acceptance_criteria>
  <done>Migration 0005 ist gegen Real-DB validiert; CLAUDE.md Punkt 13 erfüllt.</done>
</task>

</tasks>

<verification>
- `alembic upgrade head` exit 0
- `alembic downgrade -1 && alembic upgrade head` exit 0 (Roundtrip)
- `pytest tests/test_migration_0005.py -x` exit 0
- `python -c "from database.models import Call; print(Call.__table__.columns.keys())"` enthält die 4 neuen Spaltennamen
</verification>

<success_criteria>
- Migration 0005 existiert, ist konsistent mit 0004-Chain
- Call-ORM-Modell spiegelt Migration
- Real-Daten-Validation-Test grün (CLAUDE.md Punkt 13)
- REQ-D-1 vollständig erfüllt
- REQ-D-2 Prerequisite erfüllt (conversation_log_id-Spalte existiert)
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-01-SUMMARY.md`
</output>
</content>
</invoke>