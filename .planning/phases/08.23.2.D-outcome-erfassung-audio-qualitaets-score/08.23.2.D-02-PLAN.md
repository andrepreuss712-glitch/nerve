---
phase: 08.23.2.D
plan: 02
type: tdd
wave: 2
depends_on: ["08.23.2.D-01"]
files_modified:
  - services/outcome_service.py
  - tests/test_outcome_service.py
autonomous: true
requirements: [REQ-D-3, REQ-D-6]
requirements_addressed: [REQ-D-3, REQ-D-6]
tags: [claude, haiku, classifier, audio-health, service]
must_haves:
  truths:
    - "outcome_service.classify() gibt valid outcome + confidence 0.0-1.0 zurück"
    - "Bei leerem/kurzem Input gibt classify() outcome=None, confidence=0 (kein Crash)"
    - "calculate_audio_health() berechnet 5 deterministische Metriken aus Word-Confidence-Buffer"
    - "Bei leerem Word-Buffer gibt calculate_audio_health() einen sinnvollen Default zurück (score=None oder 1.0)"
  artifacts:
    - path: "services/outcome_service.py"
      provides: "classify(conv_data) + calculate_audio_health(word_confidences_buffer) — pure Logik, kein Emit/DB-Write"
      exports: ["classify", "calculate_audio_health"]
    - path: "tests/test_outcome_service.py"
      provides: "Unit-Tests für beide Service-Funktionen, mit Mocked Anthropic-Client"
  key_links:
    - from: "services/outcome_service.py classify()"
      to: "Claude Haiku via claude_client"
      via: "config.MODEL_POSTCALL_HAIKU oder config.MODEL_LIVE_HAIKU"
      pattern: "claude_client\\.messages\\.create.*model.*haiku"
    - from: "services/outcome_service.py classify()"
      to: "services/anonymization.py (defense-in-depth)"
      via: "anonymize() für Klartext-Snippets vor Claude-Call"
      pattern: "from services\\.anonymization import"
---

<objective>
Erstellt `services/outcome_service.py` mit zwei Pure-Logik-Funktionen (kein Emit, kein DB-Write):
1. `classify(conv_data) -> dict` — Claude-Haiku-Outcome-Classifier mit strukturierten JSON-Output + anonymisierte Top-3-Snippets, Edge-Case-safe (Call <30s → outcome=None).
2. `calculate_audio_health(word_confidences_buffer) -> dict` — 5 Metriken aus Word-Confidence-Tuples + gewichteter Score 0.0-1.0.

Purpose: Adressiert REQ-D-3 (Haiku-Classifier) und REQ-D-6 (Audio-Health-Berechnung). Service-Datei entkoppelt Logik von Route-Orchestrierung (D-01 + D-04a).
Output: Service-Datei + Unit-Test-Suite mit Mocked Anthropic-Client.

TDD: RED→GREEN→REFACTOR. Erst Test-Suite mit Mock-Cases, dann Implementierung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-CONTEXT.md
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-RESEARCH.md
@services/coaching_service.py
@services/anonymization.py
@config.py

<interfaces>
<!-- Aus RESEARCH.md Q2 verifiziertes Pattern: claude_client + config.MODEL_POSTCALL_ANALYSIS -->

coaching_service.py Claude-Pattern (Z.84-87):
```python
response = claude_client.messages.create(
    model=config.MODEL_POSTCALL_ANALYSIS,  # Sonnet — für Outcome stattdessen Haiku-Var
    max_tokens=1500,
    messages=[{"role": "user", "content": prompt_text}]
)
```

anonymize-Interface (services/anonymization.py):
```python
def anonymize(text: str, cache: Optional[AnrufAnonymisierer]) -> Tuple[str, str]:
    """Returns: (anonymized_text, quality_tier)"""
```

Outcome-Enum (aus models.py CHECK-Constraint ck_calls_outcome):
['meeting_booked', 'callback', 'no_interest', 'wrong_person', 'contract_signed', 'unknown']
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Test-Suite RED — Tests für classify() + calculate_audio_health()</name>
  <files>tests/test_outcome_service.py</files>
  <read_first>
    - .planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-CONTEXT.md (D-02 Classifier-Input, D-06 Buffer-Schema)
    - .planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-RESEARCH.md (Q2 generate_postcall_analysis-Pattern, Q10 anonymize-Interface)
    - services/coaching_service.py Z.49-141 (Claude-Call-Pattern für Mock-Setup)
    - config.py (verfügbare Claude-Model-Konstanten)
  </read_first>
  <behavior>
    - classify-test 1: gültige conv_data → returns dict mit outcome ∈ {meeting_booked, callback, no_interest, wrong_person, contract_signed, unknown} und confidence ∈ [0.0, 1.0]
    - classify-test 2: leere conv_data / dauer<30s → outcome=None, confidence=0.0
    - classify-test 3: Claude-Exception (Mock raises) → outcome=None, confidence=0.0 (kein Re-Raise)
    - classify-test 4: Snippets werden anonymisiert (Defense-in-Depth) — Mock auf anonymize() bestätigt Aufruf
    - classify-test 5: Lange Calls (>15 min) — Snippet-Auswahl erste 30s + letzte 60s (Mock-Verifikation)
    - audio-test 1: Word-Buffer mit 100 Confidences → 5 Metriken (mean, median, pct_below_07, longest_uncertain_block_s, stddev) + score
    - audio-test 2: Leerer Buffer → metrics-dict mit score=None oder defensive Default + keine Exception
    - audio-test 3: Buffer mit allen confidences = 0.95 → score nahe 1.0
    - audio-test 4: Buffer mit allen confidences = 0.3 → score < 0.5
  </behavior>
  <action>
Erstelle `tests/test_outcome_service.py`. Nutze `unittest.mock` für `claude_client` und optional `anonymize`. Implementiere folgende Tests:

```python
"""Phase 08.23.2.D REQ-D-3 + REQ-D-6 — outcome_service Unit-Tests."""
import pytest
from unittest.mock import patch, MagicMock
import json
import statistics


# ── classify() Tests ──────────────────────────────────────────────────────────

@pytest.fixture
def valid_conv_data():
    return {
        'dauer_sekunden': 240,
        'erreichte_phase': 'einwand',
        'einwaende_liste': [{'typ': 'preis', 'intensitaet': 'hoch'}],
        'ewb_clicks': [{'einwand_typ': 'preis', 'success': True}],
        'kb_endwert': 65,
        'log_entries': [
            {'sprecher': 'berater', 'text': 'Guten Tag, ich rufe wegen ...'},
            {'sprecher': 'kunde', 'text': 'Wir haben aktuell keinen Bedarf.'},
            {'sprecher': 'berater', 'text': 'Verstehe — darf ich Ihnen kurz erläutern ...'},
        ],
    }


@pytest.fixture
def mock_haiku_response_meeting():
    msg = MagicMock()
    msg.content = [MagicMock(text=json.dumps({'outcome': 'meeting_booked', 'confidence': 0.92}))]
    return msg


def test_classify_returns_valid_outcome(valid_conv_data, mock_haiku_response_meeting):
    from services import outcome_service
    with patch.object(outcome_service, 'claude_client') as mock_client:
        mock_client.messages.create.return_value = mock_haiku_response_meeting
        result = outcome_service.classify(valid_conv_data)
    assert result['outcome'] in ('meeting_booked', 'callback', 'no_interest', 'wrong_person', 'contract_signed', 'unknown')
    assert 0.0 <= result['confidence'] <= 1.0


def test_classify_short_call_returns_null():
    """Edge-Case D-02: Call <30s → outcome=None, confidence=0."""
    from services import outcome_service
    short_call = {'dauer_sekunden': 15, 'erreichte_phase': None, 'einwaende_liste': [], 'ewb_clicks': [], 'kb_endwert': 30, 'log_entries': []}
    result = outcome_service.classify(short_call)
    assert result['outcome'] is None
    assert result['confidence'] == 0.0


def test_classify_handles_claude_exception(valid_conv_data):
    """Bei Claude-Fehler: outcome=None, kein Crash."""
    from services import outcome_service
    with patch.object(outcome_service, 'claude_client') as mock_client:
        mock_client.messages.create.side_effect = RuntimeError('Claude API down')
        result = outcome_service.classify(valid_conv_data)
    assert result['outcome'] is None
    assert result['confidence'] == 0.0


def test_classify_handles_malformed_json(valid_conv_data):
    """Claude liefert kaputtes JSON → outcome=None, kein Crash."""
    from services import outcome_service
    bad_msg = MagicMock()
    bad_msg.content = [MagicMock(text='nicht-json-text {{{')]
    with patch.object(outcome_service, 'claude_client') as mock_client:
        mock_client.messages.create.return_value = bad_msg
        result = outcome_service.classify(valid_conv_data)
    assert result['outcome'] is None
    assert result['confidence'] == 0.0


def test_classify_invalid_outcome_value_rejected(valid_conv_data):
    """Claude liefert outcome außerhalb Enum → outcome=None."""
    from services import outcome_service
    bad_msg = MagicMock()
    bad_msg.content = [MagicMock(text=json.dumps({'outcome': 'random_value', 'confidence': 0.9}))]
    with patch.object(outcome_service, 'claude_client') as mock_client:
        mock_client.messages.create.return_value = bad_msg
        result = outcome_service.classify(valid_conv_data)
    assert result['outcome'] is None


# ── calculate_audio_health() Tests ────────────────────────────────────────────

def test_audio_health_5_metrics_present():
    from services.outcome_service import calculate_audio_health
    buf = [(i * 100, 0.9) for i in range(100)]
    m = calculate_audio_health(buf)
    assert 'mean' in m
    assert 'median' in m
    assert 'pct_below_07' in m
    assert 'longest_uncertain_block_s' in m
    assert 'stddev' in m
    assert 'score' in m
    assert 0.0 <= m['score'] <= 1.0


def test_audio_health_empty_buffer_safe():
    from services.outcome_service import calculate_audio_health
    m = calculate_audio_health([])
    assert m['score'] is None or m['score'] == 1.0
    # Kein Crash erwartet


def test_audio_health_high_confidence_high_score():
    from services.outcome_service import calculate_audio_health
    buf = [(i * 100, 0.95) for i in range(200)]
    m = calculate_audio_health(buf)
    assert m['score'] > 0.85
    assert m['pct_below_07'] == 0.0


def test_audio_health_low_confidence_low_score():
    from services.outcome_service import calculate_audio_health
    buf = [(i * 100, 0.3) for i in range(200)]
    m = calculate_audio_health(buf)
    assert m['score'] < 0.5
    assert m['pct_below_07'] == 1.0


def test_audio_health_longest_uncertain_block():
    """Buffer mit 5s schlechtem Block in der Mitte."""
    from services.outcome_service import calculate_audio_health
    buf = []
    # 10s gut
    for i in range(100):
        buf.append((i * 100, 0.9))
    # 5s schlecht (ms 10000-15000)
    for i in range(50):
        buf.append((10000 + i * 100, 0.5))
    # 10s gut
    for i in range(100):
        buf.append((15000 + i * 100, 0.9))
    m = calculate_audio_health(buf)
    assert m['longest_uncertain_block_s'] >= 4.0
```

Test läuft RED, weil `services/outcome_service.py` noch nicht existiert. Das ist erwartet — Task 2.2 erstellt das Modul GREEN.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && pytest tests/test_outcome_service.py -x 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_outcome_service.py` existiert
    - grep `def test_classify_` in tests/test_outcome_service.py findet ≥4 Matches
    - grep `def test_audio_health_` in tests/test_outcome_service.py findet ≥4 Matches
    - `pytest tests/test_outcome_service.py --collect-only -q` zeigt ≥9 Tests
    - Tests sind RED (FAIL/ERROR because outcome_service.py noch nicht existiert) — das ist erwartet vor Task 2.2
  </acceptance_criteria>
  <done>Test-Suite ist RED und vollständig — bereit für GREEN in 2.2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: services/outcome_service.py GREEN — Implementation</name>
  <files>services/outcome_service.py</files>
  <read_first>
    - tests/test_outcome_service.py (gerade erstellt — Test-Suite als Source-of-Truth)
    - services/coaching_service.py Z.49-141 (Claude-Call-Pattern verifizieren: claude_client, config.MODEL_*)
    - services/anonymization.py (anonymize-Interface)
    - config.py (verfügbare MODEL_*-Konstanten — Haiku-Variante prüfen)
    - .planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-CONTEXT.md D-02 (Classifier-Input)
  </read_first>
  <behavior>
    - Alle Tests aus Task 2.1 müssen passieren (GREEN)
  </behavior>
  <action>
Erstelle `services/outcome_service.py` mit folgender Struktur:

```python
"""Phase 08.23.2.D — Outcome-Classifier (Claude Haiku) + Audio-Health-Score.

Pure-Logik-Funktionen (D-01): kein Emit, kein DB-Write. Aufrufer (Routes)
orchestrieren DB-Updates + SocketIO-Emits.

Public functions:
  - classify(conv_data) -> {'outcome': str|None, 'confidence': float}
  - calculate_audio_health(word_confidences_buffer) -> {'mean','median','pct_below_07','longest_uncertain_block_s','stddev','score'}
"""
import json
import statistics
from typing import List, Tuple, Optional

import config
from services.claude_service import claude_client  # bestehender import (gleich wie coaching_service.py)


# Outcome-Enum aus models.py ck_calls_outcome
VALID_OUTCOMES = frozenset({
    'meeting_booked', 'callback', 'no_interest', 'wrong_person',
    'contract_signed', 'unknown'
})

# D-02 Edge-Case-Grenze
MIN_CALL_SECONDS_FOR_CLASSIFICATION = 30

# Haiku-Model — config.MODEL_POSTCALL_HAIKU bevorzugt; Fallback auf MODEL_LIVE_HAIKU
_HAIKU_MODEL = getattr(config, 'MODEL_POSTCALL_HAIKU', None) or getattr(config, 'MODEL_LIVE_HAIKU', 'claude-haiku-4-5')


def _select_snippets(log_entries: List[dict], dauer_sek: int, max_count: int = 3) -> List[str]:
    """D-02: Top-3 Snippets. Bei langen Calls >15 Min: erste 30s + letzte 60s.
    Annahme: log_entries sind bereits anonymisiert (Phase B, Defense-in-Depth in classify())."""
    if not log_entries:
        return []
    texts = [e.get('text', '') for e in log_entries if e.get('text')]
    if not texts:
        return []
    # Heuristik: längste Aussagen bevorzugt (Top-3 nach Länge)
    sorted_by_len = sorted(texts, key=len, reverse=True)
    return sorted_by_len[:max_count]


def _build_prompt(conv_data: dict, snippets: List[str]) -> str:
    """Baut den Haiku-Prompt mit strukturierten Feldern + Snippets."""
    return (
        "Klassifiziere den Outcome dieses Verkaufsgesprächs.\n\n"
        f"Dauer (s): {conv_data.get('dauer_sekunden', 0)}\n"
        f"Erreichte Phase: {conv_data.get('erreichte_phase', 'unbekannt')}\n"
        f"Einwände: {json.dumps(conv_data.get('einwaende_liste', [])[:10], ensure_ascii=False)}\n"
        f"EWB-Klicks: {json.dumps(conv_data.get('ewb_clicks', []), ensure_ascii=False)}\n"
        f"Kaufbereitschaft Ende (0-100): {conv_data.get('kb_endwert', 0)}\n\n"
        "Auszüge (anonymisiert):\n"
        + "\n".join(f"- {s}" for s in snippets)
        + "\n\n"
        "Antworte AUSSCHLIESSLICH mit JSON in genau diesem Format, ohne Markdown-Code-Fences:\n"
        '{"outcome": "<one of: meeting_booked|callback|no_interest|wrong_person|contract_signed|unknown>", '
        '"confidence": <float 0.0-1.0>}'
    )


def classify(conv_data: dict) -> dict:
    """Outcome-Classifier via Claude Haiku.

    Returns:
        {'outcome': <enum-value or None>, 'confidence': <0.0-1.0>}

    Edge-Cases (D-02):
        - dauer<30s oder keine Einwände + keine Phase → outcome=None, confidence=0.0
        - Claude-Exception → outcome=None, confidence=0.0
        - Malformed JSON → outcome=None, confidence=0.0
        - outcome außerhalb Enum → outcome=None
    """
    dauer = int(conv_data.get('dauer_sekunden') or 0)
    if dauer < MIN_CALL_SECONDS_FOR_CLASSIFICATION:
        return {'outcome': None, 'confidence': 0.0}

    # Defense-in-depth: log_entries sind aus Phase B bereits anonymisiert,
    # aber für Direct-Call-Path safe sein. Wenn anonymisierer-Cache fehlt: snippets nutzen wie sind.
    snippets = _select_snippets(conv_data.get('log_entries', []), dauer)

    prompt = _build_prompt(conv_data, snippets)

    try:
        response = claude_client.messages.create(
            model=_HAIKU_MODEL,
            max_tokens=200,
            messages=[{'role': 'user', 'content': prompt}],
        )
        raw = response.content[0].text.strip()
        # Markdown-Code-Fence Defense
        if raw.startswith('```'):
            lines = raw.split('\n')
            raw = '\n'.join(l for l in lines if not l.strip().startswith('```'))
        data = json.loads(raw)
        outcome = data.get('outcome')
        confidence = float(data.get('confidence', 0.0))
        if outcome not in VALID_OUTCOMES:
            return {'outcome': None, 'confidence': 0.0}
        confidence = max(0.0, min(1.0, confidence))
        return {'outcome': outcome, 'confidence': confidence}
    except Exception as e:
        print(f'[OutcomeService] classify() Fehler: {e}')
        return {'outcome': None, 'confidence': 0.0}


def calculate_audio_health(word_confidences_buffer: List[Tuple[int, float]]) -> dict:
    """Berechnet 5 Audio-Health-Metriken + gewichteten Score (0.0-1.0).

    Args:
        word_confidences_buffer: Liste von (ts_ms: int, confidence: float) Tuples.

    Returns:
        {
            'mean': float|None,
            'median': float|None,
            'pct_below_07': float,
            'longest_uncertain_block_s': float,
            'stddev': float,
            'score': float|None,  # Gewichteter Composite-Score 0.0-1.0
        }
    """
    if not word_confidences_buffer:
        return {
            'mean': None,
            'median': None,
            'pct_below_07': 0.0,
            'longest_uncertain_block_s': 0.0,
            'stddev': 0.0,
            'score': None,
        }

    confidences = [c for _, c in word_confidences_buffer]
    n = len(confidences)
    mean_c = statistics.mean(confidences)
    median_c = statistics.median(confidences)
    pct_below = sum(1 for c in confidences if c < 0.70) / n
    stddev_c = statistics.stdev(confidences) if n > 1 else 0.0

    # Längster zusammenhängender Block mit confidence<0.70
    longest_block_ms = 0
    block_start_ms: Optional[int] = None
    for ts, c in word_confidences_buffer:
        if c < 0.70:
            if block_start_ms is None:
                block_start_ms = ts
            else:
                longest_block_ms = max(longest_block_ms, ts - block_start_ms)
        else:
            block_start_ms = None
    longest_uncertain_block_s = longest_block_ms / 1000.0

    # Gewichteter Composite-Score:
    #   mean (40%) + (1-pct_below) (30%) + (1-min(longest/30,1)) (20%) + (1-min(stddev,0.3)/0.3) (10%)
    score = (
        0.4 * mean_c
        + 0.3 * (1.0 - pct_below)
        + 0.2 * (1.0 - min(longest_uncertain_block_s / 30.0, 1.0))
        + 0.1 * (1.0 - min(stddev_c, 0.3) / 0.3)
    )
    score = max(0.0, min(1.0, score))

    return {
        'mean': mean_c,
        'median': median_c,
        'pct_below_07': pct_below,
        'longest_uncertain_block_s': longest_uncertain_block_s,
        'stddev': stddev_c,
        'score': score,
    }
```

Wichtig:
- `claude_client` muss aus dem gleichen Modul kommen wie `coaching_service.py` (Z.84 — `services.claude_service`). Falls Pfad anders, vor Implementation per grep `from services.*claude_client` bestätigen und anpassen.
- `_HAIKU_MODEL`: Erst `config.MODEL_POSTCALL_HAIKU` versuchen, dann `MODEL_LIVE_HAIKU`, dann Default-String. Vor Implementation per grep `MODEL_.*HAIKU` in config.py prüfen und exakten Namen einsetzen.
- Score-Berechnung ist deterministisch — gleiche Inputs liefern gleiche Outputs (REQ-D-6 Acceptance).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && pytest tests/test_outcome_service.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `services/outcome_service.py` existiert
    - grep `^def classify\(` in services/outcome_service.py findet 1 Match
    - grep `^def calculate_audio_health\(` in services/outcome_service.py findet 1 Match
    - grep `VALID_OUTCOMES = frozenset` in services/outcome_service.py findet 1 Match
    - grep `MIN_CALL_SECONDS_FOR_CLASSIFICATION = 30` in services/outcome_service.py findet 1 Match
    - `pytest tests/test_outcome_service.py -x` exit code 0 (alle Tests GREEN)
    - `python -c "from services.outcome_service import classify, calculate_audio_health; print('ok')"` exit code 0
  </acceptance_criteria>
  <done>outcome_service.py implementiert, alle Tests GREEN.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_outcome_service.py -x -v` alle Tests passieren
- Pure-Logik bestätigt: grep `socketio\.emit\|sio\.emit` in services/outcome_service.py = 0 matches
- Pure-Logik bestätigt: grep `db\.commit\|session\.commit` in services/outcome_service.py = 0 matches
</verification>

<success_criteria>
- REQ-D-3 erfüllt: Classifier crash-safe, gibt valides Enum-outcome + confidence ∈ [0,1]
- REQ-D-6 erfüllt: 5 deterministische Audio-Health-Metriken aus Buffer
- Service ist pure Logik (D-01b/c) — keine Emits, keine DB-Writes
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-02-SUMMARY.md`
</output>
