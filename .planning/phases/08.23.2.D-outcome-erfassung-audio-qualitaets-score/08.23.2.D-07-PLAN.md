---
phase: 08.23.2.D
plan: 07
type: execute
wave: 6
depends_on: ["08.23.2.D-01", "08.23.2.D-05"]
files_modified:
  - routes/performance.py
  - templates/dashboard.html
  - static/nerve.css
  - tests/test_dashboard_outcome_reminder.py
  - .planning/04 Entscheidungen/Foundation-Code-Register.md
autonomous: false
requirements: [REQ-D-8, REQ-D-9, REQ-D-10]
requirements_addressed: [REQ-D-8, REQ-D-9, REQ-D-10]
tags: [dashboard, reminder, inline-correction, brand-tokens, foundation-code-register]
user_setup: []
must_haves:
  truths:
    - "Dashboard-Endpoint /api/dashboard liefert recent_sessions mit outcome + outcome_source pro Session (JOIN calls via conversation_log_id)"
    - "Dashboard-Endpoint /api/dashboard liefert unsichere_outcomes_count fuer 7-Tage-Reminder-Card"
    - "Reminder-Card erscheint NUR wenn unsichere_outcomes_count > 0; Brand-Konsistenz: Left-Border + Background Teal (#00D4AA, KEIN Gelb)"
    - "Calls-Liste rendert Unsicher-Marker (8px Teal-Outline-Kreis) bei outcome_source='ai_auto_unsicher'"
    - "Klick auf Unsicher-Marker oeffnet Inline-Korrektur (5 Buttons + Anonymize-Notiz nicht-noetig hier - nur Outcome-Auswahl)"
    - "Auswahl im Inline-Korrektur -> POST /api/calls/<id>/correct_outcome -> Row aktualisiert sich + Marker verschwindet"
    - "Foundation-Code-Register kanonisch in .planning/04 Entscheidungen/Foundation-Code-Register.md aktualisiert mit Phase-D-Eintraegen (calls.outcome_confidence, calls.outcome_source, calls.outcome_note, calls.conversation_log_id)"
  artifacts:
    - path: "routes/performance.py"
      provides: "api_dashboard ergaenzt um calls-JOIN (outcome + outcome_source) + unsichere_outcomes_count"
    - path: "templates/dashboard.html"
      provides: "Reminder-Card-Markup + Calls-Listen-Render erweitert um Unsicher-Marker + Inline-Korrektur-Logic"
    - path: "static/nerve.css"
      provides: ".n-outcome-reminder-stripe (Teal-Left-Border) + .outcome-unsicher-dot + .outcome-correction-inline-row"
    - path: "tests/test_dashboard_outcome_reminder.py"
      provides: "Tests: JOIN-Logic, 7-Tage-Filter, /api/dashboard endpoint integration test"
    - path: ".planning/04 Entscheidungen/Foundation-Code-Register.md"
      provides: "Kanonische Foundation-Code-Register-Eintraege fuer 4 neue calls-Spalten (REQ-D-10)"
  key_links:
    - from: "routes/performance.py api_dashboard"
      to: "calls-Tabelle (Call.outcome, Call.outcome_source)"
      via: "JOIN auf ConversationLog.id = Call.conversation_log_id"
      pattern: "Call\\.conversation_log_id|outer.*Call"
    - from: "templates/dashboard.html"
      to: "POST /api/calls/<id>/correct_outcome"
      via: "Inline-Click-Handler im Calls-Listen-Row"
      pattern: "correct_outcome"
---

<objective>
Implementiert Dashboard-UX fuer Outcome-Reminder + Inline-Korrektur (REQ-D-8, REQ-D-9, REQ-D-10):

1. **`routes/performance.py::api_dashboard`** erweitern:
   - `recent_sessions`-Loop joint Call-Tabelle ueber `Call.conversation_log_id = ConversationLog.id` und liefert pro Session `outcome` + `outcome_source` + `call_id` mit.
   - Neuer Key `unsichere_outcomes_count`: COUNT der Calls in den letzten 7 Tagen mit `outcome_source='ai_auto_unsicher'` OR `outcome IS NULL`.

2. **`templates/dashboard.html`**:
   - Reminder-Stripe-Markup (versteckt per default, gezeigt wenn count>0): Teal-Left-Border, Heading "{N} Anruf-Outcomes warten auf Bestaetigung", Body "Letzte 7 Tage - klicke auf einen Anruf um den Outcome zu korrigieren."
   - `renderSessions()` Funktion erweitert: pro Row Outcome-Label rendern + 8px Teal-Outline-Kreis bei `outcome_source==='ai_auto_unsicher'`.
   - Inline-Korrektur: Click auf Unsicher-Marker expandiert Row mit 5 n-btn-ghost-Buttons; Auswahl postet an `/api/calls/<id>/correct_outcome`; bei Success Row collapsed + Marker weg + Outcome-Label aktualisiert.

3. **`static/nerve.css`**: 3 neue Klassen (`.n-outcome-reminder-stripe`, `.outcome-unsicher-dot`, `.outcome-correction-inline-row`). Brand-Lock: ausschliesslich Teal-Familie.

4. **Foundation-Code-Register kanonisch updaten** (REQ-D-10): Task 7.6 hängt Phase-D-Eintraege an die kanonische Datei `.planning/04 Entscheidungen/Foundation-Code-Register.md` an. Das in dieser PLAN.md unten gepflegte `<foundation_code_register>`-Markup dient nur als Quelltext fuer die Eintraege.

Purpose: Adressiert REQ-D-8 (7-Tage-Reminder), REQ-D-9 (Inline-Korrektur), REQ-D-10 (Foundation-Code-Register-Eintrag — kanonisch).
Output: Backend-Erweiterung + Template + CSS + Tests + Foundation-Code-Register-Update.

CHECKPOINT: autonomous=false weil Inline-Korrektur visuell + funktional verifiziert werden muss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-CONTEXT.md
@.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-RESEARCH.md
@.planning/phases/08.23.2-anonymisierungs-strecke-vor-mitschrift-schreibungen-inserted-2026-05-12/08.23.2.D-UI-SPEC.md
@routes/performance.py
@templates/dashboard.html
@static/nerve.css
@database/models.py
@.planning/04 Entscheidungen/Foundation-Code-Register.md

<interfaces>
api_dashboard (routes/performance.py Z.218-470):
  - Aktuell: recent_logs = query ConversationLog where user_id=g.user.id order by created_at desc limit 5
  - Response key: 'recent_sessions': [{id, date_str, session_type, score}, ...]
  - NEU: pro Session 'outcome', 'outcome_source', 'call_id' (oder None) mitliefern via outer join Call

Call-Modell (nach Migration 0005):
  Call.conversation_log_id = FK -> ConversationLog.id
  Call.outcome, Call.outcome_source, Call.outcome_confidence

dashboard.html (RESEARCH.md Q8 Z.780-788):
  renderSessions(d) iteriert d.recent_sessions, baut .db2-session-row mit date/type/score
  Stripe-Pattern: <div id="..." class="n-pipeline-warning|n-backup-warning" style="display:none">

POST /api/calls/<call_id>/correct_outcome (Plan 05): Body {outcome, note?} -> {ok, outcome, source}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 7.1: api_dashboard erweitern um calls-JOIN + unsichere_outcomes_count</name>
  <files>routes/performance.py</files>
  <read_first>
    - routes/performance.py Z.218-470 (api_dashboard komplett, recent_sessions-Loop, return-jsonify)
    - database/models.py Z.644-693 (Call-Modell mit conversation_log_id)
    - Migration 0005 (Spalten verfuegbar nach Plan 01)
  </read_first>
  <action>
EDIT `routes/performance.py api_dashboard()`:

(A) Imports (oben in der Datei) erweitern falls noch nicht da:

    from database.models import Call  # bestehend ist nur ConversationLog evtl.

(B) IM `recent_sessions`-Loop (ca. Z.368-391) - VOR dem `.append({...})`-Call - Call-Lookup pro Log:

    # -- Phase 08.23.2.D - calls-Daten pro Session laden (REQ-D-8 + REQ-D-9) --
    _call_row = db.query(Call).filter(Call.conversation_log_id == l.id, Call.user_id == g.user.id).first()
    _outcome = _call_row.outcome if _call_row else None
    _outcome_source = _call_row.outcome_source if _call_row else None
    _call_id = str(_call_row.id) if _call_row else None

(C) `.append({...})`-Dict erweitern (Z.386-391):

    recent_sessions.append({
        'id':            l.id,
        'date_str':      date_str,
        'session_type':  stype,
        'score':         l.kb_end or 0,
        # Phase 08.23.2.D - Outcome-Daten fuer Dashboard-Render
        'outcome':         _outcome,
        'outcome_source':  _outcome_source,
        'call_id':         _call_id,
    })

(D) VOR dem `return jsonify(...)` (ca. Z.437) - neue Aggregation:

    # -- Phase 08.23.2.D REQ-D-8 - 7-Tage-Reminder: COUNT unsichere Outcomes --
    from datetime import timedelta as _td_d
    _seven_days_ago = now - _td_d(days=7)
    try:
        _unsichere = (
            db.query(Call)
              .filter(
                  Call.user_id == g.user.id,
                  Call.ended_at >= _seven_days_ago,
                  (Call.outcome_source == 'ai_auto_unsicher') | (Call.outcome.is_(None)),
              )
              .count()
        )
    except Exception as _e_unsi:
        print(f'[Phase08.23.2.D] unsichere_outcomes_count Fehler: {_e_unsi}')
        _unsichere = 0

(E) `return jsonify({...})` (Z.437-470) - ergaenze key:

    'unsichere_outcomes_count': _unsichere,

Wichtig:
- `now` ist bereits im scope (oben in der Funktion definiert - vorher verifizieren).
- Performance: Call-Lookup pro Session ist N+1; bei 5 Sessions akzeptabel. Optimierung deferred.
- Filter `ai_auto_unsicher OR outcome IS NULL` matched die Definition aus REQ-D-8 (SPEC.md).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "import routes.performance; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep `Call\.conversation_log_id == l\.id` in routes/performance.py findet 1 Match
    - grep `'outcome':\s*_outcome` in routes/performance.py findet 1 Match
    - grep `'outcome_source':\s*_outcome_source` in routes/performance.py findet 1 Match
    - grep `unsichere_outcomes_count` in routes/performance.py findet >=2 Matches (Variable + jsonify-Key)
    - grep `'ai_auto_unsicher'` in routes/performance.py findet 1 Match
    - grep `Phase 08\.23\.2\.D` in routes/performance.py findet >=2 Matches
    - Import-Smoke `python -c "import routes.performance"` exit 0
  </acceptance_criteria>
  <done>Dashboard-Endpoint liefert Outcome-Daten + Reminder-Count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7.2: dashboard.html - Reminder-Card + Calls-Liste Unsicher-Marker + Inline-Korrektur</name>
  <files>templates/dashboard.html</files>
  <read_first>
    - templates/dashboard.html Z.1-50 (Stripe-Pattern n-backup-warning / n-pipeline-warning)
    - templates/dashboard.html Z.768-811 (renderSessions JS-Funktion)
    - .planning/phases/08.23.2-anonymisierungs-strecke-vor-mitschrift-schreibungen-inserted-2026-05-12/08.23.2.D-UI-SPEC.md Component Inventory 4+5 (Reminder-Card + Unsicher-Marker + Inline-Correction)
  </read_first>
  <action>
EDIT `templates/dashboard.html`:

(A) Reminder-Stripe-Markup oben in der Datei (zusammen mit `n-backup-warning` und `n-pipeline-warning`, ca. Z.5-16) ergaenzen:

    <!-- Phase 08.23.2.D REQ-D-8 - Outcome-Reminder-Card (Teal-Brand, kein Gelb) -->
    <div id="outcome-reminder-stripe" class="n-outcome-reminder-stripe" style="display:none">
      <span class="n-outcome-reminder-icon">!</span>
      <div class="n-outcome-reminder-body">
        <div id="outcome-reminder-heading" class="n-outcome-reminder-heading"></div>
        <div class="n-outcome-reminder-sub">Letzte 7 Tage — klicke auf einen Anruf um den Outcome zu korrigieren.</div>
      </div>
    </div>

(B) Im `renderSessions(d)` JS-Block (Z.768-811) am Anfang der Funktion folgenden Block ergaenzen (Reminder-Update):

    // Phase 08.23.2.D REQ-D-8 - Reminder-Stripe Update
    var unsicherCount = +(d && d.unsichere_outcomes_count) || 0;
    var stripe = document.getElementById('outcome-reminder-stripe');
    if (stripe) {
      if (unsicherCount > 0) {
        stripe.style.display = 'flex';
        var headEl = document.getElementById('outcome-reminder-heading');
        if (headEl) {
          headEl.textContent = unsicherCount + (unsicherCount === 1 ? ' Anruf-Outcome wartet auf Bestätigung' : ' Anruf-Outcomes warten auf Bestätigung');
        }
      } else {
        stripe.style.display = 'none';
      }
    }

(C) Die HTML-Generierungs-Schleife in renderSessions erweitern - bei der Row-Erstellung VOR dem schliessenden `</div>`:

    var outcomeLabel = s.outcome ? _outcomeLabelDe(s.outcome) : '';
    var unsicherDot = (s.outcome_source === 'ai_auto_unsicher')
      ? '<span class="outcome-unsicher-dot" data-call-id="' + esc(s.call_id || '') + '" title="Outcome unsicher — klicken zum Korrigieren"></span>'
      : '';
    html += '<span class="db2-session-outcome">' + esc(outcomeLabel) + unsicherDot + '</span>';

Und einen Helper am Anfang des `<script>`-Blocks (falls nicht schon aus pip-launcher.js global verfuegbar):

    function _outcomeLabelDe(v) {
      var m = {
        meeting_booked: 'Termin gebucht',
        callback: 'Rückruf',
        no_interest: 'Kein Interesse',
        wrong_person: 'Falsche Person',
        contract_signed: 'Abschluss',
        unknown: 'Unklar',
      };
      return m[v] || (v || '');
    }

(D) NACH der `renderSessions`-Schleife (oder am Ende des Scripts) Click-Handler-Wireup:

    // Phase 08.23.2.D REQ-D-9 - Inline-Korrektur Click-Handler (event delegation)
    document.addEventListener('click', function(ev) {
      var dot = ev.target.closest && ev.target.closest('.outcome-unsicher-dot');
      if (!dot) return;
      ev.stopPropagation();
      ev.preventDefault();
      var callId = dot.getAttribute('data-call-id');
      if (!callId) return;
      var row = dot.closest('.db2-session-row');
      if (!row) return;
      // Toggle Inline-Korrektur
      var existing = row.nextSibling;
      if (existing && existing.classList && existing.classList.contains('outcome-correction-inline-row')) {
        existing.parentNode.removeChild(existing);
        return;
      }
      var ext = document.createElement('div');
      ext.className = 'outcome-correction-inline-row';
      ext.setAttribute('data-call-id', callId);
      var opts = [
        ['meeting_booked', 'Termin gebucht'],
        ['callback', 'Rückruf'],
        ['no_interest', 'Kein Interesse'],
        ['wrong_person', 'Falsche Person'],
        ['contract_signed', 'Abschluss'],
      ];
      opts.forEach(function(pair){
        var btn = document.createElement('button');
        btn.type = 'button';
        btn.className = 'n-btn-ghost outcome-correction-btn';
        btn.textContent = pair[1];
        btn.addEventListener('click', function(e){
          e.stopPropagation();
          fetch('/api/calls/' + encodeURIComponent(callId) + '/correct_outcome', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ outcome: pair[0] }),
          })
          .then(function(r){ return r.ok ? r.json() : null; })
          .then(function(json){
            if (json && json.ok) {
              // Marker entfernen, Outcome-Label updaten
              var outcomeSpan = row.querySelector('.db2-session-outcome');
              if (outcomeSpan) outcomeSpan.textContent = pair[1];
              ext.parentNode.removeChild(ext);
            }
          })
          .catch(function(e){ console.error('[PhaseD] inline-correct Fehler:', e); });
        });
        ext.appendChild(btn);
      });
      row.parentNode.insertBefore(ext, row.nextSibling);
    });

Wichtig:
- `esc()` ist vermutlich bestehender Helper - andernfalls inline: `String(v).replace(/</g,'&lt;')`.
- Umlaute in user-facing Strings echte Umlaute (Bestätigung, Rückruf) - Source ist UTF-8 per CLAUDE.md.
- Event-Delegation am document - kein Re-Bind bei jedem renderSessions noetig.
- KEIN outcome_note hier (D-08 Option a + RESEARCH "outcome_note in Dashboard out of scope" - Deferred).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && node -e "var fs=require('fs'); var s=fs.readFileSync('templates/dashboard.html','utf8'); ['outcome-reminder-stripe','outcome-unsicher-dot','outcome-correction-inline-row','correct_outcome','unsichere_outcomes_count','Termin gebucht','Rückruf','Bestätigung'].forEach(function(t){if(s.indexOf(t)<0){console.error('MISSING:',t);process.exit(1);}}); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - grep `outcome-reminder-stripe` in templates/dashboard.html findet >=2 Matches
    - grep `outcome-unsicher-dot` in templates/dashboard.html findet >=2 Matches
    - grep `outcome-correction-inline-row` in templates/dashboard.html findet >=2 Matches
    - grep `unsichere_outcomes_count` in templates/dashboard.html findet >=1 Match
    - grep `/api/calls/.*correct_outcome` in templates/dashboard.html findet >=1 Match
    - grep `Termin gebucht` in templates/dashboard.html findet >=1 Match
    - grep `Bestätigung` in templates/dashboard.html findet >=1 Match
    - grep `Phase 08\.23\.2\.D` in templates/dashboard.html findet >=2 Matches
  </acceptance_criteria>
  <done>Reminder + Marker + Inline-Korrektur im Template integriert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7.3: CSS - Reminder-Stripe (Teal) + Unsicher-Dot (Outline-Teal) + Inline-Correction-Row</name>
  <files>static/nerve.css</files>
  <read_first>
    - static/nerve.css (n-backup-warning / n-pipeline-warning fuer Stripe-Pattern, n-btn-ghost)
    - .planning/phases/08.23.2-anonymisierungs-strecke-vor-mitschrift-schreibungen-inserted-2026-05-12/08.23.2.D-UI-SPEC.md Component 4 + 5
  </read_first>
  <action>
EDIT `static/nerve.css` - am Ende der Datei (in der Phase-08.23.2.D-Sektion aus Plan 06 oder neue Section) ergaenzen:

    /* Phase 08.23.2.D - Dashboard Outcome-Reminder + Inline-Korrektur (REQ-D-8/9) */

    .n-outcome-reminder-stripe {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 16px;
      border-radius: 12px;
      border-left: 4px solid #00D4AA;
      background: rgba(0,212,170,0.04);
      border-top: 1px solid rgba(0,212,170,0.15);
      border-right: 1px solid rgba(0,212,170,0.15);
      border-bottom: 1px solid rgba(0,212,170,0.15);
      margin: 0 0 16px 0;
    }

    .n-outcome-reminder-icon {
      color: #00D4AA;
      font-weight: 700;
      font-size: 20px;
      line-height: 1;
      margin-top: 2px;
    }

    .n-outcome-reminder-heading {
      font-size: 14px;
      font-weight: 600;
      color: var(--page-text-color);
    }

    .n-outcome-reminder-sub {
      font-size: 12px;
      color: var(--page-text-secondary);
      margin-top: 2px;
    }

    /* Unsicher-Marker im Calls-Listen-Row - Teal-Outline (KEIN Gelb) */
    .outcome-unsicher-dot {
      display: inline-block;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      border: 2px solid #00D4AA;
      background: rgba(0,212,170,0.15);
      margin-left: 6px;
      vertical-align: middle;
      cursor: pointer;
    }

    .outcome-unsicher-dot:hover {
      background: rgba(0,212,170,0.30);
    }

    .db2-session-outcome {
      font-size: 12px;
      color: var(--page-text-secondary);
      margin-left: 8px;
    }

    .outcome-correction-inline-row {
      display: flex;
      gap: 8px;
      padding: 8px 16px;
      margin: 0 0 8px 0;
      background: rgba(0,212,170,0.03);
      border-left: 2px solid #00D4AA;
      border-radius: 0 8px 8px 0;
      flex-wrap: wrap;
    }

    .outcome-correction-btn {
      min-height: 36px;
      font-size: 12px;
      font-weight: 600;
      padding: 6px 12px;
    }

KEIN #f59e0b / #fbbf24 / yellow in diesen Klassen (CLAUDE.md HART Punkt 8).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && node -e "var fs=require('fs'); var s=fs.readFileSync('static/nerve.css','utf8'); ['.n-outcome-reminder-stripe','.outcome-unsicher-dot','.outcome-correction-inline-row','.outcome-correction-btn','.db2-session-outcome'].forEach(function(t){if(s.indexOf(t)<0){console.error('MISSING:',t);process.exit(1);}}); var section=s.substring(s.indexOf('n-outcome-reminder-stripe')); if(section.match(/#f59e0b|#fbbf24/)){console.error('BRAND-VIOLATION');process.exit(2);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - grep `\.n-outcome-reminder-stripe` in static/nerve.css findet 1 Match
    - grep `\.outcome-unsicher-dot` in static/nerve.css findet >=1 Match
    - grep `\.outcome-correction-inline-row` in static/nerve.css findet 1 Match
    - grep `border-left:\s*4px solid #00D4AA` in static/nerve.css findet >=1 Match
    - Brand-Check: in der Section grep `#f59e0b|#fbbf24` findet 0 Matches
  </acceptance_criteria>
  <done>CSS Brand-konform Teal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7.4: Tests - JOIN-Logic + 7-Tage-Filter + api_dashboard endpoint integration</name>
  <files>tests/test_dashboard_outcome_reminder.py</files>
  <read_first>
    - routes/performance.py api_dashboard (aktualisiert)
    - database/models.py Call + ConversationLog
    - app.py (Flask test_client Setup, ggf. conftest.py fuer login fixture)
  </read_first>
  <behavior>
    - Test 1: Call mit outcome_source='ai_auto_unsicher' in den letzten 7 Tagen -> in count enthalten
    - Test 2: Call alter als 7 Tage -> NICHT in count
    - Test 3: Call mit outcome_source='ai_auto' -> NICHT in count (sicher)
    - Test 4: Call mit outcome=NULL -> in count
    - Test 5: JOIN Call.conversation_log_id liefert Outcome-Daten
    - Test 6: GET /api/dashboard liefert unsichere_outcomes_count im JSON-Response (Endpoint-Test via Flask test_client, KEIN Source-Presence)
  </behavior>
  <action>
Erstelle `tests/test_dashboard_outcome_reminder.py`.

**CLAUDE.md-Pflicht:** KEINE Source-Presence-Tests (`open(p.__file__).read()` + string-in-src). Stattdessen einen echten Endpoint-Test via Flask test_client der die HTTP-Response inspiziert.

    """Phase 08.23.2.D REQ-D-8 + REQ-D-9 - Dashboard Reminder + JOIN Tests.

    CLAUDE.md-konform: ausschliesslich Runtime-Behavior-Tests.
    KEIN open(__file__).read() / inspect.getsource / string-in-source.
    """
    import pytest
    import json
    import uuid
    from datetime import datetime, timezone, timedelta
    from sqlalchemy import or_

    from database.db import get_session
    from database.models import Call, ConversationLog


    def _now():
        return datetime.now(timezone.utc)


    def _make_call(user_id=1, source=None, outcome=None, ended_offset_days=0, conv_log_id=None):
        db = get_session()
        try:
            cid = uuid.uuid4()
            row = Call(
                id=cid,
                user_id=user_id,
                call_mode='cold_call',
                started_at=_now() - timedelta(days=ended_offset_days),
                ended_at=_now() - timedelta(days=ended_offset_days),
                transcript_storage='none',
                outcome=outcome,
                outcome_source=source,
                conversation_log_id=conv_log_id,
            )
            db.add(row)
            db.commit()
            return cid
        finally:
            db.close()


    def _cleanup(call_ids):
        db = get_session()
        try:
            for cid in call_ids:
                db.query(Call).filter(Call.id == cid).delete()
            db.commit()
        finally:
            db.close()


    def test_unsichere_count_filter_logic():
        """Filter: ai_auto_unsicher OR outcome IS NULL, ended_at >= now - 7d."""
        user_id = 1
        ids = []
        try:
            ids.append(_make_call(user_id=user_id, source='ai_auto_unsicher', outcome='callback', ended_offset_days=1))
            ids.append(_make_call(user_id=user_id, source=None, outcome=None, ended_offset_days=2))
            ids.append(_make_call(user_id=user_id, source='ai_auto', outcome='meeting_booked', ended_offset_days=1))
            ids.append(_make_call(user_id=user_id, source='ai_auto_unsicher', outcome=None, ended_offset_days=10))

            seven_days_ago = _now() - timedelta(days=7)
            db = get_session()
            try:
                count = (
                    db.query(Call)
                      .filter(
                          Call.user_id == user_id,
                          Call.ended_at >= seven_days_ago,
                          or_(Call.outcome_source == 'ai_auto_unsicher', Call.outcome.is_(None)),
                      )
                      .count()
                )
                assert count >= 2
            finally:
                db.close()
        finally:
            _cleanup(ids)


    def test_join_call_to_conversation_log():
        """Call.conversation_log_id FK funktioniert."""
        db = get_session()
        try:
            conv = ConversationLog(user_id=1, org_id=1, created_at=_now())
            db.add(conv)
            db.commit()
            cid = uuid.uuid4()
            row = Call(
                id=cid,
                user_id=1,
                call_mode='cold_call',
                started_at=_now(),
                ended_at=_now(),
                transcript_storage='none',
                conversation_log_id=conv.id,
                outcome='callback',
                outcome_source='ai_auto_unsicher',
            )
            db.add(row)
            db.commit()
            row2 = db.query(Call).filter(Call.conversation_log_id == conv.id).first()
            assert row2 is not None
            assert row2.outcome == 'callback'
            assert row2.outcome_source == 'ai_auto_unsicher'
            db.query(Call).filter(Call.id == cid).delete()
            db.query(ConversationLog).filter(ConversationLog.id == conv.id).delete()
            db.commit()
        finally:
            db.close()


    def test_api_dashboard_returns_unsichere_outcomes_count():
        """REQ-D-8: GET /api/dashboard JSON-Body enthaelt unsichere_outcomes_count als Zahl.

        Echter Endpoint-Test via Flask test_client. Setzt voraus dass eine login
        fixture ein authentifiziertes Session-Cookie liefert ODER der Endpoint mit
        force-login zugaenglich gemacht wird.
        """
        try:
            from app import app  # Flask-App-Instanz
        except Exception:
            pytest.skip('Flask app nicht importierbar im Test-Env')

        # Setup: einen unsicher-Call anlegen damit der Counter > 0 sein kann
        user_id = 1
        ids = []
        try:
            ids.append(_make_call(user_id=user_id, source='ai_auto_unsicher', outcome='callback', ended_offset_days=1))

            client = app.test_client()
            # Versuche Login via Test-Session (conftest.py-Pattern erwartet)
            with client.session_transaction() as sess:
                sess['user_id'] = user_id

            resp = client.get('/api/dashboard')
            if resp.status_code == 401 or resp.status_code == 302:
                pytest.skip('Login-Fixture fehlt - Endpoint braucht authentifizierte Session')

            assert resp.status_code == 200, f'erwartet 200, bekam {resp.status_code}'
            data = resp.get_json()
            assert data is not None, 'Response muss JSON sein'
            assert 'unsichere_outcomes_count' in data, \
                'JSON-Response muss key unsichere_outcomes_count enthalten'
            assert isinstance(data['unsichere_outcomes_count'], int), \
                'unsichere_outcomes_count muss int sein'
            assert data['unsichere_outcomes_count'] >= 1, \
                f"Counter muss >=1 sein (wir haben einen ai_auto_unsicher-Call angelegt), war: {data['unsichere_outcomes_count']}"

            # recent_sessions-Eintraege muessen die neuen Outcome-Felder haben (JOIN-Beweis)
            if 'recent_sessions' in data and isinstance(data['recent_sessions'], list) and data['recent_sessions']:
                first = data['recent_sessions'][0]
                # Mindestens die Felder muessen existieren (auch wenn None)
                assert 'outcome' in first or 'outcome_source' in first or 'call_id' in first, \
                    'recent_sessions muss outcome/outcome_source/call_id mitliefern'
        finally:
            _cleanup(ids)


    def test_api_dashboard_zero_when_no_unsichere():
        """Counter ist 0 wenn keine unsicheren Outcomes in den letzten 7 Tagen.

        Wird ggf. geskippt wenn andere Test-Daten existieren - der Test prueft
        das Default-Verhalten, dass der key existiert und int ist.
        """
        try:
            from app import app
        except Exception:
            pytest.skip('Flask app nicht importierbar')

        client = app.test_client()
        with client.session_transaction() as sess:
            sess['user_id'] = 1

        resp = client.get('/api/dashboard')
        if resp.status_code != 200:
            pytest.skip(f'Endpoint nicht zugaenglich (Status {resp.status_code}) - Login-Fixture fehlt')

        data = resp.get_json()
        assert 'unsichere_outcomes_count' in data
        assert isinstance(data['unsichere_outcomes_count'], int)
        assert data['unsichere_outcomes_count'] >= 0
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && pytest tests/test_dashboard_outcome_reminder.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_dashboard_outcome_reminder.py` existiert
    - grep `def test_` in tests/test_dashboard_outcome_reminder.py findet >=4 Matches
    - `pytest tests/test_dashboard_outcome_reminder.py -x` exit 0 (Skip bei fehlenden User-Fixtures OK)
    - grep `app\.test_client` in tests/test_dashboard_outcome_reminder.py findet >=1 Match (Endpoint-Test)
    - grep `unsichere_outcomes_count` in tests/test_dashboard_outcome_reminder.py findet >=3 Matches
    - grep `open\(.*__file__` in tests/test_dashboard_outcome_reminder.py findet 0 Matches (Source-Presence-Verbot)
    - grep `inspect\.getsource` in tests/test_dashboard_outcome_reminder.py findet 0 Matches
  </acceptance_criteria>
  <done>Filter-Logic + JOIN + Endpoint-Response getestet via Runtime-Behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7.5: Visuelle + Funktionale Verifikation Dashboard-UX</name>
  <what-built>
    - api_dashboard liefert Outcome-Daten + unsichere_outcomes_count
    - Reminder-Card (Teal-Left-Border, KEIN Gelb) erscheint wenn unsichere Outcomes existieren
    - Unsicher-Marker (8px Teal-Outline-Kreis) in Calls-Liste neben Outcome-Label
    - Inline-Korrektur expandiert mit 5 Buttons, Klick postet, Marker verschwindet
  </what-built>
  <how-to-verify>
    1. DB-Setup: 1 Call mit outcome_source='ai_auto_unsicher' und ended_at in den letzten 24h erzeugen (via Test-Script oder manuell)
    2. Dashboard oeffnen `/dashboard`
    3. Reminder-Card sichtbar mit "1 Anruf-Outcome wartet auf Bestätigung" + Teal-Left-Border
    4. In Calls-Liste den Row mit 8px Teal-Outline-Kreis identifizieren
    5. Klick auf den Kreis -> Inline-Row mit 5 Buttons (Termin gebucht, Rückruf, Kein Interesse, Falsche Person, Abschluss) erscheint
    6. Klick auf "Termin gebucht" -> Inline-Row collapsed, Marker weg, Outcome-Label in Row aktualisiert auf "Termin gebucht"
    7. DevTools Network: POST /api/calls/<id>/correct_outcome -> 200 mit ok=true
    8. Dashboard reload -> Reminder-Card weg (count=0), kein Marker mehr
    9. Brand-Konsistenz: Alle Outcome-bezogenen Marker/Stripes sind Teal (#00D4AA), KEINE Gelb-Toene
  </how-to-verify>
  <resume-signal>Type "approved" wenn alle 9 Schritte gruen, sonst beschreibe Bugs</resume-signal>
</task>

<task type="auto">
  <name>Task 7.6: Foundation-Code-Register kanonisch updaten (REQ-D-10)</name>
  <files>.planning/04 Entscheidungen/Foundation-Code-Register.md</files>
  <read_first>
    - .planning/04 Entscheidungen/Foundation-Code-Register.md (bestehende Eintragsstruktur + Format)
    - PLAN.md `<foundation_code_register>`-Block unten in dieser Datei (Quelltext-Eintraege)
  </read_first>
  <action>
APPEND an `.planning/04 Entscheidungen/Foundation-Code-Register.md` die 4 Phase-D-Eintraege.

WICHTIG: Behalte die bestehenden Eintraege unveraendert. Fuege NUR neue Sections am Ende der Datei hinzu (oder vor einem evtl. existierenden "Historie/Changelog"-Block). Format konsistent mit bestehenden Eintraegen (siehe Eintrag 1 als Template — Tabelle mit Feld/Wert + Rationale-Section).

Anzuhaengende Eintraege:

    ---

    ## Eintrag: `calls.outcome_confidence`

    | Feld | Wert |
    |------|------|
    | DB-Spalte | `calls.outcome_confidence` (Float, nullable) |
    | Angelegt von | Phase 08.23.2.D (REQ-D-1, Migration 0005) |
    | Schreib-Pfade | `routes/learning.py::api_postcall_analysis` (Haiku-Classifier-Result); `routes/app_routes.py::api_calls_correct_outcome` laesst Feld unveraendert (Audit-Trail) |
    | Lese-Pfade (downstream) | **Phase E (DPO-Paar-Sammler):** Gate-Logic `WHERE outcome_confidence >= 0.90 AND audio_health_score >= 0.80` fuer Trainings-Korpus-Aufnahme; **Phase H (Vorschlags-System):** Confidence-Trend-Analyse fuer adaptive UX |
    | Kalibrierungs-Trigger | Post-Launch nach 100 EA-Calls: `SELECT outcome, outcome_source, COUNT(*) FROM calls GROUP BY outcome, outcome_source ORDER BY COUNT(*) DESC;` — falls >40% in ai_auto_unsicher landen, Schwelle nach unten korrigieren |

    **Rationale (Phase 08.23.2.D):** Die 70%/90%-Confidence-Schwellen (REQ-D-4) sind initial-Default und brauchen Real-Daten-Kalibrierung. Eintrag dokumentiert downstream-Konsumenten + Kalibrierungs-Plan.

    ---

    ## Eintrag: `calls.outcome_source`

    | Feld | Wert |
    |------|------|
    | DB-Spalte | `calls.outcome_source` (Text, nullable, CHECK in `('ai_auto','ai_auto_unsicher','user_corrected')` OR NULL) |
    | Angelegt von | Phase 08.23.2.D (REQ-D-1) |
    | Schreib-Pfade | `routes/learning.py::api_postcall_analysis` (Schwellenlogik aus REQ-D-4); `routes/app_routes.py::api_calls_correct_outcome` setzt `'user_corrected'` |
    | Lese-Pfade (downstream) | **Phase E (DPO-Gate):** Filter `WHERE outcome_source IN ('ai_auto','user_corrected')` — `ai_auto_unsicher` wird vom Trainings-Korpus AUSGESCHLOSSEN; **Phase H (Korrektur-Statistik):** Counter `user_corrected` als Klassifikator-Qualitaets-KPI; **routes/performance.py::api_dashboard:** Filter fuer 7-Tage-Reminder (REQ-D-8) |
    | Kalibrierungs-Hinweis | Wenn `user_corrected`-Anteil > 20% → Prompt-Engineering-Review fuer Classifier |

    **Rationale (Phase 08.23.2.D):** Dreistufiges Provenance-Modell (REQ-D-4) ist Foundation fuer Phase E DPO-Gate-Auswahl. Ohne diese Spalte koennte Phase E nicht zwischen high-quality + low-quality Trainings-Daten diskriminieren.

    ---

    ## Eintrag: `calls.outcome_note`

    | Feld | Wert |
    |------|------|
    | DB-Spalte | `calls.outcome_note` (Text, nullable) |
    | Angelegt von | Phase 08.23.2.D (REQ-D-1, REQ-D-5) |
    | Constraint | **IMMER** anonymisiert via `services/anonymization.py::anonymize()` vor DB-Insert (REQ-D-5 Defense-in-Depth) |
    | Schreib-Pfade | `routes/app_routes.py::api_calls_correct_outcome` ruft `anonymize(text, cache=None)` und persistiert das Ergebnis (oder NULL bei `[ART9_REDACTED]`/`[ANON_FEHLER]`) |
    | Lese-Pfade (downstream) | Aktuell keine (Dashboard-Display deferred per D-08 Option a); **Phase O Analytics:** potentielle Aggregat-Auswertung (anonymisiert) |

    **Rationale (Phase 08.23.2.D):** D-08 Option a entschied gegen Note-Display im Dashboard (Aufwand vs. Nutzen). Spalte existiert + wird gefuellt fuer spaetere Read-Pfade ohne Schema-Migration in Phase O.

    ---

    ## Eintrag: `calls.conversation_log_id`

    | Feld | Wert |
    |------|------|
    | DB-Spalte | `calls.conversation_log_id` (Integer FK → `conversation_logs.id`, nullable) |
    | Angelegt von | Phase 08.23.2.D (REQ-D-1, Migration 0005) |
    | Schreib-Pfade | `routes/app_routes.py::api_beenden` (UPDATE nach ConvLog-Save, Plan 04 REQ-D-2) |
    | Lese-Pfade (downstream) | **routes/performance.py::api_dashboard:** JOIN `Call.conversation_log_id = ConversationLog.id` fuer Outcome-Daten pro Session (REQ-D-8/9); **Phase E:** FK-Traversal Call → ConvLog fuer Trainings-Korpus-Reichhaltigkeit (Transcript-Snippets + Outcome) |
    | Nullable-Rationale | Early-Call-Records aus `create_call_for_sid()` haben noch keinen ConvLog — FK wird erst in `api_beenden` gesetzt |

    **Rationale (Phase 08.23.2.D):** FK ist die einzige Bruecke zwischen `calls` (Outcome-Welt) und `conversation_logs` (Transcript-Welt). Phase E benoetigt diese Verbindung fuer DPO-Paar-Erstellung — daher kanonisch dokumentiert.

WICHTIG:
- KEINE bestehenden Eintraege loeschen oder veraendern.
- Wenn die Datei einen Trailing-Block (z.B. Changelog) hat, neue Eintraege DAVOR einfuegen.
- Format-Konsistenz: gleiche Tabellen-Spalten-Reihenfolge wie bestehende Eintraege.
- Die Eintraege oben im `<foundation_code_register>`-Block dieser PLAN.md bleiben als Quelltext erhalten — Task 7.6 sorgt fuer die kanonische Persistierung im Entscheidungen-Ordner.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && node -e "var fs=require('fs'); var s=fs.readFileSync('.planning/04 Entscheidungen/Foundation-Code-Register.md','utf8'); ['calls.outcome_confidence','calls.outcome_source','calls.outcome_note','calls.conversation_log_id','Phase 08.23.2.D','Phase E','Phase H'].forEach(function(t){if(s.indexOf(t)<0){console.error('MISSING:',t);process.exit(1);}}); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/04 Entscheidungen/Foundation-Code-Register.md` existiert und ist groesser als vorher
    - grep `calls\.outcome_confidence` in Foundation-Code-Register.md findet >=1 Match
    - grep `calls\.outcome_source` in Foundation-Code-Register.md findet >=1 Match
    - grep `calls\.outcome_note` in Foundation-Code-Register.md findet >=1 Match
    - grep `calls\.conversation_log_id` in Foundation-Code-Register.md findet >=1 Match
    - grep `Phase 08\.23\.2\.D` in Foundation-Code-Register.md findet >=4 Matches (4 neue Eintraege)
    - grep `Phase E` in Foundation-Code-Register.md findet >=2 Matches (DPO-Gate Konsumenten)
    - Bestehende Eintraege (z.B. `populate_context_notes`) bleiben unveraendert (grep findet weiterhin den existierenden Eintrag)
  </acceptance_criteria>
  <done>REQ-D-10 erfuellt — kanonische Foundation-Code-Register-Eintraege fuer Phase D persistiert.</done>
</task>

</tasks>

<verification>
- /api/dashboard liefert pro Session outcome + outcome_source + call_id
- /api/dashboard liefert unsichere_outcomes_count (durch Endpoint-Test verifiziert)
- Reminder-Card Brand-konform (Teal)
- Inline-Korrektur funktional + persistiert in DB
- Marker erscheint nur bei ai_auto_unsicher
- Foundation-Code-Register kanonisch aktualisiert (REQ-D-10)
- Source-Presence-Test-Verbot eingehalten
</verification>

<success_criteria>
- REQ-D-8 erfuellt: 7-Tage-Reminder sichtbar wenn Calls mit ai_auto_unsicher/NULL existieren
- REQ-D-9 erfuellt: Unsicher-Marker + Inline-Korrektur funktional
- REQ-D-10 erfuellt: Foundation-Code-Register kanonisch (`.planning/04 Entscheidungen/Foundation-Code-Register.md`) dokumentiert die 4 neuen calls-Spalten + downstream-Konsumenten
</success_criteria>

<foundation_code_register>
# Foundation-Code-Register (CLAUDE.md Punkt 20, REQ-D-10)

**HINWEIS:** Die kanonische Persistierung erfolgt in `.planning/04 Entscheidungen/Foundation-Code-Register.md` via Task 7.6. Der folgende Block ist die Quelle, aus der Task 7.6 die Eintraege uebernimmt.

## calls.outcome_confidence
- **Phase:** 08.23.2.D (this phase)
- **Schreib-Pfade:**
  - `routes/learning.py::api_postcall_analysis` setzt confidence nach Haiku-Classifier
  - `routes/app_routes.py::api_calls_correct_outcome` laesst confidence unveraendert (Audit-Trail)
- **Lese-Pfade (downstream):**
  - **Phase E (DPO-Paar-Sammler):** Gate-Logic `WHERE outcome_confidence >= 0.90 AND audio_health_score >= 0.80` fuer Trainings-Korpus-Aufnahme
  - **Phase H (Vorschlags-System):** Confidence-Trend-Analyse fuer adaptive UX
- **Kalibrierungs-Trigger (Post-Launch, nach 100 EA-Calls):**
  ```sql
  SELECT outcome, outcome_source, COUNT(*)
  FROM calls
  GROUP BY outcome, outcome_source
  ORDER BY COUNT(*) DESC;
  ```
  Verteilung pruefen ob 70%/90%-Grenzen real-data-fit sind. Falls >40% in ai_auto_unsicher landen, Schwelle nach unten korrigieren.

## calls.outcome_source
- **Phase:** 08.23.2.D
- **Werte:** `'ai_auto'` | `'ai_auto_unsicher'` | `'user_corrected'` | NULL
- **Schreib-Pfade:**
  - `routes/learning.py::api_postcall_analysis` (Schwellenlogik)
  - `routes/app_routes.py::api_calls_correct_outcome` setzt 'user_corrected'
- **Lese-Pfade (downstream):**
  - **Phase E (DPO-Gate):** Filter `WHERE outcome_source IN ('ai_auto', 'user_corrected')` - ai_auto_unsicher wird vom Trainings-Korpus ausgeschlossen
  - **Phase H (Korrektur-Statistik):** Counter `user_corrected` als Klassifikator-Qualitaets-KPI
  - **dashboard/api_dashboard:** Filter fuer 7-Tage-Reminder (REQ-D-8)
- **Kalibrierungs-Hinweis:** Wenn `user_corrected` Anteil > 20% -> Prompt-Engineering-Review fuer Classifier.

## calls.outcome_note
- **Phase:** 08.23.2.D
- **Constraint:** IMMER anonymisiert via `services/anonymization.py::anonymize()` (REQ-D-5)
- **Schreib-Pfad:** `routes/app_routes.py::api_calls_correct_outcome` (anonymize-Aufruf mit cache=None)
- **Lese-Pfade:** Aktuell keine (Dashboard-Display deferred); Phase O Analytics potentiell.

## calls.conversation_log_id
- **Phase:** 08.23.2.D
- **Schreib-Pfad:** `routes/app_routes.py::api_beenden` (UPDATE nach ConvLog-Save)
- **Lese-Pfade:**
  - **dashboard/api_dashboard:** JOIN Call.conversation_log_id = ConversationLog.id
  - **Phase E:** FK-Traversal Call -> ConvLog fuer Trainings-Korpus-Reichhaltigkeit
</foundation_code_register>

<output>
After completion, create `.planning/phases/08.23.2.D-outcome-erfassung-audio-qualitaets-score/08.23.2.D-07-SUMMARY.md`
</output>
