# Phase 08.23.2.AUTH-LOGS-TENANT — Research / Verbindungs-Karte

**Erstellt:** 2026-07-08 · Alle Fakten am **live Code** verifiziert (git-synchron) + grep/Read-Belege.

## 1. Der Leak (bewiesen)

| Loch | Anker | Ist-Verhalten | Belegzeile |
|------|-------|---------------|-----------|
| Liste | `routes/logs_routes.py:22-25` | `if not is_admin: if _U{id}_ not in fname: continue` → **admins: kein continue = alle Firmen** | gelesen |
| Download | `routes/logs_routes.py:40-42` | `if not is_admin: if _U{id}_ not in filename: abort(403)` → **admins: kein abort = jede Datei** | gelesen |
| Widget | `routes/dashboard.py:157-172` | `get_recent_logs(user_id, org_id, rolle)` — `org_id` **nie benutzt**; admins ungefiltert | gelesen (Caller :526 `g.org.id`) |

`is_admin = rolle in ('owner','admin')` (logs_routes.py:15/37, dashboard.py:158). Kein `is_superadmin`-Sonderfall heute.

## 2. Die Naht (warum Dateiname-Match nicht reicht)

- Dateiname: `nerve_log_U{g.user.id}_{ts}.txt` (`app_routes.py:334`) — **nur User-ID, keine Org**.
- Enumeration: `os.listdir(LOG_DIR)` (logs_routes.py:19, dashboard.py:162). `LOG_DIR` = `services/live_session.py:37`.
- → Firmen-Filter braucht **DB-Lookup**: `db.query(User).filter_by(org_id=g.org.id)` → Set der org-User-IDs
  (Muster `routes/organisations.py:28`). `User.org_id` = FK NOT NULL (`models.py:65-69`). g.org/g.user aus `auth.py:56`.
- UID aus Dateiname: Regex `_U(\d+)_` (robust gegen `_U12_` vs `_U123_`-Substring-Fehlmatch).

## 3. Founder-Pfad-Anker (getrennt, superadmin-only)

| Baustein | Anker | Rolle |
|----------|-------|-------|
| Sicherheits-Muster | `routes/admin_views.py:224-235` `register_admin_screenshot_route` | superadmin_required + Traversal-Guard + send_from_directory — KOPIEREN (mit LOG_DIR) |
| Decorator | `services/auth_decorators.py:5` `superadmin_required` | 403 für nicht-superadmin |
| Audit | `services/audit.py:5-27` `log_action(db,user_id,org_id,action,target_type,target_id,details,request)` | fail-open swallow `:24-26` → für Founder-Pfad umkehren (strict/re-raise) |
| NICHT wiederverwenden | `routes/learning.py:608` `api_transcript` | hart owner-scoped (`user_id==g.user.id`, 404 bei fremd) → für cross-org untauglich |

log_action-Nutzung real: `auth.py:163/315`, `app_routes.py:601/604`, `admin_views.py:75` — Muster bestätigt.

## 4. Persistenz-/Sicherheits-Schicht (Punkt 21)

| Objekt | Schicht | Lese-Pfad | Schutz nach Fix |
|--------|---------|-----------|-----------------|
| Call-Log-Dateien | Datei (`LOG_DIR`, tar-excluded, DEPLOY-PRUNE-tabu) | listdir + send_file | Normal-UI: org-User-ID-Filter (fail-closed); Founder: superadmin + Audit-vor-Download |
| org-User-IDs | DB `users.org_id` | `filter_by(org_id=g.org.id)` | Quelle der Filter-Menge; DB-Fehler → leer/403 (nie „alle") |
| audit_log | DB-Tabelle (append-only, Migration 0026) | `log_action` | Founder-Zugriff protokolliert (Metadaten-only), fail-closed |

**Punkt 26 (Schreib-Zeitpunkt):** Log-Datei ist beim Lesen längst geschrieben (Post-Call). Kein Race. Der Filter
liest die org-User-Liste synchron pro Request — kein Voraus-Lauf.

## 5. Bei Diskrepanz: STOP + Replan
Wenn der Executor findet, dass (a) `is_admin` bereits org-gefiltert ist, ODER (b) der Dateiname doch eine Org trägt,
ODER (c) `superadmin_required`/`log_action` andere Signaturen haben → Diskrepanz → STOP, im SUMMARY dokumentieren.
