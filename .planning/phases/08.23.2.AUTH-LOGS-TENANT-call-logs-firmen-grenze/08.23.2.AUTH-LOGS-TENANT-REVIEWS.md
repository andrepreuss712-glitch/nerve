# Phase 08.23.2.AUTH-LOGS-TENANT — Cross-AI Review (Fable) + Einarbeitung

**Reviewer:** Fable (3. Sicht), am echten Code read-only. **Datum:** 2026-07-08.
**Verdikt:** baubereit mit **3 Auflagen** + 3 Kleinigkeiten. Eingearbeitet via `/gsd-plan-phase --reviews`.

## Auflagen (3) — eingearbeitet

| # | Finding | Fix im Plan | Beleg |
|---|---------|-------------|-------|
| A-1 | **ConvLogAdmin** (`admin_views.py:93-99`): unauditierte firmenübergreifende Metadaten-Sicht (Flask-Admin SecureModelView, superadmin-only, kein Transkript). | André-Entscheidung: NICHT in diese Phase aufreißen → DOKU-only als bewusste Rest-Fläche im SUMMARY + Verweis, dass die auditierte Founder-Metadaten-Sicht in **AUTH-4** nachgezogen wird. | CONTEXT Rest-Flächen, Plan `<output>` |
| A-2 | **T-LOGS-06 (fail-closed) hatte keinen Wächter** — nur versprochen. | 5. Test in `test_logs_org_boundary`: `monkeypatch _org_user_ids`→Exception → Assertion Liste leer / Download 403. Erst-rot sinngemäß gegen eine fail-open-Variante. | Task 1 (>=5 Tests, grep `_org_user_ids`) |
| A-3 | **Template-Naht:** `logs_page.html:74` verlinkt hart `/logs/download/` (Normal-Pfad). | Founder-Liste verlinkt `/admin/logs/download/` via parametrisiertem `download_url` (Default = Normal-Pfad). **VERBOTEN:** Normal-Download per is_superadmin-Bypass „reparieren" (= T-LOGS-02). `templates/logs_page.html` in files_modified. | Task 4 (Schritt 6, grep `download_url`) |

## Kleinigkeiten (3) — eingearbeitet
- (a) Founder-Route: bei fehlgeschlagenem strict-Audit `db.rollback()` + `db.close()` VOR `abort` → Task 4 behavior/action (grep `rollback`).
- (b) Boundary-Test-Assertion robust als „B-Datei NICHT sichtbar" (Substring), nicht exakte Listen-Gleichheit → Task 1.
- (c) Founder-Liste behält `endswith('.txt')`-Filter (persönliche `_summary_*.json` nicht mitlisten) → Task 4.

## Nächster Schritt
Plan neu verifiziert → André Pre-Execute-Audit → `/gsd-execute-phase 08.23.2.AUTH-LOGS-TENANT` (supervised). NICHT direkt bauen.
