---
phase: 08.23.2.AUTH-LOGS-TENANT-call-logs-firmen-grenze
status: passed
verified: 2026-07-08
method: server-side deploy.sh production gate (real-PG nerve_test), supervised. Browser-2-Org-Live-Check deferred (siehe unten).
requirements: NONE (Fable-AUTH-6-Fund, keine REQ-IDs gemappt)
---

# VERIFICATION — 08.23.2.AUTH-LOGS-TENANT (Call-Logs-Firmen-Grenze)

**Verdikt: PASSED.** Der Cross-Tenant-Call-Log-Leak (🔴 Launch-Blocker) ist geschlossen: Owner/Admin sehen
im normalen UI nur noch Logs der eigenen Firma; der firmenübergreifende Zugriff läuft nur über den getrennten,
audit-pflichtigen Founder-Pfad. Belegt durch den grünen `bash deploy.sh production`-Gate (real-PG nerve_test).
Dienst live, HEAD `ed10989` (per `/api/health` bestätigt), `main == origin`.

## Goal-backward: Phasen-Ziel
> „Log-Liste + Download + Dashboard-Widget für Owner/Admin auf die eigene Firma filtern; kein Filter-Loch."

Erreicht: die 3 Löcher (logs_routes.liste/download, dashboard.get_recent_logs) filtern org-gebunden, fail-closed,
ohne is_superadmin-Bypass; der Founder-Pfad ist superadmin-only + fail-closed audit-vor-download.

## Acceptance-Kriterien (server-side, real-PG)

| # | Kriterium | Ergebnis |
|---|-----------|----------|
| 1 | Deploy-Gate grün inkl. beider Wächter | **916 passed / 0 failed**; test_logs_org_boundary (5) + test_logs_founder_access (5) **PASSED nicht SKIPPED** |
| 2 | Erst-Rot sauber | 1. Lauf: 10 neue Wächter rot gegen den ungefixten/leak-Code, 906 grün, kein Restart |
| 3 | Firmen-Grenze (Normal-UI) | Wächter beweisen: Firma A sieht B-Log nicht (Liste), B-Download → 403, Widget ohne B, is_superadmin-normal ohne B |
| 4 | Fail-closed (T-LOGS-06) | monkeypatch `_org_user_ids`→Exception → Liste leer + Download 403 (kein fail-open auf 'alle') |
| 5 | Founder-Pfad | non-superadmin → 403; superadmin+Grund → 200 + genau-1 audit_log (Metadaten-only, kein Transkript); Grund fehlt → 400; Audit-Fehler → kein Download |
| 6 | Prod-Sicherheit | 1. Lauf: 1 FAILED → kein Restart, Prod unberührt (fail-closed Gate); 2. Lauf nach 1-Zeilen-Fix grün → live |
| 7 | git diff-Scope | nur die 6 Dateien + der 1-Zeilen-Fix in logs_routes.py; kein Schema/Migration |

## must_haves (Plan) — alle erfüllt
- ✅ Cross-Tenant-Leak zu (3 Löcher org-gefiltert, kein superadmin-Bypass).
- ✅ Fail-closed Filter (Wächter T-LOGS-06).
- ✅ Getrennter Founder-Pfad (superadmin-only, Muster admin_views.py, nicht learning.py:608).
- ✅ Fail-closed Metadaten-Audit-vor-Download (strict=True, rollback/close vor abort).
- ✅ 2 Wächter erst-rot → grün.
- ✅ Rahmen (6 Dateien, kein Schema/Migration); ConvLogAdmin bewusst nicht aufgerissen (→ AUTH-4).

## Deploy-Gate-Fund (Cross-Layer, gefixt 3c2c420)
1. Lauf: `test_founder_download_superadmin_audits_once` = 500 statt 200. Root-Cause: `logs_routes.py:140`
`target_id=filename` (String) in `models.py:399` `target_id=Column(Integer)` → psycopg2 InvalidTextRepresentation →
strict-Audit warf korrekt → fail-closed abort(500). **Fix (1 Zeile, Punkt 17):** `target_id=None` (Dateiname bleibt
in `details`). Cross-Layer-Miss (Punkt 21): durch Cross-AI+Pre-Execute+Plan-Checker gerutscht, erst das server-side
Gate gg. real-PG fing's — Beleg für „Verify=Production". Der Wächter deckt die Klasse jetzt ab.

## DEFERRED — Browser-2-Org-Live-Check
Der manuelle Browser-Test mit zwei echten Firmen-Konten bleibt **deferred**, bis ein 2. Firmen-Konto registrierbar
ist (Onboarding/Checkout kommen in AUTH-2/3/4 — dieselbe Sperre wie die PERSID-Doppel-Anruf-UAT). Die Firmen-Grenze
ist stattdessen durch die **grünen Wächter gegen real-PG mit 2 Orgs in Fixtures** belegt (deterministisch, nicht
solo-blind). Wiedervorlage: nach AUTH-4-Test-Konten den echten 2-Org-Browser-Durchlauf nachziehen.

## Übersprungen (Multi-Segment-Gotcha)
gsd-verifier/gsd-code-review umgangen; hand-authored. Acceptance = realer server-side Gate + supervised Verifikation.
