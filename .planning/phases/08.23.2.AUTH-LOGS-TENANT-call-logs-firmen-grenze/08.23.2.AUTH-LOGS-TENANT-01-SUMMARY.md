---
phase: 08.23.2.AUTH-LOGS-TENANT-call-logs-firmen-grenze
plan: 01
status: code-complete-pending-deploy-gate
executed: 2026-07-08
files_modified:
  - services/audit.py
  - routes/logs_routes.py
  - routes/dashboard.py
  - templates/logs_page.html
  - tests/test_logs_org_boundary.py
  - tests/test_logs_founder_access.py
commits:
  - 8e0aa7f   # Task 1 — Wächter test_logs_org_boundary
  - 3c3cab8   # Task 2 — Wächter test_logs_founder_access
  - 669f6b3   # Task 3 — audit.py strict
  - 7032ea5   # Task 4 — Firmen-Grenze + Founder-Pfad + Template
key-files:
  created:
    - tests/test_logs_org_boundary.py
    - tests/test_logs_founder_access.py
---

# SUMMARY — 08.23.2.AUTH-LOGS-TENANT Plan 01 (Call-Logs-Firmen-Grenze)

**Status:** Code-complete, 4 atomische Commits auf `main`, `git diff` zeigt AUSSCHLIESSLICH die 6 geplanten
Dateien. **Deploy-Gate-Acceptance NOCH OFFEN** — supervised (`bash deploy.sh production`), André fährt; danach
Live-2-Org-Check mit Test-Usern. Lokal nur `py_compile` (Syntax) + Plan-Acceptance-Greps — **kein Acceptance**
(HART Kein-Local-Dev; die 2 Wächter laufen server-side im Gate gegen real-PG nerve_test).

## Task 1 — tests/test_logs_org_boundary.py (5 Tests, erst-rot)
Firma A + Firma B (je owner) + je eine `nerve_log_U{id}_…txt` im LOG_DIR (Teardown entfernt sie). Tests:
list_hides_other_org (B nicht im Body, A schon) · download_other_org_403 · widget_excludes_other_org
(get_recent_logs direkt) · superadmin_normal_hides_other_org (is_superadmin auf /logs nur eigene Firma) ·
**fail_closed_when_org_lookup_raises** (monkeypatch `logs_routes._org_user_ids`→Exception → Liste leer + Download 403, T-LOGS-06).
Robuste Assertions „B nicht sichtbar" (Substring), nicht exact-match. cleanup_rows für Org/User/tenant_orgs.

## Task 2 — tests/test_logs_founder_access.py (5 Tests, erst-rot)
founder_list_non_superadmin_403 · founder_download_non_superadmin_403 · **founder_download_superadmin_audits_once**
(200 + Delta audit_log == 1, details = Datei+Grund, KEIN Transkript-Fragment) · missing_grund_400 ·
**fail_closed_when_audit_fails** (monkeypatch `logs_routes.log_action`→Exception → kein 200, keine Datei-Bytes).
audit_log-Rows räumt der Baseline-Guard (TEST-AUFRAEUM Trigger-Bypass) automatisch ab.

## Task 3 — services/audit.py strict-Variante
`log_action(..., strict=False)`: Default unverändert (swallow+print) → alle Bestands-Caller (auth/app_routes/admin_views) safe.
`strict=True`: `except`-Block re-raist → fail-closed Founder-Pfad. grep `strict` == 5, py_compile grün.

## Task 4 — routes/logs_routes.py + dashboard.py + logs_page.html
- **3 Löcher org-gefiltert** (fail-closed, KEIN is_superadmin-Bypass): `_org_user_ids(db,org_id)` (fail-closed leere Menge)
  + `_uid_from_name` (Regex `_U(\d+)_`, kein Substring-Fehlmatch). `liste()` + `download()` (admin nur wenn UID ∈ org-Set,
  sonst 403) + `get_recent_logs` (org_id JETZT genutzt).
- **Founder-Pfad** (superadmin-only): `/admin/logs` (Liste aller, `endswith('.txt')`) + `/admin/logs/download/<filename>`:
  `@login_required @superadmin_required` + Traversal-Guard + Grund-Pflicht (400) + **fail-closed Audit-vor-Download**
  (`strict=True`, bei Exception `db.rollback()`+`db.close()` VOR `abort(500)`) + `send_file`. Metadaten-only.
- **Template:** `logs_page.html:74` nutzt `download_url|default('/logs/download/')`; Founder-Liste rendert mit
  `download_url='/admin/logs/download/'`. KEIN is_superadmin-Normal-Bypass (= T-LOGS-02 vermieden).

## Lokale Checks (kein Acceptance)
`py_compile` audit.py/logs_routes.py/dashboard.py/beide Tests ✓ · alle Plan-Greps grün (filter_by(org_id, superadmin_required,
strict=True, rollback, download_url, /admin/logs/download/, org_ids; 5+5 def test_; _org_user_ids-monkeypatch; founder_log_access).

## BEWUSSTE REST-FLÄCHE (Fable-Auflage 1, dokumentiert)
`ConvLogAdmin` (Flask-Admin SecureModelView, `admin_views.py:93-99`) = unauditierte firmenübergreifende **Metadaten**-Sicht
auf ConversationLog (superadmin-only, KEIN Transkript). BEWUSST NICHT in dieser Phase aufgerissen — die **auditierte**
Founder-Metadaten-Sicht wird in **AUTH-4 (Founder-Maske)** nachgezogen.

## OFFEN (supervised, André) — Deploy-Gate = Acceptance
1. `bash deploy.sh production` — Gate grün inkl. test_logs_org_boundary.py (5) + test_logs_founder_access.py (5), PASSED nicht SKIPPED.
2. **Erst-rot-Beleg:** die Boundary/Founder-Assertions failen gegen den Code vor Task 4 (dokumentieren).
3. **Live-2-Org-Check (Test-User):** Firma A sieht B nicht auf /logs; /logs/download/<B> → 403; is_superadmin-Konto auf /logs nur eigene Firma; /admin/logs als superadmin (mit Grund) lädt cross-org + 1 audit_log; owner → 403.
4. `git diff`: nur die 6 Dateien. Kein Schema/Migration.
