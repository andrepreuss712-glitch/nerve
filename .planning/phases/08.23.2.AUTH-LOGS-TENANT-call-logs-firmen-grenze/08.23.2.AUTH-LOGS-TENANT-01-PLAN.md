---
phase: 08.23.2.AUTH-LOGS-TENANT-call-logs-firmen-grenze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/audit.py
  - routes/logs_routes.py
  - routes/dashboard.py
  - templates/logs_page.html   # Fable-Auflage 3: Download-Link parametrisieren (Founder-Route-Naht)
  - tests/test_logs_org_boundary.py
  - tests/test_logs_founder_access.py
autonomous: false   # 🔴 LAUNCH-BLOCKER DSGVO/Cross-Tenant, supervised, prod-deploy
requirements: NONE   # Herkunft: Fable-AUTH-6-Fund, keine REQ-IDs gemappt
complexity: "🔴 LAUNCH-BLOCKER (DSGVO/Cross-Tenant) — Cross-AI/Fable Pflicht VOR Execute (Punkt 24)"

must_haves:
  truths:
    - "Cross-Tenant-Leak zu: Owner/Admin sehen auf der normalen Logs-Seite (routes/logs_routes.py:liste) UND im Dashboard-Widget (get_recent_logs) NUR Call-Logs der eigenen Firma — gefiltert über die User-IDs von g.org (DB-Lookup). Ein Cross-Org-Download (routes/logs_routes.py:download) liefert 403. Auch ein is_superadmin-Konto sieht auf dem NORMALEN Pfad nur die eigene Firma (keine Bypass-Ausnahme)."
    - "Fail-closed Filter: kann die Org-User-Liste nicht ermittelt werden (DB-Fehler), zeigt der Normal-Pfad eine LEERE Liste bzw. 403 — NIE 'alle Firmen' (kein fail-open Rückfall auf das alte admin-ungefilterte Verhalten)."
    - "Getrennter Founder-Pfad: eine superadmin-only Route (routes/logs_routes.py, @superadmin_required) listet/lädt firmenübergreifend — owner/admin (nicht-superadmin) bekommen dort 403 (Muster admin_views.py:224-235: superadmin_required + Path-Traversal-Guard + send_file/LOG_DIR). learning.py:608 wird NICHT wiederverwendet."
    - "FAIL-CLOSED Audit: jeder erfolgreiche Founder-Download schreibt GENAU EINEN audit_log-Eintrag (services/audit.py, action='founder_log_access', details=Datei+Grund, Metadaten-only, KEIN Transkript) VOR dem send_file. Schlägt der Audit-Write fehl, wird NICHT gesendet (abort) — die stille Fehler-Schluckung audit.py:24-26 ist für diesen Pfad umgekehrt (strict re-raise)."
    - "Zwei Wächter erst-rot dann grün im deploy.sh-Gate: test_logs_org_boundary (Normal-Grenze inkl. is_superadmin + FAIL-CLOSED-Beweis: _org_user_ids wirft → Liste leer/Download 403, Fable-Auflage 2/T-LOGS-06) + test_logs_founder_access (403 für non-superadmin, genau-1-audit_log pro Download, Audit-Fehler → kein Download)."
    - "Template-Naht (Fable-Auflage 3): die Founder-Liste verlinkt Downloads auf die FOUNDER-Route (/admin/logs/download/), NICHT auf den Normal-Pfad /logs/download/ (logs_page.html:74) — via parametrisiertem download_url. VERBOTEN: den Normal-Download per is_superadmin-Bypass 'reparieren' (wäre T-LOGS-02)."
    - "Rahmen: NUR services/audit.py (strict additiv), routes/logs_routes.py, routes/dashboard.py, templates/logs_page.html (Link-Parametrisierung) + 2 neue Test-Dateien. Kein Schema/Migration. Bestehendes non-admin-Verhalten (_U{own_id}_) unverändert. ConvLogAdmin (admin_views.py:93-99) BEWUSST NICHT aufgerissen (Rest-Fläche → AUTH-4)."
  artifacts:
    - path: "routes/logs_routes.py"
      provides: "Org-gefilterte Liste+Download (fail-closed, kein superadmin-Bypass) + getrennte superadmin-only Founder-Route (Audit-vor-Download fail-closed)."
      contains: "superadmin_required"
    - path: "services/audit.py"
      provides: "log_action mit optionalem strict-Modus (re-raise statt swallow) für den fail-closed Founder-Pfad."
      contains: "strict"
  key_links:
    - from: "routes/logs_routes.py:liste/download (Normal)"
      to: "database users.org_id (org-User-ID-Set)"
      via: "db.query(User).filter_by(org_id=g.org.id) + Regex _U(\\d+)_ Mitgliedschaft"
      pattern: "filter_by\\(org_id"
    - from: "routes/logs_routes.py Founder-Route"
      to: "services/auth_decorators.superadmin_required + services/audit.log_action(strict)"
      via: "@superadmin_required + Audit-vor-send_file, fail-closed"
      pattern: "superadmin_required"
    - from: "tests/test_logs_founder_access.py"
      to: "audit_log-Tabelle"
      via: "genau 1 Eintrag pro Founder-Download; Audit-Fehler → kein Download"
      pattern: "founder_log_access"
---

<objective>
Den Cross-Tenant-Call-Log-Leak schließen (🔴 Launch-Blocker, DSGVO): Owner/Admin sehen im normalen UI nur
noch Call-Logs der EIGENEN Firma; zusätzlich ein getrennter, **audit-pflichtiger** Founder-Pfad (kein
Filter-Loch). André-Entscheidung 2026-07-08: **absolute Firmen-Grenze, keine is_superadmin-Ausnahme im
Normal-Filter** (CONTEXT D-01..D-07).

**Naht (live-belegt):** Log-Dateiname trägt nur die User-ID (`app_routes.py:334`), keine Org → der Filter
mappt über einen DB-Lookup der User-IDs von `g.org` (`filter_by(org_id=g.org.id)`, Muster `organisations.py:28`).

**Rahmen:** services/audit.py (strict-Variante) + routes/logs_routes.py + routes/dashboard.py + 2 Wächter.
Kein Schema/Migration. autonomous:false (supervised prod-deploy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.AUTH-LOGS-TENANT-call-logs-firmen-grenze/08.23.2.AUTH-LOGS-TENANT-CONTEXT.md
@.planning/phases/08.23.2.AUTH-LOGS-TENANT-call-logs-firmen-grenze/08.23.2.AUTH-LOGS-TENANT-RESEARCH.md
@routes/logs_routes.py
@routes/dashboard.py
@services/audit.py
@routes/admin_views.py

<interfaces>
<!-- Live-verifiziert 2026-07-08. -->
- `routes/logs_routes.py` — `logs_bp`='logs'; `liste()` (:13, Filter :22-25), `download(filename)` (:35, Filter :40-42). Import: `from services.live_session import LOG_DIR`.
- `routes/dashboard.py:157` — `get_recent_logs(user_id, org_id, rolle, limit=5)` (org_id ungenutzt), Caller `:526` `get_recent_logs(g.user.id, g.org.id, g.user.rolle)`.
- `services/audit.py:5` — `log_action(db, user_id, org_id, action, target_type=None, target_id=None, details=None, request=None)`; swallow `:24-26`.
- `services/auth_decorators.py:5` — `superadmin_required`.
- `routes/admin_views.py:224-235` — `register_admin_screenshot_route` (Muster: superadmin_required + `if '..' in rel or rel.startswith('/'): abort(400)` + send_from_directory).
- `database/models.py:65-69` — `User.org_id` FK NOT NULL. `routes/organisations.py:28` — `db.query(User).filter_by(org_id=g.org.id).all()`. `get_session`/`db`-Muster wie in dashboard.py.
- `routes/app_routes.py:334` — `nerve_log_U{g.user.id}_{ts}.txt`.
</interfaces>
</context>

## 5. Persistenz-Schicht-Verifikation (Punkt 21/22)
Diese Phase LIEST Call-Log-Dateien (LOG_DIR) + LIEST users.org_id + SCHREIBT audit_log (Founder-Pfad).
Verbindungs-Karte live-belegt in RESEARCH.md §1-4. Kundendaten-Dateien werden nur GELESEN/GESENDET (nie verändert).
**Bei Diskrepanz** (is_admin schon gefiltert / Org im Dateinamen / andere Decorator-Signatur) → STOP + Replan (RESEARCH §5).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wächter test_logs_org_boundary.py — erst-rot (D-06 Normal-Grenze)</name>
  <files>tests/test_logs_org_boundary.py</files>
  <read_first>
    - routes/logs_routes.py (vollständig — das zu sichernde Verhalten).
    - routes/dashboard.py:157-175 + :526 (get_recent_logs).
    - tests/conftest.py (App-/Client-/DB-Fixtures, cleanup_rows-Pflicht für committende Tests — CLAUDE.md Test-Cleanup-Regel).
    - tests/test_rolle_source_sweep.py oder test_waitlist_admin_auth.py (Muster: Client + 2 Orgs + User + Rollen-Setup).
  </read_first>
  <behavior>
    - Legt 2 Orgs (A,B) + je einen owner-User an; legt im LOG_DIR (tmp/monkeypatched ODER echtes LOG_DIR mit
      cleanup) je eine Log-Datei `nerve_log_U{idA}_...txt` und `nerve_log_U{idB}_...txt` an.
    - Assertion Liste: owner A sieht auf `/logs` die A-Datei; **die B-Datei ist NICHT sichtbar** (robuste Assertion:
      „B-Dateiname nicht im Response-Body", NICHT exakte Listen-Gleichheit, Fable-Kleinigkeit b).
    - Assertion Download: owner A auf `/logs/download/<B-Datei>` → 403.
    - Assertion Widget: `get_recent_logs(userA, orgA, 'owner')` enthält keine B-Datei.
    - Assertion Superadmin-normal: ein is_superadmin-Konto (Org A) sieht auf `/logs` die B-Datei NICHT (kein Bypass).
    - **Assertion FAIL-CLOSED (Fable-Auflage 2, T-LOGS-06):** `monkeypatch` `_org_user_ids` (bzw. den org-Lookup)
      so, dass er eine Exception wirft → owner A auf `/logs`: Liste enthält KEINE fremde Datei (leer/nur-eigene),
      UND `/logs/download/<B-Datei>` → 403. Beweist fail-closed statt fail-open. Erst-rot-Anspruch: sinngemäß rot
      gegen eine fail-open-Variante (die bei Fehler „alle" zeigt).
    - **Erst-rot:** gegen den aktuellen Code (admins ungefiltert) MÜSSEN die Boundary-Assertions failen.
  </behavior>
  <action>
Schreibe `tests/test_logs_org_boundary.py`. Nutze die conftest-App/Client/DB-Fixtures. Für die LOG_DIR-Dateien:
entweder `monkeypatch`/tmp auf `services.live_session.LOG_DIR` + die in logs_routes/dashboard importierten Referenzen,
ODER echte Dateien in LOG_DIR mit garantiertem `os.remove`-Teardown (best-effort, kein Leak). Login-Simulation über
die vorhandenen Test-Helfer (Session/g.user). Für den FAIL-CLOSED-Test `monkeypatch.setattr` auf den org-User-ID-Helfer
(`routes.logs_routes._org_user_ids`) → `raise RuntimeError`. Assertions robust als „B nicht sichtbar" (Substring im
Body), nicht exakte Gleichheit. Marker: NICHT `live`/`perf`. cleanup_rows für alle committeten Rows (Orgs/Users) im Teardown.
  </action>
  <verify><automated>
    Server-side deploy.sh-Gate. grep lokal: grep -c 'def test_' tests/test_logs_org_boundary.py  # >=5
    grep -c '_org_user_ids' tests/test_logs_org_boundary.py  # >=1 (fail-closed monkeypatch)
    ERST-ROT-Beleg: der Test failt gegen den Code VOR Task 4 (im SUMMARY dokumentieren).
  </automated></verify>
  <acceptance_criteria>
    - >=5 Tests (Liste-B-nicht-sichtbar, Download-B-403, Widget-B-nicht-drin, Superadmin-normal-B-nicht-sichtbar, FAIL-CLOSED).
    - FAIL-CLOSED-Test monkeypatcht `_org_user_ids`→Exception und beweist leer/403 (T-LOGS-06).
    - Assertions robust („B nicht sichtbar", kein exact-match). Nicht `live`/`perf`; cleanup_rows.
    - Erst-rot gegen aktuellen Code belegt; grün nach Task 4.
  </acceptance_criteria>
  <done>Wächter der Normal-Firmen-Grenze steht (erst-rot).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wächter test_logs_founder_access.py — erst-rot (D-06 Founder-Pfad)</name>
  <files>tests/test_logs_founder_access.py</files>
  <read_first>
    - routes/admin_views.py:224-235 (superadmin_required-Muster) + services/auth_decorators.py:5.
    - services/audit.py (log_action + swallow) + database/models.py (AuditLog-Felder, action/details).
    - tests/conftest.py (Fixtures) + ein bestehender superadmin-Test falls vorhanden (grep 'superadmin' tests/).
  </read_first>
  <behavior>
    - (a) owner/admin (nicht-superadmin) → GET Founder-Route → 403.
    - (b) is_superadmin + Grund → Founder-Download einer Cross-Org-Datei → 200 UND genau 1 neuer audit_log-Eintrag
      (action='founder_log_access', details enthält Dateiname + Grund, KEIN Transkript-Inhalt).
    - (c) FAIL-CLOSED: wenn der Audit-Write scheitert (monkeypatch log_action/DB → Exception), liefert die Route
      KEIN File (abort, kein 200, keine Bytes).
    - **Erst-rot:** die Route existiert noch nicht → 404/Fehler (rot) bis Task 3.
  </behavior>
  <action>
Schreibe `tests/test_logs_founder_access.py`. Zähle audit_log-Rows vor/nach (Delta == 1). Für (c) den strict-Audit-
Pfad zum Werfen bringen (monkeypatch auf `services.audit.log_action` ODER DB-commit). Metadaten-only prüfen: der
details-JSON enthält Dateiname+Grund, aber KEIN erwartetes Transkript-Fragment. Marker nicht `live`/`perf`; cleanup_rows.
  </action>
  <verify><automated>
    grep -c 'def test_' tests/test_logs_founder_access.py  # >=3 ; grep -c 'founder_log_access' tests/test_logs_founder_access.py # >=1
    Erst-rot vor Task 3 belegt.
  </automated></verify>
  <acceptance_criteria>
    - >=3 Tests (403-non-superadmin, 200+genau-1-audit, fail-closed-kein-download).
    - action-String `founder_log_access` geprüft; Delta audit_log == 1; Metadaten-only.
    - Erst-rot belegt; grün nach Task 3.
  </acceptance_criteria>
  <done>Wächter des Founder-Pfads steht (erst-rot).</done>
</task>

<task type="auto">
  <name>Task 3: services/audit.py — strict-Variante (fail-closed Audit) für den Founder-Pfad</name>
  <files>services/audit.py</files>
  <read_first>
    - services/audit.py (vollständig — der swallow :24-26).
    - Alle log_action-Caller (auth.py:163/315, app_routes.py:601/604, admin_views.py:75) — dürfen NICHT brechen (Default strict=False).
  </read_first>
  <behavior>
    - `log_action(..., strict=False)`: bei `strict=False` Verhalten UNVERÄNDERT (swallow+print — alle Bestands-Caller safe).
    - Bei `strict=True`: der `except` re-raist (kein stiller Schluck) → der Aufrufer kann fail-closed abbrechen.
  </behavior>
  <action>
Ergänze in `services/audit.py:log_action` einen Parameter `strict=False` (letzter kw-Param). Im `except Exception as e:`
Block: `print(...)` bleibt, danach `if strict: raise`. KEINE Änderung an der Default-Signatur-Semantik für Bestands-Caller.
  </action>
  <verify><automated>
    grep -c 'strict' services/audit.py  # >=2 ; python -m py_compile services/audit.py
    Bestands-Caller unverändert: grep -c 'log_action(' routes/*.py  # unverändert (kein Caller MUSS strict setzen ausser Founder)
  </automated></verify>
  <acceptance_criteria>
    - `strict=False`-Param vorhanden; Default-Verhalten (swallow) für Bestands-Caller unverändert.
    - Bei strict=True re-raise (grep `if strict` + `raise`).
    - py_compile grün.
  </acceptance_criteria>
  <done>audit.py kann fail-closed (strict) — Basis für den Founder-Pfad.</done>
</task>

<task type="auto">
  <name>Task 4: routes/logs_routes.py + dashboard.py + logs_page.html — Normal-Filter auf Org (3 Löcher) + Founder-Route</name>
  <files>routes/logs_routes.py, routes/dashboard.py, templates/logs_page.html</files>
  <read_first>
    - routes/logs_routes.py + routes/dashboard.py:157-175 (die 3 Löcher).
    - routes/admin_views.py:224-235 (Founder-Muster) + services/auth_decorators.py:5 (superadmin_required) + services/audit.py (jetzt strict).
    - routes/organisations.py:28 (filter_by(org_id=g.org.id)) + database/models.py:65-69 (User.org_id) + wie db/get_session in dashboard.py bezogen wird.
    - routes/app_routes.py:334 (Dateiname-Format _U{id}_).
  </read_first>
  <behavior>
    - **Helfer** `_org_user_ids(db, org_id) -> set[int]`: `{u.id for u in db.query(User).filter_by(org_id=org_id)}`. Fail-closed: bei Fehler leere Menge (Caller zeigt dann nichts).
    - **UID-Extraktion** aus Dateiname via Regex `_U(\d+)_`; None → Datei gehört keinem eindeutigen User → im Normal-Pfad NICHT zeigen.
    - **Loch 1 (liste):** für owner/admin NICHT mehr ungefiltert — Kandidat nur wenn extrahierte UID ∈ `_org_user_ids(g.org.id)`. non-admins unverändert (nur eigene UID).
    - **Loch 2 (download):** owner/admin dürfen nur laden, wenn UID(filename) ∈ org-Set, sonst 403. non-admins unverändert.
    - **Loch 3 (get_recent_logs):** `org_id` jetzt NUTZEN — für admins auf org-Set filtern (gleiche Logik). Signatur bleibt.
    - **KEIN is_superadmin-Bypass** im Normal-Pfad (D-01).
    - **Founder-Route(n)** (superadmin-only, im logs_bp): `/admin/logs` (Liste ALLER Dateien, weiterhin `endswith('.txt')`-gefiltert
      — persönliche `_summary_*.json` NICHT mitlisten, Fable-Kleinigkeit c) + `/admin/logs/download/<path:filename>`:
      `@login_required @superadmin_required`, Path-Traversal-Guard (`..`/`/` → 400, + bestehendes Regex-Muster), **Grund-Pflicht**
      (Query/Form `grund`, leer → 400), **Audit-vor-Download fail-closed**: `log_action(db, g.user.id, g.org.id, 'founder_log_access',
      target_type='call_log', target_id=filename, details={'datei':filename,'grund':grund}, request=request, strict=True)` in try/except →
      bei Exception **`db.rollback()` + `db.close()` VOR `abort(500)`** (Fable-Kleinigkeit a), OHNE send_file; erst nach erfolgreichem
      Audit `send_file`. Metadaten-only (kein Transkript).
    - **Template-Naht (Fable-Auflage 3):** `templates/logs_page.html:74` verlinkt hart `/logs/download/`. Die Founder-Liste MUSS
      auf `/admin/logs/download/` verlinken. Weg: `logs_page.html` bekommt eine `download_url`-Variable (Default `/logs/download/`
      bleibt für den Normal-Pfad), die Founder-Liste rendert mit `download_url='/admin/logs/download/'`. **VERBOTEN:** den Normal-Download
      per is_superadmin-Bypass „reparieren" (= T-LOGS-02).
  </behavior>
  <action>
1. In `routes/logs_routes.py`: `import re` (vorhanden), `from database.models import User`, `from database.db import get_session`
   (Muster dashboard.py), `from services.auth_decorators import superadmin_required`, `from services.audit import log_action`.
2. Helfer `_org_user_ids` + `_uid_from_name(fname)` (Regex `_U(\d+)_` → int|None) ergänzen.
3. `liste()`: den `if not is_admin`-Zweig ersetzen durch: non-admin → eigene UID; admin → `uid = _uid_from_name(fname); if uid is None or uid not in org_ids: continue` (org_ids einmal pro Request via `_org_user_ids`). db-Session öffnen/schließen sauber.
4. `download()`: analog — admin nur wenn `_uid_from_name(filename) in org_ids`, sonst abort(403). non-admin unverändert.
5. `routes/dashboard.py:get_recent_logs`: den `if not is_admin`-Zweig um die org-Filterung für admins erweitern (org_id-Param nutzen; org_ids via `_org_user_ids`). Signatur + Caller (:526) unverändert.
6. **Template (Fable-Auflage 3):** `templates/logs_page.html:74` `href="/logs/download/{{ log.filename }}"` → `href="{{ download_url|default('/logs/download/') }}{{ log.filename }}"`. Der Normal-`liste()`-Render bleibt unverändert (Default greift); die Founder-Liste rendert `logs_page.html` mit `download_url='/admin/logs/download/'`.
7. Founder-Routen (siehe behavior) in logs_routes.py ergänzen: Liste (`endswith('.txt')`, alle Dateien, render `logs_page.html` mit `download_url='/admin/logs/download/'`) + Download (Grund-Pflicht + strict-Audit-vor-send_file, bei Audit-Exception `db.rollback()`+`db.close()` VOR `abort`).
WICHTIG: KEIN Schema/Migration. KEIN is_superadmin-Bypass im Normal-Pfad (Founder = getrennte Route, NICHT Normal-Download aufweichen — T-LOGS-02). Bestehendes non-admin-Verhalten (`_U{id}_`) bleibt. url_for/Blueprint-Regel (routes/CLAUDE.md, `logs`-Blueprint).
  </action>
  <verify><automated>
    Server-side deploy.sh-Gate: beide Wächter (Task 1+2, jetzt inkl. fail-closed) GRÜN. py_compile logs_routes.py + dashboard.py.
    grep -c 'filter_by(org_id' routes/logs_routes.py  # >=1
    grep -c 'superadmin_required' routes/logs_routes.py  # >=1
    grep -c "strict=True" routes/logs_routes.py  # >=1 (fail-closed Audit)
    grep -c 'rollback' routes/logs_routes.py  # >=1 (Audit-Fail-Pfad, Kleinigkeit a)
    grep -c 'download_url' templates/logs_page.html  # >=1 (parametrisierter Link, Auflage 3)
    grep -c '/admin/logs/download/' routes/logs_routes.py  # >=1 (Founder verlinkt Founder-Route)
  </automated></verify>
  <acceptance_criteria>
    - Alle 3 Löcher gefiltert; admin ohne org-Match → nicht sichtbar / 403; `_org_user_ids` fail-closed (Exception → leer, nie 'alle').
    - `filter_by(org_id` + `superadmin_required` + `strict=True` + `rollback` in logs_routes.py vorhanden.
    - get_recent_logs nutzt org_id (Signatur/Caller unverändert).
    - Template: `logs_page.html` nutzt `download_url` (Default = Normal-Pfad); Founder-Liste rendert mit `/admin/logs/download/`. KEIN Normal-Download-Bypass.
    - Founder-Liste `endswith('.txt')`-gefiltert. Kein is_superadmin-Bypass im Normal-Pfad. py_compile grün. Beide Wächter grün im Gate.
  </acceptance_criteria>
  <done>Die 3 Löcher sind org-gefiltert (fail-closed, kein Bypass); der getrennte Founder-Pfad ist superadmin-only + audit-fail-closed. Beide Wächter grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Firma A ↔ Firma B (Call-Logs) | Owner/Admin A dürfen NIE Logs B lesen/laden (DSGVO, besondere Kategorie). |
| Normal-UI ↔ Founder-Pfad | Cross-Org NUR über den superadmin-only + audit-pflichtigen Pfad; der Normal-Pfad hat KEIN Bypass. |
| Audit-Schreibung ↔ Download | Founder-Download nur NACH erfolgreichem Audit (fail-closed). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-LOGS-01 | Information Disclosure | admin-ungefilterte Liste/Download | mitigate | org-User-ID-Filter (D-02), fail-closed leer-statt-alle; Wächter test_logs_org_boundary. |
| T-LOGS-02 | Elevation/Bypass | is_superadmin umgeht Normal-Filter | mitigate | KEIN is_superadmin-Zweig im Normal-Pfad (D-01); Wächter deckt superadmin-normal ab. |
| T-LOGS-03 | Repudiation | Founder liest fremde Logs unprotokolliert | mitigate | fail-closed Audit-vor-Download (D-04), genau-1-Eintrag-Wächter. |
| T-LOGS-04 | Information Disclosure | Audit-Eintrag leakt Transkript | mitigate | Metadaten-only (D-05, audit.py-Docstring), Wächter prüft kein-Transkript. |
| T-LOGS-05 | Path Traversal | Founder-Download `../` | mitigate | Traversal-Guard (Muster admin_views.py:231) + Regex + isfile. |
| T-LOGS-06 | Fail-open Regression | DB-Fehler → Rückfall auf 'alle' | mitigate | leere Menge bei Fehler → nichts zeigen (D-02 fail-closed); NIE alte admin-Logik als Fallback. |
| T-LOGS-07 | IDOR (Naht) | UID-Substring-Fehlmatch (_U12_ vs _U123_) | mitigate | Regex `_U(\d+)_` exakt + Set-Membership statt Substring. |
</threat_model>

<verification>
Server-side via deploy.sh-Gate (HART Kein-Local-Dev), supervised (André):
1. **Gate grün** inkl. `test_logs_org_boundary.py` + `test_logs_founder_access.py` (PASSED nicht SKIPPED).
2. **Erst-rot belegt:** beide Wächter failen gegen den Code vor Task 4 (im SUMMARY dokumentiert).
3. **Live-Check (Test-User, 2 Orgs):** owner A sieht auf /logs nur A; /logs/download/<B> → 403; is_superadmin-Konto auf /logs nur eigene Firma.
4. **Founder-Pfad:** /admin/logs als superadmin (mit Grund) lädt Cross-Org + erzeugt 1 audit_log; owner/admin → 403; Audit-Fehler → kein Download.
5. **git diff:** nur die 6 Dateien — services/audit.py, routes/logs_routes.py, routes/dashboard.py, templates/logs_page.html, tests/test_logs_org_boundary.py, tests/test_logs_founder_access.py. Kein Schema/Migration.
</verification>

<success_criteria>
- Cross-Tenant-Leak zu (3 Löcher org-gefiltert, fail-closed, kein is_superadmin-Bypass im Normal-Pfad).
- Getrennter Founder-Pfad superadmin-only + Grund-Pflicht + fail-closed Metadaten-Audit-vor-Download.
- audit.py strict-Variante (Bestands-Caller unverändert).
- 2 Wächter erst-rot → grün im deploy.sh-Gate.
- Kein Schema/Migration; git diff nur die 6 Dateien (services/audit.py, routes/logs_routes.py, routes/dashboard.py, templates/logs_page.html, tests/test_logs_org_boundary.py, tests/test_logs_founder_access.py). Punkt 17/27 gewahrt.
</success_criteria>

<output>
After completion create `.planning/phases/08.23.2.AUTH-LOGS-TENANT-call-logs-firmen-grenze/08.23.2.AUTH-LOGS-TENANT-01-SUMMARY.md`.
Dokumentiere: Erst-rot-Beleg beider Wächter (inkl. fail-closed T-LOGS-06), Gate-Ergebnis, Live-2-Org-Check
(B nicht sichtbar, B-Download 403, superadmin-normal B nicht sichtbar), Founder-Pfad (Audit-Delta 1 + 403 für
non-superadmin + fail-closed + Founder-Liste verlinkt /admin/logs/download/), git-diff-Scope.
**BEWUSSTE REST-FLÄCHE (Fable-Auflage 1, André-Entscheidung):** `ConvLogAdmin` (Flask-Admin SecureModelView,
`admin_views.py:93-99`) ist eine unauditierte firmenübergreifende **Metadaten**-Sicht auf ConversationLog
(superadmin-only, KEIN Transkript). BEWUSST NICHT in dieser Phase aufgerissen — die **auditierte** Founder-Metadaten-Sicht
wird in **AUTH-4 (Founder-Maske)** nachgezogen. Im SUMMARY explizit als Rest-Fläche + AUTH-4-Verweis festhalten.
</output>
