---
phase: 08.23.2.SCHILD
plan: 02
status: complete
completed: 2026-06-10
subsystem: database
tags: [models, comment, schild, taxo, dokumentation]
files_modified: [database/models.py]
provides: "comment= (Schild) auf 30 Tabellen (Cluster A Call-Analyse + Cluster C Identitaet/Abrechnung/Infra) + nicht-triviale Spalten"
---

# Phase 08.23.2.SCHILD Plan 02: Schilder Cluster A (Call-Analyse) + Cluster C (Identitaet/Abrechnung/Infra) Summary

SQLAlchemy `comment=` ("Schilder") auf 30 public-Tabellen + deren nicht-triviale Spalten in `database/models.py` gesetzt — Zweck + grep-belegter Status + Schreiber/Leser pro Tabelle. Erst-Einfuehrung des `comment=`-Patterns (Baseline war 0). Keine Verhaltensaenderung, NICHTS geloescht (D-04). Migration folgt in Plan 04.

## Cluster-Abgrenzung

- **Cluster A (Call-Analyse, 9 Tabellen):** ConversationLog, Phrase, ObjectionEvent, EwbRating, LearningCard, LearningEvent, Call, CallEvent, TranscriptSegment.
- **Cluster C (Identitaet/Abrechnung/Infra, 21 Tabellen):** Organisation, User, Session, Invitation, BillingEvent, FeedbackEvent, CoachAssignment, Waitlist, Changelog, AuditLog, Feedback, PlanningFeedbackLink, PromptVersion, ApiCostLog, ApiRate, PriceChangeLog, FixedCost, RevenueLog, ExchangeRate, CrmNote, TenantOrg.
- **NICHT angefasst (Plan 03):** Profile, ProfileSkript, ProfileFaq, ProfileOpener, TrainingScenario, PersonalityType, CoachingReport, Account, Contact, AccountMemory, Meeting, UserPreference, PreferencePair — verifiziert 0 `comment=` je Klasse.

## Verbindungs-Karte (Status grep-belegt, Punkt 22)

| Tabelle | Status | Schreibt (file:line) | Liest (file:line) | grep-Beleg |
|---|---|---|---|---|
| `sessions` (Session) | **write-only [ZOMBIE]** | routes/auth.py:134 (DbSession) | KEIN Reader | Auth nutzt Flask-session, nicht DB-Tabelle (Orchestrator grep-verifiziert) |
| `feedback_events` (FeedbackEvent) | **write-only [ZOMBIE]** | routes/app_routes.py:1253 | KEIN Reader | Orchestrator grep-verifiziert |
| `price_change_log` (PriceChangeLog) | **write-only [ZOMBIE]** | routes/admin_dashboard.py:434 | KEIN Reader | Orchestrator grep-verifiziert |
| `learning_events` (LearningEvent) | **lebt** (KORREKTUR — NICHT write-only) | services/integration_engine.py:54+ (log_learning_event) | services/integration_engine.py:163+188 (raw-SQL Muster-Erkennung) | Orchestrator grep-verifiziert; raw-SQL-Reader existiert |
| `objection_events` (ObjectionEvent) | **lebt** | routes/app_routes.py:384 (db.add) | routes/app_routes.py:471, :1389; routes/dashboard.py:736 | eigener grep `query(ObjectionEvent)` |
| `ewb_ratings` (EwbRating) | **lebt** | routes/admin_ewb.py:192 (db.add) | routes/admin_ewb.py:65, :181 | eigener grep `query(EwbRating)` |
| `learning_cards` (LearningCard) | **lebt** | services/coaching_service.py | routes/learning.py:429+; services/coaching_service.py:65, :170 | eigener grep `query(LearningCard)` |
| `crm_notes` (CrmNote) | **lebt** | routes/admin_views.py:209 | services/customer_success_service.py:48; routes/admin_views.py:209 | eigener grep `query(CrmNote)` |
| `conversation_logs` (ConversationLog) | **lebt (TAXO-Umbau-Zone)** | routes/app_routes.py (end_session) | routes/dashboard.py, routes/learning.py | RUECKGRAT mit 6 Kindern (Orchestrator) |
| `calls`/`call_events`/`transcript_segments` | **lebt** (neue Architektur 08.23.2.A+) | services/ neue Call-Pipeline | services/+routes/ neue Pipeline | Orchestrator |
| `tenant_orgs` (TenantOrg) | **Reserve/Foundation** | noch nicht aktiv | noch nicht aktiv | UUID-Tenancy, FK-Aktivierung Phase 08.23.2.F (Orchestrator) |
| `users`/`organisations` | **lebt** (Kern-Identitaet) | routes/auth.py + ueberall | g.user/g.org ueberall | Orchestrator |
| uebrige Cluster-C-Tabellen (Invitation, BillingEvent, CoachAssignment, Waitlist, Changelog, AuditLog, Feedback, PlanningFeedbackLink, PromptVersion, ApiCostLog, ApiRate, FixedCost, RevenueLog, ExchangeRate) | **lebt** | jeweiliger services/routes-Pfad (im Schild zitiert) | jeweiliger Reader (im Schild zitiert) | aktive Features; Zweck aus Klasse/Spalten + inline-Kommentaren abgeleitet |

## CrmNote — PUBLIC bestaetigt (W4-Note)

grep-Gegenpruefung vor dem Edit: `class CrmNote` (Z.629) hatte **KEIN `__table_args__`** und **KEIN `{'schema': 'crm'}`-Dict** — nur `__tablename__ = 'crm_notes'`. Trotz "Crm"-Klassenname liegt die Tabelle im **public-Schema**. Daher das public-Muster `({'comment': '...'},)` verwendet, NICHT das `{'schema':'crm','comment':...}`-Merge-Muster (das ist Plan 03 fuer die echten crm-Tabellen). Erwartung bestaetigt, kein STOP noetig.

## __table_args__-Behandlung (Punkt 14, additiv)

- **Neues `__table_args__ = ({'comment': '...'},)`** (kein bestehendes Tuple): ConversationLog, Phrase, ObjectionEvent, LearningCard, LearningEvent, Organisation, User, Session, Invitation, BillingEvent, FeedbackEvent, CoachAssignment, Waitlist, Changelog, AuditLog, Feedback, PlanningFeedbackLink, ApiCostLog, PriceChangeLog, FixedCost, RevenueLog, CrmNote, TenantOrg.
- **comment-Dict als LETZTES Element an bestehendes Tuple angehaengt** (Constraints/Indexes unveraendert): EwbRating (UniqueConstraint), PromptVersion (UniqueConstraint), ApiRate (UniqueConstraint), ExchangeRate (UniqueConstraint), Call (CheckConstraints + 3 Indexes), CallEvent (CheckConstraint + 3 Indexes), TranscriptSegment (CheckConstraint + Index).
- Kein bestehender Constraint/Index/Schema-Dict ueberschrieben oder umgeordnet.

## Trivial-Spalten (L-04) ausgelassen

Nicht kommentiert: `id`, `created_at`/`erstellt_am`/`updated_at`/`aktualisiert_am`, alle `*_id`-ForeignKeys, boolean-Flags (`is_*`, `aktiv`, `verwendet`, `active`, `onboarding_done`, `email_confirmed`-Pattern wurde dennoch kommentiert weil Sicherheits-relevant H-18), UUID-PKs. Alle uebrigen Spalten kommentiert — Zweck aus bestehendem inline-`#`-Kommentar abgeleitet wo vorhanden.

## Final Counts

- `grep -c "comment=" database/models.py` → **242**
- Davon Tabellen-Schilder (`{'comment'`-Dicts): **30** (= exakt der Cluster-A+C-Soll-Set, keine Plan-03-Tabelle)
- Davon Spalten-Kommentare: **212**
- AST-Parse-Check: `python -c "import ast; ast.parse(open('database/models.py').read())"` → **exit 0**
- Baseline vor Plan 02: 0 (L-09 bestaetigt)

## Deviations from Plan

Keine architektonischen Abweichungen. `learning_events`-Status wurde gemaess Orchestrator-grep von "write-only-Verdacht" zu **lebt** korrigiert (raw-SQL-Reader in integration_engine.py belegt) — in der Verbindungs-Karte dokumentiert. Sonst plangetreu ausgefuehrt.

## Commits

- `b853e2a` feat(08.23.2.SCHILD-02): Schilder Cluster A (Call-Analyse) in models.py
- `e67b505` feat(08.23.2.SCHILD-02): Schilder Cluster C (Identitaet/Abrechnung/Infra)

## Self-Check: PASSED

- database/models.py existiert + editiert (AST exit 0).
- 30 Tabellen-Schilder = Cluster-A+C-Soll, Plan-03-Tabellen 0 comment= (verifiziert).
- Commits b853e2a + e67b505 im git log.
