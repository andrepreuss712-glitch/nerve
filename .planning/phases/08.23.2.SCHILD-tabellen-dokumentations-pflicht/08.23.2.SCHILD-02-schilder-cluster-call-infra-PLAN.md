---
phase: 08.23.2.SCHILD
plan: 02
type: execute
wave: 2
complexity: 🔴
depends_on: [01]
files_modified: [database/models.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Jede Tabelle des Call-Analyse-Clusters + Identitaet/Abrechnung/Infrastruktur-Clusters hat ein comment= in models.py (Zweck + Status + Leser/Schreiber)."
    - "Jede nicht-triviale Spalte dieser Tabellen hat ein comment= (Trivial-Konvention L-04 ausgenommen)."
    - "Jeder Status (lebt/Reserve/Zombie/write-only) im Schild ist per grep file:line belegt — KEIN angenommener Status."
    - "Die write-only/Zombie-Verdachts-Tabellen (sessions, feedback_events, price_change_log, learning_events) sind NUR als [ZOMBIE]/Status markiert, NICHT entfernt."
  artifacts:
    - path: "database/models.py"
      provides: "comment= auf Tabellen + nicht-trivialen Spalten fuer Cluster A (Call-Analyse) + Cluster C (Identitaet/Abrechnung/Infra)"
      contains: "comment="
  key_links:
    - from: "models.py __table_args__/Column(comment=)"
      to: "Plan 04 Migration (autogenerate liest Base.metadata)"
      via: "SQLAlchemy comment-Attribut"
      pattern: "comment="
---

<objective>
`comment=` (das "Schild") auf zwei public-Cluster setzen: **Call-Analyse-Cluster** (ConversationLog + 8 Kinder, TAXO-Umbau-Zone) und **Identitaet/Abrechnung/Infrastruktur-Cluster** (User, Organisation, TenantOrg, Billing/Cost/Revenue, sowie die 4 Zombie-Verdachts-Tabellen). Basis = §G-Schild-Entwuerfe, getrimmt (D-06), Status grep-verifiziert (D-01/Punkt 20).

Purpose: Erst-Einfuehrung des `comment=`-Patterns (L-09: heute 0x genutzt). Jede Tabelle wird selbst-erklaerend → spaeterer TAXO-Bau (Zombie-Rename + Konsolidierung) wird sicher. Diese Phase aendert KEIN Verhalten und loescht NICHTS (D-04).

Output: models.py mit comment= auf ~21 Tabellen + deren nicht-trivialen Spalten. Migration kommt in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-CONTEXT.md
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-RESEARCH.md
@database/models.py
<!-- §G-Schild-Entwuerfe (Quelle der Texte, KANDIDATEN): Nerve-Vault/03 Planung/TAXO Aufraeum-Inventur (Verstaendnis + Scoring).md Abschnitt G -->

<interfaces>
<!-- L-04 Trivial-Spalten-Konvention (NICHT kommentieren): id, created_at, updated_at, erstellt_am, aktualisiert_am, *_id (FK), is_*/aktiv (Flags), UUID-PK. -->
<!-- __table_args__-Muster (RESEARCH §1.1): -->
<!-- (a) Public-Tabelle OHNE __table_args__ → neues __table_args__ = ({'comment': '...'},) hinzufuegen. -->
<!-- (b) Public-Tabelle MIT __table_args__-Tuple (Constraints/Index) → {'comment': '...'} als LETZTES Tuple-Element anhaengen. Vorsicht trailing-comma bei Single-Element-Tuple. -->
<!-- Spalten-Kommentar: name = Column(String(200), nullable=False, comment='...') — additiv, ungefaehrlich. -->
<!-- Schild-Formel (§0.2): "<Zweck>. Status: <lebt|Reserve|Zombie|write-only>. Schreibt <datei.py:zeile>; liest <datei.py>." knackig (D-06), aber alle 3 Teile + >=10 Zeichen. -->
<!-- Umlaute (CLAUDE.md): COMMENT-Strings sind menschenlesbar → echte Umlaute erlaubt. Spalten-/Tabellen-Namen bleiben ASCII. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Punkt-20-grep-Verifikation aller umstrittenen Status (D-01) — BEVOR Schild-Text geschrieben wird</name>
  <read_first>
    - §G der Aufraeum-Inventur (Status-Kandidaten je Tabelle dieses Clusters)
    - database/models.py Z.189-196 (Session/DbSession), Z.219-226 (FeedbackEvent), Z.518-528 (PriceChangeLog), Z.616-624 (LearningEvent), Z.325-339 (Phrase mode), Z.266-322 (ConversationLog)
    - CONTEXT.md D-02/D-03/D-04 (bekannte Korrekturen + Zombie-Liste)
  </read_first>
  <action>
    Fuer JEDE der folgenden Tabellen den Status im Production-Code-Pfad selbst greppen (lokal auf git-synced Code: `grep -rn "<Model>\|<tabelle>" services/ routes/ database/`; bei Unklarheit `inspect.sh grep`). Pflicht-Kandidaten (CONTEXT D-04 + RESEARCH §2):
    - `Session`/DbSession (sessions): gibt es einen READ-Pfad (query(Session)/token-lookup im Live-Auth) oder nur Writes? → "write-only [ZOMBIE-Verdacht]" nur wenn 0 aktive Leser bewiesen.
    - `FeedbackEvent` (feedback_events): READ-Pfad? → Status belegen.
    - `PriceChangeLog` (price_change_log): Anzeige-/Read-Pfad? → faktisch write-only nur wenn bewiesen.
    - `LearningEvent` (learning_events): Leser? (grep zeigt integration_engine.py als Treffer — pruefen ob WRITE oder READ). → Status belegen.
    - `Phrase` mode!='gatekeeper': wird der Nicht-Gatekeeper-Pfad gelesen? → Status differenziert.
    - `AccountMemory`: NICHT hier (Cluster crm, Plan 03) — aber bekannte Korrektur D-02 (LEBT) notieren.
    - `CoachingReport`: NICHT hier (Cluster Verkaeufer-Wissen, Plan 03) — bekannte Korrektur D-03 (LEBT) notieren.

    Ergebnis als Verbindungs-Karte in die SUMMARY schreiben: pro Tabelle `Status | Schreibt: datei.py:zeile | Liest: datei.py:zeile | grep-Beleg`. Status-Vokabular einheitlich: "lebt", "lebt (TAXO-Umbau-Zone)", "Reserve/Foundation", "write-only", "[ZOMBIE]". KEINE Tabelle bekommt einen Status ohne grep-Beleg (Punkt 22: Behauptung ohne Beleg = BLOCK). NICHTS loeschen (D-04).
  </action>
  <verify>
    <automated>MISSING — grep-Verifikation, Beleg = Verbindungs-Karte in SUMMARY. Maschinen-Acceptance erfolgt in Plan 05 (Guard) + Plan 04 (Migration-Apply).</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY enthaelt eine Verbindungs-Karte mit grep file:line pro umstrittener Tabelle.
    - Jeder Zombie-/write-only-Status hat 0-aktive-Leser-Beleg ODER wurde nach grep zu "lebt" korrigiert.
    - `grep -rn "comment=" database/models.py | wc -l` zeigt 0 VOR diesem Task (Baseline, L-09).
  </acceptance_criteria>
  <done>Jeder Status fuer Cluster A+C ist bewiesen, nicht angenommen — Voraussetzung fuers Schild-Schreiben.</done>
</task>

<task type="auto">
  <name>Task 2: comment= fuer Call-Analyse-Cluster (Cluster A) setzen</name>
  <read_first>
    - database/models.py Z.266-322 (ConversationLog), 325-339 (Phrase), 388-401 (ObjectionEvent), 403-429 (EwbRating), 580-594 (LearningCard), 616-624 (LearningEvent), 645-697 (Call), 700-715 (CallEvent), 718-733 (TranscriptSegment)
    - §G-Entwuerfe fuer genau diese Tabellen (Text-Basis, trimmen D-06)
    - Task-1-Verbindungs-Karte (Status-Quelle)
  </read_first>
  <action>
    Tabellen-`comment=` + nicht-triviale-Spalten-`comment=` setzen fuer: ConversationLog, Phrase, ObjectionEvent, EwbRating, LearningCard, LearningEvent, Call, CallEvent, TranscriptSegment.

    Punkt-14-Control-Flow pro Edit (4 Schichten): (1) lokaler Kontext = bestehendes __table_args__ der Tabelle lesen; (2) Skelett = comment additiv, Tuple-Ordnung wahren ({'comment'} bzw. {'schema':...,'comment':...} LETZTES Element); (3) Cross-File = keine; nur models.py; (4) Edge-Cases = Tabellen mit Single-Element-Tuple (z.B. EwbRating Z.411-414 hat UniqueConstraint mit trailing comma → {'comment'} sauber anhaengen), Call (Z.678-697 grosser __table_args__-Block → comment-Dict als letztes Element NACH den Indexes).

    Schild-Texte: §G getrimmt (D-06), Status aus Task-1-Karte. Beispiele: ConversationLog = "lebt (TAXO-Umbau-Zone), RUECKGRAT mit 6 Kindern"; Call/CallEvent/TranscriptSegment = neue Architektur (08.23.2.A+); LearningEvent = Status exakt aus Task-1-grep (kein blindes "write-only"). Nicht-triviale Spalten kommentieren (z.B. ConversationLog.einwaende_gesamt, redeanteil_avg, kb_verlauf, precall_briefing; Call.call_mode, outcome, transcript_storage, followup_intent). Trivial-Spalten (L-04) auslassen.
    Umlaute in COMMENT-Strings erlaubt; Identifier ASCII.
  </action>
  <verify>
    <automated>MISSING — kein Local-Pytest erlaubt (CLAUDE.md). Maschinen-Verify = autogenerate-Migration (Plan 04) + Guard (Plan 05) auf Production. Lokaler Syntax-Check: `python -c "import ast; ast.parse(open('database/models.py').read())"` ist reines Parsen (kein App-Run) — erlaubt als Editor-Funktion.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "comment=" database/models.py` steigt um die Anzahl gesetzter Schilder.
    - `python -c "import ast; ast.parse(open('database/models.py').read())"` exit 0 (Datei syntaktisch valide).
    - Jede der 9 Tabellen hat ein Tabellen-comment >=10 Zeichen mit Zweck+Status+Leser/Schreiber.
    - Kein bestehender Constraint/Index/Schema-Dict ueberschrieben (Punkt 14).
  </acceptance_criteria>
  <done>Call-Analyse-Cluster vollstaendig beschildert in models.py.</done>
</task>

<task type="auto">
  <name>Task 3: comment= fuer Identitaet/Abrechnung/Infrastruktur-Cluster (Cluster C) setzen</name>
  <read_first>
    - database/models.py Z.19-132 (Organisation, User), 189-235 (Session, Invitation, BillingEvent, FeedbackEvent, CoachAssignment), 343-385 (Waitlist, Changelog, AuditLog), 432-472 (Feedback, PlanningFeedbackLink, PromptVersion), 477-575 (ApiCostLog, ApiRate, PriceChangeLog, FixedCost, RevenueLog, ExchangeRate), 629-635 (CrmNote), 736-745 (TenantOrg)
    - §G-Entwuerfe fuer diese Tabellen
    - Task-1-Verbindungs-Karte (Status sessions/feedback_events/price_change_log)
  </read_first>
  <action>
    Tabellen- + nicht-triviale-Spalten-`comment=` setzen fuer: Organisation, User, Session, Invitation, BillingEvent, FeedbackEvent, CoachAssignment, Waitlist, Changelog, AuditLog, Feedback, PlanningFeedbackLink, PromptVersion, ApiCostLog, ApiRate, PriceChangeLog, FixedCost, RevenueLog, ExchangeRate, CrmNote, TenantOrg.

    Punkt-14 wie Task 2 (additiv, Tuple-Ordnung, kein Ueberschreiben). Achtung: ApiRate/PromptVersion/ExchangeRate haben bereits UniqueConstraint-Tuples → comment-Dict als LETZTES Element anhaengen. TenantOrg (Z.736-745) hat KEIN __table_args__ → neues __table_args__ = ({'comment': '...'},) hinzufuegen; ist Foundation (→ Plan 04 Register D-05).

    ⚠️ W4-NOTE — CrmNote (models.py ~Z.629-635) ist trotz des Namens eine PUBLIC-schema Tabelle (KEIN {'schema':'crm'}-Dict in ihrem __table_args__ — der "Crm"-Praefix ist nur der Klassen-Name, die Tabelle liegt im public-Schema). DESHALB: bei CrmNote das **public-`__table_args__`-Muster (a)/(b)** verwenden (`({'comment': '...'},)` bzw. comment ans bestehende Tuple anhaengen) — NICHT das `{'schema':'crm','comment':...}`-Merge-Muster (das ist Plan 03 fuer die ECHTEN crm-Tabellen Account/Contact/AccountMemory/Meeting/UserPreference). CrmNote vor dem Edit per grep gegenpruefen: existiert ein `'schema': 'crm'` in seinem __table_args__? → Nein erwartet. Wenn doch wider Erwarten → STOP + in SUMMARY notieren.

    Status aus Task-1-Karte: Session = "write-only [ZOMBIE-Verdacht]" NUR wenn Task 1 0 Leser belegt (sonst korrigierter Status); FeedbackEvent/PriceChangeLog analog. NICHTS loeschen — nur markieren (D-04). User/Organisation = "lebt" (Kern-Identitaet). TenantOrg = "Reserve/Foundation (UUID-Tenancy, FK-Aktivierung Phase 08.23.2.F)".
    User hat sehr viele Spalten — nur nicht-triviale kommentieren (z.B. plan, rolle, market, language, oauth_provider; NICHT die is_*/aktiv/created-Flags, NICHT FK *_id).
  </action>
  <verify>
    <automated>MISSING — kein Local-Pytest. Verify = Plan 04 Migration + Plan 05 Guard auf Production. AST-Parse-Check lokal erlaubt.</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('database/models.py').read())"` exit 0.
    - Alle 21 Tabellen dieses Clusters haben Tabellen-comment >=10 Zeichen.
    - CrmNote nutzt das public-__table_args__-Muster (KEIN {'schema':'crm'}-Merge) — in SUMMARY bestaetigt.
    - sessions/feedback_events/price_change_log tragen den in Task 1 BEWIESENEN Status (kein angenommenes "write-only").
    - Kein bestehender Constraint/Index ueberschrieben.
  </acceptance_criteria>
  <done>Identitaet/Abrechnung/Infra-Cluster beschildert. (crm + Verkaeufer-Wissen + training-ORM folgen in Plan 03.)</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- models.py wird editiert (Schema-DEFINITION, nicht Daten). KEINE Daten gelesen/geschrieben. Die Tabellen selbst (conversation_logs, calls, users, organisations, sessions, ... ) werden NICHT in dieser Wave gegen Production verifiziert weil kein Daten-Pfad angefasst wird — nur ihr comment-Attribut. Die Schema-Existenz pro Schema wurde in Plan 01 (Discovery) gegen Production belegt; das comment-Diffing prueft Plan 04.

### Cross-Layer-Konsistenz-Tabelle
| Daten | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| comment-Attribut auf Column/__table_args__ | models.py → Base.metadata | SQLAlchemy-Metadata (wird in Plan 04 via autogenerate zu pg_description) | ⬜ Plan 04 verifiziert das DB-seitige Ankommen |
| CrmNote-Schema-Zugehoerigkeit | grep __table_args__ in models.py | public-Schema (KEIN crm-Dict) | ⬜ Task 3 grep-Gegenpruefung vor dem Edit |

### Status-Behauptungen
- Jede Status-Behauptung (lebt/zombie/write-only) wird in Task 1 per grep file:line belegt → Verbindungs-Karte in SUMMARY. Behauptung ohne Beleg = BLOCK (Punkt 22).

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| keine | Reine Schema-Annotation. Kein User-Input, kein Laufzeit-Pfad geaendert. COMMENT-Strings landen via Migration (Plan 04) in der DB. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SCHILD-02 | T (Tampering) | __table_args__-Edit bricht bestehendes Schema | mitigate | Punkt-14-Control-Flow pro Edit; AST-Parse-Check; comment-Dict additiv als LETZTES Tuple-Element; Plan 04 prueft autogenerate-Diff manuell (nur COMMENTs, keine Struktur-Diffs). |
| T-SCHILD-03 | R (Repudiation) | Falscher Status im Schild (Drift) | mitigate | Punkt-20-grep-Verifikation (Task 1); kein Status ohne file:line-Beleg. |
| T-SCHILD-02b | T (Tampering) | CrmNote faelschlich als crm-Schema-Tabelle behandelt → autogenerate-Drift | mitigate | W4-Note: CrmNote ist public; grep-Gegenpruefung vor Edit; public-Muster statt schema-Merge. |
</threat_model>

<verification>
- AST-Parse von models.py grün.
- Verbindungs-Karte mit grep-Belegen in SUMMARY.
- comment= auf allen Cluster-A+C-Tabellen + nicht-trivialen Spalten.
- CrmNote als public bestaetigt.
</verification>

<success_criteria>
- ~30 Tabellen (Cluster A: 9 + Cluster C: 21) beschildert in models.py.
- Jeder Status grep-belegt, NICHTS geloescht (D-04).
- Trivial-Spalten-Konvention (L-04) angewandt.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-02-SUMMARY.md` (inkl. Verbindungs-Karte).
</output>
</content>
