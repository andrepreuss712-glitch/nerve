# Foundation-Code-Register — Einträge aus Phase 08.23.2.SCHILD (D-05)

> ⚠ Das eigentliche Register `Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md` ist auf dieser
> Maschine NICHT erreichbar (Vault nicht im Repo). Diese 4 Einträge sind zum **Einfügen in dein Vault-Register**
> aufbereitet (Plan 04 Task 3, files_modified → Vault-Pfad konnte nicht geschrieben werden = bewusste Deviation).

## Die 4 Foundation-Tabellen

| Tabelle | Was | Warum stehengeblieben (Foundation) | Aktivierende Phase |
|---|---|---|---|
| `crm.account_memory` | MEDDPICC-Gedächtnis je Account (JSONB) | Aktiv genutzt (lebt) — gelesen `services/precall_service.py:175` für PreCall-Briefing — aber Foundation für den Account-Intelligence-Ausbau (mehr Schreiber/Konsolidierung). | laufender Ausbau (G-MEET-Pipeline schreibt) |
| `training.preference_pairs` | DPO-Präferenz-Triples (prompt/chosen/rejected) | Created-not-populated: 0 aktive Befüller im Code. DPO-Trainings-Foundation. | **08.23.2.E** (befüllt + DPO-Job) |
| `training.transcript_archive` | Anonymisierungs-Rohmaterial (Pipeline B), ORM-los | Foundation für die DPO-Pipeline; vom Anonymizer-Worker befüllt, kein ORM-Model (raw-DDL 0008). | **08.23.2.E** (DPO-Konsum) |
| `tenant_orgs` (public) | UUID-Tenancy-Wurzel parallel zur Integer-org_id (Brücke `legacy_org_id`) | Reserve/Foundation — noch kein aktiver FK; UUID-Tenancy-Migration ausstehend. | **08.23.2.F** (FK-Aktivierung calls/accounts→tenant_orgs) |

Alle 4 tragen ihr Schild in models.py + Migration 0015 (pg_description). Status im Schild: account_memory="lebt (Foundation, crm)", preference_pairs/transcript_archive="Reserve/Foundation", tenant_orgs="Reserve/Foundation".
