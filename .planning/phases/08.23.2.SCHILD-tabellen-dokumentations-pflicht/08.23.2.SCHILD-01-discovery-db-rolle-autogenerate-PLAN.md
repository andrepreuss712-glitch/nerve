---
phase: 08.23.2.SCHILD
plan: 01
type: execute
wave: 1
complexity: 🔴
depends_on: []
files_modified: [.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-DISCOVERY-DECISIONS.md]
autonomous: false
requirements: []
must_haves:
  truths:
    - "Es ist BEWIESEN (psql-Output) welche DB-Rolle pg_description aller 3 Schemas (public/crm/training) lesen kann."
    - "Es ist BEWIESEN ob Alembic-autogenerate COMMENT-Diffs erkennt — aber op.execute ist der EMPFOHLENE Default-Stil (hauseigenes Muster, alle 14 Migrationen); autogenerate nur dokumentierter Fallback."
    - "Die env.py include_schemas-Strategie ist gegen das echte Production-Schema verifiziert (kein fremdes Schema wird mitgerissen) — relevant fuer den autogenerate-Fallback, NICHT kritisch fuer den op.execute-Default."
    - "DISCOVERY-DECISIONS.md enthaelt maschinen-parsebare Schluessel (DSN_ENV_VAR, MIGRATION_STYLE, ALEMBIC_VERSION), die Plan 04/05/06 grep-verifizieren koennen."
  artifacts:
    - path: ".planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-DISCOVERY-DECISIONS.md"
      provides: "Verriegelte Antworten: Guard-DB-Rolle/DSN-ENV-Var, MIGRATION_STYLE (Default=op.execute), include_schemas-Filter (Fallback-relevant), Provisioning-SQL. Konsumiert von Plan 04 + 05 + 06."
      min_lines: 25
  key_links:
    - from: "Plan 04 (Migration) + Plan 05 (Guard) + Plan 06 (inspect/CLAUDE.md)"
      to: "DISCOVERY-DECISIONS.md"
      via: "gelesene Rollen-/Migrations-Stil-Entscheidung + maschinen-parsebare Keys"
      pattern: "DISCOVERY-DECISIONS"
---

<objective>
Die drei offenen Unbekannten der Phase BEWEISEN bevor irgendein Code/Guard gebaut wird — gegen das echte Production-Postgres, nicht gegen Theorie/SQLite (CLAUDE.md Punkt 13/21/22, RESEARCH §1.3+§1.2).

Purpose: Guard, inspect.sh und Migration haengen alle an einer Frage, die NIEMAND raten darf: **Welche DB-Rolle sieht pg_description von public+crm+training?** nerve_app hat LEEREN search_path (db.py) + ist NICHT Owner von crm/training (models.py:750) → vielleicht sieht es deren Kommentare nicht. Falsch geraten = stille gruene Sicherheit (Gemini-Warnung L-08). Zweitens: der Migrations-Stil — der EMPFOHLENE Default ist `op.execute` (hauseigenes Muster, alle 14 Bestands-Migrationen; vermeidet den fehleranfaelligen Server-autogenerate-Round-Trip / Split-Brain-Risiko, Cross-AI-Finding 1). autogenerate bleibt nur als dokumentierter, NICHT-empfohlener Fallback. Drittens: zieht include_schemas=True fremde Schemas mit (relevant NUR fuer den autogenerate-Fallback)?

Output: EIN Entscheidungs-Artefakt (DISCOVERY-DECISIONS.md), das Plan 04 + 05 + 06 verriegelt konsumieren — inkl. maschinen-parsebarer Schluessel + fertiger Provisioning-SQL. KEIN Code-Edit in dieser Wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-CONTEXT.md
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-RESEARCH.md
@database/db.py
@alembic/env.py

<interfaces>
<!-- Aus db.py: nerve_app-Verbindung hat transaction-local app.tenant_id-GUC, aber KEINE search_path-Setzung. -->
<!-- Aus models.py:750-754: crm/training-Tabellen sind explizit schema-qualifiziert weil nerve_app rolconfig EMPTY ist (kein role search_path). -->
<!-- Aus inspect.sh:59-77: _run_psql nutzt `sudo -u nerve_app psql` (peer-auth). Fuer privilegierten Zugriff existiert das Muster `sudo -u postgres psql` (Memory project_nerve_prod_deploy: Migrationen laufen als postgres). -->
<!-- Production-SSH (Memory project_nerve_prod_deploy): ssh -i ~/.ssh/nerve_vps root@178.104.82.166 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Rollen-pg_description-Sichtbarkeit gegen Production beweisen (L-08, Punkt 21/22)</name>
  <read_first>
    - database/db.py Z.31-78 (nerve_app: leerer search_path, transaction-local GUC, KEINE search_path-Setzung)
    - database/models.py Z.748-754 (Begruendung warum crm/training explizit schema-qualifiziert: nerve_app rolconfig EMPTY)
    - scripts/inspect.sh Z.59-77 (_run_psql nutzt sudo -u nerve_app; postgres-Variante existiert auf dem Server)
    - Memory project_nerve_prod_deploy (Host 178.104.82.166, key nerve_vps, Migrationen laufen als postgres)
  </read_first>
  <action>
    Gegen Production-Postgres (ssh -i ~/.ssh/nerve_vps root@178.104.82.166) fuer BEIDE Rollen-Kandidaten testen, ob obj_description/col_description aus allen 3 Schemas sichtbar ist. Konkret ausfuehren und Output WOERTLICH ins Artefakt schreiben:

    1. Als nerve_app (peer):
       `sudo -u nerve_app psql -d nerve -c "SET search_path TO public, crm, training; SELECT n.nspname, c.relname, obj_description(c.oid, 'pg_class') FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace WHERE n.nspname IN ('public','crm','training') AND c.relkind='r' ORDER BY 1,2;"`
    2. Pruefen: Liefert nerve_app Zeilen fuer crm.* UND training.* (ohne permission-denied)? Oder werden Schemas still uebersprungen (0 Zeilen trotz existierender Tabellen)?
    3. Falls nerve_app crm/training NICHT vollstaendig sieht: dieselbe Query als `sudo -u postgres psql -d nerve -c "..."` ausfuehren und bestaetigen dass postgres ALLE 3 Schemas sieht.
    4. Pruefen ob USAGE-Grant fuer nerve_app eine gangbare Alternative waere ODER ob der Guard eine eigene privilegierte read-only DSN braucht (L-08 Option a vs. b).

    ENTSCHEIDUNG ins Artefakt: Welche Rolle/DSN nutzt (a) der pytest-Guard, (b) der inspect.sh schilder-Befehl. Mit psql-Output als Beweis pro Behauptung (Punkt 22: kein Satz ohne Beleg). Falls "nerve_app reicht NICHT" → konkrete DSN/Rolle benennen (z.B. postgres-Socket read-only) inkl. wie die conftest-Fixture sie bekommt.

    MASCHINEN-PARSEBARER SCHLUESSEL (W1/W2 — Plan 04/05/06 grep-en darauf): am Kopf des Artefakts eine eindeutige Key-Zeile schreiben:
      `DSN_ENV_VAR: <Name>`  (z.B. `DSN_ENV_VAR: NERVE_APP_TEST_DSN` ODER `DSN_ENV_VAR: NERVE_SCHILD_TEST_DSN`)
      `GUARD_ROLE: <nerve_app+USAGE | nerve_schild_guard | postgres-socket>`
    Plan 05/06 lesen GENAU diesen Key — KEIN Hardcode des Namens irgendwo sonst.

    CONTINGENCY-PROVISIONING (INFO — VOR dem Checkpoint ins Artefakt schreiben, damit Andre am Checkpoint nicht feststeckt): Falls nerve_app crm/training-pg_description NICHT sieht, die ZWEI konkreten Provisioning-Wege als fertige copy-paste-SQL ins Artefakt schreiben:
      - OPTION A (minimal-invasiv): `GRANT USAGE ON SCHEMA crm, training TO nerve_app;` → danach RE-TEST der obj_description-Query als nerve_app. USAGE reicht fuer Catalog-Sicht der Objekte (KEINE Tabellen-SELECT-Rechte noetig fuer obj_description). Wenn JA → Guard nutzt nerve_app + SET search_path, KEINE neue Rolle.
      - OPTION B (sauber abgeschottet): `CREATE ROLE nerve_schild_guard LOGIN PASSWORD '<gen>'; GRANT USAGE ON SCHEMA public, crm, training TO nerve_schild_guard;` (nur Catalog-Sicht, KEINE Daten-GRANTs) → eigene DSN-ENV-Var. Verwenden wenn nerve_app NICHT mit crm/training-USAGE belastet werden soll (RLS-Sauberkeit).
    EINE Option mit Begruendung als gewaehlt markieren (oder "nerve_app reicht ohne Grant" wenn der Test es zeigt). Plan 05/06 uebernehmen die gewaehlte Option 1:1 — keine Provisioning-Erfindung durch den Executor.
  </action>
  <verify>
    <automated>MISSING — Discovery-Task, Beleg ist der psql-Output im Artefakt (manuell gegen Production). Acceptance via human-verify Task 4.</automated>
  </verify>
  <acceptance_criteria>
    - DISCOVERY-DECISIONS.md enthaelt WOERTLICHEN psql-Output beider Rollen-Tests (nicht zusammengefasst).
    - Maschinen-Key `DSN_ENV_VAR:` + `GUARD_ROLE:` am Artefakt-Kopf vorhanden.
    - Provisioning-SQL (Option A + B) als copy-paste-Block dokumentiert, eine als gewaehlt markiert.
    - Eine eindeutige Rollen/DSN-Entscheidung fuer Guard + inspect.sh steht fest, mit Beleg.
  </acceptance_criteria>
  <done>Guard-Connection-Privileg-Frage explizit beantwortet (nicht angenommen) — der genaue Grund warum diese Phase Discovery braucht.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations-Stil festlegen (op.execute = EMPFOHLEN) + autogenerate-Comment-Diffing/include_schemas nur als Fallback pruefen</name>
  <read_first>
    - alembic/env.py Z.19-56 (target_metadata, compare_type=True, KEIN include_schemas)
    - alembic/versions/0014_crm_user_preferences_rls.py (juengste Migration = Basis-Revision 0014; op.execute-Muster)
    - alembic/versions/0008_training_schema_foundation.py Z.26-40 (transcript_archive raw-DDL, KEIN ORM-Model)
    - requirements.txt (alembic-Version — informativ; op.execute braucht keine Mindest-Version)
  </read_first>
  <action>
    EMPFEHLUNGS-LEITLINIE (Cross-AI-Finding 1, MEDIUM): `op.execute` ist der EMPFOHLENE Default-Migrations-Stil. Begruendung: alle 14 Bestands-Migrationen nutzen op.execute (etabliertes Hausmuster); op.execute("COMMENT ON TABLE/COLUMN ... IS '...'") ist fuer reines COMMENT-Hinzufuegen perfekt geeignet; der autogenerate-Pfad braucht einen fehleranfaelligen Server-Round-Trip (push → `alembic revision` auf dem Server → scp zurueck → Server-Temp loeschen) mit Split-Brain-Risiko. Default-Erwartung = `MIGRATION_STYLE: op.execute`. Diesen Wert nur dann NICHT waehlen, wenn ein konkreter, belegter Grund dagegen spricht.

    Klaeren OHNE Code dauerhaft zu aendern:

    1. Migrations-Stil festlegen: `MIGRATION_STYLE: op.execute` als Default ins Artefakt schreiben (mit obiger Begruendung). autogenerate NUR als dokumentierten, NICHT-empfohlenen Fallback notieren (z.B. fuer den Fall, dass jemand spaeter doch einen ORM-getriebenen Diff bevorzugt) — klar als "nicht empfohlen, Split-Brain-Risiko" markieren.
    2. include_schemas-Strategie (FALLBACK-relevant, NICHT kritisch fuer op.execute-Default): Bestaetigen, dass mit `include_schemas=True` ein autogenerate-Lauf nur public/crm/training erfasst und KEINE Postgres-internen/fremden Schemas (pg_catalog, information_schema) mitzieht. Den genauen include_name/include_object-Filter-Code-Block (Callback auf {public, crm, training}) trotzdem ins Artefakt schreiben — Plan 04 setzt ihn additiv in env.py (hilft anderen autogenerate-Laeufen), MARKIERT als nicht-kritisch fuer die op.execute-Migration selbst (RESEARCH §1.2 Landmine bleibt fuer Fallback dokumentiert). Optional: alembic-Versions-Check (`pip show alembic` server-side) nur informativ ins Artefakt — fuer op.execute irrelevant.
    3. Migrations-Rolle bestaetigen: Migration laeuft auf Production als postgres (Memory) — relevant fuer die transcript_archive-COMMENT-DDL und crm/training-Schreibrechte.

    MASCHINEN-PARSEBARE SCHLUESSEL (W1 — Plan 04 grep-verifiziert sie BEVOR es die Migration baut): am Artefakt-Kopf zwei Key-Zeilen schreiben:
      `MIGRATION_STYLE: op.execute`  (EMPFOHLENER Default; nur mit belegtem Grund auf `autogenerate` aendern — autogenerate ist Fallback)
      `ALEMBIC_VERSION: x.y.z`  (informativ; aus `alembic --version` server-side — fuer op.execute nicht entscheidend)
    Plan 04 verzweigt rein auf `MIGRATION_STYLE` (Default-Branch = op.execute). KEINE Stil-Entscheidung in Plan 04 — nur Konsum dieses Keys.
  </action>
  <verify>
    <automated>MISSING — Discovery, Beleg = Migrations-Stil-Entscheidung + (falls autogenerate-Fallback) Filter-Codeblock im Artefakt. Acceptance via Task 4.</automated>
  </verify>
  <acceptance_criteria>
    - Maschinen-Key `MIGRATION_STYLE: op.execute` (Default) + `ALEMBIC_VERSION: x.y.z` (informativ) am Artefakt-Kopf (grep-bar fuer Plan 04).
    - op.execute ist klar als EMPFOHLEN markiert, autogenerate explizit als NICHT-empfohlener Fallback (Split-Brain-Begruendung) dokumentiert.
    - include_name/include_object-Filter-Codeblock steht 1:1-uebernehmbar im Artefakt, MARKIERT als nur-Fallback / nicht-kritisch.
    - Bestaetigt: Migration-Rolle = postgres.
  </acceptance_criteria>
  <done>Plan 04 kann die Migration ohne weitere offene Frage bauen — Default-Stil = op.execute, maschinen-greifbar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>DISCOVERY-DECISIONS.md mit (a) Guard/inspect-DB-Rolle + Beleg + DSN_ENV_VAR/GUARD_ROLE-Key + Provisioning-SQL, (b) MIGRATION_STYLE (Default=op.execute) + Begruendung + ALEMBIC_VERSION (informativ), (c) include_schemas-Filter-Codeblock (Fallback-markiert).</what-built>
  <how-to-verify>
    1. Oeffne `.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-DISCOVERY-DECISIONS.md`.
    2. Pruefe: Steht echter psql-Output drin (nicht "vermutlich")? Ist die Rollen-Entscheidung eindeutig? Steht `DSN_ENV_VAR:` + `GUARD_ROLE:` als Key?
    3. Pruefe: Ist die Provisioning-SQL (Option A/B) fertig zum Ausfuehren, falls nerve_app blind ist? (Damit du am Guard-Plan nicht feststeckst.)
    4. Pruefe: Steht `MIGRATION_STYLE: op.execute` (Default) + `ALEMBIC_VERSION:` als Key, mit op.execute=empfohlen / autogenerate=Fallback-Begruendung?
    5. Andre-Bestaetigung dass diese Entscheidungen die Phasen-Architektur (Guard-Rolle, Migrations-Stil) korrekt verriegeln.
  </how-to-verify>
  <resume-signal>"approved" oder beschreibe was nachgeschaerft werden muss</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- KEINE Tabelle wird in dieser Wave geschrieben. Es werden NUR Catalog-Sichten gelesen: `pg_class`, `pg_namespace`, `pg_description` (via obj_description/col_description) — read-only, gegen Production.

### Cross-Layer-Konsistenz-Tabelle
| Gelesenes Datum | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| obj_description/col_description | psql gegen Production | pg_description (Postgres-Catalog, alle 3 Schemas) | ⬜ DIESER Task beweist die Sichtbarkeit pro Rolle |

### Bei Diskrepanz: STOP — die ganze Phase haengt an dieser Antwort.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| keine | Reine read-only Inspektion gegen Production-Catalog; kein User-Input, kein Schreibpfad. (Provisioning-SQL wird NUR dokumentiert, nicht in dieser Wave ausgefuehrt.) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SCHILD-01 | I (Info Disclosure) | psql-Output ins Artefakt | accept | Catalog-Metadaten (Tabellen-/Spalten-Namen + Kommentare) sind kein Secret; KEINE Daten-Rows, KEINE env-Werte ins Artefakt. |
</threat_model>

<verification>
- DISCOVERY-DECISIONS.md existiert mit den 3 verriegelten Antworten + Belegen.
- Maschinen-Keys DSN_ENV_VAR / GUARD_ROLE / MIGRATION_STYLE (Default=op.execute) / ALEMBIC_VERSION vorhanden.
- Provisioning-SQL (A/B) dokumentiert.
- Human-verify-Checkpoint approved.
</verification>

<success_criteria>
- Guard/inspect-DB-Rolle bewiesen (psql-Output), nicht geraten.
- Migrations-Stil = op.execute (empfohlener Default), autogenerate als Fallback dokumentiert + begruendet + maschinen-parsebar.
- include_schemas-Filter 1:1-uebernehmbar dokumentiert (Fallback-markiert).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-01-SUMMARY.md`
</output>
</content>
