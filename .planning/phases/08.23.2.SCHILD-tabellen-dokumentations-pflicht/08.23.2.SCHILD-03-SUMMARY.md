---
phase: 08.23.2.SCHILD
plan: 03
subsystem: database-models
status: complete
completed: 2026-06-10
tags: [schild, tabellen-doku, crm, training, cluster-b, foundation]
requires: [08.23.2.SCHILD-02]
provides: "comment= fuer Cluster B (7) + crm-Schema (5) + training-ORM (1) — alle ORM-Models beschildert"
affects: [database/models.py]
key-files:
  modified:
    - database/models.py
decisions:
  - "AccountMemory Status korrigiert auf 'lebt (Foundation, crm)' (D-02 — Fehlbefund 'toter Import' widerlegt, precall_service.py:175)"
  - "CoachingReport Status korrigiert auf 'lebt (Read-Through-Wochen-Cache)' (D-03 — Fehlbefund 'write-only' widerlegt, coaching_service.py:195/351)"
  - "PreferencePair Status 'Reserve/Foundation' (0 aktive Befueller in Production-Code; nur Migration-DDL + Grant; Phase 08.23.2.E befuellt)"
  - "crm/training-comment IN bestehendes {'schema':...}-Dict gemerged, Dict bleibt LETZTES Tuple-Element (SQLAlchemy-Schema-Qualifizierung)"
metrics:
  duration: ~15min
  tasks: 3
  files: 1
  tables_beschildert: 13
  comment_total: 306
---

# Phase 08.23.2.SCHILD Plan 03: Schilder Cluster B + crm + training Summary

Die letzten 13 ORM-Tabellen beschildert: Verkaeufer-Wissen/Training-Config-Cluster (7 public), crm-Schema (5, comment ins Schema-Dict gemerged) und training-ORM PreferencePair (1). Nach Plan 03 traegt JEDES ORM-Model ein Schild; nur die ORM-lose transcript_archive bleibt fuer Plan 04.

## Tasks

- **Task 1** — Status-grep-Bestaetigung (D-02/D-03/Foundation) aus git-synchronem Code: alle belegt.
- **Task 2** — Cluster B (7 public Tabellen, kein Schema-Dict): neue `({'comment': ...},)` je Tabelle. Commit `4cc5b0d`.
- **Task 3** — crm (5) + training (1): comment IN bestehendes `{'schema': ...}`-Dict gemerged, Dict bleibt LETZTES Element. Commit `489e32f`.

## Verbindungs-Karte (Punkt 22 — Beleg pro Status)

| Tabelle | Schema | Status | Schreibt (file:line) | Liest (file:line) | grep-Beleg |
|---|---|---|---|---|---|
| Profile | public | lebt | routes/profiles.py | live_session.set_active_profile (daten) | Profil-Pfad, models.py:137 |
| ProfileSkript | public | lebt | routes/profiles.py | PreCall-Personalisierung services/ | models.py:151 |
| ProfileFaq | public | lebt | routes/profiles.py | Live-Loop services/ | models.py:166 |
| ProfileOpener | public | lebt | routes/profiles.py | PreCall-Personalisierung services/ | models.py:178 |
| TrainingScenario | public | lebt | routes/training.py | services/training_service.py | models.py:246 |
| PersonalityType | public | lebt | routes/training.py | Trainings-Loop services/ | models.py:260 |
| CoachingReport | public | **lebt (Read-Through-Wochen-Cache)** | services/coaching_service.py:351 | services/coaching_service.py:195 (db.query(CoachingReport)) + Dashboard | grep coaching_service.py:182/195/351 (D-03) |
| Account | crm | **lebt (crm, RLS-isoliert)** | services/crm_service.py | routes/crm_export.py | grep crm_service.py + crm_export.py |
| Contact | crm | **lebt (crm, RLS-isoliert)** | services/crm_service.py | routes/crm_export.py | grep crm_service.py + crm_export.py |
| AccountMemory | crm | **lebt (Foundation, crm)** | G-MEET-Pipeline | services/precall_service.py:175 (db.query(AccountMemory), PreCall-MEDDPICC-Briefing) | grep precall_service.py:17/175-177 (D-02) |
| Meeting | crm | **lebt (crm, RLS-isoliert)** | services/crm_service.py + routes/app_routes.py | routes/app_routes.py (PiP Termin-Form) | grep crm_service.py + app_routes.py |
| UserPreference | crm | **lebt (crm, RLS-isoliert)** | services/crm_service.py (g.user.id, MM-07) | routes/app_routes.py | grep crm_service.py |
| PreferencePair | training | **Reserve/Foundation** | noch nicht aktiv (Phase 08.23.2.E) | spaeterer DPO-Trainings-Job | grep: nur alembic/0013 (DDL+GRANT) + anonymizer_worker docstring "does NOT write" — 0 aktive Befueller |

**D-02 widerlegt:** AccountMemory ist KEIN toter Import — precall_service.py:175 `db.query(AccountMemory).filter(AccountMemory.account_id == ...).order_by(AccountMemory.updated_at.desc())` liefert das PreCall-MEDDPICC-Briefing.

**D-03 widerlegt:** CoachingReport ist NICHT write-only — `get_or_generate_weekly_report` (coaching_service.py:182) liest mit `db.query(CoachingReport).filter_by(user_id=...)` (:195, Read-Through-Cache) und schreibt nur bei Cache-Miss (:351).

## Foundation-Liste fuer Plan 04 (D-05 — Foundation-Code-Register)

| Tabelle | Schema | Foundation-Grund | beschildert in |
|---|---|---|---|
| crm.account_memory | crm | Reserve-lastig, PreCall-Briefing-Quelle; aktiv gelesen, Schreiber G-MEET-Pipeline | Plan 03 |
| training.preference_pairs | training | DPO-Foundation, 0 aktive Befueller (Phase 08.23.2.E befuellt) | Plan 03 |
| tenant_orgs | public | UUID-Tenancy-Root, FK-Aktivierung Phase 08.23.2.F | Plan 02 |
| training.transcript_archive | training | ORM-los (kein Model in models.py) | **Plan 04** (offen) |

## Deviations from Plan

None — plan executed exactly as written. Status-Korrekturen D-02/D-03 waren im Plan vorgesehen, keine ungeplanten Aenderungen.

## Final Checks

- `python -c "import ast; ast.parse(open('database/models.py').read())"` → **exit 0** (AST OK)
- `grep -nE "'schema': '(crm|training)', 'comment':" database/models.py` → **6 Treffer** (5 crm: Account/Contact/AccountMemory/Meeting/UserPreference + 1 training: PreferencePair). Dict bleibt in allen 6 das LETZTE Tuple-Element.
- `grep -c "comment=" database/models.py` → **306**
- `git diff --diff-filter=D HEAD~2 HEAD` → keine Datei-Loeschungen.
- Die 30 Plan-02-Tabellen unangetastet.

## Verifikations-Grenze (Plan 04/05)

Das DB-seitige Ankommen der crm/training-COMMENTs (pg_description in den Schemas crm/training) prueft Plan 04 (include_schemas-autogenerate) + Plan 05 (Guard ueber pg_description aller 3 Schemas). Lokaler Pytest verboten (CLAUDE.md HART: kein Local-Dev).

## Self-Check: PASSED

- database/models.py existiert und wurde editiert (FOUND).
- Commit `4cc5b0d` (Cluster B) FOUND.
- Commit `489e32f` (crm + training) FOUND.
- 6-Treffer-Schema-Dict-grep bestaetigt.
- AST exit 0 bestaetigt.
