---
phase: 08.23.2.SCHILD
plan: 04
status: complete
completed: 2026-06-10
autonomous: false
---

# Plan 04 — Migration 0015 (op.execute COMMENTs) + env.py + Foundation-Register — SUMMARY

## Was geliefert wurde
- `alembic/versions/0015_table_column_comments_schilder.py` — **NUR op.execute COMMENT ON TABLE/COLUMN**, 44 Tabellen + 333 Spalten-COMMENTs, alle 3 Schemas (public/crm/training) inkl. ORM-loser `training.transcript_archive`. `revision='0015'`, `down_revision='0014'`. Reversibel (`downgrade()` → `IS NULL`). KEIN DROP/CREATE/ALTER (verifiziert per grep).
- `alembic/env.py` — `include_schemas=True` + `include_name`-Filter (public/crm/training) in BEIDEN context.configure-Blöcken. **Additiv, nicht-kritisch für op.execute** (nur für künftige autogenerate-Läufe; die 0015-COMMENT-Migration braucht es nicht).
- Foundation-Register-Einträge (D-05) → `08.23.2.SCHILD-FOUNDATION-REGISTER-ENTRIES.md` (Vault nicht im Repo erreichbar → als Deliverable für André, bewusste Deviation gegenüber `files_modified`-Vault-Pfad).
- models.py: +20 Spalten-Schilder (strict-L-04, vorher übersehen) + 12 Spalten auf ≥10 Zeichen verlängert.

## Migrations-Stil & Generierung
- `MIGRATION_STYLE: op.execute` (Plan-01-Key) — hauseigenes Muster, KEIN autogenerate-Server-Round-Trip, kein Split-Brain (Cross-AI-Finding 1).
- COMMENT-Texte = **Single-Source models.py comment=** (per AST-Parse extrahiert, KEIN App-Import — reine statische Analyse). transcript_archive (ORM-los) hand-geschrieben (0008-Schema). Einfach-Quote-Escaping via `_q()` (z.B. `'cold_call'` → `''cold_call''`) zur Apply-Zeit.
- **Coverage-Cross-Check gegen Live-Prod-pg_description** (vor Deploy): GAPS=0, STALE=0, SHORT(<10)=0 → garantierter GREEN-Flip. Der Cross-Check fand: 20 strict-L-04-Spalten ohne comment= (Subagenten hatten Flags/Metadaten zu großzügig als trivial behandelt) + 12 Spalten <10 Zeichen. Beide Klassen in models.py nachgezogen (drift-frei: models.py ↔ DB).

## Production-Deploy (manuell als postgres, kein Restart — reine Metadaten)
André-Go (Option „manuell als postgres"). Pure COMMENT-Migration → kein Service-Restart, kein deploy.sh (dessen flakiges `test_ft_seed`-Gate ist für COMMENT-only irrelevant).
1. `scp 0015 → /opt/nerve/app/alembic/versions/`
2. `sudo -u postgres … alembic upgrade head` → `0014 -> 0015`.
3. Nach Längen-Fix: `alembic downgrade 0014 && alembic upgrade head` (reversibel re-applied).
4. `SELECT version_num FROM alembic_version` → **0015**. ✓

## RED → GREEN Beleg (Test-Qualitäts-Regel)
**RED (Plan 05 / Wave 4, vor Migration):** 44 Tabellen + 317 Spalten schildlos (verbatim in 08.23.2.SCHILD-05-SUMMARY.md).

**GREEN (dieser Plan / Wave 5, nach Migration 0015), Lauf als nerve_app gegen Production:**
```
tests/test_schild_guard.py::test_alle_tabellen_haben_schild PASSED       [ 50%]
tests/test_schild_guard.py::test_nicht_triviale_spalten_haben_schild PASSED [100%]
============================== 2 passed in 0.05s ===============================
```
- Beispiel-Beleg pg_description live: `obj_description('training.transcript_archive')` = „Anonymisierungs-Rohmaterial der DPO-Pipeline …" (ORM-lose Tabelle, nur via Migration beschildert).
- Detektion (RED) → Erfüllung (GREEN) explizit server-beobachtet.

## Persistenz-Schicht
Migration SCHREIBT nur pg_description (Catalog), KEINE Datenzeilen, KEINE Struktur. Migration-Rolle = postgres (crm/training-Schreibrechte; Plan 01 bestätigt).

## Abweichungen
- Foundation-Register: Vault nicht erreichbar → Einträge als Phase-Deliverable (s.o.).
- env.py/models.py NICHT auf Prod gepusht (nur 0015 für den Apply nötig — Migration ist selbst-enthaltend). Landen beim nächsten regulären Code-Deploy; DB hat die Schilder bereits. Kein Runtime-Effekt (COMMENTs).
- deploy.sh „echt deploy-blockend"-Zeile (André Option 2) folgt in Wave 6 (NACH GRÜN, Deadlock-Schutz) — als `sudo -u nerve_app`-Stufe (peer-auth-Refinement aus Plan 05).

## Self-Check: PASSED
Migration nur-COMMENT, reversibel, 0015 live, Guard GRÜN (RED→GREEN belegt), transcript_archive abgedeckt, Cross-Check 0/0/0.
