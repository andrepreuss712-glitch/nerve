---
phase: 08.23.2.SCHILD
plan: 01
status: complete
completed: 2026-06-10
autonomous: false
---

# Plan 01 — Discovery (DB-Rolle + Migrations-Stil) — SUMMARY

## Was geliefert wurde

`08.23.2.SCHILD-DISCOVERY-DECISIONS.md` — die drei offenen Phasen-Unbekannten gegen **Production**
(read-only, zero-persistence) bewiesen + verriegelt für Plan 04/05/06.

## Verriegelte Antworten (Maschinen-Keys)

```
DSN_ENV_VAR: NERVE_SCHILD_TEST_DSN
GUARD_ROLE: nerve_app
MIGRATION_STYLE: op.execute
ALEMBIC_VERSION: 1.18.4
ALEMBIC_HEAD: 0014
```

- **(a) DB-Rolle:** `nerve_app` liest `pg_description` ALLER 3 Schemas (public 38 / crm 5 / training 2 = 45 Tabellen)
  **ohne jeden GRANT** — bewiesen per `SET ROLE nerve_app` + `obj_description` (`PROOF_SCHILD_READ_TEST`, ROLLBACK).
  Catalog-Read hängt nicht an Ownership/USAGE/search_path. **Kein Provisioning nötig.** Guard + inspect.sh nutzen nerve_app.
- **(b) Migrations-Stil:** `op.execute` (Hausmuster aller 14 Migrationen, kein Split-Brain). autogenerate = dokumentierter
  Nicht-empfohlener Fallback. `down_revision = '0014'`. Migration läuft als `postgres`.
- **(c) include_name-Filter:** als 1:1-übernehmbarer Codeblock dokumentiert, als nur-Fallback / nicht-kritisch markiert
  (op.execute braucht kein include_schemas).

## Checkpoint-Entscheidung (André, 2026-06-10)

**Guard-Deploy-Block = Option 2, deadlock-sicher sequenziert.** `deploy.sh` fährt den Test-Gate nur mit
SQLite-in-memory (Z.130-143) → ein pg_description-Guard skippt dort. Beschluss:
1. Plan 05 (Wave 4): Guard + **manuelle** server-seitige RED-Beobachtung (separater pytest-Lauf gegen Prod-DSN, NICHT deploy.sh).
2. Plan 04 (Wave 5): Migration 0015 → GRÜN (manueller Lauf).
3. **NACH GRÜN** (Ende Plan 04 / Plan 06): 1 Zeile in deploy.sh (`export NERVE_SCHILD_TEST_DSN=...`) → ab dann echt deploy-blockend.

`deploy.sh` = bewusste Scope-Erweiterung (nicht in files_modified). Reihenfolge-Invariante: deploy.sh-Edit
NACH GRÜN-Deploy (sonst blockt der RED-Guard genau den setzenden Deploy = Deadlock).

## Belege
- Verbatim psql-Output beider Rollen (TEST A nerve_app / TEST B postgres) im Artefakt.
- `SET ROLE`-Proof-Block (zero-persistence ROLLBACK) im Artefakt.
- `alembic --version` = 1.18.4, `SELECT version_num FROM alembic_version` = 0014.

## Key-Files
- created: `.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-DISCOVERY-DECISIONS.md`

## Abweichungen
- KEIN Code-Edit in dieser Wave (Discovery-only, plan-konform).
- Tooling-Bypass (Multi-Segment-Gotcha): gsd-sdk/gsd-tools nicht genutzt, Pfade hardcoded, STATE/ROADMAP hand-editiert.

## Self-Check: PASSED
- Artefakt > 25 Zeilen, alle Maschinen-Keys vorhanden, Provisioning A/B dokumentiert, Checkpoint approved.
