---
phase: 08.23.2.SCHILD
plan: 04
type: execute
wave: 5
complexity: 🔴
depends_on: [01, 02, 03, 05]
files_modified: [alembic/env.py, alembic/versions/0015_table_column_comments_schilder.py, Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md]
autonomous: false
requirements: []
must_haves:
  truths:
    - "Migration 0015 setzt per op.execute(\"COMMENT ON TABLE/COLUMN ...\") ALLE Schilder fuer public + crm + training (empfohlener Default-Stil, Cross-AI-Finding 1) — keine Struktur-Drops/Creates."
    - "training.transcript_archive (ORM-los) ist KEIN Sonderfall mehr, sondern eine der op.execute-Statements wie jede andere Tabelle."
    - "Nach deploy.sh production tragen ALLE Tabellen + nicht-trivialen Spalten in allen 3 Schemas ihr Schild in pg_description."
    - "Der in Plan 05 (Wave 4) server-seitig ROT beobachtete Guard wird durch DIESE Migration server-seitig GRUEN (RED→GREEN-Uebergang als explizite Acceptance, beide Outputs in SUMMARY)."
    - "Die 4 Foundation-Tabellen (account_memory, preference_pairs, transcript_archive, tenant_orgs) stehen im Foundation-Code-Register (D-05)."
  artifacts:
    - path: "alembic/env.py"
      provides: "include_schemas=True + include_name-Filter (NUR fuer autogenerate-Fallback hilfreich, nicht kritisch fuer die op.execute-Migration) — Entscheidung aus Plan 01"
      contains: "include_schemas"
    - path: "alembic/versions/0015_table_column_comments_schilder.py"
      provides: "COMMENT-Migration via op.execute (empfohlen) fuer alle 3 Schemas inkl. transcript_archive"
      contains: "transcript_archive"
    - path: "Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md"
      provides: "D-05-Eintraege fuer 4 Foundation-Tabellen"
      contains: "transcript_archive"
  key_links:
    - from: "alembic/versions/0015 (down_revision)"
      to: "0014 (juengste Migration)"
      via: "Alembic-Revisions-Kette"
      pattern: "down_revision = '0014'"
    - from: "0015 op.execute transcript_archive"
      to: "pg_description training.transcript_archive"
      via: "COMMENT ON TABLE/COLUMN"
      pattern: "COMMENT ON TABLE training.transcript_archive"
---

> ⚠️ **CROSS-AI-GATE (D-08 / CLAUDE.md GSD-Workflow-Pflichten, 🔴-Phase):** Diese Phase wird JETZT geplant, aber NICHT sofort ausgefuehrt. Der Production-Deploy + Migrations-Apply in diesem Plan (Task 4) laeuft ERST NACHDEM die GANZE Phase Cross-AI-Peer-Review durchlaufen hat (+ ggf. `--reviews`-Replan). Der Migrations-/Deploy-Checkpoint ist auf Cross-AI-Clearance gegated — siehe Terminal-Block in Plan 06. Kein Sprung von Planning-Complete direkt zu `bash deploy.sh production`.

<objective>
Die in Plan 02+03 gesetzten `comment=` in die DB schieben (Alembic-Migration 0015) — per **op.execute (empfohlener Default-Stil, Cross-AI-Finding 1)**, hauseigenes Muster aller 14 Bestands-Migrationen. transcript_archive (ORM-los) ist dabei einfach eine der op.execute-Statements, kein Sonderfall. Foundation-Tabellen ins Register (D-05). Diese Migration (Wave 5) flippt den in Plan 05 (Wave 4) ROT beobachteten Guard auf GRUEN.

Purpose: Ohne Migration bleibt das Schild nur im Code (Drift). op.execute("COMMENT ON TABLE/COLUMN ... IS '...'") ist fuer reines COMMENT-Setzen perfekt geeignet und vermeidet den fehleranfaelligen autogenerate-Server-Round-Trip (push → `alembic revision` auf dem Server → scp zurueck → Server-Temp loeschen) mit Split-Brain-Risiko (Cross-AI-Finding 1, MEDIUM). Der Migrations-Stil kommt verriegelt aus Plan 01 (`MIGRATION_STYLE`, Default = op.execute); autogenerate ist nur dokumentierter, NICHT-empfohlener Fallback.

Output: Migration 0015 (op.execute), env.py-Edit (additiv, nicht-kritisch — nur fuer den autogenerate-Fallback / andere Diff-Laeufe nuetzlich), Foundation-Register-Eintraege. Deploy + Verify auf Production (kein Local-Dev) — GATED auf Cross-AI-Clearance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-CONTEXT.md
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-RESEARCH.md
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-DISCOVERY-DECISIONS.md
@alembic/env.py
@alembic/versions/0008_training_schema_foundation.py
@alembic/versions/0014_crm_user_preferences_rls.py

<interfaces>
<!-- Plan 01 DISCOVERY-DECISIONS.md liefert verriegelt + maschinen-parsebar: (a) `MIGRATION_STYLE` (Default = op.execute, Cross-AI-Finding 1), (b) `ALEMBIC_VERSION: x.y.z` (informativ), (c) include_name-Filter-Codeblock (nur Fallback-relevant), (d) Migration-Rolle=postgres. NICHT neu entscheiden — den Key greppen + uebernehmen. -->
<!-- transcript_archive-Schema (0008:29-40): id BIGSERIAL PK, source_call_hash TEXT, segment_index INT, speaker TEXT CHECK, text TEXT, ts_offset_ms INT, schema_version SMALLINT, archived_at TIMESTAMPTZ. -->
<!-- Revisions-Kette: juengste = 0014 → neue Migration revision='0015', down_revision='0014'. -->
<!-- env.py Z.43-48 (online-Block) + Z.24-31 (offline-Block): context.configure(...) — include_schemas + include_name dort ergaenzen (additiv, nicht-kritisch fuer op.execute). -->
<!-- Migration laeuft auf Production als postgres (Memory project_nerve_prod_deploy). -->
<!-- op.execute-Muster: alle 14 Bestands-Migrationen nutzen es; 0008/0014 als Vorlage. KEIN Server-Round-Trip, KEIN scp, KEIN Split-Brain. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: env.py um include_schemas + Schema-Filter erweitern (Plan-01-Entscheidung uebernehmen — additiv, nicht-kritisch fuer op.execute)</name>
  <read_first>
    - DISCOVERY-DECISIONS.md — `MIGRATION_STYLE:`-Key greppen + include_name-Filter-Codeblock + `ALEMBIC_VERSION:`-Key
    - alembic/env.py Z.22-56 (offline + online context.configure)
    - RESEARCH §1.2 (include_schemas-Nebenwirkung: fremde Schemas filtern)
  </read_first>
  <action>
    HINWEIS (Cross-AI-Finding 1): Bei `MIGRATION_STYLE: op.execute` (Default) ist dieser env.py-Edit fuer die 0015-Migration NICHT strikt noetig — op.execute setzt COMMENTs unabhaengig von autogenerate. Der Edit bleibt trotzdem drin, weil include_schemas/include_name spaetere autogenerate-Laeufe (andere Phasen) korrekt auf public/crm/training begrenzt. Als NICHT-kritisch markieren; falls der Edit lokal Reibung erzeugt, ist er ohne Risiko aufschiebbar (die 0015-Migration funktioniert ohne ihn). Bei `MIGRATION_STYLE: autogenerate` (Fallback) ist er Pflicht.

    In BEIDEN context.configure-Bloecken (run_migrations_offline Z.24-31 + run_migrations_online Z.43-48) ergaenzen:
    - `include_schemas=True`
    - `include_name=<Callback aus DISCOVERY-DECISIONS.md>` der nur schemas in {None,'public','crm','training'} und deren Objekte zulaesst (pg_catalog/information_schema/fremde Schemas raus).
    Punkt-14-Control-Flow: (1) lokaler Kontext = bestehende configure-Args (compare_type, render_as_batch) NICHT entfernen; (2) Skelett = include_schemas/include_name additiv; (3) Cross-File = env.py importiert Base.metadata (Z.7-8) — unveraendert; (4) Edge = render_as_batch=True (SQLite) bleibt.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('alembic/env.py').read())"` lokal erlaubt. Funktional irrelevant fuer die op.execute-Migration; Production-Verify via Task 4.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "include_schemas=True" alembic/env.py` == 2 (offline + online).
    - `grep "include_name" alembic/env.py` zeigt den Filter-Callback in beiden Bloecken.
    - AST-Parse exit 0; compare_type/render_as_batch noch vorhanden.
    - In SUMMARY vermerkt: Edit ist bei op.execute nicht-kritisch, nur fuer kuenftige autogenerate-Laeufe gesetzt.
  </acceptance_criteria>
  <done>autogenerate-Laeufe (kuenftige Phasen) sehen public+crm+training, keine Fremd-Schemas; fuer 0015 (op.execute) nicht-kritisch.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0015 via op.execute (empfohlen) — alle COMMENTs aller 3 Schemas inkl. transcript_archive; manuell auf Nur-COMMENT pruefen</name>
  <read_first>
    - DISCOVERY-DECISIONS.md — `MIGRATION_STYLE:`-Key (Default = op.execute; nur bei explizit gesetztem `autogenerate` den Fallback-Pfad unten nehmen)
    - alembic/versions/0008_training_schema_foundation.py (op.execute-Muster, transcript_archive-Schema)
    - alembic/versions/0014_crm_user_preferences_rls.py (Revisions-Header-Muster, down_revision, op.execute-Muster)
    - database/models.py (die in Plan 02+03 gesetzten comment= — Single-Source der COMMENT-Texte)
  </read_first>
  <action>
    Migration `alembic/versions/0015_table_column_comments_schilder.py` erstellen, revision='0015', down_revision='0014'. Verzweigung rein auf `MIGRATION_STYLE`-Key aus Plan 01.

    --- DEFAULT + EMPFOHLEN: `MIGRATION_STYLE: op.execute` (hauseigenes Muster, KEIN Server-Round-Trip, KEIN Split-Brain — Cross-AI-Finding 1) ---
    Migration von Hand schreiben. Fuer JEDE beschilderte Tabelle/Spalte in ALLEN 3 Schemas (public, crm, training):
      `op.execute("COMMENT ON TABLE <schema>.<t> IS '<schild>'")`
      `op.execute("COMMENT ON COLUMN <schema>.<t>.<col> IS '<schild>'")` (nur nicht-triviale Spalten, L-04-Konvention)
    Die COMMENT-Texte sind EXAKT die models.py-comment-Strings (Single-Source — bei ORM-Tabellen aus den in Plan 02+03 gesetzten comment=). transcript_archive ist dabei KEIN Sonderfall, sondern einfach eine weitere op.execute-Statement-Gruppe (sie hat nur kein ORM-Model, deshalb stammt ihr Text aus dem Plan / 0008-Schema, nicht aus models.py):
      `op.execute("COMMENT ON TABLE training.transcript_archive IS 'Anonymisierungs-Rohmaterial (Pipeline B). Status: Reserve/Foundation (Anonymizer-Worker). Schreibt services/...; liest Phase 08.23.2.E.'")`
      + Spalten-COMMENTs fuer source_call_hash, segment_index, speaker, text, ts_offset_ms, schema_version (nicht-trivial).
    downgrade(): pro gesetztem COMMENT ein `op.execute("COMMENT ON TABLE/COLUMN ... IS NULL")` — reversibel, KEIN Daten-/Strukturverlust (D-04).

    --- FALLBACK (NICHT EMPFOHLEN — nur falls `MIGRATION_STYLE: autogenerate` explizit in Plan 01 gesetzt; Split-Brain-Risiko): ---
    Server-Round-Trip (Production ist TAR-deployed, KEIN git auf dem Server):
    1. env.py-Edit (Task 1) committen + pushen.
    2. `bash deploy.sh production` (bringt env.py auf den Server).
    3. Auf dem Server als postgres autogenerate (gegen die echte DB, KEIN apply):
       `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u postgres bash -c "source venv/bin/activate && alembic revision --autogenerate -m table_column_comments_schilder"'`
    4. Datei vom Server zurueck ins Repo holen (scp) → revision='0015', down_revision='0014' setzen.
    5. KRITISCH (RESEARCH §3 Landmine): die Migration HAENDISCH durchsehen — NUR COMMENT-Statements behalten; jeden DROP/CREATE/ALTER COLUMN TYPE/Index-Diff ENTFERNEN. transcript_archive (ORM-los) per op.execute manuell ergaenzen (autogenerate sieht sie nicht).
    6. SPLIT-BRAIN vermeiden: die auf dem Server generierte temporaere Datei wieder LOESCHEN (`ssh ... 'rm /opt/nerve/app/alembic/versions/<gen>_*.py'`).
    7. 0015 committen + pushen; finaler Apply im deploy-Checkpoint (Task 4).

    IMMER (beide Stile): Umlaute in COMMENT-Strings OK (UTF-8). Migration laeuft als postgres. downgrade() setzt COMMENTs auf NULL (kein DROP).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. Verify auf Production via Task 4 (deploy.sh production Pytest-Gate laeuft den Guard; GREEN nach Apply). `python -c "import ast; ast.parse(open('alembic/versions/0015_table_column_comments_schilder.py').read())"` lokal erlaubt.</automated>
  </verify>
  <acceptance_criteria>
    - Datei 0015_*.py existiert, revision='0015', down_revision='0014'.
    - Default-Pfad (op.execute): Migration besteht NUR aus op.execute("COMMENT ON ...")-Statements (Tabellen + nicht-triviale Spalten, alle 3 Schemas) — kein Server-Round-Trip noetig.
    - `grep "COMMENT ON TABLE training.transcript_archive" alembic/versions/0015_*.py` zeigt 1 Treffer (transcript_archive als regulaere op.execute-Statement, kein Sonderfall).
    - Migration enthaelt KEINEN op.drop_*/op.create_table/op.alter_column-Type-Change (nur COMMENT-Statements) — bestaetigt + in SUMMARY dokumentiert.
    - Bei autogenerate-Fallback (nur falls so verriegelt): die temporaere Server-Migrations-Datei wurde geloescht (kein Split-Brain) — in SUMMARY bestaetigt.
    - downgrade() setzt COMMENTs auf NULL (kein DROP).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Migration bereit (op.execute, kein Round-Trip), die alle Schilder (inkl. transcript_archive) in alle 3 Schemas schiebt, ohne Struktur zu aendern.</done>
</task>

<task type="auto">
  <name>Task 3: Foundation-Code-Register-Eintraege (D-05)</name>
  <read_first>
    - Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md (bestehendes Format)
    - Plan-02-SUMMARY + Plan-03-SUMMARY (Foundation-Markierungen: tenant_orgs, account_memory, preference_pairs, transcript_archive)
    - CONTEXT.md D-05
  </read_first>
  <action>
    Im Foundation-Code-Register je Tabelle einen Eintrag (was / warum stehen geblieben / welche Phase aktiviert es):
    - `crm.account_memory` — MEDDPICC-Memory, gelesen precall_service.py:175, aktiv genutzt (lebt) aber Foundation fuer Account-Intelligence-Ausbau.
    - `training.preference_pairs` — DPO-Trainings-Foundation, leer, befuellt von Phase 08.23.2.E.
    - `training.transcript_archive` — Anonymizer-Rohmaterial, Foundation fuer DPO-Pipeline (Phase E).
    - `tenant_orgs` — UUID-Tenancy-Wurzel, FK-Aktivierung Phase 08.23.2.F.
    Format wie bestehende Register-Eintraege. Umlaute OK (Vault-Doku).
  </action>
  <verify>
    <automated>MISSING — Doku-Edit. Acceptance via grep auf die 4 Tabellennamen im Register.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "account_memory|preference_pairs|transcript_archive|tenant_orgs" "Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md"` zeigt alle 4.
    - Jeder Eintrag nennt aktivierende Phase.
  </acceptance_criteria>
  <done>Foundation-Tabellen dokumentiert (D-05 erfuellt).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>env.py-Edit + Migration 0015 (op.execute) + Foundation-Register. Bereit zum Deploy auf Production.</what-built>
  <how-to-verify>
    ⚠️ **GATE: Dieser Deploy laeuft ERST NACH Cross-AI-Peer-Review der GESAMTEN Phase (D-08).** Wenn die Phase noch NICHT Cross-AI-gecleart ist (+ ggf. `--reviews`-Replan), wird hier NICHT deployed — zuerst der Terminal-Block in Plan 06 (Cross-AI). Erst nach Clearance fuehrt Andre den Production-Deploy aus (kein Local-Dev — der Migrations-Apply braucht die echte DB als postgres):
    1. `git add -A && git commit && git push origin main`
    2. `bash deploy.sh production` — das eingebaute Pytest-Gate laeuft VOR dem Restart; der Schild-Guard (Plan 05) muss nach diesem Migrations-Apply GRUEN sein. Bei rot: kein Restart, alter Code bleibt live.
    3. Alembic-Upgrade auf Production anwenden (als postgres) falls deploy.sh es nicht automatisch tut (Memory project_nerve_prod_deploy: Migrationen ggf. manuell als postgres).
    4. Verifizieren dass 0015 angekommen ist: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` zeigt 0015.
    5. GREEN-FLIP-BELEG (Test-Qualitaets-Regel, RED→GREEN): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && pytest tests/test_schild_guard.py -x -v'` MUSS jetzt GRUEN sein — der verbatim GREEN-Output kommt in die SUMMARY, GEGENUEBER dem RED-Output aus Plan 05 (Detektion → Erfuellung bewiesen).
  </how-to-verify>
  <resume-signal>"deployed + 0015 live + Guard GRUEN (RED→GREEN belegt)" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- Migration 0015 SCHREIBT COMMENTs (pg_description) auf ALLE Tabellen aller 3 Schemas + transcript_archive via op.execute. KEINE Datenzeilen, KEINE Strukturaenderung. Schema-Existenz pro Schema in Plan 01 belegt.

### inspect.sh-Beleg (vor Festlegung, aus Plan 01)
```
# Plan 01 DISCOVERY-DECISIONS.md enthaelt den psql-Beleg welche Tabellen in welchem Schema existieren
# + welche Rolle pg_description aller 3 Schemas sieht + MIGRATION_STYLE-Key (Default op.execute). Dieser Plan konsumiert diese Belege.
```

### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| Tabellen/Spalten-COMMENT (ORM) | models.py comment= → op.execute → 0015 | pg_description public/crm/training | ⬜ Task 2 (nur-COMMENT) + Task 4 Production-Apply + Guard-GREEN |
| transcript_archive-COMMENT (ORM-los) | 0015 op.execute (eine Statement-Gruppe wie jede andere) | pg_description training.transcript_archive | ⬜ Task 4 Guard-GREEN |

### Bei Diskrepanz (Fallback-autogenerate zieht Struktur-Diffs): STOP — Diffs entfernen, nur COMMENT behalten. (Beim op.execute-Default tritt das nicht auf.)

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Migration → Production-DB | Migration laeuft als postgres (privilegiert) gegen alle 3 Schemas. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SCHILD-06 | T (Tampering) | (NUR autogenerate-Fallback) include_schemas-autogenerate zieht ungewollte Struktur-Drops/Creates in 0015 | mitigate | Default = op.execute (hand-geschrieben, NUR COMMENT-Statements → Risiko entfaellt, Cross-AI-Finding 1); bei autogenerate-Fallback: manuelle Durchsicht (nur COMMENT behalten), include_name-Filter, Server-Temp-Datei loeschen (kein Split-Brain); downgrade nur COMMENT→NULL. |
| T-SCHILD-07 | D (DoS) | Migration bricht Deploy wenn als nerve_app statt postgres laeuft (crm/training write-deny) | mitigate | Migration explizit als postgres (Plan 01 bestaetigt + Memory); deploy.sh-Pytest-Gate faengt rot vor Restart. |
</threat_model>

<verification>
- env.py: 2x include_schemas + include_name-Filter (additiv, nicht-kritisch fuer op.execute).
- 0015 existiert, NUR op.execute-COMMENT-Statements, transcript_archive abgedeckt, reversibel; (Fallback) Server-Temp-Datei geloescht.
- Foundation-Register: 4 Tabellen.
- Production: inspect.sh migrations zeigt 0015; Guard GRUEN (RED→GREEN belegt).
</verification>

<success_criteria>
- Alle Schilder (inkl. ORM-lose transcript_archive) in pg_description aller 3 Schemas nach Deploy — via op.execute (empfohlen).
- KEINE Struktur-Aenderung durch die Migration (nur COMMENTs).
- Guard-RED (Plan 05) → Guard-GREEN (dieser Plan) explizit belegt.
- D-05 Foundation-Register erfuellt.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-04-SUMMARY.md` (inkl. Beleg dass 0015 nur COMMENTs enthaelt + Guard-GREEN-Output).
</output>
</content>
