---
phase: 08.23.2.SCHILD
plan: 05
type: execute
wave: 4
complexity: 🔴
depends_on: [01, 02, 03]
files_modified: [tests/test_schild_guard.py, tests/conftest.py]
autonomous: false
requirements: []
must_haves:
  truths:
    - "Ein pytest-Guard FAILT, wenn irgendeine Tabelle/nicht-triviale Spalte in public/crm/training KEIN Schild (>=10 Zeichen) in pg_description hat."
    - "Der Guard prueft DIREKT ueber pg_description auf der DB (alle 3 Schemas), NICHT ueber models.py — faengt so auch die ORM-lose transcript_archive (L-07)."
    - "Der Guard SKIPPT sauber auf SQLite/lokal (kein False-Green, kein Crash) und laeuft nur server-side gegen Postgres."
    - "Der Guard wird VOR der Migration (Plan 04) server-seitig ROT beobachtet (Tabellen noch schildlos) — der verbatim RED-Output ist Pflicht-Beleg in der SUMMARY (Test-Qualitaets-Regel: Runtime-Verhalten bewiesen, nicht nur Negativ-Probe)."
  artifacts:
    - path: "tests/test_schild_guard.py"
      provides: "Deploy-blockender Guard ueber pg_description, 3 Schemas, >=10-Zeichen, kein FK-Text-Match"
      exports: ["test_alle_tabellen_haben_schild", "test_nicht_triviale_spalten_haben_schild"]
    - path: "tests/conftest.py"
      provides: "schild_guard-Postgres-Fixture (DSN aus DISCOVERY-DECISIONS.md, skip ohne DSN)"
      contains: "schild_guard"
  key_links:
    - from: "tests/test_schild_guard.py"
      to: "pg_description (public/crm/training)"
      via: "obj_description/col_description Query ueber die Plan-01-Rolle"
      pattern: "obj_description|col_description"
---

<objective>
Die Drift-Versicherung BAUEN und ihre Greifkraft am Server BEWEISEN — VOR der Migration: ein pytest-Guard der jeden Deploy blockt, der eine Tabelle/Spalte ohne Schild laesst — direkt ueber pg_description auf allen 3 Schemas (L-05/L-07), mit der in Plan 01 bewiesenen DB-Rolle. Dieser Plan laeuft in **Wave 4 VOR der Migration (Plan 04, Wave 5)**, damit der Guard im natuerlichen Fluss server-seitig ROT beobachtet werden kann (Tabellen noch schildlos = Detektion bewiesen). Plan 04 (Migration) flippt ihn danach GRUEN. inspect.sh + CLAUDE.md folgen in Plan 06.

Purpose: Ohne Guard verfaellt die Schild-Disziplin beim ersten Termin-Druck (CONTEXT specifics). Der Guard ist der eigentliche langfristige Wert (Anti-Abrieb). Er prueft pg_description (DB = Wahrheit), NICHT models.py — nur so faengt er die ORM-lose transcript_archive. Er MUSS gegen Postgres laufen + auf SQLite skippen (RESEARCH §1.3: SQLite kennt keine Schemas/COMMENTs → False-Green-Falle). Die DB-Rolle/DSN-ENV-Var ist die in Plan 01 verriegelte Entscheidung (DSN_ENV_VAR/GUARD_ROLE-Key). **Test-Qualitaets-Regel (CLAUDE.md):** der Guard wird NICHT nur "gruen nach Migration" gezeigt, sondern sein RED→GREEN-Uebergang ist eine explizite, server-beobachtete Acceptance — RED hier (Wave 4), GREEN in Plan 04.

Output: Guard-Test + conftest-Fixture. Server-seitig ROT beobachtet (verbatim Output in SUMMARY). inspect.sh schilder + CLAUDE.md Punkt 23 = Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-CONTEXT.md
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-RESEARCH.md
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-DISCOVERY-DECISIONS.md
@tests/conftest.py
@database/db.py

<interfaces>
<!-- Plan 01 DISCOVERY-DECISIONS.md liefert verriegelt den maschinen-parsebaren Key `DSN_ENV_VAR:` + `GUARD_ROLE:` (z.B. NERVE_APP_TEST_DSN + USAGE-Grant ODER eine eigene nerve_schild_guard-DSN). KEIN Hardcode des ENV-Var-Namens — aus dem Key lesen. -->
<!-- conftest.py:111-133 nerve_app_pg_conn-Muster (ENV-DSN, skip ohne DSN, psycopg2). Der Schild-Guard reused/erweitert genau dieses Muster. -->
<!-- L-04 Trivial-Spalten-Konvention im Test abbilden (gleiche Liste wie models.py): id, created_at, updated_at, erstellt_am, aktualisiert_am, *_id, is_*/aktiv, UUID-PK. -->
<!-- KEIN FK-im-Text-Abgleich (L-05). Nur Existenz + >=10 Zeichen. -->
<!-- Test-Qualitaet (CLAUDE.md): Guard ist echter Integration-Test (DB-Query + Assertion auf reale COMMENT-Werte) = KEIN Source-Presence-False-Green. -->
<!-- WAVE-ORDER (Blocker 3): dieser Plan (W4) wird VOR der Migration (Plan 04, W5) gebaut. RED hier, GREEN dort. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pytest-Schild-Guard + Fixture (pg_description, 3 Schemas, >=10 Zeichen, SQLite-Skip)</name>
  <read_first>
    - DISCOVERY-DECISIONS.md — den `DSN_ENV_VAR:`- und `GUARD_ROLE:`-Key lesen (DB-Rolle/DSN + ENV-Var-Name fuer den Guard). KEIN Hardcode — Name aus diesem Artefakt.
    - tests/conftest.py Z.100-133 (nerve_app_pg_conn-Muster: ENV-DSN, skip, psycopg2)
    - RESEARCH §1.3 (SQLite kennt keine Schemas/COMMENTs → Skip-Pflicht)
    - CONTEXT L-04 (Trivial-Spalten-Konvention) + L-05/L-07 (Guard-Logik)
  </read_first>
  <behavior>
    - Test 1 (test_alle_tabellen_haben_schild): Fuer jede regular table (relkind='r') in public+crm+training MUSS obj_description(oid,'pg_class') existieren UND >=10 Zeichen sein. Sonst FAIL mit Liste der schildlosen Tabellen (inkl. transcript_archive-Faehigkeit beweisen — sie hat kein Model, der Guard faengt sie nur ueber pg_description).
    - Test 2 (test_nicht_triviale_spalten_haben_schild): Fuer jede nicht-triviale Spalte (L-04-Konvention im Test, NICHT FK-Text-Match) MUSS col_description(oid,attnum) >=10 Zeichen sein. Sonst FAIL mit (schema.table.column)-Liste.
    - Skip-Verhalten: ohne DSN (lokal/SQLite) → pytest.skip (KEIN SQLite-Fallback, kein Crash). Beweis dass es Postgres ist.
    - <10-Zeichen-Blocker: ein Schild "tbd" (3 Zeichen) MUSS als fehlend zaehlen.
    - RED-Erwartung (Wave 4, VOR Migration): da models.py-comments noch NICHT in der DB sind (Migration = Plan 04/Wave 5), MUSS der Guard server-seitig FAILEN und die schildlosen Tabellen auflisten. Das ist die geplante RED-Beobachtung.
  </behavior>
  <action>
    `tests/test_schild_guard.py` schreiben. Fixture: die in Plan 01 entschiedene Connection in conftest.py ergaenzen (entweder neue `schild_guard_pg_conn`-Fixture mit der in DISCOVERY-DECISIONS.md festgelegten DSN-ENV-Var ODER nerve_app_pg_conn + SET search_path/USAGE — exakt wie Plan 01 `GUARD_ROLE` verriegelt). Den ENV-Var-Namen aus dem `DSN_ENV_VAR:`-Key des Artefakts uebernehmen, NICHT hardcoden. Fixture skippt ohne DSN (Muster conftest:113-118).

    Guard-Query (parametrisiert, SQL-injection-safe): pg_class JOIN pg_namespace, n.nspname IN ('public','crm','training'), c.relkind='r', alembic_version + Postgres-interne Tabellen ausschliessen. obj_description fuer Tabelle, col_description je pg_attribute (attnum>0, NOT attisdropped). Trivial-Spalten via L-04-Konvention im Python-Filter ausnehmen (nicht im SQL FK-raten). Assertion auf reale COMMENT-Werte (Integration-Test, kein Source-Presence). Failure-Message listet die konkreten fehlenden Tabellen/Spalten (damit ein Executor weiss, wo ein Schild fehlt).

    KEIN FK-im-Text-Abgleich (L-05). Guard prueft NUR Existenz + Mindestlaenge.
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.SCHILD): schild-guard (pre-migration, erwartet RED)" && git push origin main && bash deploy.sh production && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && pytest tests/test_schild_guard.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - Server-side (Postgres, DSN aus DISCOVERY-DECISIONS.md gesetzt) VOR Migration: beide Tests FAILEN und listen die schildlosen Tabellen/Spalten → **verbatim RED-pytest-Output in der SUMMARY** als Beleg dass der Guard Detektion leistet (kein False-Green). Dies ist die geplante, gewuenschte Rot-Beobachtung.
    - HINWEIS Deploy-Gate: Falls das deploy.sh-Pytest-Gate test_schild_guard.py mitlaufen laesst, wuerde es den Restart blockieren. Diesen Lauf so fahren dass der Guard fuer die RED-Beobachtung SEPARAT ausgefuehrt wird (oder erst nach Migration ins Gate aufgenommen). In SUMMARY dokumentieren ob/wie der Guard ins deploy.sh-Gate eingehaengt wird (greift voll erst nach Plan 04 GRUEN).
    - Lokal/ohne DSN: beide Tests SKIPPEN (kein SQLite-Fallback, kein Crash).
    - Guard queryt pg_description aller 3 Schemas (grep "crm" + "training" + "public" in der Test-Query).
    - GREEN-Uebergang wird in Plan 04 (nach Migration) verifiziert — hier NUR RED beobachtet.
  </acceptance_criteria>
  <done>Deploy-blockender Guard geschrieben + server-seitig ROT beobachtet (Detektion bewiesen) — bereit fuer den GREEN-Flip durch die Migration (Plan 04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>pytest-Schild-Guard + conftest-Fixture, server-seitig ROT beobachtet (vor Migration). Detektion bewiesen.</what-built>
  <how-to-verify>
    Kein Local-Dev — gegen Production:
    1. Guard ist committed + gepusht + deployed (kein Schild in der DB → Tabellen schildlos).
    2. `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && pytest tests/test_schild_guard.py -x -v'` → MUSS ROT sein und schildlose Tabellen/Spalten listen.
    3. Bestaetigen: der verbatim RED-Output steht in der SUMMARY (Beleg fuer Detektion, nicht nur spaetere Negativ-Probe).
    4. Lokal ohne DSN: `pytest tests/test_schild_guard.py` SKIPPT (kein SQLite-Crash).
  </how-to-verify>
  <resume-signal>"approved — Guard server-seitig ROT, listet schildlose Tabellen, RED-Output in SUMMARY" oder beschreibe Fehler</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- Der Guard LIEST pg_description (Catalog) aller 3 Schemas. KEIN Daten-Schreibpfad. Die DB-Rolle/Sichtbarkeit ist die in Plan 01 gegen Production BEWIESENE Entscheidung (psql-Output + DSN_ENV_VAR/GUARD_ROLE-Key im DISCOVERY-DECISIONS.md).

### inspect.sh-Beleg (aus Plan 01)
```
# DISCOVERY-DECISIONS.md: welche Rolle obj_description/col_description von crm+training sieht (DSN_ENV_VAR/GUARD_ROLE-Key).
# Falls nerve_app crm/training NICHT sieht → Guard nutzt die dort festgelegte privilegierte read-only Rolle/DSN (Provisioning-SQL Option A/B).
```

### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| obj_description/col_description (3 Schemas) | Guard-Query | pg_description (Postgres-Catalog) | ⬜ Task 1 server-side: RED vor Migration (schildlos), GREEN in Plan 04 |

### Bei Diskrepanz (Guard sieht crm/training nicht): STOP — die in Plan 01 entschiedene Rolle/Provisioning nutzen, sonst stille gruene Luecke (L-08).

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Guard-DSN → Postgres | read-only Catalog-Query; privilegierte Rolle nur fuer pg_description-Sicht, keine Datenrechte noetig. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SCHILD-08 | I (Info Disclosure) | Guard nutzt privilegierte Rolle | mitigate | read-only Catalog-Query (pg_description), KEINE Datenzeilen; DSN server-side aus ENV, nie im Repo. |
| T-SCHILD-10 | D (DoS / False-Green) | Guard laeuft versehentlich gegen SQLite → False-Green | mitigate | Fixture skippt ohne Postgres-DSN (kein SQLite-Fallback, RESEARCH §1.3); RED-Beobachtung vor Migration beweist Greifen. |
| T-SCHILD-11 | E (Elevation / stille Luecke) | Guard-Rolle sieht crm/training-pg_description nicht → uebersieht fehlende Schilder | mitigate | Plan-01-bewiesene Rolle (DSN_ENV_VAR/GUARD_ROLE); Test-Query grep public+crm+training; RED-Output muss crm/training-Tabellen listen. |
</threat_model>

<verification>
- Guard server-seitig ROT vor Migration (verbatim Output in SUMMARY), lokal skip.
- Guard queryt pg_description aller 3 Schemas; DSN-ENV-Var aus DISCOVERY-DECISIONS.md (kein Hardcode).
</verification>

<success_criteria>
- Deploy-blockender Guard geschrieben; Detektion server-seitig bewiesen (RED listet schildlose Tabellen aller 3 Schemas).
- Guard ist echter Integration-Test (pg_description-Assertion), kein Source-Presence-False-Green.
- GREEN-Flip ist Plan 04 (nach Migration); inspect.sh + CLAUDE.md sind Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-05-SUMMARY.md` (inkl. verbatim RED-pytest-Output als Detektions-Beleg).
</output>
</content>
