---
phase: 08.23.2.SCHILD
plan: 03
type: execute
wave: 3
complexity: 🔴
depends_on: [02]
files_modified: [database/models.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Jede Tabelle des Verkaeufer-Wissen/Training-Config-Clusters + crm-Schema + training-ORM hat ein comment= in models.py."
    - "AccountMemory traegt Status 'lebt' (D-02, precall_service.py:175 belegt) und coaching_reports 'lebt' (D-03)."
    - "Alle crm/training-Tabellen haben das comment IM bestehenden {'schema': ...}-Dict gemerged (Dict bleibt LETZTES Tuple-Element)."
    - "Nach Plan 03 hat JEDE ORM-Tabelle (alle ~42 Models) ein Tabellen-comment — transcript_archive (ORM-los) folgt in Plan 04."
  artifacts:
    - path: "database/models.py"
      provides: "comment= fuer Cluster B (Verkaeufer-Wissen/Training-Config) + crm (5) + training-ORM (PreferencePair)"
      contains: "comment="
  key_links:
    - from: "models.py crm/training __table_args__ {'schema':...,'comment':...}"
      to: "Plan 04 Migration (include_schemas autogenerate)"
      via: "schema-qualifiziertes comment-Dict"
      pattern: "'schema':.*'comment':"
---

<objective>
Den Schild-Rollout in models.py ABSCHLIESSEN: **Verkaeufer-Wissen/Training-Config-Cluster** (Profile + Skript/FAQ/Opener, PromptVersion gehoerte zu Plan 02 — hier PersonalityType, TrainingScenario, CoachingReport) und das **crm-Schema** (Account, Contact, AccountMemory, Meeting, UserPreference) + **training-ORM** (PreferencePair). Nach diesem Plan tragen ALLE ORM-Models ein Schild; nur transcript_archive (ORM-los) bleibt fuer Plan 04.

Purpose: crm/training sind RLS-isoliert/abgeschottet und Foundation-lastig (D-05). Das comment MUSS hier IN das bestehende {'schema': 'crm'/'training'}-Dict gemerged werden, ohne die schema-Qualifizierung zu brechen (models.py:750-754 erklaert warum die zwingend ist). AccountMemory + coaching_reports sind die zwei bekannten Status-Korrekturen (D-02/D-03) — "lebt", NICHT Zombie.

Output: models.py vollstaendig beschildert (alle ORM-Models). Foundation-Markierungen vorbereitet fuer Plan-04-Register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-CONTEXT.md
@.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-RESEARCH.md
@database/models.py
<!-- §G-Schild-Entwuerfe: Nerve-Vault/03 Planung/TAXO Aufraeum-Inventur (Verstaendnis + Scoring).md Abschnitt G -->

<interfaces>
<!-- crm/training-Merge-Muster (RESEARCH §1.1, models.py:762-767 Vorbild): -->
<!--   __table_args__ = ( UniqueConstraint(...), Index(...), {'schema': 'crm', 'comment': 'Schild: ...'} )  -->
<!--   das schema/comment-Dict MUSS LETZTES Tuple-Element bleiben (nerve_app rolconfig EMPTY → ORM muss schema-qualifizieren). -->
<!-- L-04 Trivial-Spalten: id, created_at, updated_at, erstellt_am, aktualisiert_am, *_id, is_*/aktiv, UUID-PK NICHT kommentieren. -->
<!-- D-02: AccountMemory LEBT — gelesen precall_service.py:175 (db.query(AccountMemory) PreCall-MEDDPICC-Briefing). -->
<!-- D-03: CoachingReport LEBT — Read-Through-Cache get_or_generate_weekly_report (coaching_service.py:198-210), Dashboard dashboard.py:599. -->
<!-- D-05 Foundation: crm.account_memory, training.preference_pairs, tenant_orgs (Plan 02), transcript_archive (Plan 04) → Foundation-Code-Register. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Punkt-20-grep-Bestaetigung der crm/Wissen-Status (D-01/D-02/D-03) + Foundation-Markierung</name>
  <read_first>
    - §G der Aufraeum-Inventur (Status-Kandidaten Cluster B + crm + training)
    - database/models.py Z.786-808 (AccountMemory), 597-611 (CoachingReport), 858-878 (PreferencePair)
    - CONTEXT.md D-02 (AccountMemory LEBT), D-03 (coaching_reports LEBT), D-05 (Foundation-Liste)
  </read_first>
  <action>
    grep-Bestaetigung (lokal git-synced ODER inspect.sh grep) der Status fuer Cluster-B/crm/training-Tabellen:
    - `AccountMemory`: bestaetigen `db.query(AccountMemory)` in services/precall_service.py:175 existiert → Status "lebt (Foundation, crm)". (D-02 — Agent-Fehlbefund "toter Import" widerlegen.)
    - `CoachingReport`: bestaetigen get_or_generate_weekly_report (coaching_service.py:198-210) + dashboard.py:599 → Status "lebt (Read-Through-Wochen-Cache)". (D-03 — Gemini-Fehlbefund "write-only" widerlegen.)
    - `PreferencePair` (training): bestaetigen 0 aktive Befueller im aktuellen Code → "Reserve/Foundation (DPO, Phase 08.23.2.E befuellt)".
    - `Account`/`Contact`/`Meeting`/`UserPreference` (crm): Leser/Schreiber-Pfad belegen (G-MEET-Phase) → "lebt (crm, RLS-isoliert)".
    - `Profile`/`ProfileSkript`/`ProfileFaq`/`ProfileOpener`/`PersonalityType`/`TrainingScenario`: "lebt" belegen (Profil/Training-Pfade).

    Verbindungs-Karte (grep file:line je Tabelle) in SUMMARY. Foundation-Tabellen markieren fuer Plan-04-Register (account_memory, preference_pairs, tenant_orgs schon in Plan 02 markiert, transcript_archive in Plan 04). Behauptung ohne Beleg = BLOCK (Punkt 22).
  </action>
  <verify>
    <automated>MISSING — grep-Verifikation, Beleg = Verbindungs-Karte in SUMMARY. Maschinen-Acceptance via Plan 05 Guard.</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY-Verbindungs-Karte belegt AccountMemory + CoachingReport als "lebt" mit file:line.
    - Jede crm/training/Wissen-Tabelle hat einen grep-belegten Status.
    - Foundation-Tabellen-Liste fuer Plan 04 (D-05) in SUMMARY notiert.
  </acceptance_criteria>
  <done>Alle restlichen Status bewiesen; bekannte Fehlbefunde D-02/D-03 als "lebt" bestaetigt.</done>
</task>

<task type="auto">
  <name>Task 2: comment= fuer Verkaeufer-Wissen/Training-Config-Cluster (Cluster B) setzen</name>
  <read_first>
    - database/models.py Z.135-186 (Profile, ProfileSkript, ProfileFaq, ProfileOpener), 238-263 (TrainingScenario, PersonalityType), 597-611 (CoachingReport)
    - §G-Entwuerfe dieser Tabellen
    - Task-1-Verbindungs-Karte
  </read_first>
  <action>
    Tabellen- + nicht-triviale-Spalten-`comment=` fuer: Profile, ProfileSkript, ProfileFaq, ProfileOpener, TrainingScenario, PersonalityType, CoachingReport.
    Punkt-14-Control-Flow pro Edit (additiv, kein Ueberschreiben). Diese Tabellen sind alle public ohne Schema-Dict → Muster (a)/(b) aus dem interfaces-Block. CoachingReport (Z.597-611) hat KEIN __table_args__ → neues ({'comment': '...'},).
    Status aus Task 1: alle "lebt"; CoachingReport explizit "lebt (Read-Through-Wochen-Cache)" (D-03). Nicht-triviale Spalten kommentieren (z.B. Profile.daten 'JSON-Methodik-Container', ProfileFaq.mode, CoachingReport.report_text/avg_readiness_score). Trivial (L-04) auslassen. Umlaute in Strings OK.
  </action>
  <verify>
    <automated>MISSING — kein Local-Pytest (CLAUDE.md). Verify = Plan 04 Migration + Plan 05 Guard auf Production. `python -c "import ast; ast.parse(open('database/models.py').read())"` lokal erlaubt (reines Parsen).</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('database/models.py').read())"` exit 0.
    - 7 Cluster-B-Tabellen haben Tabellen-comment >=10 Zeichen.
    - CoachingReport-comment enthaelt "lebt" (NICHT write-only).
  </acceptance_criteria>
  <done>Verkaeufer-Wissen/Training-Config-Cluster beschildert.</done>
</task>

<task type="auto">
  <name>Task 3: comment= fuer crm-Schema (5) + training-ORM (PreferencePair) setzen — Merge ins Schema-Dict</name>
  <read_first>
    - database/models.py Z.748-808 (Account, Contact, AccountMemory — crm), 811-848 (Meeting, UserPreference — crm), 851-878 (PreferencePair — training)
    - models.py:750-754 (warum schema-Qualifizierung zwingend: nerve_app rolconfig EMPTY)
    - §G-Entwuerfe + Task-1-Karte
  </read_first>
  <action>
    Tabellen-`comment=` IN das bestehende {'schema': 'crm'/'training'}-Dict mergen (Muster models.py:762-767) fuer: Account, Contact, AccountMemory, Meeting, UserPreference (crm), PreferencePair (training). Resultat z.B.:
      `{'schema': 'crm', 'comment': 'Account-Stammdaten je Tenant. Status: lebt (RLS-isoliert). Schreibt routes/...; liest services/...'}`
    Das Dict MUSS LETZTES Tuple-Element bleiben (Punkt 14 Schicht 4 — sonst SQLAlchemy-Registrierungsfehler + Verlust der Schema-Qualifizierung → autogenerate verloere die crm-Tabellen).

    Status aus Task 1: AccountMemory = "lebt (Foundation, crm; precall_service.py:175)"; PreferencePair = "Reserve/Foundation (DPO, Phase E befuellt)"; Account/Contact/Meeting/UserPreference = "lebt (crm, RLS-isoliert)". UserPreference.auto_save_meeting kommentieren ("DSGVO Opt-in, default OFF, serverseitig gehonort"). AccountMemory.meddpicc ("MEDDPICC-JSONB, 8 ASCII-Keys"). PreferencePair nicht-triviale Spalten (prompt/chosen/rejected/split). Trivial (L-04, inkl. UUID-PK + tenant_id-FK) auslassen.
  </action>
  <verify>
    <automated>MISSING — kein Local-Pytest. Verify = Plan 04 (include_schemas-autogenerate erfasst crm/training-comments) + Plan 05 Guard. AST-Parse lokal erlaubt.</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('database/models.py').read())"` exit 0.
    - `grep -nE "'schema': '(crm|training)', 'comment':" database/models.py` zeigt 6 Treffer (5 crm + 1 training).
    - Kein crm/training-Model verliert seine Schema-Qualifizierung (Dict bleibt letztes Element).
    - AccountMemory-comment enthaelt "lebt".
  </acceptance_criteria>
  <done>ALLE ORM-Models (public+crm+training) beschildert. Nur transcript_archive (ORM-los) offen → Plan 04.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- models.py-DEFINITION editiert (comment-Attribut), kein Daten-Pfad. crm/training-Schema-Existenz gegen Production wurde in Plan 01 belegt (Discovery psql-Output). Das DB-seitige Ankommen der crm/training-COMMENTs prueft Plan 04 (include_schemas-autogenerate) + Plan 05 (Guard ueber pg_description aller 3 Schemas).

### Cross-Layer-Konsistenz-Tabelle
| Daten | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| {'schema','comment'}-Dict | models.py → Base.metadata | SQLAlchemy-Metadata → (Plan 04) pg_description in crm/training | ⬜ Plan 04/05 verifizieren crm/training-COMMENT-Ankunft (genau die L-08-Sichtbarkeitsfrage) |

### Status-Behauptungen
- AccountMemory/CoachingReport-Status "lebt" via Task-1-grep (D-02/D-03) belegt. Behauptung ohne Beleg = BLOCK.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| keine | Schema-Annotation. crm/training sind RLS-isoliert; das comment-Edit beruehrt KEINE RLS-Policy, KEINE schema-Qualifizierung (nur Dict-Erweiterung). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SCHILD-04 | T (Tampering) | crm/training Schema-Dict-Merge bricht Schema-Qualifizierung | mitigate | comment IN bestehendes Dict mergen, Dict bleibt LETZTES Element (Punkt 14 Schicht 4); grep-Check 6 Treffer; Plan 04 autogenerate-Diff manuell pruefen (nur COMMENT, kein DROP/CREATE). |
| T-SCHILD-05 | R (Repudiation) | Fehlbefund-Status (AccountMemory/CoachingReport faelschlich Zombie) | mitigate | D-02/D-03 grep-Bestaetigung in Task 1; Status "lebt" mit file:line. |
</threat_model>

<verification>
- AST-Parse grün.
- 6 schema-qualifizierte comment-Dicts (grep).
- Verbindungs-Karte mit AccountMemory/CoachingReport = "lebt".
</verification>

<success_criteria>
- Cluster B (7) + crm (5) + training-ORM (1) beschildert.
- Alle ORM-Models tragen jetzt ein Schild (transcript_archive folgt Plan 04).
- D-02/D-03 als "lebt" bestaetigt; Foundation-Liste fuer Register notiert.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.SCHILD-tabellen-dokumentations-pflicht/08.23.2.SCHILD-03-SUMMARY.md` (inkl. Verbindungs-Karte + Foundation-Liste fuer Plan 04).
</output>
