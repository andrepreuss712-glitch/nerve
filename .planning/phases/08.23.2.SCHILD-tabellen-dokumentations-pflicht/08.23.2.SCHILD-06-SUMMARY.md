---
phase: 08.23.2.SCHILD
plan: 06
status: complete
completed: 2026-06-10
autonomous: false
---

# Plan 06 — inspect.sh schilder + CLAUDE.md Punkt 23 (+ deploy.sh-Guard-Stufe) — SUMMARY

## Was geliefert wurde
- `scripts/inspect.sh` — neuer `schilder <table>`-Befehl: Tabellen-Schild + Spalten-Schilder (`obj_description`/`col_description`, alle 3 Schemas) + best-effort Migrations-Historie. Hilfe-Eintrag ergänzt.
- `CLAUDE.md` (Root) — **Punkt 23: Tabellen-Dokumentations-Pflicht** (§0.2, 6 Regeln + Workflow-Nachzieh-Satz + Guard + inspect.sh-Werkzeug + op.execute-Stil).
- `deploy.sh` — **Schild-Guard als separate `sudo -u nerve_app`-Stufe** nach dem SQLite-Gate (André Option 2). Bewusste Scope-Erweiterung (deploy.sh nicht in files_modified).

## Connection-Fall (Plan-01-GUARD_ROLE)
**FALL A** — `nerve_app` liest pg_description aller 3 Schemas (Plan 01 bewiesen, kein GRANT). `inspect.sh schilder` nutzt den bestehenden `_run_psql` (sudo -u nerve_app, peer-auth) + `SET search_path TO public, crm, training`. KEIN privilegierter Zweig nötig.

## Migrations-Historie — präziser grep (Cross-AI-Finding 2)
Statt nacktem `grep -l "$tbl"` (Substring-/Kommentar-False-Positives) ein präziser `grep -lE`:
`op.(create_table|alter_table|drop_table|add_column|drop_column)('tbl'` ODER `COMMENT ON TABLE/COLUMN ...tbl` ODER **die schema-qualifizierte Tupel-Form der 0015-Migration** `('schema', 'tbl',`. Letzteres war nötig, weil 0015 die COMMENTs datengetrieben (`op.execute("..." % tuple)`) statt als literale Strings setzt — ein reiner `COMMENT ON TABLE`-grep hätte 0015 verfehlt. `$tbl` ist via `_validate_table`-Whitelist injection-safe. Als best-effort dokumentiert (zeile-basiert; mehrzeilige `op.create_table(\n 'x'` werden nicht erfasst).

## Production-Verifikation (live)
inspect.sh auf den Server gescp't + getestet:
- `inspect.sh schilder conversation_logs` (public) → Tabellen-Schild („RUECKGRAT … Status: lebt (TAXO-Umbau-Zone) …") + Spalten-Schilder; Trivial-Spalten (id/user_id/org_id/profile_id) korrekt OHNE Schild. ✓
- `inspect.sh schilder account_memory` (crm) → crm-Schild („MEDDPICC-Gedaechtnis … D-02 — NICHT toter Import …") + Spalten → **beweist crm-Sichtbarkeit via nerve_app**. ✓
- `inspect.sh schilder calls` → Migrations-Historie listet `0015_table_column_comments_schilder.py` (Tupel-Form-Match, keine Fremd-Treffer). ✓
- `inspect.sh help` listet `schilder`. ✓

## deploy.sh Schild-Guard-Stufe (echt deploy-blockend)
Separate Stufe nach „Tests bestanden": `sudo -u nerve_app … NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve pytest tests/test_schild_guard.py -q`. Bei ROT → `exit 1`, kein Restart. Eingehängt NACH dem GRÜN-Deploy (Migration 0015) = Deadlock-Schutz (sonst hätte der RED-Guard genau den setzenden Deploy blockiert). `deploy.sh` läuft lokal (SSH-Heredoc) → die Stufe ist ab dem nächsten `bash deploy.sh production` aktiv. Identischer Guard-Befehl wurde manuell als nerve_app gefahren → GRÜN (Plan 04). `bash -n deploy.sh` = OK.

> Hinweis: Ein voller `bash deploy.sh production`-Lauf wurde NICHT erzwungen — sein SQLite-Test-Gate hat einen pre-existing `test_ft_seed`-Collection-Fehler (Memory; Fix = Phase 08.23.2.STAGING), der einen Restart blockieren würde, unabhängig von dieser Phase. Die Schild-Guard-Stufe selbst ist validiert (identischer Befehl GRÜN) + syntaktisch korrekt; sie greift beim nächsten regulären Deploy.

## Abweichungen
- deploy.sh = bewusste Scope-Erweiterung (André Option 2), dokumentiert.
- Kein erzwungener Voll-Deploy (pre-existing test_ft_seed-Gate; inspect.sh direkt gescp't + getestet, CLAUDE.md ist Repo-Doc).

## Self-Check: PASSED
schilder-Befehl live (public+crm bewiesen, präzise Historie), Punkt 23 in Root-CLAUDE.md verankert, deploy.sh-Guard-Stufe committet + validiert.
