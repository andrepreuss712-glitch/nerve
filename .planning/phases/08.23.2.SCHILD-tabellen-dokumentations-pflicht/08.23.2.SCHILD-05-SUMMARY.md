---
phase: 08.23.2.SCHILD
plan: 05
status: complete
completed: 2026-06-10
autonomous: false
---

# Plan 05 — Schild-Guard (pg_description, 3 Schemas) — SUMMARY

## Was geliefert wurde
- `tests/test_schild_guard.py` — Deploy-blockender Guard, 2 Tests:
  - `test_alle_tabellen_haben_schild` — jede regular table in public/crm/training braucht `obj_description >= 10` Zeichen.
  - `test_nicht_triviale_spalten_haben_schild` — jede nicht-triviale Spalte (L-04-Filter) braucht `col_description >= 10` Zeichen.
- `tests/conftest.py` — Fixture `schild_guard_pg_conn` (liest `NERVE_SCHILD_TEST_DSN`, skippt ohne DSN → kein SQLite-False-Green).
- Prüft pg_description DIREKT auf der DB (nicht models.py) → fängt auch die ORM-lose `training.transcript_archive`.
- Existenz + Mindestlänge only, KEIN FK-Text-Match (L-05). `alembic_version` ausgenommen.
- **KEIN `pytest.mark.xfail`** — Cross-AI-Finding 3 (xfail) erwogen, bewusst verworfen: expliziter server-beobachteter RED ist die checker-geforderte, stärkere Evidenz; Deploy-Flow via separatem Guard-Lauf gelöst.
- Commit: `765621e`.

## ⚠ AUTH-REFINEMENT (kritisch für Plan 04 deploy.sh-Integration)
Plan 01 sagte `GUARD_ROLE=nerve_app`, `DSN_ENV_VAR=NERVE_SCHILD_TEST_DSN`. Bei der RED-Beobachtung
zeigte sich der genaue Auth-Mechanismus:
- `DATABASE_URL=postgresql://nerve_app@/nerve` — **KEIN Passwort**, Unix-Socket.
- `pg_hba.conf`: `local nerve nerve_app peer` → nerve_app authentifiziert per **PEER** (OS-User muss `nerve_app` sein). `host ... 127.0.0.1 scram-sha-256`, aber nerve_app hat kein Passwort → TCP geht nicht.
- Folge: Der Guard MUSS **als OS-User nerve_app** laufen (`sudo -u nerve_app`), NICHT als root. Lauf als root → `FATAL: Peer authentication failed for user "nerve_app"`.
- **Korrekter Lauf-Befehl (RED hier, GREEN in Plan 04):**
  ```
  ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
    'sudo -u nerve_app bash -c "cd /opt/nerve/app && NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest tests/test_schild_guard.py -v"'
  ```
- **Konsequenz für Plan 04 Schritt 3 (deploy.sh „echt deploy-blockend"):** Der Guard kann NICHT Teil des normalen root-SQLite-`pytest tests/`-Laufs sein (root → Peer-Fail; SQLite → skip). Er braucht eine SEPARATE deploy.sh-Stufe:
  `sudo -u nerve_app bash -c 'cd /opt/nerve/app && NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest tests/test_schild_guard.py -q' || { echo "Schild fehlt — Deploy blockiert"; exit 1; }`
  Diese Stufe wird per André-Entscheidung (Option 2) ERST NACH dem GRÜN-Deploy (Migration 0015) in deploy.sh eingehängt (Deadlock-Schutz). conftest-Fixture-Env-Var-Name bleibt `NERVE_SCHILD_TEST_DSN`.

## Server-seitige RED-Beobachtung (VOR Migration — Detektions-Beweis, echtes FAIL)
Lauf als nerve_app gegen Production-pg_description, 2026-06-10:

```
tests/test_schild_guard.py::test_alle_tabellen_haben_schild FAILED       [ 50%]
tests/test_schild_guard.py::test_nicht_triviale_spalten_haben_schild FAILED [100%]

E   AssertionError: 44 Tabelle(n) ohne Schild (>=10 Zeichen in pg_description):
E       crm.account_memory  (Schild=None)
E       crm.accounts  (Schild=None)
E       crm.contacts  (Schild=None)
E       crm.meetings  (Schild=None)
E       crm.user_preferences  (Schild=None)
E       public.api_cost_log ... public.users ... public.waitlist  (Schild=None)
E       public.conversation_logs  (Schild=None)
E       public.transcript_segments  (Schild=None)
E       training.preference_pairs  (Schild=None)
E       training.transcript_archive  (Schild=None)   <-- ORM-lose Tabelle, NUR via pg_description gefangen (L-07)

E   AssertionError: 317 nicht-triviale Spalte(n) ohne Schild (>=10 Zeichen):
E       crm.account_memory.schema_version  (Schild=None)
E       crm.account_memory.meddpicc  (Schild=None)
E       ... (317 total)
```

- **44 Tabellen** (alle 3 Schemas, inkl. ORM-lose `transcript_archive`) + **317 nicht-triviale Spalten** als schildlos gelistet. Echtes FAIL, KEIN xfail.
- Lokal/ohne DSN: beide Tests SKIPPEN (psycopg2-Fixture, kein SQLite-Fallback) — als root gegen Prod: sauberer `pytest.skip`-Pfad bzw. peer-FATAL ohne sudo (kein False-Green).
- Detektion bewiesen: der Guard greift, listet die konkreten Lücken, und fängt die ORM-lose Tabelle, die ein models.py-basierter Check verfehlt hätte.

## Cross-AI-Finding-3-Vermerk
xfail (Finding 3) erwogen, verworfen — expliziter RED-Beweis ist checker-Vorgabe + Deploy-Flow via separatem (sudo -u nerve_app) Lauf gelöst, nicht via xfail.

## Self-Check: PASSED
Guard geschrieben, server-seitig ROT (echtes FAIL, 44 Tabellen + 317 Spalten gelistet, transcript_archive gefangen), lokal skip. GREEN-Flip = Plan 04 (Migration 0015). Auth-Refinement (peer/sudo) dokumentiert für deploy.sh-Integration.
