---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 06
type: execute
wave: 6
depends_on: [04]
files_modified:
  - services/adoption_runner.py
  - tests/test_adoption_runner.py
  # database/models.py: NOT modified — Schild text remains accurate (see Schild-Determination below)
  # services/slow_lane.py: NOT modified — run_adoption_judge(call, db) signature unchanged
autonomous: true
requirements: []
gap_closure: true
gaps_addressed: [GATE-ADOPTION-PAIRING]
must_haves:
  truths:
    - "run_adoption_judge empfaengt das GANZE anonymisierte Transkript (transcript_segments ts_ms ASC, alle speaker+text+ts_ms) UND die Vorschlags-Liste (interaction_id + suggestion_text; ts_offered NUR als Reihenfolge-Hinweis) — NO mechanical pairing, NO _build_adoption_pairs."
    - "Das LLM selbst urteilt je interaction_id (voll/teilweise/ignoriert + Beleg-Zitat) auf Basis des Gesamt-Transkripts — KEIN BLEU/ROUGE/difflib/Cosine/Wort-Match irgendwo im Code-Pfad."
    - "_build_adoption_pairs existiert NICHT mehr in services/adoption_runner.py (Funktion entfernt)."
    - "tests/test_adoption_runner.py: test_pair_build ist ERSETZT durch test_whole_transcript_and_suggestion_list (beweist dass das LLM ganzes Transkript + Vorschlags-Liste bekommt, kein mechanisches Pairing involviert)."
    - "Alle anderen bestehenden Tests (test_no_word_match, test_bundled_single_call, test_prompt_has_no_outcome, test_write_adoption) bleiben erhalten und bleiben gruenbar."
    - "Outcome (calls.outcome) bleibt physisch aus dem Prompt (T-HT-04-03, grep == 0)."
    - "Adoption-Call bleibt SEPARAT vom Verhaltens-Call (judge_runner.py kennt Vorschlaege nicht)."
    - "following_utterance_ref speichert das LLM-identifizierte Beleg-Zitat (max. 120 Zeichen) — Semantik unveraendert, Inhalt jetzt LLM-bewertet statt mechanisch-gematcht."
  artifacts:
    - path: "services/adoption_runner.py"
      provides: "run_adoption_judge: laedt ganzes Transkript + Vorschlaege, EIN gebuendelter Sonnet-Call, Write per interaction_id. KEINE _build_adoption_pairs."
      contains: "run_adoption_judge"
    - path: "tests/test_adoption_runner.py"
      provides: "test_whole_transcript_and_suggestion_list + 4 bestehende Tests (kein test_pair_build)"
      contains: "test_whole_transcript_and_suggestion_list"
  key_links:
    - from: "services/adoption_runner.py run_adoption_judge"
      to: "transcript_segments (ganzes Transkript ts_ms ASC)"
      via: "DB-Query in run_adoption_judge (NICHT in _build_adoption_pairs)"
      pattern: "transcript_segments.*ts_ms.*asc|order_by.*ts_ms"
    - from: "services/adoption_runner.py run_adoption_judge"
      to: "suggestion_reactions (adoption_value/reaction_class/following_utterance_ref)"
      via: "Write per interaction_id unter Tenant-GUC"
      pattern: "adoption_value"
---

<objective>
GAP-CLOSURE fuer GATE-ADOPTION-PAIRING (RED: tests/test_adoption_runner.py::test_pair_build).

Root cause: `_build_adoption_pairs` in services/adoption_runner.py (Plan-04-Bau, Zeilen ~115-203) kann
`suggestion_reactions.ts_offered` (Wall-Clock-Datetime) NICHT mit `transcript_segments.ts_ms`
(Call-interne Millisekunden) vergleichen. Sein eigener Kommentar (Z.173-177) gesteht:
es faellt fuer JEDEN Vorschlag auf "das erste berater-Segment des GANZEN Calls" zurueck —
jeder Vorschlag bekommt dieselbe (falsche) Folge-Aeusserung. Das ist genau die
Wall-Clock/ts_ms-Anker-Fragilitaet, die fuer den Verhaltens-Judge (Plan 03) bewusst
eliminiert wurde.

Fix (Leitsatz 2 / Punkt 27): die mechanische (suggestion -> following_text) Paaring
AUFGEBEN. Das LLM liest das ganze Transkript und urteilt selbst.

Purpose: test_pair_build => test_whole_transcript_and_suggestion_list; gleichzeitig wird
die adoption_runner.py-Implementierung auf das gleiche Paradigma wie judge_runner.py
(Plan 03) gehoben — konsistent, einfacher, besser.

Output:
- services/adoption_runner.py: _build_adoption_pairs entfernt, run_adoption_judge +
  _build_adoption_prompt auf Ganztransskript-Paradigma umgeschrieben, ADOPTION_TOOL-Schema
  per interaction_id (nicht per Paar).
- tests/test_adoption_runner.py: test_pair_build ersetzt durch test_whole_transcript_and_suggestion_list;
  4 bestehende Tests unveraendert erhalten + gruenbar.

Komplexitaet: SIMPLIFICATION (kein Cross-AI-Review, kein Replan — dies ist eine
VEREINFACHUNG, konsistent mit dem bereits Cross-AI-reviewten Verhaltens-Judge-Paradigma).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-CONTEXT.md
@CLAUDE.md
@services/judge_runner.py
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-04-uebernahme-call-SUMMARY.md

<interfaces>
<!-- Unveraenderte Schnittstellen — der Executor braucht diese nicht zu suchen. -->

<!-- slow_lane.py _adoption_step (UNVERAENDERT): -->
<!-- Z.904: register_call_end_step(_adoption_step) -->
<!-- _adoption_step ruft: run_adoption_judge(call, db); db.commit() -->
<!-- Signatur von run_adoption_judge BLEIBT: run_adoption_judge(call, db) -> dict -->
<!-- slow_lane.py wird NICHT angefasst. -->

<!-- suggestion_reactions-Spalten (aus models.py, Plan 04 Schild-Stand): -->
<!--   adoption_value: Float nullable — Uebernahme-Grad 0-1 -->
<!--   following_utterance_ref: String(128) nullable — Beleg-Zitat (max. 120 Zeichen, DSGVO) -->
<!--   reaction_class: String(24) nullable — voll|teilweise|ignoriert -->
<!--   interaction_id: UUID nullable — Moment-Klammer -->
<!--   suggestion_text: Text nullable — ANGEBOT-Haelfte (FOLD A, bereits befuellt) -->
<!--   ts_offered: DateTime nullable — Angebotszeitpunkt (Wall-Clock, NUR als Reihenfolge-Hinweis) -->
<!--   call_id: UUID FK CASCADE -->

<!-- transcript_segments-Spalten (Plan 04 SUMMARY belegt): -->
<!--   conversation_log_id: FK zu ConversationLog -->
<!--   ts_ms: Integer — Sprech-Zeit in Millisekunden seit Call-Start (Anker, NICHT created_at) -->
<!--   speaker: String — 'berater' | 'kunde' | 'system' -->
<!--   text: Text — anonymisierter Transkript-Text -->

<!-- judge_runner.py Muster (bereits Cross-AI-reviewed, Plan 03): -->
<!-- Laedt ganzes Transkript: -->
<!--   segments = db.query(TranscriptSegment) -->
<!--               .filter(TranscriptSegment.conversation_log_id == call.conversation_log_id) -->
<!--               .order_by(TranscriptSegment.ts_ms.asc()) -->
<!--               .all() -->
<!-- User-Prompt Transkript-Block: -->
<!--   for i, seg in enumerate(segments, start=1): -->
<!--       f'[#{i} {speaker} {ts_ms}ms] {text}' -->

<!-- ADOPTION_TOOL aktuelles Schema (Plan 04, Z.57-110): -->
<!-- input_schema erwartet 'ergebnisse' Array mit {interaction_id, beleg, urteil, adoption_value} -->
<!-- DIESES SCHEMA BLEIBT — nur der Prompt-Bau aendert sich. -->
<!-- Das Tool empfaengt weiterhin ergebnisse[] keyed by interaction_id. -->
<!-- Die Write-Schleife (Z.366-391) bleibt unveraendert — sie iteriert ueber res.interaction_id. -->
</interfaces>
</context>

<schild_determination>
## Punkt 23 Schild-Aktualitaets-Pruefung (Pflicht, Andre-Direktive 2026-06-25)

Die Gap-Fix-Aenderung: _build_adoption_pairs entfernt, run_adoption_judge laedt jetzt
ganzes Transkript intern.

Aktueller Schild-Text (models.py Z.858, bereits von Plan 04 aktualisiert):
  adoption_value: "... Schreibt services/adoption_runner.py (gebuendelter Sonnet-Call am Call-Ende) ..."
  following_utterance_ref: "Verweis/Hash auf die folgende Berater-Aeusserung (Uebernahme-Beleg). Befuellt ab TAXO2 LLM-Uebernahme-Call (services/adoption_runner.py) ..."
  reaction_class: "... Befuellt ab TAXO2 LLM-Uebernahme-Call (services/adoption_runner.py) ..."
  Tabellen-Comment: "... services/adoption_runner.py (Reaktions-Haelfte ... gebuendelter LLM-Uebernahme-Call am Call-Ende) ..."

Beurteilung:
  - Der Schreiber bleibt services/adoption_runner.py. UNVERAENDERT.
  - Der Mechanismus ist "gebuendelter LLM-Uebernahme-Call" — bleibt true, LLM-Call bleibt gebuendelt.
  - Die Aenderung ist INTERN (wie run_adoption_judge seinen Prompt baut, nicht wer schreibt).
  - "Verweis/Hash auf die folgende Berater-Aeusserung" — leicht unpraezise, da jetzt das LLM
    das beleg_zitat aus dem Gesamttranskript extrahiert, aber "Beleg-Zitat" war bereits die
    Absicht. Kein nachweisberg falscher Schild-Teil.

Entscheidung: database/models.py NICHT aendern, KEINE neue Migration.
  Die Schild-Texte bleiben inhaltlich korrekt — wer schreibt, womit (LLM-Call), wo
  (adoption_runner.py) bleibt alles identisch. Die interne Algorithmus-Aenderung ist
  kein Schild-relevantes Ereignis (Punkt 23 gilt fuer Aenderungen am Lese-/Schreib-Pfad,
  nicht fuer interne Prompt-Umstrukturierung).
</schild_determination>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: adoption_runner.py — _build_adoption_pairs entfernen, run_adoption_judge auf Ganztransskript-Paradigma umschreiben</name>
  <files>services/adoption_runner.py</files>

  <read_first>
    - services/adoption_runner.py vollstaendig (aktuelle Implementierung: _build_adoption_pairs Z.115-200,
      _build_adoption_prompt Z.204-286, run_adoption_judge Z.291-403, ADOPTION_TOOL Z.57-110)
    - services/judge_runner.py Z.164-271 (_build_judge_prompt: Ganztransskript-Muster als Vorbild —
      TranscriptSegment-Query ts_ms ASC, f'[#{i} {speaker} {ts_ms}ms] {text}' Formatierung)
  </read_first>

  <behavior>
    - Nach dem Fix: `hasattr(adoption_runner, '_build_adoption_pairs')` == False (Funktion entfernt).
    - Der Prompt (system + user) enthaelt alle Transkript-Segmente als '[#{i} {speaker} {ts_ms}ms] {text}'.
    - Der Prompt enthaelt die Vorschlags-Liste als je eine Zeile 'Vorschlag #{i}: interaction_id={iid} suggestion_text={text}'.
    - ts_offered ist ausschliesslich als Reihenfolge-Hinweis formuliert ('grobe Reihenfolge' / 'rough ordering hint') — NICHT als Join-Key.
    - KEIN outcome-Wert im Prompt (grep == 0, T-HT-04-03).
    - KEIN mechanischer Vergleich importiert (difflib/SequenceMatcher/bleu/rouge/cosine).
    - ADOPTION_TOOL-Schema unveraendert (ergebnisse[] keyed by interaction_id, beleg/urteil/adoption_value).
    - Write-Schleife unveraendert (iteriert ueber ergebnisse[] per interaction_id).
    - following_utterance_ref speichert das LLM-identifizierte beleg_zitat (max. 120 Zeichen, DSGVO).
  </behavior>

  <action>
**Die Aenderung ist eine INTERNE Umstrukturierung von run_adoption_judge + _build_adoption_prompt.
ADOPTION_TOOL, die Write-Schleife, der Fehler-Mantel und run_adoption_judge-Signatur bleiben unveraendert.**

**(A) _build_adoption_pairs ENTFERNEN (Z.115-200 loeschen):**
Die gesamte Funktion `_build_adoption_pairs` faellt weg — sie ist die Quelle des Fehlers
(kann ts_offered-Wall-Clock nicht mit ts_ms vergleichen; faellt auf "erster berater-Satz
des ganzen Calls" zurueck, d.h. JEDER Vorschlag bekommt dieselbe, falsche Folge-Aeusserung).
KEIN Ersatz-Pairing. Das LLM uebernimmt die Zuordnung.

**(B) TranscriptSegment-Import beibehalten** (wird jetzt direkt in run_adoption_judge
verwendet statt in _build_adoption_pairs).
**SuggestionReaction-Import beibehalten** (fuer die Query der Vorschlaege + die Write-Schleife).

**(C) _build_adoption_prompt umschreiben auf Signatur (transcript_segments, suggestions) -> (system_str, user_str):**

System-Prompt (unveraenderte Bewertungs-Prinzipien, kompakt, wie judge_runner.py):
```
== DEINE ROLLE ==
Du beurteilst fuer jeden NERVE-Vorschlag im Transkript, ob der Berater die
STRATEGISCHE INTENTION des Vorschlags aufgegriffen hat. Urteile auf INTENT,
NICHT auf Wort-Ueberlappung: Paraphrase und eigene Formulierung zaehlen als
Uebernahme, wenn die Kernidee aufgegriffen wurde.

== BEWERTUNGS-PRINZIPIEN ==
1. Lese das GANZE Transkript der Reihe nach. Beurteile je Vorschlag was der
   Berater DANACH gesagt hat (nicht nur der naechste Satz — auch spaetere
   Saetze koennen die Intention aufgreifen).
2. voll (1.0): Kernidee klar aufgegriffen — auch bei abweichender Formulierung.
3. teilweise (0.5): Aspekte aufgegriffen, wesentliche Teile der Intention fehlen.
4. ignoriert (0.0): kein erkennbarer Bezug zur Intention; auch wenn der Berater
   nach dem Vorschlag schweigt oder das Gespraech abbricht.
5. Beleg-VOR-Urteil: erst das woertliche Zitat aus dem Transkript, dann das Urteil.
   Kein Urteil ohne Beleg (leeres beleg nur wenn Berater danach gar nicht gesprochen hat).

== WICHTIG ==
Du siehst WEDER das Ergebnis des Gespraechs (kein Abschluss/Ablehnung-Info)
NOCH andere Bewertungen. Beurteile NUR die Uebernahme der Intention.
```

User-Prompt (drei Sektionen, wie judge_runner.py):

Sektion 1 — ganzes Transkript:
```
== TRANSKRIPT (chronologisch, ts_ms ASC) ==
[#1 berater 1200ms] Guten Tag, hier ist Andreas von NERVE.
[#2 kunde 3400ms] Ja, hallo?
...
```
Format: `f'[#{i} {seg.speaker} {seg.ts_ms}ms] {seg.text}'`
Wenn keine Segmente: `(Kein Transkript verfuegbar — kein Urteil moeglich)`.

Sektion 2 — Vorschlags-Liste (interaction_id + suggestion_text + ts_offered als
Reihenfolge-Hinweis, explizit NOT als Anker):
```
== NERVE-VORSCHLAEGE (zu beurteilen) ==
Vorschlag #1: interaction_id=<uuid> | Vorschlag-Text: "Erwaehnen Sie unsere Erfolgsrate." | grobe Reihenfolge: ~10230ms (Hinweis, kein Anker)
Vorschlag #2: interaction_id=<uuid> | Vorschlag-Text: "Fragen Sie nach dem Budget." | grobe Reihenfolge: unbekannt
```
ts_offered-Hinweis: wenn ts_offered gesetzt, eine grobe ms-Schaetzung ausgeben
(z.B. anhand Position im Transkript — wenn nicht bestimmbar: "unbekannt"). Da
ts_offered ein Wall-Clock-Datetime und ts_ms call-interne ms sind, KEINE rechnerische
Umwandlung vornehmen. Einfachste Loesung: ts_offered als lesbares Label `"angeboten gegen Mitte des Calls"` ODER einfach weglassen (NUR interaction_id + suggestion_text). **Entscheidung:** ts_offered WEGLASSEN aus dem Prompt — der Hinweis waere unzuverlaessig
(Wall-Clock vs. ts_ms), und das LLM findet die Stelle selbst im Transkript.
Format reduziert auf:
```
Vorschlag #1: interaction_id=<uuid> | Text: "Erwaehnen Sie unsere Erfolgsrate."
Vorschlag #2: interaction_id=<uuid> | Text: "Fragen Sie nach dem Budget."
```

Sektion 3 — Aufgabe:
```
== AUFGABE ==
Lies das Transkript von oben nach unten. Finde fuer jeden NERVE-Vorschlag,
ob und wie der Berater die strategische Intention danach aufgegriffen hat.
Befuelle das Werkzeug record_adoption mit einem Urteil pro Vorschlag.
Alle Vorschlaege in EINEM Aufruf. Beleg -> Urteil -> adoption_value.
```

**(D) run_adoption_judge Internals umschreiben:**

Altes Muster:
```python
pairs = _build_adoption_pairs(call, db)
if not pairs: return no_suggestions
system_str, user_str = _build_adoption_prompt(pairs)
```

Neues Muster:
```python
# Vorschlaege laden (suggestion_text gesetzt, FOLD A)
suggestions = db.query(SuggestionReaction).filter(
    SuggestionReaction.call_id == call.id,
    SuggestionReaction.suggestion_text.isnot(None),
).all()
if not suggestions:
    return {'status': _STATUS_NO_SUGGESTIONS, 'written': 0}

# Ganzes Transkript laden (ts_ms ASC — Sprech-Zeit, NICHT created_at)
# Punkt 26: nur nach transcript_resolved==True (Gate im _call_end_merge)
segments = db.query(TranscriptSegment).filter(
    TranscriptSegment.conversation_log_id == call.conversation_log_id,
).order_by(TranscriptSegment.ts_ms.asc()).all() if call.conversation_log_id else []

system_str, user_str = _build_adoption_prompt(segments, suggestions)
```

Die Write-Schleife (Z.366-391 des Plans 04, iteriert ueber `ergebnisse[]` per
`interaction_id`) bleibt **wortgleich unveraendert** — sie haengt nur vom LLM-Output
ab, der weiterhin `ergebnisse[]{interaction_id, beleg, urteil, adoption_value}` liefert.

**following_utterance_ref:** speichert `beleg[:_FOLLOWING_REF_MAX]` — unveraendert.
Das LLM extrahiert jetzt das Beleg-Zitat aus dem Gesamttranskript; der Speicher-Weg
ist identisch. Semantik-Notiz in Docstring: "LLM-identifiziertes woertliches Zitat
aus dem Gesamttranskript (max. 120 Zeichen, DSGVO)."

**(E) Docstrings aktualisieren:** _build_adoption_prompt Docstring auf neue Signatur
(transcript_segments, suggestions). run_adoption_judge Docstring: Verweis auf
"_build_adoption_pairs" entfernen, "ganzes Transkript" erwaehnen.

**KEIN anderer Code ausserhalb der beschriebenen Aenderungen.**
  </action>

  <verify>
    <automated>python -m py_compile services/adoption_runner.py && echo "AST-OK"</automated>
    <grep_no_pair_fn>grep -c "def _build_adoption_pairs" services/adoption_runner.py (== 0 — Funktion entfernt)</grep_no_pair_fn>
    <grep_transcript_query>grep -nE "TranscriptSegment|ts_ms.*asc|order_by.*ts_ms" services/adoption_runner.py (Treffer — ganzes Transkript wird in run_adoption_judge geladen)</grep_transcript_query>
    <grep_no_mechanical>grep -nE "difflib|SequenceMatcher|bleu|rouge|cosine|bert_score" services/adoption_runner.py (== 0 — KEIN mechanischer Match)</grep_no_mechanical>
    <grep_no_outcome>grep -nE "calls\.outcome|meeting_booked|no_interest|contract_signed" services/adoption_runner.py (== 0 im Runtime-Pfad — Outcome raus, T-HT-04-03)</grep_no_outcome>
    <grep_tool_choice>grep -nE "tool_choice|tools=" services/adoption_runner.py (Treffer — forced Tool-Use)</grep_tool_choice>
    <grep_write_cols>grep -nE "adoption_value|reaction_class|following_utterance_ref" services/adoption_runner.py (Treffer — Write in DEFERRED-Spalten)</grep_write_cols>
    <grep_run_fn>grep -c "def run_adoption_judge" services/adoption_runner.py (== 1)</grep_run_fn>
    <note>HART Kein-Local-Dev: pytest ist DEFERRED (Deploy-Gate). py_compile = einzige lokale Exec.</note>
  </verify>

  <done>
services/adoption_runner.py: _build_adoption_pairs entfernt; _build_adoption_prompt nimmt
(transcript_segments, suggestions) und baut Prompt mit ganzem Transkript [#{i} speaker ts_ms ms] + Vorschlags-Liste
per interaction_id (ohne Wall-Clock-Anker); run_adoption_judge laedt Transkript + Vorschlaege direkt,
feuert EINEN gebuendelten forced-Tool-Use-Call; Write-Schleife unveraendert; ADOPTION_TOOL-Schema
unveraendert; grep "def _build_adoption_pairs" == 0; py_compile AST-OK.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_adoption_runner.py — test_pair_build durch test_whole_transcript_and_suggestion_list ersetzen; 4 bestehende Tests auf neue Implementierung anpassen</name>
  <files>tests/test_adoption_runner.py</files>

  <read_first>
    - tests/test_adoption_runner.py vollstaendig (aktuelle 5 Tests; insbesondere test_pair_build
      Z.143-214 der durch test_whole_transcript_and_suggestion_list ersetzt wird; und
      test_bundled_single_call Z.221-279 der jetzt _build_adoption_pairs NICHT mehr mockt
      sondern transcript_segments + suggestions mocken muss)
    - services/adoption_runner.py (Task 1 Stand — neue Signatur von _build_adoption_prompt,
      neue Internals von run_adoption_judge)
  </read_first>

  <behavior>
    - test_pair_build EXISTIERT NICHT mehr (entfernt).
    - test_whole_transcript_and_suggestion_list: beweist dass run_adoption_judge:
        (a) das GANZE Transkript (alle segments ts_ms ASC) im Prompt enthaelt,
        (b) die Vorschlags-Liste (interaction_id + suggestion_text) im Prompt enthaelt,
        (c) KEINE `_build_adoption_pairs`-Funktion aufruft (hasattr-Negativguard: acceptable
            per CLAUDE.md Grenzfall, weil "Abwesenheit einer Funktion" anders nicht testbar ist),
        (d) genau 1 LLM-Call feuert.
    - test_no_word_match: unveraendert weiter gueltig (kein mechanisches Modul importiert).
    - test_bundled_single_call: muss angepasst werden — patcht jetzt NICHT mehr
      `_build_adoption_pairs` (existiert nicht mehr), sondern stellt echte
      transcript_segments + suggestions ueber DB-Mock bereit.
    - test_prompt_has_no_outcome: unveraendert (outcome darf nicht im Prompt sein; der Test
      prueft den tatsaechlichen Prompt-Text — bleibt gueltig).
    - test_write_adoption: unveraendert (prueft Write-Schleife auf ORM-Attributen — bleibt
      unveraendert, da Write-Schleife identisch ist; muss jetzt aber segments-Query mocken).
  </behavior>

  <action>
**RED -> GREEN. Erst den neuen Test schreiben, dann sicherstellen dass alle 5 bestehenden
Tests (minus test_pair_build, plus test_whole_transcript_and_suggestion_list) auf die neue
Implementierung passen.**

**(A) test_pair_build LOESCHEN (Z.143-214).**

**(B) test_whole_transcript_and_suggestion_list HINZUFUEGEN (ersetzt test_pair_build):**

```python
def test_whole_transcript_and_suggestion_list(monkeypatch):
    """run_adoption_judge gibt dem LLM das GANZE Transkript (alle segments ts_ms ASC)
    UND die Vorschlags-Liste (interaction_id + suggestion_text).
    Kein mechanisches Pairing (_build_adoption_pairs darf nicht existieren).
    Genau 1 LLM-Call (gebuendelt).
    """
    import services.adoption_runner as ar
    from database.models import SuggestionReaction, TranscriptSegment

    iid1 = str(uuid.uuid4())
    call = _make_call(conversation_log_id=99)

    # Test-Double fuer Vorschlag
    sug1 = _make_suggestion(iid=iid1, text='Erwaehnen Sie die Erfolgsrate.')

    # Test-Double fuer Transkript (3 Segmente, verschiedene speaker)
    seg_kunde = _make_segment(idx=1, speaker='kunde', ts_ms=1000, text='Was kostet das?')
    seg_berater1 = _make_segment(idx=2, speaker='berater', ts_ms=2000, text='Das kostet 500 Euro.')
    seg_berater2 = _make_segment(idx=3, speaker='berater', ts_ms=4000, text='Unsere Erfolgsrate ist 85 Prozent.')
    all_segments = [seg_kunde, seg_berater1, seg_berater2]

    # Mock LLM
    fake_client = MagicMock()
    fake_client.messages.create.return_value = _make_adoption_tool_response([
        {'interaction_id': iid1, 'beleg': 'Unsere Erfolgsrate ist 85 Prozent.', 'urteil': 'voll', 'adoption_value': 1.0}
    ])
    monkeypatch.setattr(ar, 'claude_client', fake_client)

    # Mock DB: SuggestionReaction -> [sug1], TranscriptSegment -> all_segments
    def fake_query(model_class):
        mock_q = MagicMock()
        if model_class is SuggestionReaction:
            # filter(...).all() -> [sug1]
            mock_q.filter.return_value.all.return_value = [sug1]
            # filter(...).first() -> sug1 (fuer die Write-Schleife)
            mock_q.filter.return_value.first.return_value = sug1
        elif model_class is TranscriptSegment:
            # filter(...).order_by(...).all() -> all_segments
            mock_q.filter.return_value.order_by.return_value.all.return_value = all_segments
        return mock_q

    fake_db = MagicMock()
    fake_db.query.side_effect = fake_query

    result = ar.run_adoption_judge(call, fake_db)

    # (a) Genau 1 LLM-Call (gebuendelt, Soll-Verhalten §6 Schaerfung b)
    assert fake_client.messages.create.call_count == 1

    # (b) Prompt enthaelt das ganze Transkript (alle 3 Segmente)
    kwargs = fake_client.messages.create.call_args[1]
    user_str = ' '.join(str(m.get('content', '')) for m in kwargs.get('messages', []))
    full_prompt = kwargs.get('system', '') + ' ' + user_str

    assert 'Was kostet das?' in full_prompt, "Transkript-Segment 1 fehlt im Prompt"
    assert 'Das kostet 500 Euro.' in full_prompt, "Transkript-Segment 2 fehlt im Prompt"
    assert 'Unsere Erfolgsrate ist 85 Prozent.' in full_prompt, "Transkript-Segment 3 fehlt im Prompt"

    # (c) Prompt enthaelt die Vorschlags-Liste (interaction_id + suggestion_text)
    assert iid1 in full_prompt, "interaction_id fehlt im Prompt"
    assert 'Erwaehnen Sie die Erfolgsrate.' in full_prompt, "suggestion_text fehlt im Prompt"

    # (d) _build_adoption_pairs darf NICHT existieren (Negativguard — Funktion ist entfernt)
    # CLAUDE.md Grenzfall: Abwesenheit einer Funktion ist nur ueber hasattr beweisbar.
    assert not hasattr(ar, '_build_adoption_pairs'), (
        "_build_adoption_pairs existiert noch — muss entfernt sein (Wall-Clock/ts_ms-Fragilitaet)"
    )

    # (e) Status OK
    assert result.get('status') == 'adoption_done'
```

**(C) test_bundled_single_call ANPASSEN:**
Der Test patcht bisher `ar._build_adoption_pairs` via monkeypatch — diese Funktion
existiert nicht mehr. Stattdessen: DB-Mock direkt fuer SuggestionReaction-Query (all())
und TranscriptSegment-Query (filter().order_by().all()) einrichten.
Assertion "genau 1 LLM-Call" und "status == adoption_done" bleiben.

**(D) test_prompt_has_no_outcome ANPASSEN:**
Bisher: `monkeypatch.setattr(ar, '_build_adoption_pairs', ...)`. Das entfernen; stattdessen
DB-Mock direkt einrichten (SuggestionReaction.filter.all -> [sug], TranscriptSegment-Query -> []).
Outcomes-Check im full_prompt bleibt unveraendert.

**(E) test_write_adoption ANPASSEN:**
Bisher: `monkeypatch.setattr(ar, '_build_adoption_pairs', ...)`. Das entfernen; stattdessen
DB-Mock fuer beide Queries (SuggestionReaction all + first; TranscriptSegment order_by all).
Write-Assertion auf sug_row.adoption_value/reaction_class/following_utterance_ref bleibt.

**Test-Qualitaets-Regel (CLAUDE.md):**
- test_whole_transcript_and_suggestion_list: Function-Call-Return-Assertion (was landet im Prompt
  + wie viele LLM-Calls) + hasattr-Negativguard (CLAUDE.md Grenzfall-Ausnahme mit Kommentar).
- Kein `inspect.getsource`, kein `open().read()` grep auf Source-Code.
  </action>

  <verify>
    <automated>python -m py_compile tests/test_adoption_runner.py && echo "AST-OK"</automated>
    <grep_no_pair_test>grep -c "def test_pair_build" tests/test_adoption_runner.py (== 0 — entfernt)</grep_no_pair_test>
    <grep_new_test>grep -c "def test_whole_transcript_and_suggestion_list" tests/test_adoption_runner.py (== 1)</grep_new_test>
    <grep_5_tests>grep -cE "^def test_" tests/test_adoption_runner.py (== 5 — gleiche Anzahl wie vorher)</grep_5_tests>
    <grep_no_pair_mock>grep -c "_build_adoption_pairs" tests/test_adoption_runner.py (== 0 — kein Mock mehr auf entfernte Funktion)</grep_no_pair_mock>
    <grep_whole_transcript_assert>grep -nE "full_prompt|all_segments|seg_berater|seg_kunde" tests/test_adoption_runner.py (Treffer im neuen Test)</grep_whole_transcript_assert>
    <note>HART Kein-Local-Dev: pytest DEFERRED (Deploy-Gate). py_compile = einzige lokale Exec.</note>
  </verify>

  <done>
tests/test_adoption_runner.py: test_pair_build entfernt; test_whole_transcript_and_suggestion_list
vorhanden (beweist ganzes Transkript im Prompt + Vorschlags-Liste + kein _build_adoption_pairs +
1 LLM-Call); test_bundled_single_call / test_prompt_has_no_outcome / test_write_adoption angepasst
(kein _build_adoption_pairs-Mock mehr, direkter DB-Mock stattdessen); test_no_word_match unveraendert;
grep "def test_pair_build" == 0; 5 Test-Funktionen; py_compile AST-OK.
  </done>
</task>

</tasks>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (Punkt 21) — Plan 06 Gap-Closure

### Angefasste Tabellen

- `suggestion_reactions` (lesen: suggestion_text/interaction_id; schreiben: adoption_value/reaction_class/following_utterance_ref) — Schema UNVERAENDERT seit Plan 04 (Spalten bereits DEFERRED, jetzt befuellt). DB ist auf 0030.
- `transcript_segments` (lesen: ganzes Transkript ts_ms ASC) — Schema UNVERAENDERT.
- Kein neues Schema, keine neue Migration (0030 ist HEAD).

### inspect.sh-Beleg

Schema wurde in Plan 04 verifiziert (Plan 04 SUMMARY, Acceptance-Tabelle, "schema suggestion_reactions —
adoption_value/reaction_class/following_utterance_ref existieren (nullable, jetzt befuellt)"). Diese
Gap-Closure-Phase aendert KEIN Schema — der Beleg ist von Plan 04 getragen.

HART-Kein-SSH-Regel: Executor darf kein `ssh nerve_vps` waehrend des Plans aufrufen. Schema-Verifikation
gilt als getragen durch Plan-04-Acceptance (statisch verifiziert per SUMMARY Z.76-93). Bei Zweifeln:
`inspect.sh schema suggestion_reactions` im Rahmen des Deploy-Gates pruefen.

### Cross-Layer-Konsistenz-Tabelle (Punkt 26)

| Feld | Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann Leser vorauslaufen? | Verifiziert |
|---|---|---|---|---|---|
| suggestion_text/interaction_id (lesen) | Vorschlags-Liste in run_adoption_judge | DB suggestion_reactions | Call-Ende-Flush (FOLD A, Bestand) | entschaerft: Adoption-Schritt hinter Merge-Gate (transcript_resolved) | ✓ (Plan 04) |
| transcript_segments (lesen, GANZES Transkript) | run_adoption_judge direkt | DB | Batch am Call-Ende | entschaerft: nur nach transcript_resolved==True (gate im _call_end_merge) | ✓ Punkt 26 |
| adoption_value/reaction_class/following_utterance_ref (schreiben) | Write-Schleife UNVERAENDERT | DB suggestion_reactions (DEFERRED-Spalten) | Call-Ende-Schritt unter GUC | nein (Schreiber) | ✓ Schema existiert (0030) |
| calls.outcome | NICHT geladen | DB calls | — | — | ✓ grep == 0 |

### Punkt 26 Async-Naht

Der _adoption_step sitzt im _call_end_merge HINTER dem transcript_resolved-Gate (Plan 03/01).
Das Lesen von transcript_segments (ganzes Transkript) passiert jetzt DIREKT in run_adoption_judge
(nicht mehr vermittelt durch _build_adoption_pairs). Das Gate ist unveraendert: nur nach
transcript_resolved==True, committete Daten. Kein Voraus-Lauf moeglich.

### Punkt 23 Schild

Siehe `<schild_determination>` oben: database/models.py UNVERAENDERT, keine neue Migration.
Die Schild-Texte bleiben inhaltlich korrekt nach der Gap-Closure.
</persistenz_schicht_verifikation>

<threat_model>
## Trust Boundaries (unveraendert von Plan 04)

| Boundary | Description |
|----------|-------------|
| NERVE-Server → Anthropic (Sonnet) | suggestion_text (anonymisiert) + ganzes anonymisiertes Transkript an den LLM (mehr Kontext als vorher, gleiche DSGVO-Schicht). |
| Slow-Lane-Daemon ↔ suggestion_reactions | FORCE-RLS-Write unter M-4-GUC (Bestand). |

## STRIDE Threat Register (Carry-Over aus Plan 04, unveraendert)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HT-04-01 | Tampering/Bias | Self-Enhancement — Claude beurteilt eigene Vorschlaege milde | mitigate (Phase 2 voll) | Getrennt vom Verhaltens-Call. Cross-Family = Phase-2-TICKET. |
| T-HT-04-02 | Info Disclosure (DSGVO) | suggestion_text/ganzes Transkript an Drittland-LLM | mitigate | beide anonymisiert; D-RESIDENZ (Plan 05 checkpoint). Das ganze Transkript verschlechtert DSGVO-Risiko nicht (war bereits anonymisiert). |
| T-HT-04-03 | Info Disclosure | Outcome-Leakage in den Adoption-Prompt | mitigate | Outcome nicht geladen/uebergeben; grep == 0; unveraendert. |
| T-HT-04-04-revised | DoS/Korrektheit | FRUEHERES Risiko: falsche Roh-Paar-Zuordnung (Wall-Clock/ts_ms-Fragilitaet) | **aufgeloest** | _build_adoption_pairs entfernt. Das LLM liest das ganze Transkript — kein mechanischer Anker, keine falsche Zuordnung. Leitsatz 2 / Punkt 27. |
| T-HT-04-05 | Elevation/Cross-Tenant | Adoption-Write fremder Tenant | accept | FORCE-RLS + M-4-GUC (Bestand). |
| T-HT-04-06 | Repudiation/Doku | Schild-Veralterung | accept (kein Update noetig) | Schild-Texte bleiben korrekt (Mechanismus-Aenderung ist intern, Schreiber + Methode identisch). |
| T-HT-06-01 | Korrektheit | LLM-Halluzination: Beleg-Zitat existiert nicht im Transkript | mitigate | Beleg-VOR-Urteil-Prinzip im Prompt. Post-Launch Launch-Huerde: ~10 Bewertungen lesen, Zitat gegen Transkript pruefen (CONTEXT.md Constraints). |
</threat_model>

<verification>
## Gesamtverifikation Gap-Closure Plan 06

### Static Checks (lokal, HART Kein-Local-Dev: py_compile only)

```bash
# Task 1: adoption_runner.py
python -m py_compile services/adoption_runner.py && echo "AST-OK"
grep -c "def _build_adoption_pairs" services/adoption_runner.py          # == 0
grep -c "def run_adoption_judge" services/adoption_runner.py              # == 1
grep -nE "difflib|SequenceMatcher|bleu|rouge|cosine|bert_score" services/adoption_runner.py  # == 0
grep -nE "calls\.outcome|meeting_booked|no_interest" services/adoption_runner.py             # == 0
grep -nE "TranscriptSegment|order_by.*ts_ms|ts_ms.*asc" services/adoption_runner.py          # Treffer
grep -nE "tool_choice|tools=" services/adoption_runner.py                 # Treffer
grep -nE "adoption_value|reaction_class|following_utterance_ref" services/adoption_runner.py # Treffer

# Task 2: test_adoption_runner.py
python -m py_compile tests/test_adoption_runner.py && echo "AST-OK"
grep -c "def test_pair_build" tests/test_adoption_runner.py               # == 0
grep -c "def test_whole_transcript_and_suggestion_list" tests/test_adoption_runner.py  # == 1
grep -cE "^def test_" tests/test_adoption_runner.py                       # == 5
grep -c "_build_adoption_pairs" tests/test_adoption_runner.py             # == 0
```

### Deferred (Deploy-Gate — HART, kein local pytest/SSH)

```
bash deploy.sh production
  → pytest tests/test_adoption_runner.py -x -v (laeuft am Prod-Server im deploy.sh gate)
     GATE-ADOPTION-PAIRING: test_pair_build war RED -> nach Fix REMOVED (entfernt)
     test_whole_transcript_and_suggestion_list: muss GREEN sein
     test_no_word_match: muss GREEN sein (unveraendert)
     test_bundled_single_call: muss GREEN sein (angepasst)
     test_prompt_has_no_outcome: muss GREEN sein (angepasst)
     test_write_adoption: muss GREEN sein (angepasst)
  → Live-Test im Browser mit Test-User (mind. 1 Call mit Vorschlaegen)
  → inspect.sh sample suggestion_reactions (adoption_value/reaction_class/following_utterance_ref
    befuellt nach dem Adoption-Call-Ende-Schritt)
```

### Punkt 21 Reachability

- "Gap geschlossen" (test_pair_build RED entfernt) → test_whole_transcript_and_suggestion_list
  GREEN: Pfad existiert (Task 1 + Task 2). ✓
- "LLM sieht ganzes Transkript" → run_adoption_judge laedt TranscriptSegment ts_ms ASC. ✓
- "NIE mechanisch" → grep difflib/etc == 0; _build_adoption_pairs entfernt. ✓
- "Outcome raus" → grep == 0 (T-HT-04-03). ✓
- "Write in DEFERRED-Spalten" → Write-Schleife unveraendert; Schema existiert (0030). ✓
- "slow_lane.py unveraendert" → run_adoption_judge(call, db)-Signatur identisch. ✓

### Next Up (per Andre Flow — KEIN weiteres Cross-AI-Review)

Diese Gap-Closure ist eine Simplifikation konsistent mit dem bereits Cross-AI-reviewten
Verhaltens-Judge-Paradigma. Kein Review-Schritt noetig.

```
1. Claudian Pre-Execute-Audit (Pruefung des Fix auf Konsistenz)
2. /gsd-execute-phase 08.23.2.TAXO2.HANDLING-TIMING --plan 06
3. bash deploy.sh production  (Code + Gate re-deploy; DB bereits auf 0030, keine Migration)
4. Live-Test (Test-User, Call mit Vorschlaegen, adoption_value nachher befuellt)
```
</verification>

<success_criteria>
- services/adoption_runner.py: _build_adoption_pairs entfernt (grep == 0); run_adoption_judge laedt
  ganzes Transkript (ts_ms ASC) + Vorschlags-Liste; EIN gebuendelter forced-Tool-Use-Sonnet-Call;
  KEIN BLEU/ROUGE/difflib/Cosine (grep == 0); Outcome raus (grep == 0); Write-Schleife unveraendert;
  py_compile AST-OK.
- tests/test_adoption_runner.py: test_pair_build entfernt; test_whole_transcript_and_suggestion_list
  vorhanden (beweist ganzes Transkript im Prompt + Vorschlags-Liste + kein _build_adoption_pairs + 1 LLM-Call);
  alle 4 verbleibenden Tests angepasst; py_compile AST-OK.
- database/models.py: NICHT geaendert (Schild-Texte weiterhin korrekt).
- services/slow_lane.py: NICHT geaendert (Signatur run_adoption_judge(call, db) unveraendert).
- kein new Schema-Migration (DB auf 0030, keine Spalten-Aenderung).
- Deploy-Gate: alle 5 Tests in test_adoption_runner.py GREEN; Live-Test bestaetigt adoption_value
  befuellt nach Call-Ende.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-06-SUMMARY.md`
</output>
