---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - services/adoption_runner.py
  - services/slow_lane.py
  - tests/test_adoption_runner.py
  - database/models.py
  - alembic/versions/0030_suggestion_reactions_adoption_schild.py
must_haves:
  truths:
    - "Ein SEPARATER LLM-Call (Uebernahme/Adoption) beurteilt je interaction_id, ob der Berater die strategische Intention des NERVE-Vorschlags aufgegriffen hat (voll/teilweise/ignoriert) + Beleg — per LLM-Urteil, NIE mechanisch (kein BLEU/ROUGE/Cosine/Wort-Match)."
    - "Der Uebernahme-Call ist VOM Verhaltens-Call getrennt: er kennt die NERVE-Vorschlaege (suggestion_reactions.suggestion_text) UND die folgende Berater-Aeusserung; der Verhaltens-Call (Plan 03) kennt die Vorschlaege NICHT."
    - "Roh-Paar = suggestion_reactions.suggestion_text (das ANGEBOT, schon befuellt) + die folgende Berater-Aeusserung aus transcript_segments (per interaction_id/Sprech-Reihenfolge zusammengefuehrt) — NUR aus committeten Daten (NACH transcript_resolved, Punkt 26)."
    - "Das Ergebnis wird in suggestion_reactions geschrieben (adoption_value/reaction_class/following_utterance_ref — die im Schema bereits angelegten DEFERRED-Spalten, jetzt befuellt) ODER als Zusatz; KEINE neue Tabelle. Outcome NICHT im Adoption-Prompt (Verhalten/Adoption bleiben outcome-blind)."
    - "Der Adoption-Call laeuft NUR async im Slow-Lane-Call-Ende-Schritt (kein LLM Fast-Lane) und NUR nach transcript_resolved==True; gebuendelt pro Call (NICHT pro Moment live, Kosten/Latenz Soll-Verhalten §6 Schaerfung b)."
    - "Das Schild (Punkt 23) der 3 befuellten Spalten + das Tabellen-Schild von suggestion_reactions ist AKTUELL: kein '[DEFERRED] / NICHT in TAXO2' mehr fuer adoption_value/reaction_class/following_utterance_ref — sie sagen jetzt 'befuellt ab TAXO2 LLM-Uebernahme-Call (services/adoption_runner.py)'. Single-Source models.py comment= + DB-Schild (COMMENT-Migration 0030) angeglichen."
  artifacts:
    - path: "services/adoption_runner.py"
      provides: "run_adoption_judge(call, db): Roh-Paare bauen (suggestion_text + folgende Berater-Aeusserung je interaction_id) + EIN gebuendelter Sonnet-Tool-Use-Call (Intention voll/teilweise/ignoriert + Beleg) + Write"
      contains: "run_adoption_judge"
    - path: "services/slow_lane.py"
      provides: "Adoption-Call-Ende-Schritt registriert (separater Schritt nach dem Judge-Schritt, gleiche GUC-Klammer)"
      contains: "run_adoption_judge"
    - path: "tests/test_adoption_runner.py"
      provides: "Tests: getrennt vom Verhaltens-Call; LLM-Urteil (kein Wort-Match); Roh-Paar-Bau aus committeten Daten; gebuendelt; Outcome raus"
      contains: "run_adoption_judge"
    - path: "database/models.py"
      provides: "suggestion_reactions Schild-Aktualisierung (Punkt 23): adoption_value/reaction_class/following_utterance_ref comment= von '[DEFERRED] NICHT in TAXO2' auf 'befuellt ab TAXO2 LLM-Uebernahme-Call' + Tabellen-Schild Schreibt/liest-Liste um adoption_runner ergaenzt"
      contains: "adoption_runner"
    - path: "alembic/versions/0030_suggestion_reactions_adoption_schild.py"
      provides: "COMMENT-Migration: gleicht die DB-Schilder (pg_description) der 3 Spalten + Tabelle an models.py comment= an (Single-Source, da Migration 0019 bereits DB-Schilder mit '[DEFERRED]' gelegt hat)"
      contains: "COMMENT ON COLUMN public.suggestion_reactions.adoption_value"
  key_links:
    - from: "services/slow_lane.py (Call-Ende-Schritt)"
      to: "services/adoption_runner.py run_adoption_judge"
      via: "register_call_end_step (separater Schritt, nach _judge_step, gleiche GUC)"
      pattern: "run_adoption_judge"
    - from: "services/adoption_runner.py"
      to: "suggestion_reactions (adoption_value/reaction_class/following_utterance_ref)"
      via: "Write der Uebernahme-Einstufung in die DEFERRED-Spalten (jetzt befuellt)"
      pattern: "adoption_value"
    - from: "alembic/versions/0030_suggestion_reactions_adoption_schild.py"
      to: "pg_description (suggestion_reactions Spalten + Tabelle)"
      via: "COMMENT ON COLUMN/TABLE — Schild-Aktualisierung (Punkt 23), spiegelt models.py comment="
      pattern: "COMMENT ON COLUMN public.suggestion_reactions.adoption_value"
---

<objective>
Der **separate Uebernahme-Call (Adoption)**: ein eigener LLM-Call beurteilt je Kundenmoment (interaction_id), ob der
Berater die strategische Intention des NERVE-Vorschlags aufgegriffen hat (voll / teilweise / ignoriert) + Beleg —
per **LLM-Urteil auf INTENT, NIE mechanisch** (BLEU/ROUGE/Cosine/Wort-Match scheitern an Paraphrase + am
Stichpunkt-Toggle, Soll-Verhalten §6). GETRENNT vom Verhaltens-Call (Bias-Schutz): der Verhaltens-Call (Plan 03)
kennt die Vorschlaege NICHT, der Adoption-Call kennt sie.

Purpose: Die Uebernahme-Messung (Soll-Verhalten §6 "Vorschlags-Uebernahme messen"). Das Roh-Paar
(suggestion_text + folgende Berater-Aeusserung) liegt teils schon vor (suggestion_reactions FOLD A — das ANGEBOT
ist befuellt); die folgende Berater-Aeusserung kommt aus dem committeten Transkript. Die Einstufung wird in die
bereits angelegten DEFERRED-Spalten geschrieben.

Output: `services/adoption_runner.py` (Roh-Paar-Bau + EIN gebuendelter Sonnet-Tool-Use-Call + Write) + ein
separater registrierter Call-Ende-Schritt in slow_lane.py + die **Schild-Aktualisierung** (Punkt 23) der 3 jetzt
befuellten Spalten + Tabellen-Schild (models.py comment= + COMMENT-Migration 0030) + Tests.

**Komplexitaet: 🔴** — zweiter LLM-Call + Persistenz + DSGVO. Cross-AI Pflicht.
**Leitsatz 2 / Punkt 27:** EIN gebuendelter Call pro Call (NICHT pro Moment live — das spraengte Kosten/Latenz).
Schreiben in vorhandene Spalten, KEINE neue Tabelle, KEIN Embedding-Apparat (LLM-Urteil reicht; Embedding-Vorfilter
= Phase 2).

<deferred_ideas>
## Bewusst NICHT in dieser Phase
- Embedding-Vorsortierung / BERTScore-Sanity-Check → Phase 2 (LLM-Urteil allein reicht zum Start, Leitsatz 2).
- Vier-Felder-Tafel (Uebernahme × Ergebnis), Churn-Frueherkennung, Selbst-Verbesserungs-Maengelliste, Marketing-
  Kennzahl → Post-Launch (Auswertung; jetzt nur die Roh-Einstufung erzeugen + speichern).
- Cross-Family-Zweit-Urteil gegen Self-Enhancement (Claude beurteilt eigene Vorschlaege) → Phase-2-TICKET (notieren).
</deferred_ideas>
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-CONTEXT.md
@CLAUDE.md
@services/judge_runner.py
@services/slow_lane.py
@alembic/versions/0019_suggestion_reactions_table.py

<interfaces>
<!-- suggestion_reactions Ist-Stand (models.py:821-860): das ANGEBOT ist befuellt, die Reaktions-Haelfte ist -->
<!-- DEFERRED (nullable) und wird JETZT befuellt. -->
suggestion_reactions:
  - suggestion_text (Text, anonymisiert) — was NERVE ausgab. BEFUELLT (FOLD A).
  - interaction_id (UUID) — Moment-Klammer (Korrelation zu intent_event.interaction_id). BEFUELLT.
  - call_id, conversation_log_id, slot (A/B), source (auto_variante/manual_button/keyword), model, ts_offered, tenant_id (FORCE-RLS).
  - adoption_value (Float, DEFERRED) — Uebernahme-Grad 0-1. JETZT befuellen.
  - reaction_class (String, DEFERRED) — voll|teilweise|ignoriert. JETZT befuellen.
  - following_utterance_ref (String, DEFERRED) — Verweis auf die folgende Berater-Aeusserung. JETZT befuellen.
suggestion_reactions ist FORCE-RLS tenant_isolation (Write fail-closed bei fehlendem Tenant → unter M-4-GUC, wie rubric_score).

<!-- SCHILD-IST (grep-belegt) — die 3 Spalten + die Tabelle tragen JETZT noch ein veraltetes '[DEFERRED] NICHT in TAXO2'-Schild: -->
<!--   models.py: Z.848 adoption_value, Z.849 following_utterance_ref, Z.850 reaction_class (comment=), Z.859 Tabellen-comment. -->
<!--   DB (pg_description) via Migration 0019: Z.101 adoption_value, Z.102 following_utterance_ref, Z.103 reaction_class, Z.87 Tabelle. -->
<!-- Beide Schicht-Quellen (models.py + 0019) sagen woertlich "[DEFERRED, post-Launch] ... NICHT in TAXO2" — durch DIESEN Plan FALSCH. -->
<!-- Punkt 23 (Aktualitaets-Pflicht): ein veraltetes Schild ist schlimmer als keines; der SCHILD-Guard prueft nur Laenge >=10, NICHT Aktualitaet. -->

transcript_segments: conversation_log_id, ts_ms, speaker (berater/kunde/system), text. Die "folgende Berater-
Aeusserung" = das berater-Segment nach dem Vorschlags-/Moment-Zeitpunkt (Sprech-Reihenfolge ts_ms).

services/judge_runner.py (Plan 03): claude_client-Aufruf-Muster + Tool-Use-Schema-Muster wiederverwenden (gleiche
SDK-Form). MODEL_ADOPTION = ENV-Override, Default Sonnet (config.MODEL_POSTCALL_ANALYSIS).

services/slow_lane.py: register_call_end_step (Plan 03 haengt _judge_step ein; dieser Plan haengt einen ZWEITEN
Schritt _adoption_step ein — laeuft NACH dem Judge-Schritt, in derselben GUC-Klammer/ctx).

alembic/versions/0019_suggestion_reactions_table.py (Schild-Muster + das ALT-Schild, das angeglichen wird):
  Z.87 COMMENT ON TABLE, Z.101-103 COMMENT ON COLUMN adoption_value/following_utterance_ref/reaction_class.
  Migrations-Stil (hauseigen): op.execute("COMMENT ON ... IS '...'") mit verdoppelten einfachen Quotes.
  Migrations-Chain: 0027 (live) -> 0028 (Plan 01) -> 0029 (Plan 02) -> 0030 (DIESER Plan, down_revision '0029').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/adoption_runner.py — Roh-Paare (suggestion_text + folgende Berater-Aeusserung) + EIN gebuendelter Sonnet-Call (Intent-Urteil)</name>
  <files>services/adoption_runner.py, tests/test_adoption_runner.py</files>

  <behavior>
    - Test (kein Wort-Match): adoption_runner enthaelt KEINEN BLEU/ROUGE/Cosine/difflib/SequenceMatcher-Aufruf; die Einstufung kommt aus dem LLM (Tool-Use), nicht aus Textaehnlichkeit.
    - Test (Roh-Paar-Bau): aus suggestion_reactions (suggestion_text + interaction_id) + transcript_segments (folgende berater-Aeusserung) wird je Moment ein Paar gebaut; ein Vorschlag ohne folgende Berater-Aeusserung → Paar mit following=None (Urteil 'ignoriert'/None, kein Crash).
    - Test (gebuendelt): EIN messages.create-Call fuer ALLE Paare eines Calls (nicht ein Call pro Moment) — Monkeypatch-Assertion: genau 1 Aufruf.
    - Test (Outcome raus): der Adoption-Prompt enthaelt KEIN calls.outcome.
    - Test (forced Tool-Use): tool_choice erzwungen; Schema je Paar interaction_id -> beleg(folgende Aeusserung) -> urteil(voll|teilweise|ignoriert) -> adoption_value(0-1).
    - Test (Write): die Einstufung landet in suggestion_reactions.adoption_value/reaction_class/following_utterance_ref der jeweiligen Zeile (per interaction_id), unter Tenant-GUC.
  </behavior>

  <read_first>
    - database/models.py:821-860 (suggestion_reactions voll — die DEFERRED-Spalten adoption_value/reaction_class/following_utterance_ref, die jetzt befuellt werden; FORCE-RLS)
    - services/judge_runner.py (Plan 03 — claude_client-Aufruf + Tool-Use-Schema-Muster wiederverwenden; Outcome-Trennung als Vorbild)
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample suggestion_reactions' (echte Form: ist suggestion_text befuellt? interaction_id gesetzt? — Real-Daten-Validierung Punkt 13: liegt das Roh-Angebot wirklich vor? Wenn KEINE Zeilen → der Adoption-Call hat nichts zu tun = sauberer No-Op, im SUMMARY vermerken)
    - ssh ... 'bash scripts/inspect.sh sample transcript_segments' (folgende berater-Aeusserung per ts_ms)
    - **Context7 (MCP):** Anthropic SDK messages.create tools/tool_choice (gleiche Verifikation wie Plan 03; gegen installierte Version).
  </read_first>

  <action>
**RED → GREEN. Erst die behavior-Tests, dann der Code. Neue Datei services/adoption_runner.py.**

**(A) Roh-Paar-Bau `_build_adoption_pairs(call, db) -> list[dict]`:**
- Lade suggestion_reactions des Calls (call_id == call.id), die ein suggestion_text tragen. Wenn keine → return []
  (No-Op; der Schritt schreibt nichts — kein LLM auf leere Menge).
- Fuer jeden Vorschlag: finde die **folgende Berater-Aeusserung** = das erste berater-Segment in transcript_segments
  (conversation_log_id == call.conversation_log_id) NACH dem Moment des Vorschlags. **Anker:** wenn ts_offered/
  interaction_id eine Sprech-Position zulassen, ts_ms-Reihenfolge nutzen; sonst (Executor's Discretion, am Real-Daten-
  Sample entscheiden) das berater-Segment im selben/folgenden Moment. KEINE Wall-Clock-Bruecke erfinden (gleiche
  Lehre wie Plan 03 retired anchor) — wenn keine saubere Zuordnung moeglich ist, following=None → Urteil bleibt
  konservativ (LLM bekommt "folgende Aeusserung unbekannt"). Roh-Paar = {interaction_id, suggestion_text, following_text}.
- **Punkt 26:** der Bau liest transcript_segments — laeuft NUR nach transcript_resolved==True (der Schritt ist hinter
  dem Merge-Gate, Plan 03). committete Daten.

**(B) Der gebuendelte Call `run_adoption_judge(call, db) -> dict`:**
- pairs = _build_adoption_pairs(call, db). Wenn leer → return {status:'no_suggestions', written:0}.
- EIN `claude_client.messages.create(model=MODEL_ADOPTION, temperature=0, system=..., messages=[...alle Paare...],
  tools=[ADOPTION_TOOL], tool_choice forced)` — gebuendelt (alle Paare in EINEM Call, Soll-Verhalten §6 Schaerfung b:
  NIE pro Moment live, gebuendelt/gesampelt). System-Prompt: "Beurteile pro Paar, ob der Berater die strategische
  INTENTION des Vorschlags aufgegriffen hat (voll/teilweise/ignoriert) — auf INTENT, NICHT auf Wort-Ueberlappung
  (Paraphrase/Stichpunkt-Stil zaehlt als Uebernahme). Erst Beleg (die folgende Aeusserung), dann Urteil." KEIN Outcome.
- ADOPTION_TOOL input_schema: Array von {interaction_id, beleg(following), urteil enum voll|teilweise|ignoriert,
  adoption_value (0|0.5|1 grob)}.
- **Write** je Paar in suggestion_reactions: adoption_value, reaction_class=urteil, following_utterance_ref (kurzer
  Verweis/Hash auf das Segment — KEIN voller Roh-Text doppelt, DSGVO). UPDATE per id/interaction_id unter Tenant-GUC
  (der Schritt laeuft in der M-4-GUC-Klammer, Plan 03/Bestand). return {status:'adoption_done', written:n}.
- **KEIN mechanischer Vergleich** (kein difflib/SequenceMatcher/Cosine/BLEU). LLM-Urteil only (Soll-Verhalten §6).
- **Fehler-Mantel:** LLM-/Parse-Fehler → return {status:'adoption_failed', error:...} (kein Crash; idempotenter Re-Run).
- **Latenz Punkt 25:** async im Slow-Lane-Consumer, post-call. Kein Fast-Lane-LLM.
  </action>

  <verify>
    <automated>python -c "import ast; [ast.parse(open(f,encoding='utf-8').read()) for f in ['services/adoption_runner.py','tests/test_adoption_runner.py']]; print('AST-OK')"</automated>
    <grep>grep -c "def run_adoption_judge" services/adoption_runner.py (== 1)</grep>
    <grep>grep -nE "difflib|SequenceMatcher|bleu|rouge|cosine|bert_score" services/adoption_runner.py (== 0 — KEIN mechanischer Match, Soll-Verhalten §6)</grep>
    <grep>grep -nE "outcome|meeting_booked|no_interest" services/adoption_runner.py (== 0 — Outcome raus)</grep>
    <grep>grep -nE "tool_choice|tools=" services/adoption_runner.py (Treffer — forced Tool-Use)</grep>
    <grep>grep -nE "adoption_value|reaction_class|following_utterance_ref" services/adoption_runner.py (Treffer — Write in die DEFERRED-Spalten)</grep>
    <grep>grep -nE "def test_no_word_match|def test_bundled_single_call|def test_prompt_has_no_outcome|def test_pair_build|def test_write_adoption" tests/test_adoption_runner.py (5 Treffer)</grep>
  </verify>

  <done>
services/adoption_runner.py: run_adoption_judge baut Roh-Paare (suggestion_text + folgende berater-Aeusserung aus
committetem Transkript), feuert EINEN gebuendelten forced-Tool-Use-Sonnet-Call (Intent-Urteil voll/teilweise/ignoriert,
KEIN Wort-Match), schreibt die Einstufung in suggestion_reactions' DEFERRED-Spalten unter Tenant-GUC; Outcome raus.
Tests gruen-faehig (Runtime). AST-OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: slow_lane.py — separaten Adoption-Call-Ende-Schritt registrieren (nach dem Judge-Schritt, gleiche GUC)</name>
  <files>services/slow_lane.py</files>

  <read_first>
    - services/slow_lane.py (Plan-03-Stand: _judge_step + register_call_end_step; die GUC-Klammer im _call_end_merge; run_call_end_steps-Reihenfolge)
    - services/adoption_runner.py (Task 1 — run_adoption_judge(call, db))
  </read_first>

  <action>
**Control-Flow-Audit (Punkt 14) kurz: der Adoption-Schritt reiht sich als ZWEITER register_call_end_step ein —
run_call_end_steps ruft die Schritte in Registrierungs-Reihenfolge (slow_lane.py:103), beide in DERSELBEN
M-4-GUC-Klammer + ctx. all-or-nothing pro Call (idempotente Writes, Re-Run sauber).**

Definiere `_adoption_step(ctx)`: liest call/db aus ctx (wie _judge_step); bei not_gradable_reason (Audio-Gate D-09)
→ skip (kein Adoption-Call auf Muell-Audio, konsistent mit dem Judge-Schritt); sonst
`from services.adoption_runner import run_adoption_judge; run_adoption_judge(ctx['call'], ctx['db']); ctx['db'].commit()`.
Registriere `register_call_end_step(_adoption_step)` NACH `register_call_end_step(_judge_step)` (Reihenfolge:
Verhaltens-Call zuerst, dann Adoption — sie sind unabhaengig, aber stabile Reihenfolge erleichtert das Debugging).
KEINE Aenderung am Merge-Gate (die 4 Fan-In-Bits aus Plan 03 gelten fuer beide Schritte). Bau-Regel 1: nur async.
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('services/slow_lane.py',encoding='utf-8').read()); print('AST-OK')"</automated>
    <grep>grep -c "run_adoption_judge" services/slow_lane.py (>=1)</grep>
    <grep>grep -c "register_call_end_step(_adoption_step)" services/slow_lane.py (== 1)</grep>
    <grep>grep -nE "register_call_end_step\(" services/slow_lane.py (>=2 — _judge_step + _adoption_step)</grep>
  </verify>

  <done>
slow_lane: _adoption_step registriert als zweiter Call-Ende-Schritt (nach dem Judge-Schritt, gleiche GUC/ctx),
ruft run_adoption_judge, skippt bei not_gradable. Nur async. AST-OK.
  </done>
</task>

<task type="auto">
  <name>Task 3: Schild-Aktualisierung (Punkt 23) — 3 Spalten + Tabellen-Schild von '[DEFERRED] NICHT in TAXO2' auf 'befuellt ab TAXO2 LLM-Uebernahme-Call' umstellen (models.py comment= + COMMENT-Migration 0030)</name>
  <files>database/models.py, alembic/versions/0030_suggestion_reactions_adoption_schild.py</files>

  <read_first>
    - database/models.py:847-859 (die 3 DEFERRED-Spalten-comments Z.848 adoption_value / Z.849 following_utterance_ref / Z.850 reaction_class + das Tabellen-comment Z.859 — die JETZT veraltet werden, weil dieser Plan sie befuellt)
    - alembic/versions/0019_suggestion_reactions_table.py:87,101-103 (das ALT-DB-Schild, das angeglichen wird — die wortgleichen '[DEFERRED]'-Strings; PLUS das Migrations-Muster: op.execute("COMMENT ON ... IS '...'"), einfache Quotes verdoppelt, upgrade/downgrade)
    - alembic/versions/0029_rubric_score_observations.py (Plan 02 — Migrations-Chain-HEAD: 0030 traegt down_revision '0029')
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schilder suggestion_reactions' (BELEG: das DB-Schild der 3 Spalten + Tabelle traegt LIVE noch '[DEFERRED] NICHT in TAXO2' — bestaetigt, dass ein DB-Schild existiert und mitgezogen werden MUSS, nicht nur models.py)
  </read_first>

  <action>
**Warum dieser Task (Punkt 23 Aktualitaets-Pflicht):** Task 1 schreibt die 3 Spalten adoption_value/reaction_class/
following_utterance_ref ERSTMALS in TAXO2. Deren Spalten-Schilder + das Tabellen-Schild sagen aber woertlich
"[DEFERRED, post-Launch] ... In TAXO2 NICHT befuellt" — das wird durch Task 1 FALSCH. Ein veraltetes Schild ist
schlimmer als keines (der SCHILD-Guard prueft nur Laenge >=10, NICHT Aktualitaet). Single-Source = models.py
`comment=`; weil Migration 0019 zusaetzlich ein DB-Schild (pg_description) gelegt hat, MUSS eine COMMENT-Migration
das DB-Schild angleichen (sonst driftet models.py vs. DB). KEINE Schema-Aenderung (Spalten existieren) — nur COMMENT.

**(A) database/models.py — die 3 Spalten-comments + das Tabellen-comment umstellen (Z.847-859):**
- `adoption_value` (Z.848): comment= weg von "[DEFERRED, post-Launch] ... In TAXO2 NICHT befuellt (Soll-Verhalten §6)."
  → sinngemaess: `"Uebernahme-Grad 0-1 (voll/teilweise/ignoriert). Befuellt ab TAXO2 LLM-Uebernahme-Call. Schreibt services/adoption_runner.py (gebuendelter Sonnet-Call am Call-Ende); gelesen: Uebernahme-Auswertung post-Launch."`
- `following_utterance_ref` (Z.849): → `"Verweis/Hash auf die folgende Berater-Aeusserung (Uebernahme-Beleg). Befuellt ab TAXO2 LLM-Uebernahme-Call (services/adoption_runner.py); gelesen: Auswertung post-Launch."`
- `reaction_class` (Z.850): → `"Klassifikation der Reaktion (voll|teilweise|ignoriert). Befuellt ab TAXO2 LLM-Uebernahme-Call (services/adoption_runner.py); gelesen: Auswertung post-Launch."`
- Tabellen-comment (Z.859, `__table_args__ {'comment': ...}`): die Stelle "Reaktions-Haelfte (adoption_value/...)
  DEFERRED post-Launch" + "liest Uebernahme-Scoring (Post-Launch)" so umformulieren, dass sie die Reaktions-Haelfte
  als ab TAXO2 befuellt beschreibt und adoption_runner.py als Schreiber nennt — z.B.:
  Status-Teil "Status: lebt (neu, TAXO2 FOLD A)." bleibt; "Schreibt services/suggestion_capture.py (Flush) +
  services/live_session.py (RAM)" wird ergaenzt um " + services/adoption_runner.py (Reaktions-Haelfte adoption_value/
  reaction_class/following_utterance_ref, gebuendelter LLM-Uebernahme-Call am Call-Ende)"; "liest Uebernahme-Scoring
  (Post-Launch)" → "liest Uebernahme-Auswertung (post-Launch)". KEIN "DEFERRED"/"NICHT in TAXO2" mehr fuer diese Spalten.
- Den Section-Kommentar Z.847 "# ── DEFERRED-Reaktions-Spalten (nullable, JETZT NICHT befuellt) ──" entsprechend
  entschaerfen (z.B. "# ── Reaktions-Spalten (nullable; ab TAXO2 vom LLM-Uebernahme-Call befuellt) ──") — Konsistenz,
  damit kein Rest-"NICHT befuellt" stehen bleibt, das der grep-Check faengt.
- Trivial-Spalten-Konvention (Punkt 23 L-04) unveraendert; hier nur die 3 nicht-trivialen Reaktions-Spalten + Tabelle.

**(B) alembic/versions/0030_suggestion_reactions_adoption_schild.py — DB-Schild angleichen (Single-Source-Spiegel):**
- Datei-Header + `revision = '0030'`, `down_revision = '0029'` (Chain: 0027 live → 0028 Plan01 → 0029 Plan02 → 0030).
  **Vor dem Bauen** den HEAD via `inspect.sh migrations` pruefen; wenn Prod-HEAD nicht 0029 ist → down_revision
  anpassen / Reihenfolge klaeren (kein blindes Bauen, wie Plan 01/02).
- `upgrade()`: VIER `op.execute("COMMENT ON COLUMN/TABLE public.suggestion_reactions... IS '...'")` — die NEUEN
  Schild-Texte WORTGLEICH zu den models.py-comment= aus (A) (Single-Source Punkt 23; einfache Quotes verdoppelt,
  hauseigenes Migrations-Muster). 3x COLUMN (adoption_value, following_utterance_ref, reaction_class) + 1x TABLE.
- `downgrade()`: die 4 Schilder zurueck auf die 0019-ALT-Texte (das "[DEFERRED] ... NICHT in TAXO2", wortgleich aus
  0019 Z.87/101-103) — Symmetrie wie das 0027/0028-Downgrade-Muster.
- KEINE Spalten-/Schema-Aenderung (kein ADD/ALTER COLUMN) — nur COMMENT. Idempotenz ist durch COMMENT-Natur gegeben
  (COMMENT ist deklarativ ueberschreibend).
  </action>

  <verify>
    <automated>python -c "import ast; [ast.parse(open(f,encoding='utf-8').read()) for f in ['database/models.py','alembic/versions/0030_suggestion_reactions_adoption_schild.py']]; print('AST-OK')"</automated>
    <grep>grep -c "NICHT in TAXO2\|DEFERRED.*nicht befuellt\|In TAXO2 NICHT befuellt" database/models.py (== 0 — die 3 Reaktions-Spalten + Tabelle tragen kein veraltetes Schild mehr; Acceptance-Kriterium aus dem Warning)</grep>
    <grep>grep -nE "adoption_value|reaction_class|following_utterance_ref" database/models.py (Treffer — comments existieren, jetzt aktuell)</grep>
    <grep>grep -c "adoption_runner" database/models.py (>=1 — Tabellen-/Spalten-Schild nennt den neuen Schreiber)</grep>
    <grep>grep -E "revision = '0030'|down_revision = '0029'" alembic/versions/0030_suggestion_reactions_adoption_schild.py (beide Treffer)</grep>
    <grep>grep -c "COMMENT ON COLUMN public.suggestion_reactions.adoption_value" alembic/versions/0030_suggestion_reactions_adoption_schild.py (>=1)</grep>
    <grep>grep -cE "COMMENT ON (COLUMN|TABLE) public.suggestion_reactions" alembic/versions/0030_suggestion_reactions_adoption_schild.py (>=4 — 3 Spalten + Tabelle im upgrade)</grep>
    <grep>grep -c "adoption_runner" alembic/versions/0030_suggestion_reactions_adoption_schild.py (>=1 — DB-Schild nennt den Schreiber, wortgleich models.py)</grep>
    <note>HART Kein-Local-Dev: SCHILD-Guard + Schild-Aktualitaet werden am Deploy-Gate (pytest gegen nerve_test / inspect.sh schilder) geprueft. AST = Syntax. Single-Source-Wortgleichheit models.py vs. 0030 ist Pflicht (Punkt 23).</note>
  </verify>

  <done>
database/models.py: adoption_value/following_utterance_ref/reaction_class + Tabellen-Schild von suggestion_reactions
sagen kein "[DEFERRED]/NICHT in TAXO2" mehr, sondern "befuellt ab TAXO2 LLM-Uebernahme-Call (services/adoption_runner.py)";
grep "NICHT in TAXO2" == 0. Migration 0030 (down_revision 0029) gleicht die 4 DB-Schilder wortgleich an (Single-Source
Punkt 23), downgrade stellt die 0019-ALT-Texte wieder her, keine Schema-Aenderung. AST-OK.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NERVE-Server → Anthropic (Sonnet) | suggestion_text (anonymisiert) + folgende Berater-Aeusserung (anonymisiert) an den LLM. DSGVO wie Plan 03 (D-RESIDENZ). |
| Slow-Lane-Daemon ↔ suggestion_reactions | FORCE-RLS-Write der Einstufung unter M-4-GUC (Bestand). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HT-04-01 | Tampering/Bias | Self-Enhancement — Claude beurteilt eigene Vorschlaege milde | mitigate (Phase 2 voll) | Bewusst getrennt vom Verhaltens-Call (kein Halo aufs Verhalten). Cross-Family-Zweit-Urteil auf Stichprobe = Phase-2-TICKET (notiert). Jetzt: ehrlich als KI-Einschaetzung, Roh-Einstufung neu-berechenbar (Soll-Verhalten §6 Schaerfung a). |
| T-HT-04-02 | Info Disclosure (DSGVO) | suggestion_text/Transkript an Drittland-LLM | mitigate | beide anonymisiert (suggestion_text am Erfassen, Transkript §1); call_id-FK CASCADE; Residenz = D-RESIDENZ (Plan 05 checkpoint). |
| T-HT-04-03 | Info Disclosure | Outcome-Leakage in den Adoption-Prompt | mitigate | Outcome nicht geladen/uebergeben (grep == 0); Verhalten+Adoption bleiben outcome-blind. |
| T-HT-04-04 | DoS/Korrektheit | falsche Roh-Paar-Zuordnung (folgende Aeusserung) | mitigate | konservativ: keine saubere Zuordnung → following=None → LLM bekommt "unbekannt" statt einer falschen Aeusserung; KEINE Wall-Clock-Bruecke erfunden (Leitsatz 2). Roh-Einstufung post-Launch neu-berechenbar. |
| T-HT-04-05 | Elevation/Cross-Tenant | Adoption-Write fremder Tenant | accept | suggestion_reactions FORCE-RLS + M-4-GUC (Bestand); Schritt laeuft pro Call unter dessen Tenant-GUC. |
| T-HT-04-06 | Repudiation/Doku-Integritaet | veraltetes Schild (Punkt 23) taeuscht falsche Wahrheit vor (Spalten als DEFERRED, obwohl befuellt) | mitigate | Task 3 zieht models.py comment= + DB-Schild (Migration 0030) in DERSELBEN Phase nach; grep "NICHT in TAXO2" == 0; Single-Source-Wortgleichheit. |
</threat_model>

<verification>
## 5. Persistenz-Schicht-Verifikation (Punkt 21) — Welle 4

### Angefasste Tabellen
- `suggestion_reactions` (lesen: suggestion_text/interaction_id/ts_offered; schreiben: adoption_value/reaction_class/
  following_utterance_ref — die DEFERRED-Spalten, jetzt befuellt; FORCE-RLS) + Schild-Aktualisierung (COMMENT, kein Schema)
- `transcript_segments` (lesen: folgende berater-Aeusserung)
- `calls` (lesen: conversation_log_id/call_mode; NICHT outcome)

### inspect.sh-Beleg (read_first)
```
$ ssh ... 'bash scripts/inspect.sh sample suggestion_reactions' → ist suggestion_text befuellt + interaction_id gesetzt?
  (Real-Daten-Validierung Punkt 13: liegt das Roh-Angebot vor? KEINE Zeilen → sauberer No-Op.)
$ ssh ... 'bash scripts/inspect.sh schema suggestion_reactions' → adoption_value/reaction_class/following_utterance_ref existieren (nullable, DEFERRED) — KEINE Schema-Migration noetig (jetzt nur befuellen).
$ ssh ... 'bash scripts/inspect.sh schilder suggestion_reactions' → das DB-Schild der 3 Spalten + Tabelle traegt LIVE noch '[DEFERRED] NICHT in TAXO2' (aus Migration 0019) → MUSS via COMMENT-Migration 0030 angeglichen werden (Task 3); models.py allein reicht NICHT, weil ein DB-Schild existiert.
```

### Cross-Layer-Konsistenz-Tabelle (Punkt 26)
| Feld | Pfad | Schicht | Schreib-Zeitpunkt | Kann Leser vorauslaufen? | Verifiziert |
|---|---|---|---|---|---|
| suggestion_text/interaction_id (lesen) | Roh-Paar-Bau | DB suggestion_reactions | Call-Ende-Flush (FOLD A, Bestand) | entschaerft: Adoption-Schritt hinter Merge-Gate (transcript_resolved) | ✓ |
| folgende berater-Aeusserung (lesen) | transcript_segments | DB | Batch am Call-Ende | entschaerft: nur nach transcript_resolved==True | ✓ |
| adoption_value/reaction_class/following_utterance_ref (schreiben) | Adoption-Write | DB suggestion_reactions (DEFERRED-Spalten) | Call-Ende-Schritt unter GUC | nein (Schreiber) | ✓ Schema existiert (kein Schema-Migrations-Bedarf) |
| Schild dieser 3 Spalten + Tabelle (schreiben) | Schild-Aktualisierung (Task 3) | pg_description (DB) + models.py comment= | Migration 0030 (COMMENT, deploy-time) | nein | ✓ Single-Source models.py = 0030 (Punkt 23) |
| outcome | NICHT gelesen | DB calls | — | — | ✓ Leakage-Schutz |

### Punkt 26
Der Adoption-Schritt sitzt im _call_end_merge HINTER dem transcript_resolved-Gate (Plan 03) → liest committete
suggestion_reactions + committetes Transkript. Kein Voraus-Lauf.

### Punkt 23 — Schild-Aktualitaet (neu in dieser Revision)
Task 1 befuellt 3 bisher als DEFERRED beschilderte Spalten. Task 3 zieht die Schilder (models.py comment= +
DB-Schild via COMMENT-Migration 0030) in DERSELBEN Phase nach — weil Migration 0019 sowohl models.py- als auch
DB-Schilder mit "[DEFERRED] NICHT in TAXO2" gelegt hat, sind BEIDE Schichten anzugleichen (Single-Source-Wortgleichheit).
Acceptance: grep "NICHT in TAXO2" in models.py fuer diese 3 Spalten == 0.

## Latenz (Punkt 25)
Gebuendelter Call (EIN messages.create pro Call, NICHT pro Moment), async im Consumer. Kein Fast-Lane-LLM.
Task 3 (Schild) ist deploy-time COMMENT — kein Laufzeit-/Latenz-Effekt.

## Goal-Backward Reachability
- "Uebernahme gemessen" → Roh-Paar (suggestion_text + folgende Aeusserung) → LLM-Intent-Urteil → Write in
  suggestion_reactions. Pfad existiert (Task 1+2). ✓ (Bei 0 Vorschlaegen: sauberer No-Op — kein Pflicht-Output, der
  Wert entsteht sobald echte Vorschlaege erfasst werden.)
- "getrennt vom Verhaltens-Call, Bias-Schutz" → eigener Runner, eigener Schritt; Verhaltens-Call kennt keine
  Vorschlaege (Plan 03 grep == 0). ✓
- "NIE mechanisch" → kein difflib/Cosine/BLEU (grep == 0); LLM-Urteil only. ✓
- "Schild aktuell (Punkt 23)" → models.py comment= + Migration 0030 angeglichen; grep "NICHT in TAXO2" == 0. ✓ (Task 3)
</verification>

<success_criteria>
- services/adoption_runner.py: separater LLM-Call, Intent-Urteil (voll/teilweise/ignoriert + Beleg), NIE mechanisch
  (kein BLEU/ROUGE/Cosine/difflib), gebuendelt pro Call (1 messages.create), Outcome raus, Write in
  suggestion_reactions' DEFERRED-Spalten unter GUC.
- slow_lane: _adoption_step als zweiter Call-Ende-Schritt registriert (nach Judge, gleiche GUC), nur async.
- Schild-Aktualitaet (Punkt 23): die 3 jetzt befuellten Spalten + das Tabellen-Schild sagen kein "[DEFERRED]/NICHT in
  TAXO2" mehr (models.py comment= + DB-Schild via Migration 0030 angeglichen, Single-Source-wortgleich); grep
  "NICHT in TAXO2" == 0; das Schild nennt services/adoption_runner.py als Schreiber.
- Getrennt vom Verhaltens-Call (Bias-Schutz); Roh-Paar nur aus committeten Daten (Punkt 26); kein neuer SCHEMA-
  Migrations-Bedarf (DEFERRED-Spalten existieren; Migration 0030 ist reine COMMENT-Angleichung).
- Leitsatz 2 / Punkt 27: EIN gebuendelter Call, vorhandene Spalten, kein Embedding-Apparat (Phase 2).
- Cross-Family/Vier-Felder-Tafel/Embedding = deferred, notiert.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-04-SUMMARY.md`
</output>
