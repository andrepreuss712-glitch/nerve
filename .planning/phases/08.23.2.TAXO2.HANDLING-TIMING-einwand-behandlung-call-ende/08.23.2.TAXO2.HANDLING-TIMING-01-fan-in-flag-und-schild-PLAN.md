---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0028_calls_transcript_resolved.py
  - database/models.py
autonomous: true
requirements: [GOAL-Anstoss, RESEARCH-FanIn]
must_haves:
  truths:
    - "Die Spalte calls.transcript_resolved existiert als BOOLEAN NOT NULL DEFAULT FALSE (Migration 0028, down_revision 0027)."
    - "Das ORM-Modell Call traegt transcript_resolved mit server_default text('false') + Schild >=10 Zeichen, das den Zweck 'Bewerter-Anstoss-Signal' nennt."
    - "Das calls-TABELLEN-Schild nennt den neuen Schreiber (api_beenden transcript_resolved) + Leser (slow_lane Judge-Anstoss-Gate)."
  artifacts:
    - path: "alembic/versions/0028_calls_transcript_resolved.py"
      provides: "Migration: ADD COLUMN transcript_resolved + COMMENT-Schilder (idempotent, down_revision 0027)"
      contains: "transcript_resolved"
    - path: "database/models.py"
      provides: "Call.transcript_resolved-Spalte + erweitertes calls-Tabellen-Schild"
      contains: "transcript_resolved"
  key_links:
    - from: "alembic/versions/0028_calls_transcript_resolved.py"
      to: "database/models.py Call.transcript_resolved"
      via: "Single-Source: COMMENT-Migration == models.py comment="
      pattern: "transcript_resolved"
---

<objective>
Foundation-Welle des LLM-Gesamtbewerters: die Fan-In-Bereitschafts-Naht `calls.transcript_resolved`
als ECHTE DB-Spalte anlegen (Migration 0028 + ORM), exakt nach dem erprobten Muster von
`audio_health_resolved` (Migration 0027, live).

**Neu gerahmt (Rescope 2026-06-28):** Dieses Flag ist der **Bewerter-Anstoss** — "Transkript am
Call-Ende fertig (oder bewiesen leer) → der LLM-Verhaltens-Call darf angestossen werden". Es ist der
EINZIGE Ueberrest des alten HANDLING-TIMING-Scopes (die maschinische Anker-Mechanik ist RETIRED,
Soll-Verhalten §6). `audio_health_resolved` (0027) bleibt das ZWEITE Anstoss-Signal (Audio-Gate).

Purpose: Plan 03 (der Judge-Anstoss im Slow-Lane-Call-Ende-Schritt) braucht ein zuverlaessiges
"Transkript ist da"-Signal, BEVOR es das Transkript an Sonnet gibt — sonst bewertet der Judge gegen
ein noch nicht geschriebenes Transkript (Punkt 26, dieselbe Klasse wie der Audio-Race). Diese Welle
legt NUR die durable Spalte + Schilder — KEINE Anstoss-Logik (die kommt in Plan 03). Saubere
Datei-Trennung: Plan 01 fasst NUR Migration + models.py an.

Output: Migration `0028_calls_transcript_resolved.py` (down_revision '0027') + `Call.transcript_resolved`
im ORM + erweitertes calls-Tabellen-Schild.

**Komplexitaet: 🔴** (Teil der LLM-Bewertungs-Pipeline + Persistenz/Race — Cross-AI Pflicht VOR Execute
fuer die Gesamt-Phase; diese Welle ist die mechanische Foundation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-CONTEXT.md
@CLAUDE.md
@alembic/versions/0027_calls_audio_health_resolved.py

<interfaces>
<!-- Das exakte 0027-Muster, das diese Welle spiegelt (audio_health_resolved → transcript_resolved). -->
<!-- Executor uebernimmt: revision/down_revision-Stil, idempotentes ADD COLUMN IF NOT EXISTS, -->
<!-- COMMENT ON COLUMN + COMMENT ON TABLE (Single-Source mit models.py), downgrade mit Schild-Reset. -->

Bestehende Call-Spalte (das Vorbild), database/models.py:703-711:
```python
audio_health_resolved = Column(
    Boolean, nullable=False, server_default=text('false'),
    comment="Fan-In-Join-Flag (TAXO2-04 Audio-Race-Fix): TRUE sobald der async Audio-Zustand "
            "endgueltig festgeschrieben ist ... Schreibt routes/app_routes.py (api_beenden / "
            "_audio_health_bg); liest services/slow_lane.py (Merge-Gate).",
)
```

Das aktuelle calls-TABELLEN-Schild, database/models.py:744 (im __table_args__):
```python
{'comment': 'Zentraler Call-Datensatz der neuen Architektur (UUID-PK, Outcome/Coaching/Transkript-Storage). Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt/liest services/+routes/ der neuen Call-Pipeline; audio_health_resolved geschrieben routes/app_routes.py (api_beenden/_audio_health_bg), gelesen services/slow_lane.py (Call-Ende-Merge-Gate, TAXO2-04 Audio-Race-Fix).'},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0028 — calls.transcript_resolved (Bewerter-Anstoss-Fan-In) + calls-Tabellen-Schild</name>
  <files>alembic/versions/0028_calls_transcript_resolved.py</files>

  <read_first>
    - alembic/versions/0027_calls_audio_health_resolved.py (das EXAKTE Muster: Datei-Header-Docstring, revision/down_revision, idempotentes op.execute, COMMENT ON COLUMN + COMMENT ON TABLE, downgrade mit Schild-Reset)
    - database/models.py:744 (calls-Tabellen-Schild Ist-Stand — muss in COMMENT ON TABLE wortgleich erweitert werden)
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations' → MUSS 0027 zeigen (Prod-HEAD; down_revision='0027'). CONTEXT belegt HEAD live = 0027. Wenn der HEAD NICHT 0027 ist → STOP, down_revision anpassen (kein blindes Bauen).
  </read_first>

  <action>
Erzeuge `alembic/versions/0028_calls_transcript_resolved.py` strikt nach dem 0027-Muster. HART (CLAUDE.md):
KEIN lokales alembic, KEIN lokales Ausfuehren — nur die Datei schreiben. Die Migration laeuft am Deploy-Gate
als postgres VOR Restart (create_all-Race, wie 0020-0027).

`revision = '0028'`, `down_revision = '0027'`, branch_labels/depends_on None.

Datei-Header-Docstring (analog 0027): erklaere knapp den Zweck — Fan-In-Anstoss-Signal fuer den
LLM-Verhaltens-Call: TRUE sobald das Transkript am Call-Ende festgeschrieben ist (oder bewiesen leer);
der Judge-Anstoss (slow_lane Call-Ende-Schritt) wartet darauf, BEVOR er das Transkript an Sonnet gibt
(verhindert die Race, in der gegen ein noch nicht geschriebenes Transkript bewertet wird, Punkt 26 —
dieselbe Klasse wie der Audio-Race 0027). Nenne den create_all-Falle-Hinweis + Prod-HEAD 0027.

`upgrade()` — zwei `op.execute(...)`-Bloecke:
1. **Spalte (idempotent, hauseigenes IF-NOT-EXISTS-Muster):**
   `ALTER TABLE public.calls ADD COLUMN IF NOT EXISTS transcript_resolved BOOLEAN NOT NULL DEFAULT FALSE`
   (NOT NULL DEFAULT FALSE deckt das Backfill auf Bestands-Zeilen ab).
2. **SCHILD COMMENT ON COLUMN** public.calls.transcript_resolved — wortgleich zum models.py-comment= aus Task 2
   (Single-Source-Pflicht Punkt 23). Inhalt (>=10 Zeichen, §0.2-Form Zweck+Status+Schreibt/liest):
   "Fan-In-Anstoss-Signal fuer den LLM-Verhaltens-Bewerter (Beobachtung statt Note): TRUE sobald das
   Transkript am Call-Ende festgeschrieben ist (Segmente geschrieben ODER bewiesen leer = resolved-als-absent).
   Der Judge-Anstoss (slow_lane Call-Ende-Schritt) wartet darauf, BEVOR er das Transkript an Sonnet gibt —
   verhindert die Race, in der gegen ein noch nicht geschriebenes Transkript bewertet wird (Punkt 26, analog
   audio_health_resolved). Status: lebt (TAXO2 LLM-Bewerter). Schreibt routes/app_routes.py (api_beenden);
   liest services/slow_lane.py (Judge-Anstoss-Gate + Merge-Gate)."
3. **calls-TABELLEN-Schild erweitern COMMENT ON TABLE** public.calls — den Ist-Text (models.py:744) uebernehmen
   und um den neuen Schreiber/Leser von transcript_resolved ERGAENZEN (Aktualitaets-Pflicht Punkt 23):
   "... ; audio_health_resolved geschrieben routes/app_routes.py (api_beenden/_audio_health_bg), gelesen
   services/slow_lane.py (Call-Ende-Merge-Gate, TAXO2-04 Audio-Race-Fix); transcript_resolved geschrieben
   routes/app_routes.py (api_beenden), gelesen services/slow_lane.py (LLM-Judge-Anstoss-Gate + Merge-Gate)."

`downgrade()`:
- `ALTER TABLE public.calls DROP COLUMN IF EXISTS transcript_resolved`
- calls-TABELLEN-Schild auf den Vor-0028-Stand (mit audio_health_resolved, OHNE transcript_resolved) zuruecksetzen.

UTF-8-Regel: Code-Identifier ASCII (transcript_resolved); Schild-Text darf Umlaute tragen (DB-Doku, kein Identifier) — halte dich am 0027-Stil.
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('alembic/versions/0028_calls_transcript_resolved.py',encoding='utf-8').read()); print('AST-OK')"</automated>
    <grep>grep -E "revision = '0028'|down_revision = '0027'" alembic/versions/0028_calls_transcript_resolved.py (beide Treffer)</grep>
    <grep>grep -c "ADD COLUMN IF NOT EXISTS transcript_resolved BOOLEAN NOT NULL DEFAULT FALSE" alembic/versions/0028_calls_transcript_resolved.py (== 1)</grep>
    <grep>grep -E "COMMENT ON COLUMN public.calls.transcript_resolved" alembic/versions/0028_calls_transcript_resolved.py (Treffer)</grep>
    <grep>grep -E "COMMENT ON TABLE public.calls" alembic/versions/0028_calls_transcript_resolved.py (Treffer — Tabellen-Schild erweitert)</grep>
    <note>HART Kein-Local-Dev: KEIN lokales `alembic upgrade`. Der scharfe Lauf ist der supervised Deploy (als postgres VOR Restart). AST-Parse beweist nur Syntax-Korrektheit.</note>
  </verify>

  <done>
Datei `alembic/versions/0028_calls_transcript_resolved.py` existiert, AST-parsebar, revision='0028'/down_revision='0027',
enthaelt das idempotente ADD COLUMN, die COMMENT ON COLUMN fuer transcript_resolved und die erweiterte
COMMENT ON TABLE calls. downgrade droppt die Spalte + setzt das Tabellen-Schild zurueck.
  </done>
</task>

<task type="auto">
  <name>Task 2: ORM — Call.transcript_resolved-Spalte + calls-Tabellen-Schild (models.py, Single-Source zur Migration)</name>
  <files>database/models.py</files>

  <read_first>
    - database/models.py:702-711 (audio_health_resolved-Spalte — das EXAKTE Column(...)-Muster: Boolean, nullable=False, server_default=text('false'), comment=)
    - database/models.py:712-745 (Call-Restspalten + __table_args__-Tabellen-Schild:744 — die transcript_resolved-Spalte gehoert in denselben Block, das Tabellen-Schild muss erweitert werden)
    - alembic/versions/0028_calls_transcript_resolved.py (aus Task 1 — die comment=-Texte MUESSEN wortgleich sein, Single-Source Punkt 23)
  </read_first>

  <action>
**Zwei In-Place-Edits in database/models.py — KEINE Logik, nur ORM-Deklaration + Schild (Single-Source zur 0028-Migration):**

**(A) Neue Spalte `transcript_resolved` im Call-Modell**, direkt NACH `audio_health_resolved` (models.py:711),
exakt nach dessen Muster:
```python
# --- Phase 08.23.2.TAXO2 LLM-Bewerter — Fan-In-Anstoss-Signal fuer den Verhaltens-Call (Migration 0028) ---
transcript_resolved = Column(
    Boolean, nullable=False, server_default=text('false'),
    comment="Fan-In-Anstoss-Signal fuer den LLM-Verhaltens-Bewerter (Beobachtung statt Note): TRUE sobald "
            "das Transkript am Call-Ende festgeschrieben ist (Segmente geschrieben ODER bewiesen leer = "
            "resolved-als-absent). Der Judge-Anstoss (slow_lane Call-Ende-Schritt) wartet darauf, BEVOR er "
            "das Transkript an Sonnet gibt — verhindert die Race, in der gegen ein noch nicht geschriebenes "
            "Transkript bewertet wird (Punkt 26, analog audio_health_resolved). Status: lebt (TAXO2 LLM-Bewerter). "
            "Schreibt routes/app_routes.py (api_beenden); liest services/slow_lane.py (Judge-Anstoss-Gate + Merge-Gate).",
)
```
Der comment=-Text MUSS wortgleich zum COMMENT ON COLUMN in 0028 (Task 1) sein (Single-Source).

**(B) calls-Tabellen-Schild erweitern** (__table_args__-comment, models.py:744): den Ist-Text uebernehmen und
um den transcript_resolved-Schreiber/Leser ergaenzen — wortgleich zur COMMENT ON TABLE in 0028. (Anhaengen:
"... ; transcript_resolved geschrieben routes/app_routes.py (api_beenden), gelesen services/slow_lane.py
(LLM-Judge-Anstoss-Gate + Merge-Gate).")

`Boolean` und `text` sind in models.py bereits importiert (audio_health_resolved nutzt beide) — KEIN neuer
Import noetig. Verifiziere das via grep VOR dem Edit (siehe verify). KEINE weitere Aenderung.
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('database/models.py',encoding='utf-8').read()); print('AST-OK')"</automated>
    <grep>grep -n "transcript_resolved = Column" database/models.py (== 1 Treffer, im Call-Block nach audio_health_resolved)</grep>
    <grep>grep -n "transcript_resolved geschrieben routes/app_routes.py" database/models.py (Treffer — Tabellen-Schild erweitert)</grep>
    <grep>grep -nE "Boolean|text\(" database/models.py | head (Boolean + text bereits importiert — kein neuer Import)</grep>
    <note>HART Kein-Local-Dev: KEIN lokaler Import von models. AST-Parse genuegt als Syntax-Beleg; der scharfe SCHILD-Guard-Lauf (test_schild_guard.py gegen pg_description) faellt im deploy.sh-Gate an.</note>
  </verify>

  <done>
Call traegt `transcript_resolved` (Boolean, nullable=False, server_default text('false'), Schild >=10 Zeichen
wortgleich zur Migration, nennt 'Bewerter-Anstoss'). Das calls-Tabellen-Schild nennt den neuen
transcript_resolved-Schreiber/Leser. models.py AST-parsebar, kein neuer Import.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (keine neue) | Diese Welle legt eine interne DB-Spalte + Schilder an. KEIN Netz-/Auth-/Tenant-Pfad neu. `calls` ist NICHT FORCE-RLS (wie audio_health_resolved) → kein M-4-GUC-Bezug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HT-01-01 | Tampering | Migration auf Prod (als postgres VOR Restart) | accept | Idempotentes ADD COLUMN IF NOT EXISTS (re-run-sicher); NOT NULL DEFAULT FALSE backfillt Bestand. Supervised Deploy (André) prueft HEAD 0027 vor 0028. |
| T-HT-01-02 | Info Disclosure | Schild-Text (COMMENT) | accept | Schilder sind interne Doku ohne PII/Secrets; sichtbar nur ueber privilegierten Catalog-Read (nerve_app peer-auth). |
| T-HT-01-03 | Denial of Service | Schema-Drift Migration↔ORM | mitigate | Single-Source-Pflicht (Punkt 23): comment= in models.py == COMMENT in 0028; grep-Acceptance prueft Wortgleichheit. Drift → SCHILD-Guard im Deploy-Gate. |
</threat_model>

<verification>
## 5. Persistenz-Schicht-Verifikation (Punkt 21) — Welle 1

### Angefasste Tabellen
- `calls` (Schema-Aenderung: neue Spalte transcript_resolved) — Schreib-/Lese-Logik kommt in Plan 03.

### inspect.sh-Beleg (read_first, vom Executor zu ziehen — read-only gegen Prod 178.104.82.166)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
→ MUSS 0027 zeigen (Prod-HEAD; down_revision='0027'). CONTEXT-Beleg: HEAD live = 0027.
$ ssh ... 'bash scripts/inspect.sh schema calls'
→ KEINE Zeile transcript_resolved PRE-Migration (erwartet — 0028 legt sie an, exakt wie 0027 bei audio_health_resolved).
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad (Plan 03) | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Verifiziert? |
|---|---|---|---|---|
| `call.transcript_resolved` | Schreiben api_beenden / Lesen slow_lane Judge-Anstoss (Plan 03) | DB calls.transcript_resolved (BOOLEAN) | Batch am Call-Ende (api_beenden, selber commit wie ended_at) — Leser (Judge-Anstoss) laeuft NUR nach gesetztem Flag | ✓ Migration 0028 + models.py (diese Welle); Logik Plan 03 |

### Punkt 26 — Async-Daten-Bereitschafts-Naht (Foundation-Bezug)
Diese Welle SCHREIBT keine Lese-Logik — sie legt das Flag, das in Plan 03 die Naht bildet (Judge-Anstoss wartet
auf transcript_resolved==True). Der Beweis, dass der Anstoss dem Transkript-Write nicht vorauslaeuft, ist
Pflicht-Inhalt von Plan 03. Hier nur: das Flag ist NOT NULL DEFAULT FALSE → ein Leser sieht NIE NULL, sondern
FALSE (= "noch nicht resolved").
</verification>

<success_criteria>
- Migration 0028 existiert (down_revision '0027'), idempotent, mit Spalte + zwei Schild-Bloecken.
- models.py: Call.transcript_resolved deklariert (Boolean/nullable=False/server_default text('false')/Schild
  nennt 'Bewerter-Anstoss'), calls-Tabellen-Schild erweitert — wortgleich zur Migration (Single-Source).
- Beide Dateien AST-parsebar. KEIN lokaler alembic-/Import-Lauf (HART Kein-Local-Dev).
- KEINE Verhaltens-Logik in dieser Welle (slow_lane/app_routes UNANGETASTET — Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-01-SUMMARY.md`
</output>
