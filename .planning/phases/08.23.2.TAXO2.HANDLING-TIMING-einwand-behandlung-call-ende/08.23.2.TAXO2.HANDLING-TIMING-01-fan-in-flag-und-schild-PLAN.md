---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0028_calls_transcript_resolved.py
  - database/models.py
autonomous: true
requirements: [HT-1, HT-3]
must_haves:
  truths:
    - "Die Spalte calls.transcript_resolved existiert als BOOLEAN NOT NULL DEFAULT FALSE (Migration 0028, down_revision 0027)."
    - "Das ORM-Modell Call traegt transcript_resolved mit server_default text('false') + Schild >=10 Zeichen."
    - "Das transcript_segments.ts_ms-Schild sagt korrekt 'ms ab erster Aussage' (NICHT 'ms ab Call-Start')."
    - "Das calls-TABELLEN-Schild nennt den neuen Schreiber (api_beenden transcript_resolved) + Leser (slow_lane Merge-Gate)."
  artifacts:
    - path: "alembic/versions/0028_calls_transcript_resolved.py"
      provides: "Migration: ADD COLUMN transcript_resolved + COMMENT-Schilder (idempotent, down_revision 0027)"
      contains: "transcript_resolved"
    - path: "database/models.py"
      provides: "Call.transcript_resolved-Spalte + korrigiertes ts_ms-Schild"
      contains: "transcript_resolved"
  key_links:
    - from: "alembic/versions/0028_calls_transcript_resolved.py"
      to: "database/models.py Call.transcript_resolved"
      via: "Single-Source: COMMENT-Migration == models.py comment="
      pattern: "transcript_resolved"
---

<objective>
Foundation-Welle des Handling-Timing-Fix: die Fan-In-Bereitschafts-Naht `calls.transcript_resolved`
als ECHTE DB-Spalte anlegen (Migration 0028 + ORM), exakt nach dem erprobten Muster von
`audio_health_resolved` (Migration 0027). Plus die zwei Punkt-23-Schild-Korrekturen, die diese Phase
ausloest: (a) das falsche `transcript_segments.ts_ms`-Schild ("ms ab Call-Start" → faktisch "ms ab
erster Aussage", RESEARCH Pitfall 5/Landmine), (b) das `calls`-Tabellen-Schild um den neuen
Schreiber/Leser von `transcript_resolved` ergaenzen.

Purpose: Plan 02 (der eigentliche Code-Fix in slow_lane.py / app_routes.py / intent_event_writer.py)
braucht diese Spalte als Lese-Gate (Merge) UND als Defer-Guard-Quelle. Diese Welle legt NUR die
durable Spalte + die korrekten Schilder — KEINE Verhaltens-Logik (die kommt in Plan 02). Saubere
Datei-Trennung: Plan 01 fasst NUR Migration + models.py an, Plan 02 NUR den Code.

Output: Migration `0028_calls_transcript_resolved.py` (down_revision '0027') + `Call.transcript_resolved`
im ORM + korrigiertes ts_ms-Schild + aktualisiertes calls-Tabellen-Schild.

**Komplexitaet: 🔴** (Slow-Lane-Timing/Race + Persistenz-Schicht — Cross-AI Pflicht VOR Execute fuer die
Gesamt-Phase; diese Welle ist die mechanische Foundation, der Fork wird in Plan 02 bewertet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-RESEARCH.md
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-CONTEXT.md
@CLAUDE.md
@alembic/versions/0027_calls_audio_health_resolved.py

<interfaces>
<!-- Das exakte 0027-Muster, das diese Welle spiegelt (audio_health_resolved → transcript_resolved). -->
<!-- Executor uebernimmt: revision/down_revision-Stil, idempotentes ADD COLUMN IF NOT EXISTS, -->
<!-- COMMENT ON COLUMN + COMMENT ON TABLE (Single-Source mit models.py), downgrade mit Schild-Reset. -->

Bestehende Call-Spalte (das Vorbild), database/models.py:703-711:
```python
audio_health_resolved = Column(
    Boolean, nullable=False, server_default=text('false'),
    comment="Fan-In-Join-Flag (TAXO2-04 Audio-Race-Fix): TRUE sobald der async Audio-Zustand "
            "endgueltig festgeschrieben ist (Score gesetzt ODER bewiesen kein Buffer). Der "
            "Call-Ende-Merge wartet darauf, BEVOR er ein NULL-audio_health_score als "
            "poor_audio_health wertet — verhindert die Race, in der der Merge VOR dem "
            "Audio-Thread liest. Schreibt routes/app_routes.py (api_beenden / _audio_health_bg); "
            "liest services/slow_lane.py (Merge-Gate).",
)
```

Das aktuelle (FALSCHE) ts_ms-Schild, database/models.py:927 (TranscriptSegment):
```python
ts_ms = Column(Integer, nullable=False, comment='ms ab Call-Start, fuer Reihenfolge')  # ms ab Call-Start, fuer Reihenfolge
```
RESEARCH-Beweis (app_routes.py:60-66 `_transcript_entries_to_segments`): ts_ms = wall-clock-ms MINUS
wall-clock-ms des ERSTEN Transkript-Entry. Nullpunkt = erste Aussage, NICHT calls.started_at. Jeder
conv_log startet bei ts_ms=0 (inspect.sh-Beleg). Schild ist faktisch falsch → korrigieren.

Das aktuelle calls-TABELLEN-Schild, database/models.py:744 (im __table_args__):
```python
{'comment': 'Zentraler Call-Datensatz der neuen Architektur (UUID-PK, Outcome/Coaching/Transkript-Storage). Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt/liest services/+routes/ der neuen Call-Pipeline; audio_health_resolved geschrieben routes/app_routes.py (api_beenden/_audio_health_bg), gelesen services/slow_lane.py (Call-Ende-Merge-Gate, TAXO2-04 Audio-Race-Fix).'},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0028 — calls.transcript_resolved (Fan-In-Flag) + ts_ms-Schild-Fix + calls-Tabellen-Schild</name>
  <files>alembic/versions/0028_calls_transcript_resolved.py</files>

  <read_first>
    - alembic/versions/0027_calls_audio_health_resolved.py (das EXAKTE Muster, das diese Migration spiegelt — Datei-Header-Docstring, revision/down_revision, idempotentes op.execute, COMMENT ON COLUMN + COMMENT ON TABLE, downgrade mit Schild-Reset)
    - database/models.py:744 (calls-Tabellen-Schild Ist-Stand — muss in COMMENT ON TABLE wortgleich erweitert werden)
    - database/models.py:923-939 (TranscriptSegment — ts_ms-Schild Ist-Stand fuer COMMENT ON COLUMN-Korrektur)
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations' → MUSS 0027 zeigen (Prod-HEAD; down_revision='0027'). RESEARCH belegt HEAD 0027 (2026-06-26). Wenn der HEAD NICHT 0027 ist → STOP, down_revision anpassen (kein blindes Bauen).
  </read_first>

  <action>
Erzeuge `alembic/versions/0028_calls_transcript_resolved.py` strikt nach dem 0027-Muster. HART (CLAUDE.md):
KEIN lokales alembic, KEIN lokales Ausfuehren — nur die Datei schreiben. Die Migration laeuft am Deploy-Gate
als postgres VOR Restart (create_all-Race, wie 0020-0027).

`revision = '0028'`, `down_revision = '0027'`, branch_labels/depends_on None.

Datei-Header-Docstring (analog 0027): erklaere knapp den Bug (Handling-Benotung lief LIVE beim Emit, aber
transcript_segments werden gebuendelt am Call-Ende geschrieben → Benoter findet die Berater-Antwort nie →
systemisch 'abstained') und den Fix (Fan-In-Flag transcript_resolved, gesetzt im SELBEN calls-commit wie
ended_at/conversation_log_id, NACHDEM der transcript-Write lief — analog audio_health_resolved). Nenne den
create_all-Falle-Hinweis + Prod-HEAD 0027.

`upgrade()` — drei `op.execute(...)`-Bloecke:
1. **Spalte (idempotent, hauseigenes IF-NOT-EXISTS-Muster):**
   `ALTER TABLE public.calls ADD COLUMN IF NOT EXISTS transcript_resolved BOOLEAN NOT NULL DEFAULT FALSE`
   (NOT NULL DEFAULT FALSE deckt das Backfill auf Bestands-Zeilen ab).
2. **SCHILD COMMENT ON COLUMN** public.calls.transcript_resolved — wortgleich zum models.py-comment= aus Task 2
   (Single-Source-Pflicht Punkt 23). Inhalt (>=10 Zeichen, §0.2-Form Zweck+Status+Schreibt/liest):
   "Fan-In-Join-Flag (HANDLING-TIMING-Fix): TRUE sobald das Transkript am Call-Ende festgeschrieben ist
   (Segmente geschrieben ODER bewiesen leer = resolved-als-absent). Der Handling-Benoter (Deferred Scoring)
   + der Call-Ende-Merge warten darauf, BEVOR sie ein leeres transcript_segments-Lookup als gueltige
   Abstention werten — verhindert die Race, in der live beim Emit (vor dem Transkript-Write) benotet wird.
   Schreibt routes/app_routes.py (api_beenden); liest services/slow_lane.py (Defer-Guard + Merge-Gate)."
3. **ts_ms-Schild-Korrektur COMMENT ON COLUMN** public.transcript_segments.ts_ms:
   "ms ab erster Transkript-Aussage (NICHT ab Call-Start/started_at — der Writer _transcript_entries_to_segments
   in routes/app_routes.py zieht den ersten Entry als Nullpunkt ab; jeder conversation_log startet bei ts_ms=0),
   monoton geklemmt fuer Sprech-Reihenfolge. Schreibt routes/app_routes.py; liest services/slow_lane.py (Handling-Lookup-Anker)."
4. **calls-TABELLEN-Schild erweitern COMMENT ON TABLE** public.calls — den Ist-Text (models.py:744) uebernehmen
   und um den neuen Schreiber/Leser von transcript_resolved ERGAENZEN (Aktualitaets-Pflicht Punkt 23):
   "... ; audio_health_resolved geschrieben routes/app_routes.py (api_beenden/_audio_health_bg), gelesen
   services/slow_lane.py (Call-Ende-Merge-Gate, TAXO2-04 Audio-Race-Fix); transcript_resolved geschrieben
   routes/app_routes.py (api_beenden), gelesen services/slow_lane.py (Deferred-Handling-Defer-Guard +
   Merge-Gate, HANDLING-TIMING-Fix)."

`downgrade()`:
- `ALTER TABLE public.calls DROP COLUMN IF EXISTS transcript_resolved`
- ts_ms-Schild auf den (alten/falschen) Stand zuruecksetzen ODER besser: auf den korrekten Text lassen mit
  Kommentar "Schild-Korrektur bleibt — sachlich richtig, unabh. von der Spalte" (Executor's Discretion;
  bevorzugt: ts_ms-Schild-Korrektur NICHT zuruecknehmen, da sie unabhaengig richtig ist — im downgrade-Kommentar begruenden).
- calls-TABELLEN-Schild auf den Vor-0028-Stand (mit audio_health_resolved, OHNE transcript_resolved) zuruecksetzen.

UTF-8-Regel: Code-Identifier ASCII (transcript_resolved); Schild-Text darf Umlaute tragen (es ist DB-Doku, kein Identifier) — konsistent mit 0027 (das "verhindert"/"waertet" mit ae schreibt; halte dich an den 0027-Stil, ASCII-Ersatz im Schild ist OK aber nicht zwingend).
  </action>

  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('alembic/versions/0028_calls_transcript_resolved.py',encoding='utf-8').read()); print('AST-OK')"</automated>
    <grep>grep -E "revision = '0028'|down_revision = '0027'" alembic/versions/0028_calls_transcript_resolved.py (beide Treffer)</grep>
    <grep>grep -c "ADD COLUMN IF NOT EXISTS transcript_resolved BOOLEAN NOT NULL DEFAULT FALSE" alembic/versions/0028_calls_transcript_resolved.py (== 1)</grep>
    <grep>grep -E "COMMENT ON COLUMN public.transcript_segments.ts_ms" alembic/versions/0028_calls_transcript_resolved.py (Treffer — ts_ms-Schild-Fix vorhanden)</grep>
    <grep>grep -E "COMMENT ON COLUMN public.calls.transcript_resolved" alembic/versions/0028_calls_transcript_resolved.py (Treffer)</grep>
    <grep>grep -E "COMMENT ON TABLE public.calls" alembic/versions/0028_calls_transcript_resolved.py (Treffer — Tabellen-Schild erweitert)</grep>
    <note>HART Kein-Local-Dev: KEIN lokales `alembic upgrade`. Der scharfe Lauf ist der supervised Deploy (als postgres VOR Restart). AST-Parse beweist nur Syntax-Korrektheit.</note>
  </verify>

  <done>
Datei `alembic/versions/0028_calls_transcript_resolved.py` existiert, AST-parsebar, revision='0028'/down_revision='0027',
enthaelt das idempotente ADD COLUMN, die COMMENT ON COLUMN fuer transcript_resolved UND fuer die ts_ms-Korrektur,
und die erweiterte COMMENT ON TABLE calls. downgrade droppt die Spalte + setzt das Tabellen-Schild zurueck.
  </done>
</task>

<task type="auto">
  <name>Task 2: ORM — Call.transcript_resolved-Spalte + ts_ms-Schild-Korrektur (models.py, Single-Source zur Migration)</name>
  <files>database/models.py</files>

  <read_first>
    - database/models.py:702-711 (audio_health_resolved-Spalte — das EXAKTE Column(...)-Muster, das transcript_resolved spiegelt: Boolean, nullable=False, server_default=text('false'), comment=)
    - database/models.py:712-745 (Call-Restspalten + __table_args__-Tabellen-Schild:744 — die transcript_resolved-Spalte gehoert in denselben Block, das Tabellen-Schild muss erweitert werden)
    - database/models.py:923-939 (TranscriptSegment — ts_ms:927 mit dem falschen comment= + dem nachgestellten `# ms ab Call-Start`-Kommentar, BEIDE korrigieren)
    - alembic/versions/0028_calls_transcript_resolved.py (aus Task 1 — die comment=-Texte MUESSEN wortgleich sein, Single-Source Punkt 23)
  </read_first>

  <action>
**Drei In-Place-Edits in database/models.py — KEINE Logik, nur ORM-Deklaration + Schild (Single-Source zur 0028-Migration):**

**(A) Neue Spalte `transcript_resolved` im Call-Modell**, direkt NACH `audio_health_resolved` (models.py:711),
exakt nach dessen Muster:
```python
# --- Phase 08.23.2.TAXO2.HANDLING-TIMING — Fan-In-Join-Flag gegen die Handling-Timing-Race (Migration 0028) ---
transcript_resolved = Column(
    Boolean, nullable=False, server_default=text('false'),
    comment="Fan-In-Join-Flag (HANDLING-TIMING-Fix): TRUE sobald das Transkript am Call-Ende "
            "festgeschrieben ist (Segmente geschrieben ODER bewiesen leer = resolved-als-absent). "
            "Der Handling-Benoter (Deferred Scoring) + der Call-Ende-Merge warten darauf, BEVOR sie "
            "ein leeres transcript_segments-Lookup als gueltige Abstention werten — verhindert die "
            "Race, in der live beim Emit (vor dem Transkript-Write) benotet wird. Schreibt "
            "routes/app_routes.py (api_beenden); liest services/slow_lane.py (Defer-Guard + Merge-Gate).",
)
```
Der comment=-Text MUSS wortgleich zum COMMENT ON COLUMN in 0028 (Task 1) sein (Single-Source).

**(B) calls-Tabellen-Schild erweitern** (__table_args__-comment, models.py:744): den Ist-Text uebernehmen und
um den transcript_resolved-Schreiber/Leser ergaenzen — wortgleich zur COMMENT ON TABLE in 0028. (Anhaengen:
"... ; transcript_resolved geschrieben routes/app_routes.py (api_beenden), gelesen services/slow_lane.py
(Deferred-Handling-Defer-Guard + Merge-Gate, HANDLING-TIMING-Fix).")

**(C) ts_ms-Schild korrigieren** (models.py:927): den comment= UND den nachgestellten `# ms ab Call-Start`-Inline-
Kommentar auf die korrekte Semantik aendern (wortgleich zur COMMENT ON COLUMN ts_ms in 0028):
```python
ts_ms = Column(Integer, nullable=False, comment='ms ab erster Transkript-Aussage (NICHT ab Call-Start/started_at; jeder conversation_log startet bei ts_ms=0), monoton geklemmt fuer Sprech-Reihenfolge. Schreibt routes/app_routes.py; liest services/slow_lane.py (Handling-Lookup-Anker)')  # ms ab erster Aussage (Nullpunkt = erster Entry), nicht started_at
```

`Boolean` und `text` sind in models.py bereits importiert (audio_health_resolved nutzt beide, models.py:703-704) —
KEIN neuer Import noetig. Verifiziere das via grep VOR dem Edit (siehe verify).

KEINE weitere Aenderung (keine neue Logik, keine Migration-Logik in models.py).
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('database/models.py',encoding='utf-8').read()); print('AST-OK')"</automated>
    <grep>grep -n "transcript_resolved = Column" database/models.py (== 1 Treffer, im Call-Block nach audio_health_resolved)</grep>
    <grep>grep -n "ms ab erster" database/models.py (Treffer — ts_ms-Schild korrigiert; `grep -c "ms ab Call-Start" database/models.py` muss 0 sein)</grep>
    <grep>grep -n "transcript_resolved geschrieben routes/app_routes.py" database/models.py (Treffer — Tabellen-Schild erweitert)</grep>
    <grep>grep -nE "^from sqlalchemy import|Boolean|^from sqlalchemy import text|text\b" database/models.py | head (Boolean + text bereits importiert — kein neuer Import)</grep>
    <note>HART Kein-Local-Dev: KEIN lokaler Import von models (DB-Engine-Init). AST-Parse genuegt als Syntax-Beleg; der scharfe Schema-/SCHILD-Guard-Lauf (test_schild_guard.py gegen pg_description) faellt im deploy.sh-Gate an.</note>
  </verify>

  <done>
Call traegt `transcript_resolved` (Boolean, nullable=False, server_default text('false'), Schild >=10 Zeichen
wortgleich zur Migration). Das ts_ms-Schild sagt "ms ab erster Aussage" (kein "ms ab Call-Start" mehr). Das
calls-Tabellen-Schild nennt den neuen transcript_resolved-Schreiber/Leser. models.py AST-parsebar, kein neuer Import.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (keine neue) | Diese Welle legt eine interne DB-Spalte + Schilder an. KEIN Netz-/Auth-/Tenant-Pfad neu. `calls` ist NICHT FORCE-RLS (wie audio_health_resolved) → kein M-4-GUC-Bezug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HT-01-01 | Tampering | Migration auf Prod (als postgres VOR Restart) | accept | Idempotentes ADD COLUMN IF NOT EXISTS (re-run-sicher); NOT NULL DEFAULT FALSE backfillt Bestand. Supervised Deploy (André) prueft HEAD 0027 vor 0028. |
| T-HT-01-02 | Info Disclosure | Schild-Text (COMMENT) | accept | Schilder sind interne Doku ohne PII/Secrets; sichtbar nur ueber privilegierten Catalog-Read (nerve_app peer-auth). |
| T-HT-01-03 | Denial of Service | Schema-Drift Migration↔ORM | mitigate | Single-Source-Pflicht (Punkt 23): comment= in models.py == COMMENT in 0028; grep-Acceptance prueft Wortgleichheit. Drift → SCHILD-Guard ODER create_all-Inkonsistenz → faellt im Deploy-Gate auf. |
</threat_model>

<verification>
## Persistenz-Schicht-Verifikation (Punkt 21) — Welle 1

### Angefasste Tabellen
- `calls` (Schema-Aenderung: neue Spalte transcript_resolved) — Schreib-/Lese-Logik kommt in Plan 02.
- `transcript_segments` (NUR Schild-Korrektur ts_ms, keine Schema-/Daten-Aenderung).

### inspect.sh-Beleg (read_first, vom Executor zu ziehen — read-only gegen Prod 178.104.82.166)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
→ MUSS 0027 zeigen (Prod-HEAD; down_revision='0027'). RESEARCH-Beleg: HEAD 0027 (2026-06-26).
$ ssh ... 'bash scripts/inspect.sh schema calls'
→ KEINE Zeile transcript_resolved PRE-Migration (erwartet — 0028 legt sie an, exakt wie 0027 bei audio_health_resolved).
$ ssh ... 'bash scripts/inspect.sh schema transcript_segments'
→ ts_ms integer NOT NULL vorhanden (nur das Schild aendert sich, nicht die Spalte).
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad (Plan 02) | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Verifiziert? |
|---|---|---|---|---|
| `call.transcript_resolved` | Schreiben api_beenden / Lesen slow_lane (Plan 02) | DB calls.transcript_resolved (BOOLEAN) | Batch am Call-Ende (api_beenden, selber commit wie ended_at) — Leser (Merge/Defer-Guard) laeuft NUR nach gesetztem Flag | ✓ Migration 0028 + models.py (diese Welle); Logik Plan 02 |
| `transcript_segments.ts_ms` | gelesen slow_lane Lookup (Plan 02) | DB-Spalte (existiert, Schild korrigiert) | Batch am Call-Ende (api_beenden:454-470) | ✓ existiert; Semantik-Korrektur in dieser Welle |

### Punkt 26 — Async-Daten-Bereitschafts-Naht (Foundation-Bezug)
Diese Welle SCHREIBT keine Lese-Logik — sie legt das Flag, das in Plan 02 die Naht bildet. Der Beweis, dass
der Leser (Merge/Defer-Guard) dem Schreiber (api_beenden transcript_resolved) nicht vorauslaeuft, ist Pflicht-
Inhalt von Plan 02 (dort die 4 Race-Antworten). Hier nur: das Flag ist NOT NULL DEFAULT FALSE → ein Leser sieht
NIE NULL, sondern FALSE (= "noch nicht resolved") → kein Mehrdeutigkeits-Risiko.
</verification>

<success_criteria>
- Migration 0028 existiert (down_revision '0027'), idempotent, mit allen drei Schild-Bloecken.
- models.py: Call.transcript_resolved deklariert (Boolean/nullable=False/server_default text('false')/Schild),
  ts_ms-Schild korrigiert, calls-Tabellen-Schild erweitert — alles wortgleich zur Migration (Single-Source).
- Beide Dateien AST-parsebar. KEIN lokaler alembic-/Import-Lauf (HART Kein-Local-Dev).
- KEINE Verhaltens-Logik in dieser Welle (slow_lane/app_routes/intent_event_writer UNANGETASTET — Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-01-SUMMARY.md`
</output>
