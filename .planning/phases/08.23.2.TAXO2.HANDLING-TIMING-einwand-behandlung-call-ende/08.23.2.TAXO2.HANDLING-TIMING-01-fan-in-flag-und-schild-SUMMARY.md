---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 01
subsystem: database/schema
tags: [migration, orm, fan-in, transcript_resolved, schild, punkt23, punkt26]
key-files:
  created:
    - alembic/versions/0028_calls_transcript_resolved.py
  modified:
    - database/models.py
key-decisions:
  - "down_revision='0027' bestaetigt (0027 ist aktueller Prod-HEAD, verifiziert per glob auf alembic/versions/)"
  - "Schild-Text wortgleich in Migration + models.py (Single-Source Punkt 23) — kein abweichendes Wording"
  - "transcript_resolved direkt nach audio_health_resolved im Call-Modell platziert (analoges Fan-In-Muster)"
requirements-completed: [GOAL-Anstoss, RESEARCH-FanIn]
completed: 2026-06-28
---

# Phase 08.23.2.TAXO2.HANDLING-TIMING Plan 01: Fan-In-Flag + Schild Summary

**One-liner:** `calls.transcript_resolved` als BOOLEAN NOT NULL DEFAULT FALSE angelegt (Migration 0028 + ORM), Single-Source-Schilder (Punkt 23) nach dem erprobten `audio_health_resolved`-Muster (0027) — Bewerter-Anstoss-Signal fuer den LLM-Verhaltens-Judge.

## Was gebaut wurde

Diese Welle legt die Foundation fuer den LLM-Gesamtbewerter (Plan 03). Sie besteht aus zwei Artefakten:

### 1. Migration `alembic/versions/0028_calls_transcript_resolved.py`

- `revision='0028'`, `down_revision='0027'`
- `upgrade()`:
  - `ALTER TABLE public.calls ADD COLUMN IF NOT EXISTS transcript_resolved BOOLEAN NOT NULL DEFAULT FALSE` (idempotent, backfills Bestand)
  - `COMMENT ON COLUMN public.calls.transcript_resolved IS '...'` (Schild >=10 Zeichen, alle 3 Pflicht-Teile nach §0.2)
  - `COMMENT ON TABLE public.calls IS '...'` (erweitert den Ist-Stand um transcript_resolved-Schreiber/Leser)
- `downgrade()`: `DROP COLUMN IF EXISTS transcript_resolved` + Tabellen-Schild auf Vor-0028-Stand (mit audio_health_resolved, ohne transcript_resolved)

### 2. ORM-Edit `database/models.py`

- `Call.transcript_resolved`: `Column(Boolean, nullable=False, server_default=text('false'), comment="...")` direkt nach `audio_health_resolved` (Zeile 713)
- `comment=`-Text wortgleich zum COMMENT ON COLUMN in 0028 (Single-Source-Pflicht Punkt 23)
- `__table_args__` comment (Zeile 754): erweitert um `transcript_resolved geschrieben routes/app_routes.py (api_beenden), gelesen services/slow_lane.py (LLM-Judge-Anstoss-Gate + Merge-Gate)`
- Kein neuer Import (Boolean + text bereits auf Zeile 2)

## Commits

| Task | Commit | Nachricht |
|------|--------|-----------|
| Task 1 — Migration 0028 | 141e373 | feat(08.23.2.TAXO2.HANDLING-TIMING-01): Migration 0028 — calls.transcript_resolved Fan-In-Anstoss-Signal |
| Task 2 — ORM models.py | 39b8c88 | feat(08.23.2.TAXO2.HANDLING-TIMING-01): ORM Call.transcript_resolved + calls-Tabellen-Schild (Single-Source zur Migration 0028) |

## Deviations from Plan

None — Plan wurde exakt wie geschrieben ausgefuehrt. down_revision='0027' ist korrekt (0027 ist verifizierter aktueller HEAD; kein Abweichen noetig).

## Acceptance

### Statisch verifiziert (HART Kein-Local-Dev — kein alembic/import-Lauf)

| Kriterium | Methode | Ergebnis |
|-----------|---------|----------|
| `revision = '0028'` + `down_revision = '0027'` in 0028-Datei | grep | PASS (beide Treffer) |
| `ADD COLUMN IF NOT EXISTS transcript_resolved BOOLEAN NOT NULL DEFAULT FALSE` | grep -c | PASS (count == 1) |
| `COMMENT ON COLUMN public.calls.transcript_resolved` vorhanden | grep | PASS |
| `COMMENT ON TABLE public.calls` vorhanden (erweitert) | grep | PASS |
| Migration AST-parsebar | python -m py_compile | SYNTAX-OK |
| `transcript_resolved = Column` in models.py (1 Treffer, nach audio_health_resolved) | grep -n | PASS (Zeile 713) |
| `transcript_resolved geschrieben routes/app_routes.py` im Tabellen-Schild | grep -n | PASS (Zeile 754) |
| Boolean + text bereits importiert (kein neuer Import) | grep -nE | PASS (Zeile 2) |
| models.py AST-parsebar | python -m py_compile | SYNTAX-OK |

### Deferred zum supervised Deploy

- **Scharfer Alembic-Lauf** (`alembic upgrade 0028` als postgres VOR Restart): laeuft am Deploy-Gate
- **SCHILD-Guard** (`test_schild_guard.py` gegen pg_description): laeuft im deploy.sh-Gate (prueft Vorhandensein + Laenge >=10 in der echten DB)
- **inspect.sh schema calls** auf Prod: bestaetigt `transcript_resolved`-Spalte post-Deploy

### Keine Verhaltens-Logik in dieser Welle

`routes/app_routes.py` und `services/slow_lane.py` wurden NICHT angefasst (Punkt 26-Logik kommt in Plan 03).

## Known Stubs

Keine. Diese Welle legt nur Schema + ORM-Deklaration an — kein UI, keine Daten-Fluss-Logik.

## Threat Flags

Keine neuen Threat-Surface-Einfuehrungen. Die Welle legt eine interne DB-Spalte an (kein Netz-/Auth-/Tenant-Pfad neu).

## Self-Check: PASSED

- [x] `alembic/versions/0028_calls_transcript_resolved.py` existiert
- [x] Commit 141e373 in git log vorhanden
- [x] `database/models.py` enthaelt `transcript_resolved = Column` (Zeile 713)
- [x] Commit 39b8c88 in git log vorhanden
- [x] Beide Dateien py_compile-sauber
- [x] Single-Source: COMMENT ON COLUMN-Text == models.py comment= (wortgleich)
- [x] Tabellen-Schild in Migration + models.py beide erweitert
