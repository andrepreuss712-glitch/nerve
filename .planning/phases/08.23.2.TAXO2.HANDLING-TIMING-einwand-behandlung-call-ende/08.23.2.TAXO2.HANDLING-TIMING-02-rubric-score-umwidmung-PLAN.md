---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - alembic/versions/0029_rubric_score_observations.py
  - database/models.py
  - services/judge_dimensions.py
autonomous: true
requirements: [GOAL-Beobachtung, REQ-Dimensionen, CONTEXT-Umwidmung, CONTEXT-Intern-Auspraegung]
must_haves:
  truths:
    - "rubric_score traegt observations_jsonb (JSONB) fuer Beobachtungen+woertliche Beleg-Zitate je Dimension und ratings_jsonb (JSONB) fuer die INTERNE grobe Auspraegung schwach/ok/stark je Dimension — KEINE neue sichtbare Zahl."
    - "Die feste Dimensions-Liste (~4) liegt versioniert als Daten/Config in services/judge_dimensions.py (je Dim: key ASCII, Anzeige-Name, Ein-Satz-Definition, BARS-Anker schwach/ok/stark mit je einem Beispiel). Erweitern = eine Zeile."
    - "judge_dimensions hat eine DIMENSIONS_VERSION-Konstante; rubric_score.score_schema_version traegt sie pro Zeile (Vergleichbarkeit ueber die Zeit)."
    - "Die alten Noten-Spalten (coaching_score etc.) BLEIBEN als Spalten (kein blindes Drop, Punkt 20) aber werden vom Judge NICHT mehr befuellt — der Cutover (Plan 03) hoert auf, sie zu schreiben; Schild markiert sie als '[Alt-Engine, write-stop ab LLM-Bewerter]'."
    - "Das rubric_score-Tabellen-Schild + die neuen Spalten-Schilder sind aktuell (Punkt 23): Schreiber = services/judge_runner.py (Plan 03), Leser = routes/dashboard.py (Preview Plan 05)."
  artifacts:
    - path: "alembic/versions/0029_rubric_score_observations.py"
      provides: "Migration: ADD observations_jsonb + ratings_jsonb (idempotent) + Schild-COMMENTs, down_revision 0028"
      contains: "observations_jsonb"
    - path: "services/judge_dimensions.py"
      provides: "Feste Dimensions-Liste (~4) als versionierte Daten + BARS-Anker-Texte + DIMENSIONS_VERSION"
      contains: "DIMENSIONS_VERSION"
    - path: "database/models.py"
      provides: "RubricScore.observations_jsonb + ratings_jsonb + Alt-Spalten-Schild-Update"
      contains: "observations_jsonb"
  key_links:
    - from: "services/judge_dimensions.py DIMENSIONS"
      to: "services/judge_runner.py (Plan 03) Prompt-Bau"
      via: "Dimensions-Liste + BARS-Anker speisen den Judge-Prompt"
      pattern: "DIMENSIONS"
    - from: "alembic/versions/0029_rubric_score_observations.py"
      to: "database/models.py RubricScore.observations_jsonb"
      via: "Single-Source: COMMENT-Migration == models.py comment="
      pattern: "observations_jsonb"
---

<objective>
Die Schema-Umwidmung der `rubric_score`-Tabelle von "Note-Zahl" auf "Beobachtung + Beleg-Zitat + interne
grobe Auspraegung" (Soll-Verhalten §6, Entscheidung.md "Was rein, was raus, was intern") + die feste
versionierte Dimensions-Liste (~4) als Daten/Config.

Purpose: Der LLM-Verhaltens-Call (Plan 03) schreibt seine Beobachtungen mit woertlichem Beleg-Zitat je
fester Dimension hierher; die interne grobe Auspraegung (schwach/ok/stark) wird SEPARAT (unsichtbar)
gespeichert; der Outcome bleibt — wie schon im Bestand — getrennt in `calls.outcome`, NIE in rubric_score
dupliziert (Outcome-Leakage-Schutz). Diese Welle legt NUR Schema + Dimensions-Daten — KEIN LLM-Call (Plan 03),
KEINE Preview (Plan 05).

Output: Migration `0029_rubric_score_observations.py` (down_revision '0028') + RubricScore-ORM-Spalten +
`services/judge_dimensions.py` (feste Liste + BARS-Anker + Version).

**Komplexitaet: 🔴** (Schema-Umwidmung einer FORCE-RLS-Tabelle + Persistenz-Schicht — Cross-AI Pflicht).
**Leitsatz-2/Punkt-27-Selbstcheck:** Zwei JSONB-Spalten + eine Daten-Liste. KEIN Gewichts-Schema, KEINE
Aggregations-Tabelle, KEINE Score-Spalten neu. Das Simpelste, das die Beobachtungen + die interne Auspraegung
trennt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- RubricScore Ist-Stand (database/models.py:863-895): bestehende Noten-Spalten, die BLEIBEN aber -->
<!-- vom Judge nicht mehr befuellt werden. Die zwei neuen JSONB-Spalten kommen NEBEN diese. -->
<!-- JSON_TYPE/UUID_TYPE/text sind in models.py importiert (RubricScore nutzt sie bereits). -->

Bestehende Spalten (Auszug, models.py:872-887):
  call_id, conversation_log_id, session_mode, origin, coaching_score (Float, ALT),
  is_provisional, measured_weight_pct, unmeasured_dimensions, dimensions (JSON, ALT-Engine),
  status, tenant_id (FORCE-RLS, NOT NULL), payload_jsonb, score_schema_version (SmallInteger), created_at.
Partieller Unique-Index ux_rubric_score_live_call_id (call_id WHERE origin='live') — der Judge-UPSERT
(Plan 03) nutzt ihn weiter (idempotenter Re-Run). FORCE ROW LEVEL SECURITY (D-11).

Outcome: liegt in calls.outcome (separat). rubric_score hat KEINE outcome-Spalte und bekommt KEINE
(Outcome-Leakage-Schutz, Entscheidung.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/judge_dimensions.py — feste Dimensions-Liste (~4) als versionierte Daten + BARS-Anker</name>
  <files>services/judge_dimensions.py</files>

  <read_first>
    - services/rubric_dimensions.py (die ALTE Dimensions-Liste der Marker-Engine — als REFERENZ fuer Funktionalitaet/Naming, NICHT als Vorlage fuer Mechanik; die neue Datei traegt KEINE Gewichte/Scores)
    - Entscheidung.md "Klein anfangen: 4 statt 7 Dimensionen" + Soll-Verhalten §6 (welche Dimensionen: erst die erklaerbaren — offene vs. geschlossene Fragen — + die subjektiven: Gespraechsfuehrung, Einwand-Behandlung)
    - CLAUDE.md Umlaut-Regel: dimension-key = ASCII-Identifier; Anzeige-Name + Definition + BARS-Texte = User-/Prompt-facing → echte Umlaute OK
  </read_first>

  <action>
**Neue Datei `services/judge_dimensions.py` — reine Daten + eine Versions-Konstante. KEIN LLM-Call, KEIN
DB-Zugriff, KEINE Gewichte, KEINE Score-Rechnung (Leitsatz 2 / Punkt 27).**

Inhalt:
1. `DIMENSIONS_VERSION = 1` (SmallInteger-kompatibel; wird in rubric_score.score_schema_version geschrieben).
2. `DIMENSIONS` = Liste von ~4 Dimensions-Dicts (festgezurrt, "Liste festzurren, Bewertung locker lassen").
   Jedes Dict (ASCII keys):
   ```python
   {
     'key': 'fragen_offen_geschlossen',      # ASCII identifier — DB/Prompt-Anker
     'name': 'Offene vs. geschlossene Fragen',  # Anzeige (Umlaute OK)
     'definition': 'Ein Satz: Stellt der Berater offene Fragen (Was/Wie/Wozu), die den Kunden reden lassen, statt geschlossener Ja/Nein-Fragen?',
     'bars': {
       'schwach': 'Fast nur geschlossene/Suggestiv-Fragen; Kunde kommt kaum zu Wort. Beispiel: "Haetten Sie nicht auch gern mehr Sicherheit?"',
       'ok':      'Mischung; einige offene Fragen, aber oft wieder geschlossen. Beispiel: "Wie loesen Sie das heute? ... Also brauchen Sie X, oder?"',
       'stark':   'Konsequent offene Fragen, die den Kunden oeffnen. Beispiel: "Was waere fuer Sie der groesste Hebel?"',
     },
   }
   ```
   Konkret diese ~4 Start-Dimensionen (Soll-Verhalten §6 + Entscheidung.md):
   - `fragen_offen_geschlossen` — Offene vs. geschlossene Fragen (fast regel-checkbar, "erklaerbar").
   - `gespraechsfuehrung` — Gespraechsfuehrung/Steuerung (subjektiver Kern). **Hard-Cap-Hinweis in der
     Definition/BARS:** Weiterdruecken nach mehrfacher klarer Ablehnung = "nicht Verkauf, sondern Belaestigung"
     → deckelt diese Dimension auf hoechstens 'schwach' (Report-Ergaenzung 4). Plus Kaltakquise-Redeanteil
     55:45 als Norm (NICHT 43:57) in der BARS-'stark'-Beschreibung erwaehnen (Report-Ergaenzung 3).
   - `einwand_behandlung` — Einwand-Behandlung (subjektiver Kern; das Herz der Phase).
   - `gespraechs_eroeffnung` — Eroeffnung/Opener (erklaerbar; gibt dem Judge einen vierten, gut verankerbaren
     Punkt). *(Executor's Discretion: falls eine andere vierte erklaerbare Dimension klarer ist — z.B.
     `aktives_zuhoeren` — waehlen + im SUMMARY begruenden; Start ~4, erklaerbar, erweiterbar.)*
3. Eine kleine Helfer-Funktion `dimensions_for_prompt() -> str`, die die Liste als KLARTEXT-Block fuer den
   Judge-Prompt rendert (Dimensions-Name + Definition + BARS schwach/ok/stark) — "BARS als Klartext im Prompt"
   (Entscheidung.md Pflicht-Technik). Reiner String-Bau, kein LLM. Plan 03 ruft diese Funktion.

KEINE Gewichte, KEINE Gesamtnote, KEINE Aggregation. Kommentar am Dateikopf: "Feste Dimensions-Liste fuer den
LLM-Verhaltens-Bewerter (Beobachtung statt Note). Erweitern = ein Listen-Eintrag. KEINE Gewichte/Scores
(Urteil von Rechnung getrennt, Soll-Verhalten §6 / Entscheidung.md)."
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('services/judge_dimensions.py',encoding='utf-8').read()); print('AST-OK')"</automated>
    <grep>grep -c "DIMENSIONS_VERSION" services/judge_dimensions.py (>=1)</grep>
    <grep>grep -nE "'key':|'bars':|'definition':" services/judge_dimensions.py (>=4 dims je Schluessel)</grep>
    <grep>grep -nE "einwand_behandlung|gespraechsfuehrung|fragen_offen_geschlossen" services/judge_dimensions.py (3 Kern-Dims vorhanden)</grep>
    <grep>grep -nE "weight|gewicht|coaching_score|total" services/judge_dimensions.py (== 0 — KEINE Gewichte/Gesamtnote, Leitsatz 2)</grep>
    <grep>grep -c "def dimensions_for_prompt" services/judge_dimensions.py (== 1)</grep>
  </verify>

  <done>
services/judge_dimensions.py existiert: ~4 feste Dimensionen (key ASCII, name/definition/BARS mit je einem
Beispiel pro Stufe), DIMENSIONS_VERSION, dimensions_for_prompt(). Keine Gewichte/Scores/Gesamtnote. AST-OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration 0029 + RubricScore-ORM — observations_jsonb + ratings_jsonb + Alt-Spalten-Schild</name>
  <files>alembic/versions/0029_rubric_score_observations.py, database/models.py</files>

  <read_first>
    - alembic/versions/0028_calls_transcript_resolved.py (aus Plan 01 — Migrations-Stil; 0029 down_revision='0028')
    - database/models.py:863-895 (RubricScore voll: Spalten + __table_args__ + Tabellen-Schild:894 + Unique-Index)
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema rubric_score' (Live-Spalten — Beleg, dass observations_jsonb/ratings_jsonb noch NICHT existieren; JSONB-Typ-Konvention der Tabelle pruefen: payload_jsonb ist JSONB)
    - ssh ... 'bash scripts/inspect.sh migrations' → MUSS 0028 zeigen NACH Plan-01-Deploy (Reihenfolge: 0028 vor 0029). Wenn Prod noch 0027 → 0028 zuerst deployen (Plan 01), dann diese.
  </read_first>

  <action>
**(A) Migration `0029_rubric_score_observations.py` (down_revision '0028'), idempotent, HART kein lokales alembic:**
`upgrade()` op.execute-Bloecke:
1. `ALTER TABLE public.rubric_score ADD COLUMN IF NOT EXISTS observations_jsonb JSONB NOT NULL DEFAULT '{}'::jsonb`
   (Beobachtungen + woertliche Beleg-Zitate je Dimension; Form: `{dim_key: [{"beobachtung": str, "beleg_zitat": str}, ...]}`).
2. `ALTER TABLE public.rubric_score ADD COLUMN IF NOT EXISTS ratings_jsonb JSONB NOT NULL DEFAULT '{}'::jsonb`
   (INTERNE grobe Auspraegung schwach/ok/stark je Dimension; Form: `{dim_key: "schwach"|"ok"|"stark"}`). NIE an den
   Nutzer ausgegeben (Plan 05 zeigt nur observations_jsonb).
3. COMMENT ON COLUMN fuer beide (Single-Source zu models.py Task 2B). observations_jsonb-Schild: "Beobachtungen +
   WOERTLICHE Beleg-Zitate je fester Dimension (LLM-Verhaltens-Bewerter, Beobachtung statt Note). Form
   {dim_key:[{beobachtung,beleg_zitat}]}. SICHTBAR fuer den Nutzer (als KI-Einschaetzung gelabelt). Status: lebt
   (TAXO2 LLM-Bewerter). Schreibt services/judge_runner.py; liest routes/dashboard.py (Preview)." ratings_jsonb-Schild:
   "INTERNE grobe Auspraegung schwach/ok/stark je Dimension (Lern-Signal, Soll-Verhalten §6). NIE an den Nutzer
   ausgegeben. Form {dim_key:'schwach'|'ok'|'stark'}. Status: lebt (TAXO2 LLM-Bewerter, intern). Schreibt
   services/judge_runner.py; liest spaeter Korrelation/Lernen (post-Launch)."
4. COMMENT ON COLUMN fuer die ALT-Engine-Spalten, die ab Plan 03 nicht mehr befuellt werden (Punkt 20/23 —
   nicht droppen, sondern als write-stopped markieren): coaching_score, measured_weight_pct, unmeasured_dimensions,
   dimensions, is_provisional → Schild-Suffix " [ALT-Marker-Engine, write-stop ab LLM-Bewerter TAXO2 — nicht mehr
   befuellt; Cutover services/slow_lane.py Plan 03; nicht geloescht (Foundation-Register/Punkt 20)]."
5. COMMENT ON TABLE public.rubric_score erweitern: Zweck jetzt "Beobachtungen + Beleg-Zitate + interne Auspraegung
   (LLM-Bewerter), nicht mehr maschinelle Note"; Schreiber = services/judge_runner.py (Plan 03), Leser =
   routes/dashboard.py (Preview Plan 05).
`downgrade()`: DROP COLUMN IF EXISTS observations_jsonb / ratings_jsonb; Alt-Spalten-Schilder + Tabellen-Schild
auf Vor-0029-Stand zuruecksetzen.

**(B) models.py RubricScore — zwei neue Spalten + Schild-Updates (Single-Source, wortgleich zur Migration):**
- `observations_jsonb = Column(JSON_TYPE, nullable=False, default=dict, server_default='{}', comment="...")`
  (JSON_TYPE = die JSONB-Abbildung der Tabelle, wie payload_jsonb — grep verifizieren).
- `ratings_jsonb = Column(JSON_TYPE, nullable=False, default=dict, server_default='{}', comment="...")`.
- Die comment= der ALT-Spalten (coaching_score/measured_weight_pct/unmeasured_dimensions/dimensions/is_provisional)
  um das write-stop-Suffix ergaenzen (wortgleich zur Migration). KEINE Spalte droppen.
- Das __table_args__-Tabellen-Schild (models.py:894) erweitern (wortgleich zur COMMENT ON TABLE).
KEINE Logik. JSON_TYPE/text bereits importiert (RubricScore nutzt sie) — grep verifizieren, kein neuer Import.
  </action>

  <verify>
    <automated>python -c "import ast; [ast.parse(open(f,encoding='utf-8').read()) for f in ['alembic/versions/0029_rubric_score_observations.py','database/models.py']]; print('AST-OK')"</automated>
    <grep>grep -E "revision = '0029'|down_revision = '0028'" alembic/versions/0029_rubric_score_observations.py (beide Treffer)</grep>
    <grep>grep -c "ADD COLUMN IF NOT EXISTS observations_jsonb JSONB NOT NULL DEFAULT" alembic/versions/0029_rubric_score_observations.py (== 1)</grep>
    <grep>grep -c "ADD COLUMN IF NOT EXISTS ratings_jsonb JSONB NOT NULL DEFAULT" alembic/versions/0029_rubric_score_observations.py (== 1)</grep>
    <grep>grep -nE "observations_jsonb = Column|ratings_jsonb = Column" database/models.py (2 Treffer im RubricScore-Block)</grep>
    <grep>grep -n "write-stop ab LLM-Bewerter" database/models.py (Treffer — Alt-Spalten als write-stopped markiert, nicht geloescht)</grep>
    <grep>grep -nE "outcome" database/models.py | grep -i rubric (== 0 — KEINE outcome-Spalte in rubric_score, Leakage-Schutz)</grep>
    <note>HART Kein-Local-Dev: kein lokales alembic/Import. AST = Syntax. Schema-Beleg im Deploy-Gate (SCHILD-Guard gegen pg_description).</note>
  </verify>

  <done>
Migration 0029 (down_revision 0028) legt observations_jsonb + ratings_jsonb idempotent an + Schilder; ALT-Engine-
Spalten als write-stopped markiert (nicht geloescht, Punkt 20). RubricScore-ORM traegt beide neuen Spalten +
aktualisierte Schilder, Single-Source zur Migration, KEINE outcome-Spalte. AST-OK.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rubric_score ↔ Schreiber/Leser | FORCE ROW LEVEL SECURITY (D-11). Die neuen JSONB-Spalten erben die RLS. Diese Welle aendert nur das Schema, keinen Schreib-Pfad (Plan 03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HT-02b-01 | Info Disclosure | observations_jsonb enthaelt woertliche Transkript-Zitate (potenziell PII-nah) | mitigate | Transkript ist bereits anonymisiert (Tokenisierung, DSGVO §1); die Zitate stammen aus dem anonymisierten Transkript. FORCE-RLS tenant_isolation + call_id-FK ON DELETE CASCADE (DSGVO-Cleanup). Kein Roh-PII. |
| T-HT-02b-02 | Tampering | Outcome-Leakage ueber eine versehentliche outcome-Spalte | mitigate | rubric_score bekommt KEINE outcome-Spalte (grep-Acceptance == 0); Outcome bleibt in calls.outcome, NIE im Bewerter-Prompt (Plan 03 erzwingt das). |
| T-HT-02b-03 | DoS | Schema-Drift Migration↔ORM | mitigate | Single-Source (Punkt 23) + SCHILD-Guard im Deploy-Gate; grep-Acceptance prueft Wortgleichheit. |
</threat_model>

<verification>
## 5. Persistenz-Schicht-Verifikation (Punkt 21) — Welle 2

### Angefasste Tabellen
- `rubric_score` (Schema-Aenderung: +observations_jsonb +ratings_jsonb; Schild-Update Alt-Spalten + Tabelle).

### inspect.sh-Beleg (read_first, vom Executor zu ziehen)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema rubric_score'
→ PRE-Migration: observations_jsonb/ratings_jsonb existieren NICHT; payload_jsonb ist jsonb (Typ-Konvention bestaetigt);
  KEINE outcome-Spalte (Leakage-Schutz bestaetigt).
$ ssh ... 'bash scripts/inspect.sh migrations' → MUSS 0028 zeigen (nach Plan-01-Deploy).
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Verifiziert? |
|---|---|---|---|---|
| `observations_jsonb` | schreiben judge_runner (Plan 03), lesen dashboard Preview (Plan 05) | DB rubric_score.observations_jsonb (JSONB) | Batch am Call-Ende (Judge-Schritt, NACH transcript_resolved) | ✓ diese Welle (Schema); Schreiber Plan 03 |
| `ratings_jsonb` | schreiben judge_runner (Plan 03) | DB rubric_score.ratings_jsonb (JSONB, INTERN) | s.o. | ✓ |
| ALT coaching_score/dimensions | write-stop ab Plan 03; Bestand-Leser bleibt bis Preview-Umstellung | DB-Spalten (bleiben) | nicht mehr geschrieben ab Plan 03 | ✓ als write-stopped markiert |
| outcome | NICHT in rubric_score | DB calls.outcome (separat) | Bestand | ✓ Leakage-Schutz |

### Punkt 26
Diese Welle schreibt keine Lese-Logik. Der Judge (Plan 03) schreibt observations/ratings NUR im Call-Ende-Schritt
NACH transcript_resolved==True (Plan 01 Fan-In) — kein Voraus-Lauf.
</verification>

<success_criteria>
- services/judge_dimensions.py: ~4 feste Dimensionen (key ASCII + name + Ein-Satz-Definition + BARS schwach/ok/stark
  mit je einem Beispiel) + DIMENSIONS_VERSION + dimensions_for_prompt(); KEINE Gewichte/Scores/Gesamtnote.
- Migration 0029 (down_revision 0028) legt observations_jsonb + ratings_jsonb idempotent an + Schilder; ALT-Engine-
  Spalten write-stop-markiert (nicht geloescht, Punkt 20); KEINE outcome-Spalte.
- RubricScore-ORM Single-Source zur Migration; JSON_TYPE wiederverwendet, kein neuer Import.
- Leitsatz 2 / Punkt 27: zwei JSONB-Spalten + eine Daten-Liste, KEIN Gewichts-/Aggregations-Apparat.
- Punkt 21 (Persistenz) + Punkt 23 (Schild aktuell) belegt.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-02-SUMMARY.md`
</output>
