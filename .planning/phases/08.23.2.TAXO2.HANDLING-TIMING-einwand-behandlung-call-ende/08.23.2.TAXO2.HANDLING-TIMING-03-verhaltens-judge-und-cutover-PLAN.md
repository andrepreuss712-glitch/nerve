---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - services/judge_runner.py
  - services/slow_lane.py
  - tests/test_judge_runner.py
  - tests/test_coaching_score_cutover.py
autonomous: true
requirements: [GOAL-Beobachtung, GOAL-Anstoss, CONTEXT-Defer-Anstoss, CONTEXT-Outcome-getrennt, CONTEXT-Verhaltens-Call, CONTEXT-compute_rubric-retire]
must_haves:
  truths:
    - "EIN LLM-Call (Sonnet) liest am Call-Ende das ganze Transkript der Reihe nach + Markierungen (wer/was/wann, *ewb button*, NERVE-Erkennungen/Phase/Aussetzer) + Profil + Pre-Call-Briefing — und liefert je fester Dimension Beobachtungen mit woertlichem Beleg-Zitat + interne grobe Auspraegung schwach/ok/stark. KEINE Zahl, KEINE Gewichte, KEINE Gesamtnote."
    - "Outcome (calls.outcome) ist PHYSISCH NICHT im Prompt — der Judge sieht es nicht."
    - "Der Judge kennt die NERVE-Vorschlaege NICHT (kein suggestion_reactions im Prompt) — Bias-Schutz; die Adoption ist ein SEPARATER Call (Plan 04)."
    - "Pflicht-Technik: Beleg-VOR-Note (erzwungene JSON-Reihenfolge beleg_zitat -> beobachtung -> auspraegung), BARS als Klartext im Prompt, erzwungenes JSON-Schema (Anthropic Tool-Use). Lost-in-the-Middle: Transkript getaggt + Dimensions-Rubrik an ANFANG UND ENDE des Prompts."
    - "Der Cutover ist sauber (Punkt 20): _compute_rubric_step (Marker-Engine) wird als rubric_score-Quelle abgeloest — der registrierte Call-Ende-Schritt ruft jetzt den Judge an (UPSERT observations_jsonb + ratings_jsonb), NICHT mehr compute_rubric. compute_rubric/rubric_engine bleibt im Baum (Foundation), wird aber nicht mehr als Noten-Quelle aufgerufen — dokumentiert."
    - "Der Judge-Anstoss laeuft NUR async im Slow-Lane-Call-Ende-Schritt (kein LLM in der Fast/Live-Lane, Bau-Regel 1) und NUR nachdem transcript_resolved==True (Plan 01 Fan-In) — der Judge laeuft dem Transkript-Write nie voraus (Punkt 26)."
    - "Der rubric_score-UPSERT laeuft unter der M-4-Daemon-GUC-Klammer (FORCE-RLS-Write) — wie der bisherige _compute_rubric_step (gleiche ctx/GUC, kein neuer RLS-Pfad)."
    - "Das Transkript an Sonnet ist anonymisiert (Bestand); der Call laeuft ueber den EU-Daten-Residenz-Pfad (DSGVO) — die konkrete Residenz-Wahl (direkter Anthropic-Client vs. Bedrock Frankfurt) ist als offene Produkt-/Compliance-Frage markiert (Ist-Code nutzt anthropic.Anthropic direkt)."
    - "Der write-stop von coaching_score/dimensions ist im Leser-Strang routes/performance.py (999.2-Schild-Referenz) bewusst geprueft (Punkt 21 Cross-Layer): performance.py liest diese Felder HEUTE GAR NICHT (grep-belegt) → der write-stop bricht keinen lebenden Leser; die 999.2-Anzeige bleibt ein NOCH-NICHT-gebauter Future-Reader, der spaeter auf observations_jsonb/ratings_jsonb gebaut wird."
  artifacts:
    - path: "services/judge_runner.py"
      provides: "run_behavior_judge(call, events, db): Prompt-Bau (Transkript-Tags + Marker + Profil + Briefing, Rubrik Anfang+Ende, KEIN Outcome, KEINE Vorschlaege) + EIN Sonnet-Tool-Use-Call + Parse + UPSERT observations_jsonb/ratings_jsonb"
      contains: "run_behavior_judge"
    - path: "services/slow_lane.py"
      provides: "Cutover: der registrierte Call-Ende-Schritt ruft run_behavior_judge statt compute_rubric (Anstoss); compute_rubric-Aufruf als Noten-Quelle entfernt"
      contains: "run_behavior_judge"
    - path: "tests/test_judge_runner.py"
      provides: "Tests: Prompt enthaelt KEIN Outcome + KEINE Vorschlaege; Rubrik an Anfang+Ende; Tool-Use erzwingt Schema; Parse->UPSERT-Form; Anstoss gated auf transcript_resolved"
      contains: "run_behavior_judge"
  key_links:
    - from: "services/slow_lane.py (Call-Ende-Schritt)"
      to: "services/judge_runner.py run_behavior_judge"
      via: "register_call_end_step ruft den Judge-Anstoss (Cutover von _compute_rubric_step)"
      pattern: "run_behavior_judge"
    - from: "services/judge_runner.py"
      to: "rubric_score.observations_jsonb / ratings_jsonb"
      via: "UPSERT unter M-4-GUC (Tenant-GUC im Merge-Gate gesetzt, Bestand)"
      pattern: "observations_jsonb"
    - from: "services/judge_runner.py"
      to: "services/judge_dimensions.py dimensions_for_prompt"
      via: "feste Dimensions-Liste + BARS speisen den Prompt"
      pattern: "dimensions_for_prompt"
---

<objective>
Der Kern der Phase: der **LLM-Verhaltens-Call (der Judge)**. EIN Sonnet-Call liest am Call-Ende (async, Slow Lane)
das ganze Transkript der Reihe nach + Markierungen + Profil + Pre-Call-Briefing und liefert je fester Dimension
**Beobachtungen mit woertlichem Beleg-Zitat** + die **interne** grobe Auspraegung schwach/ok/stark. KEINE Zahl.
Plus der **saubere Cutover** weg von der Marker-Engine `compute_rubric` (Punkt 20 — ablösen, nicht blind loeschen).

Purpose: Loest die maschinelle Marker-/Anker-Noten-Engine ab (Soll-Verhalten §6). Der Judge **beobachtet +
belegt, verrechnet NICHT** (kein Gewicht, keine Gesamtnote im Modell — Urteil von Rechnung getrennt).

Output: `services/judge_runner.py` (Prompt-Bau + EIN Tool-Use-Sonnet-Call + Parse + UPSERT) + Cutover in
slow_lane.py (der Call-Ende-Schritt stoesst den Judge an statt compute_rubric) + Tests.

**Komplexitaet: 🔴** — LLM-Pipeline + Cutover einer FORCE-RLS-Schreibstelle + DSGVO. Cross-AI Pflicht.

<decision id="D-RESIDENZ" status="OPEN — checkpoint:decision (Plan 05 / André)">
## DSGVO-Daten-Residenz des Judge-Calls — offene Produkt-Entscheidung (NICHT vom Planner geraten)
**Ist-Stand (grep-belegt):** Der gesamte Live-/Post-Call-Code nutzt `claude_client = anthropic.Anthropic(api_key=...)`
(claude_service.py:30) — den DIREKTEN Anthropic-API-Client, NICHT AWS Bedrock. Es existiert KEIN Bedrock-Client im
Repo (grep: kein AnthropicBedrock/boto3/bedrock). Das Vault/Soll sagt "Claude via Bedrock Frankfurt" (DSGVO) —
das ist eine Soll/Ist-Luecke, die GROESSER ist als diese Phase (sie betraefe ALLE Claude-Calls, nicht nur den Judge).
**Planner-Entscheidung (Leitsatz 2 / kein-Guessing):** Der Judge nutzt JETZT den BESTEHENDEN `claude_client`-Pfad
(konsistent mit allen anderen Post-Call-Calls — KEIN neuer Bedrock-Client erfunden). Die Residenz-Frage
(direkter Anthropic-Client EU-tauglich? Bedrock-Migration aller Calls als eigene Phase?) wird als
**checkpoint:decision an André** in Plan 05 gestellt — es ist eine Produkt-/Compliance-Entscheidung (🟢 DEINE WAHL),
KEINE Code-Innerei, und sie gehoert NICHT in diesen Judge-Plan eingebaut (sonst Scope-Creep auf die ganze App).
Das Transkript ist bereits anonymisiert (DSGVO §1) — der Judge fuegt KEIN neues Roh-PII hinzu.
</decision>

<deferred_ideas>
## Bewusst NICHT in dieser Phase (Out-of-Scope, Soll-Verhalten §6 / CONTEXT)
- Gewichte, Gesamtnote, sichtbare Ziffer, Self-Consistency (Mehrfachlaeufe), Aggregation/Korrelation → Phase 2.
- Cross-Family-Zweit-Urteil (Gemini/GPT auf Stichprobe gegen Self-Enhancement) → Phase-2-TICKET (nur notieren).
- κ-/Experten-Validierungs-Apparat → Phase 2 (Launch-Huerde = halluzinierte-Belege-Check, Plan 05).
- "Wiederkehrender Einwand → tiefere EWB"-LIVE-Antwort → TAXO3 (RECUR-EWB-DEEPER). Hier nur: der Judge DARF
  Wiederholung als Beobachtung nennen, baut aber KEINE Live-Antwort.
- Bedrock-Frankfurt-Migration aller Claude-Calls → eigene Phase (D-RESIDENZ checkpoint Plan 05).
- 999.2-Coaching-Anzeige (routes/performance.py als Leser von coaching_score/dimensions, laut rubric_score-Schild
  models.py:894) → NOCH NICHT gebaut (performance.py liest diese Felder heute nicht, siehe <performance_reader_verdict>).
  Der Future-Reader 999.2 wird spaeter auf observations_jsonb/ratings_jsonb gebaut, NICHT in dieser Phase.
</deferred_ideas>
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-CONTEXT.md
@CLAUDE.md
@services/slow_lane.py
@services/judge_dimensions.py

<interfaces>
<!-- Die exakten Vertraege, die der Executor anfasst. -->

services/slow_lane.py — der Cutover-Punkt (IST):
- _compute_rubric_step(ctx) (:461-530): laeuft INNERHALB der M-4-GUC-Klammer des Merge-Gates; ctx = {call, events,
  db, high_conf, not_gradable_reason}. Ruft compute_rubric (:507) + _upsert_rubric_score (:516). Wird via
  register_call_end_step(_compute_rubric_step) (:821) eingehaengt; _call_end_merge ruft run_call_end_steps(ctx)
  (:607) innerhalb der GUC. → HIER wird der Judge angestossen statt compute_rubric.
- run_call_end_steps(ctx) (:90-104): KEIN per-Schritt try/except (all-or-nothing pro Call); ein Fehler propagiert,
  der Re-Run ist idempotent (UPSERT). Der Consumer/Merge-Gate isoliert pro Call.
- Das Audio-Gate D-09 (not_gradable_reason 'poor_audio_health'/'too_few_high_confidence_events', :575-578) bleibt:
  bei not_gradable_reason schreibt der Schritt status=not_gradable + KEIN Judge-Call (kein LLM auf Muell).
- _upsert_rubric_score(db, *, ...) (:426-458): ON CONFLICT (call_id) WHERE origin='live' — der Judge nutzt
  denselben UPSERT, erweitert um observations_jsonb/ratings_jsonb (oder ein analoger UPSERT in judge_runner).

services/judge_dimensions.py (Plan 02): DIMENSIONS, DIMENSIONS_VERSION, dimensions_for_prompt() -> str.

services/claude_service.py:30 — der bestehende Client (Judge nutzt DIESEN, D-RESIDENZ):
  claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)
  Aufruf-Muster (Bestand, z.B. :533): claude_client.messages.create(model=..., max_tokens=..., system=..., messages=[...]).

config.py:76 — MODEL_POSTCALL_ANALYSIS = "claude-sonnet-4-5" (der Post-Call-Sonnet; der Judge nutzt diesen
  ODER eine eigene MODEL_JUDGE-ENV mit Default sonnet — Punkt 12 Override).

services/prompt_pipeline.py:111 — build_profile_context(user_id, mode='cold_call', sid=None, profile_id=None) -> str
  liefert das 9-Sektionen-Profil INKL. Sektion 8 PreCall-Briefing + Sektion 9 Lead-Kontext (der legitime "Referenz"-
  Massstab fuer den Judge). Der Judge ruft diese Funktion (Profil + Briefing in EINEM Aufruf).

Transkript + Marker (woher): transcript_segments (conversation_log_id, ts_ms, speaker berater/kunde/system, text)
  — die *ewb button*-Marker stehen IM Segment-Text (kunde-Segment "<label> *ewb button*"); intent_event liefert
  NERVE-Erkennungen/Phase/triggering_text; calls.audio_health_score/Aussetzer als Markierung. (Executor liest die
  echten Spalten via inspect.sh, read_first.)
</interfaces>

<performance_reader_verdict>
<!-- WARNING 2 (plan-checker) — stiller Leser-Strand routes/performance.py (Punkt 21 Cross-Layer). -->
<!-- Das rubric_score-Tabellen-Schild (models.py:894) nennt routes/performance.py (999.2) als Leser von -->
<!-- coaching_score/dimensions. Dieser Plan schreibt coaching_score NICHT mehr (write-stop, Plan 02). -->
<!-- Frage: laeuft performance.py fuer NEUE Calls dann stale/NULL? -->

GREP-BELEG (Planner, gegen den synchronen Repo-Stand, vor Execute zu bestaetigen):
  grep -n "coaching_score\|rubric_score\|RubricScore\|dimensions" routes/performance.py  → 0 Treffer.
  performance.py liest aktuell ausschliesslich: ConversationLog.kb_end/result/einwaende_gesamt/einwaende_behandelt/
  gegenargument_details + Call.outcome/outcome_source. KEIN coaching_score, KEIN rubric_score, KEIN dimensions.

VERDIKT = (a)/(b) — NICHT (c):
  Das models.py:894-Schild "liest routes/dashboard.py + performance.py (999.2)" ist fuer performance.py ein
  ASPIRATIONALES/Future-Schild (999.2 = Backlog-Anzeige, NOCH NICHT gebaut). performance.py ist HEUTE KEIN Leser
  von coaching_score/rubric_score/dimensions. Daher:
    - (a) trifft auf den IST-Code zu: kein lebender Leser dieser Felder in performance.py → der write-stop von
      coaching_score (Plan 02/03) bricht NICHTS Lebendes in performance.py (weder neue noch alte Calls).
    - (b) der spaetere 999.2-Reader (wenn gebaut) wird auf observations_jsonb/ratings_jsonb gebaut, NICHT auf
      coaching_score — bewusster Uebergang, hier als Future notiert.
  (c) — performance.py MUSS in dieser Phase umgestellt werden — ist NICHT noetig (es gibt nichts umzustellen).

KONSEQUENZ FUER DEN EXECUTOR:
  - Den grep `grep -n "coaching_score\|rubric_score\|dimensions" routes/performance.py` im Execute NOCHMAL ausfuehren
    (Repo kann zwischen Plan + Execute driften) und das Ergebnis (erwartet: 0 Treffer) im SUMMARY festhalten.
  - Bestaetigt der grep weiterhin 0 → die bewusste Entscheidung (a/b) ins SUMMARY/Foundation-Register schreiben:
    "performance.py liest coaching_score/dimensions nicht; 999.2-Anzeige ist Future-Reader auf observations/ratings.
    Das models.py:894-Schild zaehlt performance.py als Future-Leser — bewusst belassen, KEIN stiller Bruch."
  - NUR falls der grep wider Erwarten Treffer zeigt (Repo-Drift, Case c): KEIN stilles Weiterbauen — als kleines
    Folge-Ticket "performance.py auf observations_jsonb/ratings_jsonb umstellen" benennen + im SUMMARY flaggen.
</performance_reader_verdict>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/judge_runner.py — Prompt-Bau (KEIN Outcome, KEINE Vorschlaege, Rubrik Anfang+Ende, Tags) + EIN Tool-Use-Sonnet-Call + Parse</name>
  <files>services/judge_runner.py, tests/test_judge_runner.py</files>

  <behavior>
    - Test (kein Outcome): der gebaute Prompt-String enthaelt NIRGENDS calls.outcome / 'meeting_booked' / 'no_interest' / 'contract_signed' / 'callback' / 'wrong_person' (physische Outcome-Trennung). Runtime-Assertion auf den Prompt-Text.
    - Test (keine Vorschlaege): der Prompt enthaelt KEINEN suggestion_reactions-Inhalt / kein 'NERVE schlug vor' (Bias-Schutz; Adoption ist Plan 04).
    - Test (Rubrik Anfang+Ende): dimensions_for_prompt()-Block kommt MINDESTENS zweimal vor (Lost-in-the-Middle: Anfang UND Ende).
    - Test (Transkript getaggt): jede Transkript-Zeile traegt einen Tag (z.B. [#1 berater 0ms] ...), inkl. *ewb button*-Markierung, in Sprech-Reihenfolge ts_ms ASC.
    - Test (Tool-Use erzwingt Schema): der messages.create-Call wird mit tools + tool_choice (forced) aufgerufen; der erzwungene Schema verlangt pro Dimension die Reihenfolge beleg_zitat -> beobachtung -> auspraegung (Beleg-VOR-Note). Monkeypatch des Clients, Assertion auf die uebergebenen kwargs.
    - Test (Parse->UPSERT-Form): aus einer gemockten Tool-Antwort baut der Parser observations_jsonb {dim_key:[{beobachtung,beleg_zitat}]} + ratings_jsonb {dim_key:'schwach'|'ok'|'stark'} (auspraegung NUR intern, NICHT in observations).
  </behavior>

  <read_first>
    - services/claude_service.py:30 + :525-560 (claude_client + ein bestehendes messages.create-Muster: model/max_tokens/system/messages) — der Judge nutzt DIESEN Client (D-RESIDENZ)
    - services/prompt_pipeline.py:111-175 (build_profile_context — Profil+Briefing in einem Aufruf; mode aus call.call_mode, sid aus conversation_log)
    - services/judge_dimensions.py (Plan 02 — DIMENSIONS/dimensions_for_prompt/DIMENSIONS_VERSION)
    - config.py:76 (MODEL_POSTCALL_ANALYSIS Sonnet) + CLAUDE.md Punkt 12 (ENV-Override → MODEL_JUDGE)
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample transcript_segments' (echte Form: ts_ms/speaker/text, *ewb button*-Marker im kunde-Segment-Text)
    - ssh ... 'bash scripts/inspect.sh sample intent_event 12' (NERVE-Erkennungen/Phase/triggering_text als Marker-Quelle)
    - **Context7 (MCP, Pflicht bei SDK-Call):** Anthropic Python SDK — messages.create mit tools + tool_choice fuer erzwungenes JSON (structured output via Tool-Use). Das exakte tool_choice-Format (type='tool', name=...) gegen die installierte anthropic-Version verifizieren BEVOR der Call finalisiert wird (SDK-Drift, CLAUDE.md MCP-Trigger).
  </read_first>

  <action>
**RED → GREEN. Erst die behavior-Tests (rot), dann der Code. Neue Datei services/judge_runner.py.**

**(A) Prompt-Bau `_build_judge_prompt(call, events, profile_briefing, segments) -> (system_str, user_str)`:**
- **System-Prompt:** Rolle = "Du beobachtest + belegst Verhalten in einem Verkaufsgespraech, du VERRECHNEST NICHT.
  KEINE Note, KEINE Zahl, KEINE Gesamtbewertung. Pro Dimension: erst das WOERTLICHE Beleg-Zitat aus dem Transkript,
  dann die Beobachtung, dann die grobe Auspraegung schwach/ok/stark." (Beleg-VOR-Note + Urteil-von-Rechnung-getrennt.)
  Plus die Decentering-Anweisung gegen Outcome-Bias ("bewerte NUR das beobachtbare Verhalten; ein ablehnender Kunde
  ist KEINE schlechte Leistung") UND die Laengen-Neutralitaet (Verbosity-Bias: nicht der laengere/fluessigere Sprecher
  ist besser). Plus Kaltakquise-Redeanteil 55:45 als Norm (NICHT 43:57) + Hard-Cap (Weiterdruecken nach mehrfacher
  klarer Ablehnung deckelt Gespraechsfuehrung) — beides kommt ohnehin aus den BARS-Texten (Plan 02), hier nochmal kurz.
- **dimensions_for_prompt()** (Plan 02, BARS als Klartext) wird ZWEIMAL eingebettet: einmal am ANFANG des
  System-/User-Prompts, einmal am ENDE (Lost-in-the-Middle, Report-Ergaenzung 2).
- **User-Prompt:** (1) Profil + Pre-Call-Briefing (build_profile_context — der legitime Massstab). (2) Das **getaggte
  Transkript** in Sprech-Reihenfolge (ts_ms ASC): jede Zeile `[#<i> <speaker> <ts_ms>ms] <text>`, *ewb button*-Marker
  bleibt im Text (= "vom Berater geflaggter Kunden-Einwand"). (3) NERVE-Erkennungen/Phase/Aussetzer als kompakte
  Marker-Liste (aus events + calls.audio_health_score). **(4) STRIKT NICHT enthalten: calls.outcome, suggestion_reactions
  (NERVE-Vorschlaege).** Diese werden GAR NICHT geladen/uebergeben (physische Trennung — nicht nur "im Prompt weggelassen",
  sondern die Funktion nimmt sie nicht entgegen).
- Marker-Assemblierung in einer kleinen Helfer-Funktion `_assemble_markers(events, call) -> str` (wer/was/wann,
  Phase, Aussetzer). KEIN Outcome, KEINE Vorschlaege.

**(B) Der Call `run_behavior_judge(call, events, db) -> dict`:**
- mode = call.call_mode; sid = call.conversation_log_id; profile_briefing = build_profile_context(call.user_id, mode, sid).
- segments = db.query(TranscriptSegment).filter(conversation_log_id==sid).order_by(ts_ms.asc(), id.asc()).all().
- prompt = _build_judge_prompt(...).
- EIN `claude_client.messages.create(model=MODEL_JUDGE, max_tokens=..., temperature=0, system=system_str,
  messages=[{role:'user', content:user_str}], tools=[JUDGE_TOOL], tool_choice={'type':'tool','name':'record_observations'})`
  — **forced Tool-Use** als erzwungenes JSON-Schema (Context7-verifiziertes Format). MODEL_JUDGE = ENV-Override,
  Default config.MODEL_POSTCALL_ANALYSIS (Sonnet). temperature=0 (Bewertung).
- JUDGE_TOOL input_schema: pro Dimension (DIMENSIONS keys) ein Objekt-Array mit Feld-Reihenfolge
  `beleg_zitat` (string, woertlich aus Transkript) → `beobachtung` (string) → `auspraegung` (enum schwach|ok|stark).
  Der Schema-Beschreibungstext erzwingt "beleg_zitat MUSS woertlich aus dem Transkript stammen".
- **Parse** `_parse_judge_output(tool_input) -> (observations_jsonb, ratings_jsonb)`: observations = {dim:[{beobachtung,
  beleg_zitat}]}; ratings = {dim: auspraegung} (auspraegung NUR nach ratings, NIE in observations — sichtbar/intern getrennt).
- **Return** ein dict {observations_jsonb, ratings_jsonb, dimensions_version=DIMENSIONS_VERSION, status='judged'} —
  der UPSERT passiert in Task 2 (Cutover, unter der GUC-Klammer). run_behavior_judge macht KEINEN DB-Write selbst
  (saubere Trennung: Prompt+Call+Parse hier; persistenter Write im GUC-geklammerten Schritt).
- **Fehler-Mantel:** bei LLM-/Parse-Fehler → return {status:'judge_failed', error:...} (der Schritt schreibt dann
  status=judge_failed in rubric_score, KEIN Crash des Consumers — die all-or-nothing-Semantik liegt im Merge-Gate).
- **Latenz (Punkt 25):** dieser Call laeuft NUR async im Slow-Lane-Call-Ende-Schritt (Task 2), NIE in der Live-Lane
  (Bau-Regel 1). Latenz egal (CONTEXT/Punkt 25 — Slow Lane).
  </action>

  <verify>
    <automated>python -c "import ast; [ast.parse(open(f,encoding='utf-8').read()) for f in ['services/judge_runner.py','tests/test_judge_runner.py']]; print('AST-OK')"</automated>
    <grep>grep -c "def run_behavior_judge" services/judge_runner.py (== 1)</grep>
    <grep>grep -nE "outcome|meeting_booked|no_interest|contract_signed" services/judge_runner.py (== 0 — Outcome physisch raus)</grep>
    <grep>grep -nE "suggestion_reaction|NERVE schlug|Vorschlag" services/judge_runner.py (== 0 — keine Vorschlaege, Bias-Schutz)</grep>
    <grep>grep -nE "tool_choice|tools=" services/judge_runner.py (Treffer — forced Tool-Use / erzwungenes Schema)</grep>
    <grep>grep -c "dimensions_for_prompt" services/judge_runner.py (>=2 Aufruf-/Einbettungs-Stellen ODER Doppel-Einbettung im String — Anfang+Ende)</grep>
    <grep>grep -nE "def test_prompt_has_no_outcome|def test_prompt_has_no_suggestions|def test_rubric_at_start_and_end|def test_forced_tool_use|def test_parse_separates_ratings" tests/test_judge_runner.py (5 Treffer)</grep>
    <grep>grep -c "getsource\|assert '.*' in open(" tests/test_judge_runner.py (== 0 — keine Source-Presence-Tests)</grep>
  </verify>

  <done>
services/judge_runner.py: run_behavior_judge baut den Prompt (Profil+Briefing, getaggtes Transkript ts_ms ASC,
Marker; Rubrik Anfang+Ende; KEIN Outcome, KEINE Vorschlaege), feuert EINEN forced-Tool-Use-Sonnet-Call (temp=0),
parst zu observations_jsonb + ratings_jsonb (Auspraegung nur intern), kein DB-Write. Tests gruen-faehig (Runtime).
Context7 fuer das Tool-Use-Format genutzt. AST-OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cutover in slow_lane.py — der Call-Ende-Schritt stoesst den Judge an statt compute_rubric (Punkt 20)</name>
  <files>services/slow_lane.py, tests/test_coaching_score_cutover.py</files>

  <read_first>
    - services/slow_lane.py:461-530 (_compute_rubric_step — der abzuloesende Schritt: Audio-Gate D-09 :480-497 BLEIBT; der compute_rubric-Aufruf :499-530 wird durch den Judge-Anstoss ersetzt)
    - services/slow_lane.py:533-607 (_call_end_merge: F-02-Gate, audio_health_resolved-Gate :566, M-4-GUC-Klammer, run_call_end_steps(ctx) :607) — HIER muss zusaetzlich das transcript_resolved-Gate rein (Plan-01-Fan-In), analog audio_health_resolved
    - services/slow_lane.py:426-458 (_upsert_rubric_score — um observations_jsonb/ratings_jsonb erweitern ODER ein analoger Judge-UPSERT)
    - services/slow_lane.py:817-821 (register_call_end_step(_compute_rubric_step) — der Einhaeng-Punkt)
    - tests/test_coaching_score_cutover.py (Bestands-Merge-/Step-Tests — die compute_rubric-Erwartung wird auf den Judge-Anstoss umgestellt; Fixture transcript_resolved=True analog audio_health_resolved)
    - **WARNING 2 / Leser-Strang (Punkt 21):** grep -n "coaching_score\|rubric_score\|dimensions" routes/performance.py — ERWARTET 0 Treffer (Planner-Beleg, siehe <performance_reader_verdict>). Das BESTAETIGT, dass der write-stop von coaching_score in DIESEM Task keinen lebenden Leser in performance.py bricht. Den grep im Execute ausfuehren + das Ergebnis (a/b/c) im SUMMARY dokumentieren (siehe Task 2 (D)).
  </read_first>

  <action>
**Control-Flow-Audit zuerst (Punkt 14, siehe <control_flow_audit>), DANN editieren.**

**(A) transcript_resolved-Gate im Merge (Plan-01-Fan-In, Punkt 26).** In `_call_end_merge`, DIREKT nach dem
audio_health_resolved-Gate (:566-567), analog:
```python
        if not getattr(call, 'transcript_resolved', False):
            return  # Transkript noch nicht festgeschrieben -> Judge wuerde gegen leeres Transkript bewerten (Punkt 26)
```
Block-Kommentar oben am Merge: jetzt VIER Vorbedingungen (ended + 0 pending + audio_resolved + transcript_resolved).
KEIN Deadlock: api_beenden setzt transcript_resolved IMMER True (resolved-als-absent, Task 3). [Plan-01-Naht.]

**(B) Cutover des registrierten Schritts (Punkt 20 — ablösen, nicht blind loeschen).** Im Schritt (`_compute_rubric_step`,
umbenennen zu `_judge_step` ODER Body ersetzen — Executor's Discretion, aber der register-Aufruf :821 muss auf den
neuen/gleichen Namen zeigen):
- Das **Audio-Gate D-09 BLEIBT** (not_gradable_reason → status=not_gradable, KEIN Judge-Call: kein LLM auf Muell-Audio).
- Der **compute_rubric-Block (:499-530) wird ERSETZT** durch:
  ```python
  from services.judge_runner import run_behavior_judge
  result = run_behavior_judge(call, events, db)
  # UPSERT der Beobachtungen + internen Auspraegung (KEINE Zahl). Laeuft unter der M-4-GUC (Merge-Gate gesetzt).
  _upsert_rubric_score(db, call_id=call.id, conversation_log_id=call.conversation_log_id,
                       session_mode=mode_key, tenant_id=tenant_id,
                       observations=result.get('observations_jsonb') or {},
                       ratings=result.get('ratings_jsonb') or {},
                       dimensions_version=result.get('dimensions_version'),
                       status=result.get('status') or 'judged')
  db.commit()
  ```
  (Erweitere `_upsert_rubric_score` um observations/ratings/dimensions_version-Kwargs → schreibt observations_jsonb/
  ratings_jsonb/score_schema_version; die ALT-Spalten coaching_score/dimensions etc. werden NICHT mehr gesetzt
  (write-stop, Plan 02). Bei status=judge_failed: status schreiben, observations/ratings = {} — kein Crash.)
- **compute_rubric / rubric_engine.py NICHT loeschen** (Foundation, Punkt 20). Kommentar am alten Aufruf-/
  Registrier-Punkt: "compute_rubric (Marker-Engine) als Noten-Quelle abgeloest durch run_behavior_judge (LLM-Bewerter,
  Soll-Verhalten §6); rubric_engine bleibt im Baum als Foundation, kein lebender Noten-Aufruf mehr." Eintrag fuers
  Foundation-Code-Register im SUMMARY (Pruning-Notiz 3a/3b).
- Der register_call_end_step-Aufruf (:821) zeigt auf den Judge-Schritt. **Bau-Regel 1:** der Judge laeuft NUR hier
  (Slow-Lane-Consumer, async) — KEIN LLM in der Fast/Live-Lane. **M-4:** der UPSERT laeuft unter der bestehenden
  GUC-Klammer des Merge-Gates (FORCE-RLS rubric_score-Write) — unveraendert.

**(C) Tests (tests/test_coaching_score_cutover.py):**
- Den Bestands-Test `test_compute_rubric_step_registered` umstellen: der registrierte Schritt ruft jetzt
  run_behavior_judge (monkeypatch run_behavior_judge → Assertion, dass _upsert_rubric_score mit observations/ratings
  aufgerufen wird, NICHT mit coaching_score/dimensions der Alt-Engine).
- `test_merge_waits_for_transcript_resolved`: transcript_resolved=False → Merge feuert NICHT (auch bei ended + 0
  pending + audio_resolved). Plus Fixture-Default transcript_resolved=True in den Bestands-Merge-Tests (sonst
  falsch-rot durch das neue Gate-Bit — exakt audio_health_resolved-Muster).
- `test_judge_step_skips_on_not_gradable`: bei not_gradable_reason → status=not_gradable, run_behavior_judge NICHT
  aufgerufen (kein LLM auf Muell-Audio). Runtime-Assertion, kein Source-Presence.

**(D) Leser-Strang performance.py verifizieren + bewusst entscheiden (WARNING 2 / Punkt 21 — KEIN Bau, nur Beleg):**
- Fuehre aus: `grep -n "coaching_score\|rubric_score\|dimensions" routes/performance.py`. ERWARTET (Planner-Beleg):
  **0 Treffer** — performance.py liest diese Felder heute nicht (siehe <performance_reader_verdict>).
- **Wenn 0 Treffer (Fall a/b, erwartet):** schreibe die bewusste Entscheidung ins SUMMARY/Foundation-Register:
  "write-stop coaching_score bricht performance.py NICHT (kein lebender Leser); die 999.2-Anzeige (laut
  rubric_score-Schild models.py:894 ein Leser) ist ein NOCH-NICHT-gebauter Future-Reader, der spaeter auf
  observations_jsonb/ratings_jsonb gebaut wird — bewusst belassen, kein stiller Bruch." KEIN Code in performance.py.
- **Nur falls Treffer (Fall c, Repo-Drift gegen den Planner-Beleg):** KEIN stilles Weiterbauen — benenne ein kleines
  Folge-Ticket "routes/performance.py auf observations_jsonb/ratings_jsonb umstellen (coaching_score abgeloest)" und
  flagge es im SUMMARY. Diese Phase stellt performance.py NICHT um (Scope).
  </action>

  <control_flow_audit>
**Punkt 14 — 4-Layer (Code-Insert in _call_end_merge + Schritt-Body):**
- **Layer 1 (lokal):** _call_end_merge hat early returns: call None (:553), ended_at None (:556), pending!=0 (:558),
  audio_health_resolved False (:566). Das neue transcript_resolved-Gate reiht sich DIREKT danach ein (vor der M-4-GUC).
  Der Schritt-Body laeuft in run_call_end_steps INNERHALB der GUC (:607) — der Judge-UPSERT erbt die Tenant-GUC.
- **Layer 2 (Skelett):** api_beenden setzt Flags+put → Consumer → _call_end_merge (4 Gates) → M-4-GUC set → ctx bauen
  → run_call_end_steps → _judge_step (Audio-Gate D-09 → Judge-Call → UPSERT) → commit → GUC reset. Der Judge sitzt
  NACH allen 4 Gates → Transkript garantiert da (transcript_resolved) + Audio resolved.
- **Layer 3 (cross-file):** grep `run_behavior_judge` (slow_lane Aufruf + judge_runner def); `register_call_end_step`
  (genau 1 Schritt registriert); `compute_rubric` (nur noch in rubric_engine.py + Tests, KEIN lebender slow_lane-
  Noten-Aufruf mehr) → grep-Beleg im SUMMARY; `transcript_resolved` (Schreiber app_routes Plan-Folge + Leser slow_lane
  Merge-Gate). PLUS Leser-Strang: grep `coaching_score/rubric_score/dimensions` in routes/performance.py (erwartet 0 —
  write-stop bricht keinen lebenden Leser, WARNING 2).
- **Layer 4 (Edge + 4 Race-Fragen):** (1) Gleichzeitig: EIN Consumer (app.py:2222) serialisiert; Audio-Thread schreibt
  nur audio_health_resolved. (2) Abbruch mid-LLM: Judge-Fehler-Mantel → status=judge_failed, UPSERT idempotent, Re-Run
  sauber. (3) Reconnect/alter put: doppelter {'call_id'}-put → UPSERT idempotent (ON CONFLICT). (4) Voraus-Lauf:
  transcript_resolved-Gate + audio-Gate verhindern, dass der Judge vor Transkript/Audio laeuft (Punkt 26). Der Judge
  liest ein committetes Transkript (api_beenden-commit STRIKT vor dem Merge-put, Plan-01-Naht).
  </control_flow_audit>

  <verify>
    <automated>python -c "import ast; [ast.parse(open(f,encoding='utf-8').read()) for f in ['services/slow_lane.py','tests/test_coaching_score_cutover.py']]; print('AST-OK')"</automated>
    <grep>grep -c "run_behavior_judge" services/slow_lane.py (>=1 — Cutover-Aufruf)</grep>
    <grep>grep -nE "compute_rubric\(" services/slow_lane.py (== 0 — Marker-Engine als Noten-Quelle abgeloest; nur Kommentar/Import-Verweis erlaubt, KEIN Aufruf)</grep>
    <grep>grep -n "transcript_resolved" services/slow_lane.py (>=1 — Merge-Gate Fan-In)</grep>
    <grep>grep -nE "observations=|ratings=" services/slow_lane.py (Treffer — UPSERT schreibt Beobachtungen/Auspraegung)</grep>
    <grep>grep -c "coaching_score\|rubric_score\|dimensions" routes/performance.py (== 0 erwartet — WARNING 2: write-stop bricht keinen lebenden Leser; bei !=0 → Folge-Ticket statt stillem Weiterbau, Ergebnis ins SUMMARY)</grep>
    <grep>grep -n "def test_merge_waits_for_transcript_resolved\|def test_judge_step_skips_on_not_gradable" tests/test_coaching_score_cutover.py (2 Treffer)</grep>
    <note>HART Kein-Local-Dev: Lauf am Deploy-Gate (pytest gegen nerve_test). AST = Syntax.</note>
  </verify>

  <done>
slow_lane: Merge hat das transcript_resolved-Gate (4. Fan-In); der registrierte Call-Ende-Schritt stoesst
run_behavior_judge an + UPSERTet observations_jsonb/ratings_jsonb unter der M-4-GUC; compute_rubric als Noten-Quelle
abgeloest (kein lebender Aufruf, rubric_engine bleibt als Foundation); Audio-Gate D-09 bleibt (kein LLM auf Muell).
Leser-Strang performance.py geprueft (grep 0 erwartet) + bewusste a/b-Entscheidung im SUMMARY (WARNING 2 / Punkt 21).
Tests umgestellt (Runtime). 4-Layer-Audit dokumentiert. AST-OK.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NERVE-Server → Anthropic (Sonnet) | Anonymisiertes Transkript + Profil/Briefing + Marker verlassen den Server an den LLM. DSGVO: Transkript bereits tokenisiert (§1); EU-Residenz = D-RESIDENZ (offene Frage, checkpoint Plan 05). |
| Slow-Lane-Daemon ↔ rubric_score | FORCE-RLS-Write unter M-4-GUC (Merge-Gate gesetzt, Bestand). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HT-03-01 | Info Disclosure | Outcome-Leakage in den Judge-Prompt | mitigate | Outcome wird GAR NICHT in run_behavior_judge geladen/uebergeben (physische Trennung); grep-Acceptance == 0. Decentering-Anweisung im System-Prompt zusaetzlich. |
| T-HT-03-02 | Tampering/Bias | Self-Enhancement — Judge kennt NERVE-Vorschlaege | mitigate | suggestion_reactions wird NICHT in den Verhaltens-Prompt geladen (grep == 0); Adoption ist ein SEPARATER Call (Plan 04). Cross-Family-Zweit-Urteil = Phase-2-Ticket. |
| T-HT-03-03 | Info Disclosure (DSGVO) | Transkript an Drittland-LLM | mitigate | Transkript anonymisiert (§1, kein Roh-PII); Judge fuegt keins hinzu. Residenz-Pfad (Anthropic direkt vs. Bedrock EU) = checkpoint:decision André (Plan 05) — bewusst NICHT geraten. |
| T-HT-03-04 | Spoofing | Halluzinierte Beleg-Zitate (Judge erfindet Zitate) | mitigate | Beleg-VOR-Note + Schema-Zwang "beleg_zitat woertlich aus Transkript"; Launch-Huerde = halluzinierte-Belege-Check (~10 Bewertungen, Plan 05) — der eine Fehler, den auch ein Laie fAengt. |
| T-HT-03-05 | DoS | LLM-Fehler/Timeout reisst den Consumer ab | mitigate | Fehler-Mantel in run_behavior_judge → status=judge_failed; UPSERT idempotent; pro-Call-Isolierung im Merge-Gate (Bestand). |
| T-HT-03-06 | Korrektheit/Stiller-Leser | write-stop coaching_score → performance.py liest stale/NULL (Punkt 21 Cross-Layer) | accept (verifiziert) | performance.py liest coaching_score/rubric_score/dimensions HEUTE NICHT (grep == 0, <performance_reader_verdict>); write-stop bricht keinen lebenden Leser. 999.2-Anzeige = Future-Reader auf observations/ratings (Deferred, notiert). Bei Repo-Drift (grep !=0) → Folge-Ticket statt stillem Bruch. |
</threat_model>

<verification>
## 5. Persistenz-Schicht-Verifikation (Punkt 21) — Welle 3

### Angefasste Tabellen
- `rubric_score` (schreiben: observations_jsonb/ratings_jsonb/score_schema_version/status via Judge-UPSERT unter GUC)
- `transcript_segments` (lesen: das getaggte Transkript fuer den Prompt)
- `intent_event` (lesen: Marker — NERVE-Erkennungen/Phase/triggering_text)
- `calls` (lesen: call_mode/conversation_log_id/audio_health_score/transcript_resolved; NICHT outcome an den Prompt)

### inspect.sh-Beleg (read_first)
```
$ ssh ... 'bash scripts/inspect.sh schema rubric_score' → observations_jsonb/ratings_jsonb existieren (Plan 02 deployed).
$ ssh ... 'bash scripts/inspect.sh sample transcript_segments' → ts_ms/speaker/text, *ewb button*-Marker im kunde-Text.
$ ssh ... 'bash scripts/inspect.sh migrations' → 0029 (Plan 02 vor Plan 03).
```

### Cross-Layer-Konsistenz-Tabelle (inkl. Punkt-26-Schreib-Zeitpunkt)
| Feld | Pfad | Schicht | Schreib-Zeitpunkt | Kann Leser vorauslaufen? | Verifiziert |
|---|---|---|---|---|---|
| transcript_segments (lesen) | Judge-Prompt | DB | Batch am Call-Ende | JA — entschaerft: Judge laeuft NUR nach transcript_resolved==True (Merge-Gate, Plan 01) | ✓ |
| calls.outcome | NICHT gelesen fuer den Prompt | DB calls | — | — (physisch raus) | ✓ Leakage-Schutz |
| observations_jsonb/ratings_jsonb (schreiben) | Judge-UPSERT | DB rubric_score (JSONB, FORCE-RLS) | Call-Ende-Schritt unter GUC | nein (Schreiber) | ✓ Plan 02 Schema |
| coaching_score/dimensions (write-stop) | NICHT mehr geschrieben (Plan 02/03) | DB rubric_score (ALT-Spalten) | — | — | ✓ Leser-Check: performance.py liest sie nicht (grep 0); 999.2 = Future-Reader |

### Punkt 26 — der Judge-Anstoss laeuft dem Transkript-Write NICHT voraus
Der Judge-Schritt sitzt im _call_end_merge HINTER dem transcript_resolved-Gate (Plan-01-Fan-In) + dem
audio_health_resolved-Gate. Das Transkript ist committed (api_beenden-commit STRIKT vor dem Merge-put). resolved-als-
absent garantiert das Flag IMMER True → kein Hang. → der Leser (Judge) kann dem Schreiber (Transkript-Write) nie
vorauslaufen.

### WARNING 2 — stiller Leser-Strang routes/performance.py (Punkt 21 Cross-Layer, bewusste Entscheidung)
Das rubric_score-Tabellen-Schild (models.py:894) nennt routes/performance.py (999.2) als Leser von coaching_score/
dimensions. Dieser Plan stoppt den coaching_score-Write (Plan 02/03 write-stop). GREP-BELEG (Planner):
`grep -n "coaching_score\|rubric_score\|dimensions" routes/performance.py` → **0 Treffer**. performance.py liest
diese Felder HEUTE NICHT (es liest ConversationLog.kb_end/result/einwaende_* + Call.outcome/outcome_source).
**VERDIKT (a)/(b), NICHT (c):** der write-stop bricht keinen lebenden Leser in performance.py; die 999.2-Referenz im
Schild ist ein NOCH-NICHT-gebauter Future-Reader, der spaeter auf observations_jsonb/ratings_jsonb gebaut wird —
bewusst belassen, KEIN stiller Bruch, performance.py wird in DIESER Phase NICHT umgestellt. Der Executor fuehrt den
grep im Execute erneut aus (Repo-Drift-Schutz) und dokumentiert das Ergebnis + diese Entscheidung im SUMMARY;
nur bei wider Erwarten !=0 Treffern (Fall c) → Folge-Ticket statt stillem Bruch. Voller Beleg: <performance_reader_verdict>.

## Latenz (Punkt 25)
Der Judge-Call ist async im Slow-Lane-Consumer (post-call), KEIN Block des Call-Endes, KEIN LLM in der Live-Lane
(Bau-Regel 1). api_beenden macht nur leichte Flag-Sets + puts (Plan-Folge). Live-Antwort-Pfad UNBERUEHRT.
</verification>

<success_criteria>
- services/judge_runner.py: EIN forced-Tool-Use-Sonnet-Call (temp=0), Prompt = getaggtes Transkript ts_ms ASC +
  Marker + Profil/Briefing, Rubrik (BARS) an Anfang UND Ende, Beleg-VOR-Note; OUTCOME physisch raus; NERVE-Vorschlaege
  raus; Parse → observations_jsonb (sichtbar) + ratings_jsonb (intern). Context7 fuer das SDK-Tool-Use-Format genutzt.
- Cutover: der registrierte Call-Ende-Schritt stoesst run_behavior_judge an + UPSERTet Beobachtungen/Auspraegung unter
  der M-4-GUC; compute_rubric als Noten-Quelle abgeloest (kein lebender Aufruf in slow_lane), rubric_engine bleibt als
  Foundation (Punkt 20, Foundation-Register-Eintrag im SUMMARY).
- Merge-Gate hat das transcript_resolved-Fan-In-Bit (Punkt 26); Audio-Gate D-09 bleibt (kein LLM auf Muell-Audio).
- WARNING 2 (Punkt 21): Leser-Strang performance.py geprueft (grep 0 erwartet) — write-stop bricht keinen lebenden
  Leser; 999.2 = Future-Reader auf observations/ratings (Deferred); bewusste Entscheidung im SUMMARY, KEIN Bau hier.
- Bau-Regel 1 (kein LLM Fast-Lane), M-4-GUC, Outcome-/Vorschlags-Trennung, Punkt 14/21/25/26 belegt.
- Leitsatz 2 / Punkt 27: EIN Call, schauen+zitieren — KEINE Self-Consistency, KEINE Gewichte, KEINE Aggregation.
- Deferred sauber notiert (Cross-Family/κ/Bedrock = Phase 2 bzw. eigene Phase).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-03-SUMMARY.md`

**SUMMARY-Pflicht-Inhalt:** Wo der Judge lebt (services/judge_runner.py); der compute_rubric-Cutover (kein lebender
Aufruf mehr, rubric_engine als Foundation im Register); dass Outcome + NERVE-Vorschlaege physisch aus dem Prompt sind;
D-RESIDENZ (Anthropic-Client direkt genutzt, Bedrock-Frage an André offen); der performance.py-Leser-Strang-Check
(grep-Ergebnis + a/b-Entscheidung: write-stop bricht keinen lebenden Leser, 999.2 = Future-Reader, WARNING 2).
</output>
