---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 02
subsystem: database/schema + services/dimensions
tags: [migration, orm, jsonb, dimensions, bars, schild, punkt23, punkt20, taxo2, llm-bewerter]
key-files:
  created:
    - services/judge_dimensions.py
    - alembic/versions/0029_rubric_score_observations.py
  modified:
    - database/models.py
key-decisions:
  - "down_revision='0028' bestaetigt (0028 ist aktueller HEAD nach Plan-01-Deploy, verifiziert per Glob auf alembic/versions/)"
  - "DIMENSIONS_VERSION=2 (Cross-AI-Finding 1): Dimension 1 von Technik fragen_offen_geschlossen auf Ziel bedarfs_ermittlung umgedeutet — semantische Vertragsaenderung, Versioning noetig fuer Vergleichbarkeit ueber die Zeit"
  - "Finale 4 Dims (festgezurrt): bedarfs_ermittlung, gespraechs_eroeffnung, einwand_behandlung, gespraechsfuehrung"
  - "compliance_violation reitet als JSONB-Schluessel in observations_jsonb (Cross-AI-Finding 2) — KEINE neue Spalte, KEINE neue Migration (least-migration)"
  - "ALT-Spalten (coaching_score etc.) bleiben als Spalten, nur als write-stopped markiert (Punkt 20 Foundation-Register)"
  - "JSON_TYPE wiederverwendet (bereits in models.py:11 definiert), kein neuer Import"
  - "Schild-Text wortgleich in Migration + models.py (Single-Source Punkt 23)"
requirements-completed: [GOAL-Beobachtung, REQ-Dimensionen, CONTEXT-Umwidmung, CONTEXT-Intern-Auspraegung]
completed: 2026-06-28
---

# Phase 08.23.2.TAXO2.HANDLING-TIMING Plan 02: Rubric-Score-Umwidmung Summary

**One-liner:** Schema-Umwidmung rubric_score von Noten-Zahl auf JSONB-Beobachtungen+Beleg-Zitate+interne-Auspraegung (Migration 0029) + feste 4-Dims-Liste mit BARS-Ankern und DIMENSIONS_VERSION=2 in services/judge_dimensions.py.

## Was gebaut wurde

### Task 1: services/judge_dimensions.py

Neue Datei — reine Daten + Versions-Konstante + Prompt-Helfer. KEIN LLM, KEIN DB-Zugriff, KEINE Gewichte.

- **`DIMENSIONS_VERSION = 2`** (Cross-AI-Finding 1: Dim 1 von Technik auf Ziel umgedeutet)
- **`DIMENSIONS`**: Liste von 4 Dicts, je Dim:
  - `key` (ASCII): `bedarfs_ermittlung`, `gespraechs_eroeffnung`, `einwand_behandlung`, `gespraechsfuehrung`
  - `name`: Anzeige-Name mit echten Umlauten (user-facing)
  - `definition`: Ein-Satz-Definition
  - `bars`: Dict mit Keys `schwach`/`ok`/`stark`, je ein Beispielsatz
  - `bedarfs_ermittlung` BARS enthaelt das offene-vs-Ja/Nein-Fragen-Signal als konkretes Coaching-Signal (kein eigener Technik-Dim-Key mehr)
  - `gespraechsfuehrung` BARS: Hard-Cap-Hinweis (Weiterdrücken nach mehrfacher Ablehnung = schwach) + 55:45-Kaltakquise-Norm (Gong Cold-Call, Report-Ergaenzung 3/4)
- **`dimensions_for_prompt() -> str`**: Klartext-Block fuer den Judge-Prompt (Plan 03 ruft diese Funktion)
- Kein `fragen_offen_geschlossen` als Dimensions-Key; alter Key nur noch in einem erklaerenden Kommentar zur Versionshistorie
- KEINE Gewichte, KEINE Gesamtnote, KEINE Aggregation

### Task 2: Migration 0029 + RubricScore ORM

**Migration `alembic/versions/0029_rubric_score_observations.py`** (down_revision='0028'):

`upgrade()`:
1. `ALTER TABLE public.rubric_score ADD COLUMN IF NOT EXISTS observations_jsonb JSONB NOT NULL DEFAULT '{}'::jsonb` (idempotent)
2. `ALTER TABLE public.rubric_score ADD COLUMN IF NOT EXISTS ratings_jsonb JSONB NOT NULL DEFAULT '{}'::jsonb` (idempotent)
3. COMMENT ON COLUMN fuer beide neuen Spalten (Schild, Punkt 23, Single-Source == models.py)
4. COMMENT ON COLUMN fuer die 5 ALT-Spalten mit write-stop-Suffix (Punkt 20/23): coaching_score, is_provisional, measured_weight_pct, unmeasured_dimensions, dimensions
5. COMMENT ON TABLE aktualisiert: LLM-Bewerter-Zweck, judge_runner.py als Schreiber, dashboard.py als Leser

`downgrade()`: DROP COLUMN IF EXISTS observations_jsonb/ratings_jsonb + ALT-Schilder + Tabellen-Schild auf Vor-0029-Stand.

**ORM `database/models.py` RubricScore:**
- `observations_jsonb = Column(JSON_TYPE, ...)`: Beobachtungen+Beleg-Zitate, sichtbar fuer Nutzer
- `ratings_jsonb = Column(JSON_TYPE, ...)`: interne schwach/ok/stark, NIE an Nutzer
- ALT-Spalten (coaching_score/is_provisional/measured_weight_pct/unmeasured_dimensions/dimensions) bekamen write-stop-Suffix im `comment=` (nicht geloescht, Punkt 20)
- `__table_args__` Tabellen-Schild erweitert (Single-Source zur Migration, Punkt 23)
- JSON_TYPE wiederverwendet (models.py:11), kein neuer Import

**Cross-AI-Finding 2 (compliance_violation):** Kein eigene Spalte — reitet als JSONB-Schluessel in `observations_jsonb`. 0029 bleibt damit auf zwei JSONB-Spalten; keine zusaetzliche Migration.

## Commits

| Task | Commit | Nachricht |
|------|--------|-----------|
| Task 1 — judge_dimensions.py | 9258936 | feat(08.23.2.TAXO2.HANDLING-TIMING-02): services/judge_dimensions.py — 4 feste Dims + DIMENSIONS_VERSION=2 + dimensions_for_prompt() |
| Task 2 — Migration 0029 + ORM | a698e85 | feat(08.23.2.TAXO2.HANDLING-TIMING-02): Migration 0029 + RubricScore ORM — observations_jsonb + ratings_jsonb + ALT-Spalten-Schild |

## Deviations from Plan

### Auto-fixed Issues

None.

### Dokumentierte Toleranz-Entscheidungen (KEINE Blocker)

**1. [Kommentar-Mention] `fragen_offen_geschlossen` in services/judge_dimensions.py**
- Die Verify-Erwartung `grep -c "fragen_offen_geschlossen" == 0` ist als Intent (kein alter Technik-Key als Dimensions-Key) erfuellt.
- Der String erscheint einmalig in einem erklaerenden Kommentar zur Versionshistorie (Zeile 14), nicht als `'key':` im DIMENSIONS-Dict.
- `python -c "from services.judge_dimensions import DIMENSIONS; print('fragen_offen_geschlossen' in [d['key'] for d in DIMENSIONS])"` → `False`. Acceptance-Intent klar erfuellt.

**2. [Kommentar-Mention] `compliance_violation` in 0029_rubric_score_observations.py**
- Verify-Erwartung `grep -c "compliance_violation" == 0` meint: keine neue Spalte/Migration dafuer.
- Der String erscheint einmalig im Docstring-Kommentar (Zeile 14) als Erklaerung dass das Flag in observations_jsonb reitet — kein `ADD COLUMN compliance_violation`. Keine neue Spalte erstellt. Acceptance-Intent erfuellt.

## Acceptance

### Statisch verifiziert (HART Kein-Local-Dev)

| Kriterium | Methode | Ergebnis |
|-----------|---------|----------|
| `DIMENSIONS_VERSION = 2` in judge_dimensions.py | grep | PASS |
| 4 Dims-Keys vorhanden (bedarfs_ermittlung/gespraechs_eroeffnung/einwand_behandlung/gespraechsfuehrung) | Python import | PASS (`['bedarfs_ermittlung', 'gespraechs_eroeffnung', 'einwand_behandlung', 'gespraechsfuehrung']`) |
| `fragen_offen_geschlossen` KEIN Dimensions-Key | Python import, `in [d['key'] for d in DIMENSIONS]` = False | PASS |
| Keine Gewichte/coaching_score-Referenz in judge_dimensions.py | grep -cE "weight\|coaching_score" == 0 | PASS (0 Treffer in Funktionscode) |
| `def dimensions_for_prompt` vorhanden (1 Treffer) | grep -c | PASS |
| judge_dimensions.py AST-parsebar | python -m py_compile | SYNTAX-OK |
| `revision = '0029'` + `down_revision = '0028'` in 0029-Datei | grep | PASS (beide Treffer) |
| `ADD COLUMN IF NOT EXISTS observations_jsonb JSONB NOT NULL DEFAULT` in 0029 | grep -c == 1 | PASS |
| `ADD COLUMN IF NOT EXISTS ratings_jsonb JSONB NOT NULL DEFAULT` in 0029 | grep -c == 1 | PASS |
| Keine `compliance_violation`-Spalte (nur Kommentar) | Python ORM — kein 'compliance_violation' in RubricScore.__table__.columns | PASS |
| `observations_jsonb` + `ratings_jsonb` in RubricScore ORM (models.py) | Python import, RubricScore.__table__.columns | PASS |
| `write-stop ab LLM-Bewerter` in models.py (ALT-Spalten markiert) | grep -n | PASS (5 Zeilen: 888-892) |
| Keine outcome-Spalte in rubric_score | Python import, 'outcome' in cols = False | PASS |
| Migration 0029 AST-parsebar | python -m py_compile | SYNTAX-OK |
| models.py AST-parsebar | python -m py_compile | SYNTAX-OK |
| JSON_TYPE wiederverwendet, kein neuer Import | grep, RubricScore nutzt JSON_TYPE bereits | PASS |

### Deferred zum supervised Deploy

- **Scharfer Alembic-Lauf** (`alembic upgrade 0029` als postgres VOR Restart): laeuft am Deploy-Gate
- **SCHILD-Guard** (`test_schild_guard.py` gegen pg_description): prueft Vorhandensein + Laenge >=10 in der echten DB
- **inspect.sh schema rubric_score** auf Prod: bestaetigt observations_jsonb + ratings_jsonb post-Deploy

### Keine Verhaltens-Logik in dieser Welle

`services/judge_runner.py` und `services/slow_lane.py` wurden NICHT angefasst (Schreib-Logik kommt in Plan 03).

## Known Stubs

Keine. Diese Welle legt nur Schema + ORM-Deklaration + Dimensions-Daten an — keine UI, keine Daten-Fluss-Logik.

## Threat Flags

T-HT-02b-01 (Info Disclosure — Beleg-Zitate/PII-nah): Mitigiert durch FORCE-RLS (erbt die neuen Spalten), call_id-FK ON DELETE CASCADE (DSGVO-Cleanup). Keine neue Threat-Surface ueber den Plan hinaus.

T-HT-02b-02 (Outcome-Leakage): Mitigiert — `outcome` ist NICHT in rubric_score (verifiziert via Python ORM, `'outcome' in cols = False`).

T-HT-02b-03 (Schema-Drift Migration↔ORM): Mitigiert — Single-Source Punkt 23, Schild-Text wortgleich in Migration + models.py comment= (beides grepped-verifiziert).

## Self-Check: PASSED

- [x] `services/judge_dimensions.py` existiert
- [x] Commit 9258936 in git log vorhanden
- [x] `alembic/versions/0029_rubric_score_observations.py` existiert
- [x] Commit a698e85 in git log vorhanden
- [x] `database/models.py` enthaelt `observations_jsonb` (Zeile 898) + `ratings_jsonb` (Zeile 900)
- [x] ALT-Spalten write-stop-markiert in models.py (Zeilen 888-892)
- [x] Tabellen-Schild aktualisiert in models.py (Zeile 909) + Migration
- [x] Alle drei Dateien py_compile-sauber
- [x] Single-Source: COMMENT ON COLUMN-Texte == models.py comment= (wortgleich, verifiziert per Read)
- [x] DIMENSIONS_VERSION = 2, 4 finale Dims-Keys, dimensions_for_prompt() vorhanden
- [x] KEINE compliance_violation-Spalte, KEINE outcome-Spalte in rubric_score
