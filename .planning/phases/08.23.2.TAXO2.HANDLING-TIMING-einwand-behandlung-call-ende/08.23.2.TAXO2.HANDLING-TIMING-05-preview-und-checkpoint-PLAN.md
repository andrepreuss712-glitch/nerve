---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 05
type: execute
wave: 5
depends_on: [03, 04]
autonomous: false
files_modified:
  - routes/dashboard.py
  - templates/session_detail.html
  - static/nerve.css
  - services/beleg_check.py
must_haves:
  truths:
    - "Das 'Neu — Vorschau'-Panel in session_detail zeigt BEOBACHTUNGEN STATT ZAHL: je Dimension den Anzeige-Namen + die Beobachtung(en) mit woertlichem Beleg-Zitat (aus rubric_score.observations_jsonb). KEINE coaching_score-Zahl, keine Schulnote pro Dimension."
    - "Wenn observations_jsonb['_compliance'].verletzt == true wechselt der Feedback-TON (Compliance VOR Coaching): die Compliance-Notiz + ihr Beleg-Zitat werden PROMINENT oben angezeigt, NICHT zwischen den Dimensions-Beobachtungen vergraben (Cross-AI-Finding 2)."
    - "Das Panel ist klar als KI-Einschaetzung / Coaching-Hinweis gelabelt (nicht als Gutachten/Note)."
    - "Die interne Auspraegung (ratings_jsonb schwach/ok/stark) wird NICHT angezeigt (intern/Lernen)."
    - "Die Anzeige-Sperre (Note erst nach Outcome-Bestaetigung) + die Edge-Reihenfolge (judge_failed / not_gradable / row-absent / Beobachtungen) bleiben; status-Werte auf den Judge umgestellt (judged/judge_failed/not_gradable)."
    - "Der halluzinierte-Belege-Check (Launch-Huerde) ist FUZZY (normalisiert + Token-Overlap/Levenshtein-Schwelle via services/beleg_check.py), NICHT striktes substring-contains; Near-Miss → FLAG fuer menschliche Sicht, KEIN Auto-Verwerfen (Cross-AI-Finding 3). Kein BLEU/ROUGE/Cosine."
    - "Keine hardcoded Hex-Farben / Inline-Styles — nur .n-*-Klassen + CSS-Variablen aus static/nerve.css (CLAUDE.md UI-Regel)."
    - "Der dashboard.py-Lese-Pfad liest observations_jsonb (statt coaching_score/dimensions) aus der rubric_score-Zeile."
  artifacts:
    - path: "routes/dashboard.py"
      provides: "session_detail liest observations_jsonb (Beobachtungen + '_compliance'-Hard-Gate) statt der Alt-Noten-Felder"
      contains: "observations_jsonb"
    - path: "templates/session_detail.html"
      provides: "Preview-Panel rendert Beobachtungen+Beleg-Zitate je Dimension (keine Zahl), KI-Hinweis-Label, prominente Compliance-Notiz bei Verstoss"
      contains: "observations"
    - path: "static/nerve.css"
      provides: ".n-*-Klassen fuer das Beobachtungs-Panel + die prominente Compliance-Notiz (kein hardcoded Hex)"
      contains: "n-"
    - path: "services/beleg_check.py"
      provides: "Fuzzy-Beleg-Helfer: beleg_im_transkript(zitat, transkript_text) -> (ok: bool, score: float) via Normalisierung + Token-Overlap/Levenshtein (difflib, stdlib); KEIN BLEU/ROUGE/Cosine"
      contains: "def beleg_im_transkript"
  key_links:
    - from: "templates/session_detail.html"
      to: "rubric_preview.observations_jsonb"
      via: "Jinja iteriert je Dimension Beobachtungen + Beleg-Zitate; Compliance prominent oben"
      pattern: "observations"
    - from: "services/beleg_check.py"
      to: "der halluzinierte-Belege-Check (Checkpoint Task 3)"
      via: "Fuzzy-Schwelle statt substring-contains; Near-Miss flaggt statt zu verwerfen"
      pattern: "beleg_im_transkript"
---

<objective>
Die Anzeige-Seite: das bestehende "Neu — Vorschau"-Panel in session_detail von "Zahl + Dimensions-Tabelle" auf
**Beobachtungen mit woertlichem Beleg-Zitat (KEINE Zahl)** umstellen — der sichtbare Ausdruck von "Beobachtung statt
Note" (Soll-Verhalten §6) — inkl. **prominenter Compliance-Notiz** bei einem Hard-Gate-Verstoss (Compliance VOR
Coaching, Cross-AI-Finding 2). PLUS der **finale BLOCKENDE human-verify-Checkpoint** (André: Migration als postgres VOR
Restart → deploy.sh production → cold-call Live-Test → halluzinierte-Belege-Check, jetzt FUZZY, Finding 3) und die
D-RESIDENZ-Entscheidung.

Purpose: Der Nutzer bekommt KEINE Zahl, sondern Klartext-Beobachtungen mit Beleg, gelabelt als KI-Hinweis
(Entscheidung.md); ein echter Compliance-Verstoss wird nicht zwischen Coaching-Tipps vergraben. Diese Welle schliesst
die Kette (Schema → Judge → Adoption → Anzeige) und verifiziert sie live.

Output: dashboard.py liest observations_jsonb; session_detail-Panel rendert Beobachtungen+Belege + prominente
Compliance-Notiz; nerve.css-Klassen; services/beleg_check.py (Fuzzy-Helfer); + ein autonomous:false Checkpoint, der
bis grün offen bleibt.

**Komplexitaet: 🔴** (Abschluss der LLM-Pipeline + DSGVO-Checkpoint + UI). Cross-AI Pflicht (inkl. UI-Token-Check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Ist-Stand routes/dashboard.py:916-948 (Preview-Lese-Pfad) + templates/session_detail.html:119-187 (Panel). -->

routes/dashboard.py:923-941 — rubric_preview wird gelesen (rubric_score-Zeile, origin='live', call_id-Join, FORCE-RLS
über Request-GUC). outcome_confirmed = (call.outcome is not None). Beide an render_template uebergeben (:947-948).
Der Lese-Pfad bleibt; er liest jetzt zusaetzlich observations_jsonb (ist eine Spalte der gelesenen Zeile — kein
Extra-Query). KEIN Lesen von calls.outcome-WERT (nur is-not-None fuer die Sperre — Leakage-Schutz bleibt).

templates/session_detail.html:119-187 — das Panel mit der Edge-Reihenfolge (0 outcome-Sperre / a failed / b
not_gradable / c coaching_score NULL / d row-absent / e Score+Tabelle). Die Zweige (e) [Zahl + dimensions-Tabelle]
+ (c) [coaching_score NULL] werden umgebaut; (0)/(a)/(b)/(d) bleiben sinngemaess (status-Werte angepasst).

rubric_score.observations_jsonb (Plan 02/03): {dim_key: [{"beobachtung": str, "beleg_zitat": str}, ...]}
  PLUS der reservierte Schluessel '_compliance': {"verletzt": bool, "beleg_zitat": str} (Cross-AI-Finding 2,
  Hard-Gate, KEINE eigene Spalte).
rubric_score.ratings_jsonb (Plan 02): {dim_key: "schwach"|"ok"|"stark"} — NICHT anzeigen.
services/judge_dimensions.py (Plan 02): DIMENSIONS (key→name-Mapping fuer die Anzeige; '_compliance' ist KEINE
  Dimension → in der Dimensions-Schleife ueberspringen, separat prominent rendern).
status-Werte ab Plan 03: 'judged' (ok) / 'judge_failed' / 'not_gradable'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dashboard.py + session_detail.html + nerve.css — Preview zeigt Beobachtungen statt Zahl + prominente Compliance-Notiz</name>
  <files>routes/dashboard.py, templates/session_detail.html, static/nerve.css</files>

  <read_first>
    - routes/dashboard.py:916-955 (Preview-Lese-Pfad + render_template-Uebergabe)
    - templates/session_detail.html:119-187 (das Panel — Edge-Reihenfolge + die umzubauenden Zweige e/c)
    - services/judge_dimensions.py (DIMENSIONS key→name fuer die Anzeige; ggf. an die Route geben oder im Template via gerendertem dict)
    - 08.23.2.TAXO2.HANDLING-TIMING-REVIEWS.md Finding 2 (Compliance VOR Coaching — prominent, nicht vergraben)
    - static/nerve.css — VORAB scannen (CLAUDE.md UI-Pflicht-Recherche): welche .n-*-Klassen + CSS-Variablen existieren (.n-session-detail-card, .n-label, .n-badge, var(--page-text), var(--page-text-muted), var(--accent), var(--border-color), eine vorhandene Warn-/Alert-Klasse fuer die Compliance-Notiz)? Bestehende Card/Label/Pill-Muster KOPIEREN, nicht neu erfinden.
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema rubric_score' (observations_jsonb existiert — Plan 02 deployed)
  </read_first>

  <action>
**(A) routes/dashboard.py — observations bereitstellen.** Der bestehende rubric_preview-Lese-Block (:923-941) liest
die rubric_score-Zeile bereits; observations_jsonb/ratings_jsonb sind Spalten dieser Zeile (kein Extra-Query). Gib
zusaetzlich das DIMENSIONS-key→name-Mapping ans Template (z.B. `judge_dimensions=[{key,name} ...]` aus
services.judge_dimensions importiert) ODER baue ein dict observations_display = [{name, items:[{beobachtung,
beleg_zitat}]}] in der Route (sauberer — Template bleibt dumm). Executor's Discretion; bevorzugt: in der Route ein
geordnetes observations_display nach DIMENSIONS-Reihenfolge bauen (feste Liste → stabile Reihenfolge), an
render_template geben. **Den '_compliance'-Schluessel SEPARAT extrahieren** (NICHT in observations_display mischen):
`compliance = (observations_jsonb or {}).get('_compliance') or {}` → an das Template als eigene Variable
(compliance_verletzt = bool, compliance_beleg = zitat). KEIN coaching_score/dimensions-Lesen mehr noetig fuer das
Panel. outcome_confirmed bleibt (is-not-None, Leakage-Schutz).

**(B) templates/session_detail.html — Panel umbauen (Zweige e + c + Compliance-Notiz).**
- (0) outcome-Sperre, (a) judge_failed ("Berechnung fehlgeschlagen"), (b) not_gradable ("Audio zu schlecht"/"zu wenig
  Momente"), (d) row-absent ("wird im Hintergrund ausgewertet …") BLEIBEN — status-Werte auf 'judge_failed'/'not_gradable'
  anpassen (Plan 03).
- **Compliance VOR Coaching (Cross-AI-Finding 2):** WENN compliance_verletzt → GANZ OBEN im (e)-Zweig (vor den
  Dimensions-Beobachtungen) eine PROMINENTE Notiz rendern: eine klar abgesetzte .n-*-Warn-/Alert-Box mit kurzem
  Klartext ("Achtung: hier wurde nach mehrfacher klarer Ablehnung weiter gedrueckt — das ist kein Verkauf, sondern
  Belaestigung. Compliance geht vor Coaching.") + dem woertlichen compliance_beleg als zitiertes Element. Der TON des
  Panels wechselt damit (Compliance zuerst), die Notiz wird NICHT zwischen den Dimensions-Beobachtungen vergraben.
  WENN nicht verletzt → keine Box (kein Rauschen).
- **(e) Beobachtungen statt Zahl:** KEINE `coaching_score`-Zahl, KEINE Dimensions-Score-Tabelle. Stattdessen je
  Dimension (in DIMENSIONS-Reihenfolge) den Anzeige-Namen als Ueberschrift + darunter die Beobachtung(en) als Klartext
  mit dem **woertlichen Beleg-Zitat** (z.B. Beobachtung normal, Zitat als eingeruecktes/zitiertes `<blockquote>`-aehnliches
  .n-*-Element). Wenn eine Dimension keine Beobachtung hat → kurz "keine auffaellige Beobachtung" (nicht weglassen, feste
  Liste = Vergleichbarkeit). **'_compliance' ist KEINE Dimension → in der Dimensions-Schleife ueberspringen** (es wird
  separat oben gerendert).
- **(c) "kein Material":** wenn observations_jsonb leer/alle Dimensionen leer (und kein Compliance-Verstoss) → "Nicht
  genug zum Bewerten." (Daten-Substanz-Schutz, ergebnis-BLIND, Soll-Verhalten §6 — NICHT "wegen des Neins").
- **Label:** das bestehende "KI-Einschaetzung / Read-only"-Label schaerfen: "KI-Einschaetzung / Coaching-Hinweis — keine
  Note, kein Urteil." (Entscheidung.md "als KI-Einschaetzung labeln".)
- **ratings_jsonb (schwach/ok/stark) NICHT rendern** (intern).
- **KEIN hardcoded Hex, KEINE Inline-Styles mit Farben** (CLAUDE.md UI-Regel) — nur .n-*-Klassen.

**(C) static/nerve.css — Klassen fuer das Beobachtungs-Panel + die Compliance-Notiz.** Falls ein passendes Muster
fehlt: neue .n-*-Klassen (z.B. .n-observation-dim, .n-observation-quote, .n-compliance-alert) NUR mit CSS-Variablen
(var(--page-text)/var(--page-text-muted)/var(--border-color)/var(--accent) bzw. eine vorhandene Warn-Variable fuer die
Compliance-Box) — KEINE hardcoded Farben, KEINE Brand-Fremdfarben (Teal-Familie, CLAUDE.md Punkt 8 Brand-Check).
Bestehende .n-session-detail-card-Optik weiterfuehren. Die .n-compliance-alert nutzt ein bestehendes Warn-/Alert-Muster
(vorab scannen) — kein neuer Hex.
  </action>

  <verify>
    <automated>python -c "import ast; ast.parse(open('routes/dashboard.py',encoding='utf-8').read()); print('AST-OK')"</automated>
    <grep>grep -c "observations" routes/dashboard.py (>=1 — Beobachtungen ans Template)</grep>
    <grep>grep -c "_compliance\|compliance_verletzt\|compliance_beleg" routes/dashboard.py (>=1 — Compliance separat extrahiert, Finding 2)</grep>
    <grep>grep -nE "observations|beleg_zitat|beobachtung" templates/session_detail.html (Treffer — Panel rendert Beobachtungen+Belege)</grep>
    <grep>grep -nE "compliance_verletzt|n-compliance-alert|Compliance" templates/session_detail.html (Treffer — prominente Compliance-Notiz, Finding 2)</grep>
    <grep>grep -nE "coaching_score \| round|n-session-detail-score" templates/session_detail.html (== 0 — keine Zahl mehr im Panel-Zweig e)</grep>
    <grep>grep -nE "ratings_jsonb|schwach|stark" templates/session_detail.html (== 0 — interne Auspraegung NICHT angezeigt)</grep>
    <grep>grep -nE "style=\"[^\"]*#[0-9a-fA-F]{3,6}" templates/session_detail.html (== 0 — kein hardcoded Hex inline, CLAUDE.md UI)</grep>
    <grep>grep -nE "#[0-9a-fA-F]{6}" static/nerve.css | grep -iE "1a1a2e|2a2a4e|f0c040|3b82f6" (== 0 — keine alten Dark-Mode-/Fremd-Farben in neuen Klassen)</grep>
    <note>HART Kein-Local-Dev: kein lokaler Flask-/Browser-Lauf. Visueller Beleg = der Live-Test-Checkpoint (Task 3). url_for hier nicht betroffen (kein neuer Endpoint).</note>
  </verify>

  <done>
session_detail-Panel zeigt je Dimension (feste Reihenfolge) Beobachtung(en) + woertliches Beleg-Zitat, KEINE Zahl,
KI-Hinweis-Label, "Nicht genug zum Bewerten" bei leer; bei Compliance-Verstoss eine PROMINENTE Notiz GANZ OBEN
(Compliance VOR Coaching, Finding 2); '_compliance' wird in der Dimensions-Schleife uebersprungen; ratings_jsonb NICHT
angezeigt. dashboard.py stellt observations + compliance separat bereit. nerve.css-Klassen nur mit CSS-Variablen
(kein Hex). AST-OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: services/beleg_check.py — Fuzzy-Beleg-Helfer (normalisiert + Token-Overlap/Levenshtein, stdlib) fuer den halluzinierte-Belege-Check</name>
  <files>services/beleg_check.py, tests/test_beleg_check.py</files>

  <read_first>
    - 08.23.2.TAXO2.HANDLING-TIMING-REVIEWS.md Finding 3 (FUZZY statt strikt; Near-Miss flaggen, nicht verwerfen; KEIN BLEU/ROUGE/Cosine)
    - CLAUDE.md Leitsatz 2 / Punkt 27 (einfachster tragfaehiger Weg — stdlib difflib, keine neue Dependency)
    - services/anonymization.py (als Muster fuer einfache Text-Normalisierung im Repo, falls vorhanden)
  </read_first>

  <action>
**Neue kleine Datei `services/beleg_check.py` — reine Funktion, KEIN DB-/LLM-Zugriff, KEINE neue Dependency
(stdlib difflib + re; Leitsatz 2 / Punkt 27). Finding 3: FUZZY, nicht strikt.**

1. `_normalisiere(text: str) -> str`: lowercase, Whitespace kollabieren, Satzzeichen entfernen (re), trim.
2. `beleg_im_transkript(zitat: str, transkript_text: str, *, schwelle: float = 0.8) -> tuple[bool, float, str]`:
   - Beide via `_normalisiere`.
   - **Best-Match-Score** des normalisierten Zitats gegen das normalisierte Transkript ueber EIN einfaches Mass
     (Leitsatz 2): entweder (a) Token-Overlap (Anteil der Zitat-Tokens, die als zusammenhaengende/verstreute Tokens
     im Transkript vorkommen) UND/ODER (b) `difflib.SequenceMatcher(None, zitat_norm, fenster).ratio()` ueber ein
     gleitendes Fenster der Zitat-Laenge im Transkript (stdlib, KEIN BLEU/ROUGE/Cosine). Executor's Discretion welches
     der beiden (oder Kombination max(score_a, score_b)) — einfach halten.
   - Return `(ok, score, befund)`: `ok = score >= schwelle` (klarer Treffer); bei `near_schwelle <= score < schwelle`
     (z.B. near_schwelle=0.6) → `ok=False` aber befund='near_miss' (→ FLAG fuer menschliche Sicht, NICHT verwerfen);
     bei `score < near_schwelle` → befund='no_match'. **Eine leichte Paraphrase (hoher, aber nicht perfekter Score)
     darf KEIN gutes Feedback kippen** — deshalb Near-Miss = flaggen, nicht hart verwerfen.
3. Dateikopf-Kommentar: "Fuzzy-Beleg-Check (Cross-AI-Finding 3): prueft, ob ein Judge-Beleg-Zitat (ungefaehr) im
   Transkript steht — normalisiert + Token-Overlap/Levenshtein (stdlib difflib). NICHT striktes substring-contains.
   Near-Miss → flaggen (menschliche Sicht), nicht auto-verwerfen. KEIN BLEU/ROUGE/Cosine (bleibt verboten, Leitsatz 2)."

**Tests `tests/test_beleg_check.py` (Runtime-Assertion auf Rueckgabewert, kein Source-Presence):**
- exakter Treffer → ok=True.
- leichte Paraphrase (Wort umgestellt / ein Fuellwort mehr) → ok=True ODER near_miss (NICHT no_match) — beweist:
  Paraphrase kippt gutes Feedback nicht.
- echtes Halluzinat (Satz steht gar nicht im Transkript) → ok=False, befund='no_match'.
- Near-Miss → befund='near_miss', ok=False (flaggen, nicht hart verwerfen).
  </action>

  <verify>
    <automated>python -c "import ast; [ast.parse(open(f,encoding='utf-8').read()) for f in ['services/beleg_check.py','tests/test_beleg_check.py']]; print('AST-OK')"</automated>
    <grep>grep -c "def beleg_im_transkript" services/beleg_check.py (== 1)</grep>
    <grep>grep -nE "SequenceMatcher|difflib|token" services/beleg_check.py (Treffer — stdlib-Fuzzy, kein BLEU/ROUGE/Cosine)</grep>
    <grep>grep -niE "bleu|rouge|cosine|sentence_transformers|sklearn" services/beleg_check.py (== 0 — verbotene Masse nicht verwendet, Finding 3 / Leitsatz 2)</grep>
    <grep>grep -c "near_miss" services/beleg_check.py (>=1 — Near-Miss flaggt statt verwirft)</grep>
    <grep>grep -nE "def test_exakter_treffer|def test_paraphrase_kippt_nicht|def test_halluzinat_no_match|def test_near_miss_flaggt" tests/test_beleg_check.py (4 Treffer)</grep>
    <note>HART Kein-Local-Dev: Lauf am Deploy-Gate (pytest gegen nerve_test). AST = Syntax.</note>
  </verify>

  <done>
services/beleg_check.py: beleg_im_transkript normalisiert + fuzzy (stdlib difflib/Token-Overlap), Schwelle + Near-Miss
(flaggen statt verwerfen), KEIN BLEU/ROUGE/Cosine. Tests beweisen: exakter Treffer ok, Paraphrase kippt nicht,
Halluzinat = no_match, Near-Miss = flag. AST-OK.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: ENTSCHEIDUNG (André) — DSGVO-Daten-Residenz des Judge-Calls (D-RESIDENZ)</name>
  <decision>Ueber welchen Pfad gehen die Transkripte an Sonnet — der bestehende direkte Anthropic-Client (Ist-Stand aller Claude-Calls) oder eine Migration auf AWS Bedrock Frankfurt (Vault-Soll "EU-Server")?</decision>
  <context>
🟢 DEINE WAHL (Klartext, kein Code-Detail zum Durchwinken): NERVE schickt am Call-Ende das (bereits anonymisierte)
Gespraechs-Protokoll an Claude, damit Claude Beobachtungen liefert. Heute laeuft JEDER Claude-Aufruf der App ueber
den direkten Anthropic-Weg — NICHT ueber den "EU-Server-Frankfurt"-Weg (Bedrock), den dein Vault als Ziel nennt.
Das ist kein neuer Fehler dieser Phase: es betrifft ALLE Claude-Aufrufe der App, nicht nur den neuen Bewerter.
Der neue Bewerter nutzt bewusst denselben Weg wie alle anderen (kein Sonderweg erfunden). Die Frage ist: ist der
heutige direkte Weg fuer dich DSGVO-okay bis Launch (das Protokoll ist ja schon anonymisiert — keine echten
Namen/Firmen/Nummern), ODER soll der Wechsel auf den Frankfurt-Weg (Bedrock) vorgezogen werden — das waere eine
EIGENE Phase fuer ALLE Claude-Aufrufe, nicht hier mit reingequetscht.
  </context>
  <options>
    <option id="weiter-direkt">
      <name>Bis Launch beim heutigen direkten Weg bleiben (anonymisiert), Frankfurt-Wechsel als eigene spaetere Phase</name>
      <pros>Kein Scope-Creep; der Bewerter geht live; das Protokoll ist bereits anonymisiert; Wechsel betrifft sowieso die ganze App und wird sauber separat gemacht.</pros>
      <cons>Bis dahin laeuft auch der Bewerter nicht ueber den Frankfurt-Weg (wie schon alle anderen Calls).</cons>
    </option>
    <option id="bedrock-zuerst">
      <name>Erst den Frankfurt-Weg (Bedrock) bauen, dann den Bewerter live nehmen</name>
      <pros>Erfuellt das Vault-Soll "EU-Server" vor dem ersten Bewerter-Live-Lauf.</pros>
      <cons>Grosse eigene Phase (alle Claude-Aufrufe umstellen) — verzoegert diese Phase deutlich; Anti-Abrieb spricht fuers Trennen.</cons>
    </option>
  </options>
  <resume-signal>Waehle: weiter-direkt ODER bedrock-zuerst. Bei bedrock-zuerst: diese Phase pausiert bis die Bedrock-Phase steht. Die Entscheidung in Nerve-Vault/05 Log + Soll-Verhalten festhalten.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: FINALER BLOCKENDER human-verify-Checkpoint — André deployt + Live-Test + halluzinierte-Belege-Check (FUZZY)</name>
  <what-built>
Die komplette LLM-Bewerter-Kette: transcript_resolved-Fan-In (0028, Plan 01) + rubric_score-Umwidmung
(observations_jsonb/ratings_jsonb, 0029, Plan 02) + Dimensions-Liste (judge_dimensions, 4 Dims inkl. bedarfs_ermittlung)
+ der Verhaltens-Call (judge_runner) mit compute_rubric-Cutover + separatem Compliance-Hard-Gate (Plan 03) + der
separate Uebernahme-Call (adoption_runner, Plan 04) + das Preview-Panel "Beobachtung statt Zahl" mit prominenter
Compliance-Notiz + der Fuzzy-Beleg-Helfer (diese Welle). KEIN Deploy durch den Executor (HART Kein-Local-Dev).
  </what-built>

  <how-to-verify>
**VORBEDINGUNG:** Cross-AI/Gemini-Review der ganzen Phase GELAUFEN (🔴, Pflicht — inkl. UI-Token-Check Plan 05) +
Claudian Pre-Execute-Audit. D-RESIDENZ (Task 3) entschieden.

**Schritt 1 — Migrationen als postgres VOR Restart (create_all-Race):**
- `inspect.sh migrations` → HEAD pruefen. `alembic upgrade 0028` dann `0029` als postgres-OS-User (Reihenfolge!).
- `inspect.sh schema calls` → transcript_resolved vorhanden. `inspect.sh schema rubric_score` → observations_jsonb +
  ratings_jsonb vorhanden. `inspect.sh schilder rubric_score` → Schilder aktuell (Beobachtungen/intern/write-stop).

**Schritt 2 — Deploy:** `bash deploy.sh production`. Pytest-Gate laeuft test_judge_runner / test_adoption_runner /
test_coaching_score_cutover / test_beleg_check gegen nerve_test; bei Rot kein Restart (alter Code bleibt live).
SCHILD-Guard gruen.

**Schritt 3 — Cold-Call Live-Test (DER KERN):** Als Test-User einen Cold-Call mit mehreren klaren Einwaenden +
Berater-Antworten fahren, EWB-Knopf nutzen, Call beenden, Outcome bestaetigen. Erwartung:
- `inspect.sh sample rubric_score` → die Zeile traegt observations_jsonb (Beobachtungen + Beleg-Zitate je Dimension
  + ggf. '_compliance'-Schluessel) + ratings_jsonb (schwach/ok/stark, intern) + status='judged'. KEINE coaching_score-
  Zahl mehr neu gesetzt. score_schema_version = 2 (Finding-1-Reframe).
- session_detail "Neu — Vorschau"-Panel → zeigt je Dimension Beobachtung + woertliches Zitat, KEINE Zahl, als
  KI-Hinweis gelabelt; ratings nicht sichtbar; die 4 Dimensionen inkl. **Bedarfsermittlung** (nicht mehr "Offene vs.
  geschlossene Fragen" als eigene Technik-Dimension).
- **Compliance-Test (Finding 2):** EINEN Test-Call gezielt fahren, in dem der Kunde mehrfach klar Nein sagt und du
  (als Berater) weiter drueckst → erwarte: observations_jsonb['_compliance'].verletzt=true + Beleg; im Panel erscheint
  die PROMINENTE Compliance-Notiz GANZ OBEN (Compliance VOR Coaching), nicht zwischen den Dimensionen vergraben. Ein
  sauberer Call (kein Weiterdruecken) → KEINE Compliance-Box.
- `inspect.sh sample suggestion_reactions` → falls Vorschlaege erfasst wurden: adoption_value/reaction_class befuellt
  (Uebernahme-Urteil). Falls 0 Vorschlaege: leer (sauberer No-Op).

**Schritt 4 — halluzinierte-Belege-Check FUZZY (DIE Launch-Huerde, Soll-Verhalten §6 / Entscheidung.md / Finding 3):**
~10 Bewertungen lesen (mehrere Test-Calls). Pro Beobachtung die EINE Frage: **steht das Beleg-Zitat (ungefaehr) so im
Transkript?** — geprueft FUZZY ueber `services/beleg_check.beleg_im_transkript(zitat, transkript_text)` gegen das
`inspect.sh sample transcript_segments` des Calls (NICHT striktes substring-contains: eine leichte Paraphrase des
Judge-Zitats darf gutes Feedback NICHT verwerfen). Auswertung:
- `ok=True` → Beleg sitzt.
- `befund='near_miss'` → FLAG fuer menschliche Sicht (Andre liest gegen), NICHT automatisch verwerfen/kippen.
- `befund='no_match'` → echtes Halluzinat = der eine Fehler, den auch ein Laie faengt + der gefaehrlichste.
Bei no_match-Halluzinaten → Gap-Fix (Prompt schaerfen: "beleg_zitat MUSS woertlich aus dem Transkript", ggf.
nachgelagerter beleg_check-Filter im Schreib-Pfad). κ/Experten-Validierung NICHT jetzt (Phase 2).

**Schritt 5 — Outcome-/Vorschlags-Trennung live belegen:**
- `inspect.sh sample rubric_score` → KEINE outcome-Spalte/-Wert in rubric_score (Leakage-Schutz).
- `inspect.sh logs-errors` → keine RLS/permission-denied, kein "working outside of request context", kein Endlos-
  Re-Queue, kein Consumer-Crash durch LLM-Fehler.

**Schritt 6 — Live-Pfad unberuehrt (Bau-Regel 1 / Punkt 25):** waehrend des Calls liefen EWB-Cues/Antworten normal,
KEINE spuerbare Latenz-Aenderung (die Bewertung ist async post-call). `inspect.sh logs` → der Judge/Adoption-Call
laeuft NUR im Slow-Lane-Consumer.

**Schritt 7 — kein Hang (resolved-als-absent):** ein sehr kurzer Cold-Call ohne verwertbares Transkript →
transcript_resolved=t, status terminal (judged/not_gradable), KEIN ewiges 'wird ausgewertet …', rubric_score-Zeile da.
  </how-to-verify>
  <resume-signal>"approved" — ODER Abweichungen beschreiben (welches inspect.sh-Sample, welcher Screen, welcher Beleg). Bei no_match-Halluzinaten oder fehlender Beobachtung: Gap-Fix-Plan (/gsd-plan-phase --gaps) statt Abschluss. Near-Miss-Flags sind KEIN Abbruch — Andre liest sie gegen. Plan 05 bleibt OFFEN bis dieser Live-Test + der Fuzzy-Belege-Check gruen sind (CONTEXT-Constraint).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ session_detail (Anzeige) | Liest die eigene rubric_score-Zeile (FORCE-RLS über Request-GUC, Bestand). observations_jsonb = anonymisierte Beleg-Zitate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-HT-05-01 | Info Disclosure | Beleg-Zitate (Transkript-naher Text) im Browser | mitigate | Zitate stammen aus dem anonymisierten Transkript (§1); FORCE-RLS zeigt nur die eigene Zeile (g.tenant_id-GUC, Bestand). |
| T-HT-05-02 | Spoofing | Halluzinierte Beleg-Zitate erreichen den Nutzer | mitigate | halluzinierte-Belege-Check als BLOCKENDE Launch-Huerde (Task 4 Schritt 4), jetzt FUZZY (services/beleg_check.py, Finding 3) — Paraphrase kippt nicht, Near-Miss flaggt, no_match = Halluzinat; Beleg-VOR-Note + Schema-Zwang (Plan 03). |
| T-HT-05-03 | Tampering | Outcome-Leakage in die Anzeige | accept | Panel liest observations_jsonb (kein Outcome); outcome_confirmed nur als is-not-None-Sperre (kein Wert). |
| T-HT-05-04 | (UI-Regel) | hardcoded Hex/Inline-Styles | mitigate | grep-Acceptance == 0 (Task 1 verify); Cross-AI UI-Token-Check (CLAUDE.md UI-Regel + Punkt 8 Brand). |
| T-HT-05-05 | Elevation/Compliance-Bypass | Compliance-Verstoss wird in der Anzeige zwischen Coaching-Tipps vergraben | mitigate | Compliance VOR Coaching (Finding 2): bei _compliance.verletzt prominente .n-compliance-alert GANZ OBEN, Ton-Wechsel; grep-Acceptance auf compliance_verletzt/n-compliance-alert. |
</threat_model>

<verification>
## 5. Persistenz-Schicht-Verifikation (Punkt 21) — Welle 5

### Angefasste Tabellen
- `rubric_score` (lesen: observations_jsonb [inkl. '_compliance'] fuer das Panel — die Zeile wird ohnehin schon gelesen, Bestand)
- services/beleg_check.py liest KEINE DB (reine Text-Funktion); der Checkpoint liest transcript_segments read-only via inspect.sh.

### inspect.sh-Beleg (read_first)
```
$ ssh ... 'bash scripts/inspect.sh schema rubric_score' → observations_jsonb/ratings_jsonb existieren (Plan 02/03 deployed).
$ ssh ... 'bash scripts/inspect.sh sample rubric_score' → observations_jsonb befuellt nach einem Judge-Lauf (inkl. '_compliance' bei Verstoss).
```

### Cross-Layer-Konsistenz-Tabelle
| Feld | Pfad | Schicht | Schreib-Zeitpunkt (Punkt 26) | Verifiziert |
|---|---|---|---|---|
| observations_jsonb (lesen) | dashboard session_detail | DB rubric_score (JSONB) | Batch am Call-Ende (Judge, Plan 03) — Panel liest read-only, async-Merge ggf. noch nicht durch → (d)-Zweig "wird ausgewertet" | ✓ |
| _compliance (lesen, Finding 2) | dashboard session_detail (prominente Notiz) | DB rubric_score.observations_jsonb (JSONB-Schluessel) | s.o. (Judge-Schreiber, Plan 03) | ✓ |
| outcome | nur is-not-None (Sperre) | DB calls.outcome | Bestand | ✓ Leakage-Schutz (kein Wert gelesen) |

### Punkt 26
Das Panel ist read-only und hat den (d)-row-absent-Zweig fuer den Fall, dass der async Judge-Merge noch nicht durch
ist — kein falscher Schluss aus einer fehlenden Zeile.

## Latenz (Punkt 25)
Reine Anzeige-Welle + Fuzzy-Helfer (stdlib, ms) + Checkpoint. Kein Live-Pfad beruehrt.
</verification>

<success_criteria>
- Preview-Panel zeigt Beobachtungen + woertliche Beleg-Zitate je fester Dimension, KEINE Zahl, KI-Hinweis-Label,
  "Nicht genug zum Bewerten" bei leer; ratings_jsonb (intern) NICHT angezeigt.
- Cross-AI-Finding 2: bei _compliance.verletzt prominente Compliance-Notiz GANZ OBEN (Compliance VOR Coaching,
  Ton-Wechsel), nicht zwischen den Dimensionen vergraben.
- dashboard.py stellt observations (geordnet nach DIMENSIONS) + compliance separat bereit; outcome nur als Sperre.
- Cross-AI-Finding 3: services/beleg_check.py macht den halluzinierte-Belege-Check FUZZY (normalisiert + Token-Overlap/
  Levenshtein via stdlib difflib), Near-Miss → flaggen statt verwerfen, KEIN BLEU/ROUGE/Cosine.
- nerve.css: nur .n-*-Klassen + CSS-Variablen, kein hardcoded Hex/Inline-Style/Fremd-Farbe (CLAUDE.md UI + Punkt 8).
- checkpoint:decision D-RESIDENZ (André) + finaler BLOCKENDER human-verify-Checkpoint (Migration als postgres VOR
  Restart → deploy.sh production → cold-call Live-Test inkl. Compliance-Test → halluzinierte-Belege-Check FUZZY).
- autonomous: false. Plan 05 bleibt offen bis Live-Test + Fuzzy-Belege-Check gruen.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO2.HANDLING-TIMING-einwand-behandlung-call-ende/08.23.2.TAXO2.HANDLING-TIMING-05-SUMMARY.md`

**SUMMARY-Pflicht-Inhalt:** Screenshot/Beleg des Beobachtungs-Panels (keine Zahl) + der prominenten Compliance-Notiz
bei Verstoss (Finding 2); Ergebnis des halluzinierte-Belege-Checks FUZZY (~10 Bewertungen: ok/near_miss/no_match je
Beobachtung, Finding 3); D-RESIDENZ-Entscheidung; Bestaetigung dass Outcome + NERVE-Vorschlaege aus dem Verhaltens-
Prompt physisch raus sind; Phase-2-Tickets notiert (κ/Experten, Cross-Family, Bedrock-Frankfurt, Embedding-Vorfilter,
Vier-Felder-Tafel).
</output>
