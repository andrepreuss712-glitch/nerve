---
phase: 08.23.2.TAXO2.HANDLING-TIMING
plan: 06
gap_closure: true
gaps_addressed: [GATE-ADOPTION-PAIRING]
subsystem: adoption-judge
tags: [adoption, llm-judge, gap-closure, simplification]
dependency_graph:
  requires: [plan-04]
  provides: [whole-transcript-adoption-judge, test-gate-green]
  affects: [services/adoption_runner.py, tests/test_adoption_runner.py]
tech_stack:
  patterns: [whole-transcript-LLM-judge, forced-tool-use, direct-DB-mock]
key_files:
  modified:
    - services/adoption_runner.py
    - tests/test_adoption_runner.py
  unchanged:
    - database/models.py
    - services/slow_lane.py
    - services/judge_runner.py
key_decisions:
  - Drop _build_adoption_pairs entirely; LLM reads whole transcript and judges per interaction_id itself (Punkt 27 — einfachster tragfaehiger Weg)
  - ts_offered omitted from prompt (Wall-Clock vs ts_ms unvergleichbar; LLM finds the relevant passage itself)
  - Mirror judge_runner.py prompt format exactly ([#i speaker ts_ms ms] text)
  - hasattr-Negativguard in test_whole_transcript_and_suggestion_list accepted as CLAUDE.md Grenzfall (absence of function only testable via hasattr)
requirements_completed: []
completed: 2026-06-28
duration_minutes: 15
tasks_completed: 2
files_modified: 2
---

# Phase 08.23.2.TAXO2.HANDLING-TIMING Plan 06: Adoption LLM-Judgement Gap-Closure Summary

**One-liner:** Drop `_build_adoption_pairs` (Wall-Clock/ts_ms fragility); `run_adoption_judge` now feeds the whole anonymized transcript (ts_ms ASC) + suggestion list to one bundled Sonnet tool-use call that judges per interaction_id itself.

## Files Modified

| File | Change |
|------|--------|
| `services/adoption_runner.py` | `_build_adoption_pairs` removed; `_build_adoption_prompt(transcript_segments, suggestions)` rewritten on whole-transcript paradigm; `run_adoption_judge` queries TranscriptSegment ts_ms ASC + SuggestionReaction directly; write-loop + ADOPTION_TOOL schema unchanged |
| `tests/test_adoption_runner.py` | `test_pair_build` removed; `test_whole_transcript_and_suggestion_list` added; `test_bundled_single_call` / `test_prompt_has_no_outcome` / `test_write_adoption` adapted to direct DB mock (no `_build_adoption_pairs` monkeypatch) |

## Commits

| Hash | Message |
|------|---------|
| `9c3bfaf` | fix(08.23.2.TAXO2.HANDLING-TIMING-06): remove _build_adoption_pairs, whole-transcript LLM paradigm |
| `ac4623f` | fix(08.23.2.TAXO2.HANDLING-TIMING-06): replace test_pair_build with test_whole_transcript_and_suggestion_list; adapt 3 tests to direct DB mock |

## Invariant Verification

| Invariant | Status |
|-----------|--------|
| `def _build_adoption_pairs` grep == 0 in adoption_runner.py | CONFIRMED (grep 0) |
| `def run_adoption_judge` grep == 1 | CONFIRMED |
| TranscriptSegment + order_by ts_ms asc in run_adoption_judge | CONFIRMED (lines 264-271) |
| No difflib/SequenceMatcher/bleu/rouge/cosine in adoption_runner.py | CONFIRMED (grep 0) |
| calls.outcome not in runtime path (only in docstrings/comments) | CONFIRMED |
| tool_choice + tools= in adoption_runner.py | CONFIRMED (lines 287-288) |
| adoption_value / reaction_class / following_utterance_ref written | CONFIRMED (lines 301-309) |
| database/models.py UNCHANGED | CONFIRMED (0 diff) |
| services/slow_lane.py UNCHANGED | CONFIRMED (0 diff) |
| services/judge_runner.py UNCHANGED | CONFIRMED (0 diff) |
| py_compile services/adoption_runner.py AST-OK | PASSED |
| py_compile tests/test_adoption_runner.py AST-OK | PASSED |

## Test Changes

| Test | Status |
|------|--------|
| test_no_word_match | UNCHANGED |
| test_pair_build | REMOVED (tested deleted function) |
| test_whole_transcript_and_suggestion_list | ADDED — asserts all transcript segments in prompt, suggestion list (iid+text) in prompt, hasattr-Negativguard for _build_adoption_pairs, exactly 1 LLM call |
| test_bundled_single_call | ADAPTED — direct DB mock, no _build_adoption_pairs patch |
| test_prompt_has_no_outcome | ADAPTED — direct DB mock, no _build_adoption_pairs patch |
| test_write_adoption | ADAPTED — direct DB mock, no _build_adoption_pairs patch |

Total: 5 test functions (same count as before).

## Deviations from Plan

None — plan executed exactly as written. `ts_offered` omitted from prompt as per the plan's final decision in action section (D): "ts_offered WEGLASSEN aus dem Prompt".

## Acceptance

### Static (local, completed)

- `python -m py_compile services/adoption_runner.py` — PASSED (AST-OK)
- `python -m py_compile tests/test_adoption_runner.py` — PASSED (AST-OK)
- grep `def _build_adoption_pairs` adoption_runner.py == 0 — PASSED
- grep `def run_adoption_judge` adoption_runner.py == 1 — PASSED
- grep difflib/bleu/cosine adoption_runner.py == 0 (runtime) — PASSED
- grep outcome in runtime path == 0 — PASSED
- grep TranscriptSegment/order_by ts_ms adoption_runner.py — FOUND
- grep tool_choice/tools= adoption_runner.py — FOUND
- grep def test_pair_build test file == 0 — PASSED
- grep def test_whole_transcript_and_suggestion_list == 1 — PASSED
- grep ^def test_ count == 5 — PASSED
- grep monkeypatch.*_build_adoption_pairs == 0 — PASSED

### Deferred to Deploy-Gate (per CLAUDE.md HART Kein-Local-Dev)

```
bash deploy.sh production
  -> pytest tests/test_adoption_runner.py -x -v (auf Prod-Server im deploy.sh gate)
     test_whole_transcript_and_suggestion_list: muss GREEN sein
     test_no_word_match: muss GREEN sein
     test_bundled_single_call: muss GREEN sein
     test_prompt_has_no_outcome: muss GREEN sein
     test_write_adoption: muss GREEN sein
  -> Live-Test im Browser mit Test-User (1 Call mit Vorschlaegen)
  -> inspect.sh sample suggestion_reactions (adoption_value/reaction_class/following_utterance_ref
     befuellt nach dem Adoption-Call-Ende-Schritt)
```

## Self-Check: PASSED

- `services/adoption_runner.py` exists and compiles: FOUND
- `tests/test_adoption_runner.py` exists and compiles: FOUND
- commit `9c3bfaf` in git log: FOUND
- commit `ac4623f` in git log: FOUND
- `_build_adoption_pairs` grep == 0 in adoption_runner.py: CONFIRMED
- `test_pair_build` grep == 0 in test file: CONFIRMED
- `test_whole_transcript_and_suggestion_list` grep == 1 in test file: CONFIRMED
- database/models.py unchanged: CONFIRMED
- services/slow_lane.py unchanged: CONFIRMED
- services/judge_runner.py unchanged: CONFIRMED
