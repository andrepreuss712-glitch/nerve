---
phase: 04.6-sales-performance-calculator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - app.py
  - routes/performance.py
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "ConversationLog.result Spalte existiert mit Werten 'gewonnen' | 'verloren' | NULL"
    - "User.avg_deal_wert Spalte existiert als INTEGER mit Default NULL"
    - "GET /api/performance liefert calls_pro_woche, closing_rate, einwand_erfolgsquote, avg_deal_wert, umsatz_pro_monat, forecast_3m, forecast_6m, forecast_12m, trend_prozent, chart_data, hat_daten"
    - "PATCH /api/session/<id>/result speichert 'gewonnen' | 'verloren' | NULL fuer live Sessions"
    - "PATCH /api/user/deal-wert speichert avg_deal_wert fuer eingeloggten User"
  artifacts:
    - path: "database/models.py"
      provides: "ConversationLog.result + User.avg_deal_wert columns"
      contains: "avg_deal_wert"
    - path: "app.py"
      provides: "DB migrations fuer beide neuen Spalten"
      contains: "avg_deal_wert"
    - path: "routes/performance.py"
      provides: "performance_bp mit 3 API Endpoints"
      contains: "performance_bp"
  key_links:
    - from: "routes/performance.py (api_performance)"
      to: "database/models.py (ConversationLog)"
      via: "SQLAlchemy query filtered by typ='live' and user_id"
      pattern: "typ.*==.*live"
    - from: "routes/performance.py (api_performance)"
      to: "database/models.py (User)"
      via: "g.user.avg_deal_wert fuer Deal-Wert"
      pattern: "avg_deal_wert"
---

<objective>
DB-Migrationen + neues performance Blueprint mit 3 API-Endpoints — die Datenbasis fuer den Sales Performance Calculator.

Purpose: Ohne die neuen DB-Felder und die Backend-API hat das Frontend keine Daten. Dieser Plan legt das gesamte Backend-Fundament.
Output: Zwei neue DB-Spalten (ConversationLog.result, User.avg_deal_wert), drei API-Endpoints (GET /api/performance, PATCH /api/session/id/result, PATCH /api/user/deal-wert), Blueprint registriert in app.py.
</objective>

<execution_context>
@.planning/phases/04.6-sales-performance-calculator/04.6-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@database/models.py
@app.py
@routes/dashboard.py

<interfaces>
<!-- Bestehende Interfaces die der Executor braucht -->

From database/models.py:
```python
class User(Base):
    __tablename__ = 'users'
    id                  = Column(Integer, primary_key=True)
    org_id              = Column(Integer, ForeignKey('organisations.id'), nullable=False)
    # ... vorhandene Felder ...
    weekly_goal         = Column(Integer, default=5)
    # avg_deal_wert EXISTIERT NOCH NICHT — muss ergaenzt werden

class ConversationLog(Base):
    __tablename__ = 'conversation_logs'
    id                       = Column(Integer, primary_key=True)
    user_id                  = Column(Integer, ForeignKey('users.id'), nullable=False)
    org_id                   = Column(Integer, ForeignKey('organisations.id'), nullable=False)
    einwaende_gesamt         = Column(Integer, default=0)
    einwaende_behandelt      = Column(Integer, default=0)
    typ                      = Column(String(20), default='live')   # 'live' oder 'training'
    session_mode             = Column(String(20), default='meeting')
    created_at               = Column(DateTime, default=utcnow)
    # result EXISTIERT NOCH NICHT — muss ergaenzt werden
```

From app.py _migrate() pattern (lines 47-96):
```python
for col, typedef in [
    ('some_col', 'INTEGER DEFAULT 0'),
]:
    try:
        conn.execute(text(f'ALTER TABLE users ADD COLUMN {col} {typedef}'))
        conn.commit()
        print(f"[DB] Migration: added users.{col}")
    except Exception:
        pass
# Gleiche Struktur fuer conversation_logs
```

From routes/auth.py (login_required decorator):
```python
from routes.auth import login_required
# g.user, g.org verfuegbar in jedem @login_required request
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Schema — result + avg_deal_wert + Migrationen</name>
  <files>database/models.py, app.py</files>
  <action>
  **1. database/models.py — Neue Spalten ergaenzen:**

  In der `User`-Klasse (nach `weekly_goal`, Zeile ~106), ergaenzen:
  ```python
  # Block 12: Sales Performance Calculator
  avg_deal_wert       = Column(Integer, nullable=True)   # Euro, NULL = nicht gesetzt
  ```

  In der `ConversationLog`-Klasse (nach `session_mode`, Zeile ~224), ergaenzen:
  ```python
  result                   = Column(String(20), nullable=True)  # 'gewonnen' | 'verloren' | NULL
  ```

  **2. app.py — _migrate() Bloecke erweitern:**

  Im `users`-Migrations-Block (nach dem letzten vorhandenen Eintrag, vor dem schliessenden `]:`):
  ```python
  # Block 12: Sales Performance Calculator
  ('avg_deal_wert', 'INTEGER'),
  ```

  Im `conversation_logs`-Migrations-Block (nach `session_mode`):
  ```python
  ('result', 'VARCHAR(20)'),
  ```

  Wenn noch kein `conversation_logs`-Block in `_migrate()` fuer neue Spalten existiert, den bestehenden Block (Zeile ~157-165) um diesen Eintrag erweitern.
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && python -c "from database.models import User, ConversationLog; print('avg_deal_wert:', hasattr(User, 'avg_deal_wert')); print('result:', hasattr(ConversationLog, 'result'))"</automated>
  </verify>
  <done>
  - `User.avg_deal_wert` Column existiert in models.py als INTEGER nullable=True
  - `ConversationLog.result` Column existiert in models.py als String(20) nullable=True
  - Beide Migrationen in app.py `_migrate()` eingetragen
  - Flask App startet ohne Fehler (migrations laufen beim Start)
  </done>
</task>

<task type="auto">
  <name>Task 2: routes/performance.py — 3 API-Endpoints + Blueprint-Registrierung</name>
  <files>routes/performance.py, app.py</files>
  <action>
  **1. routes/performance.py — Neue Datei anlegen:**

  ```python
  import json
  from datetime import datetime, timedelta
  from flask import Blueprint, jsonify, request, g
  from routes.auth import login_required
  from database.db import get_session
  from database.models import ConversationLog, User as UserModel

  performance_bp = Blueprint('performance', __name__)

  # ── Hilfsfunktionen ───────────────────────────────────────────────────────────

  def _get_calendar_week(dt):
      """Gibt ISO-Kalenderwoche als String zurueck: '2026-W14'"""
      return dt.strftime('%G-W%V')

  def _berechne_forecast(calls_pro_monat, closing_rate, avg_deal_wert):
      """
      Berechnet Umsatz-Forecast fuer 3/6/12 Monate.
      S-Kurven-Effekt: leicht exponentiell (Faktor 1.05 pro Monat durch NERVE-Verbesserung).
      """
      if not avg_deal_wert or calls_pro_monat == 0:
          return {'forecast_3m': 0, 'forecast_6m': 0, 'forecast_12m': 0}

      umsatz_monat = calls_pro_monat * (closing_rate / 100.0) * avg_deal_wert
      wachstum = 1.05   # 5% monatliche Verbesserung durch NERVE (Marketing-Annahme)

      f3, f6, f12 = 0, 0, 0
      for i in range(1, 13):
          monat_umsatz = round(umsatz_monat * (wachstum ** i))
          if i <= 3:
              f3 += monat_umsatz
          if i <= 6:
              f6 += monat_umsatz
          f12 += monat_umsatz

      return {
          'forecast_3m':  f3,
          'forecast_6m':  f6,
          'forecast_12m': f12,
      }

  # ── GET /api/performance ──────────────────────────────────────────────────────

  @performance_bp.route('/api/performance')
  @login_required
  def api_performance():
      db = get_session()
      try:
          # Lade User fuer avg_deal_wert
          user = db.get(UserModel, g.user.id)
          avg_deal_wert = user.avg_deal_wert if user else None

          # Alle Live-Sessions des Users (letzte 90 Tage fuer Charts)
          cutoff_90 = datetime.now() - timedelta(days=90)
          logs_90 = (db.query(ConversationLog)
                     .filter(
                         ConversationLog.user_id == g.user.id,
                         ConversationLog.typ == 'live',
                         ConversationLog.created_at >= cutoff_90,
                     )
                     .order_by(ConversationLog.created_at.asc())
                     .all())

          hat_daten = len(logs_90) >= 3

          # ── Calls pro Woche (letzte 8 Wochen) ────────────────────────────
          cutoff_56 = datetime.now() - timedelta(days=56)
          logs_8w = [l for l in logs_90 if l.created_at and l.created_at >= cutoff_56]

          wochen_calls = {}   # '2026-W14' -> count
          wochen_abschluesse = {}
          for l in logs_8w:
              if not l.created_at:
                  continue
              kw = _get_calendar_week(l.created_at)
              wochen_calls[kw] = wochen_calls.get(kw, 0) + 1
              if getattr(l, 'result', None) == 'gewonnen':
                  wochen_abschluesse[kw] = wochen_abschluesse.get(kw, 0) + 1

          # Letzten 8 Wochen aufsteigend
          alle_wochen = sorted(set(list(wochen_calls.keys())))[-8:]
          chart_data = {
              'labels': alle_wochen,
              'calls':  [wochen_calls.get(w, 0) for w in alle_wochen],
              'abschluesse': [wochen_abschluesse.get(w, 0) for w in alle_wochen],
          }

          # ── Calls pro Woche (Durchschnitt letzte 4 Wochen) ───────────────
          cutoff_28 = datetime.now() - timedelta(days=28)
          logs_4w = [l for l in logs_90 if l.created_at and l.created_at >= cutoff_28]
          calls_pro_woche = round(len(logs_4w) / 4.0, 1) if logs_4w else 0
          calls_pro_monat = round(calls_pro_woche * 4.3)

          # ── Closing Rate ──────────────────────────────────────────────────
          getaggt = [l for l in logs_90 if getattr(l, 'result', None) in ('gewonnen', 'verloren')]
          gewonnen = [l for l in getaggt if getattr(l, 'result', None) == 'gewonnen']
          closing_rate = round(len(gewonnen) / len(getaggt) * 100, 1) if getaggt else None

          # ── Einwand-Erfolgsquote ──────────────────────────────────────────
          total_einwaende = sum(l.einwaende_gesamt or 0 for l in logs_90)
          total_behandelt = sum(l.einwaende_behandelt or 0 for l in logs_90)
          einwand_erfolgsquote = round(total_behandelt / total_einwaende * 100, 1) if total_einwaende > 0 else None

          # ── Umsatz-Berechnung ─────────────────────────────────────────────
          cr_fuer_formel = closing_rate or 20.0   # Fallback fuer Simulation
          dw_fuer_formel = avg_deal_wert or 0
          umsatz_pro_monat = round(calls_pro_monat * (cr_fuer_formel / 100.0) * dw_fuer_formel) if dw_fuer_formel else 0

          forecast = _berechne_forecast(calls_pro_monat, cr_fuer_formel, dw_fuer_formel)

          # ── Trend vs. Vormonat ────────────────────────────────────────────
          cutoff_30 = datetime.now() - timedelta(days=30)
          cutoff_60 = datetime.now() - timedelta(days=60)
          logs_letzter_monat = [l for l in logs_90 if l.created_at and l.created_at >= cutoff_30]
          logs_vormonat = [l for l in logs_90 if l.created_at and cutoff_60 <= l.created_at < cutoff_30]
          trend_prozent = None
          if logs_vormonat:
              trend_prozent = round((len(logs_letzter_monat) - len(logs_vormonat)) / max(len(logs_vormonat), 1) * 100)

          # ── ROI ──────────────────────────────────────────────────────────
          # ROI = (15% Einwand-Verbesserung) * Closing-Rate-Delta * Calls/Monat * avg_deal_wert - 99
          # Annahme: NERVE verbessert Einwand-Erfolgsquote um 15% (Marketing-Annahme, klar gelabelt)
          roi_mehrwert = None
          if dw_fuer_formel and closing_rate is not None:
              nerve_verbesserung = 0.15   # 15% Einwand-Erfolgsquote-Verbesserung (Annahme)
              cr_delta = closing_rate * nerve_verbesserung / 100.0
              roi_mehrwert = round(calls_pro_monat * cr_delta * dw_fuer_formel) - 99

          return jsonify({
              'hat_daten':            hat_daten,
              'calls_pro_woche':      calls_pro_woche,
              'calls_pro_monat':      calls_pro_monat,
              'closing_rate':         closing_rate,
              'einwand_erfolgsquote': einwand_erfolgsquote,
              'avg_deal_wert':        avg_deal_wert,
              'umsatz_pro_monat':     umsatz_pro_monat,
              'forecast_3m':          forecast['forecast_3m'],
              'forecast_6m':          forecast['forecast_6m'],
              'forecast_12m':         forecast['forecast_12m'],
              'trend_prozent':        trend_prozent,
              'chart_data':           chart_data,
              'roi_mehrwert':         roi_mehrwert,
              'total_live_calls':     len(logs_90),
          })
      finally:
          db.close()

  # ── PATCH /api/session/<id>/result ───────────────────────────────────────────

  @performance_bp.route('/api/session/<int:session_id>/result', methods=['PATCH'])
  @login_required
  def api_session_result(session_id):
      data = request.get_json(force=True) or {}
      result = data.get('result')
      # Nur erlaubte Werte akzeptieren
      if result not in ('gewonnen', 'verloren', None):
          return jsonify({'error': 'Ungültiger Wert — erlaubt: gewonnen | verloren | null'}), 400

      db = get_session()
      try:
          log = (db.query(ConversationLog)
                 .filter(
                     ConversationLog.id == session_id,
                     ConversationLog.user_id == g.user.id,   # Nur eigene Sessions
                     ConversationLog.typ == 'live',
                 )
                 .first())
          if not log:
              return jsonify({'error': 'Session nicht gefunden'}), 404
          log.result = result
          db.commit()
          return jsonify({'ok': True, 'result': result})
      finally:
          db.close()

  # ── PATCH /api/user/deal-wert ─────────────────────────────────────────────────

  @performance_bp.route('/api/user/deal-wert', methods=['PATCH'])
  @login_required
  def api_user_deal_wert():
      data = request.get_json(force=True) or {}
      wert = data.get('avg_deal_wert')
      try:
          wert = int(wert)
          if wert < 0 or wert > 10_000_000:
              raise ValueError
      except (TypeError, ValueError):
          return jsonify({'error': 'Ungültiger Wert — muss positive Ganzzahl in Euro sein'}), 400

      db = get_session()
      try:
          user = db.get(UserModel, g.user.id)
          if not user:
              return jsonify({'error': 'User nicht gefunden'}), 404
          user.avg_deal_wert = wert
          db.commit()
          return jsonify({'ok': True, 'avg_deal_wert': wert})
      finally:
          db.close()
  ```

  **2. app.py — Blueprint registrieren:**

  Finde die Stelle in app.py wo andere Blueprints importiert und registriert werden (nach `from routes.dashboard import dashboard_bp`). Ergaenze:

  ```python
  from routes.performance import performance_bp
  ```

  Und in der `app.register_blueprint()`-Sektion:
  ```python
  app.register_blueprint(performance_bp)
  ```

  Wenn kein expliziter Register-Block existiert, suche nach einem Pattern wie `app.register_blueprint(dashboard_bp)` und fuege die Zeile direkt danach ein.
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && python -c "from routes.performance import performance_bp; routes = [str(r) for r in performance_bp.deferred_functions]; print('Blueprint created'); print('Rules:', [r.rule for r in performance_bp.url_map.iter_rules()] if hasattr(performance_bp, 'url_map') else 'pending')" 2>&1; python -c "import app; rules = [r.rule for r in app.app.url_map.iter_rules() if 'performance' in r.rule or 'deal' in r.rule or 'result' in r.rule]; print('Performance routes:', rules)"</automated>
  </verify>
  <done>
  - `routes/performance.py` existiert mit `performance_bp` Blueprint
  - `GET /api/performance` liefert JSON mit allen Feldern: hat_daten, calls_pro_woche, calls_pro_monat, closing_rate, einwand_erfolgsquote, avg_deal_wert, umsatz_pro_monat, forecast_3m, forecast_6m, forecast_12m, trend_prozent, chart_data, roi_mehrwert, total_live_calls
  - `PATCH /api/session/<id>/result` akzeptiert 'gewonnen' | 'verloren' | null, validiert User-Eigentuemer, gibt 404 bei fremder Session
  - `PATCH /api/user/deal-wert` akzeptiert positive Ganzzahl, speichert in User.avg_deal_wert
  - Blueprint in app.py registriert
  - Flask App startet ohne ImportError oder Blueprint-Konflikte
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> PATCH /api/session/<id>/result | User-Input 'result' — muss auf Whitelist geprueft werden |
| Client -> PATCH /api/user/deal-wert | User-Input Zahl — muss als int validiert und auf Plausibilitaet geprueft werden |
| Client -> GET /api/performance | Keine Input-Parameter — nur Auth-Pruefung noetig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.6-01 | S (Spoofing) | api_session_result() | mitigate | Filter `ConversationLog.user_id == g.user.id` stellt sicher, dass User nur eigene Sessions taggen kann — kein IDOR moeglich |
| T-04.6-02 | T (Tampering) | result Feld Whitelist | mitigate | Explicit Whitelist-Check: `if result not in ('gewonnen', 'verloren', None): return 400` — kein beliebiger String kann eingeschleust werden |
| T-04.6-03 | T (Tampering) | avg_deal_wert Validierung | mitigate | `int()` cast + Range-Check 0-10M — verhindert negative Werte und Integer-Overflow |
| T-04.6-04 | I (Info Disclosure) | api_performance | accept | Endpoint liefert nur Daten des eingeloggten Users (g.user.id Filter) — kein Cross-User-Leak |
| T-04.6-05 | D (DoS) | api_performance Query | accept | Queries auf conversation_logs mit user_id Index (PKs existieren), 90-Tage-Cutoff begrenzt Result-Set — kein full-table-scan |
</threat_model>

<verification>
1. Flask App starten: `python app.py` — kein ImportError, Migration gibt "[DB] Migration: added users.avg_deal_wert" und "[DB] Migration: added conversation_logs.result" aus (beim ersten Start)
2. API-Endpoint testen (nach Login): `curl -b cookies.txt http://localhost:5000/api/performance` — gibt JSON mit allen Feldern zurueck (hat_daten: false wenn keine Live-Calls)
3. Deal-Wert setzen: `curl -X PATCH -b cookies.txt -H 'Content-Type: application/json' -d '{"avg_deal_wert": 5000}' http://localhost:5000/api/user/deal-wert` — gibt `{"ok": true, "avg_deal_wert": 5000}` zurueck
4. Ungueltige Werte testen: `avg_deal_wert: -1` gibt 400, `result: "unknown"` gibt 400
</verification>

<success_criteria>
- Beide neuen DB-Spalten existieren und werden bei App-Start migriert
- GET /api/performance liefert vollstaendiges JSON (alle 14 Felder)
- PATCH-Endpoints validieren Input und verhindern Cross-User-Zugriff
- Kein Einfluss auf bestehende Dashboard-Route oder andere Blueprints
</success_criteria>

<output>
After completion, create `.planning/phases/04.6-sales-performance-calculator/04.6-01-SUMMARY.md`
</output>
