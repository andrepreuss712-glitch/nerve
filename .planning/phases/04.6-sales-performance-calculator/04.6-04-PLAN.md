---
phase: 04.6-sales-performance-calculator
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06]
must_haves:
  truths:
    - "GET /api/performance gibt 200 zurueck mit allen 14 Feldern"
    - "Dashboard zeigt Sales Performance Sektion sichtbar ohne JS-Fehler"
    - "KPI-Karten zeigen Werte (oder Leerzustand mit Hinweis auf Simulation)"
    - "Beide Charts rendern fehlerfrei"
    - "Modus-Toggle wechselt korrekt zwischen Echte Daten und Simulation"
    - "Schieberegler berechnen Werte live"
    - "Deal-Wert-Modal erscheint ohne gespeicherten avg_deal_wert"
    - "PATCH /api/user/deal-wert speichert Wert (200 Response)"
    - "PATCH /api/session/<id>/result valiert Whitelist und gibt 400 bei ungueltigem Wert"
  artifacts:
    - path: "routes/performance.py"
      provides: "Alle 3 Endpoints funktionsfaehig"
      contains: "performance_bp"
    - path: "templates/dashboard.html"
      provides: "Sales Performance Sektion vollstaendig gerendert"
      contains: "perf-section"
    - path: "static/nerve.css"
      provides: "Slider CSS und responsive Grid-Regeln"
      contains: "perf-slider"
  key_links:
    - from: "Browser (dashboard.html)"
      to: "routes/performance.py (/api/performance)"
      via: "fetch('/api/performance') in perfInit()"
      pattern: "api/performance"
---

<objective>
End-to-End visuelle Verifikation des Sales Performance Calculators — alle Elemente rendern korrekt, alle API-Endpoints antworten korrekt, interaktive Features funktionieren.

Purpose: Letzte Gate-Pruefung bevor die Phase abgeschlossen wird. Claude fuehrt alle automatisierbaren Tests durch, dann verifiziert der User das visuelle Ergebnis im Browser.
Output: Bestaetigung dass alle 6 PERF-Requirements erfuellt sind.
</objective>

<execution_context>
@.planning/phases/04.6-sales-performance-calculator/04.6-CONTEXT.md
@.planning/phases/04.6-sales-performance-calculator/04.6-01-SUMMARY.md
@.planning/phases/04.6-sales-performance-calculator/04.6-02-SUMMARY.md
@.planning/phases/04.6-sales-performance-calculator/04.6-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automatisierte Verifikation — DB, Imports, API-Struktur</name>
  <files></files>
  <action>
  Fuehre folgende automatisierte Checks aus und behebe gefundene Probleme:

  **Check 1: DB-Modell**
  ```bash
  cd /c/Users/andre/OneDrive/Desktop/salesnerve && python -c "
  from database.models import User, ConversationLog
  u_cols = [c.key for c in User.__table__.columns]
  cl_cols = [c.key for c in ConversationLog.__table__.columns]
  print('avg_deal_wert in User:', 'avg_deal_wert' in u_cols)
  print('result in ConversationLog:', 'result' in cl_cols)
  "
  ```
  Erwartet: beide True.

  **Check 2: Blueprint registriert**
  ```bash
  python -c "
  import app
  routes = [r.rule for r in app.app.url_map.iter_rules()]
  perf_routes = [r for r in routes if 'performance' in r or 'deal-wert' in r or '/result' in r]
  print('Performance routes:', perf_routes)
  "
  ```
  Erwartet: Mindestens `/api/performance`, `/api/user/deal-wert`, `/api/session/<session_id>/result`

  **Check 3: HTML-Vollstaendigkeit**
  ```bash
  python -c "
  content = open('templates/dashboard.html', encoding='utf-8').read()
  required = ['perf-section', 'kpi-deals', 'kpi-umsatz', 'kpi-forecast', 'kpi-roi',
              'call-activity-chart', 'growth-chart', 'sl-calls-tag', 'sl-show-rate',
              'sl-closing-rate', 'sl-deal-wert', 'sl-arbeitstage', 'deal-wert-modal',
              'result-tag-bar', 'perfInit', 'perfSimUpdate', 'saveDealWert', 'tagSession',
              'chart.js', '/api/performance', '/api/user/deal-wert']
  for r in required:
    status = 'OK' if r.lower() in content.lower() else 'FEHLT'
    print(f'{status}: {r}')
  "
  ```
  Erwartet: Alle OK.

  **Check 4: CSS vorhanden**
  ```bash
  python -c "
  css = open('static/nerve.css', encoding='utf-8').read()
  checks = ['perf-slider', '#00D4AA', 'perf-kpis', 'perf-charts']
  for c in checks:
    print('OK' if c in css else 'FEHLT', c)
  "
  ```

  **Check 5: Flask App startet**
  ```bash
  timeout 10 python -c "
  import app
  print('OK: Flask app created')
  print('OK: Blueprints registered:', len(list(app.app.url_map.iter_rules())))
  " 2>&1
  ```

  **Falls Probleme gefunden:** Behebe sie direkt in den betroffenen Dateien bevor du zum Checkpoint weitermachst.
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && python -c "
from database.models import User, ConversationLog
u_ok  = hasattr(User, 'avg_deal_wert')
cl_ok = hasattr(ConversationLog, 'result')
import app
rules = [r.rule for r in app.app.url_map.iter_rules()]
perf_ok = any('performance' in r for r in rules)
deal_ok = any('deal-wert' in r for r in rules)
sess_ok = any('result' in r and 'session' in r for r in rules)
content = open('templates/dashboard.html', encoding='utf-8').read()
html_ok = all(t in content for t in ['perf-section', 'kpi-deals', 'perfInit', 'chart.js'])
css = open('static/nerve.css', encoding='utf-8').read()
css_ok = 'perf-slider' in css
all_ok = u_ok and cl_ok and perf_ok and deal_ok and sess_ok and html_ok and css_ok
print('User.avg_deal_wert:', u_ok)
print('ConversationLog.result:', cl_ok)
print('/api/performance:', perf_ok)
print('/api/user/deal-wert:', deal_ok)
print('/api/session/<id>/result:', sess_ok)
print('HTML vollstaendig:', html_ok)
print('CSS vorhanden:', css_ok)
print()
print('ALLE CHECKS OK' if all_ok else 'FEHLER — nicht alle Checks bestanden')
"</automated>
  </verify>
  <done>
  - Alle 7 automatisierten Checks zeigen "OK"
  - Keine FEHLT-Ausgaben in allen Checks
  - Flask App startet ohne ImportError oder AttributeError
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
  Sales Performance Calculator — vollstaendig implementiert:
  - Plan 01: DB-Felder (ConversationLog.result, User.avg_deal_wert) + 3 API-Endpoints
  - Plan 02: Dashboard HTML — Sektion, KPI-Karten, Charts, Slider, Modal, Ergebnis-Tags
  - Plan 03: nerve.css Slider-Styling + vollstaendiges JavaScript (Charts, Simulation, Modal, Tagging)
  </what-built>
  <how-to-verify>
  **Schritt 1 — App starten**
  ```
  cd C:\Users\andre\OneDrive\Desktop\salesnerve
  python app.py
  ```
  Dann http://localhost:5000/dashboard oeffnen (eingeloggt als Testuser).

  **Schritt 2 — Deal-Wert-Modal**
  Wenn `avg_deal_wert` noch nicht gesetzt ist (erstmaliger Aufruf nach Migration):
  - [ ] Modal erscheint automatisch mit Eingabefeld
  - [ ] Zahl eingeben (z.B. 5000), "Speichern" klicken
  - [ ] Modal schliesst sich, Performance-Sektion laedt neu

  **Schritt 3 — Sales Performance Sektion**
  Scrolle auf der Dashboard-Seite nach unten:
  - [ ] "Sales Performance" Sektion sichtbar (unterhalb NBA-Card, oberhalb Footer-Chips)
  - [ ] Modus-Toggle "Echte Daten / Simulation" sichtbar
  - [ ] 4 KPI-Karten sichtbar (Deals/Monat, Umsatz/Monat, Forecast 12M, ROI vs 99€)
  - [ ] Design: weisse Cards (#FFFFFF), kein dunkler Hintergrund, teal Werte

  **Schritt 4 — Charts (wenn Live-Calls vorhanden)**
  - [ ] Balkendiagramm (Call-Aktivitaet) rendert ohne Fehler
  - [ ] Wachstumskurve (Umsatz-Forecast) rendert als teal Linie
  - [ ] Wenn keine Daten: Leerzustand mit "Starte deinen ersten Live-Call" sichtbar

  **Schritt 5 — Simulations-Modus**
  - [ ] Auf "Simulation" klicken — Panel mit 5 Schiebereglern erscheint
  - [ ] Schieberegler verschieben — Werte in "Umsatz/Monat", "Forecast 12M", "ROI" aendern sich live (ca. 150ms Verzoegerung)
  - [ ] Slider-Thumb ist teal (#00D4AA), kein Browser-Standard-Styling

  **Schritt 6 — API-Direkttest**
  Im Browser-Tab: http://localhost:5000/api/performance aufrufen (eingeloggt)
  - [ ] Gibt JSON zurueck mit allen Feldern (hat_daten, calls_pro_woche, avg_deal_wert, etc.)

  **Schritt 7 — Ergebnis-Tagging**
  URL aufrufen: http://localhost:5000/dashboard?tag_session=1 (beliebige Session-ID)
  - [ ] "Wie lief der Call?" Bar erscheint oberhalb des Footers
  - [ ] "Gewonnen" klicken — "Gespeichert." Feedback erscheint, Bar verschwindet nach 2s

  **Schritt 8 — Keine Regression**
  - [ ] Bestehende Dashboard-Elemente (North Star Score, Quick Actions, Usage, Recent Calls, NBA-Tip) weiterhin korrekt
  - [ ] Sidebar unveraendert
  - [ ] Keine JS-Fehler in der Browser-Console

  **Schritt 9 — Mobile Check**
  Browser-DevTools: Breakpoint auf 375px setzen
  - [ ] KPI-Karten stacken einspaltig
  - [ ] Charts stacken einspaltig
  - [ ] Schieberegler stacken einspaltig
  </how-to-verify>
  <resume-signal>
  Wenn alle Checks bestanden: Antworte mit "approved"
  Wenn Probleme gefunden: Beschreibe welche Elemente fehlen oder nicht korrekt dargestellt werden
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifikationsphase | Keine neuen Trust-Boundaries in diesem Plan — reine Verifikation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.6-12 | I (Info Disclosure) | Verifikationsschritte | accept | Alle Tests laufen lokal — kein Produktionsdaten-Risiko |
</threat_model>

<verification>
Alle Checks in Task 1 automatisiert. Task 2 ist der manuelle Checkpoint.
</verification>

<success_criteria>
- Alle 7 automatisierten Checks in Task 1 zeigen OK
- User bestaetigt alle 9 manuellen Verifikationsschritte
- Keine JS-Fehler in Browser-Console
- Bestehende Dashboard-Funktionalitaet unveraendert
- Phase 04.6 als vollstaendig markierbar
</success_criteria>

<output>
After completion, create `.planning/phases/04.6-sales-performance-calculator/04.6-04-SUMMARY.md`

Then update ROADMAP.md: Mark Phase 04.6 as complete (add checkmark and completion date 2026-04-XX).
Update STATE.md: current focus to next planned phase.
</output>
