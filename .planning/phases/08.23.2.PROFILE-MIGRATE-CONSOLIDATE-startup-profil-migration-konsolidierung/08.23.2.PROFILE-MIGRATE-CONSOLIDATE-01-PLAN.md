---
phase: 08.23.2.PROFILE-MIGRATE-CONSOLIDATE
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - services/profile_schema.py
  - tests/test_profile_migrate_txn.py
autonomous: false
requirements: [CONS-A1, CONS-A2, CONS-A3, CONS-B1, CONS-B2, CONS-B3, CONS-B4]
complexity: "🔴 komplex (Startup-Migration + Postgres-Transaktions-Semantik + delikater Daten-Code → Cross-AI PFLICHT vor Execute; A3 ist eine OFFENE Entscheidung für Cross-AI)"

# ★★ BAUBEREITSCHAFT: A1 + A2 + Teil B (B1/B2/B4) sind entschieden + baubereit.
#    A3 (Task 4) ist eine OFFENE 🔴-ENTSCHEIDUNG (Drei-Wege) → wird von Cross-AI (Fable+Gemini)
#    + André gewählt, DANN via /gsd-plan-phase --reviews in EINE Option gelockt. B3 (Docstring)
#    hängt an der A3-Wahl. NICHT auto-advance zum Bau, bevor A3 gelockt ist.

must_haves:
  truths:
    - "Genau EIN auf Postgres lebender Profil-Migrationsdurchlauf existiert: _migrate_profile_json (app.py:2078). Der PG-tote v4-Batch (_migrate(), app.py:989-1031) ist GELÖSCHT."
    - "Die Datenverlust-Invariante bleibt: opener/pitch/erlaubnis-Sync (rohe INSERTs + commit) läuft VOR dem opener-Pop in _mpd (services/profile_schema.py:304) — Sync ist persistiert, bevor gepoppt wird."
    - "v2/v3-Bestandsprofile werden tatsächlich auf v4 gehoben: der Idempotenz-Skip in _migrate_profile_json steht auf >= LATEST_SCHEMA_VERSION statt >= 2 (rein JSON via _mpd, KEIN Schema-Change, Single-Deploy)."
    - "profiles hat KEINE schema_version-Spalte — jede Versions-Prüfung liest aus dem daten-JSON (Cross-Layer Punkt 21/22)."
    - "A3 (Advisory-Lock 819) ist NICHT vorfestgelegt: die drei Wege stehen mit Trade-offs im Plan; die Wahl trifft Cross-AI + André, dann Lock via --reviews."
  artifacts:
    - path: "app.py"
      provides: "v4-Batch gelöscht (A1); Skip-Schwelle >=LATEST (A2); Boot-Crash-Netz + created_at (B1/B2); Guard-a-WARN-Text aktualisiert"
      contains: "LATEST_SCHEMA_VERSION"
    - path: "services/profile_schema.py"
      provides: "stale Kommentare (opener-Sync „in app.py _migrate()\") auf den lebenden Pfad korrigiert"
      contains: "_migrate_profile_json"
    - path: "tests/test_profile_migrate_txn.py"
      provides: "Erst-Rot v2/v3→v4-Lift-Guard + geschärfte Rollback-Isolation (echter Fehler) + pitch/erlaubnis-Sync-Test"
      contains: "LATEST_SCHEMA_VERSION"
  key_links:
    - from: "_migrate() PG early-return (app.py:140)"
      to: "v4-Batch ist tot → gelöscht, _migrate_profile_json überlebt"
      via: "A1 Konsolidierung auf den lebenden Pfad"
      pattern: "_migrate_profile_json"
    - from: "Idempotenz-Skip (app.py:1461)"
      to: "v2/v3 werden gehoben"
      via: "Schwelle >=2 → >=LATEST_SCHEMA_VERSION"
      pattern: "schema_version"
---

<objective>
Konsolidiert die zwei konkurrierenden Startup-Profil-Migrationspfade auf **EINEN lebenden Pfad**,
hebt die Bestandsprofile tatsächlich auf v4 und repariert die kaputte Advisory-Lock-Semantik —
plus 4 mechanische Hardening-Funde (B1-B4). Aufbauend auf der Härtung aus PROFILE-MIGRATE-TXN-FIX.

**★ Entschieden (CONTEXT.md, code-verifiziert 2026-07-16):**
- **A1:** `_migrate_profile_json` (app.py:1392, Aufruf :2078) ist DER EINE Überlebende. Der v4-Batch
  (app.py:989-1031, in `_migrate()`) ist **PG-tot** (`_migrate()` early-return :140-142) → wird
  **gelöscht**. ★ KEHRT die stale Backlog-Formulierung um (die sagte rückwärts: v4-Batch behalten,
  `_migrate_profile_json` löschen — hätte den lebenden Pfad weggeworfen).
- **A2:** v2/v3→v4-Anhebung via **Skip-Schwelle heben** (`app.py:1461` `>= 2` → `>= LATEST_SCHEMA_VERSION`).
  Rein JSON (`_mpd` fasst keine DB-Spalte an, profile_schema.py:266) → **kein Schema-Change, keine
  Expand/Contract-Ordnung, Single-Deploy** sicher.

**★ OFFEN — A3 (Task 4):** der Advisory-Lock `pg_advisory_xact_lock(819)` (app.py:1429) ist xact-scoped
und stirbt beim ersten `conn.commit()` in der Schleife → Doppel-Row-Race. **Drei Wege stehen mit
Trade-offs im Plan; NICHT vorfestgelegt** — Cross-AI (Fable+Gemini) + André wählen, dann Lock via `--reviews`.

**★ Datenverlust-Invariante (delikat, MUSS erhalten bleiben):** im lebenden Pfad läuft der
opener/pitch/erlaubnis-Sync (rohe INSERTs + `commit`, app.py:1468-1509) BEREITS VOR dem `_mpd`-Aufruf
(app.py:1517), und der opener-Pop sitzt IN `_mpd` (profile_schema.py:304). Diese Reihenfolge darf beim
Löschen des toten Batches NICHT gebrochen werden.

Purpose: EIN Pfad, kein Race, v2/v3 real gehoben, kein stiller Opener/Pitch-Verlust, robuster Boot.
Output: app.py-Konsolidierung + profile_schema.py-Doku-Korrektur + geschärfter Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

★ Multi-Segment-Phase-ID-Gotcha ([[project_nerve_multisegment_phase_ids]]): phase_dir HART =
`.planning/phases/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-startup-profil-migration-konsolidierung`.
SUMMARY = `08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01-SUMMARY.md`. KEIN gsd-tools/gsd-sdk (nicht installiert)
→ plain `git`-Commits, Tracking (STATE/ROADMAP/SUMMARY/VERIFICATION) hand-editiert.

★ HART: Kein Local-Dev. Verify=Production. Tests im deploy.sh-Pytest-Gate auf nerve_test (real-PG;
die Race/Poison-Klasse ist Postgres-only → SQLite wäre False-Green). Live-Verify = human checkpoint
(autonomous:false, Zwei-Tore, Claudian fährt Deploy).

★ ABLAUF (André-Direktive): dieser Plan → Cross-AI Fable+Gemini Plan-Review (löst A3) → /gsd-plan-phase
--reviews (lockt A3 in EINE Option, verdrahtet B3-Docstring) → Claudian-Pre-Execute-Audit →
/gsd-execute-phase (supervised) → Claudian-Deploy. NICHT auto-advance vor A3-Lock.
</execution_context>

<context>
@.planning/phases/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-startup-profil-migration-konsolidierung/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-CONTEXT.md
@.planning/phases/08.23.2.PROFILE-MIGRATE-TXN-FIX-startup-migration-txn-haertung/08.23.2.PROFILE-MIGRATE-TXN-FIX-01-SUMMARY.md
@salesnerve/CLAUDE.md

<interfaces>
<!-- Anker per INHALT re-verifizieren (Zeilen driften). Anker POST-TXN-FIX, verifiziert 2026-07-16: -->

<!-- _migrate() PG-Skip (app.py:140-142): `if db_url.startswith('postgresql'): print(...); return`
     → ALLES in _migrate() (inkl. v4-Batch 989-1031) ist PG-tot. -->

<!-- v4-Batch (app.py:989-1031) — PG-TOT, ORM-basiert (_db.query(_Profile).all()), ruft _mpd je Profil,
     Skip `>= LATEST_VER` (v4). KEIN opener-Sync. → A1: LÖSCHEN. -->

<!-- _migrate_profile_json (app.py:1392, Aufruf :2078) = ÜBERLEBENDER. LÄUFT auf Prod + SQLite.
     - Advisory-Lock: SQLite BEGIN EXCLUSIVE (:1421); PG pg_advisory_xact_lock(819) (:1429) — A3.
     - SELECT id,daten,consent_text (:1435).
     - Idempotenz-Skip (:1461): `if isinstance(_daten,dict) and (_daten.get('schema_version') or 1) >= 2: continue`
       → NUR v1/versions-los. A2: `>= 2` → `>= LATEST_SCHEMA_VERSION`.
     - opener-Sync roher INSERT :1475-1478 (commit :1479), pitch :1482-1485 (commit :1486),
       erlaubnis :1501-1504 (commit :1505) — COUNT-vor-INSERT idempotent (:1469/:1497). B2: created_at fehlt.
     - meta-Zugriff (:1512): `if not isinstance(_daten.get('meta'), dict):` — bereits isinstance-safe für meta;
       B1 verlangt zusätzlich Schutz falls _daten selbst kein dict ist (siehe Task 5).
     - _mpd(_daten) (:1517) — DER opener-Pop. UPDATE profiles (:1521-1525).
     - Guard a (:1532-1550): re-check, WARN-Text (:1546-1547) sagt „v4-Batch PG-tot … CONSOLIDATE" → nach A1/A2 stale. -->

<!-- Modul-Level-Call (app.py:2078): `_migrate_profile_json()`  — UNGEWRAPPT (Nachbarn _seed():2076
     ebenfalls). B1: in try/except wrappen. -->

<!-- _mpd (services/profile_schema.py:255): hebt v1→v4, rein JSON. opener-Pop :304 (nur version<3).
     Stale Kommentare :266 + :302 („Sync … in app.py _migrate()") → zeigen auf den GELÖSCHTEN Batch, A1 korrigieren.
     pitch/erlaubnis werden „dual-written zurück in daten (transitional)" (:303) → v2/v3 tragen sie evtl. noch. -->

<!-- database/models.py ProfileOpener (:192-204): KEIN UniqueConstraint(profile_id,type) [A3-Option-2-Blocker];
     created_at Column(DateTime, default=utcnow) [Python-default → raw INSERT NULL, B2];
     is_personalized nullable=False default=False kein server_default [raw INSERT muss liefern — schon TXN-09]. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (CONS-B4 + CONS-A2-TEST): Erst-Rot — v2/v3→v4-Lift-Guard + geschärfte Rollback-Isolation + pitch/erlaubnis-Sync (real-PG)</name>
  <read_first>
    - tests/test_profile_migrate_txn.py:1-120 (Bestand — Test A/B; ★ die 89-118-Test-B ist zu schärfen, `grep -n "def test_" tests/test_profile_migrate_txn.py`)
    - app.py:1392-1550 (_migrate_profile_json — Skip :1461, Sync :1468-1509, _mpd :1517, Guard a :1532)
    - services/profile_schema.py:255-446 (_mpd VOLLSTÄNDIG lesen — was passiert mit pitch/erlaubnis bei v1→v2→v3→v4; opener-Pop :304)
    - tests/conftest.py (client/cleanup_rows-Fixtures, PG nerve_test; ★ engine-Umbindung VOR `import app` — Import-Reihenfolge, TXN-FIX-Lehre)
    - `bash scripts/inspect.sh schema profile_opener` (Spalten-Realität nerve_test)
  </read_first>
  <behavior>
    VALID Runtime-Verhalten (real-PG nerve_test, KEIN Source-Presence):
    - **Test C (Erst-Rot A2-Lift):** Insert ein **v3**-Profil (daten mit `schema_version=3`, plus ggf.
      pitch/erlaubnis-Text). `_migrate_profile_json()` aufrufen. ERWARTET nach A2: Profil auf
      `LATEST_SCHEMA_VERSION` gehoben (`json.loads(daten)['schema_version'] == LATEST`). GEGEN HEAD:
      der Skip `>= 2` (:1461) überspringt v3 → bleibt v3 → Test C ROT.
    - **Test B geschärft (Rollback-Isolation, ECHTER Fehler):** zwei gesunde v1-Profile + EIN Profil
      mit einem deterministisch fehlerauslösenden Sync (z.B. ein Feld, das den INSERT/UPDATE real
      zum Scheitern bringt — konkreten Mechanismus aus dem echten Code ableiten, NICHT künstlich
      monkeypatchen, wenn ein echter Defekt billig konstruierbar ist). ERWARTET: die zwei gesunden
      Profile werden BEIDE auf v4 migriert (keine Kaskade), das defekte wird geloggt + übersprungen,
      der Aufruf wirft NICHT, ein Folge-SELECT auf der Fixture-Session funktioniert (kein
      InFailedSqlTransaction). Falls kein echter Fehler billig deterministisch: den Sync-Fehlerpfad
      via minimalem monkeypatch der Sync-INSERT-Naht auslösen — dokumentiert im Test, warum.
    - **Test D (pitch/erlaubnis-Sync):** ein v1-Profil mit `opener`+`pitch`+`erlaubnis` im daten-JSON →
      nach dem Lauf existieren in profile_opener drei Rows (type='opener'/'pitch'/'erlaubnis') für das
      Profil, mit nicht-NULL `inhalt`. (Deckt die A2-Naht ab: v2/v3 mit noch vorhandenem pitch/erlaubnis
      werden jetzt gesynct.)
    Cleanup via cleanup_rows (profiles + profile_opener), uuid-suffixed Namen. KEIN live/perf-Marker.
    ★ Import-Reihenfolge: `_migrate_profile_json` erst NACH der Fixture-Engine-Umbindung importieren.
  </behavior>
  <action>
    tests/test_profile_migrate_txn.py erweitern (Test C/D neu, Test B :89-118 schärfen). `from services.profile_schema
    import LATEST_SCHEMA_VERSION` für die Assertion (KEINE Magic-Number 4). `from app import _migrate_profile_json`
    innerhalb der Tests (nach Fixture). Assertions via frischem SELECT auf profiles.daten (json-parse
    schema_version) + profile_opener (COUNT/type).
    ★ Committe die neuen/geschärften Tests ROT zuerst (Test C rot gegen HEAD wegen Skip>=2):
    `test(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): ERST-ROT v2/v3->v4 lift guard + sharpened rollback isolation + pitch/erlaubnis sync`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: Test C rot gegen HEAD (dokumentiert), grün nach A2 (Task 3)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_" tests/test_profile_migrate_txn.py` zeigt >= 4 Tests (A/B/C/D)
    - `grep -n "LATEST_SCHEMA_VERSION" tests/test_profile_migrate_txn.py` vorhanden (keine Magic-Number)
    - `grep -niE "schema_version.?.?.?3|'schema_version': 3|schema_version=3" tests/test_profile_migrate_txn.py` (v3-Fixture für Lift-Test)
    - `grep -niE "pitch|erlaubnis" tests/test_profile_migrate_txn.py` vorhanden (Sync-Pfad-Test)
    - `grep -n "cleanup_rows" tests/test_profile_migrate_txn.py` vorhanden; KEIN getsource/open(/hasattr/grep auf Quellen; KEIN live/perf-Marker
  </acceptance_criteria>
  <done>v3-Profil wird zu v4 (Test C rot gegen HEAD); Rollback-Isolation mit echtem Fehler bewiesen; pitch/erlaubnis-Sync getestet.</done>
</task>

<task type="auto">
  <name>Task 2 (CONS-A1): v4-Batch löschen — _migrate_profile_json ist der EINE Pfad + stale Kommentare korrigieren</name>
  <read_first>
    - app.py:986-1031 (v4-Batch-Block inkl. `# ── Phase 08.20: Batch-Migration …` Kopf :989 + try/except-Rahmen — `grep -n "Batch-Migration alle Profile" app.py`)
    - app.py:1517 (_mpd-Call) + app.py:1468-1509 (Sync VOR _mpd) — die Datenverlust-Invariante, die erhalten bleiben MUSS
    - services/profile_schema.py:266 + :302 (stale Kommentare „Sync … in app.py _migrate()")
  </read_first>
  <action>
    (a) **v4-Batch löschen:** den kompletten Block app.py:989-1031 entfernen (vom Kommentar-Kopf
        `# ── Phase 08.20: Batch-Migration alle Profile -> LATEST_SCHEMA_VERSION (v4) ──` bis zum
        Ende des zugehörigen `except Exception as _e: print(...)`). Er ist PG-tot (`_migrate()`
        early-return :140) UND nach A2 auch für SQLite redundant (der überlebende Pfad hebt dort
        v2/v3 ebenfalls). Kein `_mpd`-Import/Nutzung sonst in `_migrate()` verwaisen lassen — prüfen,
        ob nach dem Löschen noch `_mpd`/`_Profile`/`_get_session`-Imports im v4-Batch-Scope hingen (waren
        lokal im try-Block → verschwinden mit).
        Kommentar an die Löschstelle: „CONS-A1: v4-Batch (PG-tot via _migrate() early-return :140)
        gelöscht — EIN lebender Pfad = _migrate_profile_json (:2078), der jetzt auch v2/v3→v4 hebt (CONS-A2)."
    (b) **Invariante NICHT brechen:** die Reihenfolge Sync (:1468-1509) VOR `_mpd` (:1517) im lebenden
        Pfad bleibt unangetastet. Einen expliziten Kommentar an der Sync-Stelle setzen:
        „CONS-A1-Invariante: opener/pitch/erlaubnis-Sync (persistiert) MUSS vor dem opener-Pop in
        _mpd (profile_schema.py:304) laufen — nicht umordnen (stiller Datenverlust)."
    (c) **stale Kommentare korrigieren:** services/profile_schema.py:266 („opener/pitch-Sync in app.py
        _migrate()") + :302 („Sync in ProfileOpener-Tabelle erfolgt in app.py _migrate()") zeigen auf
        den GELÖSCHTEN Batch → auf den lebenden Pfad umschreiben: „… Sync erfolgt in app.py
        _migrate_profile_json (VOR diesem Pop, CONS-A1)."
    Committe: `refactor(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): delete PG-dead v4-batch, consolidate on _migrate_profile_json + fix stale sync comments (CONS-A1)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün (Test A/B/D grün; C erst nach A2)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Batch-Migration alle Profile" app.py` liefert 0 Treffer (v4-Batch weg)
    - `grep -c "def _migrate_profile_json" app.py` == 1 (der Überlebende bleibt)
    - `grep -n "CONS-A1" app.py` vorhanden (Löschmarker + Invarianten-Kommentar)
    - `grep -n "in app.py _migrate()" services/profile_schema.py` liefert 0 Treffer (stale Kommentare korrigiert)
    - Sync (:1468-1509) steht weiterhin VOR dem `_mpd`-Call (:1517) — Reihenfolge unverändert
  </acceptance_criteria>
  <done>Genau EIN Migrationspfad; v4-Batch weg; Datenverlust-Invariante dokumentiert erhalten; stale Kommentare korrigiert.</done>
</task>

<task type="auto">
  <name>Task 3 (CONS-A2): v2/v3→v4-Anhebung — Skip-Schwelle heben + Guard-a-WARN aktualisieren</name>
  <read_first>
    - app.py:1459-1462 (Idempotenz-Skip — `grep -n "schema_version') or 1) >= 2" app.py`)
    - app.py:1468-1509 (Sync — idempotent via COUNT, deckt durchfließende v2/v3 ab)
    - app.py:1532-1550 (Guard a; WARN-Text :1546-1547 „stuck … v4-Batch PG-tot … CONSOLIDATE")
    - services/profile_schema.py:255-446 (_mpd bringt v2/v3 auf v4 — bestätigen)
    - `bash scripts/inspect.sh sample profiles 30` (Pre-Execute: wie viele v2/v3 real auf Prod? Erwartung 0, aber verifizieren)
  </read_first>
  <action>
    (a) **Skip-Schwelle heben:** in `_migrate_profile_json` den Idempotenz-Skip (app.py:1461)
        `if isinstance(_daten, dict) and (_daten.get('schema_version') or 1) >= 2:` →
        `>= LATEST_SCHEMA_VERSION` (den Import `from services.profile_schema import LATEST_SCHEMA_VERSION`
        nutzt die Funktion bereits als `_LATEST`, :1409 — denselben Namen verwenden). Kommentar:
        „CONS-A2: v2/v3 werden jetzt auf v4 gehoben (_mpd, rein JSON) — nicht mehr bei >=2 überspringen."
        ★ Sicherstellen, dass durchfließende v2/v3 den Sync-Block nicht doppeln: der COUNT-vor-INSERT
        (:1469/:1497) ist idempotent → belassen. Die Sync-vor-Pop-Invariante (Task 2b) gilt weiter.
    (b) **Guard-a-WARN aktualisieren (app.py:1546-1547):** nach A1/A2 ist die Begründung „v4-Batch
        PG-tot" hinfällig und die Phase CONSOLIDATE ist erledigt. Text ändern: ein `< LATEST`-Profil
        NACH dem Lauf ist jetzt ein **echter Migrations-Ausfall** (nicht mehr „bekannt/stuck"). WARN→
        z.B. „[Schema] WARN Profil {id}: schema_version={cv} nach Konsolidierung nicht auf {LATEST}
        gehoben — Migrations-Ausfall prüfen." (v1/versions-los bleibt CRITICAL wie gehabt.) Den
        Verweis auf „PROFILE-MIGRATE-CONSOLIDATE" aus dem WARN entfernen (Phase erledigt).
    Committe: `feat(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): lift v2/v3->v4 via skip threshold >=LATEST + refresh guard-a WARN (CONS-A2)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: Test C jetzt grün (v3->v4)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n ">= 2:" app.py` — der alte v2-Skip in _migrate_profile_json ist weg (auf LATEST umgestellt)
    - `grep -n "or 1) >= LATEST_SCHEMA_VERSION\|or 1) >= _LATEST" app.py` vorhanden (neue Schwelle)
    - `grep -n "CONS-A2" app.py` vorhanden
    - `grep -n "v4-Batch PG-tot" app.py` liefert 0 Treffer (Guard-a-WARN aktualisiert, kein stale CONSOLIDATE-Verweis)
    - KEINE neue Alembic-Migration / kein Schema-DDL in dieser Task (rein JSON)
  </acceptance_criteria>
  <done>v2/v3 werden auf v4 gehoben (Test C grün); Guard-a-WARN spiegelt den konsolidierten Zustand; kein Schema-Change.</done>
</task>

<task type="decision" gate="blocking">
  <name>Task 4 (CONS-A3): 🔴 OFFENE ENTSCHEIDUNG — Advisory-Lock 819 reparieren (Drei-Wege, Cross-AI + André)</name>
  <status>⚠ NICHT vorfestgelegt. Cross-AI (Fable+Gemini) + André wählen EINE Option; DANN via
  /gsd-plan-phase --reviews in konkreten Code + B3-Docstring gelockt. Executor baut diese Task NICHT,
  bevor die Wahl gelockt ist.</status>
  <read_first>
    - app.py:1417-1432 (Advisory-Lock-Setup: SQLite BEGIN EXCLUSIVE :1421 / PG pg_advisory_xact_lock(819) :1429)
    - app.py:1479/1486/1505/1525 (die mid-Loop-`conn.commit()` — jeder gibt den xact-Lock frei)
    - app.py:1488-1490/1507-1509/1528-1530 (Per-Profil-conn.rollback() = TXN-FIX-Isolation, MUSS bei Option-Wahl bedacht werden)
    - database/models.py:192-204 (ProfileOpener — KEIN UniqueConstraint(profile_id,type) → Option-2-Blocker)
  </read_first>
  <problem>
    `pg_advisory_xact_lock(819)` (:1429) ist **xact-scoped**. Die Schleife committet mehrfach
    (:1479/1486/1505/1525) → der **erste commit gibt den Lock frei** → zwei Gunicorn-Worker sehen
    beide `COUNT=0` (:1469) und INSERTen beide → **Doppel-Row-Race**. Der daten-UPDATE ist
    last-write-wins (identisches Ergebnis, unkritisch); der Race betrifft die profile_opener-INSERTs.
  </problem>
  <options>
    ## Drei-Wege-Vergleich (für Cross-AI + André — KEINE Vorfestlegung)

    | Option | Multi-Worker | Deadlock | Schema-Change? | Spannung mit TXN-FIX-Isolation | Einfachheit |
    |---|---|---|---|---|---|
    | **1. Session-Level `pg_advisory_lock(819)` / `pg_advisory_unlock(819)` (try/finally)** | Ein Worker migriert, andere warten → v4-Check → idempotenter Skip | Niedrig (ein Key, keine Lock-Ordnung); Restrisiko = finally-unlock muss sicher feuern (auch im Fehlerpfad + SQLite-Zweig) | Nein | Keine — Per-Profil-rollback-Isolation bleibt unangetastet | Mittel |
    | **2. Compare-and-Swap-INSERT (`INSERT … ON CONFLICT DO NOTHING`)** | Beide Worker parallel; Doppel-INSERT von PG abgewiesen; daten-UPDATE last-write-wins | Keins | **Ja** — Unique-Index `(profile_id, type)` auf profile_opener nötig (existiert NICHT, models.py:192-204) + Dup-Row-Vorabprüfung auf Prod vor dem Index | Keine, aber bringt eine eigene Alembic-Migration + Expand/Contract-Frage rein | Hoch für den INSERT-Race, aber Schema-Aufwand |
    | **3. Xact-Lock halten: mid-Loop-Commits raus, EIN Commit am Ende** | Lock hält bis End-Commit → echte Serialisierung | Keins | Nein | **KOLLIDIERT** — hebt die Per-Profil-rollback-Isolation der TXN-FIX auf (ein defektes Profil rollt alle zurück), außer man führt `SAVEPOINT`/`begin_nested` pro Profil ein | Hoch mechanisch, aber Isolations-Rückschritt |

    ### Konkrete Umsetzungs-Skizzen (damit --reviews-Lock trivial ist)
    - **Option 1:** `SELECT pg_advisory_xact_lock(819)` (:1429) → `SELECT pg_advisory_lock(819)`;
      am Funktions-Ende (nach Guard a, im `with`-Block, in einem `finally` das AUCH den SQLite-Zweig
      + jeden frühen `return` abdeckt) `SELECT pg_advisory_unlock(819)`. Achtung: der Lock ist an die
      **Connection** gebunden — sicherstellen, dass unlock auf DERSELBEN `conn` läuft und die `conn`
      nicht vorher geschlossen/getauscht wird.
    - **Option 2:** neue Alembic-Migration `UNIQUE (profile_id, type)` auf profile_opener (nach
      Dup-Row-Check als postgres); die 3 Sync-INSERTs auf `INSERT … ON CONFLICT (profile_id, type)
      DO NOTHING` umstellen; COUNT-vor-INSERT kann dann entfallen. **★ Expand/Contract:** Migration
      VOR dem ON-CONFLICT-Code deployen (Reader-vor-Migration-Falle). Verletzt Single-Deploy von A2 →
      eigener Migrations-Deploy-Schritt (Punkt 24, Real-Daten-Validation).
    - **Option 3:** die 4 mid-Loop-`conn.commit()` entfernen, EIN `conn.commit()` nach der Schleife;
      um die TXN-FIX-Isolation zu retten, jeden Profil-Durchlauf in `conn.begin_nested()` (SAVEPOINT)
      kapseln, Fehler rollt nur den Savepoint. (Mehr Umbau; berührt genau die Isolation, die TXN-FIX
      gerade gebaut hat → Punkt 17/27 kritisch prüfen.)
  </options>
  <cross_ai_briefing>
    Fable + Gemini bitte am REALEN Code (read-only) prüfen: (1) Welche Option löst den Doppel-Row-Race
    OHNE die Per-Profil-rollback-Isolation der TXN-FIX zu opfern? (2) Bei Option 1: feuert das
    finally-unlock in ALLEN Pfaden (früher return :1411/:1426/:1432/:1438, SQLite-Zweig, Exception)?
    Ist der Advisory-Lock connection-gebunden korrekt gehandhabt? (3) Bei Option 2: Ist ein
    Unique-Index (profile_id,type) mit dem Bestand kompatibel (Dup-Rows?) — inspect.sh gegen Prod. (4)
    Latenz/Boot-Zeit-Auswirkung? Claudian-Tendenz (unverbindlich): Option 1 (kleinster tragfähiger Weg,
    Punkt 27, keine Isolations-Kollision). NICHT als Vorfestlegung werten.
  </cross_ai_briefing>
  <b3_coupling>
    ★ CONS-B3 (Docstring ehrlich) ist an diese Task gekoppelt. Beim A3-Lock via /gsd-plan-phase --reviews
    MUSS ein KONKRETER Docstring-Edit materialisiert werden (eigener Sub-Schritt/Task): der Docstring
    `_migrate_profile_json` (app.py:1393-1404) ist aktuell stale — er sagt „Idempotenz-Skip schema_version
    >= 2" (jetzt >=LATEST, CONS-A2), „v2/v3 … NICHT gehoben" (jetzt gehoben) und beschreibt den xact-Lock
    (:1400-1403), der je nach A3-Wahl anders lautet. Der Edit stellt Skip-Schwelle + Lift-Verhalten + die
    GEWÄHLTE Lock-Semantik ehrlich dar. Acceptance beim Lock: `grep -n ">= 2" app.py` liefert im Docstring
    0 stale Treffer; die Lock-Beschreibung nennt die gewählte A3-Option.
  </b3_coupling>
  <acceptance_criteria>
    - Diese Task bleibt bis zum Cross-AI/André-Lock UNGEBAUT (kein Code-Change an :1429 vor der Wahl)
    - Nach dem Lock (via --reviews): genau EINE Option ist im Plan konkretisiert; die anderen zwei als „verworfen: {Grund}" notiert
    - Der A3-Lock-Replan materialisiert ZUGLEICH den CONS-B3-Docstring-Edit (siehe b3_coupling) — nicht durchrutschen lassen (W-1 Plan-Checker)
  </acceptance_criteria>
  <done>Eine der drei Optionen ist von Cross-AI + André gewählt + im Plan gelockt (dann baubar); B3-Docstring-Edit mit-materialisiert.</done>
</task>

<task type="auto">
  <name>Task 5 (CONS-B1 + CONS-B2): Boot-Crash-Netz + created_at in die 3 rohen INSERTs</name>
  <read_first>
    - app.py:2076-2084 (Modul-Level-Startup-Calls — `_migrate_profile_json()` :2078 ungewrappt)
    - app.py:1510-1516 (meta-Zugriff :1512 `_daten.get('meta')` — _daten kann non-dict sein?)
    - app.py:1475-1478/1482-1485/1501-1504 (die 3 rohen Opener/Pitch/Erlaubnis-INSERTs ohne created_at)
    - database/models.py:201 (ProfileOpener.created_at = Column(DateTime, default=utcnow) — Python-default)
  </read_first>
  <action>
    (a) **CONS-B1 Boot-Crash-Netz:** den Modul-Level-Call `_migrate_profile_json()` (app.py:2078) in
        try/except wrappen (Muster der anderen Startup-Calls — nicht-fatal, print `[Schema]`-Fehler):
        ```python
        try:
            _migrate_profile_json()
        except Exception as _e:
            print(f"[Schema] _migrate_profile_json failed at startup (non-fatal): {_e}")
        ```
        PLUS isinstance-Guard vor dem `.get('meta')`-Pfad (app.py:1512): sicherstellen, dass ein
        non-dict `_daten` (defektes JSON, das trotz try als Nicht-Dict durchkommt) den App-Start NICHT
        crasht — falls `_daten` kein dict ist, den meta-Block überspringen (`if isinstance(_daten, dict)`
        um den meta/consent-Block, oder `_daten` früh auf `{}` normalisieren wenn kein dict). Am echten
        Code den kleinsten sicheren Guard wählen (die Sync-Zeilen :1464-1465 sind bereits isinstance-safe).
    (b) **CONS-B2 created_at:** in die 3 rohen INSERTs (opener :1476, pitch :1483, erlaubnis :1502)
        die Spalte `created_at` in Spaltenliste + `:created_at`-Param aufnehmen und mit UTC-Now füllen
        (raw SQL umgeht den Python-`default=utcnow`, models.py:201 → sonst NULL). Konsistent mit dem
        vorhandenen utcnow-Muster (denselben utcnow-Import wie models nutzen, z.B.
        `from datetime import datetime; ... datetime.utcnow()` oder das im Modul vorhandene utcnow).
    Committe: `fix(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): boot-crash guard around _migrate_profile_json + created_at in raw opener INSERTs (CONS-B1/B2)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün; Test D prüft profile_opener-Rows (jetzt created_at nicht-NULL)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_migrate_profile_json()" app.py` (Modul-Level) steht in einem try/except-Block (nächste Zeilen enthalten `except` + `[Schema]`)
    - `grep -c "created_at" app.py` — die 3 Sync-INSERTs enthalten created_at (>=3 im _migrate_profile_json-Kontext)
    - `grep -n "CONS-B1\|CONS-B2" app.py` vorhanden
    - meta-Block (:1512) ist gegen non-dict _daten geschützt (isinstance-Guard sichtbar)
    - Test D-Assertion prüft profile_opener.created_at IS NOT NULL (im Test ergänzen falls sinnvoll)
  </acceptance_criteria>
  <done>Startup-Migration crasht die App nicht mehr (non-fatal + non-dict-safe); rohe INSERTs setzen created_at.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: Deploy + Live-Verify (Zwei-Tore, supervised — Restart, journalctl sauber, EIN Pfad)</name>
  <what-built>
    v4-Batch gelöscht (A1), v2/v3→v4-Lift (A2), A3-Lock-Option gebaut (nach Cross-AI-Wahl),
    Boot-Crash-Netz + created_at (B1/B2), Docstring ehrlich (B3), geschärfter Test (B4). Alle committet
    + gepusht. Erwartung: sauberer Start, EIN Migrationsdurchlauf, kein Doppel-Row-Race, kein Poison,
    v2/v3 (falls je vorhanden) auf v4.
  </what-built>
  <how-to-verify>
    ★ Verify=Production (Claudian fährt). Reihenfolge (Zwei-Tore):
    1. Erst-Rot-Beleg: Test C war VOR A2 rot (dokumentiert), jetzt grün.
    2. `bash deploy.sh production` — Pytest-Gate (real-PG nerve_test) grün: alle Tests A/B/C/D +
       Bestand-Suite. Bei rot: kein Restart, Prod unberührt.
    3. Service-Restart → `journalctl`/inspect.sh logs: KEIN `[Schema]`-Fehler, KEIN
       `InFailedSqlTransaction`, KEIN Doppel-Row-Log, KEIN „Profile ?"; Guard-a nur informativ.
    4. `bash scripts/inspect.sh sample profiles 30` (bzw. sudo -u postgres): alle Profile auf v4
       (versions-lose + v2/v3 gehoben); profile_opener created_at nicht-NULL für neu gesyncte Rows.
    André/Claudian bestätigt: EIN sauberer Migrationsdurchlauf, kein Race-/Poison-Log.
  </how-to-verify>
  <resume-signal>Type "consolidate-live" wenn Start sauber (ein Pfad, kein InFailedSqlTransaction/Doppel-Row), alle Profile v4 — oder beschreibe Abweichungen.</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `profiles` (LESEN daten-JSON in Skip/Guard-a/Sync-Loop; SCHREIBEN daten via bestehendem UPDATE). **KEINE Schema-Änderung** (A1/A2 rein JSON).
- `profile_opener` (LESEN COUNT + SCHREIBEN INSERT im Sync; B2 ergänzt created_at). **KEINE Schema-Änderung in A1/A2/B** — ★ NUR falls A3-Option 2 gewählt wird, kommt EIN Unique-Index (profile_id,type) dazu (eigener Migrations-Deploy).
- `organisations` — NICHT angefasst (die _data_migrate-UPDATEs bleiben unverändert; nicht Teil dieser Phase).

### Beleg (Executor zitiert via inspect.sh / models.py — NICHT raten)
```
profiles: KEINE Spalte schema_version — Version lebt im daten-JSON (Text). Cross-Layer Punkt 21/22.
profile_opener: models.py:192-204 — KEIN UniqueConstraint(profile_id,type) [A3-Option-2 bräuchte ihn];
  created_at Column(default=utcnow) Python-default → raw INSERT NULL [B2]; is_personalized NOT NULL kein server_default [raw INSERT liefert, TXN-09].
_migrate() PG-early-return (app.py:140-142) → v4-Batch (989-1031) PG-tot → GELÖSCHT (A1).
_mpd (profile_schema.py:255-446): hebt v1→v4 rein per JSON, kein DB-Zugriff.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt / Leser-Voraus? (Punkt 26) | Verifiziert? |
|---|---|---|---|---|
| `schema_version` | Skip (:1461) + Guard a (:1541) lesen; daten-UPDATE (:1521) schreibt | **JSON-Key IM `profiles.daten` (Text), NICHT Spalte** | Start-Zeit; PG: NUR _migrate_profile_json (v4-Batch gelöscht). v1+v2+v3 → v4 | ✓ models.py + app.py:1461/1541 |
| `profile_opener.(type, created_at)` | INSERT im Sync (:1475-1504) | DB-Spalten | Start-Zeit; created_at raw → utcnow (B2); Multi-Worker-Race = A3 | ✓ models.py:200-201 |
| A3-Race (Doppel-INSERT) | COUNT-vor-INSERT (:1469) + Advisory-Lock (:1429) | profile_opener | erster commit gibt xact-Lock frei → Race; Fix = A3-Wahl | ✓ app.py:1429/1479 |

### Async-Naht (Punkt 26)
Auf PG läuft als Profil-Migration nach A1 nur noch `_migrate_profile_json` (EIN Pfad). Multi-Worker
(Gunicorn): der Race entsteht, weil der xact-Advisory-Lock beim ersten Commit fällt (A3). Kein fremder
Schreiber läuft dem Guard-a-Read voraus (läuft am Ende derselben Funktion). Guard a liest nur JSON.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Startup-Migrationsprozess (Multi-Worker) → profiles/profile_opener | Kein User-Input; Risiko = Doppel-Row-Race (Multi-Worker) + stiller Opener/Pitch-Verlust (Pop-vor-Sync) + Boot-Crash (defektes Profil). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-CONS-01 | Tampering | Doppel-Pfad (v4-Batch + _migrate_profile_json) → Doppel-Arbeit/Drift | mitigate | v4-Batch gelöscht, EIN Pfad (CONS-A1). |
| T-CONS-02 | Information Disclosure/Loss | opener-Pop vor Sync → stiller Opener/Pitch-Verlust | mitigate | Sync-vor-Pop-Invariante dokumentiert erhalten (CONS-A1b); Test D belegt Sync. |
| T-CONS-03 | Tampering | v2/v3-Profile bleiben ungehoben (Onboarding-Gate) | mitigate | Skip-Schwelle >=LATEST (CONS-A2); Test C belegt Lift. |
| T-CONS-04 | Tampering (Race) | Multi-Worker Doppel-Row (xact-Lock fällt beim 1. Commit) | mitigate (Option offen) | A3 — Cross-AI wählt Session-Lock / CAS-INSERT / Xact-halten. |
| T-CONS-05 | Denial of Service | defektes Profil crasht App-Start (ungewrappter Call / non-dict daten) | mitigate | Boot-Crash-Netz try/except + isinstance-Guard (CONS-B1). |
| T-CONS-06 | Repudiation | falscher created_at (NULL) auf Opener-Rows | mitigate | created_at in rohe INSERTs (CONS-B2). |
</threat_model>

<verification>
- Genau EIN PG-lebender Migrationspfad (_migrate_profile_json); v4-Batch gelöscht (CONS-A1).
- Sync-vor-Pop-Invariante erhalten (Reihenfolge unverändert; Test D belegt Opener/Pitch/Erlaubnis-Rows).
- v2/v3 → v4 gehoben (Skip >=LATEST; Test C rot gegen HEAD, grün nach A2); kein Schema-Change/Single-Deploy.
- A3: EINE der drei Optionen von Cross-AI + André gewählt + gelockt; die anderen zwei als „verworfen: Grund" notiert.
- Boot-Crash-Netz (try/except + non-dict-Guard) + created_at in rohe INSERTs (B1/B2).
- Docstring (:1400-1403) ehrlich zur gewählten Lock-Semantik (B3, an A3 gekoppelt).
- Geschärfter Test: echter Fehler beweist Rollback-Isolation (keine Kaskade) + pitch/erlaubnis-Sync (B4).
- Live: Start sauber (ein Pfad, kein InFailedSqlTransaction/Doppel-Row), alle Profile v4.
</verification>

<success_criteria>
- Zwei Pfade → EINER; kein Doppel-Ritual, kein stiller Opener/Pitch-Verlust.
- v2/v3-Bestandsprofile real auf v4 (Onboarding-Launch-Gate), ohne Schema-Change.
- Advisory-Lock-Race behoben durch die von Cross-AI+André gewählte A3-Option (nicht vorfestgelegt).
- Robuster Boot (kein Crash bei defektem Profil); created_at korrekt.
- Cross-AI (Fable+Gemini) Plan-Review vor Execute erfolgt (löst A3); NICHT auto-advance.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-startup-profil-migration-konsolidierung/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01-SUMMARY.md`
</output>
