---
phase: 08.23.2.PROFILE-MIGRATE-CONSOLIDATE
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - services/profile_schema.py
  - tests/test_profile_migrate_txn.py
autonomous: false
requirements: [CONS-A1, CONS-A2, CONS-A2b, CONS-A2c, CONS-A3, CONS-B1, CONS-B2, CONS-B3, CONS-B4]
complexity: "🔴 komplex (Startup-Migration + Postgres-Transaktions-/Lock-Semantik + delikater Daten-Code). REPLANNED via --reviews 2026-07-16: Cross-AI (Fable+Gemini) → A3 GELOCKT auf Option 1 (Session-Lock) + 2 neue Bugs (A2b/A2c) eingearbeitet."

# ★★ BAUBEREIT (Cross-AI durch, A3 gelockt). NICHT auto-advance — Claudian-Pre-Execute-Audit, dann
#    Bau (Zwei-Tore, supervised, autonomous:false). Reviews: siehe *-REVIEWS.md.

must_haves:
  truths:
    - "Genau EIN auf Postgres lebender Profil-Migrationsdurchlauf existiert: _migrate_profile_json (app.py:2078). Der PG-tote v4-Batch (_migrate(), app.py:989-1031) ist GELÖSCHT."
    - "Der Advisory-Lock leakt nicht mehr: Session-Level pg_try_advisory_lock(819) (nicht-blockierend) + try/finally mit rollback-DANN-unlock (PG-only, gleiche conn) → kein Boot-Hang, kein geleakter Lock im Pool."
    - "v2/v3-Bestandsprofile werden auf v4 gehoben (Skip >= LATEST_SCHEMA_VERSION), rein JSON via _mpd, KEIN Schema-Change, Single-Deploy."
    - "Kein '[Schema] Profile ?' + kein AuditLog mit target_id=None mehr: _migrate_profile_json injiziert _migration_profile_id vor _mpd und poppt es danach (wie der gelöschte Batch)."
    - "Kein stiller Opener-Verlust: der opener-INSERT hängt nicht mehr an einem type-losen COUNT-Gate — pro-Typ geprüft, opener wird gesynct bevor _mpd ihn poppt."
    - "profiles hat KEINE schema_version-Spalte — jede Versions-Prüfung liest aus dem daten-JSON (Cross-Layer Punkt 21/22)."
  artifacts:
    - path: "app.py"
      provides: "v4-Batch gelöscht (A1); Session-try_advisory_lock + finally-unlock (A3); Skip >=LATEST (A2); _migration_profile_id-Injektion (A2b); pro-Typ-COUNT (A2c); Boot-Crash-Netz + created_at (B1/B2); ehrlicher Docstring (B3)"
      contains: "pg_try_advisory_lock"
    - path: "services/profile_schema.py"
      provides: "stale Kommentare (opener-Sync „in app.py _migrate()\") auf den lebenden Pfad korrigiert"
      contains: "_migrate_profile_json"
    - path: "tests/test_profile_migrate_txn.py"
      provides: "Erst-Rot: v2/v3→v4-Lift (C) + Rollback-Isolation echter Fehler (B) + pitch/erlaubnis-Sync (D) + kein 'Profile ?'/AuditLog-target_id (E) + COUNT-Gate-Opener-Erhalt (F)"
      contains: "LATEST_SCHEMA_VERSION"
  key_links:
    - from: "_migrate() PG early-return (app.py:140)"
      to: "v4-Batch tot → gelöscht, _migrate_profile_json überlebt"
      via: "A1 Konsolidierung auf den lebenden Pfad"
      pattern: "_migrate_profile_json"
    - from: "with engine.connect() (app.py:1420, kein close → Pool-Rollback gibt session-Lock NICHT frei)"
      to: "kein geleakter Advisory-Lock"
      via: "pg_try_advisory_lock + finally rollback-dann-unlock (A3/H-1)"
      pattern: "pg_advisory_unlock"
    - from: "_migration_profile_id nur vom Batch injiziert (app.py:1017)"
      to: "kein 'Profile ?' / target_id=None nach A2"
      via: "Injektion + Pop im lebenden Pfad (A2b)"
      pattern: "_migration_profile_id"
---

<objective>
Konsolidiert die zwei Startup-Profil-Migrationspfade auf **EINEN lebenden Pfad**, hebt v2/v3-Bestandsprofile
auf v4, repariert die Advisory-Lock-Semantik (Cross-AI: Session-Lock ohne Leak) und schließt zwei von der
Konsolidierung EXPONIERTE Bugs — plus 4 mechanische Hardening-Funde. Baut auf PROFILE-MIGRATE-TXN-FIX auf.

**★ Entschieden (CONTEXT.md, code-verifiziert 2026-07-16):**
- **A1:** `_migrate_profile_json` (app.py:1392, Aufruf :2078) ist DER EINE Überlebende. Der v4-Batch
  (app.py:989-1031) ist **PG-tot** (`_migrate()` early-return :140-142) → wird **gelöscht**.
- **A2:** v2/v3→v4 via **Skip-Schwelle heben** (`app.py:1461` `>= 2` → `>= LATEST_SCHEMA_VERSION`), rein JSON,
  **kein Schema-Change, Single-Deploy**.

**★ A3 GELOCKT via Cross-AI (Fable+Gemini, REVIEWS.md) = OPTION 1 (Session-Level Advisory-Lock):**
Option 2 (Unique-Index) VERWORFEN — mehrere Bausteine je Typ sind Feature (routes/profiles.py:363-379/:457/:539),
Index bräche die UI. Option 3 (Xact-Lock halten) VERWORFEN — rollback gibt den Lock frei → die 4
TXN-FIX-rollback-Stellen wären Race-Minen + Isolations-Rückschritt. Option 1 mit 4 Pflicht-Härtungen (H-1 a-d,
Task 6) gegen den **Lock-Leak in den Connection-Pool** (der echte Killer: `with engine.connect()` schließt
nicht → Pool-Rollback gibt session-Locks NICHT frei → Boot-Hang).

**★ Zwei von A2 EXPONIERTE Bugs (Cross-AI, am Code verifiziert):**
- **A2b (Bug #2):** `_migration_profile_id` wird nur vom (gelöschten) Batch injiziert → `_mpd` druckt sonst
  `[Schema] Profile ?` + AuditLog `target_id=None` für jedes durchfließende v2-Profil. Injektion im lebenden Pfad.
- **A2c (Bug #3):** der type-lose opener-COUNT-Gate (:1469) lässt bei COUNT>0 den opener-INSERT aus → `_mpd`
  poppt den opener → stiller Verlust. Pro-Typ-COUNT.

**★ Datenverlust-Invariante:** Sync (rohe INSERTs + commit, :1468-1509) läuft VOR dem opener-Pop in `_mpd`
(profile_schema.py:304) — Reihenfolge darf beim Refactor NICHT brechen.

Purpose: EIN Pfad, kein Lock-Leak, kein Race, v2/v3 real gehoben, kein 'Profile ?', kein Opener-Verlust, robuster Boot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

★ Multi-Segment-Phase-ID-Gotcha ([[project_nerve_multisegment_phase_ids]]): phase_dir HART =
`.planning/phases/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-startup-profil-migration-konsolidierung`.
SUMMARY = `08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01-SUMMARY.md`. KEIN gsd-tools/gsd-sdk → plain `git`, Tracking hand-editiert.

★ HART: Kein Local-Dev. Verify=Production. Tests im deploy.sh-Pytest-Gate auf nerve_test (real-PG; Lock-Leak +
Poison-Klasse sind Postgres-only → SQLite wäre False-Green). Live-Verify = human checkpoint (autonomous:false, Zwei-Tore).

★ Cross-AI (Fable+Gemini) durch — A3 gelockt, 2 neue Bugs eingearbeitet (REVIEWS.md). NACH diesem Replan:
Claudian-Pre-Execute-Audit → /gsd-execute-phase (supervised) → Claudian-Deploy. NICHT auto-advance.

★ Alle Anker per INHALT re-verifizieren (grep frisch, Zeilen driften). Anker POST-TXN-FIX, verifiziert 2026-07-16.
</execution_context>

<context>
@.planning/phases/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-startup-profil-migration-konsolidierung/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-CONTEXT.md
@.planning/phases/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-startup-profil-migration-konsolidierung/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-REVIEWS.md
@.planning/phases/08.23.2.PROFILE-MIGRATE-TXN-FIX-startup-migration-txn-haertung/08.23.2.PROFILE-MIGRATE-TXN-FIX-01-SUMMARY.md
@salesnerve/CLAUDE.md

<interfaces>
<!-- _migrate() PG-Skip (app.py:140-142): `if db_url.startswith('postgresql'): print(...); return`
     → ALLES in _migrate() (inkl. v4-Batch 989-1031) ist PG-tot. -->
<!-- v4-Batch (app.py:989-1031) — PG-TOT, ORM-basiert, ruft _mpd je Profil, injiziert _migration_profile_id (:1017)
     + poppt (:1020), Skip >= LATEST_VER. KEIN opener-Sync. → A1: LÖSCHEN. -->
<!-- _migrate_profile_json (app.py:1392, Aufruf :2078) = ÜBERLEBENDER. LÄUFT auf Prod + SQLite.
     - with engine.connect() (:1420) — conn wird NICHT geschlossen (Pool-Rückgabe = rollback, kein close). H-1!
     - Lock: SQLite BEGIN EXCLUSIVE (:1421); PG pg_advisory_xact_lock(819) (:1429) → A3 auf try_advisory + finally-unlock.
     - SELECT id,daten,consent_text (:1435, early-return :1436-1438 = Lock-Leak-Punkt bei Session-Lock).
     - Idempotenz-Skip (:1461): `>= 2` → `>= LATEST_SCHEMA_VERSION` (A2). Import _LATEST bereits :1409.
     - opener-COUNT-Gate type-LOS (:1469 `WHERE profile_id=:pid`); opener-INSERT :1475, pitch-INSERT :1481 (BEIDE im
       `if _existing_opener == 0`); erlaubnis eigener typ-COUNT (:1497) + INSERT (:1501). A2c!
     - meta-Zugriff (:1512). _mpd(_daten) (:1517) = opener-Pop. UPDATE profiles (:1521-1524).
     - Guard a (:1532-1550): WARN-Text (:1546-1547) „v4-Batch PG-tot … CONSOLIDATE" → nach A1/A2 stale. -->
<!-- Modul-Level-Call (app.py:2078) UNGEWRAPPT; stale Kommentar :1552-1553 „Zeile ~1877" (real :2078). B1/#4. -->
<!-- _mpd (services/profile_schema.py:255): hebt v1→v4, rein JSON. opener-Pop :304 (nur version<3).
     _migration_profile_id gelesen :321 + :417 (nur Batch setzt es → 'Profile ?' + AuditLog target_id=None, A2b).
     AuditLog-Write eigene Session :395-409 (SQLite-Kollision mit BEGIN EXCLUSIVE = minor, non-fatal).
     Stale Kommentare :266 + :302 („Sync … in app.py _migrate()") → GELÖSCHTER Batch, A1 korrigieren. -->
<!-- database/models.py ProfileOpener (:192-204): KEIN UniqueConstraint(profile_id,type) [A3-Opt-2 VERWORFEN];
     created_at default=utcnow Python-default [raw INSERT NULL, B2]; is_personalized NOT NULL kein server_default. -->
<!-- routes/profiles.py:363-379/:457/:539: mehrere Opener/Pitch/Erlaubnis je Typ = Feature (begründet Opt-2-Verwerfung). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (CONS-B4 + Erst-Rot für A2/A2b/A2c): 5 real-PG-Tests (v3→v4-Lift, Rollback-Isolation echter Fehler, pitch/erlaubnis-Sync, kein 'Profile ?', COUNT-Gate-Opener-Erhalt)</name>
  <read_first>
    - tests/test_profile_migrate_txn.py:1-120 (Bestand Test A/B; B :89-118 schärfen — `grep -n "def test_"`)
    - app.py:1392-1550 (Skip :1461, COUNT-Gate :1469, INSERTs :1475/1481/1501, _mpd :1517, Guard a :1532)
    - services/profile_schema.py:255-446 (_mpd; _migration_profile_id-Read :321/:417; AuditLog :392-411; opener-Pop :304)
    - database/models.py:192-204 (ProfileOpener) + AuditLog-Modell (action/target_type/target_id/details)
    - tests/conftest.py (client/cleanup_rows, PG nerve_test; ★ engine-Umbindung VOR `import app`; audit_log-Cleanup via Trigger-Bypass = TEST-AUFRAEUM)
  </read_first>
  <behavior>
    VALID Runtime (real-PG nerve_test, KEIN Source-Presence):
    - **Test C (Erst-Rot A2-Lift):** ein v3-Profil (`schema_version=3`). Nach `_migrate_profile_json()`:
      `schema_version == LATEST_SCHEMA_VERSION`. GEGEN HEAD (Skip >=2) bleibt v3 → ROT.
    - **Test B geschärft (Rollback-Isolation, ECHTER Fehler):** zwei gesunde v1-Profile + EIN Profil, dessen
      Sync/UPDATE deterministisch scheitert (echten Defekt aus dem Code ableiten; nur wenn nicht billig
      konstruierbar minimalen monkeypatch der Sync-INSERT-Naht, dokumentiert). ERWARTET: die zwei gesunden →
      BEIDE v4 (keine Kaskade), das defekte geloggt+übersprungen, Aufruf wirft nicht, Folge-SELECT ok (kein
      InFailedSqlTransaction).
    - **Test D (pitch/erlaubnis-Sync):** v1-Profil mit opener+pitch+erlaubnis im JSON, KEINE Bestands-
      profile_opener-Rows → nach dem Lauf drei Rows (type opener/pitch/erlaubnis) mit nicht-NULL inhalt.
    - **Test E (Erst-Rot A2b, kein 'Profile ?'):** ein v2-Profil migrieren. ERWARTET: audit_log-Row
      action='profile_schema_v2_to_v3' hat `target_id == <profile.id>` (NICHT None). Optional caplog: kein
      "Profile ?" im Log. GEGEN HEAD (nach A2, vor A2b): target_id=None → ROT.
    - **Test F (Erst-Rot A2c, Opener-Erhalt):** v1-Profil mit `opener`-Text im JSON UND EINER bestehenden
      profile_opener-Row eines ANDEREN Typs (z.B. type='erlaubnis') → COUNT>0. Nach dem Lauf existiert
      trotzdem eine type='opener'-Row (opener nicht verloren). GEGEN HEAD (type-loser Gate) fehlt sie → ROT.
    Cleanup via cleanup_rows (profiles + profile_opener + audit_log). uuid-suffixed Namen. KEIN live/perf-Marker.
    ★ Import-Reihenfolge: `_migrate_profile_json` NACH der Fixture-Engine-Umbindung importieren.
  </behavior>
  <action>
    tests/test_profile_migrate_txn.py erweitern (C/D/E/F neu, B schärfen). `from services.profile_schema import
    LATEST_SCHEMA_VERSION` (keine Magic-Number). Assertions via frischem SELECT (profiles.daten json-parse;
    profile_opener type/inhalt; audit_log target_id).
    ★ Committe ROT zuerst: `test(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): ERST-ROT v3-lift(C) + rollback-isolation(B) + pitch/erlaubnis(D) + no 'Profile ?'(E) + opener count-gate(F)`.
    Doku im Commit welche Tests gegen HEAD rot sind (C: Skip>=2; E: target_id=None; F: type-loser Gate).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: C/E/F rot gegen HEAD (dokumentiert), grün nach Task 3/4/5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_" tests/test_profile_migrate_txn.py` zeigt >= 6 Tests (A/B/C/D/E/F)
    - `grep -n "LATEST_SCHEMA_VERSION" tests/test_profile_migrate_txn.py` vorhanden
    - `grep -niE "target_id|audit" tests/test_profile_migrate_txn.py` vorhanden (Test E)
    - `grep -niE "pitch|erlaubnis|opener" tests/test_profile_migrate_txn.py` vorhanden (Test D/F)
    - `grep -n "cleanup_rows" tests/...` vorhanden; KEIN getsource/open(/hasattr/grep auf Quellen; KEIN live/perf-Marker
  </acceptance_criteria>
  <done>6 Tests; C/E/F rot gegen HEAD; Rollback-Isolation mit echtem Fehler bewiesen; Opener-Erhalt + AuditLog-target_id getestet.</done>
</task>

<task type="auto">
  <name>Task 2 (CONS-A1): v4-Batch löschen — EIN Pfad + Sync-vor-Pop-Invariante erhalten + stale Kommentare (#4)</name>
  <read_first>
    - app.py:986-1031 (v4-Batch — `grep -n "Batch-Migration alle Profile" app.py`)
    - app.py:1468-1509 (Sync VOR _mpd :1517) — die Datenverlust-Invariante
    - app.py:1552-1553 (stale „Aufruf … Zeile ~1877") + app.py:837 (referenziert „~:140" — KORREKT, nur prüfen)
    - services/profile_schema.py:266 + :302 (stale „Sync … in app.py _migrate()")
  </read_first>
  <action>
    (a) **v4-Batch löschen:** den Block app.py:989-1031 (Kommentar-Kopf `# ── Phase 08.20: Batch-Migration …`
        bis zum abschließenden `except Exception as _e: print(...)`) entfernen. PG-tot (:140) + nach A2 auch für
        SQLite redundant. Verwaiste lokale Imports (_mpd/_Profile/_get_session lagen im try-Block) verschwinden mit.
        Löschmarker: „CONS-A1: v4-Batch (PG-tot via _migrate() early-return :140) gelöscht — EIN lebender Pfad =
        _migrate_profile_json (:2078), hebt jetzt auch v2/v3→v4 (CONS-A2)."
    (b) **Invariante NICHT brechen:** Reihenfolge Sync (:1468-1509) VOR `_mpd` (:1517) bleibt. Kommentar an der
        Sync-Stelle: „CONS-A1-Invariante: opener/pitch/erlaubnis-Sync (persistiert) MUSS vor dem opener-Pop in
        _mpd (profile_schema.py:304) laufen — nicht umordnen (stiller Datenverlust)."
    (c) **stale Kommentare (#4):** services/profile_schema.py:266 + :302 („Sync … in app.py _migrate()") → „…
        in app.py _migrate_profile_json (VOR diesem Pop, CONS-A1)". app.py:1552-1553 „Zeile ~1877" → „:2078"
        (per Inhalt re-ankern). app.py:837 nur prüfen (referenziert korrekt _migrate() early-return :140 — NICHT ändern).
    Committe: `refactor(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): delete PG-dead v4-batch, one live path + fix stale comments (CONS-A1)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün (A/B/D grün; C/E/F erst nach 3/4/5)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Batch-Migration alle Profile" app.py` → 0 Treffer
    - `grep -c "def _migrate_profile_json" app.py` == 1
    - `grep -n "CONS-A1" app.py` vorhanden (Löschmarker + Invarianten-Kommentar)
    - `grep -n "in app.py _migrate()" services/profile_schema.py` → 0 Treffer
    - `grep -n "Zeile ~1877\|~:1877" app.py` → 0 Treffer (auf :2078 korrigiert)
    - Sync (:1468-1509) steht weiterhin VOR `_mpd`-Call (:1517)
  </acceptance_criteria>
  <done>EIN Migrationspfad; v4-Batch weg; Invariante dokumentiert erhalten; stale Kommentare korrekt.</done>
</task>

<task type="auto">
  <name>Task 3 (CONS-A2): v2/v3→v4 via Skip-Schwelle heben + Guard-a-WARN aktualisieren</name>
  <read_first>
    - app.py:1459-1462 (Idempotenz-Skip — `grep -n "or 1) >= 2" app.py`)
    - app.py:1532-1550 (Guard a; WARN :1546-1547 „v4-Batch PG-tot … CONSOLIDATE")
    - services/profile_schema.py:255-446 (_mpd bringt v2/v3 auf v4)
    - `bash scripts/inspect.sh sample profiles 30` (Pre-Execute: wie viele v2/v3 real? Erwartung 0, verifizieren)
  </read_first>
  <action>
    (a) **Skip-Schwelle heben:** app.py:1461 — real ist `if isinstance(_daten, dict) and (_daten.get('schema_version')
        or 1) >= 2:` (den `isinstance(_daten, dict) and`-Präfix ERHALTEN, Plan-Checker-W-3); nur das `>= 2` →
        `>= LATEST_SCHEMA_VERSION` (Import als `_LATEST` bereits :1409 vorhanden — denselben Namen nutzen, NICHT den
        gelöschten `_LATEST_VER` aus dem Batch). Kommentar: „CONS-A2: v2/v3 werden jetzt auf v4 gehoben (_mpd, rein JSON) — nicht mehr bei >=2 skippen."
    (b) **Guard-a-WARN aktualisieren (:1546-1547):** ein `< LATEST`-Profil NACH dem Lauf ist jetzt echter
        Migrations-Ausfall (nicht „stuck/bekannt"). WARN-Text: „[Schema] WARN Profil {id}: schema_version={cv}
        nach Konsolidierung nicht auf {LATEST} gehoben — Migrations-Ausfall prüfen." Verweis „PROFILE-MIGRATE-CONSOLIDATE"
        + „v4-Batch PG-tot" aus dem WARN entfernen (Phase erledigt).
    Committe: `feat(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): lift v2/v3->v4 via skip threshold >=LATEST + refresh guard-a WARN (CONS-A2)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: Test C grün (v3->v4)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "or 1) >= 2:" app.py` → 0 Treffer (auf LATEST umgestellt)
    - `grep -n "or 1) >= LATEST_SCHEMA_VERSION\|or 1) >= _LATEST" app.py` vorhanden
    - `grep -n "CONS-A2" app.py` vorhanden
    - `grep -n "v4-Batch PG-tot" app.py` → 0 Treffer (Guard-a-WARN aktualisiert)
    - KEINE neue Alembic-Migration / kein Schema-DDL (rein JSON)
  </acceptance_criteria>
  <done>v2/v3 → v4 (Test C grün); Guard-a-WARN spiegelt konsolidierten Zustand; kein Schema-Change.</done>
</task>

<task type="auto">
  <name>Task 4 (CONS-A2b, Bug #2): _migration_profile_id im lebenden Pfad injizieren + poppen — kein 'Profile ?' / target_id=None</name>
  <read_first>
    - app.py:1516-1524 (`_mpd(_daten)`-Call :1517 + UPDATE :1521 — Injektions-/Pop-Fenster)
    - app.py:1015-1020 (Batch-Vorbild: `_daten['_migration_profile_id'] = _p.id` :1017, `.pop('_migration_profile_id', None)` :1020)
    - services/profile_schema.py:321 + :417 (`daten.get('_migration_profile_id', '?')`), :388/:390 (print), :399-407 (AuditLog target_id)
  </read_first>
  <action>
    In `_migrate_profile_json`, im Schleifenkörper UNMITTELBAR VOR `_daten = _mpd(_daten)` (:1517) die Profil-ID
    injizieren, und NACH `_mpd` (vor dem `json.dumps`/UPDATE :1520-1521) wieder poppen — exakt analog zum
    gelöschten Batch (:1017/:1020):
    ```python
    _daten['_migration_profile_id'] = _pid          # CONS-A2b: sonst druckt _mpd 'Profile ?' + AuditLog target_id=None
    _daten = _mpd(_daten)
    _daten.pop('_migration_profile_id', None)        # CONS-A2b: Hilfsfeld NICHT in profiles.daten persistieren
    ```
    ★ isinstance-Guard beachten (falls _daten non-dict — B1-Naht): nur injizieren wenn `isinstance(_daten, dict)`.
    ★ `_pid` ist die Loop-Variable (`_row[0]`, :1453). NICHT über die Sync-Naht hinweg verändern.
    Committe: `fix(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): inject _migration_profile_id in live path so _mpd logs real id + audit target_id (CONS-A2b, Fable bug #2)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: Test E grün (audit_log.target_id == profile.id, kein 'Profile ?')</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_migration_profile_id" app.py` → in _migrate_profile_json vorhanden (Injektion + Pop, >=2 Treffer außerhalb des gelöschten Batches)
    - Injektion steht VOR `_mpd(_daten)` (:1517), Pop DANACH und VOR dem `json.dumps`/UPDATE
    - `grep -n "CONS-A2b" app.py` vorhanden
    - Test E belegt target_id == profile.id (nicht None)
  </acceptance_criteria>
  <done>Durchfließende v2/v3 loggen echte Profil-ID; AuditLog target_id korrekt; kein '_migration_profile_id' in profiles.daten.</done>
</task>

<task type="auto">
  <name>Task 5 (CONS-A2c, Bug #3): pro-Typ-COUNT-Gate für opener/pitch — kein stiller Opener-Verlust</name>
  <read_first>
    - app.py:1467-1490 (opener-COUNT-Gate :1469 `WHERE profile_id=:pid` type-LOS; opener-INSERT :1475, pitch-INSERT :1481 BEIDE im `if _existing_opener == 0`)
    - app.py:1492-1509 (erlaubnis: bereits eigener typ-COUNT :1497 `AND type='erlaubnis'` — Vorbild)
    - services/profile_schema.py:304 (opener-Pop — läuft NACH dem Sync; genau deshalb muss opener vorher gesynct sein)
  </read_first>
  <action>
    Den type-losen opener-COUNT-Gate (:1469) so umbauen, dass opener und pitch **je Typ** geprüft werden (Muster
    der erlaubnis-Naht :1497), statt beide unter EINEM `if _existing_opener == 0` zu bündeln:
    ```python
    # CONS-A2c: pro-Typ prüfen — ein bestehender Row EINES Typs darf den Sync eines ANDEREN Typs nicht überspringen.
    #           Sonst überspringt COUNT>0 (type-los) den opener-INSERT → _mpd poppt opener (profile_schema.py:304) → Verlust.
    if _opener_text:
        _has_opener = conn.execute(_text3("SELECT COUNT(*) FROM profile_opener WHERE profile_id=:pid AND type='opener'"), {'pid': _pid}).scalar()
        if _has_opener == 0:
            conn.execute(<opener-INSERT>); conn.commit()
    if _pitch_text:
        _has_pitch = conn.execute(_text3("SELECT COUNT(*) FROM profile_opener WHERE profile_id=:pid AND type='pitch'"), {'pid': _pid}).scalar()
        if _has_pitch == 0:
            conn.execute(<pitch-INSERT>); conn.commit()
    ```
    ★ Die vorhandene rollback-Hygiene (except → conn.rollback(), TXN-FIX) + created_at (B2, Task 7) bleiben je INSERT.
    ★ Erlaubnis-Naht (:1497) unverändert (war schon pro-Typ). Kommentar an der alten :1469-Stelle: die type-lose
    COUNT-Semantik war die Verlust-Ursache.
    Committe: `fix(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): per-type COUNT gate for opener/pitch sync — no silent opener loss (CONS-A2c, Fable/Gemini bug #3)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: Test F grün (opener-Row entsteht trotz Bestands-Row anderen Typs)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AND type='opener'\|AND type='pitch'\|AND type='erlaubnis'" app.py` — opener + pitch + erlaubnis je eigener typ-COUNT (>=3)
    - `grep -n "WHERE profile_id=:pid\")" app.py` — der type-LOSE opener-COUNT-Gate ist weg/ersetzt
    - `grep -n "CONS-A2c" app.py` vorhanden
    - Sync steht weiterhin VOR `_mpd` (:1517); rollback-je-except + created_at (B2) je INSERT erhalten
    - Test F belegt Opener-Erhalt bei Bestands-Row anderen Typs
  </acceptance_criteria>
  <done>opener/pitch/erlaubnis je pro-Typ gesynct; kein stiller Opener-Verlust an der COUNT-Gate/Pop-Naht.</done>
</task>

<task type="auto">
  <name>Task 6 (CONS-A3 = Option 1 + CONS-B3): Session-Level pg_try_advisory_lock(819) ohne Leak + ehrlicher Docstring</name>
  <read_first>
    - app.py:1417-1438 (Lock-Setup: SQLite BEGIN EXCLUSIVE :1421 / PG pg_advisory_xact_lock(819) :1429; SELECT :1435, early-return :1436-1438)
    - ★ app.py:1054 (`pg_advisory_xact_lock(81930)` in `_migrate_fragen_to_faqs()` — ANDERER Key/andere Funktion, OUT OF SCOPE, NICHT anfassen; Plan-Checker-W-1)
    - app.py:1420 (`with engine.connect() as conn:` — conn wird NICHT geschlossen → Pool-Rollback gibt session-Lock NICHT frei = H-1)
    - app.py:1532-1550 (Funktions-Ende / Guard a — hier endet der try, davor das finally)
    - app.py:1393-1404 (Docstring — stale: „skip >= 2", xact-Lock, „v2/v3 NICHT gehoben")
    - REVIEWS.md (A3-Entscheidung + H-1 a-d)
  </read_first>
  <action>
    **A3 = Option 1 (Session-Level Advisory-Lock), gelockt via Cross-AI. Optionen 2/3 VERWORFEN (Gründe s. REVIEWS.md
    / objective).** ★ NUR den `pg_advisory_xact_lock(819)` in `_migrate_profile_json` (:1429) umstellen — der
    `pg_advisory_xact_lock(81930)` in `_migrate_fragen_to_faqs()` (:1054, anderer Key/andere Funktion) bleibt
    UNBERÜHRT (out of scope, Plan-Checker-W-1). Umbau mit ALLEN 4 Härtungen (H-1 a-d):
    (a) **Nicht-blockierend (H-1a):** den PG-Zweig (:1428-1432) von `pg_advisory_xact_lock(819)` auf
        `pg_try_advisory_lock(819)` umstellen:
        ```python
        else:
            try:
                _got_lock = conn.execute(_text3("SELECT pg_try_advisory_lock(819)")).scalar()
            except Exception as _e:
                print(f"[Schema] _migrate_profile_json: pg_try_advisory_lock failed: {_e}")
                return
            if not _got_lock:
                print("[Schema] _migrate_profile_json: advisory lock 819 busy — anderer Worker migriert, skip (idempotent)")
                return
        ```
    (b) **try/finally UNMITTELBAR nach Acquire, SELECT :1435 EINGESCHLOSSEN (H-1b):** den bisherigen
        `try: _rows = SELECT … except: print+return` (:1432-1438) in den durch finally geschützten Block ziehen —
        der gesamte Rest (SELECT + Loop + Guard a) läuft in EINEM `try:` mit `finally:` am Funktions-/with-Ende.
        Ein SELECT-Fehler darf NICHT mehr per return am finally vorbei (sonst Lock-Leak).
    (c+d) **finally: rollback DANN unlock, PG-only, gleiche conn (H-1c/d):**
        ```python
        finally:
            if not _is_sqlite:
                try:
                    conn.rollback()          # H-1c: sonst scheitert unlock an InFailedSqlTransaction
                except Exception:
                    pass
                try:
                    conn.execute(_text3("SELECT pg_advisory_unlock(819)"))   # H-1d: PG-only, DERSELBE conn
                except Exception as _e:
                    print(f"[Schema] _migrate_profile_json: pg_advisory_unlock failed: {_e}")
        ```
        ★ Begründungs-Kommentar an :1420: „H-1: `with engine.connect()` schließt die conn NICHT (Pool-Rückgabe =
        rollback); rollback gibt session-level Advisory-Locks NICHT frei → explizites unlock im finally Pflicht."
        ★ Die per-Profil-`conn.commit()`/`conn.rollback()` in der Schleife bleiben — bei SESSION-Level-Lock geben sie
        den Lock NICHT frei (genau der Grund für Option 1 statt xact-Lock). SQLite-Zweig (BEGIN EXCLUSIVE) unverändert.
    **CONS-B3 Docstring (:1393-1404):** an die neue Realität anpassen: Skip `>= LATEST_SCHEMA_VERSION` (nicht >=2),
    „v2/v3 werden auf v4 gehoben" (nicht „NICHT gehoben"), Lock-Beschreibung = „PostgreSQL: Session-Level
    pg_try_advisory_lock(819), im finally rollback+unlock (kein Leak); nicht-blockierend → wer den Lock nicht
    kriegt, überspringt (anderer Worker migriert) + idempotenter Skip." SQLite-Zeile (BEGIN EXCLUSIVE) bleibt.
    Committe: `fix(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): session-level pg_try_advisory_lock(819) w/ finally rollback+unlock (no pool leak) + honest docstring (CONS-A3 Option 1, CONS-B3)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün; Multi-Worker-Boot testet die Concurrency-Naht real</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pg_try_advisory_lock(819)" app.py` vorhanden
    - ★ `grep -n "pg_advisory_xact_lock(819)" app.py` → 0 Treffer (der 819-Lock ist umgestellt). NICHT `grep pg_advisory_xact_lock` ohne (819) — `pg_advisory_xact_lock(81930)` (:1054, `_migrate_fragen_to_faqs`) bleibt bewusst unberührt (Plan-Checker-W-1)
    - `grep -n "pg_advisory_unlock(819)" app.py` vorhanden, im `finally`-Block, PG-only-guarded, VOR ihm ein `conn.rollback()`
    - Der SELECT (:1435) liegt INNERHALB des try, dessen finally das unlock hält (kein früher return am finally vorbei)
    - `grep -n "CONS-A3\|H-1" app.py` vorhanden
    - ★ Docstring ehrlich: `grep -n "schema_version >= 2" app.py` → 0 Treffer (stale Docstring/Kommentar-Formulierung weg; W-2); der Docstring-Block (:1393-1404) nennt „pg_try_advisory_lock" + „unlock" + „v2/v3 … auf v4 gehoben"
  </acceptance_criteria>
  <done>Session-Lock nicht-blockierend + leak-frei (finally rollback→unlock, PG-only); Docstring ehrlich zur Session-Lock-Semantik + Skip>=LATEST.</done>
</task>

<task type="auto">
  <name>Task 7 (CONS-B1 + CONS-B2): Boot-Crash-Netz + created_at in die 3 rohen INSERTs</name>
  <read_first>
    - app.py:2076-2084 (Modul-Level-Call `_migrate_profile_json()` :2078 ungewrappt)
    - app.py:1510-1516 (meta-Zugriff :1512 `_daten.get('meta')`)
    - app.py:1475-1478/1481-1485/1501-1504 (die 3 rohen INSERTs ohne created_at — nach A2c pro-Typ)
    - database/models.py:201 (ProfileOpener.created_at = Column(DateTime, default=utcnow) — Python-default)
  </read_first>
  <action>
    (a) **CONS-B1 Boot-Crash-Netz:** Modul-Level-Call :2078 in try/except (Muster der Nachbar-Startup-Calls, non-fatal):
        ```python
        try:
            _migrate_profile_json()
        except Exception as _e:
            print(f"[Schema] _migrate_profile_json failed at startup (non-fatal): {_e}")
        ```
        PLUS isinstance-Guard: ein non-dict `_daten` darf App-Start nicht crashen — den meta/consent-Block (:1512-1515)
        + die A2b-Injektion (Task 4) mit `if isinstance(_daten, dict)` absichern bzw. `_daten` früh auf `{}` normalisieren.
        Kleinsten sicheren Guard am echten Code wählen.
    (b) **CONS-B2 created_at:** in die 3 rohen INSERTs (opener :1475, pitch :1481, erlaubnis :1501) `created_at` in
        Spaltenliste + Param aufnehmen und mit UTC-Now füllen (raw SQL umgeht Python-`default=utcnow`, models.py:201 →
        sonst NULL). Denselben utcnow/`datetime.utcnow()`-Weg wie models nutzen.
    Committe: `fix(08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01): boot-crash guard around _migrate_profile_json + created_at in raw opener INSERTs (CONS-B1/B2)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün; Test D/F prüfen created_at nicht-NULL</automated>
  </verify>
  <acceptance_criteria>
    - Modul-Level `_migrate_profile_json()` (:2078) steht in try/except (folgende Zeilen: `except` + `[Schema]`)
    - `grep -c "created_at" app.py` — die 3 Sync-INSERTs enthalten created_at (>=3 im _migrate_profile_json-Kontext)
    - `grep -n "CONS-B1\|CONS-B2" app.py` vorhanden; meta-Block/A2b-Injektion gegen non-dict _daten geschützt
    - Test D/F-Assertion prüft profile_opener.created_at IS NOT NULL
  </acceptance_criteria>
  <done>Startup-Migration crasht App nicht (non-fatal + non-dict-safe); rohe INSERTs setzen created_at.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: Deploy + Live-Verify (Zwei-Tore, supervised — EIN Pfad, kein Lock-Leak, kein 'Profile ?', kein Opener-Verlust)</name>
  <what-built>
    v4-Batch gelöscht (A1), v2/v3→v4-Lift (A2), _migration_profile_id-Injektion (A2b), pro-Typ-COUNT (A2c),
    Session-try_advisory_lock ohne Leak (A3=Option 1), Boot-Crash-Netz + created_at (B1/B2), ehrlicher Docstring (B3),
    6 real-PG-Tests (B4). Alle committet + gepusht. Erwartung: ein sauberer Migrationsdurchlauf, kein Boot-Hang,
    kein 'Profile ?', kein InFailedSqlTransaction, kein Opener-Verlust, alle Profile v4.
  </what-built>
  <how-to-verify>
    ★ Verify=Production (Claudian fährt). Zwei-Tore:
    1. Erst-Rot-Beleg: C/E/F waren VOR den Fixes rot (dokumentiert), jetzt grün.
    2. `bash deploy.sh production` — Pytest-Gate (real-PG nerve_test) grün: alle A-F + Bestand-Suite. Bei rot: kein Restart.
    3. Service-Restart (Multi-Worker/Gunicorn!) → `journalctl`/inspect.sh logs: KEIN `[Schema] Profile ?`, KEIN
       `InFailedSqlTransaction`, KEIN Boot-Hang eines Workers (Lock-Leak), KEIN Doppel-Row-Log; Guard-a nur informativ.
    4. `bash scripts/inspect.sh sample profiles 30` (bzw. sudo -u postgres): alle Profile v4; profile_opener created_at
       nicht-NULL; für ein v2/v3 (falls je vorhanden) AuditLog target_id gesetzt.
    André/Claudian bestätigt: EIN sauberer Durchlauf, alle Worker booten, kein Race-/Poison-/'Profile ?'-Log.
  </how-to-verify>
  <resume-signal>Type "consolidate-live" wenn Start sauber (ein Pfad, alle Worker booten, kein Lock-Leak/InFailedSqlTransaction/'Profile ?'), alle Profile v4 — oder beschreibe Abweichungen.</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `profiles` (LESEN daten-JSON in Skip/Guard-a/Sync-Loop; SCHREIBEN daten via UPDATE). **KEINE Schema-Änderung** (A1/A2 rein JSON).
- `profile_opener` (LESEN pro-Typ-COUNT + SCHREIBEN INSERT im Sync; B2 created_at). **KEINE Schema-Änderung** — ★ Option 2 (Unique-Index) VERWORFEN, kein neuer Constraint.
- `audit_log` (SCHREIBEN via _mpd v2→v3-Event, profile_schema.py:399-407) — A2b setzt target_id korrekt; Test-Cleanup via Trigger-Bypass (TEST-AUFRAEUM).

### Beleg (Executor zitiert via inspect.sh / models.py — NICHT raten)
```
profiles: KEINE Spalte schema_version — Version im daten-JSON (Text). Punkt 21/22.
profile_opener: models.py:192-204 — KEIN UniqueConstraint(profile_id,type) [mehrere je Typ = Feature, routes/profiles.py:363-379];
  created_at default=utcnow Python-default → raw INSERT NULL [B2]; is_personalized NOT NULL kein server_default.
_migrate() PG-early-return (:140-142) → v4-Batch (989-1031) PG-tot → GELÖSCHT (A1).
_migration_profile_id: nur Batch injizierte (:1017) → nach A2 im lebenden Pfad injizieren (A2b).
with engine.connect() (:1420): conn NICHT geschlossen → Pool-Rollback gibt session-Lock NICHT frei → explizites unlock (A3/H-1).
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt / Leser-Voraus? (Punkt 26) | Verifiziert? |
|---|---|---|---|---|
| `schema_version` | Skip (:1461) + Guard a (:1541) lesen; daten-UPDATE (:1521) schreibt | **JSON-Key IM `profiles.daten` (Text), NICHT Spalte** | Start-Zeit; PG: NUR _migrate_profile_json (Batch gelöscht). v1+v2+v3 → v4 | ✓ models.py + app.py:1461/1541 |
| `profile_opener.(type, created_at)` | pro-Typ-COUNT + INSERT (:1475-1504) | DB-Spalten | Start-Zeit; created_at raw → utcnow (B2); Sync VOR _mpd-Pop | ✓ models.py:200-201 |
| `_migration_profile_id` | injiziert vor _mpd, gepoppt vor UPDATE (A2b) | **NUR RAM (Hilfsfeld), NIE in profiles.daten** | Start-Zeit; _mpd liest es (profile_schema.py:321/417) | ✓ app.py:1017-Vorbild |
| Advisory-Lock 819 | try_advisory_lock (acquire) / unlock (finally) | PG session-level (conn-gebunden) | Boot; session-Lock überlebt commit/rollback → explizites unlock nötig | ✓ app.py:1420/1429 |
| `audit_log.target_id` | _mpd v2→v3-Write (profile_schema.py:403) | DB-Spalte (Integer) | Start-Zeit; nach A2b == echte Profil-ID (nicht None) | ✓ profile_schema.py:399-407 |

### Async-Naht (Punkt 26)
Auf PG läuft als Profil-Migration nach A1 nur `_migrate_profile_json` (EIN Pfad). Multi-Worker (Gunicorn): der
Session-`pg_try_advisory_lock(819)` serialisiert nicht-blockierend — genau EIN Worker migriert, die anderen
überspringen (idempotenter Skip). Kein fremder Schreiber läuft dem Guard-a-Read voraus (Ende derselben Funktion).
Kein Leser der Migration liest voraus (Migration ist Boot-abgeschlossen bevor Requests kommen).

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Startup-Migration (Multi-Worker/Gunicorn) → profiles/profile_opener/audit_log | Kein User-Input; Risiken = Lock-Leak (Boot-Hang), Doppel-Row-Race, stiller Opener/Pitch-Verlust, 'Profile ?'-Fehl-Audit, Boot-Crash. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-CONS-01 | Tampering | Doppel-Pfad (v4-Batch + _migrate_profile_json) | mitigate | v4-Batch gelöscht, EIN Pfad (CONS-A1). |
| T-CONS-02 | Denial of Service | Session-Lock leakt in Pool → Worker-Boot-Hang | mitigate | pg_try_advisory_lock (non-blocking) + finally rollback→unlock (CONS-A3/H-1). |
| T-CONS-03 | Tampering | v2/v3 bleiben ungehoben (Onboarding-Gate) | mitigate | Skip >=LATEST (CONS-A2); Test C. |
| T-CONS-04 | Repudiation | Fehl-Audit `Profile ?` / target_id=None | mitigate | _migration_profile_id-Injektion (CONS-A2b); Test E. |
| T-CONS-05 | Information Loss | type-loser COUNT-Gate → opener gepoppt ohne Sync | mitigate | pro-Typ-COUNT (CONS-A2c); Test F; Sync-vor-Pop-Invariante (CONS-A1). |
| T-CONS-06 | Denial of Service | defektes Profil crasht App-Start | mitigate | Boot-Crash-Netz try/except + isinstance-Guard (CONS-B1). |
| T-CONS-07 | Repudiation | NULL created_at auf Opener-Rows | mitigate | created_at in rohe INSERTs (CONS-B2). |
</threat_model>

<verification>
- Genau EIN PG-lebender Migrationspfad; v4-Batch gelöscht (CONS-A1); Sync-vor-Pop-Invariante erhalten.
- Session-`pg_try_advisory_lock(819)` nicht-blockierend + finally rollback→unlock (PG-only, gleiche conn) → kein Leak/Boot-Hang (CONS-A3/H-1). Options 2/3 verworfen (REVIEWS.md).
- v2/v3 → v4 (Skip >=LATEST; Test C rot gegen HEAD, grün nach A2); kein Schema-Change/Single-Deploy.
- _migration_profile_id im lebenden Pfad injiziert+gepoppt → kein 'Profile ?'/target_id=None (CONS-A2b; Test E).
- pro-Typ-COUNT für opener/pitch → kein stiller Opener-Verlust (CONS-A2c; Test F).
- Boot-Crash-Netz + created_at (B1/B2); ehrlicher Docstring (B3).
- 6 real-PG-Tests (A-F); C/E/F rot gegen HEAD, grün nach Fix.
- Live: EIN sauberer Durchlauf, alle Worker booten, kein Leak/InFailedSqlTransaction/'Profile ?', alle Profile v4.
</verification>

<success_criteria>
- Zwei Pfade → EINER; kein Doppel-Ritual, kein Lock-Leak, kein stiller Opener/Pitch-Verlust.
- v2/v3-Bestandsprofile real auf v4 (Onboarding-Launch-Gate), ohne Schema-Change.
- Advisory-Lock-Race + Pool-Leak behoben (Option 1 + H-1 a-d, Cross-AI-gelockt).
- Kein Fehl-Audit ('Profile ?'/target_id=None); robuster Boot; created_at korrekt.
- Cross-AI (Fable+Gemini) Plan-Review erfolgt (A3 gelockt, 2 neue Bugs eingearbeitet); NICHT auto-advance.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-startup-profil-migration-konsolidierung/08.23.2.PROFILE-MIGRATE-CONSOLIDATE-01-SUMMARY.md`
</output>
