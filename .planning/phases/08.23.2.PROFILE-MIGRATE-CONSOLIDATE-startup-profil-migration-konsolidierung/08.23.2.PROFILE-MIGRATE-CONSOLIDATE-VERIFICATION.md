---
phase: 08.23.2.PROFILE-MIGRATE-CONSOLIDATE
status: passed
score: 6/6 must-have truths verified
verified: 2026-07-17
git_head: 7ac813c
---

# Phase 08.23.2.PROFILE-MIGRATE-CONSOLIDATE — Verification

**Status:** passed
**Score:** 6/6 Observable Truths verified
**Authoritative runtime evidence:** Claudian Zwei-Tore-Deploy 2026-07-17 (real-PG nerve_test + Live-Prod).

## Runtime-Beleg (Zwei-Tore, supervised)
| Tor | Command | Result | Status |
|---|---|---|---|
| 1 (Erst-Rot) | Deploy-Gate gegen Test-Commit `1372dff` (Tests ohne Fixes) | C/D/E/F **ROT**, kein Restart (Prod unberührt) | ✓ Erst-Rot bewiesen |
| 2 (Grün) | `bash deploy.sh production` @ HEAD `7ac813c` | **948 passed / 0 failed**, Baseline- + Schild-Guard grün → Restart sauber | ✓ |
| Live-Verify | Migrationslog + `inspect.sh` + `/api/health` | Log still (kein `Profile ?`/`InFailedSqlTransaction`/Boot-Hang), alle 3 Profile v4, `created_at` 0× NULL, `git_head=7ac813c` | ✓ |

## Observable Truths (from PLAN must_haves.truths)
| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Genau EIN auf PG lebender Migrationsdurchlauf (`_migrate_profile_json`, app.py:2097); v4-Batch (`_migrate()`, app.py:989-1031) GELÖSCHT | ✓ PASS | `grep "Batch-Migration alle Profile" app.py`=0; `def _migrate_profile_json`=1; Live-Log ein Durchlauf |
| 2 | Advisory-Lock leakt nicht: Session-`pg_try_advisory_lock(819)` (nicht-blockierend) + try/finally rollback-DANN-unlock (PG-only, gleiche conn) → kein Boot-Hang, kein Pool-Leak | ✓ PASS | `pg_advisory_xact_lock(819)`=0, `pg_advisory_unlock(819)`=1 im finally; Multi-Worker-Restart alle Worker sauber (kein Boot-Hang) |
| 3 | v2/v3 → v4 gehoben (Skip `>= LATEST_SCHEMA_VERSION`), rein JSON, KEIN Schema-Change, Single-Deploy | ✓ PASS | `or 1) >= _LATEST`=1; Test C grün nach A2; alle 3 Prod-Profile v4; keine Alembic-Migration |
| 4 | Kein `[Schema] Profile ?` + kein AuditLog `target_id=None`: `_migration_profile_id` vor `_mpd` injiziert + danach gepoppt | ✓ PASS | Injektion+Pop im lebenden Pfad; Test E grün; Live-Migrationslog ohne `Profile ?` |
| 5 | Kein stiller Opener-Verlust: opener-INSERT nicht mehr am type-losen COUNT-Gate — pro-Typ geprüft, Sync VOR `_mpd`-Pop | ✓ PASS | 3 pro-Typ-COUNTs (opener/pitch/erlaubnis); Test F grün; CONS-A1-Invariante erhalten |
| 6 | `profiles` hat KEINE `schema_version`-Spalte — jede Versions-Prüfung liest aus dem `daten`-JSON (Punkt 21/22) | ✓ PASS | Skip + Guard a parsen JSON, kein Spalten-SELECT; models.py bestätigt |

## Required Artifacts (from PLAN must_haves.artifacts)
| Artifact | Expected (`contains`) | Status |
|---|---|---|
| `app.py` | `pg_try_advisory_lock` | ✓ (Session-Lock + finally-unlock + Skip>=LATEST + A2b-Injektion + A2c pro-Typ + B1/B2 + Docstring) |
| `services/profile_schema.py` | `_migrate_profile_json` | ✓ (stale „in app.py _migrate()"-Kommentare korrigiert → lebender Pfad) |
| `tests/test_profile_migrate_txn.py` | `LATEST_SCHEMA_VERSION` | ✓ (6 real-PG-Tests A-F; C/D/E/F erst-rot) |

## Key Link Verification (from PLAN must_haves.key_links)
| From | To | Via | Status |
|---|---|---|---|
| `_migrate()` PG early-return (:140) | v4-Batch tot → gelöscht, `_migrate_profile_json` überlebt | A1-Konsolidierung | ✓ |
| `with engine.connect()` (:1420, kein close → Pool-Rollback gibt session-Lock NICHT frei) | kein geleakter Lock | `pg_try_advisory_lock` + finally rollback→unlock (A3/H-1) | ✓ (alle Worker booten) |
| `_migration_profile_id` nur vom Batch injiziert (:1017) | kein `Profile ?`/target_id=None nach A2 | Injektion+Pop im lebenden Pfad (A2b) | ✓ (Live-Log sauber) |

## Cross-AI-Funde (REVIEWS.md) — Disposition
- A3 = Option 1 (Session-Lock) gelockt; Opt 2 (Unique-Index) + Opt 3 (Xact-halten) verworfen → korrekt nicht gebaut.
- H-1 a-d (Pool-Lock-Leak) → umgesetzt (pg_try, try/finally inkl. SELECT, rollback-dann-unlock, PG-only). ✓
- Bug #2 (`_migration_profile_id`, A2b) → gefixt + Test E. ✓
- Bug #3 (type-loser COUNT-Gate, A2c) → gefixt + Test F. ✓
- Minor (v2→v3-Audit-Session-Kollision auf SQLite) → nur dokumentiert (non-fatal, Prod=PG). ✓

## Scope-Sauberkeit
- git diff `feb5900..7ac813c` = NUR app.py + services/profile_schema.py + tests/test_profile_migrate_txn.py (kein Schema/Migration, kein Fremd-Code).
- Task-6-Re-Indent per `git show -w` als inhaltlich verbatim bestätigt (nur Lock-Logik geändert).
- `pg_advisory_xact_lock(81930)` (`_migrate_fragen_to_faqs`, anderer Key/Funktion) UNBERÜHRT.

## Gaps
Keine. Alle 6 Must-Have-Truths runtime-verifiziert (948 passed, Live sauber, alle Profile v4, created_at 0× NULL).
