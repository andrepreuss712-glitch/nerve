# Phase 08.23.2.PROFILE-MIGRATE-CONSOLIDATE — Plan 01 SUMMARY

**Status:** ✅ COMPLETE + LIVE + VERIFIED (2026-07-17, git_head 7ac813c)
**Complexity:** 🔴 (Startup-Migration + Postgres-Lock-/Transaktions-Semantik + delikater Daten-Code)
**Mode:** supervised (autonomous:false), Cross-AI (Fable+Gemini) vor Execute, Claudian fuhr den Zwei-Tore-Deploy.

## Was gebaut wurde
Die zwei konkurrierenden Startup-Profil-Migrationspfade auf **EINEN lebenden Pfad** konsolidiert, v2/v3-Bestandsprofile
tatsächlich auf v4 gehoben, die kaputte Advisory-Lock-Semantik repariert und zwei von der Konsolidierung exponierte
Bugs geschlossen — plus 4 mechanische Hardening-Funde. 7 atomische Commits (`feb5900..7ac813c`), nur 3 Dateien
(`app.py`, `services/profile_schema.py`, `tests/test_profile_migrate_txn.py`), kein Schema/Migration.

| Task | Req | Ergebnis |
|---|---|---|
| 1 (`1372dff`) | CONS-B4 | 6 real-PG-Tests (A-F); **Erst-Rot C/D/E/F**; Test B geschärft (echter Fehler → rollback-Isolation) |
| 2 (`d0c641d`) | CONS-A1 | v4-Batch (app.py:989-1031, PG-tot) **gelöscht** → EIN Pfad `_migrate_profile_json`; Sync-vor-Pop-Invariante als Kommentar; stale Kommentare (profile_schema.py:266/302, „Zeile ~1877"→:2078) |
| 3 (`13f0f58`) | CONS-A2 | Skip `>=2` → `>= LATEST_SCHEMA_VERSION` → v2/v3→v4 (rein JSON, kein Schema-Change); Guard-a-WARN aktualisiert |
| 4 (`157190c`) | CONS-A2b | `_migration_profile_id` vor `_mpd` injiziert + danach gepoppt (Bug #2: sonst `Profile ?` + AuditLog target_id=None) |
| 5 (`357fd61`) | CONS-A2c | pro-Typ-COUNT für opener/pitch (Bug #3: type-loser Gate → stiller Opener-Verlust); INSERTs **verbatim** übernommen |
| 6 (`68214b6`) | CONS-A3 + B3 | Session-`pg_try_advisory_lock(819)` + `finally` rollback→unlock (H-1 a-d gegen Pool-Lock-Leak); ehrlicher Docstring |
| 7 (`7ac813c`) | CONS-B1 + B2 | Boot-Crash-Netz (try/except + isinstance-Guard) + `created_at` in die 3 rohen INSERTs |

## Delikate Entscheidungen (Cross-AI-gelockt)
- **A3 = Option 1 (Session-Level Lock).** Opt 2 (Unique-Index) verworfen — mehrere Bausteine je Typ sind Feature
  (routes/profiles.py:363-379). Opt 3 (Xact-Lock halten) verworfen — rollback gibt den Lock frei = Race-Minen +
  Isolations-Rückschritt. Der **eigentliche Killer** war der Pool-Lock-Leak (H-1): `with engine.connect()` schließt
  die conn nicht → Pool-Rückgabe (rollback) gibt session-level Advisory-Locks NICHT frei → explizites unlock im finally.
- **Zwei neue Bugs (Fable, am Code) exponiert durch A2:** A2b `_migration_profile_id` (nur der gelöschte Batch injizierte)
  + A2c type-loser COUNT-Gate. Beide gefixt + je ein Erst-Rot-Test (E/F).

## Runtime-Beleg (VERIFICATION.md)
- Tor 1: C/D/E/F rot gegen `1372dff` (kein Restart, Prod unberührt).
- Tor 2: HEAD `7ac813c`, **948 passed / 0 failed**, Baseline- + Schild-Guard grün → Restart sauber.
- Live: Migrationslog still (kein `Profile ?`/`InFailedSqlTransaction`/Boot-Hang), alle 3 Profile v4, `created_at` 0× NULL,
  `/api/health git_head=7ac813c`. Verifier **6/6 Truths**.

## Lehren / Notizen
- **Cross-AI fing 3 Funde, die alle 3 Claude-Sichten übersahen** (der Pool-Lock-Leak + 2 A2-exponierte Bugs) — der
  Pflicht-Cross-AI-Schritt (Punkt 24) war hier ausschlaggebend für Korrektheit.
- **Delikater Re-Indent sauber:** der Task-6-try/finally-Wrap wurde per `git show -w` als inhaltlich verbatim belegt
  (nur Lock-Logik geändert, Schleifenkörper unverändert). Muster für große Einrück-Umbauten: `git show -w` als Beweis.
- **Multi-Segment-Gotcha:** Pfade hart kodiert, gsd-tools umgangen, VERIFICATION/SUMMARY/ROADMAP/STATE hand-editiert.
- **Kein Auto-Advance** trotz auto_advance-Möglichkeit; André/Claudian fuhr den Deploy (STOP-vor-Deploy).
- Minor (nur dokumentiert): v2→v3-Audit-Write öffnet eine eigene Session → auf SQLite Kollision mit `BEGIN EXCLUSIVE`
  („database is locked", non-fatal, bereits in try/except). Prod=PG, nicht betroffen.

## Backlog-Bezug
Backlog-Eintrag `PROFILE-MIGRATE-CONSOLIDATE` war bereits als PROMOTED markiert. Der Deferred-Eintrag
`TXN-ROLLBACK-GUARD-AST` (AST-Wächter) bleibt offen — nicht Teil dieser Phase.
