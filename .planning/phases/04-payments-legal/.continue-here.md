---
phase: 04-payments-legal
task: 3
total_tasks: 7
status: in_progress
last_updated: 2026-04-02T17:36:29.807Z
---

<current_state>
Wave 1 of Phase 4 (Payments & Legal) is mid-execution. Plan 04-01 (Stripe backend) is fully complete. Plan 04-03 (German legal pages) hit a human-action checkpoint at Task 3 — all code is done, waiting for André to sign AVVs with vendors. Wave 2 (Plan 04-02, pricing page + fair-use metering) has not started yet.
</current_state>

<completed_work>

- **04-01 Task 1** — Stripe dependency + DB schema + config (commit 7727fe0)
  - `stripe>=11.0.0` in requirements.txt
  - `config.py`: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_IDS
  - `.env.example`: 5 Stripe env var placeholders
  - `database/models.py`: Organisation + BillingEvent extended with Stripe columns
  - `app.py`: _migrate() extended with new columns + UNIQUE index on billing_events.stripe_event_id

- **04-01 Task 2** — Payments blueprint: Checkout, Webhook, Portal, Success (commit 0c5e1e9)
  - `routes/payments.py` created: /checkout/<plan>, /checkout/success, /webhook, /portal, /pricing
  - Webhook idempotent via stripe_event_id UNIQUE index
  - Webhook handles: checkout.session.completed, subscription.updated/deleted, invoice.paid/payment_failed
  - `templates/settings.html`: "Abo verwalten" button added to billing tab
  - 04-01 SUMMARY.md created ✓

- **04-03 Task 1** — Legal page templates + routes + footer links (commit ff0515e)
  - `routes/legal.py` created, registered in app.py
  - `templates/impressum.html`, `templates/agb.html`, `templates/datenschutz.html` created
  - Footer links added to `templates/base.html` and `templates/landing.html`

- **04-03 Task 2** — Deepgram EU endpoint + nginx webhook config + LEGAL-01 verified (commit 934c329)
  - `DEEPGRAM_HOST=api.eu.deepgram.com` added to `.env.example`
  - nginx `/payments/webhook` location with `proxy_request_buffering off` added to `deploy.sh`
  - LEGAL-01 verified: DSGVO banner fires on socket.on('connect') — no code changes needed
</completed_work>

<remaining_work>

- **04-03 Task 3** — AVV signing checklist (CHECKPOINT: human-action)
  - All code is done — André must manually sign AVVs with 4 vendors (see blockers)
  - Also: fill 2 placeholders in templates/impressum.html
  - After André confirms → spawn 04-03 continuation agent to finalize plan, create SUMMARY.md

- **04-02 Task 1** — Pricing page (Wave 2, not started)
  - Create templates/pricing.html: 3 NERVE plans (Starter 49/Pro 59/Business 69 EUR/mo)
  - Gründerrabatt badge at 50% discount for Early Access
  - CTA button per plan → /payments/checkout/<plan>

- **04-02 Task 2** — Fair-use metering (Wave 2, not started)
  - Dashboard: soft warning at 80% of monthly limits
  - /live session start: check quota before allowing session
  - Uses org-level counters (live_minutes_used, training_sessions_used) already in DB
</remaining_work>

<decisions_made>

- Webhook uses raw request.data (not get_json) — required for Stripe signature verification
- Idempotency via stripe_event_id UNIQUE index — silent skip on duplicate events
- checkout_success only flashes and redirects — subscription activation in webhook only (authoritative source)
- stripe_customer_id stored on Organisation — reused on subsequent Checkout Sessions
- All services on EU servers (Deepgram api.eu.deepgram.com, Claude Bedrock Frankfurt, ElevenLabs EU Residency, Stripe Frankfurt) — DSGVO compliance, no SCCs needed
- Audio never stored — ephemeral processing only (key Datenschutz argument)
</decisions_made>

<blockers>

- **AVV signing** (human-action, non-blocking for Wave 2 code):
  - Stripe: Dashboard → Settings → Compliance → Data Processing Agreement
  - Deepgram: Dashboard → Settings → Privacy/Legal (or privacy@deepgram.com)
  - Anthropic: console.anthropic.com → Account Settings → Legal/Privacy (or privacy@anthropic.com)
  - ElevenLabs: Dashboard → Account → Legal/Privacy (or legal@elevenlabs.io)

- **Impressum placeholders** (fill when available):
  - `templates/impressum.html` has `[STRASSE HAUSNUMMER, PLZ STADT]` and `[DE123456789]`

- **Stripe Dashboard setup** (required before payment testing):
  - Create 3 Products: Starter (49 EUR/mo), Pro (59 EUR/mo), Business (69 EUR/mo)
  - Enable: Kreditkarte, SEPA Lastschrift, PayPal (+ optional Klarna)
  - Create webhook endpoint: `https://getnerve.app/payments/webhook`
  - Configure Customer Portal (allow cancel + plan switching)
  - Set VPS .env: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID_STARTER/PRO/BUSINESS

- **VPS .env** (required for production):
  - Set `DEEPGRAM_HOST=api.eu.deepgram.com`
  - Set all STRIPE_* vars
</blockers>

<context>
Phase 4 is the payment + legal phase — the last major blocker before launch. The backend is fully wired (Stripe checkout + webhook + portal, all legal routes). Wave 2 (pricing page + fair-use metering) is the final code work. After Wave 2, run verifier then mark Phase 4 complete. Phase 5 (Launch) is next.

The 04-03 checkpoint is non-blocking for Wave 2 code — André can reply "done" at any time (even if he hasn't signed all AVVs yet, they can be in-progress). The continuation agent just finalizes the plan tracking and creates SUMMARY.md.
</context>

<next_action>
André replies "done" (or confirms AVVs in progress) → orchestrator spawns 04-03 continuation agent → plan finalized → then execute Wave 2 (04-02) → run verifier → phase complete.

Command to resume: `/gsd:execute-phase 4`
</next_action>
