---
phase: 04-payments-legal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/impressum.html
  - templates/agb.html
  - templates/datenschutz.html
  - routes/payments.py
  - templates/base.html
  - templates/landing.html
  - .env.example
autonomous: false
requirements:
  - LEGAL-01
  - LEGAL-02
  - LEGAL-03

must_haves:
  truths:
    - "Impressum page is accessible at /impressum with TMG section 5 compliant content"
    - "AGB page is accessible at /agb with Drittdaten-Verarbeitungs-Klausel for voice processing"
    - "Datenschutzerklaerung is accessible at /datenschutz naming Deepgram, Anthropic, ElevenLabs, Stripe as Auftragsverarbeiter"
    - "Footer on base.html and landing.html links to all three legal pages"
    - "Deepgram EU endpoint (api.eu.deepgram.com) is configured via DEEPGRAM_HOST env var"
    - "DSGVO consent banner still appears before first microphone access in /live (LEGAL-01 verification)"
  artifacts:
    - path: "templates/impressum.html"
      provides: "TMG section 5 legal page"
      contains: "Impressum"
    - path: "templates/agb.html"
      provides: "Terms of service with voice processing clause"
      contains: "Allgemeine"
    - path: "templates/datenschutz.html"
      provides: "Privacy policy naming all 4 Auftragsverarbeiter"
      contains: "Deepgram"
    - path: "templates/base.html"
      provides: "Footer links to legal pages"
      contains: "/impressum"
  key_links:
    - from: "templates/base.html"
      to: "/impressum, /agb, /datenschutz"
      via: "footer anchor links"
      pattern: "href=./impressum"
    - from: "templates/landing.html"
      to: "/impressum, /agb, /datenschutz"
      via: "footer anchor links"
      pattern: "href=./impressum"
---

<objective>
Ship the three German legal pages (Impressum, AGB, Datenschutzerklaerung), add footer links, switch Deepgram to EU endpoint, verify LEGAL-01 (DSGVO banner), and provide AVV signing checklist.

Purpose: Legal compliance is a hard launch blocker — no paying customer without Impressum, AGB, Datenschutzerklaerung, and AVVs. Deepgram EU endpoint is required for DSGVO data residency.
Output: 3 legal templates, footer links in base.html + landing.html, DEEPGRAM_HOST env var, AVV checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-payments-legal/04-CONTEXT.md
@.planning/phases/04-payments-legal/04-RESEARCH.md

<interfaces>
<!-- Existing templates and routes to modify -->

From templates/base.html: extends pattern, block content, footer section (currently no legal links)

From templates/landing.html: standalone template (does not extend base.html), has its own footer

From routes/payments.py (created in Plan 01):
```python
payments_bp = Blueprint('payments', __name__, url_prefix='/payments')
```
Legal routes will be added here or in a separate legal section.

From .planning/phases/04-payments-legal/04-RESEARCH.md:
- Impressum TMG section 5 fields: Name, Anschrift, Kontakt, Verantwortlich, USt-IdNr
- AGB Drittdaten-Klausel: "Der Nutzer ist verpflichtet, seinen Gespraechspartnern die Nutzung von NERVE mitzuteilen..."
- Datenschutzerklaerung Auftragsverarbeiter block: Deepgram (535 Mission St), Anthropic (548 Market St), ElevenLabs (New York), Stripe Payments Europe (Dublin)
- DEEPGRAM_HOST=api.eu.deepgram.com in .env
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legal page templates + routes + footer links</name>
  <files>templates/impressum.html, templates/agb.html, templates/datenschutz.html, routes/payments.py, templates/base.html, templates/landing.html</files>
  <read_first>
    - templates/base.html (full file — find footer section to add legal links, understand block structure)
    - templates/landing.html (full file — find footer section to add legal links)
    - routes/payments.py (after Plan 01 — add legal routes here)
    - static/nerve.css (design tokens for legal page styling)
    - .planning/phases/04-payments-legal/04-RESEARCH.md (lines 587-618 for Auftragsverarbeiter block, lines 612-618 for Impressum fields, lines 450-457 for AGB Drittdaten-Klausel)
  </read_first>
  <action>
    **1. Add legal routes to `routes/payments.py`** — Add these routes (no @login_required, per D-17):

    ```python
    # ── Legal Pages (LEGAL-02, D-17) ─────────────────────────────────────────
    @payments_bp.route('/impressum')
    def impressum():
        return render_template('impressum.html')

    @payments_bp.route('/agb')
    def agb():
        return render_template('agb.html')

    @payments_bp.route('/datenschutz')
    def datenschutz():
        return render_template('datenschutz.html')
    ```

    IMPORTANT: These routes will be at `/payments/impressum` etc. For SEO-friendly URLs (D-17 says `/impressum`, `/agb`, `/datenschutz`), add top-level redirects in app.py:
    ```python
    @app.route('/impressum')
    def impressum_redirect():
        return redirect('/payments/impressum')

    @app.route('/agb')
    def agb_redirect():
        return redirect('/payments/agb')

    @app.route('/datenschutz')
    def datenschutz_redirect():
        return redirect('/payments/datenschutz')
    ```

    OR better: register the legal routes WITHOUT the `/payments` prefix. Create them directly on the payments_bp but override url_prefix, or use app_routes_bp. Simplest: add the routes directly to `app_routes_bp` in `routes/app_routes.py` since they are public pages like landing. Claude's discretion on exact placement — the KEY requirement is that `/impressum`, `/agb`, `/datenschutz` return 200.

    **2. Create `templates/impressum.html`** — Extends base.html. Content in German, TMG section 5 compliant (per research lines 612-618):

    ```html
    {% extends "base.html" %}
    {% block title %}Impressum — NERVE{% endblock %}
    {% block content %}
    <div style="max-width:720px;margin:40px auto;padding:0 20px;">
      <h1 style="color:var(--n-text-primary);margin-bottom:24px;">Impressum</h1>

      <h2>Angaben gemaess section 5 TMG</h2>
      <p>
        Andre Preuss<br>
        [Strasse und Hausnummer]<br>
        58636 Iserlohn<br>
        Deutschland
      </p>

      <h2>Kontakt</h2>
      <p>
        E-Mail: [kontakt@getnerve.app]<br>
        Telefon: Auf Anfrage per E-Mail
      </p>

      <h2>Umsatzsteuer-ID</h2>
      <p>
        Umsatzsteuer-Identifikationsnummer gemaess section 27a Umsatzsteuergesetz:<br>
        [USt-IdNr. beantragt / DE...]
      </p>

      <h2>Verantwortlich fuer den Inhalt nach section 55 Abs. 2 RStV</h2>
      <p>
        Andre Preuss<br>
        [Strasse und Hausnummer]<br>
        58636 Iserlohn
      </p>

      <h2>Haftungsausschluss</h2>
      <h3>Haftung fuer Inhalte</h3>
      <p>Die Inhalte unserer Seiten wurden mit groesster Sorgfalt erstellt. Fuer die Richtigkeit, Vollstaendigkeit und Aktualitaet der Inhalte koennen wir jedoch keine Gewaehr uebernehmen.</p>

      <h3>Haftung fuer Links</h3>
      <p>Unser Angebot enthaelt Links zu externen Webseiten Dritter, auf deren Inhalte wir keinen Einfluss haben. Fuer die Inhalte der verlinkten Seiten ist stets der jeweilige Anbieter oder Betreiber der Seiten verantwortlich.</p>
    </div>
    {% endblock %}
    ```

    NOTE: Use proper German umlauts (ae/oe/ue or actual umlauts) — the template file should use UTF-8 encoding with actual umlauts (ae, oe, ue, ss). The above uses ASCII for plan readability; the actual template MUST use real umlauts.

    Placeholders marked with `[...]` are for Andre to fill in — these are personal data that Claude cannot know.

    **3. Create `templates/agb.html`** — Extends base.html. Content must include the Drittdaten-Verarbeitungs-Klausel (per research line 456):

    Key sections:
    - section 1: Geltungsbereich (SaaS-Vertrag, B2B)
    - section 2: Leistungsbeschreibung (KI-gestuetzter Vertriebsassistent, Echtzeit-Analyse, Training)
    - section 3: Vertragsschluss und Laufzeit (monatlich kuendbar)
    - section 4: Preise und Zahlung (Flat-Rate, Abrechnung ueber Stripe)
    - section 5: Fair-Use (1000 Minuten, 50 Trainings, Soft-Warning, kein harter Block)
    - section 6: Datenschutz und Drittdaten-Verarbeitung — CRITICAL CLAUSE:
      "Der Nutzer ist verpflichtet, seine Gespraechspartner ueber die Nutzung von NERVE zu informieren und, sofern erforderlich, deren Einwilligung einzuholen. NERVE verarbeitet Audiodaten ausschliesslich in Echtzeit zur Analyse; eine dauerhafte Speicherung der Rohaudiodaten findet nicht statt, sofern der Nutzer den DSGVO-Modus nicht deaktiviert."
    - section 7: Haftungsbeschraenkung
    - section 8: Kuendigung
    - section 9: Schlussbestimmungen (Gerichtsstand: Iserlohn, deutsches Recht)

    **4. Create `templates/datenschutz.html`** — Extends base.html. MUST name all 4 Auftragsverarbeiter per LEGAL-02 and D-18.

    Key sections:
    - Verantwortlicher (Andre Preuss, Iserlohn)
    - Erhobene Daten (Stammdaten, Nutzungsdaten, Audiodaten — Echtzeit, nicht gespeichert)
    - Rechtsgrundlagen (Art. 6 Abs. 1 lit. b DSGVO — Vertragserfullung)
    - Auftragsverarbeiter — MUST include exactly these 4 (per research lines 587-610):
      1. **Deepgram, Inc.**, 535 Mission St, San Francisco, CA 94105, USA — Echtzeit-Transkription. Transfer: EU-Standardvertragsklauseln (SCC). Datenverarbeitung auf EU-Servern (api.eu.deepgram.com).
      2. **Anthropic, PBC**, 548 Market St PMB 90375, San Francisco, CA 94104, USA — KI-gestuetzte Gespraechsanalyse. Transfer: SCC. Hinweis: API-Daten nicht fuer Modelltraining verwendet.
      3. **ElevenLabs, Inc.**, New York, USA — Text-zu-Sprache-Synthese fuer Trainingsszenarien. Transfer: SCC.
      4. **Stripe Payments Europe, Ltd.**, 1 Grand Canal Street Lower, Dublin 2, Irland — Zahlungsabwicklung. EU-Verarbeitungsentitaet (keine SCC erforderlich).
    - Betroffenenrechte (Auskunft, Berichtigung, Loeschung, Einschraenkung, Datenportabilitaet, Widerspruch)
    - Aufsichtsbehoerde (LDI NRW, Landesbeauftragte fuer Datenschutz und Informationsfreiheit Nordrhein-Westfalen)

    **5. Add footer links to `templates/base.html`** — Find the footer or bottom of the body. Add:
    ```html
    <footer style="text-align:center;padding:20px;font-size:12px;color:var(--n-text-muted);border-top:1px solid var(--n-border);margin-top:40px;">
      <a href="/impressum" style="color:var(--n-text-muted);text-decoration:none;margin:0 12px;">Impressum</a>
      <a href="/agb" style="color:var(--n-text-muted);text-decoration:none;margin:0 12px;">AGB</a>
      <a href="/datenschutz" style="color:var(--n-text-muted);text-decoration:none;margin:0 12px;">Datenschutz</a>
    </footer>
    ```
    Per D-19.

    **6. Add footer links to `templates/landing.html`** — Find the existing footer section and add the same three links. Landing.html does not extend base.html so it needs its own footer links. Per D-19.
  </action>
  <verify>
    <automated>cd C:/Users/andre/OneDrive/Desktop/salesnerve && python -c "
from app import app
with app.test_client() as c:
    for path in ['/impressum', '/agb', '/datenschutz']:
        r = c.get(path, follow_redirects=True)
        assert r.status_code == 200, f'{path} returned {r.status_code}'
        print(f'{path} -> 200 OK')
    # Check datenschutz has all 4 Auftragsverarbeiter
    r = c.get('/datenschutz', follow_redirects=True)
    html = r.data.decode()
    for name in ['Deepgram', 'Anthropic', 'ElevenLabs', 'Stripe']:
        assert name in html, f'Missing Auftragsverarbeiter: {name}'
    print('All 4 Auftragsverarbeiter present in Datenschutz')
" && grep -q "impressum" templates/base.html && grep -q "impressum" templates/landing.html && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - templates/impressum.html exists, extends base.html, contains "Impressum" and "TMG" (or "section 5 TMG" in German)
    - templates/impressum.html contains "Andre" and "Iserlohn"
    - templates/agb.html exists, extends base.html, contains "Allgemeine" (or "AGB")
    - templates/agb.html contains the Drittdaten clause: "Gespraechspartner" (or "Gesprächspartner") and "informieren"
    - templates/datenschutz.html exists, extends base.html, contains all 4 names: "Deepgram", "Anthropic", "ElevenLabs", "Stripe"
    - templates/datenschutz.html contains "api.eu.deepgram.com"
    - templates/datenschutz.html contains "Art. 6" and "Art. 28"
    - GET /impressum returns 200 (directly or via redirect)
    - GET /agb returns 200 (directly or via redirect)
    - GET /datenschutz returns 200 (directly or via redirect)
    - templates/base.html contains `href="/impressum"` or `href="/payments/impressum"`
    - templates/base.html contains `href="/agb"` or `href="/payments/agb"`
    - templates/base.html contains `href="/datenschutz"` or `href="/payments/datenschutz"`
    - templates/landing.html contains links to all three legal pages
  </acceptance_criteria>
  <done>Three legal pages live with German-compliant content, all 4 Auftragsverarbeiter named in Datenschutz, AGB has Drittdaten clause, footer links on base.html and landing.html.</done>
</task>

<task type="auto">
  <name>Task 2: Deepgram EU endpoint + LEGAL-01 verify + nginx webhook config</name>
  <files>.env.example, deploy.sh</files>
  <read_first>
    - .env.example (current env template — add DEEPGRAM_HOST)
    - services/deepgram_service.py (current Deepgram client init — verify DEEPGRAM_HOST env var approach works)
    - templates/app.html (verify DSGVO banner is still present before microphone access — LEGAL-01)
    - static/app.js (verify DSGVO consent check fires on socket connect, before mic access)
    - deploy.sh (or nginx config reference — for webhook proxy_request_buffering off)
  </read_first>
  <action>
    **1. Add DEEPGRAM_HOST to `.env.example`** — Add this line in the Deepgram section:
    ```
    DEEPGRAM_HOST=api.eu.deepgram.com
    ```
    Per D-20 and LEGAL-03: the Deepgram SDK reads `DEEPGRAM_HOST` env var automatically — no code change needed in deepgram_service.py. This is the safest approach per research (line 466). The actual `.env` on the server needs this value set.

    **2. Verify LEGAL-01** — The DSGVO consent banner was implemented in Phase 02 (decision log: "DSGVO banner triggered on socket connect event"). Read `templates/app.html` and `static/app.js` to confirm:
    - A DSGVO consent banner/modal exists in app.html
    - The consent check fires BEFORE microphone access (on socket connect, not after transcript)
    - If the banner is present and functional, LEGAL-01 is satisfied — no code changes needed
    - If the banner is missing or fires after mic access, create a fix task (but based on Phase 02 decisions, it should be intact)

    Document the verification result in the task output.

    **3. Nginx webhook config** — Per research Pitfall 1 (line 392-406), the webhook endpoint needs `proxy_request_buffering off` in nginx to prevent signature verification failures. Check if `deploy.sh` or nginx config templates exist and add the webhook location block:
    ```nginx
    location /payments/webhook {
        proxy_request_buffering off;
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 1m;
    }
    ```
    If nginx config is managed in deploy.sh or a separate nginx.conf file, add this block. If it is managed manually on the server, document what needs to be added.
  </action>
  <verify>
    <automated>cd C:/Users/andre/OneDrive/Desktop/salesnerve && grep -q "DEEPGRAM_HOST" .env.example && grep -q "dsgvo\|DSGVO\|datenschutz" templates/app.html && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains `DEEPGRAM_HOST=api.eu.deepgram.com`
    - templates/app.html contains DSGVO banner/consent element (confirming LEGAL-01 is intact)
    - Nginx config (deploy.sh or nginx.conf) contains `proxy_request_buffering off` for `/payments/webhook` location
  </acceptance_criteria>
  <done>DEEPGRAM_HOST env var configured for EU endpoint, LEGAL-01 (DSGVO banner) verified intact from Phase 02, nginx webhook buffering configured for signature verification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: AVV signing checklist (manual — LEGAL-03)</name>
  <files>none</files>
  <action>
    This is a manual task for Andre. All code work is complete. The following AVVs must be signed with each vendor to satisfy LEGAL-03. Andre must access each vendor dashboard and sign their Data Processing Agreement (Auftragsverarbeitungsvertrag).

    Vendor checklist:
    1. Stripe — Dashboard -> Settings -> Compliance -> Data Processing Agreement
    2. Deepgram — Dashboard -> Settings -> Privacy/Legal -> DPA (or email privacy@deepgram.com)
    3. Anthropic — console.anthropic.com -> Account Settings -> Legal/Privacy -> DPA (or email privacy@anthropic.com)
    4. ElevenLabs — Dashboard -> Account -> Legal/Privacy -> DPA (or email legal@elevenlabs.io)

    Also confirm production .env has:
    5. DEEPGRAM_HOST=api.eu.deepgram.com
    6. STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID_STARTER/PRO/BUSINESS
  </action>
  <verify>
    <automated>echo "Manual verification required — AVV signing cannot be automated"</automated>
  </verify>
  <done>All 4 AVVs signed (Stripe, Deepgram, Anthropic, ElevenLabs). Production .env has DEEPGRAM_HOST and all Stripe env vars configured.</done>
</task>

</tasks>

<verification>
- GET /impressum returns 200
- GET /agb returns 200
- GET /datenschutz returns 200
- Datenschutz page contains "Deepgram", "Anthropic", "ElevenLabs", "Stripe"
- base.html footer has all 3 legal links
- landing.html footer has all 3 legal links
- .env.example contains DEEPGRAM_HOST
- DSGVO banner is present in app.html (LEGAL-01)
</verification>

<success_criteria>
- Three German legal pages are live and accessible without login
- Impressum is TMG section 5 compliant with Andre's details (placeholders for address)
- AGB has explicit Drittdaten-Verarbeitungs-Klausel for voice processing
- Datenschutzerklaerung names all 4 Auftragsverarbeiter with addresses, purposes, and transfer mechanisms
- Footer links present on base.html and landing.html
- DEEPGRAM_HOST=api.eu.deepgram.com in .env.example
- DSGVO consent banner verified intact (LEGAL-01)
- AVV signing checklist provided for manual completion (LEGAL-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-payments-legal/04-03-SUMMARY.md`
</output>
