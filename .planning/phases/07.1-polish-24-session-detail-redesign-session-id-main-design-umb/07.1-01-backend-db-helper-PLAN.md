---
phase: 07.1
plan: 01
name: backend-db-helper
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - database/models.py
  - routes/app_routes.py
  - routes/dashboard.py
autonomous: true
requirements:
  - POLISH-24
must_haves:
  truths:
    - "SQLite-Spalte conversation_logs.kb_verlauf existiert idempotent nach App-Start"
    - "ConversationLog.kb_verlauf ist als ORM-Column deklariert"
    - "/api/beenden persistiert kb_verlauf als JSON beim Create des ConversationLog-Eintrags"
    - "_derive_practice_recommendations liefert typ-aware Empfehlungen (live ODER training) mit max 3 Eintraegen"
    - "Cross-Context-Feld ist nur bei objection-Recommendations befuellt, sonst None"
    - "session_detail-Route liefert Template-Context: conv, events, pt, trend_avg, chart_data_json, schwierigkeit_label, recommendations, score_total"
    - "score_total wird per _calc_call_score(conv) fuer Live-Sessions berechnet und an Template uebergeben (W-06 Plan-01-side: Score-Hero und Trend-Delta operieren beide auf dem Gesamt-Score, NICHT nur auf kb_end)"
    - "Fuer Training-Sessions ist score_total = conv.kb_end (kein _calc_call_score-Call), und trend_avg wird fuer Training NICHT gerendert (Trend-Badge nur Live)"
    - "Training-Sessions bekommen kb_end als Score-Hero-Wert (NICHT sterne — ist immer NULL)"
  artifacts:
    - path: "app.py"
      provides: "kb_verlauf Migration idempotent, Spiegel 04.9-Block"
      contains: "ADD COLUMN kb_verlauf"
    - path: "database/models.py"
      provides: "ConversationLog.kb_verlauf Column(Text, nullable=True)"
      contains: "kb_verlauf"
    - path: "routes/app_routes.py"
      provides: "kb_verlauf=_json.dumps(...) in ConversationLog-Create + _derive_practice_recommendations Helper"
      contains: "_derive_practice_recommendations"
    - path: "routes/dashboard.py"
      provides: "session_detail-Route erweitert: trend_avg, chart_data_json, pt, schwierigkeit_label, recommendations, score_total"
      contains: "_derive_practice_recommendations"
  key_links:
    - from: "routes/app_routes.py /api/beenden"
      to: "conversation_logs.kb_verlauf"
      via: "_json.dumps(kb_verlauf, ensure_ascii=False) im ConversationLog(...) Constructor"
      pattern: "kb_verlauf=_json\\.dumps\\(kb_verlauf"
    - from: "routes/dashboard.py session_detail"
      to: "routes/app_routes.py _derive_practice_recommendations + _calc_call_score"
      via: "from routes.app_routes import _calc_call_score, _derive_practice_recommendations"
      pattern: "_derive_practice_recommendations\\(db, conv, events\\)"
    - from: "routes/dashboard.py session_detail score_total"
      to: "routes/app_routes.py _calc_call_score"
      via: "score_total = _calc_call_score(conv) if conv.typ == 'live' else (conv.kb_end or 0)"
      pattern: "score_total"
    - from: "routes/dashboard.py session_detail"
      to: "PersonalityType + TrainingScenario lookups"
      via: "db.query(PersonalityType).filter_by(id=conv.personality_type_id).first()"
      pattern: "PersonalityType"
---

<objective>
Backend-Grundlage fuer POLISH-24 Session-Detail-Redesign: DB-Spalte `kb_verlauf` migrieren, in `/api/beenden` persistieren, Helper `_derive_practice_recommendations(db, conv, events)` anlegen (typ+mode-aware mit Cross-Context-Query), und die bestehende `session_detail`-Route erweitern um alle Template-Kontext-Variablen fuer die 11 Sektionen (trend_avg, chart_data_json typ-diskriminierend, pt pre-resolved, schwierigkeit_label aus phasen_details, recommendations, score_total).

Purpose: Frontend-Plan 02 kann ausschliesslich gegen Template-Kontext arbeiten, ohne eigene DB-Queries. Separation-of-Concerns. Keine Race zwischen Backend und Frontend.
Output: 4 geaenderte Dateien, App startet ohne 500, neue `/api/beenden`-Calls persistieren `kb_verlauf`, `/session/<id>` liefert alle Template-Variablen die Plan 02 benoetigt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-CONTEXT.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-RESEARCH.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-PATTERNS.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key models / exports the executor needs. Extracted from codebase per PATTERNS.md. -->

From database/models.py (ConversationLog, line ~260-284):
```python
class ConversationLog(Base):
    __tablename__ = 'conversation_logs'
    id              = Column(Integer, primary_key=True)
    user_id         = Column(Integer, ForeignKey('users.id'))
    org_id          = Column(Integer, ForeignKey('organisations.id'))
    typ             = Column(String(20), default='live')     # 'live' | 'training'
    session_mode    = Column(String(20))                     # 'cold_call' | 'meeting' (live) or training modus
    sterne          = Column(Integer)                        # 1-5, NULL fuer Training (SC-1)
    kb_end          = Column(Integer)                        # 0-100, fuer Training = scoring.gesamt_score
    einwaende_gesamt   = Column(Integer, default=0)
    einwaende_behandelt= Column(Integer, default=0)
    redeanteil_avg     = Column(Float)
    skript_abdeckung   = Column(Float)
    phasen_details     = Column(Text)                        # JSON — fuer Training enthaelt scoring-Dict
    painpoints_details = Column(Text)
    gegenargument_details = Column(Text)
    personality_type_id = Column(Integer, ForeignKey('personality_types.id'), nullable=True)
    stimmung_history    = Column(Text, nullable=True)        # JSON [{turn, wert, grund}, ...]
    precall_briefing    = Column(Text, nullable=True)
    # NEU 07.1:
    # kb_verlauf       = Column(Text, nullable=True)         # JSON [{ts, wert}, ...]
    created_at      = Column(DateTime, default=utcnow)
```

From database/models.py (ObjectionEvent FK — SC-8):
```python
class ObjectionEvent(Base):
    __tablename__ = 'objection_events'
    id                    = Column(Integer, primary_key=True)
    conversation_log_id   = Column(Integer, ForeignKey('conversation_logs.id'))   # SC-8: NICHT conv_id
    einwand_typ           = Column(String(50))
    option_gewaehlt       = Column(String(200))
    erfolgreich           = Column(Boolean)
    created_at            = Column(DateTime, default=utcnow)
```

From database/models.py (PersonalityType, line ~217-228):
```python
class PersonalityType(Base):
    __tablename__ = 'personality_types'
    id               = Column(Integer, primary_key=True)
    name             = Column(String(100), nullable=False)
    icon             = Column(String(10), nullable=True)
    # ...kein relationship() auf ConversationLog — Route muss pre-resolven
```

From routes/app_routes.py:573-583 (Analog-Signatur fuer neuen Helper):
```python
def _calc_call_score(conv):
    """Server-seitige Spiegelung der client-seitigen _calcScore()-Formel.
    Gewichtung: kb_end 40%, behandeltRate 30%, redeScore 20%, skript 10%."""
    kb = conv.kb_end if conv.kb_end is not None else 30
    einw_total = conv.einwaende_gesamt or 0
    einw_ok = conv.einwaende_behandelt or 0
    behandelt_rate = (einw_ok / einw_total) if einw_total > 0 else 0.5
    redeanteil = conv.redeanteil_avg if conv.redeanteil_avg is not None else 50
    rede_score = max(0, 100 - abs(redeanteil - 40) * 2)
    skript = conv.skript_abdeckung or 0
    return min(100, max(0, round(kb * 0.4 + behandelt_rate * 100 * 0.3 + rede_score * 0.2 + skript * 0.1)))
```

From routes/app_routes.py:602-618 (ORM-Query + Tenant-Isolation Pattern fuer Helper):
```python
db = get_session()
try:
    recent = (db.query(ConversationLog)
                .filter(ConversationLog.user_id == g.user.id)
                .filter(ConversationLog.typ == 'live')
                .order_by(ConversationLog.created_at.desc())
                .limit(n).all())
    # ...
finally:
    db.close()
```

From routes/app_routes.py:384 (JSON-Serializer-Import bereits vorhanden):
```python
import json as _json
```

From routes/app_routes.py:283 (lokale Variable kb_verlauf bereits definiert in /api/beenden):
```python
kb_verlauf  = list(ls.kaufbereitschaft_verlauf)   # [{ts, wert}, ...]
```

From routes/dashboard.py:659-676 (bestehende Route — Basis zum Erweitern):
```python
@dashboard_bp.route('/session/<int:sid>')
@login_required
def session_detail(sid):
    from database.models import ConversationLog as CL, ObjectionEvent
    db = get_session()
    try:
        conv = db.query(CL).filter(CL.id == sid, CL.user_id == g.user.id).first()
        if not conv:
            abort(404)
        events = db.query(ObjectionEvent).filter(ObjectionEvent.conversation_log_id == sid).all()
        return render_template('session_detail.html', conv=conv, events=events)
    finally:
        db.close()
```

## Cross-Plan Data Contract — Template Context Keys (Plan 01 → Plan 02)

session_detail-Route MUSS folgende Keys an `render_template('session_detail.html', ...)` uebergeben:

| Key                  | Type                | Live                                | Training                              |
|----------------------|---------------------|-------------------------------------|---------------------------------------|
| `conv`               | ConversationLog     | required                            | required                              |
| `events`             | List[ObjectionEvent]| required                            | required                              |
| `pt`                 | PersonalityType\|None | None (live nutzt es nicht)        | resolve aus personality_type_id       |
| `trend_avg`          | int \| None         | int wenn >=1 Vorgaenger sonst None  | **None** (Training rendert kein Trend-Badge — W-06) |
| `chart_data_json`    | str (JSON)          | conv.kb_verlauf or '[]'             | conv.stimmung_history or '[]'         |
| `schwierigkeit_label`| str                 | '—'                                 | 'Einsteiger'/'Fortgeschritten'/'Experte'/'—' |
| `recommendations`    | List[Dict]          | typ-aware Helper-Output             | typ-aware Helper-Output               |
| `score_total`        | int                 | `_calc_call_score(conv)` (Gesamt)   | `conv.kb_end or 0` (Training-Score-Fallback) |

**W-06 Fix:** `score_total` ersetzt fruehere Template-seitige Berechnung aus `conv.kb_end`. Score-Hero im Template MUSS `score_total` lesen (NICHT mehr `conv.kb_end`), damit Score-Hero-Zahl und Trend-Delta semantisch konsistent gegen denselben Gesamt-Score-Wert vergleichen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DB Migration kb_verlauf + ORM Column + Persistenz in /api/beenden</name>
  <files>app.py, database/models.py, routes/app_routes.py</files>
  <read_first>
    - app.py (bestehender Zustand — insbesondere Zeilen 28 und 310-330 fuer Migrations-Block)
    - database/models.py (ConversationLog Klasse um Zeile 260-284 — stimmung_history ist Analog)
    - routes/app_routes.py (Zeilen 280-430 — `/api/beenden` Handler mit ConversationLog-Create)
    - .planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-PATTERNS.md (Sektion 2.1, 2.2, 2.3 — exakte Spiegel-Patterns)
    - .planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-RESEARCH.md (Q1 kb_verlauf Shape-Dokumentation)
  </read_first>
  <behavior>
    - App startet ohne Exception bei erstem Start nach Deploy (Migration legt Spalte an)
    - App startet ohne Exception bei zweitem Start (Migration ist idempotent, fangt Duplicate-Column-Fehler)
    - Neuer Live-Call endet -> DB-Row hat `kb_verlauf` nicht-null, valider JSON-String `[{"ts":"...","wert":N}, ...]`
    - Alte Calls (vor Migration) haben `kb_verlauf IS NULL` — Frontend behandelt das als Empty-State
    - ConversationLog.kb_verlauf als ORM-Attribut lesbar: `conv.kb_verlauf` gibt den JSON-String zurueck
  </behavior>
  <action>
    **1) app.py — Migration-Block einfuegen (Spiegel Phase 04.9):**

    Direkt NACH dem bestehenden 04.9-Block (nach Zeile 322 `pass` — d.h. vor dem existierenden Phase 04.13-Block um Zeile 323) folgenden Block einfuegen:

    ```python
    # ── Phase 07.1: kb_verlauf fuer Live-Session-Chart ────────────────────
    for col, typedef in [
        ('kb_verlauf', 'TEXT'),
    ]:
        try:
            conn.execute(text(f'ALTER TABLE conversation_logs ADD COLUMN {col} {typedef}'))
            conn.commit()
            print(f"[DB] Migration: added conversation_logs.{col}")
        except Exception:
            pass
    ```

    Exakter Spiegel des 04.9-Blocks in app.py:312-322 (stimmung_history etc.). Das `conn`-Objekt ist durch den umgebenden Context bereitgestellt. Idempotent durch `except Exception: pass`.

    **2) database/models.py — ConversationLog.kb_verlauf Column anhaengen:**

    Direkt NACH `precall_briefing = Column(Text, nullable=True)` (um Zeile 281) und VOR der naechsten Klasse (`class Phrase` um Zeile 284) einfuegen:

    ```python
    # Phase 07.1: Kaufbereitschafts-Verlauf fuer Live-Session-Chart
    kb_verlauf               = Column(Text, nullable=True)     # JSON list [{ts: "HH:MM:SS", wert: 0-100}]
    ```

    Spiegel der `stimmung_history`-Column-Definition auf Zeile 279. Column-Name ASCII (Umlaut-Regel, CLAUDE.md).

    **3) routes/app_routes.py — kb_verlauf im /api/beenden ConversationLog-Create persistieren:**

    In routes/app_routes.py zwischen `precall_briefing=precall_briefing,` (Zeile 424) und der schliessenden Klammer `)` (Zeile 425) folgende Zeile einfuegen:

    ```python
                precall_briefing=precall_briefing,
                kb_verlauf=_json.dumps(kb_verlauf, ensure_ascii=False),   # Phase 07.1
            )
    ```

    `_json` ist bereits importiert (Zeile 384 `import json as _json`). `kb_verlauf` ist bereits lokale Variable (Zeile 283 `kb_verlauf = list(ls.kaufbereitschaft_verlauf)`). Keine weiteren Imports noetig.

    **Umlaut-Regel:** Alle Identifier ASCII. User-facing `print`-Text `'[DB] Migration: added conversation_logs.kb_verlauf'` ist ASCII, kein Umlaut.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" &amp;&amp; python -c "from app import app; print('APP_OK')" 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Phase 07.1: kb_verlauf" app.py returns >= 1
    - grep -c "ADD COLUMN {col} {typedef}" app.py returns >= 2 (04.9 block + 07.1 block)
    - grep -c "kb_verlauf" database/models.py returns >= 1
    - grep "kb_verlauf" database/models.py returns a line containing "Column(Text, nullable=True)"
    - grep -c "kb_verlauf=_json.dumps(kb_verlauf, ensure_ascii=False)" routes/app_routes.py returns 1
    - `python -c "from app import app"` runs without ImportError or syntax error
    - `python -c "from database.models import ConversationLog; assert hasattr(ConversationLog, 'kb_verlauf')"` exits 0
    - After running the app once against a test DB, `PRAGMA table_info(conversation_logs)` in sqlite shows a `kb_verlauf` column (manual verification via execute-phase if DB present)
  </acceptance_criteria>
  <done>
    app.py enthaelt idempotenten 07.1-Migrations-Block direkt nach 04.9-Block, database/models.py enthaelt kb_verlauf-Column auf ConversationLog, routes/app_routes.py persistiert kb_verlauf im ConversationLog-Create des /api/beenden-Handlers. App laedt ohne Import-Error.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: _derive_practice_recommendations Helper + session_detail-Route Erweiterung</name>
  <files>routes/app_routes.py, routes/dashboard.py</files>
  <read_first>
    - routes/app_routes.py (Zeilen 573-620 — _calc_call_score + api_postcall_trend als Signatur/Pattern-Analog)
    - routes/dashboard.py (Zeilen 655-680 — bestehende session_detail-Route)
    - database/models.py (ObjectionEvent und PersonalityType Klassen, etwa Zeilen 217-350)
    - services/training_service.py (SCHWIERIGKEITEN-Dict — import fuer schwierigkeit_label)
    - .planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-PATTERNS.md (Sektion 2.4 + 2.5 — exakte Helper-Signatur + Route-Patch)
    - .planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-RESEARCH.md (Q2, Q3 — Sterne=NULL fuer Training, Schwierigkeit aus phasen_details, SC-7 datetime nicht INTERVAL, SC-8 conversation_log_id)
    - .planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-UI-SPEC.md (Sektion 9 "Was du üben solltest" Helper-Contract mit cross_context dict keys)
  </read_first>
  <behavior>
    - Live-Session mit 3 unbehandelten Einwaenden -> recommendations enthaelt mindestens einen Eintrag mit `training_focus='objections:kein_bedarf'` (oder passendem einwand_typ) und `cross_context` dict mit `sessions`, `avg_score`, `focus_match`
    - Live-Session mit hohem Score und ohne Schwachstellen -> `recommendations == []`
    - Training-Session mit `kb_end < 50` -> recommendations enthaelt `training_focus='training:generic_weakness'` mit `cross_context` gesetzt auf Live-avg-Abfrage ueber ObjectionEvent
    - Training-Session mit letztem `stimmung.wert == -5` (Kunde aufgelegt) -> recommendations enthaelt `training:personality_{id}` oder `training:mood_management`
    - Non-objection recommendations (kb:drop, redeanteil, skript:opener) haben `cross_context=None`
    - Liste ist auf max 3 Elemente begrenzt, sortiert nach Prioritaet
    - Bei DB-Exception in Cross-Context-Query: Helper logged Fehler, setzt `cross_context=None`, gibt Recommendation trotzdem zurueck (nie crash)
    - session_detail-Route gibt alle 8 Template-Kontext-Variablen korrekt gesetzt aus: conv, events, pt, trend_avg, chart_data_json, schwierigkeit_label, recommendations, score_total
    - Fuer Training-Session: `chart_data_json = conv.stimmung_history or '[]'`, `score_total = conv.kb_end or 0`, `trend_avg = None` (W-06: Trend-Badge nur fuer Live)
    - Fuer Live-Session: `chart_data_json = conv.kb_verlauf or '[]'`, `score_total = _calc_call_score(conv)`, `trend_avg = avg(_calc_call_score) der letzten 5 Live-Sessions`
    - `pt` ist None wenn `conv.personality_type_id` NULL, sonst PersonalityType-Instance
    - `trend_avg` ist int wenn `>= 1` vergangener Live-Call existiert (NUR fuer typ='live'), sonst None
    - `schwierigkeit_label` ist 'Einsteiger' / 'Fortgeschritten' / 'Experte' oder em-dash '—' wenn nicht aus phasen_details extrahierbar
  </behavior>
  <action>
    **W-01 FIX (Cross-Context-Helper Root-Entity):** Beide `_cross_context_objections_*`-Helper MUESSEN `select_from(OE)` explizit setzen, weil das query-tuple mit Aggregaten und JOIN auf CL die Root-Entity sonst ambig laesst (potentieller `InvalidRequestError`).

    **W-02 FIX (Circular-Import-Vermeidung):** routes/app_routes.py darf KEIN `from routes.dashboard` Import enthalten. Dashboard importiert app_routes-Helper, nicht umgekehrt.

    **W-06 FIX Plan-01-side:** Route berechnet `score_total` server-side und uebergibt als Template-Variable. Live-Session: `_calc_call_score(conv)`. Training-Session: `conv.kb_end or 0`. Trend-Badge wird nur fuer Live gerendert (`trend_avg` bleibt fuer Training None).

    **1) routes/app_routes.py — Neuer Helper DIREKT nach _calc_call_score (nach Zeile 583):**

    Zwei Cross-Context-Helpers + Main-Function. Imports einmal oben im File ergaenzen falls noch nicht da (`datetime`, `timedelta`, `func` — pruefen!):

    ```python
    # ========================================================================
    # Phase 07.1: POLISH-24 Practice Recommendations Helper
    # ========================================================================

    def _cross_context_objections_live(db, user_id, einwand_typ):
        """Live-Session -> wie oft/gut im Training mit diesem Einwand?
        Returns: {sessions: int, avg_score: float|None, focus_match: str}
        Filter: typ='training', letzte 14 Tage, einwand_typ match.
        Score basiert auf kb_end (Training), NICHT sterne (SC-1 — Training setzt sterne NICHT).
        Root-Entity: OE explizit via select_from() (W-01 — query mit Aggregaten + JOIN).
        """
        from datetime import datetime, timedelta
        from sqlalchemy import func
        from database.models import ConversationLog as CL, ObjectionEvent as OE
        try:
            cutoff = datetime.utcnow() - timedelta(days=14)
            row = (db.query(
                        func.count(func.distinct(CL.id)).label('n'),
                        func.avg(CL.kb_end).label('avg_score'),
                    )
                    .select_from(OE)                              # W-01: Root explizit
                    .join(CL, CL.id == OE.conversation_log_id)
                    .filter(CL.user_id == user_id)
                    .filter(CL.typ == 'training')
                    .filter(CL.created_at >= cutoff)
                    .filter(OE.einwand_typ == einwand_typ)
                    .first())
            n = int(row.n or 0) if row else 0
            avg_score = float(row.avg_score) if (row and row.avg_score is not None) else None
            return {'sessions': n, 'avg_score': avg_score, 'focus_match': einwand_typ}
        except Exception as exc:
            print(f"[07.1] cross_context_objections_live failed: {exc}")
            return None


    def _cross_context_objections_training(db, user_id, einwand_typ):
        """Training-Session -> wie oft/erfolgreich im Live-Call mit diesem Einwand?
        Returns: {sessions: int, n_success: int, focus_match: str}
        Filter: typ='live', letzte 14 Tage.
        Root-Entity: OE explizit via select_from() (W-01).
        """
        from datetime import datetime, timedelta
        from sqlalchemy import func, case
        from database.models import ConversationLog as CL, ObjectionEvent as OE
        try:
            cutoff = datetime.utcnow() - timedelta(days=14)
            rows = (db.query(OE.erfolgreich)
                    .select_from(OE)                              # W-01: Root explizit
                    .join(CL, CL.id == OE.conversation_log_id)
                    .filter(CL.user_id == user_id)
                    .filter(CL.typ == 'live')
                    .filter(CL.created_at >= cutoff)
                    .filter(OE.einwand_typ == einwand_typ)
                    .all())
            n = len(rows)
            n_success = sum(1 for r in rows if r.erfolgreich)
            return {'sessions': n, 'n_success': n_success, 'focus_match': einwand_typ}
        except Exception as exc:
            print(f"[07.1] cross_context_objections_training failed: {exc}")
            return None


    def _derive_practice_recommendations(db, conv, events):
        """POLISH-24 R3 — typ+mode-aware practice recommendations.
        Returns: List[Dict] mit max 3 Eintraegen, sortiert nach Prioritaet.

        Keys pro Eintrag:
          icon: 'target' | 'alert-circle' | 'trending-down'
          observation: str (14px/600 konkrete Zahl + Beobachtung)
          explanation: str (14px/400 ein Satz)
          training_focus: str (ASCII-Slug)
          training_url: str (heute immer '/training')
          cross_context: dict | None (nur bei objection-Recommendations)
        """
        recs = []
        typ = getattr(conv, 'typ', 'live') or 'live'

        # ── Training-spezifische Regeln ────────────────────────────────
        if typ == 'training':
            # Regel 1: kb_end < 50 -> generic weakness
            if (conv.kb_end or 0) < 50:
                recs.append({
                    'icon': 'alert-circle',
                    'observation': f'Gesamt-Score unter 50 ({conv.kb_end or 0}/100)',
                    'explanation': 'Allgemeine Schwaeche erkannt. Wiederhole ein aehnliches Szenario.',
                    'training_focus': 'training:generic_weakness',
                    'training_url': '/training',
                    'cross_context': None,
                })

            # Regel 2: Kunde hat aufgelegt (stimmung[-1] == -5)
            try:
                import json as _json_local
                hist = _json_local.loads(conv.stimmung_history or '[]')
                if hist and hist[-1].get('wert') == -5:
                    pt_id = getattr(conv, 'personality_type_id', None)
                    focus = f'training:personality_{pt_id}' if pt_id else 'training:mood_management'
                    recs.append({
                        'icon': 'target',
                        'observation': 'Kunde hat aufgelegt (Stimmung -5)',
                        'explanation': 'Wiederhole mit dem gleichen Persoenlichkeitstyp und fang Stimmungs-Drop frueher ab.',
                        'training_focus': focus,
                        'training_url': '/training',
                        'cross_context': None,
                    })
            except Exception:
                pass

            # Regel 3: Einwaende mit niedriger Behandlungs-Rate (Training -> Live cross)
            for ev in (events or [])[:3]:
                if not getattr(ev, 'erfolgreich', True):
                    recs.append({
                        'icon': 'alert-circle',
                        'observation': f'Einwand "{ev.einwand_typ}" nicht erfolgreich behandelt',
                        'explanation': 'Im echten Call ist dieser Einwand haeufig — uebe die Antwort.',
                        'training_focus': f'objections:{ev.einwand_typ}',
                        'training_url': '/training',
                        'cross_context': _cross_context_objections_training(db, conv.user_id, ev.einwand_typ),
                    })

            # Regel 4: Stimmungs-Verschlechterung (Drop >= 3 Punkte ueber Verlauf)
            try:
                import json as _json_local
                hist = _json_local.loads(conv.stimmung_history or '[]')
                if len(hist) >= 2:
                    max_wert = max((h.get('wert', 0) for h in hist), default=0)
                    min_wert = min((h.get('wert', 0) for h in hist), default=0)
                    if (max_wert - min_wert) >= 3 and len(recs) < 3:
                        recs.append({
                            'icon': 'trending-down',
                            'observation': f'Stimmung fiel um {max_wert - min_wert} Punkte',
                            'explanation': 'Die Kundin wurde deutlich negativer. Uebe Stimmungs-Management.',
                            'training_focus': 'training:mood_management',
                            'training_url': '/training',
                            'cross_context': None,
                        })
            except Exception:
                pass

        # ── Live-Regeln (cold_call + meeting) ──────────────────────────
        else:
            # Regel 1: Nicht behandelte Einwaende
            not_handled = [ev for ev in (events or []) if not getattr(ev, 'erfolgreich', True)]
            for ev in not_handled[:3]:
                recs.append({
                    'icon': 'target',
                    'observation': f'Einwand "{ev.einwand_typ}" nicht behandelt',
                    'explanation': 'Im Training kannst du die Antwort mit weniger Druck festigen.',
                    'training_focus': f'objections:{ev.einwand_typ}',
                    'training_url': '/training',
                    'cross_context': _cross_context_objections_live(db, conv.user_id, ev.einwand_typ),
                })

            # Regel 2: kb_end < 40 -> drop
            if (conv.kb_end is not None) and conv.kb_end < 40 and len(recs) < 3:
                recs.append({
                    'icon': 'trending-down',
                    'observation': f'Kaufbereitschaft Ende: {conv.kb_end}/100',
                    'explanation': 'Der Kunde ist am Ende skeptischer als gesund. Uebe Qualifizierungs-Fragen.',
                    'training_focus': 'kb:drop',
                    'training_url': '/training',
                    'cross_context': None,
                })

            # Regel 3: Redeanteil zu hoch / zu niedrig (Optimum ~40%)
            rede = conv.redeanteil_avg
            if rede is not None and len(recs) < 3:
                if rede > 65:
                    recs.append({
                        'icon': 'alert-circle',
                        'observation': f'Du redest {int(rede)}% — zu viel',
                        'explanation': 'Gute Berater reden ca. 40%. Uebe aktives Zuhoeren und offene Fragen.',
                        'training_focus': 'redeanteil:too_high',
                        'training_url': '/training',
                        'cross_context': None,
                    })
                elif rede < 25:
                    recs.append({
                        'icon': 'alert-circle',
                        'observation': f'Du redest nur {int(rede)}% — zu wenig',
                        'explanation': 'Fuehre das Gespraech aktiver. Stelle gezielte Fragen und setze Impulse.',
                        'training_focus': 'redeanteil:too_low',
                        'training_url': '/training',
                        'cross_context': None,
                    })

            # Regel 4: Skript-Abdeckung < 40%
            if (conv.skript_abdeckung or 0) < 40 and len(recs) < 3:
                recs.append({
                    'icon': 'alert-circle',
                    'observation': f'Skript nur zu {int(conv.skript_abdeckung or 0)}% abgedeckt',
                    'explanation': 'Die wichtigen Bausteine deines Skripts sind nicht gefallen. Uebe den Opener.',
                    'training_focus': 'skript:opener',
                    'training_url': '/training',
                    'cross_context': None,
                })

        return recs[:3]
    ```

    **2) routes/dashboard.py — session_detail erweitern:**

    Komplette Route ersetzen (Zeilen 659-676). Imports inline halten (Pattern aus bestehender Route):

    ```python
    @dashboard_bp.route('/session/<int:sid>')
    @login_required
    def session_detail(sid):
        from database.models import ConversationLog as CL, ObjectionEvent, PersonalityType
        from routes.app_routes import _calc_call_score, _derive_practice_recommendations
        import json as _json

        db = get_session()
        try:
            conv = db.query(CL).filter(
                CL.id == sid,
                CL.user_id == g.user.id,
            ).first()
            if not conv:
                abort(404)

            events = db.query(ObjectionEvent).filter(
                ObjectionEvent.conversation_log_id == sid,
            ).order_by(ObjectionEvent.id.asc()).all()

            # Phase 07.1: personality pre-resolve (kein relationship() am Model, PATTERNS 4.9)
            pt = None
            if conv.personality_type_id:
                pt = db.query(PersonalityType).filter_by(id=conv.personality_type_id).first()

            # Phase 07.1 (W-06): score_total typ-aware
            #  - Live: _calc_call_score (Gesamt-Score, 4 Komponenten)
            #  - Training: kb_end Fallback (kein _calc_call_score fuer Training)
            conv_typ = (conv.typ or 'live')
            if conv_typ == 'live':
                score_total = _calc_call_score(conv)
            else:
                score_total = conv.kb_end or 0

            # Phase 07.1 (W-06): Trend-Avg NUR fuer Live-Sessions berechnen.
            # Training-Sessions rendern KEIN Trend-Badge (sparse Datenlage,
            # _calc_call_score nicht fuer Training definiert).
            trend_avg = None
            if conv_typ == 'live':
                recent = (db.query(CL)
                            .filter(CL.user_id == g.user.id)
                            .filter(CL.typ == 'live')
                            .filter(CL.id != conv.id)
                            .order_by(CL.created_at.desc())
                            .limit(5).all())
                trend_avg = round(sum(_calc_call_score(c) for c in recent) / len(recent)) if recent else None

            # Phase 07.1: chart_data_json typ-diskriminierend
            if conv_typ == 'training':
                chart_data_json = conv.stimmung_history or '[]'
            else:
                chart_data_json = conv.kb_verlauf or '[]'

            # Phase 07.1: schwierigkeit_label aus phasen_details parsen (RESEARCH Q2 Option A)
            schwierigkeit_label = '—'
            if conv_typ == 'training' and conv.phasen_details:
                try:
                    pd = _json.loads(conv.phasen_details)
                    raw = (pd.get('schwierigkeit') if isinstance(pd, dict) else None) or ''
                    mapping = {
                        'leicht':   'Einsteiger',
                        'mittel':   'Fortgeschritten',
                        'schwer':   'Experte',
                        'einsteiger': 'Einsteiger',
                        'fortgeschritten': 'Fortgeschritten',
                        'experte':  'Experte',
                    }
                    schwierigkeit_label = mapping.get(str(raw).lower(), '—')
                except Exception:
                    schwierigkeit_label = '—'

            # Phase 07.1: Recommendations
            recommendations = _derive_practice_recommendations(db, conv, events)

            return render_template(
                'session_detail.html',
                conv=conv,
                events=events,
                pt=pt,
                trend_avg=trend_avg,
                chart_data_json=chart_data_json,
                schwierigkeit_label=schwierigkeit_label,
                recommendations=recommendations,
                score_total=score_total,            # W-06: Gesamt-Score fuer Score-Hero
            )
        finally:
            db.close()
    ```

    **CRITICAL: KEIN `relationship()` am ConversationLog-Model ergaenzen** (PATTERNS 4.9). Pre-Resolve via zweite Query ist das etablierte Pattern.

    **SC-7:** Python-side `datetime.utcnow() - timedelta(days=14)`, NICHT SQL `INTERVAL`.
    **SC-8:** `ObjectionEvent.conversation_log_id` (NICHT `conv_id`).
    **SC-1:** Cross-Context-Query nutzt `AVG(kb_end)`, NICHT `AVG(sterne)` — Training setzt `sterne` NULL.
    **W-01:** `select_from(OE)` explizit in beiden Helpers (Root-Entity disambiguation).
    **W-02:** routes/app_routes.py darf NICHT von routes/dashboard importieren (Circular).
    **W-06:** `score_total` typ-aware via Route, Trend-Badge nur Live.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" &amp;&amp; python -c "from routes.app_routes import _derive_practice_recommendations, _cross_context_objections_live, _cross_context_objections_training; print('HELPERS_OK')" 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def _derive_practice_recommendations" routes/app_routes.py returns 1
    - grep -c "def _cross_context_objections_live" routes/app_routes.py returns 1
    - grep -c "def _cross_context_objections_training" routes/app_routes.py returns 1
    - grep -c "OE.conversation_log_id" routes/app_routes.py returns at least 2 (both cross_context helpers)
    - grep -c "conv_id" routes/app_routes.py for new helpers returns 0 (SC-8 — FK is conversation_log_id)
    - grep -c "datetime.utcnow() - timedelta(days=14)" routes/app_routes.py returns at least 2
    - grep -c "func.avg(CL.kb_end)" routes/app_routes.py returns at least 1 (SC-1 — kb_end not sterne)
    - grep -c "INTERVAL" routes/app_routes.py within new helpers returns 0 (SC-7)
    - **W-01:** grep -c "select_from(OE)" routes/app_routes.py returns at least 2 (both helpers — root entity disambiguation)
    - **W-02:** grep -c "from routes.dashboard" routes/app_routes.py returns 0 (no circular import)
    - **W-01 Smoke-Test:** `python -c "from routes.app_routes import _cross_context_objections_live, _cross_context_objections_training; from database.db import get_session; db=get_session(); r1=_cross_context_objections_live(db, 1, 'price-objection'); r2=_cross_context_objections_training(db, 1, 'price-objection'); assert r1 is None or isinstance(r1, dict), f'live not dict: {r1!r}'; assert r2 is None or isinstance(r2, dict), f'train not dict: {r2!r}'; db.close(); print('CROSS_OK', r1, r2)"` exits 0 (call must not raise InvalidRequestError)
    - grep -c "chart_data_json" routes/dashboard.py returns >= 1
    - grep -c "recommendations" routes/dashboard.py returns >= 2
    - grep -c "schwierigkeit_label" routes/dashboard.py returns >= 2
    - grep -c "trend_avg" routes/dashboard.py returns >= 2
    - grep -c "PersonalityType" routes/dashboard.py returns >= 2
    - grep -c "from routes.app_routes import _calc_call_score, _derive_practice_recommendations" routes/dashboard.py returns 1
    - **W-06:** grep -c "score_total" routes/dashboard.py returns >= 3 (compute live, compute training, render_template kwarg)
    - **W-06:** grep -c "score_total=_calc_call_score(conv)" routes/dashboard.py returns 1 (live-branch)
    - **W-06:** grep -c "score_total=score_total" routes/dashboard.py returns 1 (render_template kwarg)
    - **W-06:** grep -c "if conv_typ == 'live'" routes/dashboard.py returns >= 2 (score_total + trend_avg both gated)
    - `python -c "from routes.app_routes import _derive_practice_recommendations"` exits 0
    - `python -c "from routes.dashboard import session_detail"` exits 0 (function importable — no syntax errors)
    - grep -c "relationship(" database/models.py in ConversationLog section around kb_verlauf/personality_type_id returns 0 (PATTERNS 4.9 — no relationship() was added)
  </acceptance_criteria>
  <done>
    routes/app_routes.py enthaelt _derive_practice_recommendations(db, conv, events) + _cross_context_objections_live + _cross_context_objections_training mit explizitem select_from(OE). routes/dashboard.py session_detail-Route liefert Template-Context mit conv, events, pt, trend_avg (None fuer Training), chart_data_json, schwierigkeit_label, recommendations, score_total (typ-aware: _calc_call_score live, kb_end training). Imports fuer PersonalityType und neue Helper sind inline. Kein circular import. Smoke-Test der Cross-Context-Helpers wirft keine SQLAlchemy-Exception. Python laedt ohne Error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /session/<id> | Authenticated user may attempt cross-user session access via manipulated sid |
| Browser -> /api/beenden | Authenticated user submits session-end payload with kb_verlauf data |
| DB ORM -> Template | JSON columns (kb_verlauf, stimmung_history, phasen_details) reach template layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Information Disclosure | routes/dashboard.py session_detail | mitigate | Query filtert bereits `CL.user_id == g.user.id` (bestehend). Recommendations-Helper uebergibt `conv.user_id` — gleicher scope. IDOR auf Basis `sid` verhindert. |
| T-07.1-02 | Tampering | routes/app_routes.py /api/beenden kb_verlauf | mitigate | `kb_verlauf` wird server-seitig aus `ls.kaufbereitschaft_verlauf` gelesen (live_session-internal), NICHT aus client-Payload. Mass-Assignment unmoeglich. |
| T-07.1-03 | Information Disclosure | Cross-Context-Helpers | mitigate | JOIN filtert `CL.user_id == user_id` aus conv-Objekt (server-side). Kein Raw-SQL, nur ORM-filter. Tenant-Isolation bleibt. |
| T-07.1-04 | Denial of Service | Cross-Context-JOIN unbounded | mitigate | Filter `CL.created_at >= cutoff` (14 Tage) + typ-Filter begrenzen Ergebnis-Menge. Index auf (user_id, created_at) existiert implizit durch FK + default timestamps. |
| T-07.1-05 | Injection | phasen_details JSON parse | accept | JSON-Content ist server-written (keine user-controlled strings in phasen_details keys). `_json.loads` + try/except catcht malformed JSON. Kein eval, kein exec. |
| T-07.1-06 | Repudiation | Persistenz von kb_verlauf | accept | audit_log bleibt unberuehrt, kb_verlauf ist rein analytische Daten. Keine Auditability-Anforderung. |

</threat_model>

<verification>
After both tasks:
- `python -c "from app import app; print('ok')"` runs without error
- `python -c "from routes.app_routes import _derive_practice_recommendations; from routes.dashboard import session_detail; print('imports ok')"` runs without error
- grep -c "kb_verlauf" app.py database/models.py routes/app_routes.py routes/dashboard.py (combined) >= 5
- App startet lokal (`python app.py`), /session/<id> rendert fuer existierende Session ohne 500 (auch wenn Template noch der alte ist — Route gibt nur context vor)
- W-01: Cross-Context-Helper-Smoke-Test exit 0 (keine InvalidRequestError, beide Aufrufe geben Dict oder None zurueck)
- W-02: grep "from routes.dashboard" routes/app_routes.py returns 0
- W-06: grep "score_total" routes/dashboard.py returns >= 3
</verification>

<success_criteria>
- Migration-Block idempotent, App kann mehrfach startet werden
- ConversationLog.kb_verlauf ORM-Attribut existiert
- /api/beenden persistiert kb_verlauf
- Helper liefert max 3 Recommendations, sortiert nach Prioritaet
- Cross-Context ist nur bei objection-Recommendations gesetzt
- session_detail-Route exponiert 8 Template-Variablen fehlerfrei (inkl. score_total)
- Alle SC-1 bis SC-8 Spec-Corrections respektiert (kein sterne_rating, kein INTERVAL, conversation_log_id, kb_end statt sterne in Cross-Context)
- W-01 / W-02 / W-06 Fixes verifizierbar via grep + Smoke-Test
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-01-SUMMARY.md`
</output>
