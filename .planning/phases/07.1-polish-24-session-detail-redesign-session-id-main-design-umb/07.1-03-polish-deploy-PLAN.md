---
phase: 07.1
plan: 03
name: polish-deploy
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - app.py
autonomous: false
requirements:
  - POLISH-24
user_setup: []
must_haves:
  truths:
    - "CSS_VERSION in app.py:28 ist auf '20260418-2' gebumpt"
    - "Cold-Call-Session rendert /session/<id> mit 11 Sektionen ohne 500, Chart rendert mit kb_verlauf"
    - "Meeting-Session rendert /session/<id> mit identischem Layout wie Cold Call"
    - "Training-Session rendert /session/<id> mit Training-Header (3 Badges), kb_end im Score-Hero, stimmung_history-Chart"
    - "Cross-Context-Badge erscheint bei mindestens einer Live-Session mit Einwand-Recommendation (oder dokumentiert kein passender Case gefunden)"
    - "deploy.sh wurde ausgefuehrt (git pull + restart), CSS_VERSION '20260418-2' erscheint im Browser-Source der live Seite"
  artifacts:
    - path: "app.py"
      provides: "CSS_VERSION bump von '20260418-1' auf '20260418-2'"
      contains: "CSS_VERSION']          = '20260418-2'"
  key_links:
    - from: "app.py CSS_VERSION"
      to: "base.html {{ config.get('CSS_VERSION', '1') }}"
      via: "Flask config lookup"
      pattern: "20260418-2"
    - from: "Browser cache-bust"
      to: "nerve.css?v=20260418-2"
      via: "base.html CSS link"
      pattern: "\\?v=20260418-2"
---

<objective>
Polish + Deploy-Wave fuer POLISH-24: CSS_VERSION bumpen, deploy.sh ausfuehren, und manuelle Smoke-Tests im Browser fuer alle drei Session-Typen (cold_call, meeting, training) durchfuehren. Cross-Context-Badge-Verifikation. Optional: Chart-Error-Copy Review (non-blocking).

Purpose: Sicherstellen, dass die Aenderungen aus Plan 01 (Backend) und Plan 02 (Frontend) live auf getnerve.app sichtbar sind und fuer alle drei Session-Typen fehlerfrei rendern. Browser-Cache wird durch Version-Bump invalidiert.
Output: Getnerve.app zeigt neue Session-Detail-Seite mit 11 Sektionen in MAIN DESIGN, Deploy-Check im Server-Log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-CONTEXT.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-01-backend-db-helper-PLAN.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-02-frontend-template-css-PLAN.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-PATTERNS.md
@CLAUDE.md

<interfaces>
CSS_VERSION Position (CORRECTION per PATTERNS 4.8):
- Lives in app.py:28, NICHT in templates/base.html
- base.html:7 reads via `{{ config.get('CSS_VERSION', '1') }}`
- Aktueller Wert: `app.config['CSS_VERSION']          = '20260418-1'`
- Neuer Wert:     `app.config['CSS_VERSION']          = '20260418-2'`

deploy.sh (PATTERNS 4.11):
- Existiert bereits, braucht KEINEN Code-Touch
- Macht `git pull` + Server-Restart per Projekt-Convention
- Nicht in files_modified listen

Session-Typen fuer Smoke-Test:
- Cold Call: conv.typ='live', conv.session_mode='cold_call', hat kb_verlauf (bei neuen Calls nach Plan 01 Deploy)
- Meeting:   conv.typ='live', conv.session_mode='meeting',   hat kb_verlauf (bei neuen Calls nach Plan 01 Deploy)
- Training:  conv.typ='training',                            hat stimmung_history

Cross-Context-Badge:
- Rendert in Sektion 9 fuer Recommendations mit rec.cross_context != None
- Nur bei objection-Recommendations (focus startet mit 'objections:')
- Drei States: neutral / --danger (0 sessions) / --success (>=3 sessions + avg_score >=70)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: CSS_VERSION bump in app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py (Zeilen 25-32 — CSS_VERSION Deklaration)
    - .planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-PATTERNS.md Sektion 2.1 + Risk Callout 4.8 (CSS_VERSION location correction)
  </read_first>
  <behavior>
    - app.py:28 enthaelt `app.config['CSS_VERSION']          = '20260418-2'`
    - Alter Wert `'20260418-1'` ist nicht mehr im File
    - Keine anderen Zeilen in app.py veraendert (minimal-invasive Aenderung)
    - Python laedt App ohne Error
  </behavior>
  <action>
    In `app.py` Zeile 28 den Wert des CSS_VERSION-Configs aendern:

    Vorher:
    ```python
    app.config['CSS_VERSION']          = '20260418-1'
    ```

    Nachher:
    ```python
    app.config['CSS_VERSION']          = '20260418-2'
    ```

    Formatierung (Whitespace + Alignment) exakt beibehalten — nur der Literal-String-Wert wechselt. Kein anderer Code wird beruehrt.

    **WICHTIG:** NICHT templates/base.html editieren — dort steht nur der Reader `{{ config.get('CSS_VERSION', '1') }}`, nicht die Deklaration. Briefing-Spawn-Prompt hatte das urspruenglich falsch zugeordnet, PATTERNS 4.8 korrigiert es.
  </action>
  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" &amp;&amp; python -c "
import pathlib
src = pathlib.Path('app.py').read_text(encoding='utf-8')
assert \"CSS_VERSION']          = '20260418-2'\" in src, 'CSS_VERSION not bumped to 20260418-2'
assert \"CSS_VERSION']          = '20260418-1'\" not in src, 'old CSS_VERSION 20260418-1 still present'
from app import app
assert app.config.get('CSS_VERSION') == '20260418-2', f'runtime config: {app.config.get(\"CSS_VERSION\")}'
print('CSS_VERSION_OK')
" 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CSS_VERSION']          = '20260418-2'" app.py` returns exactly 1
    - `grep -c "CSS_VERSION']          = '20260418-1'" app.py` returns exactly 0
    - `python -c "from app import app; assert app.config.get('CSS_VERSION') == '20260418-2'"` exits 0
    - `grep -c "CSS_VERSION" templates/base.html` returns >= 1 (reader exists unchanged)
    - templates/base.html wurde NICHT modifiziert (nur app.py) — verify via git diff showing only app.py changed
  </acceptance_criteria>
  <done>
    app.py:28 enthaelt CSS_VERSION '20260418-2'. App laedt ohne Error. templates/base.html wurde nicht angefasst.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy + Browser-Smoke-Test fuer alle 3 Session-Typen + Cross-Context-Verifikation</name>
  <what-built>
    Plan 01 persistiert kb_verlauf in /api/beenden und liefert vollstaendigen Template-Context.
    Plan 02 hat templates/session_detail.html komplett neu gebaut (11 Sektionen, typ-diskriminierend) und nerve.css um 21+ Klassen erweitert.
    Plan 03 Task 1 hat CSS_VERSION auf '20260418-2' gebumpt fuer Cache-Bust.

    Claude automatisiert:
    - Deploy: `bash deploy.sh` (erledigt git pull + Server-Restart per Projekt-Convention)
    - lokalen Render-Smoke: `python -c "from app import app; app.jinja_env.get_template('session_detail.html')"` ohne Error
  </what-built>
  <how-to-verify>
    Claude fuehrt zuerst den Deploy aus:

    ```bash
    bash deploy.sh
    ```

    Output muss zeigen: git pull war erfolgreich, Server wurde neu gestartet. Falls Server-Log-Zugriff verfuegbar: keine neuen 500-Errors in den ersten 30 Sekunden nach Restart.

    ANSCHLIESSEND: Du (André) oeffnest getnerve.app im Browser und testest folgende Flows:

    **Test 1 — Cold Call Session (typ='live', session_mode='cold_call'):**
    1. Gehe zu https://getnerve.app/logs
    2. Oeffne eine existierende Cold-Call-Session per Klick auf einen Eintrag (URL sollte `/session/<id>` sein)
    3. Pruefe in Chrome DevTools → Network → nerve.css → Request-URL hat `?v=20260418-2` (Cache-Bust sichtbar)
    4. Visuell pruefen:
       - Header: `Session #<id>`, Badge `Cold Call` (teal), Datum, Dauer, Result
       - Score-Hero: grosser Score, 4 Breakdown-Komponenten (Kaufbereitschaft Ende 40% / Einwände behandelt 30% / Redeanteil 20% / Skript-Abdeckung 10%), Trend-Badge vs Schnitt letzte 5
       - Section 3: Kaufbereitschafts-Verlauf Chart rendert (Teal-Linie, X=Zeitstempel, Y=0-100). Bei alten Sessions ohne kb_verlauf: Empty-State "Noch keine Verlaufsdaten"
       - Section 4: Einwand-Timeline mit allen Events, "Erfolgreich"-Badges teal, "Nicht behandelt"-Badges rot
       - Sections 5-8: Phasen-Strip / Skript-Progress / Painpoints / PreCall (collapsible)
       - Section 9 "Was du üben solltest": bis 3 Cards mit teal Left-Border, Lucide-Icon, Observation, Explanation, CTA "Im Training üben"
       - Sections 10 + 11: gestrichelte Border, Opacity 0.6, "Kommt mit Phase 4.19" / "Kommt mit Phase 4.11"
       - Keine Dark-Mode-Farben (#1C2333, #0D1117) sichtbar
       - Zurueck-Link links oben navigiert zu /logs

    **Test 2 — Meeting Session (typ='live', session_mode='meeting'):**
    1. Gehe zu https://getnerve.app/logs und finde eine Meeting-Session (falls vorhanden). Falls keine Meeting-Session in der DB ist: **simuliere** eine durch neuen Call-Start mit Meeting-Consent → Call beenden → neu aufrufen. Oder melde Claude, dass kein Meeting-Case verfuegbar ist → Test 2 wird als "not applicable" markiert.
    2. Session-Detail aufrufen
    3. Layout identisch zu Cold Call, nur Badge ist `Meeting` (neutral-grau statt teal)

    **Test 3 — Training Session (typ='training'):**
    1. Gehe zu https://getnerve.app/logs und finde eine Training-Session
    2. Session-Detail aufrufen
    3. Pruefen:
       - Header: 3 Badges (`Training` + Persoenlichkeitstyp wie `Beschäftigter Chef` + Schwierigkeit wie `Fortgeschritten`)
       - Score-Hero: Basis-Wert ist `kb_end` (NICHT sterne — Training setzt sterne immer NULL)
       - Section 3: **Stimmungs-Verlauf** Chart (nicht Kaufbereitschafts-Verlauf), Y-Achse -5 bis +5, positive Werte mit `+`-Prefix (`+3`, `+2`), Canvas-ID `sd-stimmung-chart`
       - Section 6: Empty-State "Training-Session — kein Skript getrackt"
       - Keine 500-Errors

    **Test 4 — Cross-Context-Badge Verifikation:**
    1. Finde eine Live-Session mit mindestens einem nicht-behandelten Einwand (Timeline hat mindestens einen roten Badge "Nicht behandelt")
    2. Scrolle zu Sektion 9
    3. Pruefe: Mindestens eine Recommendation-Card hat einen Cross-Context-Badge zwischen Explanation und CTA-Button
    4. Badge-Farbe:
       - `--danger` (rosa) bei "Noch nie im Training geübt"
       - neutral (grau) bei "Im Training: N Sessions"
       - `--success` (teal) bei "Zuletzt im Training: Ø X/100 aus N Sessions"
    5. Falls KEINE Session mit Einwand-Recommendation findbar: dokumentiere das als "Cross-Context-Case nicht verfuegbar in aktueller DB, Code-Pfad via Logs verifiziert" — nicht-blockierend.

    **Test 5 — Chart-Error-Copy Review (OPTIONAL, non-blocking UI-SPEC Rev 3 FLAG):**
    1. Devtools oeffnen, Chart-Canvas manuell entfernen
    2. Nicht kritisch, da Chart-Fehler-Empty-State im Template schon einen passiven Follow-up-Satz hat ("Bitte erneut versuchen — falls das Problem bleibt, melde dich.")
    3. Falls Copy noch nicht passive genug ist: als Kommentar an André zurueckmelden, kein separater Fix in dieser Phase
  </how-to-verify>
  <resume-signal>
    Antworte mit "approved" wenn alle 4 Pflicht-Tests (1, 3, 4) bestanden sind (Test 2 optional wenn keine Meeting-Session verfuegbar).
    Bei Problemen: schreibe "issue: &lt;beschreibung&gt;" — dann laeuft ein Follow-Up-Plan oder Gap-Closure.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local code -> Deploy-Pipeline | git pull + restart bringt alle Aenderungen aus Plan 01 + 02 + 03 Task 1 live |
| Deployed app -> Browser Smoke-Tests | User testet via https live die visuelle und funktionale Korrektheit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-03-01 | Denial of Service | Deploy bricht Server | mitigate | deploy.sh ist etabliertes Pattern (Phase 7 precedent). Restart ist Sekunden. Rollback-Option: `git revert <commit>` + redeploy. |
| T-07.1-03-02 | Information Disclosure | Cross-Context-Query leakt andere User | mitigate | Query filtert `CL.user_id == user_id` (Plan 01). Nochmal visuell im Browser pruefen, dass Cross-Context-Counts plausibel sind (nicht absurd hoch). |
| T-07.1-03-03 | Tampering | Alte Browser-Cache zeigt Dark-Mode | mitigate | CSS_VERSION bump invalidiert Cache. DevTools-Check auf `?v=20260418-2` ist verification. Hard-Refresh (Ctrl+Shift+R) als Fallback. |

</threat_model>

<verification>
- `grep -c "20260418-2" app.py` returns exactly 1
- `bash deploy.sh` exits with 0
- Browser shows `nerve.css?v=20260418-2` in DevTools Network-Tab
- /session/<id> fuer alle 3 Session-Typen liefert HTTP 200 (pruefbar via `curl -I` oder DevTools)
- User bestaetigt visuellen Smoke-Test fuer Cold Call + Training (Meeting optional)
- Cross-Context-Badge sichtbar bei mindestens einer Live-Session-Recommendation ODER dokumentiert als "kein Case verfuegbar"
</verification>

<success_criteria>
- CSS_VERSION ist '20260418-2' in app.py und im Deployed-Stack
- Cache-Bust-Query-Param `?v=20260418-2` erscheint in Browser-Source
- Alle 3 Session-Typen rendern /session/<id> ohne 500 und mit MAIN DESIGN Optik
- User approved nach visuellem Smoke-Test
- Deploy-Check (RULE-03) erfuellt
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-03-SUMMARY.md`
</output>
