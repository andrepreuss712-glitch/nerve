---
phase: 08.19.1-schema-realitaet-kalibrierung-extra-forbid
plan: "03"
subsystem: database
tags: [pydantic, schema, migration, profile, python]

# Dependency graph
requires:
  - phase: 08.19.1-02
    provides: ProfileSchema kalibriert mit einwaende/phasen top-level; BasisSchema Dead Fields entfernt
provides:
  - _migrate_profile_data() mit v2->v3 Migrationsstufe
  - D-02 Merge-Semantik (absent/null/empty edge cases) fuer einwaende + phasen
  - fragen-Key-Removal (D-05) in v2->v3
  - branche top-level Removal (D-01) in v2->v3
  - schema_version=3 nach Migration
  - Idempotenz: schema_version>=3 Profile unveraendert
affects:
  - 08.19.1-04
  - 08.19.1-05
  - routes/profiles.py (lazy migration im Read-Pfad)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "D-02 Merge-Semantik: __MISSING__-Sentinel unterscheidet absent/null von leerem Array"
    - "basis-Dict in-place pop + Rueckschreiben: leeres basis-Dict bleibt erhalten fuer Sub-Schema-Defaults"
    - "v2->v3 Block nach v1->v2 Block in gleicher Funktion — v1-Profile durchlaufen beide Stufen"

key-files:
  created: []
  modified:
    - services/profile_schema.py

key-decisions:
  - "Idempotenz-Guard auf >= 3 angehoben (war >= 2) — v2-Profile durchlaufen jetzt v3-Block"
  - "__MISSING__-Sentinel fuer einwaende/phasen: unterscheidet absent/null (merge) von leerem Array (nicht mergen)"
  - "basis-Dict nach einwaende/phasen-Pop zurueckgeschrieben — leeres {} bleibt (kein KeyError bei Sub-Schema)"
  - "_migration_profile_id-Key als optionaler Kontext fuer Audit-Log (wird von Plan-04 Batch-Migration gesetzt)"

patterns-established:
  - "Multi-Stage-Migration: if version < N Guard pro Stufe, version-Bump am Ende jeder Stufe"

requirements-completed:
  - TASK-3
  - TASK-6

# Metrics
duration: 10min
completed: 2026-04-29
---

# Phase 08.19.1 Plan 03: v2->v3 Migration Summary

**`_migrate_profile_data()` erweitert um v2->v3 Stufe mit D-02 Merge-Semantik: einwaende/phasen upward-merge aus basis.*, fragen+branche-Removal, schema_version=3, Idempotenz per >= 3 Guard — alle 8 Assertions bestanden**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-29
- **Completed:** 2026-04-29
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Idempotenz-Guard von `>= 2` auf `>= 3` angehoben — v2-Profile durchlaufen jetzt die neue v3-Migrationsstufe
- v2->v3 Block implementiert mit exakter D-02 Semantik (absent/null → upward-merge; [] → User-Intent respektieren)
- einwaende + phasen je unabhaengig aus basis.* nach top-level hochgezogen wenn top-level fehlt oder null
- fragen-Key entfernt (D-05) und branche top-level entfernt (D-01)
- basis-Dict nach pops zurueckgeschrieben; leeres {} bleibt erhalten (Sub-Schema-Defaults greifen, kein KeyError)
- [Schema]-Audit-Log mit genau den ausgefuehrten Schritten (nur tatsaechliche Aenderungen gelistet)
- Alle 8 Python-Assertions bestanden, basis-Retention-Check OK

## Task Commits

1. **Task 1: v2->v3 Migration mit D-02 Semantik** — `2808385` (feat)

## Files Created/Modified

- `services/profile_schema.py` — Idempotenz-Guard auf >= 3; v2->v3 Block nach schema_version=2; Docstring aktualisiert

## Decisions Made

- `__MISSING__`-Sentinel fuer einwaende/phasen: `daten.get('einwaende', '__MISSING__')` unterscheidet "Key fehlt" von "Key ist leer-Array" — kritisch fuer D-02 Semantik
- basis-Dict wird nach dem pops immer zurueckgeschrieben (auch wenn leer), damit Sub-Schema-Defaults greifen
- `_migration_profile_id` als optionaler Kontext-Key fuer Audit-Log — Plan-04 kann diesen setzen bevor _migrate_profile_data() aufruft

## Deviations from Plan

None — Plan exakt wie spezifiziert ausgefuehrt. Code aus dem Plan-Dokument direkt uebernommen, alle Acceptance Criteria verifiziert.

## Issues Encountered

None — alle 8 Tests auf Anhieb bestanden. basis-Retention-Check OK.

## Known Stubs

None — keine Placeholder-Werte. Migration ist vollstaendig implementiert.

## Threat Flags

None — keine neuen Sicherheits-relevanten Surfaces eingefuehrt. Funktion operiert rein in-memory auf dict, kein DB-Zugriff, kein Netzwerk.

## Next Phase Readiness

- Plan-04 (DB-Level Batch-Migration) kann jetzt alle Profile per `_migrate_profile_data()` auf v3 migrieren
- `_migration_profile_id`-Key-Konvention vorbereitet fuer Batch-Migration-Audit-Log
- v1-Profile durchlaufen beide Stufen (v1->v2 und v2->v3) in einem Aufruf — korrekt, keine Doppelverarbeitung

---
*Phase: 08.19.1-schema-realitaet-kalibrierung-extra-forbid*
*Completed: 2026-04-29*
