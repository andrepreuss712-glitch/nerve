---
phase: 08.19.1
plan: 04
type: execute
wave: 3
depends_on:
  - 08.19.1-02
  - 08.19.1-03
files_modified:
  - app.py
autonomous: true
requirements:
  - TASK-4
  - TASK-6
must_haves:
  truths:
    - "Alle Production-Profile haben schema_version=3 nach der Batch-Migration"
    - "Kein Profil hat noch fragen-Key in daten-JSON"
    - "Kein Profil hat noch branche top-level in daten-JSON"
    - "Profile mit basis.einwaende haben diese nach top-level verschoben"
    - "Batch-Migration ist idempotent (zweimaliges Ausfuehren aendert nichts)"
    - "Migration-Log zeigt [Schema]-Audit-Eintraege pro Profil"
  artifacts:
    - path: "app.py"
      provides: "DB-Level Batch-Migration aller Profile auf v3 in _migrate()"
      contains: "v2->v3"
  key_links:
    - from: "app.py _migrate()"
      to: "_migrate_profile_data()"
      via: "Batch-Aufruf mit _migration_profile_id Kontext"
      pattern: "_migrate_profile_data"
    - from: "app.py _migrate()"
      to: "Profile.daten"
      via: "JSON-Schreib-Commit pro Profil"
      pattern: "profile.daten"
---

<objective>
DB-Level Batch-Migration aller Production-Profile auf schema_version=3 in app.py `_migrate()`. Ruft `_migrate_profile_data()` (Plan-03) fuer jedes Profil auf, schreibt aktualisertes JSON in `Profile.daten`, committed pro Batch. Idempotent: Profile mit schema_version>=3 werden uebersprungen.

Purpose: Lazy-Migration (routes/profiles.py) migriert nur beim Lesen — Batch-Migration stellt sicher dass alle Profile vor extra='forbid'-Aktivierung (Plan-05) bereits v3 sind. Gleicher Ansatz wie Phase 08.19 v1→v2 Batch-Migration.

Output: _migrate()-Erweiterung in app.py die beim naechsten App-Start alle v2-Profile auf v3 hebt.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.1-schema-realitaet-kalibrierung-extra-forbid\08.19.1-CONTEXT.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\PROJECT.md
@C:\Users\andre\dev\salesnerve\app.py
@C:\Users\andre\dev\salesnerve\services\profile_schema.py
@C:\Users\andre\dev\salesnerve\database\models.py

<interfaces>
<!-- app.py _migrate() Pattern (Zeile 123-244) — Advisory-Lock, idempotenter ALTER TABLE Stil -->
def _migrate():
    from sqlalchemy import text
    with engine.connect() as conn:
        # ... ALTER TABLE Blocks ...
        try:
            conn.execute(text("..."))
            conn.commit()
            print("[DB] Migration: ...")
        except Exception:
            pass

<!-- Profile-Model (database/models.py Zeile 125-135) -->
class Profile(Base):
    __tablename__ = 'profiles'
    id              = Column(Integer, primary_key=True)
    daten           = Column(Text)   # JSON

<!-- _migrate_profile_data() Signatur (nach Plan-03) -->
def _migrate_profile_data(daten: dict) -> dict:
    # Idempotent v1→v2→v3
    # Liest/schreibt daten.get('_migration_profile_id') fuer Audit-Log

<!-- Batch-Migration Pattern aus Phase 08.19 als Vorlage: -->
# In _migrate() wurde nach den ALTER TABLE Blocks eine Sektion eingefuegt die
# alle Profile mit schema_version < 2 auf v2 gehoben hat (analog zu diesem Plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: v2→v3 Batch-Migration-Block in app.py _migrate() einfuegen</name>
  <files>app.py</files>
  <read_first>
    - C:\Users\andre\dev\salesnerve\app.py (vollstaendig _migrate() Funktion lesen — insb. das Ende der Funktion wo bestehende Profil-Migration aus Phase 08.19 steht)
    - C:\Users\andre\dev\salesnerve\services\profile_schema.py (_migrate_profile_data Signatur nach Plan-03 — insb. _migration_profile_id Mechanismus)
  </read_first>
  <action>
In `app.py`, in der `_migrate()` Funktion, am ENDE des Funktionskörpers (vor dem schliessenden Block falls vorhanden) folgenden Block einfuegen:

```python
        # ── Phase 08.19.1: Batch-Migration alle Profile v2 -> v3 ──────────────
        # Idempotent: Profile mit schema_version >= 3 werden uebersprungen.
        # Muss NACH den ALTER TABLE Blocks laufen (kein ALTER TABLE hier — nur daten-JSON).
        # Acceptable at startup for NERVE's current profile count (<=20); revisit if >1000 profiles
        try:
            from database.db import get_session as _get_session
            from database.models import Profile as _Profile
            import json as _json
            from services.profile_schema import _migrate_profile_data as _mpd

            _db = _get_session()
            try:
                _profiles = _db.query(_Profile).all()
                _migrated_count = 0
                _skipped_count = 0
                for _p in _profiles:
                    try:
                        _daten = _json.loads(_p.daten) if _p.daten else {}
                    except Exception:
                        _daten = {}
                    _version = _daten.get('schema_version') or 1
                    if _version >= 3:
                        _skipped_count += 1
                        continue
                    # Profil-ID fuer Audit-Log in _migrate_profile_data() injizieren
                    _daten['_migration_profile_id'] = _p.id
                    _daten_migrated = _mpd(_daten)
                    # _migration_profile_id nicht in DB speichern (Hilfsvariable)
                    _daten_migrated.pop('_migration_profile_id', None)
                    _p.daten = _json.dumps(_daten_migrated, ensure_ascii=False)
                    _migrated_count += 1
                _db.commit()
                if _migrated_count > 0:
                    print(f"[Schema] Batch-Migration v2->v3: {_migrated_count} Profile migriert, {_skipped_count} uebersprungen")
                else:
                    print(f"[Schema] Batch-Migration v2->v3: alle {_skipped_count} Profile bereits auf v3")
            finally:
                _db.close()
        except Exception as _e:
            print(f"[Schema] Batch-Migration v2->v3 FEHLER (nicht kritisch): {_e}")
```

POSITIONIERUNG: Der Block muss NACH allen `ALTER TABLE`-Blocks in `_migrate()` eingefuegt werden,
aber INNERHALB von `_migrate()`. Der `with engine.connect() as conn:` Block wird dabei NICHT
verwendet — stattdessen eigene Session via `get_session()` (analog zur Profil-Zugriffs-Methode
in routes/).

FEHLERBEHANDLUNG: outer try/except schluckt alle Fehler mit print — App-Start darf nicht
blockiert werden (analog zu bestehenden except-Blocks in _migrate()).
  </action>
  <verify>
    <automated>python3 -c "
import ast, pathlib
src = pathlib.Path('C:/Users/andre/dev/salesnerve/app.py').read_text(encoding='utf-8')
assert 'v2->v3' in src or 'v2 -> v3' in src, 'Batch-Migration-Block nicht gefunden'
assert '_migration_profile_id' in src, '_migration_profile_id Injizierung fehlt'
assert 'Batch-Migration' in src, 'Batch-Migration Log fehlt'
print('app.py Verifikation OK')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "v2.*v3\|v2->v3" app.py` zeigt mindestens 1 Treffer im _migrate() Bereich
    - `grep -n "_migration_profile_id" app.py` zeigt den Injizierungs-Schritt
    - `grep -n "Batch-Migration" app.py` zeigt den print-Statement
    - `python3 -c "import app"` startet ohne SyntaxError (app.py syntaktisch korrekt)
    - Nach lokalem App-Start (`python3 app.py` fuer ein paar Sekunden): stdout enthaelt "[Schema] Batch-Migration" Zeile
  </acceptance_criteria>
  <done>Batch-Migration-Block in _migrate() eingefuegt. Beim naechsten App-Start werden alle v2-Profile auf v3 gehoben.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>DB-Level Batch-Migration v2→v3 in app.py. Beim App-Start werden alle Profile auf schema_version=3 gehoben.</what-built>
  <how-to-verify>
1. App lokal starten: `cd C:\Users\andre\dev\salesnerve && python app.py`
2. In der Startup-Ausgabe nach "[Schema]" Zeilen suchen:
   - Erwartet: "[Schema] Batch-Migration v2->v3: N Profile migriert" ODER "alle N Profile bereits auf v3"
3. App stoppen (Ctrl+C)
4. DB-Check: lokale Profile auf v3 pruefen:
   ```
   python3 -c "
   import sys, os, json
   sys.path.insert(0, 'C:/Users/andre/dev/salesnerve')
   os.chdir('C:/Users/andre/dev/salesnerve')
   from database.db import get_session
   from database.models import Profile
   db = get_session()
   for p in db.query(Profile).all():
       d = json.loads(p.daten) if p.daten else {}
       print(f'Profil {p.id}: schema_version={d.get(\"schema_version\")}')
   db.close()
   "
   ```
   Erwartet: ALLE Profile zeigen schema_version=3
5. Zweiter Start: erneut starten — "[Schema] alle N Profile bereits auf v3" erwartet (Idempotenz)
  </how-to-verify>
  <resume-signal>Schreibe "ok" wenn alle Profile schema_version=3 zeigen und Idempotenz bestaetigt. Schreibe "fehler: [beschreibung]" wenn etwas nicht stimmt.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| _migrate() → Production-Profile-Daten | Batch-Migration modifiziert alle Profile.daten — Fehler koennen Profil-Daten beschaedigen |
| _migration_profile_id → DB | Muss aus _daten_migrated gepoppt werden bevor Profile.daten geschrieben wird |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigation |
|-----------|-----------|------------|-------------|------------|
| T-08191-12 | Tampering | Batch-Migration DB-Write | mitigate | `_migrate_profile_data()` ist idempotent und in Plan-03 vollstaendig getestet; outer try/except verhindert App-Start-Blockierung |
| T-08191-13 | Denial of Service | App-Start blockiert bei Migration-Fehler | mitigate | Outer try/except mit print (nicht raise) — App startet auch bei Migrations-Fehler |
| T-08191-14 | Tampering | _migration_profile_id in DB-Column | mitigate | Explizites `.pop('_migration_profile_id', None)` vor json.dumps |
| T-08191-15 | Information Disclosure | Profil-Daten in Log | accept | print([Schema]) loggt nur Profil-ID und Zaehler, kein Profil-Content |
</threat_model>

<verification>
```bash
cd C:\Users\andre\dev\salesnerve

# Syntaxcheck
python3 -m py_compile app.py && echo "Syntaxcheck OK"

# Batch-Migration Block vorhanden
grep -n "Batch-Migration\|v2.*v3" app.py

# Nach App-Start: alle Profile v3
python3 -c "
import sys, os, json
sys.path.insert(0, '.')
os.chdir('C:/Users/andre/dev/salesnerve')
from database.db import get_session
from database.models import Profile
db = get_session()
versions = [json.loads(p.daten).get('schema_version') if p.daten else 0 for p in db.query(Profile).all()]
db.close()
print('Versions:', versions)
assert all(v >= 3 for v in versions), f'Nicht alle Profile v3: {versions}'
print('OK: Alle Profile schema_version >= 3')
"
```

Cross-AI Review required before merge — /gsd-review --phase 08.19.1
</verification>

<success_criteria>
- Batch-Migration-Block in app.py _migrate() eingefuegt
- App-Start zeigt "[Schema] Batch-Migration" Log-Zeile
- Alle lokalen Profile haben schema_version=3 nach erstem Start
- Zweiter Start zeigt "alle N Profile bereits auf v3" (Idempotenz)
- Checkpoint: Human bestaetigt schema_version=3 fuer alle Profile
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.19.1-schema-realitaet-kalibrierung-extra-forbid/08.19.1-04-SUMMARY.md` erstellen.
</output>
