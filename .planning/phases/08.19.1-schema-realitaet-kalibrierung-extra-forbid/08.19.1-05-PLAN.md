---
phase: 08.19.1
plan: 05
type: execute
wave: 4
depends_on:
  - 08.19.1-04
files_modified:
  - services/profile_schema.py
  - tests/test_profile_schema_v3.py
autonomous: false
requirements:
  - TASK-7
  - TASK-8
  - TASK-9
must_haves:
  truths:
    - "ProfileSchema hat extra='forbid' (ConfigDict(extra='forbid'))"
    - "ALLE Sub-Schemas haben extra='forbid'"
    - "Alle Production-Profile (nach v3-Batch-Migration) durchlaufen model_validate(strict=True) ohne Fehler"
    - "Test-Suite mit 6 expliziten Validation-Tests existiert"
    - "Test-Suite laeuft ohne Fehler: pytest tests/test_profile_schema_v3.py -v"
    - "routes/profiles.py bearbeiten()-POST bleibt funktional (extra='forbid' schlaegt kein valid Profile weg)"
  artifacts:
    - path: "services/profile_schema.py"
      provides: "ProfileSchema + alle Sub-Schemas mit extra='forbid'"
      contains: "ConfigDict(extra='forbid')"
    - path: "tests/test_profile_schema_v3.py"
      provides: "Test-Suite: model_validate gegen alle lokalen Profile + edge cases"
  key_links:
    - from: "ProfileSchema"
      to: "ConfigDict(extra='forbid')"
      via: "model_config Attribut"
      pattern: "extra='forbid'"
    - from: "tests/test_profile_schema_v3.py"
      to: "ProfileSchema.model_validate"
      via: "pytest-Tests mit echten lokalen Profilen"
      pattern: "model_validate"
---

<objective>
extra='forbid' auf ProfileSchema Top-Level und ALLE Sub-Schemas aktivieren. Test-Suite erstellen die alle lokalen Profile (nach v3-Migration) gegen ProfileSchema mit strict=True validiert. Wenn alle Tests gruenen, ist die Phase abgeschlossen.

Purpose: Das war der urspruengliche Hotfix-Schuldzettel aus Phase 08.19 — extra='ignore' war ein temporaerer Sicherheitspuffer. Jetzt ist das Schema kalibriert (Plan-02) und alle Profile sind migriert (Plan-04). extra='forbid' kann sicher aktiviert werden.

Output: production-reifes strict-Schema + gruene Test-Suite.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.1-schema-realitaet-kalibrierung-extra-forbid\08.19.1-CONTEXT.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\PROJECT.md
@C:\Users\andre\dev\salesnerve\services\profile_schema.py
@C:\Users\andre\dev\salesnerve\routes\profiles.py

<interfaces>
<!-- Alle Sub-Schemas die extra='forbid' bekommen (D-04) -->
Betroffene Klassen in profile_schema.py:
  BasisSchema, ZielgruppeSchema, ZielkundeSchema, ValueSchema,
  EinwandDetailSchema, KiSchema, SchmerzenSchema, MetaSchema,
  ProfileSchema (Top-Level Write-Schema)

Nicht betroffene Klassen (bleiben extra='ignore' — Permissive Read-Pfad):
  _ZielgruppeReadSchema, _KiReadSchema, _ZielkundeReadSchema, _BasisReadSchema,
  _SchmerzenReadSchema, _MetaReadSchema, _ValueReadSchema, ProfileReadSchema

<!-- Pydantic v2 ConfigDict Syntax (aus CLAUDE.md/CONTEXT.md) -->
# RICHTIG (Pydantic v2):
model_config = ConfigDict(extra='forbid')

# FALSCH (Pydantic v1 Syntax — nicht verwenden):
class Config:
    extra = 'forbid'

<!-- routes/profiles.py bearbeiten() Write-Validation (Zeile 219) -->
ProfileSchema.model_validate(_daten_neue)
# Wird NACH _migrate_profile_data() aufgerufen (v3-Migration laeuft lazy hier)
# Nach Plan-04 Batch-Migration: alle Profile schon v3 → extra='forbid' schlaegt nichts weg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extra='forbid' auf alle Write-Schemas aktivieren</name>
  <files>services/profile_schema.py</files>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\profile_schema.py (vollstaendig — alle model_config Zeilen identifizieren)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.19.1-schema-realitaet-kalibrierung-extra-forbid\08.19.1-CONTEXT.md (D-04: vollstaendige Liste betroffener Sub-Schemas)
  </read_first>
  <behavior>
    - Test 1: ProfileSchema.model_validate({'schema_version': 3, 'unbekannter_key': 'x'}) loest ValidationError aus
    - Test 2: BasisSchema.model_validate({'unbekannter_key': 'x'}) loest ValidationError aus
    - Test 3: KiSchema.model_validate({'unbekannter_key': 'x'}) loest ValidationError aus
    - Test 4: ProfileReadSchema.model_validate({'unbekannter_key': 'x', 'schema_version': 3}) laeuft OHNE Fehler (Read-Schema bleibt permissiv)
    - Test 5: Minimales gueltiges ProfileSchema ({'schema_version': 3}) validiert OHNE Fehler
  </behavior>
  <action>
In services/profile_schema.py ALLE folgenden Klassen von `extra='ignore'` auf `extra='forbid'` aendern:

1. BasisSchema: `model_config = ConfigDict(extra='forbid')`
2. ZielgruppeSchema: `model_config = ConfigDict(extra='forbid')`
3. ZielkundeSchema: `model_config = ConfigDict(extra='forbid')`
4. ValueSchema: `model_config = ConfigDict(extra='forbid')`
5. EinwandDetailSchema: `model_config = ConfigDict(extra='forbid')`
6. KiSchema: `model_config = ConfigDict(extra='forbid')`
7. SchmerzenSchema: `model_config = ConfigDict(extra='forbid')`
8. MetaSchema: `model_config = ConfigDict(extra='forbid')`
9. ProfileSchema: `model_config = ConfigDict(extra='forbid')`

NICHT aendern (bleiben extra='ignore'):
- _ZielgruppeReadSchema, _KiReadSchema, _ZielkundeReadSchema, _BasisReadSchema,
  _SchmerzenReadSchema, _MetaReadSchema, _ValueReadSchema, ProfileReadSchema

Modul-Docstring UPDATE:
  Zeile 9 (`# ProfileSchema (extra='forbid')`) ist bereits korrekt.
  Kommentar in ProfileSchema-Klasse anpassen:
    """Write-Schema: strict (extra='forbid'). Phase 08.19.1: Schema kalibriert,
    alle Profile auf v3 migriert. model_validate() gegen alle Production-Profile
    ohne Fehler verifiziert.
    """

NACH der Aenderung — Smoke-Test MUSS vor dem Commit ausgefuehrt werden:
  python3 -c "
  import sys, os, json
  sys.path.insert(0, '.')
  from services.profile_schema import ProfileSchema, _migrate_profile_data
  from database.db import get_session
  from database.models import Profile
  db = get_session()
  try:
      errors = []
      for p in db.query(Profile).all():
          try:
              d = json.loads(p.daten) if p.daten else {}
          except Exception:
              d = {}
          d = _migrate_profile_data(d)
          try:
              ProfileSchema.model_validate(d)
          except Exception as e:
              errors.append(f'Profil {p.id}: {e}')
      if errors:
          print('VALIDATION ERRORS:')
          for e in errors: print(e)
          exit(1)
      else:
          print(f'OK: Alle {db.query(Profile).count()} Profile validieren gegen ProfileSchema(extra=forbid)')
  finally:
      db.close()
  "

Falls dieser Smoke-Test Fehler zeigt: die fehlerhaften Felder identifizieren und entweder:
  a) In ProfileSchema ergaenzen (falls legitimes Production-Feld das in KEY-FINDINGS.md auftaucht)
  b) In _migrate_profile_data() v3-Block poppen (falls bekanntes Transitional-Feld)
KEINE extra='ignore' als Fallback zurueck setzen.
  </action>
  <verify>
    <automated>python3 -c "
import sys, os
sys.path.insert(0, 'C:/Users/andre/dev/salesnerve')
os.chdir('C:/Users/andre/dev/salesnerve')
from services.profile_schema import (ProfileSchema, BasisSchema, ZielgruppeSchema,
    ZielkundeSchema, ValueSchema, EinwandDetailSchema, KiSchema, SchmerzenSchema,
    MetaSchema, ProfileReadSchema)
from pydantic import ValidationError

# Test 1: ProfileSchema wirft bei unbekanntem Key
try:
    ProfileSchema.model_validate({'schema_version': 3, 'unbekannter_key': 'x'})
    raise AssertionError('Test1 FEHLER: kein ValidationError bei unbekanntem Key')
except ValidationError:
    pass

# Test 2: BasisSchema strict
try:
    BasisSchema.model_validate({'unbekannter_key': 'x'})
    raise AssertionError('Test2 FEHLER: BasisSchema kein Fehler bei unbekanntem Key')
except ValidationError:
    pass

# Test 3: ProfileReadSchema bleibt permissiv
ProfileReadSchema.model_validate({'unbekannter_key': 'x', 'schema_version': 3})

# Test 4: Minimales gueltiges ProfileSchema
ProfileSchema.model_validate({'schema_version': 3})

print('Alle 4 extra-forbid Tests bestanden.')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "extra='forbid'" services/profile_schema.py` gibt mindestens 9 zurueck (alle Write-Schemas)
    - `grep -c "extra='ignore'" services/profile_schema.py` gibt hoechstens 8 zurueck (nur Read-Sub-Schemas + ProfileReadSchema)
    - Alle 4 Python-Assertions bestehen
    - Smoke-Test: alle lokalen Profile validieren gegen ProfileSchema ohne Fehler
  </acceptance_criteria>
  <done>extra='forbid' auf allen 9 Write-Schemas aktiviert. Lokale Profile validieren ohne Fehler.</done>
</task>

<task type="auto">
  <name>Task 2: Test-Suite tests/test_profile_schema_v3.py erstellen</name>
  <files>tests/test_profile_schema_v3.py</files>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\profile_schema.py (nach Task 1 — aktueller Stand mit extra='forbid')
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Test-Qualitaets-Regel: Integration-Assertion vs. Source-Presence-False-Green)
  </read_first>
  <action>
Neue Datei: tests/test_profile_schema_v3.py

WICHTIG: Alle Tests nach CLAUDE.md Test-Qualitaets-Regel:
- Funktions-Calls mit Assertions auf Rueckgabewerte (Integration-Assertion)
- KEIN `inspect.getsource()`, kein `open().read()`, kein grep auf Source-Text

```python
"""
tests/test_profile_schema_v3.py
Phase 08.19.1: Verifikation ProfileSchema v3 (extra='forbid') + _migrate_profile_data v2->v3

Test-Kategorien:
  A) extra='forbid' Enforcement (unbekannte Keys werden abgelehnt)
  B) _migrate_profile_data v2->v3 Semantik (D-02 Merge-Logik)
  C) Lokale Profil-Validierung (alle DB-Profile durchlaufen ProfileSchema ohne Fehler)
"""
import json
import os
import sys

import pytest
from pydantic import ValidationError

# Sicherstellen dass das Projektverzeichnis im Pfad ist
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from services.profile_schema import (
    ProfileSchema,
    BasisSchema,
    KiSchema,
    ProfileReadSchema,
    _migrate_profile_data,
)


# ── A) extra='forbid' Enforcement ────────────────────────────────────────────

class TestExtraForbid:
    def test_profileschema_rejects_unknown_toplevel_key(self):
        """ProfileSchema muss unbekannte Top-Level-Keys ablehnen."""
        with pytest.raises(ValidationError) as exc_info:
            ProfileSchema.model_validate({'schema_version': 3, 'unbekannter_key': 'test'})
        assert 'unbekannter_key' in str(exc_info.value)

    def test_basisschema_rejects_unknown_key(self):
        """BasisSchema muss unbekannte Keys ablehnen (Sub-Schema strict)."""
        with pytest.raises(ValidationError):
            BasisSchema.model_validate({'unbekannter_basis_key': 'x'})

    def test_kischema_rejects_unknown_key(self):
        """KiSchema muss unbekannte Keys ablehnen."""
        with pytest.raises(ValidationError):
            KiSchema.model_validate({'anrede': 'Sie', 'unbekannt': 'x'})

    def test_profilereadschema_accepts_unknown_key(self):
        """ProfileReadSchema bleibt permissiv (extra='ignore') — Read-Pfad."""
        result = ProfileReadSchema.model_validate({
            'schema_version': 3,
            'unbekannter_key': 'x',
            'noch_ein_key': [1, 2, 3],
        })
        assert result.schema_version == 3

    def test_minimal_valid_profileschema(self):
        """Minimales gueltiges ProfileSchema (nur schema_version) muss durchgehen."""
        result = ProfileSchema.model_validate({'schema_version': 3})
        assert result.schema_version == 3

    def test_profileschema_with_known_keys_validates(self):
        """Vollstaendiges gueltiges Profil-Dict muss ohne Fehler validieren."""
        valid_data = {
            'schema_version': 3,
            'einwaende': [{'einwand': 'Zu teuer', 'gegenargument': 'ROI'}],
            'phasen': [{'name': 'Opening'}],
            'basis': {'unternehmen': 'Testfirma', 'produktbeschreibung': 'SaaS'},
            'ki': {'anrede': 'Sie', 'ton': 'professionell'},
        }
        result = ProfileSchema.model_validate(valid_data)
        assert result.schema_version == 3
        assert len(result.einwaende) == 1


# ── B) _migrate_profile_data v2→v3 Semantik ──────────────────────────────────

class TestMigrateV3:
    def test_basis_einwaende_upward_merge_when_toplevel_absent(self):
        """basis.einwaende wird nach top-level verschoben wenn top-level fehlt."""
        daten = {'schema_version': 2, 'basis': {'einwaende': ['Test-Einwand']}}
        result = _migrate_profile_data(daten)
        assert result['einwaende'] == ['Test-Einwand']
        assert 'einwaende' not in result.get('basis', {})

    def test_basis_einwaende_not_overwritten_when_toplevel_empty_list(self):
        """Leeres top-level einwaende=[] bleibt (User-Intent 'alles geloescht')."""
        daten = {'schema_version': 2, 'basis': {'einwaende': ['A']}, 'einwaende': []}
        result = _migrate_profile_data(daten)
        assert result['einwaende'] == []

    def test_basis_einwaende_upward_merge_when_toplevel_null(self):
        """top-level einwaende=None gilt als nicht vorhanden — basis.* wird hochgezogen."""
        daten = {'schema_version': 2, 'basis': {'einwaende': ['B']}, 'einwaende': None}
        result = _migrate_profile_data(daten)
        assert result['einwaende'] == ['B']

    def test_phasen_upward_merge(self):
        """basis.phasen wird nach top-level verschoben wenn top-level fehlt."""
        daten = {'schema_version': 2, 'basis': {'phasen': [{'name': 'Opening'}]}}
        result = _migrate_profile_data(daten)
        assert result['phasen'] == [{'name': 'Opening'}]
        assert 'phasen' not in result.get('basis', {})

    def test_fragen_key_removed(self):
        """fragen-Key wird aus daten entfernt (war gesetzt auf [] in 08.19.3)."""
        daten = {'schema_version': 2, 'fragen': []}
        result = _migrate_profile_data(daten)
        assert 'fragen' not in result

    def test_branche_toplevel_removed(self):
        """branche top-level wird entfernt (lebt auf Profile.branche DB-Column)."""
        daten = {'schema_version': 2, 'branche': 'saas_b2b'}
        result = _migrate_profile_data(daten)
        assert 'branche' not in result

    def test_schema_version_bumped_to_3(self):
        """schema_version wird auf 3 gesetzt."""
        daten = {'schema_version': 2}
        result = _migrate_profile_data(daten)
        assert result['schema_version'] == 3

    def test_idempotent_v3(self):
        """v3-Profile werden unveraendert zurueckgegeben (Idempotenz)."""
        daten = {'schema_version': 3, 'einwaende': ['X'], 'phasen': [{'name': 'Test'}]}
        result = _migrate_profile_data(daten)
        assert result == daten

    def test_toplevel_einwaende_wins_over_basis(self):
        """Wenn top-level einwaende vorhanden: basis.einwaende wird gedroppt (top-level gewinnt)."""
        daten = {
            'schema_version': 2,
            'einwaende': ['Top-Level'],
            'basis': {'einwaende': ['Basis-Wert']},
        }
        result = _migrate_profile_data(daten)
        assert result['einwaende'] == ['Top-Level']
        assert 'einwaende' not in result.get('basis', {})


# ── C) Lokale Profil-Validierung ──────────────────────────────────────────────

class TestLocalProfileValidation:
    """Alle lokalen Profile durchlaufen nach Migration ProfileSchema ohne Fehler.

    HINWEIS: Diese Tests erfordern eine lokale DB-Verbindung.
    Werden bei fehlendem DB-File uebersprungen (pytest.mark.skipif).
    """

    @pytest.fixture
    def local_profiles(self):
        """Laedt alle lokalen Profile aus der Dev-DB."""
        db_path = os.path.join(os.path.dirname(__file__), '..', 'database', 'salesnerve.db')
        if not os.path.exists(db_path):
            pytest.skip("Lokale DB nicht gefunden — Test uebersprungen")
        from database.db import get_session
        from database.models import Profile
        db = get_session()
        try:
            profiles = db.query(Profile).all()
            result = []
            for p in profiles:
                try:
                    daten = json.loads(p.daten) if p.daten else {}
                except Exception:
                    daten = {}
                result.append((p.id, p.name, daten))
            return result
        finally:
            db.close()

    def test_all_local_profiles_validate_after_migration(self, local_profiles):
        """Alle lokalen Profile durchlaufen ProfileSchema.model_validate() nach v3-Migration."""
        errors = []
        for pid, pname, daten in local_profiles:
            migrated = _migrate_profile_data(daten.copy())
            try:
                ProfileSchema.model_validate(migrated)
            except ValidationError as e:
                errors.append(f"Profil {pid} ({pname[:30]}): {e.error_count()} Fehler — {e.errors()[0]}")
        assert not errors, f"Validation-Fehler in {len(errors)} Profilen:\n" + "\n".join(errors)

    def test_all_local_profiles_have_v3_after_migration(self, local_profiles):
        """Nach Migration haben alle Profile schema_version=3."""
        for pid, pname, daten in local_profiles:
            migrated = _migrate_profile_data(daten.copy())
            assert migrated.get('schema_version') == 3, (
                f"Profil {pid} hat schema_version={migrated.get('schema_version')} nach Migration"
            )
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_profile_schema_v3.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_profile_schema_v3.py -v` laeuft ohne Fehler
    - Alle Tests in Klassen TestExtraForbid und TestMigrateV3 zeigen PASSED
    - TestLocalProfileValidation::test_all_local_profiles_validate_after_migration zeigt PASSED (oder SKIPPED wenn DB-Pfad nicht Standard)
    - Keine Source-Presence-Tests (kein `inspect.getsource`, kein `open().read()`)
    - `pytest tests/test_profile_schema_v3.py --co -q` zeigt mindestens 15 Test-Items
  </acceptance_criteria>
  <done>Test-Suite mit 15+ Tests existiert, gruent komplett durch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    extra='forbid' auf ProfileSchema + alle Sub-Schemas aktiviert.
    Test-Suite tests/test_profile_schema_v3.py erstellt und gruen.
    Alle lokalen Profile (v3 nach Batch-Migration) validieren ohne Fehler.
  </what-built>
  <how-to-verify>
1. Test-Suite ausfuehren:
   ```
   cd C:\Users\andre\dev\salesnerve
   python3 -m pytest tests/test_profile_schema_v3.py -v
   ```
   Erwartet: Alle Tests PASSED, keine FAILED

2. extra='forbid' Enforcement verifizieren:
   ```
   python3 -c "
   from services.profile_schema import ProfileSchema
   from pydantic import ValidationError
   try:
       ProfileSchema.model_validate({'schema_version': 3, 'alien_key': 'x'})
       print('FEHLER: kein ValidationError!')
   except ValidationError as e:
       print('OK: extra=forbid wirft ValidationError:', e.error_count(), 'Fehler')
   "
   ```

3. Profil-Editor Smoke-Test (HTTP):
   - App starten: `python app.py`
   - Browser: http://localhost:5000/profiles
   - Ein Profil im Editor oeffnen und speichern
   - Erwartung: Kein 400/500 Fehler, Flash "Profil gespeichert."

4. Optionaler Production-Check (SSH):
   ```
   ssh root@getnerve.app "cd /opt/nerve && python3 -c \"
   import json
   import sys
   sys.path.insert(0, '.')
   from services.profile_schema import ProfileSchema, _migrate_profile_data
   from database.db import get_session
   from database.models import Profile
   db = get_session()
   errors = []
   for p in db.query(Profile).all():
       d = json.loads(p.daten) if p.daten else {}
       d = _migrate_profile_data(d)
       try:
           ProfileSchema.model_validate(d)
       except Exception as e:
           errors.append(f'Profil {p.id}: {e}')
   db.close()
   print('Fehler:', errors if errors else 'keine')
   \""
   ```
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn Tests gruen sind, Profil-Editor funktioniert, und extra='forbid' Validation wirft korrekt Fehler. Schreibe "fehler: [beschreibung]" wenn etwas nicht stimmt.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| extra='forbid' → bearbeiten() POST | Wenn ein Client-POST unbekannte Keys schickt, gibt es jetzt HTTP 400 (vorher ignoriert) |
| routes/profiles.py bearbeiten() → ValidationError | Already handled: _ve catch mit flash() + redirect (Zeile 220-225) |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigation |
|-----------|-----------|------------|-------------|------------|
| T-08191-16 | Tampering | extra='forbid' → false-positive 400 | mitigate | Plan-02 kalibriert Schema auf echte Editor-Output-Struktur; Plan-04 Batch-Migration bereinigt Profil-JSONs; Smoke-Test bestaetigt alle Profile vor Aktivierung |
| T-08191-17 | Elevation of Privilege | Sub-Schema extra='forbid' | accept | Sub-Schemas koennen keine Privilege-Escalation erleiden; forbid verhindert Schmuggeln von undokumentierten Felder in DB |
| T-08191-18 | Denial of Service | Test-Suite DB-Abhaengigkeit | accept | DB-Fixture skippt Test falls DB nicht gefunden (kein CI-Breaker) |
| T-08191-19 | Repudiation | Keine Audit-Spur fuer Schema-Aktivierung | accept | Git-Commit mit Phasennummer liefert Audit-Spur; _migrate() Print-Log dokumentiert Migration-Zeitpunkt |
</threat_model>

<verification>
```bash
cd C:\Users\andre\dev\salesnerve

# Test-Suite gruen
python3 -m pytest tests/test_profile_schema_v3.py -v

# extra='forbid' Count korrekt
grep -c "extra='forbid'" services/profile_schema.py  # >= 9

# Read-Schemas noch extra='ignore'
grep -c "extra='ignore'" services/profile_schema.py  # >= 7 (nur _*ReadSchema + ProfileReadSchema)

# Profile-Editor validiert weiterhin (kein 400 bei legitimem Speichern)
# -> Manueller Smoke-Test im Checkpoint
```

Cross-AI Review required before merge — /gsd-review --phase 08.19.1
</verification>

<success_criteria>
- extra='forbid' auf 9 Write-Schemas aktiv
- Test-Suite mit 15+ Tests: alle PASSED
- Alle lokalen Profile validieren gegen ProfileSchema nach Migration
- Profil-Editor bearbeiten()-POST funktioniert ohne 400-Fehler
- Human-Checkpoint bestaetigt Funktionalitaet
- Phase-08.19.1-Schuldzettel bezahlt: kein extra='ignore' auf Write-Schemas
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.19.1-schema-realitaet-kalibrierung-extra-forbid/08.19.1-05-SUMMARY.md` erstellen.
</output>
