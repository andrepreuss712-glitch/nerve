---
phase: 08.19.1
plan: 02
type: execute
wave: 2
depends_on:
  - 08.19.1-01
files_modified:
  - services/profile_schema.py
autonomous: true
requirements:
  - TASK-3
  - TASK-4
  - TASK-5
must_haves:
  truths:
    - "ProfileSchema enthaelt einwaende und phasen als top-level-Felder (nicht nur in BasisSchema)"
    - "BasisSchema enthaelt KEINE einwaende und phasen mehr (Dead Fields entfernt)"
    - "ProfileReadSchema enthaelt KEIN fragen-Feld mehr"
    - "profile_faqs.mode-Feld ist im Schema-Universum dokumentiert (Kommentar)"
    - "Alle Sub-Schemas haben model_config = ConfigDict(extra='ignore') (noch nicht 'forbid' — kommt Plan-05)"
    - "schema_version Default ist noch 2 (wird in Plan-03 auf 3 angehoben)"
  artifacts:
    - path: "services/profile_schema.py"
      provides: "Kalibriertes ProfileSchema mit korrekter Top-Level-Struktur"
      contains: "einwaende: List[dict] = []"
    - path: "services/profile_schema.py"
      provides: "BasisSchema ohne einwaende/phasen Dead Fields"
  key_links:
    - from: "ProfileSchema"
      to: "BasisSchema"
      via: "Felder einwaende/phasen NICHT mehr in BasisSchema"
      pattern: "class BasisSchema"
    - from: "ProfileSchema"
      to: "einwaende, phasen"
      via: "Top-Level-Felder in ProfileSchema body"
      pattern: "einwaende: List\\[dict\\]"
---

<objective>
services/profile_schema.py auf Production-Realitaet kalibrieren: einwaende + phasen als Top-Level-Felder in ProfileSchema legalisieren, Dead Fields aus BasisSchema entfernen, fragen-Key aus ProfileReadSchema entfernen. Sub-Schemas bleiben vorerst extra='ignore' (extra='forbid' kommt erst in Plan-05 nach erfolgreicher Migration).

Purpose: Editor-Realitaet schlaegt Schema-Theorie (CLAUDE.md Nudelcode-Regel 1, D-01). Plan-01 KEY-FINDINGS.md bestaetigt die Drift-Struktur — jetzt wird das Schema der Realitaet angepasst.

Output: Kalibriertes profile_schema.py das die tatsaechliche JSON-Struktur aller Production-Profile korrekt abbildet.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.1-schema-realitaet-kalibrierung-extra-forbid\08.19.1-CONTEXT.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\PROJECT.md
@C:\Users\andre\dev\salesnerve\services\profile_schema.py
@C:\Users\andre\dev\salesnerve\.planning\audits\profile-schema-realdata-2026-04-29\KEY-FINDINGS.md

<interfaces>
<!-- Aktueller Stand BasisSchema (profile_schema.py Zeile 43-58) — wird geaendert -->
class BasisSchema(BaseModel):
    model_config = ConfigDict(extra='ignore')
    unternehmen: str = ''
    produktbeschreibung: str = ''
    branche: str = ''
    branche_kontext: str = ''
    usps: List[str] = []
    preismodell: str = ''
    konsequenz: str = ''
    eigene_formulierungen: List[str] = []
    beweise: List[str] = []
    tabu_begriffe: List[dict] = []
    zielkunden: str = ''
    einwaende: List[str] = []    # <-- WIRD ENTFERNT (Dead Field, D-01)
    phasen: List[dict] = []      # <-- WIRD ENTFERNT (Dead Field, D-01)

<!-- Aktueller Stand ProfileSchema (profile_schema.py Zeile 123-145) — wird erweitert -->
class ProfileSchema(BaseModel):
    model_config = ConfigDict(extra='ignore')
    schema_version: int = 2
    basis: BasisSchema = BasisSchema()
    ...
    einwaende_detail: List[EinwandDetailSchema] = []
    # einwaende + phasen fehlen hier noch — WERDEN HINZUGEFUEGT (D-01)

<!-- _BasisReadSchema (Zeile 177-191) — einwaende + phasen bleiben dort (Read-Pfad) -->
<!-- ProfileReadSchema (Zeile 213-234) — fragen-Feld WIRD ENTFERNT (D-05) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BasisSchema Dead-Fields entfernen + ProfileSchema Top-Level-Felder hinzufuegen</name>
  <files>services/profile_schema.py</files>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\profile_schema.py (vollstaendig lesen — alle Klassen, insb. BasisSchema, ProfileSchema, _BasisReadSchema)
    - C:\Users\andre\dev\salesnerve\.planning\audits\profile-schema-realdata-2026-04-29\KEY-FINDINGS.md (Sektion "Ungemappte Keys" — gibt es weitere Keys die ins Schema muessen?)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.19.1-schema-realitaet-kalibrierung-extra-forbid\08.19.1-CONTEXT.md (D-01 exakte Spezifikation)
  </read_first>
  <action>
Aenderung 1 — BasisSchema (per D-01: Dead Fields entfernen):
  ENTFERNEN aus BasisSchema:
    einwaende: List[str] = []
    phasen: List[dict] = []

  Kommentar hinzufuegen nach dem letzten verbleibenden Feld in BasisSchema:
    # einwaende + phasen wurden in Phase 08.19.1 nach top-level ProfileSchema verschoben
    # (Editor schreibt seit Phase 08 ausschliesslich top-level — basis.* waren Dead Fields)

Aenderung 2 — ProfileSchema (per D-01: Top-Level-Felder ergaenzen):
  Nach der Zeile `einwaende_detail: List[EinwandDetailSchema] = []` HINZUFUEGEN:
    # Phasen 08.19.1 D-01: Editor schreibt einwaende + phasen top-level (nie in basis.*)
    einwaende: List[dict] = []   # war faelschlicherweise in BasisSchema (Dead Field dort)
    phasen: List[dict] = []      # war faelschlicherweise in BasisSchema (Dead Field dort)

  HINWEIS: Typ-Aenderung fuer einwaende: BasisSchema hatte List[str], ProfileSchema bekommt
  List[dict] — der Editor schreibt seit Phase 08.19 EinwandDetail-Objekte (dicts) top-level.
  Lokale Verifizierung per KEY-FINDINGS.md: falls Production tatsaechlich List[str] fuer
  einwaende top-level zeigt, dann List[Union[str, dict]] verwenden. Im Zweifel: List[dict] = []
  (Migration in Plan-03 normalisiert ohnehin).

Aenderung 3 — Modul-Docstring anpassen:
  In der Datei-Docstring (Zeile 1-24) Kommentar ergaenzen:
    Phase 08.19.1: schema_version v2 -> v3. einwaende + phasen top-level in ProfileSchema,
    Dead Fields aus BasisSchema entfernt. extra='forbid' wird in Plan-05 aktiviert.

Aenderung 4 — _BasisReadSchema (NICHT aendern):
  _BasisReadSchema behaelt einwaende und phasen — der Read-Pfad muss alte Profile mit
  basis.einwaende/basis.phasen noch lesen koennen (Migration laeuft lazy).

Aenderung 5 — ProfileReadSchema fragen-Key entfernen (per D-05):
  Falls ProfileReadSchema ein Feld `fragen` enthaelt: entfernen.
  (08.19.3 hat fragen auf [] gesetzt, Plan-03 poppt es aus dem daten-JSON.)

Aenderung 6 — Ungemappte Keys aus KEY-FINDINGS.md:
  KEY-FINDINGS.md Sektion "## Ungemappte Keys" pruefen.
  Falls dort Keys auftauchen die NICHT in der Deferred-Liste stehen (pitch, erlaubnis,
  opener, branche top-level sind bekannte Transitional-Keys — kein Schema-Eintrag noetig):
    -> Fuer jeden echten ungemappten Key: als `Any = None` in ProfileSchema ergaenzen
       mit Kommentar "# Production-Key — via v2->v3 Migration entfernen oder behalten?"
  Falls keine echten ungemappten Keys: keine Aktion.
  </action>
  <verify>
    <automated>python3 -c "
from services.profile_schema import ProfileSchema, BasisSchema
# BasisSchema darf kein einwaende/phasen-Feld mehr haben
bs_fields = BasisSchema.model_fields
assert 'einwaende' not in bs_fields, 'BasisSchema.einwaende nicht entfernt'
assert 'phasen' not in bs_fields, 'BasisSchema.phasen nicht entfernt'
# ProfileSchema muss einwaende und phasen top-level haben
ps_fields = ProfileSchema.model_fields
assert 'einwaende' in ps_fields, 'ProfileSchema.einwaende fehlt'
assert 'phasen' in ps_fields, 'ProfileSchema.phasen fehlt'
print('OK: ProfileSchema kalibriert')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from services.profile_schema import BasisSchema; assert 'einwaende' not in BasisSchema.model_fields"` erfolgreich
    - `python3 -c "from services.profile_schema import BasisSchema; assert 'phasen' not in BasisSchema.model_fields"` erfolgreich
    - `python3 -c "from services.profile_schema import ProfileSchema; assert 'einwaende' in ProfileSchema.model_fields"` erfolgreich
    - `python3 -c "from services.profile_schema import ProfileSchema; assert 'phasen' in ProfileSchema.model_fields"` erfolgreich
    - `grep -n "einwaende" services/profile_schema.py` zeigt einwaende NICHT in BasisSchema-Block (Zeilen ca. 43-58), ABER in ProfileSchema-Block
    - `python3 -c "from services.profile_schema import ProfileReadSchema"` importiert ohne Fehler
    - `python3 -c "from services.profile_schema import ProfileSchema; s = ProfileSchema(); print(s.schema_version)"` gibt 2 aus (Version-Bump kommt in Plan-03)
  </acceptance_criteria>
  <done>BasisSchema ohne Dead Fields, ProfileSchema mit einwaende + phasen top-level. Alle Imports fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-Test Schema-Kalibrierung gegen lokale Profile</name>
  <files>services/profile_schema.py</files>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\profile_schema.py (nach Task 1 — verifizierter Stand)
    - C:\Users\andre\dev\salesnerve\database\models.py (Profile-Model fuer DB-Query)
  </read_first>
  <action>
Lokalen Smoke-Test ausfuehren (noch OHNE extra='forbid' — das kommt in Plan-05):

python3 - <<'PYEOF'
import sys, os
sys.path.insert(0, os.path.abspath('.'))

import json
from database.db import get_session
from database.models import Profile
from services.profile_schema import ProfileSchema, ProfileReadSchema, _migrate_profile_data

db = get_session()
try:
    profiles = db.query(Profile).all()
    print(f"Lokale Profile: {len(profiles)}")
    errors = []
    for p in profiles:
        try:
            daten = json.loads(p.daten) if p.daten else {}
        except Exception:
            daten = {}
        migrated = _migrate_profile_data(daten.copy())
        try:
            parsed = ProfileReadSchema.model_validate(migrated)
            print(f"  Profil {p.id} ({p.name[:20]}): READ-Schema OK — schema_version={migrated.get('schema_version')}")
        except Exception as e:
            errors.append(f"  Profil {p.id}: READ-Schema FEHLER: {e}")
    if errors:
        print("FEHLER gefunden:")
        for e in errors: print(e)
        sys.exit(1)
    else:
        print("Alle lokalen Profile durchlaufen ProfileReadSchema ohne Fehler.")
finally:
    db.close()
PYEOF

ERWARTETES ERGEBNIS:
- Alle lokalen Profile (4 Stueck) durchlaufen ProfileReadSchema ohne Fehler
- schema_version wird als 2 (oder hoeher falls schon migriert) angezeigt
- Wenn ein Profil fehlt oder Fehler zeigt: Fehler analysieren und BasisSchema/ProfileSchema anpassen

HINWEIS: Write-Schema (ProfileSchema mit extra='ignore') wird hier noch NICHT getestet
gegen alle Profile — das geschieht erst in Plan-05 nach extra='forbid' Aktivierung.
  </action>
  <verify>
    <automated>python3 -c "
import sys, os, json
sys.path.insert(0, os.path.abspath('C:/Users/andre/dev/salesnerve'))
os.chdir('C:/Users/andre/dev/salesnerve')
from services.profile_schema import ProfileReadSchema, _migrate_profile_data
# Minimaler Test: leeres Dict durchlaeuft Read-Schema
d = _migrate_profile_data({})
r = ProfileReadSchema.model_validate(d)
print('Smoke-Test OK:', r.schema_version)
"
</automated>
  </verify>
  <acceptance_criteria>
    - Python-Script laeuft ohne Exception durch
    - Ausgabe enthaelt "Alle lokalen Profile durchlaufen ProfileReadSchema ohne Fehler" ODER zeigt spezifische Fehler die sofort im selben Task behoben werden
    - `python3 -c "from services.profile_schema import ProfileSchema, BasisSchema, ProfileReadSchema; print('Imports OK')"` ohne Fehler
  </acceptance_criteria>
  <done>Schema-Kalibrierung smoke-getestet gegen alle lokalen Profile. Keine Import-Fehler, keine Validation-Fehler im Read-Pfad.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Schema-Aenderung → routes/profiles.py | bearbeiten() POST validiert gegen ProfileSchema — Typ-Aenderungen koennen false-positive 400 erzeugen |
| Dead-Field-Removal → bestehende Profil-Saves | Profile die noch basis.einwaende enthalten werden nach Migration (Plan-03) korrekt umgebaut |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigation |
|-----------|-----------|------------|-------------|------------|
| T-08191-05 | Tampering | BasisSchema Dead-Field-Removal | mitigate | _BasisReadSchema behaelt einwaende/phasen fuer Lese-Kompatibilitaet; Write-Schema-Typen stimmen mit Editor-Output ueberein |
| T-08191-06 | Denial of Service | ProfileSchema Typ-Drift | mitigate | extra='ignore' in diesem Plan verhindert false-positive Validation-Fehler bis Plan-05; Smoke-Test verifiziert lokale Profile |
| T-08191-07 | Information Disclosure | List[dict] vs List[str] einwaende | accept | Typ-Union in ProfileSchema falls noetig; Migration in Plan-03 normalisiert; kein Datenverlust |
</threat_model>

<verification>
```bash
# Schema-Import ohne Fehler
cd C:\Users\andre\dev\salesnerve
python3 -c "from services.profile_schema import ProfileSchema, BasisSchema, ProfileReadSchema, _migrate_profile_data; print('OK')"

# BasisSchema Dead-Fields weg
python3 -c "from services.profile_schema import BasisSchema; f = BasisSchema.model_fields; print('einwaende in basis:', 'einwaende' in f); print('phasen in basis:', 'phasen' in f)"

# ProfileSchema Top-Level-Felder vorhanden
python3 -c "from services.profile_schema import ProfileSchema; f = ProfileSchema.model_fields; print('einwaende top-level:', 'einwaende' in f); print('phasen top-level:', 'phasen' in f)"
```

Cross-AI Review required before merge — /gsd-review --phase 08.19.1
</verification>

<success_criteria>
- BasisSchema: einwaende und phasen entfernt
- ProfileSchema: einwaende und phasen als top-level List[dict] vorhanden
- ProfileReadSchema: kein fragen-Feld
- Alle Imports fehlerfrei
- Lokale Profile durchlaufen ProfileReadSchema ohne Validation-Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.19.1-schema-realitaet-kalibrierung-extra-forbid/08.19.1-02-SUMMARY.md` erstellen.
</output>
