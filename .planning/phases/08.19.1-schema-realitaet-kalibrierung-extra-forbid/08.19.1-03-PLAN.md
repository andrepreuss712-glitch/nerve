---
phase: 08.19.1
plan: 03
type: execute
wave: 2
depends_on:
  - 08.19.1-01
files_modified:
  - services/profile_schema.py
autonomous: true
requirements:
  - TASK-3
  - TASK-6
must_haves:
  truths:
    - "_migrate_profile_data() hat eine v2→v3 Stufe die idempotent ist"
    - "einwaende aus basis.* wird nach top-level hochgezogen wenn top-level fehlt (D-02 Semantik exakt)"
    - "phasen aus basis.* wird nach top-level hochgezogen wenn top-level fehlt (D-02 Semantik exakt)"
    - "fragen-Key wird aus daten-JSON entfernt (D-05)"
    - "branche top-level wird aus daten-JSON entfernt (D-01)"
    - "Audit-Log-Zeile wird pro Profil geprintet mit [Schema]-Prefix und Profil-ID"
    - "schema_version wird auf 3 gesetzt"
    - "Idempotenz: zweimaliges Ausfuehren aendert nichts ab dem zweiten Lauf"
    - "basis: {} (leeres Dict) bleibt nach Migration erhalten (Sub-Schema-Defaults greifen) — kein KeyError, kein Pop des basis-Keys selbst"
  artifacts:
    - path: "services/profile_schema.py"
      provides: "_migrate_profile_data() mit v2→v3 Stufe"
      contains: "schema_version.*3"
  key_links:
    - from: "_migrate_profile_data()"
      to: "einwaende top-level"
      via: "upward-merge aus basis.einwaende"
      pattern: "basis.*einwaende"
    - from: "_migrate_profile_data()"
      to: "schema_version = 3"
      via: "version < 3 Guard"
      pattern: "version < 3"
---

<objective>
`_migrate_profile_data()` in services/profile_schema.py um eine v2→v3 Migrationsstufe erweitern. Implementiert: einwaende/phasen upward-merge aus basis.*, fragen-Key-Removal, branche-top-level-Removal, schema_version=3. Idempotenz per `if version >= 3: return daten`.

Purpose: Lazy-Migration im Read-Pfad (routes/profiles.py ruft _migrate_profile_data() auf). DB-Level-Batch-Migration folgt in Plan-04. Diese Funktion muss korrekt sein BEVOR Plan-04 alle Profile batch-migriert.

Output: Erweiterte _migrate_profile_data() mit exakter D-02 Merge-Semantik.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.1-schema-realitaet-kalibrierung-extra-forbid\08.19.1-CONTEXT.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\PROJECT.md
@C:\Users\andre\dev\salesnerve\services\profile_schema.py
@C:\Users\andre\dev\salesnerve\routes\profiles.py

<interfaces>
<!-- Bestehende _migrate_profile_data() Signatur (profile_schema.py Zeile 238-293) -->
def _migrate_profile_data(daten: dict) -> dict:
    """Idempotente Migration: schema_version v1 -> v2."""
    if not isinstance(daten, dict):
        daten = {}
    version = daten.get('schema_version') or 1    # None-safe
    if version >= 2:
        return daten    # bereits migriert — idempotent
    # ... v1→v2 Migration Steps ...
    daten['schema_version'] = 2
    return daten

<!-- D-02 Merge-Semantik (EXAKT implementieren, kein Spielraum): -->
# "Top-Level gewinnt, basis.* als Fallback"
# - Key existiert NICHT top-level  → basis.* hochziehen, basis.* droppen
# - Key existiert mit []            → basis.* NICHT hochziehen (User-Intent "alles geloescht")
# - Key existiert mit null          → wie "nicht vorhanden" → basis.* hochziehen

<!-- D-03 Audit-Log Format: -->
# [Schema] Profile {id}: v2->v3 applied — dropped fragen, upward-merged basis.einwaende->einwaende, upward-merged basis.phasen->phasen, dropped branche top-level
# (nur die Steps listen die tatsaechlich ausgefuehrt wurden, nicht alle)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v2→v3 Migration implementieren mit exakter D-02 Semantik</name>
  <files>services/profile_schema.py</files>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\profile_schema.py (vollstaendig — bestehende _migrate_profile_data(), v1→v2 Muster als Vorlage)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.19.1-schema-realitaet-kalibrierung-extra-forbid\08.19.1-CONTEXT.md (D-02 exakte Merge-Semantik, D-03 Audit-Log-Format, D-05 fragen-Removal)
  </read_first>
  <behavior>
    - Test 1: daten mit FEHLENDEM top-level einwaende + basis.einwaende=['A'] → nach Migration: einwaende=['A'] top-level, basis.einwaende nicht mehr vorhanden
    - Test 2: daten mit LEEREM top-level einwaende=[] + basis.einwaende=['A'] → nach Migration: einwaende=[] bleibt, basis.einwaende nicht mehr vorhanden (User-Intent respektiert)
    - Test 3: daten mit top-level einwaende=None + basis.einwaende=['A'] → nach Migration: einwaende=['A'] (null = nicht vorhanden)
    - Test 4: daten mit top-level einwaende=['B'] + basis.einwaende=['A'] → nach Migration: einwaende=['B'] bleibt (top-level gewinnt), basis.einwaende entfernt
    - Test 5: daten mit fragen-Key → nach Migration: fragen-Key nicht mehr vorhanden
    - Test 6: daten mit branche='saas_b2b' top-level → nach Migration: branche-Key nicht mehr vorhanden
    - Test 7: schema_version=3-Profil → unveraendert zurueck (idempotenz)
    - Test 8: schema_version=2 → schema_version=3 nach Migration
  </behavior>
  <action>
DATEI: services/profile_schema.py

In `_migrate_profile_data()`, NACH dem Block `daten['schema_version'] = 2` und VOR dem finalen `return daten`, folgende v2→v3 Stufe einfuegen:

```python
    # ── v2 → v3: Schema-Realitaet-Kalibrierung (Phase 08.19.1) ─────────────────
    version = daten.get('schema_version') or 1
    if version < 3:
        _audit_parts = []
        _profile_id = daten.get('_migration_profile_id', '?')  # wird von Batch-Migration gesetzt

        # Schritt 1: fragen-Key entfernen (war gesetzt auf [] in 08.19.3, jetzt komplett raus)
        if 'fragen' in daten:
            daten.pop('fragen', None)
            _audit_parts.append('dropped fragen')

        # Schritt 2: branche top-level entfernen (lebt auf Profile.branche DB-Column)
        if 'branche' in daten:
            daten.pop('branche', None)
            _audit_parts.append('dropped branche top-level')

        # Schritt 3: einwaende upward-merge (D-02 Semantik)
        basis = daten.get('basis') if isinstance(daten.get('basis'), dict) else {}
        _basis_einwaende = basis.get('einwaende')
        _top_einwaende = daten.get('einwaende', '__MISSING__')
        if _top_einwaende == '__MISSING__' or _top_einwaende is None:
            # Key fehlt oder ist null → basis.* hochziehen
            if _basis_einwaende is not None:
                daten['einwaende'] = _basis_einwaende
                _audit_parts.append('upward-merged basis.einwaende->einwaende')
        # Key existiert mit [] → basis.* NICHT hochziehen (User-Intent "alles geloescht")
        basis.pop('einwaende', None)

        # Schritt 4: phasen upward-merge (D-02 Semantik — analog zu einwaende)
        _basis_phasen = basis.get('phasen')
        _top_phasen = daten.get('phasen', '__MISSING__')
        if _top_phasen == '__MISSING__' or _top_phasen is None:
            if _basis_phasen is not None:
                daten['phasen'] = _basis_phasen
                _audit_parts.append('upward-merged basis.phasen->phasen')
        basis.pop('phasen', None)

        # basis-Referenz zurueckschreiben (wurde in-place modifiziert)
        # Leeres basis-Dict BEHALTEN — Sub-Schema-Defaults greifen (kein KeyError)
        if basis:
            daten['basis'] = basis
        elif 'basis' in daten and isinstance(daten['basis'], dict):
            daten['basis'] = basis  # leeres dict behalten (sub-schema Default greift)

        # Schritt 5: schema_version auf 3 setzen
        daten['schema_version'] = 3

        if _audit_parts:
            print(f"[Schema] Profile {_profile_id}: v2->v3 applied — {', '.join(_audit_parts)}")
        else:
            print(f"[Schema] Profile {_profile_id}: v2->v3 applied — no changes needed")
```

WICHTIG: Den bestehenden `return daten` am Ende der Funktion BEHALTEN.

WICHTIG: Den bestehenden Idempotenz-Guard `if version >= 2: return daten` am Funktionsanfang
AENDERN zu `if version >= 3: return daten` (nicht >= 2, damit v2-Profile die v3-Stufe durchlaufen).

v1-Profile laufen durch BEIDE Stufen (v1→v2 und v2→v3) in einem Aufruf.

KEINE weiteren Aenderungen an der Datei in diesem Task.
  </action>
  <verify>
    <automated>python3 - <<'PYEOF'
import sys, os
sys.path.insert(0, os.path.abspath('C:/Users/andre/dev/salesnerve'))
os.chdir('C:/Users/andre/dev/salesnerve')
from services.profile_schema import _migrate_profile_data

# Test 1: basis.einwaende hochziehen wenn top-level fehlt
d = {'schema_version': 2, 'basis': {'einwaende': ['A'], 'phasen': []}}
r = _migrate_profile_data(d)
assert r.get('einwaende') == ['A'], f"Test1 fehlgeschlagen: {r.get('einwaende')}"
assert 'einwaende' not in r.get('basis', {}), f"Test1: basis.einwaende nicht entfernt"

# Test 2: leeres top-level einwaende bleibt (User-Intent)
d2 = {'schema_version': 2, 'basis': {'einwaende': ['A']}, 'einwaende': []}
r2 = _migrate_profile_data(d2)
assert r2.get('einwaende') == [], f"Test2 fehlgeschlagen: {r2.get('einwaende')}"

# Test 3: null → basis hochziehen
d3 = {'schema_version': 2, 'basis': {'einwaende': ['B']}, 'einwaende': None}
r3 = _migrate_profile_data(d3)
assert r3.get('einwaende') == ['B'], f"Test3 fehlgeschlagen: {r3.get('einwaende')}"

# Test 4: top-level gewinnt (basis.einwaende wird trotzdem entfernt)
d4 = {'schema_version': 2, 'basis': {'einwaende': ['A']}, 'einwaende': ['B']}
r4 = _migrate_profile_data(d4)
assert r4.get('einwaende') == ['B'], f"Test4 fehlgeschlagen: {r4.get('einwaende')}"
assert 'einwaende' not in r4.get('basis', {}), f"Test4: basis.einwaende nicht entfernt"

# Test 5: fragen entfernen
d5 = {'schema_version': 2, 'fragen': []}
r5 = _migrate_profile_data(d5)
assert 'fragen' not in r5, f"Test5: fragen nicht entfernt"

# Test 6: branche top-level entfernen
d6 = {'schema_version': 2, 'branche': 'saas_b2b'}
r6 = _migrate_profile_data(d6)
assert 'branche' not in r6, f"Test6: branche nicht entfernt"

# Test 7: Idempotenz
d7 = {'schema_version': 3, 'einwaende': ['X']}
r7 = _migrate_profile_data(d7)
assert r7 == d7, f"Test7 Idempotenz fehlgeschlagen: {r7}"

# Test 8: schema_version bump
d8 = {'schema_version': 2}
r8 = _migrate_profile_data(d8)
assert r8['schema_version'] == 3, f"Test8: schema_version={r8['schema_version']}"

print('Alle 8 Tests bestanden.')
PYEOF
</automated>
  </verify>
  <acceptance_criteria>
    - Alle 8 Python-Test-Assertions bestehen ohne Exception
    - `grep -n "version >= 3" services/profile_schema.py` zeigt exakt 1 Treffer (Idempotenz-Guard)
    - `grep -n "version < 3" services/profile_schema.py` zeigt exakt 1 Treffer (v3-Block-Guard)
    - `grep -n "schema_version.*=.*3" services/profile_schema.py` zeigt Treffer in der Migrationsfunktion
    - `grep -n "\[Schema\]" services/profile_schema.py` zeigt 2 Treffer (applied mit und ohne changes)
    - Python-Imports weiterhin fehlerfrei: `python3 -c "from services.profile_schema import _migrate_profile_data; print('OK')"`
    - `basis: {}` (leeres Dict) bleibt nach Migration erhalten — verifiziert durch: `python3 -c "from services.profile_schema import _migrate_profile_data; r = _migrate_profile_data({'schema_version': 2, 'basis': {'einwaende': ['x']}}); assert 'basis' in r and r['basis'] == {}, f'basis: {r.get(\"basis\")}'; print('basis-Retention OK')"`
  </acceptance_criteria>
  <done>v2→v3 Migration mit exakter D-02 Semantik implementiert und durch 8 direkte Python-Assertions verifiziert. basis:{} Retention explizit bestaetigt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| _migrate_profile_data() → daten-Dict in-place | Funktion modifiziert daten in-place UND gibt es zurueck — Aufrufer darf nicht Original-Dict-Referenz weiter nutzen |
| basis.pop() → basis-Dict | Wenn basis ein leeres Dict wird: daten['basis'] bleibt als {} (Sub-Schema-Defaults greifen) |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigation |
|-----------|-----------|------------|-------------|------------|
| T-08191-08 | Tampering | einwaende=[] edge case | mitigate | Expliziter `__MISSING__`-Sentinel statt `daten.get('einwaende')` (None vs. absent unterscheidbar) |
| T-08191-09 | Tampering | basis-Dict in-place Pop | mitigate | basis-Referenz nach pops zurueckgeschrieben; leeres basis-Dict bleibt erhalten (kein KeyError) |
| T-08191-10 | Denial of Service | v1-Profile doppelt migriert | accept | v1→v2 und v2→v3 in einem Aufruf korrekt — version-Guards verhindern doppeltes v3-Processing |
| T-08191-11 | Information Disclosure | Audit-Log [Schema] | accept | Nur Profil-ID + Key-Namen, kein Profil-Inhalt; print-Statement analog zu bestehendem [DB]-Pattern |
</threat_model>

<verification>
```bash
cd C:\Users\andre\dev\salesnerve

# Idempotenz-Guard korrekt
grep -n "version >= 3" services/profile_schema.py

# v3-Block vorhanden
grep -n "version < 3" services/profile_schema.py

# Alle Tests bestanden
python3 -c "
from services.profile_schema import _migrate_profile_data
d = {'schema_version': 2, 'basis': {'einwaende': ['test']}, 'fragen': [], 'branche': 'x'}
r = _migrate_profile_data(d)
assert r['schema_version'] == 3
assert r['einwaende'] == ['test']
assert 'fragen' not in r
assert 'branche' not in r
print('Verifikation OK')
"
```

Cross-AI Review required before merge — /gsd-review --phase 08.19.1
</verification>

<success_criteria>
- _migrate_profile_data() hat v2→v3 Stufe
- D-02 Merge-Semantik (3 Cases: absent/null/empty) korrekt implementiert
- fragen und branche top-level werden entfernt
- schema_version = 3 nach Migration
- Idempotenz: schema_version=3-Profile werden unveraendert zurueckgegeben
- 8 direkte Python-Test-Assertions bestehen (inkl. Test 4: top-level gewinnt)
- basis: {} bleibt nach Migration erhalten — kein KeyError, kein unbeabsichtigtes Loeschen des basis-Keys
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.19.1-schema-realitaet-kalibrierung-extra-forbid/08.19.1-03-SUMMARY.md` erstellen.
</output>
