---
phase: 04.8-ki-logik-upgrade-inserted
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - services/claude_service.py
autonomous: false
requirements: [KI-09]
must_haves:
  truths:
    - "coaching_loop calls claude-haiku-4-5-20251001 (not claude-sonnet-4-6)"
    - "No Sonnet model-ID remains in any live-loop code path"
    - "Human verifies coaching output quality is still acceptable"
  artifacts:
    - path: services/claude_service.py
      provides: "coaching_loop migrated to Haiku"
  key_links:
    - from: coaching_loop
      to: "claude-haiku-4-5-20251001 model"
      via: "claude_client.messages.create"
      pattern: "claude-haiku-4-5-20251001"
---

<objective>
Migrate `coaching_loop` in services/claude_service.py from Sonnet to Haiku. Close pre-existing Haiku-only-live constraint violation (user override #3: in-scope for 04.8).

Purpose: D-09 — violation of CLAUDE.md constraint "Sonnet MUSS raus aus dem Live-Loop — nur Haiku für alles Live".
Output: Live loop is 100% Haiku. Sonnet only in post-call paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04.8-ki-logik-upgrade-inserted/04.8-CONTEXT.md
@.planning/phases/04.8-ki-logik-upgrade-inserted/04.8-RESEARCH.md
@services/claude_service.py

<interfaces>
- `coaching_loop` function in services/claude_service.py (around line 525 per RESEARCH §1.1)
- Currently uses `model='claude-sonnet-4-6'` (or similar sonnet ID)
- Outputs coaching tips consumed by live UI or logged to DB
- Prompt template is separate (e.g. COACHING_PROMPT) — stays unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap model ID + cost-tag + validate output shape</name>
  <files>services/claude_service.py</files>
  <action>
1. Locate `coaching_loop` function (grep for `def coaching_loop` and for `sonnet` model ID).
2. Replace the model ID in the `claude_client.messages.create(model=...)` call with `'claude-haiku-4-5-20251001'`.
3. Update the cost-hook `context_tag` (if the call uses one) from the old tag to `'coaching_haiku'` to distinguish in analytics.
4. If `max_tokens` was large (e.g., 1024) because Sonnet gave richer output, keep it the same initially — Haiku handles the same cap fine. Do NOT reduce max_tokens in this plan.
5. Add a comment above the model line:
```python
# Phase 04.8 P07: migrated Sonnet→Haiku per Haiku-only-live constraint
```
6. Grep the full file for any other `sonnet` references in live-loop paths (not post-call). If any remain, flag them with a `# TODO(04.8-P07)` comment. Do NOT migrate post-call Sonnet uses — those are legitimate.

7. Verify JSON parsing of the response still works: Haiku's output format is compatible, but the response may be slightly different in verbosity. If the parser is strict, add try/except resilience around the JSON parse (matching the pattern used in `classify_phase`).
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "src=open('services/claude_service.py').read(); import re; sonnet_hits=[l for l in src.split(chr(10)) if 'sonnet' in l.lower() and 'post' not in l.lower() and '#' not in l.split('sonnet')[0][-3:]]; print('remaining live sonnet refs:', sonnet_hits); assert 'claude-haiku-4-5-20251001' in src"</automated>
  </verify>
  <done>coaching_loop uses Haiku, no stray live-loop Sonnet refs, comment documents migration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies coaching output quality under Haiku</name>
  <what-built>
coaching_loop migrated from Sonnet to Haiku. Same prompt, same max_tokens, new model.
  </what-built>
  <how-to-verify>
1. Restart the Flask app: `python app.py`
2. Start a live session in /live
3. Speak 2-3 minutes with varied content (a greeting, a question, an objection)
4. Observe coaching output:
   - Coaching tips still appear in the UI (same panel as before)
   - Tips are still in German and relevant
   - JSON parse errors? Check terminal stdout for `[coaching_loop]` or similar errors
5. If tips are noticeably worse or parse errors appear, report findings — Haiku may need a prompt tweak or max_tokens bump.
6. If acceptable, approve.
  </how-to-verify>
  <resume-signal>Type "approved" or describe output quality issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| coaching_loop → Haiku API | Transcript context sent to Anthropic |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.8.7-01 | Information Disclosure | Same as existing coaching_loop (no new data) | accept | DSGVO posture unchanged by model swap |
| T-04.8.7-02 | Tampering | Haiku JSON output shape drift | mitigate | Try/except parse resilience; fallback to empty tip |
| T-04.8.7-03 | DoS | Haiku call cost | mitigate | Haiku is cheaper than Sonnet; net cost decrease |
| T-04.8.7-04 | Repudiation | Cost analytics gap | mitigate | New context_tag='coaching_haiku' separates this channel |
</threat_model>

<verification>
- grep confirms `claude-sonnet` no longer in live-loop paths
- Human approval after live session test
</verification>

<success_criteria>
- coaching_loop on Haiku
- Constraint "Haiku only in live loop" restored
- No regression in coaching UX per human check
</success_criteria>

<output>
After completion, create `.planning/phases/04.8-ki-logik-upgrade-inserted/04.8-07-SUMMARY.md`
</output>
