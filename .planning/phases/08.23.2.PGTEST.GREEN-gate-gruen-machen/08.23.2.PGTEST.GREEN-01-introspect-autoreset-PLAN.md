---
phase: 08.23.2.PGTEST.GREEN
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_schema_introspect.py
  - tests/conftest.py
autonomous: true
requirements: [Req-2, Req-3, Req-9]
must_haves:
  truths:
    - "Im Gate-Lauf erscheinen 0 _baseline_cleanup_guard-Fails aus geleakten public-Extra-Rows"
    - "Ein test-erzeugter Extra-Row in einer public-Tabelle wird nach dem Test automatisch geloescht und der naechste Test sieht ihn nicht"
    - "Ein geloeschter/mutierter Baseline-Seed-Row (id=1) wirft sofort einen harten pytest.fail mit nodeid (kein Heilen)"
    - "Jedes Auto-Delete emittiert eine laute [BASELINE-AUTO-FIX]-Warnung mit nodeid + Tabelle + PKs"
    - "Die bewachte public-Tabellen-Liste + reverse-FK-Loeschorder stammen aus dem Katalog (pg_constraint topo-Sort), nicht aus einer Hardcode-Liste"
    - "Die abgeleitete Liste enthaelt mindestens eine Tabelle, die in der alten _BASELINE_PUBLIC_TABLES-Hardcode-Liste fehlte (z.B. calls/objection_event)"
    - "ALLE FK-Kanten (auch CASCADE, confdeltype='c') sind im topo-Sort — auch eine CASCADE->RESTRICT-Kette sortiert Leaves-vor-Roots, sodass der Waechter-Cleanup nie an einer FK-Violation crasht (kein DoS)"
    - "Das Auto-Delete loescht ueber die KATALOG-ABGELEITETE PK-Spalte ({pk_col}::text = ANY(:ids)), nicht ueber hardcoded id — eine non-id-PK-Tabelle (intent_event.event_id) crasht NICHT mit 'column id does not exist'"
    - "Eine Tabelle mit zusammengesetztem PK (>1 indisprimary-Zeile) wird NICHT fuers Auto-Delete freigegeben, sondern mit Grund 'composite PK not supported for auto-delete' ins foundation_register (Denylist) gelegt und geloggt — KEINE Ueber-Loeschung/Crash (Gemini-Re-Review R2 / Fund #6)"
    - "cleanup_rows liest die dynamische reverse-FK-Order/PK-Spalten aus einem MODUL-LEVEL-Cache (einmal bei Session-Start gefuellt) — KEINE Signatur-Aenderung, KEINE per-call DB-Query"
    - "Der Modul-Level-Cache (_DERIVED_FK_ORDER / _DERIVED_PK_COLS) ist nach Session-Start garantiert GEFUELLT, weil _baseline_snapshot _baseline_schema als Fixture-Parameter (Dependency) anfordert — die dynamische Ableitung (Req-9) faellt NICHT still auf den Hardcode-Fallback zurueck (Gemini-Re-Review R2 / Fund #7)"
  artifacts:
    - path: "tests/_schema_introspect.py"
      provides: "Katalog-Ableitung: baseline-table-list + reverse-FK-Loeschorder aus pg_constraint, parametrisiert auf (DSN/Conn, Schema); composite-PK-Tabellen ins foundation_register"
      contains: "def derive_baseline_tables"
    - path: "tests/conftest.py"
      provides: "Auto-Reset gespaltener Baseline-Waechter (leaked->loeschen+warnen, missing/mutated->pytest.fail); _baseline_snapshot fordert _baseline_schema als Dependency (Cache-Fill-Garantie)"
      contains: "[BASELINE-AUTO-FIX]"
  key_links:
    - from: "tests/conftest.py::_baseline_cleanup_guard"
      to: "tests/_schema_introspect.py::derive_baseline_tables"
      via: "import + consume (table_list + reverse_fk_order)"
      pattern: "_schema_introspect"
    - from: "tests/conftest.py auto-delete"
      to: "nerve_test public-Tabellen"
      via: "DELETE FROM {tbl} WHERE {pk_col}::text = ANY(:ids) in engine.begin()/commit(), pk_col aus primary_key_column()"
      pattern: "::text = ANY"
    - from: "tests/conftest.py::_baseline_snapshot"
      to: "tests/conftest.py::_baseline_schema"
      via: "Fixture-Parameter (Dependency) -> garantiert Modul-Cache-Fill bei Session-Start (Fund #7)"
      pattern: "_baseline_snapshot\\([^)]*_baseline_schema"
---

<objective>
Loese die suite-weite Baseline-Leak-Dimension (Req-2/Req-3) + ersetze die hardcodierten Waechter-Tabellenlisten durch Katalog-Ableitung (Req-9, public-Haelfte). Das geteilte Schema-Introspect-Modul (`tests/_schema_introspect.py`) liefert aus EINER Wahrheit (pg_constraint topo-Sort) die baseline-table-list UND die reverse-FK-Loeschorder; der Auto-Reset-gespaltene Baseline-Waechter in conftest.py KONSUMIERT beides.

Purpose: Heute verlangt der globale fail-closed `_baseline_cleanup_guard`, dass jeder der ~600 Tests die ganze public-Baseline pristine laesst → ~507 errors. Der Auto-Reset-Waechter heilt test-erzeugte Extra-Rows selbst (laut), blockt aber harte Baseline-Verletzungen (missing/mutated id=1-Seed). Das ist die ~600-Test-Wurzel.

Output: `tests/_schema_introspect.py` (NEU), erweiterter `_baseline_cleanup_guard` + `_baseline_guard_engine` + Liste-Quelle in conftest.py.

D-G19-KOPPLUNG (KRITISCH): Modul + Waechter sind EIN gekoppelter Plan. Der Waechter darf nicht gebaut werden, bevor das Modul list+order liefert — sonst stirbt der Waechter beim eigenen Cleanup an einer FK-Verletzung. Innerhalb dieses Plans: Task 1 (Modul) ZUERST, dann Task 2 (Waechter konsumiert es).

THEMA-KLAMMER (Gemini-Review-Fold 2026-06-16): topo-Sort + Auto-Delete VOLL katalog-allgemein machen, nicht halb. (a) ALLE FK-Kanten in den Sort (auch CASCADE — sonst Waechter-DoS bei CASCADE->RESTRICT-Ketten); (b) Auto-Delete ueber die abgeleitete PK-Spalte, nicht hardcoded id (sonst Crash an non-id-PK-Tabellen wie intent_event.event_id); (c) cleanup_rows liest die dynamische Order aus einem Modul-Level-Cache, KEINE Signatur-Aenderung / keine per-call DB-Query.

THEMA-KLAMMER R2 (Gemini-Re-Review R2 / Fund #6 + #7 — vervollstaendigt die PK-Generalitaet + schliesst das stille-Fallback-Loch): (d) PK-Generalitaet ZU ENDE (Fund #6, der Rest von #2): eine Tabelle mit zusammengesetztem PK (>1 indisprimary-Zeile) wird NICHT fuers Auto-Delete freigegeben — sonst loescht `{first_pk_col}::text = ANY(...)` viel mehr Rows als test-erzeugt (Over-Deletion) oder crasht. Solche Tabellen landen mit Grund "composite PK not supported for auto-delete" im foundation_register (Denylist, Req-7), geloggt, NICHT still gedroppt. (e) Cache-Fill-Garantie (Fund #7): `_baseline_snapshot` fordert `_baseline_schema` als Fixture-Parameter (Dependency) an → der Modul-Level-Cache ist bei Session-Start hart garantiert gefuellt, BEVOR irgendein Fallback-Pfad triggern kann; sonst no-opt Req-9 still (Meta-False-Green: Tor denkt es ist dynamisch, ist es nicht).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-SPEC.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-CONTEXT.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-PATTERNS.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/_green_isolation_gemini_OUT.md

<interfaces>
<!-- Bestehende conftest-Vertraege, die Task 2 erweitert (verbatim aus tests/conftest.py). -->

_diff_baseline (conftest.py:289-303) — liefert BEREITS den 3-Wege-Split, nur anders konsumieren:
```python
def _diff_baseline(current, baseline):
    drift = {}
    for tbl, base_map in baseline.items():
        cur_map = current.get(tbl, {})
        cur_pks = set(cur_map); base_pks = set(base_map)
        leaked = cur_pks - base_pks
        missing = base_pks - cur_pks
        mutated = {pk for pk in (cur_pks & base_pks) if cur_map[pk] != base_map[pk]}
        if leaked or missing or mutated:
            drift[tbl] = {"leaked": leaked, "missing": missing, "mutated": mutated}
    return drift
```

_baseline_guard_engine (conftest.py:306-321) — eigene session-scoped Read-Engine, ENTKOPPELT von der per-Test umgebundenen MODUL-SessionLocal (db_session/client machen pro Test configure(bind=None)+dispose()). nerve_app-peer, public-only, KEINE BYPASSRLS-Rolle.

id::text-Cast-Muster (cleanup_rows, conftest.py:210/221-224) — verbatim (der `::text`-Cast bleibt; nur der COLUMN-NAME wird in Task 2 dynamisch/katalog-abgeleitet):
```python
conn.execute(text(f"DELETE FROM {tbl} WHERE id::text = ANY(:ids)"), {"ids": [str(x) for x in ids]})
```

_BASELINE_PUBLIC_TABLES (conftest.py:263-267) — alte 16er-Hardcode-Liste, wird von Task 1 abgeloest.
_CLEANUP_FK_ORDER (conftest.py:114-130) — alte reverse-FK-Hardcode-Liste; BLEIBT als best-effort-Fallback fuer cleanup_rows (Gemini-Fund #3), die primaere Quelle wird ein Modul-Level-Cache (siehe Task 2).

Katalog-Query-Analoga (test_schild_guard.py:53-66 pg_class/pg_namespace, deploy.sh:168-174 pg_policies counts) — beweisen: Katalog-Reads sind rollen-agnostisch, nerve_app darf pg_constraint/pg_class lesen ohne GRANT.
</interfaces>
</context>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

Dieser Plan LIEST den Postgres-Katalog (pg_constraint/pg_class/pg_namespace/pg_index/pg_attribute) und LOESCHT Rows aus public.*-Tabellen in nerve_test.

### Angefasste Tabellen
- **Katalog (lesen):** pg_constraint, pg_class, pg_namespace, pg_index, pg_attribute — world-readable, rollen-agnostisch (Beleg: test_schild_guard.py liest pg_class als nerve_app ohne GRANT; deploy.sh:168 liest pg_policies als postgres).
- **public.* base-Tabellen (loeschen, NUR test-erzeugte leaked-IDs):** dynamisch abgeleitet — Default "watch everything" minus Foundation-Register-Denylist. Geloescht wird ueber die KATALOG-ABGELEITETE PK-Spalte pro Tabelle (D-G17 REFINEMENT, Gemini-Fund #2), NICHT ueber hardcoded id. Tabellen mit zusammengesetztem PK werden GAR NICHT auto-geloescht (Gemini-Re-Review R2 / Fund #6) — sie liegen in der Denylist.

### inspect.sh-Beleg (PFLICHT — Executor zitiert verbatim)
Der Executor MUSS vor dem Bau die Produktions-Katalog-Wahrheit ziehen und VERBATIM in die SUMMARY uebernehmen:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh tables'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT con.conrelid::regclass::text AS child, con.confrelid::regclass::text AS parent, con.confdeltype FROM pg_constraint con JOIN pg_namespace n ON n.oid=con.connamespace WHERE con.contype='\''f'\'' AND n.nspname='\''public'\'' ORDER BY 1;"'
```
> Daraus leitet der Executor ab: welche public-base-Tabellen existieren, welche FK-Kanten/CASCADE/self-ref es gibt, und benennt im Code-Kommentar die konkreten NERVE-self-ref/mutual-FK-Tabellen (D-G16 Bau-Wachpunkt 1). Achtung: CASCADE-Kanten werden ALS KANTEN MITGENOMMEN (Gemini-Fund #1) — der Executor prueft im FK-Output explizit, ob es CASCADE->RESTRICT-Ketten gibt (z.B. intent_event->calls->...), und belegt im Kommentar, dass diese Kette korrekt Leaves-vor-Roots sortiert. Zusaetzlich (Gemini-Re-Review R2 / Fund #6) zieht der Executor die PK-Spalten-Wahrheit, um Composite-PK-Tabellen zu identifizieren:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT i.indrelid::regclass::text AS tbl, count(*) AS pk_cols FROM pg_index i JOIN pg_namespace n ON n.oid=(SELECT relnamespace FROM pg_class WHERE oid=i.indrelid) WHERE i.indisprimary AND n.nspname='\''public'\'' GROUP BY 1 HAVING count(*) > 1 ORDER BY 1;"'
```
> Jede Tabelle mit pk_cols > 1 MUSS im Code-Kommentar benannt + ins foundation_register mit Grund "composite PK not supported for auto-delete" gelegt werden (Fund #6).

### Cross-Layer-Konsistenz-Tabelle
| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert via |
|---|---|---|---|
| FK-Kanten (child/parent/confdeltype) | gelesen in derive (ALLE Kanten in den Sort) | pg_constraint (Katalog) | inspect.sh psql-Query oben |
| base-Tabellen-Set | gelesen in derive | pg_class relkind='r', schema | inspect.sh tables |
| PK-Spaltenname pro Tabelle | gelesen in derive (D-G17 REFINEMENT) + im Auto-Delete VERWENDET | pg_index indisprimary + pg_attribute | psql-Query (PATTERNS.md:63-66) |
| PK-Spalten-ANZAHL pro Tabelle | gelesen in derive (Composite-PK-Guard, Fund #6) | pg_index indisprimary count | psql HAVING count(*)>1 Query oben |
| composite-PK-Tabellen | NICHT geloescht; ins foundation_register (Denylist) | n/a (ausgeschlossen) | foundation_register-Eintrag + logging.warning |
| leaked Extra-Rows | geloescht im Waechter-Teardown via {pk_col}::text | public.* DB-Rows in nerve_test | _diff_baseline leaked-Set |
</persistenz_schicht_verifikation>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema-Introspect-Modul tests/_schema_introspect.py (Katalog-Ableitung, D-G16/D-G17/D-G18)</name>
  <read_first>
    - tests/test_schild_guard.py:53-66 (pg_class/pg_namespace cursor-Muster, %s-parametrisiert auf Schema — der Katalog-Read-Analog)
    - deploy.sh:168-174 (pg_policies/pg_class counts via psql -tAc als postgres — beweist Katalog rollen-agnostisch)
    - tests/conftest.py:114-130 (alte _CLEANUP_FK_ORDER — die reverse-FK-Order, die zum best-effort-Fallback wird) + :263-267 (alte _BASELINE_PUBLIC_TABLES — die zu ersetzende Liste)
    - PATTERNS.md Sektion 1 (FK-edge-Query confdeltype, self-ref child=parent, PK-name-from-catalog-Query verbatim + Composite-PK-Denylist-Note)
    - inspect.sh-Output aus der Persistenz-Schicht-Verifikation oben (echte public-FK-Kanten + Tabellen-Set + Composite-PK-Tabellen von Prod)
  </read_first>
  <behavior>
    - Test 1 (DSN-frei, gegen Fixture-Edges): topo-Sort einer kuenstlichen FK-Kanten-Liste {users->organisations, profiles->users} liefert reverse-FK-Order = [profiles, users, organisations] (Kind vor Eltern).
    - Test 2 (Zyklus, Bau-Wachpunkt 1): self-ref Kante {accounts->accounts} haengt NICHT — die Kante wird ignoriert/gebrochen, accounts erscheint genau einmal in der Order.
    - Test 3 (CASCADE-Kante BLEIBT im Sort, Bau-Wachpunkt 2 — Gemini-Fund #1): eine Kette mit einer CASCADE-Kante (confdeltype='c', z.B. intent_event->calls) UND einer RESTRICT-Kante darunter (calls->organisations) → die CASCADE-Kante wird NICHT uebersprungen, sondern bleibt als Kante im topo-Sort. Assertion: das CASCADE-Kind (intent_event) steht VOR seinem Eltern (calls) in der reverse-FK-Order UND bleibt in der baseline-table-list (Snapshot-Pflicht). Beweist: Leaves-vor-Roots haelt auch ueber eine CASCADE->RESTRICT-Kette → kein Waechter-DoS.
    - Test 4 (Denylist/Foundation-Register, D-G17): alembic_version + Views/Matviews (relkind != 'r') sind NICHT in der baseline-table-list; eine Tabelle ohne bewachbaren PK landet mit Grund in der Denylist (geloggt), wird NICHT still gedroppt.
    - Test 5 (Req-9-Beleg): die abgeleitete public-Liste enthaelt calls + objection_event (die in der alten _BASELINE_PUBLIC_TABLES fehlten).
    - Test 6 (PK-from-catalog, D-G17 REFINEMENT — Gemini-Fund #2): primary_key_column liefert fuer eine non-id-PK-Tabelle den korrekten Spaltennamen (z.B. intent_event -> 'event_id', nicht 'id'); fuer eine id-PK-Tabelle 'id'. Beweist: das spaetere Auto-Delete (Task 2) bekommt die richtige PK-Spalte pro Tabelle.
    - Test 7 (Composite-PK-Guard, Gemini-Re-Review R2 / Fund #6 — Rest von #2): eine Tabelle mit zusammengesetztem PK (Katalog-Query liefert >1 indisprimary-Zeile, z.B. eine kuenstliche/echte Join-Tabelle mit (a_id, b_id) als PK) landet NICHT in der auto-delete-faehigen baseline_table_list, sondern im foundation_register mit Grund "composite PK not supported for auto-delete". Assertion: (a) die Tabelle ist im foundation_register mit genau diesem Grund; (b) primary_key_column gibt fuer sie KEINE einzelne Spalte zur Auto-Delete-Nutzung frei (bzw. die Derivation markiert sie als nicht-watchbar); (c) logging.warning wurde emittiert (caplog). Beweist: keine Ueber-Loeschung/kein Crash an Composite-PK-Tabellen.
  </behavior>
  <action>
    Erstelle `tests/_schema_introspect.py` — ein geteiltes, parametrisiertes Python-Modul (D-G18), ASCII-Identifier.

    Public-API (Signaturen exakt, damit Task 2 + Plan 02 gegen sie bauen):
    ```python
    def derive_baseline_tables(conn_or_dsn, schema='public'):
        """Liefert (baseline_table_list, reverse_fk_delete_order, foundation_register).
        baseline_table_list: alle base-Tabellen (relkind='r') des Schemas minus begruendete Denylist
            (inkl. composite-PK-Tabellen, die NICHT auto-delete-faehig sind — Gemini-Fund #6).
        reverse_fk_delete_order: topo-sortiert Kind-vor-Eltern; ALLE FK-Kanten gehen in den Sort
            (auch CASCADE/confdeltype='c') — garantiert Leaves-vor-Roots auch ueber CASCADE->RESTRICT-Ketten
            (D-G16 WP2, Gemini-Fund #1). Nur self-ref/mutual-Kanten werden zur Zyklus-Brechung ignoriert.
        foundation_register: dict {table: reason} jeder bewusst exkludierten Tabelle (D-G17, Req-7 — nie still).
            Enthaelt u.a. composite-PK-Tabellen mit reason 'composite PK not supported for auto-delete' (Fund #6)."""
    def primary_key_column(conn_or_cur, table, schema='public'):
        """PK-Spaltenname AUS DEM KATALOG (pg_index indisprimary), NICHT 'id' hardcoden (D-G17 REFINEMENT).
        Wird von Task 2 fuer das Auto-Delete pro Tabelle konsumiert (Gemini-Fund #2).
        Composite PK (>1 indisprimary-Zeile): die Tabelle ist NICHT auto-delete-faehig (Gemini-Fund #6) —
        sie wird in der Derivation ins foundation_register gelegt (Grund 'composite PK not supported for
        auto-delete') und NICHT fuer den Auto-Delete-Pfad freigegeben (gibt keine einzelne Spalte zurueck)."""
    ```

    Implementierung:
    1. FK-Kanten lesen (D-G16) — exakte Query aus PATTERNS.md:56-61:
       `SELECT con.conrelid::regclass::text AS child, con.confrelid::regclass::text AS parent, con.confdeltype FROM pg_constraint con JOIN pg_namespace n ON n.oid=con.connamespace WHERE con.contype='f' AND n.nspname=%s;`
       Parametrisiert auf schema (%s + Tuple, wie SCHILD).
    2. base-Tabellen lesen: `relkind='r'` filtert Views/Matviews automatisch (PATTERNS.md:46). Schema via %s.
    3. PK-Spaltenname + PK-Spalten-ANZAHL pro Tabelle aus Katalog (PATTERNS.md:63-66):
       `SELECT a.attname FROM pg_index i JOIN pg_attribute a ON a.attrelid=i.indrelid AND a.attnum=ANY(i.indkey) WHERE i.indrelid=%s::regclass AND i.indisprimary;`
       - `len(pk_cols) == 0` (keine PK-Spalte gefunden) → in foundation_register mit Grund "no watchable PK", LOGGEN (`logging.warning`), NICHT still droppen (D-G17 REFINEMENT).
       - `len(pk_cols) == 1` → primary_key_column(...) gibt den abgeleiteten Namen zurueck (z.B. 'event_id' fuer intent_event), damit Task 2 das Auto-Delete ueber die korrekte Spalte fahren kann (Gemini-Fund #2).
       - `len(pk_cols) != 1` (zusammengesetzter PK, >1 Zeile) → die Tabelle wird NICHT fuers Auto-Delete freigegeben (Gemini-Re-Review R2 / Fund #6, der Rest von #2): sonst loescht `{first_pk_col}::text = ANY(...)` mit nur der ersten Spalte VIEL mehr Rows als test-erzeugt (Over-Deletion) oder crasht. Stattdessen → foundation_register mit Grund "composite PK not supported for auto-delete", `logging.warning` (sichtbar, Req-7, NIE still gedroppt), und die Tabelle ist NICHT in der auto-delete-faehigen baseline_table_list. `primary_key_column` gibt fuer eine solche Tabelle KEINE einzelne Spalte zur Auto-Delete-Nutzung frei (markiert sie als nicht-watchbar).
    4. Denylist (Foundation-Register, D-G17): `alembic_version` (Reason "migration-state, not test-data"). Views/Matviews fallen schon durch relkind. composite-PK-Tabellen via Schritt 3 (Fund #6). crm.* fuer Plan 02 via schema='crm'.
    5. topo-Sort (Kahn ODER DFS — Discretion D-G16, mit Gemini abgewogen): ALLE FK-Kanten (child->parent) gehen in den Sort-Graph — UNABHAENGIG von confdeltype. CASCADE-Kanten (confdeltype='c') werden NICHT mehr weggelassen (Gemini-Fund #1, BLOCKER): in einer Kette A->(CASCADE)->B->(RESTRICT)->C wuerde das Weglassen der A->B-Kante den Sortierer A vor C platzieren lassen → Loeschen von A cascadet auf B → scheitert weil C noch auf B zeigt (FK-Violation) → Waechter crasht → Tor dauerhaft blockiert (DoS). Mit ALLEN Kanten gilt Leaves-vor-Roots auch ueber CASCADE->RESTRICT-Ketten; die spaeteren CASCADE-Deletes werden dann redundant (das Kind ist schon explizit geloescht), aber NIE fehlerhaft. confdeltype wird weiterhin GELESEN — nur noch zur FK-Kanten-Aufzaehlung/Diagnose, NICHT mehr zum Kanten-Ausschluss. Zyklus-Brechung (D-G16 WP1) BLEIBT: self-ref (child==parent) und mutual FKs erkennen → NUR diese eine Kante ignorieren (sonst haengt Kahn/DFS). Im Code-Kommentar die konkreten NERVE-self-ref/mutual-Tabellen benennen (aus dem inspect.sh-FK-Output).
    6. Rueckgabe `reverse_fk_delete_order` = topo-Order Kind-vor-Eltern (so loescht der Waechter Kinder zuerst). Da CASCADE-Kinder jetzt explizit in der Order stehen, loescht der Waechter sie direkt; der ON-DELETE-CASCADE des Eltern wird damit redundant, aber nie eine Quelle fuer FK-Violations.
    7. Unit-Tests in `tests/_schema_introspect.py` selbst NICHT — eine separate `tests/test_schema_introspect.py` mit den 7 behavior-Tests (DSN-frei fuer Test 1-3 via injizierte Kanten-Liste; Test 4-7 gegen real-PG via vorhandenes Skip-when-env-Muster conftest.py:495). Test 7 (Composite-PK) kann gegen real-PG laufen ODER DSN-frei via injizierte pk_cols-Liste mit >1 Spalte — Discretion des Executors; der Composite-PK-Guard muss aber gegen einen >1-pk_cols-Fall asserten (foundation_register-Eintrag + logging.warning).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_schema_introspect.py -v'</automated>
    HINWEIS: triage.sh existiert erst nach Plan 03. Bis dahin verifiziert dieser Task ueber den vollen Gate-Lauf (deploy.sh production) gemeinsam mit Task 2. Lokaler pytest ist NICHT zulaessig (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "def derive_baseline_tables" tests/_schema_introspect.py` liefert genau 1 Treffer
    - `grep -n "def primary_key_column" tests/_schema_introspect.py` liefert genau 1 Treffer
    - `grep -n "pg_constraint" tests/_schema_introspect.py` liefert >=1 Treffer (FK-Kanten-Query vorhanden)
    - `grep -n "confdeltype" tests/_schema_introspect.py` liefert >=1 Treffer — Zweck: FK-Kanten-Aufzaehlung deckt CASCADE MIT ab (Gemini-Fund #1); die Spalte wird gelesen, aber NICHT mehr zum Kanten-Ausschluss benutzt
    - `grep -nE "confdeltype.*(NOT IN|!=|<>).*'c'|skip.*cascade|cascade.*skip|WEGLASSEN" tests/_schema_introspect.py` liefert 0 Treffer (kein confdeltype-basierter Kanten-Ausschluss mehr — Gemini-Fund #1)
    - `grep -n "indisprimary" tests/_schema_introspect.py` liefert >=1 Treffer (PK-from-catalog, D-G17 REFINEMENT — speist das Auto-Delete in Task 2)
    - `grep -nE "len\(pk_cols\)|composite PK" tests/_schema_introspect.py` liefert >=1 Treffer (Gemini-Re-Review R2 / Fund #6: Composite-PK-Guard — `len(pk_cols) != 1` → foundation_register, nicht auto-delete)
    - `grep -n "composite PK not supported for auto-delete" tests/_schema_introspect.py` liefert >=1 Treffer (exakter Denylist-Grund, Fund #6)
    - `grep -nE "logging\.warning|log\.warning" tests/_schema_introspect.py` belegt: jede Exklusion (no-PK + composite-PK) wird GELOGGT, nie still (Req-7)
    - `grep -n "foundation_register\|alembic_version" tests/_schema_introspect.py` belegt explizite Denylist mit Grund
    - `grep -rn "_CLEANUP_FK_ORDER\|_BASELINE_PUBLIC_TABLES" tests/conftest.py` → _BASELINE_PUBLIC_TABLES ist nach Task 2 entfernt/ersetzt; _CLEANUP_FK_ORDER BLEIBT nur als best-effort-Fallback fuer cleanup_rows (siehe Task 2 acceptance)
    - Im Gate-Lauf: test_schema_introspect.py erscheint als PASSED; die abgeleitete Liste enthaelt calls + objection_event (Req-9-Beleg im Log/Test-Assertion); Test 3 belegt CASCADE-Kante bleibt im Sort; Test 6 belegt primary_key_column liefert non-id-PK korrekt; Test 7 belegt eine Composite-PK-Tabelle landet im foundation_register mit Grund "composite PK not supported for auto-delete" (Fund #6) und NICHT im Auto-Delete-Pfad
  </acceptance_criteria>
  <done>Modul existiert, liefert (table_list, reverse_fk_order, foundation_register) aus pg_constraint topo-Sort mit ALLEN FK-Kanten (auch CASCADE); PK-Name katalog-abgeleitet und fuer Task-2-Auto-Delete nutzbar; Composite-PK-Tabellen (>1 indisprimary-Zeile) ins foundation_register mit Grund "composite PK not supported for auto-delete" (geloggt, NICHT auto-geloescht, Fund #6); Denylist begruendet; 7 behavior-Tests gruen im Gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-Reset gespaltener Baseline-Waechter in conftest.py (D-G01..D-G06, konsumiert Task 1)</name>
  <read_first>
    - tests/_schema_introspect.py (das in Task 1 gebaute Modul — Task 2 importiert es, inkl. primary_key_column + foundation_register)
    - tests/conftest.py:289-303 (_diff_baseline, der 3-Wege-Split-Kern, unveraendert wiederverwendet)
    - tests/conftest.py:306-321 (_baseline_guard_engine, read-only → read+write erweitern, D-G05)
    - tests/conftest.py:343-368 (_baseline_cleanup_guard, fail-closed → Auto-Reset gespalten)
    - tests/conftest.py (die bestehende _baseline_snapshot-Fixture — Task 2 fuegt ihr _baseline_schema als Dependency-Parameter hinzu, Fund #7)
    - tests/conftest.py:114-130 (_CLEANUP_FK_ORDER — bleibt als best-effort-Fallback) + :152-237 (cleanup_rows — id::text-Cast-Muster, D-G06; der `::text`-Cast bleibt, der Spaltenname wird dynamisch)
    - PATTERNS.md Sektion 2 (Auto-Reset-Split-Shape, TX-Hygiene, uuid-Cast, Cache-Fill-Ordering)
    - _green_isolation_gemini_OUT.md (Req-4-Beleg: Option 1 gespalten, missing/mutated MUSS fail, datiert vor Bau — commit 11b8fcf)
  </read_first>
  <behavior>
    - Test 1 (heal-leaked, D-G01): ein Test committet 1 Extra-Row in public.calls und raeumt NICHT auf → nach dem Test ist die Row weg (Auto-Delete) UND eine [BASELINE-AUTO-FIX]-Warnung mit nodeid+calls+id ist im Log. KEIN pytest.fail.
    - Test 2 (Isolation-Beleg, Req-3): zwei Tests, die dieselbe Tabelle nutzen, sehen sich nicht — der zweite startet mit der pristinen Baseline (Beleg gegen Cross-Test-Vergiftung).
    - Test 3 (STRICT SPLIT, D-G02): ein Test loescht Baseline-Row id=1 (organisations) → harter pytest.fail mit nodeid; KEIN Re-Insert (kein Heilen). Gleiches fuer mutated (xmin geaendert).
    - Test 4 (TX-Hygiene, D-G05): nach dem Auto-Delete sieht der naechste Test die geloeschte Extra-Row wirklich nicht mehr (commit ist passiert).
    - Test 5 (derived-PK-Cast, D-G06 + Gemini-Fund #2): Auto-Delete einer Extra-Row in einer uuid-PK-Tabelle (tenant_orgs) wirft KEINEN `operator does not exist: uuid = text` (der `::text`-Cast greift) UND das Delete laeuft ueber die KATALOG-ABGELEITETE PK-Spalte (primary_key_column), nicht ueber hardcoded id → eine non-id-PK-Tabelle (intent_event.event_id) wuerde NICHT mit "column id does not exist" crashen.
    - Test 6 (Modul-Level-Cache + Cache-Fill-Garantie, Gemini-Fund #3 + Gemini-Re-Review R2 / Fund #7): der session-scoped Aufbau fuellt den Modul-Level-Cache (_DERIVED_FK_ORDER / _DERIVED_PK_COLS) GENAU EINMAL; cleanup_rows liest daraus OHNE Signatur-Aenderung und OHNE per-call derive_baseline_tables-DB-Query. ZUSAETZLICH (Fund #7): nach Session-Start ist der Modul-Level-Cache NACHWEISLICH NON-EMPTY (assert `_DERIVED_FK_ORDER` und `_DERIVED_PK_COLS` sind nicht leer) — Beweis, dass die harte Ordering-Garantie (`_baseline_snapshot` fordert `_baseline_schema` als Parameter) gehalten hat und Req-9 NICHT still auf den Hardcode-Fallback zurueckgefallen ist. Bei leerem Cache (z.B. Skip-Pfad/SQLite) faellt cleanup_rows weiterhin auf _CLEANUP_FK_ORDER zurueck.
  </behavior>
  <action>
    Erweitere `tests/conftest.py` IN PLACE (D-G01..D-G06). ASCII-Identifier (`_baseline_cleanup_guard`, Tag `[BASELINE-AUTO-FIX]`).

    1. Liste-Quelle ersetzen + Modul-Level-Cache + Cache-Fill-Garantie (D-G17/D-G19, Gemini-Fund #3 + Gemini-Re-Review R2 / Fund #7): Die Hardcode-Liste `_BASELINE_PUBLIC_TABLES` (Z.263-267) durch session-scoped Ableitung via `tests/_schema_introspect.derive_baseline_tables(_baseline_guard_engine, schema='public')` ersetzen. Eine session-scoped Fixture `_baseline_schema` ruft die Ableitung EINMAL bei Session-Start auf und schreibt das Ergebnis in MODUL-LEVEL-Globals: `_DERIVED_FK_ORDER` (= reverse_fk_order) und `_DERIVED_PK_COLS` (= dict {table: pk_col} via primary_key_column; composite-PK- und no-PK-Tabellen sind hier NICHT enthalten, weil sie im foundation_register liegen — Fund #6).
       CACHE-FILL-GARANTIE (Fund #7): `_baseline_snapshot` MUSS `_baseline_schema` als Fixture-PARAMETER (Dependency) anfordern — also die Signatur `def _baseline_snapshot(..., _baseline_schema):` (oder die existierende Signatur entsprechend erweitern). Das ist eine HARTE Reihenfolge-Garantie: pytest baut `_baseline_schema` (und damit den Modul-Cache) auf, BEVOR `_baseline_snapshot` laeuft und BEVOR irgendein cleanup_rows-Fallback triggern kann. Ohne diese Dependency ist nicht garantiert, dass der Cache vor dem ersten Cleanup gefuellt ist → cleanup_rows fiele STILL auf den Hardcode-`_CLEANUP_FK_ORDER` zurueck → die dynamische Ableitung (Req-9) waere praktisch wirkungslos (Meta-False-Green). `_baseline_schema` ist NICHT nur session-scoped-aber-unreferenziert, sondern explizit als Parameter verdrahtet.
       KEINE Signatur-Aenderung an `cleanup_rows` und KEINE per-call DB-Query: `cleanup_rows` liest die dynamische Order/PK-Spalten aus den MODUL-LEVEL-Globals (`_DERIVED_FK_ORDER` / `_DERIVED_PK_COLS`). FALLBACK (Sicherheitsnetz): ist der Cache leer (Ableitung nicht gelaufen, z.B. non-PG/SQLite-Skip-Pfad), faellt `cleanup_rows` auf die bestehende hardcoded `_CLEANUP_FK_ORDER`-Order zurueck (best-effort). `_CLEANUP_FK_ORDER` BLEIBT deshalb im File als Fallback erhalten — wird aber im Normal-Pfad NICHT mehr benutzt (die Cache-Fill-Garantie aus Fund #7 stellt sicher, dass der Normal-Pfad den dynamischen Cache nimmt). Die 20+ bestehenden cleanup_rows-Caller bleiben unveraendert (kein neuer Parameter).
       `_snapshot_public_tables` iteriert ueber `table_list` statt `_BASELINE_PUBLIC_TABLES` und snapshottet jede Tabelle KEYED ueber ihre abgeleitete PK-Spalte (`_DERIVED_PK_COLS[tbl]`), nicht ueber hardcoded `SELECT id`.
    2. `_baseline_guard_engine` (D-G05): bleibt session-scoped, nerve_app-peer, public-only. Der Auto-Delete laeuft in `with engine.begin() as conn:` (commit-on-exit), NICHT auf der Read-Connection — explizite TX-Hygiene, damit geloeschte Rows fuer den naechsten Test wirklich weg sind.
    3. `_baseline_cleanup_guard` (D-G01/D-G02/D-G03) — Split:
       - `drift = _diff_baseline(current, _baseline_snapshot)` (unveraendert).
       - Pro Tabelle: `leaked` → SAMMELN fuer Auto-Delete. `missing` ODER `mutated` → SAMMELN fuer hard-fail.
       - Wenn missing/mutated nicht leer: SOFORT `pytest.fail(f"[BASELINE-GUARD] {request.node.nodeid}: protected baseline drifted (missing/mutated) -> ...")` — KEIN Heilen (D-G02: Re-Insert feuert trg_mk_tenant_org → neue UUID → bricht Folge-Tests).
       - Wenn nur leaked: pro betroffener Tabelle `logging.warning(f"[BASELINE-AUTO-FIX] {request.node.nodeid} leaked rows in {tbl}: {sorted(ids)}")` (D-G03), DANN `with engine.begin() as conn:` Loeschen in `reverse_fk_order` (Kind vor Eltern) via die KATALOG-ABGELEITETE PK-Spalte (Gemini-Fund #2): `pk_col = _DERIVED_PK_COLS[tbl]` (Fallback 'id' nur falls Cache leer), dann `conn.execute(text(f"DELETE FROM {tbl} WHERE {pk_col}::text = ANY(:ids)"), {"ids":[str(x) for x in ids]})`. Der `::text`-Cast bleibt verbatim (D-G06 uuid-Cast aus cleanup_rows:221-224) — nur der COLUMN-NAME ist jetzt dynamisch, sodass non-id-PK-Tabellen (intent_event.event_id) nicht crashen. Composite-PK-Tabellen kommen hier nicht vor (sie sind nicht in der baseline_table_list / nicht in _DERIVED_PK_COLS, Fund #6) — ein dortiger Leak wuerde stattdessen ueber den missing/mutated-bzw.-Snapshot-Pfad sichtbar, nie ueber einen falschen Single-Column-DELETE.
    4. Scope (D-G04): NUR public.*. crm.* bleibt UNVERAENDERT POST-SUITE-Check in deploy.sh (Plan 02) — kein Auto-Clean fuer crm. Im Kommentar bekraeftigen.
    5. behavior-Tests aus oben in `tests/test_schema_introspect.py` ODER einer `tests/test_baseline_autoreset.py` (real-PG, Skip-when-env-Muster). Test 6 muss zusaetzlich asserten, dass `_DERIVED_FK_ORDER` + `_DERIVED_PK_COLS` nach Session-Start NON-EMPTY sind (Fund #7 Cache-Fill-Beweis).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_baseline_autoreset.py -v'</automated>
    HINWEIS: Endgueltige Verifikation gemeinsam mit Plan 02 im vollen `deploy.sh production`-Gate-Lauf (0 _baseline_cleanup_guard-Leak-Fails). Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "\[BASELINE-AUTO-FIX\]" tests/conftest.py` liefert >=1 Treffer (laute Attribution, D-G03)
    - `grep -n "engine.begin()" tests/conftest.py` belegt explizite TX-Hygiene fuer Auto-Delete (D-G05)
    - `grep -nE "\{pk_col\}::text = ANY|_DERIVED_PK_COLS" tests/conftest.py` belegt: Auto-Delete laeuft ueber die KATALOG-ABGELEITETE PK-Spalte (Gemini-Fund #2), nicht ueber hardcoded id
    - `grep -nc "id::text = ANY" tests/conftest.py` im Auto-Delete-Pfad ist 0 (kein hardcoded-id-Delete mehr im Auto-Reset-Pfad — Gemini-Fund #2; der `::text`-Cast bleibt, nur dynamisch via {pk_col})
    - `grep -n "_DERIVED_FK_ORDER\|_DERIVED_PK_COLS" tests/conftest.py` >=1 Treffer (Modul-Level-Cache, Gemini-Fund #3 — einmal bei Session-Start gefuellt)
    - `grep -nE "_baseline_snapshot\([^)]*_baseline_schema" tests/conftest.py` liefert >=1 Treffer (Gemini-Re-Review R2 / Fund #7: `_baseline_snapshot` fordert `_baseline_schema` als Fixture-Parameter/Dependency an → harte Cache-Fill-Ordering-Garantie, Req-9 no-opt sonst still)
    - `grep -n "_CLEANUP_FK_ORDER" tests/conftest.py` >=1 Treffer (Fallback bleibt erhalten); cleanup_rows-Signatur UNVERAENDERT (kein neuer Parameter — manuell gegen die 20+ Caller verifizieren)
    - `grep -n "pytest.fail" tests/conftest.py` im missing/mutated-Pfad vorhanden (D-G02 hard-block)
    - `grep -n "from tests._schema_introspect\|import _schema_introspect\|derive_baseline_tables\|primary_key_column" tests/conftest.py` belegt Konsum des Moduls (D-G19)
    - `grep -cn "_BASELINE_PUBLIC_TABLES = \[" tests/conftest.py` == 0 (Hardcode-Liste entfernt, Req-9) — oder ein Rest ist im foundation_register/Kommentar begruendet
    - Im Gate-Lauf: 0 `_baseline_cleanup_guard`-Leak-Fails/Errors; ein absichtlicher Leak erzeugt sichtbare [BASELINE-AUTO-FIX]-Warnung MIT nodeid; Test 6 belegt `_DERIVED_FK_ORDER`/`_DERIVED_PK_COLS` NON-EMPTY nach Session-Start (Fund #7)
  </acceptance_criteria>
  <done>Waechter heilt leaked public-Rows laut, blockt missing/mutated hart, konsumiert table_list+reverse_fk_order+PK-Spalten aus Task 1; Auto-Delete ueber abgeleitete PK-Spalte (kein hardcoded id, keine Composite-PK im Pfad); cleanup_rows liest Modul-Level-Cache ohne Signatur-Aenderung/per-call-Query (Fallback _CLEANUP_FK_ORDER); `_baseline_snapshot` fordert `_baseline_schema` als Dependency → Cache garantiert vor erstem Cleanup gefuellt (Fund #7); uuid-cast + TX-Hygiene korrekt. Im Gate 0 Leak-Errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test-Body → nerve_test public.* | Test committet Rows; Waechter-Teardown loescht test-erzeugte Extra-Rows |
| Waechter → geschuetzte Baseline (id=1-Seed) | Waechter darf Baseline NIE heilen, nur sichtbar blocken |
| Katalog-Ableitung → bewachte Tabellen-Liste | eine still gedroppte Tabelle = unbewachter Leak-Pfad |
| Katalog-Ableitung → Auto-Delete-Eignung pro Tabelle | eine Composite-PK-Tabelle im Auto-Delete-Pfad = Over-Deletion/Crash |
| Cache-Init → cleanup_rows-Datenquelle | ungefuellter Cache = stiller Fallback auf Hardcode = Req-9 wirkungslos |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-GREEN-01 | Tampering (False-Green) | _baseline_cleanup_guard missing/mutated-Pfad | mitigate | D-G02 STRICT SPLIT: missing/mutated → harter pytest.fail mit nodeid, NIE Re-Insert/Heal. grep belegt pytest.fail im Pfad. Faengt die 7 base-seed-Delete-Bugs. |
| T-PGTEST-GREEN-02 | Information-Disclosure (silent-skip) | Auto-Delete von leaked Rows | mitigate | D-G03: jedes Auto-Delete emittiert [BASELINE-AUTO-FIX] mit nodeid+tbl+ids — nie stumm (Req-3/Req-7). grep belegt Tag. |
| T-PGTEST-GREEN-03 | Tampering (False-Green-Blindspot) | derive_baseline_tables Denylist | mitigate | D-G17 + Gemini-Re-Review R2 / Fund #6: Default "watch everything"; jede Exklusion ins foundation_register mit Grund + LOGGEN; PK-loses ODER Composite-PK-Tabelle nie still gedroppt (Grund "no watchable PK" bzw. "composite PK not supported for auto-delete"). grep belegt foundation_register + composite-PK-Grund + logging.warning. |
| T-PGTEST-GREEN-04 | Denial-of-Service (Waechter-Tod) | Auto-Delete FK-Order | mitigate | D-G19 + Gemini-Fund #1: Waechter konsumiert reverse_fk_order aus Modul (Kind vor Eltern), in den ALLE FK-Kanten eingehen — auch CASCADE (confdeltype='c'). Damit sortiert auch eine CASCADE->RESTRICT-Kette Leaves-vor-Roots → kein FK-Violation-Tod/DoS beim Cleanup. self-ref/mutual-Zyklus weiterhin gebrochen. grep belegt: kein confdeltype-basierter Kanten-Ausschluss mehr. |
| T-PGTEST-GREEN-05 | Tampering (uuid-cast / PK-Annahme / Over-Deletion) | Auto-Delete uuid-PK + non-id-PK + composite-PK-Tabellen | mitigate | D-G06 + Gemini-Fund #2 + Gemini-Re-Review R2 / Fund #6: `{pk_col}::text = ANY(...)` mit KATALOG-ABGELEITETER PK-Spalte (primary_key_column) statt hardcoded `id`. Der `::text`-Cast bleibt (sonst `operator does not exist: uuid = text`); der dynamische Spaltenname verhindert `column id does not exist` bei non-id-PK-Tabellen (intent_event.event_id). ZUSAETZLICH (Fund #6): Tabellen mit zusammengesetztem PK (>1 indisprimary-Zeile) werden GAR NICHT auto-geloescht — sie liegen in der Denylist (Grund "composite PK not supported for auto-delete"), sonst loescht ein Single-Column-`{first_pk_col}::text = ANY(...)` VIEL mehr Rows als test-erzeugt (Over-Deletion) oder crasht. grep belegt {pk_col}/_DERIVED_PK_COLS + composite-PK-Guard + Denylist-Grund. |
| T-PGTEST-GREEN-06 | Tampering (Meta-False-Green: stiller Hardcode-Fallback) | Modul-Cache-Init / cleanup_rows-Datenquelle | mitigate | Gemini-Re-Review R2 / Fund #7: `_baseline_snapshot` fordert `_baseline_schema` als Fixture-Parameter (Dependency) an → harte Ordering-Garantie, dass der Modul-Level-Cache (_DERIVED_FK_ORDER/_DERIVED_PK_COLS) bei Session-Start gefuellt ist, BEVOR ein cleanup_rows-Fallback triggern kann. Ohne diese Dependency faellt cleanup_rows STILL auf den Hardcode-`_CLEANUP_FK_ORDER` zurueck → die dynamische Ableitung (Req-9) ist praktisch wirkungslos, das Tor denkt es sei dynamisch, ist es aber nicht (Meta-False-Green). grep belegt `_baseline_snapshot(... _baseline_schema)`; Test 6 belegt Cache NON-EMPTY nach Session-Start. |

Block severity = high (ein False-Green auf dem Baseline-Waechter green-lightet bad deploys).
</threat_model>

<verification>
- Voller `deploy.sh production`-Gate-Lauf (gemeinsam mit Plan 02): 0 `_baseline_cleanup_guard`-Leak-Fails/Errors.
- Absichtlicher Leak-Test erzeugt sichtbare [BASELINE-AUTO-FIX]-Warnung mit nodeid.
- Abgeleitete public-Liste enthaelt calls + objection_event (Req-9-Beleg).
- CASCADE-Kante bleibt im topo-Sort (kein DoS bei CASCADE->RESTRICT-Kette); Auto-Delete laeuft ueber abgeleitete PK-Spalte (kein Crash an non-id-PK-Tabellen).
- Composite-PK-Tabelle (>1 indisprimary-Zeile) landet im foundation_register mit Grund "composite PK not supported for auto-delete", wird geloggt + NICHT auto-geloescht (Fund #6).
- Modul-Cache (_DERIVED_FK_ORDER/_DERIVED_PK_COLS) ist nach Session-Start NON-EMPTY — `_baseline_snapshot` fordert `_baseline_schema` als Dependency an, kein stiller Hardcode-Fallback (Fund #7).
- KEIN lokaler pytest als Acceptance (CLAUDE.md HART).
</verification>

<success_criteria>
- tests/_schema_introspect.py liefert (table_list, reverse_fk_order, foundation_register) aus pg_constraint mit ALLEN FK-Kanten; Composite-PK-Tabellen denylisted (Fund #6).
- conftest Auto-Reset-Waechter: leaked→loeschen+warnen (ueber abgeleitete PK-Spalte, keine Composite-PK), missing/mutated→pytest.fail.
- cleanup_rows liest Modul-Level-Cache ohne Signatur-Aenderung/per-call-Query (Fallback _CLEANUP_FK_ORDER); `_baseline_snapshot` fordert `_baseline_schema` als Dependency → Cache garantiert gefuellt (Fund #7).
- 0 Baseline-Leak-Errors im Gate; Leaks sichtbar; Isolation erhalten (Req-2/Req-3).
- Hardcode-Listen ersetzt (Req-9 public-Haelfte).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-01-SUMMARY.md`
</output>
</content>
