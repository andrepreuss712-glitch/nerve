---
phase: 08.23.2.PGTEST.GREEN
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_schema_introspect.py
  - tests/conftest.py
autonomous: true
requirements: [Req-2, Req-3, Req-9]
must_haves:
  truths:
    - "Im Gate-Lauf erscheinen 0 _baseline_cleanup_guard-Fails aus geleakten public-Extra-Rows"
    - "Ein test-erzeugter Extra-Row in einer public-Tabelle wird nach dem Test automatisch geloescht und der naechste Test sieht ihn nicht"
    - "Ein geloeschter/mutierter Baseline-Seed-Row (id=1) wirft sofort einen harten pytest.fail mit nodeid (kein Heilen)"
    - "Jedes Auto-Delete emittiert eine laute [BASELINE-AUTO-FIX]-Warnung mit nodeid + Tabelle + PKs"
    - "Die bewachte public-Tabellen-Liste + reverse-FK-Loeschorder stammen aus dem Katalog (pg_constraint topo-Sort), nicht aus einer Hardcode-Liste"
    - "Die abgeleitete Liste enthaelt mindestens eine Tabelle, die in der alten _BASELINE_PUBLIC_TABLES-Hardcode-Liste fehlte (z.B. calls/objection_event)"
  artifacts:
    - path: "tests/_schema_introspect.py"
      provides: "Katalog-Ableitung: baseline-table-list + reverse-FK-Loeschorder aus pg_constraint, parametrisiert auf (DSN/Conn, Schema)"
      contains: "def derive_baseline_tables"
    - path: "tests/conftest.py"
      provides: "Auto-Reset gespaltener Baseline-Waechter (leaked->loeschen+warnen, missing/mutated->pytest.fail)"
      contains: "[BASELINE-AUTO-FIX]"
  key_links:
    - from: "tests/conftest.py::_baseline_cleanup_guard"
      to: "tests/_schema_introspect.py::derive_baseline_tables"
      via: "import + consume (table_list + reverse_fk_order)"
      pattern: "_schema_introspect"
    - from: "tests/conftest.py auto-delete"
      to: "nerve_test public-Tabellen"
      via: "DELETE FROM {tbl} WHERE id::text = ANY(:ids) in engine.begin()/commit()"
      pattern: "id::text = ANY"
---

<objective>
Loese die suite-weite Baseline-Leak-Dimension (Req-2/Req-3) + ersetze die hardcodierten Waechter-Tabellenlisten durch Katalog-Ableitung (Req-9, public-Haelfte). Das geteilte Schema-Introspect-Modul (`tests/_schema_introspect.py`) liefert aus EINER Wahrheit (pg_constraint topo-Sort) die baseline-table-list UND die reverse-FK-Loeschorder; der Auto-Reset-gespaltene Baseline-Waechter in conftest.py KONSUMIERT beides.

Purpose: Heute verlangt der globale fail-closed `_baseline_cleanup_guard`, dass jeder der ~600 Tests die ganze public-Baseline pristine laesst → ~507 errors. Der Auto-Reset-Waechter heilt test-erzeugte Extra-Rows selbst (laut), blockt aber harte Baseline-Verletzungen (missing/mutated id=1-Seed). Das ist die ~600-Test-Wurzel.

Output: `tests/_schema_introspect.py` (NEU), erweiterter `_baseline_cleanup_guard` + `_baseline_guard_engine` + Liste-Quelle in conftest.py.

D-G19-KOPPLUNG (KRITISCH): Modul + Waechter sind EIN gekoppelter Plan. Der Waechter darf nicht gebaut werden, bevor das Modul list+order liefert — sonst stirbt der Waechter beim eigenen Cleanup an einer FK-Verletzung. Innerhalb dieses Plans: Task 1 (Modul) ZUERST, dann Task 2 (Waechter konsumiert es).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-SPEC.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-CONTEXT.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-PATTERNS.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/_green_isolation_gemini_OUT.md

<interfaces>
<!-- Bestehende conftest-Vertraege, die Task 2 erweitert (verbatim aus tests/conftest.py). -->

_diff_baseline (conftest.py:289-303) — liefert BEREITS den 3-Wege-Split, nur anders konsumieren:
```python
def _diff_baseline(current, baseline):
    drift = {}
    for tbl, base_map in baseline.items():
        cur_map = current.get(tbl, {})
        cur_pks = set(cur_map); base_pks = set(base_map)
        leaked = cur_pks - base_pks
        missing = base_pks - cur_pks
        mutated = {pk for pk in (cur_pks & base_pks) if cur_map[pk] != base_map[pk]}
        if leaked or missing or mutated:
            drift[tbl] = {"leaked": leaked, "missing": missing, "mutated": mutated}
    return drift
```

_baseline_guard_engine (conftest.py:306-321) — eigene session-scoped Read-Engine, ENTKOPPELT von der per-Test umgebundenen MODUL-SessionLocal (db_session/client machen pro Test configure(bind=None)+dispose()). nerve_app-peer, public-only, KEINE BYPASSRLS-Rolle.

id::text-Cast-Muster (cleanup_rows, conftest.py:210/221-224) — verbatim:
```python
conn.execute(text(f"DELETE FROM {tbl} WHERE id::text = ANY(:ids)"), {"ids": [str(x) for x in ids]})
```

_BASELINE_PUBLIC_TABLES (conftest.py:263-267) — alte 16er-Hardcode-Liste, wird von Task 1 abgeloest.
_CLEANUP_FK_ORDER (conftest.py:114-130) — alte reverse-FK-Hardcode-Liste, wird von Task 1 abgeloest.

Katalog-Query-Analoga (test_schild_guard.py:53-66 pg_class/pg_namespace, deploy.sh:168-174 pg_policies counts) — beweisen: Katalog-Reads sind rollen-agnostisch, nerve_app darf pg_constraint/pg_class lesen ohne GRANT.
</interfaces>
</context>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

Dieser Plan LIEST den Postgres-Katalog (pg_constraint/pg_class/pg_namespace/pg_index/pg_attribute) und LOESCHT Rows aus public.*-Tabellen in nerve_test.

### Angefasste Tabellen
- **Katalog (lesen):** pg_constraint, pg_class, pg_namespace, pg_index, pg_attribute — world-readable, rollen-agnostisch (Beleg: test_schild_guard.py liest pg_class als nerve_app ohne GRANT; deploy.sh:168 liest pg_policies als postgres).
- **public.* base-Tabellen (loeschen, NUR test-erzeugte leaked-IDs):** dynamisch abgeleitet — Default "watch everything" minus Foundation-Register-Denylist.

### inspect.sh-Beleg (PFLICHT — Executor zitiert verbatim)
Der Executor MUSS vor dem Bau die Produktions-Katalog-Wahrheit ziehen und VERBATIM in die SUMMARY uebernehmen:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh tables'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT con.conrelid::regclass::text AS child, con.confrelid::regclass::text AS parent, con.confdeltype FROM pg_constraint con JOIN pg_namespace n ON n.oid=con.connamespace WHERE con.contype='\''f'\'' AND n.nspname='\''public'\'' ORDER BY 1;"'
```
> Daraus leitet der Executor ab: welche public-base-Tabellen existieren, welche FK-Kanten/CASCADE/self-ref es gibt, und benennt im Code-Kommentar die konkreten NERVE-self-ref/mutual-FK-Tabellen (D-G16 Bau-Wachpunkt 1).

### Cross-Layer-Konsistenz-Tabelle
| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert via |
|---|---|---|---|
| FK-Kanten (child/parent/confdeltype) | gelesen in derive | pg_constraint (Katalog) | inspect.sh psql-Query oben |
| base-Tabellen-Set | gelesen in derive | pg_class relkind='r', schema | inspect.sh tables |
| PK-Spaltenname pro Tabelle | gelesen in derive (D-G17 REFINEMENT) | pg_index indisprimary + pg_attribute | psql-Query (PATTERNS.md:63-66) |
| leaked Extra-Rows | geloescht im Waechter-Teardown | public.* DB-Rows in nerve_test | _diff_baseline leaked-Set |
</persistenz_schicht_verifikation>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema-Introspect-Modul tests/_schema_introspect.py (Katalog-Ableitung, D-G16/D-G17/D-G18)</name>
  <read_first>
    - tests/test_schild_guard.py:53-66 (pg_class/pg_namespace cursor-Muster, %s-parametrisiert auf Schema — der Katalog-Read-Analog)
    - deploy.sh:168-174 (pg_policies/pg_class counts via psql -tAc als postgres — beweist Katalog rollen-agnostisch)
    - tests/conftest.py:114-130 (alte _CLEANUP_FK_ORDER — die zu ersetzende reverse-FK-Order) + :263-267 (alte _BASELINE_PUBLIC_TABLES — die zu ersetzende Liste)
    - PATTERNS.md Sektion 1 (FK-edge-Query confdeltype, self-ref child=parent, PK-name-from-catalog-Query verbatim)
    - inspect.sh-Output aus der Persistenz-Schicht-Verifikation oben (echte public-FK-Kanten + Tabellen-Set von Prod)
  </read_first>
  <behavior>
    - Test 1 (DSN-frei, gegen Fixture-Edges): topo-Sort einer kuenstlichen FK-Kanten-Liste {users->organisations, profiles->users} liefert reverse-FK-Order = [profiles, users, organisations] (Kind vor Eltern).
    - Test 2 (Zyklus, Bau-Wachpunkt 1): self-ref Kante {accounts->accounts} haengt NICHT — die Kante wird ignoriert/gebrochen, accounts erscheint genau einmal in der Order.
    - Test 3 (CASCADE, Bau-Wachpunkt 2): eine Kante mit confdeltype='c' (intent_event->calls) wird im topo-Sort uebersprungen (Eltern-Delete raeumt CASCADE-Kind), aber das Kind bleibt in der baseline-table-list (Snapshot-Pflicht).
    - Test 4 (Denylist/Foundation-Register, D-G17): alembic_version + Views/Matviews (relkind != 'r') sind NICHT in der baseline-table-list; eine Tabelle ohne bewachbaren PK landet mit Grund in der Denylist (geloggt), wird NICHT still gedroppt.
    - Test 5 (Req-9-Beleg): die abgeleitete public-Liste enthaelt calls + objection_event (die in der alten _BASELINE_PUBLIC_TABLES fehlten).
  </behavior>
  <action>
    Erstelle `tests/_schema_introspect.py` — ein geteiltes, parametrisiertes Python-Modul (D-G18), ASCII-Identifier.

    Public-API (Signaturen exakt, damit Task 2 + Plan 02 gegen sie bauen):
    ```python
    def derive_baseline_tables(conn_or_dsn, schema='public'):
        """Liefert (baseline_table_list, reverse_fk_delete_order, foundation_register).
        baseline_table_list: alle base-Tabellen (relkind='r') des Schemas minus begruendete Denylist.
        reverse_fk_delete_order: topo-sortiert Kind-vor-Eltern; CASCADE-Kinder uebersprungen (D-G16 WP2).
        foundation_register: dict {table: reason} jeder bewusst exkludierten Tabelle (D-G17, Req-7 — nie still)."""
    def primary_key_column(conn_or_cur, table, schema='public'):
        """PK-Spaltenname AUS DEM KATALOG (pg_index indisprimary), NICHT 'id' hardcoden (D-G17 REFINEMENT)."""
    ```

    Implementierung:
    1. FK-Kanten lesen (D-G16) — exakte Query aus PATTERNS.md:56-61:
       `SELECT con.conrelid::regclass::text AS child, con.confrelid::regclass::text AS parent, con.confdeltype FROM pg_constraint con JOIN pg_namespace n ON n.oid=con.connamespace WHERE con.contype='f' AND n.nspname=%s;`
       Parametrisiert auf schema (%s + Tuple, wie SCHILD).
    2. base-Tabellen lesen: `relkind='r'` filtert Views/Matviews automatisch (PATTERNS.md:46). Schema via %s.
    3. PK-Spaltenname pro Tabelle aus Katalog (PATTERNS.md:63-66):
       `SELECT a.attname FROM pg_index i JOIN pg_attribute a ON a.attrelid=i.indrelid AND a.attnum=ANY(i.indkey) WHERE i.indrelid=%s::regclass AND i.indisprimary;`
       Keine PK-Spalte gefunden → in foundation_register mit Grund "no watchable PK", LOGGEN (`logging.warning`), NICHT still droppen (D-G17 REFINEMENT).
    4. Denylist (Foundation-Register, D-G17): `alembic_version` (Reason "migration-state, not test-data"). Views/Matviews fallen schon durch relkind. crm.* fuer Plan 02 via schema='crm'.
    5. topo-Sort (Kahn ODER DFS — Discretion D-G16, mit Gemini abgewogen): nur RESTRICT/NO-ACTION-FKs als Kanten (confdeltype NOT IN ('c')) — CASCADE-Kinder (confdeltype='c') als Kanten WEGLASSEN (Eltern-Delete raeumt sie). Zyklus-Brechung (D-G16 WP1): self-ref (child==parent) und mutual FKs erkennen → diese Kante ignorieren (NICHT haengen). Im Code-Kommentar die konkreten NERVE-self-ref/mutual-Tabellen benennen (aus dem inspect.sh-FK-Output).
    6. Rueckgabe `reverse_fk_delete_order` = topo-Order Kind-vor-Eltern (so loescht der Waechter Kinder zuerst).
    7. Unit-Tests in `tests/_schema_introspect.py` selbst NICHT — eine separate `tests/test_schema_introspect.py` mit den 5 behavior-Tests (DSN-frei fuer Test 1-2 via injizierte Kanten-Liste; Test 3-5 gegen real-PG via vorhandenes Skip-when-env-Muster conftest.py:495).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_schema_introspect.py -v'</automated>
    HINWEIS: triage.sh existiert erst nach Plan 03. Bis dahin verifiziert dieser Task ueber den vollen Gate-Lauf (deploy.sh production) gemeinsam mit Task 2. Lokaler pytest ist NICHT zulaessig (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "def derive_baseline_tables" tests/_schema_introspect.py` liefert genau 1 Treffer
    - `grep -n "def primary_key_column" tests/_schema_introspect.py` liefert genau 1 Treffer
    - `grep -n "pg_constraint" tests/_schema_introspect.py` liefert >=1 Treffer (FK-Kanten-Query vorhanden)
    - `grep -n "confdeltype" tests/_schema_introspect.py` liefert >=1 Treffer (CASCADE-Awareness, D-G16 WP2)
    - `grep -n "indisprimary" tests/_schema_introspect.py` liefert >=1 Treffer (PK-from-catalog, D-G17 REFINEMENT)
    - `grep -n "foundation_register\|alembic_version" tests/_schema_introspect.py` belegt explizite Denylist mit Grund
    - `grep -rn "_CLEANUP_FK_ORDER\|_BASELINE_PUBLIC_TABLES" tests/conftest.py` → die Hardcode-Listen sind nach Task 2 entfernt/ersetzt (siehe Task 2 acceptance)
    - Im Gate-Lauf: test_schema_introspect.py erscheint als PASSED; die abgeleitete Liste enthaelt calls + objection_event (Req-9-Beleg im Log/Test-Assertion)
  </acceptance_criteria>
  <done>Modul existiert, liefert (table_list, reverse_fk_order, foundation_register) aus pg_constraint topo-Sort; PK-Name katalog-abgeleitet; Denylist begruendet; 5 behavior-Tests gruen im Gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-Reset gespaltener Baseline-Waechter in conftest.py (D-G01..D-G06, konsumiert Task 1)</name>
  <read_first>
    - tests/_schema_introspect.py (das in Task 1 gebaute Modul — Task 2 importiert es)
    - tests/conftest.py:289-303 (_diff_baseline, der 3-Wege-Split-Kern, unveraendert wiederverwendet)
    - tests/conftest.py:306-321 (_baseline_guard_engine, read-only → read+write erweitern, D-G05)
    - tests/conftest.py:343-368 (_baseline_cleanup_guard, fail-closed → Auto-Reset gespalten)
    - tests/conftest.py:152-237 (cleanup_rows — id::text-Cast-Muster, D-G06, verbatim kopieren)
    - PATTERNS.md Sektion 2 (Auto-Reset-Split-Shape, TX-Hygiene, uuid-Cast)
    - _green_isolation_gemini_OUT.md (Req-4-Beleg: Option 1 gespalten, missing/mutated MUSS fail, datiert vor Bau — commit 11b8fcf)
  </read_first>
  <behavior>
    - Test 1 (heal-leaked, D-G01): ein Test committet 1 Extra-Row in public.calls und raeumt NICHT auf → nach dem Test ist die Row weg (Auto-Delete) UND eine [BASELINE-AUTO-FIX]-Warnung mit nodeid+calls+id ist im Log. KEIN pytest.fail.
    - Test 2 (Isolation-Beleg, Req-3): zwei Tests, die dieselbe Tabelle nutzen, sehen sich nicht — der zweite startet mit der pristinen Baseline (Beleg gegen Cross-Test-Vergiftung).
    - Test 3 (STRICT SPLIT, D-G02): ein Test loescht Baseline-Row id=1 (organisations) → harter pytest.fail mit nodeid; KEIN Re-Insert (kein Heilen). Gleiches fuer mutated (xmin geaendert).
    - Test 4 (TX-Hygiene, D-G05): nach dem Auto-Delete sieht der naechste Test die geloeschte Extra-Row wirklich nicht mehr (commit ist passiert).
    - Test 5 (uuid-Cast, D-G06): Auto-Delete einer Extra-Row in einer uuid-PK-Tabelle (tenant_orgs) wirft KEINEN `operator does not exist: uuid = text` (id::text-Cast greift).
  </behavior>
  <action>
    Erweitere `tests/conftest.py` IN PLACE (D-G01..D-G06). ASCII-Identifier (`_baseline_cleanup_guard`, Tag `[BASELINE-AUTO-FIX]`).

    1. Liste-Quelle ersetzen (D-G17/D-G19): Die Hardcode-Listen `_BASELINE_PUBLIC_TABLES` (Z.263-267) und `_CLEANUP_FK_ORDER` (Z.114-130) durch session-scoped Ableitung via `tests/_schema_introspect.derive_baseline_tables(_baseline_guard_engine, schema='public')` ersetzen. Eine session-scoped Fixture `_baseline_schema` cached `(table_list, reverse_fk_order, foundation_register)` EINMAL bei Session-Start. `_snapshot_public_tables` iteriert dann ueber `table_list` statt `_BASELINE_PUBLIC_TABLES` und nutzt `primary_key_column(...)` statt hardcoded `SELECT id`. (cleanup_rows behaelt seine eigene `_CLEANUP_FK_ORDER`-Logik fuer test-eigene Teardowns NICHT — auch hier auf reverse_fk_order umstellen; falls Risiko fuer bestehende Caller, in der SUMMARY dokumentieren.)
    2. `_baseline_guard_engine` (D-G05): bleibt session-scoped, nerve_app-peer, public-only. Der Auto-Delete laeuft in `with engine.begin() as conn:` (commit-on-exit), NICHT auf der Read-Connection — explizite TX-Hygiene, damit geloeschte Rows fuer den naechsten Test wirklich weg sind.
    3. `_baseline_cleanup_guard` (D-G01/D-G02/D-G03) — Split:
       - `drift = _diff_baseline(current, _baseline_snapshot)` (unveraendert).
       - Pro Tabelle: `leaked` → SAMMELN fuer Auto-Delete. `missing` ODER `mutated` → SAMMELN fuer hard-fail.
       - Wenn missing/mutated nicht leer: SOFORT `pytest.fail(f"[BASELINE-GUARD] {request.node.nodeid}: protected baseline drifted (missing/mutated) -> ...")` — KEIN Heilen (D-G02: Re-Insert feuert trg_mk_tenant_org → neue UUID → bricht Folge-Tests).
       - Wenn nur leaked: pro betroffener Tabelle `logging.warning(f"[BASELINE-AUTO-FIX] {request.node.nodeid} leaked rows in {tbl}: {sorted(ids)}")` (D-G03), DANN `with engine.begin() as conn:` Loeschen in `reverse_fk_order` (Kind vor Eltern) via `conn.execute(text(f"DELETE FROM {tbl} WHERE id::text = ANY(:ids)"), {"ids":[str(x) for x in ids]})` (D-G06 uuid-Cast verbatim aus cleanup_rows:221-224).
    4. Scope (D-G04): NUR public.*. crm.* bleibt UNVERAENDERT POST-SUITE-Check in deploy.sh (Plan 02) — kein Auto-Clean fuer crm. Im Kommentar bekraeftigen.
    5. behavior-Tests aus oben in `tests/test_schema_introspect.py` ODER einer `tests/test_baseline_autoreset.py` (real-PG, Skip-when-env-Muster).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_baseline_autoreset.py -v'</automated>
    HINWEIS: Endgueltige Verifikation gemeinsam mit Plan 02 im vollen `deploy.sh production`-Gate-Lauf (0 _baseline_cleanup_guard-Leak-Fails). Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "\[BASELINE-AUTO-FIX\]" tests/conftest.py` liefert >=1 Treffer (laute Attribution, D-G03)
    - `grep -n "engine.begin()" tests/conftest.py` belegt explizite TX-Hygiene fuer Auto-Delete (D-G05)
    - `grep -n "id::text = ANY" tests/conftest.py` im Auto-Delete-Pfad (D-G06)
    - `grep -n "pytest.fail" tests/conftest.py` im missing/mutated-Pfad vorhanden (D-G02 hard-block)
    - `grep -n "from tests._schema_introspect\|import _schema_introspect\|derive_baseline_tables" tests/conftest.py` belegt Konsum des Moduls (D-G19)
    - `grep -cn "_BASELINE_PUBLIC_TABLES = \[" tests/conftest.py` == 0 (Hardcode-Liste entfernt, Req-9) — oder ein Rest ist im foundation_register/Kommentar begruendet
    - Im Gate-Lauf: 0 `_baseline_cleanup_guard`-Leak-Fails/Errors; ein absichtlicher Leak erzeugt sichtbare [BASELINE-AUTO-FIX]-Warnung MIT nodeid
  </acceptance_criteria>
  <done>Waechter heilt leaked public-Rows laut, blockt missing/mutated hart, konsumiert table_list+reverse_fk_order aus Task 1, uuid-cast + TX-Hygiene korrekt. Im Gate 0 Leak-Errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test-Body → nerve_test public.* | Test committet Rows; Waechter-Teardown loescht test-erzeugte Extra-Rows |
| Waechter → geschuetzte Baseline (id=1-Seed) | Waechter darf Baseline NIE heilen, nur sichtbar blocken |
| Katalog-Ableitung → bewachte Tabellen-Liste | eine still gedroppte Tabelle = unbewachter Leak-Pfad |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-GREEN-01 | Tampering (False-Green) | _baseline_cleanup_guard missing/mutated-Pfad | mitigate | D-G02 STRICT SPLIT: missing/mutated → harter pytest.fail mit nodeid, NIE Re-Insert/Heal. grep belegt pytest.fail im Pfad. Faengt die 7 base-seed-Delete-Bugs. |
| T-PGTEST-GREEN-02 | Information-Disclosure (silent-skip) | Auto-Delete von leaked Rows | mitigate | D-G03: jedes Auto-Delete emittiert [BASELINE-AUTO-FIX] mit nodeid+tbl+ids — nie stumm (Req-3/Req-7). grep belegt Tag. |
| T-PGTEST-GREEN-03 | Tampering (False-Green-Blindspot) | derive_baseline_tables Denylist | mitigate | D-G17: Default "watch everything"; jede Exklusion ins foundation_register mit Grund + LOGGEN; PK-loses Tabelle nie still gedroppt. grep belegt foundation_register. |
| T-PGTEST-GREEN-04 | Denial-of-Service (Waechter-Tod) | Auto-Delete FK-Order | mitigate | D-G19: Waechter konsumiert reverse_fk_order aus Modul (Kind vor Eltern) → kein FK-Violation-Tod beim Cleanup. CASCADE-Kinder uebersprungen, self-ref-Zyklus gebrochen. |
| T-PGTEST-GREEN-05 | Tampering (uuid-cast) | Auto-Delete uuid-PK-Tabellen | mitigate | D-G06: id::text = ANY(...) verbatim aus 10e5d0a — sonst `operator does not exist: uuid = text` → Waechter crasht → Leak unbemerkt. |

Block severity = high (ein False-Green auf dem Baseline-Waechter green-lightet bad deploys).
</threat_model>

<verification>
- Voller `deploy.sh production`-Gate-Lauf (gemeinsam mit Plan 02): 0 `_baseline_cleanup_guard`-Leak-Fails/Errors.
- Absichtlicher Leak-Test erzeugt sichtbare [BASELINE-AUTO-FIX]-Warnung mit nodeid.
- Abgeleitete public-Liste enthaelt calls + objection_event (Req-9-Beleg).
- KEIN lokaler pytest als Acceptance (CLAUDE.md HART).
</verification>

<success_criteria>
- tests/_schema_introspect.py liefert (table_list, reverse_fk_order, foundation_register) aus pg_constraint.
- conftest Auto-Reset-Waechter: leaked→loeschen+warnen, missing/mutated→pytest.fail.
- 0 Baseline-Leak-Errors im Gate; Leaks sichtbar; Isolation erhalten (Req-2/Req-3).
- Hardcode-Listen ersetzt (Req-9 public-Haelfte).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-01-SUMMARY.md`
</output>
