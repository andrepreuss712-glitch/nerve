---
phase: 08.23.2.PGTEST.GREEN
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/_schema_introspect.py
  - tests/conftest.py
autonomous: true
requirements: [Req-2, Req-3, Req-9]
must_haves:
  truths:
    - "Im Gate-Lauf erscheinen 0 _baseline_cleanup_guard-Fails aus geleakten public-Extra-Rows"
    - "Ein test-erzeugter Extra-Row in einer public-Tabelle wird nach dem Test automatisch geloescht und der naechste Test sieht ihn nicht"
    - "Ein geloeschter/mutierter Baseline-Seed-Row (id=1) wirft sofort einen harten pytest.fail mit nodeid (kein Heilen)"
    - "Jedes Auto-Delete emittiert eine laute [BASELINE-AUTO-FIX]-Warnung mit nodeid + Tabelle + PKs"
    - "Die bewachte public-Tabellen-Liste + reverse-FK-Loeschorder stammen aus dem Katalog (pg_constraint topo-Sort), nicht aus einer Hardcode-Liste"
    - "Die abgeleitete Liste enthaelt mindestens eine Tabelle, die in der alten _BASELINE_PUBLIC_TABLES-Hardcode-Liste fehlte (z.B. calls/objection_event)"
    - "ALLE FK-Kanten (auch CASCADE, confdeltype='c') sind im topo-Sort — auch eine CASCADE->RESTRICT-Kette sortiert Leaves-vor-Roots, sodass der Waechter-Cleanup nie an einer FK-Violation crasht (kein DoS)"
    - "Das Auto-Delete loescht ueber die KATALOG-ABGELEITETE PK-Spalte ({pk_col}::text = ANY(:ids)), nicht ueber hardcoded id — eine non-id-PK-Tabelle (intent_event.event_id) crasht NICHT mit 'column id does not exist'"
    - "Eine Tabelle mit zusammengesetztem PK (>1 indisprimary-Zeile) wird NICHT fuers Auto-Delete freigegeben, sondern mit Grund 'composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)' ins foundation_register (Denylist) gelegt und geloggt (logging.warning) — KEINE Ueber-Loeschung/Crash, KEINE falsche Sichtbarkeits-Behauptung (Gemini-Re-Review R2 / Fund #6 + Gemini-Re-Review R3 / Fund #9). EMPIRISCH: 0 composite-PK-Tabellen in public/crm/training per 2026-06-16 (prod-Katalog-Check), die Luecke ist heute LATENT/leer"
    - "derive_baseline_tables ist parametrisiert auf MEHRERE Schemas (schemas-Tuple, n.nspname IN %s) — der zurueckgegebene reverse_fk_delete_order ist eine GLOBALE CROSS-SCHEMA-topo-Order (crm-Kinder VOR ihren public-Eltern, z.B. crm.accounts vor public.tenant_orgs), sodass cleanup_rows (das crm UND public raeumt, D-G04) keine FK-Violation wirft (Gemini-Re-Review R3 / Fund #8)"
    - "cleanup_rows liest die dynamische reverse-FK-Order/PK-Spalten aus einem MODUL-LEVEL-Cache (einmal bei Session-Start ueber ALLE Schemas gefuellt) — KEINE Signatur-Aenderung, KEINE per-call DB-Query; der Snapshot-WAECHTER filtert die table_list dagegen LOKAL auf public. (D-G04 public-only bleibt)"
    - "Der Modul-Level-Cache (_DERIVED_FK_ORDER / _DERIVED_PK_COLS) ist nach Session-Start garantiert GEFUELLT (cross-schema), weil _baseline_snapshot _baseline_schema als Fixture-Parameter (Dependency) anfordert — die dynamische Ableitung (Req-9) faellt NICHT still auf den Hardcode-Fallback zurueck (Gemini-Re-Review R2 / Fund #7)"
  artifacts:
    - path: "tests/_schema_introspect.py"
      provides: "Katalog-Ableitung: baseline-table-list + reverse-FK-Loeschorder aus pg_constraint, parametrisiert auf (conn, schemas-Tuple) — CROSS-SCHEMA-topo-Sort (Fund #8); composite-PK-Tabellen ins foundation_register (nicht auto-delete-faehig UND nicht snapshot-ueberwacht, Fund #9)"
      contains: "def derive_baseline_tables"
    - path: "tests/conftest.py"
      provides: "Auto-Reset gespaltener Baseline-Waechter (leaked->loeschen+warnen, missing/mutated->pytest.fail); _baseline_snapshot fordert _baseline_schema als Dependency (Cache-Fill-Garantie, cross-schema); Snapshot-Waechter public-only gefiltert"
      contains: "[BASELINE-AUTO-FIX]"
  key_links:
    - from: "tests/conftest.py::_baseline_cleanup_guard"
      to: "tests/_schema_introspect.py::derive_baseline_tables"
      via: "import + consume (table_list public-gefiltert + cross-schema reverse_fk_order)"
      pattern: "_schema_introspect"
    - from: "tests/conftest.py auto-delete"
      to: "nerve_test public-Tabellen"
      via: "DELETE FROM {tbl} WHERE {pk_col}::text = ANY(:ids) in engine.begin()/commit(), pk_col aus primary_key_column()"
      pattern: "::text = ANY"
    - from: "tests/conftest.py::cleanup_rows"
      to: "_DERIVED_FK_ORDER (cross-schema)"
      via: "Modul-Cache cross-schema gefuellt -> crm vor public -> keine FK-Violation (Fund #8)"
      pattern: "_DERIVED_FK_ORDER"
    - from: "tests/conftest.py::_baseline_snapshot"
      to: "tests/conftest.py::_baseline_schema"
      via: "Fixture-Parameter (Dependency) -> garantiert Modul-Cache-Fill bei Session-Start (Fund #7)"
      pattern: "_baseline_snapshot\\([^)]*_baseline_schema"
---

<objective>
Loese die suite-weite Baseline-Leak-Dimension (Req-2/Req-3) + ersetze die hardcodierten Waechter-Tabellenlisten durch Katalog-Ableitung (Req-9, public-Haelfte). Das geteilte Schema-Introspect-Modul (`tests/_schema_introspect.py`) liefert aus EINER Wahrheit (pg_constraint topo-Sort) die baseline-table-list UND die reverse-FK-Loeschorder; der Auto-Reset-gespaltene Baseline-Waechter in conftest.py KONSUMIERT beides.

Purpose: Heute verlangt der globale fail-closed `_baseline_cleanup_guard`, dass jeder der ~600 Tests die ganze public-Baseline pristine laesst → ~507 errors. Der Auto-Reset-Waechter heilt test-erzeugte Extra-Rows selbst (laut), blockt aber harte Baseline-Verletzungen (missing/mutated id=1-Seed). Das ist die ~600-Test-Wurzel.

Output: `tests/_schema_introspect.py` (NEU), erweiterter `_baseline_cleanup_guard` + `_baseline_guard_engine` + Liste-Quelle in conftest.py.

D-G19-KOPPLUNG (KRITISCH): Modul + Waechter sind EIN gekoppelter Plan. Der Waechter darf nicht gebaut werden, bevor das Modul list+order liefert — sonst stirbt der Waechter beim eigenen Cleanup an einer FK-Verletzung. Innerhalb dieses Plans: Task 1 (Modul) ZUERST, dann Task 2 (Waechter konsumiert es).

THEMA-KLAMMER (Gemini-Review-Fold 2026-06-16): topo-Sort + Auto-Delete VOLL katalog-allgemein machen, nicht halb. (a) ALLE FK-Kanten in den Sort (auch CASCADE — sonst Waechter-DoS bei CASCADE->RESTRICT-Ketten); (b) Auto-Delete ueber die abgeleitete PK-Spalte, nicht hardcoded id (sonst Crash an non-id-PK-Tabellen wie intent_event.event_id); (c) cleanup_rows liest die dynamische Order aus einem Modul-Level-Cache, KEINE Signatur-Aenderung / keine per-call DB-Query.

THEMA-KLAMMER R2 (Gemini-Re-Review R2 / Fund #6 + #7 — vervollstaendigt die PK-Generalitaet + schliesst das stille-Fallback-Loch): (d) PK-Generalitaet ZU ENDE (Fund #6, der Rest von #2): eine Tabelle mit zusammengesetztem PK (>1 indisprimary-Zeile) wird NICHT fuers Auto-Delete freigegeben — sonst loescht `{first_pk_col}::text = ANY(...)` viel mehr Rows als test-erzeugt (Over-Deletion) oder crasht. Solche Tabellen landen mit Grund "composite PK..." im foundation_register (Denylist, Req-7), geloggt, NICHT still gedroppt. (e) Cache-Fill-Garantie (Fund #7): `_baseline_snapshot` fordert `_baseline_schema` als Fixture-Parameter (Dependency) an → der Modul-Level-Cache ist bei Session-Start hart garantiert gefuellt, BEVOR irgendein Fallback-Pfad triggern kann; sonst no-opt Req-9 still (Meta-False-Green: Tor denkt es ist dynamisch, ist es nicht).

THEMA-KLAMMER R3 (Gemini-Re-Review R3 / Fund #8 + #9 — schliesst die Cross-Schema-Naht + die unehrliche Composite-PK-Behauptung):
(f) CROSS-SCHEMA-FK-Regression (Fund #8, BLOCKER — Regression AUS dem #3-Fold): `cleanup_rows` raeumt crm UND public auf (Plan 02 Z.109, D-G04 crm-Writer-Self-Clean). Der #3-Fold stellte cleanup_rows auf den Modul-Cache `_DERIVED_FK_ORDER` um, der aber NUR mit public-Tabellen gefuellt war (`schema='public'`). crm-Tabellen fielen in den Fallback-Bucket ans ENDE der Lösch-Liste → public zuerst geloescht → `crm.accounts → public.tenant_orgs` (FK) → harte FK-Violation → jeder crm+public-raeumende Test crasht; non-id-PK crm-Tabellen crashten zusaetzlich (nicht in _DERIVED_PK_COLS). FIX: `derive_baseline_tables` nimmt MEHRERE Schemas (`schemas`-Tuple, `n.nspname IN %s`, exakt wie der SCHILD-Guard); conftest fuellt den Cache EINMAL fuer ALLE Schemas (`('public','crm','training')`) → der reverse_fk_delete_order ist eine GLOBALE Cross-Schema-topo-Order (crm-Kinder VOR ihren public-Eltern). Der Snapshot-WAECHTER (`_snapshot_public_tables`/`_baseline_cleanup_guard`) filtert die table_list dagegen LOKAL auf public (`[t for t in table_list if t.startswith('public.')]`) — D-G04 public-only bleibt erhalten (nur der Waechter-Snapshot + Auto-Delete sind public-only; crm bleibt POST-SUITE-Check in deploy.sh/Plan 02). cleanup_rows nutzt die VOLLE Cross-Schema-Order aus dem Cache.
(g) Composite-PK False-Green + unehrliche Behauptung (Fund #9, HOCH — der Rest von #6): der #6-Fold schloss Composite-PK-Tabellen korrekt aus dem Auto-Delete aus, ABER der Plan behauptete falsch, ein Leak dort sei "ueber den missing/mutated-bzw.-Snapshot-Pfad sichtbar". Technisch unmoeglich: ausgeschlossen = nie iteriert von `_snapshot_public_tables` = nie in current/baseline = `_diff_baseline` sieht sie nie → ein komplett BLINDES Loch (False-Green), der Plan log ueber die Sicherheit (verletzt Req-7). FIX (BRANCH B, ehrliche Doku — denn es gibt HEUTE 0 composite-PK-Tabellen): die falsche Sichtbarkeits-Behauptung RAUS; composite-PK-Tabellen werden EHRLICH als bekannte Tor-Luecke dokumentiert (weder auto-deleted NOCH snapshot-ueberwacht), Grund im foundation_register macht das explizit ("composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)"), logging.warning. PROD-KATALOG-FAKT (2026-06-16, sudo -u postgres psql gegen live nerve auf 178.104.82.166): 0 composite-PK-Tabellen in public (38 base-Tabellen), crm (5), training (2) → die Luecke ist heute LATENT/leer, deshalb ist Dokumentieren (Branch B) korrekt statt Tuple-Key-Rebuild (Branch A). FOLLOW-UP (deferred, NICHT jetzt gebaut, YAGNI): falls TAXO (oder eine kuenftige Migration) je eine composite-PK-Tabelle einfuehrt, MUSS `_snapshot_public_tables` auf einen Tuple-Key (alle PK-Spalten als dict-Key) umgebaut werden, um die Luecke zu schliessen — als Backlog-999.x-Folge erfasst (Stil D-G10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-SPEC.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-CONTEXT.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-PATTERNS.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/_green_isolation_gemini_OUT.md

<interfaces>
<!-- Bestehende conftest-Vertraege, die Task 2 erweitert (verbatim aus tests/conftest.py). -->

_diff_baseline (conftest.py:289-303) — liefert BEREITS den 3-Wege-Split, nur anders konsumieren:
```python
def _diff_baseline(current, baseline):
    drift = {}
    for tbl, base_map in baseline.items():
        cur_map = current.get(tbl, {})
        cur_pks = set(cur_map); base_pks = set(base_map)
        leaked = cur_pks - base_pks
        missing = base_pks - cur_pks
        mutated = {pk for pk in (cur_pks & base_pks) if cur_map[pk] != base_map[pk]}
        if leaked or missing or mutated:
            drift[tbl] = {"leaked": leaked, "missing": missing, "mutated": mutated}
    return drift
```

_baseline_guard_engine (conftest.py:306-321) — eigene session-scoped Read-Engine, ENTKOPPELT von der per-Test umgebundenen MODUL-SessionLocal (db_session/client machen pro Test configure(bind=None)+dispose()). nerve_app-peer, public-only, KEINE BYPASSRLS-Rolle.

id::text-Cast-Muster (cleanup_rows, conftest.py:210/221-224) — verbatim (der `::text`-Cast bleibt; nur der COLUMN-NAME wird in Task 2 dynamisch/katalog-abgeleitet):
```python
conn.execute(text(f"DELETE FROM {tbl} WHERE id::text = ANY(:ids)"), {"ids": [str(x) for x in ids]})
```

_BASELINE_PUBLIC_TABLES (conftest.py:263-267) — alte 16er-Hardcode-Liste, wird von Task 1 abgeloest.
_CLEANUP_FK_ORDER (conftest.py:114-130) — alte reverse-FK-Hardcode-Liste; sie sortiert crm.* VOR public.* (crm.account_memory ... crm.user_preferences, dann public.objection_event ... public.organisations), GENAU weil `crm.accounts -> public.tenant_orgs` ein FK ist (crm-Kinder vor public-Eltern). BLEIBT als best-effort-Fallback fuer cleanup_rows (Gemini-Fund #3); die primaere Quelle wird ein Modul-Level-Cache, der jetzt CROSS-SCHEMA gefuellt wird (Fund #8), damit der dynamische Pfad dieselbe crm-vor-public-Order liefert wie der Hardcode-Fallback.

Katalog-Query-Analoga (test_schild_guard.py:53-66 pg_class/pg_namespace MIT `n.nspname IN (...)` + SCHEMAS-Tuple, deploy.sh:168-174 pg_policies counts) — beweisen: Katalog-Reads sind rollen-agnostisch UND mehr-schema-faehig (SCHILD liest public+crm+training in EINER Query via `IN %s`-Tuple). derive_baseline_tables baut genau auf diesem Muster (`n.nspname IN %s`, schemas-Tuple) — nerve_app darf pg_constraint/pg_class lesen ohne GRANT.
</interfaces>
</context>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

Dieser Plan LIEST den Postgres-Katalog (pg_constraint/pg_class/pg_namespace/pg_index/pg_attribute) und LOESCHT Rows aus public.*-Tabellen in nerve_test. Die Ableitung selbst ist CROSS-SCHEMA (public+crm+training), damit cleanup_rows (das crm UND public raeumt) eine korrekte globale FK-Order bekommt (Fund #8); der Snapshot-Waechter bleibt public-only (D-G04).

### Angefasste Tabellen
- **Katalog (lesen):** pg_constraint, pg_class, pg_namespace, pg_index, pg_attribute — world-readable, rollen-agnostisch (Beleg: test_schild_guard.py liest pg_class als nerve_app ohne GRANT, MIT `n.nspname IN (...)`-Multi-Schema; deploy.sh:168 liest pg_policies als postgres).
- **public.* base-Tabellen (loeschen, NUR test-erzeugte leaked-IDs):** dynamisch abgeleitet — Default "watch everything" minus Foundation-Register-Denylist. Der Snapshot-Waechter filtert die table_list LOKAL auf public. Geloescht wird ueber die KATALOG-ABGELEITETE PK-Spalte pro Tabelle (D-G17 REFINEMENT, Gemini-Fund #2), NICHT ueber hardcoded id. Tabellen mit zusammengesetztem PK werden GAR NICHT auto-geloescht UND GAR NICHT snapshot-ueberwacht (Gemini-Re-Review R2 / Fund #6 + R3 / Fund #9) — sie liegen in der Denylist (bekannte Tor-Luecke, heute leer).
- **crm.* / training.* (NUR in der Cross-Schema-Ableitung fuer die FK-Order, NICHT im Waechter-Snapshot):** der Cache wird ueber alle drei Schemas gefuellt, damit cleanup_rows crm vor public loescht (Fund #8). Der in-pytest-Waechter snapshottet/loescht crm NIE (nerve_app saehe crm nur tenant-gefiltert) — crm bleibt der POST-SUITE deploy.sh-Check (Plan 02).

### inspect.sh-Beleg (PFLICHT — Executor zitiert verbatim)
Der Executor MUSS vor dem Bau die Produktions-Katalog-Wahrheit ziehen und VERBATIM in die SUMMARY uebernehmen:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh tables'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT con.conrelid::regclass::text AS child, con.confrelid::regclass::text AS parent, con.confdeltype FROM pg_constraint con JOIN pg_namespace n ON n.oid=con.connamespace WHERE con.contype='\''f'\'' AND n.nspname IN ('\''public'\'','\''crm'\'','\''training'\'') ORDER BY 1;"'
```
> Daraus leitet der Executor ab: welche base-Tabellen pro Schema existieren, welche FK-Kanten/CASCADE/self-ref/CROSS-SCHEMA-Kanten es gibt (insb. `crm.accounts -> public.tenant_orgs`), und benennt im Code-Kommentar die konkreten NERVE-self-ref/mutual-FK-Tabellen (D-G16 Bau-Wachpunkt 1). Achtung: CASCADE-Kanten werden ALS KANTEN MITGENOMMEN (Gemini-Fund #1) — der Executor prueft im FK-Output explizit, ob es CASCADE->RESTRICT-Ketten gibt (z.B. intent_event->calls->...), und belegt im Kommentar, dass diese Kette korrekt Leaves-vor-Roots sortiert. Cross-Schema (Fund #8): der Executor belegt im Kommentar, dass `crm.accounts` VOR `public.tenant_orgs` in der globalen reverse_fk_delete_order steht. BAU-WACHPUNKT (Naming-Konsistenz, Fund #8): `con.conrelid::regclass::text` liefert fuer Tabellen im search_path UNQUALIFIZIERTE Namen — der Executor MUSS die Namen konsistent schema-qualifizieren (Form `schema.table`, z.B. erzwingen via `n.nspname || '.' || ...` oder per-Schema-Query), damit `_normalize_table_name`s `schema.table`-Output (conftest.py:133-149), der `startswith('public.')`-Waechter-Filter und `_CLEANUP_FK_ORDER`s qualifizierte Eintraege (crm.x / public.x) alle aufeinander passen. Zusaetzlich (Gemini-Re-Review R2 / Fund #6 + R3 / Fund #9) zieht der Executor die PK-Spalten-Wahrheit ueber ALLE drei Schemas, um Composite-PK-Tabellen zu identifizieren:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT n.nspname AS schema, t.relname AS tbl, count(*) AS pk_cols FROM pg_index i JOIN pg_class t ON t.oid=i.indrelid JOIN pg_namespace n ON n.oid=t.relnamespace WHERE i.indisprimary AND n.nspname IN ('\''public'\'','\''crm'\'','\''training'\'') GROUP BY 1,2 HAVING count(*) > 1 ORDER BY 1,2;"'
```
> PROD-KATALOG-FAKT (bereits gezogen 2026-06-16, vom Plan-Author): obige Query gegen live nerve (178.104.82.166) liefert **KEINE Zeilen** → **0 composite-PK-Tabellen** in public (38 base-Tabellen), crm (5), training (2). Die composite-PK-Denylist ist damit heute LATENT/leer (nichts faellt aktuell hinein). Genau DESHALB ist Branch B (ehrlich dokumentieren statt jetzt Tuple-Key bauen, Fund #9) korrekt. Jede Tabelle mit pk_cols > 1 — falls je eine auftaucht — MUSS im Code-Kommentar benannt + ins foundation_register mit Grund "composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)" gelegt + geloggt werden, und der Tuple-Key-Folge-Build (Backlog-999.x) wird scharf.

### Cross-Layer-Konsistenz-Tabelle
| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert via |
|---|---|---|---|
| FK-Kanten (child/parent/confdeltype) ALLER 3 Schemas | gelesen in derive (ALLE Kanten in den Sort, cross-schema, Fund #8) | pg_constraint (Katalog) | inspect.sh psql-Query oben (n.nspname IN public/crm/training) |
| base-Tabellen-Set pro Schema | gelesen in derive (schemas-Tuple) | pg_class relkind='r', schema | inspect.sh tables |
| PK-Spaltenname pro Tabelle | gelesen in derive (D-G17 REFINEMENT) + im Auto-Delete VERWENDET | pg_index indisprimary + pg_attribute | psql-Query (PATTERNS.md) |
| PK-Spalten-ANZAHL pro Tabelle (3 Schemas) | gelesen in derive (Composite-PK-Guard, Fund #6/#9) | pg_index indisprimary count | psql HAVING count(*)>1 Query oben → 0 Zeilen (2026-06-16) |
| composite-PK-Tabellen | NICHT geloescht UND NICHT snapshot-ueberwacht; ins foundation_register (bekannte Luecke) | n/a (ausgeschlossen, heute leer) | foundation_register-Eintrag + logging.warning |
| cross-schema reverse_fk_delete_order | aufgebaut in derive, in cleanup_rows konsumiert (crm vor public) | Modul-Cache _DERIVED_FK_ORDER | Test: crm-vor-public, crm.accounts vor public.tenant_orgs |
| leaked Extra-Rows (NUR public im Waechter) | geloescht im Waechter-Teardown via {pk_col}::text | public.* DB-Rows in nerve_test | _diff_baseline leaked-Set (public-gefiltert) |
</persistenz_schicht_verifikation>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema-Introspect-Modul tests/_schema_introspect.py (Katalog-Ableitung, CROSS-SCHEMA, D-G16/D-G17/D-G18)</name>
  <read_first>
    - tests/test_schild_guard.py:53-66 (pg_class/pg_namespace cursor-Muster, `n.nspname IN (...)` MIT SCHEMAS-Tuple — der Multi-Schema-Katalog-Read-Analog, Fund #8)
    - deploy.sh:168-174 (pg_policies/pg_class counts via psql -tAc als postgres — beweist Katalog rollen-agnostisch)
    - tests/conftest.py:114-130 (alte _CLEANUP_FK_ORDER — die reverse-FK-Order; crm VOR public, weil crm.accounts->public.tenant_orgs; wird zum best-effort-Fallback, die dynamische Cross-Schema-Order muss dieselbe crm-vor-public-Reihenfolge liefern) + :263-267 (alte _BASELINE_PUBLIC_TABLES — die zu ersetzende Liste)
    - PATTERNS.md Sektion 1 (FK-edge-Query confdeltype, self-ref child=parent, PK-name-from-catalog-Query verbatim, Composite-PK-Denylist-Note, Cross-Schema-Note)
    - inspect.sh-Output aus der Persistenz-Schicht-Verifikation oben (echte FK-Kanten ALLER 3 Schemas inkl. crm.accounts->public.tenant_orgs + Tabellen-Set + Composite-PK-Tabellen von Prod = 0 per 2026-06-16)
  </read_first>
  <behavior>
    - Test 1 (DSN-frei, gegen Fixture-Edges): topo-Sort einer kuenstlichen FK-Kanten-Liste {users->organisations, profiles->users} liefert reverse-FK-Order = [profiles, users, organisations] (Kind vor Eltern).
    - Test 2 (Zyklus, Bau-Wachpunkt 1): self-ref Kante {accounts->accounts} haengt NICHT — die Kante wird ignoriert/gebrochen, accounts erscheint genau einmal in der Order.
    - Test 3 (CASCADE-Kante BLEIBT im Sort, Bau-Wachpunkt 2 — Gemini-Fund #1): eine Kette mit einer CASCADE-Kante (confdeltype='c', z.B. intent_event->calls) UND einer RESTRICT-Kante darunter (calls->organisations) → die CASCADE-Kante wird NICHT uebersprungen, sondern bleibt als Kante im topo-Sort. Assertion: das CASCADE-Kind (intent_event) steht VOR seinem Eltern (calls) in der reverse-FK-Order UND bleibt in der baseline-table-list (Snapshot-Pflicht). Beweist: Leaves-vor-Roots haelt auch ueber eine CASCADE->RESTRICT-Kette → kein Waechter-DoS.
    - Test 4 (Denylist/Foundation-Register, D-G17): alembic_version + Views/Matviews (relkind != 'r') sind NICHT in der baseline-table-list; eine Tabelle ohne bewachbaren PK landet mit Grund in der Denylist (geloggt), wird NICHT still gedroppt.
    - Test 5 (Req-9-Beleg): die abgeleitete public-Liste enthaelt calls + objection_event (die in der alten _BASELINE_PUBLIC_TABLES fehlten).
    - Test 6 (PK-from-catalog, D-G17 REFINEMENT — Gemini-Fund #2): primary_key_column liefert fuer eine non-id-PK-Tabelle den korrekten Spaltennamen (z.B. intent_event -> 'event_id', nicht 'id'); fuer eine id-PK-Tabelle 'id'. Beweist: das spaetere Auto-Delete (Task 2) bekommt die richtige PK-Spalte pro Tabelle.
    - Test 7 (Composite-PK-Guard, Gemini-Re-Review R2 / Fund #6 + R3 / Fund #9 — Rest von #2): eine Tabelle mit zusammengesetztem PK (Katalog-Query liefert >1 indisprimary-Zeile, z.B. eine kuenstliche/injizierte Join-Tabelle mit (a_id, b_id) als PK) landet NICHT in der auto-delete-faehigen baseline_table_list, sondern im foundation_register mit Grund "composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)". Assertion: (a) die Tabelle ist im foundation_register mit genau diesem Grund; (b) primary_key_column gibt fuer sie KEINE einzelne Spalte zur Auto-Delete-Nutzung frei (bzw. die Derivation markiert sie als nicht-watchbar); (c) logging.warning wurde emittiert (caplog). Beweist: keine Ueber-Loeschung/kein Crash an Composite-PK-Tabellen UND keine falsche Sichtbarkeits-Behauptung (Fund #9 — der Grund sagt ehrlich, dass sie auch NICHT snapshot-ueberwacht wird). HINWEIS: heute existiert keine solche Tabelle (Prod-Katalog 2026-06-16 = 0) → Test laeuft gegen einen INJIZIERTEN >1-pk_cols-Fall, nicht gegen eine echte Prod-Tabelle.
    - Test 8 (CROSS-SCHEMA-FK-Order, Gemini-Re-Review R3 / Fund #8): bei Aufruf mit `schemas=('public','crm','training')` und einer FK-Kante `crm.accounts -> public.tenant_orgs` (cross-schema) steht in der reverse_fk_delete_order `crm.accounts` VOR `public.tenant_orgs` (crm-Kind vor public-Eltern). Assertion: beide Tabellen schema-qualifiziert (`crm.accounts`, `public.tenant_orgs`) UND die Index-Position von crm.accounts < die von public.tenant_orgs. Beweist: cleanup_rows (crm+public) loescht crm zuerst → keine FK-Violation (Fund #8). Kann DSN-frei via injizierte cross-schema-Kanten-Liste laufen.
  </behavior>
  <action>
    Erstelle `tests/_schema_introspect.py` — ein geteiltes, parametrisiertes Python-Modul (D-G18), ASCII-Identifier.

    Public-API (Signaturen exakt, damit Task 2 + Plan 02 gegen sie bauen — Gemini-Re-Review R3 / Fund #8: MULTI-SCHEMA-Signatur):
    ```python
    def derive_baseline_tables(conn_or_dsn, schemas=('public',)):
        """Liefert (baseline_table_list, reverse_fk_delete_order, foundation_register).
        schemas: ein Tuple von Schema-Namen (z.B. ('public',), ('crm',) oder ('public','crm','training')).
            Die Katalog-Queries parametrisieren auf `n.nspname IN %s` mit dem Tuple — exakt wie der
            SCHILD-Guard (test_schild_guard.py) public+crm+training in EINER Query liest (Gemini-Fund #8).
            Alle zurueckgegebenen Tabellen sind SCHEMA-QUALIFIZIERT ('schema.table').
        baseline_table_list: alle base-Tabellen (relkind='r') der Schemas minus begruendete Denylist
            (inkl. composite-PK-Tabellen, die NICHT auto-delete-faehig sind — Fund #6).
        reverse_fk_delete_order: GLOBALER CROSS-SCHEMA-topo-Sort Kind-vor-Eltern; ALLE FK-Kanten gehen
            in den Sort (auch CASCADE/confdeltype='c') — garantiert Leaves-vor-Roots auch ueber
            CASCADE->RESTRICT-Ketten (D-G16 WP2, Gemini-Fund #1) UND ueber CROSS-SCHEMA-Kanten
            (crm.accounts -> public.tenant_orgs → crm.accounts steht VOR public.tenant_orgs, Fund #8).
            Nur self-ref/mutual-Kanten werden zur Zyklus-Brechung ignoriert.
        foundation_register: dict {table: reason} jeder bewusst exkludierten Tabelle (D-G17, Req-7 — nie still).
            Enthaelt u.a. composite-PK-Tabellen mit reason
            'composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)' (Fund #6/#9)."""
    def primary_key_column(conn_or_cur, table, schema='public'):
        """PK-Spaltenname AUS DEM KATALOG (pg_index indisprimary), NICHT 'id' hardcoden (D-G17 REFINEMENT).
        Wird von Task 2 fuer das Auto-Delete pro Tabelle konsumiert (Gemini-Fund #2).
        Composite PK (>1 indisprimary-Zeile): die Tabelle ist NICHT auto-delete-faehig (Gemini-Fund #6) —
        sie wird in der Derivation ins foundation_register gelegt (Grund 'composite PK: not auto-delete-
        eligible AND not snapshot-monitored (known gate gap)') und NICHT fuer den Auto-Delete-Pfad
        freigegeben (gibt keine einzelne Spalte zurueck)."""
    ```

    Implementierung:
    1. FK-Kanten lesen (D-G16, CROSS-SCHEMA — Gemini-Fund #8) — Query aus PATTERNS.md, parametrisiert auf das schemas-Tuple via `IN %s` (wie SCHILD):
       `SELECT con.conrelid::regclass::text AS child, con.confrelid::regclass::text AS parent, con.confdeltype FROM pg_constraint con JOIN pg_namespace n ON n.oid=con.connamespace WHERE con.contype='f' AND n.nspname IN %s;`
       Parametrisiert auf das schemas-Tuple. NAMING-KONSISTENZ (Bau-Wachpunkt, Fund #8): `::regclass::text` liefert fuer search_path-Schemas UNQUALIFIZIERTE Namen — die Namen MUESSEN konsistent schema-qualifiziert werden (Form `schema.table`), damit der startswith('public.')-Filter (Task 2) und `_CLEANUP_FK_ORDER`s qualifizierte Eintraege passen. Cross-Schema-Kanten (z.B. `crm.accounts -> public.tenant_orgs`) MUESSEN korrekt mit Schema erfasst werden.
    2. base-Tabellen lesen (CROSS-SCHEMA): `relkind='r'` filtert Views/Matviews automatisch (PATTERNS.md). Schema via `IN %s`-Tuple. Tabellen schema-qualifiziert zurueckgeben.
    3. PK-Spaltenname + PK-Spalten-ANZAHL pro Tabelle aus Katalog (PATTERNS.md):
       `SELECT a.attname FROM pg_index i JOIN pg_attribute a ON a.attrelid=i.indrelid AND a.attnum=ANY(i.indkey) WHERE i.indrelid=%s::regclass AND i.indisprimary;`
       - `len(pk_cols) == 0` (keine PK-Spalte gefunden) → in foundation_register mit Grund "no watchable PK", LOGGEN (`logging.warning`), NICHT still droppen (D-G17 REFINEMENT).
       - `len(pk_cols) == 1` → primary_key_column(...) gibt den abgeleiteten Namen zurueck (z.B. 'event_id' fuer intent_event), damit Task 2 das Auto-Delete ueber die korrekte Spalte fahren kann (Gemini-Fund #2).
       - `len(pk_cols) != 1` (zusammengesetzter PK, >1 Zeile) → die Tabelle wird NICHT fuers Auto-Delete freigegeben (Gemini-Re-Review R2 / Fund #6, der Rest von #2): sonst loescht `{first_pk_col}::text = ANY(...)` mit nur der ersten Spalte VIEL mehr Rows als test-erzeugt (Over-Deletion) oder crasht. Stattdessen → foundation_register mit Grund "composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)", `logging.warning` (sichtbar, Req-7, NIE still gedroppt). EHRLICHKEIT (Gemini-Re-Review R3 / Fund #9): der Grund sagt EXPLIZIT, dass die Tabelle WEDER auto-deleted NOCH snapshot-ueberwacht wird — sie ist ein unueberwachter blinder Fleck, OFFEN protokolliert, NICHT faelschlich als "ueber den Snapshot/missing-mutated-Pfad sichtbar" behauptet (das waere technisch unmoeglich, weil sie nie in current/baseline iteriert wird). Die Tabelle ist NICHT in der auto-delete-faehigen baseline_table_list. `primary_key_column` gibt fuer eine solche Tabelle KEINE einzelne Spalte zur Auto-Delete-Nutzung frei. PROD-FAKT: heute 0 solche Tabellen (2026-06-16) → die Luecke ist leer; der Tuple-Key-Umbau ist ein FOLLOW-UP (Backlog-999.x), KEIN Bau jetzt (YAGNI). FOLLOW-UP-NOTIZ im Code-Kommentar + foundation_register-Grund referenzieren: falls je eine composite-PK-Tabelle auftaucht, `_snapshot_public_tables` auf Tuple-Key (alle PK-Spalten) umbauen.
    4. Denylist (Foundation-Register, D-G17): `alembic_version` (Reason "migration-state, not test-data"). Views/Matviews fallen schon durch relkind. composite-PK-Tabellen via Schritt 3 (Fund #6/#9). Cross-Schema: training.transcript_archive bleibt der POST-SUITE-Check (im foundation_register als bewusste Ausnahme dokumentiert).
    5. topo-Sort (Kahn ODER DFS — Discretion D-G16, mit Gemini abgewogen): ALLE FK-Kanten (child->parent) gehen in den Sort-Graph — UNABHAENGIG von confdeltype UND UNABHAENGIG vom Schema (Cross-Schema, Fund #8). CASCADE-Kanten (confdeltype='c') werden NICHT mehr weggelassen (Gemini-Fund #1, BLOCKER): in einer Kette A->(CASCADE)->B->(RESTRICT)->C wuerde das Weglassen der A->B-Kante den Sortierer A vor C platzieren lassen → Loeschen von A cascadet auf B → scheitert weil C noch auf B zeigt (FK-Violation) → Waechter crasht → Tor dauerhaft blockiert (DoS). Cross-Schema-Kanten (z.B. `crm.accounts -> public.tenant_orgs`) gehen GENAUSO in den Sort → der globale topo-Sort stellt crm-Kinder VOR public-Eltern (Fund #8) → cleanup_rows (crm+public) loescht crm zuerst → keine FK-Violation. Mit ALLEN Kanten gilt Leaves-vor-Roots; die spaeteren CASCADE-Deletes werden dann redundant, aber NIE fehlerhaft. confdeltype wird weiterhin GELESEN — nur noch zur FK-Kanten-Aufzaehlung/Diagnose, NICHT mehr zum Kanten-Ausschluss. Zyklus-Brechung (D-G16 WP1) BLEIBT: self-ref (child==parent) und mutual FKs erkennen → NUR diese eine Kante ignorieren. Im Code-Kommentar die konkreten NERVE-self-ref/mutual-Tabellen + die konkrete cross-schema crm->public-Kante benennen (aus dem inspect.sh-FK-Output).
    6. Rueckgabe `reverse_fk_delete_order` = globale Cross-Schema-topo-Order Kind-vor-Eltern (so loescht cleanup_rows crm vor public, Kinder vor Eltern). Da CASCADE-Kinder jetzt explizit in der Order stehen, loescht der Waechter sie direkt; der ON-DELETE-CASCADE des Eltern wird damit redundant, aber nie eine Quelle fuer FK-Violations.
    7. Unit-Tests in `tests/_schema_introspect.py` selbst NICHT — eine separate `tests/test_schema_introspect.py` mit den 8 behavior-Tests (DSN-frei fuer Test 1-3 + Test 7 + Test 8 via injizierte Kanten-/pk_cols-Liste; Test 4-6 gegen real-PG via vorhandenes Skip-when-env-Muster conftest.py:495). Test 7 (Composite-PK) laeuft gegen einen INJIZIERTEN >1-pk_cols-Fall (Prod hat heute 0). Test 8 (Cross-Schema) laeuft DSN-frei via injizierte cross-schema-Kante crm.accounts->public.tenant_orgs.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_schema_introspect.py -v'</automated>
    HINWEIS: triage.sh existiert erst nach Plan 03. Bis dahin verifiziert dieser Task ueber den vollen Gate-Lauf (deploy.sh production) gemeinsam mit Task 2. Lokaler pytest ist NICHT zulaessig (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "def derive_baseline_tables" tests/_schema_introspect.py` liefert genau 1 Treffer
    - `grep -n "def primary_key_column" tests/_schema_introspect.py` liefert genau 1 Treffer
    - `grep -nE "schemas=\(|schemas-Tuple|n\.nspname IN %s|IN %s" tests/_schema_introspect.py` liefert >=1 Treffer (MULTI-SCHEMA-Signatur + IN-%s-Tuple-Parametrisierung, Gemini-Re-Review R3 / Fund #8)
    - `grep -n "pg_constraint" tests/_schema_introspect.py` liefert >=1 Treffer (FK-Kanten-Query vorhanden)
    - `grep -n "confdeltype" tests/_schema_introspect.py` liefert >=1 Treffer — Zweck: FK-Kanten-Aufzaehlung deckt CASCADE MIT ab (Gemini-Fund #1); die Spalte wird gelesen, aber NICHT mehr zum Kanten-Ausschluss benutzt
    - `grep -nE "confdeltype.*(NOT IN|!=|<>).*'c'|skip.*cascade|cascade.*skip|WEGLASSEN" tests/_schema_introspect.py` liefert 0 Treffer (kein confdeltype-basierter Kanten-Ausschluss mehr — Gemini-Fund #1)
    - `grep -n "indisprimary" tests/_schema_introspect.py` liefert >=1 Treffer (PK-from-catalog, D-G17 REFINEMENT — speist das Auto-Delete in Task 2)
    - `grep -nE "len\(pk_cols\)|composite PK" tests/_schema_introspect.py` liefert >=1 Treffer (Gemini-Re-Review R2 / Fund #6: Composite-PK-Guard — `len(pk_cols) != 1` → foundation_register, nicht auto-delete)
    - `grep -n "composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)" tests/_schema_introspect.py` liefert >=1 Treffer (exakter EHRLICHER Denylist-Grund — Fund #9; KEINE falsche "Snapshot-sichtbar"-Behauptung mehr)
    - `grep -nE "snapshot.*sichtbar|missing/mutated.*sichtbar|visible.*snapshot" tests/_schema_introspect.py` liefert 0 Treffer (die unehrliche Sichtbarkeits-Behauptung ist RAUS — Fund #9)
    - `grep -nE "logging\.warning|log\.warning" tests/_schema_introspect.py` belegt: jede Exklusion (no-PK + composite-PK) wird GELOGGT, nie still (Req-7)
    - `grep -n "foundation_register\|alembic_version" tests/_schema_introspect.py` belegt explizite Denylist mit Grund
    - `grep -rn "_CLEANUP_FK_ORDER\|_BASELINE_PUBLIC_TABLES" tests/conftest.py` → _BASELINE_PUBLIC_TABLES ist nach Task 2 entfernt/ersetzt; _CLEANUP_FK_ORDER BLEIBT nur als best-effort-Fallback fuer cleanup_rows (siehe Task 2 acceptance)
    - Im Gate-Lauf: test_schema_introspect.py erscheint als PASSED; die abgeleitete Liste enthaelt calls + objection_event (Req-9-Beleg); Test 3 belegt CASCADE-Kante bleibt im Sort; Test 6 belegt primary_key_column liefert non-id-PK korrekt; Test 7 belegt eine Composite-PK-Tabelle landet im foundation_register mit dem ehrlichen Grund (Fund #6/#9); Test 8 belegt crm.accounts steht VOR public.tenant_orgs in der cross-schema reverse_fk_delete_order (Fund #8)
  </acceptance_criteria>
  <done>Modul existiert, liefert (table_list, reverse_fk_order, foundation_register) aus pg_constraint CROSS-SCHEMA-topo-Sort (schemas-Tuple, IN %s) mit ALLEN FK-Kanten (auch CASCADE, auch cross-schema crm->public, Fund #8); PK-Name katalog-abgeleitet und fuer Task-2-Auto-Delete nutzbar; Composite-PK-Tabellen (>1 indisprimary-Zeile) ins foundation_register mit ehrlichem Grund "composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)" (geloggt, NICHT auto-geloescht, NICHT als snapshot-sichtbar behauptet, Fund #6/#9); Prod-Fakt 0 composite-PK-Tabellen (2026-06-16) dokumentiert; Denylist begruendet; 8 behavior-Tests gruen im Gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-Reset gespaltener Baseline-Waechter in conftest.py (D-G01..D-G06, konsumiert Task 1, CROSS-SCHEMA-Cache + public-only-Waechter)</name>
  <read_first>
    - tests/_schema_introspect.py (das in Task 1 gebaute Modul — Task 2 importiert es, inkl. primary_key_column + foundation_register; derive_baseline_tables nimmt jetzt `schemas`-Tuple)
    - tests/conftest.py:289-303 (_diff_baseline, der 3-Wege-Split-Kern, unveraendert wiederverwendet)
    - tests/conftest.py:306-321 (_baseline_guard_engine, read-only → read+write erweitern, D-G05)
    - tests/conftest.py:343-368 (_baseline_cleanup_guard, fail-closed → Auto-Reset gespalten)
    - tests/conftest.py (die bestehende _baseline_snapshot-Fixture — Task 2 fuegt ihr _baseline_schema als Dependency-Parameter hinzu, Fund #7)
    - tests/conftest.py:114-130 (_CLEANUP_FK_ORDER — bleibt als best-effort-Fallback; beachte: crm.* VOR public.* — genau diese crm-vor-public-Order muss der cross-schema Cache reproduzieren, Fund #8) + :152-237 (cleanup_rows — raeumt crm UND public, id::text-Cast-Muster, D-G06; der `::text`-Cast bleibt, der Spaltenname wird dynamisch)
    - tests/conftest.py:270-286 (_snapshot_public_tables — iteriert HEUTE _BASELINE_PUBLIC_TABLES; muss table_list LOKAL auf public filtern, Fund #8)
    - PATTERNS.md Sektion 2 (Auto-Reset-Split-Shape, TX-Hygiene, uuid-Cast, Cache-Fill-Ordering, Cross-Schema-Cache, public-only-Waechter-Filter)
    - _green_isolation_gemini_OUT.md (Req-4-Beleg: Option 1 gespalten, missing/mutated MUSS fail, datiert vor Bau — commit 11b8fcf)
  </read_first>
  <behavior>
    - Test 1 (heal-leaked, D-G01): ein Test committet 1 Extra-Row in public.calls und raeumt NICHT auf → nach dem Test ist die Row weg (Auto-Delete) UND eine [BASELINE-AUTO-FIX]-Warnung mit nodeid+calls+id ist im Log. KEIN pytest.fail.
    - Test 2 (Isolation-Beleg, Req-3): zwei Tests, die dieselbe Tabelle nutzen, sehen sich nicht — der zweite startet mit der pristinen Baseline (Beleg gegen Cross-Test-Vergiftung).
    - Test 3 (STRICT SPLIT, D-G02): ein Test loescht Baseline-Row id=1 (organisations) → harter pytest.fail mit nodeid; KEIN Re-Insert (kein Heilen). Gleiches fuer mutated (xmin geaendert).
    - Test 4 (TX-Hygiene, D-G05): nach dem Auto-Delete sieht der naechste Test die geloeschte Extra-Row wirklich nicht mehr (commit ist passiert).
    - Test 5 (derived-PK-Cast, D-G06 + Gemini-Fund #2): Auto-Delete einer Extra-Row in einer uuid-PK-Tabelle (tenant_orgs) wirft KEINEN `operator does not exist: uuid = text` (der `::text`-Cast greift) UND das Delete laeuft ueber die KATALOG-ABGELEITETE PK-Spalte (primary_key_column), nicht ueber hardcoded id → eine non-id-PK-Tabelle (intent_event.event_id) wuerde NICHT mit "column id does not exist" crashen.
    - Test 6 (Modul-Level-Cache + Cache-Fill-Garantie + CROSS-SCHEMA, Gemini-Fund #3 + Gemini-Re-Review R2 / Fund #7 + R3 / Fund #8): der session-scoped Aufbau fuellt den Modul-Level-Cache (_DERIVED_FK_ORDER / _DERIVED_PK_COLS) GENAU EINMAL ueber ALLE Schemas (public+crm+training); cleanup_rows liest daraus OHNE Signatur-Aenderung und OHNE per-call derive_baseline_tables-DB-Query. ZUSAETZLICH (Fund #7): nach Session-Start ist der Modul-Level-Cache NACHWEISLICH NON-EMPTY (assert `_DERIVED_FK_ORDER` und `_DERIVED_PK_COLS` sind nicht leer). ZUSAETZLICH (Fund #8): assert `_DERIVED_FK_ORDER` enthaelt crm.* Eintraege UND `crm.accounts` steht VOR `public.tenant_orgs` (cross-schema Order, crm vor public) → cleanup_rows (crm+public) loescht crm zuerst, keine FK-Violation. Bei leerem Cache (z.B. Skip-Pfad/SQLite) faellt cleanup_rows weiterhin auf _CLEANUP_FK_ORDER zurueck.
    - Test 7 (Waechter public-only, Gemini-Re-Review R3 / Fund #8 + D-G04): der Snapshot-Waechter (`_snapshot_public_tables`/`_baseline_cleanup_guard`) iteriert/snapshottet/loescht NUR public.* — assert: die Waechter-table_list enthaelt KEINE crm.* Tabelle (lokal auf `startswith('public.')` gefiltert), obwohl der Modul-Cache crm.* in der FK-Order enthaelt. Beweist: D-G04 public-only ist erhalten; der Waechter loescht crm NIE; crm bleibt POST-SUITE in deploy.sh.
  </behavior>
  <action>
    Erweitere `tests/conftest.py` IN PLACE (D-G01..D-G06). ASCII-Identifier (`_baseline_cleanup_guard`, Tag `[BASELINE-AUTO-FIX]`).

    1. Liste-Quelle ersetzen + CROSS-SCHEMA Modul-Level-Cache + Cache-Fill-Garantie + public-only-Waechter-Filter (D-G17/D-G19, Gemini-Fund #3 + Gemini-Re-Review R2 / Fund #7 + R3 / Fund #8): Die Hardcode-Liste `_BASELINE_PUBLIC_TABLES` (Z.263-267) durch session-scoped Ableitung ersetzen. Eine session-scoped Fixture `_baseline_schema` ruft die Ableitung EINMAL bei Session-Start auf — und zwar CROSS-SCHEMA ueber ALLE drei Schemas: `tests/_schema_introspect.derive_baseline_tables(_baseline_guard_engine, schemas=('public','crm','training'))` (Fund #8). Sie schreibt das Ergebnis in MODUL-LEVEL-Globals: `_DERIVED_FK_ORDER` (= globale cross-schema reverse_fk_order, crm-Kinder VOR public-Eltern) und `_DERIVED_PK_COLS` (= dict {table: pk_col} via primary_key_column; composite-PK- und no-PK-Tabellen sind hier NICHT enthalten, weil sie im foundation_register liegen — Fund #6/#9).
       CROSS-SCHEMA-CACHE WARUM (Fund #8): cleanup_rows raeumt crm UND public (Plan 02 Z.109, D-G04 crm-Writer-Self-Clean). Fuellte man den Cache nur mit `schema='public'`, fehlten crm-Tabellen im _DERIVED_FK_ORDER → sie fielen in den Fallback-Bucket ans ENDE → public zuerst geloescht → `crm.accounts -> public.tenant_orgs` (FK) → harte FK-Violation → jeder crm+public-raeumende Test crasht (Regression aus dem #3-Fold). Der cross-schema Cache liefert dieselbe crm-vor-public-Order wie der hardcoded `_CLEANUP_FK_ORDER` (conftest.py:114-130).
       WAECHTER PUBLIC-ONLY (Fund #8, D-G04 erhalten): `_snapshot_public_tables` (Z.270-286) UND `_baseline_cleanup_guard` operieren NUR auf public. Sie filtern die cross-schema table_list LOKAL: `public_table_list = [t for t in table_list if t.startswith('public.')]`. Der Waechter snapshottet/loescht NIE crm (nerve_app saehe crm nur tenant-gefiltert; crm bleibt der POST-SUITE deploy.sh-Check). NUR der cleanup_rows-Cache nutzt die VOLLE cross-schema Order; der Waechter-Snapshot ist public-only.
       CACHE-FILL-GARANTIE (Fund #7): `_baseline_snapshot` MUSS `_baseline_schema` als Fixture-PARAMETER (Dependency) anfordern — also `def _baseline_snapshot(..., _baseline_schema):`. HARTE Reihenfolge-Garantie: pytest baut `_baseline_schema` (und damit den cross-schema Modul-Cache) auf, BEVOR `_baseline_snapshot` laeuft und BEVOR irgendein cleanup_rows-Fallback triggern kann. Ohne diese Dependency fiele cleanup_rows STILL auf den Hardcode-`_CLEANUP_FK_ORDER` zurueck → Req-9 wirkungslos (Meta-False-Green).
       KEINE Signatur-Aenderung an `cleanup_rows` und KEINE per-call DB-Query: `cleanup_rows` liest die dynamische Order/PK-Spalten aus den MODUL-LEVEL-Globals. FALLBACK: ist der Cache leer (non-PG/SQLite-Skip-Pfad), faellt `cleanup_rows` auf die hardcoded `_CLEANUP_FK_ORDER`-Order zurueck (best-effort). `_CLEANUP_FK_ORDER` BLEIBT als Fallback erhalten. Die 20+ bestehenden cleanup_rows-Caller bleiben unveraendert.
       `_snapshot_public_tables` iteriert ueber `public_table_list` (cross-schema table_list lokal auf public gefiltert) statt `_BASELINE_PUBLIC_TABLES` und snapshottet jede Tabelle KEYED ueber ihre abgeleitete PK-Spalte (`_DERIVED_PK_COLS[tbl]`), nicht ueber hardcoded `SELECT id`.
    2. `_baseline_guard_engine` (D-G05): bleibt session-scoped, nerve_app-peer, public-only. Der Auto-Delete laeuft in `with engine.begin() as conn:` (commit-on-exit), NICHT auf der Read-Connection — explizite TX-Hygiene, damit geloeschte Rows fuer den naechsten Test wirklich weg sind.
    3. `_baseline_cleanup_guard` (D-G01/D-G02/D-G03) — Split:
       - `drift = _diff_baseline(current, _baseline_snapshot)` (unveraendert; current ist public-only durch den public-gefilterten Snapshot).
       - Pro Tabelle: `leaked` → SAMMELN fuer Auto-Delete. `missing` ODER `mutated` → SAMMELN fuer hard-fail.
       - Wenn missing/mutated nicht leer: SOFORT `pytest.fail(f"[BASELINE-GUARD] {request.node.nodeid}: protected baseline drifted (missing/mutated) -> ...")` — KEIN Heilen (D-G02: Re-Insert feuert trg_mk_tenant_org → neue UUID → bricht Folge-Tests).
       - Wenn nur leaked: pro betroffener Tabelle `logging.warning(f"[BASELINE-AUTO-FIX] {request.node.nodeid} leaked rows in {tbl}: {sorted(ids)}")` (D-G03), DANN `with engine.begin() as conn:` Loeschen in der (public-gefilterten) reverse_fk_order (Kind vor Eltern) via die KATALOG-ABGELEITETE PK-Spalte (Gemini-Fund #2): `pk_col = _DERIVED_PK_COLS[tbl]` (Fallback 'id' nur falls Cache leer), dann `conn.execute(text(f"DELETE FROM {tbl} WHERE {pk_col}::text = ANY(:ids)"), {"ids":[str(x) for x in ids]})`. Der `::text`-Cast bleibt verbatim (D-G06 uuid-Cast aus cleanup_rows:221-224) — nur der COLUMN-NAME ist jetzt dynamisch, sodass non-id-PK-Tabellen (intent_event.event_id) nicht crashen. Composite-PK-Tabellen kommen hier nicht vor (sie sind nicht in der baseline_table_list / nicht in _DERIVED_PK_COLS, Fund #6). EHRLICHKEIT (Gemini-Re-Review R3 / Fund #9): composite-PK-Tabellen werden WEDER auto-geloescht NOCH snapshot-ueberwacht — das ist eine bekannte, offen im foundation_register protokollierte Tor-Luecke (heute leer: 0 composite-PK-Tabellen per 2026-06-16). KEINE Behauptung, ein Leak dort sei "ueber den Snapshot/missing-mutated-Pfad sichtbar" (technisch unmoeglich — sie werden nie iteriert).
    4. Scope (D-G04): Der WAECHTER snapshottet/loescht NUR public.* (table_list lokal public-gefiltert, Fund #8). crm.* bleibt UNVERAENDERT POST-SUITE-Check in deploy.sh (Plan 02) — kein in-pytest Auto-Clean fuer crm. Der cleanup_rows-Cache nutzt zwar die cross-schema Order (damit cleanup_rows crm vor public loescht), aber der Baseline-WAECHTER selbst fasst crm NIE an. Im Kommentar beide Aspekte bekraeftigen.
    5. behavior-Tests aus oben in `tests/test_schema_introspect.py` ODER einer `tests/test_baseline_autoreset.py` (real-PG, Skip-when-env-Muster). Test 6 muss zusaetzlich asserten, dass `_DERIVED_FK_ORDER` + `_DERIVED_PK_COLS` nach Session-Start NON-EMPTY sind (Fund #7) UND dass `crm.accounts` VOR `public.tenant_orgs` in `_DERIVED_FK_ORDER` steht (Fund #8). Test 7 muss asserten, dass die Waechter-table_list KEINE crm.* enthaelt (public-only-Filter, Fund #8/D-G04).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_baseline_autoreset.py -v'</automated>
    HINWEIS: Endgueltige Verifikation gemeinsam mit Plan 02 im vollen `deploy.sh production`-Gate-Lauf (0 _baseline_cleanup_guard-Leak-Fails). Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "\[BASELINE-AUTO-FIX\]" tests/conftest.py` liefert >=1 Treffer (laute Attribution, D-G03)
    - `grep -n "engine.begin()" tests/conftest.py` belegt explizite TX-Hygiene fuer Auto-Delete (D-G05)
    - `grep -nE "\{pk_col\}::text = ANY|_DERIVED_PK_COLS" tests/conftest.py` belegt: Auto-Delete laeuft ueber die KATALOG-ABGELEITETE PK-Spalte (Gemini-Fund #2), nicht ueber hardcoded id
    - `grep -nc "id::text = ANY" tests/conftest.py` im Auto-Delete-Pfad ist 0 (kein hardcoded-id-Delete mehr im Auto-Reset-Pfad — Gemini-Fund #2; der `::text`-Cast bleibt, nur dynamisch via {pk_col})
    - `grep -n "_DERIVED_FK_ORDER\|_DERIVED_PK_COLS" tests/conftest.py` >=1 Treffer (Modul-Level-Cache, Gemini-Fund #3 — einmal bei Session-Start gefuellt)
    - `grep -nE "schemas=\('public', ?'crm', ?'training'\)|schemas=\(.*crm.*\)" tests/conftest.py` liefert >=1 Treffer (CROSS-SCHEMA-Cache-Fill, Gemini-Re-Review R3 / Fund #8 — derive_baseline_tables ueber alle 3 Schemas, damit crm-vor-public Order entsteht)
    - `grep -nE "startswith\('public\.'\)|startswith\(\"public\.\"\)" tests/conftest.py` liefert >=1 Treffer (Waechter public-only-Filter der cross-schema table_list, Gemini-Re-Review R3 / Fund #8 — D-G04 erhalten)
    - `grep -nE "_baseline_snapshot\([^)]*_baseline_schema" tests/conftest.py` liefert >=1 Treffer (Gemini-Re-Review R2 / Fund #7: `_baseline_snapshot` fordert `_baseline_schema` als Fixture-Parameter/Dependency an → harte Cache-Fill-Ordering-Garantie)
    - `grep -n "_CLEANUP_FK_ORDER" tests/conftest.py` >=1 Treffer (Fallback bleibt erhalten); cleanup_rows-Signatur UNVERAENDERT (kein neuer Parameter — manuell gegen die 20+ Caller verifizieren)
    - `grep -n "pytest.fail" tests/conftest.py` im missing/mutated-Pfad vorhanden (D-G02 hard-block)
    - `grep -n "from tests._schema_introspect\|import _schema_introspect\|derive_baseline_tables\|primary_key_column" tests/conftest.py` belegt Konsum des Moduls (D-G19)
    - `grep -cn "_BASELINE_PUBLIC_TABLES = \[" tests/conftest.py` == 0 (Hardcode-Liste entfernt, Req-9) — oder ein Rest ist im foundation_register/Kommentar begruendet
    - Im Gate-Lauf: 0 `_baseline_cleanup_guard`-Leak-Fails/Errors; ein absichtlicher Leak erzeugt sichtbare [BASELINE-AUTO-FIX]-Warnung MIT nodeid; Test 6 belegt `_DERIVED_FK_ORDER`/`_DERIVED_PK_COLS` NON-EMPTY + crm.accounts vor public.tenant_orgs (Fund #7/#8); Test 7 belegt Waechter-table_list ohne crm.* (Fund #8/D-G04)
  </acceptance_criteria>
  <done>Waechter heilt leaked public-Rows laut, blockt missing/mutated hart, konsumiert table_list+reverse_fk_order+PK-Spalten aus Task 1; Auto-Delete ueber abgeleitete PK-Spalte (kein hardcoded id, keine Composite-PK im Pfad); cleanup_rows liest CROSS-SCHEMA Modul-Level-Cache (crm vor public, Fund #8) ohne Signatur-Aenderung/per-call-Query (Fallback _CLEANUP_FK_ORDER); der WAECHTER-Snapshot ist public-only gefiltert (D-G04 erhalten, Fund #8); `_baseline_snapshot` fordert `_baseline_schema` als Dependency → Cache garantiert vor erstem Cleanup gefuellt (Fund #7); uuid-cast + TX-Hygiene korrekt. Im Gate 0 Leak-Errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test-Body → nerve_test public.* | Test committet Rows; Waechter-Teardown loescht test-erzeugte Extra-Rows |
| Waechter → geschuetzte Baseline (id=1-Seed) | Waechter darf Baseline NIE heilen, nur sichtbar blocken |
| Katalog-Ableitung → bewachte Tabellen-Liste | eine still gedroppte Tabelle = unbewachter Leak-Pfad |
| Katalog-Ableitung → Auto-Delete-Eignung pro Tabelle | eine Composite-PK-Tabelle im Auto-Delete-Pfad = Over-Deletion/Crash; eine als "sichtbar" behauptete aber nicht-ueberwachte Tabelle = unehrliches False-Green |
| Cross-Schema-Cleanup-Order → cleanup_rows | public-only Cache = crm faellt ans Listen-Ende = crm.accounts->public.tenant_orgs FK-Violation |
| Cache-Init → cleanup_rows-Datenquelle | ungefuellter Cache = stiller Fallback auf Hardcode = Req-9 wirkungslos |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-GREEN-01 | Tampering (False-Green) | _baseline_cleanup_guard missing/mutated-Pfad | mitigate | D-G02 STRICT SPLIT: missing/mutated → harter pytest.fail mit nodeid, NIE Re-Insert/Heal. grep belegt pytest.fail im Pfad. Faengt die 7 base-seed-Delete-Bugs. |
| T-PGTEST-GREEN-02 | Information-Disclosure (silent-skip) | Auto-Delete von leaked Rows | mitigate | D-G03: jedes Auto-Delete emittiert [BASELINE-AUTO-FIX] mit nodeid+tbl+ids — nie stumm (Req-3/Req-7). grep belegt Tag. |
| T-PGTEST-GREEN-03 | Tampering (False-Green-Blindspot) | derive_baseline_tables Denylist | mitigate | D-G17 + Gemini-Re-Review R2 / Fund #6 + R3 / Fund #9: Default "watch everything"; jede Exklusion ins foundation_register mit Grund + LOGGEN; PK-loses ODER Composite-PK-Tabelle nie still gedroppt. Composite-PK-Grund sagt EHRLICH "not auto-delete-eligible AND not snapshot-monitored (known gate gap)" — KEINE falsche Snapshot-Sichtbarkeits-Behauptung (Fund #9). Prod-Fakt: 0 composite-PK-Tabellen (2026-06-16) → Luecke leer; Tuple-Key ist deferred Follow-up. grep belegt foundation_register + ehrlicher Grund + logging.warning + 0 "snapshot-sichtbar"-Behauptung. |
| T-PGTEST-GREEN-04 | Denial-of-Service (Waechter-Tod) | Auto-Delete FK-Order | mitigate | D-G19 + Gemini-Fund #1: Waechter/cleanup_rows konsumieren reverse_fk_order aus Modul (Kind vor Eltern), in den ALLE FK-Kanten eingehen — auch CASCADE (confdeltype='c'). Damit sortiert auch eine CASCADE->RESTRICT-Kette Leaves-vor-Roots → kein FK-Violation-Tod/DoS. self-ref/mutual-Zyklus weiterhin gebrochen. grep belegt: kein confdeltype-basierter Kanten-Ausschluss mehr. |
| T-PGTEST-GREEN-05 | Tampering (uuid-cast / PK-Annahme / Over-Deletion) | Auto-Delete uuid-PK + non-id-PK + composite-PK-Tabellen | mitigate | D-G06 + Gemini-Fund #2 + Gemini-Re-Review R2 / Fund #6 + R3 / Fund #9: `{pk_col}::text = ANY(...)` mit KATALOG-ABGELEITETER PK-Spalte statt hardcoded `id`. Der `::text`-Cast bleibt; der dynamische Spaltenname verhindert `column id does not exist` bei non-id-PK-Tabellen. Composite-PK-Tabellen werden GAR NICHT auto-geloescht UND GAR NICHT snapshot-ueberwacht (ehrlich als bekannte Luecke protokolliert, Fund #9), sonst Over-Deletion/Crash. grep belegt {pk_col}/_DERIVED_PK_COLS + composite-PK-Guard + ehrlicher Denylist-Grund. |
| T-PGTEST-GREEN-06 | Tampering (Meta-False-Green: stiller Hardcode-Fallback) | Modul-Cache-Init / cleanup_rows-Datenquelle | mitigate | Gemini-Re-Review R2 / Fund #7: `_baseline_snapshot` fordert `_baseline_schema` als Fixture-Parameter (Dependency) an → harte Ordering-Garantie, dass der Modul-Level-Cache bei Session-Start gefuellt ist, BEVOR ein cleanup_rows-Fallback triggern kann. grep belegt `_baseline_snapshot(... _baseline_schema)`; Test 6 belegt Cache NON-EMPTY nach Session-Start. |
| T-PGTEST-GREEN-07 | Denial-of-Service (Cross-Schema-FK-Violation, Regression aus #3-Fold) | cleanup_rows Cross-Schema-Loeschorder | mitigate | Gemini-Re-Review R3 / Fund #8: cleanup_rows raeumt crm UND public (D-G04). Der Modul-Cache wird CROSS-SCHEMA gefuellt (`schemas=('public','crm','training')`) → der globale reverse_fk_delete_order sortiert crm-Kinder VOR public-Eltern (z.B. crm.accounts VOR public.tenant_orgs), sonst public-zuerst → `crm.accounts -> public.tenant_orgs` FK-Violation → jeder crm+public-raeumende Test crasht. Der WAECHTER-Snapshot bleibt public-only (table_list lokal auf `startswith('public.')` gefiltert), D-G04 erhalten. grep belegt `schemas=(...crm...)` + `startswith('public.')`; Test 6 belegt crm.accounts vor public.tenant_orgs; Test 7 belegt Waechter ohne crm.*. |

Block severity = high (ein False-Green auf dem Baseline-Waechter green-lightet bad deploys).
</threat_model>

<verification>
- Voller `deploy.sh production`-Gate-Lauf (gemeinsam mit Plan 02): 0 `_baseline_cleanup_guard`-Leak-Fails/Errors.
- Absichtlicher Leak-Test erzeugt sichtbare [BASELINE-AUTO-FIX]-Warnung mit nodeid.
- Abgeleitete public-Liste enthaelt calls + objection_event (Req-9-Beleg).
- CASCADE-Kante bleibt im topo-Sort (kein DoS bei CASCADE->RESTRICT-Kette); Auto-Delete laeuft ueber abgeleitete PK-Spalte (kein Crash an non-id-PK-Tabellen).
- CROSS-SCHEMA-Order (Fund #8): der Modul-Cache wird ueber alle 3 Schemas gefuellt; `crm.accounts` steht VOR `public.tenant_orgs` in _DERIVED_FK_ORDER → cleanup_rows (crm+public) wirft keine FK-Violation. Der WAECHTER-Snapshot ist public-only gefiltert (D-G04 erhalten; crm nie im Waechter).
- Composite-PK-Tabelle (>1 indisprimary-Zeile) landet im foundation_register mit ehrlichem Grund "composite PK: not auto-delete-eligible AND not snapshot-monitored (known gate gap)", wird geloggt + NICHT auto-geloescht + NICHT als snapshot-sichtbar behauptet (Fund #6/#9). PROD-FAKT: 0 solche Tabellen per 2026-06-16 → Luecke leer; Tuple-Key-Umbau ist deferred Follow-up (Backlog-999.x), falls je eine auftaucht.
- Modul-Cache (_DERIVED_FK_ORDER/_DERIVED_PK_COLS) ist nach Session-Start NON-EMPTY — `_baseline_snapshot` fordert `_baseline_schema` als Dependency an, kein stiller Hardcode-Fallback (Fund #7).
- KEIN lokaler pytest als Acceptance (CLAUDE.md HART).
</verification>

<success_criteria>
- tests/_schema_introspect.py liefert (table_list, reverse_fk_order, foundation_register) aus pg_constraint CROSS-SCHEMA-topo-Sort (schemas-Tuple) mit ALLEN FK-Kanten; Composite-PK-Tabellen denylisted mit ehrlichem Grund (Fund #6/#9); crm-Kinder vor public-Eltern (Fund #8).
- conftest Auto-Reset-Waechter: leaked→loeschen+warnen (ueber abgeleitete PK-Spalte, keine Composite-PK), missing/mutated→pytest.fail; Waechter-Snapshot public-only gefiltert (D-G04, Fund #8).
- cleanup_rows liest CROSS-SCHEMA Modul-Level-Cache (crm vor public) ohne Signatur-Aenderung/per-call-Query (Fallback _CLEANUP_FK_ORDER); `_baseline_snapshot` fordert `_baseline_schema` als Dependency → Cache garantiert gefuellt (Fund #7).
- 0 Baseline-Leak-Errors im Gate; Leaks sichtbar; Isolation erhalten (Req-2/Req-3); crm+public-Cleanup ohne FK-Violation (Fund #8).
- Hardcode-Listen ersetzt (Req-9 public-Haelfte).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-01-SUMMARY.md`
</output>
