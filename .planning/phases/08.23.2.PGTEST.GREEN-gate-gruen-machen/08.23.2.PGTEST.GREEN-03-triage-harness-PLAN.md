---
phase: 08.23.2.PGTEST.GREEN
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - scripts/triage.sh
autonomous: true
requirements: [Req-5]
must_haves:
  truths:
    - "scripts/triage.sh provisioniert nerve_test 1:1 wie der deploy.sh-Gate-Block (pg_dump-Restore + alembic upgrade head + 4 DSNs + Whitelist-Guard + trap-Drop)"
    - "scripts/triage.sh laeuft NUR gezielte Tests (uebergebene Argumente) und loest NIE systemctl restart / Deploy aus"
    - "scripts/triage.sh reicht die uebergebenen Argumente HART in die innere single-quoted bash -c durch (bash -c '... pytest \"$@\"' _ \"$@\") — sonst expandiert $@ nicht und der Harness laeuft trotz Argument die GANZE Suite (Gemini-Re-Review R2 / Fund #5)"
    - "scripts/triage.sh droppt nerve_test per trap auch bei Test-Fehler rueckstandsfrei"
    - "Kein DSN in scripts/triage.sh zeigt auf @/nerve (immer @/nerve_test)"
  artifacts:
    - path: "scripts/triage.sh"
      provides: "Server-Triage-Harness: Gate-Provisioning 1:1, targeted pytest (Argument-Forwarding via _ \"$@\"), KEIN Restart (bleibendes Werkzeug/Ratchet)"
      contains: "trap cleanup EXIT"
  key_links:
    - from: "scripts/triage.sh"
      to: "nerve_test (Wegwerf-DB)"
      via: "pg_dump-Restore vom Prod-nerve + trap-DROP, nerve_test-only"
      pattern: "DROP DATABASE IF EXISTS"
    - from: "scripts/triage.sh pytest-Aufruf"
      to: "targeted tests"
      via: "bash -c '... pytest \"$@\"' _ \"$@\" (Argumente hart in die innere Subshell), KEIN systemctl restart"
      pattern: "' _ \"\\$@\""
---

<objective>
Baue `scripts/triage.sh` (Teil b Infrastruktur, D-G08): einen Server-Triage-Harness, der den deploy.sh-Gate-Provisioning-Block 1:1 wiederverwendet (pg_dump-Restore + alembic upgrade head + 4 nerve_test-DSNs + Whitelist-Guard + trap-Drop + pipefail), aber NUR gezielte Tests laeuft und NIE `systemctl restart`/Deploy ausloest. nerve_test-only → Prod-nerve nie beruehrt (Req-5/Req-10).

Purpose: Empirische Triage (Plan 04) + Determinismus-Identifikation (Plan 05) brauchen iteratives Instrumentieren + Re-Run am echten PG ohne Deploy. Der Harness ist ein BLEIBENDES Werkzeug (Ratchet, Andre-Direktive 15.06.), kein Wegwerf-Skript. Diagnose-Prints leben in Harness-Flags, NICHT committed in Tests.

KERN-FAEHIGKEIT (Gemini-Re-Review R2 / Fund #5): targeted runs MUESSEN funktionieren — die uebergebenen Argumente werden hart in die innere single-quoted `bash -c` durchgereicht (`bash -c '... pytest "$@"' _ "$@"`). Ohne dieses Forwarding expandiert `$@` im single-quoted Block nicht → die innere bash bekommt keine Argumente → der Harness laeuft trotz `triage.sh test_x.py` die GANZE Suite → die empirische Triage (Plan 04) ist unmoeglich.

Output: `scripts/triage.sh` (NEU).

depends_on Plan 01+02: der Harness laeuft die Suite, die Plan 01 (Auto-Reset-Waechter) + Plan 02 (Marker-Registrierung) bereits geaendert haben — sonst triagiert er gegen die kaputte alte Sicht.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-SPEC.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-CONTEXT.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-PATTERNS.md

<interfaces>
<!-- Der zu faktorierende deploy.sh-Provisioning-Block (verbatim, PATTERNS.md Sektion 3). -->
Whitelist-Guard (deploy.sh:139-143): TEST_DB="nerve_test"; if != nerve_test → exit 1.
trap cleanup EXIT (deploy.sh:145-147): DROP DATABASE IF EXISTS nerve_test.
Pre-Run-DROP (deploy.sh:149-150).
CREATE OWNER postgres (deploy.sh:152-153) — NICHT nerve_app (sonst crm owner-bypass RLS).
Schema-Dump set -o pipefail (deploy.sh:155-158): pg_dump --schema-only nerve | psql ON_ERROR_STOP=1.
Stamp-Row-Dump pipefail (deploy.sh:160-161): alembic_version.
alembic upgrade head (deploy.sh:163-164).
pytest-Block (deploy.sh:178-188): ANON_PW via env, single-quoted inner bash -c, 4 DSNs + DATABASE_URL, alle @/${TEST_DB}.
  > ACHTUNG (Gemini-Re-Review R2 / Fund #5): deploy.sh haengt in diesem Block ein FESTES `pytest tests/ --tb=short -q` an, OHNE `$@`. triage.sh ersetzt das durch `pytest "$@"` UND muss die aeusseren Argumente per `' _ "$@"` NACH dem schliessenden single-quote in die innere bash durchreichen — sonst leerlaeuft die Argument-Liste.
</interfaces>
</context>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

### Angefasste Tabellen
- KEINE App-Tabelle wird gelesen/geschrieben — der Harness provisioniert nerve_test (CREATE/DROP DATABASE) und laeuft pytest. Die Tests selbst lesen/schreiben (das deckt Plan 01/04/05 ab).
- alembic_version (Dump-Stamp) — wie deploy.sh, unveraendert wiederverwendet.

### inspect.sh-Beleg (Executor zitiert verbatim in SUMMARY)
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
> Belegt prod-head (alembic_version), damit der Harness denselben Stamp-Dump-Pfad nutzt wie deploy.sh.

### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Verifiziert via |
|---|---|---|---|
| nerve_test-Schema | pg_dump --schema-only nerve | echte DB (Wegwerf) | deploy.sh-Block 1:1 |
| alembic_version-Stamp | pg_dump --data-only alembic_version | DB-Row | inspect.sh migrations |
</persistenz_schicht_verifikation>

<tasks>

<task type="auto">
  <name>Task 1: scripts/triage.sh — Gate-Provisioning 1:1 minus Restart, targeted pytest mit Argument-Forwarding (D-G08)</name>
  <read_first>
    - deploy.sh:130-200 (der gesamte Gate-Provisioning-Block — die 1:1-Vorlage; ACHTUNG der pytest-Block dort haengt ein FESTES `pytest tests/ --tb=short -q` an, OHNE `$@` — Gemini-Re-Review R2 / Fund #5)
    - PATTERNS.md Sektion 3 (verbatim Block (1)-(9) + was triage.sh aendert: `bash -c '... pytest "$@"' _ "$@"`, KEIN POST-SUITE, KEIN .deploy_meta, KEIN systemctl restart)
    - inspect.sh migrations-Output aus der Persistenz-Schicht-Verifikation (prod-head)
  </read_first>
  <action>
    Erstelle `scripts/triage.sh` (ASCII, bash, `set -euo pipefail` Header). Faktoriere den deploy.sh-Gate-Provisioning-Block 1:1 heraus:

    BEHALTEN verbatim (PATTERNS.md Sektion 3, Bloecke 1-9):
    1. Whitelist-Guard: `TEST_DB="nerve_test"`; `if [ "$TEST_DB" != "nerve_test" ]; then echo "[triage] FATAL: Test-DB != nerve_test"; exit 1; fi` (Prod-Schutz, Req-5/10).
    2. `cleanup() { sudo -u postgres psql -c "DROP DATABASE IF EXISTS \"$TEST_DB\";" 2>/dev/null || true; }` + `trap cleanup EXIT` (rueckstandsfrei auch bei Fehler).
    3. Pre-Run-DROP.
    4. `CREATE DATABASE "$TEST_DB" OWNER postgres;` (NICHT nerve_app — crm owner-bypass RLS).
    5. Schema-Dump MIT `set -o pipefail`: `sudo -u postgres bash -c "set -o pipefail; pg_dump --schema-only nerve | psql -v ON_ERROR_STOP=1 -d $TEST_DB"`.
    6. Stamp-Row-Dump (alembic_version) MIT pipefail.
    7. `alembic upgrade head` gegen nerve_test.
    8. (Optional) das Dump-Treue-Katalog-Gate (deploy.sh:166-174) — uebernehmen fuer Konsistenz, aber nicht zwingend; falls weggelassen, im Kommentar begruenden.
    9. pytest-Block: ANON_PW via env, single-quoted inner bash -c, 4 DSNs + DATABASE_URL alle `@/${TEST_DB}`.

    AENDERN gegenueber deploy.sh:
    - Die letzte pytest-Zeile wird `... /opt/nerve/venv/bin/pytest "$@"` — die uebergebenen Argumente (z.B. `tests/test_x.py::test_y -v --tb=long`, D-G08/D-G09). KEIN festes `tests/ --tb=short -q`, KEIN festes `-m`.
    - ARGUMENT-DURCHREICHUNG IN DIE INNERE SUBSHELL (Gemini-Re-Review R2 / Fund #5, BLOCKER): Das innere `pytest "$@"` steht INNERHALB des single-quoted `sudo -u nerve_app env ... bash -c '...'`-Blocks. `$@` wird dort von der AEUSSEREN Shell NICHT expandiert (single-quote) → die innere bash bekaeme KEINE Argumente → `bash scripts/triage.sh tests/test_x.py` wuerde die GANZE Suite laufen → der Harness verliert seine Kern-Faehigkeit (targeted runs) → empirische Triage (Plan 04) unmoeglich. FIX: die aeusseren Argumente HART in die innere Subshell durchreichen, indem NACH dem schliessenden single-quote von `bash -c` `_ "$@"` angehaengt wird. Das `_` belegt `$0` der inneren bash, danach folgen die echten Argumente als `$1, $2, ...`. Exakte Aufruf-Form (verbindlich):
      ```
      sudo -u nerve_app env ANON_PW="$ANON_PW" TEST_DB="$TEST_DB" bash -c '
        cd /opt/nerve/app && \
        DATABASE_URL="postgresql://nerve_app@/${TEST_DB}" \
        TEST_DATABASE_URL="postgresql://nerve_app@/${TEST_DB}" \
        NERVE_APP_TEST_DSN="postgresql://nerve_app@/${TEST_DB}" \
        NERVE_SCHILD_TEST_DSN="postgresql://nerve_app@/${TEST_DB}" \
        ANON_WORKER_TEST_DSN="postgresql://nerve_anon_worker:${ANON_PW}@127.0.0.1:5432/${TEST_DB}" \
        /opt/nerve/venv/bin/pytest "$@"
      ' _ "$@"
      ```
      Beachte das abschliessende `' _ "$@"` NACH dem schliessenden single-quote: `_` = inneres `$0`, dann werden die `"$@"` des Triage-Skripts positional in die innere bash gereicht, sodass `pytest "$@"` sie expandiert. OHNE dieses `_ "$@"` ist der Harness funktional kaputt (laeuft immer die ganze Suite).
    - KEIN POST-SUITE-Check (Z.191-199).
    - KEIN `.deploy_meta`-Write (Z.218-219).
    - KEIN `systemctl restart` / kein Deploy (D-G08: triagiert nur, deployt NIE — Req-5/10).
    - Diagnose-Prints: NICHT in den Code; der Harness laeuft die Tests so wie sie sind. Iteratives Instrumentieren passiert in Plan 04 (temporaere Prints in Tests, NICHT committet).

    Usage-Kommentar im Header: `# Usage: bash scripts/triage.sh <pytest-args>  z.B. bash scripts/triage.sh tests/test_eur_calculator.py -v --tb=long`. Bleibendes Werkzeug (Ratchet) — im Kommentar vermerken.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_schema_introspect.py -v'</automated>
    HINWEIS: Smoke-Test gegen einen einzelnen (in Plan 01 erstellten) Test — belegt, dass der Harness provisioniert, NUR diesen einen Test laeuft (Argument-Forwarding griff — pytest sammelt 1 Datei, nicht die ganze Suite), und per trap nerve_test droppt OHNE Restart. Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "trap cleanup EXIT" scripts/triage.sh` liefert >=1 Treffer (rueckstandsfreier Teardown)
    - `grep -n 'TEST_DB="nerve_test"' scripts/triage.sh` belegt Whitelist-Guard
    - `grep -n "set -o pipefail" scripts/triage.sh` liefert >=2 Treffer (pg_dump|psql-Maskierungs-Schutz)
    - `grep -n 'pytest "\$@"' scripts/triage.sh` belegt targeted-Aufruf
    - `grep -n "' _ \"\$@\"" scripts/triage.sh` liefert >=1 Treffer (Gemini-Re-Review R2 / Fund #5: Argument-Forwarding-Token nach dem schliessenden single-quote von `bash -c` — beweist, dass `$@` hart in die innere Subshell gereicht wird, sonst leerlaeuft die Argument-Liste und der Harness laeuft die ganze Suite)
    - `grep -c "systemctl restart" scripts/triage.sh` == 0 (deployt NIE, D-G08)
    - `grep -c ".deploy_meta" scripts/triage.sh` == 0 (kein Deploy-Meta-Write)
    - `grep -c "@/nerve\b" scripts/triage.sh` zeigt KEINEN @/nerve-DSN; alle DSNs @/${TEST_DB} (Req-5/10)
    - Smoke-Test: `triage.sh tests/test_schema_introspect.py -v` collected NUR die Tests dieser EINEN Datei (nicht ~600) — empirischer Beleg, dass das Forwarding griff (Fund #5); danach existiert keine verwaiste nerve_test-DB (trap griff)
  </acceptance_criteria>
  <done>scripts/triage.sh provisioniert nerve_test 1:1, laeuft targeted pytest via `bash -c '... pytest "$@"' _ "$@"` (Argument-Forwarding, Fund #5), droppt per trap, deployt NIE, kein @/nerve. Bleibendes Werkzeug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Harness → Datenbank | darf nur nerve_test anlegen/droppen, nie Prod nerve beruehren |
| Harness → Deploy-Pfad | darf NIE systemctl restart / Deploy ausloesen |
| Harness → Argument-Forwarding | uebergebene Test-Selektoren MUESSEN die innere bash erreichen, sonst stille Full-Suite (Triage-Integritaet) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-GREEN-10 | Elevation/Prod-Safety | Harness DB-Provisioning | mitigate | Whitelist-Guard TEST_DB="nerve_test" + CREATE OWNER postgres; grep belegt kein @/nerve. nerve_test-only (Req-5/10). |
| T-PGTEST-GREEN-11 | Denial-of-Service (orphan-DB) | Harness-Abbruch | mitigate | trap cleanup EXIT droppt nerve_test auch bei Test-Fehler/SIGTERM; grep belegt trap. |
| T-PGTEST-GREEN-12 | Tampering (Harness-Integritaet: unbeabsichtigter Deploy + stille Full-Suite) | Harness pytest-Pfad | mitigate | D-G08: KEIN systemctl restart, KEIN .deploy_meta; grep-count == 0 fuer beide. ZUSAETZLICH (Gemini-Re-Review R2 / Fund #5): die Argumente werden per `bash -c '... pytest "$@"' _ "$@"` in die innere single-quoted bash durchgereicht — ohne dieses Forwarding expandiert `$@` nicht, der Harness laeuft STILL die ganze Suite statt der gezielten Tests (Triage-Integritaet verloren, Plan 04 unmoeglich). grep `' _ "$@"` >=1 + Smoke-Test belegt: nur die EINE Datei collected. |
| T-PGTEST-GREEN-13 | Tampering (silent false-green) | pg_dump | psql Pipe | mitigate | set -o pipefail vor jedem pg_dump|psql (sonst maskiert psql-exit-0 pg_dump-Crash → leere DB). grep >=2. |

Block severity = high (ein Harness, der versehentlich Prod nerve anfasst, deployt, ODER trotz Argument die ganze Suite laeuft, ist hochgefaehrlich bzw. wertlos fuer die Triage).
</threat_model>

<verification>
- Smoke-Lauf via SSH: scripts/triage.sh laeuft einen einzelnen Test (Argument-Forwarding griff — pytest collected nur die eine Datei), droppt nerve_test per trap, kein Restart.
- grep belegt: kein @/nerve, kein systemctl restart, trap vorhanden, `' _ "$@"`-Forwarding vorhanden (Fund #5).
- KEIN lokaler pytest (CLAUDE.md HART).
</verification>

<success_criteria>
- scripts/triage.sh ist ein bleibendes Werkzeug, das gezielte Tests gegen nerve_test laeuft ohne Deploy (Req-5).
- Argument-Forwarding (`bash -c '... pytest "$@"' _ "$@"`) griff — targeted runs funktionieren, nicht stille Full-Suite (Fund #5).
- Prod-nerve nie beruehrt; nerve_test trap-gedroppt (Req-5/10).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-03-SUMMARY.md`
</output>
</content>
</invoke>
