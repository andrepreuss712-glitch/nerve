---
phase: 08.23.2.PGTEST.GREEN
plan: 05
type: execute
wave: 5
depends_on: [02, 03, 04]
files_modified:
  - tests/
  - tests/MARKERS.md
autonomous: false
requirements: [Req-1, Req-7, Req-8, Req-10]
must_haves:
  truths:
    - "Die ~29 echt env-abhaengigen Tests (real-Haiku-Integration, p95/Perf-Latenz, Re-ID-Rate) tragen live/perf-Marker und werden vom Gate exkludiert"
    - "Kein Security/RLS/Anonymisierung/Isolations-Test traegt einen live/perf-Marker (grep belegt das)"
    - "Security-Tests die real-API brauchen sind deterministisch gemockt (Mock macht NUR die externe Abhaengigkeit deterministisch, NICHT die Sicherheits-Logik) und erscheinen im Gate als PASSED"
    - "tests/MARKERS.md listet jeden markierten Test + Grund + separaten Lauf-Befehl + Voraussetzungen"
    - "Ein voller deploy.sh production-Lauf endet exit 0; der systemctl-Restart passiert; nerve_test ist trap-gedroppt; Prod-nerve ist Schema/Daten-unveraendert"
  artifacts:
    - path: "tests/MARKERS.md"
      provides: "lebende Betriebs-Doku: Test→Marker→Grund + separater Lauf-Befehl + Voraussetzungen"
      contains: "pytest -m"
  key_links:
    - from: "live/perf-Marker in tests/"
      to: "deploy.sh -m \"not live and not perf\""
      via: "Gate-Exklusion (Plan 02 verdrahtet, Plan 05 wendet an)"
      pattern: "pytest.mark.(live|perf)"
    - from: "Security-Test mock"
      to: "deterministischer Security-Pfad im Tor"
      via: "monkeypatch des anthropic-Clients (canned response, Sicherheits-Logik laeuft echt)"
      pattern: "monkeypatch.*setattr"
---

<objective>
Wende live/perf-Marker an + mache Security-Tests deterministisch (Teil c, D-G13/D-G14/D-G15) und fahre den finalen `deploy.sh production`-Lauf (Req-1). Markiere die ~29 echt env-abhaengigen Tests (`live`=real-API, `perf`=Timing). Mache Security/RLS/Anon-Tests, die heute nur wegen fehlendem API-Key skippen/roten, via monkeypatch deterministisch (PRÄZISIERUNG 1: Mock macht NUR die externe Abhaengigkeit deterministisch, NICHT die Sicherheits-Logik — sonst false-green) und LASSE sie im Tor (PASSED). Schreibe `tests/MARKERS.md`. Final: voller Production-Deploy exit 0 + Restart.

Purpose: Env-abhaengige Tests verlassen das Gate sauber (Req-7, kein verstecktes Skippen); Security bleibt deterministisch im Tor (Req-8). Der finale Deploy ist der Grün-Nachweis (Req-1) + Prod-Safety-Beleg (Req-10).

Output: live/perf-Marker auf ~29 Tests, monkeypatch-Mocks auf betroffenen Security-Tests, `tests/MARKERS.md` (NEU), gruener Production-Deploy.

autonomous: false — der finale `deploy.sh production` (Task 3) ist ein echter Prod-CREATE/DROP-DATABASE-Deploy + Restart (🔴, hard-to-reverse). Andre startet ihn selbst.

depends_on: Plan 02 (Marker registriert + Gate-Exklusion verdrahtet), Plan 03 (Harness fuer Determinismus-Identifikation, D-G14), Plan 04 (Triage abgeschlossen, keine offenen kritischen Bugs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-SPEC.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-CONTEXT.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-PATTERNS.md

<interfaces>
<!-- Determinismus-Mock-Vertraege (PATTERNS.md Sektion 7). -->

_fake_anonymize (test_anonymizer_worker.py:45-48) — der canned-stub-Analog (PRÄZISIERUNG 1: macht NUR den Modell-Call deterministisch, Sicherheits-Logik laeuft echt):
```python
def _fake_anonymize(text_in, _cache):
    if 'GEHEIM' in text_in:
        return ('[ART9_REDACTED]', 'C')
    return (text_in.replace('Mueller', '[PERSON_A]'), 'A')
```
Injiziert als `anonymize_fn=_fake_anonymize` (call sites 161/395/406) — dependency injection. Aequivalent fuer anthropic-Client: `monkeypatch.setattr(<client>, ...)` mit realistischer canned response, sodass RLS/Filter/anon-merge ECHT laeuft + asserted wird.

SKIP-when-env-Muster (conftest.py:495-517) — heute skippen Security-Tests bei fehlendem API-Key; D-G14: empirisch via triage.sh feststellen welche, dann monkeypatchen statt skippen.

Marker registriert (Plan 02 pytest.ini): live, perf, security.
Gate-Exklusion verdrahtet (Plan 02 deploy.sh): -m "not live and not perf".
</interfaces>
</context>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

Dieser Plan aendert Test-Marker + Test-Mocks; er fasst keine NEUEN DB-Tabellen an. Die gemockten Security-Tests lesen/schreiben weiter die gleichen Tabellen wie bisher (crm.*/conversation_logs/...) — der Mock ersetzt NUR den externen anthropic-Call, nicht den DB-Pfad. Kein Schema-Eingriff.

### inspect.sh-Beleg
Nicht erforderlich (keine Schema-/Tabellen-Entscheidung). Falls ein gemockter Test eine Tabellen-Annahme prueft, in der SUMMARY belegen.
</persistenz_schicht_verifikation>

<tasks>

<task type="auto">
  <name>Task 1: live/perf-Marker auf ~29 env-abhaengige Tests + tests/MARKERS.md (D-G11/D-G15, Req-7/Req-8)</name>
  <read_first>
    - scripts/triage.sh (Plan 03 — zum empirischen Identifizieren welche Tests env-abhaengig skippen/roten)
    - PATTERNS.md Sektion 5 + 7 (Marker-Shape; No-Analog: tests/MARKERS.md ist neu)
    - CONTEXT.md D-G11/D-G13/D-G15 (live=real-API, perf=Timing; Re-ID-Rate-Bedingung; MARKERS.md-Inhalt)
    - pytest.ini (Plan 02 — live/perf registriert)
  </read_first>
  <action>
    1. Via Harness empirisch identifizieren, welche Tests echt env-abhaengig sind (real-Haiku-Integration, p95/Perf-Latenz, Re-ID-Rate-Messung am echten Modell).
    2. `@pytest.mark.live` auf real-API-Tests, `@pytest.mark.perf` auf Timing/p95-Tests (~29 gesamt). NUR fuer echt env-abhaengige (D-G11) — ein Test wird NICHT exkludiert, nur weil er stoert (Req-8).
    3. KRITISCH (Req-8): KEIN live/perf-Marker auf Security/RLS/Anonymisierung/Isolations-Tests. Re-ID-Rate (statistische Echt-Modell-Qualitaet) darf `live` tragen, NUR weil die Sicherheit/Logik der Anonymisierung deterministisch im Tor bleibt (Task 2, _fake_anonymize-Stub, D-G13).
    4. Erstelle `tests/MARKERS.md` (lebende Betriebs-Doku, NICHT Phasen-Dir, D-G15): Tabelle Test→Marker(live/perf)→Grund + separater Lauf-Befehl (`pytest -m "live or perf"`) + Voraussetzungen (ANTHROPIC_API_KEY, erwartbare Flakiness). Die Phasen-Triage-Tabelle (Plan 04) verlinkt MARKERS.md.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/ -v -m "not live and not perf"'</automated>
    HINWEIS: markierte Tests erscheinen als deselected; keine unknown-marker-Warnung. Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -rcn "pytest.mark.live\|pytest.mark.perf" tests/` deckt ~29 Tests ab (Groessenordnung)
    - `grep -rln "pytest.mark.live\|pytest.mark.perf" tests/ | grep -E "rls_isolation|anonymizer_worker|isolation|security"` → JEDER Treffer wird manuell geprueft; ein Security/RLS/Anon-Test mit live/perf == 0 (Req-8). Bei test_anonymizer_worker: nur ein etwaiger reiner Re-ID-Rate-Test darf `live` tragen (D-G13), der RLS/Anon-Logik-Teil NICHT.
    - `tests/MARKERS.md` existiert; `grep -n "pytest -m" tests/MARKERS.md` belegt separaten Lauf-Befehl
    - `grep -n "ANTHROPIC_API_KEY\|Flakiness\|Voraussetzung" tests/MARKERS.md` belegt Voraussetzungen
    - Im Harness-Lauf: keine unknown-marker-Warnung
  </acceptance_criteria>
  <done>~29 env-abhaengige Tests live/perf-markiert; kein Security/RLS/Anon-Test markiert; MARKERS.md mit Grund+Lauf-Befehl+Voraussetzungen.</done>
</task>

<task type="auto">
  <name>Task 2: Security/RLS/Anon-Tests deterministisch mocken (D-G14, Req-8) — bleiben im Tor PASSED</name>
  <read_first>
    - test_anonymizer_worker.py:45-48 (_fake_anonymize — der canned-stub-Analog, PRÄZISIERUNG 1)
    - conftest.py:495-517 (SKIP-when-env-Muster — die heute skippenden Security-Fixtures)
    - PATTERNS.md Sektion 7 (Mock macht NUR externe Abhaengigkeit; realistische canned response; PRÄZISIERUNG 2 fuer uncoverbaren Fall)
    - 08.23.2.PGTEST.GREEN-TRIAGE.md (Plan 04 — welche Security-Tests empirisch nur wegen API-Key skippen/roten)
  </read_first>
  <action>
    1. Via Harness (Plan 03) + Triage (Plan 04) empirisch feststellen, welche Security/DSGVO/RLS/Anon-Tests heute nur wegen fehlendem API-Key skippen/roten.
    2. Diese deterministisch machen via `monkeypatch.setattr(<anthropic-client>, ...)` mit realistischer canned response — DEPENDENCY INJECTION wo moeglich (anonymize_fn=_fake_anonymize-Muster), sonst monkeypatch.
       - PRÄZISIERUNG 1 (false-green-Guard, KRITISCH): der Mock macht NUR die nicht-deterministische EXTERNE Abhaengigkeit (Modell-/API-Aufruf) deterministisch. Er darf die SICHERHEITS-LOGIK NICHT umgehen. Die canned response muss realistisch genug sein, dass der echte Security-Pfad (RLS/Filter/Gating/Anon-Merge) WIRKLICH laeuft + asserted wird. Ein Mock, der die zu testende Eigenschaft wegmockt = false-green.
       - PRÄZISIERUNG 2 (Edge-Case): laesst sich ein Security-Test NICHT mocken ohne seinen Zweck zu zerstoeren → NICHT still per Marker raus. Stattdessen deterministische Variante ODER explizit dokumentierte Tor-Deckungs-Luecke in MARKERS.md mit kompensierenden Kontrollen (deterministischer Logik-Test + on-demand live-Lauf). Sichtbar, nicht versteckt.
    3. Die gemockten Tests BLEIBEN im Tor (kein live/perf-Marker) → erscheinen als PASSED.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_anonymizer_worker.py tests/test_rls_isolation.py -v'</automated>
    HINWEIS: die gemockten Security-Tests erscheinen als PASSED (nicht SKIPPED/FAILED). Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -rn "monkeypatch\|anonymize_fn=" tests/test_anonymizer_worker.py tests/test_rls_isolation.py` belegt deterministische Mocks
    - `grep -rcn "pytest.mark.live\|pytest.mark.perf" tests/test_rls_isolation.py` == 0 (RLS-Test bleibt im Tor, Req-8)
    - Die gemockten Security-Tests erscheinen im Harness-Lauf als PASSED (nicht SKIPPED)
    - Falls PRÄZISIERUNG 2 greift: `grep -n "Tor-Deckungs-Luecke\|coverage gap\|kompensierend" tests/MARKERS.md` dokumentiert sie sichtbar
  </acceptance_criteria>
  <done>Betroffene Security/RLS/Anon-Tests deterministisch gemockt (Logik laeuft echt), bleiben im Tor PASSED; etwaige Luecke sichtbar dokumentiert.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Finaler deploy.sh production-Lauf — Grün-Nachweis + Prod-Safety (Req-1/Req-10, Andre)</name>
  <what-built>Alle 4 Teile fertig: Auto-Reset-Waechter (Plan 01), crm-Derivation + Marker-Wiring (Plan 02), Triage-Harness (Plan 03), empirische Triage + xfail (Plan 04), live/perf-Marker + Security-Mocks (Plan 05 Task 1-2). Das Tor sollte jetzt gruen durchlaufen.</what-built>
  <how-to-verify>
    Andre startet den finalen Deploy selbst (🔴, Prod-CREATE/DROP-DATABASE + Restart, hard-to-reverse — Punkt 22):
    1. `bash deploy.sh production`
    2. Pruefe im Log:
       - Der pytest-Schritt endet gruen (modulo policy-erlaubter xfail aus Plan 04).
       - 0 `_baseline_cleanup_guard`-Leak-Fails.
       - POST-SUITE crm-Check (crm.* == 0) + training.transcript_archive == 0 gruen.
       - Der `systemctl restart` erscheint NACH dem Gate.
       - Der trap-Teardown droppt nerve_test.
       - `deploy.sh production` endet exit 0; neuer Code ist live.
    3. Prod-Safety-Gegenpruefung (Req-10):
       - `ssh ... 'sudo -u postgres psql -tAc "SELECT 1 FROM pg_database WHERE datname='\''nerve_test'\''"'` → leer (keine verwaiste nerve_test-DB).
       - `ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` → Prod-nerve Schema unveraendert (alembic_version unveraendert).
       - `grep -rn "@/nerve\b" deploy.sh scripts/ tests/conftest.py` → kein Fixture-/Gate-DSN auf @/nerve.
  </how-to-verify>
  <acceptance_criteria>
    - `deploy.sh production` endet exit 0; systemctl restart im Log nach dem Gate
    - 0 _baseline_cleanup_guard-Leak-Fails; POST-SUITE crm.*==0 + training==0 gruen
    - keine verwaiste nerve_test-DB/-Rolle (trap griff); Prod-nerve Schema/Daten unveraendert
    - `grep -rn "@/nerve\b"` zeigt keinen Fixture-/Gate-DSN auf @/nerve (Req-10)
  </acceptance_criteria>
  <resume-signal>Type "deploy gruen — exit 0, restart, nerve_test gedroppt, prod unveraendert" ODER beschreibe den Fehler-Schritt</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| live/perf-Marker → Test-Auswahl | ein Security-Test mit Marker = aus dem Tor versteckte Sicherheit |
| Security-Mock → getestete Eigenschaft | ein Mock, der die Sicherheits-Logik wegmockt = false-green |
| deploy.sh production → Prod nerve | der Deploy darf Prod nerve nicht beruehren; nerve_test trap-gedroppt |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-GREEN-18 | Information-Disclosure (silent-skip) | live/perf auf Security-Test | mitigate | Req-8: grep belegt kein Security/RLS/Anon/Isolations-Test mit live/perf; nur reine Re-ID-Rate darf live (D-G13). |
| T-PGTEST-GREEN-19 | Tampering (false-green via Mock) | Security-Determinismus-Mock | mitigate | D-G14 PRÄZISIERUNG 1: Mock macht NUR externe Abhaengigkeit deterministisch, Sicherheits-Logik laeuft echt + asserted. canned response realistisch. |
| T-PGTEST-GREEN-20 | Information-Disclosure (versteckte Luecke) | uncoverbarer Security-Test | mitigate | D-G14 PRÄZISIERUNG 2: nicht still per Marker raus → sichtbare Tor-Deckungs-Luecke in MARKERS.md + kompensierende Kontrolle. |
| T-PGTEST-GREEN-21 | Elevation/Prod-Safety | finaler deploy.sh production | mitigate | Req-10: nerve_test trap-gedroppt; Prod-nerve Schema/Daten unveraendert (inspect.sh-Gegenpruefung); kein @/nerve-DSN. Checkpoint-Andre-Verifikation. |
| T-PGTEST-GREEN-22 | Repudiation (stale xfail) | Gate gruen trotz offenem kritischen Bug | mitigate | Plan 04 Checkpoint stellt sicher: kein offener (iii)-kritischer Bug; sonst bleibt das Tor rot (kein gruener Deploy). |

Block severity = high (False-Green auf dem Deploy-Gate green-lightet bad deploys).
</threat_model>

<verification>
- ~29 env-abhaengige Tests live/perf-markiert; kein Security-Test markiert (Req-7/Req-8).
- Security-Tests deterministisch gemockt, im Tor PASSED (Req-8).
- Finaler deploy.sh production exit 0 + Restart; nerve_test gedroppt; Prod unveraendert (Req-1/Req-10).
- KEIN lokaler pytest (CLAUDE.md HART) — alles via Harness/Production.
</verification>

<success_criteria>
- live/perf-Marker korrekt angewendet, Security deterministisch im Tor (Req-7/Req-8).
- tests/MARKERS.md vollstaendig (D-G15).
- deploy.sh production gruen, Restart passiert, Prod-safe (Req-1/Req-10).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-05-SUMMARY.md`
</output>
