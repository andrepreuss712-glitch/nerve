---
phase: 08.23.2.PGTEST.GREEN
reviewers: [gemini]
reviewed_at: 2026-06-16
reviewer_model: gemini-3.1-pro-preview
plans_reviewed: [01, 02, 03, 04, 05]
note: "Gemini las die 5 Plaene + echten Code (conftest.py, deploy.sh) read-only via Claudian-gemini-stdin (NICHT GSD-CLI — die kam in PGTEST leer/flaky). Roh-OUT: _green_plan_gemini_OUT.md. Verdikt: BLOCK."
---

# Cross-AI Plan Review — Phase 08.23.2.PGTEST.GREEN (voller Plan-Review)

Fokus: die NEUEN Details seit dem Isolations-Direction-Review (`_green_isolation_gemini_OUT.md`, Option-1-split bereits abgesegnet) — topo-Sort, Harness, Mocks, Wave-Integration. **Verdikt: BLOCK, Risiko HIGH.** 4 Funde, alle von Claudian am Plan-Text + Schema gegengeprüft = real. Thema: der Plan setzt die Katalog-Allgemeinheit nur HALB um (CASCADE-Kanten weggelassen + PK-Spalte hardcoded `id`) → der Wächter würde bei CASCADE-Ketten / künftigen non-id-PK-TAXO-Tabellen still crashen = untergräbt genau die Req-9-Zukunftssicherheit.

## Funde

- **🔴 #1 BLOCKER (Plan 01 Task 1, topo-Sort) — CASCADE-Kanten weggelassen → Wächter-Crash bei CASCADE-Ketten:** Plan Z.158 fordert „CASCADE-Kinder (confdeltype='c') als Kanten WEGLASSEN". Bei einer Kette A→(CASCADE)→B→(RESTRICT)→C fehlt dem Sortierer die A→B-Kante → A wird evtl. vor C sortiert → Löschen von A cascadet auf B, scheitert weil C noch auf B zeigt (FK-Violation) → Wächter crasht → Tor dauerhaft blockiert (DoS). **FIX:** ALLE FK-Kanten (unabhängig von confdeltype) in den topo-Sort aufnehmen → garantiert Leaves-vor-Roots auch bei CASCADE. CASCADE-Deletes werden dann redundant (Kind schon explizit gelöscht), aber nie fehlerhaft. (Claudian-verifiziert: Plan sagt das wörtlich; Bug latent heute, aktiviert bei künftiger CASCADE-RESTRICT-Kette = TAXO-Risiko.)

- **🟠 #2 HOCH (Plan 01 Task 2, Auto-Delete) — hardcoded `id::text` ignoriert die abgeleitete PK-Spalte:** Task 1 leitet die PK-Spalte aus dem Katalog ab (D-G17 `primary_key_column`), aber das Auto-Delete (Z.34/81) hardcodet `WHERE id::text = ANY(:ids)`. `intent_event` hat PK `event_id` (nicht `id`) — sobald TAXO1 live + ein Test leakt eine intent_event-Zeile → DELETE crasht an „column id does not exist". **FIX:** Auto-Delete nutzt zwingend die in Task 1 abgeleitete PK-Spalte: `DELETE FROM {tbl} WHERE {pk_col}::text = ANY(:ids)`. (Claudian-verifiziert: models.py — intent_event.event_id ist PK; untergräbt Req-9-Zukunftssicherheit.)

- **🟠 #3 HOCH (Plan 01 Task 2, cleanup_rows-Kopplung):** Der Plan stellt das bestehende `cleanup_rows` (von 20+ Tests aufgerufen) auf die dynamische `reverse_fk_order` um. `cleanup_rows` ist aber eine normale Funktion ohne Zugriff auf die session-scoped Fixture → Executor müsste entweder die Signatur ändern (bricht 20+ Aufrufer) ODER `derive_baseline_tables` pro Aufruf gegen die DB laufen lassen (Suite extrem langsam). **FIX:** dynamische `reverse_fk_order` auf Modul-Ebene cachen (globales Dict in conftest, einmal bei Session-Start), ODER `cleanup_rows` behält die hardcoded `_CLEANUP_FK_ORDER` als best-effort-Fallback.

- **🟡 #4 NIEDRIG (Plan 02 Task 1, crm_leak_count):** `scripts/_crm_leak_count.py` nutzt `derive_baseline_tables(schema='crm')` (liefert Tupel), aber der Plan lässt offen, wie die Counts laufen. **FIX:** ergänzen — Tabellen-Array aus dem Tupel extrahieren, pro Tabelle iteratives `SELECT count(*)`.

## Cross-AI-Bewertung (Punkt 7/8/24)
Gemini stark + code-belegt — fand 1 echten latenten Wächter-Crash + 1 Req-9-Zukunftssicherheits-Loch, die Plan-Checker (grün) + Claudian-Pre-Review (Entscheidungen gelandet ✓) übersahen, weil beide nicht adversarial die HALB-umgesetzte Katalog-Allgemeinheit prüften. Kein Übereifer. Read-only, am realen Bundle (163 KB), gemini-3.1-pro-preview, Claudian-stdin-Pfad.

## Status
4 Funde zum Fold via `/gsd-plan-phase --reviews`. Thema-Klammer für den Fold: **topo-Sort + Auto-Delete VOLL katalog-allgemein machen** (alle FK-Kanten + abgeleitete PK-Spalte überall), nicht halb. Danach Claudian-Re-Check + ggf. erneute Gemini-Runde (BLOCK→fold→Gemini-Loop wie PGTEST), dann supervised Execute.

---

## Re-Review RUNDE 2 (nach Fold der 4 + PATTERNS.md) — Gemini 3.1 Pro — 2026-06-16

Claudian-stdin, 194-KB-Bundle (5 Pläne + PATTERNS.md + conftest.py + deploy.sh). Roh-OUT: `_green_plan_gemini2_OUT.md`. **Verdikt: BLOCK, Risiko HIGH.** Konvergiert (R1=4 → R2=3).

**Teil 1 — die 4 Folds:** #1 (CASCADE-Kanten alle im Sort) OK · #3 (Modul-Cache + Fallback) OK · #4 (3-Tupel) OK · PATTERNS.md-Konsistenz OK. **#2 (derived PK): nur HALB** — Cast + Spalten-Ableitung drin, ABER Composite-PK-Edge-Case offen (→ #6).

**Teil 2 — 3 neue Funde (Claudian-gegengeprüft = real):**
- **🔴 #5 BLOCKER (Plan 03 triage.sh) — `$@` expandiert nicht im single-quoted bash -c → gezielte Tests unmöglich:** Plan Z.28/91 fordert `pytest "$@"` INNERHALB des single-quoted `sudo … bash -c '…'`. `$@` wird dort NICHT von der äußeren Shell expandiert → die innere bash erhält keine Argumente → `triage.sh test_x.py` läuft die GANZE Suite. Der Harness verliert seine Kern-Fähigkeit (targeted runs) → empirische Triage (Plan 04) unmöglich. **FIX:** Argumente hart an die innere Subshell durchreichen: `sudo -u nerve_app env … bash -c '… pytest "$@"' _ "$@"` (`_` belegt $0 der inneren bash, dann folgen die Argumente). (Claudian-verifiziert am Plan-Text.)
- **🟠 #6 HOCH (Plan 01 Task 1, Composite-PK) — Über-Lösch-Risiko/Crash:** Die PK-Ableitungs-Query (`indisprimary`) liefert bei zusammengesetztem PK MEHRERE Zeilen. Nimmt der Code nur die erste Spalte → `DELETE … WHERE org_id::text = ANY(…)` löscht VIEL mehr Rows als test-erzeugt (Over-Deletion) ODER crasht. **FIX:** in `primary_key_column` prüfen ob `len(pk_cols) != 1` → Tabelle NICHT fürs Auto-Delete freigeben, sondern mit Grund `"composite PK not supported for auto-delete"` ins foundation_register (Denylist, Req-7 — sichtbar). Das ist der Rest von #2 (PK-Generalität zu Ende denken).
- **🟡 #7 MITTEL (Plan 01 Task 2, Cache-Init) — Req-9 läuft sonst still ins Leere:** nicht hart garantiert, dass `_baseline_schema` (füllt den Modul-Cache) VOR anderen Tests läuft. Wenn nicht autouse / nicht als Dependency referenziert → Cache leer → cleanup_rows fällt STILL auf den Hardcode-Fallback zurück → die dynamische Ableitung (Req-9) ist praktisch wirkungslos (Meta-False-Green: Tor denkt es ist dynamisch, ist es nicht). **FIX:** `_baseline_schema` zwingend als Dependency (Parameter) der früh-laufenden `_baseline_snapshot`-Fixture anfordern → harte Reihenfolge-Garantie.

**Cross-AI-Bewertung:** Gemini erneut stark + konvergierend — bestätigte 3 der 4 Folds sauber, fand den #2-Rest (Composite-PK) + 2 echte neue (Harness-Bash-Bug = Show-Stopper für die Triage, Cache-Init = stilles Req-9-Loch). Kein Übereifer. Read-only, gemini-3.1-pro-preview, Claudian-stdin.

**Status:** 3 Funde (#5/#6/#7) zum Fold via `/gsd-plan-phase --reviews`. Danach Gemini Runde 3 (Loop wie PGTEST bis PASS), dann Pre-Execute-Audit + supervised Execute.

---

## Re-Review RUNDE 3 (nach Fold #5/#6/#7) — Gemini 3.1 Pro — 2026-06-16

Claudian-stdin, 214-KB-Bundle. Roh-OUT: `_green_plan_gemini3_OUT.md`. **Verdikt: BLOCK, Risiko HIGH.** Weiter konvergierend (R1=4 → R2=3 → R3=2).

**Teil 1 — die 3 Folds:** #5 (Forwarding `' _ "$@"`) OK · #7 (Cache-Fill-Garantie via Fixture-Dependency) OK · #1-#4 intakt, PATTERNS konsistent. **#6 (Composite-PK): nur HALB** — Crash/Over-Deletion behoben, ABER der Ausschluss reißt ein unbemerktes Loch auf + der Plan behauptet fälschlich dessen Sicherheit (→ #9).

**Teil 2 — 2 neue Funde (Claudian-gegengeprüft = real):**
- **🔴 #8 BLOCKER (Plan 01, cleanup_rows × Cross-Schema-FK) — Regression aus dem #3-Fold:** `cleanup_rows` räumt crm UND public auf (Plan 02 Z.109, D-G04 crm-Writer-Self-Clean). Der #3-Fold stellte cleanup_rows auf den `_DERIVED_FK_ORDER` um — der wird aber mit `schema='public'` gefüllt → enthält NUR public.*. crm-Tabellen fehlen → fallen in den Fallback-Bucket ans ENDE der Lösch-Liste. Aber `crm.accounts → public.tenant_orgs` (FK) → public zuerst gelöscht → harte FK-Violation → jeder crm+public-aufräumende Test crasht. Die alte hardcoded `_CLEANUP_FK_ORDER` hatte crm VOR public sortiert. Plus `_DERIVED_PK_COLS` kennt keine crm-Tabellen (non-id-PK crm → Crash). **FIX:** `derive_baseline_tables` akzeptiert mehrere Schemas (`n.nspname IN %s`, z.B. public+crm+training); conftest füllt den Cache für ALLE → globale FK-Order stimmt schema-übergreifend; der Snapshot-Wächter filtert die table_list lokal auf `public.` (D-G04 public-only bleibt). (Claudian-verifiziert: Plan 02 Z.109 cleanup_rows räumt crm.)
- **🟠 #9 HOCH (Plan 01, Composite-PK False-Green + unehrliche Behauptung):** der #6-Fold schließt Composite-PK-Tabellen aus der baseline_table_list aus (gegen Over-Deletion, korrekt), ABER der Plan behauptet, ein Leak dort wäre „über den Snapshot/missing-mutated-Pfad sichtbar". Technisch unmöglich: ausgeschlossen = nie iteriert = nie in current/baseline = `_diff_baseline` sieht sie nie → komplett blindes Loch für Leaks/Mutationen (False-Green) + der Plan LÜGT über die Sicherheit. **FIX:** ehrlich sein (Req-7) — die falsche Sichtbarkeits-Behauptung RAUS, stattdessen offen als „Bekannte Tor-Lücke: Composite-PK-Tabellen werden nicht überwacht" ins foundation_register. ODER `_snapshot_public_tables` auf Tuple-Key (alle PK-Spalten) umbauen → Lücke ganz schließen. (Empfehlung: prüfen ob heute eine public/crm-Tabelle Composite-PK hat — wenn ja, Tuple-Key bauen; wenn nein, Lücke ehrlich dokumentieren + Tuple-Key als Folge wenn TAXO eine bringt.)

**Cross-AI-Bewertung:** Gemini erneut stark — bestätigte 2 der 3 Folds sauber, fand 1 echte Fold-Regression (Cross-Schema, der #3-Fold brach crm-Cleanup) + 1 unehrliche False-Green-Behauptung (#6-Rest). Genau das Muster „jeder Fold deckt die nächste Katalog-Generalität-Naht auf" — konvergierend. Kein Übereifer. gemini-3.1-pro-preview, Claudian-stdin.

**Status:** 2 Funde (#8/#9) zum Fold via `/gsd-plan-phase --reviews`. Danach Gemini Runde 4 (Loop bis PASS), dann Pre-Execute-Audit + supervised Execute. Konvergenz 4→3→2 → PASS nah.

---

## Re-Review RUNDE 4 (nach Fold #8/#9) — Gemini 3.1 Pro — 2026-06-16 — ✅ PASS

Claudian-stdin, 237-KB-Bundle. Roh-OUT: `_green_plan_gemini4_OUT.md`. **Verdikt: GESAMT-PASS, Risiko gering, „Execute kann erfolgen."** (Hinweis: stderr zeigte einen transienten 429, aber die zurückgegebene Ausgabe ist ein vollständiger, spezifischer Pro-grade Review — verifiziert jeden Fold akkurat + eigener Sweep, kein Flash-Stempel.)

- **#8 (Cross-Schema-FK): OK** — Multi-Schema-Tuple (public+crm+training), globale Order reiht crm-Kinder strikt vor public-Eltern, cleanup_rows löscht korrekt, Wächter filtert via `startswith('public.')` lokal → D-G04 public-only unangetastet, keine neuen Lücken.
- **#9 (Composite-PK): OK** — falsche Sicherheit raus, ehrlich als `known gate gap` im foundation_register; 0 Composite-PK-Tabellen im Prod-Katalog (16.06.) → leere Lücke, YAGNI-Aufschub korrekt + transparent.
- **#1-#7: alle intakt** — CASCADE-Kanten im Sort, `{pk_col}::text` statt id, Cache-Fill via Fixture-Dependency, iterativer crm-Count, `_ "$@"`-Forwarding.
- **Sweep:** triage.sh-Bash-Quoting wasserdicht; Security-Mocks verhindern false-green (RLS-/Filter-Logik läuft echt durch); Cross-Layer keine Diskrepanzen (nerve_app- vs. postgres-peer-Trennung schlüssig).

**🎯 KONVERGENZ KOMPLETT: R1=4 → R2=3 → R3=2 → R4=PASS.** Gefangene Fehler über die 4 Runden (alle auf dem Papier, vor dem Bau): CASCADE-Topo-Crash, hardcoded-id-Crash, cleanup_rows-Kopplung, crm-count-Detail, triage.sh-`$@`-Bug (Harness tot), Composite-PK-Over-Deletion + False-Green, Cross-Schema-FK-Regression. **Jeder einzelne wäre ein Laufzeit-Crash/False-Green beim Bau gewesen = Anti-Abrieb in Reinform.**

**Cross-AI-Bewertung (Punkt 7/8/24):** Gemini über 4 Runden durchgehend stark — jede Runde echte, code-belegte Funde, sauber konvergierend, ehrliches PASS am Ende (kein Übereifer). Claudian-stdin-Pfad (GSD-CLI war flaky), gemini-3.1-pro-preview, read-only. Volltexte: `_green_plan_gemini_OUT.md` (R1), `_green_plan_gemini2/3/4_OUT.md`.

**OFFEN:** Claudian Pre-Execute-Audit → supervised Execute (Plan 04/05 + finaler `deploy.sh production` = autonomous:false, Prod CREATE/DROP = Punkt 22, Claudian supervises + verifiziert Prod-Sicherheit per SSH).
