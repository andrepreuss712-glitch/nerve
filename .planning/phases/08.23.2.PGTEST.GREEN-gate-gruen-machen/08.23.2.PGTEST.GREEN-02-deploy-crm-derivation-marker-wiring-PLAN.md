---
phase: 08.23.2.PGTEST.GREEN
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - deploy.sh
  - pytest.ini
  - scripts/_crm_leak_count.py
autonomous: true
requirements: [Req-7, Req-9, Req-10]
must_haves:
  truths:
    - "Der deploy.sh POST-SUITE crm-Leak-Check zaehlt alle crm.*-Tabellen dynamisch aus dem Katalog (pg_tables WHERE schemaname='crm'), nicht aus einer Hand-getippten UNION"
    - "Eine neu angelegte crm-Tabelle wird automatisch mit-bewacht, ohne deploy.sh-Hand-Edit"
    - "Der deploy.sh pytest-Aufruf traegt -m \"not live and not perf\""
    - "pytest.ini registriert live + perf (keine unknown-marker-Warnung)"
    - "Kein Fixture-/Gate-DSN zeigt auf @/nerve (immer @/nerve_test)"
  artifacts:
    - path: "scripts/_crm_leak_count.py"
      provides: "python -c-Wrapper, der _schema_introspect fuer crm-Schema aufruft und die Leak-Count-Summe liefert (postgres peer)"
      contains: "schema='crm'"
    - path: "deploy.sh"
      provides: "dynamischer crm-Leak-Check + -m not live and not perf am pytest-Aufruf"
      contains: "not live and not perf"
    - path: "pytest.ini"
      provides: "live + perf Marker registriert"
      contains: "live:"
  key_links:
    - from: "deploy.sh POST-SUITE crm-Check"
      to: "scripts/_crm_leak_count.py → tests/_schema_introspect.py"
      via: "python -c als postgres peer, schema='crm'"
      pattern: "_crm_leak_count\\|_schema_introspect"
    - from: "deploy.sh pytest-Aufruf"
      to: "Gate-Exklusion env-abhaengiger Tests"
      via: "-m \"not live and not perf\""
      pattern: "not live and not perf"
---

<objective>
Verdrahte das Tor (Teil c Gate-Wiring + Teil d crm-Haelfte): (1) der deploy.sh POST-SUITE crm-Leak-Check wird von der hand-getippten UNION (deploy.sh:195) auf eine dynamische Katalog-Ableitung via `tests/_schema_introspect.py` umgestellt (D-G16/D-G17/D-G18, Req-9 crm-Haelfte); (2) der deploy.sh pytest-Aufruf bekommt `-m "not live and not perf"` (D-G12, Req-7); (3) pytest.ini registriert `live`+`perf` (D-G11).

Purpose: Die crm-UNION ist heute hand-getippt → kennt `intent_event`/kuenftige TAXO-Tabellen nicht → False-Green-Blindspot vor jedem TAXO-Deploy. Die Marker-Registrierung + Gate-Exklusion ist die Grundverdrahtung, auf die Plan 05 (Marker-Anwendung) aufsetzt.

Output: `scripts/_crm_leak_count.py` (NEU, der postgres-peer-Wrapper), modifizierte `deploy.sh` (crm-Check + pytest-Aufruf), modifizierte `pytest.ini`.

Disjoint zu Plan 01 (files_modified: deploy.sh/pytest.ini/scripts vs. tests/conftest.py/tests/_schema_introspect.py). depends_on Plan 01, weil Plan 02 das von Plan 01 erstellte `tests/_schema_introspect.py` wiederverwendet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-SPEC.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-CONTEXT.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-PATTERNS.md

<interfaces>
<!-- Vertraege, die Plan 02 konsumiert (aus Plan 01 + bestehender deploy.sh). -->

tests/_schema_introspect.derive_baseline_tables(conn_or_dsn, schema='crm') (Plan 01) → liefert (table_list, reverse_fk_order, foundation_register). Plan 02 nutzt NUR table_list fuer die crm-Leak-Count-Summe.

deploy.sh:195 — die zu ersetzende hand-getippte crm-UNION (verbatim):
```bash
CRM_LEFTOVER=$(sudo -u postgres psql -tAc "SELECT coalesce(sum(c),0) FROM (SELECT count(*) c FROM crm.account_memory UNION ALL SELECT count(*) FROM crm.accounts UNION ALL SELECT count(*) FROM crm.contacts UNION ALL SELECT count(*) FROM crm.meetings UNION ALL SELECT count(*) FROM crm.user_preferences) s" -d "$TEST_DB")
```

deploy.sh:187 — der zu aendernde pytest-Aufruf (verbatim, NUR die letzte Zeile):
```bash
    /opt/nerve/venv/bin/pytest tests/ --tb=short -q
```
(single-quoted inner bash -c bleibt — PW nie string-interpoliert, T-PGTEST-06)

pytest.ini (komplett):
```ini
[pytest]
norecursedirs = tests/archive
markers =
    security: marks tests as security tests requiring ANTHROPIC_API_KEY (deselect with -m "not security")
```

deploy.sh:197 — training.transcript_archive bleibt explizit single-table (D-G04, ORM-los, non-public — im foundation_register begruendet, NICHT dynamisiert).
</interfaces>
</context>

<verbindungs_karte>
## Verbindungs-Karte (CLAUDE.md Punkt 22 — Teil-d crm-Haelfte)

Dieser Plan trifft Tabellen-/Schema-Entscheidungen (welche crm.*-Tabellen werden gezaehlt). Jede Behauptung mit grep-Beleg UND inspect.sh/psql-Output (Production, read-only). Was LEBT gewinnt.

### NAMEN/IDENTIFIER
- crm-Tabellen-Set: NICHT aus dem Doc, sondern aus dem Prod-Katalog ableiten. Executor zieht VERBATIM:
  ```
  ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -tAc "SELECT tablename FROM pg_tables WHERE schemaname='\''crm'\'' ORDER BY 1;"'
  ```
  → belegt die LEBENDE crm-Tabellen-Liste (heute: account_memory/accounts/contacts/meetings/user_preferences laut deploy.sh:195 — gegen Prod gegenpruefen, evtl. mehr).
- `_schema_introspect` (Plan 01) als DRY-Quelle: `grep -rn "schemaname='crm'\|schema='crm'" deploy.sh scripts/ tests/` belegt, dass die crm-Liste NUR noch via Modul abgeleitet wird, nicht mehr hand-getippt.

### TABELLEN/SPALTEN — gegen Production verifiziert
- crm.* sieht nerve_app nur tenant-gefiltert → der Leak-Count MUSS als `postgres` peer laufen (RLS-bypassed, sieht alle Tenants). Beleg: deploy.sh:192 Kommentar + deploy.sh:195 laeuft bereits als `sudo -u postgres`.
- `inspect.sh schema <crm-tabelle>` ODER `sudo -u postgres psql -c "\d crm.<t>"` fuer jede gezaehlte Tabelle (Existenz/Befuellung).

### WER SCHREIBT / WER LIEST + Schicht-Check (Punkt 21)
- Leser: deploy.sh POST-SUITE (postgres peer) → `scripts/_crm_leak_count.py` → `_schema_introspect` (Katalog) → COUNT(*) pro crm-Tabelle. Persistenz-Schicht: echte DB-Rows in nerve_test crm.*.
- Schreiber (Test-Body): die wenigen crm-Writer (test_rls_isolation/test_anonymizer_worker) raeumen ihre crm-Rows weiter selbst via cleanup_rows (D-G04) — Plan 02 aendert das NICHT.

### REICHWEITE
- Die Aenderung beruehrt: den POST-SUITE-Block (deploy.sh:191-199) + den pytest-Aufruf (deploy.sh:180-188) + pytest.ini. KEIN App-Code-Pfad, KEINE Migration, KEINE RLS-Policy.
- training.transcript_archive (deploy.sh:197) BLEIBT explizit single-table (D-G04) — im foundation_register/Kommentar als bewusste Ausnahme dokumentiert.
</verbindungs_karte>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

### Angefasste Tabellen
- crm.* (lesen, COUNT als postgres peer) — siehe inspect.sh-Beleg unten
- training.transcript_archive (lesen, COUNT) — unveraendert single-table
- pg_tables/pg_class (Katalog, lesen) — rollen-agnostisch

### inspect.sh-Beleg (Executor zitiert verbatim in SUMMARY)
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -tAc "SELECT tablename FROM pg_tables WHERE schemaname='\''crm'\'' ORDER BY 1;"'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh tables'
```

### Cross-Layer-Konsistenz-Tabelle
| Datum | Lese-Pfad | Persistenz-Schicht | Verifiziert via |
|---|---|---|---|
| crm-Tabellen-Set | _crm_leak_count → _schema_introspect | pg_tables schemaname='crm' | inspect.sh psql oben |
| crm-Leak-Count | deploy.sh POST-SUITE als postgres | crm.* DB-Rows nerve_test | COUNT(*)-Summe |
| training-Leak-Count | deploy.sh single-table | training.transcript_archive DB-Rows | COUNT(*) (unveraendert) |
</persistenz_schicht_verifikation>

<tasks>

<task type="auto">
  <name>Task 1: scripts/_crm_leak_count.py + deploy.sh crm-Check dynamisieren (D-G16/D-G17/D-G18, Req-9 crm)</name>
  <read_first>
    - tests/_schema_introspect.py (Plan 01 — die DRY-Quelle, schema='crm' parametrisierbar)
    - deploy.sh:191-199 (POST-SUITE-Check: crm-UNION + training-single-table — der zu ersetzende Block)
    - deploy.sh:168-174 (pg_policies/pg_class counts via psql -tAc als postgres — das postgres-peer-Muster)
    - PATTERNS.md Sektion 6 (crm-UNION → python -c via _schema_introspect, postgres peer, training bleibt single-table)
    - inspect.sh crm-Tabellen-Liste aus der Verbindungs-Karte oben (LEBENDE crm-Liste von Prod)
  </read_first>
  <action>
    1. Erstelle `scripts/_crm_leak_count.py` (ASCII): ein minimales Wrapper-Skript, das via `_schema_introspect.derive_baseline_tables` mit `schema='crm'` die crm-Tabellen-Liste holt und die Summe der COUNT(*) ueber alle crm-Tabellen ausgibt (eine Zahl auf stdout). DSN/Connection als postgres peer (DATABASE_URL=postgresql://postgres@/$TEST_DB oder psycopg2-peer). sys.path-Einfuegen wie conftest.py:9-13, damit `from tests._schema_introspect import ...` aufgeloest wird. Beispiel-Aufruf in deploy.sh: `CRM_LEFTOVER=$(sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/$TEST_DB /opt/nerve/venv/bin/python scripts/_crm_leak_count.py")`.
    2. Ersetze in `deploy.sh` (Z.195) die hand-getippte `SELECT count(*) FROM crm.account_memory UNION ALL ...`-UNION durch den `_crm_leak_count.py`-Aufruf. Die fail-closed Pruefung `[ "$CRM_LEFTOVER" = "0" ] || { ...; exit 1; }` bleibt unveraendert (D-G04: harter BLOCK bei crm != 0, KEIN Auto-Clean).
    3. `training.transcript_archive`-Check (Z.197) BLEIBT explizit single-table (D-G04, ORM-los/non-public). Im Code-Kommentar als bewusste foundation_register-Ausnahme dokumentieren (Req-7: nicht still).
    4. Req-9-Beleg: nach der Aenderung wuerde eine frisch angelegte crm-Tabelle automatisch mit-gezaehlt (dynamisch via pg_tables).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve /opt/nerve/venv/bin/python scripts/_crm_leak_count.py"'</automated>
    HINWEIS: read-only gegen Prod nerve nur zum Smoke-Test der crm-Liste (liefert eine Zahl — Prod crm darf != 0 sein, hier nur "laeuft ohne Crash"). Endgueltig im vollen deploy.sh production-Gate (crm-Check gegen nerve_test == 0). Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "schema='crm'\|schemaname='crm'" scripts/_crm_leak_count.py` liefert >=1 Treffer (dynamische crm-Ableitung)
    - `grep -n "_schema_introspect\|derive_baseline_tables" scripts/_crm_leak_count.py` belegt DRY-Quelle (D-G18)
    - `grep -c "UNION ALL SELECT count(\*) FROM crm\." deploy.sh` == 0 (hand-getippte UNION entfernt, Req-9)
    - `grep -n "_crm_leak_count.py" deploy.sh` liefert >=1 Treffer (neuer dynamischer Aufruf)
    - `grep -n "transcript_archive" deploy.sh` bleibt vorhanden (training single-table unveraendert, D-G04)
    - `grep -n "CRM_LEFTOVER.*= .0." deploy.sh` belegt: fail-closed Pruefung crm==0 unveraendert
    - Im Gate: POST-SUITE crm-Check gegen nerve_test gruen (0 Leak-Rows)
  </acceptance_criteria>
  <done>crm-Leak-Check dynamisch via _schema_introspect (postgres peer); UNION entfernt; training single-table dokumentiert; fail-closed crm==0 erhalten.</done>
</task>

<task type="auto">
  <name>Task 2: pytest.ini live/perf-Marker registrieren + deploy.sh pytest-Aufruf -m "not live and not perf" (D-G11/D-G12, Req-7/Req-10)</name>
  <read_first>
    - pytest.ini (komplett — heute nur security-Marker)
    - deploy.sh:178-188 (pytest-Aufruf, single-quoted inner bash -c, kein -m heute)
    - PATTERNS.md Sektion 4+5 (pytest-Aufruf nur letzte Zeile aendern; pytest.ini live+perf neben security)
  </read_first>
  <action>
    1. `pytest.ini` (D-G11): `live` + `perf` neben `security` registrieren (gleicher Indent, `name: description`-Shape). Kein preemptiver `reid` (YAGNI). Beispiel:
       ```ini
       markers =
           security: marks tests as security tests requiring ANTHROPIC_API_KEY (deselect with -m "not security")
           live: marks tests that require a real external API (real-Haiku-Integration, Re-ID-Rate measurement) — excluded from the deploy gate (-m "not live")
           perf: marks p95/latency/timing tests — excluded from the deploy gate (-m "not perf")
       ```
    2. `deploy.sh` (D-G12, Z.187): NUR die letzte pytest-Zeile aendern auf:
       `/opt/nerve/venv/bin/pytest tests/ --tb=short -q -m "not live and not perf"`
       Den single-quoted inner bash -c NICHT aufbrechen (T-PGTEST-06: PW nie string-interpoliert). Den `-m`-String VERBATIM `"not live and not perf"`.
    3. Req-10-Regressionsschutz: NICHT den DATABASE_URL/TEST_DATABASE_URL/4-DSN-Block anfassen — alle zeigen weiter auf `@/${TEST_DB}` (=nerve_test). Kein `@/nerve` einbauen.
    HINWEIS: Security-Tests tragen KEINEN Marker (D-G13/D-G14) → `not live and not perf` schliesst sie NICHT aus, sie BLEIBEN im Tor. Die Marker-ANWENDUNG auf die ~29 env-abhaengigen Tests passiert in Plan 05 — dieser Task verdrahtet nur die Registrierung + Exklusion.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/python -c "import configparser; c=configparser.ConfigParser(); c.read(\"pytest.ini\"); print(c[\"pytest\"][\"markers\"])"'</automated>
    HINWEIS: read-only Smoke-Test der Marker-Registrierung gegen deployed Code (nach git push). Endgueltig im vollen deploy.sh production-Gate (keine unknown-marker-Warnung + -m greift). Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -n "^    live:" pytest.ini` liefert genau 1 Treffer (live registriert)
    - `grep -n "^    perf:" pytest.ini` liefert genau 1 Treffer (perf registriert)
    - `grep -n 'pytest tests/ --tb=short -q -m "not live and not perf"' deploy.sh` liefert genau 1 Treffer (Gate-Exklusion verbatim, D-G12)
    - `grep -c "@/nerve\b" deploy.sh` zeigt KEINEN neuen `@/nerve`-DSN im Gate-pytest-Block (Req-10; bestehende @/${TEST_DB} bleiben)
    - Im Gate: keine "unknown marker"-Warnung fuer live/perf; -m schliesst markierte Tests aus, Security bleibt drin
  </acceptance_criteria>
  <done>live+perf in pytest.ini registriert; deploy.sh pytest-Aufruf traegt -m "not live and not perf"; Security-Tests unmarkiert/im Tor; kein @/nerve-Regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Gate pytest-Aufruf → Test-Auswahl | ein Marker darf keinen Security-Test verstecken |
| crm-Leak-Check → crm-Tabellen-Set | eine ungezaehlte crm-Tabelle = unbewachter Cross-Tenant-Leak |
| Gate-DSN → Datenbank | jeder DSN muss auf nerve_test zeigen, nie auf Prod nerve |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-GREEN-06 | Tampering (False-Green-Blindspot) | deploy.sh crm-UNION | mitigate | D-G16/D-G18: crm-Liste dynamisch via pg_tables → neue crm/TAXO-Tabelle automatisch gezaehlt. grep belegt UNION entfernt. |
| T-PGTEST-GREEN-07 | Information-Disclosure (silent-skip) | -m "not live and not perf" | mitigate | D-G12/D-G13: Security-Tests tragen KEINEN Marker → bleiben im Tor; Plan 05 grep-belegt kein Security-Test mit live/perf. |
| T-PGTEST-GREEN-08 | Elevation/Prod-Safety | Gate-DSN | mitigate | Req-10: nur @/${TEST_DB} (nerve_test); grep belegt kein neuer @/nerve. crm-Count laeuft als postgres peer gegen nerve_test, nie Prod nerve. |
| T-PGTEST-GREEN-09 | Tampering | training.transcript_archive bleibt single-table | accept | D-G04: bewusst, ORM-los/non-public; im foundation_register/Kommentar dokumentiert (Req-7 — nicht still). |

Block severity = high.
</threat_model>

<verification>
- Voller `deploy.sh production`-Gate-Lauf (mit Plan 01): POST-SUITE crm-Check gegen nerve_test gruen; keine unknown-marker-Warnung; -m greift.
- Eine frisch angelegte crm-Tabelle wuerde automatisch mit-gezaehlt (Req-9 crm-Beleg).
- Kein @/nerve-DSN im Gate (Req-10).
- KEIN lokaler pytest als Acceptance (CLAUDE.md HART).
</verification>

<success_criteria>
- crm-Leak-Check dynamisch via _schema_introspect (Req-9 crm-Haelfte).
- live+perf registriert; deploy.sh pytest traegt -m "not live and not perf" (Req-7-Wiring).
- Prod-Safety unveraendert: kein @/nerve-DSN (Req-10).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-02-SUMMARY.md`
</output>
