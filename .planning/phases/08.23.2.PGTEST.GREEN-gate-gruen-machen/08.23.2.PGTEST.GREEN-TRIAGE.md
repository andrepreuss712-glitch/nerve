# PGTEST.GREEN — Wave-4 Empirische Triage

> Plan 04, Req-5/Req-6. Jeder Fail EMPIRISCH klassifiziert via Harness-Vollauf (`scripts/triage.sh tests/ -v --tb=long -m "not live and not perf"`) gegen echtes `nerve_test` @ Code `d00fd5a`. Roh-Tracebacks: `_tb_failures.log` / `_tb_errors.log` (lokal, gitignored).
>
> **Klassen:** (i) veralteter Test → fixen+grün · (ii) echter unkritischer Bug → `xfail(strict=True)`+999.x · (iii) KRITISCH (Security/DSGVO/RLS/Anon/Datenverlust) → BLOCK.

**Stand:** Klassifikation VOLLSTÄNDIG. Vollauf = **48 failed + 9 errors** (613 passed, 5 skipped).
**Ergebnis: 0 echte App-Bugs, 0 kritische (iii).** Alle 9 Errors = 1 conftest-Infrastruktur-Bug (gefixt). Alle 48 Fails = veraltete Tests (43) oder env-abhängig→Wave5 (5).

---

## Errors (9) — EINE Wurzel, gefixt

| nodeids | Symptom | Klasse | empirischer Beleg | Aktion |
|---|---|---|---|---|
| test_ewb_pipeline (4), test_prompt_pipeline (4), test_per_sid_migration (1) — alle „at teardown" | `AttributeError: 'function' object has no attribute 'configure'` @ conftest.py:702 | (i) Infra | Test patcht `database.db.SessionLocal` per monkeypatch auf eine Lambda (test_ewb_pipeline:66 / test_prompt_pipeline:72); Fixture-Teardown läuft LIFO VOR monkeypatch-undo → liest die Lambda statt sessionmaker | **GEFIXT**: conftest `db_session`+`client` halten echte sessionmaker-Referenz `_real_sm` beim Setup fest, Teardown nutzt diese (robust gg. Test-Monkeypatch) |

---

## Fails (43 veraltet, class i) — 4 Muster + App-Evolution

### Muster A — `get_session()` ohne bindende Fixture → `UnboundExecutionError: ...Mapper[Call(calls)]` (12)
Test (SQLite-Ära) ruft `get_session()`/`text()` direkt, hängt NICHT an `db_session`/`client` → MODUL-`SessionLocal` nie an `nerve_test` gebunden. **Fix: `db_session`-Fixture-Param ergänzen** (löst die Bindung als Seiteneffekt aus).

| nodeid | krit.Klasse? | empirischer Beleg |
|---|---|---|
| test_postcall_outcome_route::test_ownership_check_filter_blocks_foreign_call | **ja (ownership)** | scheitert in `_make_call`→`db.commit()` im SETUP, erreicht ownership-Prüfung NIE → kein Regress. Re-Run muss echte Prüfung grün zeigen |
| test_postcall_outcome_route::test_ownership_check_finds_own_call | **ja (ownership)** | dito |
| test_postcall_outcome_route::test_correct_outcome_sets_user_corrected_source | nein | `UnboundExecutionError` im SETUP |
| test_postcall_outcome_route::test_correct_outcome_empty_note_becomes_null | nein | dito |
| test_api_beenden_calls_update:: (6: update_helper×3, audio_health_bg×2, no_call_id_no_update_no_crash) | nein | `UnboundExecutionError` — `_make_test_call`/`get_session()` ohne Fixture |
| test_migration_0005::test_0005_existing_rows_null, ::test_0005_check_constraint_allows_valid_source | nein | `UnboundExecutionError` @ raw `text()` Session ohne Bind (Zeile 9348) |

### Muster B — Pflicht-Spalte fehlt im manuellen Test-INSERT → `NotNullViolation` (10)
Schema erhielt NOT-NULL-Spalte ohne server_default; SQLite-Ära-Test-INSERT setzt sie nicht. **Fix: Spalte im Seed/INSERT ergänzen.**

| nodeid | krit.Klasse? | empirischer Beleg |
|---|---|---|
| test_anonymizer_worker:: (5: test_worker_processes_unstamped, _skips_stamped, _filters_test_user, test_no_crm_id_in_training, test_dry_run_writes_nothing) | **ja (Anon/DSGVO)** | `NotNullViolation: users.is_superadmin` in `_seed_pg_account` (test_anonymizer_worker.py:78) — scheitert im SEED, Anon-Logik NIE erreicht → kein Anon-Leck. Re-Run muss echte Anon-Asserts grün zeigen |
| test_postcall_split:: (4: outcome_endpoint ×3, cards_endpoint) | nein | `NotNullViolation: conversation_logs.started_at` (Test setzt nur created_at, test_postcall_split.py:40) |
| test_dashboard_outcome_reminder::test_join_call_to_conversation_log | nein | `NotNullViolation: conversation_logs.started_at` (test_dashboard_outcome_reminder.py:190) |

### Muster C — Seed-Daten-Drift (echtes `nerve_test` trägt Gründer-Dashboard-Defaults) (5)
Test nimmt leere DB an; App-Start sät Wechselkurse/Preise/FixedCosts. **Fix: Vorher/Nachher-Delta statt absoluter Werte.**

| nodeid | empirischer Beleg |
|---|---|
| test_cost_tracker::test_freeze_fx_on_write | `assert 0.86252 == 0.92` — `get_current_rate` liefert gesäten heutigen Kurs |
| test_cost_tracker::test_eur_rate_currency_no_fx | `UniqueViolation: uix_api_rate_active` — Baseline-ApiRate aktiv, Test inserted Duplikat (Test-Isolation) |
| test_eur_calculator::test_stripe_not_in_rc13b | `assert 161.2 == 5.0` — Z57 summiert gesäte FixedCosts |
| test_exchange_rates::test_update_daily_rate_skips_on_api_failure | `assert 1 == 0` — gesäte source='seed'-Row |
| test_exchange_rates::test_get_current_rate_with_data | `assert 0.86252 == 0.91` — neuere gesäte heutige Row gewinnt date.desc() |

### Muster D — Legitime App-Evolution, Test nicht nachgezogen (16)
| nodeid | empirischer Beleg + Fix |
|---|---|
| test_08_13_01_config_constants::test_model_pip_autovar_default, ::test_model_pip_variante_default | config.py setzt Sonnet (D-07 DSGVO/Qualität), Test erwartet Haiku → Assertion auf `'claude-sonnet-4-5'` |
| test_claude_service_phase08::test_analysiere_mit_claude_streaming_uses_pipeline, ::test_haiku_model_preserved_in_both, ::test_import_smoke_no_side_effects | `analysiere_mit_claude_streaming` entfernt (konsolidiert) → 0 Aufrufer (grep). Test fixen/entfernen |
| test_claude_service_phase08::test_legacy_symbols_preserved | `_build_system_prompt` entfernt → **0 Aufrufer im App-Code (grep verifiziert)** → Assertion ist falsch, Test entfernen |
| test_profile_schema_v3:: (6: TestMigrateV3 basis_einwaende ×3, schema_version_bumped_to_3, toplevel_einwaende_wins, real_drift) | App migriert jetzt bis **v4** (`einwaende→einwaende_detail`, Phase 08.20 D-04, **gewollt + app-weit gelesen, kein Datenverlust** verifiziert). Tests erwarten v3-Zwischenstand → auf v4/`einwaende_detail` umstellen |
| test_qa_pipeline_rueckfrage::test_build_tabu_instruction_populated | Template-Text geändert: `'WICHTIG:'` → `'TABU-ALTERNATIVEN'` |
| test_qa_pipeline_t1::test_haiku_model_id_in_file | grep-subprocess mit kaputtem cwd + Modell-ID-String verschoben (mehrdeutig → Re-Run) |
| test_ewb_pipeline::test_build_ewb_prompt_anrede_du (FAILED-Teil) | Test erwartet `'Anrede: Du'`, Output hat `'Anrede: Sie'` (kein aktives Du-Profil im Setup) — mehrdeutig → Re-Run |
| test_phase_08_migration::test_blocks_ordered_a_b_c_d_e | brüchiger Test auf Text-Positionen in app.py; Block-Reihenfolge-Kommentare verschoben |

---

## env-abhängig → Wave 5 (live/perf-Marker, KEINE echten Fails) (5)
- test_anonymization_perf:: test_p95_latency, test_short_snippet_latency, test_art9_short_circuit_faster — `perf`
- test_anonymization_security::test_reid_rate_below_5_percent — `live` (echtes Modell)
- test_phase_classifier::test_phase_classifier_integration_real_haiku — `live`

---

## Kritische (iii)-Liste (Checkpoint Task 4 — André)
**KEINE.** Beide kritischen Familien (Anonymisierung 5, Zugriffskontrolle 4) scheitern ausschließlich im Test-SETUP (fehlende Pflicht-Spalte / fehlende Fixture-Bindung) und erreichen ihre Sicherheits-Asserts NIE. → (i) veraltet, NICHT (iii).
**Vorbehalt (Pflicht):** nach dem Fix muss der Harness-Re-Run beweisen, dass die echten Anon-/ownership-Asserts grün laufen. Erst dann ist „kein (iii)" bestätigt.

## (ii)-Liste (xfail-Kandidaten)
**KEINE** echten unkritischen App-Bugs gefunden. Alle Fails sind veraltete Tests (i) → fixen, kein xfail nötig.

---

## Iterativer Fix-Fortschritt (empirisch, Re-Runs via triage.sh)

**Gefixt + verifiziert:**
- conftest configure-Bug → 9 Errors weg (Wave-1-Infra).
- test_postcall_outcome_route::test_correct_outcome_sets_user_corrected_source + ::test_correct_outcome_empty_note_becomes_null → **GRÜN** (db_session-Param-Bindung).

**Nächste Schicht aufgedeckt (Fixes folgen iterativ, schema-bewusst):**
- **anonymizer (5):** users-Seed-Fix (is_superadmin/market/language) griff → nächste `NotNullViolation: conversation_logs.market` (auch NOT NULL ohne Default). Plus Sandbox-Befund: `[ANON] GLiNER nicht ladbar: PermissionError '/nonexistent'` → die Worker-Tests brauchen evtl. den `live`-Marker (Wave 5) statt nur Seed-Fix — beim Fixen klären ob die Anon-Logik ohne echtes NER-Modell überhaupt testbar ist.
- **ownership (2):** `ForeignKeyViolation: calls.user_id=999/42 not present in users` (FK auf PG aktiv, SQLite-Ära-Test) → echte Nutzer mit diesen IDs anlegen (kritische Klasse: muss die echte ownership-Prüfung WIRKLICH ausführen).
- Nebenbefund (nicht-blockierend): `[DB] Audit-Log Trigger setup failed: SyntaxError at "NOT"` im Test-Lauf — beobachten, separat von den Triage-Zielen.

**Muster bestätigt:** Fixes sind iterativ + schema-bewusst (pro Familie 1-3 Schichten + Re-Run). Klassifikation steht; reine Anwendung verbleibt.

---

## ✅ ABGESCHLOSSEN 2026-06-16 (Plan 04 + 05, supervised execute via triage.sh)

**FULL-SUITE-BEWEIS:** `scripts/triage.sh tests/ -m "not live and not perf"` → **638 passed, 6 skipped,
5 deselected, 0 failed, 0 errors** (echtes nerve_test, Code @ 4563603). Das gesamte Deploy-Gate ist GRÜN.

**Angewandte (i)-Fixes (alle via Server-triage.sh grün bewiesen, commits):**
- **Muster A** (659f7d6): db_session-Bindung — test_migration_0005 (3), test_api_beenden_calls_update (6,
  + fake conversation_log_id=12345 → echte ConversationLog wg. FK), outcome_route correct_outcome (2, e58c343).
- **Muster D** (00732ad): config_constants (2→Sonnet), qa_rueckfrage (1→TABU-ALTERNATIVEN),
  profile_schema_v3 (6→v4/einwaende_detail). **+6 PHANTOME** (git-gelöschte Stale-Files vom Server entfernt:
  test_claude_service_phase08, test_phase_08_migration, test_qa_pipeline_t1, tts_comparison) — waren keine
  Code-Fehler. Deploy-Tar re-added sie NICHT → permanent weg.
- **Muster C** (ff6b441): exchange_rates (2), cost_tracker (2), eur_calculator (1) — Seed-Drift → Delta /
  Future-Date / eindeutiges Modell.
- **non-krit Muster B + ewb** (8c6d57b): postcall_split (4) + dashboard test_join (1) → started_at;
  dashboard count-Tests (5) → db_session-Bindung (Triage unter-klassifiziert, Order-abhängige False-Greens);
  ewb anrede_du → build_profile_context leer monkeypatchen (echte Du-Direktive).
- 🔴 **KRITISCH ownership ×2** (5779151): echte FK-User (999/42) → echte Ownership-Prüfung läuft + besteht.
- 🔴 **KRITISCH anonymizer ×5** (3895035): conversation_logs-Seed market/language ergänzt. NER-Mock war
  bereits da (_fake_anonymize via anonymize_fn → GLiNER nie geladen, nur externe NLP ersetzt). Die ECHTE
  Logik läuft IM Gate: Anonymisierung schreibt um, should_persist droppt ART9/GEHEIM, is_test_user gefiltert,
  SHA-256-Hash kein Re-ID-Surface, dry-run schreibt nichts, cross-tenant-RLS + kein Leak. 7/7 grün.

**(iii)-Befund nach Anwendung: KEINE.** Beide kritischen Familien laufen ihre echten Asserts UND bestehen
(Vorbehalt aus Task 4 erfüllt). Kein echter Anon-/Zugriffs-Fehler → kein BLOCK, kein xfail nötig.

**Plan 05** (4563603): live/perf-Marker auf die 5 env-abhängigen (anonymization_perf ×3 perf,
anon_security reid + phase_classifier real_haiku live) + tests/MARKERS.md. collect-only: exakt 5 deselected.
grep belegt: kein RLS/Isolation/anon-LOGIK-Test trägt live/perf.

**STOP-PUNKT (eingehalten):** finaler `deploy.sh production` (grün + Restart) NICHT gefahren — Übergabe an
Claudian (Server-Selbst-Verifikation) / André (beaufsichtigter Produktiv-Deploy).
