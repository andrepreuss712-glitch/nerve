---
phase: 08.23.2.PGTEST.GREEN
plan: 04
type: execute
wave: 4
depends_on: [01, 03]
files_modified:
  - tests/
  - .planning/backlog.md
  - .planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-TRIAGE.md
autonomous: false
requirements: [Req-5, Req-6]
must_haves:
  truths:
    - "Jeder der ~22 Port-Assertion-Fails ist EMPIRISCH klassifiziert (instrumentiert + re-run via triage.sh), nicht aus dem Code-Lesen geraten"
    - "Eine Triage-Tabelle (pro Test: Klassifikation + empirischer Beleg) existiert im Phasen-Verzeichnis"
    - "Jeder als veraltet klassifizierte Test ist gefixt und gruen"
    - "Jeder empirisch bestaetigte echte NICHT-kritische Bug ist xfail(strict=True)+reason+verlinktem 999.x-Backlog-Ticket"
    - "Kein Security/DSGVO/RLS/Anonymisierung/Datenverlust-Test ist ge-xfailed oder ge-skippt (kritische Klassen BLOCKEN)"
  artifacts:
    - path: ".planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-TRIAGE.md"
      provides: "Triage-Tabelle: pro Test Klassifikation (i veraltet / ii unkritischer Bug / iii kritischer Bug) + empirischer Beleg"
      contains: "Klassifikation"
    - path: ".planning/backlog.md"
      provides: "999.x-Backlog-Tickets fuer jeden ge-xfailten echten NICHT-kritischen Bug"
      contains: "999."
  key_links:
    - from: "xfail-Marker in tests/"
      to: ".planning/backlog.md 999.x-Ticket"
      via: "reason= verlinkt die 999.x-ID"
      pattern: "xfail.*strict=True.*reason"
---

<objective>
Empirische Triage der ~22 phasen-eigenen Port-Assertion-Fails (Teil b, D-G09/D-G10) + Anwendung der Bug-Handling-Policy (Req-6). Jeder Fail wird via Triage-Harness (Plan 03) EMPIRISCH klassifiziert — instrumentiert + re-run, NICHT statisch geraten (die "2x daneben"-Lektion). Klassen: (i) veralteter Test → fixen, (ii) echter unkritischer Bug → xfail(strict=True)+reason+999.x-Ticket, (iii) echter KRITISCHER Bug (Security/DSGVO/RLS-Cross-Tenant/Anonymisierungs-Leck/Datenverlust) → BLOCK rot (eigene Sofort-Phase, NIE xfail).

Purpose: Das Tor geht grün, ohne echte Bugs zu maskieren. Anti-Versickern 2-schichtig: xfail(strict=True) selbst-aufdeckend (XPASS → Suite rot) + 999.x getrackt.

Output: `08.23.2.PGTEST.GREEN-TRIAGE.md` (Triage-Tabelle), gefixte veraltete Tests, xfail-Marker in tests/, 999.x-Backlog-Tickets.

autonomous: false — Plan 04 braucht iterative human-supervised Instrumentierung gegen echtes PG (Andre startet/begleitet die triage.sh-Laeufe). Checkpoint: kritische-Bug-BLOCK-Entscheidung MUSS Andre vorgelegt werden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-SPEC.md
@.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-CONTEXT.md
@.planning/backlog.md

<interfaces>
<!-- Backlog-Ticket-Format (aus .planning/backlog.md): ### TICKET-ID — Titel + Severity/Entdeckt/Scope-Felder. -->
<!-- xfail-Vertrag: pytest.mark.xfail(strict=True, reason="999.x: <nodeid> — <kurz>; Fix in eigener Phase"). -->
<!-- triage.sh (Plan 03): bash scripts/triage.sh <pytest-args> — provisioniert nerve_test, laeuft targeted, kein Deploy. -->

Die ~22 Plan-03/04-Port-Assertion-Fail-Dateien (aus SPEC.md / PGTEST-CLOSEOUT): anonymizer_worker, postcall_split, postcall_outcome_route, eur_calculator, cost_tracker, ewb_pipeline, exchange_rates, dashboard_outcome_reminder.
KRITISCHE Klassen (BLOCK, nie xfail): test_rls_isolation, test_anonymizer_worker (Anon-Leck), Cross-Tenant-RLS, Datenverlust/-korruption, DSGVO.
</interfaces>
</context>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

Dieser Plan fasst keine NEUEN Tabellen an — er klassifiziert bestehende Tests, die DB-Tabellen lesen/schreiben. Wo die Triage eine veraltete Test-Annahme als Bug identifiziert, die eine DB-Spalte falsch annimmt (vgl. D.UX log_entries-Saga), gilt: der empirische Beleg MUSS via `inspect.sh schema <table>` belegen, ob die angenommene Spalte existiert.

### inspect.sh-Beleg (pro mehrdeutigem Fail, der eine Schema-Annahme trifft)
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema <table>'
```
> In die Triage-Tabelle als empirischer Beleg eintragen, NICHT raten.
</persistenz_schicht_verifikation>

<tasks>

<task type="auto">
  <name>Task 1: Voller Harness-Lauf + grobe Klassifikation aller Fails (D-G09 Schritt 1)</name>
  <read_first>
    - scripts/triage.sh (Plan 03 — der Harness)
    - SPEC.md Req-5/Req-6 + CONTEXT.md D-G09/D-G10 (Empirie-Workflow, xfail-Policy, kritische Klassen)
    - die ~22 Fail-Test-Dateien (oben gelistet) — Code-Lesen NUR zur Orientierung, NICHT als Klassifikations-Beleg
  </read_first>
  <action>
    1. Ein voller Harness-Lauf via `bash scripts/triage.sh tests/ -v --tb=long` (ggf. mit `-m "not live and not perf"` um Marker-Tests auszublenden — Plan 02 hat sie registriert). Alle Fail-Tracebacks einsammeln.
    2. Grobe Erst-Klassifikation pro Fail in `08.23.2.PGTEST.GREEN-TRIAGE.md` anlegen: Spalten | nodeid | Symptom (Traceback-Kurzfassung) | vorlaeufige Klasse (i/ii/iii/unklar) | empirischer Beleg (zunaechst leer fuer "unklar").
    3. Eindeutige Faelle (klar veralteter Test ODER klar gruen nach Plan 01-Aenderung) direkt markieren.
    HINWEIS: Dieser Task ist die Datensammlung — KEINE finale Klassifikation der mehrdeutigen Faelle (das ist Task 2). KEIN statisches Raten.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/ -v --tb=long -m "not live and not perf"'</automated>
    HINWEIS: voller Lauf via Harness; Output ist die Triage-Datenbasis. Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-TRIAGE.md` existiert mit einer Zeile pro Fail-nodeid
    - `grep -c "nodeid\|::test" 08.23.2.PGTEST.GREEN-TRIAGE.md` deckt alle aus dem Harness-Lauf gesammelten Fails ab
    - kein Fail fehlt in der Tabelle (Abgleich gegen den --tb=long-Output)
  </acceptance_criteria>
  <done>Voller Harness-Lauf gemacht; alle Fails in TRIAGE.md mit Symptom + vorlaeufiger Klasse erfasst.</done>
</task>

<task type="auto">
  <name>Task 2: Empirische Einzel-Triage mehrdeutiger Fails + Fix veralteter Tests (D-G09 Schritt 2-3)</name>
  <read_first>
    - 08.23.2.PGTEST.GREEN-TRIAGE.md (aus Task 1 — die "unklar"-Faelle)
    - scripts/triage.sh (fuer gezielte Einzel-Re-Runs)
    - inspect.sh schema-Output fuer jeden Fail, der eine DB-Spalten-Annahme trifft (Persistenz-Schicht-Verifikation oben)
  </read_first>
  <action>
    Pro mehrdeutigem Fail (echter App-Bug vs. veralteter Test?):
    1. Gezielt instrumentieren (temporaere Diagnose-Prints im Test/Code — NICHT committen, D-G08) + einzeln re-runnen via `bash scripts/triage.sh tests/test_x.py::test_y -v --tb=long` bis die Ursache EMPIRISCH belegt ist (nicht aus dem Code-Lesen geraten — die "2x daneben"-Lektion).
    2. Klassifikation in TRIAGE.md mit empirischem Beleg (Traceback-Ausschnitt / inspect.sh-Schema-Diff / Re-Run-Ergebnis) eintragen:
       - (i) veralteter Test → FIXEN (Test an die gelebte Realitaet anpassen, Test-Infrastruktur, kein App-Logik-Eingriff) → muss danach gruen sein.
       - (ii) echter unkritischer Bug → NICHT fixen (out of scope), markieren fuer Task 3 (xfail).
       - (iii) echter KRITISCHER Bug (Security/DSGVO/RLS-Cross-Tenant/Anon-Leck/Datenverlust) → markieren fuer Checkpoint Task 4 (BLOCK).
    3. Diagnose-Prints nach Befund WIEDER ENTFERNEN (nicht committen).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/ -v -m "not live and not perf"'</automated>
    HINWEIS: nach den Fixes der veralteten Tests sollten diese gruen sein; verbleibende Fails sind (ii)/(iii)-klassifiziert. Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - Jeder Fail in TRIAGE.md hat eine finale Klasse (i/ii/iii) + nicht-leeren empirischen Beleg — `grep -c "unklar\|TODO\|TBD" 08.23.2.PGTEST.GREEN-TRIAGE.md` == 0
    - Jeder als (i) klassifizierte Test ist gefixt und im Re-Run gruen
    - `grep -rn "print(" tests/` zeigt keine neuen temporaeren Diagnose-Prints (entfernt)
    - Jeder (iii)-Fall ist als KRITISCH markiert und fuer Task 4 (Checkpoint) gelistet
  </acceptance_criteria>
  <done>Alle mehrdeutigen Fails empirisch klassifiziert; veraltete Tests gefixt+gruen; (ii)/(iii)-Faelle markiert; keine Diagnose-Prints committet.</done>
</task>

<task type="auto">
  <name>Task 3: xfail(strict=True)+reason+999.x-Backlog-Tickets fuer echte unkritische Bugs (D-G10, Req-6)</name>
  <read_first>
    - 08.23.2.PGTEST.GREEN-TRIAGE.md ((ii)-klassifizierte Faelle)
    - .planning/backlog.md (Ticket-Format: ### TICKET-ID — Titel + Severity/Entdeckt/Scope)
  </read_first>
  <action>
    Pro (ii) echter NICHT-kritischer Bug:
    1. Backlog-999.x-Ticket in `.planning/backlog.md` anlegen (gsd-add-backlog-Muster): nodeid + reason + "xfail(strict) in GREEN, Fix in eigener Phase". 999.x-Nummerierung sequenziell (999.1, 999.2, ...).
    2. xfail-Marker am Test setzen: `@pytest.mark.xfail(strict=True, reason="999.x: <nodeid> — <kurz>; Fix in eigener Phase")`. strict=True PFLICHT (XPASS → Suite rot → Marker-Zwang, Anti-Versickern).
    3. KEINEN xfail auf kritische Klassen (Security/DSGVO/RLS/Anon/Datenverlust) — die sind (iii)/Task 4.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/triage.sh tests/ -v -m "not live and not perf"'</automated>
    HINWEIS: nach xfail sollten die (ii)-Tests als XFAIL erscheinen (nicht FAILED). Kein lokaler pytest (CLAUDE.md HART).
  </verify>
  <acceptance_criteria>
    - `grep -rn "xfail" tests/` — jeder Treffer traegt `strict=True` UND `reason=` UND eine 999.x-ID
    - `grep -n "999\." .planning/backlog.md` — pro xfail genau ein korrespondierendes 999.x-Ticket
    - `grep -rn "xfail" tests/test_rls_isolation.py tests/test_anonymizer_worker.py` == 0 (kein xfail auf kritische Klassen, Req-6)
    - Im Harness-Lauf erscheinen (ii)-Tests als XFAIL, nicht FAILED
  </acceptance_criteria>
  <done>Jeder unkritische Bug xfail(strict=True)+reason+999.x-Ticket; keine kritische Klasse ge-xfailed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — Triage-Review + kritische-Bug-BLOCK-Entscheidung (Andre)</name>
  <what-built>Triage-Tabelle (TRIAGE.md) mit empirischer Klassifikation aller ~22 Fails; veraltete Tests gefixt; unkritische Bugs xfail(strict)+999.x; etwaige (iii)-kritische Bugs gelistet.</what-built>
  <how-to-verify>
    1. Lies `08.23.2.PGTEST.GREEN-TRIAGE.md` — pruefe: hat jeder Fail eine empirisch belegte Klasse?
    2. Pruefe die (iii)-Liste: sind das WIRKLICH kritische Bugs (Security/DSGVO/RLS-Cross-Tenant/Anon-Leck/Datenverlust)?
    3. Entscheidung: gibt es einen (iii)-kritischen Bug?
       - JA → das Tor BLOCKT rot bis gefixt (eigene Sofort-Phase). GREEN kann NICHT gruen werden, bis er gefixt ist. Andre entscheidet ueber die Sofort-Phase.
       - NEIN → alle Fails sind (i) gefixt oder (ii) xfail; das Tor kann gruen werden (Plan 05 + Final).
    4. Pruefe `grep -rn "xfail" tests/test_rls_isolation.py tests/test_anonymizer_worker.py` == 0.
  </how-to-verify>
  <acceptance_criteria>
    - Andre hat die Triage-Tabelle bestaetigt
    - Bei (iii)-Fund: explizite Andre-Entscheidung ueber Sofort-Phase dokumentiert (GREEN bleibt rot)
    - Bei keinem (iii): explizite Freigabe fuer Plan 05 + Final
  </acceptance_criteria>
  <resume-signal>Type "approved — keine kritischen Bugs" ODER "BLOCK — kritischer Bug, Sofort-Phase: <beschreibung>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Triage-Klassifikation → Tor-Gruen | ein als "veraltet" fehlklassifizierter echter Bug = maskierter Bug |
| xfail-Marker → Suite | ein xfail ohne strict=True = stale-Masking (XPASS bleibt unbemerkt) |
| kritische Bug-Klasse → xfail | ein ge-xfailter Security/DSGVO/RLS-Bug = green-lighted Sicherheitsleck |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-GREEN-14 | Tampering (False-Green durch Fehl-Triage) | Triage-Klassifikation | mitigate | D-G09: jede Klasse EMPIRISCH belegt (instrumentiert+re-run), nicht geraten ("2x daneben"-Lektion). TRIAGE.md grep == 0 unklar. |
| T-PGTEST-GREEN-15 | Tampering (stale-masking) | xfail ohne strict | mitigate | Req-6: jeder xfail strict=True+reason+999.x; grep belegt. XPASS → Suite rot → Marker-Zwang. |
| T-PGTEST-GREEN-16 | Information-Disclosure (green-lighted Leck) | xfail auf kritische Klasse | mitigate | Req-6 HARTE AUSNAHME: Security/DSGVO/RLS/Anon/Datenverlust NIE ge-xfailed → BLOCK; grep auf rls_isolation/anonymizer_worker == 0 xfail; Checkpoint-Andre-Entscheidung. |
| T-PGTEST-GREEN-17 | Information-Disclosure | committete Diagnose-Prints | mitigate | D-G08: Diagnose-Prints temporaer im Harness, NICHT committet; grep belegt keine neuen prints in tests/. |

Block severity = high.
</threat_model>

<verification>
- Triage-Tabelle vollstaendig + empirisch belegt (Req-5).
- xfail(strict)+999.x fuer alle unkritischen Bugs; kein xfail auf kritische Klassen (Req-6).
- Checkpoint-Andre-Freigabe (kritische-Bug-Entscheidung).
- Harness-Laeufe statt lokalem pytest (CLAUDE.md HART).
</verification>

<success_criteria>
- Jeder ~22 Fail empirisch klassifiziert + in TRIAGE.md belegt (Req-5).
- Veraltete Tests gefixt+gruen; unkritische Bugs xfail(strict)+999.x; kritische BLOCKEN (Req-6).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST.GREEN-gate-gruen-machen/08.23.2.PGTEST.GREEN-04-SUMMARY.md`
</output>
