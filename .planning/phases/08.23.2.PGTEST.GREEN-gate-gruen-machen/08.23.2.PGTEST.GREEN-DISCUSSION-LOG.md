# Phase 08.23.2.PGTEST.GREEN: Echtes PG-Tor GRÜN machen - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-16
**Phase:** 08.23.2.PGTEST.GREEN-gate-gruen-machen
**Areas discussed:** Isolations-Mechanismus (a), Empirische Triage (b), Marker-Registrierung (c), Schema-Ableitung (d)
**Mode:** discuss (SPEC.md geladen — 10 Requirements gelockt; nur HOW-Entscheidungen)

---

## Teil a — Isolations-Mechanismus (Req-2/3/4)

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Auto-Reset, gespalten | Heilt leaked Extra-Rows + laute Warnung; missing/mutated der Baseline BLOCKT | ✓ |
| Reiner Auto-Reset (CLOSEOUT-Favorit) | Heilt ALLE Drift, nichts blockt — verliert den base-seed-Delete-Bug-Fang | |
| Transaktions-Rollback pro Test | Generische Tests in txn, rollback — kehrt PGTEST-Real-Commit um | |

**User's choice:** Option 1 (Auto-Reset, GESPALTEN).
**Notes:** Bereits 3-Sichten-validiert (Claudian + GSD + Gemini-3.1-Pro am echten Code, Artefakt 11b8fcf / `_green_isolation_gemini_OUT.md` = Req-4-Beleg, vor Bau datiert). 6 Gemini-Wachpunkte aufgenommen: (1) STRICT SPLIT leaked-löschen/missing+mutated-fail, (2) laute Attribution `[BASELINE-AUTO-FIX]`, (3) FK-Lösch-Order × Req-9 → Teil a+d zusammen planen, (4) id::text-uuid-Cast, (5) Wächter-TX-Hygiene, (6) Multi-Conn-Race (Block M vormerken).

---

## Teil b — Empirische Triage (Req-5/6)

### Triage-Mechanismus (kein Local-Dev)

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Server-Triage-Harness, kein Restart | Gate-Provisioning 1:1, nur gezielte Tests, nie Deploy/Restart | ✓ |
| Voller Gate-Lauf, Tracebacks klassifizieren | Einmal -v --tb=long, aus Tracebacks klassifizieren | (teilweise: Schritt 1 des Workflows) |
| Instrument-Prints + Gate + Logs | commit+push+Gate pro Iteration — langsam, History-Schmutz | |

**User's choice:** Option 1 (Server-Triage-Harness). Bleibendes Werkzeug (Ratchet). Empirie-Workflow: voller Lauf → mehrdeutige Fails gezielt instrumentieren + einzeln re-runnen → Triage-Tabelle mit Beleg.

### xfail-Ticket-Ziel

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Backlog 999.x | gsd-add-backlog, reason= verlinkt 999.x-ID | ✓ |
| Eine gesammelte Folge-Phase | Neuer Roadmap-Eintrag 08.23.2.PGTEST.BUGS | |

**User's choice:** Option 1 (Backlog 999.x). Kein BUGS-Phasen-Eintrag jetzt (Scope vor Triage unbekannt). Anti-Versickern: xfail(strict) selbst-aufdeckend + 999.x getrackt. Post-Triage Backlog-Review → opportunistisch fixen ODER Phase promoten.

---

## Teil c — Marker-Registrierung (Req-7/8)

### Marker-Granularität

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Nur live + perf | Re-ID-Rate fällt unter live; kein Wildwuchs | ✓ |
| live + perf + reid | Eigener reid-Marker | |

**User's choice:** Option 1 (live + perf). Re-ID-Rate via `pytest -m live -k reid` sub-selektierbar. Bedingung: Anonymisierungs-Sicherheit/Logik bleibt deterministisch im Tor (_fake_anonymize-Stub); nur die statistische Echt-Modell-Qualität ist exkludiert.

### Determinismus-Strategie (Req-8)

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Mock + im Tor, Identifikation via Harness | monkeypatch anthropic-Client, Security-Tests bleiben PASSED im Tor | ✓ |
| Erst inventarisieren, Mock-Scope dann | Separater statischer Inventur-Schritt | |

**User's choice:** Option 1. Präzisierung 1 (kritisch): Mock NUR die externe Modell-Abhängigkeit, NIE die Sicherheits-Logik (sonst false-green). Präzisierung 2: nicht-mockbarer Security-Test → deterministische Variante ODER dokumentierte Lücke mit kompensierenden Kontrollen, nie still per Marker raus.

### Doku-Ort

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Markdown im Phasen-Verzeichnis | MARKER-DOKU.md im GREEN-Dir | |
| Im Repo bei den Tests (tests/MARKERS.md) | Lebende Betriebs-Doku, überlebt Archivierung | ✓ |

**User's choice:** Option 2 (tests/MARKERS.md). Phasen-Dir wird beim Milestone-Abschluss archiviert → operative Doku gehört neben die Suite. Phasen-Triage-Tabelle (Req-5) verlinkt tests/MARKERS.md.

---

## Teil d — Schema-Ableitung (Req-9)

### Katalog-Quelle + FK-Lösch-Order

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| pg_catalog topo-Sort aus pg_constraint | Liste UND Order aus Live-Katalog | ✓ |
| information_schema + kuratierte Order | Order bleibt teil-hardcoded → verfehlt Req-9 | |

**User's choice:** Option 1. Bau-Wachpunkte: (1) Zyklen explizit brechen (self-ref/mutual FK → DELETE-Loop bis stabil ODER bekannte Kanten ignorieren), (2) ON DELETE CASCADE nutzen wo vorhanden, id::text-Cast gilt auch hier.

### Tabellen-Set + Foundation-Register

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Alle public base-Tabellen minus Denylist | Explizites Foundation-Register, jede Exklusion begründet | ✓ |
| Nur Tabellen mit id-PK, kein Register | Implizit via id-Spalte → droppt still | |

**User's choice:** Option 1. Refinement: PK-Spaltenname aus Katalog ableiten (nicht `id` hardcoden); non-PK-Tabelle → loggen+Denylist, nie still droppen. crm.* dynamisch via pg_tables.

### DRY-Quelle

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Geteiltes Python-Ableitungs-Modul | tests/_schema_introspect.py, DSN+Schema-parametrisiert | ✓ |
| Getrennt: Python conftest + inline-SQL deploy.sh | Risiko Ableitungs-Drift | |

**User's choice:** Option 1. EINE Wahrheit, unit-testbar, Ratchet. conftest importiert (nerve_app/public), deploy.sh via python -c (postgres-peer/crm).

---

## Zusatz-Festlegungen (finale Bestätigung)

1. **Auto-Reset-Scope = PUBLIC-ONLY:** crm.* bleibt POST-SUITE-Check (harter BLOCK), kein Auto-Clean. crm-Writer räumen selbst auf.
2. **Naht Teil-a × Teil-d:** geteiltes Introspect-Modul liefert list+order, Auto-Reset konsumiert — EIN gekoppelter Task.
3. **Wave-Order:** W1 Introspect+Auto-Reset → W2 Triage-Harness → W3 triagieren+markern+mocken → Final grüner Deploy.
4. **Gemini-Status:** Req-4 erledigt (11b8fcf); vollständiger Plan geht vor Execute trotzdem nochmal durch Gemini (Punkt 24, 🔴 — neue Details).

## Claude's Discretion
- Exakte Datei-/Funktions-Namen (Vorschläge tests/_schema_introspect.py, scripts/triage.sh, tests/MARKERS.md).
- Topo-Sort-/Zyklus-Brechungs-Implementierung (Plan + Gemini abwägen).

## Deferred Ideas
- Multi-Worker (Block M) Race-Handling; reid-Marker (YAGNI); Fix der xfailten Bugs (eigene Phase post-Review); Template-DB-Tempo.
