# Phase 08.19.5.4: Dark-Mode-Reste raus + Modal im neuen Design — Specification

**Created:** 2026-05-05
**Updated:** 2026-05-05 (Cross-AI-Findings: +login.html, +landing.html-Verschiebung, +Modal-ESC/Overlay, +Form-Interceptor)
**Ambiguity score:** 0.095 (gate: ≤ 0.20)
**Requirements:** 14 locked
**Complexity:** 🟡

## Goal

Alle hardcoded Dark-Mode-Farben (#0c0c18, #1a1a2e, #2e2e48 etc.) in 9 App-Templates und nerve.css werden durch nerve.css-Design-Tokens ersetzt; landing.html wird physisch in templates/marketing/ verschoben; das Nav-Bestätigungs-Modal wird ohne Inline-Styles mit den neuen .n-modal-*-Klassen neu gebaut, mit ESC/Overlay-Dismiss und Form-Submit-Interception in pip-launcher.js re-integriert.

## Background

Am 2026-05-03 wurde in Phase 08.19.5.2 ein Custom-Modal in base.html mit hardcoded Dark-Mode-Inline-Styles (#1a1a2e, #f0c040, #00D4AA, #2a2a4e) gebaut und dann auf Andre's Entscheidung hin wieder entfernt (Commit 1ea3d5a). Gleichzeitig wurde in CLAUDE.md eine neue harte Sektion "Keine hardcoded Dark-Mode-Farben + Inline-Styles in UI-Komponenten" mit 7 Pflicht-Regeln eingeführt.

Pattern-Quelle identifiziert: GSD's Plan-Agent findet alte Dark-Mode-Farben in 10+ App-Templates, weil diese noch nie auf das aktuelle MAIN-DESIGN-System migriert wurden. Die Templates haben standalone `<style>`-Blöcke mit hardcoded Hex-Werten statt nerve.css-Tokens zu nutzen.

Aktueller Ist-Zustand (codebase-verifiziert):
- `templates/coach_dashboard.html` — `.firma-card`, `.co-modal`: `background:#0c0c18; border:1.5px solid #1a1a2e`
- `templates/coach_firma.html` — `.tabs`, `.stat-box`, `.profile-card`: `border-bottom:1px solid #1a1a2e`
- `templates/coach_methodik.html` — `.m-card`, `.co-modal`: `background:#0c0c18; border:1.5px solid #1a1a2e`
- `templates/team.html` — `.plan-bar`, `.invite-section`: `background:#0c0c18; border:1.5px solid #1a1a2e`
- `templates/register.html` — `body`, `.card`, `input`: vollständiges Dark-Mode-Theme
- `templates/changelog.html` — `.changelog-entry`, `.type-bugfix`: `background:#1a1a2e`
- `templates/waitlist_admin.html` — `.wl-stat`, `.wl-table-wrap`: `background:#1a1a2e`
- `templates/admin/kpi_dashboard.html` — gelber Warning-Banner: `background:#fff8e1; border:1px solid #f0c040`
- `static/nerve.css:~862` — `.badge-gray`: `background:#1a1a2e; color:#6b6b80; border:1.5px solid #2e2e40`
- `templates/landing.html` — **BEWUSSTE AUSNAHME**: behält eigenes Dark-Mode-Design-System; wird nach `templates/marketing/landing.html` verschoben (2 render_template-Referenzen in routes/dashboard.py verifiziert)
- `templates/login.html` — **NICHT GEFUNDEN** beim Codebase-Scout; Andre bestätigt Dark-Mode vorhanden — Plan-Agent muss Datei lokalisieren (ggf. anderer Name oder in Subordner) vor Wave-1-Start
- `static/pip-launcher.js:3016` — Click-Interceptor entfernt (Kommentar seit Commit 1ea3d5a)
- `templates/base.html` — kein `.n-modal-*` vorhanden

Nerve.css Design-Token-Basis (verifiziert vorhanden): `--page-bg`, `--page-text-color`, `--page-text-secondary`, `--page-text-muted`, `--glass-bg`, `--glass-border`, `--glass-radius`, `--glass-shadow`.

## Requirements

### Wave 1: Dark-Mode-Migration

1. **Template-Farbmigration**: Alle 9 App-Templates ersetzen hardcoded Dark-Mode-Farben durch nerve.css-Tokens.
   - Current: 9 Templates haben `<style>`-Blöcke mit `#0c0c18`, `#1a1a2e`, `#2e2e48` etc. als Inline-Farben (login.html-Dateiname vom Plan-Agenten zu verifizieren — siehe Background)
   - Target: Alle Farb-Werte in diesen Templates referenzieren ausschließlich `var(--token-name)` aus nerve.css; keine hex-Literale außer `#fff`/`#000` als strukturelle Ausnahmen
   - Acceptance: Grep-Befehl gegen alle 9 Ziel-Templates (inkl. lokalisiertem Login-Template) → 0 Treffer für `#0c0c18\|#1a1a2e\|#2e2e48\|#2a2a4e\|#f0c040\|#E8B040\|#0d0d14`

2. **Pflicht-Vorab-Token-Audit**: Bevor eine einzige Zeile geändert wird, dokumentiert der Plan-Agent welche nerve.css-Tokens für die Migration geeignet sind.
   - Current: Token-Namen in der Phase-Beschreibung (--accent, --border-color) stimmen NICHT mit tatsächlichen nerve.css-Namen überein (tatsächlich: --page-text-color, --glass-border, --btn-primary-bg-from)
   - Target: PLAN.md enthält eine Mapping-Tabelle: `#0c0c18 → var(--X)`, `#1a1a2e → var(--Y)` etc. für alle betroffenen Werte, abgeleitet aus aktuellem nerve.css-Stand via Grep
   - Acceptance: PLAN.md-Mapping-Tabelle existiert vor dem ersten Edit-Aufruf; kein Token-Name der nicht in nerve.css `:root {}` steht

3. **nerve.css .badge-gray-Bereinigung**: `.badge-gray`-Klasse in nerve.css nutzt nerve.css-Tokens statt hardcoded Werte.
   - Current: `nerve.css:~862` — `.badge-gray{background:#1a1a2e;color:#6b6b80;border:1.5px solid #2e2e40}`
   - Target: `.badge-gray` nutzt `var(--glass-border)` o.ä. für Background, `var(--page-text-muted)` für Color; exakte Token-Wahl per Token-Audit in REQ-2 bestimmt
   - Acceptance: `grep '.badge-gray' static/nerve.css` zeigt keine hex-Literale außer ggf. rgba() mit 0-Werten

4. **Pattern-Marker-Kommentar**: Jedes der 9 geänderten Templates erhält einen Kommentar als Schutz gegen Regression.
   - Current: Keine Markierung in den Templates
   - Target: Jedes der 9 geänderten Templates enthält genau diesen Kommentar unmittelbar nach dem `<style>`-Tag oder vor dem ersten Token-`var()`: `<!-- Design-Tokens aus nerve.css, KEINE hardcoded Farben (CLAUDE.md Regel) -->`
   - Acceptance: `grep -l 'Design-Tokens aus nerve.css'` gegen alle 9 Ziel-Templates → 9 Dateien

5. **landing.html-Verschiebung**: landing.html wird physisch in ein eigenes Marketing-Unterverzeichnis verschoben, damit GSD's Plan-Agent sie nicht als App-Template-Vorlage scannt. Bewusste-Ausnahme-Status bleibt erhalten.
   - Current: `templates/landing.html` liegt im selben Ordner wie alle App-Templates; 2 render_template-Aufrufe in `routes/dashboard.py`
   - Target: Datei liegt unter `templates/marketing/landing.html`; alle render_template- und url_for-Referenzen auf neuen Pfad aktualisiert; Dark-Mode-Design-System der Datei bleibt unverändert
   - Acceptance: `ls templates/landing.html` → not found; `ls templates/marketing/landing.html` → exists; App lauffähig (Dashboard-Route rendert Landing-Page ohne 404)

### Wave 2: Modal-Neubau

6. **nerve.css Modal-Klassen**: Drei neue CSS-Klassen in nerve.css definiert — ausschließlich nerve.css-Tokens, kein Inline-Style, keine hardcoded Farben.
   - Current: Keine `.n-modal-*`-Klassen in nerve.css vorhanden
   - Target: `static/nerve.css` enthält `.n-modal-overlay`, `.n-modal-card`, `.n-modal-actions` mit nerve.css-Token-Referenzen für alle Farb- und Spacing-Werte
   - Acceptance: `grep 'n-modal-overlay\|n-modal-card\|n-modal-actions' static/nerve.css` → 3 Treffer; `grep '#[0-9a-fA-F]\{3,6\}' <modal-CSS-block>` → 0 Treffer (ausgenommen rgba() structural values)

7. **base.html Modal-HTML**: Modal-HTML in base.html mit den neuen CSS-Klassen.
   - Current: Kein Modal in base.html (nach Commit 1ea3d5a)
   - Target: `templates/base.html` enthält `<div id="nerveNavModal" class="n-modal-overlay" ...>` mit zwei Buttons: "Hier bleiben" + "Call beenden & verlassen"; kein `style=""`-Attribut an Modal-Elementen
   - Acceptance: `grep 'nerveNavModal' templates/base.html` → 1 Treffer; `grep 'style=' templates/base.html | grep modal` → 0 Treffer

8. **pip-launcher.js Click-Interceptor — Links + Logout**: Nav-Link-Klicks und Logout-Link werden beim aktiven Mic abgefangen.
   - Current: `pip-launcher.js:3016` — Kommentar "Click-Interceptor entfernt"; keine Interception-Logik
   - Target: Click-Interceptor re-implementiert für alle `<a>`-Tags die `.n-nav-item` oder `.popup-item-logout` sind, beim `click`-Event wenn `state.micStarted === true` → `preventDefault()` + Modal öffnen + Ziel-URL speichern. Hinweis: In base.html sind keine Nav-`<form>`-Elemente vorhanden; Form-Submit-Interception ist nicht nötig.
   - Acceptance: Im Live-Call: Nav-Link klicken → Modal; Logout-Link im User-Popup klicken → Modal (Andre Live-Test beide Pfade)

9. **_nerveNavConfirm()-Funktion**: Modal-Buttons rufen korrekte Aktionen auf.
   - Current: Funktion existiert nicht
   - Target: `_nerveNavConfirm(confirm)` implementiert: `confirm === true` → `_stopMic()` + Navigation zur gespeicherten URL (via `window.location.href` für Links, via Form-Submit für Forms); `confirm === false` → Modal schließen, keine Navigation
   - Acceptance: "Call beenden & verlassen" navigiert zur angeklickten URL nach Mic-Stop; "Hier bleiben" schließt Modal ohne Seitennavigation

10. **Modal-Dismiss via ESC + Overlay-Klick**: ESC-Taste und Klick auf `.n-modal-overlay` (außerhalb `.n-modal-card`) verhalten sich identisch zu "Hier bleiben".
    - Current: kein Keyboard- oder Overlay-Dismiss implementiert (Modal existiert noch nicht)
    - Target: `keydown`-Listener auf `document` für `Escape` → `_nerveNavConfirm(false)`; `click`-Listener auf `.n-modal-overlay` mit Target-Check (nur wenn `event.target === overlay`, nicht wenn Klick auf `.n-modal-card`) → `_nerveNavConfirm(false)`
    - Acceptance: Im Live-Call → Modal öffnen → ESC drücken → Modal schließt, keine Navigation; Modal öffnen → auf Overlay-Bereich neben Card klicken → Modal schließt, keine Navigation

11. **Programmatic Navigation — Discuss-Phase-Entscheidung**: Ob `window.location.href`-Zuweisungen in `onclick`-Attributen auch abgefangen werden sollen, ist eine offene Frage für die Discuss-Phase.
    - Current: unklar ob onclick-basierte programmatic Navigation in der Nav vorkommt
    - Target: Discuss-Phase klärt: (a) Kommt programmatic Navigation in `.n-nav-item`-Elementen vor? (b) Wenn ja: soll der Interceptor auch diese abfangen oder reichen `<a>`-Tags + `<form>`-Elements?
    - Acceptance: Discuss-Phase dokumentiert Entscheidung in CONTEXT.md; Planner setzt sie um

12. **beforeunload bleibt erhalten**: Fallback für Tab-Close/Browser-Zurück/URL-Eingabe bleibt unverändert.
    - Current: `beforeunload`-Handler in pip-launcher.js funktional (nicht Scope dieser Phase)
    - Target: `beforeunload`-Handler unverändert — nur der Click-Interceptor für In-App-Navigation wird hinzugefügt
    - Acceptance: `git diff HEAD -- static/pip-launcher.js | grep beforeunload` → keine Änderungen an beforeunload-Block

## Boundaries

**In scope:**
- 9 App-Templates (coach_dashboard, coach_firma, coach_methodik, team, register, changelog, waitlist_admin, admin/kpi_dashboard, login/[Name vom Plan-Agenten zu verifizieren]) — Dark-Mode-Farbmigration auf nerve.css-Tokens
- `templates/landing.html` → `templates/marketing/landing.html` — physische Verschiebung + Referenz-Updates (Dark-Mode-Inhalt bleibt unverändert)
- `static/nerve.css` — `.badge-gray`-Klasse bereinigen + `.n-modal-overlay/.n-modal-card/.n-modal-actions` hinzufügen
- `templates/base.html` — Modal-HTML mit `.n-modal-*`-Klassen
- `static/pip-launcher.js` — Click-Interceptor (`<a>` + `<form>`) + `_nerveNavConfirm()` + ESC/Overlay-Dismiss re-implementieren
- Pattern-Marker-Kommentar in allen 9 geänderten Templates
- Pruning-Notiz pro Wave (CLAUDE.md Punkt 3a Pflicht)

**Out of scope:**
- Dark-Mode-Inhalt von `landing.html` — bleibt erhalten, nur Dateipfad ändert sich
- Alle anderen Templates die NICHT in der 9er-Liste stehen — kein scope-creep
- nerve.css allgemeine Refaktorierung oder neue Design-Tokens definieren — nur bestehende Tokens nutzen
- Modal-Animationen oder CSS-Transitions — nicht verlangt, YAGNI
- `beforeunload`-Handler-Änderungen — bleibt wie live
- Analytics.html (M-05 aus UI-Audit: Dark-Theme-Table) — separater Scope, NICHT diese Phase
- Changelog-Sichtbarkeit in Nav (C-03 aus UI-Audit) — NICHT diese Phase
- Irgendwelche Backend-Änderungen außer render_template-Pfad-Update für landing.html

## Constraints

- **Kein neuer Token**: Nur bestehende nerve.css-`:root{}`-Tokens nutzen. Keine neuen CSS-Variablen definieren.
- **Kein Inline-Style**: Kein `style=""`-Attribut an neuen oder geänderten UI-Elementen. CLAUDE.md Anti-Hardcoded-Farben-Sektion.
- **Token-Audit zuerst**: Wave-1-Plan muss Token-Mapping-Tabelle enthalten bevor erste Edit-Calls (REQ-2).
- **Cross-AI-Pflicht**: Gemini-Review nach Execute, Briefing explizit mit "prüfe auf hardcoded Farben + Inline-Styles + Design-Token-Konsistenz — nicht nur Logik-Review".
- **Code-Review-Pflicht**: Post-Execute Code-Review prüft jede geänderte UI-Datei explizit auf Inline-Styles + hardcoded Farben. Findings CRITICAL → müssen vor Phase-Approval gefixt werden.
- **Modal-CSS-Klassen-Namen gesperrt**: `.n-modal-overlay`, `.n-modal-card`, `.n-modal-actions` — diese Namen sind locked (pip-launcher.js referenziert sie).
- **JS-Funktionsname gesperrt**: `_nerveNavConfirm()` — locked.
- **State-Check gesperrt**: `state.micStarted` — der Bedingungsausdruck für den Click-Interceptor ist locked.

## Acceptance Criteria

- [ ] Grep gegen alle 9 Ziel-Templates (inkl. lokalisiertem Login-Template) für `#0c0c18\|#1a1a2e\|#2e2e48\|#2a2a4e\|#f0c040\|#E8B040\|#0d0d14` → 0 Treffer
- [ ] `grep -l 'Design-Tokens aus nerve.css'` gegen alle 9 Ziel-Templates → 9 Dateien
- [ ] `grep '#[0-9a-fA-F]' static/nerve.css | grep 'badge-gray\|n-modal'` → 0 Treffer (keine hardcoded Farben in diesen Klassen)
- [ ] `ls templates/landing.html` → not found; `ls templates/marketing/landing.html` → exists; Dashboard-Route lädt ohne 404
- [ ] `grep 'n-modal-overlay\|n-modal-card\|n-modal-actions' static/nerve.css` → 3 Klassendeklarationen vorhanden
- [ ] `grep 'nerveNavModal' templates/base.html` → 1 Treffer; kein `style=`-Attribut an Modal-Elementen
- [ ] `grep '_nerveNavConfirm' static/pip-launcher.js` → Funktion definiert + aufgerufen
- [ ] **Andre Live-Test — Links**: Mic aktiv → Nav-`<a>`-Link klicken → Modal erscheint; "Hier bleiben" → Modal schließt; "Call beenden & verlassen" → navigiert zur URL
- [ ] **Andre Live-Test — Logout-Form**: Mic aktiv → Logout klicken → Modal erscheint statt sofortigem Logout
- [ ] **Andre Live-Test — ESC**: Modal offen → ESC drücken → Modal schließt, keine Navigation
- [ ] **Andre Live-Test — Overlay**: Modal offen → auf Overlay-Bereich neben Card klicken → Modal schließt, keine Navigation
- [ ] **Cross-AI-Review**: Gemini findet keine hardcoded Farben oder Inline-Styles in geänderten Dateien
- [ ] **Code-Review**: Keine CRITICAL-Findings bezüglich Inline-Styles oder hardcoded Farben nach `/gsd-code-review`

## Ambiguity Report

| Dimension          | Score | Min  | Status | Notes                                                        |
|--------------------|-------|------|--------|--------------------------------------------------------------|
| Goal Clarity       | 0.92  | 0.75 | ✓      | 4 Deliverables klar; login.html-Name TBD (Plan-Agent)        |
| Boundary Clarity   | 0.95  | 0.70 | ✓      | landing.html: Ausnahme + Verschiebung gesperrt               |
| Constraint Clarity | 0.88  | 0.65 | ✓      | Token-Audit-Pflicht, Klassen-Namen, ESC/Overlay locked       |
| Acceptance Criteria| 0.90  | 0.70 | ✓      | 13 Checks: Grep + 4× Live-Test + Cross-AI + Code-Review      |
| **Ambiguity**      | 0.095 | ≤0.20| ✓      | Programmatic-Nav-Frage an Discuss-Phase delegiert            |

## Interview Log

| Round  | Perspektive     | Frage                                             | Entscheidung gesperrt                                                              |
|--------|-----------------|---------------------------------------------------|------------------------------------------------------------------------------------|
| Scout  | Researcher      | Was existiert heute im Codebase?                  | 8 Templates mit Dark-Mode-Styles; nerve.css-Tokens; Click-Interceptor fehlt        |
| Scout  | Researcher      | Token-Mismatch?                                   | --accent/--border-color existieren nicht → Token-Audit als Pflicht-Schritt gesperrt |
| 1      | Boundary Keeper | landing.html: Ausnahme oder migrieren?            | **Ausnahme** — Dark-Mode bleibt, physische Verschiebung nach marketing/ (Andre 2026-05-05) |
| Cross-AI | Gemini        | login.html fehlt in Liste?                        | **login.html hinzugefügt** — Dateiname vom Plan-Agenten zu verifizieren            |
| Cross-AI | Gemini        | landing.html-Verschiebung nötig?                  | **In Scope** — physische Trennung reduziert false-positive Scans (Andre bestätigt) |
| Cross-AI | Gemini        | ESC + Overlay-Dismiss fehlen?                     | **Gesperrt** — ESC + Overlay = "Hier bleiben"-Verhalten (REQ-10)                  |
| Cross-AI | Gemini        | Nur `<a>`-Tags abfangen reicht nicht?             | **Erweitert** — `<form submit>` ebenfalls abfangen (REQ-8); programmatic nav → Discuss-Phase (REQ-11) |

---

*Phase: 08.19.5.4-dark-mode-reste-raus-modal-neu*
*Spec created: 2026-05-05*
*Next step: /gsd-discuss-phase 08.19.5.4 — Implementierungs-Entscheidungen (Token-Mapping, Modal-Styling-Details, Wave-Reihenfolge)*
