# Phase 08.19.5.4: Dark-Mode-Reste raus + Modal im neuen Design — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-05
**Phase:** 08.19.5.4 — dark-mode-reste-raus-modal-neu
**Areas discussed:** Logout-Intercept-Scope, REQ-8 Form-Korrektur, Modal Show/Hide-Mechanismus, login.html Fallback-Strategie

---

## Logout-Intercept-Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Ja — abfangen | Selector '.n-nav-item, a.popup-item-logout' — auch Logout im Popup abfangen | ✓ |
| Nein — nur .n-nav-item | Nur Haupt-Nav-Links, Logout als bewusste Aktion nicht abfangen | |

**User's choice:** Ja — abfangen
**Notes:** Logout trägt Datenverlust-Risiko wenn Call aktiv. Selector auf `.n-nav-item, a.popup-item-logout` erweitert.

---

## REQ-8 Form-Korrektur

| Option | Description | Selected |
|--------|-------------|----------|
| REQ-8 still beschränken | Plan-Agent ignoriert Form-Teil, implementiert nur <a>-Interception | |
| SPEC jetzt korrigieren | REQ-8 in SPEC.md aktualisieren: Form-Submit-Teil entfernen | ✓ |

**User's choice:** SPEC jetzt korrigieren
**Notes:** Codebase-Scout bestätigte: logout ist `<a>`-Tag, keine Nav-`<form>` in base.html. SPEC REQ-8 wurde direkt aktualisiert.

---

## Modal Show/Hide-Mechanismus

| Option | Description | Selected |
|--------|-------------|----------|
| classList.add('open') | Konsistent mit consent-overlay + headset-overlay in pip-launcher.js | ✓ |
| display:none toggle via JS | element.style.display direkt setzen — inkonsistent | |

**User's choice:** classList.add('open') — Recommended
**Notes:** Pip-launcher.js hat bereits 3 Overlay-Patterns die dieses Muster nutzen. Konsistenz bevorzugt.

---

## login.html Fallback-Strategie

| Option | Description | Selected |
|--------|-------------|----------|
| Alle Templates nach Dark-Mode durchsuchen | Grep aller templates/*.html — Treffer kommen in Wave 1 | ✓ |
| Checkpoint setzen + Andre fragen | Explizit nach Dateinamen fragen vor Wave-1-Start | |

**User's choice:** Alle Templates durchsuchen — Recommended
**Notes:** login.html nicht in templates/ gefunden. Plan-Agent grepped alle Templates selbständig — Dateiname ist irrelevant.

---

## Claude's Discretion

- Token-Mapping-Tabelle: Plan-Agent erstellt selbständig via nerve.css-Grep (REQ-2)
- Wave-Granularität: Plan-Agent entscheidet Plan-Anzahl
- Programmatic-Navigation-Check: Scout zeigt nur NerveLauncher.open() (kein Page-Load) → kein Intercept nötig; Plan-Agent verifiziert

## Deferred Ideas

- Modal-Animationen/CSS-Transitions — YAGNI
- Programmatic window.location.href Interception — Scout zeigt keinen Handlungsbedarf
