---
phase: 08.19.5.4-dark-mode-reste-raus-modal-neu
fixed_at: 2026-05-05T07:47:44Z
review_path: .planning/phases/08.19.5.4-dark-mode-reste-raus-modal-neu/08.19.5.4-REVIEW.md
iteration: 1
findings_in_scope: 7
fixed: 7
skipped: 0
status: all_fixed
---

# Phase 08.19.5.4: Code Review Fix Report

**Fixed at:** 2026-05-05T07:47:44Z
**Source review:** .planning/phases/08.19.5.4-dark-mode-reste-raus-modal-neu/08.19.5.4-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 7
- Fixed: 7
- Skipped: 0

## Fixed Issues

### CR-01: XSS — Unescaped server data injected into Toast innerHTML

**Files modified:** `templates/dashboard.html`
**Commit:** d9380e7
**Applied fix:** Applied `esc()` to both `n.icon` and `n.text` in the toast innerHTML build at line 832. The `esc()` helper was already defined at line 854 in the same file.

---

### CR-02: XSS — User-supplied email rendered unescaped in waitlist_admin.html innerHTML

**Files modified:** `templates/waitlist_admin.html`
**Commit:** fb43e67
**Applied fix:** Added `escHtml()` helper function before `invite()`. Extracted link to a local `const link` variable. Replaced template literal innerHTML with string concatenation using `escHtml(d.email)` and `escHtml(link)`. Replaced inline `onclick` attribute on the copy button with a `data-copy-link` attribute and a proper `querySelector().onclick` event handler assigned after the innerHTML is set.

---

### WR-01: Hardcoded hex colors remaining in admin/kpi_dashboard.html (migration incomplete)

**Files modified:** `templates/admin/kpi_dashboard.html`
**Commit:** d353a02
**Applied fix:** Replaced `border:1px solid #e5e7eb` with `border:1px solid var(--glass-border)`, `background:#fff` with `background:var(--glass-bg)`, and `color:#00B894` with `color:var(--btn-primary-bg-from)` in the `.card` and `.card h3` rules.

---

### WR-02: Hardcoded hex colors remaining in coach_firma.html (migration incomplete)

**Files modified:** `templates/coach_firma.html`
**Commit:** 3e139fe
**Applied fix:** Replaced all 5 occurrences of `color:#444466` inline styles (lines 43, 133, 149, 155, 183) with `color:var(--page-text-muted)` using a global replace.

---

### WR-03: Hardcoded hex colors in logs_page.html (migration incomplete)

**Files modified:** `templates/logs_page.html`, `static/nerve.css`
**Commit:** a882a55
**Applied fix:** Added two new tokens `--color-warn-orange: #f0a05a` and `--color-warn-red: #ff8040` to the badge token section in nerve.css. In logs_page.html: replaced `#6B7280` with `var(--page-text-secondary)` (time column), replaced all 3 occurrences of `#9CA3AF` with `var(--page-text-muted)` (empty state spans), replaced `#f0a05a` with `var(--color-warn-orange)` (objection count), and `#ff8040` with `var(--color-warn-red)` (painpoint count).

---

### WR-04: copyInviteLink restores wrong text after clipboard copy (coach_dashboard.html)

**Files modified:** `templates/coach_dashboard.html`
**Commit:** 3d64eba
**Applied fix:** Captured `var originalText = el.textContent` before any mutation at the top of `copyInviteLink()`. Changed `navigator.clipboard.writeText()` to use `originalText` instead of `el.textContent`. Changed the `setTimeout` restore to use `originalText` instead of the never-set `document.getElementById('inviteLink').dataset.url`.

---

### WR-05: _calculate_roi() always uses 'Sonstiges' branche (dead lookup variable)

**Files modified:** `routes/dashboard.py`
**Commit:** 45d606b
**Applied fix:** Removed the `DEAL_VALUES` dict (9 lines) and the `branche = 'Sonstiges'` assignment. Replaced with `avg_deal = 4000  # TODO: derive from org.branche or user profile`. Fixed the dangling `'branche': branche` key in the return dict to use the string literal `'Sonstiges'` directly.

---

_Fixed: 2026-05-05T07:47:44Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
