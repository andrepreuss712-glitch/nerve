---
phase: 06.5-meeting-consent-modal
verified: 2026-04-17T00:00:00Z
status: human_needed
score: 10/10 must-haves verified (code-level)
overrides_applied: 0
human_verification:
  - test: "Meeting-Card-Klick springt direkt zu Step 2 (kein Inline-Consent)"
    expected: "Launcher zeigt PreCall-Option Step 2, nicht den alten Consent-Screen"
    why_human: "UI-Flow im Browser, nur durch Klicken testbar"
  - test: "Steps 2-5 durchlaufen im Meeting-Modus"
    expected: "Identischer Flow wie Cold Call (PreCall-Option, Form, Result, Skript/Opener)"
    why_human: "End-to-end UI-Flow, interaktive Navigation"
  - test: "In Step 5 'Call starten' klicken -> Consent-Modal erscheint"
    expected: "consent-overlay.open wird sichtbar mit Titel, Script-Text, 3 Buttons"
    why_human: "Visual rendering, overlay z-index, Focus-Setzung"
  - test: "'Stattgegeben' startet Meeting-Call"
    expected: "Modal schliesst, PiP oeffnet, Mode-Badge zeigt 'Meeting'"
    why_human: "Real-time Socket.IO + getUserMedia Lifecycle"
  - test: "Zweiter Meeting-Call in gleicher Session: kein Consent-Modal"
    expected: "state.consentDone=true verhindert zweiten Modal-Aufruf"
    why_human: "State-Persistenz nur durch zweiten kompletten Flow testbar"
  - test: "Logout/Reset + neuer Meeting-Call: Consent-Modal kommt wieder"
    expected: "_cleanup() hat consentDone=false gesetzt -> Modal erscheint erneut"
    why_human: "Vollstaendiger Logout-Zyklus mit neuer Session"
  - test: "'Abgelehnt' schaltet auf Cold-Call + Headset-Modal erscheint"
    expected: "state.mode='cold_call', Headset-Gate greift im Re-Call"
    why_human: "Zwei verkettete Modals, Mode-Switch im Live-Flow"
  - test: "'Abbrechen' schliesst Modal ohne Call-Start"
    expected: "User bleibt auf Step 5, Launcher sichtbar, kein PiP"
    why_human: "Cancelled-Path ohne side effects pruefen"
  - test: "In laufender PiP erscheint KEIN zweites Consent-Popup"
    expected: "pip-section-consent komplett entfernt, Live-View direkt"
    why_human: "PiP-Fenster-Rendering, visueller Check ohne Consent-Screen"
  - test: "Cold Call direkt (ohne vorher Meeting): unveraendertes Verhalten"
    expected: "Nur Headset-Modal, kein Consent-Modal"
    why_human: "Regression gegen Phase 06.4"
---

# Phase 06.5: Meeting-Modus Flow-Umbau — Consent als Modal Verification Report

**Phase Goal:** Meeting-Modus bekommt denselben Launcher-Flow wie Cold Call (Profil -> PreCall -> Skript/Opener). Inline-Consent-Screen wird entfernt. Consent erscheint stattdessen als Modal (analog Headset-Modal aus Phase 06.4) beim Klick auf "Call starten". "Stattgegeben" startet Meeting-Call. "Abgelehnt" schaltet auf Cold-Call-Modus mit regulaerem Headset-Gate. "Abbrechen" laesst User auf Step 5. state.consentDone einmal pro Session. Alter PiP-Consent-Screen komplett ausgebaut.
**Verified:** 2026-04-17
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Klick auf Meeting-Card springt direkt zu Step 2, NICHT zum alten Inline-Consent-Screen | VERIFIED | `static/pip-launcher.js:174-182` — Meeting-Card-Handler identisch zu Cold-Card (state.mode='meeting', state.step=2 oder 5, renderStep()). Kein `renderConsentStep`-Aufruf mehr. |
| 2 | Steps 2-5 funktionieren im Meeting-Modus identisch zum Cold-Call-Flow | VERIFIED (code-level) | Meeting-Card setzt state.step=2 oder 5 wie Cold-Card; gleicher renderStep-Switch wird durchlaufen. UI-Flow selbst nur per UAT final verifizierbar. |
| 3 | Klick auf 'Call starten' in Step 5 oeffnet das Consent-Modal | VERIFIED | `static/pip-launcher.js:821-835` — Consent-Gate in startCall: `if (state.mode === 'meeting' && !state.consentDone) { _showConsentModal(...) }`. `templates/base.html:375-387` — consent-overlay HTML mit Title/Script/3 Buttons. |
| 4 | Stattgegeben schliesst Modal, Call startet im Meeting-Modus, state.mode bleibt 'meeting' | VERIFIED | `static/pip-launcher.js:823-826` — `result === 'accepted'` -> `state.consentDone=true; startCall(setProfile)`. Kein mode-Change, Re-Call faellt durch beide Gates (meeting consentDone=true, nicht cold_call), erreicht close() + _openPipAndMic. |
| 5 | Zweiter Meeting-Call zeigt KEIN Consent-Modal (state.consentDone Gate) | VERIFIED | `static/pip-launcher.js:822` — Gate-Bedingung `!state.consentDone` ist nach erstem Accept=true, somit wird `_showConsentModal` uebersprungen. |
| 6 | Nach Reset/Logout erscheint Consent-Modal beim naechsten Meeting-Call wieder | VERIFIED | `static/pip-launcher.js:1979` — `_cleanup() { ... state.consentDone = false; ... }`. Funktion wird bei endCall/Logout/nextCall ausgeloest. |
| 7 | Abgelehnt setzt state.mode auf 'cold_call', Headset-Modal greift dann, danach startet Cold-Call | VERIFIED | `static/pip-launcher.js:827-830` — `result === 'rejected'` -> `state.mode='cold_call'; state.consentDone=true; startCall(setProfile)`. Re-Call triggert dann Headset-Gate (Z.813-819) wenn headsetConfirmed fehlt. |
| 8 | Abbrechen schliesst Modal, kein Call-Start, Launcher bleibt auf Step 5 | VERIFIED | `static/pip-launcher.js:832` — `result === 'cancelled'` -> kein State-Update, kein Re-Call, Gate-return vor `close()` (Z.837) bleibt wirksam. |
| 9 | Innerhalb der laufenden PiP erscheint KEIN zweites Consent-Popup (alte pip-section-consent entfernt) | VERIFIED | `static/pip-launcher.js:1178-1188` — `_initPipLive` ruft direkt `_showPipLive`, kein Branch mehr. `_showPipConsent`, `pip-section-consent`, `pip-consent-*` alle raus (Grep: 0 Treffer in pip-launcher.js). base.html: alter `<div id="pip-section-consent">` + `.pip-consent-*` CSS entfernt. |
| 10 | Cold-Call ohne vorher Meeting funktioniert unveraendert wie in Phase 06.4 | VERIFIED | `static/pip-launcher.js:813-819` — Headset-Gate unveraendert von Phase 06.4. Cold-Card-Handler (Z.165-173) identisch. Consent-Gate (Z.822) greift nur bei mode='meeting', nicht bei 'cold_call'. |

**Score:** 10/10 truths verified at code level. Truths 1-8 und 10 benoetigen zusaetzliche UAT-Bestaetigung im Browser fuer visuellen Flow + real-time Verhalten.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---------|---------|--------|---------|
| `static/pip-launcher.js` | Meeting-Card setzt mode+nextStep, `_showConsentModal` Funktion, Consent-Gate in startCall, alte `renderConsentStep`+`_showPipConsent` entfernt | VERIFIED | Alle Aenderungen vorhanden (Zeile 174-182 Card-Handler, Z.626-698 `_showConsentModal`, Z.821-835 Gate, Z.1178-1188 vereinfachtes `_initPipLive`). Grep confirmed: 0 Treffer fuer `renderConsentStep`, `_showPipConsent`, `pip-section-consent`, `pip-consent-` in pip-launcher.js. |
| `templates/base.html` | `consent-overlay` HTML neben `headset-overlay`, `.consent-*` CSS-Klassen analog `.headset-*`, alter `pip-section-consent` entfernt | VERIFIED | CSS Z.332-354 (alle 10 `.consent-*` Regeln), HTML Z.375-387 (overlay + 3 Buttons). Alter `.pip-consent-*` Block und `<div id="pip-section-consent">` restlos weg (Grep: 0 Treffer). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `static/pip-launcher.js startCall()` | consent-overlay Modal | `state.mode==='meeting' && !state.consentDone -> _showConsentModal` | WIRED | Z.822-823: Exakter Code-Pfad vorhanden. |
| `static/pip-launcher.js _showConsentModal callback` | `startCall()` re-call | `callback({accepted|rejected|cancelled}) -> set flags + retry/abort` | WIRED | Z.823-834: alle drei result-Branches implementiert, accepted+rejected rufen `startCall(setProfile)` rekursiv, cancelled no-op. |
| `templates/base.html consent-overlay` | `_showConsentModal` DOM lookups | `getElementById('consent-overlay'/'consent-script'/'consent-btn-accept'/...)` | WIRED | Z.630-634 in JS holt alle 5 DOM-Elemente (overlay, script, accept, reject, cancel). Alle existieren in base.html Z.376-385. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|----------------------|--------|
| consent-script (HTML `<p>`) | text | `state.profileDaten.consent_text` (Fallback: `CONSENT_DEFAULT_TEXT`) + `[Name]`-Token-Replace aus `state.precallFormData.person` | Ja — profileDaten kommt von `/api/launcher/init` (Z.98-107), precallFormData wird bei Step 3 gesetzt | FLOWING |
| Consent-Gate state.consentDone | boolean | `state.consentDone` initialisiert in state-Objekt (Z.36), gesetzt durch accept/reject Callback, resettet in `_cleanup()` (Z.1979) | Ja — vollstaendige Lifecycle-Coverage | FLOWING |
| _cleanup state reset | state.consentDone | `_cleanup()` Z.1966-1984 wird aus endCall (Z.1944, 1957) + disconnect-Handler (Z.910) aufgerufen | Ja — Reset greift auf allen terminal paths | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `_showConsentModal` Funktion existiert | Grep `function _showConsentModal` in pip-launcher.js | 1 Treffer (Z.629) | PASS |
| `CONSENT_DEFAULT_TEXT` Konstante definiert | Grep `CONSENT_DEFAULT_TEXT` in pip-launcher.js | 2 Treffer (Definition Z.627 + Nutzung Z.640) | PASS |
| Consent-Gate in startCall | Grep `Consent-Gate \(Meeting only` in pip-launcher.js | 1 Treffer (Z.821) | PASS |
| consent-overlay HTML in base.html | Grep `id="consent-overlay"` in templates/base.html | 1 Treffer (Z.376) | PASS |
| 3 Button-IDs in base.html | Grep `consent-btn-accept|consent-btn-reject|consent-btn-cancel` | 3 Treffer (je einmal) | PASS |
| alter pip-section-consent weg | Grep `pip-section-consent` in static+templates | 0 Treffer in pip-launcher.js/base.html/dashboard.html; 4 Treffer in static/app.js (out-of-scope: alter /live Flow, dokumentiert im Plan) | PASS |
| `_showPipConsent` geloescht | Grep `_showPipConsent` in pip-launcher.js | 0 Treffer | PASS |
| `renderConsentStep` geloescht | Grep `renderConsentStep` in pip-launcher.js | 0 Treffer | PASS |
| state.consentDone Reset in _cleanup | Grep `state.consentDone = false` | 1 Treffer (Z.1979 in `_cleanup()`) | PASS |
| Keine Headset-CSS-Regression | Grep `.headset-overlay` in base.html | 2 Treffer (Definition + .open) — unveraendert | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| POLISH-16 | 06.5-01-PLAN.md | Headset-/Consent-Pflicht-Modal fuer DSGVO Cold Call/Meeting Compliance (Folgearbeit zu Phase 06.4) | SATISFIED | Phase 06.4 hat POLISH-16 original erfuellt (Headset-Modal). Phase 06.5 ist explicit "POLISH-16 Folgearbeit" — komplettiert die DSGVO-Abdeckung fuer Meeting-Modus. Consent-Modal im Launcher-Gate mit dreistufigem Outcome implementiert wie spezifiziert. |

**Hinweis zur Requirements-Inkonsistenz:**

POLISH-16 erscheint NICHT in `.planning/REQUIREMENTS.md` (Requirement-Datei definiert nur PROD-*, INFRA-*, PAY-*, LEGAL-*, MIC-*, MODE-*, DU-*, TA-*, BE-*, PIP-*, BIZ-*, LAUNCH-*). Die POLISH-Serie wurde durch "NERVE Finaler Polish Pass" (externes Quelldokument, siehe 06.4-CONTEXT.md) eingefuehrt und in Phase 06.4-VERIFICATION.md bereits als SATISFIED markiert. Das ist ein Requirements-Tracking-Gap, aber nicht ein Phase-Goal-Gap. Die Implementation selbst erfuellt die Phase-Spezifikation vollstaendig.

**Empfehlung fuer Milestone-Audit:** POLISH-16 in REQUIREMENTS.md nachtragen unter einer neuen Sektion "Final Polish Pass" mit Traceability-Zuordnung zu Phase 06.4 + 06.5.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `static/pip-launcher.js` | 635 | Silent early-return ohne Callback bei fehlenden DOM-Elementen | Warning | REVIEW WR-01: Falls `consent-overlay` im DOM fehlt (Template-Bug), returnt `_showConsentModal` ohne callback zu rufen; `startCall` bleibt im return-Zweig haengen. Symptom: Button tut nichts. Fix-Vorschlag im REVIEW: `callback('cancelled')` im Guard. Kein Goal-Blocker (Ausnahmepfad in Produktion). |
| `static/pip-launcher.js` | 629-698 | Fehlt Backdrop-Click-Dismiss | Info | REVIEW WR-02: Nicht konsistent mit `_showSaveToProfileDialog`. ESC-Key ist vorhanden (Z.670-673), deshalb low-impact. |
| `templates/base.html` | 378-379 | ASCII-Umlaute ("Gespraechspartner") statt nativer ä/ö/ü | Info | REVIEW IN-01: UI-Mix zwischen ASCII-Ersatz und UTF-8 in derselben Datei. Explizit vom Plan gefordert (POLISH-12 Encoding-Bug-Regel) — kein Bug aber UI-Inkonsistenz. |
| `static/pip-launcher.js` | 643 | Magic String `'[Name]'` Token, nur erstes Vorkommen ersetzt | Info | REVIEW IN-02: `.replace('[Name]', ...)` ersetzt nur erstes Vorkommen (kein Regex + /g). Profil-Texte koennten ``[Name]`` mehrfach enthalten. |
| `templates/base.html` | 376-387 | Fehlendes `aria-describedby="consent-script"` | Info | REVIEW IN-04: Screenreader lesen nur Title, nicht den rechtlich relevanten Vorlese-Text. A11y Polish. |

### Human Verification Required

Alle zehn UAT-Punkte aus dem Plan benoetigen Browser-Interaktion auf getnerve.app oder lokal:

1. **Meeting-Card springt zu Step 2**
   - Dashboard oeffnen, Launcher oeffnen, Meeting-Card klicken
   - Expected: Step 2 (PreCall-Option) erscheint sofort, kein Inline-Consent

2. **Steps 2-5 durchlaufen im Meeting-Flow**
   - PreCall-Option -> Form -> Result -> Skript/Opener durchklicken
   - Expected: Identischer Flow wie Cold Call

3. **Consent-Modal bei Call starten**
   - In Step 5 "Call starten" klicken
   - Expected: consent-overlay.open sichtbar, 3 Buttons (Abbrechen/Abgelehnt/Stattgegeben), Script-Text gesetzt, Primary-Button fokussiert

4. **Stattgegeben -> Meeting-Call**
   - "Stattgegeben" klicken
   - Expected: Modal schliesst, PiP-Window oeffnet, Mode-Badge "Meeting"

5. **Zweiter Meeting-Call: kein Consent-Modal**
   - Call beenden, erneut starten
   - Expected: Direkt PiP ohne Consent-Modal (consentDone=true)

6. **Logout/neuer Tab: Consent-Modal wieder da**
   - Logout, neu einloggen, Meeting-Call
   - Expected: Consent-Modal erscheint erneut

7. **Abgelehnt -> Cold-Call + Headset-Modal**
   - "Abgelehnt" klicken
   - Expected: Mode wechselt auf cold_call, Headset-Modal erscheint (falls nicht confirmed), nach Confirm startet Cold-Call

8. **Abbrechen -> Launcher bleibt auf Step 5**
   - "Abbrechen" klicken
   - Expected: Modal zu, kein PiP, User weiterhin auf Step 5

9. **Keine zweite Consent-Popup in PiP**
   - Waehrend Live-Call visuell pruefen
   - Expected: Kein pip-section-consent mehr (Wave 4)

10. **Cold-Call Regression**
    - Launcher -> Cold-Card -> Step 5 -> Call starten
    - Expected: Nur Headset-Modal, kein Consent-Modal, Flow wie Phase 06.4

### Gaps Summary

**Keine blockierenden Gaps auf Code-Ebene.** Alle 10 Plan-Truths sind in `static/pip-launcher.js` und `templates/base.html` vollstaendig implementiert. Alle Must-Haves-Artefakte existieren, sind substantiell (nicht stub), sind mit korrekten DOM-IDs/Callbacks verdrahtet, und die Data-Flows (profileDaten.consent_text, precallFormData.person, consentDone Lifecycle) fliessen korrekt.

Der Review-Report (06.5-REVIEW.md) identifizierte 2 Warnings und 4 Infos als Polish-Verbesserungen — nicht als Goal-Blocker:
- **WR-01** (stiller Early-Return): Edge-Case bei fehlendem DOM. Fix lohnt sich defensiv, aber blockiert Goal-Erfuellung nicht.
- **WR-02** (fehlender Backdrop-Dismiss): Inkonsistenz zu anderen Overlays, ESC-Key mitigiert.
- **IN-01 bis IN-04**: A11y + Polish-Items (aria-describedby, Token-Regex, Umlaut-Inkonsistenz, focus-restore-Doku).

**Externes Dokumentations-Gap:** POLISH-16 fehlt in REQUIREMENTS.md. Die Implementation ist korrekt und als SATISFIED klassifiziert (auch in 06.4-VERIFICATION.md), aber das Requirement selbst wurde nie in REQUIREMENTS.md eingetragen. Fuer Milestone-Audit empfohlen: neue Sektion "Final Polish Pass" in REQUIREMENTS.md mit POLISH-16-Eintrag und Phase-Traceability.

**Human UAT steht aus.** Die 10 Goal-Truths sind alle via Code-Pfad verifiziert, aber die tatsaechliche UI-Abfolge (Modal-Darstellung, Focus-Trap-Verhalten, Live-Call-Start nach Accept, Mode-Switch nach Reject + Headset-Modal-Chain) muss im Browser durchlaufen werden. Bis dahin: `status: human_needed`.

---

*Verified: 2026-04-17*
*Verifier: Claude (gsd-verifier)*
