---
phase: 08.23.2.PGTEST
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy.sh
autonomous: true
requirements: [Req-1, Req-3, Req-4, Req-5, Req-7, Req-8, Req-9]
complexity: "🔴 (security-near — CREATE/DROP DATABASE auf Prod-Instanz, anon_worker-PW-Handling, fail-closed)"
user_setup: []

must_haves:
  truths:
    - "Das deploy.sh-Gate provisioniert nerve_test, baut Schema per Cutover-Sequenz, fährt pytest dagegen, räumt nerve_test ab"
    - "CREATE/DROP DATABASE + alle 4 Test-DSNs zielen ausschliesslich auf nerve_test (Whitelist-Guard, Abbruch statt Raten)"
    - "Jeder Schritt (Pre-DROP, CREATE, create_all, stamp, upgrade, pytest) bricht bei Fehler mit eigenem Klartext-Grund ab — kein SQLite/Prod-Ausweich"
    - "trap cleanup EXIT garantiert DROP nerve_test auch bei Test-Fehler/SIGTERM; Pre-Run-DROP entfernt verwaiste nerve_test"
    - "anon_worker-PW wird aus ionos-s3.env gesourct und nie geloggt; Schild-Guard läuft gegen nerve_test"
  artifacts:
    - path: "deploy.sh"
      provides: "Postgres-Test-Gate-Block (Provision → Cutover-Build → pytest 4-DSN → Teardown) mit Whitelist-Guard + fail-closed"
      contains: "nerve_test"
  key_links:
    - from: "deploy.sh Gate-Block"
      to: "scripts/create_postgres_schema.py + alembic stamp 0001 + alembic upgrade head"
      via: "DATABASE_URL=postgresql://postgres@/nerve_test als postgres"
      pattern: "create_postgres_schema|stamp 0001|upgrade head"
    - from: "deploy.sh Gate-Block"
      to: "pytest tests/ mit 4 nerve_test-DSNs"
      via: "sudo -u nerve_app bash -c mit inline-Env"
      pattern: "TEST_DATABASE_URL=postgresql://nerve_app@/nerve_test"
---

<objective>
Ersetze die kaputte SQLite-Test-Stufe in `deploy.sh` (Z.130-143) durch einen echten Postgres-Test-Gate-
Block: provisioniere die Wegwerf-DB `nerve_test` auf der Prod-Instanz (als `postgres`), baue ihr Schema
per Cutover-Sequenz (`create_postgres_schema.py` → `alembic stamp 0001` → `alembic upgrade head` — weil
`alembic upgrade head` gegen eine LEERE DB bei 0008 scheitert, RESEARCH Q3), fahre pytest mit 4 nerve_test-
DSNs dagegen, und räume garantiert ab. Fail-closed pro Schritt, Whitelist-Guard `nerve_test` (D-02).

Purpose: Req-1 (pytest gegen echtes PG), Req-3 (Schema via alembic), Req-4 (RLS/Anon laufen — DSNs gesetzt),
Req-5 (alle DSNs → nerve_test), Req-7 (fail-closed), Req-8 (Teardown), Req-9 (Prod + Schild-Guard grün).
Output: umgebauter deploy.sh-Test-Block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PGTEST-echtes-postgres-test-gate/08.23.2.PGTEST-SPEC.md
@.planning/phases/08.23.2.PGTEST-echtes-postgres-test-gate/08.23.2.PGTEST-CONTEXT.md
@.planning/phases/08.23.2.PGTEST-echtes-postgres-test-gate/08.23.2.PGTEST-RESEARCH.md

<interfaces>
<!-- Verträge + Production-Fakten (RESEARCH, alle live bewiesen). Executor baut daraus, ohne zu raten. -->

deploy.sh IST-Stand (relevant):
- Z.99 `ssh ... bash -s << ENDHEREDOC` mit `set -e`; der gesamte Server-Block läuft als root.
- Z.130-143 = die zu ERSETZENDE SQLite-pytest-Stufe (`pytest tests/` ohne DSN → SQLite; PYTEST_EXIT-Check existiert).
- Z.145-156 = Schild-Guard-Stufe (`sudo -u nerve_app bash -c '... NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve ...'`) → DSN auf nerve_test umlenken (Req-5/Req-9).
- Z.158-169 = .deploy_meta + systemctl restart — NACH dem Gate, NICHT brechen.

Production-Fakten (RESEARCH, verbatim-belegt):
- postgres = super+createdb (CREATE/DROP DATABASE bewiesen). OWNER-Klausel MUSS `OWNER postgres` sein
  (NICHT nerve_app — sonst crm-Tabellen nerve_app-owned → RLS bypassed → False-Green, Migration 0012-Doku).
- nerve_app: peer-socket, KEIN PW → DSN `postgresql://nerve_app@/nerve_test`. OS-User nerve_app existiert.
- nerve_anon_worker: KEIN OS-User → peer unmöglich → scram-host:
  `postgresql://nerve_anon_worker:<pw>@127.0.0.1:5432/nerve_test`; PW = `NERVE_ANON_WORKER_DB_PASSWORD`
  in `/etc/nerve/ionos-s3.env` (als root via sudo grep sourcen, VOR sudo -u nerve_app, nie loggen).
- Live HEAD = 0015 (Repo kann höher sein) → `alembic upgrade head` (NICHT hardcoden, D-09).
- Eine verwaiste leere `nerve_test`-DB existiert HEUTE auf Prod (owner postgres) → Pre-Run-DROP zwingend (D-06).
- scripts/create_postgres_schema.py: liest DATABASE_URL (muss postgresql:// sein), create_all, prüft calls/call_events.

Q5b-Skelett (RESEARCH — als Bauvorlage, exakt so strukturiert übernehmen):
```bash
TEST_DB="nerve_test"
if [ "$TEST_DB" != "nerve_test" ]; then echo "[deploy] FATAL: ... Prod-Schutz D-02"; exit 1; fi
cleanup() { sudo -u postgres psql -c "DROP DATABASE IF EXISTS \"$TEST_DB\";" 2>/dev/null || true; }
trap cleanup EXIT
sudo -u postgres psql -c "DROP DATABASE IF EXISTS \"$TEST_DB\";" || { echo FEHLER; exit 1; }
sudo -u postgres psql -c "CREATE DATABASE \"$TEST_DB\" OWNER postgres;" || { echo FEHLER; exit 1; }
sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/$TEST_DB /opt/nerve/venv/bin/python scripts/create_postgres_schema.py" || { echo FEHLER; exit 1; }
sudo -u postgres bash -c "... alembic stamp 0001" || { echo FEHLER; exit 1; }
sudo -u postgres bash -c "... alembic upgrade head" || { echo FEHLER; exit 1; }
ANON_PW=$(sudo grep ^NERVE_ANON_WORKER_DB_PASSWORD= /etc/nerve/ionos-s3.env | cut -d= -f2-)
sudo -u nerve_app bash -c "cd /opt/nerve/app && TEST_DATABASE_URL=... NERVE_APP_TEST_DSN=... NERVE_SCHILD_TEST_DSN=... ANON_WORKER_TEST_DSN=postgresql://nerve_anon_worker:$ANON_PW@127.0.0.1:5432/$TEST_DB /opt/nerve/venv/bin/pytest tests/ --tb=short -q" || { echo FEHLER; exit 1; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SQLite-Test-Stufe durch Postgres-Gate-Block ersetzen (Provision → Cutover-Build → pytest 4-DSN → Teardown)</name>
  <read_first>
    - deploy.sh Z.99-156 (IST: heredoc set -e, SQLite-pytest-Stufe Z.130-143, Schild-Guard Z.145-156)
    - .planning/.../08.23.2.PGTEST-RESEARCH.md Q3 (Cutover-Sequenz, warum NICHT bare upgrade head), Q5b (Gate-Skelett), Q1 (DSN-Formen + OWNER postgres)
    - scripts/create_postgres_schema.py (der create_all-Baustein — liest DATABASE_URL, prüft calls/call_events)
    - docs/cutover-runbook.md (Step 4a/4b — die dokumentierte create_all→stamp→upgrade-Sequenz)
  </read_first>
  <behavior>
    - Innerhalb des ENDHEREDOC-Blocks (läuft als root, set -e aktiv): die SQLite-pytest-Stufe (Z.130-143) wird ersetzt durch den Gate-Block.
    - Whitelist-Guard: TEST_DB ≠ "nerve_test" → sofort exit 1 mit Prod-Schutz-Grund (D-02).
    - trap cleanup EXIT registriert DROP DATABASE IF EXISTS nerve_test (garantiert auch bei pytest-Fehler/SIGTERM, D-06).
    - Pre-Run-DROP der verwaisten nerve_test, dann CREATE OWNER postgres.
    - Schema-Build als postgres: create_postgres_schema.py → alembic stamp 0001 → alembic upgrade head. Jeder Schritt eigener Exit-Check + Klartext-Grund (D-07).
    - pytest als nerve_app mit 4 inline-DSNs (3× peer-socket nerve_app + 1× scram nerve_anon_worker); ANON_PW vorab als root aus ionos-s3.env, nie geloggt.
    - Jeder Schritt fail-closed (exit 1), kein || -Zweig der auf SQLite/Prod ausweicht.
  </behavior>
  <action>
    Ersetze in deploy.sh die SQLite-pytest-Stufe (Z.130-143, inkl. des Kommentars Z.131-134 der den
    conftest-Refactor als Folge-Phase nennt — der ist jetzt erledigt) durch den folgenden Block. Baue ihn
    nach dem Q5b-Skelett (interfaces oben), heredoc-konform (im `<< ENDHEREDOC` werden lokale `$` escaped
    wie heute `\$PYTEST_EXIT` — beachte: TEST_DB/ANON_PW sind SERVER-seitige Vars, also `\$` escapen wo sie
    erst auf dem Server expandieren sollen):

    1. `TEST_DB="nerve_test"` + Whitelist-Guard:
       `if [ "\$TEST_DB" != "nerve_test" ]; then echo "[deploy] FATAL: Test-DB-Name != nerve_test — Abbruch (Prod-Schutz D-02)"; exit 1; fi`
    2. `cleanup() { sudo -u postgres psql -c "DROP DATABASE IF EXISTS \"\$TEST_DB\";" 2>/dev/null || true; }` + `trap cleanup EXIT`
    3. Pre-Run-DROP: `sudo -u postgres psql -c "DROP DATABASE IF EXISTS \"\$TEST_DB\";" || { echo "[deploy] FEHLER: Pre-Run-DROP nerve_test fehlgeschlagen"; exit 1; }`
    4. CREATE: `sudo -u postgres psql -c "CREATE DATABASE \"\$TEST_DB\" OWNER postgres;" || { echo "[deploy] FEHLER: CREATE DATABASE nerve_test fehlgeschlagen"; exit 1; }`
    5. create_all: `sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/\$TEST_DB /opt/nerve/venv/bin/python scripts/create_postgres_schema.py" || { echo "[deploy] FEHLER: create_all (Basis-Schema) gegen nerve_test fehlgeschlagen"; exit 1; }`
    6. stamp: `... /opt/nerve/venv/bin/alembic stamp 0001 ... || { echo "[deploy] FEHLER: alembic stamp 0001 fehlgeschlagen"; exit 1; }`
    7. upgrade: `... /opt/nerve/venv/bin/alembic upgrade head ... || { echo "[deploy] FEHLER: alembic upgrade head gegen nerve_test fehlgeschlagen"; exit 1; }`  (NICHT hardcoden — `head`, D-09)
    8. ANON_PW: `ANON_PW=\$(sudo grep ^NERVE_ANON_WORKER_DB_PASSWORD= /etc/nerve/ionos-s3.env | cut -d= -f2-)` — danach NIE in echo/set -x sichtbar machen.
    9. pytest als nerve_app mit 4 DSNs inline:
       ```
       sudo -u nerve_app bash -c "cd /opt/nerve/app && \
         TEST_DATABASE_URL=postgresql://nerve_app@/\$TEST_DB \
         NERVE_APP_TEST_DSN=postgresql://nerve_app@/\$TEST_DB \
         NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/\$TEST_DB \
         ANON_WORKER_TEST_DSN=postgresql://nerve_anon_worker:\$ANON_PW@127.0.0.1:5432/\$TEST_DB \
         /opt/nerve/venv/bin/pytest tests/ --tb=short -q" \
         || { echo "[deploy] FEHLER: pytest gegen nerve_test ROT — kein Restart, kein Deploy"; exit 1; }
       ```
       Log-Echo der DSN (Req-1-Acceptance: Beleg dass postgresql://…/nerve_test lief) — aber NUR die
       nerve_app-DSNs echoen, NIE die anon_worker-DSN (enthält PW). Z.B. `echo "[deploy] pytest gegen \$TEST_DATABASE_URL"`.

    KEIN Code-Zweig der bei Fehler auf SQLite/Prod ausweicht (Req-7). `set -e` bleibt, aber jeder Schritt
    hat zusätzlich seinen expliziten `|| { echo FEHLER; exit 1; }` (D-07: Klartext-Grund pro Schritt).
    Achtung Q3d/A1 (Build-Ordering): falls create_postgres_schema.py an "schema crm does not exist"
    scheitert (create_all vor CREATE SCHEMA crm), siehe Task 2 — dann ist ein vorgeschaltetes
    CREATE SCHEMA crm/training nötig. Im ersten Lauf beobachten.
  </action>
  <verify>
    <automated>bash deploy.sh production 2>&1 | tee /tmp/pgtest_deploy.log; grep -E "pytest gegen postgresql://nerve_app@/nerve_test|create_postgres_schema|alembic upgrade head" /tmp/pgtest_deploy.log; echo "EXIT=$?"  # Req-1/Req-3-Beleg: Gate lief gegen nerve_test mit Cutover-Sequenz. Bei Build-Ordering-Fehler (A1) → Task 2.</automated>
  </verify>
  <done>
    deploy.sh enthält den Postgres-Gate-Block: Whitelist-Guard + trap cleanup EXIT + Pre-Run-DROP + CREATE
    OWNER postgres + create_all → stamp 0001 → upgrade head + pytest mit 4 nerve_test-DSNs, jeder Schritt
    fail-closed mit Klartext-Grund. Deploy-Log belegt pytest gegen postgresql://…/nerve_test. anon_worker-PW
    nirgendwo geloggt: `grep ANON_WORKER_TEST_DSN.*nerve_anon_worker /tmp/pgtest_deploy.log` zeigt KEIN Klartext-PW.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build-Ordering verifizieren (A1) + Schild-Guard-DSN auf nerve_test umlenken (Req-9)</name>
  <read_first>
    - deploy.sh Z.145-156 (Schild-Guard-Stufe — DSN `@/nerve` → `@/nerve_test`)
    - .planning/.../08.23.2.PGTEST-RESEARCH.md Q3d + Assumptions A1/A3 (create_all-vs-crm-Schema-Ordering; Schild-Guard-Aussagekraft gegen frische DB)
    - alembic/versions/0012*.py (CREATE SCHEMA IF NOT EXISTS crm — falls create_all crm.* vor diesem Schritt baut)
  </read_first>
  <behavior>
    - A1-Verify: gegen die im Gate gebaute nerve_test ist nachweisbar, dass das Schema vollständig steht (alembic_version=head, crm/training-Schemas + RLS-Policies da). Falls create_all an fehlendem crm-Schema scheiterte → vorgeschaltetes CREATE SCHEMA crm; CREATE SCHEMA training; im Gate (vor create_all).
    - Schild-Guard: läuft gegen nerve_test (nicht Prod-nerve), bleibt grün, behält Aussagekraft (0015-Schilder in nerve_test vorhanden).
  </behavior>
  <action>
    1. **A1-Build-Ordering-Verify (RESEARCH-Pflicht Q3d):** Nach einem Gate-Lauf gegen die (im Lauf noch
       existierende — temporär ohne trap, oder via separatem manuellem Build) bzw. via einem read-only-
       Nachbau prüfen. Da das Gate nerve_test am Ende droppt, verifiziere die Build-Ordering durch einen
       gezielten manuellen Cutover-Build gegen einen Wegwerf-Namen `nerve_test` und Katalog-Query, BEVOR
       der trap droppt — KONKRET über inspect-artige psql-Checks als postgres (server-side):
       ```
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -c "DROP DATABASE IF EXISTS nerve_test;" && \
         sudo -u postgres psql -c "CREATE DATABASE nerve_test OWNER postgres;" && \
         sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve_test /opt/nerve/venv/bin/python scripts/create_postgres_schema.py" && \
         sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve_test /opt/nerve/venv/bin/alembic stamp 0001" && \
         sudo -u postgres bash -c "cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve_test /opt/nerve/venv/bin/alembic upgrade head" && \
         sudo -u postgres psql -d nerve_test -c "\dn" && \
         sudo -u postgres psql -d nerve_test -c "SELECT version_num FROM alembic_version;" && \
         sudo -u postgres psql -d nerve_test -c "SELECT schemaname,tablename FROM pg_policies WHERE schemaname=$$crm$$;" && \
         sudo -u postgres psql -c "DROP DATABASE nerve_test;"'
       ```
       Erwartung: create_all läuft sauber (35 public.* Tabellen), `\dn` zeigt crm+training, alembic_version
       = Repo-HEAD, crm-RLS-Policies (0012/0013/0014) vorhanden. FALLS create_all an "schema crm does not
       exist" scheitert (A1 falsch): füge im deploy.sh-Gate VOR create_postgres_schema.py ein
       `sudo -u postgres psql -d "\$TEST_DB" -c "CREATE SCHEMA IF NOT EXISTS crm; CREATE SCHEMA IF NOT EXISTS training;"`
       (eigener fail-closed-Check) hinzu. Dokumentiere im SUMMARY, welcher Pfad zutraf (A1 bestätigt vs.
       vorgeschaltetes CREATE SCHEMA nötig).
    2. **Schild-Guard-DSN umlenken (Req-9/Req-5):** In deploy.sh Z.151 ändere
       `NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve` → `...@/nerve_test`. Da die Schild-Guard-Stufe
       NACH dem Gate-Block läuft, nerve_test aber vom trap am EXIT gedroppt wird: ziehe entweder den
       Schild-Guard IN den Gate-Block (vor dem trap-DROP, in dieselbe nerve_app-Subshell wie pytest — dann
       läuft er gegen die noch existierende nerve_test) ODER lass ihn als separate Stufe und baue nerve_test
       für ihn neu. WÄHLE: Schild-Guard als zusätzlichen pytest-Aufruf IN die nerve_app-Gate-Subshell
       aufnehmen (`pytest tests/ ...` deckt test_schild_guard.py bereits mit ab, da NERVE_SCHILD_TEST_DSN
       gesetzt ist) → die separate Z.145-156-Stufe wird damit redundant; ersetze sie durch einen Hinweis-
       Kommentar, dass der Schild-Guard jetzt im Postgres-Gate gegen nerve_test mitläuft. Verifiziere A3:
       0015-Schilder sind in nerve_test vorhanden (upgrade head enthält 0015) → Schild-Guard grün.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && grep -nE "NERVE_SCHILD_TEST_DSN" deploy.sh'; echo "---"; bash deploy.sh production 2>&1 | tee /tmp/pgtest_deploy2.log | grep -E "test_schild_guard|alembic_version|crm|nerve_test"; echo "EXIT=$?"  # Schild-Guard gegen nerve_test grün; crm/training-Schemas + RLS da.</automated>
  </verify>
  <done>
    A1 verifiziert (SUMMARY dokumentiert: create_all sauber ODER vorgeschaltetes CREATE SCHEMA ergänzt);
    Katalog-Query gegen nerve_test zeigt alembic_version=Repo-HEAD + crm/training-Schemas + crm-RLS-Policies
    (Req-3). Schild-Guard läuft im Gate gegen nerve_test (kein `@/nerve` ohne `_test` mehr in deploy.sh) und
    ist grün (Req-9). Voller deploy.sh production endet grün; Prod-`nerve` unverändert (kein Test-DSN zeigt drauf).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.sh (root) → postgres-Rolle | CREATE/DROP DATABASE auf der Prod-Instanz — ein falscher Name = Prod-Verlust |
| deploy.sh → ionos-s3.env | anon_worker-PW-Secret quert in die Gate-Subshell-Env |
| Gate-pytest → nerve_test | alle 4 DSNs müssen nerve_test treffen, nie Prod-nerve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PGTEST-05 | Tampering/Denial | `DROP DATABASE` Tippfehler trifft Prod-`nerve` | mitigate | D-02 Whitelist-Guard `[ "$TEST_DB" != "nerve_test" ] && exit 1` VOR jedem CREATE/DROP; TEST_DB ist EINZIGE Quelle des Namens; grep-verifiziert |
| T-PGTEST-06 | Information Disclosure | anon_worker-PW landet im Deploy-Log | mitigate | ANON_PW nur in Sub-Shell-Env injiziert, nie echoed; nur nerve_app-DSNs (PW-frei) werden geloggt; Code-Review-grep nach `$ANON_PW` in echo-Zeilen |
| T-PGTEST-07 | Tampering | Test-DSN zeigt auf Prod-`nerve` → Test mutiert Prod | mitigate | alle 4 DSNs hartkodiert auf `\$TEST_DB`=nerve_test (Req-5); Schild-Guard-DSN auf nerve_test umgelenkt |
| T-PGTEST-08 | Denial/Spoofing | Gate skippt still bei nicht-provisionierbarer DB → False-Green-Deploy | mitigate | D-07 fail-closed pro Schritt (exit 1 + Klartext-Grund); KEIN SQLite/Prod-Ausweich-Zweig; trap droppt nerve_test |
| T-PGTEST-09 | Tampering | nerve_test OWNER nerve_app → crm-Tabellen owner-bypassen RLS (False-Green) | mitigate | CREATE DATABASE ... OWNER postgres (RESEARCH Q1e/0012-Doku); nerve_app bleibt non-owner → RLS engaged |
| T-PGTEST-10 | Denial | verwaiste nerve_test (existiert HEUTE) blockiert CREATE | mitigate | D-06 Pre-Run-DROP IF EXISTS vor CREATE; trap cleanup EXIT für Lauf-Ende |
</threat_model>

<verification>
- Req-1: Deploy-Log zeigt `pytest gegen postgresql://nerve_app@/nerve_test`.
- Req-3: Katalog-Query gegen nerve_test (Task 2) → alembic_version=Repo-HEAD, crm/training-Schemas, crm-RLS-Policies.
- Req-5: kein `@/nerve` (ohne `_test`) in deploy.sh-DSNs; alle 4 zeigen auf nerve_test.
- Req-7: simulierter Fehler (z.B. falsche Rolle / gestopptes PG) → exit≠0, kein systemctl restart, expliziter Log-Grund — MANUELL via Manual-Only-Verification (VALIDATION.md).
- Req-8: nach Lauf `sudo -u postgres psql -c "\l"` → kein nerve_test (trap + Pre-Run-DROP).
- Req-9: voller deploy.sh production grün (Tests + Schild-Guard gegen nerve_test + Restart); Prod-nerve unverändert.
</verification>

<success_criteria>
- Gate-Block in deploy.sh: Whitelist-Guard + trap EXIT + Pre-Run-DROP + CREATE OWNER postgres + Cutover-Build + pytest 4-DSN, alles fail-closed.
- A1-Build-Ordering verifiziert + dokumentiert; Schild-Guard gegen nerve_test grün.
- anon_worker-PW nie geloggt; alle DSNs → nerve_test; Prod-nerve unberührt.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PGTEST-echtes-postgres-test-gate/08.23.2.PGTEST-02-SUMMARY.md`
</output>
