---
phase: 08.23.2.PGTEST-echtes-postgres-test-gate
plan: 01
subsystem: testing
tags: [pytest, postgres, rls, conftest, fixtures, nerve_test, baseline-guard, tenant-isolation]

# Dependency graph
requires:
  - phase: 08.23.2.G-MEET
    provides: crm.*-RLS-Policies + tenant_orgs-Trigger + set_current_tenant/after_begin-Hook (db.py)
  - phase: 08.23.2.SCHILD
    provides: schild_guard_pg_conn-Fixture-Muster (peer-auth, NERVE_SCHILD_TEST_DSN)
provides:
  - "PG-basierte generische Fixtures (db_session + client) gegen nerve_test statt sqlite-in-memory"
  - "TEST_TENANT_UUID + _seed_test_tenant (Trigger-Muster) + set_current_tenant am Fixture-Start (D-05)"
  - "client._test_session/_test_engine-Vertrag (MODUL-SessionLocal, hook-tragend) re-exponiert; db_from_client unveraendert"
  - "Session-Scope Base-Seed (Org+User id=1, ORM-Pfad, trigger-aware, Sequenz-Advance)"
  - "cleanup_rows-Teardown-Helfer (Ext 1): reverse-FK, crm.* unter Tenant-GUC, #6 fuehrender rollback, #5 laute Warnung"
  - "Baseline-Cleanup-Waechter (Ext 2): _baseline_guard_engine + _baseline_snapshot + _baseline_cleanup_guard, PUBLIC.*-only, {pk:xmin}-Vergleich (#7), #9 app-Import-vor-Snapshot"
  - "A-1-Tripwire (test_rls_generic_smoke.py) + Baseline-Mechanismus-Selbsttest (test_baseline_guard.py)"
  - "CLAUDE.md Test-Cleanup-Regel (committende Tests raeumen via cleanup_rows auf)"
affects: [08.23.2.PGTEST-02, 08.23.2.PGTEST-03, 08.23.2.PGTEST-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "MODUL-SessionLocal.configure(bind=engine)-Umbindung (NICHT frische sessionmaker) -> after_begin-RLS-Hook bleibt erhalten"
    - "Seed-erzeugte TEST_TENANT_UUID via Trigger-Read-Back (kein FK-Bruch)"
    - "Baseline-Guard via {pk:xmin}-Snapshot ueber EIGENE session-scoped Read-Engine (entkoppelt von per-Test-MODUL-Engine-Churn)"
    - "cleanup_rows als EINE kanonische Teardown-Mechanik fuer committende Tests"

key-files:
  created:
    - tests/test_rls_generic_smoke.py
    - tests/test_baseline_guard.py
  modified:
    - tests/conftest.py
    - CLAUDE.md

key-decisions:
  - "Generische Fixtures binden MODUL-SessionLocal um statt frischer sessionmaker (Gemini-HIGH: Hook-Erhalt)"
  - "Baseline-Waechter prueft NUR public.* (HYBRID, André locked) — crm.*/training.* via POST-SUITE-Check Plan 02"
  - "Waechter nutzt eigene session-scoped Read-Engine (#2) gegen UnboundExecutionError"
  - "{pk:xmin}-Vergleich statt PK-Set (#7) faengt committete UPDATEs an Baseline-Rows"
  - "Self-Test via DSN-freien Vergleichs-Kern _diff_baseline statt pytester (robust + lokal lauffaehig)"

patterns-established:
  - "Pattern 1: committende Tests registrieren Row-IDs + rufen cleanup_rows im Teardown (CLAUDE.md-Regel)"
  - "Pattern 2: A-1-Tripwire als loud-red-Mechanismus gegen DATABASE_URL-unset-False-Green"

requirements-completed: [Req-2, Req-5, Req-9]

# Metrics
duration: 8min
completed: 2026-06-16
---

# Phase 08.23.2.PGTEST Plan 01: conftest-Fixtures Summary

**conftest.py refactored — db_session/client binden gegen REAL-PG nerve_test (MODUL-SessionLocal-Umbindung, Hook-erhaltend), plus Base-Seed, cleanup_rows-Helfer, PUBLIC.*-Baseline-Waechter ({pk:xmin}/#7) und A-1-Tripwire.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-06-16T09:08:13Z
- **Completed:** 2026-06-16T09:16:34Z
- **Tasks:** 6
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- Generische Fixtures (db_session + client) auf TEST_DATABASE_URL/nerve_test umgebaut — kein aktiver sqlite-Pfad mehr, MODUL-SessionLocal via `configure(bind=engine)` umgebunden (after_begin-RLS-Hook bleibt erhalten), set_current_tenant(TEST_TENANT_UUID) am Fixture-Start (D-05).
- client._test_session/_test_engine-Vertrag re-exponiert (MODUL-SessionLocal, hook-tragend) → die ~20 Konsumenten-Tests laufen ohne AttributeError; db_from_client unveraendert.
- Session-Scope Base-Seed (Org id=1 + User id=1, ORM-Pfad, trigger-aware, Sequenz-Advance) → FK-tragende generische Tests brechen nicht.
- cleanup_rows-Teardown-Helfer (Ext 1) mit #6 fuehrendem rollback + #5 lauter [PGTEST-CLEANUP]-Warnung; Konvention in conftest.py UND CLAUDE.md.
- Baseline-Cleanup-Waechter (Ext 2) PUBLIC.*-only mit eigener session-scoped Read-Engine (#2), {pk:xmin}-Vergleich (#7), #9 app-Import-vor-Snapshot; crm.*/training.* an POST-SUITE-Check (Plan 02) delegiert.
- A-1-Tripwire + Baseline-Mechanismus-Selbsttest als echte Runtime-Assertionen (keine Source-Presence).

## Task Commits

Jede Task wurde atomar committed (mit Hooks, kein --no-verify):

1. **Task 1: db_session+client auf nerve_test umbauen** - `e35e031` (feat)
2. **Task 2: A-1-Tripwire test_rls_generic_smoke.py** - `fb171ce` (feat)
3. **Task 3: 3 Spezial-Fixture-DSNs auf nerve_test (Doku)** - `8f7f9ee` (docs)
4. **Task 4: Session-Scope Base-Seed (Org+User id=1)** - `f8caf0a` (feat)
5. **Task 5: cleanup_rows-Helfer (Ext 1) + Konvention** - `2d08318` (feat)
6. **Task 6: Baseline-Waechter (Ext 2) + Selbsttest** - `97341f6` (feat), Nachzug `9a0f120` (docs: artifact-contains)

## Files Created/Modified
- `tests/conftest.py` - db_session/client PG-Umbau, _seed_test_tenant + TEST_TENANT_UUID, _pgtest_base_seed, cleanup_rows + _CLEANUP_FK_ORDER, _baseline_guard_engine/_baseline_snapshot/_baseline_cleanup_guard + _diff_baseline; 3 Spezial-Fixture-Doku auf nerve_test.
- `tests/test_rls_generic_smoke.py` - A-1-Tripwire (GUC NON-null + crm-Read ≥1 Zeile), Teardown via cleanup_rows.
- `tests/test_baseline_guard.py` - Mechanismus-Selbsttest des _diff_baseline-Kerns (leak/missing/#7-xmin-Mutation/clean + PK-Set-only-blind-Kontrollbeweis).
- `CLAUDE.md` - neue Test-Cleanup-Regel (committende Tests raeumen via cleanup_rows auf).

## Decisions Made
- **Self-Test-Mechanik:** Statt pytester (schwer, braucht echte DB) den DSN-freien Vergleichs-Kern `_diff_baseline` direkt aufrufen — robust, lokal lauffaehig, deckt leak/missing/#7-xmin-Mutation/clean ab. Plan erlaubt diese ALTERNATIVE explizit.
- **SQL-Casts:** `CAST(:x AS uuid)` statt `:x::uuid` im A-1-Tripwire, um SQLAlchemy-bindparam-Misparse (`:tid::uuid`) zu vermeiden.
- Sonst exakt nach Plan.

## Deviations from Plan
None - plan executed exactly as written (die zwei Detail-Entscheidungen oben sind plan-konforme Discretion, keine Abweichung von Vorgaben).

## Issues Encountered
None. Statische Verifikation: `python -m py_compile` auf allen 3 Test-Dateien OK; `_diff_baseline`-Logik per Inline-Python-Sanity-Check bestaetigt (leak/mutation/clean korrekt); alle key_link-Muster present, `NERVE_BASELINE_GUARD_DSN`-Count == 0, kein aktiver `sqlite:///:memory:`, kein hardcoded `@/nerve` (ohne `_test`).

## HARD_OVERRIDE-Konformitaet (Orchestrator)
- KEIN `deploy.sh` ausgefuehrt (Voll-Beleg = der EINE orchestrator-Lauf nach allen 4 Plans).
- KEIN voller pytest-Lauf (nerve_test existiert nur im Gate; lokal nicht verfuegbar). Rein STATISCHE Verifikation: py_compile + grep der key_links + Inline-Logik-Check des DSN-freien Kerns.
- Kein realer Issue maskiert/geskippt.

## User Setup Required
None - die Env-Vars (DATABASE_URL/TEST_DATABASE_URL/NERVE_APP_TEST_DSN/NERVE_SCHILD_TEST_DSN/ANON_WORKER_TEST_DSN auf nerve_test) setzt das Gate in Plan 02.

## Next Phase Readiness
- Vertrags-Schicht fuer Plan 02 (deploy.sh-Gate) + Plan 03 (Klasse-A-Port + Deltas) + Plan 04 steht: cleanup_rows + Baseline-Waechter sind die strukturelle Baseline-Sauberkeits-Durchsetzung, die die committenden Tests adoptieren.
- A-1-HARD-PRECONDITION bleibt eine Gate-Verantwortung (Plan 02 FIX 1 MUSS `DATABASE_URL=postgres` in der pytest-Subshell exportieren) — der A-1-Tripwire macht ihr Fehlen loud-red.
- POST-SUITE-crm-/training-Check (crm.* == 0, training.transcript_archive == 0) ist an Plan 02 delegiert (HYBRID, André locked).
- Voll-Beleg (alle Tests PASSED, kein UnboundExecutionError, Waechter gruen) erst im orchestrator-`deploy.sh production`-Lauf nach allen 4 Plans.

## Self-Check: PASSED
- tests/conftest.py — FOUND (modified, py_compile OK)
- tests/test_rls_generic_smoke.py — FOUND (created, py_compile OK)
- tests/test_baseline_guard.py — FOUND (created, py_compile OK)
- CLAUDE.md Cleanup-Regel — FOUND
- Commits e35e031/fb171ce/8f7f9ee/f8caf0a/2d08318/97341f6/9a0f120 — FOUND in git log

---
*Phase: 08.23.2.PGTEST-echtes-postgres-test-gate*
*Completed: 2026-06-16*
