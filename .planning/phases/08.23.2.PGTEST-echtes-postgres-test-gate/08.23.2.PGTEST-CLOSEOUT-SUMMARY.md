---
phase: 08.23.2.PGTEST
type: closeout
status: executed-known-red
created: 2026-06-16
supervised_by: Claudian
gate_result: "51 failed, 595 passed, 5 skipped, 555 errors"
prod_touched: false
---

# Phase 08.23.2.PGTEST — Close-Out (supervised execute, Claudian)

**Entscheidung André 2026-06-16: Option-3-Schnitt.** Infrastruktur + die 2 echten Bug-Fixes liefern, Gate
ehrlich KNOWN-RED lassen (Req-7 — NICHT maskieren), den suite-weiten Rest + die 51 Assertion-Fails in die
Folge-Phase **08.23.2.PGTEST.GREEN** eskalieren. KEIN grüner Deploy, KEIN Restart.

## Was geliefert wurde (committet + gepusht)

- **PG-Gate-Block in `deploy.sh`** (Plan 02): provision Wegwerf-`nerve_test` → `pg_dump`-Restore vom Prod-`nerve`
  (Schema+RLS+FORCE+GRANTs+Schilder) → `alembic upgrade head` → inline Katalog-Treue-Gate (crm-Policies≥7,
  FORCE≥5, anon-SELECT-GRANTs≥5) → pytest (4 DSN + DATABASE_URL) → POST-SUITE crm/training==0 → trap-Teardown.
  Whitelist-Guard (`$TEST_DB != nerve_test` → Abbruch), pipefail, fail-closed pro Schritt.
- **conftest PG-Fixtures** (Plan 01): `db_session`/`client` gegen real-PG `nerve_test`, Modul-SessionLocal-Rebind
  (hält den after_begin-RLS-Hook), Tenant-Kontext, Base-Seed (Org+User id=1), `cleanup_rows`-Helfer,
  Baseline-Wächter (public.*), A-1-Tripwire.
- **SQLite-Entfernung** (Plan 03): cf5de6d ATTACH-Listener (db.py) + app.py-SQLite-alembic-Hook raus, Klasse-A-Tests
  auf PG-Semantik portiert.
- **Persistenz-Härtung** (Plan 04): Gruppe-A/B-Tests, cleanup_rows-Adoption.

## Gate-Validierung — EIN Lauf-Typ, 4× ausgeführt (alle prod-sicher rot)

`bash deploy.sh production` lief 4× (1 initial + 1 voller Log + 2 nach Bug-Fixes). **Jeder Lauf brach am
pytest-Schritt ab → kein systemctl restart → Prod-Service unangetastet, `nerve` auf Migration 0015.** Der
tar-Overwrite (`/opt/nerve/app`) lief jeweils, aber ohne Restart bleibt der laufende Service auf altem Code.
Katalog-Treue-Gate jedes Mal grün (`crm-Policies=7, FORCE=5, anon-SELECT-GRANTs=5`) → Dump-Restore ist treu.
Gate-Ergebnis: **`51 failed, 595 passed, 5 skipped, 555 errors`**.

## 2 ECHTE Bugs gefangen + gefixt (Claudian-verifizierbar)

1. **`6253676` — Seed-Tenant-Leak in den Fixtures.** `_seed_test_tenant` committet 1 `organisations`- +
   1 Trigger-`tenant_orgs`-Row im `db_session`/`client`-Setup (`engine.begin()`), aber die Teardowns räumten
   sie nie weg → JEDE fixture-nutzende Testfunktion leakte 1 org + 1 tenant_org → Baseline-Wächter rot über die
   ganze Suite (Cascade). **Fix:** `_seed_test_tenant` gibt `(tenant_id, org_id)` zurück; neuer
   `_leak_cleanup_seed_tenant`-Helfer räumt via `cleanup_rows` (reverse-FK) im `finally` beider Fixtures VOR
   `engine.dispose()` auf.
   - *Hinweis:* die urspüngliche „empirische Leaker-Map" (5 Files) war ein Artefakt der dokumentiert
     unzuverlässigen per-nodeid-Attribution des Wächters (conftest.py-Docstring) — sie maß WO der Wächter feuerte,
     nicht WER die Org erzeugte. Der Executor stoppte korrekt statt No-Op-`cleanup_rows` auf DB-freie Files zu
     bolzen (Req-7).

2. **`10e5d0a` — `cleanup_rows` uuid-PK-Bug.** `DELETE … WHERE id = ANY(:ids)` mit Text-String-ids warf
   `operator does not exist: uuid = text` bei uuid-PK-Tabellen (tenant_orgs/crm.*) → die ganze
   cleanup-Transaktion (inkl. organisations-Löschung) rollte zurück → nichts gelöscht (150 `[PGTEST-CLEANUP]`-
   Warnungen). Pre-existing Plan-01-Bug, erst durch Fix #1 exerziert. **Fix:** `id::text = ANY(:ids)` mit
   stringifizierten ids — uniform für int-PK und uuid-PK.

## Bewusst KNOWN-RED (eskaliert nach 08.23.2.PGTEST.GREEN, NICHT gefixt)

Nach beiden Fixes blieb das Gate rot (`555→507 errors`). Empirische Wurzel: **suite-weite Baseline-Nicht-
Konformität** — **61 Test-Files** leaken über **11 public-Tabellen** (organisations, users, tenant_orgs,
ewb_ratings, conversation_logs, revenue_log, api_cost_log, fixed_costs, prompt_versions, exchange_rates,
profiles) + **7 base-seed-Delete-Bugs** (Tests registrieren id=1 zum Löschen → FK-blockiert). Plan-01s globaler
fail-closed Baseline-Wächter verlangt, dass JEDER der ~600 Tests die GANZE public-Baseline pristine lässt — die
über viele Phasen gegen Wegwerf-SQLite (ohne Wächter) geschriebene Suite war nie so gebaut. Das ist eine
Architektur-/Design-Entscheidung (Plan-01), kein lokaler Fix. Plus **51 Assertion-Fails** (~22 Plan-03/04-Port-
Bugs zum Triagieren, ~29 fremde/env-abhängige reds wie real-Haiku/Perf).

→ Voller Scope + 4 🔴-Teile in **08.23.2.PGTEST.GREEN** (ROADMAP.md + Vault `01 Roadmap.md`). Isolations-Bauart
wird dort spec't (Auto-Reset-Wächter führender Kandidat), NICHT jetzt entschieden.

## Prod-Sicherheit bestätigt

`nerve_test` per trap garantiert weggeräumt, `nerve` (Prod) NIE angefasst (nur-lesender `pg_dump`, Whitelist-Guard).
Kein Restart/Deploy passierte. Lokales `origin/main` enthält die Infrastruktur-Commits + die 2 Bug-Fixes.
