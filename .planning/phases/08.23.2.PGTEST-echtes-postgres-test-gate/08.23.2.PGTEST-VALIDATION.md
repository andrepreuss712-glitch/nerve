---
phase: 08.23.2.PGTEST
slug: echtes-postgres-test-gate
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-15
---

# Phase 08.23.2.PGTEST — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> ⚠️ Multi-Segment-ID-Gotcha: Pfade hartkodiert auf `.planning/phases/08.23.2.PGTEST-echtes-postgres-test-gate/`.
> ⚠️ HART (CLAUDE.md): Kein Local-Dev. Alle Checks laufen SERVERSEITIG im `deploy.sh production`-Gate gegen `nerve_test` — NICHT lokal.
> Voll-Architektur + STRIDE-Threat-Model: siehe `08.23.2.PGTEST-RESEARCH.md` → "Validation Architecture" + "Security Domain".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (in `/opt/nerve/venv`) |
| **Config file** | `tests/conftest.py` (keine pytest.ini) |
| **Quick run command** | `pytest tests/test_X.py -x -q` (serverseitig im deploy-Gate, gegen `nerve_test`) |
| **Full suite command** | `pytest tests/ --tb=short -q` (im `deploy.sh`-Gate gegen `nerve_test`) |
| **Estimated runtime** | Gate + Provision/Teardown ~+30s (D-08: Korrektheit vor Tempo) |

---

## Sampling Rate

- **After every task commit:** kein lokaler Run (HART) — Code committen + pushen; der echte Check ist der Gate-Lauf.
- **After every plan wave:** `bash deploy.sh production` (Gate baut `nerve_test` → `alembic upgrade head` → pytest → teardown).
- **Before `/gsd-verify-work`:** vollständiger `deploy.sh production`-Lauf grün (Tests + Schild-Guard + Restart), Prod-`nerve` unverändert.
- **Max feedback latency:** ein Deploy-Roundtrip (kein lokaler Schnell-Pfad — bewusste Konsequenz der Kein-Local-Dev-Regel).

---

## Per-Task Verification Map

> Req = die 9 SPEC-Requirements (intern Req-1..9, keine projektweiten REQ-IDs). Plan/Wave füllt der Planner; hier der Req→Check-Vertrag aus RESEARCH.md.

| Req | Verhalten | Threat Ref | Secure Behavior | Test Type | Automated Command (serverseitig) | File Exists | Status |
|-----|-----------|------------|-----------------|-----------|----------------------------------|-------------|--------|
| Req-1 | pytest gegen `postgresql://` | — | Gate-DSN ist `…/nerve_test`, künstlicher Schema-Defekt → 1 Test rot (auf SQLite grün geblieben) | integration | Deploy-Log zeigt `postgresql://…/nerve_test` + False-Green-Gegenprobe | Gate neu | ⬜ pending |
| Req-2 | conftest kein SQLite | — | kein hardcoded `sqlite:///:memory:` im aktiven Fixture-Code | static | `grep "sqlite:///:memory:" tests/conftest.py` → leer | ❌ W-Edit | ⬜ pending |
| Req-3 | Schema via `alembic upgrade head` | T: Schema-Drift | `alembic_version`=Live-HEAD, crm/training-Schemas + 0013/0015-RLS-Policies da | integration | Katalog-Query nach Build (`\dn`, `pg_policies`, `alembic_version`) | Gate neu | ⬜ pending |
| Req-4 | RLS+Anon laufen WIRKLICH | T: BYPASSRLS/rollback False-Green | beide PASSED (nicht SKIPPED); nerve_app/anon_worker `rolbypassrls=f`; D-04 Real-Commit | integration | `test_rls_isolation.py` + `test_anonymizer_worker.py` PASSED im Gate-Log | ✅ existieren, brauchen DSN+Port | ⬜ pending |
| Req-5 | alle 4 DSNs → `nerve_test` | T: Test berührt Prod-`nerve` | kein DSN zeigt im Test-Lauf auf `nerve` | static+integration | DSN-Beleg im Log; kein `@/nerve` (ohne `_test`) im Gate-Env | ❌ W-Edit | ⬜ pending |
| Req-6 | SQLite-Emulation entfernt | T: toter SQLite-Pfad | ATTACH-Listener (db.py) + app.py:1115 weg; Suite grün ohne | static | `grep` ATTACH-Listener + SQLite-alembic-Hook → leer/Foundation-begründet | ❌ Edit + Klasse-A-Port | ⬜ pending |
| Req-7 | FAIL-CLOSED | T: stille Hintertür | Provision/Connect/Schema/pytest je eigener Exit-Check, kein SQLite/Prod-Fallback-Zweig | integration | Simulierter Fehler → `deploy.sh` Exit≠0, kein Restart, expliziter Grund | Gate neu | ⬜ pending |
| Req-8 | sauberer Teardown | T: Orphan-Rest | kein verwaister `nerve_test`/Rolle/Artefakt nach Lauf | integration | `\l` nach Lauf → kein `nerve_test`; `trap cleanup EXIT` + Pre-Run-DROP | trap EXIT | ⬜ pending |
| Req-9 | Prod + Schild-Guard grün | T: Regression Prod-Pfad | vollständiger `deploy.sh production` grün, Prod-`nerve` unverändert | integration | Voll-Deploy-Lauf grün (Tests+Schild-Guard+Restart) | ✅ existiert, ggf. umgelenkt | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Gaps die VOR/IM ersten Wave geschlossen werden müssen (aus RESEARCH.md Q6 + Validation Architecture):

- [ ] `tests/test_account_memory_briefing.py` — von SQLite-StaticPool auf `nerve_test`-Fixture portieren (**Klasse A** — hängt am ATTACH-Listener den Req-6 entfernt, MUSS im selben Wave mit Req-6)
- [ ] `tests/test_anonymizer_worker.py` Logic-Group — Backend-Wechsel SQLite→PG (**Klasse A**)
- [ ] `TEST_TENANT_UUID`-Konstante + `tenant_orgs`-Seed in conftest (D-05, FK-Zwang)
- [ ] `db_session`/`client` auf `TEST_DATABASE_URL` + `nerve_app` + `set_current_tenant(TEST_TENANT_UUID)` umbauen (D-05; sonst RLS-fail-closed 0 Zeilen via after_begin-Hook)
- [ ] 3 Spezial-Fixture-DSNs `@/nerve` → `@/nerve_test` (anon_worker: scram `@127.0.0.1`, PW aus `ionos-s3.env`)
- [ ] `deploy.sh` Gate-Block (BEWIESENER pg_dump-Pfad, siehe RESEARCH „⚑ BUILD-PATH LOCKED"): Whitelist-Guard D-02 → `trap cleanup EXIT` → Pre-Run-DROP (verwaiste `nerve_test` existiert HEUTE schon!) → `CREATE DATABASE nerve_test OWNER postgres` → `pg_dump --schema-only nerve | psql -v ON_ERROR_STOP=1 -d nerve_test` (trägt Schema+RLS+FORCE+GRANTs) → `pg_dump --data-only --table=alembic_version nerve | psql nerve_test` (Stamp-Row) → `alembic upgrade head` (nur neue Revs über prod-head) → pytest (4 DSNs) → `trap` DROP

*Wichtigster Forschungsbefund + empirischer Beweis (2026-06-15, supervised): `alembic upgrade head` gegen LEERE `nerve_test` scheitert bei 0008 (Migration 0001 ist No-op-Marker); UND `create_all (volles Modell) → stamp 0001 → upgrade head` kollidiert bei 0002 (add_column doppelt). GEWÄHLTER & BEWIESENER Pfad = **pg_dump --schema-only + data-only-alembic_version-Row + upgrade head (nur neue Revs)**. Treue empirisch belegt: 7 crm-RLS-Policies + FORCE auf allen 5 crm-Tabellen + GRANTs vom Dump getragen; echte Cross-Tenant-Tests (11 passed); upgrade head applizierte nur 0016. `create_postgres_schema.py` ist NICHT mehr Gate-Baustein (nur realer Cutover).*

---

## Manual-Only Verifications

| Behavior | Req | Why Manual | Test Instructions |
|----------|-----|------------|-------------------|
| Voll-Deploy-Lauf endet grün + Prod-`nerve` unverändert | Req-9 | Production-Deploy ist kein automatisierbarer Unit-Test; verlangt den echten `deploy.sh production`-Roundtrip | `bash deploy.sh production` ausführen, Log auf grün prüfen, danach `inspect.sh migrations`/`schilder` gegen Prod (unverändert) |
| Fail-closed bei simuliertem Provisionierungs-Fehler | Req-7 | erfordert bewusstes Kaputtmachen der Test-DB-Provisionierung im Gate-Kontext | Provision-Schritt künstlich brechen (z.B. falsche Rolle), prüfen: Exit≠0, KEIN Service-Restart, expliziter Log-Grund |

---

## Validation Sign-Off

- [ ] Alle Tasks haben `<automated>`-verify (serverseitig) oder Wave-0-Dependency
- [ ] Sampling-Kontinuität: keine 3 aufeinanderfolgenden Tasks ohne Check (Gate-Lauf pro Wave)
- [ ] Wave 0 deckt alle MISSING/Klasse-A-Referenzen ab
- [ ] Keine watch-mode-Flags
- [ ] False-Green-Gegenproben verankert (Req-1 künstlicher Defekt, Req-4 Real-Commit, BYPASSRLS abgelehnt)
- [ ] `nyquist_compliant: true` gesetzt nach Plan-Checker-Pass

**Approval:** pending
