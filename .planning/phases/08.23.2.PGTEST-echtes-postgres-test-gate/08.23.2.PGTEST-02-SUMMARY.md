---
phase: 08.23.2.PGTEST
plan: 02
subsystem: testing
tags: [postgres, deploy, pg_dump, rls, alembic, fail-closed, ci-gate, schild-guard]

# Dependency graph
requires:
  - phase: 08.23.2.PGTEST Plan 01
    provides: "conftest-Fixtures (db_session/client -> nerve_test MODUL-SessionLocal-Rebind, _seed_test_tenant, TEST_TENANT_UUID, cleanup_rows-Helfer, public.*-only _baseline_cleanup_guard, NERVE_*_TEST_DSN/schild_guard_pg_conn-Routing auf nerve_test, A-1-Tripwire test_rls_generic_smoke.py)"
provides:
  - "deploy.sh Postgres-Test-Gate-Block: Whitelist-Guard nerve_test + trap cleanup EXIT + Pre-Run-DROP + CREATE OWNER postgres + pg_dump-Restore (Schema + alembic_version-Stamp, beide set -o pipefail) + alembic upgrade head"
  - "inline Dump-Treue-Katalog-Gate (crm-RLS-Policies >=7, FORCE >=5, anon-SELECT-GRANTs >=5, fail-closed, NACH upgrade VOR pytest)"
  - "pytest gegen nerve_test via sudo -u nerve_app env ANON_PW=... TEST_DB=... bash -c (env als Kommando) mit DATABASE_URL (A-1) + 4 nerve_test-DSNs (single-quoted inner)"
  - "POST-SUITE-Baseline-Check (sudo postgres, RLS-bypassed): crm.* == 0 Rows + training.transcript_archive == 0 Rows, fail-closed"
  - "Schild-Guard ins Gate gefaltet (kein bare @/nerve-DSN mehr; test_schild_guard PASSED statt SKIPPED, MED-2)"
affects: [08.23.2.PGTEST-03 (SQLite-Entfernung db.py/app.py + Klasse-A-Port), 08.23.2.PGTEST-04, 08.23.2.TAXO1 (intent_event-Migration wird vom Gate gegatet)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "pg_dump-Restore-Build statt create_all+stamp+upgrade (kollisionsfrei, traegt RLS/FORCE/GRANTs treu)"
    - "set -o pipefail in Dump-Pipes + psql -v ON_ERROR_STOP=1 (kein pg_dump-Crash-Masking durch psql-Exit-0)"
    - "sudo -u USER env NAME=VALUE bash -c '...' (env als auszufuehrendes Kommando — Secret-Env-Uebergabe ohne String-Interpolation)"
    - "DATABASE_URL=postgres in der pytest-Subshell zwingend, damit db.py den after_begin-RLS-Hook beim Import registriert (A-1)"
    - "POST-SUITE-Baseline-Check via sudo -u postgres psql (peer-auth, passwordless, SCHILD-Muster) — RLS-bypassed Cross-Tenant/Anonymizer-Leak-Guard"

key-files:
  created:
    - ".planning/phases/08.23.2.PGTEST-echtes-postgres-test-gate/08.23.2.PGTEST-02-SUMMARY.md"
  modified:
    - "deploy.sh (SQLite-Test-Stufe Z.130-143 -> Postgres-Gate-Block; separate Schild-Guard-Stufe -> Gate-Fold-Note)"

key-decisions:
  - "Manueller SSH-Katalog-Build (Task 2 action Schritt 1, Pre-Execute-Zusatzbeleg) DEFERRED an den orchestrator-deploy.sh-Lauf — HARD_OVERRIDE verbietet jeden Prod-Postgres-Kontakt jetzt; der inline-Katalog-Gate (Task 1 Schritt 8) ist der automatisierte fail-closed Guard und ist committet."
  - "Schild-Guard-Stufe komplett ins Gate gefaltet statt nur DSN umgelenkt — eine separate Stufe NACH dem Gate-Block liefe nach trap-Teardown von nerve_test (DB weg) und ein DSN @/nerve verletzt Req-5."

patterns-established:
  - "Gate-Build via pg_dump --schema-only (mit owners+privileges) + --data-only alembic_version-Stamp + upgrade head"
  - "Jeder Gate-Schritt fail-closed mit eigenem Klartext-FEHLER-Grund (D-07), kein SQLite/Prod-Ausweich-Zweig"

requirements-completed: [Req-1, Req-3, Req-4, Req-5, Req-7, Req-8, Req-9]

# Metrics
duration: ~20min
completed: 2026-06-16
---

# Phase 08.23.2.PGTEST Plan 02: deploy.sh Postgres-Test-Gate-Block Summary

**deploy.sh faehrt die pytest-Suite jetzt gegen eine per pg_dump-Restore vom Prod-nerve gebaute Wegwerf-`nerve_test` (Schema+RLS+FORCE+GRANTs treu) mit Whitelist-Guard, inline-Dump-Treue-Katalog-Gate, DATABASE_URL-A-1-Fix, sudo-env-pytest und POST-SUITE crm.*/training.*-Baseline-Check — alles fail-closed, statt der alten SQLite-in-memory-Stufe.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-06-16
- **Completed:** 2026-06-16
- **Tasks:** 2 (Task 1 voll umgesetzt; Task 2 Code-Teil voll, Pre-Execute-Proof deferred — siehe Deviations)
- **Files modified:** 1 (deploy.sh)

## Accomplishments
- Alte SQLite-in-memory-Test-Stufe (deploy.sh Z.130-143) durch echten Postgres-Gate-Block ersetzt: Whitelist-Guard `nerve_test` (D-02) + `trap cleanup EXIT` (D-06) + Pre-Run-DROP + CREATE OWNER postgres + pg_dump-Restore (Schema + alembic_version-Stamp, beide `set -o pipefail`) + `alembic upgrade head`.
- Inline-Dump-Treue-Katalog-Gate (NACH upgrade, VOR pytest): fail-closed Counts crm-RLS-Policies >=7, FORCE >=5, anon-SELECT-GRANTs >=5 — der automatisierte False-Green-Guard pro Deploy.
- pytest-Subshell via `sudo -u nerve_app env ANON_PW=... TEST_DB=... bash -c '...'` (#1 BLOCKER: `env` als Kommando, sonst „command not found"-Crash) mit `DATABASE_URL` (A-1, sonst kein RLS-Hook beim db.py-Import) + 4 nerve_test-DSNs; innerer `bash -c` single-quoted (PW nie interpoliert/geloggt).
- POST-SUITE-Baseline-Check (HYBRID, André locked) via `sudo -u postgres psql` (peer-auth, passwordless, SCHILD-Muster): crm.* == 0 Rows UND `training.transcript_archive` == 0 Rows, fail-closed — deckt die crm.*- + training.*-Seite, die Plan 01s public.*-only-Waechter nicht sieht.
- Schild-Guard ins Gate gefaltet: separate `@/nerve`-Stufe entfernt (Req-5 — kein bare Prod-DSN mehr), `test_schild_guard.py` laeuft im Haupt-`pytest tests/`-Lauf mit `NERVE_SCHILD_TEST_DSN=@/nerve_test` → PASSED statt SKIPPED (MED-2).

## Task Commits

Jeder Task atomar committet (mit Hooks, kein --no-verify):

1. **Task 1: SQLite-Test-Stufe -> Postgres-Gate-Block (Provision -> pg_dump-Restore-Build -> inline-Katalog-Gate -> pytest DATABASE_URL+4-DSN -> POST-SUITE-Baseline -> Teardown)** - `76536b1` (feat)
2. **Task 2: Schild-Guard ins Gate gegen nerve_test falten (Req-9/Req-5/MED-2)** - `3201265` (feat)

**Plan metadata:** (final docs-Commit nach diesem SUMMARY + STATE/ROADMAP)

## Files Created/Modified
- `deploy.sh` — SQLite-Test-Stufe (Z.130-143) durch Postgres-Gate-Block ersetzt (10 nummerierte Schritte); separate Schild-Guard-pytest-Stufe durch Gate-Fold-Note-Kommentar ersetzt.
- `.planning/phases/08.23.2.PGTEST-echtes-postgres-test-gate/08.23.2.PGTEST-02-SUMMARY.md` — dieses Summary.

## Static Verification (HARD_OVERRIDE: kein deploy.sh-Lauf, kein voller pytest — nerve_test existiert nur im Gate auf der Prod-Instanz)

- `bash -n deploy.sh` → **PASS** (Syntax valide, nach beiden Tasks).
- key_links / Guard-greps (alle present):
  - `set -o pipefail; pg_dump --schema-only nerve` ×1, `--table=alembic_version` ×1, `alembic upgrade head` ×1
  - Whitelist-Guard `"$TEST_DB" != "nerve_test"` ×1, `trap cleanup EXIT` ×1 (+ cleanup-Def)
  - `sudo -u nerve_app env ANON_PW=` ×1 (#1 BLOCKER-Fix)
  - `DATABASE_URL="postgresql://nerve_app@/` ×1 (A-1) + `TEST_DATABASE_URL=...` ×1
  - inline-Gate `pg_policies WHERE schemaname='crm'` ×1, FORCE-Count, anon-SELECT-GRANTs-Count
  - POST-SUITE `crm.account_memory UNION ALL ...` ×1 + `training.transcript_archive` count ×1
- Req-5: **0 bare `@/nerve`-DSNs** (ohne `_test`) in deploy.sh — `NERVE_SCHILD_TEST_DSN` zeigt nur noch auf `${TEST_DB}`=nerve_test.
- `--no-privileges`/`--no-owner`: **nur im Kommentar** (Schritt 5), in keinem pg_dump-Kommando → Dump traegt owners+privileges (RLS-Treue).
- Heredoc-Escaping-Simulation: bestaetigt, dass die `\$`-/single-quote-Escapes die LOKALE Shell-Expansion neutralisieren — Server erhaelt literal `$TEST_DB`/`$ANON_PW` (root-level) bzw. single-quoted `${TEST_DB}`/`${ANON_PW}` (inner bash via `env`-Env). Kein vorzeitiges Leck.
- A-1-Vertrag gegen `database/db.py` verifiziert: db.py:9 default sqlite, db.py:86 `if 'sqlite' not in _DATABASE_URL` registriert den `after_begin`-Hook EINMAL zur Import-Zeit → `DATABASE_URL=postgres` in der Subshell ist load-bearing (bestaetigt).
- `tests/test_schild_guard.py` nutzt `schild_guard_pg_conn` (SKIP ohne DSN, scharf mit `NERVE_SCHILD_TEST_DSN`) → laeuft im Gate gegen nerve_test als PASSED (MED-2-Mechanik bestaetigt).

**Voll-Beleg (Deploy-Log-greps: `Dump-Treue-Katalog-Gate OK`, `Running upgrade 0015 -> 0016`, `POST-SUITE Baseline-Check OK`, `test_schild_guard ... PASSED`, `grep -c "command not found" == 0`, anon_worker-PW nirgendwo geloggt) erst im EINEN orchestrator-deploy.sh-production-Lauf nach allen 4 Plans (MED-3 Ein-Deploy-Constraint).**

## Decisions Made
- **Manueller SSH-Katalog-Build (Task 2 action Schritt 1) deferred:** Per HARD_OVERRIDE #1 darf jetzt kein Prod-Postgres beruehrt werden (CREATE/DROP nerve_test + pg_dump gegen Prod mitten im halb-konvertierten Baum = Noise + Prod-Kontakt). Der inline-Katalog-Gate (Task 1 Schritt 8) IST der automatisierte fail-closed Guard pro Deploy — der manuelle Build war nur ein Pre-Execute-Zusatzbeleg, nicht der einzige Fidelity-Check. Verbatim-Katalog-Output wird im orchestrator-Lauf erfasst.
- **Schild-Guard ganz ins Gate gefaltet** statt nur DSN umlenken: eine separate Stufe NACH dem Gate-Block laeuft nach `trap cleanup EXIT` (nerve_test gedroppt) ins Leere und ein DSN `@/nerve` verletzt Req-5. Der Haupt-`pytest tests/`-Lauf deckt `test_schild_guard.py` mit gesetztem nerve_test-DSN bereits ab.

## Deviations from Plan

### Deferred (HARD_OVERRIDE-bedingt, keine Auto-Fix-Regel)

**1. Task 2 action Schritt 1 — manueller SSH-Pre-Execute-Katalog-Build deferred**
- **Found during:** Task 2 (Schild-Guard + Dump-Treue-Doku)
- **Issue:** Der Plan verlangt einen manuellen `ssh ... sudo -u postgres ...`-Build gegen einen Wegwerf-nerve_test auf der Prod-Instanz, um verbatim-Katalog-Output (pg_policies/relforcerowsecurity/role_table_grants/alembic_version) ins SUMMARY zu schreiben. Das beruehrt die Prod-Postgres-Instanz (CREATE/DROP DATABASE + pg_dump gegen Prod-nerve).
- **Resolution:** Deferred an den EINEN orchestrator-`deploy.sh production`-Lauf (HARD_OVERRIDE #1+#2 — kein Prod-Postgres-Kontakt, kein deploy.sh-Lauf, kein voller pytest jetzt). Der inline-Katalog-Gate (Task 1, Schritt 8) ist der automatisierte fail-closed Guard pro Deploy und ist committet (`76536b1`) — die Dump-Treue-Garantie ist NICHT manuell-only (Gemini-HIGH erfuellt). Erwartete Katalog-Werte sind in der Plan-Persistenz-Schicht-Sektion verbatim dokumentiert (7 Policies, FORCE auf 5, anon-SELECT-GRANTs, alembic_version=Repo-HEAD).
- **Files modified:** keine (reine Doku-Aktion, deferred).

---

**Total deviations:** 1 deferred (HARD_OVERRIDE-bedingt, kein Code-Impact — der automatisierte Guard ist committet).
**Impact on plan:** Kein Funktions-/Code-Verlust. Der manuelle Build war Zusatzbeleg, nicht der einzige Fidelity-Check. Code-Teil von Task 2 (Schild-Guard-Fold, Req-5/Req-9/MED-2) voll umgesetzt + committet.

## Issues Encountered
None — beide deploy.sh-Edits gingen sauber durch, `bash -n` PASS nach jedem, Heredoc-Escaping per Simulation bestaetigt.

## User Setup Required
None — keine externe Service-Konfiguration. (Der eine integrierte `deploy.sh production`-Lauf nach allen 4 Plans wird vom Orchestrator/André gefahren — Prod-only, HART.)

## Next Phase Readiness
- Plan 02 von 4 fertig (Wave 1). Der deploy.sh-Gate-Block ist statisch valide und committet.
- **Naechster Schritt:** Plan 03 (SQLite-Emulation entfernen: db.py:41-49 ATTACH-Listener + app.py:1115 SQLite-Alembic-Hook + Klasse-A-Port) — der Gate-Baum ist erst MIT Wave-2-Code konsistent (MED-3: kein Zwischen-Deploy nach Wave 1).
- **Voll-Validierung:** EIN integrierter `deploy.sh production`-Lauf NACHDEM alle 4 Plans committet sind (vom Orchestrator). Erst dort laeuft das Gate gegen die echte nerve_test auf der Prod-Instanz und belegt A-1-Hook-Registrierung, Dump-Treue, POST-SUITE-Baseline, Schild-Guard-PASSED, #1-env-Fix.

## Self-Check: PASSED
- deploy.sh: FOUND (modified)
- SUMMARY.md: FOUND
- Commit 76536b1 (Task 1): FOUND
- Commit 3201265 (Task 2): FOUND
- `bash -n deploy.sh`: PASS

---
*Phase: 08.23.2.PGTEST-echtes-postgres-test-gate*
*Completed: 2026-06-16*
