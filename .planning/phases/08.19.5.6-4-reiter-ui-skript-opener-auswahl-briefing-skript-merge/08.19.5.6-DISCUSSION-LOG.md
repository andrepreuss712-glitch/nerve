# Phase 08.19.5.6 — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-05
**Phase:** 08.19.5.6 — 4-Reiter-UI Skript+Opener-Auswahl + Briefing-Skript-Merge
**Areas discussed:** Tab-CSS-Pattern, parent_id FK-Entscheidung, renderStep5() Struktur

---

## Tab-CSS-Pattern

| Option | Description | Selected |
|--------|-------------|----------|
| .tab-btn / .tabs (coach_firma-Pattern) | Inline style, onclick-basiert, CSS-Vars | |
| .fcd-tab / .fcd-tabs (admin-Pattern) | nerve.css-defined, data-tab-Attribut | ✓ |

**User's choice:** `.fcd-tab / .fcd-tabs`
**Notes:** Trotz Hinweis auf Event-Listener-Verlust bei innerHTML-Replace — gelöst durch inline onclick im JS-String statt addEventListener.

---

### Tab-CSS Follow-up: Event-Listener-Strategie

| Option | Description | Selected |
|--------|-------------|----------|
| onclick inline im HTML | `onclick="window.switchTab5('opener')"` direkt im JS-String | ✓ |
| Event-Delegation auf Container | Ein Listener auf Launcher-Container mit target-Check | |

**User's choice:** onclick inline
**Notes:** Konsistent mit bestehendem pip-launcher.js-Muster.

---

### Tab-CSS Follow-up: PiP-spezifische CSS-Erweiterungen

| Option | Description | Selected |
|--------|-------------|----------|
| Bestehende fcd-tab-Styles 1:1 | Kein neues CSS, Override nur wenn optisch nötig | ✓ |
| Neue n-tab-*-Klassen für PiP | Eigene PiP-spezifische CSS-Klassen in nerve.css | |

**User's choice:** 1:1 — nur `.pip-window .fcd-tab {}` Override wenn nötig nach Sichttest.

---

## parent_id FK-Entscheidung

| Option | Description | Selected |
|--------|-------------|----------|
| FK entfernen, nullable int | models.py ändern, kein app.py-Migration-Snippet | ✓ |
| FK-Migration auf profile_skripte.id | Alembic/Recreation-Migration | |
| Neue Spalte source_skript_id | parent_id unberührt, neue Spalte | |

**User's choice:** FK entfernen (models.py only)
**Notes:** SQLite enforced FKs nur bei PRAGMA foreign_keys=ON (Flask-Default OFF) → de facto nie enforced. Kein relationship() auf parent_id vorhanden.

---

### parent_id Follow-up: Scope der models.py-Änderung

| Option | Description | Selected |
|--------|-------------|----------|
| Nur models.py ändern | ForeignKey() entfernen, Column bleibt | |
| models.py + ORM-Relationship entfernen | Zusatzcheck auf relationship() | ✓ |

**User's choice:** models.py + relationship-Check
**Notes:** Grep bestätigt: kein relationship() auf parent_id — nur Column-Definition-Änderung nötig.

---

## renderStep5() Struktur

| Option | Description | Selected |
|--------|-------------|----------|
| Monolithisch mit Tab-Guard | Alles in renderStep5(), if/else per activeTab | ✓ |
| Dispatcher-Pattern (renderTabContent) | renderStep5() → renderTabContent(tabName) | |

**User's choice:** Monolithisch
**Notes:** Konsistent mit renderStep4b/4c-Muster im File.

---

### renderStep5() Follow-up: Helper-Funktionen

| Option | Description | Selected |
|--------|-------------|----------|
| Inline-Variablen in renderStep5 | var openerContent etc. als lokale Vars | ✓ |
| Benannte Helper-Funktionen | buildOpenerTab() etc. im File-Scope | |

**User's choice:** Inline-Variablen

---

### renderStep5() Follow-up: switchTab5() Ort

| Option | Description | Selected |
|--------|-------------|----------|
| window.switchTab5 im File-Scope | function switchTab5(tab) { state.activeTab = tab; renderStep5(); } | ✓ |
| Anonyme onclick-Lambdas | state.activeTab direkt in onclick-String setzen | |

**User's choice:** window.switchTab5 als File-Scope-Funktion

---

## Claude's Discretion

- Vorwissen-Picker Sub-Sektion Reihenfolge innerhalb Step 5
- activeTab Reset-Verhalten bei Profil-Wechsel

## Deferred Ideas

Keine.
