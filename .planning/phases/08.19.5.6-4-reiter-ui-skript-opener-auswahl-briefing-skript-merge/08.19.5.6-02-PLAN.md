---
phase: 08.19.5.6
plan: 02
type: execute
wave: 1
depends_on: []
deploy_sync: "PFLICHT — Plan 01 + Plan 02 müssen ATOMAR deployed werden (gleicher Git-Commit). Plan 01 sendet call_mode + skript_id/opener_id je nach Modus; Plan 02 routet modus-abhängig im Backend. Getrennt deployed → 400 Bad Request."
files_modified:
  - database/models.py
  - app.py
  - routes/app_routes.py
autonomous: true
requirements:
  - R8

must_haves:
  truths:
    - "ProfileSkript hat is_personalized, parent_id (kein FK), briefing_source_firma Columns"
    - "DB-Migration für profile_skripte (3 neue Columns) in app.py vorhanden und idempotent"
    - "api_personalize_skript() akzeptiert call_mode + skript_id (meeting) ODER opener_id (cold_call/backwards-compat), lädt das passende Modell, gibt personalisierten Text zurück"
    - "api_personalize_skript_save() akzeptiert call_mode + skript_id (meeting) ODER opener_id (cold_call/backwards-compat), speichert Ergebnis IMMER als ProfileSkript(is_personalized=True)"
    - "Cold-Call-Pfad in api_personalize_skript(): lädt ProfileOpener per opener_id, KI-Input = opener.inhalt"
    - "Meeting-Pfad in api_personalize_skript(): lädt ProfileSkript per skript_id, KI-Input = skript.inhalt"
    - "Cap-Check in api_personalize_skript_save() läuft gegen ProfileSkript.is_personalized (beide Modi)"
    - "KEIN neues ProfileOpener-Row wird in api_personalize_skript_save() erstellt"
    - "generate_personalized_skript() in precall_service.py UNVERÄNDERT"
    - "/api/launcher/init und /api/launcher/profile/<pid> Routen UNVERÄNDERT"
  artifacts:
    - path: "database/models.py"
      provides: "ProfileSkript mit is_personalized + parent_id (kein FK) + briefing_source_firma"
    - path: "app.py"
      provides: "Migration für profile_skripte.is_personalized, .parent_id, .briefing_source_firma"
    - path: "routes/app_routes.py"
      provides: "api_personalize_skript() + api_personalize_skript_save() modus-abhängig (call_mode-Routing)"
  key_links:
    - from: "api_personalize_skript()"
      to: "ProfileSkript (meeting) / ProfileOpener (cold_call)"
      via: "call_mode-Routing: skript_id→ProfileSkript oder opener_id→ProfileOpener"
      pattern: "call_mode.*meeting.*ProfileSkript"
    - from: "api_personalize_skript_save()"
      to: "ProfileSkript(is_personalized=True)"
      via: "Insert IMMER als ProfileSkript — auch bei opener_id-Fallback"
      pattern: "new_item = ProfileSkript.*is_personalized=True"
---

<objective>
Wave 1 Plan B — v3: Backend-Umstellung der KI-Personalisierungs-Endpoints auf modus-abhängiges Routing.

Zweck: /api/precall/personalize und /api/precall/personalize/save erhalten modus-abhängige Logik gesteuert durch `call_mode` im Request-Body.

**MODUS-ABHÄNGIGE PERSONALISIERUNG (NEU v3 — Andre-Decision Architektur-Erweiterung):**
- call_mode === 'meeting': Lädt aus ProfileSkript (per skript_id). KI bekommt skript.inhalt. Ergebnis als ProfileSkript(is_personalized=True) gespeichert.
- call_mode === 'cold_call' (oder kein call_mode): Lädt aus ProfileOpener (per opener_id). KI bekommt opener.inhalt. Ergebnis als ProfileSkript(is_personalized=True) gespeichert — auch im Cold-Call-Pfad landet das Ergebnis in profile_skripte (kein ProfileOpener-Insert mehr).

**BACKWARDS-COMPAT:** Beide Endpoints akzeptieren skript_id ODER opener_id als Fallback wenn call_mode fehlt. Vorrangig: call_mode steuert den Pfad; id-Typ ist der Fallback.

**ATOMIC DEPLOY:** Dieser Plan MUSS im selben Git-Commit wie Plan 01 deployed werden. Plan 01 sendet call_mode + passende id; Plan 02 liefert die modus-abhängige Backend-Logik.

ProfileSkript-Modell bekommt die dafür nötigen Columns (is_personalized, parent_id ohne FK, briefing_source_firma). generate_personalized_skript() in precall_service.py bleibt unverändert.

Output: models.py mit 3 neuen ProfileSkript-Columns. Migration in app.py. Beide Endpoints auf call_mode-Routing umgestellt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-SPEC.md
@.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-CONTEXT.md

<interfaces>
<!-- ProfileSkript AKTUELL (models.py:138-145) — noch OHNE neue Columns -->
```python
class ProfileSkript(Base):
    __tablename__ = 'profile_skripte'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)
```

<!-- ProfileOpener AKTUELL (models.py:162-173) — Referenz für neue Columns -->
```python
class ProfileOpener(Base):
    __tablename__ = 'profile_opener'
    ...
    parent_id             = Column(Integer, ForeignKey('profile_opener.id'), nullable=True)
    is_personalized       = Column(Boolean, default=False, nullable=False)
    briefing_source_firma = Column(String(200), nullable=True)
```

<!-- Nach diesem Plan: ProfileSkript mit 3 neuen Columns -->
```python
    parent_id             = Column(Integer, nullable=True)              # D-04: KEIN ForeignKey
    is_personalized       = Column(Boolean, default=False, nullable=False)
    briefing_source_firma = Column(String(200), nullable=True)
```

<!-- Migrations-Pattern (app.py — identisch zu profile_opener-Block, nur Tabellenname ändert) -->
```python
        for col, typedef in [
            ('parent_id',             'INTEGER'),
            ('is_personalized',       'BOOLEAN DEFAULT 0'),
            ('briefing_source_firma', 'VARCHAR(200)'),
        ]:
            try:
                conn.execute(text(f'ALTER TABLE profile_skripte ADD COLUMN {col} {typedef}'))
                conn.commit()
                print(f"[DB] Migration: added profile_skripte.{col}")
            except Exception:
                pass  # Idempotent
```

<!-- generate_personalized_skript Signatur (precall_service.py) — NICHT ÄNDERN -->
```python
def generate_personalized_skript(briefing_dict, opener_inhalt, profil_daten, user_id):
```
Parameter-Name `opener_inhalt` bleibt — nur Wert-Quelle ändert sich (skript.inhalt oder opener.inhalt je nach Modus).

<!-- call_mode-Routing-Logik (NEU v3) -->
Primär-Entscheidung: call_mode aus request body
  'meeting'   → skript_id + ProfileSkript-Pfad
  'cold_call' → opener_id + ProfileOpener-Pfad (für KI-Input); Speicherung IMMER ProfileSkript
  fehlt/None  → Fallback: wenn skript_id vorhanden → meeting-Pfad; wenn opener_id → cold_call-Pfad

<!-- PERSONALIZED_SCRIPTS_CAP (config.py) — unveränderte Konstante, gegen ProfileSkript zählen -->
<!-- Cap-Response Format — identisch -->
{'cap_exceeded': True, 'items': [{'id': ..., 'name': ..., 'weeks_old': ...}]}

<!-- R7: Launcher-Init-Routen dürfen NICHT geändert werden -->
/api/launcher/init  (app_routes.py ~840)
/api/launcher/profile/<pid>  (app_routes.py ~850)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileSkript — 3 neue Columns in models.py</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py Zeilen 138-145 (ProfileSkript AKTUELL — Einfügepunkt nach created_at)
    - database/models.py Zeilen 162-173 (ProfileOpener als Referenz für Spalten-Definitionen)
  </read_first>
  <action>
Nach `created_at = Column(DateTime, default=utcnow)` in der ProfileSkript-Klasse (aktuell Zeile 145) drei neue Columns einfügen.

**Aktuell (Zeilen 138-145):**
```python
class ProfileSkript(Base):
    __tablename__ = 'profile_skripte'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)
```

**Nach Edit:**
```python
class ProfileSkript(Base):
    __tablename__ = 'profile_skripte'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)
    parent_id             = Column(Integer, nullable=True)            # D-04: kein FK-Constraint; zeigt auf Quell-Item (ProfileSkript oder ProfileOpener id)
    is_personalized       = Column(Boolean, default=False, nullable=False)
    briefing_source_firma = Column(String(200), nullable=True)
```

WICHTIG: ProfileSkript.parent_id hat KEIN ForeignKey-Argument — per D-04 (SQLite enforced FKs nur bei PRAGMA foreign_keys=ON, Flask-Default ist OFF; keine relationship() auf parent_id; Constraint würde aber auf falscher Tabelle zeigen).
  </action>
  <verify>
    <automated>grep -A 12 "class ProfileSkript" database/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "is_personalized" database/models.py` → mindestens 2 Treffer (ProfileOpener + ProfileSkript)
    - `grep -n "parent_id" database/models.py` → mindestens 2 Treffer (ProfileOpener + ProfileSkript)
    - `grep -n "briefing_source_firma" database/models.py` → mindestens 2 Treffer (ProfileOpener + ProfileSkript)
    - `grep -A 12 "class ProfileSkript" database/models.py | grep "ForeignKey.*parent_id\|parent_id.*ForeignKey"` → 0 Treffer (kein FK auf parent_id in ProfileSkript)
    - `python -c "from database.models import ProfileSkript; cols = [c.key for c in ProfileSkript.__table__.columns]; assert 'is_personalized' in cols and 'parent_id' in cols and 'briefing_source_firma' in cols; print('OK')"` → OK
  </acceptance_criteria>
  <done>ProfileSkript hat is_personalized, parent_id (ohne FK-Constraint), briefing_source_firma als neue Columns nach created_at.</done>
</task>

<task type="auto">
  <name>Task 2: DB-Migration für profile_skripte in app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py Zeilen 395-430 (bestehenden _migrate()-Bereich lesen — profile_opener-Block als Referenz-Pattern und exakten Einfügepunkt bestimmen)
  </read_first>
  <action>
Nach dem bestehenden Migrations-Block für profile_opener einen identischen Block für profile_skripte einfügen. Den exakten Einfügepunkt aus dem Read bestimmen (nach dem letzten `pass  # Idempotent`-Statement des profile_opener-Blocks, vor dem nächsten Kommentar oder Block).

**Neuer Block (einfügen nach profile_opener-Migration):**

```python
        # ── Phase 08.19.5.6: is_personalized + parent_id + briefing_source_firma für ProfileSkript ──
        for col, typedef in [
            ('parent_id',             'INTEGER'),
            ('is_personalized',       'BOOLEAN DEFAULT 0'),
            ('briefing_source_firma', 'VARCHAR(200)'),
        ]:
            try:
                conn.execute(text(f'ALTER TABLE profile_skripte ADD COLUMN {col} {typedef}'))
                conn.commit()
                print(f"[DB] Migration: added profile_skripte.{col}")
            except Exception as e:
                if 'duplicate column name' in str(e).lower():
                    pass  # Spalte bereits vorhanden — idempotente Migration, erwartet
                else:
                    raise  # echter Migrations-Bug → sichtbar machen
```

WICHTIG: Der Block muss innerhalb desselben `conn`-Kontexts stehen wie die profile_opener-Migration. Denselben Einzug verwenden wie der profile_opener-Block. text-Import ist bereits verfügbar.
  </action>
  <verify>
    <automated>grep -n "profile_skripte.*ADD COLUMN\|added profile_skripte" app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "profile_skripte" app.py | grep "ADD COLUMN"` → 3 Treffer (parent_id, is_personalized, briefing_source_firma)
    - `grep -n "added profile_skripte" app.py` → 3 Print-Zeilen
    - Migration-Block steht nach dem profile_opener-Block im selben conn-Kontext
    - `python -c "from app import app; print('OK')"` → kein SyntaxError
  </acceptance_criteria>
  <done>app.py enthält Migration für profile_skripte (is_personalized, parent_id, briefing_source_firma) mit idempotenter try/except-Struktur, nach dem profile_opener-Block eingefügt.</done>
</task>

<task type="auto">
  <name>Task 3: api_personalize_skript() auf call_mode-Routing umstellen</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py Zeilen 1040-1107 (api_personalize_skript() vollständig lesen — aktueller Stand mit opener_id-Logik)
    - services/precall_service.py — grep nach "def generate_personalized_skript" (Signatur verifizieren, NICHT ändern)
    - database/models.py — grep nach "class ProfileSkript" (neue Columns bestätigen nach Task 1)
  </read_first>
  <action>
api_personalize_skript() komplett umschreiben (Innereien der Funktion, NICHT Route-Dekoratoren oder Docstring-Struktur).

**Neue Logik (Funktions-Body ersetzen):**

```python
    from services.precall_service import generate_personalized_skript
    from database.db import get_session as get_db_session

    data = request.get_json(force=True) or {}
    briefing_dict = data.get('briefing') or {}
    call_mode = (data.get('call_mode') or '').strip()  # 'meeting' | 'cold_call' | '' (backwards-compat)

    # ── call_mode-Routing ──
    # Primär: call_mode steuert den Pfad
    # Fallback (backwards-compat): call_mode leer → skript_id-Präsenz → meeting; sonst cold_call
    skript_id = None
    opener_id = None

    if call_mode == 'meeting':
        try:
            skript_id = int(data.get('skript_id'))
            if skript_id <= 0: raise ValueError
        except (TypeError, ValueError):
            return jsonify({'error': 'skript_id muss eine positive Ganzzahl sein'}), 400
    elif call_mode == 'cold_call':
        try:
            opener_id = int(data.get('opener_id'))
            if opener_id <= 0: raise ValueError
        except (TypeError, ValueError):
            return jsonify({'error': 'opener_id muss eine positive Ganzzahl sein'}), 400
    else:
        # Kein call_mode — Fallback: skript_id hat Vorrang, dann opener_id
        if data.get('skript_id') is not None:
            try:
                skript_id = int(data.get('skript_id'))
                if skript_id <= 0: raise ValueError
            except (TypeError, ValueError):
                return jsonify({'error': 'skript_id muss eine positive Ganzzahl sein'}), 400
        elif data.get('opener_id') is not None:
            try:
                opener_id = int(data.get('opener_id'))
                if opener_id <= 0: raise ValueError
            except (TypeError, ValueError):
                return jsonify({'error': 'opener_id muss eine positive Ganzzahl sein'}), 400
        else:
            return jsonify({'error': 'skript_id oder opener_id erforderlich'}), 400

    user_id = g.user.id if g.user else None
    profile_id = flask_session.get('active_profile_id')
    if not profile_id:
        return jsonify({'error': 'Kein aktives Profil'}), 400

    _db = get_db_session()
    try:
        # Org-Isolation-Check via Profile
        profile = _db.query(Profile).filter_by(id=profile_id).first()
        if not profile or profile.org_id != g.org.id:
            return jsonify({'error': 'Profil nicht gefunden oder kein Zugriff'}), 403

        profil_daten = {}
        if profile.daten:
            try:
                import json as _json
                profil_daten = _json.loads(profile.daten) if isinstance(profile.daten, str) else (profile.daten or {})
            except Exception:
                profil_daten = {}

        # ── Modus-abhängiges Item-Loading ──
        if skript_id:
            from database.models import ProfileSkript
            item = _db.query(ProfileSkript).filter_by(
                id=skript_id, profile_id=profile_id
            ).first()
            if not item:
                return jsonify({'error': 'Skript nicht gefunden'}), 400
        else:
            from database.models import ProfileOpener
            item = _db.query(ProfileOpener).filter_by(
                id=opener_id, profile_id=profile_id
            ).first()
            if not item:
                return jsonify({'error': 'Opener nicht gefunden'}), 400

        opener_inhalt = item.inhalt or ''  # Parameter-Name bleibt (generate_personalized_skript Signatur)
    finally:
        _db.close()

    personalized_text, error = generate_personalized_skript(
        briefing_dict=briefing_dict,
        opener_inhalt=opener_inhalt,   # Wert: skript.inhalt (meeting) ODER opener.inhalt (cold_call)
        profil_daten=profil_daten,
        user_id=user_id,
    )
    if error:
        return jsonify({'error': error}), 502

    return jsonify({'personalized_text': personalized_text})
```


PFLICHT-CHECK nach Edit: `grep -n "def api_launcher\|/api/launcher" routes/app_routes.py` — diese Routen NIE ANFASSEN (R7).

WICHTIG: Die Imports `Profile` und `ProfileOpener`/`ProfileSkript` müssen lokal in der Funktion oder am Funktions-Anfang stehen. Der bestehende Top-Level-Import von `ProfileOpener` aus dem alten Code muss ggf. entfernt oder durch lokale Imports ersetzt werden.
  </action>
  <verify>
    <automated>grep -n "call_mode\|skript_id\|opener_id\|ProfileSkript\|ProfileOpener" routes/app_routes.py | grep -v "api_personalize_skript_save\|api/launcher\|#" | head -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "call_mode" routes/app_routes.py | grep "api_personalize_skript\b"` → mindestens 1 Treffer (call_mode wird ausgewertet)
    - `grep -n "call_mode == .meeting." routes/app_routes.py` → mindestens 1 Treffer (meeting-Branch in api_personalize_skript)
    - `grep -n "call_mode == .cold_call." routes/app_routes.py` → mindestens 1 Treffer (cold_call-Branch)
    - `grep -n "ProfileSkript" routes/app_routes.py | grep "api_personalize_skript\b"` → Treffer (Meeting-Pfad lädt ProfileSkript)
    - `grep -n "ProfileOpener" routes/app_routes.py | grep "api_personalize_skript\b"` → Treffer (Cold-Call-Pfad lädt ProfileOpener — erlaubt)
    - `grep -n "/api/launcher/init\|/api/launcher/profile" routes/app_routes.py` → Routen vorhanden, unberührt
    - `python -c "from routes.app_routes import app_routes_bp; print('OK')"` → kein ImportError
  </acceptance_criteria>
  <done>api_personalize_skript() routet per call_mode: 'meeting' → ProfileSkript per skript_id; 'cold_call' → ProfileOpener per opener_id. Fallback ohne call_mode: skript_id-Präsenz → meeting-Pfad; opener_id-Präsenz → cold_call-Pfad. generate_personalized_skript() unverändert. Org-Isolation erhalten.</done>
</task>

<task type="auto">
  <name>Task 4: api_personalize_skript_save() auf call_mode-Routing + ProfileSkript-Insert umstellen</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py Zeilen 1110-1230 (api_personalize_skript_save() vollständig lesen — Cap-Check, Delete+Insert-Logik, Transaktions-Pattern)
    - config.py — grep nach "PERSONALIZED_SCRIPTS_CAP" (Konstante und Wert verifizieren)
  </read_first>
  <action>
api_personalize_skript_save() umstellen. Transaktions- und Cap-Logik bleibt strukturell identisch — nur Modell-Routing und Insert-Ziel ändern.

**Schritt 1 — Imports (am Funktions-Anfang oder lokal):**
```python
    from database.db import get_session as get_db_session
    from config import PERSONALIZED_SCRIPTS_CAP
    import datetime
```
ProfileOpener und ProfileSkript werden lokal per call_mode-Branch importiert.

**Schritt 2 — call_mode-Routing (ersetzt alten opener_id-Block):**
```python
    data = request.get_json(force=True) or {}
    personalized_text = (data.get('personalized_text') or '').strip()
    delete_ids = data.get('delete_ids') or []
    firmenname = (data.get('firmenname') or '').strip()[:50]
    call_mode = (data.get('call_mode') or '').strip()  # 'meeting' | 'cold_call' | ''

    skript_id = None
    opener_id = None

    if call_mode == 'meeting':
        try:
            skript_id = int(data.get('skript_id'))
            if skript_id <= 0: raise ValueError
        except (TypeError, ValueError):
            return jsonify({'error': 'skript_id muss eine positive Ganzzahl sein'}), 400
    elif call_mode == 'cold_call':
        try:
            opener_id = int(data.get('opener_id'))
            if opener_id <= 0: raise ValueError
        except (TypeError, ValueError):
            return jsonify({'error': 'opener_id muss eine positive Ganzzahl sein'}), 400
    else:
        # Kein call_mode — Fallback: skript_id Vorrang, dann opener_id
        if data.get('skript_id') is not None:
            try:
                skript_id = int(data.get('skript_id'))
                if skript_id <= 0: raise ValueError
            except (TypeError, ValueError):
                return jsonify({'error': 'skript_id muss eine positive Ganzzahl sein'}), 400
        elif data.get('opener_id') is not None:
            try:
                opener_id = int(data.get('opener_id'))
                if opener_id <= 0: raise ValueError
            except (TypeError, ValueError):
                return jsonify({'error': 'opener_id muss eine positive Ganzzahl sein'}), 400
        else:
            return jsonify({'error': 'skript_id oder opener_id erforderlich'}), 400

    if not personalized_text:
        return jsonify({'error': 'personalized_text ist Pflicht'}), 400
```

**Schritt 3 — Org-Isolation + Original-Lookup (beide Pfade):**
```python
        # Org-Isolation: verify profile belongs to current org
        _prof_check = _db.query(Profile).filter_by(id=profile_id, org_id=g.org.id).first()
        if not _prof_check:
            return jsonify({'error': 'Zugriff verweigert'}), 403

        # Modus-abhängiger Lookup des Original-Items
        if skript_id:
            from database.models import ProfileSkript
            original = _db.query(ProfileSkript).filter_by(
                id=skript_id, profile_id=profile_id
            ).first()
            if not original:
                return jsonify({'error': 'Skript nicht gefunden'}), 400
        else:
            from database.models import ProfileOpener
            original = _db.query(ProfileOpener).filter_by(
                id=opener_id, profile_id=profile_id
            ).first()
            if not original:
                return jsonify({'error': 'Opener nicht gefunden'}), 400
        # ab hier: original.name, original.id — beide Modelle haben diese Felder
```

**Schritt 4 — Cap-Check IMMER gegen ProfileSkript (beide Modi):**
```python
            # Cap-Check gegen ProfileSkript — in beiden Modi (meeting + cold_call)
            from database.models import ProfileSkript
            personalized_count = _db.query(ProfileSkript).filter_by(
                profile_id=profile_id, is_personalized=True
            ).with_for_update().count()

            if personalized_count >= cap:
                items_query = _db.query(ProfileSkript).filter_by(
                    profile_id=profile_id, is_personalized=True
                ).order_by(ProfileSkript.created_at.asc()).all()

                now = datetime.datetime.utcnow()
                items_list = []
                for item in items_query:
                    weeks_old = 0
                    if item.created_at:
                        delta = now - item.created_at
                        weeks_old = int(delta.days / 7)
                    items_list.append({
                        'id': item.id,
                        'name': item.name or '',
                        'created_at': str(item.created_at) if item.created_at else '',
                        'firmenname': (item.name or '')[:20],
                        'weeks_old': weeks_old,
                    })
                cap_exceeded_response = items_list
                raise _CapExceeded()
```

**Schritt 5 — Delete-Phase IMMER gegen ProfileSkript:**
```python
            for item_id in delete_ids:
                item = _db.query(ProfileSkript).filter_by(
                    id=item_id,
                    profile_id=profile_id,
                    is_personalized=True
                ).first()
                if not item:
                    raise ValueError(f"Item {item_id} not found or not personalized")
                firmenname_hint = (item.name or '')[:20]
                print(
                    f"[DSGVO-Audit] User-Aktion: personalisiertes Skript gelöscht zur Cap-Befreiung "
                    f"(item_id={item.id}, firmenname={firmenname_hint}, erstellt={item.created_at})"
                )
                _db.delete(item)
```

**Schritt 6 — Insert IMMER als ProfileSkript (auch Cold-Call-Pfad):**
```python
            from database.models import ProfileSkript
            new_item = ProfileSkript(
                profile_id=profile_id,
                name=new_name,
                inhalt=personalized_text,
                parent_id=original.id,          # Quell-Item ID (ProfileSkript oder ProfileOpener)
                is_personalized=True,
                briefing_source_firma=firmenname or None,
                sortierung=0,
            )
            _db.add(new_item)
            _db.flush()
            new_item_id = new_item.id
            _db.commit()
```

WICHTIG: ProfileSkript hat KEIN `type`-Column — dieses Argument beim Insert weglassen. ProfileSkript.sortierung ist vorhanden. created_at via default=utcnow automatisch.

ABSCHLUSS-GREP nach dem Edit:
```bash
grep -n "new_item = ProfileOpener" routes/app_routes.py
```
→ 0 Treffer in api_personalize_skript_save (kein ProfileOpener-Insert mehr).

PFLICHT: Org-Isolation-Zeile `_db.query(Profile).filter_by(id=profile_id, org_id=g.org.id)` UNVERÄNDERT lassen.
  </action>
  <verify>
    <automated>grep -n "new_item = ProfileOpener\|new_item = ProfileSkript\|is_personalized" routes/app_routes.py | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "new_item = ProfileSkript" routes/app_routes.py` → mindestens 1 Treffer in api_personalize_skript_save
    - `grep -n "new_item = ProfileOpener" routes/app_routes.py` → 0 Treffer in api_personalize_skript_save
    - `grep -n "call_mode" routes/app_routes.py | grep -v "#"` → mindestens 2 Treffer in api_personalize_skript_save (call_mode wird ausgewertet)
    - `grep -n "ProfileSkript.*is_personalized\|is_personalized.*ProfileSkript" routes/app_routes.py | grep -v "#"` → mindestens 2 Treffer (cap-check filter_by + insert)
    - `grep -n "type=.opener." routes/app_routes.py | grep "ProfileSkript"` → 0 Treffer (kein type-Arg beim ProfileSkript-Insert)
    - `grep -n "/api/launcher" routes/app_routes.py` → Routen vorhanden, unberührt
    - `python -c "from routes.app_routes import app_routes_bp; print('OK')"` → kein Error
  </acceptance_criteria>
  <done>api_personalize_skript_save() routet per call_mode: 'meeting' → original aus ProfileSkript; 'cold_call' → original aus ProfileOpener. Cap-Check + Delete + Insert IMMER gegen ProfileSkript. Kein ProfileOpener-Insert. Transaktions-Struktur + Org-Isolation erhalten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Input | call_mode, skript_id, opener_id kommen vom Browser — müssen validiert werden |
| DB Isolation | ProfileSkript.profile_id + Profile.org_id gegen aktive Session geprüft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.5.6-02-01 | Elevation | api_personalize_skript: skript_id aus anderem Profil | mitigate | filter_by(id=skript_id, profile_id=profile_id) + Profile.org_id-Check — kein Cross-Profile-Zugriff |
| T-08.19.5.6-02-02 | Elevation | api_personalize_skript: opener_id aus anderem Profil (cold_call) | mitigate | filter_by(id=opener_id, profile_id=profile_id) + Profile.org_id-Check — identischer Schutz |
| T-08.19.5.6-02-03 | Spoofing | call_mode-Manipulation (Client sendet 'meeting' statt 'cold_call') | accept | call_mode steuert nur welches Modell geladen wird; Org-Isolation + profile_id-Filter verhindern Cross-Profil-Zugriff unabhängig von call_mode |
| T-08.19.5.6-02-04 | Tampering | api_personalize_skript_save: Cap-Bypass via Race | accept | with_for_update() auf SQLite ist no-op; Race-Window existiert aber Cap ist soft-limit (UX nicht sicherheitskritisch) |
| T-08.19.5.6-02-05 | Tampering | delete_ids aus anderem Profil | mitigate | delete_ids-Schleife filtert by (profile_id, is_personalized=True) — kein Cross-Profile-Delete |
</threat_model>

<verification>
Nach Abschluss aller 4 Tasks:

```bash
# 1. ProfileSkript hat neue Columns
grep -A 12 "class ProfileSkript" database/models.py | grep "is_personalized\|parent_id\|briefing_source_firma"
# → 3 Treffer

# 2. ProfileSkript.parent_id hat kein ForeignKey
grep -A 12 "class ProfileSkript" database/models.py | grep "ForeignKey.*parent_id\|parent_id.*ForeignKey"
# → 0 Treffer

# 3. Migration vorhanden
grep -n "profile_skripte" app.py | grep "ADD COLUMN"
# → 3 Treffer

# 4. call_mode-Routing in beiden Endpoints
grep -n "call_mode" routes/app_routes.py | grep -v "#"
# → mindestens 4 Treffer (2 Endpoints × 2 Stellen)

# 5. Meeting-Pfad lädt ProfileSkript
grep -n "ProfileSkript" routes/app_routes.py | grep "filter_by.*skript_id\|skript_id.*filter_by"
# → mindestens 1 Treffer

# 6. Cold-Call-Pfad lädt ProfileOpener (für KI-Input)
grep -n "ProfileOpener" routes/app_routes.py | grep "filter_by.*opener_id\|opener_id.*filter_by"
# → mindestens 1 Treffer in api_personalize_skript

# 7. Insert immer als ProfileSkript
grep -n "new_item = ProfileSkript" routes/app_routes.py
# → 1 Treffer in api_personalize_skript_save

grep -n "new_item = ProfileOpener" routes/app_routes.py
# → 0 Treffer

# 8. Launcher-Routen unberührt
git diff routes/app_routes.py | grep "^+" | grep "api/launcher/init\|api/launcher/profile"
# → 0 Treffer

# 9. generate_personalized_skript unverändert
grep -n "def generate_personalized_skript" services/precall_service.py
# → 1 Treffer, Signatur mit opener_inhalt-Parameter

# 10. Python-Syntax OK
python -c "from routes.app_routes import app_routes_bp; from database.models import ProfileSkript; print('OK')"
# → 'OK'
```
</verification>

<success_criteria>
- ProfileSkript.is_personalized, .parent_id (nullable, KEIN FK), .briefing_source_firma in models.py
- app.py _migrate() enthält ALTER TABLE profile_skripte ADD COLUMN für alle 3 Felder (idempotent)
- api_personalize_skript(): call_mode 'meeting' → lädt ProfileSkript per skript_id; 'cold_call' → lädt ProfileOpener per opener_id; Fallback ohne call_mode → skript_id-Präsenz entscheidet
- api_personalize_skript_save(): call_mode-Routing für Original-Lookup; Cap-Check + Insert IMMER gegen ProfileSkript; Kein ProfileOpener-Insert
- generate_personalized_skript() in precall_service.py UNVERÄNDERT (Signatur + Körper)
- /api/launcher/init + /api/launcher/profile/<pid> UNVERÄNDERT (git diff zeigt keine Änderung)
- Python-Import OK (kein SyntaxError, kein ImportError)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-02-SUMMARY.md` erstellen.

SUMMARY enthält:
- models.py: neue ProfileSkript-Columns (Vorher/Nachher, konkrete Zeilen)
- app.py: Migration-Block (Zeilen-Referenz)
- app_routes.py: api_personalize_skript + api_personalize_skript_save — call_mode-Routing-Logik als diff-Zusammenfassung
- Verifikations-Grep-Ergebnisse
- Etwaige Abweichungen

Format: @$HOME/.claude/get-shit-done/templates/summary.md
</output>
