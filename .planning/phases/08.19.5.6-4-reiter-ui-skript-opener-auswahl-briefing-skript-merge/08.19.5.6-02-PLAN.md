---
phase: 08.19.5.6
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - app.py
  - routes/app_routes.py
autonomous: true
requirements:
  - R8

must_haves:
  truths:
    - "ProfileSkript hat is_personalized, parent_id, briefing_source_firma Columns"
    - "DB-Migration für profile_skripte (3 neue Columns) in app.py vorhanden"
    - "api_personalize_skript() akzeptiert skript_id, lädt ProfileSkript, gibt personalisierten Text zurück"
    - "api_personalize_skript_save() speichert Ergebnis als ProfileSkript-Row mit is_personalized=True"
    - "KEIN neues ProfileOpener-Row wird in api_personalize_skript_save() erstellt"
    - "Cap-Check in api_personalize_skript_save() läuft gegen ProfileSkript.is_personalized"
    - "generate_personalized_skript() Funktion in precall_service.py UNVERÄNDERT"
    - "/api/launcher/init und /api/launcher/profile/<pid> Routen UNVERÄNDERT"
  artifacts:
    - path: "database/models.py"
      provides: "ProfileSkript mit is_personalized + parent_id + briefing_source_firma Columns"
    - path: "app.py"
      provides: "Migration für profile_skripte.is_personalized, .parent_id, .briefing_source_firma"
    - path: "routes/app_routes.py"
      provides: "api_personalize_skript() + api_personalize_skript_save() auf ProfileSkript umgestellt"
  key_links:
    - from: "api_personalize_skript()"
      to: "ProfileSkript"
      via: "_db.query(ProfileSkript).filter_by(id=skript_id, profile_id=profile_id)"
      pattern: "skript_id.*ProfileSkript"
    - from: "api_personalize_skript_save()"
      to: "ProfileSkript"
      via: "new_skript = ProfileSkript(profile_id=..., is_personalized=True)"
      pattern: "ProfileSkript.*is_personalized"
---

<objective>
Wave 1 Plan B: Backend-Umstellung der KI-Personalisierungs-Endpoints auf ProfileSkript.

Zweck: /api/precall/personalize und /api/precall/personalize/save akzeptieren jetzt skript_id statt opener_id. Das Ergebnis wird als ProfileSkript (is_personalized=True) gespeichert statt als ProfileOpener. ProfileSkript-Modell bekommt die dafür nötigen Columns. generate_personalized_skript() in precall_service.py bleibt unverändert (nimmt ohnehin generischen Text-Input).

Output: models.py mit is_personalized/parent_id/briefing_source_firma auf ProfileSkript. Migration in app.py. Beide Endpoints auf skript_id/ProfileSkript umgestellt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-SPEC.md
@.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-CONTEXT.md

<interfaces>
<!-- ProfileSkript aktuell (models.py:138-145) — KEIN is_personalized, parent_id, briefing_source_firma -->
```python
class ProfileSkript(Base):
    __tablename__ = 'profile_skripte'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)
```

<!-- Nach diesem Plan: 3 neue Columns auf ProfileSkript -->
```python
    parent_id             = Column(Integer, nullable=True)                     # D-04: kein FK-Constraint
    is_personalized       = Column(Boolean, default=False, nullable=False)
    briefing_source_firma = Column(String(200), nullable=True)
```

<!-- Migrations-Pattern (app.py:405-416 — Referenz für profile_opener) -->
```python
        for col, typedef in [
            ('parent_id',             'INTEGER'),
            ('is_personalized',       'BOOLEAN DEFAULT 0'),
            ('briefing_source_firma', 'VARCHAR(200)'),
        ]:
            try:
                conn.execute(text(f'ALTER TABLE profile_opener ADD COLUMN {col} {typedef}'))
                conn.commit()
                print(f"[DB] Migration: added profile_opener.{col}")
            except Exception:
                pass
```
Gleiche Struktur für profile_skripte — nur Tabellenname ändert.

<!-- generate_personalized_skript Signatur (precall_service.py:517) — NICHT ÄNDERN -->
```python
def generate_personalized_skript(briefing_dict, opener_inhalt, profil_daten, user_id):
```
Parameter-Name `opener_inhalt` bleibt — nur der Wert (skript.inhalt statt opener.inhalt) ändert sich im Call.

<!-- PERSONALIZED_SCRIPTS_CAP (config.py) — gleicher CAP für ProfileSkript -->
Same constant, same cap number — nur gegen ProfileSkript.is_personalized geprüft.

<!-- Cap-Response Format — bleibt identisch -->
{'cap_exceeded': True, 'items': [{'id': ..., 'name': ..., 'weeks_old': ...}]}

<!-- R7: Launcher-Init-Routen dürfen NICHT geändert werden -->
/api/launcher/init  (app_routes.py ~840)
/api/launcher/profile/<pid>  (app_routes.py ~850)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileSkript — 3 neue Columns in models.py</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py Zeilen 138-145 (ProfileSkript aktuell)
    - database/models.py Zeilen 162-173 (ProfileOpener als Referenz für is_personalized/parent_id/briefing_source_firma)
  </read_first>
  <action>
Nach `created_at = Column(DateTime, default=utcnow)` in ProfileSkript (Zeile 145) drei neue Columns einfügen:

**Aktuell (Zeilen 138-145):**
```python
class ProfileSkript(Base):
    __tablename__ = 'profile_skripte'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)
```

**Nach Edit:**
```python
class ProfileSkript(Base):
    __tablename__ = 'profile_skripte'
    id          = Column(Integer, primary_key=True)
    profile_id  = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    name        = Column(String(200), nullable=False)
    inhalt      = Column(Text)
    sortierung  = Column(Integer, default=0)
    created_at  = Column(DateTime, default=utcnow)
    parent_id             = Column(Integer, nullable=True)            # D-04: kein FK, Self-Ref ohne Constraint
    is_personalized       = Column(Boolean, default=False, nullable=False)
    briefing_source_firma = Column(String(200), nullable=True)
```

Kein ForeignKey auf parent_id — per D-04: SQLite enforced FKs nur bei PRAGMA foreign_keys=ON (Flask-Default OFF). Column bleibt plain Integer nullable.
  </action>
  <verify>
    <automated>grep -A 12 "class ProfileSkript" database/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "is_personalized" database/models.py` → mindestens 2 Treffer (ProfileOpener + ProfileSkript)
    - `grep -n "parent_id" database/models.py` → mindestens 2 Treffer (ProfileOpener + ProfileSkript)
    - `grep -n "briefing_source_firma" database/models.py` → mindestens 2 Treffer (ProfileOpener + ProfileSkript)
    - ProfileSkript.parent_id hat KEIN ForeignKey-Argument (`grep "ForeignKey.*profile_skripte\|ForeignKey.*profile_opener.*ProfileSkript" database/models.py` → 0 Treffer in ProfileSkript-Klasse)
    - `python -c "from database.models import ProfileSkript; print(ProfileSkript.__table__.columns.keys())"` → enthält 'is_personalized', 'parent_id', 'briefing_source_firma'
  </acceptance_criteria>
  <done>ProfileSkript hat is_personalized, parent_id (ohne FK), briefing_source_firma als neue Columns.</done>
</task>

<task type="auto">
  <name>Task 2: DB-Migration für profile_skripte in app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py Zeilen 400-420 (bestehende Migration für profile_opener als Referenz-Pattern)
    - app.py Zeilen 122-130 (def _migrate() Kopf — Verbindungs-Kontext verstehen)
  </read_first>
  <action>
Nach dem bestehenden Migrations-Block für profile_opener (Zeilen 405-416) einen identischen Block für profile_skripte einfügen.

**Nach Zeile 416 (`pass  # Idempotent: duplicate ALTER TABLE fails silently`) einfügen:**

```python
        # ── Phase 08.19.5.6: is_personalized + parent_id + briefing_source_firma für ProfileSkript ──
        for col, typedef in [
            ('parent_id',             'INTEGER'),
            ('is_personalized',       'BOOLEAN DEFAULT 0'),
            ('briefing_source_firma', 'VARCHAR(200)'),
        ]:
            try:
                conn.execute(text(f'ALTER TABLE profile_skripte ADD COLUMN {col} {typedef}'))
                conn.commit()
                print(f"[DB] Migration: added profile_skripte.{col}")
            except Exception:
                pass  # Idempotent: duplicate ALTER TABLE fails silently
```

WICHTIG: Den Block exakt an dieser Stelle einfügen — nach dem profile_opener-Block, BEVOR dem nächsten Kommentar (`# ── Phase 04.9: Seed 6 system personality types`). Der Kontext (conn-Objekt, text-Import) ist in diesem Scope verfügbar.
  </action>
  <verify>
    <automated>grep -n "profile_skripte.*ADD COLUMN\|added profile_skripte" app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "profile_skripte" app.py` → mindestens 3 Treffer (je eine ALTER TABLE Zeile für die 3 Columns)
    - `grep -n "added profile_skripte" app.py` → 3 Print-Zeilen (is_personalized, parent_id, briefing_source_firma)
    - Migration-Block steht nach dem profile_opener-Block und vor dem Seed-Block
    - `python -c "from app import app; print('OK')"` → kein SyntaxError (Import-Test)
  </acceptance_criteria>
  <done>app.py enthält Migration für profile_skripte (is_personalized, parent_id, briefing_source_firma) mit idempotenter try/except-Struktur.</done>
</task>

<task type="auto">
  <name>Task 3: api_personalize_skript() auf skript_id/ProfileSkript umstellen</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py Zeilen 1040-1107 (api_personalize_skript() vollständig lesen)
    - services/precall_service.py (grep: "def generate_personalized_skript" — Signatur verifizieren)
    - database/models.py (grep: "class ProfileSkript" — neue Columns bestätigen)
  </read_first>
  <action>
api_personalize_skript() komplett umschreiben — nur die Logik, nicht die Route-Dekoratoren oder Docstring-Struktur.

**Aktuell (Zeilen 1044-1107):** Lädt ProfileOpener per opener_id, ruft generate_personalized_skript(opener_inhalt=opener.inhalt) auf.

**Neu:** Lädt ProfileSkript per skript_id, ruft generate_personalized_skript(opener_inhalt=skript.inhalt) auf.

**Konkreter Diff — nur die Innereien der Funktion:**

1. Import-Zeile ändern (Zeile ~1054-1056):
```python
# ALT:
    from database.models import ProfileOpener, Profile

# NEU:
    from database.models import ProfileSkript, Profile
```

2. Validierung ändern (Zeile ~1062-1067):
```python
# ALT:
    try:
        opener_id = int(data.get('opener_id'))
        if opener_id <= 0:
            raise ValueError
    except (TypeError, ValueError):
        return jsonify({'error': 'opener_id muss eine positive Ganzzahl sein'}), 400

# NEU:
    try:
        skript_id = int(data.get('skript_id'))
        if skript_id <= 0:
            raise ValueError
    except (TypeError, ValueError):
        return jsonify({'error': 'skript_id muss eine positive Ganzzahl sein'}), 400
```

3. DB-Query ändern (Zeile ~1076-1094):
```python
# ALT:
        opener = _db.query(ProfileOpener).filter_by(
            id=opener_id, profile_id=profile_id
        ).first()
        if not opener:
            return jsonify({'error': 'Opener nicht gefunden'}), 400
        ...
        opener_inhalt = opener.inhalt or ''

# NEU:
        skript = _db.query(ProfileSkript).filter_by(
            id=skript_id, profile_id=profile_id
        ).first()
        if not skript:
            return jsonify({'error': 'Skript nicht gefunden'}), 400
        ...
        opener_inhalt = skript.inhalt or ''
```

4. generate_personalized_skript() Call bleibt identisch (opener_inhalt Parameter-Name bleibt):
```python
    personalized_text, error = generate_personalized_skript(
        briefing_dict=briefing_dict,
        opener_inhalt=opener_inhalt,    # GLEICH — nur der Wert (skript.inhalt) ändert sich
        profil_daten=profil_daten,
        user_id=user_id,
    )
```

SICHERHEITS-CHECK: Org-Isolation bleibt erhalten — `profile.org_id != g.org.id` Check auf Profile bleibt unverändert. ProfileSkript ist über profile_id an das richtige Profile gebunden.

PFLICHT: `grep -n "def api_launcher\|/api/launcher" routes/app_routes.py` — diese Routen NIE ANFASSEN (R7).
  </action>
  <verify>
    <automated>grep -n "opener_id\|skript_id\|ProfileOpener\|ProfileSkript" routes/app_routes.py | grep -A2 -B2 "api_personalize_skript"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "opener_id" routes/app_routes.py | grep "api_personalize_skript\|skript_id"` → 0 Treffer (opener_id nicht mehr in api_personalize_skript)
    - `grep -n "skript_id" routes/app_routes.py` → mindestens 2 Treffer in api_personalize_skript() + api_personalize_skript_save()
    - `grep -n "ProfileSkript" routes/app_routes.py` → Treffer in beiden Endpoint-Funktionen
    - `grep -n "ProfileOpener" routes/app_routes.py | grep "def api_personalize"` → 0 Treffer (ProfileOpener nicht mehr in Personalisierungs-Endpoints)
    - `grep -n "/api/launcher/init\|/api/launcher/profile" routes/app_routes.py` → Routen vorhanden, unverändert (git diff zeigt keine Änderung dort)
    - `python -c "from routes.app_routes import app_routes_bp; print('OK')"` → kein ImportError
  </acceptance_criteria>
  <done>api_personalize_skript() akzeptiert skript_id, lädt ProfileSkript, übergibt skript.inhalt als opener_inhalt an generate_personalized_skript(). Org-Isolation + profile_id-Filter erhalten.</done>
</task>

<task type="auto">
  <name>Task 4: api_personalize_skript_save() auf skript_id/ProfileSkript umstellen</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py Zeilen 1110-1230 (api_personalize_skript_save() vollständig lesen — Atomic-Transaction-Pattern, Cap-Check, Delete+Insert-Logik)
    - config.py (grep: "PERSONALIZED_SCRIPTS_CAP" — Konstante verifizieren)
  </read_first>
  <action>
api_personalize_skript_save() auf ProfileSkript umstellen. Die Transaktions- und Cap-Logik bleibt strukturell identisch — nur Modell-Klasse, Field-Name und Tabellenreferenz ändern.

**Import-Zeile (Zeile ~1127-1129):**
```python
# ALT:
    from database.models import ProfileOpener, Profile

# NEU:
    from database.models import ProfileSkript, Profile
```

**Validierung skript_id (Zeile ~1138-1144):**
```python
# ALT:
    try:
        opener_id = int(data.get('opener_id'))
        if opener_id <= 0:
            raise ValueError
    except (TypeError, ValueError):
        return jsonify({'error': 'opener_id muss eine positive Ganzzahl sein'}), 400

# NEU:
    try:
        skript_id = int(data.get('skript_id'))
        if skript_id <= 0:
            raise ValueError
    except (TypeError, ValueError):
        return jsonify({'error': 'skript_id muss eine positive Ganzzahl sein'}), 400
```

**Org-Isolation-Check + Original-Lookup (Zeile ~1157-1167):**
```python
# ALT:
        original = _db.query(ProfileOpener).filter_by(
            id=opener_id, profile_id=profile_id
        ).first()
        if not original:
            return jsonify({'error': 'Opener nicht gefunden'}), 400

# NEU:
        original = _db.query(ProfileSkript).filter_by(
            id=skript_id, profile_id=profile_id
        ).first()
        if not original:
            return jsonify({'error': 'Skript nicht gefunden'}), 400
```

**Cap-Check-Query (Zeile ~1183-1186):**
```python
# ALT:
                personalized_count = _db.query(ProfileOpener).filter_by(
                    profile_id=profile_id, is_personalized=True
                ).with_for_update().count()

# NEU:
                personalized_count = _db.query(ProfileSkript).filter_by(
                    profile_id=profile_id, is_personalized=True
                ).with_for_update().count()
```

**Cap-Items-Query (Zeile ~1190-1192):**
```python
# ALT:
                    items_query = _db.query(ProfileOpener).filter_by(
                        profile_id=profile_id, is_personalized=True
                    ).order_by(ProfileOpener.created_at.asc()).all()

# NEU:
                    items_query = _db.query(ProfileSkript).filter_by(
                        profile_id=profile_id, is_personalized=True
                    ).order_by(ProfileSkript.created_at.asc()).all()
```

**Delete-Query (suche nach `filter_by(profile_id=profile_id, id=did)` oder ähnlichem Delete-Block in delete_ids-Schleife):**
```python
# ALT: _db.query(ProfileOpener).filter_by(...).delete()
# NEU: _db.query(ProfileSkript).filter_by(...).delete()
```

**Insert (neues Item erstellen):**
```python
# ALT:
            new_item = ProfileOpener(
                profile_id=profile_id,
                name=new_name,
                inhalt=personalized_text,
                type='opener',             # ← ENTFÄLLT (ProfileSkript hat kein type-Column)
                parent_id=original.id,
                is_personalized=True,
                briefing_source_firma=firmenname or None,
                sortierung=0,
            )

# NEU:
            new_item = ProfileSkript(
                profile_id=profile_id,
                name=new_name,
                inhalt=personalized_text,
                parent_id=original.id,
                is_personalized=True,
                briefing_source_firma=firmenname or None,
                sortierung=0,
            )
```

WICHTIG: ProfileSkript hat KEIN `type`-Column — dieses Argument weglassen. ProfileSkript.sortierung ist vorhanden (models.py Zeile 144). ProfileSkript.created_at wird via default=utcnow automatisch gesetzt.

**name-Variable (bleibt identisch — original ist jetzt ProfileSkript, original.name ist vorhanden):**
```python
        new_name = f"{firma_display} — {original.name} (personalisiert, {today_str})"
```
Diese Zeile braucht KEINE Änderung.

**Return-Wert bleibt identisch:**
```python
        return jsonify({'item_id': new_item_id, 'ok': True})
```

SICHERHEITS-CHECK: Org-Isolation `_db.query(Profile).filter_by(id=profile_id, org_id=g.org.id)` bleibt UNVERÄNDERT — diese Zeile nicht anfassen.

ABSCHLUSS-GREP: Nach dem Edit ausführen:
```bash
grep -n "ProfileOpener" routes/app_routes.py
```
→ api_personalize_skript_save() darf KEINEN ProfileOpener-Import oder -Query mehr enthalten.
  </action>
  <verify>
    <automated>grep -n "ProfileOpener\|ProfileSkript\|skript_id\|opener_id" routes/app_routes.py | grep -v "^.*#\|api_launcher\|launcher_init" | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "opener_id" routes/app_routes.py | grep -v "#\|launcher"` → 0 Treffer (opener_id nur noch in Kommentaren oder launcher-Routen)
    - `grep -n "new_item = ProfileSkript" routes/app_routes.py` → 1 Treffer in api_personalize_skript_save
    - `grep -n "new_item = ProfileOpener" routes/app_routes.py` → 0 Treffer in api_personalize_skript_save (evtl. noch in anderen Kontexten OK)
    - `grep -n "ProfileSkript.*is_personalized\|is_personalized.*ProfileSkript" routes/app_routes.py` → mindestens 2 Treffer (cap-check + insert)
    - `grep -n "type='opener'" routes/app_routes.py | grep "ProfileSkript"` → 0 Treffer (kein type-Arg beim ProfileSkript-Insert)
    - `grep -n "/api/launcher" routes/app_routes.py` → Routen vorhanden, unberührt
    - Smoke-Test: `python -c "from routes.app_routes import app_routes_bp; print('OK')"` → kein Error
  </acceptance_criteria>
  <done>api_personalize_skript_save() speichert Ergebnis als ProfileSkript(is_personalized=True). Cap-Check läuft gegen ProfileSkript. Kein ProfileOpener-Insert mehr. Transaktions-Struktur + Org-Isolation erhalten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Input | skript_id kommt vom Browser — muss als positive Integer validiert werden |
| DB Isolation | ProfileSkript.profile_id muss gegen aktive session profile_id geprüft werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.5.6-02-01 | Elevation | api_personalize_skript: skript_id aus anderem Profil | mitigate | filter_by(id=skript_id, profile_id=profile_id) + Org-Check auf Profile — kein Cross-Profile-Zugriff möglich |
| T-08.19.5.6-02-02 | Tampering | api_personalize_skript_save: Cap-Bypass via Race | accept | with_for_update() auf SQLite ist no-op; Race-Window existiert aber Cap ist soft-limit (UX nicht sicherheitskritisch) |
| T-08.19.5.6-02-03 | Tampering | delete_ids aus anderem Profil | mitigate | delete_ids-Schleife filtert by profile_id — kein Cross-Profile-Delete |
</threat_model>

<verification>
Nach Abschluss aller 4 Tasks:

```bash
# 1. ProfileSkript hat neue Columns
grep -A 15 "class ProfileSkript" database/models.py | grep "is_personalized\|parent_id\|briefing_source_firma"
# → 3 Treffer

# 2. Migration vorhanden
grep -n "profile_skripte" app.py
# → mindestens 3 Treffer

# 3. Endpoints auf skript_id umgestellt
grep -n "skript_id" routes/app_routes.py | grep -v "#"
# → mindestens 4 Treffer (je 2 pro Endpoint)

# 4. Kein opener_id mehr in Personalisierungs-Endpoints
grep -n "opener_id" routes/app_routes.py | grep -v "#\|launcher"
# → 0 Treffer

# 5. Launcher-Routen unberührt
git diff routes/app_routes.py | grep "^+" | grep "api/launcher/init\|api/launcher/profile"
# → 0 Treffer (keine Änderung an diesen Routen)

# 6. Python-Syntax OK
python -c "from routes.app_routes import app_routes_bp; from database.models import ProfileSkript; print('OK')"
# → 'OK'

# 7. generate_personalized_skript unverändert
grep -n "def generate_personalized_skript" services/precall_service.py
# → 1 Treffer, Signatur identisch zu vor dem Plan
```
</verification>

<success_criteria>
- ProfileSkript.is_personalized, .parent_id (nullable, kein FK), .briefing_source_firma in models.py
- app.py _migrate() enthält ALTER TABLE profile_skripte ADD COLUMN für alle 3 Felder
- api_personalize_skript(): akzeptiert skript_id, lädt ProfileSkript, gibt personalisierten Text zurück (kein 400/500 bei gültiger skript_id)
- api_personalize_skript_save(): skript_id, Cap gegen ProfileSkript, Insert als ProfileSkript(is_personalized=True)
- Kein neues ProfileOpener-Row in Personalisierungs-Endpoints
- generate_personalized_skript() in precall_service.py UNVERÄNDERT
- /api/launcher/init + /api/launcher/profile/<pid> UNVERÄNDERT (git diff zeigt keine Änderung)
- Python-Import OK (kein SyntaxError, kein ImportError)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-02-SUMMARY.md` erstellen.

SUMMARY enthält:
- models.py: neue ProfileSkript-Columns (Vorher/Nachher)
- app.py: Migration-Block (Zeilen-Referenz)
- app_routes.py: api_personalize_skript + api_personalize_skript_save (diff-Zusammenfassung)
- Verifikations-Grep-Ergebnisse
- Etwaige Abweichungen

Format: @$HOME/.claude/get-shit-done/templates/summary.md
</output>
