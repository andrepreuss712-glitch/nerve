---
phase: 08.19.5.6
plan: 01
type: execute
wave: 1
depends_on: []
deploy_sync: "PFLICHT — Plan 01 + Plan 02 müssen ATOMAR deployed werden (gleicher Git-Commit). Plan 01 sendet skript_id/opener_id + call_mode; Plan 02 liefert modus-abhängige Backend-Logik. Getrennt deployed → 400 Bad Request auf Personalisierung."
files_modified:
  - static/pip-launcher.js
autonomous: true
requirements:
  - R1
  - R2
  - R3
  - R4
  - R5
  - R6
  - R7
  - R9

must_haves:
  truths:
    - "renderStep5() zeigt 4 horizontale Tabs: Opener, Erlaubnisfrage, Pitch, Skript (fcd-tab-Klassen)"
    - "state.activeTab persistiert durch renderStep5()-Rerender; Tab-Wechsel ändert nur sichtbaren Content"
    - "Opener/Erlaubnisfrage/Pitch-Tabs filtern state.openerItems nach o.type; Skript-Tab nutzt state.skripte"
    - "Leerer Tab zeigt 'Noch kein [Typ] hinterlegt' mit Link auf /profiles"
    - "Fenster-Titel ist 'Gesprächsvorbereitung' — 'Skript & Opener wählen' nicht mehr vorhanden"
    - "Vorwissen-Picker + Anrede-Toggle erscheinen IMMER in Step 5, unabhängig von state.precallBriefing"
    - "Personalisieren-Button erscheint MODUS-ABHÄNGIG: nur im Skript-Tab wenn state.mode==='meeting'; nur im Opener-Tab wenn state.mode==='cold_call' (oder kein mode)"
    - "Kein neues Hex-Literal (#xxxxxx) in der Tab-UI-HTML-Strings eingeführt"
    - "renderStep4b sendet {call_mode: state.mode, skript_id: ...} wenn meeting ODER {call_mode: state.mode, opener_id: ...} wenn cold_call"
    - "renderStep4c liest originalText aus state.skripte (meeting) ODER state.openerItems (cold_call)"
    - "_savePersonalizedAndStartCall + cap-modal senden call_mode + den passenden skript_id oder opener_id"
    - "4 separate Selection-State-Variablen: state.selectedOpenerId (Opener-Tab), state.selectedErlaubnisId (Erlaubnisfrage-Tab), state.selectedPitchId (Pitch-Tab), state.selectedSkriptId (Skript-Tab)"
    - "_collectEditedTexts() liest aktiven Reiter-Textarea (lnr-opener-textarea ODER lnr-skript-textarea) und speichert in state._editedOpenerText bzw. state._editedSkriptText"
    - "startCall() nutzt selectedSkriptId wenn gesetzt (Skript-Priorität), sonst selectedPitchId als Fallback (OD-01 Option A)"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "renderStep5() als 4-Tab-UI + switchTab5() file-scope function + state.activeTab + 4 tab-spezifische Selection-Variablen + modus-abhängige Personalisierung"
  key_links:
    - from: "window.switchTab5(tab)"
      to: "renderStep5()"
      via: "state.activeTab = tab; renderStep5();"
      pattern: "function switchTab5"
    - from: "Skript-Tab Personalisieren-Button (meeting)"
      to: "renderStep4b/4c"
      via: "state.selectedSkriptId + state.briefingModus = 'C' + call_mode='meeting'"
      pattern: "lnr-step5-personalize"
    - from: "Opener-Tab Personalisieren-Button (cold_call)"
      to: "renderStep4b/4c"
      via: "state.selectedOpenerId + state.briefingModus = 'C' + call_mode='cold_call'"
      pattern: "lnr-step5-personalize"
    - from: "renderStep4b"
      to: "api/precall/personalize"
      via: "{call_mode: state.mode, skript_id/opener_id: ...}"
      pattern: "call_mode.*state.mode"
    - from: "startCall()"
      to: "_openPipAndMic()"
      via: "state._resolvedTeleprompterSkript = {id, inhalt} gesetzt vor close(); _openPipAndMic() liest + löscht es"
      pattern: "_resolvedTeleprompterSkript"
---

<objective>
Wave 1 Plan A — v3: Frontend-Umbau renderStep5() zu 4-Tab-UI (Opener / Erlaubnisfrage / Pitch / Skript) mit modus-abhängiger Personalisierung.

Zweck: Das flache Auswahl-Fenster in pip-launcher.js Step 5 auf eine klare Tab-Struktur umstellen. ProfileOpener-Items nach type trennen. Vorwissen-Picker immer sichtbar. Personalisierungs-Flow modus-abhängig: Cold Call → Personalisieren-Button im Opener-Tab, sendet opener_id; Meeting → Button im Skript-Tab, sendet skript_id. Beide Pfade senden call_mode. startCall() priorisiert selectedSkriptId > selectedPitchId (OD-01 Option A).

**ATOMIC DEPLOY (Cross-AI-Finding #1):** Dieser Plan MUSS im selben Git-Commit wie Plan 02 committed und deployed werden. Plan 01 sendet call_mode + skript_id/opener_id; Plan 02 routet diese modus-abhängig im Backend.

**4 separate State-Variablen (Andre-Decision):** Jeder Reiter hat eine eigene Selection-State-Variable. state.selectedOpenerId (Opener), state.selectedErlaubnisId (Erlaubnisfrage), state.selectedPitchId (Pitch), state.selectedSkriptId (Skript). Beim Call-Start werden alle 4 Auswahlen genutzt.

**MODUS-ABHÄNGIGE PERSONALISIERUNG (NEU v3 — Andre-Decision Architektur-Erweiterung):**
- state.mode === 'cold_call': Personalisieren-Button NUR im Opener-Tab. renderStep4b sendet {opener_id: state.selectedOpenerId, call_mode: 'cold_call', briefing: {...}}. renderStep4c liest originalText aus state.openerItems. Ergebnis → _editedOpenerText.
- state.mode === 'meeting': Personalisieren-Button NUR im Skript-Tab. renderStep4b sendet {skript_id: state.selectedSkriptId, call_mode: 'meeting', briefing: {...}}. renderStep4c liest originalText aus state.skripte. Ergebnis → _editedSkriptText.
- WICHTIG: state.mode im Code = 'cold_call' (nicht 'cold') und 'meeting' — verifiziert aus pip-launcher.js:29.

**PITCH + SKRIPT PRIORITÄT (OD-01 → Option A):** startCall() erhält eine Prioritätsprüfung: wenn selectedSkriptId gesetzt → Skript-Text als Teleprompter, gespeichert in state._resolvedTeleprompterSkript. _openPipAndMic() liest dieses Property zuerst, bevor es eigene Auflösung macht. Wenn nur selectedPitchId gesetzt → Pitch-Inhalt. Wenn weder noch → nur Opener + Erlaubnisfrage.

**_collectEditedTexts() (Cross-AI-Finding #2):** In Task 4 explizit angepasst. Liest aktiven Reiter-Textarea und setzt `&& skTa.value` Guard.

Output: pip-launcher.js mit state.activeTab, 4 tab-spezifischen Selection-Variablen, window.switchTab5(), komplett umgebautem renderStep5(), modus-abhängiger Personalisierungs-Logik, aktualisiertem _collectEditedTexts(), Pitch/Skript-Priorität in startCall() + _openPipAndMic().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-SPEC.md
@.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-CONTEXT.md

<interfaces>
<!-- fcd-tab / fcd-tabs Pattern (templates/admin/dashboard.html:19-25) -->
```html
<nav class="fcd-tabs" role="tablist">
  <button class="fcd-tab" data-tab="uebersicht">Übersicht</button>
  <button class="fcd-tab active" data-tab="einnahmen">...</button>
</nav>
```
Im pip-launcher.js: kein data-tab, onclick direkt: `onclick="window.switchTab5('opener')"`.
Active-Klasse: `class="fcd-tab${state.activeTab==='opener' ? ' active' : ''}"`.

<!-- State-Properties (pip-launcher.js:27-70) — VERIFIZIERT -->
state.mode          // 'cold_call' | 'meeting' (PIP-LAUNCHER.JS:29 — NICHT 'cold', NICHT 'kalt')
state.activeTab     // NEU: null | 'opener' | 'erlaubnis' | 'pitch' | 'skript'
state.openerItems   // Array von ProfileOpener mit .type ('opener'|'erlaubnis'|'pitch')
state.skripte       // Array von ProfileSkript
state.selectedOpenerId   // Zeile 41 — Opener-Tab
state.selectedSkriptId   // Zeile 40 — Skript-Tab (BEREITS VORHANDEN — nicht neu einfügen)
// NEU hinzufügen:
state.selectedErlaubnisId  // Erlaubnisfrage-Tab
state.selectedPitchId      // Pitch-Tab

<!-- ProfileOpener.type Werte (models.py:169) -->
'opener', 'erlaubnis', 'pitch' — relevant für Tab-Filterung
'skript' — ignorieren (existiert nicht in Prod, per SPEC)

<!-- Inline-Edit-Pattern (bestehend) -->
_wireInlineEdit('lnr-skript-edit-btn', 'lnr-skript-preview', 'lnr-skript-textarea', 'skript')
_wireInlineEdit('lnr-opener-edit-btn', 'lnr-opener-preview', 'lnr-opener-textarea', 'opener')

<!-- startCall() aktuelle Logik (pip-launcher.js:1285-1325) -->
Prüft state.mode === 'cold_call' → Headset-Gate
Prüft state.mode === 'meeting' → Consent-Gate
Dann close() + _ensureSocketIO()
// NEU in v3: Vor dem close() — Pitch/Skript Prioritätsprüfung für Teleprompter-Text

<!-- _openPipAndMic() Teleprompter-Auflösung (pip-launcher.js:1416-1434) — VERIFIZIERT -->
Aktuelle Logik (Zeilen 1416-1434):
  var skriptInhalt = '';
  var skriptBlöcke = [];
  if (state._editedSkriptText) {
    skriptInhalt = state._editedSkriptText;
  } else if (state.selectedSkriptId && state.skripte.length > 0) {
    var sk = state.skripte.find(function (s) { return s.id === state.selectedSkriptId; });
    if (sk && sk.inhalt) skriptInhalt = sk.inhalt;
  }
  if (skriptInhalt) { skriptBlöcke = skriptInhalt.split(/\n\n+/).filter(...) }
NEU: Vor dieser Logik in _openPipAndMic() prüfen: if (state._resolvedTeleprompterSkript) { skriptInhalt = state._resolvedTeleprompterSkript.inhalt; state._resolvedTeleprompterSkript = null; } else if ...

<!-- Modus-abhängige Personalisierung — Payload-Shapes (NEU v3) -->
Cold-Call: {opener_id: state.selectedOpenerId, call_mode: state.mode, briefing: {...}}
Meeting:   {skript_id: state.selectedSkriptId,  call_mode: state.mode, briefing: {...}}

<!-- cap-modal success handler (pip-launcher.js:756-758) — VERIFIZIERT AKTUELLER STAND -->
Aktuell (Zeilen 756-758):
  state._personalizedSkriptText = null;
  _collectEditedTexts();
  startCall(true);
KEIN _editedOpenerText-Assignment vorhanden — Änderung D muss unbedingt einfügen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: state-Objekt erweitern + window.switchTab5() einfügen</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeilen 26-70 (state-Objekt vollständig lesen — alle Properties notieren)
    - static/pip-launcher.js Zeilen 770-775 (Position direkt vor renderStep5 für switchTab5-Einfügepunkt)
  </read_first>
  <action>
**Schritt 1 — 3 neue Properties in state-Objekt einfügen, nach Zeile 41 (nach `selectedOpenerId: null,`):**

Bestehend (Zeilen 40-41):
```js
    selectedSkriptId: null,
    selectedOpenerId: null,
```

Nach `selectedOpenerId: null,` einfügen:
```js
    selectedErlaubnisId: null,  // Andre-Decision: 1 Auswahl pro Reiter, eigene Variable
    selectedPitchId: null,      // Andre-Decision: 1 Auswahl pro Reiter, eigene Variable
    activeTab: null,            // D-06: 'opener' | 'erlaubnis' | 'pitch' | 'skript'; null → Default 'opener'
```

HINWEIS: selectedSkriptId existiert bereits (Zeile 40). Nicht doppelt einfügen. Task 1 fügt NUR [selectedErlaubnisId, selectedPitchId, activeTab] hinzu.

**Schritt 2 — window.switchTab5() als file-scope Funktion DIREKT VOR `function renderStep5()` einfügen:**

```js
  // ── Tab-Switch: Step 5 ────────────────────────────────────────────────────
  function switchTab5(tab) { state.activeTab = tab; renderStep5(); }
  window.switchTab5 = switchTab5;
```

WARUM window.*: pip-launcher.js-onclick-Strings im HTML rufen window.switchTab5() auf — konsistent mit window.NerveLauncher._setAnrede etc.
  </action>
  <verify>
    <automated>grep -n "activeTab\|selectedErlaubnisId\|selectedPitchId\|switchTab5" static/pip-launcher.js | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "selectedErlaubnisId" static/pip-launcher.js` → mindestens 1 Treffer im state-Objekt
    - `grep -n "selectedPitchId" static/pip-launcher.js` → mindestens 1 Treffer im state-Objekt
    - `grep -n "activeTab" static/pip-launcher.js` → mindestens 1 Treffer im state-Objekt
    - `grep -n "function switchTab5" static/pip-launcher.js` → 1 Treffer, direkt vor renderStep5
    - `grep -n "window.switchTab5" static/pip-launcher.js` → mindestens 1 Treffer in der switchTab5-Registrierung
  </acceptance_criteria>
  <done>state.activeTab, state.selectedErlaubnisId, state.selectedPitchId im state-Objekt. window.switchTab5 als file-scope Function direkt vor renderStep5() definiert und auf window registriert.</done>
</task>

<task type="auto">
  <name>Task 2: renderStep5() komplett zu 4-Tab-UI umbauen (mit modus-abhängigem Personalisieren-Button)</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeilen 771-1013 (komplette bestehende renderStep5() vollständig lesen — PFLICHT vor Umbau)
    - static/pip-launcher.js Zeilen 26-29 (state.mode Werte: 'cold_call' | 'meeting' verifizieren)
    - templates/admin/dashboard.html Zeilen 19-26 (fcd-tabs / fcd-tab Pattern)
    - static/nerve.css — grep nach "\.fcd-tab" um existierende Klasse und active-State zu verifizieren, und grep nach "\-\-btn-primary-bg-from" um sicherzustellen dass diese CSS-Variable existiert bevor sie in Leerstand-Links verwendet wird
    - .planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-CONTEXT.md (D-01 bis D-07)
  </read_first>
  <action>
Kompletten Inhalt von `function renderStep5()` ersetzen. Neue Struktur (D-05: monolithisch, inline lokale Variablen):

```
renderStep5() {
  c = content(); Guard.
  Tab-Guard: if (!state.activeTab) state.activeTab = 'opener';   // D-06

  // ── Init (identisch zur bestehenden Logik übernehmen) ──
  savedAnrede5 = state.lastSessionAnrede || profileKI.ansprache || 'Sie'
  if (!state.precallFormData) state.precallFormData = {};
  if (!state.precallFormData.anrede) state.precallFormData.anrede = savedAnrede5;
  // single-script auto-select (bestehende Logik übernehmen)
  if (state.skripte.length === 1 && !state.selectedSkriptId) { ... }

  // ── Profil-Dropdown ──
  var profileOptions = state.profiles.map(...).join('');

  // ── Tab-Nav (D-02: inline onclick) ──
  var tabNavHtml = '<nav class="fcd-tabs" style="margin-bottom:12px">'
    + '<button class="fcd-tab' + (state.activeTab === 'opener'   ? ' active' : '') + '" onclick="window.switchTab5(\'opener\')">Opener</button>'
    + '<button class="fcd-tab' + (state.activeTab === 'erlaubnis'? ' active' : '') + '" onclick="window.switchTab5(\'erlaubnis\')">Erlaubnisfrage</button>'
    + '<button class="fcd-tab' + (state.activeTab === 'pitch'    ? ' active' : '') + '" onclick="window.switchTab5(\'pitch\')">Pitch</button>'
    + '<button class="fcd-tab' + (state.activeTab === 'skript'   ? ' active' : '') + '" onclick="window.switchTab5(\'skript\')">Skript</button>'
    + '</nav>';

  // ── Tab-Content: modus-abhängiger Personalisieren-Button ──
  // MODUS-LOGIK (NEU v3):
  //   state.mode === 'meeting'   → Personalisieren-Button nur im Skript-Tab
  //   state.mode === 'cold_call' → Personalisieren-Button nur im Opener-Tab
  //   kein mode (null)           → wie cold_call behandeln
  var isMeeting = (state.mode === 'meeting');
  var tabContentHtml;

  if (state.activeTab === 'opener') {
    var openerItems = (state.openerItems || []).filter(function(o) { return o.type === 'opener'; });
    if (openerItems.length === 0) {
      tabContentHtml = '<div style="color:var(--page-text-muted);font-size:13px">Noch kein Opener hinterlegt — <a href="/profiles" style="color:var(--btn-primary-bg-from)">Profil bearbeiten</a></div>';
    } else {
      var opSel5 = openerItems.find(function(o) { return o.id === state.selectedOpenerId; });
      var opOpts = openerItems.map(function(o) {
        return '<option value="' + o.id + '"' + (o.id === state.selectedOpenerId ? ' selected' : '') + '>' + escHtml(o.name) + '</option>';
      }).join('');
      var opPrev = opSel5 ? escHtml(opSel5.inhalt || '') : '';
      tabContentHtml =
        '<select class="launcher-select" id="lnr-opener-select">' + opOpts + '</select>'
        + '<div class="launcher-opener-preview" id="lnr-opener-preview" style="white-space:pre-wrap' + (opPrev ? '' : ';color:var(--page-text-muted);font-style:italic') + '">' + (opPrev || 'Opener auswählen für Vorschau') + '</div>'
        + '<textarea class="launcher-briefing" id="lnr-opener-textarea" style="display:none;margin-top:4px" rows="3"></textarea>'
        + '<button type="button" class="launcher-inline-edit-btn" id="lnr-opener-edit-btn">Bearbeiten</button>'
        // Personalisieren-Button: nur im Opener-Tab wenn Cold-Call-Modus (oder kein Modus)
        + (!isMeeting ? '<button class="launcher-btn-ghost" id="lnr-step5-personalize">✨ Personalisieren + Call ▶</button>' : '');
    }

  } else if (state.activeTab === 'erlaubnis') {
    var erlaubnisItems = (state.openerItems || []).filter(function(o) { return o.type === 'erlaubnis'; });
    if (erlaubnisItems.length === 0) {
      tabContentHtml = '<div style="color:var(--page-text-muted);font-size:13px">Noch keine Erlaubnisfrage hinterlegt — <a href="/profiles" style="color:var(--btn-primary-bg-from)">Profil bearbeiten</a></div>';
    } else {
      var erlSel5 = erlaubnisItems.find(function(o) { return o.id === state.selectedErlaubnisId; });
      var erlOpts = erlaubnisItems.map(function(o) {
        return '<option value="' + o.id + '"' + (o.id === state.selectedErlaubnisId ? ' selected' : '') + '>' + escHtml(o.name) + '</option>';
      }).join('');
      var erlPrev = erlSel5 ? escHtml(erlSel5.inhalt || '') : '';
      tabContentHtml =
        '<select class="launcher-select" id="lnr-opener-select">' + erlOpts + '</select>'
        + '<div class="launcher-opener-preview" id="lnr-opener-preview" style="white-space:pre-wrap' + (erlPrev ? '' : ';color:var(--page-text-muted);font-style:italic') + '">' + (erlPrev || 'Erlaubnisfrage auswählen für Vorschau') + '</div>'
        + '<textarea class="launcher-briefing" id="lnr-opener-textarea" style="display:none;margin-top:4px" rows="3"></textarea>'
        + '<button type="button" class="launcher-inline-edit-btn" id="lnr-opener-edit-btn">Bearbeiten</button>';
        // Kein Personalisieren-Button in Erlaubnisfrage-Tab (weder Cold-Call noch Meeting)
    }

  } else if (state.activeTab === 'pitch') {
    var pitchItems = (state.openerItems || []).filter(function(o) { return o.type === 'pitch'; });
    if (pitchItems.length === 0) {
      tabContentHtml = '<div style="color:var(--page-text-muted);font-size:13px">Noch kein Pitch hinterlegt — <a href="/profiles" style="color:var(--btn-primary-bg-from)">Profil bearbeiten</a></div>';
    } else {
      var pitchSel5 = pitchItems.find(function(o) { return o.id === state.selectedPitchId; });
      var pitchOpts = pitchItems.map(function(o) {
        return '<option value="' + o.id + '"' + (o.id === state.selectedPitchId ? ' selected' : '') + '>' + escHtml(o.name) + '</option>';
      }).join('');
      var pitchPrev = pitchSel5 ? escHtml(pitchSel5.inhalt || '') : '';
      tabContentHtml =
        '<select class="launcher-select" id="lnr-opener-select">' + pitchOpts + '</select>'
        + '<div class="launcher-opener-preview" id="lnr-opener-preview" style="white-space:pre-wrap' + (pitchPrev ? '' : ';color:var(--page-text-muted);font-style:italic') + '">' + (pitchPrev || 'Pitch auswählen für Vorschau') + '</div>'
        + '<textarea class="launcher-briefing" id="lnr-opener-textarea" style="display:none;margin-top:4px" rows="3"></textarea>'
        + '<button type="button" class="launcher-inline-edit-btn" id="lnr-opener-edit-btn">Bearbeiten</button>';
        // Kein Personalisieren-Button in Pitch-Tab (Pitch ist Fallback für Meeting — User geht in Skript-Tab zum Personalisieren)
    }

  } else {  // 'skript' (default fallback wenn kein anderer Tab matcht)
    if ((state.skripte || []).length === 0) {
      tabContentHtml = '<div style="color:var(--page-text-muted);font-size:13px">Noch kein Skript hinterlegt — <a href="/profiles" style="color:var(--btn-primary-bg-from)">Profil bearbeiten</a></div>';
    } else {
      var skSel5 = state.skripte.find(function(s) { return s.id === state.selectedSkriptId; });
      var skOpts = state.skripte.map(function(s) {
        return '<option value="' + s.id + '"' + (s.id === state.selectedSkriptId ? ' selected' : '') + '>' + escHtml(s.name) + '</option>';
      }).join('');
      var skPrev = skSel5 ? escHtml(skSel5.inhalt || '') : '';
      tabContentHtml =
        '<select class="launcher-select" id="lnr-skript-select">' + skOpts + '</select>'
        + '<div class="launcher-opener-preview" id="lnr-skript-preview" style="white-space:pre-wrap' + (skPrev ? '' : ';color:var(--page-text-muted);font-style:italic') + '">' + (skPrev || 'Skript auswählen für Vorschau') + '</div>'
        + '<textarea class="launcher-briefing" id="lnr-skript-textarea" style="display:none;margin-top:4px" rows="4"></textarea>'
        + '<button type="button" class="launcher-inline-edit-btn" id="lnr-skript-edit-btn">Bearbeiten</button>'
        // Personalisieren-Button: nur im Skript-Tab wenn Meeting-Modus (NEU v3)
        + (isMeeting ? '<button class="launcher-btn-ghost" id="lnr-step5-personalize">✨ Personalisieren + Call ▶</button>' : '');
    }
  }

  // ── innerHTML zusammensetzen ──
  c.innerHTML = [
    '<div class="launcher-step">',
    '<div class="nav-live-title">Gesprächsvorbereitung</div>',   // R4

    // Profil-Picker
    state.profiles.length > 0
      ? '<label style="font-size:11px;color:var(--page-text-muted);margin-bottom:2px;display:block">Profil</label><select class="launcher-select" id="lnr-profile-select">' + profileOptions + '</select>'
      : '<div style="color:var(--page-text-muted);font-size:13px">Noch kein Profil angelegt. <a href="/profiles" style="color:var(--btn-primary-bg-from)">Profil erstellen</a></div>',

    // Tab-Nav
    tabNavHtml,

    // Tab-Content
    tabContentHtml,

    // ── Anrede (immer sichtbar, kein Conditional) ── R5
    '<div class="launcher-form-label" style="margin-top:12px;font-size:13px;color:var(--page-text-muted)">Anrede im Gespräch *</div>',
    '<div class="launcher-anrede-row" id="lnr-anrede-row5">',
    '<button type="button" class="launcher-anrede-btn' + (savedAnrede5 === 'Du' ? ' active' : '') + '" data-val="Du" onclick="window.NerveLauncher._setAnrede(\'Du\')">Du</button>',
    '<button type="button" class="launcher-anrede-btn' + (savedAnrede5 === 'Sie' ? ' active' : '') + '" data-val="Sie" onclick="window.NerveLauncher._setAnrede(\'Sie\')">Sie</button>',
    '</div>',

    // ── Vorwissen-Picker (immer sichtbar, kein Conditional auf precallBriefing) ── R5
    '<div class="launcher-form-label" style="margin-top:12px;font-size:13px;color:var(--page-text-muted)">Lead-Vorwissen (optional)</div>',
    '<div class="launcher-anrede-row" id="lnr-vorwissen-row5" style="gap:8px;flex-wrap:wrap">',
    '<button type="button" class="launcher-anrede-btn' + (state.vorwissenLevel === 'niedrig' ? ' active' : '') + '" data-val="niedrig" onclick="window.NerveLauncher._setVorwissen(\'niedrig\')">Wenig Ahnung</button>',
    '<button type="button" class="launcher-anrede-btn' + (state.vorwissenLevel === 'mittel'  ? ' active' : '') + '" data-val="mittel"  onclick="window.NerveLauncher._setVorwissen(\'mittel\')">Vertraut damit</button>',
    '<button type="button" class="launcher-anrede-btn' + (state.vorwissenLevel === 'hoch'    ? ' active' : '') + '" data-val="hoch"    onclick="window.NerveLauncher._setVorwissen(\'hoch\')">Kennt uns gut</button>',
    '<button type="button" class="launcher-anrede-btn' + (!state.vorwissenLevel              ? ' active' : '') + '" data-val="null"    onclick="window.NerveLauncher._setVorwissen(null)">Weiß nicht</button>',
    '</div>',

    // ── Navigation ──
    '<div class="launcher-actions">',
    '<button class="launcher-btn-ghost" id="lnr-step5-back">&#8592; Zurück</button>',
    '<button class="launcher-btn-ghost" id="lnr-step5-skip">Überspringen</button>',
    '<button class="launcher-btn-primary" id="lnr-step5-start">Call starten &#9654;</button>',
    '</div>',
    '</div>'
  ].join('');

  // ── Event Wiring nach innerHTML ──

  // Profile-Change (reset activeTab + alle 4 Selection-Variablen beim Profil-Wechsel)
  var profileSel5 = document.getElementById('lnr-profile-select');
  if (profileSel5) {
    profileSel5.onchange = function () {
      var pid = parseInt(profileSel5.value);
      if (!pid) return;
      fetch('/api/launcher/profile/' + pid)
        .then(function (r) { return r.json(); })
        .then(function (data) {
          state.activeProfileId    = data.id;
          state.profileDaten       = data.daten || {};
          state.skripte            = data.skripte || [];
          state.openerItems        = data.opener || [];
          state.selectedSkriptId   = null;
          state.selectedOpenerId   = null;
          state.selectedErlaubnisId = null;
          state.selectedPitchId    = null;
          state.activeTab          = null;  // reset to opener-default on profile change
          renderStep5();
        })
        .catch(function () {});
    };
  }

  // Opener/Erlaubnisfrage/Pitch-Select — Tab-aware: schreibt in tab-spezifische Variable
  var openerSel5 = document.getElementById('lnr-opener-select');
  if (openerSel5) {
    openerSel5.onchange = function () {
      var id = parseInt(openerSel5.value) || null;
      if      (state.activeTab === 'opener')   state.selectedOpenerId    = id;
      else if (state.activeTab === 'erlaubnis') state.selectedErlaubnisId = id;
      else if (state.activeTab === 'pitch')     state.selectedPitchId     = id;
      var preview = document.getElementById('lnr-opener-preview');
      if (preview) {
        var tab   = state.activeTab;
        var items = (state.openerItems || []).filter(function(o) { return o.type === tab; });
        var selId = tab === 'erlaubnis' ? state.selectedErlaubnisId
                  : tab === 'pitch'     ? state.selectedPitchId
                  :                       state.selectedOpenerId;
        var op5 = items.find(function(o) { return o.id === selId; });
        preview.textContent   = op5 ? op5.inhalt : (tab + ' auswählen für Vorschau');
        preview.style.fontStyle = op5 ? 'normal' : 'italic';
        preview.style.color     = op5 ? '' : 'var(--page-text-muted)';
      }
    };
  }

  // Skript-Select (nur Skript-Tab)
  var skriptSel5 = document.getElementById('lnr-skript-select');
  if (skriptSel5) {
    skriptSel5.onchange = function () {
      state.selectedSkriptId = parseInt(skriptSel5.value) || null;
      var preview = document.getElementById('lnr-skript-preview');
      if (preview) {
        var sk5 = state.skripte.find(function(s) { return s.id === state.selectedSkriptId; });
        preview.textContent   = sk5 ? sk5.inhalt : 'Skript auswählen für Vorschau';
        preview.style.fontStyle = sk5 ? 'normal' : 'italic';
        preview.style.color     = sk5 ? '' : 'var(--page-text-muted)';
      }
    };
  }

  // Inline-Edit Wiring (je aktiver Tab)
  if (state.activeTab === 'skript') {
    _wireInlineEdit('lnr-skript-edit-btn', 'lnr-skript-preview', 'lnr-skript-textarea', 'skript');
  } else {
    _wireInlineEdit('lnr-opener-edit-btn', 'lnr-opener-preview', 'lnr-opener-textarea', 'opener');
  }

  // Navigation: Back
  var backBtn5 = document.getElementById('lnr-step5-back');
  if (backBtn5) {
    backBtn5.onclick = function () {
      if (state.precallBriefing)       { state.step = 4; }
      else if (state.precallVerfuegbar){ state.step = 2; }
      else                             { state.step = 1; }
      renderStep();
    };
  }

  // Navigation: Skip
  var skipBtn5 = document.getElementById('lnr-step5-skip');
  if (skipBtn5) {
    skipBtn5.onclick = function () { _collectEditedTexts(); startCall(false); };
  }

  // Navigation: Start
  var startBtn5 = document.getElementById('lnr-step5-start');
  if (startBtn5) {
    startBtn5.onclick = function () {
      var s5 = document.getElementById('lnr-profile-select');
      if (s5 && s5.value) state.activeProfileId = parseInt(s5.value);
      _collectEditedTexts();
      startCall(true);
    };
  }

  // Personalisieren-Button (existiert nur wenn modus-passendem Tab aktiv)
  var personalizeBtn5 = document.getElementById('lnr-step5-personalize');
  if (personalizeBtn5) {
    personalizeBtn5.onclick = function () {
      // Modus-abhängig: Meeting → selectedSkriptId; Cold-Call → selectedOpenerId
      if (state.mode === 'meeting') {
        var skSel = document.getElementById('lnr-skript-select');
        if (skSel && skSel.value) state.selectedSkriptId = parseInt(skSel.value, 10) || null;
      } else {
        var opSel = document.getElementById('lnr-opener-select');
        if (opSel && opSel.value) state.selectedOpenerId = parseInt(opSel.value, 10) || null;
      }
      state.briefingModus = 'C';
      state.step = '4b';
      renderStep();
    };
  }
}
```

WICHTIG — Abweichungen vom bestehenden Code:
- optgroup-Logik (opGroupCurrent/opGroupStandard/opGroupOther) ENTFÄLLT — Tabs trennen nach type
- `'Skript & Opener wählen'` String eliminieren — grep bestätigt nachher 0 Treffer
- Null-Guard für skripte: `(state.skripte || [])` in Leerstand-Check des Skript-Tabs
- var(--btn-primary-bg-from) aus nerve.css verifizieren (read_first) bevor in Leerstand-Links verwenden
  </action>
  <verify>
    <automated>grep -n "Skript & Opener wählen\|opGroupCurrent" static/pip-launcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Gesprächsvorbereitung" static/pip-launcher.js` → mindestens 1 Treffer in renderStep5
    - `grep -n "Skript & Opener wählen" static/pip-launcher.js` → 0 Treffer
    - `grep -n "fcd-tab" static/pip-launcher.js` → mindestens 4 Treffer (je Tab-Button)
    - `grep -n "switchTab5" static/pip-launcher.js` → mindestens 4 Treffer in Tab-Button-onclick-Strings
    - `grep -n "o.type === .opener." static/pip-launcher.js` → Treffer
    - `grep -n "o.type === .erlaubnis." static/pip-launcher.js` → Treffer
    - `grep -n "o.type === .pitch." static/pip-launcher.js` → Treffer
    - `grep -n "Noch kein Opener hinterlegt\|Noch keine Erlaubnisfrage\|Noch kein Pitch\|Noch kein Skript" static/pip-launcher.js` → je 1 Treffer
    - `grep -n "lnr-vorwissen-row5\|lnr-anrede-row5" static/pip-launcher.js` → Treffer OHNE Conditional auf precallBriefing
    - `grep -n "isMeeting.*lnr-step5-personalize\|lnr-step5-personalize.*isMeeting" static/pip-launcher.js` → Treffer (Button ist modus-bedingt, nicht tab-fest)
    - `grep -n "#[0-9a-fA-F]\{3,6\}" static/pip-launcher.js | grep "fcd-tab\|tabNavHtml\|tabContentHtml\|Opener hinterlegt\|Erlaubnisfrage\|Pitch hinterlegt\|Skript hinterlegt"` → 0 Treffer (keine neuen Hex-Werte)
    - `grep -n "btn-primary-bg-from" static/pip-launcher.js` → Treffer für /profiles-Links in Leerstand-States
    - `grep -n "isMeeting" static/pip-launcher.js` → mindestens 3 Treffer (Definition + Button-Bedingung in Opener-Tab + Button-Bedingung in Skript-Tab)
  </acceptance_criteria>
  <done>renderStep5() vollständig umgebaut. 4 fcd-Tab-Buttons. openerItems-Filterung nach type. Leerstand-States pro Tab. Titel 'Gesprächsvorbereitung'. Vorwissen + Anrede immer sichtbar. Personalisieren-Button modus-abhängig: Cold-Call → Opener-Tab, Meeting → Skript-Tab. Keine neuen Hex-Werte.</done>
</task>

<task type="auto">
  <name>Task 3: renderStep4b + renderStep4c + _savePersonalizedAndStartCall auf modus-abhängige Payloads umstellen</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeilen 511-588 (renderStep4b — sendet aktuell opener_id, vollständig lesen)
    - static/pip-launcher.js Zeilen 590-635 (renderStep4c — liest aktuell state.openerItems, vollständig lesen)
    - static/pip-launcher.js Zeilen 638-684 (_savePersonalizedAndStartCall — sendet aktuell opener_id, vollständig lesen)
    - static/pip-launcher.js Zeilen 686-768 (_showCapSubModal — exakten aktuellen Stand des cap-delete-save success handlers lesen, insbesondere Zeilen 749-758)
  </read_first>
  <action>
Vier gezielte Änderungen — modus-abhängig (state.mode === 'meeting' vs. 'cold_call'). KEIN Gesamt-Rewrite.

**WICHTIG VOR JEDEM EDIT:** Exakte Zeilennummern aus dem Read-First-Schritt entnehmen, dann präzise ersetzen.

**Änderung A — renderStep4b: Fetch-Body modus-abhängig**

Bestehend (irgendwo in Zeilen 511-588, fetch body):
```js
      body: JSON.stringify({
        opener_id: state.selectedOpenerId,
        briefing: state.precallBriefing || {}
      }),
```

Ersetzen durch:
```js
      body: JSON.stringify(
        state.mode === 'meeting'
          ? { skript_id: state.selectedSkriptId,  call_mode: state.mode, briefing: state.precallBriefing || {} }
          : { opener_id: state.selectedOpenerId,  call_mode: state.mode || 'cold_call', briefing: state.precallBriefing || {} }
      ),
```

**Änderung B — renderStep4c: originalText-Lookup modus-abhängig**

Bestehend (irgendwo in Zeilen 590-635):
```js
    var opener = state.openerItems && state.openerItems.find(function (o) {
      return o.id === state.selectedOpenerId;
    });
    var originalText = opener ? (opener.inhalt || '') : '';
```

Ersetzen durch:
```js
    // Modus-abhängige Quelle: Meeting → state.skripte; Cold-Call → state.openerItems
    var originalText = '';
    if (state.mode === 'meeting') {
      var skript4c = (state.skripte || []).find(function (s) { return s.id === state.selectedSkriptId; });
      originalText = skript4c ? (skript4c.inhalt || '') : '';
    } else {
      var opener4c = (state.openerItems || []).find(function (o) { return o.id === state.selectedOpenerId; });
      originalText = opener4c ? (opener4c.inhalt || '') : '';
    }
```

**Änderung C — _savePersonalizedAndStartCall: Fetch-Body + Ergebnis-Speicherung modus-abhängig**

Bestehend (fetch body, Zeilen 638-684 Bereich):
```js
        body: JSON.stringify({
          opener_id: state.selectedOpenerId,
          personalized_text: state._personalizedSkriptText || '',
          firmenname: (state.precallBriefing && state.precallBriefing.firmenname) || ''
        })
```

Ersetzen durch:
```js
        body: JSON.stringify(Object.assign(
          state.mode === 'meeting'
            ? { skript_id: state.selectedSkriptId }
            : { opener_id: state.selectedOpenerId },
          {
            call_mode: state.mode || 'cold_call',
            personalized_text: state._personalizedSkriptText || '',
            firmenname: (state.precallBriefing && state.precallBriefing.firmenname) || ''
          }
        ))
```

Bestehend (success handler in _savePersonalizedAndStartCall, irgendwo kurz nach dem fetch):
```js
        state._editedOpenerText = state._personalizedSkriptText || "";
        state._personalizedSkriptText = null;
```

Ersetzen durch:
```js
        // Modus-abhängig: Meeting → _editedSkriptText; Cold-Call → _editedOpenerText
        if (state.mode === 'meeting') {
          state._editedSkriptText  = state._personalizedSkriptText || "";
        } else {
          state._editedOpenerText  = state._personalizedSkriptText || "";
        }
        state._personalizedSkriptText = null;
```

**Änderung D — cap-modal (cap-delete-save) success handler: Ergebnis-Speicherung modus-abhängig**

Bestehend (fetch body in lnr-cap-delete-save onclick, Zeilen 686-747 Bereich):
```js
          body: JSON.stringify({
            opener_id: state.selectedOpenerId,
            personalized_text: state._personalizedSkriptText || '',
            firmenname: (state.precallBriefing && state.precallBriefing.firmenname) || '',
            delete_ids: deleteIds
          })
```

Ersetzen durch:
```js
          body: JSON.stringify(Object.assign(
            state.mode === 'meeting'
              ? { skript_id: state.selectedSkriptId }
              : { opener_id: state.selectedOpenerId },
            {
              call_mode: state.mode || 'cold_call',
              personalized_text: state._personalizedSkriptText || '',
              firmenname: (state.precallBriefing && state.precallBriefing.firmenname) || '',
              delete_ids: deleteIds
            }
          ))
```

Bestehend (cap-delete-save success handler — VERIFIZIERT Zeilen 756-758 aus read_first):
```js
          state._personalizedSkriptText = null;
          _collectEditedTexts();
          startCall(true);
```

KEIN `state._editedOpenerText`-Assignment existiert hier aktuell. Den modus-abhängigen Assignment-Block UNBEDINGT vor `state._personalizedSkriptText = null;` einfügen:

Ersetzen durch:
```js
          // Modus-abhängig: Meeting → _editedSkriptText; Cold-Call → _editedOpenerText
          if (state.mode === 'meeting') {
            state._editedSkriptText = state._personalizedSkriptText || "";
          } else {
            state._editedOpenerText = state._personalizedSkriptText || "";
          }
          state._personalizedSkriptText = null;
          _collectEditedTexts();
          startCall(true);
```

SICHERHEITS-CHECK nach allen Änderungen:
```bash
grep -n "opener_id\|skript_id\|call_mode" static/pip-launcher.js | grep -v "//"
```
→ opener_id-Treffer NUR in Cold-Call-Branches. skript_id-Treffer NUR in Meeting-Branches. call_mode in allen 4 fetch-Bodies.
  </action>
  <verify>
    <automated>grep -n "call_mode" static/pip-launcher.js | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "call_mode" static/pip-launcher.js | grep -v "//"` → mindestens 3 Treffer (renderStep4b + _savePersonalizedAndStartCall + cap-modal)
    - `grep -n "state.mode === .meeting." static/pip-launcher.js` → mindestens 4 Treffer (renderStep4b, renderStep4c, _savePersonalizedAndStartCall, cap-modal)
    - `grep -n "skript_id: state.selectedSkriptId" static/pip-launcher.js` → mindestens 2 Treffer (renderStep4b Meeting-Branch + _savePersonalizedAndStartCall Meeting-Branch)
    - `grep -n "opener_id: state.selectedOpenerId" static/pip-launcher.js` → mindestens 2 Treffer (renderStep4b Cold-Call-Branch + _savePersonalizedAndStartCall Cold-Call-Branch)
    - `grep -n "_editedSkriptText.*_personalizedSkriptText\|_editedOpenerText.*_personalizedSkriptText" static/pip-launcher.js` → je mindestens 2 Treffer (save + cap-modal, modus-abhängig)
    - `grep -n "skript4c\|opener4c" static/pip-launcher.js` → Treffer in renderStep4c (modus-abhängiger Lookup)
  </acceptance_criteria>
  <done>renderStep4b sendet modus-abhängig skript_id+call_mode (Meeting) oder opener_id+call_mode (Cold-Call). renderStep4c liest originalText modus-abhängig aus state.skripte (Meeting) oder state.openerItems (Cold-Call). _savePersonalizedAndStartCall + cap-modal-success-handler senden modus-abhängig und speichern Ergebnis in _editedSkriptText (Meeting) oder _editedOpenerText (Cold-Call). cap-modal-Speicherung ist UNBEDINGT vorhanden (kein Conditional "falls noch vorhanden").</done>
</task>

<task type="auto">
  <name>Task 4: _collectEditedTexts() Guard-Fix + startCall() Pitch/Skript-Priorität + _openPipAndMic() Teleprompter-Übergabe</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeilen 1173-1179 (_collectEditedTexts() aktueller Stand — VOLLSTÄNDIG lesen)
    - static/pip-launcher.js Zeilen 1283-1325 (startCall() vollständig lesen)
    - static/pip-launcher.js Zeilen 1414-1434 (_openPipAndMic() Teleprompter-Auflösung — VOLLSTÄNDIG lesen, insbesondere wie skriptInhalt aufgelöst wird)
    - static/pip-launcher.js — grep nach "_editedOpenerText\|_editedSkriptText\|skript_inhalt\|teleprompterBlocks" um zu verstehen wie die Texte beim Call-Start genutzt werden
  </read_first>
  <action>
**Teil A — _collectEditedTexts() gezielter Guard-Fix (Cross-AI-Finding #2):**

Hintergrund: _collectEditedTexts() existiert bereits bei Zeilen 1173-1179. Die Funktion sammelt Textarea-Inhalte bevor startCall() aufgerufen wird. Problem: Zeile 1176 fehlt ein `&& skTa.value`-Guard, sodass der Wert auch bei leerer Textarea überschrieben wird.

Bestehend (Zeilen 1173-1179 — exakt so wie es steht):
```js
  function _collectEditedTexts() {
    // If user edited inline, store the edited text for the session
    var skTa = document.getElementById('lnr-skript-textarea');
    if (skTa && skTa.style.display !== 'none') state._editedSkriptText = skTa.value;
    var opTa = document.getElementById('lnr-opener-textarea');
    if (opTa && opTa.style.display !== 'none') state._editedOpenerText = opTa.value;
  }
```

Gezielte Änderung: NUR Zeile 1176 anpassen — `&& skTa.value` Guard hinzufügen. Analog Zeile 1178 für opTa.

Ersetzen durch:
```js
  function _collectEditedTexts() {
    // If user edited inline, store the edited text for the session
    var skTa = document.getElementById('lnr-skript-textarea');
    if (skTa && skTa.style.display !== 'none' && skTa.value) state._editedSkriptText = skTa.value;
    var opTa = document.getElementById('lnr-opener-textarea');
    if (opTa && opTa.style.display !== 'none' && opTa.value) state._editedOpenerText = opTa.value;
  }
```

HINWEIS: Das ist ein gezieltes 2-Zeilen-Edit, KEIN Function-Rewrite. Callers (skipBtn5.onclick, startBtn5.onclick) bleiben unverändert.

**Teil B — startCall() Pitch/Skript-Priorität + state._resolvedTeleprompterSkript setzen (OD-01 Option A):**

_openPipAndMic() (Zeilen 1416-1434) löst Teleprompter-Inhalt bereits selbst auf (state._editedSkriptText → state.selectedSkriptId → leer). startCall() muss jedoch den Pitch-Fallback (selectedPitchId) VORHER auflösen und das Ergebnis in state._resolvedTeleprompterSkript ablegen, damit _openPipAndMic() es prioritär lesen kann.

Einfügepunkt in startCall(): NACH den Gate-Checks (Headset + Consent) und DIREKT VOR dem `close()`-Aufruf bei ca. Zeile 1310.

Bestehend (Zeile 1310):
```js
    close();
```

Einfügen VOR `close()`:
```js
    // OD-01 Option A: Pitch/Skript-Priorität für Teleprompter-Übergabe an _openPipAndMic()
    // selectedSkriptId gesetzt → _openPipAndMic() nutzt eigene Auflösung (state._editedSkriptText > skripte)
    // nur selectedPitchId gesetzt (kein Skript) → Pitch-Inhalt als Fallback vorberechnen
    // Ergebnis in state._resolvedTeleprompterSkript — _openPipAndMic() liest es zuerst und löscht es
    if (!state.selectedSkriptId && state.selectedPitchId) {
      var selPitch = (state.openerItems || []).find(function(o) { return o.id === state.selectedPitchId; });
      if (selPitch) {
        state._resolvedTeleprompterSkript = { id: state.selectedPitchId, inhalt: selPitch.inhalt || '' };
      }
    }
    // wenn selectedSkriptId gesetzt: _resolvedTeleprompterSkript nicht setzen
    // → _openPipAndMic() nutzt dann seine eigene Auflösung via state._editedSkriptText / selectedSkriptId
    close();
```

**Teil C — _openPipAndMic() Teleprompter-Auflösung erweitern:**

Bestehend (Zeilen 1416-1423 — aus read_first entnehmen):
```js
      var skriptInhalt = '';
      var skriptBlöcke = [];
      if (state._editedSkriptText) {
        skriptInhalt = state._editedSkriptText;
      } else if (state.selectedSkriptId && state.skripte.length > 0) {
        var sk = state.skripte.find(function (s) { return s.id === state.selectedSkriptId; });
        if (sk && sk.inhalt) skriptInhalt = sk.inhalt;
      }
```

Ersetzen durch (state._resolvedTeleprompterSkript als erste Priorität DAVOR einfügen):
```js
      var skriptInhalt = '';
      var skriptBlöcke = [];
      // OD-01: Pitch-Fallback-Inhalt aus startCall() (nur wenn kein selectedSkriptId + selectedPitchId gesetzt war)
      if (state._resolvedTeleprompterSkript) {
        skriptInhalt = state._resolvedTeleprompterSkript.inhalt || '';
        state._resolvedTeleprompterSkript = null;
      } else if (state._editedSkriptText) {
        skriptInhalt = state._editedSkriptText;
      } else if (state.selectedSkriptId && state.skripte.length > 0) {
        var sk = state.skripte.find(function (s) { return s.id === state.selectedSkriptId; });
        if (sk && sk.inhalt) skriptInhalt = sk.inhalt;
      }
```

WICHTIG: Zeilen nach der Auflösung (skriptBlöcke-Splitting, openerFuerKi-Block) bleiben UNVERÄNDERT.
  </action>
  <verify>
    <automated>grep -n "_collectEditedTexts\|_editedOpenerText\|_editedSkriptText\|_resolvedTeleprompterSkript\|selectedPitchId\|OD-01" static/pip-launcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_collectEditedTexts" static/pip-launcher.js` → Funktion-Definition vorhanden + mindestens 2 Callers
    - `grep -n "skTa.value\|opTa.value" static/pip-launcher.js | grep "_collectEditedTexts\|display.*none"` → beide Guard-Zeilen mit value-Check vorhanden
    - `grep -n "_resolvedTeleprompterSkript" static/pip-launcher.js` → mindestens 2 Treffer (1x in startCall Pitch-Fallback setzen, 1x in _openPipAndMic lesen + löschen)
    - `grep -n "selectedPitchId" static/pip-launcher.js | grep "startCall\|selPitch\|Fallback"` → Treffer (Pitch-Fallback in startCall)
    - `grep -n "state._resolvedTeleprompterSkript = null" static/pip-launcher.js` → Treffer in _openPipAndMic (cleanup nach Lesen)
  </acceptance_criteria>
  <verify_atomic_deploy>
    <!-- WARNING FIX: Stellt sicher dass Plan 02 Backend-Änderungen deployed sind bevor Plan 01 als abgeschlossen markiert wird -->
    <automated>grep -n "skript_id\|call_mode" routes/app_routes.py | head -5</automated>
    <!-- Erwartetes Ergebnis: mindestens 1 Treffer für skript_id UND mindestens 1 Treffer für call_mode.
         0 Treffer bedeutet Plan 02 wurde noch nicht ausgeführt — NICHT als Complete markieren. -->
  </verify_atomic_deploy>
  <done>_collectEditedTexts() hat && skTa.value / && opTa.value Guards (kein Überschreiben bei leerer Textarea). startCall() setzt state._resolvedTeleprompterSkript wenn nur Pitch ausgewählt (kein Skript). _openPipAndMic() liest _resolvedTeleprompterSkript als erste Priorität und löscht es — OD-01 Option A vollständig realisiert durch 3-stufige Auflösung: Pitch-Fallback-State > _editedSkriptText > selectedSkriptId. Atomic-Deploy-Verify bestätigt Plan 02 Backend-Änderungen vorhanden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS Frontend | pip-launcher.js läuft im Browser — state.mode kommt aus User-Click auf Step 1 (cold_call/meeting) |
| Modus-Routing | isMeeting-Flag steuert welcher Button/Payload genutzt wird — kein User-Override möglich |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.5.6-01-01 | Tampering | Tab-Switch onclick | accept | window.switchTab5 setzt nur state.activeTab (String); kein eval, keine XSS-Vektoren |
| T-08.19.5.6-01-02 | Information Disclosure | Leerstand-Link /profiles | accept | /profiles ist login_required-Route; kein Datenleck durch den Link |
| T-08.19.5.6-01-03 | Spoofing | state.mode Manipulation | accept | state.mode wird nur im Step-1-Click-Handler gesetzt; kein URL-Parameter; Client-seitig änderbar ist kein Security-Issue (das Backend validiert ohnehin per Auth) |
</threat_model>

<verification>
Nach Abschluss aller 4 Tasks:

```bash
# 1. Titel-Änderung
grep -n "Gesprächsvorbereitung" static/pip-launcher.js
# → mindestens 1 Treffer

# 2. Alter Titel weg
grep -n "Skript & Opener wählen" static/pip-launcher.js
# → 0 Treffer

# 3. Tab-System vorhanden
grep -c "fcd-tab" static/pip-launcher.js
# → >= 4

# 4. Filterung nach type
grep -n "o.type" static/pip-launcher.js
# → mindestens 3 Treffer (opener, erlaubnis, pitch)

# 5. Leerstand-States
grep -n "Noch kein" static/pip-launcher.js | grep -i "profil bearbeiten"
# → mindestens 4 Treffer

# 6. Modus-abhängige Personalisierung
grep -n "isMeeting\|state.mode === .meeting." static/pip-launcher.js | grep -v "//"
# → mindestens 4 Treffer (renderStep5 + renderStep4b + renderStep4c + _savePersonalizedAndStartCall)

# 7. call_mode in Fetch-Bodies
grep -n "call_mode" static/pip-launcher.js | grep -v "//"
# → mindestens 3 Treffer

# 8. Kein neues Hex in Tab-HTML
grep -n "tabNavHtml\|fcd-tab\|Opener hinterlegt\|Erlaubnisfrage\|Pitch hinterlegt\|Skript hinterlegt" static/pip-launcher.js | grep "#[0-9a-fA-F]\{3,6\}"
# → 0 Treffer

# 9. 4 selection-state-Variablen im State
grep -n "selectedErlaubnisId\|selectedPitchId" static/pip-launcher.js
# → mindestens 3 Treffer je (state-Definition + reset in profile-change + onchange-Wiring)

# 10. Pitch/Skript-Priorität via _resolvedTeleprompterSkript
grep -n "_resolvedTeleprompterSkript" static/pip-launcher.js
# → mindestens 2 Treffer (startCall Setzung + _openPipAndMic Lesen+Löschen)

# 11. _collectEditedTexts() Guard vorhanden
grep -n "skTa.value\|opTa.value" static/pip-launcher.js
# → 2 Treffer (Guard-Lines in _collectEditedTexts)

# 12. cap-modal modus-abhängige Speicherung vorhanden
grep -n "_editedSkriptText\|_editedOpenerText" static/pip-launcher.js | grep -v "//"
# → mindestens 4 Treffer (save + cap-modal, je Meeting + Cold-Call Branch)

# 13. Atomic-Deploy: Plan 02 Backend vorhanden
grep -n "skript_id\|call_mode" routes/app_routes.py | head -5
# → mindestens 1 Treffer je (Plan 02 muss deployed sein)
```
</verification>

<success_criteria>
- renderStep5() zeigt 4 klickbare fcd-Tab-Buttons; aktiver Tab state persistiert durch Rerender
- Opener-Tab: openerItems mit type==='opener'; Erlaubnisfrage: type==='erlaubnis'; Pitch: type==='pitch'; Skript: state.skripte
- Leere Tabs: Leerstand-State mit /profiles-Link (var(--btn-primary-bg-from) statt #00D4AA — aus nerve.css verifiziert)
- Titel 'Gesprächsvorbereitung' — 'Skript & Opener wählen' 0 Treffer
- Vorwissen + Anrede immer sichtbar (kein if(precallBriefing)-Conditional)
- Personalisieren-Button: Cold-Call → Opener-Tab (setzt selectedOpenerId + sendet opener_id+call_mode); Meeting → Skript-Tab (setzt selectedSkriptId + sendet skript_id+call_mode)
- renderStep4c liest originalText modus-abhängig (skripte oder openerItems)
- cap-modal success handler: modus-abhängige Speicherung (_editedSkriptText / _editedOpenerText) UNBEDINGT vor state._personalizedSkriptText = null (kein Conditional)
- _collectEditedTexts() hat && value Guards; startCall() setzt _resolvedTeleprompterSkript für Pitch-Fallback; _openPipAndMic() liest + löscht _resolvedTeleprompterSkript als erste Priorität (OD-01 Option A vollständig)
- window.switchTab5 auf window registriert, direkt vor renderStep5
- Keine neuen Hex-Werte in Tab-HTML
- Atomic-Deploy-Verify: `grep -n "skript_id\|call_mode" routes/app_routes.py` zeigt Treffer (Plan 02 deployed)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge/08.19.5.6-01-SUMMARY.md` erstellen.

SUMMARY enthält:
- Alle geänderten Zeilen-Ranges in pip-launcher.js
- switchTab5 Einfügepunkt
- renderStep5 Vorher/Nachher-Struktur (kompakt)
- Modus-abhängige Änderungen (isMeeting-Flag, call_mode in 4b/4c/save/cap)
- cap-modal Änderung D: unbedingte modus-abhängige Speicherung vor _personalizedSkriptText = null
- OD-01 Pitch/Skript-Priorität: startCall() → _resolvedTeleprompterSkript → _openPipAndMic() 3-stufige Auflösung
- _collectEditedTexts() Guard-Fix (Zeilen-Diff)
- Etwaige Abweichungen

Format: @$HOME/.claude/get-shit-done/templates/summary.md
</output>
