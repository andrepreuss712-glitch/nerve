---
phase: "08.19.5.6"
plan: "01"
subsystem: "pip-launcher / step5-tab-ui"
tags: ["frontend", "tab-ui", "personalization", "teleprompter", "pip-launcher"]
dependency_graph:
  requires: ["08.19.5.6-02"]
  provides: ["renderStep5-4-tab-ui", "window.switchTab5", "modus-abhaengige-personalisierung"]
  affects: ["static/pip-launcher.js", "templates/base.html"]
tech_stack:
  added: []
  patterns: ["4-Tab-UI via fcd-tab/fcd-tabs", "isMeeting-Flag für modus-abhängige Logik", "OD-01 Pitch/Skript-Priorität via _resolvedTeleprompterSkript"]
key_files:
  created: []
  modified:
    - "static/pip-launcher.js"
    - "templates/base.html"
decisions:
  - "Andre-Decision D-08: Mode-Wechsel behält alle 4 Selection-State-Variablen — kein Reset beim Mode-Click"
  - "OD-01 Option A: selectedSkriptId > selectedPitchId Fallback via state._resolvedTeleprompterSkript"
  - "Deviation: fcd-tab CSS-Styles in base.html eingefügt da admin_dashboard.css im PiP-Kontext nicht geladen wird"
metrics:
  duration: "364 Sekunden (~6 Minuten)"
  completed: "2026-05-05"
  tasks_completed: 4
  tasks_total: 4
  files_modified: 2
---

# Phase 08.19.5.6 Plan 01: 4-Tab-UI renderStep5() + modus-abhängige Personalisierung — Summary

**One-liner:** renderStep5() von flachem Auswahl-UI zu 4-Tab-UI (Opener/Erlaubnisfrage/Pitch/Skript) umgebaut mit modus-abhängiger Personalisierungspipeline (isMeeting-Flag), OD-01 Pitch-Fallback via _resolvedTeleprompterSkript, und _collectEditedTexts() Guard-Fix.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | state-Objekt erweitern + window.switchTab5() | 6446f4d | pip-launcher.js |
| 2 | renderStep5() zu 4-Tab-UI + fcd-tab Styles | 90557dc | pip-launcher.js, base.html |
| 3 | renderStep4b/4c + save + cap-modal modus-abhängig | 91491d8 | pip-launcher.js |
| 4 | _collectEditedTexts Guard + Pitch-Prio + Teleprompter | 487b806 | pip-launcher.js |

## Changes Summary

### Task 1 — state-Erweiterung + window.switchTab5()

**pip-launcher.js Zeilen 40-44 (state-Objekt):**
- `selectedErlaubnisId: null` — eigene Selection-Variable für Erlaubnisfrage-Tab
- `selectedPitchId: null` — eigene Selection-Variable für Pitch-Tab
- `activeTab: null` — aktiver Tab ('opener'|'erlaubnis'|'pitch'|'skript'), null → Default 'opener'

**pip-launcher.js Zeile 775-776 (vor renderStep5):**
```js
function switchTab5(tab) { state.activeTab = tab; renderStep5(); }
window.switchTab5 = switchTab5;
```

### Task 2 — renderStep5() Vollständiger Umbau

**Vorher:** Flache Ansicht mit Skript-Dropdown + Opener-Dropdown mit optgroup-Gruppierung (opGroupCurrent/Standard/Other). Titel "Skript & Opener wählen". Personalisieren-Button immer wenn openerItems.length > 0.

**Nachher:** 4-Tab-Navigation (fcd-tab/fcd-tabs) + Tab-Content-If/else-Block:
- **Opener-Tab:** filtert openerItems nach o.type==='opener'. Personalisieren-Button wenn !isMeeting.
- **Erlaubnisfrage-Tab:** filtert openerItems nach o.type==='erlaubnis'. Kein Personalisieren-Button.
- **Pitch-Tab:** filtert openerItems nach o.type==='pitch'. Kein Personalisieren-Button.
- **Skript-Tab:** nutzt state.skripte. Personalisieren-Button wenn isMeeting.

Titel: "Gesprächsvorbereitung". Vorwissen + Anrede immer sichtbar (kein precallBriefing-Conditional).

Profile-Change-Reset: alle 4 Selection-Variablen + activeTab → null.

**fcd-tab Styles in base.html (Deviation):** Zeilen 371-375 (neue .fcd-tabs/.fcd-tab/.fcd-tab.active Styles).

### Task 3 — Modus-abhängige Payloads (4 Änderungen)

**Änderung A — renderStep4b Fetch-Body (Zeile 552-556):**
```js
body: JSON.stringify(
  state.mode === 'meeting'
    ? { skript_id: state.selectedSkriptId,  call_mode: state.mode, briefing: state.precallBriefing || {} }
    : { opener_id: state.selectedOpenerId,  call_mode: state.mode || 'cold_call', briefing: state.precallBriefing || {} }
),
```

**Änderung B — renderStep4c originalText-Lookup (Zeile 599-606):**
```js
if (state.mode === 'meeting') {
  var skript4c = (state.skripte || []).find(function (s) { return s.id === state.selectedSkriptId; });
  originalText = skript4c ? (skript4c.inhalt || '') : '';
} else {
  var opener4c = (state.openerItems || []).find(function (o) { return o.id === state.selectedOpenerId; });
  originalText = opener4c ? (opener4c.inhalt || '') : '';
}
```

**Änderung C — _savePersonalizedAndStartCall:** Fetch-Body mit Object.assign + modus-abhängiger ID. Ergebnis-Speicherung in `_editedSkriptText` (Meeting) oder `_editedOpenerText` (Cold-Call).

**Änderung D — cap-modal success handler:** Pflicht-Speicherung VOR `_personalizedSkriptText = null` eingefügt. Kein Conditional — immer ausgeführt.

### Task 4 — Guard-Fix + OD-01 Pitch-Priorität

**_collectEditedTexts() Guard-Fix (Zeile 1237/1239):**
```js
if (skTa && skTa.style.display !== 'none' && skTa.value) state._editedSkriptText = skTa.value;
if (opTa && opTa.style.display !== 'none' && opTa.value) state._editedOpenerText = opTa.value;
```

**startCall() OD-01 Pitch-Fallback (Zeile 1371-1383):**
```js
if (!state.selectedSkriptId && state.selectedPitchId) {
  var selPitch = (state.openerItems || []).find(function (o) { return o.id === state.selectedPitchId; });
  if (selPitch) {
    state._resolvedTeleprompterSkript = { id: state.selectedPitchId, inhalt: selPitch.inhalt || '' };
  }
}
```

**_openPipAndMic() 3-stufige Teleprompter-Auflösung (Zeile 1492-1498):**
1. `state._resolvedTeleprompterSkript` (Pitch-Fallback aus startCall) — lesen + löschen
2. `state._editedSkriptText` (personalisierter Text)
3. `state.selectedSkriptId` via state.skripte

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Fehlende kritische Funktionalität] fcd-tab CSS-Styles in base.html eingefügt**
- **Found during:** Task 2 read_first
- **Issue:** `.fcd-tab` / `.fcd-tabs` CSS-Klassen sind nur in `admin_dashboard.css` definiert, die im PiP-Modal-Kontext nicht geladen wird (nur nerve.css via base.html). Tab-Buttons wären ohne Styling unsichtbar/nicht funktional.
- **Fix:** Minimale Tab-Styles in base.html inline-CSS-Block eingefügt (nach `.launcher-inline-edit-btn`). Styles verwenden CSS-Variablen aus nerve.css (`--glass-bg`, `--page-text-muted`, `--btn-primary-bg-from`, `--page-text-color`). Keine Hex-Werte.
- **Files modified:** `templates/base.html`
- **Commit:** 90557dc

## Atomic Deploy Verification

Plan 01 (Frontend) + Plan 02 (Backend) müssen atomar deployed sein.

Verify-Check bestanden: `grep -n "skript_id\|call_mode" routes/app_routes.py` zeigt Treffer auf Zeile 1059/1064 — Plan 02 Backend-Änderungen sind committed und deployed.

## Known Stubs

Keine. Alle Tabs zeigen echte Daten aus state.openerItems (gefiltert nach type) bzw. state.skripte.

## Threat Flags

Keine neuen Sicherheitsrelevanten Surfaces eingeführt. Tab-Switch via window.switchTab5() setzt nur state.activeTab (String-Wert); kein eval, keine XSS-Vektoren. Leerstand-Links zu /profiles sind login_required-Routen.

## Self-Check

PASSED (folgt nach Datei-Erstellung)
