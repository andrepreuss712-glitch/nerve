---
phase: 06.3-analyse-loop-entkoppeln-von-live-slots
verified: 2026-04-17T00:00:00Z
status: human_needed
score: 4/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "PiP Cold Call starten, 3x EWB-Button klicken und vorlesen lassen"
    expected: "0 Anthropic-529-Fehler in Server-Logs"
    why_human: "Cannot reproduce live API load conditions programmatically; requires actual ElevenLabs TTS playback feeding Deepgram -> analyse_loop under concurrent Claude API calls"
  - test: "PiP Cold Call starten, 'keine Zeit' sagen, Backend-Logs pruefen"
    expected: "[PiP-AutoVar] ENTRY trigger=analyse_loop taucht NICHT auf; nur trigger=keyword oder trigger=manual_ewb"
    why_human: "Requires live session with Deepgram transcription active to generate PiP-AutoVar log entries"
---

# Phase 06.3: analyse_loop entkoppeln von Live-Slots — Verification Report

**Phase Goal:** Den 529 overloaded_error beim EWB-Vorlesen strukturell unterbinden. Keyword-Matcher (Phase 06.2) wird alleiniger Primary fuer Live-EWB-Slots. analyse_loop behaelt Intelligence-Funktionen (FT-Events, Kaufbereitschaft, Phase-Classifier, Coaching-Hints), verliert aber jeden UI-Render-Pfad in Slot 0 und Slot 1.
**Verified:** 2026-04-17
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 3x EWB-Vorlesen erzeugt 0 Anthropic-529-Fehler | ? HUMAN | Cannot verify without live EWB-Vorlesen UAT session |
| 2 | analyse_loop ruft nie mehr streame_auto_variante auf (kein trigger=analyse_loop) | VERIFIED | `grep "streame_auto_variante.*analyse_loop" services/` = 0 active calls; only function definition at line 747 with default param string |
| 3 | analyse_loop rendert nie mehr in Slot 0 (kein analysiere_mit_claude_streaming call) | VERIFIED | Only `def` at line 664 and a doc comment at line 759; no call site from analyse_loop |
| 4 | FT-Trainingsdaten werden weiterhin geschrieben (_write_ft_assistant_event laeuft) | VERIFIED | Call site at line 1074 (analyse_loop FT hook), plus def at line 118, additional call sites at lines 733 and 1358 |
| 5 | Kaufbereitschafts-Tracking, Phase-Classifier, Cold-Call-Inference, Active-Hint-Orchestration funktionieren unveraendert | VERIFIED | `compute_readiness_score` (line 1143/1163), `select_active_hint` (1144/1187), `classify_phase` (1102), `infer_cold_call_context` (1123/1130) all present and called in analyse_loop body |

**Score:** 4/5 truths verified (Truth 1 requires human UAT)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `services/claude_service.py` | Entkoppelter analyse_loop ohne Slot-0/Slot-1-Render-Pfade | VERIFIED | `analysiere_mit_claude(neuer_text, kontext)` called in both PiP (active_sid) and non-PiP branches (lines 1011-1018) |
| `config.py` | Erhoehtes ANALYSE_INTERVALL | VERIFIED | Line 34: `ANALYSE_INTERVALL = 4  # Phase 06.3: raised from 2s — analyse_loop is intelligence-only now, fewer calls = less 529 risk + lower cost` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `analyse_loop` (active_sid branch) | `analysiere_mit_claude` (non-streaming) | direct call line 1016 | WIRED | `ergebnis = analysiere_mit_claude(neuer_text, kontext)` confirmed |
| `analyse_loop` (non-PiP else branch) | `analysiere_mit_claude` (non-streaming) | direct call line 1018 | WIRED | Both branches now identical — non-streaming only |
| `analyse_loop` -> `streame_auto_variante` | REMOVED | was via `sio.start_background_task` | NOT_WIRED (correct) | 0 call sites from analyse_loop — goal achieved |
| `analyse_loop` -> `analysiere_mit_claude_streaming` | REMOVED | was direct call | NOT_WIRED (correct) | 0 call sites from analyse_loop — goal achieved |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase removes data flows (streaming render paths) rather than adding new ones. The remaining data flow (`ergebnis` dict from non-streaming Haiku call -> intelligence state) is unchanged from pre-Phase-06.3 non-PiP behavior.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| No streame_auto_variante trigger from analyse_loop | `grep -rn "sio.start_background_task.*streame_auto_variante" services/claude_service.py` | 0 matches | PASS |
| No analysiere_mit_claude_streaming called from analyse_loop | `grep -n "analysiere_mit_claude_streaming" services/claude_service.py` (excluding def and comments) | Only def (664) + comment (759) | PASS |
| No _last_slot dead code remaining | `grep -n "_last_slot" services/claude_service.py` | 0 matches | PASS |
| ANALYSE_INTERVALL = 4 | `grep "ANALYSE_INTERVALL" config.py` | Line 34: value 4 with Phase 06.3 comment | PASS |
| FT hook intact | `grep -n "_write_ft_assistant_event" services/claude_service.py` | def + 3 call sites (lines 118, 733, 1074, 1358) | PASS |
| Intelligence functions present | `grep -n "compute_readiness_score\|classify_phase\|infer_cold_call_context"` | All present in analyse_loop body | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| BUG-10-529 | 06.3-01-PLAN.md | Anthropic 529 overloaded_error beim EWB-Vorlesen | ORPHANED | Not in REQUIREMENTS.md traceability table; bug-fix ID exists only in PLAN frontmatter. The implementation addresses the root cause structurally — but the requirement is not formally tracked in REQUIREMENTS.md. No action needed for phase pass (bug fixes often tracked separately from v1 product requirements). |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `services/claude_service.py` | 981 | `from extensions import socketio as sio` — unused import in analyse_loop (sio never referenced after slot code removal) | Warning | Cosmetic dead code; no runtime impact. Identified in REVIEW.md as LW-01. |
| `services/claude_service.py` | 1011-1018 | Redundant `if active_sid: / else:` — both branches execute identical `ergebnis = analysiere_mit_claude(neuer_text, kontext)` | Warning | Cognitive overhead for future readers; no correctness impact. Identified in REVIEW.md as LW-02. |

Both anti-patterns are pre-catalogued in `06.3-REVIEW.md` as low-severity items (LW-01, LW-02). Neither blocks goal achievement.

---

### Human Verification Required

#### 1. EWB-Vorlesen 529-Free UAT

**Test:** Starte eine PiP Cold Call Session. Klicke 3x den EWB-Button (z.B. "keine Zeit") und lass jedes Mal ElevenLabs den Text vorlesen. Warte jeweils bis der Vorlese-Audio endet.
**Expected:** Keine `529 overloaded_error` Eintraege in Server-Logs. Kein `[PiP-AutoVar] ENTRY trigger=analyse_loop` in Logs — nur `trigger=keyword` oder `trigger=manual_ewb`.
**Why human:** Erfordert live EWB-Vorlesen mit echtem Deepgram-Feed. Die 529-Fehler entstehen durch parallele Claude-API-Calls unter Last — nicht durch statische Code-Analyse pruefbar.

#### 2. Keyword-Matcher bleibt alleiniger Slot-0 Primary

**Test:** Im selben PiP Cold Call: Sag "keine Zeit" ins Mikrofon (ohne EWB-Button). Pruefe ob Slot 0 in <1ms reagiert (Keyword-Matcher) und kein `trigger=analyse_loop` in PiP-AutoVar Logs erscheint.
**Expected:** Slot 0 zeigt Keyword-Match-Antwort. analyse_loop-Zyklus erscheint nur als `[Claude-1]` Log, nicht als PiP-Slot-Render.
**Why human:** Erfordert aktives Mikrofon + Deepgram-Transkription.

---

### Gaps Summary

No structural gaps found. All code-verifiable must-haves are satisfied:

- `streame_auto_variante` is no longer called from `analyse_loop` (0 active call sites)
- `analysiere_mit_claude_streaming` is no longer called from `analyse_loop` (0 active call sites)
- Both PiP and non-PiP branches now use non-streaming `analysiere_mit_claude`
- All intelligence functions (FT-events, Kaufbereitschaft, Phase-Classifier, Cold-Call-Inference, Active-Hint-Orchestration) are intact and wired
- `ANALYSE_INTERVALL = 4` is set with explanatory comment
- `_last_slot` / `_last_slot_time` dead code removed (0 matches)

The phase goal is structurally achieved. Two low-severity cleanup items (unused `sio` import, redundant if/else branch) were pre-identified in REVIEW.md and are not blockers.

Remaining open item: UAT confirmation that 3x EWB-Vorlesen produces 0 Anthropic-529-Fehler in a live session. This is the primary acceptance criterion from the phase spec and cannot be verified programmatically.

**Requirement note:** `BUG-10-529` referenced in PLAN frontmatter does not exist in `REQUIREMENTS.md`. This is expected for a bug-fix phase — bug IDs are not part of the v1 product requirements traceability table. No REQUIREMENTS.md update is strictly needed, but adding a Bug Fixes section to REQUIREMENTS.md would improve traceability for future phases.

---

_Verified: 2026-04-17_
_Verifier: Claude (gsd-verifier)_
