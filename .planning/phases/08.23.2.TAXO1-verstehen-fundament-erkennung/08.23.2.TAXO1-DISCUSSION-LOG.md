# Phase 08.23.2.TAXO1: Verstehen — Fundament + Erkennung - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-10
**Phase:** 08.23.2.TAXO1-verstehen-fundament-erkennung
**Areas discussed:** Einwand-Schubladen (#3), Umbau-Risiko + Test-Takt (#2). Areas #1 (Wegwerf-Brücke) + #4 (Antwort-Baustein) by recommended default.

---

## Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Die Wegwerf-Brücke (Dual-Write) | Wie viel Mühe in die temporäre objection_events-Brücke? | (default) |
| Umbau-Risiko | Big-Bang vs. kleine Schritte mit Test-Anruf | ✓ |
| Einwand-Schubladen | Kern-Taxonomie editierbar vs. fest im Code | ✓ |
| Neuer Antwort-Baustein (EWB v2) | Wann/wie v2-modular live testen | (default) |

**User's choice:** #2 + #3 besprechen; #1 + #4 nach empfohlenem Weg. André lieferte für #2 und #3 direkt fertige Vorab-Positionen.

---

## Einwand-Schubladen (#3)

**User's choice:** Kern-Taxonomie FEST im Code als versioniertes Enum (`taxonomy_version`),
NICHT user-editierbar. User-Erweiterung NUR über `custom_objection_*` pro Profil.

**Notes:** Begründung André — Kern-Taxonomie ist der geteilte Vertrag für Erkennung + Scoring
(TAXO2) + DPO-Korpus; user-editierbar würde späteren Kategorie-Split → Analytics/Korpus zerhauen.
Punkt 12 (Eingriff/Marge) über custom_objection_* erfüllt, ohne den Kern weichzumachen.
Löst gleichzeitig SPEC-Req-5 „D2 zwei Welten": altes `ist_vorwand`-String/Boolean
(claude_service.py:966) + profile_schema-Enum auf diese EINE Quelle umziehen.
Claudian-Einschätzung: zugestimmt (kein Gefälligkeits-Ja — die Vertrags-Begründung ist tragfähig).

---

## Umbau-Risiko (#2)

**User's choice:** Wellen-Schnitt wie SCHILD, KEIN Big-Bang, Test-Anruf auf Prod nach jedem Stück.

**Notes:** Begründung — Kernfeature-Umbau + Pfad-Löschungen + kein Local-Dev = Inkrement de-riskt.

### Follow-up: Test-Takt

| Option | Description | Selected |
|--------|-------------|----------|
| Nur bei riskanten Wellen | Test-Anruf nur wo ein laufender Anruf kaputtgehen könnte; reine Schema-Addition überspringt | ✓ |
| Stur nach jeder Welle | Ausnahmslos ~6 Test-Anrufe | |

**User's choice:** Option 1 (nur riskante Wellen) mit drei Auflagen:
1. „Riskant" großzügig: alles was Live-Erkennung anfasst / alten Pfad löscht / Modus-Routing
   ändert = Test-Anruf. Nur reine Schema-Addition überspringt. Im Zweifel Test-Anruf.
2. Auto-Check ist schwach (deploy.sh-Tor kaputt bis STAGING) → bei JEDER Schema-Welle
   (intent_event, ewb_ratings-Zombie) zusätzlich SCHILD-Wächter MANUELL gegen Prod-DSN.
3. Test-Anruf muss DATEN-FLUSS verifizieren (intent_event-Zeilen geschrieben, Einwand-Zähler
   stimmt, Phase/Readiness bewegen sich), nicht nur „Anruf ok" — weil K1/K8 deployed-aber-tot
   waren (kein Crash, Wert kam nie an).

---

## Claude's Discretion (recommended defaults, #1 + #4)

**Wegwerf-Brücke (#1):** Minimal, ein gekapselter Expand-Contract-Shim mit `# SUNSET: TAXO2`-
Marker, kein Rück-Sync/Backfill. Anti-Abrieb.

**Antwort-Baustein EWB v2 (#4):** v2-modular-Umstellung ist eine riskante Welle (eigener
Test-Anruf inkl. Fachbegriff-Check „ANKER/REFRAME nicht vorgelesen"); ENV-Override bleibt Notbremse.

Zusätzlich an Researcher/Planner delegiert: Slow-Lane-Queue/Adapter-Bauform, intent_event-
Spalte-vs-JSONB-Detailaufteilung, ModeStrategy-Registry-Innenstruktur, konkreter Wellen-Schnitt,
K3/K4-Fixes.

## Deferred Ideas
Editierbare Kern-Taxonomie (verworfen), objection_events-Zombifizierung (TAXO2), Scoring (TAXO2),
build_answer_context (TAXO3), Tabellen-Konsolidierung (spätere Phase), meeting_ext/training-Modi
(später), deploy.sh-Tor-Fix (STAGING), Prompt-Caching/Top-2 (Post-Launch).
