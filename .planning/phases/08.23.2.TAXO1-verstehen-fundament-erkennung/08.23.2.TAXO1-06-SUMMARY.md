---
phase: 08.23.2.TAXO1
plan: 06
status: superseded
superseded_date: 2026-06-18
result: cancelled-as-obsolete
requirements: [9]
req_disposition: resolved-by-deletion
---

# Plan 06 (ewb-v2-modular) — SUPERSEDED / CANCELLED (2026-06-18)

**Dieser Plan wurde NICHT gebaut.** Er wurde am 2026-06-18 bewusst als obsolet
gestrichen und durch eine Cleanup-Mini-Phase (`quick/20260618-ewb-deadcode-cleanup`)
ERSETZT, die gebaut + auf Production deployed ist.

## Warum gestrichen (3-Sichten-Befund 2026-06-18: André/Claudian + GSD + Gemini)

Der MEDFIX (2026-06-18, head 595d446) hat `analysiere_mit_claude` von `build_ewb_prompt`
auf `SYSTEM_PROMPT_BASE` umgestellt. Damit hatte die gesamte EWB-Prompt-Pipeline
(`build_ewb_prompt` + `resolve_prompt_version('ewb')`) **0 lebende Aufrufer** — bewiesen
per `grep` + Gemini-Gegencheck + `inspect.sh` auf Production.

Plan 06 hätte tote DB-Daten (die `ewb` `prompt_versions`-Rows v1-legacy/v2-modular)
umgeschaltet — der Log `[EWB] vv2-modular assembled` kann nach dem MEDFIX nie mehr
erscheinen, weil der Pfad, der ihn emittierte, tot ist. Ein Varianten-Schalter auf
totem Code ist sinnlos.

## Was stattdessen passierte (gebaut + LIVE)

Cleanup-Mini-Phase, 3 atomare Commits + Migration 0018 (auf Production angewendet):

- **951ad68** — `refactor(ewb-cleanup): toten build_ewb_prompt-Pfad entfernen (Task 1+2)`
  (`services/ewb_pipeline.py` komplett gelöscht, tote Importe + verwaiste per-SID-Felder raus)
- **4d889c7** — `refactor(ewb-cleanup): ewb prompt_versions-Rows + Seeder entfernen (Task 4)`
  (`app.py _seed_ewb_v2()` raus + **Migration 0018** `0018_drop_ewb_prompt_versions.py`,
  down_revision 0017, DELETE der 2 `ewb`-Rows, reversibel + idempotent)
- **3289484** — `test(ewb-cleanup): ewb-Pipeline-Tests entfernen + Stale-Refs nachziehen (Task 3)`

**Production-Stand:** Migration 0018 angewendet (die zwei `ewb` `prompt_versions`-Rows
gelöscht), Deploy live verifiziert. Die Live-EWB-Antwort läuft über
`streame_auto_variante` / `streame_manual_ewb_variante` (nicht über das gelöschte
Varianten-System).

## Requirement-Disposition

**REQ 9 (ewb-Varianten-Frage v1-legacy vs v2-modular) = aufgelöst-durch-Löschung.**
Das ewb-Prompt-Varianten-System existiert nicht mehr. Die Antwort-QUALITÄT (gutes
Antwort-Paradigma, Rollen-Bewusstsein) ist **TAXO3-Scope**, NICHT ein
Prompt-Varianten-Schalter in TAXO1.

## Beleg

- `grep -rn "build_ewb_prompt" services/ routes/ app.py` == 0 lebende Aufrufer
  (nur Aufräum-Kommentare in claude_service.py:24/517/519, app.py:1226)
- `alembic/versions/0018_drop_ewb_prompt_versions.py` vorhanden + auf Prod angewendet
- Voller Hergang: `Nerve-Vault/05 Log` Eintrag 2026-06-18
- Quick-SUMMARY: `.planning/quick/20260618-ewb-deadcode-cleanup/`
