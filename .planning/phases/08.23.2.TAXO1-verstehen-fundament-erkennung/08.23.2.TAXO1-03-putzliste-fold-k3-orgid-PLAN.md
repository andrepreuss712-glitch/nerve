---
phase: 08.23.2.TAXO1
plan: 03
type: execute
wave: 3
complexity: 🔴
depends_on: [01, 02]
files_modified: [services/claude_service.py, services/einwand_keyword_matcher.py, services/ki_logik.py, services/live_session.py, tests/test_k3_phase_coercion.py]
autonomous: false
requirements: [8, 4]
risiko: riskant
test_anruf: true
user_setup: []
must_haves:
  truths:
    - "Alle 12 §0.1-Punkte lesen+schreiben aus DER EINEN per-SID-Quelle (live_session._session_state[sid]['state']); alte globale Lese-/Schreib-Pfade GELOESCHT (nicht stehengelassen)."
    - "current_phase ist durchgaengig int; detect_phase wirft den int-vs-str-Fehler (K3) nicht mehr."
    - "tests/test_k3_phase_coercion.py beweist per pure-logic Regression-Assertion (server-seitig lauffaehig): detect_phase mit String-current_phase ('2') wirft KEINEN TypeError und liefert dasselbe Ergebnis wie int (2) — Cross-AI Finding #4, schuetzt die K3-Coercion vor Regression."
    - "org_id (K8) wird explizit per-SID durch die emittierenden Bahnen durchgereicht; org_id im Kosten-Log eines Live-Calls ist nicht NULL."
    - "Kein user_id-leer-Warnung mehr nach Live-Call (user_id global-read :452 geloescht, per-SID gelesen)."
    - "kw_fired_for_line wird per-SID geschrieben (matcher) + gelesen (claude); globaler Write:253 + Fallback-Read geloescht (Doppel-Feuer-Schutz lebt wieder)."
    - "Bei den noten-nahen Werten (kaufbereitschaft, readiness_*, score_factors_seen) wird NUR der WRITE per-SID-konsistent; KEIN neuer Scoring-Algorithmus (TAXO2)."
  artifacts:
    - path: "services/claude_service.py"
      provides: "per-SID-Lesung statt global fuer user_id/current_phase/cold_call_inference/last_einwand_typ/anrede; org_id explizit an log_api_cost; alte Globale geloescht"
      contains: "_session_state"
    - path: "services/einwand_keyword_matcher.py"
      provides: "kw_fired_for_line per-SID-Write (statt global :253)"
      contains: "kw_fired_for_line"
    - path: "services/ki_logik.py"
      provides: "detect_phase int-Erzwingung am Funktionseingang (K3-Defense-in-Depth)"
      contains: "int(current_phase)"
    - path: "tests/test_k3_phase_coercion.py"
      provides: "Pure-logic Regression-Test (Finding #4): detect_phase mit String-current_phase wirft keinen TypeError, int/str-Parität"
      contains: "def test_"
  key_links:
    - from: "claude_service.py:452 (user_id global-read, GELOESCHT)"
      to: "per-SID _session_state[sid]['user_id']"
      via: "per-SID-Lesung (Muster claude:1262)"
      pattern: "_session_state\\[.*\\].*user_id"
    - from: "claude_service.py log_api_cost (user_id=None HARDCODED, ERSETZT)"
      to: "explizit durchgereichte per-SID org_id/user_id/session_id"
      via: "log_api_cost(session_id=sid, org_id=..., user_id=...)"
      pattern: "log_api_cost\\(.*session_id"
    - from: "detect_phase Funktionseingang"
      to: "current_phase als int"
      via: "int(current_phase) Coercion"
      pattern: "int\\(current_phase\\)"
---

> RISIKO-BANNER: **riskant.** Faltet den Live-Loop (Daemon-Thread analyse_loop), LOESCHT alte globale Pfade, fixt K3, reicht org_id durch. → **Test-Anruf PFLICHT (D-03, alle 3 Auflagen).** Heaviste Punkt-14-Arbeit der Phase (jede der 12 Stellen = 4-Schichten-Audit + Race-Fragen).

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Geplant, nicht sofort ausgefuehrt. Deploy + Test-Anruf erst nach Cross-AI-Clearance + André-Ineinandergreifen-Check.

> 🔎 **VOR-EXECUTE-RE-GREP PFLICHT (RESEARCH "Valid until" + Open Question 1/A1):** Live-Loop-Code aendert sich schnell. Vor dem Bau erneut `grep -rn "current_phase\|user_id\|kw_fired_for_line\|cold_call_inference\|org_id\|last_einwand_typ\|anrede\|score_factors_seen\|kaufbereitschaft\|readiness" services/claude_service.py services/einwand_keyword_matcher.py services/live_session.py services/ki_logik.py services/cost_tracker.py` laufen; die Zeilennummern unten (RESEARCH §7/§9, Stand 2026-06-10) gegen den aktuellen Stand abgleichen. **A1 ungeloest:** wo schreibt etwas current_phase als String? — vollstaendigen current_phase-Grep + DB-/JSON-Lade-Pfad finden, BEVOR der int-Fix gesetzt wird (nicht auf kaputtem Pfad neu bauen).

<objective>
Die Single-Source-Putzliste (§0.1) komplett falten (REQ 8): alle 12 verifizierten Stellen aus RESEARCH §7 auf DIE EINE per-SID-Quelle (`live_session._session_state[sid]['state']`) umstellen und die alten globalen Pfade LOESCHEN. Dabei K3 aufloesen (current_phase durchgaengig int → detect_phase wirft den TypeError nicht mehr, REQ 4-Teil) und org_id (K8) explizit per-SID durch die Bahnen durchreichen statt auf den globalen Resolver-Scan zu hoffen.

Purpose: §0.1 (Geruest): jedes Datum hat GENAU EINE Quelle; parallele Pfade sind die Wurzel der Drift (Speech-Stats-Bug, K1/K8 deployed-aber-tot). Diese Welle muss VOR dem Cutover (Welle 4) laufen — sonst baut der Cutover auf kaputten Globalen auf (RESEARCH §10 Reihenfolge-Begruendung).

Output: konsolidierte per-SID-Lesungen + geloeschte Globale in claude_service.py / einwand_keyword_matcher.py / live_session.py; detect_phase int-Coercion in ki_logik.py; org_id-Durchreichung. Deploy + Test-Anruf (Daten-Fluss-Beleg) — GATED.

**Rider (CONTEXT D-01/SPEC REQ 8):** Bei noten-nahen Werten (kaufbereitschaft, readiness_*, score_factors_seen) NUR den WRITE per-SID-konsistent machen — KEIN Scoring-Redesign (TAXO2 entscheidet ob sie ueberleben oder von rubric_score abgeloest werden).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-RESEARCH.md
@services/claude_service.py
@services/einwand_keyword_matcher.py
@services/live_session.py
@services/ki_logik.py
@services/cost_tracker.py

<interfaces>
<!-- §0.1-Putzliste verbatim aus RESEARCH §7 (12 Punkte, Stand 2026-06-10 — VOR Bau re-greppen!): -->
<!-- 1 user_id: per-SID write (deepgram setzt _session_state[sid]['user_id']) / GLOBAL read claude:452 (sid=None) → global-read loeschen, per-SID lesen (Muster claude:1262). -->
<!-- 2 current_phase: per-SID write claude:1012 / GLOBAL read claude:1085 → global-read auf per-SID; int erzwingen (loest K3). Korrektes per-SID-Read-Muster: claude:1061/1259-1267. -->
<!-- 3 cold_call_inference: per-SID write claude:1071 / GLOBAL read claude:1086 → global-read auf per-SID. -->
<!-- 4 kw_fired_for_line: REVERSE — GLOBAL write matcher:253 / per-SID read claude:1265 → matcher MUSS per-SID schreiben; globaler write:253 + fallback-read:1273 loeschen. -->
<!-- 5 score_factors_seen: global rw claude:1084 → per-SID write (Rider: nur WRITE, Scoring=TAXO2). -->
<!-- 6 last_einwand_typ: GLOBAL write claude:1148 / GLOBAL read claude:1145 → per-SID. -->
<!-- 7 kaufbereitschaft: global write claude:982 → per-SID write (Rider). -->
<!-- 8 readiness_score/bucket: global → per-SID write (Rider). -->
<!-- 9 current_phase_name/phase_confidence: per-SID claude:1013/1018 / global Geister-Fallback → echte einzige per-SID-Quelle oder weg. -->
<!-- 10 precall_briefing: lebt korrekt per-SID (live_session:260/266) → verify globaler Fallback wirklich weg (Inventur-Korrektur: kein Fix noetig, nur Beleg). -->
<!-- 11 anrede: per-SID claude:1266 / GLOBAL fallback claude:1274 → global-fallback:1274 loeschen. -->
<!-- 12 org_id (K8): per-SID write live_session:306 / global resolver-scan im Daemon-Thread trifft Session nicht zuverlaessig. claude:489/492/499/503 log_api_cost(user_id=None HARDCODED). → org_id/user_id/session_id EXPLIZIT durchreichen. -->
<!-- K3-Wurzel (RESEARCH §3/§9): ki_logik.py:178 `if raw_phase > current_phase:` → int>str TypeError wenn current_phase="1"; gefangen im inneren except claude:1075 (8× in 14 Tagen, Phase still tot). Defense-in-Depth: int(current_phase) am detect_phase-Eingang. -->
<!-- cost_tracker: _resolve_org_id_from_live_session (cost_tracker.py:51-62) scannt aktive Session; laeuft nur wenn user_id/org_id is None (cost_tracker:95-97). Im Daemon-Thread unzuverlaessig → explizit durchreichen schlaegt Resolver-Scan. -->
<!-- live_session._session_state[sid]['state'] = kanonische per-SID-Quelle (Muster claude:1259-1267). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: K3 + current_phase/cold_call_inference/phase_name auf per-SID + int-Erzwingung (ki_logik.py + claude_service.py)</name>
  <read_first>
    - ⚠️ RE-GREP zuerst (Banner oben), A1 current_phase-String-Quelle finden
    - services/ki_logik.py:161-205 (detect_phase, insb. :176/:178 int-vs-str) — 30 Zeilen vor/nach VERBATIM
    - services/claude_service.py:992 / :1012 / :1061 / :1075 / :1085 / :1086 / :1013 / :1018 (current_phase + cold_call_inference + phase_name Lese-/Schreib-Stellen) — 30 Zeilen vor/nach JEDER VERBATIM
    - RESEARCH §3 (K3-Diagnose) + §7 Punkte 2/3/9
  </read_first>
  <action>
    Punkt-14-Inserts/Loeschungen. Pro Stelle Control-Flow + Skelett + Cross-File aus Sektionen 1-3 unten ableiten + beim Lesen verifizieren.

    (a) **K3 Defense-in-Depth (ki_logik.py detect_phase):** am Funktionseingang `current_phase = int(current_phase)` (robust gegen String-Kontamination), BEVOR Zeile :176/:178-Vergleiche laufen. Falls current_phase None/leer sein kann: `int(current_phase or 1)`. → der `>`-Vergleich (:178) bekommt nie mehr str.
    (b) **A1-Wurzel:** zusaetzlich die gefundene String-Schreib-/Lade-Quelle (Re-Grep) auf int normalisieren — current_phase wird NUR als int in die per-SID-Quelle geschrieben. Falls A1-Quelle ein DB/JSON-Roundtrip ist: an der Lade-Stelle `int(...)` coercen.
    (c) **current_phase global-read loeschen (claude:1085):** die globale Lesung `ls.state.get('current_phase')` ersetzen durch per-SID-Lesung aus `_session_state[sid]['state']` (Muster claude:1061/1259-1267). Globalen Lese-Pfad LOESCHEN (nicht auskommentieren).
    (d) **cold_call_inference global-read loeschen (claude:1086):** analog auf per-SID umstellen.
    (e) **current_phase_name/phase_confidence (claude:1013/1018 per-SID write):** Geister-Global-Fallback aufloesen — entweder echte einzige per-SID-Quelle nutzen oder den toten Fallback loeschen (Beleg: wird der Global-Read ueberhaupt erreicht? grep).
    KEINE Scoring-Logik anfassen. detect_phase-Signatur unveraendert (nur Eingangs-Coercion). Der innere except (claude:1075) bleibt als Sicherheitsnetz, soll aber nicht mehr getriggert werden.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('services/ki_logik.py').read()); ast.parse(open('services/claude_service.py').read())"` lokal erlaubt. Funktional via Test-Anruf (Task 5): logs-errors ohne phase_classify int/str.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "int(current_phase" services/ki_logik.py` 1 Treffer (Eingangs-Coercion).
    - Globale current_phase/cold_call_inference-Lesungen (RESEARCH :1085/:1086) sind WEG: `grep -n "ls.state.get('current_phase')\|ls.state.get('cold_call_inference')" services/claude_service.py` == 0 (nur per-SID-Lesung bleibt).
    - per-SID-Lesung vorhanden: `grep -n "_session_state\[.*\].*current_phase\|_session_state\[.*\]\['state'\]" services/claude_service.py` zeigt die neue Lesung.
    - AST-Parse exit 0 fuer beide Dateien.
  </acceptance_criteria>
  <done>K3 aufgeloest (int-erzwungen), current_phase/cold_call_inference/phase_name single-source per-SID, alte Globale geloescht.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: user_id + kw_fired_for_line + last_einwand_typ + anrede auf per-SID; Globale loeschen (claude_service.py + einwand_keyword_matcher.py)</name>
  <read_first>
    - ⚠️ RE-GREP zuerst
    - services/claude_service.py:452 (user_id global-read sid=None) / :1262 (korrektes per-SID-Muster) / :1145/:1148 (last_einwand_typ) / :1265/:1266/:1273/:1274 (kw_fired_for_line + anrede) — 30 Zeilen vor/nach VERBATIM
    - services/einwand_keyword_matcher.py:253 (kw_fired_for_line GLOBAL write) — 30 Zeilen vor/nach VERBATIM
    - RESEARCH §7 Punkte 1/4/6/11
  </read_first>
  <action>
    Punkt-14-Inserts/Loeschungen.
    (a) **user_id (claude:452):** globale Lesung `ls.state.get('user_id')` (sid=None-Pfad) ersetzen durch per-SID-Lesung `_session_state[sid]['user_id']` (Muster claude:1262). Globalen :452-Pfad LOESCHEN. → keine `user_id leer`-Warnung mehr (REQ 8 Acceptance).
    (b) **kw_fired_for_line REVERSE-Fix (matcher:253):** der Matcher schreibt heute global `_ls.state['kw_fired_for_line']`. Umstellen auf per-SID: `_session_state[sid]['state']['kw_fired_for_line']` (gleiche Quelle die claude:1265 liest). Globalen Write:253 LOESCHEN. In claude_service.py den Fallback-Read :1273 (globaler Fallback) LOESCHEN — nur per-SID-Read :1265 bleibt. → Doppel-Feuer-Schutz lebt wieder. **Achtung Sprecher-/SID-Verfuegbarkeit:** der Matcher muss die sid kennen — pruefen ob sie im Matcher-Kontext verfuegbar ist (Re-Grep); falls nicht, sid durchreichen (gleiche Faedel-Logik wie org_id Task 4).
    (c) **last_einwand_typ (claude:1145 read / :1148 write):** beide global → auf per-SID (`_session_state[sid]['state']['last_einwand_typ']`). Globale rw LOESCHEN.
    (d) **anrede (claude:1266 per-SID / :1274 global-fallback):** globalen Fallback :1274 LOESCHEN — nur per-SID :1266 bleibt.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal erlaubt. Funktional via Test-Anruf (Task 5): logs ohne user_id-leer; Doppel-Feuer-Schutz greift.</automated>
  </verify>
  <acceptance_criteria>
    - user_id global-read weg: `grep -n "ls.state.get('user_id')" services/claude_service.py` == 0; per-SID-Lesung vorhanden.
    - kw_fired_for_line: `grep -n "kw_fired_for_line" services/einwand_keyword_matcher.py` zeigt per-SID-Write (`_session_state`), KEIN `_ls.state['kw_fired_for_line']`-Global mehr.
    - last_einwand_typ + anrede globale Pfade weg: `grep -n "ls.state\['last_einwand_typ'\]\|ls.state.get('anrede')" services/claude_service.py` == 0.
    - AST-Parse exit 0 fuer beide Dateien.
  </acceptance_criteria>
  <done>user_id/kw_fired_for_line/last_einwand_typ/anrede single-source per-SID, alte Globale geloescht, Doppel-Feuer-Schutz reaktiviert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Noten-nahe Werte (score_factors_seen, last_einwand-Klasse, kaufbereitschaft, readiness_*) — NUR WRITE per-SID-konsistent (Rider, KEIN Scoring-Redesign)</name>
  <read_first>
    - ⚠️ RE-GREP zuerst
    - services/claude_service.py:982 (kaufbereitschaft) / :1084 (score_factors_seen) + readiness-Schreibstellen — 30 Zeilen vor/nach VERBATIM
    - services/ki_logik.py:95 (compute_readiness_score — UNVERAENDERT lassen) / RESEARCH §1 Rider + §7 Punkte 5/7/8
    - CONTEXT D-01 Rider + SPEC REQ 8 Rider (nur WRITE per-SID, Scoring-Design=TAXO2)
  </read_first>
  <action>
    Punkt-14. **Rider strikt:** NUR die WRITE-Stellen per-SID-konsistent machen, KEINE Score-Formel aendern.
    (a) **kaufbereitschaft (claude:982):** globaler Write `ls.state['kaufbereitschaft']` → per-SID `_session_state[sid]['state']['kaufbereitschaft']`.
    (b) **score_factors_seen (claude:1084):** globaler rw → per-SID WRITE. compute_readiness_score (ki_logik:95) liest score_factors_seen — der Leser bleibt unveraendert in der Formel, liest aber jetzt die per-SID-Quelle (sicherstellen dass der Read-Pfad die per-SID-Quelle trifft, sonst liest er leer). **Rider-Grenze:** falls der Reader strukturell global liest und nicht trivial per-SID umstellbar ist OHNE Scoring-Logik zu beruehren → dokumentieren + minimal-invasiv halten (nur Container-Quelle umbiegen, Formel unangetastet).
    (c) **readiness_score/bucket:** WRITE per-SID. Formel (ki_logik:95 / app_routes:735, RESEARCH §1) UNVERAENDERT.
    KEIN neuer Scoring-Algorithmus. KEIN handling_score. Reine Container-Quellen-Umstellung der Writes.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Task 5): Readiness bewegt sich (Daten-Fluss D-03c).</automated>
  </verify>
  <acceptance_criteria>
    - kaufbereitschaft/score_factors_seen globale Writes weg: `grep -n "ls.state\['kaufbereitschaft'\]\s*=\|ls.state\['score_factors_seen'\]\s*=" services/claude_service.py` == 0; per-SID-Writes vorhanden.
    - compute_readiness_score-Formel unveraendert: `git diff services/ki_logik.py` zeigt KEINE Aenderung an der Score-Berechnung (nur ggf. Read-Quelle).
    - KEIN neuer Score-Code: `grep -iE "def.*score|rubric|handling_score" services/claude_service.py` zeigt keine NEUE Berechnung gegenueber Vorher.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Noten-nahe Werte schreiben per-SID; Scoring-Logik unangetastet (Rider erfuellt, TAXO2-Grenze respektiert).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: org_id (K8) explizit per-SID durch die Bahnen durchreichen an log_api_cost (claude_service.py + cost_tracker-Aufrufe)</name>
  <read_first>
    - ⚠️ RE-GREP + `inspect.sh schema api_cost_log` (A6 — org_id/user_id/session_id-Spalten verifizieren, Punkt 21)
    - services/claude_service.py:489/492/499/503 (log_api_cost(user_id=None HARDCODED, context_tag='live_haiku')) — 30 Zeilen vor/nach VERBATIM
    - services/cost_tracker.py:51-62 (_resolve_org_id_from_live_session) / :95-97 (Scan-Trigger nur wenn None)
    - RESEARCH §7 org_id-Abschnitt
  </read_first>
  <action>
    Punkt-14. **Wurzel (RESEARCH §7):** der Daemon-Thread (analyse_loop) ruft log_api_cost mit `user_id=None` hardcoded → der Resolver-Scan laeuft zwar, trifft die aktive Session im Daemon-Thread aber unzuverlaessig (Multi-SID-Ambiguitaet). Fix: org_id/user_id/session_id EXPLIZIT durchreichen statt auf den Scan zu hoffen.
    - An den emittierenden Stellen (claude:489/492/499/503) den per-SID-Kontext fadeln: die sid ist im analyse_loop pro Iteration bekannt (per-SID-State) → `uid = _session_state[sid]['user_id']`, `oid = _session_state[sid].get('org_id')` (per-SID write lebt live_session:306) lesen und an `log_api_cost(session_id=sid, user_id=uid, org_id=oid, ...)` uebergeben.
    - `user_id=None`-Hardcode ERSETZEN durch den per-SID-Wert. session_id mitgeben (cost_tracker kann es als Fallback nutzen, aber der explizite org_id schlaegt den Scan).
    - cost_tracker.py: der Resolver-Scan (51-62) bleibt als Fallback; er wird seltener gebraucht weil explizit durchgereicht. KEINE Loeschung des Resolvers (er fungiert als Sicherheitsnetz), aber der explizite Pfad muss Vorrang haben (cost_tracker:95-97 — wenn org_id != None uebergeben, scannt es nicht).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Task 5): org_id im Kosten-Log ≠ NULL.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "log_api_cost(" services/claude_service.py` zeigt KEINEN `user_id=None`-Hardcode mehr an den live_haiku-Stellen; stattdessen per-SID uid/oid + session_id=sid.
    - `grep -n "session_id=" services/claude_service.py` deckt die log_api_cost-Aufrufe ab.
    - api_cost_log-Schema-Beleg (Punkt 21): `inspect.sh schema api_cost_log` verbatim in SUMMARY; org_id/user_id/session_id-Spalten bestaetigt.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>org_id/user_id/session_id explizit per-SID durchgereicht; Kosten-Zuordnung trifft im Daemon-Thread (K8 geloest).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: tests/test_k3_phase_coercion.py — K3 pure-logic Regression-Test (int-vs-str, Cross-AI Finding #4)</name>
  <read_first>
    - CLAUDE.md "Test-Qualitaets-Regel" (Function-Call-Return-Assertion, KEIN Source-Presence/getsource)
    - services/ki_logik.py detect_phase (echte Signatur — Pflichtparameter ablesen, um einen minimalen Aufruf zu bauen) + die in Task 1(a) gesetzte `int(current_phase ...)`-Coercion
    - tests/ Bestands-Muster (z.B. test_schild_guard.py: server-seitig lauffaehig, sauberes Skip-Muster wenn Abhaengigkeit fehlt)
  </read_first>
  <action>
    NEUE Datei `tests/test_k3_phase_coercion.py`. Pure-logic Regression (KEIN Source-Presence, KEIN Live-Call, KEINE DB) — testet NUR die K3-Coercion in detect_phase:
    - `def test_detect_phase_accepts_string_current_phase():` — detect_phase mit `current_phase="2"` (String, wie der K3-Bug ihn lieferte) aufrufen; assert KEIN TypeError (`'>' not supported between 'int' and 'str'`) wird geworfen. Die uebrigen Pflicht-Parameter aus der echten Signatur minimal befuellen (read_first), so dass der `>`-Vergleich (ki_logik:178) erreicht wird.
    - `def test_detect_phase_int_str_parity():` — detect_phase mit `current_phase="3"` (str) und `current_phase=3` (int) bei sonst identischen Argumenten aufrufen; assert dass das Ergebnis IDENTISCH ist (die Coercion macht str==int aequivalent).
    - `def test_detect_phase_none_current_phase_seeds_one():` — falls `int(current_phase or 1)` gewaehlt wurde (Task 1a Edge 2): `current_phase=None` → kein Crash, Seed 1.
    Server-seitig via pytest lauffaehig (`/opt/nerve/venv`); sauberer Skip wenn ein Import-Dependency fehlt (kein False-Green). KEIN Mock von detect_phase selbst (echter Function-Call).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('tests/test_k3_phase_coercion.py').read())"` lokal erlaubt. Lauf server-seitig via Checkpoint: `ssh ... pytest tests/test_k3_phase_coercion.py -x`.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def test_detect_phase_accepts_string_current_phase" tests/test_k3_phase_coercion.py` 1 Treffer.
    - Function-Call-Assertion (echter detect_phase-Aufruf), KEIN Source-Presence: `grep -E "getsource|hasattr|open\(.*ki_logik.*\)\.read" tests/test_k3_phase_coercion.py` == 0.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>K3-Coercion durch pure-logic Regression-Test abgesichert (str-Input kein TypeError, int/str-Parität); server-seitig lauffaehig.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Alle 12 §0.1-Punkte auf per-SID gefaltet, alte Globale geloescht, K3 int-erzwungen + K3-Regression-Test (Finding #4), org_id durchgereicht. Bereit fuer Deploy + Test-Anruf (riskante Welle, D-03).</what-built>
  <how-to-verify>
    ⚠️ **GATE: Deploy + Test-Anruf ERST NACH Cross-AI-Clearance + André-Ineinandergreifen-Check.** Nach Clearance fuehrt André aus:

    1. `git add -A && git commit && git push origin main` → `bash deploy.sh production` (Pytest-Gate vor Restart).
    1b. **K3-Regression-Test server-seitig (Finding #4):** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_k3_phase_coercion.py -x -v'` → **GRUEN** (str-current_phase kein TypeError, int/str-Parität). Verbatim-Output in SUMMARY.
    2. **TEST-ANRUF auf Production mit Test-User (D-03 Auflage 3 — DATEN-FLUSS, nicht nur "Anruf ok"). Beide Modi: cold_call UND meeting.** Waehrend/nach dem Anruf belegen:
       - **(a) K3 weg:** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs-errors 200'` → KEIN `[phase_classify] loop error` / `'>' not supported between 'int' and 'str'`. Phase bewegt sich (Daten-Fluss).
       - **(b) user_id-Warnung weg:** `ssh ... 'bash scripts/inspect.sh logs 300'` → KEINE `user_id leer`-Warnung mehr.
       - **(c) org_id gesetzt (K8):** `ssh ... 'bash scripts/inspect.sh sample api_cost_log 20'` → die Live-Call-Kosten-Zeilen haben `org_id` ≠ NULL (und user_id ≠ NULL).
       - **(d) Readiness/Phase bewegen sich:** im Browser sichtbar + logs zeigen Phasenwechsel.
       - **(e) Doppel-Feuer-Schutz:** kw_fired_for_line verhindert Doppel-Cue fuer dieselbe Zeile (logs/UI kein doppelter Slot-A-Treffer).
       Verbatim-Belege (a)-(e) in SUMMARY. **Hinweis:** intent_event-Schreiben kommt erst Welle 4 — diese Welle belegt NICHT count(intent_event) (noch kein Writer), sondern die §0.1-/K3-/org_id-Daten-Fluesse.
    3. KEIN SCHILD-Guard noetig (keine Schema-Aenderung in dieser Welle).
  </how-to-verify>
  <resume-signal>"deployed + K3-Regression-Test GRUEN + Test-Anruf beide Modi OK: logs-errors ohne phase_classify, keine user_id-leer, org_id≠NULL, Phase/Readiness bewegen sich, kein Doppel-Cue" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 1. Lokaler Kontext
Insert-/Loesch-Sites (alle im Live-Loop, Daemon-Thread analyse_loop): claude_service.py :452, :982, :1012/:1013/:1018, :1061, :1075, :1084, :1085/:1086, :1145/:1148, :1262, :1265/:1266, :1273/:1274, :489/:492/:499/:503; einwand_keyword_matcher.py :253; ki_logik.py :176/:178 (detect_phase-Eingang). **Pflicht beim Bau:** je Stelle 30 Zeilen vor/nach VERBATIM lesen + alle return/continue/break/raise/early-if mit Zeilennummer listen (RE-GREP zuerst, Zeilen koennen gewandert sein). Besonders: die inner-except an claude:1075 faengt heute K3 — der int-Fix muss VOR :178 greifen, nicht im except. Bei jeder Loeschung pruefen: gibt es einen frueh-return zwischen per-SID-Write und global-read, der die Konsolidierung umgeht? (das war RESEARCH-Befund "split-brain").

## 2. Funktions-Skelett
- **detect_phase (ki_logik.py):** Eingang → [INSERT int-Coercion] → :176 `if raw_phase == current_phase: return` → :178 `if raw_phase > current_phase: ...` → finaler return. Der int-Cast sitzt VOR allen Vergleichen.
- **analyse_loop (claude_service.py, Daemon-Thread):** while-Loop pro SID → Haiku-Call → ergebnis-dict-Bau (Intent/Phase) → per-SID-Writes (:1012 etc.) → Globale-Reads (:1085/:1086/:1145 — werden GELOESCHT/umgestellt) → log_api_cost (:489 etc., user_id-Fix) → inner try/except (:1075). Alle Exit-Pfade beim Lesen lokalisieren; Loeschungen duerfen keinen return ueberspringen der einen per-SID-Write enthaelt.
- **matcher (einwand_keyword_matcher.py):** Keyword-Match-Pfad → :253 global-write (→ per-SID-write). sid-Verfuegbarkeit pruefen.

## 3. Cross-File-Awareness
- `grep -rn "current_phase" services/ routes/` — VOLLSTAENDIG (A1: String-Quelle finden). Erwartete Schreiber: claude:1012/:122/:335 (int). Leser: claude:992/:1061 (per-SID ok), :1085 (global, LOESCHEN). DB-/JSON-Lade-Pfad = A1-Verdacht. detect_phase (ki_logik) liest current_phase → int-Coercion schuetzt es.
- `grep -rn "kw_fired_for_line" services/` — matcher:253 (write, →per-SID), claude:1265 (read per-SID), :1273 (fallback-read, LOESCHEN). REVERSE-Muster: nach Fix beide per-SID.
- `grep -rn "user_id" services/claude_service.py services/cost_tracker.py` — :452 (global-read, LOESCHEN), :1262 (per-SID-Muster), :489 (=None hardcoded, FIX). cost_tracker:95-97 (Scan-Trigger).
- `grep -rn "org_id" services/cost_tracker.py services/live_session.py services/claude_service.py` — live_session:306 (per-SID write), cost_tracker:51-62 (resolver-scan, bleibt Fallback), claude:489 (durchreichen).
- **Cross-Thread-Pflicht:** analyse_loop + coaching_loop + qa_pipeline laufen als Daemon-Threads (app.py:2317/2318). Die per-SID-Quelle `_session_state[sid]['state']` wird von mehreren gelesen → die Konsolidierung muss sicherstellen dass kein Thread weiter den geloeschten Globalen liest. `grep -rn "ls.state.get('current_phase'\|ls.state\['kaufbereitschaft'\]\|ls.state.get('user_id')" services/` nach Bau == 0.

## 4. Edge-Case-Analyse
- **Edge 1 — sid fehlt im Matcher-Kontext (kw_fired_for_line per-SID-Write):** wenn der Matcher die sid nicht kennt, kann er nicht per-SID schreiben. Behandlung: sid durchreichen (gleiche Faedel-Logik wie org_id); falls strukturell unmoeglich → FLAG + Cross-AI (nicht raten).
- **Edge 2 — current_phase ist None beim int-Cast:** `int(None)` crasht. Behandlung: `int(current_phase or 1)` (Seed 1 ist korrekt, RESEARCH §3).
- **Edge 3 — per-SID-Quelle existiert noch nicht (Call-Start-Race):** Lesung aus `_session_state[sid]['state']` bevor init_session_state lief → KeyError. Behandlung: `.get('state', {}).get(key, default)`-Muster (wie korrektes per-SID-Read claude:1259-1267) statt harter Index.
- **Edge 4 — org_id per-SID noch nicht gesetzt:** `_session_state[sid].get('org_id')` → None → cost_tracker-Resolver-Scan-Fallback greift (bleibt als Netz). Kein Crash, schlimmstenfalls Scan statt explizit.

### Race-Condition-Audit (KRITISCH — Live-Loop in Daemon-Thread, Multi-SID, Punkt-14-Kernmaterial)
1. **Gleichzeitiges Feuern (2 Calls/2 SIDs parallel):** Genau DAS war die §0.1-Wurzel (globale Zaehler kollidieren ueber SIDs). Nach Konsolidierung schreibt/liest jeder Pfad `_session_state[sid][...]` → SID-isoliert, keine Cross-Session-Kollision mehr. Belastbar: das ist der Zweck der Welle. Innerhalb EINER SID schreibt nur der eine analyse_loop-Durchlauf (sequentiell pro SID) → kein Intra-SID-Race auf demselben Feld.
2. **Verbindungsabbruch mid-Operation:** bricht der Call mitten im analyse_loop ab, bleibt ein halb-geschriebener per-SID-State zurueck — wird bei Call-Ende (pop_session_state) aufgeraeumt. Kein globaler Rest mehr (Globale geloescht) → kein Cross-Session-Leak des halben Zustands. org_id-Durchreichung: bricht der Haiku-Call ab, wird log_api_cost evtl. nicht erreicht → keine Kosten-Zeile (kein NULL-org_id-Muell).
3. **Wiederverbinden mit altem Zustand:** neue SID bei Reconnect → frischer per-SID-State (init_session_state). Alter globaler Zustand existiert nicht mehr (geloescht) → kein Geister-Read aus alter Session. Das war vorher der Bug (global-read traf Fremd-Session).
4. **Tab im Hintergrund:** analyse_loop ist server-seitig, laeuft unabhaengig vom Tab-Fokus. Pausiert der Browser-Tab, pausiert nur die UI-Anzeige, nicht der Server-State. Kein veralteter Client-State speist die per-SID-Quelle (Server ist Quelle). Belastbar: keine Client-getriebene Schreibung der konsolidierten Felder.

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `api_cost_log` (SCHREIBEN via log_api_cost — org_id/user_id/session_id-Spalten). Punkt 21: Schema vor Festlegung verifizieren (A6).
- Die §0.1-Werte (current_phase, user_id, kw_fired_for_line, etc.) leben in **RAM** (`_session_state[sid]['state']`), NICHT in DB → Cross-Layer-Falle: das sind Code/RAM-Daten, keine DB-Spalten.

### inspect.sh-Beleg (Pflicht beim Bau, Task 4)
```
$ ssh ... 'bash scripts/inspect.sh schema api_cost_log'
[Output verbatim in Task-4-SUMMARY — org_id/user_id/session_id-Spalten + Typen bestaetigen]
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `current_phase` | per-SID write/read | RAM (_session_state[sid]['state']) — KEINE DB-Spalte | ⬜ grep: kein global-read; int-Coercion |
| `user_id` (live) | per-SID read (war global :452) | RAM (_session_state[sid]['user_id']) | ⬜ grep: :452 weg; logs ohne user_id-leer |
| `kw_fired_for_line` | per-SID write (matcher) + read (claude) | RAM (_session_state[sid]['state']) | ⬜ grep: matcher per-SID, fallback-read weg |
| `org_id` (K8) | per-SID read → log_api_cost durchgereicht | RAM (_session_state[sid]) → DB api_cost_log.org_id | ⬜ Test-Anruf: api_cost_log.org_id ≠ NULL |
| `score_factors_seen`/`kaufbereitschaft`/`readiness_*` | per-SID WRITE (Rider) | RAM (_session_state[sid]['state']) | ⬜ grep: globale Writes weg; Formel unveraendert |

### Bei Diskrepanz (api_cost_log hat keine org_id/session_id-Spalte / per-SID-Quelle existiert nicht): STOP + Re-Grep + Cross-AI, nicht raten.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Daemon-Thread (analyse_loop) → per-SID-State (RAM) | Multi-SID-Zugriff; SID-Isolation ist das Sicherheitsziel (§0.1). |
| Live-Loop → api_cost_log (DB) | Kosten-Zuordnung; org_id/user_id muessen korrekt durchgereicht sein (Multi-Tenancy/Access-Control, ASVS V4). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO1-07 | I (Info Disclosure) | global-read trifft Fremd-Session (Cross-Session-Leak ueber globale Zaehler) | mitigate | §0.1-Fold: alle Globale geloescht, SID-isolierte per-SID-Quelle; grep == 0 global-read; Test-Anruf 2 parallele SIDs sauber. |
| T-TAXO1-08 | R (Repudiation) / Access Control | org_id NULL → Kosten nicht zuordenbar (Multi-Tenancy-Luecke, K8) | mitigate | org_id/user_id explizit per-SID durchgereicht; Test-Anruf belegt api_cost_log.org_id ≠ NULL. |
| T-TAXO1-09 | D (DoS) | K3 int-vs-str crasht phase_classify still (Phase tot) | mitigate | int-Coercion am detect_phase-Eingang + A1-Quelle normalisiert; logs-errors-Beleg im Test-Anruf. |
</threat_model>

<verification>
- 12 §0.1-Punkte per-SID; alte Globale grep == 0.
- K3: int(current_phase) am Eingang; logs-errors ohne phase_classify-TypeError; K3-Regression-Test (test_k3_phase_coercion.py) GRUEN server-seitig (Finding #4).
- org_id durchgereicht; api_cost_log.org_id ≠ NULL im Test-Anruf.
- Noten-nahe Werte: nur WRITE per-SID, Scoring-Formel unveraendert (git diff ki_logik).
- Test-Anruf (cold_call + meeting): Daten-Fluss (a)-(e) belegt.
</verification>

<success_criteria>
- grep belegt fuer jeden §0.1-Punkt: kein global-read-neben-per-SID-write mehr (REQ 8).
- logs ohne user_id-leer-Warnung; org_id im Kosten-Log ≠ NULL (REQ 8).
- phase_classify wirft int-vs-str nicht mehr (REQ 4-Teil/K3); K3-Regression-Test GRUEN (Finding #4).
- Kein neuer Scoring-Algorithmus (Rider/Out-of-Scope TAXO2).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-03-SUMMARY.md` (inkl. grep-Belege "global-read weg" je Punkt + inspect.sh schema api_cost_log + K3-Regression-Test-GREEN-Output (Finding #4) + Test-Anruf-Daten-Fluss-Belege (a)-(e) + git-diff-Beleg dass Scoring-Formel unveraendert).
</output>
