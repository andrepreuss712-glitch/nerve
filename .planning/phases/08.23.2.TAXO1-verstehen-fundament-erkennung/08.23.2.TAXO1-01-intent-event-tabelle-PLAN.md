---
phase: 08.23.2.TAXO1
plan: 01
type: execute
wave: 1
complexity: 🔴
depends_on: []
files_modified: [database/models.py, alembic/versions/0016_intent_event_table.py, services/intent_taxonomy.py]
autonomous: false
requirements: [1, 2]
risiko: schema-addition
test_anruf: false
user_setup: []
must_haves:
  truths:
    - "public.intent_event existiert auf Production mit 9 Geruest-§3-Kern-indizierten Spalten + interaction_id (10 indiziert) + handling_status (11. indiziert, INTERLOCK I-1) + reaction_latency_ms + payload_jsonb (13 Spalten total), Owner nerve_app."
    - "Alle 4+3 Pflichtfelder (handling_score, was_correct, cue_fired, dimension_available, reaction_latency_ms, cue_visible/ui_state_hash, speaker_role+speaker_id) existieren als Spalte ODER dokumentiertes JSONB-Feld — leer/NULL in TAXO1."
    - "INTERLOCK I-1 (TAXO1↔TAXO2, Cross-AI Gemini 3.1 bestaetigt): intent_event traegt ab Tag 1 die Spalte handling_status (varchar(16), NOT NULL, server_default='pending', indiziert) — der TAXO2-Wurzel-Fix gegen die dreifach-ueberladene NULL. In TAXO1 leer/'pending' (wie handling_score_numeric); TAXO2 setzt scored/abstained/failed. Hier gebaut statt von TAXO2 additiv nachgezogen (TAXO1-Prinzip 'vollstaendiges Schema ab Tag 1' + leere Tabelle = kein Backfill noetig). KEIN Status-Schreib-Code in TAXO1."
    - "INTERLOCK I-2 (TAXO1↔TAXO2, DSGVO, Gemini 3.1 bestaetigt 'kritisch'): call_id ist ein HARTER FK auf calls(id) ON DELETE CASCADE (DD-01-Hauskonvention) — die A2-'lose FK'-Entscheidung wird revidiert. Grund: F-08-Lösch-Kette calls→intent_event→abstain_log(TAXO2, traegt gesprochenen Wortlaut) muss durchgehend cascaden; lose FK = DSGVO-Waisen nach Call-Löschung. Insert-Timing unkritisch (calls-Zeile existiert ab Call-Start, vor jedem Emit — Gemini bestaetigt)."
    - "intent_event traegt die indizierte Korrelations-Spalte interaction_id (UUID, nullable, Schild gesetzt) als Moment-Klammer (Cross-AI Gemini 3.1) — angelegt in dieser Welle, befuellt im Cutover (Plan 04); KEIN Lern-/Outcome-Code in TAXO1."
    - "KEIN Code-Pfad in TAXO1 berechnet/schreibt handling_score_numeric (Scoring = TAXO2)."
    - "intent_event + alle nicht-trivialen Spalten tragen ein Schild (Punkt 23); SCHILD-Guard server-seitig GRUEN."
    - "intent_event startet leer (count=0 vor erstem Live-Call)."
    - "services/intent_taxonomy.py definiert INTENT_TAXONOMY_V1 (11 Kern+Gemini-Werte) + TAXONOMY_VERSION='v1' als die EINE versionierte Quelle (D-01), aber KEIN Live-Writer in dieser Welle (nur Konstante + Validator)."
  artifacts:
    - path: "database/models.py"
      provides: "ORM-Model IntentEvent mit comment= an Tabelle + nicht-trivialen Spalten"
      contains: "class IntentEvent"
    - path: "alembic/versions/0016_intent_event_table.py"
      provides: "CREATE TABLE intent_event + Indizes + ALTER OWNER nerve_app + op.execute COMMENT-Schilder"
      contains: "intent_event"
    - path: "services/intent_taxonomy.py"
      provides: "INTENT_TAXONOMY_V1 + TAXONOMY_VERSION + is_valid_intent_type() (Kern-Wert ODER custom_objection_*-Praefix)"
      contains: "INTENT_TAXONOMY_V1"
  key_links:
    - from: "alembic/versions/0016 (down_revision)"
      to: "0015 (juengste Migration, Prod-HEAD verifiziert)"
      via: "Alembic-Revisions-Kette"
      pattern: "down_revision = '0015'"
    - from: "0016 CREATE TABLE intent_event"
      to: "pg_description public.intent_event (Schild)"
      via: "op.execute COMMENT ON TABLE/COLUMN"
      pattern: "COMMENT ON TABLE public.intent_event"
    - from: "IntentEvent.__table__"
      to: "Owner nerve_app"
      via: "ALTER TABLE ... OWNER TO nerve_app"
      pattern: "OWNER TO nerve_app"
---

> RISIKO-BANNER: **schema-addition.** Leere Tabelle, KEINE Live-Anbindung. → Auto-Check + MANUELLER SCHILD-Guard server-seitig. **KEIN Test-Anruf** (D-03 Auflage 1: reine Schema-Addition ohne Live-Eingriff darf ueberspringen). Auflage 2 greift trotzdem: Guard MANUELL gegen Prod-DSN.

> ⚠️ **CROSS-AI-GATE (🔴-Phase, CLAUDE.md GSD-Workflow-Pflichten):** Diese Phase wird geplant, NICHT sofort ausgefuehrt. Der Production-Deploy + Migrations-Apply (Task 3) laeuft ERST NACH Cross-AI-Peer-Review der gesamten TAXO1-Phase (+ ggf. `--reviews`-Replan) UND nach der André-Direktive (TAXO1/2/3 alle bis kurz vor Execute, dann Ineinandergreifen pruefen). Kein Sprung von Planning-Complete zu `bash deploy.sh production`.

<objective>
Die EINE zentrale Ereignis-Tabelle `public.intent_event` als Single Source of Truth fuer "was sagt der Kunde gerade" anlegen (REQ 1) — hybrides Schema (9 indizierte Spalten + payload_jsonb), alle 4+3 rueckwirkend-nicht-rekonstruierbaren Pflichtfelder ab Tag 1 (REQ 2, existieren, bleiben leer — KEIN Scoring-Code). Plus die EINE versionierte Taxonomie-Quelle `INTENT_TAXONOMY_V1` als Konstante (D-01), noch OHNE Live-Writer.

**Dies ist der verriegelte gemeinsame Vertrag mit TAXO2/TAXO3 (Geruest §3).** Das Schema wird JETZT vollstaendig entworfen — handling_score-Spalten existieren, bleiben NULL; Scoring kommt in TAXO2. Spaeter ergaenzen = Abrieb.

Purpose: Ohne diese Tabelle kann keine andere Welle etwas schreiben (Dependency-Zwang, D-02/CONTEXT). Ohne vollstaendiges Schema ab Tag 1 sind Coaching-Hub + Training-Flywheel spaeter unmoeglich (Geruest §3).

Output: ORM-Model IntentEvent (mit Schildern), Migration 0016 (CREATE + Indizes + ALTER OWNER + COMMENTs), services/intent_taxonomy.py (Konstante + Validator). Deploy + Verify auf Production — GATED auf Cross-AI-Clearance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-SPEC.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-CONTEXT.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-RESEARCH.md
@alembic/versions/0008_training_schema_foundation.py
@alembic/versions/0014_crm_user_preferences_rls.py

<interfaces>
<!-- Geruest §3 — DER verriegelte intent_event-Daten-Vertrag (gemeinsamer Klebstoff mit TAXO2/TAXO3): -->
<!-- Indizierte relationale Spalten (9): event_id, session_id, call_id, mode, timestamp, intent_type, phase, handling_score_numeric, confidence. -->
<!-- INTERLOCK I-1 (Zusatz-Spalte, indiziert): handling_status varchar(16) NOT NULL server_default='pending' — TAXO2-Wurzel-Fix (pending|scored|abstained|failed), leer in TAXO1, hier gebaut (kein TAXO2-Nachzieh-ALTER auf befuellter Tabelle). -->
<!-- INTERLOCK I-2 (call_id-FK revidiert): call_id = harter FK calls(id) ON DELETE CASCADE (DD-01) statt lose A2 — DSGVO-Lösch-Kette calls→intent_event→abstain_log muss cascaden. nullable=True bleibt. -->
<!-- payload_jsonb-Felder: source, inference_basis, taxonomy_version(Pflicht non-null), abstained, speaker_role, speaker_id, is_simulation, origin_type, source_context, outcome.resolution, resolved_at_event_id, superseded_by, inference_config_id. -->
<!-- Die 4+3 Pflichtfelder ab Tag 1 (RESEARCH §1 DDL-Empfehlung): handling_score→Spalte handling_score_numeric (NULL); reaction_latency_ms→Spalte; was_correct, cue_fired, dimension_available-Map, cue_visible, ui_state_hash, speaker_role, speaker_id → JSONB-Keys. -->
<!-- Korrelations-Spalte interaction_id (Cross-AI Gemini 3.1, Quell-Anker Soll-Verhalten §6): UUID, nullable, INDIZIERT — Moment-Klammer ueber alle Emits (Fast/Medium/Button) eines Kundenmoments. event_id zu fein (pro Emit), call_id zu grob (pro Call). Typ = UUID_TYPE (models.py:12, identisch zu call_id). Nullable + KEIN harter FK (suggestion_reactions/Phase H ist FK-Quelle, nicht intent_event). Befuellung im Cutover (Plan 04); hier nur Spalte+Index+Schild. -->
<!-- interaction_id endet auf _id (L-04 trivial-by-suffix → Guard verlangt KEIN Schild), bekommt aber wie call_id bewusst ein Schild (dokumentiert Klammer-Zweck); Guard bleibt GRUEN (zusaetzliches Schild ist nie ein Guard-Fehler). -->

<!-- INTENT_TAXONOMY_V1 (Geruest §1, 11 Werte): echter_einwand, vorwand, reflexeinwand, kaufsignal, aufschub, info_frage, gatekeeper, wettbewerber_referenz, hard_opt_out, commitment, meta_kommunikation. custom_objection_* = Praefix-Match (KEIN Enum-Eintrag). TAXONOMY_VERSION='v1'. -->
<!-- Prod-HEAD = 0015 (RESEARCH §1 inspect.sh verbatim) → neue Migration revision='0016', down_revision='0015'. -->
<!-- Migration als postgres (nerve_app hat kein CREATE auf public); danach ALTER TABLE intent_event OWNER TO nerve_app (RESEARCH §1). -->
<!-- op.execute COMMENT-Muster: hauseigen, alle Bestands-Migrationen; 0008/0014 als Vorlage; SCHILD-04-Praezedenz. -->
<!-- intent_event existiert NICHT auf Prod (RESEARCH §1: "ERROR: relation intent_event does not exist") → reine Neu-Tabelle, startet leer. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: services/intent_taxonomy.py — die EINE versionierte Taxonomie-Quelle (Konstante + Validator, KEIN Live-Writer)</name>
  <read_first>
    - RESEARCH §4 (D2-Migration, INTENT_TAXONOMY_V1-Empfehlung, custom_objection_*-Praefix)
    - CONTEXT D-01 (Kern fest im Code, versioniert, nicht user-editierbar; Erweiterung nur custom_objection_*)
    - Geruest §1 (11 Werte verbatim oben im interfaces-Block)
    - services/profile_schema.py:45 (alter EinwandTypEnum — NICHT loeschen in dieser Welle, nur neue Quelle anlegen; Loeschung = Welle 4)
  </read_first>
  <action>
    NEUE Datei `services/intent_taxonomy.py` (kein existierender Control-Flow → Punkt 14 nicht anwendbar, neue Datei). Inhalt:
    - `TAXONOMY_VERSION = "v1"` (Modul-Konstante, Pflicht non-null an jedem spaeteren Insert).
    - `INTENT_TAXONOMY_V1: tuple[str, ...] = (` exakt diese 11 Werte (ASCII-Identifier, Code-Konstante): `"echter_einwand", "vorwand", "reflexeinwand", "kaufsignal", "aufschub", "info_frage", "gatekeeper", "wettbewerber_referenz", "hard_opt_out", "commitment", "meta_kommunikation"` `)`.
    - `CUSTOM_PREFIX = "custom_objection_"`.
    - `def is_valid_intent_type(value: str) -> bool:` return `value in INTENT_TAXONOMY_V1 or (isinstance(value, str) and value.startswith(CUSTOM_PREFIX) and len(value) > len(CUSTOM_PREFIX))`.
    - `def all_core_intents() -> tuple[str, ...]:` return INTENT_TAXONOMY_V1 (fuer Tests/Prompt-Bau in Welle 4).
    KEIN Import von claude_service/live_session (Single-Source-Konstante, abhaengigkeitsfrei). KEINE Scoring-Logik. KEIN Live-Writer — diese Welle legt nur die Quelle, Welle 4 verdrahtet sie.
    Modul-Docstring: "Geteilte Intent-Taxonomie (Geruest §1, D-01). Versionierter Code-Vertrag, NICHT user-editierbar. User-Erweiterung nur via custom_objection_*-Praefix pro Profil. Geschrieben in intent_event.intent_type (Welle 4)."
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('services/intent_taxonomy.py').read())"` lokal erlaubt (reine Syntax). Funktional in Welle 4 verdrahtet/getestet.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INTENT_TAXONOMY_V1" services/intent_taxonomy.py` >= 1; alle 11 Werte als grep-Treffer vorhanden (`grep -E "echter_einwand|hard_opt_out|meta_kommunikation" services/intent_taxonomy.py`).
    - `grep "TAXONOMY_VERSION = \"v1\"" services/intent_taxonomy.py` 1 Treffer.
    - `grep "def is_valid_intent_type" services/intent_taxonomy.py` 1 Treffer; Validator akzeptiert Kern-Wert UND custom_objection_<x>.
    - KEIN `import` von claude_service/live_session in der Datei (`grep -E "claude_service|live_session" services/intent_taxonomy.py` == 0).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Die EINE versionierte Taxonomie-Quelle existiert als abhaengigkeitsfreie Konstante (D-01). Noch kein Live-Writer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ORM-Model IntentEvent in models.py (hybrid, 9 Spalten + payload_jsonb, alle Pflichtfelder, Schilder)</name>
  <read_first>
    - database/models.py (Bestands-Model-Muster, Base-Import, utcnow, Column-Konventionen, comment=-Muster aus SCHILD-Phase Plan 02/03)
    - RESEARCH §1 DDL-Empfehlung (Spalten-Tabelle verbatim) + §1 Schild-Entwurf
    - Geruest §3 (interfaces-Block oben — vollstaendige Feld-Liste)
    - SPEC REQ 1 + REQ 2 Acceptance (alle 9 Spalten + payload_jsonb; 7 Pflichtfelder; taxonomy_version Pflicht)
  </read_first>
  <action>
    NEUE Klasse `class IntentEvent(Base)` in database/models.py (neue Klasse → Punkt 14 Control-Flow nicht anwendbar; ABER Punkt 21 Persistenz-Schicht-Verifikation Pflicht, siehe Sektion 5 unten). `__tablename__ = 'intent_event'`.

    9 indizierte relationale Spalten (Spalten-Namen = DB-Spalten = ASCII):
    - `event_id = Column(BigInteger, primary_key=True, autoincrement=True)` (BIGSERIAL PK) — Trivial, KEIN Schild (L-04).
    - `session_id = Column(String(128), index=True, nullable=False, comment="SocketIO-sid des Live-Calls. Korreliert mit live_session._session_state[sid]. Per-Call-Filter.")`
    - `call_id = Column(UUID(as_uuid=True), ForeignKey('calls.id', ondelete='CASCADE'), index=True, nullable=True, comment="Bezug zum Call-Record. HARTER FK ON DELETE CASCADE (INTERLOCK I-2 / DD-01-Konvention wie CallEvent models.py:738) — geloeschter Call raeumt Einwand + abstain_log (TAXO2-Wortlaut) DSGVO-sauber mit. A2-'lose FK' revidiert (F-08). nullable: Events ohne call_id moeglich, FK greift nur fuer gesetzte Werte.")` — UUID-Typ aus sqlalchemy.dialects.postgresql + ForeignKey importieren wie bestehende FK-Spalten (CallEvent.call_id Vorbild). **INTERLOCK I-2: NICHT mehr lose — harter Cascade-FK** (DSGVO-Lösch-Kette).
    - `mode = Column(String(32), index=True, nullable=False, comment="Modus-Dimension (cold_call/meeting/...). First-Class, nicht aus Intent ableitbar. Quelle: ModeStrategy-Registry (Welle 7).")`
    - `timestamp = Column(DateTime, index=True, nullable=False, default=utcnow)` — Trivial-aehnlich, ABER Zeit-Achse fuer Latenz → Schild: `comment="Erzeugungs-Zeitpunkt des Events (Zeit-Achse/Latenz-Auswertung)."`
    - `intent_type = Column(String(64), index=True, nullable=False, comment="Taxonomie-Wert (Geruest §1): Kern+Gemini-Werte ∪ custom_objection_*. Quelle services/intent_taxonomy.py. Geschrieben Fast+Medium Lane (Welle 4).")`
    - `phase = Column(SmallInteger, index=True, nullable=True, comment="Gespraechs-Phase 1-6 als INT (getrennt vom Intent). NICHT String (K3-Falle). Quelle detect_phase (Welle 4).")`
    - `handling_score_numeric = Column(SmallInteger, index=True, nullable=True, comment="REQ 2: Behandlungs-Note 1-3. Existiert ab Tag 1, bleibt NULL in TAXO1. Befuellung = TAXO2 (Slow Lane). KEIN Scoring-Code in TAXO1.")`
    - `handling_status = Column(String(16), index=True, nullable=False, server_default='pending', comment="INTERLOCK I-1: Verarbeitungs-Status der Slow-Lane-Benotung (pending|scored|abstained|failed). TAXO2-Wurzel-Fix gegen dreifach-ueberladene NULL. Arbeitsliste = WHERE handling_status='pending'; abstained/failed = abgeschlossen. In TAXO1 leer/'pending' (Default), KEIN Status-Schreib-Code hier — TAXO2 setzt die Werte.")` — String(16) ASCII; server_default='pending' (NOT NULL ohne Backfill-Bedarf, da intent_event in TAXO1 leer startet).
    - `confidence = Column(Float, nullable=True, comment="Konfidenz der Klassifikation (ui_asserted=1.0). Steuert spaeter Cue-Aufdringlichkeit + Score-Beitrag.")`

    Zusatz-Spalte aus 4+3 (RESEARCH §1 empfiehlt Spalte, numerisch aggregierbar):
    - `reaction_latency_ms = Column(Integer, nullable=True, comment="Stress-Metrik: Reaktionszeit des Beraters in ms. Existiert ab Tag 1, befuellt spaeter (TAXO2).")`

    Zusatz-Spalte Moment-Klammer (Cross-AI Gemini 3.1, INDIZIERT, Quell-Anker Soll-Verhalten §6):
    - `interaction_id = Column(UUID_TYPE, index=True, nullable=True, comment="Korrelations-ID pro Kundenmoment — klammert alle Emits (Fast/Medium/Button) + Cue + Reaktion + Abstain eines Moments zusammen; FK-Ziel für spätere suggestion_reactions (Phase H). call_id zu grob, event_id zu fein.")` — Typ `UUID_TYPE` (models.py:12, identisch zu call_id). Nullable, KEIN harter FK (Phase H ist FK-Quelle). `_id`-Suffix ist laut L-04 trivial → Guard verlangt kein Schild, ABER wie call_id bewusst beschildert (Klammer-Zweck dokumentiert) → behalten. Befuellung im Cutover (Plan 04) — HIER nur Spalte+Index+Schild, KEIN Schreib-/Lern-Code.

    payload_jsonb (Rest aus Geruest §3 + restliche Pflichtfelder als Keys):
    - `payload_jsonb = Column(JSON, nullable=False, default=dict, comment="Hybrid-Rest: source, inference_basis, taxonomy_version(Pflicht non-null), abstained, speaker_role, speaker_id, is_simulation, origin_type, source_context, outcome, resolved_at_event_id, superseded_by, inference_config_id, was_correct, cue_fired, dimension_available, cue_visible, ui_state_hash. Provenance+Kontext-Felder; Pflichtfelder ab Tag 1, viele NULL in TAXO1.")` — JSON-Typ wie bestehende JSON-Spalten (z.B. Profile.daten); auf Postgres = jsonb-faehig (verify in Migration: `JSONB`).

    Tabellen-Schild (`__table_args__ = {'comment': "..."}`), exakt aus RESEARCH §1:
    "Zentrale Ereignis-Tabelle (Single Source of Truth) fuer erkannte Kunden-Intents pro Call. Fast+Medium Lane emittieren; Slow Lane reichert via separatem Score-Objekt an (TAXO2). Hybrid: indizierte Kern-Spalten + payload_jsonb. Status: lebt (neu, TAXO1). Schreibt services/einwand_keyword_matcher.py, services/claude_service.py; liest TAXO2-Scoring."

    Imports ergaenzen falls noetig: BigInteger, SmallInteger, Float, Integer, DateTime, JSON, String, **ForeignKey** aus sqlalchemy; UUID aus sqlalchemy.dialects.postgresql (Muster bestehender UUID/FK-Spalten in models.py greppen + uebernehmen — CallEvent.call_id:738 als FK-CASCADE-Vorbild). KEINE Scoring-Methode auf der Klasse. KEIN handling_score-Default ausser NULL; handling_status-Default='pending' (INTERLOCK I-1).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('database/models.py').read())"` lokal erlaubt. Schema-Verify auf Production via Task 3.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class IntentEvent" database/models.py` 1 Treffer; `grep "__tablename__ = 'intent_event'" database/models.py` 1 Treffer.
    - Alle 9 indizierten Spalten-Namen als grep-Treffer (`grep -E "event_id|session_id|call_id|mode|timestamp|intent_type|phase|handling_score_numeric|confidence" database/models.py` deckt alle ab) + `payload_jsonb` + `reaction_latency_ms` + `interaction_id` + `handling_status` (INTERLOCK I-1).
    - INTERLOCK I-1: `grep "handling_status" database/models.py` zeigt `Column(String(16), index=True, nullable=False, server_default='pending', comment=...)`.
    - INTERLOCK I-2: `grep -E "ForeignKey\('calls.id', ondelete='CASCADE'\)" database/models.py` deckt intent_event.call_id ab (harter Cascade-FK, A2 revidiert — NICHT mehr lose).
    - `grep "interaction_id" database/models.py` zeigt `Column(UUID_TYPE, index=True, nullable=True, comment=...)` (Moment-Klammer, indiziert, beschildert); kein `ForeignKey` auf interaction_id (Phase H ist FK-Quelle).
    - `grep "comment=" database/models.py` deckt mode/intent_type/phase/handling_score_numeric/confidence/payload_jsonb ab (nicht-triviale Spalten beschildert, L-04).
    - `grep -i "def.*score\|handling_score.*=" database/models.py` zeigt KEINE Score-Berechnung auf IntentEvent (nur Spalten-Definition + NULL).
    - `phase`-Spalte ist SmallInteger (nicht String) — K3-Vorbeugung im Schema.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>ORM-Model IntentEvent vollstaendig (verriegelter Vertrag), alle Spalten + nicht-trivialen Schilder gesetzt, handling_score_numeric existiert leer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migration 0016 — CREATE TABLE intent_event + Indizes + ALTER OWNER nerve_app + op.execute COMMENT-Schilder</name>
  <read_first>
    - alembic/versions/0008_training_schema_foundation.py (CREATE TABLE + op.execute-Muster, public-Schema)
    - alembic/versions/0014_crm_user_preferences_rls.py (Revisions-Header, down_revision-Muster)
    - alembic/versions/0015_table_column_comments_schilder.py (op.execute COMMENT-Muster, Prod-HEAD)
    - RESEARCH §1 (DDL, ALTER OWNER nerve_app, Migration als postgres)
    - database/models.py IntentEvent (Single-Source der COMMENT-Texte aus Task 2)
  </read_first>
  <action>
    Migration `alembic/versions/0016_intent_event_table.py`, `revision='0016'`, `down_revision='0015'`. Reine Schema-Addition (KEIN autogenerate-Round-Trip — hauseigenes op.execute/op.create_table-Muster, Cross-AI-Finding 1 aus SCHILD).

    upgrade():
    1. `op.create_table('intent_event', ...)` mit allen 13 Spalten (9 indiziert + handling_status + reaction_latency_ms + interaction_id + payload_jsonb). Typen: BigInteger PK autoincrement; String laengen wie Model; `sa.dialects.postgresql.UUID(as_uuid=True)` fuer call_id UND interaction_id (Model-Alias UUID_TYPE → in der Migration der postgresql-UUID-Typ wie bei bestehenden UUID-Spalten); **call_id als `sa.ForeignKey('calls.id', ondelete='CASCADE')` (INTERLOCK I-2 / DD-01 — harter Cascade-FK, A2 revidiert)**; `sa.SmallInteger` fuer phase/handling_score_numeric; **`sa.String(16)` fuer handling_status (nullable=False, server_default='pending', INTERLOCK I-1)**; `postgresql.JSONB` fuer payload_jsonb (nullable=False, server_default='{}'); DateTime timestamptz fuer timestamp.
    2. Indizes auf: session_id, call_id, mode, timestamp, intent_type, phase, handling_score_numeric, **handling_status (INTERLOCK I-1 — Arbeitsliste WHERE handling_status='pending')**, interaction_id (`op.create_index('ix_intent_event_<col>', 'intent_event', ['<col>'])`) — interaction_id ist die Moment-Klammer und MUSS indiziert sein (Lookup aller Emits eines Moments).
    3. `op.execute("ALTER TABLE public.intent_event OWNER TO nerve_app")` (RESEARCH §1: Migration laeuft als postgres, danach Owner umlegen).
    4. op.execute COMMENT-Schilder (Single-Source = die comment=-Strings aus models.py Task 2, verbatim kopieren):
       - `op.execute("COMMENT ON TABLE public.intent_event IS '<Tabellen-Schild>'")`
       - je nicht-triviale Spalte (session_id, call_id, mode, timestamp, intent_type, phase, handling_score_numeric, **handling_status**, confidence, reaction_latency_ms, payload_jsonb) + bewusst `interaction_id` (trotz `_id`-Suffix, wie call_id): `op.execute("COMMENT ON COLUMN public.intent_event.<col> IS '<Spalten-Schild>'")`. event_id (PK) ausgenommen (L-04). interaction_id-Schild verbatim aus models.py (Task 2): "Korrelations-ID pro Kundenmoment — klammert alle Emits (Fast/Medium/Button) + Cue + Reaktion + Abstain eines Moments zusammen; FK-Ziel für spätere suggestion_reactions (Phase H). call_id zu grob, event_id zu fein."
    downgrade(): `op.drop_table('intent_event')` (reversibel; Tabelle startet leer → kein Datenverlust). Indizes + FK fallen mit der Tabelle.

    Umlaute in COMMENT-Strings OK (UTF-8). **HARTER FK auf calls(id) ON DELETE CASCADE (INTERLOCK I-2, A2 revidiert — DSGVO-Lösch-Kette; DD-01-Konvention).** KEIN CHECK-Constraint auf intent_type in der DB (Validierung in Python via intent_taxonomy.is_valid_intent_type, Welle 4 — varchar(64) akzeptiert custom_objection_* ohne DDL, REQ 5-Acceptance). handling_status ebenfalls KEIN DB-CHECK (Werte-Whitelist Python-seitig in TAXO2).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('alembic/versions/0016_intent_event_table.py').read())"` lokal erlaubt. Production-Verify via Task 4 (deploy + inspect.sh).</automated>
  </verify>
  <acceptance_criteria>
    - Datei 0016_*.py existiert; `grep "revision = '0016'" alembic/versions/0016_*.py` + `grep "down_revision = '0015'" alembic/versions/0016_*.py`.
    - `grep "op.create_table('intent_event'" alembic/versions/0016_*.py` 1 Treffer; alle 13 Spalten als grep-Treffer (inkl. `interaction_id` + `handling_status`).
    - `grep "OWNER TO nerve_app" alembic/versions/0016_*.py` 1 Treffer.
    - `grep -E "ix_intent_event_interaction_id|ix_intent_event_handling_status" alembic/versions/0016_*.py` == 2 Treffer (Moment-Klammer + Status-Arbeitsliste indiziert).
    - INTERLOCK I-1: `grep "handling_status" alembic/versions/0016_*.py` deckt Spalte (String(16) server_default 'pending') + Index + COMMENT ab.
    - INTERLOCK I-2: `grep -E "ForeignKey.*calls.id.*ondelete='CASCADE'|REFERENCES (public\.)?calls\(id\) ON DELETE CASCADE" alembic/versions/0016_*.py` >= 1 (**harter Cascade-FK auf calls — MUSS jetzt vorhanden sein, A2 revidiert**); `grep -E "ForeignKey.*interaction" alembic/versions/0016_*.py` == 0 (kein FK auf/von interaction_id — Phase H ist FK-Quelle).
    - `grep -c "COMMENT ON COLUMN public.intent_event" alembic/versions/0016_*.py` >= 11 (nicht-triviale Spalten + interaction_id + handling_status) + 1 `COMMENT ON TABLE`; `grep "interaction_id" alembic/versions/0016_*.py` deckt Spalte + Index + COMMENT ab.
    - downgrade() = op.drop_table('intent_event'); AST-Parse exit 0.
  </acceptance_criteria>
  <done>Migration 0016 bereit: legt intent_event als nerve_app-Tabelle an, vollstaendig beschildert, reversibel, ohne harten calls-FK.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>intent_taxonomy.py + IntentEvent-Model + Migration 0016. Bereit zum Deploy auf Production (reine Schema-Addition, leere Tabelle).</what-built>
  <how-to-verify>
    ⚠️ **GATE: Deploy ERST NACH Cross-AI-Clearance der gesamten TAXO1-Phase + André-Ineinandergreifen-Check (TAXO1/2/3).** Vorher NICHT deployen. Nach Clearance fuehrt André aus (Migrations-Apply braucht echte DB als postgres):

    1. `git add -A && git commit && git push origin main`
    2. `bash deploy.sh production` — eingebautes Pytest-Gate laeuft VOR Restart; bei Rot kein Restart.
    3. Migration als **postgres** anwenden (RESEARCH §1; nerve_app hat kein CREATE auf public): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u postgres bash -c "source /opt/nerve/venv/bin/activate && alembic upgrade head"'`
    4. Migrations-HEAD pruefen: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` → zeigt **0016**.
    5. Schema-Beleg (REQ 1 Acceptance): `ssh ... 'bash scripts/inspect.sh schema intent_event'` → zeigt alle 9 indizierten Spalten + payload_jsonb + reaction_latency_ms + interaction_id (UUID, indiziert: `ix_intent_event_interaction_id`) + **handling_status (varchar(16) NOT NULL default 'pending', indiziert: `ix_intent_event_handling_status`, INTERLOCK I-1)** + **call_id mit `REFERENCES calls(id) ON DELETE CASCADE` (INTERLOCK I-2)**; Owner nerve_app. Verbatim-Output in SUMMARY.
    6. Leer-Beleg (REQ 1/Daten-Status): `ssh ... 'bash scripts/inspect.sh count intent_event'` → **0**.
    7. **MANUELLER SCHILD-Guard (D-03 Auflage 2, Punkt 23):** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && NERVE_SCHILD_TEST_DSN=$(grep ^DATABASE_URL= /etc/nerve/.env | cut -d= -f2-) /opt/nerve/venv/bin/pytest tests/test_schild_guard.py -q'` → **GRUEN** (intent_event + alle nicht-trivialen Spalten beschildert). Verbatim GREEN-Output in SUMMARY.
  </how-to-verify>
  <resume-signal>"0016 live + intent_event leer (count=0) + Schema 9 Spalten + Guard GRUEN" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 1. Lokaler Kontext
Kein Code-Insert in bestehende Funktionen in dieser Welle. Task 1 (intent_taxonomy.py) + Task 2 (IntentEvent-Klasse) sind NEUER Code (neue Datei / neue Klasse) → Punkt 14 Schichten 1-2 nicht anwendbar (kein umgebender Control-Flow). Task 3 ist eine neue Migrations-Datei.

## 2. Funktions-Skelett
Nicht anwendbar — keine bestehende Funktion angefasst. IntentEvent ist eine reine Daten-Klasse (Column-Definitionen, keine Methoden mit Exit-Pfaden).

## 3. Cross-File-Awareness
- `grep -rn "intent_event" services/ routes/ database/ alembic/` VOR Bau erwartet: 0 aktive Treffer (RESEARCH §1: Tabelle existiert nicht, 0 Code-Treffer). Nach Bau: nur models.py + 0016-Migration. KEIN Live-Writer in dieser Welle (matcher/analyse_loop/qa-dispatch werden erst Welle 4 angebunden) → keine Cross-Thread-Konflikte.
- `grep -rn "IntentEvent\|intent_taxonomy" services/ routes/` nach Bau: nur die neue Datei (intent_taxonomy.py wird erst Welle 4 importiert). Keine bestehenden Leser/Schreiber → kein Konflikt mit analyse_loop/coaching_loop/qa_pipeline-Daemon.
- `EinwandTypEnum` (profile_schema.py:45) bleibt in dieser Welle UNANGETASTET (Loeschung = Welle 4, RESEARCH §4) — keine Doppel-Quelle-Aktivierung jetzt, nur die neue Quelle angelegt.

## 4. Edge-Case-Analyse
- **Edge 1 — Migration laeuft als nerve_app statt postgres:** CREATE TABLE auf public schlaegt fehl (nerve_app hat kein CREATE-Recht, RESEARCH §1). Behandlung: Task-4-Schritt 3 erzwingt `sudo -u postgres`; deploy.sh-Pytest-Gate faengt Rot vor Restart.
- **Edge 2 — payload_jsonb nullable-Default:** wird nullable=False mit server_default '{}' angelegt → leere Inserts (Welle 4) crashen nicht an NOT NULL. In TAXO1 wird ohnehin nicht geschrieben (leere Tabelle).
- **Edge 3 — call_id UUID-Typ-Match (INTERLOCK I-2, jetzt harter FK):** call_id ist jetzt ein harter FK ON DELETE CASCADE → der Typ MUSS exakt zu calls.id passen. Beim Bau via `inspect.sh schema calls` bestaetigen dass calls.id UUID ist (RESEARCH/§A1 zeigt es; rubric_score-Plan-01 nutzt dieselbe calls.id-FK). nullable=True bleibt — NULL-call_id-Events werden vom Cascade nicht beruehrt. Insert-Timing unkritisch: die calls-Zeile existiert ab Call-Start, vor jedem Emit (Gemini-Interlock bestaetigt) → kein FK-Violation beim Live-Insert.

### Race-Condition-Audit
1. **Gleichzeitiges Feuern:** Nicht relevant — KEIN Live-Writer in dieser Welle. Die Tabelle ist leer und wird von keinem Thread beschrieben (matcher/analyse_loop angebunden erst Welle 4). Belastbar: `grep` (Sektion 3) zeigt 0 Schreiber.
2. **Verbindungsabbruch mid-Operation:** Nicht relevant — kein Live-Schreibpfad. Migration ist transaktional (Alembic) — Abbruch waehrend CREATE rollt zurueck, Tabelle existiert dann nicht (idempotent re-runnbar).
3. **Wiederverbinden mit altem Zustand:** Nicht relevant — kein Session-State angefasst, keine SID-Logik in dieser Welle.
4. **Tab im Hintergrund:** Nicht relevant — kein Frontend/Timer/SocketIO-Code beruehrt.

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `public.intent_event` (CREATE — neu). Existiert NICHT vor dieser Welle (RESEARCH §1 inspect.sh-Beleg).

### inspect.sh-Beleg (Ist-Zustand vor Bau, RESEARCH §1 verbatim zitiert)
```
$ inspect.sh count intent_event
ERROR:  relation "intent_event" does not exist
LINE 1: SELECT COUNT(*) FROM intent_event;
$ inspect.sh tables | grep -iE 'intent|objection|ewb'
 public | ewb_ratings            | table | nerve_app
 public | objection_events       | table | nerve_app
$ inspect.sh migrations  →  0015
```
→ intent_event neu, Migration 0016, Owner muss nerve_app werden. Soll-Schema (nach 0016) wird in Task-4-Schritt 5 via `inspect.sh schema intent_event` verbatim belegt.

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `IntentEvent.handling_score_numeric` | nur definiert, NICHT geschrieben (TAXO1) | DB-Spalte intent_event.handling_score_numeric (SmallInteger, NULL) | ⬜ Task 4 Schema-Beleg; grep zeigt 0 Score-Writer |
| `IntentEvent.intent_type` (varchar64) | Definition; Live-Write erst Welle 4 | DB-Spalte intent_event.intent_type | ⬜ Task 4 Schema-Beleg |
| `IntentEvent.phase` (SmallInteger) | Definition; INT erzwungen (K3-Vorbeugung) | DB-Spalte intent_event.phase (smallint, NULL) | ⬜ Task 4 Schema-Beleg |
| `IntentEvent.interaction_id` (UUID) | nur definiert+indiziert, NICHT geschrieben (TAXO1); befuellt im Cutover (Plan 04) | DB-Spalte intent_event.interaction_id (UUID, NULL, indiziert) | ⬜ Task 4 Schema-Beleg (ix_intent_event_interaction_id); kein FK |
| `taxonomy_version` | payload_jsonb-Key (Pflicht non-null ab Welle 4) | JSONB-Key in payload_jsonb | ⬜ Doku im Spalten-Schild; Insert-Pflicht Welle 4 |
| `INTENT_TAXONOMY_V1` / `TAXONOMY_VERSION` | Code-Konstante (intent_taxonomy.py) | Python-Modul (RAM/Code), KEINE DB-Spalte | ✓ bewusst Code-Vertrag (D-01), nicht DB |

### Bei Diskrepanz (Schema-Beleg zeigt fehlende Spalte / falschen Typ / falschen Owner): STOP + Migration korrigieren, nicht weiterbauen.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Migration → Production-DB | Migration laeuft als postgres (privilegiert) gegen public; danach Owner-Wechsel auf nerve_app. |
| (kuenftig) Live-Lane → intent_event | In dieser Welle KEIN Schreibpfad — Boundary erst ab Welle 4 aktiv. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO1-01 | D (DoS) | Migration bricht Deploy wenn als nerve_app statt postgres (CREATE-deny auf public) | mitigate | Task-4 erzwingt `sudo -u postgres`; ALTER OWNER nerve_app danach; deploy.sh-Pytest-Gate faengt Rot vor Restart. |
| T-TAXO1-02 | T (Tampering) | autogenerate zieht ungewollte Struktur-Diffs | accept/mitigate | Migration hand-geschrieben (op.create_table + op.execute COMMENT, kein autogenerate) — Risiko entfaellt (SCHILD-Praezedenz). |
| T-TAXO1-03 | I (Info Disclosure) | intent_type nimmt beliebigen String (varchar64) → Injection? | mitigate | Schreibpfad (Welle 4) nutzt SQLAlchemy-ORM (parametrisiert) + is_valid_intent_type-Whitelist; varchar(64) ist Datenfeld, kein SQL. |
</threat_model>

<verification>
- services/intent_taxonomy.py: 11 Kern-Werte + TAXONOMY_VERSION + Validator, abhaengigkeitsfrei.
- IntentEvent-Model: 9 indizierte Spalten + payload_jsonb + reaction_latency_ms + interaction_id (indizierte Moment-Klammer, beschildert), alle nicht-trivialen Schilder; handling_score_numeric leer (kein Scoring-Code).
- Migration 0016: CREATE + Indizes (inkl. ix_intent_event_interaction_id) + ALTER OWNER nerve_app + COMMENTs, reversibel, kein calls-/interaction-FK.
- Production: inspect.sh migrations = 0016; schema intent_event komplett; count = 0; SCHILD-Guard GRUEN.
</verification>

<success_criteria>
- intent_event existiert auf Prod (9 Spalten + payload_jsonb + reaction_latency_ms + interaction_id + handling_status [I-1, indiziert, default 'pending'] + call_id harter FK CASCADE [I-2]), Owner nerve_app, leer (REQ 1).
- Alle 4+3 Pflichtfelder vorhanden (Spalte oder JSONB-Key), handling_score_numeric NULL, KEIN Scoring-Code (REQ 2).
- INTENT_TAXONOMY_V1 als die EINE versionierte Quelle definiert (D-01), noch ohne Live-Writer.
- SCHILD-Guard GRUEN server-seitig (Punkt 23, D-03 Auflage 2). KEIN Test-Anruf (schema-addition).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-01-SUMMARY.md` (inkl. verbatim `inspect.sh schema intent_event` + `count intent_event`=0 + Guard-GREEN-Output + Bestaetigung: kein Scoring-Code schreibt handling_score_numeric).
</output>
