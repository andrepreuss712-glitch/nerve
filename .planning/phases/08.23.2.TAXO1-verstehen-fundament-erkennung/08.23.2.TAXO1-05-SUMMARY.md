---
phase: 08.23.2.TAXO1
plan: 05
wave: 5
status: code-complete-gated
complexity: 🔴
autonomous: false
deployed: false
migration_applied: false
git_head: 099f3c8
commits: [c07753a, 059ca51, 96338b8, 099f3c8]
---

# 08.23.2.TAXO1 Welle 5 — Zombie-Rename + Dual-Write-Bruecke — SUMMARY

**Status: CODE-COMPLETE + committet/gepusht. NICHT migriert, NICHT deployed, KEIN Test-Anruf.**
Deploy + Migrations-Apply (0017) + manueller SCHILD-Guard + Test-Anruf = **GATED** (André/Claudian
fahren den DB-Schritt + beaufsichtigten Deploy nach Cross-AI-Clearance). STOP-PUNKT eingehalten.

## Materieller Naht-Befund VOR Bau (Punkt 21 + VOR-EXECUTE-RE-GREP)

Der Re-Grep am Bruecken-Tor fing einen **Plan-vs-Live-Persistenz-Widerspruch**: Plan 05
plante die Bruecke als „intent_event = Quelle → objection_events = Spiegel". Das ist falsch —
der Welle-4 `ui_asserted`-Emit (`deepgram_service.py:848`, `intent_event_writer.py:86-120`) ist
bewusst **schlank**: `intent_type='echter_einwand'` hartkodiert, **kein** `success` /
`antwort_text` / `einwand_text` / `conversation_log_id`. Ein Spiegel aus intent_event haette
`success=NULL` geliefert → `app_routes.py:447 SUM(success)` → `einwaende_behandelt=0` →
**Dashboard-Einwand-Zaehler kaputt** (genau D-03, das der Plan schuetzt).

**André-Entscheid 2026-06-18 (AskUserQuestion):** Quelle = **ewb_clicks (RAM)** durch den einen
gekapselten Shim. `ewb_clicks` (`live_session.py:121`) traegt alle Leser-Felder; intent_event
bleibt Trigger/Korrelation. Damit Dashboard-Zaehler 1:1 korrekt.

## Was gebaut wurde

### Task 1 — Zombie-Rename ewb_ratings → zombie_ewb_ratings (c07753a)
- `database/models.py` EwbRating: `__tablename__='zombie_ewb_ratings'` + `[ZOMBIE]`-Schild
  (Klassen-Name `EwbRating` unveraendert → alle Importe/ORM-Queries laufen weiter).
- `alembic/versions/0017_zombie_ewb_ratings.py` revision=`0017`, down_revision=`0016`:
  upgrade `ALTER TABLE public.ewb_ratings RENAME TO zombie_ewb_ratings` + `COMMENT [ZOMBIE]`;
  downgrade benennt zurueck + Original-Schild (reversibel, **KEIN DROP**). Index/UNIQUE +
  Spalten-COMMENTs (0015) wandern automatisch mit; Owner bleibt nerve_app.
- `routes/admin_ewb.py:128` raw SQL `LEFT JOIN ewb_ratings r` → `LEFT JOIN zombie_ewb_ratings r`
  (einzige manuelle Stelle; ORM `:65/:181/:192` folgt `__tablename__`).

### Task 2 — services/objection_bridge.py (059ca51)
- `mirror_ewb_clicks_to_objection_events(*, conversation_log_id, user_id, org_id, ewb_clicks, db) -> int`
- `# SUNSET: TAXO2`-Marker (D-04). KEIN Rueck-Sync, KEIN Backfill (verifiziert: einzige DB-Ops
  sind keyed DELETE objection_events + add ObjectionEvent; intent_event wird nicht beruehrt).
- **Idempotenz-Guard (Finding #3):** bei nicht-leeren ewb_clicks DELETE der eigenen Spiegel-Zeilen
  dieser `conversation_log_id` → frisch re-spiegeln (DELETE-then-INSERT, last-write-wins). Leere
  ewb_clicks = No-Op (kein DELETE → kein versehentliches Loeschen nach Session-Reset).
- Felder 1:1 wie der ersetzte Direkt-Write. Committet nicht selbst (Caller-Transaktion).

> **DEVIATION (Rule 3, dokumentiert):** Funktion heisst `mirror_ewb_clicks_to_objection_events`
> (nicht `mirror_intent_events_...` wie im Plan) — der Name spiegelt die reale Quelle (ewb_clicks)
> nach André-Entscheid. Plan-Acceptance-Greps auf den intent_event-Namen sind durch diesen
> Entscheid obsolet; die Idempotenz-/Sunset-/No-Backfill-Akzeptanz ist erfuellt.

### Task 3 — Bruecke an app_routes:380-394 verdrahtet (96338b8)
- Der fruehere `for click in ewb_clicks: db_conv.add(ObjectionEvent(...))` ist durch **einen**
  Bruecken-Aufruf **ersetzt** (nicht zusaetzlich). `ObjectionEvent`-Import bleibt (Re-Aggregation
  `:446/:471`). `if ewb_clicks: db_conv.commit()` unveraendert. objection_events-Leser unberuehrt.
- Cross-File-Beleg: einziger Live-`add(ObjectionEvent(` ist jetzt `objection_bridge.py:63`.

### Stale-Test-Fix — die „Lehre" (099f3c8)
Contract-aendernde Welle bricht stale Tests mit RAW-Tabellennamen (ORM folgt automatisch,
Raw-SQL nicht). Gefangen + gefixt:
- `tests/conftest.py:128` `_CLEANUP_FK_ORDER` `"public.ewb_ratings"` → `"public.zombie_ewb_ratings"`.
- `tests/test_ab_stats.py` Teardown (tables-Tuple + `DELETE FROM public.ewb_ratings`) → zombie.
Sonst: Baseline-Cleanup-Waechter rot / `relation does not exist` im deploy.sh-Gate.

## Verifikation (lokal, Build-Zeit)
- **AST-Parse exit 0:** models.py, 0017, objection_bridge.py, app_routes.py, admin_ewb.py, conftest.py, test_ab_stats.py.
- **Acceptance-Greps gruen:** zombie-tablename(1), [ZOMBIE](vorhanden), RENAME up/down(1/1), down_revision 0016(1),
  admin_ewb kein bare `LEFT JOIN ewb_ratings`, SUNSET-Marker(1), def(1), Idempotenz-Guard vorhanden,
  EINE Bruecken-Aufruf-Stelle, kein alter for-Loop-Write, dashboard.py unveraendert, kein Rest-`ewb_ratings`-Raw.
- **Voller triage.sh-Lauf: NICHT lokal moeglich** — triage.sh ist server-only (`sudo -u postgres`
  peer-auth, provisioniert nerve_test aus Prod-Schema; mein Code ist noch nicht deployed). Der volle
  Suite-Lauf IST der `deploy.sh`-Gate-Block beim beaufsichtigten Deploy. Lokaler Ersatz = der
  Stale-Test-Sweep oben (das eigentliche „Lehre"-Risiko: contract-gebrochene stale Tests — gefunden + gefixt).

## OFFEN — André/Claudian (GATED, nach Cross-AI-Clearance + Ineinandergreifen-Check)
1. **Cross-AI (Gemini, 🔴-Pflicht Punkt 24)** auf die Welle-5-Diffs (Quelle-Wechsel ewb_clicks!).
2. `bash deploy.sh production` (Pytest-Gate laeuft server-seitig — fuer volle triage-Gruen-Aussage).
3. **Migration als postgres:** `sudo -u postgres ... alembic upgrade head` (RENAME auf public).
4. Rename-Beleg: `inspect.sh schema zombie_ewb_ratings` existiert; `inspect.sh count ewb_ratings` → ERROR (weg).
5. **Manueller SCHILD-Guard** (`NERVE_SCHILD_TEST_DSN ... pytest tests/test_schild_guard.py`) → GRUEN.
6. **Test-Anruf** (D-03): (a) objection_events-Zaehler steigt um #EWB-Klicks, Dashboard-Zaehler korrekt;
   (a2) zweites `/api/beenden` = **gleiche** Anzahl (Idempotenz Finding #3); (b) intent_event steigt;
   (c) Dashboard/Coaching/Transkript intakt. Verbatim-Belege hier nachtragen.

## Resume-Signal
„0017 live + zombie_ewb_ratings da + ewb_ratings weg + Guard GRUEN + Test-Anruf: objection_events-Zaehler
stimmt (keine Doppel) + Idempotenz (zweites /api/beenden = gleiche Anzahl) + Dashboard/Coaching/Transkript OK"
oder beschreibe den Fehler.
