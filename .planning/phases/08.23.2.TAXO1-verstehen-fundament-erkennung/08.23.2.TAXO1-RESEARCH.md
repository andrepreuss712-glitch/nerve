# Phase 08.23.2.TAXO1: Verstehen — Fundament + Erkennung — RESEARCH

**Researched:** 2026-06-10
**Domain:** Schema-weite Migration (intent_event) + Live-Erkennungs-Pfad-Umbau (Fast/Medium/Slow Lane) + Single-Source-Konsolidierung (§0.1) + Modus-Registry. Flask 3 / Flask-SocketIO / Postgres / Anthropic Haiku.
**Confidence:** HIGH für alle Code-/Prod-belegten Befunde (grep file:line + inspect.sh verbatim). LOW/UNVERIFIED explizit markiert.
**Migrations-Kette:** Prod-HEAD = **0015** (verifiziert) → neue Migration = **0016**.

> **Grounding-Disziplin (Punkt 22):** Jede Status-Behauptung unten ist mit grep file:line UND/ODER inspect.sh-verbatim belegt. Code-Lesen lag auf dieser Codebase 3× daneben (Speech-Stats, phase_classify, AccountMemory-Agent) — deshalb kein „lebt/tot" ohne Beleg. Production-only (kein Local-Dev). SSH-Muster:
> `ssh -i "$env:USERPROFILE\.ssh\nerve_vps" -o ConnectTimeout=15 root@178.104.82.166 "cd /opt/nerve/app && bash scripts/inspect.sh <cmd>"`

---

## Summary

TAXO1 baut EINE neue Tabelle `public.intent_event` als Single Source of Truth für „was sagt der Kunde gerade", lenkt die zwei lebenden Erkennungs-Bahnen (Fast = `einwand_keyword_matcher.py` + EWB-Button, Medium = `analyse_loop` in `claude_service.py`) dorthin um, baut die Slow-Lane-Klempnerei (Queue + 1 Daemon-Consumer + Graceful-Flush, OHNE Benotung), faltet die §0.1-Putzliste komplett auf die eine per-SID-Quelle (`live_session._session_state[sid]`), und löst dabei K3 (phase_classify int-vs-str) + K4 (qa_slot1-Unterfeuern). `ewb_ratings` wird zu `zombie_ewb_ratings` umbenannt; `objection_events` bleibt per dünner Dual-Write-Brücke befüllt, damit Dashboard/admin/integration_engine nicht brechen.

Production ist verifiziert: `intent_event` existiert NICHT (relation does not exist), `objection_events` (8 Zeilen) + `ewb_ratings` (0 Zeilen) existieren mit den vom Orchestrator genannten Schemas, Migrations-HEAD = 0015. Die §0.1-Verletzungen sind alle noch im Code (grep-belegt unten): `current_phase` per-SID-write `:1012` / global-read `:1085`, `cold_call_inference` per-SID-write `:1071` / global-read `:1086`, `score_factors_seen`/`last_einwand_typ` global, `user_id` global-read `:452`, `kw_fired_for_line` REVERSE (matcher-global-write `:253` / per-SID-read `:1265`).

**Primary recommendation:** SCHILD-Stil-Wellenschnitt in **7 Wellen** mit strikter Dependency-Ordnung (intent_event-Tabelle EXISTIERT als Welle 1, bevor irgendwer schreibt). Migration als `postgres` (nerve_app hat kein CREATE auf public), Schild via `op.execute("COMMENT ON …")` (hauseigenes Muster, kein autogenerate-Round-Trip), SCHILD-Guard MANUELL server-seitig gegen Prod-DSN. Reihenfolge: Schema-Addition zuerst (riskarm) → Gerüst → Putzliste+K3 (riskant) → Cutover (riskant) → Zombie+Dual-Write (riskant) → EWB v2 (riskant) → Modus-Registry.

---

## User Constraints (aus CONTEXT.md — verbatim)

### Locked Decisions (D-01..D-05)
- **D-01:** Kern-Taxonomie (`echter_einwand`, `vorwand`, `reflexeinwand`, `kaufsignal`, `aufschub`, `info_frage` + Gemini-Ergänzungen `gatekeeper`, `wettbewerber_referenz`, `hard_opt_out`, `commitment`, `meta_kommunikation`) bleibt **fest im Code als versioniertes Enum/Konstante** mit `taxonomy_version` an jedem Event. **Bewusst NICHT user-editierbar.** User-Erweiterung NUR via `custom_objection_*` pro Profil. Altes `ist_vorwand`-String/Boolean (claude_service.py:966/968) + `EinwandTypEnum` (profile_schema.py:45) auf GENAU DIESE EINE Quelle umziehen, alte parallele Pfade löschen.
- **D-02:** Wellen-Schnitt wie SCHILD, KEIN Big-Bang. Jede riskante Welle einzeln auf Production verifiziert.
- **D-03:** Test-Anruf nur bei riskanten Wellen (Live-Erkennung anfasst / alten Pfad löscht / Modus-Routing ändert). Reine Schema-Addition ohne Live-Anbindung überspringt (nur Auto-Check). **Auflage 1:** „riskant" großzügig auslegen, im Zweifel Test-Anruf. **Auflage 2:** SCHILD-Wächter bei JEDER Schema-Welle MANUELL server-seitig gegen Prod-DSN laufen lassen. **Auflage 3:** Test-Anruf muss DATEN-FLUSS verifizieren: (a) intent_event-Zeilen werden geschrieben (count/sample steigt mit korrektem intent_type/phase/mode/source/speaker_role), (b) Dashboard-Einwand-Zähler stimmt (objection_events-Spiegel), (c) Phase/Readiness bewegen sich, org_id im Kosten-Log ≠ NULL, keine `user_id leer`-Warnung.
- **D-04:** Dual-Write-Brücke so dünn wie möglich, EIN gekapselter Expand-Contract-Shim mit `# SUNSET: TAXO2`-Marker. Kein Rück-Sync, kein Backfill, keine Zusatz-Features.
- **D-05:** EWB-v2-modular-Umstellung ist eigene riskante Welle mit eigenem Test-Anruf inkl. Fachbegriff-Check (ANKER/REFRAME dürfen NICHT als Vokabular in vorgelesenen Antworten landen) + Log-Beleg `[EWB] vv2-modular assembled`.

### Claude's Discretion (Researcher/Planner entscheiden)
- Bauform Slow-Lane-Queue (`queue.Queue` + 1 Daemon-Consumer) + Adapter-Interface (Redis-Zukunft) + Graceful-Shutdown-Flush-Anbindung an Flask-SocketIO-Lifecycle.
- Aufteilung intent_event-Hybrid-Schema: welche der 7 Pflichtfelder echte Spalte vs. JSONB-Feld.
- Innere Struktur ModeStrategy-Registry (Datei-Ort, Registrierungs-Mechanismus, Methoden-Signaturen) — Pflicht: 3 künftige Modi ohne Schnittstellen-Umbau.
- Konkreter Wellen-Schnitt. Dependency-Zwang: intent_event-Tabelle existiert, BEVOR etwas hineinschreibt.
- Konkrete K3/K4-Fixes — Claude-Code-Debug-Ergebnis einarbeiten, NICHT auf kaputtem Pfad neu aufbauen.

### Deferred Ideas (OUT OF SCOPE — ignorieren)
- Benotung/Scoring/handling_score-Befüllung/BARS/Proration → TAXO2.
- `build_answer_context()`/Säule-3/Slot-B-line_id-Dedup (D3) → TAXO3.
- objection_events-Zombifizierung + Leser-Umzug → TAXO2 (in TAXO1 nur Dual-Write-Brücke).
- Konsolidierung conversation_logs/calls/call_events/transcript_segments → spätere Phase.
- Daten-Migration der Test-Schrott-Logs → intent_event startet leer.
- Ausbau meeting_ext/training_cold/training_meeting + Training-Air-Gap-Event-Bus → nur Registry-Steckplatz.
- Editierbare Kern-Taxonomie (user-self-service) → verworfen.

---

## Project Constraints (aus CLAUDE.md)

- **Punkt 14:** Pre-Insert-Control-Flow-Audit (4 Schichten + Race-Fragen) bei jedem Code-Insert in bestehende Funktionen. Live-Loop-Inserts (analyse_loop, qa-dispatch, matcher) sind Punkt-14-Pflicht.
- **Punkt 20:** Pflicht-grep aktiver Code-Pfad vor jedem Daten-Migration/Code-Insert.
- **Punkt 21:** Persistenz-Schicht-Verifikation („## 5.") mit verbatim `inspect.sh schema <table>` + Cross-Layer-Konsistenz-Tabelle bei jedem Plan der intent_event/objection_events/ewb_ratings/conversation_logs liest/schreibt. Anti-K1/K8: Code-Variable-Name ≠ DB-Spalte.
- **Punkt 22:** Verbindungs-Karte (grep+inspect-belegt) vor jeder Namen-/Schema-/Tabellen-Entscheidung — physisch ins Plan/RESEARCH, keine Prosa-Zusammenfassung.
- **Punkt 23:** Schild (COMMENT) an jeder neuen/umbenannten Tabelle + nicht-trivialer Spalte. Migration via `op.execute("COMMENT ON …")`. Guard `tests/test_schild_guard.py` (läuft als nerve_app, peer-auth) MANUELL server-seitig gegen Prod (deploy.sh-Auto-Block inert bis 08.23.2.STAGING).
- **HART Kein Local-Dev:** Verify = Production. Daten/Schema/Logs via inspect.sh. Migration als postgres.
- **Haiku-only im Live-Loop** (Sonnet nur Post-Call).
- **Multi-Segment-ID-Gotcha:** Pfade auf `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/` hartkodieren; gsd-tools/gsd-code-review/gsd-verifier umgehen.
- **Cross-AI PFLICHT (🔴) vor Execute** (agy/Gemini 3.1 Pro, nur Antwort-Modus `agy -p`).
- **Klartext:** nur user-facing Text; Technik-Sektionen hier dürfen präzise Begriffe nutzen.

---

## Phase Requirements (REQ → Forschungs-Beleg)

| REQ | Beschreibung | Forschungs-Beleg (Welle) |
|----|-------------|--------------------------|
| 1 | intent_event-Tabelle (hybrides Schema) | §1 DDL-Empfehlung; intent_event absent auf Prod verifiziert (Welle 1) |
| 2 | handling_score-Spalten ohne Scoring-Logik | §1 (Spalten leer/NULL, kein Score-Code) (Welle 1) |
| 3 | Drei-Bahnen-Gerüst (Fast/Medium/Slow) | §2 (Queue+Consumer, lifecycle hooks app.py:2317) (Welle 2) |
| 4 | Live-Erkennungs-Cutover + K3/K4 | §3 (Write-Pfad-Map, K3 detect_phase:178, K4 funnel) (Welle 4) |
| 5 | Intent-Taxonomie §1 als intent_type | §4 (EinwandTypEnum:45 vs ist_vorwand:966→968) (Welle 4) |
| 6 | ModeStrategy-Registry + 2 echte Strategien | §5 (_session_modes:18, session_mode) (Welle 7) |
| 7 | Zombie-Rename + Dual-Write | §6 (ewb_ratings→zombie, objection_events Brücke) (Welle 5) |
| 8 | Single-Source-Putzliste (§0.1) | §7 (alle Verletzungen grep-belegt) (Welle 3) |
| 9 | EWB v2-modular als die EINE Variante | §8 (is_active-Toggle, resolve_prompt_version:67) (Welle 6) |

---

## §1 — intent_event Schema (REQ 1/2)

### Production-Beleg: Tabelle existiert NICHT (Neu-Tabelle, startet leer)
```
$ inspect.sh count intent_event
ERROR:  relation "intent_event" does not exist
LINE 1: SELECT COUNT(*) FROM intent_event;
```
```
$ inspect.sh tables | grep -iE 'intent|objection|ewb'
 public | ewb_ratings            | table | nerve_app
 public | objection_events       | table | nerve_app
$ inspect.sh migrations  →  0015
```
→ intent_event ist neu. Owner muss `nerve_app` werden (`ALTER TABLE … OWNER TO nerve_app`), Migration als `postgres` (nerve_app hat kein CREATE auf schema public). **Startet leer — KEINE Migration der Test-Schrott-Logs** (Acceptance: count=0 vor erstem Live-Call).

### DDL-Empfehlung (hybrid: 9 indizierte relationale Spalten + payload_jsonb)

Aus Gerüst §3. **Empfehlung: die 9 Gerüst-Kern-Metriken werden echte Spalten, die 7 Pflichtfelder gemischt** — Score-/Provenance-Pflichtfelder die später gequeried/aggregiert werden = Spalte; reine Doku-/Kontext-Felder = JSONB-Key.

**Echte indizierte Spalten (Gerüst §3 Kern):**
| Spalte | Typ | Index | Begründung |
|--------|-----|-------|-----------|
| `event_id` | BIGSERIAL / UUID PK | PK | Stabile Event-ID (Slow-Lane Score-Mapping per ID, Bau-Regel 1) |
| `session_id` | TEXT (= SocketIO-sid) | btree | Per-Call-Filter, korreliert mit live_session._session_state[sid] |
| `call_id` | UUID (FK→calls? siehe Risiko) | btree | Verknüpfung zum Call-Record |
| `mode` | varchar(32) | btree | First-Class-Dimension (cold_call/meeting/…) |
| `timestamp` | timestamptz | btree | Zeit-Achse, Latenz-Auswertung |
| `intent_type` | varchar(64) | btree | Taxonomie-Wert (§4) — Kern + custom_objection_* |
| `phase` | smallint (1–6) | btree | Phasen-Achse (getrennt von Intent) — **als int, NICHT string (K3!)** |
| `handling_score_numeric` | smallint NULL | btree | **REQ 2: existiert, bleibt NULL in TAXO1** |
| `confidence` | real | (btree) | Konfidenz (ui_asserted=1.0) |

**payload_jsonb (Rest aus Gerüst §3):** `source` (llm_inferred/ui_asserted), `inference_basis`, `taxonomy_version` (**Pflicht non-null bei jedem Insert** — Acceptance), `abstained`, `speaker_role`, `speaker_id`, `is_simulation`, `origin_type`, `source_context`, `outcome.resolution`, `resolved_at_event_id`, `superseded_by`, `inference_config_id`.

**Die 4+3 rückwirkend-nicht-rekonstruierbaren Pflichtfelder (ab Tag 1, REQ 2 — existieren, bleiben leer):**
| Pflichtfeld | Empfehlung Spalte vs JSONB | Begründung |
|-------------|---------------------------|-----------|
| `handling_score` (1–3) | **Spalte** `handling_score_numeric` (oben) | wird in TAXO2 aggregiert/gefiltert → Spalte; NULL in TAXO1 |
| `was_correct` (nur Training) | JSONB-Key | nur Training-Modus, dünn befüllt |
| `cue_fired` (loud/soft/silent) | JSONB-Key (oder Spalte `cue_fired varchar(8)`) | später Cue-Wirksamkeit; JSONB reicht für TAXO1 |
| `dimension_available`-Map | JSONB-Key | Map-Struktur, JSONB natürlich |
| `reaction_latency_ms` | **Spalte** `reaction_latency_ms int NULL` | Stress-Metrik, numerisch aggregierbar |
| `cue_visible`/`ui_state_hash` | JSONB-Keys | UI-Snapshot, kein Query-Bedarf |
| `speaker_role`+`speaker_id` | JSONB-Keys (Gerüst §3) | Acceptance verlangt „bei jedem Event" → Pflicht-Keys, NULL-Check im Insert |

> **Discretion-Entscheidung dokumentiert:** SPEC erlaubt Spalte ODER dokumentiertes JSONB-Feld. Empfehlung oben optimiert auf TAXO2-Query-Last (Score/Latenz/Provenance als Spalte) ohne Über-Normalisierung. **Cross-AI-Frage:** ist `call_id` ein FK auf `calls(id)` (UUID) — wenn ja, kollidiert das mit „intent_event startet leer + calls bleibt unangetastet"? Empfehlung: `call_id` als **nullable, KEIN harter FK** in TAXO1 (Brücke locker halten, calls bleibt „lebt/untouched"). UNVERIFIED — beim Plan gegen `inspect.sh schema calls` (calls.id-Typ) gegenprüfen.

### §1-ADDENDUM (2026-06-11, Cross-AI Gemini 3.1) — `interaction_id` Moment-Klammer

Nachgetragen NACH dem ersten Cross-AI-Durchlauf (Gemini 3.1) als Foundation für späteres Outcome-verankertes Lernen (Phase H). Quell-Anker: `Nerve-Vault/04 Entscheidungen/NERVE Konstrukt - Soll-Verhalten.md §6`.

**10. indizierte Spalte (zusätzlich zu den 9 Gerüst-§3-Kern-Spalten oben):**
| Spalte | Typ | Index | Begründung |
|--------|-----|-------|-----------|
| `interaction_id` | UUID (= `UUID_TYPE`, models.py:12) NULL | btree (`ix_intent_event_interaction_id`) | **Korrelations-ID pro Kundenmoment** — klammert alle Emits (Fast/Medium/Button) + Cue + Reaktion + Abstain eines Moments zusammen. `event_id` ist pro Emit (mehrere pro Moment → zu fein), `call_id` pro Call (→ zu grob). FK-**Ziel** für spätere `suggestion_reactions` (Phase H); in TAXO1 selbst KEIN harter FK (nullable). |

**Begründung (warum jetzt, nicht später):** Ohne Moment-Klammer ist „welcher ignorierte Vorschlag ↔ welcher Berater-Move ↔ welcher Outcome" später nur per fragilem Zeitstempel-Raten rekonstruierbar — rückwirkend nicht sauber. Spalte+Index ab Tag 1 = billig; nachträglich auf befüllte Tabelle = Abrieb.

**Befüllung (Cutover, §3 / Plan 04):** EINE `interaction_id` (uuid4) pro Kundenmoment. Moment-Anker = Transkript-Zeile (`line_id`/`current_line`) — **derselbe Anker wie `kw_fired_for_line`**. Erzeugt+geteilt von EINER lock-geschützten Quelle `get_or_create_interaction_id(line_id)` am module-globalen `state`-Dict unter `state_lock` (live_session.py:113/118/145; matcher schreibt `kw_fired_for_line` unter `_ls.state_lock` :250-253; app_routes liest `state` unter `ls.state_lock` :322). Erster Emitter einer Zeile mintet, Fast+Medium+Button derselben Zeile teilen die ID; der Writer mintet NICHT. (Race-Mitigation: check-and-set atomar unter `state_lock`; Reentrancy-Falle bei matcher:250 beachten.)

**Schild (Punkt 23):** `interaction_id` endet auf `_id` → L-04 trivial-by-suffix → Guard verlangt KEIN Schild; wie `call_id` bekommt es trotzdem bewusst eins (Klammer-Zweck) → Guard bleibt GRÜN. Spalten-Schild-Text verbatim: „Korrelations-ID pro Kundenmoment — klammert alle Emits (Fast/Medium/Button) + Cue + Reaktion + Abstain eines Moments zusammen; FK-Ziel für spätere suggestion_reactions (Phase H). call_id zu grob, event_id zu fein."

**Out of scope TAXO1:** Outcome-verankertes Lernen / `suggestion_reactions`-Tabelle / Auswertung der Klammer = Phase H. TAXO1 legt nur Spalte+Index+Schild an + füllt sie konsistent pro Moment.

### REQ 2 — kein Scoring in TAXO1
Acceptance: `grep` zeigt keine Score-Formel die `intent_event.handling_score_numeric` schreibt. Heute existiert `compute_readiness_score` (ki_logik.py:95) + Live-Formel (app_routes.py:735) — die bleiben unverändert (Readiness ≠ handling_score; Rider: nur WRITE-per-SID-konsistent). Kein neuer Score-Pfad.

### Schild (Punkt 23) — Entwurf intent_event
> "Zentrale Ereignis-Tabelle (Single Source of Truth) für erkannte Kunden-Intents pro Call (Fast+Medium Lane emittieren; Slow Lane reichert via separatem Score-Objekt an, TAXO2). Hybrid: indizierte Kern-Spalten + payload_jsonb. Status: lebt (neu, TAXO1). Schreibt services/einwand_keyword_matcher.py, services/claude_service.py (analyse_loop/qa-dispatch); liest TAXO2-Scoring (Folge-Phase)."
Plus Spalten-Schilder für nicht-triviale Spalten (mode, intent_type, phase, handling_score_numeric, confidence). Trivial-ausgenommen (L-04): event_id, *_id, timestamp.

---

## §2 — Drei-Bahnen-Scaffolding (REQ 3)

### Runtime-Realität (verifiziert)
```
app.py:2317:  threading.Thread(target=analyse_loop,     daemon=True).start()
app.py:2318:  threading.Thread(target=coaching_loop,    daemon=True).start()
app.py:2344:  socketio.run(app, host='0.0.0.0', port=5000, debug=False)
```
1 Gunicorn-Worker, gthread, 4 Threads, async_mode='threading', Audio bereits entkoppelt → **1 Daemon-Consumer reicht** (Bau-Regel 2).

### Fast Lane — `einwand_keyword_matcher.py` + EWB-Button → emit intent_event
- Heute: matcher schreibt nur das Doppel-Feuer-Flag (matcher:253, global). EWB-Button-Klicks landen in `ls.state['ewb_clicks']` (app_routes.py:323, global) und werden erst bei Call-Ende in objection_events geschrieben (app_routes.py:384).
- TAXO1: Stichwort-Treffer + EWB-Button erzeugen SOFORT eine `intent_event`-Zeile (`source=ui_asserted` für Button, `source=llm_inferred`/lokal für Keyword). `<800ms`, kein LLM.

### Medium Lane — `analyse_loop` (claude_service.py) → emit intent_event
- Heute: analyse_loop schreibt Intent/Phase ins per-SID/globale state + `objection_events` indirekt via ewb_clicks. qa-dispatch (`generate_qa_response`, claude:~1245) ist die KI-Antwort-Bahn (Slot B).
- TAXO1: Haiku-Intent + Phase erzeugen `intent_event` (`source=llm_inferred`, `inference_basis=advisor_paraphrase`/`direct_customer_utterance` je Modus).

### Slow Lane — queue.Queue + 1 Daemon-Consumer + Adapter + Graceful-Flush
**Empfehlung (Discretion):**
- Neue Datei `services/slow_lane.py`: `class SlowLaneQueue` mit Adapter-Interface (`put(event_ref)`, `drain()`), Default-Impl `queue.Queue` (Redis-Impl später ohne Interface-Umbau).
- Consumer-Thread startet **neben** analyse_loop/coaching_loop in `app.py:2317-2318`: `threading.Thread(target=slow_lane_consumer, daemon=True).start()`.
- **Bau-Regel 1:** intent_event nach Erzeugung read-only für Live-Bahnen. Slow Lane arbeitet auf Kopie + schreibt separates Score-Objekt per `event_id` (Merge bei Call-Ende). **In TAXO1: KEIN Score** — die Slow Lane konsumiert nur + persistiert/reichert (Test: kein UPDATE auf bestehende intent_event aus Fast/Medium; kein Scoring-Code).
- **Bau-Regel 2:** Graceful-Shutdown flusht offene Queue-Items in DB. Hook: `atexit.register(...)` ODER SocketIO-Shutdown. **Befund:** heute KEIN `atexit`/Graceful-Shutdown im Code (grep: 0 Treffer für `atexit` in app.py/deepgram_service.py). → Neu bauen: `atexit.register(slow_lane.flush_to_db)` + Sentinel-Item in der Queue für sauberen Consumer-Stop. Test: simulierter Shutdown flusht offene Items.

> **Punkt-14-Flag für Plan:** Der Consumer-Thread-Start ist KEIN Insert in bestehende Funktion (neuer Code in app.py-Bootstrap), aber der Graceful-Flush-Hook + die emit-Calls in matcher/analyse_loop SIND Inserts → 4-Schichten-Audit + Race-Fragen (gleichzeitiges put aus 2 Bahnen; Verbindungsabbruch mid-flush; Worker-Tod mit halbvoller Queue).

---

## §3 — Live-Erkennungs-Cutover (REQ 4) + K3/K4

### Aktueller Write-Pfad (grep-belegt) — was wohin schreibt
| Bahn | Funktion | schreibt heute | Datei:Zeile |
|------|----------|----------------|-------------|
| Fast (Keyword) | matcher | global `kw_fired_for_line` Flag | einwand_keyword_matcher.py:253 |
| Fast (Button) | UI→app_routes | `ls.state['ewb_clicks']` (global) → objection_events bei Call-Ende | app_routes.py:323/384 |
| Medium (Haiku Intent) | analyse_loop | `ergebnis` dict + global `last_einwand_typ` | claude_service.py:966/968, 1145/1148 |
| Medium (Phase) | analyse_loop/detect_phase | per-SID `current_phase` | claude_service.py:1012 |
| Medium (KI-Antwort qa_slot1) | generate_qa_response | Slot-B-Emit (kein DB-Write des Intents) | claude_service.py:~1245 |

→ **Cutover:** ALLE diese Erkennungs-Ausgaben erzeugen zusätzlich/stattdessen `intent_event` (Single Source). objection_events wird zur Spiegelung (§6 Dual-Write).

### K3 — phase_classify int-vs-str (Wurzel exakt lokalisiert)
```
ki_logik.py:176:  if raw_phase == current_phase:        # int==str → False (kein Crash)
ki_logik.py:178:  if raw_phase > current_phase:         # int > str → TypeError  ← K3
claude_service.py:992:  cur_phase = _sid_phase_st.get('current_phase', 1) or 1
claude_service.py:1075:  except Exception as e: print(f"[phase_classify] loop error: {e}")  ← inner except fängt es
```
**Diagnose (deckt sich mit qa-slot1-feuert-fast-nie.md):** `current_phase` kommt als String `"1"` (JSON/DB-Roundtrip) in `detect_phase`, Vergleich `>` (Zeile 178) wirft `'>' not supported between 'int' and 'str'`. Gefangen im inneren except (claude:1075), 8× in 14 Tagen Prod-Logs → Phasen-Klassifikation still tot.
**Fix (im Zuge Medium-Lane-Cutover, NICHT auf kaputtem Pfad neu):** `current_phase` durchgängig als **int** halten (per-SID Single-Source: Seed int 1 ist korrekt an live_session.py:122/335; die String-Kontamination beim Schreiben/Lesen finden — vermutlich beim global-read-Pfad oder DB-Roundtrip). Mit der `current_phase`-Konsolidierung (§7) zwingt man int → K3 löst sich auf. **Belastbarer Defense-in-Depth:** in `detect_phase` `current_phase = int(current_phase)` am Funktionseingang.
> **UNVERIFIED — beim Plan greppen:** wo schreibt etwas `current_phase` als String? Aktuell sind alle gefundenen Writes int (1012, 122, 335). Die Kontamination muss aus einem DB-/JSON-Lade-Pfad kommen — Plan-Zeit `grep -rn "current_phase" services/ routes/` vollständig + inspect.sh auf den Lade-Pfad.

### K4 — qa_slot1 unterfeuert (NICHT überbauen)
**Diagnose (aus Debug-Doc, status=diagnosed):** KEIN Pipeline-Bug. Single-Speaker-Testcalls → `roles_confirmed = ls._second_sp_seen` (deepgram_service.py:71) bleibt False → QA-Gate (live_session.py:695 `if not roles_confirmed or speaker != 0:`) flutet Verkäufer-Monolog in QA → korrekt als `smalltalk_none` unterdrückt (qa_pipeline.py:64). sp_map {0:Berater, 1:Kunde} (live_session.py:886). **Echtes Sekundär-Problem:** `CLASSIFIER_CONFIDENCE_THRESHOLD=0.80` (config.py:42) unterdrückt 13 legitime `frage` bei conf 0.45–0.75 (Haiku clustert ~0.65). [QA-INT]-Marker: claude:1279/1284/1310/1319/1335/1398.
**Fix-Empfehlung TAXO1:** (a) Cutover schreibt intent_event AUCH bei abstain/low-conf (mit `abstained=true`/`confidence`) statt zu droppen → der Funnel wird sichtbar/messbar. (b) Threshold-Tuning 0.80→~0.55 als ENV-gesteuerter Wert prüfen (Marge-Mentalität Punkt 12). **NICHT** den korrekten Single-Speaker-Funnel brechen. Acceptance erfüllt sich erst im 2-Sprecher-Test-Anruf (Kunden-Einwand → intent_event/qa_slot1 feuert).

### Punkt-14-Territory (jeder Insert-Site)
Live-Loop-Inserts die intent_event schreiben: matcher:253-Umfeld, analyse_loop 966/1012/1071-Umfeld, qa-dispatch 1245-Umfeld, EWB-Button-Pfad. Jeder bekommt 4-Schichten-Audit (lokaler Kontext, Funktions-Skelett mit allen return/continue, Cross-File-grep, Edge+Race). Besonders: analyse_loop läuft in Daemon-Thread (app.py:2317) — Cross-Thread-State-Fragen Pflicht.

---

## §4 — Intent-Taxonomie §1 als intent_type (REQ 5)

### D2 — die zwei Welten (grep-belegt)
```
profile_schema.py:45:   EinwandTypEnum = Literal['echt', 'vorschiebe', 'unbekannt']   ← Schema-Enum
profile_schema.py:103:  einwand_typ: EinwandTypEnum = 'unbekannt'
claude_service.py:66/76: Haiku-Prompt fragt "ist_vorwand" (Boolean) + freies "typ"
claude_service.py:966:  'einwand_typ': ergebnis.get('typ', '')        ← freier String
claude_service.py:968:  'ist_vorwand': ergebnis.get('ist_vorwand', False)  ← Boolean
```
→ Zwei parallele Welten: 3-Wert-Enum vs. freier String + Boolean. Live-Klassifikator liest den Enum NICHT.

### Empfehlung — die EINE versionierte Quelle
- **Ort:** `services/profile_schema.py` (D-01-Kandidat), aber die Kern-Taxonomie ist breiter als der heutige 3-Wert-Enum. Empfehlung: neues versioniertes Konstrukt `INTENT_TAXONOMY_V1 = (...)` + `TAXONOMY_VERSION = "v1"` als Modul-Konstante (oder eigene `services/intent_taxonomy.py`, sauberer Single-Source-Ort). Werte = Gerüst §1 Kern + Gemini-Ergänzungen (11 Werte) + `custom_objection_*` (Präfix-Match, kein Enum-Eintrag).
- **`custom_objection_*` ohne Schema-Umbau:** `intent_type` ist `varchar(64)` (Spalte) — akzeptiert `custom_objection_<label>` direkt. Validierung: Kern-Wert ODER `startswith('custom_objection_')`. Pro-Profil-Label aus dem Profil-JSON (`daten`), kein DDL.
- **Migration der alten Pfade:** `ist_vorwand` (Boolean) + freier `typ`-String + `EinwandTypEnum` → auf `intent_type` mappen. Live-Klassifikator (Haiku-Prompt claude:66/76) auf die §1-Werte umstellen, schreibt nur noch `intent_type`. **Alte parallele Pfade LÖSCHEN** (§0.1-Direktive): `ist_vorwand`-Boolean raus, `EinwandTypEnum` durch die eine Quelle ersetzt (oder darauf umgezogen).
- **Phasen-Achse bleibt getrennt** (`phase`-Spalte, smallint) — wird als Kontext mitgegeben, NICHT mit intent vermischt (Gerüst §1: „Phase + Wortlaut + Timing = Klassifizierung").
- **`taxonomy_version` Pflicht** an jedem intent_event-Insert (Acceptance: non-null).

> **Punkt-22-Hinweis:** Vor Festklopfen des Enum-Orts greppen wo `EinwandTypEnum`/`einwand_typ` sonst gelesen wird (profile_schema:431/437, objection_events.einwand_typ-Spalte varchar(100)). objection_events.einwand_typ bleibt (Dual-Write spiegelt den Wert). Cross-Layer: intent_event.intent_type (varchar64) vs objection_events.einwand_typ (varchar100) — Mapping dünn halten.

---

## §5 — ModeStrategy Registry (REQ 6)

### Aktuelle Modus-Quelle (grep-belegt)
```
deepgram_service.py:18:   _session_modes = {}   # {sid: 'cold_call'|'meeting'}
deepgram_service.py:427:  _session_modes[sid] = mode
deepgram_service.py:880:  def handle_manual_mode_toggle(data=None)
live_session.py:995:      mode = (ls._session_state.get(sid) or {}).get('mode', 'meeting')
conversation_logs.session_mode (DB-Spalte, persistiert)
```
Nur `cold_call` + `meeting` real. Keine Registry, kein Routing-Contract — komplett neu.

### Empfehlung — Registry + Strategy-Interface
- **Datei:** `services/mode_strategy.py`. `class ModeStrategy(ABC)` mit `setup_audio_routes()`, `get_classification_prompt()`, `extract_intent(...)`. `MODE_REGISTRY: dict[str, ModeStrategy]` + `register(mode_key, cls)`-Decorator/-Funktion.
- **2 echte Klassen:** `ColdCallStrategy` (Echo-Detektion aus Berater-Paraphrase; Platzhalter-Pflicht; `unknown`/Abstention erlaubt; Konfidenz modus-gedeckelt; speaker fest `berater`/`local`) + `MeetingStrategy` (direkt aus Kunden-Aussage, Sprecher-Trennung).
- **3 künftige Modi als Steckplatz:** `meeting_ext`/`training_cold`/`training_meeting` nur als dokumentierter Registry-Eintrag (NotImplemented-Platzhalter). **KEIN Training-Air-Gap-Bus** (§6 Gerüst — nur Interface aufnahmefähig).
- **Contract-Pflicht JETZT:** intent_event trägt `mode` + `speaker_role` + `speaker_id` bei jedem Event, so geschnitten dass die 3 künftigen Modi ohne Interface-Umbau passen (cold_call = fest berater/local).
- **Routing:** session-mode (heute `_session_modes[sid]` / `_session_state[sid]['mode']`) → `MODE_REGISTRY[mode]` → Strategy. Die per-SID-mode-Lesung ist bereits per-SID (live_session.py:995) — sauber, kein §0.1-Konflikt.
> **Cross-AI-Frage:** `_session_modes` (deepgram_service global dict) vs. `_session_state[sid]['mode']` (live_session per-SID) — sind das zwei Quellen für dasselbe Datum (§0.1-Verletzung)? Beim Plan greppen ob beide geschrieben+gelesen werden. Falls ja: auf die per-SID-Quelle konsolidieren (Registry liest per-SID).

---

## §6 — Zombie-Rename + Dual-Write (REQ 7)

### (a) ewb_ratings → zombie_ewb_ratings
**Production-Schema (verbatim):**
```
$ inspect.sh schema ewb_ratings
 id | integer | not null
 conversation_log_id | integer | not null  (FK→conversation_logs)
 einwand_typ_key | varchar(100) | not null
 klingt_wie_mensch | boolean | not null
 keine_halluzination | boolean | not null
 trifft_einwand | boolean | not null
 rater_id | integer | not null  (FK→users)
 rated_at | timestamp | not null
UNIQUE (conversation_log_id, einwand_typ_key)
$ inspect.sh count ewb_ratings → 0
```
**Leser/Schreiber (grep-belegt) — KORREKTUR zum Blueprint:** ewb_ratings hat **5 aktive Code-Stellen, ALLE in `routes/admin_ewb.py`** (NICHT 0 Reader):
```
admin_ewb.py:19   from database.models import EwbRating
admin_ewb.py:65   db.query(EwbRating).all()                    ← READ
admin_ewb.py:128  LEFT JOIN ewb_ratings r                      ← READ (raw SQL)
admin_ewb.py:181  db.query(EwbRating)                          ← READ
admin_ewb.py:192  db.add(EwbRating(...))                       ← WRITE
database/models.py:422  class EwbRating  __tablename__='ewb_ratings'
```
admin_ewb_bp IST registriert (app.py:2155/2161) — also ein lebendes (wenn auch admin-only, 0-Zeilen) Feature. **Folge fürs Rename:** Die Acceptance „alter Code der ewb_ratings nutzt ist umgestellt oder als tot belegt" heißt hier: admin_ewb.py muss auf `zombie_ewb_ratings` umgestellt ODER (sauberer, da Noten-Engine-thematisch zu TAXO2 gehört) das admin_ewb-Rating-Feature als „schläft bis TAXO2" markiert + auf den neuen Namen gezeigt. **Empfehlung:** Rename-Migration (`ALTER TABLE ewb_ratings RENAME TO zombie_ewb_ratings`) + `EwbRating.__tablename__='zombie_ewb_ratings'` + admin_ewb.py-Referenzen (ORM nutzt __tablename__ automatisch; raw SQL Zeile 128 manuell). `[ZOMBIE]`-Schild. NICHT droppen (0 Zeilen, Rückhol-Sicherung).
> Migration als **postgres** (RENAME auf public). Schild: "Manuelle EWB-Qualitäts-Bewertungen (pre-launch). Status: [ZOMBIE] — gehört zur Noten-Engine TAXO2, schläft. Schreibt+liest routes/admin_ewb.py (umgestellt)."

### (b) objection_events Dual-Write-Brücke (NICHT umbenennen)
**Production-Schema (verbatim):**
```
$ inspect.sh schema objection_events
 id | integer | not null
 user_id | integer | not null  (FK→users)
 org_id | integer | nullable   (FK→organisations)
 conversation_log_id | integer | not null  (FK→conversation_logs)
 einwand_typ | varchar(100) | not null
 success | boolean | nullable   (3-State)
 created_at | timestamp | not null
 antwort_text | text | nullable
 einwand_text | text | nullable
$ inspect.sh count objection_events → 8
```
**Lebende Leser (grep-belegt + Schild bestätigt) — die NICHT brechen dürfen:**
```
WRITE: app_routes.py:384   db_conv.add(ObjectionEvent(...))   ← bei Call-FINALISIERUNG aus ewb_clicks (NICHT live-loop)
READ:  app_routes.py:446/449/471/473   Re-Aggregation Einwand-Zähler + Merge in postcall
READ:  app_routes.py:1389  db.query(ObjectionEvent).filter_by(id=event_id)   (Rating-Detail)
READ:  routes/dashboard.py:736   db.query(ObjectionEvent).filter(conversation_log_id==sid)   ← Dashboard-Einwand-Zähler
READ:  routes/admin_views.py:9   Import ObjectionEvent
REF:   services/integration_engine.py:43   Kommentar (Pattern-Referenz, kein aktiver Query)
```
Schild (Prod verbatim): *"Einzel-Einwand-Ereignis pro Call mit Erfolgs-Status (Kind von conversation_logs). Status: lebt. Schreibt routes/app_routes.py:384; liest routes/app_routes.py:471/1389, routes/dashboard.py:736."*

**Dual-Write-Empfehlung (D-04, dünn):** EIN gekapselter Shim (z.B. `services/objection_bridge.py` oder eine Funktion am Erkennungs-Schreibpfad): intent_event = primär, objection_events = abgeleiteter Spiegel. **Wichtige Erkenntnis:** objection_events wird heute NUR bei Call-Ende aus `ewb_clicks` geschrieben (app_routes.py:384) — NICHT im Live-Loop. → Die dünnste Brücke spiegelt bei Call-Finalisierung die relevanten intent_event-Zeilen (EWB-Button-Events) nach objection_events, GENAU an der bestehenden Stelle (app_routes.py:380-394). `# SUNSET: TAXO2`-Marker. Kein Live-Loop-Dual-Write nötig → minimales Risiko. Acceptance: Dashboard-Einwand-Zähler (dashboard.py:736) stimmt nach Live-Call.

### (c) Unangetastet (bestätigt lebend)
`conversation_logs` (36, Rückgrat), `calls` (37), `call_events` (52), `transcript_segments` (196) bleiben unverändert (Schilder existieren aus SCHILD, „TAXO-Umbau-Zone" aber in TAXO1 NICHT angefasst). FK-Warnung: conversation_logs hat 6 Kinder — NICHT umbenennen.

---

## §7 — Putzliste-Konsolidierung (REQ 8) — der §0.1 Single-Source-Fold

**Kanonische per-SID-Quelle:** `live_session._session_state[sid]['state']` (korrektes Read-Muster: claude_service.py:1259-1267). **Alte globale Pfade LÖSCHEN.**

| # | Datum | WRITE (Beleg) | READ (Beleg) | Verletzung | Konsolidierung |
|---|-------|---------------|--------------|-----------|----------------|
| 1 | `user_id` | per-SID (deepgram setzt `_session_state[sid]['user_id']`) | **global** `ls.state.get('user_id')` claude:452 (sid=None Pfad) | global-read trotz per-SID-Quelle → Prod 164× Warn, EWB v1-pin | analyse-Loop muss per-SID-user_id lesen (Muster claude:1262). Globaler `:452`-Pfad löschen |
| 2 | `current_phase` | **per-SID** claude:1012 (`_sid_phase_st2['current_phase']=new_phase`) | **global** claude:1085 (`ls.state.get('current_phase')`) | split-brain; speist K3 | global-read `:1085` auf per-SID umstellen, int erzwingen (löst K3) |
| 3 | `cold_call_inference` | **per-SID** claude:1071 | **global** claude:1086 | split-brain | global-read `:1086` auf per-SID |
| 4 | `kw_fired_for_line` | **global** matcher:253 (`_ls.state['kw_fired_for_line']`) | **per-SID** claude:1265 (`_sid_st_state.get('kw_fired_for_line')`) | REVERSE → Doppel-Feuer-Schutz tot | matcher muss per-SID schreiben (`_session_state[sid]['state']`). Globaler write:253 + globaler fallback-read:1273 löschen |
| 5 | `score_factors_seen` | global claude:1084 (`ls.state.get('score_factors_seen')`) | global | Cross-Session-Global | per-SID write (Rider: nur WRITE-konsistent, Scoring-Design=TAXO2) |
| 6 | `last_einwand_typ` | **global** claude:1148 (`ls.state['last_einwand_typ']`) | **global** claude:1145 | Cross-Session-Global | per-SID |
| 7 | `kaufbereitschaft` | global claude:982 (`ls.state['kaufbereitschaft']`) | global | Cross-Session-Global (noten-nah) | per-SID write (Rider) |
| 8 | `readiness_score/bucket` | global (compute_readiness liest `ls.state['score_factors_seen']`) | global | noten-nah | per-SID write (Rider) |
| 9 | `current_phase_name`/`phase_confidence` | per-SID claude:1013/1018 | global (Geister-Fallback) | Geister-Feld | echte einzige per-SID-Quelle oder weg |
| 10 | `precall_briefing` | per-SID `_session_state[sid]['_briefing']` (live_session:260) | per-SID get_briefing:266 | **KEIN Geister-Fallback mehr nötig** (Briefing lebt korrekt per-SID, Inventur T5-Korrektur) | verify: globaler Fallback wirklich weg? |
| 11 | `anrede` | per-SID claude:1266 / global-fallback claude:1274 | beides | Global-Fallback | global-fallback `:1274` löschen |
| 12 | `org_id` (K8) | **per-SID** live_session:306 (`init_session_state`) | **global** cost_tracker (`_resolve_org_id_from_live_session` scannt aktive Session) | deployed-aber-tot → org_id NULL auf Prod | siehe unten |

### org_id (K8) — Wurzel im Hintergrund-Thread
cost_tracker wurde bereits gefixt (`_resolve_org_id_from_live_session`, cost_tracker.py:51-62 — scannt aktive per-SID-Session statt global `ls.state`). ABER: **Prod zeigt org_id WEITER NULL** (Inventur K8). Wurzel: die Live-Loop-Kosten-Logs schreiben `user_id=None` HARDCODED:
```
claude_service.py:489/492/499/503  log_api_cost('anthropic','haiku-4-5', user_id=None, ... context_tag='live_haiku')
```
Der Resolver-Scan (`_resolve_*_from_live_session`) wird nur aufgerufen wenn `user_id is None`/`org_id is None` (cost_tracker:95-97) — d.h. der Scan LÄUFT, trifft aber im Daemon-Thread (analyse_loop, app.py:2317) die aktive Session nicht zuverlässig (Multi-SID-Ambiguität / Scan greift nicht). **Fix-Empfehlung TAXO1:** den emittierenden Code (analyse_loop/coaching_loop/qa_pipeline) die per-SID-`org_id`/`user_id`/`session_id` EXPLIZIT durchreichen (`log_api_cost(session_id=sid, org_id=..., user_id=...)`) statt auf den globalen Resolver-Scan zu hoffen. Session durch die Bahnen fädeln (passt zum Drei-Bahnen-Bau). Acceptance: org_id im Kosten-Log eines Live-Calls ≠ NULL.
> **Punkt-21:** api_cost_log-Spalten (org_id/user_id/session_id) gegen inspect.sh schema api_cost_log beim Plan verifizieren (Cross-Layer: Code-Variable vs DB-Spalte).

**Heaviste Punkt-14-Arbeit:** Punkte 1–12 sind Code-Inserts/Löschungen in analyse_loop (Daemon-Thread). 4-Schichten-Audit + Race-Fragen Pflicht je Stelle.

---

## §8 — EWB v2-modular als die EINE Variante (REQ 9)

### Mechanismus (grep + Prod-belegt)
```
prompt_pipeline.py:67:  resolved = variants[user_id % len(variants)]
prompt_pipeline.py:83:  .filter_by(module=module, is_active=True).order_by(PromptVersion.version)
prompt_pipeline.py:44-47: ENV-Override PROMPT_EWB_VERSION_OVERRIDE beats everything
```
```
$ inspect.sh sample prompt_versions  (gefiltert)
 6 | v1-legacy  | ewb | ... | is_active=t | is_default=t
 7 | v2-modular | ewb | ... | is_active=t | is_default=f
```
Beide aktiv → `variants=['v1-legacy','v2-modular']` (alphabetisch) → `variants[user_id % 2]`. user_id=0-Bug pinnt alle auf `[0]`=v1-legacy. **Nach user_id-Fix (§7 Punkt 1) würde `% 2` 50/50 splitten.**

### Empfehlung — v2-modular als EINZIGE aktive Variante (kein 50/50)
**Datenänderung statt Code:** `UPDATE prompt_versions SET is_active=false WHERE module='ewb' AND version='v1-legacy'` (+ optional `is_default=true` auf v2-modular für Konsistenz). Dann `variants=['v2-modular']` → `variants[user_id % 1]` = **immer v2-modular**, deterministisch, kein Split. Cache leeren: `invalidate_resolver_cache()` (prompt_pipeline.py:99). v1-legacy bleibt als Zeile (Reserve, reaktivierbar). **ENV `PROMPT_EWB_VERSION_OVERRIDE` bleibt** Notbremse (<30s reversibel, Punkt 12).
> Migration ist eine Daten-UPDATE (Punkt 20 Pflicht-grep: wird is_active im aktiven Pfad gelesen? JA — prompt_pipeline.py:83). Migration als nerve_app/postgres (UPDATE auf public.prompt_versions, nerve_app hat schreibrecht auf Daten).

**Live-Test (D-05, eigene riskante Welle):** Log-Beleg `[EWB] vv2-modular assembled`. Fachbegriff-Check: ANKER/REFRAME dürfen NICHT als Vokabular in der vorgelesenen Antwort landen (Gerüst §4.5: Technik unsichtbar). **Caveat:** v2-modular ist Brücke, nicht Ziel — TAXO3 ersetzt den hardcoded Prompt durch `build_answer_context()`; nicht über-investieren.

---

## §9 — VERBINDUNGS-KARTE (Punkt 22) — grep file:line + inspect/psql verbatim

> **Regel:** was LEBT gewinnt. Treffer in services/ routes/ database/ alembic/ gezählt, Tests separat. Jede Zeile mit grep-Beleg UND (für Tabellen) inspect.sh-Output.

### Tabellen / Spalten

| Name | Existiert? (inspect.sh) | Typ/Constraints | WER SCHREIBT (file:line, Schicht) | WER LIEST (file:line, Schicht) | Reichweite |
|------|------------------------|-----------------|-----------------------------------|-------------------------------|-----------|
| `intent_event` | **NEIN** — `ERROR: relation "intent_event" does not exist` | wird neu (§1 DDL) | NEU: matcher, analyse_loop, qa-dispatch, EWB-Button → intent_event | NEU: Slow Lane (TAXO2 Scoring) | neu, leer, owner→nerve_app |
| `objection_events` | JA, 8 Zeilen, owner nerve_app | id int PK; user_id int NN FK→users; org_id int FK→organisations; conversation_log_id int NN FK→conversation_logs; einwand_typ varchar(100) NN; success bool (3-State); created_at ts NN; antwort_text text; einwand_text text | app_routes.py:384 (Call-Finalisierung, aus ewb_clicks) — DB-write | app_routes.py:446/449/471/473/1389; dashboard.py:736; admin_views.py:9 — DB-read | Dual-Write-Spiegel, bleibt (SUNSET TAXO2) |
| `ewb_ratings` | JA, 0 Zeilen, owner nerve_app | id int PK; conversation_log_id int NN FK; einwand_typ_key varchar(100) NN; klingt_wie_mensch/keine_halluzination/trifft_einwand bool NN; rater_id int NN FK→users; rated_at ts NN; UNIQUE(conversation_log_id, einwand_typ_key) | admin_ewb.py:192 (db.add) — DB-write | admin_ewb.py:65/128/181 — DB-read | → zombie_ewb_ratings (rename); admin_ewb umstellen |
| `prompt_versions` | JA (v1-legacy id6 active+default, v2-modular id7 active) | module/version/is_active/is_default | app.py-Seed; admin | prompt_pipeline.py:83 (filter is_active) | UPDATE is_active=false bei v1 (REQ 9) |
| `conversation_logs` | JA, 36 (Rückgrat, 6 Kinder) | (SCHILD-beschildert) | app_routes.py (Call-Start/Finalisierung) | dashboard/coaching/performance/learning | UNANGETASTET |
| `calls`/`call_events`/`transcript_segments` | JA (37/52/196) | (SCHILD-beschildert) | live_session/deepgram/app_routes | app_routes/learning/settings | UNANGETASTET |
| `api_cost_log` | JA (4964) | org_id/user_id/session_id (verify schema beim Plan) | cost_tracker.py | admin_dashboard | org_id-Fix (§7 K8) |

### Identifier / Code-Symbole

| Symbol | Treffer (Production-Pfad) | WRITE | READ | Schicht | Status |
|--------|---------------------------|-------|------|---------|--------|
| `user_id` (live-EWB) | claude:452 | per-SID (deepgram) | claude:452 GLOBAL (sid=None) | RAM (_session_state vs ls.state) | KAPUTT (global-read) |
| `current_phase` | claude:992/1012/1085 | claude:1012 per-SID | claude:992 per-SID / 1085 GLOBAL | RAM | KAPUTT (split) + K3 |
| `cold_call_inference` | claude:1071/1086 | claude:1071 per-SID | claude:1086 GLOBAL | RAM | KAPUTT |
| `kw_fired_for_line` | matcher:253 / claude:1265/1273 | matcher:253 GLOBAL | claude:1265 per-SID | RAM | KAPUTT (REVERSE) |
| `score_factors_seen` | claude:1084 | global | claude:1084 global | RAM | Cross-Session-Global |
| `last_einwand_typ` | claude:1145/1148 | claude:1148 global | claude:1145 global | RAM | Cross-Session-Global |
| `kaufbereitschaft` | claude:982 | claude:982 global | dashboard/postcall | RAM→DB | noten-nah (Rider) |
| `org_id` | live_session:306; cost_tracker:51-62; claude:489 (user_id=None) | live_session:306 per-SID | cost_tracker resolver-scan | RAM→DB api_cost_log | deployed-tot (NULL) |
| `EinwandTypEnum` | profile_schema:45/103/431 | profile save | profile_schema | Pydantic/Profil-JSON | D2 — auf §1 umziehen |
| `ist_vorwand` | claude:66/76/968 | Haiku-Prompt | claude:968 | RAM (ergebnis dict) | D2 — löschen, auf intent_type |
| `_session_modes` | deepgram:18/427/435 | deepgram:427 global dict | live_session:995 per-SID `mode` | RAM | Modus-Quelle (verify §0.1) |
| `resolve_prompt_version` | prompt_pipeline:30; claude:459/706 | — | claude:459 (analyse), 706 (manual EWB) | — | EWB-Routing (REQ 9) |
| `analyse_loop`/`coaching_loop` | app.py:2317/2318; claude (def) | Daemon-Thread-Start | — | Thread | Slow-Lane-Consumer hier dazu |
| `EwbRating` | models:422; admin_ewb:19/65/181/192 | admin_ewb:192 | admin_ewb:65/128/181 | DB | rename-Ziel |

### detect_phase (K3-Wurzel, verbatim)
```
ki_logik.py:176:  if raw_phase == current_phase:   return current_phase, raw_confidence
ki_logik.py:178:  if raw_phase > current_phase:    ← int > str TypeError wenn current_phase="1"
claude_service.py:1075:  except Exception as e: print(f"[phase_classify] loop error: {e}")
```

### Lifecycle-Hooks (Slow Lane)
```
app.py:2317:  threading.Thread(target=analyse_loop,  daemon=True).start()
app.py:2318:  threading.Thread(target=coaching_loop, daemon=True).start()
app.py:2344:  socketio.run(...)
grep atexit app.py deepgram_service.py → 0 Treffer (Graceful-Flush muss neu)
```

---

## §10 — Proposed WAVE-CUT (SCHILD-Stil)

Strikte Dependency-Ordnung: **intent_event-Tabelle EXISTIERT (Welle 1) bevor irgendwer schreibt.** Migrationen als `postgres`, Schild via `op.execute("COMMENT ON …")`, SCHILD-Guard MANUELL server-seitig (`NERVE_SCHILD_TEST_DSN=$(grep ^DATABASE_URL= /etc/nerve/.env | cut -d= -f2-) pytest tests/test_schild_guard.py`). Neue Migrations-Nummern ab **0016**.

| Welle | Titel | RISIKO | REQ | Mandatory Plan-Sektionen |
|-------|-------|--------|-----|--------------------------|
| **1** | intent_event-Tabelle anlegen (hybrid, 9 Spalten + JSONB + 4+3 Pflichtfelder leer) + Schild + Owner nerve_app | **schema-addition** (leere Tabelle, KEINE Live-Anbindung → Auto-Check + MANUELLER SCHILD-Guard, kein Test-Anruf) | 1, 2 | P21 (schema intent_event verbatim), P22 (Tabellen-Karte), P23 (Schild) |
| **2** | Drei-Bahnen-Gerüst: SlowLaneQueue + Adapter + 1 Daemon-Consumer (app.py:2317) + Graceful-Flush (atexit), OHNE Live-Anbindung/Scoring | **schema-addition / gerüst** (kein Live-Erkennungs-Eingriff; Consumer läuft leer → Auto-Check; Test: simulierter Shutdown-Flush) | 3 | P14 (Consumer/Flush-Inserts + Race), P21 (intent_event-Write-Schicht) |
| **3** | §0.1-Putzliste-Fold: alle 12 Punkte auf per-SID, alte Globale LÖSCHEN + K3 (current_phase int) + org_id (K8 explizit durchreichen) | **riskant** (faltet Live-Loop, löscht alte Pfade, fixt K3 → Test-Anruf D-03 mit 3 Auflagen) | 8, 4(K3) | P14 (heaviste — jede Stelle 4 Schichten + Race), P21 (api_cost_log Cross-Layer), P22 (Symbol-Karte) |
| **4** | Live-Erkennungs-Cutover: Fast (matcher/Button) + Medium (analyse_loop Haiku) emittieren intent_event als Single Source; §1-Taxonomie als intent_type; ist_vorwand/EinwandTypEnum-Migration; K4 (abstain→intent_event + Threshold) | **riskant** (Live-Erkennung umgelenkt, alte Pfade gelöscht → Test-Anruf cold_call + meeting, Daten-Fluss verifizieren) | 4, 5 | P14 (alle emit-Sites), P21 (intent_event Write), P22 (intent_type-Mapping) |
| **5** | Zombie-Rename ewb_ratings→zombie_ewb_ratings (+admin_ewb umstellen, [ZOMBIE]-Schild) + objection_events Dual-Write-Shim (`# SUNSET: TAXO2`, an app_routes.py:380-394) | **riskant** (Tabellen-Rename + lebende Leser; Test-Anruf: Dashboard-Einwand-Zähler stimmt) | 7 | P21 (schema beider Tabellen verbatim), P22 (Reader-Karte), P23 (Zombie-Schild) |
| **6** | EWB v2-modular als EINZIGE Variante (is_active=false auf v1, invalidate_cache); ENV-Override bleibt | **riskant** (ändert Live-Antwort-Verhalten → eigener Test-Anruf D-05 + Fachbegriff-Check ANKER/REFRAME + Log `[EWB] vv2-modular assembled`) | 9 | P20 (is_active-Lese-Pfad), P21 (prompt_versions), P22 (Routing-Karte) |
| **7** | ModeStrategy-Registry (services/mode_strategy.py) + ColdCall/Meeting echte Klassen + 3 Steckplätze (kein Air-Gap-Bus); session-mode-Routing | **riskant** (ändert Modus-Routing → Test-Anruf beide Modi; verify _session_modes vs per-SID-mode §0.1) | 6 | P14 (Routing-Insert), P22 (mode-Quellen-Karte) |

**Reihenfolge-Begründung:** 1 vor allem (Dependency-Zwang). 2 baut Gerüst ohne Live-Risiko. 3 vor 4 (Putzliste + K3 sauber, BEVOR der Cutover auf den konsolidierten per-SID-Pfaden aufsetzt — sonst baut Cutover auf kaputten Globalen). 4 = der eigentliche Cutover. 5 (Zombie+Brücke) nach Cutover (Brücke spiegelt die neuen intent_event-Events). 6 (EWB) braucht user_id-Fix aus Welle 3. 7 (Registry) zuletzt — strukturell isoliert, profitiert vom fertigen intent_event-Contract (mode/speaker_role/speaker_id).

> **Alternative Welle-Reihenfolge-Erwägung:** §0.1-Putzliste (Welle 3) und Cutover (Welle 4) könnten verschmelzen, da beide analyse_loop anfassen — ABER getrennt halten reduziert Diff-Größe pro Test-Anruf (André: „eine Phase/Tag", Anti-Abrieb). Empfehlung: getrennt. Cross-AI entscheidet.

---

## Validation Architecture

> nyquist_validation: Production-only (kein Local-Pytest als Acceptance). „Test" = inspect.sh-Daten-Fluss-Beleg + manueller SCHILD-Guard + Test-Anruf (D-03).

### Test-Framework
| Property | Value |
|----------|-------|
| Framework | pytest (server-seitig, /opt/nerve/venv) — NUR für SCHILD-Guard + reine Logik-Unit-Tests |
| SCHILD-Guard | `tests/test_schild_guard.py` (existiert, prüft pg_description aller 3 Schemas, skippt ohne DSN) |
| Guard-Lauf (manuell) | `ssh … 'NERVE_SCHILD_TEST_DSN=$(grep ^DATABASE_URL= /etc/nerve/.env \| cut -d= -f2-) /opt/nerve/venv/bin/pytest /opt/nerve/app/tests/test_schild_guard.py -q'` |
| Daten-Fluss-Verify | `inspect.sh count/sample intent_event`, `inspect.sh logs-errors`, `inspect.sh schema <t>` |

### REQ → Verify-Map
| REQ | Behavior | Verify-Typ | Befehl/Beleg |
|-----|----------|-----------|--------------|
| 1 | intent_event existiert, 9 Spalten + JSONB, 7 Pflichtfelder | inspect | `inspect.sh schema intent_event` zeigt alle; Guard grün |
| 2 | kein handling_score-Scoring | grep | keine Score-Formel schreibt handling_score_numeric |
| 3 | Slow-Lane-Queue + Consumer + Flush | unit + sim | simulierter Shutdown flusht Queue (pytest); kein UPDATE intent_event aus Live-Bahn (grep) |
| 4 | Cutover + K3 + K4 | Test-Anruf | `inspect.sh count intent_event` steigt, sample korrekt; logs-errors ohne phase_classify int/str; qa feuert bei Einwand |
| 5 | intent_type Taxonomie + custom | unit | neuer custom-Einwand ohne Schema-Umbau (varchar akzeptiert) |
| 6 | Registry + 2 Klassen | struktur | neuer Modus = neue Klasse ohne Kern-Änderung; intent_event trägt mode/speaker_role/speaker_id |
| 7 | Zombie + Dual-Write | inspect + Test-Anruf | `schema zombie_ewb_ratings` da, ewb_ratings weg; Dashboard-Zähler stimmt |
| 8 | Putzliste gefaltet | grep + logs | kein global-read-neben-per-SID-write; `logs` ohne `user_id leer`; org_id ≠ NULL |
| 9 | v2-modular deterministisch | Test-Anruf | Log `[EWB] vv2-modular assembled`; ENV-Override <30s; keine Baustein-Labels |

### Wave 0 Gaps
- SCHILD-Guard existiert bereits (SCHILD-Phase) — KEIN neuer Guard nötig, nur erweitern (intent_event + zombie_ewb_ratings + neue Spalten müssen beschildert sein, sonst FAILt der Guard automatisch).
- Neu: `tests/test_slow_lane_flush.py` (Graceful-Shutdown-Flush, Welle 2) — Integration-Assertion (Queue-Item nach simuliertem Shutdown in DB), KEIN Source-Presence.
- Neu (optional): Unit-Test für `detect_phase` mit `current_phase="1"` (String) → kein TypeError (K3-Regression).

---

## Security Domain

> security_enforcement aktiv (kein explizites false).

### Applicable ASVS
| Kategorie | Applies | Control |
|-----------|---------|---------|
| V5 Input Validation | yes | intent_type Whitelist (Kern-Enum ∪ custom_objection_*-Präfix); SQL via SQLAlchemy/parametrisiert (inspect.sh ist Whitelist-safe) |
| V6 Cryptography | no | keine neue Krypto |
| V4 Access Control | yes (indirekt) | intent_event trägt org_id/user_id → RLS-Konsistenz (org_id-Fix K8); DSGVO: kein Audio, anonymisierte Transkripte unverändert |

### Threat-Patterns (Stack)
| Pattern | STRIDE | Mitigation |
|---------|--------|-----------|
| SQL-Injection neue Tabelle | Tampering | SQLAlchemy ORM / parametrisierte Queries; intent_type whitelisted |
| Migration als falsche Rolle (nerve_app statt postgres → CREATE-deny) | DoS | Migration explizit als postgres; ALTER OWNER nerve_app danach |
| Dual-Write bricht lebende Leser | Tampering/DoS | dünner Shim, Dashboard-Zähler-Verify im Test-Anruf; objection_events-Schema unverändert |
| Slow-Lane Worker-Tod → Trainingsdaten weg | DoS / Datenverlust | Graceful-Flush (Bau-Regel 2) + Test |

---

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | `current_phase` String-Kontamination kommt aus DB-/JSON-Roundtrip (alle gefundenen Writes sind int) | §3 K3 | Fix greift nicht; Plan muss vollständigen current_phase-Grep machen |
| A2 | `call_id` in intent_event = nullable, KEIN harter FK auf calls | §1 | Falls FK gewünscht → calls.id-Typ (UUID?) prüfen, intent_event-leer-Constraint |
| A3 | objection_events nur bei Call-Ende geschrieben (kein Live-Loop-Write) → Dual-Write nur an app_routes.py:384 nötig | §6 | Falls woanders live geschrieben → Brücke breiter |
| A4 | `_session_modes` (global) vs per-SID `mode` sind NICHT zwei lebende Quellen für dasselbe Datum | §5 | Falls doch → zusätzliche §0.1-Konsolidierung in Welle 7 |
| A5 | v1-legacy is_active=false → variants=['v2-modular'] reicht (kein weiterer Pin-Bug) | §8 | Falls Cache nicht invalidiert → ENV-Override als Notbremse |
| A6 | api_cost_log hat org_id/user_id/session_id-Spalten | §7 K8 | Plan-Zeit `inspect.sh schema api_cost_log` |

---

## Open Questions

1. **current_phase String-Quelle** — vollständiger `grep -rn current_phase services/ routes/` + DB-Lade-Pfad beim Plan (A1).
2. **intent_event.call_id FK ja/nein** — gegen `calls.id`-Typ entscheiden; locker halten empfohlen (A2).
3. **_session_modes §0.1-Status** — beim Plan greppen ob global-dict + per-SID-mode parallel leben (A4).
4. **K4 Threshold-Wert** — 0.80→? : empirisch im 2-Sprecher-Test-Anruf kalibrieren, ENV-gesteuert.
5. **admin_ewb nach Zombie-Rename** — umstellen vs. „schläft bis TAXO2"-markieren (André/Cross-AI).

---

## Environment Availability
| Dependency | Required By | Available | Beleg |
|------------|------------|-----------|-------|
| Postgres (Prod) | alle Migrationen | ✓ | inspect.sh läuft, HEAD 0015 |
| postgres-Rolle | CREATE/RENAME public | ✓ | SCHILD-Präzedenz (Migration 0015 als postgres) |
| nerve_app-Rolle | Daten-UPDATE (prompt_versions), Catalog-Read | ✓ | DISCOVERY-DECISIONS: nerve_app liest pg_description ohne GRANT |
| SCHILD-Guard | Punkt 23 | ✓ | tests/test_schild_guard.py existiert |
| queue/threading (stdlib) | Slow Lane | ✓ | Python-stdlib |
| atexit (stdlib) | Graceful-Flush | ✓ (neu einzubauen) | grep: 0 Treffer heute → neu |

---

## Sources

### Primary (HIGH)
- Production inspect.sh (2026-06-10): intent_event absent, objection_events/ewb_ratings schema+count, migrations 0015, prompt_versions ewb-Varianten, objection_events-Schild.
- Code (git-sync local): claude_service.py (452, 966/968, 992-1095, 1075, 1145-1148, 1259-1267), ki_logik.py (95, 161-205 incl. detect_phase:178), einwand_keyword_matcher.py:253, live_session.py (122/306/335, 995, 260-266), cost_tracker.py (31-127), prompt_pipeline.py (30-99), profile_schema.py (45/103), deepgram_service.py (18/427/746/880), app.py (2155/2161/2314-2318/2344), routes/app_routes.py (323/380-394/446-473/1389), routes/dashboard.py:736, routes/admin_ewb.py (19/65/128/181/192), database/models.py:422.
- Gerüst §0.1/§1/§2/§3/§5 (verriegelt); Inventur §F/§G; qa-slot1-feuert-fast-nie.md (status=diagnosed); SCHILD-04/05-PLAN + DISCOVERY-DECISIONS (Migration-Stil op.execute, GUARD_ROLE nerve_app, NERVE_SCHILD_TEST_DSN).

### Tertiary (LOW / UNVERIFIED)
- A1 current_phase-Quelle, A2 call_id-FK, A6 api_cost_log-Schema — beim Plan verifizieren.

## Metadata
**Confidence breakdown:**
- Schema/Stack (intent_event/objection_events/ewb_ratings): HIGH (inspect.sh verbatim).
- §0.1-Verletzungen + K3: HIGH (grep file:line + detect_phase:178 + Debug-Doc).
- K4: HIGH (Debug-Doc diagnosed) — Threshold-Wert MEDIUM (empirisch zu kalibrieren).
- Wave-Cut/Slow-Lane-Bauform: MEDIUM (Discretion, Cross-AI-pflichtig).
- A1–A6: LOW/UNVERIFIED (explizit markiert).

**Research date:** 2026-06-10
**Valid until:** ~7 Tage (Live-Loop-Code ändert sich schnell; vor Execute re-greppen).
