---
phase: 08.23.2.TAXO1
plan: 05
type: execute
wave: 5
complexity: 🔴
depends_on: [01, 04]
files_modified: [database/models.py, alembic/versions/0017_zombie_ewb_ratings.py, routes/admin_ewb.py, services/objection_bridge.py, routes/app_routes.py]
autonomous: false
requirements: [7]
risiko: riskant
test_anruf: true
user_setup: []
must_haves:
  truths:
    - "ewb_ratings ist hart zu zombie_ewb_ratings umbenannt (0 Zeilen, NICHT gedroppt); [ZOMBIE]-Schild gesetzt; admin_ewb.py zeigt auf den neuen Namen (ORM __tablename__ + raw SQL)."
    - "objection_events wird NICHT umbenannt; EIN gekapselter Dual-Write-Shim (services/objection_bridge.py) spiegelt bei Call-Finalisierung die relevanten intent_event-Events nach objection_events (D-04 minimal, # SUNSET: TAXO2-Marker)."
    - "Der Dashboard-Einwand-Zaehler (dashboard.py:736) + integration_engine + admin_views zeigen nach einem Live-Call weiter korrekte Werte."
    - "conversation_logs/calls/call_events/transcript_segments bleiben unangetastet."
    - "Der Shim hat KEINEN Rueck-Sync, KEIN Backfill, KEINE Zusatz-Features (D-04); EINE Stelle, nicht verstreut."
    - "mirror_intent_events_to_objection_events ist IDEMPOTENT (keyed per conversation_log_id, Cross-AI Finding #3): ein doppeltes /api/beenden fuer dieselbe Session erzeugt KEINE doppelten objection_events-Zeilen → der Dashboard-Einwand-Zaehler (Punkt 21 Daten-Korrektheit) zaehlt nicht doppelt."
    - "SCHILD-Guard server-seitig GRUEN (zombie_ewb_ratings + Schild)."
  artifacts:
    - path: "alembic/versions/0017_zombie_ewb_ratings.py"
      provides: "ALTER TABLE ewb_ratings RENAME TO zombie_ewb_ratings + [ZOMBIE]-COMMENT (op.execute)"
      contains: "RENAME TO zombie_ewb_ratings"
    - path: "services/objection_bridge.py"
      provides: "EIN Expand-Contract-Shim: intent_event-EWB-Events → objection_events bei Call-Ende, # SUNSET: TAXO2"
      contains: "# SUNSET: TAXO2"
    - path: "database/models.py"
      provides: "EwbRating.__tablename__='zombie_ewb_ratings' + [ZOMBIE]-Schild"
      contains: "zombie_ewb_ratings"
  key_links:
    - from: "alembic/versions/0017 (down_revision)"
      to: "0016 (Welle 1)"
      via: "Alembic-Revisions-Kette"
      pattern: "down_revision = '0016'"
    - from: "intent_event EWB-Button-Events (source=ui_asserted)"
      to: "objection_events (Spiegel bei Call-Ende)"
      via: "objection_bridge (app_routes.py:380-394)"
      pattern: "objection_bridge"
    - from: "routes/admin_ewb.py raw SQL :128"
      to: "zombie_ewb_ratings"
      via: "manuelle Umstellung (ORM nutzt __tablename__ automatisch)"
      pattern: "zombie_ewb_ratings"
---

> RISIKO-BANNER: **riskant.** Tabellen-Rename + lebende Leser (admin_ewb) + Dual-Write-Bruecke an lebendem Pfad (Dashboard-Zaehler). → **Test-Anruf PFLICHT (D-03: Dashboard-Einwand-Zaehler muss nach Call weiter stimmen) + MANUELLER SCHILD-Guard (Schema-Welle, D-03 Auflage 2).**

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Geplant, nicht sofort ausgefuehrt. Deploy + Migrations-Apply + Test-Anruf erst nach Cross-AI-Clearance + André-Ineinandergreifen-Check.

> 🔎 **VOR-EXECUTE-RE-GREP (RESEARCH Valid until + Open Question 5):** `grep -rn "ewb_ratings\|EwbRating\|ObjectionEvent\|objection_events" services/ routes/ database/` erneut; admin_ewb-Leser (:65/:128/:181/:192) + objection_events-Leser (app_routes:446/449/471/473/1389, dashboard:736, admin_views:9, integration_engine:43) gegen aktuellen Stand abgleichen. **A3 verifizieren:** objection_events wird heute NUR bei Call-Ende (app_routes:384) geschrieben, kein Live-Loop-Write → Bruecke nur an EINER Stelle noetig.

<objective>
Nur die Erkennungs-Tabellen anfassen, lebende Leser nicht brechen (REQ 7): (a) `ewb_ratings` (0 Zeilen) hart zu `zombie_ewb_ratings` umbenennen (gehoert thematisch zur Noten-Engine TAXO2, schlaeft — [ZOMBIE]-Schild, NICHT droppen), admin_ewb.py auf den neuen Namen zeigen. (b) `objection_events` NICHT umbenennen, sondern EINE duenne Dual-Write-Bruecke bauen (D-04): intent_event = primaer, objection_events = abgeleiteter Spiegel bei Call-Finalisierung, damit Dashboard-Einwand-Zaehler + integration_engine + admin_views nicht brechen. (c) conversation_logs/calls/call_events/transcript_segments unangetastet.

Purpose: §0.1-Zombie-Rename (Geruest): physisch weg = alter Code crasht sofort = Neubau auf leerem Boden; umbenannt ≠ geloescht (Rueckhol-Sicherung). Die Bruecke ist eine bewusste, befristete Expand-Contract-Ausnahme (Sunset TAXO2) — minimal, gekapselt, damit sie selbst keine Bugs in den Live-Pfad einschleppt.

Output: Migration 0017 (RENAME + [ZOMBIE]-Schild), EwbRating-Model-Umstellung + admin_ewb-Anpassung, services/objection_bridge.py (EIN Shim, # SUNSET: TAXO2), Einbau an app_routes:380-394. Deploy + manueller Guard + Test-Anruf — GATED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-RESEARCH.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-04-SUMMARY.md
@database/models.py
@routes/admin_ewb.py
@routes/app_routes.py
@routes/dashboard.py
@alembic/versions/0015_table_column_comments_schilder.py

<interfaces>
<!-- ewb_ratings (RESEARCH §6 inspect.sh verbatim): id, conversation_log_id(FK), einwand_typ_key varchar(100), klingt_wie_mensch/keine_halluzination/trifft_einwand bool, rater_id(FK), rated_at; UNIQUE(conversation_log_id, einwand_typ_key); 0 Zeilen; owner nerve_app. -->
<!-- ewb_ratings-Leser/Schreiber (ALLE in admin_ewb.py): :19 import EwbRating, :65 query.all() READ, :128 LEFT JOIN ewb_ratings r RAW-SQL READ, :181 query READ, :192 db.add WRITE. models.py:422 class EwbRating __tablename__='ewb_ratings'. admin_ewb_bp registriert app.py:2155/2161. -->
<!-- objection_events (RESEARCH §6 inspect.sh verbatim): id, user_id(FK), org_id(FK nullable), conversation_log_id(FK), einwand_typ varchar(100), success bool(3-state), created_at, antwort_text, einwand_text; 8 Zeilen; owner nerve_app. -->
<!-- objection_events-WRITE: app_routes.py:384 db_conv.add(ObjectionEvent(...)) bei Call-FINALISIERUNG aus ewb_clicks (NICHT live-loop, A3). -->
<!-- objection_events-READ (duerfen NICHT brechen): app_routes:446/449/471/473 (Re-Aggregation Zaehler), :1389 (Rating-Detail), dashboard.py:736 (Dashboard-Einwand-Zaehler), admin_views.py:9 (import), integration_engine.py:43 (Kommentar-Referenz). -->
<!-- Prod-HEAD nach Welle 1 = 0016 → diese Migration revision='0017', down_revision='0016'. RENAME als postgres. -->
<!-- Welle 4: intent_event traegt EWB-Button-Events (source=ui_asserted). Die Bruecke spiegelt diese bei Call-Ende nach objection_events. -->
<!-- conversation_logs (36, 6 Kinder), calls(37), call_events(52), transcript_segments(196) — UNANGETASTET (SCHILD-beschildert). -->
<!-- SCHILD-Guard: tests/test_schild_guard.py; manueller Lauf mit NERVE_SCHILD_TEST_DSN. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Zombie-Rename — EwbRating-Model + Migration 0017 (RENAME + [ZOMBIE]-Schild) + admin_ewb.py umstellen</name>
  <read_first>
    - ⚠️ RE-GREP zuerst (Open Question 5: admin_ewb umstellen vs "schlaeft"-markieren)
    - database/models.py:422 (class EwbRating __tablename__='ewb_ratings' + comment=) — VERBATIM
    - routes/admin_ewb.py:19/65/128/181/192 (Leser/Schreiber) — 30 Zeilen vor/nach JEDER VERBATIM (insb. raw SQL :128)
    - alembic/versions/0015_*.py (op.execute COMMENT-Muster) + RESEARCH §6 (Schild-Text)
  </read_first>
  <action>
    (a) **Model (models.py:422):** `EwbRating.__tablename__ = 'zombie_ewb_ratings'`. Tabellen-Schild (`__table_args__` comment, RESEARCH §6): "Manuelle EWB-Qualitaets-Bewertungen (pre-launch). Status: [ZOMBIE] — gehoert zur Noten-Engine TAXO2, schlaeft. Schreibt+liest routes/admin_ewb.py (umgestellt)."
    (b) **Migration 0017** `alembic/versions/0017_zombie_ewb_ratings.py`, revision='0017', down_revision='0016'. upgrade(): `op.execute("ALTER TABLE public.ewb_ratings RENAME TO zombie_ewb_ratings")` (als postgres) + `op.execute("COMMENT ON TABLE public.zombie_ewb_ratings IS '<[ZOMBIE]-Schild>'")` + ggf. nicht-triviale Spalten-COMMENTs (einwand_typ_key, klingt_wie_mensch, keine_halluzination, trifft_einwand — Schilder uebernehmen/ergaenzen). downgrade(): `op.execute("ALTER TABLE public.zombie_ewb_ratings RENAME TO ewb_ratings")` + COMMENT zurueck/NULL (reversibel, kein Drop). Index/UNIQUE wandern automatisch mit RENAME.
    (c) **admin_ewb.py umstellen (Open Question 5 — Empfehlung RESEARCH §6: umstellen, nicht nur markieren):** ORM-Queries (:65/:181/:192) nutzen `EwbRating.__tablename__` automatisch → laufen nach Model-Aenderung auf zombie_ewb_ratings. **Raw SQL :128 (`LEFT JOIN ewb_ratings r`) MANUELL** auf `LEFT JOIN zombie_ewb_ratings r` umstellen. Damit crasht admin_ewb nicht (das Feature schlaeft bis TAXO2, zeigt aber auf den richtigen Namen).
    KEIN Drop. KEIN Daten-Verlust (0 Zeilen, aber Rueckhol-Sicherung).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal (models.py + 0017). Production via Task 3.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "__tablename__ = 'zombie_ewb_ratings'" database/models.py` 1 Treffer; `grep "\[ZOMBIE\]" database/models.py` 1 Treffer (Schild).
    - `grep "RENAME TO zombie_ewb_ratings" alembic/versions/0017_*.py` 1 Treffer (upgrade); `grep "RENAME TO ewb_ratings" alembic/versions/0017_*.py` 1 Treffer (downgrade, reversibel).
    - `grep -n "ewb_ratings" routes/admin_ewb.py` zeigt KEIN `LEFT JOIN ewb_ratings ` mehr (raw SQL :128 auf zombie umgestellt); ORM-Referenzen nutzen das Model.
    - `grep "down_revision = '0016'" alembic/versions/0017_*.py`.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>ewb_ratings → zombie_ewb_ratings (Model + Migration + admin_ewb), [ZOMBIE]-Schild, reversibel, nicht gedroppt.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: objection_bridge.py — EIN duenner Dual-Write-Shim (intent_event-EWB-Events → objection_events bei Call-Ende, # SUNSET: TAXO2)</name>
  <read_first>
    - ⚠️ RE-GREP zuerst (A3: objection_events nur bei Call-Ende geschrieben?)
    - routes/app_routes.py:380-394 (Call-Finalisierung, ObjectionEvent-Write aus ewb_clicks :384) — 30 Zeilen vor/nach VERBATIM
    - routes/dashboard.py:736 (Dashboard-Einwand-Zaehler, der NICHT brechen darf) — VERBATIM
    - database/models.py ObjectionEvent (Spalten) + IntentEvent (Welle 1)
    - CONTEXT D-04 (minimal, EIN gekapselter Shim, # SUNSET: TAXO2, kein Rueck-Sync/Backfill)
    - RESEARCH §6(b) Dual-Write-Empfehlung
  </read_first>
  <action>
    NEUE Datei `services/objection_bridge.py` — EIN gekapselter Expand-Contract-Shim:
    - `# SUNSET: TAXO2 — diese Bruecke fliegt in TAXO2 raus (objection_events-Zombifizierung + Leser-Umzug). KEIN Rueck-Sync, KEIN Backfill, keine Zusatz-Features (D-04).`
    - `def mirror_intent_events_to_objection_events(*, conversation_log_id, session_id, db) -> int:` — liest die relevanten intent_event-EWB-Events dieser Session (source=ui_asserted, also Button-Klicks — die heute via ewb_clicks → objection_events flossen) und schreibt pro Event EINE ObjectionEvent-Zeile mit den Spalten die die Leser brauchen: user_id, org_id, conversation_log_id, einwand_typ (= intent_type oder gemappt auf varchar100), success (3-state, wie heute), created_at, antwort_text, einwand_text. Return Anzahl gespiegelter Zeilen.
    - **IDEMPOTENZ-GUARD (Cross-AI Finding #3 — PFLICHT, keyed per conversation_log_id):** der Spiegel MUSS idempotent sein, sonst zaehlt ein doppeltes `/api/beenden` fuer dieselbe Session den Dashboard-Einwand-Zaehler (dashboard.py:736) doppelt (Punkt-21-Daten-Korrektheit). **Implementierung (Gemini-Empfehlung, robust):** am Funktionseingang ZUERST die von der Bruecke gespiegelten Zeilen dieser Session loeschen — `db.query(ObjectionEvent).filter(ObjectionEvent.conversation_log_id == conversation_log_id).delete(synchronize_session=False)` — DANN frisch aus intent_event re-spiegeln. So konvergiert ein Doppel-Aufruf auf denselben Zeilen-Satz (last-write-wins, kein Duplikat). **Alternative (falls DELETE zu breit greift):** check-then-skip — `if db.query(ObjectionEvent).filter_by(conversation_log_id=conversation_log_id).first(): return 0` (first-write-wins). Die DELETE-then-INSERT-Variante ist vorzuziehen (re-spiegelt sauber bei geaenderten intent_events); die gewaehlte Variante im Code-Kommentar + SUMMARY dokumentieren. **Begruendung warum DELETE-by-conversation_log_id sicher ist:** in TAXO1 ist die Bruecke der EINZIGE objection_events-Schreiber fuer Live-Calls (ersetzt app_routes:384, Task 3); historische 8 Zeilen haben andere conversation_log_id → unberuehrt.
    - Mapping duenn halten (RESEARCH §4-Hinweis): intent_event.intent_type (varchar64) → objection_events.einwand_typ (varchar100) direkt; keine Transformation ausser Laengen-Vertraeglichkeit.
    - KEINE Live-Loop-Schreibung (A3: objection_events nur bei Call-Ende) → die Bruecke laeuft NUR bei Call-Finalisierung.
    - KEIN Lesen-aus-objection_events-zurueck (kein Rueck-Sync, ausser dem Idempotenz-Guard-DELETE der eigenen Zeilen). KEIN Backfill alter Zeilen.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Task 3): Dashboard-Zaehler stimmt.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "# SUNSET: TAXO2" services/objection_bridge.py` 1 Treffer (Pflicht-Marker D-04).
    - `grep "def mirror_intent_events_to_objection_events" services/objection_bridge.py` 1 Treffer.
    - **Idempotenz-Guard vorhanden (Finding #3):** `grep -nE "conversation_log_id.*\.delete\(|filter_by\(conversation_log_id|filter\(ObjectionEvent.conversation_log_id" services/objection_bridge.py` zeigt den keyed DELETE-then-INSERT (oder check-then-skip) am Funktionseingang.
    - KEIN Rueck-Sync/Backfill: `grep -iE "backfill|sync.*back|UPDATE.*intent_event" services/objection_bridge.py` == 0 (der Idempotenz-DELETE auf objection_events ist kein Rueck-Sync zu intent_event).
    - Shim ist EINE Datei/Funktion (nicht ueber Code verstreut) — Cross-File-grep zeigt nur EINE Aufruf-Stelle (Task 3).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>EIN gekapselter, befristeter Dual-Write-Shim (Sunset-Marker), idempotenter Spiegel (keyed per conversation_log_id, Finding #3), kein Rueck-Sync/Backfill.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Bruecke an app_routes:380-394 einbauen (Call-Finalisierung) — bestehenden ewb_clicks→objection_events-Pfad durch intent_event-Spiegel ersetzen/ergaenzen</name>
  <read_first>
    - ⚠️ RE-GREP zuerst
    - routes/app_routes.py:380-394 (bestehender ObjectionEvent-Write aus ewb_clicks) — 30 Zeilen vor/nach VERBATIM
    - services/objection_bridge.py (mirror-Funktion, Task 2)
  </read_first>
  <action>
    Punkt-14-Insert an der Call-Finalisierung. Control-Flow/Skelett/Cross-File aus Sektionen 1-3.
    - An der bestehenden Stelle (app_routes:380-394) wo heute `db_conv.add(ObjectionEvent(...))` aus ewb_clicks laeuft: den Schreib-Pfad auf die Bruecke umstellen — statt direkt aus ewb_clicks zu schreiben, `mirror_intent_events_to_objection_events(conversation_log_id=..., session_id=sid, db=db_conv)` aufrufen. intent_event (Welle 4, ui_asserted-Button-Events) ist jetzt die Quelle; objection_events der Spiegel.
    - **Vorsicht (Idempotenz/Doppel-Write):** sicherstellen dass NICHT beide Pfade schreiben (alt: ewb_clicks-direkt + neu: Bruecke) → sonst doppelte objection_events-Zeilen → Dashboard-Zaehler doppelt. Den alten ewb_clicks-direkt-Write durch den Bruecken-Aufruf ERSETZEN (nicht beide). Falls ewb_clicks woanders noch gebraucht wird (grep): nur den objection_events-Schreibteil ersetzen.
    - Die Leser (dashboard:736 etc.) bleiben unveraendert — sie lesen objection_events weiter, das jetzt von der Bruecke befuellt wird.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Task 4): Dashboard-Zaehler korrekt, keine Doppel-Zeilen.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mirror_intent_events_to_objection_events" routes/app_routes.py` 1 Treffer (EINE Aufruf-Stelle).
    - Kein Doppel-Write: der alte direkte `ObjectionEvent(...)`-Write aus ewb_clicks ist ersetzt (nicht zusaetzlich) — grep zeigt nur den Bruecken-Pfad fuer objection_events-Schreibung.
    - dashboard.py:736 unveraendert (`git diff routes/dashboard.py` == 0).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Bruecke an EINER Stelle (Call-Finalisierung) verdrahtet; objection_events-Leser unveraendert; kein Doppel-Write.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Zombie-Rename (0017) + EwbRating-Model + admin_ewb + objection_bridge-Shim + Einbau. Bereit fuer Deploy + manuellen Guard + Test-Anruf.</what-built>
  <how-to-verify>
    ⚠️ **GATE: Deploy + Migrations-Apply + Test-Anruf ERST NACH Cross-AI-Clearance + André-Ineinandergreifen-Check.** Nach Clearance:

    1. `git add -A && git commit && git push origin main` → `bash deploy.sh production`.
    2. Migration als **postgres** (RENAME auf public): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u postgres bash -c "source /opt/nerve/venv/bin/activate && alembic upgrade head"'`.
    3. Rename-Beleg (REQ 7): `ssh ... 'bash scripts/inspect.sh schema zombie_ewb_ratings'` existiert; `ssh ... 'bash scripts/inspect.sh count ewb_ratings'` → ERROR relation does not exist (alter Name weg). Verbatim in SUMMARY.
    4. **MANUELLER SCHILD-Guard (D-03 Auflage 2):** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && NERVE_SCHILD_TEST_DSN=$(grep ^DATABASE_URL= /etc/nerve/.env | cut -d= -f2-) /opt/nerve/venv/bin/pytest tests/test_schild_guard.py -q'` → **GRUEN** (zombie_ewb_ratings beschildert). Verbatim in SUMMARY.
    5. **TEST-ANRUF (D-03 Auflage 3) — Fokus Dashboard-Einwand-Zaehler:**
       - **(a) Dashboard-Zaehler stimmt:** vor Anruf Zaehler notieren; Test-Anruf mit EWB-Button-Klicks (intent_event ui_asserted-Events); nach Call-Ende: `ssh ... 'bash scripts/inspect.sh count objection_events'` STEIGT um die Anzahl Button-Klicks (Bruecke gespiegelt); Dashboard-Einwand-Zaehler im Browser zeigt korrekte Zahl (kein Doppel, keine Null).
       - **(a2) Idempotenz (Finding #3):** `/api/beenden` fuer dieselbe Session ein ZWEITES Mal ausloesen (z.B. Doppel-Klick/Reload am Call-Ende oder erneuter end_session-Request); danach `ssh ... 'bash scripts/inspect.sh count objection_events'` zeigt DIESELBE Zahl wie nach (a) — KEINE Verdopplung; Dashboard-Zaehler unveraendert. Verbatim in SUMMARY.
       - **(b) intent_event weiter befuellt (Welle 4 intakt):** `ssh ... 'bash scripts/inspect.sh count intent_event'` steigt.
       - **(c) lebende Tabellen intakt:** Dashboard/Coaching/Transkript-Reiter funktionieren (conversation_logs/calls/call_events/transcript_segments unveraendert).
       Verbatim-Belege in SUMMARY.
  </how-to-verify>
  <resume-signal>"0017 live + zombie_ewb_ratings da + ewb_ratings weg + Guard GRUEN + Test-Anruf: objection_events-Zaehler stimmt (keine Doppel) + Idempotenz (zweites /api/beenden = gleiche Anzahl) + Dashboard/Coaching/Transkript OK" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 1. Lokaler Kontext
Insert-/Aenderungs-Sites: models.py:422 (EwbRating), admin_ewb.py:128 (raw SQL), app_routes.py:380-394 (Call-Finalisierung, ObjectionEvent-Write). objection_bridge.py = neue Datei. **Pflicht beim Bau:** app_routes:380-394 + admin_ewb:128 je 30 Zeilen vor/nach VERBATIM + return/continue/raise listen. KRITISCH: an app_routes:380-394 NICHT beide Schreibpfade aktiv lassen (Doppel-Write → Doppel-Zaehler). RE-GREP zuerst — Welle 4 hat app_routes (EWB-Button :323) schon angefasst; pruefen dass der Welle-4-Emit (ui_asserted) + die Welle-5-Bruecke (Call-Ende-Spiegel) sich nicht doppeln.

## 2. Funktions-Skelett
- **Call-Finalisierung (app_routes, ~ /api/end_session):** Eingang → Aggregation/Merge → [bestehend: ObjectionEvent-Write aus ewb_clicks :384] → [ERSETZT durch: mirror_intent_events_to_objection_events(...)] → conversation_log finalisieren → return. Der Bruecken-Aufruf ersetzt den direkten Write an genau dieser Stelle.
- **mirror_intent_events_to_objection_events (neue Funktion):** Eingang → **[IDEMPOTENZ-GUARD: DELETE objection_events WHERE conversation_log_id == clid (eigene Spiegel-Zeilen), Finding #3]** → query intent_event WHERE session_id + source=ui_asserted → for event: db.add(ObjectionEvent(...)) → db nicht committen (Caller-Transaktion) oder eigenes commit (dokumentieren) → return count. Re-spiegelt sauber bei Doppel-Aufruf (last-write-wins), kein UPDATE auf intent_event.
- **admin_ewb-Queries:** ORM nutzt __tablename__ (automatisch zombie); raw SQL :128 manuell. Feature schlaeft (admin-only, 0 Zeilen).

## 3. Cross-File-Awareness
- `grep -rn "EwbRating\|ewb_ratings" services/ routes/ database/` — models:422, admin_ewb:19/65/128/181/192. Nach Rename: alle zeigen auf zombie_ewb_ratings (ORM via __tablename__, raw SQL manuell). KEIN anderer Leser (RESEARCH §6: 5 Stellen, alle admin_ewb).
- `grep -rn "ObjectionEvent\|objection_events" services/ routes/` — WRITE app_routes:384 (→Bruecke), READ app_routes:446/449/471/473/1389, dashboard:736, admin_views:9, integration_engine:43. Die READ-Stellen bleiben ALLE unveraendert (sie lesen objection_events, das die Bruecke befuellt). Cross-Check: kein Leser bricht weil die Spalten gleich bleiben (objection_events NICHT umbenannt, Schema unveraendert).
- `grep -rn "ewb_clicks" services/ routes/` — pruefen ob ewb_clicks ausser fuer objection_events-Write noch woanders gebraucht wird; nur den objection_events-Schreibteil ersetzen, ewb_clicks-State sonst belassen.
- `grep -rn "mirror_intent_events\|objection_bridge" services/ routes/` nach Bau: EINE Aufruf-Stelle (app_routes-Finalisierung) + die Datei. Gekapselt (D-04).

## 4. Edge-Case-Analyse
- **Edge 1 — Call ohne EWB-Button-Klicks:** intent_event hat keine ui_asserted-Events fuer die Session → Bruecke spiegelt 0 Zeilen → objection_events unveraendert → Dashboard-Zaehler 0 (korrekt, kein Crash).
- **Edge 2 — Doppel-Finalisierung (end_session/`/api/beenden` zweimal) — Cross-AI Finding #3:** Bruecke wuerde ohne Guard doppelt spiegeln → Doppel-Zeilen → Dashboard-Einwand-Zaehler doppelt (Punkt-21-Datenkorrektheit-Verletzung). **Behandlung (jetzt PFLICHT, nicht "falls Risiko"):** der Idempotenz-Guard in mirror() (Task 2) loescht am Eingang die eigenen Spiegel-Zeilen dieser conversation_log_id und re-spiegelt frisch (DELETE-then-INSERT, last-write-wins) → ein zweiter `/api/beenden` konvergiert auf denselben Zeilen-Satz, KEINE Duplikate. Test-Anruf-Beleg: zweimaliges Finalisieren erzeugt dieselbe objection_events-Anzahl.
- **Edge 3 — admin_ewb raw SQL nicht umgestellt:** :128 LEFT JOIN ewb_ratings crasht nach Rename (relation does not exist). Behandlung: Task 1(c) stellt :128 explizit um; Test: admin_ewb-Seite laedt ohne Fehler (oder Feature schlaeft sauber).

### Race-Condition-Audit
1. **Gleichzeitiges Feuern (2 Calls finalisieren parallel):** jede Bruecken-Ausfuehrung filtert auf ihre session_id/conversation_log_id → SID-isoliert, kein Cross-Call-Doppel. objection_events-Inserts sind unabhaengig (verschiedene conversation_log_id).
2. **Verbindungsabbruch mid-Operation (Finalisierung bricht ab):** Bruecke laeuft in der Finalisierungs-Transaktion → Abbruch rollt zurueck, keine halben Spiegel-Zeilen. Beim naechsten Finalisierungs-Versuch (falls Retry) Edge-2-Dedup greift.
3. **Wiederverbinden mit altem Zustand:** Rename ist einmalig (Migration). Nach Reconnect liest admin_ewb (falls aufgerufen) zombie_ewb_ratings — kein alter Name lebt. intent_event/objection_events per Session sauber.
4. **Tab im Hintergrund:** Finalisierung ist server-seitig (end_session) — Tab-Fokus irrelevant. Dashboard-Zaehler liest beim naechsten Dashboard-Load aus objection_events (Server-Quelle).

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `ewb_ratings` → `zombie_ewb_ratings` (RENAME, public, als postgres). 0 Zeilen.
- `objection_events` (SCHREIBEN via Bruecke, insert-only — NICHT umbenannt). 8 Zeilen + neue Spiegel.
- `intent_event` (LESEN via Bruecke — ui_asserted-Events). Schema Welle 1.
- conversation_logs/calls/call_events/transcript_segments — UNANGETASTET.

### inspect.sh-Beleg (RESEARCH §6 verbatim — beide Schemas)
```
$ inspect.sh schema ewb_ratings
 id | integer | not null
 conversation_log_id | integer | not null  (FK→conversation_logs)
 einwand_typ_key | varchar(100) | not null
 klingt_wie_mensch / keine_halluzination / trifft_einwand | boolean | not null
 rater_id | integer | not null  (FK→users)
 rated_at | timestamp | not null
 UNIQUE (conversation_log_id, einwand_typ_key) ; count → 0
$ inspect.sh schema objection_events
 id | integer | not null ; user_id int NN(FK) ; org_id int nullable(FK) ; conversation_log_id int NN(FK)
 einwand_typ varchar(100) NN ; success bool(3-state) ; created_at ts NN ; antwort_text text ; einwand_text text ; count → 8
```
→ Nach 0017: `inspect.sh schema zombie_ewb_ratings` (Task 3 Schritt 3, verbatim in SUMMARY); objection_events-Schema UNVERAENDERT.

### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| EwbRating.__tablename__ | ORM + raw SQL :128 | DB zombie_ewb_ratings (umbenannt) | ⬜ inspect.sh schema zombie_ewb_ratings; admin_ewb laedt |
| objection_events-Spiegel | intent_event(ui_asserted) → Bruecke → objection_events | DB objection_events (insert-only, Schema unveraendert) | ⬜ Test-Anruf: count objection_events steigt, Zaehler stimmt |
| Dashboard-Einwand-Zaehler | dashboard.py:736 liest objection_events | DB objection_events (unveraendert) | ⬜ Test-Anruf (a): Browser-Zaehler korrekt |
| ewb_clicks (alt) | nur objection_events-Schreibteil ersetzt | RAM (ls.state) — sonst belassen | ⬜ grep: kein Doppel-Write |

### Bei Diskrepanz (zombie_ewb_ratings fehlt / objection_events-Schema veraendert / Doppel-Zeilen): STOP + Migration/Bruecke korrigieren.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Migration (RENAME) → Production-DB | als postgres; FK/Index wandern mit; reversibel. |
| Bruecke → objection_events (lebende Leser) | Spiegel-Write darf Leser nicht brechen (Dashboard-Zaehler). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO1-14 | D (DoS) | admin_ewb crasht nach Rename (raw SQL :128 zeigt auf alten Namen) | mitigate | Task 1(c) stellt raw SQL um; ORM via __tablename__; admin_ewb-Load-Test. |
| T-TAXO1-15 | T (Tampering)/DoS | Dual-Write bricht Dashboard-Zaehler (Doppel/Null-Zeilen); doppeltes /api/beenden zaehlt doppelt (Finding #3) | mitigate | EINE Aufruf-Stelle (kein Doppel-Write-Pfad); mirror() idempotent per conversation_log_id (DELETE-then-INSERT, Finding #3) → Doppel-Finalisierung konvergiert; Test-Anruf belegt Zaehler + zweimaliges Finalisieren = gleiche Anzahl. |
| T-TAXO1-16 | D (Datenverlust) | Drop statt Rename → 0-Zeilen-Tabelle weg (keine Rueckhol-Sicherung) | mitigate | RENAME (kein DROP); downgrade benennt zurueck; [ZOMBIE]-Schild dokumentiert Schlaf-Status. |
</threat_model>

<verification>
- zombie_ewb_ratings existiert, ewb_ratings weg, [ZOMBIE]-Schild; admin_ewb umgestellt (ORM + raw SQL).
- objection_bridge: EIN Shim, # SUNSET: TAXO2, kein Rueck-Sync/Backfill; EINE Aufruf-Stelle; idempotent (keyed conversation_log_id, Finding #3).
- Test-Anruf: objection_events-Zaehler steigt korrekt (keine Doppel), zweites /api/beenden = gleiche Anzahl (Idempotenz), Dashboard/Coaching/Transkript OK; intent_event weiter befuellt.
- SCHILD-Guard GRUEN; objection_events-Schema unveraendert; lebende Tabellen unangetastet.
</verification>

<success_criteria>
- zombie_ewb_ratings da, ewb_ratings weg, alter Code umgestellt/tot belegt (REQ 7).
- Dashboard-Einwand-Zaehler nach Live-Call korrekt (objection_events parallel befuellt); lebende Tabellen unveraendert (REQ 7).
- Dual-Write minimal (EIN Shim, Sunset-Marker, kein Rueck-Sync/Backfill — D-04) UND idempotent (keyed conversation_log_id — doppeltes /api/beenden zaehlt nicht doppelt, Finding #3).
- SCHILD-Guard GRUEN.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-05-SUMMARY.md` (inkl. inspect.sh schema zombie_ewb_ratings + ewb_ratings-weg-Beleg + Guard-GREEN + Test-Anruf objection_events-Zaehler-Beleg (keine Doppel) + Idempotenz-Beleg (zweites /api/beenden = gleiche Anzahl, Finding #3) + Bestaetigung objection_events-Schema + lebende Tabellen unveraendert).
</output>
