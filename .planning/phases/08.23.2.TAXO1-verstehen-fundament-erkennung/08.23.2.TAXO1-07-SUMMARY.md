---
phase: 08.23.2.TAXO1
plan: 07
subsystem: api
tags: [intent_event, mode_strategy, registry, anonymization, deepgram, speaker_role, dsgvo]

# Dependency graph
requires:
  - phase: 08.23.2.TAXO1-04
    provides: emit_intent_event-Writer + intent_event-Schema (mode/speaker_role/speaker_id/inference_basis)
  - phase: 08.23.2.TAXO1.MEDFIX
    provides: SYSTEM_PROMPT_BASE als der EINE Klassifikations-Prompt (Medium-Lane-Fix)
provides:
  - services/mode_strategy.py — ModeStrategy(ABC) + MODE_REGISTRY + ColdCall/Meeting + 3 Steckplaetze
  - Modus als First-Class-Dimension ueber Strategy-Pattern (REQ 6)
  - Sprecher-Bug behoben — cold_call schreibt speaker_role='berater'/advisor_paraphrase statt faelschlich 'kunde'
  - FUND 3 — anonymisierter triggering_text denormalisiert in intent_event.payload_jsonb (Fundament TAXO2/3)
  - FUND 2 — Call-Start-Race eliminiert (per-SID mode VOR _open_deepgram_connection)
  - §0.1 — globales _session_modes geloescht, per-SID einzige Quelle der cold_call/meeting-Achse
  - Refinement 1 + FUND 1 — EWB-Button schreibt genau EINE markierte Kunde-Transcript-Zeile (`*ewb button*`-Suffix)
affects: [TAXO2, TAXO3, intent_event, mode_strategy, anonymization]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Strategy-Registry (Decorator-registriertes Modul-Dict MODE_REGISTRY) als deklarative Routing-/Audibility-Tabelle"
    - "Zustandslose Strategy-Instanzen (Kontext nur via Argumente, nichts auf self) — Race-Safety bei parallelen SIDs"
    - "Per-SID-Modus-Quelle (_session_state[sid]['mode']) als die EINE cold_call/meeting-Achse, Call-Start-only"
    - "Denormalisierter, vor-Persist-anonymisierter triggering_text als JSON-Key (keine Schema-Migration)"
    - "Round-trip-sicherer Transcript-Marker als Text-Suffix (ueberlebt _build_log_content), nicht als data-Flag"

key-files:
  created:
    - services/mode_strategy.py
    - tests/test_mode_registry.py
  modified:
    - services/claude_service.py
    - services/deepgram_service.py
    - services/einwand_keyword_matcher.py
    - services/intent_event_writer.py
    - services/live_session.py
    - tests/test_medium_lane_intent_event_live.py

key-decisions:
  - "Decision 1: KEIN get_classification_prompt in der ABC — SYSTEM_PROMPT_BASE bleibt der EINE Prompt (MEDFIX nicht re-gebrochen)"
  - "Decision 2: cold_call FEST berater/local/advisor_paraphrase; EWB-Button bleibt die EINZIGE bewusste kunde-Ausnahme (ui_asserted)"
  - "Decision 3+4 + Refinement 2: per-SID mode einzige cold_call/meeting-Quelle, Default 'cold_call', Call-Start-only, kein Live-Toggle"
  - "FUND 1: kanonischer EWB-Marker = `*ewb button*`-Text-Suffix; data={'ewb_button':True} ist RAM-only (beim Persist verworfen)"
  - "FUND 3 (Andre-Entscheid A): triggering_text denormalisiert (nicht join), anonymisiert vor dem Schreiben"

patterns-established:
  - "Pattern 1: Neuer Modus = neue Strategy-Klasse via @register(...), Kern-Code (claude/deepgram/matcher) unberuehrt"
  - "Pattern 2: intent_event traegt mode + speaker_role + speaker_id + inference_basis + (anonymisierten) triggering_text bei JEDEM Event"

requirements-completed: [6]

# Metrics
duration: ~Tasks 1-5 (Bau) + Deploy + Test-Anruf (Gate)
completed: 2026-06-22
---

# Phase 08.23.2.TAXO1 Plan 07: Mode-Strategy-Registry Summary

**Modus als First-Class-Dimension via ModeStrategy-Registry (cold_call/meeting echte Klassen + 3 Steckplaetze); Sprecher-Bug behoben (cold_call=berater/advisor_paraphrase), FUND 2 Call-Start-Race eliminiert, §0.1 globales _session_modes geloescht, FUND 3 anonymisierter triggering_text denormalisiert, EWB-Button schreibt genau EINE markierte Kunde-Transcript-Zeile — am Server in BEIDEN Modi verifiziert.**

## Performance

- **Tasks:** 5 (Task 1-5, TDD wo markiert) + Gate-Fix (stale Mock)
- **Files modified:** 8 (2 created, 6 modified)
- **Completed:** 2026-06-22 (Deploy + Test-Anruf-Gate GRÜN)

## Accomplishments

- `services/mode_strategy.py`: `ModeStrategy(ABC)` mit GENAU zwei abstrakten Methoden (`setup_audio_routes` + `extract_intent`, KEIN `get_classification_prompt`), `MODE_REGISTRY`, Decorator `register()`, `ColdCallStrategy` + `MeetingStrategy` als echte Klassen + `meeting_ext`/`training_cold`/`training_meeting` als NotImplementedError-Steckplaetze. Kein Air-Gap-Bus, keine event_bus/sim.internal/audio.*-Topics (grep == 0).
- Sprecher-Bug behoben: die 3 LLM/KW-Bahnen (Medium, A/B-abstain, Keyword-Fast) routen ueber `MODE_REGISTRY[mode].extract_intent()`; cold_call liefert kuenftig `speaker_role='berater'`, `speaker_id='local'`, `inference_basis='advisor_paraphrase'` statt der frueheren Daten-Vergiftung mit `'kunde'`.
- §0.1-Konsolidierung: globales `_session_modes`-Dict GELOESCHT; alle 4 Reads per-SID (`_session_state[sid]['mode']`).
- FUND 2: per-SID mode wird VOR `_open_deepgram_connection` gesetzt — das erste Event eines meeting-Calls traegt `mode=meeting` (kein cold_call-Default-Race mehr).
- FUND 3: alle 4 Emit-Stellen tragen den anonymisierten `triggering_text`; im Writer als `payload_jsonb`-Key persistiert (keine Migration).
- Refinement 1 + FUND 1: EWB-Button schreibt ZUSAETZLICH zum intent_event eine markierte `conversation_log`-Zeile (speaker INT 1 = Kunde, text + `*ewb button*`-Suffix, anonymisiert) — genau EINE Kunde-Zeile im Cold-Call-Transcript.

## Struktur-Test GRÜN (7/7)

Andre-bestaetigt: `tests/test_mode_registry.py` GRÜN (7/7) im Deploy-Pytest-Gate auf Production (kein Restart bei rotem Gate). Deckt ab: Registry enthaelt cold_call/meeting als Instanzen; neuer Modus = neue Klasse ohne Kern-Aenderung (register/pop-cleanup); ColdCallStrategy → berater/local/advisor_paraphrase + Conf-Cap; MeetingStrategy speaker=1→kunde/speaker=0→berater/direct_customer_utterance; Steckplaetze NotImplementedError; ABC ohne get_classification_prompt (assert not hasattr).

## Test-Anruf — Server-Evidenz (inspect.sh, READ-ONLY, beide Modi)

Quelle: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample intent_event'`. Die Pre-Deploy-Rows (event_id 1-16, 2026-06-18) zeigen noch den alten Bug (`speaker_role: kunde` ueberall) — die Post-Deploy-Test-Anruf-Rows (event_id 17+, 2026-06-22) belegen alle Fixes:

### cold_call (session k27tysQW2oC0gmfTAAAB, 2026-06-22 09:05-09:08)

```
17 | cold_call | echter_einwand | {"source":"llm_inferred","speaker_id":"local","speaker_role":"berater","inference_basis":"advisor_paraphrase","triggering_text":"Verstehe, Ihnen ist das gerade zu teuer.","taxonomy_version":"v1"}  conf=0.9
18 | cold_call | echter_einwand | {"source":"llm_inferred","speaker_id":"local","speaker_role":"berater","inference_basis":"advisor_paraphrase","triggering_text":"Verstehe, [PERSON_A] ist das gerade zu teuer.","taxonomy_version":"v1"}  conf=0.85 (Cap)
19 | cold_call | echter_einwand | {"...","speaker_role":"berater","inference_basis":"advisor_paraphrase","triggering_text":"Okay, Sie sagen, Sie haben gerade keine Zeit."}
21 | cold_call | echter_einwand | {"...","speaker_role":"berater","inference_basis":"advisor_paraphrase","triggering_text":"[PERSON_B] so, Sie sind gerade mit Ihrem aktuellen Anbieter zufrieden."}
22 | cold_call | echter_einwand | {"source":"ui_asserted","speaker_id":"local","speaker_role":"kunde","inference_basis":"ui_button","triggering_text":"Hat Partner"}  conf=1.0
```

cold_call → `speaker_role=berater` / `inference_basis=advisor_paraphrase` (Sprecher-Bug behoben; die alten Rows 1-16 waren `kunde`); Conf-Cap 0.85 greift (event 18). Button → `kunde`/`ui_asserted`/`ui_button` ("Hat Partner"). `triggering_text` anonymisiert: `[PERSON_A]`/`[PERSON_B]`-Token in payload, keine Roh-PII.

### meeting (session OvAbsRKczRuw1vZwAAAD, 2026-06-22 09:20-09:21)

```
23 | meeting | echter_einwand | {"source":"llm_inferred","speaker_id":"local","speaker_role":"kunde","inference_basis":"direct_customer_utterance","triggering_text":"Verstehe, Ihnen ist es gerade zu teuer,"}  conf=0.9
24 | meeting | echter_einwand | {"...","speaker_role":"kunde","inference_basis":"direct_customer_utterance","triggering_text":"Sie sagen, Sie haben gerade keine Zeit."}
25 | meeting | echter_einwand | {"source":"ui_asserted","speaker_id":"local","speaker_role":"kunde","inference_basis":"ui_button","triggering_text":"Kein Bedarf"}  conf=1.0
```

meeting → `speaker_role=kunde` / `inference_basis=direct_customer_utterance` (MeetingStrategy trennt korrekt via Diarisierung). Button → `kunde`/`ui_button` ("Kein Bedarf").

### FUND 2 — meeting traegt mode=meeting ab dem ERSTEN Event

Das erste meeting-Event (event_id 23, der frueheste Timestamp der Session OvAbs..., 09:20:55) traegt bereits `mode=meeting` — KEIN cold_call-Default. Das Call-Start-Race-Fenster zwischen `_open_deepgram_connection` und `init_session_state` ist geschlossen (per-SID mode wird VOR `_open_deepgram_connection` gesetzt). Andre verbatim: *"FUND 2: meeting-Events tragen mode=meeting ab dem ersten Event (kein cold_call-Default)."*

## EWB-Button-Transcript-Round-Trip (Refinement 1 + FUND 1)

Andre-verifiziert (transcript_segments / Export beider Test-Anrufe): in BEIDEN Modi erzeugt der Button GENAU EINE markierte Kunde-Transcript-Zeile mit `*ewb button*`-Suffix ("Hat Partner" im cold_call, "Kein Bedarf" im meeting); gesprochene cold_call-Zeilen bleiben Berater.

**Persist-Pfad lokalisiert (Code-Beleg):**
- Schreiber: `services/deepgram_service.py:910-916` — `ls.conversation_log.append({'type':'transcript','speaker':1,'text':f"{_anon_typ} *ewb button*",'data':{'ewb_button':True}})` unter `ls.log_lock`. `speaker=1` (INT) = Kunde-Label, wie der gesprochene Pfad.
- Marker kanonisch: `services/deepgram_service.py:905,914` — der `*ewb button*`-Suffix lebt im `text`-Feld.
- Persist verwirft data: `_build_log_content` (live_session:994 `sp_map={0:'Berater',1:'Kunde'}`, :1019 rendert `entry['speaker']`+`entry['text']`; `data` wird nur fuer `type=='analyse'`-Eintraege gelesen). Der `data={'ewb_button':True}`-Flag ist RAM-only und erscheint NICHT im Export.
- Reader-Encoding verifiziert: `routes/app_routes.py:33` `_sp_map={0:'berater',1:'kunde'}`, :69 `None/unbekannt -> 'system'`. INT 1 → kunde bestaetigt — NICHT geraten.

Andre verbatim: *"Button beide Modi = kunde/ui_asserted + GENAU EINE markierte Kunde-Transcript-Zeile mit *ewb button*-Suffix (Hat Partner / Kein Bedarf), gesprochene Zeilen sonst berater."*

## §0.1-Beleg — grep _session_modes == 0 aktiver Code

Lokal UND auf dem deployed Server (`inspect.sh grep "_session_modes"`) — ALLE Treffer sind Kommentar-Referenzen ("ENTFERNT"/"geloescht"), KEINE aktive Lese-/Schreib-Stelle:

```
services/deepgram_service.py:18   # _session_modes geloescht TAXO1-07: ...
services/deepgram_service.py:438  # _session_modes[sid] = mode  ENTFERNT (TAXO1-07): ...
services/deepgram_service.py:448  # _session_modes.pop(sid, None)  ENTFERNT (TAXO1-07): ...
services/deepgram_service.py:842  # (TAXO1-07: globales _session_modes geloescht).
services/claude_service.py:970    # mode-Quelle per-SID (TAXO1-07: W7 erledigt — globales _session_modes ...
services/claude_service.py:1534   # mode-Quelle per-SID (TAXO1-07: globales _session_modes geloescht).
services/einwand_keyword_matcher.py:279  # mode-Quelle per-SID (TAXO1-07: globales _session_modes geloescht).
tests/test_medium_lane_intent_event_live.py:145  # TAXO1-07: globales _session_modes geloescht — ...
```

Andre verbatim: *"grep _session_modes == 0 aktiver Code."*

## FUND-3-Beleg — grep triggering_text = 1 Writer + 4 Aufrufer

```
services/intent_event_writer.py:50   triggering_text=None,          # Writer-Signatur (keyword-only)
services/intent_event_writer.py:101  'triggering_text': triggering_text,  # payload-Build (JSON-Key, keine Migration)
services/einwand_keyword_matcher.py:321  triggering_text=_kw_trig,   # Aufrufer 1: Fast-Lane (matched utterance)
services/claude_service.py:1040          triggering_text=_med_trig,  # Aufrufer 2: Medium-Lane (neuer_text)
services/claude_service.py:1575          triggering_text=_ab_trig,   # Aufrufer 3: A/B-abstain (neuer_text)
services/deepgram_service.py:882         triggering_text=_btn_trig,  # Aufrufer 4: EWB-Button (typ-Label)
```

1 Writer-Signatur + 1 payload-Build (intent_event_writer.py) + 4 Aufrufer-Emits (matcher, claude Medium, claude A/B, deepgram Button). Jeder Aufrufer fuehrt `anonymize_output(text, ls.get_anonymisierer(sid))` vor dem Emit; Sentinel/leer → `triggering_text=None`, Event wird TROTZDEM emittiert (kein Drop, keine Roh-PII).

## Gatekeeper-Achse unberuehrt + Namens-Caveat im Code

`handle_manual_mode_toggle` (`state['current_mode']` / `contact_category`, Kontakt-Achse cold_call-vs-gatekeeper) ist SEPARAT/ORTHOGONAL und wurde NICHT migriert/gelesen/geloescht. Namens-Caveat als Code-Kommentar an den Lese-Stellen (z.B. `deepgram_service.py:843-846`): 'cold_call' existiert in ZWEI Achsen (Kontakt vs Hoerbarkeit) — nicht vermischen.

## Consent-Rueckfall existiert heute NICHT (nicht erfunden)

Belegt: `live_session.py:471` ist ein Moment-Fenster-Reset-Kommentar in `get_or_open_moment` ("z.B. Meeting->Cold-Call bei Consent-Verweigerung" als Beispiel fuer den `moment_opened_mode != mode`-Fensterschluss), KEIN echter Laufzeit-mode-Flip-Write. Der Consent-Rueckfall wurde in diesem Plan NICHT erfunden. Falls/sobald er gebaut wird, MUSS er das konsolidierte `_session_state[sid]['mode']` schreiben.

## Kein Air-Gap-Bus

`grep event_bus|sim.internal|audio.*|air-gap` in `services/mode_strategy.py` == 0. Die 3 Steckplaetze sind dokumentierte Registry-Eintraege mit NotImplementedError, kein Training-Air-Gap-Bus.

## Task Commits

1. **Task 1: ModeStrategy registry + cold_call/meeting + 3 Steckplaetze** - `cb77bb7` (feat)
2. **Task 2: §0.1-Konsolidierung, _session_modes geloescht + FUND 2 Race-Fix** - `fad8313` (refactor)
3. **Task 3: Registry an 4 Emits + Sprecher-Bug-Fix + triggering_text + EWB-Transcript-Zeile** - `c9807c3` (feat)
4. **Task 4: tests/test_mode_registry.py Runtime-Struktur-Test** - `a449057` (test)
5. **Task 5: emit_intent_event nimmt triggering_text auf (FUND 3)** - `7fa3d03` (feat)

**Gate-Fix:** `19a3267` (test) — stale `_session_modes`-Mock in `tests/test_medium_lane_intent_event_live.py` entfernt (das globale Dict existiert nicht mehr; der per-SID-Read ist nun die Quelle). Cleanup-Anpassung, kein Verhaltens-Bruch.

## Files Created/Modified

- `services/mode_strategy.py` (NEU) - ModeStrategy(ABC) + MODE_REGISTRY + register() + ColdCall/Meeting + 3 Steckplaetze
- `tests/test_mode_registry.py` (NEU) - Runtime-Struktur-/Verhaltens-Test (7 Tests)
- `services/claude_service.py` - Medium + A/B-Lane ueber MODE_REGISTRY; per-SID mode-Read; triggering_text (anonymisiert)
- `services/deepgram_service.py` - _session_modes geloescht; FUND 2 mode VOR _open_deepgram_connection; EWB-Button-Transcript-Zeile + Suffix-Marker; Button-triggering_text
- `services/einwand_keyword_matcher.py` - Fast-Lane ueber MODE_REGISTRY; per-SID mode-Read; triggering_text
- `services/intent_event_writer.py` - emit_intent_event nimmt triggering_text kwarg auf; payload-Key (keine Migration)
- `services/live_session.py` - init_session_state Default 'meeting' → 'cold_call' (D4)
- `tests/test_medium_lane_intent_event_live.py` - stale _session_modes-Mock entfernt (Gate-Fix)

## Decisions Made

- Folgt dem Plan wie spezifiziert (alle 3 Cross-AI-Funde + 2 Refinements waren bereits in den Plan eingefaltet und wurden 1:1 gebaut).

## Deviations from Plan

### In-Scope Zusatz (im Plan-Scope, dokumentiert)

**1. [Rule 2 - D4-Konsistenz] _open_deepgram_connection mode= Default 'meeting' → 'cold_call'**
- **Found during:** Task 2 (§0.1-Konsolidierung)
- **Issue:** Der Default-Modus war an einer never-hit Fallback-Stelle noch 'meeting', inkonsistent mit dem einheitlichen D4-Default 'cold_call' (mode wird ohnehin explizit bei Call-Start gesetzt).
- **Fix:** Default auf 'cold_call' vereinheitlicht (D4-Konsistenz; Fallback wird nie erreicht, da Call-Start mode explizit setzt).
- **Verification:** Test-Anruf — erstes Event traegt korrekten Client-Modus (cold_call + meeting), kein falscher Default.

**2. [Gate-Fix] stale _session_modes-Mock entfernt**
- **Found during:** Deploy-Pytest-Gate (vor Test-Anruf)
- **Issue:** `tests/test_medium_lane_intent_event_live.py:145` mockte das nun geloeschte globale `_session_modes`-Dict → stale nach §0.1-Konsolidierung.
- **Fix:** Mock entfernt; der Test liest jetzt die per-SID-Quelle. Commit `19a3267`.
- **Verification:** Deploy-Gate GRÜN; Struktur-Test 7/7.

---

**Total deviations:** 2 (1 in-scope D4-Konsistenz, 1 Gate-Fix). Beide notwendig fuer Konsistenz/grüne Tests. Kein Scope-Creep.

## Issues Encountered

- Block-7 zeigte beim Test-Anruf NUR einen sauber abgefangenen Anthropic-Budget-Fehler (kein Crash). Andre verbatim: *"kein Block-7-Crash (nur Anthropic-Budget-Fehler, sauber abgefangen)."* Der Live-Loop blieb stabil (try-except an allen neuen Emit-/Append-Stellen).

## User Setup Required

None - keine externe Service-Konfiguration noetig.

## Next Phase Readiness

- intent_event-Schema-Fundament fuer TAXO2/3 steht: mode + speaker_role + speaker_id + inference_basis + anonymisierter triggering_text bei JEDEM Event. Beide Modi server-verifiziert.
- Konsolidierte per-SID-mode-Quelle (kein Split-Brain), Sprecher-Korrektheit (cold_call=berater, meeting getrennt), EWB-Marker round-trip-sicher.
- Offen/Foundation (kein Blocker): Consent-Rueckfall existiert heute nicht (falls spaeter gebaut → _session_state[sid]['mode'] schreiben).

## Self-Check

- mode_strategy.py: FOUND (services/mode_strategy.py, 175 Zeilen)
- test_mode_registry.py: FOUND (tests/test_mode_registry.py)
- Commit cb77bb7: FOUND
- Commit fad8313: FOUND
- Commit c9807c3: FOUND
- Commit a449057: FOUND
- Commit 7fa3d03: FOUND
- Commit 19a3267: FOUND (Gate-Fix)
- Server-Evidenz: intent_event sample BEIDE Modi (cold_call event 17-22, meeting event 23-25) — captured verbatim
- grep _session_modes == 0 aktiver Code (lokal + deployed): VERIFIED
- grep triggering_text = 1 Writer + 4 Aufrufer: VERIFIED

## Self-Check: PASSED

---
*Phase: 08.23.2.TAXO1*
*Plan: 07*
*Completed: 2026-06-22*
