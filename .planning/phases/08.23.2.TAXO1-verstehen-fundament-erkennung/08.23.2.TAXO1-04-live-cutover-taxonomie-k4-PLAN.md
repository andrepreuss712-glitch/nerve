---
phase: 08.23.2.TAXO1
plan: 04
type: execute
wave: 4
complexity: 🔴
depends_on: [01, 02, 03]
files_modified: [services/claude_service.py, services/einwand_keyword_matcher.py, services/intent_event_writer.py, services/live_session.py, services/deepgram_service.py, routes/app_routes.py, services/profile_schema.py, config.py, tests/test_intent_event_writer.py, tests/test_k4_threshold_funnel.py, tests/test_moment_window.py]
autonomous: false
requirements: [4, 5]
risiko: riskant
test_anruf: true
user_setup: []
must_haves:
  truths:
    - "Fast Lane (Keyword-Treffer + EWB-Button) UND Medium Lane (Haiku Intent+Phase) erzeugen echte intent_event-Zeilen als Single Source (Cutover)."
    - "Moment-Klammer (I-4-FOLD, ereignis-getriebenes Einwand-FENSTER — NICHT zeilen-basiert): EINE interaction_id (UUID) pro Einwand-Moment. Das Fenster OEFFNET beim ersten erkannten Kunden-Intent und SCHLIESST ereignis-getrieben — Meeting: Berater spricht wieder (Sprecher-Wechsel zum Berater); Cold Call: Berater geht zum naechsten Thema (phase_did_change) ODER Timeout-Backstop. Mehrere Einwaende am Stueck OHNE Berater-Antwort dazwischen = EIN Moment (eine interaction_id, mehrere Intents). Alle Emits desselben offenen Fensters (Fast-Keyword + Medium-Haiku + Button) teilen die ID; ein neues Fenster traegt eine neue ID. Die Klammer ist ein HINTERGRUND-Etikett — sie GATET die Live-Reaktion NICHT (NERVE feuert sofort beim ersten Einwand). Zustands-Fenster per-SID (`_session_state[sid]['state']`, `_session_state_lock`), KEINE starre Stoppuhr, KEIN line_id-Key."
    - "intent_type nutzt INTENT_TAXONOMY_V1 (Welle 1); jeder Insert traegt taxonomy_version non-null, mode, speaker_role, speaker_id, source (llm_inferred/ui_asserted)."
    - "Das alte ist_vorwand-Boolean + der freie typ-String + EinwandTypEnum sind auf intent_type migriert; alte parallele Pfade GELOESCHT (§0.1-Direktive)."
    - "K4: Cutover schreibt intent_event AUCH bei abstain/low-conf (abstained=true/confidence) statt zu droppen; Threshold ist ENV-gesteuert (kein Hardcode 0.80)."
    - "IL-2 (Live-Uebergabe-Vertrag fuer TAXO3): die Medium-Lane legt `primary_intent`(=intent_type) + `confidence` in `_session_state[sid]['state']` ab, BEVOR die Antwort getriggert wird — denn TAXO3 build_answer_context(*, primary_intent, mode, confidence) liest sie LIVE per-SID aus dem RAM (nicht aus der DB). intent_event-DB-Insert allein erfuellt den Vertrag NICHT."
    - "H-4 (abstain-Emit nur am echten Drop): die 6 [QA-INT]-Stellen sind klassifiziert — abstain-emit NUR an den echten low-conf-Drops (claude:1340 einwand_unknown / :1382 frage-no-faq, beide mit ECHTEM _conf), NICHT am Doppel-Feuer-Skip (:1278 kw_fired), NICHT am Mutex-Skip (:1284 slot1-busy), NICHT am no-sid-Skip (:1288). smalltalk_none/einwand_known (:1395) bleibt no-action (Single-Speaker-Filter). Sonst flutet abstained=True jeden keyword-vorgefeuerten Moment."
    - "M-2 (mode-Read cross-wave): die mode-Quelle des Medium-Emits (`deepgram_service._session_modes.get(sid)`) ist als Naht zu TAXO1-07 markiert — TAXO1-07 konsolidiert/loescht `_session_modes` und muss DIESEN Emit-mode-Read mit-migrieren (sonst leerer mode im intent_event nach W7)."
    - "Die K4-Funnel-Entscheidung (abstain vs normal) liegt in einer kleinen reinen Helper-Funktion (should_abstain(confidence, threshold) in config.py), damit sie unit-testbar ist; tests/test_k4_threshold_funnel.py beweist pure-logic (server-seitig): low-conf → abstained=True (nicht gedroppt), high-conf → normal, Threshold ENV-default 0.55 (Cross-AI Finding #4)."
    - "Live-Bahnen fuehren KEIN UPDATE auf bestehende intent_event-Zeilen aus (Bau-Regel 1)."
    - "custom_objection_* wird ohne Schema-/Code-Umbau akzeptiert (varchar64 + is_valid_intent_type-Praefix)."
  artifacts:
    - path: "services/intent_event_writer.py"
      provides: "EINE gekapselte emit_intent_event(...)-Funktion (insert-only, Pflichtfelder-Validierung, taxonomy_version+mode+speaker_role/id+interaction_id)"
      contains: "def emit_intent_event"
    - path: "services/live_session.py"
      provides: "get_or_open_moment(sid, *, mode, now) + close_moment(sid, *, reason) — ereignis-getriebenes Einwand-Fenster (LOCK-FREE Helper, Aufrufer haelt _session_state_lock; per-SID `_session_state[sid]['state']` mit interaction_id/moment_opened_mode/moment_last_activity_monotonic); additiv; roles_confirmed/smalltalk_none/sp_map unangetastet"
      contains: "def get_or_open_moment"
    - path: "services/claude_service.py"
      provides: "Medium-Lane-Emit (Haiku Intent+Phase → emit_intent_event); ist_vorwand/typ-Pfad geloescht, intent_type geschrieben"
      contains: "emit_intent_event"
    - path: "services/einwand_keyword_matcher.py"
      provides: "Fast-Lane-Emit (Keyword-Treffer → emit_intent_event source=llm_inferred)"
      contains: "emit_intent_event"
    - path: "config.py"
      provides: "ENV-gesteuerter Classifier-Threshold (war 0.80 hardcoded) + reine should_abstain()-Funnel-Helper (K4, unit-testbar)"
      contains: "CLASSIFIER_CONFIDENCE_THRESHOLD"
    - path: "tests/test_k4_threshold_funnel.py"
      provides: "Pure-logic K4-Regression (Finding #4): low-conf→abstain (nicht drop), high-conf→normal, ENV-default"
      contains: "def test_"
  key_links:
    - from: "matcher Keyword-Treffer + EWB-Button (app_routes)"
      to: "intent_event (source=ui_asserted/llm_inferred)"
      via: "emit_intent_event"
      pattern: "emit_intent_event"
    - from: "analyse_loop Haiku Intent+Phase"
      to: "intent_event (source=llm_inferred)"
      via: "emit_intent_event"
      pattern: "emit_intent_event"
    - from: "ist_vorwand/EinwandTypEnum (alte Welten, GELOESCHT)"
      to: "intent_type aus INTENT_TAXONOMY_V1"
      via: "Taxonomie-Migration (D-01/§0.1)"
      pattern: "intent_type"
    - from: "get_or_open_moment(sid, mode, now) (live_session, lock-free, per-SID _session_state[sid]['state'] unter _session_state_lock)"
      to: "intent_event.interaction_id (geteilt von Fast+Medium+Button desselben offenen Fensters)"
      via: "emit_intent_event(..., interaction_id=...)"
      pattern: "get_or_open_moment"
    - from: "Fenster-Schliesser (Meeting: Berater-Utterance deepgram; Cold Call: phase_did_change claude:1010; Modus-Downgrade; Timeout)"
      to: "close_moment(sid) → interaction_id=None (naechster Intent oeffnet neues Fenster)"
      via: "ereignis-getriebenes Schliessen"
      pattern: "close_moment"
    - from: "Medium-Lane Haiku Intent+Phase (analyse_loop) — IL-2 Live-Uebergabe"
      to: "_session_state[sid]['state']['primary_intent'] + ['confidence'] (RAM, VOR Antwort-Trigger)"
      via: "per-SID-Write vor emit, von TAXO3 build_answer_context gelesen"
      pattern: "_session_state\\[.*\\]\\['state'\\]\\['primary_intent'\\]"
---

> 🛑 **PRE-BUILD-BLOCKER I-4 (TAXO1↔TAXO2-Interlock 2026-06-11, Claudian + Gemini 3.1 — code-verifiziert): interaction_id-Moment-Klammer NICHT bauen wie unten geplant. Erst Anker + Granularität überarbeiten.**
> Die unten geplante Moment-Klammer keyt `get_or_create_interaction_id` auf den **MODUL-GLOBALEN** `state`-Dict. Das ist falsch + ein Launch-Daten-Vergifter:
> - **B-1 (Anruf-Vermischung, KRITISCH):** `line_id = next_line_id(sid)` ist ein PRO-SID-Zähler (1,2,3…) → zwei gleichzeitige Anrufe haben beide "Zeile 3". Der modul-globale `state['line_id']` (ein geteilter Slot, gesetzt in claude_service.py:979/1167) wird über ALLE SIDs geteilt → Anruf B liest Anruf A's interaction_id → die Momente zweier Verkäufer landen unter EINER id. Bei 50 EA-Usern (parallele Calls) deterministisch auslösbar. Gemini: "ruiniert sofort die TAXO2-Datenbasis". `state_lock` schützt nur Concurrent-Writes, NICHT die semantische Kollision.
> - **B-2 (Fix-Richtung, bestätigt):** Klammer MUSS **pro-SID** verankert werden — `_session_state[sid]['interaction_id'/'interaction_id_line']` unter `_session_state_lock`, gekeyt auf (sid, line_id). Welle 3 hat kw_fired_for_line schon Richtung pro-SID migriert (claude_service:1265 liest pro-SID-first); der Plan hängt die neue Klammer fälschlich an den Legacy-Global-Pfad. Die "bedingte Notausfahrt" unten (per-SID) ist der BEDINGUNGSLOSE Default. **+ Reentrancy-Audit für den NEUEN Lock (`_session_state_lock`) neu machen** (analyse_loop/matcher-Lock-Haltung prüfen; ggf. RLock).
> - **B-3 (Granularität, KRITISCH):** "ein Moment = eine line_id" ist zu fragil — Deepgram-Endpointing splittet einen Einwand mit Sprechpause in 2 line_ids → 2 ids für EINEN Moment → TAXO2-Konsolidierung fragmentiert. Gemini-Empfehlung: Moment ZEIT- oder ZUSTANDS-basiert definieren (z.B. gleitendes 5-8s-Fenster ab erstem Match, ODER "Einwand-Fenster" bis Berater signifikant selbst spricht), NICHT starr an line_id. **André-Entscheidung nötig** (Granularitäts-Ansatz).
> - **Folgefehler (Gemini, gegengeprüft):** (1) Button-vs-Transcript-Race — Button-Klick bevor die line_id finalisiert ist → klammert an die VORHERIGE Zeile; (2) Memory-Cleanup der per-SID-Klammer bei `/api/beenden` sicherstellen (pop_session_state).
> **Disposition (AKTUALISIERT 2026-06-12 nach Interlock + 2. Gemini-Review am echten Code + André-Entscheidung):**
> - **B-2 Anker → wandert in TAXO1-03 (Putzliste/Welle 3), NICHT in TAXO1-04.** Interlock-Befund: TAXO1-03 macht ohnehin die ganze Live-Buchführung pro-SID — der interaction_id-Anker (+ der `line_id`-Merker, der in der 12er-Liste fehlt) kommt dort mit rein. Damit löst sich der Anker-Teil des Blockers an der Wurzel; TAXO1-04 baut den Anker NICHT selbst, sondern nutzt den per-SID-Zustand aus TAXO1-03. **TAXO1-03-Scope-Ergänzung Pflicht: `line_id` per-SID + ALLE log_api_cost-Stellen (Cost-Attrib-Vollständigkeit, Roadmap-Sync c54ca26).**
> - **B-3 Granularität → ENTSCHIEDEN (André) + GEFOLDET IN DIE TASKS (2026-06-13):** Zustands-Fenster (Einwand-Fenster), **ereignis-getrieben** geschlossen (Meeting = Sprecher-Wechsel zum Berater; Cold Call = Berater geht zum nächsten Thema [phase_did_change] ODER Timeout-Backstop; „neues/anderes Einwand-Signal" = natürliche Folge des bereits geschlossenen Fensters, NICHT ein eigener Mid-Window-Close — siehe Reconciliation Task 1b), Timeout NUR Notbremse-Hausmeister. **KEINE starre Stoppuhr, KEIN line_id-Key.** Die Moment-Klammer ist ein Hintergrund-Etikett — sie GATET die Live-Reaktion NICHT (NERVE feuert sofort). Detail: Soll-Verhalten §5/§6. **✅ Die Tasks unten (1b/2/3 + neuer Fenster-Schliesser) bauen jetzt DIESES ereignis-getriebene Fenster — NICHT mehr die line_id-gekoppelte Version. Der frühere „Launch-Daten-Vergifter"-Stand (if interaction_id_line==line_id) ist ersetzt.**
> - **2. Gemini-Review (am echten Code, 2026-06-12) — zusätzliche Pflicht-Punkte für die Moment-LOGIK in TAXO1-04:** (a) **Reentrancy/Deadlock akut** — `_session_state_lock` ist `threading.Lock` (kein RLock), analyse_loop hält ihn schon (claude_service.py:990/1007/1058/1259) → Helper MUSS lock-free sein (Aufrufer hält Lock + schreibt direkt, wie matcher:252) ODER RLock. (b) **Cold-Call-Schließbedingung** braucht Timeout-Backstop gegen Endlos-Momente. (c) **Button-vor-line_id-Race** — Button hängt an den AKTUELL offenen Moment, nicht an die Vorzeile. (d) **Modus-Downgrade** (Meeting→Cold-Call bei Consent-Verweigerung) → offenen Moment zwingend schließen/resetten.
> - **Unberührt:** emit_intent_event, Taxonomie-Cutover, K4. Detail: 08.23.2.TAXO2-REVIEWS.md §5 (I-4) + die zwei Gemini-Reviews 2026-06-12.

> RISIKO-BANNER: **riskant.** Lenkt die Live-Erkennung um (Fast+Medium → intent_event), LOESCHT alte Pfade (ist_vorwand/typ/EinwandTypEnum), tunt K4-Threshold. → **Test-Anruf PFLICHT (D-03, cold_call + meeting, Daten-Fluss).** Setzt auf den in Welle 3 konsolidierten per-SID-Pfaden auf (Reihenfolge-Zwang RESEARCH §10).

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Geplant, nicht sofort ausgefuehrt. Deploy + Test-Anruf erst nach Cross-AI-Clearance + André-Ineinandergreifen-Check (gemeinsamer Klebstoff intent_event mit TAXO2/3).

> 🔎 **VOR-EXECUTE-RE-GREP (RESEARCH Valid until):** `grep -rn "ist_vorwand\|einwand_typ\|EinwandTypEnum\|generate_qa_response\|CLASSIFIER_CONFIDENCE_THRESHOLD" services/ routes/ config.py` erneut laufen; Zeilen unten (RESEARCH §3/§4 Stand 2026-06-10) abgleichen. **PLUS Moment-Fenster (I-4-FOLD, Punkt 22):** `grep -n "_session_state_lock\|_session_state\[sid\]\|'current_phase'\|phase_did_change\|_session_modes\[" services/claude_service.py services/live_session.py services/deepgram_service.py` — bestaetigen: (1) `_session_state[sid]['state']` ist der lebende per-SID-Store (TAXO1-03/W3), in den current_phase geschrieben wird (claude:991/1007) → DA liegt das Fenster-State; (2) `phase_did_change` existiert als Cold-Call-Topic-Shift-Signal (claude:1010); (3) deepgram on_message hat den finalisierten Per-Utterance-Sprecher (`speaker`/`sp_label=='Berater'`, :63/:86) für den Meeting-Schliesser + `_session_modes[sid]=mode` (:427) für den Downgrade-Schliesser. Falls W3 den per-SID-Store anders benannt hat → Pfade angleichen, Fenster bleibt per-SID (NIE module-global, B-1).

<objective>
Den Live-Erkennungs-Cutover vollziehen (REQ 4): Fast Lane (Keyword-Matcher + EWB-Button) UND Medium Lane (analyse_loop Haiku Intent+Phase) schreiben ihren Output als echte `intent_event`-Zeilen — die Single Source. Die geteilte Intent-Taxonomie §1 wird das `intent_type`-Vokabular (REQ 5): das alte `ist_vorwand`-Boolean + der freie `typ`-String + `EinwandTypEnum` werden darauf migriert, die alten parallelen Pfade geloescht. K4 (qa_slot1 unterfeuert) wird aufgeloest, NICHT ueberbaut: intent_event wird auch bei abstain/low-conf geschrieben (Funnel sichtbar), Threshold ENV-gesteuert.

Purpose: Ohne Cutover bleibt objection_events die de-facto-Quelle und intent_event leer. Mit der EINEN Schreibstelle (emit_intent_event) + der EINEN Taxonomie ist der gemeinsame Vertrag mit TAXO2/3 live befuellt. K4-Diagnose (RESEARCH §3): kein Pipeline-Bug, sondern Threshold-Unterdrueckung + Single-Speaker-Funnel — NICHT den korrekten Funnel brechen.

Output: services/intent_event_writer.py (EINE gekapselte Emit-Funktion), Emit-Calls in matcher/analyse_loop/EWB-Button, Taxonomie-Migration in claude_service/profile_schema, ENV-Threshold in config.py. Deploy + Test-Anruf — GATED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-RESEARCH.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-01-SUMMARY.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-03-SUMMARY.md
@services/claude_service.py
@services/einwand_keyword_matcher.py
@routes/app_routes.py
@services/profile_schema.py

<interfaces>
<!-- Welle 1: IntentEvent-Model + intent_taxonomy.py (INTENT_TAXONOMY_V1, TAXONOMY_VERSION='v1', is_valid_intent_type). -->
<!-- Welle 2: slow_lane.put() existiert (Slow Lane konsumiert; in TAXO1 kein Score). emit_intent_event darf optional event_ref an slow_lane.put geben (read-only Kopie). -->
<!-- Welle 3: per-SID-Quelle konsolidiert (user_id/current_phase/org_id per-SID) — emit liest sid-Kontext sauber. -->
<!-- Write-Pfad heute (RESEARCH §3, VOR Bau re-greppen): -->
<!--   Fast Keyword: matcher (lokal, <800ms, kein LLM). Fast Button: app_routes.py:323 ls.state['ewb_clicks'] → objection_events bei Call-Ende :384. -->
<!--   Medium Intent: analyse_loop claude:966 'einwand_typ'=ergebnis.get('typ') / :968 'ist_vorwand'=ergebnis.get('ist_vorwand') (freier String + Boolean — D2). -->
<!--   Medium Phase: detect_phase per-SID current_phase (int nach Welle 3). -->
<!--   qa-dispatch: generate_qa_response claude:~1245 (Slot B, KI-Antwort). -->
<!-- D2 (RESEARCH §4): profile_schema.py:45 EinwandTypEnum=Literal['echt','vorschiebe','unbekannt'] / :103 einwand_typ. claude:66/76 Haiku-Prompt fragt ist_vorwand+typ. -->
<!-- K4 (RESEARCH §3): CLASSIFIER_CONFIDENCE_THRESHOLD=0.80 (config.py:42) unterdrueckt 13 legitime frage bei conf 0.45-0.75; Haiku clustert ~0.65. [QA-INT]-Marker claude:1279/1284/1310/1319/1335/1398. Fix: intent_event auch bei abstain/low-conf + ENV-Threshold ~0.55. NICHT Single-Speaker-Funnel brechen (roles_confirmed/QA-Gate korrekt). -->
<!-- H-4 (TAXO-INTERLOCK-FINDINGS — die 6 [QA-INT]-Stellen klassifiziert, _qa_pipeline_dispatch claude_service.py code-verifiziert 2026-06-13): -->
<!--   :1278 `if _kw_fired_for == line_id: return` = D-02 DOPPEL-FEUER-SKIP (Keyword-Matcher hat fuer DIESE Zeile schon gefeuert) — KEIN low-conf-Drop; der Moment hat bereits ein Fast-Lane-Keyword-intent_event mit GETEILTER interaction_id → HIER abstain-emiten flutet (kein _conf berechnet, return VOR classify:1307 → confidence waere None → abstained=True fuer JEDEN vorgefeuerten Moment). NICHT emiten. -->
<!--   :1284 `if monotonic() < slot1_busy_until: return` = Slot-1-MUTEX-SKIP — kein Drop, Moment schon gecued. NICHT emiten. -->
<!--   :1288 `if not _active_sid: return` = keine Session. NICHT emiten. -->
<!--   :1307-1310 classify_utterance → ECHTER _conf (_qa_result['confidence']). -->
<!--   :1340 `if _conf < THRESHOLD: _emit_soft_hint('low_confidence')` (kat=einwand_unknown) = ECHTER LOW-CONF-DROP → HIER emit_intent_event(abstained=True, confidence=_conf, intent_type=...) VOR/mit dem soft_hint. -->
<!--   :1382 `if _conf < THRESHOLD: _emit_soft_hint('no_faq_low_conf')` (kat=frage, kein FAQ) = ECHTER LOW-CONF-DROP → HIER abstain-emit. -->
<!--   else-Zweige (conf>=THRESHOLD, _emit_qa_slot1) = NORMAL-Emit (abstained=False). -->
<!--   :1395 smalltalk_none/einwand_known = no-action (Single-Speaker-Monolog-Filter, korrekt) — NICHT emiten. -->
<!-- IL-2 (TAXO3↔TAXO1 BLOCKER, Gemini+Claudian verifiziert): TAXO3 build_answer_context(*, user_id, sid, primary_intent, mode, confidence, ...) (TAXO3-03-PLAN) liest primary_intent+confidence+mode LIVE per-SID aus _session_state[sid] (RAM), NICHT aus intent_event-DB. mode (W7) + interaction_id (W3/I-4) + call_id liegen schon per-SID; primary_intent+confidence NICHT. → Medium-Lane MUSS `_session_state[sid]['state']['primary_intent']=intent_type` + `['confidence']=confidence` setzen BEVOR die Antwort getriggert wird (Live-Uebergabe-Vertrag). Keys frisch in init_session_state['state'] seeden (primary_intent=None, confidence=None) ist additiv-sauber (TAXO3 `.get()` toleriert None). **ZWEI-SEITIGER VERTRAG (Pflicht-Abgleich):** der EXAKTE Key-Pfad `_session_state[sid]['state']['primary_intent'/'confidence']` IST der Vertrag — TAXO3-03's IL-2-VOR-EXECUTE-RE-GREP muss build_answer_context an GENAU diesen Pfad haengen (mode liegt top-level `[sid]['mode']`, primary_intent/confidence unter `['state']`, wie current_phase). Im SUMMARY als `## Vertrag fuer TAXO3 — primary_intent/confidence-Quelle` mit dem exakten Pfad festhalten, damit TAXO3-03 nicht raet. -->
<!-- M-2 (TAXO1-04↔TAXO1-07 Naht): der Medium-Emit liest mode via deepgram_service._session_modes.get(sid) (Task 2c). TAXO1-07 (mode-strategy-registry) konsolidiert/loescht _session_modes → TAXO1-07 muss DIESEN mode-Read mit-migrieren. Marker im Code + Cross-Wave-Note (depends-Richtung: W7 nach W4, aber W7-Plan referenziert diese Stelle). -->
<!-- intent_event.intent_type = varchar(64) → custom_objection_* ohne DDL. objection_events.einwand_typ=varchar(100) bleibt (Dual-Write Welle 5). -->
<!-- Geruest §3 Pflicht je Insert: taxonomy_version(non-null), mode, speaker_role(berater/kunde/gatekeeper/system), speaker_id(UUID/local; cold_call default berater/local), source(llm_inferred/ui_asserted), inference_basis. -->
<!-- Moment-Klammer (I-4-FOLD 2026-06-13 — EREIGNIS-GETRIEBENES Einwand-Fenster, NICHT line_id-gekoppelt): interaction_id (UUID-Spalte aus Welle 1). EIN Moment = ein Einwand-FENSTER, offen bis der Berater antwortet. NICHT pro Transkript-Zeile (B-3: Deepgram-Endpointing splittet einen Einwand in 2 line_ids → zwei ids → TAXO2-Fragmentierung; deshalb KEIN line_id-Key). -->
<!-- FENSTER-STATE (per-SID, B-2-Fix): liegt in `_session_state[sid]['state']` unter `_session_state_lock` (DERSELBE Store/Lock wie current_phase :991/:1007, die TAXO1-03 per-SID gelegt hat). Felder (additiv in init_session_state['state'] seeden): `interaction_id` (offene UUID oder None), `moment_opened_mode` (mode bei OEFFNUNG, fuer Downgrade-Erkennung), `moment_last_activity_monotonic` (fuer Timeout-Backstop). KEIN module-globaler state-Dict, KEIN line_id-Key. -->
<!-- OEFFNEN: get_or_open_moment(sid, *, mode, now) — beim ersten erkannten Kunden-Intent (kein offenes Fenster) mintet uuid4, setzt interaction_id+moment_opened_mode+last_activity. Offenes Fenster → refresh last_activity + dieselbe id zurueck (mehrere Einwaende am Stueck ohne Berater-Antwort = EIN Moment, mehrere Intents). -->
<!-- SCHLIESSEN: close_moment(sid, *, reason) setzt interaction_id=None → naechster Intent oeffnet ein neues Fenster. Ausgeloest ereignis-getrieben: (1) Meeting: Berater spricht (deepgram on_message, finalisierte Utterance speaker==0/'Berater', :63/:86 — additiver Hook, Sprecher-Detection UNVERAENDERT); (2) Cold Call: Berater geht zum naechsten Thema = `phase_did_change` (claude_service.py:1010, schon unter _session_state_lock); (3) Modus-Downgrade (moment_opened_mode != aktueller mode → im Helper + am _session_modes-Switch deepgram:427/892); (4) Timeout-Backstop MOMENT_WINDOW_TIMEOUT_S (Notbremse, im Helper). -->
<!-- LOCK/REENTRANCY (Gemini-Punkt a, EINDEUTIG): get_or_open_moment + close_moment sind LOCK-FREE und operieren direkt auf dem per-SID state-Dict; der AUFRUFER haelt `_session_state_lock` (Muster matcher:252 „caller holds lock, writes directly"). KEIN RLock noetig. KEIN Verschachteln mit `state_lock` (module-global) — die Lanes nehmen state_lock und _session_state_lock NIE gleichzeitig (keine Lock-Ordering-Deadlock). analyse_loop haelt _session_state_lock ohnehin in diskreten Bloecken (:990/:1007/:1058) → Helper-Call dort lock-frei innerhalb des bestehenden Blocks. -->
<!-- event_id pro Emit (mehrere/Moment), interaction_id pro Moment/Fenster, call_id pro Call. WELLE-3-PRECONDITION (depends_on 03): der per-SID-Anker (_session_state[sid] + _session_state_lock) kommt aus TAXO1-03; TAXO1-04 NUTZT ihn, baut ihn NICHT. VOR-EXECUTE-RE-GREP bestaetigt, dass _session_state[sid]['state'] der lebende per-SID-Store ist (Stand: ja, :991/:1007). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/intent_event_writer.py — EINE gekapselte emit_intent_event() (insert-only, Pflichtfelder, Validierung)</name>
  <read_first>
    - database/models.py IntentEvent (Welle 1: Spalten + payload_jsonb)
    - services/intent_taxonomy.py (is_valid_intent_type, TAXONOMY_VERSION)
    - database/db.py (get_session-Muster)
    - Geruest §3 (Pflichtfelder je Insert — interfaces-Block)
    - services/slow_lane.py (slow_lane.put — read-only Kopie an Slow Lane)
  </read_first>
  <behavior>
    - Test 1: emit_intent_event(session_id, mode, intent_type='echter_einwand', phase=2, source='llm_inferred', confidence=0.7, speaker_role='kunde', speaker_id='uuid', user_id, org_id) → INSERT eine intent_event-Zeile; payload_jsonb enthaelt taxonomy_version='v1', source, speaker_role, speaker_id, abstained=False; handling_score_numeric bleibt NULL.
    - Test 2: intent_type='custom_objection_preis' → akzeptiert (Praefix), Insert erfolgt.
    - Test 3: intent_type='quatsch_unbekannt' (kein Kern, kein Praefix) → ValueError ODER abstained=True-Fallback (dokumentierte Entscheidung), KEIN ungueltiger intent_type in DB.
    - Test 4: abstain/low-conf (confidence < threshold) → Zeile MIT abstained=True + confidence geschrieben (K4: nicht droppen).
    - Test 5: zwei Aufrufe → zwei INSERTs, KEIN UPDATE einer bestehenden Zeile (Bau-Regel 1).
    - Test 6: interaction_id durchgereicht → zwei emit_intent_event-Aufrufe mit DERSELBEN interaction_id (gleicher Moment) schreiben zwei Zeilen mit identischer interaction_id-Spalte; ohne interaction_id (None) bleibt die Spalte NULL (Klammer ist optional/nullable, kein Crash).
  </behavior>
  <action>
    NEUE Datei `services/intent_event_writer.py`. EINE Funktion `def emit_intent_event(*, session_id, mode, intent_type, phase=None, source, confidence=None, speaker_role, speaker_id, user_id=None, org_id=None, call_id=None, interaction_id=None, inference_basis=None, abstained=False, is_simulation=False, origin_type='human_live', extra_payload=None) -> int:` (gibt event_id zurueck). `interaction_id` = Moment-Fenster-id (UUID, optional/nullable) — vom Aufrufer via get_or_open_moment bestimmt (Task 1b, I-4-FOLD) und durchgereicht; der Writer setzt nur die Spalte, mintet selbst KEINE id (sonst teilt sie sich nicht ueber Lanes/das offene Fenster).
    - Validierung: `is_valid_intent_type(intent_type)` — sonst ValueError (oder abstained-Fallback; dokumentiere die Wahl, Test 3). Pflichtfelder pruefen: session_id, mode, source, speaker_role nicht leer (Geruest §3 "bei jedem Event").
    - payload_jsonb bauen: `{'source': source, 'inference_basis': inference_basis, 'taxonomy_version': TAXONOMY_VERSION, 'abstained': abstained, 'speaker_role': speaker_role, 'speaker_id': speaker_id, 'is_simulation': is_simulation, 'origin_type': origin_type, ...extra_payload}` — taxonomy_version Pflicht non-null.
    - INSERT (insert-only, get_session try-finally): neue IntentEvent-Zeile; handling_score_numeric NICHT setzen (bleibt NULL — REQ 2). confidence in Spalte, phase als int (oder None), `interaction_id` in die gleichnamige UUID-Spalte (None → NULL erlaubt).
    - Optional: `slow_lane.put({'event_id': eid})` (read-only Referenz; Slow Lane reichert spaeter an, TAXO2 — in TAXO1 nur konsumieren).
    - KEIN UPDATE-Pfad (Bau-Regel 1). KEIN Scoring.
    Marker-Kommentar: `# SINGLE SOURCE: alle Erkennungs-Bahnen emittieren ueber DIESE Funktion (Cutover TAXO1).`
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. Unit-Test server-seitig via Checkpoint: `ssh ... pytest tests/test_intent_event_writer.py -x`. (Test-Datei in diesem Task mit-erstellen.) AST-Parse lokal erlaubt.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def emit_intent_event" services/intent_event_writer.py` 1 Treffer.
    - `grep "TAXONOMY_VERSION" services/intent_event_writer.py` 1 Treffer (taxonomy_version Pflicht im payload).
    - `grep -iE "UPDATE|\.query.*\.update\(|setattr" services/intent_event_writer.py` == 0 (insert-only, Bau-Regel 1).
    - `grep -iE "handling_score|score.*=|rubric" services/intent_event_writer.py` zeigt KEINE Score-Setzung (nur ggf. NULL).
    - `grep "is_valid_intent_type" services/intent_event_writer.py` 1 Treffer (Validierung).
    - `grep "interaction_id" services/intent_event_writer.py` zeigt den Param + die Spalten-Zuweisung; der Writer mintet KEINE uuid selbst (`grep -E "uuid4|uuid\\.uuid" services/intent_event_writer.py` == 0 — Klammer kommt vom Aufrufer, Task 1b).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>EINE gekapselte insert-only Emit-Funktion mit Pflichtfeldern + Taxonomie-Validierung + durchgereichter interaction_id, ohne Scoring, ohne UPDATE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: services/live_session.py — ereignis-getriebenes Einwand-Fenster: get_or_open_moment(sid, mode, now) + close_moment(sid, reason) (per-SID, LOCK-FREE Helper) [I-4-FOLD]</name>
  <read_first>
    - ⚠️ RE-GREP zuerst (Moment-Fenster-grep aus dem VOR-EXECUTE-RE-GREP-Banner)
    - services/live_session.py:299-371 init_session_state — wo `_session_state[sid]['state']` aufgebaut wird (current_phase etc.); HIER die 3 Fenster-Keys seeden (interaction_id=None, moment_opened_mode=None, moment_last_activity_monotonic=0.0). `_session_state_lock` (:209) ist der per-SID-Lock.
    - services/claude_service.py:990-1018 VERBATIM — `with ls._session_state_lock:` um den per-SID current_phase-Write + `phase_did_change` (:1010) = das Cold-Call-Topic-Shift-Signal (HIER close_moment bei phase_did_change+cold_call).
    - services/claude_service.py:854/862/885/1058 — analyse_loop haelt `_session_state_lock` in DISKRETEN Bloecken (kein durchgehender Halt) → Helper lock-frei im jeweiligen Block aufrufbar (Gemini-Punkt a).
    - services/einwand_keyword_matcher.py:249-253 VERBATIM (`with _ls.state_lock: ... kw_fired_for_line`) — matcher haelt `state_lock` (module-global), NICHT `_session_state_lock`; Muster „caller holds lock, writes directly" (matcher:252).
    - services/deepgram_service.py:56-94 (finalisierte Utterance, `speaker`/`sp_label=='Berater'`, meeting) + :427 (`_session_modes[sid]=mode`) — für Meeting- + Downgrade-Schliesser (Task 3c). Sprecher-Detection (stabilize_speaker/sp_map/roles_confirmed) NUR LESEN, NICHT aendern.
    - config.py — neuer ENV `MOMENT_WINDOW_TIMEOUT_S` (Timeout-Backstop, Punkt b).
  </read_first>
  <action>
    Punkt-14. ADDITIV — KEINE Aenderung an roles_confirmed / smalltalk_none / sp_map / stabilize_speaker / QA-Gate (T-TAXO1-12).
    **Ziel (I-4-FOLD, André §5/§6): EIN Moment = ein Einwand-FENSTER, offen bis der Berater antwortet — NICHT pro Transkript-Zeile. KEIN line_id-Key.**

    **(1) Fenster-State per-SID seeden (init_session_state, live_session.py:299-371):** in `_session_state[sid]['state']` drei additive Keys: `interaction_id=None` (offene UUID oder None), `moment_opened_mode=None` (mode bei Oeffnung), `moment_last_activity_monotonic=0.0`. (Derselbe per-SID-Store wie current_phase — B-2-Fix; KEIN module-globaler state, KEIN line_id-Key.)

    **(2) `def get_or_open_moment(sid, *, mode, now) -> str` — LOCK-FREE (Aufrufer haelt `_session_state_lock`, Gemini-Punkt a):** operiert direkt auf `_session_state[sid]['state']` (kein eigenes Lock-Acquire — der Aufrufer haelt den Lock schon, Muster matcher:252). Logik:
       - `st = _session_state[sid]['state']`
       - **(d) Modus-Downgrade-Reset:** wenn `st['interaction_id'] is not None and st['moment_opened_mode'] != mode` → offenes Fenster gehoert zum alten Modus → schliessen (interaction_id=None) BEVOR weiter (Consent-Verweigerung Meeting→Cold-Call zwingt frisches Fenster).
       - **(b) Timeout-Backstop:** wenn `st['interaction_id'] is not None and now - st['moment_last_activity_monotonic'] > MOMENT_WINDOW_TIMEOUT_S` → stale → schliessen (Notbremse-Hausmeister; Wert nennen: ENV `MOMENT_WINDOW_TIMEOUT_S`, Default **45.0** s — lang genug fuer gestapelte Einwaende, kurz genug dass kein Geister-Moment ueber einen Themen-Wechsel hinaus lebt).
       - **OEFFNEN:** wenn `st['interaction_id'] is None` → `iid=str(uuid.uuid4())`, `st['interaction_id']=iid`, `st['moment_opened_mode']=mode`, `st['moment_last_activity_monotonic']=now` → return iid (Fenster oeffnet beim ersten erkannten Kunden-Intent).
       - **FORTSETZEN:** sonst → `st['moment_last_activity_monotonic']=now` (refresh) → return `st['interaction_id']` (mehrere Einwaende am Stueck OHNE Berater-Antwort = EIN Moment, mehrere Intents).
    **(3) `def close_moment(sid, *, reason) -> None` — LOCK-FREE (Aufrufer haelt `_session_state_lock`):** `st['interaction_id']=None` (moment_opened_mode/last_activity koennen bleiben/genullt werden). Idempotent (schon None → no-op). Marker-Log `[MOMENT] close sid=… reason=…`.
    **(4) Reconciliation „mehrere Einwaende → EIN Moment" vs. Cold-Call „neues/anderes Einwand-Signal" (André §5/§6, explizit dokumentiert):** Wir schliessen NICHT auf einen blossen anders-typed Einwand mitten im Fenster — das wuerde gestapelte Einwaende fragmentieren (verletzt „mehrere Einwaende am Stueck = EIN Moment" + die spaetere Multi-Einwand-Antwort). Das Cold-Call-Fenster schliesst auf **Topic-Shift (phase_did_change)** ODER **Timeout** ODER **Modus-Downgrade**; ein „neues/anderes Einwand-Signal" oeffnet dann NATUERLICH ein neues Fenster, weil das vorige bereits geschlossen ist. → Diese Interpretation im SUMMARY + an Cross-AI/André zur Bestaetigung markieren (einzige Stelle, an der §5-Wortlaut eine Lesart braucht).
    - **Reentrancy/Lock (Gemini-Punkt a, EINDEUTIG geloest):** beide Helfer NEHMEN KEINEN Lock — der Aufrufer haelt `_session_state_lock`. KEIN RLock. Aufrufer nehmen `state_lock` (module-global) und `_session_state_lock` NIE gleichzeitig (kein Lock-Ordering-Deadlock) — siehe Task 2/3 Aufruf-Disziplin.
    - `import uuid`/`time` (Bestand pruefen). KEINE DB, KEIN LLM, KEIN Scoring.
    Marker-Kommentar: `# MOMENT-FENSTER (I-4-FOLD): ereignis-getriebenes Einwand-Fenster pro-SID; oeffnet bei erstem Kunden-Intent, schliesst bei Berater-Antwort/Topic-Shift/Timeout/Downgrade. Hintergrund-Etikett, GATET NICHT.`

    Test `tests/test_moment_window.py` (pure-logic, Function-Call-Return — KEIN Live-Call/DB/LLM; baut ein Mini-`_session_state[sid]['state']`-Dict + ruft die Helfer direkt unter manuell gehaltenem Lock-Kontext):
    - test_open_mints_id: leeres Fenster → get_or_open_moment gibt eine UUID, state['interaction_id'] gesetzt.
    - test_consecutive_same_window: zwei Aufrufe ohne Schliess-Event (now leicht erhoeht, gleicher mode) → IDENTISCHE id (mehrere Einwaende = EIN Moment).
    - test_close_then_open_new_id: get_or_open → close_moment → get_or_open → VERSCHIEDENE id (neues Fenster).
    - test_timeout_backstop_closes: zweiter Aufruf mit now > last_activity+MOMENT_WINDOW_TIMEOUT_S → NEUE id (stale-Janitor, Punkt b).
    - test_mode_downgrade_resets: Fenster in mode='meeting' geoeffnet, dann get_or_open mit mode='cold_call' → NEUE id + moment_opened_mode=='cold_call' (Punkt d).
    - test_close_idempotent: close_moment zweimal → kein Crash, interaction_id bleibt None.
  </action>
  <verify>
    <automated>ssh ... '/opt/nerve/venv/bin/pytest tests/test_moment_window.py -x'</automated> (server-seitig; AST-Parse lokal erlaubt)
  </verify>
  <acceptance_criteria>
    - `grep -n "def get_or_open_moment\|def close_moment" services/live_session.py` 2 Treffer.
    - `grep -nE "uuid4|uuid\\.uuid" services/live_session.py` zeigt das Minten im Oeffnen-Pfad.
    - **LOCK-FREE belegt:** die Helfer enthalten KEIN `with _session_state_lock`/`with state_lock` in sich (`grep -nA15 "def get_or_open_moment" services/live_session.py` zeigt direkten state-Dict-Zugriff, kein Lock-Acquire) — Aufrufer haelt den Lock (Punkt a).
    - `grep -n "'interaction_id'\|moment_opened_mode\|moment_last_activity" services/live_session.py` zeigt die 3 Fenster-Keys + ihre init-Seeds (=None/0.0).
    - **KEIN line_id-Key:** `grep -n "interaction_id_line" services/live_session.py` == 0 (die alte zeilen-gekoppelte Logik ist WEG).
    - `grep -n "MOMENT_WINDOW_TIMEOUT_S" config.py services/live_session.py` zeigt den Timeout-Backstop (Punkt b, Default 45.0, ENV).
    - test_open_mints_id + test_consecutive_same_window + test_close_then_open_new_id + test_timeout_backstop_closes + test_mode_downgrade_resets + test_close_idempotent gruen.
    - QA-Gate unangetastet: `git diff services/live_session.py` zeigt KEINE Aenderung an roles_confirmed/smalltalk_none/sp_map/stabilize_speaker (nur additive Helfer + 3 State-Keys + init-Seeds).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Ereignis-getriebenes Einwand-Fenster (get_or_open_moment/close_moment, per-SID, lock-frei): Fenster oeffnet bei erstem Kunden-Intent, schliesst bei Berater-Antwort/Topic-Shift/Timeout/Downgrade; mehrere Einwaende am Stueck = EIN Moment. KEIN line_id-Key. Single-Speaker-Funnel + Sprecher-Detection unberuehrt. 6 pure-logic Tests gruen.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Medium-Lane-Cutover (analyse_loop) — Haiku Intent+Phase → emit_intent_event; ist_vorwand/typ-Pfad + EinwandTypEnum loeschen, intent_type schreiben</name>
  <read_first>
    - ⚠️ RE-GREP zuerst
    - services/claude_service.py:66/76 (Haiku-Prompt ist_vorwand+typ) / :966/:968 (ergebnis-dict ist_vorwand/einwand_typ) / Umfeld analyse_loop — 30 Zeilen vor/nach VERBATIM
    - services/profile_schema.py:45/103/431/437 (EinwandTypEnum + Leser)
    - services/intent_event_writer.py (emit_intent_event-Signatur, Task 1)
    - **IL-2:** .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-03-build-answer-context-ton-PLAN.md (build_answer_context-Signatur: liest primary_intent/mode/confidence per-SID) + services/live_session.py:299-371 init_session_state['state'] (wo primary_intent/confidence zu seeden sind) + claude_service.py:907 `_qa_pipeline_dispatch(...)`-Aufruf (der Antwort-Trigger — der per-SID-Write muss DAVOR liegen)
    - **M-2:** services/deepgram_service.py:18/427 (`_session_modes`) + TAXO1-07-Plan (Konsolidierungs-Ziel)
    - RESEARCH §4 (D2-Migration) + CONTEXT D-01 + TAXO-INTERLOCK-FINDINGS IL-2/M-2
  </read_first>
  <action>
    Punkt-14. Control-Flow/Skelett/Cross-File aus Sektionen 1-3 + beim Lesen verifizieren (analyse_loop = Daemon-Thread).
    (a) **Haiku-Prompt (claude:66/76):** den Klassifikator-Prompt auf die §1-Werte umstellen — er soll `intent_type` aus INTENT_TAXONOMY_V1 liefern (Kern+Gemini), nicht mehr `ist_vorwand`-Boolean + freien `typ`. Phase getrennt (Kontext, nicht vermischt — Geruest §1).
    (b) **ergebnis-dict (claude:966/968):** `'einwand_typ': ergebnis.get('typ','')` + `'ist_vorwand': ergebnis.get('ist_vorwand',False)` ERSETZEN durch `intent_type` aus dem neuen Prompt-Output (validiert via is_valid_intent_type). Alte Keys LOESCHEN (parallele Welt weg, §0.1).
    (c) **Emit:** an der Stelle wo heute Intent+Phase im per-SID-State landen → zusaetzlich `emit_intent_event(session_id=sid, mode=<per-SID mode>, intent_type=..., phase=<int current_phase>, source='llm_inferred', inference_basis='advisor_paraphrase' bei cold_call / 'direct_customer_utterance' bei meeting, confidence=..., speaker_role=..., speaker_id=..., user_id=<per-SID>, org_id=<per-SID>, interaction_id=<offener Moment>)`. **mode-Quelle (bewiesen):** `deepgram_service._session_modes.get(sid)` (deepgram_service.py:18/427 — per-SID `{sid: 'cold_call'|'meeting'}`), NICHT ein angenommenes `_session_state[sid]['mode']`; speaker/user_id/org_id aus der Welle-3-per-SID-Quelle (org_id live_session:306/752). **Moment-Fenster (Task 1b, I-4-FOLD — KEIN line_id):** `interaction_id = ls.get_or_open_moment(sid, mode=<per-SID mode>, now=time.monotonic())` — der Haiku-Intent ist ein erkannter Kunden-Intent → er OEFFNET das Fenster (oder setzt das offene fort). Der Helper ist lock-frei → den Call INNERHALB des bestehenden `with ls._session_state_lock:`-Blocks setzen, in dem ohnehin der per-SID-State geschrieben wird (Gemini-Punkt a: Aufrufer haelt den Lock, kein Reentrancy). KEIN line_id mehr. (Die finale Modus-/Speaker-Bestimmung verfeinert Welle 7 ModeStrategy; HIER mit per-SID-mode + cold_call=berater/local-Default arbeiten.)
    (c2) **COLD-CALL-SCHLIESSER (Topic-Shift, Gemini-Punkt b-Kontext):** am bestehenden `phase_did_change`-Block (claude_service.py:1010-1016, schon `with ls._session_state_lock:`) → bei `phase_did_change and mode=='cold_call'`: `ls.close_moment(sid, reason='topic_shift')` (lock-frei, Lock wird gehalten). „Berater geht erkennbar zum naechsten Thema" = Phase-Wechsel. Der naechste Kunden-Intent oeffnet dann ein frisches Fenster. (Meeting-Schliesser = Task 3c in deepgram; Timeout+Downgrade im Helper.)
    (d) **EinwandTypEnum (profile_schema:45):** auf die EINE Quelle umziehen ODER ersetzen — der Live-Klassifikator liest den Enum nicht (RESEARCH §4); die Profil-Speicherung von einwand_typ auf intent_taxonomy-Werte mappen oder den Enum entfernen, wenn keine lebenden Leser (grep profile_schema:431/437). **Beleg-Pflicht (§0.1):** alte parallele Pfade nur loeschen wenn grep 0 lebende Leser zeigt; sonst auf die neue Quelle umstellen.
    (e) **IL-2 LIVE-UEBERGABE-VERTRAG (TAXO3-Naht — PFLICHT, BLOCKER-Aufloesung):** an derselben Stelle, wo Intent+Phase im per-SID-State landen (und VOR dem Antwort-Trigger / vor `_qa_pipeline_dispatch` bzw. dem qa-dispatch), zusaetzlich `_session_state[sid]['state']['primary_intent'] = intent_type` und `_session_state[sid]['state']['confidence'] = confidence` schreiben (unter `_session_state_lock`, gleiches Muster wie die Welle-3-per-SID-Writes). Begruendung: TAXO3 `build_answer_context(*, primary_intent, mode, confidence, ...)` (TAXO3-03) liest diese drei LIVE per-SID aus dem RAM — der intent_event-DB-Insert allein erfuellt den Vertrag NICHT (TAXO3 liest nicht die DB im Live-Pfad). mode liegt schon per-SID (Task 2c-Quelle), interaction_id/call_id auch — NUR primary_intent+confidence fehlen heute. **Additiv:** primary_intent+confidence frisch in `init_session_state['state']` seeden (=None), damit TAXO3 `.get()` vor dem ersten Intent kein KeyError sieht. KEIN neuer Score (nur die rohe Haiku-confidence durchreichen). Der emit_intent_event-Call bleibt; dies ist der zusaetzliche RAM-Write fuer die Live-Naht.
    (f) **M-2 CROSS-WAVE-MARKER (TAXO1-07-Naht):** die mode-Quelle `deepgram_service._session_modes.get(sid)` (Task 2c) mit Code-Kommentar markieren: `# M-2/TAXO1-07: mode-Quelle — bei _session_modes-Konsolidierung (W7) DIESEN Read mit-migrieren.` Im SUMMARY als Cross-Wave-Vertrag notieren, damit TAXO1-07 diese Stelle anfasst. KEINE Aenderung der Quelle in DIESER Welle (W4 nutzt _session_modes; W7 zieht um).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Checkpoint).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "emit_intent_event" services/claude_service.py` zeigt den Medium-Lane-Emit.
    - ist_vorwand-Pfad weg: `grep -n "'ist_vorwand'\|ergebnis.get('ist_vorwand'" services/claude_service.py` == 0.
    - intent_type geschrieben + validiert: `grep -n "intent_type\|is_valid_intent_type" services/claude_service.py` zeigt die neue Logik.
    - EinwandTypEnum: entweder entfernt ODER auf neue Quelle gemappt — grep-Beleg in SUMMARY dass keine lebende Doppel-Quelle bleibt.
    - `grep -n "get_or_open_moment\|interaction_id" services/claude_service.py` zeigt, dass der Medium-Emit das offene Fenster durchreicht (get_or_open_moment, KEIN line_id-Key).
    - **Cold-Call-Schliesser:** `grep -n "close_moment" services/claude_service.py` zeigt den `phase_did_change and mode=='cold_call'`-Close (Topic-Shift).
    - **IL-2:** `grep -n "\['primary_intent'\]\s*=\|\['confidence'\]\s*=" services/claude_service.py` zeigt den per-SID-Write von primary_intent+confidence VOR dem Antwort-Trigger; `grep -n "'primary_intent'\|'confidence'" services/live_session.py` zeigt die init_session_state-Seeds (=None). SUMMARY belegt: Write sitzt VOR _qa_pipeline_dispatch/qa-dispatch (nicht danach).
    - **M-2:** `grep -n "M-2/TAXO1-07" services/claude_service.py` 1 Treffer (mode-Quelle-Marker fuer W7).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Medium Lane emittiert intent_event mit §1-Taxonomie + geteilter interaction_id; ist_vorwand/freier typ geloescht; EinwandTypEnum konsolidiert; IL-2 Live-Uebergabe (primary_intent+confidence per-SID vor Trigger) erfuellt; M-2-Naht markiert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fast-Lane-Cutover — Keyword-Treffer (matcher) + EWB-Button (app_routes) → emit_intent_event</name>
  <read_first>
    - ⚠️ RE-GREP zuerst
    - services/einwand_keyword_matcher.py (Keyword-Treffer-Pfad, sid-Verfuegbarkeit nach Welle 3) — 30 Zeilen vor/nach VERBATIM
    - routes/app_routes.py:323 (ls.state['ewb_clicks'] EWB-Button) — 30 Zeilen vor/nach VERBATIM
    - services/intent_event_writer.py (emit_intent_event)
    - RESEARCH §2/§3 Fast-Lane-Abschnitt (Button=ui_asserted, Keyword=llm_inferred/lokal, <800ms kein LLM)
  </read_first>
  <action>
    Punkt-14.
    (a) **Keyword-Treffer (matcher):** beim lokalen Stichwort-Treffer SOFORT `emit_intent_event(..., source='llm_inferred', inference_basis='advisor_label', confidence=<lokaler Wert>, speaker_role='berater', speaker_id='local', interaction_id=<offener Moment>)` (cold_call-Default; <800ms, kein LLM). **Moment-Fenster (I-4-FOLD, KEIN line_id):** `iid = ls.get_or_open_moment(sid, mode=<per-SID mode/cold_call-Default>, now=time.monotonic())` — der Keyword-Treffer ist ein erkannter Kunden-Intent → oeffnet/setzt das Fenster fort, dieselbe id wie Medium/Button desselben offenen Fensters. **Lock-Disziplin (Gemini-Punkt a, EINDEUTIG):** der matcher haelt heute `_ls.state_lock` (module-global) fuer `kw_fired_for_line`. Der Helper braucht `_session_state_lock` (per-SID). → den `get_or_open_moment`-Call in einem EIGENEN `with ls._session_state_lock:`-Block machen, **NICHT genested im `with _ls.state_lock:`-Block** (kein gleichzeitiges Halten beider Locks → kein Lock-Ordering-Deadlock). Helper ist lock-frei, der `with`-Block des Aufrufers haelt den Lock. KEIN line_id, KEIN doppeltes Acquire.
    (b) **EWB-Button (app_routes:323) — loest Gemini-Punkt c (Button-vor-line_id-Race) STRUKTURELL:** beim Button-Klick zusaetzlich `emit_intent_event(..., source='ui_asserted', inference_basis='ui_button', confidence=1.0, interaction_id=ls.get_or_open_moment(sid, mode=<per-SID mode>, now=time.monotonic()))` SOFORT (nicht erst bei Call-Ende), in einem `with ls._session_state_lock:`-Block (Helper lock-frei). **Der Button haengt damit an den AKTUELL OFFENEN Moment** — NICHT an eine line_id/Vorzeile (Race eliminiert, weil die Klammer nicht mehr line_id-gekeyt ist). Ist kein Fenster offen (Berater drueckt proaktiv ohne erkannten Kunden-Intent) → get_or_open_moment OEFFNET eines (Button-getriebener Moment ist erlaubt). `line_id`/`ls.state.get('line_id')` wird fuer die Klammer NICHT mehr gebraucht. Der bestehende `ls.state['ewb_clicks']`-Pfad bleibt vorerst (Welle 5 Dual-Write); das Call-Ende-Schreiben (app_routes:384) wird in Welle 5 zur Bruecke umgebaut. **Lock-Disziplin:** `_session_state_lock` fuer den Moment-Call; NICHT gleichzeitig mit `ls.state_lock` halten.
    **Plausibilitaets-Hinweis (Geruest §3):** ui_asserted confidence=1.0, aber Buttons luegen unter Stress — kein Live-Scoring darauf (TAXO2). In TAXO1 nur als Event mit source=ui_asserted festhalten.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Checkpoint).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "emit_intent_event" services/einwand_keyword_matcher.py` 1+ Treffer (source=llm_inferred).
    - `grep -n "emit_intent_event" routes/app_routes.py` 1+ Treffer (source=ui_asserted, confidence=1.0).
    - Fast Lane ruft KEIN LLM (`grep -iE "anthropic|haiku|messages.create" services/einwand_keyword_matcher.py` zeigt keinen neuen LLM-Call).
    - `grep -n "get_or_open_moment\|interaction_id" services/einwand_keyword_matcher.py routes/app_routes.py` zeigt, dass Keyword UND Button das offene Fenster durchreichen (get_or_open_moment, KEIN line_id-Key).
    - **Lock-Disziplin (Punkt a):** in matcher + app_routes wird `get_or_open_moment` in einem `with ...._session_state_lock:`-Block aufgerufen, der NICHT gleichzeitig `state_lock` haelt (grep/manuelle Sicht: kein genestetes `with state_lock: ... with _session_state_lock:`).
    - **Button-Race (Punkt c):** `grep -n "interaction_id_line\|line_id" routes/app_routes.py` belegt, dass die Button-Klammer NICHT mehr an line_id haengt (kein line_id-Read fuer interaction_id).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Fast Lane (Keyword + Button) emittiert intent_event sofort mit der id des OFFENEN Fensters (get_or_open_moment); Button haengt am offenen Moment statt an line_id (Punkt c geloest); Button=ui_asserted, Keyword=llm_inferred.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3c: Fenster-Schliesser in deepgram_service.py — Meeting: Berater spricht (Sprecher-Wechsel) + Modus-Downgrade (additiv, Sprecher-Detection UNVERAENDERT) [I-4-FOLD §5]</name>
  <read_first>
    - ⚠️ RE-GREP zuerst
    - services/deepgram_service.py:56-94 VERBATIM — finalisierte Utterance: `speaker = ls.stabilize_speaker(...)` (:63), `roles_confirmed` (:71/85), `sp_label = 'Berater' if log_sp == 0 else 'Kunde'...` (:86), Persist (:94). DAS ist die Stelle, wo der Per-Utterance-Sprecher (meeting) feststeht — NUR LESEN.
    - services/deepgram_service.py:427 VERBATIM (`_session_modes[sid] = mode`) + :880-892 (Modus-Switch `new_mode = 'gatekeeper' if ... else 'cold_call'`) — Modus-Downgrade-Stelle.
    - services/live_session.py close_moment (Task 1b) — der Schliesser-Helper (lock-frei, Aufrufer haelt `_session_state_lock`).
  </read_first>
  <action>
    Punkt-14. ADDITIV — `ls.stabilize_speaker`/`sp_map`/`roles_confirmed`/Persist NUR LESEN, NICHT aendern (T-TAXO1-12). Nur close_moment-Aufrufe ergaenzen.
    (a) **Meeting-Schliesser (André §5: „Berater spricht wieder = Sprecher-Wechsel zum Berater"):** auf dem finalisierten-Utterance-Pfad (deepgram:56-94), wenn `mode == 'meeting'` (bzw. nicht cold_call) UND der stabilisierte Sprecher der **Berater** ist (`sp_label == 'Berater'` / `log_sp == 0`, der bereits berechnete Wert): `with ls._session_state_lock: ls.close_moment(sid, reason='advisor_spoke')`. Damit schliesst das Einwand-Fenster, sobald der Berater (in einem 2-Sprecher-Meeting) wieder spricht — der naechste Kunden-Intent oeffnet ein frisches. (Cold Call ist Single-Speaker = immer Berater :81 → HIER NICHT schliessen, sonst wuerde jede Zeile schliessen; Cold-Call-Schliesser ist der Topic-Shift in Task 2 c2.) **Nur auf FINALISIERTEN** Utterances (nicht interim), damit kein Flackern.
    (b) **Modus-Downgrade-Schliesser (Gemini-Punkt d, „zwingend schliessen/resetten"):** an der Stelle, wo `_session_modes[sid]` auf einen ANDEREN Wert gesetzt wird (Meeting→Cold-Call/gatekeeper, :427/:880-892), zusaetzlich `with ls._session_state_lock: ls.close_moment(sid, reason='mode_downgrade')` — der offene Moment des alten Modus wird sofort verworfen. (Belt-and-suspenders zum Helper-internen moment_opened_mode-Mismatch in Task 1b: der Helper faengt es spaetestens beim naechsten Intent, dieser Hook macht es SOFORT.)
    - **Lock-Disziplin (Punkt a):** close_moment lock-frei, der `with ls._session_state_lock:`-Block haelt den Lock; NICHT gleichzeitig mit deepgram-internen Locks/`state_lock` halten (kein Lock-Ordering-Deadlock — eigener kurzer Block).
    - KEINE Aenderung an der Sprecher-Erkennung selbst (stabilize_speaker/sp_map/roles_confirmed/diarize). Reiner additiver close_moment-Aufruf basierend auf dem schon berechneten Sprecher/Modus.
    Marker-Kommentar: `# MOMENT-FENSTER Schliesser (I-4-FOLD §5): Meeting=Berater spricht / Modus-Downgrade → close_moment.`
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Checkpoint, meeting 2-Sprecher).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "close_moment" services/deepgram_service.py` zeigt den Meeting-Berater-Schliesser (mode=='meeting' + sp_label=='Berater') UND den Modus-Downgrade-Schliesser (am _session_modes-Set).
    - Sprecher-Detection unangetastet: `git diff services/deepgram_service.py` zeigt KEINE Aenderung an stabilize_speaker/sp_map/roles_confirmed/diarize — nur additive close_moment-Aufrufe + Marker-Kommentar.
    - Cold-Call schliesst NICHT auf jede Zeile: der Meeting-Schliesser ist auf `mode=='meeting'` gegated (manuelle Sicht; cold_call nutzt Topic-Shift Task 2 c2).
    - Lock-Disziplin: close_moment im eigenen `with _session_state_lock:`-Block, nicht genested mit anderen Locks (manuelle Sicht).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Meeting-Schliesser (Berater-Sprecher-Wechsel) + Modus-Downgrade-Schliesser additiv in deepgram_service.py; Sprecher-Detection unberuehrt; Cold-Call ueber Topic-Shift (Task 2) statt Sprecher.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: K4 — ENV-Threshold + intent_event bei abstain/low-conf (config.py + analyse_loop/qa-dispatch), Single-Speaker-Funnel NICHT brechen</name>
  <read_first>
    - ⚠️ RE-GREP zuerst
    - config.py:42 (CLASSIFIER_CONFIDENCE_THRESHOLD=0.80 hardcoded) — 30 Zeilen vor/nach VERBATIM
    - **H-4 (PFLICHT VERBATIM, _qa_pipeline_dispatch claude_service.py):1277-1398** — die 6 [QA-INT]-Stellen klassifiziert lesen: :1278 D-02 kw_fired-Skip (DOPPEL-FEUER, return VOR classify), :1284 slot1-busy-Mutex-Skip, :1288 no-sid-Skip, :1307-1310 classify→_conf, :1340 einwand_unknown `_conf<THRESHOLD` (ECHTER DROP), :1382 frage-no-faq `_conf<THRESHOLD` (ECHTER DROP), :1395 smalltalk_none/einwand_known (no-action)
    - services/qa_pipeline.py:64 (smalltalk_none-Unterdrueckung) / live_session.py:695 (QA-Gate roles_confirmed)
    - RESEARCH §3 K4-Diagnose (KEIN Pipeline-Bug; Threshold + Single-Speaker-Funnel) + TAXO-INTERLOCK-FINDINGS H-4
  </read_first>
  <action>
    Punkt-14. **K4 aufloesen, NICHT ueberbauen (RESEARCH §3):**
    (a) **ENV-Threshold (config.py:42):** `CLASSIFIER_CONFIDENCE_THRESHOLD = float(os.getenv('CLASSIFIER_CONFIDENCE_THRESHOLD', '0.55'))` — Hardcode 0.80 → ENV-gesteuert, Default ~0.55 (Punkt-12-Marge, <30s reversibel). Wert im Test-Anruf empirisch kalibrieren (Haiku clustert ~0.65).
    (a2) **Reine Funnel-Helper (config.py, K4 unit-testbar — Finding #4):** zusaetzlich eine kleine reine Funktion in config.py definieren: `def should_abstain(confidence, threshold=None) -> bool: t = CLASSIFIER_CONFIDENCE_THRESHOLD if threshold is None else threshold; return confidence is None or confidence < t`. KEINE I/O, KEIN LLM, KEINE DB — reine Schwellen-Entscheidung. Die [QA-INT]-Stellen (b) nutzen diese Funktion statt eines inline `if confidence < 0.x` → die K4-Entscheidung wird damit ohne Live-Call unit-testbar (Test in Checkpoint / tests/test_k4_threshold_funnel.py).
    (b) **H-4 — abstain/low-conf → intent_event NUR am ECHTEN Drop (TAXO-INTERLOCK-FINDINGS: die 6 [QA-INT]-Stellen sauber unterscheiden, sonst flutet abstained=True den intent_event):**
       Die [QA-INT]-Stellen sind NICHT alle low-conf-Drops. Klassifikation (beim Lesen :1277-1398 verifizieren):
       - **:1278 `if _kw_fired_for == line_id: return` = DOPPEL-FEUER-SKIP (D-02), KEIN Drop.** Der Keyword-Matcher (Fast Lane) hat fuer diese Zeile bereits ein intent_event mit GETEILTER interaction_id geschrieben. HIER abstain-emiten ist FALSCH: (1) Doppel-Event fuer denselben Moment, (2) der return sitzt VOR classify (:1307) → es gibt KEIN _conf → confidence=None → abstained=True fuer JEDEN keyword-vorgefeuerten Moment → Flut. → **NICHT emiten.**
       - **:1284 slot1-busy-Mutex-Skip + :1288 no-active_sid-Skip = KEINE Drops** (Moment schon gecued / keine Session). → **NICHT emiten.**
       - **:1340 `if _conf < CLASSIFIER_CONFIDENCE_THRESHOLD: _emit_soft_hint('low_confidence')` (kat=einwand_unknown) = ECHTER LOW-CONF-DROP.** HIER `emit_intent_event(..., intent_type=<aus _kat/Klassifikation>, abstained=should_abstain(_conf), confidence=_conf, interaction_id=ls.get_or_open_moment(sid, mode=<per-SID mode>, now=time.monotonic()))` VOR/mit dem `_emit_soft_hint` (Emit VOR dem Drop-Pfad, kein Dead-Code; der Moment-Helper lock-frei im `with _session_state_lock:`-Block). _conf ist ECHT (aus classify_utterance:1307). KEIN line_id-Key (I-4-FOLD).
       - **:1382 `if _conf < CLASSIFIER_CONFIDENCE_THRESHOLD: _emit_soft_hint('no_faq_low_conf')` (kat=frage, kein FAQ) = ECHTER LOW-CONF-DROP.** Gleicher abstain-Emit hier.
       - **else-Zweige (conf>=THRESHOLD → `_emit_qa_slot1`) = NORMAL-Emit** (abstained=False, intent_type gesetzt) — die Medium/QA-Lane emittiert das echte Event (ggf. schon ueber Task 2c abgedeckt; hier nur sicherstellen, dass der hohe-conf-Pfad ein non-abstained Event hat, kein Doppel).
       - **:1395 smalltalk_none/einwand_known = no-action (Single-Speaker-Monolog-Filter, korrekt) → NICHT emiten.**
       Die UI-Cue-Schwelle bleibt am Threshold (kein lauter Cue bei abstain), aber das EVENT wird an :1340/:1382 festgehalten. **Anti-Flut-Pflicht:** `grep` darf KEINEN abstain-Emit am :1278/:1284/:1288-Skip zeigen.
    (c) **Single-Speaker-Funnel NICHT brechen:** den QA-Gate (live_session:695 `roles_confirmed`) + smalltalk_none-Unterdrueckung (qa_pipeline:64) UNVERAENDERT lassen — RESEARCH §3: der Single-Speaker-Monolog-Filter ist korrekt, nur die Threshold-Unterdrueckung legitimer frage war das Problem. KEIN Eingriff in roles_confirmed/sp_map.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Checkpoint): qa feuert bei echtem Einwand (2-Sprecher).</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CLASSIFIER_CONFIDENCE_THRESHOLD" config.py` zeigt `os.getenv(...)` (kein Hardcode 0.80).
    - `grep -n "def should_abstain" config.py` 1 Treffer (reine Funnel-Helper, Finding #4); `grep -n "should_abstain" services/claude_service.py` zeigt die Nutzung an der [QA-INT]-Stelle.
    - `grep -nE "abstained=should_abstain|abstained=True|abstained" services/claude_service.py` zeigt den abstain-Emit (Funnel, nicht gedroppt).
    - **H-4 Anti-Flut:** der abstain-Emit sitzt NUR an den echten Drops (:1340/:1382-Umfeld, beim `_conf < THRESHOLD`-Zweig) — SUMMARY belegt mit Zeilen-Kontext, dass am :1278 kw_fired-Skip + :1284 slot1-Skip + :1288 no-sid-Skip KEIN emit_intent_event steht (kein abstain VOR classify). smalltalk_none/einwand_known (:1395) unveraendert no-action.
    - QA-Gate unveraendert: `git diff services/live_session.py services/qa_pipeline.py` zeigt KEINE Aenderung an roles_confirmed/smalltalk_none (Single-Speaker-Funnel intakt).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>K4 aufgeloest: Threshold ENV-gesteuert, abstain/low-conf landet als intent_event NUR am echten Drop (:1340/:1382), NICHT am Doppel-Feuer-/Mutex-Skip (H-4); Single-Speaker-Funnel unangetastet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: tests/test_k4_threshold_funnel.py — K4 Threshold-Funnel pure-logic Regression-Test (Cross-AI Finding #4)</name>
  <read_first>
    - CLAUDE.md "Test-Qualitaets-Regel" (Function-Call-Return-Assertion, KEIN Source-Presence)
    - config.py (CLASSIFIER_CONFIDENCE_THRESHOLD ENV-Default 0.55 + should_abstain(), Task 4 a/a2)
    - tests/ Bestands-Muster (server-seitig lauffaehig, sauberes Skip wenn Dependency fehlt)
  </read_first>
  <action>
    NEUE Datei `tests/test_k4_threshold_funnel.py`. Pure-logic Regression der K4-Funnel-Entscheidung (KEIN Live-Call, KEINE DB, KEIN LLM — importiert nur config.should_abstain, das keine schweren Deps zieht):
    - `def test_low_conf_abstains_not_dropped():` — `should_abstain(confidence=0.40, threshold=0.55)` == True (low-conf → abstained=True; der Aufrufer schreibt das Event mit abstained statt es zu droppen — K4-Kern).
    - `def test_high_conf_normal_emit():` — `should_abstain(confidence=0.80, threshold=0.55)` == False (high-conf → normaler Emit).
    - `def test_boundary_at_threshold():` — `should_abstain(0.55, 0.55)` == False (>= threshold ist normal) und `should_abstain(0.5499, 0.55)` == True (knapp drunter abstain) — Funnel-Grenze sauber.
    - `def test_none_confidence_abstains():` — `should_abstain(None, 0.55)` == True (fehlende Confidence ist konservativ abstain, kein Crash).
    - `def test_env_default_threshold():` — ohne explizites threshold nutzt should_abstain den ENV-Default; assert dass ein klar-niedriger Wert (z.B. 0.30) abstain und ein klar-hoher (0.95) normal liefert (deckt die ENV-Verdrahtung ab, ohne den exakten Default zu hardcoden).
    Server-seitig via pytest lauffaehig; sauberer Skip wenn Import fehlschlaegt (kein False-Green). Echter Function-Call von should_abstain, KEIN getsource/grep-auf-Quelle.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('tests/test_k4_threshold_funnel.py').read())"` lokal erlaubt. Lauf server-seitig via Checkpoint: `ssh ... pytest tests/test_k4_threshold_funnel.py -x`.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def test_low_conf_abstains_not_dropped" tests/test_k4_threshold_funnel.py` 1 Treffer.
    - Function-Call-Assertion (echter should_abstain-Aufruf), KEIN Source-Presence: `grep -E "getsource|hasattr|open\(.*config.*\)\.read" tests/test_k4_threshold_funnel.py` == 0.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>K4-Funnel-Entscheidung durch pure-logic Regression abgesichert (low-conf abstain statt drop, Grenze, ENV-default); server-seitig lauffaehig.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cutover komplett: emit_intent_event als Single Source, Fast+Medium emittieren, §1-Taxonomie als intent_type, ist_vorwand/EinwandTypEnum geloescht, K4 ENV+abstain + K4-Funnel-Regression-Test (Finding #4), ereignis-getriebenes Moment-FENSTER (I-4-FOLD: get_or_open_moment/close_moment, Meeting=Berater-Sprecher / Cold-Call=Topic-Shift / Timeout / Downgrade) geteilt ueber alle Emits eines offenen Fensters + 6 pure-logic Fenster-Tests. Bereit fuer Deploy + Test-Anruf.</what-built>
  <how-to-verify>
    ⚠️ **GATE: Deploy + Test-Anruf ERST NACH Cross-AI-Clearance + André-Ineinandergreifen-Check.** Nach Clearance:

    1. `git add -A && git commit && git push origin main` → `bash deploy.sh production`.
    2. Unit-Tests server-seitig: `ssh ... '/opt/nerve/venv/bin/pytest tests/test_intent_event_writer.py tests/test_k4_threshold_funnel.py -x -v'` → GRUEN (Writer: Insert + custom + abstain + insert-only; K4-Funnel: low-conf abstain statt drop, Grenze, ENV-default — Finding #4). Verbatim-Output in SUMMARY.
    3. **TEST-ANRUF (D-03 Auflage 3, cold_call UND meeting):**
       - **(a) intent_event-Zeilen geschrieben (REQ 4 Kern):** `ssh ... 'bash scripts/inspect.sh count intent_event'` STEIGT von 0; `ssh ... 'bash scripts/inspect.sh sample intent_event 20'` zeigt korrekte `intent_type` (aus §1), `phase` (int 1-6), `mode` (cold_call/meeting), `source` (llm_inferred/ui_asserted), `speaker_role`; payload_jsonb mit `taxonomy_version='v1'` non-null bei JEDER Zeile.
       - **(b) K3 weiter sauber:** `ssh ... 'bash scripts/inspect.sh logs-errors 200'` → kein phase_classify int/str.
       - **(c) qa/Medium feuert bei echtem Einwand (K4, 2-Sprecher-meeting):** Log-Beleg dass bei einem Kunden-Einwand ein intent_event mit korrektem intent_type geschrieben wird; low-conf-faelle als abstained=True sichtbar (Funnel), nicht gedroppt. **H-4-Beleg:** ein keyword-vorgefeuerter Moment (Fast-Lane-Treffer + sofortiger Haiku) erzeugt KEINE Flut von abstained=True-Zeilen — `sample intent_event` zeigt abstained=True nur bei echten low-conf-Momenten, nicht bei jedem kw_fired-Skip.
       - **(c2) IL-2 Live-Uebergabe (TAXO3-Naht):** waehrend eines Calls belegen, dass primary_intent+confidence im per-SID-RAM stehen (kommt erst in TAXO3 voll zum Tragen — hier struktureller Beleg): `ssh ... 'bash scripts/inspect.sh logs 200'` zeigt nach Haiku-Intent einen per-SID-Write-Logpunkt ODER (falls kein Log) grep-Beleg im SUMMARY, dass der `_session_state[sid]['state']['primary_intent']=`-Write VOR `_qa_pipeline_dispatch` sitzt. (Kein DB-Beleg moeglich — es ist RAM; der Vertrag wird in TAXO3 funktional konsumiert.)
       - **(d) handling_score leer (REQ 2):** `sample intent_event` zeigt handling_score_numeric = NULL bei allen Zeilen.
       - **(e) custom_objection_* (REQ 5):** falls testbar — ein Profil-custom-Einwand erzeugt intent_type=`custom_objection_<label>` ohne Fehler (sonst struktureller Beleg via Unit-Test Task 1).
       - **(f) Moment-FENSTER (I-4-FOLD, ereignis-getrieben):** (i) **Bündelung:** mehrere Einwaende am Stueck OHNE Berater-Antwort + die mehreren Bahnen (Keyword+Haiku+ggf. Button) → `sample intent_event 20` zeigt fuer dasselbe offene Fenster mehrere `event_id` mit IDENTISCHER `interaction_id` (verschiedene `source`/`intent_type`). (ii) **Schliessen Meeting:** im 2-Sprecher-Meeting nach einem Einwand der Berater spricht → der NAECHSTE Kunden-Einwand traegt eine NEUE `interaction_id` (Fenster geschlossen). (iii) **Schliessen Cold Call:** nach einem Themen-/Phasen-Wechsel (phase_did_change) traegt der naechste Einwand eine neue id. (iv) **Timeout:** ein Einwand >45s nach dem letzten ohne Schliess-Event → neues Fenster. Log-Beleg `[MOMENT] close ... reason=advisor_spoke|topic_shift|mode_downgrade` + `sample` gruppiert nach interaction_id. Verbatim in SUMMARY.
       Verbatim-Belege in SUMMARY.
    4. KEIN SCHILD-Guard noetig (keine Schema-Aenderung — intent_event-Schema steht aus Welle 1).
  </how-to-verify>
  <resume-signal>"deployed + Test-Anruf beide Modi: count intent_event steigt, sample korrekt (intent_type/phase/mode/source/speaker_role + taxonomy_version + interaction_id geteilt pro Moment), K3 sauber, qa feuert, handling_score NULL" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 1. Lokaler Kontext
Insert-Sites: claude_service.py :66/:76 (Prompt), :966/:968 (ergebnis-dict), analyse_loop-Emit-Stelle, :990-1018 (per-SID current_phase + **phase_did_change = Cold-Call-Schliesser**), :1245-Umfeld (qa-dispatch), :1279ff ([QA-INT]-Drops); einwand_keyword_matcher.py (Keyword-Treffer-Pfad); routes/app_routes.py:323 (EWB-Button); **deepgram_service.py:56-94 (Meeting-Berater-Schliesser) + :427/:880-892 (Modus-Downgrade-Schliesser)**; config.py:42 (+MOMENT_WINDOW_TIMEOUT_S); profile_schema.py:45/:103; **live_session.py:299-371 (init_session_state — 3 Fenster-Keys seeden) + NEUE additive Helfer get_or_open_moment/close_moment (Task 1b, per-SID `_session_state[sid]['state']`, lock-frei).** **Pflicht beim Bau:** je Site 30 Zeilen vor/nach VERBATIM + alle return/continue/break/raise mit Zeilennummer. RE-GREP zuerst (Zeilen koennen seit Welle 3 gewandert sein). KRITISCH: der EWB-Button-Emit darf den bestehenden ewb_clicks-Pfad NICHT brechen (additiv); K4-abstain-Emit VOR dem Drop-return (sonst Dead-Code wie 08.19.5.6.4.2-Bug).
**Moment-FENSTER (I-4-FOLD, Punkt-14 — wo die ID erzeugt/geteilt/geschlossen wird):** EIN Moment = ein Einwand-FENSTER, NICHT pro Zeile. Erzeugung+Teilung: `get_or_open_moment(sid, mode, now)` (live_session, lock-frei) oeffnet beim ersten Kunden-Intent / setzt das offene Fenster fort, am per-SID `_session_state[sid]['state']`. Schliessen: `close_moment(sid, reason)` — Meeting (Berater-Sprecher, deepgram Task 3c), Cold Call (phase_did_change Task 2 c2), Modus-Downgrade (Helper-Mismatch + deepgram Task 3c), Timeout (Helper, MOMENT_WINDOW_TIMEOUT_S). Alle Emit-Sites (Matcher/analyse_loop/QA-Drop/Button) RUFEN get_or_open_moment und reichen die id durch — sie minten NICHT selbst. **Lock/Reentrancy (Gemini-Punkt a, EINDEUTIG):** Helfer lock-frei, Aufrufer haelt `_session_state_lock` (Muster matcher:252). KEIN RLock. `state_lock` (module-global) und `_session_state_lock` NIE gleichzeitig halten → kein Lock-Ordering-Deadlock. KEIN line_id-Key mehr (die alte `interaction_id_line==line_id`-Logik ist WEG — sie war der B-3-„Daten-Vergifter").

## 2. Funktions-Skelett
**Moment-FENSTER-Quelle (Punkt-14: Oeffnen/Fortsetzen/Schliessen der interaction_id):**
- **get_or_open_moment(sid, *, mode, now) (live_session, Task 1b) — LOCK-FREE:** operiert auf `st = _session_state[sid]['state']` (Aufrufer haelt `_session_state_lock`). (d) wenn offen und `st['moment_opened_mode']!=mode` → close (Downgrade). (b) wenn offen und `now-st['moment_last_activity_monotonic']>MOMENT_WINDOW_TIMEOUT_S` → close (Timeout). dann: wenn `st['interaction_id'] is None` → mint uuid4, setze interaction_id/moment_opened_mode/last_activity → return (OEFFNEN); sonst refresh last_activity → return bestehende id (FORTSETZEN: mehrere Einwaende am Stueck = EIN Moment).
- **close_moment(sid, *, reason) (live_session, Task 1b) — LOCK-FREE:** `st['interaction_id']=None` (idempotent). Aufgerufen von: Cold-Call-Topic-Shift (claude phase_did_change), Meeting-Berater-Sprecher (deepgram), Modus-Downgrade (deepgram). Timeout/Downgrade auch helper-intern beim naechsten Oeffnen.

Die Emit-Bahnen holen die id via get_or_open_moment und reichen sie durch — sie minten NICHT selbst; alle Helfer-Calls lock-frei unter `_session_state_lock`:
- **analyse_loop (Daemon-Thread):** while-Loop/SID → Haiku-Call → ergebnis-dict (intent_type) → per-SID-State-Write → `iid = get_or_open_moment(sid, mode, now)` (im `with _session_state_lock:`-Block) → [INSERT emit_intent_event(..., interaction_id=iid)] → log_api_cost (Welle 3). PLUS am phase_did_change-Block (mode=='cold_call'): `close_moment(sid, reason='topic_shift')`.
- **generate_qa_response / [QA-INT]-Pfad:** roles_confirmed-Gate (live_session:695, UNVERAENDERT) → Klassifikation → if confidence < threshold (echter Drop :1340/:1382): `iid=get_or_open_moment(sid, mode, now)` → [INSERT emit abstained=True, interaction_id=iid] VOR dem Drop-return → sonst Slot-B-Emit.
- **matcher:** Keyword-Match → in eigenem `with ls._session_state_lock:`-Block `iid=get_or_open_moment(sid, mode, now)` (NICHT genested mit `_ls.state_lock` — Lock-Ordering!) → [INSERT emit_intent_event source=llm_inferred, interaction_id=iid] → bestehender Cue-Pfad. Kein LLM.
- **EWB-Button (app_routes):** Klick-Handler → `with ls._session_state_lock: iid=get_or_open_moment(sid, mode, now)` → [INSERT emit source=ui_asserted, interaction_id=iid] → bestehender ewb_clicks-Append (bleibt). **Punkt c geloest:** haengt am OFFENEN Fenster, NICHT an line_id.
- **deepgram on_message (Task 3c):** finalisierte Utterance + meeting + Berater-Sprecher → `with ls._session_state_lock: close_moment(sid, reason='advisor_spoke')`. `_session_modes[sid]`-Switch (Downgrade) → `close_moment(sid, reason='mode_downgrade')`.

## 3. Cross-File-Awareness
- `grep -rn "ist_vorwand\|einwand_typ" services/ routes/` — claude:66/76/966/968, profile_schema:45/103/431/437, objection_events.einwand_typ (DB, varchar100, bleibt fuer Dual-Write Welle 5). Nach Loeschung: ist_vorwand 0 Treffer im Live-Pfad; einwand_typ nur noch im objection_events-Bruecken-Kontext.
- `grep -rn "emit_intent_event\|intent_event_writer" services/ routes/` nach Bau: writer-Datei + matcher + analyse_loop + app_routes (alle Bahnen ueber DIE EINE Funktion). KEIN direkter IntentEvent(...)-Insert ausserhalb des writers (Single-Schreibstelle).
- `grep -rn "CLASSIFIER_CONFIDENCE_THRESHOLD" services/ routes/ config.py` — config:42 (→ENV), Leser in claude_service/qa_pipeline. Threshold-Aenderung trifft alle Leser konsistent (ein ENV-Wert).
- `grep -rn "generate_qa_response\|roles_confirmed\|smalltalk_none" services/` — qa-dispatch (claude:1245), QA-Gate (live_session:695, UNVERAENDERT), smalltalk_none (qa_pipeline:64, UNVERAENDERT). Cross-Thread: analyse_loop + qa laufen im Daemon-Kontext — emit_intent_event ist thread-safe (eigene get_session pro Call).
- **slow_lane (Welle 2):** emit_intent_event darf `slow_lane.put` aufrufen → der Consumer (idle bisher) bekommt jetzt echte Items. KEIN Score (Consumer persistiert/no-op in TAXO1, Bau-Regel 1).

## 4. Edge-Case-Analyse
- **Edge 1 — Haiku liefert intent_type ausserhalb der Taxonomie:** is_valid_intent_type False → emit wirft ValueError/abstained-Fallback (Task 1 Test 3) → KEIN ungueltiger intent_type in DB. Behandlung im writer zentral.
- **Edge 2 — Emit vor mode verfuegbar (Call-Start-Race):** mode-Quelle = `deepgram_service._session_modes.get(sid, 'meeting')` (siehe Task 2c, NICHT `_session_state[sid]['mode']`), `.get(..., Default)` statt harter Index → kein KeyError. cold_call=berater/local-Default für speaker.
- **Edge 3 — DB-Insert schlaegt fehl (DB weg):** emit_intent_event try-finally; bei Fehler loggt + schluckt (Live-Loop darf nicht crashen) — der Cue/QA-Pfad laeuft weiter (Erkennung > Persistenz-Garantie im Live-Loop; Slow Lane/atexit faengt nichts Verlorenes, EA-tragbar).
- **Edge 4 — abstain-Emit + lauter Cue gleichzeitig:** abstained=True schreibt Event, triggert aber KEINEN lauten Cue (Cue-Schwelle am Threshold) → kein UI-Spam bei low-conf. Funnel sichtbar in DB, nicht am Schirm.

### Race-Condition-Audit (KRITISCH — Live-Erkennung umgelenkt, Daemon-Thread + Fast/Medium parallel)
1. **Gleichzeitiges Feuern (Fast Keyword + Medium Haiku + Button fuer dasselbe offene Fenster):** alle rufen emit_intent_event → separate intent_event-Zeilen (verschiedene source). GEWOLLT (Single Source haelt Provenance); KEIN Doppel-Cue (kw_fired_for_line-Schutz Welle 3). Inserts unabhaengig (insert-only) → keine Row-Race. **Moment-FENSTER-Race (I-4-FOLD):** die Zeilen desselben offenen Fensters MUESSEN dieselbe interaction_id tragen. Gefahr: Fast (Matcher-Thread) + Medium (analyse_loop-Daemon) rufen get_or_open_moment nebenlaeufig → zwei uuid4. Mitigation: get_or_open_moment macht check-and-open (`interaction_id is None`?) — der Aufrufer haelt `_session_state_lock` waehrend des Calls (lock-frei im Helper, aber im `with`-Block des Aufrufers), also ist das check-and-open atomar pro-SID; der zweite Thread sieht die schon gesetzte id und teilt sie. **Lock-Ordering (Gemini-Punkt a):** der matcher haelt sonst `state_lock` (module-global) fuer kw_fired — der get_or_open_moment-Call laeuft in einem SEPARATEN `with _session_state_lock:`-Block, NIE genested mit `state_lock` → kein Deadlock (zwei verschiedene Locks, nie gleichzeitig gehalten). KEIN RLock noetig (Helfer nehmen keinen Lock). KEINE line_id im Spiel.
1b. **Schliess-Race (NEU):** close_moment (deepgram Berater-Sprecher / claude phase_did_change / Downgrade) laeuft im jeweiligen `with _session_state_lock:`-Block; get_or_open_moment ebenso. Schliessen+Oeffnen sind damit pro-SID serialisiert. Worst case: Berater-Utterance (close) und ein gleichzeitiger Kunden-Intent (open) wettrennen → der Lock serialisiert; je nach Reihenfolge entsteht ggf. ein zusaetzliches Fenster — harmlos (Hintergrund-Etikett, GATET die Reaktion NICHT; TAXO2 konsolidiert robuster als bei line_id-Fragmenten). Der Timeout-Backstop faengt jedes verwaiste Fenster.
2. **Verbindungsabbruch mid-Operation (Haiku-Call bricht ab):** emit wird nicht erreicht → keine Zeile (kein halber Insert). Der per-SID-State (Welle 3) bleibt konsistent. Kein Muell.
3. **Wiederverbinden mit altem Zustand:** neue SID → frischer per-SID-mode/speaker; emit nutzt die neue SID. Alte intent_event-Zeilen der alten SID bleiben (immutable, korrekt — Provenance). Kein Geister-Emit aus alter Session (per-SID, Welle 3).
4. **Tab im Hintergrund:** EWB-Button-Emit ist user-getriggert (Klick) → im Hintergrund-Tab kein Klick, kein Emit. analyse_loop laeuft server-seitig weiter (Tab-unabhaengig). Kein veralteter Client-State erzeugt Phantom-intent_event.

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `public.intent_event` (SCHREIBEN, insert-only — neuer Single-Source-Write). Schema aus Welle 1.
- `objection_events` (LESEN/Kontext — der EWB-Button-Call-Ende-Pfad bleibt vorerst; voll umgebaut in Welle 5). Schema RESEARCH §6.
- `profiles.daten` (LESEN — custom_objection_*-Label aus Profil-JSON; kein DDL).

### inspect.sh-Beleg
```
# intent_event-Schema: Welle-1-SUMMARY (verbatim dort).
# Test-Anruf (Checkpoint) belegt count steigt + sample korrekt.
$ ssh ... 'bash scripts/inspect.sh sample intent_event 20'   # nach Test-Anruf, verbatim in SUMMARY
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `intent_type` | Haiku/Matcher/Button → emit → DB | DB intent_event.intent_type (varchar64) | ⬜ sample zeigt §1-Werte + custom_* |
| `taxonomy_version` | emit payload | DB intent_event.payload_jsonb-Key (non-null) | ⬜ sample zeigt 'v1' bei jeder Zeile |
| `ist_vorwand` (ALT) | GELOESCHT | war RAM (ergebnis-dict) — keine DB-Spalte | ⬜ grep == 0 im Live-Pfad |
| `phase` | detect_phase int → emit | DB intent_event.phase (smallint) | ⬜ sample zeigt int 1-6 |
| `interaction_id` (Moment-FENSTER, I-4-FOLD) | get_or_open_moment(sid,mode,now) / close_moment(sid) — per-SID `_session_state[sid]['state']` unter `_session_state_lock` (lock-frei im Helper, Aufrufer haelt Lock) → emit → DB | DB intent_event.interaction_id (UUID, indiziert) + per-SID RAM `_session_state[sid]['state']['interaction_id'/'moment_opened_mode'/'moment_last_activity_monotonic']` (kurzlebig; KEIN module-global, KEIN line_id-Key) | ⬜ sample: gleiche id ueber Fast/Medium/Button DESSELBEN offenen Fensters; neue id nach Schliess-Event (Berater spricht/Topic-Shift/Timeout/Downgrade) |
| `handling_score_numeric` | NICHT gesetzt (REQ 2) | DB intent_event.handling_score_numeric (NULL) | ⬜ sample zeigt NULL |
| `primary_intent` + `confidence` (IL-2 Live-Uebergabe) | Medium-Lane schreibt per-SID VOR Trigger → TAXO3 build_answer_context liest | RAM `_session_state[sid]['state']['primary_intent'/'confidence']` (KEINE DB-Spalte; Live-Handoff) + init-Seed (=None) | ⬜ grep: Write vor _qa_pipeline_dispatch; init seedet beide |
| custom_objection_*-Label | profiles.daten (JSON) → intent_type | DB profiles.daten (JSON, kein DDL) → intent_event.intent_type | ⬜ Test 2 / Test-Anruf (e) |

### Bei Diskrepanz (sample zeigt fehlende taxonomy_version / falschen intent_type / gesetzten handling_score): STOP + writer-Validierung pruefen.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Haiku-Antwort → intent_type (DB) | LLM-Output ist untrusted; muss gegen Taxonomie-Whitelist validiert werden (ASVS V5). |
| EWB-Button (Client) → intent_event (ui_asserted) | Client-getriggert; source-getaggt, confidence=1.0 aber kein Live-Scoring (Buttons luegen). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO1-10 | T (Tampering) | Haiku liefert beliebigen intent_type → ungueltiger Wert in DB | mitigate | is_valid_intent_type-Whitelist im writer (Kern ∪ custom_objection_*); SQLAlchemy-ORM parametrisiert. |
| T-TAXO1-11 | T (Tampering) | Live-Bahn mutiert bestehende intent_event-Zeile (Bau-Regel 1) | mitigate | writer insert-only; grep == 0 UPDATE; Test 5. |
| T-TAXO1-12 | D (DoS) | K4-Fix/Fenster-Schliesser bricht Single-Speaker-Funnel / Sprecher-Detection | mitigate | roles_confirmed/smalltalk_none/sp_map/stabilize_speaker UNVERAENDERT (git diff zeigt an DIESEN Symbolen keine Aenderung); live_session.py-Aenderung = additive get_or_open_moment/close_moment + 3 State-Keys; deepgram-Aenderung = NUR additive close_moment-Aufrufe (Sprecher-Detection nur gelesen); claude-Aenderung = Threshold+abstain-Emit+Fenster-Calls. |
| T-TAXO1-14 | T (Tampering) | Lanes minten je eigene interaction_id → Moment-Fenster zerfaellt (Race) | mitigate | EINZIGE Oeffnen-Stelle get_or_open_moment, check-and-open pro-SID atomar (Aufrufer haelt `_session_state_lock`); Emit-Sites minten nicht (grep uuid4 == 0 in writer/Emit-Sites); Lock-Ordering: `state_lock` + `_session_state_lock` nie gleichzeitig (Gemini-Punkt a, kein Deadlock); Timeout-Backstop faengt verwaiste Fenster. |
| T-TAXO1-15 | D (DoS) | Endlos-Moment (Cold-Call-Schliessbedingung greift nie) → eine interaction_id verschluckt einen ganzen Call | mitigate | Timeout-Backstop MOMENT_WINDOW_TIMEOUT_S (Default 45s, ENV) im Helper schliesst stale Fenster (Gemini-Punkt b); plus Topic-Shift (phase_did_change) + Downgrade-Close. |
| T-TAXO1-13 | D (DoS) | DB-Insert-Fehler im emit crasht Live-Loop | mitigate | emit try-finally + schluckt Fehler (Erkennung laeuft weiter); kein harter Raise im Loop. |
</threat_model>

<verification>
- emit_intent_event: insert-only, Pflichtfelder + taxonomy_version + durchgereichte interaction_id, Validierung; Unit-Test GRUEN.
- Fast+Medium+Button emittieren ueber DIE EINE Funktion; ist_vorwand/EinwandTypEnum geloescht (grep).
- Moment-FENSTER (I-4-FOLD, ereignis-getrieben): get_or_open_moment/close_moment (live_session, lock-frei, per-SID, KEIN line_id) — eine interaction_id pro offenem Fenster, geschlossen bei Berater-Antwort (Meeting)/Topic-Shift (Cold Call)/Timeout/Downgrade; 4 Gemini-Punkte (a Reentrancy lock-frei, b Timeout-Backstop 45s, c Button am offenen Moment, d Downgrade-Reset) gebaut; Single-Speaker-Funnel/roles_confirmed/sp_map/stabilize_speaker unangetastet (git diff). 6 pure-logic Tests + Test-Anruf belegen Buendelung + ereignis-Schliessen.
- K4: ENV-Threshold + reine should_abstain()-Helper + abstain-Emit; Single-Speaker-Funnel unveraendert (git diff). K4-Funnel-Regression-Test (test_k4_threshold_funnel.py) GRUEN (Finding #4).
- H-4: abstain-Emit NUR am echten low-conf-Drop (:1340/:1382), NICHT am :1278 kw_fired-Doppel-Feuer-Skip / :1284 Mutex / :1288 no-sid (grep-Beleg, kein Flut).
- IL-2: Medium-Lane schreibt primary_intent+confidence per-SID in _session_state[sid]['state'] VOR dem Antwort-Trigger (Live-Uebergabe fuer TAXO3 build_answer_context); init_session_state seedet beide (=None).
- M-2: mode-Quelle-Marker (_session_modes) fuer TAXO1-07-Konsolidierung gesetzt (Cross-Wave-Naht).
- Test-Anruf (cold_call+meeting): count intent_event steigt, sample korrekt (intent_type/phase/mode/source/speaker_role/taxonomy_version), handling_score NULL, K3 sauber, qa feuert.
</verification>

<success_criteria>
- Fast + Medium Lane erzeugen echte intent_event-Zeilen (REQ 4); K3/K4 aufgeloest, nicht ueberbaut; K4-Funnel pure-logic Regression-Test GRUEN (Finding #4).
- intent_type = §1-Taxonomie (Kern+Gemini+custom_objection_*); taxonomy_version Pflicht (REQ 5).
- Alte parallele Pfade (ist_vorwand/freier typ/EinwandTypEnum) geloescht (§0.1).
- Live-Bahnen mutieren kein bestehendes intent_event (Bau-Regel 1).
- Moment-FENSTER interaction_id (I-4-FOLD, ereignis-getrieben): EINE id pro Einwand-Fenster (oeffnet bei erstem Kunden-Intent, schliesst bei Berater-Antwort/Topic-Shift/Timeout/Downgrade), geteilt ueber Fast/Medium/Button desselben offenen Fensters; mehrere Einwaende am Stueck = EIN Moment (Multi-Intent-Foundation); KEIN line_id-Key. Foundation fuer Outcome-verankertes Lernen (Phase H), KEIN Lern-/Outcome-Code in TAXO1.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-04-SUMMARY.md` (inkl. sample intent_event verbatim nach Test-Anruf — mit Beleg geteilter interaction_id pro offenem FENSTER vs. neuer id nach Schliess-Event + grep-Belege ist_vorwand/EinwandTypEnum weg + grep get_or_open_moment/close_moment in matcher/claude_service/app_routes/deepgram_service + grep `interaction_id_line`==0 (line_id-Logik weg) + git-diff-Beleg Single-Speaker-Funnel/sp_map/stabilize_speaker unveraendert + Unit-Test-GREEN test_intent_event_writer + test_moment_window (6 Fenster-Tests, I-4-FOLD) + K4-Funnel-Regression-GREEN test_k4_threshold_funnel (Finding #4) + **I-4-FOLD-Beleg** (4 Gemini-Punkte a/b/c/d + die §5-Reconciliation „neues Einwand-Signal vs. ein-Moment" zur André/Cross-AI-Bestaetigung) + **IL-2-Beleg** (primary_intent+confidence per-SID-Write VOR _qa_pipeline_dispatch + init-Seed) + **H-4-Beleg** (abstain-Emit nur an :1340/:1382, kein Emit an :1278/:1284/:1288-Skip — Zeilen-Kontext) + **M-2-Marker** (mode-Quelle-Kommentar fuer TAXO1-07)).
</output>
