# Phase 08.23.2.TAXO1: Verstehen — Fundament + Erkennung — Specification

**Created:** 2026-06-10
**Ambiguity score:** 0.14 (gate: ≤ 0.20)
**Requirements:** 9 locked
**Komplexität:** 🔴 — Schema-weite Migration + Live-Pfad-Umbau + Single-Source-Konsolidierung. Cross-AI **Pflicht** vor Execute.
**Pflicht-Pre-Read:** `Nerve-Vault/04 Entscheidungen/NERVE TAXO-Gerüst (verriegelt).md` (verriegelter Bauplan, Single Source of Truth) · `Nerve-Vault/03 Planung/TAXO Aufräum-Inventur (Verständnis + Scoring).md` (§F Tabellen-Landkarte, §0.1 Putzliste).

## Goal

NERVE bekommt EINE zentrale Ereignis-Tabelle `intent_event` als Single Source of Truth für "was sagt der Kunde gerade", der Live-Erkennungs-Pfad (Fast + Medium Lane) schreibt seinen Output ausschließlich dorthin, das Drei-Bahnen-Gerüst wird als echte Klempnerei verdrahtet, die Single-Source-Putzliste (§0.1) wird komplett auf per-SID-Quellen umgestellt — **TAXO1 erzeugt + erkennt, es benotet NICHT** (Benoten = TAXO2, Antworten = TAXO3).

## Background

Grounded gegen Code + Production (inspect.sh / psql, 2026-06-10):

- **`intent_event` existiert nicht** (0 Code-Treffer) — komplette Neu-Tabelle.
- **6 Call-Analyse-Tabellen auf Prod** (alle Zeilen = Test-Schrott bis EA-Release, kein Trainingswert): `conversation_logs` (36, Rückgrat), `transcript_segments` (196), `calls` (37), `call_events` (52), `objection_events` (8), `ewb_ratings` (0).
- **Lebende Leser, die NICHT brechen dürfen:** `conversation_logs` + `calls` + `call_events` + `transcript_segments` füttern Dashboard, Coaching, Auswertung und den frisch gebauten Transkript-Reiter (D.UX.2). `objection_events` füttert den Dashboard-Einwand-Zähler (dashboard.py), `integration_engine.py`, `admin_views.py` (grep-belegt §F/§G).
- **Erkennung existiert heute schon:** Fast Lane = lokaler Keyword-Matcher (`einwand_keyword_matcher.py`) + EWB-Button; Medium Lane = `analyse_loop` (Haiku Intent + Phase, `claude_service.py`). TAXO1 lenkt deren Output um — kein Neubau der Erkennung.
- **Single-Source-Verletzungen (verifiziert §0.1):** `user_id` (claude_service.py:452 global gelesen, per-SID existiert → Prod 164× Warn), `current_phase` (:1012 write / :1085 read), `cold_call_inference` (:1071 / :1086), `kw_fired_for_line` (REVERSE: matcher:253 write / claude:1265 read) + Cross-Session-Globale (`score_factors_seen`, `last_einwand_typ`, `kaufbereitschaft`, `readiness_*`) + Geister-Felder (`precall_briefing`-Global, `current_phase_name`/`phase_confidence`-Global, anrede-Global) + `org_id` (K8: live_session.py:306 write per-SID / cost_tracker.py:46 global → Kosten-Zuordnung kommt nie an).
- **EWB-Varianten:** `resolve_prompt_version('ewb', user_id)` routet `variants[user_id % 2]`. 2 aktive Varianten (`v1-legacy` 241 Zeichen Einzeiler / `v2-modular` 1086 Zeichen, 4-Baustein-Gerüst). Wegen `user_id=0` (kaputt) hängt JEDER Call auf `v1-legacy`, `v2-modular` lief nie. ENV `PROMPT_EWB_VERSION_OVERRIDE` = Notbremse.
- **Modi:** real existieren nur `cold_call` + `meeting` (deepgram_service `_session_modes`, `conversation_logs.session_mode`). Keine ModeStrategy-Registry — komplett neu.

## Requirements

1. **intent_event-Tabelle (hybrides Schema)**: Neue zentrale Ereignis-Tabelle, indizierte Spalten + JSONB-Payload, alle Pflichtfelder ab Tag 1.
   - Current: Tabelle existiert nicht; Einwände landen in `objection_events`.
   - Target: `public.intent_event` mit indizierten Spalten `event_id`, `session_id`, `call_id`, `mode`, `timestamp`, `intent_type`, `phase`, `handling_score_numeric`, `confidence` + `payload_jsonb` (enthält `source`/`inference_basis`, `taxonomy_version`, `abstained`, `speaker_role`, `speaker_id`, `is_simulation`, `origin_type`, `source_context`, `outcome.resolution`, `resolved_at_event_id`, `superseded_by`, `inference_config_id` — Gerüst §3). Die 4+3 rückwirkend-nicht-rekonstruierbaren Pflichtfelder existieren ab Tag 1 als Spalte/Feld: `handling_score`, `was_correct`, `cue_fired`, `dimension_available`-Map, `reaction_latency_ms`, `cue_visible`/`ui_state_hash`. **Plus eine indizierte Korrelations-Spalte `interaction_id` (UUID, nullable) — die Moment-Klammer** (Cross-AI-Fund Gemini 3.1, Quell-Anker Gerüst §6 / Soll-Verhalten §6): `event_id` ist pro Emit (MEHRERE pro Kundenmoment durch Fast+Medium+Button), `call_id` pro Call — KEINE Klammer pro Kundenmoment. Ohne `interaction_id` ist „welcher ignorierte Vorschlag ↔ welcher Berater-Move ↔ welcher Outcome" später nur per fragilem Zeitstempel-Raten (rückwirkend nicht sauber) rekonstruierbar. Foundation für späteres Outcome-verankertes Lernen (FK-Ziel `suggestion_reactions`, Phase H). TAXO1 legt nur die Spalte an + füllt sie im Cutover (Plan 04) — kein Lern-/Scoring-Code. Owner `nerve_app`. Schild (Punkt 23) gesetzt.
   - Acceptance: `inspect.sh schema intent_event` zeigt die 9 Gerüst-§3-Kern-indizierten Spalten + die Korrelations-Spalte `interaction_id` (UUID, indiziert) = **10 indizierte Spalten gesamt** + `reaction_latency_ms` + `payload_jsonb` (**12 Spalten total**); alle 7 Pflichtfelder vorhanden (als Spalte ODER dokumentiertes JSONB-Feld); `taxonomy_version` ist Pflicht (nicht-null bei jedem Insert); Guard grün (Tabelle + nicht-triviale Spalten beschildert).

2. **handling_score-Spalten ohne Scoring-Logik**: Die Score-tragenden Felder existieren, werden aber in TAXO1 NICHT von einer Benotungs-Logik befüllt.
   - Current: kein handling_score irgendwo.
   - Target: `handling_score_numeric` (Spalte) + `handling_score`/`dimension_available`/`was_correct` (Felder) existieren auf intent_event, bleiben in TAXO1 leer/null oder tragen einen neutralen Platzhalter. Die Befüllung (regel-/marker-basiert + LLM-async) ist TAXO2.
   - Acceptance: Spalten existieren; kein Code-Pfad in TAXO1 berechnet einen handling_score (grep: keine Score-Formel schreibt intent_event.handling_score_numeric).

3. **Drei-Bahnen-Gerüst (Fast / Medium / Slow Lane)**: Geschwindigkeits-Bahnen als echte Klempnerei mit den 2 Bau-Regeln; Slow Lane gebaut, aber ohne Benotung.
   - Current: `analyse_loop` läuft, Audio entkoppelt; keine gekapselte Lane-Struktur, keine Slow-Lane-Queue.
   - Target: Fast Lane (<800ms, lokal, kein LLM) + Medium Lane (Haiku, analyse_loop) erzeugen intent_event. Slow Lane = `queue.Queue` + 1 Daemon-Consumer-Thread, Interface als Adapter gekapselt (Redis-Zukunft), Graceful-Shutdown flusht die Queue in DB (Bau-Regel 2). intent_event ist nach Erzeugung **read-only für Live-Bahnen**; die Slow Lane arbeitet auf einer Kopie und schreibt ein separates Score-Objekt per Event-ID, Merge erst bei Call-Ende (Bau-Regel 1). **Die eigentliche Benotung auf der Slow Lane ist TAXO2 — NICHT in TAXO1.**
   - Acceptance: Slow-Lane-Queue + Consumer-Thread existieren und sind über ein Adapter-Interface gekapselt; ein simulierter Worker-Shutdown flusht offene Queue-Items in DB (Test); Live-Bahnen mutieren kein bestehendes intent_event (Test/grep: kein UPDATE auf intent_event aus Fast/Medium Lane); Slow Lane enthält keine Scoring-Berechnung.

4. **Live-Erkennungs-Cutover auf intent_event**: Der bestehende Erkennungs-Schreibpfad wird vollständig auf intent_event umgelenkt; bekannte Live-Bugs werden dabei aufgelöst statt überbaut.
   - Current: Fast Lane (Keyword/Button) + Medium Lane (Haiku Intent+Phase) schreiben in alte Pfade/`objection_events`; `phase_classify` int-vs-str-Bug (K3) feuert live; `qa_slot1` unterfeuert (K4, nur 3× in 14 Tagen).
   - Target: Stichwort-Treffer + EWB-Button (Fast Lane) UND Haiku-Intent+Phase (Medium Lane) erzeugen echte `intent_event`-Zeilen als Single Source. Der phase_classify-Bug (K3) + das qa_slot1-Unterfeuern (K4) werden im Zuge der Medium-Lane-Umstellung mit-aufgelöst (Ergebnis aus dem Claude-Code-Debug einarbeiten), nicht auf dem kaputten Pfad neu aufgebaut.
   - Acceptance: Live-Test-Call (cold_call + meeting) erzeugt `intent_event`-Zeilen (`inspect.sh count intent_event` steigt, `sample` zeigt korrekte `intent_type`/`phase`/`mode`/`source`); phase_classify wirft den int-vs-str-Fehler nicht mehr (`inspect.sh logs-errors` sauber); qa_slot1/Medium-Lane feuert bei einem echten Einwand (Log-Beleg).

5. **Geteilte Intent-Taxonomie (§1) als Klassifikations-Ziel**: Die EINE Schubladen-Liste wird das `intent_type`-Vokabular auf intent_event.
   - Current: `einwand_typ` als Enum (profile_schema) UND als String/Boolean im Live-Pfad (claude_service.py:966, `ist_vorwand`) — zwei Welten (D2).
   - Target: `intent_type` nutzt die Gerüst-§1-Taxonomie: Kern (`echter_einwand`, `vorwand`, `reflexeinwand`, `kaufsignal`, `aufschub`, `info_frage`) + Gemini-Ergänzungen (`gatekeeper`, `wettbewerber_referenz`, `hard_opt_out`, `commitment`, `meta_kommunikation`) + `custom_objection_*` (pro-Profil überschreibbares Label ohne Schema-Umbau). Pro Aussage EINE primäre Schublade (`primary_intent`); Mehrfach-Intent intern als Array erlaubt. Die Phasen-Achse (§1: Opener/Qualifizierung/.../Abschluss) bleibt vom Intent getrennt klassifiziert, wird aber als Kontext mitgegeben.
   - Acceptance: Die Taxonomie-Werte sind als EINE Quelle definiert (Enum/Konstante/Config), die der Live-Klassifikator schreibt; `intent_event.intent_type` akzeptiert die Kern- + Ergänzungs-Werte + `custom_objection_*`; ein neuer custom-Einwand erfordert keinen Schema-/Code-Umbau (Test).

6. **Modi + Audibility-Contract (§2): Registry + 2 echte Strategien**: Modus als First-Class-Dimension über ein Strategy-Pattern; 2 Modi echt, 3 als zukunftssichere Steckplätze.
   - Current: nur `cold_call` + `meeting` als loses `_session_modes`-Dict; keine Registry, kein Routing-Contract.
   - Target: ModeStrategy-Registry (`setup_audio_routes()`, `get_classification_prompt()`, `extract_intent()`) mit Audibility-Contract als deklarativer Routing-Tabelle. `cold_call` + `meeting` werden als echte Strategy-Klassen ausimplementiert (cold_call: Echo-Detektion aus Berater-Paraphrase, Platzhalter-Pflicht, `unknown`/Abstention erlaubt, Konfidenz modus-gedeckelt). `meeting_ext` + `training_cold` + `training_meeting` nur als Registry-Steckplatz vermerkt (Ausbau = spätere Phasen). **Die Registry-Schnittstelle UND der intent_event-Contract (`mode` + `speaker_role` + `speaker_id`) müssen JETZT so entworfen sein, dass die 3 künftigen Modi ohne Schnittstellen-Umbau hineinpassen** (cold_call = fest `berater`/`local`). Der Training-Air-Gap-Event-Bus wird NICHT gebaut (§6) — nur das Interface aufnahmefähig gehalten.
   - Acceptance: Registry existiert; `cold_call` + `meeting` als registrierte Strategy-Klassen, neuer Modus = neue Klasse ohne Kern-Code-Änderung (Test/Struktur-Beleg); intent_event trägt `mode` + `speaker_role` + `speaker_id` bei jedem Event; ein dokumentierter Steckplatz-Eintrag für die 3 künftigen Modi existiert; kein Training-Air-Gap-Bus im Code.

7. **Zombie-Rename — nur Erkennungs-Tabellen, mit Dual-Write-Brücke**: Nur was `intent_event` wirklich ersetzt wird angefasst; lebende Leser brechen nicht.
   - Current: `objection_events` (8 Zeilen, lebende Leser) + `ewb_ratings` (0 Zeilen) existieren; `conversation_logs`/`calls`/`call_events`/`transcript_segments` lebend.
   - Target: (a) `ewb_ratings` → `zombie_ewb_ratings` (0 Zeilen, sicher; gehört thematisch zur Noten-Engine TAXO2; `[ZOMBIE]`-Schild, NICHT droppen). (b) `objection_events` wird in TAXO1 **NICHT umbenannt**: der Erkennungs-Pfad schreibt `intent_event` als Single Source UND spiegelt übergangsweise weiter nach `objection_events` (Dual-Write-Brücke), damit der Dashboard-Einwand-Zähler + integration_engine + admin_views nicht brechen. Die eigentliche Zombifizierung von `objection_events` + Umzug seiner Leser auf intent_event/rubric_score ist **TAXO2**. (c) `conversation_logs`, `calls`, `call_events`, `transcript_segments` bleiben unangetastet ("lebt").
   - Acceptance: `inspect.sh schema zombie_ewb_ratings` existiert, `ewb_ratings` weg; alter Code, der `ewb_ratings` direkt nutzt, ist umgestellt oder als tot belegt (grep); Dashboard-Einwand-Zähler zeigt nach einem Live-Call weiter korrekte Werte (objection_events wird parallel befüllt); `conversation_logs`/`calls`/`call_events`/`transcript_segments` unverändert + Dashboard/Coaching/Transkript-Reiter funktionieren.

8. **Single-Source-Putzliste komplett gefaltet (§0.1)**: Alle gelisteten Stellen auf EINE per-SID-Quelle, alte globale Pfade gelöscht.
   - Current: 4 kaputte per-SID/global-Stellen + Cross-Session-Globale + Geister-Felder + org_id laufen parallel/falsch (siehe Background).
   - Target: `user_id`, `current_phase`, `cold_call_inference`, `kw_fired_for_line` auf EINE per-SID-Quelle; Cross-Session-Globale (`score_factors_seen`, `last_einwand_typ`, `kaufbereitschaft`, `readiness_*`) per-SID; Geister-Felder (`precall_briefing`-Global-Fallback, `current_phase_name`/`phase_confidence`-Global, anrede-Global-Fallback) aufgelöst (echte einzige Quelle oder weg); `org_id` (K8/COST-ATTRIB) als Teil der Konsolidierung. **Die alten globalen Lese-/Schreib-Pfade werden GELÖSCHT, nicht stehengelassen.** **Rider:** Bei den noten-nahen Werten (`kaufbereitschaft`, `readiness_*`, `score_factors_seen`) macht TAXO1 NUR den WRITE per-SID-konsistent — es designt das Scoring NICHT um; ob sie als per-SID überleben oder von `rubric_score` abgelöst werden, entscheidet TAXO2.
   - Acceptance: grep belegt für jeden Punkt: kein global-read-neben-per-SID-write mehr (alte Globale gelöscht); `inspect.sh logs` zeigt nach Live-Call keine `user_id leer`-Warnung mehr; `org_id` ist im Kosten-Log eines Live-Calls gesetzt (nicht NULL); kein neuer Scoring-Algorithmus in TAXO1 (nur Write-Pfad-Konsolidierung).

9. **EWB-Varianten-Frage aufgelöst — v2-modular als die EINE Variante**: Echtes Routing wiederhergestellt, eine konsistente Variante statt 50/50-Split.
   - Current: `user_id=0` pinnt jeden Call auf `v1-legacy`; `v2-modular` läuft nie; `variants[user_id % 2]` würde nach dem Fix 50/50 splitten.
   - Target: Der `user_id`-Fix (Req 8) stellt echtes Routing her; `v2-modular` wird die EINE aktive Variante für alle (`v1-legacy` → Reserve/deaktiviert, kein 50/50-Split). ENV `PROMPT_EWB_VERSION_OVERRIDE` bleibt Notbremse → reversibel. **Caveat 1 (interim):** v2-modular ist die bessere Brücke, NICHT das Ziel — TAXO3 (Säule 3) ersetzt diesen hardcoded Prompt durch `build_answer_context()`; nicht über-investieren. **Caveat 2 (Live-Test):** v2 lief nie live → 1 Live-Test-Call vor endgültigem Festnageln; prüfen dass die Technik-Schritt-Labels ("ANKER"/"REFRAME") NICHT als Fachsprech in den vorgelesenen Antworten landen (Gerüst §4.5: Technik unsichtbar).
   - Acceptance: nach dem Fix routet ein Live-Call deterministisch auf `v2-modular` (Log: `[EWB] vv2-modular assembled`); kein User bekommt `v1-legacy` ohne ENV-Override; ENV-Override schaltet in <30s zurück (Test); Live-Test bestätigt: Antworten enthalten keine Baustein-Labels als Vokabular.

## Boundaries

**In scope:**
- `intent_event`-Tabelle (hybrides Schema, 9 indizierte Spalten + JSONB, alle 4+3 Pflichtfelder als leere/Platzhalter-Spalten)
- `interaction_id` (UUID, nullable, indiziert) als Moment-Klammer auf `intent_event` (Cross-AI Gemini 3.1); im Cutover EINE ID pro Kundenmoment generiert + an alle Emits (Fast/Medium/Button) desselben Moments durchgereicht. KEIN Lern-/Outcome-Code in TAXO1 — nur die Klammer-Foundation.
- Drei-Bahnen-Gerüst als echte Klempnerei (Fast/Medium emit; Slow Lane Queue+Consumer+Graceful-Flush, gekapselt; Bau-Regeln 1+2)
- Live-Erkennungs-Cutover: Fast + Medium Lane schreiben intent_event als Single Source
- Auflösung K3 (phase_classify int-vs-str) + K4 (qa_slot1-Unterfeuern) im Zuge der Medium-Lane-Umstellung
- Intent-Taxonomie §1 als `intent_type`-Vokabular (inkl. Gemini-Ergänzungen + custom_objection_*); Phasen-Achse getrennt
- ModeStrategy-Registry + cold_call + meeting als echte Klassen; 3 künftige Modi als zukunftssichere Steckplätze
- Zombie-Rename `ewb_ratings` → `zombie_ewb_ratings`
- objection_events Dual-Write-Brücke (intent_event primär + objection_events-Spiegel für lebende Leser)
- Single-Source-Putzliste komplett (4 Kaputte + Cross-Session-Globale + Geister-Felder + org_id) auf per-SID, alte Globale gelöscht
- EWB: v2-modular als die EINE Variante, ENV-Override bleibt
- Schilder (Punkt 23) für intent_event + zombie_ewb_ratings; SCHILD-Guard MANUELL gegen Prod laufen lassen

**Out of scope:**
- **Benotung / Scoring-Engine / BARS / Rubrik / handling_score-Befüllung** — TAXO2 (TAXO1 erzeugt nur die Spalten, befüllt sie nicht)
- **`build_answer_context()` / Säule-3-Antwortversorgung / Slot-B-line_id-Dedup (D3)** — TAXO3
- **Zombifizierung von `objection_events` + Umzug seiner Dashboard-/integration_engine-/admin_views-Leser** — TAXO2 (in TAXO1 nur Dual-Write-Brücke)
- **Konsolidierung von `conversation_logs` / `calls` / `call_events` / `transcript_segments`** — spätere Phase (Note/Aggregate in TAXO2, Ereignis-Strom/Transkript eigene Folge-Phase); in TAXO1 unangetastet
- **Daten-Migration der Test-Schrott-Logs** — intent_event startet leer; alte Logs/Transkripte werden NICHT nach intent_event migriert (kein Trainingswert)
- **Ausbau von `meeting_ext` / `training_cold` / `training_meeting`** — nur Registry-Steckplatz; Training-Air-Gap-Event-Bus NICHT gebaut (§6)
- **FSM-Simulator, komplexes LLM-Live-Scoring, In-Prozess-Bus als Dauerlösung, Coaching-Hub-Oberfläche, ambienter Visual-Kanal (Symbole/Tempo-Regler)** — Post-Launch / spätere Stufen
- **`droppen` der Zombie-Tabellen** — umbenannt ≠ gelöscht; Drop erst nach verifiziertem Neubau
- **Outcome-verankertes Lernen / `suggestion_reactions`-Tabelle / Auswertung der Moment-Klammer** — Phase H. TAXO1 legt nur die `interaction_id`-Spalte an + füllt sie konsistent pro Moment; kein Reaktions-/Outcome-/Lern-Code.

## Constraints

- **Verify = Production only** (`bash deploy.sh production` + `inspect.sh` gegen Prod). KEIN Local-Dev, kein lokales pytest als Acceptance, kein `git pull` auf Prod (tar-deployed). Daten/Schema/Logs via `inspect.sh` gegen Production (`ssh -i ~/.ssh/nerve_vps root@178.104.82.166`).
- **Postgres** ist die Wahrheit; Migrationen als `postgres`-User (nerve_app hat kein CREATE auf schema public); neue public-Tabellen `ALTER TABLE … OWNER TO nerve_app`.
- **Zombie ≠ Drop:** hart umbenennen (`zombie_`-Prefix + `[ZOMBIE]`-Schild), NICHT droppen — Rückhol-Sicherung bis verifizierter Neubau.
- **intent_event read-only für Live-Bahnen** (Bau-Regel 1); Slow Lane auf Kopie + separates Score-Objekt, Merge bei Call-Ende.
- **Graceful-Shutdown flusht die Slow-Lane-Queue in DB** (Bau-Regel 2); 1 Consumer-Thread reicht für EA; Queue als Adapter gekapselt.
- **Dual-Write ist eine bewusste, zeitlich befristete Expand-Contract-Ausnahme** zu §0.1 (intent_event = Single Source; objection_events = abgeleiteter Spiegel mit Sunset in TAXO2), KEINE Dauer-Doppelquelle.
- **Haiku-only im Live-Loop** (Sonnet nur Post-Call); Medium Lane nutzt Haiku.
- **Pflichtfelder ab Tag 1** auf intent_event (sonst Coaching-Hub + Training-Flywheel später unmöglich, §3).
- **SCHILD-Guard (Punkt 23):** intent_event + jede neue/umbenannte Tabelle + nicht-triviale Spalten brauchen ein Schild; Guard automatische Deploy-Blockade inert bis 08.23.2.STAGING → **bei diesen Tabellen-Änderungen MANUELL server-seitig gegen Prod-DSN laufen lassen**.
- **Multi-Segment-ID-Gotcha:** Pfade auf `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/` hartkodieren; `gsd-tools`/`gsd-code-review`/`gsd-verifier` umgehen (würden den falschen Dir lesen); STATE/ROADMAP hand-editieren.
- **Plan-Pflicht-Sektionen:** Punkt 14 (Control-Flow-Audit) + Punkt 21 (Persistenz-Schicht-Verifikation) + Punkt 22 (Verbindungs-Karte, grep+inspect-belegt) bei jedem Code-Insert/Schema-Entscheidung; Cross-AI **Pflicht** (🔴) vor Execute; Real-Daten-Validation (Punkt 13) + Pflicht-grep (Punkt 20).

## Acceptance Criteria

- [ ] `inspect.sh schema intent_event` zeigt die 9 Gerüst-§3-Kern-indizierten Spalten + `interaction_id` (= 10 indiziert gesamt) + `reaction_latency_ms` + `payload_jsonb` (12 Spalten total); alle 7 Pflichtfelder (4+3) als Spalte oder dokumentiertes JSONB-Feld vorhanden; Owner `nerve_app`; `taxonomy_version` bei jedem Insert nicht-null.
- [ ] `intent_event` trägt die indizierte Korrelations-Spalte `interaction_id` (UUID, nullable, Schild gesetzt); im Test-Anruf teilen alle Emits desselben Kundenmoments (Fast-Keyword + Medium-Haiku + Button für dieselbe Aussage/Zeile) EINE `interaction_id`, während Emits verschiedener Momente verschiedene IDs tragen (`inspect.sh sample intent_event` belegt: gleiche `interaction_id` über mehrere `event_id`/`source` einer Aussage).
- [ ] Kein Code-Pfad in TAXO1 berechnet/schreibt einen `handling_score` (Scoring = TAXO2).
- [ ] Slow-Lane-Queue + 1 Daemon-Consumer existieren, über Adapter-Interface gekapselt; simulierter Shutdown flusht offene Items in DB (Test grün).
- [ ] Live-Bahnen führen kein `UPDATE` auf bestehende `intent_event`-Zeilen aus (Bau-Regel 1; grep/Test).
- [ ] Live-Test-Call (cold_call + meeting) erzeugt `intent_event`-Zeilen mit korrektem `intent_type`/`phase`/`mode`/`source`/`speaker_role`/`speaker_id` (`inspect.sh sample`).
- [ ] `phase_classify` wirft den int-vs-str-Fehler nicht mehr (`inspect.sh logs-errors` sauber nach Live-Call); Medium-Lane/qa feuert bei echtem Einwand (Log-Beleg).
- [ ] `intent_type` akzeptiert Kern- + Gemini-Ergänzungs-Werte + `custom_objection_*`; neuer custom-Einwand ohne Schema-/Code-Umbau (Test).
- [ ] ModeStrategy-Registry existiert; `cold_call` + `meeting` als echte Klassen; neuer Modus = neue Klasse ohne Kern-Code-Änderung; dokumentierter Steckplatz für 3 künftige Modi; kein Training-Air-Gap-Bus im Code.
- [ ] `zombie_ewb_ratings` existiert, `ewb_ratings` weg; `[ZOMBIE]`-Schild gesetzt.
- [ ] Dashboard-Einwand-Zähler zeigt nach Live-Call korrekte Werte (objection_events parallel befüllt); `conversation_logs`/`calls`/`call_events`/`transcript_segments` unverändert; Dashboard/Coaching/Transkript-Reiter funktionieren.
- [ ] grep belegt: alte globale Pfade aller §0.1-Punkte gelöscht (kein global-read-neben-per-SID-write); `inspect.sh logs` ohne `user_id leer`-Warnung; `org_id` im Kosten-Log eines Live-Calls gesetzt (≠ NULL).
- [ ] Live-Call routet deterministisch auf `v2-modular` (`[EWB] vv2-modular assembled`); ENV-Override schaltet in <30s zurück; Antworten ohne Baustein-Labels als Vokabular.
- [ ] SCHILD-Guard läuft GRÜN server-seitig (intent_event + zombie_ewb_ratings + neue Spalten beschildert).
- [ ] intent_event startet leer — keine Migration der Test-Schrott-Logs (Beleg: count = 0 vor erstem Live-Call).

## Ambiguity Report

| Dimension          | Score | Min  | Status | Notes                                                            |
|--------------------|-------|------|--------|------------------------------------------------------------------|
| Goal Clarity       | 0.90  | 0.75 | ✓      | TAXO1 = Erzeugen+Erkennen, nicht Benoten; Schema exakt (§3)      |
| Boundary Clarity   | 0.90  | 0.70 | ✓      | Zombie-Umfang, Cutover-Tiefe, Modus-Umfang, Putzliste fixiert    |
| Constraint Clarity | 0.82  | 0.65 | ✓      | Bau-Regeln 1+2, Dual-Write-Ausnahme, Prod-only, SCHILD manuell   |
| Acceptance Criteria| 0.78  | 0.70 | ✓      | 14 pass/fail-Kriterien, gegen Prod verifizierbar                 |
| **Ambiguity**      | 0.14  | ≤0.20| ✓      |                                                                  |

Status: ✓ = met minimum, ⚠ = below minimum (planner treats as assumption)

## Interview Log

| Runde | Perspektive     | Frage                                          | Entscheidung verriegelt                                                                 |
|-------|-----------------|------------------------------------------------|-----------------------------------------------------------------------------------------|
| 1     | Researcher/Boundary | Zombie-Umfang der 6 Call-Analyse-Tabellen? | NUR `objection_events`(Dual-Write-Brücke) + `ewb_ratings`(hart); die 4 anderen bleiben "lebt", Konsolidierung später |
| 1     | Boundary/Simplifier | Tiefe der Live-Umstellung in TAXO1?        | Voll verdrahten (Fast+Medium emit intent_event); Slow-Lane-Klempnerei ja, Benotung=TAXO2; K3/K4 mit-auflösen |
| 1     | Researcher      | EWB-Variante v1-legacy vs v2-modular?          | Erst Code-Diff recherchieren (geliefert: v2 = 4-Baustein-Gerüst vs v1 Einzeiler)        |
| 2     | Seed Closer     | EWB-Default nach Diff?                          | `v2-modular` = die EINE Variante (v1→Reserve); interim bis TAXO3; Live-Test + Label-Check |
| 2     | Boundary Keeper | Modus-Registry-Umfang (5 Modi §2)?             | Registry + nur cold_call+meeting echt; 3 künftige Modi als zukunftssichere Steckplätze; kein Air-Gap-Bus |
| 2     | Boundary Keeper | Putzlisten-Umfang (§0.1)?                       | Komplett wie §0.1; noten-nahe Werte nur WRITE-per-SID, Scoring-Design = TAXO2            |

---

*Phase: 08.23.2.TAXO1-verstehen-fundament-erkennung*
*Spec created: 2026-06-10*
*Next step: /gsd-discuss-phase 08.23.2.TAXO1 — Implementierungs-Entscheidungen (WIE die obigen Anforderungen gebaut werden). Danach Plan + Cross-AI; Execute erst nachdem TAXO1/2/3 alle bis kurz vor Execute gebracht + auf sauberes Ineinandergreifen geprüft sind (Andre-Direktive 2026-06-10).*
