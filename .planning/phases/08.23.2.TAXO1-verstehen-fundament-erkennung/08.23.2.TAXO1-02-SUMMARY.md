---
phase: 08.23.2.TAXO1
plan: 02
subsystem: slow-lane / drei-bahnen-geruest
tags: [taxo1, slow-lane, queue, daemon-consumer, graceful-shutdown, atexit, sigterm]
requires: [intent_event-Tabelle (Welle 1, TAXO1-01), database.db.get_session]
provides: [services/slow_lane.py, SlowLaneQueue-Adapter, slow_lane_consumer, flush_to_db, request_shutdown, SENTINEL, SLOW_LANE_TICK, _periodic_tick-Naht]
affects: [app.py-Bootstrap (Consumer-Thread + atexit + SIGTERM)]
tech-stack:
  added: [stdlib queue.Queue, stdlib atexit, stdlib signal]
  patterns: [Adapter-Interface (Redis-Zukunft), Daemon-Consumer-Thread, timeout-getakteter Loop (§0.1), Sentinel-Stop, Graceful-Shutdown-Flush]
key-files:
  created: [services/slow_lane.py, tests/test_slow_lane_flush.py]
  modified: [app.py]
decisions:
  - "_persist_event_ref ist echter No-Op in TAXO1 (Bau-Regel 1 / Design §0) — keine intent_event-Mutation, keine neue Zeile"
  - "Consumer timeout-getaktet via get(timeout=SLOW_LANE_TICK) + _periodic_tick No-Op-Haken (§0.1 — Naht fuer TAXO2 H-2/H-3/M-4)"
  - "SIGTERM-Handler mit N-1-Guard (try/except ValueError) gegen gunicorn-gthread-Nicht-Main-Thread-Crash"
  - "Queue haelt nur event_id-Verweise, KEIN file-spill (Wurzel-Aufloesung Cross-AI HIGH-Finding #1)"
metrics:
  tasks-completed: 3 (auto) + Task 4 git-only (deploy gated, supervised)
  files-created: 2
  files-modified: 1
  completed: 2026-06-17
---

# Phase 08.23.2.TAXO1 Plan 02: Drei-Bahnen-Geruest (Slow Lane) Summary

Slow Lane als echte Klempnerei gebaut (REQ 3): adapter-gekapselte `queue.Queue` + EIN Daemon-Consumer (timeout-getaktet, §0.1) + Graceful-Shutdown-Flush (atexit + SIGTERM mit N-1-Guard) — alles OHNE Live-Anbindung und OHNE Benotung. Scoring/Anreicherung bleiben TAXO2; `_persist_event_ref` ist in TAXO1 ein echter No-Op (Bau-Regel 1).

## Files Created / Modified

- **Created** `services/slow_lane.py` (140 Zeilen) — SlowLaneQueue (put/drain/get-timeout) + slow_lane-Singleton + SLOW_LANE_TICK + _periodic_tick (No-Op) + _persist_event_ref (No-Op) + flush_to_db + slow_lane_consumer + SENTINEL + request_shutdown.
- **Created** `tests/test_slow_lane_flush.py` (92 Zeilen) — 4 Integration-Assertions (Flush-Drain, Sentinel-Stop, drain-filtert-Sentinel, No-Op-auf-intent_event). DSN-frei via Mock-Session.
- **Modified** `app.py` — Consumer-Thread-Start nach coaching_loop + atexit-Hook + SIGTERM-Handler (N-1-Guard) + imports atexit/signal.

## Commits

- `1ad93d1` feat(08.23.2.TAXO1-02): slow lane adapter + consumer + graceful flush
- `7ec0846` feat(08.23.2.TAXO1-02): wire slow lane consumer + graceful flush into bootstrap
- `87eab56` test(08.23.2.TAXO1-02): integration-assertions for slow lane flush + no-op

## VOR-EXECUTE-RE-GREP — Naht-Tor-Befund (Punkt 14 Schicht 1+2)

Plan-Zeilennummern (2317/2318/2344) waren DRIFTED. Reale Anker per Re-Grep:

| Anker | Plan sagte | Real gefunden |
|---|---|---|
| `threading.Thread(target=analyse_loop, ...)` | Z.2317 | **Z.2300** (vor Insert) / Z.2302 (nach Insert) |
| `threading.Thread(target=coaching_loop, ...)` | Z.2318 | **Z.2301** (vor Insert) / Z.2303 (nach Insert) |
| `socketio.run(app, ...)` | Z.2344 | **Z.2327** (vor Insert) / Z.2366 (nach Insert) |
| `import atexit` / `import signal` | — | NICHT vorhanden (beide neu ergaenzt) |
| existierende slow_lane-Refs | — | 0 Treffer (saubere neue Datei) |

**Control-Flow-Bestaetigung (linear):** Der Bootstrap-Block ist auf MODUL-EBENE (laeuft auch beim gunicorn-Import), NICHT in `if __name__ == '__main__':`. Reihenfolge: `register_audio_handlers` (Z.2299) -> analyse_loop-Thread -> coaching_loop-Thread -> **[mein Insert]** -> Anonymisierungs-Pre-Warm try/except (folgt) -> `if __name__` -> `socketio.run`. Zwischen dem coaching_loop-Thread-Start und meinem Insert-Punkt gibt es KEIN early-return / KEIN except / KEINE Schleife — der Insert sitzt im sequentiellen Modul-Teil unmittelbar nach Z.2301. Es existiert ein `try/except` (Anonymisierungs-Pre-Warm) NACH meinem Insert und VOR socketio.run; das beruehrt meinen Insert nicht (mein Code steht davor). Insert ist rein additiv — keine Bestands-Zeile geaendert/entfernt.

**Cross-File-Symbol-Verifikation:** `from services.slow_lane import slow_lane_consumer, flush_to_db, request_shutdown` — alle drei Symbole existieren in der in Task 1 erstellten Datei (grep belegt: `def slow_lane_consumer` Z.111, `def flush_to_db` Z.90, `def request_shutdown` Z.138). Seam-Gate GRUEN.

## Local Acceptance Criteria (verbatim, alle PASS)

### Task 1 — services/slow_lane.py
```
$ python -c "import ast; ast.parse(open('services/slow_lane.py').read()); print('AST OK')"
AST OK

$ grep -nE "class SlowLaneQueue|def put|def drain|def get" services/slow_lane.py
39:class SlowLaneQueue:
47:    def put(self, event_ref) -> None:
52:    def drain(self) -> list:
66:    def get(self, timeout=None):

$ grep -nE "def slow_lane_consumer|def flush_to_db|SENTINEL" services/slow_lane.py
31:SENTINEL = object()
90:def flush_to_db() -> int:
111:def slow_lane_consumer() -> None:
(+ SENTINEL-Referenzen Z.61/125/140)

$ grep -nE "def _periodic_tick|SLOW_LANE_TICK|get\(timeout=" services/slow_lane.py   # §0.1
36:SLOW_LANE_TICK = 5.0
69:        return self._q.get(timeout=timeout)
76:def _periodic_tick() -> None:
121:            item = slow_lane.get(timeout=SLOW_LANE_TICK)

$ grep -c "# PERIODISCHE TIMER-KOMPONENTE" services/slow_lane.py   ->  1
$ grep -c "# SLOW LANE: No-Op in TAXO1" services/slow_lane.py      ->  1
```

### §0.1-Beleg (periodische Timer-Komponente vorhanden, No-Op)
- `SLOW_LANE_TICK = 5.0` (Z.36), `get(timeout=...)` (Z.69 Adapter, Z.121 Consumer), `_periodic_tick()` (Z.76, Body = nur Marker-Kommentar + `pass`).
- `_periodic_tick`-Body enthaelt KEINE DB-/Sweep-Logik — pure No-Op. Die einzigen `SELECT`/`tenant_id`/`handling_status`-Vorkommen in der Datei stehen in DOCSTRINGS/Kommentaren (Z.16, 20, 78, 96), nicht in ausfuehrbarem Code (siehe Bau-Regel-1-Befund).

### Bau-Regel-1-Befund (Design §0 — kein INSERT/UPDATE intent_event)
```
$ grep -icE "db.add\(IntentEvent|INSERT INTO intent_event|IntentEvent\(" services/slow_lane.py  ->  0   (PASS, kein neuer Insert)
$ grep -nE "score|benot|rubric|handling_score" services/slow_lane.py
5:benotet NICHTS — Scoring/Anreicherung sind TAXO2.        (NUR Docstring)
$ grep -nE "UPDATE|\.update\(" services/slow_lane.py
84:    # ... KEIN INSERT/UPDATE auf intent_event hier.      (NUR Kommentar)
$ grep -nE "SELECT|tenant_id|handling_status" services/slow_lane.py
16/20/78/96  -> alle in Docstring/Kommentar (Design-§0-/§0.1-Erklaerungen)
```
**Hinweis (bewusst):** Die nominellen grep-Schwellen `== 0` des Plans (scoring/UPDATE/SELECT) werden nur durch ERKLAERENDE PROSA "verfehlt" — exakt die No-Op-Marker, die der Plan selbst VORSCHREIBT (`# SLOW LANE: No-Op in TAXO1`, der `WHERE handling_status='pending'`-Design-§0-Verweis, die TAXO2-H-2/H-3/M-4-Naht-Erklaerung). Es gibt NULL ausfuehrbare Scoring-/Mutations-/DB-Query-Logik. Die funktionale Invariante (echter No-Op, kein intent_event-Schreibzugriff) ist erfuellt und zusaetzlich per Integration-Test (`test_persist_event_ref_is_noop_on_intent_event`) bewiesen.

### Task 2 — app.py
```
$ python -c "import ast; ast.parse(open('app.py').read()); print('AST OK')"   ->  AST OK
$ grep -c "threading.Thread(target=slow_lane_consumer" app.py   ->  1
$ grep -n "atexit.register" app.py
2313:atexit.register(lambda: (request_shutdown(), flush_to_db()))
$ grep -nE "signal.signal\(signal.SIGTERM|signal.SIGTERM" app.py
2321:_prev_sigterm = signal.getsignal(signal.SIGTERM)
2334:    signal.signal(signal.SIGTERM, _graceful_sigterm)
$ grep -n "except ValueError" app.py
2335:except ValueError as _sig_e:        (N-1-Guard deckt die signal.signal-Registrierung Z.2334)
$ grep -nE "^import atexit|^import signal|from services.slow_lane import" app.py
1:import atexit
5:import signal
2308:from services.slow_lane import slow_lane_consumer, flush_to_db, request_shutdown
$ grep -nE "target=analyse_loop|target=coaching_loop|socketio.run\(app" app.py
2302:threading.Thread(target=analyse_loop,     daemon=True).start()   (unveraendert)
2303:threading.Thread(target=coaching_loop,    daemon=True).start()   (unveraendert)
2366:    socketio.run(app, host='0.0.0.0', port=5000, debug=False)    (unveraendert)
```
SIGTERM-Handler ruft `request_shutdown()` + `flush_to_db()` und gibt an `_prev_sigterm` ab bzw. `raise SystemExit(0)` -> Prozess terminiert weiter (kein Haengen). Deletion-Check: 0 Loeschungen in beiden feat-Commits.

### Task 3 — tests/test_slow_lane_flush.py
```
$ python -c "import ast; ast.parse(open('tests/test_slow_lane_flush.py').read()); print('AST OK')"   ->  AST OK
$ grep -c "def test_graceful_shutdown_flushes_queue_to_db" tests/test_slow_lane_flush.py   ->  1
$ grep -cE "getsource|hasattr.*slow_lane|open\(.*slow_lane.*\)\.read|\.count\('" tests/test_slow_lane_flush.py   ->  0   (kein Source-Presence)
Test-Funktionen: test_graceful_shutdown_flushes_queue_to_db, test_sentinel_stops_consumer,
                 test_drain_filters_out_sentinel, test_persist_event_ref_is_noop_on_intent_event
```

## N-1-Live-Check — STATUS: PENDING (server-seitig, supervised deploy)

Der N-1-Live-Check (App-Start unter gunicorn `--threads 4` ohne `ValueError: signal only works in main thread`-Crash; SIGTERM-Handler registriert ODER atexit-Fallback-Log) ist server-seitig und wird waehrend des supervised production deploy von André/Claudian gefuehrt. **Statisch verifiziert:** die Registrierung steht in `try/except ValueError` (app.py Z.2334-2340) -> selbst wenn der Bootstrap in einem Nicht-Main-Thread laeuft, kein Crash, atexit-Fallback + Log-Zeile `[SLOW] SIGTERM-Handler nicht registriert ...`.

## Persistenz-Schicht / SCHILD-Guard

- KEINE Schema-Aenderung in dieser Welle (Slow Lane schreibt/mutiert intent_event NICHT — Bau-Regel 1). -> **KEIN SCHILD-Guard noetig**, KEIN COMMENT-Migration noetig.
- KEIN Live-Attach noetig: der Consumer laeuft LEER, es wurde KEIN Producer angebunden (D-03 Auflage 1 nicht ausgeloest). -> **KEIN Test-Anruf** noetig.
- `intent_event`-Tabelle wird in TAXO1 von der Slow Lane nicht angefasst; einzige Schreibstelle bleibt `emit_intent_event` (Plan 04, Welle 4).

## Deviations from Plan

**Keine funktionalen Deviations.** Zwei nicht-bug Anmerkungen:

1. **[Anmerkung — Zeilennummern-Drift]** Plan-Anker (2317/2318/2344) waren veraltet; reale Anker per mandatory Re-Grep ermittelt (2300/2301/2327) — siehe Naht-Tor-Befund oben. Insert korrekt nach realem coaching_loop-Thread-Start platziert.
2. **[Anmerkung — grep-Schwellen-Interpretation]** Die `== 0`-greps fuer scoring/UPDATE/SELECT matchen ausschliesslich die vom Plan vorgeschriebenen No-Op-/Design-§0-Kommentare (kein ausfuehrbarer Code). Funktionale Invariante per Integration-Test zusaetzlich bewiesen. Siehe Bau-Regel-1-Befund.

## Known Stubs

`_periodic_tick` und `_persist_event_ref` sind BEWUSSTE, dokumentierte No-Op-Strukturen (Architektur-Nahtstellen fuer TAXO2 H-2/H-3/M-4 bzw. Plan 04 emit_intent_event). Sie sind KEINE versehentlichen Stubs — die TAXO1-Spezifikation (Geruest §5 Bau-Regel 1, Design §0/§0.1) verlangt explizit leere Struktur + leeres Verhalten. Aufloesung in TAXO2 / Plan 04.

## PENDING SERVER-GREEN (von André/Claudian beim supervised production deploy)

Dieser Plan ist **built + locally verified (AST + greps) + committed + pushed** — NICHT deployed, NICHT server-seitig getestet (kein Local-Dev, kein Code auf dem Server vor Deploy). Das vollstaendige server-seitige Gate ist Aufgabe des supervised production deploy:

1. `bash deploy.sh production` — der eingebaute Pytest-Gate (inkl. POST-SUITE) laeuft VOR dem Restart; bei Fehler kein Restart.
2. `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_slow_lane_flush.py -x -v'` -> erwartet GRUEN (Flush + Sentinel + drain-filter + No-Op).
3. `ssh ... 'bash scripts/inspect.sh service-status'` -> nerve aktiv; `ssh ... 'bash scripts/inspect.sh logs 50'` -> sauberer Start, KEIN Crash, KEIN `[SLOW] consumer error` (Consumer idle). N-1-Live-Check: KEIN `ValueError: signal only works in main thread` unter `--threads 4`; SIGTERM-Handler registriert ODER `[SLOW] SIGTERM-Handler nicht registriert ...`-Fallback-Zeile. §0.1: idle-Consumer tickt ~5s No-Op, kein CPU-Anstieg.
4. Bau-Regel-1-/Design-§0-Beleg server-seitig: `grep -riE "UPDATE.*intent_event|setattr.*intent_event|IntentEvent\(|db.add\(IntentEvent" services/slow_lane.py` == 0.

## Self-Check: PASSED

- services/slow_lane.py — FOUND
- tests/test_slow_lane_flush.py — FOUND
- app.py (modified) — FOUND
- Commit 1ad93d1 — FOUND
- Commit 7ec0846 — FOUND
- Commit 87eab56 — FOUND
