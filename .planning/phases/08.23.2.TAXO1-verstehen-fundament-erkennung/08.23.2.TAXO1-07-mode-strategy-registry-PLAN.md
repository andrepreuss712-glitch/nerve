---
phase: 08.23.2.TAXO1
plan: 07
type: execute
wave: 7
complexity: 🔴
depends_on: [04]
files_modified: [services/mode_strategy.py, services/claude_service.py, services/deepgram_service.py, services/live_session.py, services/einwand_keyword_matcher.py, tests/test_mode_registry.py]
autonomous: false
requirements: [6]
risiko: riskant
test_anruf: true
user_setup: []
must_haves:
  truths:
    - "services/mode_strategy.py definiert ModeStrategy(ABC) mit setup_audio_routes() + extract_intent() (KEIN get_classification_prompt — SYSTEM_PROMPT_BASE bleibt der EINE Klassifikations-Prompt, MEDFIX 18.06.) + MODE_REGISTRY + register()."
    - "cold_call + meeting sind echte registrierte Strategy-Klassen; ein neuer Modus = neue Klasse ohne Kern-Code-Aenderung (Struktur-/Runtime-Beleg)."
    - "meeting_ext + training_cold + training_meeting sind dokumentierte Registry-Steckplaetze (NotImplementedError); KEIN Training-Air-Gap-Bus, KEINE event_bus/sim.internal/audio.*-Topics im Code."
    - "intent_event traegt mode + speaker_role + speaker_id bei JEDEM Event. ColdCallStrategy.extract_intent liefert speaker_role='berater', speaker_id='local', inference_basis='advisor_paraphrase' (Sprecher-Bug behoben: cold_call ist NICHT 'kunde'). MeetingStrategy liefert speaker_role aus der Sprecher-Trennung."
    - "Der EWB-Button schreibt im cold_call EINE markierte conversation_log-Transcript-Zeile (speaker=Kunde-Label INT 1, text=Knopf-Kurztext, data-Marker {'ewb_button':True}, anonymisiert) — die EINZIGE Kunde-Zeile im Cold-Call-Transcript; alle gesprochenen Cold-Call-Zeilen bleiben Berater. Der Marker round-trippt RAM->DB->/api/transcript."
    - "Modus-Quelle (cold_call/meeting-Achse) ist EINHEITLICH per-SID (_session_state[sid]['mode']), geschrieben NUR bei Call-Start (+ zukuenftigem Consent-Rueckfall, falls gebaut), gelesen vom Register. Das globale _session_modes-Dict (deepgram:18/437/445) ist GELOESCHT; alle 4 Lese-Stellen lesen per-SID (grep-Beleg: grep -rn '_session_modes' services/ == 0). Default einheitlich 'cold_call'. Die Tuersteher/Zielperson-Achse (state['current_mode']/contact_category) ist SEPARAT und UNVERAENDERT."
  artifacts:
    - path: "services/mode_strategy.py"
      provides: "ModeStrategy(ABC, setup_audio_routes/extract_intent) + ColdCallStrategy + MeetingStrategy + MODE_REGISTRY + register() + 3 Steckplaetze"
      contains: "class ModeStrategy"
    - path: "services/claude_service.py"
      provides: "Medium-Lane + A/B-Lane nutzen MODE_REGISTRY[mode].extract_intent() fuer speaker_role/speaker_id/inference_basis; mode-Read per-SID"
      contains: "MODE_REGISTRY"
    - path: "services/deepgram_service.py"
      provides: "_session_modes-Dict geloescht; Button-mode-Read + alle Lese-Stellen per-SID; EWB-Button schreibt markierte conversation_log-Zeile (Kunde-Label INT 1 + ewb_button-Marker, anonymisiert)"
      contains: "_session_state"
    - path: "services/einwand_keyword_matcher.py"
      provides: "Fast-Lane nutzt MODE_REGISTRY[mode].extract_intent(); mode-Read per-SID"
      contains: "MODE_REGISTRY"
    - path: "tests/test_mode_registry.py"
      provides: "Struktur-/Runtime-Test: neuer Modus = neue Klasse ohne Kern-Aenderung; cold_call/meeting registriert; cold_call=berater/local; Steckplaetze NotImplemented"
      contains: "def test_"
  key_links:
    - from: "per-SID mode (_session_state[sid]['mode'], live_session.py:313) — die EINE cold_call/meeting-Quelle, geschrieben bei Call-Start (+ ggf. Consent-Rueckfall)"
      to: "MODE_REGISTRY[mode] -> Strategy"
      via: "Registry-Lookup (MODE_REGISTRY.get(mode) or 'cold_call'-Default)"
      pattern: "MODE_REGISTRY\\["
    - from: "ColdCallStrategy.extract_intent"
      to: "intent_event (speaker_role='berater', speaker_id='local', inference_basis='advisor_paraphrase')"
      via: "emit_intent_event (Welle 4, intent_event_writer.py:32)"
      pattern: "'berater'"
    - from: "EWB-Button handle_manual_ewb (deepgram:813)"
      to: "conversation_log-Transcript-Zeile (speaker=Kunde-Label INT 1, text=Knopf-Kurztext, data={'ewb_button':True}, anonymisiert) -> DB -> /api/transcript-Export"
      via: "ls.conversation_log.append unter ls.log_lock (spiegelt deepgram:130-135), ZUSAETZLICH zum bestehenden intent_event-Emit"
      pattern: "ewb_button"
    - from: "_session_modes (global dict, deepgram_service.py:18 — GELOESCHT)"
      to: "per-SID mode (_session_state[sid]['mode'])"
      via: "§0.1-Konsolidierung (A4=YES, doppelte lebende Quelle bewiesen); Gatekeeper-Achse state['current_mode'] bleibt SEPARAT"
      pattern: "_session_modes"
---

> RISIKO-BANNER: **riskant.** Aendert das Modus-Routing (Registry statt loses globales `_session_modes`-Dict), behebt den Sprecher-Zuordnungs-Bug (cold_call schrieb faelschlich `speaker_role='kunde'`), faedelt mode/speaker durch alle 4 Cutover-Emit-Stellen, loescht ein globales Cross-Session-Dict (§0.1), und laesst den EWB-Button eine markierte Transcript-Zeile schreiben (Refinement 1). → **Test-Anruf PFLICHT (beide Modi cold_call + meeting; intent_event traegt korrekten mode/speaker_role/speaker_id; EWB-Button erzeugt genau EINE markierte Kunde-Transcript-Zeile).**

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Dieser Plan wird GEPLANT, nicht sofort ausgefuehrt. Deploy + Test-Anruf erst NACH Cross-AI-Plan-Review (Gemini, PFLICHT bei 🔴) + Andre-Ineinandergreifen-Check (TAXO1/2/3-Pläne nebeneinander, gemeinsamer Klebstoff = intent_event-Schema). KEIN Sprung zu Execute.

> 🔎 **VOR-EXECUTE-RE-GREP (Pflicht beim Bau, Punkt 20+22):** Vor dem ersten Edit erneut greppen — der Code aendert sich zwischen Plan und Execute:
> ```
> grep -rn "_session_modes" services/
> grep -rn "_session_state\[.*\].get('mode'\|\.get('mode'" services/
> grep -rn "speaker_role='kunde'\|speaker_role=\"kunde\"" services/
> grep -n "SYSTEM_PROMPT_BASE" services/claude_service.py
> grep -rn "conversation_log.append\|'type': 'transcript'" services/deepgram_service.py services/live_session.py
> grep -rn "current_mode\|contact_category" services/deepgram_service.py
> grep -rin "consent\|verweigerung\|moment_opened_mode" services/live_session.py services/deepgram_service.py   # Refinement 2: Consent-Rueckfall-mode-Write suchen — falls EINER existiert, auf per-SID migrieren
> grep -rn "speaker" routes/ templates/ static/ | grep -i "transcript\|pip\|launcher"                          # Refinement 1: Reader-Speaker-Mapping (INT 1 vs String 'Kunde') verifizieren
> ```
> Erwartung beim Schreiben dieses Plans (HEAD 6e98781, verifiziert): `_session_modes` lebt an 9 Stellen (def:18, write:437, pop:445, read deepgram:833 / claude:974 / claude:1520 / matcher:280). `SYSTEM_PROMPT_BASE` ist der EINE Prompt (claude:32, used :523). Kanonischer Transcript-Append: deepgram:130-135 (`'speaker': log_sp` = INT 0/1/None). `current_mode`/`contact_category` lebt NUR in handle_manual_mode_toggle (Gatekeeper-Achse). **KEIN** Laufzeit-Consent-Rueckfall-mode-Write (orchestrator-verifiziert; live_session:471 ist ein Moment-Fenster-Reset-Kommentar, kein mode-Flip). Wenn die Realitaet beim Execute abweicht → STOP, Plan-Anker korrigieren.

<objective>
Modus als First-Class-Dimension ueber ein Strategy-Pattern bauen (REQ 6): eine `ModeStrategy`-Registry mit `cold_call` + `meeting` als echten Klassen und `meeting_ext`/`training_cold`/`training_meeting` als zukunftssicheren Steckplaetzen (NotImplementedError). Die Registry ist die deklarative Routing-/Audibility-Tabelle; ein neuer Modus = neue Klasse registrieren, der Kern-Code aendert sich nicht.

**Diese Re-Planung begradigt die Drift seit Welle 4 (13.06.) + MEDFIX (18.06.) + faltet zwei Andre-Refinements ein (18.06.):**
- Die ABC besitzt **KEINEN** `get_classification_prompt()` — der MEDFIX hat `SYSTEM_PROMPT_BASE` (claude_service.py:32, used :523) zur SOLE Klassifikations-Quelle gemacht. Ein per-Modus-Prompt wuerde den MEDFIX re-brechen (intent_event wieder leer). Die ModeStrategy regiert NUR `extract_intent` (speaker_role/speaker_id/inference_basis/confidence-Cap) + den dokumentierten Audibility-Contract (`setup_audio_routes`).
- **Sprecher-Bug behoben (Bau-Pflicht, Decision 2):** Heute schreiben ALLE 4 Emit-Stellen `speaker_role='kunde'` — auch fuer Berater-Paraphrase im cold_call (Daten-Vergiftung). `ColdCallStrategy.extract_intent` liefert kuenftig `speaker_role='berater'`, `speaker_id='local'`, `inference_basis='advisor_paraphrase'`. Die EINZIGE Kunde-Ausnahme bleibt der EWB-Button (`source='ui_asserted'`).
- **REFINEMENT 1 (Andre 18.06.) — EWB-Button schreibt eine sichtbare, markierte Transcript-Zeile:** Der `handle_manual_ewb`-Handler (deepgram:813) schreibt ZUSAETZLICH zum bestehenden intent_event-Emit (:847-853, BLEIBT unveraendert) EINE conversation_log-Transcript-Zeile (speaker=Kunde-Label, text=Knopf-Kurztext, data-Marker, anonymisiert), damit der Knopf-Einwand beim Export/Lesen sichtbar UND als Button-Einwand markiert ist. Das ist die EINZIGE Kunde-Zeile im Cold-Call-Transcript; gesprochene Cold-Call-Zeilen bleiben Berater (D2-Hauptregel unveraendert).
- **REFINEMENT 2 (Andre 18.06.) — Modus-Quelle praezisiert, KEIN Live-Umschalter:** Es gibt KEINEN Live-Toggle cold_call↔meeting (keinen bauen). Der cold_call/meeting-Modus wird beim Call-Start explizit gesetzt (Default cold_call, D4) und — falls/sobald gebaut — NUR durch einen Consent-Rueckfall geaendert (der HEUTE im Code NICHT existiert, Punkt 22). Die D3-Konsolidierung betrifft NUR die cold_call/meeting-Achse (`_session_state[sid]['mode']`). Die Tuersteher/Zielperson-Achse (`state['current_mode']`/`contact_category` aus `handle_manual_mode_toggle`) ist SEPARAT/ORTHOGONAL — NICHT vermischen, NICHT migrieren, NICHT loeschen. Namens-Ueberlappung ('cold_call' in beiden Achsen) als Caveat dokumentiert.
- **§0.1-Konsolidierung (A4=YES, bewiesen):** Das globale `_session_modes`-Dict lebt PARALLEL zur per-SID-Quelle `_session_state[sid]['mode']` (Split-Brain belegt: claude:974 liest GLOBAL, claude:1152/1224 liest PER-SID — gleiche Datei, zwei Quellen). Globales Dict wird GELOESCHT, alle Leser auf per-SID.
- **Einheitlicher Default (Decision 4):** `init_session_state` default `'meeting'` → `'cold_call'` (sicherere Annahme); mode wird ohnehin explizit bei Call-Start gesetzt (deepgram:527 → :643).

Purpose: Geruest §2 (Audibility-Contract als deklarative Tabelle). Solides Fundament fuer TAXO2/3 — Drift begradigen, NICHT einzementieren.

Output: services/mode_strategy.py (Registry + 2 Klassen + 3 Steckplaetze), Verdrahtung der 4 Emit-Stellen (claude_service Medium + A/B, deepgram Button, matcher Fast), EWB-Button-Transcript-Zeile (Refinement 1), §0.1-Konsolidierung (_session_modes geloescht, cold_call/meeting-Achse only — Refinement 2), Default-Vereinheitlichung, tests/test_mode_registry.py. Deploy + Test-Anruf — GATED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-RESEARCH.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-SPEC.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-04-SUMMARY.md
@.planning/phases/08.23.2.TAXO1.MEDFIX-medium-lane-intent-event-fix/08.23.2.TAXO1.MEDFIX-01-SUMMARY.md
@services/intent_event_writer.py
@services/deepgram_service.py
@services/live_session.py
@services/claude_service.py
@services/einwand_keyword_matcher.py

<interfaces>
<!-- ALLE Anker frisch gegen HEAD 6e98781 verifiziert. Executor MUSS vor dem Edit re-greppen (Banner). -->

<!-- emit_intent_event-Contract (Welle 4, intent_event_writer.py:32) — die Strategy fuettert DAS: -->
<!--   emit_intent_event(*, session_id, mode, intent_type, source, speaker_role, speaker_id, -->
<!--     phase=None, confidence=None, user_id=None, org_id=None, call_id=None, interaction_id=None, -->
<!--     inference_basis=None, abstained=False, ...) -> int -->
<!--   Pflicht non-empty (sonst ValueError): session_id, mode, source, speaker_role. -->
<!--   intent_type wird gegen intent_taxonomy.is_valid_intent_type validiert. -->
<!--   payload_jsonb fuehrt: source, inference_basis, taxonomy_version, abstained, speaker_role, speaker_id. -->

<!-- DER EINE Klassifikations-Prompt (MEDFIX 18.06., NICHT per-Modus): -->
<!--   claude_service.py:32  SYSTEM_PROMPT_BASE = """...""" -->
<!--   claude_service.py:523 _system_prompt = SYSTEM_PROMPT_BASE  (in analysiere_mit_claude) -->
<!--   => ModeStrategy hat KEIN get_classification_prompt(). -->

<!-- per-SID mode (die SURVIVING single source der cold_call/meeting-Achse) — live_session.py: -->
<!--   :300 def init_session_state(... mode: str = 'meeting')  <-- DECISION 4: -> 'cold_call' -->
<!--   :313 'mode': mode  (gesetzt bei jedem Call-Start) -->
<!--   ALREADY READ per-SID (beweist A4=YES): claude:1152 (ls._session_state.get(sid) or {}).get('mode','meeting') ; claude:1224 _sid_cc_st.get('mode','meeting') -->
<!--   Call-Start-Plumbing: deepgram:524-527 mode=data.get('mode','meeting') -> :562 _open_deepgram_connection(mode) -> :636/:643 init_session_state(mode=mode) -->
<!--   REFINEMENT 2: dies ist die EINZIGE Schreib-Stelle der cold_call/meeting-Achse (Call-Start). Kein Live-Toggle. -->

<!-- Global _session_modes (A4 = LOESCHEN) — deepgram_service.py: -->
<!--   :18  _session_modes = {}  ;  :437 _session_modes[sid]=mode  ;  :445 _session_modes.pop(sid,None) -->
<!--   READ-Stellen (alle -> per-SID umstellen): deepgram:833 (Button, default 'cold_call'), -->
<!--     claude:974 (Medium, default 'meeting'), claude:1520 (A/B-abstain, default 'meeting'), matcher:280 (default 'cold_call') -->
<!--   Migrations-Marker im Code: claude:971-972 "M-2/TAXO1-07: ... bei _session_modes-Konsolidierung (W7) DIESEN Read mit-migrieren" -->

<!-- 4 intent_event-Emit-Stellen (Decision 2 — alle schreiben HEUTE speaker_role='kunde'): -->
<!--   1. Medium-Lane: claude:1011-1026 (mode read :974). cold_call-Zweig :1011-1013 setzt FAELSCHLICH 'kunde'/'local'/advisor_paraphrase. source='llm_inferred'. -->
<!--   2. A/B-abstain-Lane: claude:1534-1543 (mode read :1520). speaker_role='kunde' hardcoded :1541; inference_basis schon conditional advisor_paraphrase/direct_customer_utterance. source='llm_inferred'. -->
<!--   3. EWB-Button (DIE erlaubte Kunde-Ausnahme): deepgram:847-853 (mode read :833). source='ui_asserted', inference_basis='ui_button', speaker_role='kunde'. -->
<!--   4. Keyword-Matcher (Fast): matcher:297-302 (mode read :280). speaker_role='kunde','local' hardcoded; source='llm_inferred', inference_basis='advisor_label'. -->

<!-- Cold-Call-TRANSCRIPT-Pfad (schon Berater-korrekt, NICHT regressieren): -->
<!--   deepgram:79-84 cold_call -> sp_label='Berater', emit_speaker=0, log_sp=0, roles_confirmed=True. -->
<!--   deepgram:185-190 cold_call -> sp_name='Berater'. sp_map/Sprecher-Trennung lebt in live_session (meeting) — NICHT anfassen ausser mode-Read-Quelle. -->

<!-- KANONISCHER conversation_log-APPEND (die Struktur, die die EWB-Button-Zeile spiegeln MUSS) — deepgram:130-135: -->
<!--   with ls.log_lock: ls.conversation_log.append({'ts': ts, 'type': 'transcript', -->
<!--     'speaker': log_sp if roles_confirmed else None, 'text': _anon_text, 'data': None}) -->
<!--   speaker ist ein INT-Label (0=Berater / 1=Kunde / None) — KEIN String 'Kunde'. -->
<!--   => Fuer die Kunde-Button-Zeile MUSS speaker=1 (INT) gesetzt werden (so wie der gesprochene Pfad), NICHT der String 'Kunde'. -->
<!--   Der Reader (D.UX.2 pip-launcher) mappt INT 0/1/None -> berater/kunde/system. -->
<!--   EXECUTOR-PFLICHT (VOR-EXECUTE-RE-GREP): den /api/transcript-Reader + pip-launcher-Speaker-Mapping greppen -->
<!--   und die EXAKTE Encoding (INT 1 vs String) verifizieren — NICHT raten. Wenn der Reader String erwartet -> anpassen. -->

<!-- REFINEMENT 1 — EWB-Button schreibt JETZT eine sichtbare, markierte Transcript-Zeile (Andre-Entscheid): -->
<!--   handle_manual_ewb (deepgram:813) schreibt ZUSAETZLICH zum bestehenden intent_event-Emit (:847-853, BLEIBT) -->
<!--   EINE conversation_log-Zeile: speaker=Kunde-Label INT 1, text=Knopf-Kurztext (`typ`, data.get('text') :819), -->
<!--   mit Marker (data-Flag {'ewb_button': True}), anonymisiert wie der gesprochene Pfad (:114-135). -->
<!--   `typ` ist ein Typ-LABEL ('zu_teuer') — das ist OK: es ist der Knopf-Text, den Andre im Export sehen will. -->
<!--   DIES ist die EINZIGE 'Kunde'-Zeile im Cold-Call-Transcript; alle gesprochenen Cold-Call-Zeilen bleiben 'Berater'. -->
<!--   Anonymisierung: `from services.anonymization import anonymize` (anonymization.py:534, returns (text, tier)); -->
<!--     Sentinel '[ART9_REDACTED]'/'[ANON_FEHLER]' -> Zeile NICHT schreiben (kein DB-Spam), aber intent_event-Emit BLEIBT (lief schon). -->
<!--     Cache via ls.get_anonymisierer(_sid) (live_session.py:292). GLEICHE Behandlung wie der gesprochene Pfad (:114-135). -->
<!--   Lock: ls.log_lock fuer den conversation_log-Append (wie der gesprochene Pfad), EIGENER Block, NICHT mit _session_state_lock genested (:830-Warnung). -->
<!--   Marker MUSS round-trippen: RAM conversation_log -> DB-Persist (session-end) -> /api/transcript-Reader. Executor verifiziert den Round-Trip. Siehe Task 3 + §5. -->

<!-- handle_manual_mode_toggle (deepgram:922-963) — ORTHOGONALE Tuersteher/Zielperson-Achse, NICHT die cold_call/meeting-Achse: -->
<!--   Schreibt st['contact_category'] in {target,gatekeeper} + st['current_mode'] in {cold_call,gatekeeper} (:944-945; -->
<!--   new_mode='gatekeeper' if category=='gatekeeper' else 'cold_call' :934). Liest/schreibt _session_modes GAR NICHT. -->
<!--   => Keine _session_modes-Migration noetig; nur belegen dass es nicht bricht. -->
<!--   ⚠️ NAMENS-UEBERLAPPUNG (Caveat, Refinement 2): 'cold_call' kommt in BEIDEN Achsen vor, mit UNTERSCHIEDLICHER Bedeutung: -->
<!--     - state['current_mode'] (Kontakt-Achse: cold_call vs gatekeeper)  <- handle_manual_mode_toggle, NICHT ins Register -->
<!--     - _session_state[sid]['mode'] (Hoerbarkeits-/Registry-Achse: cold_call vs meeting)  <- das Register-Feld, Task 2 -->
<!--   Task 2 darf state['current_mode'] NICHT migrieren/loeschen/ins Register lesen. CAVEAT-Kommentar im Code Pflicht. -->

<!-- REFINEMENT 2 — Modus-Quelle praezisiert (Andre-Klarstellung): KEIN Live-Toggle cold_call<->meeting bauen. -->
<!--   Der cold_call/meeting-Modus wird HEUTE NUR bei Call-Start gesetzt: deepgram:437 _session_modes[sid]=mode (GELOESCHT, Task 2) -->
<!--   + init_session_state(mode=...) live_session:300/313. mode aus start_live_session-Payload (deepgram:516/527, call_mode :688-690). -->
<!--   Nach D3-Konsolidierung schreibt Call-Start _session_state[sid]['mode'] (per-SID); das geloeschte globale Dict faellt weg. -->
<!--   ⚠️ EHRLICH (Punkt 22): Es gibt HEUTE KEINEN Laufzeit-Consent-Rueckfall-mode-Flip im Code. Gegrept (orchestrator-verifiziert): die einzigen mode-Writes -->
<!--     sind die 2 Call-Start-Writes + die orthogonale handle_manual_mode_toggle (Gatekeeper-Achse). Die live_session:471-Referenz -->
<!--     ("z.B. Meeting->Cold-Call bei Consent-Verweigerung") ist ein KOMMENTAR in get_or_open_moment, der den MOMENT-FENSTER-Reset -->
<!--     beschreibt (schliesst das offene Moment wenn moment_opened_mode != mode), KEIN echter mode-Flip-Write. -->
<!--   => Der Consent-Rueckfall wird in DIESEM Plan NICHT erfunden. Falls/sobald er gebaut wird, MUSS er das konsolidierte -->
<!--     _session_state[sid]['mode'] schreiben (das EINZIGE Feld, das das Register liest) — NIEMALS das geloeschte globale _session_modes. -->
<!--   EXECUTOR VOR-EXECUTE-RE-GREP: nach einem Consent-Rueckfall-mode-Write greppen; falls beim Execute einer existiert -> auf per-SID migrieren. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/mode_strategy.py — ModeStrategy(ABC, setup_audio_routes+extract_intent) + Registry + ColdCall/Meeting + 3 Steckplaetze (neue Datei)</name>
  <read_first>
    - services/intent_event_writer.py (emit_intent_event-Signatur — extract_intent liefert speaker_role/speaker_id/inference_basis/confidence dorthin)
    - services/intent_taxonomy.py (is_valid_intent_type — extract_intent erfindet KEINEN intent_type, der bleibt Aufrufer-Sache)
    - RESEARCH §5 (Registry-Empfehlung, Datei-Ort, Decorator/Funktion)
    - Geruest §2 (Modi-Tabelle, Audibility-Contract) + §3 (speaker_role berater/kunde/gatekeeper/system; speaker_id UUID/local; cold_call default berater/local)
    - SPEC REQ 6 Acceptance (Z.55: Registry, 2 echte Klassen, neuer Modus ohne Kern-Aenderung, intent_event mode/speaker_role/speaker_id, 3 Steckplaetze, kein Air-Gap-Bus)
    - claude_service.py:32 + :523 (SYSTEM_PROMPT_BASE = der EINE Prompt — DESHALB KEIN get_classification_prompt in der ABC)
  </read_first>
  <behavior>
    - Test 1: MODE_REGISTRY enthaelt 'cold_call' und 'meeting' als registrierte Strategy-INSTANZEN.
    - Test 2: register('x_test', XStrategy) fuegt einen neuen Eintrag hinzu, ohne dass bestehende Eintraege sich aendern (neuer Modus = neue Klasse, Kern unberuehrt). Cleanup (pop) im Test.
    - Test 3: ColdCallStrategy().extract_intent(...) -> dict mit speaker_role=='berater', speaker_id=='local', inference_basis=='advisor_paraphrase'. confidence-Cap aktiv (Rueckgabe <= COLD_CALL_CONF_CAP, z.B. 0.85).
    - Test 4: MeetingStrategy().extract_intent(speaker=1, ...) -> speaker_role=='kunde'; extract_intent(speaker=0, ...) -> speaker_role=='berater'; inference_basis=='direct_customer_utterance' bei Kunde.
    - Test 5: MODE_REGISTRY['meeting_ext'].extract_intent(...) wirft NotImplementedError (gilt fuer alle 3 Steckplaetze).
    - Test 6: ModeStrategy hat KEIN Attribut/Methode get_classification_prompt (assert not hasattr — Decision 1 strukturell; Runtime-API-Assertion, kein Source-Presence).
  </behavior>
  <action>
    NEUE Datei `services/mode_strategy.py` (Code-Identifier ASCII):
    - `from abc import ABC, abstractmethod`.
    - `class ModeStrategy(ABC):` mit GENAU zwei abstrakten Methoden:
      - `setup_audio_routes(self) -> dict` — dokumentierter Audibility-Contract (welche Sprecher hoerbar sind). cold_call: nur Berater hoerbar (diarize=False); meeting: beide. Rueckgabe als deklaratives dict (z.B. `{'diarize': bool, 'audible': [...]}`), wird in TAXO1 NICHT verdrahtet (nur Vertrag), klar kommentiert.
      - `extract_intent(self, *, speaker=None, ergebnis=None, **ctx) -> dict` — liefert `{'speaker_role', 'speaker_id', 'inference_basis', 'confidence'}` fuer emit_intent_event. ZUSTANDSLOS (kein per-Call-State auf der Instanz; alles via Argumente — Race-Safety, T-TAXO1-22).
      - **KEIN `get_classification_prompt`** (Decision 1 — SYSTEM_PROMPT_BASE ist der EINE Prompt; Kommentar im Code: "Klassifikation laeuft zentral ueber SYSTEM_PROMPT_BASE (claude_service.py:32, MEDFIX 2026-06-18). Die Strategy regiert NUR Sprecher-Zuordnung + Audibility, NICHT den Prompt — sonst re-brechen wir den MEDFIX.").
    - `MODE_REGISTRY: dict[str, ModeStrategy] = {}` + `def register(mode_key): def deco(cls): MODE_REGISTRY[mode_key] = cls(); return cls; return deco` (Decorator; Instanz registrieren, da zustandslos/shared).
    - `COLD_CALL_CONF_CAP = 0.85` (Modul-Konstante; cold_call ist haerter als meeting — Berater-Paraphrase ist Inferenz, nicht direkte Kunden-Aussage). Kommentar mit Begruendung.
    - `@register('cold_call') class ColdCallStrategy(ModeStrategy):`
      - `setup_audio_routes` -> `{'diarize': False, 'audible': ['berater']}` (NERVE hoert nur den Berater).
      - `extract_intent` -> `{'speaker_role': 'berater', 'speaker_id': 'local', 'inference_basis': 'advisor_paraphrase', 'confidence': min(<incoming_conf or 0.7>, COLD_CALL_CONF_CAP)}`. FEST berater/local (Decision 2: jede transkribierte Aeusserung im cold_call ist der Berater; advisor-abgeleitete Erkennung wird als Berater attribuiert, NIE als Kunde). **Caveat-Kommentar (Refinement 2):** "ACHTUNG Namens-Ueberlappung: 'cold_call' hier = Hoerbarkeits-Achse (vs meeting). NICHT verwechseln mit state['current_mode']=='cold_call' (Kontakt-Achse vs gatekeeper, handle_manual_mode_toggle). Diese Strategy regiert die Hoerbarkeits-Achse."
    - `@register('meeting') class MeetingStrategy(ModeStrategy):`
      - `setup_audio_routes` -> `{'diarize': True, 'audible': ['berater', 'kunde']}`.
      - `extract_intent(speaker, ...)` -> speaker_role aus Sprecher-Trennung: speaker==0 -> 'berater', speaker==1 -> 'kunde', sonst 'system'/Default 'kunde' (dokumentiert). speaker_id='local'. inference_basis='direct_customer_utterance' (bei Kunde) bzw. 'advisor_paraphrase' (bei Berater). confidence durchreichen (kein Cap, meeting ist direkte Aussage).
    - 3 Steckplaetze: `@register('meeting_ext')`, `@register('training_cold')`, `@register('training_meeting')` als Platzhalter-Klassen, deren BEIDE Methoden `raise NotImplementedError("Steckplatz: Ausbau spaetere Phase (Geruest §2). KEIN Air-Gap-Bus in TAXO1.")`. Klar kommentiert: "Zukunftssicherer Registry-Eintrag. KEIN Training-Air-Gap-Event-Bus (Geruest §6) — nur Interface aufnahmefaehig."
    - KEIN In-Prozess-Event-Bus, KEINE `event_bus`/`sim.internal`/`audio.*`-Topics/Namespaces.
    Punkt-23-Modul-Docstring oben: Zweck, woher der mode kommt (per-SID `_session_state[sid]['mode']`, Call-Start-only, KEIN Live-Toggle), Verweis auf emit_intent_event + SYSTEM_PROMPT_BASE + den Caveat zur Namens-Ueberlappung mit der Gatekeeper-Achse.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('services/mode_strategy.py', encoding='utf-8').read())"</automated>
    <note>Kein Local-Dev: echter Runtime-Lauf des Tests server-seitig im Gate-Task. AST-Parse lokal erlaubt (kein Ausfuehren).</note>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ModeStrategy" services/mode_strategy.py` 1 Treffer + `grep -nE "def setup_audio_routes|def extract_intent" services/mode_strategy.py` (beide Methoden).
    - `grep -n "get_classification_prompt" services/mode_strategy.py` == 0 (Decision 1: NICHT vorhanden).
    - `grep -nE "class ColdCallStrategy|class MeetingStrategy" services/mode_strategy.py` (2 echte Klassen) + `grep -nE "meeting_ext|training_cold|training_meeting" services/mode_strategy.py` (3 Steckplaetze).
    - ColdCall fest berater/local/advisor_paraphrase: `grep -n "advisor_paraphrase" services/mode_strategy.py` 1+ Treffer; `grep -n "'berater'" services/mode_strategy.py` 1+ Treffer.
    - Namens-Ueberlappungs-Caveat vorhanden: `grep -in "current_mode\|gatekeeper\|namens" services/mode_strategy.py` 1+ Treffer (Refinement 2: dokumentiert dass 'cold_call' zwei Achsen hat).
    - KEIN Air-Gap-Bus: `grep -inE "event_bus|sim\.internal|audio\.\*|topic" services/mode_strategy.py` == 0.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Registry + 2 echte Strategien + 3 Steckplaetze; ABC OHNE get_classification_prompt; cold_call fest berater/local/advisor_paraphrase + confidence-Cap; zustandslos; Namens-Ueberlappungs-Caveat dokumentiert; kein Air-Gap-Bus.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: §0.1-Konsolidierung (NUR cold_call/meeting-Achse) — globales _session_modes LOESCHEN, alle Lese-Stellen auf per-SID; Default 'cold_call'; Gatekeeper-Achse + Consent-Rueckfall-Realitaet ehrlich belegen (Decision 3+4 + Refinement 2)</name>
  <read_first>
    - ⚠️ RE-GREP (Banner): `grep -rn "_session_modes" services/` + `grep -rn "current_mode\|contact_category" services/deepgram_service.py` + `grep -rin "consent\|verweigerung\|moment_opened_mode" services/live_session.py services/deepgram_service.py`
    - services/deepgram_service.py:18 (def), :437 (write, Call-Start — die EINZIGE cold_call/meeting-Schreibstelle), :445 (pop), :516/:524-527 (start_live_session mode-Quelle), :636-644 (init_session_state(mode=mode)), :688-690 (call_mode mapping), :820-855 (Button-Emit + :833 mode-Read), :922-963 (handle_manual_mode_toggle — schreibt state['current_mode']/contact_category, NICHT _session_modes) — je Site 30 Zeilen vor/nach VERBATIM
    - services/claude_service.py:969-1028 (Medium-Lane, mode-Read :974 + Marker :971-972), :1145-1230 (per-SID-Read-Beweis :1152/:1224), :1510-1545 (A/B-abstain, mode-Read :1520)
    - services/einwand_keyword_matcher.py:269-305 (Fast-Lane, mode-Read :280)
    - services/live_session.py:300-313 (init_session_state, default :302), :471 (Consent-Verweigerung-KOMMENTAR in get_or_open_moment — KEIN mode-Flip, nur Moment-Fenster-Reset; Refinement 2 ehrlich belegen)
  </read_first>
  <action>
    Punkt-14 (4-Schichten) + Punkt-22 (Verbindungs-Karte mit grep-Beleg). A4 ist DECIDED=YES (Split-Brain bewiesen: claude:974 liest GLOBAL, claude:1152/1224 liest PER-SID — zwei lebende Quellen fuer dasselbe Datum). **Refinement 2: Die Konsolidierung beruehrt AUSSCHLIESSLICH die cold_call/meeting-Achse (`_session_state[sid]['mode']`). Die Tuersteher/Zielperson-Achse (`state['current_mode']`/`contact_category`) ist ORTHOGONAL und bleibt UNVERAENDERT.** Bau die Konsolidierung:

    (a) **Globales Dict loeschen (deepgram_service.py):**
      - :18 `_session_modes = {}` ENTFERNEN (optional ein 1-Zeilen-Marker-Kommentar `# _session_modes geloescht TAXO1-07: cold_call/meeting-Modus-Quelle ist per-SID _session_state[sid]['mode'] (Call-Start-only, kein Live-Toggle)`).
      - :437 `_session_modes[sid] = mode` ENTFERNEN. **Das ist die EINZIGE cold_call/meeting-Schreibstelle (Call-Start).** Der mode landet bereits per-SID via init_session_state(mode=mode) (:643) — pruefen+belegen dass init_session_state VOR der ersten mode-Lese-Stelle laeuft (Call-Start-Reihenfolge: :562 connection open, init :636). Falls _open_deepgram_connection den mode noch fuer `diarize`/`_make_on_message` braucht: der `mode`-PARAMETER der Funktion bleibt (lokale Variable, kein globales Dict) — NUR das Dict-Write faellt weg.
      - :445 `_session_modes.pop(sid, None)` ENTFERNEN (per-SID raeumt pop_session_state).
      - :833 Button-Read `_btn_mode = _session_modes.get(_sid, 'cold_call')` -> `_btn_mode = (ls._session_state.get(_sid) or {}).get('mode', 'cold_call')` (per-SID; ls ist im Handler importiert, :823).
    (b) **claude_service.py Lese-Stellen per-SID:**
      - :974 `_med_mode = _dg._session_modes.get(sid, 'meeting')` -> `_med_mode = (ls._session_state.get(sid) or {}).get('mode', 'cold_call')`. Import `_dg` entfernen falls sonst ungenutzt. Marker-Kommentar :971-972 aktualisieren ("W7 erledigt: Read per-SID").
      - :1520 `_ab_mode = _dg2._session_modes.get(sid, 'meeting')` -> per-SID analog. `_dg2`-Import entfernen falls ungenutzt.
    (c) **einwand_keyword_matcher.py:**
      - :280 `_kw_mode = _dg_kw._session_modes.get(sid, 'cold_call')` -> `_kw_mode = (_ls._session_state.get(sid) or {}).get('mode', 'cold_call')` (`_ls` ist via `_get_ls()` :277 schon da). `_dg_kw`-Import entfernen falls ungenutzt.
    (d) **Default vereinheitlichen (Decision 4):** live_session.py:302 `mode: str = 'meeting'` -> `mode: str = 'cold_call'`. deepgram:524 `mode = 'meeting'` + :527 `data.get('mode', 'meeting')` -> `'cold_call'` (mode wird ohnehin explizit vom Client gesetzt; der Default greift nur bei fehlendem Payload). Alle per-SID-Read-Defaults oben einheitlich `'cold_call'`.
    (e) **Gatekeeper-Achse belegen + Namens-Ueberlappung dokumentieren (Refinement 2, deepgram:922-963):** Belegen (grep), dass `handle_manual_mode_toggle` `_session_modes` NICHT liest/schreibt (es setzt `state['current_mode']`/`contact_category`, eine SEPARATE Kontakt-Achse). Keine Migration noetig. **CAVEAT-Kommentar in den Code setzen** (an die per-SID-Button-Read-Stelle :833 ODER an handle_manual_mode_toggle): `# CAVEAT (TAXO1-07): 'cold_call' existiert in ZWEI orthogonalen Achsen — state['current_mode'] (Kontakt: cold_call vs gatekeeper, HIER) und _session_state[sid]['mode'] (Hoerbarkeit: cold_call vs meeting, Registry). NICHT vermischen: diese Achse wird NICHT ins Register migriert.` Task 2 darf `state['current_mode']`/`contact_category` NICHT lesen/migrieren/loeschen. In der SUMMARY festhalten, dass die Loesch-Aktion (a) die Gatekeeper-Achse nicht beruehrt (`git diff` zeigt keine Aenderung an :922-963 ausser ggf. dem Caveat-Kommentar). NICHT erfinden, dass es migriert werden muss.
    (f) **Consent-Rueckfall ehrlich belegen (Refinement 2, Punkt 22 — NICHT erfinden):** Mit grep belegen, dass HEUTE KEIN Laufzeit-Consent-Rueckfall-mode-Flip im Code existiert (die einzigen cold_call/meeting-mode-Writes sind die 2 Call-Start-Writes; live_session:471 ist ein Moment-Fenster-Reset-Kommentar in get_or_open_moment, KEIN mode-Write). In der SUMMARY wortwoertlich festhalten: "Consent-Rueckfall existiert heute NICHT als Laufzeit-mode-Flip; der cold_call/meeting-mode wird ausschliesslich bei Call-Start gesetzt (kein Live-Toggle). Falls/sobald ein Consent-Rueckfall gebaut wird, MUSS er das konsolidierte `_session_state[sid]['mode']` schreiben (das einzige Feld, das das Register liest) — niemals das geloeschte globale `_session_modes`." KEINEN Consent-Rueckfall-Write in diesem Plan erfinden/bauen.
    KEINE Aenderung an sp_map/Sprecher-Trennung (live_session) ausser den oben genannten Lese-Quellen/Defaults.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f, encoding='utf-8').read()) for f in ['services/deepgram_service.py','services/claude_service.py','services/einwand_keyword_matcher.py','services/live_session.py']]"</automated>
    <note>Funktionaler Beweis im Gate-Task via Test-Anruf (mode landet korrekt im intent_event). AST-Parse lokal.</note>
  </verify>
  <acceptance_criteria>
    - `grep -rn "_session_modes" services/` == 0 (ausser ggf. ein expliziter Lösch-Marker-Kommentar) — globales Dict + alle Reads weg (cold_call/meeting-Achse konsolidiert).
    - Alle 4 ex-Lese-Stellen lesen jetzt per-SID: `grep -rn "_session_state.*\.get('mode'" services/` zeigt Button/Medium/A-B/Fast.
    - `grep -n "mode: str = 'cold_call'" services/live_session.py` 1 Treffer (Default vereinheitlicht).
    - **Refinement 2 — Gatekeeper-Achse UNVERAENDERT:** `git diff services/deepgram_service.py` zeigt KEINE Logik-Aenderung an handle_manual_mode_toggle (:922-963) / `state['current_mode']` / `contact_category` (nur ggf. ein CAVEAT-Kommentar). `grep -n "current_mode\|contact_category" services/deepgram_service.py` Treffer-Zahl unveraendert (kein Read ins Register).
    - **Refinement 2 — Namens-Ueberlappungs-Caveat im Code:** `grep -in "CAVEAT.*current_mode\|orthogonal\|zwei.*achs" services/deepgram_service.py` 1+ Treffer.
    - `git diff services/live_session.py` zeigt KEINE Aenderung an sp_map/Sprecher-Trennung (nur Default-Zeile + ggf. Lese-Quelle).
    - SUMMARY enthaelt grep-Beleg, dass (i) handle_manual_mode_toggle _session_modes nie nutzte (separater state['current_mode']/contact_category) und (ii) HEUTE kein Consent-Rueckfall-mode-Flip existiert (live_session:471 = Moment-Fenster-Reset-Kommentar, kein mode-Write).
    - AST-Parse aller 4 Dateien exit 0.
  </acceptance_criteria>
  <done>Globales _session_modes geloescht (cold_call/meeting-Achse); alle Modus-Reads per-SID; Default einheitlich 'cold_call'; Call-Start ist die EINZIGE Schreibstelle (kein Live-Toggle); Gatekeeper-Achse (state['current_mode']/contact_category) belegt unberuehrt + Namens-Caveat im Code; Consent-Rueckfall-Realitaet ehrlich belegt (existiert nicht, nicht erfunden); sp_map unberuehrt.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Registry an die 4 Emit-Stellen verdrahten + Sprecher-Bug fixen (Decision 2) + EWB-Button schreibt markierte Transcript-Zeile (Refinement 1)</name>
  <read_first>
    - services/mode_strategy.py (MODE_REGISTRY, ColdCall/Meeting.extract_intent-Rueckgabe-Keys) — aus Task 1
    - services/claude_service.py:1011-1028 (Medium-Lane cold_call-Bug :1011-1013) + :1534-1543 (A/B-abstain hardcoded 'kunde') — VERBATIM
    - services/deepgram_service.py:813-919 (handle_manual_ewb GANZ: :819 typ-Quelle, :847-853 Button-Emit/Kunde-Ausnahme, :857-919 Profil/Variante/record_ewb_click) — VERBATIM
    - services/deepgram_service.py:108-143 (kanonischer Transcript-Append-Pfad mit Anonymisierung + log_lock — die Struktur, die die Button-Zeile spiegelt) — VERBATIM
    - services/deepgram_service.py:79-90 (cold_call sp_label/log_sp=0/Berater — beweist die INT-Speaker-Encoding) — VERBATIM
    - services/einwand_keyword_matcher.py:294-302 (Fast-Lane hardcoded 'kunde') — VERBATIM
    - services/intent_event_writer.py:32 (welche Felder der Emit erwartet)
    - services/anonymization.py:534 (anonymize(text, cache) -> (text, tier); Sentinels) + :597 (anonymize_output) + services/live_session.py:292 (get_anonymisierer)
    - ⚠️ Reader-Mapping VERIFIZIEREN (Refinement 1): `grep -rn "speaker" routes/ templates/ static/ | grep -i "transcript\|pip\|launcher"` — die EXAKTE Speaker-Encoding (INT 1 vs String 'Kunde') ermitteln, die der /api/transcript-Reader + pip-launcher erwartet. NICHT raten.
  </read_first>
  <action>
    **TEIL A — Registry an die 3 LLM/Keyword-Emit-Stellen (Sprecher-Bug-Fix, Decision 2):**
    An jeder der 3 LLM/Keyword-Emit-Stellen (Medium, A/B-abstain, Fast) den hardcoded `speaker_role='kunde', speaker_id='local'` durch den Registry-Lookup ersetzen. Helper-Muster (Edge-1-sicher):
    `_strategy = MODE_REGISTRY.get(<mode>) or MODE_REGISTRY['cold_call']` → `_attr = _strategy.extract_intent(speaker=<speaker_or_None>, ergebnis=<ergebnis_or_None>, confidence=<conf>)` → die Emit-Argumente `speaker_role=_attr['speaker_role']`, `speaker_id=_attr['speaker_id']`, `inference_basis=_attr['inference_basis']` (und ggf. `confidence=_attr['confidence']` fuer den cold_call-Cap). Try-except um den Strategy-Aufruf (Edge 2 — Steckplatz-Modus duerfte nie greifen, aber Belt-and-suspenders: faellt auf cold_call-Default zurueck, kein Live-Loop-Crash, T-TAXO1-21).

    (1) **Medium-Lane (claude:1011-1026):** Den `if _med_mode == 'cold_call': ... 'kunde' ...`-Block (:1011-1013, der BUG) ERSETZEN durch den Registry-Lookup. cold_call -> berater/local/advisor_paraphrase (Bug behoben); meeting -> MeetingStrategy (speaker hier nicht pro-Sprecher verfuegbar in der Medium-Lane -> Default kunde/direct_customer_utterance, wie heute fuer meeting korrekt). emit unveraendert (:1019-1026) ausser den 3 Werten + ggf. capped confidence.
    (2) **A/B-abstain-Lane (claude:1534-1543):** `speaker_role='kunde', speaker_id='local'` (:1541) durch `_attr` ersetzen. Das vorhandene `inference_basis`-Conditional (:1537-1538) faellt weg (kommt jetzt aus extract_intent). mode-Read ist nach Task 2 per-SID.
    (3) **Fast-Lane Keyword (matcher:297-302):** `speaker_role='kunde', speaker_id='local'` durch `_attr` ersetzen. cold_call -> berater (Keyword-Treffer im cold_call = Berater nennt den Einwand-Typ -> advisor_label/advisor_paraphrase-Attribution als Berater). intent_type='echter_einwand' bleibt (matcher-Sache, nicht Strategy).

    **TEIL B — EWB-Button: intent_event-Ausnahme BLEIBT + NEU markierte Transcript-Zeile (Refinement 1):**
    (4) **EWB-Button intent_event (deepgram:847-853) — DIE ERLAUBTE KUNDE-AUSNAHME, UNVERAENDERT:** speaker_role='kunde' BLEIBT (Decision 2: Button-Druck = der Knopf-Text zaehlt als Kunde-Einwand), source='ui_asserted', inference_basis='ui_button' bleibt. **NICHT** ueber die Registry leiten (die wuerde cold_call->berater erzwingen und die erlaubte Ausnahme brechen). Kommentar ergaenzen, WARUM der Button hier von der Strategy-Regel abweicht (die einzige Stelle, die im cold_call 'kunde' schreiben darf).
    (5) **NEU (Refinement 1) — markierte conversation_log-Transcript-Zeile:** In `handle_manual_ewb`, NACH dem bestehenden intent_event-Emit (:853, der UNVERAENDERT bleibt) und idealerweise vor/parallel zum Variante-Streaming (:876+), eine Transcript-Zeile in `ls.conversation_log` schreiben — spiegelt den kanonischen Append (deepgram:130-135):
      - **Anonymisierung ZUERST** (gleiche Behandlung wie der gesprochene Pfad :114-135): `from services.anonymization import anonymize`; `_anon_cache = ls.get_anonymisierer(_sid)`; `_anon_typ, _ = anonymize(typ, _anon_cache)`. Wenn `_anon_typ in ('[ART9_REDACTED]', '[ANON_FEHLER]')` → Zeile NICHT schreiben (kein DB-Spam), Print-Log wie der gesprochene Pfad; der intent_event-Emit (lief schon :853) BLEIBT trotzdem. try-except um den ganzen Block (Live-Loop crasht nie).
      - **Append unter ls.log_lock** (EIGENER Block, NICHT mit `_session_state_lock` genested — :830-Warnung beachten):
        ```
        with ls.log_lock:
            ls.conversation_log.append({
                'ts': time.time(), 'type': 'transcript',
                'speaker': 1,                       # Kunde-Label (INT) — wie der gesprochene Pfad (log_sp). VERIFIZIEREN gegen Reader-Mapping!
                'text': f"{_anon_typ} *ewb button*", # Knopf-Kurztext anonymisiert + KANONISCHER Marker (ueberlebt _build_log_content, das nur text+speaker rendert)
                'data': {'ewb_button': True},       # Zusatz-Flag (RAM/Analyse) — NICHT der Export-Marker (data wird fuer transcript-Eintraege nicht persistiert, live_session:988-1020)
            })
        ```
      - **Speaker-Encoding (Refinement 1 — VERIFIZIEREN, NICHT RATEN):** Der kanonische Pfad schreibt `'speaker': log_sp` als INT (0/1/None). Fuer die Kunde-Button-Zeile MUSS speaker den Kunde-Wert tragen, den der Reader (/api/transcript + pip-launcher INT 0/1/None->berater/kunde/system) erwartet — voraussichtlich INT `1`. Der Executor greppt den Reader (read_first) und setzt den EXAKT passenden Wert; wenn der Reader einen String erwartet, entsprechend anpassen. Diese Zeile MUSS beim Export als Kunde erscheinen, alle gesprochenen cold_call-Zeilen bleiben Berater.
      - **Marker-Round-Trip (Refinement 1) — BEWIESEN durch Plan-Checker, nicht annehmen:** Der Persist-Pfad ist `_build_log_content` (live_session.py:988-1020). Fuer `type=='transcript'`-Eintraege rendert er NUR `entry['speaker']` (via sp_map INT->Label, :994) + `entry['text']` — er liest `data` NICHT (das `data`-Feld wird ausschliesslich fuer `type=='analyse'` konsumiert, :1022). → Das strukturierte Flag `data={'ewb_button': True}` ueberlebt den Round-Trip NACHWEISLICH NICHT. **Daher ist der Text-Suffix `*ewb button*` am `text` der KANONISCHE Marker** (nicht der Fallback) — er ueberlebt garantiert ueber `entry['text']`. Bauanweisung: den `*ewb button*`-Suffix an `_anon_typ` haengen (`text=f"{_anon_typ} *ewb button*"`); das `data={'ewb_button': True}`-Flag DARF zusaetzlich gesetzt bleiben (RAM-/Analyse-seitig nuetzlich, schadet nicht), ist aber NICHT der Export-Marker. Der Executor BESTAETIGT am Gate via inspect.sh, dass die exportierte Transcript-Zeile den `*ewb button*`-Suffix traegt (Round-Trip RAM conversation_log -> `_build_log_content` -> /api/transcript). VOR-EXECUTE-RE-GREP: `_build_log_content` + `'type': 'transcript'`-Render-Logik gegenpruefen (Pfad kann sich seit HEAD aendern).
      - Per-SID/Lock-Disziplin wie im restlichen Handler. Diese Zeile ist die EINZIGE Kunde-Zeile im Cold-Call-Transcript.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f, encoding='utf-8').read()) for f in ['services/claude_service.py','services/deepgram_service.py','services/einwand_keyword_matcher.py']]"</automated>
    <note>Funktionaler Beweis (cold_call=berater, button=kunde + EINE markierte Transcript-Zeile, meeting getrennt) im Gate-Task via Test-Anruf + sample intent_event + inspect.sh transcript-Segmente.</note>
  </verify>
  <acceptance_criteria>
    - `grep -n "MODE_REGISTRY\[" services/claude_service.py services/einwand_keyword_matcher.py` zeigt den Lookup in Medium + A/B + Fast (3 Bahnen).
    - Sprecher-Bug weg (Tupel-Form): `grep -n "'kunde', 'local'\|'kunde','local'" services/claude_service.py services/einwand_keyword_matcher.py` == 0 (faengt die Tupel-Zuweisung claude:1012).
    - Sprecher-Bug weg (Kwargs-Form — Plan-Checker WARNING 1): `grep -nE "speaker_role=['\"]kunde['\"]" services/claude_service.py services/einwand_keyword_matcher.py` == 0. PFLICHT zusaetzlich zur Tupel-Form: matcher:300 und claude:1541 schreiben `speaker_role='kunde', speaker_id='local'` (Kwargs) — dazwischen steht `speaker_id=`, also matcht die Tupel-Grep '"kunde", "local"' NICHT. Nur beide Greps zusammen schliessen alle 3 LLM/KW-Bahnen ab (deepgram-Button bewusst ausgenommen — die erlaubte Ausnahme).
    - Button-intent_event-Ausnahme intakt: `grep -n "speaker_role='kunde'" services/deepgram_service.py` zeigt NUR die Button-Stelle (:847-853-Bereich) + `grep -n "ui_button\|ui_asserted" services/deepgram_service.py` vorhanden.
    - **Refinement 1 — EWB-Button-Transcript-Zeile vorhanden:** `grep -n "ewb_button" services/deepgram_service.py` 1+ Treffer (der Marker); `grep -n "conversation_log.append" services/deepgram_service.py` zeigt jetzt AUCH die Button-Stelle (zusaetzlich zum kanonischen Pfad); `grep -n "log_lock" services/deepgram_service.py` zeigt die Button-Append unter log_lock.
    - **Refinement 1 — Anonymisierung im Button-Pfad:** `grep -n "anonymize\|get_anonymisierer" services/deepgram_service.py` zeigt den Aufruf im handle_manual_ewb (Sentinel-Skip belegt im Code).
    - **Refinement 1 — Lock-Disziplin (kein Nesting):** Code-Review/Inspektion bestaetigt, dass die conversation_log.append-Zeile unter `ls.log_lock` in EIGENEM Block laeuft (NICHT innerhalb des `_session_state_lock`-Blocks :838-846).
    - **Refinement 1 — Export-Marker (Suffix, kanonisch):** `grep -n "ewb button" services/deepgram_service.py` zeigt den `*ewb button*`-Text-Suffix an der Button-Transcript-Zeile (der Marker, der `_build_log_content` ueberlebt — live_session:988-1020 rendert nur text+speaker fuer transcript-Eintraege).
    - SUMMARY: inspect.sh-Beleg, dass die EXPORTIERTE Transcript-Zeile (via _build_log_content/api/transcript) genau EINE Kunde-Zeile mit sichtbarem `*ewb button*`-Suffix erzeugt (Round-Trip RAM conversation_log -> _build_log_content -> Export bestaetigt).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>3 LLM/KW-Bahnen routen ueber MODE_REGISTRY.extract_intent (cold_call=berater/advisor_paraphrase, meeting getrennt); Button-intent_event bleibt die einzige erlaubte Kunde-Ausnahme (ui_asserted); EWB-Button schreibt ZUSAETZLICH genau EINE markierte Kunde-Transcript-Zeile (anonymisiert, log_lock, ewb_button-Marker, Round-Trip-belegt).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: tests/test_mode_registry.py — Struktur-/Runtime-Test (in-memory, kein DB)</name>
  <read_first>
    - CLAUDE.md Test-Qualitaets-Regel (Integration/Runtime-Assertion; KEIN getsource/grep-auf-Quelle/hasattr-als-Loesch-Schutz)
    - CLAUDE.md Test-Cleanup-Regel (committende Tests raeumen Rows — hier N/A bestaetigen: rein in-memory, kein nerve_test-Write)
    - services/mode_strategy.py (MODE_REGISTRY, register, Klassen, COLD_CALL_CONF_CAP)
  </read_first>
  <action>
    NEUE Datei `tests/test_mode_registry.py` — Runtime-Assertionen auf Registry-Inhalt + extract_intent-Rueckgaben (KEINE DB, KEIN getsource):
    - `def test_cold_call_and_meeting_registered():` assert 'cold_call' in MODE_REGISTRY and 'meeting' in MODE_REGISTRY (Runtime-Registry-Inhalt).
    - `def test_new_mode_registers_without_core_change():` Dummy-Strategy-Klasse + register('x_test', ...); assert 'x_test' in MODE_REGISTRY und die Kern-Eintraege (cold_call/meeting) unveraendert. Teardown: `MODE_REGISTRY.pop('x_test', None)`.
    - `def test_cold_call_speaker_is_berater():` `MODE_REGISTRY['cold_call'].extract_intent(...)` -> speaker_role=='berater', speaker_id=='local', inference_basis=='advisor_paraphrase' (DER Bug-Fix-Beweis, Runtime-Return).
    - `def test_cold_call_confidence_capped():` extract_intent(confidence=0.99) -> Rueckgabe-confidence <= COLD_CALL_CONF_CAP.
    - `def test_meeting_speaker_separation():` extract_intent(speaker=0) -> 'berater'; extract_intent(speaker=1) -> 'kunde'.
    - `def test_future_slots_raise_not_implemented():` fuer 'meeting_ext'/'training_cold'/'training_meeting': MODE_REGISTRY[k].extract_intent(...) wirft NotImplementedError (pytest.raises).
    - `def test_abc_has_no_classification_prompt():` assert not hasattr(ModeStrategy, 'get_classification_prompt') — Runtime-API-Assertion (Decision 1; das ist KEIN Source-Presence-Schutz, sondern prueft die Klassen-Schnittstelle zur Laufzeit; Kommentar im Test begruendet warum).
    Server-seitig lauffaehig; rein in-memory (kein DB-Commit -> Cleanup-Regel N/A, im Test-Docstring vermerkt).
    HINWEIS: Der EWB-Button-Transcript-Pfad (Refinement 1) ist KEIN mode_strategy-Test — er wird im Gate-Task per Test-Anruf + inspect.sh transcript-Segment belegt (Laufzeit-/Integrations-Beweis, kein in-memory mock).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('tests/test_mode_registry.py', encoding='utf-8').read())"</automated>
    <note>Echter Lauf server-seitig im Gate-Task: pytest tests/test_mode_registry.py -x. Kein Local-Dev.</note>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_new_mode_registers_without_core_change\|def test_cold_call_speaker_is_berater" tests/test_mode_registry.py` 2 Treffer.
    - KEIN Source-Presence: `grep -nE "getsource|open\(.*mode_strategy.*\)\.read|\.count\('|subprocess" tests/test_mode_registry.py` == 0 (hasattr nur fuer die Runtime-API-Assertion in test_abc_has_no_classification_prompt, kommentiert).
    - Tests asserten Runtime-Registry-Inhalt + extract_intent-Returns (Integration), nicht Code-Existenz.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Struktur-/Runtime-Test: neuer Modus ohne Kern-Aenderung; cold_call=berater/local/capped; meeting getrennt; Steckplaetze NotImplemented; ABC ohne classification_prompt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ModeStrategy-Registry (ABC ohne get_classification_prompt) + ColdCall/Meeting + 3 Steckplaetze; §0.1-Konsolidierung (NUR cold_call/meeting-Achse: _session_modes geloescht, alle Reads per-SID, Default 'cold_call', Call-Start-only/kein Live-Toggle, Gatekeeper-Achse + Consent-Rueckfall-Realitaet belegt); Sprecher-Bug-Fix an 3 LLM/KW-Bahnen (cold_call=berater); Button-intent_event als einzige Kunde-Ausnahme; EWB-Button schreibt ZUSAETZLICH eine markierte Kunde-Transcript-Zeile (Refinement 1); in-memory Struktur-Test. Bereit fuer Deploy + Test-Anruf (beide Modi).</what-built>
  <how-to-verify>
    ⚠️ **GATE: Deploy + Test-Anruf ERST NACH Cross-AI-Plan-Review-Clearance (Gemini, PFLICHT bei 🔴) + Andre-Ineinandergreifen-Check.** Nach Clearance:

    1. `git add -A && git commit && git push origin main` → `bash deploy.sh production` (eingebautes Pytest-Tor laeuft server-seitig; bei rot KEIN Restart).
    2. Struktur-Test server-seitig (kein Local-Dev): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_mode_registry.py -x -v'` → GRUEN (Registry + neuer Modus + cold_call=berater + Cap + Steckplaetze NotImplemented + ABC ohne classification_prompt).
    3. **TEST-ANRUF (D-03 Auflage 3) — BEIDE Modi, Daten-Fluss beweisen:**
       - **(a) cold_call:** Test-Anruf im cold_call-Modus (Berater nennt einen Einwand-Typ / paraphrasiert). → `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample intent_event 20'` zeigt Zeilen mit `mode='cold_call'`, payload `speaker_role='berater'`, `speaker_id='local'`, `inference_basis='advisor_paraphrase'` (Medium/Fast). VERBATIM in SUMMARY.
       - **(b) EWB-Button im cold_call (Refinement 1):** Knopf druecken →
         - intent_event: sample zeigt EINE Zeile `mode='cold_call'`, `source='ui_asserted'`, `inference_basis='ui_button'`, `speaker_role='kunde'` (die erlaubte Ausnahme).
         - **Transcript-Zeile:** `bash scripts/inspect.sh` (transcript-Segment-Read oder /api/transcript des Test-Calls) zeigt GENAU EINE Kunde-gelabelte Transcript-Zeile, die den `ewb_button`-Marker traegt (data-Flag oder *ewb button*-Suffix), mit dem Knopf-Kurztext (anonymisiert). ALLE gesprochenen cold_call-Transcript-Zeilen bleiben Berater. VERBATIM in SUMMARY.
       - **(c) meeting:** Test-Anruf im meeting-Modus → sample zeigt `mode='meeting'` + speaker_role aus Sprecher-Trennung (Kunden-Aussage -> 'kunde', inference_basis='direct_customer_utterance').
       - **(d) Routing/Toggle intakt (Refinement 2):** Tuersteher/Zielperson-Umschaltung (manual_mode_toggle, target/gatekeeper) funktioniert weiter (state['current_mode']); logs zeigen keinen `KeyError`/`_session_modes`-Fehler; KEIN Live-Toggle cold_call↔meeting (existiert nicht); keine `user_id leer`-Warnung; `org_id` im Kosten-Log ≠ NULL (D-03 Auflage 3c).
       - **(e) §0.1-Beleg:** `ssh ... 'bash scripts/inspect.sh grep "_session_modes"'` (oder lokal `grep -rn "_session_modes" services/`) == 0 — globales Dict weg, kein Doppel-Read mehr.
       VERBATIM-Belege in SUMMARY.
    4. KEIN SCHILD-Guard noetig (keine Schema-Aenderung — intent_event-Schema stammt aus Welle 1; die EWB-Transcript-Zeile nutzt die bestehende conversation_logs-Persistenz UNVERAENDERT, nur ein zusaetzlicher RAM-Eintrag mit data-Marker im bestehenden JSON-Feld).
  </how-to-verify>
  <resume-signal>"deployed + Struktur-Test GRUEN + Test-Anruf beide Modi: cold_call=berater/local/advisor_paraphrase, Button-intent_event=kunde/ui_asserted, Button-Transcript=EINE markierte Kunde-Zeile (ewb_button) bei sonst nur Berater-Zeilen, meeting=getrennt; Routing/Toggle intakt (kein cold_call/meeting-Live-Toggle); org_id gesetzt; grep _session_modes == 0" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 1. Lokaler Kontext
`mode_strategy.py` + `test_mode_registry.py` = neue Dateien (kein bestehender Control-Flow). Insert-/Edit-Sites: deepgram_service.py (:18/:437/:445 Dict-Loeschung, :524-527 Default, :833 Button-Read, :847-853 Button-intent_event-Ausnahme, **NEU :853+ Button-Transcript-Zeile Refinement 1**, :922-963 Gatekeeper-Achse-Caveat Refinement 2); claude_service.py (:974 Medium-Read, :1011-1026 Medium-Emit/Bug, :1520 A/B-Read, :1534-1543 A/B-Emit); einwand_keyword_matcher.py (:280 Read, :297-302 Emit); live_session.py (:302 Default). **Pflicht beim Bau:** je Site 30 Zeilen vor/nach VERBATIM + return/continue/raise listen. RE-GREP zuerst (Banner). Welle 4 + MEDFIX haben diese Stellen seit dem stale Plan 07 (10.06.) veraendert — die Anker oben sind gegen HEAD 6e98781 frisch verifiziert.

## 2. Funktions-Skelett
- **analyse_loop / Medium-Lane (claude:962→1028):** analysiere_mit_claude (SYSTEM_PROMPT_BASE, :523) → ergebnis → [mode = per-SID-Read, Task 2] → ergebnis.get('einwand') → [INSERT: _strategy = MODE_REGISTRY.get(mode) or cold_call; _attr = _strategy.extract_intent(...), Task 3] → emit_intent_event(mode, speaker_role=_attr[...], speaker_id, inference_basis, ...). Der Bug-Block :1011-1013 (cold_call→'kunde') faellt weg.
- **A/B-abstain (claude:1511-1545):** _emit_abstain_event → [mode per-SID] → _strategy.extract_intent → emit (speaker_role aus _attr statt hardcoded :1541).
- **Fast-Lane (matcher:269-306):** Keyword-Match → [mode per-SID, :280] → _strategy.extract_intent → emit (speaker_role aus _attr statt :300 hardcoded).
- **EWB-Button (deepgram:813-919, Refinement 1):** Klick → typ=data.get('text') (:819) → [mode per-SID, :833] → (i) emit_intent_event mit speaker_role='kunde' FEST (NICHT ueber Registry — erlaubte Ausnahme, :847-853 UNVERAENDERT) → (ii) **NEU: anonymize(typ) → wenn kein Sentinel: with ls.log_lock: ls.conversation_log.append({type:'transcript', speaker:1(Kunde-INT), text:_anon_typ, data:{'ewb_button':True}})** (eigener Block, nicht mit _session_state_lock genested) → bestehender Variante-Stream/record_ewb_click (:876+) UNVERAENDERT.
- **ModeStrategy-Klassen (neu):** extract_intent → dict {speaker_role, speaker_id, inference_basis, confidence}. ColdCall: fest berater/local/advisor_paraphrase + Cap. Meeting: speaker→role. Steckplaetze: NotImplementedError. KEIN get_classification_prompt. Namens-Caveat (cold_call in 2 Achsen).
- **handle_manual_mode_toggle (deepgram:922-963, Refinement 2):** setzt state['current_mode']/contact_category (Gatekeeper/Kontakt-Achse) — beruehrt _session_modes NICHT, wird NICHT ins Register migriert (belegt + Caveat-Kommentar). ORTHOGONAL zur cold_call/meeting-Achse.
- **Consent-Rueckfall (Refinement 2):** existiert HEUTE NICHT als Laufzeit-mode-Flip (belegt, nicht erfunden). live_session:471 = Moment-Fenster-Reset-Kommentar. Falls zukuenftig gebaut → muss per-SID _session_state[sid]['mode'] schreiben.

## 3. Cross-File-Awareness
- `grep -rn "_session_modes" services/` — VOR Bau (Erwartung 9 Treffer), NACH Bau == 0 (globales Dict + alle Reads weg). DER §0.1-Konsolidierungs-Beleg (Punkt 22). Betrifft NUR die cold_call/meeting-Achse.
- `grep -rn "current_mode\|contact_category" services/deepgram_service.py` — VOR/NACH Bau gleiche Treffer-Zahl (Refinement 2: Gatekeeper-Achse unberuehrt, nur ggf. Caveat-Kommentar dazu).
- `grep -rn "MODE_REGISTRY\|ModeStrategy\|mode_strategy" services/ routes/` — nach Bau: mode_strategy.py + 3 Lookups (Medium/A-B/Fast). Registry = Single-Source des Routings.
- `grep -rn "ewb_button\|conversation_log.append" services/deepgram_service.py` — nach Bau (Refinement 1): die NEUE Button-Transcript-Zeile zusaetzlich zum kanonischen Append (:130-135). Marker round-trippt RAM->DB->/api/transcript.
- `grep -rn "\.get('mode'" services/` — nach Bau: per-SID-Reads (Button/Medium/A-B/Fast + die schon bestehenden claude:1152/1224). Eine Quelle: _session_state[sid]['mode'].
- `grep -rn "emit_intent_event" services/` — Welle 4 (writer:32) + 4 Aufrufer; die Strategy liefert die speaker-Felder dorthin. Ein writer, Registry liefert die Werte.
- `grep -rn "SYSTEM_PROMPT_BASE" services/claude_service.py` — :32/:523; bleibt der EINE Prompt (Decision 1). mode_strategy.py darf KEINEN konkurrierenden Prompt definieren.
- Cross-Thread: deepgram-Callback (Button, inkl. neuer Transcript-Append), analyse_loop (Medium), Daemon/Matcher (Fast) laufen in verschiedenen Pfaden — alle lesen DIESELBE per-SID-mode-Quelle. conversation_log-Appends laufen alle unter ls.log_lock (gesprochener Pfad + Button-Pfad). MODE_REGISTRY ist prozess-global (Registrierung beim Import), zustandslose Instanzen → thread-safe.

## 4. Edge-Case-Analyse
- **Edge 1 — mode nicht in MODE_REGISTRY (alter/unbekannter Modus):** `MODE_REGISTRY[mode]` → KeyError. Behandlung: `MODE_REGISTRY.get(mode) or MODE_REGISTRY['cold_call']` (Default cold_call, Decision 4) → kein Crash.
- **Edge 2 — Steckplatz-Modus live aktiviert (meeting_ext etc.):** extract_intent wirft NotImplementedError → koennte Live-Loop crashen. Behandlung: die 3 Steckplaetze sind NICHT UI-waehlbar (nur cold_call/meeting real); zusaetzlich try-except um den Strategy-Aufruf → faellt auf cold_call-Default zurueck (T-TAXO1-21). Test deckt NotImplementedError ab.
- **Edge 3 — per-SID mode noch nicht gesetzt (Race Call-Start):** per-SID-Read-Default 'cold_call' greift → ColdCallStrategy. Kein Crash, sichere Annahme (Decision 4).
- **Edge 4 — emit_intent_event ValueError (speaker_role leer):** extract_intent liefert IMMER ein non-empty speaker_role (berater/kunde) → kein ValueError. Falls Strategy-Aufruf scheitert (Edge 2 try-except), Default cold_call → berater. Der bestehende try-except um emit (claude:1027, matcher:303, deepgram:854) schluckt Rest (Live-Loop crasht nie, Edge-3-Vertrag des writers).
- **Edge 5 (Refinement 1) — EWB-Button-Anonymisierung-Sentinel/Fehler:** `anonymize(typ)` liefert '[ART9_REDACTED]'/'[ANON_FEHLER]' oder wirft → die Transcript-Zeile wird NICHT geschrieben (kein DB-Spam, wie der gesprochene Pfad), ABER der intent_event-Emit (lief schon :853) BLEIBT. try-except um den Append-Block → Live-Loop crasht nie. `typ` ist ein Typ-Label ('zu_teuer') → Art-9-Sentinel praktisch unwahrscheinlich, aber defensiv behandelt.
- **Edge 6 (Refinement 1) — Persist-Pfad rendert `data` NICHT (BEWIESEN, Plan-Checker):** `_build_log_content` (live_session:988-1020) rendert fuer `type=='transcript'`-Eintraege NUR `text`+`speaker` (sp_map :994); `data` wird nur fuer `type=='analyse'` gelesen (:1022). → Der strukturelle `data`-Flag ueberlebt den Export NACHWEISLICH nicht. **Deshalb ist der `*ewb button*`-Text-Suffix (ueber `entry['text']`) der kanonische Export-Marker** — er ist garantiert sichtbar. Der `data`-Flag bleibt als RAM-/Analyse-Zusatz erlaubt, ist aber nicht der Export-Marker. Executor bestaetigt am Gate per inspect.sh den Suffix im Export (kein Verlass aufs data-Feld).

### Race-Condition-Audit
1. **Gleichzeitige Calls verschiedene Modi:** jeder Pfad liest seine per-SID-mode → MODE_REGISTRY[mode] read-only, Strategy-Instanzen zustandslos (kein Instanz-State) → kein Cross-Call-Konflikt. Pflicht beim Bau: extract_intent bekommt ALLES via Argumente, schreibt NICHTS auf self.
2. **Verbindungsabbruch mid-Operation:** Strategy-Aufruf bricht ab → kein emit (try-except), kein halber Zustand (zustandslos). per-SID-mode bleibt konsistent. EWB-Button-Append unter log_lock atomar (kein halber conversation_log-Eintrag).
3. **Wiederverbinden mit altem Zustand:** neue SID → frischer per-SID-mode via init_session_state (pop+init, deepgram:633-636). MODE_REGISTRY prozess-global, kein SID-Bezug → kein Alt-Eintrag. Toggle mid-Call (manual_mode_toggle) aendert state['current_mode'] (Kontakt-Achse), NICHT die pipeline-mode — der pipeline-mode (cold_call/meeting) bleibt der bei Call-Start gesetzte (kein Live-Toggle, Refinement 2); bewusst (Diarize/Audibility wechselt nicht mid-Call).
4. **Tab im Hintergrund:** Modus ist server-seitig per-SID; kein veralteter Client-State erzwingt falschen Modus.
5. **_session_modes-Loeschung vs. laufender Call:** Deploy startet den Worker neu (deploy.sh) → kein Live-Call ueberlebt den Restart → kein Read auf das geloeschte Dict aus einem alten Call. Neue Calls nach Restart nutzen ausschliesslich per-SID.
6. **EWB-Button-Append vs. gesprochener Transcript-Append (Refinement 1):** beide unter `ls.log_lock` → serialisiert, kein Race auf conversation_log. Der Button-Append nutzt EIGENEN log_lock-Block (NICHT mit _session_state_lock genested) → kein Lock-Ordering-Deadlock mit dem :838-846-Block.

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen / Persistenz-Schichten
- `intent_event` — SCHREIBEN via emit_intent_event (Welle-4-Pfad; hier nur die WERTE-Quelle fuer speaker_role/speaker_id/inference_basis geaendert). Spalte `mode` (varchar), payload_jsonb-Keys `speaker_role`/`speaker_id`/`inference_basis`. Schema unveraendert (Welle 1).
- `conversation_logs` — SCHREIBEN (Refinement 1): die EWB-Button-Transcript-Zeile landet zuerst in RAM `ls.conversation_log` (per-SID-Mirror) und wird beim Session-Ende in die bestehende conversation_logs-Persistenz geschrieben — UNVERAENDERTES Schema, nur ein zusaetzlicher Listen-Eintrag mit `data={'ewb_button': True}` im bestehenden JSON-Struktur-Feld. KEINE neue Spalte. `conversation_logs.session_mode` — LESEN (bestehende Modus-Persistenz), UNVERAENDERT.
- mode-Wert (cold_call/meeting-Achse) lebt primaer in **RAM**: `_session_state[sid]['mode']` (per-SID, Decision 3, Call-Start-only). Das geloeschte `_session_modes`-Dict war eine REDUNDANTE RAM-Kopie (Split-Brain) — keine DB-Schicht betroffen.
- `state['current_mode']`/`contact_category` (Gatekeeper-Achse, Refinement 2) — RAM (per-SID state), UNVERAENDERT, NICHT konsolidiert.
- **Anti-K1/K8-Falle:** `mode` ist ein RAM-Key (`_session_state[sid]['mode']`), KEINE eigene DB-Spalte mit diesem Namen. Der DB-Persistenz-Pfad fuer den Modus ist `conversation_logs.session_mode` (anderer Name!) + `intent_event.mode`. Code-Variable ≠ DB-Spalte — bewusst getrennt dokumentiert.
- **EWB-Button-Marker-Falle (Refinement 1) — AUFGELOEST (Plan-Checker-Beweis):** Der `data={'ewb_button': True}`-Dict-Key ueberlebt den Export NICHT — `_build_log_content` (live_session:988-1020) rendert transcript-Eintraege nur aus `text`+`speaker`, liest `data` nur fuer `analyse`-Eintraege (:1022). Kanonischer Export-Marker ist daher der `*ewb button*`-Suffix im `text` (Cross-Layer-konsistent ueber `entry['text']`). Der `data`-Flag bleibt als RAM-Zusatz, traegt aber NICHT den Export. Gate-Beleg: inspect.sh zeigt den Suffix in der exportierten Zeile.

### inspect.sh-Beleg (Platzhalter — beim Gate-Task verbatim fuellen)
```
# intent_event-Schema (mode = Spalte; speaker_role/speaker_id/inference_basis = payload_jsonb-Keys):
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema intent_event'
# <verbatim in SUMMARY>

# conversation_logs-Schema (Refinement 1 — bestaetigen wo der Transcript-Listen-Eintrag + data-Marker persistiert):
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema conversation_logs'
# <verbatim in SUMMARY — belegt, dass die Transcript-Zeile in bestehende Persistenz geht, keine neue Spalte>

# Nach Test-Anruf BEIDE Modi:
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample intent_event 20'
# <verbatim: cold_call=berater/local/advisor_paraphrase, button=kunde/ui_asserted, meeting=getrennt>

# EWB-Button-Transcript-Round-Trip (Refinement 1) — die markierte Kunde-Zeile im Export:
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <transcript-read des Test-Calls / sample conversation_logs>'
# <verbatim: GENAU EINE Kunde-Zeile mit ewb_button-Marker, alle gesprochenen cold_call-Zeilen Berater>
```

### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| mode (cold_call/meeting) | per-SID read (_session_state[sid]['mode'], Call-Start-only) → MODE_REGISTRY → emit | RAM (per-SID) → DB intent_event.mode | ⬜ Test-Anruf: sample zeigt cold_call/meeting |
| speaker_role / speaker_id | Strategy.extract_intent → emit payload | DB intent_event.payload_jsonb | ⬜ Test-Anruf: cold_call=berater/local, meeting getrennt, button=kunde |
| inference_basis | Strategy.extract_intent → emit payload | DB intent_event.payload_jsonb | ⬜ advisor_paraphrase (cold_call) / direct_customer_utterance (meeting) / ui_button (Button) |
| EWB-Button-Transcript-Zeile | handle_manual_ewb append (Refinement 1) | RAM conversation_log (log_lock) → DB conversation_logs → /api/transcript export | ⬜ verify marker (ewb_button) round-trips; genau EINE Kunde-Zeile |
| state['current_mode'] / contact_category (Gatekeeper) | unveraendert (Refinement 2) | RAM (per-SID state) | ✓ nicht angefasst (orthogonale Achse) |
| Consent-Rueckfall-mode-Flip | existiert HEUTE NICHT (Refinement 2) | — | ✓ belegt: kein Write; live_session:471 = Moment-Reset-Kommentar |
| _session_modes (global, A4) | GELOESCHT | RAM (war redundante Kopie) | ⬜ grep == 0 |
| conversation_logs.session_mode | unveraendert | DB-Spalte | ✓ nicht angefasst |

### Bei Diskrepanz (intent_event.mode falsch / speaker_role im cold_call = 'kunde' statt 'berater' / _session_modes-Read bleibt / EWB-Button-Marker ueberlebt den DB-Persist nicht / mehr als eine Kunde-Zeile im cold_call-Transcript): STOP + Registry-Verdrahtung/Konsolidierung/Button-Append pruefen.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| per-SID mode (RAM) → MODE_REGISTRY-Routing | Modus (cold_call/meeting-Achse) bestimmt Sprecher-Zuordnung + Audibility; muss per-SID-konsistent sein (§0.1). Call-Start-only, kein Live-Toggle (Refinement 2). |
| Strategy.extract_intent → intent_event | speaker_role/speaker_id getaggt; cold_call FEST berater/local (kein Kunden-Audio im cold_call — DSGVO; Berater-Paraphrase darf NICHT als Kunde gebucht werden). |
| EWB-Button (ui_asserted) → intent_event + conversation_log | Die EINZIGE Stelle, die im cold_call speaker_role/Transcript-Speaker='kunde' schreiben darf (Berater drueckt Knopf, Kunde-Einwand markiert). Refinement 1: zusaetzlich eine markierte Transcript-Zeile (anonymisiert). |
| EWB-Button-Text → conversation_log → DB → /api/transcript | Refinement 1: Knopf-Text (Typ-Label) wird anonymisiert (Sentinel-Skip) und mit ewb_button-Marker persistiert; Marker muss round-trippen. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO1-20 | I (Info Disclosure) | _session_modes global + per-SID parallel → Modus-Split-Brain ueber SIDs (falscher Modus, DSGVO: cold_call hoert nur Berater) | mitigate | **A4=YES erledigt:** globales _session_modes GELOESCHT, alle Reads per-SID; grep == 0 belegt. cold_call fest berater/local. Refinement 2: Call-Start-only, kein Live-Toggle. |
| T-TAXO1-21 | D (DoS) | unbekannter/Steckplatz-Modus → KeyError/NotImplementedError crasht Live-Loop | mitigate | MODE_REGISTRY.get(mode) or cold_call-Default; try-except um Strategy-Aufruf; Steckplaetze nicht UI-waehlbar; Test deckt ab. |
| T-TAXO1-22 | T (Tampering) | Strategy-Instanz haelt per-Call-State → Cross-Call-Leak zwischen parallelen SIDs | mitigate | zustandslose Strategien (Kontext NUR via Argumente, nichts auf self); Race-Audit Punkt 1; Test parallele Registrierung. |
| T-TAXO1-23 | T (Tampering) | Daten-Vergiftung: Berater-Paraphrase im cold_call als 'kunde' gebucht (Trainings-Korpus DPO falsch) | mitigate | ColdCallStrategy.extract_intent FEST berater/advisor_paraphrase; Bug-Fix an 3 Bahnen; Button bleibt die einzige bewusste kunde-Ausnahme. Test test_cold_call_speaker_is_berater. |
| T-TAXO1-24 | I (Info Disclosure) | EWB-Button-Transcript-Zeile (Refinement 1) persistiert Knopf-Text un-anonymisiert / Art-9-Daten ins Transcript | mitigate | anonymize(typ) ZUERST (gleiche Behandlung wie gesprochener Pfad); Sentinel '[ART9_REDACTED]'/'[ANON_FEHLER]' → Zeile NICHT schreiben (kein DB-Spam); try-except. `typ` ist ein Typ-Label, geringes PII-Risiko, aber defensiv anonymisiert. |
| T-TAXO1-25 | R (Repudiation) | EWB-Button-Transcript-Zeile ununterscheidbar von gesprochener Kunde-Aussage (falsche Korpus-/Export-Interpretation) | mitigate | Marker `data={'ewb_button': True}` (struktureller Flag) + optional Text-Suffix-Fallback; Round-Trip RAM→DB→/api/transcript belegt; Gate-Test-Anruf zeigt genau EINE markierte Kunde-Zeile. |
</threat_model>

<verification>
- mode_strategy.py: ABC (setup_audio_routes + extract_intent, KEIN get_classification_prompt) + Registry + ColdCall/Meeting + 3 Steckplaetze; cold_call fest berater/local/advisor_paraphrase + Cap; Namens-Caveat (cold_call in 2 Achsen); kein Air-Gap-Bus.
- §0.1: globales _session_modes geloescht (grep == 0); alle 4 Reads per-SID; Default 'cold_call'; Call-Start-only (kein Live-Toggle); Gatekeeper-Achse unberuehrt + belegt; Consent-Rueckfall-Realitaet ehrlich belegt (existiert nicht).
- Sprecher-Bug behoben: 3 LLM/KW-Bahnen routen ueber Registry (cold_call=berater); Button-intent_event = einzige kunde-Ausnahme.
- Refinement 1: EWB-Button schreibt genau EINE markierte Kunde-Transcript-Zeile (anonymisiert, log_lock, ewb_button-Marker, Round-Trip RAM→DB→/api/transcript belegt); alle gesprochenen cold_call-Zeilen bleiben Berater.
- Struktur-Test GRUEN (neuer Modus ohne Kern-Aenderung; cold_call=berater; Steckplaetze NotImplemented; ABC ohne classification_prompt).
- Test-Anruf beide Modi: intent_event traegt korrekten mode/speaker_role/speaker_id/inference_basis; EWB-Button-Transcript-Zeile sichtbar+markiert; Routing/Toggle intakt; org_id gesetzt.
</verification>

<success_criteria>
- Registry existiert; cold_call + meeting echte Klassen; neuer Modus = neue Klasse ohne Kern-Aenderung (REQ 6).
- intent_event traegt mode + speaker_role + speaker_id bei JEDEM Event (REQ 6); cold_call = berater/local (Decision 2).
- 3 dokumentierte Steckplaetze; kein Training-Air-Gap-Bus im Code (REQ 6).
- §0.1-mode konsolidiert (cold_call/meeting-Achse): globales _session_modes geloescht, per-SID einzige Quelle, Default 'cold_call', Call-Start-only (Decision 3+4 + Refinement 2). Gatekeeper-Achse separat/unberuehrt.
- Refinement 1: EWB-Button-Knopf-Einwand erscheint als genau EINE markierte Kunde-Zeile im Cold-Call-Transcript-Export (anonymisiert); gesprochene Zeilen bleiben Berater.
- Klassifikation bleibt EIN Prompt (SYSTEM_PROMPT_BASE) — MEDFIX nicht re-gebrochen (Decision 1).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-07-SUMMARY.md` (inkl. Struktur-Test-GREEN + sample intent_event BEIDE Modi verbatim (mode/speaker_role/speaker_id/inference_basis; cold_call=berater, button=kunde/ui_asserted, meeting=getrennt) + EWB-Button-Transcript-Round-Trip-Beleg (genau EINE markierte Kunde-Zeile, ewb_button-Marker, alle gesprochenen cold_call-Zeilen Berater, DB-Persist-Pfad lokalisiert) + §0.1-Beleg `grep _session_modes == 0` + Bestaetigung handle_manual_mode_toggle/Gatekeeper-Achse unberuehrt + Namens-Caveat im Code + Bestaetigung Consent-Rueckfall existiert heute nicht (nicht erfunden, live_session:471 = Moment-Reset-Kommentar) + Bestaetigung kein Air-Gap-Bus).
</output>
