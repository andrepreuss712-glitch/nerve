---
phase: 08.23.2.TAXO1
plan: 07
type: execute
wave: 7
complexity: 🔴
depends_on: [04]
files_modified: [services/mode_strategy.py, services/claude_service.py, services/deepgram_service.py, tests/test_mode_registry.py]
autonomous: false
requirements: [6]
risiko: riskant
test_anruf: true
user_setup: []
must_haves:
  truths:
    - "services/mode_strategy.py definiert ModeStrategy(ABC) mit setup_audio_routes()/get_classification_prompt()/extract_intent() + MODE_REGISTRY + register()."
    - "cold_call + meeting sind echte registrierte Strategy-Klassen; ein neuer Modus = neue Klasse ohne Kern-Code-Aenderung (Struktur-Beleg)."
    - "meeting_ext + training_cold + training_meeting sind dokumentierte Registry-Steckplaetze (NotImplemented-Platzhalter); KEIN Training-Air-Gap-Bus im Code."
    - "intent_event traegt mode + speaker_role + speaker_id bei jedem Event (Contract so geschnitten, dass die 3 kuenftigen Modi ohne Schnittstellen-Umbau passen; cold_call=fest berater/local)."
    - "Modus-Routing liest die per-SID-mode-Quelle; falls _session_modes (global dict) UND per-SID-mode parallel leben (§0.1-Verletzung, A4) → auf per-SID konsolidiert, globales dict geloescht."
  artifacts:
    - path: "services/mode_strategy.py"
      provides: "ModeStrategy(ABC) + ColdCallStrategy + MeetingStrategy + MODE_REGISTRY + 3 Steckplaetze"
      contains: "class ModeStrategy"
    - path: "services/claude_service.py"
      provides: "Medium-Lane nutzt MODE_REGISTRY[mode].get_classification_prompt()/extract_intent()"
      contains: "MODE_REGISTRY"
    - path: "tests/test_mode_registry.py"
      provides: "Struktur-Test: neuer Modus = neue Klasse ohne Kern-Aenderung; cold_call/meeting registriert"
      contains: "def test_"
  key_links:
    - from: "session-mode (per-SID, live_session:995)"
      to: "MODE_REGISTRY[mode] → Strategy"
      via: "Registry-Lookup"
      pattern: "MODE_REGISTRY\\["
    - from: "ColdCallStrategy.extract_intent"
      to: "intent_event (speaker_role=berater, speaker_id=local)"
      via: "emit_intent_event (Welle 4)"
      pattern: "berater"
    - from: "_session_modes (global dict, deepgram:18, ggf. GELOESCHT)"
      to: "per-SID mode (live_session:995)"
      via: "§0.1-Konsolidierung (falls A4 bestaetigt)"
      pattern: "_session_modes"
---

> RISIKO-BANNER: **riskant.** Aendert das Modus-Routing (Registry statt loses _session_modes-Dict), faedelt mode/speaker durch den Cutover-Emit. → **Test-Anruf PFLICHT (beide Modi cold_call + meeting; intent_event traegt korrekten mode/speaker_role/speaker_id).** Strukturell isoliert (RESEARCH §10) — profitiert vom fertigen intent_event-Contract (Welle 4).

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Geplant, nicht sofort ausgefuehrt. Deploy + Test-Anruf erst nach Cross-AI-Clearance + André-Ineinandergreifen-Check.

> 🔎 **VOR-EXECUTE-RE-GREP (RESEARCH Open Question 3 / A4):** `grep -rn "_session_modes\|session_mode\|handle_manual_mode_toggle\|'mode'" services/deepgram_service.py services/live_session.py services/claude_service.py` erneut. **A4 ENTSCHEIDEN:** leben `_session_modes` (deepgram:18/427/435 global dict) UND `_session_state[sid]['mode']` (live_session:995 per-SID) parallel als zwei Quellen fuer dasselbe Datum? Wenn JA → §0.1-Verletzung, auf per-SID konsolidieren + globales dict loeschen (in dieser Welle). Wenn NEIN (per-SID ist die einzige lebende Quelle, _session_modes nur Schreib-Durchgang) → Registry liest per-SID, dokumentieren dass kein §0.1-Konflikt.

<objective>
Modus als First-Class-Dimension ueber ein Strategy-Pattern bauen (REQ 6): ModeStrategy-Registry (`setup_audio_routes()`, `get_classification_prompt()`, `extract_intent()`) mit cold_call + meeting als echten Klassen; meeting_ext + training_cold + training_meeting als zukunftssichere Steckplaetze (NotImplemented). Den intent_event-Contract (mode + speaker_role + speaker_id) so schneiden, dass die 3 kuenftigen Modi ohne Schnittstellen-Umbau passen (cold_call = fest berater/local). KEIN Training-Air-Gap-Bus.

Purpose: Geruest §2: Audibility-Contract als deklarative Routing-Tabelle; neuer Modus = neue Klasse registrieren, Kern-Code aendert sich nicht. Heute nur loses `_session_modes`-Dict, keine Registry, kein Routing-Contract. Diese Welle ist strukturell isoliert und nutzt den fertigen intent_event-Contract aus Welle 4.

Output: services/mode_strategy.py (Registry + 2 Klassen + 3 Steckplaetze), Verdrahtung in claude_service (Medium Lane nutzt MODE_REGISTRY), ggf. §0.1-Konsolidierung _session_modes, tests/test_mode_registry.py. Deploy + Test-Anruf — GATED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-RESEARCH.md
@.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-04-SUMMARY.md
@services/deepgram_service.py
@services/live_session.py
@services/claude_service.py

<interfaces>
<!-- RESEARCH §5 (grep verbatim): -->
<!-- deepgram_service.py:18  _session_modes = {}   # {sid: 'cold_call'|'meeting'} -->
<!-- deepgram_service.py:427  _session_modes[sid] = mode ; :880 handle_manual_mode_toggle(data=None) -->
<!-- live_session.py:995  mode = (ls._session_state.get(sid) or {}).get('mode', 'meeting')   ← per-SID -->
<!-- conversation_logs.session_mode (DB-Spalte, persistiert) -->
<!-- Nur cold_call + meeting real. Keine Registry, kein Routing-Contract. -->
<!-- Empfehlung (RESEARCH §5): ModeStrategy(ABC): setup_audio_routes(), get_classification_prompt(), extract_intent(...). MODE_REGISTRY: dict + register(mode_key, cls)-Decorator. -->
<!-- ColdCallStrategy: Echo-Detektion aus Berater-Paraphrase; Platzhalter-Pflicht; unknown/Abstention erlaubt; Konfidenz modus-gedeckelt; speaker fest berater/local. -->
<!-- MeetingStrategy: direkt aus Kunden-Aussage, Sprecher-Trennung. -->
<!-- 3 Steckplaetze meeting_ext/training_cold/training_meeting = dokumentierter Registry-Eintrag (NotImplemented). KEIN Air-Gap-Bus (Geruest §6). -->
<!-- Welle 4: emit_intent_event(mode, speaker_role, speaker_id, ...) existiert — die Strategy liefert mode/speaker. -->
<!-- Geruest §3: speaker_role berater/kunde/gatekeeper/system; speaker_id UUID/local; cold_call default berater/local. -->
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/mode_strategy.py — ModeStrategy(ABC) + Registry + ColdCall/Meeting + 3 Steckplaetze (neue Datei)</name>
  <read_first>
    - RESEARCH §5 (Registry-Empfehlung, Methoden-Signaturen, cold_call-Regeln)
    - Geruest §2 (Modi-Tabelle, Audibility-Contract, ModeStrategy) + §3 (speaker_role/speaker_id)
    - services/intent_event_writer.py (emit_intent_event-Signatur — extract_intent liefert die Felder)
    - SPEC REQ 6 Acceptance (Registry, 2 echte Klassen, 3 Steckplaetze, kein Air-Gap-Bus)
  </read_first>
  <behavior>
    - Test 1: MODE_REGISTRY enthaelt 'cold_call' und 'meeting' als registrierte Strategy-Klassen.
    - Test 2: register('test_mode', TestStrategy) fuegt eine neue Klasse hinzu OHNE Aenderung am Registry-Kern (neuer Modus = neue Klasse).
    - Test 3: ColdCallStrategy liefert speaker_role='berater', speaker_id='local' (fest, Geruest §3); erlaubt unknown/Abstention.
    - Test 4: MeetingStrategy liefert speaker_role aus Sprecher-Trennung (berater/kunde).
    - Test 5: MODE_REGISTRY enthaelt dokumentierte Steckplaetze meeting_ext/training_cold/training_meeting, die bei Aufruf NotImplementedError werfen (zukunftssicher, kein Air-Gap-Bus).
  </behavior>
  <action>
    NEUE Datei `services/mode_strategy.py`:
    - `class ModeStrategy(ABC):` abstrakte Methoden `setup_audio_routes(self, ...)`, `get_classification_prompt(self, ...)`, `extract_intent(self, ...) -> dict` (liefert mode/intent_type/speaker_role/speaker_id/confidence-Kandidaten fuer emit_intent_event).
    - `MODE_REGISTRY: dict[str, ModeStrategy] = {}` + `def register(mode_key): def deco(cls): MODE_REGISTRY[mode_key] = cls(); return cls; return deco` (Decorator) ODER `register(mode_key, cls)`-Funktion.
    - `@register('cold_call') class ColdCallStrategy(ModeStrategy):` Echo-Detektion aus Berater-Paraphrase; Platzhalter-Pflicht (nichts erfinden); unknown/Abstention erlaubt+erwuenscht; Konfidenz modus-gedeckelt (cold_call haerter als meeting — z.B. confidence-Cap); `extract_intent` setzt speaker_role='berater', speaker_id='local' FEST.
    - `@register('meeting') class MeetingStrategy(ModeStrategy):` direkt aus Kunden-Aussage; Sprecher-Trennung (sp_map {0:berater,1:kunde}, live_session:886); speaker_role aus dem erkannten Sprecher.
    - 3 Steckplaetze: `@register('meeting_ext')`/`@register('training_cold')`/`@register('training_meeting')` als dokumentierte Platzhalter-Klassen die in den Methoden `raise NotImplementedError("Steckplatz: Ausbau spaetere Phase, kein Air-Gap-Bus in TAXO1")`. Klar kommentiert: "Zukunftssicherer Registry-Eintrag (Geruest §2). KEIN Training-Air-Gap-Event-Bus (Geruest §6) — nur Interface aufnahmefaehig."
    - KEIN In-Prozess-Event-Bus, KEINE audio.*/sim.internal.*-Topics (Air-Gap = spaetere Phase).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. Unit-Test server-seitig via Task 4. AST-Parse lokal erlaubt.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class ModeStrategy" services/mode_strategy.py` + `grep -E "def setup_audio_routes|def get_classification_prompt|def extract_intent" services/mode_strategy.py` (alle 3 Methoden).
    - `grep -E "class ColdCallStrategy|class MeetingStrategy" services/mode_strategy.py` (2 echte Klassen) + `grep -E "meeting_ext|training_cold|training_meeting" services/mode_strategy.py` (3 Steckplaetze).
    - ColdCall fest berater/local: `grep -E "'berater'|'local'" services/mode_strategy.py` 1+ Treffer.
    - KEIN Air-Gap-Bus: `grep -iE "event_bus|sim.internal|audio\.\*|topic.*namespace" services/mode_strategy.py` == 0.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Registry + 2 echte Strategien + 3 Steckplaetze; cold_call fest berater/local; kein Air-Gap-Bus.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Medium-Lane verdrahten (claude_service nutzt MODE_REGISTRY) + §0.1-Konsolidierung _session_modes (falls A4 bestaetigt)</name>
  <read_first>
    - ⚠️ RE-GREP + A4-Entscheidung (Banner)
    - services/claude_service.py (analyse_loop Emit-Stelle aus Welle 4 — mode/speaker kamen dort als per-SID-Default) — 30 Zeilen vor/nach VERBATIM
    - services/deepgram_service.py:18/427/435/880 (_session_modes global dict + toggle) — 30 Zeilen vor/nach VERBATIM
    - services/live_session.py:995 (per-SID mode-Read) / :886 (sp_map)
    - RESEARCH §5 Open Question 3 / A4
  </read_first>
  <action>
    Punkt-14.
    (a) **Medium-Lane-Verdrahtung:** an der Welle-4-Emit-Stelle (analyse_loop) statt des per-SID-mode-Defaults jetzt `strategy = MODE_REGISTRY[mode]` (mode aus per-SID-Quelle live_session:995) → `strategy.get_classification_prompt()` fuer den Haiku-Prompt + `strategy.extract_intent(...)` liefert mode/speaker_role/speaker_id an `emit_intent_event(...)`. cold_call → berater/local; meeting → Sprecher-getrennt. So traegt JEDES intent_event mode+speaker_role+speaker_id (REQ 6 Acceptance).
    (b) **§0.1-Konsolidierung (A4):** Wenn das RE-GREP zeigt dass `_session_modes` (global dict) UND `_session_state[sid]['mode']` parallel leben (beide geschrieben+gelesen) → §0.1-Verletzung. Dann: auf die per-SID-Quelle konsolidieren (Registry + alle Leser lesen `_session_state[sid]['mode']`), das globale `_session_modes`-dict + seine Schreibungen (deepgram:427) LOESCHEN, handle_manual_mode_toggle (:880) auf per-SID umstellen. **Wenn A4 NEIN** (per-SID ist einzige lebende Quelle, _session_modes nur Schreib-Durchgang ohne lebende Leser) → dokumentieren + minimal lassen (Registry liest per-SID). **Entscheidung mit grep-Beleg begruenden (Punkt 22), nicht raten.**
    KEINE Aenderung an sp_map/Sprecher-Trennung-Logik (live_session:886) ausser der Lese-Quellen-Konsolidierung.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Task 4): intent_event traegt mode/speaker korrekt je Modus.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MODE_REGISTRY\[" services/claude_service.py` zeigt den Registry-Lookup in der Medium Lane.
    - A4-Entscheidung dokumentiert in SUMMARY mit grep-Beleg: entweder `_session_modes` global geloescht (`grep -n "_session_modes" services/deepgram_service.py` == 0 ausser ggf. Definition-weg) ODER Beleg dass keine lebende Doppel-Quelle existiert (per-SID einzige Quelle).
    - `git diff services/live_session.py` zeigt KEINE Aenderung an sp_map/Sprecher-Trennung (nur ggf. Lese-Quelle).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Medium Lane nutzt die Registry; mode/speaker je Modus korrekt; §0.1-mode-Status entschieden (konsolidiert oder belegt sauber).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: tests/test_mode_registry.py — Struktur-Test (neuer Modus ohne Kern-Aenderung; cold_call/meeting registriert)</name>
  <read_first>
    - CLAUDE.md Test-Qualitaets-Regel (Integration/Struktur-Assertion, kein Source-Presence)
    - services/mode_strategy.py (MODE_REGISTRY, register, Klassen)
  </read_first>
  <action>
    NEUE Datei `tests/test_mode_registry.py` (Struktur-/Runtime-Assertion, KEIN getsource/grep-auf-Quelle):
    - `def test_cold_call_and_meeting_registered():` assert 'cold_call' in MODE_REGISTRY and 'meeting' in MODE_REGISTRY (Runtime-Registry-Inhalt).
    - `def test_new_mode_registers_without_core_change():` definiert eine Dummy-Strategy, ruft register('x_test', ...), assert MODE_REGISTRY['x_test'] vorhanden + die anderen Eintraege unveraendert (neuer Modus = neue Klasse, Kern unberuehrt). Cleanup danach (pop).
    - `def test_cold_call_speaker_fixed():` ColdCallStrategy.extract_intent(...) liefert speaker_role=='berater', speaker_id=='local' (Runtime-Return-Assertion).
    - `def test_future_slots_raise_not_implemented():` MODE_REGISTRY['meeting_ext'].get_classification_prompt(...) wirft NotImplementedError (Steckplatz).
    Server-seitig lauffaehig; skip sauber bei fehlender Abhaengigkeit (kein False-Green).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. Lauf via Task 4: `ssh ... pytest tests/test_mode_registry.py -x`. AST-Parse lokal erlaubt.</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def test_new_mode_registers_without_core_change" tests/test_mode_registry.py` 1 Treffer.
    - KEIN Source-Presence: `grep -E "getsource|open\(.*mode_strategy.*\)\.read|\.count\('" tests/test_mode_registry.py` == 0.
    - Tests asserten Runtime-Registry-Inhalt + Return-Werte (Integration), nicht Code-Existenz.
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>Struktur-Test beweist: neuer Modus ohne Kern-Aenderung, cold_call/meeting registriert, Steckplaetze werfen NotImplemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ModeStrategy-Registry + ColdCall/Meeting + 3 Steckplaetze + Medium-Lane-Verdrahtung + §0.1-mode-Entscheidung + Struktur-Test. Bereit fuer Deploy + Test-Anruf (beide Modi).</what-built>
  <how-to-verify>
    ⚠️ **GATE: Deploy + Test-Anruf ERST NACH Cross-AI-Clearance + André-Ineinandergreifen-Check.** Nach Clearance:

    1. `git add -A && git commit && git push origin main` → `bash deploy.sh production`.
    2. Struktur-Test server-seitig: `ssh ... '/opt/nerve/venv/bin/pytest tests/test_mode_registry.py -x -v'` → GRUEN (Registry + neuer Modus + Steckplaetze + cold_call fest berater/local).
    3. **TEST-ANRUF (D-03 Auflage 3) — BEIDE Modi:**
       - **(a) cold_call:** Test-Anruf im cold_call-Modus → `ssh ... 'bash scripts/inspect.sh sample intent_event 20'` zeigt die Zeilen mit `mode='cold_call'`, `speaker_role='berater'`, `speaker_id='local'` (payload_jsonb). Verbatim in SUMMARY.
       - **(b) meeting:** Test-Anruf im meeting-Modus → sample zeigt `mode='meeting'` + speaker_role aus Sprecher-Trennung (berater/kunde) bei Kunden-Aussagen.
       - **(c) Routing intakt:** Modus-Umschaltung (handle_manual_mode_toggle) funktioniert weiter; logs zeigen den richtigen Strategy-Pfad; keine `KeyError MODE_REGISTRY`.
       - **(d) §0.1-mode:** falls konsolidiert — `grep`/logs belegen kein global-_session_modes-Read mehr neben per-SID.
       Verbatim-Belege in SUMMARY.
    4. KEIN SCHILD-Guard noetig (keine Schema-Aenderung).
  </how-to-verify>
  <resume-signal>"deployed + Struktur-Test GRUEN + Test-Anruf beide Modi: intent_event mode/speaker_role/speaker_id korrekt (cold_call=berater/local, meeting=getrennt), Routing/Toggle intakt, §0.1-mode entschieden" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 1. Lokaler Kontext
mode_strategy.py + test_mode_registry.py = neue Dateien (kein Control-Flow). Insert-Sites (Task 2): claude_service.py (Welle-4-Emit-Stelle — mode/speaker-Bestimmung); deepgram_service.py:18/427/435/880 (_session_modes, falls A4-Konsolidierung); live_session.py:995 (per-SID-Read, nur Lese-Quelle). **Pflicht beim Bau:** je Site 30 Zeilen vor/nach VERBATIM + return/continue/raise listen. RE-GREP zuerst — Welle 4 hat die Emit-Stelle in claude_service schon angefasst (mode/speaker als Default); diese Welle ersetzt den Default durch den Registry-Lookup. A4-Entscheidung mit grep belegen (Punkt 22), nicht raten.

## 2. Funktions-Skelett
- **analyse_loop (Medium Lane, Welle-4-Emit):** Eingang → mode = per-SID-Read (live_session:995) → [INSERT: strategy = MODE_REGISTRY[mode]] → strategy.get_classification_prompt() → Haiku → strategy.extract_intent() → emit_intent_event(mode, speaker_role, speaker_id, ...) → Loop. Registry-Lookup ersetzt den Welle-4-Default.
- **ModeStrategy-Klassen (neu):** extract_intent → return dict mit mode/speaker_role/speaker_id/intent_type-Kandidat. ColdCall: speaker fest. Meeting: speaker aus sp_map.
- **handle_manual_mode_toggle (deepgram:880, falls A4):** setzt mode → per-SID statt global dict.

## 3. Cross-File-Awareness
- `grep -rn "_session_modes\|session_mode\|'mode'" services/deepgram_service.py services/live_session.py services/claude_service.py` — deepgram:18/427/435 (global dict), live_session:995 (per-SID read), :886 (sp_map), conversation_logs.session_mode (DB). A4-Entscheidung: leben global+per-SID parallel? grep-Beleg ENTSCHEIDET (Punkt 22).
- `grep -rn "MODE_REGISTRY\|ModeStrategy\|mode_strategy" services/ routes/` nach Bau: mode_strategy.py + claude_service-Lookup. Registry ist Single-Source des Routings.
- `grep -rn "handle_manual_mode_toggle" services/` — deepgram:880; falls A4-Konsolidierung, auf per-SID umstellen. Cross-Thread: deepgram + analyse_loop laufen in verschiedenen Pfaden — beide muessen dieselbe per-SID-mode-Quelle lesen.
- `grep -rn "emit_intent_event" services/` — Welle 4 (writer); die Strategy liefert die mode/speaker-Felder dorthin. Konsistent: ein writer, Registry liefert die Werte.

## 4. Edge-Case-Analyse
- **Edge 1 — mode nicht in MODE_REGISTRY (unbekannter/alter Modus):** `MODE_REGISTRY[mode]` → KeyError. Behandlung: `MODE_REGISTRY.get(mode) or MODE_REGISTRY['meeting']` (Default meeting, wie live_session:995-Default) → kein Crash.
- **Edge 2 — Steckplatz-Modus wird live aktiviert (meeting_ext etc.):** extract_intent wirft NotImplementedError → koennte Live-Loop crashen. Behandlung: die 3 Steckplaetze sind NICHT in der UI waehlbar (nur cold_call/meeting real); falls doch erreicht → try-except faengt + faellt auf meeting zurueck (dokumentieren). KEIN echter Modus in TAXO1.
- **Edge 3 — per-SID mode noch nicht gesetzt (Call-Start):** live_session:995-Default 'meeting' greift; Registry-Lookup bekommt 'meeting' → MeetingStrategy. Kein Crash.

### Race-Condition-Audit
1. **Gleichzeitiges Feuern (2 Calls verschiedene Modi parallel):** jeder analyse_loop-Durchlauf liest seine per-SID-mode → MODE_REGISTRY[mode] ist read-only (Strategy-Instanzen sind zustandslos/shared) → kein Cross-Call-Konflikt. Wichtig: Strategy-Instanzen duerfen KEINEN per-Call-State halten (sie bekommen sid/Kontext als Argument) — beim Bau sicherstellen (zustandslose Strategien).
2. **Verbindungsabbruch mid-Operation:** Strategy-Aufruf bricht ab → kein intent_event (emit nicht erreicht). Kein halber Zustand (Strategien zustandslos). per-SID-mode bleibt konsistent.
3. **Wiederverbinden mit altem Zustand:** neue SID → frischer per-SID-mode (init). MODE_REGISTRY ist prozess-global (Registrierung beim Import), kein SID-Bezug → kein alter Eintrag. Modus-Toggle mid-Call (handle_manual_mode_toggle) aendert per-SID-mode → naechster Loop nutzt die neue Strategy (gewollt).
4. **Tab im Hintergrund:** Modus-Toggle ist user-getriggert (UI); im Hintergrund kein Toggle. analyse_loop laeuft server-seitig mit dem zuletzt gesetzten per-SID-mode. Kein veralteter Client-State erzwingt falschen Modus (Server-Quelle).

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `intent_event` (SCHREIBEN via emit_intent_event — mode/speaker_role/speaker_id in payload/Spalte; Welle-4-Pfad, hier nur die Werte-Quelle geaendert). Schema Welle 1.
- `conversation_logs.session_mode` (LESEN — bestehende Modus-Persistenz; unveraendert).
- mode-Wert lebt primaer in **RAM** (`_session_state[sid]['mode']` per-SID) — Cross-Layer: Code/RAM, plus DB-Persistenz session_mode.

### inspect.sh-Beleg
```
# intent_event-Schema: Welle-1-SUMMARY. mode = Spalte (varchar32), speaker_role/speaker_id = payload_jsonb-Keys.
# Test-Anruf (Task 4) belegt mode/speaker je Modus via sample intent_event.
$ ssh ... 'bash scripts/inspect.sh sample intent_event 20'   # nach Test-Anruf beide Modi, verbatim in SUMMARY
```

### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| mode | per-SID read (live_session:995) → MODE_REGISTRY → emit | RAM (_session_state[sid]['mode']) → DB intent_event.mode | ⬜ Test-Anruf: sample zeigt cold_call/meeting |
| speaker_role/speaker_id | Strategy.extract_intent → emit payload | DB intent_event.payload_jsonb-Keys | ⬜ Test-Anruf: cold_call=berater/local, meeting getrennt |
| _session_modes (global, A4) | ggf. GELOESCHT | RAM (deepgram global dict) | ⬜ grep-Beleg: konsolidiert oder keine lebende Doppel-Quelle |
| conversation_logs.session_mode | unveraendert | DB-Spalte | ✓ nicht angefasst |

### Bei Diskrepanz (intent_event.mode falsch / speaker fehlt / §0.1-Doppel-Quelle bleibt unkonsolidiert): STOP + Registry-Verdrahtung/Konsolidierung pruefen.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| per-SID mode (RAM) → MODE_REGISTRY-Routing | Modus bestimmt Klassifikation + speaker; muss per-SID-konsistent sein (§0.1). |
| Strategy.extract_intent → intent_event | speaker_role/speaker_id getaggt; cold_call fest berater/local (kein Kunden-Audio im cold_call, DSGVO). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO1-20 | I (Info Disclosure) | _session_modes global + per-SID parallel → Modus-Leak ueber SIDs (falscher Modus, DSGVO-relevant: cold_call hoert nur Berater) | mitigate | A4-Konsolidierung auf per-SID (falls Doppel-Quelle); grep-Beleg; cold_call fest berater/local. |
| T-TAXO1-21 | D (DoS) | unbekannter/Steckplatz-Modus → KeyError/NotImplementedError crasht Live-Loop | mitigate | MODE_REGISTRY.get(mode) or 'meeting'-Default; try-except um Strategy-Aufruf; Steckplaetze nicht UI-waehlbar. |
| T-TAXO1-22 | T (Tampering) | Strategy-Instanz haelt per-Call-State → Cross-Call-Leak | mitigate | zustandslose Strategien (Kontext als Argument); Test (parallele Calls); kein Instanz-State. |
</threat_model>

<verification>
- mode_strategy.py: ABC + Registry + ColdCall/Meeting + 3 Steckplaetze; cold_call fest berater/local; kein Air-Gap-Bus.
- Medium Lane nutzt MODE_REGISTRY; §0.1-mode entschieden (konsolidiert/belegt).
- Struktur-Test GRUEN (neuer Modus ohne Kern-Aenderung).
- Test-Anruf beide Modi: intent_event traegt korrekten mode/speaker_role/speaker_id; Routing/Toggle intakt.
</verification>

<success_criteria>
- Registry existiert; cold_call + meeting echte Klassen; neuer Modus = neue Klasse ohne Kern-Aenderung (REQ 6).
- intent_event traegt mode + speaker_role + speaker_id bei jedem Event (REQ 6).
- 3 dokumentierte Steckplaetze; kein Training-Air-Gap-Bus im Code (REQ 6).
- §0.1-mode-Status entschieden (per-SID konsolidiert oder keine Doppel-Quelle belegt).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-07-SUMMARY.md` (inkl. Struktur-Test-GREEN + sample intent_event beide Modi verbatim (mode/speaker_role/speaker_id) + A4-Entscheidung mit grep-Beleg + Bestaetigung kein Air-Gap-Bus).
</output>
