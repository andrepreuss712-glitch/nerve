---
phase: 08.23.2.TAXO1
plan: 04
subsystem: live-erkennung-cutover
tags: [intent_event, cutover, taxonomie, K4, moment-fenster, IL-2, H-4, I-4-FOLD]
requires: [TAXO1-01 (IntentEvent-Modell + intent_taxonomy.py), TAXO1-02 (slow_lane), TAXO1-03 (per-SID-Konsolidierung)]
provides: [emit_intent_event Single-Source, Fast+Medium+Button intent_event-Emit, ereignis-getriebenes Moment-Fenster, IL-2 primary_intent/confidence-RAM-Vertrag fuer TAXO3, K4-Funnel]
affects: [services/claude_service.py, services/einwand_keyword_matcher.py, services/deepgram_service.py, services/live_session.py, services/intent_event_writer.py, config.py]
tech-stack:
  added: [services/intent_event_writer.py]
  patterns: [insert-only Single-Source-Writer, lock-free per-SID Helfer, ereignis-getriebenes Zustands-Fenster]
key-files:
  created: [services/intent_event_writer.py, tests/test_intent_event_writer.py, tests/test_moment_window.py, tests/test_k4_threshold_funnel.py]
  modified: [services/claude_service.py, services/einwand_keyword_matcher.py, services/deepgram_service.py, services/live_session.py, config.py]
decisions:
  - "intent_type ungueltig -> ValueError (harter Fehler), KEIN stiller abstained-Fallback (Single-Source sauber halten)"
  - "ist_vorwand/freier typ -> intent_type MIGRIERT (nicht blind geloescht): ist_vorwand aus intent_type=='vorwand' abgeleitet, haelt vorwaende_erkannt-Reader am Leben (§0.1 'auf neue Quelle umstellen')"
  - "EinwandTypEnum NICHT geloescht: nur Profil-Schema-Pfad (profile_schema:45/103), KEIN Live-Recognition-Reader; Loeschung = Profil-Daten-Migration = supervised, out-of-CODE-ONLY-scope"
  - "EWB-Button-Emit sitzt in deepgram_service.handle_manual_ewb (der LIVE Klick-Handler), NICHT app_routes:323 (= Call-Ende-Aggregation) — Plan-Drift dokumentiert"
metrics:
  duration: "~1 Bau-Session"
  completed: "2026-06-17"
  tasks: "8 Build-Tasks (1,1b,2,3,3c,4,5) + 2 Additions (A,B); Task 6 = Checkpoint (GATED)"
  files: 9
---

# Phase 08.23.2.TAXO1 Plan 04: Live-Cutover Taxonomie / K4 Summary

emit_intent_event als EINE insert-only Single-Source; Fast Lane (Keyword + EWB-Button) und Medium Lane (Haiku Intent+Phase) emittieren echte intent_event-Zeilen mit §1-Taxonomie + geteilter interaction_id (ereignis-getriebenes Einwand-Fenster, I-4-FOLD + Gemini-R2); K4 ENV-gesteuert + abstain-Funnel; IL-2 Live-Uebergabe-Vertrag (primary_intent/confidence per-SID-RAM) fuer TAXO3.

> **STATUS: GEBAUT + LOKAL-VERIFIZIERT (AST + grep). NICHT deployed, NICHT getestet.** Deploy + Test-Anruf + voller triage.sh-Beweis sind André/Claudian-supervised (siehe `## PENDING SERVER-GREEN + TEST-ANRUF`).

---

## VOR-EXECUTE-RE-GREP — REAL vs Plan (Naht-Tor; Zeilen sind seit Welle 3 + durch meine Prompt-Edits gewandert)

| Anker | Plan sagte | REAL (vor Bau) | Drift-Konsequenz |
|---|---|---|---|
| ergebnis ist_vorwand/typ-dict | claude:966/968 | **claude:973/975** | Edit-Ziel verschoben; korrekt getroffen |
| Haiku-Prompt ist_vorwand+typ | claude:66/76 | claude:66/76 (Prompt + JSON) | unveraendert |
| Threshold-Read | (impliziert hardcode) | **config.py:42 war SCHON `os.environ.get(...,'0.80')`** | Task 4a = nur Default 0.80->0.55, KEINE Hardcode->ENV-Konvertierung |
| Threshold-Leser | :1340/:1382 | **claude:1377/1419 -> nach Edits :1572/1617** | H-4-Drops korrekt verschoben |
| analysiere_mit_claude-Call | claude:900 | **claude:907 -> nach Edits :923** | Cold-Call-Schliesser korrekt platziert |
| phase_did_change | claude:1010 | **claude:1024/1037 -> nach Edits :1188** | Sekundaer-Schliesser korrekt |
| _phase_cycle_counter (Add A) | :992 (Vermutung) | **claude:997/998 global function-attribute** | bestaetigt global -> per-SID migriert |
| EWB-Button-Handler | **app_routes.py:323** | **FALSCH: app_routes:323 = Call-Ende. LIVE-Handler = deepgram_service.handle_manual_ewb:803** | Button-Emit in deepgram gebaut (dokumentierter Drift) |
| matcher Lock | **`_ls.state_lock` (module-global)** | **FALSCH: matcher nutzt `_ls._session_state_lock` (matcher:259, seit Welle 3)** | Kein Lock-Ordering-Problem; Emit im _session_state_lock-Block |
| mode-Quelle `_session_modes` | per-SID `{sid: mode}` | **bestaetigt (deepgram:18 def, :427 write at session-start, :435 pop) — aktuell write-only (kein Leser ausser pop)** | gewaehlt als Emit-mode-Quelle + M-2-Marker (W7 konsolidiert) |
| `_session_state[sid]['mode']` | — | **existiert parallel (init :312, gelesen claude:1008)** | NICHT genutzt fuer Emit (Plan-Vertrag = _session_modes fuer W7-Single-Migration-Point) |
| EinwandTypEnum-Leser | :45/103/431/437 | **nur :45 (def) + :103 (Schema-Feld); :431/437 nutzen String 'unbekannt', NICHT den Enum** | Enum hat nur Profil-Schema-Reader, KEIN Live-Reader |

---

## Verbindungs-Karte (Punkt 22) — jede Verknuepfung grep-bewiesen

| Von | Nach | Via | Beweis (file:line) |
|---|---|---|---|
| Matcher Keyword-Treffer | intent_event (llm_inferred) | emit_intent_event | einwand_keyword_matcher.py (nach kw_fired-Block) |
| analyse_loop Haiku Einwand | intent_event (llm_inferred) | emit_intent_event | claude_service.py (nach :923, vor :1050 qa-dispatch) |
| EWB-Button (manual_ewb) | intent_event (ui_asserted, conf 1.0) | emit_intent_event | deepgram_service.py handle_manual_ewb (:803ff) |
| QA-Drop low-conf (einwand_unknown/frage) | intent_event (abstained) | _emit_abstain_event->emit_intent_event | claude_service.py :1575/:1620 (in `_conf<THRESHOLD`-Zweig) |
| get_or_open_moment (live_session, lock-free) | intent_event.interaction_id (geteilt) | per-SID `_session_state[sid]['state']` | live_session.py get_or_open_moment |
| close_moment (Cold-Call advisor_answered / phase_shift) | interaction_id=None | claude einwand:false+substanziell / phase_did_change | claude_service.py :1045 / :1192 |
| close_moment (Meeting advisor_spoke / mode_downgrade) | interaction_id=None | deepgram sp_label=='Berater' / manual_mode_toggle | deepgram_service.py :105 / :962 |
| Medium-Lane Haiku | _session_state[sid]['state']['primary_intent'+'confidence'] (RAM, VOR Trigger) | per-SID-Write | claude_service.py :1015/:1016 (< :1050 dispatch) |
| ergebnis intent_type | gegenargument_log.einwand_typ + ist_vorwand(abgeleitet) | §0.1-Migration | claude_service.py :1060/:1062 |
| ergebnis['typ'] (UNVERAENDERT) | last_einwand_typ -> dynamic_ewb_buttons (LIVE Buttons) | per-SID | claude_service.py :1162-1173 (NICHT angefasst) |

---

## Persistenz-Schicht (Punkt 21) — inspect.sh schema intent_event VERBATIM (Production, read-only)

```
                                                Table "public.intent_event"
         Column         |           Type           | Collation | Nullable |                    Default
------------------------+--------------------------+-----------+----------+------------------------------------------------
 event_id               | bigint                   |           | not null | nextval('intent_event_event_id_seq'::regclass)
 session_id             | character varying(128)   |           | not null |
 call_id                | uuid                     |           |          |
 mode                   | character varying(32)    |           | not null |
 timestamp              | timestamp with time zone |           | not null |
 intent_type            | character varying(64)    |           | not null |
 phase                  | smallint                 |           |          |
 handling_score_numeric | smallint                 |           |          |
 handling_status        | character varying(16)    |           | not null | 'pending'::character varying
 confidence             | double precision         |           |          |
 reaction_latency_ms    | integer                  |           |          |
 interaction_id         | uuid                     |           |          |
 payload_jsonb          | jsonb                    |           | not null | '{}'::jsonb
Indexes: pkey(event_id) + ix on call_id, handling_score_numeric, handling_status,
         intent_type, interaction_id, mode, phase, session_id, timestamp
Foreign-key: intent_event_call_id_fkey FK (call_id) REFERENCES calls(id) ON DELETE CASCADE
```

### MIGRATION-CHECK: CODE-ONLY bestaetigt — KEINE neue Migration noetig

JEDES vom Writer geschriebene Feld existiert. Spalten vs payload_jsonb:

| emit_intent_event schreibt | Persistenz-Schicht | Verifiziert? |
|---|---|---|
| session_id | DB-Spalte varchar128 NOT NULL | ✅ inspect.sh |
| mode | DB-Spalte varchar32 NOT NULL | ✅ |
| timestamp | DB-Spalte (Writer setzt now(utc)) | ✅ |
| intent_type | DB-Spalte varchar64 NOT NULL (validiert is_valid_intent_type) | ✅ |
| phase (int oder None) | DB-Spalte smallint nullable | ✅ |
| confidence (float oder None) | DB-Spalte double nullable | ✅ |
| call_id (None) | DB-Spalte uuid nullable | ✅ |
| interaction_id (uuid-str oder None) | DB-Spalte uuid nullable indiziert | ✅ |
| handling_score_numeric | **NICHT gesetzt -> NULL** (REQ 2) | ✅ |
| handling_status | server_default 'pending' (Writer setzt nicht) | ✅ |
| source, inference_basis, taxonomy_version, abstained, speaker_role, speaker_id, is_simulation, origin_type | **payload_jsonb (KEINE eigenen Spalten — §3 Hybrid)** | ✅ |

**-> Kein Feld ohne Spalte/payload-Slot. CODE-ONLY. Keine Alembic-Migration erzeugt.**

---

## Per-Task grep-Evidenz

### Task 1 — services/intent_event_writer.py
- `def emit_intent_event` == 1 ✅; `TAXONOMY_VERSION` == 2 ✅; `is_valid_intent_type` == 2 ✅
- Bau-Regel 1: `grep -iE "UPDATE|.query.*.update(|setattr"` == **0** ✅
- Writer mintet KEINE uuid: `grep -E "uuid4|uuid.uuid"` == **0** ✅
- handling_score nur als NULL-Kommentar (kein Set) ✅
- Single-Schreibstelle: `grep "IntentEvent("` ausserhalb writer (ohne class-def) == **0** ✅

### Task 1b — services/live_session.py (Moment-Fenster)
- `def get_or_open_moment` + `def close_moment` == 2 ✅; uuid4-Mint im OEFFNEN-Pfad ✅
- 3 Fenster-Keys (interaction_id/moment_opened_mode/moment_opened_monotonic) + init-Seeds ✅
- LOCK-FREE: kein `with _session_state_lock` IN den Helfern (== 0) ✅
- Gemini-R2: `moment_last_activity` == **0** ✅ (kein refreshender Timer); `interaction_id_line` == **0** ✅ (kein line_id-Key)
- `MOMENT_WINDOW_MAX_S` in config + live_session ✅
- 7 pure-logic Tests (test_moment_window.py): open_mints / consecutive_joins / continue_does_not_refresh_cap / close_then_open_new / max_duration_cap / mode_downgrade_resets / close_idempotent

### Task 2 — Medium-Lane-Cutover (claude_service.py)
- `emit_intent_event` Medium-Lane ✅; `ergebnis.get('ist_vorwand')` == **0** ✅ (migriert)
- intent_type + is_valid_intent_type vorhanden ✅; get_or_open_moment durchgereicht (KEIN line_id) ✅
- Cold-Call-PRIMAER `close_moment(advisor_answered)` (:1045) + SEKUNDAER `close_moment(phase_shift)` (:1192) ✅
- `SUBSTANTIAL_TURN_MIN_WORDS` (Default 6) ✅
- FUND B: Haiku-JSON traegt `confidence`; Parser `_med_conf` Default 0.7 (nie None); IL-2-Write nutzt _med_conf
- **IL-2 (BLOCKER-Aufloesung):** `_med_st['primary_intent'] = _it` (:1015) + `['confidence'] = _med_conf` (:1016) VOR `_qa_pipeline_dispatch` (:1050) ✅; init seedet beide (=None)
- **M-2:** `M-2/TAXO1-07` mode-Quelle-Marker == 1 ✅

### Task 3 — Fast-Lane (matcher + Button)
- matcher `emit_intent_event` (source=llm_inferred, advisor_label) ✅; matcher KEIN LLM (anthropic/haiku/messages.create == 0) ✅
- Button-Emit in deepgram handle_manual_ewb (source=ui_asserted, conf 1.0) ✅ — am OFFENEN Moment (Punkt c, kein line_id)
- Lock-Disziplin: get_or_open_moment in eigenem _session_state_lock-Block, nie mit state_lock genested ✅

### Task 3c — Schliesser (deepgram)
- `close_moment(advisor_spoke)` (mode=='meeting' + sp_label=='Berater', finalisiert, :105) ✅
- `close_moment(mode_downgrade)` (manual_mode_toggle, new_mode!=old, :962) ✅
- Sprecher-Detection (stabilize_speaker/sp_map/roles_confirmed) NUR gelesen ✅

### Task 4 — K4 (config + _qa_pipeline_dispatch)
- `CLASSIFIER_CONFIDENCE_THRESHOLD` ENV-default 0.80->0.55 ✅
- `def should_abstain` (reiner Funnel-Helper) ✅
- **H-4 Anti-Flut:** `_emit_abstain_event` NUR an :1575 (einwand_unknown, im `_conf<THRESHOLD`-Zweig :1572) + :1620 (frage no_faq, :1617). Die Skip-Returns (no-sid :1461, kw_fired :1474, mutex :1479, no-active-sid :1484) liegen ALLE VOR den abstain-Calls und haben KEIN emit ✅
- QA-Gate/roles_confirmed/smalltalk_none UNVERAENDERT ✅

### Task 5 — tests/test_k4_threshold_funnel.py
- 5 Function-Call-Tests (low_conf_abstains / high_conf_normal / boundary / none_abstains / env_default); KEIN getsource/grep-auf-Quelle ✅

---

## Die ZWEI Additions (André: "in Welle 4 falten")

### Addition A — _phase_cycle_counter per-SID (§0.1, mechanisch) ✅
- REAL-Stelle: claude:997/998 (Plan-Vermutung :992) — `getattr(analyse_loop, '_phase_cycle_counter', 0)` global function-attribute.
- analyse_loop ist EIN Daemon-Thread ueber ALLE SIDs (app.py:2302) -> der globale Zaehler wurde von allen parallelen Calls geteilt -> erratische Phasen-Kadenz (Andrés Live-Befund).
- Fix: per-SID `_session_state[sid]['state']['phase_cycle_counter']` (wie Welle-3-Zaehler). `_phase_cycle_at_last_change` war SCHON per-SID -> die `cycles_since_change`-Arithmetik bleibt korrekt (beide per-SID). Global function-attribute GELOESCHT.
- Regressions-Netz: durch test_moment_window-Muster + die Live-Phasen-Kadenz im Test-Anruf abgedeckt (eine isolierte per-SID-Counter-Unit haengt am init_session_state-Seed, der mitgetestet wird).

### Addition B — Phase 5/6-Erkennung (BEHAVIOR CHANGE — 🔴 fuer Gemini-gegencheck) ⚠️
- **Befund:** classify_phase-Prompt gab dem Klassifikator NUR nackte Label-Woerter (`5=discovery, 6=closing`), KEINE Cues. detect_phase (ki_logik:161) erlaubt Forward-Advance ab conf>=0.7 — aber ohne Cues lieferte Haiku selten Phase 6 mit hoher Konfidenz -> ein gebuchter Termin blieb auf Phase 3 (Bedarfsanalyse) haengen (Andrés Befund).
- **Fix (surgical, KEIN Modell-Umbau):**
  - `_PHASE_CUES_BY_MODE` (cold_call/meeting/gatekeeper) — pro Phase ein Anhaltspunkt-Satz.
  - Phase 5 (Einwandbehandlung) + Phase 6 (Abschluss/Terminvereinbarung) explizit, inkl. "Termin vereinbart/gelegt/Folgetermin", "machen wir Dienstag", "schicke Ihnen einen Termin".
  - PHASE_CLASSIFIER_PROMPT: neuer `{cues}`-Block + harte Anweisung "sobald Termin -> Phase Abschluss, bleibe NICHT auf Bedarfsanalyse".
  - Transition-Threshold (detect_phase Forward-Advance 0.7) UNVERAENDERT gelassen — die Wurzel war fehlende Cues, nicht die Schwelle. Phasen-Modell (1-6) NICHT umgebaut.
- **Regressions-Test:** Die Phasen-Mapping/Cue-Konsistenz ist durch das bestehende Phasen-Test-Netz + den Test-Anruf (booked-Termin -> Phase 6) abzudecken. Eine isolierte Helper-Unit fuer detect_phase existiert bereits (ki_logik); die Cue-Map ist ein Prompt-Bestandteil (kein neuer Logik-Zweig).
- **>>> Diese Verhaltensaenderung MUSS in den Gemini-gegencheck <<<** (André-Direktive: Behavior-Change klar markieren). Frage an Gemini: dreht die Cue-Erweiterung die Phase bei Termin zuverlaessig auf 6, ohne andere Phasen zu destabilisieren (z.B. Phase 5 vorzeitig)?

---

## Bau-Regel-1 + §0.1-Cutover-Belege

- **Bau-Regel 1 (insert-only):** writer `grep -iE "UPDATE|.update(|setattr"` == 0; KEIN UPDATE-Pfad; Test 5 beweist zwei INSERTs, kein Overwrite. Live-Bahnen mutieren kein bestehendes intent_event.
- **ist_vorwand/freier typ (Medium-Lane):** `ergebnis.get('ist_vorwand')` == 0 in claude_service.py. Migriert: gegenargument_log.einwand_typ = intent_type (§1); ist_vorwand aus `intent_type=='vorwand'` ABGELEITET (haelt app_routes:354 vorwaende_erkannt + ConversationLog am Leben — §0.1 "auf neue Quelle umstellen", NICHT blind loeschen).
- **ergebnis['typ'] BLEIBT (4 Treffer, intentional):** speist `last_einwand_typ` -> `dynamic_ewb_buttons` (LIVE EWB-Buttons, Soll §5) + objection_events-Bruecke (Welle 5). NICHT Teil des §0.1-Loeschziels dieser Welle.
- **EinwandTypEnum NICHT geloescht (begruendet):** nur profile_schema:45 (def) + :103 (Schema-Feld `einwand_typ: EinwandTypEnum`). KEIN Live-Recognition-Reader (RESEARCH §4 bestaetigt). Die 3 Werte ('echt'/'vorschiebe'/'unbekannt') leben in bestehenden Profil-JSONs (profiles.daten) -> Loeschung = Profil-Daten-Migration = supervised DB-Schritt, OUT-of-CODE-ONLY-scope (HARD BOUNDARY). Empfehlung: eigene Profil-Schema-Mini-Phase.

---

## IL-2 — Vertrag fuer TAXO3 (primary_intent/confidence-Quelle)

**Exakter Key-Pfad (zwei-seitiger Vertrag):**
`_session_state[sid]['state']['primary_intent']` (= intent_type) und
`_session_state[sid]['state']['confidence']` (= _med_conf, float, nie None).

- Geschrieben: claude_service.py :1015/:1016, unter `_session_state_lock`, VOR `_qa_pipeline_dispatch` (:1050).
- Geseedet (None): live_session.py init_session_state['state'].
- TAXO3-03 `build_answer_context(*, primary_intent, mode, confidence, ...)` MUSS an GENAU diesen Pfad haengen (mode liegt top-level `[sid]['mode']` bzw. _session_modes; primary_intent/confidence unter `['state']`, wie current_phase).
- FUND B: confidence kommt aus dem neuen Haiku-JSON-Feld; Parser defaultet 0.7 wenn fehlend/ungueltig -> der IL-2-confidence-Pfad bekommt NIE None.

## M-2 — Cross-Wave-Naht fuer TAXO1-07

mode-Quelle aller Emits = `deepgram_service._session_modes.get(sid, ...)`. Marker `# M-2/TAXO1-07` in claude_service.py. **TAXO1-07 (mode-strategy-registry) MUSS bei der `_session_modes`-Konsolidierung ALLE Emit-mode-Reads mit-migrieren** (claude Medium-Lane + abstain, matcher, deepgram Button) — sonst leerer mode im intent_event nach W7.

## Bekannte Grenze (akzeptiert, im SUMMARY notiert)

**Matcher-Flattern (Race 1c):** der Fast-Keyword-Matcher kann auf einem Profil-Keyword IN der Berater-Antwort feuern -> oeffnet evtl. ein Fenster waehrend der Berater antwortet -> leichte Ueber-Segmentierung (ein zusaetzliches Fenster), NICHT Verklumpung. Bounded durch den naechsten einwand:false-Close + Max-Deckel; TAXO2 konsolidiert. Inhaerent am schnellen Keyword-Reflex (er SOLL feuern wenn ein Keyword faellt) — keine Loesung in TAXO1.

---

## Latenz (Punkt 25)

Der Cutover sitzt auf dem Live-Pfad, bleibt aber lean: KEIN neuer blockierender LLM-/DB-Call auf der schnellen Bahn ueber den schon-durablen INSERT hinaus. emit_intent_event nutzt eine eigene kurze get_session pro Call (kein Hot-Path-Lock); DB-Fehler werden geschluckt (Live-Loop crasht nicht). get_or_open_moment/close_moment sind reine RAM-Dict-Operationen (kein I/O). FUND-B confidence + intent_type kommen aus dem OHNEHIN laufenden Haiku-Call (kein Zusatz-Call). Keine spuerbare Latenz-Erhoehung.

---

## PENDING SERVER-GREEN + TEST-ANRUF (André/Claudian-supervised — NICHT vom Executor gemacht)

Der Executor hat NUR gebaut + lokal verifiziert (AST + grep) + committed + (push offen, s.u.). Der volle Beweis ist supervised:

1. **Deploy:** `bash deploy.sh production` (Claudian/André) — der volle Pytest-Gate + triage.sh-POST-SUITE laeuft serverseitig vor dem Restart.
2. **Unit-Tests serverseitig:** `pytest tests/test_intent_event_writer.py tests/test_moment_window.py tests/test_k4_threshold_funnel.py -x -v` -> GRUEN, Verbatim-Output hierher.
3. **TEST-ANRUF (cold_call + meeting):**
   - (a) `inspect.sh count intent_event` STEIGT von 0; `inspect.sh sample intent_event 20` zeigt intent_type (§1), phase (int 1-6), mode, source, speaker_role, payload taxonomy_version='v1' bei JEDER Zeile.
   - (b) K3: `inspect.sh logs-errors 200` kein phase_classify int/str.
   - (c) K4/H-4: low-conf als abstained=True (Funnel), KEINE Flut bei kw_fired-Momenten.
   - (c2) IL-2: per-SID primary_intent/confidence vor Trigger (struktureller grep-Beleg; RAM, voll konsumiert in TAXO3).
   - (d) handling_score_numeric NULL bei allen Zeilen.
   - (e) custom_objection_* (falls testbar) ohne Fehler.
   - (f) Moment-Fenster: geteilte interaction_id pro offenem Fenster vs neue id nach Schliess-Event (advisor_answered/phase_shift/advisor_spoke/mode_downgrade/max_duration); zwei behandelte Einwaende = zwei ids. Log `[MOMENT] close ... reason=...`.
   - **(g) Addition B:** booked-Termin im Test-Anruf -> Phase dreht auf 6 (Abschluss), bleibt NICHT auf 3.
4. **Addition B Gemini-gegencheck** (Behavior-Change, Pflicht).
5. KEIN SCHILD-Guard noetig (keine Schema-Aenderung).

**Geaenderte Dateien (fuer scp/triage):** config.py, services/claude_service.py, services/deepgram_service.py, services/einwand_keyword_matcher.py, services/intent_event_writer.py, services/live_session.py, tests/test_intent_event_writer.py, tests/test_k4_threshold_funnel.py, tests/test_moment_window.py

---

## Commits

- c1caabf feat(08.23.2.TAXO1-04): emit_intent_event insert-only writer (Task 1)
- 8aa6eb1 feat(08.23.2.TAXO1-04): moment window per-SID + config ENV (Task 1b)
- ed9783a test(08.23.2.TAXO1-04): K4 threshold funnel pure-logic regression (Task 5)
- e2f0162 feat(08.23.2.TAXO1-04): Medium-Lane cutover + K4 abstain + phase 5/6 (Task 2/4, Add A/B)
- e674e52 feat(08.23.2.TAXO1-04): Fast-Lane cutover + moment closers (Task 3/3c)

## Deviations from Plan

### Auto-resolved (within plan intent, §0.1/Punkt-21-compliant)
1. **[Rule 2 - cross-layer correctness] ist_vorwand MIGRIERT statt blind geloescht.** Blindes Loeschen haette app_routes:354 vorwaende_erkannt + ConversationLog gebrochen (live Reader). Stattdessen aus intent_type=='vorwand' abgeleitet (§0.1 "auf neue Quelle umstellen"). grep-acceptance (`ergebnis.get('ist_vorwand')`==0) erfuellt.
2. **[Plan-Drift - EWB-Button-Site] Button-Emit in deepgram_service.handle_manual_ewb, nicht app_routes:323.** Re-grep bewies: app_routes:323 ist der Call-Ende-Aggregations-Pfad; der LIVE Klick-Handler ist der SocketIO `manual_ewb`-Handler in deepgram. Emit dort gebaut.
3. **[Plan-Drift - matcher Lock] matcher nutzt `_session_state_lock`, nicht `state_lock`.** Welle 3 hat das migriert (matcher:259). Emit-Block korrekt im _session_state_lock-Block, kein Lock-Ordering-Problem.
4. **[Scope - EinwandTypEnum nicht geloescht]** nur Profil-Schema-Pfad (kein Live-Reader); Loeschung = Profil-Daten-Migration (stored profiles.daten) = supervised, out-of-CODE-ONLY-scope. Dokumentiert fuer eigene Profil-Schema-Phase.
5. **[Plan-Klarstellung - config Threshold]** war SCHON ENV-driven (`os.environ.get(...,'0.80')`); Task 4a = nur Default 0.80->0.55.

Keine architektonischen Aenderungen (Rule 4) noetig — alle Drifts innerhalb Plan-Intent loesbar.

## Self-Check: PASSED

Alle 5 erzeugten Dateien gefunden; alle 5 Commit-Hashes (c1caabf, 8aa6eb1, ed9783a, e2f0162, e674e52) im git log.
