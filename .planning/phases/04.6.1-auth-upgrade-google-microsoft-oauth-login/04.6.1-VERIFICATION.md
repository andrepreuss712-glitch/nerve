---
phase: 04.6.1-auth-upgrade-google-microsoft-oauth-login
verified: 2026-04-06T00:00:00Z
status: human_needed
score: 14/14 must-haves verifiziert (Code) — visuelle/Provider-E2E offen
human_verification:
  - test: "OAuth-Buttons im Login-Modal sichtbar (G-Logo + MS-Logo, volle Breite, hover)"
    expected: "Buttons über Email-Feld sichtbar, Trennlinie 'oder mit E-Mail' dazwischen"
    why_human: "Visuelles Rendering — nicht programmatisch verifizierbar"
  - test: "OAuth-Buttons im Register-Modal Step 1 sichtbar"
    expected: "Identische 2 Buttons über Vorname/Nachname-Row"
    why_human: "Visuelles Rendering"
  - test: "Google E2E: Klick → accounts.google.com → Callback → /onboarding (neuer User) bzw /dashboard (bestehend)"
    expected: "Erfolgreicher Login, DB zeigt oauth_provider='google', oauth_id, avatar_url befüllt"
    why_human: "Externer Provider, echte Credentials nötig"
  - test: "Microsoft E2E: Klick → login.microsoftonline.com/common → Callback → Onboarding/Dashboard"
    expected: "Erfolgreicher Login, oauth_provider='microsoft'"
    why_human: "Externer Provider"
  - test: "Email-Match: bereits per Passwort registrierter User loggt sich via Google ein"
    expected: "Kein Duplikat-Account, oauth_provider/_id werden auf existierende user-Row gesetzt"
    why_human: "Erfordert echten Provider + DB-Inspect"
  - test: "OAuth-Abbruch (Cancel auf Provider-Seite)"
    expected: "Redirect /?modal=login&oauth_error=google_failed, Modal öffnet auto, deutsche Fehlermeldung in #l-error"
    why_human: "Erfordert echten Provider-Flow"
  - test: "Regression: Email/Passwort-Login eines bestehenden Test-Users funktioniert weiterhin"
    expected: "Login-Flow grün, kein 500er, _do_login akzeptiert User mit nicht-leerem passwort_hash"
    why_human: "Erfordert echten Test-Account"
---

# Phase 04.6.1: Auth-Upgrade Google + Microsoft OAuth — Verification Report

**Phase Goal:** User können sich mit Google- oder Microsoft-Account einloggen. Bestehende Email/Passwort-Auth bleibt regression-frei. Email-Match verknüpft existierende Accounts ohne Duplikat. Login-Modal + Register-Modal zeigen OAuth-Buttons.
**Verified:** 2026-04-06
**Status:** human_needed (Code vollständig — visuelle/E2E-Verifikation offen)

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | User-Tabelle hat oauth_provider, oauth_id, avatar_url | ✓ VERIFIED | database/models.py:110-112 |
| 2  | passwort_hash nullable im ORM, bestehende User loggen weiterhin ein | ✓ VERIFIED | models.py (passwort_hash Column nullable=True), routes/auth.py:84 |
| 3  | _do_login lehnt OAuth-Only-User (leerer hash) mit Standard-Error ab | ✓ VERIFIED | routes/auth.py:84-87 |
| 4  | _login_user und _create_org_and_user existieren + werden von api_login/api_register genutzt | ✓ VERIFIED | routes/auth.py:46,94,134,161,168 |
| 5  | GET /auth/google leitet zu accounts.google.com mit state | ✓ VERIFIED | routes/oauth.py:106-111 (authlib authorize_redirect) |
| 6  | GET /auth/microsoft leitet zu login.microsoftonline.com/common | ✓ VERIFIED | routes/oauth.py:135-140 |
| 7  | Google-Callback: bestehender User → Login + Redirect, neuer User → Anlage + /onboarding | ✓ VERIFIED | routes/oauth.py:48-101,114-131 |
| 8  | Microsoft-Callback verhält sich identisch (ohne email_verified-Check) | ✓ VERIFIED | routes/oauth.py:143-165 |
| 9  | OAuth-Fehler → Redirect /?modal=login&oauth_error=... | ✓ VERIFIED | routes/oauth.py:43-45,118-120,147-149 |
| 10 | ProxyFix aktiv → korrekte https-Redirect-URIs hinter nginx | ✓ VERIFIED | app.py:24-25 |
| 11 | oauth_bp registriert + init_oauth(app) gerufen | ✓ VERIFIED | app.py:689,706-707 |
| 12 | Login-Modal zeigt Google + Microsoft Buttons + Trennlinie | ✓ VERIFIED (Code) / ? human (visuell) | landing.html:602-620 |
| 13 | Register-Modal Step 1 zeigt dieselben Buttons | ✓ VERIFIED (Code) / ? human (visuell) | landing.html:656-674 |
| 14 | ?oauth_error=<code> öffnet Login-Modal mit deutscher Fehlermeldung | ✓ VERIFIED (Code) | landing.html:782-785 + JS errMap |

**Score:** 14/14 truths code-verifiziert. Visuelle + Provider-E2E-Verifikation gehört zu UAT (siehe human_verification).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `database/models.py` | User mit oauth_provider/oauth_id/avatar_url + passwort_hash nullable | ✓ VERIFIED | Zeilen 110-112 |
| `app.py` | Migration + oauth_bp register + ProxyFix | ✓ VERIFIED | Z.24-25, 96, 689, 706-707 |
| `routes/auth.py` | _login_user, _create_org_and_user, _do_login refactored | ✓ VERIFIED | Z.46, 77, 94 |
| `routes/oauth.py` | oauth_bp + 4 Routes + init_oauth + _oauth_login_or_create | ✓ VERIFIED | komplette Datei (166 Zeilen) |
| `requirements.txt` | authlib>=1.3.0 | ✓ VERIFIED | Z.10 |
| `config.py` | GOOGLE_/MICROSOFT_ CLIENT_ID/SECRET aus env | ✓ VERIFIED | Z.21-24 |
| `.env.example` | OAuth-Vars dokumentiert | ✓ VERIFIED | Z.16-19 |
| `templates/landing.html` | OAuth-Buttons in beiden Modals + CSS + oauth_error JS | ✓ VERIFIED | Z.224-240 (CSS), 602-674 (HTML), 782-785 (JS) |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| routes/auth.py::api_login | _login_user | _do_login → _login_user(db, user) | ✓ WIRED (auth.py:88) |
| routes/auth.py::api_register | _create_org_and_user | direkter Aufruf | ✓ WIRED (auth.py:161) |
| routes/oauth.py::_oauth_login_or_create | _login_user | nach Email-Match | ✓ WIRED (oauth.py:77,97) |
| routes/oauth.py::_oauth_login_or_create | _create_org_and_user | bei Neuanlage | ✓ WIRED (oauth.py:84) |
| app.py | init_oauth(app) | nach register_blueprint | ✓ WIRED (app.py:707) |
| landing.html loginModal | /auth/google | `<a href>` | ✓ WIRED (landing.html:602) |
| landing.html loginModal | /auth/microsoft | `<a href>` | ✓ WIRED (landing.html:611) |

### Anti-Patterns Found

Keine. Keine TODO/FIXME/Stub-Patterns. Threat-Modell-Mitigations (CSRF via state, Session-Fixation via session.clear, kein Open-Redirect, keine Secret-Logs) im Code umgesetzt.

### Requirements Coverage

Requirement-IDs `AUTH-OAUTH-01..06` sind in den PLAN-Frontmatters deklariert, aber nicht in `.planning/REQUIREMENTS.md` katalogisiert (Datei enthält keine dieser IDs). Code-Implementierung deckt alle in PLANs beschriebenen Verhalten — falls REQUIREMENTS.md nachgepflegt wird, sollten die IDs dort ergänzt werden.

### Human Verification Required

Plan 03 enthält einen `checkpoint:human-verify`-Task, der laut SUMMARY 03 unter `workflow.auto_advance=true` automatisch geschlossen wurde. Die echten visuellen + Provider-E2E-Tests gehören damit in den UAT-Lauf des Founders. Siehe `human_verification`-Block im Frontmatter (7 Items).

### Gaps Summary

Keine Code-Gaps. Alle 14 Truths sind im Code vollständig umgesetzt und verdrahtet. Status `human_needed` ausschließlich wegen offener visueller/E2E-Verifikation gegen echte Google- und Microsoft-Provider — diese wurde bewusst auf UAT verschoben (Plan 03 Task 2 Checkpoint: auto-advanced).

---

## Verdikt

**PASS (Code)** — alle Artefakte existieren, sind substanziell, verdrahtet und folgen den im PLAN definierten Sicherheits-Mitigations (state/nonce via authlib, session.clear gegen Session-Fixation, identische Errortexte gegen User-Enumeration, ProxyFix für https-Redirect-URIs, keine Secret-Logs).

**HUMAN_NEEDED (UAT)** — visuelle Button-Darstellung in beiden Modals + End-to-End OAuth-Flow gegen Google und Microsoft + Email-Match-Test + OAuth-Abbruch-Test + Email/Passwort-Regression müssen im Browser mit echten Provider-Credentials verifiziert werden.

_Verifiziert: 2026-04-06_
_Verifier: Claude (gsd-verifier)_
