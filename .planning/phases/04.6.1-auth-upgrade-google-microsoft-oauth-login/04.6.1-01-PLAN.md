---
phase: 04.6.1-auth-upgrade-google-microsoft-oauth-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - app.py
  - routes/auth.py
autonomous: true
requirements: [AUTH-OAUTH-03, AUTH-OAUTH-04, AUTH-OAUTH-06]
must_haves:
  truths:
    - "User-Tabelle enthält Spalten oauth_provider, oauth_id, avatar_url"
    - "passwort_hash ist im ORM nullable, bestehende Email/Passwort-User können sich weiterhin einloggen"
    - "_do_login akzeptiert leere/None passwort_hash ohne Crash und lehnt OAuth-Only-User für Email-Login ab"
    - "Helper _login_user(db, user) und _create_org_and_user(...) sind exportierbar und werden von api_login/api_register verwendet"
  artifacts:
    - path: database/models.py
      provides: "User-Model mit oauth_provider, oauth_id, avatar_url, passwort_hash nullable"
      contains: "oauth_provider"
    - path: app.py
      provides: "[DB] Migration für users.oauth_provider/oauth_id/avatar_url"
      contains: "oauth_provider"
    - path: routes/auth.py
      provides: "Refactored _do_login + neue Helpers _login_user, _create_org_and_user"
      contains: "_login_user"
  key_links:
    - from: routes/auth.py::api_login
      to: routes/auth.py::_login_user
      via: "Refactor — _do_login ruft _login_user nach Passwort-Check"
      pattern: "_login_user\\("
    - from: routes/auth.py::api_register
      to: routes/auth.py::_create_org_and_user
      via: "Refactor — api_register ruft Helper, OAuth-Flow ruft denselben"
      pattern: "_create_org_and_user\\("
---

<objective>
Datenbank-Schema und Auth-Helper für OAuth vorbereiten — OHNE den OAuth-Flow selbst.

Ergebnis: User-Tabelle hat OAuth-Felder, `passwort_hash` ist nullable-tolerant, `_do_login` ist gesplittet in `_login_user(db, user)` (Session-Setup für ein bereits validiertes User-Objekt) und der Email/Passwort-Pfad bleibt funktional. Register-Logik aus `api_register` wandert in `_create_org_and_user(...)`. Plan 02 (OAuth-Routes) und Plan 03 (UI) bauen darauf auf.

Output: Migrations-Logs `[DB] Migration: added users.oauth_provider/oauth_id/avatar_url`, refactored `routes/auth.py`, bestehende Email/Passwort-Login + Register weiterhin grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6.1-auth-upgrade-google-microsoft-oauth-login/04.6.1-CONTEXT.md
@.planning/phases/04.6.1-auth-upgrade-google-microsoft-oauth-login/04.6.1-RESEARCH.md
@CLAUDE.md
@routes/auth.py
@database/models.py
@app.py
</context>

<interfaces>
Bestehende Signaturen (vor Refactor):

```python
# routes/auth.py
def _do_login(email, passwort) -> tuple[dict | None, str | None]
def api_login()  # ruft _do_login
def api_register()  # legt Org+User+Session inline an
```

Neue Signaturen (nach Refactor):

```python
# routes/auth.py
def _login_user(db, user) -> dict
    """Setzt Flask-session + DbSession für ein bereits validiertes User-Objekt.
    Liest ALLE benoetigten User-Attribute VOR db.close() (CLAUDE.md Konvention).
    Returns {id, org_id, rolle, is_coach, onboarding_done}.
    """

def _do_login(email, passwort) -> tuple[dict | None, str | None]
    """Email/Passwort-Pfad. Muss leeren passwort_hash sicher ablehnen."""

def _create_org_and_user(
    db, *, email, vorname, nachname, firmenname,
    teamgroesse='1-5', passwort_hash=None,
    oauth_provider=None, oauth_id=None, avatar_url=None
) -> User
    """Legt Organisation + User in derselben DB-Session an. db.commit() muss
    der Caller machen. Wird sowohl von api_register als auch vom OAuth-Flow
    (Plan 02) genutzt."""
```

User-Model neue Felder (database/models.py):
```python
passwort_hash  = Column(String(256), nullable=True)  # vorher nullable=False
oauth_provider = Column(String(50),  nullable=True)
oauth_id       = Column(String(200), nullable=True)
avatar_url     = Column(String(500), nullable=True)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: User-Model + DB-Migration für OAuth-Felder</name>
  <files>database/models.py, app.py</files>
  <behavior>
    - Bestehende Email/Passwort-User können sich weiterhin einloggen (Regression-Check)
    - Neue Spalten existieren in users-Tabelle nach App-Start
    - passwort_hash akzeptiert NULL auf ORM-Ebene (SQLite toleriert in Praxis NULL trotz NOT NULL aus altem CREATE TABLE — pragmatischer Weg per CONTEXT)
  </behavior>
  <action>
    1. **database/models.py** (~Zeile 60): Im `User`-Model
       - `passwort_hash = Column(String(256), nullable=True)` (war `nullable=False`)
       - Direkt nach Block 12 (avg_deal_wert, ~Zeile 108) NEUEN Block 13 anhängen:
         ```python
         # Block 13: OAuth (Google + Microsoft) — Phase 04.6.1
         oauth_provider = Column(String(50),  nullable=True)  # 'google' | 'microsoft' | None
         oauth_id       = Column(String(200), nullable=True)  # Provider Sub-ID (eindeutig pro Provider)
         avatar_url     = Column(String(500), nullable=True)
         ```
       - KEINE neuen Indizes — Email-Lookup bleibt der Pfad zur Account-Verknüpfung (per D aus CONTEXT.md)

    2. **app.py** `_migrate()` (users-Block, ~Zeile 47-92): An Ende der Spaltenliste anhängen:
       ```python
       # Block 13: OAuth — Phase 04.6.1
       ('oauth_provider', 'VARCHAR(50)'),
       ('oauth_id',       'VARCHAR(200)'),
       ('avatar_url',     'VARCHAR(500)'),
       ```
       Pattern folgt exakt bestehendem `ALTER TABLE users ADD COLUMN ...` mit `try/except: pass`.

    3. **NICHT** versuchen, `passwort_hash NOT NULL` per ALTER COLUMN aufzuheben — SQLite kann das nicht und ein Tabellen-Rebuild ist riskant. Pragma per RESEARCH: ORM-Nullability reicht, neue OAuth-User bekommen in Plan 02 `passwort_hash=''` (leerer String) — `check_password_hash('', x)` gibt False, Email-Login bleibt sicher gesperrt. Diese Entscheidung als Inline-Kommentar an `passwort_hash`-Definition dokumentieren:
       ```python
       passwort_hash = Column(String(256), nullable=True)  # Phase 04.6.1: nullable für OAuth-User. SQLite Tabellen-NOT-NULL bleibt — OAuth-Flow setzt '' als Sentinel.
       ```

    4. App lokal starten (`python app.py` oder via `flask run`) und prüfen dass `[DB] Migration: added users.oauth_provider/oauth_id/avatar_url` in stdout erscheint UND der Server fehlerfrei hochkommt.
  </action>
  <verify>
    <automated>python -c "from database.db import engine; from sqlalchemy import inspect; cols=[c['name'] for c in inspect(engine).get_columns('users')]; assert 'oauth_provider' in cols and 'oauth_id' in cols and 'avatar_url' in cols, cols; print('OK', cols)"</automated>
  </verify>
  <done>users-Tabelle hat oauth_provider, oauth_id, avatar_url; Migration-Logs erschienen einmalig; bestehender Email/Passwort-Login funktioniert weiterhin (manuell mit Test-Account verifiziert).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: routes/auth.py refactoren — _login_user + _create_org_and_user extrahieren</name>
  <files>routes/auth.py</files>
  <behavior>
    - `_login_user(db, user)` setzt session/DbSession analog zum bestehenden _do_login-Erfolgs-Pfad
    - `_do_login(email, passwort)` ruft `_login_user` nach erfolgreichem Passwort-Check
    - `_do_login` lehnt User mit leerem oder None `passwort_hash` ab (Schutz gegen Email-Login für OAuth-Only-User) — Errortext: "E-Mail oder Passwort falsch." (gleicher Text, kein User-Enumeration)
    - `_create_org_and_user(db, ...)` legt Org + User an, ruft KEIN commit (Caller-Verantwortung)
    - `api_register` ruft `_create_org_and_user` und danach `_login_user`, Verhalten unverändert
    - `api_login` Verhalten unverändert (immer noch JSON {ok, coach})
  </behavior>
  <action>
    1. **routes/auth.py** — neuen Helper `_login_user(db, user)` direkt vor `_do_login` (~Zeile 46) einfügen. Logik: kopiere den Erfolgs-Pfad aus dem aktuellen `_do_login` (Zeilen 54-77), nimm aber `user` als Parameter statt es selbst zu laden. Beachte CLAUDE.md-Konvention: alle User-Attribute VOR Funktion-Ende lesen (db.close() macht der Caller).

    2. **`_do_login` umbauen** (~Zeile 46-80):
       ```python
       def _do_login(email, passwort):
           db = get_session()
           try:
               user = db.query(User).filter_by(email=email, aktiv=True).first()
               # Phase 04.6.1: leerer passwort_hash = OAuth-Only-User → Email-Login verboten
               if not user or not user.passwort_hash:
                   return None, 'E-Mail oder Passwort falsch.'
               if not check_password_hash(user.passwort_hash, passwort):
                   return None, 'E-Mail oder Passwort falsch.'
               user_info = _login_user(db, user)
               return user_info, None
           finally:
               db.close()
       ```
       Wichtig: Errortext IDENTISCH bei "User existiert nicht", "User hat keinen Passwort-Hash" und "Passwort falsch" — verhindert User-Enumeration (Threat T-04.6.1-04).

    3. **`_create_org_and_user` Helper** unter `_do_login` einfügen:
       ```python
       def _create_org_and_user(db, *, email, vorname, nachname, firmenname,
                                teamgroesse='1-5', passwort_hash=None,
                                oauth_provider=None, oauth_id=None, avatar_url=None):
           """Legt Organisation + User an. Caller macht db.commit().
           Wird von api_register (Email-Pfad) und routes/oauth.py (OAuth-Pfad, Plan 02) genutzt.
           """
           from database.models import Organisation as OrgModel
           from config import PLANS
           from datetime import timedelta
           size_to_plan = {'1-5': 'starter', '6-15': 'starter', '16-30': 'business', '30+': 'business'}
           plan      = size_to_plan.get(teamgroesse, 'starter')
           max_users = PLANS[plan]['max_users']
           now       = datetime.now(timezone.utc).replace(tzinfo=None)
           trial_end = now + timedelta(days=14)
           org = OrgModel(name=firmenname, plan=plan, max_users=max_users,
                          billing_email=email, trial_starts_at=now)
           db.add(org)
           db.flush()
           user = User(
               org_id=org.id, email=email,
               passwort_hash=passwort_hash if passwort_hash is not None else '',
               rolle='owner', is_trial=True, trial_ends_at=trial_end,
               vorname=vorname, nachname=nachname,
               oauth_provider=oauth_provider,
               oauth_id=oauth_id,
               avatar_url=avatar_url,
               onboarding_done=False,  # OAuth-User durchlaufen Wizard wie Email-User
           )
           db.add(user)
           db.flush()
           return user
       ```

    4. **`api_register` umbauen** (~Zeile 96-155): Innenleben des try-Blocks ersetzen durch Aufruf des Helpers — Validierung bleibt VOR dem DB-Open. Nach erfolgreicher Anlage: `_login_user(db, user)` aufrufen (statt manuell session zu setzen + DbSession anzulegen). `session['welcome_trial'] = True` weiterhin nach `_login_user` setzen. Verhalten 1:1 erhalten — gleiche JSON-Response `{'ok': True}`.

    5. Bestehende Imports oben in `routes/auth.py` prüfen — `Organisation`, `MAX_SESSION_HOURS`, `PLANS` sind bereits importiert. Keine neuen Top-Level-Imports nötig.
  </action>
  <verify>
    <automated>python -c "from routes.auth import _login_user, _do_login, _create_org_and_user; import inspect; sig=inspect.signature(_create_org_and_user); assert 'oauth_provider' in sig.parameters and 'firmenname' in sig.parameters; print('OK', list(sig.parameters))"</automated>
  </verify>
  <done>_login_user + _create_org_and_user existieren und sind importierbar; api_login + api_register funktionieren unverändert (manuell: bestehender Test-User Login + Neuanlage via Landing-Modal beide grün); _do_login lehnt User mit leerem passwort_hash mit Standard-Error ab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP-Request → Flask-Route | Untrusted Input (email, passwort) erreicht api_login |
| ORM → SQLite | passwort_hash NULL/'' wird zum ersten Mal toleriert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.6.1-01 | Spoofing | _do_login | mitigate | Leerer passwort_hash → sofortige Ablehnung mit identischem Errortext, verhindert Login-Bypass für OAuth-Only-User |
| T-04.6.1-02 | Tampering | DB-Migration | mitigate | ALTER TABLE in try/except/pass — Wiederholte Starts idempotent, keine Schema-Drift |
| T-04.6.1-03 | Information Disclosure | _do_login Errortext | mitigate | Identischer Errortext für "User existiert nicht", "kein Hash", "Passwort falsch" → keine User-Enumeration |
| T-04.6.1-04 | Elevation of Privilege | _create_org_and_user | mitigate | rolle hardcoded auf 'owner' nur für die NEUE Org (eigene), kein Cross-Org-Zugriff möglich |
| T-04.6.1-05 | Repudiation | DbSession-Token | accept | Bestehendes Pattern unverändert — out of scope für diese Phase |
</threat_model>

<verification>
- `python -c "import routes.auth"` lädt fehlerfrei
- App-Start: `[DB] Migration: added users.oauth_provider` (einmalig) erscheint
- Bestehender Email/Passwort-Login eines Test-Users funktioniert
- Neuanlage via `/?modal=register` schließt bis "Jetzt kostenlos starten" durch
- `users`-Tabelle Schema enthält die 3 neuen Spalten (`PRAGMA table_info(users)`)
</verification>

<success_criteria>
1. users-Tabelle hat oauth_provider, oauth_id, avatar_url
2. _login_user und _create_org_and_user existieren in routes/auth.py
3. _do_login lehnt OAuth-Only-User (leerer passwort_hash) ab
4. api_login und api_register arbeiten regression-frei weiter
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04.6.1-auth-upgrade-google-microsoft-oauth-login/04.6.1-01-SUMMARY.md` erstellen.
</output>
</content>
</invoke>