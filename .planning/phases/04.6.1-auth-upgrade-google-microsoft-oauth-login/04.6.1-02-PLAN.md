---
phase: 04.6.1-auth-upgrade-google-microsoft-oauth-login
plan: 02
type: execute
wave: 2
depends_on: ["04.6.1-01"]
files_modified:
  - requirements.txt
  - .env.example
  - config.py
  - routes/oauth.py
  - app.py
autonomous: true
requirements: [AUTH-OAUTH-01, AUTH-OAUTH-02, AUTH-OAUTH-03, AUTH-OAUTH-04]
user_setup:
  - service: google
    why: "OAuth Login mit Google-Account"
    env_vars:
      - name: GOOGLE_CLIENT_ID
        source: "Google Cloud Console → APIs & Services → Credentials → OAuth Client ID"
      - name: GOOGLE_CLIENT_SECRET
        source: "Google Cloud Console (zusammen mit Client-ID generiert)"
    dashboard_config:
      - task: "Authorized redirect URIs eintragen (bereits erledigt)"
        location: "https://app.getnerve.app/auth/google/callback und http://localhost:5000/auth/google/callback"
  - service: microsoft
    why: "OAuth Login mit Microsoft-Account (Work + Personal via common-Tenant)"
    env_vars:
      - name: MICROSOFT_CLIENT_ID
        source: "Azure Portal → App registrations → Application (client) ID"
      - name: MICROSOFT_CLIENT_SECRET
        source: "Azure Portal → Certificates & secrets"
    dashboard_config:
      - task: "Redirect URI eintragen (bereits erledigt)"
        location: "https://app.getnerve.app/auth/microsoft/callback"
must_haves:
  truths:
    - "GET /auth/google leitet zu accounts.google.com mit state-Parameter um"
    - "GET /auth/microsoft leitet zu login.microsoftonline.com/common um"
    - "GET /auth/google/callback mit gültigem Code: bestehender User → Login + Redirect /dashboard, neuer User → Org+User angelegt + Redirect /onboarding"
    - "GET /auth/microsoft/callback verhält sich identisch (ohne email_verified-Check)"
    - "OAuth-Abbruch oder Provider-Fehler: Redirect /?modal=login&oauth_error=... mit kurzer deutscher Fehlermeldung"
    - "ProxyFix in app.py aktiv → Redirect URIs werden mit https:// gebaut hinter dem Reverse-Proxy"
  artifacts:
    - path: routes/oauth.py
      provides: "oauth_bp Blueprint mit 4 Routes + init_oauth(app) + _oauth_login_or_create Helper"
      contains: "google_callback"
    - path: requirements.txt
      provides: "authlib>=1.3.0"
      contains: "authlib"
    - path: config.py
      provides: "GOOGLE_CLIENT_ID/SECRET, MICROSOFT_CLIENT_ID/SECRET aus env geladen"
      contains: "GOOGLE_CLIENT_ID"
    - path: .env.example
      provides: "OAuth-Vars dokumentiert"
      contains: "GOOGLE_CLIENT_ID"
    - path: app.py
      provides: "oauth_bp registriert + init_oauth(app) gerufen + ProxyFix verifiziert"
      contains: "oauth_bp"
  key_links:
    - from: routes/oauth.py::google_callback
      to: routes/auth.py::_login_user
      via: "Email-Match in DB → bestehender User → _login_user(db, user)"
      pattern: "_login_user\\("
    - from: routes/oauth.py::google_callback
      to: routes/auth.py::_create_org_and_user
      via: "Email kein Match → neuer User+Org mit oauth_provider/_id/avatar_url"
      pattern: "_create_org_and_user\\("
    - from: app.py
      to: routes/oauth.py::init_oauth
      via: "init_oauth(app) nach register_blueprint(oauth_bp)"
      pattern: "init_oauth\\(app\\)"
---

<objective>
Server-seitiger OAuth-Flow für Google + Microsoft via authlib. Nutzt die Helper aus Plan 01 (`_login_user`, `_create_org_and_user`).

Nach diesem Plan funktioniert der OAuth-Flow vollständig per direkter URL (`https://localhost:5000/auth/google` startet den Redirect, nach Provider-Login landet der User auf /onboarding oder /dashboard). UI-Buttons im Login-Modal kommen erst in Plan 03.

Output: `routes/oauth.py` mit oauth_bp, registriert in app.py; ProxyFix verifiziert/aktiviert; authlib in requirements.txt; .env.example dokumentiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.6.1-auth-upgrade-google-microsoft-oauth-login/04.6.1-CONTEXT.md
@.planning/phases/04.6.1-auth-upgrade-google-microsoft-oauth-login/04.6.1-RESEARCH.md
@.planning/phases/04.6.1-auth-upgrade-google-microsoft-oauth-login/04.6.1-01-SUMMARY.md
@CLAUDE.md
@routes/auth.py
@app.py
@config.py
@requirements.txt
@.env.example
</context>

<interfaces>
**Aus Plan 01 (Voraussetzung):**
```python
# routes/auth.py
from routes.auth import _login_user, _create_org_and_user

_login_user(db, user) -> dict
_create_org_and_user(db, *, email, vorname, nachname, firmenname,
                     teamgroesse='1-5', passwort_hash=None,
                     oauth_provider=None, oauth_id=None, avatar_url=None) -> User
```

**Authlib OIDC Discovery URLs:**
- Google: `https://accounts.google.com/.well-known/openid-configuration`
- Microsoft (common, Work+Personal): `https://login.microsoftonline.com/common/v2.0/.well-known/openid-configuration`

**Authlib Userinfo-Felder (aus ID-Token, authlib ≥1.0 parst automatisch):**
- Google: `sub`, `email`, `email_verified` (bool), `given_name`, `family_name`, `picture`
- Microsoft: `sub` (oid), `email` ODER `preferred_username`, `name`, KEIN garantiertes `email_verified`

**bestehende Routes-Imports in app.py (Zeile 683-697):**
```python
app.register_blueprint(auth_bp)
# … andere
app.register_blueprint(performance_bp)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dependencies + Config + Env-Beispiel</name>
  <files>requirements.txt, .env.example, config.py</files>
  <behavior>
    - `pip install -r requirements.txt` zieht authlib 1.3+
    - config.py exportiert GOOGLE_CLIENT_ID/SECRET und MICROSOFT_CLIENT_ID/SECRET aus os.environ
    - .env.example dokumentiert die 4 neuen Vars (Werte als Platzhalter, NICHT die echten Secrets)
  </behavior>
  <action>
    1. **requirements.txt**: Zeile anhängen `authlib>=1.3.0`. Bestehende Reihenfolge (alphabetisch nicht erzwungen) — Anhang am Ende OK.

    2. **config.py** (~Zeile 19, nach STRIPE-Block): einfügen
       ```python
       # OAuth (Phase 04.6.1)
       GOOGLE_CLIENT_ID        = os.environ.get('GOOGLE_CLIENT_ID', '')
       GOOGLE_CLIENT_SECRET    = os.environ.get('GOOGLE_CLIENT_SECRET', '')
       MICROSOFT_CLIENT_ID     = os.environ.get('MICROSOFT_CLIENT_ID', '')
       MICROSOFT_CLIENT_SECRET = os.environ.get('MICROSOFT_CLIENT_SECRET', '')
       ```
       KEIN Default mit echtem Wert. KEIN print/log dieser Werte.

    3. **.env.example** anhängen:
       ```
       
       # OAuth (Phase 04.6.1)
       GOOGLE_CLIENT_ID=your_google_client_id_here
       GOOGLE_CLIENT_SECRET=your_google_client_secret_here
       MICROSOFT_CLIENT_ID=your_microsoft_client_id_here
       MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here
       ```

    4. **NICHT** die echten Secrets aus CONTEXT.md irgendwo committen — sie gehören nur in `.env` (gitignored). Falls `.env` lokal noch nicht existiert: anlegen ist Task des Users (steht in user_setup), nicht dieses Tasks.

    5. `pip install -r requirements.txt` lokal ausführen, prüfen dass authlib + cryptography ohne Fehler installieren.
  </action>
  <verify>
    <automated>python -c "import authlib; from authlib.integrations.flask_client import OAuth; from config import GOOGLE_CLIENT_ID, MICROSOFT_CLIENT_ID; print('authlib', authlib.__version__, 'config OK')"</automated>
  </verify>
  <done>authlib importierbar, config exportiert die 4 Vars, .env.example dokumentiert sie, keine Secrets im Repo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: routes/oauth.py — oauth_bp Blueprint mit Google + Microsoft</name>
  <files>routes/oauth.py</files>
  <behavior>
    - Modul-globale `oauth = OAuth()` Instanz
    - `init_oauth(app)` registriert Google + Microsoft mit server_metadata_url
    - 4 Routes: /auth/google, /auth/google/callback, /auth/microsoft, /auth/microsoft/callback
    - Helper `_oauth_login_or_create(provider, oauth_id, email, vorname, nachname, avatar_url)` macht die DB-Logik EINMAL für beide Provider
    - Email-Match → bestehender User → OAuth-Felder nachziehen (UPDATE) → _login_user → Redirect
    - Kein Match → _create_org_and_user mit OAuth-Feldern + Platzhalter-Firmenname (z. B. "Mein Unternehmen") + passwort_hash='' → _login_user → Redirect /onboarding
    - Onboarding-Check: wenn bestehender User onboarding_done=False → Redirect /onboarding, sonst /dashboard
    - Fehler-Handling: try/except um authorize_access_token, Redirect /?modal=login&oauth_error=<code> mit flash
    - Google: `email_verified` MUSS True sein
    - Microsoft: KEIN email_verified-Check (Research-Befund: Microsoft setzt das Feld nicht zuverlässig)
    - Session-Fixation-Schutz: VOR _login_user `session.clear()` aufrufen (außer csrf-relevante authlib-Keys, die authorize_access_token bereits konsumiert hat)
  </behavior>
  <action>
    1. **routes/oauth.py** neu anlegen:
       ```python
       """OAuth 2.0 / OIDC Login für Google + Microsoft (Phase 04.6.1)."""
       from flask import Blueprint, redirect, url_for, session, flash, request
       from authlib.integrations.flask_client import OAuth
       from database.db import get_session
       from database.models import User
       from routes.auth import _login_user, _create_org_and_user
       from config import (
           GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET,
           MICROSOFT_CLIENT_ID, MICROSOFT_CLIENT_SECRET,
       )

       oauth_bp = Blueprint('oauth', __name__)
       oauth = OAuth()


       def init_oauth(app):
           """Wird in app.py nach register_blueprint(oauth_bp) aufgerufen."""
           oauth.init_app(app)
           if GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET:
               oauth.register(
                   name='google',
                   client_id=GOOGLE_CLIENT_ID,
                   client_secret=GOOGLE_CLIENT_SECRET,
                   server_metadata_url='https://accounts.google.com/.well-known/openid-configuration',
                   client_kwargs={'scope': 'openid email profile'},
               )
               print('[OAuth] Google client registered')
           else:
               print('[OAuth] Google credentials missing — /auth/google disabled')
           if MICROSOFT_CLIENT_ID and MICROSOFT_CLIENT_SECRET:
               oauth.register(
                   name='microsoft',
                   client_id=MICROSOFT_CLIENT_ID,
                   client_secret=MICROSOFT_CLIENT_SECRET,
                   server_metadata_url='https://login.microsoftonline.com/common/v2.0/.well-known/openid-configuration',
                   client_kwargs={'scope': 'openid email profile'},
               )
               print('[OAuth] Microsoft client registered')
           else:
               print('[OAuth] Microsoft credentials missing — /auth/microsoft disabled')


       def _oauth_error_redirect(code, msg):
           flash(msg, 'error')
           return redirect(f'/?modal=login&oauth_error={code}')


       def _oauth_login_or_create(*, provider, oauth_id, email, vorname, nachname, avatar_url):
           """Email-Match → existierenden User loggen + OAuth-Felder nachziehen.
           Sonst neuen Org+User anlegen. Redirect zu /onboarding bzw. /dashboard.
           """
           if not email:
               return _oauth_error_redirect('no_email', 'Provider hat keine E-Mail geliefert.')
           email = email.strip().lower()
           db = get_session()
           try:
               user = db.query(User).filter_by(email=email).first()
               if user:
                   if not user.aktiv:
                       return _oauth_error_redirect('inactive', 'Account ist deaktiviert.')
                   # OAuth-Felder nachziehen (idempotent)
                   updated = False
                   if not user.oauth_provider:
                       user.oauth_provider = provider
                       updated = True
                   if not user.oauth_id:
                       user.oauth_id = oauth_id
                       updated = True
                   if not user.avatar_url and avatar_url:
                       user.avatar_url = avatar_url
                       updated = True
                   if updated:
                       db.flush()
                   onboarding_done_flag = bool(getattr(user, 'onboarding_done', True))
                   # Session-Fixation-Schutz: alte Session-Keys löschen, dann frisch setzen
                   session.clear()
                   _login_user(db, user)
                   db.commit()
                   print(f'[OAuth] {provider} login: existing user id={user.id}')
                   return redirect(url_for('onboarding.wizard') if not onboarding_done_flag else url_for('dashboard.index'))
               # Neuanlage
               firmenname_platzhalter = (vorname + ' ' + nachname).strip() or email.split('@')[0]
               firmenname_platzhalter = firmenname_platzhalter + ' (Workspace)'
               new_user = _create_org_and_user(
                   db,
                   email=email,
                   vorname=vorname or '',
                   nachname=nachname or '',
                   firmenname=firmenname_platzhalter,  # Onboarding-Wizard fragt richtigen Namen ab
                   teamgroesse='1-5',
                   passwort_hash='',  # OAuth-Only Sentinel
                   oauth_provider=provider,
                   oauth_id=oauth_id,
                   avatar_url=avatar_url,
               )
               session.clear()
               _login_user(db, new_user)
               db.commit()
               print(f'[OAuth] {provider} register: new user id={new_user.id}')
               return redirect(url_for('onboarding.wizard'))
           finally:
               db.close()


       # ── Google ────────────────────────────────────────────────────────────────
       @oauth_bp.route('/auth/google')
       def google_login():
           if not (GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET):
               return _oauth_error_redirect('not_configured', 'Google-Login nicht konfiguriert.')
           redirect_uri = url_for('oauth.google_callback', _external=True)
           return oauth.google.authorize_redirect(redirect_uri)


       @oauth_bp.route('/auth/google/callback')
       def google_callback():
           try:
               token = oauth.google.authorize_access_token()
           except Exception as e:
               print(f'[OAuth] Google callback failed: {type(e).__name__}')
               return _oauth_error_redirect('google_failed', 'Google-Login abgebrochen oder fehlgeschlagen.')
           userinfo = token.get('userinfo') or {}
           if not userinfo.get('email_verified', False):
               return _oauth_error_redirect('unverified', 'Google-Email ist nicht verifiziert.')
           return _oauth_login_or_create(
               provider='google',
               oauth_id=str(userinfo.get('sub', '')),
               email=userinfo.get('email', ''),
               vorname=userinfo.get('given_name', ''),
               nachname=userinfo.get('family_name', ''),
               avatar_url=userinfo.get('picture'),
           )


       # ── Microsoft ─────────────────────────────────────────────────────────────
       @oauth_bp.route('/auth/microsoft')
       def microsoft_login():
           if not (MICROSOFT_CLIENT_ID and MICROSOFT_CLIENT_SECRET):
               return _oauth_error_redirect('not_configured', 'Microsoft-Login nicht konfiguriert.')
           redirect_uri = url_for('oauth.microsoft_callback', _external=True)
           return oauth.microsoft.authorize_redirect(redirect_uri)


       @oauth_bp.route('/auth/microsoft/callback')
       def microsoft_callback():
           try:
               token = oauth.microsoft.authorize_access_token()
           except Exception as e:
               print(f'[OAuth] Microsoft callback failed: {type(e).__name__}')
               return _oauth_error_redirect('microsoft_failed', 'Microsoft-Login abgebrochen oder fehlgeschlagen.')
           userinfo = token.get('userinfo') or {}
           # Microsoft: KEIN email_verified-Check (siehe RESEARCH.md)
           email = userinfo.get('email') or userinfo.get('preferred_username') or ''
           name  = userinfo.get('name') or ''
           # Microsoft liefert nur "name" als ganzen String — pragmatisch splitten
           parts = name.split(' ', 1)
           vorname  = parts[0] if parts else ''
           nachname = parts[1] if len(parts) > 1 else ''
           return _oauth_login_or_create(
               provider='microsoft',
               oauth_id=str(userinfo.get('sub', '') or userinfo.get('oid', '')),
               email=email,
               vorname=vorname,
               nachname=nachname,
               avatar_url=None,  # Microsoft Graph /me/photo wäre ein zweiter Call — out of scope
           )
       ```

    2. Wichtig zu beachten: KEIN Open-Redirect — wir nutzen ausschließlich `url_for(...)` und literal `/?modal=login`, nie ein `next`-Param aus der Query.

    3. KEINE der `client_id`/`client_secret`-Werte loggen.
  </action>
  <verify>
    <automated>python -c "from routes.oauth import oauth_bp, init_oauth, _oauth_login_or_create, google_login, google_callback, microsoft_login, microsoft_callback; print('oauth_bp routes OK')"</automated>
  </verify>
  <done>routes/oauth.py existiert und importiert sauber; alle 4 Route-Handler + init_oauth + _oauth_login_or_create exportiert; keine Secrets in Logs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: app.py — oauth_bp registrieren + init_oauth + ProxyFix verifizieren</name>
  <files>app.py</files>
  <behavior>
    - oauth_bp ist registriert (analog auth_bp)
    - init_oauth(app) wird genau einmal nach Blueprint-Registrierung aufgerufen
    - ProxyFix ist aktiv (x_proto=1, x_host=1) — sodass `url_for(..., _external=True)` hinter nginx https://app.getnerve.app/... baut, nicht http://localhost
    - App startet ohne Fehler auch wenn OAuth-Env-Vars fehlen (graceful degrade — Routes geben "not_configured" Error)
  </behavior>
  <action>
    1. **app.py** Imports-Block (~Zeile 1-12): nach den anderen Route-Imports (die unten in app.py vorhanden sein müssen — falls nicht, prüfe wo `auth_bp` importiert wird) ergänzen:
       ```python
       from routes.oauth import oauth_bp, init_oauth
       ```
       (Falls die Route-Imports zentral als `from routes.auth import auth_bp` weiter unten passieren — analog dort einfügen, NICHT oben am Anfang. Suche per grep `from routes.auth import auth_bp`.)

    2. **app.py** Blueprint-Registrierungen (~Zeile 683-697): NACH `app.register_blueprint(performance_bp)` einfügen:
       ```python
       app.register_blueprint(oauth_bp)
       init_oauth(app)
       ```

    3. **ProxyFix verifizieren** — grep nach `ProxyFix` in app.py. Wenn bereits aktiv: nichts tun. Wenn NICHT aktiv: nach `app = Flask(__name__)` (~Zeile 22) einfügen:
       ```python
       from werkzeug.middleware.proxy_fix import ProxyFix
       app.wsgi_app = ProxyFix(app.wsgi_app, x_proto=1, x_host=1, x_for=1)
       ```
       Begründung als Inline-Kommentar: `# Phase 04.6.1: für korrekte https-Redirect-URIs hinter nginx`.

    4. App lokal starten (`python app.py`). Erwartetes Log:
       - `[OAuth] Google client registered` (oder `… missing — disabled`, wenn .env nicht gesetzt)
       - `[OAuth] Microsoft client registered`
       - Keine Tracebacks.

    5. Manueller Smoke-Test (falls .env gesetzt):
       - Browser auf `http://localhost:5000/auth/google`
       - Erwartung: Redirect zu `accounts.google.com/o/oauth2/v2/auth?...&state=...&nonce=...`
       - Nach Provider-Login: Redirect zu `http://localhost:5000/auth/google/callback?code=...&state=...`
       - Endzustand: User landet auf `/onboarding` (neuer User) oder `/dashboard` (existing).
  </action>
  <verify>
    <automated>python -c "import app; assert 'oauth.google_login' in {r.endpoint for r in app.app.url_map.iter_rules()}; print('oauth_bp registered, ProxyFix:', type(app.app.wsgi_app).__name__)"</automated>
  </verify>
  <done>oauth_bp registriert, init_oauth aufgerufen, ProxyFix aktiv, App startet fehlerfrei mit den OAuth-Init-Logs; manueller Browser-Test zu /auth/google leitet zu accounts.google.com.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /auth/google[/callback] | User-controlled query params (code, state) |
| OAuth-Provider → Callback | ID-Token kommt von Google/Microsoft, signiert |
| OS-Env → config.py | Secrets aus .env, dürfen nie in Logs landen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.6.1-06 | Spoofing | google_callback / microsoft_callback | mitigate | authlib `authorize_access_token()` validiert state-Parameter (CSRF) und Nonce im ID-Token automatisch — eigener Code MUSS authlib nutzen, kein manuelles Token-Handling |
| T-04.6.1-07 | Tampering | ID-Token Signatur | mitigate | authlib lädt JWKS via server_metadata_url und prüft Signatur automatisch — keine hardcoded Endpoints |
| T-04.6.1-08 | Repudiation | Replay des Authorization-Codes | mitigate | authlib verbraucht state+nonce einmalig pro Session-Eintrag; Code wird vom Provider nach Einlösung invalidiert |
| T-04.6.1-09 | Information Disclosure | client_secret Logging | mitigate | KEINE print() der Werte aus config; nur "registered" / "missing" als Status |
| T-04.6.1-10 | Information Disclosure | Open Redirect | mitigate | Redirect-Ziele sind ausschließlich `url_for(...)` (intern) und literal `/?modal=login` — kein `next`-Query-Param, keine Whitelist nötig weil keine User-Input-Redirects |
| T-04.6.1-11 | Denial of Service | OAuth-Discovery-Endpoint nicht erreichbar | accept | authlib cached Discovery-Daten; bei Ausfall der Provider-Endpoints ist Login vorübergehend nicht nutzbar — Email/Passwort bleibt funktional |
| T-04.6.1-12 | Elevation of Privilege | Account Takeover via unverifizierte Email | mitigate | Google: `email_verified` MUSS True sein; Microsoft: Workaround dass nur der `common`-Tenant Personal+Work-Accounts mit Microsoft-validierten Emails liefert (akzeptiertes Restrisiko, in CONTEXT.md so beschlossen) |
| T-04.6.1-13 | Elevation of Privilege | Session Fixation | mitigate | session.clear() VOR _login_user → frische Session-ID, alte Tokens entwertet |
| T-04.6.1-14 | Spoofing | Falsches Redirect-URI hinter Reverse-Proxy | mitigate | ProxyFix(x_proto=1, x_host=1) → url_for(..., _external=True) baut https://app.getnerve.app/... korrekt; verhindert dass authlib http://localhost als Redirect-URI an den Provider sendet |
| T-04.6.1-15 | Tampering | Microsoft email_verified fehlt | accept | Bewusst akzeptiert (CONTEXT.md): MS `common`-Tenant validiert Emails serverseitig vor Token-Ausgabe; kein eigener Check |
</threat_model>

<verification>
- `python -c "import app"` lädt fehlerfrei
- `app.url_map` enthält `oauth.google_login`, `oauth.google_callback`, `oauth.microsoft_login`, `oauth.microsoft_callback`
- ProxyFix in `type(app.wsgi_app).__name__` sichtbar
- Manueller Browser-Test gegen `http://localhost:5000/auth/google` → Provider-Redirect mit state-Parameter
- Nach Callback: Login funktioniert, neuer OAuth-User landet im Onboarding-Wizard
- DB-Check: `SELECT email, oauth_provider, oauth_id FROM users WHERE oauth_provider IS NOT NULL` — Spalten gefüllt
- Email/Passwort-Login eines NICHT-OAuth-Test-Users funktioniert weiterhin (Regression)
- Server-Logs: keine `client_secret`-Werte sichtbar
</verification>

<success_criteria>
1. `/auth/google` und `/auth/microsoft` starten OIDC-Flow mit state+nonce
2. Callback verarbeitet existing User (Email-Match) und neuen User (Anlage)
3. ProxyFix sorgt für https-Redirect-URI in Produktion
4. OAuth-Fehler landen auf `/?modal=login&oauth_error=...` mit deutscher flash-Nachricht
5. Bestehende Email/Passwort-Auth ist regression-frei
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04.6.1-auth-upgrade-google-microsoft-oauth-login/04.6.1-02-SUMMARY.md` erstellen.
</output>
</content>
</invoke>