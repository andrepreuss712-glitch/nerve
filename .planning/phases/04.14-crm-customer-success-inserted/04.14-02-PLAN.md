---
phase: 04.14-crm-customer-success-inserted
plan: 02
type: execute
wave: 2
depends_on: ["04.14-01"]
files_modified:
  - routes/admin_views.py
  - templates/admin/crm_overview.html
  - app.py
autonomous: false
requirements:
  - CRM-06
  - CRM-07
  - CRM-08
  - CRM-09
  - CRM-10
  - CRM-11
  - CRM-12

must_haves:
  truths:
    - "Superadmin can see /admin/crm page with all non-superadmin users listed"
    - "Each user row shows a colored status badge (Aktiv/Ruhig/Churn-Risiko/Top)"
    - "Superadmin can save a free-text CRM note per user via AJAX"
    - "Follow-up hints section shows users who need attention with reasons"
    - "User overview is sorted: churn first, then ruhig, then aktiv, then top"
    - "Non-superadmin users cannot access /admin/crm"
    - "No raw transcript text displayed anywhere (DSGVO)"
  artifacts:
    - path: "routes/admin_views.py"
      provides: "CrmView (BaseView) with index and save_note endpoints"
      contains: "class CrmView"
    - path: "templates/admin/crm_overview.html"
      provides: "CRM overview page with user table, badges, notes, follow-up hints"
      contains: "crm_overview"
    - path: "app.py"
      provides: "CrmView registration with admin.add_view"
      contains: "CrmView"
  key_links:
    - from: "routes/admin_views.py"
      to: "services/customer_success_service.py"
      via: "import get_all_user_crm_data, get_followup_hints"
      pattern: "from services.customer_success_service import"
    - from: "app.py"
      to: "routes/admin_views.py"
      via: "admin.add_view(CrmView(...))"
      pattern: "admin\\.add_view.*CrmView"
    - from: "templates/admin/crm_overview.html"
      to: "routes/admin_views.py"
      via: "self.render('admin/crm_overview.html', ...)"
      pattern: "crm_overview\\.html"
---

<objective>
Build the Flask-Admin CRM view, template, and wire it into the app. The superadmin gets a CRM overview page at /admin/crm showing all users with status badges, inline CRM notes (AJAX save), and follow-up hints.

Purpose: Gives the founder a single-page view of customer health and actionable follow-ups per D-04, D-07, D-08, D-11.
Output: CrmView in admin_views.py, crm_overview.html template, app.py registration.
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.14-crm-customer-success-inserted/04.14-CONTEXT.md
@.planning/phases/04.14-crm-customer-success-inserted/04.14-RESEARCH.md
@.planning/phases/04.14-crm-customer-success-inserted/04.14-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01. Executor needs these. -->

From services/customer_success_service.py (created in Plan 01):
```python
STATUS_LABELS = {
    'top':    'Top',
    'aktiv':  'Aktiv',
    'ruhig':  'Ruhig',
    'churn':  'Churn-Risiko',
}

def get_all_user_crm_data(db):
    """Returns list of dicts with keys:
    'user' (User obj), 'user_id' (int), 'email' (str),
    'status' (str: top|aktiv|ruhig|churn), 'status_label' (str),
    'last_call' (datetime|None), 'total_calls_30d' (int),
    'avg_kb' (int), 'avg_dauer_min' (float), 'einwand_rate' (int),
    'notiz' (str), 'notiz_updated' (datetime|None),
    'letzte_aktivitaet' (datetime|None)
    Sorted: churn first, then ruhig, aktiv, top.
    """

def get_followup_hints(users_crm):
    """Returns list of dicts with keys:
    'user_email' (str), 'user_id' (int),
    'urgency' (str: high|medium), 'typ' (str: churn|inaktiv|performance),
    'reason' (str)
    Sorted: high urgency first.
    """
```

From database/models.py (CrmNote, created in Plan 01):
```python
class CrmNote(Base):
    __tablename__ = 'crm_notes'
    id         = Column(Integer, primary_key=True)
    user_id    = Column(Integer, ForeignKey('users.id'), nullable=False, unique=True)
    notiz      = Column(Text, nullable=True)
    updated_at = Column(DateTime, default=utcnow, onupdate=utcnow)
    created_at = Column(DateTime, default=utcnow)
```

From routes/admin_views.py (existing patterns):
```python
def _is_superadmin():
    return getattr(g, 'user', None) is not None and getattr(g.user, 'is_superadmin', False)

class KpiDashboardView(BaseView):
    def is_accessible(self):
        return _is_superadmin()
    def inaccessible_callback(self, name, **kwargs):
        return redirect(url_for('auth.login'))
    @expose('/')
    def index(self):
        db = get_session()
        try:
            ...
            return self.render('admin/kpi_dashboard.html', stats=stats)
        finally:
            db.close()
```

From app.py (registration pattern, lines 1286-1298):
```python
from routes.admin_views import (
    KpiDashboardView, PlanningListView, FeedbackAdmin,
    UserAdmin, OrgAdmin, ConvLogAdmin, AuditLogAdmin,
)
admin.add_view(KpiDashboardView(name='KPI', endpoint='kpi', url='/admin/kpi'))
admin.add_view(PlanningListView(name='Planung', endpoint='planning', url='/admin/planning'))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CrmView to admin_views.py and create crm_overview.html template</name>
  <files>routes/admin_views.py, templates/admin/crm_overview.html</files>
  <read_first>
    - routes/admin_views.py (full file — existing imports, _is_superadmin, KpiDashboardView pattern, all existing classes)
    - templates/admin/kpi_dashboard.html (existing admin template pattern — extends admin/master.html, block structure, Bootstrap4 classes used)
    - services/customer_success_service.py (Plan 01 output — verify function signatures)
    - database/models.py (CrmNote model — verify it exists from Plan 01)
  </read_first>
  <action>
**Part A: Add CrmView class to routes/admin_views.py**

Add at the end of the file (after PlanningListView class). Add `CrmNote` to the existing import from `database.models` at the top of the file (line 7-10 area).

```python
# Add CrmNote to existing import line:
from database.models import (
    User, Organisation, Feedback, AuditLog, ConversationLog,
    ObjectionEvent, PlanningFeedbackLink, CrmNote,
)

# ... (existing code stays unchanged) ...

# ── CRM & Customer Success ─────────────────────────────────────────

class CrmView(BaseView):
    def is_accessible(self):
        return _is_superadmin()

    def inaccessible_callback(self, name, **kwargs):
        if getattr(g, 'user', None) is None:
            return redirect(url_for('auth.login'))
        abort(403)

    @expose('/')
    def index(self):
        db = get_session()
        try:
            from services.customer_success_service import (
                get_all_user_crm_data, get_followup_hints,
            )
            users_crm = get_all_user_crm_data(db)
            hints = get_followup_hints(users_crm)
            status_counts = {}
            for u in users_crm:
                s = u['status']
                status_counts[s] = status_counts.get(s, 0) + 1
            return self.render(
                'admin/crm_overview.html',
                users=users_crm,
                hints=hints,
                status_counts=status_counts,
                total_users=len(users_crm),
            )
        finally:
            db.close()

    @expose('/note', methods=['POST'])
    def save_note(self):
        """AJAX endpoint: save CRM note for a user. Per D-05."""
        from flask import jsonify
        user_id = request.form.get('user_id', type=int)
        text = (request.form.get('notiz') or '').strip()
        if not user_id:
            return jsonify({'ok': False, 'error': 'user_id fehlt'}), 400
        # Cap note length at 5000 chars (input validation per ASVS V5)
        if len(text) > 5000:
            text = text[:5000]
        db = get_session()
        try:
            note = db.query(CrmNote).filter_by(user_id=user_id).first()
            if note:
                note.notiz = text
            else:
                note = CrmNote(user_id=user_id, notiz=text)
                db.add(note)
            db.commit()
            return jsonify({'ok': True})
        except Exception as e:
            db.rollback()
            return jsonify({'ok': False, 'error': str(e)}), 500
        finally:
            db.close()
```

**Part B: Create templates/admin/crm_overview.html**

Create the template file. It extends `admin/master.html` (Flask-Admin's base template, already used by kpi_dashboard.html). Uses Bootstrap4 badge classes already available in the admin theme. Per D-07: overview table as main view. Per D-12: Bootstrap4 standard, no custom theming.

The template must:
1. Show summary cards at top (total users, count per status) per D-07
2. Show follow-up hints section if any exist per D-08/D-09/D-10
3. Show user table with columns: Email, Status (colored badge), Letzter Call, Calls (30d), KB-Avg, Avg Dauer, EW-Rate, Notiz, Actions per D-06/D-07
4. Status badge colors: `badge-success` (aktiv), `badge-primary` (top), `badge-warning` (ruhig), `badge-danger` (churn) per RESEARCH.md
5. Inline note editing via AJAX (textarea + save button) — POST to `/admin/crm/note`
6. Auto-escape all note content (Jinja2 default) — NEVER use `|safe` on notiz field (XSS/DSGVO)
7. Date formatting: `strftime('%d.%m.%Y %H:%M')` for German locale

```html
{% extends 'admin/master.html' %}
{% block body %}

<h2>CRM & Customer Success</h2>

{# -- Summary Cards -- #}
<div class="row mb-4">
  <div class="col-md-2">
    <div class="card text-center">
      <div class="card-body py-2">
        <h4 class="mb-0">{{ total_users }}</h4>
        <small class="text-muted">Gesamt</small>
      </div>
    </div>
  </div>
  <div class="col-md-2">
    <div class="card text-center border-primary">
      <div class="card-body py-2">
        <h4 class="mb-0 text-primary">{{ status_counts.get('top', 0) }}</h4>
        <small>Top</small>
      </div>
    </div>
  </div>
  <div class="col-md-2">
    <div class="card text-center border-success">
      <div class="card-body py-2">
        <h4 class="mb-0 text-success">{{ status_counts.get('aktiv', 0) }}</h4>
        <small>Aktiv</small>
      </div>
    </div>
  </div>
  <div class="col-md-2">
    <div class="card text-center border-warning">
      <div class="card-body py-2">
        <h4 class="mb-0 text-warning">{{ status_counts.get('ruhig', 0) }}</h4>
        <small>Ruhig</small>
      </div>
    </div>
  </div>
  <div class="col-md-2">
    <div class="card text-center border-danger">
      <div class="card-body py-2">
        <h4 class="mb-0 text-danger">{{ status_counts.get('churn', 0) }}</h4>
        <small>Churn-Risiko</small>
      </div>
    </div>
  </div>
</div>

{# -- Follow-Up Hints -- #}
{% if hints %}
<div class="card mb-4">
  <div class="card-header bg-warning text-dark">
    <strong>Follow-Up Hinweise</strong> ({{ hints|length }})
  </div>
  <ul class="list-group list-group-flush">
    {% for h in hints %}
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <span>
        <strong>{{ h.user_email }}</strong> —
        {{ h.reason }}
      </span>
      <span class="badge badge-{{ 'danger' if h.urgency == 'high' else 'warning' }}">
        {{ h.urgency|upper }}
      </span>
    </li>
    {% endfor %}
  </ul>
</div>
{% endif %}

{# -- User Table -- #}
<div class="table-responsive">
<table class="table table-striped table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Email</th>
      <th>Status</th>
      <th>Letzter Call</th>
      <th>Calls (30d)</th>
      <th>KB-Avg</th>
      <th>Dauer (Avg)</th>
      <th>EW-Rate</th>
      <th>Letzter Login</th>
      <th style="min-width:250px">Notiz</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  {% set badge_map = {'aktiv': 'success', 'top': 'primary', 'ruhig': 'warning', 'churn': 'danger'} %}
  {% for u in users %}
    <tr>
      <td>{{ u.email }}</td>
      <td>
        <span class="badge badge-{{ badge_map.get(u.status, 'secondary') }}">
          {{ u.status_label }}
        </span>
      </td>
      <td>{{ u.last_call.strftime('%d.%m.%Y') if u.last_call else '---' }}</td>
      <td>{{ u.total_calls_30d }}</td>
      <td>{{ u.avg_kb }}%</td>
      <td>{{ u.avg_dauer_min }} min</td>
      <td>{{ u.einwand_rate }}%</td>
      <td>{{ u.letzte_aktivitaet.strftime('%d.%m.%Y') if u.letzte_aktivitaet else '---' }}</td>
      <td>
        <div class="crm-note-container" id="note-{{ u.user_id }}">
          <div class="crm-note-display" id="note-display-{{ u.user_id }}">
            {{ u.notiz or '' }}
            {% if u.notiz_updated %}
            <br><small class="text-muted">{{ u.notiz_updated.strftime('%d.%m.%Y %H:%M') }}</small>
            {% endif %}
          </div>
          <div class="crm-note-edit" id="note-edit-{{ u.user_id }}" style="display:none">
            <textarea class="form-control form-control-sm" rows="3"
                      id="note-text-{{ u.user_id }}" maxlength="5000">{{ u.notiz or '' }}</textarea>
            <div class="mt-1">
              <button class="btn btn-sm btn-success" onclick="saveNote({{ u.user_id }})">Speichern</button>
              <button class="btn btn-sm btn-secondary" onclick="cancelNote({{ u.user_id }})">Abbrechen</button>
            </div>
          </div>
        </div>
      </td>
      <td>
        <button class="btn btn-sm btn-outline-secondary" onclick="editNote({{ u.user_id }})">
          Notiz
        </button>
      </td>
    </tr>
  {% endfor %}
  </tbody>
</table>
</div>

<script>
function editNote(uid) {
  document.getElementById('note-display-' + uid).style.display = 'none';
  document.getElementById('note-edit-' + uid).style.display = 'block';
}
function cancelNote(uid) {
  document.getElementById('note-display-' + uid).style.display = 'block';
  document.getElementById('note-edit-' + uid).style.display = 'none';
}
function saveNote(uid) {
  var text = document.getElementById('note-text-' + uid).value;
  var fd = new FormData();
  fd.append('user_id', uid);
  fd.append('notiz', text);
  fetch('/admin/crm/note', {method: 'POST', body: fd})
    .then(function(r) { return r.json(); })
    .then(function(data) {
      if (data.ok) {
        document.getElementById('note-display-' + uid).innerHTML =
          text + '<br><small class="text-muted">gerade gespeichert</small>';
        cancelNote(uid);
      } else {
        alert('Fehler: ' + (data.error || 'Unbekannt'));
      }
    })
    .catch(function(e) { alert('Netzwerkfehler: ' + e); });
}
</script>

{% endblock %}
```

Key constraints:
- NEVER use `|safe` filter on `u.notiz` — Jinja2 auto-escaping handles XSS prevention
- Note text saved via `innerHTML` after save is from user's own textarea (founder-only tool, D-11)
- No raw transcript fields displayed (no gegenargument_details, painpoints_details) per DSGVO
- Per D-08: dashboard display only, no automatic emails
- Per D-12: Bootstrap4 standard, no custom CSS theming
  </action>
  <verify>
    <automated>python -c "from routes.admin_views import CrmView; print('CrmView OK')" && python -c "import os; assert os.path.exists('templates/admin/crm_overview.html'), 'Template missing'; print('Template OK')"</automated>
  </verify>
  <acceptance_criteria>
    - routes/admin_views.py contains `class CrmView(BaseView):`
    - routes/admin_views.py contains `def save_note(self):`
    - routes/admin_views.py contains `from services.customer_success_service import`
    - routes/admin_views.py contains `CrmNote` in the models import line
    - routes/admin_views.py contains `len(text) > 5000` (input validation)
    - templates/admin/crm_overview.html exists
    - templates/admin/crm_overview.html contains `{% extends 'admin/master.html' %}`
    - templates/admin/crm_overview.html contains `badge-success` and `badge-danger` and `badge-warning` and `badge-primary`
    - templates/admin/crm_overview.html contains `Follow-Up Hinweise`
    - templates/admin/crm_overview.html contains `saveNote` JavaScript function
    - templates/admin/crm_overview.html contains `/admin/crm/note` fetch URL
    - templates/admin/crm_overview.html does NOT contain `|safe` applied to notiz
    - templates/admin/crm_overview.html does NOT contain `gegenargument_details` or `painpoints_details`
  </acceptance_criteria>
  <done>CrmView class with index() and save_note() exists in admin_views.py. crm_overview.html template shows user table with status badges, follow-up hints, and inline note editing via AJAX.</done>
</task>

<task type="auto">
  <name>Task 2: Register CrmView in app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py (lines 1280-1300 area — existing admin.add_view calls and import from routes.admin_views)
    - routes/admin_views.py (verify CrmView class exists from Task 1)
  </read_first>
  <action>
Two changes to app.py:

1. **Add CrmView to the import** (around line 1286-1290). The existing import is:
```python
from routes.admin_views import (
    KpiDashboardView, PlanningListView, FeedbackAdmin,
    UserAdmin, OrgAdmin, ConvLogAdmin, AuditLogAdmin,
)
```
Add `CrmView` to this import list.

2. **Add admin.add_view call** (after the existing add_view calls, around line 1298). Add:
```python
admin.add_view(CrmView(name='CRM', endpoint='crm_view', url='/admin/crm'))
```

Use `endpoint='crm_view'` (NOT `endpoint='crm'`) to avoid potential namespace collision per RESEARCH.md Pitfall 2.

No other changes to app.py.
  </action>
  <verify>
    <automated>python -c "import app; print('App imports OK')" && grep -n "CrmView" app.py</automated>
  </verify>
  <acceptance_criteria>
    - app.py contains `CrmView` in the import from routes.admin_views
    - app.py contains `admin.add_view(CrmView(name='CRM', endpoint='crm_view', url='/admin/crm'))`
    - app.py does NOT use `endpoint='crm'` (must be `endpoint='crm_view'`)
  </acceptance_criteria>
  <done>CrmView registered in app.py. /admin/crm route is accessible to superadmin users.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CRM dashboard end-to-end</name>
  <files>n/a</files>
  <action>Human verifies the complete CRM dashboard at /admin/crm — status badges, notes, follow-up hints, access control.</action>
  <what-built>Complete CRM and Customer Success dashboard in Flask-Admin: status badges (Aktiv/Ruhig/Churn-Risiko/Top), inline CRM notes with AJAX save, follow-up hints panel, user metrics table.</what-built>
  <how-to-verify>
    1. Start the app: `python app.py`
    2. Log in as superadmin user
    3. Navigate to /admin/crm
    4. Verify: Summary cards show user counts per status at the top
    5. Verify: User table shows all non-superadmin users with colored status badges
    6. Verify: Badge colors — green (Aktiv), blue (Top), yellow (Ruhig), red (Churn-Risiko)
    7. Verify: Table columns show Email, Status, Letzter Call, Calls (30d), KB-Avg, Dauer, EW-Rate, Letzter Login, Notiz
    8. Click "Notiz" button on any user — verify textarea appears
    9. Type a note (e.g. "Test-Notiz fuer CRM") and click "Speichern" — verify it saves and displays
    10. Refresh page — verify saved note persists
    11. If users have been inactive: verify Follow-Up Hinweise section appears with reasons
    12. Verify: No raw transcript text visible anywhere
    13. Log out and try /admin/crm as non-superadmin — verify access denied
  </how-to-verify>
  <verify>Human confirms all 13 verification steps pass</verify>
  <done>CRM dashboard verified working end-to-end by human</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /admin/crm | Superadmin-only route, auth gate via is_accessible() |
| Browser -> /admin/crm/note POST | AJAX note save, input validation needed |
| Template -> user data | Jinja2 auto-escaping protects against stored XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.14-03 | Elevation of Privilege | CrmView | mitigate | `is_accessible()` checks `_is_superadmin()`. `inaccessible_callback` returns 403 for logged-in non-superadmins, redirect to login for anonymous. Same pattern as KpiDashboardView. |
| T-04.14-04 | Tampering | save_note POST | mitigate | Input validation: `user_id` must be int, `notiz` capped at 5000 chars. Upsert pattern prevents duplicate key errors. db.rollback() on exception. |
| T-04.14-05 | Tampering (XSS) | crm_overview.html | mitigate | Jinja2 auto-escaping is ON by default. Template MUST NOT use `\|safe` on any user-controlled content (notiz field). Verified in acceptance_criteria. |
| T-04.14-06 | Information Disclosure | crm_overview.html | mitigate | Template only displays aggregated metrics (avg_kb, call_count, einwand_rate). Never displays raw ConversationLog text fields (gegenargument_details, painpoints_details). Enforced via acceptance_criteria grep check. |
| T-04.14-07 | Spoofing (CSRF) | save_note POST | accept | Flask session cookie provides same-origin protection. Admin is login-gated. Low risk for internal founder-only tool with 1 user. |
</threat_model>

<verification>
- /admin/crm loads for superadmin, returns 403 for non-superadmin
- User table shows status badges with correct colors
- CRM notes save and persist via AJAX
- Follow-up hints appear for inactive/underperforming users
- No `|safe` filter on notiz content in template
- No raw transcript fields in template
</verification>

<success_criteria>
- CrmView registered at /admin/crm in app.py
- Superadmin sees all users with colored status badges per D-01/D-02/D-07
- Notes are saveable per user via AJAX per D-05
- Follow-up hints display inactivity + performance reasons per D-08/D-09/D-10
- Access restricted to superadmin per D-11
- Bootstrap4 standard styling per D-12
- DSGVO: no raw transcripts displayed
</success_criteria>

<output>
After completion, create `.planning/phases/04.14-crm-customer-success-inserted/04.14-02-SUMMARY.md`
</output>
