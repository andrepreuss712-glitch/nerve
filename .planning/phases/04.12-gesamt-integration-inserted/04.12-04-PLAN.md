---
phase: 04.12-gesamt-integration-inserted
plan: 04
type: execute
wave: 3
depends_on: ["04.12-02", "04.12-03"]
files_modified:
  - templates/app.html
  - templates/dashboard.html
  - routes/app_routes.py
  - routes/dashboard.py
autonomous: false
requirements: []
must_haves:
  truths:
    - "PostCall-Overlay zeigt Trainings-Empfehlung Block nach Lernkarten-Vorschlaegen (D-12)"
    - "Dashboard zeigt persistente Trainings-Empfehlung Card bis Training absolviert (D-12)"
    - "Ein-Klick-Start fuehrt zu /training mit vorkonfiguriertem Einwand-Typ (D-12)"
    - "Empfehlung verschwindet nach Training-Abschluss (D-12)"
  artifacts:
    - path: "templates/app.html"
      provides: "Trainings-Empfehlung Block im PostCall-Overlay"
      contains: "training-recommendation-block"
    - path: "templates/dashboard.html"
      provides: "Persistente Trainings-Empfehlung Card"
      contains: "training-rec-card"
    - path: "routes/dashboard.py"
      provides: "pending_training_recommendation an Template"
      contains: "pending_training_recommendation"
  key_links:
    - from: "templates/dashboard.html"
      to: "routes/dashboard.py"
      via: "Jinja Variable training_recommendation"
      pattern: "training_recommendation"
    - from: "templates/app.html"
      to: "/api/beenden Response"
      via: "postcall.training_recommendation im JSON"
      pattern: "training_recommendation"
---

<objective>
Trainings-Empfehlung UI: PostCall-Overlay Block und persistente Dashboard-Card (D-12). Komplettiert die user-facing Integration.

Purpose: User sieht nach einem schwachen Call oder Training die konkrete Empfehlung "Trainiere diesen Einwand-Typ" und kann mit einem Klick starten.
Output: PostCall-Overlay Block, Dashboard-Card, Ein-Klick-Training-Start.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-CONTEXT.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-RESEARCH.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-01-SUMMARY.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-02-SUMMARY.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-03-SUMMARY.md

<interfaces>
<!-- PostCall-Overlay Struktur in app.html (Zeile 928-1053) -->
```html
<div class="postcall-overlay" id="postcallOverlay">
  <div class="postcall-box" id="postcallBox">
    <!-- ... postcall-grid ... -->
    <!-- Zeile 1016: Lernkarten-Vorschlaege -->
    <div id="pc-lernkarten" style="display:none;margin-top:24px">
      ...
    </div>
    <!-- Zeile 1025: Lernkarte angewendet? -->
    <div id="pc-lk-applied" style="display:none;margin-top:20px">
      ...
    </div>
    <!-- Zeile 1031: Bewertung -->
    <!-- *** HIER kommt der neue Trainings-Empfehlung Block *** -->
    <!-- Zeile 1044: postcall-actions Buttons -->
  </div>
</div>
```

<!-- api_beenden() Response (app_routes.py Zeile 518) -->
```python
return jsonify({'ok': True, 'filename': filename, 'postcall': postcall, 'conv_id': saved_conv_id})
```
postcall dict enthaelt: einwaende, kaufsignale, painpoints, berater_words, kunde_words, kb_start, kb_end, etc.
*** pending_training_recommendation muss hier ergaenzt werden ***

<!-- Dashboard Template (dashboard.html) -->
```python
# routes/dashboard.py liefert Kontext-Variablen an dashboard.html via render_template()
# Bestehende Kontext-Variablen: recent_calls, stats, recommendations, etc.
```

<!-- User.pending_training_recommendation (aus Plan 01) -->
JSON: {"einwand_typ": "...", "scenario_name": "...", "scenario_id": ..., "created_at": "..."}

<!-- Dashboard Card CSS Pattern -->
```html
<div class="db2-card">
  <div class="db2-card-head"><span class="db2-card-icon">...</span> Titel</div>
  <div class="db2-card-body">...</div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostCall-Overlay Trainings-Empfehlung + Dashboard Card</name>
  <files>templates/app.html, templates/dashboard.html, routes/app_routes.py, routes/dashboard.py</files>
  <read_first>templates/app.html, templates/dashboard.html, routes/app_routes.py, routes/dashboard.py, database/models.py</read_first>
  <action>
**1. routes/app_routes.py — pending_training_recommendation in PostCall-Response einfuegen:**

In `api_beenden()`, NACH dem Engine-Hook (Plan 02) und VOR `return jsonify(...)` (Zeile ~518):

```python
    # D-12: Trainings-Empfehlung fuer PostCall-Overlay
    try:
        db_rec = get_session()
        try:
            from database.models import User as _URec
            _u_rec = db_rec.get(_URec, g.user.id)
            if _u_rec and _u_rec.pending_training_recommendation:
                import json as _jrec
                postcall['training_recommendation'] = _jrec.loads(_u_rec.pending_training_recommendation)
        finally:
            db_rec.close()
    except Exception:
        pass
```

Dieser Code liest die (moeglicherweise gerade durch die Engine gesetzte) pending_training_recommendation und fuegt sie dem postcall-Dict hinzu.

**2. templates/app.html — Trainings-Empfehlung Block im PostCall-Overlay:**

NACH dem `pc-lk-applied` Block (Zeile ~1029) und VOR dem Bewertungs-Block (Zeile ~1031), neuen Block einfuegen:

```html
      <!-- ── Phase 04.12: Trainings-Empfehlung (D-12) ────────────────────────── -->
      <div id="training-recommendation-block" style="display:none;margin-top:16px">
        <div style="border-top:1px solid #2D3748;padding-top:16px">
          <div style="color:#00D4AA;font-size:12px;font-weight:600;margin-bottom:8px;letter-spacing:0.5px">
            NERVE HAT WAS F&Uuml;R DICH
          </div>
          <div style="color:#E2E8F0;font-size:14px;margin-bottom:12px" id="training-rec-text"></div>
          <button class="n-btn n-btn-primary" onclick="startRecommendedTraining()" id="training-rec-btn"
                  style="font-size:13px;padding:8px 20px">
            Training starten &rarr;
          </button>
        </div>
      </div>
```

**3. templates/app.html — JavaScript fuer Trainings-Empfehlung:**

Im bestehenden `<script>` Block (nach Zeile ~1063), die PostCall-Rendering-Logik erweitern. Suche nach der Funktion die `postcallOverlay` anzeigt (wird nach api/beenden Response aufgerufen). Dort folgendes hinzufuegen:

```javascript
// D-12: Trainings-Empfehlung im PostCall-Overlay anzeigen
function showTrainingRecommendation(rec) {
  if (!rec || !rec.einwand_typ) return;
  var block = document.getElementById('training-recommendation-block');
  var text = document.getElementById('training-rec-text');
  if (!block || !text) return;
  var label = rec.einwand_typ.replace('einwand_', '').replace(/_/g, ' ');
  label = label.charAt(0).toUpperCase() + label.slice(1);
  text.textContent = 'Du hattest Schwierigkeiten mit "' + label + '"-Einw\u00e4nden. ' +
    'Trainiere dieses Szenario: ' + (rec.scenario_name || 'Freies Training');
  block.style.display = 'block';
  window._trainingRecScenarioId = rec.scenario_id || null;
  window._trainingRecEinwandTyp = rec.einwand_typ || null;
}

function startRecommendedTraining() {
  var url = '/training';
  if (window._trainingRecEinwandTyp) {
    url += '?quick=1&einwand_typ=' + encodeURIComponent(window._trainingRecEinwandTyp);
  }
  window.location.href = url;
}
```

Dann in der bestehenden PostCall-Display-Funktion (die nach dem api/beenden fetch aufgerufen wird), nach dem Lernkarten-Block-Code, diese Zeile einfuegen:

```javascript
if (data.postcall && data.postcall.training_recommendation) {
  showTrainingRecommendation(data.postcall.training_recommendation);
}
```

Suche nach dem Code der `postcallOverlay` oeffnet — typischerweise nach `fetch('/api/beenden', ...)` Response-Handling. Dort die `showTrainingRecommendation` Zeile einfuegen.

**4. routes/dashboard.py — pending_training_recommendation an Template weitergeben:**

In der Dashboard-Route (Funktion die `render_template('dashboard.html', ...)` aufruft), die pending_training_recommendation aus dem User lesen und als Kontext-Variable uebergeben:

```python
    # D-12: Persistente Trainings-Empfehlung
    training_recommendation = None
    try:
        if hasattr(g.user, 'pending_training_recommendation') and g.user.pending_training_recommendation:
            import json as _jtr
            training_recommendation = _jtr.loads(g.user.pending_training_recommendation)
    except Exception:
        pass
```

Dann `training_recommendation=training_recommendation` zum `render_template()` Aufruf hinzufuegen.

**5. templates/dashboard.html — Persistente Trainings-Empfehlung Card:**

An prominenter Stelle im Dashboard (z.B. ganz oben, vor den bestehenden Cards), eine bedingte Card einfuegen:

```html
{% if training_recommendation %}
<!-- Phase 04.12: Persistente Trainings-Empfehlung (D-12) -->
<div class="db2-card" id="training-rec-card" style="border-left:3px solid #00D4AA;margin-bottom:16px">
  <div class="db2-card-head">
    <span class="db2-card-icon">&#127919;</span> Empfohlenes Training
  </div>
  <div class="db2-card-body">
    <p style="color:var(--page-text-secondary);font-size:14px;margin-bottom:12px">
      {% set label = training_recommendation.einwand_typ | replace('einwand_', '') | replace('_', ' ') | capitalize %}
      Du hattest wiederholt Schwierigkeiten mit &bdquo;{{ label }}&ldquo;-Einw&auml;nden.
      {% if training_recommendation.scenario_name %}
        Empfohlenes Szenario: <strong>{{ training_recommendation.scenario_name }}</strong>
      {% endif %}
    </p>
    <a href="/training{% if training_recommendation.einwand_typ %}?quick=1&einwand_typ={{ training_recommendation.einwand_typ }}{% endif %}"
       class="n-btn n-btn-primary" style="font-size:13px;padding:8px 20px;text-decoration:none">
      Training starten &rarr;
    </a>
  </div>
</div>
{% endif %}
```

Diese Card bleibt sichtbar bis `pending_training_recommendation` auf NULL gesetzt wird (nach absolviertem Training, per Engine in Plan 02).
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
with open('templates/app.html') as f: src = f.read()
assert 'training-recommendation-block' in src, 'Training rec block missing in app.html'
assert 'showTrainingRecommendation' in src, 'JS function missing in app.html'
assert 'startRecommendedTraining' in src, 'Start function missing in app.html'
with open('templates/dashboard.html') as f: src2 = f.read()
assert 'training-rec-card' in src2, 'Training rec card missing in dashboard.html'
assert 'training_recommendation' in src2, 'Jinja variable missing in dashboard.html'
with open('routes/app_routes.py') as f: src3 = f.read()
assert 'training_recommendation' in src3, 'training_recommendation missing in app_routes.py'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
- templates/app.html enthaelt `id="training-recommendation-block"` mit display:none
- templates/app.html enthaelt `function showTrainingRecommendation(rec)`
- templates/app.html enthaelt `function startRecommendedTraining()`
- templates/app.html ruft `showTrainingRecommendation` nach PostCall-Response auf
- templates/dashboard.html enthaelt `{% if training_recommendation %}`
- templates/dashboard.html enthaelt `id="training-rec-card"` mit Teal-Akzent
- templates/dashboard.html enthaelt Link zu `/training?quick=1&einwand_typ=...`
- routes/app_routes.py liest `pending_training_recommendation` und fuegt es dem postcall-Dict hinzu
- routes/dashboard.py uebergibt `training_recommendation` an `render_template()`
  </acceptance_criteria>
  <done>PostCall-Overlay zeigt "NERVE HAT WAS FUER DICH" mit Trainings-Empfehlung und Ein-Klick-Start. Dashboard zeigt persistente Card bis Training absolviert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manuelle Verifikation des Lern-Oekosystems</name>
  <files>keine</files>
  <action>
Vollstaendiges Lern-Oekosystem (Phase 04.12) manuell verifizieren:
1. learning_events Tabelle mit 12 Event-Typen
2. Event-Logging nach jedem Call und Training
3. Muster-Erkennung: EWB mehr als 3x, Call-Schwaeche, Training-Schwaeche
4. Trainings-Empfehlung im PostCall-Overlay ("NERVE HAT WAS FUER DICH")
5. Persistente Dashboard-Card "Empfohlenes Training" mit Ein-Klick-Start
6. Lernkarten auch nach Training (nicht nur nach echtem Call)
7. Wochenbericht mit Training-Daten und Cross-Modul Insights
8. Rating-Diskrepanz wird als Event geloggt (intern)

Schritte:
1. App starten (`python app.py`)
2. Dashboard aufrufen — wenn kein pending_training_recommendation gesetzt, keine Card sichtbar (korrekt)
3. Ein Training starten und abschliessen — pruefen ob training_completed Event in learning_events Tabelle steht: `sqlite3 database/salesnerve.db "SELECT * FROM learning_events LIMIT 5"`
4. Mehrere Trainings mit schlechtem Score am gleichen Einwand-Typ absolvieren — Dashboard sollte Trainings-Empfehlung Card zeigen
5. Einen Live-Call simulieren und beenden — PostCall-Overlay pruefen ob Trainings-Empfehlung Block sichtbar ist (nur wenn Engine ein Muster erkannt hat)
6. Coach-Report abrufen — pruefen ob Training-Daten im Wochenbericht erscheinen
  </action>
  <verify>User bestaetigt Funktionalitaet per manueller Pruefung</verify>
  <done>Alle 8 Punkte des Lern-Oekosystems funktionieren korrekt im Browser und in der DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> PostCall Response | training_recommendation aus Server-seitigem User-Objekt, nicht manipulierbar |
| Dashboard -> Template | Jinja-escaped Output, kein XSS-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04.12-09 | Information Disclosure | PostCall Response | accept | training_recommendation enthaelt nur einwand_typ + scenario_name — keine sensitiven Daten |
| T-04.12-10 | Spoofing | Dashboard Card | mitigate | pending_training_recommendation nur durch Server-Engine gesetzt, nicht via Client-Request |
</threat_model>

<verification>
- PostCall-Overlay: "NERVE HAT WAS FUER DICH" Block sichtbar wenn Engine Muster erkannt hat
- Dashboard: Trainings-Empfehlung Card mit Teal-Akzent und Ein-Klick-Start
- Card verschwindet nach absolviertem Training
- Alle Events in learning_events Tabelle nachvollziehbar
</verification>

<success_criteria>
- D-12 vollstaendig: PostCall-Overlay Block + Dashboard Card + Ein-Klick-Start + Persistenz bis Training absolviert
- Kein neues CSS-Framework, NERVE Design-System verwendet (#00D4AA Teal, db2-card Pattern)
- Alle 12 D-Entscheidungen der Phase implementiert
</success_criteria>

<output>
After completion, create `.planning/phases/04.12-gesamt-integration-inserted/04.12-04-SUMMARY.md`
</output>
