---
phase: 04.12-gesamt-integration-inserted
plan: 03
type: execute
wave: 2
depends_on: ["04.12-01"]
files_modified:
  - services/coaching_service.py
  - routes/learning.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "generate_postcall_analysis() kann auch fuer Training-Sessions aufgerufen werden (D-09)"
    - "Wendepunkt-Analyse nach Training generiert Lernkarten-Vorschlag (D-09)"
    - "Wochenbericht enthaelt Training-Daten neben Live-Call-Daten (D-08)"
    - "LearningCard Events (accepted/rejected/custom/applied) werden als learning_events geloggt (D-01)"
  artifacts:
    - path: "services/coaching_service.py"
      provides: "Erweiterter Wochenbericht mit Training-Daten"
      contains: "typ == 'training'"
    - path: "routes/learning.py"
      provides: "Training-Lernkarten Endpoint + Event-Logging"
      contains: "log_learning_event"
  key_links:
    - from: "routes/learning.py"
      to: "services/coaching_service.py"
      via: "generate_postcall_analysis() Aufruf fuer Training-Sessions"
      pattern: "generate_postcall_analysis"
    - from: "routes/learning.py"
      to: "services/integration_engine.py"
      via: "log_learning_event() bei Karten-Aktionen"
      pattern: "log_learning_event"
---

<objective>
Training-basierte Lernkarten und erweiterter Wochenbericht. Verbindung 3 (Training ↔ Coach, D-09) und Verbindung 2 (Call ↔ Coach, D-07/D-08).

Purpose: Lernkarten entstehen nicht nur nach echten Calls, sondern auch nach Trainings. Wochenbericht zeigt Cross-Modul Insights ("Uebst du X im Training, aber im Call klappt es nicht").
Output: Erweiterter Wochenbericht, Training-Lernkarten, Event-Logging fuer Karten-Aktionen.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-CONTEXT.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-RESEARCH.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-01-SUMMARY.md

<interfaces>
<!-- coaching_service.py: get_or_generate_weekly_report() -->
```python
def get_or_generate_weekly_report(user_id):
    # Zeile 217-222: Filtert nur typ == 'live'
    calls = db.query(ConversationLog).filter(
        ConversationLog.user_id == user_id,
        ConversationLog.typ == 'live',
        ConversationLog.started_at >= week_start_dt,
        ConversationLog.started_at <= week_end_dt,
    ).all()
    # Zeile 250ff: report_prompt mit Wochendaten
```

<!-- coaching_service.py: generate_postcall_analysis() -->
```python
def generate_postcall_analysis(conv_id, user_id, einwaende, painpoints,
                               kb_start, kb_end, redeanteil_berater,
                               redeanteil_kunde, dauer_sek,
                               skript_abdeckung, ga_details):
    # Zeile 62-72: Duplikat-Guard per call_id
    # Generiert bis zu 3 LearningCard-Vorschlaege via Sonnet
```

<!-- routes/learning.py Endpoints -->
```python
@learning_bp.route('/api/postcall/analysis', methods=['POST'])  # Startet Analyse
@learning_bp.route('/api/learning-cards', methods=['GET'])       # Aktive Karten
@learning_bp.route('/api/learning-cards/<int:card_id>/save', methods=['POST'])     # Akzeptieren
@learning_bp.route('/api/learning-cards/<int:card_id>/reject', methods=['POST'])   # Ablehnen
@learning_bp.route('/api/learning-cards/<int:card_id>/applied', methods=['POST'])  # Angewendet
```

<!-- integration_engine.py (Plan 01) -->
```python
def log_learning_event(db_session, user_id, event_type, source_module, source_id=None, metadata=None)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LearningCard Event-Logging + Training-Lernkarten API</name>
  <files>routes/learning.py</files>
  <read_first>routes/learning.py, services/coaching_service.py, services/integration_engine.py</read_first>
  <action>
**1. Event-Logging fuer alle Karten-Aktionen (D-01):**

In jeder bestehenden LearningCard-Route ein learning_event loggen:

a) In `/api/learning-cards/<card_id>/save` (Akzeptieren):
```python
        try:
            from services.integration_engine import log_learning_event
            log_learning_event(db, g.user.id, 'learning_card_accepted', 'coach', card.call_id, {
                'card_id': card.id, 'category': card.category,
            })
            db.commit()
        except Exception as _le:
            print(f"[Engine] LearningCard Event Fehler: {_le}")
```

b) In `/api/learning-cards/<card_id>/reject` (Ablehnen):
```python
        try:
            from services.integration_engine import log_learning_event
            log_learning_event(db, g.user.id, 'learning_card_rejected', 'coach', card.call_id, {
                'card_id': card.id, 'category': card.category,
            })
            db.commit()
        except Exception as _le:
            print(f"[Engine] LearningCard Event Fehler: {_le}")
```

c) In `/api/learning-cards/<card_id>/applied` (Angewendet):
```python
        try:
            from services.integration_engine import log_learning_event
            log_learning_event(db, g.user.id, 'learning_card_applied', 'coach', card.call_id, {
                'card_id': card.id, 'category': card.category,
            })
            db.commit()
        except Exception as _le:
            print(f"[Engine] LearningCard Event Fehler: {_le}")
```

d) Wenn User eigenen Text schreibt (custom Karte, Route fuer user-text):
```python
        try:
            from services.integration_engine import log_learning_event
            log_learning_event(db, g.user.id, 'learning_card_custom', 'coach', card.call_id, {
                'card_id': card.id, 'category': card.category,
            })
            db.commit()
        except Exception as _le:
            print(f"[Engine] LearningCard Event Fehler: {_le}")
```

Jedes Event-Logging in try/except, nach dem jeweiligen db.commit() der bestehenden Logik.

**2. Training-Lernkarten Endpoint (D-09):**

Neuen Endpoint in `routes/learning.py` hinzufuegen:

```python
@learning_bp.route('/api/training/postcall-analysis', methods=['POST'])
@login_required
def api_training_postcall_analysis():
    """D-09: Lernkarten-Vorschlaege nach Training generieren.
    Gleicher Mechanismus wie Post-Call Analyse, aber mit Training-ConversationLog."""
    req_data = request.get_json(silent=True) or {}
    conv_id = req_data.get('conv_id')
    if not conv_id:
        return jsonify({'ok': False, 'error': 'conv_id fehlt'}), 400

    db = get_session()
    try:
        from database.models import ConversationLog
        conv = db.query(ConversationLog).filter_by(id=conv_id, user_id=g.user.id).first()
        if not conv or conv.typ != 'training':
            return jsonify({'ok': False, 'error': 'Training-Session nicht gefunden'}), 404

        # Wendepunkt-Saetze aus phasen_details (scoring) extrahieren
        import json
        scoring = json.loads(conv.phasen_details or '{}')
        wendepunkt_saetze = scoring.get('wendepunkt_saetze', [])
        einwaende = [{'typ': ws.get('einwand_typ', '?'), 'zitat': ws.get('text', '')} for ws in wendepunkt_saetze]
        ga_details = json.loads(conv.gegenargument_details or '[]')

        from services.coaching_service import generate_postcall_analysis
        suggestions = generate_postcall_analysis(
            conv_id=conv.id,
            user_id=g.user.id,
            einwaende=einwaende,
            painpoints=[],
            kb_start=0,
            kb_end=conv.kb_end or scoring.get('gesamt_score', 0),
            redeanteil_berater=60,
            redeanteil_kunde=40,
            dauer_sek=conv.dauer_sekunden or 0,
            skript_abdeckung=scoring.get('gesamt_score', 0),
            ga_details=ga_details,
        )
        return jsonify({'ok': True, 'suggestions': suggestions})
    except Exception as e:
        print(f"[Learning] Training PostCall Analysis Fehler: {e}")
        return jsonify({'ok': False, 'error': str(e)}), 500
    finally:
        db.close()
```

Dieser Endpoint wird vom Frontend nach Training-Ende aufgerufen (Plan 04 verdrahtet das UI).
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
with open('routes/learning.py') as f: src = f.read()
assert 'log_learning_event' in src, 'log_learning_event not in learning.py'
assert 'learning_card_accepted' in src, 'learning_card_accepted event missing'
assert 'learning_card_rejected' in src, 'learning_card_rejected event missing'
assert 'learning_card_applied' in src, 'learning_card_applied event missing'
assert 'learning_card_custom' in src, 'learning_card_custom event missing'
assert 'api_training_postcall_analysis' in src, 'training postcall analysis endpoint missing'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
- routes/learning.py enthaelt `log_learning_event` Import und Aufrufe
- routes/learning.py loggt 'learning_card_accepted' bei save
- routes/learning.py loggt 'learning_card_rejected' bei reject
- routes/learning.py loggt 'learning_card_applied' bei applied
- routes/learning.py loggt 'learning_card_custom' bei user-text
- routes/learning.py enthaelt `def api_training_postcall_analysis()` Endpoint
- Endpoint prueft `conv.user_id == g.user.id` (Ownership-Check, T-04.11-02 Pattern)
- Endpoint prueft `conv.typ == 'training'`
- Endpoint ruft `generate_postcall_analysis()` mit Training-Daten auf
  </acceptance_criteria>
  <done>Alle 4 Karten-Events werden als learning_events geloggt. Training-Lernkarten Endpoint existiert und ruft bestehende generate_postcall_analysis() auf.</done>
</task>

<task type="auto">
  <name>Task 2: Wochenbericht um Training-Daten erweitern (D-08)</name>
  <files>services/coaching_service.py</files>
  <read_first>services/coaching_service.py</read_first>
  <action>
In `services/coaching_service.py`, Funktion `get_or_generate_weekly_report()`:

**1. Training-Sessions Query hinzufuegen (nach der Live-Calls Query, Zeile ~222):**

Nach der bestehenden `calls` Query (Zeile 217-222), eine zweite Query fuer Training-Sessions einfuegen:

```python
        # D-08: Training-Sessions dieser Woche einbeziehen
        training_sessions = db.query(ConversationLog).filter(
            ConversationLog.user_id == user_id,
            ConversationLog.typ == 'training',
            ConversationLog.started_at >= week_start_dt,
            ConversationLog.started_at <= week_end_dt,
        ).all()
```

**2. Training-Aggregation berechnen (nach den Live-Aggregationen, vor dem Prompt):**

```python
        # Training-Aggregation fuer Cross-Modul Insight
        training_count = len(training_sessions)
        training_avg_score = (
            sum(t.kb_end or 0 for t in training_sessions) / training_count
            if training_count else 0
        )
        # Einwand-Typen aus Training extrahieren
        training_einwand_types = set()
        for t in training_sessions:
            if t.gegenargument_details:
                try:
                    for ga in json.loads(t.gegenargument_details):
                        et = ga.get('einwand_typ', '')
                        if et:
                            training_einwand_types.add(et)
                except Exception:
                    pass
        # Live-Call Einwand-Typen extrahieren
        call_einwand_types = set()
        for c in calls:
            if c.gegenargument_details:
                try:
                    for ga in json.loads(c.gegenargument_details):
                        et = ga.get('einwand_typ', '')
                        if et:
                            call_einwand_types.add(et)
                except Exception:
                    pass
        # Cross-Modul Insight: Einwaende die in beiden vorkommen
        cross_einwaende = training_einwand_types & call_einwand_types
```

**3. Report-Prompt erweitern (Zeile ~250ff):**

Den bestehenden `report_prompt` erweitern um Training-Kontext. NACH den bestehenden Wochendaten-Zeilen, BEVOR "Schreibe:" steht:

Neue Zeilen im Prompt einfuegen:

```python
        # Training-Kontext zum Prompt hinzufuegen
        training_context = ""
        if training_count > 0:
            training_context = f"""
Trainingsdaten:
- {training_count} Trainings-Sessions absolviert
- Durchschnittlicher Training-Score: {training_avg_score:.0f}%
- Trainierte Einwand-Typen: {', '.join(training_einwand_types) if training_einwand_types else 'keine'}
"""
            if cross_einwaende:
                training_context += f"- Cross-Modul: Einwaende die sowohl im Training als auch im echten Call vorkamen: {', '.join(cross_einwaende)}\n"
```

Diesen `training_context` String in den report_prompt einfuegen, z.B. nach der "Schwaechste Phase" Zeile und vor "Schreibe:".

Den alten Prompt-String anpassen: `{training_context}` zwischen Wochendaten und "Schreibe:" einfuegen.

**4. Caching-Key bleibt gleich (Woche) — Report wird nur 1x pro Woche generiert.**

KEIN neuer Cache-Key noetig. Wenn ein Report bereits generiert wurde (existing != None), wird er nicht neu generiert — das ist gewuenschtes Verhalten per Research Pitfall 6.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
with open('services/coaching_service.py') as f: src = f.read()
assert \"typ == 'training'\" in src, 'Training query missing in coaching_service'
assert 'training_count' in src, 'training_count variable missing'
assert 'training_avg_score' in src or 'training_sessions' in src, 'Training aggregation missing'
assert 'cross_einwaende' in src or 'Cross-Modul' in src, 'Cross-module insight missing'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
- coaching_service.py enthaelt Query `ConversationLog.typ == 'training'` in get_or_generate_weekly_report()
- coaching_service.py berechnet training_count und training_avg_score
- coaching_service.py extrahiert Einwand-Typen aus Training UND Live-Calls
- coaching_service.py berechnet cross_einwaende (Schnittmenge)
- Report-Prompt enthaelt Training-Kontext ("Trainingsdaten:", Trainings-Sessions, Training-Score)
- Report-Prompt enthaelt Cross-Modul Insight wenn cross_einwaende nicht leer
- Caching-Key bleibt unveraendert (period_start = Montag der Woche)
  </acceptance_criteria>
  <done>Wochenbericht zeigt Training-Daten (D-08): Anzahl Training-Sessions, Avg Score, trainierte Einwaende, Cross-Modul-Insight ("Einwaende die in Training UND Call vorkamen").</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Training PostCall API | conv_id aus Request, muss User-Ownership pruefen |
| Learning Card Routes → Engine | card_id verifiziert, user_id aus g.user.id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04.12-07 | Information Disclosure | api_training_postcall_analysis | mitigate | Ownership-Check: `conv.user_id == g.user.id` (via filter_by) |
| T-04.12-08 | Tampering | LearningCard Events | mitigate | card.id Lookup verifiziert Card gehoert zu g.user.id |
</threat_model>

<verification>
- python -c Tests bestanden
- Training-Lernkarten Endpoint erreichbar (POST /api/training/postcall-analysis)
- Wochenbericht-Prompt enthaelt Training-Kontext
</verification>

<success_criteria>
- Alle 4 Karten-Events geloggt (D-01: accepted, rejected, custom, applied)
- Training → Lernkarten funktioniert via bestehenden Service (D-09)
- Wochenbericht mit Training-Daten und Cross-Modul Insight (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/04.12-gesamt-integration-inserted/04.12-03-SUMMARY.md`
</output>
