---
phase: 04.12-gesamt-integration-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - app.py
  - services/integration_engine.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "LearningEvent Model existiert in database/models.py mit allen 7 Spalten per D-02"
    - "learning_events Tabelle wird bei App-Start idempotent erstellt (CREATE TABLE IF NOT EXISTS)"
    - "Index ix_learning_events_user_type existiert auf (user_id, event_type, created_at)"
    - "log_learning_event() Helper schreibt Events ohne eigenes Commit"
    - "VALID_EVENT_TYPES Whitelist enthält alle 12 Event-Typen aus D-01"
  artifacts:
    - path: "database/models.py"
      provides: "LearningEvent ORM Model"
      contains: "class LearningEvent"
    - path: "app.py"
      provides: "learning_events Migration"
      contains: "CREATE TABLE IF NOT EXISTS learning_events"
    - path: "services/integration_engine.py"
      provides: "Event-Logging Helper + Engine-Skeleton"
      exports: ["log_learning_event", "VALID_EVENT_TYPES"]
  key_links:
    - from: "services/integration_engine.py"
      to: "database/models.py"
      via: "import LearningEvent"
      pattern: "from database.models import LearningEvent"
---

<objective>
Foundation fuer das Gesamt-Integrations-Oekosystem: LearningEvent Model, DB-Migration, Event-Logging Helper und Engine-Skelett.

Purpose: Alle nachfolgenden Plans bauen auf dieser Tabelle und dem Helper auf. Ohne dieses Fundament kann kein Event geloggt werden.
Output: LearningEvent Model, idempotente Migration, integration_engine.py mit log_learning_event() und VALID_EVENT_TYPES.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-CONTEXT.md
@.planning/phases/04.12-gesamt-integration-inserted/04.12-RESEARCH.md

<interfaces>
<!-- Bestehende Model-Pattern aus database/models.py -->
From database/models.py (Zeile 553-584):
```python
class LearningCard(Base):
    __tablename__ = 'learning_cards'
    id                  = Column(Integer, primary_key=True)
    user_id             = Column(Integer, ForeignKey('users.id'), nullable=False)
    call_id             = Column(Integer, ForeignKey('conversation_logs.id'), nullable=True)
    category            = Column(String(100), nullable=False)
    # ...
    created_at          = Column(DateTime, default=utcnow)

class CoachingReport(Base):
    __tablename__ = 'coaching_reports'
    id                  = Column(Integer, primary_key=True)
    user_id             = Column(Integer, ForeignKey('users.id'), nullable=False)
    # ...
    created_at          = Column(DateTime, default=utcnow)

def init_db(engine_instance):
    """Create all tables."""
    Base.metadata.create_all(engine_instance)
```

From database/db.py:
```python
from database.db import Base, get_session
```

Bestehende _migrate() Pattern in app.py:
```python
# try/except mit conn.execute(text(...)) + conn.commit(), dann print("[DB] Migration: ...")
# except Exception: pass
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LearningEvent Model + Migration</name>
  <files>database/models.py, app.py</files>
  <read_first>database/models.py, app.py</read_first>
  <action>
1. In `database/models.py` nach dem CoachingReport Model (nach Zeile 584, vor `def init_db`):

Neuen Kommentarblock und Model hinzufuegen:

```python
# ── Phase 04.12: Gesamt-Integration — Learning Events ────────────────────────

class LearningEvent(Base):
    __tablename__ = 'learning_events'
    id            = Column(Integer, primary_key=True)
    user_id       = Column(Integer, ForeignKey('users.id'), nullable=False)
    event_type    = Column(String(50), nullable=False)
    source_module = Column(String(20), nullable=False)
    source_id     = Column(Integer, nullable=True)
    metadata      = Column(Text, nullable=True)
    created_at    = Column(DateTime, default=utcnow)
```

Spalten per D-02:
- `id` INTEGER PRIMARY KEY
- `user_id` INTEGER NOT NULL REFERENCES users(id)
- `event_type` VARCHAR(50) NOT NULL — einer der 12 Typen aus D-01
- `source_module` VARCHAR(20) NOT NULL — 'assistant'|'training'|'coach'|'rating'
- `source_id` INTEGER nullable — conv_id oder training log id
- `metadata` TEXT nullable — JSON-String
- `created_at` DATETIME DEFAULT utcnow

2. In `app.py` innerhalb der `_migrate()` Funktion, nach dem letzten Phase-04.11 Block:

```python
# ── Phase 04.12: learning_events ─────────────────────────────────────────────
try:
    conn.execute(text("""
        CREATE TABLE IF NOT EXISTS learning_events (
            id           INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id      INTEGER NOT NULL REFERENCES users(id),
            event_type   VARCHAR(50) NOT NULL,
            source_module VARCHAR(20) NOT NULL,
            source_id    INTEGER,
            metadata     TEXT,
            created_at   DATETIME DEFAULT (datetime('now'))
        )
    """))
    conn.commit()
except Exception:
    pass
try:
    conn.execute(text("""
        CREATE INDEX IF NOT EXISTS ix_learning_events_user_type
        ON learning_events(user_id, event_type, created_at)
    """))
    conn.commit()
    print("[DB] Migration: created learning_events + index")
except Exception:
    pass
```

3. In `app.py` innerhalb von `_migrate()`, nach den learning_events Migrationen, die `pending_training_recommendation` Spalte auf User hinzufuegen (fuer D-12 persistente Dashboard-Card):

```python
for col in ['pending_training_recommendation']:
    try:
        conn.execute(text(f"ALTER TABLE users ADD COLUMN {col} TEXT"))
        conn.commit()
        print(f"[DB] Migration: added users.{col}")
    except Exception:
        pass
```

4. In `database/models.py` im User Model (nach Zeile ~107, z.B. nach `weekly_goal`), die neue Spalte ergaenzen:

```python
    # Block 7: Integration Engine
    pending_training_recommendation = Column(Text, nullable=True)  # JSON: {"einwand_typ": "...", "scenario_name": "...", "created_at": "..."}
```
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "from database.models import LearningEvent; print('LearningEvent:', LearningEvent.__tablename__); assert hasattr(LearningEvent, 'event_type'); assert hasattr(LearningEvent, 'source_module'); assert hasattr(LearningEvent, 'metadata'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
- database/models.py enthaelt `class LearningEvent(Base)`
- database/models.py enthaelt `__tablename__ = 'learning_events'`
- database/models.py enthaelt alle 7 Spalten: id, user_id, event_type, source_module, source_id, metadata, created_at
- database/models.py User-Klasse enthaelt `pending_training_recommendation = Column(Text`
- app.py enthaelt `CREATE TABLE IF NOT EXISTS learning_events`
- app.py enthaelt `CREATE INDEX IF NOT EXISTS ix_learning_events_user_type`
- app.py enthaelt `ALTER TABLE users ADD COLUMN pending_training_recommendation`
  </acceptance_criteria>
  <done>LearningEvent Model in models.py, Migration in app.py _migrate(), pending_training_recommendation auf User, Import funktioniert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: integration_engine.py — Event-Logging Helper + Engine-Skeleton</name>
  <files>services/integration_engine.py</files>
  <read_first>database/models.py, services/coaching_service.py</read_first>
  <action>
Neues Modul `services/integration_engine.py` erstellen mit folgendem Inhalt:

```python
"""
Phase 04.12: Gesamt-Integration Engine
Synchrone Verarbeitungs-Engine fuer Cross-Modul Muster-Erkennung.
Laeuft in api_beenden() (Post-Call) und training/end (Post-Training).
Kein Background-Worker, kein Scheduler (D-03).
"""
import json
from datetime import datetime, timedelta

# D-01: Alle 12 Event-Typen — Whitelist fuer Input-Validierung (T-04.12 Security)
VALID_EVENT_TYPES = frozenset([
    'hint_used',
    'hint_ignored',
    'button_pressed',
    'learning_card_accepted',
    'learning_card_rejected',
    'learning_card_custom',
    'learning_card_applied',
    'training_completed',
    'training_hangup',
    'call_rated',
    'score_milestone',
    'training_recommended',   # interner Typ fuer Engine-Empfehlungen
])

VALID_SOURCE_MODULES = frozenset(['assistant', 'training', 'coach', 'rating'])


def log_learning_event(db_session, user_id, event_type, source_module,
                       source_id=None, metadata=None):
    """Schreibt einen learning_event. db_session wird vom Caller verwaltet.
    
    KEIN db_session.commit() hier — Caller committet (Pattern aus ObjectionEvent bulk-insert).
    Whitelist-Check auf event_type und source_module (Security: T-04.12 Input Validation).
    """
    if event_type not in VALID_EVENT_TYPES:
        print(f"[Engine] Ungueltiger event_type: {event_type}")
        return
    if source_module not in VALID_SOURCE_MODULES:
        print(f"[Engine] Ungueltiges source_module: {source_module}")
        return

    from database.models import LearningEvent
    ev = LearningEvent(
        user_id=user_id,
        event_type=event_type,
        source_module=source_module,
        source_id=source_id,
        metadata=json.dumps(metadata or {}, ensure_ascii=False) if metadata else None,
    )
    db_session.add(ev)


# ── Engine Entry Points (werden in Plan 02 implementiert) ────────────────────

def run_postcall_engine(db_session, user_id, conv_id, einwaende, ewb_clicks, ga_details):
    """Synchrone Engine nach Live-Call. Silent failure guaranteed. (D-03, D-04)
    Wird in Plan 02 mit Logik gefuellt."""
    pass


def run_posttraining_engine(db_session, user_id, log_id, scoring, wendepunkt_saetze, modus):
    """Synchrone Engine nach Training-Session. Silent failure guaranteed. (D-03, D-04)
    Wird in Plan 02 mit Logik gefuellt."""
    pass
```

Die Engine-Entry-Points sind Stubs die in Plan 02 implementiert werden.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "from services.integration_engine import log_learning_event, VALID_EVENT_TYPES, run_postcall_engine, run_posttraining_engine; print('VALID_EVENT_TYPES:', len(VALID_EVENT_TYPES)); assert len(VALID_EVENT_TYPES) == 12; assert 'hint_used' in VALID_EVENT_TYPES; assert 'training_completed' in VALID_EVENT_TYPES; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
- services/integration_engine.py existiert
- VALID_EVENT_TYPES enthaelt genau 12 Eintraege: hint_used, hint_ignored, button_pressed, learning_card_accepted, learning_card_rejected, learning_card_custom, learning_card_applied, training_completed, training_hangup, call_rated, score_milestone, training_recommended
- VALID_SOURCE_MODULES enthaelt: assistant, training, coach, rating
- log_learning_event() Funktion hat Parameter: db_session, user_id, event_type, source_module, source_id, metadata
- log_learning_event() macht KEINEN db_session.commit()
- log_learning_event() prueft event_type gegen VALID_EVENT_TYPES
- run_postcall_engine() und run_posttraining_engine() existieren als Stubs
  </acceptance_criteria>
  <done>integration_engine.py mit log_learning_event() Helper, VALID_EVENT_TYPES Whitelist, und Engine-Stubs erstellt. Import funktioniert fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route Handler → Engine | user_id kommt aus g.user.id (server-seitig), nie aus Request-Body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04.12-01 | Tampering | log_learning_event | mitigate | event_type Whitelist-Check in log_learning_event() — nur VALID_EVENT_TYPES akzeptiert |
| T-04.12-02 | Tampering | log_learning_event | mitigate | source_module Whitelist-Check — nur VALID_SOURCE_MODULES akzeptiert |
| T-04.12-03 | Tampering | user_id | mitigate | user_id IMMER aus g.user.id, nie aus Request-Body (enforced in Caller-Code Plan 02) |
</threat_model>

<verification>
- `python -c "from database.models import LearningEvent; print('OK')"` erfolgreich
- `python -c "from services.integration_engine import log_learning_event, VALID_EVENT_TYPES; print(len(VALID_EVENT_TYPES))"` gibt 12 aus
- App startet fehlerfrei (learning_events Tabelle wird erstellt)
</verification>

<success_criteria>
- LearningEvent Model importierbar
- Migration idempotent (CREATE TABLE IF NOT EXISTS)
- log_learning_event() mit Whitelist-Validierung
- pending_training_recommendation Spalte auf User
- Kein neues Paket, keine neuen Dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/04.12-gesamt-integration-inserted/04.12-01-SUMMARY.md`
</output>
