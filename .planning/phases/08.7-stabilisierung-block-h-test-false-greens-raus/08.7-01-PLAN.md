---
plan: 08.7-01
phase: "08.7"
title: "Block H — Test-False-Greens raus (6 Tasks)"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_qa_pipeline_t1.py
  - tests/tts_comparison.py
  - tests/test_claude_service_phase08.py
  - tests/test_08_5_05_training_pipeline_t2.py
  - tests/test_phase_08_migration.py
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "tests/test_claude_service_phase08.py is deleted — all 7 source-presence tests removed"
    - "tests/test_08_5_05_training_pipeline_t2.py retains exactly 3 tests: test_generate_response_mocked_call, test_generate_response_with_mood_mocked_call, test_public_signatures_unchanged"
    - "tests/test_phase_08_migration.py is moved to tests/archive/test_phase_08_migration.py"
    - "tests/test_qa_pipeline_t1.py is deleted — all 4 RED-gate tests removed"
    - "scripts/tts_comparison.py exists (moved from tests/tts_comparison.py)"
    - "CLAUDE.md contains a Test-Qualitaets-Regel section distinguishing Integration-Assertion from Source-Presence-False-Green"
    - "pytest exits 0 after each task commit"
  artifacts:
    - path: "tests/test_08_5_05_training_pipeline_t2.py"
      provides: "3 genuine integration tests with mocked Anthropic client"
    - path: "tests/archive/test_phase_08_migration.py"
      provides: "Archived migration tests — preserved for reference, not collected by pytest"
    - path: "scripts/tts_comparison.py"
      provides: "TTS comparison script — runnable manually, not a pytest test"
    - path: "CLAUDE.md"
      provides: "Codified rule preventing future Source-Presence-False-Greens"
  key_links:
    - from: "tests/test_claude_service_phase08.py (deleted)"
      to: "services/claude_service.py"
      via: "inspect.getsource protected analysiere_mit_claude_streaming (H-3 dead code) and _build_system_prompt (H-4 dead code) — deletion unblocks Block I prune"
      pattern: "analysiere_mit_claude_streaming|_build_system_prompt|_ACTIVE_PROMPT_CACHE"
---

<objective>
Befreie die pytest-Suite von Source-Presence-basierten False-Green-Tests (inspect.getsource,
hasattr, Datei-Lesen + String-Find), die aktiv Dead-Code vor dem Prune schuetzen.

Hintergrund: MASTER-AUDIT v2 Block H identifiziert ~24 Tests in 4 Dateien die via
inspect.getsource() pruefen ob Code *existiert*, statt ob er *korrekt laeuft*.
Diese Tests geben GREEN auch wenn der geprufte Code Dead-Code ist — sie blockieren
damit Block I (Dead-Code-Prune von H-3/H-4).

Entscheidungsregel:
  - Source-Presence OHNE Integration-Wert (inspect.getsource/hasattr/file-read+string) → LOESCHEN
  - Source-Presence MIT Integration-Hint → UMBAUEN zu Mocked-Integration-Test
  - Genuine Integration (Funktion aufrufen, Return-Wert assertieren) → BEHALTEN

Baseline VOR Start: pytest ausfuehren, Anzahl passing-Tests notieren.
Nach jedem Task: pytest ausfuehren, Delta notieren.

Output: 6 atomare Commits. pytest-Suite bleibt nach jedem Commit gruen.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\PROJECT.md
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\STATE.md
@C:\Users\andre\dev\salesnerve\.planning\audits\MASTER-AUDIT-v2.md
</context>

<tasks>

<task type="auto">
  <name>BASELINE: pytest-Run vor Beginn</name>
  <read_first>
    tests/test_claude_service_phase08.py (gelesen — 7 Tests)
    tests/test_08_5_05_training_pipeline_t2.py (gelesen — 14 Tests)
    tests/test_phase_08_migration.py (gelesen — 6 Tests)
    tests/test_qa_pipeline_t1.py (gelesen — 4 Tests)
  </read_first>
  <files></files>
  <action>
Fuehre aus dem Projektverzeichnis (C:\Users\andre\dev\salesnerve) aus:

```
cd C:\Users\andre\dev\salesnerve && python -m pytest tests/ -v --tb=no -q 2>&1 | tail -5
```

Notiere:
- Anzahl passing Tests
- Anzahl failing Tests
- Gesamtanzahl collected Tests

Diese Baseline-Zahlen ins SUMMARY.md schreiben. Kein Commit, kein Datei-Aenderung.
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=no -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - pytest-Aufruf hat eine Ausgabe mit "passed" oder "failed" Zeilen
    - Baseline-Zahlen sind notiert (fuer spaeteres Delta)
  </acceptance_criteria>
  <done>Baseline dokumentiert: X passing, Y failing, Z collected.</done>
</task>

<task type="auto">
  <name>T-01: DELETE tests/test_qa_pipeline_t1.py (4 RED-Gate-Tests)</name>
  <read_first>tests/test_qa_pipeline_t1.py (vollstaendig gelesen)</read_first>
  <files>tests/test_qa_pipeline_t1.py</files>
  <action>
Loesche die Datei tests/test_qa_pipeline_t1.py komplett.

Begruendung pro Test:
- test_import_classify_utterance: Integration-Test (ruft classify_utterance() auf) ABER als RED-Gate-TDD-Stub geschrieben — Phase 08.5 ist abgeschlossen, der Test hat keinen dauerhaften Heimatort; kein dedizierter test_qa_pipeline.py existiert der diese Tests aufnehmen sollte
- test_apply_tabu_filter_basic: Integration-Test mit Assertions auf return-Wert → hat Wert, aber der Dateiname "t1" und der Datei-Kommentar "MUST fail before the file is created" zeigen: diese Datei war ein temporaerer RED-Gate, kein dauerhafter Test
- test_match_faq_stub: erwartet None fuer einen Nicht-Match — zurueckhaltende Stub-Assertion, kein vollstaendiger Integration-Test
- test_haiku_model_id_in_file: subprocess.run(['grep', ...]) auf Quelldatei → Source-Presence → LOESCHEN

Ausfuehren:
```
cd C:\Users\andre\dev\salesnerve && del tests\test_qa_pipeline_t1.py
```
oder:
```
Remove-Item tests\test_qa_pipeline_t1.py
```

Danach: pytest ausfuehren, Delta vs. Baseline notieren.

Commit: `test(08.7): delete test_qa_pipeline_t1.py RED-gate stubs (phase 08.5 complete)`
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=no -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_qa_pipeline_t1.py` existiert NICHT mehr im Dateisystem
    - pytest exits 0 (keine neuen Failures durch diese Loeschung)
    - Delta: ca. -4 collected Tests (evtl. -3 passing wenn test_haiku_model_id_in_file auf Windows bereits failte)
  </acceptance_criteria>
  <done>test_qa_pipeline_t1.py geloescht; pytest gruen; committed.</done>
</task>

<task type="auto">
  <name>T-02: MOVE tests/tts_comparison.py → scripts/tts_comparison.py</name>
  <read_first>tests/tts_comparison.py (vollstaendig gelesen — kein pytest-Test, sondern print-basiertes Vergleichsscript)</read_first>
  <files>tests/tts_comparison.py, scripts/tts_comparison.py</files>
  <action>
Die Datei tests/tts_comparison.py ist kein pytest-Test — sie hat eine `main()`-Funktion
und einen `if __name__ == '__main__': main()` Guard, aber keine `test_*`-Funktionen.
Sie gehoert in scripts/, nicht in tests/.

Schritte:
1. Pruefe ob scripts/ Verzeichnis existiert (falls nicht, erstelle es):
   ```
   mkdir -p C:\Users\andre\dev\salesnerve\scripts
   ```
2. Verschiebe die Datei:
   ```
   Move-Item C:\Users\andre\dev\salesnerve\tests\tts_comparison.py C:\Users\andre\dev\salesnerve\scripts\tts_comparison.py
   ```
3. Verifiziere: scripts/tts_comparison.py existiert, tests/tts_comparison.py existiert NICHT mehr.

Danach: pytest ausfuehren, Delta notieren (sollte unveraendert bleiben da keine test-Funktionen entfernt).

Commit: `test(08.7): move tts_comparison.py to scripts/ (not a pytest test file)`
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=no -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tts_comparison.py` existiert
    - `tests/tts_comparison.py` existiert NICHT mehr
    - pytest Delta: 0 (keine Test-Funktionen waren in der Datei)
  </acceptance_criteria>
  <done>tts_comparison.py nach scripts/ verschoben; pytest unveraendert; committed.</done>
</task>

<task type="auto">
  <name>T-03: DELETE tests/test_claude_service_phase08.py (7 Source-Presence-Tests)</name>
  <read_first>tests/test_claude_service_phase08.py (vollstaendig gelesen)</read_first>
  <files>tests/test_claude_service_phase08.py</files>
  <action>
Loesche tests/test_claude_service_phase08.py komplett.

Alle 7 Tests verwenden inspect.getsource() oder hasattr() — Source-Presence-Pattern:

1. test_phase08_imports_present: inspect.getsource(cs) + Import-String-Assert → Source-Presence → DELETE
2. test_phase08_marker_present: inspect.getsource(cs) + Kommentar-String-Assert → Source-Presence → DELETE
3. test_analysiere_mit_claude_uses_pipeline: inspect.getsource(fn) + String-Assert → Source-Presence → DELETE
4. test_analysiere_mit_claude_streaming_uses_pipeline: inspect.getsource auf DEAD FUNCTION (H-3) → DELETE (dies ist der primaere Blocker fuer H-3 Prune)
5. test_haiku_model_preserved_in_both: inspect.getsource auf beide Funktionen incl. dead streaming → DELETE
6. test_legacy_symbols_preserved: hasattr(cs, '_build_system_prompt') etc → haelt H-4 Dead-Code am Leben → DELETE (primaerer Blocker fuer H-4 Prune)
7. test_import_smoke_no_side_effects: importiert analysiere_mit_claude_streaming (dead code) als callable → nach Prune wuerde das failen → DELETE

Ausfuehren:
```
del C:\Users\andre\dev\salesnerve\tests\test_claude_service_phase08.py
```

Danach: pytest ausfuehren, Delta notieren.

Commit: `test(08.7): delete test_claude_service_phase08.py — 7 inspect.getsource false-greens unblock H-3/H-4 prune`
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=no -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_claude_service_phase08.py` existiert NICHT mehr
    - `grep -r "test_legacy_symbols_preserved\|test_analysiere_mit_claude_streaming_uses_pipeline" tests/` gibt keinen Match
    - pytest exits 0
    - Delta: ca. -7 collected Tests
  </acceptance_criteria>
  <done>test_claude_service_phase08.py geloescht; H-3/H-4 Prune-Blockade aufgehoben; committed.</done>
</task>

<task type="auto">
  <name>T-04: REBUILD tests/test_08_5_05_training_pipeline_t2.py (11 löschen, 3 behalten)</name>
  <read_first>tests/test_08_5_05_training_pipeline_t2.py (vollstaendig gelesen — 14 Tests)</read_first>
  <files>tests/test_08_5_05_training_pipeline_t2.py</files>
  <action>
Ersetze tests/test_08_5_05_training_pipeline_t2.py mit einer bereinigten Version
die nur die 3 echten Integration-Tests behaelt (Tests 12, 13, 14 aus dem Original).

Tests 1-11 sind alle Source-Presence via inspect.getsource() → LOESCHEN:
  1. test_source_has_4_module_references — getsource + string
  2. test_source_has_4_loader_calls — getsource + count
  3. test_source_has_3_log_pipeline_event_calls — getsource + count
  4. test_constants_still_present — getsource + string
  5. test_haiku_model_still_present — getsource + string
  6. test_generate_response_uses_loader — getsource(fn) + string
  7. test_generate_response_logs_pipeline_event — getsource(fn) + string
  8. test_generate_scoring_uses_loader — getsource(fn) + string
  9. test_generate_scoring_logs_pipeline_event — getsource(fn) + string
  10. test_generate_response_with_mood_uses_loader — getsource(fn) + string
  11. test_generate_response_with_mood_logs_stimmung — getsource(fn) + string

Tests 12, 13, 14 sind genuine Integration-Tests → BEHALTEN:
  12. test_generate_response_mocked_call — monkeypatch + echter Function-Call + Return-Wert-Assert
  13. test_generate_response_with_mood_mocked_call — monkeypatch + echter Function-Call + Return-Wert-Assert
  14. test_public_signatures_unchanged — inspect.signature() prueft Runtime-API-Schnittstelle (OK)

Schreibe die Datei neu mit folgendem Inhalt (exakt so — keine weiteren Aenderungen):

```python
"""
tests/test_08_5_05_training_pipeline_t2.py
──────────────────────────────────────────────────────────────────────────────
Phase 08.5 Plan 05 — Integration-Tests fuer Training-Pipeline Mocked-Calls.

Source-Presence-Tests (Tests 1-11 der urspruenglichen Datei) entfernt in
Phase 08.7 (Block H Test-False-Greens). Die 3 Mocked-Integration-Tests
und der Signature-Test bleiben als genuine Integration-Gates.
"""
import sys
import types
import pytest

# ─── Stub heavy imports ──────────────────────────────────────────────────────

import os
os.environ.setdefault('ANTHROPIC_API_KEY', 'test-key')
os.environ.setdefault('ELEVENLABS_API_KEY', 'test-key')

_fake_anthropic = types.ModuleType('anthropic')


class _FakeAnthropic:
    def __init__(self, **kwargs):
        pass


_fake_anthropic.Anthropic = _FakeAnthropic
sys.modules.setdefault('anthropic', _fake_anthropic)
sys.modules.setdefault('requests', types.ModuleType('requests'))


# ─── Integration Tests ────────────────────────────────────────────────────────

def test_generate_response_mocked_call(monkeypatch):
    """generate_response works end-to-end with mocked Anthropic client."""
    import services.training_service as ts

    class _FakeContent:
        text = "Das ist ein Test-Antwort des simulierten Kunden."

    class _FakeResponse:
        content = [_FakeContent()]

    def _fake_create(**kwargs):
        return _FakeResponse()

    class _FakeMessages:
        create = staticmethod(_fake_create)

    class _FakeClient:
        messages = _FakeMessages()

    monkeypatch.setattr(ts, 'claude_client', _FakeClient())

    log_calls = []
    monkeypatch.setattr(ts, 'log_pipeline_event', lambda *a, **kw: log_calls.append((a, kw)))
    monkeypatch.setattr(ts, 'resolve_prompt_version', lambda module, uid: 'v1')

    conversation = [{'speaker': 'berater', 'text': 'Guten Tag'}]
    system_prompt = ts.build_sekretaerin_prompt({'chef_nachname': 'Test', 'firma': 'TestFirma', 'sek_name': 'Test Sek'})
    result = ts.generate_response(conversation, system_prompt)

    assert isinstance(result, str)
    assert len(result) > 0
    assert len(log_calls) >= 1, "log_pipeline_event was not called from generate_response"


def test_generate_response_with_mood_mocked_call(monkeypatch):
    """generate_response_with_mood works with mocked Anthropic client."""
    import services.training_service as ts

    class _FakeContent:
        text = '{"text": "Antwort.", "neue_stimmung": 1, "aufgelegt": false, "letzte_chance": false}'

    class _FakeResponse:
        content = [_FakeContent()]

    def _fake_create(**kwargs):
        return _FakeResponse()

    class _FakeMessages:
        create = staticmethod(_fake_create)

    class _FakeClient:
        messages = _FakeMessages()

    monkeypatch.setattr(ts, 'claude_client', _FakeClient())
    log_calls = []
    monkeypatch.setattr(ts, 'log_pipeline_event', lambda *a, **kw: log_calls.append((a, kw)))
    monkeypatch.setattr(ts, 'resolve_prompt_version', lambda module, uid: 'v1')

    conversation = [{'speaker': 'berater', 'text': 'Hallo'}]
    system_prompt = "Test system prompt"
    result = ts.generate_response_with_mood(conversation, system_prompt, current_mood=0)

    assert isinstance(result, dict)
    assert 'text' in result
    assert 'neue_stimmung' in result
    assert len(log_calls) >= 1, "log_pipeline_event was not called from generate_response_with_mood"


def test_public_signatures_unchanged():
    """Public function signatures of generate_response and generate_response_with_mood unchanged."""
    import inspect
    import services.training_service as ts

    sig_gr = inspect.signature(ts.generate_response)
    params_gr = list(sig_gr.parameters.keys())
    assert 'conversation_history' in params_gr
    assert 'system_prompt' in params_gr

    sig_grm = inspect.signature(ts.generate_response_with_mood)
    params_grm = list(sig_grm.parameters.keys())
    assert 'conversation_history' in params_grm
    assert 'system_prompt' in params_grm
    assert 'current_mood' in params_grm


if __name__ == '__main__':
    pytest.main([__file__, '-v'])
```

Danach: pytest ausfuehren, alle 3 behaltenen Tests muessen PASS sein.

Commit: `test(08.7): rebuild test_08_5_05_training_pipeline_t2.py — keep 3 integration tests, remove 11 source-presence`
  </action>
  <verify>
    <automated>python -m pytest tests/test_08_5_05_training_pipeline_t2.py -v --tb=short 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_08_5_05_training_pipeline_t2.py -v` zeigt genau 3 Tests: test_generate_response_mocked_call, test_generate_response_with_mood_mocked_call, test_public_signatures_unchanged
    - Alle 3 Tests PASS
    - `grep "inspect.getsource" tests/test_08_5_05_training_pipeline_t2.py` gibt keinen Match
    - gesamte pytest-Suite: 0 neue Failures
  </acceptance_criteria>
  <done>3 Integration-Tests behalten, 11 Source-Presence-Tests entfernt; alle 3 gruen; committed.</done>
</task>

<task type="auto">
  <name>T-05: ARCHIVE tests/test_phase_08_migration.py → tests/archive/</name>
  <read_first>tests/test_phase_08_migration.py (vollstaendig gelesen — 6 Tests, alle Source-Presence via _read_app())</read_first>
  <files>tests/test_phase_08_migration.py</files>
  <action>
Verschiebe tests/test_phase_08_migration.py nach tests/archive/test_phase_08_migration.py.

Begruendung: Alle 6 Tests lesen app.py als Datei-Inhalt und pruefen String-Praesenz:
  - test_block_a_backup_present: open(APP_PY).read() + 'nerve.db.bak_pre_v08_01' in src
  - test_block_b_table_rebuild_present: open(APP_PY).read() + 'CREATE TABLE objection_events_new' in src
  - test_block_c_reset_and_audit_marker_present: open(APP_PY).read() + Marker-Strings
  - test_block_d_anrede_column_present: open(APP_PY).read() + 'VARCHAR(10)'
  - test_block_e_is_default_column_and_backfill: open(APP_PY).read() + SQL-Strings
  - test_blocks_ordered_a_b_c_d_e: find() auf app.py Source-Text

Die Phase-08-Migration durchlief einmalig. Die Migration-Bloecke in app.py koennen
entfernt werden wenn Block I (Dead-Code-Prune) laeuft — dann wuerden diese Tests FAIL.
Archivieren statt Loeschen bewahrt die Test-Logik als Referenz.

pytest sammelt tests/archive/ standardmaessig NICHT ein (kein __init__.py noetig,
aber `conftest.py` oder `pytest.ini` kann archive/ ausschliessen wenn noetig).

Schritte:
1. Archive-Verzeichnis erstellen:
   ```
   mkdir C:\Users\andre\dev\salesnerve\tests\archive
   ```
2. Datei verschieben:
   ```
   Move-Item C:\Users\andre\dev\salesnerve\tests\test_phase_08_migration.py C:\Users\andre\dev\salesnerve\tests\archive\test_phase_08_migration.py
   ```
3. pytest ausfuehren und pruefen ob archive/ eingesammelt wird.
   Falls pytest die Datei einsammelt und Fehler produziert (weil APP_PY-Pfad relativ),
   fuege eine leere `tests/archive/conftest.py` ODER eine `pytest.ini`/`pyproject.toml`
   Regel hinzu die `tests/archive/` aus der Collection ausschliesst:
   
   Option A — pytest.ini (falls noch nicht vorhanden):
   ```ini
   [pytest]
   testpaths = tests
   collect_ignore_glob = tests/archive/*
   ```
   
   Option B — conftest.py in tests/archive/ (einfachster Weg):
   Erstelle `tests/archive/conftest.py` mit Inhalt:
   ```python
   collect_ignore = ['*']
   ```
   
   Pruefe zuerst ob pytest.ini oder setup.cfg oder pyproject.toml bereits existiert
   und erweitere dort. Falls nicht, erstelle tests/archive/conftest.py (Option B).

Danach: pytest ausfuehren. Delta: ca. -6 collected Tests.

Commit: `test(08.7): archive test_phase_08_migration.py — one-time migration complete, source-presence pattern`
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=no -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/archive/test_phase_08_migration.py` existiert
    - `tests/test_phase_08_migration.py` existiert NICHT mehr
    - pytest sammelt tests/archive/ NICHT ein (die 6 archivierten Tests erscheinen nicht in der Collection)
    - pytest exits 0, keine neuen Failures
    - Delta: ca. -6 collected Tests
  </acceptance_criteria>
  <done>test_phase_08_migration.py archiviert; pytest ignoriert archive/; committed.</done>
</task>

<task type="auto">
  <name>T-06: CLAUDE.md — Test-Qualitaets-Regel ergaenzen</name>
  <read_first>CLAUDE.md (GSD Workflow Enforcement Sektion lesen)</read_first>
  <files>CLAUDE.md</files>
  <action>
Ergaenze CLAUDE.md um eine Test-Qualitaets-Regel nach dem "GSD Workflow Enforcement"
Block (oder als separaten H2-Abschnitt am Ende der Datei wenn passender).

Der Regel-Text muss PRAEZISE zwischen "valid Integration-Assertion" und
"Source-Presence-False-Green" unterscheiden — keine Nudelregeln.

Fuege folgende Sektion ein (als neuen H2-Block nach "## GSD Workflow Enforcement"):

```markdown
## Test-Qualitaets-Regel: Integration-Assertion vs. Source-Presence-False-Green

Ein Test ist VALID wenn er Runtime-Verhalten prueft — Verhalten das kaputt gehen
kann ohne dass der Source-Code sich aendert:
- **DB-Write/Read:** Query auf echte oder In-Memory-DB, Assertion auf Ergebnis-Row oder Feldwert
- **Function-Call-Return:** Funktion aufrufen (ggf. mit monkeypatched I/O), Assertion auf Rueckgabewert
- **State-Mutation:** Zustandsaenderung in Dict/Objekt nach Function-Call pruefen
- **API-Response:** HTTP-Request oder Socket-Emit, Assertion auf Response-Body oder Status-Code
- **inspect.signature():** Prueft Runtime-API-Schnittstelle (Parameter-Namen) — OK

Ein Test ist ein SOURCE-PRESENCE-FALSE-GREEN wenn er nur prueft ob Code *existiert*:
- `inspect.getsource(fn)` + `assert 'string' in src` → LOESCHEN
- `hasattr(module, 'symbol')` als "Schutz vor Loeschung" → LOESCHEN
- `open('datei.py').read()` + `assert 'string' in src` → LOESCHEN
- `subprocess.run(['grep', ...])` auf Quelldateien → LOESCHEN
- `src.count('funktionsaufruf(')` → LOESCHEN

Source-Presence-Tests geben GREEN solange der Code *existiert* — auch wenn er Dead-Code
ist, nie aufgerufen wird, oder fehlerhafte Logik hat. Sie blockieren Dead-Code-Prune
und geben falsche Sicherheit.

Grenzfall: `inspect.getsource` fuer Regex-Muster die Runtime-Constraints sichern
(z.B. "kein Opus-Model im Live-Loop") sind OK NUR wenn kein Function-Call-Mock
die Constraint direkt testbar macht. Dokumentiere den Grund mit Kommentar im Test.
```

Commit: `docs(08.7): add Test-Qualitaets-Regel to CLAUDE.md — integration-assertion vs source-presence`
  </action>
  <verify>
    <automated>grep -n "Source-Presence-False-Green" C:\Users\andre\dev\salesnerve\CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Source-Presence-False-Green" CLAUDE.md` gibt mindestens 1 Match
    - `grep "inspect.getsource" CLAUDE.md` gibt mindestens 1 Match (im Regel-Text)
    - `grep "inspect.signature" CLAUDE.md` gibt mindestens 1 Match (Ausnahme-Regel)
    - Die Sektion befindet sich nach "## GSD Workflow Enforcement" oder am Ende der Datei
    - pytest unveraendert gruen (CLAUDE.md ist kein Test)
  </acceptance_criteria>
  <done>Test-Qualitaets-Regel in CLAUDE.md eingefuegt; Regel unterscheidet klar Integration-Assertion von Source-Presence; committed.</done>
</task>

<task type="auto">
  <name>FINALE: pytest-Gesamtlauf + Delta-Bericht</name>
  <read_first></read_first>
  <files></files>
  <action>
Fuehre abschliessenden pytest-Gesamtlauf aus:

```
cd C:\Users\andre\dev\salesnerve && python -m pytest tests/ -v --tb=short 2>&1
```

Dokumentiere:
- Baseline (vor Phase 08.7): X passing, Y failing, Z collected
- Nach Phase 08.7: A passing, B failing, C collected
- Delta passing: A - X
- Delta collected: C - Z (erwarteter Wert: ca. -24 bis -26 Tests weniger)
- Neue Failures: sollte 0 sein

Pruefe explizit dass die 3 behaltenen Integration-Tests ALLE gruen sind:
- test_generate_response_mocked_call PASS
- test_generate_response_with_mood_mocked_call PASS
- test_public_signatures_unchanged PASS

Falls neue Failures aufgetaucht sind: root cause analysieren und fixen vor SUMMARY.md.
  </action>
  <verify>
    <automated>python -m pytest tests/ --tb=short -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 0 neue Failures gegenueber Baseline
    - 3 behaltene Integration-Tests PASS
    - Gesamtzahl collected Tests reduziert um ~24-26
    - pytest exits 0
  </acceptance_criteria>
  <done>Finaler pytest-Run: X passing, 0 neue Failures, Delta -N collected. Block-I-Prune kann beginnen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/ → services/ | Test-Imports von services.* — Stub-Mechanismus muss robust bleiben |
| tests/archive/ → pytest collection | archive/ darf nicht in Collection landen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.7-01 | Tampering | Dead-Code nach Prune | accept | Block H loescht Test-Schutz; Block I pruent Dead-Code; Reihenfolge H-vor-I ist Pflicht |
| T-08.7-02 | Denial of Service | pytest collection von tests/archive/ | mitigate | T-05 schliesst archive/ aus collection aus via conftest.py oder pytest.ini |
| T-08.7-03 | Repudiation | Kein Nachweis was geloescht wurde | accept | SUMMARY.md dokumentiert Baseline/Delta; git-History zeigt welche Tests entfernt wurden |
</threat_model>

<verification>
Run nach allen 6 Tasks:

```bash
# 1. Geloeschte Dateien sind weg
test ! -f tests/test_claude_service_phase08.py && echo "OK: phase08 deleted"
test ! -f tests/test_qa_pipeline_t1.py && echo "OK: t1 deleted"
test ! -f tests/tts_comparison.py && echo "OK: tts moved"

# 2. Archivierte Datei ist am richtigen Ort
test -f tests/archive/test_phase_08_migration.py && echo "OK: migration archived"

# 3. Rebuilt-Datei hat genau 3 Test-Funktionen
grep -c "^def test_" tests/test_08_5_05_training_pipeline_t2.py

# 4. Keine inspect.getsource mehr in der rebuilt-Datei
grep "inspect.getsource" tests/test_08_5_05_training_pipeline_t2.py || echo "OK: no getsource"

# 5. CLAUDE.md-Regel vorhanden
grep "Source-Presence-False-Green" CLAUDE.md

# 6. Gesamter pytest gruen
python -m pytest tests/ --tb=short -q 2>&1 | tail -5
```

Erwartung: Kommandos 1-5 alle OK. pytest exits 0.
</verification>

<success_criteria>
- tests/test_claude_service_phase08.py GELOESCHT (7 inspect.getsource/hasattr Tests weg → H-3/H-4 Prune unblocked)
- tests/test_08_5_05_training_pipeline_t2.py enthaelt genau 3 Funktionen: test_generate_response_mocked_call, test_generate_response_with_mood_mocked_call, test_public_signatures_unchanged — alle PASS
- tests/archive/test_phase_08_migration.py ARCHIVIERT (tests/ sammelt sie nicht ein)
- tests/test_qa_pipeline_t1.py GELOESCHT (4 RED-Gate-Stubs weg)
- scripts/tts_comparison.py existiert (aus tests/ verschoben)
- CLAUDE.md enthaelt Test-Qualitaets-Regel mit "Source-Presence-False-Green" als Keyword
- pytest exits 0 nach allen 6 Commits
- Delta: ca. -24 bis -26 collected Tests, 0 neue Failures
- Block I (Dead-Code-Prune H-3/H-4) kann jetzt gestartet werden
</success_criteria>

<output>
Nach allen 6 Tasks erstelle:
.planning/phases/08.7-stabilisierung-block-h-test-false-greens-raus/08.7-01-SUMMARY.md

Enthaelt:
- baseline_tests: X passing / Z collected (vor Phase 08.7)
- final_tests: A passing / C collected (nach Phase 08.7)
- delta_collected: Z - C (erwarteter Wert ~24-26 weniger)
- files_deleted: test_claude_service_phase08.py, test_qa_pipeline_t1.py
- files_archived: test_phase_08_migration.py → tests/archive/
- files_rebuilt: test_08_5_05_training_pipeline_t2.py (14 → 3 Tests)
- files_moved: tests/tts_comparison.py → scripts/tts_comparison.py
- new_failures: 0
- block_i_ready: true (H-3 analysiere_mit_claude_streaming und H-4 _build_system_prompt prune-bar)
</output>
