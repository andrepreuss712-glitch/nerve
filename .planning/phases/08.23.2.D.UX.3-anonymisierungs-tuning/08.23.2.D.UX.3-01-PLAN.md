---
phase: 08.23.2.D.UX.3-anonymisierungs-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/anonymization.py
  - services/deepgram_service.py
autonomous: false   # Task 4 = checkpoint:human-verify (Production-only Goldstandard-Re-Test)
requirements: []    # Keine REQ-IDs in ROADMAP fuer diese Phase — Tasks referenzieren ROADMAP-Task-Nummern (R1..R6)
user_setup: []

must_haves:
  truths:
    - "Pronomen (Ich/mich/Sie/ihr/wir/uns/...) erscheinen unveraendert im anonymisierten Text — nie als [PERSON_X]"
    - "Ganze Woerter bleiben intakt: 'ausfuehrliche' und 'wirklich' werden NICHT mitten im Wort ersetzt (kein Wortteil-Bug)"
    - "GLiNER-Fehlalarme wie 'nach dem Anruf'->[LOC_A] verschwinden bei Threshold 0.55"
    - "Generische Berufs-/Org-Nomen (Berater/Manager/Firmen/Unternehmen) werden nicht als ORG tokenisiert"
    - "Echte Namen ([PERSON_A]) und der Firmenname ([ORG_A]) bleiben weiterhin geschwaerzt — KEIN echter Name rutscht bei Threshold 0.55 ungeschwaerzt durch (DSGVO Art. 32)"
    - "Firmenname aus Profil basis.unternehmen wird vorab in den Token-Cache registriert"
    - "Whitelist-Filterlogik ist in EINEM Helper _is_whitelisted zentralisiert, an allen 3 NER-Stellen identisch aufgerufen (keine duplizierte Inline-Logik)"
  artifacts:
    - path: "services/anonymization.py"
      provides: "PRONOMEN_WHITELIST + GENERIC_BERUF_WHITELIST Modul-Konstanten, GLINER_THRESHOLD ENV-Konstante (Default 0.55), Pflicht-Helper _is_whitelisted, wortgrenzen-gebundener anonymize_output"
      contains: "_is_whitelisted"
    - path: "services/deepgram_service.py"
      provides: "basis.unternehmen-Registrierung in den Anonymizer-Cache nach init_anonymisierer (defensiver Dict-Zugriff)"
      contains: "unternehmen"
  key_links:
    - from: "services/anonymization.py::_apply_ner / _apply_ner_parallel / extract_entities"
      to: "_is_whitelisted (zentraler Helper)"
      via: "continue-Filter vor get_or_assign_token / append"
      pattern: "_is_whitelisted"
    - from: "services/anonymization.py::anonymize_output"
      to: "Wortgrenzen-Replace"
      via: "re.sub mit Lookaround statt str.replace"
      pattern: "re\\.sub"
    - from: "services/deepgram_service.py:639"
      to: "cache.get_or_assign_token(firma, 'ORG')"
      via: "get_anonymisierer(_sid) nach init_anonymisierer"
      pattern: "get_or_assign_token"
---

<objective>
Den DSGVO-Anonymizer (`services/anonymization.py`, GLiNER + spaCy + Token-Cache-Substitution) reparieren und entschaerfen, ohne echte PII durchzulassen. Vier Wirk-Hebel (RESEARCH-korrigiert gegenueber ROADMAP):

1. **Pronomen-Whitelist (Root-Cause)** — `'ich'/'Sie'/...` werden nie getaggt, landen nie im Cache → behebt gleichzeitig die Ueber-Schwaerzung UND den Wortteil-Bug (kein `'ich'`→`[PERSON_C]`-Mapping mehr, das Substrings in `ausfuehrliche`/`wirklich` zerreisst).
2. **Generic-Berufs-/Org-Nomen-Whitelist** — generische Nomen werden nicht als ORG tokenisiert.
3. **GLiNER-Threshold 0.5→0.55** als ENV-Konstante (3 Stellen) — Low-Confidence-Fehlalarme (`nach dem Anruf`→LOC) raus, echte Namen (score typ. >0.85) bleiben. **Code-Default bewusst 0.55, NICHT 0.6** (Cross-AI Gemini HIGH: 0.6 riskiert False Negatives bei Namen mit score ~0.55 → DSGVO-Leck). ENV-Override `GLINER_THRESHOLD` erlaubt spaetere, validierte Anhebung auf 0.6 ohne Redeploy.
4. **`anonymize_output` wortgrenzen-haerten (Defense-in-depth)** — falls je wieder ein kurzer Token in den Cache geraet, kann er keine Substrings mehr zerreissen.

Plus Nebendarsteller: Firmenname aus Profil `basis.unternehmen` vorab registrieren (loest die NERVE→`[ORG_A]`-Ueberlappung) und Doppel-Klammer-Token-Check.

**KRITISCHE RESEARCH-KORREKTUR (hat Vorrang vor ROADMAP-Task-1):** Der Wortteil-Bug lebt NICHT in `_apply_ner` (der ersetzt bereits korrekt span-/offset-basiert rueckwaerts — siehe anonymization.py:289-301). Er lebt im OUTPUT-Pfad `anonymize_output()` (anonymization.py:485-488): nacktes `text.replace(original, token)`. `_apply_ner` wird NICHT angefasst (Aenderung dort waere schaedlich).

**Cross-AI Gemini Review (08.23.2.D.UX.3-REVIEWS.md) eingearbeitet — 5 Findings:**
- HIGH: Threshold-Default 0.55 statt 0.6 (PII-False-Negative-Risiko, Art. 32). 0.6 wird zur bewussten ENV-Folge-Entscheidung NACH Korpus-Validierung. Task 4 prueft gegen 5-10 historische Prod-Calls.
- MEDIUM: `_is_whitelisted`-Helper VERPFLICHTEND (statt Empfehlung) — keine Filterlogik-Duplikation ueber die 3 NER-Funktionen.
- MEDIUM: Firmenname-Exact-Match als dokumentierte Known-Limitation (KEIN Fuzzy-Matching-Scope-Creep in dieser Phase).
- LOW: defensiver Dict-Zugriff `.get('unternehmen') or ''` (faengt `unternehmen: None`).
- LOW: Whitelist-Case-Sensitivity als Known-Limitation dokumentiert.

Purpose: Trainings-Daten-Qualitaet fuer Phase E (DPO-Paar-Sammler) + lesbare, korrekt anonymisierte Transkripte sichern.
Output: Geaenderte `services/anonymization.py` (3 Modul-Konstanten, 1 Pflicht-Helper, gehaerteter Output-Replace, Whitelist-Filter an 3 NER-Stellen) + 1 Wiring-Zeile in `services/deepgram_service.py`. KEINE Schema-Aenderung, KEIN DB-Write.

**Komplexitaet:** 🟡 mittel (Cross-AI gelaufen — Gemini, 5 Findings actionable). Backend-only, im Kern ein File.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.3-anonymisierungs-tuning/08.23.2.D.UX.3-RESEARCH.md
@.planning/phases/08.23.2.D.UX.3-anonymisierungs-tuning/08.23.2.D.UX.3-REVIEWS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Schluessel-Vertraege aus dem Codebase. Executor nutzt diese direkt — keine Codebase-Exploration noetig. -->

services/anonymization.py — AnrufAnonymisierer (anon.py:101-129):
```python
class AnrufAnonymisierer:
    self.mapping: dict       # 'Mueller' -> '[PERSON_A]'
    self._lock: threading.Lock
    def get_or_assign_token(self, entity_text: str, entity_type: str) -> str
```

services/anonymization.py — anonymize_output (anon.py:466-488), HEUTIGER BUG-CODE:
```python
def anonymize_output(text: str, cache: Optional[AnrufAnonymisierer]) -> str:
    if not text or not cache:
        return text if text else ''
    with cache._lock:
        items = sorted(cache.mapping.items(), key=lambda x: len(x[0]), reverse=True)
    for original, token in items:
        if original in text:
            text = text.replace(original, token)   # <-- Substring-Replace = Wortteil-Bug
    return text
```

services/anonymization.py — _apply_ner (anon.py:229-303), KORREKT, NICHT AENDERN:
```python
# Span-Replace bereits korrekt, rueckwaerts (anon.py:294-301):
for start, end, token_type, original in reversed(unique_spans):
    token = cache.get_or_assign_token(original, token_type) if cache else f'[{token_type}_X]'
    result = result[:start] + token + result[end:]
```
GLiNER-Treffer werden gesammelt in `all_spans.append((start, end, token_type, ent_text))` (anon.py:279/283).
spaCy-Treffer in anon.py:252: `all_spans.append((ent.start_char, ent.end_char, token_type, ent.text))`.

services/live_session.py — Cache-Zugriff (live_session.py:277-296):
```python
def init_anonymisierer(sid: str) -> None      # erstellt Cache in _session_state[sid]['anonymisierer']
def get_anonymisierer(sid: str)               # gibt AnrufAnonymisierer oder None (Ghost-SID-safe)
```

services/deepgram_service.py — Wiring-Stelle (deepgram_service.py:639-640), `_profile_daten2`-Struktur {'basis': {'unternehmen': ...}}:
```python
ls.init_anonymisierer(_sid)   # Zeile 639
ls.set_profile_for_sid(_sid, _profile_name2, _profile_daten2)   # Zeile 640
```

services/profile_schema.py:52 — `unternehmen: str = ''` in BasisSchema (VERIFIED grep, gelesen in claude_service.py:159, deepgram_service.py:359 = 3 aktive Production-Leser).
</interfaces>
</context>

<persistence_verification>
## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21/22)

**Angefasste DB-Tabellen:** KEINE. Diese Phase aendert ausschliesslich RAM-Text-Transformation und Modul-Konstanten. Kein lesender oder schreibender DB-Zugriff, keine Migration, keine neue Spalte/Tabelle.

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|----------------------|--------------------|--------------------|--------------|
| `cache.mapping` (`'ich'→[PERSON_C]`) | geschrieben `get_or_assign_token` (anon.py:127); gelesen `anonymize_output` (anon.py:484) | **RAM-only** — `_session_state[sid]['anonymisierer']` (live_session.py:296), NIE DB (BfDI-Kommentar anon.py:104) | ✓ Code-Beleg RESEARCH §Persistenz-Karte |
| `PRONOMEN_WHITELIST` / `GENERIC_BERUF_WHITELIST` / `GLINER_THRESHOLD` | neue Modul-Konstanten | **Code-Literal / ENV** (kein DB, kein Persist) | ✓ RESEARCH §244 |
| `basis.unternehmen` | gelesen aus `_profile_daten2`-Dict (RAM, aus `Profile.daten` JSONB geladen) | **RAM-Dict** im Live-Pfad; Quelle DB `Profile.daten` (nur gelesen, nie geschrieben) | ✓ profile_schema.py:52, deepgram_service.py:624 |

**Begruendung warum trivial:** Der Anonymizer schreibt NICHTS in die DB (RESEARCH §37 — Persistenz passiert beim Caller via `ls.conversation_log.append` / `ls.painpoints.append`). Der Token-Cache lebt bewusst nur im RAM und wird nie persistiert. `basis.unternehmen` wird nur gelesen. → Punkt-21-Verifikation: keine Diskrepanz moeglich, kein STOP-Risiko.
</persistence_verification>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Whitelists + GLiNER-Threshold-Konstante (Default 0.55) + Pflicht-Helper _is_whitelisted — Pronomen/Berufe nie taggen, Threshold an 3 Stellen (R2/R3/R4)</name>
  <files>services/anonymization.py</files>

  <read_first>
    - services/anonymization.py:83-97 (Regex-Pattern-Block — direkt danach Modul-Konstanten + Helper einfuegen)
    - services/anonymization.py:241-283 (_apply_ner: spaCy-Loop + GLiNER-Loop, hier Helper-Filter einsetzen)
    - services/anonymization.py:306-372 (_apply_ner_parallel: run_spacy + run_gliner, hier spiegeln)
    - services/anonymization.py:533-588 (extract_entities: spaCy + GLiNER, hier spiegeln — speist gatekeeper.classify_contact)
    - services/anonymization.py:261, 331, 567 (drei threshold=0.5 Vorkommen)
    - RESEARCH.md Task 2 (Zeile 122-144), Task 3 (146-153), Task 4 (155-168), Pitfall 2 (253-254)
    - REVIEWS.md Concern 1 (Threshold 0.55), Suggestion "Helper-Funktion erzwingen"
  </read_first>

  <action>
**Schritt A — Drei Modul-Konstanten direkt nach den Regex-Patterns (~nach anon.py:96, vor `# ── AnrufAnonymisierer-Klasse`):**

```python
# ── GLiNER-Konfidenz-Schwelle (Phase 08.23.2.D.UX.3 Task R3) ──────────────────
# 0.5 (GLiNER-Default) liess Low-Confidence-Fehlalarme durch ('nach dem Anruf'->LOC).
# Code-Default bewusst 0.55 (NICHT 0.6): Cross-AI Gemini HIGH — 0.6 riskiert
# False Negatives bei echten Namen mit score ~0.55 (DSGVO Art. 32, PII-Leck).
# 0.55 = milder Precision-Schwenk; echte Namen haben score typ. >0.85, bleiben getaggt.
# ENV-Override erlaubt Live-Tuning ohne Redeploy (Production-only-Verify, Task R6/Task 4).
# Eine Anhebung auf 0.6 ist eine BEWUSSTE Folge-Entscheidung NACH Korpus-Validierung
# (Task 4: 5-10 historische Prod-Calls), NICHT im Code festgeschrieben.
GLINER_THRESHOLD = float(os.environ.get('GLINER_THRESHOLD', '0.55'))

# ── Pronomen-Whitelist (Phase 08.23.2.D.UX.3 Task R2) ─────────────────────────
# NIE anonymisieren — sonst landen 'ich'/'Sie' im Cache und zerreissen via
# Substring-Replace ganze Woerter (Wortteil-Bug). Lowercase-Vergleich.
# DATEN-Strings -> echte Umlaute erlaubt (CLAUDE.md UTF-8-Regel, Identifier ASCII).
# Bewusst KONSERVATIV: nur eindeutige Funktionswoerter (kein echter Kontakt heisst so).
# Known-Limitation (Gemini LOW): .lower()-Vergleich koennte theoretisch 'Wir GmbH'
# (falls GLiNER nur 'Wir' taggt) ueberspringen — bei B2B-Cold-Calls akzeptabel.
PRONOMEN_WHITELIST = frozenset({
    'ich', 'mich', 'mir', 'mein', 'meine', 'meiner', 'meinem', 'meinen', 'meins',
    'du', 'dich', 'dir', 'dein', 'deine', 'deiner', 'deinem', 'deinen',
    'sie', 'ihr', 'ihre', 'ihrer', 'ihrem', 'ihren', 'ihres',
    'er', 'es', 'ihm', 'ihn', 'seiner', 'sein', 'seine',
    'wir', 'uns', 'unser', 'unsere', 'unserer', 'unserem', 'unseren',
    'man', 'wer', 'wen', 'wem',
})

# ── Generic-Berufs-/Org-Nomen-Whitelist (Phase 08.23.2.D.UX.3 Task R4) ────────
# Generische Berufs- UND Org-Nomen werden nie als ORG tokenisiert.
# Real-Daten-Befund: sichtbarer ORG-Laerm kommt v.a. von generischen Nomen
# ('Firmen'/'Unternehmen'), nicht Berufen -> beide Listen (Claude's Discretion,
# sinnvoller Default = beides, da billig; RESEARCH Open Question 1 + A3).
GENERIC_BERUF_WHITELIST = frozenset({
    # Berufe
    'vertriebler', 'berater', 'beraterin', 'manager', 'managerin',
    'verkäufer', 'verkäuferin', 'geschäftsführer', 'geschäftsführerin',
    'mitarbeiter', 'mitarbeiterin', 'kollege', 'kollegin', 'chef', 'chefin',
    'kunde', 'kundin', 'ansprechpartner', 'ansprechpartnerin',
    'entscheider', 'entscheiderin', 'leiter', 'leiterin',
    # Generische Org-Nomen
    'firma', 'firmen', 'unternehmen', 'betrieb', 'betriebe', 'gesellschaft',
})
```

**Schritt B — Pflicht-Helper `_is_whitelisted` (Gemini MEDIUM: KEINE Duplikation).** Direkt nach den Konstanten (vor der Klasse) den EINEN zentralen Helper definieren. Die Filterlogik liegt NUR hier — die 3 NER-Funktionen rufen ihn auf, schreiben die `if ... in PRONOMEN_WHITELIST`-Logik NICHT inline aus:

```python
def _is_whitelisted(ent_text: str, token_type: str) -> bool:
    """Zentrale Whitelist-Filterlogik (Phase 08.23.2.D.UX.3, Gemini-Review MEDIUM).
    EINZIGE Quelle der Wahrheit — an allen 3 NER-Stellen aufgerufen, nie inline
    dupliziert (verhindert Gatekeeper-Inkonsistenz). Pronomen typ-unabhaengig,
    Generic-Nomen nur fuer ORG.
    """
    low = ent_text.strip().lower()
    if low in PRONOMEN_WHITELIST:
        return True
    if token_type == 'ORG' and low in GENERIC_BERUF_WHITELIST:
        return True
    return False
```

**Schritt C — Helper an ALLEN 3 NER-Stellen aufrufen (identisch, keine Inline-Duplikation).**

In `_apply_ner`:
- spaCy-Loop (anon.py:241-252): direkt nach der `token_type`-Bestimmung, vor `all_spans.append`:
  ```python
  if _is_whitelisted(ent.text, token_type):
      continue
  ```
- GLiNER-Loop (anon.py:263-283): nach der `token_type`-Bestimmung (vor `if 'start' in ent ...`):
  ```python
  if _is_whitelisted(ent_text, token_type):
      continue
  ```

In `_apply_ner_parallel`:
- `run_spacy` (anon.py:313-320): die List-Comprehension in eine Schleife umschreiben, die pro Entity `_is_whitelisted(e.text, token_type)` prueft und bei True `continue`.
- `run_gliner` (anon.py:333-350): nach `token_type`-Bestimmung `if _is_whitelisted(ent_text, token_type): continue`.

In `extract_entities` (speist gatekeeper.classify_contact, Pitfall 2):
- spaCy-Loop (anon.py:552-558): vor `entities.append(...)` → `if _is_whitelisted(ent.text, token_type): continue` (token_type vorher bestimmen).
- GLiNER-Loop (anon.py:570-584): vor `entities.append(...)` → `if _is_whitelisted(ent.get('text', ''), token_type): continue`.

**Schritt D — `threshold=0.5` durch `threshold=GLINER_THRESHOLD` ersetzen an allen drei Stellen:** anon.py:261, anon.py:331, anon.py:567.

**UTF-8 (CLAUDE.md):** Listen-Strings mit echten Umlauten (`verkäufer`, `geschäftsführer`), Identifier ASCII (`PRONOMEN_WHITELIST`, `_is_whitelisted`). `.lower()` faengt Gross/Klein (`'Sie'`==`'sie'`).
  </action>

  <acceptance_criteria>
    - `grep -n "PRONOMEN_WHITELIST = frozenset" services/anonymization.py` → 1 Treffer
    - `grep -n "GENERIC_BERUF_WHITELIST = frozenset" services/anonymization.py` → 1 Treffer
    - `grep -n "GLINER_THRESHOLD = float(os.environ.get" services/anonymization.py` → 1 Treffer
    - **Default-Wert 0.55, NICHT 0.6:** `grep -n "os.environ.get('GLINER_THRESHOLD', '0.55')" services/anonymization.py` → 1 Treffer (Gemini HIGH)
    - **KEIN hartes 0.6 als Threshold-Default im Code:** `grep -n "GLINER_THRESHOLD', '0.6'" services/anonymization.py` → 0 Treffer
    - **ENV-Override behalten (live-tunbar):** `grep -c "os.environ.get('GLINER_THRESHOLD'" services/anonymization.py` → 1
    - `grep -c "threshold=GLINER_THRESHOLD" services/anonymization.py` → exakt 3 (Count-Wert lesen, nicht exit-code)
    - `grep -c "threshold=0.5" services/anonymization.py` → 0 (kein hartes 0.5 mehr in predict_entities-Calls)
    - **Pflicht-Helper existiert + wird 3× aufgerufen (Gemini MEDIUM, keine Duplikation):** `grep -c "_is_whitelisted" services/anonymization.py` → ≥4 (1 def + ≥3 calls)
    - **Keine duplizierte Inline-Filterlogik:** `grep -c "in PRONOMEN_WHITELIST" services/anonymization.py` → genau 1 (NUR im Helper, nicht an den NER-Stellen)
    - `_apply_ner` ist im Span-Replace-Block (anon.py:294-301) UNVERAENDERT (Diff zeigt keine Aenderung an `result = result[:start] + token + result[end:]`)
  </acceptance_criteria>

  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "import ast; ast.parse(open('services/anonymization.py', encoding='utf-8').read()); print('SYNTAX_OK')"</automated>
    <note>Reine lokale Syntax-/Grep-Pruefung erlaubt (kein Import, kein GLiNER-Lauf — HART Production-only). Funktionale NER-Verifikation NUR via Task 4 (Deploy + Prod-Log-Diff). Kein lokales pytest gegen Live-NER (CLAUDE.md HART).</note>
  </verify>

  <done>
3 Modul-Konstanten existieren (PRONOMEN_WHITELIST, GENERIC_BERUF_WHITELIST, GLINER_THRESHOLD mit ENV-Override-Default **0.55**, NICHT 0.6). Pflicht-Helper `_is_whitelisted` existiert genau einmal (def) und wird an allen 3 NER-Stellen (_apply_ner, _apply_ner_parallel, extract_entities) aufgerufen — keine Inline-Duplikation der Filterlogik (`in PRONOMEN_WHITELIST` nur 1× im Helper). Alle 3 predict_entities-Calls nutzen GLINER_THRESHOLD. _apply_ner Span-Replace unveraendert. Datei parst (ast.parse OK), UTF-8-konform.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: anonymize_output wortgrenzen-haerten — Defense-in-depth gegen Substring-Replace + Doppel-Klammer-Check (R1/R5B)</name>
  <files>services/anonymization.py</files>

  <read_first>
    - services/anonymization.py:466-488 (anonymize_output — der zu haertende Output-Pfad)
    - RESEARCH.md Task 1 (98-120: Fix-Code mit Lookaround), Pitfall 3 (256-257: \b an Umlaut-Grenzen), Task 5B (170-186: Doppel-Klammer)
    - services/anonymization.py:6 (`import re` bereits vorhanden — KEIN neuer Import noetig)
  </read_first>

  <action>
**Schritt A — Substring-Replace durch wortgrenzen-gebundenen Replace ersetzen (anon.py:485-488).** `import re` ist bereits modul-level vorhanden (anon.py:6). Lookarounds `(?<!\w)...(?!\w)` statt `\b` (robuster an Umlaut-Grenzen, Pitfall 3). Len-absteigende Sortierung (anon.py:484) BLEIBT (schuetzt `Dr. Mueller` vor `Mueller`).

Ersetze die Schleife (anon.py:485-488):
```python
    for original, token in items:
        if original in text:
            text = text.replace(original, token)
    return text
```
durch:
```python
    for original, token in items:
        if original not in text:
            continue
        # Wortgrenzen-Replace (Phase 08.23.2.D.UX.3 Task R1): Defense-in-depth
        # gegen Substring-Replace mitten im Wort ('ich'->[PERSON_C] in 'wirklich').
        # Lookarounds statt \b — robuster an Umlaut-Grenzen (Pitfall 3).
        # Mehrteilige Keys ('Dr. Mueller') bleiben an den aeusseren Grenzen korrekt.
        pattern = r'(?<!\w)' + re.escape(original) + r'(?!\w)'
        text = re.sub(pattern, lambda _m, _t=token: _t, text, flags=re.UNICODE)
    return text
```
**Wichtig:** `lambda _m, _t=token: _t` (Default-Arg bindet `token` korrekt pro Iteration — verhindert Late-Binding-Closure-Bug) UND umgeht re.sub-Backreference-Interpretation in `token` (eckige Klammern `[PERSON_A]` sind in Replacement-Strings sonst harmlos, aber die Callable-Form ist sicherer als String-Replacement).

**Schritt B — Doppel-Klammer-Check (R5B, defensive Haertung, [ASSUMED]).** Der Roadmap-Befund `[PERSON_B]B]` ist in der heutigen Stichprobe NICHT reproduziert (RESEARCH §88, A2) — Task 1+Task-2A reduzieren die Wahrscheinlichkeit bereits drastisch (kein `'ich'`-Cache, kein Substring-Treffer). KEINE grosse eigenstaendige Code-Aenderung. Defensiv: einen Guard ergaenzen, der Mapping-Keys ueberspringt, die selbst schon wie ein Token aussehen (verhindert Re-Tokenisierung bereits-anonymisierten Texts). Direkt nach der `items = sorted(...)`-Zeile, vor der Schleife:
```python
    # R5B (defensiv): Keys ueberspringen die selbst wie ein fertiger Token aussehen
    # ([PERSON_A] etc.) — verhindert Re-Tokenisierung/Doppel-Klammer bei Re-Run.
    _token_like = re.compile(r'^\[[A-Z_]+_[A-Z]+\]$')
    items = [(o, t) for (o, t) in items if not _token_like.match(o)]
```
Den Doppel-Klammer-Endbeleg traegt Task 4 (Goldstandard-Regression-Check), nicht ein lokaler Test.

**Edge-Case dokumentieren (Kommentar im Code):** Keys die auf `.`/Ziffer enden (z.B. `Commerzbank AG`, IBAN-Tokens mit Leerzeichen) — `(?<!\w)...(?!\w)` sitzt korrekt an den aeusseren Wortgrenzen; `re.escape` neutralisiert Sonderzeichen im Key.
  </action>

  <acceptance_criteria>
    - `grep -c "text.replace(original, token)" services/anonymization.py` → 0 (nacktes Substring-Replace entfernt)
    - `grep -n "re.sub(pattern" services/anonymization.py` → ≥1 Treffer in anonymize_output
    - `grep -n "(?<!\\\\w)" services/anonymization.py` → ≥1 (Lookaround-Pattern vorhanden)
    - `grep -n "re.escape(original)" services/anonymization.py` → ≥1
    - `grep -n "_token_like" services/anonymization.py` → ≥1 (Doppel-Klammer-Guard)
    - len-absteigende Sortierung erhalten: `grep -c "key=lambda x: len(x\[0\]), reverse=True" services/anonymization.py` → 1
    - Regex-Filter-Pfade in _apply_regex_filter (IBAN/EMAIL/TEL, anon.py:176/185/193 etc.) UNVERAENDERT (Diff zeigt keine Aenderung dort — die nutzen weiterhin str.replace auf strukturierten Volltreffern, kein Wortteil-Risiko)
  </acceptance_criteria>

  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "import ast; ast.parse(open('services/anonymization.py', encoding='utf-8').read()); print('SYNTAX_OK')"</automated>
    <note>Lokal nur Syntax + Grep. Funktionaler Wortteil-Bug-Beweis (ausfuehrliche/wirklich intakt) = Task 4 Prod-Log-Diff. HART: kein lokaler Lauf.</note>
  </verify>

  <done>
anonymize_output nutzt wortgrenzen-gebundenen re.sub mit Lookarounds (?<!\w)...(?!\w) + re.escape statt nacktem str.replace. Len-absteigende Sortierung erhalten. Doppel-Klammer-Guard (_token_like) ueberspringt token-foermige Keys. _apply_regex_filter unveraendert. Datei parst.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Firmenname aus Profil basis.unternehmen vorab registrieren — defensiver Dict-Zugriff (R5A)</name>
  <files>services/deepgram_service.py</files>

  <read_first>
    - services/deepgram_service.py:620-654 (Profil-Load + init_anonymisierer + set_profile_for_sid — Wiring-Stelle, _profile_daten2 verfuegbar)
    - services/anonymization.py:112-129 (get_or_assign_token — registriert in cache.mapping)
    - services/live_session.py:277-296 (init_anonymisierer / get_anonymisierer — Ghost-SID-safe, get gibt None bei unbekanntem SID)
    - services/profile_schema.py:52 (`unternehmen: str = ''` in BasisSchema)
    - RESEARCH.md Task 5A (170-184: Injektions-Punkt + defensives .get)
    - REVIEWS.md Concern 4 (defensiver Dict-Zugriff), Concern 2 (Firmenname-Exact-Match = Known-Limitation)
  </read_first>

  <action>
**Wiring direkt nach `ls.init_anonymisierer(_sid)` (deepgram_service.py:639), vor oder nach `set_profile_for_sid` (Reihenfolge unkritisch — beide Ghost-SID-safe).** `get_anonymisierer(_sid)` gibt None bei Ghost-SID → defensiv pruefen. `_profile_daten2` ist an dieser Stelle bereits geladen (deepgram_service.py:624) als Dict `{'basis': {'unternehmen': ...}}`.

Einfuegen nach Zeile 640 (`ls.set_profile_for_sid(...)`):
```python
            # Phase 08.23.2.D.UX.3 Task R5: Firmenname aus Profil vorab in Token-Cache
            # registrieren (loest NERVE->[ORG_A]-Ueberlappung). basis.unternehmen ist
            # ein bestehendes Feld (profile_schema.py:52), kein neues Profilfeld.
            # RAM-only: get_or_assign_token schreibt in cache.mapping, nie DB.
            # Defensiver Dict-Zugriff (Gemini LOW): '.get('unternehmen') or ''' faengt
            # zusaetzlich den Fall ab dass der Key existiert aber Wert explizit None ist
            # (sonst .strip()-AttributeError); '.get('basis') or {}' faengt fehlende Section.
            _anon_cache = ls.get_anonymisierer(_sid)
            if _anon_cache and _profile_daten2:
                _firma = ((_profile_daten2.get('basis') or {}).get('unternehmen') or '')
                if isinstance(_firma, str) and _firma.strip():
                    _anon_cache.get_or_assign_token(_firma.strip(), 'ORG')
                    print(f"[ANON] Firmenname aus Profil registriert ({len(_firma.strip())} Zeichen)")
```

**Known-Limitation (Gemini MEDIUM, BEWUSST nicht behoben in dieser Phase):** Der Match ist Exact-String aus `basis.unternehmen`. STT transkribiert den Firmennamen oft abgewandelt ('Nerve GmbH' vs Profil 'NERVE') → GLiNER taggt die Abweichung dann als neues `[ORG_X]` statt des Profil-Tokens. **KEIN Fuzzy-/phonetisches Matching in dieser Phase** — das waere Scope-Creep fuer einen Nebendarsteller-Task (Firmenname loest laut RESEARCH ohnehin nur 1 Wort, und GLiNER schwaerzt die Variante immer noch als irgendein ORG-Token, also kein PII-Leck — nur eine zweite ORG-Nummer). Die `_firma.strip()`-Normalisierung (trim) ist trivial-OK; ein case-insensitiver/normalisierter Vergleich waere ebenfalls vertretbar falls trivial, aber NICHT verpflichtend. Dokumentiert als bekannte Grenze; Fuzzy-Matching ggf. eigene Folge-Phase.

**Defensiv (RESEARCH A5):** `.get('basis') or {}` faengt fehlende `basis`-Section. `(... ).get('unternehmen') or ''` faengt `unternehmen: None`. `isinstance(_firma, str)` faengt falsch-typisierte JSON-Werte. `get_anonymisierer`-None-Check faengt Ghost-SID (Cache noch nicht/nicht mehr da). Kein PII-Klartext im Log (nur Laenge).

**Variablennamen gegen Live-Datei verifizieren (RESEARCH A5):** `_profile_daten2`, `_sid`, `ls` sind an dieser Stelle (deepgram_service.py:624/639/640) im Scope — beim Edit bestaetigen, dass die Namen exakt so heissen (read_first deckt das ab).
  </action>

  <acceptance_criteria>
    - `grep -n "get('basis')" services/deepgram_service.py` → ≥1 Treffer im neuen Block
    - **defensiver Dict-Zugriff (Gemini LOW): `.get('unternehmen') or ''` statt `.get('unternehmen', '')`:** `grep -n "get('unternehmen') or ''" services/deepgram_service.py` → 1 Treffer
    - **KEIN `.get('unternehmen', '')`-Default-Pattern (faengt None nicht ab):** `grep -c "get('unternehmen', '')" services/deepgram_service.py` → 0
    - `grep -n "get_or_assign_token(_firma" services/deepgram_service.py` → 1 Treffer
    - `grep -n "ls.get_anonymisierer(_sid)" services/deepgram_service.py` → ≥1 (None-Guard)
    - Der Block steht NACH `ls.init_anonymisierer(_sid)` (Zeile 639) — manueller Diff-Check
    - Kein PII-Klartext im print (nur `len(...)` geloggt): `grep -n "Firmenname.*{_firma}" services/deepgram_service.py` → 0
  </acceptance_criteria>

  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "import ast; ast.parse(open('services/deepgram_service.py', encoding='utf-8').read()); print('SYNTAX_OK')"</automated>
    <note>Lokal nur Syntax + Grep. Funktionaler Beleg (NERVE bleibt [ORG_A], keine Ueber-/Unter-Schwaerzung) = Task 4. HART: kein lokaler Server-Start.</note>
  </verify>

  <done>
Nach init_anonymisierer wird basis.unternehmen (falls vorhanden, non-empty, str) via get_or_assign_token als ORG im Cache registriert. Ghost-SID-None-Guard + defensives `.get('basis') or {}` + `.get('unternehmen') or ''` (faengt None). Kein PII im Log. Firmenname-Exact-Match-Limitation als Code-Kommentar dokumentiert (KEIN Fuzzy-Matching). Datei parst.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Goldstandard-Re-Test auf Production (R6) — Deploy + Korpus-Validierung gegen 5-10 historische Calls + Log-Diff</name>
  <what-built>
Tasks 1-3 haben `services/anonymization.py` (3 Whitelists/Konstanten, Pflicht-Helper _is_whitelisted, gehaerteter Output-Replace, Doppel-Klammer-Guard, Threshold-Default 0.55) und `services/deepgram_service.py` (Firmenname-Registrierung, defensiver Dict-Zugriff) geaendert. KEINE Schema-Aenderung, KEIN DB-Write. Jetzt der einzige gueltige funktionale Beweis (HART Production-only, CLAUDE.md): Deploy + Korpus-Validierung gegen mehrere historische Prod-Calls mit echten Personennamen (Gemini HIGH — ein einzelner Test-Call validiert die DSGVO-Konformitaet des neuen Thresholds statistisch NICHT).
  </what-built>

  <how-to-verify>
1. **Deploy:** `bash deploy.sh production` (tar-over-ssh, kein git pull — Prod ist tar-deployed mit .git excluded; Pytest-Gate test_ft_seed pre-existing rot → Py-Deploy landet trotzdem; bei echtem Test-Fehler kein Restart). **Achtung (CLAUDE.md):** vor Overwrite kurz pruefen ob auf Prod ein SCP-Hotfix liegt der nicht in origin/main ist.

2. **Frischer Cold-Call (Funktions-Smoke):** Test-User (`andre-test@nerve.local`, is_test_user=TRUE — vergiftet Trainings-Korpus nicht) faehrt 1x Cold-Call mit dem IDENTISCHEN NERVE-Sales-Skript von heute (reproduzierbar — 3x heute gelaufen). Profil mit `basis.unternehmen` = "NERVE" verwenden.

3. **KORPUS-VALIDIERUNG (Gemini HIGH — DSGVO-Gate gegen False Negatives bei 0.55):** Gegen einen **groesseren Korpus von 5-10 historischen Prod-Calls mit echten, verschiedenen Personennamen** pruefen, dass bei Threshold 0.55 KEIN echter Name ungeschwaerzt durchrutscht. Quellen:
   - Vorhandene Goldstandard-Transkripte aus RESEARCH (heutige Calls 2026-06-05).
   - Weitere historische Prod-Logs ziehen: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 500'` (oder mehrere `nerve_log_*.txt`-Dateien) — Calls mit echten Namen identifizieren, anonymisierten Output gegen Klartext-Erwartung pruefen.
   - Falls weniger als 5 echte-Namen-Calls in den Logs verfuegbar sind: mehrere Test-Calls mit unterschiedlichen, bewusst niedrig-konfidenten Namen (kurze Namen, Vornamen-only, ungewoehnliche Schreibweisen) fahren, um den 0.55-Grenzbereich zu testen.

4. **Log-Pull + Diff:** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cat /opt/nerve/app/logs/<neuestes nerve_log_*.txt>'` (oder `cd /opt/nerve/app && bash scripts/inspect.sh logs`). Manueller Soll-Ist-Diff gegen die RESEARCH-Erwartungstabelle (RESEARCH.md Zeile 191-199):

   | Im Transkript | Heute (falsch) | SOLL nach Fix |
   |---------------|----------------|---------------|
   | `ausführliche Gespräche` | `ausführl[PERSON_C]e` | `ausführliche` ✅ |
   | `was wirklich passiert` | `wirkl[PERSON_C]` | `wirklich` ✅ |
   | `Ich rufe an` | `[PERSON_B] rufe an` | `Ich rufe an` ✅ |
   | `Sie haben grad wenig Zeit` | `[PERSON_D] haben grad` | `Sie haben grad` ✅ |
   | `nach dem Anruf` | `nach [LOC_A]` | `nach dem Anruf` ✅ |
   | echter Personenname (ALLE im Korpus!) | `[PERSON_A]` | `[PERSON_A]` BLEIBT ✅ (KEIN echter Name durchgelassen!) |
   | Firmenname NERVE | `[ORG_A]` | `[ORG_A]` BLEIBT ✅ |
   | Doppel-Klammer `[...][...]` | (frueher gesehen) | NICHT vorhanden ✅ |

5. **Acceptance (alle muessen zutreffen):** Pronomen unversehrt, ganze Woerter intakt (kein Wortteil-Bug), `nach dem Anruf` nicht mehr LOC, generische Nomen (Firmen/Unternehmen) nicht mehr ORG — UND zugleich: **JEDER echte Personenname im 5-10-Call-Korpus weiterhin geschwaerzt** (DSGVO-Kern-Gate bei 0.55), Firmenname weiterhin `[ORG_A]`, kein `[…][…]`-Doppel.
   **DSGVO-kritisch (Gemini HIGH):** Wenn bei 0.55 ein echter Name durchrutscht → 0.55 ist zu hoch, via ENV `GLINER_THRESHOLD` auf 0.5 senken und Korpus erneut pruefen (NICHT mergen mit PII-Leck). Erst wenn 0.55 ueber den Korpus sauber ist (kein Name durchgelassen), ist eine spaetere Anhebung auf 0.6 via ENV eine bewusste, separat zu validierende Folge-Entscheidung — NICHT in dieser Phase festschreiben.
  </how-to-verify>

  <resume-signal>Tippe "approved" wenn der Prod-Log-Diff alle Erwartungszeilen erfuellt UND der 5-10-Call-Korpus bei Threshold 0.55 KEINEN echten Namen durchgelassen hat (DSGVO-Gate). Sonst beschreibe welche Zeile/welcher Call abweicht (z.B. "echter Name X in Call Y durchgelassen" → Threshold via ENV auf 0.5 + Re-Test, oder "Pronomen Z noch geschwaerzt" → Whitelist erweitern).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STT/Claude-Text → Anonymizer | Roher PII-haltiger Text (Namen, Firmen, Orte) kreuzt hier in die Anonymisierungs-Pipeline. Nach dem Anonymizer gilt Text als DSGVO-konform fuer Persist/Training. |
| Profil-Daten (`basis.unternehmen`) → Token-Cache | Vom User eingegebener Firmenname kreuzt in den RAM-Cache (kein Vertrauens-Upgrade, nur Vorab-Registrierung). |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UX3-01 | Information Disclosure | `GLINER_THRESHOLD`-Erhoehung von 0.5 (anon.py:261/331/567) | **mitigate** | Code-Default bewusst **0.55** (NICHT 0.6 — Cross-AI Gemini HIGH: 0.6 riskiert False Negatives bei score~0.55). Echte Namen haben score >0.85, bleiben getaggt. **Pflicht-Korpus-Gate Task 4: Validierung gegen 5-10 historische Prod-Calls mit echten Namen** — ein einzelner Call validiert DSGVO-Konformitaet statistisch nicht. Falls echter Name durchgelassen → ENV-Override `GLINER_THRESHOLD` sofort auf 0.5 runter, KEIN Merge mit PII-Leck. Anhebung auf 0.6 nur als separat-validierte ENV-Folge-Entscheidung NACH sauberem Korpus, nie im Code. (RESEARCH Pitfall 4, REVIEWS Concern 1) |
| T-UX3-02 | Information Disclosure | `PRONOMEN_WHITELIST` / `GENERIC_BERUF_WHITELIST` via Pflicht-Helper `_is_whitelisted` | **mitigate** | Listen bewusst konservativ — nur eindeutige Funktions-/Gattungswoerter. Kein echter NERVE-Kontakt heisst wie ein Pronomen (RESEARCH A4). Zentralisiert im EINEN Helper (Gemini MEDIUM) → keine Filter-Inkonsistenz zwischen den 3 NER-Pfaden (inkl. Gatekeeper). Known-Limitation (Gemini LOW): `.lower()`-Vergleich koennte 'Wir GmbH' ueberspringen — bei B2B akzeptabel, dokumentiert. |
| T-UX3-03 | Information Disclosure | `anonymize_output` wortgrenzen-Replace | **mitigate** | Aenderung erhoeht Praezision (kein Substring-Replace), senkt NIE Schwaerzungs-Coverage: len-absteigende Sortierung bleibt, Lookarounds matchen weiterhin den vollen Key. Defense-in-depth, keine neue Leak-Flaeche. |
| T-UX3-04 | Tampering | `basis.unternehmen` aus User-Profil → Cache | **accept** | User registriert eigenen Firmennamen zur Schwaerzung — kein Angriffsvektor (User schwaerzt eigene Daten staerker, nicht schwaecher). Defensiv: `.get('basis') or {}` + `.get('unternehmen') or ''` (faengt None, Gemini LOW) + isinstance-str-Check + .strip() + Ghost-SID-Guard verhindern Crash bei Fehl-Input. Known-Limitation: Exact-Match (kein Fuzzy) — STT-Varianten werden als zweites ORG-Token geschwaerzt, kein PII-Leck. |
| T-UX3-05 | Denial of Service | `re.sub` mit user-naher `re.escape(original)` | **accept** | `re.escape` neutralisiert ReDoS-Metazeichen; Lookarounds `(?<!\w)...(?!\w)` sind konstant, kein katastrophales Backtracking. Cache-Keys sind kurze Entity-Strings, kein adversarialer Input. |
| T-UX3-06 | Information Disclosure | PII-Klartext in Logs (Firmenname-Registrierung) | **mitigate** | print loggt nur `len(...)`, nie den Firmennamen-Klartext (acceptance-grep prueft das explizit). |

**Persistenz/Schema-Sicherheit:** Kein DB-Write, keine Migration, keine RLS-Aenderung — Angriffsflaeche auf Persistenz-Ebene = 0 (siehe Persistenz-Schicht-Verifikation).
</threat_model>

<verification>
**Production-only (CLAUDE.md HART):** Lokal nur `python -c "import ast; ast.parse(...)"` (Syntax) + Grep-Acceptance je Task. KEIN lokaler Import/GLiNER-Lauf/pytest/Server.

Funktionale Gesamtverifikation = Task 4 (checkpoint):
- Deploy `bash deploy.sh production`
- Frischer Test-User Cold-Call mit identischem NERVE-Skript
- **Korpus-Validierung gegen 5-10 historische Prod-Calls mit echten Namen (Gemini HIGH)** — Threshold-0.55-DSGVO-Gate
- Prod-Log-Diff (`inspect.sh logs` / SSH cat) gegen RESEARCH-Erwartungstabelle (8 Zeilen)
- DSGVO-Gate: KEIN echter Name durchgelassen (sonst Threshold via ENV auf 0.5 runter, kein Merge)

nyquist_validation = false → KEINE VALIDATION.md / Dimension-8-Anforderung. Real-Daten-Validation (CLAUDE.md Punkt 13) ist via Task 4 (Korpus) erfuellt.
</verification>

<success_criteria>
- [ ] 3 Modul-Konstanten in anonymization.py (PRONOMEN_WHITELIST, GENERIC_BERUF_WHITELIST, GLINER_THRESHOLD mit ENV-Default **0.55**, KEIN hartes 0.6 im Code)
- [ ] Pflicht-Helper `_is_whitelisted` existiert genau 1× (def) + 3× aufgerufen, keine Inline-Filter-Duplikation (`in PRONOMEN_WHITELIST` nur 1×)
- [ ] Whitelist-Filter via Helper an allen 3 NER-Stellen (_apply_ner, _apply_ner_parallel, extract_entities) — Pitfall 2 abgedeckt
- [ ] threshold=GLINER_THRESHOLD an 3 Stellen, kein hartes 0.5 mehr
- [ ] anonymize_output wortgrenzen-gebunden (re.sub + Lookaround + re.escape), len-Sort erhalten, Doppel-Klammer-Guard
- [ ] _apply_ner Span-Replace + _apply_regex_filter UNVERAENDERT
- [ ] basis.unternehmen-Registrierung nach init_anonymisierer (deepgram_service.py), Ghost-SID-Guard, defensiver `.get('unternehmen') or ''`, kein PII im Log, Exact-Match als Known-Limitation dokumentiert (kein Fuzzy)
- [ ] Beide Dateien parsen (ast.parse)
- [ ] Task 4: Prod-Log-Diff erfuellt alle Erwartungszeilen + **5-10-Call-Korpus bei 0.55 ohne durchgelassenen echten Namen** (DSGVO)
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.D.UX.3-anonymisierungs-tuning/08.23.2.D.UX.3-01-SUMMARY.md`
</output>
</content>
</invoke>
