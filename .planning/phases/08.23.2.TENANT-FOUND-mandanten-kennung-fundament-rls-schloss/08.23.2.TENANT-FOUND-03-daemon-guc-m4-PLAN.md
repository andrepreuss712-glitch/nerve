---
phase: 08.23.2.TENANT-FOUND
plan: 03
type: execute
wave: 3
complexity: "🔴"
depends_on: ["08.23.2.TENANT-FOUND-02"]
files_modified:
  - services/slow_lane.py
  - tests/test_abstain_log_rls.py
  - tests/test_slow_lane_tenant_guc.py
autonomous: true
requirements: [TF-3]
user_setup: []

must_haves:
  truths:
    - "Der Slow-Lane-Daemon setzt set_current_tenant(str(tenant)) BEVOR die TX beginnt, in der der abstain_log-Write committet (Variante A)"
    - "Ohne gesetzten GUC weist FORCE-RLS WITH CHECK den abstain_log-INSERT fail-closed ab (M-4 Negativ-Test raised)"
    - "Mit gesetztem GUC geht der abstain_log-INSERT durch + ist lesbar; der GUC-lose Negativ-INSERT schrieb nachweislich nichts (M-4 Positiv-Test)"
    - "Der Daemon ruft clear_current_tenant() im finally nach jedem Item (Hygiene, Thread-Reuse)"
    - "Der Slow-Lane-Abstain-Pfad schreibt mit gesetztem GUC + geseedetem calls.tenant_id eine abstain_log-Row (Integration)"
  artifacts:
    - path: "services/slow_lane.py"
      provides: "set_current_tenant-Klammer im Consumer-Loop vor dem Schreib-TX (M-4 Variante A) + clear im finally"
      contains: "set_current_tenant"
    - path: "tests/test_abstain_log_rls.py"
      provides: "M-4 Negativ+Positiv-Test für abstain_log (Muster test_rubric_score_schema.py:195-269)"
      contains: "def test_abstain_log_rls_requires_tenant_guc"
    - path: "tests/test_slow_lane_tenant_guc.py"
      provides: "Slow-Lane-Integration: Abstain-Pfad schreibt abstain_log MIT GUC (seeded calls.tenant_id)"
      contains: "abstain_log"
  key_links:
    - from: "services/slow_lane.py slow_lane_consumer"
      to: "_persist_event_ref abstain_log-Write"
      via: "set_current_tenant VOR get_session()/Schreib-TX, clear im finally"
      pattern: "set_current_tenant\\(.*tenant"
    - from: "tests/test_abstain_log_rls.py"
      to: "FORCE RLS WITH CHECK (Plan 02)"
      via: "clear_current_tenant → INSERT raised; set_current_tenant → INSERT ok"
      pattern: "test_abstain_log_rls_requires_tenant_guc"
---

<objective>
Schließt die M-4-Daemon-GUC-Falle. Der Slow-Lane-Daemon (`services/slow_lane.py`) schreibt `abstain_log` (Z.246) OHNE je `set_current_tenant` zu rufen — im Daemon-Thread ist der `_current_tenant_id`-contextvar leer (kein before_request). Solange `abstain_log` keine RLS hat, geht das. Sobald Plan 02 FORCE RLS forciert, schlägt der INSERT fail-closed fehl (WITH CHECK gegen leeren GUC) → rollback → Event bleibt 'pending' → H-3 re-queued endlos → abstain_log nie geschrieben, LAUTLOS. Das ist M-4.

Fix (RESEARCH §4, Variante A — empfohlen): Tenant zuerst aus der DB ermitteln (`_tenant_id_for`, calls hat keine RLS → GUC-frei lesbar), `set_current_tenant(str(tid))` setzen, DANN die Schreib-TX öffnen — der `after_begin`-Hook (db.py:73-89) liest den contextvar bei TX-Begin. Plus `clear_current_tenant()` im finally (Thread-Reuse-Hygiene, analog app.py:2134-2138).

Plus die Test-Schicht (Wave 0 Gaps, VALIDATION): M-4-Negativ/Positiv-Test für abstain_log (Muster `test_rubric_score_schema.py:195-269` verbatim) + Slow-Lane-Integration-Test (Abstain-Pfad schreibt mit GUC).

Locked decisions:
- Variante A (RESEARCH §4): set VOR der Schreib-TX, nicht Mid-TX-Reset. Sauberer, weil calls/intent_event keine RLS haben (Lookup GUC-frei).
- clear_current_tenant() im finally pro Item (Hygiene).
- M-4-Test pro neu-ge-RLS-ter Tabelle: NUR abstain_log (rubric_score hat den Test schon: test_rubric_score_schema.py:195; suggestion_reactions braucht keinen Daemon-Test, RESEARCH §7).

Purpose: Ohne diesen Fix ist Plan 02 ein lautloser Daten-Verlust (abstain_log-Writes verschwinden). Setzt Plan 02 voraus (RLS muss aktiv sein, damit der Test fail-closed beweisen kann).
Output: slow_lane.py-GUC-Klammer + M-4-Test (abstain_log) + Slow-Lane-Integration-Test.

Latenz-Budget (Punkt 25): 0 ms Live-Bahn. Der Daemon-GUC-Set läuft in der Slow Lane (async, post-call, off dem kritischen Pfad — slow_lane.py Doc-String). Per Definition nie auf Fast/Medium Lane.

INTERLOCK (Punkt 24) — TAXO2-Plan-04: Plan-04 (rubric_score-Write im Daemon) erbt EXAKT diese set_current_tenant-Klammer (gleicher Daemon, gleiche M-4-Falle). Pre-Execute-Interlock: Plan-04 darf KEINE zweite/abweichende GUC-Mechanik bauen — er nutzt die hier etablierte Klammer (kein Doppel-Bau, kein Drift). Notiz unten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

PRE-EXECUTE (durch Executor/André):
- Plan 02 MUSS deployt/aktiv sein (abstain_log existiert MIT FORCE RLS), sonst beweist der M-4-Negativ-Test nichts (kein fail-closed ohne RLS). W3 hängt zwingend an W2.
- INTERLOCK TAXO2-Plan-04: die set_current_tenant-Klammer hier ist der EINE Daemon-GUC-Vertrag. Plan-04 nebeneinander legen → greppen, dass es dieselbe Klammer wiederverwendet (kein eigener GUC-Pfad). Divergenz = STOP.
- HART: kein Local-Dev, Verify server-seitig über deploy.sh-Gate / SSH.
- Multi-Segment-Gotcha: Pfade hardcoden.
</execution_context>

<context>
@.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-RESEARCH.md
@.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-VALIDATION.md

<interfaces>
<!-- Verträge aus dem Codebase, direkt nutzbar. -->

GUC-Mechanik (database/db.py:54-89):
```python
def set_current_tenant(tid):   # setzt contextvar _current_tenant_id
def clear_current_tenant():    # leert contextvar (Hygiene; SET LOCAL auto-clears bei commit/rollback)
# after_begin-Hook (Z.73-89): liest contextvar BEI TX-BEGIN -> SELECT set_config('app.tenant_id', %s, true)
#   tid leer -> KEIN SET -> current_setting(...) NULL -> RLS fail-closed (0 rows)
```
→ Der SET feuert bei TX-Begin (vor den Queries). set_current_tenant MUSS VOR der committenden TX gerufen werden.

Consumer-Loop (services/slow_lane.py:362-379, der M-4-Einbau-Ort):
```python
while True:
    item = slow_lane.get(timeout=SLOW_LANE_TICK)   # Z.364
    if item is SENTINEL: flush_to_db(); break
    db = get_session()                              # Z.371 — hier beginnt die Schreib-TX
    try:
        _persist_event_ref(item, db)                # Z.373 — abstain_log-Write drin
        db.commit()                                 # Z.374 — PER-ITEM-COMMIT
    except Exception as e:
        db.rollback()                               # Z.376 — Zeile bleibt 'pending'
    finally:
        db.close()                                  # Z.379
```

abstain_log-Write (services/slow_lane.py:244-252, in _persist_event_ref):
```python
ev.handling_status = 'abstained'
db.add(AbstainLog(
    event_id=ev.event_id, interaction_id=ev.interaction_id,
    next_advisor_sentence=next_utt, intent_type=ev.intent_type,
    tenant_id=_tenant_id_for(ev, db),    # Z.251 — liest calls.tenant_id (DB, jetzt gesetzt dank Plan 01)
))
```

_tenant_id_for (services/slow_lane.py:161-169): liest call.tenant_id aus DB (calls hat KEINE RLS → GUC-frei lesbar). None wenn nicht ermittelbar.

M-4-Test-Muster (tests/test_rubric_score_schema.py:195-269, VERBATIM für abstain_log replizieren):
- (a) NEGATIV: clear_current_tenant(); db_session.rollback() → frische TX mit leerem GUC; SELECT current_setting('app.tenant_id', true) MUSS leer sein (Anti-False-Green); INSERT mit tenant_id → erwarte raise; rollback.
- (b) POSITIV: set_current_tenant(str(tenant)); db_session.rollback() → INSERT geht durch + lesbar; UND: Negativ-Row unter korrektem GUC abwesend (echte Absenz, nicht RLS-Read-Filter).
- finally: set_current_tenant(str(tenant)); db_session.rollback(); cleanup_rows (Test-Cleanup-Regel). Skip ohne TEST_DATABASE_URL (SQLite hat kein RLS).

conftest: TEST_TENANT_UUID (Z.36-57), cleanup_rows (Z.238), db_session-Fixture (Z.669, skip ohne TEST_DATABASE_URL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: set_current_tenant-Klammer im Slow-Lane-Consumer (M-4 Variante A) + clear im finally</name>
  <files>services/slow_lane.py</files>
  <read_first>
    - services/slow_lane.py:161-169 (_tenant_id_for — GUC-freier Lookup, calls hat keine RLS)
    - services/slow_lane.py:199-252 (_persist_event_ref — abstain_log-Write Z.246-252)
    - services/slow_lane.py:337-379 (slow_lane_consumer — der Einbau-Ort, Per-Item-Commit Z.371-379)
    - database/db.py:54-89 (set_current_tenant/clear_current_tenant + after_begin TX-Timing)
    - RESEARCH.md §4 (M-4-Mechanik komplett, Variante A empfohlen, TX-Timing-Falle, clear im finally)
    - app.py:2134-2138 (clear_current_tenant-Hygiene-Muster im Request-Teardown, Referenz)
  </read_first>
  <action>
    M-4-Fix, Variante A (RESEARCH §4): den Tenant ermitteln + GUC setzen BEVOR die committende Schreib-TX beginnt, dann clear im finally. Da der Tenant pro Item variiert (verschiedene Calls in der Queue), wird der Wert pro Item gesetzt.

    Im Consumer-Loop (`slow_lane_consumer`, services/slow_lane.py um Z.371) — der GUC MUSS vor der Schreib-TX gesetzt sein, deren INSERT committet. Empfohlene Struktur (Variante A):
    ```python
    from database.db import set_current_tenant, clear_current_tenant
    ...
    db = get_session()
    try:
        # M-4 (TENANT-FOUND Plan 03): den Tenant des Items VOR der committenden TX setzen,
        # sonst weist FORCE-RLS WITH CHECK den abstain_log-INSERT fail-closed ab (Daemon-Thread
        # ohne Request -> GUC leer -> INSERT lautlos verworfen -> Event ewig 'pending').
        # calls/intent_event haben KEINE RLS -> _tenant_id_for liest GUC-frei. Variante A:
        # erst auflösen, GUC setzen, dann committen -> after_begin liest den contextvar bei
        # der naechsten/committenden TX (db.py:73-89).
        _tid = _tenant_id_for_item(item, db)   # Helper unten ODER _tenant_id_for nach ev-Load
        if _tid is not None:
            set_current_tenant(str(_tid))
        _persist_event_ref(item, db)
        db.commit()
    except Exception as e:
        db.rollback()
        print(f"[SLOW] consumer error: {e}")
    finally:
        clear_current_tenant()   # Thread-Reuse-Hygiene (analog app.py:2134-2138)
        db.close()
    ```
    WICHTIG (TX-Timing, RESEARCH §4): Falls der Tenant-Lookup auf `db` schon eine TX öffnet, bevor der GUC gesetzt ist (after_begin feuert dann mit leerem contextvar), die committende abstain_log-TX MUSS NACH set_current_tenant beginnen. Zwei saubere Wege (Executor wählt, beide getestet durch den Integration-Test):
    - (A1) Den Tenant über eine SEPARATE kurze Read-Session ermitteln (calls keine RLS), DANN set_current_tenant, DANN `db = get_session()` für den Write öffnen. Sauberster Variante-A-Schnitt.
    - (A2) Falls auf derselben `db`: nach set_current_tenant ein `db.rollback()` (beendet die GUC-lose Lookup-TX), sodass _persist_event_ref/commit in einer frischen TX mit gesetztem GUC läuft (exakt das getestete test_rubric_score_schema.py:217-225-Muster).
    Den gewählten Schnitt im Code-Kommentar + SUMMARY begründen. Der Integration-Test (Task 3) beweist, dass der GUC beim Write greift.

    `_persist_event_ref` selbst bleibt unverändert (es liest tenant_id weiter via `_tenant_id_for(ev, db)` Z.251 für die Spalte) — der GUC-Set ist die ergänzende Klammer im Consumer, nicht ein Umbau des Persist-Bodys. (Falls A1 genutzt wird, denselben aufgelösten Tenant an beide Stellen geben, kein Doppel-Lookup-Drift.)
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_slow_lane_tenant_guc.py tests/test_slow_lane_flush.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `services/slow_lane.py` ruft `set_current_tenant(` im Consumer-Pfad vor dem abstain_log-Write (grep: `set_current_tenant`)
    - `services/slow_lane.py` ruft `clear_current_tenant()` im `finally` des Consumer-Loops (grep: `clear_current_tenant` im finally-Block)
    - Der Tenant-Wert kommt aus `_tenant_id_for`/calls.tenant_id (DB), NICHT aus `g` (grep: kein `g.tenant_id` in slow_lane.py)
    - set_current_tenant wird VOR `db.commit()` der abstain_log-TX gerufen (Code-Reihenfolge + Kommentar belegt den TX-Timing-Schnitt A1 oder A2)
    - test_slow_lane_flush.py (bestehende Mechanik) bleibt grün (kein Regress an Queue/Flush)
  </acceptance_criteria>
  <done>Daemon setzt GUC vor dem RLS-Write (Variante A) + clear im finally; bestehende Slow-Lane-Mechanik intakt.</done>
</task>

<task type="auto">
  <name>Task 2: M-4 Negativ+Positiv-Test für abstain_log</name>
  <files>tests/test_abstain_log_rls.py</files>
  <read_first>
    - tests/test_rubric_score_schema.py:195-271 (M-4-Test-Muster VERBATIM — Negativ-Bein, Positiv-Bein, Anti-False-Green-GUC-Check, Negativ-Row-Absenz-Beweis, cleanup_rows-finally)
    - database/models.py:781-808 (AbstainLog — Pflichtfelder: event_id NOT NULL FK intent_event.event_id; tenant_id NOT NULL nach Plan 02)
    - RESEARCH.md §4 (M-4-Test-Muster, „ein Test für abstain_log; rubric_score hat ihn schon; suggestion_reactions braucht keinen")
    - tests/conftest.py:36-57 (TEST_TENANT_UUID) + 238 (cleanup_rows) + 669 (db_session, skip ohne DSN)
    - alembic/versions/0022_abstain_log.py (event_id BigInteger FK intent_event.event_id ON DELETE CASCADE — der Test braucht ein gültiges event_id)
  </read_first>
  <action>
    Neue Datei `tests/test_abstain_log_rls.py`. M-4-Test `test_abstain_log_rls_requires_tenant_guc(db_session)`, 1:1 nach test_rubric_score_schema.py:195-269, aber für `AbstainLog`:
    - (a) NEGATIV: `clear_current_tenant(); db_session.rollback()` → frische TX, leerer GUC; `SELECT current_setting('app.tenant_id', true)` MUSS in (None, '') sein (Anti-False-Green-Vorbedingung); `db_session.add(AbstainLog(...tenant_id=tenant...)); commit()` → erwarte Exception (raised=True); rollback.
    - (b) POSITIV: `set_current_tenant(str(tenant)); db_session.rollback()` → derselbe INSERT geht durch + ist lesbar; UND: die Negativ-Row ist unter korrektem GUC abwesend (echte Absenz, nicht RLS-Read-Filter).
    - finally: `set_current_tenant(str(tenant)); db_session.rollback()` + `cleanup_rows(db_session, {"public.abstain_log": written, "public.intent_event": [seeded_event_id], ...}, tenant=tenant)`.

    ABHÄNGIGKEIT abstain_log.event_id (NOT NULL, FK intent_event.event_id CASCADE): der Test MUSS vorab eine gültige intent_event-Row (event_id) seeden (intent_event hat KEINE RLS, RESEARCH §1.3/§2 → GUC-frei seedbar), deren event_id im AbstainLog nutzen, und am Ende mit-wegräumen (reverse-FK-clean: erst abstain_log, dann intent_event). Falls intent_event weitere NOT-NULL-Felder hat: aus models.py:763-ff lesen und minimal seeden. KEINE Source-Presence-Assertion (CLAUDE.md Test-Qualitäts-Regel) — echte DB-Row-Effekte asserten. Skip ohne TEST_DATABASE_URL (SQLite hat kein RLS → kein False-Green).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_abstain_log_rls.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_abstain_log_rls.py` enthält `def test_abstain_log_rls_requires_tenant_guc(db_session):`
    - Negativ-Bein: `clear_current_tenant()` + `assert raised` (INSERT abgewiesen) — V-TF-3
    - Anti-False-Green: `assert guc in (None, '')` VOR dem Negativ-INSERT (GUC wirklich leer)
    - Positiv-Bein: `set_current_tenant` → INSERT durch (`got is not None`) + Negativ-Row-Absenz (`... id == id_neg ... is None`) — V-TF-4
    - Test seedet eine gültige intent_event-Row für event_id (FK NOT NULL) und räumt sie reverse-FK-clean weg (cleanup_rows)
    - Test grün server-seitig (oder skip wenn DSN fehlt — im SUMMARY als skip vermerkt)
  </acceptance_criteria>
  <done>M-4 Negativ+Positiv-Test für abstain_log grün; fail-closed bewiesen (V-TF-3/4).</done>
</task>

<task type="auto">
  <name>Task 3: Slow-Lane-Integration-Test — Abstain-Pfad schreibt abstain_log MIT GUC</name>
  <files>tests/test_slow_lane_tenant_guc.py</files>
  <read_first>
    - tests/test_slow_lane_flush.py:1-45 (bestehendes Slow-Lane-Test-Muster, _fresh_slow_lane, MagicMock-Session-Schnitt)
    - services/slow_lane.py:199-252 (_persist_event_ref Abstain-Pfad) + 362-379 (Consumer mit der neuen GUC-Klammer aus Task 1)
    - RESEARCH.md §4 + VALIDATION Wave 0 („Slow-Lane-Integration-Test: Abstain-Pfad schreibt abstain_log MIT gesetztem GUC, seeded calls.tenant_id") — V-TF-5
    - tests/conftest.py:36-57/238/669 (TEST_TENANT_UUID, cleanup_rows, db_session)
    - database/models.py:682-720 (Call — für seeded calls.tenant_id) + 781-808 (AbstainLog) + intent_event (models.py:763-ff)
  </read_first>
  <action>
    Neue Datei `tests/test_slow_lane_tenant_guc.py`. Integration gegen echtes PG (db_session, skip ohne DSN): beweist, dass der Daemon-Abstain-Pfad mit der Task-1-GUC-Klammer eine abstain_log-Row schreibt, die ohne GUC fail-closed verworfen würde (V-TF-5).

    Setup (alles GUC-frei seedbar außer dem RLS-Write):
    - Eine `calls`-Row mit `tenant_id = TEST_TENANT_UUID` seeden (calls hat keine RLS).
    - Eine `intent_event`-Row (call_id → die calls-Row, handling_status='pending', confidence so dass NICHT 'failed' wird, payload so dass grade_handling abstainiert → score None). intent_event hat keine RLS.
    - Den Abstain-Pfad triggern: ENTWEDER `_persist_event_ref({'event_id': eid}, db)` direkt aufrufen NACH `set_current_tenant(str(TEST_TENANT_UUID))` (beweist: mit GUC schreibt der Write); ODER den Consumer-Klammer-Pfad nachstellen. Bevorzugt: der Test deckt die Consumer-Klammer ab — also den Tenant via die Task-1-Mechanik setzen, dann _persist_event_ref + commit.
    - ASSERT: nach dem Pfad existiert genau eine abstain_log-Row mit event_id==eid und tenant_id==TEST_TENANT_UUID (echte Row-Query).
    - GEGEN-BEWEIS (optional, stärkt V-TF-5): denselben Pfad mit clear_current_tenant() (kein GUC) → der INSERT wird fail-closed verworfen (rollback) → keine Row. (Das ist der Daemon-ohne-Fix-Zustand.)

    Falls grade_handling/_find_next_advisor_utterance zu viel Live-Infra braucht: gezielt monkeypatchen (grade_handling → return None für Abstention; _find_next_advisor_utterance → return None), damit der Test den GUC/RLS-Write isoliert prüft (KEIN Mock der DB-Session — der Write MUSS gegen echtes PG laufen, sonst kein RLS-Beleg). Reverse-FK-clean wegräumen (abstain_log → intent_event → calls) via cleanup_rows mit tenant=TEST_TENANT_UUID. Echte Row-Assertion, KEIN Source-Presence.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_slow_lane_tenant_guc.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_slow_lane_tenant_guc.py` seedet eine `calls`-Row mit `tenant_id == TEST_TENANT_UUID` (grep: `tenant_id` + TEST_TENANT_UUID)
    - Der Test ruft den Abstain-Pfad MIT gesetztem GUC (set_current_tenant) und asserted eine geschriebene `abstain_log`-Row (echte Query, `is not None`)
    - Der DB-Write läuft gegen echtes PG (db_session-Fixture), NICHT gegen eine MagicMock-Session (RLS-Beleg)
    - Reverse-FK-clean Teardown via cleanup_rows (abstain_log/intent_event/calls) mit tenant
    - Test grün server-seitig (oder skip wenn DSN fehlt — im SUMMARY vermerkt)
  </acceptance_criteria>
  <done>Slow-Lane-Abstain-Pfad schreibt abstain_log mit GUC (V-TF-5); der Fix ist integrativ bewiesen, nicht nur unit.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `abstain_log` (SCHREIBEN via Daemon, jetzt mit GUC; FORCE RLS aus Plan 02) — Schema-unverändert in dieser Welle
- `calls` (LESEN: _tenant_id_for + Test-Seed der tenant_id; keine RLS) — keine Änderung
- `intent_event` (LESEN/Test-Seed: event_id-FK; keine RLS) — keine Änderung

### inspect.sh-/psql-Beleg (verbatim aus RESEARCH §1.3 + §2, prod 2026-06-25 — NICHT zusammengefasst)

RLS-Realität (RESEARCH §1.3, nach Plan 02 ist abstain_log FORCE RLS; calls/intent_event bleiben RLS-frei):
```
       relname        | rls_enabled | rls_forced |   owner
----------------------+-------------+------------+-----------
 calls                | f           | f          | nerve_app   ← keine RLS -> _tenant_id_for liest GUC-frei
 intent_event         | f           | f          | nerve_app   ← keine RLS -> Test-Seed GUC-frei
 rubric_score         | t           | t          | nerve_app
 suggestion_reactions | t           | t          | nerve_app
(abstain_log → nach Plan 02: t/t, FORCE RLS)
```

Policy-Mechanik (RESEARCH §1.3): `tenant_isolation` polcmd='*', nullif-fail-closed. GUC leer → `nullif(...)` → `NULL::uuid` → Prädikat false → WITH CHECK weist ab (fail-closed). Genau das trifft der Daemon ohne set_current_tenant.

GUC-TX-Timing (RESEARCH §4, db.py:73-89): der `after_begin`-Hook issued `SELECT set_config('app.tenant_id', %s, true)` bei TX-Begin aus dem contextvar. set_current_tenant MUSS VOR der committenden TX gerufen werden (sonst greift der GUC nicht → M-4).

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `AbstainLog(tenant_id=...)` | slow_lane.py:246-252 (schreibt, Daemon) | DB-Spalte `abstain_log.tenant_id` (UUID, NOT NULL, FORCE RLS nach Plan 02) | ✓ 0022 (Plan 02) |
| `_tenant_id_for(ev, db)` | slow_lane.py:161-167 (liest call.tenant_id) | DB-Spalte `calls.tenant_id` (gesetzt durch Plan 01) | ✓ §2; daher MUSS Plan 01 vorausgehen |
| `set_current_tenant(tid)` | slow_lane.py Consumer (NEU) → after_begin | GUC `app.tenant_id` (SET LOCAL, TX-local, RAM/Session-State) | ✓ db.py:73-89; KEINE DB-Spalte, lebt nur in der TX |
| `_current_tenant_id` contextvar | db.py:51 | Thread-lokaler contextvar (RAM) — im Daemon leer ohne set_current_tenant | ✓ genau die M-4-Falle |

**Cross-Naming-Falle-Check:** `app.tenant_id` (GUC) ist KEINE DB-Spalte — es ist ein TX-lokaler Session-Parameter (set_config), gespeist aus dem contextvar. Im Daemon-Thread ist der contextvar ohne expliziten set_current_tenant leer → GUC NULL → RLS fail-closed. Der Fix setzt den contextvar VOR der committenden TX. ✓

## Verbindungs-Karte (Punkt 22)

| Datum | SCHREIBT (file:line, Thread) | LIEST | Beleg |
|-------|------------------------------|-------|-------|
| `abstain_log` | `_persist_event_ref` slow_lane.py:246 (Daemon-Thread → M-4) | Active-Learning (Post-Launch) | RESEARCH §1.2 |
| `app.tenant_id`-GUC | NEU: set_current_tenant im Consumer (Daemon); auch before_request (Request) | after_begin-Hook db.py:87 (set_config) | RESEARCH §4 |
| `calls.tenant_id` | Plan 01 (create_call_for_sid + Backfill) | `_tenant_id_for` slow_lane.py:166-167 | RESEARCH §1.2 |

grep-Beleg (RESEARCH §1.2): der einzige Daemon-RLS-Write in dieser Welle ist abstain_log (slow_lane.py:246). intent_event hat keine RLS (kein GUC nötig). suggestion_reactions wird NICHT im Daemon geschrieben (§7) → kein Daemon-GUC dort.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon-Thread (kein Request) → RLS-Tabelle abstain_log | GUC muss explizit gesetzt werden; fehlt er → fail-closed silent write loss |
| GUC-TX-Timing | set_current_tenant nach TX-Begin → GUC greift nicht → Write verworfen, lautlos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TF3-01 | Denial of Service / Repudiation | Daemon ohne GUC → abstain_log-INSERT fail-closed verworfen → Event ewig 'pending' → lautloser Daten-Verlust (M-4) | mitigate | set_current_tenant-Klammer VOR der Schreib-TX (Variante A); M-4-Negativ-Test beweist fail-closed, Integration-Test beweist Write mit GUC |
| T-TF3-02 | Tampering | Falsches TX-Timing → GUC greift nicht obwohl gesetzt → still verworfen | mitigate | TX-Timing-Schnitt A1/A2 explizit (separate Lookup-Session ODER rollback nach set); Integration-Test (Task 3) beweist den Write greift end-to-end |
| T-TF3-03 | Information Disclosure | Residual-GUC über Thread-Reuse → falscher Tenant beim nächsten Item | mitigate | clear_current_tenant() im finally pro Item; SET LOCAL auto-clears bei commit/rollback (db.py:85-86) |
| T-TF3-04 | Tampering | Plan-04 baut abweichende GUC-Mechanik → Drift, doppelte/inkonsistente Klammer | mitigate | INTERLOCK (Punkt 24): Plan-04 erbt EXAKT diese Klammer; Pre-Execute-grep gegen Plan-04-PLAN.md, Divergenz=STOP |

T-TF3-01 (die teuerste stille Regression, HIGH) ist durch Negativ-Test (fail-closed-Beweis) + Integration-Test (Write-mit-GUC-Beweis) doppelt gedeckt. Block auf HIGH bis beide Tests grün.
</threat_model>

<verification>
- M-4 Negativ (V-TF-3): abstain_log-INSERT ohne GUC raised (fail-closed).
- M-4 Positiv (V-TF-4): mit GUC durch + Negativ-Row nachweislich absent.
- Integration (V-TF-5): Daemon-Abstain-Pfad schreibt abstain_log mit GUC (seeded calls.tenant_id).
- Bestehende Slow-Lane-Mechanik (test_slow_lane_flush.py) grün (kein Regress).
- clear_current_tenant im finally (Hygiene, grep-belegt).
</verification>

<success_criteria>
- Daemon setzt GUC vor RLS-Write (M-4 geschlossen) — kein lautloser abstain_log-Verlust.
- Negativ+Positiv+Integration-Test grün (V-TF-3/4/5).
- INTERLOCK Plan-04: dieselbe Klammer, kein Drift (Punkt 24).
- 0 ms Live-Latenz-Impact (Slow Lane, Punkt 25).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-03-SUMMARY.md`. Den gewählten TX-Timing-Schnitt (A1/A2) + INTERLOCK-Plan-04-Bestätigung dort belegen.
</output>
