---
phase: 08.23.2.TENANT-FOUND
plan: 02
type: execute
wave: 2
complexity: "🔴"
depends_on: ["08.23.2.TENANT-FOUND-01"]
files_modified:
  - alembic/versions/0022_abstain_log.py
  - alembic/versions/0024_rubric_score_tenant_not_null.py
  - database/models.py
  - services/suggestion_capture.py
  - routes/app_routes.py
autonomous: true
requirements: [TF-2]
user_setup: []

must_haves:
  truths:
    - "abstain_log hat nach Deploy FORCE ROW LEVEL SECURITY + tenant_isolation-Policy (nullif-fail-closed), identisch zu rubric_score"
    - "abstain_log.tenant_id ist NOT NULL (DDL + ORM-konsistent)"
    - "rubric_score.tenant_id ist NOT NULL (0024; Tabelle leer → trivial)"
    - "suggestion_reactions.tenant_id ist NOT NULL + der Request-Flush skippt fail-closed bei fehlendem g.tenant_id (kein IntegrityError bricht die Call-Finalisierung)"
    - "Jede neue/geänderte tenant_id-Spalte trägt ein Schild ≥10 Zeichen (models.py comment= + COMMENT-Migration)"
  artifacts:
    - path: "alembic/versions/0022_abstain_log.py"
      provides: "abstain_log MIT RLS+NOT NULL gefaltet (0022 noch nicht deployt, head=0021)"
      contains: "FORCE ROW LEVEL SECURITY"
    - path: "alembic/versions/0024_rubric_score_tenant_not_null.py"
      provides: "rubric_score.tenant_id NOT NULL, down_revision=0023"
      contains: "down_revision = '0023'"
    - path: "database/models.py"
      provides: "AbstainLog/RubricScore/SuggestionReaction tenant_id nullable=False + Schild"
      contains: "nullable=False"
  key_links:
    - from: "alembic 0022 abstain_log"
      to: "tenant_isolation Policy (0020-Muster)"
      via: "CREATE POLICY USING/WITH CHECK nullif(current_setting('app.tenant_id'...))"
      pattern: "CREATE POLICY tenant_isolation ON public.abstain_log"
    - from: "routes/app_routes.py flush-Aufruf"
      to: "fail-closed skip bei g.tenant_id None"
      via: "if-guard vor flush_suggestion_offers"
      pattern: "_sr_tenant_id"
---

<objective>
Konsistentes FORCE RLS + `tenant_id NOT NULL` auf die Bewertungs-Kinder. Heute inkonsistent (RESEARCH §1.3): `rubric_score`/`suggestion_reactions` haben FORCE RLS, `abstain_log` existiert prod gar nicht (Migration 0022 lokal, head=0021) und hat im Code KEINE RLS.

Drei Schritte (RESEARCH §5/§7):
1. `abstain_log` RLS + `tenant_id NOT NULL` in die **noch-nicht-deployte** 0022 falten (kein Migration-on-Migration; head=0021 prod-belegt). Das 0020-RLS-Muster verbatim wiederverwenden.
2. `rubric_score.tenant_id NOT NULL` via neue Revision 0024 (Tabelle leer → trivial).
3. `suggestion_reactions`-Entscheid (Grau-Zone §7 aufgelöst): **NOT NULL + fail-closed-Skip im Request-Flush** für Konsistenz aller Bewertungs-Kinder. KEINE Daemon-Arbeit (Request-Context, §7). Der Skip verhindert, dass ein User ohne tenant_org einen IntegrityError im Call-Ende-Flush auslöst.

Locked decisions:
- 0022 direkt editieren (RESEARCH §5/§11.6 — noch nicht deployt). Pre-Execute: head erneut auf 0021 prüfen (BLOCKIEREND, siehe PRE-EXECUTE Schritt 0).
- Revisions-Reihenfolge: 0022(editiert) → 0023(Plan 01) → 0024(rubric_score NOT NULL). EIN Deploy, Migrationen VOR Restart (create_all-Race, §5).
- Schild-Pflicht (Punkt 23): tenant_id-Schild der Kinder anpassen (RLS-Status), COMMENT-Migration nachziehen.

Purpose: Das fail-closed RLS-Schloss ist die DSGVO-Cross-Tenant-Wall. Setzt Plan 01 voraus (calls.tenant_id gesetzt, sonst _tenant_id_for=NULL → fail-closed silent).
Output: 0022 gefaltet + 0024 + models.py NOT NULL + suggestion_reactions-Skip + Schilder + VALIDATION.md V-TF-6/7/8 auf green.

Latenz-Budget (Punkt 25): 0 ms Live-Bahn. Der suggestion_reactions-Flush läuft am Call-ENDE (Request-Kontext, app_routes.py:411 „KEIN Live-Loop-DB-Write"), nicht im Antwort-Pfad. RLS-Policies sind set_config-basiert (sub-ms TX-local SET).

INTERLOCK (Punkt 24) — TAXO2-Plan-04: rubric_score erbt die NOT-NULL-Härte hier; Plan-04 (rubric_score-Write im Daemon) MUSS den Daemon-GUC-Vertrag aus Plan 03 (A1-set_current_tenant-Klammer) nutzen, sonst schreibt es fail-closed nichts. Pre-Execute-Interlock-Notiz unten (HARTES Akzeptanzkriterium, vgl. Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

PRE-EXECUTE (durch Executor/André):

> ### 🔴 SCHRITT 0 — BLOCKIERENDER ERSTER CHECK: Alembic-head MUSS `0021` sein
> **Vor JEDER anderen Aktion in diesem Plan** (Cross-AI Gemini HIGH #1 — der größte Migrations-Sicherheits-Punkt):
> ```bash
> ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
> ```
> **Erwartet: `0021`.** Nur dann ist 0022 noch NICHT deployt und darf direkt editiert werden.
> **Falls NICHT 0021** (z.B. ein Hotfix landete 0022+ zwischen Plan und Execute): **STOP — KEIN Edit an 0022.** Das Editieren einer bereits deployten Migration wäre katastrophal. Stattdessen: neue Revision 0025 für abstain_log RLS+NOT NULL (RESEARCH §11.6) → STOP + replan genau diesen Punkt, BEVOR irgendetwas anderes passiert.
> Dieser Check ist der erste, rote, blockierende Schritt des gesamten Go-Live-Runbooks für W2.

- INTERLOCK TAXO2-Plan-04: `.planning/phases/08.23.2.TAXO2-bewerten-eine-noten-engine/08.23.2.TAXO2-04-coaching-score-cutover-async-PLAN.md` nebeneinander legen → greppen, dass Plan-04 die A1-set_current_tenant-Klammer (Plan 03) für rubric_score nutzt (kein Doppel-Bau, kein Drift). Divergenz = STOP. (HARTES Akzeptanzkriterium, vgl. Plan 03 INTERLOCK-Forward-Contract.)
- HART: kein Local-Dev, Verify server-seitig über deploy.sh-Gate / SSH.
- Multi-Segment-Gotcha: Pfade hardcoden.
</execution_context>

<context>
@.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-RESEARCH.md
@.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-VALIDATION.md

<interfaces>
<!-- Verträge aus dem Codebase, direkt nutzbar. -->

RLS-Muster (alembic/versions/0020_rubric_score_table.py:88-98, VERBATIM für abstain_log wiederverwenden):
```python
op.execute("ALTER TABLE public.rubric_score OWNER TO nerve_app")
op.execute("ALTER TABLE public.rubric_score ENABLE ROW LEVEL SECURITY")
op.execute("ALTER TABLE public.rubric_score FORCE ROW LEVEL SECURITY")
op.execute("""
    CREATE POLICY tenant_isolation ON public.rubric_score
      USING (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
      WITH CHECK (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
""")
```
Downgrade-Symmetrie (0020:120): `op.execute("DROP POLICY IF EXISTS tenant_isolation ON public.<t>")` VOR drop_table.

0022 aktueller Zustand (alembic/versions/0022_abstain_log.py): tenant_id Z.53 `nullable=True`, KEINE RLS (Z.20-21 Kommentar „KEINE RLS … bewusst minimal"), Owner-Switch Z.62, COMMENT-Block Z.64-70, downgrade Z.73-76 nur drop_table.

models.py tenant_id-Spalten (Schild bereits ≥10 Zeichen vorhanden, Text muss RLS-Status nachziehen):
- AbstainLog.tenant_id models.py:804 (nullable=True, comment="Mandanten-Abschottung (abgeleitet aus calls.tenant_id). Per-Tenant-Filter der Nachbewertung.")
- RubricScore.tenant_id models.py:874 (nullable=True, comment="Mandanten-Abschottung (D-11, RLS-ready). Abgeleitet aus calls.tenant_id. Policy erst COACH.")
- SuggestionReaction.tenant_id models.py:834 (nullable=True, comment="Mandanten-Abschottung (RLS FORCE, abgeleitet aus calls.tenant_id).")

suggestion_reactions Request-Flush (routes/app_routes.py:414-440, Call-Ende, g.tenant_id):
```python
_sr_tenant_id = getattr(g, 'tenant_id', None)
_sr_n = flush_suggestion_offers(..., tenant_id=_sr_tenant_id, db=db_conv)
```
flush_suggestion_offers (services/suggestion_capture.py:50-96) schreibt SuggestionReaction(..., tenant_id=tenant_id). EINZIGER Schreiber im Request-Kontext (RESEARCH §7); KEIN Daemon-Schreiber.

RLS-Realität prod (RESEARCH §1.3): rubric_score=FORCE, suggestion_reactions=FORCE, abstain_log existiert nicht. Policy `tenant_isolation` polcmd='*', USING==WITH CHECK, nullif-fail-closed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: abstain_log RLS + tenant_id NOT NULL in 0022 falten + Schild</name>
  <files>alembic/versions/0022_abstain_log.py, database/models.py</files>
  <read_first>
    - alembic/versions/0022_abstain_log.py (komplett — Falt-Ziel; tenant_id Z.53, Owner-Switch Z.62, downgrade Z.73-76)
    - alembic/versions/0020_rubric_score_table.py:88-98 (RLS-Trio verbatim) + Z.117-122 (Downgrade-Symmetrie)
    - RESEARCH.md §5 („Was RLS+NOT NULL in 0022 falten konkret erfordert", 5 Schritte) + §1.3 (Policy-Text verbatim)
    - database/models.py:781-808 (AbstainLog — tenant_id Z.804 nullable=True + Schild)
    - CLAUDE.md Punkt 23 (Schild-Pflicht: comment= + COMMENT-Migration in DERSELBEN Änderung)
  </read_first>
  <action>
    VORAUSSETZUNG: PRE-EXECUTE Schritt 0 (head==0021) MUSS grün sein. Erst dann 0022 editieren.

    In `alembic/versions/0022_abstain_log.py` (head=0021 → noch nicht deployt → direkt editieren, RESEARCH §5):
    1. `tenant_id`-Spalte Z.53: `nullable=True` → `nullable=False`. (Tabelle startet leer → kein Backfill nötig.)
    2. NACH `op.execute("ALTER TABLE public.abstain_log OWNER TO nerve_app")` (Z.62) das RLS-Trio aus 0020:91-98 anhängen (verbatim, public.abstain_log):
       ```python
       op.execute("ALTER TABLE public.abstain_log ENABLE ROW LEVEL SECURITY")
       op.execute("ALTER TABLE public.abstain_log FORCE ROW LEVEL SECURITY")
       op.execute("""
           CREATE POLICY tenant_isolation ON public.abstain_log
             USING (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
             WITH CHECK (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
       """)
       ```
    3. `downgrade()` (Z.73-76): `op.execute("DROP POLICY IF EXISTS tenant_isolation ON public.abstain_log")` VOR `op.drop_table('abstain_log')` (0020:120-Muster).
    4. Den Modul-Docstring-Hinweis „KEINE RLS auf dieser Tabelle … bewusst minimal" (Z.20-22) korrigieren auf: „FORCE RLS + tenant_isolation (D-11, konsistent mit rubric_score/suggestion_reactions, TENANT-FOUND); tenant_id NOT NULL." (Konsistenz Doc/Code.)

    In `database/models.py`:
    5. `AbstainLog.tenant_id` (Z.804): `nullable=True` → `nullable=False`; Schild-Text auf RLS-Status ziehen, z.B. comment="Mandanten-Abschottung (FORCE RLS tenant_isolation, NOT NULL; abgeleitet aus calls.tenant_id via Daemon-GUC Plan 03). Per-Tenant-Wall der Nachbewertung." (≥10 Zeichen, Punkt 23). KEINE COMMENT-Migration für den Spalten-Text nötig falls Text gegenüber 0022:70 unverändert bleibt — ABER da der Text geändert wird, die COMMENT-Zeile in 0022 (Z.70) mit-anpassen (Single-Source models.py == 0022 COMMENT).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_schild_guard.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/versions/0022_abstain_log.py` enthält `CREATE POLICY tenant_isolation ON public.abstain_log` (grep)
    - 0022 enthält `ALTER TABLE public.abstain_log FORCE ROW LEVEL SECURITY` (grep)
    - 0022 tenant_id-Column ist `nullable=False` (grep im abstain_log create_table-Block)
    - 0022 downgrade enthält `DROP POLICY IF EXISTS tenant_isolation ON public.abstain_log` (grep)
    - `database/models.py` AbstainLog.tenant_id: `nullable=False` UND Schild-comment ≥10 Zeichen mit „RLS"/„NOT NULL"-Bezug
    - models.py-Schild-Text == 0022 COMMENT-Text für abstain_log.tenant_id (Single-Source-Konsistenz)
    - test_schild_guard.py grün server-seitig
  </acceptance_criteria>
  <done>abstain_log konsistent zu rubric_score (FORCE RLS + NOT NULL) gefaltet; Schild aktuell + single-source.</done>
</task>

<task type="auto">
  <name>Task 2: rubric_score.tenant_id NOT NULL (Migration 0024) + Schild</name>
  <files>alembic/versions/0024_rubric_score_tenant_not_null.py, database/models.py</files>
  <read_first>
    - alembic/versions/0020_rubric_score_table.py (rubric_score-Definition, tenant_id Z.67-68 nullable=True + FK tenant_orgs)
    - alembic/versions/0022_abstain_log.py (Revisions-Muster für 0024-down_revision-Kette)
    - RESEARCH.md §5 („eine separate Revision 0024 für rubric_score.tenant_id NOT NULL; Tabelle leer → trivial; models.py:874 nachziehen") + §11.6 (Reihenfolge)
    - database/models.py:853-885 (RubricScore — tenant_id Z.874 nullable=True + Schild)
    - RESEARCH.md §1.3 (rubric_score ist leer — `ALTER COLUMN SET NOT NULL` trivial, kein Backfill)
  </read_first>
  <action>
    Neue Migration `alembic/versions/0024_rubric_score_tenant_not_null.py`, `revision='0024'`, `down_revision='0023'` (RESEARCH §5/§11.6, hängt an den Backfill aus Plan 01):
    ```python
    def upgrade() -> None:
        # rubric_score ist leer (RESEARCH §1.3) -> SET NOT NULL trivial, kein Backfill.
        # Konsistente NOT-NULL-Härte auf den Bewertungs-Kindern (TF-2, D-11).
        op.alter_column('rubric_score', 'tenant_id', nullable=False,
                        existing_type=postgresql.UUID(as_uuid=True))
        # Schild-Update (Single-Source models.py) — RLS+NOT-NULL-Status.
        op.execute("COMMENT ON COLUMN public.rubric_score.tenant_id IS '<neuer Schild-Text, identisch zu models.py>'")

    def downgrade() -> None:
        op.alter_column('rubric_score', 'tenant_id', nullable=True,
                        existing_type=postgresql.UUID(as_uuid=True))
    ```
    (postgresql aus `from sqlalchemy.dialects import postgresql` importieren, Hausmuster 0020:43.)

    In `database/models.py` RubricScore.tenant_id (Z.874): `nullable=True` → `nullable=False`; Schild-Text auf NOT-NULL/RLS-Status ziehen (z.B. „Mandanten-Abschottung (D-11 FORCE RLS, NOT NULL). Abgeleitet aus calls.tenant_id via Daemon-GUC (Plan 04 erbt Plan-03-A1-Klammer)."). Den COMMENT-Text in 0024 == models.py (Single-Source). Den partiellen Unique-Index + RLS-Policy NICHT anfassen (0020 deckt sie, rubric_score ist prod schon FORCE RLS).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_rubric_score_schema.py tests/test_schild_guard.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/versions/0024_rubric_score_tenant_not_null.py` existiert mit `revision = '0024'` UND `down_revision = '0023'`
    - upgrade enthält `op.alter_column('rubric_score', 'tenant_id', nullable=False` (grep)
    - downgrade setzt nullable=True zurück (reversibel)
    - `database/models.py` RubricScore.tenant_id: `nullable=False` + Schild ≥10 Zeichen
    - 0024 COMMENT-Text == models.py-Schild (Single-Source)
    - test_rubric_score_schema.py + test_schild_guard.py grün server-seitig (V-TF-6)
  </acceptance_criteria>
  <done>rubric_score.tenant_id NOT NULL erzwungen (0024); Schild aktuell; bestehende RLS-Tests grün.</done>
</task>

<task type="auto">
  <name>Task 3: suggestion_reactions NOT NULL + fail-closed-Skip im Request-Flush + Schild</name>
  <files>database/models.py, services/suggestion_capture.py, routes/app_routes.py, alembic/versions/0024_rubric_score_tenant_not_null.py</files>
  <read_first>
    - RESEARCH.md §7 (suggestion_reactions-Verdikt: Request-Context, kein Daemon; NOT NULL + fail-closed-Skip empfohlen) + §1.2 (einziger Schreiber suggestion_capture.py:76 ← app_routes.py:423)
    - routes/app_routes.py:408-441 (Call-Ende-Flush, g.tenant_id Z.422, non-fatal try/except)
    - services/suggestion_capture.py:50-96 (flush_suggestion_offers, tenant_id-Param)
    - database/models.py:811-850 (SuggestionReaction — tenant_id Z.834 nullable=True + Schild)
    - RESEARCH.md §1.3 (suggestion_reactions prod: 3 Zeilen, alle MIT tenant → SET NOT NULL ok ohne Backfill)
  </read_first>
  <action>
    Verdikt (Grau-Zone §7 aufgelöst): **NOT NULL + fail-closed-Skip im Flush** — Konsistenz aller Bewertungs-Kinder. KEINE Daemon-Arbeit (Request-Context). Begründung im SUMMARY: prod hat 3/3 Zeilen mit tenant, der einzige Schreiber ist der Request-Flush (g.tenant_id); der Skip schützt vor IntegrityError, falls ein User je ohne tenant_org einen Call beendet.

    1. `database/models.py` SuggestionReaction.tenant_id (Z.834): `nullable=True` → `nullable=False`; Schild-Text auf NOT-NULL/RLS ziehen (≥10 Zeichen, Punkt 23).
    2. `alembic/versions/0024_rubric_score_tenant_not_null.py` (dieselbe Revision, Sammel-NOT-NULL der Kinder) ergänzen:
       ```python
       op.alter_column('suggestion_reactions', 'tenant_id', nullable=False,
                       existing_type=postgresql.UUID(as_uuid=True))
       op.execute("COMMENT ON COLUMN public.suggestion_reactions.tenant_id IS '<neuer Schild-Text == models.py>'")
       ```
       Downgrade symmetrisch (nullable=True). (3/3 prod-Zeilen mit tenant → kein Backfill, RESEARCH §1.3.)
    3. `routes/app_routes.py` (um Z.422): fail-closed-Skip VOR dem flush-Aufruf — kein tenant → kein Flush (statt IntegrityError den Call-Abschluss zu brechen):
       ```python
       _sr_tenant_id = getattr(g, 'tenant_id', None)
       if not _sr_tenant_id:
           print("[TAXO2-08] suggestion-flush skip: kein g.tenant_id (fail-closed, kein IntegrityError)")
       else:
           _sr_n = flush_suggestion_offers(... tenant_id=_sr_tenant_id, db=db_conv)
           if _sr_n:
               db_conv.commit()
       ```
       Den bestehenden non-fatal try/except (Z.435-440) als äußere Klammer behalten. Der bestehende Kommentar Z.416-421 (tenant_id == g.tenant_id == RLS-GUC) bleibt gültig.
    4. `services/suggestion_capture.py`: keine Signatur-Änderung nötig (Aufrufer schützt). Optional defensiver Early-Return falls `tenant_id is None` (Doppel-Defense) — wenn ergänzt, als reiner Guard ohne Verhalten-Bruch.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/ -k "suggestion or schild" -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `database/models.py` SuggestionReaction.tenant_id: `nullable=False` + Schild ≥10 Zeichen
    - 0024 enthält `op.alter_column('suggestion_reactions', 'tenant_id', nullable=False` (grep)
    - `routes/app_routes.py` enthält einen `if not _sr_tenant_id:`-Skip vor dem flush-Aufruf (grep: `if not _sr_tenant_id`)
    - 0024 COMMENT-Text == models.py-Schild für suggestion_reactions.tenant_id (Single-Source)
    - suggestion/schild-Tests grün server-seitig (V-TF-7 Regression: bestehender Flush-Pfad bleibt funktional + V-TF-8 Schild-Guard)
  </acceptance_criteria>
  <done>suggestion_reactions konsistent (NOT NULL); Request-Flush fail-closed bei fehlendem Tenant; Schild aktuell; KEINE Daemon-Arbeit.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `abstain_log` (SCHEMA-Änderung: RLS + tenant_id NOT NULL — existiert prod NICHT, head=0021)
- `rubric_score` (SCHEMA-Änderung: tenant_id NOT NULL — leer)
- `suggestion_reactions` (SCHEMA-Änderung: tenant_id NOT NULL — 3 Zeilen, alle mit tenant)
- `tenant_orgs` (FK-Ziel, lesend — keine Änderung)

### inspect.sh-/psql-Beleg (verbatim aus RESEARCH §1.3 + §2, prod 2026-06-25 — NICHT zusammengefasst)

RLS-Realität (RESEARCH §1.3, `pg_class` als postgres):
```
       relname        | rls_enabled | rls_forced |   owner
----------------------+-------------+------------+-----------
 rubric_score         | t           | t          | nerve_app
 suggestion_reactions | t           | t          | nerve_app
(abstain_log → NICHT in pg_class: Tabelle existiert nicht auf prod)
```

Policy-Text (RESEARCH §1.3, `pg_policy` als postgres) — Falt-Vorlage für abstain_log:
```
 polname          | polcmd | using_expr / withcheck_expr (identisch)                              |   tbl
------------------+--------+----------------------------------------------------------------------+----------------------
 tenant_isolation | *      | (tenant_id = (NULLIF(current_setting('app.tenant_id'::text, true), ''::text))::uuid) | suggestion_reactions
 tenant_isolation | *      | (tenant_id = (NULLIF(current_setting('app.tenant_id'::text, true), ''::text))::uuid) | rubric_score
```

Migration-0022-State (RESEARCH §5): live head=`0021` (`SELECT version_num FROM alembic_version` → 0021). 0022 lokal (`revision='0022'`, `down_revision='0021'`), abstain_log existiert prod nicht (`relation "abstain_log" does not exist`).

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `AbstainLog.tenant_id` | slow_lane.py:251 (schreibt, Plan 03) | DB-Spalte `abstain_log.tenant_id` (UUID, → NOT NULL, RLS) | ✓ 0022:53 (Falt-Ziel); existiert noch nicht prod |
| `RubricScore.tenant_id` | Plan-04 (schreibt, künftig) | DB-Spalte `rubric_score.tenant_id` (UUID, FK tenant_orgs, → NOT NULL, FORCE RLS) | ✓ psql §1.3 + 0020:67-68 |
| `SuggestionReaction.tenant_id` | suggestion_capture.py:82 (schreibt, Request) | DB-Spalte `suggestion_reactions.tenant_id` (UUID, → NOT NULL, FORCE RLS) | ✓ psql §1.3 (3/3 mit tenant) |
| `g.tenant_id` (Flush-Quelle) | app_routes.py:422 | Flask-`g` (RAM, per-Request) — kann NULL sein (User ohne tenant_org) | ✓ Request-Context; fail-closed-Skip ergänzt |

**Cross-Naming-Falle-Check:** Der Flush nutzt `g.tenant_id` (RAM, kann NULL) als tenant_id-Quelle. Bei NOT NULL würde NULL einen IntegrityError werfen → der fail-closed-Skip (Task 3) fängt das ab, ohne den Call-Abschluss zu brechen. ✓

## Verbindungs-Karte (Punkt 22)

| Datum | SCHREIBT (file:line, Kontext) | LIEST | Beleg |
|-------|-------------------------------|-------|-------|
| `abstain_log` | `_persist_event_ref` slow_lane.py:246 (Daemon-Thread → M-4, Plan 03) | Active-Learning (Post-Launch) | RESEARCH §1.2 |
| `rubric_score` | Plan-04 (Daemon-Thread → M-4) | dashboard.py + performance.py (999.2) | RESEARCH §1.2 |
| `suggestion_reactions` | `flush_suggestion_offers` suggestion_capture.py:76 ← app_routes.py:423 (REQUEST, g.tenant_id) | Übernahme-Scoring (Post-Launch) | RESEARCH §1.2/§7 |

grep-Beleg (RESEARCH §7): kein `SuggestionReaction(` im Daemon-Pfad (slow_lane.py) — nur suggestion_capture.py:76 + app_routes.py:423 → suggestion_reactions braucht KEINEN Daemon-GUC-Fix.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Beliebiger Tenant → Bewertungs-Kinder | RLS-Wall: ohne korrekten app.tenant_id-GUC darf keine Zeile geschrieben/gelesen werden (Cross-Tenant-Isolation) |
| Request-Flush (g.tenant_id) → suggestion_reactions | NULL-Tenant würde NOT-NULL-Constraint verletzen und Call-Finalisierung brechen |
| Plan-vs-Execute-Zeitfenster → Migration 0022 | Ein zwischenzeitlicher Hotfix-Deploy würde das Editieren von 0022 katastrophal machen (Migration-on-Migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TF2-01 | Information Disclosure | abstain_log ohne RLS → Cross-Tenant-Lesbarkeit der Wortlaut-Zeilen | mitigate | FORCE RLS + tenant_isolation (nullif-fail-closed) in 0022 gefaltet, identisch zu rubric_score; M-4-Negativ-Test (Plan 03) beweist fail-closed |
| T-TF2-02 | Tampering | tenant_id NULL auf Bewertungs-Kind → Zeile entkommt der RLS-Wall (für jeden GUC unsichtbar/falsch) | mitigate | tenant_id NOT NULL auf allen drei Kindern (0022/0024) erzwingt eindeutige Mandanten-Zuordnung |
| T-TF2-03 | Denial of Service | NOT NULL + g.tenant_id=NULL → IntegrityError bricht Call-Finalisierung | mitigate | fail-closed-Skip in app_routes.py (kein tenant → kein Flush, Call-Abschluss intakt) |
| T-TF2-04 | Tampering | 0022 zwischenzeitlich deployt → Edit auf deployter Migration (Migration-on-Migration) | mitigate | PRE-EXECUTE Schritt 0 (BLOCKIEREND, rot): head-Recheck (==0021); falls deployt → neue Revision 0025 statt Edit (STOP+replan). Cross-AI HIGH #1 — als erster blockierender Runbook-Schritt geführt. |

T-TF2-01/02 (HIGH, DSGVO-Kern) durch FORCE RLS + NOT NULL gemindert; der fail-closed-Beweis kommt in Plan 03 (M-4-Test). Bis dahin: Plan 03 ist Pflicht-Folge (W3), KEIN Deploy von W2 ohne W3 im selben Deploy. T-TF2-04 (HIGH, Migrations-Sicherheit) ist durch den blockierenden PRE-EXECUTE-Schritt 0 gemindert.
</threat_model>

<verification>
- PRE-EXECUTE Schritt 0 (BLOCKIEREND): head==0021 bestätigt, BEVOR 0022 editiert wird (Cross-AI HIGH #1).
- abstain_log: FORCE RLS + tenant_isolation + NOT NULL in 0022 (grep-belegt) — fail-closed-Beweis in Plan 03 (V-TF-3/4).
- rubric_score.tenant_id NOT NULL (0024) — `inspect.sh schema rubric_score` zeigt `not null` nach Deploy (V-TF-6, supervised).
- suggestion_reactions.tenant_id NOT NULL + Flush-Skip — bestehender Flush-Test grün (V-TF-7 Regression).
- Schild-Guard grün (V-TF-8): alle drei tenant_id-Schilder ≥10 Zeichen, models.py == COMMENT-Migration.
</verification>

<success_criteria>
- Konsistentes FORCE RLS + tenant_id NOT NULL auf abstain_log/rubric_score/suggestion_reactions.
- Request-Flush robust (fail-closed bei fehlendem Tenant).
- Schilder aktuell + single-source.
- Migrationen 0022(editiert)→0024 reversibel; Reihenfolge mit Plan-01-0023 konsistent (0022→0023→0024); head==0021 vor Edit bestätigt.
- INTERLOCK Plan-04 dokumentiert (rubric_score erbt Daemon-GUC-A1-Klammer aus Plan 03, HARTES Akzeptanzkriterium).
- VALIDATION.md V-TF-6 + V-TF-7 + V-TF-8 von `pending` auf `green` gesetzt (DoD).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-02-SUMMARY.md`.
- suggestion_reactions-Verdikt-Begründung + INTERLOCK-Plan-04-Status (HARTES Akzeptanzkriterium) dort belegen.
- PRE-EXECUTE Schritt 0 (head==0021) Ergebnis dort belegen.
- DEFINITION OF DONE (Cross-AI Process #4a): In `.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-VALIDATION.md` die Zeilen **V-TF-6**, **V-TF-7** und **V-TF-8** von Status `⬜ pending` auf `✅ green` setzen (nur diese drei — V-TF-1/2 gehören Plan 01, V-TF-3/4/5 Plan 03). Beleg im SUMMARY verlinken.
</output>
</content>
