---
phase: 08.23.2.TENANT-FOUND
plan: 01
title: Anlage + Tenant-Backfill
status: built — pending supervised deploy-gate
built: 2026-06-25
commits: [4b603a6, 609277a, 4193ffa, 7cf7551]
---

# Plan 01 — Anlage-Lücke + Backfill (SUMMARY)

**Status: CODE-COMPLETE + gepusht (W1). NICHT deployed, NICHT server-seitig verifiziert.**
Acceptance läuft scharf im `deploy.sh production`-Pytest-Gate (HART: kein Local-Dev, kein lokales
pytest als Acceptance). André/Claudian fahren den beaufsichtigten Deploy.

## Was gebaut wurde

| Task | Datei | Commit | Inhalt |
|------|-------|--------|--------|
| 1 | `database/db.py` | 4b603a6 | `resolve_tenant_uuid_for_user(user_id, db)` — single-source user→org→tenant_orgs.id-Join (EINE indizierte Query, None bei Edge, kein Default-Tenant, kein raise). Kontext-unabhängig (Callback-Thread-tauglich). |
| 1 | `tests/test_tenant_resolution.py` | 4b603a6 | Resolution-Test: Positiv (`== TEST_TENANT_UUID`) + Negativ (`is None` für user_id 99999999). Echte Funktions-Rückgabe, kein Source-Presence. Skip ohne `TEST_DATABASE_URL`. |
| 2 | `services/live_session.py` | 609277a | `create_call_for_sid` löst `_tid = resolve_tenant_uuid_for_user(user_id, _db)` VOR dem `Call(...)`-Insert auf und setzt `tenant_id=_tid`. Kein `set_current_tenant`/GUC (calls hat keine RLS). None → NULL, Call-Anlage bricht nicht (fail-soft). |
| 3 | `alembic/versions/0023_backfill_calls_tenant_id.py` | 4193ffa | Idempotente set-based Backfill-Migration, `revision='0023'`, `down_revision='0022'`. `SET tenant_id = t.id ... WHERE c.user_id = u.id AND c.tenant_id IS NULL`. KEIN Schema-DDL (kein ALTER/NOT NULL/FK/RLS). downgrade = no-op (rekonstruierbar, AUDIT-NOTE). |
| 3 | `tests/test_tenant_resolution.py` | 4193ffa | Idempotenz-Test (auf Test-Call gescoped): Lauf 1 setzt Tenant (rowcount 1, == erwarteter Tenant), Lauf 2 rowcount 0 (idempotent). Echte Row-Assertion. |
| 4 | `routes/auth.py` | 7cf7551 | `_login_user` nutzt `resolve_tenant_uuid_for_user(user_id, db)` statt der Inline-`TenantOrg.filter_by(legacy_org_id)`-Query. Single source of truth (Anti-Drift). |

## auth.py-Refactor-Beleg (grep, lokaler git-Stand)

```
grep -c "filter_by(legacy_org_id=user_org_id)" routes/auth.py   -> 0  (Inline-Quelle entfernt)
grep -c "TenantOrg as _TenantOrg" routes/auth.py                -> 0  (kein zweiter Inline-Import)
grep -c "resolve_tenant_uuid_for_user(user_id, db)" routes/auth.py -> 1
grep -c "session['tenant_id'] = tenant_id_str" routes/auth.py   -> 1  (unverändert)
grep -c "session['org_id']" routes/auth.py                      -> 1  (kein Kollateral)
```
→ Nach dem Refactor lebt nur noch EINE user→tenant-Auflösung (`db.resolve_tenant_uuid_for_user`).

## Backfill-Verifikations-Runbook — AUSZUFÜHREN beim beaufsichtigten Deploy (NICHT in W1)

Migration 0023 läuft als postgres VOR dem Restart (create_all-Falle). Nach 0023:

(a) **RESIDUAL-COUNT**
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh count calls'
psql: SELECT count(*) FROM calls WHERE tenant_id IS NULL;   -- erwartet 0 (37/37 auflösbar, §6)
```
(b) **STICHPROBEN-PRÜFUNG** (3-4 zuvor-NULL Calls, read-only psql gegen Prod):
```sql
SELECT c.id, c.user_id, c.tenant_id AS gesetzte_tid,
       t.id AS erwartete_tid, (c.tenant_id = t.id) AS match
FROM calls c
JOIN users u ON u.id = c.user_id
JOIN tenant_orgs t ON t.legacy_org_id = u.org_id
ORDER BY c.started_at DESC LIMIT 4;
```
Erwartung: jede Zeile `match = true`. **Ergebnis hier eintragen nach dem Deploy:** ⬜ pending.

## DoD (Cross-AI Process #4a)
- **V-TF-1** (new calls get tenant_id) + **V-TF-2** (backfill idempotent + sets all resolvable):
  Code + Tests gebaut. VALIDATION.md auf `🔨 built (pending deploy-gate)` gesetzt — Flip auf
  ✅ green durch André nach grünem Deploy-Gate (server-seitiger pytest) + Runbook (a)/(b).

## Latenz (Punkt 25)
0 ms Live-Bahn. Resolution = 1× indizierter Join bei Call-Anlage (Callback-Thread), im Login-Pfad
ersetzt sie eine bereits existierende Single-Query. Backfill = einmalig (37 Rows) beim Deploy.

## Offen / Risiken
- **PRE-EXECUTE head==0021** (RESEARCH §5): VOR dem Deploy erneut prüfen — falls 0022 zwischenzeitlich
  deployt wäre, 0023-down_revision neu bewerten. (Claudian-Audit 2026-06-25: head==0021 bestätigt.)
- W1 ist Fundament für W2/W3: ohne gesetztes `calls.tenant_id` liefert `_tenant_id_for` None →
  RLS-Kinder schreiben fail-closed nichts.
