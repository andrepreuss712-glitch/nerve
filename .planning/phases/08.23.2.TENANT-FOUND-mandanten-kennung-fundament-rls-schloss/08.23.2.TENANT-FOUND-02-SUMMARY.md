---
phase: 08.23.2.TENANT-FOUND
plan: 02
title: RLS-Schloss auf die Bewertungs-Kinder
status: built — pending supervised deploy-gate
built: 2026-06-25
commits: [0e02e51, 462bb59, 32b3513]
---

# Plan 02 — Konsistentes FORCE RLS + tenant_id NOT NULL (SUMMARY)

**Status: CODE-COMPLETE + gepusht (W2). NICHT deployed, NICHT server-seitig verifiziert.**
Acceptance im `deploy.sh production`-Pytest-Gate (HART). W2 + W3 deployen ZUSAMMEN (sobald
abstain_log FORCE RLS bekommt, MUSS die Plan-03-A1-Klammer im Daemon stehen, sonst bricht der
abstain_log-Write fail-closed).

## PRE-EXECUTE Schritt 0 (BLOCKIEREND) — head==0021

Claudian-Pre-Execute-Audit 2026-06-25 (am Prod-Server verifiziert): **head == 0021**. Damit ist
0022 NICHT deployt und durfte direkt editiert werden (kein Migration-on-Migration). Lokal ist die
Revisions-Kette `0019 → 0020 → 0021 → 0022(editiert) → 0023 → 0024` — die neuen Revisionen
0023/0024 hängen sauber an. **Re-Check unmittelbar vor dem Deploy Pflicht** (falls ein Hotfix
zwischenzeitlich 0022+ deployt hätte → STOP, neue Revision 0025 statt Edit, replan).

## Was gebaut wurde

| Task | Datei | Commit | Inhalt |
|------|-------|--------|--------|
| 1 | `alembic/versions/0022_abstain_log.py` | 0e02e51 | `tenant_id` nullable=False; RLS-Trio (ENABLE + FORCE + `CREATE POLICY tenant_isolation ... nullif-fail-closed`) verbatim aus 0020:91-98; downgrade `DROP POLICY IF EXISTS tenant_isolation` vor drop_table; Docstring „KEINE RLS" → „FORCE RLS + NOT NULL"; COMMENT-Text aktualisiert. |
| 1 | `database/models.py` (AbstainLog) | 0e02e51 | `tenant_id` nullable=False + Schild (FORCE RLS, NOT NULL) — **== 0022 COMMENT (Single-Source)**. |
| 2 | `alembic/versions/0024_rubric_score_tenant_not_null.py` | 462bb59 | `revision='0024'`, `down_revision='0023'`; `op.alter_column('rubric_score','tenant_id',nullable=False)` (Tabelle leer → trivial) + COMMENT. |
| 2 | `database/models.py` (RubricScore) | 462bb59 | `tenant_id` nullable=False + Schild (D-11 FORCE RLS, NOT NULL) == 0024 COMMENT. |
| 3 | `alembic/versions/0024_...py` | 32b3513 | `op.alter_column('suggestion_reactions','tenant_id',nullable=False)` + COMMENT; downgrade symmetrisch (beide zurück auf nullable=True). |
| 3 | `database/models.py` (SuggestionReaction) | 32b3513 | `tenant_id` nullable=False + Schild == 0024 COMMENT. |
| 3 | `routes/app_routes.py` | 32b3513 | Call-Ende-Flush: `if not _sr_tenant_id:` → skip (fail-closed, kein IntegrityError) statt NOT-NULL-Insert; sonst flush wie bisher. Äußerer non-fatal try/except bleibt. |

## suggestion_reactions-Verdikt (Grau-Zone §7 aufgelöst)
**NOT NULL + fail-closed-Skip im Request-Flush** — Konsistenz aller drei Bewertungs-Kinder.
Begründung: prod hat 3/3 Zeilen mit Tenant (RESEARCH §1.3); der EINZIGE Schreiber ist der
Request-Flush (`flush_suggestion_offers` ← app_routes.py, g.tenant_id), KEIN Daemon-Schreiber
(grep: kein `SuggestionReaction(` in slow_lane.py). Der Skip schützt vor einem IntegrityError im
Call-Ende-Flush, falls ein User je ohne tenant_org einen Call beendet. KEINE Daemon-Arbeit nötig.

## INTERLOCK (Punkt 24) — TAXO2-Plan-04 (HARTES Akzeptanzkriterium)
rubric_score erbt hier die NOT-NULL-Härte. Plan-04 (`rubric_score`-Write im Daemon) MUSS den
EINEN Daemon-GUC-Vertrag aus Plan 03 (A1-`set_current_tenant`-Klammer) nutzen. **Interlock-Grep
gegen `08.23.2.TAXO2-04-coaching-score-cutover-async-PLAN.md` (2026-06-25):** Plan-04 nutzt
`set_current_tenant(str(call.tenant_id))` + `clear_current_tenant()` im `finally` (dieselben
db.py-Helfer), explizit „KEINE neue Mechanik — der Daemon nutzt die bestehende"; **kein
A2-rollback-Pfad, kein eigener set_config**. → KEINE Divergenz, kein Doppel-Bau. Forward-Note für
die Plan-04-Ausführung: die A1-PRÄZISION (Read-Session geschlossen VOR set_current_tenant) ist beim
Plan-04-Bau einzuhalten (Plan-04-Prosa beschreibt „call.tenant_id zuerst lesen, dann set" — der
Plan-04-Executor muss sicherstellen, dass die committende Write-TX ERST nach set_current_tenant
beginnt, exakt wie die A1-Form aus Plan 03).

## DoD (Cross-AI Process #4a)
- **V-TF-6** (rubric_score NOT NULL), **V-TF-7** (suggestion_reactions flush regression-frei),
  **V-TF-8** (Schild-Guard, alle drei tenant_id-Schilder ≥10 Zeichen, models.py == COMMENT):
  Code gebaut, VALIDATION.md auf `🔨 built (pending deploy-gate)`. Flip auf ✅ green durch André
  nach grünem Gate (`test_rubric_score_schema.py` + `test_schild_guard.py` + suggestion-Tests).

## Latenz (Punkt 25)
0 ms Live-Bahn. suggestion_reactions-Flush am Call-ENDE (Request-Kontext, kein Live-Loop-Write).
RLS-Policies = set_config-basiert (sub-ms TX-local SET).

## Offen / Risiken
- T-TF2-01/02 (HIGH, DSGVO-Kern) fail-closed-Beweis kommt aus Plan 03 (M-4-Test). **KEIN Deploy
  von W2 ohne W3 im selben Deploy** (sonst abstain_log FORCE RLS ohne Daemon-A1-Klammer → lautloser
  Write-Verlust). Auf main liegt zudem der wartende TAXO2-Plan-03 — dieser Deploy aktiviert beide
  zusammen (gewollt).
