---
phase: 08.23.2.TENANT-FOUND
plan: 01
type: execute
wave: 1
complexity: "🔴"
depends_on: []
files_modified:
  - database/db.py
  - services/live_session.py
  - alembic/versions/0023_backfill_calls_tenant_id.py
  - tests/test_tenant_resolution.py
autonomous: true
requirements: [TF-1]
user_setup: []

must_haves:
  truths:
    - "Eine neu via create_call_for_sid angelegte Call-Row hat tenant_id == resolve(user_id) (nicht NULL fuer auflösbare User)"
    - "resolve_tenant_uuid_for_user(user_id, db) gibt die tenant_orgs.id (UUID-str) zurück, abgeleitet user->org->tenant_orgs, None bei nicht-auflösbarem Edge"
    - "Backfill-Migration 0023 setzt für ALLE auflösbaren NULL-Tenant-Calls den korrekten tenant_id (37/37 prod-belegt)"
    - "Der Backfill ist idempotent: zweite Ausführung ändert 0 Rows (WHERE tenant_id IS NULL)"
    - "calls.tenant_id bleibt NULLABLE (kein NOT NULL, kein FK diese Runde — Live-Anlage-Schutz)"
  artifacts:
    - path: "database/db.py"
      provides: "resolve_tenant_uuid_for_user Helper (single source der user->tenant-Auflösung)"
      contains: "def resolve_tenant_uuid_for_user"
    - path: "services/live_session.py"
      provides: "create_call_for_sid setzt tenant_id beim Insert"
      contains: "tenant_id=_tid"
    - path: "alembic/versions/0023_backfill_calls_tenant_id.py"
      provides: "idempotente set-based Backfill-Migration, down_revision=0022"
      contains: "down_revision = '0022'"
  key_links:
    - from: "services/live_session.py create_call_for_sid"
      to: "database/db.py resolve_tenant_uuid_for_user"
      via: "import + Aufruf VOR Call(...)"
      pattern: "resolve_tenant_uuid_for_user\\(user_id"
    - from: "alembic 0023 UPDATE calls"
      to: "users JOIN tenant_orgs"
      via: "set-based UPDATE FROM, WHERE tenant_id IS NULL"
      pattern: "SET tenant_id = t.id"
---

<objective>
Schließt die Anlage-Lücke: `calls.tenant_id` wird bei Call-Anlage NIE gesetzt (prod 2026-06-25: 37/52 NULL). Diese Welle (1) baut den kontext-unabhängigen Helper `resolve_tenant_uuid_for_user(user_id, db)` in `database/db.py`, (2) lässt `create_call_for_sid` (Deepgram-Callback-Thread, KEIN Request-Kontext) den Tenant aus `user_id` per DB-Query auflösen und setzen, (3) liefert die idempotente Backfill-Migration `0023` für die 37 bestehenden NULL-Calls + ein Verifikations-Runbook.

Locked decisions (RESEARCH §3/§6, adoptiert):
- Tenant wird aus `user_id` per DB-Query aufgelöst, NICHT aus `g.tenant_id` (im Callback-Thread nicht verfügbar).
- Backfill = idempotenter set-based UPDATE (Revision 0023). KEIN Default-Tenant (DSGVO-Leck) — fail-closed by construction: nur was eindeutig per Join auflöst wird gesetzt, Rest bleibt NULL.
- `calls.tenant_id` bleibt NULLABLE diese Runde (NOT NULL + FK = Phase F). NOT-NULL-Härte gehört auf die Bewertungs-Kinder (Plan 02), nicht auf calls.

Purpose: Ohne gesetztes `calls.tenant_id` liefert `_tenant_id_for` (slow_lane.py:166-167) None → die in Plan 02 ge-RLS-ten Kinder schreiben fail-closed nichts. Diese Welle ist das Fundament, auf dem W2/W3 stehen.
Output: db.py-Helper + create_call_for_sid-Patch + 0023-Migration + Resolution/Idempotenz-Tests.

Latenz-Budget (Punkt 25): 0 ms auf der Live-Bahn. Die Resolution läuft 1×/Call bei Anlage im Deepgram-Callback-Thread (indizierter Join: tenant_orgs.legacy_org_id UNIQUE + users.id PK → sub-ms), NICHT pro Transkript-Chunk, NICHT im EWB/QA/Slot-A/B-Antwort-Pfad. Backfill = einmalig beim Deploy (37 Rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

PRE-EXECUTE (vor jeglichem Bau, durch Executor/André):
- Alembic-head erneut prüfen: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` MUSS `0021` zeigen (RESEARCH §5). Falls 0022 zwischenzeitlich deployt: STOP, 0023-down_revision neu bewerten.
- Multi-Segment-Gotcha: Pfade hardcoden, gsd-tools/gsd-code-review/gsd-verifier umgehen, ROADMAP/STATE hand-editieren.
- HART: kein Local-Dev, kein lokales pytest als Acceptance. Verify-Sektionen laufen server-seitig über `deploy.sh production`-Gate / SSH. Code-LESEN lokal OK (git-sync).
</execution_context>

<context>
@.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-RESEARCH.md
@.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-VALIDATION.md

<interfaces>
<!-- Verträge aus dem Codebase, vom Executor direkt nutzbar — KEINE Codebase-Exploration nötig. -->

Bestehende Auflösung (routes/auth.py:114-123, Login-Pfad, Request-Kontext, hat user_org_id schon):
```python
from database.models import TenantOrg as _TenantOrg
_to = db.query(_TenantOrg).filter_by(legacy_org_id=user_org_id).first()
if _to is not None:
    tenant_id_str = str(_to.id)
```

Tenant-Kette (RESEARCH §1.1, prod-belegt):
- User.org_id (models.py:69, Integer, ForeignKey organisations.id, NOT NULL)
- TenantOrg.id (models.py:938, UUID PK = DIE tenant_id)
- TenantOrg.legacy_org_id (models.py:939, Integer FK organisations.id, NOT NULL, UNIQUE)
→ 1:1, genau eine tenant_id pro Org. 0 Users ohne Org, 0 Orgs ohne tenant_org (prod §6).

create_call_for_sid Insert-Stelle (services/live_session.py:566-571, AKTUELL ohne tenant_id):
```python
call = Call(
    user_id=user_id,
    call_mode=_db_mode,
    started_at=datetime.now(_tz.utc),
    transcript_storage='none',
)
_db.add(call)
```
Aufrufer = services/deepgram_service.py:712 (Deepgram-Callback-Thread, KEIN Flask-Request; g.tenant_id NICHT verfügbar). user_id ist IMMER gesetzt (handle_connect rejectet unauth, deepgram_service.py:696).

db.py-Anker (database/db.py:54-60): set_current_tenant/clear_current_tenant + after_begin-Hook liegen hier. Der neue Helper kommt daneben.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_tenant_uuid_for_user Helper in db.py + Resolution-Test</name>
  <files>database/db.py, tests/test_tenant_resolution.py</files>
  <read_first>
    - database/db.py:42-110 (set_current_tenant/after_begin-Block — der Helper kommt daneben; SessionLocal/get_session-Muster)
    - RESEARCH.md §1.1 (Tenant-Kette user->org->tenant_orgs, prod-belegt) + §3 (Helper-Signatur + One-Query-Join)
    - routes/auth.py:114-123 (bestehende legacy_org_id->tenant-Auflösung, Anti-Drift-Referenz)
    - database/models.py:65-76 (User.org_id) + 932-940 (TenantOrg.id/legacy_org_id)
    - tests/conftest.py:36-57 (TEST_TENANT_UUID-Seed) + 669-724 (db_session-Fixture, skip ohne TEST_DATABASE_URL)
  </read_first>
  <behavior>
    - resolve_tenant_uuid_for_user(<user_id eines Users mit tenant_org>, db) gibt str(tenant_orgs.id) zurück (== TEST_TENANT_UUID für den geseedeten Test-User)
    - resolve_tenant_uuid_for_user(<user_id ohne existierenden User / ohne tenant_org>, db) gibt None zurück (kein Crash, kein Default)
    - Genau EINE Query (Join), kein N+1
  </behavior>
  <action>
    In `database/db.py`, NACH dem after_begin-Block (nach Z.89), neuen Helper anlegen — EINE Join-Query (RESEARCH §3, verbatim Muster):
    ```python
    def resolve_tenant_uuid_for_user(user_id, db):
        """Löst die Mandanten-UUID kontext-unabhängig aus user_id auf:
        user_id -> users.org_id -> tenant_orgs.legacy_org_id -> tenant_orgs.id (UUID-str).
        Single Source der user->tenant-Auflösung (Anti-Drift, Punkt 22; ersetzt die
        eingebettete auth.py:117-Logik konzeptionell). Im Deepgram-Callback-Thread nutzbar
        (kein Request-Kontext, kein g). None bei nicht-auflösbarem Edge (heute 0 Fälle,
        prod §6) -> Aufrufer schreibt tenant_id NULL (fail-closed, KEIN Default-Tenant)."""
        from database.models import TenantOrg, User
        row = (db.query(TenantOrg.id)
                 .join(User, User.org_id == TenantOrg.legacy_org_id)
                 .filter(User.id == user_id).first())
        return str(row[0]) if row else None
    ```
    Import von TenantOrg/User INNERHALB der Funktion (db.py importiert models nicht auf Modul-Ebene → Zirkular-Schutz, Hausmuster).

    In `tests/test_tenant_resolution.py` (neu): Resolution-Test gegen echtes PG. Den geseedeten Test-User (org -> TEST_TENANT_UUID) auflösen lassen → erwarte TEST_TENANT_UUID; einen nicht-existenten user_id (z.B. 99999999) → erwarte None. Nutze die `db_session`-Fixture (skippt ohne TEST_DATABASE_URL → kein False-Green; SQLite-Schutz wie test_rubric_score_schema.py). Den seeded-User aus der db_session-Baseline ziehen (conftest seedet org+tenant). Falls kein Baseline-User mit tenant existiert: einen Test-User in der TEST_TENANT_UUID-Org anlegen, am Ende via cleanup_rows wegräumen (Test-Cleanup-Regel). KEINE Source-Presence-Assertion (CLAUDE.md Test-Qualitäts-Regel) — echte Funktions-Rückgabe asserten.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_tenant_resolution.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `database/db.py` enthält `def resolve_tenant_uuid_for_user(user_id, db):`
    - Die Query joint `User.org_id == TenantOrg.legacy_org_id` und filtert `User.id == user_id` (grep: `User.org_id == TenantOrg.legacy_org_id`)
    - Funktion gibt `str(row[0]) if row else None` zurück (KEIN Default-UUID, KEIN raise bei None)
    - `tests/test_tenant_resolution.py` enthält eine Assertion `== conftest.TEST_TENANT_UUID` (Positiv) UND eine Assertion `is None` (Negativ)
    - Server-seitiger pytest grün (oder skip wenn DSN fehlt — dann im SUMMARY als skip vermerkt, nicht als pass)
  </acceptance_criteria>
  <done>Helper existiert + ist single-source; Resolution-Test (Positiv + Negativ) grün server-seitig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: create_call_for_sid setzt tenant_id beim Insert</name>
  <files>services/live_session.py</files>
  <read_first>
    - services/live_session.py:529-623 (create_call_for_sid, der Insert Z.566-571 + Thread-Kontext)
    - services/deepgram_service.py:690-718 (Aufrufer Z.712 — Callback-Thread, user_id immer gesetzt Z.696)
    - RESEARCH.md §3 (Write-Path, Mechanismus, „calls hat keine RLS → kein GUC nötig, nur der aufgelöste Wert")
    - database/db.py (resolve_tenant_uuid_for_user aus Task 1)
  </read_first>
  <behavior>
    - Nach create_call_for_sid für einen User mit tenant_org: die geschriebene calls-Row hat tenant_id == resolve_tenant_uuid_for_user(user_id) (V-TF-1)
    - Bei nicht-auflösbarem user_id (Edge): tenant_id bleibt NULL, Call-Anlage bricht NICHT (Live-Schutz)
  </behavior>
  <action>
    In `services/live_session.py`, innerhalb des try-Blocks (nach Z.564 `_db = _SL()`, VOR `call = Call(...)` Z.566): Tenant auflösen und in den Insert hängen (RESEARCH §3):
    ```python
    from database.db import resolve_tenant_uuid_for_user
    _tid = resolve_tenant_uuid_for_user(user_id, _db)   # user->org->tenant_orgs.id; None bei Edge
    call = Call(
        user_id=user_id,
        tenant_id=_tid,
        call_mode=_db_mode,
        started_at=datetime.now(_tz.utc),
        transcript_storage='none',
    )
    ```
    WICHTIG (RESEARCH §3): `calls` hat KEINE RLS (pg_class §1.3) → dieser INSERT braucht KEINEN set_current_tenant/GUC, nur den aufgelösten Wert. Den Resolve-Call NICHT fail-hard machen: gibt der Helper None zurück, wird tenant_id=NULL geschrieben (kein Crash der Call-Anlage). Bestehende Fehler-/Sentinel-Behandlung (Z.610-622) unverändert lassen.
    Latenz-Notiz im Code-Kommentar: 1× indizierter Join bei Call-Anlage, nicht im Live-Antwort-Pfad (Punkt 25).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_tenant_resolution.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `services/live_session.py` enthält `tenant_id=_tid` im `Call(...)`-Insert (grep: `tenant_id=_tid`)
    - `resolve_tenant_uuid_for_user(user_id, _db)` wird VOR `call = Call(` aufgerufen (grep: `resolve_tenant_uuid_for_user(user_id, _db)`)
    - KEIN `set_current_tenant` in create_call_for_sid (calls hat keine RLS — grep darf 0 Treffer für `set_current_tenant` in der Funktion liefern)
    - Resolution-Test grün server-seitig (Anlage-Integration deckt V-TF-1 in der Manual-Only-Verification ab, da der echte Callback-Thread Live-Daemon braucht)
  </acceptance_criteria>
  <done>create_call_for_sid setzt tenant_id aus user_id; Call-Anlage robust bei Edge (None → NULL, kein Crash).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backfill-Migration 0023 + Idempotenz-Test + Verifikations-Runbook</name>
  <files>alembic/versions/0023_backfill_calls_tenant_id.py, tests/test_tenant_resolution.py</files>
  <read_first>
    - alembic/versions/0022_abstain_log.py (Revisions-Muster, down_revision-Kette; 0023 hängt an 0022)
    - RESEARCH.md §5 (Backfill-UPDATE verbatim, Revisions-Reihenfolge 0022->0023->0024) + §6 (Backfill-Design, fail-closed, 37/37 auflösbar, Verifikations-Runbook)
    - RESEARCH.md §1.1 (tenant_orgs/users-Schema für den Join)
  </read_first>
  <behavior>
    - upgrade() setzt für alle calls mit tenant_id IS NULL den korrekten tenant via user->org->tenant_orgs-Join (V-TF-2)
    - Zweiter upgrade()-Lauf ändert 0 Rows (idempotent, WHERE tenant_id IS NULL)
    - Nicht-auflösbare Calls (kein Join-Match) bleiben NULL (KEIN Default-Tenant)
  </behavior>
  <action>
    Neue Migration `alembic/versions/0023_backfill_calls_tenant_id.py`, `revision='0023'`, `down_revision='0022'` (RESEARCH §5). Set-based, idempotent (verbatim RESEARCH §5):
    ```python
    def upgrade() -> None:
        # Idempotenter set-based Backfill: nur NULL-Tenant-Calls, eindeutig per Join aufgelöst.
        # fail-closed by construction — KEIN Default-Tenant (DSGVO: ein Default würde fremde
        # Calls unter einen falschen Mandanten ziehen). Nicht-auflösbare bleiben NULL. (§6)
        op.execute("""
            UPDATE calls c
            SET tenant_id = t.id
            FROM users u JOIN tenant_orgs t ON t.legacy_org_id = u.org_id
            WHERE c.user_id = u.id AND c.tenant_id IS NULL
        """)

    def downgrade() -> None:
        # Daten-verlust-frei: tenant_id ist aus user_id rekonstruierbar. No-Op (reversibel-trivial).
        pass
    ```
    KEIN Schema-DDL hier (kein ALTER, kein RLS — reine Daten-Migration). calls.tenant_id bleibt nullable (Plan-Scope, §6).

    Idempotenz-Test in `tests/test_tenant_resolution.py` ergänzen: einen calls-Row mit tenant_id=NULL (user mit tenant_org) anlegen → den UPDATE-Statement-Body 2× ausführen → nach Lauf 1 ist tenant_id gesetzt (== erwarteter Tenant), Lauf 2 ändert nichts (rowcount 0 ODER tenant_id unverändert). Test-eigene Rows via cleanup_rows wegräumen (Test-Cleanup-Regel). Echte DB-Row-Assertion (KEIN Source-Presence).

    Verifikations-Runbook (im SUMMARY zu belegen, NICHT als Task ausführen — supervised deploy): nach 0023 server-seitig
    `ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh count calls'` + psql
    `SELECT count(*) FROM calls WHERE tenant_id IS NULL` → erwartet 0 (alle 37 auflösbar, §6).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && TEST_DATABASE_URL=$NERVE_TEST_DSN python -m pytest tests/test_tenant_resolution.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/versions/0023_backfill_calls_tenant_id.py` existiert mit `revision = '0023'` UND `down_revision = '0022'`
    - upgrade() enthält `SET tenant_id = t.id` und `WHERE c.user_id = u.id AND c.tenant_id IS NULL` (grep beide)
    - KEIN `ALTER TABLE`, KEIN `NOT NULL`, KEIN `ROW LEVEL SECURITY` in 0023 (grep: 0 Treffer — reine Daten-Migration)
    - downgrade() ist no-op (`pass`)
    - Idempotenz-Test in test_tenant_resolution.py: UPDATE 2× → zweiter Lauf rowcount 0 oder tenant_id unverändert (echte Row-Assertion)
    - Server-seitiger pytest grün
  </acceptance_criteria>
  <done>0023 idempotent + fail-closed; Idempotenz-Test grün; Runbook im SUMMARY dokumentiert (V-TF-2).</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `calls` (SCHREIBEN: tenant_id beim Insert + Backfill) — nullable, KEIN FK diese Runde
- `users` (LESEN: org_id für Join) — keine Änderung
- `tenant_orgs` (LESEN: id/legacy_org_id für Join) — keine Änderung
- `organisations` (implizit über die FK-Kette) — keine Änderung

### inspect.sh-/psql-Beleg (verbatim aus RESEARCH §1.1 + §2, prod 2026-06-25 — NICHT zusammengefasst)

`calls` (RESEARCH §2, inspect.sh schema calls):
```
        Column         |            Type             | Nullable |    Default
-----------------------+-----------------------------+----------+---------------
 id                    | uuid                        | not null |
 tenant_id             | uuid                        |          |          ← NULLABLE, KEIN FK (Phase F)
 user_id               | integer                     | not null |          ← FK users(id), IMMER da
 call_mode             | text                        | not null |
Foreign-key constraints:
    "calls_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id)
```

`tenant_orgs` (RESEARCH §1.1, psql als postgres):
```
    Column     |           Type           | Nullable |      Default
---------------+--------------------------+----------+-------------------
 id            | uuid                     | not null | gen_random_uuid()
 legacy_org_id | integer                  | not null |
Indexes:
    "tenant_orgs_pkey" PRIMARY KEY, btree (id)
    "tenant_orgs_legacy_org_id_key" UNIQUE CONSTRAINT, btree (legacy_org_id)
Foreign-key constraints:
    "tenant_orgs_legacy_org_id_fkey" FOREIGN KEY (legacy_org_id) REFERENCES organisations(id)
```

`users.org_id` (RESEARCH §1.1, models.py:69): `Integer, ForeignKey('organisations.id'), nullable=False` — 0 Users ohne Org (§6).

Backfill-Resolvability (RESEARCH §6, psql LEFT JOINs prod 2026-06-25):
```
 null_calls | user_exists | org_exists | tenant_resolvable
------------+-------------+------------+-------------------
         37 |          37 |         37 |                37
 users_no_org | orgs_no_tenant | total_orgs | total_tenant_orgs
--------------+----------------+------------+-------------------
            0 |              0 |          4 |                 4
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `_tid` (resolve-Ergebnis) | live_session.py create_call_for_sid (geschrieben) | DB-Spalte `calls.tenant_id` (UUID, nullable) | ✓ Schema oben — existiert, wird jetzt gesetzt |
| `user_id` (Param) | deepgram_service.py:712 übergibt | DB-Spalte `calls.user_id` (int, NOT NULL, FK) | ✓ immer gesetzt (§2) |
| `User.org_id` | resolve-Join (gelesen) | DB-Spalte `users.org_id` (int, NOT NULL, FK) | ✓ models.py:69 + §6 (0 ohne Org) |
| `TenantOrg.id` | resolve-Ergebnis (gelesen) | DB-Spalte `tenant_orgs.id` (UUID PK) | ✓ psql §1.1 |
| `TenantOrg.legacy_org_id` | resolve-Join (gelesen) | DB-Spalte `tenant_orgs.legacy_org_id` (int, UNIQUE) | ✓ psql §1.1 |
| `g.tenant_id` | NICHT genutzt hier | Flask-`g` (RAM, per-Request) — im Callback-Thread NICHT verfügbar | ✓ Cross-Naming-Falle bewusst umgangen (§2) |

**Cross-Variable-Naming-Falle-Check:** `g.tenant_id` sieht aus wie eine DB-Spalte, ist aber RAM-only (Flask-g). Der Callback-Thread (kein Request) hat kein `g` → der Tenant MUSS aus der DB via `user_id` aufgelöst werden, NICHT aus `g`. ✓ Genau das tut der Helper.

## Verbindungs-Karte (Punkt 22)

| Datum | SCHREIBT (file:line, Thread) | LIEST | Beleg |
|-------|------------------------------|-------|-------|
| `calls.tenant_id` | NEU: create_call_for_sid live_session.py:566 (Deepgram-Callback-Thread) + Backfill 0023 | `_tenant_id_for` slow_lane.py:166-167; auth/dashboard | RESEARCH §1.2 grep-Tabelle |
| `resolve_tenant_uuid_for_user` | NEU db.py (single source) | create_call_for_sid + Backfill (gleiche Quelle, Anti-Drift) | RESEARCH §1.1 (kein zentraler Helper existiert heute) |

grep-Beleg dass keine andere user->tenant-Auflösung lebt (RESEARCH §1.1):
```
database/models.py:936/939   (TenantOrg-Definition)
routes/auth.py:117           (legacy_org_id-Lookup, hat user_org_id schon)
services/slow_lane.py:161    (_tenant_id_for: liest call.tenant_id, NICHT user->org)
```

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deepgram-Callback-Thread → DB | Tenant-Zuordnung außerhalb Request-Kontext; falscher/fehlender Tenant = Cross-Tenant-Fehlattribution |
| Backfill-Migration → bestehende calls | Massen-UPDATE der Mandanten-Zuordnung auf Produktiv-Daten (DSGVO) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TF1-01 | Information Disclosure / Tampering | Backfill 0023 setzt falschen Tenant → Cross-Tenant-Leck | mitigate | NUR eindeutiger user->org->tenant_org-Join setzt; KEIN Default-Tenant; 37/37-Resolvability prod-belegt (§6); Verifikations-Runbook (count NULL == erwartet) im SUMMARY |
| T-TF1-02 | Denial of Service | resolve wirft Exception → Call-Anlage bricht (Live-Pfad) | mitigate | Helper gibt None statt raise bei Edge; tenant_id=NULL geschrieben, Call-Anlage robust; calls.tenant_id bleibt NULLABLE (kein NOT NULL-Bruch) |
| T-TF1-03 | Tampering | Backfill nicht idempotent → Doppel-/Fehl-Setzung bei Re-Run | mitigate | `WHERE tenant_id IS NULL` macht Re-Run zum No-Op; Idempotenz-Test (UPDATE 2×) beweist 0 Rows zweiter Lauf |
| T-TF1-04 | Information Disclosure | Default-Tenant für unauflösbare Calls (DSGVO-Leck) | accept→reject | Default-Tenant EXPLIZIT verworfen (§6); unauflösbar bleibt NULL (für RLS-Kinder unsichtbar = fail-closed korrekt) |

Keine HIGH-Severity offen: T-TF1-01 (das gefährlichste) ist durch eindeutigen Join + No-Default + prod-Resolvability-Beleg + Runbook gemindert.
</threat_model>

<verification>
- resolve_tenant_uuid_for_user: Positiv (== TEST_TENANT_UUID) + Negativ (None) grün (V-TF-1-Basis).
- 0023 idempotent (UPDATE 2× → zweiter Lauf 0 Rows) (V-TF-2).
- Manual-Only (supervised deploy, RESEARCH §6 / VALIDATION Manual): nach 0023 `SELECT count(*) FROM calls WHERE tenant_id IS NULL` == 0 (auflösbare).
- calls.tenant_id bleibt nullable (grep: kein NOT NULL/FK in dieser Welle).
</verification>

<success_criteria>
- create_call_for_sid setzt tenant_id aus user_id (kontext-unabhängig).
- 0023 Backfill idempotent + fail-closed (kein Default-Tenant).
- W2 kann auf gesetztes calls.tenant_id bauen (_tenant_id_for liefert nicht-NULL).
- 0 ms Live-Latenz-Impact (Punkt 25).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TENANT-FOUND-mandanten-kennung-fundament-rls-schloss/08.23.2.TENANT-FOUND-01-SUMMARY.md`. Backfill-Verifikations-Runbook-Ergebnis (count NULL nach Deploy) dort belegen.
</output>
