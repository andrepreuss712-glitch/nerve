---
phase: 08.23.2.TENANT-FOUND
slug: mandanten-kennung-fundament-rls-schloss
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-25
---

# Phase 08.23.2.TENANT-FOUND — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from RESEARCH.md §10 (Validation Architecture). HART: tests run server-side against real Postgres `nerve_test` via `deploy.sh production` gate — NO local pytest.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (server-side against real Postgres `nerve_test`, PGTEST gate) |
| **Config file** | `tests/conftest.py` (TEST_TENANT_UUID seed, `cleanup_rows`, `db_session` fixture with GUC) |
| **Quick run command** | `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && <pytest target>'` (server-side only) |
| **Full suite command** | `bash deploy.sh production` (pytest gate runs on prod server BEFORE restart; red → no restart) |
| **Estimated runtime** | full gate ~ minutes (SSH roundtrip) |

> RLS tests SKIP without `TEST_DATABASE_URL` (SQLite has no RLS → no false-green). This is by design.

---

## Sampling Rate

- **After every task commit:** server-side pytest of the affected test file (no local run)
- **After every plan wave:** full `deploy.sh production` gate (supervised deploy per ROADMAP — André drives)
- **Before `/gsd-verify-work`:** full suite green on prod gate + Schild-Guard green
- **Max feedback latency:** SSH roundtrip bound (supervised, not autonomous)

---

## Per-Task Verification Map (falsifiable points — RESEARCH §10)

| ID | Claim | Test Type | Automated Command / Proof | File Exists | Status |
|----|-------|-----------|---------------------------|-------------|--------|
| V-TF-1 | New calls get tenant_id at creation | integration: `create_call_for_sid` → `row.tenant_id == resolve(user)` | `test_tenant_resolution.py` (resolution) + create_call_for_sid patch | ✅ built | 🔨 built (pending deploy-gate) |
| V-TF-2 | Backfill idempotent + sets all resolvable | integration: UPDATE 2× → same count; `count(tenant_id IS NULL)` == expected | `test_tenant_resolution.py` idempotency + prod runbook (SUMMARY 01) | ✅ built | 🔨 built (pending deploy-gate) |
| V-TF-3 | `abstain_log` RLS fail-closed without GUC | M-4 negative: `clear_current_tenant` → INSERT rejected (raises) | `test_abstain_log_rls_requires_tenant_guc` (neg leg) | ✅ built | 🔨 built (pending deploy-gate) |
| V-TF-4 | `abstain_log` writable WITH GUC | M-4 positive: `set_current_tenant` → INSERT succeeds + readable | (pos leg) + proof neg row absent | ✅ built | 🔨 built (pending deploy-gate) |
| V-TF-5 | Daemon sets GUC before abstain_log write | integration: `_persist_event_ref` abstain path with RLS table → row written | `test_slow_lane_tenant_guc.py` (seeded `calls.tenant_id`, A1 clamp) | ✅ built | 🔨 built (pending deploy-gate) |
| V-TF-6 | `rubric_score.tenant_id` NOT NULL enforced | schema: `inspect.sh schema rubric_score` shows `not null`; INSERT NULL → reject | 0024 + `test_rubric_score_schema.py` + SUMMARY proof | ✅ built | 🔨 built (pending deploy-gate) |
| V-TF-7 | `suggestion_reactions` unchanged functional (request flush) | existing flush test green (regression) | `app_routes` flush path (fail-closed skip) | ✅ existing | 🔨 built (pending deploy-gate) |
| V-TF-8 | Schild-Guard green (abstain_log/rubric_score tenant_id Schild ≥10 chars) | server-side `test_schild_guard.py` | pg_description | ✅ existing | 🔨 built (pending deploy-gate) |

*Status: ⬜ pending · 🔨 built (code+tests done, server-side deploy-gate run pending — André flips to ✅ after green) · ✅ green · ❌ red · ⚠️ flaky*

> **W1→W3 code-complete + pushed 2026-06-25 (NICHT deployed).** HART: kein lokales pytest als
> Acceptance — alle V-TF-Tests laufen scharf erst im `deploy.sh production`-Pytest-Gate (server-seitig
> gegen `nerve_test`). André/Claudian fahren den beaufsichtigten Deploy (Migrationen 0022-edit→0023→0024
> als postgres VOR dem Restart; head==0021 unmittelbar vorher prüfen). Erst nach grünem Gate + Backfill-
> Runbook (SUMMARY 01) → Status auf ✅ green.

---

## Wave 0 Requirements

- [ ] `tests/test_abstain_log_rls_requires_tenant_guc` — new M-4 test (pattern: `tests/test_rubric_score_schema.py:195-269`)
- [ ] Slow-Lane integration test: abstain path writes `abstain_log` WITH set GUC (seeded `calls.tenant_id`)
- [ ] Backfill idempotency test (set-based UPDATE 2×)
- *(rubric_score M-4 test already exists: `test_rubric_score_schema.py:195`)*

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Prod backfill ran + 0 remaining resolvable NULL | supervised deploy, prod-only data | After 0023 migration: `inspect.sh count calls` then psql `SELECT count(*) FROM calls WHERE tenant_id IS NULL` + join-resolvability check |
| Test call end-to-end through slow lane writes abstain_log | needs live daemon + real call | Supervised test-user call → confirm abstain_log row with tenant_id set |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify (server-side) or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (V-TF-1/2/3/4/5)
- [ ] No watch-mode flags
- [ ] M-4 negative leg proves fail-closed (not just positive leg)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
