---
phase: 08.23.2.C.R
plan: "07"
type: execute
wave: 4
depends_on:
  - "08.23.2.C.R-04"
  - "08.23.2.C.R-05"
  - "08.23.2.C.R-06"
files_modified:
  - tests/test_gatekeeper_classifier.py
  - tests/test_hysteresis.py
  - tests/test_phase_classifier.py
  - tests/test_session_state_phase_c.py
autonomous: true
requirements:
  - C.R-REQ-1
  - C.R-REQ-2
  - C.R-REQ-5
  - C.R-REQ-6

must_haves:
  truths:
    - "tests/test_gatekeeper_classifier.py ist gelöscht"
    - "tests/test_hysteresis.py ist gelöscht"
    - "tests/test_phase_classifier.py: apply_hysteresis Import + test_call_events_phase_change_persisted entfernt"
    - "pytest läuft ohne ImportError durch"
    - "test_live_session_gatekeeper.py: 3 Tests GRÜN"
    - "Alle 8 Acceptance-Greps aus SPEC.md sauber"
  artifacts:
    - path: "tests/test_phase_classifier.py"
      provides: "Bereinigter Phase-Classifier-Test ohne apply_hysteresis"
  key_links:
    - from: "pytest tests/"
      to: "0 ImportErrors"
      via: "gelöschte Testdateien + bereinigte Imports"
      pattern: "passed.*0 error"
---

<objective>
Test-Cleanup: test_gatekeeper_classifier.py + test_hysteresis.py vollständig löschen. test_phase_classifier.py partiell bereinigen. Anschließend alle Acceptance-Greps aus SPEC.md verifizieren und pytest-Suite durchlaufen lassen.

Purpose: Gelöschte Funktionen dürfen keine Testdateien mehr haben — ImportErrors blockieren die gesamte pytest-Suite. CLAUDE.md Punkt 3 Pruning-Pflicht. Nach Test-Cleanup: Phase-Gate-Verifikation aller SPEC.md Acceptance Criteria.

Output: Bereinigte Test-Suite + alle Acceptance-Greps grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-RESEARCH.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-04-SUMMARY.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-05-SUMMARY.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-06-SUMMARY.md

<interfaces>
<!-- Test-Cleanup aus RESEARCH.md: -->
<!-- tests/test_gatekeeper_classifier.py → KOMPLETT LÖSCHEN (classify_contact, detect_trigger_phrases, detect_uwg_hard_block) -->
<!-- tests/test_hysteresis.py            → KOMPLETT LÖSCHEN (apply_hysteresis) -->
<!-- tests/test_phase_classifier.py      → PARTIELL: apply_hysteresis Import(s) löschen + test_call_events_phase_change_persisted() löschen -->
<!-- tests/test_session_state_phase_c.py → PRÜFEN: uwg_blocked-Tests identifizieren + entfernen wenn vorhanden -->

<!-- CLAUDE.md Test-Qualitäts-Regel: Source-Presence-Tests verboten: -->
<!-- inspect.getsource + assert 'string' in src → LÖSCHEN -->
<!-- open('datei.py').read() + assert 'string' in src → LÖSCHEN -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testdateien löschen + partiell bereinigen</name>
  <files>tests/test_gatekeeper_classifier.py, tests/test_hysteresis.py, tests/test_phase_classifier.py, tests/test_session_state_phase_c.py</files>

  <read_first>
    - tests/test_gatekeeper_classifier.py — vollständig lesen (Import-Bestätigung)
    - tests/test_hysteresis.py — vollständig lesen
    - tests/test_phase_classifier.py — vollständig lesen (apply_hysteresis Stellen + test_call_events_phase_change_persisted finden)
    - tests/test_session_state_phase_c.py — vollständig lesen (uwg_blocked-Referenzen identifizieren)
  </read_first>

  <action>
**Schritt 1: test_gatekeeper_classifier.py löschen:**
`git rm tests/test_gatekeeper_classifier.py`
Testet classify_contact, detect_trigger_phrases, detect_uwg_hard_block — alle in Phase C.R gelöscht.

**Schritt 2: test_hysteresis.py löschen:**
`git rm tests/test_hysteresis.py`
Testet apply_hysteresis — in Phase C.R gelöscht.

**Schritt 3: test_phase_classifier.py partiell bereinigen:**
Durch Lesen der Datei exakt identifizieren:
- `from services.gatekeeper import apply_hysteresis` Import-Zeile löschen (oder den apply_hysteresis-Teil aus dem Import entfernen falls grouped)
- `test_call_events_phase_change_persisted()` Testfunktion vollständig löschen
- Restliche Testfunktionen (Phase-Classifier-Tests) UNVERÄNDERT lassen
- Falls nach Bereinigung noch Source-Presence-Tests existieren (inspect.getsource etc.) → ebenfalls löschen

**Schritt 4: test_session_state_phase_c.py prüfen und bereinigen:**
`grep -n "uwg_blocked" tests/test_session_state_phase_c.py` ausführen.
Falls Treffer:
- Tests die `'uwg_blocked' in state` als True prüfen → löschen (Dead-State-Key)
- Tests die `'uwg_blocked' not in state` prüfen → behalten (validiert C.R-Cleanup)
Falls keine Treffer: Datei unverändert lassen.

**Schritt 5: pytest-Smoke-Run:**
`pytest tests/ -x -q --ignore=tests/test_anonymization_reid.py 2>&1 | head -30`
Neue Failures (nicht in pre-existing Liste) sofort debuggen.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -m pytest tests/test_live_session_gatekeeper.py tests/test_mode_switch_event.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `test -f tests/test_gatekeeper_classifier.py` → Exit 1 (Datei nicht vorhanden)
    - `test -f tests/test_hysteresis.py` → Exit 1 (Datei nicht vorhanden)
    - `grep -n "apply_hysteresis" tests/test_phase_classifier.py` → 0 Treffer
    - `grep -n "test_call_events_phase_change_persisted" tests/test_phase_classifier.py` → 0 Treffer
    - pytest läuft ohne ERROR-Zeile die auf ImportError für gelöschte Funktionen hinweist
    - tests/test_live_session_gatekeeper.py: `pytest tests/test_live_session_gatekeeper.py -v` → alle 3 Tests PASSED
  </acceptance_criteria>

  <done>Zwei Testdateien gelöscht, test_phase_classifier.py bereinigt, pytest-Suite importiert ohne Fehler.</done>
</task>

<task type="auto">
  <name>Task 2: Alle SPEC.md Acceptance-Greps verifizieren</name>
  <files></files>

  <read_first>
    - .planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md — Acceptance Criteria Sektion lesen
  </read_first>

  <action>
Alle Acceptance-Greps aus SPEC.md sequenziell ausführen. Bei jedem Fehler: Bug lokalisieren, Fix anwenden, Grep wiederholen.

```bash
# REQ-1: Auto-Erkennung gelöscht
grep -rn "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py services/claude_service.py
# Soll: 0 Treffer

# REQ-2: UWG vollständig weg (JS + HTML + Python + CSS)
grep -rn "uwg\|UWG\|pip-uwg" static/pip-launcher.js templates/base.html services/deepgram_service.py static/nerve.css
# Soll: 0 Treffer

# REQ-3: Ctrl+G/E keydown-Handler weg
grep -n "ctrlKey.*gatekeeper\|Ctrl+G\|Ctrl+E" static/pip-launcher.js
# Soll: 0 Treffer

# REQ-5: pytest init_session_state Default
pytest tests/test_live_session_gatekeeper.py -v
# Soll: 3 Tests PASSED

# REQ-6: pytest mode_switch
pytest tests/test_mode_switch_event.py -v
# Soll: Tests PASSED

# REQ-7: pipeline-warning nicht mehr in pip-mode-indicator Block
grep "pipeline-warning" static/nerve.css
# Soll: 0 Treffer ODER nur in anderen Klassen (nicht pip-mode-indicator)

# REQ-8: Hex-Werte weg
grep -n "#06060a\|rgba(255,255,255,0.1)\|#F8FAFC" static/pip-launcher.js
# Soll: 0 Treffer

# REQ-9: Terminologie
grep -n "Vorzimmer" static/pip-launcher.js templates/base.html
# Soll: 0 Treffer
```

Falls ein Grep rot ist: den betreffenden Plan-07-Fix notieren (welche Datei, welche Stelle) und chirurgisch fixen. KEIN Refactor im Fix (CLAUDE.md Punkt 17).

Abschlusslauf pytest (ohne externe Corpus-Tests):
```bash
pytest tests/ -q --ignore=tests/test_anonymization_reid.py
```
Pre-existing Failures die in STATE.md dokumentiert sind (test_ewb_pipeline, test_exchange_rates, test_profile_schema_v3, test_qa_pipeline_rueckfrage, MODEL_PIP_AUTOVAR) sind akzeptabel. Neu entstandene Failures sind NICHT akzeptabel.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && echo "REQ-1:" && (grep -rn "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py services/claude_service.py || echo "PASS") && echo "REQ-8:" && (grep -n "#06060a\|rgba(255,255,255,0.1)\|#F8FAFC" static/pip-launcher.js || echo "PASS") && echo "REQ-9:" && (grep -n "Vorzimmer" static/pip-launcher.js templates/base.html || echo "PASS")</automated>
  </verify>

  <acceptance_criteria>
    - ALLE 8 Acceptance-Greps aus SPEC.md geben 0 Treffer (oder bei REQ-5/REQ-6: pytest PASSED)
    - pytest tests/test_live_session_gatekeeper.py → 3 Tests PASSED
    - pytest tests/test_mode_switch_event.py → Tests PASSED
    - `pytest tests/ -q --ignore=tests/test_anonymization_reid.py` enthält keine neuen Failures die nicht in pre-existing Liste stehen
  </acceptance_criteria>

  <done>Alle SPEC.md Acceptance-Greps sauber. pytest-Suite läuft durch. Phase bereit für Staging-Test (Plan 08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Suite → Codebase | pytest-Imports transitiv — gelöschte Testdateien dürfen nicht zu ImportErrors führen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-07-01 | Tampering | Test-Cleanup | mitigate | Nur gelöschte Funktionen werden als Tests entfernt — Phase-Classifier-Tests (nicht gatekeeper) bleiben unberührt |
| T-C.R-07-02 | Denial of Service | pytest-Suite | accept | Pre-existing Failures sind dokumentiert in STATE.md — neue Failures müssen behoben werden |
</threat_model>

<verification>
```bash
# Alle SPEC.md Acceptance-Greps auf einmal:
grep -rn "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py services/claude_service.py && echo "FAIL REQ-1" || echo "PASS REQ-1"
grep -rn "uwg\|UWG\|pip-uwg" static/pip-launcher.js templates/base.html services/deepgram_service.py static/nerve.css && echo "FAIL REQ-2" || echo "PASS REQ-2"
grep -n "ctrlKey.*gatekeeper\|Ctrl+G\|Ctrl+E" static/pip-launcher.js && echo "FAIL REQ-3" || echo "PASS REQ-3"
grep -n "#06060a\|rgba(255,255,255,0.1)\|#F8FAFC" static/pip-launcher.js && echo "FAIL REQ-8" || echo "PASS REQ-8"
grep -n "Vorzimmer" static/pip-launcher.js templates/base.html && echo "FAIL REQ-9" || echo "PASS REQ-9"
pytest tests/test_live_session_gatekeeper.py -v
```
</verification>

<success_criteria>
- test_gatekeeper_classifier.py + test_hysteresis.py gelöscht
- test_phase_classifier.py: apply_hysteresis + test_call_events_phase_change_persisted entfernt
- pytest ohne neue ImportErrors
- Alle 8 Acceptance-Greps aus SPEC.md: 0 Treffer / Tests GRÜN
- test_live_session_gatekeeper.py: 3 Tests PASSED
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-07-SUMMARY.md` erstellen.
</output>
