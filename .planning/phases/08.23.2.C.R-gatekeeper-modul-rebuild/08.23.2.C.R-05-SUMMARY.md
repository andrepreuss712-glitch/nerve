---
phase: 08.23.2.C.R
plan: "05"
subsystem: static/nerve.css, templates/base.html
tags: [css-tokens, button-migration, uwg-cleanup, gatekeeper, accessibility]
dependency_graph:
  requires:
    - 08.23.2.C.R-02
    - 08.23.2.C.R-03
  provides:
    - nerve.css mit 7 neuen CSS-Tokens (pip-gatekeeper-bg/text/bg-hover, pip-bg, badge-default-bg/text) + aktualisiertem --btn-primary-text
    - nerve.css #pip-mode-indicator Button-Reset (border:none, color:inherit, appearance:none, min-height:32px)
    - nerve.css UWG-CSS vollstaendig geloescht (C.R-REQ-2)
    - base.html pip-mode-indicator als button-Element mit data-mode=gatekeeper + Sekretar-Label
  affects:
    - static/nerve.css
    - templates/base.html
tech_stack:
  added: []
  patterns:
    - CSS-Token-System erweitert (--pip-gatekeeper-* Namespace fuer Sekretar-Modus-Indikator)
    - span->button Migration fuer native Tastatur-Zugaenglichkeit (D-03a)
    - Browser-Button-Defaults Reset via #pip-mode-indicator Selektor
key_files:
  created: []
  modified:
    - static/nerve.css
    - templates/base.html
key_decisions:
  - "--btn-primary-text von #0D1117 auf #06060a aktualisiert (Plan-06-Hex-Sweep-Vorbereitung, muss_have im Plan)"
  - "pip-uwg-banner CSS-Bloecke vollstaendig geloescht — kein visueller Ersatz noetig (C.R-REQ-2)"
  - "pip-mode-indicator span->button Migration ohne neues DOM-Element (D-03a) — button bringt Tab+Enter kostenlos mit"
  - "aria-label zeigt Ziel-Modus ('Modus wechseln zu Entscheider') — Screenreader-UX nach D-03b"
metrics:
  duration_seconds: 85
  completed_date: "2026-05-22"
  tasks_completed: 2
  tasks_total: 2
  files_created: 0
  files_modified: 2
requirements_completed:
  - C.R-REQ-4
  - C.R-REQ-7
  - C.R-REQ-8
---

# Phase 08.23.2.C.R Plan 05: nerve.css Token-System + base.html span->button Migration Summary

**nerve.css: 7 neue CSS-Tokens (pip-gatekeeper-Namespace + pip-bg + badge-default + btn-primary-text-Update), Button-Reset fuer #pip-mode-indicator, UWG-CSS geloescht; base.html: pip-uwg-banner entfernt, pip-mode-indicator ist button mit gatekeeper-Default + Sekretar-Label.**

## Performance

- **Duration:** ~85 Sekunden
- **Started:** 2026-05-22T10:48:55Z
- **Completed:** 2026-05-22T10:50:20Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

### Task 1 — nerve.css (C.R-REQ-7 + C.R-REQ-2 CSS-Part)

**Neue CSS-Tokens im :root Block (6 neue + 1 aktualisiert):**
- `--pip-gatekeeper-bg: rgba(96, 165, 250, 0.12)` — Sekretar-Modus-Indikator bg (Blau, kein Warn-Amber)
- `--pip-gatekeeper-text: #3b82f6` — Blue-500, lesbar auf hellem Hintergrund
- `--pip-gatekeeper-bg-hover: rgba(96, 165, 250, 0.22)` — Hover-Feedback Toggle-Button
- `--pip-bg: #F8FAFC` — PiP-Body-Hintergrund (ersetzt hardcoded Wert in Plan 06)
- `--badge-default-bg: rgba(100, 116, 139, 0.08)` — Default-Badge bg (Light-Mode-konform)
- `--badge-default-text: #475569` — Slate-600, lesbar auf hellem Hintergrund
- `--btn-primary-text: #0D1117` → `#06060a` (Plan-06-Hex-Sweep-Vorbereitung)

**pip-mode-indicator[data-mode="gatekeeper"] Token-Update:**
- `--pipeline-warning-bg` → `--pip-gatekeeper-bg` (semantisch korrekt — Amber ist fuer Pipeline-Fehler)
- `--pipeline-warning-text` → `--pip-gatekeeper-text`

**#pip-mode-indicator Button-Reset (neu, Pitfall-4-Vermeidung):**
- `border: none`, `background: var(--glass-bg)`, `font-family: inherit`
- `color: inherit`, `appearance: none`, `font-size: 11px`, `font-weight: 600`
- `min-height: 32px` (D-03e — ausreichend Klick-Flaeche), `cursor: pointer`
- `padding: 2px 8px`, `border-radius: 4px`
- Hover: `background-color: var(--pip-gatekeeper-bg-hover)` (D-03d)

**UWG-CSS geloescht (C.R-REQ-2):**
- `.pip-uwg-banner { ... }` — vollstaendig entfernt
- `.pip-uwg-banner.is-visible { ... }` — vollstaendig entfernt
- `.pip-uwg-banner__close { ... }` — vollstaendig entfernt

**pip-ewb-btn[data-mode-button="gatekeeper"] Token-Migration:**
- `border-color: var(--pipeline-warning-text)` → `border-color: var(--pip-gatekeeper-text)`

### Task 2 — base.html (C.R-REQ-2 DOM + C.R-REQ-4)

**pip-uwg-banner div vollstaendig geloescht:**
- `<div id="pip-uwg-banner" class="pip-uwg-banner" role="alert">` inkl. Kinder-Elemente entfernt

**span→button Migration (D-03a bis D-03h):**
- `<span id="pip-mode-indicator" data-mode="cold_call">Cold-Call</span>` ersetzt durch:
- `<button type="button" id="pip-mode-indicator" class="pip-mode-indicator" data-mode="gatekeeper" aria-label="Modus wechseln zu Entscheider">Sekretär</button>`
- `type="button"` verhindert versehentlichen Form-Submit (T-C.R-05-01 mitigiert)
- `data-mode="gatekeeper"` als HTML-Default (REQ-5 DOM-Seite, DSGVO Single-Speaker)
- `aria-label="Modus wechseln zu Entscheider"` zeigt Ziel-Modus (D-03b)
- Textinhalt "Sekretär" zeigt aktuellen Modus (D-03f)

**Kommentar aktualisiert:**
- `Phase 08.23.2.C: UWG-Hard-Block-Banner + Mode-Indikator` → `Phase 08.23.2.C.R: Modus-Toggle-Button (manuell, DSGVO Single-Speaker)`

## Task Commits

| Commit | Typ | Beschreibung |
|--------|-----|--------------|
| 68b709e | feat | nerve.css — 7 neue Tokens + Button-Reset + UWG-CSS loeschen + Token-Updates |
| df25d48 | feat | base.html — pip-uwg-banner loeschen + span->button Migration |

## Verification Results

| Check | Ergebnis |
|-------|----------|
| pip-gatekeeper Treffer in nerve.css | 7 (erwartet: >=4) |
| btn-primary-text in nerve.css | 3 Treffer |
| --pip-bg + badge-default in nerve.css | 3 Treffer |
| pipeline-warning im pip-mode-indicator Block | 0 Treffer (OK) |
| pip-uwg-banner in nerve.css | 0 Treffer (OK) |
| #pip-mode-indicator Button-Reset | vorhanden (Z.3085) |
| min-height 32px im #pip-mode-indicator Block | Z.3093 |
| color: inherit + appearance: none | Z.3089-3090 |
| pip-uwg-banner in base.html | 0 Treffer (OK) |
| pip-mode-indicator ist button-Element | Z.558 |
| data-mode="gatekeeper" | Z.559 |
| aria-label="Modus wechseln zu Entscheider" | Z.560 |
| Textinhalt "Sekretär" | Z.560 |
| Cold-Call / Vorzimmer in base.html | 0 Treffer (OK) |
| type="button" beim pip-mode-indicator | Z.558 |

## Deviations from Plan

### Auto-fixed Issues

Keine — Plan exakt wie spezifiziert ausgefuehrt.

Hinweis: `--btn-primary-text` existierte bereits mit Wert `#0D1117`. Plan-Must-Have spezifiziert `#06060a`. Der Token wurde aktualisiert (kein neuer Token, aber Wert-Update — korrekt gemaess Planintention).

## Known Stubs

Keine — Plan 05 bringt vollstaendige CSS + HTML-Implementierung ohne Platzhalter.

Offene Verdrahtung fuer spaetere Plaene:
- Klick-Handler fuer #pip-mode-indicator wird in Plan 06 (pip-launcher.js) verdrahtet
- `var(--pip-bg)` wird in Plan 06 fuer `body.style.background`-Ersatz verwendet
- `var(--badge-default-bg/text)` wird in Plan 06 fuer Z.2582-Fix verwendet

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| T-C.R-05-01 (Tampering) | templates/base.html | button type="button" verhindert form-submit — korrekt gesetzt (D-03a). MITIGIERT. |
| T-C.R-05-02 (Information Disclosure) | templates/base.html | aria-label zeigt Ziel-Modus — kein PII, Accessibility-Anforderung erfuellt. AKZEPTIERT. |

## Self-Check: PASSED

- static/nerve.css: FOUND (modifiziert)
- templates/base.html: FOUND (modifiziert)
- Commit 68b709e: vorhanden
- Commit df25d48: vorhanden
