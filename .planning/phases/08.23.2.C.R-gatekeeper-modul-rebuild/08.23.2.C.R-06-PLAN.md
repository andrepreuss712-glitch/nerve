---
phase: 08.23.2.C.R
plan: "06"
type: execute
wave: 3
depends_on:
  - "08.23.2.C.R-02"
  - "08.23.2.C.R-03"
files_modified:
  - static/pip-launcher.js
autonomous: true
requirements:
  - C.R-REQ-2
  - C.R-REQ-3
  - C.R-REQ-4
  - C.R-REQ-8
  - C.R-REQ-9

must_haves:
  truths:
    - "pip-launcher.js: _showUwgBanner() + _wireUwgBannerClose() gelöscht"
    - "pip-launcher.js: uwg_hard_block Socket-Listener gelöscht"
    - "pip-launcher.js: Ctrl+G/E keydown-Handler vollständig gelöscht"
    - "pip-launcher.js: 3 Hex-Werte ersetzt (Z.1226 #06060a, Z.2582 rgba/c5c9d4, Z.1710 #F8FAFC)"
    - "pip-launcher.js: _updateContactCategory() zeigt 'Sekretär' statt 'Vorzimmer'"
    - "pip-launcher.js: _updateContactCategory() aktualisiert aria-label dynamisch"
    - "pip-launcher.js: Klick-Handler auf pip-mode-indicator Button emittet manual_mode_toggle"
    - "pip-launcher.js: Klick-Handler enthält 300ms Throttle gegen Rapid-Click-Spam"
    - "pip-launcher.js: Klick-Handler ruft e.stopPropagation() auf"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "Bereinigter PiP-JS: UWG/Ctrl+G weg, Klick-Handler mit Throttle, Hex-Sweep, Terminologie"
  key_links:
    - from: "static/pip-launcher.js"
      to: "socket.emit('manual_mode_toggle', {category: ...})"
      via: "button click handler auf #pip-mode-indicator"
      pattern: "manual_mode_toggle.*category"
---

<objective>
pip-launcher.js von UWG-Funktionen + Ctrl+G/E bereinigen, Hex-Sweep durchführen, _updateContactCategory() für "Sekretär"-Label + dynamisches aria-label erweitern und Klick-Handler auf pip-mode-indicator Button einbauen.

Purpose: C.R-REQ-2 (UWG-JS weg), C.R-REQ-3 (Ctrl+G/E weg), C.R-REQ-4 (Klick-Handler), C.R-REQ-8 (Hex-Sweep), C.R-REQ-9 (Terminologie). Alle fünf Requirements treffen pip-launcher.js — daher ein dedizierter Plan.

Output: Bereinigtes pip-launcher.js ohne UWG-Funktionen, ohne Ctrl+G/E, mit Klick-Handler (300ms Throttle + stopPropagation), mit CSS-Token-Referenzen statt Hex, mit 'Sekretär'-Label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-RESEARCH.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-UI-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-CONTEXT.md

<interfaces>
<!-- pip-launcher.js zu löschende Stellen (aus RESEARCH.md): -->

<!-- 1. UWG-Socket-Listener (Z.2422-2424) — LÖSCHEN: -->
<!-- state.socket.on('uwg_hard_block', function (data) { -->
<!--     _showUwgBanner(data && data.phrase); -->
<!-- }); -->

<!-- 2. _showUwgBanner() Funktion (Z.2492-ca.2511) — LÖSCHEN: -->
<!-- function _showUwgBanner(phrase) { ... } -->

<!-- 3. _wireUwgBannerClose() Funktion + Aufruf (Z.ca.2505-2512) — LÖSCHEN: -->
<!-- function _wireUwgBannerClose() { ... } -->
<!-- _wireUwgBannerClose();  // Aufruf-Zeile ebenfalls löschen -->

<!-- 4. Ctrl+G/E keydown-Handler (Z.3492-3516) — LÖSCHEN: -->
<!-- // ── Phase 08.23.2.C — Ctrl+G / Ctrl+E Keyboard-Shortcuts (Req-6) ─────────── -->
<!-- document.addEventListener('keydown', function (ev) { ... }); -->

<!-- pip-launcher.js zu ändernde Stellen: -->

<!-- 5. _updateContactCategory() (Z.2446) — Label-Änderung: -->
<!-- AKTUELL: var label = (mode === 'gatekeeper') ? 'Vorzimmer' : (mode === 'meeting') ? 'Meeting' : 'Cold-Call'; -->
<!-- SOLL:    var label = (mode === 'gatekeeper') ? 'Sekretär' : (mode === 'meeting') ? 'Meeting' : 'Entscheider'; -->

<!-- 6. _updateContactCategory() — aria-label dynamisch (Pitfall 5): -->
<!-- AKTUELL: indicator.dataset.mode = mode; indicator.textContent = label; -->
<!-- SOLL: -->
<!-- indicator.dataset.mode = mode; -->
<!-- indicator.textContent = label; -->
<!-- indicator.setAttribute('aria-label', -->
<!--     'Modus wechseln zu ' + ((mode === 'gatekeeper') ? 'Entscheider' : 'Sekretär')); -->

<!-- 7. Hex-Sweep Z.1226: color:#06060a → color:var(--btn-primary-text) -->
<!-- 8. Hex-Sweep Z.2582: rgba(255,255,255,0.1);color:#c5c9d4 → var(--badge-default-bg);color:var(--badge-default-text) -->
<!-- 9. Hex-Sweep Z.1710: body.style.background = '#F8FAFC' → body.style.background = 'var(--pip-bg)' -->

<!-- 10. Klick-Handler NEU (D-03i) — mit 300ms Throttle + stopPropagation: -->
<!-- State-Key Pre-Resolved (Claudian-Audit 2026-05-22, CLAUDE.md Punkt 14): -->
<!-- pip-launcher.js Z.2441-2442 zeigt _updateContactCategory() setzt ZWEI State-Keys: -->
<!--   state.contactCategory = category;   // 'target' oder 'gatekeeper' (Sekretär/Entscheider) -->
<!--   state.currentMode = mode;            // 'gatekeeper' / 'meeting' / 'cold_call' (Workflow-Modus) -->
<!-- Für den Toggle-Check ist state.contactCategory semantisch korrekt — wir toggeln die KATEGORIE -->
<!-- (target↔gatekeeper), nicht den Workflow-Modus. state.currentMode wäre falsch weil beim Wert -->
<!-- 'meeting' der Toggle kollabiert (immer 'gatekeeper' als newCategory). -->
<!-- var _modeBtnThrottle = 0; -->
<!-- var _modeBtn = document.getElementById('pip-mode-indicator'); -->
<!-- if (_modeBtn) { -->
<!--     _modeBtn.addEventListener('click', function (e) { -->
<!--         e.stopPropagation(); -->
<!--         var _now = Date.now(); -->
<!--         if (_now - _modeBtnThrottle < 300) { return; } -->
<!--         _modeBtnThrottle = _now; -->
<!--         var newCategory = (state.contactCategory === 'gatekeeper') ? 'target' : 'gatekeeper'; -->
<!--         if (state.socket && state.socket.emit) { -->
<!--             state.socket.emit('manual_mode_toggle', { category: newCategory }); -->
<!--         } -->
<!--     }); -->
<!-- } -->
<!-- Einfügen im Initialisierungs-Block nahe anderen Socket-Event-Registrierungen -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pip-launcher.js — UWG-Funktionen + Ctrl+G/E löschen (C.R-REQ-2 + C.R-REQ-3)</name>
  <files>static/pip-launcher.js</files>

  <read_first>
    - static/pip-launcher.js Z.2415-2430 — uwg_hard_block Socket-Listener Kontext
    - static/pip-launcher.js Z.2485-2515 — _showUwgBanner + _wireUwgBannerClose Blöcke
    - static/pip-launcher.js Z.3485-3520 — Ctrl+G/E keydown-Handler
  </read_first>

  <action>
Vier Löschungen in pip-launcher.js:

**Löschung 1: uwg_hard_block Socket-Listener (ca. Z.2422-2424):**
```javascript
state.socket.on('uwg_hard_block', function (data) {
    _showUwgBanner(data && data.phrase);
});
```
Drei Zeilen löschen.

**Löschung 2: _showUwgBanner() Funktion (ca. Z.2492-2511):**
```javascript
function _showUwgBanner(phrase) {
  var banner = document.getElementById('pip-uwg-banner');
  // ... komplette Funktion ...
}
```
Komplette Funktion löschen.

**Löschung 3: _wireUwgBannerClose() Funktion + Aufruf-Zeile (ca. Z.2505-2512):**
```javascript
function _wireUwgBannerClose() {
  // ...
}
_wireUwgBannerClose();
```
Funktion + direkte Aufruf-Zeile löschen.

**Löschung 4: Ctrl+G/E keydown-Handler (ca. Z.3492-3516) — KOMPLETT:**
```javascript
// ── Phase 08.23.2.C — Ctrl+G / Ctrl+E Keyboard-Shortcuts (Req-6) ─────────────
document.addEventListener('keydown', function (ev) {
    if (!(ev.ctrlKey || ev.metaKey)) return;
    // ...
    if (state.socket && state.socket.emit) {
        state.socket.emit('manual_mode_toggle', { category: category });
    }
});
```
Von der Kommentar-Zeile bis zum Ende des Handlers (inkl. schließende }); ) löschen.

**ACHTUNG:** Andere keydown-Handler (falls vorhanden) NICHT löschen — nur der Modus-Toggle-Handler.
**Control-Flow-Audit (CLAUDE.md Punkt 14):** Nach Löschungen prüfen ob noch weitere Aufrufe von _showUwgBanner oder _wireUwgBannerClose existieren.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "uwg\|UWG\|pip-uwg\|ctrlKey.*gatekeeper\|Ctrl+G\|Ctrl+E" static/pip-launcher.js || echo "0 Treffer — PASS"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "uwg\|UWG\|pip-uwg" static/pip-launcher.js` → 0 Treffer
    - `grep -n "ctrlKey.*gatekeeper\|Ctrl+G\|Ctrl+E\|_showUwgBanner\|_wireUwgBannerClose" static/pip-launcher.js` → 0 Treffer
    - VOLLSTÄNDIGER REQ-2 Acceptance-Grep: `grep -rn "uwg\|UWG\|pip-uwg" static/pip-launcher.js templates/base.html services/deepgram_service.py static/nerve.css` → 0 Treffer
    - VOLLSTÄNDIGER REQ-3 Acceptance-Grep: `grep -n "ctrlKey\|Ctrl+G\|Ctrl+E\|keydown.*gatekeeper" static/pip-launcher.js` → 0 Treffer für Modus-Toggle-Handler
  </acceptance_criteria>

  <done>pip-launcher.js: UWG-Socket-Listener, _showUwgBanner(), _wireUwgBannerClose() und Ctrl+G/E Handler vollständig gelöscht.</done>
</task>

<task type="auto">
  <name>Task 2: pip-launcher.js — Hex-Sweep + Terminologie + aria-label + Klick-Handler (C.R-REQ-4 + C.R-REQ-8 + C.R-REQ-9)</name>
  <files>static/pip-launcher.js</files>

  <read_first>
    - static/pip-launcher.js Z.1220-1232 — Z.1226 Hex-Kontext lesen
    - static/pip-launcher.js Z.1705-1715 — Z.1710 Hex-Kontext lesen
    - static/pip-launcher.js Z.2575-2590 — Z.2582 Hex-Kontext lesen
    - static/pip-launcher.js Z.2440-2460 — _updateContactCategory() vollständig lesen
    - static/pip-launcher.js — Initialisierungs-Block lesen (wo andere Listener registriert werden)
    - static/pip-launcher.js — State-Key bereits pre-resolved (Claudian-Audit 2026-05-22, CLAUDE.md Punkt 14): `state.contactCategory` ist der semantisch korrekte Key für den Category-Toggle (target↔gatekeeper), gesetzt in `_updateContactCategory()` Z.2441. NICHT `state.currentMode` — das ist Workflow-Modus (gatekeeper/meeting/cold_call) und würde die Toggle-Logik bei `mode==='meeting'` kollabieren lassen. Verifikations-grep: `grep -n "state.contactCategory" static/pip-launcher.js` → ≥1 Treffer in Z.2441
    - static/nerve.css — confirm --btn-primary-text token exists (defined in Plan 05)
  </read_first>

  <action>
Fünf Änderungen in pip-launcher.js:

**Änderung 1: Hex-Sweep Z.1226 (ca.):**
```javascript
// AKTUELL: color:#06060a
// SOLL:    color:var(--btn-primary-text)
```
Exakte Zeile durch Lesen der Datei bestimmen. Der Token `--btn-primary-text: #06060a` wurde in Plan 05 nerve.css definiert. Direkt `color:var(--btn-primary-text)` einsetzen — kein Conditional nötig.

**Änderung 2: Hex-Sweep Z.2582 (ca.):**
```javascript
// AKTUELL: 'background:rgba(255,255,255,0.1);color:#c5c9d4'
// SOLL:    'background:var(--badge-default-bg);color:var(--badge-default-text)'
```
Die neuen Tokens --badge-default-bg und --badge-default-text wurden in Plan 05 (nerve.css) definiert.

**Änderung 3: Hex-Sweep Z.1710 (ca.):**
```javascript
// AKTUELL: body.style.background = '#F8FAFC';
// SOLL:    body.style.background = 'var(--pip-bg)';
```
--pip-bg wurde in Plan 05 (nerve.css) definiert.

**Änderung 4: _updateContactCategory() — Label 'Vorzimmer'→'Sekretär' + 'Cold-Call'→'Entscheider' + aria-label dynamisch (D-03b, C.R-REQ-9):**
```javascript
// AKTUELL (ungefähr Z.2446):
var label = (mode === 'gatekeeper') ? 'Vorzimmer' : (mode === 'meeting') ? 'Meeting' : 'Cold-Call';

// SOLL:
var label = (mode === 'gatekeeper') ? 'Sekretär' : (mode === 'meeting') ? 'Meeting' : 'Entscheider';
```
Direkt danach, nach dem textContent-Set, aria-label dynamisch aktualisieren (Pitfall 5 Vermeidung):
```javascript
if (indicator) {
    indicator.dataset.mode = mode;
    indicator.textContent = label;
    // Dynamisches aria-label: zeigt Ziel-Modus (nicht aktuellen)
    indicator.setAttribute('aria-label',
        'Modus wechseln zu ' + ((mode === 'gatekeeper') ? 'Entscheider' : 'Sekretär'));
}
```
Den tatsächlichen Code durch Lesen bestimmen und aria-label-Zeile organisch einfügen.

**Änderung 5: Klick-Handler für pip-mode-indicator Button (D-03i) — mit 300ms Throttle + stopPropagation:**

**State-Key pre-resolved (Claudian-Audit 2026-05-22, CLAUDE.md Punkt 14):** `state.contactCategory` ist der semantisch korrekte Key für den Toggle-Check. Wir toggeln die KATEGORIE (target↔gatekeeper), nicht den Workflow-Modus (cold_call/meeting/gatekeeper). `state.currentMode` wäre falsch weil beim Wert `'meeting'` die Toggle-Logik kollabieren würde (immer `'gatekeeper'` als newCategory). Verankert via pip-launcher.js Z.2441 wo `state.contactCategory = category` gesetzt wird.

Im Initialisierungs-Block (nahe anderen Event-Listener-Registrierungen) einfügen:
```javascript
// Phase 08.23.2.C.R: Klick-Handler fuer pip-mode-indicator Toggle-Button (300ms Throttle)
var _modeBtnThrottle = 0;
var _modeBtn = document.getElementById('pip-mode-indicator');
if (_modeBtn) {
    _modeBtn.addEventListener('click', function (e) {
        e.stopPropagation();
        var _now = Date.now();
        if (_now - _modeBtnThrottle < 300) { return; }
        _modeBtnThrottle = _now;
        var newCategory = (state.contactCategory === 'gatekeeper') ? 'target' : 'gatekeeper';
        if (state.socket && state.socket.emit) {
            state.socket.emit('manual_mode_toggle', { category: newCategory });
        }
    });
}
```
**KEIN Placeholder ersetzen nötig** — `state.contactCategory` ist final und korrekt. Verifikations-grep nach Execute: `grep -n "state.contactCategory === 'gatekeeper'" static/pip-launcher.js` → ≥1 Treffer.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && echo "Hex-Sweep Check:" && (grep -n "#06060a\|rgba(255,255,255,0.1)\|#F8FAFC" static/pip-launcher.js || echo "0 Treffer — PASS") && echo "Terminologie Check:" && (grep -n "Vorzimmer" static/pip-launcher.js || echo "0 Treffer — PASS") && echo "Klick-Handler Check:" && grep -n "pip-mode-indicator.*click\|click.*pip-mode-indicator\|_modeBtn" static/pip-launcher.js</automated>
  </verify>

  <acceptance_criteria>
    - VOLLSTÄNDIGER REQ-8 Acceptance-Grep: `grep -n "#06060a\|rgba(255,255,255,0.1)\|#F8FAFC" static/pip-launcher.js` → 0 Treffer
    - VOLLSTÄNDIGER REQ-9 Acceptance-Grep: `grep -n "Vorzimmer" static/pip-launcher.js templates/base.html` → 0 Treffer
    - `grep -n "Sekretär" static/pip-launcher.js` → ≥1 Treffer (in _updateContactCategory)
    - `grep -n "Entscheider" static/pip-launcher.js` → ≥1 Treffer (in _updateContactCategory + aria-label)
    - `grep -n "setAttribute.*aria-label" static/pip-launcher.js` → ≥1 Treffer (dynamisches aria-label)
    - `grep -n "manual_mode_toggle" static/pip-launcher.js` → ≥1 Treffer (Klick-Handler)
    - `grep -n "var(--badge-default-bg)\|var(--pip-bg)\|var(--btn-primary-text)" static/pip-launcher.js` → ≥3 Treffer (Hex-Sweep Ersatz)
    - `grep -n "stopPropagation" static/pip-launcher.js` → ≥1 Treffer (Klick-Handler)
    - `grep -n "modeBtnThrottle\|throttle\|Date.now" static/pip-launcher.js` → ≥1 Treffer (Debounce)
    - `grep -n "state.contactCategory === 'gatekeeper'" static/pip-launcher.js` → ≥1 Treffer (Klick-Handler nutzt semantisch korrekten Category-Key, pre-resolved Claudian-Audit 2026-05-22)
    - `grep -n "{ACTUAL_MODE_KEY}" static/pip-launcher.js` → 0 Treffer (kein Placeholder im Code)
  </acceptance_criteria>

  <done>pip-launcher.js: 3 Hex-Werte durch CSS-Tokens ersetzt, 'Vorzimmer'→'Sekretär'+'Cold-Call'→'Entscheider', aria-label dynamisch in _updateContactCategory(), Klick-Handler auf pip-mode-indicator mit 300ms Throttle + stopPropagation registriert. State-Key aus pre-flight grep verwendet (kein Placeholder).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-Click → socket.emit | Klick-Handler sendet manual_mode_toggle — Backend hat Whitelist-Validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-06-01 | Spoofing | Klick-Handler category-Wert | mitigate | Backend-Whitelist in manual_mode_toggle Handler (category not in ('target', 'gatekeeper')) — verhindert invalide Werte auch wenn JS manipuliert |
| T-C.R-06-02 | Tampering | aria-label dynamisch | accept | aria-label ist UX/Accessibility-Only — kein Security-Risk durch dynamische Aktualisierung |
</threat_model>

<verification>
```bash
# REQ-2 Acceptance-Grep (JS-Teil):
grep -rn "uwg\|UWG\|pip-uwg" static/pip-launcher.js
# Erwartung: 0 Treffer

# REQ-3 Acceptance-Grep:
grep -n "ctrlKey\|Ctrl+G\|Ctrl+E\|keydown.*gatekeeper" static/pip-launcher.js
# Erwartung: 0 Treffer

# REQ-8 Acceptance-Grep:
grep -n "#06060a\|rgba(255,255,255,0.1)\|#F8FAFC" static/pip-launcher.js
# Erwartung: 0 Treffer

# REQ-9 Acceptance-Grep:
grep -n "Vorzimmer" static/pip-launcher.js templates/base.html
# Erwartung: 0 Treffer

# Klick-Handler Qualität:
grep -n "stopPropagation\|modeBtnThrottle\|Date.now" static/pip-launcher.js
# Erwartung: ≥2 Treffer

# State-Key-Nachweis (D-03i, pre-resolved Claudian-Audit 2026-05-22):
grep -n "state.contactCategory === 'gatekeeper'" static/pip-launcher.js
# Erwartung: ≥1 Treffer (Klick-Handler nutzt state.contactCategory — semantisch korrekt für Category-Toggle)
grep -n "{ACTUAL_MODE_KEY}" static/pip-launcher.js
# Erwartung: 0 Treffer (kein Placeholder im Code)
```
</verification>

<success_criteria>
- pip-launcher.js: 0 UWG-Referenzen, 0 Ctrl+G/E Handler, 0 Hex-Reste (#06060a, rgba/c5c9d4, #F8FAFC)
- pip-launcher.js: 'Sekretär' + 'Entscheider' in _updateContactCategory()
- pip-launcher.js: aria-label dynamisch via setAttribute in _updateContactCategory()
- pip-launcher.js: Klick-Handler auf pip-mode-indicator emittet manual_mode_toggle mit 300ms Throttle + stopPropagation
- pip-launcher.js: Klick-Handler nutzt `state.contactCategory === 'gatekeeper'` (semantisch korrekt für Category-Toggle, pre-resolved Claudian-Audit 2026-05-22, kein Placeholder)
- Alle 4 Acceptance-Greps aus SPEC.md sauber
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-06-SUMMARY.md` erstellen.
</output>
