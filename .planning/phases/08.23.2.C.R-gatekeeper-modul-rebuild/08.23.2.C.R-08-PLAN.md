---
phase: 08.23.2.C.R
plan: "08"
type: execute
wave: 5
depends_on:
  - "08.23.2.C.R-07"
files_modified: []
autonomous: false
requirements:
  - C.R-REQ-4
  - C.R-REQ-5
  - C.R-REQ-9

must_haves:
  truths:
    - "PiP öffnet auf Staging mit pip-mode-indicator = 'Sekretär' (ohne vorherigen Toggle)"
    - "4 Gatekeeper-Buttons sichtbar beim PiP-Öffnen"
    - "Toggle-Klick wechselt auf 'Entscheider' + andere Buttons sichtbar"
    - "Screenshot im SUMMARY als Beleg hinterlegt"
  artifacts:
    - path: ".planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-08-SUMMARY.md"
      provides: "Screenshot-Beleg + Staging-Test-Ergebnis"
      contains: "screenshot"
  key_links:
    - from: "staging.getnerve.app/live"
      to: "pip-mode-indicator"
      via: "PiP-Fenster öffnen"
      pattern: "Sekretär"
---

<objective>
Manueller Staging-Smoke-Test: PiP auf staging.getnerve.app öffnen, Default-Sekretär-Modus prüfen, Toggle einmal klicken und Screenshot im SUMMARY hinterlegen.

Purpose: SPEC.md Req-5 Acceptance Criterion Schicht 3 — "Manueller Browser-Test auf Staging: PiP ohne vorherigen Toggle öffnen → Indikator 'Sekretär', Button-Row 4 Gatekeeper-Buttons. Screenshot in SUMMARY anhängen." Dieses Kriterium kann nicht automatisiert werden und ist BLOCKING für den Phase-Gate.

Output: Staging-Test-Ergebnis + Screenshot im SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy auf Staging</name>
  <files></files>

  <read_first>
    - deploy/RUNBOOK-staging.md — Deploy-Prozedur
  </read_first>

  <action>
Deploy auf staging.getnerve.app via:
```bash
./deploy.sh staging
```

Erwartete Ausgabe:
- git push origin main erfolgreich
- SSH-Verbindung zu Staging-VPS
- pip install + systemctl restart nerve-staging
- Health-Check-Response mit status=ok

Falls deploy.sh nicht verfügbar: manuell via SSH und git pull + systemctl restart.

Warten bis Staging antwortet (Health-Check: curl https://staging.getnerve.app/api/health).
  </action>

  <verify>
    <automated>curl -s https://staging.getnerve.app/api/health | python -c "import sys,json; d=json.load(sys.stdin); print('OK' if d.get('status')=='ok' else 'FAIL: ' + str(d))"</automated>
  </verify>

  <acceptance_criteria>
    - `curl https://staging.getnerve.app/api/health` gibt `{"status": "ok", ...}` zurück
    - git_head in Health-Response entspricht aktuellem lokalen Commit-Hash
  </acceptance_criteria>

  <done>Staging läuft mit aktuellem Code aus Phase C.R.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manueller PiP-Test auf Staging — Screenshot-Pflicht</name>

  <what-built>
    Phase 08.23.2.C.R wurde auf staging.getnerve.app deployed. Der PiP-Mode-Indicator wurde von span zu button migriert, Default-Modus ist jetzt Sekretär (gatekeeper), 4 Gatekeeper-Buttons werden beim PiP-Öffnen angezeigt. Ctrl+G/E und UWG-Banner wurden entfernt.
  </what-built>

  <how-to-verify>
    1. Browser öffnen: https://staging.getnerve.app (Basic-Auth: Zugangsdaten aus .env.staging)
    2. Einloggen mit Test-Account
    3. Zur /live-Seite navigieren
    4. PiP-Fenster öffnen (PiP-Launcher-Button klicken)
    5. PRÜFEN: pip-mode-indicator zeigt "Sekretär" — NICHT "Cold-Call" oder "Vorzimmer"
    6. PRÜFEN: Button-Row zeigt 4 Gatekeeper-Phrasen-Buttons
    7. PRÜFEN: UWG-Banner ist NICHT sichtbar
    8. Toggle-Button KLICKEN
    9. PRÜFEN: pip-mode-indicator wechselt zu "Entscheider"
    10. PRÜFEN: Button-Row zeigt Standard-EWB-Buttons (nicht Gatekeeper)
    11. Zweiten Klick ausführen
    12. PRÜFEN: wechselt zurück zu "Sekretär" + Gatekeeper-Buttons
    13. SCREENSHOT machen — zeigt pip-mode-indicator "Sekretär" + 4 Buttons

    Browser-Console prüfen: Keine JS-Errors beim Öffnen des PiP-Fensters.
  </how-to-verify>

  <resume-signal>
    Screenshot aufgenommen und als Datei gespeichert (Pfad nennen). Dann: "approved" tippen wenn alle Prüfpunkte bestanden, oder Fehler beschreiben.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging-Server → Browser | HTTP-Basic-Auth schützt Staging vor externem Zugriff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-08-01 | Information Disclosure | staging.getnerve.app | mitigate | Basic-Auth auf Nginx-Ebene (nginx-staging.conf) — kein öffentlicher Zugang |
| T-C.R-08-02 | Tampering | Staging-Deploy | accept | Staging ist kein Production — Datenverlust akzeptables Risiko; Backup-Script existiert (scripts/refresh_staging_from_production.sh) |
</threat_model>

<verification>
Manuelle Verifikation:
- PiP zeigt "Sekretär" beim ersten Öffnen (ohne Toggle) — REQ-5 Schicht 3
- 4 Gatekeeper-Buttons sichtbar — REQ-5 Schicht 2 (UI-Pfad)
- Toggle-Klick wechselt Modus — REQ-4
- "Vorzimmer" nirgendwo sichtbar — REQ-9
- UWG-Banner nicht vorhanden — REQ-2
- Screenshot im SUMMARY als Beleg
</verification>

<success_criteria>
- Staging-Deploy erfolgreich (Health-Check grün)
- PiP zeigt "Sekretär" beim ersten Öffnen
- 4 Gatekeeper-Buttons beim PiP-Öffnen sichtbar
- Toggle-Klick funktioniert (Modus wechselt)
- Screenshot in SUMMARY hinterlegt
- Kein JS-Error in Browser-Console
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-08-SUMMARY.md` erstellen.
Screenshot-Pfad im SUMMARY dokumentieren.
</output>
