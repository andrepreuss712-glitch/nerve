---
phase: 08.23.2.C.R
plan: "04"
type: execute
wave: 2
depends_on:
  - "08.23.2.C.R-02"
  - "08.23.2.C.R-03"
files_modified:
  - services/deepgram_service.py
  - services/live_session.py
autonomous: true
requirements:
  - C.R-REQ-6

must_haves:
  truths:
    - "manual_mode_toggle Handler in deepgram_service.py hat Skip-Guard wenn call_id=None"
    - "manual_mode_toggle Handler persistiert mode_switch-Event in call_events"
    - "mode_switch payload enthält old_mode, new_mode, old_category, new_category, timestamp"
    - "create_call_for_sid() schreibt mode_initial-Event nach Call-Record-Anlage"
    - "mode_initial payload enthält mode, category, sid, timestamp"
    - "mode_initial-INSERT nutzt lokale Variable cid (nicht aus _session_state lesen — Pitfall 2)"
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "manual_mode_toggle Handler mit mode_switch-INSERT + Skip-Guard"
    - path: "services/live_session.py"
      provides: "create_call_for_sid() mit mode_initial-INSERT"
  key_links:
    - from: "services/deepgram_service.py"
      to: "database call_events"
      via: "CallEvent INSERT event_type='mode_switch'"
      pattern: "event_type.*mode_switch"
    - from: "services/live_session.py"
      to: "database call_events"
      via: "CallEvent INSERT event_type='mode_initial'"
      pattern: "event_type.*mode_initial"
---

<objective>
mode_switch-INSERT in den manual_mode_toggle Handler (deepgram_service.py) einbauen und mode_initial-INSERT in create_call_for_sid() (live_session.py) ergänzen. Beide Änderungen setzen die bereinigten Dateien aus Wave 1 voraus.

Purpose: Req-6 — Jeder Toggle muss einen persistierten Zeitstempel in call_events erzeugen. Ohne mode_switch-Event kann Post-Call-Analyse nicht wissen wann der Berater vom Sekretär-Modus in den Entscheider-Modus gewechselt hat. Ohne mode_initial-Event ist der Startpunkt unbekannt wenn der Berater VOR dem Anruf togglet.

Output: deepgram_service.py mit mode_switch-INSERT, live_session.py mit mode_initial-INSERT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-RESEARCH.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-CONTEXT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-02-SUMMARY.md

<interfaces>
<!-- deepgram_service.py manual_mode_toggle Handler — aktueller Zustand nach Plan 02 (Z.723-750): -->
<!-- Handler setzt contact_category + current_mode im State, emittet contact_category_update + ack -->
<!-- Whitelist-Check bereits vorhanden: category not in ('target', 'gatekeeper') → return ok:False -->
<!-- _old_cat und _old_mode MÜSSEN VOR dem State-Update gelesen werden (für payload) -->

<!-- live_session.py create_call_for_sid() — Ende der Funktion (aus RESEARCH.md Z.270-277): -->
<!-- cid = str(call.id) -->
<!-- with _session_state_lock: -->
<!--     if sid in _session_state: -->
<!--         _session_state[sid].setdefault('state', {})['call_id'] = cid -->
<!-- print(f'[live_session] create_call_for_sid: call_id={cid!r} sid={sid!r}') -->
<!-- return cid -->
<!-- mode_initial INSERT: NACH dem call_id-Write, VOR dem return -->
<!-- cid-Variable direkt nutzen (nicht aus _session_state lesen — Pitfall 2) -->

<!-- phase_change-INSERT Referenz-Muster (aus claude_service.py Block der in Plan 03 gelöscht wird): -->
<!-- from database.db import SessionLocal -->
<!-- from database.models import CallEvent -->
<!-- db = SessionLocal() -->
<!-- try: -->
<!--     db.add(CallEvent(call_id=..., event_type='phase_change', event_ts_ms=..., payload={...})) -->
<!--     db.commit() -->
<!-- finally: -->
<!--     db.close() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deepgram_service.py — mode_switch-INSERT + Skip-Guard in manual_mode_toggle (D-04a + D-04d)</name>
  <files>services/deepgram_service.py</files>

  <read_first>
    - services/deepgram_service.py Z.720-760 — manual_mode_toggle Handler vollständig lesen (aktueller Zustand nach Plan 02)
    - services/deepgram_service.py — Import-Abschnitt prüfen ob SessionLocal + CallEvent bereits importiert
  </read_first>

  <action>
Im `manual_mode_toggle` Socket-Handler nach dem bestehenden State-Update-Block den mode_switch-INSERT einfügen.

**Schritt 1:** _old_cat und _old_mode VOR dem State-Update lesen (müssen bereits im Handler sichtbar sein oder hinzufügen). Wenn der Handler die alten Werte nicht liest, diese Zeilen VOR dem State-Update ergänzen:
```python
_old_cat = st.get('contact_category')
_old_mode = st.get('current_mode')
```

**Schritt 2:** Nach dem State-Update und Phase-Reset, Skip-Guard + INSERT (D-04a):
```python
# D-04a: Skip-Guard — kein INSERT wenn call_id nicht gesetzt (kein aktiver Anruf)
_call_id = st.get('call_id') if 'st' in dir() else None
if _call_id is None:
    print(f'[pip] mode_switch: call_id not set, skip event')
else:
    _db_ms = None
    try:
        from database.db import SessionLocal as _SL_ms
        from database.models import CallEvent as _CE_ms
        import time as _t_ms
        _db_ms = _SL_ms()
        try:
            _db_ms.add(_CE_ms(
                call_id=_call_id,
                event_type='mode_switch',
                event_ts_ms=int(_t_ms.time() * 1000),
                payload={
                    'old_mode': _old_mode,
                    'new_mode': new_mode,
                    'old_category': _old_cat,
                    'new_category': category,
                    'timestamp': _t_ms.monotonic(),
                },
            ))
            _db_ms.commit()
            print(f'[pip] mode_switch event written: {_old_mode!r} → {new_mode!r}')
        finally:
            _db_ms.close()
    except Exception as _ms_err:
        if _db_ms is not None:
            _db_ms.rollback()
        print(f'[pip] mode_switch persist Fehler (non-fatal): {type(_ms_err).__name__}: {_ms_err}')
```

Den tatsächlichen Handler-Code durch Lesen bestimmen und den INSERT organisch einfügen ohne die bestehende Logik zu beschädigen. Die Variable-Namen `st`, `new_mode`, `category` müssen mit dem tatsächlichen Handler-Code übereinstimmen.

**ACHTUNG:** Whitelist-Validation `category not in ('target', 'gatekeeper')` NICHT entfernen — Security-Pattern bleibt (RESEARCH.md Security Domain).
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('services/deepgram_service.py'); print('Syntax OK')" && grep -n "mode_switch" services/deepgram_service.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "event_type.*mode_switch\|mode_switch.*event_type" services/deepgram_service.py` → ≥1 Treffer
    - `grep -n "call_id not set, skip event" services/deepgram_service.py` → 1 Treffer (Skip-Guard vorhanden)
    - `grep -n "old_mode.*new_mode\|new_mode.*old_mode" services/deepgram_service.py` → ≥1 Treffer (payload-Felder)
    - services/deepgram_service.py hat valide Syntax
    - Whitelist-Check noch vorhanden: `grep -n "not in.*target.*gatekeeper\|gatekeeper.*target" services/deepgram_service.py` → ≥1 Treffer
    - `grep -n "is not None.*rollback\|rollback.*is not None\|_db_ms = None" services/deepgram_service.py` → ≥1 Treffer (rollback-Guard vorhanden)
  </acceptance_criteria>

  <done>manual_mode_toggle Handler persistiert mode_switch-Event mit Skip-Guard und 4-Felder-payload. _db_ms = None vor try-Block, rollback nur wenn _db_ms is not None. Whitelist-Security-Pattern intakt.</done>
</task>

<task type="auto">
  <name>Task 2: live_session.py — mode_initial-INSERT in create_call_for_sid() (D-04b + D-04c)</name>
  <files>services/live_session.py</files>

  <read_first>
    - services/live_session.py — create_call_for_sid() vollständig lesen (Ende der Funktion mit call_id-Write + return)
    - services/live_session.py — Import-Abschnitt prüfen
    - database/models.py Z.677-692 — CallEvent model lesen: call_id Column type prüfen (Integer FK?)
  </read_first>

  <action>
In `create_call_for_sid()` den mode_initial-INSERT NACH dem call_id-Write in _session_state und VOR dem `return cid` einfügen.

**Wichtig: cid lokale Variable verwenden (NICHT aus _session_state lesen — Pitfall 2 aus RESEARCH.md):**
Die lokale Variable `cid` ist am gleichen Stack-Frame verfügbar. Nicht `_session_state[sid]['state']['call_id']` lesen — zwischen State-Write und INSERT könnte theoretisch pop_session_state() laufen.

**call_id Typ-Entscheidung (firm):** `CallEvent.call_id` ist ein Integer FK (aus database/models.py). `cid = str(call.id)` — daher `call_id=int(cid)` verwenden (nicht `call_id=cid`). Diese Entscheidung ist nicht conditional — immer int() verwenden.

**_current_mode und _current_cat aus State lesen (separater Lock-Block):**
```python
# NEU: mode_initial-Event — liest aktuellen Modus aus State (nach call_id-Write)
with _session_state_lock:
    _mode_init_state = _session_state.get(sid, {}).get('state', {})
    _current_mode = _mode_init_state.get('current_mode', 'gatekeeper')
    _current_cat = _mode_init_state.get('contact_category', 'gatekeeper')

_db_mi = None
try:
    from database.db import SessionLocal as _SL_mi
    from database.models import CallEvent as _CE_mi
    import time as _t_mi
    _db_mi = _SL_mi()
    try:
        _db_mi.add(_CE_mi(
            call_id=int(cid),
            event_type='mode_initial',
            event_ts_ms=int(_t_mi.time() * 1000),
            payload={
                'mode': _current_mode,
                'category': _current_cat,
                'sid': sid,
                'timestamp': _t_mi.monotonic(),
            },
        ))
        _db_mi.commit()
        print(f'[live_session] mode_initial event written: call_id={cid!r} mode={_current_mode!r}')
    finally:
        _db_mi.close()
except Exception as _mi_err:
    if _db_mi is not None:
        _db_mi.rollback()
    print(f'[live_session] mode_initial persist Fehler (non-fatal): {type(_mi_err).__name__}: {_mi_err}')
```

Falls _session_state_lock in create_call_for_sid() nicht direkt zugänglich ist, den tatsächlichen Lock-Namen aus der Datei lesen und anpassen.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('services/live_session.py'); print('Syntax OK')" && grep -n "mode_initial" services/live_session.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "event_type.*mode_initial\|mode_initial.*event_type" services/live_session.py` → ≥1 Treffer
    - `grep -n "'sid'.*sid\|sid.*'sid'" services/live_session.py` → ≥1 Treffer (sid im payload)
    - `grep -n "int(cid)" services/live_session.py` → ≥1 Treffer (kein str-FK-Mismatch)
    - services/live_session.py hat valide Syntax
    - `grep -n "mode_initial persist Fehler" services/live_session.py` → 1 Treffer (non-fatal Exception-Handling)
    - `grep -n "is not None.*rollback\|rollback.*is not None\|_db_mi = None" services/live_session.py` → ≥1 Treffer (rollback-Guard vorhanden)
  </acceptance_criteria>

  <done>create_call_for_sid() schreibt mode_initial-Event nach Call-Record-Anlage mit 4-Felder-payload. int(cid) für call_id (Integer FK). _db_mi = None vor try-Block, rollback nur wenn _db_mi is not None. Non-fatal Exception-Handling vorhanden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socket-Event → call_events | manual_mode_toggle emittet DB-Write — category-Whitelist verhindert invalide Werte |
| create_call_for_sid → call_events | Neuer INSERT-Pfad bei Call-Anlage — fail-safe via try/except |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-04-01 | Tampering | mode_switch payload | mitigate | Whitelist-Check (category in ('target', 'gatekeeper')) bleibt — verhindert invalide Kategorie-Werte in DB (RESEARCH.md V5 Input Validation) |
| T-C.R-04-02 | Denial of Service | mode_initial INSERT in create_call_for_sid | mitigate | try/except macht INSERT non-fatal — Call-Record-Anlage schlägt nie wegen DB-Fehler fehl |
| T-C.R-04-03 | Elevation of Privilege | call_id Skip-Guard | accept | call_id=None bedeutet kein aktiver Anruf — Skip ist korrekt und sicher; kein INSERT auf fake call_id möglich |
</threat_model>

<verification>
```bash
# mode_switch in deepgram_service.py:
grep -n "mode_switch\|skip event" services/deepgram_service.py
# Erwartung: ≥2 Treffer

# mode_initial in live_session.py:
grep -n "mode_initial" services/live_session.py
# Erwartung: ≥1 Treffer

# rollback-Guard in beiden Dateien:
grep -n "is not None.*rollback\|_db_ms = None\|_db_mi = None" services/deepgram_service.py services/live_session.py
# Erwartung: ≥1 Treffer pro Datei

# int(cid) in live_session.py:
grep -n "int(cid)" services/live_session.py
# Erwartung: ≥1 Treffer

# Syntax beider Dateien:
python -c "import py_compile; [py_compile.compile(f) for f in ['services/deepgram_service.py', 'services/live_session.py']]; print('OK')"

# REQ-6 Test-Scaffold (aus Plan 01):
pytest tests/test_mode_switch_event.py -v
# Erwartung: Tests GRÜN (CallEvent-Objekte mit korrekten Feldern)
```
</verification>

<success_criteria>
- deepgram_service.py: mode_switch-INSERT mit Skip-Guard + 4 payload-Feldern
- live_session.py: mode_initial-INSERT mit int(cid) für call_id (Integer FK) + 4 payload-Feldern
- Beide Dateien syntaktisch korrekt
- Non-fatal Exception-Handling: _db_ms/_db_mi = None vor try-Block, rollback nur wenn is not None
- Whitelist-Security-Pattern in manual_mode_toggle intakt
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-04-SUMMARY.md` erstellen.
</output>
