---
phase: 08.23.2.C.R
plan: "02"
type: execute
wave: 1
depends_on:
  - "08.23.2.C.R-01"
files_modified:
  - services/gatekeeper.py
  - services/deepgram_service.py
autonomous: true
requirements:
  - C.R-REQ-1
  - C.R-REQ-2

must_haves:
  truths:
    - "gatekeeper.py enthält nur noch populate_context_notes() — alle 6 Auto-Erkennungs-Funktionen gelöscht"
    - "Alle Dead Imports aus gatekeeper.py entfernt (kein anonymization, ki_logik, config.phase_transitions)"
    - "deepgram_service.py: UWG-Guard Z.47-49 gelöscht"
    - "deepgram_service.py: Trigger-Phrasen-Detection Block Z.118-155 gelöscht"
    - "deepgram_service.py: UWG-Hard-Block-Detection Block Z.157-191 gelöscht"
    - "Acceptance-Grep für deepgram_service.py gibt 0 Treffer (detect_trigger_phrases, classify_contact, apply_hysteresis)"
  artifacts:
    - path: "services/gatekeeper.py"
      provides: "Single-Function-Stub — nur populate_context_notes() + Modul-Docstring"
      contains: "populate_context_notes"
    - path: "services/deepgram_service.py"
      provides: "Bereinigter Transkript-Handler ohne Auto-Erkennungs-Aufrufe"
  key_links:
    - from: "services/gatekeeper.py"
      to: "populate_context_notes"
      via: "__all__"
      pattern: "__all__.*populate_context_notes"
---

<objective>
gatekeeper.py auf Single-Function-Stub reduzieren (D-02) und deepgram_service.py von allen Auto-Erkennungs-Aufrufen bereinigen (C.R-REQ-1 + C.R-REQ-2 partiell).

Purpose: Die Auto-Erkennungs-Funktionen in gatekeeper.py sind konzeptuell unmöglich (DSGVO Single-Speaker — Sekretärs-Audio nie bei NERVE). CLAUDE.md Punkt 11 Modul-Rewrite-Trigger erfüllt. Punkt 3 Pruning-Pflicht: Dead Code nach Deletion sofort entfernen.

Output: Bereinigtes services/gatekeeper.py (nur populate_context_notes), bereinigtes services/deepgram_service.py (ohne UWG-Guard + Trigger/UWG-Detection-Blöcke).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-RESEARCH.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-CONTEXT.md

<interfaces>
<!-- gatekeeper.py ZIEL-ZUSTAND nach diesem Plan (aus RESEARCH.md): -->
<!--
"""Gatekeeper-Modul (Phase 08.23.2.I Foundation).

Auto-Erkennungs-Funktionen wurden in Phase 08.23.2.C.R entfernt (DSGVO Single-Speaker-Constraint
macht automatische Gatekeeper-Erkennung konzeptuell unmoeglich -- Sekretaers-Audio nie bei NERVE).
"""

def populate_context_notes(state: dict, entity: dict) -> None:
    """Foundation-Stub fuer Phase 08.23.2.I (Sekretaer-Uebergabe-Feature).
    ..."""
    pass


__all__ = ['populate_context_notes']
-->

<!-- deepgram_service.py — zu löschende Blöcke (aus RESEARCH.md): -->
<!-- Block 1 — UWG-Guard (Z.47-49): -->
<!--
    # UWG §7 Guard — Review Finding 5: keine weitere Verarbeitung nach Hard-Block
    if ls._session_state.get(sid, {}).get('state', {}).get('uwg_blocked'):
        return
-->
<!-- Block 2 — Trigger-Phrasen-Detection (Z.118-155): -->
<!--     try: detect_trigger_phrases(...) ... except: ... — komplett löschen -->
<!-- Block 3 — UWG-Hard-Block-Detection (Z.157-191): -->
<!--     try: detect_uwg_hard_block(...) ... except: ... — komplett löschen -->

<!-- ACHTUNG: manual_mode_toggle Handler (Z.723) NICHT löschen — wird in Plan 04 erweitert -->
<!-- ACHTUNG: deepgram Import-Block für gatekeeper AUCH löschen wenn durch Prune Dead -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gatekeeper.py auf populate_context_notes Stub reduzieren (D-02)</name>
  <files>services/gatekeeper.py</files>

  <read_first>
    - services/gatekeeper.py — vollständig lesen (alle Funktionen + Imports identifizieren)
  </read_first>

  <action>
services/gatekeeper.py KOMPLETT neu schreiben als Single-Function-Stub. Die Datei bleibt erhalten (D-02a), aber alle Funktionen außer populate_context_notes() werden gelöscht.

**Zu löschen (D-02b + D-02c + D-02f):**
- `_names_match()` Z.31-71 — Helper für classify_contact, Dead Code nach Löschung
- `classify_contact()` Z.76-119 — UNION/CONSENSUS-Voting
- `detect_trigger_phrases()` Z.124-139 — Trigger-Phrasen-Scan
- `detect_uwg_hard_block()` Z.144-153 — UWG §7 Hard-Block-Check
- `get_gatekeeper_phase_for_window()` Z.158-173 — Phasen-Heuristik
- `apply_hysteresis()` Z.178-232 — Hysterese-Zustandsautomat
- ALLE Dead Imports: `from services.anonymization import extract_entities`, `from services.ki_logik import (TRIGGER_PHRASES, UWG_HARD_BLOCK_PATTERNS, _phrase_matches)`, `from config.phase_transitions import (MIN_PHASE_DURATIONS, ALLOWED_TRANSITIONS, FORBIDDEN_TRANSITIONS, MODE_TRANSITION_AUTO, HYSTERESIS_REQUIRED_HINTS)`, `import re`, `import time`, `from typing import Optional`

**Zu behalten (D-02d):**
- `populate_context_notes()` — Foundation-Stub für Phase 08.23.2.I, UNVERÄNDERT

**Ziel-Zustand der Datei:**
```python
"""Gatekeeper-Modul (Phase 08.23.2.I Foundation).

Auto-Erkennungs-Funktionen wurden in Phase 08.23.2.C.R entfernt (DSGVO Single-Speaker-Constraint
macht automatische Gatekeeper-Erkennung konzeptuell unmoeglich -- Sekretaers-Audio nie bei NERVE).
"""


def populate_context_notes(state: dict, entity: dict) -> None:
    """Foundation-Stub fuer Phase 08.23.2.I (Sekretaer-Uebergabe-Feature).

    Wird in Phase 08.23.2.I aktiviert wenn Sekretaer-Uebergabe-Logik implementiert wird.
    Aktuell kein Verhalten.
    """
    pass


__all__ = ['populate_context_notes']
```

NICHT den Foundation-Code-Register-Eintrag ändern. populate_context_notes() Signatur und Docstring exakt beibehalten.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('services/gatekeeper.py'); print('Syntax OK')" && python -c "from services.gatekeeper import populate_context_notes; print('Import OK')"</automated>
  </verify>

  <acceptance_criteria>
    - services/gatekeeper.py importiert fehlerfrei
    - `grep -n "def classify_contact\|def apply_hysteresis\|def detect_trigger_phrases\|def detect_uwg_hard_block\|def get_gatekeeper_phase_for_window\|def _names_match" services/gatekeeper.py` → 0 Treffer
    - `grep -n "from services.anonymization\|from services.ki_logik\|from config.phase_transitions\|import re\|import time\|from typing" services/gatekeeper.py` → 0 Treffer (alle Dead Imports entfernt)
    - `grep -n "def populate_context_notes" services/gatekeeper.py` → 1 Treffer
    - `grep "__all__" services/gatekeeper.py` → Treffer mit 'populate_context_notes'
    - Datei hat weniger als 25 Zeilen
  </acceptance_criteria>

  <done>gatekeeper.py ist Single-Function-Stub mit nur populate_context_notes() und Modul-Docstring.</done>
</task>

<task type="auto">
  <name>Task 2: deepgram_service.py — UWG-Guard + Auto-Erkennungs-Blöcke löschen (C.R-REQ-1 + C.R-REQ-2)</name>
  <files>services/deepgram_service.py</files>

  <read_first>
    - services/deepgram_service.py Z.40-200 — alle drei Lösch-Blöcke vollständig lesen und genau abgrenzen
    - services/deepgram_service.py Z.720-760 — manual_mode_toggle Handler lesen (NICHT löschen)
  </read_first>

  <action>
Drei Blöcke aus deepgram_service.py löschen. NICHT den manual_mode_toggle Handler (Z.723) berühren — dieser wird in Plan 04 erweitert.

**Block 1: UWG-Guard (ungefähr Z.47-49) — LÖSCHEN:**
```python
    # UWG §7 Guard — Review Finding 5: keine weitere Verarbeitung nach Hard-Block
    if ls._session_state.get(sid, {}).get('state', {}).get('uwg_blocked'):
        return
```
Drei Zeilen inkl. Kommentar löschen. Prüfen ob Kommentar und if-Block beisammen stehen.

**Block 2: Trigger-Phrasen-Detection (ungefähr Z.118-155) — LÖSCHEN:**
```python
    # ── Req-7: Trigger-Phrasen-Detection ──────
    try:
        from services.gatekeeper import detect_trigger_phrases, detect_uwg_hard_block
        _tp_result = detect_trigger_phrases(text)
        if _tp_result['matches']:
            # ... sio.emit('trigger_phrase_hint', ...) + call_events INSERT ...
    except Exception as _tp_err:
        print(...)
```
Kompletten try/except-Block löschen — von der Kommentar-Zeile `# ── Req-7` bis Ende des except-Arms (inkl. print-Zeile). Den genauen Block-Anfang/-Ende aus der Datei lesen (exakte Zeilennummern können leicht abweichen).

**Block 3: UWG-Hard-Block-Detection (ungefähr Z.157-191) — LÖSCHEN:**
```python
    # ── Req-8: UWG §7 Hard-Block-Detection ──────
    try:
        if detect_uwg_hard_block(text):
            # ... uwg_blocked=True + call_events INSERT + sio.emit('uwg_hard_block') + return ...
    except Exception as _uwg_err:
        print(...)
```
Kompletten try/except-Block löschen — von der Kommentar-Zeile `# ── Req-8` bis Ende des except-Arms.

**Nach Löschung auch prüfen:** Falls `from services.gatekeeper import detect_trigger_phrases, detect_uwg_hard_block` als Top-Level-Import existiert (nicht nur im try-Block) → ebenfalls löschen. Control-Flow-Audit (CLAUDE.md Punkt 14): Sicherstellen dass kein anderer Code in der Datei auf uwg_blocked oder detect_uwg_hard_block referenziert.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('services/deepgram_service.py'); print('Syntax OK')" && grep -c "detect_trigger_phrases\|detect_uwg_hard_block\|uwg_blocked" services/deepgram_service.py || echo "0 Treffer — OK"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py` → 0 Treffer
    - `grep -n "uwg_blocked\|detect_uwg_hard_block\|uwg_hard_block" services/deepgram_service.py` → 0 Treffer
    - `grep -n "uwg" services/deepgram_service.py` → 0 Treffer (kein uwg-Restbestand)
    - services/deepgram_service.py importiert ohne Fehler: `python -c "import py_compile; py_compile.compile('services/deepgram_service.py')"`
    - manual_mode_toggle Handler noch vorhanden: `grep -n "manual_mode_toggle" services/deepgram_service.py` → ≥1 Treffer
  </acceptance_criteria>

  <done>deepgram_service.py enthält keine Auto-Erkennungs-Aufrufe mehr. UWG-Guard und beide Detection-Blöcke vollständig gelöscht. manual_mode_toggle Handler intakt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live-Loop → gatekeeper.py | Direkter Funktionsaufruf aus deepgram_service.py — wird nach Löschung nicht mehr aufgerufen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-02-01 | Tampering | gatekeeper.py Prune | mitigate | populate_context_notes() Signatur exakt beibehalten — Foundation-Code-Register-Vertrag erfüllen; D-02d |
| T-C.R-02-02 | Denial of Service | deepgram_service.py Prune | mitigate | Control-Flow-Audit nach Löschung: `grep uwg_blocked services/deepgram_service.py` → 0 vor Commit |
</threat_model>

<verification>
```bash
# Acceptance-Grep REQ-1 (partial — claude_service.py kommt in Plan 03):
grep -n "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py
# Erwartung: 0 Treffer

# Acceptance-Grep REQ-2 (partial — JS/CSS/base.html kommen in Wave 3):
grep -n "uwg\|UWG" services/deepgram_service.py
# Erwartung: 0 Treffer

# gatekeeper.py Funktions-Inventur:
grep -n "^def \|^class " services/gatekeeper.py
# Erwartung: nur 'def populate_context_notes'

# Syntax beider Dateien:
python -c "import py_compile; [py_compile.compile(f) for f in ['services/gatekeeper.py', 'services/deepgram_service.py']]; print('OK')"
```
</verification>

<success_criteria>
- gatekeeper.py: 6 Funktionen gelöscht, alle Dead Imports entfernt, populate_context_notes() intakt
- deepgram_service.py: UWG-Guard + 2 Detection-Blöcke gelöscht, 0 Treffer für detect_trigger_phrases/uwg_blocked
- Beide Dateien syntaktisch korrekt importierbar
- manual_mode_toggle Handler in deepgram_service.py unverändert vorhanden
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-02-SUMMARY.md` erstellen.
</output>
