---
phase: 08.23.2.C.R
plan: "07"
subsystem: tests
tags: [test-cleanup, behavioral-test, mode-initial, import-error-fix, acceptance-greps]
dependency_graph:
  requires:
    - 08.23.2.C.R-04
    - 08.23.2.C.R-05
    - 08.23.2.C.R-06
  provides:
    - test_gatekeeper_classifier.py + test_hysteresis.py geloescht (ImportError-Quelle beseitigt)
    - test_phase_classifier.py: apply_hysteresis Import + test_call_events_phase_change_persisted entfernt
    - test_session_state_phase_c.py: uwg_blocked-Test geloescht, Werte auf 'gatekeeper' korrigiert
    - test_mode_initial_db.py: Behavioral-Test fuer mode_initial DB-INSERT (REQ-6 Nachweis)
    - Alle 8 SPEC.md Acceptance-Greps PASS
  affects:
    - tests/test_gatekeeper_classifier.py (geloescht)
    - tests/test_hysteresis.py (geloescht)
    - tests/test_phase_classifier.py
    - tests/test_session_state_phase_c.py
    - tests/test_mode_initial_db.py (neu)
    - static/nerve.css
tech_stack:
  added: []
  patterns:
    - Mock-Session-Pattern via database.db.SessionLocal (lokal importiert in Funktion)
    - Behavioral-Test via add()-Side-Effect-Recording statt In-Memory-DB
key_files:
  created:
    - tests/test_mode_initial_db.py
  modified:
    - tests/test_phase_classifier.py
    - tests/test_session_state_phase_c.py
    - static/nerve.css
key_decisions:
  - "database.db.SessionLocal patchen (nicht services.live_session.SessionLocal — existiert nicht im Modul-Namespace, da lokal in create_call_for_sid importiert)"
  - "test_session_state_phase_c.py: contact_category/current_mode auf 'gatekeeper' korrigiert (REQ-5 Compliance)"
  - "nerve.css Kommentar 'UWG-Banner' bereinigt (REQ-2 Acceptance-Grep war rot wegen Kommentar-Residue)"
metrics:
  duration_seconds: 282
  completed_date: "2026-05-22"
  tasks_completed: 2
  tasks_total: 2
  files_created: 1
  files_modified: 4
requirements_completed:
  - C.R-REQ-1
  - C.R-REQ-2
  - C.R-REQ-5
  - C.R-REQ-6
---

# Phase 08.23.2.C.R Plan 07: Test-Cleanup + Acceptance-Verifikation Summary

**test_gatekeeper_classifier.py + test_hysteresis.py geloescht; test_phase_classifier.py partiell bereinigt; test_session_state_phase_c.py auf gatekeeper-Default korrigiert; test_mode_initial_db.py als REQ-6 DB-INSERT-Behavioral-Nachweis erstellt; alle 8 SPEC.md Acceptance-Greps PASS.**

## Performance

- **Duration:** ~5 Minuten
- **Started:** 2026-05-22T10:57:54Z
- **Completed:** 2026-05-22T11:02:36Z
- **Tasks:** 2
- **Files created:** 1
- **Files modified:** 4 (+ 2 geloescht)

## Accomplishments

### Task 1 — Test-Cleanup + mode_initial Behavioral-Test

**Geloeschte Dateien:**
1. `tests/test_gatekeeper_classifier.py` — testete classify_contact, detect_trigger_phrases, detect_uwg_hard_block (alle in C.R geloescht)
2. `tests/test_hysteresis.py` — testete apply_hysteresis (in C.R geloescht)

**Bereinigte Dateien:**
- `tests/test_phase_classifier.py`: inline `from services.gatekeeper import apply_hysteresis` Import + `test_call_events_phase_change_persisted()` Funktion entfernt
- `tests/test_session_state_phase_c.py`: `test_init_session_state_has_uwg_blocked` geloescht (Dead-State-Key), `contact_category`-Erwartung `'unknown'` → `'gatekeeper'`, `current_mode`-Erwartung `'cold_call'` → `'gatekeeper'`

**Neuer Behavioral-Test:**
`tests/test_mode_initial_db.py` — prueft via Mock-SessionLocal dass `create_call_for_sid()` tatsaechlich einen mode_initial-CallEvent schreibt:
- Patcht `database.db.SessionLocal` (korrekte Patch-Strategie — Funktion importiert lokal)
- Prueft `evt.event_type == 'mode_initial'`
- Prueft payload: mode, category, sid, timestamp
- 1 Test PASSED

### Task 2 — SPEC.md Acceptance-Greps

Alle 8 Greps verifiziert:

| REQ | Grep | Ergebnis |
|-----|------|----------|
| REQ-1 | detect_trigger_phrases\|classify_contact\|apply_hysteresis in deepgram/claude_service | PASS (0 Treffer) |
| REQ-2 | uwg\|UWG\|pip-uwg in pip-launcher.js / base.html / deepgram_service / nerve.css | PASS (0 Treffer nach Kommentar-Fix) |
| REQ-3 | ctrlKey.*gatekeeper\|Ctrl+G\|Ctrl+E in pip-launcher.js | PASS (0 Treffer) |
| REQ-4 | button.*pip-mode-indicator in base.html | PASS (≥1 Treffer — D-03a konform) |
| REQ-5 | pytest tests/test_live_session_gatekeeper.py | PASS (3 Tests GRUEN) |
| REQ-6 | pytest tests/test_mode_switch_event.py + test_mode_initial_db.py | PASS (3 Tests GRUEN) |
| REQ-7 | pipeline-warning in nerve.css | PASS (nur in .n-pipeline-warning Klasse, nicht in pip-mode-indicator Block) |
| REQ-8 | #06060a\|rgba(255,255,255,0.1)\|#F8FAFC in pip-launcher.js | PASS (0 Treffer) |
| REQ-9 | Vorzimmer in pip-launcher.js / base.html | PASS (0 Treffer) |

**pytest Abschluss-Lauf:** 493 PASSED, 17 FAILED (alle pre-existing), 4 SKIPPED — keine neuen Failures.

## Task Commits

| Commit | Typ | Beschreibung |
|--------|-----|--------------|
| 3c3d118 | chore | Test-Cleanup + mode_initial Behavioral-Test (REQ-1/2/5/6) |
| f76840e | chore | nerve.css Kommentar-Cleanup REQ-2 (UWG-Banner Referenz) |

## Verification Results

| Check | Ergebnis |
|-------|----------|
| `test -f tests/test_gatekeeper_classifier.py` | Exit 1 — PASS |
| `test -f tests/test_hysteresis.py` | Exit 1 — PASS |
| `grep apply_hysteresis tests/test_phase_classifier.py` | 0 Treffer — PASS |
| `grep test_call_events_phase_change_persisted tests/test_phase_classifier.py` | 0 Treffer — PASS |
| `pytest tests/test_live_session_gatekeeper.py -v` | 3/3 PASSED |
| `pytest tests/test_mode_switch_event.py -v` | 2/2 PASSED |
| `pytest tests/test_mode_initial_db.py -v` | 1/1 PASSED |
| `grep mode_initial services/live_session.py` | 4 Treffer — PASS |
| Alle REQ-1..REQ-9 Greps | ALLE PASS |
| `pytest tests/ -q --ignore=test_anonymization_reid.py` | 493 passed, 17 pre-existing failed |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] nerve.css Kommentar enthielt noch 'UWG-Banner' (REQ-2 Grep war rot)**
- **Found during:** Task 2 — REQ-2 Acceptance-Grep
- **Issue:** `static/nerve.css:3066` hatte Kommentar `/* Phase 08.23.2.C — Gatekeeper-Modus + UWG-Banner */` — Grep auf `uwg\|UWG` schlug an
- **Fix:** Kommentar zu `/* Phase 08.23.2.C.R — Gatekeeper-Modus */` aktualisiert
- **Files modified:** static/nerve.css
- **Commit:** f76840e

**2. [Rule 1 - Bug] test_session_state_phase_c.py: veraltete Erwartungswerte**
- **Found during:** Task 1 — Smoke-Run Analyse
- **Issue:** `test_init_session_state_has_contact_category` erwartete `'unknown'`, `test_init_session_state_has_current_mode` erwartete `'cold_call'` — beides veraltet nach Plan 03 (REQ-5 Default auf 'gatekeeper')
- **Fix:** Erwartungswerte auf `'gatekeeper'` korrigiert
- **Files modified:** tests/test_session_state_phase_c.py
- **Commit:** 3c3d118

**3. [Rule 1 - Bug] test_mode_initial_db.py: Patch-Strategie fuer lokalen Import**
- **Found during:** Task 1 — Test-Lauf
- **Issue:** Plan-Template patchte `services.live_session.SessionLocal` — existiert nicht im Modul-Namespace (create_call_for_sid importiert SessionLocal lokal). AttributeError beim Patch.
- **Fix:** `database.db.SessionLocal` gepatcht (korrekte Stelle fuer lokalen Import)
- **Files modified:** tests/test_mode_initial_db.py
- **Commit:** 3c3d118

## Known Stubs

Keine — alle Tests sind vollstaendig implementiert und getestet.

## Threat Flags

Keine neuen Threat-Surfaces. Test-only Aenderungen.

## Self-Check: PASSED

- tests/test_mode_initial_db.py: FOUND (erstellt)
- tests/test_phase_classifier.py: FOUND (modifiziert)
- tests/test_session_state_phase_c.py: FOUND (modifiziert)
- static/nerve.css: FOUND (modifiziert)
- Commit 3c3d118: vorhanden
- Commit f76840e: vorhanden
- test_gatekeeper_classifier.py: NICHT VORHANDEN (korrekt geloescht)
- test_hysteresis.py: NICHT VORHANDEN (korrekt geloescht)
