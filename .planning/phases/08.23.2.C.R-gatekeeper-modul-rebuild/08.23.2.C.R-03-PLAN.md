---
phase: 08.23.2.C.R
plan: "03"
type: execute
wave: 1
depends_on:
  - "08.23.2.C.R-01"
files_modified:
  - services/claude_service.py
  - services/live_session.py
autonomous: true
requirements:
  - C.R-REQ-1
  - C.R-REQ-5

must_haves:
  truths:
    - "claude_service.py: classify_contact + apply_hysteresis Block Z.1080-1183 vollständig gelöscht"
    - "Acceptance-Grep für claude_service.py gibt 0 Treffer (classify_contact, apply_hysteresis)"
    - "live_session.py: contact_category='gatekeeper' (war: 'unknown')"
    - "live_session.py: current_mode='gatekeeper' (war: 'cold_call')"
    - "live_session.py: uwg_blocked Key entfernt aus init_session_state()"
  artifacts:
    - path: "services/claude_service.py"
      provides: "Bereinigter Analyse-Loop ohne Auto-Erkennungs-Block"
    - path: "services/live_session.py"
      provides: "init_session_state() mit gatekeeper-Default + kein uwg_blocked"
  key_links:
    - from: "services/live_session.py"
      to: "init_session_state() return dict"
      via: "contact_category key"
      pattern: "contact_category.*gatekeeper"
---

<objective>
claude_service.py Auto-Erkennungs-Block (Z.1080-1183) löschen (zweiter Fundort aus RESEARCH.md — wird oft vergessen!) und live_session.py init_session_state() auf gatekeeper-Default setzen + uwg_blocked entfernen.

Purpose: Der Acceptance-Grep aus SPEC.md prüft BEIDE Dateien: `grep -rn "detect_trigger_phrases|classify_contact|apply_hysteresis" services/deepgram_service.py services/claude_service.py`. Wenn nur deepgram_service.py bereinigt wird (Plan 02), schlägt der Grep für claude_service.py noch fehl. Dieser Plan schließt REQ-1 ab. REQ-5 (Default gatekeeper) setzt direkt die State-Init.

Output: Bereinigter claude_service.py Analyse-Loop, bereinigtes live_session.py init_session_state().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-RESEARCH.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-CONTEXT.md

<interfaces>
<!-- claude_service.py — zu löschender Block (aus RESEARCH.md Z.232-244): -->
<!-- Der gesamte Block "# ── Phase 08.23.2.C: classify_contact + Hysterese + phase_change ──" (Z.1080-1183): -->
<!--   - Import von apply_hysteresis, classify_contact (Z.1082) -->
<!--   - classify_contact-Aufruf wenn category == 'unknown' (Z.1101-1124) -->
<!--   - apply_hysteresis-Aufruf alle 5 Zyklen (Z.1129-1181) -->
<!--   - phase_change-INSERT bei akzeptiertem Phasen-Wechsel (Z.1148-1172) -->
<!-- ACHTUNG: phase_change-INSERT INNERHALB des Blocks ist ein Referenz-Muster für Plan 04 -->
<!-- ACHTUNG: Sonstige phase_change-Aufrufe außerhalb dieses Blocks NICHT berühren -->

<!-- live_session.py init_session_state() — Ziel-Änderungen (aus RESEARCH.md Z.264-267): -->
<!-- 'contact_category': 'unknown'   →  'contact_category': 'gatekeeper' -->
<!-- 'current_mode':   'cold_call'   →  'current_mode': 'gatekeeper' -->
<!-- 'uwg_blocked': False             → ZEILE LÖSCHEN -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: claude_service.py — classify_contact + apply_hysteresis Block löschen (C.R-REQ-1 Abschluss)</name>
  <files>services/claude_service.py</files>

  <read_first>
    - services/claude_service.py Z.1070-1190 — gesamten zu löschenden Block + unmittelbaren Kontext lesen
  </read_first>

  <action>
Den Block `# ── Phase 08.23.2.C: classify_contact + Hysterese + phase_change ──` in claude_service.py vollständig löschen (ca. Z.1080-1183).

Dieser Block enthält:
1. `from services.gatekeeper import apply_hysteresis, classify_contact` — lokaler Import
2. classify_contact-Aufrufs-Block (wenn contact_category == 'unknown')
3. apply_hysteresis-Aufrufs-Block (alle 5 Zyklen) inkl. phase_change-INSERT

Den Block von der Kommentar-Zeile `# ── Phase 08.23.2.C: classify_contact + Hysterese + phase_change ──` bis zum Ende des Blocks löschen. Den Beginn und das Ende durch Lesen der Datei exakt bestimmen.

**NICHT berühren:**
- Alle anderen Teile von claude_service.py
- Andere phase_change-INSERT-Stellen außerhalb dieses Blocks (falls vorhanden)
- Der _PHASE_NAMES / _PHASE_NAMES_BY_MODE Mapping-Block (Phasen-Klassifikator bleibt)
- classify_phase() Funktion bleibt (nur die classify_contact-Aufrufe werden gelöscht)

**Control-Flow-Audit (CLAUDE.md Punkt 14):** Nach Löschung prüfen ob noch weitere Referenzen auf classify_contact oder apply_hysteresis in claude_service.py existieren (grep). Falls ja: ebenfalls löschen.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('services/claude_service.py'); print('Syntax OK')" && echo "REQ-1 Acceptance-Grep:" && (grep -n "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py services/claude_service.py || echo "0 Treffer — PASS")</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "classify_contact\|apply_hysteresis" services/claude_service.py` → 0 Treffer
    - VOLLSTÄNDIGER REQ-1 Acceptance-Grep: `grep -rn "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py services/claude_service.py` → 0 Treffer
    - services/claude_service.py hat valide Python-Syntax
    - `grep -n "def classify_phase" services/claude_service.py` → ≥1 Treffer (Phasen-Klassifikator bleibt)
  </acceptance_criteria>

  <done>claude_service.py enthält keine Auto-Erkennungs-Aufrufe. REQ-1 Acceptance-Grep sauber für beide Service-Dateien.</done>
</task>

<task type="auto">
  <name>Task 2: live_session.py — init_session_state() gatekeeper-Default + uwg_blocked entfernen (C.R-REQ-5)</name>
  <files>services/live_session.py</files>

  <read_first>
    - services/live_session.py Z.340-380 — init_session_state() vollständig lesen (Phase C Keys suchen)
  </read_first>

  <action>
Drei chirurgische Änderungen in init_session_state() — nur diese drei Zeilen ändern, nichts anderes:

**Änderung 1:** `'contact_category': 'unknown'` → `'contact_category': 'gatekeeper'`

**Änderung 2:** `'current_mode': 'cold_call'` → `'current_mode': 'gatekeeper'`

**Änderung 3:** Zeile mit `'uwg_blocked': False` vollständig löschen (inkl. etwaigem Kommentar auf derselben Zeile).

Die exakten Zeilennummern durch Lesen der Datei bestimmen — die Zeilen-Angaben aus RESEARCH.md (Z.356-368) können leicht abweichen wenn vorherige Phasen andere Zeilen eingefügt haben.

**Control-Flow-Audit (CLAUDE.md Punkt 14):** Nach Änderung mit `grep -rn "uwg_blocked" services/` prüfen ob noch andere Service-Dateien auf uwg_blocked zugreifen. Da deepgram_service.py in Plan 02 bereits bereinigt wurde, sollten 0 Treffer erscheinen. Falls doch Treffer → ebenfalls löschen.

**NICHT ändern:** Alle anderen Keys in init_session_state() — insbesondere context_notes, phase_hint_count, pending_phase, phase_entered_at, call_id bleiben unverändert.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('services/live_session.py'); print('Syntax OK')" && python -c "from services.live_session import init_session_state; s=init_session_state(); assert s['contact_category']=='gatekeeper', f'Got: {s[\"contact_category\"]}'; assert s['current_mode']=='gatekeeper', f'Got: {s[\"current_mode\"]}'; assert 'uwg_blocked' not in s; print('PASS: contact_category=gatekeeper, current_mode=gatekeeper, no uwg_blocked')"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from services.live_session import init_session_state; s=init_session_state(); assert s['contact_category']=='gatekeeper'"` → kein AssertionError
    - `python -c "from services.live_session import init_session_state; s=init_session_state(); assert s['current_mode']=='gatekeeper'"` → kein AssertionError
    - `python -c "from services.live_session import init_session_state; s=init_session_state(); assert 'uwg_blocked' not in s"` → kein AssertionError
    - `grep -n "uwg_blocked" services/live_session.py` → 0 Treffer
    - `grep -n "'contact_category'.*unknown" services/live_session.py` → 0 Treffer
    - `grep -n "'current_mode'.*cold_call" services/live_session.py` → 0 Treffer in init_session_state (andere Stellen erlaubt)
  </acceptance_criteria>

  <done>init_session_state() setzt contact_category='gatekeeper', current_mode='gatekeeper', kein uwg_blocked Key mehr. Test aus Plan 01 (test_live_session_gatekeeper.py) wird jetzt GRÜN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyse-Loop → gatekeeper.py | Block in claude_service.py ruft classify_contact/apply_hysteresis — nach Löschung kein Aufruf mehr |
| Session-State → Downstream-Handler | init_session_state() liefert Default-State an alle Handler — Wert-Änderung propagiert in alle Sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-03-01 | Tampering | claude_service.py Prune | mitigate | classify_phase() NICHT löschen — nur classify_contact-Aufrufe. Acceptance-Grep verifiziert beide Service-Dateien |
| T-C.R-03-02 | Spoofing | init_session_state() Default | accept | Default 'gatekeeper' ist korrekt (DSGVO Single-Speaker) — kein Security-Risk; bestätigt in SPEC.md |
</threat_model>

<verification>
```bash
# VOLLSTÄNDIGER REQ-1 Acceptance-Grep:
grep -rn "detect_trigger_phrases\|classify_contact\|apply_hysteresis" services/deepgram_service.py services/claude_service.py
# Erwartung: 0 Treffer

# REQ-5 State-Check:
python -c "from services.live_session import init_session_state; s=init_session_state(); print(s.get('contact_category'), s.get('current_mode'), 'uwg_blocked' in s)"
# Erwartung: gatekeeper gatekeeper False

# Test-Scaffold aus Plan 01 nun GRÜN:
pytest tests/test_live_session_gatekeeper.py -v
# Erwartung: 3 Tests PASSED
```
</verification>

<success_criteria>
- claude_service.py: 0 Treffer für classify_contact + apply_hysteresis
- REQ-1 Acceptance-Grep für beide Service-Dateien: 0 Treffer
- live_session.py: contact_category='gatekeeper', current_mode='gatekeeper', kein uwg_blocked
- test_live_session_gatekeeper.py: alle 3 Tests GRÜN
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-03-SUMMARY.md` erstellen.
</output>
