---
phase: 08.23.2.C.R
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - alembic/versions/0004_add_mode_events_to_call_events.py
  - tests/test_live_session_gatekeeper.py
  - tests/test_mode_switch_event.py
autonomous: true
requirements:
  - C.R-REQ-6

must_haves:
  truths:
    - "Alembic Migration 0004 existiert mit korrekter Chain 0003→0004"
    - "batch_alter_table() wird verwendet (kein raw ALTER TABLE SQL)"
    - "mode_switch und mode_initial sind im neuen CHECK-Constraint enthalten"
    - "alembic upgrade head läuft ohne Fehler durch"
    - "Test-Scaffolds für Wave-4-Tests existieren (noch fehlschlagend — RED-Phase)"
  artifacts:
    - path: "alembic/versions/0004_add_mode_events_to_call_events.py"
      provides: "Alembic Migration 0004 mit batch_alter_table"
      contains: "batch_op.drop_constraint"
    - path: "tests/test_live_session_gatekeeper.py"
      provides: "Test-Scaffold für init_session_state Default-Prüfung"
    - path: "tests/test_mode_switch_event.py"
      provides: "Test-Scaffold für mode_switch call_events-INSERT"
  key_links:
    - from: "alembic/versions/0004_add_mode_events_to_call_events.py"
      to: "database call_events.ck_call_events_event_type"
      via: "batch_op.create_check_constraint"
      pattern: "batch_op.create_check_constraint"
---

<objective>
Alembic Migration 0004 erstellen die den call_events CHECK-Constraint um 'mode_switch' und 'mode_initial' erweitert. Zusätzlich: Test-Scaffolds für Wave-4-Tests anlegen (noch fehlschlagend, da der zu testende Code erst in Wave 1-2 kommt — RED-Phase).

Purpose: Ohne Migration 0004 schlägt jeder mode_switch/mode_initial INSERT auf PostgreSQL mit CHECK-Violation fehl (SQLite ignoriert das still). Migration ist BLOCKING — alle Backend-Änderungen in Wave 1-2 setzen voraus dass der Constraint korrekt ist.

Output: alembic/versions/0004_add_mode_events_to_call_events.py, tests/test_live_session_gatekeeper.py, tests/test_mode_switch_event.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-RESEARCH.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-CONTEXT.md

<interfaces>
<!-- Referenz-Implementierung: alembic/versions/0003_add_phrases_mode.py (Pattern übernehmen) -->
<!-- Aktueller CHECK-Constraint (database/models.py Z.688): -->
<!-- "event_type IN ('transcript_chunk', 'suggestion_shown', 'reaction', 'phase_change', 'audio_health', 'objection_detected', 'consent_optin')" -->
<!-- Constraint-Name: 'ck_call_events_event_type' -->
<!-- alembic/env.py: render_as_batch=True bereits aktiv (Phase 08.23.2.C.1 Plan 04) -->

<!-- init_session_state() Ziel-Signatur (nach Wave 1 Plan 03 Änderung): -->
<!-- 'contact_category': 'gatekeeper'  (aktuell: 'unknown') -->
<!-- 'current_mode': 'gatekeeper'      (aktuell: 'cold_call') -->
<!-- 'uwg_blocked' Key wird gelöscht   (aktuell: False) -->

<!-- CallEvent Model (database/models.py): -->
<!-- class CallEvent(Base): -->
<!--     call_id = Column(Integer, ForeignKey('calls.id')) -->
<!--     event_type = Column(String(50), nullable=False) -->
<!--     event_ts_ms = Column(BigInteger, ...) -->
<!--     payload = Column(JSON) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic Migration 0004 erstellen (batch_alter_table) [BLOCKING]</name>
  <files>alembic/versions/0004_add_mode_events_to_call_events.py</files>

  <read_first>
    - alembic/versions/0003_add_phrases_mode.py — Migration-Pattern, Header-Format, Chain-Syntax
    - alembic/env.py — render_as_batch=True Konfiguration bestätigen (Z.30 + Z.47)
    - database/models.py Z.677-692 — aktueller CHECK-Constraint exakt lesen
  </read_first>

  <action>
Neue Datei `alembic/versions/0004_add_mode_events_to_call_events.py` erstellen:

```python
"""add mode_switch and mode_initial to call_events event_type check constraint

Revision ID: 0004
Revises: 0003
Create Date: 2026-05-21

Phase 08.23.2.C.R: Gatekeeper-Modus-Toggle ersetzt Auto-Erkennung.
mode_switch: Manueller Toggle durch Berater (manual_mode_toggle Socket-Handler).
mode_initial: Startmodus bei Call-Anlage (create_call_for_sid).
DSGVO Single-Speaker-Constraint: Auto-Erkennung konzeptuell unmoeglich.
"""
from alembic import op

revision = '0004'
down_revision = '0003'
branch_labels = None
depends_on = None

_NEW_CONSTRAINT = (
    "event_type IN ("
    "'transcript_chunk', 'suggestion_shown', 'reaction', 'phase_change', "
    "'audio_health', 'objection_detected', 'consent_optin', "
    "'mode_switch', 'mode_initial'"
    ")"
)

_OLD_CONSTRAINT = (
    "event_type IN ("
    "'transcript_chunk', 'suggestion_shown', 'reaction', 'phase_change', "
    "'audio_health', 'objection_detected', 'consent_optin'"
    ")"
)

_CONSTRAINT_NAME = 'ck_call_events_event_type'


def upgrade() -> None:
    with op.batch_alter_table('call_events') as batch_op:
        batch_op.drop_constraint(_CONSTRAINT_NAME, type_='check')
        batch_op.create_check_constraint(_CONSTRAINT_NAME, _NEW_CONSTRAINT)


def downgrade() -> None:
    with op.batch_alter_table('call_events') as batch_op:
        batch_op.drop_constraint(_CONSTRAINT_NAME, type_='check')
        batch_op.create_check_constraint(_CONSTRAINT_NAME, _OLD_CONSTRAINT)
```

WICHTIG: batch_alter_table() verwenden (nicht op.execute() mit raw SQL) — render_as_batch=True in env.py transformiert nur Alembic-Op-Aufrufe, nicht raw SQL. Ohne batch_op crasht SQLite mit "Cannot drop constraint".
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('alembic/versions/0004_add_mode_events_to_call_events.py'); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - alembic/versions/0004_add_mode_events_to_call_events.py existiert
    - Datei enthält `revision = '0004'` und `down_revision = '0003'`
    - Datei enthält `batch_op.drop_constraint` (NICHT `op.execute("ALTER TABLE`)
    - Datei enthält `batch_op.create_check_constraint`
    - `'mode_switch'` UND `'mode_initial'` im neuen Constraint-String enthalten
    - py_compile gibt kein SyntaxError
    - `grep "op.execute" alembic/versions/0004_add_mode_events_to_call_events.py` → 0 Treffer
  </acceptance_criteria>

  <done>Migration 0004 existiert, chain 0003→0004 korrekt, batch_alter_table verwendet, beide neuen event_types enthalten, kein raw SQL.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] alembic upgrade head ausführen</name>
  <files></files>

  <read_first>
    - alembic/versions/0004_add_mode_events_to_call_events.py — soeben erstellt, Syntax prüfen
    - alembic/env.py — DATABASE_URL Konfiguration
  </read_first>

  <action>
Vom Projekt-Root-Verzeichnis `alembic upgrade head` ausführen.

Erwartetes Ergebnis:
```
INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
INFO  [alembic.runtime.migration] Running upgrade 0003 -> 0004, add mode_switch and mode_initial to call_events event_type check constraint
```

Falls DATABASE_URL nicht gesetzt: `.env`-Datei laden oder DATABASE_URL=sqlite:///database/salesnerve.db setzen.

Falls Fehler "NotImplementedError: ALTER TABLE" erscheint: render_as_batch=True in alembic/env.py prüfen — muss in BEIDEN Funktionen (run_migrations_offline + run_migrations_online) in context.configure() stehen.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "from dotenv import load_dotenv; load_dotenv(); import subprocess; r = subprocess.run(['alembic', 'upgrade', 'head'], capture_output=True, text=True); print(r.stdout); print(r.stderr); exit(r.returncode)"</automated>
  </verify>

  <acceptance_criteria>
    - `alembic upgrade head` gibt Exit-Code 0
    - `alembic current` zeigt `0004 (head)`
    - KEIN "NotImplementedError" oder "Cannot drop constraint" in stderr
  </acceptance_criteria>

  <done>Datenbank-Schema ist auf Migration 0004 — call_events akzeptiert mode_switch und mode_initial event_types.</done>
</task>

<task type="auto">
  <name>Task 3: Test-Scaffolds anlegen (RED-Phase — Funktionen existieren noch nicht)</name>
  <files>tests/test_live_session_gatekeeper.py, tests/test_mode_switch_event.py</files>

  <read_first>
    - tests/test_session_state_phase_c.py — Referenz-Muster für init_session_state-Tests
    - services/live_session.py Z.356-370 — aktueller init_session_state() Aufbau
    - services/deepgram_service.py Z.723-750 — manual_mode_toggle Handler (für Integration-Test-Struktur)
    - database/models.py Z.677-692 — CallEvent-Modell
  </read_first>

  <action>
Zwei neue Test-Dateien erstellen. Diese Tests sind in Wave 0 noch ROT (fehlschlagend) — die Funktionen die sie testen werden in Wave 1-2 geändert.

**tests/test_live_session_gatekeeper.py:**
```python
"""Tests fuer init_session_state() Gatekeeper-Default.

Phase 08.23.2.C.R: init_session_state() muss contact_category='gatekeeper'
und current_mode='gatekeeper' liefern (DSGVO Single-Speaker — Default ist Sekretaer).
"""
import pytest
from services import live_session as ls


def test_init_session_state_default_contact_category():
    """contact_category muss 'gatekeeper' sein — nicht 'unknown'."""
    state = ls.init_session_state()
    assert state['contact_category'] == 'gatekeeper', (
        f"Expected 'gatekeeper', got {state['contact_category']!r}. "
        "Phase C.R: Default ist Sekretaer-Modus."
    )


def test_init_session_state_default_current_mode():
    """current_mode muss 'gatekeeper' sein — nicht 'cold_call'."""
    state = ls.init_session_state()
    assert state['current_mode'] == 'gatekeeper', (
        f"Expected 'gatekeeper', got {state['current_mode']!r}. "
        "Phase C.R: Default ist Sekretaer-Modus."
    )


def test_init_session_state_no_uwg_blocked_key():
    """uwg_blocked darf nicht mehr in init_session_state() vorkommen — UWG-System entfernt."""
    state = ls.init_session_state()
    assert 'uwg_blocked' not in state, (
        "uwg_blocked Key muss aus init_session_state() entfernt werden (UWG-System geloescht in C.R)."
    )
```

**tests/test_mode_switch_event.py:**
```python
"""Tests fuer mode_switch call_events INSERT im manual_mode_toggle Handler.

Phase 08.23.2.C.R: Jeder Toggle erzeugt einen call_events-Eintrag mit
event_type='mode_switch' und 4 payload-Feldern (D-04d).
"""
import pytest
from unittest.mock import patch, MagicMock
from database.models import CallEvent


def test_mode_switch_event_payload_fields():
    """mode_switch call_events-Eintrag muss 4 Pflicht-Felder im payload haben."""
    # Simulierter Eintrag wie er nach manual_mode_toggle entstehen soll
    event = CallEvent(
        call_id=1,
        event_type='mode_switch',
        event_ts_ms=1000000,
        payload={
            'old_mode': 'gatekeeper',
            'new_mode': 'cold_call',
            'old_category': 'gatekeeper',
            'new_category': 'target',
            'timestamp': 123.456,
        },
    )
    assert event.event_type == 'mode_switch'
    assert 'old_mode' in event.payload
    assert 'new_mode' in event.payload
    assert 'old_category' in event.payload
    assert 'new_category' in event.payload


def test_mode_initial_event_payload_fields():
    """mode_initial call_events-Eintrag muss mode, category, sid, timestamp haben."""
    import time
    event = CallEvent(
        call_id=1,
        event_type='mode_initial',
        event_ts_ms=int(time.time() * 1000),
        payload={
            'mode': 'gatekeeper',
            'category': 'gatekeeper',
            'sid': 'test-sid-123',
            'timestamp': time.monotonic(),
        },
    )
    assert event.event_type == 'mode_initial'
    assert 'mode' in event.payload
    assert 'category' in event.payload
    assert 'sid' in event.payload
    assert 'timestamp' in event.payload
```

Diese Tests sind Runtime-Behavior-Tests (prüfen Funktionsaufrufe und Objekt-State) — kein Source-Presence-Test (kein inspect.getsource).
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import py_compile; py_compile.compile('tests/test_live_session_gatekeeper.py'); py_compile.compile('tests/test_mode_switch_event.py'); print('Syntax OK')"</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_live_session_gatekeeper.py existiert mit 3 Test-Funktionen
    - tests/test_mode_switch_event.py existiert mit 2 Test-Funktionen
    - Beide Dateien haben valide Python-Syntax (py_compile ohne Fehler)
    - `grep "inspect.getsource\|open.*read\|subprocess.*grep" tests/test_live_session_gatekeeper.py tests/test_mode_switch_event.py` → 0 Treffer (kein Source-Presence-Test)
    - In Wave 0 dürfen test_live_session_gatekeeper.py Tests FEHLSCHLAGEN (contact_category ist noch 'unknown') — das ist erwartet (RED-Phase)
  </acceptance_criteria>

  <done>Beide Test-Scaffold-Dateien existieren mit valider Syntax. Tests schlagen in Wave 0 fehl (erwartet) und werden in Wave 4 grün sein.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration-Script → SQLite/PostgreSQL | Alembic-Operationen mutieren das DB-Schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-01-01 | Tampering | Alembic Migration 0004 | mitigate | batch_alter_table() statt raw SQL — verhindert SQLite-Crash + Schema-Divergenz zwischen Environments |
| T-C.R-01-02 | Denial of Service | alembic upgrade head | accept | Migration schlägt fehl bei ungültiger Chain → alembic_version-Tabelle verhindert Doppel-Anwendung; idempotent by design |
</threat_model>

<verification>
```bash
# Chain korrekt:
alembic history | grep "0004\|0003"
# Erwartung: 0004 -> 0003 -> 0002 -> ...

# batch_alter_table vorhanden:
grep "batch_op.drop_constraint" alembic/versions/0004_add_mode_events_to_call_events.py

# Kein raw SQL:
grep "op.execute" alembic/versions/0004_add_mode_events_to_call_events.py
# Erwartung: 0 Treffer

# mode_initial und mode_switch im Constraint:
grep "mode_switch\|mode_initial" alembic/versions/0004_add_mode_events_to_call_events.py
```
</verification>

<success_criteria>
- alembic upgrade head Exit-Code 0
- alembic current zeigt 0004 (head)
- Migration nutzt batch_alter_table() (nicht raw SQL)
- mode_switch und mode_initial im neuen CHECK-Constraint
- 2 Test-Scaffold-Dateien mit valider Syntax vorhanden
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-01-SUMMARY.md` erstellen.
</output>
