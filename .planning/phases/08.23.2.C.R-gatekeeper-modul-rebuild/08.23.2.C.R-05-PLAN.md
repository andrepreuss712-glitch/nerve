---
phase: 08.23.2.C.R
plan: "05"
type: execute
wave: 3
depends_on:
  - "08.23.2.C.R-02"
  - "08.23.2.C.R-03"
files_modified:
  - static/nerve.css
  - templates/base.html
autonomous: true
requirements:
  - C.R-REQ-4
  - C.R-REQ-5
  - C.R-REQ-7
  - C.R-REQ-8

must_haves:
  truths:
    - "nerve.css: 7 neue CSS-Tokens in :root definiert (pip-gatekeeper-bg/text/bg-hover, pip-bg, badge-default-bg/text, btn-primary-text)"
    - "nerve.css: .pip-mode-indicator[data-mode='gatekeeper'] nutzt --pip-gatekeeper-bg + --pip-gatekeeper-text"
    - "nerve.css: pipeline-warning-bg NICHT mehr im pip-mode-indicator Block"
    - "nerve.css: .pip-uwg-banner + .pip-uwg-banner.is-visible + .pip-uwg-banner__close gelöscht"
    - "nerve.css: #pip-mode-indicator Button-Reset (border:none, min-height:32px, cursor:pointer, color:inherit, appearance:none, hover)"
    - "base.html: pip-uwg-banner div vollständig gelöscht"
    - "base.html: pip-mode-indicator ist jetzt button-Element mit data-mode='gatekeeper' + Text 'Sekretär'"
  artifacts:
    - path: "static/nerve.css"
      provides: "Neue gatekeeper CSS-Tokens + Button-Reset + UWG-CSS gelöscht"
      contains: "--pip-gatekeeper-bg, --btn-primary-text"
    - path: "templates/base.html"
      provides: "pip-mode-indicator als button + pip-uwg-banner gelöscht"
  key_links:
    - from: "templates/base.html"
      to: "static/nerve.css"
      via: "#pip-mode-indicator Button-Klasse + CSS-Tokens"
      pattern: "pip-mode-indicator.*button"
---

<objective>
nerve.css CSS-Token-System erweitern (7 neue Tokens, UWG-CSS löschen, pip-mode-indicator Button-Reset) und base.html span→button Migration + pip-uwg-banner löschen.

Purpose: C.R-REQ-7: pip-mode-indicator nutzte --pipeline-warning-bg (semantisch falsch — Amber ist für Pipeline-Fehler). Neue Tokens geben dem Sekretär-Modus eigene semantische Farbe (Blau). C.R-REQ-4: span→button bringt native Tastatur-Zugänglichkeit + Klickbarkeit. C.R-REQ-2: UWG-CSS-Reste entfernen.

Output: nerve.css mit 7 neuen Tokens + bereinigtem pip-mode-indicator + gelöschtem UWG-CSS. base.html mit button-Element als pip-mode-indicator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-RESEARCH.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-UI-SPEC.md
@.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-CONTEXT.md

<interfaces>
<!-- nerve.css neue Tokens (aus UI-SPEC + RESEARCH.md — EXAKTE Werte): -->
<!--   --pip-gatekeeper-bg: rgba(96, 165, 250, 0.12);   Sekretär-Indikator bg -->
<!--   --pip-gatekeeper-text: #3b82f6;                   Sekretär-Indikator text (Blue-500) -->
<!--   --pip-gatekeeper-bg-hover: rgba(96, 165, 250, 0.22);  Hover-Feedback -->
<!--   --pip-bg: #F8FAFC;                               PiP-Body-Hintergrund -->
<!--   --badge-default-bg: rgba(100, 116, 139, 0.08);   Default-Badge bg -->
<!--   --badge-default-text: #475569;                    Default-Badge text (Slate-600) -->
<!--   --btn-primary-text: #06060a;                      Button Primary Text (Plan 06 Hex-Sweep) -->

<!-- nerve.css zu löschender CSS-Block (Z.3076-3102): -->
<!--   .pip-uwg-banner { ... }             LÖSCHEN -->
<!--   .pip-uwg-banner.is-visible { ... }  LÖSCHEN -->
<!--   .pip-uwg-banner__close { ... }      LÖSCHEN -->

<!-- nerve.css .pip-mode-indicator[data-mode="gatekeeper"] — ZU ÄNDERN: -->
<!--   AKTUELL: background-color: var(--pipeline-warning-bg); color: var(--pipeline-warning-text); -->
<!--   SOLL:    background-color: var(--pip-gatekeeper-bg);   color: var(--pip-gatekeeper-text); -->

<!-- nerve.css .pip-ewb-btn[data-mode-button="gatekeeper"] — ZU ÄNDERN: -->
<!--   border-color: var(--pipeline-warning-text) → border-color: var(--pip-gatekeeper-text) -->

<!-- nerve.css #pip-mode-indicator Button-Reset (NEU): -->
<!--   border: none; -->
<!--   background: var(--glass-bg); -->
<!--   font-family: inherit; -->
<!--   color: inherit; -->
<!--   appearance: none; -->
<!--   font-size: 11px; -->
<!--   font-weight: 600; -->
<!--   min-height: 32px; -->
<!--   cursor: pointer; -->
<!-- #pip-mode-indicator:hover { background-color: var(--pip-gatekeeper-bg-hover); } -->

<!-- base.html aktueller Zustand (Z.557-562): -->
<!-- <div id="pip-uwg-banner" class="pip-uwg-banner" role="alert"> -->
<!--   <span id="pip-uwg-banner-text">UWG §7: Anrufer hat Opt-Out erklärt.</span> -->
<!--   <button type="button" id="pip-uwg-banner-close" class="pip-uwg-banner__close" aria-label="Schließen">×</button> -->
<!-- </div> -->
<!-- <span id="pip-mode-indicator" class="pip-mode-indicator" data-mode="cold_call">Cold-Call</span> -->

<!-- base.html ZIEL-Zustand: -->
<!-- <!-- Phase 08.23.2.C.R: Modus-Toggle-Button (manuell, DSGVO Single-Speaker) --> -->
<!-- <button type="button" id="pip-mode-indicator" class="pip-mode-indicator" -->
<!--         data-mode="gatekeeper" -->
<!--         aria-label="Modus wechseln zu Entscheider">Sekretär</button> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: nerve.css — 7 neue Tokens + Button-Reset + UWG-CSS löschen + Token-Updates (C.R-REQ-7)</name>
  <files>static/nerve.css</files>

  <read_first>
    - static/nerve.css Z.3055-3110 — vollständigen Phase-C-Block lesen (pip-mode-indicator + pip-uwg-banner)
    - static/nerve.css Z.1-30 — :root Block prüfen (wo neue Tokens einfügen)
  </read_first>

  <action>
Vier CSS-Änderungen in nerve.css:

**Änderung 1: Neue Tokens im :root Block einfügen:**
Nach den bestehenden Phase-C oder Phase-C.R Tokens (oder am Ende des :root Blocks) einfügen:
```css
/* Phase 08.23.2.C.R — Gatekeeper-Toggle + PiP-Body + Default-Badge */
--pip-gatekeeper-bg: rgba(96, 165, 250, 0.12);
--pip-gatekeeper-text: #3b82f6;
--pip-gatekeeper-bg-hover: rgba(96, 165, 250, 0.22);
--pip-bg: #F8FAFC;
--badge-default-bg: rgba(100, 116, 139, 0.08);
--badge-default-text: #475569;
--btn-primary-text: #06060a;
```

**Änderung 2: .pip-mode-indicator[data-mode="gatekeeper"] Token-Update:**
```css
/* AKTUELL (löschen/ersetzen): */
background-color: var(--pipeline-warning-bg);
color: var(--pipeline-warning-text);

/* SOLL: */
background-color: var(--pip-gatekeeper-bg);
color: var(--pip-gatekeeper-text);
```

**Änderung 3: #pip-mode-indicator Button-Reset hinzufügen (Pitfall 4 Vermeidung):**
```css
/* span→button Migration: Browser-Button-Defaults reset */
#pip-mode-indicator {
    border: none;
    background: var(--glass-bg);
    font-family: inherit;
    color: inherit;
    appearance: none;
    font-size: 11px;
    font-weight: 600;
    min-height: 32px;
    cursor: pointer;
    padding: 2px 8px;
    border-radius: 4px;
}
#pip-mode-indicator:hover {
    background-color: var(--pip-gatekeeper-bg-hover);
}
```
Direkt nach dem `.pip-mode-indicator` Block einfügen.

**Änderung 4: .pip-uwg-banner Blöcke LÖSCHEN (C.R-REQ-2):**
Folgende Blöcke vollständig löschen:
- `.pip-uwg-banner { ... }` (inkl. aller Regeln)
- `.pip-uwg-banner.is-visible { ... }`
- `.pip-uwg-banner__close { ... }`

Außerdem: `.pip-ewb-btn[data-mode-button="gatekeeper"]` border-color von `var(--pipeline-warning-text)` zu `var(--pip-gatekeeper-text)` ändern.

CLAUDE.md CSS-Token-Pflicht: Keine Hex-Farben in Klassen-Regeln oder style-Assignments.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "pip-gatekeeper\|btn-primary-text" static/nerve.css | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "pip-gatekeeper" static/nerve.css` → ≥4 Treffer (Token-Definition + Klassen-Verwendung)
    - `grep "btn-primary-text" static/nerve.css` → ≥1 Treffer (Token-Definition im :root)
    - `grep "--pip-bg\|badge-default" static/nerve.css` → ≥2 Treffer
    - `grep "pipeline-warning" static/nerve.css` → 0 Treffer im pip-mode-indicator-Block (grep auf `pip-mode-indicator` Sektion prüfen)
    - `grep "pip-uwg-banner" static/nerve.css` → 0 Treffer
    - `grep "#pip-mode-indicator" static/nerve.css` → ≥1 Treffer (Button-Reset vorhanden)
    - `grep "min-height.*32px\|cursor.*pointer" static/nerve.css` → ≥1 Treffer (#pip-mode-indicator Block)
    - `grep "color.*inherit\|appearance.*none" static/nerve.css` → ≥1 Treffer (#pip-mode-indicator Button-Reset)
  </acceptance_criteria>

  <done>nerve.css hat 7 neue Tokens (inkl. --btn-primary-text), Button-Reset mit min-height 32px + color:inherit + appearance:none, UWG-CSS gelöscht, pip-mode-indicator[gatekeeper] nutzt korrekte Tokens statt pipeline-warning-bg.</done>
</task>

<task type="auto">
  <name>Task 2: base.html — pip-uwg-banner löschen + span→button Migration (D-03 + C.R-REQ-2 + C.R-REQ-4)</name>
  <files>templates/base.html</files>

  <read_first>
    - templates/base.html Z.545-575 — PiP-DOM-Block vollständig lesen (uwg-banner + mode-indicator)
  </read_first>

  <action>
Zwei Änderungen in base.html:

**Änderung 1: pip-uwg-banner div vollständig löschen (C.R-REQ-2):**
```html
<!-- Löschen — komplett: -->
<div id="pip-uwg-banner" class="pip-uwg-banner" role="alert">
  <span id="pip-uwg-banner-text">UWG §7: Anrufer hat Opt-Out erklärt.</span>
  <button type="button" id="pip-uwg-banner-close" class="pip-uwg-banner__close" aria-label="Schließen">×</button>
</div>
```
Den div inkl. aller Kinder-Elemente löschen.

**Änderung 2: span→button Migration für pip-mode-indicator (D-03a bis D-03h):**
```html
<!-- AKTUELL (löschen): -->
<span id="pip-mode-indicator" class="pip-mode-indicator" data-mode="cold_call">Cold-Call</span>

<!-- SOLL: -->
<!-- Phase 08.23.2.C.R: Modus-Toggle-Button (manuell, DSGVO Single-Speaker) -->
<button type="button" id="pip-mode-indicator" class="pip-mode-indicator"
        data-mode="gatekeeper"
        aria-label="Modus wechseln zu Entscheider">Sekretär</button>
```

Gemäß D-03b: aria-label zeigt ZIELmodus im HTML-Default (wenn Sekretär angezeigt → "wechseln zu Entscheider"). Nach Toggle-Klick aktualisiert pip-launcher.js das aria-label dynamisch (Plan 06).

Kommentar-Zeile aktualisieren: `<!-- Phase 08.23.2.C: UWG-Hard-Block-Banner + Mode-Indikator -->` → `<!-- Phase 08.23.2.C.R: Modus-Toggle-Button (manuell, DSGVO Single-Speaker) -->`
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "pip-mode-indicator\|pip-uwg-banner\|Vorzimmer\|Cold-Call" templates/base.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "pip-uwg-banner" templates/base.html` → 0 Treffer
    - `grep -n "pip-mode-indicator" templates/base.html` → Treffer mit `<button` (nicht `<span`)
    - `grep -n 'data-mode="gatekeeper"' templates/base.html` → 1 Treffer (im pip-mode-indicator)
    - `grep -n 'aria-label="Modus wechseln zu Entscheider"' templates/base.html` → 1 Treffer
    - `grep -n ">Sekretär<" templates/base.html` → 1 Treffer (Textinhalt)
    - `grep -n "Cold-Call\|Vorzimmer" templates/base.html` → 0 Treffer im pip-mode-indicator Block
    - `grep -n 'type="button"' templates/base.html` → Treffer für pip-mode-indicator (button-Attribut)
  </acceptance_criteria>

  <done>base.html: pip-uwg-banner gelöscht, pip-mode-indicator ist button-Element mit data-mode='gatekeeper', aria-label='Modus wechseln zu Entscheider', Textinhalt 'Sekretär'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → DOM | button-Element öffnet neue Klick-Oberfläche — Klick-Handler in Plan 06 verdrahtet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C.R-05-01 | Tampering | button type="button" | mitigate | type="button" verhindert form-submit — korrekt gesetzt (D-03a) |
| T-C.R-05-02 | Information Disclosure | aria-label | accept | aria-label zeigt Ziel-Modus — kein PII, kein Security-Risk; Accessibility-Anforderung erfüllt |
</threat_model>

<verification>
```bash
# REQ-7 Acceptance-Check:
grep "pip-gatekeeper" static/nerve.css | wc -l
# Erwartung: ≥4

grep "btn-primary-text" static/nerve.css
# Erwartung: ≥1 Treffer (Token-Definition)

grep "pipeline-warning" static/nerve.css
# MUSS 0 Treffer im pip-mode-indicator Block haben

# REQ-2 CSS-Part:
grep "pip-uwg-banner" static/nerve.css
# Erwartung: 0 Treffer

# base.html button Migration:
grep -n "pip-mode-indicator" templates/base.html
# Erwartung: <button type="button" id="pip-mode-indicator" ...
```
</verification>

<success_criteria>
- nerve.css: 7 neue CSS-Tokens definiert (inkl. --btn-primary-text: #06060a), UWG-CSS weg, Button-Reset vorhanden (border:none, color:inherit, appearance:none, min-height:32px), pip-gatekeeper-Tokens im gatekeeper-Selektor
- base.html: pip-uwg-banner weg, pip-mode-indicator ist button mit data-mode='gatekeeper' + 'Sekretär' + aria-label
- Keine pipeline-warning-Tokens mehr im pip-mode-indicator-Block
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.C.R-gatekeeper-modul-rebuild/08.23.2.C.R-05-SUMMARY.md` erstellen.
</output>
