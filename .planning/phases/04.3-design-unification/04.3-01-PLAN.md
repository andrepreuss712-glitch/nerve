---
phase: 04.3-design-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/app.html
autonomous: true
requirements:
  - DU-01

must_haves:
  truths:
    - "User can click a Dashboard link in the Live-Assistent header and navigate to /dashboard at any time"
  artifacts:
    - path: "templates/app.html"
      provides: "Permanent Dashboard back-link in header"
      contains: "href=\"/dashboard\""
  key_links:
    - from: "templates/app.html"
      to: "/dashboard"
      via: "anchor tag in .header-nav"
      pattern: "back-link.*href=./dashboard"
---

<objective>
Add a permanent "Dashboard" back-link to the Live-Assistent header so users can always navigate away from /live.

Purpose: The Beenden button currently triggers a confirm dialog and post-call flow — there is no simple, always-visible way to leave /live. This is a critical UX bug (DU-01).
Output: A `<a class="back-link" href="/dashboard">` link in the app.html header that works unconditionally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-design-unification/04.3-CONTEXT.md
@.planning/phases/04.3-design-unification/04.3-RESEARCH.md

<interfaces>
<!-- app.html is STANDALONE — does NOT extend base.html -->
<!-- Header structure (app.html lines 14-22, 605-612): -->
<!-- .header has .nav-mark on left, .header-nav in center, .header-right on right -->
<!-- .back-link CSS class already defined (lines 23-26) — styled muted button with hover -->
<!-- .header-right contains: status dot, pause button, beenden button, kompakt button -->
<!-- .header-nav contains: mode badge -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add permanent Dashboard back-link to app.html header</name>
  <files>templates/app.html</files>
  <read_first>
    - templates/app.html (full file — standalone template, need to see header structure around lines 600-615)
  </read_first>
  <action>
Per DU-01 (locked decision): Add a Dashboard navigation link to the Live-Assistent header.

In `templates/app.html`, find the `.header` div (around line 605). Inside the left area (after the `.nav-mark` div), add:

```html
<a class="back-link" href="/dashboard">&larr; Dashboard</a>
```

Place it AFTER the `.nav-mark` div and BEFORE `.header-nav`. The `.back-link` CSS class is already defined at lines 23-26 with appropriate styling (muted text, border, hover effect).

Do NOT modify the `beenden()` function — it handles session cleanup and post-call analytics. The back-link is an ADDITIONAL always-available escape, not a replacement for beenden().

Do NOT use `history.back()` — use `href="/dashboard"` because the Live page sets up Socket.IO connections and AudioContext state that could cause issues with browser history navigation.
  </action>
  <verify>
    <automated>grep -n "back-link.*dashboard" templates/app.html</automated>
  </verify>
  <acceptance_criteria>
    - `templates/app.html` contains `<a class="back-link" href="/dashboard">` (exact class and href)
    - The link appears inside the `.header` div
    - The existing `beenden()` function and `#t-endBtn` button are NOT modified
    - The `.back-link` CSS class definition (lines 23-26) is NOT modified
  </acceptance_criteria>
  <done>A "Dashboard" back-link is visible in the Live-Assistent header. Clicking it navigates to /dashboard. The existing Beenden button continues to work for proper session cleanup.</done>
</task>

</tasks>

<verification>
- Open /live in browser, confirm "Dashboard" link visible in header
- Click the link, confirm navigation to /dashboard
- Confirm Beenden button still works (triggers confirm dialog and post-call flow)
</verification>

<success_criteria>
User can always navigate from /live to /dashboard via the header back-link without needing to interact with the Beenden flow.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-design-unification/04.3-01-SUMMARY.md`
</output>
