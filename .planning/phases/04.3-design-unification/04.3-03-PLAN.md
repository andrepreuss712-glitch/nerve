---
phase: 04.3-design-unification
plan: 03
type: execute
wave: 3
depends_on:
  - 04.3-02
files_modified:
  - templates/settings.html
  - templates/logs_page.html
autonomous: true
requirements:
  - DU-04
  - DU-05

must_haves:
  truths:
    - "Settings page has dark background with seamlessly integrated cards"
    - "Analytics/Logs table has dark page background and dark-themed rows"
  artifacts:
    - path: "templates/settings.html"
      provides: "Dark theme colors for cards, inputs, tabs, toggles, modals"
    - path: "templates/logs_page.html"
      provides: "Dark theme table with alternating rows"
  key_links:
    - from: "templates/settings.html"
      to: "static/nerve.css"
      via: "CSS custom properties consumed by inline styles"
      pattern: "#1C2333|#0D1117|#00D4AA"
---

<objective>
Apply unified dark theme to Einstellungen and Analytics/Logs pages by replacing all hardcoded legacy colors.

Purpose: These two pages still use legacy hex values (#1a1a2e, #0c0c18, #E8B040) instead of the target dark color scheme. Cards appear to "float" on mismatched backgrounds (DU-04, DU-05).
Output: Both pages use the target color scheme consistently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.3-design-unification/04.3-CONTEXT.md
@.planning/phases/04.3-design-unification/04.3-RESEARCH.md
@.planning/phases/04.3-design-unification/04.3-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix settings.html hardcoded colors to target dark theme</name>
  <files>templates/settings.html</files>
  <read_first>
    - templates/settings.html (full file — need to see all inline style blocks and hardcoded hex values)
  </read_first>
  <action>
Per DU-04 (locked): Update ALL hardcoded color values in settings.html inline `<style>` blocks.

**Exact replacements (search and replace in the `<style>` block):**

1. `.settings-card{background:#1a1a2e}` -> `.settings-card{background:#1C2333}`
2. `.tab-btn.active{color:#E8B040;border-bottom-color:#E8B040}` -> `.tab-btn.active{color:#00D4AA;border-bottom-color:#00D4AA}`
3. `.form-input,.form-select{background:#0c0c18;border:1px solid #1a1a2e;color:#f0ebe0}` -> `.form-input,.form-select{background:#1C2333;border:1px solid #2D3748;color:#FFFFFF}`
4. `.toggle input:checked + .toggle-track{background:#E8B040}` -> `.toggle input:checked + .toggle-track{background:#00D4AA}`
5. `.plan-card.active{border-color:#E8B040}` -> `.plan-card.active{border-color:#00D4AA}`
6. `.plan-price{color:#E8B040}` -> `.plan-price{color:#00D4AA}`
7. `.modal-box{background:#0c0c18;border-top:3px solid #E8B040}` -> `.modal-box{background:#1C2333;border-top:3px solid #00D4AA}`
8. `.info-box strong{color:#E8B040}` -> `.info-box strong{color:#00D4AA}`
9. Any remaining `#1a1a2e` -> `#1C2333`
10. Any remaining `#0c0c18` -> `#1C2333`
11. Any remaining `#E8B040` (accent uses) -> `#00D4AA`
12. Any remaining `#f0ebe0` (text) -> `#FFFFFF`
13. Any remaining `#2e2e40` or `#2e2e48` (borders) -> `#2D3748`

Also check for inline `style="..."` attributes on HTML elements (e.g., plan cards, usage bars) that contain hardcoded colors and update those too.

Do NOT change any Jinja template logic or backend-related attributes.
  </action>
  <verify>
    <automated>grep -c "#1a1a2e\|#0c0c18\|#E8B040\|#f0ebe0" templates/settings.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#1a1a2e' templates/settings.html` returns 0
    - `grep -c '#0c0c18' templates/settings.html` returns 0
    - `grep -c '#E8B040' templates/settings.html` returns 0 (all accent uses replaced with #00D4AA)
    - `grep '#1C2333' templates/settings.html` returns multiple matches (card bg, input bg)
    - `grep '#2D3748' templates/settings.html` returns at least 1 match (borders)
    - `grep '#00D4AA' templates/settings.html` returns multiple matches (active tab, toggle, prices)
  </acceptance_criteria>
  <done>Settings page uses target dark color scheme. Cards are #1C2333 on #0D1117 page background. All accent colors are teal #00D4AA.</done>
</task>

<task type="auto">
  <name>Task 2: Fix logs_page.html hardcoded colors and add dark table styling</name>
  <files>templates/logs_page.html</files>
  <read_first>
    - templates/logs_page.html (full file — need to see all inline styles and table markup)
  </read_first>
  <action>
Per DU-05 (locked): Update all hardcoded colors in logs_page.html to the target dark theme.

**Exact replacements in the `<style>` block:**

1. `.log-profil-tag{background:#1a1a2e;border:1px solid #2e2e40}` -> `.log-profil-tag{background:#1C2333;border:1px solid #2D3748}`
2. `.empty-state{color:#444466}` -> `.empty-state{color:#8B949E}`
3. Any `/live` link with `color:#E8B040` -> `color:#00D4AA`
4. Any remaining `#1a1a2e` -> `#1C2333`
5. Any remaining `#2e2e40` or `#2e2e48` (borders) -> `#2D3748`

**Add alternating row styling for the table:**
Find the `.tbl` or table styling section. Add or update:
```css
.tbl tr:nth-child(odd) td { background: #1C2333; }
.tbl tr:nth-child(even) td { background: #161B22; }
.tbl thead th { background: #0D1117; color: #8B949E; border-bottom: 1px solid #2D3748; }
```

If the table uses `.tbl-wrap` with a background, update it to `background: #0D1117` or `background: transparent` (page bg handles it).

Also search for any inline `style="background:..."` attributes on HTML elements and update hardcoded values.
  </action>
  <verify>
    <automated>grep -c "#1a1a2e\|#444466\|#2e2e40" templates/logs_page.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#1a1a2e' templates/logs_page.html` returns 0
    - `grep -c '#444466' templates/logs_page.html` returns 0
    - `grep '#1C2333' templates/logs_page.html` returns at least 1 match
    - `grep '#161B22' templates/logs_page.html` returns at least 1 match (alternating row)
    - `grep '#8B949E' templates/logs_page.html` returns at least 1 match (empty state text)
  </acceptance_criteria>
  <done>Analytics/Logs page has dark page background, dark-themed table with alternating #1C2333/#161B22 rows, and consistent teal accent.</done>
</task>

</tasks>

<verification>
- Open /settings in browser: all 4 tabs show dark backgrounds, no light/grey areas, teal accents
- Open /analytics or logs page: table has dark alternating rows, no legacy colors visible
</verification>

<success_criteria>
Settings and Analytics pages are visually consistent with the dark theme. No legacy gold #E8B040, no light backgrounds, no mismatched card colors.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-design-unification/04.3-03-SUMMARY.md`
</output>
