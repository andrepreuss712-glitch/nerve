---
phase: 04.3-design-unification
plan: 05
type: execute
wave: 4
depends_on:
  - 04.3-02
files_modified:
  - templates/base.html
  - templates/settings.html
  - templates/app.html
autonomous: true
requirements:
  - DU-09
  - DU-10

must_haves:
  truths:
    - "No footer links visible on any page"
    - "No email or logout button in the header area"
    - "Settings page has a Rechtliches tab with links to /impressum, /agb, /datenschutz"
  artifacts:
    - path: "templates/base.html"
      provides: "No footer block, no .g-hright header div"
    - path: "templates/settings.html"
      provides: "New Rechtliches tab with legal links"
    - path: "templates/app.html"
      provides: "No footer or redundant header elements"
  key_links:
    - from: "templates/settings.html"
      to: "/impressum, /agb, /datenschutz"
      via: "anchor links in Rechtliches tab pane"
      pattern: "href=./impressum"
---

<objective>
Remove footer links from all pages, remove header email/logout, and add "Rechtliches" tab to Settings.

Purpose: Footer legal links and header email/logout are redundant — email and logout already exist in the sidebar user dropdown, and legal links will live in a new Settings tab (DU-09, DU-10).
Output: Clean header/footer across all pages, new Rechtliches tab in Settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.3-design-unification/04.3-CONTEXT.md
@.planning/phases/04.3-design-unification/04.3-RESEARCH.md
@.planning/phases/04.3-design-unification/04.3-02-SUMMARY.md

<interfaces>
<!-- Settings tab structure (current): -->
<!-- Tab buttons: Profil, Abrechnung (owner/admin), Datenschutz (owner/admin), Benachrichtigungen -->
<!-- Tab panes: #tab-profile, #tab-billing, #tab-privacy, #tab-notifications -->
<!-- Tab switching via showTab('name') JS function -->
<!-- Abrechnung and Datenschutz are inside {% if session.get('rolle') in ('owner','admin') %} -->
<!-- New "Rechtliches" tab must be OUTSIDE the role check — visible to ALL users -->

<!-- base.html footer (lines 135-139): <footer> with /impressum, /agb, /datenschutz links -->
<!-- base.html header (lines 33-36): .g-hright with email span + Abmelden link -->
<!-- Email already in sidebar-user-email (base.html line 102) -->
<!-- Logout already in sidebar dropdown (base.html line 113) -->

<!-- app.html is STANDALONE — check if it has its own footer or header email -->
<!-- Legal routes exist: routes/legal.py serves /impressum, /agb, /datenschutz -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove footer and header email/logout from base.html and app.html</name>
  <files>templates/base.html, templates/app.html</files>
  <read_first>
    - templates/base.html (need lines 33-36 for .g-hright, lines 135-139 for footer)
    - templates/app.html (check for any footer or email/logout elements)
  </read_first>
  <action>
**In templates/base.html:**

Per DU-10 (locked): DELETE the entire `.g-hright` div (lines 33-36 approximately):
```html
<div class="g-hright">
  <span class="g-user">{{ g.user.email if g.user else '' }}</span>
  <a href="/logout" class="n-btn n-btn-ghost n-btn-sm">Abmelden</a>
</div>
```
Delete these 4 lines. The email is already shown in `sidebar-user-email` (line 102). Logout is already in the sidebar dropdown (line 113). Both are verified in the research.

Per DU-09 (locked): DELETE the entire `<footer>` block (lines 135-139 approximately):
```html
<footer style="text-align:center;padding:20px;font-size:12px;...">
  <a href="/impressum">Impressum</a> ...
  <a href="/agb">AGB</a> ...
  <a href="/datenschutz">Datenschutz</a>
</footer>
```
Delete these lines completely. Legal links will live in Settings "Rechtliches" tab.

Do NOT remove the `<a href="/settings/help" class="help-fab">?</a>` — that is the help FAB, not a footer link.

**In templates/app.html:**
Check if app.html has any footer or header email/logout elements. If it has a footer with legal links, delete it. If it has an email display or logout button in the header area (distinct from the Beenden button), remove those too. app.html is standalone so base.html changes do NOT affect it.
  </action>
  <verify>
    <automated>grep -c "g-hright\|g-user.*email\|Abmelden" templates/base.html && grep -c "<footer" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'g-hright' templates/base.html` returns 0
    - `grep -c 'Abmelden' templates/base.html` returns 0 (no logout in header — sidebar dropdown still has it)
    - `grep -c '<footer' templates/base.html` returns 0
    - `grep 'sidebar-user-email' templates/base.html` still returns a match (sidebar email preserved)
    - `grep '/logout' templates/base.html` still returns at least 1 match (sidebar dropdown logout preserved)
    - The `help-fab` element is still present
  </acceptance_criteria>
  <done>Footer links and header email/logout removed from base.html (and app.html if applicable). Sidebar retains both email display and logout.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Rechtliches" tab to settings.html</name>
  <files>templates/settings.html</files>
  <read_first>
    - templates/settings.html (full file — need to see tab button section, tab pane section, showTab() JS function)
  </read_first>
  <action>
Per DU-09 (locked): Add a "Rechtliches" tab to Settings that is visible to ALL users.

**Step 1: Add tab button.**
Find the tab buttons section (around lines 110-121). Add a new tab button AFTER "Benachrichtigungen" (the last tab visible to all users) and OUTSIDE any `{% if session.get('rolle') %}` block:

```html
<button class="tab-btn" onclick="showTab('legal')">Rechtliches</button>
```

**Step 2: Add tab pane.**
Find the tab panes section. Add a new pane AFTER the last pane (Benachrichtigungen), OUTSIDE any role-gated block:

```html
<div id="tab-legal" class="tab-pane" style="display:none">
  <div class="settings-card">
    <h3 style="margin-bottom:16px;color:#FFFFFF">Rechtliches</h3>
    <div style="display:flex;flex-direction:column;gap:12px">
      <a href="/impressum" style="color:#00D4AA;text-decoration:none;font-size:14px">Impressum</a>
      <a href="/agb" style="color:#00D4AA;text-decoration:none;font-size:14px">Allgemeine Geschäftsbedingungen (AGB)</a>
      <a href="/datenschutz" style="color:#00D4AA;text-decoration:none;font-size:14px">Datenschutzerklärung</a>
    </div>
  </div>
</div>
```

Use the same `.settings-card` class as other tab content. Links use teal `#00D4AA` color. Background uses the card bg which should now be `#1C2333` (from Plan 03). No backend route change needed — the legal pages already exist at `/impressum`, `/agb`, `/datenschutz` via `routes/legal.py`.

**Step 3: Verify showTab() function.**
Check that the existing `showTab()` JS function works generically (hides all `.tab-pane`, shows the selected one). It should already handle a new tab without modification since it uses class-based selection. If it uses a hardcoded list of tab IDs, add `'legal'` to that list.
  </action>
  <verify>
    <automated>grep -c "tab-legal\|Rechtliches" templates/settings.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'tab-legal' templates/settings.html` returns at least 2 matches (button onclick + pane id)
    - `grep 'Rechtliches' templates/settings.html` returns at least 2 matches (button text + heading)
    - `grep '/impressum' templates/settings.html` returns at least 1 match (inside legal tab pane)
    - `grep '/agb' templates/settings.html` returns at least 1 match
    - `grep '/datenschutz' templates/settings.html` returns at least 1 match
    - The Rechtliches tab button is NOT inside a `{% if session.get('rolle') %}` block
    - The `#tab-legal` pane contains links to all three legal pages
  </acceptance_criteria>
  <done>Settings page has a "Rechtliches" tab visible to all users, containing links to Impressum, AGB, and Datenschutz pages.</done>
</task>

</tasks>

<verification>
- Load any page (dashboard, training, etc.): no footer visible, no email/logout in header
- Open /settings: "Rechtliches" tab visible, clicking it shows links to Impressum/AGB/Datenschutz
- Click each legal link from Settings: navigates to the correct page
- Sidebar user dropdown still shows email and Abmelden
</verification>

<success_criteria>
Footer removed from all pages. Header email/logout removed. Legal links accessible via Settings "Rechtliches" tab. No dead links.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-design-unification/04.3-05-SUMMARY.md`
</output>
