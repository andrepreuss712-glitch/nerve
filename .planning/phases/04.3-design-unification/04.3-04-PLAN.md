---
phase: 04.3-design-unification
plan: 04
type: execute
wave: 3
depends_on:
  - 04.3-02
files_modified:
  - templates/training.html
  - templates/profile_editor.html
  - templates/help.html
autonomous: true
requirements:
  - DU-06
  - DU-07
  - DU-08

must_haves:
  truths:
    - "Training page has dark background with teal Anrufen button"
    - "Profile Editor has dark background with properly styled dark inputs"
    - "Help Center section headers are teal, not orange"
  artifacts:
    - path: "templates/training.html"
      provides: "Dark background, teal call button"
    - path: "templates/profile_editor.html"
      provides: "Dark inputs with #1C2333 bg and #2D3748 border"
    - path: "templates/help.html"
      provides: "Teal section headers replacing orange"
  key_links:
    - from: "templates/help.html"
      to: "static/nerve.css"
      via: "CSS custom properties"
      pattern: "#00D4AA"
---

<objective>
Apply unified dark theme to Training, Profile Editor, and Help Center pages.

Purpose: These three pages have hardcoded legacy colors (orange headers in Help, dark-black backgrounds in Training, token-only inputs in Profile Editor that are too dark). Each needs specific color fixes to match the target scheme (DU-06, DU-07, DU-08).
Output: All three pages visually consistent with the dark theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.3-design-unification/04.3-CONTEXT.md
@.planning/phases/04.3-design-unification/04.3-RESEARCH.md
@.planning/phases/04.3-design-unification/04.3-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix training.html dark theme and teal accent</name>
  <files>templates/training.html</files>
  <read_first>
    - templates/training.html (full file — need inline styles, .t-call-screen, .t-call-btn, .t-avatar colors)
  </read_first>
  <action>
Per DU-06 (locked): Ensure Training has dark background and teal Anrufen button.

**Exact replacements in the `<style>` block:**

1. `.t-call-screen{background:#000}` -> `.t-call-screen{background:#0D1117}` (or if it uses `#000000`)
2. `.t-call-btn` — if it uses `#2dd4a8`, update to `#00D4AA`. If hover uses `#20b090`, update to `#00B894`.
3. Any remaining `#2dd4a8` in training.html inline styles -> `#00D4AA`
4. Any remaining `#20b090` -> `#00B894`

**AI persona gold — KEEP as-is:**
- `.t-avatar{color:#d4a853}` — DO NOT CHANGE (intentional AI persona signal)
- `.t-msg-name{color:#d4a853}` — DO NOT CHANGE

**Other hardcoded values:**
- Search for any `#1a1a2e` -> replace with `#1C2333`
- Search for any `#0c0c18` -> replace with `#1C2333`
- Search for any `#E8B040` that is NOT AI-related -> replace with `#00D4AA`

Do NOT remove language buttons yet (that is DU-11, handled in Plan 06).
  </action>
  <verify>
    <automated>grep -c "#000" templates/training.html && grep "t-call-btn" templates/training.html | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `.t-call-screen` background is `#0D1117` (not `#000`)
    - `.t-call-btn` uses `#00D4AA` for teal (not `#2dd4a8`)
    - `#d4a853` (AI gold) is still present and unchanged
    - No `#1a1a2e` or `#0c0c18` values remain
  </acceptance_criteria>
  <done>Training page has dark #0D1117 background, teal #00D4AA Anrufen button, AI gold preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix profile_editor.html dark inputs and help.html teal headers</name>
  <files>templates/profile_editor.html, templates/help.html</files>
  <read_first>
    - templates/profile_editor.html (full file — need inline styles for inputs, labels, section headers)
    - templates/help.html (full file — need all #E8B040 occurrences)
  </read_first>
  <action>
**Profile Editor (DU-07):**

Find all input styling in the `<style>` block. Update:
1. Input backgrounds using `var(--page-bg)` (too dark, same as page) -> change to `background:#1C2333`
2. Input borders: ensure `border:1px solid #2D3748`
3. Input text color: ensure `color:#FFFFFF`
4. Any `.pe-` prefixed input classes that use legacy colors -> update
5. Section header colors: if any use orange `#E8B040`, change to `#00D4AA`
6. Any `#2dd4a8` -> `#00D4AA`
7. Labels: ensure white (`#FFFFFF`) or teal (`#00D4AA`), not orange

Search for inline `style="background:var(--page-bg)"` on input/textarea elements and change to `style="background:#1C2333"`.

**Help Center (DU-08):**

Replace ALL `#E8B040` (orange) occurrences with `#00D4AA` (teal):
1. `.help-section-title{color:#E8B040}` -> `.help-section-title{color:#00D4AA}`
2. `.help-search:focus{border-color:#E8B040}` -> `.help-search:focus{border-color:#00D4AA}`
3. `.faq-title:hover{color:#E8B040}` -> `.faq-title:hover{color:#00D4AA}`
4. `.faq-item.open .faq-arrow{color:#E8B040}` -> `.faq-item.open .faq-arrow{color:#00D4AA}`
5. `.help-contact a{color:#E8B040}` -> `.help-contact a{color:#00D4AA}`
6. `.help-search{background:#1a1a2e;border:1px solid #2e2e48}` -> `.help-search{background:#1C2333;border:1px solid #2D3748}`
7. Any remaining `#1a1a2e` -> `#1C2333`
8. Any remaining `#2e2e48` -> `#2D3748`
  </action>
  <verify>
    <automated>grep -c "#E8B040" templates/help.html && grep -c "#1a1a2e\|#2e2e48" templates/help.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#E8B040' templates/help.html` returns 0 (all orange replaced with teal)
    - `grep '#00D4AA' templates/help.html` returns at least 4 matches
    - `grep -c '#1a1a2e' templates/help.html` returns 0
    - `grep '#1C2333' templates/profile_editor.html` returns at least 1 match (input bg)
    - `grep '#2D3748' templates/profile_editor.html` returns at least 1 match (input border)
    - Profile editor inputs have explicit `#1C2333` background (not `var(--page-bg)`)
  </acceptance_criteria>
  <done>Profile Editor has dark inputs (#1C2333 bg, #2D3748 border, white text). Help Center headers are teal #00D4AA, no orange remains.</done>
</task>

</tasks>

<verification>
- Open /training: dark background, teal Anrufen button, AI gold avatar preserved
- Open /profile-editor (or whatever the route is): dark inputs clearly distinguishable from page background
- Open /settings/help (or /help): section headers in teal, no orange visible
</verification>

<success_criteria>
Training, Profile Editor, and Help Center pages are visually consistent with the unified dark theme. No legacy orange, no mismatched backgrounds.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-design-unification/04.3-04-SUMMARY.md`
</output>
