---
phase: 04.3-design-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - static/nerve.css
  - templates/base.html
  - templates/settings.html
autonomous: true
requirements:
  - DU-02
  - DU-03

must_haves:
  truths:
    - "No light theme CSS rules exist in the codebase"
    - "No theme toggle UI is visible anywhere in the app"
    - "All CSS custom properties in nerve.css :root use the target dark color values"
  artifacts:
    - path: "static/nerve.css"
      provides: "Dark-only CSS tokens with target color values"
    - path: "templates/base.html"
      provides: "No FOUC script, no toggleTheme(), no theme toggle link"
    - path: "templates/settings.html"
      provides: "No Design/Theme card"
  key_links:
    - from: "static/nerve.css"
      to: "all templates"
      via: "CSS custom properties"
      pattern: "--page-bg.*#0D1117"
---

<objective>
Remove Light Mode completely and update all CSS custom property tokens to the target dark color values.

Purpose: The app should have ONE theme (dark). Light mode CSS, JS toggle logic, and UI elements are dead code that causes FOUC and inconsistency (DU-02). The CSS tokens need updating to the target color scheme (DU-03 foundation).
Output: nerve.css with target dark tokens only, base.html without theme toggle, settings.html without theme card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-design-unification/04.3-CONTEXT.md
@.planning/phases/04.3-design-unification/04.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Light Mode from nerve.css and update :root tokens to target values</name>
  <files>static/nerve.css</files>
  <read_first>
    - static/nerve.css (full file — need to see :root block lines 9-367 and [data-theme="light"] block lines 369-384)
  </read_first>
  <action>
Per DU-02 (locked): DELETE the entire `[data-theme="light"]` block (lines 369-384 approximately). Remove it completely, do not comment it out.

Per DU-03 (locked color scheme): Update the following CSS custom properties in the `:root` block to match the target values:

**Page/Layout tokens:**
- `--page-bg`: change from `#06060a` to `#0D1117`
- `--sidebar-bg` (or equivalent): change from `rgba(6,6,10,0.95)` to `#0A0E14`

**Primary accent (Teal):**
- `--btn-primary-bg-from` (and any `#2dd4a8` token): change to `#00D4AA`
- `--btn-primary-bg-to` (hover/gradient end): change to `#00B894`
- Any `--n-primary` or similar teal token: `#00D4AA`
- Any teal hover token: `#00B894`

**Text tokens:**
- `--page-text-color` / `--n-text`: ensure `#FFFFFF` or `#E6E6E6`
- `--page-text-muted` / `--n-text-muted`: ensure `#8B949E`

**Status colors:**
- Success green: `#2EA043`
- Warning/gold: `#E8B040` (for warnings only)
- Error/danger: `#F85149`

**Card/Panel tokens:**
- Any glass-bg or card-bg token: note that `.n-glass` uses rgba() — keep the glass effect for live/dashboard panels. The solid `#1C2333` value will be applied per-page in later plans for settings/analytics/help cards via inline style overrides.

**Input tokens:**
- `--n-input-bg` or equivalent: `#1C2333`
- `--n-input-border` or equivalent: `#2D3748`

Do NOT change `--ai-*` gold tokens (`#d4a853`) — these are intentional AI persona styling.

Search for any remaining `#2dd4a8` values in nerve.css and update to `#00D4AA`. Search for `#20b090` and update to `#00B894`.
  </action>
  <verify>
    <automated>grep -c "data-theme.*light" static/nerve.css && grep "page-bg" static/nerve.css | head -3 && grep -c "#2dd4a8" static/nerve.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-theme.*light' static/nerve.css` returns 0
    - `grep 'page-bg' static/nerve.css` shows `#0D1117` (not `#06060a`)
    - `grep -c '#2dd4a8' static/nerve.css` returns 0 (all replaced with `#00D4AA`)
    - `grep '#0A0E14' static/nerve.css` returns at least 1 match (sidebar bg)
    - `grep -c '#d4a853' static/nerve.css` returns same count as before (AI gold preserved)
    - No `prefers-color-scheme` media queries exist
  </acceptance_criteria>
  <done>nerve.css has no light theme block, all :root tokens use target dark color values, teal is #00D4AA everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Remove theme toggle JS and UI from base.html and settings.html</name>
  <files>templates/base.html, templates/settings.html</files>
  <read_first>
    - templates/base.html (full file — need lines 2, 8-13, 109-111, 280-304)
    - templates/settings.html (need lines 231-240 for theme card)
  </read_first>
  <action>
Per DU-02 (locked): Remove ALL theme toggle code and UI.

**In templates/base.html:**

1. **Line 2** (`<html ... data-server-theme="...">`): Remove the `data-server-theme` attribute entirely. Keep `data-theme="dark"` hardcoded.

2. **Lines 8-13** (FOUC prevention `<script>` in `<head>`): Replace the entire script block with a single line:
   ```html
   <script>document.documentElement.setAttribute('data-theme','dark')</script>
   ```

3. **Lines 109-111** ("Theme wechseln" link in sidebar dropdown): DELETE the entire `<a href="javascript:void(0)" onclick="toggleTheme()...">` element (the line with "Theme wechseln").

4. **Lines 280-304** (`toggleTheme()` function and the `DOMContentLoaded` listener for theme icons): DELETE the entire `toggleTheme()` function definition AND the DOMContentLoaded event listener that sets theme icon classes. Find the exact boundaries by looking for `function toggleTheme()` start and the closing `}` of the DOMContentLoaded handler.

**In templates/settings.html:**

1. **Lines 231-240** (the "Design" `.settings-card` with theme toggle): DELETE the entire card block that contains the theme toggle button. Look for a `.settings-card` that contains "Design" or "Theme" text and a toggle.

2. If there is a `settingsThemeLabel` element or theme-related JS in the settings script block, DELETE it too.

Do NOT remove `saveLangPref()` (language, not theme).
Do NOT remove `preferred_theme` from backend routes — just stop rendering the UI for it.
  </action>
  <verify>
    <automated>grep -c "toggleTheme\|Theme wechseln\|data-server-theme\|settingsThemeLabel" templates/base.html templates/settings.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'toggleTheme' templates/base.html` returns 0
    - `grep -c 'Theme wechseln' templates/base.html` returns 0
    - `grep -c 'data-server-theme' templates/base.html` returns 0
    - `grep 'data-theme' templates/base.html | head -1` shows `data-theme="dark"` hardcoded on `<html>` tag
    - The FOUC script in `<head>` is a single line setting `data-theme` to `dark`
    - `grep -c 'settingsThemeLabel\|Design.*theme\|theme-toggle' templates/settings.html` returns 0
    - `saveLangPref` function still exists in settings.html
  </acceptance_criteria>
  <done>No theme toggle UI exists in base.html sidebar dropdown or settings.html. Theme is hardcoded to dark. toggleTheme() function is deleted.</done>
</task>

</tasks>

<verification>
- `grep -r "toggleTheme\|prefers-color-scheme\|data-theme.*light" templates/ static/nerve.css` returns no matches
- `grep "#0D1117" static/nerve.css` confirms page-bg updated
- `grep "#00D4AA" static/nerve.css` confirms teal updated
- App loads without JS errors (no references to deleted theme functions)
</verification>

<success_criteria>
Light Mode is completely removed from the codebase. CSS tokens use the target dark color values. No theme toggle UI is visible anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-design-unification/04.3-02-SUMMARY.md`
</output>
