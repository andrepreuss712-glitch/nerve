---
phase: 04.3-design-unification
plan: 06
type: execute
wave: 5
depends_on:
  - 04.3-02
  - 04.3-03
  - 04.3-04
files_modified:
  - templates/training.html
  - templates/base.html
  - templates/app.html
autonomous: true
requirements:
  - DU-11
  - DU-12
  - DU-03

must_haves:
  truths:
    - "No language selection buttons visible on the Training page"
    - "Training uses server-side preferred_language without client-side language switching"
    - "Settings button in sidebar is always at the same vertical position on all pages"
    - "Dashboard and all remaining pages show consistent dark theme"
  artifacts:
    - path: "templates/training.html"
      provides: "No language buttons, uses Jinja preferred_language directly"
    - path: "templates/base.html"
      provides: "Sidebar with flex spacer before Einstellungen nav item"
    - path: "templates/app.html"
      provides: "Consistent dark theme colors (teal #00D4AA, no #2dd4a8)"
  key_links:
    - from: "templates/training.html"
      to: "settings preferred_language"
      via: "Jinja template variable"
      pattern: "preferred_language"
    - from: "templates/base.html"
      to: "sidebar layout"
      via: "flex spacer div"
      pattern: "flex.*1.*Einstellungen"
---

<objective>
Remove language buttons from Training, fix sidebar Settings button position, and ensure all remaining pages have consistent dark theme.

Purpose: Language selection belongs in Settings (already exists there per DU-11). The sidebar Settings button jumps between pages due to flex layout (DU-12). Dashboard and app.html may still have #2dd4a8 teal instead of #00D4AA (DU-03 completion).
Output: Clean Training page, stable sidebar, fully consistent dark theme across all pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.3-design-unification/04.3-CONTEXT.md
@.planning/phases/04.3-design-unification/04.3-RESEARCH.md
@.planning/phases/04.3-design-unification/04.3-02-SUMMARY.md
@.planning/phases/04.3-design-unification/04.3-04-SUMMARY.md

<interfaces>
<!-- training.html language elements (from research): -->
<!-- Lines 193-198: CSS classes .t-lang-btns, .t-lang-btn, .t-lang-btn.t-lang-active -->
<!-- Lines 270-274: Language buttons HTML block -->
<!-- Lines 489-514: TRAINING_LANGUAGES JS constant and selectLanguage() call on load -->
<!-- Lines 533-630: selectLanguage() function -->
<!-- Line 838: fd.append('language', currentLanguage) — must change to Jinja var -->
<!-- preferred_language is already passed as Jinja template variable from backend -->

<!-- base.html sidebar (from research): -->
<!-- .g-sidebar-inner has flex:1, overflow-y:auto -->
<!-- Einstellungen is the last visible nav item inside .g-sidebar-inner -->
<!-- Fix: add <div style="flex:1"></div> spacer BEFORE Einstellungen nav item -->

<!-- Language preference select ALREADY EXISTS in settings.html Profil tab -->
<!-- /settings/language POST endpoint already exists in settings.py -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove language buttons from training.html</name>
  <files>templates/training.html</files>
  <read_first>
    - templates/training.html (full file — need lines 193-198, 270-274, 489-514, 533-630, ~838)
  </read_first>
  <action>
Per DU-11 (locked): Remove all language selection UI from Training page. Language preference already exists in Settings.

**Step 1: Delete language button CSS (lines ~193-198).**
Remove the style rules for `.t-lang-btns`, `.t-lang-btn`, `.t-lang-btn.t-lang-active`.

**Step 2: Delete language buttons HTML (lines ~270-274).**
Find the HTML block that renders language buttons (likely a `{% for lang in training_languages %}` loop or similar). Delete the entire container div.

**Step 3: Delete language JS (lines ~489-514 and ~533-630).**
Remove:
- The `TRAINING_LANGUAGES` constant definition
- The `currentLanguage` variable declaration
- The `selectLanguage()` function definition (entire function body)
- The `DOMContentLoaded` call to `selectLanguage()` for non-default language

**Step 4: Fix the language submission (line ~838).**
Find `fd.append('language', currentLanguage)` in the training submission code. Replace with:
```javascript
fd.append('language', {{ preferred_language | tojson }})
```
This uses the Jinja-injected server value directly. `preferred_language` is already passed from the backend route as a template variable.

If `currentLanguage` is referenced anywhere else in training.html JS (e.g., in `tUI` object calls or condition checks), replace those references with `{{ preferred_language | tojson }}` too. Create a single JS const at the top of the script block:
```javascript
const currentLanguage = {{ preferred_language | tojson }};
```
This way all existing references to `currentLanguage` keep working, but the variable is set from the server, not from UI buttons.
  </action>
  <verify>
    <automated>grep -c "t-lang-btn\|selectLanguage\|TRAINING_LANGUAGES" templates/training.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 't-lang-btn' templates/training.html` returns 0 (CSS and HTML removed)
    - `grep -c 'selectLanguage' templates/training.html` returns 0 (function removed)
    - `grep -c 'TRAINING_LANGUAGES' templates/training.html` returns 0 (constant removed)
    - `grep 'preferred_language' templates/training.html` returns at least 1 match (server value used)
    - Language is still submitted in the training form data (via Jinja variable, not UI selection)
  </acceptance_criteria>
  <done>Language buttons removed from Training page. Language preference comes from server-side preferred_language (set in Settings). No client-side language switching in Training.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sidebar Settings position and sweep remaining dark theme inconsistencies</name>
  <files>templates/base.html, templates/app.html</files>
  <read_first>
    - templates/base.html (need sidebar structure — .g-sidebar-inner, nav items, Einstellungen link)
    - templates/app.html (need to check for #2dd4a8 teal values that should be #00D4AA)
  </read_first>
  <action>
**Sidebar Settings fix (DU-12):**

In `templates/base.html`, find the `.g-sidebar-inner` div. Inside it, locate the Einstellungen `<a class="n-nav-item">` tag. Add a flex spacer div IMMEDIATELY BEFORE the Einstellungen link:

```html
<div style="flex:1"></div>
<a class="n-nav-item {% if request.path == '/settings' %}n-nav-item-active{% endif %}" href="/settings" title="Einstellungen">
```

This pushes the Einstellungen link to the bottom of `.g-sidebar-inner`, giving it a consistent position regardless of content above. This mirrors the `margin-top:auto` pattern used by `.sidebar-user-block`.

Do NOT use `position:fixed` — that breaks the collapsed sidebar state.
Do NOT move the Einstellungen link outside `.g-sidebar-inner` — that would break the collapsed sidebar CSS.

**Dark theme sweep for app.html (DU-03 completion):**

In `templates/app.html`, search for any remaining instances of `#2dd4a8` and replace with `#00D4AA`. Search for `#20b090` and replace with `#00B894`. These are teal color values that should match the updated nerve.css tokens.

Check for any `#E8B040` (gold/orange) in app.html that is NOT the AI panel gold `#d4a853`. If found, replace with `#00D4AA`.

**Dark theme sweep for dashboard template:**
The dashboard template (likely `templates/dashboard.html`) inherits from base.html so it gets the updated nerve.css tokens. But check for any hardcoded `#2dd4a8` or `#E8B040` in its inline styles and replace accordingly:
- `#2dd4a8` -> `#00D4AA`
- `#E8B040` -> `#00D4AA` (if used as accent, not AI gold)
  </action>
  <verify>
    <automated>grep -c "flex:1.*Einstellungen\|flex: *1" templates/base.html && grep -c "#2dd4a8" templates/app.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'flex:1' templates/base.html` shows a spacer div before the Einstellungen nav item
    - The spacer div is inside `.g-sidebar-inner`, immediately before the Einstellungen `<a>` tag
    - `grep -c '#2dd4a8' templates/app.html` returns 0 (all replaced with #00D4AA)
    - `#d4a853` AI gold in app.html is preserved (if present)
    - No `position:fixed` added to any sidebar element
  </acceptance_criteria>
  <done>Sidebar Settings button pinned to bottom via flex spacer — consistent position on all pages. All remaining #2dd4a8 teal values replaced with #00D4AA across app.html and dashboard.</done>
</task>

</tasks>

<verification>
- Open /training: no language buttons visible, training still works (uses server-side language)
- Navigate between /dashboard, /training, /settings, /analytics: Settings nav item in sidebar stays at same vertical position
- Open /live (app.html): teal colors are #00D4AA, no #2dd4a8 values in DevTools
- Open /dashboard: consistent dark theme, teal accents
</verification>

<success_criteria>
Language buttons gone from Training. Sidebar Settings position stable across all pages. All 8 app areas (Dashboard, Training, Live, Analytics, Profil, Profil-Editor, Einstellungen, Hilfe-Center) show the unified dark color scheme with #00D4AA teal.
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-design-unification/04.3-06-SUMMARY.md`
</output>
