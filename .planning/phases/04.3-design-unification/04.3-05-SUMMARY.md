---
phase: 04.3-design-unification
plan: 05
subsystem: templates
tags: [design-unification, footer, header, settings, legal]
dependency_graph:
  requires: [04.3-02]
  provides: [clean-header-footer, rechtliches-tab]
  affects: [base.html, settings.html]
tech_stack:
  added: []
  patterns: [jinja2-tab-pattern]
key_files:
  created: []
  modified:
    - templates/base.html
    - templates/settings.html
decisions:
  - "app.html stats footer (Wörter/Einwände/Analysen) is not a legal footer — left untouched"
  - "app.html g-user-email span is display:none JS data, not visual header element — left untouched"
  - "showTab() generic class-based implementation handles new tab without modification"
  - "Rechtliches tab button placed outside role-check block — visible to all users"
metrics:
  duration: "5min"
  completed_date: "2026-04-04"
  tasks_completed: 2
  files_modified: 2
---

# Phase 04.3 Plan 05: Footer/Header Cleanup + Rechtliches Tab Summary

Remove footer legal links from all pages, remove header email/logout, and add "Rechtliches" tab to Settings with links to Impressum, AGB, and Datenschutz.

## What Was Built

**Task 1: Remove footer and header email/logout from base.html**

- Deleted `.g-hright` div (email span + Abmelden button) from base.html header — email and logout already exist in sidebar user dropdown
- Deleted `<footer>` block with /impressum, /agb, /datenschutz legal links from base.html
- Sidebar `sidebar-user-email` and Abmelden link in dropdown preserved intact
- `help-fab` anchor preserved
- app.html had no legal links footer and no header email/logout elements — no changes needed

**Task 2: Add Rechtliches tab to settings.html**

- Added `Rechtliches` tab button after `Benachrichtigungen`, outside `{% if session.get('rolle') %}` block — visible to all users
- Added `#tab-legal` pane with links to /impressum, /agb, /datenschutz using teal `#00D4AA` color
- `showTab()` JS function already handles new tabs generically via class-based selection — no modification needed
- Legal routes already exist via `routes/legal.py`

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1    | 4ef630c | feat(04.3-05): remove header email/logout and footer legal links from base.html |
| 2    | 06c8d44 | feat(04.3-05): add Rechtliches tab to settings with legal page links |

## Deviations from Plan

None — plan executed exactly as written. app.html inspection confirmed no legal links footer and no header email/logout elements requiring removal.

## Verification Checklist

- `grep -c 'g-hright' templates/base.html` → 0 (removed)
- `grep -c '<footer' templates/base.html` → 0 (removed)
- `grep -c 'sidebar-user-email' templates/base.html` → 1 (preserved)
- `grep -c '/logout' templates/base.html` → 2 (sidebar dropdown preserved)
- `grep -c 'help-fab' templates/base.html` → 1 (preserved)
- `grep -c 'Rechtliches' templates/settings.html` → 2 (button + heading)
- `grep -c '/impressum' templates/settings.html` → 1
- `grep -c '/agb' templates/settings.html` → 1
- `grep -c '/datenschutz' templates/settings.html` → 1

## Known Stubs

None.

## Self-Check: PASSED
