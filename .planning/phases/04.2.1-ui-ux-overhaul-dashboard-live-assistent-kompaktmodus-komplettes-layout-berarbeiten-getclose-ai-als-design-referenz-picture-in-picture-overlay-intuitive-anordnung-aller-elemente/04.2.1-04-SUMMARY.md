---
phase: 04.2.1-ui-ux-overhaul
plan: "04"
subsystem: ui
tags: [flask, jinja2, vanilla-js, css, live-session, chrome-reduction]

# Dependency graph
requires:
  - phase: 04.2.1-03
    provides: openPipWindow() Document PiP implementation that Kompakt button in header calls
provides:
  - Single 52px header for /live replacing 3-bar chrome (~120px reclaimed to ~52px)
  - Profile switcher dropdown integrated into header
  - Opener section in AI panel column showing phasen[0] text from active profile
  - Kompakt button in header triggering PiP window
affects:
  - 04.2.1-05 (any remaining UI work builds on single-header layout)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Hidden-not-deleted: removed header elements kept in DOM with display:none for JS handler continuity"
    - "Profile menu positioned relative to parent container (right:0) not viewport edge"
    - "active_profile_daten passed as pre-parsed JSON dict to template — avoids double json.loads in template"

key-files:
  created: []
  modified:
    - templates/app.html
    - routes/app_routes.py

key-decisions:
  - "confirmBeenden() in plan HTML does not exist — used existing beenden() function instead (Rule 1 auto-fix)"
  - "id=st (status text) kept as display:none span inside status div — JS in app.js writes mic error text to it"
  - "profile-menu right:0 (not right:20px) — anchors dropdown to the toggle button, not viewport edge"
  - "setProfile() updated to write to both #profileNameLabel (new header) and #activeProfileName (old hidden bar) for full compatibility"

patterns-established:
  - "Display:none-not-deleted pattern: all removed header elements hidden but kept for JS compat"

requirements-completed: [D-02, D-17, D-18, D-19]

# Metrics
duration: 8min
completed: 2026-04-03
---

# Phase 04.2.1 Plan 04: Vollbild Chrome Reduction Summary

**Single 52px header with logo, mode badge, timer, profile dropdown, Pause/Beenden/Kompakt — replacing the 3-bar chrome (header + profile-bar + phasen-bar)**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-04-03T14:28:12Z
- **Completed:** 2026-04-03T14:36:00Z
- **Tasks:** 5
- **Files modified:** 2

## Accomplishments

- Replaced 3-bar ~120px chrome with single 52px header containing all essential controls
- Profile switcher merged into header as ghost dropdown (reuses existing .profile-menu CSS pattern)
- Profile-bar and phasen-bar hidden (display:none, not deleted) per D-15 — ~72px vertical space reclaimed
- Opener section added as first element in #ai scroll container, showing active profile's phasen[0] text (D-19)
- Kompakt button in header calls openPipWindow() from Plan 03
- active_profile_daten passed as pre-parsed JSON dict from live() route to template

## Task Commits

Each task was committed atomically:

1. **Tasks 1-5: Header restructure, bar hiding, opener section, route update, profile menu fix** - `ae43fd4` (feat)

**Plan metadata:** (pending docs commit)

## Files Created/Modified

- `templates/app.html` — New single header, hidden profile-bar/phasen-bar, hidden removed elements, Opener section in #ai, updated profile toggle JS
- `routes/app_routes.py` — Added active_profile_daten (parsed JSON) passed to render_template()

## Decisions Made

- `confirmBeenden()` referenced in plan HTML does not exist in JS — used `beenden()` (existing function) instead. Auto-fix Rule 1.
- `id="st"` (status text span) kept as `display:none` inside status div — app.js writes "Mikrofon-Zugriff verweigert" to it on mic errors. Removing it entirely would silence mic error feedback.
- profile-menu CSS `right:0` instead of `right:20px` — dropdown anchors to the toggle button container, not the page edge, since the button is now in the center of the header.
- `setProfile()` JS updated to write to both `#profileNameLabel` (new) and `#activeProfileName` (legacy hidden bar) for full backward compatibility.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] confirmBeenden() does not exist — used beenden()**
- **Found during:** Task 1 (header restructure)
- **Issue:** Plan's new header HTML used `onclick="confirmBeenden()"` but no such function exists in app.js or app.html; the actual function is `beenden()`
- **Fix:** Used `onclick="beenden()"` in the new Beenden button
- **Files modified:** templates/app.html
- **Verification:** beenden() confirmed at line 477 of app.js
- **Committed in:** ae43fd4

**2. [Rule 2 - Missing Critical] Preserved id="st" for mic error feedback**
- **Found during:** Task 1 (header restructure)
- **Issue:** Removing the status text span would lose mic error display ("Mikrofon-Zugriff verweigert") — app.js writes to it on getUserMedia failure
- **Fix:** Kept `<span id="st" style="display:none">` inside status div; hidden normally but writable by JS
- **Files modified:** templates/app.html
- **Committed in:** ae43fd4

---

**Total deviations:** 2 auto-fixed (1 bug, 1 missing critical)
**Impact on plan:** Both fixes necessary for correctness. No scope creep.

## Issues Encountered

None beyond the two auto-fixed deviations above.

## Known Stubs

- **Opener section text**: If active profile has no `phasen[0].text` or `.inhalt` field, falls back to "Kein Opener definiert". This is expected behavior — not a stub. Profiles without opener content display a link to /profiles to edit.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Single-header layout complete; all hidden elements remain available for JS
- Profile switching works from header dropdown
- Opener section visible in AI panel — dismissable via X button
- Ready for Plan 05 (final UI polish or next phase work)

---
*Phase: 04.2.1-ui-ux-overhaul*
*Completed: 2026-04-03*

## Self-Check: PASSED

- templates/app.html: FOUND
- routes/app_routes.py: FOUND
- Commit ae43fd4: FOUND
