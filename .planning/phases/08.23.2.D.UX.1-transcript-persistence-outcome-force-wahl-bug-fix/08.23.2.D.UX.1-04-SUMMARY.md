---
phase: 08.23.2.D.UX.1
plan: 04
subsystem: ui
tags: [vanilla-js, pip-launcher, node-test, outcome-modal, umd]
requires:
  - phase: 08.23.2.D.UX.1-03
    provides: outcome_ready payload {outcome, confidence, source, call_id}
provides:
  - _decideModalState 5-state decider (shared UMD helper)
  - state-aware _renderOutcomeUx (final/kein_versuch/sicher/unsicher/defensiv)
  - 3 call-sites rewired (Bug-C skip-guards removed)
affects: [08.23.2.D.UX.1-05]
tech-stack:
  added: [static/outcome-modal-state.js (UMD helper)]
  patterns: [UMD helper requirable under node:test + browser global]
key-files:
  created: [static/outcome-modal-state.js, tests/test_decide_modal_state.test.js]
  modified: [static/pip-launcher.js, templates/base.html]
key-decisions:
  - "BLOCKER-2 fallback: extract decider to UMD helper (pip-launcher.js touches window at top-level, not Node-requirable)"
  - "Reuse existing CSS classes only (WARN-1): pip-outcome-hint/badge-unsicher/btn--selected/summary"
patterns-established:
  - "Shared pure-logic helper as single source of truth for browser + Node tests"
requirements-completed: [DC-01, DC-02, DC-03, DC-04]
duration: ~35min
completed: 2026-05-30
---

# Phase 08.23.2.D.UX.1 Plan 04: 5-state _decideModalState decider (Bug C)

**Central 5-state decider + state-aware modal; 3 call-sites rewired so the outcome modal always renders (Bug C); 5 node:test tests green**

## Accomplishments
- `_decideModalState` (final/kein_versuch/sicher/unsicher/defensiv); `user_corrected`→final ordered before conf===0 (F1)
- `_renderOutcomeUx` state-aware: read-only final, no-badge kein_versuch hint, teal unsicher badge, defensive console.warn; reuses existing CSS classes (no inline color, no yellow — WARN-1/brand)
- Removed `(outcome||source)` skip-guards at response-fallback + reconnect-pull call-sites (LANDMINE 7)
- 5 Node built-in (node:test) Function-Call-Return tests, all green, zero-install

## Task Commits
1. **Tasks 1-3: decider + rewire + tests** - `0be7a52` (feat)

## Deviations from Plan
**1. [BLOCKER-2 fallback — documented in plan] Decider extracted to UMD helper.**
- pip-launcher.js accesses `window` at top-level (line 830) → not requirable under Node. Per the plan's documented fallback, extracted `_decideModalState` into `static/outcome-modal-state.js` (UMD: browser global `window.NerveOutcomeModalState` + Node `module.exports`). pip-launcher.js consumes the global; base.html loads the helper before pip-launcher.js. Test requires the helper.
- Added files beyond plan's files_modified: `static/outcome-modal-state.js`, `templates/base.html` (script tag). Single-source-of-truth (DC-01) preserved.

## Live verification
- Modal renders after call end (test call). NOTE: an ordering/timing issue (score shown before outcome confirmation) was found in the live test — that is a SEPARATE follow-up phase, not a Bug-C render failure. Bug C (modal silently not rendering) is fixed.

## Next Phase Readiness
- Modal renders for all states. Follow-up: score-before-outcome ordering (separate phase, see backlog).

---
*Phase: 08.23.2.D.UX.1*
*Completed: 2026-05-30*
