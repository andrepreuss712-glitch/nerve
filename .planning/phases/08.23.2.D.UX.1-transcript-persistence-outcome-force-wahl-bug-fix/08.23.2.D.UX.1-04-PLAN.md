---
phase: 08.23.2.D.UX.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - static/pip-launcher.js
  - tests/test_decide_modal_state.test.js
autonomous: true
requirements: [DC-01, DC-02, DC-03, DC-04]
complexity: "🔴 komplex"
must_haves:
  truths:
    - "A central _decideModalState(data) function computes one of 4 states and is the single entry to outcome rendering"
    - "All three live call-sites (socket outcome_ready, response fallback, reconnect-pull) route through _decideModalState — none uses the old (outcome || source) guard"
    - "confidence === 0 always renders the modal with 7 buttons, no badge, text 'Automatische Erkennung nicht verfügbar', confirm disabled (no more 'kein Modal' case)"
    - "ai_auto renders pre-selected (teal) confirm-enabled; ai_auto_unsicher renders no highlight + 'KI unsicher — bitte wählen' badge + confirm disabled"
    - "defensive state (outcome null, conf>0) console.warns but still renders the unsicher modal; no yellow anywhere"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "_decideModalState 4-state decider + 3 call-sites rewired + _renderOutcomeUx state-aware + guarded CommonJS export for Node tests"
      contains: "_decideModalState"
    - path: "tests/test_decide_modal_state.test.js"
      provides: "Node built-in (node:test + node:assert) unit tests for all 4 states (DC-04) — zero-dependency, no Jest install"
      contains: "_decideModalState"
  key_links:
    - from: "static/pip-launcher.js socket/response/reconnect handlers"
      to: "_decideModalState"
      via: "all three call _decideModalState(data) then render"
      pattern: "_decideModalState\\("
---

<objective>
Fix Bug C: the modal silently fails to render for every low/zero-confidence call because three call-sites
guard on `(outcome || source)` and skip render when both are null. Introduce a central
`_decideModalState(data)` (DC-01) returning a 4-state enum (DC-02), make `_renderOutcomeUx` state-aware,
rewire ALL THREE live call-sites (socket `outcome_ready`, response fallback, reconnect-pull — LANDMINE 7
says three, not two) through it, and add zero-dependency Node unit tests (node:test + node:assert) for the
four states (DC-04). State 1
(confidence===0) now ALWAYS renders the modal — there is no "kein Modal" branch anymore (D.UX-Direktive:
Outcome-Pflicht-Schritt vor Score). Brand: badge teal, never yellow (CLAUDE.md Punkt 8). User-facing badge
text keeps real umlauts; code identifiers ASCII.

Purpose: This is the visible end of the chain — even with correct backend data, the force-choice modal must
appear so the user can pick an outcome.
Output: 4-state decider + rewired call-sites + 4 unit tests.

Implements: DC-01 (central decider), DC-02 (4 states), DC-03 (defensive console.warn), DC-04 (unit tests).

BLOCKER-2 note: DC-04 says "Jest oder vergleichbar" — Jest is NOT locked and this is a vanilla-JS repo
with NO package.json / no jest installed. The unit tests therefore use Node's BUILT-IN test runner
(`node:test` + `node:assert`, Node 18+) so the acceptance is runnable with zero install and no interactive
`npx jest` install prompt. Assumes Node >= 18 is available locally (confirm via read_first); state the
assumption if the runner choice depends on it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.1-transcript-persistence-outcome-force-wahl-bug-fix/08.23.2.D.UX.1-CONTEXT.md
@.planning/phases/08.23.2.D.UX.1-transcript-persistence-outcome-force-wahl-bug-fix/08.23.2.D.UX.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Payload shape at all call-sites: {outcome, confidence, source, call_id}. -->
<!-- Call-site A (socket, ~Z.2444-2454): state.socket.on('outcome_ready', d => { ... _renderOutcomeUx(d) }) -->
<!-- Call-site B (response fallback, ~Z.2949-2962): the Bug-C guard
       `if (paResult.call_id && (paResult.outcome || paResult.source)) { _renderOutcomeUx({...}) }` -->
<!-- Call-site C (reconnect-pull, ~Z.2478-2485): SAME buggy `(json.outcome || json.source)` guard (LANDMINE 7). -->
<!-- Test hook ~Z.3402 (existing) — make sure it still calls the render entry. -->
<!-- _renderOutcomeUx (~Z.3656-3734): idempotency guard `sec.dataset.outcomeRendered === '1'` (KEEP);
       `if (!callId) return;` (KEEP); pre-select `(conf>=0.70 && haikuOutcome) ? haikuOutcome : null`;
       badge block `if (conf < 0.70) {...}` teal; button-grid/confirm DOM Z.3693-3734 (keep intact). -->
<!-- 4-state table (DC-02):
     1 kein_versuch   : confidence===0          -> 7 buttons, NO badge, text "Automatische Erkennung nicht verfügbar", confirm DISABLED
     2 sicher         : source==='ai_auto'      -> pre-select teal, confirm ENABLED
     3 unsicher       : source==='ai_auto_unsicher' -> NO highlight, badge "KI unsicher — bitte wählen", confirm DISABLED
     4 defensiv       : outcome===null && conf>0 -> like 3 + console.warn('[D.UX.1] Inconsistent state', data); no UI element (DC-03) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _decideModalState + make _renderOutcomeUx state-aware (DC-02/DC-03)</name>
  <files>static/pip-launcher.js</files>
  <behavior>
    - _decideModalState({confidence:0}) === 'kein_versuch'
    - _decideModalState({source:'ai_auto', confidence:0.95}) === 'sicher'
    - _decideModalState({source:'ai_auto_unsicher', confidence:0.6}) === 'unsicher'
    - _decideModalState({outcome:null, confidence:0.5, source:null}) === 'unsicher' AND console.warn fired
    - _renderOutcomeUx renders the modal for ALL four states (no early-return that skips the modal except the existing !callId guard)
  </behavior>
  <read_first>
    - static/pip-launcher.js Z.3656-3734 (_renderOutcomeUx full body: idempotency guard, !callId guard, conf, pre-select logic, conf<0.70 badge block, button grid + confirm)
    - static/pip-launcher.js Z.2440-2490 (socket handler + reconnect-pull guard) and Z.2945-2965 (response fallback guard)
    - static/pip-launcher.js ~Z.3402 (existing test hook)
    - static/pip-launcher.js end-of-file (where the guarded CommonJS export will be added — confirm the file does not already `module.exports` anything)
    - CLAUDE.md Punkt 8 (no yellow; teal brand tokens) + UTF-8 rule (badge user-text keeps umlauts; identifiers ASCII)
  </read_first>
  <action>
Add `_decideModalState(data)` near `_renderOutcomeUx` (before it, same scope):
```javascript
function _decideModalState(data) {
  var conf = +(data && data.confidence) || 0;
  if (conf === 0) return 'kein_versuch';                      // Zustand 1
  if (data.source === 'ai_auto') return 'sicher';             // Zustand 2
  if (data.source === 'ai_auto_unsicher') return 'unsicher';  // Zustand 3
  if (data.outcome === null && conf > 0) {                    // Zustand 4 (defensive, DC-03)
    console.warn('[D.UX.1] Inconsistent state', data);
    return 'unsicher';
  }
  return 'unsicher';
}
```

Make `_renderOutcomeUx` state-aware. Extend it to compute `var modalState = _decideModalState(data);`
at the top (after the existing `if (!callId) return;` and idempotency guard), then branch the
presentation on modalState while KEEPING the existing button-grid/confirm DOM (Z.3693-3734):
- KEEP: `if (sec.dataset.outcomeRendered === '1') return;` and `if (!callId) return;`.
- `kein_versuch`: render 7 buttons, NO badge, set the info text to "Automatische Erkennung nicht verfügbar"
  (real umlaut — user-facing), confirm button disabled, no pre-selection.
- `sicher`: pre-select the haiku outcome (teal), confirm enabled.
- `unsicher`: no pre-selection / no highlight, badge text "KI unsicher — bitte wählen" (real umlaut,
  teal badge class `pip-outcome-badge-unsicher`, NEVER yellow), confirm disabled until user picks.
- The existing `selectedOutcome = (conf>=0.70 && haikuOutcome) ? ... : null` becomes redundant for
  pre-selection; drive pre-selection off modalState==='sicher' instead. Confirm-enabled state must be
  false for kein_versuch and unsicher, true for sicher (or once the user clicks a button in any state).

ASCII rule: enum strings ('kein_versuch','sicher','unsicher'), function names, dataset keys, CSS class
names = ASCII. Only the visible badge/info text strings carry umlauts.
Brand rule: confirm there is no yellow color/class introduced; reuse existing teal badge class.

BLOCKER-2 (testability under Node built-in runner): at the BOTTOM of static/pip-launcher.js add a guarded
CommonJS export so `_decideModalState` is requirable under Node WITHOUT breaking browser load:
```javascript
if (typeof module !== 'undefined' && module.exports) {
  module.exports._decideModalState = _decideModalState;
}
```
The browser never defines `module`, so this block is inert in-browser. This makes the pure function
importable by tests/test_decide_modal_state.test.js under `node --test`. `_decideModalState` itself uses
only `console.warn` (global in Node) and no DOM — so it requires cleanly. If the FILE as a whole touches
DOM globals at top-level (not inside functions) and breaks the require, see Task 3 for the extract-to-helper
fallback.
  </action>
  <acceptance_criteria>
    - `grep "function _decideModalState" static/pip-launcher.js` returns the line.
    - `grep "return 'kein_versuch'" static/pip-launcher.js`, `grep "return 'sicher'" ...`, `grep "return 'unsicher'" ...` all present.
    - `grep "console.warn('\[D.UX.1\] Inconsistent state'" static/pip-launcher.js` returns the defensive log.
    - `grep "Automatische Erkennung nicht verfügbar" static/pip-launcher.js` returns the kein_versuch text (real umlaut).
    - `grep "KI unsicher" static/pip-launcher.js` returns the unsicher badge text.
    - `grep -i "yellow\|#ff0\|#ffd\|gold" static/pip-launcher.js` returns NOTHING new in the outcome block (brand check).
    - `_renderOutcomeUx` retains the `outcomeRendered === '1'` idempotency guard and `if (!callId) return;`.
    - `grep "module.exports._decideModalState" static/pip-launcher.js` returns the guarded CommonJS export (BLOCKER-2 testability).
    - Node unit tests (Task 3) pass via `node --test`.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && node --test tests/test_decide_modal_state.test.js</automated>
  </verify>
  <done>Central 4-state decider exists; _renderOutcomeUx renders all four states with correct badge/confirm/teal; defensive console.warn present; guarded CommonJS export added for Node tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire all three call-sites through _decideModalState (DC-01, LANDMINE 7)</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Z.2444-2454 (socket outcome_ready handler)
    - static/pip-launcher.js Z.2478-2485 (reconnect-pull — SAME (outcome||source) guard, the third site)
    - static/pip-launcher.js Z.2949-2962 (response fallback — Bug-C guard)
    - static/pip-launcher.js ~Z.3402 (test hook — ensure it still reaches render)
  </read_first>
  <action>
Remove the broken `(outcome || source)` / `(json.outcome || json.source)` skip-guards at all THREE
call-sites and route each through the render entry so the modal renders regardless of null outcome/source.
Keep the legitimate guards (call_id present, call_id match, outcomeRendered idempotency).

Call-site A (socket, Z.2444-2454): already calls `_renderOutcomeUx(d)` — leave the call but ensure no
`(d.outcome || d.source)` precondition gates it; keep the `lastCallId` match + `!d.call_id` guards.

Call-site B (response fallback, Z.2949-2962): change
```javascript
if (paResult.call_id && (paResult.outcome || paResult.source)) {   // OLD
```
to
```javascript
if (paResult.call_id) {                                            // NEW — render even when both null
```
keeping the `sec.dataset.outcomeRendered === '1'` early-return above it.

Call-site C (reconnect-pull, Z.2478-2485): drop the `(json.outcome || json.source)` condition the same
way; render when `json.call_id` is present (+ existing idempotency guard).

All three must pass the full `{outcome, confidence, source, call_id}` payload into `_renderOutcomeUx`
(which now calls `_decideModalState` internally). Do NOT call `_decideModalState` separately at the
call-sites — render entry owns it (single source of truth, DC-01). Verify the test hook (~Z.3402) still
invokes the same render entry.
  </action>
  <acceptance_criteria>
    - `grep -c "_renderOutcomeUx(" static/pip-launcher.js` shows at least the three call-sites + test hook still calling it.
    - `grep "(paResult.outcome || paResult.source)" static/pip-launcher.js` returns NOTHING (Bug-C guard removed).
    - `grep "json.outcome || json.source" static/pip-launcher.js` returns NOTHING (reconnect guard removed).
    - The `if (paResult.call_id)` guard (without the outcome/source clause) is present at the response fallback.
    - call_id-match and outcomeRendered idempotency guards still present.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && node -e "require('fs').readFileSync('static/pip-launcher.js','utf8').includes('paResult.outcome || paResult.source') ? (console.error('STILL HAS BUG-C GUARD'), process.exit(1)) : console.log('guard-removed-ok')"</automated>
  </verify>
  <done>All three call-sites render through _decideModalState/_renderOutcomeUx; the old (outcome||source) skip-guards are gone; modal renders for null-outcome calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Node built-in (node:test) unit tests for all 4 states (DC-04, BLOCKER-2)</name>
  <files>tests/test_decide_modal_state.test.js</files>
  <behavior>
    - Test 1: _decideModalState({confidence:0, outcome:null, source:null}) -> 'kein_versuch'
    - Test 2: _decideModalState({confidence:0.95, source:'ai_auto', outcome:'meeting_booked'}) -> 'sicher'
    - Test 3: _decideModalState({confidence:0.6, source:'ai_auto_unsicher', outcome:'callback'}) -> 'unsicher'
    - Test 4: _decideModalState({confidence:0.5, source:null, outcome:null}) -> 'unsicher' AND console.warn called once
  </behavior>
  <read_first>
    - static/pip-launcher.js (the _decideModalState definition from Task 1 — confirm the guarded CommonJS export `if (typeof module !== 'undefined' && module.exports) { module.exports._decideModalState = _decideModalState; }` was added at the bottom in Task 1; this is what makes it requirable under Node)
    - BLOCKER-2: confirm Node is available and >= 18 (built-in `node:test` requires Node 18+). Run `node --version`. If Node is unavailable or < 18, state the assumption / blocker explicitly. This repo has NO package.json and NO jest — DO NOT use `npx jest` (it would trigger an interactive install prompt).
    - existing tests/ directory for the file-naming convention (the `.test.js` suffix is fine for `node --test` glob discovery, but here we run the file explicitly).
  </read_first>
  <action>
BLOCKER-2: DC-04 ("Jest oder vergleichbar") is satisfied with Node's BUILT-IN test runner — zero
dependencies, no install, no `npx jest` prompt. Create `tests/test_decide_modal_state.test.js` using
`node:test` + `node:assert`. It `require`s the guarded CommonJS export from pip-launcher.js (added in
Task 1). Per CLAUDE.md test-quality rule these are Function-Call-Return tests (real runtime behavior),
not source-presence tests.
```javascript
const { test } = require('node:test');
const assert = require('node:assert');
const { _decideModalState } = require('../static/pip-launcher.js');

test('Zustand 1 kein_versuch when confidence 0', () => {
  assert.strictEqual(_decideModalState({confidence: 0, outcome: null, source: null}), 'kein_versuch');
});

test('Zustand 2 sicher when source ai_auto', () => {
  assert.strictEqual(_decideModalState({confidence: 0.95, source: 'ai_auto', outcome: 'meeting_booked'}), 'sicher');
});

test('Zustand 3 unsicher when source ai_auto_unsicher', () => {
  assert.strictEqual(_decideModalState({confidence: 0.6, source: 'ai_auto_unsicher', outcome: 'callback'}), 'unsicher');
});

test('Zustand 4 defensive warns and returns unsicher', () => {
  const orig = console.warn;
  let calls = 0;
  console.warn = () => { calls += 1; };
  try {
    assert.strictEqual(_decideModalState({confidence: 0.5, source: null, outcome: null}), 'unsicher');
    assert.strictEqual(calls, 1);   // DC-03 defensive console.warn fired exactly once
  } finally {
    console.warn = orig;
  }
});
```
If `require('../static/pip-launcher.js')` fails under Node because the file touches DOM globals at
top-level (e.g. `document`/`window` at module scope, not inside functions), the guarded export must be
positioned so the require does not execute DOM code — if the whole file cannot be required cleanly,
extract `_decideModalState` into a tiny requirable helper module that pip-launcher.js also uses (single
source of truth, DC-01) and require THAT in the test. Document the chosen approach in the SUMMARY.
NOTE: this FE unit test runs locally under Node tooling — permitted: CLAUDE.md "Kein Local-Dev" governs
the Flask app + DB, NOT FE unit-test tooling. Live FE verification still happens on Production in Plan 05
(DT-01/DT-02).
  </action>
  <acceptance_criteria>
    - `tests/test_decide_modal_state.test.js` exists and uses `require('node:test')` + `require('node:assert')` (NO jest, NO `describe/expect/jest.spyOn`).
    - The file `require`s the real `_decideModalState` from `../static/pip-launcher.js` (or the extracted helper) — Function-Call-Return, not grep/source-presence.
    - `node --test tests/test_decide_modal_state.test.js` exits 0 with all 4 tests passing (no install prompt).
    - 4 `test(...)` blocks cover all 4 states; Test 4 asserts console.warn was called exactly once (DC-03).
    - `grep -i "jest\|npx jest" tests/test_decide_modal_state.test.js` returns NOTHING (no Jest dependency).
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && node --test tests/test_decide_modal_state.test.js</automated>
  </verify>
  <done>Four Node built-in (node:test) tests pass against the real _decideModalState with zero install, including the defensive console.warn assertion (BLOCKER-2 resolved — no Jest toolchain required).</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

This plan is FE-only (static/pip-launcher.js + a JS test). It reads no DB tables and writes no DB tables —
it consumes the in-memory socket/response payload `{outcome, confidence, source, call_id}` produced by
Plan 03's backend. Per CLAUDE.md Punkt 21 Geltungsbereich ("Skip-OK: rein UI-Tweaks ohne Daten-Pfad-Änderung"),
no inspect.sh evidence is required.

### Cross-Layer-Konsistenz-Tabelle (data-flow only, no persistence)
| Code-Variable / Feld | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| `data.confidence / .source / .outcome / .call_id` | socket/response payload from learning.py | RAM (network message) — produced by Plan 03 calls-UPDATE + emit | ✓ contract matches learning.py jsonify Z.153-159 + emit payload Z.140-145 |

<verification>
- _decideModalState returns the correct enum for all 4 inputs (node:test).
- All three call-sites + test hook route through the render entry; old (outcome||source) guards gone.
- No yellow; badge teal; user text keeps umlauts; identifiers ASCII.
</verification>

<success_criteria>
- Modal renders for confidence=0 and for low-confidence calls (the Bug-C failure no longer occurs).
- 4 Node built-in (node:test) unit tests green via `node --test` — zero install, no Jest required (BLOCKER-2).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.D.UX.1-transcript-persistence-outcome-force-wahl-bug-fix/08.23.2.D.UX.1-04-SUMMARY.md`
</output>
</content>
