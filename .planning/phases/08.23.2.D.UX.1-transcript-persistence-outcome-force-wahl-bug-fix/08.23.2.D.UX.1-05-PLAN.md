---
phase: 08.23.2.D.UX.1
plan: 05
type: execute
wave: 3
depends_on: ["08.23.2.D.UX.1-01", "08.23.2.D.UX.1-02", "08.23.2.D.UX.1-03", "08.23.2.D.UX.1-04"]
files_modified:
  - routes/settings.py
  - "04 Entscheidungen/NERVE DSGVO Analyse.md"
autonomous: false
requirements: [DD-01, DD-02, DD-03, DD-04, DP-01, DP-02, DT-01, DT-02, DT-03]
complexity: "🔴 komplex"
must_haves:
  truths:
    - "Deleting a user's calls cascades to transcript_segments via the conversation_logs FK (verified live, not assumed)"
    - "The account-deletion path writes an audit_log row ('user_deletion_request') with counts+IDs only, before any deletion — using the real audit_log columns (action/target_type/target_id/details), not the nonexistent event_type/payload"
    - "The soft-delete vs hard-delete gap (LANDMINE 8) is explicitly resolved with the user, not silently assumed to cascade"
    - "The cleanup/expiry path traverses calls.conversation_log_id -> conversation_logs.id -> transcript_segments (transcript_expires_at on calls does NOT auto-delete segments)"
    - "DSGVO Analyse.md Sektion 7 documents the two-table architecture, cascade path, 90-day retention, Art.17 + Art.5(2)"
    - "End-to-end: after a real test call, transcript_segments has rows and the modal rendered (DT-01/02/03)"
  artifacts:
    - path: "routes/settings.py"
      provides: "audit log_action on account deletion + resolved delete path"
      contains: "user_deletion_request"
    - path: "04 Entscheidungen/NERVE DSGVO Analyse.md"
      provides: "Sektion 7 extended for DB persistence + cascade + retention"
      contains: "transcript_segments"
  key_links:
    - from: "routes/settings.py delete path"
      to: "audit_log"
      via: "log_action(... 'user_deletion_request' ...)"
      pattern: "user_deletion_request"
    - from: "cleanup/delete traversal"
      to: "transcript_segments"
      via: "calls.conversation_log_id -> conversation_logs.id -> CASCADE"
      pattern: "conversation_log"
---

<objective>
Close the DSGVO loop and verify the whole chain end-to-end. This plan (a) resolves the soft-delete gap
(LANDMINE 8: today's account deletion is a soft `aktiv=False` flip with NO row delete, NO cascade trigger,
NO audit log — so DD-01's CASCADE never fires from app code and DD-02's counts assume a hard delete that
doesn't exist) via an explicit decision checkpoint with André; (b) adds the missing `log_action`
audit-entry (DD-02) using the REAL audit_log columns; (c) documents the cleanup/expiry traversal
(LANDMINE 6: `transcript_expires_at` is on `calls`, cascade is on `conversation_logs`); (d) extends
DSGVO Analyse.md Sektion 7 (DD-04); and (e) runs the post-deploy re-test (DT-01/02/03) verifying the modal
renders and transcript_segments persists on a real call.

Purpose: This phase exists to enforce cross-layer persistence integrity AND DSGVO accountability — the
final plan proves both hold in production.
Output: Resolved delete path + audit entry + DSGVO doc + live verification evidence.

Implements: DD-01 (cascade verified live), DD-02 (audit log_action), DD-03 (90-day retention documented),
DD-04 (DSGVO doc), DP-01 (Persistenz-Schicht-Verifikation), DP-02 (Pre-Insert-Audit), DT-01/02/03 (re-test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.1-transcript-persistence-outcome-force-wahl-bug-fix/08.23.2.D.UX.1-CONTEXT.md
@.planning/phases/08.23.2.D.UX.1-transcript-persistence-outcome-force-wahl-bug-fix/08.23.2.D.UX.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- audit helper (services/audit.py Z.5-26): log_action(db, user_id, org_id, action, target_type,
       target_id, details, request). details=json.dumps(...) into TEXT column. Already used app_routes.py Z.366-377. -->
<!-- AuditLog model (models.py Z.374-385) live columns: action(String100), target_type(String100),
       target_id(Integer), details(Text), ip_address, user_agent, created_at. NO event_type, NO payload. -->
<!-- delete endpoint TODAY (routes/settings.py delete_account ~Z.188-205) = SOFT delete:
       User.update({aktiv:False}); org.aktiv=False; db.commit(); flask_session.clear().
       NO row deletion, NO cascade, NO log_action. (LANDMINE 8) -->
<!-- cascade reality (LANDMINE 6): transcript_segments ON DELETE CASCADE hangs off conversation_logs.
       transcript_expires_at lives on calls. Deleting calls does NOT delete segments.
       Real Art.17 purge must delete conversation_logs rows -> cascade -> transcript_segments. -->
<!-- settings blueprint: settings_bp = Blueprint('settings', ...). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: [DECISION] Resolve the soft-delete vs hard-delete gap (LANDMINE 8)</name>
  <decision>
Today `settings.py::delete_account` is a SOFT delete (`aktiv=False`, no row removal, no cascade, no audit).
DD-01's ON DELETE CASCADE on transcript_segments therefore NEVER fires from the app, and DD-02's audit
counts assume a hard delete that does not exist. How should D.UX.1 satisfy DSGVO Art.17 for transcript data?
  </decision>
  <context>
This was flagged by Gemini + Claudian as unresolved in CONTEXT. We must not silently assume CASCADE works.
The cleanup/expiry path also matters: transcript_expires_at lives on `calls`, but the cascade hangs off
`conversation_logs` — so an expiring call would orphan its segments unless the purge traverses
calls.conversation_log_id -> conversation_logs.id -> transcript_segments.
  </context>
  <options>
    <option id="audit-only-now">
      <name>A — Keep soft-delete; add audit entry now; defer hard-purge job to a separate Art.17 phase</name>
      <pros>Minimal scope, no risky destructive code in this bug-fix phase, audit accountability satisfied immediately. transcript_segments still protected by 90-day expiry job (separate). DD-02 audit can record intended counts.</pros>
      <cons>Actual row deletion (and thus live CASCADE) is deferred; DD-01 remains a DB-level safety net not yet exercised by app code. Must explicitly create a follow-up roadmap item.</cons>
    </option>
    <option id="hard-delete-now">
      <name>B — Add a real hard-delete path now (delete conversation_logs rows so CASCADE fires) + audit</name>
      <pros>DD-01 cascade actually exercised; Art.17 fully satisfied in-phase; DD-02 counts are real.</pros>
      <cons>Introduces destructive deletion logic into a bug-fix phase; higher blast radius (touches users/orgs/calls/conversation_logs deletion ordering, FK constraints across 6+ tables); needs careful staging test. Larger scope, more review risk on a 🔴 phase.</cons>
    </option>
    <option id="expiry-purge-job">
      <name>C — Add the 90-day transcript-expiry purge job now (deletes conversation_logs/segments by age) + audit on account-delete</name>
      <pros>Directly implements DD-03 retention + exercises CASCADE via the purge; account-delete stays soft + audited; targets the transcript data specifically (the phase's subject).</pros>
      <cons>Cron/scheduled-job infrastructure may not exist; needs a runner; broader than a bug-fix.</cons>
    </option>
  </options>
  <resume-signal>Select: audit-only-now, hard-delete-now, or expiry-purge-job (and confirm whether a follow-up roadmap item should be created for the deferred part).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add audit log_action to the account-deletion path + implement chosen delete resolution (DD-01/DD-02)</name>
  <files>routes/settings.py</files>
  <read_first>
    - routes/settings.py Z.180-210 (delete_account in full — current SOFT-delete body)
    - services/audit.py Z.1-30 (log_action signature + details=json.dumps into TEXT)
    - database/models.py Z.374-385 (AuditLog real columns — confirm NO event_type/payload)
    - ssh -i ~/.ssh/id_ed25519_nerve root@staging.getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema audit_log' (verify live columns before writing the INSERT — Bug-A-class guard)
    - ssh -i ~/.ssh/id_ed25519_nerve root@staging.getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh constraints transcript_segments' (confirm FK CASCADE -> conversation_logs)
  </read_first>
  <action>
Implement the resolution chosen in Task 1. In ALL cases, add the DD-02 audit entry BEFORE any state
change, gathering counts via the cascade-aware traversal (LANDMINE 6):
```python
from services.audit import log_action
from database.models import ConversationLog, Call, TranscriptSegment

# counts for audit (no cleartext, IDs + counts only). Traverse calls -> conversation_logs -> segments.
_conv_ids = [r[0] for r in db.query(ConversationLog.id).filter(ConversationLog.user_id == g.user.id).all()]
_n_conv = len(_conv_ids)
_n_calls = db.query(Call).filter(Call.user_id == g.user.id).count()
_n_seg = (db.query(TranscriptSegment).filter(TranscriptSegment.conversation_log_id.in_(_conv_ids)).count()
          if _conv_ids else 0)
log_action(db, g.user.id, g.org.id, 'user_deletion_request',
           target_type='user', target_id=g.user.id,
           details={'transcript_segments_count': _n_seg, 'calls_count': _n_calls,
                    'conversation_logs_count': _n_conv, 'initiator': 'user_self_request'},
           request=request)
```
Then, per Task 1 decision:
- **audit-only-now (A):** keep the existing soft-delete (aktiv=False) UNCHANGED below the audit call.
  Add a code comment: "# Art.17 hard-purge of conversation_logs (-> CASCADE transcript_segments) deferred
  to follow-up phase; see roadmap." Create the follow-up roadmap item if André confirmed.
- **hard-delete-now (B):** after the audit call, DELETE the conversation_logs rows for this user
  (`db.query(ConversationLog).filter(ConversationLog.user_id==g.user.id).delete(synchronize_session=False)`)
  which CASCADES to transcript_segments; handle calls + other child rows per their FKs in correct order;
  commit; then proceed with the existing aktiv=False for the user/org account shell. Test FK ordering on
  staging carefully.
- **expiry-purge-job (C):** keep soft-delete + audit here; implement the 90-day purge in the chosen
  scheduler (delete conversation_logs older than 90 days for ephemeral storage -> CASCADE segments).

Pre-Insert-Control-Flow-Audit (DP-02 point — delete path): the audit INSERT goes through log_action
(verified helper), uses real audit_log columns, runs before deletion, stores only counts+IDs.
Identifiers ASCII; any user-facing flash text keeps umlauts.
  </action>
  <acceptance_criteria>
    - `grep "user_deletion_request" routes/settings.py` returns the audit action.
    - `grep "from services.audit import log_action" routes/settings.py` returns the import.
    - audit details contains transcript_segments_count, calls_count, conversation_logs_count, initiator (grep each key).
    - The audit call appears BEFORE any aktiv=False / delete statement in delete_account (verify ordering by reading the function).
    - `grep "event_type\|payload" routes/settings.py` shows NO use of those columns for audit (real columns only).
    - For option B only: a `.delete(` on ConversationLog filtered by user_id is present; for A/C: a deferral comment is present.
    - Staging import + (option-dependent) deletion test passes.
  </acceptance_criteria>
  <verify>
    <automated>bash deploy.sh staging && ssh -i ~/.ssh/id_ed25519_nerve deploy@staging.getnerve.app 'cd /opt/nerve && source venv/bin/activate && python -c "import routes.settings"' && echo "import-ok"</automated>
  </verify>
  <done>Account-deletion writes a counts-only audit_log row with real columns before any state change; chosen delete-resolution implemented; cascade traversal correct.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend DSGVO Analyse.md Sektion 7 (DD-03/DD-04)</name>
  <files>04 Entscheidungen/NERVE DSGVO Analyse.md</files>
  <read_first>
    - "04 Entscheidungen/NERVE DSGVO Analyse.md" Sektion 7 (Anonymisierungs-Pipeline — current content to extend)
  </read_first>
  <action>
Extend Sektion 7 of the DSGVO document with the new DB-persistence reality (DD-04). Add subsections covering:
- **Zwei-Tabellen-Architektur:** `public.transcript_segments` (FK to conversation_logs, user-bound, 90-day
  'ephemeral' retention, opt-in 'consented_full') vs. `training.transcript_archive` (no FK, cryptographically
  anonymized via salted source_call_hash + per-call randomized ts_offset_ms, permanent — decoupled from user
  identity for DPO training).
- **Cascade-Delete-Pfad:** ON DELETE CASCADE on transcript_segments.conversation_log_id; the Art.17 deletion
  path traverses calls.conversation_log_id -> conversation_logs.id -> transcript_segments (note explicitly
  that transcript_expires_at lives on calls and does NOT auto-delete segments — purge must go via
  conversation_logs).
- **Aufbewahrungsfristen (DD-03):** 90 Tage ephemeral for transcript_segments; permanent for the anonymized
  training archive.
- **Rechenschaftspflicht (Art. 5 Abs. 2) + Art. 17:** the audit_log 'user_deletion_request' entry (counts+IDs
  only, no cleartext) documents deletion requests; reference the resolution chosen in Task 1 (soft+audit /
  hard-delete / expiry-job).
- **Timestamp-Korrelations-Angriff-Schutz:** rationale for the randomized ts_offset_ms (Gemini 2026-05-28).
German user-facing prose; ASCII not required in a Markdown doc (umlauts fine).
  </action>
  <acceptance_criteria>
    - `grep "transcript_segments" "04 Entscheidungen/NERVE DSGVO Analyse.md"` returns new content.
    - `grep "transcript_archive" "04 Entscheidungen/NERVE DSGVO Analyse.md"` returns new content.
    - `grep -i "90 Tage\|90-Tage\|ephemeral" "04 Entscheidungen/NERVE DSGVO Analyse.md"` returns retention content.
    - `grep -i "Art. 17\|Art.17\|Art. 5" "04 Entscheidungen/NERVE DSGVO Analyse.md"` returns the accountability reference.
    - `grep -i "ts_offset_ms\|Korrelations" "04 Entscheidungen/NERVE DSGVO Analyse.md"` returns the attack-protection rationale.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "transcript_segments" "04 Entscheidungen/NERVE DSGVO Analyse.md"</automated>
  </verify>
  <done>Sektion 7 documents two-table architecture, cascade path, retention, Art.17/Art.5(2), and the offset-randomization rationale.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [RE-TEST] Live verification on Production — modal renders + transcript persists (DT-01/02/03)</name>
  <what-built>All of D.UX.1: migration 0010 + model (Plan 01), write-path + training double-write (Plan 02), read-path + threshold fix (Plan 03), 4-state modal (Plan 04), DSGVO audit (this plan). Now prove it works on a real call.</what-built>
  <read_first>
    - CLAUDE.md HART: default test env is Production; verify on the server, never locally. Use the dedicated is_test_user account (D.UX.0) so the test call is filtered from DPO + analytics.
  </read_first>
  <how-to-verify>
1. Deploy everything to Production: `bash deploy.sh production` (confirm migration 0010 already applied on Production — if Plan 01 only applied to staging, apply head on Production now as the DB-owning user).
2. DT-01 — Re-run André's D.UX-UAT script-reading test on getnerve.app with the test-user account: do a short live call, end it, and confirm the outcome modal APPEARS (it previously did not).
3. DT-02 — Edge case "kurzer Call ohne klares Outcome": do a very short/ambiguous call. Confirm the Force-Wahl modal renders with 7 buttons (Zustand 1 'Automatische Erkennung nicht verfügbar' if confidence=0, or Zustand 3 'KI unsicher — bitte wählen' if low confidence). Confirm NO yellow, badge is teal.
4. DT-03 — Verify persistence via inspect.sh (run against Production, or staging if that is where the test call landed):
   `ssh -i ~/.ssh/id_ed25519_nerve root@staging.getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh sample transcript_segments 20'`
   and the count for the last call:
   `ssh ... 'cd /opt/nerve/app && bash scripts/inspect.sh count transcript_segments'`
   (CONTEXT DT-03 SQL: `SELECT COUNT(*) FROM transcript_segments WHERE conversation_log_id = <letzter_call_id>` — expect > 0.)
5. Confirm classify() got real text (no blind 0.65): check `inspect.sh logs` for the `[D.UX.1] transcript_segments INSERT conv=... added=N` (N>0) and confirm calls.outcome_confidence for the test call is no longer the degenerate blind value.
6. Paste the inspect.sh sample/count output into Sektion 5 below.
  </how-to-verify>
  <resume-signal>Type "re-test passed" once the modal renders for both a normal and an ambiguous call AND transcript_segments count > 0 for the test call, OR describe what failed (which plan to revisit).</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `audit_log` (schreiben) — real columns action/target_type/target_id/details (TEXT JSON).
- `conversation_logs` (lesen für Counts; ggf. löschen je Task-1-Entscheidung) — FK-Quelle für Cascade.
- `transcript_segments` (lesen für Counts + DT-03 Verifikation; CASCADE-Ziel).
- `calls` (lesen für Counts).

### inspect.sh-Beleg (executor MUST paste verbatim)
```
$ ssh -i ~/.ssh/id_ed25519_nerve root@staging.getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema audit_log'
[PLACEHOLDER — paste verbatim; confirm columns action/target_type/target_id/details; NO event_type/payload]

$ ssh -i ~/.ssh/id_ed25519_nerve root@staging.getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh constraints transcript_segments'
[PLACEHOLDER — confirm FK ON DELETE CASCADE -> conversation_logs(id)]

$ ssh -i ~/.ssh/id_ed25519_nerve root@staging.getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh count transcript_segments'   # DT-03 after test call
[PLACEHOLDER — expect > 0 for the test call's conversation_log_id]
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `log_action(... 'user_deletion_request' ...)` details | geschrieben | DB audit_log.details (TEXT JSON) | ⬜ inspect.sh audit_log (real columns) |
| counts `_n_seg/_n_calls/_n_conv` | gelesen für audit | DB transcript_segments/calls/conversation_logs | ⬜ |
| transcript_segments.conversation_log_id CASCADE | FK | DB-Constraint (deletion of conversation_logs row -> segments) | ⬜ inspect.sh constraints |
| `transcript_expires_at` | (calls column — NICHT cascade-Quelle) | DB calls.transcript_expires_at | ✓ documented gap (LANDMINE 6) — purge must go via conversation_logs |

### Bei Diskrepanz: STOP + Replan

<verification>
- Audit entry written with real audit_log columns before any deletion; counts traverse the cascade chain.
- Soft/hard-delete gap explicitly resolved (Task 1 decision), not assumed.
- DSGVO doc Sektion 7 extended.
- Live re-test: modal renders + transcript_segments count > 0 (DT-01/02/03).
</verification>

<success_criteria>
- DSGVO accountability satisfied (audit entry, documented retention + cascade path).
- The phase goal proven live: the outcome modal renders for low/zero-confidence calls and the transcript persists to DB.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.D.UX.1-transcript-persistence-outcome-force-wahl-bug-fix/08.23.2.D.UX.1-05-SUMMARY.md`
</output>
