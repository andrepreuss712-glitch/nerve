---
phase: 08.23.2.D.UX.1
plan: 03
subsystem: api
tags: [flask, learning, outcome_service, socketio, confidence-threshold]
requires:
  - phase: 08.23.2.D.UX.1-01
    provides: transcript_segments table
provides:
  - learning.py reads transcript_segments (DA-05) instead of nonexistent getattr log_entries
  - confidence-threshold rewrite (keeps best-guess <0.70, typed telemetry at conf=0)
  - guard-independent outcome_ready emit (BLOCKER-1, fires for confidence=0)
affects: [08.23.2.D.UX.1-04, 08.23.2.D.UX.1-05]
tech-stack:
  added: []
  patterns: [stable two-column order (ts_ms, id) for tie-safe transcript ordering]
key-files:
  created: []
  modified: [routes/learning.py]
key-decisions:
  - "Bug A read-half: query transcript_segments order_by (ts_ms, id), emit {ts_ms,speaker,text}"
  - "Bug B: ==0 first (null+typed telemetry); <0.70 keeps Haiku best-guess as ai_auto_unsicher; 0.90/0.70 unchanged"
  - "BLOCKER-1: outcome_ready emit confirmed sibling of write-guard -> fires for conf=0"
patterns-established:
  - "outcome_ready emit guard-independent so FE always gets a payload"
requirements-completed: [DA-05, DB-01, DB-02, DB-03, DB-04, DP-02]
duration: ~30min
completed: 2026-05-30
---

# Phase 08.23.2.D.UX.1 Plan 03: learning.py read-path + threshold + emit

**classify() now gets real transcript text from DB (no blind 0.65); low-confidence best-guess kept; emit fires for confidence=0**

## Accomplishments
- Replaced broken `getattr(_conv, 'log_entries')` with `query(TranscriptSegment).order_by(ts_ms, id)` → `{ts_ms,speaker,text}` dicts (DA-05)
- Threshold rewrite: conf==0 → null + typed telemetry (`empty_transcript`/`classify_zero`); 0<conf<0.70 → keep best-guess, source `ai_auto_unsicher` (DB-02); thresholds 0.90/0.70 unchanged (DB-03)
- Confirmed + documented the `outcome_ready` emit is a sibling of the calls-UPDATE write-guard (already guard-independent) → fires for conf=0 so FE renders Zustand 1 (BLOCKER-1); jsonify fallback unconditional

## Task Commits
1. **Tasks 1-3: read-path + threshold + emit** - `3c5ff46` (fix)

## Deviations from Plan
- **Plan verify referenced `tests/test_outcome_threshold.py` which does not exist** (Plan 03 files_modified = learning.py only, no test artifact). Verified instead via existing `test_postcall_outcome_route.py` + live re-test. No code deviation.
- Emit was already a sibling (not nested) — Task 3 was confirm + comment, as the plan's expected case.

## Live verification
- Test call (conv 200): outcome=meeting_booked, confidence=0.96, source=ai_auto → Haiku got real transcript text (Bug A read-half working). A separate call shows 0.75 → ai_auto_unsicher (best-guess kept, Bug B working).

## Persistenz-Schicht-Verifikation (DP-02)
- Read: transcript_segments (ts_ms/speaker/text). Write: calls.outcome/outcome_confidence/outcome_source (existing UPDATE). Emit: SocketIO room message (RAM) → Plan 04 FE.

## Next Phase Readiness
- Backend feeds correct data + payload to the FE modal (Plan 04). Live-verified.

---
*Phase: 08.23.2.D.UX.1*
*Completed: 2026-05-30*
