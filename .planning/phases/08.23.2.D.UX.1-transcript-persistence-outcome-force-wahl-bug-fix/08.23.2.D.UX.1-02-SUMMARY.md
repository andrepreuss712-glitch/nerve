---
phase: 08.23.2.D.UX.1
plan: 02
subsystem: api
tags: [flask, transcript_segments, anonymization, idempotency, pytest]
requires:
  - phase: 08.23.2.D.UX.1-01
    provides: transcript_segments table + TranscriptSegment model
provides:
  - call-end transcript_segments INSERT (idempotent) in api_beenden
  - module-scope transform helper (_transcript_entries_to_segments)
affects: [08.23.2.D.UX.1-03, 08.23.2.D.UX.1-05]
tech-stack:
  added: []
  patterns: [pure transform helper at module scope for Function-Call-Return testing]
key-files:
  created: [tests/test_transcript_segments_write.py]
  modified: [routes/app_routes.py]
key-decisions:
  - "WARN-4: ts is wall-clock (deepgram_service.py:64) -> derive ts_ms relative to first entry + monotonic clamp"
  - "WARN-5: speaker int 0/1 -> berater/kunde, None/unknown -> system (CHECK-safe)"
  - "DA-06 training double-write DEFERRED to Phase E (nerve_app lacks training-schema privs; no consumer yet)"
patterns-established:
  - "Idempotent child-insert mirroring ObjectionEvent loop; never breaks finalization"
requirements-completed: [DA-04, DP-02]
duration: ~40min
completed: 2026-05-30
---

# Phase 08.23.2.D.UX.1 Plan 02: Call-end transcript write-path

**api_beenden persists anonymized RAM transcript into transcript_segments (idempotent), with a unit-tested pure transform; DA-06 training double-write deferred**

## Accomplishments
- `_transcript_entries_to_segments` (module scope): filters non-transcript/empty, maps speaker, derives ts_ms ms-from-start (monotonic-clamped), list order authoritative
- Idempotent INSERT in api_beenden after saved_conv_id, mirroring ObjectionEvent loop; try/except so transcript failure never breaks call finalization
- Function-Call-Return pytest (4 tests, green on Production)

## Task Commits
1. **Task 1: write-path + transform + test** - `381054c` (feat)
2. **DA-06 deferral documented** - `55a3cde` (docs)

## Deviations from Plan
**1. DA-06 (training.transcript_archive double-write) DEFERRED — Andre decision 2026-05-30 (Option A path).**
- The app connects as nerve_app, which has no USAGE on schema training nor grant on transcript_archive (only postgres + nerve_anon_worker). A direct bg-write would be permission-denied. Sole consumer (Phase E DPO collector) does not exist yet. Deferred to Phase E. Plan 02 Task 2 not implemented; deferral documented in code.
- **Requirements impact:** DA-06 moved to Phase E.

**2. WARN-4 ts derivation corrected from plan's naive _ts_to_ms.**
- Evidence: `deepgram_service.py:64` writes `ts = datetime.now().strftime('%H:%M:%S')` (wall-clock). Plan's literal `_ts_to_ms` would store time-of-day. Per WARN-4 case-3 rule, derive ts_ms = (entry - first_entry) with monotonic clamp. Verified live: ts_ms 29000/34000/38000/43000 (monotonic, ms-from-start).

## Issues Encountered
- Cold-call segments map to speaker='system' (single-speaker → roles never confirmed → entry speaker=None → 'system'). CHECK-safe, ordering intact. Training-label refinement (berater attribution) is a future consideration, not a D.UX.1 bug.

## Persistenz-Schicht-Verifikation (DP-02)
- Write: transcript_segments (ts_ms/speaker/text/conversation_log_id) — verified live, 11 rows for conv 200.
- Read source: ls.conversation_log (RAM, module-global), unchanged (Z.85-86).

## Next Phase Readiness
- transcript_segments populated at call-end; Plan 03 read-path consumes it. Live-verified.

---
*Phase: 08.23.2.D.UX.1*
*Completed: 2026-05-30*
