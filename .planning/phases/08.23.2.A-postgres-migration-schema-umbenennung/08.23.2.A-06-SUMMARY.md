---
phase: 08.23.2.A
plan: "06"
subsystem: database/alembic
tags: [alembic, migration, postgres, schema]
dependency_graph:
  requires:
    - 08.23.2.A-01  # Alembic setup
    - 08.23.2.A-02  # env.py + alembic.ini
    - 08.23.2.A-03  # models.py with Call + CallEvent
    - 08.23.2.A-04  # FT-table prune
  provides:
    - Baseline Alembic migration file 0001
    - Foundation for all future schema changes via alembic revision --autogenerate
  affects: []
tech_stack:
  added: []
  patterns:
    - Alembic op.create_table for all 35 tables
    - Manual op.execute() for dialect-specific CHECK constraints
    - postgresql_using='gin' for GIN index on JSONB column
key_files:
  created:
    - alembic/versions/0001_initial_postgres_schema.py
  modified: []
decisions:
  - Migration created manually because Postgres 16 is not yet installed; must be regenerated/verified once Postgres is available
  - CHECK constraints added via op.execute() ALTER TABLE because Alembic autogenerate typically misses dialect-specific constraints
  - 35 tables total: 33 legacy tables + calls + call_events
metrics:
  duration: "15 minutes"
  completed: "2026-05-12"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 1
---

# Phase 08.23.2.A Plan 06: Alembic Baseline Migration Summary

**One-liner:** Manually created Alembic baseline migration (0001) capturing all 35 Postgres tables with CHECK constraints and GIN index — requires regeneration once Postgres 16 is installed.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Generate Alembic baseline migration | 9e9b745 | alembic/versions/0001_initial_postgres_schema.py |

## Acceptance Criteria Verification

| Criterion | Status |
|-----------|--------|
| `alembic/versions/0001_initial_postgres_schema.py` exists | PASS |
| `def upgrade()` function present | PASS |
| `ft_call_sessions` / `ft_assistant_events` NOT in migration | PASS |
| `op.create_table('calls', ...)` present | PASS |
| `op.create_table('call_events', ...)` present | PASS |
| `ck_calls_call_mode` CHECK constraint present | PASS |
| `ck_call_events_event_type` CHECK constraint present | PASS |
| `ck_calls_transcript_storage` CHECK constraint present | PASS |
| `ck_calls_outcome` CHECK constraint present | PASS |
| `idx_call_events_payload_gin` GIN index present | PASS |
| `revision = '0001'` set | PASS |
| `down_revision = None` set | PASS |
| `op.create_table` count = 35 | PASS (35 found) |

## Deviations from Plan

### Auto-applied fallback

**[Fallback - Postgres not available] Manual migration file creation**

- **Found during:** Task 1 (Step 0 — pre-check)
- **Issue:** Postgres 16 is not installed on this machine (`psql: command not found`). The `alembic revision --autogenerate` command cannot run without a live Postgres connection.
- **Fix:** Created `alembic/versions/0001_initial_postgres_schema.py` manually per the plan's explicit fallback instructions (plan section: "IF Postgres is NOT available yet").
- **What was done:**
  - All 35 SQLAlchemy models from `database/models.py` transcribed to `op.create_table()` calls
  - Table creation order respects FK dependencies (organisations first, then users/profiles, then dependent tables)
  - CHECK constraints added via `op.execute("ALTER TABLE ... ADD CONSTRAINT ...")` for `calls` (3 constraints) and `call_events` (1 constraint)
  - GIN index `idx_call_events_payload_gin` on `call_events.payload` added via `op.create_index(..., postgresql_using='gin')`
  - Conditional index `idx_calls_mode_outcome` added with `postgresql_where=sa.text('outcome IS NOT NULL')`
  - `revision = '0001'`, `down_revision = None`
  - No ft_call_sessions or ft_assistant_events (correctly excluded — pruned in Plan 04)
- **Files modified:** `alembic/versions/0001_initial_postgres_schema.py` (created)
- **Commit:** 9e9b745
- **Action required:** Once Postgres 16 is installed on the server, run:
  ```bash
  # Verify nerve_test has 0 tables
  sudo -u nerve_app psql -d nerve_test -c "SELECT COUNT(*) FROM pg_tables WHERE schemaname='public';"
  # Optionally regenerate to cross-check autogenerate output vs. this manual file
  DATABASE_URL=postgresql://nerve_test_user@/nerve_test alembic upgrade head
  # Then stamp current on nerve_test
  DATABASE_URL=postgresql://nerve_test_user@/nerve_test alembic current
  ```

## Known Stubs

None. The migration file captures the full schema from models.py. No placeholder data or stub values.

## Threat Flags

None. No new network endpoints, auth paths, or trust boundary changes introduced. The migration file is a pure DDL artifact (no runtime execution path until cutover in Plan 09).

## Self-Check: PASSED

- `alembic/versions/0001_initial_postgres_schema.py` exists: CONFIRMED
- Commit 9e9b745 exists: CONFIRMED
- 35 `op.create_table` calls: CONFIRMED
- All CHECK constraints present: CONFIRMED
- GIN index present: CONFIRMED
- No ft_* tables: CONFIRMED
