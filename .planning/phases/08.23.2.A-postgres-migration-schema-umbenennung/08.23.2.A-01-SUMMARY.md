---
phase: 08.23.2.A-postgres-migration-schema-umbenennung
plan: "01"
subsystem: database
tags: [sqlalchemy, postgres, uuid, jsonb, bigserial, check-constraints, gin-index, schema-migration]

# Dependency graph
requires: []
provides:
  - "Call SQLAlchemy model (calls table): UUID-PK, nullable deferred FKs, CHECK constraints, composite indices"
  - "CallEvent SQLAlchemy model (call_events table): BIGSERIAL-PK via BigInteger, UUID FK→calls CASCADE, JSONB payload, GIN index"
  - "FtCallSession and FtAssistantEvent removed from database/models.py"
affects:
  - 08.23.2.A-02 (Alembic baseline migration reads these models)
  - 08.23.2.A-03 (migration script imports Call/CallEvent)
  - 08.23.2.B (code refactor: all FtCallSession/FtAssistantEvent references)
  - 08.23.2.F (FK completion for tenant_id/account_id)
  - 08.23.2.G (FK completion for account_id/contact_id)

# Tech tracking
tech-stack:
  added:
    - sqlalchemy.dialects.postgresql.UUID
    - sqlalchemy.dialects.postgresql.JSONB
    - sqlalchemy.BigInteger
    - sqlalchemy.CheckConstraint
    - sqlalchemy.Index (with postgresql_where partial index support)
    - sqlalchemy.text
  patterns:
    - "Deferred FK pattern: nullable UUID column + mandatory phase comment until target table exists"
    - "New architecture section at bottom of models.py per D-03"
    - "BigInteger for BIGSERIAL PK and Unix-ms timestamps (2038-safe per C-4)"

key-files:
  created: []
  modified:
    - database/models.py

key-decisions:
  - "D-03: New models appended to existing models.py (not a new file) — CLAUDE.md Vault-Regel 'Bestehende Dateien erweitern'"
  - "D-04: Deferred FK columns nullable=True without ForeignKey() until target tables exist in 08.23.2.F/G; mandatory phase comments on each"
  - "D-05: CHECK constraints and indices in __table_args__ as single source of truth for Alembic autogenerate"
  - "D-09: FtCallSession/FtAssistantEvent deleted without stub — no replacement comment, no migration shim"
  - "CallEvent.event_ts_ms uses BigInteger (BIGINT) — Unix ms timestamps exceed 2^31 after 2038 (C-4 fix)"

patterns-established:
  - "Deferred FK: Column(UUID(as_uuid=True), nullable=True) with # FK-Verknüpfung wird in Phase X.Y nachgereicht comment"
  - "New architecture tables go in '# --- Neue Architektur-Tabellen (Phase 08.23.2.A+) ---' section at end of models.py"

requirements-completed: [REQ-1, REQ-2, REQ-3, REQ-10]

# Metrics
duration: 12min
completed: 2026-05-12
---

# Phase 08.23.2.A Plan 01: Schema-Umbenennung Summary

**FtCallSession + FtAssistantEvent deleted from models.py; Call (UUID-PK, CHECK constraints, deferred nullable FKs) and CallEvent (BIGSERIAL-PK, JSONB payload, GIN index, CASCADE FK) added as new architecture section**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-12T08:30:00Z
- **Completed:** 2026-05-12T08:42:00Z
- **Tasks:** 2 of 2
- **Files modified:** 1

## Accomplishments
- Deleted FtCallSession (ft_call_sessions) and FtAssistantEvent (ft_assistant_events) class definitions entirely — 0 traces remain in models.py
- Added Call model with UUID primary key, 3 deferred nullable UUID FKs (tenant/account/contact), Integer FK to users.id, 3 CHECK constraints (call_mode, transcript_storage, outcome), 2 composite indices + 1 partial index
- Added CallEvent model with BigInteger BIGSERIAL PK, UUID FK to calls.id with ON DELETE CASCADE, JSONB payload, BigInteger event_ts_ms (2038-safe), GIN index on payload, 3 additional indices
- Extended SQLAlchemy imports with BigInteger, CheckConstraint, Index, text + added postgresql dialect imports (UUID, JSONB) + uuid stdlib

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete FtCallSession and FtAssistantEvent from models.py** - `713e2e4` (feat)
2. **Task 2: Add Call and CallEvent classes to models.py** - `848786d` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `database/models.py` — FtCallSession + FtAssistantEvent removed (59 lines deleted); Call + CallEvent added (56 lines); import line extended; uuid import added

## Decisions Made
- Followed D-03/D-04/D-05/D-09 exactly as specified in CONTEXT.md
- BigInteger used for both CallEvent.id (BIGSERIAL PK) and CallEvent.event_ts_ms (Unix ms, C-4 fix) — both required per SPEC

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- database/models.py is the single source of truth for the new schema
- Alembic autogenerate (Plan 02) can now read Call + CallEvent __table_args__ to generate correct DDL with CHECK constraints and GIN index
- Migration script (Plan 03) can import Call + CallEvent for bulk_save_objects()
- All FtCallSession/FtAssistantEvent references in other files (deepgram_service.py, claude_service.py, routes/app_routes.py, test files) remain to be cleaned up in Phase 08.23.2.B

## Self-Check

- [x] `database/models.py` exists and modified
- [x] Commit 713e2e4 exists (Task 1 — delete Ft classes)
- [x] Commit 848786d exists (Task 2 — add Call/CallEvent)
- [x] `from database.models import Call, CallEvent, Organisation, User` — exits 0
- [x] `grep -c "class Ft" database/models.py` — returns 0
- [x] `grep -c "class Call" database/models.py` — returns 2
- [x] BigInteger present on both CallEvent.id and CallEvent.event_ts_ms
- [x] 4 deferred-FK comments present
- [x] 3 CHECK constraint names for Call, 1 for CallEvent

## Self-Check: PASSED

---
*Phase: 08.23.2.A-postgres-migration-schema-umbenennung*
*Completed: 2026-05-12*
