---
phase: 08.23.2.A
plan: "03"
subsystem: services
tags: [dead-code-prune, ft-logging, refactor]
dependency_graph:
  requires: [08.23.2.A-01, 08.23.2.A-02]
  provides: [services-free-of-ft-models]
  affects: [services/deepgram_service.py, services/claude_service.py]
tech_stack:
  added: []
  patterns: [ersatzlos-loeschen per D-06/D-07/D-09]
key_files:
  modified:
    - services/deepgram_service.py
    - services/claude_service.py
  deleted:
    - scripts/export_ft_jsonl.py
decisions:
  - "D-06: FtCallSession writer block deleted entirely from deepgram_service.py — no stub"
  - "D-07: _write_ft_assistant_event function and all call sites deleted entirely from claude_service.py — no stub"
  - "D-09: scripts/export_ft_jsonl.py git-removed — call_events schema is JSONB-based, fresh export in 08.23.2.E"
  - "user_id assignment retained in deepgram_service.py — used by Phase 08.19.4 SID init block after the deleted FT block"
metrics:
  duration: "8 minutes"
  completed: "2026-05-12T08:39:01Z"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 2
  files_deleted: 1
  lines_deleted: 225
---

# Phase 08.23.2.A Plan 03: FT Dead-Code Prune (services + scripts) Summary

**One-liner:** Deleted FtCallSession writer block from deepgram_service.py, entire _write_ft_assistant_event function plus both call sites from claude_service.py, and git-removed scripts/export_ft_jsonl.py — zero FT model references remain in services/.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Delete FtCallSession writer block from deepgram_service.py | c965dc0 | services/deepgram_service.py (-37 lines) |
| 2 | Delete _write_ft_assistant_event from claude_service.py + delete export_ft_jsonl.py | e4fc949 | services/claude_service.py (-188 lines), scripts/export_ft_jsonl.py (deleted) |

## What Was Done

### Task 1 — deepgram_service.py

Deleted the entire FT logging try/except block (original lines 394–427):
- `FtCallSession` insert with `User` query, `db.add()`, `db.commit()`, `ls.state` writes
- The WR-04 comment (only explained the FT block context — dead after deletion)
- `user_id = flask_session.get('user_id')` assignment **retained** — still used by the Phase 08.19.4 per-SID profile init block immediately below

No top-level `FtCallSession` import existed (all imports were inline in the deleted block).

### Task 2 — claude_service.py

Deleted:
- `_write_ft_assistant_event` function definition (original lines 142–229): 88 lines including all DSGVO enforcement logic, FtAssistantEvent DB write, json dump helpers
- Call site 1 in `analyse_loop`: FT logging hook with `assistant_live` module (22 lines including try/except wrapper)
- Call site 2 in `coaching_loop`: FT logging hook with `coaching_live` module (20 lines including try/except wrapper)
- Dead reference to `_write_ft_assistant_event` in the coaching WebSocket comment (updated comment to remove the stale mention)

### Task 2 — scripts/export_ft_jsonl.py

Removed via `git rm` — file exported `ft_call_sessions` + `ft_assistant_events` data in JSONL format for fine-tuning. New `call_events` schema is JSONB-based and fundamentally different; fresh export pipeline comes in Phase 08.23.2.E.

## Verification

```
grep -rn "FtCallSession|FtAssistantEvent" services/  → 0 matches (PASS)
ls scripts/export_ft_jsonl.py                        → No such file (PASS)
python -c "import services.deepgram_service, services.claude_service; print('ok')"  → ok (PASS)
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Dead WR-04 comment cleanup in deepgram_service.py**
- **Found during:** Task 1
- **Issue:** Comment "WR-04: user_id extracted before try-blocks so it's always defined even if the FT logging block raises" only made sense in context of the now-deleted FT block
- **Fix:** Removed the comment; kept the `user_id` assignment (still needed by Phase 08.19.4 block)
- **Files modified:** services/deepgram_service.py
- **Commit:** c965dc0

**2. [Rule 1 - Bug] Dead `_write_ft_assistant_event` reference in coaching comment**
- **Found during:** Task 2
- **Issue:** Comment at line ~1561 listed `_write_ft_assistant_event (unten)` as a data retention path — after deletion this is a misleading dead reference
- **Fix:** Removed the stale mention from the comment, kept the rest of the historical explanation intact
- **Files modified:** services/claude_service.py
- **Commit:** e4fc949

## Known Stubs

None — all deletions are complete, no placeholder code introduced.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. This plan is pure deletion — threat surface reduced.

## Self-Check: PASSED

- services/deepgram_service.py — modified, exists: confirmed
- services/claude_service.py — modified, exists: confirmed
- scripts/export_ft_jsonl.py — deleted: confirmed (git rm)
- Commit c965dc0 — exists: confirmed
- Commit e4fc949 — exists: confirmed
- `grep -rn "FtCallSession|FtAssistantEvent" services/` — 0 matches: confirmed
- Both services import cleanly: confirmed
