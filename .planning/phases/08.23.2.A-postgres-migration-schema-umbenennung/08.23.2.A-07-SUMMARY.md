---
phase: 08.23.2.A
plan: "07"
subsystem: infrastructure
tags: [postgres, server-setup, runbook, hetzner, migration-prep]
dependency_graph:
  requires: [08.23.2.A-05, 08.23.2.A-06]
  provides: [postgres-server-ready, nerve-db-configured, sqlite-backup]
  affects: [08.23.2.A-08, 08.23.2.A-09]
tech_stack:
  added: [postgresql-16, pg_hba-peer-auth]
  patterns: [unix-socket-only, restricted-db-user, ALTER-DEFAULT-PRIVILEGES]
key_files:
  created: [docs/postgres-server-setup.md]
  modified: []
decisions:
  - "DATABASE_URL NOT set during setup — switched exclusively in Plan 09 Step 7 cutover window (C-5)"
  - "TRUNCATE in dry-run cleanup runs as postgres superuser — nerve_app has no TRUNCATE right (C-3)"
  - "ALTER DEFAULT PRIVILEGES ensures future tables from 08.23.2.F/G/H auto-accessible without manual GRANT"
metrics:
  duration: "~7 min (Task 1 auto)"
  completed: 2026-05-12
  tasks_completed: 1
  tasks_total: 1 auto + 1 checkpoint
  files_created: 1
---

# Phase 08.23.2.A Plan 07: Postgres Server Setup Runbook Summary

**One-liner:** Complete Postgres 16 server setup runbook with de_DE.UTF-8 DBs, restricted peer-auth user, TCP-disabled unix socket, and dry-run procedure for Hetzner VPS.

## What Was Built

`docs/postgres-server-setup.md` — 9-section runbook covering:

1. SQLite backup to local laptop (timestamped filename + backup-log.txt)
2. Postgres 16 installation via apt on Hetzner (178.104.82.166)
3. `nerve` + `nerve_test` DBs with UTF-8 / de_DE.UTF-8 / TEMPLATE template0 (D-13)
4. `nerve_app` + `nerve_test_user` with restricted GRANT (no DROP/CREATE/ALTER/TRUNCATE) + ALTER DEFAULT PRIVILEGES for future phases 08.23.2.F/G/H (D-14)
5. `pg_hba.conf` peer auth entries + `listen_addresses=''` disables TCP (D-15)
6. Dedicated `nerve_app` Linux user for peer auth matching
7. End-to-end connection verification checklist
8. Explicit "DATABASE_URL — DO NOT SET YET" section (C-5 fix: deferred to Plan 09)
9. Dry-run procedure with TRUNCATE via `postgres` superuser (C-3: nerve_app has no TRUNCATE right)

## Status

**CHECKPOINT PENDING** — Task 1 (auto) complete and committed. Waiting for André to execute server setup steps on Hetzner VPS and confirm with "server-setup-complete".

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — this plan creates documentation only (no application code).

## Threat Flags

No new security surface introduced. This plan creates a docs file only; all mitigations from the threat model are addressed in the runbook content:
- T-07-01: nerve_app GRANT excludes DROP/CREATE/ALTER/TRUNCATE ✓
- T-07-02: listen_addresses='' in Section 5 ✓
- T-07-03: SQLite backup stored locally (accepted) ✓
- T-07-04: Timestamped backup filename in Section 1 ✓
- T-07-05: TRUNCATE via postgres superuser in Section 9 ✓

## Self-Check

- [x] `docs/postgres-server-setup.md` created: `b723d3d`
- [x] All 7 acceptance criteria grep checks pass
- [x] Commit b723d3d exists in git log
