---
phase: 08.23.2.A
plan: "04"
type: execute
wave: 2
depends_on:
  - "08.23.2.A-01"
files_modified:
  - routes/app_routes.py
  - tests/test_ft_lifecycle.py
  - tests/test_ft_models.py
  - tests/test_ft_write_hooks.py
  - tests/test_per_sid_migration.py
  - tests/test_ab_stats.py
  - tests/test_ft_seed.py
autonomous: true
requirements:
  - REQ-4

must_haves:
  truths:
    - "routes/app_routes.py has no FtCallSession import or ft_call_sessions read/write"
    - "test_ft_lifecycle.py, test_ft_models.py, test_ft_write_hooks.py are deleted"
    - "test_per_sid_migration.py has test_write_ft_event_isolation_per_sid deleted but other 5 tests intact"
    - "test_ab_stats.py imports FtCallSession removed"
    - "test_ft_seed.py assessed and cleaned if it references FtCallSession/FtAssistantEvent"
  artifacts:
    - path: "routes/app_routes.py"
      provides: "app_routes without FtCallSession update block"
    - path: "tests/test_per_sid_migration.py"
      provides: "Per-SID isolation tests (5 functions) without the FT-event isolation test"
  key_links:
    - from: "routes/app_routes.py"
      to: "FtCallSession removal"
      via: "lines 364-386 deleted (ft_call_sessions aggregates update block)"
      pattern: "FT logging.*update ft_call_sessions"
---

<objective>
Delete FtCallSession read/write block from routes/app_routes.py (D-08), delete 3 test files completely (D-10), surgically remove test_write_ft_event_isolation_per_sid from test_per_sid_migration.py (D-11), and clean unused FtCallSession import from test_ab_stats.py.

Purpose: Completes code refactoring — removes all remaining FtCallSession/FtAssistantEvent references from routes and tests. After this plan + Plan 03, the grep check `grep -rn "FtCallSession\|FtAssistantEvent" routes/ services/ scripts/ tests/` must return 0.
Output: Routes and tests free of all FT model references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.A-postgres-migration-schema-umbenennung/08.23.2.A-CONTEXT.md
@.planning/phases/08.23.2.A-postgres-migration-schema-umbenennung/08.23.2.A-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete FtCallSession block from app_routes.py; delete 3 test files</name>
  <files>routes/app_routes.py, tests/test_ft_lifecycle.py, tests/test_ft_models.py, tests/test_ft_write_hooks.py</files>
  <read_first>
    - routes/app_routes.py (lines 360–395: the FT logging update block with FtCallSession query to confirm exact boundaries)
    - tests/test_ft_lifecycle.py (full file — confirm it is exclusively FT-specific before deleting)
    - tests/test_ft_models.py (full file — confirm it is exclusively FT-specific before deleting)
    - tests/test_ft_write_hooks.py (full file — confirm it references _FakeSession which is also used by test_per_sid_migration.py — NOTE THIS)
  </read_first>
  <action>
**routes/app_routes.py:**

Find and delete the block that begins with the comment:
```python
        # FT logging: update ft_call_sessions with aggregates (Phase 04.7.1)
        try:
            from database.models import FtCallSession
```

And ends with:
```python
        except Exception as _e:
            print(f"[FT] ft_call_sessions update failed: {_e}")
```

This block spans approximately lines 364–386. Also remove the `ls.state['ft_session_id'] = None` line inside the block and the `with ls.state_lock:` wrapper for `ft_session_id` if they belong exclusively to this block.

Per D-08: The surrounding code (POLISH-54 ObjectionEvent merge block above, and the Audit logging below starting with `log_action(...)`) must remain untouched.

**Delete test files via `git rm`:**
```bash
git rm tests/test_ft_lifecycle.py
git rm tests/test_ft_models.py
git rm tests/test_ft_write_hooks.py
```

IMPORTANT NOTE about test_ft_write_hooks.py: test_per_sid_migration.py line 171 imports `_FakeSession` from `tests.test_ft_write_hooks`. When test_ft_write_hooks.py is deleted, this import in test_per_sid_migration.py will break. This is handled in Task 2 — the entire `test_write_ft_event_isolation_per_sid` function (which contains that import) is also being deleted. Verify that `_FakeSession` is ONLY used inside the `test_write_ft_event_isolation_per_sid` function before proceeding.
  </action>
  <verify>
    <automated>grep -n "FtCallSession\|ft_call_sessions\|ft_session_id" routes/app_routes.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FtCallSession\|ft_call_sessions" routes/app_routes.py` returns 0 lines
    - `python -c "from routes.app_routes import app_routes_bp; print('ok')"` exits 0
    - `ls tests/test_ft_lifecycle.py tests/test_ft_models.py tests/test_ft_write_hooks.py` → all return "No such file"
    - The POLISH-54 ObjectionEvent merge block (lines above the deleted FT block) is intact — verify via `grep -n "POLISH-54" routes/app_routes.py` still shows the merge block
  </acceptance_criteria>
  <done>FtCallSession update block deleted from app_routes.py. 3 test files deleted from filesystem and git. Surrounding route code untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Surgical delete in test_per_sid_migration.py + fix test_ab_stats.py import</name>
  <files>tests/test_per_sid_migration.py, tests/test_ab_stats.py, tests/test_ft_seed.py</files>
  <read_first>
    - tests/test_per_sid_migration.py (lines 130–200: the test_write_ft_event_isolation_per_sid function to confirm exact start/end lines; also lines 1–30 for imports)
    - tests/test_ab_stats.py (lines 1–25: the import block containing FtCallSession to remove it)
    - tests/test_ft_seed.py (full file — check if it references FtCallSession/FtAssistantEvent and handle accordingly)
  </read_first>
  <action>
**tests/test_per_sid_migration.py — surgical delete:**

Delete ONLY the function `test_write_ft_event_isolation_per_sid` (approximately lines 134–197). This includes:
1. The function docstring
2. All code inside the function
3. The import line at the top of the function (`from database.models import FtAssistantEvent, Organisation, User, FtCallSession, PromptVersion`)

Also check the imports at the TOP of test_per_sid_migration.py (lines 1–30). If there is a top-level `from database.models import FtCallSession, FtAssistantEvent` import, remove it. If it is only inside the function body, it deletes with the function.

Leave untouched: all 5 other test functions in the file (the DSGVO-critical multi-user session isolation tests from Phase 08.20). These must remain green after the deletion.

Per D-11: chirurgisch bearbeiten — exactly one function removed.

**tests/test_ab_stats.py — remove unused import:**

At lines 14–22, the import block includes `FtCallSession`. Since FtCallSession is not used anywhere else in test_ab_stats.py (grep confirms only the import line), remove it from the import list:

Change:
```python
from database.models import (
    ConversationLog,
    EwbRating,
    FtCallSession,
    ObjectionEvent,
    Organisation,
    TrainingScenario,
    User,
)
```

To:
```python
from database.models import (
    ConversationLog,
    EwbRating,
    ObjectionEvent,
    Organisation,
    TrainingScenario,
    User,
)
```

**tests/test_ft_seed.py — assess and clean:**

Read the full file. If it references FtCallSession or FtAssistantEvent:
- If the entire file is FT-specific: delete with `git rm tests/test_ft_seed.py`
- If only specific tests reference FT models: surgically delete those test functions
- If the reference is only in an import that is not used in the remaining tests: remove the import line only

Apply the same principle as test_ab_stats.py (unused import → remove the import line).
  </action>
  <verify>
    <automated>grep -rn "FtCallSession\|FtAssistantEvent" tests/ | grep -v ".pyc" | grep -v "archive/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "FtCallSession\|FtAssistantEvent" tests/` (excluding .pyc and archive/) returns 0 lines
    - `python -m pytest tests/test_per_sid_migration.py -v` runs and all remaining tests PASS (the 5 DSGVO isolation tests are intact)
    - `python -m pytest tests/test_ab_stats.py -v` runs and all tests PASS (no import error)
    - `grep -n "test_write_ft_event_isolation_per_sid" tests/test_per_sid_migration.py` returns 0 lines
    - The other 5 functions in test_per_sid_migration.py still exist: `grep -c "^def test_" tests/test_per_sid_migration.py` returns at least 5
  </acceptance_criteria>
  <done>test_per_sid_migration.py has exactly the FT isolation test removed, other 5 tests intact and passing. test_ab_stats.py has FtCallSession import removed. test_ft_seed.py assessed and cleaned. Zero FtCallSession/FtAssistantEvent references in any test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_per_sid_migration.py → surgical edit | 5 DSGVO-critical tests must survive; only 1 function deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.A-04-01 | Tampering | Accidental deletion of DSGVO isolation tests in test_per_sid_migration.py | mitigate | Acceptance criterion runs pytest on the file and counts remaining test functions |
| T-08.23.2.A-04-02 | Denial of Service | _FakeSession import breakage in test_per_sid_migration.py after test_ft_write_hooks.py deleted | mitigate | _FakeSession import is inside the function being deleted — both go away together; acceptance criterion runs pytest |
</threat_model>

<verification>
After all tasks:
- `grep -rn "FtCallSession\|FtAssistantEvent" routes/ services/ scripts/ tests/` returns 0 lines (the REQ-4 acceptance criterion)
- `python -m pytest tests/test_per_sid_migration.py tests/test_ab_stats.py -v` exits 0
</verification>

<success_criteria>
routes/app_routes.py: FtCallSession block deleted. 3 test files deleted. test_per_sid_migration.py: only the FT isolation test removed, other 5 tests green. test_ab_stats.py: unused FtCallSession import removed. `grep -rn "FtCallSession\|FtAssistantEvent" routes/ services/ scripts/ tests/` returns 0.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.A-postgres-migration-schema-umbenennung/08.23.2.A-04-SUMMARY.md`
</output>
