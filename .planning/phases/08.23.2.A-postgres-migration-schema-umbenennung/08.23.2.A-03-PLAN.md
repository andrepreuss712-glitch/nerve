---
phase: 08.23.2.A
plan: "03"
type: execute
wave: 2
depends_on:
  - "08.23.2.A-01"
files_modified:
  - services/deepgram_service.py
  - services/claude_service.py
  - scripts/export_ft_jsonl.py
autonomous: true
requirements:
  - REQ-4

must_haves:
  truths:
    - "deepgram_service.py has no FtCallSession import or usage"
    - "claude_service.py has no FtAssistantEvent import or usage"
    - "scripts/export_ft_jsonl.py is deleted"
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "Deepgram service without FT logging block"
    - path: "services/claude_service.py"
      provides: "Claude service without FtAssistantEvent writer"
  key_links:
    - from: "services/deepgram_service.py"
      to: "FtCallSession removal"
      via: "lines 394-427 deleted entirely"
      pattern: "FT logging.*ft_call_sessions"
    - from: "services/claude_service.py"
      to: "FtAssistantEvent removal"
      via: "lines 142-229 (entire _write_ft_assistant_event function) deleted"
      pattern: "_write_ft_assistant_event"
---

<objective>
Delete FtCallSession writer block from deepgram_service.py, delete FtAssistantEvent writer (_write_ft_assistant_event) from claude_service.py, and delete scripts/export_ft_jsonl.py entirely.

Purpose: Removes all FT-logging dead code from services per CLAUDE.md Punkt 3 (kein toter Code — DELETE statt Stub). Code for new write paths comes in Phase 08.23.2.C/D.
Output: deepgram_service.py and claude_service.py free of FtCallSession/FtAssistantEvent references. export_ft_jsonl.py deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.A-postgres-migration-schema-umbenennung/08.23.2.A-CONTEXT.md
@.planning/phases/08.23.2.A-postgres-migration-schema-umbenennung/08.23.2.A-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete FtCallSession writer block from deepgram_service.py</name>
  <files>services/deepgram_service.py</files>
  <read_first>
    - services/deepgram_service.py (lines 385–435: the entire "FT logging: create ft_call_sessions row" try-block to confirm exact boundaries before deletion)
  </read_first>
  <action>
Read services/deepgram_service.py starting at approximately line 388. Find the block that begins with:

```python
        # FT logging: create ft_call_sessions row (Phase 04.7.1)
        try:
            from database.db import SessionLocal
            from database.models import FtCallSession, User
```

And ends at:

```python
        except Exception as _e:
            print(f"[FT] ft_call_sessions insert failed: {_e}")
```

Delete this entire try/except block — lines 394–427. The surrounding code (the lines before about `user_id = flask_session.get('user_id')` extracted for WR-04 safety, and the lines after starting `# ── Phase 08.19.4: Per-SID Profile + Session State Init`) must remain untouched.

Also delete the line `user_id = flask_session.get('user_id')` IF AND ONLY IF it is used exclusively by the FT logging block. If `user_id` is used elsewhere in the same function after the FT block, leave the assignment. Read the full function to check.

Per D-06: ersatzlos löschen. No stub, no comment "removed in 08.23.2.A".

Also check for any module-level `import` of `FtCallSession` at the top of the file and remove it if present (the imports inside the try-block are inline so they delete with the block, but check the top too).
  </action>
  <verify>
    <automated>grep -n "FtCallSession\|ft_call_sessions\|FT.*logging\|ft_session_id" services/deepgram_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FtCallSession\|ft_call_sessions" services/deepgram_service.py` returns 0 lines
    - `python -c "import services.deepgram_service; print('import ok')"` exits 0 (no ImportError)
    - The surrounding code (Phase 08.19.4 SID init block and audit logging) is intact
  </acceptance_criteria>
  <done>FT logging block for FtCallSession removed from deepgram_service.py. Service imports without error.</done>
</task>

<task type="auto">
  <name>Task 2: Delete _write_ft_assistant_event from claude_service.py and delete export_ft_jsonl.py</name>
  <files>services/claude_service.py, scripts/export_ft_jsonl.py</files>
  <read_first>
    - services/claude_service.py (lines 142–230: the _write_ft_assistant_event function and its FtAssistantEvent imports to confirm exact boundaries)
    - scripts/export_ft_jsonl.py (read the entire file to confirm it only contains FT export logic before deleting)
  </read_first>
  <action>
**claude_service.py:**

Find the function `_write_ft_assistant_event` which starts at line 142 with:
```python
def _write_ft_assistant_event(
```

And continues through the `FtAssistantEvent(...)` constructor call and `db.commit()` / `db.close()` / outer `except` block, ending at approximately line 229:
```python
        print(f"[FT] assistant_event write failed (module={module}): {e}")
```

Delete the entire function definition from line 142 through line 229 inclusive — that is, from `def _write_ft_assistant_event(` all the way through the closing `print` of the outer `except Exception as e` block.

Also search for and delete any call sites of `_write_ft_assistant_event(...)` within claude_service.py (grep for the function name). Each call site is a FT hook that must be removed per D-07.

Also remove any `from database.models import FtAssistantEvent` line at the top of the file or inside functions that are part of the deleted blocks.

Per D-07: ersatzlos löschen. No stub, no comment.

**scripts/export_ft_jsonl.py:**

Delete the entire file. Per D-09: the file reads FtCallSession + FtAssistantEvent data; the new call_events schema is JSONB-based and fundamentally different. A fresh export script comes in Phase 08.23.2.E. Git history is sufficient as reference.

In Python/shell: delete the file using the OS (on the server this will be via git rm or simple rm). In this plan context, use the Edit tool to overwrite with empty content is NOT acceptable — the file must be deleted, not emptied. Use Bash to `git rm scripts/export_ft_jsonl.py` (preferred — removes from git tracking too).
  </action>
  <verify>
    <automated>grep -rn "FtAssistantEvent\|_write_ft_assistant_event" services/claude_service.py &amp;&amp; echo "FAIL: references remain" || echo "PASS: clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FtAssistantEvent\|_write_ft_assistant_event\|ft_assistant_events" services/claude_service.py` returns 0 lines
    - `python -c "import services.claude_service; print('import ok')"` exits 0
    - `ls scripts/export_ft_jsonl.py` returns "No such file" (file deleted)
    - `grep -rn "FtCallSession\|FtAssistantEvent" routes/ services/ scripts/` after this plan returns only test files (those are cleaned in Plan 04)
  </acceptance_criteria>
  <done>_write_ft_assistant_event function (lines 142–229) and all its call sites deleted from claude_service.py. scripts/export_ft_jsonl.py deleted from filesystem and git. No FtAssistantEvent references remain in services/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deepgram_service.py → FT logging removal | Surrounding DSGVO audit logging must remain intact after block deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.A-03-01 | Tampering | Accidental deletion of surrounding DSGVO audit code in deepgram_service.py | mitigate | Read the file first; acceptance criterion verifies Phase 08.19.4 SID init block is intact via import check |
| T-08.23.2.A-03-02 | Denial of Service | Removing _write_ft_assistant_event call site but leaving caller reference | mitigate | grep for all call sites before deleting function; acceptance criterion greps entire file |
</threat_model>

<verification>
After all tasks:
- `grep -rn "FtCallSession\|FtAssistantEvent" services/` returns 0 lines
- `ls scripts/export_ft_jsonl.py` → "No such file or directory"
- `python -c "import services.deepgram_service, services.claude_service; print('ok')"` exits 0
</verification>

<success_criteria>
Services (deepgram_service.py, claude_service.py) have zero FtCallSession/FtAssistantEvent references. export_ft_jsonl.py is deleted. Both services import without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.A-postgres-migration-schema-umbenennung/08.23.2.A-03-SUMMARY.md`
</output>
