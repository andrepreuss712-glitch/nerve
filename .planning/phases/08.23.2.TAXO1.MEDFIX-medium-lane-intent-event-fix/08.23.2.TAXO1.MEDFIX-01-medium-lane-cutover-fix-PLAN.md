---
phase: 08.23.2.TAXO1.MEDFIX
plan: 01
type: execute
wave: 1
complexity: 🔴
depends_on: [08.23.2.TAXO1-04]
files_modified: [services/claude_service.py, services/live_session.py, tests/test_medium_lane_intent_event_live.py]
autonomous: false
requirements: [MEDFIX-1, MEDFIX-2, MEDFIX-3]
risiko: riskant
test_anruf: true
user_setup: []
must_haves:
  truths:
    - "analysiere_mit_claude liefert wieder ein strukturiertes JSON-ergebnis (einwand/intent_type/confidence/...) statt {}: der System-Prompt ist das JSON-Einwand-Schema SYSTEM_PROMPT_BASE (Welle 4 enthaelt dort bereits intent_type-Liste + confidence), NICHT der EWB-ANTWORT-Prompt build_ewb_prompt."
    - "Nach einem Live-Call mit erkanntem Einwand entstehen echte intent_event-Zeilen (Medium-Lane-Cutover feuert), weil ergebnis.get('einwand') wieder True werden kann."
    - "Die Per-SID-Profil-Anreicherung der Klassifikation bleibt erhalten ODER ist bewusst entfernt (Entscheidung dokumentiert): wenn Profil-Kontext fuer die Klassifikation gewuenscht ist, wird er an SYSTEM_PROMPT_BASE angehaengt — der EWB-Antwort-Prompt wird NICHT mehr als Klassifikations-System-Prompt verwendet."
    - "Bau-Regel 1 unveraendert: emit_intent_event bleibt insert-only; diese Phase aendert NUR die Klassifikations-Quelle (ergebnis), nicht den Writer."
    - "MEDFIX-2 (Modus): geklaert + belegt, ob das 'gatekeeper'-Schreiben in mode_initial ein echter Fehler ist ODER REQ-5-Absicht (contact_category-Default fuer Cold-Call-Start am Gatekeeper). Nur ein BELEGTER echter Mismatch wird gefixt; REQ-5-Absicht bleibt. emit_intent_event ist davon unabhaengig (liest _session_modes, nicht current_mode)."
    - "MEDFIX-3 (Netz-Ratsche, Punkt 20): ein server-seitig lauffaehiger Integration-Test beweist, dass aus dem LIVE-Dispatch (analyse_loop Medium-Lane-Block) heraus mit einem gemockten Haiku-Einwand-JSON eine intent_event-Zeile in der DB landet — KEIN reiner Funktions-Unit-Test von emit_intent_event isoliert."
    - "Reaktivierungs-Bewusstsein: der Fix weckt ergebnis-abhaengige Logik (kaufbereitschaft/Phase-Signale/FT-Events/Einwand-Buttons/lernkarte_match) wieder auf — das ist erwuenscht, wird aber im Test-Anruf bewusst gegen Regressionen geprueft (Latenz Punkt 25, kein Doppel-Cue)."
  artifacts:
    - path: "services/claude_service.py"
      provides: "analysiere_mit_claude: System-Prompt = SYSTEM_PROMPT_BASE (JSON-Schema) statt build_ewb_prompt; Caching-Logik erhalten; ergebnis wieder strukturiert"
      contains: "SYSTEM_PROMPT_BASE"
    - path: "services/live_session.py"
      provides: "MEDFIX-2: current_mode/contact_category nur bei BELEGTEM Mismatch aus echtem mode gesetzt (sonst REQ-5-Default unveraendert)"
      contains: "current_mode"
    - path: "tests/test_medium_lane_intent_event_live.py"
      provides: "Integration-Assertion: Live-Dispatch (Medium-Lane) mit gemocktem Haiku-JSON -> intent_event-Row in DB"
      contains: "def test_"
  key_links:
    - from: "analyse_loop Medium-Lane-Block (ergebnis.get('einwand'))"
      to: "intent_event (via emit_intent_event)"
      via: "ergebnis wieder strukturiert (SYSTEM_PROMPT_BASE)"
      pattern: "emit_intent_event"
    - from: "analysiere_mit_claude System-Prompt"
      to: "SYSTEM_PROMPT_BASE (JSON-Schema, claude_service.py:33-109)"
      via: "_system = SYSTEM_PROMPT_BASE (+ optional Profil-Kontext)"
      pattern: "SYSTEM_PROMPT_BASE"
    - from: "tests/test_medium_lane_intent_event_live.py"
      to: "intent_event-Row aus Live-Dispatch"
      via: "Integration-Assertion (Punkt 20)"
      pattern: "intent_event"
---

> RISIKO-BANNER: **riskant — Kernpfad-Reaktivierung.** Repariert den Welle-4-Cutover-Kerndefekt (intent_event bleibt leer), indem analysiere_mit_claude wieder strukturiertes JSON liefert. Weckt ergebnis-abhaengige Logik wieder auf (kaufbereitschaft/Phase/FT/Buttons) → **Test-Anruf PFLICHT** + Latenz-Watch (Punkt 25). Diagnose: `.planning/debug/taxo1-04-intent-event-empty-medium-lane.md`.

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Gemini-Review PFLICHT vor Execute (`/gsd-review --phase 08.23.2.TAXO1.MEDFIX --gemini --claude` → `--reviews`). Reaktiviert ruhende Kern-Subsysteme — heterogene Zweitsicht auf Blast-Radius zwingend.

> STOP-PUNKT: bauen + lokal AST/grep + voller `triage.sh tests/ -m "not live and not perf"` als Beweis (Orchestrator fuehrt ihn) + commit + push. KEIN `deploy.sh production` durch GSD — beaufsichtigter Deploy + Test-Anruf durch André/Claudian, der belegt: intent_event-Zeilen erscheinen + kaufbereitschaft/Phase/Buttons leben + Latenz im Budget.

<objective>
Den Welle-4-Cutover funktionsfaehig machen: analysiere_mit_claude liefert wieder ein strukturiertes Einwand-JSON, damit die Medium-Lane echte intent_event-Zeilen schreibt. Plus: den 'gatekeeper'-mode_initial-Befund klaeren (echter Fehler vs REQ-5-Absicht) und nur einen belegten Mismatch fixen. Plus: eine Integration-Assertion, die den Schreib-Pfad AUS DEM LIVE-DISPATCH beweist (schliesst die Punkt-20-Luecke, die diesen Bug durchliess).

Purpose: Wurzel-Fix des bestaetigten Defekts (Diagnose-Doc). analysiere_mit_claude nutzt faelschlich den EWB-ANTWORT-Prompt (build_ewb_prompt) als System-Prompt → Haiku liefert Prosa → _parse_json → {} → ergebnis.get('einwand') immer falsy → Medium-Lane-Emit feuert nie. SYSTEM_PROMPT_BASE (claude_service.py:33-109) ist das korrekte JSON-Schema (Welle 4 hat dort intent_type + confidence bereits ergaenzt) und wird aktuell NIRGENDS gelesen (grep-belegt: nur Definition).

Output: services/claude_service.py (System-Prompt-Swap), ggf. services/live_session.py (Modus-Fix nur bei belegtem Mismatch), tests/test_medium_lane_intent_event_live.py (Integration-Assertion). Deploy + Test-Anruf — GATED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/debug/taxo1-04-intent-event-empty-medium-lane.md

<interfaces>
<!-- analysiere_mit_claude (claude_service.py ~:510): user_msg = Kontext+Segment; _system_prompt = build_ewb_prompt(...) [FALSCH fuer Klassifikation] -> _system; claude_client.messages.create(system=_system); return _parse_json(msg.content[0].text). -->
<!-- SYSTEM_PROMPT_BASE (claude_service.py:33-109): JSON-Einwand-Schema; gibt {"einwand": false, ...} ODER {"einwand": true, "typ":..., "intent_type":..., "confidence":..., ...}. Aktuell UNGENUTZT (nur Definition). -->
<!-- _parse_json (claude_service.py:218): findet {..} und json.loads; bei Prosa/kein-JSON -> {}. -->
<!-- Medium-Lane-Emit-Block (analyse_loop, claude_service.py ~:1004): if ergebnis.get('einwand'): ... emit_intent_event(...). Unveraendert lassen — er funktioniert, sobald ergebnis strukturiert ist. -->
<!-- Modus: init_session_state (live_session.py:300) seedet current_mode/contact_category='gatekeeper' (Kommentar 'REQ-5'); deepgram start_live_session(:636 init, :437 _session_modes[sid]=mode, mode aus data:524-527). mode_initial liest current_mode (live_session.py:582). emit liest _session_modes (NICHT current_mode). -->
<!-- Caching: config.CACHE_ANALYSE-Pfad in analysiere_mit_claude erhalten (system als list mit cache_control wenn aktiv). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: analysiere_mit_claude — System-Prompt von build_ewb_prompt auf SYSTEM_PROMPT_BASE umstellen (Wurzel-Fix)</name>
  <read_first>
    - Diagnose-Doc (.planning/debug/taxo1-04-intent-event-empty-medium-lane.md) — Root + Belege
    - claude_service.py: analysiere_mit_claude VERBATIM (def bis return _parse_json) — 30 Zeilen drumherum (Punkt 14 Schicht 1+2)
    - claude_service.py:33-109 SYSTEM_PROMPT_BASE (das JSON-Schema; bestaetigen dass intent_type-Liste + confidence drin sind — Welle 4)
    - claude_service.py:218 _parse_json (warum {} bei Prosa)
    - ewb_pipeline.py build_ewb_prompt + _FALLBACK_V1_PROMPT (Beleg: Antwort-Prompt, kein JSON) — nur lesen, NICHT aendern
  </read_first>
  <action>
    Punkt-14-Edit in analysiere_mit_claude (bestehende Funktion). RE-GREP zuerst die echten Zeilen (Naht-Tor) — die Nummern driften.

    - Den System-Prompt der Klassifikation auf SYSTEM_PROMPT_BASE umstellen: `_system_prompt = SYSTEM_PROMPT_BASE` statt `build_ewb_prompt(...)`.
    - **Profil-Anreicherung (Claude's Discretion, dokumentieren):** Wenn die Klassifikation den per-SID-Profil-Kontext (Einwand-Liste etc.) braucht, haenge `build_profile_context(user_id=_user_id, sid=sid)` an SYSTEM_PROMPT_BASE an (z.B. `_system_prompt = SYSTEM_PROMPT_BASE + "\n--- AKTIVES VERKAUFSPROFIL ---\n" + context_block`). Der EWB-ANTWORT-Prompt (build_ewb_prompt / prompt_versions-Template) wird NICHT mehr als Klassifikations-System-Prompt verwendet. Begruendung in SUMMARY.
    - Die bestehende Caching-Logik (config.CACHE_ANALYSE) + Cost-Hook + `if not msg.content: return {}` + `return _parse_json(...)` UNVERAENDERT lassen.
    - Den per-SID user_id/anrede-Read (Welle-3-Putzliste) UNVERAENDERT lassen — nur die System-Prompt-Quelle aendern.
    - KEINE Aenderung am Medium-Lane-Emit-Block (~:1004) — der feuert von selbst, sobald ergebnis wieder einwand traegt.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('services/claude_service.py').read())"` lokal erlaubt. Funktional via Task 3 (Integration-Test, Orchestrator-triage) + Test-Anruf (Task 4).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SYSTEM_PROMPT_BASE" services/claude_service.py` zeigt eine NEUE Lese-Stelle in analysiere_mit_claude (nicht nur die Definition Z.33).
    - In analysiere_mit_claude wird build_ewb_prompt NICHT mehr als Klassifikations-System-Prompt genutzt (grep zeigt: kein `_system_prompt = build_ewb_prompt` in dieser Funktion mehr — ODER bewusst nur als angehaengter Kontext, dokumentiert).
    - emit_intent_event-Aufruf + Bau-Regel-1 unveraendert: `grep -c "UPDATE.*intent_event" services/intent_event_writer.py` == 0 (Writer nicht angefasst).
    - AST-Parse exit 0.
  </acceptance_criteria>
  <done>analysiere_mit_claude liefert wieder strukturiertes JSON (Klassifikations-Schema), Medium-Lane kann emittieren.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modus-Befund klaeren + nur BELEGTEN Mismatch fixen (REQ-5-bewusst)</name>
  <read_first>
    - live_session.py:300 init_session_state (current_mode/contact_category='gatekeeper', Kommentar 'REQ-5')
    - live_session.py:579-603 mode_initial-Write (liest current_mode)
    - deepgram_service.py:524-527 (mode aus data), :437 (_session_modes[sid]=mode), :636 (init_session_state-Aufruf), :925-934 (manueller mode-Toggle: current_mode 'gatekeeper'|'cold_call')
    - SPEC/REQ-5-Kontext (warum gatekeeper-Default) — grep "REQ-5" + CONTEXT der Phase 08.23.2.C
  </read_first>
  <action>
    Punkt-22 (Verbindungs-Karte): ZUERST belegen, ob 'gatekeeper' in mode_initial ein echter Fehler ist oder REQ-5-Absicht.
    - Fakt aus Diagnose: emit_intent_event liest `_session_modes.get(sid)` (= echter mode, z.B. cold_call), NICHT current_mode → der Emit ist vom 'gatekeeper'-Wert NICHT betroffen.
    - Frage: SOLL current_mode bei einem cold_call-Start 'cold_call' oder 'gatekeeper' (Erstkontakt am Gatekeeper, REQ-5) sein? Beleg via REQ-5/Code/CONTEXT.
    - **Wenn REQ-5-Absicht (gatekeeper-Default gewollt):** KEINE Code-Aenderung — nur in SUMMARY dokumentieren, dass mode_initial='gatekeeper' bei Cold-Call-Start korrekt ist (Erstkontakt) und der Emit-mode davon unabhaengig stimmt. (Bevorzugter Ausgang, wenn belegt.)
    - **Nur wenn BELEGTER echter Mismatch** (z.B. ein meeting-Start schreibt faelschlich gatekeeper, ODER _session_modes und current_mode sollen nachweislich konsistent sein): minimaler Fix an der EINEN Stelle (z.B. nach init_session_state den current_mode aus dem echten mode setzen), Punkt 14, ohne REQ-5 zu brechen.
    Chirurgisch: kein Umbau der Modus-/contact_category-Logik.
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. AST-Parse lokal. Funktional via Test-Anruf (Task 4): mode_initial schreibt den korrekten Modus; emit_intent_event.mode korrekt.</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY enthaelt den belegten Befund (REQ-5-Absicht ODER echter Mismatch) mit grep/Code-Beleg.
    - Falls Fix: genau eine Stelle geaendert, REQ-5 nicht gebrochen, AST-Parse exit 0. Falls kein Fix: begruendet dokumentiert.
  </acceptance_criteria>
  <done>Modus-Befund belegt geklaert; nur ein echter Mismatch (falls vorhanden) chirurgisch gefixt.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integration-Assertion — Live-Dispatch schreibt intent_event (Netz-Ratsche, Punkt 20)</name>
  <read_first>
    - CLAUDE.md "Test-Qualitaets-Regel" (Integration-Assertion vs Source-Presence) + Test-Cleanup-Regel (committende Tests raeumen ihre Rows via cleanup_rows)
    - tests/test_intent_event_writer.py (Writer-Unit-Test — der bestehende, der NICHT den Live-Pfad beweist)
    - tests/conftest.py (cleanup_rows-Helfer, DSN-Setup, Baseline-Waechter)
    - analyse_loop Medium-Lane-Block (~:1004) — der zu beweisende Live-Pfad
  </read_first>
  <action>
    NEUE Datei tests/test_medium_lane_intent_event_live.py. Integration-Assertion (server-seitig gegen nerve_test, KEIN Source-Presence):
    - Treibe den Medium-Lane-Schreibpfad so, wie er live laeuft: gemocktes Haiku-ergebnis `{'einwand': True, 'intent_type': 'echter_einwand', 'confidence': 0.8}` (monkeypatch analysiere_mit_claude ODER der Haiku-Call) + ein per-SID-State mit aktivem sid, dann den Medium-Lane-Block / einen analyse_loop-Tick ausloesen.
    - ASSERT: nach dem Tick existiert >= 1 intent_event-Zeile in der DB fuer den sid (Query auf nerve_test), mit intent_type/mode/source gesetzt. (Function-Call + DB-Read-Assertion — beweist Emit AUS dem Dispatch, nicht emit_intent_event isoliert.)
    - Zusatz-Assert (optional, MEDFIX-1): analysiere_mit_claude mit gemocktem Haiku-JSON-Text gibt ein dict mit 'einwand' zurueck (nicht {}), wenn der System-Prompt das Schema ist.
    - Teardown: eigene Rows via cleanup_rows wegraeumen (Baseline-Sauberkeit, PGTEST-Regel). Sauberer Skip wenn DSN fehlt (kein False-Green).
    Marker: KEIN `live`/`perf`-Marker (laeuft im Default-Gate mit).
  </action>
  <verify>
    <automated>MISSING — kein Local-Dev. `python -c "import ast; ast.parse(open('tests/test_medium_lane_intent_event_live.py').read())"` lokal. Lauf server-seitig via Orchestrator (triage.sh).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "def test_" tests/test_medium_lane_intent_event_live.py` >= 1.
    - KEIN Source-Presence: `grep -E "getsource|hasattr|open\(.*\)\.read|\.count\('" tests/test_medium_lane_intent_event_live.py` == 0.
    - Test asserted eine DB-Row aus dem Dispatch (Integration), nicht nur emit_intent_event isoliert.
    - Teardown nutzt cleanup_rows. AST-Parse exit 0.
  </acceptance_criteria>
  <done>Integration-Test beweist: Live-Dispatch -> intent_event-Row (schliesst die Punkt-20-Luecke).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>analysiere_mit_claude liefert wieder JSON (Medium-Lane kann emittieren) + Modus-Befund geklaert + Integration-Assertion (Live-Dispatch -> intent_event). Bereit fuer Cross-AI + beaufsichtigten Deploy + Test-Anruf.</what-built>
  <how-to-verify>
    ⚠️ **GATE: Cross-AI (Gemini) PFLICHT + beaufsichtigter Deploy durch André/Claudian.** Reihenfolge:
    1. `/gsd-review --phase 08.23.2.TAXO1.MEDFIX --gemini --claude` → bei Findings `--reviews` → re-execute.
    2. Orchestrator-Beweis (vor Handoff): voller `triage.sh tests/ -m "not live and not perf"` GRUEN inkl. neuem test_medium_lane_intent_event_live.
    3. `git add -A && git commit && git push origin main` (Orchestrator; KEIN deploy.sh).
    4. André/Claudian: `bash deploy.sh production` (Gate inkl. POST-SUITE) + **Test-Anruf (beide Modi)** der BELEGT:
       (a) `inspect.sh count/sample intent_event` STEIGT mit dem Call, intent_type/mode/source/phase + taxonomy_version='v1' gesetzt, handling_score NULL;
       (b) Medium-Lane feuert bei echtem Einwand; gemeinsame interaction_id pro Moment;
       (c) **Reaktivierung lebt:** Kaufbereitschaft/Phase bewegen sich, Einwand-Buttons/Cues feuern wieder;
       (d) **Latenz im Budget (Punkt 25):** EWB-/Live-Antwort nicht spuerbar langsamer, kein Doppel-Cue;
       (e) mode_initial schreibt den korrekten Modus (bzw. REQ-5-gatekeeper-by-design bestaetigt).
  </how-to-verify>
  <resume-signal>"MEDFIX live+gruen: intent_event-Zeilen erscheinen, Kaufbereitschaft/Phase/Buttons leben, Latenz ok" oder beschreibe den Fehler</resume-signal>
</task>

</tasks>

## 1. Lokaler Kontext
Insert-Sites: (T1) analysiere_mit_claude in claude_service.py — der `_system_prompt = build_ewb_prompt(...)`-Block (~:536, RE-GREP). Control-Flow linear (Prompt-Bau → messages.create → Cost-Hook → return _parse_json); kein early-return zwischen Prompt-Bau und create. (T2) live_session.py init_session_state (:300) + mode_initial (:579) — nur lesen/minimal. (T3) neue Testdatei — kein bestehender Control-Flow.

## 2. Funktions-Skelett
analysiere_mit_claude: user_msg bauen → per-SID user_id/anrede → [AENDERUNG: _system_prompt = SYSTEM_PROMPT_BASE (+ optional Profil-Kontext)] → Caching-Branch → messages.create → Cost-Hook → `if not msg.content: return {}` → `return _parse_json(...)`. Nur die System-Prompt-Quelle aendert sich; alle Ausgangs-Pfade bleiben.

## 3. Cross-File-Awareness
- `grep -rn "SYSTEM_PROMPT_BASE" services/ routes/` → vor dem Fix nur Definition (Z.33), keine Leser → Reuse ist konfliktfrei.
- `grep -rn "build_ewb_prompt" services/` → bleibt fuer die ECHTEN EWB-Antwort-Pfade (streame_auto_variante/streame_manual_ewb_variante/_qa_pipeline_dispatch) unveraendert — NUR analysiere_mit_claude wird umgestellt.
- `grep -rn "analysiere_mit_claude" services/ routes/` → einziger Live-Aufrufer analyse_loop (claude:981); ergebnis fliesst in Medium-Lane-Emit + kaufbereitschaft/Phase/FT — Reaktivierung erwartet, im Test-Anruf pruefen.

## 4. Edge-Case-Analyse
- Haiku liefert "kein Einwand"-JSON `{"einwand": false, ...}` → ergebnis dict mit einwand=False → Medium-Lane emittiert NICHT (korrekt), kein {} mehr.
- Haiku-Output unparsebar → _parse_json → {} (bestehender Fallback, unveraendert) → kein Crash.
- Profil-Kontext leer (kein sid/Profil) → SYSTEM_PROMPT_BASE allein reicht fuer JSON-Schema (kein leerer/kaputter Prompt).
- **Latenz (Punkt 25):** SYSTEM_PROMPT_BASE ist kuerzer/fokussierter als der EWB-Antwort-Prompt → Klassifikation nicht langsamer; Caching-Pfad bleibt. Im Test-Anruf gegen Budget halten.
### Race-Condition-Audit
1. Gleichzeitiges Feuern: analyse_loop ist 1 Daemon, sequentiell pro SID — keine neue Nebenlaeufigkeit durch den Prompt-Swap. Der Medium-Lane-Emit (Welle 4) ist bereits per-SID-lock-diszipliniert.
2. Verbindungsabbruch: analysiere_mit_claude-API-Fehler → bestehende SID-Liveness-Checks in analyse_loop greifen (unveraendert).
3. Wiederverbinden: kein neuer per-SID-Zustand eingefuehrt; Reuse von SYSTEM_PROMPT_BASE ist zustandslos.
4. Tab-Hintergrund: server-seitig, nicht relevant.

## 5. Persistenz-Schicht-Verifikation
### Angefasste Tabellen
- `public.intent_event` (schreiben, INDIREKT) — diese Phase aendert NICHT den Writer; sie sorgt nur dafuer, dass der bestehende Medium-Lane-Emit (Welle 4) wieder erreicht wird. Schema = Welle 1/0016 (inspect.sh schema intent_event im Welle-4-SUMMARY belegt; bei Bedarf erneut ziehen).
### Cross-Layer-Konsistenz-Tabelle
| Datum | Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| ergebnis (einwand/intent_type/confidence) | analysiere_mit_claude → analyse_loop | RAM (Funktions-Rueckgabe) | ⬜ Task 3 Integration-Test + Test-Anruf |
| intent_event-Zeile | bestehender emit_intent_event (Welle 4) | DB intent_event | ⬜ Test-Anruf inspect.sh count/sample |
### Bei Diskrepanz (Writer wuerde mutieren): STOP — diese Phase fasst den Writer NICHT an.

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-MEDFIX-01 | D (Funktion tot) | Klassifikation liefert weiter {} (falscher Prompt) | mitigate | System-Prompt = SYSTEM_PROMPT_BASE; Integration-Test beweist Live-Row; Test-Anruf belegt count steigt. |
| T-MEDFIX-02 | T (Regression) | Reaktivierte ergebnis-Logik (kaufbereitschaft/Phase) verhaelt sich falsch | mitigate | Test-Anruf prueft Bewegung + kein Doppel-Cue; Cross-AI auf Blast-Radius. |
| T-MEDFIX-03 | D (Latenz, Punkt 25) | Klassifikation spuerbar langsamer | mitigate | SYSTEM_PROMPT_BASE fokussiert + Caching erhalten; Test-Anruf-Latenz-Check. |
</threat_model>

<verification>
- analysiere_mit_claude: System-Prompt = SYSTEM_PROMPT_BASE (JSON-Schema), build_ewb_prompt nicht mehr als Klassifikations-Prompt; ergebnis wieder strukturiert.
- Medium-Lane-Emit unveraendert, feuert wieder (ergebnis.get('einwand') kann True sein).
- Modus-Befund belegt geklaert (REQ-5 vs Mismatch); nur echter Mismatch gefixt.
- Integration-Assertion: Live-Dispatch → intent_event-Row (Punkt 20).
- Beweis: triage.sh full GRUEN (Orchestrator). Live: Test-Anruf belegt intent_event steigt + Reaktivierung lebt + Latenz ok.
</verification>

<success_criteria>
- intent_event-Zeilen entstehen nach einem Live-Call mit Einwand (Cutover-Kern funktioniert).
- analysiere_mit_claude liefert strukturiertes JSON statt {} (SYSTEM_PROMPT_BASE).
- Integration-Test beweist den Schreib-Pfad aus dem Live-Dispatch (Netz-Ratsche).
- Modus-Befund belegt geklaert; REQ-5 nicht gebrochen.
- Keine Bau-Regel-1-Verletzung (Writer unveraendert).
- Latenz im Budget (Punkt 25), keine Regression der reaktivierten Logik (Test-Anruf).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO1.MEDFIX-medium-lane-intent-event-fix/08.23.2.TAXO1.MEDFIX-01-SUMMARY.md` (inkl. VOR-EXECUTE-RE-GREP-Befunde, SYSTEM_PROMPT_BASE-Swap-Beleg + Profil-Kontext-Entscheidung, Modus-Befund belegt, Integration-Test-GRUEN via triage, Bau-Regel-1-grep, "PENDING SERVER-GREEN + TEST-ANRUF"-Sektion).
</output>
