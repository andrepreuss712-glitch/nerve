---
phase: 08.23.2.TAXO1.MEDFIX
plan: 01
status: built-local-green-PENDING-server-green+test-anruf
built: 2026-06-17
requirements: [MEDFIX-1, MEDFIX-2, MEDFIX-3]
files_modified: [services/claude_service.py, tests/test_medium_lane_intent_event_live.py]
commits: [366e5df, c9ae9d2]
---

# 08.23.2.TAXO1.MEDFIX-01 — SUMMARY

Wurzel-Fix des Welle-4-Cutover-Defekts (intent_event blieb leer). Gebaut + lokal
AST/grep-grün. **STOP-PUNKT eingehalten: KEIN deploy.sh.** Server-Triage-Beweis +
Test-Anruf stehen aus (beaufsichtigt durch André/Claudian).

## Was gebaut wurde

### Task 1 — System-Prompt-Swap (MEDFIX-1, Wurzel-Fix) ✅
`services/claude_service.py:556` — `analysiere_mit_claude` setzt jetzt
`_system_prompt = SYSTEM_PROMPT_BASE` (das JSON-Einwand-Schema, claude:33-109, von
Welle 4 um `intent_type`-Liste + `confidence` ergänzt) statt `build_ewb_prompt(...)`
(der EWB-ANTWORT-Prompt → Prosa → `_parse_json` → `{}`).

Kaskade des Bugs (Diagnose taxo1-04): falscher Prompt → Prosa → `{}` →
`ergebnis.get('einwand')` immer falsy → Medium-Lane `emit_intent_event` (claude:1038)
feuerte nie → intent_event = 0.

**Caching / Cost-Hook / `if not msg.content: return {}` / `return _parse_json(...)` /
per-SID `user_id`/`anrede`-Read — unverändert.** `build_ewb_prompt` /
`resolve_prompt_version` bleiben für die echten EWB-Antwort-Pfade unverändert.

**Profil-Kontext-Entscheidung (dokumentiert):** SYSTEM_PROMPT_BASE wird OHNE
angehängten per-SID-Profil-Kontext/anrede genutzt. Begründung: (1) SYSTEM_PROMPT_BASE
ist das vollständige Klassifikations-Schema; (2) per-SID-Profil-Einwände dienen der
ANTWORT-Generierung (separater Matcher-/`streame_auto_variante`-Pfad), nicht der
Klassifikation; (3) fokussierter Prompt hält die Klassifikations-Latenz niedrig
(Punkt 25 — SYSTEM_PROMPT_BASE ist kürzer als der EWB-Antwort-Prompt, Caching-Pfad
bleibt). Die gegenargument_1/2 aus dem Schema fließen nur in den Post-Call-Log +
intent_event (DB), nicht in eine Live-Anzeige (Task 1b) — Anrede-Feinschliff der
gegenargumente ist hier nachrangig.

### Task 1b — Doppel-Cue-Verifikation (Gemini-BLOCK #1) ✅ KEINE Code-Änderung
RE-GREP am Live-Code, Erwartung 0/0/0 — **bestätigt, sogar stärker**:

| Check | Befund |
|---|---|
| `sio.emit` in analyse_loop (claude:921-1320) | **0** Live-Emit. Alle `.emit(` liegen bei 660-884 (streame_auto_variante/Matcher, VOR der Loop) + 1522/1568 (qa_soft_hint/qa_slot1, NACH der Loop-Bande). |
| `/api/ergebnis`-Route in `routes/` + `app.py` + `templates/` | **0** (kein Endpoint; CLAUDE.md-Architektur-Doc ist veraltet). |
| Frontend-Poll von `ergebnis` in `static/` | **0** — das Wort `ergebnis` kommt in `static/` NICHT vor. `gegenargument_1`-Treffer sind alle `match`/`pe`/`r` (Keyword-Matcher / Profil / Socket-qa_slot), nicht `ls.state['ergebnis']`. |
| Reader von `state['ergebnis']` / `state['version']` (alle `.py`) | **0** — `ls.state['ergebnis']` ist WRITE-ONLY: geschrieben claude:1137/1139 (Erfolg) + 1370/1372 (Fehler), init live_session:866/868, von NICHTS gelesen. |

`ergebnis`-Treffer in `templates/` (`data.ergebnis`) = die unverwandte EUR/Buchhaltungs-
Funktion. **Entscheidung verriegelt:** `analysiere` KLASSIFIZIERT + reichert an
(intent_event/kaufbereitschaft/Phase/Post-Call-gegenargument_log); die Live-ANTWORT
gehört allein dem QA-Slot/Matcher-Pfad (Phase-06.3-Invariante). **Kein Doppel-Cue →
keine Dedup-Code-Änderung.** (Re-Anruf-Check bleibt Teil des Test-Anrufs: genau eine
Antwort pro Einwand.)

### Task 1c — Consumer-Audit + Error-Handling (Gemini #2/#3) ✅ KEINE Code-Änderung

**Consumer-Audit (alle ergebnis-Konsumenten in analyse_loop):**

| Zeile | Konsument | Zugriff |
|---|---|---|
| 997 | confidence-Parse | `.get('confidence')` + float-try/except→0.7 |
| 1004 | Emit-Gate | `.get('einwand')` |
| 1008 | intent_type | `.get('intent_type')` + is_valid_intent_type-Fallback `echter_einwand` |
| 1073/1074 | kaufbereitschaft | `.get('einwand')`, `.get('intensitaet')` |
| 1085 | gegenargument_log-Gate | `.get('einwand')` |
| 1093-1106 | Anonymisierung | `.get('einwand_zitat','')`, `.get('gegenargument_1','')`, `.get('gegenargument_2','')` |
| 1277-1311 | readiness/hint | `if ergebnis:` + durchgehend `.get(...)` |
| 1332/1339 | last_einwand_typ | `.get('einwand')` **UND** `.get('typ')` als Gate |
| 1333/1343 | `ergebnis['typ']` (direkt) | nur erreichbar HINTER `if ...get('typ'):` → **kein KeyError möglich** |

→ Alle Konsumenten schema-robust (`.get()`); der einzige Direkt-Index ist gegatet.
`einwand=True` ohne `intent_type` → Fallback `echter_einwand` (1009) → kein
inkonsistenter Zustand.

**Error-Handling (#3):** `analysiere_mit_claude` fällt sicher auf `{}`
(`if not msg.content: return {}` claude:585-587 + `_parse_json`→`{}` bei Prosa). Der
`messages.create`-Aufruf (in analysiere, claude:552) wird aus analyse_loop:981 INNERHALB
des `try:` (claude:976) aufgerufen; `except Exception` (claude:1365) → Fehler-Pfad setzt
ergebnis-Fehlerdict + version++, Loop läuft weiter → **kein Crash.** → Keine Lücke,
kein zusätzliches try/except nötig.

### Task 2 — Modus-Befund (MEDFIX-2) ✅ REQ-5-by-design, KEINE Code-Änderung (belegt)
- `create_call_for_sid` (live_session:528) schreibt `Call.call_mode=_db_mode` (:567) —
  den ECHTEN Session-Typ (cold_call/meeting_consented) — in die DB.
- Das `mode_initial`-CallEvent (:580-603) liest `current_mode`/`contact_category` —
  eine ANDERE Achse (Gatekeeper↔Ziel-Pitch, manuell via pip-mode-indicator), explizit
  kommentiert `Default gatekeeper (REQ-5)` (:357). Ein Cold-Call beginnt am Gatekeeper.
- `emit_intent_event` liest `_session_modes.get(sid)` (analyse_loop:993) — den echten
  mode — **nicht** `current_mode`. Der intent_event.mode ist also korrekt, unabhängig
  vom `gatekeeper`-Default.

→ `current_mode` (Gatekeeper-Achse) und `Call.call_mode` (Session-Typ) messen
verschiedene Dinge; kein belegter Mismatch. Der „Sekundär-Befund" der Diagnose war eine
Hypothese vor dieser Gegenprobe. **REQ-5-Absicht bleibt, kein Fix** (bevorzugter Ausgang
laut Plan-Task-2).

### Task 3 — Integration-Assertion (MEDFIX-3, Netz-Ratsche, Punkt 20) ✅
Neue Datei `tests/test_medium_lane_intent_event_live.py`, 2 Tests, kein live/perf-Marker:
1. `test_medium_lane_live_dispatch_writes_intent_event` — ein `analyse_loop`-Tick mit
   gemocktem Haiku-Einwand-JSON (`einwand:True, intent_type:echter_einwand, confidence:0.8`)
   + per-SID-State + `_session_modes='cold_call'` + one-shot-Trigger → ASSERT ≥1
   intent_event-Row für den sid in nerve_test (intent_type/mode/phase/source/interaction_id).
   `_qa_pipeline_dispatch` (NACH dem Emit) hermetisch gestubt. **Beweist den Emit AUS dem
   Live-Dispatch, nicht emit_intent_event isoliert.**
2. `test_analysiere_uses_classification_schema_returns_structured_dict` — gemockter
   Claude-Client → ASSERT analysiere liefert dict mit `einwand=True` UND der durchgereichte
   System-Prompt IST `SYSTEM_PROMPT_BASE` (Wurzel-Fix-Beweis zur Laufzeit, kein Netz/DB).

Skippt sauber ohne `TEST_DATABASE_URL` (kein False-Green); Teardown via `cleanup_rows`.

## Bau-Regel 1 (Writer unverändert)
`grep -c "UPDATE.*intent_event" services/intent_event_writer.py` == **0**. Emit bleibt
insert-only; diese Phase ändert nur die Klassifikations-Quelle (ergebnis), nicht den Writer.

## Lokaler Beweis (erlaubt: nur AST/grep, kein Local-Run)
- `ast.parse(claude_service.py)` → OK; `ast.parse(test...py)` → OK.
- `grep SYSTEM_PROMPT_BASE` → Definition :33 + NEUE Lese-Stelle :556 (`_system_prompt = SYSTEM_PROMPT_BASE`).
- kein echtes `_system_prompt = build_ewb_prompt` in analysiere (nur `# WAR:`-Kommentar :536); build_ewb_prompt weiter 5× (echte EWB-Pfade).
- `def test_` ×2; Source-Presence-Grep (`getsource|hasattr|open().read|.count('`) == 0.

## PENDING SERVER-GREEN + TEST-ANRUF (beaufsichtigt — STOP-PUNKT)
**Nicht durch GSD ausgeführt (HART: triage.sh läuft NUR server-seitig; kein deploy.sh durch GSD).**

1. (optional) Gemini-Re-Check auf die Nachschärfung (Task 1b/1c) — Plan war bereits
   execute-ready nach BLOCK→Nachschärfung.
2. **Server-Triage-Beweis:** `bash scripts/triage.sh tests/ -m "not live and not perf"`
   GRÜN inkl. `test_medium_lane_intent_event_live` — läuft server-seitig (178.104.82.166)
   gegen frisch provisioniertes nerve_test. Setzt voraus, dass dieser Stand auf dem Server
   liegt (Push + deploy.sh-Gate, dessen POST-SUITE die Suite server-seitig vor dem Restart
   läuft — = der Triage-äquivalente Beweis am neuen Code).
3. Commit (erledigt: 366e5df, c9ae9d2) + **`git push origin main`** (offen — Orchestrator).
4. **André/Claudian:** `bash deploy.sh production` (Gate inkl. POST-SUITE) + **Test-Anruf
   (beide Modi)**, der belegt:
   (a) `inspect.sh count/sample intent_event` STEIGT mit dem Call (intent_type/mode/source/
       phase + taxonomy_version='v1', handling_score NULL);
   (b) Medium-Lane feuert bei echtem Einwand; gemeinsame interaction_id pro Moment;
   (c) Reaktivierung lebt: Kaufbereitschaft/Phase bewegen sich, Einwand-Buttons/Cues feuern;
   (d) Latenz im Budget (Punkt 25), KEIN Doppel-Cue;
   (e) mode_initial schreibt den korrekten Modus (bzw. REQ-5-gatekeeper-by-design bestätigt).

Resume-Signal: „MEDFIX live+grün: intent_event-Zeilen erscheinen, Kaufbereitschaft/Phase/
Buttons leben, Latenz ok" — oder Fehlerbeschreibung.
