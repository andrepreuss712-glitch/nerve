---
# ⛔ VERSCHOBEN NACH STABIL-2 (André-Entscheidung 2026-07-23, nach Fable-Gegencheck)
#
# Dieser Plan wird in STABIL-1 NICHT gebaut. Zwei HOCH-Funde:
# (1) `consume_ended_session_by_call_id` existiert NICHT im Code (0 grep-Treffer;
#     app_routes.py:160-161 ruft sie nur hinter hasattr, das immer False ist).
#     must_have-Truth #2 ('loest auf, auch wenn Session-State weg') ist mit den
#     gelisteten Tasks UNERFUELLBAR -> die Snapshot-Lookup-Funktion muss erst
#     gebaut werden (das ist STABIL-2-Arbeit).
# (2) Dieser Plan WEICHT den STABIL-1-Not-Ausgang (Plan 02) AUF: sobald das
#     Frontend immer eine call_id postet, greift der Guard
#     `if _bs is None and not _posted_call_id` in genau den Faellen nicht mehr,
#     fuer die er gedacht ist (State-Verlust, Doppel-Beenden) -> Muell-Pfad laeuft
#     wieder (Sonnet-CRM ueber leeres Transkript, Muell-Row, doppelte Punkte).
#
# STABIL-2 MUSS beim Bau dieses Plans die Guard-Bedingung erweitern:
#   'call_id gepostet, aber Aufloesung fehlgeschlagen -> schmaler Pfad:
#    nur calls-Close via DB, KEIN CRM/INSERT/Punkte'.
# Plus: consume_ended_session_by_call_id (Snapshot-Lookup) implementieren.
#
# 01-03 haengen NICHT an diesem Plan (Separierbarkeit von Fable bestaetigt).
# Der Inhalt unten bleibt als Vorlage fuer STABIL-2 erhalten.
---

---
phase: 08.23.2.STABIL-1
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - services/deepgram_service.py
  - static/pip-launcher.js
  - tests/test_stabil1_call_id_roundtrip.py
autonomous: false
requirements: [ST1-CALLID]
complexity: "🟡"

must_haves:
  truths:
    - "Das Frontend kennt waehrend des Anrufs die laufende call_id und schickt sie im /api/beenden-Body mit."
    - "Die Stufe-1-Aufloesung in api_beenden (bisher toter Code) loest damit deterministisch auf — auch wenn der Session-State schon weg ist."
    - "Faellt der Emit oder die call_id aus, verhaelt sich der Beenden-Pfad exakt wie heute (Stufe 2, dann Guard aus Plan 02)."
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "call_started-Emit mit der durable call_id, direkt nach create_call_for_sid"
      contains: "call_started"
    - path: "static/pip-launcher.js"
      provides: "state.currentCallId aus dem Emit + call_id im /api/beenden-Body"
      contains: "currentCallId"
  key_links:
    - from: "deepgram_service call_started-Emit"
      to: "app_routes.py:147-161 Stufe-1-Aufloesung"
      via: "pip-launcher.js state.currentCallId -> POST-Body call_id"
      pattern: "currentCallId"
---

<objective>
**Fix (b), zweite Haelfte — der „empfohlen mit drin"-Teil des ROADMAP-Eintrags: `call_id` in den Beenden-Body.**

`api_beenden` hat eine dreistufige Aufloesung (`app_routes.py:141-178`). **Stufe 1** — exakte Aufloesung ueber eine geposteten `call_id` — ist heute **toter Code**, weil `static/pip-launcher.js:3106-3114` gar keine `call_id` sendet. Uebrig bleibt der `user_id`-Scan (Stufe 2), der nur bei lebender Session greift.

**Zusatz-Befund dieser Planung (RESEARCH §2.2):** das Frontend *kennt* die laufende `call_id` nicht. `state.lastCallId` wird erst **aus der Beenden-Antwort** gesetzt (`pip-launcher.js:3124`), und einen Server→Client-Emit der laufenden `call_id` gibt es nicht. Dieser Plan muss den Emit also erst anlegen — er ist damit etwas groesser als der ROADMAP-Satz vermuten laesst.

> **★ Dieser Plan ist vollstaendig separierbar.** Pläne 01/02/03 haengen nicht von ihm ab, und der Guard aus Plan 02 ist ohne ihn korrekt (er prueft dann nur „keine Sitzung"). Wenn du ihn streichen willst, streiche ihn — die drei Launch-Blocker-Fixes bleiben unberuehrt. `autonomous: false`, damit die Entscheidung bei dir liegt.

Purpose: die Session-Aufloesung wird deterministisch statt „neueste Session desselben Users", und toter Code stirbt oder lebt.
Output: `call_started`-Emit, `state.currentCallId`, `call_id` im Beenden-Body, Round-Trip-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.STABIL-1-anruf-stabilitaet-zeitlimit-notausgang-kapazitaet/08.23.2.STABIL-1-RESEARCH.md
@CLAUDE.md
@services/deepgram_service.py
@static/pip-launcher.js

<interfaces>
**Server-Anker (`services/deepgram_service.py`, in `handle_start_live_session`):**
```python
_cid_f = ls.create_call_for_sid(_sid, user_id=user_id, call_mode=_call_mode_f)   # :775
...
with ls._session_state_lock:                                                      # :786
    _init_st_f = ls._session_state.get(_sid, {}).get('state', {})
    _init_cat_f = _init_st_f.get('contact_category', 'gatekeeper')
    _init_mode_f = _init_st_f.get('current_mode', 'gatekeeper')
sio.emit('contact_category_update',                                               # :791
         {'category': _init_cat_f, 'mode': _init_mode_f, 'source': 'session_init'},
         room=_sid)
```
Dieser Block laeuft **nach** `create_call_for_sid` und **vor** dem Oeffnen der Deepgram-Verbindung (Kommentar `:797-800`, CI-2-Race-Close). Die durable `call_id` steht hier bereits im per-SID-State — der ideale, bereits lock-tragende Emit-Ort. **Reconnect-Fall beachten:** bei Reconnect wird `create_call_for_sid` uebersprungen (`:779-781`), die `call_id` steht aber trotzdem im State → der Emit muss sie aus dem State lesen, **nicht** aus `_cid_f`.

**Frontend-Anker (`static/pip-launcher.js`):**
- Socket-Handler-Block: bei den uebrigen `state.socket.on(...)`-Registrierungen (Muster `outcome_ready`, `:2559`).
- Zuruecksetzen: `_resetLiveState()` `:2973-2978` nullt bereits `state.pendingPostcall` und `state.lastCallId`.
- Beenden-Body: `:3110-3114`, heute `{session_mode, precall_briefing, precall_fields}`.

**Konsument auf Serverseite (unveraendert, wird nur endlich erreichbar):** `app_routes.py:147-161` — Stufe 1 sucht die `call_id` erst in `ls._session_state`, dann via `consume_ended_session_by_call_id` im Snapshot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: call_started-Emit mit der durable call_id</name>
  <files>services/deepgram_service.py</files>
  <read_first>
    - services/deepgram_service.py:760-800 (create_call_for_sid, Reconnect-Fast-Path, contact_category_update-Emit, CI-2-Race-Close-Kommentar)
    - services/live_session.py — `create_call_for_sid` (Rueckgabewert + wo die call_id im per-SID-State landet)
  </read_first>
  <action>
    Im bestehenden `with ls._session_state_lock:`-Block (`:786-789`) die `call_id` **mitlesen** und direkt nach dem `contact_category_update`-Emit ein zweites Emit senden:

    ```python
    _init_cid_f = _init_st_f.get('call_id')   # STABIL-1 (b): auch im Reconnect-Fall gesetzt
    ...
    # ── Phase 08.23.2.STABIL-1 (b): laufende call_id ans Frontend ────────────
    # Ohne dieses Emit kennt der Client die call_id des LAUFENDEN Anrufs nicht
    # (state.lastCallId wird erst aus der Beenden-Antwort gesetzt) -> die
    # Stufe-1-Aufloesung in api_beenden (:147-161) war toter Code.
    # Aus dem STATE gelesen, nicht aus _cid_f: im Reconnect-Fast-Path (:779)
    # wird create_call_for_sid uebersprungen, die call_id steht aber im State.
    if _init_cid_f:
        sio.emit('call_started', {'call_id': str(_init_cid_f)}, room=_sid)
    ```

    Harte Regeln:
    1. **Lock-Umfang nicht erweitern.** Die `call_id` wird im bereits bestehenden `with`-Block gelesen; das `sio.emit` steht **ausserhalb** des Locks (wie das vorhandene `contact_category_update`-Emit).
    2. **`room=_sid`** — nie ein Broadcast. Die `call_id` gehoert genau einer Sitzung.
    3. **Fail-soft:** ist `_init_cid_f` leer (DB-Fehler in `create_call_for_sid`, `:776-778`), wird **nichts** gesendet; der Client bleibt bei `null` und der Beenden-Pfad faellt auf Stufe 2 / den Guard zurueck. Kein `raise`, kein zusaetzlicher try/except (der umschliessende `except _pe` `:793` deckt den Block bereits ab).
    4. **Reihenfolge:** vor dem Oeffnen der Deepgram-Verbindung (CI-2-Race-Close) — also innerhalb des bestehenden Blocks bleiben, nicht nach unten verschieben.
    5. **Kein neuer Modul-Global** (Punkt 28) — alles bleibt per-sid.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "import ast;ast.parse(open('services/deepgram_service.py',encoding='utf-8').read());print('AST ok')"</automated>
  </verify>
  <done>`grep -n "call_started" services/deepgram_service.py` trifft genau einmal; die Trefferzeile liegt zwischen dem `contact_category_update`-Emit und dem CI-2-Race-Close-Kommentar; `grep -n "_init_cid_f" services/deepgram_service.py` zeigt Lesen im Lock-Block, Verwenden ausserhalb.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — call_id merken und im Beenden-Body mitschicken</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js:2555-2575 (`outcome_ready`-Handler — Registrierungs-Muster)
    - static/pip-launcher.js:2973-2985 (`_resetLiveState` — was heute genullt wird)
    - static/pip-launcher.js:3095-3125 (Beenden-Fetch inkl. Body)
    - CLAUDE.md „Deutsche Umlaute & UTF-8" (JS-Variablen sind Code-Identifier -> ASCII)
  </read_first>
  <action>
    1. **Handler registrieren**, bei den uebrigen `state.socket.on(...)`-Blöcken:
    ```js
    // -- Phase 08.23.2.STABIL-1 (b): laufende call_id merken ------------------
    // Macht die Stufe-1-Aufloesung in api_beenden deterministisch (vorher tot).
    state.socket.on('call_started', function (d) {
      try {
        state.currentCallId = (d && d.call_id) ? String(d.call_id) : null;
      } catch (e) {
        console.error('[NerveLauncher] call_started Handler Fehler:', e);
      }
    });
    ```
    2. **In `_resetLiveState()`** (`:2977` direkt neben `state.lastCallId = null;`) ergaenzen:
    ```js
    state.currentCallId = null;   // STABIL-1 (b): darf nicht in den naechsten Call lecken
    ```
    3. **Beenden-Body** (`:3110-3114`) um genau ein Feld erweitern:
    ```js
    body: JSON.stringify({
      session_mode: state.mode || 'cold_call',
      call_id: state.currentCallId || null,   // STABIL-1 (b): Stufe-1-Aufloesung
      precall_briefing: state.precallBriefing,
      precall_fields: (state.precallBriefing && state.precallBriefing.fields) ? state.precallBriefing.fields : null
    })
    ```

    Harte Regeln:
    1. **Reihenfolge-Falle:** `_resetLiveState()` laeuft in `beenden()` **vor** dem `fetch` (`:3098` vs. `:3106`). Der Body wuerde also `null` senden, wenn er `state.currentCallId` erst danach liest. → **`state.currentCallId` VOR dem `_resetLiveState()`-Aufruf in eine lokale Variable sichern** (Muster `endCallGen`, `:3105`) und diese im Body verwenden. Ohne diesen Schritt ist der ganze Plan wirkungslos — das ist das zentrale Detail dieses Tasks.
    2. `null` senden, nicht das Feld weglassen — die Serverseite prueft `req_data.get('call_id')` truthy (`app_routes.py:146`), `null` verhaelt sich wie „nicht gesetzt".
    3. **Nur ASCII-Identifier** (`currentCallId`), Umlaute nur in User-sichtbarem Text — hier gibt es keinen.
    4. Sonst **nichts** am Beenden-Pfad aendern (kein `_showLadebalken1`, kein `.then`-Zweig).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && grep -c "currentCallId" static/pip-launcher.js</automated>
    <!-- Erwartung: >=4 Treffer (Handler-Write, Reset, lokale Sicherung, Body). -->
  </verify>
  <done>`grep -n "currentCallId" static/pip-launcher.js` zeigt: Handler-Zuweisung, Nullung in `_resetLiveState`, lokale Sicherung **vor** `_resetLiveState()` im Beenden-Pfad, Verwendung im `body`. Die Sicherungs-Zeile steht nachweislich vor dem `_resetLiveState()`-Aufruf.</done>
</task>

<task type="auto">
  <name>Task 3: Round-Trip-Test der Stufe-1-Aufloesung</name>
  <files>tests/test_stabil1_call_id_roundtrip.py</files>
  <read_first>
    - routes/app_routes.py:141-178 (dreistufige Aufloesung — Stufe 1 ist das Testziel)
    - tests/test_stabil1_beenden_guard.py (Plan 02 — Seed-/Cleanup-Helfer wiederverwenden)
    - tests/conftest.py:815 (`client`-Fixture)
  </read_first>
  <action>
    Neue Datei `tests/test_stabil1_call_id_roundtrip.py`. Serverseitiger Round-Trip — der Emit selbst wird nicht im Browser getestet, sondern sein Vertrag:

    - `test_stufe1_loest_ueber_geposteten_call_id_auf`: einen Session-State mit bekannter `call_id` anlegen (Muster aus `ls.init_session_state` + `state['call_id']`), dann `POST /api/beenden` mit **genau dieser** `call_id` **und** einer zweiten, juengeren Session desselben Users im State. **Assert:** beendet wird die Session zur geposteten `call_id`, nicht die juengere (das ist genau der Unterschied zu Stufe 2). LLM-Aufrufe mocken, erzeugte Rows aufraeumen.
    - `test_unbekannte_call_id_faellt_auf_stufe2`: `POST` mit einer `call_id`, die es nicht gibt, bei lebender Session. **Assert:** Stufe 2 greift, der Request laeuft normal durch (kein `no_session`).
    - `test_call_started_emit_traegt_die_state_call_id`: `sio.emit` in `services.deepgram_service` mocken, `handle_start_live_session` bzw. den Init-Pfad treiben, **assert:** ein Emit mit Event `'call_started'`, `room=<sid>` und `payload['call_id'] == <call_id aus dem State>`. Falls der Init-Pfad im Test nicht isoliert treibbar ist: den Emit-Block in einen kleinen Helfer ziehen **und diesen** testen — kein `inspect.getsource`, kein Datei-Lesen.

    Kein `live`/`perf`-Marker; committende Tests raeumen via `cleanup_rows`.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -m pytest tests/test_stabil1_call_id_roundtrip.py -q</automated>
    <!-- Vorab-Signal. VERBINDLICH = deploy.sh production + Test-Anruf. -->
  </verify>
  <done>Drei Tests gruen; der erste schlaegt nachweislich fehl, wenn die `call_id` nicht mehr im Body ankommt (er ist der Waechter fuer den Reihenfolge-Fallstrick aus Task 2).</done>
</task>

</tasks>

<control_flow_audit>
## Punkt 14 — Control-Flow

1. **Reihenfolge (Server):** Emit steht nach `create_call_for_sid` und vor dem Oeffnen der Deepgram-Verbindung. Frueher ginge nicht (keine `call_id`), spaeter waere es hinter dem Race-Close.
2. **Reihenfolge (Client) — der Fallstrick:** `_resetLiveState()` laeuft im Beenden-Pfad **vor** dem `fetch`. Wird `state.currentCallId` nicht vorher gesichert, sendet der Body immer `null`. Task 2 Regel 1 + Test 1 decken das ab.
3. **Zustand/Ressourcen:** kein neues Lock, kein neuer Thread, keine neue DB-Session. Der Emit laeuft im bestehenden `try/except _pe`-Block.
4. **Races:**
   - *Reconnect:* `create_call_for_sid` wird uebersprungen, `call_id` kommt aus dem State → Emit traegt trotzdem den richtigen Wert.
   - *Emit geht verloren (Socket-Abbruch):* Client behaelt `null` → Stufe 2, dann Guard. Kein neuer Fehlerfall.
   - *Zwei Tabs / zwei Sessions:* jeder Client bekommt nur sein `room=_sid`-Emit → keine Vermischung (Punkt 28 gewahrt).
   - *Stale `call_id` aus einem Vorgaenger-Call:* `_resetLiveState()` nullt sie; zusaetzlich prueft Stufe 1 gegen den lebenden State bzw. den Snapshot und faellt bei Nichttreffer auf Stufe 2 zurueck.
5. **Konsumenten:** `app_routes.py:147-161` unveraendert — es wird nur endlich erreicht. Der Guard aus Plan 02 (`_bs is None and not _posted_call_id`) laesst einen Request mit `call_id` bewusst durch; das ist die dokumentierte Absicht.
</control_flow_audit>

<persistenz_schicht>
## Punkt 21 / 26 / 23

**Kein Schema-Change.** Der Plan transportiert einen bereits persistierten Wert (`calls.id`) ueber einen neuen Kanal.

| Datum | Pfad | Persistenz-Schicht | Verifiziert |
|---|---|---|---|
| `call_id` (Emit-Payload) | `ls._session_state[sid]['state']['call_id']` | **RAM** — gespiegelt aus DB-Spalte `calls.id` (uuid, PK, `inspect.sh schema calls`, s. Plan 02) | ✓ |
| `state.currentCallId` | Browser | **RAM im Client** | ✓ |
| `req_data['call_id']` | HTTP-Body | keine Persistenz | ✓ |

**Punkt 26 (Async-Naht):** Der Emit liest die `call_id` **nach** `create_call_for_sid` — der Schreiber ist also nachweislich fertig, bevor der Leser laeuft (derselbe Thread, sequenziell). Kein Vorauslauf-Risiko, keine Fan-In-Naht noetig. Im Reconnect-Pfad ist der Wert bereits seit dem Erst-Start im State.

**Punkt 23 (Schild):** kein neuer Schreiber und kein neuer Leser einer Tabelle — `calls.id` wird nur weitergereicht. Keine `COMMENT`-Nachfuehrung.

**Punkt 25 (Latenz):** ein zusaetzliches Socket-Emit im Session-Start (nicht im 4-Sekunden-Tick, nicht im Antwort-Pfad). Spuerbare Antwort-Latenz: 0 ms.
</persistenz_schicht>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Server → Browser (`call_started`) | Die `call_id` wird dem Client offengelegt |
| Browser → `/api/beenden` (`call_id`) | Ein Client kann eine **beliebige** `call_id` posten |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-ST1-14 | Information Disclosure | `call_id` (uuid) im Client | accept | `room=_sid`-Emit, nur an die eigene Sitzung. Eine uuid ist kein Geheimnis und war ueber `/api/beenden`-Antworten (`:3124`) ohnehin schon im Client. |
| T-ST1-15 | Spoofing / Tampering | fremde `call_id` im POST-Body | mitigate | Stufe 1 sucht **ausschliesslich** im eigenen `_session_state`/Snapshot; ein fremder Wert findet nichts und faellt auf Stufe 2. Zusaetzlich haelt der gehaertete Fallback aus Plan 02 (`user_id` + Eindeutigkeit + Alter) die `calls`-Seite dicht. **Executor-Pflicht:** im SUMMARY explizit bestaetigen, dass Stufe 1 keinen DB-Lookup ueber die gepostete `call_id` ohne `user_id`-Bindung macht. |
| T-ST1-16 | Availability | Emit faellt aus | accept | Fail-soft: Client bleibt bei `null`, Verhalten exakt wie vor diesem Plan |
</threat_model>

<verification>
- `grep -n "call_started" services/deepgram_service.py static/pip-launcher.js` trifft auf beiden Seiten.
- Die lokale Sicherung von `state.currentCallId` steht **vor** dem `_resetLiveState()`-Aufruf im Beenden-Pfad.
- Drei Round-Trip-Tests gruen; Stufe 1 loest nachweislich exakt auf, auch wenn eine juengere Session desselben Users existiert.
- **Test-Anruf (Claudian/André):** in den Prod-Logs muss beim Beenden die Stufe-1-Zeile erscheinen statt des `user_id`-Scans.
- **Verbindliches Tor:** Pytest-Gate in `deploy.sh production`, danach der Test-Anruf.
</verification>

<success_criteria>
- Frontend sendet die laufende `call_id` im Beenden-Body; Stufe 1 ist kein toter Code mehr.
- Ausfall des Emits aendert nichts am bisherigen Verhalten (fail-soft).
- Kein Modul-Global, kein neues Lock, keine Latenz im Live-Pfad.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.STABIL-1-anruf-stabilitaet-zeitlimit-notausgang-kapazitaet/08.23.2.STABIL-1-04-SUMMARY.md` — inkl. der T-ST1-15-Bestaetigung (Stufe 1 ohne `user_id`-lose DB-Suche).
</output>
