---
phase: 08.23.2.STABIL-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/app_routes.py
  - tests/test_stabil1_beenden_guard.py
autonomous: true
requirements: [ST1-GUARD, ST1-FALLBACK]
complexity: "🔴"

must_haves:
  truths:
    - "Ein /api/beenden ohne Session-State UND ohne geposteten call_id gibt sofort 200 {ok:false, reason:'no_session'} zurueck."
    - "Im Guard-Pfad laeuft KEIN CRM-Call, KEIN ConversationLog-INSERT, KEINE Punkte-Vergabe, KEIN calls-UPDATE, KEIN audit_log-Write."
    - "Der DB-Fallback schliesst nie mehr einen fremden oder veralteten Call: er greift nur bei GENAU EINEM offenen Call innerhalb des Sitzungs-Zeitfensters."
    - "Der normale Beenden-Pfad mit lebender Sitzung ist verhaltensgleich zu vorher."
  artifacts:
    - path: "routes/app_routes.py"
      provides: "Session-los-Guard am Kopf von api_beenden (nach _bs-Aufloesung) + gehaerteter DB-Fallback"
      contains: "no_session"
    - path: "tests/test_stabil1_beenden_guard.py"
      provides: "Runtime-Tests: no-op ohne Sitzung, kein Write, Fallback-Eindeutigkeit + Alters-Schranke"
      contains: "def test_"
  key_links:
    - from: "api_beenden _bs-Aufloesung (app_routes.py:141-192)"
      to: "Guard-Return"
      via: "_bs is None AND kein _posted_call_id -> 200 no_session, VOR :305 CRM und VOR :374 INSERT"
      pattern: "reason.*no_session"
---

<objective>
**Fix (b) aus dem ROADMAP-Eintrag: Session-los-Guard + Absicherung des Fallbacks.**

Live-Anruf 2 (23.07. 14:27) hatte **keine Sitzung**, und das Frontend postet **keine `call_id`** (`static/pip-launcher.js:3106-3114`) — die Stufe-1-Aufloesung (`app_routes.py:147-161`) ist damit toter Code. Ergebnis: `_bs is None`, und die 700 Zeilen danach liefen trotzdem durch:

- Sonnet-CRM-Call ueber `"(kein Transkript)"` (`:305`) — bezahlt und blockierend,
- Muell-`ConversationLog`-INSERT mit lauter Nullen (`:374-412`),
- zwei `audit_log`-Zeilen (`:601-612`),
- Punkte + Fair-Use-Kontingent hochgezaehlt (`:614-647`),
- und der DB-Fallback (`:699-717`) griff **den letzten offenen Call des Users** — er haette Anruf 1 ueberschrieben.

Der Guard ist ein Code-Insert in eine 793-Zeilen-Funktion mit dreistufiger Aufloesung → **Punkt 14 in allen vier Schichten ist Pflicht** (Sektion unten).

Purpose: ein leerer Anruf fasst keine Pipeline und keinen fremden Datensatz mehr an.
Output: Guard + gehaerteter Fallback + Runtime-Tests, die beide Halbseiten beweisen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.STABIL-1-anruf-stabilitaet-zeitlimit-notausgang-kapazitaet/08.23.2.STABIL-1-RESEARCH.md
@CLAUDE.md
@routes/CLAUDE.md
@routes/app_routes.py

<interfaces>
<!-- Ist-Zustand der beiden Ziel-Stellen. Der Executor arbeitet gegen diese Vertraege. -->

**Aufloesungs-Vertrag `api_beenden` (`routes/app_routes.py:99-892`):**

| Stelle | Zeile | Inhalt |
|---|---|---|
| Signatur | `:99` | `def api_beenden():` — `@app_routes_bp.route('/api/beenden', methods=['POST'])` + `@login_required` |
| Body | `:100` | `req_data = request.get_json(silent=True) or {}` |
| Stufe 1 | `:147-161` | exakt via `_posted_call_id` (lebend + `consume_ended_session_by_call_id`) — **heute tot** |
| Stufe 2 | `:163-171` | `user_id`-Scan ueber `ls._session_state`, max `session_start_time` |
| Stufe 3 | — | `_beenden_sid = None` (D-02: kein Raten) |
| **Anker** | `:192` | `_bs = _load_beenden_state(_beenden_sid)` ← **hier hinter kommt der Guard** |
| erster Konsument | `:194+` | `precall_briefing`-Block liest `_bs.get('_briefing')` |
| CRM-Call | `:305` | `generate_crm_export(...)` |
| INSERT | `:374-412` | `ConversationLog(...)` + `db_conv.commit()` |
| audit_log | `:601-612` | `log_action(... 'session_start' / 'session_end')` |
| Punkte | `:614-647` | `total_points`, `live_calls_used`, `minuten_used`, Org-`live_minutes_used` |
| **Fallback** | `:699-717` | „letzter Call des Users mit `ended_at IS NULL`" ← **zweite Ziel-Stelle** |
| calls-UPDATE | `:719+` | `_call_row.ended_at`, `.conversation_log_id`, `.call_mode` |
| reset | `:884` | `reset_session(_beenden_sid)` |

**Ist-Code des Fallbacks (`:699-717`), verbatim gekuerzt:**
```python
if not _phase_d_call_id:
    _latest = (_db_fb.query(_CallModel_fb)
               .filter(_CallModel_fb.user_id == g.user.id,
                       _CallModel_fb.ended_at.is_(None))
               .order_by(_CallModel_fb.started_at.desc())
               .first())
    if _latest is not None:
        _phase_d_call_id = _latest.id
```
Der Kommentar `:697` („Pragmatisch: User hat genau einen aktiven Call gleichzeitig") ist genau die Annahme, die Anruf 2 gebrochen hat.

**Frontend-Konsument der Antwort (`static/pip-launcher.js:3126`):**
```js
if (!data.ok) { console.error('[NerveLauncher] Beenden error:', data.error); _hideLadebalken1(); _showPostcallEmpty(); return; }
```
→ `{ok:false}` fuehrt bereits heute zu „Ladebalken weg + leeres Postcall". Das ist fuer einen leeren Anruf das **richtige** Verhalten; das Frontend braucht **keine** Aenderung in diesem Plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session-los-Guard einfuegen (nach _bs-Aufloesung, vor jedem Konsumenten)</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py:99-200 (Signatur, req_data, dreistufige Aufloesung, `_load_beenden_state`, `_bs`)
    - routes/app_routes.py:300-320 (CRM-Call + sein except)
    - routes/app_routes.py:370-415 (ConversationLog-INSERT + commit)
    - routes/app_routes.py:875-892 (reset_session + Erfolgs-Response — Antwort-Form spiegeln)
    - routes/CLAUDE.md (Blueprint-/url_for-Regeln)
    - Sektion `<control_flow_audit>` in DIESEM Plan (die vier Schichten)
  </read_first>
  <action>
    **Einfuegen unmittelbar NACH `_bs = _load_beenden_state(_beenden_sid)` (`:192`) und VOR dem `precall_briefing`-Block (`:194`)** — exakt an dieser Stelle, weil dort die Aufloesung abgeschlossen und noch kein Konsument gelaufen ist:

    ```python
    # ── Phase 08.23.2.STABIL-1 (b): Session-los-Guard ─────────────────────────
    # Belegt aus Live-Anruf 2 (23.07. 14:27): ohne Sitzung UND ohne geposteten
    # call_id lief der komplette Beenden-Pfad auf leeren Daten weiter — Sonnet-
    # CRM-Call ueber "(kein Transkript)" (:305), Muell-ConversationLog (:374),
    # audit_log (:601), Punkte + Fair-Use (:614) — und der DB-Fallback (:699)
    # schloss den letzten offenen Call des Users, also potenziell einen FREMDEN.
    # Nichts davon ist ohne Sitzung sinnvoll -> hier raus, VOR jedem Konsumenten.
    # reset_session wird bewusst NICHT gerufen: _beenden_sid ist None, es gibt
    # nichts zurueckzusetzen (ein Aufruf mit None traefe eine fremde/keine sid).
    if _bs is None and not _posted_call_id:
        print(f"[Beenden] Kein Session-State und keine call_id "
              f"(user_id={g.user.id}) — no-op: kein CRM-Call, kein INSERT.")
        return jsonify({'ok': False, 'reason': 'no_session'}), 200
    ```

    Harte Regeln:
    1. **Bedingung exakt so.** `_bs is None` UND `not _posted_call_id`. Ein geposteter `call_id` ohne lebende Session ist **kein** Guard-Fall (Plan 04 belebt Stufe 1) — dann laeuft der normale Pfad weiter.
    2. **Status 200, nicht 4xx.** Das Frontend behandelt `!data.ok` bereits korrekt (`pip-launcher.js:3126`); ein 4xx wuerde dort in den `.then`-Zweig laufen und nichts verbessern. ROADMAP-Vorgabe.
    3. **Kein `reset_session`, kein `db.close()`, kein `flask_session`-Write** im Guard-Pfad — bis `:192` wurde ausser den zwei kurzlebigen, bereits geschlossenen `db2`-Sessions (`:128-134`) nichts geoeffnet.
    4. **Nichts oberhalb von `:192` veraendern.** Die dreistufige Aufloesung bleibt Byte-fuer-Byte gleich.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "import ast;ast.parse(open('routes/app_routes.py',encoding='utf-8').read());print('AST ok')"</automated>
  </verify>
  <done>`grep -n "no_session" routes/app_routes.py` trifft genau einmal, und die Zeilennummer liegt zwischen der `_bs = _load_beenden_state`-Zeile und der ersten `precall_briefing`-Zuweisung danach.</done>
</task>

<task type="auto">
  <name>Task 2: DB-Fallback haerten — nur ein EINDEUTIGER, frischer offener Call</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py:689-720 (der Fallback-Block im Ist-Zustand)
    - config.py:33 (`MAX_SESSION_HOURS`, Default 8)
    - Sektion `<persistenz_schicht>` in DIESEM Plan (inspect.sh-Beleg: `calls.started_at` ist `timestamp with time zone`)
  </read_first>
  <action>
    Den Fallback-Query aus `:699-717` ersetzen. Zwei Schranken statt „nimm den neuesten":

    1. **Alters-Schranke:** nur Calls mit `started_at >= now(utc) - MAX_SESSION_HOURS`. Ein Zombie-Call von gestern ist kein Kandidat.
    2. **Eindeutigkeits-Schranke:** `.limit(2).all()` statt `.first()`. Bei **genau einem** Treffer wird er genommen; bei **zwei oder mehr** wird **nicht geraten** (D-02) — Warn-Log, `_phase_d_call_id` bleibt leer, der calls-UPDATE-Block (`:719+`) laeuft dann einfach nicht.

    ```python
    if not _phase_d_call_id:
        from datetime import datetime as _dt_fb, timezone as _tz_fb, timedelta as _td_fb
        from database.models import Call as _CallModel_fb
        import config as _cfg_fb
        # STABIL-1 (b): frueher `.first()` auf "letzter offener Call des Users" —
        # das schloss bei einem sitzungslosen /api/beenden einen FREMDEN Anruf
        # (Live-Anruf 2, 23.07.). Jetzt zwei Schranken: nur innerhalb des
        # Sitzungs-Zeitfensters, und nur wenn der Kandidat EINDEUTIG ist.
        _cutoff_fb = _dt_fb.now(_tz_fb.utc) - _td_fb(hours=_cfg_fb.MAX_SESSION_HOURS)
        _db_fb = get_session()
        try:
            _open_fb = (_db_fb.query(_CallModel_fb)
                        .filter(_CallModel_fb.user_id == g.user.id,
                                _CallModel_fb.ended_at.is_(None),
                                _CallModel_fb.started_at >= _cutoff_fb)
                        .order_by(_CallModel_fb.started_at.desc())
                        .limit(2).all())
            if len(_open_fb) == 1:
                _phase_d_call_id = _open_fb[0].id
                print(f'[Phase08.23.2.D] Fallback-Lookup via DB: call_id={_phase_d_call_id} '
                      f'(eindeutig, user_id={g.user.id})')
            elif len(_open_fb) > 1:
                print(f'[Beenden] WARN: {len(_open_fb)}+ offene Calls fuer user_id={g.user.id} '
                      f'— Fallback raet NICHT, calls-UPDATE wird uebersprungen.')
        except Exception as _e_fb:
            print(f'[Phase08.23.2.D] Fallback-Lookup Fehler: {_e_fb}')
        finally:
            _db_fb.close()
    ```

    Harte Regeln:
    1. **`started_at` ist `timestamp with time zone`** (inspect.sh-Beleg unten) → der Cutoff MUSS `tz`-aware sein (`_dt_fb.now(_tz_fb.utc)`). Kein naives `datetime.now()`.
    2. **`try/finally` mit `_db_fb.close()` bleibt** — Muster der Datei. Der `except` schluckt weiterhin, ist aber **read-only** auf der Session (kein Write, keine vergiftete TX) → die DB-Regel „nie stiller except ohne rollback" ist hier nicht verletzt; das im Kommentar NICHT behaupten, sondern einfach keinen Write hinzufuegen.
    3. **Der `if _phase_d_call_id:`-Block danach (`:719+`) bleibt unveraendert.** Er laeuft im Mehrdeutigkeits-Fall schlicht nicht.
    4. **Der Vorrang bleibt:** `_phase_d_call_id` aus dem frueh erfassten Session-State (`:693`-Kommentar) schlaegt den Fallback weiterhin — der Block feuert nur, wenn es leer ist.
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -c "import ast;ast.parse(open('routes/app_routes.py',encoding='utf-8').read());print('AST ok')"</automated>
  </verify>
  <done>`grep -n "limit(2)" routes/app_routes.py` trifft; `grep -n "_CallModel_fb.started_at >= _cutoff_fb" routes/app_routes.py` trifft; `grep -c "\.first()" routes/app_routes.py` ist um genau 1 kleiner als vor der Aenderung.</done>
</task>

<task type="auto">
  <name>Task 3: Runtime-Tests — kein Write ohne Sitzung, kein fremder Call</name>
  <files>tests/test_stabil1_beenden_guard.py</files>
  <read_first>
    - tests/test_api_beenden_calls_update.py:25-52 (`_make_test_call` / `_cleanup_call`-Muster)
    - tests/test_postcall_split.py:60-63 (`_auth_client` via `session_transaction`)
    - tests/conftest.py:815 (`client`-Fixture, real-PG) + die `cleanup_rows`-Regel in CLAUDE.md
    - routes/app_routes.py — Guard (Task 1) + Fallback (Task 2)
  </read_first>
  <action>
    Neue Datei `tests/test_stabil1_beenden_guard.py`. Ausschliesslich Runtime-Assertions gegen DB und Response (keine Source-Presence). Alle committenden Tests raeumen ihre Rows via `cleanup_rows` weg.

    **Guard-Halbseite:**
    - `test_beenden_ohne_session_ist_noop`: `client` authentifizieren, **keinen** Session-State anlegen, `POST /api/beenden` mit `{'session_mode':'cold_call'}`. **Assert:** Status 200, `data['ok'] is False`, `data['reason'] == 'no_session'`.
    - `test_guard_schreibt_nichts`: Vorher/Nachher-Zaehler ueber `ConversationLog` (fuer den Test-User), `users.total_points`, `users.live_calls_used`, `users.minuten_used`. **Assert:** alle vier identisch. Zusaetzlich `services.crm_service.generate_crm_export` mocken und `assert_not_called()`.
    - `test_guard_schliesst_keinen_offenen_call`: einen offenen Call des Test-Users anlegen (`ended_at=None`, `started_at=now-5min`), dann sitzungsloses `/api/beenden`. **Assert:** die Row hat danach immer noch `ended_at is None` und `conversation_log_id is None`. ← **das ist der Kern-Regressionstest fuer Anruf 1.**
    - `test_geposteter_call_id_umgeht_den_guard`: `POST /api/beenden` mit `{'call_id': <uuid eines offenen Calls>}` ohne lebende Session. **Assert:** Antwort ist **nicht** `reason == 'no_session'` (der normale Pfad laeuft). LLM-Aufrufe dabei mocken; erzeugte Rows aufraeumen.

    **Fallback-Halbseite** (direkt gegen den Endpoint, mit gemocktem LLM):
    - `test_fallback_nimmt_eindeutigen_frischen_call`: genau EIN offener Call (`started_at=now-5min`) + lebender Session-State. **Assert:** nach `/api/beenden` hat die Row `ended_at is not None`. (Positiv-Fall — verhindert, dass die Haertung zu scharf wird.)
    - `test_fallback_raet_nicht_bei_zwei_offenen_calls`: ZWEI offene Calls des Users. **Assert:** beide haben danach `ended_at is None`.
    - `test_fallback_ignoriert_veralteten_call`: ein offener Call mit `started_at = now - (MAX_SESSION_HOURS + 1) h`. **Assert:** `ended_at is None` danach.

    Hinweise fuer den Executor:
    - `calls.started_at` ist `timestamp with time zone` → Seeds mit `datetime.now(timezone.utc)` bauen (Muster `test_api_beenden_calls_update.py:34`).
    - `calls.call_mode` hat einen CHECK-Constraint (`cold_call` | `meeting_consented`) und `transcript_storage` (`none` | `ephemeral` | `consented_full`) — Seeds muessen gueltig sein.
    - Kein `live`/`perf`-Marker (muss im Gate `-m "not live and not perf"` laufen).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -m pytest tests/test_stabil1_beenden_guard.py -q</automated>
    <!-- Vorab-Signal; ohne TEST_DATABASE_URL skippt die client-Fixture. VERBINDLICH = deploy.sh production. -->
  </verify>
  <done>Sieben Tests gruen im Gate; keiner liest Quelltext als String; jeder committende Test raeumt seine Rows weg (Baseline-Waechter meldet nichts).</done>
</task>

</tasks>

<control_flow_audit>
## ★ Punkt 14 — Pre-Insert-Control-Flow-Audit, alle vier Schichten (PFLICHT)

Der Guard ist ein Code-Insert in `api_beenden` (`:99-892`, 793 Zeilen) mit dreistufiger Session-Aufloesung. Deshalb hier vollstaendig.

### Schicht 1 — Erreichbarkeit / Reihenfolge

| Frage | Antwort |
|---|---|
| Was steht **vor** dem Einfuege-Punkt? | Body-Parse (`:100`), `precall_*`-Normalisierung (`:104-123`), `profile_name`-Lookup (`:125-134`, eigene `db2`-Session, im `finally` geschlossen), dreistufige `_beenden_sid`-Aufloesung (`:141-178`), `_load_beenden_state`-Definition + Aufruf (`:184-192`). |
| Ist alles Noetige gesetzt? | Ja: `g.user` (`@login_required`), `req_data`, `_posted_call_id`, `_beenden_sid`, `_bs`. |
| Was wird **uebersprungen**? | Alles ab `:189` — `precall_briefing`, Statistik-Aufbereitung, CRM (`:305`), Log-Datei, `ConversationLog`-INSERT (`:374`), `objection_bridge`, `audit_log` (`:601`), Punkte + Fair-Use (`:614`), `run_postcall_engine`, Fallback + calls-UPDATE (`:697+`), `_audio_health_bg`-Thread (`:877-882`), `reset_session` (`:884`). |
| Ist ein frueherer Einfuege-Punkt moeglich? | Nein. Vor `:192` steht `_bs` noch nicht fest; ein Guard vor der Aufloesung wuerde lebende Sitzungen faelschlich abweisen. |
| Ist ein spaeterer Punkt zulaessig? | Nein. Ab `:189` beginnen die Konsumenten; der CRM-Call `:305` ist genau der Aufruf, der weg soll. |

### Schicht 2 — Zustand / Ressourcen

| Ressource | Zustand am Einfuege-Punkt | Guard-Pflicht |
|---|---|---|
| DB-Sessions | keine offen (`db2` `:128-134` ist im `finally` geschlossen) | nichts zu schliessen |
| Locks | keiner gehalten (`ls._session_state_lock` wird in `:150/:163` erworben und im `with` freigegeben) | kein Deadlock moeglich |
| Threads | `_audio_health_bg` startet erst `:877` | nichts zu joinen |
| Dateien | Log-Datei erst `:337` | nichts offen |
| `flask_session` | nur gelesen (`active_profile_id`) | kein Write rueckabzuwickeln |
| Session-State | `_beenden_sid is None` im Guard-Fall | **`reset_session` NICHT rufen** |

### Schicht 3 — Races / Nebenlaeufigkeit

| Race | Analyse |
|---|---|
| Sitzung stirbt **zwischen** Aufloesung (`:192`) und Guard | Nicht moeglich — der Guard steht unmittelbar dahinter; `_bs` ist bereits eine lokale Referenz (Peek, nicht destruktiv). |
| Sitzung entsteht parallel (zweiter Tab startet gerade) | Dann liefert Stufe 2 eine sid und der Guard feuert nicht — korrekt: es gibt etwas zu beenden. |
| Zwei gleichzeitige `/api/beenden` desselben Users | Der erste konsumiert/leert, der zweite trifft den Guard → **genau die gewuenschte Wirkung** (heute erzeugt der zweite eine Doppel-Row + schliesst einen fremden Call). |
| Fallback-Query vs. paralleler Call-Start | Die Eindeutigkeits-Schranke (Task 2) faengt es: zwei offene Calls → kein Raten. |
| Per-sid-Disziplin (Punkt 28) | Der Guard liest ausschliesslich Request-lokale Variablen (`_bs`, `_posted_call_id`, `g.user.id`). Kein Modul-Global, kein Cross-Tenant-Pfad. |

### Schicht 4 — Konsumenten der Antwort

| Konsument | Verhalten bei `{ok:false, reason:'no_session'}` |
|---|---|
| `static/pip-launcher.js:3126` | `!data.ok` → `console.error(... data.error)` (undefined, harmlos), `_hideLadebalken1()`, `_showPostcallEmpty()` → leeres Postcall. **Richtiges Verhalten fuer einen leeren Anruf.** |
| `pip-launcher.js:3124` | `state.lastConvId` / `state.lastCallId` bleiben auf ihren alten Werten — der `.then`-Zweig wird vor der Zuweisung verlassen. Kein Fremd-Bezug entsteht. |
| Folge-Endpunkte (`/api/postcall_outcome`, `/api/postcall_cards`) | Werden erst nach dem `data.ok`-Zweig aufgerufen → laufen nicht an. |
| Bestehende Tests | `tests/test_api_beenden_calls_update.py` testet die UPDATE-Logik, nicht den sitzungslosen Endpoint-Aufruf. **Executor-Pflicht:** die Datei einmal durchsehen; schlaegt ein Test wegen des Guards fehl, ist er ein Stale-Contract-Test und wird **retargetet**, nicht der Code zurueckgedreht (CLAUDE.md Punkt 18). |
</control_flow_audit>

<persistenz_schicht>
## Punkt 21 — Persistenz-Schicht-Verifikation

### Angefasste Tabellen

Der Plan aendert **kein Schema**. Er verhindert Writes und schraenkt einen Lookup ein.

| Tabelle | Zugriff heute (Session-los) | Nach dem Guard | Schicht |
|---|---|---|---|
| `public.conversation_logs` | INSERT + commit (`:411`) | **kein Write** | DB |
| `public.calls` | UPDATE eines fremden Calls (`:719+`) | **kein Write**; Fallback-Lookup nur read + eindeutig | DB |
| `public.users` | UPDATE Punkte/Kontingent (`:614-647`) | **kein Write** | DB |
| `public.organisations` | UPDATE `live_minutes_used` (`:628-640`) | **kein Write** | DB |
| `public.audit_log` | 2 INSERTs (`:601-612`) | **kein Write** | DB |
| `public.objection_events` | Dual-Write-Shim | **kein Write** | DB |
| `_bs` / `_session_state` | Peek | Peek (unveraendert) | **RAM** — keine DB-Spalte |

### inspect.sh-Beleg (Production, read-only, 2026-07-23)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
                                    Table "public.calls"
        Column         |            Type             | Nullable |    Default
-----------------------+-----------------------------+----------+---------------
 id                    | uuid                        | not null |
 user_id               | integer                     | not null |
 call_mode             | text                        | not null |
 started_at            | timestamp with time zone    |          |
 ended_at              | timestamp with time zone    |          |
 transcript_storage    | text                        |          |
 conversation_log_id   | integer                     |          |
 ...
Indexes:
    "calls_pkey" PRIMARY KEY, btree (id)
    "idx_calls_user_time" btree (user_id, started_at)
Check constraints:
    "ck_calls_call_mode" CHECK (call_mode = ANY (ARRAY['cold_call'::text, 'meeting_consented'::text]))
    "ck_calls_transcript_storage" CHECK (transcript_storage = ANY (ARRAY['none'::text, 'ephemeral'::text, 'consented_full'::text]))
Foreign-key constraints:
    "calls_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id)
    "fk_calls_conversation_log_id" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id)
```

**Drei Folgerungen fuer Task 2 + 3:**
1. `started_at` ist **`timestamp with time zone`** → der Cutoff MUSS `tz`-aware sein.
2. Der Index `idx_calls_user_time (user_id, started_at)` deckt den gehaerteten Query vollstaendig ab — **kein neuer Index noetig**.
3. Test-Seeds muessen `call_mode` und `transcript_storage` CHECK-konform setzen.

### Cross-Layer-Konsistenz

| Code-Variable | Pfad | Persistenz-Schicht | Verifiziert |
|---|---|---|---|
| `_bs` | `_load_beenden_state` | **RAM** (`ls._session_state` / Snapshot) — **keine DB-Spalte** | ✓ `live_session.py`, kein Tabellen-Bezug |
| `_posted_call_id` | `req_data.get('call_id')` | **HTTP-Body** — keine Persistenz | ✓ `app_routes.py:146` |
| `_phase_d_call_id` | frueh aus State ODER Fallback-Query | DB-Spalte `calls.id` (uuid) | ✓ inspect.sh oben |
| `_latest.ended_at` | Fallback-Filter | DB-Spalte `calls.ended_at` (tz-aware) | ✓ inspect.sh oben |

### Punkt 26 — Async-Bereitschafts-Naht

| Gelesene Quelle | Wer schreibt sie | Schreib-Zeitpunkt | Kann der Leser vorauslaufen? |
|---|---|---|---|
| `_bs` (RAM) | `init_session_state` bei Call-Start; Snapshot beim disconnect | live | Nein — im selben Request aufgeloest, unmittelbar davor |
| `req_data['call_id']` | Browser | mit dem Request | Nein |
| `calls.ended_at` | `create_call_for_sid` (Anlage, NULL) / `api_beenden` (Schliessung) | live | Nein — der Fallback liest nur, um zu schliessen; die Eindeutigkeits-Schranke faengt den mehrdeutigen Fall statt zu raten |

→ **Keine Fan-In-Naht noetig.** Der Guard liest ausschliesslich Request-lokale bzw. im selben Request aufgeloeste Daten.

### Punkt 23 — Schild-Aktualitaet

Kein neuer Schreiber, kein neuer Leser, kein geaenderter Mechanismus — nur eine zusaetzliche **Vorbedingung** an bestehenden Schreibern und eine engere **Filterbedingung** an einem bestehenden Lookup. Die `Schreibt <datei>; liest <datei>`-Listen der sechs Tabellen bleiben woertlich korrekt.
**Executor-Pflicht-Check (billig, ein Befehl):** `inspect.sh schilder calls` — wenn das Schild von `calls` den Fallback-Mechanismus explizit beschreibt („letzter offener Call"), Schild im **selben Commit** auf die neue Eindeutigkeits-Regel nachziehen (`models.py` `comment=` + `COMMENT`-Migration). Wenn nicht: nichts zu tun, im SUMMARY vermerken.
</persistenz_schicht>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser → `/api/beenden` | Authentifizierter, aber inhaltlich unvalidierter POST — kann ohne jede Sitzung kommen |
| Request → fremde `calls`-Row | Der Fallback waehlte bisher ueber `user_id` allein |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-ST1-05 | Tampering / Integrity | `calls`-Fallback `:699-717` | mitigate | Alters- + Eindeutigkeits-Schranke; bei Mehrdeutigkeit wird nicht geraten (D-02). Ein leerer Anruf kann keinen echten Anruf mehr schliessen. |
| T-ST1-06 | Denial of Service (Kosten) | Sonnet-CRM-Call auf leerem Transkript | mitigate | Guard returned vor `:305` — kein bezahlter Nutzlos-Call mehr |
| T-ST1-07 | Integrity (Abrechnung) | `users.minuten_used` / `organisations.live_minutes_used` | mitigate | Guard returned vor `:614` — kein Fair-Use-Kontingent auf einen Phantom-Anruf |
| T-ST1-08 | Repudiation | `audit_log`-Zeilen fuer Phantom-Sessions | mitigate | Guard returned vor `:601` — keine irrefuehrenden Audit-Eintraege |
| T-ST1-09 | Availability (Regression) | echter Anruf wird faelschlich abgewiesen | avoid | Guard verlangt **beides** (`_bs is None` UND kein `call_id`); Positiv-Test `test_fallback_nimmt_eindeutigen_frischen_call` sichert den Normalpfad |
</threat_model>

<verification>
- `POST /api/beenden` ohne Sitzung → 200 `{ok:false, reason:'no_session'}`; `ConversationLog`-Count, `users.total_points`, `users.live_calls_used`, `users.minuten_used` unveraendert; `generate_crm_export` nicht aufgerufen.
- Ein vorhandener offener Call bleibt nach dem sitzungslosen Aufruf offen (`ended_at is None`).
- Zwei offene Calls → Fallback schliesst keinen; ein eindeutiger frischer Call → wird geschlossen; ein Call aelter als `MAX_SESSION_HOURS` → wird nicht geschlossen.
- Normalpfad mit lebender Sitzung unveraendert (bestehende Tests gruen; Stale-Contract-Treffer werden retargetet, nicht der Code).
- **Verbindliches Tor:** Pytest-Gate in `deploy.sh production`, danach der Test-Anruf (Claudian/André).
</verification>

<success_criteria>
- Guard sitzt nachweislich zwischen `_bs`-Aufloesung und erstem Konsumenten.
- Im Guard-Pfad: 0 DB-Writes, 0 LLM-Aufrufe, 0 Thread-Starts, kein `reset_session`.
- Fallback greift nur bei genau einem offenen Call innerhalb `MAX_SESSION_HOURS`.
- Punkt 14 in allen vier Schichten dokumentiert (Sektion oben) — Pflicht laut ROADMAP.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.STABIL-1-anruf-stabilitaet-zeitlimit-notausgang-kapazitaet/08.23.2.STABIL-1-02-SUMMARY.md` — inkl. Ergebnis des `inspect.sh schilder calls`-Checks und der Liste ggf. retargeteter Stale-Contract-Tests.
</output>
