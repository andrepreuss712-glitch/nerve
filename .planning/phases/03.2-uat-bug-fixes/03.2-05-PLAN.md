---
phase: 03.2-uat-bug-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - app.py
  - routes/settings.py
  - templates/settings.html
  - routes/training.py
  - templates/training.html
  - templates/profile_wizard.html
autonomous: true
requirements:
  - UAT-06
  - UAT-10

must_haves:
  truths:
    - "User can set preferred language in /settings and it persists in DB"
    - "Training page pre-selects the saved language on load (not hardcoded 'de')"
    - "Profile wizard placeholder text is neutral (no personal names like 'André')"
  artifacts:
    - path: "database/models.py"
      provides: "User model with preferred_language column"
      contains: "preferred_language"
    - path: "routes/settings.py"
      provides: "POST /settings/language endpoint"
      contains: "settings_language"
    - path: "templates/settings.html"
      provides: "Language selector section in settings"
      contains: "preferred_language"
    - path: "routes/training.py"
      provides: "Training page route passing preferred_language to template"
      contains: "preferred_language"
    - path: "templates/training.html"
      provides: "Training page initializing language from server context"
      contains: "currentLanguage"
  key_links:
    - from: "routes/training.py"
      to: "templates/training.html"
      via: "preferred_language template variable"
      pattern: "preferred_language"
    - from: "routes/settings.py"
      to: "database/models.py"
      via: "user.preferred_language DB write"
      pattern: "preferred_language"
---

<objective>
Implement global language preference in /settings (UX-07) and fix profile wizard placeholder text (UX-11).

Purpose: Language preference should persist across sessions via DB, not reset to German every time. Profile wizard must use neutral examples.
Output: Settings language selector, DB column, training pre-selection, neutral wizard placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-uat-bug-fixes/03.2-RESEARCH.md
@database/models.py
@app.py
@routes/settings.py
@templates/settings.html
@routes/training.py
@templates/training.html
@templates/profile_wizard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preferred_language DB column, settings route, and settings UI</name>
  <files>database/models.py, app.py, routes/settings.py, templates/settings.html</files>
  <read_first>database/models.py, app.py, routes/settings.py, templates/settings.html</read_first>
  <action>
**UX-07 — Global language preference:**

1. **models.py:** Find the `User` class. Add the column alongside existing fields:
```python
preferred_language = Column(String(10), default='de')
```

2. **app.py:** Find the migration `for col, typedef in [...]` loop (approx lines 60-89). Add:
```python
('preferred_language', "VARCHAR(10) DEFAULT 'de'"),
```

3. **settings.py:** Add a new route for language preference:
```python
@settings_bp.route('/settings/language', methods=['POST'])
@login_required
def settings_language():
    data = request.get_json(silent=True) or {}
    lang = data.get('language', 'de')
    allowed = ['de', 'en', 'fr', 'es', 'it', 'pt', 'nl', 'pl', 'cs', 'tr']
    if lang not in allowed:
        lang = 'de'
    db = get_session()
    try:
        user = db.query(User).get(g.user.id)
        user.preferred_language = lang
        db.commit()
        return jsonify({'ok': True, 'language': lang})
    finally:
        db.close()
```

Check what languages are available in `TRAINING_LANGUAGES` from `services/training_service.py` to build the allowed list.

4. **settings.html:** Find the settings form sections. Add a "Sprache" section (place it after the profile section or before any existing sections):

```html
<div class="n-glass" style="padding:20px;margin-bottom:16px">
  <h3 style="font-size:15px;margin-bottom:12px;color:var(--page-text-color)">Sprache</h3>
  <p style="font-size:12px;color:var(--page-text-secondary);margin-bottom:10px">
    Bevorzugte Sprache für das Training
  </p>
  <select id="settingsLang" class="n-input" style="max-width:240px"
    onchange="saveLangPref(this.value)">
    <option value="de">Deutsch</option>
    <option value="en">English</option>
    <option value="fr">Français</option>
    <option value="es">Español</option>
    <option value="it">Italiano</option>
    <option value="pt">Português</option>
    <option value="nl">Nederlands</option>
    <option value="pl">Polski</option>
    <option value="cs">Čeština</option>
    <option value="tr">Türkçe</option>
  </select>
</div>

<script>
// Pre-select current preference
(function() {
  var current = {{ (g.user.preferred_language or 'de') | tojson }};
  var sel = document.getElementById('settingsLang');
  if (sel) sel.value = current;
})();
function saveLangPref(lang) {
  fetch('/settings/language', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({language: lang})
  });
}
</script>
```
  </action>
  <verify>
    <automated>grep -n "preferred_language\|settings_language\|settingsLang\|saveLangPref" database/models.py app.py routes/settings.py templates/settings.html | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `database/models.py` User class contains `preferred_language = Column(String(10), default='de')`
    - `app.py` migration list contains `('preferred_language', "VARCHAR(10) DEFAULT 'de'")`
    - `routes/settings.py` contains `settings_language` route with POST method
    - Route validates language against allowed list and saves to `user.preferred_language`
    - `templates/settings.html` contains `<select id="settingsLang">` with at least 10 language options
    - Select pre-selects current `g.user.preferred_language` via Jinja2
    - `saveLangPref()` JS function POSTs to `/settings/language`
  </acceptance_criteria>
  <done>Language preference saved in DB via settings page. Settings UI shows dropdown with current selection.</done>
</task>

<task type="auto">
  <name>Task 2: Wire training page to read saved language and fix wizard placeholders</name>
  <files>routes/training.py, templates/training.html, templates/profile_wizard.html</files>
  <read_first>routes/training.py, templates/training.html, templates/profile_wizard.html</read_first>
  <action>
**UX-07 — Training reads saved language:**

1. **training.py:** Find the `training_page()` route function. It renders the training template. Add `preferred_language` to the template context:
```python
preferred_language = getattr(g.user, 'preferred_language', 'de') or 'de'
# Add to render_template call:
return render_template('training.html', ..., preferred_language=preferred_language)
```

2. **training.html:** Find where `currentLanguage` is initialized (should be near top of script, approx line 430-440, currently hardcoded as `let currentLanguage = 'de';`). Replace with:
```javascript
let currentLanguage = {{ preferred_language | default('de') | tojson }};
let tUI = (TRAINING_LANGUAGES[currentLanguage] || TRAINING_LANGUAGES['de']).ui;
```

3. **training.html:** Find the language buttons (the `t-lang-btn` buttons). On page load, set the correct button as active. After the language button HTML, add:
```javascript
// Pre-activate saved language button
document.querySelectorAll('.t-lang-btn').forEach(function(btn) {
  btn.classList.toggle('active', btn.dataset.lang === currentLanguage);
});
```
Or call `selectLanguage(currentLanguage)` on DOMContentLoaded if the function handles the active class.

**UX-11 — Profile wizard placeholders:**

4. **profile_wizard.html:** Read the entire file. Search for any personal placeholder text like "André", "35", or any developer-specific example content. Replace with neutral examples:
- "André" -> "Ihr Name"
- "35" (if used as age example) -> "z.B. 30-55"
- Any company-specific examples -> "Ihr Unternehmen", "Ihr Produkt"

Per research, `profile_editor.html` is already clean (PROD-10 completed). Focus on `profile_wizard.html` only. If no personal placeholders are found, document that it's already clean.
  </action>
  <verify>
    <automated>grep -n "preferred_language\|currentLanguage.*tojson\|André" routes/training.py templates/training.html templates/profile_wizard.html | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `routes/training.py` `training_page()` passes `preferred_language` to template context
    - `templates/training.html` initializes `currentLanguage` from `{{ preferred_language | tojson }}` (not hardcoded 'de')
    - Correct language button is pre-activated on page load
    - `templates/profile_wizard.html` contains NO occurrences of "André" or other personal developer names
    - All placeholder text in wizard uses neutral examples ("Ihr Unternehmen", etc.)
  </acceptance_criteria>
  <done>Training page pre-selects saved language preference. Profile wizard uses neutral placeholder examples.</done>
</task>

</tasks>

<verification>
- Open /settings — "Sprache" section shows language dropdown
- Select "English", refresh /settings — "English" still selected
- Open /training — English language button pre-selected, English UI text shown
- Open /profiles wizard — all placeholders are neutral (no personal names)
</verification>

<success_criteria>
Language preference persists in DB via settings. Training page respects saved preference. Profile wizard has neutral placeholder text.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-uat-bug-fixes/03.2-05-SUMMARY.md`
</output>
