---
phase: 03.2-uat-bug-fixes
plan: 05
subsystem: ui
tags: [settings, language, training, profile-wizard, flask, jinja2]

# Dependency graph
requires:
  - phase: 03.1-frontend-redesign
    provides: settings.html, training.html page templates with nerve.css
provides:
  - User.preferred_language DB column persisted via POST /settings/language
  - Settings page language selector dropdown pre-selecting saved preference
  - Training page pre-selecting saved language button and initializing tUI correctly
  - Profile wizard confirmed already using neutral placeholder text
affects:
  - training
  - settings
  - any future feature that reads preferred_language

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "preferred_language persisted on User model, read via getattr(g.user, 'preferred_language', 'de') in routes"
    - "Settings language endpoint pattern: POST /settings/language with allowlist validation"

key-files:
  created: []
  modified:
    - database/models.py
    - app.py
    - routes/settings.py
    - templates/settings.html
    - routes/training.py
    - templates/training.html

key-decisions:
  - "preferred_language column defaulting to 'de' — existing users automatically get German as preference"
  - "Allowlist of 10 languages in settings_language route matches TRAINING_LANGUAGES keys in training_service.py"
  - "DOMContentLoaded calls selectLanguage() for non-default saved language to sync tUI and button states"
  - "profile_wizard.html already uses neutral placeholders — no personal names found, no changes required"

patterns-established:
  - "Language pre-selection in Jinja2 loop: {% if code == preferred_language %}active{% endif %}"
  - "Server-side language passed to JS: let currentLanguage = {{ preferred_language | default('de') | tojson }};"

requirements-completed: [UAT-06, UAT-10]

# Metrics
duration: 15min
completed: 2026-04-01
---

# Phase 03.2 Plan 05: Language Preference + Profile Wizard Placeholder Summary

**User.preferred_language DB column with POST /settings/language endpoint; training page pre-selects saved language via Jinja2 server value instead of hardcoded 'de'**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-01T15:10:00Z
- **Completed:** 2026-04-01T15:25:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Added `preferred_language = Column(String(10), default='de')` to User model with DB migration
- Implemented POST `/settings/language` route with 10-language allowlist validation, persisting to DB
- Settings page shows "Sprache" card with dropdown pre-selected from `g.user.preferred_language`
- Training page passes `preferred_language` from route, initializes `currentLanguage` from Jinja2 (not hardcoded)
- Language button active state uses `preferred_language` in Jinja2 template loop
- DOMContentLoaded handler calls `selectLanguage()` to sync `tUI` and button state for saved non-German languages
- Confirmed `profile_wizard.html` already uses neutral placeholders (no personal names found)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add preferred_language DB column, settings route, and settings UI** - `ce3e8b7` (feat)
2. **Task 2: Wire training page to read saved language and fix wizard placeholders** - `0313eb1` (feat)

**Plan metadata:** _(docs commit to follow)_

## Files Created/Modified

- `database/models.py` - Added `preferred_language = Column(String(10), default='de')` to User class
- `app.py` - Added migration entry `('preferred_language', "VARCHAR(10) DEFAULT 'de'")` for existing DBs
- `routes/settings.py` - Added `settings_language()` route: POST /settings/language with allowlist + DB write
- `templates/settings.html` - Added "Sprache" settings card with 10-option select and saveLangPref() JS
- `routes/training.py` - `training_page()` now reads `g.user.preferred_language` and passes to template
- `templates/training.html` - `currentLanguage` initialized from Jinja2, language buttons use `preferred_language`, DOMContentLoaded pre-activates saved language

## Decisions Made

- `preferred_language` defaults to `'de'` — backward-compatible, no migration required for existing users
- Allowlist in route matches the 10 languages available in TRAINING_LANGUAGES (training_service.py)
- DOMContentLoaded only calls `selectLanguage()` when `currentLanguage !== 'de'` — avoids redundant DOM operations for majority of users
- `profile_wizard.html` already clean — no personal developer names found, task confirmed complete without code changes

## Deviations from Plan

None — plan executed exactly as written. Profile wizard was already clean as anticipated.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- UAT-06 (language preference) and UAT-10 (wizard placeholders) resolved
- Language preference infrastructure in place for any future per-language features
- Ready to continue with remaining Phase 03.2 plans

---
*Phase: 03.2-uat-bug-fixes*
*Completed: 2026-04-01*
