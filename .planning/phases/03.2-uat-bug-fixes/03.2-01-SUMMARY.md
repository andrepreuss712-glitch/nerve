---
phase: 03.2-uat-bug-fixes
plan: 01
subsystem: ui
tags: [training, audioctx, tts, elevenlabs, ptt, getUserMedia, javascript]

# Dependency graph
requires:
  - phase: 03.1-frontend-redesign
    provides: training.html template with NERVE design system applied
provides:
  - AudioContext resume fix for training ring tone (BUG-01)
  - TTS autoplay error visibility in browser console (BUG-03)
  - PTT mic in-chat error feedback on getUserMedia failure (BUG-02)
  - VPS live mic limitation documented in app.js
affects: [training-flow, ptt-mic, tts-playback]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "AudioContext must be resumed after creation before scheduling audio on user gesture"
    - "TTS .play() catch should log to console.warn, not swallow silently"

key-files:
  created: []
  modified:
    - templates/training.html
    - static/app.js

key-decisions:
  - "startFreizeichen made async to allow await freizCtx.resume() — browser autoplay policy requires explicit resume after AudioContext creation"
  - "TTS autoplay errors surfaced to console.warn rather than silenced — silent catch hid critical failure mode"
  - "Live mic VPS limitation documented in app.js as comment — no code fix possible, PyAudio requires audio hardware"

patterns-established:
  - "AudioContext pattern: always check state === 'suspended' and resume() before scheduling audio nodes"

requirements-completed: [UAT-01, UAT-02]

# Metrics
duration: 25min
completed: 2026-04-01
---

# Phase 03.2 Plan 01: Training Flow Bug Fixes Summary

**AudioContext auto-suspend fix for ring tone (BUG-01), TTS autoplay error visibility (BUG-03), PTT mic in-chat error feedback (BUG-02) in training.html**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-01T16:45:00Z
- **Completed:** 2026-04-01T17:10:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- Fixed `startFreizeichen()` — made async and added `await freizCtx.resume()` before oscillator start, resolving browser AudioContext auto-suspend policy blocking ring tone on user gesture
- Fixed TTS `autoPlay()` — replaced silent `.catch(() => {})` with `console.warn('[TTS] Autoplay blocked:', e)` so autoplay policy blocks are visible during debugging
- Fixed PTT `startRec()` — added `console.log('[MIC] Requesting getUserMedia...')` for visibility, upgraded catch from status-bar-only to also call `addSysMsg()` for in-chat error message visible to user
- Documented VPS live mic limitation in `static/app.js` — server-side PyAudio cannot capture audio on headless VPS, no code fix possible, architecture note added for future WebRTC phase

## Task Commits

1. **Task 1: Fix Anruf-Button and TTS audio playback in training** - `58415c7` (fix)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `templates/training.html` - AudioContext resume fix in startFreizeichen, TTS catch improved, PTT mic debug log + in-chat error feedback
- `static/app.js` - Added architectural comment about server-side PyAudio VPS limitation

## Decisions Made
- `startFreizeichen` made `async` rather than using `.then()` chaining — cleaner and consistent with `startTraining` which is already `async` and calls `startFreizeichen()`
- TTS error surfaced to `console.warn` only (not UI alert) — autoplay blocks are a browser UX detail, not an app error. User can manually click the Abspielen button.
- PTT getUserMedia failure now shows both `showStatus()` (status bar) and `addSysMsg()` (chat message) — ensures user cannot miss the error regardless of where they are looking

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Git worktree metadata directories (`.git/worktrees/agent-a29000e9` and `agent-aa7efc14`) were stale from previous Claude agents and caused `git status` / `git diff` to fail with "not a git repository" errors. Removed the stale directories. `git add`, `git commit`, `git log`, `git ls-files -m` continued to work correctly throughout.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Training flow fixes applied. Manual verification still needed: visit getnerve.app/training over HTTPS, select profile+scenario, click Anrufen, verify ring tone plays, verify TTS audio plays or shows console.warn on block.
- Ready to proceed to Plan 02 (next UAT bug fix wave).

---
*Phase: 03.2-uat-bug-fixes*
*Completed: 2026-04-01*
