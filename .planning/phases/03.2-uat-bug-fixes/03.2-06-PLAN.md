---
phase: 03.2-uat-bug-fixes
plan: 06
type: execute
wave: 2
depends_on:
  - 03.2-01
  - 03.2-02
files_modified:
  - templates/app.html
  - templates/training.html
  - static/app.js
autonomous: true
requirements:
  - UAT-07
  - UAT-08
  - UAT-09

must_haves:
  truths:
    - "Animated teal progress bar shows between Beenden click and Auswertung data ready"
    - "Kompaktmodus opens as floating overlay in bottom-right, main content stays visible"
    - "Zurück zum Dashboard button visible in postcall actions"
  artifacts:
    - path: "templates/training.html"
      provides: "Loading overlay with animated progress bar for training Auswertung"
      contains: "t-loading-overlay"
    - path: "templates/app.html"
      provides: "Loading overlay for live Auswertung, compact overlay panel, Dashboard back button"
      contains: "postcall-loading"
    - path: "static/app.js"
      provides: "Kompakt overlay CSS changes, loading state management"
      contains: "kompakt-panel"
  key_links:
    - from: "templates/training.html"
      to: "t-loading-overlay"
      via: "show before fetch, hide on response"
      pattern: "t-loading-overlay"
    - from: "templates/app.html"
      to: "/dashboard"
      via: "Zurück zum Dashboard link in postcall-actions"
      pattern: "Dashboard"
---

<objective>
Implement UX improvements: animated loading state for Auswertung (UX-08), compact mode as floating overlay (UX-09), and Dashboard back button in postcall (UX-10).

Purpose: Loading state shows progress during analysis. Compact mode stays as overlay without hiding main content. Users can navigate back to dashboard after a session.
Output: Training + live loading overlays, floating compact panel, Dashboard button in postcall.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-uat-bug-fixes/03.2-RESEARCH.md
@.planning/phases/03.2-uat-bug-fixes/03.2-01-SUMMARY.md
@.planning/phases/03.2-uat-bug-fixes/03.2-02-SUMMARY.md
@templates/app.html
@templates/training.html
@static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add animated loading overlay to training and live Auswertung</name>
  <files>templates/training.html, templates/app.html, static/app.js</files>
  <read_first>templates/training.html, templates/app.html, static/app.js</read_first>
  <action>
**UX-08 — Animated loading state:**

1. **training.html — CSS:** Add these styles in the `<style>` block:
```css
.t-loading-overlay {
  position: fixed; inset: 0; z-index: 800;
  background: var(--page-bg);
  display: flex; flex-direction: column;
  align-items: center; justify-content: center; gap: 16px;
}
.t-loading-bar-wrap {
  width: 280px; height: 4px;
  background: var(--glass-bg);
  border-radius: 2px; overflow: hidden;
}
.t-loading-bar {
  height: 100%; width: 50%;
  background: #2dd4a8;
  border-radius: 2px;
  animation: t-indeterminate 1.4s ease infinite;
}
@keyframes t-indeterminate {
  0%   { transform: translateX(-100%); }
  100% { transform: translateX(560%); }
}
.t-loading-text {
  color: var(--page-text-secondary);
  font-size: 13px;
}
```

2. **training.html — HTML:** Add loading overlay element (before closing `</body>` or inside the main content area):
```html
<div id="t-loading-overlay" class="t-loading-overlay" style="display:none">
  <div class="n-glass" style="padding:32px;text-align:center;border-radius:12px">
    <h3 style="color:var(--page-text-color);margin-bottom:16px">Gespräch wird ausgewertet...</h3>
    <div class="t-loading-bar-wrap"><div class="t-loading-bar"></div></div>
    <p class="t-loading-text" id="t-loading-subtext">Einwände analysieren...</p>
  </div>
</div>
```

3. **training.html — JS:** In `endTraining()` (or `confirmEndTraining()` if modals from Plan 7 are present), BEFORE the fetch call to `/training/end`, show the overlay and start cycling text:
```javascript
// Show loading overlay
document.getElementById('t-loading-overlay').style.display = 'flex';
var loadTexts = ['Einwände analysieren...', 'Coaching-Punkte berechnen...', 'Zusammenfassung erstellen...'];
var loadIdx = 0;
var loadInterval = setInterval(function() {
  loadIdx = (loadIdx + 1) % loadTexts.length;
  document.getElementById('t-loading-subtext').textContent = loadTexts[loadIdx];
}, 2000);
```
After the fetch completes (in the `.then()` or after response), hide the overlay:
```javascript
clearInterval(loadInterval);
document.getElementById('t-loading-overlay').style.display = 'none';
```

4. **app.html / app.js — Live loading:** In `beenden()` function in app.js, add a similar loading overlay. Create the overlay HTML in app.html:
```html
<div id="postcall-loading" style="display:none;position:fixed;inset:0;z-index:800;background:var(--page-bg);display:none;align-items:center;justify-content:center">
  <div class="n-glass" style="padding:32px;text-align:center;border-radius:12px">
    <h3 style="color:var(--page-text-color);margin-bottom:16px">Gespräch wird ausgewertet...</h3>
    <div style="width:280px;height:4px;background:var(--glass-bg,rgba(255,255,255,0.05));border-radius:2px;overflow:hidden;margin:0 auto">
      <div style="height:100%;width:50%;background:#2dd4a8;border-radius:2px;animation:indeterminate-bar 1.4s ease infinite"></div>
    </div>
  </div>
</div>
```
Add keyframes if not already present in app.html styles:
```css
@keyframes indeterminate-bar {
  0%   { transform: translateX(-100%); }
  100% { transform: translateX(560%); }
}
```
In app.js `beenden()`, show before fetch, hide after response and before `zeigePostcall()`.
  </action>
  <verify>
    <automated>grep -n "t-loading-overlay\|postcall-loading\|indeterminate\|Gespräch wird ausgewertet" templates/training.html templates/app.html static/app.js | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `templates/training.html` contains `#t-loading-overlay` div with animated progress bar
    - CSS `@keyframes t-indeterminate` animation defined
    - `endTraining()` or `confirmEndTraining()` shows overlay before fetch and hides after response
    - Loading subtext cycles through 3 phrases ("Einwände analysieren...", "Coaching-Punkte berechnen...", "Zusammenfassung erstellen...")
    - `templates/app.html` contains `#postcall-loading` overlay with animated bar
    - `static/app.js` `beenden()` shows loading before API call
  </acceptance_criteria>
  <done>Animated teal progress bar with cycling text shows during Auswertung generation in both training and live.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Kompaktmodus to floating overlay and add Dashboard back button</name>
  <files>templates/app.html, static/app.js</files>
  <read_first>templates/app.html, static/app.js</read_first>
  <action>
**UX-09 — Kompakt mode as floating overlay:**

1. **app.html CSS:** Find the existing `.kompakt-panel` styles (should be `position:fixed; inset:0; z-index:500`). Replace with floating overlay positioning:
```css
.kompakt-panel {
  position: fixed;
  bottom: 16px;
  right: 16px;
  width: 320px;
  max-height: 90vh;
  border-radius: 12px;
  border: 1px solid var(--glass-border);
  box-shadow: 0 8px 40px rgba(0,0,0,0.7);
  z-index: 500;
  display: none;
  flex-direction: column;
  overflow-y: auto;
  background: var(--page-bg);
}
body.kompakt .kompakt-panel { display: flex !important; }
```

2. **app.html CSS:** Find and REMOVE the CSS rules that hide main content when `.kompakt` is on body. These are the rules like:
```css
/* DELETE these rules: */
body.kompakt .header { display: none !important; }
body.kompakt .profile-bar { display: none !important; }
body.kompakt .phasen-bar { display: none !important; }
body.kompakt .main { display: none !important; }
body.kompakt .footer { display: none !important; }
```
Remove ALL `body.kompakt ... display:none` rules EXCEPT `body.kompakt .kompakt-panel { display: flex !important; }`.

3. **app.html:** The Kompakt button text should toggle between "◀ Kompakt" (to open) and "✕" (to close). Check that `toggleKompakt()` handles this. If the panel has a close button, ensure it calls `toggleKompakt()` to remove the `.kompakt` class.

**UX-10 — Dashboard back button in postcall:**

4. **app.html:** Find the `postcall-actions` div (approx line 775). Add a "Zurück zum Dashboard" button as the FIRST child:
```html
<div class="postcall-actions">
  <a class="btn-postcall-close" href="/dashboard" style="text-decoration:none">← Dashboard</a>
  <button class="btn-postcall-close" onclick="closePostcall()">✕ Schließen</button>
  <button class="btn-postcall-pdf" onclick="window.print()">⬇ PDF Download</button>
  <button class="btn-postcall-save" id="pc-saveBtn" onclick="saveFeedback()">★ Bewertung speichern</button>
</div>
```
The `btn-postcall-close` class should already have appropriate styling. If needed, add specific styles for the Dashboard link to ensure it looks like a button.
  </action>
  <verify>
    <automated>grep -n "kompakt-panel\|bottom.*16px\|Dashboard\|postcall-actions" templates/app.html | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `.kompakt-panel` CSS has `position:fixed; bottom:16px; right:16px; width:320px; border-radius:12px`
    - NO `body.kompakt .main { display:none }` rules exist (main content stays visible)
    - `body.kompakt .kompakt-panel { display:flex !important }` rule exists
    - `postcall-actions` div contains `<a ... href="/dashboard">← Dashboard</a>` as first child
    - Dashboard link uses `btn-postcall-close` class for consistent styling
  </acceptance_criteria>
  <done>Kompakt mode opens as floating bottom-right overlay without hiding main content. Dashboard back button visible in postcall actions.</done>
</task>

</tasks>

<verification>
- Open /live — click "Kompakt" — floating panel appears bottom-right, main content still visible behind
- Click close in compact panel — panel disappears
- Click "Beenden" — animated loading bar shown during API call
- After postcall loads — "← Dashboard" button visible as first action
- Open /training — end a training — loading overlay with cycling text appears
</verification>

<success_criteria>
Loading overlays animate during analysis in both training and live. Compact mode is a floating overlay. Dashboard back button is accessible after every session.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-uat-bug-fixes/03.2-06-SUMMARY.md`
</output>
