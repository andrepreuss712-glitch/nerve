---
phase: 03.2-uat-bug-fixes
plan: 03
subsystem: ui
tags: [sidebar, dropdown, user-menu, vanilla-js, css]

# Dependency graph
requires:
  - phase: 03.1-frontend-redesign
    provides: nerve.css design system with CSS variables and .n-* component classes
provides:
  - Sidebar user avatar block (teal circle initials, name, email) at sidebar bottom
  - Dropdown menu with 5 account links: Profil, Einstellungen, Abrechnung, Team, Abmelden
  - toggleSidebarUserMenu() JS + document click-outside close listener
affects: [base.html, nerve.css, any page extending base.html]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sidebar user menu: avatar block at bottom via margin-top:auto on flex column, dropdown opens upward with position:absolute bottom:calc(100%+4px)"
    - "Click-outside pattern: document.addEventListener('click') removes .open class; stopPropagation on toggle prevents immediate close"

key-files:
  created: []
  modified:
    - templates/base.html
    - static/nerve.css

key-decisions:
  - "Standalone Einstellungen sidebar link removed — now lives exclusively in the user dropdown"
  - "margin-top:auto on .sidebar-user-block pushes it to bottom of existing flex column sidebar"
  - "Dropdown opens upward (bottom: calc(100% + 4px)) to avoid clipping at viewport bottom edge"

patterns-established:
  - "SaaS sidebar user menu pattern: avatar + name + email block at sidebar bottom with upward dropdown"

requirements-completed: [UAT-15]

# Metrics
duration: 8min
completed: 2026-04-01
---

# Phase 03.2 Plan 03: Sidebar User Menu Summary

**Teal avatar initials + name/email block at sidebar bottom with upward dropdown containing 5 account links (Profil, Einstellungen, Abrechnung, Team, Abmelden)**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-01T15:02:00Z
- **Completed:** 2026-04-01T15:10:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- Sidebar now shows user identity (teal avatar circle with initials, display name, email) at the bottom of the nav column
- Clicking the avatar opens an upward dropdown panel with all account-related links
- Click-outside listener closes the dropdown — same pattern as existing profileMenu in app.html
- Standalone Einstellungen nav link removed from sidebar (now consolidated in dropdown)

## Task Commits

1. **Task 1: Add sidebar user avatar block and dropdown to base.html** - `9d755c7` (feat)

**Plan metadata:** _(to be recorded in final commit)_

## Files Created/Modified
- `templates/base.html` — Added `.sidebar-user-block` div with avatar, name, email, dropdown HTML; added `toggleSidebarUserMenu()` JS; removed standalone Einstellungen nav link
- `static/nerve.css` — Added 80 lines of sidebar user menu CSS: `.sidebar-user-block`, `.sidebar-avatar`, `.sidebar-user-info`, `.sidebar-user-name`, `.sidebar-user-email`, `.sidebar-user-dropdown`, `.dropdown-divider`

## Decisions Made
- Standalone Einstellungen sidebar nav link removed — it now exists only in the user dropdown to avoid duplication
- Used `margin-top: auto` on `.sidebar-user-block` to push the block to the bottom of the existing `display:flex; flex-direction:column` sidebar (no structural changes required to `.g-sidebar`)
- Dropdown positioned upward (`bottom: calc(100% + 4px)`) matching the plan spec — avoids viewport clipping

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Sidebar user menu complete and ready for visual UAT (UAT-15)
- All pages extending base.html get the sidebar user menu automatically
- Remaining UAT bug fix plans (03.2-04 through 03.2-07) can proceed

## Known Stubs
None - avatar initials, name, and email are all wired to live `g.user` Jinja2 template context.

---
*Phase: 03.2-uat-bug-fixes*
*Completed: 2026-04-01*
