---
phase: "08.23.2.PERSID"
plan: "04"
subsystem: "live-session-state / deepgram-merge-buffer / per-sid-isolation"
tags: ["familie-b", "_merge_pending", "per-sid", "s4-lock", "cross-tenant-pii", "t-persid-09", "t-persid-10"]
dependency_graph:
  requires: ["08.23.2.PERSID-01", "08.23.2.PERSID-03"]
  provides:
    - "_merge_pending pro-SID unter _session_state_lock (EIN Lock, S4)"
    - "_flush_segment(sid, key) neue Signatur; popt aus per-SID-Bucket"
    - "Cross-Tenant-Transkript strukturell unmoeglich (T-PERSID-09 mitigiert)"
    - "S4: _merge_lock geloescht; kein Lock-Widerspruch/Deadlock-Risiko mehr (T-PERSID-10 mitigiert)"
    - "Timer-Cancel in pop_session_state (Punkt 14 sauber)"
    - "transcript_buffer/analysiert_bisher Modul-Globale entfernt (tote Post-Migration-Zweige)"
    - "_PENDING_MIGRATION Familie-B-Eintrag entfernt (Liste schrumpft monoton)"
  affects:
    - services/live_session.py
    - services/deepgram_service.py
    - tests/test_persid_04_merge_pending.py
    - tests/test_no_live_global_state.py
tech_stack:
  added: []
  patterns:
    - "_merge_pending als Sub-Key von _session_state[sid] (Form 2, SPEC-Req-1-treu)"
    - "EIN Lock fuer Merge-Bucket-Mutation (_session_state_lock, S4)"
    - "Timer-Cancel-Snapshot unter Lock, cancel ausserhalb (D-03 Lock-Disziplin)"
    - "Ghost-SID-Guard in _flush_segment + deepgram-Schreiber (kein Write in tote Session)"
key_files:
  created:
    - tests/test_persid_04_merge_pending.py
  modified:
    - services/live_session.py
    - services/deepgram_service.py
    - tests/test_no_live_global_state.py
decisions:
  - "Form 2 (Puffer unter _session_state[sid]): SPEC-Req-1-treue Wahl; EIN Lock = _session_state_lock (S4, fest)"
  - "transcript_buffer/analysiert_bisher Modul-Globale in selber Task entfernt: 0 externe Reader belegt, tote Post-Migration-Code-Pfade"
  - "triage.sh: 5 pre-existierende Fehlschlaege (GLiNER /nonexistent + Haiku-Integration) — nicht durch Plan 04 verursacht; deploy.sh -m 'not live and not perf' bestanden (844 passed)"
metrics:
  duration: "~1.5h"
  completed_date: "2026-07-04"
  tasks_completed: 1
  files_changed: 4
---

# Phase 08.23.2.PERSID Plan 04: Familie B — _merge_pending per-SID SUMMARY

**One-liner:** `_merge_pending` pro-SID unter `_session_state_lock` (EIN Lock S4); `_flush_segment(sid, key)` popt per-SID-Bucket; Cross-Tenant-Transkript strukturell unmoeglich (T-PERSID-09 mitigiert).

## Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 RED | per-SID _merge_pending Tests (Isolation, Cross-Pop, Signatur, S4 Lock) | a1ff04c | tests/test_persid_04_merge_pending.py |
| 1 GREEN | _merge_pending pro-SID; _merge_lock entfernt; _flush_segment(sid, key); Timer-Cancel | 6d5d9c3 | live_session.py, deepgram_service.py, test_no_live_global_state.py |

## Was gebaut wurde

### Task 1 RED — Failing Tests

**`tests/test_persid_04_merge_pending.py`** (neu erstellt):
- **Test 1** (`test_merge_pending_cross_tenant_isolation`): Zwei SIDs A/B, gleicher Sprecher-Key "0"; nach `_flush_segment`-Flush enthaelt Coaching-Buffer A keine Beta-Woerter (Zebra/Qualle-Beweis, Runtime-State-Mutation).
- **Test 2** (`test_flush_segment_pops_correct_per_sid_bucket`): Timer-Callback popt aus dem RICHTIGEN per-SID-Bucket (kein Cross-Pop; SID-B-Bucket unveraendert nach SID-A-Flush).
- **Test 3** (`test_flush_segment_signature_takes_sid_and_key`): `inspect.signature()` prueft `(sid, key)` Signatur.
- **Test 4** (`test_merge_lock_deleted_from_live_session`): `hasattr(ls, '_merge_lock')` → False (S4).
- **Test 5** (`test_merge_pending_global_deleted_from_live_session`): `hasattr(ls, '_merge_pending')` → False.

### Task 1 GREEN — Implementierung

**`services/live_session.py`:**
- `_merge_lock` + `_merge_pending = {}` Modul-Globale **geloescht** (S4 + Familie-B-Migration).
- `_session_state[sid]['_merge_pending'] = {}` in `init_session_state` **geseedet** (Sub-Key, Form 2).
- `_flush_segment(key)` → `_flush_segment(sid, key)`: Signatur erweitert; Pop aus `_session_state[sid]['_merge_pending']` unter `_session_state_lock`; Ghost-SID-Guard (sid weg → return); tote `transcript_buffer`/`coaching_buffer`-Fallback-Zweige **entfernt**.
- `pop_session_state`: Timer-Cancel-Block hinzugefuegt (Snapshot unter Lock, `.cancel()` ausserhalb, D-03 Lock-Disziplin).
- `reset_session`: `global transcript_buffer, analysiert_bisher, coaching_buffer` Deklarationen entfernt; `_merge_lock`/`_merge_pending`-Cancel-Block entfernt; `transcript_buffer.clear()`/`analysiert_bisher.clear()`/`coaching_buffer.clear()`-Aufrufe entfernt.
- `transcript_buffer = []` und `analysiert_bisher = []` Modul-Globale **geloescht** (0 externe Reader nach per-SID-Migration; tote Post-Migration-Zeile).

**`services/deepgram_service.py`:**
- Merge-Write-Block (Zeilen 194-214): `with ls._merge_lock:` + `ls._merge_pending[key]` → `with ls._session_state_lock:` + Ghost-SID-Guard + `ls._session_state[sid].setdefault('_merge_pending', {})[key]`.
- Timer-Args: `args=[key]` → `args=[sid, key]` (neue `_flush_segment`-Signatur).

**`tests/test_no_live_global_state.py`:**
- `_KILLED_MODULE_GLOBALS` erweitert: `_merge_lock`, `_merge_pending`, `transcript_buffer`, `analysiert_bisher` hinzugefuegt.
- `_WHITELIST`: `_merge_lock`, `_merge_pending`, `transcript_buffer`, `analysiert_bisher` entfernt.
- `_PENDING_MIGRATION`: Familie-B-Eintrag `('services/deepgram_service.py', '_merge_pending', 'B')` entfernt (MIGRIERT-Kommentar ersetzt).

## Verifikation

### Acceptance-Criteria-Grep

| Kriterium | Ergebnis |
|---|---|
| `grep "_merge_pending" deepgram_service.py` → Write in `_session_state[sid]['_merge_pending']` unter `_session_state_lock` | GRUEN |
| `grep "_merge_lock" services/` → 0 aktive Nutzungen (nur Kommentare) | GRUEN (S4) |
| `grep "def _flush_segment" live_session.py` → Signatur `(sid, key)` | GRUEN |
| `grep "^transcript_buffer =" live_session.py` → Modul-Global weg | GRUEN |
| `grep "^analysiert_bisher =" live_session.py` → Modul-Global weg | GRUEN |
| Timer-Cancel in `pop_session_state` unter Lock-Snapshot, cancel ausserhalb | GRUEN |
| deploy.sh production GRUEN | GRUEN (844 passed, 9 skipped) |

### Persistenz-Schicht

Vor Umbau geholt:
```
transcript_segments: bigint PK, conversation_log_id, ts_ms, speaker, text, created_at
Indexes: PK + idx_transcript_segments_conv_ts
```
DB-Schema unveraendert — nur RAM-Puffer-Struktur geaendert.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Critical] `transcript_buffer`/`analysiert_bisher` Modul-Globale in selber Task entfernt**
- **Found during:** Task 1 GREEN Implementierung
- **Issue:** Plan nennt das Entfernen explizit als Teil von Task 1d; Modul-Globale hatten 0 externe Leser nach der per-SID-Migration.
- **Fix:** `transcript_buffer = []` und `analysiert_bisher = []` aus `live_session.py` entfernt; zugehoerige `buffer_lock`-`clear()`-Aufrufe in `reset_session` entfernt.
- **Files modified:** `services/live_session.py`
- **Commit:** 6d5d9c3

### triage.sh Status

triage.sh returned exit 1 wegen 5 **pre-existierender** Fehlschlaege:
- `test_anonymization_perf.py` (3 Tests): GLiNER laeuft in `/nonexistent` (HF_TOKEN/Cache-Pfad Infrastruktur-Problem, kein Plan-04-Effekt).
- `test_anonymization_security.py::test_reid_rate_below_5_percent`: gleicher GLiNER-Fehler.
- `test_phase_classifier.py::test_phase_classifier_integration_real_haiku`: Haiku-API-Integration.

`deploy.sh production` verwendet `-m "not live and not perf"` und schliesst diese aus → **844 passed, deploy.sh GRUEN**. Die triage.sh-Fehlschlaege existierten vor Plan 04 und sind scope-aussen.

## Threat Surface Scan

T-PERSID-09 (Information Disclosure: `_merge_pending` global → Cross-Tenant-Transkript): **MITIGIERT** — Bucket pro SID.
T-PERSID-10 (Tampering: zwei Locks fuer dasselbe Bucket → Verschraenkung/Deadlock): **MITIGIERT** — `_merge_lock` geloescht, EIN Lock.

Keine neuen Threat-Surface-Einfuehrungen.

## Known Stubs

Keine — alle per-SID-Pfade sind vollstaendig verdrahtet.

## Self-Check

- [x] `tests/test_persid_04_merge_pending.py` vorhanden (a1ff04c)
- [x] `services/live_session.py` + `services/deepgram_service.py` + `tests/test_no_live_global_state.py` geaendert (6d5d9c3)
- [x] `hasattr(ls, '_merge_lock')` → False (deploy bestanden)
- [x] `hasattr(ls, '_merge_pending')` → False (deploy bestanden)
- [x] `_flush_segment` Signatur `(sid, key)` (deploy bestanden)
- [x] deploy.sh production 844 passed

## Self-Check: PASSED
