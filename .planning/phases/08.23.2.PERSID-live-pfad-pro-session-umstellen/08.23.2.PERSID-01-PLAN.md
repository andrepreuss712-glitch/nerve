---
phase: 08.23.2.PERSID
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/live_session.py
  - services/claude_service.py
  - routes/app_routes.py
  - app.py
  - tests/test_no_live_global_state.py
  - tests/test_persid_concurrency.py
autonomous: false
requirements: [SPEC-Req-5, SPEC-Req-7, SPEC-Req-12]

must_haves:
  truths:
    - "Die write-only-Zombie-Global-Familien (0 grep-belegte Prod-Reader) sind aus dem Code entfernt oder als DEPRECATED-GLOBAL markiert"
    - "Der statische Global-Waechter existiert und ist gruen gegen die Whitelist MIT wave-getaggter PENDING-MIGRATION-Liste (laeuft im Default-Gate, NICHT rot committet) — legit noch-unmigrierte Live-Schreiber sind in der Pending-Liste, nicht rot (B3)"
    - "Das Concurrency-Test-Geruest importiert app ohne echte Daemons zu wecken (app.py ENV-basierter TESTING-Guard, NICHT app.config — S1)"
    - "Die 3 UNKLAR-Globale (aktives_skript_inhalt/skript_bloecke, aktive_phase_idx, _bof_count) sind per grep als FIX oder DELETE entschieden; aktive_phase_idx hat ZWEI Reader (app_routes:244 + claude:206) → FIX-Kandidat (B5)"
    - "is_paused/pause_lock + _line_id_counter/_line_id_lock Modul-Globale sind in der Kill-Liste (schon per-sid, Modul-Rest tot — S5); _ewb_ttft_history/_ewb_fallback_until sind als tenant-neutral gewhitelistet (S6)"
  artifacts:
    - path: "tests/test_no_live_global_state.py"
      provides: "Statischer Global-Waechter (Punkt 28) — hasattr-Absenz + AST/grep-Sweep, Whitelist + wave-getaggte Pending-Migration-Liste (B3)"
      min_lines: 50
    - path: "tests/test_persid_concurrency.py"
      provides: "Concurrency-Test-Geruest (Skeleton + Fixtures + Mock-Naehte), Voll-Assertions folgen in Welle E"
      min_lines: 40
  key_links:
    - from: "app.py:2214"
      to: "analyse_loop/coaching_loop/slow_lane_consumer daemon start"
      via: "if os.environ.get('NERVE_TESTING') != '1' Guard (ENV-basiert, S1)"
      pattern: "NERVE_TESTING"
    - from: "tests/test_no_live_global_state.py"
      to: "services/live_session.py globals"
      via: "assert not hasattr(ls, '<global>')"
      pattern: "not hasattr\\(ls,"
---

<objective>
Welle-0-Fundament fuer die per-Session-Migration. Drei Bloecke:
1. **Zombie-Deletes (D-09):** Die grep-belegten write-only-Globalen mit 0 Prod-Readern LOESCHEN statt migrieren (spart 5 Migrationen aus SPEC Req 5 + Teile Req 7). Das reconciled den HIGH-Drift 1 (SPEC Req 5 sagt "migrieren", grep sagt "loeschen").
2. **Statischer Global-Waechter (Punkt 28, SPEC Req 12 Teil 2):** `tests/test_no_live_global_state.py` — laeuft im Default-Gate, gruen. **B3-Kern: eine wave-getaggte, pro Welle SCHRUMPFENDE PENDING-MIGRATION-Whitelist**, damit die in Welle 0 noch LEGITIMEN unmigrierten Live-Schreiber (deepgram:619 session_anrede-start, claude:1362 kaufbereitschaft, etc.) den Waechter NICHT rot faerben (waere per D-10 ein Deploy-Blocker bis Welle 5). Reichweite: auch Modul-Level-Bindungen der 4 Live-Module (mind. als Plan-06-Finalisierung).
3. **Concurrency-Test-Geruest (SPEC Req 12 Teil 1):** `tests/test_persid_concurrency.py` Skeleton + app.py **ENV-basierter** TESTING-Guard (S1). Voll-Assertions kommen erst mit Welle E (D-10: Voll-Test erst mit letzter Welle gruen committen).

Purpose: Kein Bau auf einem Fundament mit bekanntem Loch (D-06/Punkt 24). Die Inventur ist zugleich Migrations- UND Prune-Landkarte.
Output: geloeschte/markierte Zombie-Familien, 1 gruener Waechter (mit Pending-Liste), 1 Test-Geruest, 1 ENV-TESTING-Guard, 3 UNKLAR-Globale entschieden.

**Komplexitaet: 🔴 — Cross-AI/3-Sichten PFLICHT vor Execute (CLAUDE.md Punkt 24/28).**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-SPEC.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-CONTEXT.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Ziel-Fundament (existiert, live_session.py) — NICHT neu erfinden -->
_session_state: dict = {}            # {sid: {...}}  live_session.py:210
_session_state_lock = threading.Lock()
init_session_state(sid, user_id, org_id, profile_id=None, ...)  # :301, voll geseedet
pop_session_state(sid)              # :438

<!-- Whitelist-Ueberlebende (D-05, aus RESEARCH §1 + FRESH-GREP 2026-07-03) — der Waechter MUSS diese durchlassen -->
Locks: alle *_lock (pause_lock:41, log_lock, state_lock, kb_lock, _merge_lock:90 (bzw. _session_state_lock), _line_id_lock:95, phase_lock:182, ...)
Per-SID-Container (sid-gekeyte Dicts, per-sid-safe): _session_state, _per_sid_profile,
  _per_sid_transcript, _per_sid_coaching_buffer, keyword_matchers, _deepgram_sessions,
  _cost_opened_at, _stt_seconds_accumulated
Konstanten: LOG_DIR, _DU_FORMS, _SALES_KEYTERMS_BASE, MAX_KEYTERMS, _STOPWORDS, _sp_map, _AUDIO_WARN_*,
  _ROLLING_WINDOW_MS, _OUTCOME_MODIFIERS, MERGE_WINDOW_S
Tenant-neutrale Latenz-Caches (S6, EXPLIZIT gewhitelistet, dokumentiert): _ewb_ttft_history (claude:18),
  _ewb_fallback_until (claude:19) — self-contained TTFT-Circuit-Breaker des DORMANTEN streame_auto_variante
  (Konsument grep-belegt tot: deepgram:267-269 "write-only/dormant, 0 externe Produktiv-Reader"). Kein
  pro-User-Zustand → tenant-neutral whitelisten statt migrieren (keine Cross-Tenant-Semantik, nur Latenz-Mittel).
Trigger-Events (Signal, kein Zustand): analyse_trigger, coaching_trigger
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Die 3 UNKLAR-Globale per grep entscheiden (FIX vs DELETE) — aktive_phase_idx hat ZWEI Reader (B5)</name>
  <read_first>
    - services/live_session.py:174,183 (Defs `_bof_count`, `aktive_phase_idx`); :182 phase_lock; :950-951 reset
    - RESEARCH.md §1 Zeilen zu aktives_skript_inhalt/skript_bloecke/aktive_phase_idx/_bof_count
    - RESEARCH.md §6 Landmine 4+5 + Assumptions A1/A2
    - **B5-FRESH-GREP (2026-07-03):** `aktive_phase_idx` hat ZWEI aktive Prod-Reader, nicht einen:
      routes/app_routes.py:243-244 (`with ls.phase_lock: cp_snapshot.add(ls.aktive_phase_idx)`) UND
      services/claude_service.py:205-206 (`with ls.phase_lock: idx = ls.aktive_phase_idx` im Prompt-Builder,
      innerhalb `if phasen:`). Loeschen der Globale + phase_lock OHNE claude:206-Rewire → AttributeError bei
      JEDEM Prompt-Bau → Analyse-Pfad live kaputt. → aktive_phase_idx MUSS FIX (per-sid), nicht DELETE.
  </read_first>
  <decision>Sind aktives_skript_inhalt/skript_bloecke, aktive_phase_idx, _bof_count write-only-Zombies (DELETE) oder haben sie einen aktiven Prod-Reader (FIX per-SID)?</decision>
  <context>
    RESEARCH konnte fuer diese 3 Familien KEINEN aktiven Prod-Reader grep-belegen, aber auch nicht sicher ausschliessen (A1/A2 = MED-Confidence). B5-Fresh-Grep hat fuer aktive_phase_idx nun ZWEI Reader belegt (app_routes:244 + claude:206) → FIX. D-09 verlangt grep-Beleg pro Global vor Loeschen ("was LEBT gewinnt"). Vor dem Loeschen MUSS der Executor greppen:

    ```bash
    cd /c/Users/andre/dev/salesnerve
    grep -rn "aktives_skript_inhalt\|skript_bloecke" services/ routes/
    grep -rn "aktive_phase_idx" services/ routes/
    grep -rn "_bof_count" services/ routes/
    grep -rn "state\.get('aktives_skript_inhalt')\|state\['aktives_skript_inhalt'\]" services/ routes/
    ```
    Zusaetzlich in services/prompt_pipeline.py greppen (A1: moeglicher ungegreppter Reader).
    Jeder Treffer klassifizieren: aktiver Prod-Pfad vs. nur Def/reset/Test.
  </context>
  <options>
    <option id="delete-all-zombies">
      <name>Global X = DELETE (grep bestaetigt 0 Prod-Reader)</name>
      <pros>Kein toter Migrations-Aufwand; Inventur sauber</pros>
      <cons>Gilt NICHT fuer aktive_phase_idx (2 Reader belegt, B5). Fuer aktives_skript_inhalt/skript_bloecke/_bof_count nur wenn grep 0 Reader bestaetigt.</cons>
    </option>
    <option id="fix-per-sid">
      <name>Global X hat aktiven Reader → FIX-als-per-SID (in die passende Fix-Welle schieben)</name>
      <pros>Kein Verhaltens-Verlust; kein AttributeError (aktive_phase_idx-Fall, B5)</pros>
      <cons>Diese Global wandert aus Welle 0 in ihre Datenfamilie-Welle (aktive_phase_idx → Plan 05 Familie C, wo app_routes:244 + claude:206 gemeinsam auf per-sid state['aktive_phase_idx'] umgebogen werden)</cons>
    </option>
  </options>
  <resume-signal>Pro Global: "DELETE" (mit grep-0-Reader-Beleg) oder "FIX in Welle X" (mit grep-Reader-Beleg). aktive_phase_idx-Default = FIX in Welle C (Plan 05) wegen der ZWEI Reader (B5). Ergebnis wird in die Zombie-Delete-Liste (Task 2) bzw. die Fix-Welle uebernommen.</resume-signal>
  <acceptance_criteria>
    - grep-Output fuer alle 3 Familien in der Entscheidung zitiert (Datei:Zeile pro Treffer)
    - Pro Global genau ein Verdikt: DELETE (0 Prod-Reader) oder FIX (Reader belegt)
    - **aktive_phase_idx = FIX (2 Reader: app_routes:244 + claude:206 — B5); die claude:206-Reader-Umstellung wird in Plan 05 Familie C mitgezogen (per-sid state['aktive_phase_idx'], phase_lock dort entfernt)**
    - prompt_pipeline.py explizit gegriffen (A1-Absicherung)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Zombie-Globale loeschen (D-09) + DEPRECATED-GLOBAL-Marker (Punkt 29) + is_paused/_line_id_counter Modul-Rest (S5)</name>
  <read_first>
    - services/live_session.py:75 (session_meta), :63 (hilfe_log), :67 (quick_action_log), :156 (roles_swapped), :749 (last_postcall), :46-47 (transcript_buffer/analysiert_bisher Modul-Globale), :116-136 (state-Zombie-Keys)
    - **S5-FRESH-GREP (2026-07-03):** services/live_session.py:41-42 (`pause_lock`/`is_paused` Modul-Global — is_paused ist schon per-sid geseedet :354 + per-sid gelesen :110 → Modul-Global :42 tot); :94-95 (`_line_id_counter`/`_line_id_lock` — schon per-sid :102-103,:426 → Modul-Global :94 tot); reset_session-Reset :926-927 (`_line_id_counter = 0`)
    - services/claude_service.py:955,1137-1139,1360-1361,1370-1372 (Writer der state-Zombies ergebnis/aktiv/version/active_hint/ewb_buttons)
    - routes/app_routes.py:112 (precall_briefing-Reader :110-114), :296 (hilfe_log len), :298 (quick_action_log len), :1054 (roles_swapped in _build_log_content), :641 (last_postcall-Writer)
    - RESEARCH.md §1 Tabelle (Spalte "Fix-vs-Delete" = DELETE) + §6 Landmine 1/6/10
  </read_first>
  <action>
    Loesche die grep-belegten Zombie-Globalen (0 Prod-Reader). Fuer die 5 `state`-Result-Zombies (HIGH-Drift 1): SPEC Req 5 sagt "migrieren", grep sagt DELETE (Auslieferung 100% via sio.emit(room=sid); Polling-Routes /api/ergebnis + /api/status existieren nicht mehr). Reconciliation: Req-5-Intent (kein Cross-Tenant-Result-Leak) ist durch Loeschung ERFUELLT — eine nie gelesene Globale kann nicht leaken. Das ist KEIN Scope-Drop.

    KONKRET loeschen (nach Task-1-Bestaetigung fuer die UNKLAR-Faelle):
    1. `state['ergebnis']` (live_session.py:118) + Writer claude_service.py:1137,1370 — Writer-Zeile ersatzlos entfernen (Auslieferung laeuft via emit).
    2. `state['aktiv']` + Writer claude_service.py:955,1138,1371.
    3. `state['version']` (:116) + Writer claude_service.py:1139,1372 (`+= 1`).
    4. `state['active_hint']` (:134) + Writer claude_service.py:1360.
    5. `state['ewb_buttons']` (:136) + Writer claude_service.py:1361.
    6. `state['line_id']` (:119) — schon per-SID (claude_service.py:1141 Kommentar) → Modul-Key loeschen.
    7. `state['slot1_variant_busy_until']` (:143) — PIP-01 entfernt, 0 Reader → loeschen.
    8. `session_meta` (:75) Modul-Globale + per-SID-Seed-Sub-Key init_session_state:400-413 (RESEARCH §6.10: per-SID-Seed auch tot).
    9. `last_postcall` (:749) + Writer app_routes.py:641 (`= {...}`) — Writer-Zeile ersatzlos entfernen.
    10. `hilfe_log` (:63) — 0 `.append`. Reader app_routes.py:296 `len(ls.hilfe_log)` → durch Literal `0` ersetzen; `_build_log_content:1191`-Reader ebenso.
    11. `quick_action_log` (:67) — analog: Reader app_routes.py:298 `len()` → `0`; `_build_log_content:1193`.
    12. `roles_swapped` (:156) Modul-Globale — effektiv Konstante False (0 `= True`). Reader `_build_log_content:1054` liest per-SID `_session_state[sid]['roles_swapped']` NICHT — hier: swap-Map fest auf unswapped setzen (sp_map = {0:'Berater',1:'Kunde'}), tote swap-Logik :1056 entfernen. per-SID-Seed :318 bleibt (spaetere Nutzung dokumentiert oder mitloeschen — im Zweifel loeschen, D-05 Default).
    13. **S5: `is_paused` Modul-Global (:42) + `_line_id_counter` Modul-Global (:94) LOESCHEN.** Beide sind schon per-sid (is_paused: Seed :354, Read :110; _line_id_counter: per-sid :102-103,:426). Die Modul-Global-Defs sind tote Rest-Leichen. `pause_lock` (:41) NUR loeschen wenn kein anderer Reader ihn nutzt (grep `pause_lock` — falls 0 aktive Leser → weg, sonst als Lock in der Whitelist behalten). `_line_id_lock` (:95) analog. Der reset_session-Reset `_line_id_counter = 0` (:926-927) + das `global`-Statement (:873) entfernen. **Req-7-Acceptance ("keine modul-globale Live-Zustands-Variable bleibt uebrig") faellt sonst formal durch.**
    14. `precall_briefing` doppelte Wahrheit (§6.9): Reader app_routes.py:112 liest Modul-`state['precall_briefing']`; echter Wert liegt per-SID (Set via set_briefing_for_sid). Der Reader-Umbau haengt an der sid-Aufloesung von api_beenden → wird in **Plan 03 (Familie A)** gemacht (dort ist `_beenden_sid` definiert). In Welle 0 den Modul-`state['precall_briefing']`-Key NICHT loeschen, sondern als `# DEPRECATED-GLOBAL: precall_briefing — Reader-Umbau Plan 03` markieren (Punkt 29) + in die Pending-Migration-Liste des Waechters (Task 3, Welle-Tag "A") aufnehmen.

    Fuer jede Globale, die aus Zeitgruenden NICHT sofort entfernbar ist (Reader haengt an sid-Aufloesung einer spaeteren Welle): statt Loeschen einen `# DEPRECATED-GLOBAL: <global> — 0 Prod-Reader (RESEARCH §1), Loeschung in Welle <X>` Kommentar setzen (Punkt 29) — der Waechter (Task 3) macht dann JEDEN NEUEN Schreib-Zugriff darauf rot (aber lässt die EINE dokumentierte Legacy-Stelle via Pending-Liste durch).

    Loesche zusaetzlich die zugehoerigen Locks + Reset-Bloecke in reset_session (live_session.py:864-959) fuer alle geloeschten Globalen (z.B. `state['ergebnis']=None` bei :901, `state['active_hint']=None` :919, `state['ewb_buttons']=None` :920, roles_swapped-Reset :946, etc.).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Nach Deploy: grep-Beleg dass keine geloeschte Globale mehr definiert ist: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "state\['\''ergebnis'\''\]"'` → 0 Writer-Treffer im Prod-Code.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "ergebnis\|active_hint\|ewb_buttons" services/live_session.py` zeigt KEINE state-Zombie-Key-Definition mehr (nur ggf. DEPRECATED-GLOBAL-Kommentar)
    - `grep -n "last_postcall\|session_meta =" services/live_session.py` → 0 aktive Defs
    - **S5: `grep -n "^is_paused\|^_line_id_counter" services/live_session.py` → 0 Modul-Global-Defs** (beide waren schon per-sid; Modul-Rest weg); reset_session hat keinen `_line_id_counter = 0`-Reset + kein `global _line_id_counter` mehr
    - `grep -n "state\['ergebnis'\]\|state\['version'\]" services/claude_service.py` → 0 Writer
    - precall_briefing: als DEPRECATED-GLOBAL markiert + in Pending-Migration-Liste (Welle A) — NICHT hier geloescht (Reader-Umbau Plan 03)
    - deploy.sh production nerve_test-Gate GRUEN (kein Import-Fehler durch entfernte Globale)
    - Jede nicht-sofort-loeschbare Globale traegt einen `# DEPRECATED-GLOBAL`-Kommentar mit Welle-Verweis + steht in der Pending-Liste
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Statischer Global-Waechter tests/test_no_live_global_state.py + wave-getaggte PENDING-MIGRATION-Liste (B3, Punkt 28/29, SPEC Req 12 Teil 2)</name>
  <read_first>
    - tests/test_schild_guard.py:1-55 (Muster: DB-lesender Guard, MIN_LEN-Konvention, Skip-ohne-DSN)
    - tests/test_ewb_autovar_global_regression.py (Muster: Global-Regression-Sweep)
    - RESEARCH.md §1 "Allowed-Survivor-Whitelist" (D-05) + §5 "Statischer Waechter" (KEIN Source-Presence-False-Green)
    - Plan 01 Task 1 Verdikte (DELETE vs FIX) fuer die 3 UNKLAR-Globale — bestimmt _KILLED-frozenset vs per-sid-AST-Sweep-Abdeckung
    - CLAUDE.md "Test-Qualitaets-Regel" (hasattr-Absenz nach Loeschung = erlaubt; AST-Sweep = dokumentierter Regex-Grenzfall)
    - **B3-FRESH-GREP (2026-07-03) — die in Welle 0 noch LEGITIMEN unmigrierten Live-Schreiber, die der Waechter OHNE Pending-Liste faelschlich rot faerbte:**
      deepgram_service.py:619 (`ls.state['session_anrede'] = anrede_norm` — start-Writer, Migration Welle A/Plan 03);
      claude_service.py:1362 (`kaufbereitschaft` global — Welle C/Plan 05); claude_service.py:1077,1214,1233 (conversation_log/phasen_log/covered_phases — Welle C/Plan 05); deepgram_service.py:132,955 (conversation_log — Welle C); deepgram_service.py:856 (mic_muted — Welle A); deepgram_service.py:71,77 (Speaker — Welle E/Plan 06); live_session.py:995,1023 (record_* state.setdefault — Welle D/Plan 06). Jeder dieser Schreiber ist bis zu seiner Welle LEGITIM → Pending-Liste, nicht rot.
  </read_first>
  <behavior>
    - Test 1 (hasattr-Absenz): `assert not hasattr(ls, g)` fuer JEDE in Task 2 geloeschte Modul-Globale (session_meta/last_postcall/hilfe_log/quick_action_log/roles_swapped/is_paused/_line_id_counter) + `'x' not in ls.state` fuer die geloeschten state-Keys (ergebnis/aktiv/version/active_hint/ewb_buttons/line_id/slot1_variant_busy_until). Gruen nach Task 2.
    - Test 2 (AST/grep-Sweep, Whitelist + Pending-Liste): sweep ueber services/*.py + routes/*.py auf das Zuweisungs-Muster `ls.<GLOBAL> = ...` bzw. `ls.state[...] = ...` im Live-Pfad; JEDER Treffer, der nicht per-sid-gekeyt UND nicht in der statischen Whitelist (§1, Locks/Container/Konstanten/tenant-neutrale-Caches) UND nicht in der wave-getaggten `_PENDING_MIGRATION`-Liste ist → Test rot. Gruen in Welle 0, weil die legit unmigrierten Schreiber in _PENDING_MIGRATION stehen.
    - Test 3 (DEPRECATED-GLOBAL): JEDER NEUE `ls.<deprecated_global> = ` Schreib-Zugriff ausserhalb der dokumentierten Pending-Stelle → rot (Punkt 29 Halb-Migration-Falle).
  </behavior>
  <action>
    Baue den Waechter nach Muster test_schild_guard.py (reiner Static/AST-Sweep, KEIN Postgres → laeuft immer). Kern:
    - `import services.live_session as ls`
    - `_KILLED_MODULE_GLOBALS = frozenset({'session_meta','last_postcall','hilfe_log','quick_action_log','roles_swapped','is_paused','_line_id_counter', ...})` (aus Task 2, inkl. Task-1-DELETE-Verdikte + S5)
    - `_KILLED_STATE_KEYS = frozenset({'ergebnis','aktiv','version','active_hint','ewb_buttons','line_id','slot1_variant_busy_until', ...})`
    - `_WHITELIST` = die §1-Liste (Locks/Container/Konstanten/Trigger + S6 tenant-neutrale Caches `_ewb_ttft_history`/`_ewb_fallback_until`).
    - **B3: `_PENDING_MIGRATION` — eine wave-getaggte, dokumentierte dict/frozenset, die pro Welle SCHRUMPFT.** Struktur z.B.:
      ```python
      # (datei, muster/global, welle_tag) — jede Zeile ist ein LEGITIM noch-unmigrierter Live-Schreiber.
      # Diese Liste MUSS bis Plan 06 (Welle E) LEER sein (Plan 06 Task 3 assertet len==0).
      _PENDING_MIGRATION = {
          ('services/deepgram_service.py', "state['session_anrede']", 'A'),   # Plan 03
          ('services/deepgram_service.py', "state['mic_muted']", 'A'),        # Plan 03
          ('services/live_session.py', "state['precall_briefing']", 'A'),     # Plan 03 (Reader-Umbau)
          ('services/deepgram_service.py', '_merge_pending', 'B'),            # Plan 04
          ('services/claude_service.py', 'conversation_log', 'C'),            # Plan 05
          # ... alle weiteren legit unmigrierten Live-Schreiber, je mit Welle-Tag
          ('services/deepgram_service.py', '_log_last_sp', 'E'),             # Plan 06
          ('services/live_session.py', "state['ewb_clicks']", 'D'),          # Plan 06
      }
      ```
      Der AST-Sweep (Test 2) ignoriert einen Verstoss NUR wenn (datei, global) in _PENDING_MIGRATION steht. **Jede Welle-Plan (03/04/05/06) ENTFERNT beim Migrieren ihre Zeilen aus _PENDING_MIGRATION** (Teil ihrer Acceptance) → die Liste schrumpft monoton. Plan 06 Task 3 assertet `_PENDING_MIGRATION == frozenset()` bzw. `{} `.
    - **B3-Reichweite (Modul-Level-Bindungen):** der AST-Sweep erfasst nicht nur `ls.<attr> = ` in fremden Modulen, sondern auch **Modul-Level-Bindungen INNERHALB der 4 Live-Module** (`services/live_session.py`, `services/deepgram_service.py`, `services/claude_service.py`, `services/einwand_keyword_matcher.py`): ein `<bare_name> = <veraenderlicher Wert>` auf Modul-Ebene (nicht Konstante/Lock/Container/Whitelist) → Verstoss (Req-7-Vollstaendigkeit). Mindest-Umsetzung: als Plan-06-Finalisierung; in Welle 0 mindestens die fremd-Modul-`ls.*`-Zuweisungen + `ls.state[...]`-Subscripts.
    - Test 1: assert-Loop ueber _KILLED_MODULE_GLOBALS (`not hasattr`) + _KILLED_STATE_KEYS (`not in ls.state`).
    - Test 2: AST-parse jede Datei, finde Attribute-Assign-Targets auf `ls`/`live_session` + Subscript-Assigns auf `ls.state` + Modul-Level-bare-Name-Assigns in den 4 Live-Modulen; whitelist- + pending-filtern; `assert violations == []` mit Datei:Zeile + Global-Name.
    - **W4 (FIX-Verdikt-Robustheit):** Wenn eine der 3 UNKLAR-Globale (aktives_skript_inhalt/skript_bloecke, aktive_phase_idx, _bof_count) das Plan-01-Task-1-Verdikt **FIX** traegt (aktiver Reader → per-sid migriert; aktive_phase_idx = FIX per B5), wird sie NICHT ins `_KILLED_*`-frozenset aufgenommen — sonst faerbt Test 1 den Waechter rot, obwohl die per-sid-Variante korrekt lebt. Bis ihre Fix-Welle live ist, steht ihr globaler Schreiber/Leser in _PENDING_MIGRATION (Welle-Tag); danach deckt sie der per-sid-AST-Sweep ab. So faerbt weder ein FIX-Verdikt noch ein legit-unmigrierter Schreiber den Guard still rot.
    - Dokumentiere im Docstring, warum der AST-Sweep KEIN Source-Presence-False-Green ist (er prueft ein VERBOTENES Muster, nicht die Existenz von erwuenschtem Code — Regex-Grenzfall pro CLAUDE.md).
    - **D-10:** Dieser Waechter ist bereits jetzt GRUEN (Task 2 hat die Zombies entfernt; die legit unmigrierten Schreiber stehen in _PENDING_MIGRATION) → er darf committet werden, wird NICHT rot ins Gate. (Nur der Concurrency-Test bleibt bis Welle E ungetestet-committed als Skeleton.)
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Der Waechter laeuft im nerve_test-Gate (Default, kein live/perf-Marker). Bei gruenem deploy.sh ist er gruen. Kein Local-Dev.</manual>
  </verify>
  <acceptance_criteria>
    - tests/test_no_live_global_state.py existiert, >=50 Zeilen
    - Test 1 assertet `not hasattr(ls, g)` fuer alle _KILLED_MODULE_GLOBALS (inkl. is_paused/_line_id_counter — S5)
    - Test 2 AST-Sweep mit Whitelist-frozenset + **wave-getaggter _PENDING_MIGRATION-Liste (B3)**; Verstoss-Meldung nennt Datei:Zeile
    - **B3: der Waechter ist GRUEN in Welle 0** obwohl legit unmigrierte Live-Schreiber (deepgram:619, claude:1362, ...) noch existieren — weil sie in _PENDING_MIGRATION (Welle-Tag) stehen; er wurde NICHT still aufgeweicht
    - **B3-Reichweite:** der Sweep erfasst Modul-Level-Bindungen der 4 Live-Module (mind. als dokumentierte Plan-06-Finalisierung) + fremd-Modul-`ls.*`/`ls.state[...]`-Assigns
    - S6: `_ewb_ttft_history`/`_ewb_fallback_until` sind in _WHITELIST (tenant-neutral, dokumentiert)
    - **W4:** jede UNKLAR-Globale mit Verdikt=FIX ist NICHT im _KILLED-frozenset (aktive_phase_idx: bis Welle C in _PENDING_MIGRATION, danach per-sid-AST-Sweep)
    - Docstring dokumentiert den AST-Regex-Grenzfall (CLAUDE.md Test-Qualitaets-Regel)
    - deploy.sh production GRUEN (Waechter gruen, NICHT rot committet — D-10)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: app.py ENV-basierter TESTING-Guard (S1) + Concurrency-Test-Skeleton (SPEC Req 12 Teil 1, Falle b)</name>
  <read_first>
    - app.py:2209-2222 (Daemon-Start analyse_loop/coaching_loop/slow_lane_consumer — KEIN Guard, RESEARCH §5); der Start liegt auf MODUL-Ebene (`import app` startet die Daemons) → app.config kann VORHER nicht gesetzt sein (S1-Kern)
    - tests/test_medium_lane_intent_event_live.py:33-70,116-175 (Muster _OneShotTrigger, _seed_call, _StopLoop, monkeypatch-Naehte)
    - tests/conftest.py (wo eine ENV-Var VOR `import app` gesetzt werden kann — Muster fuer S1)
    - RESEARCH.md §5 (Mock-Naehte, MERGE_WINDOW_S-Patch, /api/beenden VOR disconnect, anonymize als Identity)
    - CONTEXT.md §specifics (D-10: Voll-Test erst mit letzter Welle gruen)
  </read_first>
  <action>
    a) **app.py ENV-basierter TESTING-Guard (S1, Falle b):** Umschliesse die 3 Daemon-Starts (app.py:2214-2215 analyse_loop/coaching_loop + :2222 slow_lane_consumer) mit einem **ENV-Check, NICHT app.config**:
    ```python
    import os as _os_guard
    if _os_guard.environ.get('NERVE_TESTING') != '1':
        # ... die 3 Daemon-Starts ...
    ```
    **Grund (S1, kritisch):** die Daemons starten bei `import app` auf MODUL-Ebene (app.py:2214-2223). `app.config['TESTING']` kann erst NACH `import app` gesetzt werden → zu spaet, der Guard waere wirkungslos, der Test-Client-Import weckt die echten Daemons → blind-gruen/Flakes. Eine ENV-Var (`NERVE_TESTING=1`) wird in conftest.py (bzw. per `pytest`-Setup) VOR dem `import app` gesetzt → greift zum Import-Zeitpunkt. Der Concurrency-Test setzt `os.environ['NERVE_TESTING']='1'` VOR dem App-Import. Prod: ENV nicht gesetzt → Daemons laufen normal (kein Verhaltens-Change).

    b) **Concurrency-Test-Skeleton `tests/test_persid_concurrency.py`:** Lege die Datei mit Fixtures + Mock-Naehten + ZWEI Test-Client-Setup an, aber die eigentlichen Isolations-Assertions bleiben als `pytest.skip("Voll-Assertions landen mit Welle E — D-10")`. WICHTIG (D-10): Der Test darf NICHT rot ins deploy.sh-Gate — als Skeleton mit skip ist er gruen/geskippt. Voll-scharf wird er in Plan 06 (Welle E).

    Skeleton-Inhalt (aus RESEARCH §5):
    - `NERVE_TESTING=1` ENV VOR `import app` setzen (S1) — als allererstes im Modul/conftest.
    - `_two_clients(app)`: `socketio.test_client(app)` x2, je eigene Flask-Session (User A Org 1 / User B Org 2).
    - Mock-Naehte als Fixtures/monkeypatch-Helfer: `_open_deepgram_connection` patchen; `analysiere_mit_claude`/Haiku → _fake; `anonymize`/`anonymize_output`/`anonymize_for_storage` als IDENTITY (`lambda t, c=None: (t, 'identity')`) — PFLICHT (Falle a, sonst Vacuous-Green); `_qa_pipeline_dispatch` → None; `MERGE_WINDOW_S` → 0.05 (Falle c); `_OneShotTrigger` fuer analyse_trigger.
    - Reihenfolge-Kommentar: `/api/beenden` VOR socket-disconnect (Falle d). **HINWEIS (B1-Vorgriff):** api_beenden ist eine HTTP-Route (app_routes.py:99) ohne Socket-sid; der disconnect-Handler (deepgram_service.py:806-815) poppt _session_state[sid]. Der Test MUSS beenden VOR disconnect aufrufen. Plan 06 haengt hieran den B1-Snapshot-Test (Late-Write-Ghost-Drop).
    - `_seed_call(db)` uebernehmen (FK-Ziel fuer intent_event.call_id).
    - Docstring: nennt die 7 GEPAARTEN Assertion-Ziele + Reset-Ueberlebens-Check, die Welle E scharf schaltet.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Nach Deploy: der Skeleton-Test ist gruen/geskippt im Gate (nicht rot). app.py-Daemons laufen im Prod normal (NERVE_TESTING nicht gesetzt).</manual>
  </verify>
  <acceptance_criteria>
    - **S1: app.py:2214-2215 + :2222 sind mit `if os.environ.get('NERVE_TESTING') != '1':` umschlossen (ENV, NICHT app.config)**
    - `grep -n "NERVE_TESTING" app.py` → Treffer um den Daemon-Start-Block; `grep -n "NERVE_TESTING" tests/test_persid_concurrency.py` (und/oder conftest) → wird VOR `import app` gesetzt
    - tests/test_persid_concurrency.py existiert, >=40 Zeilen, mit allen Mock-Naht-Fixtures + _two_clients-Helfer
    - Test ist gruen/geskippt (KEIN rotes Gate, D-10)
    - Docstring listet die 7 gepaarten Assertion-Ziele fuer Welle E
    - deploy.sh production GRUEN
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Client → App-Import | `import app` im Test darf keine echten Deepgram/Haiku-Daemons wecken (ENV-TESTING-Guard, S1) |
| Gelöschte Globale → neuer Code | Punkt-29-Falle: neuer Code darf kein deprecated Muster kopieren (Waechter fängt es) |
| Legit unmigrierter Schreiber → Gate | B3: ein noch-legitimer Live-Schreiber darf den Waechter nicht rot faerben (Pending-Liste), aber die Liste muss bis Plan 06 leer sein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PERSID-01 | Information Disclosure | state['ergebnis'/'active_hint'/'ewb_buttons'] Zombie-Globale | mitigate | DELETE (0 Reader kann nicht leaken) — reconciled HIGH-Drift 1 |
| T-PERSID-02 | Tampering | Neuer Schreib-Zugriff auf deprecated/pending Global | mitigate | test_no_live_global_state.py Test 3 macht jeden NEUEN Write rot (Punkt 29); Pending-Liste nur fuer die dokumentierte Legacy-Stelle |
| T-PERSID-03 | Denial of Service | import app weckt echte Daemons im Test | mitigate | app.py ENV-TESTING-Guard (S1 — app.config zu spaet) |
| T-PERSID-17 | Tampering | Waechter still aufgeweicht/vacuous weil in Welle 0 rot | mitigate | B3: wave-getaggte Pending-Liste haelt ihn ehrlich gruen ohne Aufweichung; Plan 06 assertet Liste==leer |
</threat_model>

<verification>
- deploy.sh production nerve_test-Gate gruen nach jedem Task
- test_no_live_global_state.py gruen im Gate (Zombies weg, Whitelist + Pending-Liste halten — B3)
- test_persid_concurrency.py gruen/geskippt (Skeleton, D-10)
- grep-Beleg: 0 aktive Defs/Writer der geloeschten Zombie-Globalen (inkl. is_paused/_line_id_counter — S5)
</verification>

<success_criteria>
- Zombie-Global-Familien geloescht/DEPRECATED-markiert mit grep-Beleg (inkl. S5-Modul-Reste)
- Statischer Waechter gruen im Gate, Whitelist + wave-getaggte Pending-Liste dokumentiert (B3)
- app.py ENV-basierter TESTING-Guard aktiv (S1)
- Concurrency-Skeleton + Mock-Naehte angelegt (Voll-Assertions → Welle E)
- 3 UNKLAR-Globale per grep entschieden; aktive_phase_idx = FIX (B5)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-01-SUMMARY.md`
</output>
</content>
</output>
