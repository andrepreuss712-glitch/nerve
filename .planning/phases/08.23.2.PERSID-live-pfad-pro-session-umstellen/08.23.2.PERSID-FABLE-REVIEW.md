# Fable-Audit 4 — PERSID-Bauplan (6 Wellen) gegen HEAD, 2026-07-03

> Externe 3. Sicht (Fable, Anthropic-Spitzenmodell, read-only am ECHTEN Code) auf die 6 fertigen Pläne.
> Ersetzt den Gemini-`/gsd-review`-Pfad (bei uns unzuverlässig). Cross-AI-Pflicht bei 🔴 (CLAUDE.md Punkt 7/24 + Vorgehens-Prinzip Teil B).
> Gesamturteil: Wellen-Gerüst (D-07/D-08) für die schweren Familien richtig, F2-Kette sauber — ABER 5 Blocker + mehrere Should-Includes. Ohne die Blocker: neue stille Datenverluste am heißesten Code.

## BLOCKER (müssen vor Execute in den Plan)

**B1 — F-F disconnect-vor-beenden (KRITISCH, im Plan verloren gegangen).**
`handle_disconnect` (deepgram_service.py:805-815) poppt den kompletten per-sid-State; `api_beenden` liest ihn danach. Reißt die Verbindung VOR dem Beenden-Klick (Netz-Blip, PiP/Tab zu, dann Beenden) → Scan leer → `_beenden_sid=None` → leerer Call-Record. Heute überleben die Daten in den Globalen — die Migration tauscht Vermischung gegen **Datenverlust**. Die eigenen Code-Kommentare belegen, dass diese Race real auf Prod beobachtet wurde (app_routes.py:166-171). §2c F-F fordert das explizit als KRITISCH — **kein einziger der 6 Pläne enthält es** (nur als Test-Gotcha herabgestuft = stille Scope-Erosion).
→ Fix: `handle_disconnect` legt den State in einen kurzlebigen `_ended_session_snapshots[sid]` (TTL wenige Minuten) statt sofortigem pop; `api_beenden` konsumiert bevorzugt den Snapshot. Plus Test für das gewollte Ghost-Drop der Late-Writes (record_ewb_click/record_suggestion_offer nach Stream-Ende).

**B2 — Familie A / Anrede-Kollaps (Plan 03 verletzt die eigene eiserne Regel).**
`session_anrede` liegt an DREI Stellen: (1) global `ls.state['session_anrede']` — Writer deepgram:618-619, Leser app_routes:340 + prompt_pipeline:507-518 (Fallback in `_resolve_anrede`); (2) per-sid TOP-LEVEL — Writer manueller Du/Sie-Toggle deepgram:825-827, Leser deepgram:511 + prompt_pipeline:451 + :500; (3) per-sid state-SUBKEY — Seed live_session:356, Leser claude:1481, nie beschrieben. Plan 03 schreibt in den SUBKEY + biegt nur app_routes:340 um → globaler Writer :619 stirbt → Prompt-Anrede-Fallback liest für immer tot → **Start-Anrede erreicht den Prompt nie mehr** (Regression); manueller Toggle landet weiter nie im gespeicherten `ConversationLog.anrede` (+ Groß/klein-Drift 'Du'/'du').
→ Fix: die drei auf EINE Stelle kollabieren (inkl. Case-Normalisierung), ALLE 5 Leser (app_routes:340, deepgram:511, prompt_pipeline:451, :500, :514-Fallback löschen, claude:1481) + BEIDE Writer (deepgram:619 Start, :827 Toggle) im SELBEN Deploy.

**B3 — Statischer Wächter ist wie spezifiziert nicht deploybar (Gate-Blocker).**
Plan 01 committet den AST-Sweep grün in Welle 0. Der Sweep („jede `ls.*`-Zuweisung nicht-per-sid + nicht-Whitelist → rot") trifft die in Welle 0 noch LEGITIMEN unmigrieren Schreiber (deepgram:619/641-642/856, :71, :77; claude:1362) → Wächter ROT ab Welle 0 → blockiert per D-10 jedes Deploy bis Welle 5, ODER Executor weicht ihn still auf (= vacuous). Zudem schwächer als D-04/§2c-Weiche-2 Schicht 3: fängt keine Modul-Level-Bindungen in deepgram/claude/matcher und nichts INNERHALB live_session.py (bare Namen, keine `ls.`-Attribute).
→ Fix: pro Welle schrumpfende, dokumentierte PENDING-MIGRATION-Whitelist (Welle-getaggt, leer ab Plan 06). Sweep-Reichweite auf Modul-Level-Bindungen der 4 Live-Module heben (mind. als Plan-06-Finalisierung).

**B4 — record_*-Caller unvollständig (Plan 06 — „grün aber still tot"-Klasse).**
`record_suggestion_offer` hat 3 Caller, nicht 1: deepgram:1017, **einwand_keyword_matcher.py:338 (LIVE — Slot-A-Keyword-Pfad, Meeting)**, claude:722 (dormant). `record_ewb_click` hat 3: deepgram:1006, **:1026**, **:1041** (Error-Pfade). Plan 06 nennt nur :1006/:1017. Signatur wird sid-pflichtig + nur zwei angepasst → matcher:338 wirft TypeError → vom except geschluckt → **Slot-A-Keyword-Vorschläge gehen still verloren**.
→ Fix: alle Caller (matcher:338, claude:722, deepgram:1026, :1041) in die Task-Liste + grep-Vollständigkeits-Check analog Plan 05.

**B5 — `aktive_phase_idx` zweiter Leser übersehen → AttributeError im Prompt-Bau.**
claude_service.py:205-206 (`with ls.phase_lock: idx = ls.aktive_phase_idx`, ohne try/except) liest die Globale im Prompt-Builder. Plan 01/05 nennen nur app_routes:244. Globale + phase_lock gelöscht → **AttributeError bei jedem Prompt-Bau → Analyse-Pfad live kaputt**.
→ Fix: claude:206 explizit ins DELETE/FIX-Verdikt; bei DELETE Reader auf per-sid `state['current_phase']`/Literal umbiegen + phase_lock dort entfernen.

## SOLLTE REIN

- **S1 — TESTING-Guard ENV-basiert** (conftest setzt ENV vor App-Import), NICHT `app.config.get('TESTING')` — Daemons starten bei `import app` auf Modul-Ebene (app.py:~2214-2223), config kann vorher nicht gesetzt sein → Guard wirkungslos → Test weckt echte Daemons → blind-grün.
- **S2 — Einmaliger idempotenter Sweep für bestehende hängende Prod-Calls** (`ended_at IS NOT NULL AND resolved-Flags False → True`) von „optional" auf Pflicht — der Inline-Heiler repariert nur künftige Fälle; die schon hängenden bleiben sonst. Kein periodischer Timer (Punkt 22/26).
- **S3 — `_beenden_sid` via posted `call_id` exakt auflösen** (Muster existiert schon app_routes:196-201) statt/vor `max(session_start_time)` — entschärft „ein User zwei Sessions" ab Welle 2 statt erst Welle 5.
- **S4 — RMW-Pflichtliste aus §2c wörtlich in Plan 05:** update_kaufbereitschaft clamp+append EIN Block (live_session:857-860); kb-Sequenz claude:1071-1083 in EINEN Block oder Snapshot-Semantik dokumentieren; gegenargument finalize+append (claude:1107-1135 + app_routes:286-292). Plus **Plan 04 auf EIN Merge-Lock festnageln** (Task 1a `_merge_lock` vs. 1b `_session_state_lock` widersprechen sich → neue Race-Gefahr).
- **S5 — `is_paused`/`pause_lock` (live_session:41-42) + `_line_id_counter`/`_line_id_lock` (:94-95) in die Welle-0-Kill-Liste** — stehen in keiner Kill-Liste → Req-7-Acceptance („keine modul-globale Live-Zustands-Variable bleibt übrig") formal verfehlt.
- **S6 — `_ewb_ttft_history`/`_ewb_fallback_until` (claude:18-19) Whitelist-Entscheidung treffen** (tenant-neutral whitelisten ODER per-sid) — Cross-Session-Verhaltens-Kopplung; §2c-Weiche-2 nennt es als den einen echten Entscheidungsfall. Vorher prüfen ob der einzige Konsument (schlafender Auto-Pfad deepgram:268) lebt.
- **S7 — models.py:791 COMMENT** um `not_gradable` ergänzen (heute pending|scored|abstained|failed — R3-Doc-Drift).

## OK (verifiziert, kein Handlungsbedarf)
- Prüfpunkt 2 — conversation_log 6 Schreiber + Leser vollständig, keine Alias-Schreiber.
- Prüfpunkt 4 — F2-Kette zeilenweise sauber (Idempotenz-Skip, terminaler Write persistiert via Consumer-Commit, _pending_events drainet, compute_rubric kein Div/0, Judge unabhängig).
- Prüfpunkt 8 — DELETE-Zombies riskante Verdikte nachgegreppt + bestätigt; 3 UNKLAR sauber als blockierender Checkpoint.
- Vacuous-Green — anonymize-Identity-Patch + gepaarte Assertions korrekt (bis auf S1).

## NICHT am Code verifiziert (Rest-Unsicherheit)
- Inhalt test_abstain_log_rls.py (nur Existenz), suggestion_capture.py-Flush + judge_runner.py vollständig, Prod-DB-Zustand (kein SSH im read-only-Auftrag).

## Fable-Meta-Befund
„Familie A = leichteste" war falsch (verwinkeltste); RESEARCH §2 hat die prompt_pipeline-Leser + Toggle-Writer nicht gegreppt obwohl §2c F-B sie namentlich nennt. „Wächter grün in Welle 0" = mit spezifizierter Strenge unmöglich. record_*: grep hätte in 30s 5 weitere Stellen gezeigt (Punkt-20-Klasse). F-F von KRITISCH zu Test-Gotcha herabgestuft = stille Scope-Erosion des eigenen Audit-Befunds.
