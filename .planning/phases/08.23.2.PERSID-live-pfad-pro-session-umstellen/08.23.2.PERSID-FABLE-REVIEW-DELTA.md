# Fable-Audit 4b — Zweiter Pass (NUR Deltas), 2026-07-03

> Gezielter Delta-Review nach dem Einarbeiten der 5+1 Blocker. Read-only am ECHTEN Code (deepgram_service, live_session, claude_service, prompt_pipeline, app_routes, einwand_keyword_matcher, pip-launcher.js, Tests).
> **Verdikt: KEINE Execute-Freigabe.** 4 neue Blocker (N-1..N-4), alle „grün-aber-still-tot"/Reihenfolge-Klasse. Kleine, präzise Plan-Edits — danach Freigabe realistisch.
> Das eingearbeitete Gerüst (B1-Zwei-Naht-Stash, B2-Kollaps inkl. claude:1481, B3-Liste, B4/B5) ist am Code korrekt verankert — die neuen Blocker sitzen eine Schicht tiefer im selben Delta-Code.

## NEUE BLOCKER (müssen vor Execute in den Plan)

**N-1 — Doppel-Feuer-Race überschreibt vollen Snapshot mit LEEREM (Plan 03 Task 1a/1b).**
`handle_disconnect` beginnt mit setdefault-Guard `deepgram_service.py:810-811` (`_session_state[sid] = {}` falls fehlend, UNCONDITIONAL, vor der Stash-Zeile). Ablauf: `:779`-Stash (voll) poppt die sid → Tab-Zu/PiP-Schließen direkt nach Beenden → `handle_disconnect` feuert → setdefault erzeugt `{}` → Stash findet die sid → **überschreibt `_ended_session_snapshots[sid]` mit der leeren Kopie** → api_beenden konsumiert LEER → leerer Call-Record. Die Plan-Idempotenz-Prämisse „zweiter Stash findet nichts → No-Op" ist in genau diesem Handler falsch.
→ Fix: Stash überspringt leeren/fehlenden State (`if not _session_state.get(sid): return`) ODER first-stash-wins auf dem Snapshot-Dict. Plus Doppel-Feuer-Assertion in Plan 06 (stop→disconnect → Snapshot bleibt voll).

**N-2 — Zwei Direkt-Reads in api_beenden in KEINER Rewire-Liste (Plan 03/05).**
`app_routes.py:172-177` (Redeanteil/Tempo/Monolog/session_start_time) + `:193-207` (word_confidences + call_id-Lookup) lesen `_session_state` DIREKT. Sobald der :779-Stash live ist und das Socket-Event vor dem HTTP-Request verarbeitet wird → beide lesen None/[] → **Redeanteil/Tempo/Monolog = 0 bei JEDEM normalen Auflegen**; word_confidences=[] → Audio-Health-Thread startet nie → Plan-02-Heiler setzt still `audio_health_resolved=True` auf leerer Basis. Die Blöcke tragen „K1-Fix"/„Phase-D früh"-Kommentare → SEHEN gefixt aus (die Klasse, die zweimal durchrutschte). Blanket-Acceptance würde es bei ehrlichem grep fangen, aber keine Task-Liste enumeriert sie.
→ Fix: beide Blöcke explizit auf den Snapshot-Pfad (`_bs`) umziehen — namentlich in Plan 03 Task 1c oder Plan 05 Task 2a.

**N-3 — destruktiver Ein-Schuss-Konsum × verteilte Helfer-Aufrufe (Plan 03 Task 1c, bindend für 05+06).**
`consume_ended_session` ist als POP (einmalig) spezifiziert. Die Reads verteilen sich über ~250 Zeilen und DREI Pläne (03: :110/:340; 05: :143-149/:241-244/:286-294; 06: :324/:327), jeder „via `_load_beenden_state(...)`". Ruft der Executor den Helfer pro Stelle → ZWEITER Aufruf liefert None → alle späteren Felder still leer, Tests grün (testen einen Aufruf).
→ Fix festnageln: GENAU EIN `_bs = _load_beenden_state(...)` am Kopf von api_beenden, `_bs` durchgereicht (auch an `_build_log_content`); `consume` = nicht-destruktiver PEEK; Aufräumen via TTL + expliziter Snapshot-Pop am Ende (in `reset_session(_beenden_sid)`). Doppel-Beenden bleibt so gutartig wie heute (objection_bridge/suggestion_capture konvergieren idempotent).

**N-4 — Start-Anrede-Write läuft VOR init_session_state → Anrede erneut verloren (Plan 03 Task 2a).**
`deepgram_service.py:617-620` liegt ~58 Zeilen VOR `init_session_state` (:675), und init überschreibt den kompletten per-sid-Dict (`live_session.py:306`). Plan-Snippet (in-place-Ersatz + Ghost-SID-Guard): frische Session → sid noch nicht da → Guard droppt still; Reconnect → Write gelingt, wird von :674-Pop+Init weggewischt. **Die B2-Kern-Regression bleibt — nur grüner.** Der Plan nennt „nach init prüfen" nur als Nebensatz, das Snippet widerspricht ihm.
→ Fix: Anrede in ein Local capturen, per-sid-Write HINTER :675 verschieben (Muster von `session_start_time` :687-690 + Briefing-Bridge :713-714, die aus demselben Grund nach init sitzen). Test gegen den ECHTEN Handler — NICHT gegen `test_ewb_rate_api._apply_anrede_whitelist` (Prod-Replika → würde den Bug grün testen).

## SCHWÄCHEN (sollten rein)
- **W-1 — Faktenfehler im Plan (doppelt):** Seed `'session_anrede': None` (:356) + `'mic_muted': False` (:351) liegen im **'state'-SUBDICT**, NICHT top-level wie Plan 03 zweimal behauptet. Mechanik überlebt (.get()-Defaults), aber die toten Subdict-Seeds bleiben als künftige Ebenen-Falle → mit löschen bzw. top-level nachziehen.
- **W-2 — Test-Bestand fehlt in files_modified:** `test_ewb_rate_api.py:292-410` (vacuous Global-Anrede-Replika, assertet title-case 'Du') + `test_suggestion_reactions.py:205/234` (alte record_*-Signatur → Gate rot) in die betroffenen Pläne aufnehmen.

## OK (verifiziert)
- B1(a) stop_live_session feuert beim Normal-Hangup (pip-launcher.js:3094→3034→3106, Socket bleibt verbunden); nur EIN Emitter/beenden-Caller im Frontend. (b) Reconnect-Pop :674 zu Recht exempt. (d) TTL/Wachstum ok, `_ended_session_snapshots` sid-gekeyt + in Wächter-Whitelist = kein versteckter Global.
- B2: alle 5 Leser + 2 Writer zeilengenau, im selben Deploy; claude:1481-Nebenfund stimmt exakt; Case-Norm nötig (:514 vergleicht lowercase) + an EINER Stelle.
- B3: schrumpfende _PENDING_MIGRATION trägt, Plan 06 assertet frozenset() (leer). Schmaler Rest: (Datei,Muster)-Matching maskiert einen zweiten neuen Schreiber desselben Musters bis zu dessen Welle — ggf. Count/Zeile ins Tupel.
- B4: 6 record_*-Caller bestätigt, keinen siebten. B5: 2 Leser (claude:205-206, app_routes:243-244) im FIX-Verdikt, phase_lock-Entfernung gedeckt.

## REST-RISIKO / beim Execute im Auge behalten
- Toter Subdict-Seed :356/:351 nach Migration löschen (W-1).
- **Vorwissen-Beifang (pre-existing, außerhalb Scope):** `vorwissen_level` (deepgram:625-628) wird von init genauso weggewischt wie N-4 — existiert schon heute live → Aufräum-Liste.
- Letztes Merge-Fenster: Stash cancelt `_merge_pending`-Timer (Plan 04) → letzte ≤MERGE_WINDOW_S Transkript beim Auflegen entfallen deterministisch. Klein; bewusst akzeptieren oder beim Stash flushen.
- Reader-Defaults case-gemischt ('Sie' claude:1481 vs 'sie' deepgram:511) — kosmetisch.

## NICHT verifiziert (kein SSH/Prod, read-only)
Prod-DB-Zustand; Laufzeit-Reihenfolge Socket-Event vs HTTP (N-2-Häufigkeit am Code plausibilisiert, nicht gemessen — eventlet garantiert die Reihenfolge stop→beenden per Design NICHT, darum zählt N-3 unabhängig); `_close_deepgram_connection`-Innenleben.
