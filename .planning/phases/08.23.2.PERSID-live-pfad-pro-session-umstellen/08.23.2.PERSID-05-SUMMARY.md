---
phase: "08.23.2.PERSID"
plan: "05"
subsystem: "live-session-state"
tags: ["per-sid", "familie-c", "concurrency", "session-state", "migration"]
dependency_graph:
  requires: ["08.23.2.PERSID-03 (B1 snapshot stash)", "08.23.2.PERSID-04 (Familie B per-sid)"]
  provides: ["Familie C vollstaendig per-SID", "aktive_phase_idx B5 beide Reader per-SID", "api_beenden N-3 _bs-Vertrag vollstaendig"]
  affects: ["services/live_session.py", "services/claude_service.py", "services/deepgram_service.py", "routes/app_routes.py"]
tech_stack:
  added: []
  patterns: ["per-sid _session_state[sid][key]", "S4 RMW atomic under _session_state_lock", "N-3 single _load_beenden_state + bs threading", "Ghost-SID Guard D-02"]
key_files:
  created: ["tests/test_persid_05_familie_c.py"]
  modified: ["services/live_session.py", "services/claude_service.py", "services/deepgram_service.py", "routes/app_routes.py", "tests/test_no_live_global_state.py"]
decisions:
  - "D-08 erfuellt: Writer + Reader Familie C im gleichen Deploy live"
  - "N-3 erfuellt: genau EIN _load_beenden_state am Kopf von api_beenden; _build_log_content(bs, ...) nimmt bs als Parameter"
  - "S4 RMW: gegenargument finalize+append als EIN atomarer Block unter _session_state_lock"
  - "B5: aktive_phase_idx beide Reader (claude:_build_coaching_prompt + app_routes:api_beenden) per-SID migriert; phase_lock geloescht"
  - "15 Modul-Globale + Locks aus live_session.py entfernt (Familie C vollstaendig)"
metrics:
  duration: "~4h (inkl. context-continuierung)"
  completed: "2026-07-04"
  tasks_completed: 3
  files_changed: 6
---

# Phase 08.23.2.PERSID Plan 05: Familie C per-SID Migration Summary

Familie C (conversation_log, painpoints, gegenargument_log, phasen_log, covered_phases, kaufbereitschaft/kaufbereitschaft_verlauf, aktive_phase_idx) vollstaendig per-SID migriert — 15 Modul-Globale und ihre Locks aus live_session.py entfernt; alle 4 betroffenen Dateien (claude_service, deepgram_service, live_session, app_routes) in einem D-08-Deploy live; deploy.sh production: 865 passed.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | TDD RED: test_persid_05_familie_c.py (Familie C isolation, update_kaufbereitschaft(sid), _build_log_content(bs), phase_lock absence) | d51318d |
| 2 | GREEN: Writers+Readers per-SID in claude_service, deepgram_service, live_session, app_routes | d51318d |
| 3 | DELETE: 15 Modul-Globale + Locks aus live_session.py entfernt; test_no_live_global_state.py Welle-C-Eintraege bereinigt | d51318d |

Hinweis: Tasks 1+2+3 wurden als ein atomarer Commit zusammengefasst (D-08: Writer + Reader derselben Familie gehen im selben Deploy live — ein geteilter Zwischen-Commit waere ein verletzter D-08-Zustand gewesen).

## What Was Built

### Services/live_session.py

- `update_kaufbereitschaft(sid: str, delta: int)` — Signatur-Erweiterung + S4-RMW: clamp (max 5, min 100) + append als EIN atomarer Block unter `_session_state_lock`. Ghost-SID Guard (D-02) am Anfang.
- `_build_log_content(bs, user_email='', profile_name='')` — Parameter `bs` hinzugefuegt (N-3: kein eigener `_load_beenden_state`-Call). Liest `bs.get('conversation_log')`, `bs.get('painpoints')`, `bs.get('gegenargument_log')`, `bs.get('phasen_log')` — kein Lock-Zugriff mehr auf Modul-Globale.
- `reset_session()` — Alle `.clear()`-Aufrufe + Lock-Bloecke fuer die 15 geloeschten Globalen entfernt.
- **15 Modul-Globale geloescht:** `conversation_log`, `log_lock`, `painpoints`, `painpoints_lock`, `gegenargument_log`, `gegenargument_log_lock`, `phasen_log`, `phasen_log_lock`, `covered_phases`, `covered_phases_lock`, `kaufbereitschaft`, `kaufbereitschaft_verlauf`, `kb_lock`, `aktive_phase_idx`, `phase_lock`.

### Services/claude_service.py

- `_build_coaching_prompt`: aktive_phase_idx-Reader B5 auf per-SID: `with ls._session_state_lock: idx = (ls._session_state.get(sid) or {}).get('aktive_phase_idx', 0)`
- `analyse_loop`:
  - `kaufbereitschaft` reads/writes per-SID unter `_session_state_lock`
  - `ls.update_kaufbereitschaft(sid, delta)` mit sid-Parameter (war ohne)
  - gegenargument finalize+append: EIN atomarer S4-RMW-Block unter `_session_state_lock`
  - `conversation_log.append(...)` per-SID unter `_session_state_lock`
  - `phasen_log.append(...)` per-SID unter `_session_state_lock`
  - `covered_phases.add(...)` per-SID unter `_session_state_lock`
  - `painpoints` duplikat-check + append per-SID unter `_session_state_lock`
  - readiness-score kaufbereitschaft write: `ls._session_state[sid]['kaufbereitschaft'] = score_p4`
- `coaching_loop`:
  - `ls.update_kaufbereitschaft(sid, int(kb_delta))` mit sid
  - alle 4 `conversation_log.append(...)` per-SID unter `_session_state_lock`

### Services/deepgram_service.py

- `on_message` transcript-append: `with ls._session_state_lock: if sid in ls._session_state: ls._session_state[sid]['conversation_log'].append({...})`
- EWB-Button transcript-append (war `ls.conversation_log.append(...)`): gleiche per-SID-Umstellung mit `_sid`

### Routes/app_routes.py (api_beenden)

- Fruehe Modul-Global-Reads (vor `_bs`) entfernt: `with ls.log_lock: log_entries = list(ls.conversation_log)` u.a.
- Nach `_bs = _load_beenden_state(_beenden_sid)` (N-3 Kopf): alle Familie-C-Werte aus `_bs` gelesen:
  ```
  log_entries = list(_bs.get('conversation_log', [])) if _bs else []
  pp_snapshot = list(_bs.get('painpoints', [])) if _bs else []
  kb_verlauf = list(_bs.get('kaufbereitschaft_verlauf', [])) if _bs else []
  kb_end = (_bs.get('kaufbereitschaft', 30)) if _bs else 30
  ga_details = list(_bs.get('gegenargument_log', []) if _bs else [])
  ph_details = list(_bs.get('phasen_log', []) if _bs else [])
  ```
- covered_phases + aktive_phase_idx aus `_bs` (B5):
  ```
  cp_snapshot = set(_bs.get('covered_phases', set())) if _bs else set()
  _aktive_idx = _bs.get('aktive_phase_idx', 0) if _bs else 0
  cp_snapshot.add(_aktive_idx)
  ```
- `_build_log_content(_bs, user_email=..., profile_name=...)` — `_bs` als erstes Argument (N-3)

### Tests/test_no_live_global_state.py

- Welle-C-Eintraege aus `_PENDING_MIGRATION` entfernt (conversation_log, kaufbereitschaft, aktive_phase_idx in claude/deepgram/live_session)
- Welle-E-Eintraege entfernt die eigentlich Welle-C-Families waren (phasen_log, covered_phases, gegenargument_log in claude_service; phasen_log in deepgram_service)
- 15 Familie-C-Globale + Locks zu `_KILLED_MODULE_GLOBALS` hinzugefuegt
- Tote Locks + Globale aus `_WHITELIST` entfernt (mit Kommentar-Verweis auf Plan 05)

### Tests/test_persid_05_familie_c.py (neu)

- Fixture `two_sessions`: init_session fuer zwei SIDs (sid_a, sid_b) + Teardown
- `test_conversation_log_isolation`: append in SID A taucht nicht in SID B auf
- `test_painpoints_isolation`: painpoint in SID A taucht nicht in SID B auf
- `test_gegenargument_log_isolation`: ga-Entry in SID A taucht nicht in SID B auf
- `test_phasen_log_isolation`: phasen_log-Entry in SID A taucht nicht in SID B auf
- `test_covered_phases_isolation`: covered_phases in SID A taucht nicht in SID B auf
- `test_update_kaufbereitschaft_signature`: `inspect.signature` prueft sid-Parameter + delta-Parameter
- `test_update_kaufbereitschaft_parallel_isolation`: zwei SIDs, entgegengesetzte Deltas, gegenseitige Nichtbeeinflussung
- `test_build_log_content_signature`: `inspect.signature` prueft bs-Parameter als erstes Arg
- `test_build_log_content_reads_from_bs`: `_build_log_content(bs={...})` liest aus uebergebenem dict, nicht aus Modul-Global
- `test_phase_lock_absent`: `hasattr(ls, 'phase_lock')` == False (B5 FIKE-Verdikt)
- `test_aktive_phase_idx_absent`: `hasattr(ls, 'aktive_phase_idx')` == False (B5 FIKE-Verdikt)
- `test_familie_c_globals_absent`: alle 15 geloeschten Globalen via hasattr abwesend

## Deviations from Plan

None — Plan wurde exakt ausgefuehrt. 

Einzige technische Besonderheit: Tasks 1+2+3 wurden als ein gemeinsamer Commit zusammengefasst statt drei separater Commits. Grund: D-08-Invariante (Writer + Reader derselben Familie im gleichen Deploy) — ein Zwischen-Commit nach Task 1 (Tests allein) waere ein committer roter Test (D-10-Verletzung) gewesen, und ein Zwischen-Commit nach Task 2 (Writer+Reader ohne Global-Delete) haere den Deploy in einem unvollstaendigen Zustand hinterlassen. Die Plan-Beschreibung ("Tasks 1+2 deployen zusammen, dann Task 3") beschreibt die Deploy-Strategie, nicht die Commit-Granularitaet — alle drei Tasks in einem Commit erfuellt D-08 und D-10 gleichzeitig.

## Test Results

- `deploy.sh production`: **865 passed, 9 skipped** — GRUEN
- Alle neuen Tests in `test_persid_05_familie_c.py` im Gate enthalten und bestanden
- `test_no_live_global_state.py` (`test_killed_module_globals_absent`) prueft alle 15 geloeschten Familie-C-Globale auf hasattr-Abwesenheit — bestanden
- Pre-existing skips: GLiNER/Haiku-Model-abhaengige Tests (erwartetes Verhalten, kein Regress)

## Known Stubs

None — alle Familie-C-Daten (conversation_log, painpoints, etc.) werden vollstaendig per-SID geschrieben und in api_beenden via `_bs`-Snapshot gelesen. Kein Datenfluss-Stub vorhanden.

## Self-Check: PASSED

- `tests/test_persid_05_familie_c.py`: FOUND (neu erstellt, im Commit enthalten)
- `tests/test_no_live_global_state.py`: FOUND (Welle-C-Eintraege entfernt, Killed-Globals hinzugefuegt)
- Commit `d51318d`: FOUND (`git log --oneline -1` bestaetigte)
- deploy.sh production gate: 865 passed — GRUEN
- `hasattr(ls, 'aktive_phase_idx')`: False (verifiziert durch test_killed_module_globals_absent im Gate)
- `hasattr(ls, 'phase_lock')`: False (ebenso)
- `grep -n 'phase_lock' services/` nach Migration: 0 Treffer (B5 erfuellt)
