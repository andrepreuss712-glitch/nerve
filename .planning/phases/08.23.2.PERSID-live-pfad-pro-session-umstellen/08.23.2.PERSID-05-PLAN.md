---
phase: 08.23.2.PERSID
plan: 05
type: execute
wave: 4
depends_on: [01, 04]
files_modified:
  - services/claude_service.py
  - services/deepgram_service.py
  - services/live_session.py
  - routes/app_routes.py
requirements: [SPEC-Req-3, SPEC-Req-7]

must_haves:
  truths:
    - "Der bei /api_beenden gebaute Call-Record wird aus dem Session-Bucket (lebend ODER Snapshot, B1) gebaut, nicht aus Modul-Globalen"
    - "conversation_log, painpoints, gegenargument_log, phasen_log, covered_phases, kaufbereitschaft(_verlauf), aktive_phase_idx sind pro sid — Schreiber (claude/deepgram) + Leser (api_beenden + claude:206) im SELBEN Deploy"
    - "aktive_phase_idx-BEIDE-Reader (app_routes:244 + claude:206) sind per-sid; phase_lock ist entfernt — kein AttributeError im Prompt-Bau (B5)"
    - "Die RMW-Hotspots (update_kaufbereitschaft clamp+append, kb-Sequenz, gegenargument finalize+append) sind je EIN atomarer per-sid-Block ODER dokumentierte Snapshot-Semantik (S4)"
    - "ALLE api_beenden-Reads dieser Familie gehen ueber das EINE `_bs = _load_beenden_state(...)` vom Kopf (Plan 03 Task 1c, N-3); `_build_log_content` nimmt `_bs`/den State als Param (kein zweiter destruktiver Consume)"
  artifacts:
    - path: "routes/app_routes.py"
      provides: "api_beenden liest alle ~7 Familien-Quellen pro sid aus dem EINEN `_bs` (Snapshot-faehig, N-3)"
    - path: "services/live_session.py"
      provides: "_build_log_content(_bs/sid) + update_kaufbereitschaft(sid, delta) per-sid; RMW atomar"
    - path: "services/claude_service.py"
      provides: "kb-Sequenz + gegenargument finalize + aktive_phase_idx-Reader (:206) per-sid; phase_lock weg"
  key_links:
    - from: "claude_service.py:1077"
      to: "_session_state[sid]['conversation_log']"
      via: "per-sid append unter _session_state_lock"
      pattern: "conversation_log"
    - from: "claude_service.py:206"
      to: "_session_state[sid]['aktive_phase_idx']"
      via: "per-sid Read im Prompt-Bau (phase_lock entfernt, B5)"
      pattern: "aktive_phase_idx"
    - from: "app_routes.py (das EINE _bs vom Kopf, N-3)"
      to: "_bs.get('conversation_log') + _build_log_content(_bs, ...)"
      via: "per-sid Read in api_beenden (Snapshot-faehig, B1, EIN Peek)"
      pattern: "conversation_log"
---

<objective>
**Familie C — CONVERSATION/RESULT** (die grosse Familie, SPEC Req 3 + Req 7-Teile). Der persistierte Call-Record wird heute aus ~7 Modul-Globalen gebaut, die parallele Calls teilen → der gespeicherte Datensatz kann cross-tenant vermischen.

**D-08-Beweis (KRITISCH — riskanteste Invariante):** `/api/beenden` (app_routes) liest ALLE diese Globalen; die Schreiber liegen in claude_service + deepgram_service. Ein per-sid-Leser OHNE per-sid-Schreiber ⇒ LEERER Call-Record auf Prod. Deshalb: conversation_log + painpoints + gegenargument_log + phasen_log + covered_phases + kaufbereitschaft(_verlauf) + **aktive_phase_idx (FIX, B5)** ALLE im SELBEN Deploy. Groesste Einzelwelle.

**N-3 (BINDEND aus Plan 03) — GENAU EIN `_bs` vom Kopf, durchgereicht (Fresh-Grep 2026-07-03):** Plan 03 Task 1c setzt GENAU EINEN `_bs = _load_beenden_state(_beenden_sid)` am KOPF von api_beenden; `consume_ended_session` ist ein NICHT-destruktiver PEEK. ALLE Familie-C-Reads hier lesen aus diesem EINEN `_bs` — es gibt KEINEN zweiten `_load_beenden_state`-Aufruf. Insbesondere `_build_log_content` bekommt den State/`_bs` als PARAMETER (nicht als eigenen Consume). Wuerde man den Helfer pro Read-Stelle erneut aufrufen, waere das bei einem POP fatal gewesen (zweiter Aufruf None) — mit dem PEEK (Plan 03 N-3) waere es abgefedert, aber die klare Invariante bleibt: EIN Aufruf, `_bs` threaded.

**B5 — aktive_phase_idx hat ZWEI Reader (Fresh-Grep 2026-07-03):** app_routes.py:243-244 (`with ls.phase_lock: cp_snapshot.add(ls.aktive_phase_idx)`) UND claude_service.py:205-206 (`with ls.phase_lock: idx = ls.aktive_phase_idx` im Prompt-Builder, innerhalb `if phasen:`). Plan 01 hat aktive_phase_idx als FIX (nicht DELETE) verdiktet. BEIDE Reader in DIESEM Deploy auf per-sid `_session_state[sid]['aktive_phase_idx']` (Seed :422) umbiegen + `phase_lock` (:182) entfernen. Wird claude:206 uebersehen → AttributeError bei JEDEM Prompt-Bau → Analyse-Pfad live kaputt.

**Hart gekoppelt (RESEARCH §2 Familie C):** `_build_log_content` (live_session.py:1050) liest conversation_log+painpoints+gegenargument_log+phasen_log GEMEINSAM in EINEM Aufruf → diese vier zusammen.

**S4 — RMW-Pflichtliste (Read-Modify-Write atomar halten, verbatim aus §2c, Fresh-Grep-Anker):**
1. `update_kaufbereitschaft` clamp+append: live_session.py:854-860 (`kaufbereitschaft = max(5,min(100,...+delta))` :858 → `kaufbereitschaft_verlauf.append(...)` :860) — das clamp+append muss EIN atomarer per-sid-Block unter _session_state_lock sein (kein Zwischen-Release, sonst verliert ein paralleler Tick den Wert).
2. kb-Sequenz in claude: :1072 `kb_vor_einwand = ls.kaufbereitschaft` (read) → :1075 `ls.update_kaufbereitschaft(delta)` (write) → :1083 `kb_aktuell = ls.kaufbereitschaft` (read). Diese Read-Write-Read-Sequenz entweder in EINEN per-sid-Snapshot-Block ziehen ODER die Snapshot-Semantik dokumentieren (der Zwischenwert ist ein Anzeige-Delta; wenn nur der Endwert zaehlt, reicht ein sauberer per-sid-Read nach dem update).
3. gegenargument finalize+append: claude:1107-1123 (`with ls.gegenargument_log_lock: if ls.gegenargument_log: last = [-1] mutate; ... append` :1109-1123) + app_routes-Reader (~:286-292) + _build_log_content-Reader (:1158-1159). Die finalize-last-then-append-Mutation muss EIN atomarer per-sid-Block bleiben.
Zusaetzlich der zweite kb-Schreibpfad :1362 (`ls.kaufbereitschaft = score_p4`) + :1368/:1735-1737 (weitere update_kaufbereitschaft-Caller) alle per-sid.

**_beenden_sid + Snapshot (GETEILT, Plan 03):** api_beenden nutzt den in Plan 03 festgeschriebenen Vertrag (bevorzugt exakt via posted call_id, S3) UND das EINE `_bs = _load_beenden_state(sid)` vom Kopf (B1, N-3) — jeder per-sid-Read hier liest aus diesem `_bs`, NIE direkt `_session_state`, und ruft `_load_beenden_state` NICHT erneut auf.

Purpose: Der persistierte Call-Record kann strukturell nicht mehr cross-tenant vermischen; kein AttributeError im Prompt-Bau.
Output: alle ~7 Familien-Quellen pro sid; _build_log_content sid-aware (nimmt `_bs`); api_beenden liest per-sid aus dem EINEN `_bs`; aktive_phase_idx beide Reader per-sid; RMW atomar.

**Komplexitaet: 🔴 — Cross-AI PFLICHT. GROESSTE Welle. conversation_log hat 6 Writer. D-03 Lock-Disziplin HART. Punkt 14 (RMW) + 21+26 (Persistenz-Naht) + B5.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-SPEC.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-CONTEXT.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Ziel-Sub-Keys existieren in init_session_state (live_session.py) -->
_session_state[sid]['conversation_log']    # :315 (seeded [])
_session_state[sid]['painpoints']          # :418
_session_state[sid]['gegenargument_log']   # :415
_session_state[sid]['phasen_log']          # :414
_session_state[sid]['covered_phases']      # :319 (set())
_session_state[sid]['kaufbereitschaft']    # :310
_session_state[sid]['kaufbereitschaft_verlauf']  # :320
_session_state[sid]['aktive_phase_idx']    # :422 (FIX per B5)

<!-- Writer (FRESH-GREP 2026-07-03, grep-belegt) -->
conversation_log:  deepgram_service.py:132,955 + claude_service.py:1077,1769,1804,1810 (.append)
painpoints:        claude_service.py:1791 (.append); Duplikat-Check :1775
gegenargument_log: claude_service.py:1123 (.append), :1109-1110 (mutate last, unter gegenargument_log_lock :1107)
phasen_log:        claude_service.py:1214 (.append)
covered_phases:    claude_service.py:1233 (.add)
kaufbereitschaft:  claude_service.py:1362 (=score_p4); update_kaufbereitschaft (live_session.py:854-860 clamp+append)
kaufbereitschaft_verlauf: update_kaufbereitschaft:860 (.append)
aktive_phase_idx:  (Writer grep — RESEARCH §6.5; Reader unten)

<!-- Reader (FRESH-GREP) -->
app_routes.py:144 conversation_log, :146 painpoints, :148 kb_verlauf, :149 kb_end,
  :242 covered_phases, :244 aktive_phase_idx (unter phase_lock :243), :287 gegenargument_log, :294 phasen_log
claude_service.py:205-206  with ls.phase_lock: idx = ls.aktive_phase_idx   # B5 — ZWEITER Reader (Prompt-Bau)
_build_log_content (live_session.py:1050): conversation_log+painpoints+gegenargument_log(:1158-1159)+phasen_log GEMEINSAM
claude_service.py:1072,1083,1368,1737 (kb-Feedback intern)

<!-- Zugriffsmuster D-01 (raw-dict) — ALLE Reads aus dem EINEN _bs vom Kopf (Plan 03 N-3): -->
# _bs = _load_beenden_state(_beenden_sid) steht EINMAL am Kopf von api_beenden (Plan 03 Task 1c)
log_entries = list(_bs.get('conversation_log', [])) if _bs else []
# _build_log_content bekommt _bs als PARAMETER — KEIN eigener _load_beenden_state-Aufruf

<!-- S4 RMW-Beispiel (update_kaufbereitschaft, atomar): -->
with ls._session_state_lock:
    if sid in ls._session_state:
        _st = ls._session_state[sid]
        _st['kaufbereitschaft'] = max(5, min(100, _st['kaufbereitschaft'] + delta))
        _st['kaufbereitschaft_verlauf'].append({'ts': ts, 'wert': _st['kaufbereitschaft']})
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schreiber pro sid — conversation_log + painpoints + gegenargument_log + phasen_log + covered_phases + kaufbereitschaft(RMW) + aktive_phase_idx</name>
  <read_first>
    - services/claude_service.py:1072-1083 (kb-Sequenz read-write-read, S4), :1107-1123 (gegenargument finalize+append unter Lock, S4), :1214-1233 (phasen_log/covered_phases), :1362,1368,1735-1737 (kb-Schreibpfade), :1769-1791,1804-1810 (conversation_log/painpoints)
    - services/live_session.py:854-860 (update_kaufbereitschaft clamp+append, S4-RMW), :55-71,150-195 (Modul-Globale + Locks), :314-320,414-422 (per-sid-Seeds)
    - services/deepgram_service.py:130-135,950-957 (conversation_log-Writer :132,955)
    - RESEARCH.md §2 Familie C
    - CLAUDE.md Punkt 14 (Race — 6 Writer auf conversation_log; RMW-Atomizitaet S4)
  </read_first>
  <behavior>
    - Test 1 (gepaart): zwei sids A/B; A appended conversation_log "AlphaSatz", B "BetaSatz". _session_state[A]['conversation_log'] enthaelt AlphaSatz (DA), NICHT BetaSatz (WEG). Analog painpoints/gegenargument_log/phasen_log/covered_phases.
    - Test 2 (S4 RMW): zwei parallele update_kaufbereitschaft(sidA, +5) + update_kaufbereitschaft(sidB, +3) → A und B kaufbereitschaft/_verlauf sind unabhaengig konsistent (clamp+append atomar, kein Lost-Update).
    - Test 3: der sid ist in jedem Writer-Kontext verfuegbar (claude analyse_loop haelt state[sid]; deepgram on_message hat sid).
  </behavior>
  <action>
    Alle Writer der Familie-C-Globalen auf per-sid umstellen (D-01, unter _session_state_lock, Ghost-SID-Guard). Der `sid` ist in jedem Writer-Kontext verfuegbar:
    - **claude_service analyse_loop:** haelt `state` = _session_state[sid] pro Tick + kennt sid. conversation_log-Appends (:1077,1769,1804,1810), painpoints (:1791) + Duplikat-Check (:1775), phasen_log (:1214), covered_phases (:1233 add) → alle auf `_session_state[sid][...]`.
    - **deepgram_service:** conversation_log-Writer :132,955 → per-sid (sid im on_message-Scope).

    **S4 RMW-Hotspots (atomar, verbatim):**
    - `update_kaufbereitschaft` (live_session.py:854-860): Signatur `update_kaufbereitschaft(sid, delta)`. clamp (:858) + verlauf.append (:860) als EIN Block unter _session_state_lock (siehe Interfaces-Beispiel) — kein Zwischen-Release. D-02: ohne sid → No-Op.
    - kb-Sequenz claude:1072-1083: die read(:1072)-write(:1075)-read(:1083)-Folge auf per-sid ziehen. Wenn nur der Endwert (`kb_aktuell`) verwendet wird, reicht: update_kaufbereitschaft(sid, delta) aufrufen, dann EINEN per-sid-Read `kb_aktuell = _session_state[sid]['kaufbereitschaft']`. Den `kb_vor_einwand`-Read (:1072) ebenfalls per-sid. Dokumentiere im Kommentar: Snapshot-Semantik (Anzeige-Delta, Endwert konsistent).
    - gegenargument finalize+append claude:1107-1123: die `if ls.gegenargument_log: last=[-1]; mutate; ... append`-Mutation als EIN atomarer per-sid-Block unter _session_state_lock (statt gegenargument_log_lock). Das finalize-des-letzten + append muessen unter DEMSELBEN Lock-Halt passieren (sonst schiebt ein paralleler Tick dazwischen).
    - zweiter kb-Schreibpfad :1362 (`ls.kaufbereitschaft = score_p4`) + :1368/:1735-1737 → per-sid.

    **aktive_phase_idx Writer** (grep RESEARCH §6.5 — der/die Writer) → per-sid `_session_state[sid]['aktive_phase_idx']` (Seed :422). Reader in Task 2.

    WICHTIG Vielfach-Schreiber conversation_log (6 Writer): ALLE in EINEM Deploy umstellen. Nach dem Umbau grep-Vollstaendigkeits-Check (Punkt 22): `grep -rn "conversation_log" services/ routes/` → jeder Treffer klassifiziert (per-sid vs. Rest).

    Lock-Disziplin (D-03): log_lock/painpoints_lock/gegenargument_log_lock/etc. werden durch _session_state_lock ersetzt. Snapshot unter Lock, KEINE langen Ops unter Lock. Alte Feingranular-Locks + phase_lock nach dem Umzug ueberfluessig → in Task 3 entfernen.

    Modul-Globale NOCH NICHT loeschen (bis Task 2 0 Reader hat). Task 1+2 = EIN Deploy (D-08) — NICHT zwischen Task 1 und 2 deployen. Dann Task 3 loescht.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Deploy erst NACH Task 2 (Writer+Reader zusammen).</manual>
  </verify>
  <acceptance_criteria>
    - `grep -rn "conversation_log" services/claude_service.py services/deepgram_service.py` → alle 6 Writer per-sid
    - painpoints/gegenargument_log/phasen_log/covered_phases/aktive_phase_idx-Writer alle per-sid
    - **S4: update_kaufbereitschaft(sid, delta) — clamp+append EIN Block unter _session_state_lock; kb-Sequenz claude:1072-1083 per-sid (Snapshot-Semantik dokumentiert); gegenargument finalize+append :1107-1123 EIN atomarer per-sid-Block**
    - grep-Vollstaendigkeits-Check dokumentiert (kein globaler Writer uebersehen)
    - deploy erfolgt erst mit Task 2 zusammen (D-08)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leser pro sid — api_beenden (aus dem EINEN _bs, N-3) + _build_log_content(_bs) + KB + BEIDE aktive_phase_idx-Reader (B5) (SELBER Deploy wie Task 1)</name>
  <read_first>
    - routes/app_routes.py:99-164 (das EINE `_bs = _load_beenden_state(_beenden_sid)` am Kopf aus Plan 03 Task 1c — HIER ansetzen, NICHT erneut aufrufen; conversation_log/painpoints/kb Read :143-149), :240-298 (covered_phases :242 / aktive_phase_idx :243-244 unter phase_lock / gegenargument_log :287 / phasen_log :294)
    - services/claude_service.py:205-206 (aktive_phase_idx-Reader im Prompt-Bau, B5 — ZWEITER Reader, phase_lock)
    - services/live_session.py:1050-1210 (_build_log_content — liest 4 Logs GEMEINSAM; gegenargument :1158-1159), :854-860 (update_kaufbereitschaft)
    - services/claude_service.py:1072,1083,1368,1737 (kb-Feedback interne Reader)
    - RESEARCH.md §2 Familie C + §6.5 (aktive_phase_idx)
    - Plan 03 Task 1c _beenden_sid-Vertrag + `_bs = _load_beenden_state`-Helfer (EIN Aufruf am Kopf, Peek, B1/N-3)
  </read_first>
  <behavior>
    - Test 1 (gepaart, Kern-Persistenz-Beweis): nach zwei parallelen Calls tragen ConversationLog-Felder + TXT-Log-Datei von Call A ausschliesslich A-Inhalt (DA), Call B ausschliesslich B (WEG). SPEC Req 3.
    - Test 2: _build_log_content(_bs=A-State) baut den TXT-Content NUR aus A's per-sid-Logs.
    - Test 3 (B5): claude:206-Reader liest per-sid `aktive_phase_idx` fuer sidA korrekt; KEIN AttributeError nach phase_lock-Entfernung. Prompt-Bau laeuft fuer beide sids unabhaengig.
    - **Test 4 (N-3): grep-belegt liest api_beenden GENAU EINMAL `_load_beenden_state`; alle Familie-C-Reads (inkl. _build_log_content) gehen aus diesem einen `_bs`.**
  </behavior>
  <action>
    a) **api_beenden-Reader pro sid aus dem EINEN `_bs` (N-3, B1)** (app_routes.py): ALLE Modul-Global-Reads lesen aus dem `_bs = _load_beenden_state(_beenden_sid)`, das Plan 03 Task 1c EINMAL am Kopf setzt (D-02: ohne sid → leere Liste/Default). KEIN zweiter `_load_beenden_state`-Aufruf hier:
    - :143-144 conversation_log, :145-146 painpoints, :147-149 kaufbereitschaft_verlauf + kaufbereitschaft, :241-242 covered_phases, :286-287 gegenargument_log, :293-294 phasen_log → alle aus `_bs.get(...)`.
    - :243-244 aktive_phase_idx → aus `_bs.get('aktive_phase_idx')` (per-sid, FIX); den `with ls.phase_lock:`-Wrapper hier entfernen.
    Reihenfolge (D-03/Punkt-14): `_beenden_sid` + `_bs` (das EINE _load_beenden_state) stehen VOR allen Reads (Plan-03-Kopf garantiert das). Punkt-26: die per-sid-Logs werden LIVE geschrieben → zum api_beenden-Zeitpunkt befuellt (bzw. im Snapshot); kein Voraus-Lauf.

    b) **_build_log_content sid-aware, nimmt `_bs`/State als PARAMETER (N-3)** (live_session.py:1050): Signatur `def _build_log_content(bs, user_email='', profile_name=''):` (bzw. `state`-Param). Die 4 Log-Reads (conversation_log, painpoints, gegenargument_log :1158-1159, phasen_log) lesen aus dem uebergebenen `bs`/State — NICHT aus einem eigenen `_load_beenden_state`-Aufruf und NICHT direkt aus `_session_state` (sonst verliert der Snapshot-Fall den Inhalt). Caller app_routes.py (grep `_build_log_content(`, Fresh-Grep: EIN Caller app_routes.py:300 `content = _build_log_content(user_email=g.user.email, profile_name=profile_name)`) reicht das EINE `_bs` durch: `content = _build_log_content(_bs, user_email=g.user.email, profile_name=profile_name)`. roles_swapped-Read (:1054): falls Plan 01 roles_swapped geloescht hat, sp_map fest unswapped.

    c) **KB-Familie** (update_kaufbereitschaft, live_session.py:854-860 — schon in Task 1 auf sid-Signatur): alle Caller (grep `update_kaufbereitschaft`) sid durchreichen; interne kb-Reader claude:1072,1083,1368,1737 per-sid. D-02: ohne sid → No-Op/Default.

    d) **B5 — claude:205-206 aktive_phase_idx-Reader per-sid:** der Prompt-Builder liest `with ls.phase_lock: idx = ls.aktive_phase_idx`. Umbiegen auf per-sid: der Prompt-Builder hat den sid-Kontext → `idx = _session_state[sid].get('aktive_phase_idx', 0)` unter _session_state_lock (bzw. aus dem schon gehaltenen state[sid]-Snapshot), `phase_lock` hier entfernen. **HART:** wird dieser Reader uebersehen und die Globale + phase_lock in Task 3 geloescht → AttributeError bei JEDEM Prompt-Bau. Verifizieren dass der sid im Prompt-Builder-Scope verfuegbar ist (grep die Funktions-Signatur um :205); falls nicht → sid als Parameter durchreichen (Caller kennt ihn).

    e) **Deploy JETZT** (Task 1 + Task 2 zusammen, D-08). Erst nach gruenem Deploy → Task 3.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Browser-UAT 2 Konten parallel: Tab A Painpoint "Budget Alpha", Tab B "Zeitdruck Beta". Nach Beenden: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample conversation_logs 10'` → A-Record NUR Alpha, B NUR Beta. TXT-Log pro Call isoliert. Prompt-Bau (Coaching) laeuft fuer beide ohne Fehler (B5).</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "conversation_log\|painpoints\|gegenargument_log\|phasen_log\|covered_phases\|kaufbereitschaft" routes/app_routes.py` → alle aus `_bs` (das EINE _load_beenden_state, Snapshot-faehig)
    - **N-3: `grep -n "_load_beenden_state(" routes/app_routes.py` → GENAU EIN Aufruf (der aus Plan 03, am Kopf); kein zweiter in dieser Welle hinzugekommen**
    - **`grep -n "def _build_log_content" services/live_session.py` → Signatur nimmt `bs`/State als Param; der EINE Caller app_routes.py:300 reicht `_bs` durch (kein eigener _load_beenden_state im Helfer)**
    - `grep -n "def update_kaufbereitschaft" services/live_session.py` → Signatur nimmt sid; alle Caller angepasst
    - **B5: `grep -n "aktive_phase_idx" services/claude_service.py routes/app_routes.py` → BEIDE Reader (claude:206 + app_routes:244) per-sid; `grep -n "phase_lock" services/` → 0 Treffer (entfernt, kein AttributeError)**
    - deploy.sh production GRUEN; Browser-UAT-Isolation + Prompt-Bau ohne Fehler bestaetigt
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Familie-C Modul-Globale + ueberfluessige Locks (inkl. phase_lock, B5) loeschen (0 Reader nach Task 1+2)</name>
  <read_first>
    - services/live_session.py:51-71,150-195 (Modul-Globale + Locks der Familie C), :182 (phase_lock), :183 (aktive_phase_idx), :864-959 (reset_session Familien-C-Bloecke), :948-951 (kaufbereitschaft/aktive_phase_idx-Reset)
    - RESEARCH.md §1 (Fix-vs-Delete-Spalte)
  </read_first>
  <action>
    Nach gruenem Task-1+2-Deploy (0 Reader auf den Modul-Globalen): loesche die Familie-C-Modul-Globalen + ihre ungenutzten Locks:
    - conversation_log + log_lock
    - painpoints (:55) + painpoints_lock (:54)
    - gegenargument_log (:59) + gegenargument_log_lock (:58)
    - phasen_log + phasen_log_lock
    - covered_phases + covered_phases_lock
    - kaufbereitschaft + kaufbereitschaft_verlauf + kb_lock
    - **aktive_phase_idx (:183) + phase_lock (:182) — B5: beide Reader (app_routes:244 + claude:206) sind jetzt per-sid → sicher loeschbar. VOR dem Loeschen final `grep -rn "phase_lock\|aktive_phase_idx" services/ routes/` → 0 aktive Prod-Treffer (nur Def/reset/Seed) bestaetigen, sonst AttributeError-Risiko.**
    - roles_swapped + roles_lock — falls in Plan 01 nicht schon weg
    - zugehoerige reset_session-Bloecke (:889-959: conversation_log.clear(), painpoints.clear(), kaufbereitschaft-Reset :948, covered_phases.clear(), gegenargument_log.clear() :957, phasen_log.clear(), aktive_phase_idx-Reset :951, phase_lock-Nutzung :950). Da reset_session nach pop+init laeuft → tote Geister-Writes → entfernen. Die `global`-Statements (:876,:877) entsprechend anpassen/entfernen.
    grep-Vollstaendigkeits-Check (Punkt 22): pro geloeschter Globale `grep -rn "<global>" services/ routes/` → 0 aktive Prod-Treffer.
    Der statische Waechter (Plan 01) muss diese Globalen jetzt in _KILLED haben + aus der _PENDING_MIGRATION-Liste (Welle C) streichen.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>statischer Waechter gruen: `not hasattr(ls, 'conversation_log')` etc. `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ls.conversation_log"'` → 0 aktive Reader. Coaching/Prompt-Bau live ohne AttributeError (phase_lock weg, B5).</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "^conversation_log\|^painpoints\|^gegenargument_log\|^phasen_log\|^kaufbereitschaft\|^aktive_phase_idx" services/live_session.py` → 0 Modul-Global-Defs
    - `grep -n "phase_lock" services/ routes/` → 0 Treffer (B5)
    - ungenutzte Locks (log_lock/painpoints_lock/.../phase_lock) entfernt
    - reset_session enthaelt keine toten Familie-C-Modul-Global-Resets mehr
    - statischer Waechter (Plan 01) hat die Globalen in _KILLED, Welle-C-Zeilen aus _PENDING_MIGRATION gestrichen, gruen
    - deploy.sh production GRUEN
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyse_loop/on_message → per-sid-Logs | Analyse-Ergebnis + Transkript-Logs muessen unter dem erzeugenden sid liegen |
| api_beenden (kein Socket-sid) → per-sid-Read | _beenden_sid (call_id-exakt, S3) + das EINE `_bs` (B1/N-3) darf nicht fremde Session lesen; kein zweiter Consume |
| Prompt-Builder → aktive_phase_idx | claude:206 muss per-sid lesen; Globale+phase_lock-Loeschung ohne Rewire = AttributeError (B5) |
| paralleler Tick → RMW | clamp+append / finalize+append muessen atomar (ein Lock-Halt) bleiben (S4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PERSID-11 | Information Disclosure | ~7 Familien-Globale → cross-tenant persistierter Call-Record | mitigate | alle Writer+Reader per-sid im selben Deploy (Req 3, D-08) |
| T-PERSID-12 | Tampering | uebersehener globaler Writer bei per-sid-Reader → leerer/gemischter Record | mitigate | grep-Vollstaendigkeits-Check pro Familie (Punkt 22) |
| T-PERSID-21 | Denial of Service | aktive_phase_idx/phase_lock geloescht, claude:206 nicht umgebogen → AttributeError im Prompt-Bau | mitigate | B5: BEIDE Reader per-sid im selben Deploy; grep-0-Beleg vor Loeschung |
| T-PERSID-22 | Tampering | Lost-Update bei kb clamp+append / gegenargument finalize+append | mitigate | S4: je EIN atomarer per-sid-Block unter _session_state_lock |
| T-PERSID-26 | Information Disclosure | zweiter destruktiver Consume/`_load_beenden_state`-Aufruf → spaetere Familie-C-Felder still leer | mitigate | N-3: GENAU EIN `_bs` vom Kopf (Plan 03), `_build_log_content` nimmt `_bs` als Param; consume ist PEEK |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `conversation_logs` (schreiben — api_beenden baut den Record aus den per-sid-Logs)
- `calls` (schreiben — Aggregate/Score via conversation_log_id)

### inspect.sh-Beleg (Executor holt VOR Umbau)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema conversation_logs'
$ ssh ... 'bash scripts/inspect.sh schema calls'
[Executor fuegt verbatim-Output ein]
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Leser laeuft voraus? |
|---|---|---|---|---|
| conversation_log | claude/deepgram schreiben (live) → api_beenden liest (aus dem EINEN _bs, Snapshot-faehig) | RAM per-sid / Snapshot → DB conversation_logs | live | Nein — api_beenden Endschreibung; Snapshot faengt disconnect (B1) |
| painpoints/gegenargument_log/phasen_log/covered_phases | claude schreibt (live) → api_beenden + _build_log_content(_bs) | RAM per-sid → DB/TXT | live | Nein |
| kaufbereitschaft(_verlauf) | update_kaufbereitschaft(sid) (live, RMW atomar) → api_beenden liest (via _bs) | RAM per-sid → DB | live | Nein |
| aktive_phase_idx | Writer (live) → app_routes:244 (via _bs) + claude:206 lesen per-sid | RAM per-sid | live | Nein — Reader im selben Deploy (B5) |
| conversation_logs.* (DB) | api_beenden schreibt | DB-Spalten | http (Call-Ende) | n/a |

### Bei Diskrepanz: STOP + Replan

<verification>
- deploy.sh production nerve_test-Gate gruen (Task 1+2 zusammen, dann Task 3)
- Browser-UAT 2 Konten: persistierter Call-Record + TXT-Log isoliert; Prompt-Bau ohne AttributeError (B5); GENAU EIN _load_beenden_state (N-3)
- statischer Waechter gruen nach Task 3 (phase_lock/aktive_phase_idx in _KILLED)
</verification>

<success_criteria>
- Call-Record pro sid gebaut (kein cross-tenant Datensatz), aus dem EINEN `_bs` (B1/N-3)
- _build_log_content(nimmt _bs) + update_kaufbereitschaft sid-aware; RMW-Hotspots atomar (S4)
- BEIDE aktive_phase_idx-Reader per-sid, phase_lock weg (B5)
- Familie-C-Modul-Globale + Locks geloescht
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-05-SUMMARY.md`
</output>
</content>
</output>
