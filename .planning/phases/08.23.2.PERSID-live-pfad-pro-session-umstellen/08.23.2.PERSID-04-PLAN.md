---
phase: 08.23.2.PERSID
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - services/deepgram_service.py
  - services/live_session.py
autonomous: true
requirements: [SPEC-Req-1]

must_haves:
  truths:
    - "Der _merge_pending-Puffer liegt pro sid — parallele Calls schuetten ihre Transkript-Segmente NICHT mehr in denselben Bucket"
    - "_flush_segment popt aus dem per-sid-Bucket; jeder Call sieht nur seine eigenen Segmente"
    - "Es gibt GENAU EINEN Lock fuer die Merge-Bucket-Mutation (_session_state_lock); _merge_lock ist entfernt (S4 — kein Lock-Widerspruch/keine neue Race)"
    - "Der transcript_buffer-Fallback-Zweig (toter Post-Migration-Zweig) ist entfernt; die analysiert_bisher-Modul-Globale ist weg"
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "_merge_pending-Write pro sid (unter _session_state_lock, _merge_lock weg)"
    - path: "services/live_session.py"
      provides: "_flush_segment pop pro sid (Signatur nimmt sid); transcript_buffer-Fallback entfernt; _merge_lock geloescht"
  key_links:
    - from: "deepgram_service.py:196"
      to: "_session_state[sid]['_merge_pending'][speaker]"
      via: "per-sid Merge-Bucket unter _session_state_lock (EIN Lock, S4)"
      pattern: "_merge_pending"
    - from: "live_session.py:_flush_segment"
      to: "per-sid _merge_pending pop"
      via: "_flush_segment(sid, key) pop unter _session_state_lock"
      pattern: "_merge_pending"
---

<objective>
**Familie B — RAW-TRANSCRIPT** (`_merge_pending`). Der von SPEC als "schwerster Fund" benannte Kern (Req 1): der Transkript-Zusammenfuehrungs-Puffer ist heute modul-global (live_session.py:91), gekeyt nach Sprecher-String `"0"/"1"/"unknown"` (deepgram:195) → parallele Calls schuetten Segmente in DENSELBEN Puffer → Cross-Tenant-PII bis ins rohe, persistierte Transkript.

**D-08-Beweis (Schreiber+Leser SELBER Deploy):** Schreiber deepgram_service.py:194-214 (on_message) + Leser _flush_segment live_session.py (Timer-Callback) sind eng gekoppelt — beide muessen den neuen sid-gekeyten Key GLEICHZEITIG kennen → ein Deploy. Der Timer traegt die sid bereits im pending-dict (:209 `'sid': sid`).

**S4 (Lock-Widerspruch aufloesen — MANDATORY):** die urspruengliche Task-1a nannte `_merge_lock`, Task-1b `_session_state_lock` → zwei Locks fuer dasselbe Bucket = neue Deadlock-/Race-Gefahr (verschraenkte Locks). ENTSCHEIDUNG (fest, nicht offen): **GENAU EIN Lock = `_session_state_lock`.** Der Merge-Bucket wandert nach `_session_state[sid]['_merge_pending']` (SPEC-Req-1-treue Form 2); seine Mutation laeuft unter `_session_state_lock`. Der alte `_merge_lock` (live_session.py:90) wird GELOESCHT (0 verbleibende Nutzer). Kein zweiter Lock, keine Lock-Verschraenkung.

Purpose: Das rohe Transkript kann strukturell nicht mehr cross-tenant vermischen.
Output: _merge_pending pro sid unter EINEM Lock; _merge_lock geloescht; toter transcript_buffer-Fallback + analysiert_bisher-Globale entfernt.

**Komplexitaet: 🔴 — Cross-AI PFLICHT. STT-Callback-Thread + Timer-Callback = harte Race-Naht (Punkt 14). D-03 Lock-Disziplin HART.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-SPEC.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-CONTEXT.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Aktueller globaler Bucket (live_session.py:90-91, FRESH-GREP 2026-07-03) -->
_merge_lock    = threading.Lock()   # :90 → LOESCHEN (S4: ein Lock = _session_state_lock)
_merge_pending = {}                 # :91  key = str(speaker) "0"/"1"/"unknown" → GLOBAL (Bug)

<!-- Schreiber (deepgram_service.py:194-214) — der pending-dict traegt bereits 'sid' (:209)! -->
key = str(speaker) if speaker is not None else 'unknown'   # :195
with ls._merge_lock:                                        # :196 → _session_state_lock
    if key in ls._merge_pending: ...cancel timer, append text...   # :197-200
    else: ls._merge_pending[key] = {'texts':[...], 'line_id':.., 'speaker':.., 'sid': sid, ...}  # :202-210
    t = threading.Timer(MERGE_WINDOW_S, ls._flush_segment, args=[key]); t.daemon=True; t.start()  # :211-213
    ls._merge_pending[key]['timer'] = t                     # :214

<!-- Leser (_flush_segment, live_session.py) — popt by key, liest pending['sid'] -->
def _flush_segment(key):                       # → def _flush_segment(sid, key):
    with _merge_lock: pending = _merge_pending.pop(key, None)   # → _session_state[sid]['_merge_pending'].pop(key) unter _session_state_lock
    ...
    _flush_sid = pending.get('sid')            # schon vorhanden
    if _flush_sid: → _per_sid_transcript / _per_sid_coaching_buffer (schon per-sid)
    else: transcript_buffer.append(...)        # TOTER Fallback — entfernen

<!-- SPEC Req 1 Target: "Puffer + Flush liegen unter _session_state[sid]" → Form 2. -->
<!-- S4: EIN Lock (_session_state_lock). _merge_lock geloescht. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _merge_pending pro sid keyen unter EINEM Lock (_session_state_lock); _merge_lock loeschen (S4)</name>
  <read_first>
    - services/deepgram_service.py:190-217 (Merge-Write :196 unter _merge_lock + Timer-Start :211)
    - services/live_session.py:90-91 (_merge_lock/_merge_pending-Def), :94-95 (_line_id — schon in Plan 01 S5 geloescht), _flush_segment (grep `def _flush_segment`), reset_session _merge_pending-Timer-Cancel (grep `_merge_pending` in live_session.py)
    - services/live_session.py:438-455 (pop_session_state — hier Timer der zu poppenden sid canceln)
    - RESEARCH.md §2 Familie B
    - CLAUDE.md Punkt 14 (Lock-Ordering) + Punkt 27 (einfachster tragfaehiger Weg — Form 2 mit EINEM Lock)
  </read_first>
  <behavior>
    - Test 1 (Isolation, gepaart): zwei sids A/B, beide Sprecher "0"; A schreibt "Hallo Firma Alpha", B "Hallo Firma Beta". Nach Flush enthaelt _per_sid_transcript[A] NUR Alpha-Woerter (DA), _per_sid_transcript[B] NUR Beta-Woerter, A KEINE Beta-Woerter (Isolation WEG). Cross-Tenant-PII-Beweis (SPEC Req 1).
    - Test 2: der Timer-Callback poppt aus dem RICHTIGEN per-sid-Bucket (kein Cross-Pop).
    - Test 3 (S4 Lock): keine Referenz auf `_merge_lock` bleibt; die Bucket-Mutation laeuft unter `_session_state_lock` (grep-belegt).
  </behavior>
  <action>
    **Form 2 (Puffer unter _session_state[sid]) + EIN Lock = _session_state_lock (S4, fest):**

    a) **live_session.py:** seede `_session_state[sid]['_merge_pending'] = {}` in init_session_state (neuer Sub-Key, analog den anderen). **LOESCHE `_merge_lock` (:90) + die globale `_merge_pending = {}` (:91)** — beide haben nach dem Umzug 0 Nutzer. (`_merge_lock` in die Waechter-Pending-Liste war nicht noetig; nach diesem Plan ist er weg.)

    b) **deepgram_service.py:194-214 (Schreiber):** statt `with ls._merge_lock:` + `ls._merge_pending[key]` → `with ls._session_state_lock:` + Ghost-SID-Guard `if sid in ls._session_state:` + `ls._session_state[sid]['_merge_pending'][speaker_key]`. Der `key`/`speaker_key` bleibt der Sprecher-String INNERHALB des sid-Buckets. Timer args tragen sid+key: `t = threading.Timer(MERGE_WINDOW_S, ls._flush_segment, args=[sid, speaker_key])`.
    **S4-Lock-Disziplin (D-03):** unter `_session_state_lock` laufen NUR Dict-Ops + Timer-Start/Cancel (kein LLM/Netz/DB) → kurz, kein langer Op unter Lock. **KEIN zweiter Lock** — der frueher separate `_merge_lock` ist weg; es gibt keine Lock-Verschraenkung mehr. (Der frueher in live_session.py:465 gewarnte Deadlock durch verschraenkte Locks kann nicht mehr auftreten, weil nur EIN Lock existiert.)

    c) **live_session.py _flush_segment:** Signatur `def _flush_segment(sid, key):`. Pop aus `_session_state[sid]['_merge_pending'].pop(key, None)` unter `_session_state_lock`. Ghost-SID-Guard (sid weg → return, Timer-Leiche verworfen). Der Rest (per_sid_transcript/coaching Append) bleibt — er ist schon per-sid.

    d) **Toten transcript_buffer-Fallback entfernen:** der `else`-Zweig ("should not occur post-migration") schreibt in die Modul-Globalen transcript_buffer/coaching_buffer. Nach der sid-Pflicht ist pending['sid'] IMMER gesetzt → Fallback tot. Entferne beide else-Zweige + die Modul-Globalen `transcript_buffer` (:46), `analysiert_bisher` (:47, RESEARCH §1: schon per-sid, Modul-Rest tot) inkl. deren Lock-Nutzung. `coaching_buffer` (:52) Modul-Globale pruefen — falls 0 Reader → weg (per-sid _per_sid_coaching_buffer ist Quelle).

    e) **pop_session_state/reset_session Timer-Cancel (Punkt 14):** vor dem Poppen einer sid muessen die noch offenen `_merge_pending`-Timer dieser sid gecancelt werden (sonst feuert ein Timer in einen weggeraeumten sid → Ghost-SID-Guard faengt es zwar ab, aber sauber canceln ist besser). Ergaenze in `pop_session_state(sid)` (VOR dem eigentlichen pop, unter _session_state_lock einen Snapshot der Timer nehmen, Lock freigeben, dann `.cancel()` — Timer.cancel ausserhalb des Locks, kein langer Op unter Lock): iteriere `_session_state[sid]['_merge_pending'].values()` und `t['timer'].cancel()`. **HINWEIS B1-Kopplung (Plan 03):** pop_session_state wird jetzt auch von `stash_ended_session` (Plan 03) aufgerufen → der Timer-Cancel gilt auch beim Stash. Die gestashte Snapshot-Kopie braucht die Timer NICHT (die Merge-Fenster sind beim disconnect eh abgeschlossen/irrelevant) → Timer canceln, Snapshot ohne offene Timer.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Browser-UAT (2 Konten parallel): Tab A sagt "Zebra Alpha", Tab B "Qualle Beta". Nach Beenden: `ssh ... 'bash scripts/inspect.sh sample transcript_segments 30'` → A-Call-Rows enthalten KEINE Beta-Woerter und umgekehrt. Kern-PII-Beweis.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "_merge_pending" services/deepgram_service.py` → Write in `_session_state[sid]['_merge_pending']`, unter `_session_state_lock`
    - **S4: `grep -n "_merge_lock" services/` → 0 Treffer (geloescht); `grep -n "_session_state_lock" services/deepgram_service.py` → die Merge-Mutation laeuft darunter (EIN Lock, kein Widerspruch)**
    - `grep -n "def _flush_segment" services/live_session.py` → Signatur `(sid, key)`
    - `grep -n "transcript_buffer" services/live_session.py` → 0 aktive Fallback-Zweige (Modul-Globale entfernt)
    - `grep -n "^analysiert_bisher =" services/live_session.py` → Modul-Globale weg
    - Timer werden bei pop/stash/reset der sid gecancelt (kein Feuern in weggeraeumte sid); cancel ausserhalb des Locks
    - deploy.sh production GRUEN; statischer Waechter bleibt gruen
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deepgram-STT-Thread → Merge-Bucket | Rohes Transkript (Roh-PII vor Anonymisierung) darf nie in einen fremden sid-Bucket |
| Timer-Callback → _flush_segment | Der Timer muss in den sid poppen, der ihn erzeugt hat (kein Cross-Pop) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PERSID-09 | Information Disclosure | _merge_pending global → rohes Cross-Tenant-Transkript | mitigate | Bucket pro sid; Flush popt per-sid (Req 1) — der schwerste Fund |
| T-PERSID-10 | Tampering | Zwei Locks fuer dasselbe Bucket → Verschraenkung/Deadlock | mitigate | S4: GENAU EIN Lock (_session_state_lock); _merge_lock geloescht → keine Verschraenkung moeglich |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `transcript_segments` (schreiben — indirekt: _per_sid_transcript feeds den Batch-Write am Call-Ende) — RAM-Puffer geaendert, DB-Schema unveraendert.

### inspect.sh-Beleg (Executor holt VOR Umbau)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
[Executor fuegt verbatim-Output ein]
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Leser laeuft voraus? |
|---|---|---|---|---|
| _merge_pending | deepgram schreibt → _flush_segment popt | RAM (per-sid nach Fix, EIN Lock) | live (STT-Callback + Timer MERGE_WINDOW_S) | Nein — Timer-Callback ist der einzige Leser, gekoppelt |
| _per_sid_transcript | _flush_segment append | RAM per-sid (existiert) | live | analyse_loop liest per-sid (schon sauber) |
| transcript_segments (DB) | Batch-Write am Call-Ende aus per-sid-Puffer | DB-Tabelle | batch (Call-Ende) — identische created_at pro Gruppe | Leser (api_beenden) laeuft NICHT voraus (Endschreibung) |

### Bei Diskrepanz: STOP + Replan

<verification>
- deploy.sh production nerve_test-Gate gruen
- Browser-UAT 2 Konten: rohes Transkript pro Call isoliert (Zebra/Qualle-Beweis)
- statischer Waechter bleibt gruen; `_merge_lock` restlos entfernt (S4)
</verification>

<success_criteria>
- _merge_pending pro sid unter EINEM Lock (_session_state_lock); _merge_lock geloescht (S4)
- Cross-Tenant-Transkript strukturell unmoeglich
- toter transcript_buffer-Fallback + analysiert_bisher-Modul-Globale entfernt
- Timer-Cancel bei pop/stash/reset korrekt (ausserhalb Lock)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-04-SUMMARY.md`
</output>
