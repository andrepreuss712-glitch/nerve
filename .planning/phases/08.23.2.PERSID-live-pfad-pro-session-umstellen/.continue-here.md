# Continue Here — Phase 08.23.2.PERSID (Welle 5 / Plan 06, mitten drin)

**Pausiert:** 2026-07-04, Account-Session-Limit (reset 12:20 Europe/Berlin) traf den Plan-06-Executor mitten in der Arbeit.

## Stand der Phase
Wellen 0-4 (Plan 01-05) **komplett, live auf Prod, gepusht**:
- Plan 01: Zombie-Deletes + statischer Global-Wächter (grün) + app.py NERVE_TESTING-Guard + Concurrency-Skeleton. (630 passed)
- Plan 02: F2 not_gradable-Terminal + build_intent_payload + TTFT + Heiler + Migration 0031. (817 passed)
- Plan 03: Familie A per-sid + _ended_session_snapshots-Fundament (B1/N-1/N-3) + _beenden_sid-Vertrag (S3). (839 passed)
- Plan 04: Familie B _merge_pending per-sid, EIN Lock (_merge_lock weg, S4). (844 passed)
- Plan 05: Familie C per-sid (conversation_log/kaufbereitschaft/aktive_phase_idx…), phase_lock weg (B5), api_beenden über EINEN _bs (N-3). (865 passed) — head 0a5125c = letzter grüner Prod-Deploy.

## Plan 06 — teilweise fertig
- ✅ Task 1 (Familie D): committet+gepusht — `70a1441` (RED), `3fad98a` (GREEN). record_ewb_click/record_suggestion_offer per-sid, ALLE 6 Caller (deepgram:1006/1017/1026/1041, matcher:338, claude:722) sid-durchgereicht (B4), test_suggestion_reactions.py auf neue Signatur (W-2).
- ⏳ Task 2 (Familie E): Speaker-Familie (_log_last_sp, _second_sp_seen) per sid; _bof_count-Modul-Global löschen (per-SID-Pfad claude:1711 existiert); reset_session(sid) NUR eigene sid (Objekt-Identitäts-Check) + Snapshot-Pop (pop_ended_session, N-3 finales Cleanup); analysiert_bisher deepgram-Read-Ref (Welle E) auflösen.
- ⏳ Task 3: voller tests/test_persid_concurrency.py scharf+GRÜN committet (2-Tenant, 7 gepaarte Assertions + Reset-Überleben + Same-User-2-Session + B1-Late-Write-Ghost-Drop + B1-stop_live_session-vor-beenden-Non-Empty + N-1-Doppel-Feuer-Non-Empty); statischer Wächter final: _PENDING_MIGRATION MUSS == frozenset() sein (B3, Task-3-Assertion).
- ⏳ Task 4 (checkpoint:human-verify): Founder-Doppel-Anruf-UAT durch André auf Prod (NIE Gate; UAT nach Fix). resume-signal "approved" wenn (a) keine Vermischung (b) B lief ungestört (c) A-Record bei normalem Auflegen nicht leer (B1 :779) (d) B-Record trotz Netz-Blip nicht leer (B1 :815).

## _PENDING_MIGRATION Rest (muss von Task 2/3 auf LEER geräumt werden)
- ('services/live_session.py', "state['ewb_clicks']", 'D')
- ('services/live_session.py', "state['suggestion_offers']", 'D')
- ('services/deepgram_service.py', '_second_sp_seen', 'E')
- ('services/deepgram_service.py', '_log_last_sp', 'E')
- ('services/deepgram_service.py', 'analysiert_bisher', 'E')

## So fortsetzen (nach 12:20 Berlin)
1. Frischen gsd-executor (sonnet) für Plan 06 **Tasks 2-4** spawnen (Task 1 ist DONE — NICHT wiederholen). Hartkodiertes Phase-Dir. Sequentiell auf main, normale Commits, `deploy.sh production` als Gate. SUMMARY schreiben (Orchestrator committet+pusht + STATE/ROADMAP).
2. Executor stoppt am Task-4-Checkpoint → André den Founder-Doppel-Anruf-UAT vorlegen.
3. Nach "approved": Phase-Verifikation (gsd-verifier) → phase.complete (ROADMAP/STATE/REQUIREMENTS — wegen Multi-Segment-Gotcha ggf. Direkt-Edit) → PERSID abschließen.

## Wichtig
- Multi-Segment-Phase-ID: gsd-tools/find-phase misresolvt "08.23.2.PERSID" → "08.23.2". Pfade IMMER hartkodieren; gsd-sdk state.*/roadmap.*/requirements.* NICHT nutzen.
- deploy.sh production = gewolltes Akzeptanz-Gate (manual-direct-prod, echter VPS-Deploy). Kein Regelverstoß.
- Test-Akzeptanz = volle scripts/triage.sh + deploy.sh production, nicht lokales grep/AST (Contract-Changes röten stale Tests).
