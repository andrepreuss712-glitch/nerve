---
phase: 08.23.2.PERSID
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - services/slow_lane.py
  - services/intent_payload.py
  - services/intent_event_writer.py
  - services/claude_service.py
  - routes/app_routes.py
  - database/models.py
  - tests/test_f2_stilllegung.py
  - tests/test_intent_payload_guard.py
  - tests/test_heiler_resolved.py
autonomous: true
requirements: [SPEC-Req-8, SPEC-Req-9, SPEC-Req-10, SPEC-Req-11]

must_haves:
  truths:
    - "Neue intent_event bekommen sofort 'not_gradable' terminal — _pending_events drainet auf 0, Merge/rubric_score feuert normal (kein Deadlock)"
    - "build_intent_payload() ist in intent_payload.py ausgelagert; ein Struktur-/Whitelist-Test faengt fehlende Pflichtfelder + Nicht-Whitelist-Keys"
    - "streame_manual_ewb_variante erzeugt einen TTFT-Messwert"
    - "Ein haengen gelassener Call (resolved-Flags False) wird vom Heiler terminal aufgeloest — inline (kuenftige) UND einmaliger Boot-Sweep (bereits haengende Alt-Calls, S2)"
    - "models.py:791 handling_status-Kommentar nennt 'not_gradable' (S7, Punkt 23 Schild-Aktualitaet im selben Change)"
  artifacts:
    - path: "services/intent_payload.py"
      provides: "build_intent_payload() — reine Auslagerung aus intent_event_writer.py:100-118 (die EINE abgenickte Nicht-Isolations-Naht)"
      contains: "def build_intent_payload"
    - path: "tests/test_intent_payload_guard.py"
      provides: "Pflichtfeld-/Typen-/Whitelist-Test fuer den Payload"
      min_lines: 30
  key_links:
    - from: "services/slow_lane.py:_persist_event_ref"
      to: "handling_status = 'not_gradable'"
      via: "Terminal-Setzung direkt nach Idempotenz-Skip, VOR Confidence-Gate"
      pattern: "not_gradable"
    - from: "services/claude_service.py:streame_manual_ewb_variante"
      to: "TTFT-Messwert"
      via: "_first_token/_t_stream_start monotonic-Delta im Stream-Loop"
      pattern: "EWB-TTFT"
---

<objective>
Der QUER-Cluster: F2 sicher stilllegen (Req 8, deadlock-frei) + 3 Waechter (a/b/c, Req 9/10/11). Unabhaengig vom per-sid-Umzug → frueh (Welle 1), damit die Datenfamilie-Wellen (A-E) danach ungestoert laufen. Diese Welle besitzt slow_lane, intent_event_writer, claude_service (TTFT-Naht) + app_routes (Heiler-Naht) exklusiv, BEVOR die Config-Welle app_routes anfasst.

Purpose: F2 schreibt keine Leerlauf-Noten mehr (100% abstained/NULL am Prod belegt tot), OHNE Fan-In-Deadlock. Die 3 Waechter sind Anti-Erosions-Faenge fuer TAXO3-b.
Output: 1 F2-Insertion, 1 neue intent_payload.py + Whitelist-Test, 1 TTFT-Messung, 1 Heiler (inline + Boot-Sweep S2) + Test, 1 Schild-Kommentar-Update (S7).

**Komplexitaet: 🔴 — Cross-AI/3-Sichten PFLICHT. Punkt 25 (Latenz) bei TTFT. Punkt 26 (Bereitschafts-Naht) beim Heiler. Punkt 23 (Schild-Aktualitaet) bei S7.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-SPEC.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- F2-Insertion-Point (slow_lane.py, FRESH-GREP 2026-07-03) — ACHTUNG Signatur-Reihenfolge: -->
def _persist_event_ref(event_ref, db) -> None:     # :216  (NICHT (db, event_ref)!)
  :230  if ev.handling_status != 'pending': return   # Idempotenz-Skip
  :237  gate = _confidence_gate_for(ev, db)           # Confidence-Gate (F2 setzt VOR hier terminal)
_STATUS_NOT_GRADABLE = 'not_gradable'   # slow_lane.py:364 (existierender Terminal-Status)
def _pending_events(call_id, db) -> int:  # :369 — zaehlt NUR handling_status=='pending'

<!-- Payload-Auslagerung (intent_event_writer.py:100-118) -->
payload = {'source','inference_basis','taxonomy_version','abstained','speaker_role',
           'speaker_id','is_simulation','origin_type','triggering_text'}  # 9 Pflichtfelder
# + extra_payload-Merge (:116-118); taxonomy_version=TAXONOMY_VERSION (:103, non-null)
# KEIN schema_version (models.py:795 payload_jsonb hat KEINE schema_version-Spalte)

<!-- TTFT-Muster zum Kopieren (aus totem streame_auto_variante, claude_service.py:631-654) -->
_first_token_autovar = True; _t_stream_start = monotonic()
for token in stream.text_stream:
    if _first_token_autovar:
        _ttft_ms = (monotonic() - _t_stream_start) * 1000; _first_token_autovar = False
        print(f"[EWB-TTFT] {_ttft_ms:.0f}ms ...")

<!-- Heiler-Naht (app_routes.py, FRESH-GREP: calls-UPDATE-Block ~:690-732) -->
:724 if not _phase_d_word_confidences: _call_row.audio_health_resolved = True
:725 _call_row.transcript_resolved = True
:727 _db_calls.commit()
:728 except ...: :730 rollback()   # <-- bei Fehlschlag bleiben BEIDE Flags False → Call haengt
# calls.audio_health_resolved (models.py:703) + calls.transcript_resolved (models.py:713)

<!-- S7-Ziel: models.py:791 handling_status COMMENT (heute: "pending|scored|abstained|failed") -->
# → "pending|scored|abstained|failed|not_gradable" (F2 fuehrt not_gradable als Live-Setzung ein)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: F2 stilllegen — 'not_gradable'-Terminal in _persist_event_ref (Req 8) + Schild-Update models.py:791 (S7)</name>
  <read_first>
    - services/slow_lane.py:216-284 (_persist_event_ref voll — Signatur `(event_ref, db)` :216, Idempotenz :230, Gate :237), :364 (_STATUS_NOT_GRADABLE), :369-376 (_pending_events)
    - services/rubric_engine.py:68,118,188,198-218 (compute_rubric empty-set-Toleranz — HIGH-Drift 3: NICHT database/rubric_dimensions.py)
    - tests/test_abstain_log_rls.py (Beleg: prueft RLS-Isolation, NICHT "abstain_log muss befuellt sein")
    - database/models.py:791 (handling_status-COMMENT — S7: nennt heute pending|scored|abstained|failed, OHNE not_gradable); :893 (status-COMMENT nennt not_gradable schon — Muster)
    - RESEARCH.md §3 (Insertion-Point exakt, Deadlock-Analyse)
    - CLAUDE.md Punkt 23 (Schild-Aktualitaets-Pflicht — im SELBEN Change nachziehen)
  </read_first>
  <behavior>
    - Test 1: Ein 'pending'-Event durch _persist_event_ref → handling_status wird 'not_gradable', KEIN grade_handling/AbstainLog-INSERT laeuft.
    - Test 2: Nach not_gradable zaehlt _pending_events dieses Event NICHT (drainet auf 0).
    - Test 3: Ein Event mit handling_status != 'pending' bleibt unveraendert (Idempotenz vor der neuen Terminal-Setzung).
  </behavior>
  <action>
    In services/slow_lane.py, in `_persist_event_ref(event_ref, db)`, DIREKT nach dem Idempotenz-Skip (`:230` `if ev.handling_status != 'pending': return`) und VOR dem Confidence-Gate (`:237` `gate = _confidence_gate_for(ev, db)`):

    ```python
    # F2-Stilllegung (PERSID Req 8): der per-Ereignis-Benoter schreibt keine
    # Leerlauf-Noten mehr (100% abstained/NULL am Prod belegt tot; Judge deckt Urteil ab).
    # not_gradable = existierender Terminal-Status (:364) → _pending_events zaehlt es NICHT
    # → drainet auf 0 → Call-Ende-Merge/rubric_score feuert normal (deadlock-frei).
    # NULL Schema-Change; umgeht auch den RLS-fail-closed-Loop (:262-276); DB-Felder bleiben.
    ev.handling_status = _STATUS_NOT_GRADABLE
    return
    ```

    Reconciliation-Note im Kommentar: HIGH-Drift 3 — die Empty-Set-Toleranz die Req 8 (ii) fordert liegt in `services/rubric_engine.py:118/188/198-218` (compute_rubric), NICHT in `database/rubric_dimensions.py:393-402` (existiert nicht). KEINE Aenderung an rubric_engine noetig — sie toleriert die leere handling-Menge bereits (Status insufficient_data statt Div-durch-Null). Nur verifizieren, nicht anfassen.

    DB-Felder handling_score_numeric/handling_status/abstain_log bleiben erhalten (Boundary).

    **S7 (Punkt 23, im SELBEN Change):** F2 fuehrt `not_gradable` als LIVE-Setzung fuer intent_event ein → der handling_status-Schild-Kommentar (database/models.py:791) muss den Wert nennen. Aendere den COMMENT-String von `"...(pending|scored|abstained|failed)..."` auf `"...(pending|scored|abstained|failed|not_gradable)..."` (Wertebereich vollstaendig). Da der Kommentar via `comment=` in models.py steht UND in pg_description gespiegelt ist: den `comment=`-String in models.py:791 aendern UND eine kurze Alembic-Migration `op.execute("COMMENT ON COLUMN intent_event.handling_status IS '...'")` mit dem neuen Text hinzufuegen (hauseigenes Muster, Punkt 23 Guard prueft pg_description). KEINE Schema-Aenderung (nur Kommentar).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Nach Deploy Prod-Beleg: `ssh ... 'bash scripts/inspect.sh sample intent_event 20'` → neue Rows nach Deploy tragen handling_status='not_gradable'. Und ein Call-Ende feuert rubric_score (kein Haenger in "wird ausgewertet"). Schild: `ssh ... 'bash scripts/inspect.sh schilder intent_event'` → handling_status-Kommentar nennt not_gradable.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "_STATUS_NOT_GRADABLE" services/slow_lane.py` zeigt die neue Terminal-Setzung zwischen :230 und :237 (Signatur `(event_ref, db)` beibehalten — NICHT (db, event_ref))
    - Der neue Block steht VOR `_confidence_gate_for` und VOR `grade_handling`
    - Kein Test fordert abstain_log-Befuellung (test_abstain_log_rls.py bleibt gruen)
    - **S7: `grep -n "not_gradable" database/models.py` → handling_status-COMMENT (:791) nennt not_gradable; Alembic-Migration mit COMMENT ON COLUMN vorhanden**
    - deploy.sh production GRUEN (rubric_engine empty-set-tolerant, kein Div-durch-Null; Schild-Guard gruen)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Waechter (a) — build_intent_payload() auslagern + Whitelist-Test (Req 9)</name>
  <read_first>
    - services/intent_event_writer.py:90-159 (Payload-Bau :100-118, extra_payload-Merge, TAXONOMY_VERSION-Import)
    - database/models.py:795 (payload_jsonb — KEINE schema_version-Spalte)
    - tests/test_intent_event_writer.py (falls vorhanden — Muster fuer Payload-Assertions)
    - RESEARCH.md §4(a) (9 Pflichtfelder exakt, taxonomy_version != schema_version)
    - CONTEXT.md §spec_lock (die EINE abgenickte Nicht-Isolations-Naht: reine Auslagerung, KEIN Verhaltens-Change)
  </read_first>
  <behavior>
    - Test 1: build_intent_payload(...) mit gueltigen Args liefert ein dict mit GENAU den 9 Pflichtfeldern (source, inference_basis, taxonomy_version, abstained, speaker_role, speaker_id, is_simulation, origin_type, triggering_text).
    - Test 2: taxonomy_version ist non-null; abstained + is_simulation sind bool.
    - Test 3 (Whitelist): taucht ein Key ausserhalb der Erlaubte-Keys-Whitelist auf (z.B. via extra_payload ein unbekannter Key) → Test rot. Fehlt ein Pflichtfeld → rot.
    - Test 4: schema_version ist NICHT Pflicht (darf fehlen, macht Test nicht rot) — taxonomy_version != schema_version.
  </behavior>
  <action>
    a) Neue Datei `services/intent_payload.py` mit `build_intent_payload(source, inference_basis, abstained, speaker_role, speaker_id, is_simulation, origin_type, triggering_text, extra_payload=None) -> dict`. Reine Auslagerung des Dict-Literals aus intent_event_writer.py:100-118 (inkl. extra_payload-Merge), `taxonomy_version=TAXONOMY_VERSION`. KEIN Verhaltens-Change (die EINE abgenickte Naht, CONTEXT §spec_lock).

    b) In intent_event_writer.py:100-118 den Inline-Block durch `payload = build_intent_payload(...)` ersetzen (Import aus services.intent_payload). Sonst nichts aendern.

    c) `tests/test_intent_payload_guard.py`: Pflichtfeld-Set als frozenset `_REQUIRED = {source, inference_basis, taxonomy_version, abstained, speaker_role, speaker_id, is_simulation, origin_type, triggering_text}`; Whitelist `_ALLOWED = _REQUIRED` (+ jeder bewusst aufgenommene extra_payload-Key). Test 1-4 wie <behavior>. Assertion: `set(payload) - _ALLOWED == set()` (rot bei Nicht-Whitelist-Key); `_REQUIRED - set(payload) == set()` (rot bei fehlendem Pflichtfeld); `payload['taxonomy_version'] is not None`; `isinstance(payload['abstained'], bool)`.

    d) schema_version bleibt RAUS (TAXO3-b bringt sie additiv als MIRROR_SCHEMA_VERSION). Kommentar im Test: `taxonomy_version` (Intent-Taxonomie) != `schema_version` (Payload-Form) — nicht verwechseln.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>test_intent_payload_guard.py laeuft im nerve_test-Gate. Bei gruenem deploy.sh gruen. Prod-Beleg: neue intent_event-payload_jsonb tragen dieselben 9 Keys wie vorher (kein Verhaltens-Change).</manual>
  </verify>
  <acceptance_criteria>
    - services/intent_payload.py existiert mit `def build_intent_payload`
    - `grep -n "build_intent_payload" services/intent_event_writer.py` → Aufruf statt Inline-Dict
    - tests/test_intent_payload_guard.py >=30 Zeilen, Test rot bei fehlendem Pflichtfeld / Nicht-Whitelist-Key / taxonomy_version=None
    - Kein schema_version im Payload; Test macht dessen Abwesenheit NICHT rot
    - deploy.sh production GRUEN
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Waechter (b) TTFT + Waechter (c) Heiler inline + einmaliger Boot-Sweep (Req 10 + 11, S2)</name>
  <read_first>
    - services/claude_service.py:740-865 (streame_manual_ewb_variante — Stream-Loop misst nur full_text, KEINE TTFT)
    - services/claude_service.py:620-654 (TTFT-Muster aus totem streame_auto_variante zum Kopieren)
    - routes/app_routes.py:690-732 (calls-UPDATE-Block, :724-726 resolved-Flags, :728-730 except/rollback = Failure-Site)
    - database/models.py:703 (audio_health_resolved), :713 (transcript_resolved)
    - services/slow_lane.py (der _audio_health_bg-finally-Setzer als Muster fuer eigene kurze Session)
    - app.py:2209-2223 (Boot-Sequenz — wo ein EINMALIGER Startup-Sweep landet, NACH dem ENV-TESTING-Guard aus Plan 01)
    - RESEARCH.md §4(b) TTFT-Muster + §4(c) Heiler-Failure-Site
    - CLAUDE.md Punkt 25 (Latenz) + Punkt 26 (Bereitschafts-Naht, KEIN neuer periodischer Zeit-Sweep)
  </read_first>
  <behavior>
    - Test 1 (TTFT): ein gemockter Knopf-Aufruf von streame_manual_ewb_variante erzeugt einen TTFT-Messwert (z.B. ein [EWB-TTFT]-Print/History-Eintrag). Assertion auf den Messwert-Effekt.
    - Test 2 (Heiler inline): ein kuenstlich haengen gelassener Call (audio_health_resolved=False, transcript_resolved=False) wird durch den inline-Heiler-Pfad terminal aufgeloest (beide Flags True) — Test belegt den Uebergang.
    - Test 3 (Heiler Boot-Sweep, S2): ein bereits-haengender Alt-Call (ended_at IS NOT NULL, beide resolved-Flags False) wird vom einmaligen Boot-Sweep auf True gesetzt — Test belegt, dass der Sweep genau die WHERE-Bedingung trifft und keine noch-laufenden Calls (ended_at NULL) anfasst.
  </behavior>
  <action>
    a) **Waechter (b) TTFT (Req 10):** In `streame_manual_ewb_variante` (claude_service.py:740-865) das TTFT-Idiom aus streame_auto_variante:631-654 einsetzen. VOR dem Stream-Loop: `_first_token_manual = True`, `_t_stream_start = _time.monotonic()` unmittelbar vor dem `with ...messages.stream(...)`. Im `for token in stream.text_stream`-Loop, als erstes:
    ```python
    if _first_token_manual:
        _ttft_ms = (_time.monotonic() - _t_stream_start) * 1000
        _first_token_manual = False
        print(f"[EWB-TTFT] {_ttft_ms:.0f}ms model={config.MODEL_PIP_VARIANTE} sid={sid} path=manual")
    ```
    Latenz-neutral (Punkt 25): nur ein monotonic-Delta + print, keine Blockierung. MINIMAL halten (SPEC verlangt nur "ein TTFT-Messwert entsteht"). Den Messwert NICHT in `_ewb_ttft_history` haengen (das ist der Circuit-Breaker des DORMANTEN Auto-Pfads, S6-Whitelist — nicht mit dem Knopf-Pfad koppeln, sonst Cross-Path-Verhalten).

    b) **Waechter (c) Heiler — inline (Req 11):** SPEC-Default (CONTEXT): inline-terminal an der Fehlerstelle, KEIN neuer periodischer Zeit-Sweep (Punkt 26). In app_routes.py im `except`-Zweig des calls-UPDATE (~:728-730), nach dem rollback, die beiden resolved-Flags idempotent auf True setzen mit eigener kurzer Session (Muster _audio_health_bg-finally):
    ```python
    except Exception as _e_upd:
        print(f'[Phase08.23.2.D] calls-UPDATE Fehler: {_e_upd}')
        _db_calls.rollback()
        # PERSID Req 11 Heiler (inline-terminal, Punkt 26 — KEIN periodischer Zeit-Sweep):
        try:
            _db_heal = get_session()
            _row = _db_heal.query(_CallModel).filter(_CallModel.id == _phase_d_call_id).first()
            if _row is not None:
                _row.audio_health_resolved = True
                _row.transcript_resolved = True
                _db_heal.commit()
        except Exception as _e_heal:
            print(f'[Heiler] resolved-Flag-Rettung fehlgeschlagen (non-fatal): {_e_heal}')
        finally:
            try: _db_heal.close()
            except Exception: pass
    ```

    c) **Waechter (c) Heiler — einmaliger Boot-Sweep (S2, jetzt PFLICHT statt optional):** der inline-Heiler repariert nur KUENFTIGE Fehlschlaege; die schon-haengenden Alt-Calls (vor diesem Deploy) bleiben ohne Sweep fuer immer in "wird ausgewertet". EINMAL beim Boot (app.py, in der Startup-Sequenz, HINTER dem ENV-TESTING-Guard aus Plan 01 — im Test NICHT laufen lassen) ein idempotentes UPDATE:
    ```python
    # PERSID Req 11 / S2 — EINMALIGER Boot-Sweep fuer bereits haengende Alt-Calls.
    # KEIN periodischer Timer (Punkt 22/26) — laeuft genau einmal pro Prozess-Start.
    if _os_guard.environ.get('NERVE_TESTING') != '1':
        try:
            _db_boot = get_session()
            _db_boot.query(_CallModel).filter(
                _CallModel.ended_at.isnot(None),
                or_(_CallModel.audio_health_resolved.is_(False),
                    _CallModel.transcript_resolved.is_(False))
            ).update({'audio_health_resolved': True, 'transcript_resolved': True},
                     synchronize_session=False)
            _db_boot.commit()
        except Exception as _e_bs:
            print(f'[Heiler-BootSweep] non-fatal: {_e_bs}')
        finally:
            try: _db_boot.close()
            except Exception: pass
    ```
    HART: NUR `ended_at IS NOT NULL` (beendete Calls) — ein noch laufender Call (ended_at NULL) darf NICHT angefasst werden (sonst zerreisst der Sweep die Bereitschafts-Naht eines aktiven Calls, Punkt 26). Genau EINMAL pro Prozess-Start, KEIN wiederkehrender Timer.

    d) `tests/test_heiler_resolved.py`: der inline-Heiler-Uebergangs-Test (kuenstlich haengender Call → Heiler → beide Flags True) + der Boot-Sweep-Test (Alt-Call ended_at!=NULL & Flags False → True; laufender Call ended_at=NULL bleibt unangetastet). TTFT-Test in tests/test_f2_stilllegung.py ODER eigene Datei — Executor waehlt, Hauptsache im Default-Gate.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Nach Deploy: ein manueller Knopf-Klick im Browser-UAT erzeugt eine [EWB-TTFT]-Zeile: `ssh ... 'bash scripts/inspect.sh logs 200'` → grep `EWB-TTFT.*path=manual`. Boot-Sweep-Beleg: `ssh ... 'bash scripts/inspect.sh sample calls 30'` → keine beendeten Calls mehr mit beiden resolved-Flags False.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "EWB-TTFT" services/claude_service.py` zeigt einen Treffer im streame_manual_ewb_variante-Pfad (path=manual)
    - streame_manual_ewb_variante misst _ttft_ms beim ersten Token (Latenz-neutral); Messwert NICHT in _ewb_ttft_history gekoppelt (S6-Trennung)
    - app_routes.py except-Zweig setzt beide resolved-Flags idempotent True (eigene Session) — inline-Heiler
    - **S2: einmaliger Boot-Sweep in app.py (hinter ENV-TESTING-Guard) mit `ended_at IS NOT NULL AND (resolved-Flags False) → True`; KEIN periodischer Timer (Punkt 26); laufende Calls (ended_at NULL) unangetastet**
    - tests/test_heiler_resolved.py belegt inline-Uebergang UND Boot-Sweep (inkl. "laufender Call bleibt unberuehrt")
    - deploy.sh production GRUEN
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slow-Lane-Consumer → intent_event | Terminal-Status-Schreiber muss deadlock-frei drainen (Fan-In) |
| calls-UPDATE-Failure → Call-Lifecycle | Fehlschlag darf Call nicht dauerhaft in "wird ausgewertet" haengen lassen (inline + Boot-Sweep) |
| Knopf-Pfad-Stream → Latenz | TTFT-Messung darf die Antwort nicht verlangsamen (Punkt 25) |
| Boot-Sweep → laufender Call | Der einmalige Sweep darf nur beendete Calls (ended_at!=NULL) anfassen, nie einen aktiven (Punkt 26) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PERSID-04 | Denial of Service | _pending_events drainet nie (Fan-In-Deadlock) | mitigate | not_gradable = Terminal → _pending_events zaehlt es nicht → Merge feuert (Req 8) |
| T-PERSID-05 | Denial of Service | Call haengt in "wird ausgewertet" bei UPDATE-Fehlschlag | mitigate | Inline-Heiler + einmaliger Boot-Sweep fuer Alt-Calls (Req 11, S2, Punkt 26) |
| T-PERSID-06 | Tampering | Unbekannter Key schleicht sich in payload_jsonb | mitigate | Whitelist-Test rot bei Nicht-Whitelist-Key (Req 9) |
| T-PERSID-18 | Denial of Service | Boot-Sweep zerreisst aktiven Call (ended_at NULL) | mitigate | WHERE ended_at IS NOT NULL; einmalig, kein Timer (S2, Punkt 26) |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `calls` (schreiben — Heiler-c inline + Boot-Sweep S2 setzen `audio_health_resolved`/`transcript_resolved` terminal)
- `intent_event` (schreiben — F2-Task 1 setzt `handling_status = 'not_gradable'` terminal; S7 Kommentar-Update)

### inspect.sh-Beleg (Executor holt VOR Umbau — Kein-Local-Dev, read_first)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
[Executor fuegt verbatim-Output ein — audio_health_resolved (models.py:703) + transcript_resolved (models.py:713) + Typ (BOOLEAN) + ended_at (nullable) bestaetigen]
$ ssh ... 'bash scripts/inspect.sh schema intent_event'
[Executor fuegt verbatim-Output ein — handling_status-Spalte + Enum/Wertebereich bestaetigen]
$ ssh ... 'bash scripts/inspect.sh count calls'   # S2: wie viele beendete Calls mit haengenden Flags existieren real?
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Leser laeuft voraus? |
|---|---|---|---|---|
| calls.audio_health_resolved | inline-Heiler SCHREIBT terminal (True) im except-Zweig; Boot-Sweep SCHREIBT terminal fuer beendete Alt-Calls | DB-Spalte (models.py:703) | http (Call-Ende, Fehler-Site) + boot (einmalig) | Nein — Heiler/Sweep sind selbst der Schreiber; Boot-Sweep nur ended_at!=NULL |
| calls.transcript_resolved | wie oben | DB-Spalte (models.py:713) | http + boot | Nein |
| intent_event.handling_status | F2-Task 1 SCHREIBT 'not_gradable' terminal in _persist_event_ref direkt nach Idempotenz-Skip | DB-Spalte | live (Slow-Lane-Consumer, pro Event) | Nein — _pending_events zaehlt 'not_gradable' NICHT → drainet auf 0 |
| intent_event.handling_status (Schild) | S7: models.py comment=/pg_description | Katalog (pg_description) | change-time (im selben Commit, Punkt 23) | n/a |

**Punkt-26-Fazit:** Alle Nahtstellen sind terminal-WRITES an der Stelle, an der sonst NULL/pending stehen bliebe. Der Boot-Sweep fasst NUR beendete Calls (ended_at!=NULL) an → kann keinen aktiven Call zerreissen. Kein Leser laeuft einem Schreiber voraus.

### Bei Diskrepanz: STOP + Replan

<verification>
- deploy.sh production nerve_test-Gate gruen nach jedem Task
- Prod-Beleg: neue intent_event = not_gradable; rubric_score feuert; [EWB-TTFT] path=manual im Log; keine beendeten Calls mit haengenden Flags mehr (S2)
- test_intent_payload_guard.py + test_heiler_resolved.py gruen im Gate
- Schild-Guard gruen (S7 Kommentar aktualisiert, Punkt 23)
</verification>

<success_criteria>
- F2 abgeklemmt ohne Deadlock (not_gradable terminal, _pending_events drainet auf 0)
- build_intent_payload() ausgelagert + Whitelist-Test scharf
- TTFT am echten Knopf-Pfad gemessen
- Heiler loest haengende resolved-Flags terminal auf: inline (kuenftig) + einmaliger Boot-Sweep (Alt-Calls, S2), kein periodischer Zeit-Sweep
- models.py:791 handling_status-Schild nennt not_gradable (S7, Punkt 23)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-02-SUMMARY.md`
</output>
</content>
</output>
