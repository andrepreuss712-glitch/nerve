---
phase: 08.23.2.PERSID
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - services/deepgram_service.py
  - services/prompt_pipeline.py
  - services/claude_service.py
  - routes/app_routes.py
  - services/live_session.py
  - tests/test_ewb_rate_api.py
autonomous: true
requirements: [SPEC-Req-6]

must_haves:
  truths:
    - "session_anrede liegt an EINER kanonischen per-sid-Stelle (_session_state[sid]['session_anrede'], top-level, LAZY erzeugt — es gibt KEINEN top-level-Seed; der Seed :356 liegt im 'state'-SUBDICT und ist tot, W-1); ALLE Reader + BEIDE Writer (Start + manueller Toggle) im selben Deploy; Case normalisiert (B2). ACHTUNG: claude_service.py:1481 liest HEUTE aus dem 'state'-SUBDICT (_sid_st_state) — wird auf top-level migriert."
    - "mic_muted liegt pro sid; jede Session sieht nur ihren eigenen Mute-Zustand"
    - "precall_briefing-Reader in api_beenden liest per-SID (_briefing), Modul-state-Key ist weg"
    - "_beenden_sid ist als geteilter Aufloesungs-Vertrag definiert — bevorzugt exakt via posted call_id (S3), sonst request-Kontext, sonst user_id-Scan; von Plan 05/06 mitbenutzt"
    - "_ended_session_snapshots-Fundament existiert: BEIDE Beenden-Naht-Pops (stop_live_session :779 UND handle_disconnect :815) stashen den State (TTL) statt sofort zu poppen; der stash SKIPT leeren/fehlenden State (N-1, gegen den setdefault :810-811 im disconnect-Handler) und ist first-stash-wins; der reconnect-re-init-pop :674 bleibt RAW-pop (EXEMPT, re-init sofort danach); api_beenden konsumiert den Snapshot bevorzugt via EINEM PEEK (N-3, nicht-destruktiv) — kein Datenverlust bei normalem Hangup (stop_live_session-vor-beenden) NOCH bei disconnect-vor-beenden (B1)"
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "session_anrede-Start-Write per-sid (top-level, NACH init_session_state :675 — N-4); mic_muted per-sid; stop_live_session (:779) UND handle_disconnect (:815) stashen Snapshot; reconnect-pop :674 RAW/EXEMPT (B1)"
    - path: "services/live_session.py"
      provides: "_ended_session_snapshots + stash (Leer-Skip+first-wins, N-1) / consume (PEEK, N-3)-Helfer (B1); session_anrede-Case-Normalisierung; tote 'state'-Subdict-Seeds session_anrede/mic_muted geloescht (W-1)"
    - path: "routes/app_routes.py"
      provides: "session_anrede/precall_briefing per-sid Reads in api_beenden; die ZWEI Direkt-Reads :172-177 (Redeanteil/Tempo/Monolog) + :193-207 (word_confidences) auf den Snapshot-Pfad umgezogen (N-2); GENAU EIN _load_beenden_state-Aufruf am Kopf (N-3); _beenden_sid-Aufloesungs-Vertrag (exakt via call_id, S3); Snapshot-Consumption (B1)"
  key_links:
    - from: "deepgram_service.py:675 (NACH init_session_state, N-4)"
      to: "_session_state[sid]['session_anrede']"
      via: "per-sid Start-Write (top-level, gleiche Stelle wie manueller Toggle :827; Muster session_start_time :687-690)"
      pattern: "_session_state\\[.*\\]\\['session_anrede'\\]"
    - from: "app_routes.py (session_anrede-Read via _bs)"
      to: "_bs.get('session_anrede') / Snapshot"
      via: "per-sid Read aus dem EINEN _load_beenden_state-Peek (N-3)"
      pattern: "session_anrede"
    - from: "deepgram_service.py:779 (stop_live_session) + :815 (handle_disconnect)"
      to: "_ended_session_snapshots[sid]"
      via: "stash-vor-pop an BEIDEN Beenden-Naehten (B1, TTL, Leer-Skip N-1); :674 bleibt RAW-pop"
      pattern: "_ended_session_snapshots"
---

<objective>
**Familie A — CONFIG + das B1-Fundament** (D-07 Datenfamilie-Schnitt). Diese Welle loest das Anrede-Split-Brain (SPEC Req 6) UND legt das `_ended_session_snapshots`-Fundament (B1), BEVOR ab dieser Welle der erste per-sid-Reader in api_beenden live geht.

**B2 — Anrede-Kollaps (Fresh-Grep 2026-07-03, korrigiert die urspruengliche Plan-03-Regression):**
Es gibt HEUTE genau EINE kanonische per-sid-Stelle: `_session_state[sid]['session_anrede']` (TOP-LEVEL). **W-1-Korrektur (Fresh-Grep 2026-07-03):** diese top-level-Stelle wird NICHT geseedet — der `'session_anrede': None`-Seed live_session.py:356 liegt im **'state'-SUBDICT** (der `'state': {...}`-Block oeffnet live_session.py:330, alle Zeilen :331-368 sind SUBDICT, inkl. `mic_muted` :351). Die kanonische top-level-Stelle entsteht LAZY beim ersten per-sid-Write. Die Reader nutzen `.get('session_anrede')`/`.get(sid, {}).get('session_anrede')` mit Default → Mechanik ueberlebt, ABER der tote Subdict-Seed :356 ist eine Ebenen-Falle → in Task 2 loeschen (W-1). Sie wird bereits von DREI top-level-Readern gelesen:
- prompt_pipeline.py:451 (Lead-Kontext-Block) + prompt_pipeline.py:500 (`_resolve_anrede` Hot-Path)
- deepgram_service.py:511 (`_current_anrede`)
und vom manuellen Toggle-Writer bereits korrekt geschrieben: deepgram_service.py:827 (`ls._session_state[sid]['session_anrede'] = anrede`, lowercase 'du'/'sie').
**KAPUTT sind (W-a resolved, Fresh-Grep):**
1. der Start-Writer deepgram_service.py:619 schreibt in die GLOBALE `ls.state['session_anrede']` (title-case 'Du'/'Sie') → erreicht die per-sid-Reader NIE (Split-Brain, SPEC Req 6);
2. der app_beenden-Reader app_routes.py:340 liest die tote globale `ls.state.get('session_anrede')`;
3. der `_resolve_anrede`-Fallback prompt_pipeline.py:507-518 liest die tote globale `ls.state['session_anrede']`;
4. **claude_service.py:1481 liest aus dem 'state'-SUBDICT, NICHT top-level (W-a RESOLVED via Fresh-Grep):** :1476 setzt `_sid_st_state = _sid_st.get('state') or {}`, :1481 `_anrede = _sid_st_state.get('session_anrede') or 'Sie'` → liest `_session_state[sid]['state']['session_anrede']` (SUBKEY). Das ist eine ZWEITE, falsche Ebene — genau die B2-Level-Mismatch-Klasse. → :1481 auf die KANONISCHE TOP-LEVEL-Stelle migrieren (`_sid_st.get('session_anrede')`, nicht `_sid_st_state`).

**N-4 — Start-Anrede-Write RUNS BEFORE init_session_state → Anrede erneut verloren (Fresh-Grep 2026-07-03, PFLICHT):** Der Start-Anrede-Write liegt **deepgram_service.py:617-620** (Fresh-Grep bestaetigt: `if anrede_norm in ('Du', 'Sie'): with ls.state_lock: ls.state['session_anrede'] = anrede_norm` :617-619 — Fable-Anker :617-620 EXAKT korrekt), das ist **~58 Zeilen VOR `init_session_state` (deepgram_service.py:675**, Fresh-Grep: `ls.init_session_state(_sid, ...)` :675). `init_session_state` ueberschreibt den kompletten per-sid-Dict (`_session_state[sid] = {...}` live_session.py:306). Wuerde man den Start-Write bloss "in-place per-sid" ersetzen (an :619, VOR init): frische Session → sid noch nicht in `_session_state` → Ghost-SID-Guard droppt still; Reconnect → Write gelingt, wird vom :674-pop + :675-init sofort weggewischt. Die B2-Kern-Regression BLIEBE, nur gruener. → **Fix: Anrede in ein LOCAL capturen (an :615-616, das `anrede_norm` existiert schon), den per-sid-Write NACH `init_session_state` (:675) verschieben — EXAKT das Muster von `session_start_time` (Fresh-Grep :687-690: `with ls._session_state_lock: _ss_start = ls._session_state.get(_sid); if _ss_start is not None: _ss_start['session_start_time'] = ...`) UND der Briefing-Bridge (Fresh-Grep :713-714: `if precall_briefing ...: ls.set_briefing_for_sid(_sid, precall_briefing)`), die BEIDE aus genau diesem Grund NACH init sitzen.** Der globale Write an :617-619 wird entfernt (nicht in-place ersetzt). Case-Normalisierung dabei (siehe Task 2a).

**mic_muted / precall_briefing** wie gehabt (Req 6 / §6.9).

**_beenden_sid-Aufloesungs-Vertrag (geteilt mit Plan 05/06) — S3-verschaerft:** api_beenden (HTTP-Route app_routes.py:99, KEIN Socket-sid) loest die sid auf. FRESH-GREP: das EXAKTE Aufloesungs-Muster existiert schon fuer `_phase_d_call_id` (app_routes.py:194-207: bevorzugt posted `call_id` → matcht `_session_state[sid]['state']['call_id']` :198, sonst user_id :203-204). Dieser Vertrag reused genau dieses Muster fuer `_beenden_sid`: **bevorzugt die per posted `call_id` exakt aufgeloeste sid (S3), sonst user_id-Scan** (:154-162). Das schliesst "ein User, zwei gleichzeitige Sessions" schon ab dieser Welle (statt erst Welle 5).

**B1 — normal-hangup-UND-disconnect-vor-beenden = DATENVERLUST (Fresh-Grep 2026-07-03 bestaetigt, DREI pop-Stellen, KEIN Drift zur Checker-Liste :674/:779/:815):** Es gibt in deepgram_service.py DREI `pop_session_state(_sid)`-Stellen:
- **:674** (reconnect re-init: `if _sid in ls._session_state: ls.pop_session_state(_sid)` direkt VOR `ls.init_session_state(...)` :675) — RAW-pop, **EXEMPT** (siehe unten).
- **:779** (`stop_live_session`-Handler) — poppt den kompletten per-sid-State.
- **:815** (socket.io `disconnect`-Handler) — poppt den kompletten per-sid-State.

**N-1 — Doppel-Feuer-Race (Fresh-Grep 2026-07-03):** `handle_disconnect` beginnt mit einem UNBEDINGTEN `ls._session_state.setdefault(_sid, {})` **deepgram_service.py:810-811** (Fresh-Grep bestaetigt EXAKT: `with ls._session_state_lock: ls._session_state.setdefault(_sid, {})`) VOR dem pop :815. Feuert `stop_live_session` (:779, voller Stash) und direkt danach `handle_disconnect` (Tab-/PiP-Zu), erzeugt der setdefault ein LEERES `{}` → ein naiver zweiter Stash ueberschriebe den vollen Snapshot mit LEER → leerer Call-Record. → der Stash-Helfer MUSS leeren/fehlenden State SKIPPEN + first-stash-wins sein (Task 1a).

**Der eigentliche Normal-Hangup-Pfad geht ueber :779, NICHT :815** (Fresh-Grep static/pip-launcher.js): `endCall()` (:3094) ruft `_stopMic()` (:3096); `_stopMic()` (:3029) `emit('stop_live_session')` (:3034); DANN `fetch('/api/beenden')` (:3106). Der Socket bleibt dabei VERBUNDEN — `disconnect` (:815) feuert bei einem normalen Beenden NIE. Also poppt `stop_live_session` (:779) den vollen per-sid-State BEVOR api_beenden (app_routes.py:99) liest → `_load_beenden_state` findet weder lebende Session noch Snapshot → LEERER Call-Record bei JEDEM normalen Auflegen (nicht nur beim Netz-Blip-Edge-Case). Wuerde nur :815 gestasht, waere die Migration schlimmer als der Ist-Zustand.

Zusaetzlich der Netz-Blip-Edge-Case: reisst die Verbindung VOR dem Beenden-Klick (PiP/Tab zu, Netz weg), feuert `disconnect` (:815) und poppt → derselbe Verlust. Die eigenen Code-Kommentare belegen die Race real auf Prod (app_routes.py:166-171: "der WebSocket-disconnect-Handler räumt _session_state[sid] vor dem späten Persistenz-Punkt ab").

→ **BEIDE Beenden-Naht-Pops (:779 UND :815) MUESSEN stash-vor-pop werden** (identische Behandlung, mit Leer-Skip N-1). Nur :674 bleibt RAW-pop: er re-initialisiert die Session SOFORT danach (`init_session_state`), es findet kein api_beenden-Read gegen diese sid statt → ein Snapshot waere sinnlos (wuerde vom folgenden init sofort ueberschrieben). :674 wird ausdruecklich als EXEMPT dokumentiert. Dieses Fundament MUSS her, BEVOR ab hier per-sid in api_beenden gelesen wird.

**N-2/N-3 — die api_beenden-Reads muessen ALLE ueber EINEN Snapshot-faehigen Peek (siehe Task 1c):** die ZWEI heute noch DIREKTEN `_session_state`-Reads in api_beenden (Fresh-Grep: :172-177 Speech-Stats/Redeanteil/Tempo/Monolog UND :193-207 word_confidences+call_id) tragen "K1-Fix"/"Phase-D frueh"-Kommentare und SEHEN gefixt aus, lesen aber `_session_state` direkt → nach dem :779-Stash lesen sie None/[] → Redeanteil/Tempo/Monolog=0 bei JEDEM Hangup, word_confidences=[] → Audio-Health-Thread startet nie. Sie werden explizit auf den Snapshot-Pfad umgezogen (N-2). Und es gibt GENAU EINEN `_load_beenden_state`-Aufruf am Kopf (N-3), NON-destruktiver Peek, `_bs` durchgereicht.

**AUS Familie A ausgeklammert:** `aktives_skript_inhalt`/`skript_bloecke` — Delete-Kandidaten (RESEARCH §6.4/A1); FIX-vs-DELETE-Verdikt aus Plan 01 Task 1. **W-b Guard-Placement (Fresh-Grep-Vorentscheidung, RESEARCH §6.4/A1 lehnt DELETE):** falls Plan-01-Verdikt = FIX, MUSS skript einen `_PENDING_MIGRATION`-Eintrag unter Welle-A-Tag bekommen (Task 3d) — sonst steht der statische Waechter in einem undefinierten dritten Zustand. Falls DELETE, schon weg.

Purpose: Erste echte per-sid-Familie live + kein Datenverlust bei normalem Hangup ODER disconnect-vor-beenden.
Output: session_anrede kanonisch per-sid (kein Split-Brain, claude:1481 vom Subdict auf top-level, Start-Write NACH init N-4), mic_muted + precall_briefing per-sid, _beenden_sid-Vertrag (exakt via call_id), _ended_session_snapshots-Fundament (beide Beenden-Naht-Pops gestasht, Leer-Skip N-1, ein Peek N-3).

**Komplexitaet: 🔴 — Cross-AI PFLICHT. D-01 (raw-dict), D-03 (Lock-Disziplin), Punkt 14/26 (disconnect-Race) HART.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-SPEC.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-CONTEXT.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- KANONISCHE per-sid-Anrede-Stelle (existiert LAZY, top-level): _session_state[sid]['session_anrede'] -->
<!-- W-1: KEIN top-level-Seed. Der Seed live_session.py:356 ('session_anrede': None) liegt im 'state'-SUBDICT ('state':{...} oeffnet :330) → tot, in Task 2 loeschen -->

<!-- Reader (schon korrekt, top-level per-sid — bleiben, nur Case angleichen): -->
prompt_pipeline.py:451   _sid_state.get('session_anrede')                         # Lead-Kontext-Block
prompt_pipeline.py:500   ss.get(sid, {}).get('session_anrede')                    # _resolve_anrede Hot-Path
deepgram_service.py:511  ls_module._session_state.get(sid_ref, {}).get('session_anrede', 'sie')

<!-- Reader FALSCHE Ebene (W-a RESOLVED — liest SUBDICT, migrieren auf top-level): -->
claude_service.py:1476   _sid_st_state = _sid_st.get('state') or {}               # Subdict-Referenz
claude_service.py:1481   _anrede = _sid_st_state.get('session_anrede') or 'Sie'   # SUBKEY → auf _sid_st.get('session_anrede') (top-level)

<!-- Writer manueller Toggle (schon korrekt, top-level per-sid — bleibt, Case angleichen): -->
deepgram_service.py:825-827  ls._session_state.setdefault(sid, {}); ls._session_state[sid]['session_anrede'] = anrede  # lowercase 'du'/'sie'

<!-- KAPUTTE Stellen (in DIESEM Deploy fixen): -->
deepgram_service.py:617-619  ls.state['session_anrede'] = anrede_norm   # GLOBAL Start-Write, VOR init :675 → ENTFERNEN + per-sid-Write NACH init (N-4)
app_routes.py:340        ls.state.get('session_anrede')             # tote GLOBALE → per-sid via _bs (N-3)
prompt_pipeline.py:507-518  _resolve_anrede Fallback liest ls.state['session_anrede']  # tote GLOBALE → LOESCHEN
claude_service.py:1481   liest 'state'-Subdict statt top-level      # W-a → auf top-level

<!-- N-4 Analog-Muster (BEIDE sitzen NACH init_session_state :675, aus genau diesem Grund) -->
deepgram_service.py:687-690  session_start_time-Write (with _session_state_lock; _ss_start=get(_sid); if not None: ...)
deepgram_service.py:713-714  set_briefing_for_sid(_sid, precall_briefing)   # Briefing-Bridge NACH init

<!-- Zugriffsmuster D-01 (raw-dict unter Lock) -->
with ls._session_state_lock:
    if _sid in ls._session_state:
        ls._session_state[_sid]['session_anrede'] = _anrede_norm      # WRITE (top-level, Case-normalisiert, NACH init)

<!-- Briefing schon per-sid (live_session.py): set_briefing_for_sid / get_briefing_for_sid -->

<!-- mic_muted (RESEARCH §1) -->
deepgram_service.py:856  handle_mute_mic → ls.state['mic_muted'] = ...  # WRITE (global) → per-sid
deepgram_service.py:233  ls.state.get('mic_muted')                      # READ (global) → per-sid
# W-1: Seed live_session.py:351 'mic_muted': False liegt im 'state'-SUBDICT (:330-368), NICHT top-level — toter Subdict-Seed → mit-loeschen/nachziehen (Task 3)

<!-- _beenden_sid (S3): exaktes call_id-Muster existiert app_routes.py:194-207 (_phase_d_call_id) -->
<!-- N-1: handle_disconnect setdefault deepgram_service.py:810-811 VOR pop :815 → Stash muss Leer-Skip -->
<!-- B1: DREI pop-Stellen deepgram_service.py — :674 (reconnect, RAW/EXEMPT), :779 (stop_live_session, STASH), :815 (disconnect, STASH); api_beenden app_routes.py:99 liest danach -->
<!-- Normal-Hangup-Pfad: pip-launcher.js endCall :3094 → _stopMic :3029 emit('stop_live_session') :3034 → fetch('/api/beenden') :3106 (Socket bleibt verbunden → :815 feuert NICHT → :779 ist die relevante Stash-Stelle) -->
<!-- N-2: ZWEI Direkt-Reads in api_beenden — :172-177 (bw/kw/session_start_time/laengster_monolog → Redeanteil/Tempo/Monolog) + :193-207 (word_confidences+call_id) lesen _session_state DIREKT → auf _bs umziehen -->
<!-- N-3: consume_ended_session = PEEK (nicht-destruktiv); GENAU EIN _load_beenden_state am Kopf; _bs durchgereicht; Snapshot-Pop erst in reset_session (Plan 06) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B1 _ended_session_snapshots-Fundament (BEIDE Beenden-Naht-Pops :779+:815, Leer-Skip N-1, EIN Peek N-3) + N-2 Direkt-Reads umziehen + _beenden_sid-Vertrag (S3, exakt via call_id)</name>
  <read_first>
    - services/deepgram_service.py:773-779 (stop_live_session-Handler — `ls.pop_session_state(_sid)` :779, poppt kompletten State; `_close_deepgram_connection(_sid)` :778)
    - services/deepgram_service.py:805-815 (handle_disconnect — UNBEDINGTER `ls._session_state.setdefault(_sid, {})` :810-811 VOR `ls.pop_session_state(_sid)` :815; `_chunk_counts.pop` :813; `_close_deepgram_connection` :814) — N-1-Anker
    - services/deepgram_service.py:670-676 (reconnect re-init — `if _sid in ls._session_state: ls.pop_session_state(_sid)` :673-674 direkt vor init_session_state :675; RAW/EXEMPT, NICHT anfassen)
    - static/pip-launcher.js:3029-3040 (_stopMic emit('stop_live_session') :3034) + :3094-3114 (endCall → _stopMic :3096 → fetch('/api/beenden') :3106) — belegt: normaler Hangup poppt via :779, NICHT :815
    - services/live_session.py:438-455 (pop_session_state — was genau entfernt wird)
    - routes/app_routes.py:99-210 (api_beenden Kopf; DIREKT-Read #1 Speech-Stats :172-177 (bw/kw :174-175, session_start_time :176, laengster_monolog :177) → Redeanteil/Tempo/Monolog :178-183; user_id-Scan :154-162; DIREKT-Read #2 word_confidences+call_id :193-207 (call_id-exact :196-201, user_id-fallback :202-207); K1-Kommentar :150-171 + :185-190 belegt die Race) — N-2/N-3-Anker
    - RESEARCH.md §2 Familie A + §6.9 (precall_briefing) + §1
    - CLAUDE.md Punkt 14 + Punkt 26 (disconnect-vor-beenden = Bereitschafts-Naht; Leser darf Schreiber nicht verlieren)
  </read_first>
  <behavior>
    - Test 1 (B1 Snapshot-Konsum, NORMAL-HANGUP-Pfad): fuer sid A wird `stop_live_session` gefeuert (:779) → State wandert in _ended_session_snapshots[A] (NICHT sofort weg). Danach loest api_beenden fuer A den State aus dem Snapshot auf → Call-Record traegt A-Inhalt (kein leerer Record). DAS ist der Haupt-Pfad.
    - Test 1b (B1 Snapshot-Konsum, DISCONNECT-Pfad): dasselbe via handle_disconnect (:815) — Netz-Blip-Edge-Case.
    - **Test 1c (N-1 Doppel-Feuer): fuer sid A stop_live_session (:779, voller Stash) → dann handle_disconnect (:815, dessen setdefault :810-811 ein leeres {} erzeugt) → der Snapshot _ended_session_snapshots[A] bleibt VOLL (der leere zweite Stash ueberschreibt NICHT). api_beenden(A) → Call-Record NICHT-leer.**
    - Test 2 (B1 Ghost-Drop der Late-Writes): NACH dem stash darf ein spaeter Late-Write (record_ewb_click/record_suggestion_offer/anrede fuer eine tote sid) den Snapshot NICHT wiederbeleben (D-02: ohne lebende sid → No-Op).
    - **Test 2b (N-3 Peek nicht-destruktiv): zweimaliges `_load_beenden_state(A)` (bzw. consume_ended_session(A)) liefert BEIDE Male denselben Inhalt (kein Pop beim ersten Lesen) — Doppel-Beenden bleibt gutartig.**
    - Test 3 (S3 exakte Aufloesung): api_beenden mit posted call_id C loest exakt die sid auf, deren state['call_id']==C — auch wenn derselbe user zwei Sessions hat.
    - **Test 4 (N-2): nach stop_live_session(A)+Stash liest api_beenden(A) Redeanteil/Tempo/Monolog UND word_confidences aus dem Snapshot (nicht 0/[]) — der Audio-Health-Thread bekommt eine nicht-leere Basis.**
  </behavior>
  <action>
    a) **_ended_session_snapshots-Fundament (B1) in live_session.py:**
    - Neuer Modul-Container `_ended_session_snapshots: dict = {}` + `_ended_snapshots_lock = threading.Lock()`. **Whitelist-Eintrag (Plan 01 Waechter):** sid-gekeyt (`{sid: {'state': <kopie>, 'ts': monotonic()}}`) → per-sid-safe, gehoert in die _WHITELIST/Per-SID-Container-Liste des statischen Waechters. ASCII-Identifier (Constraint).
    - `stash_ended_session(sid)`: legt eine flache Kopie des `_session_state[sid]` (unter `_session_state_lock`) unter `_ended_session_snapshots[sid] = {'state': <kopie>, 'ts': monotonic()}` (unter `_ended_snapshots_lock`), DANN `pop_session_state(sid)`. TTL: beim Stashen + beim Consume alte Eintraege (`monotonic() - ts > _SNAPSHOT_TTL_S`, z.B. 300s) verwerfen (lazy sweep, kein Timer).
    - **N-1 — LEER-/FEHLEND-SKIP + first-stash-wins (Fresh-Grep 2026-07-03, gegen setdefault :810-811, PFLICHT — BEIDES):**
      1. **Leer-/fehlend-Skip:** `if not ls._session_state.get(sid): return` VOR dem Kopieren — ein leeres `{}` (das setdefault-Artefakt aus handle_disconnect :810-811) wird NICHT gestasht.
      2. **first-stash-wins:** ist unter `sid` bereits ein TTL-frischer Snapshot vorhanden, NICHT ueberschreiben (`if sid in _ended_session_snapshots and monotonic() - _ended_session_snapshots[sid]['ts'] <= _SNAPSHOT_TTL_S: return`) — der erste (volle) Snapshot gewinnt gegen jeden spaeteren (potentiell leeren) Stash. So ueberlebt der volle :779-Snapshot, egal ob :815 danach mit leerem setdefault-Dict nachfeuert. Doppel-Stash crasht nie.
    - `consume_ended_session(sid) -> dict|None`: **N-3 — NICHT-DESTRUKTIVER PEEK (nicht pop):** liest den Snapshot unter `_ended_snapshots_lock` mit TTL-Check, gibt die gestashte state-Kopie ODER None zurueck — **entfernt den Eintrag NICHT** (mehrfaches Lesen derselben sid liefert denselben Inhalt). So bleibt Doppel-Beenden so gutartig wie heute. Aufraeumen: (i) lazy via TTL-Sweep, (ii) EXPLIZITER Snapshot-Pop am ENDE (in `reset_session(_beenden_sid)`, Plan 06 Task 2c) — ein Helfer `pop_ended_session(sid)` (bzw. `_ended_session_snapshots.pop(sid, None)` unter Lock) entfernt ihn dort final.
    b) **BEIDE Beenden-Naht-Pops auf stash-vor-pop (B1) — :779 UND :815:**
    - **stop_live_session (deepgram_service.py:779):** statt `ls.pop_session_state(_sid)` → `ls.stash_ended_session(_sid)`. Haupt-Pfad. `_close_deepgram_connection(_sid)` (:778) bleibt davor.
    - **handle_disconnect (deepgram_service.py:815):** statt `ls.pop_session_state(_sid)` → `ls.stash_ended_session(_sid)`. Der setdefault :810-811 + `_chunk_counts.pop` (:813) + `_close_deepgram_connection` (:814) bleiben — der Leer-Skip (N-1, Task 1a) faengt das leere setdefault-Dict ab.
    - **reconnect re-init (deepgram_service.py:674): NICHT anfassen** — bleibt `ls.pop_session_state(_sid)` (RAW). Ein-Zeilen-Kommentar (`# B1-EXEMPT: re-init folgt sofort (init_session_state :675), kein beenden-Read gegen diese sid → kein Snapshot noetig`).
    c) **api_beenden konsumiert Snapshot bevorzugt via GENAU EINEM Peek (B1 + N-3):** Baue einen Helfer `_load_beenden_state(sid) -> dict|None` (in api_beenden oder live_session), der zuerst die lebende Session (`_session_state[sid]`), sonst den Snapshot (`consume_ended_session(sid)` — der PEEK, N-3) liefert. **GENAU EIN Aufruf am KOPF von api_beenden:** `_bs = _load_beenden_state(_beenden_sid)` — direkt NACH der `_beenden_sid`-Aufloesung, VOR allen Reads. Dieses EINE `_bs` wird durch ALLE downstream-Reads (dieser Plan + Plan 05 + Plan 06) gereicht (INKL. als Param an `_build_log_content`, Plan 05 Task 2b). Kein zweiter `_load_beenden_state`-Aufruf (N-3 — sonst None beim zweiten Peek waere zwar durch die Nicht-Destruktivitaet abgefedert, aber EIN Aufruf ist die klare Invariante). Acceptance: grep zeigt GENAU EINEN `_load_beenden_state(`-Aufruf in app_routes.py.
    d) **N-2 — die ZWEI heute DIREKTEN _session_state-Reads in api_beenden auf `_bs` umziehen (Fresh-Grep 2026-07-03, PFLICHT):**
    - **Direkt-Read #1 — Speech-Stats app_routes.py:172-177:** heute `with ls._session_state_lock: _ss_b = ls._session_state.get(_beenden_sid) ...; bw=:174; kw=:175; _st=:176; _monolog=:177`. → auf `_bs` umziehen: `bw = _bs.get('berater_words', 0) if _bs else 0`, analog kw/`session_start_time`/`laengster_monolog_sek`. Die Redeanteil/Tempo/Monolog-Berechnung (:178-183) bleibt, liest jetzt aus `_bs`. Ohne diese Umstellung: nach dem :779-Stash liest der Direkt-Read leer → Redeanteil/Tempo/Monolog=0 bei JEDEM Hangup.
    - **Direkt-Read #2 — word_confidences+call_id app_routes.py:193-207:** heute scannt der Block `ls._session_state.items()` direkt (call_id-exact :196-201, user_id-fallback :202-207) und liest `_sd.get('word_confidences', [])`. → die call_id/user_id-Aufloesung teilt sich den `_beenden_sid`-Vertrag (siehe e), und `_phase_d_word_confidences` wird aus `_bs.get('word_confidences', [])` gelesen (Snapshot-faehig). Ohne diese Umstellung: word_confidences=[] nach Stash → Audio-Health-Thread startet nie → der Plan-02-Heiler setzt still `audio_health_resolved=True` auf leerer Basis (Punkt 26).
    - Acceptance-grep: NACH der Migration liest KEINER dieser beiden Bloecke mehr `ls._session_state[` bzw. `ls._session_state.get(_beenden_sid)`/`.items()` direkt fuer diese Felder — beide gehen ueber `_bs`.
    e) **Ghost-Drop der Late-Writes (B1, Test 2):** die record_*/anrede-Writes bleiben strikt `if sid in _session_state` (Ghost-SID-Guard, D-02) — sie schreiben NIE in `_ended_session_snapshots`. Ein Late-Write nach stash trifft eine tote sid → No-Op.
    f) **_beenden_sid-Vertrag (S3) festschreiben:** loese `_beenden_sid` in DIESER Reihenfolge auf (als EIN benannter, dokumentierter Block, den Plan 05/06 + der N-2-Block #2 referenzieren):
       1. **exakt via posted call_id (S3, bevorzugt):** wie das existierende `_phase_d_call_id`-Muster (app_routes.py:194-207) — posted `call_id` → die sid, deren lebende ODER gestashte `state['call_id']` matcht.
       2. sonst user_id-Scan (:154-162, K1-Muster: max session_start_time des eigenen user_id) — jetzt auch ueber Snapshots.
       3. keine → None (D-02, kein Raten).
    Verdikt (call_id-first) im SUMMARY. Der bestehende `_phase_d_call_id`-Block (:193-207) und `_beenden_sid` teilen dieselbe Aufloesung (nicht zwei divergente Scans) — vereinheitlichen; der word_confidences-Read haengt sich an `_bs` (N-2 #2).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Browser-UAT: (1) NORMAL: Live-Anruf starten, dann normal Beenden klicken → Call-Record traegt den Gespraechs-Inhalt (NICHT leer) + Redeanteil/Tempo/Monolog != 0 (N-2 #1) — belegt den :779-Stash-Pfad. (2) NETZ-BLIP: Live-Anruf starten, Netzwerk kurz trennen (DevTools offline) ODER Tab in den Hintergrund → Verbindung reisst → dann Beenden → Record ebenfalls NICHT leer (:815-Pfad). (3) DOPPEL-FEUER: normal Beenden, sofort Tab schliessen → Record bleibt voll (N-1). Prod-Beleg: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample conversation_logs 5'`.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "_ended_session_snapshots\|stash_ended_session\|consume_ended_session\|pop_ended_session" services/live_session.py` → Container + stash/consume(PEEK)/pop-Helfer existieren; TTL-Konstante vorhanden
    - **N-1: `stash_ended_session` enthaelt den Leer-/Fehlend-Skip (`if not ls._session_state.get(sid): return`) UND first-stash-wins (kein Ueberschreiben eines TTL-frischen Snapshots) — grep-belegt im Helfer-Body**
    - **N-3: `consume_ended_session` ist ein PEEK (kein `.pop(` auf `_ended_session_snapshots` in consume); der einzige `.pop(`/`pop_ended_session` auf dem Snapshot-Container steht in reset_session (Plan 06)**
    - **`grep -n "stash_ended_session" services/deepgram_service.py` → ZWEI Treffer (stop_live_session ~:779 UND handle_disconnect ~:815) stashen**
    - **`grep -n "pop_session_state" services/deepgram_service.py` → das EINZIGE verbleibende raw `pop_session_state` ist der reconnect-re-init-pop (~:674, mit B1-EXEMPT-Kommentar); WEDER der stop_live_session- NOCH der handle_disconnect-Handler enthaelt noch ein raw pop_session_state**
    - **N-3: `grep -n "_load_beenden_state(" routes/app_routes.py` → GENAU EIN Aufruf (am Kopf); `_bs` durch alle Reads gereicht**
    - **N-2: die zwei Bloecke ~:172-177 (Speech-Stats) + ~:193-207 (word_confidences+call_id) lesen NICHT mehr `ls._session_state.get(_beenden_sid)`/`.items()` direkt fuer diese Felder — grep-belegt beide ueber `_bs`**
    - **S3: `_beenden_sid` wird bevorzugt via posted call_id aufgeloest (Muster app_routes:194-207 wiederverwendet), user_id-Scan nur Fallback; als benannter geteilter Block dokumentiert (Plan 05/06 referenzieren ihn)**
    - Ghost-Drop: record_*/anrede-Writes bleiben `if sid in _session_state` (kein Snapshot-Write) — Test 2 gruen
    - _ended_session_snapshots in der Waechter-Whitelist (Per-SID-Container) — statischer Waechter (Plan 01) bleibt gruen
    - deploy.sh production GRUEN
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: session_anrede-Kollaps auf EINE kanonische per-sid-Stelle (B2, Start-Write NACH init N-4, claude:1481-Subdict-Fix, tote Subdict-Seeds W-1) + precall_briefing-Reader per-sid + Anrede-Test W-2</name>
  <read_first>
    - services/deepgram_service.py:610-643 (session_anrede-Start-Write GLOBAL :617-619; anrede_norm-Berechnung/Case :615-616 — das Local existiert schon), :675 (init_session_state), :687-690 (session_start_time-Write NACH init — N-4-Analog), :713-714 (Briefing-Bridge NACH init — N-4-Analog), :817-828 (manueller Toggle-Writer :827 top-level per-sid, lowercase)
    - services/prompt_pipeline.py:445-460 (Reader :451), :487-520 (`_resolve_anrede`: Hot-Path per-sid :500, Fallback global :507-518 — LOESCHEN)
    - services/deepgram_service.py:508-512 (Reader :511)
    - services/claude_service.py:1473-1483 (W-a RESOLVED: :1476 `_sid_st_state = _sid_st.get('state') or {}`; :1481 `_anrede = _sid_st_state.get('session_anrede') or 'Sie'` liest aus dem 'state'-SUBDICT → auf `_sid_st.get('session_anrede')` top-level migrieren; `_sid_st` ist im Scope :1475)
    - routes/app_routes.py:335-342 (session_anrede-Read :340 GLOBAL → per-sid via `_bs`), :377 (anrede= im ConversationLog-Insert)
    - routes/app_routes.py:105-118 (precall_briefing-Fallback :110-114 GLOBAL → per-sid via get_briefing_for_sid / `_bs`)
    - services/live_session.py:255-268 (set_briefing_for_sid/get_briefing_for_sid), :330 ('state':{...}-Block oeffnet), :356 (session_anrede-Seed im SUBDICT — tot, W-1 loeschen), :141 (Modul-state['precall_briefing']-Key), :922 (Reset)
    - tests/test_ewb_rate_api.py:290-411 (W-2: `_apply_anrede_whitelist` :309-317 Prod-Replika schreibt globale `ls.state['session_anrede']` title-case; Tests :320-383 asserten title-case 'Du'/'Sie'; autouse-Fixture :292-306 poppt `ls.state`) — diese Replika testet den B2-Bug GRUEN → migrieren/entschaerfen
    - RESEARCH.md §2 Familie A + §6.9
    - CLAUDE.md Punkt 22 (grep-Vollstaendigkeit: JEDE session_anrede-Stelle klassifiziert)
  </read_first>
  <behavior>
    - Test 1 (Split-Brain geschlossen, gepaart): Start-Write anrede fuer sidA='du', sidB='sie' → die per-sid-Reader (prompt_pipeline._resolve_anrede) lesen fuer sidA 'du', sidB 'sie' (Isolation). Der Start-Wert erreicht jetzt den Prompt (frueher NIE).
    - **Test 1b (N-4 — Start-Write ueberlebt init): der Start-Anrede-Write laeuft NACH init_session_state → der Wert bleibt (init hat den Dict schon neu gebaut, danach der Write). Reconnect-Szenario: nach :674-pop + :675-init traegt _session_state[sid]['session_anrede'] den Start-Wert (nicht None).**
    - Test 2 (Case-Konsistenz): Start-Writer und Toggle-Writer schreiben denselben Case (lowercase); ein Reader-Vergleich/Display ist stabil (kein 'Du' vs 'du'-Drift).
    - Test 3 (Fallback tot): nach Loeschen des prompt_pipeline:507-518-Fallbacks liest _resolve_anrede NUR noch per-sid (+ ki.ansprache/'Sie'-Default).
    - Test 3b (claude:1481 top-level, W-a): der claude-Reader liest fuer sidA den top-level 'du' — NICHT mehr aus dem 'state'-Subdict.
    - Test 4: get_briefing_for_sid(sidA) liefert das fuer sidA gesetzte Briefing; sidB sein eigenes / None.
  </behavior>
  <action>
    a) **N-4 — Start-Anrede-Write NACH init_session_state verschieben (Fresh-Grep 2026-07-03, PFLICHT):**
    - **Local capturen:** an :615-616 existiert `anrede_norm` bereits (`anrede_norm = anrede_raw.strip().title() if ... else None`). Behalte die Berechnung; das ist das Local.
    - **Globalen Write ENTFERNEN:** den Block :617-619 (`if anrede_norm in ('Du','Sie'): with ls.state_lock: ls.state['session_anrede'] = anrede_norm`) loeschen (NICHT in-place per-sid ersetzen — der Ort ist VOR init :675, ein per-sid-Write dort wuerde vom :674-pop+:675-init weggewischt / bei frischer Session vom Ghost-Guard gedroppt).
    - **Per-sid-Write NACH init einfuegen:** direkt beim Muster von `session_start_time` (:687-690) / Briefing-Bridge (:713-714) — also im `try`-Block NACH `ls.init_session_state(...)` (:675). Case-Normalisierung auf lowercase (kanonisch, wie Toggle :827):
    ```python
    # NACH init_session_state (Muster session_start_time :687-690 / Briefing-Bridge :713-714)
    _anrede_norm = (anrede_norm or '').lower()   # anrede_norm ist das Local von :615-616 ('Du'/'Sie'/None)
    if _anrede_norm in ('du', 'sie'):
        with ls._session_state_lock:
            if _sid in ls._session_state:
                ls._session_state[_sid]['session_anrede'] = _anrede_norm
        print(f"[Phase08] session_anrede={_anrede_norm} set per-sid={_sid} (start, nach init)")
    ```
    Test-Target: gegen den ECHTEN Handler (start_live_session mit gemocktem init) — NICHT gegen `tests/test_ewb_rate_api._apply_anrede_whitelist` (Prod-Replika, die den Bug gruen testen wuerde, siehe f).
    b) **Toggle-Writer :827 an Case angleichen:** schreibt schon lowercase per-sid — nur bestaetigen (bleibt). Falls er 'Du'/'Sie' schriebe → auf lowercase.
    c) **Fallback prompt_pipeline.py:507-518 LOESCHEN (B2):** der gesamte `# Fallback: legacy global state`-Zweig (`state = getattr(ls, 'state', ...)` bis `return session_anrede`) liest die tote globale `ls.state['session_anrede']` → ersatzlos entfernen. `_resolve_anrede` faellt auf `ki.ansprache` / `'Sie'` zurueck. Docstring (:488-491) anpassen.
    c2) **claude_service.py:1481 vom 'state'-Subdict auf top-level migrieren (B2, W-a):** ersetze `_sid_st_state.get('session_anrede')` durch `_sid_st.get('session_anrede')` (top-level, `_sid_st` :1475 im Scope). `_sid_st_state` bleibt fuer die anderen Subdict-Reads (:1480/:1482) — NUR die anrede-Zeile wechselt die Ebene.
    d) **Reader app_routes.py:340 auf per-sid via `_bs` (B2, N-3):** aus dem EINEN `_bs = _load_beenden_state(_beenden_sid)` (Task 1c, am Kopf) den top-level `session_anrede` lesen. Der ConversationLog-Insert :377 (`anrede=_session_anrede`) bekommt den korrekten per-sid-Wert (Toggle landet ENDLICH in ConversationLog.anrede — der zweite B2-Fix).
    ```python
    # _bs stammt aus dem EINEN _load_beenden_state am Kopf (Task 1c) — KEIN zweiter Aufruf (N-3)
    _session_anrede = (_bs.get('session_anrede') if _bs else None)
    ```
    e) **precall_briefing-Reader per-sid (§6.9):** app_routes.py:110-114 liest `ls.state.get('precall_briefing')` (Modul-global). Auf per-sid: aus `_bs` (`_bs.get('_briefing')`) bzw. get_briefing_for_sid(_beenden_sid) (Snapshot-faehig). Danach den Modul-`state['precall_briefing']`-Key (live_session.py:141) + Reset (:922) loeschen (0 Reader) + den DEPRECATED-GLOBAL-Marker aus Plan 01 Task 2 entfernen + aus der Waechter-Pending-Liste (Welle A) streichen.
    f) **W-2 — Anrede-Test-Bestand migrieren/entschaerfen (Fresh-Grep 2026-07-03, tests/test_ewb_rate_api.py):** die Datei enthaelt eine vacuous Global-Anrede-Replika:
    - `_apply_anrede_whitelist` (:309-317) spiegelt die ALTE Prod-Logik: `with ls.state_lock: ls.state['session_anrede'] = anrede_norm` (GLOBAL, title-case). Die Tests :320-383 asserten `ls.state.get('session_anrede') == 'Du'`/'Sie' (title-case), die autouse-Fixture :292-306 poppt `ls.state['session_anrede']`.
    - Unter der Migration (per-sid, lowercase 'du'/'sie', Start-Write NACH init) ist diese Replika ein FALSE-GREEN (sie testet den globalen Bug-Pfad und den title-case). → **ersetzen durch einen Test gegen den ECHTEN per-sid-Pfad:** entweder (i) den Start-Handler mit gemocktem init aufrufen und `_session_state[sid].get('session_anrede') == 'du'` (lowercase, per-sid) asserten, ODER (ii) die Whitelist-/Injection-Semantik gegen die REALE Normalisierung testen (lowercase-kanonisch, per-sid, NACH init). Die reinen Whitelist-Injection-Tests (rejects_invalid :365-371, rejects_partial :374-382) bleiben inhaltlich gueltig, muessen aber auf lowercase-per-sid statt title-case-global umgestellt werden. Die autouse-Fixture :292-306 (die `ls.state`-Global poppt) entfernen/auf per-sid-Cleanup umstellen. Kein Test darf mehr `ls.state['session_anrede']` (Global, title-case) als Wahrheit nehmen (CLAUDE.md Test-Qualitaets-Regel: Runtime-Verhalten des ECHTEN Pfades, kein Prod-Replika-False-Green).
    g) **W-1 — tote 'state'-Subdict-Seeds loeschen (Fresh-Grep 2026-07-03):** der Seed `'session_anrede': None` (live_session.py:356) liegt im 'state'-SUBDICT (`'state':{...}` oeffnet :330), NICHT top-level — die kanonische top-level-Stelle wird lazy erzeugt. Der Subdict-Seed ist tot (kein Reader liest `state['session_anrede']` nach c2) → aus dem Seed-Block loeschen (Ebenen-Falle beseitigen). Kein top-level-Seed noetig (die Reader nutzen `.get()`-Defaults) — falls im SUMMARY sauberer gewuenscht, EINEN top-level-Seed `'session_anrede': None` in init_session_state :306-329 ergaenzen; Subdict-Seed in JEDEM Fall weg.
    h) **grep-Vollstaendigkeit (Punkt 22):** `grep -rn "session_anrede" services/ routes/ tests/` → JEDER Treffer klassifiziert: kanonisch top-level per-sid (Writer NACH init + :827, Reader :451/:500/:511/:1481/app_routes via _bs) vs. entfernt (Fallback :507-518, globaler Start-Write :617-619, Subdict-Seed :356, Test-Replika). 0 verbliebene `ls.state['session_anrede']`/`ls.state.get('session_anrede')` (Prod), 0 `_sid_st_state.get('session_anrede')` (Subdict), 0 `state['session_anrede']`-Subdict-Seed.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Browser-UAT (2 Tabs, 2 Konten): Tab A setzt Anrede 'du' (Start ODER Toggle), Tab B 'sie'; im Live-Prompt/Coaching nutzt A durchgehend 'du', B 'sie' (Start-Anrede erreicht jetzt den Prompt — N-4). Nach Beenden: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample conversation_logs 10'` → anrede-Spalte pro Call korrekt (auch der manuelle Toggle landet jetzt drin).</manual>
  </verify>
  <acceptance_criteria>
    - **N-4: `grep -n "session_anrede" services/deepgram_service.py` → der Start-Write steht NACH `init_session_state` (grep die Zeilennummer > :675, im Muster von session_start_time :687-690); der globale `ls.state['session_anrede']`-Write bei :617-619 ist WEG; Wert lowercase; Toggle :827 unveraendert per-sid**
    - `grep -n "session_anrede" services/prompt_pipeline.py` → KEIN `ls.state`-Fallback mehr (:507-518 geloescht); nur per-sid Hot-Path :451/:500
    - **`grep -n "session_anrede" services/claude_service.py` → liest `_sid_st.get('session_anrede')` (top-level), NICHT mehr `_sid_st_state.get('session_anrede')` (W-a)**
    - `grep -n "session_anrede" routes/app_routes.py` → Read via `_bs` (das EINE _load_beenden_state, N-3), kein `ls.state`
    - **W-1: `grep -n "session_anrede" services/live_session.py` → der 'state'-Subdict-Seed :356 ist WEG (kein `'session_anrede'` innerhalb des `'state':{...}`-Blocks :330-368); optional EIN top-level-Seed**
    - **W-2: `grep -n "ls.state\['session_anrede'\]\|_apply_anrede_whitelist" tests/test_ewb_rate_api.py` → kein Test asserted mehr die globale title-case-Wahrheit; die Anrede-Tests pruefen den per-sid-lowercase-Pfad (ECHTER Handler) — kein Prod-Replika-False-Green**
    - **grep-Vollstaendigkeit: 0 Treffer `ls.state['session_anrede']` / `ls.state.get('session_anrede')` UND 0 `_sid_st_state.get('session_anrede')` im Prod-Code** (Punkt 22)
    - Case konsistent (beide Writer lowercase 'du'/'sie')
    - precall_briefing: `grep -n "'precall_briefing'" services/live_session.py` → Modul-state-Key entfernt (0 Reader); aus Waechter-Pending-Liste gestrichen
    - deploy.sh production GRUEN; statischer Waechter (Plan 01) bleibt gruen
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: mic_muted pro sid (+ tote Subdict-Seeds W-1) (+ ggf. aktives_skript_inhalt/skript_bloecke falls FIX)</name>
  <read_first>
    - services/deepgram_service.py:230-235 (mic_muted-Read :233), :850-858 (handle_mute_mic Write :856)
    - services/live_session.py:330 ('state':{...} oeffnet), :351 (mic_muted-Seed IM 'state'-SUBDICT — W-1 bestaetigt, NICHT top-level), :144 (Modul-state['mic_muted']-Def)
    - Plan 01 Task 1 Verdikt fuer aktives_skript_inhalt/skript_bloecke (DELETE oder FIX)
    - RESEARCH.md §1 (mic_muted-Zeile), §6.4 (skript-Kandidaten)
  </read_first>
  <behavior>
    - Test 1: handle_mute_mic fuer sidA setzt mic_muted=True per-sid; sidB bleibt False (Isolation gepaart).
    - Test 2: der on_message-Read fuer sidA liest True, fuer sidB False.
  </behavior>
  <action>
    a) **mic_muted Write pro sid** (deepgram_service.py:856, handle_mute_mic): der Handler hat `sid` im Scope. Ersetze `ls.state['mic_muted'] = ...` durch per-sid unter _session_state_lock mit Ghost-SID-Guard. **W-1-Ebenen-Entscheidung (Fresh-Grep 2026-07-03):** der Seed `'mic_muted': False` (live_session.py:351) liegt im **'state'-SUBDICT** (`'state':{...}` oeffnet :330, Zeilen :331-368 sind SUBDICT) — NICHT top-level, entgegen der alten Plan-Behauptung. Waehle EINE kanonische Ebene fuer Writer UND Reader (top-level `_session_state[sid]['mic_muted']` ist konsistent mit session_anrede — empfohlen) und ZIEHE den Seed dorthin (top-level ergaenzen in :306-329) bzw. loesche den toten Subdict-Seed :351. Writer UND Reader MUESSEN dieselbe Ebene treffen (sonst neues Split-Brain, dieselbe B2-Klasse wie claude:1481).
    b) **mic_muted Read pro sid** (deepgram_service.py:232-233): der on_message-Callback hat `sid` im Scope. Ersetze `ls.state.get('mic_muted', False)` durch per-sid (dieselbe Ebene wie a). D-02: ohne/unbekannte sid → False.
    c) Modul-`state['mic_muted']`-Def (live_session.py:144) + toter Subdict-Seed :351 + Reset-Zeile entfernen (0 Reader nach b).
    d) **aktives_skript_inhalt/skript_bloecke** — NUR falls Plan 01 Task 1 sie als FIX entschieden hat: Write pro sid (deepgram_service.py:640-642, `ls.state['aktives_skript_inhalt']`/`['skript_bloecke']` :641-642) analog session_anrede (Ghost-Guard, NACH init falls im start-Handler) + den belegten Reader per-sid. **W-b Guard-Placement (Pflicht bei FIX):** die skript-Zeile in `_PENDING_MIGRATION` unter Welle-A-Tag eintragen, dann in DIESEM Task (Familie A migriert) wieder streichen. Wenn DELETE → schon weg, hier NICHTS (nur SUMMARY-Vermerk).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Browser-UAT: Tab A Mikrofon stumm, Tab B aktiv → Keyword-Matcher reagiert nur bei Tab B, nicht auf stumme Tab-A-Worte.</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "mic_muted" services/deepgram_service.py` → Write+Read beide per-sid auf DERSELBEN Ebene (empfohlen top-level `_session_state[sid]['mic_muted']`), kein `ls.state[...]` mehr
    - **W-1: `grep -n "mic_muted" services/live_session.py` → Modul-state-Key entfernt UND der tote 'state'-Subdict-Seed :351 entfernt (kein `'mic_muted'` innerhalb `'state':{...}` :330-368); Seed liegt jetzt auf der gewaehlten kanonischen Ebene**
    - Falls skript-Kandidaten FIX: `grep -n "aktives_skript_inhalt" services/deepgram_service.py` → per-sid UND skript in _PENDING_MIGRATION (Welle-A-Tag) eingetragen-und-in-diesem-Task-gestrichen (W-b); falls DELETE: 0 Treffer
    - deploy.sh production GRUEN; statischer Waechter bleibt gruen
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SocketIO-Handler → per-sid-State | Anrede/Mute-Config muss auf DERSELBEN per-sid-Ebene geschrieben+gelesen werden (kein Split-Brain, B2 — inkl. claude:1481 vom Subdict auf top-level; Start-Anrede NACH init N-4; tote Subdict-Seeds weg W-1) |
| stop_live_session/disconnect (pop) → api_beenden (read) | B1: BEIDE Beenden-Naehte (:779 normaler Hangup, :815 disconnect) duerfen den State nicht verlieren → Snapshot-Fundament (Leer-Skip N-1); :674 (reconnect) exempt; api_beenden liest ueber EINEN Peek (N-3) |
| api_beenden (kein Socket-sid) → per-sid-Read | _beenden_sid via posted call_id exakt (S3), sonst user_id-Scan; die zwei Direkt-Reads (:172-177, :193-207) ueber `_bs` (N-2) statt roh `_session_state` |
| Late-Write nach stash → Snapshot | Ghost-Drop: spaete Events beleben eine tote sid nicht wieder (B1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PERSID-07 | Information Disclosure | session_anrede/mic_muted global bzw. falsche Ebene → Cross-Session-Vermischung/toter Read | mitigate | EINE kanonische per-sid-Stelle (top-level), alle Reader inkl. claude:1481 + beide Writer im selben Deploy; Start-Write NACH init (N-4); tote Subdict-Seeds weg (W-1); Split-Brain aufgeloest (Req 6) |
| T-PERSID-08 | Spoofing | _beenden_sid-Aufloesung trifft fremde/falsche Session | mitigate | S3: bevorzugt exakt via posted call_id (Muster app_routes:194-207); user_id-Scan nur Fallback |
| T-PERSID-19 | Information Disclosure | normaler Hangup (:779) ODER disconnect (:815) vor beenden → leerer Call-Record (Datenverlust); Doppel-Feuer ueberschreibt voll mit leer | mitigate | B1: _ended_session_snapshots stasht State an BEIDEN Naehten (TTL), Leer-Skip+first-wins gegen setdefault :810-811 (N-1); api_beenden konsumiert Snapshot via EINEM Peek (N-3); die zwei Direkt-Reads ueber `_bs` (N-2); :674 exempt |
| T-PERSID-20 | Tampering | Late-Write nach stash belebt tote sid | mitigate | B1 Ghost-Drop: record_*/anrede bleiben `if sid in _session_state` (kein Snapshot-Write) |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `conversation_logs` (schreiben — anrede-Spalte via api_beenden) — Read-Pfad geaendert (session_anrede-Quelle + Snapshot), Spalte selbst unveraendert.

### inspect.sh-Beleg (Executor holt VOR Umbau)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema conversation_logs'
[Executor fuegt verbatim-Output ein — anrede-Spalte + Typ bestaetigen]
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Leser laeuft voraus? |
|---|---|---|---|---|
| session_anrede | deepgram schreibt (start NACH init N-4 + toggle :827) → api_beenden (via _bs) + prompt_pipeline + claude:1481 lesen (alle top-level) | RAM: _session_state[sid]['session_anrede'] (top-level, lazy) + Snapshot | live (session-start nach init / toggle) | Nein — Snapshot (Leer-Skip N-1) faengt stop_live_session/disconnect-vor-beenden (B1) |
| mic_muted | deepgram schreibt+liest (per-sid, kanonische Ebene, W-1) | RAM: _session_state[sid]['mic_muted'] | live | Nein — beide in deepgram, on_message |
| precall_briefing | set_briefing_for_sid schreibt → api_beenden liest (via _bs, Snapshot-faehig) | RAM: _session_state[sid]['_briefing'] / Snapshot | live (PreCall) | Nein — Snapshot faengt beide Beenden-Naehte |
| _ended_session_snapshots[sid] | stop_live_session (:779) + handle_disconnect (:815) schreiben (stash, Leer-Skip N-1) → api_beenden konsumiert (Peek N-3) | RAM (TTL) | Beenden-Naht-Zeitpunkt (Hangup/disconnect) | Nein — api_beenden konsumiert genau diesen Snapshot |
| conversation_logs.anrede | api_beenden schreibt aus session_anrede | DB-Spalte | http (call-Ende) | n/a (Endschreibung) |

### Bei Diskrepanz: STOP + Replan

<verification>
- deploy.sh production nerve_test-Gate gruen
- Browser-UAT: 2 Konten, Anrede (Start+Toggle) + Mute isoliert; normaler-Hangup-vor-beenden UND disconnect-vor-beenden verlieren keinen Record (B1, beide Naehte, Leer-Skip N-1); Doppel-Feuer haelt vollen Snapshot (N-1); Redeanteil/Tempo/Monolog != 0 (N-2)
- statischer Waechter (Plan 01) bleibt gruen (keine neue globale Live-Zuweisung; _ended_session_snapshots gewhitelistet)
</verification>

<success_criteria>
- session_anrede kanonisch per-sid (Split-Brain weg, Start-Anrede erreicht Prompt via Write NACH init N-4, claude:1481 liest top-level statt Subdict, Toggle landet in ConversationLog.anrede, Case konsistent, tote Subdict-Seeds weg W-1 — B2)
- mic_muted pro sid (gleiche Ebene Writer+Reader, toter Subdict-Seed weg W-1)
- precall_briefing-Reader per-sid, Modul-Key weg
- _beenden_sid-Vertrag: exakt via posted call_id (S3), user_id-Scan Fallback — von Plan 05/06 referenziert
- _ended_session_snapshots-Fundament: normaler-Hangup-vor-beenden (:779) UND disconnect-vor-beenden (:815) verlieren keinen Record, Doppel-Feuer ueberschreibt nicht mit leer (N-1), api_beenden liest via EINEM Peek (N-3) + die zwei Direkt-Reads ueber `_bs` (N-2), :674 exempt, Late-Writes ghost-droppen (B1)
- Anrede-Test-Bestand migriert (W-2): kein globaler title-case-False-Green mehr
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-03-SUMMARY.md`
</output>
</content>
</output>
