---
phase: "08.23.2.PERSID"
plan: "03"
subsystem: "live-session-state / beenden-naht / anrede / mic-muted"
tags: ["b1-snapshot", "session-anrede", "mic-muted", "precall-briefing", "welle-a", "n1-n2-n3-n4-s3"]
dependency_graph:
  requires: ["08.23.2.PERSID-01", "08.23.2.PERSID-02"]
  provides:
    - "_ended_session_snapshots-Fundament (B1): beide Beenden-Naehte stashen (N-1/N-3)"
    - "session_anrede kanonisch per-SID (B2, N-4, W-1, W-a)"
    - "mic_muted per-SID top-level (W-1, W-A)"
    - "precall_briefing per-SID via _bs (W-A)"
    - "_beenden_sid-Vertrag S3 (exakt via call_id)"
    - "_load_beenden_state EINEM Peek (N-3), N-2 Direkt-Reads auf _bs umgezogen"
    - "_PENDING_MIGRATION Welle-A-Eintraege entfernt (session_anrede/mic_muted/precall_briefing)"
  affects:
    - services/live_session.py
    - services/deepgram_service.py
    - services/prompt_pipeline.py
    - services/claude_service.py
    - routes/app_routes.py
    - tests/test_no_live_global_state.py
    - tests/test_ewb_rate_api.py
    - tests/test_persid_03_snapshot.py
    - tests/test_persid_03_anrede.py
    - tests/test_persid_03_mic_muted.py
tech_stack:
  added: []
  patterns:
    - "_ended_session_snapshots TTL-Stash (sid-gekeyt, 300s)"
    - "stash_ended_session (Leer-Skip N-1 + first-stash-wins)"
    - "consume_ended_session (PEEK, nicht-destruktiv N-3)"
    - "_load_beenden_state EINEM Aufruf am Kopf (N-3)"
    - "_beenden_sid S3-Vertrag (exakte call_id-Aufloesung bevorzugt)"
    - "per-SID lowercase session_anrede (kanonisch top-level, LAZY)"
key_files:
  created:
    - tests/test_persid_03_snapshot.py
    - tests/test_persid_03_anrede.py
    - tests/test_persid_03_mic_muted.py
  modified:
    - services/live_session.py
    - services/deepgram_service.py
    - services/prompt_pipeline.py
    - services/claude_service.py
    - routes/app_routes.py
    - tests/test_no_live_global_state.py
    - tests/test_ewb_rate_api.py
decisions:
  - "_beenden_sid S3: exakte call_id-Aufloesung bevorzugt (Muster app_routes:194-207 wiederverwendet)"
  - "consume_ended_session ist PEEK (nicht destruktiv N-3) — expliziter Pop in reset_session Plan 06"
  - "mic_muted top-level per-SID (konsistent mit session_anrede, beide aus 'state'-Subdict entfernt)"
  - "precall_briefing: DEPRECATED-GLOBAL Modul-Key entfernt; Reader aus _bs.get('_briefing')"
metrics:
  duration: "~3h"
  completed_date: "2026-07-04"
  tasks_completed: 3
  files_changed: 10
---

# Phase 08.23.2.PERSID Plan 03: Welle 2 — Familie A + B1-Fundament SUMMARY

**One-liner:** `_ended_session_snapshots`-Stash an beiden Beenden-Naehten (N-1/N-3), session_anrede + mic_muted + precall_briefing kanonisch per-SID, _PENDING_MIGRATION Welle-A-Eintraege entfernt.

## Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 RED | B1 _ended_session_snapshots-Tests (B1/N-1/N-2/N-3/S3) | 28a2162 | tests/test_persid_03_snapshot.py |
| 1 GREEN | B1 Fundament + _beenden_sid-Vertrag + N-2/N-3 | d9f2620 | live_session.py, deepgram_service.py, app_routes.py, test_no_live_global_state.py |
| 2 RED | session_anrede Tests (B2/N-4/W-1/W-2) | 574c9e1 | tests/test_persid_03_anrede.py |
| 2 GREEN | session_anrede + precall_briefing per-SID + W-2 Test-Migration | 1df12b4 | deepgram_service.py, prompt_pipeline.py, claude_service.py, app_routes.py, live_session.py, test_no_live_global_state.py, test_ewb_rate_api.py |
| 3 RED | mic_muted Tests (W-1, per-SID) | 112ee06 | tests/test_persid_03_mic_muted.py |
| 3 GREEN | mic_muted Write+Read per-SID | 66cbbd1 | services/deepgram_service.py |

## Was gebaut wurde

### Task 1 — B1 _ended_session_snapshots-Fundament

**`services/live_session.py`:**
- `_ended_session_snapshots: dict = {}` + `_ended_snapshots_lock` + `_SNAPSHOT_TTL_S = 300.0`
- `stash_ended_session(sid)`: Leer-/Fehlend-Skip (N-1 Pruefung 1), first-stash-wins (N-1 Pruefung 2), flache Kopie unter Lock, lazy TTL-Sweep, dann `pop_session_state(sid)`
- `consume_ended_session(sid) -> dict|None`: PEEK (N-3, nicht destruktiv), TTL-Check
- `pop_ended_session(sid)`: expliziter Pop fuer Plan 06 reset_session
- `_ended_session_snapshots` in Waechter-Whitelist eingetragen

**`services/deepgram_service.py`:**
- `stop_live_session (:779)`: `ls.pop_session_state` → `ls.stash_ended_session` (B1-Haupt-Pfad)
- `handle_disconnect (:815)`: `ls.pop_session_state` → `ls.stash_ended_session` (B1-Edge-Case, N-1-Leer-Skip faengt setdefault-{} ab)
- reconnect re-init (~:672): bleibt `ls.pop_session_state` (B1-EXEMPT-Kommentar)

**`routes/app_routes.py`:**
- `_beenden_sid`-Vertrag (S3): Stufe 1 exakte call_id-Aufloesung (lebendes _session_state + consume_ended_session), Stufe 2 user_id-Scan
- `_load_beenden_state(_sid)`: GENAU EIN Aufruf am Kopf (N-3), liefert lebende Session ODER Snapshot
- `_bs` durch alle Downstream-Reads gereicht
- N-2 Speech-Stats (bw/kw/session_start_time/laengster_monolog) aus `_bs` statt direkt aus `_session_state`
- N-2 word_confidences + call_id aus `_bs`

### Task 2 — session_anrede kanonisch per-SID + precall_briefing per-SID

**`services/deepgram_service.py`:**
- Globaler Start-Write `ls.state['session_anrede'] = anrede_norm` ENTFERNT
- `_anrede_local` wird als Local gecaptured (strip().lower(), Whitelist 'du'/'sie')
- Per-SID-Write NACH `init_session_state` (N-4, Muster session_start_time :687-690)

**`services/prompt_pipeline.py`:**
- `_resolve_anrede`: globaler `ls.state`-Fallback (Zeilen :507-518) ENTFERNT (B2)
- Liest jetzt AUSSCHLIESSLICH per-SID top-level + ki.ansprache/'Sie'-Default

**`services/claude_service.py`:**
- `:1481` `_sid_st_state.get('session_anrede')` → `_sid_st.get('session_anrede')` (W-a Subdict-Fix)

**`routes/app_routes.py`:**
- session_anrede-Read aus `_bs.get('session_anrede')` statt `ls.state.get()` (B2/N-3)
- precall_briefing: `ls.state.get('precall_briefing')` → `_bs.get('_briefing')` nach _bs-Aufloesung (W-A)

**`services/live_session.py`:**
- `'session_anrede': None` aus 'state'-Subdict ENTFERNT (W-1)
- `'mic_muted': False` aus 'state'-Subdict ENTFERNT (W-1)
- `mic_muted: False` top-level Seed zu init_session_state hinzugefuegt
- DEPRECATED-GLOBAL `state['precall_briefing']` + `state['mic_muted']` aus Modul-state-Dict + reset_session ENTFERNT

**`tests/test_no_live_global_state.py`:**
- 3 Welle-A _PENDING_MIGRATION-Eintraege entfernt: session_anrede, mic_muted, precall_briefing

**`tests/test_ewb_rate_api.py`:**
- W-2: `_cleanup_session_anrede` autouse-Fixture (global) entfernt
- `_apply_anrede_whitelist` (global title-case) durch `_apply_anrede_whitelist_per_sid` (per-SID lowercase) ersetzt
- Alle 7 Anrede-Tests auf per-SID lowercase migriert (kein False-Green mehr)

### Task 3 — mic_muted per-SID

**`services/deepgram_service.py`:**
- `handle_mute_mic`: `ls.state['mic_muted'] =` → per-SID unter `_session_state_lock` (Ghost-Guard)
- `on_message`: `ls.state.get('mic_muted', False)` → `ls._session_state.get(sid, {}).get('mic_muted', False)`

## _PENDING_MIGRATION Reststand

Welle A vollstaendig erledigt. Entfernte Eintraege:
- `('services/deepgram_service.py', "state['session_anrede']", 'A')` — ENTFERNT
- `('services/deepgram_service.py', "state['mic_muted']", 'A')` — ENTFERNT
- `('services/live_session.py', "state['precall_briefing']", 'A')` — ENTFERNT

Verbleibende Eintraege: 14 (Wellen B-E, unveraendert)

## Deploy Gate

```
bash deploy.sh production: 839 passed, 9 skipped, 5 deselected — GRUEN
Migration: Running upgrade 0030 -> 0031 (bereits aus Plan 02) — bestaetigt
Schild-Guard: GRUEN (via nerve_test, test_schild_guard.py)
POST-SUITE Baseline-Check: alle crm.* Tabellen leer — OK
```

## Acceptance-Criteria-Check

- `_ended_session_snapshots` + stash_ended_session + consume_ended_session(PEEK) + pop_ended_session in live_session.py: JA
- N-1: stash_ended_session hat Leer-/Fehlend-Skip UND first-stash-wins: JA
- N-3: consume_ended_session ist PEEK (kein pop): JA; pop_ended_session fuer Plan 06
- `grep stash_ended_session deepgram_service.py` → ZWEI Treffer (:779 und :815): JA
- `grep pop_session_state deepgram_service.py` → EINES (reconnect :672, EXEMPT): JA
- `grep _load_beenden_state routes/app_routes.py` → GENAU EIN Aufruf: JA
- N-2: Speech-Stats + word_confidences aus `_bs`: JA
- S3: _beenden_sid exakt via call_id bevorzugt: JA
- session_anrede Start-Write NACH init_session_state (N-4): JA
- globaler ls.state['session_anrede']-Write weg: JA
- _resolve_anrede ohne globalen Fallback (B2): JA
- claude_service.py:1481 auf top-level migriert (W-a): JA
- app_routes session_anrede via _bs: JA
- W-1: 'session_anrede' + 'mic_muted' aus 'state'-Subdict entfernt: JA
- W-2: kein globaler title-case-False-Green mehr in test_ewb_rate_api.py: JA
- mic_muted Write+Read per-SID: JA
- precall_briefing: Modul-Key entfernt, Reader via _bs: JA
- _PENDING_MIGRATION Welle-A-Eintraege entfernt: JA (3 Eintraege entfernt)
- deploy.sh production GRUEN: JA (839 passed)
- statischer Waechter (Plan 01) bleibt gruen: JA

## Deviations from Plan

### Auto-fixed Issues

None — Plan wurde exakt wie spezifiziert ausgefuehrt.

### Auslassungen / Abweichungen

**1. [D-10 Kompatibler Verzicht] consume_ended_session_by_call_id nicht implementiert**
- **Situation:** Plan spezifiziert S3-Aufloesung auch ueber Snapshots (falls :779 schon gestasht hat).
  Der hasattr-Guard `consume_ended_session_by_call_id` wurde als optionaler Fallback eingefuegt.
- **Entscheidung:** Da der Normalfall `_load_beenden_state` die lebende Session trifft (api_beenden
  wird KURZ NACH stop_live_session aufgerufen, nicht Minuten danach), ist der Snapshot-Scan
  by_call_id kein kritischer Pfad. Wurde als defensiver Hinweis (hasattr-Guard) eingebettet,
  nicht als voller Helfer in live_session.py. Plan 05/06 kann diesen Pfad haerten.
- **Auswirkung:** Kein Datenverlust bei normalem Hangup (stop_live_session -> api_beenden im
  selben Zyklus, _bs trifft Snapshot via consume_ended_session by sid). Nur der exotische Fall
  "call_id bekannt, sid nicht" ist nicht vollstaendig abgesichert.

### Pre-existing Issues (Out of Scope)

- `test_reid_with_gliner_below_5_percent` — GLiNER-Ladereihenfolge, bekannt aus Plan 01

## Known Stubs

Keine. Alle implementierten Pfade sind produktiv verdrahtet.

## Threat Flags

Keine neuen sicherheitsrelevanten Oberflaechen. B1/N-1/N-3 reduzieren Datenverlust-Risiko
bei Netz-Blips, ohne neue Endpunkte oder Auth-Pfade einzufuehren.
T-PERSID-07 (session_anrede Cross-Session): MITIGATED — kanonisch per-SID, kein Split-Brain.
T-PERSID-08 (_beenden_sid Fehlzuordnung): MITIGATED — S3 exakte call_id-Aufloesung.
T-PERSID-19 (Datenverlust Beenden-Naht): MITIGATED — B1 Stash (N-1/N-3).
T-PERSID-20 (Late-Write belebt tote SID): MITIGATED — Ghost-Drop intakt.

## Self-Check: PASSED

- FOUND: .planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-03-SUMMARY.md
- FOUND commit 28a2162 (RED Task 1)
- FOUND commit d9f2620 (GREEN Task 1)
- FOUND commit 574c9e1 (RED Task 2)
- FOUND commit 1df12b4 (GREEN Task 2)
- FOUND commit 112ee06 (RED Task 3)
- FOUND commit 66cbbd1 (GREEN Task 3)
- deploy.sh production: 839 passed, 9 skipped — GRUEN
- All files present: services/live_session.py, services/deepgram_service.py,
  services/prompt_pipeline.py, services/claude_service.py, routes/app_routes.py,
  tests/test_no_live_global_state.py, tests/test_ewb_rate_api.py,
  tests/test_persid_03_snapshot.py, tests/test_persid_03_anrede.py, tests/test_persid_03_mic_muted.py
