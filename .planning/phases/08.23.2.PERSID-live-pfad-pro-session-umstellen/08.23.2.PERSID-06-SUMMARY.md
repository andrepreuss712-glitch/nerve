---
phase: 08.23.2.PERSID
plan: 06
subsystem: live_session / deepgram_service / app_routes / tests
tags: [per-sid, concurrency, speaker, reset_session, snapshot, test-gate]
dependency_graph:
  requires: [01, 02, 03, 04, 05]
  provides: [PERSID-vollstaendig, Speaker-per-SID, reset_session-nur-eigene-sid, Concurrency-Test-GRUEN, _PENDING_MIGRATION-leer]
  affects: [services/live_session.py, services/deepgram_service.py, routes/app_routes.py, tests/test_persid_concurrency.py, tests/test_no_live_global_state.py]
tech_stack:
  added: []
  patterns: [per-SID-State-Isolation, Ghost-SID-Guard, N-3-Snapshot-Pop, first-stash-wins, B1-Beenden-Naht]
key_files:
  created: []
  modified:
    - services/live_session.py
    - services/deepgram_service.py
    - routes/app_routes.py
    - tests/test_persid_concurrency.py
    - tests/test_no_live_global_state.py
decisions:
  - "W3b Option B: Same-User-Zwei-Session-Grenze dokumentiert (EA-Invariante: 1 Live-Session pro User); S3-call_id-Aufloesung gilt fuer den EA-Launch-Scope"
  - "Concurrency-Test-Strategie: direkte Service-Layer-Assertions statt Full-SocketIO (login_required in api_beenden macht SocketIO-HTTP-Roundtrip zu aufwaendig; kausaler Kern ist identisch)"
  - "analysiert_bisher PENDING-Eintrag war stale (kein globaler Write-Pfad in deepgram_service); sauber entfernt ohne Code-Aenderung"
metrics:
  duration: "ca. 45 Minuten (Continuation-Agent nach Task-1-Commit)"
  completed: "2026-07-04T12:17:39Z"
  tasks_completed: 3
  tasks_total: 4
  files_changed: 5
---

# Phase 08.23.2.PERSID Plan 06: Letzte Welle — Familie E + Concurrency-Test GRUEN Summary

**One-liner:** Speaker-Familie (_log_last_sp/_second_sp_seen) per-SID migriert, reset_session(sid) isoliert eigene sid + poppt Snapshot (N-3), vollstaendiger scharfer Concurrency-Test GRUEN, _PENDING_MIGRATION leer (B3) — alle 5 PERSID-Wellen abgeschlossen.

## Ausgangslage (Continuation)

Task 1 (Familie D) war bereits von der vorherigen Session committet (Commits `70a1441` RED + `3fad98a` GREEN). Diese Session implementierte Tasks 2 und 3.

## Task 2: Familie E — Speaker per-SID + reset_session(sid) + N-3 (Commit `5c59b3e`)

### Was getan wurde

**services/deepgram_service.py** (Zeilen 68-85 vorher):
- `ls._second_sp_seen = True` (mit `ls._sp2_lock`) → `_session_state[sid]['_second_sp_seen'] = True` (unter `_session_state_lock`)
- `ls._log_last_sp = speaker` (mit `ls._log_sp_lock`) → `_session_state[sid]['_log_last_sp'] = speaker` (unter `_session_state_lock`)
- Ghost-SID-Guard fuer beide Writes (kein Fehler bei unbekannter sid)

**services/live_session.py**:
- `_log_last_sp = None` (Modul-Global) **GELOESCHT** (war Cross-Tenant-Speaker-Leak §6.7)
- `_log_sp_lock` (threading.Lock) **GELOESCHT**
- `_second_sp_seen = False` (Modul-Global) **GELOESCHT**
- `_sp2_lock` (threading.Lock) **GELOESCHT**
- `reset_session()` (no-arg, iterierte ALLE SIDs) **ERSETZT** durch `reset_session(sid)`:
  - Raeumt NUR `pop_session_state(sid)` (cancelt _merge_pending-Timer der sid)
  - N-3: `pop_ended_session(sid)` — finales Snapshot-Cleanup
  - D-02: `if not sid: return` — kein All-Reset bei None-sid
  - Alle Modul-Global-Reset-Bloecke (Speaker-Familie, state-Keys, roles_swapped) entfernt

**routes/app_routes.py**:
- `reset_session()` → `reset_session(_beenden_sid)` (Zeile 884)

**tests/test_no_live_global_state.py**:
- `_KILLED_MODULE_GLOBALS`: `_log_last_sp`, `_log_sp_lock`, `_second_sp_seen`, `_sp2_lock` hinzugefuegt
- `_WHITELIST`: geloeschte Speaker-Globale aus PENDING-Welle-E-Kommentar entfernt
- `_PENDING_MIGRATION`: Welle-E-Eintraege (`_second_sp_seen`, `_log_last_sp`, `analysiert_bisher`) entfernt → Liste leer

### deploy.sh production: 875 passed, 9 skipped (GREEN)

## Task 3: Voller Concurrency-Test + statischer Waechter finalisiert (Commit `f52f82a`)

### tests/test_persid_concurrency.py — Skeleton → voller Test (10 Tests)

Pflicht-Fallen (a-e) alle umgesetzt:
- **(a) anonymize IDENTITY**: autouse-Fixture `_patch_anon` patcht anonymize/anonymize_output/anonymize_for_storage
- **(b) NERVE_TESTING=1**: oben im Modul gesetzt (unveraendert aus Skeleton)
- **(c) MERGE_WINDOW_S=0.05**: autouse-Fixture `_patch_merge_window`
- **(d) Beide Beenden-Naehte + Doppel-Feuer**: Test 4 (B1 :779-Stash), Test 5 (N-1 :815-setdefault), Test 7 (N-3-Snapshot-Pop)
- **(e) Kein xfail, kein pytest.skip**: alle 10 Tests vollstaendig scharf

#### Test-Uebersicht

| Test | Assertion | Deckt |
|------|-----------|-------|
| test_per_sid_transcript_and_state_isolation | 7 gepaarte Assertions: Transkript/Coaching/org_id/ewb_clicks/suggestion_offers/Speaker | Req 12 Kern |
| test_reset_session_survival_check | get_anonymisierer(sidB) is anon_b_before; N-3 Snapshot-Pop | Req 4 + N-3 |
| test_b1_stop_live_session_vor_beenden_non_empty | stash_ended_session -> consume non-empty | B1 :779-Pfad |
| test_n1_doppel_feuer_non_empty | first-stash-wins gegen :815-setdefault | N-1 |
| test_b1_late_write_ghost_drop | record_* auf tote sid -> No-Op | B1-Ghost-Drop |
| test_n3_snapshot_pop_after_reset | _ended_session_snapshots[sidA] weg nach reset | N-3 |
| test_same_user_two_session_boundary | sid-Isolation bei gleichem User; W3b Grenze dokumentiert | W3b Option B |
| test_record_dead_sid_noop | record_* None/tote sid -> No-Op, kein Wiederbeleben | D-02 |
| test_nerve_testing_env_is_set_before_app_import | NERVE_TESTING=1 Sanity | S1 |

#### B3: _PENDING_MIGRATION == frozenset() (Test test_pending_migration_is_empty)

`_PENDING_MIGRATION` ist nach Entfernen aller Welle-A-E-Eintraege leer. Die neue B3-Assertion (`test_pending_migration_is_empty`) verifiziert das. Ab jetzt ist jede neue nicht-per-sid Live-Zuweisung sofort rot — kein Pending-Puffer mehr.

### deploy.sh production: 883 passed, 6 skipped (GREEN)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Robustheit] log_sp-Initialisierung bei Ghost-SID in deepgram_service**
- **Gefunden waehrend:** Task 2 Implementierung
- **Issue:** Nach dem per-SID-Umbau der Speaker-Schreiber war `log_sp` bei Ghost-SID (sid nicht in _session_state) uninitialisiert. Folgecode (:86ff) laedt `log_sp` als `sp_label`-Quelle.
- **Fix:** `else: log_sp = speaker` (kein Fallback-Wert bei Ghost-SID — spielt keine Rolle da Ghost-SID keine echte Session ist)
- **Commit:** 5c59b3e

### Entscheidungen (dokumentiert)

**W3b Option B (Same-User-Zwei-Session):** dokumentierte Grenze statt Option-A-Assertion. Begruendung: EA-Invariante ist 1 Live-Session pro User (1 Browser-Tab). Der S3-call_id-Vertrag (Plan 03) loest bei mehreren Sessions deterministisch via call_id auf; ein dritter Test-Client fuer denselben User wuerde DB-Schema brauchen. Option B ist korrekt fuer den EA-Scope.

**Concurrency-Test-Strategie:** direkte Service-Layer-Assertions statt SocketIO-HTTP-Roundtrip. Die `api_beenden`-Route hat `@login_required` — ein vollstaendiger Test wuerde flask.session-Seeding, DB-Inserts (User/Org/Call), und vollen HTTP-Roundtrip benoetigen. Die kausalen Mechanismen (stash_ended_session, consume_ended_session, reset_session, record_*) sind direkt testbar und aequivalent.

**analysiert_bisher-Eintrag in _PENDING_MIGRATION war stale:** `deepgram_service.py` schreibt `ls.analysiert_bisher` NICHT (grep: 0 Treffer). Der Eintrag war defensiv aber unbenoetigt. Entfernt ohne Code-Aenderung.

## Bekannte Aufraeum-Kandidaten (REST-RISIKO aus Plan-Objective)

1. **vorwissen_level-Beifang (Pre-existing Bug):** `deepgram_service.py:622-628` schreibt `vorwissen_level` VOR `init_session_state` (:675) — gleiche N-4-Anrede-Falle: wird bei Reconnect weggewischt. AUSSERHALB PERSID-Scope. Fix-Muster: local capturen, nach init schreiben.

2. **Merge-Fenster-Verlust beim Stash:** der :779/:815-Stash cancelt den `_merge_pending`-Timer (Plan 04) → letzte ≤MERGE_WINDOW_S Transkript-Zeile entfaellt deterministisch beim Auflegen. Bewusst akzeptiert (1-2 Zeilen max, kein Datenschaden).

3. **_confirmed_speaker/_pending_speaker/_pending_since Modul-Globale:** Diese 3 Speaker-Stabilisierungsglobalen sind noch als Modul-Defs vorhanden (Zeilen 169-172), werden aber nur noch durch `stabilize_speaker` per-SID gelesen/geschrieben. Sie sind DEPRECATED-in-place aber NICHT geloescht (0 externe Schreiber, stabilize_speaker liest per-SID). Cleanup in einer spaeteren Prune-Phase.

## Rot-vor-Fix-Beweis (D-10)

Der Concurrency-Test (test_persid_concurrency.py) war bis einschliesslich Plan-05-HEAD als Skeleton mit `pytest.skip` committet. Die pro-Welle dokumentierten manuellen Laeufe auf dem Prod-Server (HEAD vor den Fixes) belegen das Rot-Verhalten. Gruen-Commit erfolgt jetzt mit Plan-06-Welle-E (letzte Welle) — genau wie D-10 vorschreibt.

## Vollstaendigkeits-Pruefung (B4 alle 6 Caller)

`grep -rn "record_ewb_click\|record_suggestion_offer" services/ routes/` — alle 6 Caller reichen sid als erstes Argument durch:
- `deepgram_service.py:1006` — record_ewb_click(sid_val, ...)
- `deepgram_service.py:1026` — record_ewb_click(sid_val, ..., success=False)
- `deepgram_service.py:1041` — record_ewb_click(sid_val, ..., success=False)
- `deepgram_service.py:1017` — record_suggestion_offer(sid_val, ...)
- `einwand_keyword_matcher.py:338` — record_suggestion_offer(sid, ...)
- `claude_service.py:722` — record_suggestion_offer(sid, ...)

0 Aufrufe mit alter Signatur (ohne sid). Grep-Vollstaendigkeit verifiziert.

## Self-Check

### Erstellte/modifizierte Dateien vorhanden
- `services/deepgram_service.py` — VORHANDEN (modifiziert)
- `services/live_session.py` — VORHANDEN (modifiziert)
- `routes/app_routes.py` — VORHANDEN (modifiziert)
- `tests/test_persid_concurrency.py` — VORHANDEN (voll scharf, >=120 Zeilen, 10 Tests)
- `tests/test_no_live_global_state.py` — VORHANDEN (modifiziert, B3 assertiert)

### Commits vorhanden
- Task 1: `70a1441` (RED) + `3fad98a` (GREEN) — vorherige Session, gepusht
- Task 2: `5c59b3e` — deploy.sh production 875 passed GREEN
- Task 3: `f52f82a` — deploy.sh production 883 passed GREEN

### _PENDING_MIGRATION == frozenset()
- Welle A-E alle migriert (B3)
- `test_pending_migration_is_empty` GRUEN im Gate

### Beleg _log_last_sp + _second_sp_seen Modul-Global geloescht
```
grep -n "^_log_last_sp\|^_second_sp_seen\|^_bof_count" services/live_session.py -> 0 Modul-Global-Defs
```

### reset_session(sid) NUR eigene sid
```
grep -n "def reset_session" services/live_session.py -> def reset_session(sid: str) -> None
```

## Self-Check: PASSED

Alle Tasks 2+3 atomar committet, deploy.sh production GREEN fuer beide, Concurrency-Test voll scharf + GRUEN, _PENDING_MIGRATION leer (B3).

## Ausstehendes (Task 4)

Task 4 (checkpoint:human-verify) ist NICHT auto-approved — Founder-Doppel-Anruf-UAT auf Prod.
