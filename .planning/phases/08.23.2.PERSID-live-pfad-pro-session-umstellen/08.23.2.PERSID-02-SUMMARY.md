---
phase: "08.23.2.PERSID"
plan: "02"
subsystem: "slow_lane / intent_payload / claude_service / app_routes / app"
tags: ["f2-stilllegung", "not_gradable", "schild-aktualisierung", "ttft", "heiler", "wachter"]
dependency_graph:
  requires: ["08.23.2.PERSID-01"]
  provides: ["F2-Stilllegung-Terminal", "build_intent_payload-Extraktion", "EWB-TTFT-Messung", "Heiler-inline+Boot-Sweep"]
  affects: ["services/slow_lane.py", "services/intent_payload.py", "services/intent_event_writer.py", "services/claude_service.py", "routes/app_routes.py", "app.py", "database/models.py", "alembic/versions/0031_*"]
tech_stack:
  added: ["services/intent_payload.py"]
  patterns: ["terminal-status-drain", "fan-in-heiler", "ttft-monotonic-measurement"]
key_files:
  created:
    - services/intent_payload.py
    - alembic/versions/0031_intent_event_handling_status_schild.py
    - tests/test_f2_stilllegung.py
    - tests/test_intent_payload_guard.py
    - tests/test_heiler_resolved.py
  modified:
    - services/slow_lane.py
    - services/intent_event_writer.py
    - services/claude_service.py
    - routes/app_routes.py
    - app.py
    - database/models.py
    - tests/test_handling_score_marker.py
    - tests/test_slowlane_callid_defensive.py
    - tests/test_slow_lane_tenant_guc.py
decisions:
  - "F2-Stilllegung: alle 'pending'-Events werden in _persist_event_ref sofort auf 'not_gradable' gesetzt (terminal, deadlock-frei via _pending_events-Zaehler)"
  - "build_intent_payload() in eigenem Modul (services/intent_payload.py) — klar abgegrenzte Verantwortung, Whitelist-testbar"
  - "TTFT: monotonic-Messung inside while-attempts-Schleife, print statt DB-Write (leichtgewichtig)"
  - "Heiler als non-fatal except-Handler — kein neuer Thread, kein Sweep-Loop, einmaliger Boot-Sweep via NERVE_TESTING-Guard"
metrics:
  duration: "~2h (inkl. 4 Deploy-Iterationen fuer stale-Test-Fixes + TTFT-Isolation-Bug)"
  completed: "2026-07-04"
  tasks_completed: 4
  files_changed: 14
---

# Phase 08.23.2.PERSID Plan 02: Welle 1 — F2-Stilllegung + 3 Waechter (a/b/c) Summary

**One-liner:** `not_gradable`-Terminal in `_persist_event_ref` (PERSID Req 8) stoppt per-Ereignis-Benoter deadlock-frei; `build_intent_payload()` ausgelagert; TTFT-Messung im manuellen EWB-Pfad; Heiler-Flags fuer haengende Calls.

## Objective Achieved

Alle 4 Deliverables aus Plan 02 sind live auf Production (817 passed, 9 skipped, deploy.sh gate GRUEN):

1. **F2-Stilllegung (PERSID Req 8):** `_persist_event_ref` setzt `handling_status='not_gradable'` fuer ALLE `pending`-Events (nach Idempotenz-Skip, vor Confidence-Gate). Terminal-Status drainet `_pending_events` auf 0 → Call-Ende-Merge/rubric_score feuert normal. S7 Punkt-23-Pflicht erfuellt: `models.py`-Kommentar + Alembic-Migration 0031 im selben Commit.

2. **Waechter (a) — `build_intent_payload()`:** Funktion aus `intent_event_writer.py:100-118` in neues Modul `services/intent_payload.py` extrahiert. `_REQUIRED = frozenset({9 Pflichtfelder})` whitelist-getestet. `schema_version` explizit NICHT required (TAXO3-b-Reserve).

3. **Waechter (b) — TTFT-Messung:** `[EWB-TTFT] Xms model=... sid=... path=manual` in `streame_manual_ewb_variante` via `time.monotonic`. Kein Coupling an `_ewb_ttft_history` (S6-Separation).

4. **Waechter (c) — Heiler:** Inline-Heiler in `app_routes.py` except-Block + einmaliger Boot-Sweep in `app.py` (hinter `NERVE_TESTING`-Guard). Beide setzen `audio_health_resolved=True` + `transcript_resolved=True`. Boot-Sweep filtert NUR `ended_at IS NOT NULL` (Punkt-26-konform).

## Commits

| Hash | Typ | Beschreibung |
|------|-----|--------------|
| `4e004ee` | test | RED — F2-Stilllegung not_gradable-Terminal-Tests |
| `f0be2b8` | feat | Task 1 GREEN — F2-Stilllegung not_gradable-Terminal + S7 Schild |
| `e9ea56b` | test | RED — Waechter (a) build_intent_payload Whitelist-/Pflichtfeld-Tests |
| `42da8b6` | feat | Task 2 GREEN — Waechter (a) build_intent_payload() ausgelagert |
| `299ad51` | test | RED — Waechter b TTFT + Waechter c Heiler inline/Boot-Sweep |
| `8277731` | feat | Task 3 GREEN — TTFT-Messung + Heiler inline + einmaliger Boot-Sweep |
| `9b666a1` | fix | TTFT-Test korrigiert — extensions.socketio statt cs.sio mocken |
| `53d0696` | fix | stale Tests auf F2-Stilllegung aktualisiert (Rule 1 — Deviation) |
| `e806c21` | fix | Test-Fixture-Fehler behoben — cleanup_rows importiert statt als Fixture |
| `c30cb86` | fix | make TTFT test robust against test-ordering FakeAnthropic |

## Deploy Gate

`bash deploy.sh production` 4. Versuch: **817 passed, 9 skipped, 5 deselected** — GRUEN.
Migration 0031 applied (`0030 -> 0031`). Service gestartet, gunicorn aktiv.

## _pending_events Drain-Beleg

`test_f2_not_gradable_drains_pending_events` beweist: nach `_persist_event_ref({'event_id': eid}, db_session)` ist `_pending_events(call_id, db_session) == 0`. Kein Deadlock moeglich.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale Tests auf F2-Stilllegung aktualisiert**
- **Gefunden bei:** Erstem deploy.sh-Lauf (8 FAILED + 6 ERROR)
- **Problem:** 4 Tests in `test_handling_score_marker.py` erwarteten `scored`/`abstained`/`failed`-Pfade die durch F2-Stilllegung nicht mehr erreichbar sind. Ebenso 2 Tests in `test_slowlane_callid_defensive.py` und 1 Test in `test_slow_lane_tenant_guc.py`.
- **Fix:** Tests auf `not_gradable`-Assertion umgeschrieben + kein `abstain_log` mehr erwartet. Alte Test-Namen auf neue Semantik umbenannt.
- **Dateien:** `tests/test_handling_score_marker.py`, `tests/test_slowlane_callid_defensive.py`, `tests/test_slow_lane_tenant_guc.py`
- **Commit:** `53d0696`

**2. [Rule 1 - Bug] cleanup_rows war kein pytest-Fixture**
- **Gefunden bei:** 2. deploy.sh-Lauf (6 ERRORs)
- **Problem:** `cleanup_rows` in `tests/conftest.py` ist eine plain-Funktion, kein `@pytest.fixture`. Nutzung als Funktionsparameter fuehrte zu "fixture 'cleanup_rows' not found" Fehlern.
- **Fix:** `from tests.conftest import cleanup_rows` als direkter Import in Testdateien. Aus Parameterlisten entfernt.
- **Dateien:** `tests/test_f2_stilllegung.py`, `tests/test_heiler_resolved.py`
- **Commit:** `e806c21`

**3. [Rule 1 - Bug] TTFT-Test scheiterte an Test-Reihenfolge (_FakeAnthropic)**
- **Gefunden bei:** 3. deploy.sh-Lauf — Test passierte in Isolation, scheiterte in voller Suite
- **Problem:** `test_08_5_05_training_pipeline_t1.py` installiert `_FakeAnthropic`-Stub via `sys.modules.setdefault('anthropic')`. Lief diese Datei vor `claude_service.py`-Import, war `cs.claude_client` ein `_FakeAnthropic`-Objekt ohne `.messages`-Attribut. `monkeypatch.setattr(cs.claude_client.messages, 'stream', ...)` warf `AttributeError`.
- **Fix:** `cs.claude_client` vollstaendig durch `MagicMock()` ersetzt (statt Attribut-Patch auf dem echten Objekt).
- **Dateien:** `tests/test_heiler_resolved.py`
- **Commit:** `c30cb86`

**4. [Rule 1 - Bug] TTFT-Test Mock-Strategie fuer extensions.socketio**
- **Gefunden bei:** Debugging des Isolation-vs-Suite-Unterschieds
- **Problem:** Erster Fix-Versuch via `monkeypatch.setitem(sys.modules, 'extensions', mock_ext)` ersetzte nur `sys.modules`-Eintrag, aber `from extensions import socketio as sio` in `streame_manual_ewb_variante` nutzt das bereits gecachte echte Modul.
- **Fix:** `import extensions as _real_ext` + `monkeypatch.setattr(_real_ext, 'socketio', MagicMock())` — patcht das Attribut auf dem ECHTEN Modul-Objekt.
- **Dateien:** `tests/test_heiler_resolved.py`
- **Commit:** `9b666a1`

## Known Stubs

Keine. Alle implementierten Pfade sind produktiv verdrahtet.

## Threat Flags

Keine neuen sicherheitsrelevanten Oberflaechen einfuehrt. F2-Stilllegung reduziert Attack-Surface (kein LLM-Aufruf mehr fuer per-Ereignis-Benoter).

## Self-Check: PASSED

- `services/intent_payload.py` — FOUND
- `alembic/versions/0031_intent_event_handling_status_schild.py` — FOUND
- `tests/test_f2_stilllegung.py` — FOUND
- `tests/test_intent_payload_guard.py` — FOUND
- `tests/test_heiler_resolved.py` — FOUND
- Commit `4e004ee` — FOUND (RED Task 1)
- Commit `f0be2b8` — FOUND (GREEN Task 1)
- Commit `42da8b6` — FOUND (GREEN Task 2)
- Commit `8277731` — FOUND (GREEN Task 3)
- Commit `c30cb86` — FOUND (fix TTFT)
- Deploy gate: 817 passed, 9 skipped — GRUEN
- Migration 0031 applied: Running upgrade 0030 -> 0031 — BESTAETIGT
