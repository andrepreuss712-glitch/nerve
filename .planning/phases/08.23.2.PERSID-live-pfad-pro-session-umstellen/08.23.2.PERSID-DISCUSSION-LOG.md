# Phase 08.23.2.PERSID: Live-Pfad per-Session — Discussion Log

> **Audit trail only.** Nicht als Input für Planning/Research/Execution nutzen.
> Entscheidungen stehen in CONTEXT.md — dieses Log bewahrt die erwogenen Alternativen.

**Date:** 2026-07-03
**Phase:** 08.23.2.PERSID-live-pfad-pro-session-umstellen
**Areas discussed:** Zugriffsmuster, Wächter-Strenge, Wellen-Schnitt
**Nicht diskutiert (SPEC-Default):** Heiler-Mechanismus (→ inline-terminal, Punkt-22-konform)

---

## Gray-Area-Auswahl (multiSelect)

| Option | Beschreibung | Gewählt |
|--------|--------------|---------|
| Zugriffsmuster | ~40× Kern-Mechanismus: Raw-Dict vs. Accessor-Helfer vs. State-Objekt | ✓ |
| Wächter-Strenge | Whitelist-Strenge des statischen Global-Wächters (Punkt 28) | ✓ |
| Heiler-Mechanismus | Wächter (c) inline vs. Sweep vs. Startup | (SPEC-Default) |
| Wellen-Schnitt | Migrations-Granularität + rot-vor-Fix-Staging | ✓ |

---

## Zugriffsmuster

| Option | Beschreibung | Selected |
|--------|--------------|----------|
| Raw-Dict, faithful | `_session_state[sid][key]` unter Lock, kein neuer Layer (Punkt 17) | ✓ |
| Dünne Accessor-Helfer | get_ss/set_ss kapseln Locking + Guard, aber neue 40-Stellen-Oberfläche | |
| Per-Session State-Objekt | Sauberste API, aber größter Umbau (Punkt-17-Verstoß) | |

**User's choice:** Raw-Dict, faithful.
**Notes:** Faithful zum bestehenden Muster (cost_tracker, init_session_state); "ohne sid → None"-No-Op
übernehmen; Lock-Disziplin bleibt manuell und wird im Review hart geprüft.

---

## Wächter-Strenge

| Option | Beschreibung | Selected |
|--------|--------------|----------|
| Streng | Guard poliziert nur Live-Pfad-Globale; enge dokumentierte Whitelist; kein Deprecated-Zustand | ✓ |
| Pragmatisch | Zusätzlich temporäre Deprecated-Globale via Marker + Foundation-Register | |

**User's choice:** Streng.
**Notes:** Pragmatische Variante nur als Fallback, falls Inventur beweist dass ein Live-Global in dieser
Phase nicht migrierbar ist.

---

## Wellen-Schnitt

| Option | Beschreibung | Selected |
|--------|--------------|----------|
| Test-first, staged | Welle 0 Guards rot vs HEAD → pro-Modul-Wellen → Test grün | ✓ (als Gerüst) |
| Vertikale Slices pro Fund | Feingranular, aber Test stückweise grün | |
| Ein atomarer PR | Kleinste Zwischenzustände, größtes Blast-Radius-Risiko | |

**User's choice:** Option 1 als Gerüst — MIT vier harten Verfeinerungen (Founder-Direktive):
1. **Schnitt nach DATEN-FAMILIE statt nach Modul.**
2. **Eiserne Regel:** Leser einer Familie NIE vor seinen Schreibern umstellen — Schreiber+Leser derselben
   Familie im selben Deploy (sonst leere Call-Records auf Prod: api_beenden liest conversation_log/
   painpoints/kaufbereitschaft/gegenargument_log/phasen_log/covered_phases, geschrieben von
   claude_service/deepgram).
3. **Welle 0 zusätzlich:** pro Global Fix-vs-Löschen mit grep-Beleg — viel toter write-only-Code
   (session_meta, last_postcall, hilfe_log/quick_action_log, aktive_phase_idx, roles_swapped, Teile von
   state) → löschen statt migrieren.
4. **Rot-Beweis manuell gegen HEAD auf dem Server dokumentieren** (committeter roter Test würde deploy.sh-
   Gate für ALLE Deploys blockieren); Voll-Test erst mit letzter Welle grün.

**Notes:** Volle Details + neue disconnect-vor-beenden-Race im aktualisierten Bau-Vorgabe-Dokument
(Vault 03 Planung, aktualisiert 2026-07-03; Gotcha c: /api/beenden VOR socket-disconnect, app_routes:165-171).

---

## Claude's Discretion

- **Heiler (Wächter c):** SPEC-Default = inline-terminal an api_beenden-Fehlerstelle (kein neuer Zeit-Sweep,
  Punkt 22). Optional Startup-Sweep für Alt-Rows — Planner-Entscheidung.
- **Konkrete Wellen-Reihenfolge** innerhalb des Daten-Familie-Schnitts (unter Einhaltung von D-08).

## Deferred Ideas

- Heiler als periodischer Background-Sweep (Punkt-22-Risiko).
- Accessor-Layer / State-Objekt (Refactor-huckepack, Punkt 17).
- SPEC-RAUS-Punkte (F3/F4/F5, RLS-Härtung, DB-Feld-Löschung, schema_version → TAXO3-b).
