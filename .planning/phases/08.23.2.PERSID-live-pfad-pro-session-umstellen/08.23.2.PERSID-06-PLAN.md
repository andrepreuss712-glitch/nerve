---
phase: 08.23.2.PERSID
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - services/live_session.py
  - services/deepgram_service.py
  - services/claude_service.py
  - services/einwand_keyword_matcher.py
  - routes/app_routes.py
  - tests/test_persid_concurrency.py
  - tests/test_no_live_global_state.py
  - tests/test_suggestion_reactions.py
autonomous: false
requirements: [SPEC-Req-2, SPEC-Req-4, SPEC-Req-7, SPEC-Req-12]

must_haves:
  truths:
    - "record_suggestion_offer/record_ewb_click schreiben pro sid; ALLE 6 Caller (deepgram:1006/1017/1026/1041, matcher:338, claude:722) sind sid-durchgereicht — kein TypeError im except-swallow (B4)"
    - "reset_session(sid) raeumt NUR die eigene sid; andere aktive Sessions bleiben unberuehrt (Reset-Ueberlebens-Check mit Objekt-Identitaet); reset_session poppt AUCH den Snapshot (pop_ended_session/_ended_session_snapshots.pop, N-3 finales Cleanup)"
    - "Die Speaker-Familie (_log_last_sp, _second_sp_seen) ist pro sid; _bof_count-Modul-Global ist geloescht (schon per-sid claude:1711)"
    - "test_persid_concurrency.py ist voll scharf, GRUEN committet; der statische Waechter deckt ALLE getoeteten Globalen ab und _PENDING_MIGRATION ist LEER (B3)"
    - "Der Concurrency-Test deckt BEIDE Beenden-Naehte ab: stop_live_session-VOR-api_beenden (Haupt-Pfad, :779-Stash) UND disconnect-vor-beenden (:815) → beide liefern einen NICHT-leeren Call-Record (B1 Stash-Fundament aus Plan 03); PLUS Doppel-Feuer (:779 dann :815-setdefault) haelt vollen Snapshot (N-1)"
    - "test_suggestion_reactions.py ist auf die neue sid-erste record_suggestion_offer-Signatur + per-sid-Bucket umgestellt (W-2) — kein Gate-Rot durch alte Signatur"
  artifacts:
    - path: "tests/test_persid_concurrency.py"
      provides: "Voller 2-Tenant-Concurrency-Test, 7 gepaarte Assertions + Reset-Ueberleben + Same-User-Zwei-Session + B1-Late-Write-Ghost-Drop + B1-stop_live_session-vor-beenden-Non-Empty + N-1-Doppel-Feuer-Non-Empty, GRUEN committet"
      min_lines: 120
    - path: "services/live_session.py"
      provides: "reset_session(sid) — nur eigene sid + Snapshot-Pop (N-3); record_* pro sid; Speaker-Globale + _bof_count-Modul-Global weg"
  key_links:
    - from: "app_routes.py:834"
      to: "reset_session(_beenden_sid)"
      via: "sid-Argument (nur eigene sid raeumen + Snapshot poppen, N-3)"
      pattern: "reset_session\\("
    - from: "deepgram_service.py:1006"
      to: "record_ewb_click(sid, ...)"
      via: "sid durchgereicht (ALLE 6 Caller, B4)"
      pattern: "record_ewb_click"
    - from: "tests/test_persid_concurrency.py"
      to: "get_anonymisierer(sidB) is anon_b_before"
      via: "Reset-Ueberlebens-Check (Objekt-Identitaet)"
      pattern: "is anon_b_before"
---

<objective>
**Letzte Welle: Familie D (EWB/Suggestion, Req 2) + Familie E (Speaker + reset_session(sid), Req 4/7) + Voll-Test GRUEN (Req 12).**

**D-08-Beweis:**
- Familie D: Schreiber record_ewb_click/record_suggestion_offer + ALLE 6 Caller + Leser app_routes → beide DIESER Deploy.
- Familie E: Speaker-Globale (deepgram :71,:77) + reset_session-Fix (app_routes:834) → koppeln ueber pop_session_state. **reset_session(sid) ist erst umstellbar, wenn ALLE Familien per-sid sind** → LETZTE Welle.

**B4 — record_*-Caller VOLLSTAENDIG (Fresh-Grep 2026-07-03, korrigiert die urspruengliche 2-Caller-Liste):**
`record_suggestion_offer` hat DREI Caller, nicht einen:
- deepgram_service.py:1017 (PiP-Button-Pfad)
- **services/einwand_keyword_matcher.py:338 (LIVE — Slot-A-Keyword-Pfad, Meeting)**
- services/claude_service.py:722 (PiP-AutoVar-Pfad — grep zeigt aktiv, nicht dormant wie Fable vermutete: der Aufruf steht im PiP-AutoVar-Capture-Hook)
`record_ewb_click` hat DREI Caller:
- deepgram_service.py:1006 (success=True)
- **deepgram_service.py:1026** (Error-Pfad success=False)
- **deepgram_service.py:1041** (Error-Pfad success=False, nach Streaming)
Alle sechs Aufrufe stehen in `try/except`, das Exceptions SCHLUCKT (`print(...skip...)`). Macht man die Signatur sid-pflichtig und passt nur 2 Caller an → die anderen werfen `TypeError` → vom except geschluckt → **Slot-A-Keyword-Vorschlaege + Error-EWB-Clicks gehen STILL verloren** (Plan-06-"gruen aber tot"-Klasse). → ALLE 6 Caller + ein grep-Vollstaendigkeits-Check.

**B1-Late-Write-Ghost-Drop (aus Plan 03):** record_ewb_click/record_suggestion_offer nach Stream-Ende/stash (via stop_live_session :779 ODER disconnect :815) duerfen eine tote/gestashte sid NICHT wiederbeleben (Ghost-SID-Guard) — hier eine Assertion im Voll-Test.

**B1-Beenden-Naht-Deckung im Voll-Test (Blocker-Fix, aus Plan 03):** der Normal-Hangup poppt via `stop_live_session` (:779), NICHT via `disconnect` (:815) — Plan 03 stasht deshalb BEIDE. Der Voll-Test MUSS den Haupt-Pfad exerzieren: `stop_live_session` fuer sidA feuern BEVOR api_beenden(sidA) laeuft, und assertieren dass der A-Call-Record NICHT-leer ist. Plus **N-1-Doppel-Feuer** (stop_live_session :779 dann disconnect :815, dessen setdefault :810-811 ein leeres {} erzeugt) → Snapshot bleibt voll, Record NICHT-leer. → eigene Assertionen (Behavior/Action/Acceptance in Task 3).

**N-3-Snapshot-Cleanup gehoert in reset_session (aus Plan 03):** `consume_ended_session` ist ein NICHT-destruktiver PEEK (Plan 03 N-3). Das FINALE Entfernen des Snapshots passiert am ENDE von api_beenden — in `reset_session(_beenden_sid)` (Task 2c): ein `pop_ended_session(sid)` (bzw. `_ended_session_snapshots.pop(sid, None)` unter Lock) raeumt den Snapshot fuer die beendete sid. So bleibt Doppel-Beenden gutartig (der Peek liefert bis zum reset denselben Inhalt), und der Snapshot waechst nicht ueber die TTL hinaus.

**D-10 (OPERATIV KRITISCH):** Der Voll-Concurrency-Test wird ERST JETZT GRUEN committet + ins deploy.sh-Gate gehaengt. In allen Vorwellen war er Skeleton/geskippt. Der Rot-vor-Fix-Beweis pro Welle war ein DOKUMENTIERTER manueller HEAD-Lauf auf dem Prod-Server.

**B3 — _PENDING_MIGRATION MUSS LEER sein:** der statische Waechter (Plan 01) trug eine wave-getaggte Pending-Migration-Liste; jede Welle hat ihre Zeilen entfernt. Plan 06 Task 3 assertet `_PENDING_MIGRATION == frozenset()` (bzw. leeres Set) — ab jetzt ist JEDE nicht-per-sid Live-Zuweisung rot. **W-b (aus Plan 03):** falls der skript-Kandidat (aktives_skript_inhalt/skript_bloecke) in Plan 01 als FIX entschieden wurde, wurde er in Plan 03 Task 3d unter Welle-A-Tag ins _PENDING_MIGRATION eingetragen UND in derselben Welle wieder gestrichen — er darf hier also NICHT mehr in _PENDING_MIGRATION stehen; falls DELETE, steht er im _KILLED. Beide Faelle → _PENDING_MIGRATION leer. Task 3 verifiziert das.

**HIGH-Drift 2 reconciled (Fresh-Grep bestaetigt):** reset_session-Caller ist app_routes.py:834 (`reset_session()`), NICHT :480.

**_bof_count (Fresh-Grep):** die LIVE-Nutzung ist bereits per-sid (claude_service.py:1711-1714 `sid_state_ref['_bof_count']`); die Modul-Globale live_session.py:174 (+ reset :944 + Seed :321) ist ein toter Rest → DELETE (Plan-01-Verdikt fuer _bof_count = DELETE).

**REST-RISIKO (aus Plan 03 Fable-Delta — BEOBACHTEN, NICHT hier fixen):**
- **vorwissen_level-Beifang:** der `vorwissen_level`-Write (deepgram_service.py:622-628, Fresh-Grep: `ls._session_state.setdefault(_sid, {}); ls._session_state[_sid]['vorwissen_level'] = _vorwissen`) sitzt VOR `init_session_state` (:675) — genau wie die N-4-Anrede-Falle wird er bei Reconnect vom :674-pop+:675-init weggewischt. **Pre-existing Bug, AUSSERHALB des PERSID-Scope** (kein neuer per-sid-Split, sondern ein Init-Ordering-Rest) → NICHT hier fixen; im SUMMARY als Aufraeum-Kandidat vermerken (Muster: gleicher Fix wie N-4 — Local capturen, nach init schreiben — in einer spaeteren Cleanup-Phase).
- **Merge-Fenster-Verlust beim Stash:** der :779/:815-Stash cancelt den `_merge_pending`-Timer (Plan 04) → die letzten ≤MERGE_WINDOW_S Transkript entfallen deterministisch beim Auflegen. Klein + akzeptiert (ein bewusster Ein-Zeilen-Verlust am Call-Ende, kein Datenschaden am persistierten Record) — im SUMMARY notieren. Ein optionaler Flush-at-Stash (falls 1-2 Zeilen) darf, ist aber kein Blocker.

Purpose: Knopf-Ereignisse + Speaker + Reset pro sid; das komplette Fundament durch den scharfen Gate-Test abgesichert.
Output: record_* pro sid (alle 6 Caller), reset_session(sid) inkl. Snapshot-Pop (N-3), Speaker-Globale + _bof_count-Modul-Global weg, voller GRUENER Concurrency-Test, _PENDING_MIGRATION leer, test_suggestion_reactions.py auf neue Signatur (W-2).

**Komplexitaet: 🔴 — Cross-AI PFLICHT. Der Voll-Test ist der eigentliche Beweis. autonomous:false (Founder-UAT-Checkpoint).**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-SPEC.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-CONTEXT.md
@.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Familie D — record_* (live_session.py:983-1023, FRESH-GREP) — aktuell OHNE sid, global state.setdefault -->
def record_ewb_click(einwand_typ, success=False, antwort_text=None, einwand_text=None):  # :983
    with state_lock: state.setdefault('ewb_clicks', []).append(entry)   # :994-995 → per-sid
def record_suggestion_offer(slot, source, model, suggestion_text, interaction_id, einwand_typ=None):  # :998
    with state_lock: state.setdefault('suggestion_offers', []).append(entry)   # :1022-1023 → per-sid

<!-- ALLE 6 CALLER (B4, FRESH-GREP 2026-07-03) — sid im Scope, alle im except-swallow -->
record_ewb_click:        deepgram_service.py:1006 (success=True), :1026 (Error), :1041 (Error)
record_suggestion_offer: deepgram_service.py:1017, einwand_keyword_matcher.py:338 (LIVE Slot-A), claude_service.py:722 (PiP-AutoVar)
<!-- Reader: app_routes.py:324 (ewb_clicks → ObjectionEvent + einwaende_gesamt), :327 (suggestion_offers → suggestion_reactions-Flush) — via _bs (Plan 03/05) -->
<!-- Seeds: _session_state[sid]['state']['ewb_clicks'] :336 (im 'state'-SUBDICT, W-1-Klasse); suggestion_offers-Sub-Key ergaenzen falls fehlt -->

<!-- Familie E — Speaker (deepgram_service.py, FRESH-GREP) -->
:71 ls._second_sp_seen = True   :72 roles_confirmed = ls._second_sp_seen   # → per-sid (Seed :421)
:77 ls._log_last_sp = speaker   :78 log_sp = ... ls._log_last_sp          # → per-sid (Seed :325, Cross-Tenant-Speaker-Leak §6.7)
<!-- _bof_count: LIVE schon per-sid (claude:1711-1714); Modul-Global live_session.py:174 = toter Rest → DELETE -->

<!-- reset_session — aktuell no-arg, iteriert ALLE SIDs (live_session.py:864) -->
def reset_session():   # → def reset_session(sid):   Caller: app_routes.py:834 (FRESH-GREP, NICHT :480)
<!-- N-3: reset_session(sid) poppt AUCH _ended_session_snapshots[sid] (finales Snapshot-Cleanup, Plan 03 PEEK-Gegenstueck) -->

<!-- Reset-Ueberlebens-Check (SPEC Req 4 Acceptance) -->
get_anonymisierer(sid)   # Objekt-Identitaet vor/nach Reset von sidA muss fuer sidB gleich sein

<!-- test_suggestion_reactions.py (W-2, FRESH-GREP 2026-07-03) — alte Signatur, Gate-rot ohne Fix -->
tests/test_suggestion_reactions.py:205-206  ls.record_suggestion_offer(slot='B', source=..., ...)   # OHNE sid-erst → TypeError unter neuer Signatur
tests/test_suggestion_reactions.py:234-235  ls.record_suggestion_offer(slot='A', source='keyword', ...)  # dito; :203/:208/:232-237 lesen/schreiben globale ls.state['suggestion_offers']

<!-- B1 Beenden-Naehte (Plan 03): stop_live_session :779 (Normal-Hangup, Haupt-Pfad) UND disconnect :815 stashen (Leer-Skip N-1 gegen setdefault :810-811); :674 reconnect exempt -->
<!-- Normal-Hangup-Reihenfolge: pip-launcher _stopMic emit('stop_live_session') → fetch('/api/beenden') → Test muss stop_live_session VOR api_beenden feuern -->

<!-- Test-Muster (RESEARCH §5): _OneShotTrigger, _seed_call, socketio.test_client x2, anonymize IDENTITY, -->
<!-- MERGE_WINDOW_S=0.05, /api/beenden VOR disconnect, NERVE_TESTING=1 ENV-Guard (Plan 01, S1) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Familie D — record_ewb_click/record_suggestion_offer pro sid + ALLE 6 Caller (B4) + test_suggestion_reactions.py auf neue Signatur (W-2)</name>
  <read_first>
    - services/live_session.py:983-1023 (beide record_-Funktionen — Signatur ohne sid, state.setdefault), :336 (ewb_clicks-Seed im 'state'-SUBDICT)
    - services/deepgram_service.py:1000-1043 (Caller :1006/:1017/:1026/:1041 im manual_ewb-_run, alle im try/except-swallow)
    - services/einwand_keyword_matcher.py:335-345 (Caller :338 — LIVE Slot-A-Keyword-Pfad, sid im Scope, except-swallow :344)
    - services/claude_service.py:705-728 (Caller :722 — PiP-AutoVar-Capture-Hook, sid im Scope, except-swallow :727)
    - routes/app_routes.py:320-330 (ewb_clicks-Read :324; suggestion_offers-Read :327 — via _bs, Plan 03/05)
    - tests/test_suggestion_reactions.py:198-241 (W-2: :205-206 + :234-235 rufen record_suggestion_offer mit ALTER Signatur; :203/:208/:232-237 globale ls.state['suggestion_offers'])
    - RESEARCH.md §2 Familie D + §1 (record_* Punkt-29-Falle live belegt)
    - CLAUDE.md Punkt 29 (record_suggestion_offer kopierte "EXAKT das Muster von record_ewb_click")
  </read_first>
  <behavior>
    - Test 1 (gepaart): sidA record_ewb_click('Kosten'), sidB record_ewb_click('Zeit'). _session_state[A]['state']['ewb_clicks'] enthaelt 'Kosten' (DA), NICHT 'Zeit' (WEG). Analog suggestion_offers.
    - Test 2 (D-02): record_ewb_click ohne/None sid → No-Op (kein globaler Fallback).
    - Test 3 (B4 — kein still-toter Caller): der Slot-A-Keyword-Pfad (matcher:338) UND der PiP-AutoVar-Pfad (claude:722) UND die Error-Pfade (deepgram:1026/1041) rufen record_* mit sid — kein TypeError, kein except-Schluck. Test ruft jeden Pfad (bzw. die Funktion mit der neuen Signatur) und prueft, dass der Eintrag im per-sid-Bucket landet.
    - Test 4 (B1-Ghost-Drop): record_* fuer eine tote/nicht-existente sid → No-Op, kein Wiederbeleben (Ghost-SID-Guard).
  </behavior>
  <action>
    a) `record_ewb_click` + `record_suggestion_offer` (live_session.py:983,998): Signatur um `sid` als ERSTES Argument erweitern — `record_ewb_click(sid, einwand_typ, success=False, ...)` / `record_suggestion_offer(sid, slot, source, ...)`. Schreiben in `_session_state[sid]['state']['ewb_clicks']` / `['suggestion_offers']` unter _session_state_lock, Ghost-SID-Guard (`if sid in _session_state`). D-02: ohne/tote sid → return (No-Op, kein globaler state.setdefault). Behebt die Punkt-29-Halb-Migration live.
    b) **ALLE 6 Caller anpassen (B4):** sid als erstes Argument durchreichen — deepgram_service.py:1006, :1026, :1041 (record_ewb_click); deepgram_service.py:1017, einwand_keyword_matcher.py:338, claude_service.py:722 (record_suggestion_offer). Jeder Caller hat sid/`_sid` im Scope (grep bestaetigt).
    c) **grep-Vollstaendigkeits-Check (B4, Punkt 22, analog Plan 05):** `grep -rn "record_ewb_click\|record_suggestion_offer" services/ routes/` → JEDER Aufruf (nicht-Def, nicht-Test) reicht sid durch. 0 Aufrufe mit alter Signatur. Im SUMMARY die Caller-Liste + Klassifikation dokumentieren.
    d) Reader app_routes.py:324,327: aus dem EINEN `_bs = _load_beenden_state(_beenden_sid)` (Plan 03/05, N-3) per-sid lesen statt `ls.state.get('ewb_clicks')`. suggestion_offers-Sub-Key in init_session_state seeden falls fehlt (ewb_clicks ist :336, im 'state'-SUBDICT).
    e) Modul-`state['ewb_clicks']`/`state['suggestion_offers']`-Keys entfernen (0 Reader nach d) + reset-Reste; aus _PENDING_MIGRATION (Welle D) streichen.
    f) **W-2 — tests/test_suggestion_reactions.py auf neue Signatur + per-sid-Bucket (Fresh-Grep 2026-07-03):** die Tests `test_record_suggestion_offer_always_sets_interaction_id` (:198-211) und `test_record_suggestion_offer_does_no_db_write` (:216-240) rufen `record_suggestion_offer(slot=..., source=..., ...)` mit der ALTEN Signatur (:205-206, :234-235) und lesen/schreiben die globale `ls.state['suggestion_offers']` (:203, :208, :232-237). Unter der sid-ersten Signatur wirft der Aufruf TypeError → Gate-Rot. → beide auf `record_suggestion_offer(sid, slot=..., ...)` mit einer echten Test-sid umstellen; die Assertions gegen den per-sid-Bucket `_session_state[sid]['state']['suggestion_offers']` (statt globale `ls.state`) fuehren. Der interaction_id-Durchreich-Beleg (:209-211) + der Latenz-Kein-DB-Write-Beleg (:239-240) bleiben inhaltlich gueltig — nur Signatur + Bucket-Ebene aendern. Die Punkt-25-Aussage (kein DB-Write) bleibt.
    LATENZ (Punkt 25): record_* bleibt ein reiner list.append — kein DB/Netz/LLM. Nicht verletzen.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Browser-UAT 2 Konten: Tab A klickt EWB 'Kosten', Tab B 'Zeit'; UND Tab A loest einen Slot-A-Keyword-Vorschlag aus (matcher-Pfad). `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample suggestion_reactions 20'` → je Call richtiger tenant_id, KEINE Cross-Vermischung, UND die Slot-A-Keyword-Vorschlaege sind da (nicht still verloren — B4).</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "def record_ewb_click\|def record_suggestion_offer" services/live_session.py` → Signatur nimmt sid als erstes Arg; schreibt per-sid; ohne/tote sid → No-Op
    - **B4: `grep -rn "record_ewb_click\|record_suggestion_offer" services/ routes/` → ALLE 6 Caller (deepgram:1006/1017/1026/1041, matcher:338, claude:722) reichen sid durch; 0 Aufrufe mit alter Signatur; grep-Vollstaendigkeit im SUMMARY**
    - `grep -n "ewb_clicks\|suggestion_offers" routes/app_routes.py` → per-sid via `_bs`
    - Modul-state-Keys ewb_clicks/suggestion_offers entfernt; aus _PENDING_MIGRATION gestrichen
    - **W-2: `grep -n "record_suggestion_offer" tests/test_suggestion_reactions.py` → beide Aufrufe (~:205/:234) reichen sid durch (neue Signatur); Assertions lesen den per-sid-Bucket, nicht globale `ls.state['suggestion_offers']` — kein Gate-Rot**
    - deploy.sh production GRUEN
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Familie E — Speaker-Familie pro sid + _bof_count-Modul-Global loeschen + reset_session(sid) inkl. Snapshot-Pop (N-3) (Req 4/7)</name>
  <read_first>
    - services/deepgram_service.py:68-78 (_second_sp_seen :71, _log_last_sp :77)
    - services/live_session.py:160 (_log_last_sp Modul-Def), :164 (_second_sp_seen), :174 (_bof_count Modul-Def — TOT, DELETE), :321 (_bof_count-Seed), :325/:421 (Speaker-Seeds), :864-887 (reset_session no-arg), :873-875 (global-Statements), :929/:935/:944 (Speaker/bof-Resets)
    - services/live_session.py (_ended_session_snapshots + pop_ended_session-Helfer aus Plan 03 Task 1a — fuer den N-3-Snapshot-Pop)
    - services/claude_service.py:1711-1714 (_bof_count LIVE schon per-sid `sid_state_ref['_bof_count']` — Beleg DELETE der Modul-Globale)
    - routes/app_routes.py:834 (`reset_session()`-Caller — FRESH-GREP, NICHT :480)
    - RESEARCH.md §2 Familie E + §6.2 (Caller :834) + §6.7 (_log_last_sp Cross-Tenant-Leak)
    - CONTEXT.md D-08 (reset erst umstellbar wenn alle Familien per-sid)
  </read_first>
  <behavior>
    - Test 1 (gepaart): sidA _log_last_sp gesetzt, sidB unabhaengig. _session_state[A]['_log_last_sp'] beeinflusst B NICHT (Cross-Tenant-Speaker-Leak weg).
    - Test 2 (Reset-Ueberleben, SPEC Req 4, Objekt-Identitaet): `anon_b_before = get_anonymisierer(sidB)`; `reset_session(sidA)`; danach `get_anonymisierer(sidB) is anon_b_before` (B lebt unveraendert); sidA geraeumt.
    - **Test 2b (N-3 Snapshot-Pop): nach `reset_session(sidA)` ist `_ended_session_snapshots` fuer sidA leer (der Snapshot wurde final gepoppt); sidB's Snapshot (falls vorhanden) bleibt.**
  </behavior>
  <action>
    a) **Speaker-Familie pro sid** (deepgram_service.py:71-78): `_second_sp_seen`-Write/Read (:71/:72) + `_log_last_sp`-Write/Read (:77/:78) auf `_session_state[sid]['_second_sp_seen']` / `['_log_last_sp']` (Seeds :421/:325). Unter _session_state_lock, Ghost-SID-Guard. Modul-Globale `_second_sp_seen`(:164)/`_log_last_sp`(:160) + zugehoerige Locks loeschen. Rest-Speaker-Globale (`_confirmed_speaker`/`_pending_speaker`/`_pending_since` :168-170) laut RESEARCH §1 schon per-sid → Modul-Defs loeschen (grep-0-Reader bestaetigen).
    b) **_bof_count-Modul-Global loeschen (Fresh-Grep DELETE):** die LIVE-Nutzung ist schon per-sid (claude:1711-1714 `sid_state_ref['_bof_count']`). Loesche die Modul-Globale live_session.py:174 + reset :944 + das `global _bof_count` (:875). Der Seed :321 bleibt (per-sid Quelle). grep-0-Modul-Reader bestaetigen.
    c) **reset_session(sid) inkl. Snapshot-Pop (N-3)** (live_session.py:864): Signatur `def reset_session(sid):`. Statt `active = list(_session_state.keys())` (ALLE) nur die EINE sid: `pop_session_state(sid)` (kein re-init noetig). **N-3-FINALES SNAPSHOT-CLEANUP:** zusaetzlich den gestashten Snapshot fuer diese sid poppen — `pop_ended_session(sid)` (bzw. `with _ended_snapshots_lock: _ended_session_snapshots.pop(sid, None)`), der Plan 03 Task 1a den PEEK-consume NICHT-destruktiv liess. Damit ist der Snapshot nach dem regulaeren Call-Ende sauber weg (TTL faengt nur den Abbruch-Fall). ALLE Modul-globalen Reset-Bloecke (:889-959) sind nach den Familien-Migrationen (Plan 03-06) tote Geister-Writes → entfernen, inkl. der `global`-Statements (:873-875). Was bleibt: per-sid-Cleanup fuer die EINE sid (der _merge_pending-Timer-Cancel dieser sid — schon in pop_session_state, Plan 04) + der Snapshot-Pop. reset_session wird ein duenner Wrapper (pop_session_state(sid) + pop_ended_session(sid)).
    d) **Caller** app_routes.py:834: `reset_session()` → `reset_session(_beenden_sid)`. Falls _beenden_sid None → skip (D-02, kein All-Reset). Grep alle weiteren Caller (`grep -rn "reset_session(" services/ routes/`) — Fresh-Grep zeigt 1 Caller (:834); verifizieren.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Browser-UAT 2 Konten: Tab A legt auf (Beenden) waehrend Tab B WEITER telefoniert → Tab B laeuft ungestoert weiter (Transkript/Coaching brechen NICHT ab). Der reset_session(sid)-Beweis (heute reisst A dem B den Zustand weg).</manual>
  </verify>
  <acceptance_criteria>
    - `grep -n "_log_last_sp\|_second_sp_seen" services/deepgram_service.py` → per-sid, keine Modul-Globale
    - `grep -n "^_log_last_sp\|^_second_sp_seen\|^_bof_count" services/live_session.py` → 0 Modul-Global-Defs (inkl. _bof_count, Fresh-Grep DELETE)
    - `grep -n "def reset_session" services/live_session.py` → Signatur `reset_session(sid)`; raeumt NUR die eine sid; **poppt AUCH den Snapshot (pop_ended_session/_ended_session_snapshots.pop, N-3)**
    - `grep -n "reset_session(" routes/app_routes.py` → `reset_session(_beenden_sid)` bei :834
    - reset_session enthaelt keine Modul-globalen Reset-Bloecke + keine `global`-Statements mehr
    - deploy.sh production GRUEN; Browser-UAT: B laeuft weiter wenn A auflegt
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Voller Concurrency-Test GRUEN committet + statischer Waechter finalisieren (Req 12, D-10, B3, B1, N-1)</name>
  <read_first>
    - tests/test_persid_concurrency.py (Skeleton aus Plan 01 Task 4)
    - tests/test_medium_lane_intent_event_live.py:33-175 (voll — _OneShotTrigger, _seed_call, monkeypatch-Naehte, db_session-Fixture, cleanup_rows)
    - tests/conftest.py (cleanup_rows, db_session, Baseline-Cleanup-Waechter; NERVE_TESTING=1 vor import app — S1)
    - tests/test_no_live_global_state.py (Plan 01 — _KILLED-Listen + _PENDING_MIGRATION)
    - Plan 01 Task 1 Verdikte fuer die 3 UNKLAR-Globale (aktive_phase_idx=FIX/B5, _bof_count=DELETE, skript=?/W-b)
    - Plan 03 (B1 _ended_session_snapshots + _load_beenden_state; BEIDE Beenden-Naehte :779 stop_live_session + :815 disconnect gestasht mit Leer-Skip N-1, :674 exempt) — fuer den Late-Write-Ghost-Drop-Test, den stop_live_session-vor-beenden-Non-Empty-Test UND den N-1-Doppel-Feuer-Test
    - static/pip-launcher.js:3029-3040 + :3094-3114 (Normal-Hangup feuert stop_live_session VOR api_beenden — die Reihenfolge, die der Test nachbilden muss)
    - RESEARCH.md §5 (Mock-Naehte, 7 gepaarte Assertion-Ziele, Fallen a-e)
    - CONTEXT.md D-10 + §specifics (rot-vor-Fix = manueller HEAD-Lauf, gruen-Commit erst jetzt)
  </read_first>
  <behavior>
    - 7 GEPAARTE Assertions (positiv eigenes DA + Isolation fremdes WEG), 2 Tenants (User A Org 1 / User B Org 2):
      1. _per_sid_transcript / _per_sid_coaching_buffer: A hat A-Segmente, NICHT B-Segmente.
      2. ConversationLog-Felder: A-Record nur A-Inhalt.
      3. transcript_segments (DB): A-Rows keine B-Woerter.
      4. ObjectionEvent: A-Events nicht bei B.
      5. suggestion_reactions: je Call richtiger tenant_id.
      6. TXT-Log-Datei: A-Datei nur A-Inhalt.
      7. Emit-Schicht: client_B.get_received() enthaelt NIE den A-MARKER.
    - + Reset-Ueberlebens-Check: get_anonymisierer(sidB) is anon_b_before nach reset_session(sidA); + Snapshot-Pop-Check (N-3): _ended_session_snapshots[sidA] weg nach reset.
    - + **B1-stop_live_session-vor-beenden-Non-Empty (Blocker-Deckung):** feuere fuer sidA `stop_live_session` (der Haupt-Pfad, poppt-jetzt-stasht via :779) BEVOR api_beenden(sidA) laeuft; assert der A-Call-Record ist NICHT-leer (eigene DA praesent — Transkript/anrede/ewb). Exerziert den :779-Stash.
    - + **N-1-Doppel-Feuer-Non-Empty (Blocker-Deckung):** fuer sidA `stop_live_session` (:779, voller Stash) → dann `disconnect` (:815, dessen setdefault :810-811 ein leeres {} erzeugt) → dann api_beenden(sidA) → Record NICHT-leer (der leere zweite Stash hat den vollen Snapshot NICHT ueberschrieben, Leer-Skip/first-wins).
    - + **B1-Late-Write-Ghost-Drop:** nach /api/beenden+stash fuer sidA ein record_ewb_click(sidA, ...) / record_suggestion_offer(sidA, ...) → belebt die tote/gestashte sidA NICHT wieder (kein neuer Eintrag, kein Snapshot-Write); der bereits gestashte/persistierte A-Record bleibt korrekt.
    - + Same-User-Zwei-Session-Behandlung (W3b): Action e).
  </behavior>
  <action>
    a) Den Skeleton (Plan 01) zum vollen Test ausbauen. PFLICHT-Fallen (RESEARCH §5):
    - (a) `anonymize`/`anonymize_output`/`anonymize_for_storage` als IDENTITY patchen (sonst Vacuous-Green).
    - (b) **ENV `NERVE_TESTING=1` VOR `import app`** (S1, Plan 01) — in der Fixture/conftest gesetzt, damit die Daemons nicht starten.
    - (c) `MERGE_WINDOW_S` → 0.05 patchen (services.deepgram_service + config).
    - (d) **BEIDE Beenden-Naehte + Doppel-Feuer testen (Blocker-Deckung):** Der Normal-Hangup poppt via `stop_live_session` (:779), NICHT via `disconnect`. Also: fuer sidA `stop_live_session` feuern (emit) VOR `/api/beenden`(sidA) → mit B1-Snapshot (Plan 03) bleibt der Record erhalten; assert NON-EMPTY. Zusaetzlich mindestens EINE Session ueber den `disconnect`-Pfad (:815) beenden (zweite Naht). Und der N-1-Doppel-Feuer-Fall (siehe unten). Reihenfolge korrekt gepaart/isoliert (SPEC Req 12).
    - (e) rot-vor-Fix: KEIN committeter roter Test; der Beweis ist der pro-Welle dokumentierte manuelle Serverlauf. Hier: GRUEN committen.
    - Zwei `socketio.test_client(app)`, je eigene Flask-Session (User A Org 1 / User B Org 2 — NICHT ein User mit zwei Tabs). start_live_session je Client, Transkript-Segmente (eindeutige Marker), record_ewb_click je Client, /api/beenden je Client (mit korrekter Beenden-Naht-Reihenfolge: stop_live_session-emit VOR api_beenden fuer den Haupt-Pfad).
    - `_seed_call` + zwei echte Org/User-Rows als FK-Ziele; cleanup_rows im Teardown (Test-Cleanup-Regel).
    - `_open_deepgram_connection` patchen; `analysiere_mit_claude`/Haiku → _fake; `_qa_pipeline_dispatch` → None.
    - KEIN live/perf-Marker → laeuft im Default-Gate.

    **B1-stop_live_session-vor-beenden-Non-Empty-Assertion (Blocker):** feuere `client_A.emit('stop_live_session')` (Haupt-Pfad :779-Stash) BEVOR `client_A` `/api/beenden` postet; assert der A-Call-Record traegt A-Inhalt (Transkript-Marker + anrede + ewb — NICHT leer). Ohne den Plan-03-:779-Stash waere der Record leer.

    **N-1-Doppel-Feuer-Assertion (Blocker):** feuere `client_A.emit('stop_live_session')` (:779, voller Stash), DANN simuliere `handle_disconnect` fuer sidA (bzw. `client_A.disconnect()`, das :815 mit dem setdefault :810-811 ausloest), DANN `/api/beenden`(sidA); assert der A-Record ist NICHT-leer (der leere zweite Stash hat den vollen Snapshot nicht ueberschrieben — Leer-Skip/first-wins aus Plan 03 N-1). Ohne N-1-Fix waere der Record leer.

    **B1-Ghost-Drop-Assertion:** nach api_beenden+stash fuer sidA einen Late-Write `record_ewb_click(sidA, 'LateGhost')` absetzen; assert der A-Call-Record/suggestion_reactions traegt KEINEN 'LateGhost'-Eintrag und keine neue Live-Session fuer sidA existiert.

    **N-3-Snapshot-Pop-Assertion:** nach der regulaeren api_beenden(sidA)+reset_session(sidA)-Sequenz ist `_ended_session_snapshots` fuer sidA leer (Task 2c).

    b) **statischer Waechter finalisieren** (tests/test_no_live_global_state.py): _KILLED_MODULE_GLOBALS + _KILLED_STATE_KEYS um ALLE in Plan 03-06 getoeteten Globalen ergaenzen (Speaker-Familie, _bof_count-Modul, ewb_clicks/suggestion_offers, Familie-C falls nicht schon in Plan 05). **B3: `assert _PENDING_MIGRATION == frozenset()`** (bzw. leeres Set). Den AST-Sweep-Whitelist gegen die VOLLSTAENDIGE Global-Inventur (Plan 01) pruefen; _ended_session_snapshots ist in der Per-SID-Container-Whitelist (Plan 03). Modul-Level-Bindungs-Reichweite der 4 Live-Module final scharf (B3-Reichweite).

    **W4 (FIX-verdict-Guard-Robustheit):** jede UNKLAR-Globale mit Plan-01-Verdikt=FIX (aktive_phase_idx) ist NICHT im _KILLED-frozenset (sonst hasattr-Absenz-Test rot trotz korrekter per-sid-Variante); sie ist per per-sid-AST-Sweep abgedeckt. _bof_count=DELETE → ins _KILLED. **skript-Kandidaten (W-b): falls Plan-01-Verdikt=FIX → in Plan 03 Task 3d unter Welle-A-Tag ins _PENDING_MIGRATION eingetragen UND dort wieder gestrichen → hier NICHT mehr pending, per per-sid-AST-Sweep abgedeckt; falls DELETE → ins _KILLED. In KEINEM Fall bleibt skript in _PENDING_MIGRATION. Verdikt im SUMMARY.**

    c) **Beide ins deploy.sh-Gate** — verifizieren dass test_persid_concurrency.py + test_no_live_global_state.py im nerve_test-Gate mitlaufen (liegen in tests/, kein live/perf-Marker). Bestaetigen.

    d) **Reset-Ueberlebens-Check:** `anon_b_before = get_anonymisierer(sidB)` vor `reset_session(sidA)`, danach `assert get_anonymisierer(sidB) is anon_b_before` (SPEC Req 4).

    e) **Same-User-Zwei-Session-Behandlung (W3b — T-PERSID-08 schliessen):** der Concurrency-Test nutzt ZWEI VERSCHIEDENE User. Der `_beenden_sid`-Vertrag (Plan 03) loest bevorzugt via posted call_id (S3, exakt). WAEHLE EINE (im SUMMARY dokumentieren):
    - **Option A (Assertion):** dritter Test-Client als ZWEITE Session desselben Users A; api_beenden mit dessen posted call_id → assert die RICHTIGE (via call_id exakt aufgeloeste) sid wird beendet, nicht die andere Same-User-Session.
    - **Option B (Out-of-Scope-Grenze):** falls EA "eine Live-Session pro User" garantiert (grep-belegen), dokumentiere die Grenze im Docstring + SUMMARY.
    Da Plan 03 S3 (call_id-exakt) umsetzte, ist Option A die staerkere Wahl. Eine der beiden MUSS erfuellt sein.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <manual>Der Voll-Test laeuft im nerve_test-Gate GRUEN (D-10). Rot-vor-Fix-Beweis fuer diese Welle: dokumentierter manueller HEAD-Lauf VOR den Fixes: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && pytest tests/test_persid_concurrency.py -x'` gegen Pre-Fix-HEAD → rot (im SUMMARY), dann Fixes → gruen.</manual>
  </verify>
  <acceptance_criteria>
    - tests/test_persid_concurrency.py >=120 Zeilen, 7 gepaarte Assertions + Reset-Ueberleben (`is anon_b_before`) + **B1-Late-Write-Ghost-Drop-Assertion** + **B1-stop_live_session-vor-beenden-Non-Empty-Assertion (Haupt-Pfad :779)** + **N-1-Doppel-Feuer-Non-Empty-Assertion (:779 dann :815-setdefault)** + N-3-Snapshot-Pop-Assertion
    - **Beide Beenden-Naehte gedeckt: mind. eine Session via stop_live_session-vor-beenden (:779, NON-EMPTY), mind. eine via disconnect (:815), + der N-1-Doppel-Feuer-Fall**
    - Alle 5 Pflicht-Fallen (a-e) umgesetzt (anonymize Identity, NERVE_TESTING-ENV-Guard/S1, MERGE_WINDOW_S, beide-Beenden-Naehte+Doppel-Feuer, kein xfail)
    - 2 Tenants (User A Org 1 / User B Org 2), 2 SocketIO-Clients, cleanup_rows im Teardown
    - **Same-User-Zwei-Session-Pfad (W3b) geschlossen:** Option A (call_id-exakte Aufloesung, S3) ODER dokumentierte Grenze (Option B). Verdikt im SUMMARY.
    - **B3: `assert _PENDING_MIGRATION == frozenset()` — die Pending-Liste ist LEER (inkl. skript, W-b); test_no_live_global_state.py deckt ALLE getoeteten Globalen ab, gruen**
    - **W4:** FIX-Globale (aktive_phase_idx) via per-sid-AST-Sweep, nicht _KILLED; _bof_count in _KILLED (DELETE); skript je Verdikt (W-b) — nie in _PENDING_MIGRATION verblieben
    - Beide Tests laufen im deploy.sh-Gate, GRUEN committet
    - deploy.sh production GRUEN; Rot-vor-Fix-Beweis im SUMMARY (manueller HEAD-Lauf)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Founder-Doppel-Anruf-UAT (einmalig, NACH Fix — NIE das Gate)</name>
  <what-built>
    Der komplette Live-Pfad ist pro-sid. Der deterministische 2-Tenant-Concurrency-Test + statische Global-Waechter (mit leerer Pending-Liste, B3) laufen gruen im Deploy-Gate. Jetzt der einmalige echte Doppel-Anruf-Beweis auf getnerve.app (SPEC Acceptance letzte Zeile, CLAUDE.md Punkt 28 Verify=Production).
  </what-built>
  <how-to-verify>
    1. Zwei Browser (oder Browser + Inkognito), ZWEI verschiedene Konten (verschiedene Orgs), auf https://getnerve.app.
    2. Beide starten gleichzeitig einen Live-Anruf (Test-User-Accounts, is_test_user).
    3. Konto A spricht eindeutige Woerter ("Zebra Alpha Budget"), Konto B ("Qualle Beta Zeitdruck").
    4. Waehrend beide laufen: Konto A klickt einen EWB-Knopf, Konto B einen anderen; Konto A setzt Anrede 'du', Konto B 'sie'.
    5. **B1-Normal-Check:** Konto A legt normal auf (Beenden) → der A-Call-Record ist NICHT leer (belegt den stop_live_session-:779-Stash-Pfad — der Haupt-Fall).
    6. **B1-Netz-Blip-Check:** Konto B trennt kurz das Netz (DevTools offline) und legt DANN auf → der B-Call-Record ist NICHT leer (:815-Pfad).
    7. Konto A legt auf (Beenden) — Konto B MUSS ungestoert weiterlaufen (Transkript/Coaching brechen NICHT ab).
    8. Beide Call-Records pruefen (Dashboard): A enthaelt NUR Alpha-Inhalt, B NUR Beta. Keine Vermischung im Transkript, Painpoints, Einwaenden, Anrede.
  </how-to-verify>
  <resume-signal>Type "approved" wenn (a) keine Vermischung, (b) B lief ungestoert weiter, (c) A-Record bei normalem Auflegen nicht leer (B1 :779), (d) B-Record trotz Netz-Blip nicht leer (B1 :815). Sonst: welche Vermischung/welcher Abbruch/welcher leere Record — dann Gap-Closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual_ewb/keyword-Klick → per-sid-Bucket | Knopf-/Keyword-Ereignis muss unter dem sid liegen, der klickte (ALLE 6 Caller, B4) |
| Auflegen (reset) → fremde Session | reset_session darf NUR die eigene sid raeumen (+ deren Snapshot poppen, N-3) |
| STT-Speaker-Fallback → fremder Call | _log_last_sp global fiel von Call A auf B ueber (Cross-Tenant-Speaker-Leak) |
| Late-Write nach stash → tote sid | B1-Ghost-Drop: record_* belebt eine gestashte/tote sid nicht wieder |
| stop_live_session (:779) vor beenden → leerer Record | B1: der Normal-Hangup-Haupt-Pfad muss den State stashen; Doppel-Feuer (:815-setdefault) darf ihn nicht mit leer ueberschreiben (N-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PERSID-13 | Information Disclosure | ewb_clicks/suggestion_offers global → falscher tenant_id | mitigate | record_* pro sid; Reader per-sid (Req 2) |
| T-PERSID-14 | Denial of Service | reset_session(no-arg) reisst allen aktiven Sessions den Zustand weg | mitigate | reset_session(sid) raeumt nur eigene sid + poppt deren Snapshot (Req 4, N-3) |
| T-PERSID-15 | Information Disclosure | _log_last_sp global → Speaker-Fallback cross-tenant | mitigate | Speaker-Familie pro sid (Req 7, §6.7) |
| T-PERSID-16 | Tampering | committeter roter Test blockiert ALLE Deploys | mitigate | D-10: gruen-Commit erst jetzt; rot-vor-Fix = dokumentierter HEAD-Lauf |
| T-PERSID-23 | Information Disclosure | uebersehener record_*-Caller (matcher:338/claude:722/deepgram:1026/1041) → TypeError still geschluckt → Vorschlag verloren | mitigate | B4: ALLE 6 Caller sid-durchgereicht + grep-Vollstaendigkeits-Check |
| T-PERSID-08b | Spoofing | Same-User-Zwei-Session: falsche eigene Session beendet | mitigate | S3 call_id-exakt (Plan 03) + W3b Assertion/Grenze |
| T-PERSID-24 | Tampering | _PENDING_MIGRATION bleibt nicht-leer → legit-unmigrierte Schreiber dauerhaft durchgelassen | mitigate | B3: Plan 06 assertet _PENDING_MIGRATION == leer (inkl. skript, W-b) |
| T-PERSID-25 | Information Disclosure | Normal-Hangup (stop_live_session :779) vor beenden → leerer Call-Record im Haupt-Pfad; Doppel-Feuer ueberschreibt voll mit leer | mitigate | B1: :779 stasht (Plan 03), Leer-Skip/first-wins gegen :815-setdefault (N-1); Voll-Test assertet stop_live_session-vor-beenden + Doppel-Feuer Non-Empty |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `suggestion_reactions` (schreiben — Call-Ende-Flush aus suggestion_offers, tenant_id pro Call)
- `intent_event` / `ObjectionEvent` (schreiben — aus ewb_clicks)
- `conversation_logs`, `calls`, `transcript_segments` (Test-Assertions)

### inspect.sh-Beleg (Executor holt VOR Umbau)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema suggestion_reactions'
[Executor fuegt verbatim-Output ein — tenant_id-Spalte bestaetigen]
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Leser laeuft voraus? |
|---|---|---|---|---|
| ewb_clicks | record_ewb_click (live, 3 Caller) → api_beenden liest (via _bs, Snapshot-faehig) | RAM per-sid → ObjectionEvent/DB | live (Knopf-Klick) | Nein — api_beenden Endschreibung; Snapshot (B1, beide Naehte, Leer-Skip N-1) |
| suggestion_offers | record_suggestion_offer (live, 3 Caller) → api_beenden Flush (via _bs) | RAM per-sid → suggestion_reactions | live | Nein |
| _log_last_sp/_second_sp_seen | deepgram schreibt+liest | RAM per-sid | live (on_message) | Nein |
| _ended_session_snapshots[sid] | stash (Plan 03) → api_beenden Peek → reset_session poppt final (N-3) | RAM (TTL + Pop) | Beenden-Naht + reset | Nein |
| suggestion_reactions.tenant_id | api_beenden schreibt aus per-sid-Bucket | DB-Spalte | http (Call-Ende) | n/a |

### Bei Diskrepanz: STOP + Replan

<verification>
- deploy.sh production nerve_test-Gate gruen
- Voller Concurrency-Test GRUEN committet (7 gepaarte Assertions + Reset-Ueberleben + Same-User-Zwei-Session + B1-Ghost-Drop + B1-stop_live_session-vor-beenden-Non-Empty + N-1-Doppel-Feuer-Non-Empty + N-3-Snapshot-Pop)
- statischer Waechter deckt vollstaendige Global-Inventur ab, _PENDING_MIGRATION leer (B3, inkl. skript W-b), gruen
- test_suggestion_reactions.py auf neue Signatur (W-2) — kein Gate-Rot
- Founder-Doppel-Anruf-UAT bestaetigt Nicht-Vermischung + ungestoertes Weiterlaufen + nicht-leerer Record bei normalem Auflegen (B1 :779) UND trotz Netz-Blip (B1 :815)
</verification>

<success_criteria>
- record_* pro sid; ALLE 6 Caller sid-durchgereicht (B4); suggestion_reactions richtiger tenant_id; test_suggestion_reactions.py migriert (W-2)
- reset_session(sid) raeumt nur eigene sid + poppt deren Snapshot (Reset-Ueberlebens-Check + N-3-Pop gruen)
- Speaker-Familie pro sid; _bof_count-Modul-Global weg
- Voller Concurrency-Test + statischer Waechter GRUEN committet; _PENDING_MIGRATION leer (B3, D-10, inkl. skript W-b)
- Same-User-Zwei-Session-Pfad geschlossen (S3/W3b); B1-Late-Write-Ghost-Drop + B1-stop_live_session-vor-beenden-Non-Empty + N-1-Doppel-Feuer-Non-Empty belegt (beide Beenden-Naehte + Doppel-Feuer)
- Founder-UAT bestaetigt das Fundament
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PERSID-live-pfad-pro-session-umstellen/08.23.2.PERSID-06-SUMMARY.md`
</output>
</content>
</output>
