# Phase 08.23.2.PERSID — Global-Inventur + Verbindungs-Karte (Punkt-22-Artefakt)

**Researched:** 2026-07-03
**Domain:** Live-Anruf-Pfad per-Session-Migration (4 Module), F2-Stilllegung, 3 Wächter, 2 Deploy-Gate-Tests
**Confidence:** HIGH (jeder `file:line`-Claim grep/Read-verifiziert gegen HEAD am 2026-07-03; SPEC-Anker teils STALE — Drift in §6)
**Method:** Pure static analysis (grep + Read + AST-Reasoning). KEIN app-run, KEIN pytest, KEIN Server (CLAUDE.md „Kein Local-Dev"). Multi-Segment-Gotcha: Pfade hartkodiert, gsd-tools NICHT aufgerufen.

> Dies IST die Punkt-22-Verbindungs-Karte, physisch in die Datei geschrieben (kein Prosa-Digest).
> SPEC + CONTEXT sind gelockt — hier NICHT re-litigiert, sondern das fehlende Pre-Plan-Artefakt gebaut.

---

## User Constraints (aus CONTEXT.md — verbatim-relevante Locks)

### Locked Decisions (D-01…D-10)
- **D-01:** Raw-Dict-Zugriff `_session_state[sid][key]` unter `_session_state_lock`. KEIN Accessor-Layer, KEIN State-Objekt (Punkt-17-Verstoß).
- **D-02:** `ohne sid → None`-No-Op-Muster (kanonisch: `cost_tracker._resolve_user_id_from_live_session`).
- **D-03:** Lock-Disziplin manuell; Snapshot unter Lock, Lock vor langen Ops (LLM/Netz/DB) freigeben.
- **D-04:** Statischer Wächter STRENG — nur Live-Pfad-Globale, rot bei jedem nicht-per-sid mutable Live-State.
- **D-05:** Erlaubte Überlebende = enge dokumentierte Whitelist (Konstanten, Locks, Modell-Caches, tenant-neutrale Caches). Default: migrieren-oder-löschen.
- **D-06:** Gerüst test-first, gestaffelt — Welle 0 Fundament.
- **D-07 (HART):** Schnitt nach **DATEN-FAMILIE**, nicht Modul.
- **D-08 (EISERN):** Leser einer Familie NIE vor seinen Schreibern; Schreiber+Leser derselben Familie im **selben Deploy**.
- **D-09:** Welle 0 = pro Global Fix-vs-Löschen mit grep-Beleg. Viel toter write-only-Code → löschen.
- **D-10 (OPERATIV KRITISCH):** Rot-Beweis MANUELL gegen HEAD dokumentieren; KEIN committeter roter Test; Voll-Test erst mit letzter Welle grün committen.

### Deferred / OUT OF SCOPE
`schema_version` an intent_event (→ TAXO3-b), Gedächtnis-Feature, F3/F4/F5, RLS-Härtung, DB-Feld-Löschung, Tenant/Auth/Billing, Emit-Schicht `sio.emit(room=sid)`, Accessor-Layer, periodischer Heiler-Sweep, Refactor huckepack.

---

## Summary

Der Live-Pfad ist **halb migriert**: `_session_state[sid]` (`live_session.py:210`) ist voll geseedet (inkl. `conversation_log`, `covered_phases`, `phasen_log`, `session_meta`, `state`-Sub-Dict), ABER die Services schreiben weiter in die **Modul-Globalen** und `/api/beenden` liest sie global.

**Zwei Ergebnisse aus der grep-Inventur, die den Plan formen:**

1. **Der Delete-Anteil ist GRÖSSER als die SPEC annimmt.** Mehrere Globale, die SPEC Req 5 + Req 7 als „per-SID migrieren" listet, haben **0 aktive Produktiv-Reader** — sie sind write-only Zombies, weil die Ergebnis-**Auslieferung** komplett auf `sio.emit(room=sid)` umgestellt wurde und die alten Polling-Routes (`/api/ergebnis`, `/api/status`) **nicht mehr existieren** (grep: 0 Treffer in `routes/`). Betroffen: `state['ergebnis'/'aktiv'/'version'/'active_hint'/'ewb_buttons']`, `session_meta`, `last_postcall`, `hilfe_log`, `quick_action_log`, `roles_swapped`. → **DELETE statt Fix** (D-09).

2. **Die echte Migrations-Last** ist die **api_beenden-Lese-Familie** (`conversation_log`/`painpoints`/`kaufbereitschaft(_verlauf)`/`gegenargument_log`/`phasen_log`/`covered_phases`/`aktive_phase_idx`), gefüttert von `claude_service` + `deepgram_service`, plus die **raw-transcript-Familie** (`_merge_pending`, `deepgram_service.py:196` → `_flush_segment` `live_session.py:794`), plus **config** (`session_anrede`/`skript`) und **ewb/suggestion** (`ewb_clicks`/`suggestion_offers`).

**Primary recommendation:** Welle 0 löscht die 6 Zombie-Familien-Globalen (grep-belegt 0 Reader) und baut den statischen Wächter + Concurrency-Test-Gerüst. Danach **5 Fix-Wellen** nach Datenfamilie (config → raw-transcript → conversation/result → ewb/suggestion → reset+speaker/rest), jede mit Schreiber+Leser im selben Deploy (D-08). F2-Stilllegung + 3 Wächter sind quer und können in die passenden Wellen eingehängt werden.

---

## Architectural Responsibility Map

| Capability | Primary Tier | Rationale |
|------------|-------------|-----------|
| Roh-Transkript-Merge/Flush | Deepgram-Callback-Thread (`deepgram_service`) | STT-Callback schreibt; muss per-sid, nicht per-Speaker-String |
| Analyse-Ergebnis/Result-State | analyse_loop-Daemon (`claude_service`) | Schreibt Ergebnis; Auslieferung schon per-sid via emit |
| Call-Record-Persistenz | HTTP-Route `/api/beenden` (`app_routes`) | Liest RAM-State, schreibt ConversationLog/DB |
| F2 Event-Benotung | Slow-Lane-Consumer (`slow_lane`) | Terminal-Status-Schreiber; muss deadlock-frei drainen |
| Session-Lifecycle | SocketIO-Handler (`deepgram_service`) | init/pop_session_state, reset_session-Caller |

---

## 1. Vollständige Global-Inventur (Welle 0 Fundament)

Definitionen alle in `services/live_session.py`. Schreib-Timing-Legende (Punkt 26): **live** = Deepgram-Callback / analyse_loop synchron; **http** = /api/beenden-Request; **reset** = reset_session.
Reader-Zählung: nur **aktive Produktiv-Pfade** (Tests/Definitionen/reset separat, Punkt 22).

| Global (`ls.<name>`) | Def `file:line` | Writers (grep-belegt) | Readers (aktiver Prod-Pfad) | Daten-Familie | Write-Timing | Fix-vs-Delete (D-09) |
|---|---|---|---|---|---|---|
| `_merge_pending` | `live_session.py:91` | `deepgram_service.py:196-214` (on_message, key=`str(speaker)`) | `_flush_segment` `live_session.py:797` (pop by key) | **raw-transcript** | live (STT-Thread) | **FIX** → `_session_state[sid]['_merge_pending']`. Schwerster Fund (SPEC Req 1). Key muss sid+speaker werden. |
| `transcript_buffer` | `live_session.py:46` | `_flush_segment` `live_session.py:837` (nur FALLBACK-Branch, „should not occur post-migration") | analyse_loop? (grep: nur reset+def) | raw-transcript | live-fallback | **DELETE** — per-SID `_per_sid_transcript` (`:216`) ist der echte Puffer; der Fallback ist toter Post-Migration-Zweig. |
| `analysiert_bisher` | `live_session.py:47` | — (per-SID migriert, `live_session.py:420`) | — | raw-transcript | — | **SCHON per-SID** (Vorbild). Modul-Global `:47` ist Rest-Leiche → DELETE die Globale + Fallback. |
| `conversation_log` | `live_session.py:152` | `deepgram_service.py:132,955`; `claude_service.py:1077,1769,1804,1810` (`.append` unter `log_lock`) | `app_routes.py:144` (`list()`), `_build_log_content` `live_session.py:1052` | **conversation/result** | live | **FIX** → `_session_state[sid]['conversation_log']` (schon geseedet `:315`). Vielfach-Schreiber! |
| `painpoints` | `live_session.py:55` | `claude_service.py:1791` (`.append`) | `app_routes.py:146`; `_build_log_content:1121`; `claude_service.py:1775` (Duplikat-Check) | conversation/result | live | **FIX** → `_session_state[sid]['painpoints']` (geseedet `:418`). |
| `gegenargument_log` | `live_session.py:59` | `claude_service.py:1123` (`.append`), `:1110` (mutate last) | `app_routes.py:287`; `_build_log_content:1159` | conversation/result | live | **FIX** → per-SID (geseedet `:415`). |
| `phasen_log` | `live_session.py:71` | `claude_service.py:1214` (`.append`) | `app_routes.py:294`; `_build_log_content:1210` | conversation/result | live | **FIX** → per-SID (geseedet `:414`). |
| `covered_phases` | `live_session.py:195` | `claude_service.py:1233` (`.add`) | `app_routes.py:242`; | conversation/result | live | **FIX** → per-SID (geseedet `:319`). |
| `aktive_phase_idx` | `live_session.py:183` | — (grep: NUR read + reset; kein Live-Writer gefunden) | `app_routes.py:244`; `claude_service.py:206` | conversation/result | reset-only | **DELETE-Kandidat** (D-09 genannt): 0 Live-Writer → immer 0. `app_routes:244` liest es in `cp_snapshot` → migrieren-oder-0. Bei Migration: per-SID seed `:422` existiert. Verdikt: **FIX-als-per-SID** (Reader lebt) ODER prüfen ob Writer verloren ging (siehe §6 Landmine). |
| `kaufbereitschaft` | `live_session.py:178` | `claude_service.py:1362` (`= score_p4`); `update_kaufbereitschaft:858` | `app_routes.py:149`; `claude_service.py:1072,1083,1368,1737` | **conversation/result (KB)** | live | **FIX** → per-SID (geseedet `:310/320`). |
| `kaufbereitschaft_verlauf` | `live_session.py:179` | `update_kaufbereitschaft:860` (`.append`) | `app_routes.py:148` | conversation/result (KB) | live | **FIX** → per-SID (geseedet `:320`). |
| `state['ergebnis']` | `live_session.py:119` | `claude_service.py:1137,1370` | **0 Prod-Reader** (grep `.get('ergebnis')`=0; `/api/ergebnis` weg) | result | live | **DELETE** (ZOMBIE). SPEC Req 5 drift — s. §6. |
| `state['aktiv']` | `live_session.py` | `claude_service.py:955,1138,1371` | **0 Prod-Reader** | result | live | **DELETE** (ZOMBIE). |
| `state['version']` | `live_session.py:116` | `claude_service.py:1139,1372` (`+= 1`) | **0 Prod-Reader** (`/api/status` weg) | result | live | **DELETE** (ZOMBIE). |
| `state['active_hint']` | `live_session.py:134` | `claude_service.py:1360` | **0 Prod-Reader** (Auslieferung via emit) | result | live | **DELETE** (ZOMBIE). SPEC Req 5 drift. |
| `state['ewb_buttons']` | `live_session.py:136` | `claude_service.py:1361` | **0 Prod-Reader** | result | live | **DELETE** (ZOMBIE). SPEC Req 5 drift. |
| `state['line_id']` | `live_session.py` | (per-SID migriert, claude_service.py:1141 Kommentar) | Keyword-Matcher per-SID | result | — | **SCHON per-SID** → DELETE Globale. |
| `state['ewb_clicks']` | `live_session.py:121` | `record_ewb_click:995` (`state.setdefault`) | `app_routes.py:324` (`list()` → ObjectionEvent + einwaende_gesamt) | **ewb/suggestion** | live (Knopf) | **FIX** → per-SID (`_session_state[sid]['state']['ewb_clicks']` geseedet `:336`). SPEC Req 2. |
| `state['suggestion_offers']` | `live_session.py:122` | `record_suggestion_offer:1023` (`state.setdefault`) | `app_routes.py:327` (`list()` → suggestion_reactions-Flush) | ewb/suggestion | live (Knopf) | **FIX** → per-SID. Punkt-29-Falle live: `:1001` „EXAKT das Muster von record_ewb_click". SPEC Req 2. |
| `state['precall_briefing']` | `live_session.py:141` | (Set-Pfad via `set_briefing_for_sid` schon per-SID `:262`) | `app_routes.py:112` (`state.get('precall_briefing')`) | config | http-fallback | **FIX/DELETE** — Reader `app_routes:112` liest Modul-`state`, aber der echte Wert liegt per-SID in `['_briefing']`. Reader auf per-SID umbiegen, Modul-Key löschen. |
| `state['session_anrede']` | (kein Modul-Def; per-SID `:356`) | `deepgram_service.py:619` (`ls.state['session_anrede']=`) | `app_routes.py:340` (`ls.state.get('session_anrede')`) | **config** | live (start) | **FIX** → per-SID (`_session_state[sid]['session_anrede']`, schon per-SID gelesen in `deepgram_service.py:511`). Split-Brain: SPEC Req 6. |
| `state['aktives_skript_inhalt']` | (kein Modul-Def) | `deepgram_service.py:641` | grep: kein aktiver Reader gefunden | config | live (start) | **DELETE-Kandidat** (write-only?) — s. §6 Landmine (SPEC Req 6 sagt „per-SID gelesen" → Reader belegen oder DELETE). |
| `state['skript_bloecke']` | (kein Modul-Def) | `deepgram_service.py:642` | grep: kein aktiver Reader gefunden | config | live (start) | **DELETE-Kandidat** — s. §6. |
| `state['mic_muted']` | `live_session.py:144` | `deepgram_service.py:856` (`handle_mute_mic`) | `deepgram_service.py:233` (`state.get('mic_muted')`) | speaker/config | live | **FIX** → per-SID. Beide in deepgram_service; einfacher Umzug. |
| `state['slot1_variant_busy_until']` | `live_session.py:143` | (PIP-01 entfernt — Kommentar `deepgram_service.py:264`) | grep: 0 aktive Reader | result | — | **DELETE** (ZOMBIE, PIP-01 stillgelegt). |
| `state['readiness_score'/'readiness_bucket'/'score_factors_seen']` | `live_session.py:129-132` | `claude_service` readiness-loop | `score_factors_seen` schon per-SID (`claude_service.py:1355`) | result | live | **FIX-oder-DELETE** — reset-only in `state` (`:916-918`); prüfen ob Live-Writer auf Modul-`state` bleibt. |
| `session_meta` | `live_session.py:75` | **0 Writer** (grep: nur def `:75`, reset `:968`) | **0 Reader** | result | reset-only | **DELETE** (ZOMBIE, D-09 genannt). Per-SID `:400` seed auch tot. |
| `last_postcall` | `live_session.py:749` | `app_routes.py:641` (`= {...}`) | **0 Reader** (grep 0) | result | http | **DELETE** (ZOMBIE, D-09 genannt). |
| `hilfe_log` | `live_session.py:63` | **0 `.append`** (grep 0) | `app_routes.py:296` (`len()`=immer 0); `_build_log_content:1191` | conversation/result | never | **DELETE** (ZOMBIE, D-09 genannt). Reader `len()` durch Literal 0 ersetzen. |
| `quick_action_log` | `live_session.py:67` | **0 `.append`** (grep 0) | `app_routes.py:298` (`len()`=0); `_build_log_content:1193` | conversation/result | never | **DELETE** (ZOMBIE, D-09 genannt). |
| `roles_swapped` | `live_session.py:156` | **0 `= True`** (grep 0; nur `= False`) | `_build_log_content:1054` (swap-map); geseedet per-SID `:318` | conversation/result | const-False | **DELETE** (effektiv Konstante False; Swap-Logik `:1056` tot). D-09 genannt. |
| `is_paused` (Modul) | `live_session.py:42` | grep: nur def+`get_sid_paused` liest per-SID `:110` | per-SID `state['is_paused']` `:354` ist die Quelle | speaker/config | — | **DELETE** Modul-Globale (per-SID schon Quelle, `get_sid_paused` liest `:110`). |
| `_confirmed_speaker/_pending_speaker/_pending_since` | `live_session.py:168-170` | (per-SID `stabilize_speaker:752` schon Quelle) | per-SID | **speaker** | — | **DELETE** Modul-Globale (per-SID migriert). |
| `_log_last_sp` | `live_session.py:160` | `deepgram_service.py:77` (`ls._log_last_sp=`) | `deepgram_service.py:78` | speaker | live | **FIX** → per-SID (`_log_last_sp` geseedet `:325`). Cross-tenant-Speaker-Leak! |
| `_second_sp_seen` | `live_session.py:164` | `deepgram_service.py:71` (`ls._second_sp_seen=True`) | `deepgram_service.py:72` | speaker | live | **FIX** → per-SID (geseedet `:421`). |
| `_bof_count` | `live_session.py:174` | grep: nur def+reset | grep: 0 aktive Reader | speaker | reset-only | **DELETE-Kandidat** — s. §6 (per-SID seed `:421` existiert, Modul-Global tot?). |

**Allowed-Survivor-Whitelist (D-05 — der statische Wächter MUSS diese durchlassen):**
- Locks: `pause_lock`, `buffer_lock`, `coaching_lock`, `painpoints_lock`, `log_lock`, `state_lock`, `kb_lock`, `_merge_lock`, `_session_state_lock`, `_per_sid_lock`, `_per_sid_transcript_lock`, `_per_sid_coaching_lock`, `keyword_matchers_lock`, alle `*_lock`.
- Per-SID-Container SELBST (nicht Live-State, sondern die sid-gekeyten Dicts): `_session_state`, `_per_sid_profile`, `_per_sid_transcript`, `_per_sid_coaching_buffer`, `keyword_matchers`, `_deepgram_sessions`, `_cost_opened_at`, `_stt_seconds_accumulated` (alle `{sid: ...}`-gekeyt = per-sid-safe).
- Konstanten: `LOG_DIR`, `_DU_FORMS`, `_SALES_KEYTERMS_BASE`, `MAX_KEYTERMS`, `_STOPWORDS`, `_sp_map`, alle `_AUDIO_WARN_*`, `_ROLLING_WINDOW_MS`, `_OUTCOME_MODIFIERS`.
- Trigger-Events (prozess-global, kein pro-User-Daten): `analyse_trigger`, `coaching_trigger` (threading.Event — Signal, kein Zustand).
- Modell-Caches (spaCy/GLiNER) — in `services/anonymization.py`, tenant-neutral, dokumentiert.

---

## 2. Verbindungs-Karte nach Datenfamilie (D-07/D-08 Wellen-Schnitt-Vorschlag)

> Nur **FIX**-Globale (Delete-Globale gehen in Welle 0). Jede Familie: Schreiber + Leser müssen im SELBEN Deploy live (D-08). Reihenfolge = Vorschlag; **Planner finalisiert**.

### Familie A — CONFIG (`session_anrede`, `mic_muted`) — leichteste, früh
- **Schreiber:** `deepgram_service.py:619` (session_anrede), `:856` (mic_muted).
- **Leser:** `app_routes.py:340` (session_anrede → ConversationLog.anrede); `deepgram_service.py:233` (mic_muted).
- **D-08-Beweis:** session_anrede: Schreiber deepgram_service, Leser app_routes → **beide im selben Deploy**. mic_muted: beide in deepgram_service (trivial).
- **Anmerkung:** `aktives_skript_inhalt`/`skript_bloecke` = Delete-Kandidaten (§6), NICHT in dieser Familie migrieren bis Reader belegt.

### Familie B — RAW-TRANSCRIPT (`_merge_pending`) — SPEC „schwerster Fund", früh
- **Schreiber:** `deepgram_service.py:196-214` (Bucket key=`str(speaker)`).
- **Leser:** `_flush_segment` `live_session.py:797` (`pop(key)`) → schreibt weiter in `_per_sid_transcript`/`_per_sid_coaching_buffer` (schon per-sid).
- **D-08-Beweis:** Schreiber+Leser sind eng gekoppelt (Timer-Callback), beide müssen den neuen sid-gekeyten Key gleichzeitig kennen → **ein Deploy**. `transcript_buffer`-Fallback (`:837`) + `analysiert_bisher`-Globale in dieselbe Welle löschen.
- **Kern-Mechanik:** Key `str(speaker)` → `(sid, speaker)` oder `_session_state[sid]['_merge_pending'][speaker]`. Cross-tenant-PII-Beweis (SPEC Req 1 Acceptance).

### Familie C — CONVERSATION/RESULT (die grosse Familie) — Kern-Deploy
- **Schreiber:** `deepgram_service.py:132,955` + `claude_service.py:1077,1769,1804,1810` (conversation_log); `claude_service.py:1791` (painpoints); `:1123,1110` (gegenargument_log); `:1214` (phasen_log); `:1233` (covered_phases); `:1362`+`update_kaufbereitschaft` (kaufbereitschaft/_verlauf).
- **Leser:** `app_routes.py:144,146,148,149,242,244,287,294` + `_build_log_content` (TXT-Datei) + `claude_service.py:1072,1083,1368,1737` (kb-Feedback).
- **D-08-Beweis (KRITISCH):** `/api/beenden` (`app_routes`) liest ALLE diese; Schreiber in `claude_service`+`deepgram_service`. Ein per-sid-Reader in api_beenden ohne per-sid-Schreiber in claude_service ⇒ **leerer Call-Record auf Prod**. → conversation_log + painpoints + gegenargument_log + phasen_log + covered_phases + kaufbereitschaft(_verlauf) **ALLE im selben Deploy** umstellen. Das ist die grösste Einzelwelle.
- **Sub-Cut-Option (Planner):** Falls zu gross, nach Reader-Kopplung teilbar — aber `_build_log_content` liest conversation_log+painpoints+gegenargument_log+phasen_log GEMEINSAM in EINEM Aufruf (`live_session.py:1052-1210`) → diese vier sind hart gekoppelt, NICHT trennbar. KB kann separat (nur app_routes-Reader).
- **Rider:** `aktive_phase_idx` (Reader `app_routes:244` + `claude_service:206`) mitnehmen; `roles_swapped` (Reader `_build_log_content:1054`) in dieselbe Welle löschen (const-False).

### Familie D — EWB/SUGGESTION (`ewb_clicks`, `suggestion_offers`) — Knopf-Pfad
- **Schreiber:** `record_ewb_click` `live_session.py:995`; `record_suggestion_offer` `:1023` (beide `state.setdefault`, aufgerufen aus `deepgram_service.py:1006,1017` im manual_ewb-`_run`).
- **Leser:** `app_routes.py:324` (ewb_clicks → ObjectionEvent + einwaende_gesamt); `:327` (suggestion_offers → suggestion_reactions-Flush).
- **D-08-Beweis:** Schreiber (record_* aus deepgram manual_ewb) + Leser (app_routes beenden) → **beide im selben Deploy**. `record_*(sid)`-Signatur muss `sid` durchreichen (D-02: ohne sid → No-Op).
- **Acceptance (SPEC Req 2):** suggestion_reactions je Call richtiger tenant_id; A-Ereignisse nicht bei B.

### Familie E — SPEAKER + RESET-Fix (`_log_last_sp`, `_second_sp_seen`) + reset_session(sid)
- **Schreiber/Leser:** `_log_last_sp` `deepgram_service.py:77/78`; `_second_sp_seen` `:71/72` (beide on_message).
- **reset_session-Fix (SPEC Req 4):** aktuell no-arg, iteriert ALLE SIDs (`live_session.py:864-887`). Ein Caller: `app_routes.py:834` (im `/api/beenden`, NICHT `:480` wie SPEC/CONTEXT sagt — s. §6). → `reset_session(sid)`: nur eigene sid poppen+neu initen.
- **D-08:** Speaker-Globale (deepgram intern) + reset (app_routes) — koppeln über pop_session_state. **Letzte Welle**, da reset_session erst umstellbar, wenn alle Familien per-sid sind.

**Vorgeschlagene Wellen-Reihenfolge (Planner finalisiert):**
Welle 0 (Fundament: Deletes + Wächter + Test-Gerüst) → A (config) → B (raw-transcript) → C (conversation/result, gross) → D (ewb/suggestion) → E (speaker + reset(sid) + Rest-Cleanup). **~5 Fix-Wellen + Welle 0.** F2 + Wächter (a/b/c) quer einhängen (F2 in eine frühe Welle, da unabhängig vom per-sid-Umzug; Wächter a in Familie D-Nähe, Wächter b/c in beliebige).

---

## 3. F2-Stilllegung Verifikation (Req 8)

Alle Anker in `services/slow_lane.py` verifiziert:

- **`_persist_event_ref`**: real bei ~`:225-284` (SPEC sagte `:230-284` — ** VALID**, Funktion beginnt oberhalb, Idempotenz-Skip bei `:230`).
- **Idempotenz-Skip:** `slow_lane.py:230` `if ev.handling_status != 'pending': return` — **VERIFIZIERT**.
- **`'not_gradable'` existiert:** `slow_lane.py:364` `_STATUS_NOT_GRADABLE = 'not_gradable'` — **VERIFIZIERT**.
- **`_pending_events` zählt nur `=='pending'`:** `slow_lane.py:369-376` `.filter(... handling_status == 'pending').count()` — **VERIFIZIERT** (SPEC sagte `~592/842` — STALE, echte Zeile `:369`). In `intent_event_writer.py:139` Kommentar bestätigt server_default='pending'.
- **`compute_rubric` toleriert leere handling-Menge:** in `services/rubric_engine.py:68` (NICHT `database/rubric_dimensions.py:393-402` — **SPEC-DRIFT §6**). `_sample_size_for` (`:198-218`) gibt 0 bei 0 konfidenten Events; `if not measurable`/`measured_weight <= 0` (`:118/:188`) → Status `insufficient_data`, **kein Div-durch-Null**. VERIFIZIERT.
- **`abstain_log`-Befüllung nicht gefordert:** `tests/test_abstain_log_rls.py` **existiert**; grep zeigt Tests prüfen RLS-Isolation, NICHT „abstain_log muss befüllt sein". Zusätzlich: F2-Stilllegung setzt VOR dem `grade_handling`-Pfad terminal → erreicht den AbstainLog-INSERT (`:278`) nie. Kein Gate bricht.
- **Deadlock-frei:** neue Events → sofort `'not_gradable'` → `_pending_events` zählt sie NICHT → drainet auf 0 → Call-Ende-Merge (`slow_lane.py:369`, F-02) feuert. VERIFIZIERT.

**Exakter Insertion-Point:** `slow_lane.py`, **direkt nach `:231`** (`return` des Idempotenz-Skips), **VOR `:237`** (Confidence-Gate `_confidence_gate_for`):
```python
# nach if ev.handling_status != 'pending': return   (:230-231)
# F2-Stilllegung (PERSID Req 8): Consumer schreibt keine Leerlauf-Noten mehr.
ev.handling_status = 'not_gradable'   # existierender Terminal-Status (:364)
return                                 # VOR _confidence_gate_for/_find_next_advisor_utterance/grade_handling
```
NULL Schema-Change, umgeht auch den RLS-fail-closed-Loop (`:262-276`), DB-Felder bleiben.

---

## 4. Wächter-Ziele Verifikation (Req 9/10/11)

### (a) `build_intent_payload()` — Auslagerung + Whitelist-Test
- **Extraktions-Quelle:** `intent_event_writer.py:100-118` (SPEC sagte `:100-115` — real endet der Payload-Dict-Literal bei `:115`, `extra_payload`-Merge `:116-118`). Der auszulagernde Block ist **`:100-118`** (inkl. extra_payload-Merge).
- **Pflichtfeld-Set (aktueller Payload, `:100-115`):** `source`, `inference_basis`, `taxonomy_version` (Pflicht non-null via `TAXONOMY_VERSION` `:103`), `abstained` (bool), `speaker_role`, `speaker_id`, `is_simulation` (bool), `origin_type`, `triggering_text`. → **Whitelist-Test = genau diese 9 Keys** (+ `extra_payload`-Keys müssen bewusst aufgenommen werden, sonst rot).
- **Kein `schema_version`:** `models.py:795` `intent_event.payload_jsonb` (JSON, nullable=False, server_default `{}`) — **KEINE `schema_version`-Spalte** (grep bestätigt: nur `score_schema_version` an calls `:725`, das ist eine andere Tabelle). VERIFIZIERT. `taxonomy_version` (im payload) ≠ `schema_version` (nicht vergeben).

### (b) TTFT am echten Knopf-Pfad
- **`streame_manual_ewb_variante` (`claude_service.py:740-865`) hat KEINE TTFT** — VERIFIZIERT: der Stream-Loop `:803-805` misst nur `full_text`, KEIN `monotonic()`-Delta beim ersten Token. Cost-Hook `:824-848` misst nur Tokens.
- **Muster zum Kopieren (aus dem toten `streame_auto_variante` `:572`):** `:631` `_first_token_autovar = True` → `:641-643` `if _first_token_autovar: _ttft_ms = (monotonic() - _t_stream_start)*1000; _first_token_autovar = False` → `:654` `print("[EWB-TTFT] ...")`. Dieses Idiom in den `for token in stream.text_stream`-Loop bei `:803` einsetzen (`_t_stream_start` vor dem `while attempts`-Loop nehmen).
- **Latenz-neutral (Punkt 25):** nur ein monotonic-Delta + print, keine Blockierung.

### (c) Heiler für hängende resolved-Flags
- **Zwei resolved-Flags:** `calls.audio_health_resolved` (`models.py:703`) + `calls.transcript_resolved` (`models.py:713`).
- **api_beenden-Setzstelle:** `app_routes.py:725` (`_call_row.audio_health_resolved = True`, nur wenn kein wc-Buffer) + `:726` (`_call_row.transcript_resolved = True`) — beide innerhalb des `if _phase_d_call_id:`-Blocks (`:690`), im `try` bei `:694`.
- **Failure-Site:** wenn der `calls`-UPDATE (`:727 commit`) fehlschlägt (`except :728 → rollback :730`), bleiben BEIDE resolved-Flags in ihrem DB-Default (False) → Call hängt „wird ausgewertet". Der Audio-BG-Thread setzt `audio_health_resolved` im `finally` (`:810`), aber `transcript_resolved` hat KEINEN zweiten Setzer → wird der commit `:727` nie erreicht, hängt der Call.
- **SPEC-Default (CONTEXT):** inline-terminal an der Fehlerstelle (`except :728`) — im rollback-Zweig die Flags einzeln idempotent auf True setzen (eigene kurze Session, wie der `_audio_health_bg`-finally `:802-816`). KEIN neuer Zeit-Sweep. Optional Startup-Sweep für Alt-Rows.

---

## 5. Test-Netz Bau-Fakten (Req 12)

### Mock-Nähte (aus `test_medium_lane_intent_event_live.py` + Bau-Vorgabe):
- **`analysiere_mit_claude`/Haiku:** `monkeypatch.setattr(cs, 'analysiere_mit_claude', _fake)` (`test_medium_lane:142`); Client-Ersatz-Muster `:96-100` (`_FakeClient` mit `.messages.create`). Für Knopf-Pfad zusätzlich `claude_client.messages.stream` mocken.
- **`_open_deepgram_connection`:** patchen, damit kein echtes Deepgram-WS öffnet (der Concurrency-Test füttert Transkript direkt in `_per_sid_transcript`/`on_message`).
- **`anonymize` als IDENTITY:** PFLICHT (`monkeypatch.setattr('services.anonymization.anonymize', lambda t, c: (t, 'identity'))`) — sonst Vacuous-Green (SPEC-Falle a). Analog `anonymize_output`/`anonymize_for_storage`.
- **`_qa_pipeline_dispatch`:** `monkeypatch.setattr(cs, '_qa_pipeline_dispatch', lambda *a,**k: None)` (`:144`).
- **`_seed_call(db)`** (`:60-70`): echte `calls`-Row als FK-Ziel für `intent_event.call_id` (NOT NULL).

### Daemon-Loop-Kontrolle:
- **`_OneShotTrigger`** (`:39-53`): ersetzt `ls.analyse_trigger` → genau EIN Tick, dann `_StopLoop`. `monkeypatch.setattr(ls, 'analyse_trigger', _OneShotTrigger())` (`:166`).
- **Daemon-TESTING-Guard (app.py:2214-2215):** app.py hat **KEINEN** TESTING/pytest-Guard (grep: 0 Treffer) — **MUSS in dieser Phase ADDIERT werden** (Bau-Vorgabe Gotcha a). Sonst weckt der Test-Client-Import bei `import app` die echten `analyse_loop`/`coaching_loop`-Daemons → Flakes. Guard: `if not app.config.get('TESTING'):` um `:2214-2215`.

### SocketIO Zwei-Client-In-Prozess:
- Muster: `socketio.test_client(app)` × 2, je eigene Flask-Session (User A Org 1 / User B Org 2). Emit `start_live_session` je Client, dann Transkript-Segmente einspeisen, dann `/api/beenden` je Client.
- **`MERGE_WINDOW_S` klein patchen:** `deepgram_service.py:211` `threading.Timer(MERGE_WINDOW_S, ...)` → `monkeypatch.setattr('services.deepgram_service.MERGE_WINDOW_S', 0.05)` (Bau-Vorgabe Gotcha b; Import auch `config.py`).
- **`/api/beenden` VOR disconnect** (Bau-Vorgabe Gotcha c, `app_routes.py:165-171` = frontend-treue Reihenfolge; sonst poppt der disconnect-Handler `_session_state[sid]` vor dem Persist → `{0,0,0}`-Stats). Der Test muss beenden aufrufen SOLANGE die Session noch in `_session_state` liegt.

### Statischer Wächter (`test_no_live_global_state.py`, Muster `test_schild_guard.py`):
- `test_schild_guard.py` liest live (pg_description); der neue Wächter ist AST/grep-Sweep über `services/`+`routes/` auf `ls.<GLOBAL> = ...` / `ls.state[...] = ...` im Live-Pfad, mit der §1-Whitelist. Plus `assert not hasattr(ls, '<global>')` für ALLE gelöschten Globalen.
- **KEIN Source-Presence-False-Green** (CLAUDE.md): der `hasattr`-Teil ist erlaubt (prüft Runtime-Attribut-Absenz nach Löschung, kein `getsource`-Contains). Der AST-Sweep ist der Regex-Grenzfall (dokumentieren).

### D-10-Constraint (OPERATIV):
- Committeter Test darf NIE rot ins `deploy.sh:193`-Gate. Rot-vor-Fix-Beweis = **manueller HEAD-Lauf auf Prod-Server, dokumentiert**. Grün-Commit erst mit der LETZTEN Welle. Kein committeter `xfail`.

---

## 6. Landmines / Gotchas (höchster Wert — SPEC-DRIFT gegen HEAD 2026-07-03)

1. **[HIGH] SPEC Req 5 überschätzt den Fix-Umfang — 5 Result-Globale sind ZOMBIES.** `state['ergebnis'/'aktiv'/'version'/'active_hint'/'ewb_buttons']` haben **0 Produktiv-Reader** (grep `.get('ergebnis'/'version'/'aktiv')` = 0; `active_hint`/`ewb_buttons`-Reader = 0). Die Auslieferung ist 100% `sio.emit(room=sid)`, die Polling-Routes `/api/ergebnis`+`/api/status` **existieren nicht mehr** (grep `routes/` = 0). SPEC Req 5 sagt „per-SID migrieren" — grep sagt **DELETE**. Der Planner spart eine ganze Migration und muss sie stattdessen in Welle 0 löschen.

2. **[HIGH] reset_session-Caller ist `app_routes.py:834`, NICHT `:480`.** SPEC Background + CONTEXT code_context (`:166`) sagen „1 Caller: `app_routes.py:480`". Grep+Read: der einzige Import+Call ist `from services.live_session import ... reset_session` (`app_routes.py:7`) → Aufruf `reset_session()` bei **`app_routes.py:834`** (Ende von `/api/beenden`). `:480` ist ein anderer Kontext (transcript_segments-Guard). Stale Anker — Plan muss `:834` targeten.

3. **[HIGH] compute_rubric liegt in `services/rubric_engine.py:68`, NICHT `database/rubric_dimensions.py:393-402`.** SPEC Req 8 Acceptance (ii) verweist auf `rubric_dimensions.py:393-402` — der Pfad `database/rubric_dimensions.py` **existiert nicht** (es gibt `services/rubric_dimensions.py` UND `services/rubric_engine.py`; `compute_rubric` ist in `rubric_engine.py`). Empty-Set-Toleranz verifiziert bei `rubric_engine.py:118/188/198-218`. Doppelter Drift (Ordner + Datei + Zeile).

4. **[MED] `state['aktives_skript_inhalt']`/`state['skript_bloecke']` = Delete-Kandidaten (write-only?).** SPEC Req 6 sagt „per-SID gelesen → kommt nie an". Grep: Writer `deepgram_service.py:641-642`, aber **0 aktiver Reader gefunden** (kein `state.get('aktives_skript_inhalt')` / `skript_bloecke` in services/routes). Entweder der Reader liegt in einem nicht-gegreppten Pfad (prompt_pipeline?) ODER es ist write-only → Planner muss den Reader in Welle 0 belegen; wenn keiner → DELETE, nicht FIX. SPEC-Annahme „per-SID gelesen" NICHT verifizierbar.

5. **[MED] `aktive_phase_idx` + `_bof_count`: kein Live-Writer gefunden.** Beide nur def + reset + Reader (`aktive_phase_idx`: `app_routes:244`, `claude_service:206`; `_bof_count`: 0). SPEC Req 7 listet beide als „umziehen". Wenn der Writer verloren ging (Halb-Migration §29-Falle), ist `aktive_phase_idx` immer 0 → der `cp_snapshot.add(ls.aktive_phase_idx)` (`app_routes:244`) fügt immer Phase 0 hinzu (potenzieller Bug). Planner: grep-belegen ob Writer existiert; wenn nicht → DELETE + Reader auf per-SID `state['current_phase']` umbiegen.

6. **[MED] claude_service globale Write-Anker sind fragmentiert, NICHT `:1137-1372` monolithisch.** SPEC Req 5 sagt „`:1137-1372`". Real: `:955` (aktiv=True), `:1137-1139` (ergebnis/aktiv/version), `:1360-1361` (active_hint/ewb_buttons), `:1370-1372` (Fehler-Branch). Plan muss alle 4 Cluster treffen (die meisten davon sind eh Zombie-Deletes, s. Landmine 1).

7. **[MED] `_log_last_sp` ist ein Cross-Tenant-Speaker-Leak (FIX, nicht in SPEC-Kernliste betont).** `deepgram_service.py:77` schreibt `ls._log_last_sp` global; `:78` liest es. Bei parallelen Calls fällt der Speaker-Fallback von Call A auf Call B über. SPEC Req 7 nennt „Speaker-Familie" generisch; dieser ist ein echter Live-Writer (nicht nur reset-tot wie `_confirmed_speaker`) → gehört zur Fix-Familie E.

8. **[LOW] `deepgram_service.py`-Config-Writes bei `:618-619/641-642`, SPEC sagte `:619,641-642`.** session_anrede-Write ist `:618-619` (mit `with ls.state_lock:` bei `:618`). Marginal.

9. **[LOW] `precall_briefing` doppelte Wahrheit.** Modul-`state['precall_briefing']` (Reader `app_routes:112`) vs per-SID `_session_state[sid]['_briefing']` (Set `:262`, Reader `get_briefing_for_sid:268`). Der HTTP-Reader liest die Modul-Globale, der Live-Pfad die per-SID. Split — Reader auf per-SID umbiegen, Modul-Key löschen (Familie A/config).

10. **[INFO] `session_meta` per-SID-Seed (`:400`) ist AUCH tot.** Nicht nur die Modul-Globale (0 Writer/Reader) ist Zombie — der per-SID `session_meta`-Sub-Key (`:400-413`) wird ebenfalls von keinem Prod-Pfad gelesen/geschrieben. Beide löschen.

---

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | `state['aktives_skript_inhalt']`/`skript_bloecke` haben 0 Reader (write-only) | §1/§6.4 | Wenn ein Reader in prompt_pipeline/ungegreptem Pfad existiert → FIX statt DELETE. Planner muss grep in `services/prompt_pipeline.py` nachziehen. |
| A2 | `aktive_phase_idx`/`_bof_count` haben 0 Live-Writer | §1/§6.5 | Wenn Writer existiert (anderer Pfad) → FIX. Planner grep-belegen. |
| A3 | `readiness_score/bucket/score_factors_seen` Modul-`state`-Writer-Status | §1 | score_factors_seen ist per-SID (`claude_service:1355`); readiness_* Modul-Write-Pfad nicht final gegreppt. |

---

## Environment Availability

Nicht anwendbar — reine Code/Test-Änderung, keine neuen externen Dependencies. Deploy-Gate `deploy.sh:193` (`nerve_test`) existiert. Postgres `nerve_test` für Concurrency-Test vorhanden (PGTEST-Phasen).

---

## Sources

### Primary (HIGH — Read/grep gegen HEAD 2026-07-03)
- `services/live_session.py` (voll gelesen, 1223 Z.) — Global-Defs, `_flush_segment`, `reset_session`, `record_*`.
- `services/deepgram_service.py` (voll gelesen, 1121 Z.) — `_merge_pending`-Writer, config-Writes, speaker-Globale, manual_ewb.
- `routes/app_routes.py:1-1101` — `/api/beenden` voller Reader-Pfad + resolved-Flags.
- `services/claude_service.py:740-865,955,1137-1372` (grep+Read) — globale Writes, TTFT-Muster.
- `services/slow_lane.py:225-393` — F2-Insertion-Point, `_pending_events`, `not_gradable`.
- `services/intent_event_writer.py:90-159` — `build_intent_payload`-Quelle.
- `services/rubric_engine.py:68,195-238` — compute_rubric empty-set-Toleranz.
- `database/models.py:703,713,795` — resolved-Flags, payload_jsonb (kein schema_version).
- `app.py:2208-2221` — Daemon-Start (KEIN TESTING-Guard).
- `tests/test_medium_lane_intent_event_live.py`, `tests/test_schild_guard.py:1-60` — Mock-Muster.
- grep-Zählungen: reader/writer-Counts aller Delete-Kandidaten (0-Reader-Belege).

### Locked Contracts (gelesen, nicht dupliziert)
- `08.23.2.PERSID-SPEC.md`, `08.23.2.PERSID-CONTEXT.md`, `CLAUDE.md` (Punkt 14/17/22/24/25/26/28/29).

## Metadata
**Confidence:** Global-Inventur HIGH (grep-belegt); Fix-vs-Delete HIGH für Zombies (0-Reader bewiesen), MED für A1-A3 (ungegreppte Reader-Pfade). Datenfamilie-Schnitt HIGH. SPEC-Drift HIGH (re-verifiziert).
**Valid until:** 7 Tage (heisser Code, häufige Edits) — vor Execute erneut gegen HEAD greppen.
