---
phase: 04.7.1
plan: 03
type: execute
wave: 2
depends_on: [04.7.1-01, 04.7.1-02]
files_modified:
  - routes/app_routes.py
  - app.py
  - services/claude_service.py
  - services/training_service.py
  - tests/test_ft_seed.py
autonomous: true
requirements: [FT-PROMPT-SEED]
must_haves:
  truths:
    - "Nach App-Start existieren 6 prompt_versions Einträge (assistant_live, coaching_live, objection_trigger, ewb_ranking, api_frage, training_persona) mit v1.0.0"
    - "Pro Modul genau ein is_active=True Eintrag"
    - "Alle inline-Prompts, die heute im Codebase existieren (OBJECTION_TRIGGER, EWB_RANKING, API_FRAGE, TRAINING_PERSONA), wurden in Modul-Konstanten extrahiert und im Seed verwendet"
    - "_seed_prompt_versions() ist idempotent (zweiter Aufruf erzeugt keine Duplikate)"
    - "get_active_prompt_version(module) gibt die v1.0.0 für bekannte Module zurück"
  artifacts:
    - path: "app.py"
      provides: "_seed_prompt_versions() Funktion, direkt nach _migrate() aufgerufen"
      contains: "def _seed_prompt_versions"
    - path: "services/claude_service.py"
      provides: "get_active_prompt_version(module) Helper mit Cache + extrahierte Konstanten SYSTEM_PROMPT_BASE, COACHING_PROMPT_BASE, EWB_RANKING_PROMPT_BASE"
      contains: "def get_active_prompt_version"
    - path: "routes/app_routes.py"
      provides: "OBJECTION_TRIGGER_PROMPT_BASE und API_FRAGE_PROMPT_BASE Konstanten extrahiert aus inline f-strings"
      contains: "OBJECTION_TRIGGER_PROMPT_BASE"
    - path: "services/training_service.py"
      provides: "TRAINING_PERSONA_PROMPT_BASE Konstante (alias/extraction aus bestehendem KUNDEN_PROMPT_TEMPLATE)"
      contains: "TRAINING_PERSONA_PROMPT_BASE"
  key_links:
    - from: "app.py::_seed_prompt_versions"
      to: "database.models.PromptVersion"
      via: "SessionLocal INSERT mit Existenz-Check"
      pattern: "PromptVersion\\(module="
    - from: "services/claude_service.py::get_active_prompt_version"
      to: "prompt_versions.is_active"
      via: "SELECT WHERE module=? AND is_active=1"
      pattern: "filter_by\\(module=.*is_active=True\\)"
---

<objective>
Prompt-Seed-Infrastruktur: Alle 6 aktiven System-Prompts als v1.0.0 in `prompt_versions` seeden und einen Cache-basierten Resolver bereitstellen.

Purpose: Jedes ft_*_event muss zur Write-Zeit die aktuelle Prompt-Version als String speichern können. Ohne Seed ist prompt_version NOT NULL nicht erfüllbar. Die inline-Prompts (EWB-Trigger in routes/app_routes.py, EWB-Ranking in claude_service.py, api_frage in routes/app_routes.py) sind derzeit f-strings mitten in den Routen/Funktionen — müssen extrahiert werden, sonst können sie nicht geseedet werden.

Output: Alle heute existierenden Inline-Prompts als Modul-Konstanten extrahiert, _seed_prompt_versions() in app.py direkt nach _migrate() aufgerufen, get_active_prompt_version() Cache-Helper in claude_service.py.

**Honors user decision:** Locked decision §3 — Seed ALLE 6 Module (assistant_live, coaching_live [fliegt später raus, trotzdem loggen], objection_trigger, ewb_ranking, api_frage, training_persona).
</objective>

<context>
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-CONTEXT.md
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-RESEARCH.md
@services/claude_service.py
@routes/app_routes.py
@app.py
@database/models.py
@services/training_service.py

<interfaces>
Aus services/claude_service.py (verifiziert per grep):
- SYSTEM_PROMPT_BASE (line 9) ✓ existiert bereits als Modul-Konstante — für module='assistant_live'
- COACHING_PROMPT_BASE (line 50) ✓ existiert bereits als Modul-Konstante — für module='coaching_live'
- rank_ewb() (line ~328): Prompt wird als **inline string concatenation** in der Funktion gebaut (keine Modul-Konstante) — MUSS als EWB_RANKING_PROMPT_BASE extrahiert werden → für module='ewb_ranking'

Aus routes/app_routes.py (verifiziert per grep):
- api_ewb_trigger (~line 603): inline f-string → MUSS als OBJECTION_TRIGGER_PROMPT_BASE extrahiert werden → für module='objection_trigger'
- api_frage (~line 543): inline f-string → MUSS als API_FRAGE_PROMPT_BASE extrahiert werden → für module='api_frage'

Aus services/training_service.py (verifiziert per grep):
- KUNDEN_PROMPT_TEMPLATE (line 338) ✓ existiert bereits als Modul-Konstante → muss als TRAINING_PERSONA_PROMPT_BASE aliased/zusätzlich exponiert werden → für module='training_persona'
- SEKRETAERIN_PROMPT (line 314) existiert ebenfalls, ist aber Discretion (nicht im 6-Modul-Set)

Aus database/models.py (von Plan 02 angelegt):
- class PromptVersion(Base) mit Spalten: id, version, module, prompt_text, changelog, is_active, created_at
- UniqueConstraint('version', 'module')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline-Prompts extrahieren (MANDATORY für alle heute existierenden Prompts)</name>
  <read_first>
    - routes/app_routes.py Zeilen 525-570 (api_frage Funktion und inline f-string Prompt)
    - routes/app_routes.py Zeilen 580-650 (api_ewb_trigger Funktion und inline f-string Prompt)
    - services/claude_service.py Zeilen 1-100 und rund um rank_ewb (Zeile ~328-360)
    - services/training_service.py Zeilen 310-360 (SEKRETAERIN_PROMPT, KUNDEN_PROMPT_TEMPLATE)
  </read_first>
  <action>
    **Grep-First-Regel:** Vor jedem Extract-Schritt grep ausführen, ob die Konstante bereits existiert. Nur wenn NICHT vorhanden → extrahieren. Aber Extraktion ist **MANDATORY**, nicht conditional, für alle Prompts, die heute im Codebase inline existieren.

    1. **services/claude_service.py** — `SYSTEM_PROMPT_BASE` und `COACHING_PROMPT_BASE`:
       ```bash
       grep -n "^SYSTEM_PROMPT_BASE" services/claude_service.py
       grep -n "^COACHING_PROMPT_BASE" services/claude_service.py
       ```
       Beide existieren bereits laut Grep (Zeile 9 und 50). **Keine Änderung nötig.**

    2. **services/claude_service.py** — `EWB_RANKING_PROMPT_BASE` (MANDATORY — der Prompt ist derzeit inline in `rank_ewb()` ab Zeile ~335 gebaut):
       ```bash
       grep -n "^EWB_RANKING_PROMPT_BASE" services/claude_service.py
       ```
       Nicht vorhanden → **MUSS extrahiert werden**. Auf Modul-Ebene (neben SYSTEM_PROMPT_BASE) als Template mit `{segments}` und `{einwaende}` Platzhaltern anlegen:
       ```python
       EWB_RANKING_PROMPT_BASE = """Basierend auf diesen letzten Gesprächssegmenten:
{segments}

Welche 2 dieser Einwände kommen am wahrscheinlichsten als nächstes?
Einwände: {einwaende}

Antworte NUR mit einem JSON-Array der 2 wahrscheinlichsten: ["typ1", "typ2"]"""
       ```
       Dann `rank_ewb()` umstellen auf `EWB_RANKING_PROMPT_BASE.format(segments='\n'.join(...), einwaende=', '.join(...))`. Route-Verhalten darf sich nicht ändern.

    3. **routes/app_routes.py** — `OBJECTION_TRIGGER_PROMPT_BASE` (MANDATORY — inline f-string in api_ewb_trigger Zeile ~603):
       ```bash
       grep -n "^OBJECTION_TRIGGER_PROMPT_BASE" routes/app_routes.py
       ```
       Nicht vorhanden → **MUSS extrahiert werden**. Im Header-Bereich des Files (neben anderen Konstanten) als Template anlegen. Die f-string-Interpolationen (`{profile_ctx}`, `{ctx_text}`, `{einwand_typ}` etc.) werden zu `{placeholder}`-Format-Tokens konvertiert. Die bestehende Route nutzt dann `OBJECTION_TRIGGER_PROMPT_BASE.format(profile_ctx=..., ctx_text=..., einwand_typ=...)`. Verhalten der Route darf sich nicht ändern.

    4. **routes/app_routes.py** — `API_FRAGE_PROMPT_BASE` (MANDATORY — inline f-string in api_frage Zeile ~543):
       ```bash
       grep -n "^API_FRAGE_PROMPT_BASE" routes/app_routes.py
       ```
       Nicht vorhanden → **MUSS extrahiert werden**. Analog wie OBJECTION_TRIGGER_PROMPT_BASE: f-string-Interpolationen (`{profile_ctx}`, `{ctx_text}`, `{frage}`) zu `.format()`-Platzhaltern konvertieren, api_frage nutzt `API_FRAGE_PROMPT_BASE.format(...)`.

    5. **services/training_service.py** — `TRAINING_PERSONA_PROMPT_BASE` (MANDATORY):
       ```bash
       grep -n "^KUNDEN_PROMPT_TEMPLATE" services/training_service.py
       grep -n "^TRAINING_PERSONA_PROMPT_BASE" services/training_service.py
       ```
       `KUNDEN_PROMPT_TEMPLATE` existiert bereits (Zeile 338). Da der Seed-Code aber `TRAINING_PERSONA_PROMPT_BASE` importieren soll (Konsistenz mit den anderen `*_PROMPT_BASE`-Namen), fügen wir direkt nach der `KUNDEN_PROMPT_TEMPLATE`-Definition einen Alias hinzu:
       ```python
       TRAINING_PERSONA_PROMPT_BASE = KUNDEN_PROMPT_TEMPLATE
       ```
       Keine Logik-Änderung. Nur Export eines stabilen Namens.

    6. Alle Änderungen sind **reine Extraktionen ohne Logik-Änderung**. Die bestehenden Routen/Funktionen verhalten sich bit-identisch wie vorher. Nur der Ort, an dem der Prompt-Text definiert wird, verschiebt sich vom Funktionskörper auf Modul-Ebene.

    7. **Wichtig zu `rank_ewb` Modellstring:** Der inline-Prompt in rank_ewb verwendet `model='claude-haiku-4-5-20251001'`. Das Modell-Literal bleibt in der Funktion (nicht in der Konstante). Nur der Prompt-Text wird extrahiert.
  </action>
  <verify>
    <automated>grep -l "OBJECTION_TRIGGER_PROMPT_BASE" routes/app_routes.py &amp;&amp; grep -l "API_FRAGE_PROMPT_BASE" routes/app_routes.py &amp;&amp; grep -l "EWB_RANKING_PROMPT_BASE" services/claude_service.py &amp;&amp; grep -l "TRAINING_PERSONA_PROMPT_BASE" services/training_service.py &amp;&amp; python -c "from routes.app_routes import OBJECTION_TRIGGER_PROMPT_BASE, API_FRAGE_PROMPT_BASE; from services.claude_service import SYSTEM_PROMPT_BASE, COACHING_PROMPT_BASE, EWB_RANKING_PROMPT_BASE; from services.training_service import TRAINING_PERSONA_PROMPT_BASE; assert all(len(p) > 30 for p in [SYSTEM_PROMPT_BASE, COACHING_PROMPT_BASE, OBJECTION_TRIGGER_PROMPT_BASE, EWB_RANKING_PROMPT_BASE, API_FRAGE_PROMPT_BASE, TRAINING_PERSONA_PROMPT_BASE]); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^OBJECTION_TRIGGER_PROMPT_BASE" routes/app_routes.py` → exit 0
    - `grep -q "^API_FRAGE_PROMPT_BASE" routes/app_routes.py` → exit 0
    - `grep -q "^SYSTEM_PROMPT_BASE" services/claude_service.py` → exit 0
    - `grep -q "^COACHING_PROMPT_BASE" services/claude_service.py` → exit 0
    - `grep -q "^EWB_RANKING_PROMPT_BASE" services/claude_service.py` → exit 0
    - `grep -q "^TRAINING_PERSONA_PROMPT_BASE" services/training_service.py` → exit 0
    - api_ewb_trigger verwendet `OBJECTION_TRIGGER_PROMPT_BASE` (grep zeigt Nutzung in der Funktion, nicht nur die Konstanten-Definition)
    - api_frage verwendet `API_FRAGE_PROMPT_BASE`
    - rank_ewb verwendet `EWB_RANKING_PROMPT_BASE.format(...)`
    - `python -c "from routes.app_routes import OBJECTION_TRIGGER_PROMPT_BASE, API_FRAGE_PROMPT_BASE; assert len(OBJECTION_TRIGGER_PROMPT_BASE) > 50 and len(API_FRAGE_PROMPT_BASE) > 50"` exit 0
    - `python -c "from services.claude_service import SYSTEM_PROMPT_BASE, COACHING_PROMPT_BASE, EWB_RANKING_PROMPT_BASE; assert all(len(p) > 30 for p in [SYSTEM_PROMPT_BASE, COACHING_PROMPT_BASE, EWB_RANKING_PROMPT_BASE])"` exit 0
    - `python -c "from services.training_service import TRAINING_PERSONA_PROMPT_BASE; assert len(TRAINING_PERSONA_PROMPT_BASE) > 30"` exit 0
    - `python -c "from app import app"` exit 0 (keine Syntax/Import-Regression)
    - Grep-Bestätigung aller neuen Konstanten in services/claude_service.py: `grep -E "^(SYSTEM_PROMPT_BASE|COACHING_PROMPT_BASE|EWB_RANKING_PROMPT_BASE)" services/claude_service.py` liefert 3 Treffer
  </acceptance_criteria>
  <done>Alle 5 notwendigen Prompt-Konstanten existieren auf Modul-Ebene (4 extrahiert, 2 bereits vorhanden), alle 4 konsumierenden Routen/Funktionen nutzen die Konstanten via .format(), App bleibt lauffähig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _seed_prompt_versions() + get_active_prompt_version() implementieren</name>
  <read_first>
    - app.py (_migrate() Funktion finden, wo die Erweiterung platziert wird)
    - database/models.py (PromptVersion Klasse von Plan 02)
    - services/claude_service.py (wo der Cache-Helper platziert wird + die nach Task 1 extrahierten Konstanten)
    - tests/test_ft_seed.py (Test-Kontrakt: _seed_prompt_versions akzeptiert optional db_session Parameter)
  </read_first>
  <behavior>
    - _seed_prompt_versions(db=None) seeded 6 PromptVersion Rows: ('assistant_live', 'coaching_live', 'objection_trigger', 'ewb_ranking', 'api_frage', 'training_persona') jeweils mit version='v1.0.0', is_active=True
    - Alle 6 Prompts verwenden ihre aus Task 1 extrahierten echten Konstanten — KEINE Placeholder-Strings. Der Seed importiert direkt die Konstanten.
    - Falls Row mit (module, version='v1.0.0') bereits existiert: skip (idempotent)
    - get_active_prompt_version('assistant_live') gibt 'v1.0.0' zurück
    - get_active_prompt_version('unknown_module') gibt 'unknown' zurück (kein Crash)
    - Cache: zweiter Aufruf für dasselbe Modul queried die DB nicht erneut
  </behavior>
  <files>app.py, services/claude_service.py, tests/test_ft_seed.py</files>
  <action>
    1. **app.py**: Neue Funktion `_seed_prompt_versions(db=None)` definieren (auf Modul-Ebene, vor `init_db` Nutzung oder neben `_migrate`). Da Task 1 ALLE Inline-Prompts bereits extrahiert hat, importiert der Seed direkt die realen Konstanten — KEINE try/except-ImportError-Fallbacks, KEINE Placeholder-Strings:
       ```python
       def _seed_prompt_versions(db=None):
           from database.db import SessionLocal
           from database.models import PromptVersion
           # Import prompt constants — alle heute existierenden Module
           from services.claude_service import (
               SYSTEM_PROMPT_BASE,
               COACHING_PROMPT_BASE,
               EWB_RANKING_PROMPT_BASE,
           )
           from routes.app_routes import (
               OBJECTION_TRIGGER_PROMPT_BASE,
               API_FRAGE_PROMPT_BASE,
           )
           from services.training_service import TRAINING_PERSONA_PROMPT_BASE

           modules = [
               ('assistant_live',    SYSTEM_PROMPT_BASE),
               ('coaching_live',     COACHING_PROMPT_BASE),
               ('objection_trigger', OBJECTION_TRIGGER_PROMPT_BASE),
               ('ewb_ranking',       EWB_RANKING_PROMPT_BASE),
               ('api_frage',         API_FRAGE_PROMPT_BASE),
               ('training_persona',  TRAINING_PERSONA_PROMPT_BASE),
           ]
           owns_session = db is None
           if owns_session:
               db = SessionLocal()
           try:
               for module, ptext in modules:
                   exists = db.query(PromptVersion).filter_by(module=module, version='v1.0.0').first()
                   if exists:
                       continue
                   db.add(PromptVersion(
                       module=module,
                       version='v1.0.0',
                       prompt_text=ptext,  # direkt der echte extrahierte Text — keine Fallbacks
                       is_active=True,
                       changelog='Initial seed (Phase 04.7.1)',
                   ))
               db.commit()
           finally:
               if owns_session:
                   db.close()
       ```

       **Hinweis:** Falls während der Entwicklung trotz Task 1 eine Konstante fehlt, soll der Import hart crashen (ImportError) — das ist das gewünschte Verhalten, weil Task 1 die Extraktion MANDATORY gemacht hat. Keine stillen Fallbacks.

    2. Direkt nach dem bestehenden `_migrate()`-Aufruf in app.py: `_seed_prompt_versions()` aufrufen. Reihenfolge: `init_db(engine)` → `_migrate()` → `_seed_prompt_versions()`.

    3. **services/claude_service.py**: Cache-Helper am Anfang des Files (nach den bestehenden Prompt-Konstanten, inklusive des in Task 1 neu angelegten EWB_RANKING_PROMPT_BASE):
       ```python
       _ACTIVE_PROMPT_CACHE: dict = {}

       def get_active_prompt_version(module: str) -> str:
           if module in _ACTIVE_PROMPT_CACHE:
               return _ACTIVE_PROMPT_CACHE[module]
           try:
               from database.db import SessionLocal
               from database.models import PromptVersion
               db = SessionLocal()
               try:
                   pv = db.query(PromptVersion).filter_by(module=module, is_active=True).first()
                   version = pv.version if pv else 'unknown'
               finally:
                   db.close()
           except Exception:
               version = 'unknown'
           _ACTIVE_PROMPT_CACHE[module] = version
           return version
       ```

    4. **tests/test_ft_seed.py**: Den `pytest.skip` in beiden Tests entfernen. Test soll `_seed_prompt_versions(db_session)` aufrufen und in der in-memory SQLite prüfen. Zusätzlich:
       - test_prompt_seed: Assert dass alle 6 Module existieren, jedes mit is_active=True, und dass prompt_text jeweils länger als 30 Zeichen ist (validiert dass echte Prompts seeded wurden, keine Placeholder).
       - test_seed_idempotent: Erweitern um assert `count_after_first == 6` und `count_after_second == 6`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ft_seed.py -x -v &amp;&amp; python -c "from services.claude_service import get_active_prompt_version; print(get_active_prompt_version('assistant_live'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def _seed_prompt_versions" app.py` → exit 0
    - `grep -c "_seed_prompt_versions" app.py` ≥ 2 (def + call)
    - `grep -q "def get_active_prompt_version" services/claude_service.py` → exit 0
    - `grep -q "_ACTIVE_PROMPT_CACHE" services/claude_service.py` → exit 0
    - `grep -q "'assistant_live'" app.py` → exit 0
    - `grep -q "'coaching_live'" app.py` → exit 0
    - `grep -q "'objection_trigger'" app.py` → exit 0
    - `grep -q "'ewb_ranking'" app.py` → exit 0
    - `grep -q "'api_frage'" app.py` → exit 0
    - `grep -q "'training_persona'" app.py` → exit 0
    - `grep -q "from services.claude_service import" app.py` → Import enthält SYSTEM_PROMPT_BASE, COACHING_PROMPT_BASE, EWB_RANKING_PROMPT_BASE
    - `grep -q "from routes.app_routes import" app.py` → Import enthält OBJECTION_TRIGGER_PROMPT_BASE, API_FRAGE_PROMPT_BASE
    - Keine Placeholder-Strings im Seed: `grep -q "extract in follow-up" app.py` → exit 1 (darf NICHT vorhanden sein)
    - `python -m pytest tests/test_ft_seed.py -q` exit 0 (2 Tests grün, kein Skip)
    - test_prompt_seed prüft alle 6 Module + prompt_text > 30 Zeichen
    - test_seed_idempotent prüft count == 6 nach erstem + zweitem Seed
  </acceptance_criteria>
  <done>6 Module werden geseedet mit echten extrahierten Prompts (keine Placeholder), idempotent, Cache-Resolver funktioniert, 2 seed-Tests grün.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` grün
- `python -c "from app import app"` keine Errors
- Manuell (optional): `sqlite3 database/nerve.db "SELECT module, version, is_active, length(prompt_text) FROM prompt_versions"` zeigt 6 Zeilen, alle mit length > 30
</verification>

<success_criteria>
6 Module geseedet mit v1.0.0 + is_active=True, alle mit echten extrahierten Prompt-Texten (keine Placeholder), idempotent, get_active_prompt_version Cache-Helper verfügbar.
</success_criteria>

<output>
`.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-03-SUMMARY.md`
</output>
