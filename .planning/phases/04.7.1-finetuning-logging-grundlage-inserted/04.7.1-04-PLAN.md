---
phase: 04.7.1
plan: 04
type: execute
wave: 3
depends_on: [04.7.1-02, 04.7.1-03]
files_modified:
  - services/claude_service.py
  - tests/test_ft_write_hooks.py
autonomous: true
requirements: [FT-WRITE-ASSISTANT]
must_haves:
  truths:
    - "analyse_loop() schreibt nach jedem generierten Haiku-Hint eine Row in ft_assistant_events mit module='assistant_live'"
    - "coaching_loop() schreibt nach jedem Sonnet-Coaching-Output eine Row in ft_assistant_events mit module-Label 'coaching_live' (hint_category='coaching')"
    - "Im Cold-Call-Modus ist transcript_segment IS NULL und speaker IS NULL (DSGVO hart enforce'd im Write-Hook, nicht nur nullable-Schema)"
    - "model_used und prompt_version werden aus der tatsächlich verwendeten Claude-Call-Konfiguration + get_active_prompt_version(module) befüllt"
    - "Der Write-Hook öffnet eine eigene SessionLocal() (Background-Thread-sicher), try/finally close"
    - "Fehler im Write-Hook crashen analyse_loop NICHT (try/except + log, swallow)"
  artifacts:
    - path: "services/claude_service.py"
      provides: "_write_ft_assistant_event() Helper + Hook-Calls in analyse_loop und coaching_loop"
      contains: "def _write_ft_assistant_event"
    - path: "tests/test_ft_write_hooks.py"
      provides: "Unit-Test für _write_ft_assistant_event + Cold-Call NULL-Enforcement"
  key_links:
    - from: "services/claude_service.py::analyse_loop"
      to: "database.models.FtAssistantEvent"
      via: "_write_ft_assistant_event(module='assistant_live', ...)"
      pattern: "_write_ft_assistant_event"
    - from: "services/claude_service.py::coaching_loop"
      to: "database.models.FtAssistantEvent"
      via: "_write_ft_assistant_event(module='coaching_live', ...)"
      pattern: "module='coaching_live'"
---

<objective>
Write-Hooks für `ft_assistant_events` in analyse_loop() und coaching_loop() einhängen.

Purpose: Jeder Live-Hint, den NERVE dem Vertriebler zeigt, wird für Fine-Tuning geloggt — inklusive Kontext, Score und welches Modell+Prompt-Version generiert hat. Cold-Call-Modus darf KEIN Kundentranskript schreiben (DSGVO-Hard-Enforcement).

**Honors user decision:** Locked decision §1 (Coaching-Loop JA loggen, module='coaching_live') und §2 (ft_session_id wird in Plan 05 beim start_live_session INSERT befüllt — hier konsumieren wir ls.state['ft_session_id']).

Output: Privater Helper _write_ft_assistant_event() in claude_service.py, zwei Hook-Calls (analyse_loop + coaching_loop), isolierter Unit-Test.
</objective>

<context>
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-CONTEXT.md
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-RESEARCH.md
@services/claude_service.py
@database/models.py
@live_session.py

<interfaces>
Aus services/claude_service.py (laut RESEARCH.md):
- analyse_loop() — Background-Thread, erzeugt Haiku-Hint via analysiere_mit_claude(); Model = 'claude-haiku-*' (exakte Bezeichnung aus dem Code entnehmen)
- coaching_loop() — Sonnet-Call (fliegt später raus, trotzdem loggen)
- Beide laufen OHNE Flask-Request-Context → eigene SessionLocal() nötig

Aus live_session.py:
- ls.state dict + state_lock
- ls.state['mode'] = 'cold_call' | 'meeting'
- ls.state['ft_session_id'] — wird in Plan 05 gesetzt, hier lesen (kann None sein → skip write)
- ls.kaufbereitschaft (int 0-100) — readiness_score
- ls.conversation_log oder session_meta (user_id lookup)

Aus database/models.py:
- FtAssistantEvent Felder (siehe Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _write_ft_assistant_event() Helper + Cold-Call NULL-Enforcement</name>
  <read_first>
    - services/claude_service.py komplett (analyse_loop, coaching_loop, analysiere_mit_claude, Modell-Konstanten)
    - live_session.py (state-Dict Struktur, Lock, mode-Feld, kaufbereitschaft, user_id-Lookup)
    - database/models.py (FtAssistantEvent Feld-Liste)
  </read_first>
  <behavior>
    - _write_ft_assistant_event(module, hint_type, hint_text, model_used, context) öffnet SessionLocal, liest ft_session_id/user_id/mode aus live_session.state (thread-safe via state_lock), erstellt FtAssistantEvent Row, committed, schließt Session
    - Bei mode=='cold_call': transcript_segment=None und speaker=None (auch wenn context['transcript_segment'] einen Wert liefert — hart überschreiben)
    - Bei mode=='meeting': transcript_segment und speaker aus context übernehmen
    - conversation_phase = context.get('conversation_phase') or 'unknown'
    - prompt_version = get_active_prompt_version(module)
    - timestamp_ms = int(time.time() * 1000)
    - Bei ft_session_id is None: früh returnen (kein Write, kein Error)
    - Exception beim Write: print("[FT] assistant_event write failed: ...") und swallow — analyse_loop darf nie crashen
  </behavior>
  <files>services/claude_service.py, tests/test_ft_write_hooks.py</files>
  <action>
    1. In `services/claude_service.py` (neben get_active_prompt_version aus Plan 03) eine neue Funktion anlegen:

       ```python
       def _write_ft_assistant_event(
           module: str,
           hint_type: str,
           hint_text: str,
           model_used: str,
           context: dict | None = None,
       ) -> None:
           """
           Write one row to ft_assistant_events. Called from background threads
           (analyse_loop, coaching_loop). MUST NOT raise — swallows all errors.

           Cold-Call DSGVO enforcement: transcript_segment and speaker are
           hard-set to None when ls.state['mode'] == 'cold_call', regardless
           of what the caller passed in.
           """
           import time, json
           context = context or {}
           try:
               import live_session as ls
               from database.db import SessionLocal
               from database.models import FtAssistantEvent

               with ls.state_lock:
                   ft_session_id = ls.state.get('ft_session_id')
                   mode = ls.state.get('mode') or 'cold_call'
                   user_id = ls.state.get('user_id')
                   market = ls.state.get('market') or 'dach'
                   language = ls.state.get('language') or 'de'
                   readiness = ls.state.get('kaufbereitschaft')

               if ft_session_id is None or user_id is None:
                   return  # Phase not yet started or anonymous — skip write

               # Cold-Call DSGVO: NULL enforce
               if mode == 'cold_call':
                   transcript_segment = None
                   speaker = None
               else:
                   transcript_segment = context.get('transcript_segment')
                   speaker = context.get('speaker')

               db = SessionLocal()
               try:
                   row = FtAssistantEvent(
                       ft_session_id=ft_session_id,
                       user_id=user_id,
                       market=market,
                       language=language,
                       timestamp_ms=int(time.time() * 1000),
                       conversation_phase=context.get('conversation_phase') or 'unknown',
                       speaker=speaker,
                       transcript_segment=transcript_segment,
                       context_window=json.dumps(context.get('context_window')) if context.get('context_window') else None,
                       customer_data=json.dumps(context.get('customer_data')) if context.get('customer_data') else None,
                       profile_data=json.dumps(context.get('profile_data')) if context.get('profile_data') else None,
                       readiness_score=readiness,
                       active_learning_cards=None,  # Phase 4.11
                       hint_type=hint_type,
                       hint_text=hint_text or '',
                       hint_category=context.get('hint_category'),
                       model_used=model_used or 'unknown',
                       prompt_version=get_active_prompt_version(module),
                   )
                   db.add(row)
                   db.commit()
               finally:
                   db.close()
           except Exception as e:
               print(f"[FT] assistant_event write failed (module={module}): {e}")
       ```

    2. **tests/test_ft_write_hooks.py** neu anlegen — testet den Cold-Call-NULL-Enforcement isoliert:

       ```python
       import pytest
       from unittest.mock import patch, MagicMock

       def test_cold_call_forces_transcript_null(db_session, monkeypatch):
           from database.models import User, FtCallSession, FtAssistantEvent, PromptVersion
           # Arrange: seed minimal fixtures in db_session
           u = User(email="t@t.de", passwort_hash="x")
           db_session.add(u); db_session.flush()
           sess = FtCallSession(user_id=u.id, mode='cold_call', market='dach', language='de')
           db_session.add(sess); db_session.flush()
           db_session.add(PromptVersion(module='assistant_live', version='v1.0.0', prompt_text='x', is_active=True))
           db_session.commit()

           # Mock live_session module state
           import live_session as ls
           original_state = dict(ls.state) if hasattr(ls, 'state') else {}
           try:
               if not hasattr(ls, 'state'):
                   ls.state = {}
               if not hasattr(ls, 'state_lock'):
                   import threading
                   ls.state_lock = threading.Lock()
               ls.state.update({
                   'ft_session_id': sess.id,
                   'user_id': u.id,
                   'mode': 'cold_call',
                   'market': 'dach',
                   'language': 'de',
                   'kaufbereitschaft': 50,
               })

               # Mock SessionLocal to use db_session
               from database import db as dbmod
               monkeypatch.setattr(dbmod, 'SessionLocal', lambda: _FakeSession(db_session))

               from services.claude_service import _write_ft_assistant_event
               _write_ft_assistant_event(
                   module='assistant_live',
                   hint_type='objection',
                   hint_text='Test hint',
                   model_used='claude-haiku-test',
                   context={'transcript_segment': 'SHOULD BE DROPPED', 'speaker': 'customer'},
               )

               rows = db_session.query(FtAssistantEvent).all()
               assert len(rows) == 1
               assert rows[0].transcript_segment is None  # D-04/D-05 hard enforce
               assert rows[0].speaker is None
               assert rows[0].hint_text == 'Test hint'
               assert rows[0].prompt_version == 'v1.0.0'
           finally:
               ls.state.clear()
               ls.state.update(original_state)


       class _FakeSession:
           """Adapter so SessionLocal() returns the pytest db_session without closing it."""
           def __init__(self, real): self._real = real
           def add(self, *a, **k): return self._real.add(*a, **k)
           def commit(self): return self._real.commit()
           def close(self): pass
           def query(self, *a, **k): return self._real.query(*a, **k)
       ```

       Falls der Import `import live_session as ls` in Tests nicht möglich ist (z.B. Circular), den Test mit `pytest.importorskip("live_session")` schützen.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ft_write_hooks.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def _write_ft_assistant_event" services/claude_service.py` → exit 0
    - `grep -q "if mode == 'cold_call'" services/claude_service.py` → exit 0
    - `grep -q "transcript_segment = None" services/claude_service.py` → exit 0
    - `grep -q "except Exception" services/claude_service.py` → mindestens 1 Treffer im _write_ft_assistant_event-Block
    - `grep -q "get_active_prompt_version" services/claude_service.py` → exit 0 (Aufruf)
    - `test -f tests/test_ft_write_hooks.py` → exit 0
    - `python -m pytest tests/test_ft_write_hooks.py::test_cold_call_forces_transcript_null -x` → grün
  </acceptance_criteria>
  <done>Helper existiert, Cold-Call NULL enforce'd, Test grün.</done>
</task>

<task type="auto">
  <name>Task 2: _write_ft_assistant_event() in analyse_loop() + coaching_loop() einhängen</name>
  <read_first>
    - services/claude_service.py (analyse_loop komplett — Hint-Generation-Stellen finden; coaching_loop komplett — Sonnet-Output-Stelle finden)
  </read_first>
  <files>services/claude_service.py</files>
  <action>
    1. In `analyse_loop()` nach der Stelle, an der ein Hint vom Haiku-Modell zurückkommt und an den Client emitted wird (d.h. nach `analysiere_mit_claude` erfolgreich resultat liefert), den Hook einfügen. Typisches Muster:

       ```python
       # ... bestehender Code: resultat = analysiere_mit_claude(...)
       # ... existing emit/state update ...

       # FT logging hook (Phase 04.7.1)
       try:
           hint_text = resultat.get('text') or resultat.get('hint') or ''
           hint_type = resultat.get('typ') or resultat.get('category') or 'hint'
           _write_ft_assistant_event(
               module='assistant_live',
               hint_type=hint_type,
               hint_text=hint_text,
               model_used='claude-haiku-4-6',  # EXAKT aus der bestehenden anthropic-Call-Zeile übernehmen
               context={
                   'transcript_segment': letzter_berater_text,  # die Variable, die im Loop bereits existiert
                   'speaker': 'rep',
                   'hint_category': resultat.get('category'),
               },
           )
       except Exception as _e:
           print(f"[FT] hook skipped: {_e}")
       ```

       **WICHTIG:** Das `model_used`-Literal MUSS exakt mit dem Modell-String matchen, den der bestehende `anthropic.messages.create(model=...)` Call verwendet. Im Code nachschlagen und 1:1 übernehmen.

    2. Analog in `coaching_loop()` nach dem Sonnet-Output:

       ```python
       try:
           _write_ft_assistant_event(
               module='coaching_live',
               hint_type='coaching',
               hint_text=coaching_output_text,
               model_used='claude-sonnet-4-6',  # EXAKT wie in coaching_loop verwendet
               context={'hint_category': 'coaching'},
           )
       except Exception as _e:
           print(f"[FT] coaching hook skipped: {_e}")
       ```

    3. Beide Hook-Calls umgibt ein try/except — analyse_loop und coaching_loop dürfen nie durch FT-Logging crashen.

    4. Kein bestehendes Verhalten ändern. Kein neuer Claude-Call. Nur zusätzlicher DB-Write nach bereits generiertem Output.
  </action>
  <verify>
    <automated>grep -c "_write_ft_assistant_event" services/claude_service.py &amp;&amp; python -c "from app import app; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_write_ft_assistant_event" services/claude_service.py` ≥ 3 (1 def + 2 calls)
    - `grep -q "module='assistant_live'" services/claude_service.py` → exit 0
    - `grep -q "module='coaching_live'" services/claude_service.py` → exit 0
    - Beide Hook-Calls sind in `try:` Blöcken eingebettet (`grep -B1 "_write_ft_assistant_event(" services/claude_service.py` zeigt try: oder ähnliches)
    - `python -c "from services.claude_service import analyse_loop, coaching_loop, _write_ft_assistant_event; print('ok')"` exit 0
    - `python -m pytest tests/ -q` grün (keine Regression)
  </acceptance_criteria>
  <done>Beide Loops rufen _write_ft_assistant_event auf, try/except geschützt, App startet, bestehende Tests grün.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` grün
- Manueller Smoke: App starten, Live-Session beginnen (nach Plan 05), kurzen Call führen, `sqlite3 database/nerve.db "SELECT count(*) FROM ft_assistant_events"` sollte >0 zeigen
</verification>

<success_criteria>
Helper existiert, Cold-Call NULL hart enforce'd, 2 Call-Sites in claude_service.py, Tests grün.
</success_criteria>

<output>
`.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-04-SUMMARY.md`
</output>
