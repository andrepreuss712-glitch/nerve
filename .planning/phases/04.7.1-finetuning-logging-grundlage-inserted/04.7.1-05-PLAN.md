---
phase: 04.7.1
plan: 05
type: execute
wave: 3
depends_on: [04.7.1-02, 04.7.1-03]
files_modified:
  - services/audio_service.py
  - routes/app_routes.py
  - scripts/export_ft_jsonl.py
  - tests/test_ft_lifecycle.py
autonomous: true
requirements: [FT-LIFECYCLE, FT-OBJECTION, FT-EXPORT-STUB]
must_haves:
  truths:
    - "Beim Socket-Event start_live_session wird eine ft_call_sessions Row INSERTED und ihre id in ls.state['ft_session_id'] abgelegt"
    - "ls.state['user_id'], ls.state['market'], ls.state['language'] sind ab dem INSERT befüllt (aus User-Lookup)"
    - "api_ewb_trigger schreibt eine ft_objection_events Row mit Markt aus ls.state (abgeleitet von user.market)"
    - "api_beenden updatet die bestehende ft_call_sessions Row mit duration_seconds, hints_shown, hints_used, buttons_pressed, outcome, readiness_score_end"
    - "scripts/export_ft_jsonl.py existiert als CLI-Stub: `python scripts/export_ft_jsonl.py --market dach --out data.jsonl` gibt mindestens eine Zeile pro ft_assistant_event aus (oder eine leere Datei mit exit 0)"
  artifacts:
    - path: "services/audio_service.py"
      provides: "start_live_session Handler erweitert um ft_call_sessions INSERT"
    - path: "routes/app_routes.py"
      provides: "api_ewb_trigger → ft_objection_events write; api_beenden → ft_call_sessions UPDATE"
      contains: "FtObjectionEvent"
    - path: "scripts/export_ft_jsonl.py"
      provides: "CLI-Stub für JSONL-Export (assistant_events), filterbar per --market"
    - path: "tests/test_ft_lifecycle.py"
      provides: "Ende-zu-Ende Lifecycle-Test: start → assistant_event → objection_event → end → export"
  key_links:
    - from: "start_live_session handler"
      to: "ft_call_sessions INSERT"
      via: "db.add(FtCallSession(...)) + ls.state['ft_session_id'] = row.id"
      pattern: "FtCallSession\\("
    - from: "routes/app_routes.py::api_ewb_trigger"
      to: "ft_objection_events"
      via: "db.add(FtObjectionEvent(...))"
      pattern: "FtObjectionEvent\\("
    - from: "routes/app_routes.py::api_beenden"
      to: "ft_call_sessions UPDATE"
      via: "db.query(FtCallSession).filter_by(id=ft_session_id).update(...)"
      pattern: "FtCallSession"
---

<objective>
Lifecycle-Write-Hooks: ft_call_sessions INSERT beim Session-Start (Socket `start_live_session`), UPDATE beim Session-Ende (api_beenden), ft_objection_events INSERT beim EWB-Button-Klick (api_ewb_trigger). Zusätzlich ein JSONL-Export-CLI-Stub für spätere Fine-Tuning-Runs.

Purpose: Ohne diesen Plan gibt es keine ft_session_id → Plan 04 würde nichts schreiben. Dieser Plan schließt den Lifecycle und stellt sicher, dass jeder Call einen vollständigen Datensatz erzeugt.

**Honors user decisions:** §2 (ft_call_sessions INSERT beim Session-Start im Socket-Handler), §4 (conversation_phase default 'unknown'). Plus Hard-Constraint: bestehender ObjectionEvent-Flow (Phase 04.7) bleibt parallel erhalten — FtObjectionEvent ist ein **zusätzlicher** Write, kein Ersatz.

Output: 3 neue Hook-Stellen, 1 Export-CLI-Stub, 1 Lifecycle-Integrationstest.
</objective>

<context>
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-CONTEXT.md
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-RESEARCH.md
@routes/app_routes.py
@services/audio_service.py
@live_session.py
@database/models.py

<interfaces>
Socket-Handler start_live_session (in services/audio_service.py oder wo register_audio_handlers lebt):
- Empfängt sid, Session-Parameter
- Aktuell: initialisiert Deepgram pro sid, setzt live_session state
- Muss erweitert werden: User-ID aus Flask-Session ableiten (Socket-Session → Flask-Session-Cookie), FtCallSession INSERT, id in ls.state['ft_session_id'] ablegen

routes/app_routes.py::api_ewb_trigger:
- Existiert, macht Haiku-Call + loggt ObjectionEvent (Phase 04.7)
- Muss zusätzlich FtObjectionEvent schreiben

routes/app_routes.py::api_beenden:
- Existiert, committed ConversationLog + ObjectionEvent bulk-insert
- Muss zusätzlich: FtCallSession.filter_by(id=ls.state['ft_session_id']).first() → Felder updaten → commit

database/models.py (aus Plan 02):
- FtCallSession, FtObjectionEvent, FtAssistantEvent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ft_call_sessions INSERT im start_live_session Socket-Handler + api_beenden UPDATE</name>
  <read_first>
    - services/audio_service.py (register_audio_handlers, start_live_session Handler — nach Phase 04.1 existiert diese Struktur)
    - routes/app_routes.py::api_beenden (komplett — wo ConversationLog committed wird, danach FtCallSession UPDATE einhängen)
    - live_session.py (state_lock, state-dict Nutzung; set_active_profile; wie user_id derzeit gesetzt wird)
    - database/models.py (FtCallSession Felder)
  </read_first>
  <behavior>
    - start_live_session Handler: liest user_id aus Flask-Session (socketio hat Zugriff via `from flask import session`; falls nicht verfügbar, aus bereits gesetztem ls.state['user_id'] oder aus den Session-Daten, die der bestehende Handler schon nutzt). Liest market+language vom User. INSERT FtCallSession mit mode=session_mode, user_id, market, language. Schreibt row.id + user_id/market/language in ls.state.
    - api_beenden: Nach bestehendem ConversationLog-Commit, vor dem final return, UPDATE auf FtCallSession.filter_by(id=ls.state.get('ft_session_id')).first() — setzt duration_seconds, hints_shown, hints_used, buttons_pressed (aus ls.state/state_counts), readiness_score_end (ls.kaufbereitschaft), outcome, conversation_log_id (auf den eben erzeugten ConversationLog verweisen)
    - Fehler swallow: try/except + print, niemals die bestehenden Flows blockieren
  </behavior>
  <files>services/audio_service.py, routes/app_routes.py</files>
  <action>
    1. **services/audio_service.py** (oder wo auch immer der Handler start_live_session lebt — RESEARCH.md deutet auf `register_audio_handlers` nach Phase 04.1): Im `start_live_session` Handler nach der bestehenden Initialisierung und BEVOR der erste analyse_loop-Tick passieren kann:

       ```python
       # FT logging: create ft_call_sessions row (Phase 04.7.1)
       try:
           from flask import session as flask_session
           from database.db import SessionLocal
           from database.models import FtCallSession, User
           import live_session as ls

           user_id = flask_session.get('user_id')
           if user_id:
               db = SessionLocal()
               try:
                   u = db.query(User).filter_by(id=user_id).first()
                   market = (u.market if u else None) or 'dach'
                   language = (u.language if u else None) or 'de'
                   mode = (data or {}).get('session_mode') or ls.state.get('mode') or 'cold_call'
                   ft_row = FtCallSession(
                       user_id=user_id,
                       mode=mode,
                       market=market,
                       language=language,
                       hints_shown=0,
                       hints_used=0,
                       buttons_pressed=0,
                   )
                   db.add(ft_row)
                   db.commit()
                   ft_session_id = ft_row.id
               finally:
                   db.close()
               with ls.state_lock:
                   ls.state['ft_session_id'] = ft_session_id
                   ls.state['user_id'] = user_id
                   ls.state['market'] = market
                   ls.state['language'] = language
       except Exception as _e:
           print(f"[FT] ft_call_sessions insert failed: {_e}")
       ```

       `data` = das Socket-Event-Payload-Dict (Parameter des Handlers). Variablennamen ggf. an bestehenden Handler anpassen.

    2. **routes/app_routes.py::api_beenden**: Nach dem bestehenden `db.commit()` (der ConversationLog + ObjectionEvent speichert), aber VOR dem `return jsonify(...)`, folgenden Block einfügen:

       ```python
       # FT logging: update ft_call_sessions with aggregates (Phase 04.7.1)
       try:
           import live_session as ls
           from database.models import FtCallSession
           with ls.state_lock:
               ft_session_id = ls.state.get('ft_session_id')
               hints_shown = ls.state.get('hints_shown') or 0
               hints_used = ls.state.get('hints_used') or 0
               buttons_pressed = len(ls.state.get('ewb_clicks') or [])
               readiness_end = ls.kaufbereitschaft if hasattr(ls, 'kaufbereitschaft') else None
               duration = ls.state.get('duration_seconds') or 0
           if ft_session_id:
               ft_row = db_conv.query(FtCallSession).filter_by(id=ft_session_id).first()
               if ft_row:
                   ft_row.duration_seconds = duration
                   ft_row.hints_shown = hints_shown
                   ft_row.hints_used = hints_used
                   ft_row.buttons_pressed = buttons_pressed
                   ft_row.readiness_score_end = readiness_end
                   ft_row.conversation_log_id = conv.id if 'conv' in locals() and conv else None
                   ft_row.outcome = (request.get_json(silent=True) or {}).get('outcome') or 'unknown'
                   db_conv.commit()
               # Clear ft_session_id so next session starts fresh
               with ls.state_lock:
                   ls.state['ft_session_id'] = None
       except Exception as _e:
           print(f"[FT] ft_call_sessions update failed: {_e}")
       ```

       Variablennamen (`db_conv`, `conv`) an bestehenden api_beenden-Kontext anpassen. Falls `db_conv` anders heißt (z.B. einfach `db`), übernehmen. Falls keine `conv`-Variable für ConversationLog existiert, `conversation_log_id=None` lassen.
  </action>
  <verify>
    <automated>grep -q "FtCallSession(" services/audio_service.py &amp;&amp; grep -q "FtCallSession" routes/app_routes.py &amp;&amp; python -c "from app import app; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "FtCallSession(" services/audio_service.py` → exit 0 (Constructor-Call)
    - `grep -q "ls.state\['ft_session_id'\]" services/audio_service.py` → exit 0
    - `grep -q "FtCallSession" routes/app_routes.py` → exit 0
    - `grep -q "ft_row.duration_seconds" routes/app_routes.py` → exit 0
    - `grep -q "ft_row.hints_shown" routes/app_routes.py` → exit 0
    - `grep -q "ft_row.buttons_pressed" routes/app_routes.py` → exit 0
    - Beide Hooks sind in try/except Blöcken
    - `python -c "from app import app"` exit 0
  </acceptance_criteria>
  <done>Socket-Handler INSERTED + api_beenden UPDATED, try/except, App startet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ft_objection_events Write-Hook in api_ewb_trigger + JSONL-Export-CLI-Stub + Lifecycle-Test</name>
  <read_first>
    - routes/app_routes.py::api_ewb_trigger (bestehender Handler — wo Haiku-Call ausgeführt wird, wo bestehender ObjectionEvent-Log greift)
    - database/models.py (FtObjectionEvent Felder)
    - tests/conftest.py (db_session Fixture)
  </read_first>
  <behavior>
    - Nach erfolgreichem Haiku-Response in api_ewb_trigger: zusätzliche FtObjectionEvent Row mit objection_type, recommended_response, model_used, prompt_version, market/language aus ls.state
    - scripts/export_ft_jsonl.py: Liest ft_assistant_events gefiltert nach --market, schreibt eine JSONL-Datei (je Row ein JSON-Objekt mit id, ft_session_id, hint_type, hint_text, model_used, prompt_version, created_at). Bei leerer Tabelle: erzeugt leere Datei, exit 0.
    - Lifecycle-Test in tests/test_ft_lifecycle.py: seed User + FtCallSession manuell, prüft dass FtObjectionEvent-Row ingeschrieben werden kann und der Export-CLI sie ausliest.
  </behavior>
  <files>routes/app_routes.py, scripts/export_ft_jsonl.py, tests/test_ft_lifecycle.py</files>
  <action>
    1. **routes/app_routes.py::api_ewb_trigger**: Nach dem bestehenden Haiku-Call und vor dem return jsonify, folgenden Block einfügen (innerhalb eines try/except, wie die anderen FT-Hooks):

       ```python
       # FT logging: ft_objection_events (Phase 04.7.1)
       try:
           import time
           import live_session as ls
           from services.claude_service import get_active_prompt_version
           from database.models import FtObjectionEvent
           with ls.state_lock:
               ft_session_id = ls.state.get('ft_session_id')
               user_id = ls.state.get('user_id')
               market = ls.state.get('market') or 'dach'
               language = ls.state.get('language') or 'de'
               readiness_before = ls.kaufbereitschaft if hasattr(ls, 'kaufbereitschaft') else None
           if ft_session_id and user_id:
               db_conv.add(FtObjectionEvent(
                   ft_session_id=ft_session_id,
                   user_id=user_id,
                   market=market,
                   language=language,
                   timestamp_ms=int(time.time() * 1000),
                   objection_type=einwand_typ or 'unknown',
                   conversation_phase='unknown',
                   readiness_score_before=readiness_before,
                   recommended_response=(antwort_text if 'antwort_text' in locals() else None),
                   recommendation_used=False,
                   model_used='claude-haiku-4-6',  # EXAKT wie im Haiku-Call oben im Handler
                   prompt_version=get_active_prompt_version('objection_trigger'),
               ))
               db_conv.commit()
       except Exception as _e:
           print(f"[FT] ft_objection_events insert failed: {_e}")
       ```

       Variablennamen (`db_conv`, `einwand_typ`, `antwort_text`) an bestehenden Handler anpassen. `model_used` Literal EXAKT mit dem Modell-String aus dem Haiku-Call in derselben Route matchen.

    2. **scripts/export_ft_jsonl.py** neu anlegen:

       ```python
       """
       Phase 04.7.1 JSONL-Export CLI-Stub.

       Usage:
           python scripts/export_ft_jsonl.py --market dach --out data.jsonl

       Exports ft_assistant_events as JSONL, one row per line.
       Full anonymization + quality filter is a later phase (see CONTEXT.md deferred ideas).
       """
       import argparse
       import json
       import sys


       def main():
           parser = argparse.ArgumentParser(description="Export ft_assistant_events to JSONL")
           parser.add_argument("--market", default="dach", help="Filter by market (dach|us)")
           parser.add_argument("--out", required=True, help="Output JSONL file path")
           parser.add_argument("--table", default="assistant", choices=["assistant", "objection"])
           args = parser.parse_args()

           from database.db import SessionLocal
           from database.models import FtAssistantEvent, FtObjectionEvent

           Model = FtAssistantEvent if args.table == "assistant" else FtObjectionEvent

           db = SessionLocal()
           count = 0
           try:
               rows = db.query(Model).filter_by(market=args.market).all()
               with open(args.out, "w", encoding="utf-8") as f:
                   for r in rows:
                       record = {c.name: getattr(r, c.name) for c in r.__table__.columns}
                       # datetime → isoformat
                       for k, v in list(record.items()):
                           if hasattr(v, "isoformat"):
                               record[k] = v.isoformat()
                       f.write(json.dumps(record, ensure_ascii=False, default=str) + "\n")
                       count += 1
           finally:
               db.close()

           print(f"[export] wrote {count} rows to {args.out} (market={args.market}, table={args.table})")
           return 0


       if __name__ == "__main__":
           sys.exit(main())
       ```

       Ordner `scripts/` anlegen falls nicht vorhanden.

    3. **tests/test_ft_lifecycle.py** neu anlegen:

       ```python
       import json
       import os
       import tempfile
       import pytest


       def test_ft_objection_insert_and_export(db_session, monkeypatch, tmp_path):
           from database.models import User, FtCallSession, FtObjectionEvent, FtAssistantEvent, PromptVersion

           # Seed
           u = User(email="t@t.de", passwort_hash="x", market="dach", language="de")
           db_session.add(u); db_session.flush()
           sess = FtCallSession(user_id=u.id, mode="cold_call", market="dach", language="de")
           db_session.add(sess); db_session.flush()
           db_session.add(FtAssistantEvent(
               ft_session_id=sess.id, user_id=u.id, market="dach", language="de",
               timestamp_ms=1, conversation_phase="unknown",
               hint_type="objection", hint_text="test hint",
               model_used="claude-haiku-4-6", prompt_version="v1.0.0",
           ))
           db_session.add(FtObjectionEvent(
               ft_session_id=sess.id, user_id=u.id, market="dach", language="de",
               timestamp_ms=1, objection_type="kein_bedarf",
               recommended_response="Antwort",
               model_used="claude-haiku-4-6", prompt_version="v1.0.0",
           ))
           db_session.commit()

           # Sanity
           assert db_session.query(FtObjectionEvent).count() == 1
           assert db_session.query(FtAssistantEvent).count() == 1

           # Export via script (mock SessionLocal to return db_session-backed fake)
           import sys
           out_file = tmp_path / "out.jsonl"

           from database import db as dbmod

           class _Fake:
               def __init__(self, real): self._real = real
               def query(self, m): return self._real.query(m)
               def close(self): pass

           monkeypatch.setattr(dbmod, "SessionLocal", lambda: _Fake(db_session))

           sys.argv = ["export_ft_jsonl.py", "--market", "dach", "--out", str(out_file), "--table", "assistant"]
           from scripts.export_ft_jsonl import main
           rc = main()
           assert rc == 0
           assert out_file.exists()
           lines = out_file.read_text(encoding="utf-8").strip().splitlines()
           assert len(lines) == 1
           rec = json.loads(lines[0])
           assert rec["market"] == "dach"
           assert rec["hint_type"] == "objection"
       ```

       Falls `scripts/__init__.py` nötig ist (damit der Import `from scripts.export_ft_jsonl import main` funktioniert): leere Datei `scripts/__init__.py` mit anlegen.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ft_lifecycle.py -x -v &amp;&amp; python scripts/export_ft_jsonl.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "FtObjectionEvent(" routes/app_routes.py` → exit 0
    - `grep -q "get_active_prompt_version('objection_trigger')" routes/app_routes.py` → exit 0
    - `test -f scripts/export_ft_jsonl.py` → exit 0
    - `grep -q "argparse" scripts/export_ft_jsonl.py` → exit 0
    - `grep -q "FtAssistantEvent" scripts/export_ft_jsonl.py` → exit 0
    - `python scripts/export_ft_jsonl.py --help` exit 0
    - `test -f tests/test_ft_lifecycle.py` → exit 0
    - `python -m pytest tests/test_ft_lifecycle.py -q` → grün
    - `python -m pytest tests/ -q` komplette Test-Suite grün
  </acceptance_criteria>
  <done>FtObjectionEvent-Hook in api_ewb_trigger, Export-Stub lauffähig, Lifecycle-Test grün, Gesamt-Suite grün.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` grün (alle Tests inklusive neuer)
- `python scripts/export_ft_jsonl.py --market dach --out /tmp/test.jsonl` läuft fehlerfrei (Datei wird erzeugt)
- `python -c "from app import app"` keine Regression
- Manueller Smoke: Live-Session starten → EWB-Button klicken → Session beenden → `sqlite3 database/nerve.db "SELECT count(*) FROM ft_call_sessions; SELECT count(*) FROM ft_objection_events"` beide >0
</verification>

<success_criteria>
(a) 4 Tabellen existieren [Plan 02]
(b) 6 prompt_versions seeded [Plan 03]
(c) Live-Events werden geloggt [Plan 04 + dieser]
(d) Markt-Flag DACH/US wird beim Session-Start gesetzt [dieser]
(e) Cold-Call: transcript_segment IS NULL hart enforce'd [Plan 04]
(f) JSONL-Export-CLI-Stub lieferbar [dieser]
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Socket.IO start_live_session | Client liefert session_mode; user_id kommt aus Flask-Session (server-side) |
| Browser → api_ewb_trigger | Client liefert einwand_typ; Server führt Claude-Call aus |
| CLI → SQLite | Export-Script liest lokal; nur Entwickler/Superadmin mit Shell-Zugriff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.7.1-01 | I (Information Disclosure) | ft_assistant_events.transcript_segment im Cold-Call | mitigate | Hard-Enforce NULL im _write_ft_assistant_event helper (Plan 04) — nicht nur Schema-nullable |
| T-04.7.1-02 | T (Tampering) | Client setzt session_mode='meeting' um Diarization-Daten zu loggen | accept | Mode-Wahl ist UX-Entscheidung; bestehende Phase 04.2 Consent-Flow bleibt Pflicht |
| T-04.7.1-03 | I | Export-JSONL enthält Rohdaten (inkl. Meeting-Transkripte) | mitigate | Export-Script nur lokal lauffähig (scripts/), kein HTTP-Endpoint; Anonymisierung in späterer Phase (D-02) |
| T-04.7.1-04 | E (EoP) | Injection via hint_text/objection_type in DB | accept | SQLAlchemy ORM parametrisiert; keine String-Concat |
| T-04.7.1-05 | I | prompt_versions.prompt_text in DB enthält interne Logik | accept | Prompts enthalten generische Templates ohne Kundendaten (D-09) |
</threat_model>

<output>
`.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-05-SUMMARY.md`
</output>
