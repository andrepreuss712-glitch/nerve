---
phase: 04.7.1
plan: 02
subsystem: database
tags: [sqlalchemy, ft-logging, schema, migration, market]
requires:
  - "pytest framework + db_session fixture (Plan 01)"
provides:
  - "FtCallSession, FtAssistantEvent, FtObjectionEvent, PromptVersion models"
  - "users.market + users.language columns (default dach/de)"
  - "conversation_logs.market + conversation_logs.language columns (default dach/de)"
  - "prompt_versions UNIQUE(version, module) constraint"
affects:
  - database/models.py
  - app.py
tech_stack:
  added: []
  patterns:
    - "Nullable transcript_segment/speaker (D-04/D-05)"
    - "ALTER TABLE idempotent try/except in _migrate()"
    - "UniqueConstraint via __table_args__"
key_files:
  created: []
  modified:
    - database/models.py
    - app.py
    - tests/test_ft_models.py
decisions:
  - "transcript_segment + speaker NULLABLE (D-04/D-05) — Cold-Call darf ehrlich NULL speichern"
  - "active_learning_cards als Text/JSON ohne FK (D-11) — learning_cards Tabelle existiert noch nicht"
  - "PromptVersion.__table_args__ UniqueConstraint(version, module) statt separates CREATE INDEX in _migrate"
  - "market/language-Columns an User/ConversationLog angehängt; Migration setzt NOT NULL DEFAULT 'dach'/'de' für bestehende Rows"
metrics:
  duration: "~4 min"
  completed: "2026-04-08"
  tasks: 2
  files: 3
requirements: [FT-SCHEMA]
---

# Phase 04.7.1 Plan 02: SQLAlchemy FT-Models + _migrate() ALTER Summary

Vier neue FineTuning-Logging-Models (`FtCallSession`, `FtAssistantEvent`, `FtObjectionEvent`, `PromptVersion`) plus Markt-Spalten (`market`, `language`) auf `users` und `conversation_logs` — das Datenbank-Fundament für alle Write-Hooks ab Plan 04.

## What Was Built

**Task 1 — Models + User/ConversationLog-Extension:** Vier Model-Klassen am Ende von `database/models.py` angehängt. `FtCallSession` mit nullable FK auf `conversation_logs.id` und NOT NULL FK auf `users.id`; `FtAssistantEvent` und `FtObjectionEvent` mit FK auf `ft_call_sessions.id`. `transcript_segment` und `speaker` sind explizit nullable (D-04/D-05 — Cold-Call ehrlich NULL). `active_learning_cards` ist Text/JSON ohne FK (D-11). `PromptVersion` hat `__table_args__ = (UniqueConstraint('version', 'module'),)`. An `User` und `ConversationLog` wurden je `market`/`language` Columns (String(10), NOT NULL, default 'dach'/'de') angehängt. `tests/test_ft_models.py` entfernt die `pytest.skip`-Aufrufe und aktiviert einen vollen Insert-Flow für `test_transcript_nullable` (Organisation → User → FtCallSession → FtAssistantEvent mit NULL speaker/transcript). Commit: `e70beef`.

**Task 2 — _migrate() ALTER TABLE Blöcke:** In `app.py::_migrate()` je zwei Einträge an die bestehenden `users`- und `conversation_logs`-Schleifen angehängt: `('market', "VARCHAR(10) NOT NULL DEFAULT 'dach'")` und `('language', "VARCHAR(10) NOT NULL DEFAULT 'de'")`. Idempotent via bestehendes try/except-Pattern. Commit: `e53ba14`.

## Verification

```
$ python -m pytest tests/test_ft_models.py -q
...                                                                      [100%]
3 passed in 0.04s

$ python -c "from database.models import FtCallSession, FtAssistantEvent, FtObjectionEvent, PromptVersion"
(ok)

$ python -c "from app import app; print('app imports ok')"
[DB] Migration: added conversation_logs.market
[DB] Migration: added conversation_logs.language
...
app imports ok
```

Live-DB Migration ist beim Import bereits durchgelaufen — die beiden `conversation_logs`-Spalten wurden auf der Dev-DB angelegt; `users.market`/`language` existierten auf dieser DB-Instanz bereits aus einem früheren Lauf (try/except idempotent).

Acceptance-Criteria Greps:
- `class FtCallSession` / `FtAssistantEvent` / `FtObjectionEvent` / `PromptVersion` in models.py ok
- `__tablename__ = 'ft_call_sessions'` / `'ft_assistant_events'` / `'ft_objection_events'` / `'prompt_versions'` ok
- `transcript_segment = Column(Text, nullable=True)` ok
- `ForeignKey('conversation_logs.id')` + `ForeignKey('ft_call_sessions.id')` ok
- market-Columns: User, ConversationLog, FtCallSession, FtAssistantEvent, FtObjectionEvent (5) ok
- `ALTER TABLE users ADD COLUMN` + `ALTER TABLE conversation_logs ADD COLUMN` + `DEFAULT 'dach'` in app.py ok

## Deviations from Plan

None — Plan wurde exakt wie spezifiziert ausgeführt. Die existierende `users`-Migrations-Schleife war vorhanden, also wurden nur die zwei Einträge angehängt (kein neuer Block nötig). `conversation_logs`-Schleife existierte ebenfalls bereits (session_mode, result), Einträge angehängt.

## Known Stubs

None. Models sind vollständig produktiv nutzbar. Write-Hooks folgen in Plan 04+05.

## Commits

| Task | Commit  | Description |
| ---- | ------- | ----------- |
| 1    | e70beef | FT models + User/ConversationLog market/language + aktivierte Tests |
| 2    | e53ba14 | _migrate() ALTER TABLE market/language auf users + conversation_logs |

## Self-Check: PASSED

- FOUND: database/models.py (4 classes present)
- FOUND: app.py (ALTER TABLE blocks present)
- FOUND: tests/test_ft_models.py (skips removed)
- FOUND commit: e70beef
- FOUND commit: e53ba14
- `pytest tests/test_ft_models.py -q` → 3 passed
- `from database.models import FtCallSession, FtAssistantEvent, FtObjectionEvent, PromptVersion` → ok
- `from app import app` → ok, migrations executed without errors
