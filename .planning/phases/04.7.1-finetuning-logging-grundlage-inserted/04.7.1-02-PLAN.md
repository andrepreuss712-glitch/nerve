---
phase: 04.7.1
plan: 02
type: execute
wave: 1
depends_on: [04.7.1-01]
files_modified:
  - database/models.py
  - app.py
  - tests/test_ft_models.py
autonomous: true
requirements: [FT-SCHEMA]
must_haves:
  truths:
    - "Die 4 Tabellen ft_call_sessions, ft_assistant_events, ft_objection_events, prompt_versions existieren nach App-Start"
    - "users.market und users.language Spalten existieren mit Default 'dach' / 'de'"
    - "conversation_logs.market und conversation_logs.language Spalten existieren mit Default 'dach' / 'de'"
    - "ft_assistant_events.transcript_segment ist nullable (D-04/D-05)"
    - "Alle bestehenden User erben market='dach' via ALTER TABLE DEFAULT"
  artifacts:
    - path: "database/models.py"
      provides: "SQLAlchemy Models: FtCallSession, FtAssistantEvent, FtObjectionEvent, PromptVersion"
      contains: "class FtCallSession"
    - path: "app.py"
      provides: "_migrate() Erweiterung für users/conversation_logs market+language ALTERs"
      contains: "'market'"
  key_links:
    - from: "database/models.py"
      to: "conversation_logs.id"
      via: "FtCallSession.conversation_log_id ForeignKey"
      pattern: "ForeignKey\\('conversation_logs\\.id'\\)"
    - from: "database/models.py"
      to: "users.id"
      via: "FtCallSession/FtAssistantEvent/FtObjectionEvent.user_id ForeignKey"
      pattern: "ForeignKey\\('users\\.id'\\)"
    - from: "app.py::_migrate"
      to: "users table"
      via: "ALTER TABLE ADD COLUMN market/language"
      pattern: "ALTER TABLE users ADD COLUMN market"
---

<objective>
SQLAlchemy Models für die 4 FineTuning-Logging-Tabellen anlegen und Markt-Spalten auf users + conversation_logs migrieren.

Purpose: Datenbank-Fundament für alle Write-Hooks in Plan 04+05. Muss PG-kompatibel sein (JSON via Text, explizite FKs) und das bestehende `_migrate()`-Pattern respektieren (kein Alembic).

Output: 4 neue Model-Klassen am Ende von database/models.py, _migrate()-Erweiterung in app.py für 4 ALTER TABLE Calls, test_ft_models.py-Skips werden entfernt und Tests laufen grün.
</objective>

<context>
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-CONTEXT.md
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-RESEARCH.md
@database/models.py
@app.py

<interfaces>
Aus database/models.py (bestehende Conventions):
- utcnow — timezone-naive UTC helper (module-level)
- Base — declarative base from database.db
- Column-Typen: Integer, String(N), Text, Float, Boolean, DateTime, ForeignKey
- Pattern: Column(DateTime, default=utcnow)
- User model hat schon: id, email, passwort_hash — market/language müssen als Column hinzugefügt werden
- ConversationLog model: id als FK-Ziel; erhält market/language analog
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 SQLAlchemy Models am Ende von database/models.py anhängen</name>
  <read_first>
    - database/models.py (komplett — utcnow, Base-Import, User/ConversationLog, vorhandene Column-Patterns)
    - .planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-RESEARCH.md §"Code Examples" (exakte Column-Liste)
  </read_first>
  <behavior>
    - test_tables_created läuft grün (keine Skip mehr): alle 4 Tabellen in Base.metadata.tables
    - test_transcript_nullable: insert von FtAssistantEvent mit transcript_segment=None + speaker=None erfolgreich
    - Models importierbar: `from database.models import FtCallSession, FtAssistantEvent, FtObjectionEvent, PromptVersion`
  </behavior>
  <files>database/models.py, tests/test_ft_models.py</files>
  <action>
    1. In `database/models.py` am Ende (nach allen bestehenden Models) die 4 Klassen exakt wie in RESEARCH.md §"Code Examples" (Zeile 206-299) anhängen — inklusive:
       - `FtCallSession` mit FK `conversation_log_id → conversation_logs.id` (nullable=True per D-11), FK `user_id → users.id` (nullable=False), Felder: mode, duration_seconds, market (default 'dach'), language (default 'de'), customer_industry/position/company_size, phases_completed (Text/JSON), talk_ratio_rep/customer (Float), readiness_score_start/end/peak, hints_shown/used (default=0), buttons_pressed (default=0), outcome, user_rating, user_feedback, model_used, prompt_version, created_at (default=utcnow)
       - `FtAssistantEvent` mit FK `ft_session_id → ft_call_sessions.id` (nullable=False), user_id FK, market+language, timestamp_ms (NOT NULL), conversation_phase (String(50) NOT NULL — Default-String auf Write-Seite 'unknown'), speaker (nullable — D-04), transcript_segment (Text, nullable — D-05), context_window (Text, nullable), customer_data (Text nullable), profile_data (Text nullable), readiness_score (Integer nullable), active_learning_cards (Text nullable — kein FK, D-11), hint_type (String(50) NOT NULL), hint_text (Text NOT NULL), hint_category, model_used (NOT NULL), prompt_version (NOT NULL), hint_action, score_change, call_rating, call_outcome, created_at
       - `FtObjectionEvent` mit FK `ft_session_id → ft_call_sessions.id` (nullable=False), user_id FK, market+language, timestamp_ms NOT NULL, objection_type (String(100) NOT NULL), conversation_phase, readiness_score_before, context_window (nullable), customer_data (nullable), ki_classification, ki_recommendation (Text), recommended_response (Text), recommendation_used (Boolean default=False), readiness_score_after, objection_resolved (Boolean), call_outcome, model_used NOT NULL, prompt_version NOT NULL, created_at
       - `PromptVersion` mit id, version (String(50) NOT NULL), module (String(50) NOT NULL), prompt_text (Text NOT NULL), changelog (Text), is_active (Boolean default=False NOT NULL), created_at. Zusätzlich `UniqueConstraint('version', 'module', name='uq_prompt_version_module')` im __table_args__.

    2. In `database/models.py` am User-Model die beiden neuen Spalten hinzufügen (NACH dem bestehenden letzten User-Feld, VOR `__tablename__` Ende):
       ```python
       market   = Column(String(10), nullable=False, default='dach')
       language = Column(String(10), nullable=False, default='de')
       ```
       Analog bei `ConversationLog` dieselben zwei Column-Definitionen anhängen.

    3. In `tests/test_ft_models.py`:
       - Den `pytest.skip` in `test_tables_created` entfernen (der `if missing`-Block verschwindet, direct `assert required.issubset(tables)`).
       - In `test_transcript_nullable` den Skip entfernen und einen minimalen Insert-Flow ergänzen: User → FtCallSession → FtAssistantEvent mit transcript_segment=None, speaker=None, hint_type='objection', hint_text='x', conversation_phase='unknown', timestamp_ms=0, model_used='claude-haiku', prompt_version='v1.0.0'. Commit + query-back + assert transcript_segment is None.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ft_models.py::test_tables_created tests/test_ft_models.py::test_transcript_nullable -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class FtCallSession" database/models.py` → exit 0
    - `grep -q "class FtAssistantEvent" database/models.py` → exit 0
    - `grep -q "class FtObjectionEvent" database/models.py` → exit 0
    - `grep -q "class PromptVersion" database/models.py` → exit 0
    - `grep -q "__tablename__ = 'ft_call_sessions'" database/models.py` → exit 0
    - `grep -q "__tablename__ = 'ft_assistant_events'" database/models.py` → exit 0
    - `grep -q "__tablename__ = 'ft_objection_events'" database/models.py` → exit 0
    - `grep -q "__tablename__ = 'prompt_versions'" database/models.py` → exit 0
    - `grep -q "transcript_segment = Column(Text, nullable=True)" database/models.py` → exit 0
    - `grep -q "ForeignKey('conversation_logs.id')" database/models.py` → exit 0
    - `grep -q "ForeignKey('ft_call_sessions.id')" database/models.py` → exit 0
    - `grep -c "market\s*=\s*Column" database/models.py` ≥ 5 (User, ConversationLog, FtCallSession, FtAssistantEvent, FtObjectionEvent)
    - `python -m pytest tests/test_ft_models.py::test_tables_created -x` → grün
    - `python -m pytest tests/test_ft_models.py::test_transcript_nullable -x` → grün
  </acceptance_criteria>
  <done>4 Model-Klassen im File, User+ConversationLog erweitert, 2 Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _migrate() um ALTER TABLE für market/language auf users + conversation_logs erweitern</name>
  <read_first>
    - app.py (komplett lesen oder zumindest `_migrate` Funktion finden — existierender For-Loop mit (col, typedef) pro Tabelle)
    - tests/test_ft_models.py::test_user_market_default
  </read_first>
  <behavior>
    - Bestehende SQLite-DB ohne market/language Spalten wird beim App-Start idempotent migriert
    - Zweiter Start löst keine Fehler aus (ALTER TABLE IF NOT EXISTS via try/except pass)
    - test_user_market_default läuft grün ohne Skip
  </behavior>
  <files>app.py, tests/test_ft_models.py</files>
  <action>
    1. In `app.py::_migrate()` die bestehenden `users`-Spalten-Schleife finden (For-Loop über Liste von Tuples `(col, typedef)`). Folgende zwei Einträge anhängen:
       ```python
       ('market',   "VARCHAR(10) NOT NULL DEFAULT 'dach'"),
       ('language', "VARCHAR(10) NOT NULL DEFAULT 'de'"),
       ```
       Falls keine `users`-Migrations-Schleife existiert (alle auf einer anderen Tabelle): einen neuen Block im identischen Pattern anlegen:
       ```python
       for col, typedef in [
           ('market',   "VARCHAR(10) NOT NULL DEFAULT 'dach'"),
           ('language', "VARCHAR(10) NOT NULL DEFAULT 'de'"),
       ]:
           try:
               conn.execute(text(f'ALTER TABLE users ADD COLUMN {col} {typedef}'))
               conn.commit()
               print(f"[DB] Migration: added users.{col}")
           except Exception:
               pass
       ```
    2. Identischen Block für `conversation_logs`:
       ```python
       for col, typedef in [
           ('market',   "VARCHAR(10) NOT NULL DEFAULT 'dach'"),
           ('language', "VARCHAR(10) NOT NULL DEFAULT 'de'"),
       ]:
           try:
               conn.execute(text(f'ALTER TABLE conversation_logs ADD COLUMN {col} {typedef}'))
               conn.commit()
               print(f"[DB] Migration: added conversation_logs.{col}")
           except Exception:
               pass
       ```
    3. Wichtig: `init_db(engine)` wird in app.py VOR `_migrate()` aufgerufen — das ist ok, da create_all die 4 neuen Tabellen bereits anlegt, bevor die ALTER TABLE Calls laufen. Keine Reihenfolge-Änderung nötig.

    4. `tests/test_ft_models.py::test_user_market_default` Skip-Block entfernen (der `if not hasattr(User, "market")`-Check fällt weg, direkter Flow bleibt).
  </action>
  <verify>
    <automated>python -m pytest tests/test_ft_models.py -x -v &amp;&amp; python -c "from app import app; print('app imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ALTER TABLE users ADD COLUMN" app.py` → exit 0
    - `grep -q "ALTER TABLE conversation_logs ADD COLUMN" app.py` → exit 0
    - `grep -c "'market'" app.py` ≥ 2
    - `grep -c "'language'" app.py` ≥ 2
    - `grep -q "DEFAULT 'dach'" app.py` → exit 0
    - `python -m pytest tests/test_ft_models.py -q` exit 0 (alle 3 Tests grün, kein Skip mehr)
    - `python -c "from app import app" ` exit 0 (keine Import-Errors)
  </acceptance_criteria>
  <done>Beide ALTER-Blöcke in _migrate(), alle test_ft_models.py Tests grün, app.py importiert sauber.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ft_models.py -q` grün (3 Tests)
- `python -c "from database.models import FtCallSession, FtAssistantEvent, FtObjectionEvent, PromptVersion"` exit 0
- `python -c "from app import app"` exit 0 — _migrate() läuft ohne Fehler auf existierender DB
</verification>

<success_criteria>
4 Models vorhanden, 4 neue Spalten auf users+conversation_logs migriert, 3 Schema-Tests grün, App startet sauber.
</success_criteria>

<output>
`.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-02-SUMMARY.md`
</output>
