---
phase: 04.7.1-finetuning-logging-grundlage
verified: 2026-04-08T00:00:00Z
status: passed
score: 8/8 must-haves verified
---

# Phase 04.7.1: FineTuning Logging Grundlage — Verification Report

**Phase Goal:** Strukturierte Logging-Tabellen fuer Fine-Tuning Trainingsdaten von Tag 1 — ft_call_sessions, ft_assistant_events, ft_objection_events, prompt_versions. Markt-Trennung DACH/US, Anonymisierung, JSONL-Export.

**Verified:** 2026-04-08
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Must-Haves

| # | Must-Have | Status | Evidence |
|---|-----------|--------|----------|
| a | 4 Tabellen (ft_call_sessions, ft_assistant_events, ft_objection_events, prompt_versions) | PASS | `database/models.py` L337 `FtCallSession`, L366 `FtAssistantEvent`, L394 `FtObjectionEvent`, L419 `PromptVersion` — alle Base-Mapper mit korrekten `__tablename__` |
| b | 6 Prompt-Module v1.0.0 geseedet (keine Placeholder) | PASS | `app.py` L236-279 `_seed_prompt_versions()` importiert echte Basis-Prompts: `SYSTEM_PROMPT_BASE`, `COACHING_PROMPT_BASE`, `EWB_RANKING_PROMPT_BASE`, `OBJECTION_TRIGGER_PROMPT_BASE`, `API_FRAGE_PROMPT_BASE`, `TRAINING_PERSONA_PROMPT_BASE` — keine String-Stubs. Seed idempotent, v1.0.0, is_active=True. `test_ft_seed.py` prueft `len(prompt_text) > 30` und alle 6 Module, erwartet `count == 6`, passed. |
| c | Live-Events werden geloggt (assistant_live, coaching_live, objection_events) | PASS | `services/claude_service.py` L117/149 `_write_ft_assistant_event` mit `FtAssistantEvent` (analyse_loop), L732 `module='coaching_live'` Hook (coaching_loop), `routes/app_routes.py` L667-699 `FtObjectionEvent` insert im `api_ewb_trigger`-Endpoint |
| d | Markt-Flag DACH/US auf User + ft_call_sessions, gefuellt beim Session-Start | PASS | `models.py` L115 `User.market`, L344 `FtCallSession.market`, L371 `FtAssistantEvent.market`, L399 `FtObjectionEvent.market` — alle `nullable=False, default='dach'`. `services/deepgram_service.py` L177-187 liest `User.market` und schreibt es in neue `FtCallSession` beim Session-Start |
| e | Cold-Call: transcript_segment hart NULL im Write-Hook | PASS | `test_ft_write_hooks.py::test_cold_call_forces_transcript_null` uebergibt `'SHOULD BE DROPPED BY DSGVO'` und asserted `row.transcript_segment is None` + `row.speaker is None`. Meeting-Mode-Gegentest `test_meeting_mode_preserves_transcript` beweist, dass NULL-Enforcement mode-gated im Write-Hook sitzt, nicht nur im Schema (Column ist `nullable=True` auf L376). Beide Tests passed. |
| f | JSONL-Export-CLI scripts/export_ft_jsonl.py mit --market und --table | PASS | `scripts/export_ft_jsonl.py` vorhanden, argparse mit `--market` (default dach), `--out` (required), `--table` (choices `assistant`/`objection`). Waehlt Model via `FtAssistantEvent if args.table == "assistant" else FtObjectionEvent`, filtert `filter_by(market=args.market)`, schreibt JSONL zeilenweise mit datetime-isoformat-Handling |
| g | Tests gruen (pytest tests/, erwartet 10 passed) | PASS | `python -m pytest tests/test_ft_models.py tests/test_ft_seed.py tests/test_ft_write_hooks.py tests/test_ft_lifecycle.py -q` → `10 passed in 1.51s` |
| h | FK heisst conversation_log_id, kein Alembic, _migrate() Pattern | PASS | `models.py` L340 `FtCallSession.conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id'), nullable=True)`. `app.py` L47 `_migrate()` Funktion, L233 direkter Aufruf. Kein Alembic im Repo (nur `init_db` → `Base.metadata.create_all`). Projekt-Konvention eingehalten. |

**Score:** 8/8 must-haves verified

### Key Link Verification

| From | To | Via | Status |
|------|-----|-----|--------|
| deepgram_service session start | ft_call_sessions | `FtCallSession(... market=u.market)` L177-187 | WIRED |
| claude_service analyse_loop | ft_assistant_events (module=assistant_live) | `_write_ft_assistant_event` L117/149 | WIRED |
| claude_service coaching_loop | ft_assistant_events (module=coaching_live) | `_write_ft_assistant_event(module='coaching_live')` L732 | WIRED |
| app_routes api_ewb_trigger | ft_objection_events | `FtObjectionEvent` insert L681 | WIRED |
| claude_service prompt load | prompt_versions | `PromptVersion.filter_by(module=..., is_active=True)` L84-87 | WIRED |
| export_ft_jsonl CLI | Ft*Event models | SessionLocal + query filter_by market | WIRED |

### Behavioural Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| FT-Tests laufen durch | `pytest tests/test_ft_*.py -q` | `10 passed in 1.51s` | PASS |
| Cold-Call drop-through | `test_cold_call_forces_transcript_null` | assertion hold | PASS |
| Seed-Idempotenz | `test_seed_idempotent` (count == 6 zweimal) | PASS | PASS |

### Anti-Patterns Scan

Keine Blocker. Prompt-Seed verwendet echte Basis-Strings aus `claude_service`, `app_routes`, `training_service` — keine `"TODO"`/`"placeholder"` Strings gefunden. Write-Hooks haben `try/except` mit `print("[FT] ... failed")` (Projekt-Konvention, kein Blocker — Logging darf Live-Loop nicht crashen).

### Gaps Summary

Keine Gaps. Alle 8 vom User spezifizierten Must-Haves sind implementiert, verdrahtet und testabgedeckt. Die DSGVO-kritische Cold-Call-NULL-Enforcement ist explizit im Write-Hook (nicht nur Schema) durch einen dedizierten Negativtest belegt. Markt-Trennung greift End-to-End von `User.market` bis in alle vier FT-Tabellen. JSONL-Export-CLI erfuellt den spezifizierten Scope (`--market`, `--table`), fortgeschrittene Anonymisierung ist laut CLI-Docstring bewusst deferred.

**Verdict: PASSED** — Phase 04.7.1 liefert das Phase-Ziel vollstaendig ab. Ready to proceed.

---

_Verified: 2026-04-08_
_Verifier: Claude (gsd-verifier)_
