---
phase: 04.7.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_ft_models.py
  - tests/test_ft_seed.py
autonomous: true
requirements: [FT-SETUP]
must_haves:
  truths:
    - "pytest ist installiert und ausführbar"
    - "tests/conftest.py stellt eine in-memory SQLite Fixture bereit"
    - "Test-Scaffolds für Models und Seed existieren (initial als skip/xfail, bis Wave 1+2 sie grün machen)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "pytest fixture 'db_session' mit in-memory SQLite + Base.metadata.create_all"
    - path: "tests/test_ft_models.py"
      provides: "Schema-Smoke-Test-Scaffolds für ft_* Tabellen"
    - path: "tests/test_ft_seed.py"
      provides: "Seed-Idempotenz-Test-Scaffolds"
  key_links:
    - from: "tests/conftest.py"
      to: "database/db.Base"
      via: "Base.metadata.create_all(engine)"
      pattern: "Base\\.metadata\\.create_all"
---

<objective>
Wave 0: pytest einführen und Test-Scaffolds für die FineTuning-Logging-Phase erstellen.

Purpose: pytest existiert nicht im Repo. Alle nachfolgenden Plans brauchen automatisierte Verifikation (`<automated>`-Commands in `<verify>`). Diese Phase legt Framework + leere Test-Dateien an, die Wave 1+2 dann mit Assertions füllen.

Output: requirements.txt mit pytest, tests/-Verzeichnis mit conftest.py und zwei Test-Modulen (Scaffolds mit `pytest.skip(...)` oder passing placeholders, damit `pytest -q` grün läuft).
</objective>

<context>
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-CONTEXT.md
@.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-RESEARCH.md
@requirements.txt
@database/db.py
@database/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest installieren und tests/-Paket anlegen</name>
  <read_first>
    - requirements.txt (aktuelle Dependencies prüfen, pytest darf nicht doppelt auftauchen)
    - database/db.py (Base, SessionLocal, engine — Pattern für in-memory Fixture)
  </read_first>
  <files>requirements.txt, tests/__init__.py, tests/conftest.py</files>
  <action>
    1. In `requirements.txt` am Ende eine neue Zeile anhängen: `pytest>=8.0.0`
    2. `tests/__init__.py` als leere Datei anlegen (nur damit tests/ ein Paket ist).
    3. `tests/conftest.py` mit folgender in-memory SQLite Fixture erstellen:

    ```python
    import pytest
    from sqlalchemy import create_engine
    from sqlalchemy.orm import sessionmaker
    from database.db import Base
    # Import all models so Base.metadata knows about them
    import database.models  # noqa: F401

    @pytest.fixture
    def db_session():
        engine = create_engine("sqlite:///:memory:")
        Base.metadata.create_all(engine)
        Session = sessionmaker(bind=engine)
        session = Session()
        try:
            yield session
        finally:
            session.close()
            engine.dispose()
    ```

    4. Lokal `pip install pytest` ausführen (dev-Installation — Produktions-Deploy zieht es aus requirements.txt nach).
  </action>
  <verify>
    <automated>python -c "import pytest; print(pytest.__version__)" &amp;&amp; python -c "from tests.conftest import db_session; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^pytest" requirements.txt` → exit 0
    - `test -f tests/__init__.py` → exit 0
    - `test -f tests/conftest.py` → exit 0
    - `grep -q "Base.metadata.create_all" tests/conftest.py` → exit 0
    - `grep -q "sqlite:///:memory:" tests/conftest.py` → exit 0
    - `python -m pytest --collect-only tests/` → exit 0 (keine Collection-Errors)
  </acceptance_criteria>
  <done>pytest installiert, conftest.py mit in-memory Fixture existiert, `pytest --collect-only` läuft ohne Import-Fehler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test-Scaffolds für ft_models und prompt_seed anlegen</name>
  <read_first>
    - tests/conftest.py (Fixture-Name: db_session)
    - .planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-RESEARCH.md §"Validation Architecture" (Test-Namen sind dort definiert)
  </read_first>
  <behavior>
    - test_tables_created: Nach `Base.metadata.create_all` existieren die 4 Tabellen ft_call_sessions, ft_assistant_events, ft_objection_events, prompt_versions in metadata.tables
    - test_user_market_default: User-Insert ohne market-Wert setzt default 'dach' / language 'de'
    - test_transcript_nullable: FtAssistantEvent mit transcript_segment=None ist insertable
    - test_prompt_seed: nach _seed_prompt_versions() existiert mind. 1 row mit module='assistant_live', is_active=True
    - test_seed_idempotent: zweiter _seed_prompt_versions() Aufruf erzeugt keine Duplikate
  </behavior>
  <files>tests/test_ft_models.py, tests/test_ft_seed.py</files>
  <action>
    Erstelle zwei Test-Dateien. **In Wave 0 sind die Tests als `pytest.importorskip` bzw. `pytest.skip("Wave 1: model/seed not yet implemented")` strukturiert**, damit `pytest -q` grün läuft, aber die Test-Signaturen und -Namen bereits fix sind. Wave 1/2 entfernt die Skips und füllt die Assertions.

    `tests/test_ft_models.py`:
    ```python
    import pytest

    def test_tables_created(db_session):
        from database.db import Base
        import database.models  # noqa: F401
        tables = set(Base.metadata.tables.keys())
        required = {"ft_call_sessions", "ft_assistant_events", "ft_objection_events", "prompt_versions"}
        missing = required - tables
        if missing:
            pytest.skip(f"Wave 1 gap: missing tables {missing}")
        assert required.issubset(tables)

    def test_user_market_default(db_session):
        from database.models import User
        if not hasattr(User, "market"):
            pytest.skip("Wave 1 gap: User.market column not yet added")
        u = User(email="t@t.de", passwort_hash="x")
        db_session.add(u); db_session.commit()
        assert u.market == "dach"
        assert u.language == "de"

    def test_transcript_nullable(db_session):
        try:
            from database.models import FtAssistantEvent, FtCallSession, User
        except ImportError:
            pytest.skip("Wave 1 gap: ft_* models not yet defined")
        # Full insert test — Wave 1 fills in.
        pytest.skip("Wave 1 gap: insert flow requires model + FK chain")
    ```

    `tests/test_ft_seed.py`:
    ```python
    import pytest

    def test_prompt_seed(db_session):
        try:
            from database.models import PromptVersion
            from app import _seed_prompt_versions
        except ImportError:
            pytest.skip("Wave 2 gap: PromptVersion / _seed_prompt_versions not yet implemented")
        _seed_prompt_versions(db_session)
        row = db_session.query(PromptVersion).filter_by(module="assistant_live", is_active=True).first()
        assert row is not None
        assert row.version == "v1.0.0"

    def test_seed_idempotent(db_session):
        try:
            from database.models import PromptVersion
            from app import _seed_prompt_versions
        except ImportError:
            pytest.skip("Wave 2 gap")
        _seed_prompt_versions(db_session)
        count_after_first = db_session.query(PromptVersion).count()
        _seed_prompt_versions(db_session)
        count_after_second = db_session.query(PromptVersion).count()
        assert count_after_first == count_after_second
    ```
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_ft_models.py` → exit 0
    - `test -f tests/test_ft_seed.py` → exit 0
    - `grep -q "def test_tables_created" tests/test_ft_models.py` → exit 0
    - `grep -q "def test_user_market_default" tests/test_ft_models.py` → exit 0
    - `grep -q "def test_transcript_nullable" tests/test_ft_models.py` → exit 0
    - `grep -q "def test_prompt_seed" tests/test_ft_seed.py` → exit 0
    - `grep -q "def test_seed_idempotent" tests/test_ft_seed.py` → exit 0
    - `python -m pytest tests/ -q` exit 0 (skipped ist ok, failed nicht)
  </acceptance_criteria>
  <done>Alle 5 Test-Scaffolds existieren, `pytest tests/ -q` läuft grün (skipped zählt als grün). Test-Namen sind exakt wie in RESEARCH.md Validation Architecture spezifiziert.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/ -q` läuft ohne Fehler (skipped Tests sind erwartet bis Wave 1+2).
`grep -q "^pytest" requirements.txt` bestätigt Dependency.
</verification>

<success_criteria>
- pytest ist in requirements.txt
- tests/conftest.py liefert db_session Fixture
- 5 Test-Scaffolds mit exakten Namen existieren
- `pytest tests/ -q` grün
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04.7.1-finetuning-logging-grundlage-inserted/04.7.1-01-SUMMARY.md`
</output>
