---
phase: 08.23.2.C.1
plan: 03
type: execute
wave: 2
depends_on:
  - 08.23.2.C.1-01
files_modified:
  - scripts/refresh_staging_from_production.sh
  - scripts/reset_sequences.py
autonomous: true
requirements:
  - Req-6
  - Req-7

must_haves:
  truths:
    - "scripts/refresh_staging_from_production.sh existiert mit Bestaetigungs-Prompt, Pre-Refresh-Backup, SSH-Pipe ohne lokalen Dump"
    - "scripts/refresh_staging_from_production.sh hat set -eo pipefail und Dump-Groessen-Check (REVIEW-HIGH-3)"
    - "scripts/reset_sequences.py existiert als eigenstaendiges ~25-Zeilen Python-Skript, idempotent"
    - "refresh_staging_from_production.sh ruft reset_sequences.py nach DB-Refresh auf"
    - "Kein lokaler pg_dump wird erstellt (DSGVO-Hygiene + DSGVO-Stream-Constraint aus SPEC.md)"
  artifacts:
    - path: "scripts/refresh_staging_from_production.sh"
      provides: "DB-Sync Production→Staging via SSH-Pipe mit pipefail-Schutz"
      contains: "set -eo pipefail"
    - path: "scripts/reset_sequences.py"
      provides: "Postgres-Sequence-Reset-Skript"
      contains: "setval"
  key_links:
    - from: "scripts/refresh_staging_from_production.sh"
      to: "scripts/reset_sequences.py"
      via: "ssh staging /opt/nerve/venv/bin/python /opt/nerve/app/scripts/reset_sequences.py"
      pattern: "reset_sequences.py"
    - from: "scripts/refresh_staging_from_production.sh"
      to: "Production-Postgres (root@178.104.82.166)"
      via: "DUMP-Variable via SSH, Groessen-Check, dann Pipe zu Staging-psql"
      pattern: "pg_dump.*psql"
---

<objective>
Zwei neue Skripte erstellen: refresh_staging_from_production.sh synchronisiert die Production-Postgres-DB auf Staging via SSH-Pipe (ohne lokalen Dump), reset_sequences.py setzt Postgres-Sequences nach dem Import auf MAX(id)+1.

Purpose: Staging muss periodisch mit Production-Daten aktualisiert werden um realistische Tests zu ermoeglichen. reset_sequences.py verhindert Duplicate-Key-Fehler nach dem Import.

Output: 2 neue Skripte in scripts/
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- reset_sequences-Logik aus scripts/migrate_to_postgres.py (Zeilen 272-301): -->
<!-- Das SQL-DO-Block-Pattern das extrahiert werden muss: -->

def reset_sequences(pg_sess):
    from sqlalchemy import text
    pg_sess.execute(text("""
        DO $$
        DECLARE
            r RECORD;
            seq_name TEXT;
            max_id BIGINT;
        BEGIN
            FOR r IN
                SELECT table_name, column_name
                FROM information_schema.columns
                WHERE table_schema = 'public'
                AND column_default LIKE 'nextval%'
            LOOP
                seq_name := pg_get_serial_sequence(r.table_name, r.column_name);
                IF seq_name IS NOT NULL THEN
                    EXECUTE format('SELECT COALESCE(MAX(%I), 0) FROM %I', r.column_name, r.table_name) INTO max_id;
                    EXECUTE format('SELECT setval(%L, %s, true)', seq_name, GREATEST(max_id, 1));
                END IF;
            END LOOP;
        END $$;
    """))

<!-- Etablierte Patterns aus CLAUDE.md: -->
<!-- - Logging-Prefix: [DB] fuer Datenbank-Operationen -->
<!-- - Python-Naming: snake_case, Kommentare mit # ── Section ── -->
<!-- - DATABASE_URL env var fuer DB-Verbindung -->
<!-- - SSH-Key fuer Production: ~/.ssh/nerve_vps, Production-IP: 178.104.82.166 -->
<!-- - SSH-Key fuer Staging: ~/.ssh/nerve_staging, Staging-IP: <STAGING_IP> (Env-Var oder Platzhalter) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/reset_sequences.py — eigenstaendiges Sequence-Reset-Skript</name>
  <files>scripts/reset_sequences.py</files>
  <read_first>
    - scripts/migrate_to_postgres.py (Zeilen 272-301: reset_sequences()-Funktion — das SQL-DO-Block-Pattern extrahieren)
  </read_first>
  <action>
Erstelle scripts/reset_sequences.py als eigenstaendiges Python-Skript. Extrahiere die Logik aus der reset_sequences()-Funktion in migrate_to_postgres.py und mache es standalone (kein Import von migrate_to_postgres).

Verbindung via DATABASE_URL-Umgebungsvariable (identisch zu Production-Konvention).

```python
#!/usr/bin/env python3
"""reset_sequences.py — Setzt alle Postgres-Sequences auf MAX(id)+1.

Idempotent: Mehrfacher Lauf ist sicher (GREATEST(max_id, 1) verhindert
setval auf 0 bei leeren Tabellen).

Aufruf:
    DATABASE_URL=postgresql://nerve_app@/nerve python scripts/reset_sequences.py

Oder direkt auf Staging-Server:
    /opt/nerve/venv/bin/python /opt/nerve/app/scripts/reset_sequences.py

Hintergrund (Phase 08.23.2.A Bugfix): Nach einem pg_dump | psql-Import starten
Postgres-Sequences bei 1, obwohl Zeilen mit IDs 1, 2, 3, ... bereits existieren.
Naechstes INSERT wuerde mit Duplicate-Key-Fehler schlagen. Sequence auf MAX(id)+1
setzen behebt das Problem.
"""
import os
import sys

from sqlalchemy import create_engine, text


def reset_sequences(engine):
    """Setzt alle Sequences auf GREATEST(MAX(id), 1) — idempotent."""
    with engine.connect() as conn:
        conn.execute(text("""
            DO $$
            DECLARE
                r RECORD;
                seq_name TEXT;
                max_id BIGINT;
            BEGIN
                FOR r IN
                    SELECT table_name, column_name
                    FROM information_schema.columns
                    WHERE table_schema = 'public'
                    AND column_default LIKE 'nextval%'
                LOOP
                    seq_name := pg_get_serial_sequence(r.table_name, r.column_name);
                    IF seq_name IS NOT NULL THEN
                        EXECUTE format('SELECT COALESCE(MAX(%I), 0) FROM %I',
                            r.column_name, r.table_name) INTO max_id;
                        EXECUTE format('SELECT setval(%L, %s, true)',
                            seq_name, GREATEST(max_id, 1));
                        RAISE NOTICE 'Sequence % -> %', seq_name, GREATEST(max_id, 1);
                    END IF;
                END LOOP;
            END $$;
        """))
        conn.commit()
    print('[DB] Sequence-Reset abgeschlossen — alle Sequences auf MAX(id)+1 gesetzt')


def main():
    database_url = os.environ.get('DATABASE_URL', '')
    if not database_url.startswith('postgresql'):
        print(f'[DB] ERROR: DATABASE_URL muss mit postgresql:// beginnen, ist: {database_url!r}')
        sys.exit(1)

    print(f'[DB] Verbinde mit: {database_url}')
    engine = create_engine(database_url)
    reset_sequences(engine)


if __name__ == '__main__':
    main()
```
  </action>
  <verify>
    <automated>python -m py_compile scripts/reset_sequences.py &amp;&amp; grep "setval" scripts/reset_sequences.py &amp;&amp; grep "GREATEST" scripts/reset_sequences.py &amp;&amp; grep "DATABASE_URL" scripts/reset_sequences.py &amp;&amp; grep "startswith('postgresql')" scripts/reset_sequences.py &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - python -m py_compile scripts/reset_sequences.py gibt exit 0 (Syntax korrekt)
    - grep "setval" scripts/reset_sequences.py gibt mindestens 1 Zeile zurueck
    - grep "GREATEST" scripts/reset_sequences.py gibt mindestens 1 Zeile zurueck (idempotent)
    - grep "DATABASE_URL" scripts/reset_sequences.py gibt mindestens 2 Zeilen zurueck (Lesen + Error-Check)
    - grep "startswith" scripts/reset_sequences.py gibt eine Zeile zurueck (Postgres-only-Check)
    - grep "def reset_sequences" scripts/reset_sequences.py gibt eine Zeile zurueck
    - grep "def main" scripts/reset_sequences.py gibt eine Zeile zurueck
    - grep "if __name__" scripts/reset_sequences.py gibt eine Zeile zurueck (ausfuehrbar als standalone)
    - Datei hat weniger als 70 Zeilen (eigenstaendig, kompakt ~25-Zeilen Logik)
  </acceptance_criteria>
  <done>scripts/reset_sequences.py ist eigenstaendig; verbindet sich via DATABASE_URL; laueft ohne Fehler bei leerer DB (GREATEST(max_id,1)); gibt klar verstaendliche [DB]-Log-Ausgabe; python -m py_compile exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/refresh_staging_from_production.sh — DB-Sync via SSH-Pipe mit pipefail-Schutz</name>
  <files>scripts/refresh_staging_from_production.sh</files>
  <read_first>
    - scripts/reset_sequences.py (gerade erstellt — fuer korrekten Aufrufpfad auf Server)
  </read_first>
  <action>
Erstelle scripts/refresh_staging_from_production.sh als Bash-Skript. Das Skript wird LOKAL in Git Bash ausgefuehrt und koordiniert SSH-Verbindungen zu Production und Staging.

Constraints aus SPEC.md/CONTEXT.md:
- Kein lokaler pg_dump (DSGVO-Hygiene) — nur SSH-Pipe
- Bestaetigungs-Prompt [y/N]
- Pre-Refresh-Backup auf Staging (nicht lokal)
- Aufruf von reset_sequences.py nach Import

REVIEW-HIGH-3 FIX: Die urspruengliche SSH-Pipe `ssh prod 'pg_dump nerve' | ssh staging 'psql nerve'`
hat ein stilles Fehlerrisiko: Falls pg_dump mid-stream fehlschlaegt (Netzwerkunterbrechung, Disk full
auf Production), erhaelt psql einen leeren oder unvollstaendigen Stream und kann trotzdem mit exit 0
enden — da bash den Exit-Code des LETZTEN Befehls einer Pipe zurueckgibt (psql), nicht von pg_dump.
Das Ergebnis waere eine leere oder korrupte Staging-DB ohne Fehlermeldung.

Fix: Zwei Massnahmen kombiniert:
1. `set -eo pipefail` als erste ausfuehrbare Zeile (pipefail: Pipe schlaegt fehl wenn irgendein Segment fehlschlaegt)
2. Dump in Variable fangen + Groessen-Check vor dem Import (< 1 KB = abbrechen)

```bash
#!/bin/bash
# ── NERVE DB-Refresh: Production → Staging ────────────────────────────────────
# Synchronisiert die Production-Postgres-Datenbank auf den Staging-Server.
# Kein lokaler pg_dump — alle Daten laufen via SSH-Pipe (DSGVO-Hygiene).
#
# Voraussetzungen:
#   - SSH-Key ~/.ssh/nerve_vps fuer Production (root@178.104.82.166)
#   - SSH-Key ~/.ssh/nerve_staging fuer Staging (root@<STAGING_IP>)
#   - pg_dump/psql auf beiden Servern verfuegbar (Postgres installiert)
#   - STAGING_IP: als Env-Var setzen oder Platzhalter unten ersetzen
#
# Aufruf:
#   bash scripts/refresh_staging_from_production.sh
#   # oder mit expliziter IP:
#   STAGING_IP=1.2.3.4 bash scripts/refresh_staging_from_production.sh
#
# DSGVO-Hinweis (Req-6 Constraint):
#   Solange kein externer Early-Access-User registriert ist, ist 1:1-Kopie
#   DSGVO-konform. Sobald erster externer User registriert:
#   -> refresh_staging_from_production.sh MUSS anonymisieren.
#   -> Trigger dokumentiert in Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md Sektion 8

# REVIEW-HIGH-3 FIX: pipefail verhindert stille DB-Korruption bei halbem pg_dump-Stream.
# Ohne pipefail: bash gibt Exit-Code von psql zurueck (letztem Befehl), nicht pg_dump.
# psql kann erfolgreich beenden obwohl der pg_dump-Input leer oder korrupt war.
set -eo pipefail

# ── Konfiguration ─────────────────────────────────────────────────────────────
PROD_HOST="root@178.104.82.166"
PROD_SSH_KEY="$HOME/.ssh/nerve_vps"
STAGING_IP="${STAGING_IP:-<STAGING_IP>}"  # Env-Var oder Platzhalter ersetzen
STAGING_HOST="root@${STAGING_IP}"
STAGING_SSH_KEY="$HOME/.ssh/nerve_staging"
DB_NAME="nerve"
BACKUP_DIR="/opt/nerve/backups/pre-refresh"
TIMESTAMP=$(date +%Y%m%dT%H%M%S)

echo "[DB] ============================================================"
echo "[DB] NERVE DB-Refresh: Production → Staging"
echo "[DB] Production: $PROD_HOST"
echo "[DB] Staging:    $STAGING_HOST"
echo "[DB] ============================================================"
echo ""
echo "[DB] WARNUNG: Die nerve-Datenbank auf Staging wird VOLLSTAENDIG UEBERSCHRIEBEN."
echo "[DB] Alle Staging-spezifischen Daten gehen verloren."
echo ""
printf "Fortfahren? [y/N] "
read -r CONFIRM
if [[ "$CONFIRM" != "y" && "$CONFIRM" != "Y" ]]; then
  echo "[DB] Abgebrochen."
  exit 0
fi

# ── Schritt 1: Pre-Refresh-Backup auf Staging ─────────────────────────────────
echo "[DB] Schritt 1/3: Erstelle Pre-Refresh-Backup auf Staging..."
ssh -i "$STAGING_SSH_KEY" "$STAGING_HOST" \
  "mkdir -p '$BACKUP_DIR' && pg_dump -U nerve_app $DB_NAME | gzip > '$BACKUP_DIR/pre-refresh-$TIMESTAMP.sql.gz'"
echo "[DB] Backup erstellt: $BACKUP_DIR/pre-refresh-$TIMESTAMP.sql.gz"

# ── Schritt 2: DB-Stream Production → Staging via SSH-Pipe ────────────────────
echo "[DB] Schritt 2/3: Streame pg_dump von Production nach Staging..."
echo "[DB] REVIEW-HIGH-3: Dump wird zuerst in Variable gefangen — Groessen-Check vor Import."
echo "[DB] Kein lokaler Dump wird erstellt (DSGVO-Hygiene)."

# Dump in Variable fangen (kein lokaler File-Dump — Daten nur im RAM)
# -o StrictHostKeyChecking=accept-new: verhindert Prompt bei neuem Host-Key
DUMP=$(ssh -i "$PROD_SSH_KEY" -o StrictHostKeyChecking=accept-new "$PROD_HOST" \
  "pg_dump -U nerve_app --no-owner --no-acl $DB_NAME 2>/dev/null")

# Groessen-Check: Ein valider nerve-Dump ist immer > 1 KB.
# < 1024 Bytes deutet auf SSH-Verbindungsfehler oder leere DB hin.
DUMP_SIZE=${#DUMP}
if [[ "$DUMP_SIZE" -lt 1024 ]]; then
  echo "[DB] ABORT: pg_dump Output zu klein (${DUMP_SIZE} Bytes) — moeglicher Verbindungsfehler auf Production"
  echo "[DB] Staging-DB wurde NICHT veraendert."
  exit 1
fi
echo "[DB] Dump-Groesse: ${DUMP_SIZE} Bytes — OK"

# Dump auf Staging importieren
echo "$DUMP" | ssh -i "$STAGING_SSH_KEY" -o StrictHostKeyChecking=accept-new "$STAGING_HOST" \
  "psql -U nerve_app $DB_NAME"

echo "[DB] DB-Stream abgeschlossen."

# ── Schritt 3: Sequence-Reset auf Staging ─────────────────────────────────────
echo "[DB] Schritt 3/3: Sequence-Reset auf Staging ausfuehren..."
ssh -i "$STAGING_SSH_KEY" "$STAGING_HOST" \
  "DATABASE_URL=postgresql://nerve_app@/$DB_NAME /opt/nerve/venv/bin/python /opt/nerve/app/scripts/reset_sequences.py"

echo "[DB] ============================================================"
echo "[DB] DB-Refresh abgeschlossen."
echo "[DB] Staging-DB entspricht jetzt Production-Stand von: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
echo "[DB] Verifikation: psql -h $STAGING_IP -U nerve_app -c 'SELECT COUNT(*) FROM users' nerve"
echo "[DB] ============================================================"
```
  </action>
  <verify>
    <automated>bash -n scripts/refresh_staging_from_production.sh &amp;&amp; grep "set -eo pipefail" scripts/refresh_staging_from_production.sh &amp;&amp; grep "pg_dump" scripts/refresh_staging_from_production.sh &amp;&amp; grep "psql" scripts/refresh_staging_from_production.sh &amp;&amp; grep "pre-refresh" scripts/refresh_staging_from_production.sh &amp;&amp; grep "reset_sequences.py" scripts/refresh_staging_from_production.sh &amp;&amp; grep "DUMP_SIZE" scripts/refresh_staging_from_production.sh &amp;&amp; grep "\[y/N\]" scripts/refresh_staging_from_production.sh &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/refresh_staging_from_production.sh gibt exit 0 (Syntax korrekt)
    - grep "set -eo pipefail" scripts/refresh_staging_from_production.sh gibt eine Zeile zurueck (REVIEW-HIGH-3 Fix — erste ausfuehrbare Zeile nach Kommentarblock)
    - grep "DUMP_SIZE" scripts/refresh_staging_from_production.sh gibt mindestens 2 Zeilen zurueck (Zuweisung + Vergleich)
    - grep "ABORT.*pg_dump\|pg_dump.*ABORT" scripts/refresh_staging_from_production.sh gibt mindestens 1 Zeile zurueck (Abbruch bei zu kleinem Dump)
    - grep "1024" scripts/refresh_staging_from_production.sh gibt mindestens 1 Zeile zurueck (Mindest-Dump-Groesse)
    - grep "178.104.82.166" scripts/refresh_staging_from_production.sh gibt eine Zeile zurueck (Production-IP)
    - grep "nerve_vps" scripts/refresh_staging_from_production.sh gibt mindestens 1 Zeile zurueck
    - grep "nerve_staging" scripts/refresh_staging_from_production.sh gibt mindestens 1 Zeile zurueck
    - grep "pg_dump" scripts/refresh_staging_from_production.sh gibt mindestens 2 Zeilen zurueck (Backup + Stream)
    - grep "pre-refresh" scripts/refresh_staging_from_production.sh gibt mindestens 2 Zeilen zurueck (Backup-Pfad + Echo)
    - grep "reset_sequences.py" scripts/refresh_staging_from_production.sh gibt eine Zeile zurueck
    - grep "\[y/N\]" scripts/refresh_staging_from_production.sh gibt eine Zeile zurueck (Bestaetigungs-Prompt)
    - grep "kein.*[Ll]okal\|DSGVO" scripts/refresh_staging_from_production.sh -i gibt mindestens 1 Zeile zurueck
    - KEIN `> /tmp/` oder `> *.sql` im Skript (kein lokaler Dump) — grep "dump.*sql\b" scripts/refresh_staging_from_production.sh gibt 0 Zeilen
  </acceptance_criteria>
  <done>scripts/refresh_staging_from_production.sh: bash -n exit 0; set -eo pipefail als erste ausfuehrbare Zeile (REVIEW-HIGH-3); Dump in Variable + Groessen-Check (< 1024 Bytes = ABORT); Bestaetigungs-Prompt [y/N]; Pre-Refresh-Backup auf Staging via pg_dump|gzip; SSH-Pipe Production→Staging ohne lokalen Dump; reset_sequences.py-Aufruf auf Staging nach Import; klare [DB]-Log-Ausgabe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lokal → Production-Postgres via SSH | refresh_staging_from_production.sh liest Production-Daten |
| lokal → Staging-Postgres via SSH | Daten werden auf Staging geschrieben |
| SSH-Verbindung Production → DUMP-Variable | Datenstrom enthaelt alle Production-Daten (PII), nur im RAM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C1-03-1 | Information Disclosure | SSH-Pipe (Production-PII) | mitigate | Kein lokaler Dump (DSGVO-Constraint aus SPEC.md); Dump nur in RAM-Variable; beide SSH-Keys getrennt |
| T-C1-03-2 | Tampering | reset_sequences.py via SSH auf Staging | accept | Skript ist idempotent; schlimmster Fall ist Sequence auf falschen Wert — keine Datenverfaelschung |
| T-C1-03-3 | Information Disclosure | Bestaetigungs-Prompt umgehbar | accept | [y/N] ist UX-Schutz vor versehentlichem Ueberschreiben, kein Security-Control |
| T-C1-03-4 | Elevation of Privilege | DATABASE_URL in SSH-Command sichtbar | accept | SSH-Verbindung ist verschluesselt; nerve_app Peer-Auth ohne Passwort; kein Secret in URL |
| T-C1-03-5 | Tampering | Stiller Dump-Fehler (partieller Stream) | mitigate | set -eo pipefail + DUMP_SIZE-Check verhindern Import eines leeren/korrupten Dumps (REVIEW-HIGH-3) |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

```bash
# Syntax-Check
python -m py_compile scripts/reset_sequences.py && echo "reset_sequences.py: OK"
bash -n scripts/refresh_staging_from_production.sh && echo "refresh: OK"

# REVIEW-HIGH-3 Fix: pipefail vorhanden
grep "set -eo pipefail" scripts/refresh_staging_from_production.sh

# REVIEW-HIGH-3 Fix: Dump-Groessen-Check
grep "DUMP_SIZE" scripts/refresh_staging_from_production.sh
grep "1024" scripts/refresh_staging_from_production.sh

# Key-Patterns
grep "setval" scripts/reset_sequences.py
grep "GREATEST" scripts/reset_sequences.py
grep "pg_dump" scripts/refresh_staging_from_production.sh | wc -l  # >= 2
grep "reset_sequences.py" scripts/refresh_staging_from_production.sh

# DSGVO-Constraint: kein lokaler Dump
grep -v "^#" scripts/refresh_staging_from_production.sh | grep "\.sql\b" || echo "OK: kein lokaler SQL-Dump"
```
</verification>

<success_criteria>
- scripts/reset_sequences.py: python -m py_compile exit 0; verbindet via DATABASE_URL; enthaelt setval + GREATEST; eigenstaendig ausfuehrbar mit if __name__ == '__main__'
- scripts/refresh_staging_from_production.sh: bash -n exit 0; set -eo pipefail als erste ausfuehrbare Zeile (REVIEW-HIGH-3 Fix); DUMP-Variable + Groessen-Check vor psql-Import; Prompt [y/N]; Pre-Refresh-Backup auf Staging; SSH-Pipe ohne lokalen Dump; reset_sequences.py-Aufruf auf Staging-Server
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.23.2.C.1-staging-server-deploy-workflow/08.23.2.C.1-03-SUMMARY.md` mit:
- Pfaden der erstellten Skripte
- Aufruf-Beispiel fuer refresh_staging_from_production.sh
- Bestaetigung REVIEW-HIGH-3 Fix: set -eo pipefail + DUMP_SIZE-Check implementiert
- Hinweis: STAGING_IP-Platzhalter in refresh_staging_from_production.sh muss nach Hetzner-Provisionierung ersetzt oder via Env-Var gesetzt werden
</output>
