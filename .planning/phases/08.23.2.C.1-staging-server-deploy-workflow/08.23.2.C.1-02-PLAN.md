---
phase: 08.23.2.C.1
plan: 02
type: execute
wave: 2
depends_on:
  - 08.23.2.C.1-01
files_modified:
  - deploy.sh
  - routes/app_routes.py
  - .env.staging.example
autonomous: true
requirements:
  - Req-2
  - Req-3
  - Req-4
  - Req-5

must_haves:
  truths:
    - "./deploy.sh ohne Argument bricht sofort mit Fehlermeldung ab"
    - "./deploy.sh staging deployt auf Staging-IP mit SSH-Key nerve_staging"
    - "./deploy.sh production deployt auf 178.104.82.166 mit SSH-Key nerve_vps"
    - "deploy.sh staging schreibt /opt/nerve/.deploy_meta VOR systemctl restart nerve-staging"
    - "deploy.sh production blockt wenn Staging-Health status != ok oder deployed_at >24h oder git_head != lokal"
    - "/api/health gibt git_head und deployed_at zurueck (null wenn .deploy_meta fehlt)"
    - ".env.staging.example existiert mit allen Pflicht-Variablen und Kommentaren"
  artifacts:
    - path: "deploy.sh"
      provides: "Deploy-Skript mit TARGET-Parameter"
      contains: "TARGET=\"${1:?Usage: ./deploy.sh staging|production}\""
    - path: "routes/app_routes.py"
      provides: "/api/health mit git_head + deployed_at Feldern"
      contains: "deploy_meta"
    - path: ".env.staging.example"
      provides: "Template fuer /etc/nerve/.env auf Staging"
      contains: "STAGING_BASIC_AUTH_USER"
  key_links:
    - from: "deploy.sh (staging-Block)"
      to: "/opt/nerve/.deploy_meta"
      via: "SSH-Heredoc VOR systemctl restart nerve-staging (REVIEW-HIGH-2 Fix)"
      pattern: "deploy_meta"
    - from: "routes/app_routes.py api_health()"
      to: "/opt/nerve/.deploy_meta"
      via: "open('/opt/nerve/.deploy_meta') bei Request"
      pattern: "deploy_meta"
    - from: "deploy.sh (production-Gate)"
      to: "https://staging.getnerve.app/api/health"
      via: "curl ohne Credentials (nginx hat auth_basic off fuer /api/health — REVIEW-HIGH-1 Fix in Plan 01)"
      pattern: "staging.getnerve.app/api/health"
---

<objective>
deploy.sh chirurgisch refactoren um TARGET-Parameter zu unterstuetzen (staging/production), .deploy_meta VOR Staging-Restart schreiben (REVIEW-HIGH-2 Fix), Production-Pre-Deploy-Gate mit 3 Staging-Checks implementieren, /api/health um git_head + deployed_at erweitern, und .env.staging.example als committed Template erstellen.

Purpose: Dies ist der Kern des Staging-Workflows. Ohne diesen Plan kann weder auf Staging deployt noch Production durch Staging-Gate gesichert werden.

Output: Modifiziertes deploy.sh (TARGET-Param + Staging/Production-Branching + Gate), erweitertes api_health() in app_routes.py, neue .env.staging.example
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- Aktueller deploy.sh Aufbau (vollstaendig lesen vor Implementierung): -->
<!-- Schluessel-Struktur die beibehalten werden muss: -->

# Zeile 1-9: Kommentar-Block (aktualisieren: "--dry-run" durch "staging|production" ersetzen)
# Zeile 10: set -e
# Zeile 11-15: VPS_HOST, APP_DIR, VENV_DIR, SSH_KEY (ERSETZEN durch TARGET-Block)
# Zeile 17-21: --dry-run Handling (ENTFERNEN — kein dry-run mehr nach Refactor)
# Zeile 23-43: TAR_EXCLUDES Array (BEHALTEN unveraendert)
# Zeile 45-55: echo + dry-run-Logik (ERSETZEN)
# Zeile 57-168: tar-upload + SSH-Heredoc (BEHALTEN, nur SSH_KEY Variable statt hardcoded)

<!-- Aktuelles api_health() in routes/app_routes.py (Zeilen 1375-1431): -->

@app_routes_bp.route('/api/health')
def api_health():
    import glob
    import time
    database_url = os.environ.get('DATABASE_URL', '')
    if not database_url.startswith('postgresql'):
        backup_status = 'ok'
        age_hours = None
    else:
        backup_status = 'missing'
        age_hours = None
        backup_dir = '/opt/nerve/backups/postgres'
        try:
            files = sorted(glob.glob(os.path.join(backup_dir, 'nerve-*.sql.gz')))
            if files:
                newest = files[-1]
                mtime = os.path.getmtime(newest)
                age_seconds = time.time() - mtime
                age_hours = round(age_seconds / 3600, 1)
                if age_hours <= 25:
                    backup_status = 'ok'
                else:
                    backup_status = 'stale'
        except Exception:
            backup_status = 'missing'
            age_hours = None
    try:
        from services.anonymization import get_pipeline_status
        _pipeline = get_pipeline_status()
        pipeline_status = _pipeline.get('status', 'unknown')
        pipeline_error_count = _pipeline.get('error_count_10min', 0)
    except Exception:
        pipeline_status = 'unknown'
        pipeline_error_count = 0
    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
        'pipeline_status': pipeline_status,
        'pipeline_error_count_10min': pipeline_error_count,
    })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy.sh Refactor — TARGET-Parameter + Staging/Production-Branching + .deploy_meta</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh (vollstaendig lesen — den gesamten bestehenden Inhalt verstehen bevor irgendeine Aenderung gemacht wird)
    - deploy/nerve-staging.service (Name des Staging-Service-Units: nerve-staging)
    - deploy/nginx-staging.conf (Name der Staging-nginx-Config: nerve-staging)
    - deploy/nginx-production.conf (Existiert nach Plan 01 — wird per scp hochgeladen statt inline-Heredoc)
  </read_first>
  <action>
Fuehre einen chirurgischen Refactor von deploy.sh durch. Das Ziel: minimale Struktur-Aenderungen — der bestehende tar-upload + SSH-Heredoc-Block bleibt vollstaendig unveraendert, nur Kopf und Tail werden angepasst.

**AENDERUNG 1: Kommentar-Header aktualisieren (Zeilen 1-9)**

Ersetze die ersten Zeilen (bis zu `set -e`):

```bash
#!/bin/bash
# ── NERVE Deploy Script ─────────────────────────────────────────────────────
# Usage: ./deploy.sh staging|production
# Deploys the latest main branch to the target VPS via tar-over-ssh.
# Works on Windows Git-Bash (no rsync required) and on Linux/macOS.
# Prod-SQLite NICHT ueberschreiben — Schema-Code (database/models.py, db.py)
# wird uebertragen, .db-Dateien nicht (via tar --exclude).
# Prerequisites: SSH key auth configured for VPS_HOST, tar available locally.
# Staging-Key: ~/.ssh/nerve_staging | Production-Key: ~/.ssh/nerve_vps

set -e
```

**AENDERUNG 2: TARGET-Block + VPS_HOST/SSH_KEY-Assignment (ersetzt alte Zeilen 11-21)**

Ersetze den Block zwischen `set -e` und `# Exclude list` komplett:

```bash
# ── Target-Parameter (Pflicht, kein Default) ───────────────────────────────
TARGET="${1:?Usage: ./deploy.sh staging|production}"

APP_DIR="/opt/nerve/app"
VENV_DIR="/opt/nerve/venv"

if [[ "$TARGET" == "production" ]]; then
  # ── Production Pre-Deploy-Gate ─────────────────────────────────────────
  echo "[deploy] TARGET=production — Pre-Deploy-Gate wird ausgefuehrt..."

  # jq-Pflicht-Check (fuer JSON-Parsing der Health-Response)
  which jq || { echo "[deploy] ERROR: jq nicht installiert — brew install jq oder https://stedolan.github.io/jq/"; exit 1; }

  STAGING_HEALTH=$(curl -fsS --max-time 10 https://staging.getnerve.app/api/health 2>/dev/null || true)

  if [[ -z "$STAGING_HEALTH" ]]; then
    echo "[deploy] BLOCKER: Staging nicht erreichbar — https://staging.getnerve.app/api/health antwortet nicht"
    exit 1
  fi

  STAGING_STATUS=$(echo "$STAGING_HEALTH" | jq -r '.status // "error"')
  if [[ "$STAGING_STATUS" != "ok" ]]; then
    echo "[deploy] BLOCKER: Staging not healthy (status=$STAGING_STATUS) — zuerst ./deploy.sh staging ausfuehren"
    exit 1
  fi

  STAGING_DEPLOYED_AT=$(echo "$STAGING_HEALTH" | jq -r '.deployed_at // ""')
  if [[ -z "$STAGING_DEPLOYED_AT" || "$STAGING_DEPLOYED_AT" == "null" ]]; then
    echo "[deploy] BLOCKER: Staging hat kein deployed_at — zuerst ./deploy.sh staging ausfuehren"
    exit 1
  fi
  # Alter pruefen: deployed_at ist ISO-8601 UTC, z.B. 2026-05-19T14:30:00Z
  DEPLOYED_TS=$(date -u -d "$STAGING_DEPLOYED_AT" +%s 2>/dev/null || date -u -j -f "%Y-%m-%dT%H:%M:%SZ" "$STAGING_DEPLOYED_AT" +%s 2>/dev/null || echo "0")
  NOW_TS=$(date -u +%s)
  AGE_HOURS=$(( (NOW_TS - DEPLOYED_TS) / 3600 ))
  if [[ "$AGE_HOURS" -ge 24 ]]; then
    echo "[deploy] BLOCKER: Staging deploy older than 24h (${AGE_HOURS}h) — zuerst ./deploy.sh staging ausfuehren"
    exit 1
  fi

  LOCAL_HEAD=$(git rev-parse HEAD)
  STAGING_HEAD=$(echo "$STAGING_HEALTH" | jq -r '.git_head // ""')
  if [[ "$STAGING_HEAD" != "$LOCAL_HEAD" ]]; then
    echo "[deploy] BLOCKER: Local HEAD ($LOCAL_HEAD) != Staging HEAD ($STAGING_HEAD) — zuerst ./deploy.sh staging mit aktuellem Commit ausfuehren"
    exit 1
  fi

  echo "[deploy] Pre-Deploy-Gate bestanden — Staging ist ok, aktuell (${AGE_HOURS}h alt), HEAD stimmt ueberein"

  VPS_HOST="root@178.104.82.166"
  SSH_KEY="$HOME/.ssh/nerve_vps"
  SERVICE_NAME="nerve"

elif [[ "$TARGET" == "staging" ]]; then
  VPS_HOST="root@<STAGING_IP>"
  SSH_KEY="$HOME/.ssh/nerve_staging"
  SERVICE_NAME="nerve-staging"

else
  echo "[deploy] ERROR: Unbekannter TARGET '$TARGET'. Erlaubt: staging, production"
  exit 1
fi

# .env-Check: Beide Targets brauchen /etc/nerve/.env auf dem Ziel-Server
echo "[deploy] Pruefe /etc/nerve/.env auf $TARGET..."
ssh -i "$SSH_KEY" "$VPS_HOST" 'test -f /etc/nerve/.env' || {
  echo "[deploy] BLOCKER: /etc/nerve/.env fehlt auf $TARGET — erstelle die Datei mit .env.staging.example als Template"
  exit 1
}
```

**AENDERUNG 3: `echo "[deploy] Connecting..."` Zeile aktualisieren**

Ersetze:
```
echo "[deploy] Connecting to $VPS_HOST..."
```
durch:
```
echo "[deploy] Connecting to $VPS_HOST (TARGET=$TARGET)..."
```

**AENDERUNG 4: tar-Upload-Befehl anpassen (VPS_HOST + SSH_KEY bereits als Variablen definiert)**

Die Zeile:
```bash
tar "${TAR_EXCLUDES[@]}" -cf - ./ | \
  ssh -i "$SSH_KEY" "$VPS_HOST" "mkdir -p '$APP_DIR' && tar -xf - -C '$APP_DIR' --no-same-owner"
```
...verwendet bereits `$SSH_KEY` und `$VPS_HOST` — diese sind jetzt korrekt durch den TARGET-Block gesetzt. Zeile unveraendert lassen.

**AENDERUNG 5: Service-Unit-Upload — TARGET-spezifisch, kein Fallthrough**

Ersetze den scp + ssh-Block fuer nerve.service durch folgende TARGET-spezifische Logik.
Entferne `2>/dev/null || true` vollstaendig — set -e soll Fehler fangen:

```bash
echo "[deploy] Installing systemd service unit ($SERVICE_NAME)..."
if [[ "$TARGET" == "staging" ]]; then
  scp -i "$SSH_KEY" deploy/nerve-staging.service "$VPS_HOST":/tmp/nerve-staging.service
  ssh -i "$SSH_KEY" "$VPS_HOST" "sudo cp /tmp/nerve-staging.service /etc/systemd/system/nerve-staging.service && sudo systemctl daemon-reload"
elif [[ "$TARGET" == "production" ]]; then
  scp -i "$SSH_KEY" deploy/nerve.service "$VPS_HOST":/tmp/nerve.service
  ssh -i "$SSH_KEY" "$VPS_HOST" "sudo cp /tmp/nerve.service /etc/systemd/system/nerve.service && sudo systemctl daemon-reload"
fi
```

Begruendung: nerve-staging.service wird NUR auf Staging kopiert, nerve.service NUR auf Production.
Kein `2>/dev/null || true` — ein fehlgeschlagener scp ist ein echter Fehler der set -e abfangen soll.

**AENDERUNG 6: nginx-Config — symmetrischer scp-Ansatz fuer beide Targets**

Ersetze den bestehenden nginx-Config-Block (der fuer Production einen verschachtelten
`<< 'NGINX'`-Heredoc im aeusseren ENDHEREDOC hatte) durch einen scp-basierten Ansatz
fuer BEIDE Targets. Das verhindert $-Variable-Expansion durch die lokale Shell.

VOR dem SSH-Heredoc (im lokalen Shell-Block):

```bash
echo "[deploy] Uploading nginx config for $TARGET..."
if [[ "$TARGET" == "staging" ]]; then
  scp -i "$SSH_KEY" deploy/nginx-staging.conf "$VPS_HOST":/tmp/nginx-staging.conf
elif [[ "$TARGET" == "production" ]]; then
  scp -i "$SSH_KEY" deploy/nginx-production.conf "$VPS_HOST":/tmp/nginx-production.conf
fi
```

IM SSH-Heredoc (statt des bisherigen verschachtelten nginx-Heredocs):

```bash
  # nginx-Config installieren (Datei wurde per scp hochgeladen — kein inline-Heredoc)
  if [[ "$TARGET" == "staging" ]]; then
    sudo cp /tmp/nginx-staging.conf /etc/nginx/sites-available/nerve-staging
    sudo ln -sf /etc/nginx/sites-available/nerve-staging /etc/nginx/sites-enabled/nerve-staging
    sudo nginx -t && sudo systemctl reload nginx
  else
    sudo cp /tmp/nginx-production.conf /etc/nginx/sites-available/nerve
    sudo ln -sf /etc/nginx/sites-available/nerve /etc/nginx/sites-enabled/nerve
    sudo nginx -t && sudo systemctl reload nginx
  fi
```

Wichtig: Da TARGET eine lokale Variable ist die beim ENDHEREDOC (ohne Quotes)
expandiert wird, ist `[[ "$TARGET" == "staging" ]]` im Heredoc auf dem Server
korrekt — der Wert ist bereits eingebettet. Alle anderen Server-seitigen
$-Variablen (z.B. \$APP_DIR, \$VENV_DIR falls vorhanden) muessen mit \$ escaped werden.

**AENDERUNG 7: SSH-Heredoc Delimiter + systemctl-Zeilen**

Das bestehende `ssh -i ~/.ssh/nerve_vps "$VPS_HOST" bash -s << 'EOF'` ersetzen durch:
```bash
ssh -i "$SSH_KEY" "$VPS_HOST" bash -s << ENDHEREDOC
  set -e
  ... (gesamter bestehender Heredoc-Inhalt — server-seitige $-Variablen mit \$ escapen)
```

Im Heredoc, systemctl-Zeilen anpassen:
```bash
  sudo systemctl restart $SERVICE_NAME
  echo "[deploy] Service status:"
  sudo systemctl status $SERVICE_NAME --no-pager -l
```

Da $SERVICE_NAME lokal expandiert wird (ENDHEREDOC ohne Quotes), ist der Wert
(nerve-staging oder nerve) beim Erreichen des Servers bereits eingebettet.

**AENDERUNG 8: .deploy_meta VOR Staging-Restart schreiben (REVIEW-HIGH-2 FIX)**

REVIEW-HIGH-2 beschreibt ein Timing-Problem: .deploy_meta wurde NACH systemctl restart
geschrieben. Wenn deploy.sh schnell nacheinander ausgefuehrt wird (z.B. schneller
Re-Deploy-Loop) oder der Production-Gate-curl direkt nach einem Staging-Deploy kommt,
liest /api/health stale Daten aus der vorherigen .deploy_meta (oder findet gar keine
Datei beim allerersten Deploy).

Korrekte Reihenfolge im SSH-Heredoc:

1. Vor dem Heredoc: GIT_HEAD_LOCAL lokal berechnen

```bash
GIT_HEAD_LOCAL=$(git rev-parse HEAD)
```

2. Im Heredoc, NACH dem pip-install und VOR dem systemctl-restart, fuer Staging:

```bash
  # REVIEW-HIGH-2 FIX: .deploy_meta VOR systemctl restart schreiben
  # Datei muss existieren bevor Service neu startet — sonst liest /api/health
  # beim ersten Request nach Restart noch stale/keine Daten.
  if [[ "$TARGET" == "staging" ]]; then
    echo "GIT_HEAD=$GIT_HEAD_LOCAL" > /opt/nerve/.deploy_meta
    echo "DEPLOYED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> /opt/nerve/.deploy_meta
    echo "[deploy] .deploy_meta geschrieben (vor Restart)"
  fi

  sudo systemctl restart $SERVICE_NAME
  echo "[deploy] Service status:"
  sudo systemctl status $SERVICE_NAME --no-pager -l
```

$GIT_HEAD_LOCAL ist eine lokale Variable → wird durch ENDHEREDOC-Expansion eingebettet.
$TARGET ist ebenfalls lokal → korrekt im `if`-Test auf dem Server.
`$(date -u ...)` im Heredoc wird SERVER-seitig ausgefuehrt (korrekt — Timestamp des Deploys).

**Gesamte neue Struktur deploy.sh:**

```
[Kommentar-Header, aktualisiert]
set -e
[TARGET-Block: Parameter, Production-Gate, Staging-Branch, .env-Check]
[TAR_EXCLUDES Array — unveraendert]
GIT_HEAD_LOCAL=$(git rev-parse HEAD)
echo "[deploy] Connecting to $VPS_HOST (TARGET=$TARGET)..."
[tar-over-ssh — unveraendert, nutzt $SSH_KEY + $VPS_HOST]
[Service-Unit-Upload — TARGET-spezifisch, kein 2>/dev/null || true]
[nginx-Config scp — TARGET-spezifisch, beide via scp]
ssh -i "$SSH_KEY" "$VPS_HOST" bash -s << ENDHEREDOC
  set -e
  [Ownership-Fix — unveraendert, server-seitige $ mit \$ escapen]
  [pip install — unveraendert]
  [spaCy download — unveraendert]
  [GLiNER pre-cache — unveraendert]
  [nginx config installieren — cp + ln + nginx -t + reload]
  [pytest — unveraendert]
  [.deploy_meta schreiben wenn TARGET==staging — VOR systemctl restart]
  [systemctl restart $SERVICE_NAME]
  [systemctl status $SERVICE_NAME]
ENDHEREDOC
echo "[deploy] Done."
```
  </action>
  <verify>
    <automated>grep "TARGET=\"\${1:?Usage: ./deploy.sh staging|production}\"" deploy.sh &amp;&amp; grep "BLOCKER: Staging not healthy" deploy.sh &amp;&amp; grep "BLOCKER: Staging deploy older than 24h" deploy.sh &amp;&amp; grep "BLOCKER: Local HEAD" deploy.sh &amp;&amp; grep ".deploy_meta" deploy.sh &amp;&amp; grep "nerve_staging" deploy.sh &amp;&amp; grep "jq" deploy.sh &amp;&amp; grep "nginx-production.conf" deploy.sh &amp;&amp; bash -n deploy.sh &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "TARGET=\"\${1:?Usage: ./deploy.sh staging|production}\"" deploy.sh gibt eine Zeile zurueck
    - grep "nerve_vps" deploy.sh gibt mindestens 1 Zeile zurueck (production-SSH-Key)
    - grep "nerve_staging" deploy.sh gibt mindestens 1 Zeile zurueck (staging-SSH-Key)
    - grep "178.104.82.166" deploy.sh gibt mindestens 1 Zeile zurueck (production-IP unveraendert)
    - grep "BLOCKER: Staging not healthy" deploy.sh gibt eine Zeile zurueck
    - grep "BLOCKER: Staging deploy older than 24h" deploy.sh gibt eine Zeile zurueck
    - grep "BLOCKER: Local HEAD" deploy.sh gibt eine Zeile zurueck
    - grep "deploy_meta" deploy.sh gibt mindestens 2 Zeilen zurueck (schreiben + referenz)
    - grep "vor Restart\|vor systemctl\|VOR.*restart\|vor Restart" deploy.sh gibt mindestens 1 Zeile zurueck (REVIEW-HIGH-2 Fix — Kommentar zur Reihenfolge)
    - grep "which jq" deploy.sh gibt eine Zeile zurueck (jq-Pflicht-Check)
    - grep "/etc/nerve/.env" deploy.sh gibt mindestens 1 Zeile zurueck (.env-Check)
    - grep "BLOCKER:.*\.env fehlt" deploy.sh gibt eine Zeile zurueck
    - bash -n deploy.sh gibt exit 0 (Syntax korrekt)
    - grep "TAR_EXCLUDES" deploy.sh gibt mindestens 3 Zeilen zurueck (Array unveraendert)
    - grep "nginx-production.conf" deploy.sh gibt mindestens 1 Zeile zurueck (scp der Production-nginx-Datei)
    - grep "nginx-staging.conf" deploy.sh gibt mindestens 1 Zeile zurueck (scp der Staging-nginx-Datei)
    - grep "2>/dev/null || true" deploy.sh gibt 0 Zeilen zurueck fuer service-unit-scp-Zeilen (kein Fehler-Unterdrucken)
    - grep "if \[\[ \"\$TARGET\" == \"staging\" \]\]" deploy.sh gibt mindestens 2 Zeilen zurueck (Service-Unit + nginx-Block)
    - REVIEW-HIGH-2: Reihenfolge in deploy.sh — deploy_meta-Schreiben kommt BEVOR systemctl restart in der Ausgabe von grep -n "deploy_meta\|systemctl restart" deploy.sh
  </acceptance_criteria>
  <done>deploy.sh akzeptiert staging|production als erstes Argument; kein Default (leerer Aufruf bricht ab); staging-Branch setzt VPS_HOST=root@&lt;STAGING_IP&gt; und SSH_KEY=~/.ssh/nerve_staging; production-Branch fuehrt 3-Check-Gate + jq-Check durch; beide Branches pruefen /etc/nerve/.env; Service-Unit-scp ist TARGET-spezifisch ohne 2>/dev/null || true; nginx-Config wird per scp hochgeladen (deploy/nginx-production.conf fuer production, deploy/nginx-staging.conf fuer staging) — kein verschachtelter nginx-Heredoc; /opt/nerve/.deploy_meta wird VOR systemctl restart nerve-staging geschrieben (REVIEW-HIGH-2 Fix); bash -n gibt exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: /api/health Extension + .env.staging.example</name>
  <files>
    routes/app_routes.py
    .env.staging.example
  </files>
  <read_first>
    - routes/app_routes.py (Zeilen 1375-1432: vollstaendige api_health()-Funktion lesen — bestehende Felder bleiben unveraendert)
  </read_first>
  <action>
**Datei 1: routes/app_routes.py — api_health() Extension**

Erweitere api_health() um git_head und deployed_at Felder. Die bestehende Funktion wird NICHT umgeschrieben — nur zwei Aenderungen:

1. Am Anfang der Funktion (nach den bestehenden Imports `import glob`, `import time`), fuge die .deploy_meta-Lese-Logik ein:

```python
    # Phase 08.23.2.C.1: Deploy-Meta fuer Staging-Gate (D-06)
    # Lese beim Request (nicht beim App-Start) — funktioniert ohne App-Neustart
    deploy_meta_path = '/opt/nerve/.deploy_meta'
    git_head = None
    deployed_at = None
    try:
        with open(deploy_meta_path, 'r') as _f:
            for _line in _f:
                _line = _line.strip()
                if _line.startswith('GIT_HEAD='):
                    git_head = _line[len('GIT_HEAD='):]
                elif _line.startswith('DEPLOYED_AT='):
                    deployed_at = _line[len('DEPLOYED_AT='):]
    except (OSError, IOError):
        # Datei fehlt (lokale Dev-Umgebung) — git_head + deployed_at bleiben None
        pass
```

2. Im return jsonify({...})-Block, die zwei neuen Felder NACH den bestehenden Feldern hinzufuegen:

```python
    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
        'pipeline_status': pipeline_status,
        'pipeline_error_count_10min': pipeline_error_count,
        'git_head': git_head,        # Phase 08.23.2.C.1: SHA oder null (lokale Dev)
        'deployed_at': deployed_at,  # Phase 08.23.2.C.1: ISO-8601 oder null (lokale Dev)
    })
```

Die .deploy_meta-Lese-Logik wird direkt vor dem bestehenden `database_url = os.environ.get(...)` Block eingefuegt (nach dem `import glob` + `import time`).

Konkreter Einbau-Punkt: Nach Zeile `import time` (aktuell Zeile 1388), vor Zeile `database_url = os.environ.get('DATABASE_URL', '')`.

**Datei 2: .env.staging.example**

Erstelle neue Datei .env.staging.example im Projekt-Root (committed ins Repo, NICHT in .gitignore):

```bash
# ── NERVE Staging .env ─────────────────────────────────────────────────────
# Vorlage fuer /etc/nerve/.env auf dem Staging-Server.
# WICHTIG: Diese Datei mit echten Werten befuellen und als /etc/nerve/.env
# auf dem Staging-Server ablegen (SSH). NIEMALS echte Keys committen!
#
# Erstellen: ssh -i ~/.ssh/nerve_staging root@<STAGING_IP>
#            nano /etc/nerve/.env
# Kopiere diesen Inhalt und ersetze alle Platzhalter.

# ── KI-APIs (Staging-Sandbox-Keys — NICHT Production-Keys verwenden) ──────
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE-WITH-STAGING-SANDBOX-KEY
DEEPGRAM_API_KEY=REPLACE-WITH-STAGING-DEEPGRAM-KEY

# ── Stripe (Test-Mode — sk_test_ Prefix ist Pflicht fuer Staging) ─────────
STRIPE_SECRET_KEY=sk_test_REPLACE-WITH-STRIPE-TEST-SECRET-KEY
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE-WITH-STRIPE-TEST-PUBLISHABLE-KEY
STRIPE_WEBHOOK_SECRET=whsec_REPLACE-WITH-STRIPE-TEST-WEBHOOK-SECRET

# ── HTTP-Basic-Auth fuer Staging-nginx (Req-8) ────────────────────────────
# Generieren: openssl rand -hex 12
STAGING_BASIC_AUTH_USER=staging
STAGING_BASIC_AUTH_PASS=REPLACE-WITH-GENERATED-PASSWORD

# ── Datenbank (Postgres auf Staging-Server, Peer-Auth) ────────────────────
DATABASE_URL=postgresql://nerve_app@/nerve

# ── Flask ─────────────────────────────────────────────────────────────────
FLASK_ENV=production
SECRET_KEY=REPLACE-WITH-RANDOM-SECRET-KEY-min-32-chars

# ── CSRF (Staging: aktiv, wie Production) ────────────────────────────────
# WTF_CSRF_ENABLED=False  # NICHT setzen auf Staging — nur lokal

# ── Weitere Vars (analog .env.example) ───────────────────────────────────
# Alle weiteren Variablen die in .env.example stehen hier ebenfalls setzen.
# Pruefen: grep "os.environ.get" config.py
```
  </action>
  <verify>
    <automated>grep "deploy_meta" routes/app_routes.py &amp;&amp; grep "'git_head'" routes/app_routes.py &amp;&amp; grep "'deployed_at'" routes/app_routes.py &amp;&amp; grep "STAGING_BASIC_AUTH_USER" .env.staging.example &amp;&amp; grep "sk_test_" .env.staging.example &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "deploy_meta_path = '/opt/nerve/.deploy_meta'" routes/app_routes.py gibt eine Zeile zurueck
    - grep "git_head" routes/app_routes.py gibt mindestens 3 Zeilen zurueck (Variable, Zuweisung, jsonify)
    - grep "deployed_at" routes/app_routes.py gibt mindestens 3 Zeilen zurueck (Variable, Zuweisung, jsonify)
    - grep "GIT_HEAD=" routes/app_routes.py gibt eine Zeile zurueck (Key-Parsing-Logik)
    - grep "DEPLOYED_AT=" routes/app_routes.py gibt eine Zeile zurueck (Key-Parsing-Logik)
    - grep "backup_status" routes/app_routes.py gibt mindestens 3 Zeilen zurueck (bestehende Felder unveraendert)
    - grep "pipeline_status" routes/app_routes.py gibt mindestens 2 Zeilen zurueck (bestehende Felder unveraendert)
    - grep "STAGING_BASIC_AUTH_USER" .env.staging.example gibt eine Zeile zurueck
    - grep "sk_test_" .env.staging.example gibt mindestens 1 Zeile zurueck (Stripe Test-Mode)
    - grep "DATABASE_URL=postgresql" .env.staging.example gibt eine Zeile zurueck
    - grep "WTF_CSRF_ENABLED" .env.staging.example gibt eine Zeile zurueck (als Kommentar, nicht aktiv)
    - test -f .env.staging.example gibt exit 0
  </acceptance_criteria>
  <done>routes/app_routes.py api_health() gibt git_head (SHA-String oder null) und deployed_at (ISO-8601-String oder null) zurueck; bestehende Felder backup_status, backup_age_hours, pipeline_status, pipeline_error_count_10min sind unveraendert. .env.staging.example ist im Repo-Root, enthaelt alle Pflicht-Variablen mit Kommentaren.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lokal → Staging/Production via SSH | deploy.sh-Aufruf traegt SSH-Key — Key-Kompromittierung = Server-Zugriff |
| Internet → /api/health | Health-Endpoint ist oeffentlich (kein Auth) — gibt git_head preis |
| deploy.sh → Staging Health curl | Production-Gate liest Staging-Antwort — MITM moeglich |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C1-02-1 | Information Disclosure | /api/health (git_head) | accept | git_head ist SHA, kein Secret; gibt kein IP/Infra-Detail preis; Staging-URL oeffentlich aber /api/health liefert kein PII |
| T-C1-02-2 | Tampering | deploy.sh Production-Gate via MITM | accept | SSL-Verifizierung via curl -fsS (default); Let's Encrypt-Cert; Low-value Angriffsszenario Pre-Launch |
| T-C1-02-3 | Elevation of Privilege | .env.staging.example mit Dummy-Keys | mitigate | Datei enthaelt nur Platzhalter (REPLACE-WITH-...); echte Keys niemals committen; README-Kommentar in Datei |
| T-C1-02-4 | Spoofing | deploy.sh TARGET ohne Default | mitigate | `${1:?...}` bricht bei leerem Aufruf ab — kein versehentlicher Production-Deploy moeglich |
| T-C1-02-5 | Denial of Service | deploy.sh staging curl-Timeout | mitigate | `--max-time 10` auf curl-Aufruf — haengt nicht unbegrenzt |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

```bash
# deploy.sh Syntax
bash -n deploy.sh && echo "deploy.sh syntax: OK"

# TARGET-Parameter
grep "TARGET=\"\${1:?Usage: ./deploy.sh staging|production}\"" deploy.sh

# Production-Gate alle 3 Checks
grep "BLOCKER: Staging not healthy" deploy.sh
grep "BLOCKER: Staging deploy older than 24h" deploy.sh
grep "BLOCKER: Local HEAD" deploy.sh

# .deploy_meta
grep "deploy_meta" deploy.sh | wc -l   # >= 2

# REVIEW-HIGH-2: Reihenfolge pruefen (deploy_meta vor systemctl restart)
grep -n "deploy_meta\|systemctl restart" deploy.sh
# deploy_meta-Zeile muss KLEINERE Zeilennummer haben als systemctl restart

# nginx scp (beide Targets, kein inline-Heredoc)
grep "nginx-production.conf" deploy.sh   # >= 1 Zeile (scp)
grep "nginx-staging.conf" deploy.sh      # >= 1 Zeile (scp)

# Service-Unit-scp TARGET-spezifisch, kein Fehler-Unterdrucken
grep "2>/dev/null || true" deploy.sh | grep -i "service" | wc -l  # muss 0 sein

# /api/health neue Felder
grep "git_head" routes/app_routes.py | wc -l   # >= 3
grep "deployed_at" routes/app_routes.py | wc -l  # >= 3

# .env.staging.example
grep "STAGING_BASIC_AUTH" .env.staging.example
grep "sk_test_" .env.staging.example
```
</verification>

<success_criteria>
- deploy.sh: TARGET="${1:?...}" ganz oben; staging/production-Branch; jq-Check; 3 Gate-Checks (status, deployed_at-Alter, git_head); .env-Check beide Targets; Service-Unit-scp TARGET-spezifisch ohne 2>/dev/null || true; nginx-Config per scp fuer beide Targets (nginx-production.conf + nginx-staging.conf) — kein verschachtelter nginx-Heredoc; .deploy_meta-Schreiben VOR systemctl restart (REVIEW-HIGH-2 Fix); bash -n exit 0
- routes/app_routes.py: api_health() liest /opt/nerve/.deploy_meta; gibt git_head + deployed_at zurueck; bestehende Felder unveraendert
- .env.staging.example: Im Repo-Root; alle Pflicht-Variablen mit Kommentaren; sk_test_-Hinweis fuer Stripe; WTF_CSRF_ENABLED als Kommentar (nicht aktiv)
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.23.2.C.1-staging-server-deploy-workflow/08.23.2.C.1-02-SUMMARY.md` mit:
- Welche Zeilen in deploy.sh veraendert wurden (Zeilen-Ranges)
- Welche Felder in api_health() hinzugefuegt wurden
- Bestaetigung REVIEW-HIGH-2 Fix: deploy_meta-Schreibzeile hat kleinere Zeilennummer als systemctl-restart-Zeile
- Staging-IP-Platzhalter &lt;STAGING_IP&gt; in deploy.sh (Andre muss nach Hetzner-Provisionierung ersetzen)
</output>
