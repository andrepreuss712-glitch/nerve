---
phase: 08.23.2.C.1
plan: 04
type: execute
wave: 2
depends_on:
  - 08.23.2.C.1-01
files_modified:
  - alembic/env.py
  - app.py
autonomous: true
requirements:
  - Req-10
  - Req-11

must_haves:
  truths:
    - "alembic/env.py enthaelt render_as_batch=True in run_migrations_online() (REVIEW-MEDIUM-5)"
    - "app.py fuehrt automatisch Alembic-Migrationen aus wenn DATABASE_URL mit sqlite:// beginnt"
    - "app.py verwendet Alembic Python API (nicht subprocess) mit explizitem config-Pfad (REVIEW-MEDIUM-4)"
    - "app.py ueberspringt den Alembic-Hook wenn DATABASE_URL mit postgresql beginnt"
    - "Der CSRF-Patch in app.py Zeilen 54-58 (Commit aae9aa8) ist unveraendert"
    - "app.py startet lokal ohne Crash nach dem Hook-Einbau"
  artifacts:
    - path: "alembic/env.py"
      provides: "Alembic-Konfiguration mit render_as_batch=True fuer SQLite-Kompatibilitaet"
      contains: "render_as_batch=True"
    - path: "app.py"
      provides: "Alembic-Auto-Hook fuer SQLite-Entwicklung via Python API"
      contains: "alembic_command.upgrade"
  key_links:
    - from: "app.py Alembic-Hook"
      to: "alembic/env.py"
      via: "AlembicConfig(os.path.join(..., 'alembic.ini')) — expliziter Pfad"
      pattern: "AlembicConfig.*alembic.ini"
    - from: "alembic/env.py run_migrations_online()"
      to: "SQLite-Tabellen (ALTER TABLE)"
      via: "render_as_batch=True — Alembic emuliert ALTER TABLE via CREATE+COPY+DROP"
      pattern: "render_as_batch"
---

<objective>
Zwei Fixes bevor der Alembic-Auto-Hook in app.py eingebaut wird: (1) render_as_batch=True in alembic/env.py sicherstellen damit SQLite-Migrationen mit ALTER TABLE nicht crashen, (2) app.py Alembic-Hook mit Python API statt subprocess implementieren — mit explizitem alembic.ini-Pfad der unabhaengig vom CWD funktioniert.

Purpose: Ohne render_as_batch=True crasht alembic.command.upgrade() bei Migrationen die Spalten zu bestehenden SQLite-Tabellen hinzufuegen (NotImplementedError). Ohne expliziten CWD-Pfad schlaegt die Python-API-Konfiguration fehl wenn app.py nicht aus /opt/nerve/app gestartet wird.

Output: Korrigiertes alembic/env.py + modifiziertes app.py mit robustem Alembic-Hook
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- Aktueller Stand alembic/env.py (vollstaendig gelesen): -->

# run_migrations_online() hat context.configure() MIT compare_type=True aber OHNE render_as_batch.
# render_as_batch=True muss in context.configure() in run_migrations_online() eingefuegt werden.
# run_migrations_offline() benoetigt es ebenfalls fuer Konsistenz.

# Aktueller run_migrations_online():
def run_migrations_online() -> None:
    connectable = engine_from_config(...)
    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=target_metadata,
            compare_type=True,
            # render_as_batch FEHLT hier — REVIEW-MEDIUM-5
        )
        with context.begin_transaction():
            context.run_migrations()

<!-- Alembic Python API Verwendung (REVIEW-MEDIUM-4 Fix): -->
# Statt: subprocess.run(['alembic', 'upgrade', 'head'], check=True)
# Besser: Alembic Python API mit explizitem config-Pfad
from alembic.config import Config as AlembicConfig
from alembic import command as alembic_command
_app_dir = os.path.dirname(os.path.abspath(__file__))
alembic_cfg = AlembicConfig(os.path.join(_app_dir, 'alembic.ini'))
alembic_command.upgrade(alembic_cfg, 'head')
# Vorteil: kein PATH-Lookup fuer 'alembic' Binary, kein CWD-Problem

<!-- CSRF-Patch (MUSS unveraendert bleiben): -->
# Zeilen 54-58 in app.py:
# if os.environ.get('WTF_CSRF_ENABLED', 'True').lower() == 'false':
#     app.config['WTF_CSRF_ENABLED'] = False
#     print('[CSRF] DISABLED — local dev mode (WTF_CSRF_ENABLED=False in .env)')

<!-- Etabliertes Pattern fuer SQLite-Erkennung (aus bestehender _migrate()): -->
# db_url = str(engine.url)
# if db_url.startswith('postgresql'):
#     print('[DB] _migrate skipped on Postgres ...')
#     return
# --> Analoges Pattern fuer Alembic-Hook verwenden
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: render_as_batch=True in alembic/env.py (REVIEW-MEDIUM-5 Precondition)</name>
  <files>alembic/env.py</files>
  <read_first>
    - alembic/env.py (vollstaendig lesen — context.configure()-Aufrufe in beiden Funktionen lokalisieren)
  </read_first>
  <action>
REVIEW-MEDIUM-5 FIX: Alembic-Migrationen die Spalten zu bestehenden SQLite-Tabellen hinzufuegen
(z.B. ALTER TABLE phrases ADD COLUMN quality_tier, ADD COLUMN mode) erfordern render_as_batch=True.

Ohne render_as_batch=True wirft SQLite eine NotImplementedError bei ALTER TABLE — die Alembic-Migration
crasht, und damit auch der Auto-Hook in Task 1 beim App-Start.

render_as_batch=True bewirkt dass Alembic SQLite-ALTER-TABLE als CREATE+COPY+DROP emuliert.

**Aenderung: render_as_batch=True in run_migrations_online() einfuegen**

In der Funktion `run_migrations_online()`, im `context.configure()`-Aufruf, `render_as_batch=True`
als neuen Parameter hinzufuegen:

```python
def run_migrations_online() -> None:
    connectable = engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix='sqlalchemy.',
        poolclass=pool.NullPool,
    )
    with connectable.connect() as connection:
        context.configure(
            connection=connection,
            target_metadata=target_metadata,
            compare_type=True,
            render_as_batch=True,  # REVIEW-MEDIUM-5: SQLite ALTER TABLE via CREATE+COPY+DROP
        )
        with context.begin_transaction():
            context.run_migrations()
```

**Aenderung: render_as_batch=True auch in run_migrations_offline() einfuegen**

Fuer Konsistenz (offline-Modus fuer Skript-Generierung):

```python
def run_migrations_offline() -> None:
    url = config.get_main_option('sqlalchemy.url')
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={'paramstyle': 'named'},
        compare_type=True,
        render_as_batch=True,  # REVIEW-MEDIUM-5: SQLite ALTER TABLE via CREATE+COPY+DROP
    )
    with context.begin_transaction():
        context.run_migrations()
```

Alle anderen Zeilen in alembic/env.py bleiben unveraendert.
  </action>
  <verify>
    <automated>grep "render_as_batch=True" alembic/env.py | wc -l | grep -qE "^[2-9]" &amp;&amp; python -m py_compile alembic/env.py &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "render_as_batch=True" alembic/env.py gibt mindestens 2 Zeilen zurueck (run_migrations_online + run_migrations_offline)
    - python -m py_compile alembic/env.py gibt exit 0 (Syntax korrekt)
    - grep "compare_type=True" alembic/env.py gibt mindestens 2 Zeilen zurueck (unveraendert vorhanden)
    - grep "target_metadata" alembic/env.py gibt mindestens 3 Zeilen zurueck (Definition + beide Funktionen, unveraendert)
    - grep "DATABASE_URL" alembic/env.py gibt mindestens 1 Zeile zurueck (env-Lese-Logik unveraendert)
  </acceptance_criteria>
  <done>alembic/env.py enthaelt render_as_batch=True in context.configure() in beiden Funktionen (run_migrations_online + run_migrations_offline). SQLite-Migrationen die Spalten zu bestehenden Tabellen hinzufuegen (ALTER TABLE) koennen jetzt ohne NotImplementedError ausgefuehrt werden. python -m py_compile exit 0.</done>
</task>

<task type="auto">
  <name>Task 1: Alembic-Auto-Hook in app.py via Python API + CSRF-Patch-Verifikation</name>
  <files>app.py</files>
  <read_first>
    - app.py (Zeilen 1-60 fuer CSRF-Patch-Lage + Import-Block; Zeilen 119-200+ fuer init_db/_migrate-Block und korrekten Einbau-Punkt)
  </read_first>
  <action>
**Schritt 1: CSRF-Patch (Z.54-58) lokalisieren und NICHT veraendern**

Vor jeder Aenderung: Zeilen 54-58 lesen und sicherstellen dass sie unveraendert bleiben:
```python
if os.environ.get('WTF_CSRF_ENABLED', 'True').lower() == 'false':
    app.config['WTF_CSRF_ENABLED'] = False
    print('[CSRF] DISABLED — local dev mode (WTF_CSRF_ENABLED=False in .env)')
```
Diese Zeilen werden in diesem Plan NICHT beruehrt.

**Schritt 2: Einbau-Punkt finden**

Lies app.py Zeilen 119-220 um den exakten Ort nach dem `_migrate()`-Aufruf zu finden.
Suche nach dem Muster `_migrate()` (Aufruf, nicht Definition).

**Schritt 3: Alembic-Auto-Hook einfuegen (REVIEW-MEDIUM-4: Python API statt subprocess)**

REVIEW-MEDIUM-4 FIX: `subprocess.run(['alembic', 'upgrade', 'head'])` hat zwei Probleme:
1. Haengt vom PATH ab (alembic-Binary muss auffindbar sein)
2. Haengt vom CWD ab (alembic.ini muss im aktuellen Verzeichnis liegen)

Fix: Alembic Python API mit explizitem os.path.abspath(__file__)-basiertem Pfad zu alembic.ini.
Der Pfad ist absolut und funktioniert unabhaengig vom CWD beim App-Start.

Fuge NACH dem `_migrate()`-Aufruf folgenden Block ein (als neuen Abschnitt mit Separator-Kommentar):

```python
# ── Alembic-Auto-Hook (SQLite-only, Phase 08.23.2.C.1 Req-10) ────────────────
# Fuehrt automatisch Alembic-Migrationen aus wenn DATABASE_URL auf SQLite zeigt.
# Postgres-DBs werden von deploy.sh verwaltet — niemals von app.py.
# Idempotent durch Alembic's alembic_version-Tabelle (keine Aktion wenn Schema aktuell).
#
# REVIEW-MEDIUM-4 FIX: Python API statt subprocess.run(['alembic', ...])
# - Kein PATH-Lookup fuer alembic-Binary erforderlich
# - Expliziter alembic.ini-Pfad via os.path.abspath(__file__) — CWD-unabhaengig
# - Funktioniert auch wenn app.py aus einem anderen Verzeichnis gestartet wird
_db_url_str = str(engine.url)
if _db_url_str.startswith('sqlite'):
    try:
        from alembic.config import Config as AlembicConfig
        from alembic import command as alembic_command
        _app_dir = os.path.dirname(os.path.abspath(__file__))
        _alembic_cfg = AlembicConfig(os.path.join(_app_dir, 'alembic.ini'))
        alembic_command.upgrade(_alembic_cfg, 'head')
        print('[DB] Alembic upgrade head: abgeschlossen (Schema aktuell oder migriert)')
    except Exception as _e:
        print(f'[DB] Alembic upgrade head FEHLER: {_e}')
        raise
else:
    print('[DB] Alembic-Hook uebersprungen (Postgres — Schema via deploy.sh verwaltet)')
```

**Schritt 4: os bereits in Imports vorhanden pruefen**

`os` ist typischerweise bereits importiert in app.py. Falls nicht, am Dateianfang `import os`
hinzufuegen. `alembic.config` und `alembic.command` werden lokal im try-Block importiert —
kein globaler Import erforderlich.

**Einbau-Punkt-Praezision:** Der Hook kommt NACH dem letzten `_migrate()`-Aufruf und VOR den
Blueprint-Registrierungen (`app.register_blueprint(...)`). Suche nach `_migrate()` als Aufruf
(nicht Definition) und fuge den Block direkt darunter ein.

**Warum kein FileNotFoundError-Catch separat:** Die alembic.config.Config()-Klasse wirft
einen regulaeren Exception wenn alembic.ini nicht gefunden wird — das `except Exception`
fangt diesen Fall ab und gibt eine Fehlermeldung aus bevor raise den App-Start abbricht.
Das ist korrektes Verhalten: fehlendes alembic.ini = Konfigurationsfehler = App soll nicht starten.
  </action>
  <verify>
    <automated>python -m py_compile app.py &amp;&amp; grep "alembic_command.upgrade" app.py &amp;&amp; grep "AlembicConfig" app.py &amp;&amp; grep "os.path.abspath(__file__)" app.py &amp;&amp; grep "startswith('sqlite')" app.py &amp;&amp; grep "WTF_CSRF_ENABLED" app.py &amp;&amp; python -c "import ast; ast.parse(open('app.py').read()); print('AST OK')"</automated>
  </verify>
  <acceptance_criteria>
    - python -m py_compile app.py gibt exit 0 (Syntax korrekt)
    - python -c "import ast; ast.parse(open('app.py').read())" gibt exit 0 (AST valide)
    - grep "alembic_command.upgrade" app.py gibt mindestens 1 Zeile zurueck (REVIEW-MEDIUM-4: Python API)
    - grep "AlembicConfig" app.py gibt mindestens 1 Zeile zurueck (Python API import)
    - grep "os.path.abspath(__file__)" app.py gibt mindestens 1 Zeile zurueck (REVIEW-MEDIUM-4: CWD-unabhaengiger Pfad)
    - grep "alembic.ini" app.py gibt mindestens 1 Zeile zurueck (expliziter Pfad zu alembic.ini)
    - grep "subprocess" app.py | grep -i "alembic" gibt 0 Zeilen zurueck (kein subprocess fuer Alembic — REVIEW-MEDIUM-4)
    - grep "_db_url_str.startswith('sqlite')" app.py gibt eine Zeile zurueck
    - grep "Postgres — Schema via deploy.sh" app.py gibt eine Zeile zurueck (Postgres-Skip-Log)
    - grep "WTF_CSRF_ENABLED" app.py gibt mindestens 2 Zeilen zurueck (CSRF-Block unveraendert)
    - grep "CSRF.*DISABLED" app.py gibt eine Zeile zurueck (CSRF-Print-Statement unveraendert)
    - git diff HEAD -- app.py | grep "^-.*WTF_CSRF_ENABLED\|^+.*WTF_CSRF_ENABLED" gibt 0 Zeilen zurueck (CSRF-Zeilen unveraendert)
    - grep "Alembic-Auto-Hook" app.py gibt mindestens 1 Zeile zurueck (Separator-Kommentar vorhanden)
    - grep "REVIEW-MEDIUM-4" app.py gibt mindestens 1 Zeile zurueck (Fix dokumentiert im Kommentar)
  </acceptance_criteria>
  <done>app.py enthaelt Alembic-Auto-Hook nach _migrate()-Aufruf; Hook verwendet Alembic Python API (AlembicConfig + alembic_command.upgrade) statt subprocess; alembic.ini-Pfad ist absolut via os.path.dirname(os.path.abspath(__file__)) — CWD-unabhaengig; nur bei sqlite://-URL ausgefuehrt; Postgres wird uebersprungen; CSRF-Patch Z.54-58 unveraendert; python -m py_compile exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.py → alembic Python API | Direkter Python-Import — kein externer Prozess, kein PATH-Lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C1-04-1 | Elevation of Privilege | alembic_command.upgrade (Python API) | mitigate | Nur bei sqlite:// — nie auf Production; Exception-Handler bricht App-Start ab bei Fehler |
| T-C1-04-2 | Denial of Service | Alembic-Upgrade blockiert App-Start | accept | Alembic-Migrationslaufzeit ist < 1s fuer kleine DBs; idempotent bei aktuellem Schema; nur lokal |
| T-C1-04-3 | Tampering | CSRF-Patch unveraendert (Req-11) | mitigate | Acceptance-Criterion: git diff zeigt keine CSRF-Zeilen-Aenderungen — automatisch pruefbar |
| T-C1-04-4 | Denial of Service | render_as_batch fehlend (REVIEW-MEDIUM-5) | mitigate | render_as_batch=True in Task 0 sicherstellen — verhindert NotImplementedError bei SQLite ALTER TABLE |
</threat_model>

<verification>
Nach Task-Abschluss:

```bash
# alembic/env.py — render_as_batch vorhanden (REVIEW-MEDIUM-5)
grep "render_as_batch=True" alembic/env.py | wc -l  # muss >= 2 sein
python -m py_compile alembic/env.py && echo "env.py syntax: OK"

# app.py — Syntax
python -m py_compile app.py && echo "app.py Syntax OK"

# Alembic Python API (REVIEW-MEDIUM-4)
grep "alembic_command.upgrade" app.py
grep "AlembicConfig" app.py
grep "os.path.abspath(__file__)" app.py

# Kein subprocess fuer Alembic
grep "subprocess" app.py | grep -i "alembic" | wc -l  # muss 0 sein

# SQLite-Only-Check
grep "startswith('sqlite')" app.py

# CSRF unveraendert
git diff HEAD -- app.py | grep "WTF_CSRF" | head -5
# Erwartetes Ergebnis: keine Zeilen

# AST-Validierung
python -c "import ast; ast.parse(open('app.py').read()); print('AST OK')"
```
</verification>

<success_criteria>
- alembic/env.py: render_as_batch=True in context.configure() in run_migrations_online() UND run_migrations_offline(); python -m py_compile exit 0 (REVIEW-MEDIUM-5 Fix)
- app.py: Alembic-Hook nach _migrate()-Aufruf; verwendet AlembicConfig + alembic_command.upgrade (Python API, kein subprocess); alembic.ini-Pfad via os.path.abspath(__file__) — CWD-unabhaengig (REVIEW-MEDIUM-4 Fix); sqlite://-Check; Postgres-Skip; python -m py_compile exit 0
- CSRF-Patch (Z.54-58, Commit aae9aa8): git diff zeigt keine Aenderungen an diesen Zeilen
- Req-11 Acceptance: grep "WTF_CSRF_ENABLED" app.py gibt identische Zeilen wie vor dem Edit
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.23.2.C.1-staging-server-deploy-workflow/08.23.2.C.1-04-SUMMARY.md` mit:
- render_as_batch=True: Zeilennummern in alembic/env.py (beide Funktionen)
- Exaktem Einbau-Punkt (Zeile) des Alembic-Hooks in app.py
- Bestaetigung REVIEW-MEDIUM-4: AlembicConfig-Pfad (kein subprocess)
- Bestaetigung REVIEW-MEDIUM-5: render_as_batch=True vorhanden
- Bestaetigung: CSRF-Patch Z.54-58 unveraendert (git diff Ausgabe)
- Hinweis: lokales Testen mit DATABASE_URL=sqlite:///database/salesnerve.db python app.py
</output>
