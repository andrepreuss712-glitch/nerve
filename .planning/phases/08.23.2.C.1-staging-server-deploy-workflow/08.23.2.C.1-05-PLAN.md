---
phase: 08.23.2.C.1
plan: 05
type: execute
wave: 3
depends_on:
  - 08.23.2.C.1-01
  - 08.23.2.C.1-02
files_modified:
  - Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md
autonomous: false
requirements:
  - Req-9
  - Req-11

must_haves:
  truths:
    - "DSGVO Analyse.md Sektion 8 enthaelt den Staging-Datenstrategie-Eintrag mit DSGVO-Trigger"
    - "PiP-Live-Test manuell durch Andre auf staging.getnerve.app bestanden"
    - "CSRF-Patch app.py Z.54-58 ist verifiziert unveraendert (automatisch geprueft)"
  artifacts:
    - path: "Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md"
      provides: "DSGVO-Trigger fuer Staging-DB-Refresh dokumentiert"
      contains: "staging"
  key_links:
    - from: "NERVE DSGVO Analyse.md §8"
      to: "scripts/refresh_staging_from_production.sh"
      via: "Trigger-Verweis: erstem externen User → Anonymisierung Pflicht"
      pattern: "refresh_staging_from_production"
---

<objective>
DSGVO Analyse.md Sektion 8 mit Staging-Datenstrategie und DSGVO-Trigger aktualisieren. Manuellen PiP-Live-Test als Checkpoint dokumentieren. CSRF-Patch automated pruefen.

Purpose: Req-9 (PiP-Live-Test auf Staging) und DSGVO-Compliance-Dokumentation (Constraint aus SPEC.md) abschliessen. Dieser Plan laeuft nach Plan 02 (Staging muss laufen) und enthaelt einen checkpoint:human-verify fuer den manuellen Browser-Test.

Output: Aktualisiertes DSGVO Analyse.md; manuell bestandener PiP-Live-Test durch Andre; automatischer CSRF-Grep-Check
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- DSGVO Analyse.md Sektion 8 — bestehenden Inhalt lesen bevor Ergaenzung -->
<!-- Ziel: Paragraph zu Staging-Datenstrategie hinzufuegen mit konkretem Trigger -->

<!-- CSRF-Patch Zeilen 54-58 in app.py (Referenz fuer Verifikation): -->
# if os.environ.get('WTF_CSRF_ENABLED', 'True').lower() == 'false':
#     app.config['WTF_CSRF_ENABLED'] = False
#     print('[CSRF] DISABLED — local dev mode (WTF_CSRF_ENABLED=False in .env)')

<!-- PiP-Live-Test Acceptance Criteria (aus SPEC.md Req-9): -->
<!-- (1) Ctrl+G zeigt Gatekeeper-Buttons + Vorzimmer-Indikator -->
<!-- (2) Ctrl+E wechselt zurueck zu Standard-EWB-Buttons -->
<!-- (3) _showUwgBanner('Test-UWG-Phrase') in Browser-Console triggert rotes UWG-Banner -->
<!-- (4) keine JS-Fehler in Browser-Console waehrend aller drei Schritte -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DSGVO Analyse.md Sektion 8 — Staging-Datenstrategie-Eintrag + CSRF-Grep-Check</name>
  <files>Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md</files>
  <read_first>
    - Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md (vollstaendig lesen — Sektion 8 lokalisieren und bestehenden Inhalt verstehen bevor Ergaenzung)
    - app.py (Zeilen 54-58: CSRF-Patch fuer Verifikation lesen)
  </read_first>
  <action>
**Teil A: DSGVO Analyse.md aktualisieren**

Lokalisiere Sektion 8 in Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md. Fuge am Ende von Sektion 8 (oder erstelle Sektion 8 falls noch nicht vorhanden) folgenden Eintrag ein:

```markdown
### §8.3 Staging-Datenstrategie (Phase 08.23.2.C.1)

**Stand:** 2026-05-19
**Status:** KONFORM (Pre-Launch-Phase)

**Aktuelle Strategie:** 1:1-Kopie der Production-Datenbank auf Staging via `scripts/refresh_staging_from_production.sh`.

**DSGVO-Konformitaet (aktuell):** Solange kein externer Early-Access-User registriert ist, enthaelt die Production-DB ausschliesslich:
- Andre Preuss (Einzelunternehmer, Verantwortlicher) — kein Dritter
- Post-Phase-B-anonymisierte Anruf-Mitschnitte (keine Klarnamen)
- Hetzner-Server: EU-Frankfurt (beide Production + Staging) — kein Drittland-Transfer

Beide Server sind im Hetzner-Rechenzentrum Frankfurt (EEA). 1:1-Kopie ist damit DSGVO-konform.

**PFLICHT-TRIGGER: Erster externer Early-Access-User**

Sobald der erste externe User sich registriert (jemand anderes als Andre Preuss):
- `scripts/refresh_staging_from_production.sh` MUSS vor dem naechsten DB-Refresh ANONYMISIEREN
- Anonymisierungs-Pflichtfelder: `users.email`, `users.vorname`, `users.nachname`, `organisations.name`
- Bis zur Anonymisierungs-Implementierung: DB-Refresh auf Staging SPERREN
- Zustaendig: Entwickler (Andre Preuss) — keine automatische Sperre

**Umzusetzende Massnahme (Folge-Phase):**
Mini-Phase 08.23.2.C.1.2 "DSGVO-Anonymisierung Staging-Refresh":
- `refresh_staging_from_production.sh` erweitern: nach pg_dump/psql-Import UPDATE-Statements fuer PII-Felder
- Muster: `UPDATE users SET email = 'user' || id || '@staging.test', vorname = 'Test', nachname = 'User'`

**Naechste Review:** Bei erstem externen User-Registrierung (Slack/E-Mail-Benachrichtigung als Trigger).
```

**Teil B: CSRF-Patch automated Verifikation**

Fuehre nach dem DSGVO-Eintrag folgenden Shell-Befehl aus und dokumentiere das Ergebnis im SUMMARY:

```bash
grep -n "WTF_CSRF_ENABLED" app.py
```

Erwartetes Ergebnis: Mindestens 2 Zeilen, davon eine mit `os.environ.get('WTF_CSRF_ENABLED'` und eine mit `app.config['WTF_CSRF_ENABLED'] = False`.

Falls git verfuegbar:
```bash
git show aae9aa8:app.py | grep -n "WTF_CSRF_ENABLED" 2>/dev/null || echo "Commit-Hash pruefen nicht moeglich"
git diff aae9aa8 -- app.py | grep "WTF_CSRF_ENABLED" | head -5
```

Wenn grep `WTF_CSRF_ENABLED` in app.py mindestens 2 Zeilen zurueckgibt UND git diff keine +/--Zeilen fuer CSRF-Block zeigt: Req-11 bestanden.
  </action>
  <verify>
    <automated>grep "staging" "Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md" | grep -i "refresh\|anonymisier\|trigger" &amp;&amp; grep -n "WTF_CSRF_ENABLED" app.py | wc -l | grep -qE "^[2-9]" &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -i "staging" "Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md" gibt mindestens 3 Zeilen zurueck (Sektion + Trigger + refresh_staging-Verweis)
    - grep "refresh_staging_from_production" "Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md" gibt mindestens 1 Zeile zurueck (konkreter Skript-Verweis)
    - grep -i "trigger\|TRIGGER\|Pflicht" "Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md" gibt mindestens 1 Zeile in der Sektion zurueck
    - grep -i "anonymisier" "Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md" gibt mindestens 2 Zeilen zurueck
    - grep -n "WTF_CSRF_ENABLED" app.py gibt mindestens 2 Zeilen zurueck (CSRF-Block unveraendert vorhanden)
    - grep "app.config\['WTF_CSRF_ENABLED'\] = False" app.py gibt eine Zeile zurueck
  </acceptance_criteria>
  <done>NERVE DSGVO Analyse.md Sektion 8 enthaelt Staging-Datenstrategie-Eintrag mit: aktuellem DSGVO-Konformitaets-Status, konkretem Pflicht-Trigger (erster externer User), Verweis auf refresh_staging_from_production.sh, Anonymisierungs-Pflichtfelder. CSRF-Patch Z.54-58 verifiziert unveraendert via grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manueller PiP-Live-Test auf staging.getnerve.app (Req-9)</name>
  <what-built>
Plan 01 hat die Staging-Server-Artefakte erstellt. Plan 02 hat deploy.sh refactored. Nach manuellem RUNBOOK-Ausfuehren und `./deploy.sh staging` laeuft die App auf staging.getnerve.app.

Dieser Checkpoint fuehrt den in Phase 08.23.2.C Plan 07 deferrierten PiP-Live-Test durch — jetzt auf Staging statt Production.
  </what-built>
  <how-to-verify>
**Voraussetzungen (zuerst pruefen):**

```bash
# Health-Check OHNE Basic-Auth (nginx hat auth_basic off fuer /api/health — Plan 01 Fix)
curl -fsS https://staging.getnerve.app/api/health
# Erwartete Antwort: {"status": "ok", "git_head": "<SHA>", "deployed_at": "<ISO-8601>", ...}
```

**Browser-Test-Schritte (Chrome/Firefox, DevTools-Console offen halten):**

1. Oeffne https://staging.getnerve.app/live (Basic-Auth-Prompt: staging / &lt;PASS&gt;)
2. Starte eine Live-Session (oder navigiere in eine bestehende)
3. Oeffne Browser-DevTools → Console-Tab

**Test A — Ctrl+G (Gatekeeper-Modus):**
- Druecke `Ctrl+G` in der Live-Session
- ERWARTET: Gatekeeper-Buttons werden sichtbar (z.B. "Gatekeeper einladen", "Vorzimmer"-Indikator)
- ERWARTET: Kein JS-Fehler in Console

**Test B — Ctrl+E (Standard-EWB-Buttons):**
- Druecke `Ctrl+E` in der Live-Session
- ERWARTET: Zurueck zu Standard-EWB-Buttons (Gatekeeper-UI verschwindet)
- ERWARTET: Kein JS-Fehler in Console

**Test C — UWG-Banner:**
- In Browser-Console eingeben: `_showUwgBanner('Test-UWG-Phrase')`
- ERWARTET: Rotes UWG-Banner erscheint im Interface mit Text "Test-UWG-Phrase"
- ERWARTET: Kein JS-Fehler in Console

**Test D — Console-Inspektion:**
- Alle drei Tests bestanden ohne rote Fehler in Console
- Warnungen (gelb) sind OK — nur Errors (rot) sind Blocker

**Falls ein Test fehlschlaegt (Fehlerpfad — LOW-Finding Fix):**
- JS-Fehler aus Console kopieren
- Screenshot des fehlerhaften Zustands erstellen
- Tippe `failed: [Beschreibung des Fehlers + Console-Output]`
- Req-9 gilt NICHT als bestanden — `/gsd-debug` Task oeffnen um Root Cause zu untersuchen
- Req-9 darf in der Phase-Abschluss-Checkliste NICHT als erledigt markiert werden bis der Debug-Task abgeschlossen ist
  </how-to-verify>
  <resume-signal>
Tippe "approved" wenn alle vier Tests (A, B, C, D) bestanden.
Tippe "failed: [Beschreibung]" wenn ein Test fehlschlaegt (mit Console-Error oder Screenshot-Beschreibung). Bei "failed" wird Req-9 NICHT als abgeschlossen markiert — naechster Schritt ist /gsd-debug.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging-Browser-Session | Manueller Test auf staging.getnerve.app mit Basic-Auth |
| DSGVO Analyse.md | Compliance-Dokumentation — kein technischer Boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C1-05-1 | Information Disclosure | PiP-Test auf Staging mit Production-Daten | mitigate | DSGVO-Trigger in §8.3 dokumentiert; Pre-Launch kein externer User; Test-Daten sind Andre-eigene Daten |
| T-C1-05-2 | Tampering | DSGVO Analyse.md Eintrag unvollstaendig | mitigate | Acceptance Criteria pruefen Schluesselbegriffe: trigger, anonymisier, refresh_staging |
</threat_model>

<verification>
Nach Task 1 (automatisch pruefbar):

```bash
# DSGVO-Eintrag
grep -i "staging\|refresh_staging\|trigger\|anonymisier" \
  "Nerve-Vault/04 Entscheidungen/NERVE DSGVO Analyse.md" | wc -l
# Erwartung: >= 5 Zeilen

# CSRF-Patch unveraendert
grep -n "WTF_CSRF_ENABLED" app.py
# Erwartung: 2 Zeilen (os.environ.get + app.config)

# Req-11: git diff
git diff aae9aa8 -- app.py | grep "WTF_CSRF" | head -3
# Erwartung: 0 Zeilen (keine Aenderungen am CSRF-Block)
```

Nach Task 2 (manuell durch Andre):
- "approved" Signal vom User
- Bei "failed": kein Phase-Abschluss fuer Req-9 — /gsd-debug oeffnen
</verification>

<success_criteria>
- NERVE DSGVO Analyse.md §8 enthaelt Staging-Datenstrategie mit Trigger-Verweis auf ersten externen User und refresh_staging_from_production.sh
- PiP-Live-Test manuell bestanden: Ctrl+G (Gatekeeper-UI), Ctrl+E (EWB-UI), _showUwgBanner('Test-UWG-Phrase') (rotes Banner), keine JS-Errors — ODER bei Fehlschlag: /gsd-debug-Task geoeffnet, Req-9 explizit als offen markiert
- CSRF-Patch Z.54-58: grep gibt mindestens 2 WTF_CSRF_ENABLED-Zeilen zurueck; git diff aae9aa8 zeigt keine Aenderungen
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.23.2.C.1-staging-server-deploy-workflow/08.23.2.C.1-05-SUMMARY.md` mit:
- Datum und Ergebnis des PiP-Live-Tests (Andre's Resume-Signal: "approved" oder "failed: ...")
- Bei "failed": Fehlerbeschreibung + Verweis auf /gsd-debug-Task
- DSGVO Analyse.md Sektion + Zeilennummer des neuen Eintrags
- CSRF-Grep-Ausgabe (Bestaetigung Req-11)
- Phase-Abschluss-Status: alle 11 Requirements bestanden (oder Req-9 offen mit Debug-Task-Referenz)
</output>
