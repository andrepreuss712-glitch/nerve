---
phase: 08.23.2.C.1
plan: "02"
subsystem: infra
tags: [staging, deploy, bash, nginx, systemd, health-check, production-gate]

dependency_graph:
  requires:
    - phase: 08.23.2.C.1-01
      provides: deploy/nginx-staging.conf, deploy/nginx-production.conf, deploy/nerve-staging.service
  provides:
    - deploy.sh (TARGET-Parameter staging|production, Production-Pre-Deploy-Gate, deploy_meta-Schreiben)
    - routes/app_routes.py api_health() mit git_head + deployed_at Feldern
    - .env.staging.example (committed Template fuer /etc/nerve/.env auf Staging)
  affects:
    - Plan 03 (deploy.sh staging kann jetzt ausgefuehrt werden nach Hetzner-Provisionierung)
    - Plan 05 (PiP-Test setzt laufenden Staging-Server mit /api/health voraus)

tech_stack:
  added: []
  patterns:
    - TARGET-Parameter-Pattern in Bash (kein Default — kein versehentlicher Prod-Deploy)
    - Production-Gate via curl + jq auf /api/health des Staging-Servers
    - deploy_meta-Datei als Deployment-Timestamping-Mechanismus (ausserhalb App-Code)
    - scp-basierter nginx-Config-Upload statt verschachteltem Heredoc (verhindert $-Expansion)

key_files:
  created:
    - .env.staging.example
  modified:
    - deploy.sh
    - routes/app_routes.py

key_decisions:
  - "ENDHEREDOC ohne Quotes — TARGET und GIT_HEAD_LOCAL werden lokal expandiert, server-seitige Variablen mit \\$ escaped"
  - ".deploy_meta VOR systemctl restart schreiben (REVIEW-HIGH-2 Fix) — Zeile 182 vs 187"
  - "nginx-Config per scp fuer beide Targets statt inline-Heredoc im SSH-Heredoc — konsistent mit Plan-01-Entscheidung fuer nginx-production.conf"
  - "api_health() liest .deploy_meta beim Request (nicht beim App-Start) — kein Neustart nach erstem Deploy noetig"

requirements-completed: [Req-2, Req-3, Req-4, Req-5]

duration: 15min
completed: "2026-05-19"
---

# Phase 08.23.2.C.1 Plan 02: Staging-Server-Deploy-Workflow Summary

**deploy.sh auf TARGET=staging|production refactored mit 3-Check Production-Gate, REVIEW-HIGH-2 deploy_meta-Fix vor Restart, und /api/health mit git_head+deployed_at fuer Staging-Gate-Verifikation.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-19T~14:30Z
- **Completed:** 2026-05-19
- **Tasks:** 2
- **Files modified:** 3 (deploy.sh, routes/app_routes.py, .env.staging.example neu)

## Accomplishments

- `deploy.sh` akzeptiert jetzt `staging|production` als Pflicht-Parameter — kein versehentlicher Prod-Deploy moeglich
- Production Pre-Deploy-Gate: 3 Checks (status==ok, deployed_at <24h, git_head==LOCAL_HEAD) via `jq`
- REVIEW-HIGH-2 Fix: `.deploy_meta` wird auf Zeile 182 geschrieben, `systemctl restart` erst auf Zeile 187
- `api_health()` gibt `git_head` (SHA) und `deployed_at` (ISO-8601) zurueck — null in lokaler Dev-Umgebung
- `.env.staging.example` als committed Template fuer alle Pflicht-Variablen auf Staging

## Task Commits

1. **Task 1: deploy.sh Refactor** - `9ea3047` (feat)
2. **Task 2: /api/health Extension + .env.staging.example** - `108a88a` (feat)

## Geaenderte Zeilen-Ranges in deploy.sh

| Block | Zeilen (neu) | Aenderung |
|---|---|---|
| Kommentar-Header | 1-9 | `--dry-run` durch `staging\|production` ersetzt |
| TARGET-Block | 11-76 | Neu: Parameter, Production-Gate, Staging-Branch, .env-Check |
| TAR_EXCLUDES | 78-96 | Unveraendert (Array identisch) |
| GIT_HEAD_LOCAL | 98 | Neu: lokaler HEAD vor Upload |
| Connecting-Zeile | 99 | `(TARGET=$TARGET)` hinzugefuegt |
| tar-Upload | 102-103 | Unveraendert — nutzt jetzt $SSH_KEY + $VPS_HOST |
| Service-Unit-scp | 105-112 | TARGET-spezifisch, kein 2>/dev/null || true |
| nginx-scp | 114-119 | Neu: scp fuer beide Targets |
| SSH-Heredoc | 121-190 | ENDHEREDOC (ohne Quotes), deploy_meta vor Restart |

## REVIEW-HIGH-2 Fix Bestaetigung

`.deploy_meta`-Schreiben: **Zeile 182-184** (vor systemctl restart)
`systemctl restart $SERVICE_NAME`: **Zeile 187** (nach deploy_meta)

Differenz: +5 Zeilen — REVIEW-HIGH-2 korrekt implementiert.

## Neue Felder in api_health()

| Feld | Typ | Quelle | Verhalten |
|---|---|---|---|
| `git_head` | string\|null | `/opt/nerve/.deploy_meta` | SHA des deploytes Commits; null wenn Datei fehlt (lokale Dev) |
| `deployed_at` | string\|null | `/opt/nerve/.deploy_meta` | ISO-8601 UTC Timestamp; null wenn Datei fehlt |

Bestehende Felder `backup_status`, `backup_age_hours`, `pipeline_status`, `pipeline_error_count_10min` unveraendert.

## Staging-IP-Platzhalter

`deploy.sh` Zeile 70 enthaelt `VPS_HOST="root@<STAGING_IP>"`.

Andre muss nach Hetzner-Provisionierung ersetzen:
```bash
sed -i 's|root@<STAGING_IP>|root@<ECHTE_IP>|' deploy.sh
```
Oder manuell editieren. Danach: `git commit -m "chore: Staging-IP in deploy.sh setzen"`.

## Deviations from Plan

Keine — Plan exakt wie spezifiziert ausgefuehrt.

## Threat Surface Scan

- T-C1-02-3 (mitigate): `.env.staging.example` enthaelt ausschliesslich Platzhalter (REPLACE-WITH-...) — keine echten Keys committed.
- T-C1-02-4 (mitigate): `${1:?...}` bricht bei leerem Aufruf ab — implementiert.
- T-C1-02-5 (mitigate): `--max-time 10` auf curl in Production-Gate — implementiert.
- T-C1-02-1 (accept): git_head ist SHA, kein Secret — akzeptiert.
- T-C1-02-2 (accept): curl -fsS verwendet SSL-Verifizierung — akzeptiert.

## Known Stubs

- `VPS_HOST="root@<STAGING_IP>"` in deploy.sh Zeile 70 — Platzhalter, muss nach Hetzner-Provisionierung ersetzt werden. Alle anderen Felder sind vollstaendig einsatzbereit.

## Self-Check: PASSED

- `deploy.sh` — existiert, `bash -n` OK, TARGET-Parameter, 3 Gate-Checks, deploy_meta Zeile 182 vor systemctl Zeile 187
- `routes/app_routes.py` — `git_head` und `deployed_at` im jsonify-Block, `.deploy_meta`-Lese-Logik eingefuegt
- `.env.staging.example` — existiert, STAGING_BASIC_AUTH_USER, sk_test_, WTF_CSRF_ENABLED als Kommentar
- Commit `9ea3047`: Task 1 (deploy.sh)
- Commit `108a88a`: Task 2 (app_routes.py + .env.staging.example)
