---
phase: 07.2
plan: 04
type: execute
wave: 4
depends_on:
  - "07.2-01"
  - "07.2-02"
  - "07.2-03"
files_modified:
  - app.py
autonomous: false
requirements:
  - POLISH-32
  - POLISH-34
must_haves:
  truths:
    - "CSS_VERSION in app.py ist auf '20260420-4' gebumpt"
    - "deploy.sh erfolgreich auf VPS deployed"
    - "3 UAT-Checkpoints abgeschlossen und approved: Training-Session, Cold-Call-Session, Meeting-Session"
    - "Keine 500er, keine JS-Console-Errors auf allen 3 Session-Typen"
    - "Redirect-Flow funktioniert: /training end -> /session/<id>"
    - "Nochmal-trainieren-Button funktioniert mit Pre-Selection"
  artifacts:
    - path: "app.py"
      provides: "CSS_VERSION-Bump"
      contains: "'20260420-4'"
  key_links:
    - from: "app.py CSS_VERSION"
      to: "browser cache invalidation"
      via: "template_global('css_v')"
      pattern: "CSS_VERSION = '20260420-4'"
---

<objective>
Wave 4 der Scoring-Konsolidierung: CSS-Version-Bump, Deploy, UAT-Smoke-Tests.

**Was diese Wave liefert:**
- `CSS_VERSION` in `app.py` auf `'20260420-4'` gebumpt (Fortsetzung des Musters aus Phase 07.1 Wave 4 `'20260420-3'`).
- `bash deploy.sh` fuer Production-Deploy.
- Drei User-Acceptance-Checkpoints — einer pro Session-Typ.

**Warum das matters:** CSS-Bump erzwingt Browser-Cache-Invalidation fuer die neue nerve.css mit `.n-session-detail-wendepunkt-*`, `.n-session-detail-einzel-score-*`, `.n-session-detail-improvement-*`, `.n-session-detail-kunden-subtext`, `.n-session-detail-repeat-btn`-Klassen. Ohne Bump sehen User alten CSS-State, obwohl HTML die neuen Klassen referenziert.

**UAT-Ziel:** Phase 07.2 soll in **einer** UAT-Runde durchgehen (im Gegensatz zu 07.1 mit 4 Runden). Checkpoints sind gezielt auf die 10 Pitfalls aus DEVIATIONS.md Phase 07.1 UAT-R1-R4 aufgebaut.

**Output:** Production-Deploy + User-Approval via 3 Checkpoints. Danach ist Phase 07.2 abgeschlossen.

**Dependencies:** MUSS auf alle drei vorherigen Waves (01, 02, 03) warten. Parallele Ausfuehrung NICHT moeglich.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-CONTEXT.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-RESEARCH.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-01-PLAN.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-02-PLAN.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-03-PLAN.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/DEVIATIONS.md

# Source files that need modification
@app.py

<interfaces>
<!-- CSS_VERSION-Muster aus Phase 07.1 -->

### bestehender CSS_VERSION-Pattern (aus Phase 07.1 DEVIATIONS)
```python
# In app.py:
CSS_VERSION = '20260420-3'  # aktueller Stand nach Phase 07.1 Wave 4 POLISH-32/33/21

# Als Jinja-Global registriert:
@app.context_processor
def inject_css_version():
    return {'css_v': CSS_VERSION}

# Templates nutzen:
# <link rel="stylesheet" href="{{ url_for('static', filename='nerve.css') }}?v={{ css_v }}">
```

### Bump-Format (aus Research Open Question 5)
```
Pattern: YYYYMMDD-N (Monotonic Counter pro Tag)
Phase 07.1 Wave 4: '20260420-3' (current)
Phase 07.2 Wave 4: '20260420-4' (next — selbes Datum, naechster Counter)
```

### deploy.sh (bestehend)
```bash
# Im Repo-Root liegendes Deploy-Script
# Workflow aus Phase 07.1: git commit -> bash deploy.sh -> UAT
```
</interfaces>
</context>

<deviation_prevention>
## Precondition Checks (MUST vor Task-Start)

1. **Alle drei vorherigen Waves committed:**
   ```bash
   git log --oneline -n 15
   ```
   MUSS Commits aus Wave 1 + Wave 2 + Wave 3 zeigen (Patterns: "data extraction", "UI extension", "overlay removal").

2. **Template-Parser clean fuer beide Files:**
   ```bash
   python -c "from app import app; app.jinja_env.get_template('session_detail.html'); app.jinja_env.get_template('training.html'); print('OK')"
   ```
   MUSS "OK" liefern. SONST STOP.

3. **Keine Scoring-Overlay-Refs mehr in training.html (Wave 3 vollstaendig):**
   ```bash
   grep -c "t-scoring\|buildScoring\|renderMoodChart\|renderWendepunkteDetail" templates/training.html
   ```
   MUSS `0` liefern.

4. **Wave-1-Context-Variablen in dashboard.py:**
   ```bash
   grep -c "scoring_kategorien\|scoring_wendepunkte_detail\|scoring_verbesserungen\|kunden_name" routes/dashboard.py
   ```
   MUSS >=5 liefern.

5. **Neue Sektionen in session_detail.html (Wave 2 vollstaendig):**
   ```bash
   grep -c "Wendepunkt-Analyse\|Einzel-Bewertungen\|Verbesserungspotenzial" templates/session_detail.html
   ```
   MUSS >=3 liefern.

6. **Action-Button in session_detail.html (Wave 3 vollstaendig):**
   ```bash
   grep -c "n-session-detail-repeat-btn\|Nochmal trainieren" templates/session_detail.html
   ```
   MUSS >=1 liefern.

7. **Aktueller CSS_VERSION-Wert:**
   ```bash
   grep -n "CSS_VERSION" app.py
   ```
   Erwartet: `CSS_VERSION = '20260420-3'` (aus Phase 07.1 Wave 4).

## Deviation-Prevention Tasks

- **P1-P7:** Waves 1-3 haben diese bereits abgearbeitet. Wave 4 hat keine Code-Changes ausser CSS_VERSION-Bump.

- **P8 (Template-Parser-Check VOR Deploy):** Checkpoint 4.1 (explizit).

- **P9 (CSS-Klassen-Namensraum):** Waves 2+3 adressiert.

- **P10 (STATE.md/ROADMAP.md/SUMMARY.md NICHT im Executor):** Nur `07.2-04-SUMMARY.md` erstellen. UAT-Fixes bekommen ggf. eigenes DEVIATIONS.md-Update nach Approval durch Orchestrator.
</deviation_prevention>

<tasks>

<task type="auto" tdd="false">
  <name>Task 4.1: CSS_VERSION Bump + Pre-Deploy Sanity-Checks</name>
  <files>app.py</files>
  <action>
  **Schritt A: CSS_VERSION bumpen**

  In `app.py` den bestehenden `CSS_VERSION`-String finden:
  ```bash
  grep -n "CSS_VERSION" app.py
  ```

  Bestehenden Wert `'20260420-3'` auf `'20260420-4'` aktualisieren:

  ```python
  # ALT:
  # CSS_VERSION = '20260420-3'
  # NEU:
  CSS_VERSION = '20260420-4'
  ```

  **Schritt B: Final-Sanity-Check-Suite**

  Alle 7 Precondition-Checks (siehe deviation_prevention oben) nochmal laufen lassen. Wenn einer fehlschlaegt → SOFORT STOP, User informieren, Details melden.

  **Schritt C: Deploy-Ready-Check**

  ```bash
  # Template-Parser final:
  python -c "from app import app; app.jinja_env.get_template('session_detail.html'); app.jinja_env.get_template('training.html'); print('TEMPLATES OK')"

  # Python-Import-Check:
  python -c "import app; print('APP OK')"

  # Git-Status sauber (alle Aenderungen committed):
  git status --short
  ```

  Falls `git status --short` irgendwas liefert: STOP. Alle Aenderungen muessen aus Wave 1-3 heraus committed sein. Nur der CSS_VERSION-Bump selbst darf uncommitted sein (wird gleich committed).

  **Schritt D: CSS_VERSION-Commit**

  ```bash
  git add app.py
  git commit -m "chore(07.2): bump CSS_VERSION to 20260420-4

  Invalidates browser cache for new .n-session-detail-wendepunkt-*,
  -einzel-score-*, -improvement-*, -kunden-subtext, -repeat-btn classes
  from Phase 07.2 Waves 2 and 3.
  
  Pattern: YYYYMMDD-N monotonic counter (continues 20260420-3 from 07.1 W4)."
  ```

  KEIN `git push` in diesem Task — User entscheidet wann.
  </action>
  <verify>
    <automated>grep -c "CSS_VERSION = '20260420-4'" app.py</automated>
  </verify>
  <done>
  - CSS_VERSION auf '20260420-4' in app.py.
  - Alle 7 Precondition-Checks gruen.
  - Template-Parser fuer beide Templates clean.
  - Python-Import clean.
  - Commit fuer CSS_VERSION-Bump erstellt.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4.2: Deploy auf getnerve.app via deploy.sh</name>
  <what-built>
  Wave 1-3 hat alle Code-Aenderungen gemacht. Task 4.1 hat CSS_VERSION gebumpt. Jetzt muss der Deploy-Script auf die Produktions-VPS (Hetzner CX22, getnerve.app) pushen.
  
  Das Deploy ist manuell weil `bash deploy.sh` eine lokale Shell-Session auf dem Dev-Rechner des Users braucht (SSH-Keys, moegliche sudo-Prompts).
  </what-built>
  <how-to-verify>
  1. Im Repo-Root ausfuehren:
     ```bash
     bash deploy.sh
     ```
  2. Warten bis Deploy-Script "Done" oder aehnlich meldet.
  3. Browser oeffnen: https://getnerve.app
  4. Seite laden, Hard-Refresh (Ctrl+Shift+R) machen.
  5. DevTools -> Network -> pruefen dass `nerve.css?v=20260420-4` geladen wird (nicht `v=20260420-3`).
  6. DevTools -> Console -> KEINE rote 500er-Meldung, KEINE "Cannot read property"-Errors.
  
  Expected Outcome: App laedt normal, Console leer, CSS-Version ist 20260420-4.
  </how-to-verify>
  <resume-signal>Antwort: "deployed" oder "deploy failed: {error-message}"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.3: UAT-Checkpoint 1 — Training-Session Full-Flow</name>
  <what-built>
  Wave 3 + Wave 1 haben den End-to-End Training-Flow neu verdrahtet: Training starten, durchfuehren, beenden -> direkter Redirect auf Session-Detail mit allen 13 Sektionen (11 aus 07.1 + 12/13/14 neu). Kein Post-Call-Overlay mehr. "Nochmal trainieren"-Button oben rechts.
  </what-built>
  <how-to-verify>
  Auf https://getnerve.app einloggen. Dann durchlaufen:

  **A. Training starten und durchfuehren:**
  1. Nav zu /training.
  2. Setup: Custom-Personality-Type waehlen (AI-generiert, mit Name+Alter — z.B. "Markus Wendland, 48" oder anderer).
  3. Schwierigkeit: Fortgeschritten.
  4. Modus: Guided.
  5. Training starten — mindestens 60 Sekunden sprechen (damit Phasen-Strip Daten bekommt).
  6. "Beenden" klicken, im Modal "Beenden bestaetigen".

  **B. Redirect-Flow pruefen (NEU 07.2):**
  - [ ] Loader-Overlay zeigt "Auswertung wird erstellt..."
  - [ ] Nach 5-15 Sekunden: Browser navigiert zu /session/<X> (URL mit Session-ID).
  - [ ] Keine JS-Console-Errors waehrend des Redirects.

  **C. Session-Detail-Seite bei Training visuell pruefen:**

  **Header:**
  - [ ] Badge "Training" sichtbar.
  - [ ] Persoenlichkeitstyp-Badge sichtbar (z.B. "Entscheider" oder Custom-Name).
  - [ ] Schwierigkeit-Badge "Fortgeschritten" sichtbar.
  - [ ] NEU: Kunden-Subtext "Markus Wendland, 48" (oder was auch immer der Custom-Type war) neben den Badges.
  - [ ] NEU: Action-Button "Nochmal trainieren" oben rechts (mit play-circle-Icon).

  **Neue Sektionen (scrollen bis ganz unten):**
  - [ ] Sektion 12 "Wendepunkt-Analyse": Liste von 3-5 Cards mit "Du hast gesagt:" / "Problem:" / "Besser wäre:"-Struktur. Teal-Left-Border am "Besser wäre:"-Block. Wenn keine stimmung_history -> Empty-State "Keine Wendepunkte erfasst" (kein Future-Hinweis).
  - [ ] Sektion 13 "Einzel-Bewertungen": 5-7 Kategorien (Gesprächseröffnung / Bedarfsanalyse / Einwandbehandlung / Gesprächsführung / Abschluss / Beziehungsaufbau). Jede mit Label + X/10 + Minibar + Feedback-Text.
  - [ ] Sektion 14 "Verbesserungspotenzial": 3-5 Bullets mit Alert-Triangle-Icon + Handlungstipp.

  **Sektion 10 entfernt:**
  - [ ] "Gespraechs-Analyse" taucht NIRGENDS mehr auf der Seite auf (vorherige Placeholder-Card mit "Kommt mit Phase 4.19").

  **"Nochmal trainieren"-Button:**
  - [ ] Klick auf Button navigiert zu /training?personality_type_id=<X>&schwierigkeit=<Y>.
  - [ ] Setup-Wizard oeffnet sich MIT Pre-Selection: korrekte Personality markiert, korrekte Schwierigkeit markiert.

  **D. Rueck-Navigation + Retest:**
  1. Zurueck zur Dashboard-History (/logs).
  2. Dieselbe Session-Detail-Seite nochmal oeffnen.
  3. Seite muss gleich aussehen (keine State-Verlust).
  4. Browser-History: "Zurueck"-Button funktioniert, keine 404/500.

  **E. Training-Seite nach Redirect pruefen:**
  1. Nav zu /training (ohne Params, direkter Aufruf).
  2. Setup-Wizard laedt normal.
  3. Neues Training starten moeglich.
  4. Keine JS-Console-Errors ueber `t-scoring`, `buildScoring`, `renderMoodChart` oder aehnlich.

  Red Flags (SOFORT melden):
  - 500er auf /training.
  - 500er auf /session/<id>.
  - JS-Console-Error "Cannot read property 'style' of null".
  - JS-Console-Error "Chart.getChart is not a function" oder "t-scoring does not exist".
  - Sektion 10 noch sichtbar.
  - Umlaut-Bug in CSS-Klasse (irgendein Bereich bleibt ungestylt).
  </how-to-verify>
  <resume-signal>Antwort: "training approved" oder Liste der gefundenen Issues (Bug-Nr, File-Pfad, Symptom).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.4: UAT-Checkpoint 2 — Cold-Call-Session End-to-End</name>
  <what-built>
  Cold-Call-Sessions sollen das gleiche Layout zeigen wie Training, aber die 3 neuen Sektionen (12/13/14) im Empty-State mit Phase-4.19-Hinweis — OHNE Doppelung mit bestehenden Sektionen.
  </what-built>
  <how-to-verify>
  Auf https://getnerve.app eingeloggt bleiben.

  **A. Cold-Call-Session starten und durchfuehren:**
  1. Nav zu /live.
  2. Mode-Overlay: "Cold Call" waehlen.
  3. Aktives Profil sicherstellen (mindestens 2-3 Einwaende definiert).
  4. Mikrofon-Consent -> OK.
  5. Mindestens 30-60 Sekunden sprechen (Einwand simulieren ideal, EWB-Button klicken).
  6. "Beenden"-Button klicken.

  **B. Session-Detail bei Cold Call:**

  **Header:**
  - [ ] Badge "Cold Call" (Teal-Primary-Badge).
  - [ ] KEIN "Training"-Badge, KEIN Persoenlichkeitstyp-Badge, KEIN Schwierigkeit-Badge.
  - [ ] KEIN Kunden-Subtext.
  - [ ] KEIN "Nochmal trainieren"-Button.

  **Neue Sektionen (Empty-States mit 4.19-Hinweis):**
  - [ ] Sektion 12 "Wendepunkt-Analyse": zeigt Dashed-Border-Card mit message-square-text-Icon und Text "Die Wendepunkt-Analyse fuer Live-Sessions kommt mit Phase 4.19 (Transkript-Persistierung)..." + Badge "Kommt mit Phase 4.19".
  - [ ] Sektion 13 "Einzel-Bewertungen": zeigt Dashed-Border-Card mit bar-chart-3-Icon und Text ueber Phase-4.19-Dependency + "Aktuell siehst du den Gesamt-Score in Sektion 2" + Badge "Kommt mit Phase 4.19".
  - [ ] Sektion 14 "Verbesserungspotenzial": Zeigt 1-3 Bullets aus bestehendem `_derive_practice_recommendations()` Output (aus 07.1), mit Alert-Triangle-Icon. Falls keine Recommendations vorhanden: "Kein Verbesserungspotenzial identifiziert"-Empty-State.

  **Sektion 10 entfernt:**
  - [ ] "Gespraechs-Analyse" nicht mehr auf der Seite.

  **Bestehende 11 Sektionen (Regression-Test):**
  - [ ] Score-Hero mit kb_end-Wert + Trend-Badge.
  - [ ] Kaufbereitschafts-Verlauf-Chart (wenn Datenpunkte existieren).
  - [ ] Einwand-Timeline mit Count.
  - [ ] Phasen-Strip.
  - [ ] Skript-Abdeckung-Progress-Bar.
  - [ ] Sektion 9 "Was du ueben solltest" (bestehend aus 07.1, MIT "Im Training ueben"-CTA + Cross-Context-Badge).
  - [ ] Painpoints-Liste.
  - [ ] PreCall-Briefing-Collapsible.
  - [ ] Sektion 11 "Lernkarten" Future-Card (noch immer Placeholder aus 07.1).

  **Console-Check:**
  - [ ] DevTools -> Console: keine 500er, keine rote Errors.

  Red Flags (SOFORT melden):
  - Sektion 13 zeigt Einzel-Scores bei Cold Call (sollte Empty-State sein — Pitfall POLISH-34-Doppelung).
  - Sektion 12 zeigt Wendepunkt-Cards bei Cold Call (sollte Empty-State sein).
  - Sektion 14 zeigt Training-Verbesserungen-Strings (sollte recommendations aus _derive_practice_recommendations sein).
  - Header zeigt Kunden-Subtext (sollte nur bei Training sein).
  - Header zeigt "Nochmal trainieren"-Button (sollte nur bei Training sein).
  - Jede JS-Error.
  </how-to-verify>
  <resume-signal>Antwort: "cold call approved" oder Liste der gefundenen Issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.5: UAT-Checkpoint 3 — Meeting-Session End-to-End</name>
  <what-built>
  Meeting-Sessions sollen identisch zu Cold-Call gerendert werden mit Ausnahme des Mode-Badges. Dritter UAT-Run validiert dass kein `session_mode`-Coupling die Typ-Diskriminierung bricht (Pitfall 7).
  </what-built>
  <how-to-verify>
  Auf https://getnerve.app eingeloggt bleiben.

  **A. Meeting-Session starten:**
  1. Nav zu /live.
  2. Mode-Overlay: "Meeting" waehlen.
  3. Consent-Pop-up: "Zugestimmt" klicken (oder simulieren).
  4. Mikrofon-Consent -> OK.
  5. Mindestens 30-60 Sekunden sprechen.
  6. "Beenden" klicken.

  **B. Session-Detail bei Meeting:**

  **Header:**
  - [ ] Badge "Meeting" (Neutral-Badge).
  - [ ] KEIN "Training"-Badge, KEIN Persoenlichkeitstyp-Badge, KEIN Schwierigkeit-Badge.
  - [ ] KEIN Kunden-Subtext.
  - [ ] KEIN "Nochmal trainieren"-Button.

  **Neue Sektionen (identisch zu Cold Call):**
  - [ ] Sektion 12 Wendepunkt-Analyse: Empty-State mit 4.19-Hinweis.
  - [ ] Sektion 13 Einzel-Bewertungen: Empty-State mit 4.19-Hinweis.
  - [ ] Sektion 14 Verbesserungspotenzial: recommendations-basierte Liste ODER Empty-State.

  **Regression-Test (Chart):**
  - [ ] Kaufbereitschafts-Verlauf-Chart rendert (wenn Datenpunkte existieren).
  - [ ] Chart-Axis-Titel vorhanden (aus 07.1 UAT-R1 Fix E).
  - [ ] Y-Achse "Kaufbereitschaft (%)", X-Achse "Zeit (Sekunden)".

  **Console-Check:**
  - [ ] DevTools -> Console: leer.

  **C. Rueck-Navigation / Cross-Typ-Test:**
  1. Zurueck zu /logs.
  2. In der History 3 verschiedene Session-Typen anklicken (Training, Cold Call, Meeting) — falls vorhanden.
  3. Jede muss korrekt rendern (je nach Typ Badges + Sektion-12/13/14-Branch).

  Red Flags (SOFORT melden):
  - Meeting rendert Training-Content.
  - "Cold Call"-Badge bei Meeting-Session.
  - Jede JS-Error.
  - Phasen-Strip-Section leer trotz >60s Session (OBS-02 style Recidiv).

  **D. Final-Check: Phase 07.2 approved wenn alle 3 UATs gruen:**
  - [ ] Training UAT (4.3) approved
  - [ ] Cold Call UAT (4.4) approved  
  - [ ] Meeting UAT (4.5) approved
  </how-to-verify>
  <resume-signal>Antwort: "meeting approved" ODER "PHASE 07.2 APPROVED" (falls 4.3+4.4+4.5 alle gruen) ODER Issue-Liste.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev → Production-VPS | `bash deploy.sh` triggert Code-Push + Restart |
| Browser → getnerve.app | User-facing Production-Site — darf keine 500er, keine Stack-Traces zeigen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-11 | I (Information Disclosure) | Deploy exposed new code path mit Vuln | accept | Waves 1-3 haben alle kein neues Input-Surface. Session-Detail bleibt read-only, /training nutzt bestehende Handler. |
| T-07.2-12 | D (Denial of Service) | deploy.sh down-time | accept | Deploy geht typischerweise <30s (bekannt aus Phase 07.1 Wave 4). |

</threat_model>

<verification>

## Wave-4-Gate (Phase 07.2 Approval)

1. **CSS_VERSION gebumpt:**
   ```bash
   grep -c "CSS_VERSION = '20260420-4'" app.py
   ```
   → `>=1`.

2. **Deploy erfolgreich:**
   - Task 4.2 approved.
   - Browser-Test: `nerve.css?v=20260420-4` laedt.

3. **UAT vollstaendig:**
   - Task 4.3 (Training) approved.
   - Task 4.4 (Cold Call) approved.
   - Task 4.5 (Meeting) approved.

Wenn alle 3 UATs approved:
- **Phase 07.2 APPROVED** — ready fuer SUMMARY + STATE-Update durch Orchestrator.
- Git-Push final: `git push origin main` (gemaess CLAUDE.md Git-Regel).

</verification>

<success_criteria>

- `CSS_VERSION = '20260420-4'` committed in app.py.
- `bash deploy.sh` erfolgreich durchgelaufen auf getnerve.app.
- Browser-Hard-Refresh laedt `nerve.css?v=20260420-4`.
- Training-Session UAT approved: Redirect-Flow, Header-Badges (Training + Persoenlichkeit + Schwierigkeit + Kunden-Subtext), Nochmal-trainieren-Button mit Pre-Selection, 3 neue Sektionen mit Daten, Sektion 10 weg.
- Cold-Call-Session UAT approved: Header nur "Cold Call"-Badge, 3 neue Sektionen im Empty-State mit 4.19-Hinweis, Sektion 10 weg, bestehende 07.1-Sektionen weiterhin funktionsfaehig.
- Meeting-Session UAT approved: Header nur "Meeting"-Badge, Layout identisch zu Cold Call.
- Keine JS-Console-Errors auf allen 3 Session-Typen.
- Keine 500er.
- Template-Parser clean vor Deploy.

</success_criteria>

<output>
Nach Abschluss (alle 3 UATs approved) erstellt der Executor AUSSCHLIESSLICH:
- `.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-04-SUMMARY.md`

Falls UAT-Findings auftreten:
- Neue PLAN-Datei in diesem Phase-Dir erstellen: `07.2-05-uat-fixes-PLAN.md` (analog zu 07.1 DEVIATIONS-Pattern).
- ODER: Fix-Commits auf main atomar (1 Commit/Fix) + CSS_VERSION erneut bumpen auf '20260420-5' + Re-Deploy + neuen UAT-Run — ganz wie Phase 07.1 UAT-R1-R4.

Executor MUSS NICHT in Wave 4:
- STATE.md updaten (Orchestrator nach Approval).
- ROADMAP.md updaten (Orchestrator).
- Andere SUMMARYs modifizieren.
- Git-Push triggern (User entscheidet gemaess CLAUDE.md Git-Regel nach Phase-Abschluss).
</output>
</content>
</invoke>