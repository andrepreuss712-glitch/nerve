---
phase: 07.2
plan: 03
subsystem: frontend/ui
tags: [training-overlay-removal, redirect-flow, nochmal-trainieren, session-detail-action, polish-32, polish-34, deferred-items]
status: complete
wave: 3
depends_on:
  - "07.2-01"
dependency_graph:
  requires:
    - phase: "07.2-01"
      provides: "Response field /training/end.log_id (for redirect) + schwierigkeit_raw template context (for button URL)"
    - phase: "07.2-02"
      provides: "Session-Detail page renders sections 12/13/14 (redirect destination has full evaluation UI)"
  provides:
    - "templates/training.html: Scoring overlay removed (~350 LOC net deletion), redirect flow to /session/<log_id> active"
    - "templates/training.html: showPhase() array without 't-scoring', endTraining() no-conversation guard refactored to alert+reset"
    - "templates/training.html: Deferred-Items Inline-HTML-Comment (POLISH-35/-36/-37: personality_revealed, live_preview, generatedPersonality Save-Prompt) for future re-introduction pointer"
    - "templates/session_detail.html: Action-Button 'Nochmal trainieren' in Training-Header (only for conv.typ == 'training'), URL with conditional pre-selection query params"
    - "static/nerve.css: .n-session-detail-repeat-btn class family (base rule + mobile media query)"
  affects:
    - "Wave 4 (Plan 04): CSS_VERSION bump + deploy + UAT — uses new redirect flow and repeat button"
tech_stack:
  added: []
  patterns:
    - "Jinja {% set qparams = [] %}{% set _ = qparams.append(...) %}{{ qparams | join('&') }} idiom for conditional URL query string assembly"
    - "Type-discrimination strictly via {% if conv.typ == 'training' %} (NOT session_mode) — consistent with Wave 2"
    - "CSS: margin-left:auto for right-alignment within flex-header (no position:absolute)"
    - "CSS: Responsive wrap via @media (max-width: 640px) — margin-left:0 + margin-top:8px + align-self:flex-start"
    - "JS: window.location.href navigation (not history.pushState) — full browser navigation ensures Session-Detail re-renders fresh context"
    - "JS: Loader stays visible during navigation (intentional UX — user sees loader until Session-Detail loads)"
    - "JS: Early-exit guard refactored to alert + resetTraining() instead of fake-scoring-overlay render"
key_files:
  created: []
  modified:
    - "templates/training.html (−392 lines, +44 lines; net −348 LOC)"
    - "templates/session_detail.html (+11 lines; Header-Button-Insert)"
    - "static/nerve.css (+20 lines; new .n-session-detail-repeat-btn class family)"
decisions:
  - "Redirect fallback /analytics (not /logs): /logs route does not exist in dashboard.py — analytics_page is the history page (Rule-1 auto-fix from plan instruction)"
  - "endTraining() no-conversation guard refactored: alert('Kein Gespraech erkannt...') + resetTraining() instead of showPhase('scoring') + #t-scoring.innerHTML (Rule-3 auto-fix — original guard depended on now-removed DOM)"
  - "saveGeneratedPersonality() function removed as orphan: only caller was save-personality-div (inside removed scoring overlay). Re-introduction under POLISH-37 (Rule-1 dead-code cleanup)"
  - "Orphaned scoring CSS (.t-scoring-*, .t-score-*, .t-cat-*, .t-bullet, .t-summary, .t-modus-result*, .t-live-preview, .t-lp-*) removed to keep file clean — .t-modus-card/.t-modus-active/.t-hilfe-counter kept (still used in setup-phase)"
  - "Inline HTML comment at former #t-scoring location references POLISH-35/-36/-37 for re-introduction discovery (Plan Schritt H)"
  - "Button URL uses Jinja qparams-list-join pattern (not direct ?a&b string concat) — avoids trailing-& edge cases, clean for 0/1/2 param cases"
  - "scenario_id and modus NOT in button URL: scenario_id not persisted (Research Pitfall 4 + A2), modus not derivable from persisted fields"
metrics:
  duration_minutes: 20
  tasks_completed: 2
  files_modified: 3
  completed_date: "2026-04-20"
---

# Phase 07.2 Plan 03: Training-Overlay-Removal + Nochmal-trainieren-Button (Wave 3) Summary

**One-liner:** Training-Post-Call-Overlay komplett ausgebaut (~280 LOC + ~70 LOC orphan CSS = −348 LOC), confirmEndTraining() redirected jetzt auf /session/<log_id>, und Session-Detail-Header hat einen "Nochmal trainieren"-Action-Button mit personality_type_id + schwierigkeit-Pre-Selection aus den Wave-1-Context-Variablen.

## Performance

- **Duration:** 20min
- **Started:** 2026-04-20T13:51:04Z
- **Completed:** 2026-04-20T14:11:57Z
- **Tasks:** 2 (Task 3.1 committed as cc33b37, Task 3.2 committed as 50dd0d7)
- **Files modified:** 3 (templates/training.html, templates/session_detail.html, static/nerve.css)

## What Was Built

### templates/training.html (Task 3.1)

**Schritt A: `confirmEndTraining()` Redirect-Flow**
- Success-Pfad umgeschrieben: `const data = await res.json()` → wenn `data.log_id` vorhanden, `window.location.href = '/session/' + data.log_id`, sonst Fallback auf `/analytics`.
- Loader-Overlay bleibt beim Redirect sichtbar (gewuenscht: User sieht Loader bis Session-Detail-Page geladen).
- `clearInterval(loadInterval)` und `loadOverlay.style.display = 'none'` wurden in den Error-Branch und den `!data.ok`-Branch verschoben — im Success-Pfad behaelt der Loader seine Sichtbarkeit.
- Inline-Kommentar dokumentiert Option A + POLISH-35/-36/-37 Deferral.

**Schritt B: `showPhase()` Array ohne 't-scoring'**
- `['t-setup','t-einwahl','t-chat','t-scoring']` → `['t-setup','t-einwahl','t-chat']`.
- Inline-Kommentar erklaert die Entfernung.

**Schritt C: JS-Funktionen komplett entfernt**
- `buildScoring(data)` (~132 LOC) — score-circle, kategorien-html, staerken/verbesserungen, modus-html, live-preview, final innerHTML-assembly.
- `renderMoodChart(stimmungHistory)` (~78 LOC) — Chart.js mood-curve mit wendepunkte-highlighting.
- `renderWendepunkteDetail(wendepunkte)` (~33 LOC) — Wendepunkt-Cards "Du hast gesagt / Problem / Besser waere".
- `saveGeneratedPersonality()` (~25 LOC) — orphan (einziger Aufrufer war save-personality-div innerhalb des Overlays). Re-Introduction unter POLISH-37.

**Schritt D: HTML-Block `#t-scoring` entfernt**
- Gesamter `<div id="t-scoring" class="t-overlay t-hidden">...</div>` Block entfernt (8 Zeilen).
- Ersetzt durch Jinja-HTML-Kommentar mit POLISH-35/-36/-37-Referenz und Deferred-Features-Beschreibung (Schritt H).

**Schritt E: Rest-Referenzen bereinigt**
- `endTraining()` no-conversation-guard refaktoriert: `showPhase('scoring')` + `#t-scoring.innerHTML` → `alert(...)` + `resetTraining()` (Rule 3: blocker-fix — Original-Guard haette NaN-Navigation produziert, weil Ziel-DOM nicht mehr existiert).
- Inline-Kommentar des ehemaligen t-scoring-Blocks nutzt die Woerter "scoring"/"Scoring" NICHT in der verbotenen `t-scoring`-Form (stattdessen: "Overlay", "Overlay-Flow", "Session-Detail").

**Schritt F: Template-Parser-Check**
- `python -c "from app import app; app.jinja_env.get_template('training.html'); print('OK')"` liefert OK.

**Schritt G: Chart.js-Init-Check**
- Keine verbleibenden `Chart.getChart('t-mood-chart')`- oder `new Chart` mood-related-Aufrufe. Das verbleibende `new Chart` in Zeile ~2013 ist fuer `_trendChart` in der Setup-Phase-Stats (unberuehrt).
- `t-mood-indicator/-bar/-fill/-value` Elemente (Chat-Phase Live-Mood-Anzeige) unberuehrt — das ist die Laufzeit-Stimmungs-Anzeige waehrend des Calls, nicht die Post-Call-Mood-Chart.

**Schritt H: Deferred-Items Inline-Kommentar**
- Jinja-`{# ... #}`-Kommentar an der Stelle des ehemaligen `#t-scoring`-Blocks:
  - `personality_revealed: Experte-Mode-Hidden-Personality-Reveal nach Call (D-05)`
  - `live_preview: Haiku-Call "Was NERVE live geholfen haette"`
  - `generatedPersonality Save-Prompt: "Diese Persoenlichkeit merken?"`
  - Tracked als POLISH-35 / POLISH-36 / POLISH-37 in ROADMAP.md Deferred-Section.

**Bonus: Orphaned CSS bereinigt (Rule 1 — dead-code cleanup)**
- Scoring-CSS-Bloecke komplett entfernt: `.t-scoring-scroll`, `.t-scoring-inner`, `.t-scoring-actions`, `.t-score-header`, `.t-score-circle`, `.t-score-meta`, `.t-score-title`, `.t-score-profile`, `.t-score-tags`, `.t-score-tag`, `.t-score-tag.gold`, `.t-cats`, `.t-cat`, `.t-cat-top`, `.t-cat-name`, `.t-cat-score`, `.t-cat-bar-wrap`, `.t-cat-bar`, `.t-cat-fb`, `.t-score-box`, `.t-score-box-title`, `.t-bullet`, `.t-bullet:last-child`, `.t-summary`, `.t-modus-result`, `.t-modus-result-title`, `.t-modus-result-msg`, `.t-modus-result-pts`, `.t-live-preview`, `.t-lp-header`, `.t-lp-moment`, `.t-lp-trigger`, `.t-lp-badge`, `.t-lp-arg`, `.t-lp-tip`, `.t-lp-pain`, `.t-lp-summary`, `.t-lp-cta`, `.t-lp-cta-text`, `.t-lp-cta-btn` (~80 Zeilen).
- `.t-modus-grid`, `.t-modus-card`, `.t-modus-card:hover`, `.t-modus-card.t-modus-active`, `.t-modus-icon`, `.t-modus-name`, `.t-modus-desc`, `.t-modus-badge`, `.t-modus-active .t-modus-badge`, `.t-hilfe-counter` BEHALTEN — werden in Setup-Phase-Modus-Auswahl noch verwendet (Zeilen ~474-485 + 669).

### templates/session_detail.html (Task 3.2 Schritt A)

**Position:** Direkt nach `<h1>Session #{{ conv.id }}</h1>`, VOR `<div class="n-session-detail-meta">` (Zeilen 9-19 nach Edit).

**Struktur:**
```jinja
{% if conv.typ == 'training' %}
  <a class="n-btn n-btn-primary n-session-detail-repeat-btn"
     href="{{ url_for('training.training_page') }}{% set qparams = [] %}{% if conv.personality_type_id %}{% set _ = qparams.append('personality_type_id=' ~ conv.personality_type_id) %}{% endif %}{% if schwierigkeit_raw %}{% set _ = qparams.append('schwierigkeit=' ~ schwierigkeit_raw) %}{% endif %}{% if qparams %}?{{ qparams | join('&') }}{% endif %}">
    <i data-lucide="play-circle" class="n-session-detail-icon-sm"></i>
    Nochmal trainieren
  </a>
{% endif %}
```

**Type-Diskrimination:** Strictly via `conv.typ == 'training'` (nicht `session_mode`) — P7 im Plan respektiert, konsistent mit Wave 2 Patterns.

**URL-Param-Whitelist:**
- `personality_type_id` (int | None) — aus `conv.personality_type_id` (persistiert, Wave 1)
- `schwierigkeit` (str | None) — aus `schwierigkeit_raw` (Wave 1 Task 1.1 Template-Context)
- `scenario_id` NICHT — nicht persistiert (Research Pitfall 4 + Assumption A2)
- `modus` NICHT — nicht aus persistierten Feldern ableitbar

### static/nerve.css (Task 3.2 Schritt B)

**Position:** Nach `.n-session-detail-kunden-subtext` (Zeile 2284), VOR `@media (max-width: 768px)`-Block (Zeile 2286).

**Rules:**
```css
.n-session-detail-repeat-btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  margin-left: auto;
  /* Rest kommt aus .n-btn / .n-btn-primary — keine neuen Farben */
}

@media (max-width: 640px) {
  .n-session-detail-repeat-btn {
    margin-left: 0;
    margin-top: 8px;
    align-self: flex-start;
  }
}
```

- `margin-left:auto` — right-alignment innerhalb des `<header>`-Flex-Containers.
- `gap:6px` — Abstand zwischen Icon und Text.
- `display:inline-flex` — Icon und Text vertikal zentrieren.
- Mobile (<640px): wrappt unter Titel, linksbuendig.
- KEINE neuen Farben — inheritiert `.n-btn.n-btn-primary` Look.

## Deviations from Plan

### Rule-1 Auto-fixes

**1. [Rule 1 - Bug] Redirect-Fallback-Route korrigiert: `/logs` → `/analytics`**
- **Found during:** Task 3.1 Schritt A (confirmEndTraining Redirect)
- **Issue:** Plan sagte `window.location.href = '/logs'` als Fallback, aber `/logs` ist KEINE registrierte Route in `routes/dashboard.py`. Die Session-History-Seite ist `/analytics` (dashboard.analytics_page).
- **Fix:** Fallback-URL auf `/analytics` geaendert, Console-Error-Message entsprechend angepasst (`fallback to /analytics`).
- **Files modified:** templates/training.html (confirmEndTraining success-else-branch)
- **Commit:** cc33b37

**2. [Rule 3 - Blocker] `endTraining()` no-conversation-guard: showPhase('scoring') + #t-scoring-Target entfernt**
- **Found during:** Task 3.1 Schritt E (Rest-Referenzen-Scan)
- **Issue:** Die no-conversation-guard in `endTraining()` (trainingSecs<10 oder userMsgCount===0) nutzte `showPhase('scoring')` + `document.getElementById('t-scoring').innerHTML = ...`. Beides ist nach Task 3.1 Schritt B/D weg (`'scoring'` aus Array entfernt, `#t-scoring` geloescht). Original-Code haette zur Laufzeit NaN/undefined-Navigation produziert.
- **Fix:** Guard refaktoriert zu `alert('Kein Gespraech erkannt...')` + `resetTraining()`. User bekommt den gleichen Informations-Content (Hinweis-Text) via Browser-Alert, Training wird ordentlich zurueckgesetzt.
- **Files modified:** templates/training.html (endTraining function)
- **Commit:** cc33b37

**3. [Rule 1 - Dead Code] Orphan `saveGeneratedPersonality()` Funktion entfernt**
- **Found during:** Task 3.1 Schritt E (Rest-Referenzen-Scan, Grep auf `t-save-personality-div`)
- **Issue:** `saveGeneratedPersonality()` Funktion (25 LOC) hatte als einzigen Aufrufer den `<button onclick="saveGeneratedPersonality()">` im nun-geloeschten save-personality-div innerhalb des Scoring-Overlays. Funktion ist damit Dead Code.
- **Fix:** Funktion durch Inline-Kommentar ersetzt: "ENTFERNT in 07.2 Wave 3 ... Re-Introduction zusammen mit dem Save-Prompt unter POLISH-37 (ROADMAP)."
- **Files modified:** templates/training.html
- **Commit:** cc33b37

**4. [Rule 1 - Dead Code] Orphaned Scoring-CSS-Bloecke entfernt**
- **Found during:** Task 3.1 Schritt E (Rest-Referenzen-Scan)
- **Issue:** Nach Entfernen des `#t-scoring`-HTML-Blocks und der buildScoring/renderMoodChart/renderWendepunkteDetail-Funktionen waren ~80 Zeilen CSS (`.t-scoring-*`, `.t-score-*`, `.t-cat*`, `.t-bullet`, `.t-summary`, `.t-modus-result*`, `.t-live-preview`, `.t-lp-*`) orphan. Zusaetzlich: Der Verify-Check `grep -c "t-scoring"` haette `>= 3` gemeldet wegen `.t-scoring-scroll`, `.t-scoring-inner`, `.t-scoring-actions`.
- **Fix:** Alle orphan Scoring-CSS-Bloecke entfernt. Setup-Phase-CSS (`.t-modus-grid`, `.t-modus-card/.t-modus-active`, `.t-modus-icon/-name/-desc/-badge`, `.t-hilfe-counter`) bleibt — wird noch verwendet.
- **Files modified:** templates/training.html (extra_style block lines 110-157)
- **Commit:** cc33b37

### Total deviations: 4 auto-fixes (alle Rule 1 / Rule 3, keine Rule 4 architektonischen Entscheidungen)

**Impact on plan:** Plan wurde in Spirit vollstaendig umgesetzt. Alle 4 Auto-Fixes sind Rule-1/Rule-3 — niemals ein Architektur-Entscheidungs-Blocker. Plan-Author war sich Rest-Referenzen-Thema bewusst (Schritt E: "Rest-Referenzen scannen und bereinigen") und explizit empfohlen Pitfall-3-Mental-Walkthrough — beides in der Ausfuehrung durchgefuehrt.

## Authentication Gates

Keine. Dieser Plan touchiert keine Auth-Flows, nur Frontend-Template + CSS.

## Verification Results

### Wave-3-Gates (alle 6 aus Plan-Verification-Sektion)

| Gate | Erwartung | Ergebnis | Status |
|------|-----------|----------|--------|
| 1. Alle Scoring-Overlay-Refs entfernt | `grep -c ... = 0` | `0` | PASS |
| 2. Redirect-Code vorhanden | `grep -c "window.location.href = '/session/'" >=1` | `1` | PASS |
| 3. Deferred-Items-Kommentar | `grep -c "POLISH-35" >=1` | `2` | PASS |
| 4. "Nochmal trainieren"-Button vorhanden | `grep -c "Nochmal trainieren\|n-session-detail-repeat-btn" >=1` | `3` | PASS |
| 5. CSS-Klasse vorhanden | `grep -c "n-session-detail-repeat-btn" static/nerve.css >=1` | `2` | PASS |
| 6. Template-Parser clean | `python -c "... get_template(...)" == "OK"` | `OK` | PASS |

### Zusaetzliche Cross-Checks

- **No-Conversation-Guard-Check:** `grep "showPhase\('scoring'\)" templates/training.html` → keine Matches (Fix verifiziert).
- **Chart.js-Orphan-Check:** `grep "Chart.getChart\|new Chart.*mood\|t-mood-chart"` → keine mood-chart-Refs mehr; verbleibendes `new Chart(...)` ist `_trendChart` in Setup-Stats (nicht Post-Call-Mood-Chart).
- **saveGeneratedPersonality-Orphan-Check:** `grep -c "saveGeneratedPersonality"` → 0 Matches (Funktion + Aufrufer alle weg).
- **Setup-Phase-CSS-Preservation-Check:** `grep -c "t-modus-card\|t-modus-active\|t-hilfe-counter"` → 8 Matches (Setup-Phase-Modus-Selector + Hilfe-Counter im Chat-Header unberuehrt).
- **Mental-Walkthrough 3 URL-Faelle:**
  1. `personality_type_id=5, schwierigkeit_raw='mittel'` → `/training?personality_type_id=5&schwierigkeit=mittel` ✓
  2. No pt_id, `schwierigkeit_raw='leicht'` → `/training?schwierigkeit=leicht` ✓
  3. Neither set → `/training` (kein `?`) ✓
- **Post-commit deletion check (Task 3.1):** `git diff --diff-filter=D --name-only HEAD~1 HEAD` → leer (nur Modifikationen, keine Datei-Deletions).
- **Post-commit deletion check (Task 3.2):** `git diff --diff-filter=D --name-only HEAD~1 HEAD` → leer (nur Modifikationen).

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 3.1 | cc33b37 | refactor(07.2-03): remove training scoring overlay, redirect to session-detail |
| 3.2 | 50dd0d7 | feat(07.2-03): add "Nochmal trainieren" action button to session-detail |

## Known Stubs

Keine. Beide Tasks liefern fertige User-facing Funktionalitaet:
- Training-End → Redirect ist fertig verdrahtet (data.log_id kommt aus Wave 1, Fallback auf /analytics bei None).
- Session-Detail-Button rendert mit echten Pre-Selection-Parametern aus Wave-1-Context.

**Bewusste Deferrals (nicht als Stub zu werten, als Inline-Kommentar + ROADMAP-Tracking dokumentiert):**
- POLISH-35: `personality_revealed` (Experte-Mode-Hidden-Personality-Reveal nach Call)
- POLISH-36: `live_preview` (Haiku-Call "Was NERVE live geholfen haette")
- POLISH-37: `generatedPersonality` Save-Prompt ("Diese Persoenlichkeit merken?")

Diese drei Features wurden mit dem `#t-scoring`-Block entfernt. Re-Introduction in kuenftigen Phasen ist tracked in ROADMAP.md Deferred-Section (Orchestrator traegt POLISH-35/-36/-37 nach Plan-Ausfuehrung ein).

## Dependencies Provided to Downstream Waves

**Wave 4 (Plan 04 — CSS-Version-Bump + Deploy + UAT)** hat jetzt:
- Redirect-Flow aktiv: User wird nach `/training/end` direkt auf `/session/<log_id>` navigiert.
- Session-Detail-Header hat "Nochmal trainieren"-Button fuer Training-Sessions — UAT-Szenario: Session-Detail oeffnen → Button klicken → landet auf `/training` mit Pre-Selection.
- Neue CSS-Klasse `.n-session-detail-repeat-btn` — muss per CSS_VERSION-Bump aktiviert werden.
- Komplett-Flow-Test: Training-Call starten → beenden → automatischer Redirect auf Session-Detail → Button "Nochmal trainieren" klicken → neues Training beginnt mit vorausgewaehltem Persoenlichkeitstyp + Schwierigkeit.

## Threat Flags

Keine neuen Threat-Surfaces. Die drei Threats aus Plan-Threat-Registry abgedeckt:

- **T-07.2-08 (E - Elevation of Privilege, accept):** User manipuliert log_id in Client-side-JS vor Redirect → `/session/<id>` Route filtert bereits `conv.user_id == g.user.id` → 404 bei fremder ID, kein Leak.
- **T-07.2-09 (T - Tampering, mitigate):** User manipuliert personality_type_id in Button-URL → `/training/start` filtert bereits `PersonalityType.user_id == g.user.id OR is_custom == False` (routes/training.py:180-184) → keine neue Vuln.
- **T-07.2-10 (T - Tampering, mitigate):** User manipuliert schwierigkeit=xxx zu invalidem Wert → Wave-1-Whitelist (`raw2_lower in ('leicht','mittel','schwer')`) garantiert dass `schwierigkeit_raw` in Template-Context nur diese 3 Werte ist; `/training/start`-Handler parst defensiv mit Int-Fallback (bekannt aus Phase 07.1) → keine neue Vuln.

Keine neuen Surface-Zugriffe auf Network, Auth, File-System oder DB-Schema.

## Next Phase Readiness

Wave 3 abgeschlossen. Wave 4 (Plan 04 — CSS-Version-Bump + Deploy + UAT) kann starten:
- Training-End-Flow: Redirect statt Overlay (cc33b37)
- Session-Detail-Header: "Nochmal trainieren"-Button (50dd0d7)
- CSS-Version-Bump noch zu machen (Wave 4)
- UAT-Test noch zu machen (Wave 4)

## Self-Check: PASSED

**Files verified present:**
- `templates/training.html` (modified, Task 3.1) — FOUND
- `templates/session_detail.html` (modified, Task 3.2 Schritt A) — FOUND
- `static/nerve.css` (modified, Task 3.2 Schritt B) — FOUND
- `.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-03-SUMMARY.md` — FOUND (this file)

**Commits verified present:**
- `cc33b37` (Task 3.1) — FOUND in git log
- `50dd0d7` (Task 3.2) — FOUND in git log

**Wave-3-Gate verified:**
- 6/6 Gates PASS (siehe Tabelle oben)
- Template parses clean (Flask + Jinja2 fuer training.html + session_detail.html)
- Button-URL-Generation 3/3 Mental-Walkthrough-Faelle korrekt

---

*Phase: 07.2-scoring-konsolidierung-inserted*
*Completed: 2026-04-20*
