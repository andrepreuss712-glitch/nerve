---
phase: 07.2
plan: 02
type: execute
wave: 2
depends_on:
  - "07.2-01"
files_modified:
  - templates/session_detail.html
  - static/nerve.css
autonomous: true
requirements:
  - POLISH-32
  - POLISH-34
must_haves:
  truths:
    - "templates/session_detail.html rendert 3 neue Sektionen: 12 Wendepunkt-Analyse, 13 Einzel-Bewertungen, 14 Verbesserungspotenzial"
    - "Sektion 10 'Gespraechs-Analyse' ist ENTFERNT (durch Sektion 12 ersetzt)"
    - "Training-Header zeigt Drei-Badge-Block (Training + Persoenlichkeit + Schwierigkeit) + Kunden-Subtext wenn pt.name match"
    - "Typ-Diskriminierung konsistent via `{% if conv.typ == 'training' %}` (NICHT session_mode)"
    - "static/nerve.css enthaelt neue Klassenfamilie: .n-session-detail-wendepunkt-*, .n-session-detail-einzel-score-*, .n-session-detail-improvement-*, .n-session-detail-kunden-subtext"
    - "Alle neuen CSS-Klassen unter .n-session-detail-* Namensraum"
  artifacts:
    - path: "templates/session_detail.html"
      provides: "3 neue Sektionen + Header-Erweiterung + Sektion 10 entfernt"
      contains: "n-session-detail-wendepunkt-card"
    - path: "static/nerve.css"
      provides: "Wendepunkt/Einzel-Score/Verbesserungs-CSS-Klassen"
      contains: ".n-session-detail-wendepunkt-list"
  key_links:
    - from: "templates/session_detail.html Sektion 12"
      to: "scoring_wendepunkte_detail (Wave 1)"
      via: "{% for w in scoring_wendepunkte_detail %}"
      pattern: "scoring_wendepunkte_detail"
    - from: "templates/session_detail.html Sektion 13"
      to: "scoring_kategorien (Wave 1)"
      via: "{% for kat in scoring_kategorien %}"
      pattern: "scoring_kategorien"
    - from: "templates/session_detail.html Sektion 14"
      to: "scoring_verbesserungen (Training) + _derive_practice_recommendations (Live)"
      via: "typ-Branch"
      pattern: "scoring_verbesserungen"
    - from: "templates/session_detail.html Header"
      to: "kunden_name + kunden_alter (Wave 1)"
      via: "{% if kunden_name %}...{% endif %}"
      pattern: "n-session-detail-kunden-subtext"
---

<objective>
Wave 2 der Scoring-Konsolidierung: Frontend UI-Extension.

**Was diese Wave liefert:**
- `templates/session_detail.html` bekommt drei neue Sektionen (12 Wendepunkt-Analyse, 13 6 Einzel-Scores, 14 Verbesserungspotenzial) UNTER den bestehenden 11.
- Sektion 10 "Gespraechs-Analyse" wird ENTFERNT (gemaess CONTEXT-Entscheidung — aufgeloest durch Sektion 12).
- Training-Header bekommt optionalen Kunden-Subtext (`{{ kunden_name }}, {{ kunden_alter }}`), wenn pt.name das "Name, Alter"-Pattern matcht.
- Typ-Diskriminierung: Training = 3 Sektionen mit Daten aktiv, Live = Empty-States mit expliziter Phase-4.19-Referenz.
- `static/nerve.css` bekommt neue Klassenfamilie: `.n-session-detail-wendepunkt-*`, `.n-session-detail-einzel-score-*`, `.n-session-detail-improvement-*`, `.n-session-detail-kunden-subtext`.

**Warum das matters:** Hier wird die eigentliche UI-Konsolidierung sichtbar. Dieser Wave setzt auf Wave 1 auf (Context-Variablen muessen existieren). Er laeuft parallel zu Wave 3 moeglich, weil er KEINE training.html aendert — nur session_detail.html + nerve.css.

**Output:** Erweiterte Session-Detail-Seite mit 13 aktiven Sektionen (11 bestehend + 12/13/14) — Sektion 10 ist weg.

**KRITISCH:** Sektion 14 Live-Branch nutzt eine BESTEHENDE Variable aus session_detail()-Context (`recommendations`), die bereits seit 07.1 aus `_derive_practice_recommendations()` kommt. Keine neue Backend-Aenderung in dieser Wave — Variable existiert schon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-CONTEXT.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-RESEARCH.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-01-PLAN.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/07.1-UI-SPEC.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/DEVIATIONS.md

# Source files that need modification
@templates/session_detail.html
@static/nerve.css

# Read-only references
@routes/dashboard.py

<interfaces>
<!-- Template-Context-Variablen, verfuegbar nach Wave 1 (07.2-01-PLAN.md) -->

### Wave-1-geliefert (Session-Detail-Route-Context)
```jinja
{# Alle folgenden sind im Template verfuegbar: #}
conv                       {# ConversationLog-Object (bestehend) #}
conv.typ                   {# 'live' | 'training' #}
conv.session_mode          {# 'cold_call' | 'meeting' | None #}
conv.personality_type_id   {# int | None #}

pt                         {# PersonalityType-Object | None #}
pt.name                    {# z.B. 'Markus Wendland, 48' ODER 'Beschaeftigter Chef' #}
pt.icon                    {# Emoji | None #}

scoring_kategorien         {# list[dict] — [] wenn nicht Training/keine Daten. Schema: {name: str, score: int, feedback: str} #}
scoring_wendepunkte_detail {# list[dict] — [] wenn nicht Training/keine Daten. Schema: {turn: int, berater_sagt: str, was_schief: str, alternativ: str} #}
scoring_verbesserungen     {# list[str] — [] wenn nicht Training/keine Daten #}
kunden_name                {# str | None #}
kunden_alter               {# str | None #}

schwierigkeit_label        {# 'Einsteiger' | 'Fortgeschritten' | 'Experte' | None — aus 07.1 Wave 4 #}
recommendations            {# list[dict] — aus _derive_practice_recommendations(), bestehend seit 07.1 #}
# recommendations[i]-Schema: {observation: str, explanation: str, training_focus: str | None, training_url: str | None, cross_context: dict | None}
```

### bestehende CSS-Klassen (07.1, duerfen NICHT geaendert werden)
```css
.n-session-detail-card                 /* Generische weisse Kachel */
.n-session-detail-card-title           /* Section-Headline H2 */
.n-session-detail-card-title--spaced   /* Heading mit Top-Margin */
.n-session-detail-minibar              /* 4px-hoher Progress-Bar */
.n-session-detail-empty                /* Empty-State Standard */
.n-session-detail-empty-future         /* Future-Phase Empty-State (dashed border) */
.n-session-detail-future-text          /* Empty-Future Body-Text */
.n-session-detail-icon-sm              /* 16px Icon */
.n-session-detail-icon-xl              /* 48px Icon */
.n-label                               /* 12px/600/uppercase/letter-spacing 0.5px */
.n-badge / .n-badge-primary / .n-badge-neutral / .n-badge-success  /* Badge-Familie */
```

### bestehende Template-Struktur (Sektionen 1-11)
```jinja
<header class="n-session-detail-header">                  {# Sektion 1 — Header #}
  ...
  {% if conv.typ == 'training' %} ... {% else %} ... {% endif %}
</header>

<section class="n-session-detail-hero">...</section>       {# Sektion 2 — Score-Hero #}
<section class="n-session-detail-card">...</section>       {# Sektion 3 — Chart (kb_verlauf/stimmung) #}
<section class="n-session-detail-card">...</section>       {# Sektion 4 — Einwand-Timeline #}
<section class="n-session-detail-card">...</section>       {# Sektion 5 — Phasen-Strip #}
<section class="n-session-detail-card">...</section>       {# Sektion 6 — Skript-Abdeckung #}
<section class="n-session-detail-card">...</section>       {# Sektion 9 — "Was du ueben solltest" #}
<section class="n-session-detail-card">...</section>       {# Sektion 7 — Painpoints #}
<section class="n-session-detail-card">...</section>       {# Sektion 8 — PreCall-Briefing #}
<section class="n-session-detail-card">...</section>       {# Sektion 10 — Gespraechs-Analyse (ENTFERNEN in dieser Wave!) #}
<section class="n-session-detail-card">...</section>       {# Sektion 11 — Lernkarten (Future) #}
```

### Wendepunkt-Card-Visual-Spec (analog zum aktuellen training.html Post-Call-Overlay)
```
┌───────────────────────────────────────────┐
│ Turn 3                                    │  <- n-label, 12px uppercase
│                                           │
│ Du hast gesagt:                           │  <- n-label
│ "Das ist guenstig, nicht guenstig."       │  <- italic, text-secondary
│                                           │
│ Problem:                                  │  <- n-label
│ Fuellworte ohne Substanz.                 │  <- text-secondary
│                                           │
│ Besser waere:                             │  <- n-label (TEAL-accent)
│ "Unser Preis amortisiert sich in 3 Mon."  │  <- regular, text-primary, teal left-border
└───────────────────────────────────────────┘
```

### Design-Tokens (aus 07.1 UI-SPEC, duerfen NICHT geaendert werden)
```css
--page-bg: #F0F2F5;
--glass-bg: #FFFFFF;
--glass-border: #E2E8F0;
--page-text-color: #1a1a1a;
--page-text-secondary: #6B7280;
--btn-primary-bg-from: #00D4AA;  /* teal accent */
--btn-danger-text: #f87171;      /* destructive */
--metric-fill-primary-from: ... /* bestehende Progress-Bar Gradient */
--metric-fill-primary-to: ...
```
</interfaces>
</context>

<deviation_prevention>
## Precondition Checks (MUST vor Task-Start)

1. **Wave-1-Context-Variablen verifizieren:**
   ```bash
   grep -n "scoring_kategorien\|scoring_wendepunkte_detail\|scoring_verbesserungen\|kunden_name" routes/dashboard.py
   ```
   MUSS >=5 Matches liefern (sonst Wave 1 nicht fertig — STOP).

2. **bestehende Sektion-10-Stelle lokalisieren:**
   ```bash
   grep -n "Gespraechs-Analyse\|Gespraechs-Analyse\|Kommt mit Phase 4.19" templates/session_detail.html
   ```
   MUSS Zeilen-Bereich liefern, die in dieser Wave entfernt werden (~Zeile 300-325 laut RESEARCH).

3. **Umlaut-Identifier-Check (Pitfall 2, UAT-R3 K Recidiv!):**
   ```bash
   grep -nE "\{\{[^}]*[aouAOU][\"]{2}[^}]*\}\}" templates/session_detail.html || echo "OK — keine Umlaut-Identifier in Jinja-Expressions"
   ```
   MUSS "OK" liefern. Alle NEUEN `{{ ... }}` in dieser Wave MUESSEN ASCII-Identifier nutzen.

4. **CSS_VERSION NICHT bumpen:**
   ```bash
   grep -n "CSS_VERSION" app.py
   ```
   MUSS aktuellen Wert (Phase 07.1 Wave 4: `'20260420-3'`) zeigen. Wave 2 aendert das NICHT — passiert in Wave 4.

## Deviation-Prevention Tasks

- **P1 (Rule-1 Feldnamen):** Template liest NUR aus Wave-1-Context-Variablen, keine DB-Attribute direkt bei neuen Sektionen. Wenn `pt.name`, `pt.icon`, `conv.typ`, `conv.personality_type_id` verwendet werden — diese sind ASCII-verifiziert (models.py). **Status: Enforced.**

- **P2 (Umlaut-Identifier):**
  - User-facing Text IN Templates MIT echten Umlauten: "Wendepunkt-Analyse", "Einzel-Bewertungen", "Verbesserungspotenzial", "Kommt mit Phase 4.19", "Du hast gesagt:", "Besser wäre:", "Problem:".
  - Jinja-Expressions + CSS-Klassen + HTML-IDs OHNE Umlaute: `scoring_kategorien`, `.n-session-detail-wendepunkt-card`, `id="sd-einzel-score-list"`.
  - **BLOCKING CHECK** nach Task-Abschluss: `grep -E "class=['\"][^'\"]*[aouAOU][\"]{2}" templates/session_detail.html` MUSS kein Match liefern (keine Umlaut-CSS-Klassen).

- **P3 (kb_end-Sync):** Nicht relevant in Wave 2 (kein neuer kb_end-Consumer).

- **P4 (JSON max_tokens):** Nicht relevant (keine neuen Claude-Prompts).

- **P5 (HTTPException-Passthrough):** Nicht relevant (kein Backend).

- **P6 (Dedupe-Logik):** Sektion 14 Training rendert `scoring_verbesserungen`-Liste. `generate_scoring()` liefert eindeutige Einträge, aber **vorsichtshalber** im Template `{% for v in scoring_verbesserungen | unique %}` verwenden, analog Sektion 7 Dedupe-Pattern.

- **P7 (Typ-Diskriminierung):** ALLE neuen Branches nutzen `{% if conv.typ == 'training' %}` (NICHT `session_mode == 'training'`). Sektion 14 Live-Branch: `{% else %}` (das faengt `typ == 'live'` ab).

- **P8 (Template-Parser-Check VOR Deploy):** Am Ende dieser Wave:
  ```bash
  python -c "from app import app; app.jinja_env.get_template('session_detail.html'); print('OK')"
  ```
  MUSS "OK" liefern.

- **P9 (CSS-Klassen-Namensraum):** Alle neuen Klassen unter `.n-session-detail-*` Familie:
  - `.n-session-detail-wendepunkt-list`, `-card`, `-quote`, `-problem`, `-better`, `-turn-label`
  - `.n-session-detail-einzel-score-list`, `-item`, `-top`, `-feedback`
  - `.n-session-detail-improvement-list`, `-item`, `-icon`
  - `.n-session-detail-kunden-subtext`
  - **BLOCKING CHECK:** `grep -E "^\.n-" static/nerve.css | grep -v "^\.n-session-detail-\|^\.n-btn\|^\.n-label\|^\.n-badge\|^\.n-h[0-9]\|^\.n-page" | grep -i "wendepunkt\|einzel-score\|improvement\|kunden-subtext"` MUSS kein Match liefern.

- **P10 (STATE.md/ROADMAP.md/SUMMARY.md NICHT im Executor):** Nur `07.2-02-SUMMARY.md` erstellen.
</deviation_prevention>

<tasks>

<task type="auto" tdd="false">
  <name>Task 2.1: Sektion 10 entfernen + Sektion 12 (Wendepunkt-Analyse) + Sektion 13 (6 Einzel-Scores) + Sektion 14 (Verbesserungspotenzial) in session_detail.html einfuegen</name>
  <files>templates/session_detail.html</files>
  <action>
  **Vier zusammengehoerige Aenderungen in templates/session_detail.html:**

  ### Schritt A: Sektion 10 "Gespraechs-Analyse" ENTFERNEN

  Lokalisieren per `grep -n "Gespraechs-Analyse\|Gespraechs-Analyse folgt" templates/session_detail.html` — erwartet Bereich um Zeile 300-325.

  Komplett entfernen: die gesamte `<section class="n-session-detail-card...">` fuer "Gespraechs-Analyse" inklusive `n-session-detail-empty-future`-Block und "Kommt mit Phase 4.19"-Badge. Der Block beginnt mit `<section class="n-session-detail-card n-session-detail-card--future">` oder aehnlich und endet mit `</section>`.

  BEACHTE: Sektion 11 "Lernkarten" BLEIBT unveraendert (ist eine andere Section mit "Kommt mit Phase 4.11").

  Nach dem Entfernen:
  ```bash
  grep -c "Gespraechs-Analyse" templates/session_detail.html
  ```
  MUSS `0` liefern.

  ### Schritt B: Sektion 12 — Wendepunkt-Analyse EINFUEGEN

  Position: Direkt NACH Sektion 11 (Lernkarten Future-Card) — d.h. als vorletztes Element vor `</main>` / vor dem `{% endblock %}` am Ende.

  Wichtig: Genaue Position per `grep -n "Lernkarten\|Kommt mit Phase 4.11" templates/session_detail.html` bestimmen. Neue Section kommt NACH Sektion 11.

  ```jinja
  {# ───── Sektion 12: Wendepunkt-Analyse (NEU 07.2, ersetzt ehemalige Sektion 10) ───── #}
  <section class="n-session-detail-card">
    <h2 class="n-session-detail-card-title">Wendepunkt-Analyse</h2>
    {% if conv.typ == 'training' and scoring_wendepunkte_detail %}
      <ul class="n-session-detail-wendepunkt-list">
        {% for w in scoring_wendepunkte_detail %}
          <li class="n-session-detail-wendepunkt-card">
            {% if w.turn is defined %}
              <div class="n-label n-session-detail-wendepunkt-turn-label">Turn {{ w.turn }}</div>
            {% endif %}
            <div class="n-label">Du hast gesagt:</div>
            <div class="n-session-detail-wendepunkt-quote">"{{ w.berater_sagt }}"</div>
            <div class="n-label">Problem:</div>
            <div class="n-session-detail-wendepunkt-problem">{{ w.was_schief }}</div>
            <div class="n-label">Besser wäre:</div>
            <div class="n-session-detail-wendepunkt-better">"{{ w.alternativ }}"</div>
          </li>
        {% endfor %}
      </ul>
    {% elif conv.typ == 'training' %}
      {# Training ohne Wendepunkt-Daten — z.B. alte Sessions vor Phase 04.10.1 #}
      <div class="n-session-detail-empty">
        <h3>Keine Wendepunkte erfasst</h3>
        <p>Für diese Training-Session wurden keine Wendepunkt-Details gespeichert. Aktuelle Trainings mit Stimmungs-Tracking zeigen Wendepunkte hier automatisch.</p>
      </div>
    {% else %}
      {# Live Cold Call / Meeting — Phase-4.19-Empty-State #}
      <div class="n-session-detail-empty-future">
        <i data-lucide="message-square-text" class="n-session-detail-icon-xl"></i>
        <h3 class="n-session-detail-card-title--spaced">Wendepunkt-Analyse folgt</h3>
        <p class="n-session-detail-future-text">Die Wendepunkt-Analyse für Live-Sessions kommt mit Phase 4.19 (Transkript-Persistierung). Der Analyse-Loop markiert dann einzelne Sätze, die den Gesprächsverlauf kritisch beeinflusst haben.</p>
        <div class="n-badge n-badge-neutral n-session-detail-card-title--spaced">Kommt mit Phase 4.19</div>
      </div>
    {% endif %}
  </section>
  ```

  ### Schritt C: Sektion 13 — 6 Einzel-Bewertungen EINFUEGEN

  Position: Direkt NACH Sektion 12.

  ```jinja
  {# ───── Sektion 13: Einzel-Bewertungen (NEU 07.2) ───── #}
  <section class="n-session-detail-card">
    <h2 class="n-session-detail-card-title">Einzel-Bewertungen</h2>
    {% if conv.typ == 'training' and scoring_kategorien %}
      <ul class="n-session-detail-einzel-score-list">
        {% for kat in scoring_kategorien %}
          <li class="n-session-detail-einzel-score-item">
            <div class="n-session-detail-einzel-score-top">
              <span class="n-label">{{ kat.name }}</span>
              <span>{{ kat.score | default(0) }}/10</span>
            </div>
            <div class="n-session-detail-minibar"><span style="--w: {{ ((kat.score or 0) * 10) }}%"></span></div>
            {% if kat.feedback %}
              <div class="n-session-detail-einzel-score-feedback">{{ kat.feedback }}</div>
            {% endif %}
          </li>
        {% endfor %}
      </ul>
    {% elif conv.typ == 'training' %}
      <div class="n-session-detail-empty">
        <h3>Keine Einzel-Bewertungen</h3>
        <p>Für diese Training-Session wurden keine Kategorien-Scores gespeichert.</p>
      </div>
    {% else %}
      {# Live — komplett Empty-State mit 4.19-Hinweis (vermeidet POLISH-34-Dopplung mit Score-Hero) #}
      <div class="n-session-detail-empty-future">
        <i data-lucide="bar-chart-3" class="n-session-detail-icon-xl"></i>
        <h3 class="n-session-detail-card-title--spaced">Einzel-Bewertungen folgen</h3>
        <p class="n-session-detail-future-text">Detaillierte Einzel-Bewertungen (Gesprächseröffnung, Bedarfsanalyse, Einwandbehandlung, Gesprächsführung, Abschluss, Beziehungsaufbau) werden ab Phase 4.19 (Transkript-Persistierung) verfügbar. Aktuell siehst du den Gesamt-Score und seine 4 Komponenten in Sektion 2.</p>
        <div class="n-badge n-badge-neutral n-session-detail-card-title--spaced">Kommt mit Phase 4.19</div>
      </div>
    {% endif %}
  </section>
  ```

  ### Schritt D: Sektion 14 — Verbesserungspotenzial EINFUEGEN

  Position: Direkt NACH Sektion 13.

  Datenquelle-Entscheidung (Research Open Question 1, Option b): Training nutzt `scoring_verbesserungen[]` (Strings aus generate_scoring), Live nutzt `recommendations[].explanation` (existiert seit 07.1).

  ```jinja
  {# ───── Sektion 14: Verbesserungspotenzial (NEU 07.2) ───── #}
  <section class="n-session-detail-card">
    <h2 class="n-session-detail-card-title">Verbesserungspotenzial</h2>
    {% if conv.typ == 'training' and scoring_verbesserungen %}
      <ul class="n-session-detail-improvement-list">
        {% for v in scoring_verbesserungen %}
          <li class="n-session-detail-improvement-item">
            <i data-lucide="alert-triangle" class="n-session-detail-improvement-icon n-session-detail-icon-sm"></i>
            <span>{{ v }}</span>
          </li>
        {% endfor %}
      </ul>
    {% elif conv.typ == 'live' and recommendations %}
      {# Live: recommendations aus _derive_practice_recommendations (Mix heute-abgeleitet + 4.19-future) #}
      <ul class="n-session-detail-improvement-list">
        {% for r in recommendations %}
          <li class="n-session-detail-improvement-item">
            <i data-lucide="alert-triangle" class="n-session-detail-improvement-icon n-session-detail-icon-sm"></i>
            <span>{{ r.explanation }}</span>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <div class="n-session-detail-empty">
        <h3>Kein Verbesserungspotenzial identifiziert</h3>
        <p>Für diese Session wurden keine konkreten Handlungstipps generiert.</p>
      </div>
    {% endif %}
  </section>
  ```

  ### Schritt E: Training-Header um Kunden-Subtext erweitern

  Lokalisieren: bestehender Training-Header-Block per `grep -n "conv.typ == 'training'" templates/session_detail.html | head -5` (erwartet ~Zeile 10-30, dort wo bereits die 3 Badges aus 07.1 Wave 4 rendern: Training + Persoenlichkeit + Schwierigkeit).

  Nach dem `{% if schwierigkeit_label %}...{% endif %}`-Block innerhalb des Training-Branches EINFUEGEN (aber innerhalb desselben `{% if conv.typ == 'training' %}`-Blocks):

  ```jinja
  {# NEU 07.2: Kunden-Subtext — nur wenn pt.name Pattern "Name, Alter" matcht #}
  {% if kunden_name %}
    <span class="n-session-detail-kunden-subtext">{{ kunden_name }}{% if kunden_alter %}, {{ kunden_alter }}{% endif %}</span>
  {% endif %}
  ```

  ### Sicherheits-Checks nach Abschluss

  1. **Template-Parser-Check:**
     ```bash
     python -c "from app import app; app.jinja_env.get_template('session_detail.html'); print('OK')"
     ```
     MUSS "OK" liefern — sonst sofort stoppen, Fehler fixen.

  2. **Sektion 10 raus:**
     ```bash
     grep -c "Gespraechs-Analyse" templates/session_detail.html
     ```
     MUSS `0` liefern.

  3. **Umlaut-Identifier-Scan:**
     ```bash
     grep -nE "\{\{[^}]*[aouAOU][\"]{2}[^}]*\}\}|\{\%[^\%]*[aouAOU][\"]{2}[^\%]*\%\}" templates/session_detail.html
     ```
     MUSS kein Match liefern.
  </action>
  <verify>
    <automated>python -c "from app import app; app.jinja_env.get_template('session_detail.html'); print('Template-Parser: OK')" && echo "Sektion 10 entfernt:" && test "$(grep -c 'Gespraechs-Analyse' templates/session_detail.html)" = "0" && echo OK</automated>
  </verify>
  <done>
  - Sektion 10 "Gespraechs-Analyse" ist komplett aus session_detail.html entfernt (grep-count 0).
  - Drei neue Sektionen (12, 13, 14) eingefuegt in dieser Reihenfolge NACH Sektion 11.
  - Alle 3 neuen Sektionen haben typ-diskriminierende Branches (`{% if conv.typ == 'training' %}`, NICHT session_mode).
  - Training-Header hat neuen Kunden-Subtext-Block mit `{% if kunden_name %}`.
  - Template-Parser-Check liefert "OK".
  - Kein `{{ ... }}` mit Umlaut-Identifier.
  - User-facing Text hat echte Umlaute ("Wendepunkt-Analyse", "Besser wäre:", "Einzel-Bewertungen", "Verbesserungspotenzial", "geführt").
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: CSS-Klassen fuer Wendepunkt, Einzel-Score, Verbesserungen, Kunden-Subtext in nerve.css erweitern</name>
  <files>static/nerve.css</files>
  <action>
  **Neue CSS-Klassen in static/nerve.css einfuegen.**

  Position: Am Ende der `.n-session-detail-*`-Klassenfamilie (per `grep -n "n-session-detail-empty-future\|n-session-detail-future-text" static/nerve.css | tail -3` die letzte `.n-session-detail-*`-Rule lokalisieren und DANACH einfuegen).

  Alle neuen Klassen MUESSEN:
  - Unter `.n-session-detail-*` Namensraum stehen
  - CSS-Tokens aus 07.1 wiederverwenden (KEINE neuen Farben, KEINE Gelb-/Gold-Hex-Werte)
  - Keine Inline-Styles im Template erzwingen (B-01-Regel aus 07.1)
  - ASCII-only in Selektoren (Pitfall 2: kein `.n-session-detail-gespräch`)

  ```css
  /* ── Phase 07.2 Sektion 12: Wendepunkt-Analyse ───────────────────── */

  .n-session-detail-wendepunkt-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 16px;
  }

  .n-session-detail-wendepunkt-card {
    background: var(--glass-bg);
    border: 1.5px solid var(--glass-border);
    border-radius: 12px;
    padding: 16px 20px;
    display: grid;
    gap: 8px;
  }

  .n-session-detail-wendepunkt-turn-label {
    color: var(--page-text-secondary);
    margin-bottom: 4px;
  }

  .n-session-detail-wendepunkt-quote {
    font-style: italic;
    color: var(--page-text-secondary);
    font-size: 14px;
    line-height: 1.5;
  }

  .n-session-detail-wendepunkt-problem {
    color: var(--page-text-secondary);
    font-size: 14px;
    line-height: 1.5;
  }

  .n-session-detail-wendepunkt-better {
    color: var(--page-text-color);
    font-size: 14px;
    line-height: 1.5;
    border-left: 3px solid var(--btn-primary-bg-from);
    padding-left: 12px;
    margin-top: 4px;
  }

  /* ── Phase 07.2 Sektion 13: Einzel-Bewertungen ───────────────────── */

  .n-session-detail-einzel-score-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 16px;
  }

  .n-session-detail-einzel-score-item {
    display: grid;
    gap: 6px;
  }

  .n-session-detail-einzel-score-top {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 8px;
  }

  .n-session-detail-einzel-score-top > span:last-child {
    font-family: 'JetBrains Mono', monospace;
    font-weight: 600;
    font-size: 14px;
    color: var(--page-text-color);
  }

  .n-session-detail-einzel-score-feedback {
    font-size: 14px;
    line-height: 1.5;
    color: var(--page-text-secondary);
    margin-top: 4px;
  }

  /* ── Phase 07.2 Sektion 14: Verbesserungspotenzial ───────────────── */

  .n-session-detail-improvement-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 12px;
  }

  .n-session-detail-improvement-item {
    display: grid;
    grid-template-columns: 20px 1fr;
    gap: 12px;
    align-items: start;
    font-size: 14px;
    line-height: 1.5;
    color: var(--page-text-color);
  }

  .n-session-detail-improvement-icon {
    color: var(--page-text-secondary);
    flex-shrink: 0;
  }

  /* ── Phase 07.2 Header-Erweiterung: Kunden-Subtext ──────────────── */

  .n-session-detail-kunden-subtext {
    font-size: 14px;
    color: var(--page-text-secondary);
    font-weight: 400;
    margin-left: 4px;
  }
  ```

  **Anti-Pattern-Check nach Abschluss:**

  1. **CSS-Klassen alle ASCII:**
     ```bash
     grep -nE "\.n-session-detail-[a-z0-9-]*[aouAOU][\"]{2}" static/nerve.css
     ```
     MUSS kein Match liefern (keine Umlaut-CSS-Klassen).

  2. **Keine Gelb-/Gold-Farben eingefuehrt (Phase 7 Rule):**
     ```bash
     grep -nE "#[eE]8[bB]040|#[bB]8872[eE]|--learning-gold" static/nerve.css
     ```
     Bestehende Treffer sind OK (aus 07.1), aber KEIN NEUER Match in Zeilen der neu eingefuegten Rules.

  3. **Alle neuen Rules nutzen `var(--*)` fuer Farben** (keine Hex-Hardcoded-Farben ausser 0 und in `0/1.5px solid`):
     Neue Rules per Visual-Read gegen-checken — alles was Farbe ist MUSS `var(--...)` sein.
  </action>
  <verify>
    <automated>grep -c "n-session-detail-wendepunkt-card\|n-session-detail-einzel-score-item\|n-session-detail-improvement-item\|n-session-detail-kunden-subtext" static/nerve.css</automated>
  </verify>
  <done>
  - Neue CSS-Klassen in nerve.css am Ende der `.n-session-detail-*` Familie eingefuegt.
  - Mindestens 4 der 4 Kern-Klassen vorhanden: wendepunkt-card, einzel-score-item, improvement-item, kunden-subtext.
  - Alle Farben ueber `var(--*)` Tokens (keine neuen Hex-Farben ausser Standard-0/Standard-1.5px).
  - Keine Umlaut-CSS-Klassen.
  - Keine Gelb-/Gold-Regression.
  - Wendepunkt-Card-Layout: Grid mit 8px gap, 16/20px Padding, 12px border-radius, left-border 3px teal am "Besser wäre:"-Block.
  - Einzel-Score-Layout: Label + Score + Minibar + optional Feedback-Text.
  - Improvement-Item: 20px Icon-Column + 1fr Text (Alert-Triangle-Icon + Tipp).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Session-Detail-Template | Jinja-Autoescape rendert User-Daten aus `phasen_details`-JSON |
| phasen_details-JSON → Template | Manipulierte Sonnet-Output-Strings koennten XSS versuchen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-05 | T (Tampering) | `{{ w.berater_sagt }}`, `{{ kat.feedback }}`, `{{ v }}` (AI-Output als Template-Variable) | mitigate | Jinja-Autoescape aktiv (Default). **KEIN `\| safe`-Filter** auf AI-Output in neuen Sektionen. Explizit verifizieren: `grep -n "scoring_\|kunden_name\|kunden_alter" templates/session_detail.html \| grep "safe"` MUSS kein Match liefern. |
| T-07.2-06 | T (Tampering) | `kat.score * 10` in `--w` Custom-Property | mitigate | Wrap mit `(kat.score or 0)` fuer None-Tolerance. Math-Operation in Jinja ist sicher (keine Expression-Injection moeglich). |
| T-07.2-07 | I (Information Disclosure) | Kunden-Name/Alter in HTML | accept | Daten sind Session-Owner's eigene pt-Data. Kein Cross-User-Leak (Wave-1-Filter `conv.user_id == g.user.id`). |

</threat_model>

<verification>

## Wave-2-Gate (alle MUSS erfuellt sein fuer Wave-3 Start — Wave 3 KANN parallel laufen, aber final UAT braucht Wave 2)

1. **Template-Parser clean:**
   ```bash
   python -c "from app import app; app.jinja_env.get_template('session_detail.html'); print('OK')"
   ```
   → "OK".

2. **Sektion 10 entfernt:**
   ```bash
   grep -c "Gespraechs-Analyse" templates/session_detail.html
   ```
   → `0`.

3. **Neue Sektionen vorhanden:**
   ```bash
   grep -c "Wendepunkt-Analyse\|Einzel-Bewertungen\|Verbesserungspotenzial" templates/session_detail.html
   ```
   → `>=3`.

4. **Neue CSS-Klassen vorhanden:**
   ```bash
   grep -c "n-session-detail-wendepunkt-card\|n-session-detail-einzel-score-item\|n-session-detail-improvement-item\|n-session-detail-kunden-subtext" static/nerve.css
   ```
   → `>=4`.

5. **Keine Umlaut-Identifier in Jinja:**
   ```bash
   grep -nE "\{\{[^}]*[aouAOU][\"]{2}[^}]*\}\}" templates/session_detail.html
   ```
   → kein Match.

6. **Keine Umlaut-Identifier in CSS:**
   ```bash
   grep -nE "\.n-session-detail-[a-z0-9-]*[aouAOU][\"]{2}" static/nerve.css
   ```
   → kein Match.

</verification>

<success_criteria>

- templates/session_detail.html rendert 13 aktive Sektionen (11 bestehend + 12/13/14), Sektion 10 ist weg.
- Sektion 12 zeigt Training-Wendepunkte (Turn + 3 Zeilen: Gesagt/Problem/Besser waere), Live-Empty-State mit 4.19-Hinweis.
- Sektion 13 zeigt Training-Einzel-Scores (5-7 Kategorien mit Minibar + Feedback), Live-Empty-State mit 4.19-Hinweis.
- Sektion 14 zeigt Training-Verbesserungen (Strings aus `scoring.verbesserungen[]`) ODER Live-Verbesserungen (aus `recommendations[].explanation`), mit Alert-Triangle-Icon.
- Training-Header hat Kunden-Subtext "Markus Wendland, 48" wenn `pt.name` matcht, sonst Subtext weg.
- static/nerve.css enthaelt >=4 neue Klassen unter `.n-session-detail-*` Namensraum.
- Alle User-Text mit echten Umlauten, alle Identifier/Klassen/IDs ASCII.
- Template-Parser clean.
- Keine neuen Farben, keine Inline-Styles (ausser `--w` Custom-Property fuer Progress-Bar-Width wie in 07.1 B-01 akzeptiert), keine Gelb-/Gold-Regression.

</success_criteria>

<output>
Nach Abschluss erstellt der Executor AUSSCHLIESSLICH:
- `.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-02-SUMMARY.md`

Executor MUSS NICHT:
- STATE.md, ROADMAP.md updaten (Orchestrator)
- CSS_VERSION bumpen (Wave 4)
- deploy.sh ausfuehren (Wave 4)
- Andere SUMMARYs modifizieren
</output>
</content>
</invoke>