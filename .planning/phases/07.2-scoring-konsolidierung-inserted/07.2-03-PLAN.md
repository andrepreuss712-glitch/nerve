---
phase: 07.2
plan: 03
type: execute
wave: 3
depends_on:
  - "07.2-01"
files_modified:
  - templates/training.html
  - templates/session_detail.html
autonomous: true
requirements:
  - POLISH-32
  - POLISH-34
must_haves:
  truths:
    - "training.html Scoring-Phase komplett entfernt (Option A): buildScoring(), renderMoodChart(), renderWendepunkteDetail() sind weg"
    - "confirmEndTraining() redirected auf /session/<log_id> statt buildScoring() zu rufen"
    - "showPhase() Array enthaelt KEIN 't-scoring' mehr"
    - "#t-scoring HTML-Block aus training.html entfernt"
    - "session_detail.html hat 'Nochmal trainieren'-Action-Button rechts oben fuer Training-Sessions"
    - "Button-URL enthaelt personality_type_id + schwierigkeit + modus als Pre-Selection"
    - "Keine Console-Errors beim Durchlaufen von Training -> Session-Detail -> zurueck-Navigation"
  artifacts:
    - path: "templates/training.html"
      provides: "Scoring-Phase komplett ausgebaut, Redirect-Flow integriert"
      contains: "window.location.href = '/session/'"
    - path: "templates/session_detail.html"
      provides: "Action-Button 'Nochmal trainieren' im Header (Training-Sessions only)"
      contains: "n-session-detail-repeat-btn"
  key_links:
    - from: "templates/training.html confirmEndTraining()"
      to: "/training/end Response.log_id (Wave 1)"
      via: "window.location.href = '/session/' + data.log_id"
      pattern: "data\\.log_id"
    - from: "templates/session_detail.html Header"
      to: "/training?personality_type_id=X&schwierigkeit=Y&modus=Z"
      via: "<a href>"
      pattern: "n-session-detail-repeat-btn"
---

<objective>
Wave 3 der Scoring-Konsolidierung: Training-Overlay-Removal (Option A) + "Nochmal trainieren"-Button.

**Was diese Wave liefert:**
- `templates/training.html`: Post-Call-Overlay komplett entfernt (~280 LOC Delete).
  - `buildScoring()`, `renderMoodChart()`, `renderWendepunkteDetail()` — alle drei Funktionen geloescht.
  - `#t-scoring` HTML-Block entfernt.
  - `showPhase()` Array-Element `'t-scoring'` gestrichen.
  - `confirmEndTraining()` macht nach `/training/end`-Response Redirect auf `/session/<log_id>` statt Overlay-Rendering.
- `templates/session_detail.html`: Action-Button "Nochmal trainieren" oben rechts im Training-Header.
  - URL mit Pre-Selection: `personality_type_id`, `schwierigkeit`, `modus` (scenario_id NICHT — ist nicht persistiert, siehe Research Pitfall 4).

**Warum das matters:** Die UX-Konsolidierung — User landet nach "Beenden" im Training direkt auf der vollen Session-Detail-Seite, kein Overlay-Zwischenschritt mehr. Ein-Button-Restart direkt aus der Auswertung zurueck ins Training.

**Dependency auf Wave 1:** Dieser Wave BRAUCHT `data.log_id` aus /training/end-Response (Wave 1 Task 1.2). Ohne Wave 1 Task 1.2 wuerde der Redirect `window.location.href = '/session/undefined'` werfen.

**KEINE Dependency auf Wave 2:** Template-Layout der neuen Sektionen ist unabhaengig. Die Button-URL-Generierung nutzt NUR Wave-1-Context-Variablen. Wave 3 kann technisch parallel zu Wave 2 laufen, sofern Wave 1 fertig ist.

**Output:** Einheitlicher Training-End-Flow ohne Overlay, plus schneller Repeat-Training-Einstieg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-CONTEXT.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-RESEARCH.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-01-PLAN.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/DEVIATIONS.md

# Source files that need modification
@templates/training.html
@templates/session_detail.html

# Read-only references
@routes/training.py

<interfaces>
<!-- Wave-1 Context Variables for Button-URL Generation -->

### session_detail Context (nach Wave 1)
```jinja
conv                       {# ConversationLog #}
conv.typ                   {# 'training' hier relevant #}
conv.personality_type_id   {# int | None — persistiert #}
schwierigkeit_label        {# 'Einsteiger' | 'Fortgeschritten' | 'Experte' | None #}

{# SCHWIERIGKEIT: In /training/start-Handler wird der raw Key erwartet ('leicht'|'mittel'|'schwer'), #}
{# nicht das Label. Wir extrahieren den raw-Key zusaetzlich aus phasen_details.schwierigkeit. #}

{# SESSION_MODE fuer Training: existiert nicht als session_mode. Training hat session_mode=None. #}
{# Was persistiert IST: conv.typ='training' plus optional im Scoring-Daten die "hilfe_count" was auf modus=guided hinweist #}
```

### /training/end-Response nach Wave 1 Task 1.2
```json
{
  "ok": true,
  "log_id": 123,
  "gesamt_score": 74,
  "kategorien": [...],
  "staerken": [...],
  "verbesserungen": [...],
  "wendepunkte_detail": [...],
  "zusammenfassung": "..."
}
```

### bestehende training.html-Struktur (Phase-Tabs)
```javascript
// showPhase() erwartet einen Phasen-Key
// Aktuell: ['t-setup', 't-einwahl', 't-chat', 't-scoring']
// Nach Wave 3: ['t-setup', 't-einwahl', 't-chat']

// DOM-Elemente die komplett weg muessen:
#t-scoring                    // Scoring-Phase-Container
#t-scoringInner               // innerHTML-Target fuer buildScoring()
#t-mood-chart                 // Canvas fuer Mood-Chart
#t-wendepunkte-detail         // Container fuer Wendepunkt-Cards
#t-save-personality-div       // Save-Personality-Prompt
// Personality-Reveal-Div (Zeile ~1493-1504)

// Funktionen die komplett weg muessen (~280 LOC):
buildScoring(data)                  // Zeile 1531-1663
renderMoodChart(stimmungHistory)    // Zeile 1666-1743
renderWendepunkteDetail(wendepunkte) // Zeile 1746-1778

// Funktion die ANGEPASST wird:
confirmEndTraining()                // Zeile ~1440-1528 — Redirect statt Overlay

// Funktion die ANGEPASST wird (nur Array):
showPhase(phaseKey)                 // Zeile ~1804 — 't-scoring' aus Array streichen
```

### bestehende session_detail.html Training-Header-Struktur (nach Wave 2 oder unabhaengig davon)
```jinja
<header class="n-session-detail-header {% if conv.typ == 'training' %}n-session-detail-header--training{% endif %}">
  <a class="n-btn n-btn-ghost" href="{{ url_for('dashboard.analytics_page') }}">← Zurück zur History</a>
  <h1 class="n-session-detail-card-title n-session-detail-card-title--spaced">Session #{{ conv.id }}</h1>

  {# NEU 07.2 Wave 3: Action-Button hier einfuegen (zwischen H1 und Meta-Row) #}

  <div class="n-session-detail-meta">
    {# bestehender Meta-Row-Content #}
  </div>
</header>
```

### routes/training.py /training/start — accepted Query-Params
```python
# Aus routes/training.py:140-190 (Muster aus 07.1 Research verifiziert):
# GET /training akzeptiert:
#   ?personality_type_id=<int>
#   ?scenario_id=<int>  (nice-to-have, aber wir persistieren keinen scenario_id)
#   ?schwierigkeit=<leicht|mittel|schwer>  (raw key, NICHT Label)
#   ?modus=<frei|guided>  (Training-Modus, falls unterstuetzt)
```
</interfaces>
</context>

<deviation_prevention>
## Precondition Checks (MUST vor Task-Start)

1. **Wave 1 fertig — log_id im training_end Response:**
   ```bash
   grep -n "log_id=_log_id\|log_id=log.id" routes/training.py
   ```
   MUSS >=1 Match liefern. Sonst STOP — Wave 3 kann nicht funktionieren.

2. **Aktuelle training.html Scoring-Phase lokalisieren:**
   ```bash
   grep -n "function buildScoring\|function renderMoodChart\|function renderWendepunkteDetail\|id=\"t-scoring\"" templates/training.html
   ```
   MUSS mindestens 4 Matches liefern (3 Funktionen + 1 DOM-Element). Exakte Zeilen-Bereiche fuer Cleanup identifizieren.

3. **showPhase-Definition lokalisieren:**
   ```bash
   grep -n "function showPhase\|'t-scoring'" templates/training.html
   ```
   MUSS Zeile ~1804 + `'t-scoring'`-Listenelement zeigen.

4. **Query-Param-Handler in /training/start:**
   ```bash
   grep -n "personality_type_id\|schwierigkeit\|modus" routes/training.py | head -20
   ```
   Verifizieren: `/training`-GET-Route (Zeile ~60-200) akzeptiert diese Query-Params und nutzt sie als Pre-Selection.

5. **schwierigkeit-raw-Key extrahieren:**
   `schwierigkeit_label` ist 'Einsteiger' — dafuer brauchen wir 'leicht' als raw-Key im Button-URL. Der raw-Key liegt in `phasen_details.schwierigkeit`. Wave-1-Code extrahiert `schwierigkeit_label`, aber NICHT den raw-Key. Wir **muessen** in diesem Wave EITHER:
     - (a) Route minimal erweitern: `schwierigkeit_raw = pd.get('schwierigkeit')` als Context-Variable zurueckgeben, ODER
     - (b) Im Template per lookup/if den raw-Key bestimmen (invertiertes Mapping von Label auf raw).

   **Entscheidung:** Option (a). Saubere Loesung, Helper in Wave 1 war bereits fuer schwierigkeit da. Diese Wave bittet um minimale Task-1.2-Erweiterung (eine Zeile in Wave 1 — siehe Task 3.0 unten als optionale Fix-Task falls Wave 1 es nicht geliefert hat).

## Deviation-Prevention Tasks

- **P1 (Rule-1 Feldnamen):** Kein DB-Field-Access in neuen JS-Pfaden. Button-URL nutzt NUR Template-Context.

- **P2 (Umlaut-Identifier):**
  - Neue CSS-Klasse: `.n-session-detail-repeat-btn` (ASCII).
  - JS-Code ASCII: `window.location.href = '/session/' + data.log_id` (keine Umlaute in JS-Variablen).
  - Query-Param-Namen ASCII: `personality_type_id`, `schwierigkeit`, `modus` (deckungsgleich mit /training/start-Handler).

- **P3 (kb_end-Sync):** Nicht relevant.

- **P4 (JSON max_tokens):** Nicht relevant.

- **P5 (HTTPException-Passthrough):** Nicht relevant.

- **P6 (Dedupe-Logik):** Nicht relevant (kein Listen-Rendering in Wave 3).

- **P7 (Typ-Diskriminierung):** Button rendert NUR fuer `{% if conv.typ == 'training' %}` — NICHT `session_mode`.

- **P8 (Template-Parser-Check):** Am Ende der Wave:
  ```bash
  python -c "from app import app; app.jinja_env.get_template('training.html'); app.jinja_env.get_template('session_detail.html'); print('OK')"
  ```
  MUSS "OK" liefern.

- **P9 (CSS-Klassen-Namensraum):** Neue Klasse `.n-session-detail-repeat-btn` in `.n-session-detail-*` Familie. KEINE `training.html`-spezifischen Klassen.

- **P10 (STATE.md/ROADMAP.md/SUMMARY.md NICHT im Executor):** Nur `07.2-03-SUMMARY.md`.

- **Pitfall-3 (Overlay-JS-State-Cleanup, siehe RESEARCH Pitfall 3):**
  Nach Task-3.2 1x kompletten Flow durchspielen ohne Browser (Mental-Walkthrough):
  - `resetTraining()` noch korrekt?
  - Alle `getElementById('t-scoring*')` entfernt?
  - Kein `getChart()`-Aufruf gegen nicht-existentes Canvas?

  Commands als Verify:
  ```bash
  grep -n "t-scoring\|t-mood-chart\|t-wendepunkte-detail\|t-save-personality-div\|t-scoringInner" templates/training.html
  ```
  MUSS kein Match liefern (ALLE Refs entfernt).
</deviation_prevention>

<tasks>

<task type="auto" tdd="false">
  <name>Task 3.0 (Fixup): schwierigkeit_raw als Template-Context fuer Pre-Selection in Wave 1 nachziehen (falls noetig)</name>
  <files>routes/dashboard.py</files>
  <action>
  **OPTIONAL** — Nur ausfuehren, wenn Wave-1-Output (routes/dashboard.py session_detail) KEINE `schwierigkeit_raw`-Variable liefert.

  Check:
  ```bash
  grep -n "schwierigkeit_raw\|schwierigkeit_label" routes/dashboard.py | head -5
  ```

  Wenn NUR `schwierigkeit_label` existiert, aber KEIN `schwierigkeit_raw`, dann erweitern:

  **Aenderung in routes/dashboard.py `session_detail()`:**

  Am Ort wo `schwierigkeit_label` aktuell (Phase 07.1 Wave 4 + Wave 1) gesetzt wird:

  ```python
  # BESTEHEND (unveraendert):
  schwierigkeit_label = None
  schwierigkeit_raw = None  # NEU
  if conv_typ == 'training' and conv.phasen_details:
      try:
          pd = _json.loads(conv.phasen_details)
          raw = (pd.get('schwierigkeit') if isinstance(pd, dict) else None) or ''
          raw_lower = str(raw).lower()
          mapping = {
              'leicht':   'Einsteiger',
              'mittel':   'Fortgeschritten',
              'schwer':   'Experte',
          }
          schwierigkeit_label = mapping.get(raw_lower) or None
          # NEU 07.2: raw-Key fuer Nochmal-trainieren-URL
          if raw_lower in mapping:
              schwierigkeit_raw = raw_lower
      except Exception:
          schwierigkeit_label = None
          schwierigkeit_raw = None
  ```

  Und in `render_template()`:
  ```python
  # Neue Kwarg hinzufuegen:
  schwierigkeit_raw=schwierigkeit_raw,
  ```
  </action>
  <verify>
    <automated>grep -c "schwierigkeit_raw" routes/dashboard.py</automated>
  </verify>
  <done>
  - `schwierigkeit_raw` existiert als Template-Context-Variable in routes/dashboard.py.
  - Wert ist einer von 'leicht' / 'mittel' / 'schwer' / None.
  - Render-Template uebergibt den Wert.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.1: training.html — Scoring-Phase komplett entfernen, Redirect einbauen</name>
  <files>templates/training.html</files>
  <action>
  **Fuenf zusammengehoerige Aenderungen in templates/training.html:**

  ### Schritt A: `confirmEndTraining()` — Redirect auf /session/<log_id>

  Lokalisieren: Funktion `confirmEndTraining()` per `grep -n "function confirmEndTraining\|async function confirmEndTraining" templates/training.html`.

  Der aktuelle Flow ruft nach `const data = await res.json()` die Funktion `buildScoring(data)` + `showPhase('scoring')`. Ersetzen durch Redirect:

  ```javascript
  // ALT (entfernen):
  // buildScoring(data);
  // showPhase('scoring');
  // if (loadOverlay) loadOverlay.style.display = 'none';
  // setWaiting(false);

  // NEU 07.2 Wave 3:
  if (data.log_id) {
    window.location.href = '/session/' + data.log_id;
  } else {
    // Fallback (shouldn't happen — Wave 1 Task 1.2 garantiert log_id)
    console.error('[End] missing log_id in /training/end response — fallback to /logs');
    window.location.href = '/logs';
  }
  ```

  Den loadOverlay-Hide und setWaiting(false) **NICHT** entfernen, wenn der Request fehlschlaegt — die bleiben im Error-Branch und im `if (!data.ok)`-Branch. Nur im Success-Pfad wird redirected und damit bleibt der Loader sichtbar bis der Browser navigiert (gewuenscht: User sieht Loader bis Session-Detail laedt).

  ### Schritt B: `showPhase()` — 't-scoring' aus Array streichen

  Lokalisieren per `grep -n "function showPhase\|'t-scoring'" templates/training.html`.

  Im Array (z.B. `const phases = ['t-setup', 't-einwahl', 't-chat', 't-scoring'];`) das letzte Element `'t-scoring'` streichen:

  ```javascript
  // ALT:
  // const phases = ['t-setup', 't-einwahl', 't-chat', 't-scoring'];
  // NEU:
  const phases = ['t-setup', 't-einwahl', 't-chat'];
  ```

  ### Schritt C: Funktionen `buildScoring()`, `renderMoodChart()`, `renderWendepunkteDetail()` komplett entfernen

  Lokalisieren per:
  ```bash
  grep -n "^    function buildScoring\|^    function renderMoodChart\|^    function renderWendepunkteDetail\|^  function buildScoring\|^  function renderMoodChart\|^  function renderWendepunkteDetail" templates/training.html
  ```

  Bereiche laut Research (RESEARCH.md Code-Beispiel 5, Zeile 600+ der Instruktion):
  - `buildScoring(data)` — Zeile ~1531-1663
  - `renderMoodChart(stimmungHistory)` — Zeile ~1666-1743
  - `renderWendepunkteDetail(wendepunkte)` — Zeile ~1746-1778

  Alle drei Funktionsbloecke komplett entfernen (Funktions-Signatur bis schliessende `}`).

  ### Schritt D: HTML-Block `#t-scoring` komplett entfernen

  Lokalisieren per `grep -n "id=\"t-scoring\"\|id='t-scoring'" templates/training.html` — erwartet Zeile ~330-630 (Range aus Research Pitfall 3).

  Den kompletten Container `<div id="t-scoring" ...>...</div>` samt aller Kinder (Canvas, innerHTML-Targets, Personality-Reveal-Div, Save-Personality-Div, etc.) entfernen.

  ### Schritt E: Verbleibende Referenzen scannen und bereinigen

  Nach Schritt A-D:
  ```bash
  grep -n "t-scoring\|t-mood-chart\|t-wendepunkte-detail\|t-save-personality-div\|t-scoringInner\|buildScoring\|renderMoodChart\|renderWendepunkteDetail" templates/training.html
  ```

  MUSS kein Match liefern. Falls noch Reste (z.B. in resetTraining(), Initialisierung, Event-Handler), diese bereinigen.

  `resetTraining()` **behalten** — Funktion wird noch fuer Setup-Phase genutzt (Research Pitfall 3). Nur Scoring-Reset-Code innerhalb resetTraining entfernen, falls vorhanden.

  ### Schritt F: Template-Parser-Check

  ```bash
  python -c "from app import app; app.jinja_env.get_template('training.html'); print('OK')"
  ```
  MUSS "OK" liefern.

  ### Schritt G: Verbleibendes Chart.js-Init fuer Training-Scoring pruefen

  Falls am Datei-Anfang (bei DOMContentLoaded) Chart.js-Initialisierung fuer mood-chart existiert — auch entfernen:
  ```bash
  grep -n "Chart.getChart\|new Chart.*mood\|t-mood-chart" templates/training.html
  ```
  Alle Matches bereinigen.
  </action>
  <verify>
    <automated>python -c "from app import app; app.jinja_env.get_template('training.html'); print('OK')" && test "$(grep -c 't-scoring\|t-mood-chart\|t-wendepunkte-detail\|t-save-personality-div\|t-scoringInner\|buildScoring\|renderMoodChart\|renderWendepunkteDetail' templates/training.html)" = "0" && echo "ALL SCORING REFS REMOVED"</automated>
  </verify>
  <done>
  - `buildScoring()`, `renderMoodChart()`, `renderWendepunkteDetail()` Funktionen komplett entfernt.
  - `#t-scoring` HTML-Block entfernt.
  - `showPhase()` phases-Array ohne `'t-scoring'`.
  - `confirmEndTraining()` macht Redirect auf `/session/<log_id>` mit Fallback auf `/logs`.
  - ALLE `t-scoring*`-, `t-mood-chart`-, `buildScoring`-Refs eliminiert (grep-count 0).
  - Template-Parser "OK".
  - ~280 LOC entfernt (wie RESEARCH geplant).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3.2: session_detail.html — "Nochmal trainieren"-Action-Button im Training-Header + CSS</name>
  <files>templates/session_detail.html, static/nerve.css</files>
  <action>
  **Zwei Aenderungen:**

  ### Schritt A: "Nochmal trainieren"-Button in session_detail.html Header einfuegen

  Lokalisieren: Header-Start per `grep -n "<header class=\"n-session-detail-header\|← Zurück zur History" templates/session_detail.html`.

  Position: NACH dem `<h1>Session #{{ conv.id }}</h1>` aber INNERHALB des `<header>`-Tags. Nur sichtbar bei Training-Sessions.

  WICHTIG: Wenn Wave 2 (parallel oder vorher) bereits an dieser Stelle Aenderungen gemacht hat, Merge die Inserts vorsichtig. Dieser Wave-3-Block kommt **NACH dem `<h1>`**, VOR dem `<div class="n-session-detail-meta">`.

  ```jinja
  {# NEU 07.2 Wave 3: Action-Button "Nochmal trainieren" — nur fuer Training-Sessions #}
  {% if conv.typ == 'training' %}
    <a class="n-btn n-btn-primary n-session-detail-repeat-btn"
       href="{{ url_for('training.training_page') }}{% set qparams = [] %}{% if conv.personality_type_id %}{% set _ = qparams.append('personality_type_id=' ~ conv.personality_type_id) %}{% endif %}{% if schwierigkeit_raw %}{% set _ = qparams.append('schwierigkeit=' ~ schwierigkeit_raw) %}{% endif %}{% if qparams %}?{{ qparams | join('&') }}{% endif %}">
      <i data-lucide="play-circle" class="n-session-detail-icon-sm"></i>
      Nochmal trainieren
    </a>
  {% endif %}
  ```

  **Fallback falls `url_for('training.training_page')` kein gueltiger Endpoint:** Per `grep -n "@training_bp.route\|def training_page\|'/training'" routes/training.py` den echten Endpoint-Namen fuer die `/training`-Seite verifizieren. Notfalls `href="/training"` hart-verdrahtet.

  **Warum kein `modus` oder `scenario_id` in URL:**
  - `scenario_id` ist NICHT in ConversationLog persistiert (Research Pitfall 4 + Assumption A2). Wegfallen lassen.
  - `modus` (frei vs guided) ist nicht aus persistierten Feldern ableitbar — im Zweifel weglassen. User waehlt beim Re-Start.

  **Kunden-Subtext** (aus Wave 2 Task 2.1 Schritt E) kommt separat zwischen Badges + Meta-Row — NICHT im Button-Block.

  ### Schritt B: CSS-Klasse `.n-session-detail-repeat-btn` in nerve.css einfuegen

  Position: Am Ende der `.n-session-detail-*` Familie in `static/nerve.css` (kann mit oder nach Wave-2-Klassen kommen — wenn Wave 2 schon lief, einfach dran anfuegen).

  ```css
  /* ── Phase 07.2 Wave 3: "Nochmal trainieren"-Action-Button ───────── */

  .n-session-detail-repeat-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-left: auto;
    /* Rest kommt aus .n-btn / .n-btn-primary — keine neuen Farben */
  }

  .n-session-detail-repeat-btn:hover {
    /* Hover bleibt .n-btn-primary Standard-Hover */
  }

  /* Responsive: Button unter Titel wrappen auf Mobile */
  @media (max-width: 640px) {
    .n-session-detail-repeat-btn {
      margin-left: 0;
      margin-top: 8px;
      align-self: flex-start;
    }
  }
  ```

  ### Schritt C: Template-Parser-Check

  ```bash
  python -c "from app import app; app.jinja_env.get_template('session_detail.html'); print('OK')"
  ```
  MUSS "OK" liefern.

  ### Schritt D: Button-URL manuell verifizieren (Mental-Walkthrough)

  Drei Test-Faelle:
  1. Training mit `personality_type_id=5, schwierigkeit_raw='mittel'` → URL `/training?personality_type_id=5&schwierigkeit=mittel`. OK.
  2. Training ohne personality_type_id (Standard-Pfad), mit `schwierigkeit_raw='leicht'` → URL `/training?schwierigkeit=leicht`. OK.
  3. Training ohne personality_type_id UND ohne schwierigkeit_raw → URL `/training`. OK (kein `?`).

  Pitfall 4 (nochmal): falsche URL bei nullable-Daten wuerde `/training?personality_type_id=None` erzeugen — das ist hier NICHT moeglich weil `{% if conv.personality_type_id %}` null-filtert.
  </action>
  <verify>
    <automated>python -c "from app import app; app.jinja_env.get_template('session_detail.html'); print('OK')" && grep -c "n-session-detail-repeat-btn" static/nerve.css</automated>
  </verify>
  <done>
  - `<a>`-Action-Button "Nochmal trainieren" im Training-Header eingefuegt, rendert NUR bei `conv.typ == 'training'`.
  - URL enthaelt `personality_type_id` und `schwierigkeit` als Query-Params conditional.
  - `.n-session-detail-repeat-btn` CSS-Klasse in nerve.css vorhanden.
  - Lucide `play-circle`-Icon im Button sichtbar.
  - Responsive-Hint fuer Mobile (<640px) enthalten.
  - Template-Parser "OK".
  - Mental-Walkthrough der 3 URL-Faelle alle korrekt.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/training/end` → `/session/<log_id>` | log_id aus Server-Response steuert Client-Navigation |
| Browser → "Nochmal trainieren"-URL | Query-Params gehen direkt in /training/start-Handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-08 | E (Elevation of Privilege) | User manipuliert log_id in Redirect | accept | Server hat seitens /session/<id>-Route bereits `conv.user_id == g.user.id`-Filter. Manipulation fuehrt zu 404/403, kein Leak. |
| T-07.2-09 | T (Tampering) | User manipuliert personality_type_id in "Nochmal trainieren"-URL | mitigate | `/training/start` filtert bereits `PersonalityType.user_id == g.user.id OR is_custom == False` (routes/training.py:180-184, siehe Research Security Domain). Keine neue Vuln. |
| T-07.2-10 | T (Tampering) | User manipuliert schwierigkeit=xxx zu invalidem Wert | mitigate | `/training/start`-Handler parst schwierigkeit defensiv (bekannt aus 07.1 — int-parse + fallback). Keine neue Vuln. |

</threat_model>

<verification>

## Wave-3-Gate

1. **Alle Scoring-Overlay-Refs entfernt:**
   ```bash
   grep -c "t-scoring\|t-mood-chart\|t-wendepunkte-detail\|t-save-personality-div\|t-scoringInner\|buildScoring\|renderMoodChart\|renderWendepunkteDetail" templates/training.html
   ```
   → `0`.

2. **Redirect-Code vorhanden:**
   ```bash
   grep -c "window.location.href = '/session/'" templates/training.html
   ```
   → `>=1`.

3. **"Nochmal trainieren"-Button vorhanden:**
   ```bash
   grep -c "Nochmal trainieren\|n-session-detail-repeat-btn" templates/session_detail.html
   ```
   → `>=1`.

4. **CSS-Klasse vorhanden:**
   ```bash
   grep -c "n-session-detail-repeat-btn" static/nerve.css
   ```
   → `>=1`.

5. **schwierigkeit_raw-Context verfuegbar (falls Task 3.0 ausgefuehrt):**
   ```bash
   grep -c "schwierigkeit_raw" routes/dashboard.py
   ```
   → `>=2` (Variable + Render-Template).

6. **Template-Parser clean:**
   ```bash
   python -c "from app import app; app.jinja_env.get_template('training.html'); app.jinja_env.get_template('session_detail.html'); print('OK')"
   ```
   → "OK".

</verification>

<success_criteria>

- templates/training.html hat Scoring-Phase komplett entfernt (~280 LOC weg).
- `confirmEndTraining()` macht Redirect auf `/session/<log_id>` (Fallback `/logs` bei fehlendem log_id).
- `showPhase()` phases-Array enthaelt KEIN 't-scoring'.
- `buildScoring()`, `renderMoodChart()`, `renderWendepunkteDetail()` Funktionen entfernt.
- `#t-scoring` HTML-Container entfernt, keine Rest-Referenzen auf Scoring-DOM-IDs.
- templates/session_detail.html hat "Nochmal trainieren"-Button im Training-Header (NUR fuer `conv.typ == 'training'`).
- Button-URL: `/training` + optional `?personality_type_id=X&schwierigkeit=Y` (conditional, nur wenn Daten vorhanden).
- `.n-session-detail-repeat-btn` CSS-Klasse in nerve.css, responsive auf Mobile.
- Keine neuen CSS-Farben, keine Umlaut-Identifier, keine neuen DB-Aenderungen.
- Template-Parser clean fuer beide Files.

</success_criteria>

<output>
Nach Abschluss erstellt der Executor AUSSCHLIESSLICH:
- `.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-03-SUMMARY.md`

Executor MUSS NICHT:
- STATE.md, ROADMAP.md updaten (Orchestrator)
- CSS_VERSION bumpen (Wave 4)
- deploy.sh ausfuehren (Wave 4)
- Browser-Test durchfuehren (Wave 4)
- Andere SUMMARYs modifizieren
</output>
</content>
</invoke>