---
phase: 07.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/dashboard.py
  - routes/training.py
autonomous: true
requirements:
  - POLISH-32
  - POLISH-34
must_haves:
  truths:
    - "routes/dashboard.py session_detail() liefert scoring_kategorien, scoring_wendepunkte_detail, scoring_verbesserungen, kunden_name, kunden_alter als Template-Context"
    - "routes/dashboard.py session_detail() liefert schwierigkeit_raw ('leicht'|'mittel'|'schwer'|None) als Template-Context fuer Plan-03-Nochmal-trainieren-URL"
    - "routes/training.py /training/end-Response enthaelt log_id im JSON-Body (pflicht fuer Wave-3-Redirect)"
    - "_parse_kunden_meta() parst 'Vorname Nachname, Alter'-Pattern und liefert (None, None) fuer Archetyp-Namen"
    - "Keine DB-Migration, keine neuen Spalten, keine neuen Packages"
  artifacts:
    - path: "routes/dashboard.py"
      provides: "session_detail() erweitert um 3 Context-Keys aus phasen_details + kunden_meta + schwierigkeit_raw"
      contains: "scoring_kategorien"
    - path: "routes/training.py"
      provides: "training_end() Response-Field log_id"
      contains: "'log_id': log.id"
  key_links:
    - from: "routes/dashboard.py session_detail()"
      to: "conv.phasen_details JSON"
      via: "_json.loads() + isinstance-Guards"
      pattern: "scoring_kategorien.*pd\\.get\\('kategorien'\\)"
    - from: "routes/training.py training_end()"
      to: "JSON-Response"
      via: "jsonify(ok=True, log_id=log.id, ...)"
      pattern: "log_id.*log\\.id"
    - from: "routes/dashboard.py _parse_kunden_meta()"
      to: "PersonalityType.name"
      via: "re.match(r'^(.+?),\\s*(\\d+)$', ...)"
      pattern: "_parse_kunden_meta"
---

<objective>
Wave 1 der Scoring-Konsolidierung: Backend-Daten-Extraktion.

**Was diese Wave liefert:**
- `routes/dashboard.py session_detail()` extrahiert drei neue Listen aus `conv.phasen_details`-JSON (Kategorien, Wendepunkte, Verbesserungen) und zwei Kunden-Metadaten (Name, Alter) aus `PersonalityType.name`.
- `routes/dashboard.py session_detail()` liefert zusaetzlich `schwierigkeit_raw` (raw-Key 'leicht'|'mittel'|'schwer'|None) als Template-Context-Variable fuer die "Nochmal trainieren"-URL in Plan 03.
- `routes/training.py /training/end` liefert `log_id` in JSON-Response (Pflicht-Dependency fuer Wave-3-Redirect).
- Helper `_parse_kunden_meta()` parst "Vorname Nachname, Alter"-Pattern, liefert `(None, None)` fuer Archetyp-Namen.

**Warum das matters:** Ohne diese Wave kann weder das Template (Wave 2) Sektionen 12/13/14 rendern noch der Redirect-Flow (Wave 3) funktionieren. Diese Wave ist blocking-dependency fuer Wave 2 und Wave 3.

**Output:** Erweiterte Route-Handler, keine DB-Aenderungen, keine neuen Packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-CONTEXT.md
@.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-RESEARCH.md
@.planning/phases/07.1-polish-24-session-detail-redesign-session-id-main-design-umb/DEVIATIONS.md

# Source files that need modification
@routes/dashboard.py
@routes/training.py

# Source files to read-only reference (DO NOT modify in this wave)
@services/training_service.py
@database/models.py
@templates/session_detail.html
@templates/training.html

<interfaces>
<!-- Extracted from RESEARCH.md and codebase — Executor MUST use these exactly -->

### ConversationLog (database/models.py:231-283) — read-only in this wave
```python
class ConversationLog(Base):
    id: int
    user_id: int (FK)
    typ: str                    # 'live' | 'training'
    session_mode: str | None    # 'cold_call' | 'meeting' | None for training
    personality_type_id: int | None  # FK — nullable
    phasen_details: str | None  # JSON-Text-Spalte — HAUPTQUELLE fuer Wave 1
    kb_end: int | None
    kb_verlauf: str | None      # JSON
    stimmung_history: str | None # JSON
    dauer_sekunden: int | None
    einwaende_gesamt: int | None
    einwaende_behandelt: int | None
    # ... weitere bestehende Felder, siehe models.py
    # WICHTIG: KEINE scenario_id-Spalte existiert!
```

### PersonalityType (database/models.py)
```python
class PersonalityType(Base):
    id: int
    user_id: int | None      # None fuer System-Types
    name: str                # Archetyp ("Beschaeftigter Chef") ODER "Vorname Nachname, Alter"
    icon: str | None
    is_custom: bool          # True fuer AI-generierte, False fuer System-6
    # ... weitere Felder
```

### phasen_details JSON-Schema (persistiert von routes/training.py:696-711)
```json
{
  "gesamt_score": 74,
  "kategorien": [                           // Array mit 5-7 Eintraegen
    {"name": "Gespraechseroeffnung", "score": 8, "feedback": "..."},
    {"name": "Bedarfsanalyse", "score": 7, "feedback": "..."},
    {"name": "Einwandbehandlung", "score": 6, "feedback": "..."},
    {"name": "Gespraechsfuehrung", "score": 7, "feedback": "..."},
    {"name": "Abschluss", "score": 8, "feedback": "..."},
    {"name": "Beziehungsaufbau", "score": 7, "feedback": "..."}  // optional
  ],
  "staerken": ["...", "..."],
  "verbesserungen": ["Handlungstipp 1", "Handlungstipp 2", "Handlungstipp 3"],
  "zusammenfassung": "...",
  "wendepunkt_saetze": ["...", "..."],
  "wendepunkte_detail": [                   // optional, nur bei stimmung_history
    {"turn": 3, "berater_sagt": "...", "was_schief": "...", "alternativ": "..."},
    {"turn": 7, "berater_sagt": "...", "was_schief": "...", "alternativ": "..."}
  ],
  "schwierigkeit": "mittel"                 // Wave-4/POLISH-32 extension — raw key
}
```

### bestehendes Pattern: phasen_details-Dict-Lookup mit Fallback (routes/dashboard.py:784-802)
```python
schwierigkeit_label = None
if conv_typ == 'training' and conv.phasen_details:
    try:
        pd = _json.loads(conv.phasen_details)
        raw = (pd.get('schwierigkeit') if isinstance(pd, dict) else None) or ''
        mapping = {'leicht': 'Einsteiger', 'mittel': 'Fortgeschritten', 'schwer': 'Experte'}
        schwierigkeit_label = mapping.get(str(raw).lower()) or None
    except Exception:
        schwierigkeit_label = None
```

### training_end Response (routes/training.py — aktueller Zustand)
```python
# VOR Phase 07.2 — KEIN log_id:
return jsonify(ok=True, **scoring)

# Diese Wave 1 MUSS das erweitern auf:
return jsonify(ok=True, log_id=log.id, **scoring)
# (log ist das ConversationLog-Object das kurz vorher in routes/training.py:696-711 committed wurde)
```
</interfaces>
</context>

<deviation_prevention>
## Precondition Checks (MUST vor Task-Start verifizieren)

Diese Checks sind aus Phase 07.1 DEVIATIONS.md (UAT-R1 bis R4) abgeleitet. Sie sind BLOCKING — wenn einer fehlschlaegt, STOP und fragen.

1. **Rule-1 Feldnamen-Verifikation:**
   ```bash
   grep -n "phasen_details\|personality_type_id" database/models.py | head -20
   ```
   Bestaetige: Felder heissen genau so (ASCII, lowercase_underscore). KEINE Umlaute in Python-Identifiern.

2. **bestehendes Pattern verifizieren (Fix B + UAT-R2 G: kb_end_effective-Pattern):**
   ```bash
   grep -n "kb_end_effective\|phasen_details" routes/dashboard.py | head -20
   ```
   Bestaetige: `session_detail()` nutzt bereits `_json.loads(conv.phasen_details)` an einer Stelle (Zeile ~784). Neue Extraktion MUSS denselben Try/Except-Isinstance-Guard-Pattern nutzen — NICHT unhashable-Key-Lookups auf Nicht-Dict-Values.

3. **log_id Return-Pattern Check:**
   ```bash
   grep -n "jsonify(ok=True" routes/training.py | head -5
   ```
   Bestaetige: `training_end()` nutzt jsonify-Pattern mit `**scoring` (Dict-Unpacking). `log_id` darf den bestehenden Key nicht ueberschreiben — scoring-Dict enthaelt kein `log_id`.

4. **HTTPException-Passthrough nicht regressieren (POLISH-21):**
   - Keine neuen `@app.errorhandler(Exception)`-Changes in diesem Wave.
   - `/training/end` nutzt bestehenden errorhandler — KEINE try/except-Wrapper hinzufuegen.

## Deviation-Prevention Tasks

Aus Pitfalls 1-10 der Plan-Instruktion:

- **P1 (Rule-1 OE.erfolgreich→OE.success):** nicht direkt relevant in Wave 1 (kein OE-Zugriff). **Status: N/A**
- **P2 (Umlaut-Identifier):** Alle neuen Python-Variablen/Dict-Keys ASCII: `scoring_kategorien`, `scoring_wendepunkte_detail`, `scoring_verbesserungen`, `kunden_name`, `kunden_alter`, `schwierigkeit_raw`, `_parse_kunden_meta`. KEINE Umlaute in Identifiern. **Status: Enforced im Code.**
- **P3 (kb_end-Sync):** Nicht relevant in dieser Wave (wir touchieren kb_end nicht).
- **P4 (JSON max_tokens):** Nicht relevant (wir erzeugen KEINE neuen Claude-Prompts in dieser Wave).
- **P5 (HTTPException-Passthrough):** s.o. Precondition Check 4.
- **P6 (Dedupe-Logik):** Nicht relevant (wir rendern hier keine Listen, nur extrahieren).
- **P7 (Typ-Diskriminierung):** Alle Guards nutzen `conv.typ == 'training'` (NICHT `session_mode == 'training'` — das ist falsch, session_mode ist nur fuer Live).
- **P8 (Template-Parser-Check):** Wave 1 touchiert KEIN Template. **Status: N/A in Wave 1.**
- **P9 (CSS-Klassen-Namensraum):** Wave 1 touchiert KEIN CSS. **Status: N/A in Wave 1.**
- **P10 (STATE.md/ROADMAP.md/SUMMARY.md NICHT im Executor schreiben):** Executor erstellt NUR `07.2-01-SUMMARY.md` am Ende (Orchestrator-Pattern wie 07.1). **Status: Enforced im Output-Block unten.**
</deviation_prevention>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1.1: _parse_kunden_meta Helper + session_detail() Context-Erweiterung (inkl. schwierigkeit_raw)</name>
  <files>routes/dashboard.py</files>
  <action>
  Implementiere drei Aenderungen in `routes/dashboard.py`:

  **Aenderung A: Helper `_parse_kunden_meta()` einfuegen**

  Platzierung: Direkt vor `_dedupe_painpoints()` (der andere Helper in dieser Datei, ~Zeile 650-680 Bereich — EXAKTE Position per `grep -n "_dedupe_painpoints" routes/dashboard.py` bestimmen).

  ```python
  import re as _re  # oben im File, falls nicht schon importiert — per grep pruefen

  def _parse_kunden_meta(pt_name):
      """Extract kunden_name + kunden_alter from PersonalityType.name.

      Pattern: 'Markus Wendland, 48' -> ('Markus Wendland', '48')
      Archetype: 'Beschaeftigter Chef' -> (None, None)

      Returns: (name:str|None, alter:str|None)
      """
      if not pt_name:
          return (None, None)
      m = _re.match(r'^(.+?),\s*(\d+)$', pt_name.strip())
      if not m:
          return (None, None)
      return (m.group(1).strip(), m.group(2).strip())
  ```

  HINWEIS: Ob `import re` bereits existiert per `grep -n "^import re\|^from re" routes/dashboard.py`. Wenn ja: direkt `re.match` verwenden statt `_re.match`. Wenn nein: `import re as _re` am Datei-Anfang analog zu `import json as _json` hinzufuegen.

  **Aenderung B: `session_detail()` um drei Context-Dict-Lookups + Kunden-Meta + schwierigkeit_raw erweitern**

  Lokalisieren: Die bestehende `phasen_details`-Extraktion (Zeile ~784-802 — Pattern `schwierigkeit_label`).

  **B.1 — schwierigkeit_raw direkt an der bestehenden schwierigkeit_label-Extraktion anfuegen:**

  Der bestehende Block sieht aktuell so aus (Pattern der Phase 07.1 Wave 4):
  ```python
  schwierigkeit_label = None
  if conv_typ == 'training' and conv.phasen_details:
      try:
          pd = _json.loads(conv.phasen_details)
          raw = (pd.get('schwierigkeit') if isinstance(pd, dict) else None) or ''
          mapping = {'leicht': 'Einsteiger', 'mittel': 'Fortgeschritten', 'schwer': 'Experte'}
          schwierigkeit_label = mapping.get(str(raw).lower()) or None
      except Exception:
          schwierigkeit_label = None
  ```

  Nach diesem Block (ohne den bestehenden Code zu veraendern) EINFUEGEN:
  ```python
  # NEU 07.2 Wave 1: schwierigkeit_raw als raw-Key fuer "Nochmal trainieren"-URL (Plan 03)
  # schwierigkeit_label ist das User-facing Mapping ("Einsteiger"), schwierigkeit_raw der raw-Key ("leicht")
  schwierigkeit_raw = None
  if schwierigkeit_label is not None and conv.phasen_details:
      try:
          pd2 = _json.loads(conv.phasen_details)
          raw2 = (pd2.get('schwierigkeit') if isinstance(pd2, dict) else None) or ''
          raw2_lower = str(raw2).lower()
          if raw2_lower in ('leicht', 'mittel', 'schwer'):
              schwierigkeit_raw = raw2_lower
      except Exception:
          schwierigkeit_raw = None
  ```

  Rationale fuer doppeltes `_json.loads(conv.phasen_details)`: Wir touchieren den bestehenden schwierigkeit_label-Block NICHT (Minimal-Diff-Prinzip, kein Regressions-Risiko fuer POLISH-32 aus 07.1). Die zweite Extraktion ist guenstig (Mini-JSON, ~200 Bytes) und defensiv (eigener try/except, eigener isinstance-Guard).

  **B.2 — Scoring-Listen + Kunden-Meta direkt NACH schwierigkeit_raw anfuegen:**

  ```python
  # NEU 07.2: Scoring-Listen aus phasen_details extrahieren (Training only)
  scoring_kategorien = []
  scoring_wendepunkte_detail = []
  scoring_verbesserungen = []
  if conv_typ == 'training' and conv.phasen_details:
      try:
          pd = _json.loads(conv.phasen_details)
          if isinstance(pd, dict):
              _cat = pd.get('kategorien')
              if isinstance(_cat, list):
                  scoring_kategorien = _cat
              _wp = pd.get('wendepunkte_detail')
              if isinstance(_wp, list):
                  scoring_wendepunkte_detail = _wp
              _vb = pd.get('verbesserungen')
              if isinstance(_vb, list):
                  scoring_verbesserungen = _vb
      except Exception:
          # Silent fallback auf leere Listen (Template rendert Empty-State)
          pass

  # NEU 07.2: Kunden-Metadaten aus PersonalityType.name parsen (Training only)
  kunden_name = None
  kunden_alter = None
  if conv_typ == 'training' and pt:
      kunden_name, kunden_alter = _parse_kunden_meta(pt.name)
  ```

  **Aenderung C: `render_template()`-Aufruf um neue Context-Keys erweitern**

  Lokalisieren: Der `return render_template('session_detail.html', ...)`-Aufruf am Ende von `session_detail()` (Zeile ~820-835). Neue Kwargs anfuegen:

  ```python
  return render_template(
      'session_detail.html',
      # ... alle bestehenden Args UNVERAENDERT ...
      # NEU 07.2:
      scoring_kategorien=scoring_kategorien,
      scoring_wendepunkte_detail=scoring_wendepunkte_detail,
      scoring_verbesserungen=scoring_verbesserungen,
      kunden_name=kunden_name,
      kunden_alter=kunden_alter,
      schwierigkeit_raw=schwierigkeit_raw,
  )
  ```

  BEACHTE:
  - Nutzung von `conv.typ == 'training'` (nicht `session_mode`) fuer Typ-Diskriminierung (Pitfall 7).
  - `_json` ist bereits als `import json as _json` in der Datei importiert (per grep verifizieren).
  - `pt` ist bereits im Route-Handler definiert (PersonalityType-Lookup, bestehender Code).
  - Silent-Fallback (except pass) — Phase 07.1 Lesson: kaputte JSON-Payloads nicht 500-en.
  - `schwierigkeit_raw` wird NUR gesetzt wenn `schwierigkeit_label` bereits erfolgreich geparsed wurde (d.h. raw-Key in mapping existierte). Damit ist `schwierigkeit_raw` garantiert konsistent zu `schwierigkeit_label`.
  </action>
  <verify>
    <automated>python -c "from routes.dashboard import _parse_kunden_meta; assert _parse_kunden_meta('Markus Wendland, 48') == ('Markus Wendland', '48'); assert _parse_kunden_meta('Beschaeftigter Chef') == (None, None); assert _parse_kunden_meta('') == (None, None); assert _parse_kunden_meta(None) == (None, None); print('OK')"</automated>
  </verify>
  <done>
  - `_parse_kunden_meta()` in `routes/dashboard.py` definiert, 4 Unit-Cases pass (Person+Alter, Archetyp, Empty-String, None).
  - `session_detail()` extrahiert scoring_kategorien/_wendepunkte_detail/_verbesserungen mit Try/Except + isinstance-Guards.
  - `session_detail()` parst kunden_name/kunden_alter NUR fuer `conv.typ == 'training'`.
  - `session_detail()` extrahiert `schwierigkeit_raw` ('leicht'|'mittel'|'schwer'|None), konsistent zu `schwierigkeit_label`.
  - `render_template()` uebergibt alle 6 neuen Kwargs (scoring_kategorien, _wendepunkte_detail, _verbesserungen, kunden_name, kunden_alter, schwierigkeit_raw).
  - Flask-App startet ohne ImportError (`python -c "from routes import dashboard"` clean).
  - Bestehende Context-Variablen (kb_end_effective, schwierigkeit_label, painpoints, etc.) sind UNVERAENDERT.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.2: training_end() Response um log_id erweitern</name>
  <files>routes/training.py</files>
  <action>
  **Aenderung: `/training/end`-Handler gibt `log_id` im JSON-Response zurueck.**

  Lokalisieren: Die `training_end()`-Funktion in `routes/training.py` (Zeilen ~609-800). Der aktuelle Response-Return ist ein `jsonify(ok=True, **scoring)` Pattern nach dem `db.add(log)` + `db.commit()`.

  Schritt 1 — Per `grep -n "def training_end\|db.commit()\|jsonify" routes/training.py` die exakten Zeilennummern bestimmen. Auch pruefen wo das ConversationLog-Object committed und dessen `log.id` verfuegbar wird.

  Schritt 2 — Nach `db.commit()` die `log.id` extrahieren (SQLAlchemy flusht bei commit, danach ist `log.id` gesetzt):

  ```python
  # Nach bestehendem: phasen_details = json.dumps(_phasen_payload, ensure_ascii=False)
  # log = ConversationLog(..., phasen_details=phasen_details, ...)
  # db.add(log); db.commit()

  # NEU 07.2: log_id fuer Wave-3-Redirect auf /session/<id>
  _log_id = log.id  # Muss vor db.close() gelesen werden (Session-Detach-Pattern aus CLAUDE.md "Read ALL needed attributes BEFORE db.close()")
  ```

  Schritt 3 — `jsonify()` erweitern:

  ```python
  # VORHER: return jsonify(ok=True, **scoring)
  # NACHHER:
  return jsonify(ok=True, log_id=_log_id, **scoring)
  ```

  WICHTIG: Gemaess CLAUDE.md Pattern (`routes/auth.py:18` Kommentar): "Read all needed attributes BEFORE db.close()". Deshalb `_log_id = log.id` NACH commit aber VOR `db.close()` (falls db.close() im finally-Block).

  EDGE-CASE: Falls `scoring` bereits einen Key namens `log_id` enthalten sollte (sehr unwahrscheinlich, aber `generate_scoring()` lesen zur Verifikation), dann `log_id=_log_id` muss NACH `**scoring` stehen, damit es nicht ueberschrieben wird. Aktuell per `grep -n "log_id" services/training_service.py` pruefen — erwartet: kein Match.

  BEACHTE:
  - `jsonify(ok=True, log_id=_log_id, **scoring)` — Python-dict-Unpacking: explizite Kwargs werden VOR **dict gemerged, d.h. scoring.log_id wuerde _log_id ueberschreiben. Deshalb expliziter grep-Check oben.
  - Keine Aenderung am DB-Schema. Keine Migration. Keine neue Spalte.
  </action>
  <verify>
    <automated>grep -n "log_id" routes/training.py</automated>
  </verify>
  <done>
  - `training_end()` extrahiert `_log_id = log.id` nach commit.
  - `return jsonify(ok=True, log_id=_log_id, **scoring)` ersetzt bestehenden Return.
  - `grep "log_id" services/training_service.py` liefert kein Match (scoring-Dict ueberschreibt log_id nicht).
  - Flask-App startet ohne Fehler.
  - Bestehender Training-Flow unveraendert: POST /training/end liefert weiterhin `ok: true` + alle scoring-Felder.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Flask-Route `/training/end` | Authenticated user's training session end trigger |
| Browser → Flask-Route `/session/<id>` | Authenticated user's session detail view |
| Flask-Route → DB-Query | SQL-Queries nutzen SQLAlchemy-ORM mit user_id-Filter (IDOR-Protection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.2-01 | I (Information Disclosure) | `session_detail()` leaks fremde Sessions | mitigate | **Bereits mitigiert** via bestehender `conv.user_id == g.user.id`-Filter (Phase 07.1, Zeile ~706-708). Wave 1 aendert diesen Filter NICHT. |
| T-07.2-02 | T (Tampering) | `phasen_details`-JSON kann manipuliertes Object enthalten | mitigate | `isinstance(pd, dict)` + `isinstance(_cat, list)` Guards vor jeder Nutzung. Silent-except-pass fallback auf leere Listen. Jinja-Autoescape (Default) verhindert XSS. |
| T-07.2-03 | I (Information Disclosure) | `_parse_kunden_meta()` koennte auf leere/malformed pt.name 500en | mitigate | `if not pt_name: return (None, None)` guard. `re.match` liefert None bei kein Match → expliziter `if not m` guard. |
| T-07.2-04 | E (Elevation of Privilege) | User manipuliert log_id in Redirect-URL nach /training/end | accept | log_id wird serverseitig generiert (log.id aus DB-Commit). Wave 3 verwendet es nur fuer Redirect. Session-Detail-Seite hat eigenen user_id-Filter (T-07.2-01) → keine neue Attack-Surface. |
| T-07.2-04b | T (Tampering) | schwierigkeit_raw koennte zu arbitrary String-Injection in Plan-03-URL fuehren | mitigate | Whitelist-Guard: `if raw2_lower in ('leicht', 'mittel', 'schwer')`. Nur diese 3 Werte koennen in schwierigkeit_raw landen — sonst None. Template rendert schwierigkeit_raw nur via `{% if schwierigkeit_raw %}` conditional. |

</threat_model>

<verification>

## Wave-1-Gate (alle MUSS erfuellt sein fuer Wave-2 Start)

1. **Helper-Test:** `python -c "from routes.dashboard import _parse_kunden_meta; print(_parse_kunden_meta('Test, 42'))"` → Output `('Test', '42')`.
2. **Archetyp-Test:** `python -c "from routes.dashboard import _parse_kunden_meta; print(_parse_kunden_meta('Beschaeftigter Chef'))"` → Output `(None, None)`.
3. **Route-Context-Erweiterung:** `grep -n "scoring_kategorien\|scoring_wendepunkte_detail\|scoring_verbesserungen\|kunden_name" routes/dashboard.py` liefert >=5 Matches (3 Variable + Render-Template-Args).
4. **schwierigkeit_raw in Route:** `grep -c "schwierigkeit_raw" routes/dashboard.py` liefert `>=3` (Variable-Init + Assignment + Render-Template-Arg).
5. **log_id im Response:** `grep -n "log_id=_log_id\|log_id=log.id" routes/training.py` liefert >=1 Match.
6. **Flask-App startet:** `python -c "import app; print('OK')"` clean ohne Exceptions.

</verification>

<success_criteria>

- routes/dashboard.py `session_detail()` extrahiert 3 neue Scoring-Listen aus `phasen_details`-JSON mit korrektem Pattern (Try/Except + isinstance-Guards).
- routes/dashboard.py `_parse_kunden_meta()` existiert als benannter Helper, ist unit-testbar.
- routes/dashboard.py liefert `schwierigkeit_raw` ('leicht'|'mittel'|'schwer'|None) als Template-Context-Variable — Whitelist-gesichert.
- routes/dashboard.py `render_template()` uebergibt 6 neue Kwargs an `session_detail.html` (scoring_kategorien, _wendepunkte_detail, _verbesserungen, kunden_name, kunden_alter, schwierigkeit_raw).
- routes/training.py `training_end()` gibt `log_id` in JSON-Response zurueck (ohne andere Response-Felder zu brechen).
- Keine Aenderungen an `templates/session_detail.html`, `templates/training.html`, `static/nerve.css` in dieser Wave.
- Keine neuen DB-Spalten, keine Migration, keine neuen Packages.
- `python -c "from routes.dashboard import session_detail, _parse_kunden_meta"` clean ohne ImportError.
- `python -c "from routes.training import training_end"` clean ohne ImportError.

</success_criteria>

<output>
Nach Abschluss erstellt der Executor AUSSCHLIESSLICH:
- `.planning/phases/07.2-scoring-konsolidierung-inserted/07.2-01-SUMMARY.md`

Executor MUSS NICHT:
- STATE.md updaten (macht Orchestrator)
- ROADMAP.md updaten (macht Orchestrator)
- Andere SUMMARY.md-Dateien modifizieren
- CSS_VERSION in app.py bumpen (passiert in Wave 4)
- deploy.sh ausfuehren (passiert in Wave 4)
</output>
</content>
</invoke>