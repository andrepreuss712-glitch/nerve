---
phase: 04.7
plan: 03
type: execute
wave: 2
depends_on: [04.7-01]
files_modified:
  - database/models.py
  - app.py
  - services/live_session.py
  - routes/app_routes.py
autonomous: true
requirements: [BE-04]

must_haves:
  truths:
    - "objection_events Tabelle existiert mit user_id, conversation_log_id, einwand_typ, success, created_at"
    - "Jeder EWB-Button-Klick wird als eigene Row in objection_events persistiert"
    - "Kein avg_deal_value Feld wird neu angelegt — bestehendes users.avg_deal_wert wird wiederverwendet"
    - "ConversationLog.session_mode + result Felder werden in der Persistenz gefüllt (bestehend, nur Verifikation)"
  artifacts:
    - path: "database/models.py"
      provides: "ObjectionEvent Modell"
    - path: "services/live_session.py"
      provides: "Sammelt EWB-Klicks pro Session-State"
    - path: "routes/app_routes.py"
      provides: "Persistierung der gesammelten Events in api_beenden"
  key_links:
    - from: "services/live_session.py"
      to: "live_session.state"
      via: "ewb_clicks Liste"
      pattern: "ewb_clicks"
    - from: "routes/app_routes.py::api_beenden"
      to: "objection_events Tabelle"
      via: "Bulk-Insert"
      pattern: "ObjectionEvent\\("
---

<objective>
Daten-Tracking härten: Granulare EWB-Klick-Persistenz pro Session in `objection_events`. Klärt Naming-Konflikte (`avg_deal_wert` bleibt, `ConversationLog` ist Sales-Session).

Purpose: Phase 04.5 Training-Analytics + Phase 04.7 KPI-Dashboard brauchen pro-Klick-Daten unterhalb der ConversationLog-Aggregate.
Output: Tabelle, Sammlung im live_session.state, Persistierung in api_beenden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.7-backend-feedback-system/04.7-CONTEXT.md
@.planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md
@database/models.py
@services/live_session.py
@routes/app_routes.py
@app.py

<interfaces>
- `ConversationLog` ist die Sales-Session-Tabelle (NICHT `Session`)
- `users.avg_deal_wert` existiert bereits aus Phase 04.6 — KEIN neues Feld
- Bestehender EWB-Mechanismus: Phase 04.2 MODE-06 loggt Klicks in `quick_action_log` mit `typ='ewb'`. Diese Phase ergänzt zusätzlich strukturierte objection_events.
- live_session.state ist Thread-safe Dict mit Lock
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ObjectionEvent Modell + In-Session-Sammlung</name>
  <files>database/models.py, services/live_session.py</files>
  <read_first>
    - database/models.py (User, ConversationLog, utcnow finden)
    - services/live_session.py (komplett — state-Dict + Lock + bestehende EWB-Hooks)
    - .planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md (Pitfall 1, 2)
  </read_first>
  <action>
    1. **database/models.py** — Neues Modell:
       ```python
       class ObjectionEvent(Base):
           __tablename__ = 'objection_events'
           id                  = Column(Integer, primary_key=True)
           user_id             = Column(Integer, ForeignKey('users.id'), nullable=False)
           org_id              = Column(Integer, ForeignKey('organisations.id'), nullable=True)
           conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id'), nullable=False)
           einwand_typ         = Column(String(100), nullable=False)
           success             = Column(Boolean, default=False, nullable=False)
           created_at          = Column(DateTime, default=utcnow, nullable=False)
       ```
       NICHT: `avg_deal_value` Spalte anlegen — RESEARCH Pitfall 2 sagt explizit: bestehendes `users.avg_deal_wert` verwenden.

    2. **services/live_session.py** — Im `state`-Dict Init eine Liste `ewb_clicks` ergänzen (innerhalb der bestehenden Lock-Sektion):
       ```python
       state.setdefault('ewb_clicks', [])  # Liste von dicts: {'einwand_typ': str, 'success': bool, 'ts': iso}
       ```
       Helper-Funktion ergänzen:
       ```python
       def record_ewb_click(einwand_typ: str, success: bool = False):
           import datetime
           with state_lock:
               state.setdefault('ewb_clicks', []).append({
                   'einwand_typ': einwand_typ,
                   'success': bool(success),
                   'ts': datetime.datetime.utcnow().isoformat(),
               })
       ```
       Das `reset_session()` (existiert bereits) muss `ewb_clicks` zurücksetzen — Eintrag in vorhandener Reset-Funktion ergänzen: `state['ewb_clicks'] = []`.
  </action>
  <acceptance_criteria>
    - `grep -n "class ObjectionEvent" database/models.py` findet 1 Treffer
    - `grep -n "__tablename__ = 'objection_events'" database/models.py` findet 1 Treffer
    - `grep -n "conversation_log_id" database/models.py` findet >=1 Treffer
    - `grep -ni "avg_deal_value" database/models.py` findet 0 Treffer (Pitfall 2)
    - `grep -n "ewb_clicks" services/live_session.py` findet >=2 Treffer
    - `grep -n "def record_ewb_click" services/live_session.py` findet 1 Treffer
    - Smoke: `python -c "import app; from database.models import ObjectionEvent; print(ObjectionEvent.__tablename__)"` druckt `objection_events`
  </acceptance_criteria>
  <verify>
    <automated>python -c "import app; from database.models import ObjectionEvent; from services.live_session import record_ewb_click; record_ewb_click('preis', True); print('OK')"</automated>
  </verify>
  <done>Modell + Helper existieren, kein Naming-Konflikt mit avg_deal_wert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EWB-Trigger ruft record_ewb_click + Persistenz in api_beenden</name>
  <files>routes/app_routes.py, services/live_session.py</files>
  <read_first>
    - routes/app_routes.py (komplett — api_beenden + EWB-Trigger-Endpoint aus Phase 04.2 finden)
    - services/live_session.py (state, get_session vom DB, conv-Lookup-Pattern)
  </read_first>
  <action>
    1. **routes/app_routes.py** — Im EWB-Trigger-Endpoint (Phase 04.2 hat ihn als `api_frage`-Variante mit Einwand-Kontext angelegt — dort wo der Einwand-Typ bekannt ist), nach erfolgreicher Verarbeitung:
       ```python
       from services.live_session import record_ewb_click
       record_ewb_click(einwand_typ=ew_typ, success=False)  # success wird ggf. später nachträglich aktualisiert
       ```
       Falls success-Signal später kommt (z.B. wenn nächster Transcript zeigt dass der Einwand neutralisiert wurde), in der entsprechenden Stelle in `services/live_session.py` (analyse_loop) success-Update auf den letzten Eintrag setzen.

    2. **routes/app_routes.py** — In `api_beenden`, NACH dem Anlegen/Updaten der `ConversationLog conv` und VOR dem `log_action(... 'session_end' ...)` aus Plan 02:
       ```python
       from database.models import ObjectionEvent
       from services.live_session import state, state_lock
       with state_lock:
           clicks = list(state.get('ewb_clicks', []))
       for click in clicks:
           db.add(ObjectionEvent(
               user_id=g.user.id,
               org_id=g.org.id,
               conversation_log_id=conv.id,
               einwand_typ=click['einwand_typ'],
               success=click['success'],
           ))
       db.commit()
       ```
       (Falls `state_lock` nicht der exakte Symbolname ist: den existierenden Lock-Symbolnamen aus services/live_session.py verwenden. Im Zweifel: Lock-Akquise einfach via bestehendem Pattern in der Datei kopieren.)
  </action>
  <acceptance_criteria>
    - `grep -n "record_ewb_click" routes/app_routes.py` findet >=1 Treffer
    - `grep -n "ObjectionEvent(" routes/app_routes.py` findet 1 Treffer
    - `grep -n "from database.models import ObjectionEvent" routes/app_routes.py` findet 1 Treffer
    - `grep -n "ewb_clicks" routes/app_routes.py` findet >=1 Treffer
    - Smoke-Boot: `python -c "import app; print('OK')"`
    - Persistenz erfolgt VOR `log_action('session_end', ...)` aus Plan 02: `python -c "
import re
src = open('routes/app_routes.py').read()
i_oe = src.find('ObjectionEvent(')
i_se = src.find(\"'session_end'\")
assert i_oe>=0 and i_se>=0 and i_oe < i_se, 'ObjectionEvent insert must precede session_end audit'
print('OK')"`
  </acceptance_criteria>
  <verify>
    <automated>python -c "import app; src=open('routes/app_routes.py').read(); assert 'ObjectionEvent(' in src and 'record_ewb_click' in src; print('OK')"</automated>
  </verify>
  <done>EWB-Klicks werden gesammelt und am Session-Ende persistiert, vor dem Audit-Log-Eintrag.</done>
</task>

</tasks>

<verification>
- DB: nach Test-Session sollte `SELECT COUNT(*) FROM objection_events WHERE conversation_log_id=?` > 0 sein
- Naming: `grep -i "avg_deal_value" database/` ist leer
- Phase 04.5 TA-03 (Heatmap) kann ab jetzt von objection_events lesen
</verification>

<success_criteria>
- ObjectionEvent persistiert pro Klick
- Kein doppeltes / falsches avg_deal_value Feld
- session_end Audit kommt NACH Persistenz
</success_criteria>

<output>
.planning/phases/04.7-backend-feedback-system/04.7-03-SUMMARY.md
</output>
</content>
</invoke>