---
phase: 04.7
plan: 02
type: execute
wave: 2
depends_on: [04.7-01]
files_modified:
  - database/models.py
  - services/audit.py
  - app.py
  - routes/auth.py
  - routes/oauth.py
  - routes/app_routes.py
  - routes/profiles.py
autonomous: true
requirements: [BE-03]

must_haves:
  truths:
    - "audit_log Tabelle existiert mit allen Pflichtfeldern"
    - "UPDATE oder DELETE auf audit_log löst SQLite RAISE(ABORT) aus"
    - "log_action() Helper schreibt erfolgreich Einträge inkl. ip_address und user_agent"
    - "Login (Password + OAuth) erzeugt audit_log Eintrag mit action='login'"
    - "Session-Start und Session-Ende erzeugen audit_log Einträge mit Aggregaten als JSON in details"
    - "Profil-Update erzeugt audit_log Eintrag mit action='profile_update'"
    - "Kein Transkript landet in audit_log.details (DSGVO)"
  artifacts:
    - path: "services/audit.py"
      provides: "log_action(db, user_id, org_id, action, target_type, target_id, details, request)"
    - path: "database/models.py"
      provides: "AuditLog Modell"
    - path: "app.py"
      provides: "audit_log_no_update + audit_log_no_delete SQLite Trigger"
  key_links:
    - from: "routes/auth.py"
      to: "services/audit.py::log_action"
      via: "Login-Erfolg"
      pattern: "log_action.*action=.login."
    - from: "routes/app_routes.py"
      to: "services/audit.py::log_action"
      via: "Session-Start/Ende"
      pattern: "log_action.*session_(start|end)"
---

<objective>
Unveränderliches Audit-Log nach DSGVO-Vorgaben. Defense-in-Depth: App-Layer + SQLite Trigger. Wire-up in alle relevanten Auth- und Session-Routes ohne Transkripte zu loggen.

Purpose: D-07. Phase 5 (Early Access) braucht eine forensisch saubere Spur, ohne Audio/Transkript-Daten zu speichern.
Output: AuditLog Tabelle, Helper, Trigger, 4+ Wire-up-Punkte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.7-backend-feedback-system/04.7-CONTEXT.md
@.planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md
@.planning/phases/04.7-backend-feedback-system/04.7-01-PLAN.md
@database/models.py
@app.py
@routes/auth.py
@routes/oauth.py
@routes/app_routes.py
@routes/profiles.py

<interfaces>
- `utcnow` Helper existiert in database/models.py
- `from database.db import get_session` in allen Routes verfügbar
- Sales-Sessions = `ConversationLog` (NICHT `database.models.Session` — das ist Auth-Token!)
- `g.user`, `g.org` aus before_request verfügbar
- `request.remote_addr` und `request.headers.get('User-Agent')` für Audit-Felder
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AuditLog Modell + Immutable-Trigger + log_action Helper</name>
  <files>database/models.py, services/audit.py, app.py</files>
  <read_first>
    - database/models.py (komplett — utcnow, Base, Imports finden)
    - app.py (init_db Block, ALTER TABLE Migration-Pattern)
    - .planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md (Pattern 2 + Pitfall 7)
  </read_first>
  <action>
    1. **database/models.py** — Neues Modell anhängen (vor anderen späteren Modellen, aber nach existierenden):
       ```python
       class AuditLog(Base):
           __tablename__ = 'audit_log'
           id          = Column(Integer, primary_key=True)
           user_id     = Column(Integer, ForeignKey('users.id'), nullable=True)
           org_id      = Column(Integer, ForeignKey('organisations.id'), nullable=True)
           action      = Column(String(100), nullable=False)
           target_type = Column(String(100), nullable=True)
           target_id   = Column(Integer, nullable=True)
           details     = Column(Text, nullable=True)
           ip_address  = Column(String(64), nullable=True)
           user_agent  = Column(String(500), nullable=True)
           created_at  = Column(DateTime, default=utcnow, nullable=False)
       ```
       Stelle sicher dass `Text`, `Boolean` etc. importiert sind.

    2. **services/audit.py** — Neue Datei:
       ```python
       import json
       from database.models import AuditLog

       def log_action(db, user_id, org_id, action, target_type=None, target_id=None, details=None, request=None):
           try:
               entry = AuditLog(
                   user_id=user_id,
                   org_id=org_id,
                   action=action,
                   target_type=target_type,
                   target_id=target_id,
                   details=json.dumps(details, ensure_ascii=False) if details is not None else None,
                   ip_address=(request.remote_addr if request else None),
                   user_agent=((request.headers.get('User-Agent') or '')[:500] if request else None),
               )
               db.add(entry)
               db.commit()
           except Exception as e:
               # Audit darf den Request nicht killen
               print(f"[AUDIT] log_action failed: {e}")
       ```

    3. **app.py** — Nach `init_db()` und NACH der is_superadmin-Migration aus Plan 01, Trigger anlegen (idempotent via IF NOT EXISTS):
       ```python
       try:
           with engine.connect() as conn:
               conn.exec_driver_sql("""
                   CREATE TRIGGER IF NOT EXISTS audit_log_no_update
                   BEFORE UPDATE ON audit_log
                   BEGIN
                     SELECT RAISE(ABORT, 'audit_log is immutable');
                   END;
               """)
               conn.exec_driver_sql("""
                   CREATE TRIGGER IF NOT EXISTS audit_log_no_delete
                   BEFORE DELETE ON audit_log
                   BEGIN
                     SELECT RAISE(ABORT, 'audit_log is immutable');
                   END;
               """)
               conn.commit()
               print("[DB] Audit-Log Trigger installed")
       except Exception as e:
           print(f"[DB] Audit-Log Trigger setup failed: {e}")
       ```
       WICHTIG: Trigger werden NACH `Base.metadata.create_all` installiert (Pitfall 7).
  </action>
  <acceptance_criteria>
    - `grep -n "class AuditLog" database/models.py` findet 1 Treffer
    - `grep -n "__tablename__ = 'audit_log'" database/models.py` findet 1 Treffer
    - Datei `services/audit.py` existiert mit `def log_action`
    - `grep -n "audit_log_no_update" app.py` findet 1 Treffer
    - `grep -n "audit_log_no_delete" app.py` findet 1 Treffer
    - `grep -n "RAISE(ABORT" app.py` findet 2 Treffer
    - Smoke: `python -c "import app; from database.db import get_session; from services.audit import log_action; db=get_session(); log_action(db, None, None, 'test', details={'ok':1}); db.close(); print('OK')"` druckt OK
    - Immutable verifiziert: `python -c "import app; from database.db import get_session; db=get_session(); 
try:
    db.execute(__import__('sqlalchemy').text(\"UPDATE audit_log SET action='x' WHERE id=(SELECT MAX(id) FROM audit_log)\")); db.commit(); print('FAIL')
except Exception as e:
    print('OK' if 'immutable' in str(e) else 'FAIL: '+str(e))
"` druckt OK
  </acceptance_criteria>
  <verify>
    <automated>python -c "import app; from database.db import get_session; from services.audit import log_action; db=get_session(); log_action(db, None, None, 'verify_test', details={'ok':1}); db.close(); print('OK')"</automated>
  </verify>
  <done>AuditLog Tabelle existiert, Trigger blockt UPDATE/DELETE, log_action() Helper funktioniert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire-up in Auth + Session + Profil Routes</name>
  <files>routes/auth.py, routes/oauth.py, routes/app_routes.py, routes/profiles.py</files>
  <read_first>
    - routes/auth.py (komplett — _login_user und Logout-Pfad finden)
    - routes/oauth.py (komplett — OAuth-Callback Erfolgs-Pfad)
    - routes/app_routes.py (komplett — api_start_session und api_beenden suchen)
    - routes/profiles.py (komplett — Profil-Update Route)
  </read_first>
  <action>
    Importiere überall: `from services.audit import log_action` und `from flask import request`.

    1. **routes/auth.py** — Im erfolgreichen Login-Pfad (nach User-Match, vor return), nach dem Superadmin-Seed aus Plan 01:
       ```python
       log_action(db, user.id, getattr(user,'org_id',None), 'login',
                  target_type='user', target_id=user.id,
                  details={'method':'password'}, request=request)
       ```
       Im Logout-Handler:
       ```python
       log_action(db, getattr(g.user,'id',None), getattr(g.user,'org_id',None), 'logout',
                  target_type='user', target_id=getattr(g.user,'id',None), request=request)
       ```

    2. **routes/oauth.py** — Im erfolgreichen OAuth-Callback (Google + Microsoft):
       ```python
       log_action(db, user.id, getattr(user,'org_id',None), 'login',
                  target_type='user', target_id=user.id,
                  details={'method': provider},  # 'google' oder 'microsoft'
                  request=request)
       ```
       (`provider` muss aus dem Callback-Kontext kommen — String literal verwenden falls keine Variable existiert.)

    3. **routes/app_routes.py** — In `api_start_session` (oder Equivalent das Session anlegt) NACH erfolgreichem Anlegen, mit `target_id` = neue ConversationLog-ID:
       ```python
       log_action(db, g.user.id, g.org.id, 'session_start',
                  target_type='conversation_log', target_id=conv.id,
                  details={'mode': session_mode}, request=request)
       ```
       In `api_beenden` NACH dem Persistieren der Aggregate:
       ```python
       log_action(db, g.user.id, g.org.id, 'session_end',
                  target_type='conversation_log', target_id=conv.id,
                  details={
                      'mode': conv.session_mode,
                      'dauer_sekunden': conv.dauer_sekunden,
                      'einwaende_total': getattr(conv,'einwaende_total', None),
                      'einwaende_ok':    getattr(conv,'einwaende_ok', None),
                  },
                  request=request)
       ```
       KEIN Transkript, KEIN Audio, KEINE Phrasen — nur die genannten Aggregate.

    4. **routes/profiles.py** — Im POST-Handler der Profil-Update durchführt, NACH commit:
       ```python
       log_action(db, g.user.id, g.org.id, 'profile_update',
                  target_type='profile', target_id=profile.id,
                  details={'name': profile.name}, request=request)
       ```
  </action>
  <acceptance_criteria>
    - `grep -n "log_action" routes/auth.py` findet >=2 Treffer (login + logout)
    - `grep -n "log_action" routes/oauth.py` findet >=1 Treffer
    - `grep -n "log_action" routes/app_routes.py` findet >=2 Treffer (session_start + session_end)
    - `grep -n "log_action" routes/profiles.py` findet >=1 Treffer
    - `grep -n "from services.audit import log_action" routes/auth.py` findet 1 Treffer
    - `grep -n "session_start" routes/app_routes.py` findet 1 Treffer
    - `grep -n "session_end" routes/app_routes.py` findet 1 Treffer
    - Kein Wort "transcript" oder "transkript" in den details-Dicts: `grep -in "transcript\|transkript" routes/app_routes.py | grep "log_action\|details"` ist leer
    - Smoke-Boot: `python -c "import app; print('OK')"` läuft
  </acceptance_criteria>
  <verify>
    <automated>python -c "import app; import routes.auth, routes.oauth, routes.app_routes, routes.profiles; print('OK')"</automated>
  </verify>
  <done>Alle 4 Routes loggen via log_action ohne Transkript-Daten, App startet sauber.</done>
</task>

</tasks>

<verification>
- DB: `sqlite3 database/nerve.db ".schema audit_log"` zeigt Tabelle + Trigger
- Manuell: Login → SELECT * FROM audit_log → 1 Eintrag mit action='login'
- Manuell: UPDATE Versuch → SQLite-Fehler 'audit_log is immutable'
- Phase 5 später: Verifizierbare Audit-Spur für DSGVO-Auskunft
</verification>

<success_criteria>
- AuditLog persistiert + immutable
- 4 Wire-up-Punkte funktionieren
- Keine PII über Aggregat-Felder hinaus geloggt
- log_action Failures killen den Request nicht
</success_criteria>

<output>
.planning/phases/04.7-backend-feedback-system/04.7-02-SUMMARY.md
</output>
</content>
</invoke>