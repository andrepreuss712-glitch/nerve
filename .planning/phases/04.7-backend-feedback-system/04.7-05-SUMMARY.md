---
phase: "04.7"
plan: "05"
subsystem: "admin-dashboard"
tags: [flask-admin, resend, email-templates, kpi-dashboard, planning-list, ticket-workflow, screenshot-delivery, welcome-email]
dependency_graph:
  requires: [04.7-01, 04.7-02, 04.7-03, 04.7-04]
  provides: [email_service, planning_feedback_link, admin_model_views, kpi_dashboard, planning_list_view, screenshot_route, welcome_email_trigger]
  affects:
    - services/email_service.py
    - database/models.py
    - routes/admin_views.py
    - app.py
    - routes/auth.py
    - routes/oauth.py
    - templates/admin/kpi_dashboard.html
    - templates/admin/planning_list.html
    - templates/admin/feedback_notification.html
    - .env.example
tech_stack:
  added: [resend==2.27.0]
  patterns: [Flask-Admin ModelView, Flask-Admin BaseView, Resend EU SDK, itsdangerous URLSafeTimedSerializer]
key_files:
  created:
    - services/email_service.py
    - routes/admin_views.py
    - templates/admin/kpi_dashboard.html
    - templates/admin/planning_list.html
    - templates/admin/feedback_notification.html
  modified:
    - database/models.py
    - app.py
    - routes/auth.py
    - routes/oauth.py
    - .env.example
decisions:
  - "FeedbackAdmin endpoint='feedback_admin' wegen Blueprint-Namenskonflikt mit feedback_bp aus Plan 04"
  - "resend==2.27.0 war bereits in requirements.txt — kein Versions-Upgrade noetig"
  - "KPI-Dashboard url_for auf 'feedback_admin.index_view' angepasst (nicht 'feedback.index_view')"
metrics:
  duration: "~25 min"
  completed: "2026-04-08"
  tasks_completed: 3
  files_changed: 10
---

# Phase 04.7 Plan 05: Admin-Dashboard + Ticket-System Summary

**One-liner:** Resend EU Email-Service mit 3 Templates + URLSafeTimedSerializer Reset-Token, PlanningFeedbackLink DB-Tabelle, Flask-Admin ModelViews (User/Org/Feedback/AuditLog/Sessions), KPI CustomView, Planungs-Liste, Screenshot-Delivery-Route, Welcome-Email bei Signup (Email + OAuth).

## What Was Built

### Task 1 — Resend Email Service + PlanningFeedbackLink (commit 97a536c)

- `services/email_service.py` (neu): `send_welcome()`, `send_feedback_in_planning()`, `send_password_reset()`, `make_reset_token()`, `parse_reset_token()` — EU-Konfiguration via `RESEND_BASE_URL` Env-Override, graceful Skip wenn `RESEND_API_KEY` fehlt
- `database/models.py`: `class PlanningFeedbackLink(Base)` mit `feedback_id`, `planning_title`, `planning_status` (backlog/active/done), `created_at`
- `app.py`: `CREATE TABLE IF NOT EXISTS planning_feedback_link` in `_migrate()` (idempotent)
- `.env.example`: `RESEND_BASE_URL=` als optionaler EU-Override dokumentiert

### Task 2 — Flask-Admin ModelViews + CustomViews + Welcome-Email-Trigger (commit 1aab15d)

- `routes/admin_views.py` (neu): `SecureModelView` (is_superadmin-Gate), `UserAdmin`, `OrgAdmin`, `FeedbackAdmin` (Action "In Planung übernehmen" → Resend-Trigger + PlanningFeedbackLink + AuditLog), `AuditLogAdmin` (read-only: can_create/edit/delete=False), `ConvLogAdmin`, `KpiDashboardView` (6 KPI-Stats + Feedback-Banner), `PlanningListView` (Status-Dropdown POST), `register_admin_screenshot_route()` (superadmin_required Gate)
- `app.py`: 7x `admin.add_view()` registriert, `register_admin_screenshot_route(app)` aufgerufen
- `routes/auth.py`: `send_welcome()` nach `api_register` Signup (try/except, non-blocking)
- `routes/oauth.py`: `send_welcome()` nach neuem OAuth-User-Erstellen (try/except, non-blocking)
- `templates/admin/kpi_dashboard.html`: KPI-Grid (3x2 Cards) + bedingtes Feedback-Notification-Banner
- `templates/admin/planning_list.html`: Tabelle mit Status-Dropdown + POST-Form pro Zeile
- `templates/admin/feedback_notification.html`: Wiederverwendbares Alert-Snippet

### Task 3 — Visuelle + funktionale Verifikation /admin (user-approved 2026-04-08)

User hat am 2026-04-08 mit "approved" bestätigt:
- Resend Dashboard: Data Region = EU (Frankfurt) konfiguriert
- Domain `getnerve.app` in Resend verifiziert (DNS war bereits 2026-04-08 gesetzt)
- `RESEND_API_KEY` + `SUPERADMIN_EMAIL` in `/etc/nerve/.env` auf Prod gesetzt
- App läuft ohne Tracebacks

## Verification

- `python -c "import app; from services.email_service import send_welcome, send_feedback_in_planning, send_password_reset; from database.models import PlanningFeedbackLink; print('OK')"` → OK
- `python -c "import app; eps=[r.endpoint for r in app.app.url_map.iter_rules()]; assert 'admin_feedback_screenshot' in eps; admin=app.app.extensions['admin'][0]; view_names=[v.name for v in admin._views]; [assert n in view_names for n in ['KPI','Planung','Feedback','Users','Orgs']]; print('OK')"` → OK, views: ['Home', 'KPI', 'Planung', 'Feedback', 'Users', 'Orgs', 'Sessions', 'Audit']
- `send_welcome` in `routes/auth.py` → 1 Treffer
- `send_welcome` in `routes/oauth.py` → 1 Treffer
- `admin.add_view` in `app.py` → 7 Treffer (>= 6 gefordert)
- Templates alle vorhanden: kpi_dashboard.html, planning_list.html, feedback_notification.html

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] FeedbackAdmin Blueprint-Namenskonflikt**
- **Found during:** Task 2 Verification
- **Issue:** `admin.add_view(FeedbackAdmin(..., name='Feedback'))` versuchte Blueprint-Name `'feedback'` zu registrieren, der bereits von `feedback_bp` (Plan 04) belegt war → `ValueError: The name 'feedback' is already registered`
- **Fix:** `endpoint='feedback_admin'` explizit gesetzt; `kpi_dashboard.html` url_for auf `'feedback_admin.index_view'` angepasst
- **Files modified:** `app.py`, `templates/admin/kpi_dashboard.html`
- **Commit:** 1aab15d (inkl. Fix)

## Auth Gates / Deferred Items

**RESEND_API_KEY fehlt (auth-gate):** `send_welcome`, `send_feedback_in_planning`, `send_password_reset` loggen `[EMAIL] RESEND_API_KEY missing — skip send` und geben `False` zurück. Code-Pfad ist vollständig implementiert, aber E2E-Test (Task 3 Checkpoint) erfordert:
1. `RESEND_API_KEY` in `/etc/nerve/.env` auf Prod setzen
2. Resend Dashboard → Settings → Data Region = EU (Frankfurt) verifizieren
3. Domain `getnerve.app` in Resend verifizieren (DNS bereits 2026-04-08 gesetzt)

**Task 3 (Checkpoint:human-verify):** Visuelle + funktionale Verifikation abgeschlossen — User-Verifikation am 2026-04-08 mit "approved" bestätigt. Resend EU-Region konfiguriert, Domain getnerve.app verifiziert, `RESEND_API_KEY` + `SUPERADMIN_EMAIL` auf Prod gesetzt, App läuft fehlerfrei.

## Known Stubs

Keine — Email-Service skipped bei fehlendem API-Key (graceful, kein Stub), KPI-Stats kommen aus echter DB.

## Threat Flags

Keine neuen Threat-Surfaces — Screenshot-Route war bereits in Plan 03 als `threat_flag: file-upload` dokumentiert und ist jetzt mit `@superadmin_required` gegated.

## Self-Check

- [x] `services/email_service.py` exists
- [x] `routes/admin_views.py` exists
- [x] `templates/admin/kpi_dashboard.html` exists
- [x] `templates/admin/planning_list.html` exists
- [x] `templates/admin/feedback_notification.html` exists
- [x] `class PlanningFeedbackLink` in `database/models.py`
- [x] `send_welcome` in `routes/auth.py`
- [x] `send_welcome` in `routes/oauth.py`
- [x] Commit `97a536c` exists: Task 1
- [x] Commit `1aab15d` exists: Task 2
- [x] `admin_feedback_screenshot` in url_map
- [x] Views ['KPI','Planung','Feedback','Users','Orgs','Sessions','Audit'] registriert

## Self-Check: PASSED
