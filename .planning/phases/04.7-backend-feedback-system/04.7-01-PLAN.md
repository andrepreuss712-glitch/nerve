---
phase: 04.7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - database/db.py
  - routes/auth.py
  - routes/oauth.py
  - services/auth_decorators.py
  - app.py
  - .env.example
  - requirements.txt
autonomous: true
requirements: [BE-01, BE-02]
user_setup:
  - service: env
    why: "Superadmin bootstrap + Resend Key vorbereiten"
    env_vars:
      - name: SUPERADMIN_EMAIL
        source: "André's Login-Email (z.B. andre@getnerve.app)"
      - name: RESEND_API_KEY
        source: "https://resend.com/api-keys (EU Region)"

must_haves:
  truths:
    - "users.is_superadmin Spalte existiert mit Default False"
    - "André wird beim Login automatisch zum Superadmin (idempotent)"
    - "@superadmin_required Decorator verweigert Nicht-Superadmins mit 403"
    - "/admin liefert HTTP 200 für Superadmin, 403/Redirect für andere"
    - "Flask-Admin nutzt scoped_session aus database/db.py (keine Connection-Leaks)"
  artifacts:
    - path: "services/auth_decorators.py"
      provides: "superadmin_required Decorator"
    - path: "database/db.py"
      provides: "ScopedSession (db_session) für Flask-Admin"
    - path: "app.py"
      provides: "Flask-Admin Init mit SecureIndexView, /admin Mount"
  key_links:
    - from: "routes/auth.py::_login_user"
      to: "users.is_superadmin"
      via: "ENV-Seed Block"
      pattern: "SUPERADMIN_EMAIL"
    - from: "app.py"
      to: "Flask-Admin"
      via: "Admin(app, index_view=SecureIndexView)"
      pattern: "Admin\\(app"
---

<objective>
Foundation für Phase 04.7: Superadmin-Flag, Decorator, ENV-Seed, Flask-Admin Setup mit Auth-Gate unter `/admin`. Alle Folge-Plans bauen darauf auf.

Purpose: Ohne diese Foundation existiert kein Admin-Bereich, kein Berechtigungs-Gate, kein scoped_session-Pattern für Admin-Views.
Output: `/admin` rendert ein leeres aber gegateetes Dashboard. Decorator + Migration laufen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.7-backend-feedback-system/04.7-CONTEXT.md
@.planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md
@database/models.py
@database/db.py
@routes/auth.py
@routes/oauth.py
@app.py
@requirements.txt
@.env.example

<interfaces>
RESEARCH bestätigt:
- `database.models.Session` ist Auth-Token-Tabelle, NICHT Sales-Session
- `users.avg_deal_wert` existiert bereits (NICHT umbenennen)
- `database/db.py` exportiert `engine`, `SessionLocal`, `Base`, `get_session()` — manuelles try/finally Pattern
- `routes/auth.py::_login_user` ist der Hook für ENV-Seed
- Flask before_request setzt `g.user` — funktioniert auch für Flask-Admin Routes
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration users.is_superadmin + scoped_session + Decorator</name>
  <files>database/models.py, database/db.py, services/auth_decorators.py, app.py, requirements.txt, .env.example</files>
  <read_first>
    - database/models.py (komplett, User-Klasse + utcnow Helper finden)
    - database/db.py (komplett, engine + SessionLocal Setup)
    - app.py (komplett, init_db + Migration-Pattern mit ALTER TABLE try/except)
    - requirements.txt
    - .env.example
  </read_first>
  <action>
    1. **requirements.txt** — Pin exakt:
       ```
       flask-admin==2.0.2
       resend==2.27.0
       ```
       (Resend-Key wird hier nicht genutzt, nur Pin damit Plan 06 ihn schon hat.)

    2. **database/models.py** — In `class User(Base)` neue Spalte direkt nach `rolle`:
       ```python
       is_superadmin = Column(Boolean, default=False, nullable=False)
       ```
       Falls `Boolean` nicht importiert ist: `from sqlalchemy import Boolean` ergänzen.

    3. **database/db.py** — `scoped_session` ergänzen (für Flask-Admin):
       ```python
       from sqlalchemy.orm import scoped_session
       db_session = scoped_session(SessionLocal)
       ```
       Export `db_session` zusätzlich zu bestehenden Symbolen. Bestehende `get_session()` UNVERÄNDERT lassen — sie wird weiterhin von Routes benutzt.

    4. **app.py** — Idempotente Migration nach `init_db()` ergänzen, exakt im bestehenden ALTER-TABLE-try/except-Stil:
       ```python
       try:
           with engine.connect() as conn:
               conn.exec_driver_sql("ALTER TABLE users ADD COLUMN is_superadmin BOOLEAN DEFAULT 0")
               conn.commit()
               print("[DB] Migration: added users.is_superadmin")
       except Exception:
           pass
       ```
       Direkt unter den anderen ALTER-TABLE-Blocks platzieren.

    5. **services/auth_decorators.py** — Neue Datei:
       ```python
       from functools import wraps
       from flask import g, abort

       def superadmin_required(f):
           @wraps(f)
           def wrapper(*args, **kwargs):
               if not getattr(g, 'user', None) or not getattr(g.user, 'is_superadmin', False):
                   abort(403)
               return f(*args, **kwargs)
           return wrapper
       ```

    6. **.env.example** — Anhängen:
       ```
       # --- Phase 04.7 Backend & Feedback ---
       SUPERADMIN_EMAIL=
       RESEND_API_KEY=
       ```
  </action>
  <acceptance_criteria>
    - `grep -n "is_superadmin = Column" database/models.py` findet 1 Treffer
    - `grep -n "from sqlalchemy.orm import scoped_session" database/db.py` findet 1 Treffer
    - `grep -n "db_session = scoped_session" database/db.py` findet 1 Treffer
    - `grep -n "is_superadmin" app.py` findet die Migration
    - Datei `services/auth_decorators.py` existiert mit `def superadmin_required`
    - `grep -n "flask-admin==2.0.2" requirements.txt` findet 1 Treffer
    - `grep -n "resend==2.27.0" requirements.txt` findet 1 Treffer
    - `grep -n "SUPERADMIN_EMAIL" .env.example` findet 1 Treffer
    - `python -c "from database.models import User; print(hasattr(User,'is_superadmin'))"` druckt `True`
    - `python -c "from services.auth_decorators import superadmin_required; print(callable(superadmin_required))"` druckt `True`
  </acceptance_criteria>
  <verify>
    <automated>python -c "from database.models import User; from services.auth_decorators import superadmin_required; from database.db import db_session; assert hasattr(User,'is_superadmin'); assert callable(superadmin_required); assert db_session is not None; print('OK')"</automated>
  </verify>
  <done>Migration läuft idempotent, Decorator + scoped_session importierbar, requirements gepinnt.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ENV-Seed im Login + Flask-Admin Mount mit Auth-Gate</name>
  <files>routes/auth.py, routes/oauth.py, app.py</files>
  <read_first>
    - routes/auth.py (komplett, _login_user / _do_login Funktion finden — Hook nach erfolgreichem Login vor Commit)
    - routes/oauth.py (komplett, OAuth-Login-Pfad finden — selbe Stelle nach erfolgreichem User-Match)
    - app.py (komplett, Blueprint-Register-Reihenfolge finden — Admin MUSS NACH allen Blueprints registriert werden)
    - .planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md (Pattern 1 + 3 + Pitfall 6)
  </read_first>
  <action>
    1. **routes/auth.py** — Im `_login_user` (oder `_do_login`) direkt nach erfolgreichem `user`-Lookup und vor `db.commit()` / return:
       ```python
       import os  # falls noch nicht
       _sa_email = os.environ.get('SUPERADMIN_EMAIL', '').strip().lower()
       if _sa_email and user.email and user.email.lower() == _sa_email and not user.is_superadmin:
           user.is_superadmin = True
           db.commit()
           print(f"[AUTH] Seeded superadmin: {user.email}")
       ```

    2. **routes/oauth.py** — Im OAuth-Login-Callback an der Stelle wo der User aus DB geladen oder erstellt wird, denselben Block einfügen (kopiert, idempotent).

    3. **app.py** — Flask-Admin NACH allen `app.register_blueprint(...)` Calls einfügen:
       ```python
       from flask_admin import Admin, AdminIndexView, expose
       from flask import g, redirect, url_for, abort
       from database.db import db_session

       class SecureIndexView(AdminIndexView):
           def is_accessible(self):
               return getattr(g, 'user', None) is not None and getattr(g.user, 'is_superadmin', False)
           def inaccessible_callback(self, name, **kwargs):
               if getattr(g, 'user', None) is None:
                   return redirect(url_for('auth.login'))
               abort(403)

       admin = Admin(
           app,
           name='NERVE Admin',
           template_mode='bootstrap4',
           index_view=SecureIndexView(url='/admin'),
       )
       ```
       Plan 02/05 fügen später ModelViews via `admin.add_view(...)` hinzu — `admin` muss daher Modul-global oder via `app.extensions['admin']` zugänglich sein. Flask-Admin registriert sich selbst in `app.extensions['admin']` → andere Plans nutzen `app.extensions['admin'][0]`.
  </action>
  <acceptance_criteria>
    - `grep -n "SUPERADMIN_EMAIL" routes/auth.py` findet 1 Treffer
    - `grep -n "SUPERADMIN_EMAIL" routes/oauth.py` findet 1 Treffer
    - `grep -n "is_superadmin = True" routes/auth.py` findet 1 Treffer
    - `grep -n "from flask_admin import Admin" app.py` findet 1 Treffer
    - `grep -n "class SecureIndexView" app.py` findet 1 Treffer
    - `grep -n "name='NERVE Admin'" app.py` findet 1 Treffer
    - `grep -n "template_mode='bootstrap4'" app.py` findet 1 Treffer
    - `grep -n "url='/admin'" app.py` findet 1 Treffer
    - `python -c "import app; assert 'admin' in app.app.extensions; print('OK')"` druckt `OK`
    - App startet ohne Exception (`python -c "import app; print('boot ok')"`)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import app; assert 'admin' in app.app.extensions, 'flask-admin not registered'; print('OK')"</automated>
  </verify>
  <done>Login-Hook seedet André, /admin gemountet und durch SecureIndexView gegated, App bootet sauber.</done>
</task>

</tasks>

<verification>
1. App startet: `python -c "import app; print('OK')"`
2. Migration idempotent: zweiter Boot loggt keinen Fehler
3. Manuell: GET /admin als nicht-eingeloggter User → Redirect zu login
4. Manuell: GET /admin als eingeloggter Nicht-Superadmin → 403
5. Manuell: Login mit SUPERADMIN_EMAIL → Flag wird gesetzt, /admin liefert 200
</verification>

<success_criteria>
- users.is_superadmin existiert und ist nullable=False mit Default False
- superadmin_required Decorator importierbar
- /admin gemountet, gegated, bootet ohne Crash
- ENV-Seed funktioniert in beiden Login-Pfaden (Password + OAuth)
- scoped_session db_session in database/db.py verfügbar (für Plan 02/05 Flask-Admin Views)
</success_criteria>

<output>
.planning/phases/04.7-backend-feedback-system/04.7-01-SUMMARY.md
</output>
</content>
</invoke>