---
phase: 04.7
plan: 05
type: execute
wave: 4
depends_on: [04.7-02, 04.7-03, 04.7-04]
files_modified:
  - database/models.py
  - services/email_service.py
  - routes/admin_views.py
  - app.py
  - templates/admin/kpi_dashboard.html
  - templates/admin/planning_list.html
  - templates/admin/feedback_notification.html
  - .env.example
autonomous: false
requirements: [BE-06, BE-08]
user_setup:
  - service: resend
    why: "Transactional Email für Welcome + Feedback-in-Planung + Password-Reset"
    env_vars:
      - name: RESEND_API_KEY
        source: "https://resend.com/api-keys (EU Region)"
    dashboard_config:
      - task: "Account auf EU Data Region setzen"
        location: "Resend Dashboard → Settings → Data Region → EU"
      - task: "Domain getnerve.app verifizieren (DNS bereits gesetzt 2026-04-08)"
        location: "Resend Dashboard → Domains → getnerve.app"

must_haves:
  truths:
    - "Resend ist auf EU-Region konfiguriert (verifiziert via Dashboard oder SDK base_url)"
    - "send_welcome, send_feedback_in_planning, send_password_reset Funktionen funktionieren"
    - "Flask-Admin ModelViews für User, Organisation, Feedback, AuditLog, ConversationLog registriert"
    - "KPI-Dashboard zeigt: aktive User, Sessions diese Woche, neue Feedbacks, Audit-Events"
    - "planning_feedback_link Tabelle existiert"
    - "Admin kann Feedback auf 'in_planning' setzen → triggert Resend send_feedback_in_planning + planning_feedback_link Eintrag + notification_sent=True"
    - "Feedback-Notification-Popup zeigt beim /admin Load offene neue Feedbacks"
    - "GET /admin/feedback/screenshot/<uuid> liefert Bild nur für Superadmin"
  artifacts:
    - path: "services/email_service.py"
      provides: "Resend EU-Wrapper mit 3 Templates"
    - path: "routes/admin_views.py"
      provides: "ModelViews + KPI CustomView + Planning CustomView + Screenshot-Route"
    - path: "database/models.py"
      provides: "PlanningFeedbackLink Modell"
  key_links:
    - from: "routes/admin_views.py"
      to: "services/email_service.py::send_feedback_in_planning"
      via: "Action 'In Planung übernehmen'"
      pattern: "send_feedback_in_planning"
    - from: "app.py"
      to: "Flask-Admin"
      via: "admin.add_view(SecureModelView(...))"
      pattern: "add_view\\(SecureModelView"
---

<objective>
Admin-Dashboard mit ModelViews, KPI-CustomView, Ticket-Workflow, Planungs-Liste, Resend-Email-Trigger für 3 Templates. Wave-4-Plan: braucht alles aus 02/03/04.

Purpose: Plans 01-04 liefern Foundation + Daten. Plan 05 macht das Ganze nutzbar für André als Solo-Founder-Admin.
Output: Funktionierendes /admin mit Listen, Filtern, Tickets, Emails. Checkpoint am Ende für visuelle Verifikation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.7-backend-feedback-system/04.7-CONTEXT.md
@.planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md
@.planning/phases/04.7-backend-feedback-system/04.7-01-PLAN.md
@.planning/phases/04.7-backend-feedback-system/04.7-02-PLAN.md
@.planning/phases/04.7-backend-feedback-system/04.7-03-PLAN.md
@.planning/phases/04.7-backend-feedback-system/04.7-04-PLAN.md
@database/models.py
@app.py

<interfaces>
- Aus Plan 01: `db_session` (scoped_session) in database/db.py, `admin` in app.extensions
- Aus Plan 02: AuditLog Modell + log_action
- Aus Plan 04: Feedback Modell, UPLOAD_DIR, ALLOWED_EXT
- Aus Plan 03: ObjectionEvent Modell
- Resend SDK: `resend.api_key = ...`, `resend.Emails.send({...})`. EU-Region Verifikation siehe Pitfall 4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Resend Email Service (EU) + 3 Templates + planning_feedback_link Tabelle</name>
  <files>services/email_service.py, database/models.py, .env.example</files>
  <read_first>
    - .planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md (Code Examples Resend, Pitfall 4)
    - database/models.py (Feedback Modell aus Plan 04)
    - .env.example
  </read_first>
  <action>
    1. **services/email_service.py** — Neue Datei. EU-Region Verifikation: Resend setzt EU per Account-Setting. SDK selbst hat optional `resend.base_url` — wir setzen es defensiv falls ENV-Var `RESEND_BASE_URL` gesetzt ist.
       ```python
       import os
       import resend
       from itsdangerous import URLSafeTimedSerializer

       resend.api_key = os.environ.get('RESEND_API_KEY', '')
       _eu_base = os.environ.get('RESEND_BASE_URL')  # optional override, e.g. 'https://api.resend.com'
       if _eu_base:
           try:
               resend.base_url = _eu_base
           except Exception:
               pass

       FROM_SYSTEM   = 'NERVE <noreply@getnerve.app>'
       FROM_FEEDBACK = 'NERVE Feedback <feedback@getnerve.app>'

       def _send(payload):
           if not resend.api_key:
               print('[EMAIL] RESEND_API_KEY missing — skip send')
               return False
           try:
               resend.Emails.send(payload)
               return True
           except Exception as e:
               print(f'[EMAIL] send failed: {e}')
               return False

       def send_welcome(to_email, vorname=''):
           name = vorname or 'da'
           html = (
             f"<p>Hallo {name},</p>"
             "<p>willkommen bei NERVE. Du kannst direkt loslegen: "
             "<a href='https://app.getnerve.app/dashboard'>app.getnerve.app</a></p>"
             "<p>Wenn etwas hakt: einfach im Feedback-Button melden. Ich lese jedes Feedback selbst.</p>"
             "<p>— André, Founder NERVE</p>"
           )
           return _send({'from': FROM_SYSTEM, 'to': [to_email],
                         'subject': 'Willkommen bei NERVE', 'html': html})

       def send_feedback_in_planning(to_email, feedback_text, vorname=''):
           name = vorname or 'da'
           snippet = (feedback_text or '')[:300]
           html = (
             f"<p>Hallo {name},</p>"
             "<p>kurze Info: dein Feedback ist bei mir gelandet und ich habe es in die Planung übernommen.</p>"
             f"<blockquote style='border-left:3px solid #00D4AA;padding-left:12px;color:#444'>{snippet}</blockquote>"
             "<p>Ich melde mich nochmal sobald es live ist. Danke dass du NERVE besser machst.</p>"
             "<p>— André</p>"
           )
           return _send({'from': FROM_FEEDBACK, 'to': [to_email],
                         'subject': 'Dein Feedback ist in der Planung', 'html': html})

       def send_password_reset(to_email, reset_url):
           html = (
             "<p>Du hast einen Passwort-Reset für NERVE angefordert.</p>"
             f"<p><a href='{reset_url}'>Neues Passwort setzen</a> (Link gültig 1 Stunde)</p>"
             "<p>Wenn du das nicht warst, ignoriere diese Email.</p>"
           )
           return _send({'from': FROM_SYSTEM, 'to': [to_email],
                         'subject': 'NERVE Passwort zurücksetzen', 'html': html})

       def make_reset_token(secret, email):
           return URLSafeTimedSerializer(secret, salt='nerve-pwreset').dumps(email)

       def parse_reset_token(secret, token, max_age=3600):
           return URLSafeTimedSerializer(secret, salt='nerve-pwreset').loads(token, max_age=max_age)
       ```

    2. **database/models.py** — Neue Tabelle:
       ```python
       class PlanningFeedbackLink(Base):
           __tablename__ = 'planning_feedback_link'
           id          = Column(Integer, primary_key=True)
           feedback_id = Column(Integer, ForeignKey('feedback.id'), nullable=False)
           planning_title = Column(String(200), nullable=False)
           planning_status = Column(String(40), default='backlog', nullable=False)  # backlog|active|done
           created_at  = Column(DateTime, default=utcnow, nullable=False)
       ```

    3. **.env.example** — Anhängen:
       ```
       # Optional Resend Override (default ist Account-Region; EU im Resend Dashboard setzen)
       RESEND_BASE_URL=
       ```
  </action>
  <acceptance_criteria>
    - Datei `services/email_service.py` existiert
    - `grep -n "def send_welcome" services/email_service.py` findet 1 Treffer
    - `grep -n "def send_feedback_in_planning" services/email_service.py` findet 1 Treffer
    - `grep -n "def send_password_reset" services/email_service.py` findet 1 Treffer
    - `grep -n "noreply@getnerve.app" services/email_service.py` findet 1 Treffer
    - `grep -n "feedback@getnerve.app" services/email_service.py` findet 1 Treffer
    - `grep -n "URLSafeTimedSerializer" services/email_service.py` findet 1 Treffer
    - `grep -n "class PlanningFeedbackLink" database/models.py` findet 1 Treffer
    - Smoke: `python -c "import app; from services.email_service import send_welcome, send_feedback_in_planning, send_password_reset, make_reset_token; print('OK')"`
  </acceptance_criteria>
  <verify>
    <automated>python -c "import app; from services.email_service import send_welcome, send_feedback_in_planning, send_password_reset; from database.models import PlanningFeedbackLink; print('OK')"</automated>
  </verify>
  <done>Email-Service mit 3 Templates, Reset-Token-Helper, PlanningFeedbackLink Tabelle.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flask-Admin ModelViews + KPI CustomView + Planning + Screenshot-Route + Welcome-Trigger</name>
  <files>routes/admin_views.py, app.py, templates/admin/kpi_dashboard.html, templates/admin/planning_list.html, templates/admin/feedback_notification.html, routes/auth.py, routes/oauth.py</files>
  <read_first>
    - .planning/phases/04.7-backend-feedback-system/04.7-RESEARCH.md (Pattern 3, Pitfall 3 + 6)
    - database/models.py (alle relevanten Modelle)
    - app.py (Flask-Admin Init aus Plan 01 — admin Variable)
    - routes/auth.py (Signup-Pfad, _create_org_and_user)
    - routes/oauth.py (OAuth Signup-Pfad — Erst-Login)
  </read_first>
  <action>
    1. **routes/admin_views.py** — Neue Datei mit ModelViews + CustomViews:
       ```python
       from flask import g, redirect, url_for, abort, request, send_from_directory, flash, jsonify
       from flask_admin import BaseView, expose
       from flask_admin.contrib.sqla import ModelView
       from flask_admin.actions import action
       from sqlalchemy import func
       from database.db import db_session, get_session
       from database.models import (
           User, Organisation, Feedback, AuditLog, ConversationLog,
           ObjectionEvent, PlanningFeedbackLink,
       )
       from services.email_service import send_feedback_in_planning
       from services.feedback_service import UPLOAD_DIR
       from services.audit import log_action
       import os

       def _is_superadmin():
           return getattr(g, 'user', None) is not None and getattr(g.user, 'is_superadmin', False)

       class SecureModelView(ModelView):
           page_size = 50
           def is_accessible(self):
               return _is_superadmin()
           def inaccessible_callback(self, name, **kwargs):
               if getattr(g, 'user', None) is None:
                   return redirect(url_for('auth.login'))
               abort(403)

       class UserAdmin(SecureModelView):
           column_list = ('id','email','rolle','is_superadmin','org_id','erstellt_am')
           column_searchable_list = ('email',)
           column_filters = ('rolle','is_superadmin','org_id')

       class OrgAdmin(SecureModelView):
           column_list = ('id','name','plan','subscription_status')
           column_searchable_list = ('name',)
           column_filters = ('plan','subscription_status')

       class FeedbackAdmin(SecureModelView):
           column_list = ('id','user_id','typ','status','rating','kategorie','created_at','notification_sent')
           column_filters = ('typ','status','kategorie','notification_sent')
           column_searchable_list = ('text','kategorie')
           form_columns = ('typ','status','kategorie','text')

           @action('mark_in_planning', 'In Planung übernehmen',
                   'Wirklich diese Feedbacks in Planung übernehmen und User benachrichtigen?')
           def action_mark_in_planning(self, ids):
               db = get_session()
               try:
                   sent = 0
                   for fid in ids:
                       fb = db.query(Feedback).get(int(fid))
                       if not fb: continue
                       fb.status = 'in_planning'
                       db.add(PlanningFeedbackLink(
                           feedback_id=fb.id,
                           planning_title=(fb.text or '')[:200],
                       ))
                       if not fb.notification_sent:
                           u = db.query(User).get(fb.user_id)
                           if u and u.email:
                               ok = send_feedback_in_planning(u.email, fb.text or '', getattr(u,'vorname','') or '')
                               if ok:
                                   fb.notification_sent = True
                                   sent += 1
                       log_action(db, getattr(g.user,'id',None), getattr(g.user,'org_id',None),
                                  'feedback_in_planning', target_type='feedback', target_id=fb.id,
                                  request=request)
                   db.commit()
                   flash(f'{len(ids)} Feedback(s) in Planung. {sent} Email(s) gesendet.', 'success')
               finally:
                   db.close()

       class AuditLogAdmin(SecureModelView):
           can_create = False
           can_edit   = False
           can_delete = False
           column_list = ('id','created_at','action','user_id','target_type','target_id','ip_address')
           column_filters = ('action','user_id','target_type')
           column_default_sort = ('id', True)

       class ConvLogAdmin(SecureModelView):
           can_edit = False
           can_delete = False
           column_list = ('id','user_id','session_mode','dauer_sekunden','einwaende_total','einwaende_ok','started_at','ended_at')
           column_filters = ('session_mode','user_id')
           column_default_sort = ('id', True)

       class KpiDashboardView(BaseView):
           def is_accessible(self):
               return _is_superadmin()
           def inaccessible_callback(self, name, **kwargs):
               return redirect(url_for('auth.login'))
           @expose('/')
           def index(self):
               db = get_session()
               try:
                   from datetime import datetime, timedelta
                   week_ago = datetime.utcnow() - timedelta(days=7)
                   stats = {
                       'users_total':       db.query(func.count(User.id)).scalar() or 0,
                       'orgs_total':        db.query(func.count(Organisation.id)).scalar() or 0,
                       'sessions_week':     db.query(func.count(ConversationLog.id)).filter(ConversationLog.started_at >= week_ago).scalar() or 0,
                       'feedback_new':      db.query(func.count(Feedback.id)).filter(Feedback.status == 'new').scalar() or 0,
                       'feedback_planning': db.query(func.count(Feedback.id)).filter(Feedback.status == 'in_planning').scalar() or 0,
                       'audit_week':        db.query(func.count(AuditLog.id)).filter(AuditLog.created_at >= week_ago).scalar() or 0,
                   }
                   return self.render('admin/kpi_dashboard.html', stats=stats)
               finally:
                   db.close()

       class PlanningListView(BaseView):
           def is_accessible(self):
               return _is_superadmin()
           def inaccessible_callback(self, name, **kwargs):
               return redirect(url_for('auth.login'))
           @expose('/', methods=['GET','POST'])
           def index(self):
               db = get_session()
               try:
                   if request.method == 'POST':
                       link_id = int(request.form.get('id') or 0)
                       new_status = (request.form.get('status') or '').strip()
                       if link_id and new_status in ('backlog','active','done'):
                           link = db.query(PlanningFeedbackLink).get(link_id)
                           if link:
                               link.planning_status = new_status
                               db.commit()
                   links = db.query(PlanningFeedbackLink).order_by(PlanningFeedbackLink.id.desc()).limit(200).all()
                   return self.render('admin/planning_list.html', links=links)
               finally:
                   db.close()

       def register_admin_screenshot_route(app):
           from services.auth_decorators import superadmin_required
           @app.route('/admin/feedback/screenshot/<path:rel>')
           @superadmin_required
           def admin_feedback_screenshot(rel):
               # rel kommt als 'feedback/<uuid>.<ext>' aus DB
               if '..' in rel or rel.startswith('/'):
                   abort(400)
               # UPLOAD_DIR endet auf 'feedback' — also strip prefix
               name = rel.split('feedback/',1)[-1]
               return send_from_directory(UPLOAD_DIR, name)
       ```

    2. **app.py** — NACH dem `admin = Admin(...)` aus Plan 01:
       ```python
       from routes.admin_views import (
           UserAdmin, OrgAdmin, FeedbackAdmin, AuditLogAdmin, ConvLogAdmin,
           KpiDashboardView, PlanningListView, register_admin_screenshot_route,
       )
       from database.models import User, Organisation, Feedback, AuditLog, ConversationLog
       from database.db import db_session

       admin.add_view(KpiDashboardView(name='KPI', endpoint='kpi', url='/admin/kpi'))
       admin.add_view(PlanningListView(name='Planung', endpoint='planning', url='/admin/planning'))
       admin.add_view(FeedbackAdmin(Feedback, db_session, name='Feedback'))
       admin.add_view(UserAdmin(User, db_session, name='Users'))
       admin.add_view(OrgAdmin(Organisation, db_session, name='Orgs'))
       admin.add_view(ConvLogAdmin(ConversationLog, db_session, name='Sessions', category='Logs'))
       admin.add_view(AuditLogAdmin(AuditLog, db_session, name='Audit', category='Logs'))
       register_admin_screenshot_route(app)
       ```

    3. **templates/admin/kpi_dashboard.html** — Erbt vom Flask-Admin Master:
       ```html
       {% extends 'admin/master.html' %}
       {% block body %}
       <h2>NERVE KPI Dashboard</h2>
       <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:16px">
         <div class="card"><h3>{{ stats.users_total }}</h3><div>Users gesamt</div></div>
         <div class="card"><h3>{{ stats.orgs_total }}</h3><div>Organisationen</div></div>
         <div class="card"><h3>{{ stats.sessions_week }}</h3><div>Sessions (7d)</div></div>
         <div class="card"><h3>{{ stats.feedback_new }}</h3><div>Neue Feedbacks</div></div>
         <div class="card"><h3>{{ stats.feedback_planning }}</h3><div>In Planung</div></div>
         <div class="card"><h3>{{ stats.audit_week }}</h3><div>Audit-Events (7d)</div></div>
       </div>
       <style>.card{padding:18px;border:1px solid #e5e7eb;border-radius:10px;background:#fff}.card h3{margin:0 0 6px;font-size:28px;color:#00B894}</style>
       {% if stats.feedback_new > 0 %}
         <div style="margin-top:24px;padding:14px;border-radius:10px;background:#fff8e1;border:1px solid #f0c040">
           <strong>{{ stats.feedback_new }}</strong> neue Feedback-Tickets warten auf dich.
           <a href="{{ url_for('feedback.index_view') }}">Jetzt ansehen →</a>
         </div>
       {% endif %}
       {% endblock %}
       ```

    4. **templates/admin/planning_list.html**:
       ```html
       {% extends 'admin/master.html' %}
       {% block body %}
       <h2>Planungs-Liste</h2>
       <table class="table table-striped">
         <thead><tr><th>ID</th><th>Titel</th><th>Status</th><th>Erstellt</th><th>Aktion</th></tr></thead>
         <tbody>
         {% for link in links %}
           <tr>
             <td>{{ link.id }}</td>
             <td>{{ link.planning_title }}</td>
             <td>{{ link.planning_status }}</td>
             <td>{{ link.created_at }}</td>
             <td>
               <form method="post" style="display:flex;gap:6px">
                 <input type="hidden" name="id" value="{{ link.id }}">
                 <select name="status">
                   <option value="backlog" {% if link.planning_status=='backlog' %}selected{% endif %}>backlog</option>
                   <option value="active"  {% if link.planning_status=='active' %}selected{% endif %}>active</option>
                   <option value="done"    {% if link.planning_status=='done' %}selected{% endif %}>done</option>
                 </select>
                 <button class="btn btn-sm btn-primary" type="submit">Speichern</button>
               </form>
             </td>
           </tr>
         {% endfor %}
         </tbody>
       </table>
       {% endblock %}
       ```

    5. **templates/admin/feedback_notification.html** — Snippet das via JS oder Jinja-Include in master.html eingebunden werden kann. Für MVP: das Banner ist BEREITS im KPI-Dashboard `{% if stats.feedback_new > 0 %}` eingebaut. Diese Datei dient als zukünftige Wiederverwendung — minimaler Inhalt:
       ```html
       {% if stats and stats.feedback_new > 0 %}
       <div class="alert alert-warning">{{ stats.feedback_new }} neue Feedback-Tickets warten.</div>
       {% endif %}
       ```

    6. **routes/auth.py** — Im Signup/`_create_org_and_user` direkt nach erfolgreichem User-Create und commit:
       ```python
       try:
           from services.email_service import send_welcome
           send_welcome(user.email, getattr(user,'vorname','') or '')
       except Exception as e:
           print(f'[AUTH] welcome mail failed: {e}')
       ```

    7. **routes/oauth.py** — Im OAuth-Pfad an der Stelle wo ein NEUER User erstellt wird (nicht bei wiederkehrenden Logins):
       ```python
       try:
           from services.email_service import send_welcome
           send_welcome(user.email, getattr(user,'vorname','') or '')
       except Exception as e:
           print(f'[OAUTH] welcome mail failed: {e}')
       ```
  </action>
  <acceptance_criteria>
    - Datei `routes/admin_views.py` existiert
    - `grep -n "class FeedbackAdmin" routes/admin_views.py` findet 1 Treffer
    - `grep -n "class KpiDashboardView" routes/admin_views.py` findet 1 Treffer
    - `grep -n "class PlanningListView" routes/admin_views.py` findet 1 Treffer
    - `grep -n "class AuditLogAdmin" routes/admin_views.py` findet 1 Treffer
    - `grep -n "can_create = False" routes/admin_views.py` findet >=1 Treffer (Audit immutable)
    - `grep -n "send_feedback_in_planning" routes/admin_views.py` findet 1 Treffer
    - `grep -n "admin.add_view" app.py` findet >=6 Treffer
    - `grep -n "register_admin_screenshot_route" app.py` findet 1 Treffer
    - Templates existieren: `templates/admin/kpi_dashboard.html`, `templates/admin/planning_list.html`, `templates/admin/feedback_notification.html`
    - `grep -n "send_welcome" routes/auth.py` findet 1 Treffer
    - `grep -n "send_welcome" routes/oauth.py` findet 1 Treffer
    - Smoke: `python -c "import app; eps=[r.endpoint for r in app.app.url_map.iter_rules()]; assert 'admin_feedback_screenshot' in eps; print('OK')"`
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import app
eps = [r.endpoint for r in app.app.url_map.iter_rules()]
assert 'admin_feedback_screenshot' in eps
admin = app.app.extensions['admin'][0]
view_names = [v.name for v in admin._views]
for n in ['KPI','Planung','Feedback','Users','Orgs']:
    assert n in view_names, f'missing {n}: {view_names}'
print('OK')"</automated>
  </verify>
  <done>Admin-UI vollständig: ModelViews, KPI, Planung, Tickets mit Email-Trigger, Screenshot-Route, Welcome-Email bei Signup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle + funktionale Verifikation /admin + Resend EU + Email-Versand</name>
  <what-built>
    Komplette Admin-UI unter /admin mit KPI-Dashboard, Feedback-Tickets, Planungs-Liste, Audit-Log-View, Resend-Email-Versand für Welcome + Feedback-in-Planung, Screenshot-Delivery.
  </what-built>
  <how-to-verify>
    1. **Resend EU-Region prüfen (Pitfall 4):**
       - Login auf https://resend.com → Settings → "Data Region" muss "EU (Frankfurt)" zeigen.
       - Wenn US: umstellen ODER `RESEND_BASE_URL` auf EU-Endpoint setzen (Resend-Docs prüfen).
       - Domain `getnerve.app` muss verifiziert sein (DNS bereits 2026-04-08 gesetzt).
    2. **ENV-Vars auf Prod gesetzt?** `/etc/nerve/.env` enthält `RESEND_API_KEY`, `SUPERADMIN_EMAIL`.
    3. **Lokal starten:** `python app.py` → keine Tracebacks beim Boot.
    4. **Login als Superadmin** (Email == `SUPERADMIN_EMAIL`).
    5. **Browser:** http://localhost:5000/admin → Bootstrap4-UI sichtbar, Sidebar zeigt KPI / Planung / Feedback / Users / Orgs / Logs.
    6. **KPI-Dashboard:** Zahlen werden angezeigt (auch wenn 0).
    7. **Feedback erstellen:** im User-App den grünen Sidebar-Button → Modal → Bug + Text + (optional) PNG → Senden → Toast.
    8. **Admin → Feedback:** Eintrag taucht auf, Status = `new`.
    9. **Action "In Planung übernehmen"** auswählen → Confirm → Erfolgsmeldung.
    10. **Email-Inbox** des Test-Users prüfen → Feedback-in-Planung Mail kommt an (Absender feedback@getnerve.app).
    11. **Admin → Planung:** Eintrag erscheint, Status-Dropdown funktioniert.
    12. **Admin → Logs → Audit:** `feedback_in_planning` und `login` Einträge sichtbar. UPDATE-Versuch im UI führt zu Fehler (read-only).
    13. **Screenshot-Delivery:** Klick im Feedback-Detail auf Screenshot-Pfad → Bild lädt unter /admin/feedback/screenshot/feedback/<uuid>.<ext> nur für Superadmin.
    14. **Logout** → /admin → Redirect zu Login.
    15. **Login als normaler User** → /admin → 403.
    16. **Signup-Flow** mit Test-Email → Welcome-Mail kommt an (Absender noreply@getnerve.app).
    17. **Deploy-Notiz:** `deploy.sh` muss `/opt/nerve/uploads/feedback/` excluden — vor nächstem Deploy verifizieren.
  </how-to-verify>
  <resume-signal>"approved" oder konkrete Issues auflisten</resume-signal>
</task>

</tasks>

<verification>
- /admin lädt nur für Superadmin
- 6 Views registriert
- 3 Email-Templates triggerbar (Welcome auto bei Signup, Feedback-in-Planung via Action, Reset-Helper bereit)
- Audit-Log read-only im UI
- Screenshot-Delivery gegated
</verification>

<success_criteria>
- BE-06 + BE-08 erfüllt
- Resend EU verifiziert (Checkpoint)
- Email-Versand E2E getestet
- Ticket-Workflow new → in_planning → done funktioniert
</success_criteria>

<output>
.planning/phases/04.7-backend-feedback-system/04.7-05-SUMMARY.md
</output>
</content>
</invoke>