---
phase: "04.7"
plan: "06"
subsystem: ui
tags: [session-history, analytics, conversation-log, objection-events, flask]

dependency_graph:
  requires:
    - phase: 04.7-03
      provides: ObjectionEvent table mit conversation_log_id FK
  provides:
    - GET /analytics — Session-History Liste mit mode-Filter (ConversationLog)
    - GET /session/<id> — Session Detail-View mit ObjectionEvent Tabelle
  affects: [templates/analytics.html, templates/session_detail.html, routes/dashboard.py]

tech_stack:
  added: []
  patterns: [user-isolated-query, abort-404-guard, jinja2-filter-form]

key_files:
  created:
    - templates/analytics.html
    - templates/session_detail.html
  modified:
    - routes/dashboard.py

key_decisions:
  - "analytics_page() statt analytics() als Funktionsname — vermeidet Kollision mit bestehender /api/analytics JSON-Route"
  - "abort importiert in dashboard.py — Cross-User-Isolation via 404 fuer fremde Session-IDs"
  - "ConversationLog.id.desc().limit(200) fuer Analytics-Liste — neueste Sessions zuerst, Performance-Grenze"

patterns-established:
  - "User-Isolation: db.query(Model).filter(Model.user_id == g.user.id) — kein Cross-User-Zugriff"
  - "Session-Detail: abort(404) wenn conv nicht gefunden oder user_id != g.user.id"

requirements-completed: [BE-07]

duration: ~10min
completed: "2026-04-08"
---

# Phase 04.7 Plan 06: Session-History (Analytics-Umbau) Summary

**Analytics-Seite zur chronologischen Session-Liste umgebaut: /analytics zeigt ConversationLog-Tabelle mit Mode-Filter, neuer /session/<id> Detail-Screen rendert Session-Aggregate und ObjectionEvent-Liste im NERVE-Dark-Theme.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-08T11:30:00Z
- **Completed:** 2026-04-08T11:40:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- GET /analytics: chronologische Session-Liste aus ConversationLog, Filter nach cold_call/meeting/training, Limit 200
- GET /session/<int:sid>: Detail-View mit 8 Aggregat-Karten (Modus, Start, Ende, Dauer, Einwaende, Erfolg, Result, Typ) + ObjectionEvent-Tabelle
- Cross-User-Isolation: abort(404) wenn conv.user_id != g.user.id — kein fremder Datenzugriff moeglich
- Alte Chart-Widgets aus analytics.html entfernt (kein `new Chart`, kein Chart.js)

## Task Commits

1. **Task 1: /analytics Route + analytics.html** - `f7b23fb` (feat)
2. **Task 2: Session Detail Template** - `e6b15c1` (feat)

## Files Created/Modified

- `routes/dashboard.py` — analytics_page() Route + session_detail() Route + abort-Import hinzugefuegt
- `templates/analytics.html` — neue Datei: Session-History Liste mit Mode-Filter, n-table Stil
- `templates/session_detail.html` — neue Datei: Session-Aggregate + ObjectionEvent-Tabelle, n-grid/n-card Stil

## Decisions Made

- `analytics_page` als Funktionsname gewaehlt (nicht `analytics`) — verhindert Kollision mit bestehender `analytics()` JSON-API-Funktion im selben Blueprint
- `abort` aus flask importiert — fuer saubere 404-Guards in session_detail()
- `ConversationLog.id.desc().limit(200)` — neueste zuerst, sinnvolle Performance-Grenze ohne Pagination

## Deviations from Plan

None — Plan exakt wie spezifiziert umgesetzt. Einzige Abweichung: Funktionsname `analytics_page` statt `analytics` um Namenskollision mit bestehender JSON-Route zu vermeiden (kein funktionaler Unterschied, url_for/endpoint angepasst).

## Known Stubs

Keine. Alle Felder werden direkt aus ConversationLog und ObjectionEvent gerendert. Session-Detail zeigt "Noch keine Sessions." bzw. "Keine EWB-Klicks erfasst." als saubere Leer-States.

## Threat Flags

Keine neuen Threat-Surfaces.
- Cross-User-Isolation implementiert: `filter(CL.user_id == g.user.id)` in analytics_page, `abort(404)` in session_detail
- Kein Transkript angezeigt (DSGVO-konform)
- Kein fremder ObjectionEvent-Zugriff (conversation_log_id gehoert dem authentifizierten User)

## Self-Check

- [x] `templates/analytics.html` existiert
- [x] `templates/session_detail.html` existiert
- [x] `grep "Session History" templates/analytics.html` — Zeile 4
- [x] `grep -ni "new Chart" templates/analytics.html` — 0 Treffer
- [x] `grep "session_detail" routes/dashboard.py` — Zeile 635, 648
- [x] `grep "ConversationLog" routes/dashboard.py` — mehrere Treffer
- [x] `grep "from database.models import Session" routes/dashboard.py` — 0 Treffer
- [x] `grep "url_for.*session_detail" templates/analytics.html` — Zeile 27
- [x] `grep "einwand_typ" templates/session_detail.html` — Zeile 24
- [x] `grep "dashboard.analytics" templates/session_detail.html` — Zeile 4
- [x] Smoke: routes dashboard.analytics_page + dashboard.session_detail registriert — OK
- [x] Commit f7b23fb: Task 1
- [x] Commit e6b15c1: Task 2

## Self-Check: PASSED
