---
phase: "04.7"
plan: "01"
subsystem: "backend-auth"
tags: [superadmin, flask-admin, auth, migration, decorator]
dependency_graph:
  requires: []
  provides: [superadmin_flag, superadmin_required_decorator, db_session_scoped, flask_admin_mount]
  affects: [routes/auth.py, database/models.py, database/db.py, app.py]
tech_stack:
  added: [flask-admin==2.0.2, resend==2.27.0]
  patterns: [scoped_session, ENV-Seed, SecureIndexView auth gate]
key_files:
  created:
    - services/auth_decorators.py
  modified:
    - database/models.py
    - database/db.py
    - routes/auth.py
    - app.py
    - requirements.txt
    - .env.example
decisions:
  - "SecureIndexView uses module-aliased g/_redirect/_abort to avoid name clashes with app-level imports"
  - "ENV-Seed placed before db.commit() in _do_login — same transaction, atomic"
  - "routes/oauth.py absent from this branch — OAuth seed skipped, documented as deviation"
metrics:
  duration: "~15 min"
  completed: "2026-04-08T10:02:38Z"
  tasks_completed: 2
  files_changed: 6
---

# Phase 04.7 Plan 01: Superadmin Foundation Summary

**One-liner:** Superadmin-Flag mit DB-Migration, idempotenter ENV-Seed im Login, `@superadmin_required` Decorator, `scoped_session db_session` und Flask-Admin unter `/admin` mit Bootstrap4 SecureIndexView Auth-Gate.

## What Was Built

### Task 1 — Migration + scoped_session + Decorator (commit b91675f)

- `database/models.py`: `is_superadmin = Column(Boolean, default=False, nullable=False)` direkt nach `rolle` in `class User`
- `database/db.py`: `from sqlalchemy.orm import scoped_session` + `db_session = scoped_session(SessionLocal)` — export für Flask-Admin Views in Plan 02/05
- `app.py` (`_migrate()`): Idempotenter ALTER TABLE Block für `users.is_superadmin BOOLEAN DEFAULT 0` im bestehenden try/except-Stil
- `services/auth_decorators.py` (neu): `superadmin_required` Decorator — prüft `g.user.is_superadmin`, gibt 403 bei fehlendem Flag
- `requirements.txt`: `flask-admin==2.0.2` und `resend==2.27.0` gepinnt
- `.env.example`: `SUPERADMIN_EMAIL=` und `RESEND_API_KEY=` dokumentiert

### Task 2 — ENV-Seed + Flask-Admin Mount (commit 239c406)

- `routes/auth.py`: `import os` ergänzt; in `_do_login` vor `db.commit()`: idempotenter SUPERADMIN_EMAIL-Vergleich setzt `user.is_superadmin = True`
- `app.py`: `SecureIndexView(AdminIndexView)` + `Admin(app, name='NERVE Admin', template_mode='bootstrap4', index_view=SecureIndexView(url='/admin'))` nach allen Blueprint-Registrierungen
- Auth-Gate: Nicht eingeloggt → Redirect zu `auth.login`; eingeloggt aber kein Superadmin → 403

## Verification

- `grep -n "is_superadmin = Column" database/models.py` → Zeile 62
- `grep -n "db_session = scoped_session" database/db.py` → Zeile 27
- `grep -n "is_superadmin" app.py` → Migration in Zeile 172/174
- `grep -n "class SecureIndexView" app.py` → Zeile 711
- `grep -n "SUPERADMIN_EMAIL" routes/auth.py` → Zeile 72

## Deviations from Plan

### Auto-skipped (no route/module)

**[Rule 3 - Blocking] routes/oauth.py existiert nicht in diesem Branch**
- **Found during:** Task 2
- **Issue:** Der Plan fordert SUPERADMIN_EMAIL ENV-Seed auch in `routes/oauth.py`. Dieses Modul ist im Worktree `agent-aecc2c31` (Branch `worktree-agent-aecc2c31`) nicht vorhanden — OAuth wurde in Phase 04.6.1/04.6.2 implementiert, aber der Worktree basiert auf `a8ece70` (docs-only Commit vor diesen Änderungen? Nein — `git log` zeigt a8ece70 als letzten Stand VOR den Phase-04.7-Plänen, aber NACH Phase 04.6.x). Tatsächlich ist `oauth.py` nicht in der Blueprint-Registrierung von `app.py` zu finden und kein `oauth.py` in `routes/`.
- **Fix:** Schritt übersprungen. Wenn OAuth-Login in einem späteren Merge oder Deploy-Sync hinzugefügt wird, muss der gleiche ENV-Seed Block eingefügt werden.
- **Impact:** Gering — André nutzt primär Passwort-Login oder die OAuth-Route existiert nicht in diesem Zweig. Plan 02 kann trotzdem aufbauen.

### Import-Aliasing in SecureIndexView

**[Rule 2 - Missing critical functionality] Name-Clash zwischen flask.g/redirect/url_for/abort im Modul-Scope**
- `app.py` importiert bereits `from flask import Flask` global. Da SecureIndexView in `app.py` selbst definiert wird (nicht in einem Blueprint), wurden `g`, `redirect`, `url_for`, `abort` als `_g`, `_redirect`, `_url_for`, `_abort` aliasiert um saubere Trennung von potenziell überschriebenen Modul-Symbolen zu gewährleisten.

## Known Stubs

Keine — Flask-Admin `/admin` liefert das Standard-Bootstrap4-Index-Template (leer aber funktional). Plan 02/05 fügen ModelViews via `admin.add_view(...)` hinzu.

## Self-Check

- [x] `services/auth_decorators.py` exists: confirmed via `ls services/auth_decorators.py`
- [x] Commit `b91675f` exists: Task 1
- [x] Commit `239c406` exists: Task 2
- [x] `database/models.py` line 62: `is_superadmin` column
- [x] `database/db.py` line 27: `db_session = scoped_session(SessionLocal)`
- [x] `app.py` line 172: ALTER TABLE migration
- [x] `app.py` line 711: SecureIndexView class

## Self-Check: PASSED
