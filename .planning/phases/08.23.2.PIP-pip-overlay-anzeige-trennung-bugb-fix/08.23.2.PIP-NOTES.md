# Phase 08.23.2.PIP — Status & Richtungs-Update

## Richtungs-Update 2026-06-30 (André, kanonisch)
Quelle: `Nerve-Vault/03 Planung/NERVE_PiP_Overlay_Entscheidung_Umsetzung.md`, Block
"Entscheidungs-Update 2026-06-29/30".

**Cold-Call bekommt KEINE Auto-Erkennung von Einwaenden mehr.** NERVE hoert im Cold-Call
nur den Berater (Single-Speaker) → eine Auto-Erkennung reagiert nur auf die eigenen Worte
des Beraters = Selbst-Trigger-Rauschen. Modell Cold-Call: **Berater hoert Einwand → klickt
Knopf → liest Antwort.** Die Auto-Erkennung (Knopf-Highlight / neuer Knopf via `ewb_signal`
+ `keyword_einwand_match`) lebt **NUR im MEETING-Modus** (da hoert NERVE den Kunden). Das
Werkzeug aus PIP.1 bleibt — im Cold-Call abgeschaltet, im Meeting aktiv.

## Was gebaut + live ist
| Scheibe | Status | Commit(s) |
|---------|--------|-----------|
| **Plan 01** — Anzeige-Trennung (Lese-Zone nie ueberschrieben; Bug B Wurzel) | ✅ live + verifiziert (30.06.) | `5072409` `c3adb2e` `7a6ca48` |
| **Cold-Call Auto-Aus** (neu, Entscheidungs-Update) | 🟡 code-complete, live-verify offen | `f27df5a` |
| **Plan 03** — Zeitstempel-Fix C (ts_ms=0) | 🟡 code-complete, live-verify offen | `0cc3470` |
| **Plan 05** — [BUGB-EWB]-Messpunkte raus | 🟡 code-complete, live-verify offen | `bd9a0af` |

## Deferred (NICHT geloescht — Foundation fuers Meeting/spaeter)
| Plan | Warum deferred |
|------|----------------|
| **Plan 02** — Pin-Lifecycle + Selbst-Trigger-Gating | Cold-Call-Auto-Aus macht den Selbst-Trigger im Cold-Call gegenstandslos. Bleibt Foundation fuer Meeting (Auto-Erkennung lebt dort). FE-Hook `_applyBufferedAutoSignal()` + `state.pinActive`-Gating aus Plan 01 bereits vorbereitet. |
| **Plan 04** — Multifire-Decision | Niedrig-Prio + durch Auto-Aus gegenstandslos im Cold-Call. `click_id` (de-tagged `_clickId`) im manual_ewb-Payload erhalten → FE-Dedup-Hebel bleibt fuer Meeting-Multifire. |

## Offene Live-Verifikation (André, ein Deploy)
`bash deploy.sh production` → Cold-Call mit Test-User:
1. **Cold-Call Auto-Aus:** Beim Sprechen erscheint KEIN Auto-Knopf-Highlight / kein Auto-
   Slot-0-Render mehr (nur eigener Knopf-Klick zeigt Antwort). (Meeting bleibt: volle Auto-Erkennung.)
2. **Plan 03:** `inspect.sh sample transcript_segments 50` → Knopf-Segmente `ts_ms != 0`,
   keine ~17h-Folgezeit. (MP6-Log entfaellt — via DB pruefen, MP6 wurde in Plan 05 entfernt.)
3. **Plan 05:** `inspect.sh logs 300` bei Cold-Call → kein `[BUGB-EWB]`-Rauschen; App normal.

Nach gruenem Deploy + ok Verifikation ist die PiP-Baustelle **fuer jetzt sauber zu**
(Cold-Call-first). Geparkt/spaeter (eigene Scheiben, TAXO3/PIP.4-gegated): PIP.2 (ein
Fenster + Coaching-Symbole), Namens-Slot + Auto-Namens-Bestaetigung, Live-Transkript-
Antworten, Vorgenerierung.
