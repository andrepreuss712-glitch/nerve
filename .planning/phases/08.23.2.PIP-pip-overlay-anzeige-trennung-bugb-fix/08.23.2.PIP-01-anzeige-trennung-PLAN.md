---
phase: 08.23.2.PIP
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/claude_service.py
  - services/deepgram_service.py
  - static/pip-launcher.js
autonomous: false
requirements: [a, d]
complexity: 🟡
cross_ai_required: true

must_haves:
  truths:
    - "Auto-Erkennung (keyword / analyse_loop / coldcall_infer) erscheint NUR als Button-Highlight oder neuer Button, NIE als Text in der Lese-Zone (pip-slot-body-1)."
    - "Ein laufender manueller Knopf-Stream wird von einem gleichzeitigen Auto-Signal NICHT mehr im Vorlesen ueberschrieben."
    - "Coaching-Text (Phase/Kaufbereitschaft/Tipp) erscheint nicht mehr in der Lese-Zone."
    - "Die Coaching-SIGNAL-Datenpfade bleiben intakt: Berechnung + Socket-Emits + die pip_token_done-Felder (result.phase / result.kb fuer Phase/Kaufbereitschaft/Tempo/Redeanteil) kommen WEITER im Frontend an (PIP.2-Foundation erhalten — NUR die Text-Ausgabe in slot 1 wird gestoppt, NICHT die Daten-Pipeline)."
    - "Newest-wins: mehrere Auto-Signale kurz hintereinander hinterlassen nur das juengste Highlight, kein Stapeln."
    - "ewb_signal traegt NUR ein kurzes Klassifikations-Label (_kat) als typ, NIE einen ganzen Antwort-Absatz; ohne sauberes Kurz-Label wird gar kein Signal emittiert."
    - "ewb_signal traegt NIE einen NON-AKTIONS-Roh-Enum-Wert (smalltalk_none / einwand_known / frage / einwand_unknown) als typ — diese erzeugen keinen Geister-Button (Cross-AI LOW)."
    - "Vor dem Kalt-Stellen von streame_auto_variante ist per grep belegt, dass kein anderer Produktiv-Pfad den TTFT-Circuit-Breaker / das Cost-Tracking dieses Moduls liest oder setzt (Cross-AI MEDIUM #1)."
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "FE: source-getrennte Stream-Verarbeitung + Button-Signal-Handler (highlight/new-button) + Newest-wins-Puffer Groesse 1; Auto-Quellen schreiben NIE pip-slot-body-1; Coaching-Text-Schreiben in slot 1 geguarded — _showProactiveContent/_showProactiveTipp-Definitionen + ihre Datenversorgung (result.phase/result.kb) BLEIBEN (PIP.2-Foundation)"
      contains: "ewb_signal"
    - path: "services/claude_service.py"
      provides: "BE: Auto-Pfad (streame_auto_variante, qa_slot1/qa_soft_hint) emittiert Button-Signal (typ=_kat-Label) statt Lese-Zonen-Text; manual-Pfad traegt source=manual_button. Coaching-Signal-Compute/-Emit (phase/kb, slot-0-Analyse-Pfad) BLEIBT unangetastet"
      contains: "ewb_signal"
    - path: "services/deepgram_service.py"
      provides: "BE: Keyword-Pfad ruft kein Lese-Zonen-Schreiben mehr an, sondern Button-Signal"
  key_links:
    - from: "services/claude_service.py streame_auto_variante / _emit_qa_slot1 / _emit_soft_hint"
      to: "FE ewb_signal-Handler"
      via: "sio.emit('ewb_signal', {'typ': _kat, ...})"
      pattern: "ewb_signal"
    - from: "static/pip-launcher.js pip_stream_start/pip_token"
      to: "pip-slot-body-1 (Lese-Zone)"
      via: "source-Gate: nur source=manual_button darf schreiben"
      pattern: "source.*manual_button"
    - from: "slot-0-Analyse-Pfad (result.phase / result.kb)"
      to: "FE _showProactiveContent / _showProactiveTipp (PIP.2-Foundation)"
      via: "pip_token_done.result.phase / .kb (Daten-Pipeline bleibt; nur slot-1-Text-Ausgabe geguarded)"
      pattern: "_showProactiveContent"
---

<objective>
Strukturelle Eingabe/Ausgabe-Trennung: der Auto-Erkenner bekommt KEINEN Schreibzugriff mehr auf die Lese-Zone (`pip-slot-body-1`). Er darf nur noch einen bekannten Button aufleuchten lassen (Highlight) oder einen neuen Button erzeugen. Damit ist "Antwort wird im Vorlesen ueberschrieben" nicht gemanagt, sondern strukturell unmoeglich (Kernregel der Phase). Deckt Scope-Item (a) ab und stoppt zugleich Coaching-Text in der Lese-Zone (Item d, Rest-Pfad `_showProactiveContent`) — wobei NUR die Text-Ausgabe in slot 1 gestoppt wird, die Coaching-Signal-Datenpipeline aber INTAKT bleibt (PIP.2-Foundation).

Purpose: Bug B (Ueberschreiben im Vorlesen) an der Wurzel kappen — nicht das Symptom im FE kaschieren, sondern den Auto-Schreibpfad in die Lese-Zone abschneiden. Der Lock-Streit (slot1_variant_busy_until in 2 Stores) wird dadurch fuer die Lese-Zone obsolet, weil Auto dort gar nicht mehr schreibt.

Output: source-getrennte Stream-Pipeline (manual vs auto), neuer `ewb_signal`-Kanal (highlight known / create new), Newest-wins-Puffer Groesse 1, Coaching-Text raus aus der Lese-Zone bei erhaltener Coaching-Daten-Pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md
@.planning/debug/ewb-pip-overwrite-multifire.md
@.planning/04 Entscheidungen/Foundation-Code-Register.md
@CLAUDE.md

# Kanonische Design-Vorgabe (Pflicht-Pre-Read, dagegen bauen):
# C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\NERVE_PiP_Overlay_Entscheidung_Umsetzung.md

<interfaces>
<!-- Verifizierte Vertraege (aus dem Code gelesen, Executor nutzt diese direkt — keine erneute Code-Suche noetig). -->

FE — die EINZIGEN Schreiber von pip-slot-body-1 (Lese-Zone), verifiziert:
  static/pip-launcher.js
  - pip_stream_start (2309) / pip_token (2351) / pip_token_done (2367): raw_text-Stream in slot=d.slot.
      BEIDE BE-Streamer (manual + auto) nutzen DIESELBEN Events mit raw_text:true, slot 1 →
      FE kann sie HEUTE nicht unterscheiden. Hier wird die source-Trennung eingefuehrt.
  - qa_slot1-Handler (2494): body.textContent = txt  (Quelle: analyse_loop/_emit_qa_slot1) → AUTO
  - qa_soft_hint-Handler (2523): body.textContent = txt  (Quelle: _emit_soft_hint) → AUTO
  - _showProactiveContent (2754) / _showProactiveTipp (2773): schreibt Phase/Kaufbereitschaft/Tipp
      in pip-slot-body-(1-slot). Aufruf in pip_token_done (2408) → kann slot 1 treffen → COACHING-TEXT.
      WICHTIG (André-Direktive): NUR diese Text-Ausgabe in slot 1 wird gestoppt (Guard). Die Datenversorgung
      (result.phase / result.kb aus dem pip_token_done-Payload) BLEIBT — das ist die PIP.2-Foundation.

FE — Button-Zone + Helfer, verifiziert:
  - nlp-ewb-row (Container der EWB-Buttons), Buttons: <button class="pip-ewb-btn" data-typ="LABEL">
  - _highlightEwbButton(typ) (2746): toggelt Klasse 'pip-ewb-ai-selected' am Button mit data-typ===typ
  - _renderEwbButtons (2213): rendert Buttons aus state.profileDaten.einwaende_detail/einwaende
  - _triggerEwb(typ, btn) (2247): manual-Klick → _renderSlotResult(0, fake) + emit('manual_ewb', {slot:1,...})

BE — Auto-Schreiber (slot 1), verifiziert:
  services/claude_service.py
  - streame_auto_variante(neuer_text, einwaende, kontext, sid, slot=1, trigger) (577):
      emit pip_stream_start (646) / pip_token (673) / pip_token_done (686), alle raw_text:true → AUTO.
      WICHTIG (grep-verifiziert 2026-06-29): das pip_token_done-result hier ist IMMER
      {'einwand': True, 'typ': 'AUTO', ...} (Z.685) → traegt NIE result.phase/result.kb →
      fuettert das FE-Coaching (`if !d.result.einwand`, FE:2407) NICHT. Pruning dieses Pfads
      kappt die Coaching-Daten NICHT (siehe Task 1 "Coaching-Signal-Erhalt").
      ENTHAELT modul-globalen TTFT-Circuit-Breaker + Cost-Tracking (siehe ewb-circuit-breaker-Block unten) →
      vor dem Kalt-Stellen Audit-pflichtig (Cross-AI MEDIUM #1).
  - _qa_pipeline_dispatch._emit_qa_slot1 (1645): emit('qa_slot1',{text}) + setzt slot1_variant_busy_until → AUTO
      ACHTUNG: wird HEUTE mit `_antwort_text` (voller Haiku-Absatz) aufgerufen — NICHT als ewb_signal-typ verwenden!
      Das kurze Klassifikations-Label liegt in der Closure-Variable `_kat` von `_qa_pipeline_dispatch`.
  - _qa_pipeline_dispatch._emit_soft_hint (1576): emit('qa_soft_hint',{text}) → AUTO
      ACHTUNG: emittiert HEUTE lange Literal-Strings ("Neuer Einwand — noch kein Vorschlag") — NIE als typ verwenden!
  - streame_manual_ewb_variante(typ, profile_einwand, kontext, sid, slot=1) (757):
      emit pip_stream_start (836) / pip_token (856) / pip_token_done (873), raw_text:true → MANUAL (der Pin).
      result hier ist ebenfalls IMMER {'einwand': True, 'typ': typ, ...} (Z.872) → traegt KEIN phase/kb.
  services/deepgram_service.py
  - Keyword-Pfad (255-269): start_background_task(streame_auto_variante, ..., "keyword") → AUTO
  - coldcall_infer (claude_service 472-510): inferiert Einwand-Typ, schreibt NICHT direkt slot1 (nur Erkennung);
      sein Ergebnis fuettert analyse_loop → qa/streame_auto. KEIN eigener Lese-Zonen-Write.

BE — `_kat`-Enum (Klassifikations-Label), verifiziert (grep 2026-06-29) — Quelle des ewb_signal-typ:
  services/qa_pipeline.py:324 + app.py:806 — `_kat` ist EXAKT eines von vier Werten:
    'einwand_unknown' | 'frage' | 'smalltalk_none' | 'einwand_known'  (Fallback default: 'smalltalk_none').
  - `einwand_known` : bekannter Einwand (Keyword-Pfad/Profil hat dafuer bereits einen Button) → HIGHLIGHT, NIE neuer Button namens "einwand_known".
  - `einwand_unknown` : neuer/unbekannter Einwand → der ROH-Enum-Wert 'einwand_unknown' ist KEIN echtes Einwand-Label, sondern ein Platzhalter → darf NIE woertlich als Button-Label rausgehen.
  - `frage` / `smalltalk_none` : NON-AKTIONS-Kategorien → KEIN ewb_signal (kein Button). claude_service.py:1736 belegt: "smalltalk_none / einwand_known → no action (Slot bleibt wie es ist)".
  → Konsequenz (Cross-AI LOW): KEINER dieser vier Roh-Enum-Werte darf als `ewb_signal`-`typ` gesendet werden. Siehe Blocklist-Guard Punkt 3/4 + Sub-Step "non-action-blocklist".

BE — TTFT-Circuit-Breaker + Cost-Tracking in streame_auto_variante, verifiziert (grep 2026-06-29) — Audit-Objekt fuer MEDIUM #1:
  services/claude_service.py
  - `_ewb_ttft_history` (deque maxlen=5, Z.18) : modul-global. GESETZT/GELESEN nur in streame_auto_variante (append 662, read 664).
  - `_ewb_fallback_until` (float, Z.19) : modul-global. `global`-deklariert in streame_auto_variante (595); GESETZT 668; GELESEN 591(Kommentar)/639 (`_cb_in_fallback`).
  - `_ewb_circuit_lock` (threading.Lock, Z.20) : nur in streame_auto_variante benutzt (638/661).
  - `_cache_writes` (Cost-Tracking, Z.715) : lokal in streame_auto_variante; weitere _cache_writes-Vorkommen (qa_pipeline.py:511, claude_service.py:559/891) sind ANDERE Funktionen mit EIGENEM Tracking (NICHT von diesem Modul abhaengig).
  - grep-Stand 2026-06-29: ausserhalb streame_auto_variante liest/setzt KEIN Produktiv-Pfad `_ewb_ttft_history`/`_ewb_fallback_until` (nur tests/test_ewb_autovar_global_regression.py referenziert sie). → Audit-Hypothese: Circuit-Breaker ist self-contained, keine externen Abhaengigen. Der Executor MUSS diesen grep-Stand auf dem deployten Code RE-verifizieren (Sub-Step unten).

BE — Coaching-Signal-Compute (phase/kb), verifiziert (grep 2026-06-29) — NICHT von Task 1 angefasst:
  services/claude_service.py
  - `phase` wird im slot-0-Analyse-Pfad gesetzt (analysiere_mit_claude: phase=int(data.get('phase',...)) Z.418-423).
  - Kaufbereitschaft/readiness: compute_readiness_score (Z.1339), ls.kaufbereitschaft / score_p4 (Z.1407-1419), kb_aktuell.
  - DIESE Compute-/Emit-Stellen liegen NICHT in den vier von Task 1 geprunten Auto-Lese-Zonen-Schreibpfaden
    (streame_auto_variante / _emit_qa_slot1 / _emit_soft_hint / Keyword-Pfad). Sie bleiben unberuehrt.
  - Der frueher separate coaching_loop-WebSocket-Emit (der _showProactiveTipp fuetterte) wurde bereits in
    Phase 06.6 entfernt (claude_service.py:1883-1889). Verbleibender Coaching-Text-in-slot-1-Pfad = NUR
    _showProactiveContent(1-slot, d.result) @ FE:2408 mit result.phase/result.kb.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BE — Auto-Pfad emittiert Button-Signal (typ=_kat-Label) statt Lese-Zonen-Text; manual-Pfad bekommt source=manual_button</name>
  <files>services/claude_service.py, services/deepgram_service.py</files>
  <read_first>
    - services/claude_service.py 577-700 (streame_auto_variante: alle drei emits pip_stream_start/pip_token/pip_token_done — result IMMER einwand:True, KEIN phase/kb)
    - services/claude_service.py 15-25 + 591-670 (TTFT-Circuit-Breaker: _ewb_ttft_history:18, _ewb_fallback_until:19, _ewb_circuit_lock:20, global-Deklaration 595, set/read 638/639/661/664/668 — Audit-Objekt MEDIUM #1)
    - services/claude_service.py 1500-1672 (_qa_pipeline_dispatch: die `_kat`-Klassifikations-Closure-Variable + _emit_qa_slot1 + _emit_soft_hint, inkl. slot1_variant_busy_until-Set) — HIER verifizieren wo `_kat` gesetzt wird und ob es im Scope der beiden _emit_*-Helfer liegt
    - services/qa_pipeline.py 320-325 (das `_kat`-Enum: einwand_unknown|frage|smalltalk_none|einwand_known + Fallback smalltalk_none) — Quelle der Blocklist (Cross-AI LOW)
    - services/claude_service.py 757-880 (streame_manual_ewb_variante: die drei emits — bleibt der einzige Lese-Zonen-Schreiber; result IMMER einwand:True, KEIN phase/kb)
    - services/claude_service.py 410-425 + 1330-1430 (Coaching-Signal-Compute: phase / readiness_score / kaufbereitschaft — NICHT anfassen, nur verifizieren dass sie ausserhalb der vier Prune-Pfade liegen)
    - services/deepgram_service.py 244-272 (Keyword-Pfad: liest GLOBAL ls.state busy_until, ruft streame_auto_variante "keyword")
    - .planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md <decisions> (a) + Kernregel
    - Nerve-Vault/03 Planung/NERVE_PiP_Overlay_Entscheidung_Umsetzung.md Abschnitt "Auto-Erkenner: bekannt vs. neu" + Checkliste Punkt 1
    - CLAUDE.md Punkt 14 (Control-Flow + 4 Race-Fragen), Punkt 17 (kein Refactor-Beifang), Punkt 25 (Latenz), Punkt 27 (einfachster tragfaehiger Weg)
    - Live-State (HART, Kein-Local-Dev): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ewb_signal"'` (muss VOR dem Edit 0 Treffer zeigen — neuer Event-Name), + `inspect.sh grep "_kat"` (verifizieren wo die Closure-Variable real lebt im deployed Stand), + `inspect.sh grep "_ewb_fallback_until"` und `inspect.sh grep "_ewb_ttft_history"` (Circuit-Breaker-Reader-Audit, MEDIUM #1)
  </read_first>
  <action>
    <executor_warnings>
    <!-- VIER BLOCKIERENDE PFLICHT-REGELN (Cross-AI HIGH #1/#2 + MEDIUM #1 + LOW Geister-Button) — VOR dem Implementieren lesen. -->
    <!-- Die ausfuehrlichen Details stehen unten in Punkt 3 / Punkt 4 / den Sub-Steps; dieser Block ist die Vorab-Zusammenfassung, damit nichts uebersehen wird (NICHT ersatzweise — beide Stellen gelten). -->

    **HIGH #1 (Label-vs-Text-Falle, BLOCKING):** `_emit_qa_slot1` MUSS als `ewb_signal`-Payload `typ=_kat` (das KURZE Klassifikations-Label, z.B. 'zu_teuer') senden — NIEMALS den vollen `text`/`_antwort_text`-Absatz. Der `text`-Parameter wird KOMPLETT verworfen. Konsequenz bei Verstoss: die FE (Task 2) rendert einen Paragraph-grossen Button UND die `data-typ`-Delegation bricht.

    **HIGH #2 (Soft-Hint-Langstring-Falle, BLOCKING):** `_emit_soft_hint` MUSS bei fehlendem sauberen Kurz-Label GAR KEIN Signal emittieren (`return`/`pass`) → kein Geister-/Hint-Text-Button. Konsequenz bei Verstoss: die FE rendert einen Button mit dem literalen Hint-Text ("Neuer Einwand — noch kein Vorschlag" o.ae.), und ein Klick schickt diesen ganzen String als Einwand-Typ an die LLM. NUR bei echtem Kurz-Label (`_kat`) darf `ewb_signal` mit `known=False` gehen.

    **Cross-AI LOW (Geister-Button non-action categories, BLOCKING):** `_kat` ist NICHT nur None/leer, sondern traegt definierte NON-AKTIONS-Roh-Enum-Werte: `smalltalk_none`, `frage`, `einwand_known`, `einwand_unknown` (qa_pipeline.py:324 + app.py:806). KEINER dieser vier Roh-Werte darf je als `ewb_signal`-`typ` rausgehen — sonst entsteht ein Button, der WOERTLICH `smalltalk_none` / `einwand_unknown` / `frage` heisst. Harte Blocklist-Guard (`return`/`pass` vor dem Emit) fuer diese Werte. Details: Sub-Step "non-action-blocklist" + Punkt 3/4.

    **Cross-AI MEDIUM #1 (Circuit-Breaker-Audit, BLOCKING vor dem Kalt-Stellen):** `streame_auto_variante` traegt den modul-globalen TTFT-Circuit-Breaker (`_ewb_ttft_history`, `_ewb_fallback_until`, `_ewb_circuit_lock`) + Cost-Tracking (`_cache_writes`). Bevor dieser Pfad als toter Auto-Lese-Zonen-Schreiber markiert / nicht mehr aufgerufen wird, MUSST du per grep BEWEISEN, dass kein anderer Produktiv-Pfad diese Symbole liest/setzt. Details: Sub-Step "ewb-circuit-breaker-audit".

    Merksatz HIGH #1/#2 + LOW: `typ` ist IMMER ein ECHTES kurzes Einwand-Label ODER kein Signal — niemals ein Absatz, niemals ein Literal-Hint-String, niemals ein Roh-Enum-Wert (smalltalk_none/frage/einwand_known/einwand_unknown).
    </executor_warnings>

    <coaching_signal_erhalt>
    <!-- GUARD-RAIL (André-Direktive 2026-06-29, PFLICHT, grep-belegt) — gegen Ueber-Prunen. -->
    **Coaching-Signal-Erhalt (Anti-Abrieb, PIP.2-Foundation):** Das Prunen der Auto-Lese-Zonen-Schreibpfade (streame_auto_variante / _emit_qa_slot1 / _emit_soft_hint / Keyword-Pfad) darf die **Coaching-Signal-Berechnung + -Emits (Phase / Kaufbereitschaft / Tempo / Redeanteil)** NICHT mit-entfernen. Grep-Beleg (Punkt 20/22), warum kein Konflikt besteht:
    - `streame_auto_variante` (claude_service.py:686) UND `streame_manual_ewb_variante` (873) emittieren ihr `pip_token_done`-result IMMER mit `{'einwand': True, ...}` (Z.685/872) → sie tragen NIE `result.phase`/`result.kb` → sie fuettern das FE-Coaching (`if (!d.result.einwand)`, pip-launcher.js:2407) GAR NICHT.
    - Die Coaching-Daten (`phase`, `kb`/`kaufbereitschaft`/`readiness_score`) werden im **slot-0-Analyse-Pfad** berechnet: `phase` in analysiere_mit_claude (claude_service.py:418-423), Kaufbereitschaft via `compute_readiness_score` (1339) / `ls.kaufbereitschaft` / `score_p4` (1407-1419). **Diese Stellen liegen AUSSERHALB der vier von Task 1 geprunten Pfade.**
    - Folge: **Task 1 entfernt KEINEN phase/kb-Compute und KEINEN phase/kb-Emit.** Der Executor prunt ausschliesslich die Auto→slot-1-*Text*-Schreibwege. Falls der Executor beim Umbau von streame_auto_variante / dem analyse_loop auf eine Stelle stoesst, die `phase`/`kb`/`kaufbereitschaft`/`readiness_score` berechnet oder emittiert → **STEHEN LASSEN** (das ist Coaching-Daten, nicht Lese-Zonen-Text). Im Zweifel `inspect.sh grep "result.phase"` / `inspect.sh grep "result.kb"` und nachweisen, dass der phase/kb-fuehrende pip_token_done weiterhin existiert.
    </coaching_signal_erhalt>

    <ewb-circuit-breaker-audit>
    <!-- REMOVAL-AUDIT (Cross-AI MEDIUM #1, PFLICHT vor dem Kalt-Stellen von streame_auto_variante) — schlank halten (Punkt 27): grep + dokumentierte Entscheidung, KEIN neuer Mechanismus. -->
    **Circuit-Breaker-Removal-Audit (Cross-AI MEDIUM #1):** `streame_auto_variante` traegt einen modul-globalen TTFT-Circuit-Breaker + Cost-Tracking, der beim Kalt-Stellen (Pfad wird nicht mehr aufgerufen / als tot markiert, Punkt 2) ebenfalls erlischt. Inhaltlich gewollt (keine Haiku-Generierung mehr → keine TTFT-Messung, keine Tokens noetig). ABER vor dem Kalt-Stellen ist zu BEWEISEN, dass kein anderer Produktiv-Pfad auf diese Schutz-/Mess-Logik angewiesen ist (sonst geht still eine erwartete Funktion verloren).

    Pflicht-Schritte (grep-belegt, gegen den deployten Prod-Stand — Kein-Local-Dev):
    1. `inspect.sh grep "_ewb_fallback_until"` und `inspect.sh grep "_ewb_ttft_history"` und `inspect.sh grep "_ewb_circuit_lock"` ausfuehren. Erwartung (grep-Stand 2026-06-29): JEDER Treffer liegt entweder (a) innerhalb der Definition/dem Body von `streame_auto_variante` in claude_service.py ODER (b) in tests/test_ewb_autovar_global_regression.py. KEIN dritter Produktiv-Leser/-Schreiber.
    2. Falls die Erwartung haelt (0 externe Produktiv-Reader): `streame_auto_variante` darf kalt gestellt werden. Der dormante Circuit-Breaker hat dann keine lebenden Abhaengigen. **Eintrag ins Foundation-Code-Register** (`.planning/04 Entscheidungen/Foundation-Code-Register.md`): "streame_auto_variante (inkl. TTFT-Circuit-Breaker _ewb_fallback_until/_ewb_ttft_history + Cost-Tracking) = PIP.4-Foundation, write-only/dormant, KEINE externen Produktiv-Reader (grep-belegt 2026-…)". Damit ist es kein Zombie-Loeschkandidat, sondern markierte Foundation (CLAUDE.md Punkt 23 Zombie-/Foundation-Regel).
    3. Falls die Erwartung NICHT haelt (ein externer Pfad liest `_ewb_fallback_until` o.ae.): **STOP** — die Circuit-Breaker-Logik darf NICHT kalt gestellt werden, ohne den Abhaengigen zu versorgen. Dokumentiere die gefundene Abhaengigkeit im SUMMARY + entscheide bewusst (Punkt 27): entweder streame_auto_variante als reinen Circuit-Breaker-Halter weiterlaufen lassen (ohne Lese-Zonen-Emit) ODER den Abhaengigen separat anpassen (eigener Punkt, nicht hier blind mit-prunen).
    4. `test_ewb_autovar_global_regression.py` ist ein Regressions-Test fuer den `global _ewb_fallback_until`-Bug. Wenn streame_auto_variante kalt gestellt aber NICHT geloescht wird (Foundation), bleibt der Test gueltig (Funktion + Circuit-Breaker existieren weiter, nur ungenutzt). Wenn die Funktion entfernt wuerde (NICHT empfohlen — Foundation behalten), muesste der Test mitgehen — in diesem Plan NICHT loeschen.

    Dieser Audit ist eine grep-Verifikation + eine dokumentierte Entscheidung — KEIN neuer Mechanismus (Punkt 27). Acceptance dazu unten.
    </ewb-circuit-breaker-audit>

    <non-action-blocklist>
    <!-- HARTE GEISTER-BUTTON-GUARD (Cross-AI LOW, PFLICHT) — Roh-Enum-Werte duerfen nie als typ rausgehen. -->
    **Non-Action-Category-Blocklist (Cross-AI LOW, Geister-Button):** Das `_kat`-Enum hat EXAKT vier Werte (qa_pipeline.py:324, grep-belegt 2026-06-29): `einwand_unknown` | `frage` | `smalltalk_none` | `einwand_known`. KEINER dieser ROH-Werte ist ein echtes, anzeigbares Einwand-Label — sie sind Klassifikations-Kategorien. Wuerde einer davon als `ewb_signal`-`typ` rausgehen, entstuende FE-seitig ein Button, der woertlich `smalltalk_none` / `einwand_unknown` / `frage` / `einwand_known` heisst (Geister-Button).

    Regel pro Kategorie (im Emit-Pfad VOR jedem `ewb_signal`-Emit anzuwenden):
    - `smalltalk_none` : NON-AKTION → KEIN ewb_signal (`return`/`pass`). Belegt durch claude_service.py:1736 ("smalltalk_none / einwand_known → no action").
    - `frage` : NON-AKTION fuer die EWB-Button-Zone → KEIN ewb_signal (kein Einwand-Button fuer eine Frage).
    - `einwand_known` : bekannter Einwand → es existiert bereits ein Profil-/Keyword-Button → HIGHLIGHT des bestehenden Buttons (known=True), ABER NIE einen NEUEN Button mit Label `einwand_known`. Der highlight-`typ` muss das ECHTE Einwand-Label/Keyword sein (z.B. `match['keyword']` aus dem Keyword-Pfad), NICHT der Roh-Enum-Wert `einwand_known`.
    - `einwand_unknown` : neuer Einwand, aber der Roh-Wert `einwand_unknown` ist ein PLATZHALTER, kein anzeigbares Label → NIE woertlich `einwand_unknown` als `typ`. Nur wenn ein konkretes Kurz-Label fuer den neuen Einwand vorliegt (z.B. ein klassifizierter Kurz-Typ aus dem einwand_zitat / der Keyword-Erkennung), darf ein neuer Button mit DIESEM Label entstehen (known=False). Liegt KEIN konkretes Kurz-Label vor → KEIN Signal (`return`/`pass`).

    Praktische Form (einfachster tragfaehiger Weg, Punkt 27): ein expliziter Blocklist-Check
    `if _kat in ('smalltalk_none', 'frage', 'einwand_known', 'einwand_unknown') or not _kat: <kein-neuer-Button-Pfad>`
    — wobei `einwand_known` in den HIGHLIGHT-Pfad (echtes Label) und die anderen in den `return`/`pass`-Pfad gehen. KEIN Roh-Enum-Wert verlaesst je die Funktion als `typ`. (Falls spaeter ein feineres Einwand-Taxonomie-Label verfuegbar ist, ist das die echte typ-Quelle — aber das ist NICHT Scope dieser Phase; hier gilt: lieber kein Button als ein Geister-Button.)
    </non-action-blocklist>

    Ziel: Auto darf die Lese-Zone (slot 1 raw_text-Stream + qa_slot1 + qa_soft_hint) NICHT mehr fuettern. Statt Text-in-Lese-Zone emittiert Auto ein neues Button-Signal `ewb_signal`. **Die Coaching-Signal-Pipeline (phase/kb) bleibt unberuehrt (siehe `coaching_signal_erhalt` oben).**

    1. Neues Emit-Schema (EIN Event, von allen Auto-Quellen genutzt):
       `sio.emit('ewb_signal', {'typ': <echtes_kurzes_einwand_label>, 'known': <bool>, 'source': <'keyword'|'analyse_loop'|'coldcall_infer'>}, room=sid)`
       - `typ` = ECHTES kurzes erkanntes Einwand-Label (z.B. 'zu_teuer' / `match['keyword']`) wenn vorhanden, sonst ein kurzes anonymisiertes Kurz-Label fuer einen NEUEN Button. **NIEMALS ein ganzer Antwort-Absatz, NIEMALS ein langer Literal-/Hint-String, NIEMALS ein Roh-Enum-Wert** (smalltalk_none/frage/einwand_known/einwand_unknown) (siehe HARTE ANWEISUNGEN unten, Cross-AI HIGH #1/#2 + LOW Geister-Button).
       - `known` = True wenn `typ` einem bestehenden Profil-Einwand entspricht (Button existiert), sonst False (neuer Button).
       - Die known/neu-Entscheidung trifft die FE (Task 2) anhand der gerenderten Buttons; BE liefert `typ` + best-effort `known`-Hinweis. Bei Unsicherheit `known=None` → FE entscheidet.
       - **INVARIANTE (W-2, Single Source of Truth): Die FE ist die alleinige Autorität über den Lese-Zonen-Pin/Lock und die Button-Anzeige.** Das BE-`known` ist ausschliesslich BERATEND (advisory) — es darf das FE-Urteil NIE ueberstimmen. Findet die FE einen passenden gerenderten Button, gewinnt `known=True` (re-highlight bestehender Button), AUCH wenn das BE `known=False` schickte; findet sie keinen, erzeugt sie einen neuen Button, AUCH wenn das BE `known=True` schickte. Der Executor darf diese Praezedenz NICHT invertieren (BE-`known` ist kein Override des FE-DOM-Abgleichs). Begruendung: nur die FE kennt den real gerenderten Button-Zustand; das BE kennt nur das Profil-Modell und kann driften.

    2. `streame_auto_variante` (claude_service.py:577): die DREI Lese-Zonen-emits (pip_stream_start:646, pip_token:673, pip_token_done:686) ENTFERNEN. Die Funktion generiert KEINEN Lese-Zonen-Text mehr. Stattdessen: NACHDEM der Einwand-Typ feststeht (der Trigger-Aufrufer kennt den erkannten `neuer_text`/Typ), EINMAL `ewb_signal` mit dem erkannten KURZEN `typ`-Label emittieren. Wenn die Funktion ihren einzigen Zweck (Lese-Zonen-Variante) verliert: pruefe ob sie noch Wert hat (z.B. fuer PIP.4-Vorgenerierung) — wenn NICHT, kann ihr Aufruf durch ein direktes `ewb_signal`-Emit im Keyword-/analyse_loop-Pfad ersetzt werden. Entscheide bewusst (Punkt 27 — einfachster tragfaehiger Weg) und dokumentiere die Wahl im Funktions-Docstring. KEINE Haiku-Generierung mehr im Live-Pfad nur fuers Highlight (Latenz, Punkt 25) — ein Highlight braucht keine LLM-Antwort. **VOR dem Kalt-Stellen: Circuit-Breaker-Removal-Audit (Cross-AI MEDIUM #1, Sub-Step `ewb-circuit-breaker-audit` oben) ausfuehren — grep-belegen dass kein externer Produktiv-Pfad `_ewb_fallback_until`/`_ewb_ttft_history` liest, sonst STOP.** **Beim Umbau NICHT die phase/kb-Compute-Stellen anfassen (liegen ohnehin in einem anderen Pfad — siehe `coaching_signal_erhalt`).**
       → Bevorzugt: streame_auto_variante NICHT mehr aufrufen; stattdessen im Keyword-Pfad (deepgram) + analyse_loop direkt `ewb_signal` emittieren. streame_auto_variante als toten Auto-Lese-Zonen-Schreiber markieren — UND NACH bestandenem Circuit-Breaker-Audit als PIP.4-Foundation ins Foundation-Code-Register (CLAUDE.md Punkt 23 Foundation/Zombie-Regel) eintragen (inkl. dem dormanten Circuit-Breaker, da keine externen Reader).

    3. `_emit_qa_slot1` (claude_service.py:1645): emit('qa_slot1',{text}) ENTFERNEN. Stattdessen `ewb_signal` mit dem klassifizierten Einwand-Typ emittieren (known=True wenn der Typ einem Profil-Einwand entspricht, sonst neuer Button mit Kurz-Label). Den `slot1_variant_busy_until`-Set-Block (1662-1669) ENTFERNEN — der Lock fuer die Lese-Zone ist obsolet, weil Auto dort nicht mehr schreibt (CONTEXT Claude's-Discretion: 2-Store-Lock wird obsolet). MP4-Print darf bleiben bis Plan 05 (Instrumentierungs-Entfernung), ist aber jetzt aussagelos → in SUMMARY vermerken.

       **HARTE EXECUTOR-ANWEISUNG (Cross-AI HIGH #1 — Label-vs-Text-Falle, PFLICHT, nicht implizit):**
       `_emit_qa_slot1(text)` wird HEUTE mit `_antwort_text` aufgerufen — einem GANZEN, von Haiku generierten Absatz. Beim Umbau auf `ewb_signal` MUSST du als `typ`-Payload das ECHTE kurze Einwand-Label nutzen (NICHT den Roh-Enum-`_kat`-Wert, NICHT den `text`-Absatz). Der `text`-Parameter (voller Absatz) wird KOMPLETT VERWORFEN — er darf NIE als Signal-Payload (`typ`) gesendet werden.
       Konkret: `sio.emit('ewb_signal', {'typ': <echtes_label>, 'known': <bool>, 'source': 'analyse_loop'}, room=sid)` — NICHT `{'typ': text, ...}`, NICHT `{'typ': _antwort_text, ...}`, NICHT `{'typ': 'einwand_unknown', ...}`.
       Begruendung: ein ganzer Absatz als `typ` erzeugt FE-seitig (Task 2) einen riesigen Button mit Paragraph-Text → zerstoerte Button-Reihe + kaputte `data-typ`-Delegation.

       **HARTE EXECUTOR-ANWEISUNG (Cross-AI LOW — Geister-Button non-action categories, PFLICHT, nicht implizit):**
       VOR dem Emit den Blocklist-Check anwenden (Sub-Step `non-action-blocklist`): bei `_kat in ('smalltalk_none', 'frage', 'einwand_unknown')` ODER leerem/None `_kat` → KEIN Signal (`return`/`pass`, kein Geister-Button). Bei `_kat == 'einwand_known'` → HIGHLIGHT (known=True) mit dem ECHTEN Einwand-Label (nie dem Roh-Wert `einwand_known`). Nur wenn ein konkretes Kurz-Label fuer einen neuen Einwand vorliegt → known=False mit DIESEM Label. KEIN Roh-Enum-Wert verlaesst je die Funktion als `typ`.

       Scoping-Pflicht (Punkt 20, grep vor Insert): verifiziere per `inspect.sh grep "_kat"`, dass `_kat` im Scope von `_emit_qa_slot1` bzw. an dessen Aufrufstelle in `_qa_pipeline_dispatch` verfuegbar ist. Falls `_emit_qa_slot1` das Label NICHT im eigenen Scope sieht → gib das echte Label als zusaetzliches Argument durch (Signatur `_emit_qa_slot1(text, label)` o.ae.) und sende NUR `label`, nie `text`. Falls kein echtes Label vorliegt (Roh-`_kat` oder None) → KEIN Signal emittieren (kein Geister-Button — gleiche Regel wie Punkt 4 + Blocklist).

    4. `_emit_soft_hint` (claude_service.py:1576): emit('qa_soft_hint',{text}) ENTFERNEN. KEIN Lese-Zonen-Text mehr.

       **HARTE EXECUTOR-ANWEISUNG (Cross-AI HIGH #2 — Soft-Hint-Langstring-Falle, PFLICHT, nicht implizit):**
       `_emit_soft_hint` emittiert HEUTE lange Literal-Strings wie "Neuer Einwand — noch kein Vorschlag". Wuerde so ein String als `ewb_signal`-`typ` (mit `known=False`) rausgehen, baut die FE (Task 2) einen Button mit GENAU diesem langen Label-Text; ein Klick darauf schickt den ganzen String als Einwand-Typ an die LLM.
       Daher: wenn KEIN sauber klassifiziertes ECHTES Kurz-Label vorliegt (kein verwertbares Einwand-Label, nur ein Roh-`_kat`-Enum-Wert oder ein Langstring), emittiert `_emit_soft_hint` GAR KEIN Signal → schlichtes `return`/`pass`. KEIN Fallback-/Hint-Text-Button, KEINE Literal-Strings ("Neuer Einwand — noch kein Vorschlag" o.ae.) als `typ`, KEIN Roh-Enum-Wert (smalltalk_none/frage/einwand_unknown/einwand_known) als `typ` (Cross-AI LOW Blocklist).
       NUR wenn ausnahmsweise ein echtes Kurz-Label vorliegt (klassifizierter neuer Einwand-Typ als anzeigbares Label), darf `ewb_signal` mit `known=False` + diesem Kurz-Label gehen — sonst nichts.

    5. Keyword-Pfad deepgram_service.py:244-272: der `slot1_variant_busy_until`-Read/Set + `start_background_task(streame_auto_variante, ...)` wird ersetzt durch ein direktes `ewb_signal`-Emit fuer den gematchten Keyword-Einwand. **`typ` = `match['keyword']`/das kurze Keyword-Label — NICHT der gematchte Roh-Transkript-Text, NICHT ein Roh-Enum-Wert.** Der GLOBAL-ls.state-Lock-Zugriff (246/256) entfaellt fuer diesen Pfad (Lese-Zonen-Lock obsolet). MP3-Prints bleiben bis Plan 05.

    6. `streame_manual_ewb_variante` (claude_service.py:757) bleibt der EINZIGE Lese-Zonen-Schreiber. Seine drei emits bekommen `'source': 'manual_button'` ins Payload-Dict mitgegeben (pip_stream_start:836, pip_token:856, pip_token_done:873) — damit die FE (Task 2) sie als legitimen Pin-Schreiber erkennt. KEINE weitere Aenderung an dieser Funktion (Punkt 17).

    KEIN Refactor-Beifang (Punkt 17): nur die Auto-Lese-Zonen-Schreibpfade + das source-Tag am manual-Pfad. Keine Umbenennungen, kein Import-Sortieren, keine Coaching-Logik anfassen (die phase/kb-Compute-Pipeline bleibt vollstaendig erhalten — siehe `coaching_signal_erhalt`; der separate coaching_loop-WS-Emit ist bereits seit Phase 06.6 entfernt, claude_service.py:1883). Der TTFT-Circuit-Breaker wird NICHT umgebaut, nur per Audit als reader-frei belegt + als Foundation markiert (MEDIUM #1).

## 1. Lokaler Kontext
    streame_auto_variante (claude:577-700): ENTRY-Print 645 → emit pip_stream_start 646 → stream-loop mit pip_token 673 → emit pip_token_done 686 (result IMMER einwand:True, KEIN phase/kb). Circuit-Breaker umschliesst den Stream (638 `with _ewb_circuit_lock`, 639 `_cb_in_fallback`, 661-668 TTFT-Messung + `_ewb_fallback_until`-Set). Exit-Pfade: try/except um den Stream. Kein early return vor 646. Insertion = die drei emits ersetzen/entfernen; Circuit-Breaker NICHT umbauen, nur via Audit als reader-frei belegen (MEDIUM #1).
    _emit_qa_slot1 (claude:1645-1672): try → MP4-Print → emit qa_slot1 1660 → busy_until-Set 1662-1669 → Print → except. Aufgerufen aus 4 Stellen (1691/1703/1735 + frage-FAQ) mit `_antwort_text`. Alle bleiben gueltig, nur das Emit-Innere wechselt auf ewb_signal mit ECHTEM Label (NICHT `text`, NICHT Roh-`_kat`). Blocklist-Check (smalltalk_none/frage/einwand_unknown → kein Signal; einwand_known → highlight echtes Label) VOR dem Emit. Pruefe das `_kat`-Scoping (siehe HARTE ANWEISUNG Punkt 3) — ggf. Signatur um echtes Label erweitern.
    _emit_soft_hint (claude:1576-1585): try → emit qa_soft_hint 1579 → Print → except. Aufgerufen aus low-conf/empty/tabu-Pfaden (1679/1686/1688/...). Inneres wechselt: nur bei echtem Kurz-Label `ewb_signal`, sonst return (siehe HARTE ANWEISUNG Punkt 4 + Blocklist).
    Keyword-Pfad (deepgram:244-271): ls.state_lock-Read 246 → busy-Check 255 → ls.state_lock-Set 256 → start_background_task 266 → else: skip-Print 270. Ersetzt durch ewb_signal-Emit (kein Lock, kein bg-task), typ=match['keyword'] (echtes Keyword-Label, kein Roh-Enum).
    Coaching-Signal-Compute (claude:418-423 phase, 1339/1407-1419 kb/readiness): SEPARATER Pfad, NICHT in dieser Task-Liste — nur lesen/belegen, nicht anfassen.
    Circuit-Breaker-Globals (claude:18-20): _ewb_ttft_history/_ewb_fallback_until/_ewb_circuit_lock — Audit-Objekt (MEDIUM #1), nur grep-belegen + Foundation-markieren, nicht umbauen.

## 2. Funktions-Skelett
    streame_auto_variante: linear, ein try-Block um den Stream, ein Fallback-Zweig (TTFT-Circuit-Breaker). Nach Umbau: keine Stream-emits in die Lese-Zone; entweder ganz nicht mehr aufgerufen (bevorzugt, nach Circuit-Breaker-Audit) oder gibt nur noch ein Erkennungs-Resultat zurueck.
    _qa_pipeline_dispatch: grosser try; reads per-SID state; klassifiziert (setzt `_kat`); ruft _emit_qa_slot1/_emit_soft_hint/_emit_abstain_event. Die intent_event-/abstain-Logik (DSGVO/TAXO) bleibt UNANGETASTET — nur die zwei FE-Emits wechseln auf ewb_signal mit echtem Label (Blocklist fuer Roh-`_kat`). Verifiziere die `_kat`-Set-Stelle vor den _emit_*-Aufrufen, damit das Label zur Emit-Zeit gesetzt ist (Punkt 26: Leser laeuft Schreiber nicht voraus — `_kat` ist synchron im selben Funktions-Scope gesetzt, kein Cross-Thread-Versatz, also unkritisch; trotzdem belegen).
    Keyword-Pfad: in einer for-sid-Schleife im on_message-Handler; try/except pro sid. ewb_signal-Emit ist idempotent-sicher (nur ein room-emit).

## 3. Cross-File-Awareness
    grep-Belege (Executor verifiziert erneut via `grep -rn` / `inspect.sh grep` vor Edit, Punkt 20):
    - 'qa_slot1' : claude_service.py (emit) + static/pip-launcher.js:2494 (Handler) → Handler wird in Task 2 umgebaut.
    - 'qa_soft_hint' : claude_service.py (emit) + static/pip-launcher.js:2523 (Handler) → Task 2.
    - 'streame_auto_variante' : claude_service.py:577 (def) + deepgram_service.py:261/267 (Aufruf) → hier geaendert.
    - 'streame_manual_ewb_variante' : claude_service.py:757 (def) + deepgram_service.py:979/983 (Aufruf, manual-Pfad) → nur source-Tag.
    - '_kat' : Closure-Variable in _qa_pipeline_dispatch (claude_service.py:1572) — 4-Werte-Enum (einwand_unknown|frage|smalltalk_none|einwand_known). NIE als typ rausgeben (Blocklist, Cross-AI LOW). grep-belegen wo gesetzt + ob im _emit_*-Scope.
    - '_ewb_fallback_until' / '_ewb_ttft_history' / '_ewb_circuit_lock' : Circuit-Breaker-Globals (claude:18-20). grep MUSS zeigen: nur streame_auto_variante (+ Regressions-Test) liest/setzt sie (MEDIUM #1 Audit). KEIN externer Produktiv-Reader → Kalt-Stellen erlaubt + Foundation-Eintrag.
    - 'slot1_variant_busy_until' : claude_service.py:1533/1547/1656/1666 + deepgram_service.py:247/257 + live_session.py:143 → Lese-Zonen-Lock wird obsolet; NICHT global loeschen (live_session.py-Default bleibt als Foundation), nur die Auto-Set/Read im Lese-Zonen-Pfad entfernen. live_session.py NICHT in dieser Phase anfassen.
    - 'ewb_signal' : neu — darf vorher NICHT existieren (grep muss 0 Treffer zeigen ausser unseren neuen).
    - 'result.phase' / 'result.kb' : Coaching-Daten-Pipeline (slot-0-Analyse → FE _showProactiveContent @2408). LIEGT NICHT in den geprunten Pfaden → MUSS nach Task 1 noch im pip_token_done-Payload ankommen. Belegen via `inspect.sh grep "result.phase"`/`"result.kb"`: derselbe Treffer-Stand wie vor dem Edit (kein Compute/Emit entfernt).

## 4. Edge-Case-Analyse
    - Auto-Signal ohne erkennbaren Typ (None/leer/kein echtes Label): KEIN ewb_signal emittieren (kein Geister-Button). Gilt fuer beide HIGH-Findings (qa_slot1 mit leerem Label UND soft_hint ohne Kurz-Label).
    - Roh-Enum-Wert als Typ-Kandidat (smalltalk_none/frage/einwand_known/einwand_unknown): NIE als `typ` senden (Cross-AI LOW Blocklist) — smalltalk_none/frage/einwand_unknown → kein Signal; einwand_known → highlight mit echtem Label.
    - Langer Absatz / Literal-Hint-String als Typ-Kandidat: NIE senden — das ist exakt die Cross-AI-HIGH-Falle. typ ist IMMER ein echtes kurzes Label oder None.
    - Profil ohne Einwaende: ewb_signal known=False → FE erzeugt neuen Button; wenn FE-seitig nicht moeglich, Signal verfaellt lautlos (lossy ist gewollt, Newest-wins).
    - Coaching-Daten (phase/kb): bleiben erhalten; KEIN Edge-Case durch Task 1, weil sie in einem anderen Pfad berechnet/emittiert werden (separater slot-0-pip_token_done ohne einwand-Flag).
    - Circuit-Breaker kalt (MEDIUM #1): nach dem Kalt-Stellen feuert _ewb_fallback_until nie mehr → harmlos, da kein externer Reader (grep-belegt). Foundation-Eintrag verhindert versehentliche Zombie-Loeschung.
    ### Race-Condition-Audit (4 Pflicht-Fragen, CLAUDE.md Punkt 14)
    1. **Gleichzeitiges Feuern manual vs auto:** Frueher schrieb Auto (streame_auto_variante/qa_slot1) denselben slot1 waehrend der manual-Stream lief → Ueberschreiben. NACH diesem Umbau schreibt Auto GAR NICHT mehr in slot1 (nur ewb_signal an die Button-Zone). Garantierter Zustand: der manual-Stream ist der einzige Lese-Zonen-Schreiber → kein Ueberschreiben strukturell moeglich. Der Lock wird damit unnoetig (entfernt). Das ist der Kern-Fix.
    2. **Verbindungsabbruch mid-Stream:** Bricht der manual-Stream ab, bleibt halbfertiger Text in slot1 — UNVERAENDERT zum Status quo (kein Regress). Auto schreibt nicht nach, also kein Cleanup-Konflikt. ewb_signal ist ein Einzel-Emit ohne Half-State.
    3. **Reconnect-Replay (alte SID / alter Socket / gepufferter Emit):** ewb_signal traegt keinen Lese-Zonen-Zustand → ein verspaeteter Replay-Emit highlightet hoechstens einen Button erneut (harmlos, Newest-wins in FE Task 2 begrenzt es auf Groesse 1). Ein verspaeteter alter qa_slot1 kann NICHT mehr feuern (Event existiert nicht mehr). room=sid sorgt fuer Adressierung an die aktuelle Session.
    4. **Tab im Hintergrund:** ewb_signal-Emits, die waehrend Hintergrund-Tab ankommen, treffen den Newest-wins-Puffer (Task 2) → beim Reaktivieren steht nur das juengste Highlight. Kein gestauter Text-Stream in der Lese-Zone (weil Auto dort nicht schreibt).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ewb_signal"' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep auf Prod-deployed Code: `inspect.sh grep "qa_slot1"` zeigt KEIN `sio.emit('qa_slot1'` mehr in claude_service.py (nur noch der FE-Handler-Umbau in Task 2 referenziert es ggf. als Legacy-Kommentar).
    - `inspect.sh grep "ewb_signal"` zeigt die neuen Emits in claude_service.py + deepgram_service.py, JEWEILS mit einem ECHTEN Label (`'typ': <label>` bzw. `'typ': match['keyword']`) — NICHT `'typ': text`/`'typ': _antwort_text` (Cross-AI HIGH #1 verifiziert).
    - `inspect.sh grep "Neuer Einwand"` zeigt KEIN ewb_signal-Emit mit diesem Literal-String als typ (Cross-AI HIGH #2 verifiziert) — `_emit_soft_hint` macht `return`/`pass` ohne Kurz-Label.
    - **Geister-Button-Blocklist (Cross-AI LOW):** `inspect.sh grep "smalltalk_none"` und `inspect.sh grep "einwand_unknown"` zeigen KEIN `sio.emit('ewb_signal'` mit einem dieser Roh-Enum-Werte als `'typ'`; statt dessen zeigt der Code im Emit-Pfad einen Blocklist-Guard (z.B. `if _kat in ('smalltalk_none', 'frage', 'einwand_unknown')` / `if _kat == 'einwand_known'`) der diese Werte VOR dem Emit abfaengt (return/pass bzw. highlight mit echtem Label). KEIN `ewb_signal`-`typ` ist je woertlich `smalltalk_none`/`frage`/`einwand_known`/`einwand_unknown`.
    - **Circuit-Breaker-Removal-Audit (Cross-AI MEDIUM #1):** `inspect.sh grep "_ewb_fallback_until"` und `inspect.sh grep "_ewb_ttft_history"` zeigen JEDEN Produktiv-Treffer ausschliesslich innerhalb von `streame_auto_variante` (claude_service.py) — KEIN externer Produktiv-Reader/-Schreiber (nur der Regressions-Test darf zusaetzlich referenzieren). Das SUMMARY enthaelt die dokumentierte Audit-Entscheidung + den Foundation-Code-Register-Eintrag fuer streame_auto_variante inkl. dem dormanten Circuit-Breaker. (Falls doch ein externer Reader gefunden wird: STOP-Vermerk + Versorgungs-Entscheidung im SUMMARY statt Kalt-Stellen.)
    - **Coaching-Signal-Erhalt (André-Direktive):** `inspect.sh grep "result.phase"` UND `inspect.sh grep "result.kb"` zeigen denselben Compute/Emit-Stand wie vor dem Edit (Task 1 hat KEINEN phase/kb-Compute oder -Emit entfernt) — die Coaching-Daten-Pipeline ist intakt.
    - Nach Deploy + Live-Cold-Call + EINEN Einwand-Knopf klicken + Antwort vorlesen: `inspect.sh logs 400 | grep "BUGB-EWB] MP4"` zeigt WAEHREND eines aktiven manual-Streams KEINEN `source=qa_slot1` / `source=streame_auto_variante`-Write mehr in slot1 (Auto-Write-Pfad ist tot). Belegt Item (a) + Bug-B-Fix strukturell.
  </acceptance_criteria>
  <done>Auto schreibt nie mehr in pip-slot-body-1; alle Auto-Quellen emittieren ewb_signal mit ECHTEM KURZEM Label (typ=echtes Einwand-Label, nie ganzer Absatz, nie Literal-Hint, nie Roh-Enum-Wert smalltalk_none/frage/einwand_known/einwand_unknown); _emit_soft_hint ohne Kurz-Label = kein Signal; non-action-Blocklist greift; manual-Pfad traegt source=manual_button; Lese-Zonen-Lock entfernt; Circuit-Breaker-Removal-Audit (MEDIUM #1) belegt keine externen Reader + Foundation-Eintrag gesetzt; Coaching-Signal-Compute/-Emit (phase/kb) UNANGETASTET; bestaetigt per inspect.sh grep + Prod-Logs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FE — source-Gate auf die Lese-Zone + ewb_signal-Handler (highlight known / new button) + Newest-wins Groesse 1 + Coaching-Text raus (Daten-Pipeline bleibt)</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js 2309-2415 (pip_stream_start / pip_token / pip_token_done — die Lese-Zonen-Schreiber, + _showProactiveContent-Aufruf 2408, im Zweig `if (!d.result.einwand)` 2407)
    - static/pip-launcher.js 2494-2544 (qa_slot1 + qa_soft_hint Handler — werden ersetzt)
    - static/pip-launcher.js 2213-2245 (_renderEwbButtons: Button-Schema <button class="pip-ewb-btn" data-typ="LABEL">)
    - static/pip-launcher.js 2746-2752 (_highlightEwbButton) + 2754-2780 (_showProactiveContent/_showProactiveTipp — DEFINITIONEN bleiben, NUR slot-1-Guard ergaenzen; result.phase/result.kb lesen sie aus d.result)
    - static/pip-launcher.js 2270-2289 (_triggerEwb-Ende: setzt slot1 placeholder + emit manual_ewb — der Pin-Start)
    - Nerve-Vault/03 Planung/NERVE_PiP_Overlay_Entscheidung_Umsetzung.md "Auto-Erkenner: bekannt vs. neu" + Checkliste Punkt 1
    - .planning/04 Entscheidungen/Foundation-Code-Register.md Eintrag "PIP.2 — Coaching-Signal-Anzeige" (NICHT loeschen — nur Text-Ausgabe in slot 1 stoppen)
    - CLAUDE.md Punkt 14, "Keine hardcoded Dark-Mode-Farben + Inline-Styles" (neuer Button via CSS-Klasse, kein Inline-Hex), Punkt 27
  </read_first>
  <action>
    Drei Aenderungen, alle in static/pip-launcher.js (ASCII-Code-Identifier, Umlaute nur in User-sichtbarem Text — CLAUDE.md Umlaut-Regel):

    A) **Source-Gate auf die Lese-Zone (pip-slot-body-1):**
       In `pip_stream_start` (2309), `pip_token` (2351), `pip_token_done` (2367): wenn `slot === 1`, NUR dann in `pip-slot-body-1` schreiben, wenn `d.source === 'manual_button'`. Fehlt `d.source` oder ist es nicht 'manual_button' → den Lese-Zonen-Schreib-Zweig ueberspringen (discard). Das ist die zweite Verteidigungslinie zum BE-Cut (Task 1): selbst wenn je ein Auto-Stream-Emit durchkaeme, erreicht er die Lese-Zone nicht. slot 0 (Profil-Antwort A) bleibt unveraendert.

    B) **ewb_signal-Handler (NEU) — highlight known / create new + Newest-wins Groesse 1:**
       `state.socket.on('ewb_signal', function (d) {...})`:
       - **Eingangs-Guard (Cross-AI HIGH #1/#2 + LOW — FE-seitige zweite Verteidigungslinie):** wenn `!d.typ` ODER `d.typ.length > 40` (langer Absatz/Literal-String statt Kurz-Label) ODER `d.typ` ein Roh-Enum-Wert ist (`['smalltalk_none','frage','einwand_known','einwand_unknown'].indexOf(d.typ) !== -1`) → das Signal VERWERFEN (return, kein Button, kein Highlight). Das BE (Task 1) sendet nur echte kurze Labels; dieser Guard faengt zusaetzlich versehentlich durchgerutschte Langstrings UND Roh-Enum-Werte ab, damit NIE ein Paragraph- oder Geister-Button (`einwand_unknown`/`smalltalk_none`) entsteht. (Der 40-Zeichen-Schwellwert ist grosszuegig fuer reale Einwand-Labels wie 'zu_teuer', 'kein_budget', 'schon_anbieter' — aber sicher unter Absatz-Laenge.)
       - Newest-wins-Puffer Groesse 1: ein Modul-State `state.lastAutoSignal` haelt NUR das juengste Signal. Vor dem Anwenden eines neuen Signals das vorige Highlight zuruecknehmen (vorigen Button entmarkieren). Kein Array, kein Stapeln.
       - **Gating (Kopplung an Plan 02):** wenn ein Pin aktiv ist (`state.pinActive === true`, von Plan 02 gesetzt), das Signal NUR puffern (in state.lastAutoSignal merken) und NICHT sofort anzeigen — beim Pin-Ende (Plan 02 ruft `_applyBufferedAutoSignal()`) wird das juengste gepufferte Signal angewandt. In DIESEM Plan: den Hook `_applyBufferedAutoSignal()` definieren + den `state.pinActive`-Check einbauen.
         **LOW-Finding (Cross-AI, state.pinActive-Default, PFLICHT):** `state.pinActive` MUSS in Plan 01 als undefined/falsy belassen werden — NIRGENDS auf `true` initialisieren. Solange Plan 02 nicht live ist, verhaelt sich der Handler damit wie "kein Gating" (ewb_signal wendet sofort an), was fuer Plan 01 allein (Welle 1) korrekt ist. Falls der Executor `state` zentral initialisiert: `pinActive` entweder gar nicht setzen oder explizit `pinActive: false` — NIE `true`. grep-belegen dass kein `pinActive = true`/`pinActive: true` in Plan 01 entsteht (das ist erst Plan 02s Job, im _triggerEwb-Pfad).
       - **known (Button existiert):** finde in `nlp-ewb-row` einen `.pip-ewb-btn` mit `data-typ` das `d.typ` matched (gleiche lower/trim-Chain wie _triggerEwb). Treffer → `_highlightEwbButton(d.typ)` (leuchtet auf).
       - **neu (kein Button):** kein Treffer (oder d.known===false) → einen zusaetzlichen Button an reservierter Position in `nlp-ewb-row` erzeugen: `<button class="pip-ewb-btn pip-ewb-auto-new" data-typ="LABEL">LABEL</button>` (CSS-Klasse, KEIN Inline-Hex — neue Token bei Bedarf in nerve.css, CLAUDE.md). LABEL ist `d.typ` (kurzes echtes Label, durch den Eingangs-Guard garantiert kurz + kein Roh-Enum-Wert). Dann diesen neuen Button highlighten. Dedup: existiert schon ein Auto-Button mit gleichem data-typ, nur re-highlighten. Max-Begrenzung beachten (die bestehende 5-Button-Logik in _renderEwbButtons nicht sprengen — neuer Auto-Button zaehlt extra, aber Newest-wins haelt es bei 1 aktiven Auto-Button: vorigen Auto-new-Button entfernen bevor ein neuer entsteht).
       - Der neue Button ist voll klickbar (laeuft ueber die bestehende `.pip-ewb-btn`-Delegation in _wirePipButtons:1802 → _triggerEwb). Damit wird aus einem Auto-Hinweis bei Klick ein echter Pin (Plan 02).

    C) **NUR die Coaching-Text-AUSGABE in der Lese-Zone (slot 1) stoppen — die Coaching-SIGNAL-Datenpfade NICHT kappen (Item d, Anti-Abrieb-Leitplanke fuer PIP.2):**

       **Praezise Abgrenzung (André-Direktive 2026-06-29, PFLICHT — nicht implizit):** Geaendert wird AUSSCHLIESSLICH der *Schreib-Seiteneffekt in die Lese-Zone (slot 1)*. Die Coaching-Daten-Pipeline (Berechnung + Socket-Emits + die `pip_token_done`-Felder `result.phase` / `result.kb`, die Phase/Kaufbereitschaft/Tempo/Redeanteil liefern) bleibt UNANGETASTET und muss WEITER im Frontend ankommen. **KEIN Loeschen der Signal-Verdrahtung. KEIN Prunen des Aufrufs als toter Code. KEIN Symbol-Bau in PIP.1** (Text→Symbol-Re-Render ist PIP.2 — siehe Foundation-Code-Register Eintrag "PIP.2 — Coaching-Signal-Anzeige"). PIP.1 haelt nur die Daten am Leben und entfernt das *Erscheinen* des Coaching-Texts in slot 1.

       **Die einzige Aenderung (konkret, ein Mechanismus — Punkt 27, sauberster Weg):** der `_showProactiveContent(1 - slot, d.result)`-Aufruf in `pip_token_done` (pip-launcher.js:2408, im Zweig `if (d.result && !d.result.einwand)` Z.2407) schreibt heute Phase/Kaufbereitschaft via `_showProactiveTipp`/`body.innerHTML` in `pip-slot-body-(1-slot)` → kann slot 1 (Lese-Zone) treffen. Den *Schreib-in-die-Lese-Zone* abschalten durch einen Guard:
       - In `_showProactiveContent` (Z.2754) UND `_showProactiveTipp` (Z.2773) als ERSTE Zeile: `if (slot === 1) return;`. Damit landet Coaching NIE mehr in der Lese-Zone, unabhaengig von der Aufrufstelle (zweite Verteidigungslinie analog zum Source-Gate A; deckt auch alle kuenftigen Aufrufe ab).
       - Der Aufruf-Block 2406-2409 selbst BLEIBT strukturell stehen (kein Prune — er liefert `d.result` weiter an die Funktion, die Daten-Pipeline ist damit explizit intakt und PIP.2-sichtbar).

       **Was NICHT angefasst wird (PFLICHT-Erhalt, PIP.2-Foundation):**
       - `_showProactiveContent` / `_showProactiveTipp` Funktions-DEFINITIONEN bleiben vollstaendig erhalten (PIP.2 rendert sie zu Symbolen/Rahmenfarbe um — Definitionen NICHT loeschen, NICHT als toten Code prunen). NUR der `slot===1`-Guard wird ergaenzt.
       - Die Daten-VERSORGUNG bleibt: `result.phase` / `result.kb` muessen weiter im `pip_token_done`-Payload (`d.result`) an die FE kommen. Sie werden NICHT in einem der von Task 1 geprunten Auto-Lese-Zonen-Pfade berechnet (Coaching-Daten leben im slot-0-Analyse-Pfad — siehe Task 1 `coaching_signal_erhalt`). KEINE FE-Aenderung, die das `d.result`-Lesen von phase/kb entfernt.
       - Eintrag im Foundation-Code-Register (`.planning/04 Entscheidungen/Foundation-Code-Register.md`, Eintrag "PIP.2 — Coaching-Signal-Anzeige") referenziert genau diese Funktionen + ihre Datenversorgung als PIP.2-Foundation ("Text → Symbol re-render; pipeline must stay alive, do not rebuild").

    Legacy-Handler `qa_slot1` (2494) + `qa_soft_hint` (2523): da das BE-Emit (Task 1) entfaellt, feuern sie nicht mehr. Entweder Handler entfernen (sauber) ODER als No-Op mit Kommentar belassen. Bevorzugt entfernen (Pruning-Notiz). Wenn entfernt: sicherstellen dass kein anderer Code `qa_slot1`/`qa_soft_hint`-Emits erwartet (grep zeigt nur BE-Emits, die in Task 1 weg sind). **Achtung Abgrenzung:** das ist eine ANDERE Sache als der Coaching-Aufruf in C — qa_slot1/qa_soft_hint sind die geprunten Auto-Antwort-Handler, NICHT die Coaching-Pipeline. Die Coaching-Funktionen (_showProactiveContent/_showProactiveTipp) bleiben (nur Guard).

## 1. Lokaler Kontext
    pip_stream_start (2309-2349): liest d.slot (default 0); setzt body.textContent=''; pip-streaming-Klasse; replace_all-Zweig clear beide Slots. Kein early return vor dem slot-1-Write. Source-Gate: vor dem body-Schreiben fuer slot 1 die source pruefen.
    pip_token (2351-2365): early return wenn slot nicht streaming (2354). raw_text → body.textContent = akku. Source-Gate hier ebenso.
    pip_token_done (2367-2415): setzt streaming=false; _renderSlotResult; _showProactiveContent-Aufruf 2408 (BLEIBT — Funktion bekommt slot===1-Guard, kein Prune des Aufrufs).
    _showProactiveContent (2754) / _showProactiveTipp (2773): lesen result.phase / result.kb aus d.result. Definitionen bleiben; ERSTE Zeile bekommt `if (slot === 1) return;`. Datenversorgung unangetastet (PIP.2-Foundation).
    ewb_signal-Handler: NEU, neben den anderen state.socket.on(...)-Registrierungen in _registerSocketEvents.

## 2. Funktions-Skelett
    _registerSocketEvents (umschliesst alle state.socket.on): linear, ein on() pro Event. Neuer ewb_signal-on() additiv. Keine return-Fallen.
    _highlightEwbButton (2746): row-Lookup, querySelectorAll, toggle. Re-used.
    _showProactiveContent/_showProactiveTipp: linearer Body; nur ein Guard `if (slot === 1) return;` als erste Zeile. Sonst unveraendert (PIP.2 baut hier spaeter das Symbol-Rendering ein).
    Neuer-Button-Erzeugung: row.insertAdjacentHTML('beforeend', ...) + danach delegation greift automatisch (document-level Listener, kein Re-Wire noetig — verifiziert _wirePipButtons:1787 ist document-level).

## 3. Cross-File-Awareness
    - 'pip-slot-body-1' : nur static/pip-launcher.js (mehrere Stellen) — Lese-Zone ist FE-only. Source-Gate betrifft nur die slot-1-Zweige.
    - 'ewb_signal' : BE-Emit (Task 1) + neuer FE-Handler. Vertrag muss feldgleich sein (typ/known/source). typ ist IMMER ein echtes kurzes Label (BE-Garantie + FE-Eingangs-Guard, kein Roh-Enum, kein Langstring).
    - '.pip-ewb-btn' / 'nlp-ewb-row' / 'data-typ' : Button-Schema, geteilt mit _renderEwbButtons + _triggerEwb + Delegation. Neuer Auto-Button muss exakt diesem Schema folgen, sonst greift die Klick-Delegation nicht.
    - '_showProactiveContent' / '_showProactiveTipp' : nur pip-launcher.js. Definitionen BLEIBEN (PIP.2-Foundation, Foundation-Code-Register). NUR slot===1-Guard ergaenzt. result.phase/result.kb-Datenfluss aus d.result unangetastet.
    - 'result.phase' / 'result.kb' : Datenversorgung der Coaching-Funktionen, kommt aus dem pip_token_done-Payload (slot-0-Analyse-Pfad, BE). MUSS weiter ankommen (Task 1 lasst es intakt). FE liest es unveraendert.
    - 'state.pinActive' : Default NICHT true (LOW-Finding). Plan 02 setzt ihn echt im _triggerEwb-Pfad.
    - 'pip-ewb-auto-new' : neue CSS-Klasse — in static/nerve.css ergaenzen falls visuelle Differenzierung gewuenscht (kein Inline-Hex, CLAUDE.md). Optional; ohne eigenen Stil sieht der Auto-Button aus wie ein normaler EWB-Button (akzeptabel fuer PIP.1; volles Einblend-Design = PIP.2).

## 4. Edge-Case-Analyse
    - ewb_signal ohne typ ODER mit Langstring (>40 Zeichen) ODER mit Roh-Enum-Wert (smalltalk_none/frage/einwand_known/einwand_unknown): Eingangs-Guard verwirft (kein Geister-/Paragraph-Button) — Cross-AI HIGH #1/#2 + LOW FE-seitig abgesichert.
    - Doppeltes Signal gleicher typ: Newest-wins → re-highlight desselben Buttons, kein Duplikat-Button (Dedup per data-typ).
    - Profil-Button + Auto-new-Button mit gleichem Label: known-Pfad greift zuerst (bestehender Button gewinnt), kein neuer.
    - Coaching-Aufruf trifft slot 1: Guard `if (slot===1) return;` greift → kein Text in der Lese-Zone, ABER d.result (mit phase/kb) ist weiterhin an die Funktion uebergeben → PIP.2 kann die Daten dort abgreifen. Trifft der Aufruf slot 0 (1-slot===0): Coaching darf dort weiter erscheinen (Status quo, kein Lese-Zonen-Konflikt).
    ### Race-Condition-Audit (4 Pflicht-Fragen)
    1. **Gleichzeitiges Feuern manual vs auto:** manual-Stream schreibt slot 1 (source=manual_button passt Gate). Gleichzeitiges ewb_signal beruehrt NUR die Button-Zone (highlight), nie slot 1 → kein DOM-Konflikt am Lese-Node. Garantiert: Lese-Zone gehoert exklusiv dem manual-Stream.
    2. **Verbindungsabbruch mid-Stream:** halbfertiger manual-Text bleibt (Status quo). ewb_signal-Puffer (Groesse 1) ist reiner FE-State, kein Half-DOM.
    3. **Reconnect-Replay:** ein alter, verspaeteter Stream-Emit ohne source / mit auto-source wird vom Source-Gate verworfen → kann die Lese-Zone nicht mehr ueberschreiben (zweite Verteidigungslinie). Ein alter ewb_signal highlightet hoechstens erneut (Newest-wins begrenzt).
    4. **Tab im Hintergrund:** ankommende ewb_signal sammeln sich nicht — nur state.lastAutoSignal (Groesse 1) ueberlebt; beim Reaktivieren ein Highlight, kein Stau. Stream-Tokens fuer slot 1 ohne source verfallen am Gate.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "source === '\''manual_button'\''"' | head; ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ewb_signal"' | head</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh grep "source"` in static/pip-launcher.js zeigt den manual_button-Gate-Check in den slot-1-Zweigen von pip_stream_start/pip_token.
    - `inspect.sh grep "ewb_signal"` zeigt den neuen FE-Handler MIT dem Eingangs-Guard (`!d.typ` / Laengen-Check / Roh-Enum-Blocklist `smalltalk_none`/`einwand_unknown`/`frage`/`einwand_known`) — Cross-AI HIGH-Falle + LOW Geister-Button FE-seitig abgesichert.
    - `inspect.sh grep "pinActive"` zeigt in pip-launcher.js KEIN `pinActive = true` / `pinActive: true` aus Plan 01 (LOW-Finding: Default falsy, true erst Plan 02).
    - **Coaching-Daten-Pipeline intakt (André-Direktive):** `inspect.sh grep "_showProactiveContent"` zeigt die Funktions-Definition + den Aufruf @2408 WEITERHIN vorhanden (NICHT geprunt); `inspect.sh grep "slot === 1"` (bzw. `slot===1`) zeigt den neuen Guard in _showProactiveContent/_showProactiveTipp; `inspect.sh grep "result.phase"` / `inspect.sh grep "result.kb"` zeigen das FE-Lesen der Coaching-Daten unveraendert.
    - Live-Cold-Call (checkpoint, Task 3): Auto-Erkennung erscheint sichtbar NUR als Button-Highlight / neuer Button mit ECHTEM KURZEM Label; KEIN Paragraph-Button, KEIN Geister-Button (`einwand_unknown`/`smalltalk_none`), KEIN Text in der Antwort-Zone (slot 1) ausser dem geklickten Vorschlag. Coaching-Text (Phase/Kaufbereitschaft/Tipp) erscheint nicht in slot 1.
    - Browser-Konsole zeigt KEINE qa_slot1/qa_soft_hint-Render-Logs mehr (Handler entfernt oder no-op).
  </acceptance_criteria>
  <done>Lese-Zone wird FE-seitig nur noch von source=manual_button beschrieben; ewb_signal highlightet bekannten / erzeugt neuen Button (echtes Kurz-Label, Langstring-Guard + Roh-Enum-Blocklist) mit Newest-wins Groesse 1; state.pinActive bleibt in Plan 01 falsy; Coaching-TEXT in slot 1 per Guard gestoppt, ABER _showProactiveContent/_showProactiveTipp-Definitionen + Aufruf + result.phase/result.kb-Datenfluss BLEIBEN (PIP.2-Foundation); Gating-Hook fuer Plan 02 vorbereitet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-Cold-Call Verifikation (Prod) — Anzeige-Trennung + kein Ueberschreiben + Coaching-Text raus (Daten-Pipeline intakt)</name>
  <what-built>
    Auto-Erkenner schreibt nicht mehr in die Lese-Zone (BE-Cut + FE-Source-Gate); Auto erscheint nur als Button-Highlight/neuer Button (Newest-wins 1, ECHTES KURZES Label, kein Geister-Button); Coaching-TEXT raus aus slot 1 (Guard) bei erhaltener Coaching-Daten-Pipeline (phase/kb kommen weiter an, PIP.2-Foundation). Deploy laeuft ueber `bash deploy.sh production` (Kein-Local-Dev, CLAUDE.md). Der Pytest-Gate in deploy.sh laeuft serverseitig vor dem Restart.
  </what-built>
  <how-to-verify>
    1. `bash deploy.sh production` (Auto-Pytest-Gate + Restart; bei Test-Fehler kein Restart).
    2. Im Browser mit Test-User auf getnerve.app einen Cold-Call starten (Headset-Modal bestaetigen).
    3. EINEN Einwand-Knopf klicken → Antwort erscheint in der Antwort-Zone.
    4. Die Antwort LANGSAM laut vorlesen (Selbst-Trigger provozieren). Beobachten:
       - Bleibt der geklickte Vorschlag-Text waehrend des Vorlesens STEHEN (wird NICHT ueberschrieben)? (erwartet: ja)
       - Erscheint eine Auto-Erkennung als Button-Highlight oder neuer Button (nicht als Text in der Antwort-Zone)? (erwartet: ja)
       - Ist ein evtl. neu erzeugter Auto-Button ein ECHTES KURZES Label (z.B. "zu_teuer"), KEIN ganzer Antwort-Absatz, KEIN langer Hint-String wie "Neuer Einwand — noch kein Vorschlag" UND KEIN Roh-Enum-Wert wie "einwand_unknown"/"smalltalk_none"/"frage"? (erwartet: ja — Cross-AI HIGH #1/#2 + LOW)
       - Taucht Coaching-/Phasen-/Kaufbereitschafts-TEXT in der Antwort-Zone (slot 1) auf? (erwartet: nein)
    5. Logs ziehen: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 500'` | grep "BUGB-EWB] MP4" → waehrend des aktiven manual-Streams KEIN auto-source-Write in slot1. Plus Beleg dass die Coaching-Daten weiter ankommen (result.phase/result.kb im pip_token_done-Pfad — fuer PIP.2 verfuegbar, auch wenn in slot 1 nicht mehr gerendert).
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn der Vorschlag stehen bleibt + Auto nur als Button (echtes kurzes Label, kein Geister-Button) erscheint + kein Coaching-Text in der Zone (aber die Coaching-Daten-Pipeline laut SUMMARY intakt) — sonst beschreibe das beobachtete Verhalten + relevante MP4/MP6-Logzeilen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Deepgram STT → BE (deepgram_service) | Transkribierter Berater-Text (kann beim Vorlesen NERVEs eigene Antwort enthalten = Selbst-Trigger-Quelle) |
| BE (claude_service/deepgram_service) → FE (Socket.IO room=sid) | ewb_signal / pip_stream-Events; FE muss Quelle (source) pruefen bevor Lese-Zone beschrieben wird; typ ist immer ein echtes kurzes Label (kein Roh-Enum, kein Langstring) |
| FE Auto-erzeugter Button → manual_ewb-Emit | Auto-Hinweis wird erst durch Berater-Klick zum Pin (kein Auto-Pin) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PIP-01 | Tampering | Auto-Stream-Emit erreicht Lese-Zone trotz Cut | mitigate | FE-Source-Gate: nur source=manual_button schreibt pip-slot-body-1 (Task 2 A) — zweite Verteidigungslinie zum BE-Cut |
| T-PIP-02 | Spoofing | verspaeteter Reconnect-Replay-Emit (alte SID) ueberschreibt aktiven Pin | mitigate | room=sid-Adressierung + Source-Gate; alte qa_slot1-Events existieren nicht mehr |
| T-PIP-03 | Information Disclosure | neuer Auto-Button-Text leakt Kundennamen/Firma (DSGVO) | mitigate | BE liefert nur anonymisiertes Kurz-Label (echtes Einwand-Label, nie Roh-STT, nie Antwort-Absatz, nie Roh-Enum) fuer den neuen Button + FE-Eingangs-Guard verwirft Langstrings + Roh-Enum-Werte |
| T-PIP-04 | Denial of Service | Auto-Signal-Flut erzeugt viele Buttons | accept | Newest-wins-Puffer Groesse 1 begrenzt aktiven Auto-Button auf 1 (Task 2 B); lossy by design |
| T-PIP-12 | Tampering | voller Antwort-Absatz / Literal-Hint-String leakt als Button-Label (Cross-AI HIGH #1/#2) | mitigate | BE sendet NUR echtes Kurz-Label (text-Param verworfen; soft_hint ohne Label = kein Signal); FE-Eingangs-Guard verwirft typ ohne Wert / >40 Zeichen |
| T-PIP-13 | Information Disclosure / Abrieb | Item-(d)-Cut entfernt versehentlich die Coaching-Signal-Pipeline (phase/kb) → PIP.2 muesste sie neu bauen (Abrieb) | mitigate | NUR Text-Ausgabe in slot 1 geguarded (Task 2 C); Compute/Emit (slot-0-Pfad) per grep belegt UNANGETASTET (Task 1 `coaching_signal_erhalt`); Definitionen + Datenfluss als PIP.2-Foundation im Foundation-Code-Register verankert |
| T-PIP-14 | Tampering | Roh-Enum-Wert (smalltalk_none/frage/einwand_known/einwand_unknown) leakt als Button-Label → Geister-Button (Cross-AI LOW) | mitigate | BE-Blocklist-Guard (non-action-blocklist, Task 1 Punkt 3/4): Roh-Enum verlaesst nie die Funktion als typ; FE-Eingangs-Guard verwirft Roh-Enum-Werte zusaetzlich (Task 2 B, zweite Verteidigungslinie) |
| T-PIP-15 | Denial of Service / Abrieb | Kalt-Stellen von streame_auto_variante nimmt einen still erwarteten TTFT-Circuit-Breaker / Cost-Tracker mit, von dem ein anderer Pfad abhaengt (Cross-AI MEDIUM #1) | mitigate | Circuit-Breaker-Removal-Audit (Task 1 Sub-Step `ewb-circuit-breaker-audit`): grep-belegt 0 externe Produktiv-Reader von `_ewb_fallback_until`/`_ewb_ttft_history` → dormant ohne Abhaengige + Foundation-Eintrag; bei gefundenem externem Reader STOP + bewusste Versorgung |

</threat_model>

<verification>
- inspect.sh grep bestaetigt: kein qa_slot1/qa_soft_hint-BE-Emit mehr; ewb_signal vorhanden BE+FE mit typ=echtes Kurz-Label; Source-Gate im FE; FE-Eingangs-Guard gegen Langstrings + Roh-Enum-Werte; state.pinActive in Plan 01 nicht true.
- **Geister-Button-Blocklist (Cross-AI LOW):** kein ewb_signal-typ ist je woertlich smalltalk_none/frage/einwand_known/einwand_unknown; Blocklist-Guard im BE-Emit-Pfad + FE-Eingangs-Guard belegt.
- **Circuit-Breaker-Removal-Audit (Cross-AI MEDIUM #1):** grep belegt 0 externe Produktiv-Reader von _ewb_fallback_until/_ewb_ttft_history; SUMMARY-Entscheidung + Foundation-Eintrag vorhanden.
- **Coaching-Daten-Pipeline intakt:** _showProactiveContent/_showProactiveTipp-Definitionen + Aufruf @2408 vorhanden (nur slot===1-Guard ergaenzt); result.phase/result.kb-Compute (BE slot-0-Pfad) + FE-Lesen unveraendert (grep-Stand wie vor Edit).
- Prod-Live-Cold-Call (Task 3): Vorschlag bleibt stehen, Auto nur als Button mit echtem Kurz-Label, kein Geister-Button, kein Coaching-Text in der Zone.
- Prod-Logs (MP4): kein auto-source-Slot1-Write waehrend aktivem manual-Stream.
</verification>

<success_criteria>
- (a) Auto-Erkenner schreibt strukturell nie mehr in die Lese-Zone — bewiesen per Code-grep (kein Auto-Emit-Pfad) UND Prod-Log (kein MP4 auto-source-Write).
- (d) Coaching-TEXT erscheint nicht mehr in der Lese-Zone (Schreib-in-slot-1 per Guard gestoppt) — UND die Coaching-Signal-Datenpipeline (Compute/Emit + result.phase/result.kb-Felder) bleibt intakt und erreicht die FE (PIP.2-Foundation erhalten, nicht neu zu bauen).
- Newest-wins Groesse 1 fuer Auto-Highlights.
- ewb_signal traegt nur ein echtes kurzes Label; kein Paragraph-/Hint-/Geister-Button moeglich (Cross-AI HIGH #1/#2 + LOW strukturell + per Guard abgesichert).
- Circuit-Breaker-Removal-Audit (Cross-AI MEDIUM #1) belegt keine externen Reader + Foundation-Eintrag gesetzt.
- Bug B (Ueberschreiben im Vorlesen) im Live-Re-Test nicht mehr reproduzierbar.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-01-SUMMARY.md`. Pflicht:
- Pruning-Notiz fuer streame_auto_variante / qa_slot1+qa_soft_hint-Handler (geloescht ODER Foundation-Register-Eintrag fuer PIP.4) + Vermerk dass der slot1_variant_busy_until-Lese-Zonen-Lock obsolet wurde + Bestaetigung dass ewb_signal nur echte Kurz-Labels traegt (Cross-AI HIGH #1/#2 + LOW Blocklist umgesetzt).
- **Circuit-Breaker-Removal-Audit (Cross-AI MEDIUM #1, Pflicht-Vermerk):** das grep-Ergebnis fuer `_ewb_fallback_until`/`_ewb_ttft_history` (0 externe Produktiv-Reader ODER die gefundene Abhaengigkeit + STOP-Entscheidung) + der Foundation-Code-Register-Eintrag fuer streame_auto_variante inkl. dem dormanten Circuit-Breaker.
- **Coaching-Signal-Erhalt (André-Direktive, Pflicht-Vermerk):** (a) WELCHE Coaching-Signale erhalten bleiben — Phase / Kaufbereitschaft / Tempo / Redeanteil + ihre Emit-Felder (`result.phase`, `result.kb` im pip_token_done-Pfad, slot-0-Analyse); (b) WO PIP.2 sie abgreift (`_showProactiveContent`/`_showProactiveTipp` bekommen `d.result` weiterhin uebergeben; PIP.2 rendert Text→Symbol); (c) Verweis auf den Foundation-Code-Register-Eintrag "PIP.2 — Coaching-Signal-Anzeige". KEINE generische "prune-or-foundation"-Notiz fuer den Coaching-Aufruf — der Aufruf BLEIBT, nur die slot-1-Text-Ausgabe ist geguarded.
</output>
