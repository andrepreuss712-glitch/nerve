---
phase: 08.23.2.PIP
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/claude_service.py
  - services/deepgram_service.py
  - static/pip-launcher.js
autonomous: false
requirements: [a, d]
complexity: 🟡
cross_ai_required: true

must_haves:
  truths:
    - "Auto-Erkennung (keyword / analyse_loop / coldcall_infer) erscheint NUR als Button-Highlight oder neuer Button, NIE als Text in der Lese-Zone (pip-slot-body-1)."
    - "Ein laufender manueller Knopf-Stream wird von einem gleichzeitigen Auto-Signal NICHT mehr im Vorlesen ueberschrieben."
    - "Coaching-Text (Phase/Kaufbereitschaft/Tipp) erscheint nicht mehr in der Lese-Zone."
    - "Newest-wins: mehrere Auto-Signale kurz hintereinander hinterlassen nur das juengste Highlight, kein Stapeln."
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "FE: source-getrennte Stream-Verarbeitung + Button-Signal-Handler (highlight/new-button) + Newest-wins-Puffer Groesse 1; Auto-Quellen schreiben NIE pip-slot-body-1"
      contains: "ewb_signal"
    - path: "services/claude_service.py"
      provides: "BE: Auto-Pfad (streame_auto_variante, qa_slot1/qa_soft_hint) emittiert Button-Signal statt Lese-Zonen-Text; manual-Pfad traegt source=manual_button"
      contains: "ewb_signal"
    - path: "services/deepgram_service.py"
      provides: "BE: Keyword-Pfad ruft kein Lese-Zonen-Schreiben mehr an, sondern Button-Signal"
  key_links:
    - from: "services/claude_service.py streame_auto_variante / _emit_qa_slot1 / _emit_soft_hint"
      to: "FE ewb_signal-Handler"
      via: "sio.emit('ewb_signal', {...})"
      pattern: "ewb_signal"
    - from: "static/pip-launcher.js pip_stream_start/pip_token"
      to: "pip-slot-body-1 (Lese-Zone)"
      via: "source-Gate: nur source=manual_button darf schreiben"
      pattern: "source.*manual_button"
---

<objective>
Strukturelle Eingabe/Ausgabe-Trennung: der Auto-Erkenner bekommt KEINEN Schreibzugriff mehr auf die Lese-Zone (`pip-slot-body-1`). Er darf nur noch einen bekannten Button aufleuchten lassen (Highlight) oder einen neuen Button erzeugen. Damit ist "Antwort wird im Vorlesen ueberschrieben" nicht gemanagt, sondern strukturell unmoeglich (Kernregel der Phase). Deckt Scope-Item (a) ab und stoppt zugleich Coaching-Text in der Lese-Zone (Item d, Rest-Pfad `_showProactiveContent`).

Purpose: Bug B (Ueberschreiben im Vorlesen) an der Wurzel kappen — nicht das Symptom im FE kaschieren, sondern den Auto-Schreibpfad in die Lese-Zone abschneiden. Der Lock-Streit (slot1_variant_busy_until in 2 Stores) wird dadurch fuer die Lese-Zone obsolet, weil Auto dort gar nicht mehr schreibt.

Output: source-getrennte Stream-Pipeline (manual vs auto), neuer `ewb_signal`-Kanal (highlight known / create new), Newest-wins-Puffer Groesse 1, Coaching-Text raus aus der Lese-Zone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md
@.planning/debug/ewb-pip-overwrite-multifire.md
@CLAUDE.md

# Kanonische Design-Vorgabe (Pflicht-Pre-Read, dagegen bauen):
# C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\NERVE_PiP_Overlay_Entscheidung_Umsetzung.md

<interfaces>
<!-- Verifizierte Vertraege (aus dem Code gelesen, Executor nutzt diese direkt — keine erneute Code-Suche noetig). -->

FE — die EINZIGEN Schreiber von pip-slot-body-1 (Lese-Zone), verifiziert:
  static/pip-launcher.js
  - pip_stream_start (2309) / pip_token (2351) / pip_token_done (2367): raw_text-Stream in slot=d.slot.
      BEIDE BE-Streamer (manual + auto) nutzen DIESELBEN Events mit raw_text:true, slot 1 →
      FE kann sie HEUTE nicht unterscheiden. Hier wird die source-Trennung eingefuehrt.
  - qa_slot1-Handler (2494): body.textContent = txt  (Quelle: analyse_loop/_emit_qa_slot1) → AUTO
  - qa_soft_hint-Handler (2523): body.textContent = txt  (Quelle: _emit_soft_hint) → AUTO
  - _showProactiveContent (2754) / _showProactiveTipp (2773): schreibt Phase/Kaufbereitschaft/Tipp
      in pip-slot-body-(1-slot). Aufruf in pip_token_done (2408) → kann slot 1 treffen → COACHING-TEXT

FE — Button-Zone + Helfer, verifiziert:
  - nlp-ewb-row (Container der EWB-Buttons), Buttons: <button class="pip-ewb-btn" data-typ="LABEL">
  - _highlightEwbButton(typ) (2746): toggelt Klasse 'pip-ewb-ai-selected' am Button mit data-typ===typ
  - _renderEwbButtons (2213): rendert Buttons aus state.profileDaten.einwaende_detail/einwaende
  - _triggerEwb(typ, btn) (2247): manual-Klick → _renderSlotResult(0, fake) + emit('manual_ewb', {slot:1,...})

BE — Auto-Schreiber (slot 1), verifiziert:
  services/claude_service.py
  - streame_auto_variante(neuer_text, einwaende, kontext, sid, slot=1, trigger) (577):
      emit pip_stream_start (646) / pip_token (673) / pip_token_done (686), alle raw_text:true → AUTO
  - _qa_pipeline_dispatch._emit_qa_slot1 (1645): emit('qa_slot1',{text}) + setzt slot1_variant_busy_until → AUTO
  - _qa_pipeline_dispatch._emit_soft_hint (1576): emit('qa_soft_hint',{text}) → AUTO
  - streame_manual_ewb_variante(typ, profile_einwand, kontext, sid, slot=1) (757):
      emit pip_stream_start (836) / pip_token (856) / pip_token_done (873), raw_text:true → MANUAL (der Pin)
  services/deepgram_service.py
  - Keyword-Pfad (255-269): start_background_task(streame_auto_variante, ..., "keyword") → AUTO
  - coldcall_infer (claude_service 472-510): inferiert Einwand-Typ, schreibt NICHT direkt slot1 (nur Erkennung);
      sein Ergebnis fuettert analyse_loop → qa/streame_auto. KEIN eigener Lese-Zonen-Write.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BE — Auto-Pfad emittiert Button-Signal statt Lese-Zonen-Text; manual-Pfad bekommt source=manual_button</name>
  <files>services/claude_service.py, services/deepgram_service.py</files>
  <read_first>
    - services/claude_service.py 577-700 (streame_auto_variante: alle drei emits pip_stream_start/pip_token/pip_token_done)
    - services/claude_service.py 1576-1672 (_emit_soft_hint + _emit_qa_slot1, inkl. slot1_variant_busy_until-Set)
    - services/claude_service.py 757-880 (streame_manual_ewb_variante: die drei emits — bleibt der einzige Lese-Zonen-Schreiber)
    - services/deepgram_service.py 244-272 (Keyword-Pfad: liest GLOBAL ls.state busy_until, ruft streame_auto_variante "keyword")
    - .planning/phases/08.23.2.PIP-.../08.23.2.PIP-CONTEXT.md <decisions> (a) + Kernregel
    - Nerve-Vault/03 Planung/NERVE_PiP_Overlay_Entscheidung_Umsetzung.md Abschnitt "Auto-Erkenner: bekannt vs. neu" + Checkliste Punkt 1
    - CLAUDE.md Punkt 14 (Control-Flow + 4 Race-Fragen), Punkt 17 (kein Refactor-Beifang), Punkt 25 (Latenz)
  </read_first>
  <action>
    Ziel: Auto darf die Lese-Zone (slot 1 raw_text-Stream + qa_slot1 + qa_soft_hint) NICHT mehr fuettern. Statt Text-in-Lese-Zone emittiert Auto ein neues Button-Signal `ewb_signal`.

    1. Neues Emit-Schema (EIN Event, von allen Auto-Quellen genutzt):
       `sio.emit('ewb_signal', {'typ': <einwand_label_or_None>, 'known': <bool>, 'source': <'keyword'|'analyse_loop'|'coldcall_infer'>}, room=sid)`
       - `typ` = erkanntes Einwand-Label (z.B. 'zu_teuer' / kurzlabel) wenn vorhanden, sonst der anonymisierte Kurz-Text fuer einen NEUEN Button.
       - `known` = True wenn `typ` einem bestehenden Profil-Einwand entspricht (Button existiert), sonst False (neuer Button).
       - Die known/neu-Entscheidung trifft die FE (Task 2) anhand der gerenderten Buttons; BE liefert `typ` + best-effort `known`-Hinweis. Bei Unsicherheit `known=None` → FE entscheidet.

    2. `streame_auto_variante` (claude_service.py:577): die DREI Lese-Zonen-emits (pip_stream_start:646, pip_token:673, pip_token_done:686) ENTFERNEN. Die Funktion generiert KEINEN Lese-Zonen-Text mehr. Stattdessen: NACHDEM der Einwand-Typ feststeht (der Trigger-Aufrufer kennt den erkannten `neuer_text`/Typ), EINMAL `ewb_signal` mit dem erkannten `typ` emittieren. Wenn die Funktion ihren einzigen Zweck (Lese-Zonen-Variante) verliert: pruefe ob sie noch Wert hat (z.B. fuer PIP.4-Vorgenerierung) — wenn NICHT, kann ihr Aufruf durch ein direktes `ewb_signal`-Emit im Keyword-/analyse_loop-Pfad ersetzt werden. Entscheide bewusst (Punkt 27 — einfachster tragfaehiger Weg) und dokumentiere die Wahl im Funktions-Docstring. KEINE Haiku-Generierung mehr im Live-Pfad nur fuers Highlight (Latenz, Punkt 25) — ein Highlight braucht keine LLM-Antwort.
       → Bevorzugt: streame_auto_variante NICHT mehr aufrufen; stattdessen im Keyword-Pfad (deepgram) + analyse_loop direkt `ewb_signal` emittieren. streame_auto_variante als toten Auto-Lese-Zonen-Schreiber markieren (Pruning-Notiz in SUMMARY, Punkt 3a) ODER als PIP.4-Foundation ins Foundation-Code-Register (CLAUDE.md Nudelcode-3b) eintragen wenn die Vorgenerierungs-Logik spaeter gebraucht wird.

    3. `_emit_qa_slot1` (claude_service.py:1645): emit('qa_slot1',{text}) ENTFERNEN. Stattdessen `ewb_signal` mit dem klassifizierten Einwand-Typ emittieren (known=True wenn der Typ einem Profil-Einwand entspricht, sonst neuer Button mit Kurz-Label). Den `slot1_variant_busy_until`-Set-Block (1662-1669) ENTFERNEN — der Lock fuer die Lese-Zone ist obsolet, weil Auto dort nicht mehr schreibt (CONTEXT Claude's-Discretion: 2-Store-Lock wird obsolet). MP4-Print darf bleiben bis Plan 05 (Instrumentierungs-Entfernung), ist aber jetzt aussagelos → in SUMMARY vermerken.

    4. `_emit_soft_hint` (claude_service.py:1576): emit('qa_soft_hint',{text}) ENTFERNEN. Low-Conf/unbekannter Einwand → `ewb_signal` mit known=False (neuer Button "Neuer Einwand"-Kandidat) ODER gar kein Signal wenn kein verwertbarer Typ. KEIN Lese-Zonen-Text mehr.

    5. Keyword-Pfad deepgram_service.py:244-272: der `slot1_variant_busy_until`-Read/Set + `start_background_task(streame_auto_variante, ...)` wird ersetzt durch ein direktes `ewb_signal`-Emit fuer den gematchten Keyword-Einwand (`match['keyword']`/Label). Der GLOBAL-ls.state-Lock-Zugriff (246/256) entfaellt fuer diesen Pfad (Lese-Zonen-Lock obsolet). MP3-Prints bleiben bis Plan 05.

    6. `streame_manual_ewb_variante` (claude_service.py:757) bleibt der EINZIGE Lese-Zonen-Schreiber. Seine drei emits bekommen `'source': 'manual_button'` ins Payload-Dict mitgegeben (pip_stream_start:836, pip_token:856, pip_token_done:873) — damit die FE (Task 2) sie als legitimen Pin-Schreiber erkennt. KEINE weitere Aenderung an dieser Funktion (Punkt 17).

    KEIN Refactor-Beifang (Punkt 17): nur die Auto-Lese-Zonen-Schreibpfade + das source-Tag am manual-Pfad. Keine Umbenennungen, kein Import-Sortieren, keine Coaching-Logik anfassen (die ist BE-seitig schon entfernt, siehe Task 3-Hinweis).

## 1. Lokaler Kontext
    streame_auto_variante (claude:577-700): ENTRY-Print 645 → emit pip_stream_start 646 → stream-loop mit pip_token 673 → emit pip_token_done 686. Exit-Pfade: try/except um den Stream (Fallback-Zweig setzt _ewb_fallback_until). Kein early return vor 646. Insertion = die drei emits ersetzen/entfernen.
    _emit_qa_slot1 (claude:1645-1672): try → MP4-Print → emit qa_slot1 1660 → busy_until-Set 1662-1669 → Print → except. Aufgerufen aus 4 Stellen (1691/1703/1735 + frage-FAQ). Alle bleiben gueltig, nur das Emit-Innere wechselt auf ewb_signal.
    _emit_soft_hint (claude:1576-1585): try → emit qa_soft_hint 1579 → Print → except. Aufgerufen aus low-conf/empty/tabu-Pfaden (1679/1686/1688/...). Inneres wechselt.
    Keyword-Pfad (deepgram:244-271): ls.state_lock-Read 246 → busy-Check 255 → ls.state_lock-Set 256 → start_background_task 266 → else: skip-Print 270. Ersetzt durch ewb_signal-Emit (kein Lock, kein bg-task).

## 2. Funktions-Skelett
    streame_auto_variante: linear, ein try-Block um den Stream, ein Fallback-Zweig. Nach Umbau: keine Stream-emits in die Lese-Zone; entweder ganz nicht mehr aufgerufen (bevorzugt) oder gibt nur noch ein Erkennungs-Resultat zurueck.
    _qa_pipeline_dispatch: grosser try; reads per-SID state; klassifiziert; ruft _emit_qa_slot1/_emit_soft_hint/_emit_abstain_event. Die intent_event-/abstain-Logik (DSGVO/TAXO) bleibt UNANGETASTET — nur die zwei FE-Emits wechseln.
    Keyword-Pfad: in einer for-sid-Schleife im on_message-Handler; try/except pro sid. ewb_signal-Emit ist idempotent-sicher (nur ein room-emit).

## 3. Cross-File-Awareness
    grep-Belege (Executor verifiziert erneut via `grep -rn` vor Edit, Punkt 20):
    - 'qa_slot1' : claude_service.py (emit) + static/pip-launcher.js:2494 (Handler) → Handler wird in Task 2 umgebaut.
    - 'qa_soft_hint' : claude_service.py (emit) + static/pip-launcher.js:2523 (Handler) → Task 2.
    - 'streame_auto_variante' : claude_service.py:577 (def) + deepgram_service.py:261/267 (Aufruf) → hier geaendert.
    - 'streame_manual_ewb_variante' : claude_service.py:757 (def) + deepgram_service.py:979/983 (Aufruf, manual-Pfad) → nur source-Tag.
    - 'slot1_variant_busy_until' : claude_service.py:1533/1547/1656/1666 + deepgram_service.py:247/257 + live_session.py:143 → Lese-Zonen-Lock wird obsolet; NICHT global loeschen (live_session.py-Default bleibt als Foundation), nur die Auto-Set/Read im Lese-Zonen-Pfad entfernen. live_session.py NICHT in dieser Phase anfassen.
    - 'ewb_signal' : neu — darf vorher NICHT existieren (grep muss 0 Treffer zeigen ausser unseren neuen).

## 4. Edge-Case-Analyse
    - Auto-Signal ohne erkennbaren Typ (None/leer): KEIN ewb_signal emittieren (kein Geister-Button). Belegt: known=False nur bei verwertbarem Kurz-Label.
    - Profil ohne Einwaende: ewb_signal known=False → FE erzeugt neuen Button; wenn FE-seitig nicht moeglich, Signal verfaellt lautlos (lossy ist gewollt, Newest-wins).
    ### Race-Condition-Audit (4 Pflicht-Fragen, CLAUDE.md Punkt 14)
    1. **Gleichzeitiges Feuern manual vs auto:** Frueher schrieb Auto (streame_auto_variante/qa_slot1) denselben slot1 waehrend der manual-Stream lief → Ueberschreiben. NACH diesem Umbau schreibt Auto GAR NICHT mehr in slot1 (nur ewb_signal an die Button-Zone). Garantierter Zustand: der manual-Stream ist der einzige Lese-Zonen-Schreiber → kein Ueberschreiben strukturell moeglich. Der Lock wird damit unnoetig (entfernt). Das ist der Kern-Fix.
    2. **Verbindungsabbruch mid-Stream:** Bricht der manual-Stream ab, bleibt halbfertiger Text in slot1 — UNVERAENDERT zum Status quo (kein Regress). Auto schreibt nicht nach, also kein Cleanup-Konflikt. ewb_signal ist ein Einzel-Emit ohne Half-State.
    3. **Reconnect-Replay (alte SID / alter Socket / gepufferter Emit):** ewb_signal traegt keinen Lese-Zonen-Zustand → ein verspaeteter Replay-Emit highlightet hoechstens einen Button erneut (harmlos, Newest-wins in FE Task 2 begrenzt es auf Groesse 1). Ein verspaeteter alter qa_slot1 kann NICHT mehr feuern (Event existiert nicht mehr). room=sid sorgt fuer Adressierung an die aktuelle Session.
    4. **Tab im Hintergrund:** ewb_signal-Emits, die waehrend Hintergrund-Tab ankommen, treffen den Newest-wins-Puffer (Task 2) → beim Reaktivieren steht nur das juengste Highlight. Kein gestauter Text-Stream in der Lese-Zone (weil Auto dort nicht schreibt).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ewb_signal"' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep auf Prod-deployed Code: `inspect.sh grep "qa_slot1"` zeigt KEIN `sio.emit('qa_slot1'` mehr in claude_service.py (nur noch der FE-Handler-Umbau in Task 2 referenziert es ggf. als Legacy-Kommentar).
    - `inspect.sh grep "ewb_signal"` zeigt die neuen Emits in claude_service.py + deepgram_service.py.
    - Nach Deploy + Live-Cold-Call + EINEN Einwand-Knopf klicken + Antwort vorlesen: `inspect.sh logs 400 | grep "BUGB-EWB] MP4"` zeigt WAEHREND eines aktiven manual-Streams KEINEN `source=qa_slot1` / `source=streame_auto_variante`-Write mehr in slot1 (Auto-Write-Pfad ist tot). Belegt Item (a) + Bug-B-Fix strukturell.
  </acceptance_criteria>
  <done>Auto schreibt nie mehr in pip-slot-body-1; alle Auto-Quellen emittieren ewb_signal; manual-Pfad traegt source=manual_button; Lese-Zonen-Lock entfernt; bestaetigt per inspect.sh grep + Prod-Logs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FE — source-Gate auf die Lese-Zone + ewb_signal-Handler (highlight known / new button) + Newest-wins Groesse 1 + Coaching-Text raus</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js 2309-2415 (pip_stream_start / pip_token / pip_token_done — die Lese-Zonen-Schreiber, + _showProactiveContent-Aufruf 2408)
    - static/pip-launcher.js 2494-2544 (qa_slot1 + qa_soft_hint Handler — werden ersetzt)
    - static/pip-launcher.js 2213-2245 (_renderEwbButtons: Button-Schema <button class="pip-ewb-btn" data-typ="LABEL">)
    - static/pip-launcher.js 2746-2752 (_highlightEwbButton) + 2754-2779 (_showProactiveContent/_showProactiveTipp)
    - static/pip-launcher.js 2270-2289 (_triggerEwb-Ende: setzt slot1 placeholder + emit manual_ewb — der Pin-Start)
    - Nerve-Vault/03 Planung/NERVE_PiP_Overlay_Entscheidung_Umsetzung.md "Auto-Erkenner: bekannt vs. neu" + Checkliste Punkt 1
    - CLAUDE.md Punkt 14, "Keine hardcoded Dark-Mode-Farben + Inline-Styles" (neuer Button via CSS-Klasse, kein Inline-Hex), Punkt 27
  </read_first>
  <action>
    Drei Aenderungen, alle in static/pip-launcher.js (ASCII-Code-Identifier, Umlaute nur in User-sichtbarem Text — CLAUDE.md Umlaut-Regel):

    A) **Source-Gate auf die Lese-Zone (pip-slot-body-1):**
       In `pip_stream_start` (2309), `pip_token` (2351), `pip_token_done` (2367): wenn `slot === 1`, NUR dann in `pip-slot-body-1` schreiben, wenn `d.source === 'manual_button'`. Fehlt `d.source` oder ist es nicht 'manual_button' → den Lese-Zonen-Schreib-Zweig ueberspringen (discard). Das ist die zweite Verteidigungslinie zum BE-Cut (Task 1): selbst wenn je ein Auto-Stream-Emit durchkaeme, erreicht er die Lese-Zone nicht. slot 0 (Profil-Antwort A) bleibt unveraendert.

    B) **ewb_signal-Handler (NEU) — highlight known / create new + Newest-wins Groesse 1:**
       `state.socket.on('ewb_signal', function (d) {...})`:
       - Newest-wins-Puffer Groesse 1: ein Modul-State `state.lastAutoSignal` haelt NUR das juengste Signal. Vor dem Anwenden eines neuen Signals das vorige Highlight zuruecknehmen (vorigen Button entmarkieren). Kein Array, kein Stapeln.
       - **Gating (Kopplung an Plan 02):** wenn ein Pin aktiv ist (`state.pinActive === true`, von Plan 02 gesetzt), das Signal NUR puffern (in state.lastAutoSignal merken) und NICHT sofort anzeigen — beim Pin-Ende (Plan 02 ruft einen Re-Apply-Hook) wird das juengste gepufferte Signal angewandt. In DIESEM Plan: den Hook `_applyBufferedAutoSignal()` definieren + den `state.pinActive`-Check einbauen (Default false, bis Plan 02 ihn echt setzt — solange verhaelt es sich wie "kein Gating", was fuer Plan 01 allein korrekt ist).
       - **known (Button existiert):** finde in `nlp-ewb-row` einen `.pip-ewb-btn` mit `data-typ` das `d.typ` matched (gleiche lower/trim-Chain wie _triggerEwb). Treffer → `_highlightEwbButton(d.typ)` (leuchtet auf). 
       - **neu (kein Button):** kein Treffer (oder d.known===false) → einen zusaetzlichen Button an reservierter Position in `nlp-ewb-row` erzeugen: `<button class="pip-ewb-btn pip-ewb-auto-new" data-typ="LABEL">LABEL</button>` (CSS-Klasse, KEIN Inline-Hex — neue Token bei Bedarf in nerve.css, CLAUDE.md). Dann diesen neuen Button highlighten. Dedup: existiert schon ein Auto-Button mit gleichem data-typ, nur re-highlighten. Max-Begrenzung beachten (die bestehende 5-Button-Logik in _renderEwbButtons nicht sprengen — neuer Auto-Button zaehlt extra, aber Newest-wins haelt es bei 1 aktiven Auto-Button: vorigen Auto-new-Button entfernen bevor ein neuer entsteht).
       - Der neue Button ist voll klickbar (laeuft ueber die bestehende `.pip-ewb-btn`-Delegation in _wirePipButtons:1802 → _triggerEwb). Damit wird aus einem Auto-Hinweis bei Klick ein echter Pin (Plan 02).

    C) **Coaching-Text raus aus der Lese-Zone (Item d, Rest-Pfad):**
       `_showProactiveContent`-Aufruf in `pip_token_done` (2408): dieser schreibt Phase/Kaufbereitschaft/Tipp in `pip-slot-body-(1-slot)` — kann slot 1 (Lese-Zone) treffen. ENTFERNEN des Aufrufs (2406-2409) ODER so absichern, dass `_showProactiveContent`/`_showProactiveTipp` NIE in slot 1 schreibt (Guard: if targetSlot===1 return). Bevorzugt: Aufruf entfernen (Coaching gehoert per Design NICHT in die Lese-Zone; volle Coaching-Symbolleiste ist PIP.2). `_showProactiveContent`/`_showProactiveTipp`-Funktionsdefinitionen koennen bleiben (von PIP.2 evtl. genutzt) — als Foundation markieren ODER toten Aufruf prunen (Punkt 3a Pruning-Notiz).

    Legacy-Handler `qa_slot1` (2494) + `qa_soft_hint` (2523): da das BE-Emit (Task 1) entfaellt, feuern sie nicht mehr. Entweder Handler entfernen (sauber) ODER als No-Op mit Kommentar belassen. Bevorzugt entfernen (Pruning-Notiz). Wenn entfernt: sicherstellen dass kein anderer Code `qa_slot1`/`qa_soft_hint`-Emits erwartet (grep zeigt nur BE-Emits, die in Task 1 weg sind).

## 1. Lokaler Kontext
    pip_stream_start (2309-2349): liest d.slot (default 0); setzt body.textContent=''; pip-streaming-Klasse; replace_all-Zweig clear beide Slots. Kein early return vor dem slot-1-Write. Source-Gate: vor dem body-Schreiben fuer slot 1 die source pruefen.
    pip_token (2351-2365): early return wenn slot nicht streaming (2354). raw_text → body.textContent = akku. Source-Gate hier ebenso.
    pip_token_done (2367-2415): setzt streaming=false; _renderSlotResult; _showProactiveContent-Aufruf 2408 (zu entfernen). 
    ewb_signal-Handler: NEU, neben den anderen state.socket.on(...)-Registrierungen in _registerSocketEvents.

## 2. Funktions-Skelett
    _registerSocketEvents (umschliesst alle state.socket.on): linear, ein on() pro Event. Neuer ewb_signal-on() additiv. Keine return-Fallen.
    _highlightEwbButton (2746): row-Lookup, querySelectorAll, toggle. Re-used.
    Neuer-Button-Erzeugung: row.insertAdjacentHTML('beforeend', ...) + danach delegation greift automatisch (document-level Listener, kein Re-Wire noetig — verifiziert _wirePipButtons:1787 ist document-level).

## 3. Cross-File-Awareness
    - 'pip-slot-body-1' : nur static/pip-launcher.js (mehrere Stellen) — Lese-Zone ist FE-only. Source-Gate betrifft nur die slot-1-Zweige.
    - 'ewb_signal' : BE-Emit (Task 1) + neuer FE-Handler. Vertrag muss feldgleich sein (typ/known/source).
    - '.pip-ewb-btn' / 'nlp-ewb-row' / 'data-typ' : Button-Schema, geteilt mit _renderEwbButtons + _triggerEwb + Delegation. Neuer Auto-Button muss exakt diesem Schema folgen, sonst greift die Klick-Delegation nicht.
    - '_showProactiveContent' / '_showProactiveTipp' : nur pip-launcher.js. Aufruf-Entfernung lokal.
    - 'pip-ewb-auto-new' : neue CSS-Klasse — in static/nerve.css ergaenzen falls visuelle Differenzierung gewuenscht (kein Inline-Hex, CLAUDE.md). Optional; ohne eigenen Stil sieht der Auto-Button aus wie ein normaler EWB-Button (akzeptabel fuer PIP.1; volles Einblend-Design = PIP.2).

## 4. Edge-Case-Analyse
    - ewb_signal ohne typ: ignorieren (kein Geister-Button/-Highlight).
    - Doppeltes Signal gleicher typ: Newest-wins → re-highlight desselben Buttons, kein Duplikat-Button (Dedup per data-typ).
    - Profil-Button + Auto-new-Button mit gleichem Label: known-Pfad greift zuerst (bestehender Button gewinnt), kein neuer.
    ### Race-Condition-Audit (4 Pflicht-Fragen)
    1. **Gleichzeitiges Feuern manual vs auto:** manual-Stream schreibt slot 1 (source=manual_button passt Gate). Gleichzeitiges ewb_signal beruehrt NUR die Button-Zone (highlight), nie slot 1 → kein DOM-Konflikt am Lese-Node. Garantiert: Lese-Zone gehoert exklusiv dem manual-Stream.
    2. **Verbindungsabbruch mid-Stream:** halbfertiger manual-Text bleibt (Status quo). ewb_signal-Puffer (Groesse 1) ist reiner FE-State, kein Half-DOM.
    3. **Reconnect-Replay:** ein alter, verspaeteter Stream-Emit ohne source / mit auto-source wird vom Source-Gate verworfen → kann die Lese-Zone nicht mehr ueberschreiben (zweite Verteidigungslinie). Ein alter ewb_signal highlightet hoechstens erneut (Newest-wins begrenzt).
    4. **Tab im Hintergrund:** ankommende ewb_signal sammeln sich nicht — nur state.lastAutoSignal (Groesse 1) ueberlebt; beim Reaktivieren ein Highlight, kein Stau. Stream-Tokens fuer slot 1 ohne source verfallen am Gate.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "source === '\''manual_button'\''"' | head; ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ewb_signal"' | head</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh grep "source"` in static/pip-launcher.js zeigt den manual_button-Gate-Check in den slot-1-Zweigen von pip_stream_start/pip_token.
    - `inspect.sh grep "ewb_signal"` zeigt den neuen FE-Handler.
    - Live-Cold-Call (checkpoint, Task 3): Auto-Erkennung erscheint sichtbar NUR als Button-Highlight / neuer Button; KEIN Text taucht in der Antwort-Zone (slot 1) auf ausser dem geklickten Vorschlag. Coaching-Text (Phase/Kaufbereitschaft/Tipp) erscheint nicht in slot 1.
    - Browser-Konsole zeigt KEINE qa_slot1/qa_soft_hint-Render-Logs mehr (Handler entfernt oder no-op).
  </acceptance_criteria>
  <done>Lese-Zone wird FE-seitig nur noch von source=manual_button beschrieben; ewb_signal highlightet bekannten / erzeugt neuen Button mit Newest-wins Groesse 1; Coaching-Text-Aufruf in slot 1 entfernt; Gating-Hook fuer Plan 02 vorbereitet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-Cold-Call Verifikation (Prod) — Anzeige-Trennung + kein Ueberschreiben + Coaching raus</name>
  <what-built>
    Auto-Erkenner schreibt nicht mehr in die Lese-Zone (BE-Cut + FE-Source-Gate); Auto erscheint nur als Button-Highlight/neuer Button (Newest-wins 1); Coaching-Text raus aus slot 1. Deploy laeuft ueber `bash deploy.sh production` (Kein-Local-Dev, CLAUDE.md). Der Pytest-Gate in deploy.sh laeuft serverseitig vor dem Restart.
  </what-built>
  <how-to-verify>
    1. `bash deploy.sh production` (Auto-Pytest-Gate + Restart; bei Test-Fehler kein Restart).
    2. Im Browser mit Test-User auf getnerve.app einen Cold-Call starten (Headset-Modal bestaetigen).
    3. EINEN Einwand-Knopf klicken → Antwort erscheint in der Antwort-Zone.
    4. Die Antwort LANGSAM laut vorlesen (Selbst-Trigger provozieren). Beobachten:
       - Bleibt der geklickte Vorschlag-Text waehrend des Vorlesens STEHEN (wird NICHT ueberschrieben)? (erwartet: ja)
       - Erscheint eine Auto-Erkennung als Button-Highlight oder neuer Button (nicht als Text in der Antwort-Zone)? (erwartet: ja)
       - Taucht Coaching-/Phasen-/Kaufbereitschafts-Text in der Antwort-Zone auf? (erwartet: nein)
    5. Logs ziehen: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 500'` | grep "BUGB-EWB] MP4" → waehrend des aktiven manual-Streams KEIN auto-source-Write in slot1.
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn der Vorschlag stehen bleibt + Auto nur als Button erscheint + kein Coaching-Text in der Zone — sonst beschreibe das beobachtete Verhalten + relevante MP4/MP6-Logzeilen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Deepgram STT → BE (deepgram_service) | Transkribierter Berater-Text (kann beim Vorlesen NERVEs eigene Antwort enthalten = Selbst-Trigger-Quelle) |
| BE (claude_service/deepgram_service) → FE (Socket.IO room=sid) | ewb_signal / pip_stream-Events; FE muss Quelle (source) pruefen bevor Lese-Zone beschrieben wird |
| FE Auto-erzeugter Button → manual_ewb-Emit | Auto-Hinweis wird erst durch Berater-Klick zum Pin (kein Auto-Pin) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PIP-01 | Tampering | Auto-Stream-Emit erreicht Lese-Zone trotz Cut | mitigate | FE-Source-Gate: nur source=manual_button schreibt pip-slot-body-1 (Task 2 A) — zweite Verteidigungslinie zum BE-Cut |
| T-PIP-02 | Spoofing | verspaeteter Reconnect-Replay-Emit (alte SID) ueberschreibt aktiven Pin | mitigate | room=sid-Adressierung + Source-Gate; alte qa_slot1-Events existieren nicht mehr |
| T-PIP-03 | Information Disclosure | neuer Auto-Button-Text leakt Kundennamen/Firma (DSGVO) | mitigate | BE liefert nur anonymisierten Kurz-Text/Label fuer den neuen Button (anonymize_output-Pfad, vorhanden); kein Roh-STT ins Label |
| T-PIP-04 | Denial of Service | Auto-Signal-Flut erzeugt viele Buttons | accept | Newest-wins-Puffer Groesse 1 begrenzt aktiven Auto-Button auf 1 (Task 2 B); lossy by design |
</threat_model>

<verification>
- inspect.sh grep bestaetigt: kein qa_slot1/qa_soft_hint-BE-Emit mehr; ewb_signal vorhanden BE+FE; Source-Gate im FE.
- Prod-Live-Cold-Call (Task 3): Vorschlag bleibt stehen, Auto nur als Button, kein Coaching-Text in der Zone.
- Prod-Logs (MP4): kein auto-source-Slot1-Write waehrend aktivem manual-Stream.
</verification>

<success_criteria>
- (a) Auto-Erkenner schreibt strukturell nie mehr in die Lese-Zone — bewiesen per Code-grep (kein Auto-Emit-Pfad) UND Prod-Log (kein MP4 auto-source-Write).
- (d) Coaching-Text erscheint nicht mehr in der Lese-Zone (Aufruf entfernt).
- Newest-wins Groesse 1 fuer Auto-Highlights.
- Bug B (Ueberschreiben im Vorlesen) im Live-Re-Test nicht mehr reproduzierbar.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-01-SUMMARY.md`. Pflicht: Pruning-Notiz (streame_auto_variante / qa_slot1-Handler / _showProactiveContent-Aufruf: geloescht ODER Foundation-Register-Eintrag fuer PIP.4) + Vermerk dass der slot1_variant_busy_until-Lese-Zonen-Lock obsolet wurde.
</output>
