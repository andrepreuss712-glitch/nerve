---
phase: 08.23.2.PIP
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - services/deepgram_service.py
autonomous: false
requirements: [e]
complexity: "mittel"
cross_ai_required: true
atomic_commit: true
note_wave: "Wave 2 wegen Datei-Kollision mit Plan 01 auf services/deepgram_service.py (anderer Bereich). depends_on:[01] macht die Nach-Plan-01-Sequenzierung explizit und verhindert jede Parallelisierung der beiden deepgram_service.py-Edits. Logisch unabhaengig, ABER: MUSS ein EIGENER, isolierter Commit sein (CLAUDE.md Punkt 17) — nur die deepgram_service.py-Datei ist geteilt, der Commit bleibt getrennt vom Anzeige-Trennungs-Fix."

must_haves:
  truths:
    - "Knopf-Transkript-Eintraege bekommen ts_ms != 0 (kein Format-Mismatch mehr)."
    - "Es entstehen keine unmoeglichen Folge-Timestamps (z.B. ~17h / 61019000ms) mehr, wenn der erste Transcript-Entry ein Knopf ist."
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "Knopf-Transcript-Zeile schreibt ts im selben 'HH:MM:SS'-Format wie die gesprochene Zeile"
      contains: "strftime"
  key_links:
    - from: "services/deepgram_service.py Knopf-Transcript-Append (ts)"
      to: "routes/app_routes.py _ts_to_ms_of_day (Transform)"
      via: "ein einheitliches ts-Format ('HH:MM:SS')"
      pattern: "strftime"
---

<objective>
Zeitstempel-Bug C (Item e) als SEPARATER atomarer Commit, isoliert vom Anzeige-Trennungs-Fix (CLAUDE.md Punkt 17). Ein Format-Mismatch, zwei Symptome: Knopf-Transkript-Eintraege schreiben `ts` als float-epoch (`time.time()`), aber `_ts_to_ms_of_day` erwartet `'HH:MM:SS'` -> `str(ts).split(':')` ohne ':' -> ValueError -> `ts_ms=0`. Folge-Muell: ist der erste Transcript-Entry ein Knopf (abs=0 -> base=0), ergibt eine spaetere gesprochene Zeile `ts_ms` = absolute Tageszeit (~61019000 ≈ 17h, unmoeglich) -> Judge-Sortierung nach ts_ms ASC bricht.

Purpose: ein einheitliches Zeitformat -> kein ts_ms=0 mehr, korrekte Transkript-Reihenfolge fuer den Post-Call-Judge (Trainings-/Bewertungs-Datenqualitaet).

Output: Knopf-Pfad schreibt ts im selben `'HH:MM:SS'`-Format wie der gesprochene Pfad. EIN isolierter Commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md
@.planning/debug/ewb-pip-overwrite-multifire.md
@CLAUDE.md

<interfaces>
<!-- Verifizierte Anker (aus dem Code gelesen): -->
  services/deepgram_service.py
  - Gesprochene Transcript-Zeile (66): ts = datetime.now().strftime('%H:%M:%S')   (str — der KORREKTE Standard)
  - Knopf-Transcript-Zeile (950-956): ls.conversation_log.append({'ts': time.time(), 'type':'transcript', 'speaker':1, 'text': f"{_anon_typ} *ewb button*", 'data':{'ewb_button':True}})  (float-epoch — der BUG)
  - `datetime` ist im Modul importiert (Zeile 66 nutzt es bereits); `time` ebenfalls (time.time()).

  routes/app_routes.py
  - _ts_to_ms_of_day(ts_str) (36): h,m,s = int(x) for x in str(ts_str).split(':'); (h*3600+m*60+s)*1000; except -> 0
  - _transcript_entries_to_segments (45): nutzt _ts_to_ms_of_day pro Entry; base = erster abs; rel = abs-base (clamp non-decreasing). MP6-Print (74) loggt btn/raw_ts_type/abs/rel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Knopf-Transcript-Zeile schreibt ts im 'HH:MM:SS'-Format (ein Format, kein Vermischen)</name>
  <files>services/deepgram_service.py</files>
  <read_first>
    - services/deepgram_service.py 60-67 (gesprochene Zeile: ts = datetime.now().strftime('%H:%M:%S') — das Zielformat)
    - services/deepgram_service.py 944-958 (Knopf-Transcript-Append mit ts=time.time() — die zu fixende Stelle)
    - routes/app_routes.py 36-81 (_ts_to_ms_of_day + _transcript_entries_to_segments — Konsument, NICHT aendern)
    - .planning/debug/ewb-pip-overwrite-multifire.md "Lead C (ts_ms=0) — STATISCH BESTAETIGT"
    - .planning/phases/08.23.2.PIP-.../08.23.2.PIP-CONTEXT.md <decisions> (e)
    - CLAUDE.md Punkt 14 (Control-Flow + Race-Fragen), Punkt 17 (kein Refactor-Beifang), Punkt 20 (grep), Punkt 21/26 (Persistenz/Schreibzeitpunkt)
  </read_first>
  <action>
    Fix-Wahl (vom Planner getroffen, CONTEXT (e) gibt zwei Optionen): **Knopf-Pfad schreibt ts im selben Format wie der gesprochene Pfad** — NICHT die Transform float-epoch-tolerant machen. Begruendung: (1) ein Format an der Quelle ist die ehrlichere Normalisierung (beide Transcript-Eintraege tragen dann denselben Typ); (2) `_ts_to_ms_of_day` bleibt simpel und einheitlich (kein Zweig fuer zwei Formate -> kein widerspruechlicher Doppel-Pfad, CONTEXT "nicht beide Pfade widerspruechlich lassen"); (3) der gesprochene Pfad (strftime '%H:%M:%S') ist bereits der gelebte Standard, dem sich der Knopf-Pfad anpasst (Punkt 22: was lebt, gewinnt).

    Konkret: in services/deepgram_service.py, der `ls.conversation_log.append({...})` fuer die Knopf-Zeile (aktuell ~952): `'ts': time.time()` ersetzen durch `'ts': datetime.now().strftime('%H:%M:%S')` — exakt der Ausdruck aus Zeile 66. KEINE weitere Aenderung an diesem append (speaker, text, data bleiben). `datetime` ist bereits importiert (Zeile 66 belegt das) — falls der grep zeigt dass `datetime` an dieser Stelle im Scope nicht verfuegbar ist (anderer Funktions-Scope), den vorhandenen Modul-Import nutzen (kein neuer Import-Block, kein Refactor).

    NICHT aendern: `_ts_to_ms_of_day` und `_transcript_entries_to_segments` in app_routes.py (der Konsument ist korrekt fuer 'HH:MM:SS'). KEINE anderen ts-Schreibstellen anfassen (Punkt 17 — nur der Knopf-Bug-Pfad). Der Coaching-Pfad (claude_service.py:1796 latenz/tipp ts=strftime) ist bereits korrekt und wird NICHT beruehrt.

    Dies ist ein EIGENER, isolierter Commit (atomic_commit:true) — er darf NICHT mit dem Anzeige-Trennungs-Diff (Plan 01) im selben Commit landen, auch wenn beide deepgram_service.py beruehren. (depends_on:[01] sorgt nur fuer die Reihenfolge — Plan 03 wird NACH Plan 01 ausgefuehrt, bekommt aber seinen EIGENEN Commit; die geteilte Datei rechtfertigt KEIN Commit-Zusammenlegen.) Commit-Message-Vorschlag: `fix(08.23.2.PIP): button-transcript ts as HH:MM:SS (ts_ms=0 fix C)`.

## 1. Lokaler Kontext
    Knopf-Append (944-958): nach dem Anon-Sentinel-Check (937-940, verworfen wenn Sentinel) folgt der else-Zweig (941+) mit `with ls.log_lock: ls.conversation_log.append({...'ts': time.time()...})` (950-956), gefolgt von except (957). Kein return zwischen Anon-Check und Append im else-Zweig. Die Aenderung ist eine reine Wert-Ersetzung im Dict-Literal — keine Control-Flow-Verschiebung.
    Gesprochene Append (vergleichbar, oben im on_message-Pfad) nutzt dasselbe log_lock + dasselbe ts-Format — Konsistenz-Ziel.

## 2. Funktions-Skelett
    Die Knopf-Logik sitzt in der manual_ewb-Empfangs-/Verarbeitungs-Funktion (handle_manual_ewb-Umgebung). try/except umschliesst den Append (kein Crash des Live-Loops). Ein Exit-Pfad: append oder skip (Sentinel/except). Die Aenderung beruehrt keinen Exit-Pfad.

## 3. Cross-File-Awareness
    grep-Belege (Executor verifiziert via grep vor Edit):
    - 'time.time()' in deepgram_service.py: pruefen welche Vorkommen ts-Transcript-relevant sind. NUR die Knopf-Transcript-Zeile (mit `*ewb button*`/`'type':'transcript'`/`'data':{'ewb_button':True}`) aendern. Andere time.time()-Nutzungen (Latenz-Messung, Timeouts) NICHT anfassen.
    - '_ts_to_ms_of_day' : routes/app_routes.py:36 (def) + 61 (Aufruf). Konsument — NICHT aendern, aber als Beleg lesen dass 'HH:MM:SS' korrekt geparst wird.
    - 'strftime' : deepgram_service.py:66 + claude_service.py:1796 (beide korrekt) — Zielformat-Vorlage.
    - "*ewb button*" : deepgram_service.py (Schreibstelle) + app_routes.py:74 (MP6 btn-Erkennung) + Reader-Pfade. Bestaetigt die Knopf-Zeile eindeutig.

## 4. Edge-Case-Analyse
    - Mitternachts-Wrap: 'HH:MM:SS' verliert das Datum -> ein Call ueber Mitternacht koennte rel negativ machen. _transcript_entries_to_segments clamped bereits non-decreasing (running, Zeile 65-67) -> bestehende Schutzlogik faengt das, UNVERAENDERT. Kein neuer Edge entsteht durch den Fix (der gesprochene Pfad hat dasselbe Verhalten schon immer).
    - Erster Entry ist ein Knopf: nach dem Fix traegt er eine echte 'HH:MM:SS' -> abs = echte ms-of-day, base = dieser Wert -> spaetere Zeilen rel = klein/korrekt. Der unmoegliche 61019000-Wert verschwindet (war Folge von base=0).
    ### Race-Condition-Audit (4 Pflicht-Fragen)
    1. Gleichzeitiges Feuern: mehrere manual_ewb (Mehrfach-Feuern, Hypothese A) -> mehrere Knopf-Appends, jeder mit korrektem strftime-ts. Reihenfolge = Append-Reihenfolge (Listenordnung autoritativ, app_routes:49). Kein ts-Konflikt. (Mehrfach-Feuern selbst ist Plan-04-Thema, nicht hier.)
    2. Verbindungsabbruch mid-Stream: der Append passiert synchron im Empfangs-Handler vor dem Stream-Start -> ein spaeterer Abbruch beeinflusst den bereits geschriebenen ts-Eintrag nicht.
    3. Reconnect-Replay: ein doppelt empfangener manual_ewb erzeugt zwei Appends mit (fast) gleichem ts -> harmlos fuer ts_ms (gleiche/monoton-geklemmte Werte). Kein Bruch.
    4. Tab im Hintergrund: irrelevant — ts wird serverseitig (deepgram_service) bei Empfang gesetzt, nicht clientseitig; Tab-Pause des Clients aendert den Server-ts nicht.
    ### Schreib-Zeitpunkt-Naht (CLAUDE.md Punkt 26)
    Der ts wird LIVE beim manual_ewb-Empfang geschrieben (nicht Batch). Der Konsument `_transcript_entries_to_segments` laeuft am Call-Ende/Export ueber die fertige RAM-Liste -> liest nie vor dem Schreiben (alle Eintraege stehen schon). Kein Vorlauf-Risiko. (Hinweis: die transcript_segments-DB-Persistierung ist Batch am Call-Ende — aber dieser Fix betrifft nur das ts-FORMAT in der RAM-conversation_log, nicht den Schreib-Zeitpunkt.)
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ewb button"'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh grep "ewb button"` / die Knopf-Append-Zeile in deepgram_service.py zeigt `'ts': datetime.now().strftime('%H:%M:%S')` statt `time.time()`.
    - Nach Deploy + Live-Cold-Call mit EINEM Einwand-Knopf: `inspect.sh logs 500 | grep "BUGB-EWB] MP6"` zeigt fuer die Knopf-Zeile `raw_ts_type=str` (nicht float) und `rel_ts_ms != 0`. Keine MP6-Zeile mit `abs_ms`~61019000 (~17h).
    - Optional Datenbeleg: `inspect.sh sample transcript_segments 50` fuer den Test-Call -> Knopf-Segmente haben ts_ms != 0 und keine unmoegliche Folge-Zeit.
  </acceptance_criteria>
  <done>Knopf-Transcript-ts ist im 'HH:MM:SS'-Format; ts_ms != 0 fuer Knopf-Events; kein unmoeglicher Folge-Timestamp; isolierter eigener Commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-Beleg (Prod) — ts_ms != 0 fuer Knopf-Events</name>
  <what-built>
    Knopf-Transcript-ts vereinheitlicht auf 'HH:MM:SS'. Deploy via `bash deploy.sh production`. Eigener isolierter Commit.
  </what-built>
  <how-to-verify>
    1. `bash deploy.sh production`.
    2. Cold-Call mit Test-User, EINEN Einwand-Knopf klicken, danach 1-2 Saetze sprechen, Call beenden.
    3. `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 500'` | grep "BUGB-EWB] MP6" -> Knopf-Zeile: raw_ts_type=str, rel_ts_ms != 0; keine ~17h-Werte.
    4. Optional: `inspect.sh sample transcript_segments 50` -> Knopf-Segmente ts_ms != 0.
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn Knopf-Events ts_ms != 0 haben und keine unmoeglichen Folge-Timestamps auftauchen — sonst die MP6-Logzeilen posten.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| BE Schreib-Pfad (Knopf-ts) -> Post-Call-Transform (_ts_to_ms_of_day) | Format-Vertrag 'HH:MM:SS' muss zwischen Schreiber und Leser konsistent sein |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PIP-08 | Tampering | inkonsistentes ts-Format bricht Judge-Sortierung (Datenqualitaet) | mitigate | Ein Format an der Quelle ('HH:MM:SS'); Transform unveraendert; Live-MP6-Beleg |
| T-PIP-09 | Information Disclosure | ts-Aenderung leakt Klartext | accept | ts ist eine Uhrzeit, kein PII; Knopf-Text bleibt anonymisiert (_anon_typ) |
</threat_model>

<verification>
- grep: Knopf-Append nutzt strftime('%H:%M:%S'); _ts_to_ms_of_day unveraendert.
- Prod-MP6-Logs: Knopf raw_ts_type=str, rel_ts_ms != 0; kein 61019000-Wert.
- Eigener isolierter Commit (kein Vermischen).
</verification>

<success_criteria>
- (e) Knopf-Events ts_ms != 0; keine unmoeglichen Folge-Timestamps; ein Format.
- Atomarer eigener Commit, getrennt vom Anzeige-Trennungs-Fix (Punkt 17).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-03-SUMMARY.md`. Pflicht: Commit-Hash des isolierten ts-Fix-Commits vermerken (Beleg fuer atomare Trennung).
</output>
</task>
</tasks>
