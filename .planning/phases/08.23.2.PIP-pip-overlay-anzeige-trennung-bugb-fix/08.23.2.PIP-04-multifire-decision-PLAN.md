---
phase: 08.23.2.PIP
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - static/pip-launcher.js
autonomous: false
requirements: [a]
complexity: "mittel"
cross_ai_required: true

must_haves:
  truths:
    - "Es ist anhand der Prod-Logs (MP1/MP2 click_id-Abgleich) bewusst entschieden, ob Mehrfach-Feuern in PIP.1 mitgefixt oder als separater Punkt notiert wird."
    - "Falls die Anzeige-Trennung Mehrfach-Feuern bereits unkritisch macht, ist das mit Begruendung dokumentiert (nicht still gedroppt)."
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "FALLS Fix gewaehlt: click_id-Dedup im manual_ewb-Emit-Pfad (FE) — sonst keine Code-Aenderung, nur dokumentierte Entscheidung"
  key_links:
    - from: "Prod-Logs MP1 (FE-Emits) vs MP2 (BE-Recv) click_id"
      to: "Entscheidung Fix-jetzt vs separater Punkt"
      via: "Reconciliation gleiche/verschiedene click_id"
      pattern: "click_id"
---

<objective>
Mehrfach-Feuern der Einwand-Knoepfe (Hypothese A aus der Diagnose: 16x/6x identische Events pro Klick) bewusst entscheiden — NICHT still droppen (CONTEXT Claude's-Discretion). Per Instrumentierungs-Logs (MP1 FE-Emit click_id vs MP2 BE-Recv click_id) klaeren: ist ein physischer Klick = N Emits mit GLEICHER click_id (FE-Doppel-Bind/Bubbling) ODER N verschiedene click_ids (Reconnect-Replay / Mehrfach-Klick)? Dann entscheiden: in PIP.1 mitfixen (click_id-Dedup) ODER als separaten Punkt notieren, falls die Anzeige-Trennung (Plan 01) + Pin-Gating (Plan 02) das Symptom bereits unkritisch machen.

Purpose: keine offene Flanke. Die Diagnose nennt Mehrfach-Feuern explizit; es darf nicht unbeantwortet bleiben. Aber: ueber-eilig fixen waere Punkt-27-Verstoss, wenn die Anzeige-Trennung es schon harmlos macht.

Output: eine dokumentierte Entscheidung (im SUMMARY) + FALLS noetig ein minimaler FE-Dedup; sonst ein Backlog-/Folgephasen-Vermerk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md
@.planning/debug/ewb-pip-overwrite-multifire.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-01-anzeige-trennung-PLAN.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-02-pin-lifecycle-gating-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Verifizierte Anker: -->
  static/pip-launcher.js
  - _triggerEwb (2247) Ende (2280-2288): generiert _bugbClickId (clk_<ts>_<rand>) SYNCHRON vor dem emit('manual_ewb',{...,click_id}). MP1-Print. (Plan 02 setzt hier auch den Pin via pinActive + _renderSlotResult.)
  - _wirePipButtons (1783-1819): document-level Delegation; .closest('.pip-ewb-btn') (1802) -> ev.preventDefault + ev.stopPropagation + _triggerEwb (1810). EIN Listener pro PiP-Fenster.
  Diagnose-Lead A (debug-Doc, Zeile 64): _wirePipButtons laeuft GENAU EINMAL pro PiP-Fenster; Delegation robust gegen Re-Render -> 16x/6x ist WAHRSCHEINLICH KEIN reines FE-Bubbling, eher Reconnect-Replay (Socket re-emit nach Drop) oder mehrfache physische Klicks bei haengender UI.
  services/deepgram_service.py
  - handle_manual_ewb (~852): MP2-Print (sid, click_id, recv_ts).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Logs ziehen + Entscheidung Fix-jetzt vs separater Punkt</name>
  <decision>
    Wird Mehrfach-Feuern in PIP.1 mitgefixt (FE click_id-Dedup) oder als separater Punkt notiert, weil die Anzeige-Trennung (Plan 01) + Pin-Gating (Plan 02) es bereits unkritisch machen?
  </decision>
  <context>
    Voraussetzung: Plan 01 + 02 sind live (depends_on). Ziehe die Prod-Logs eines frischen Cold-Call-Tests mit EINEM Knopf-Klick und gleiche MP1 (FE-Emit click_id) gegen MP2 (BE-Recv click_id) ab:
      `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 600'` | grep -E "BUGB-EWB] MP1|BUGB-EWB] MP2"

    Auswertung (Lead A):
      - GLEICHE click_id N-mal am BE (MP2) -> FE feuert mehrfach pro Klick (Doppel-Bind/Bubbling/Reconnect-Replay desselben Emits) -> FE-Dedup sinnvoll.
      - N VERSCHIEDENE click_ids -> N physische Trigger (Mehrfach-Klick bei haengender UI / Reconnect-Replay neuer Emits) -> FE-Dedup hilft nur teilweise; ggf. BE-idempotenter Empfang.
      - 1 click_id, 1 Recv -> Mehrfach-Feuern war ein Symptom der alten Lese-Zonen-Race und ist mit Plan 01/02 bereits weg -> KEIN Code-Fix, nur dokumentieren.

    Wichtige Vor-Ueberlegung (im Entscheidungs-Bericht ausfuehren): Nach Plan 01 schreibt Auto NIE mehr in die Lese-Zone, und mehrere manual-Streams landen alle im selben slot 1 (letzter gewinnt, kein Auto dazwischen). D.h. fuer die ANZEIGE ist Mehrfach-Feuern nach Plan 01/02 weitgehend harmlos. ABER: jeder manual_ewb-Emit kann (a) einen separaten Haiku-Stream + Kosten ausloesen (Latenz/Kosten, CLAUDE.md Punkt 25 + Marge Punkt 12) und (b) mehrere intent_event/Transcript-Zeilen schreiben (Datenqualitaet — die 16x identischen transcript_segments aus der Diagnose). Diese zwei Effekte bleiben auch bei sauberer Anzeige bestehen. Die Entscheidung MUSS diese beiden Nebenwirkungen explizit bewerten, nicht nur die Anzeige.
  </context>
  <options>
    <option id="fix-now">
      <name>FE click_id-Dedup jetzt (Task 2 ausfuehren)</name>
      <pros>Schliesst Kosten-/Datenqualitaets-Flanke (Doppel-Streams, 16x-Transcript-Spam) sofort; billiger FE-Guard.</pros>
      <cons>Mehr Diff in PIP.1; wenn die Wurzel Reconnect-Replay ist, faengt FE-Dedup nicht alles (BE-Idempotenz waere noetig -> eigener Punkt).</cons>
    </option>
    <option id="defer">
      <name>Als separaten Punkt notieren (kein Code in PIP.1)</name>
      <pros>PIP.1 bleibt fokussiert auf die Anzeige-Trennung; wenn Logs zeigen dass Mehrfach-Feuern selten/harmlos ist, kein voreiliger Fix (Punkt 27).</pros>
      <cons>Kosten-/Datenqualitaets-Flanke bleibt offen bis zur Folgephase.</cons>
    </option>
  </options>
  <resume-signal>Waehle: fix-now (dann Task 2) oder defer (dann Task 2 ueberspringen, Backlog-Eintrag im SUMMARY). Begruende mit den MP1/MP2-Logbefunden + der Kosten-/Datenqualitaets-Bewertung.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: (NUR falls fix-now) Minimaler FE click_id-Dedup im manual_ewb-Pfad</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js 2247-2289 (_triggerEwb + click_id-Generierung + emit; Plan-02-Stand: pinActive-Set + _renderSlotResult VOR dem emit)
    - static/pip-launcher.js 1783-1819 (_wirePipButtons Delegation)
    - .planning/debug/ewb-pip-overwrite-multifire.md "Lead A"
    - CLAUDE.md Punkt 14 (Race-Fragen) + Punkt 17 (kein Refactor-Beifang) + Punkt 25 (Latenz) + Punkt 27
  </read_first>
  <action>
    NUR ausfuehren wenn Task 1 = fix-now. Minimaler FE-Dedup, abgestimmt auf den in Task 1 ermittelten Mechanismus:

    - Bei "gleiche click_id mehrfach" (FE-seitige Dopplung): einen kurzen Sende-Throttle/Dedup im manual_ewb-Emit-Pfad. Z.B. `state.lastEwbEmit = {typ, at}` und in _triggerEwb: wenn derselbe `typ` innerhalb eines kleinen Fensters (z.B. 800ms) erneut feuert, den zweiten emit unterdruecken (nur EIN manual_ewb pro physischem Klick). Das ist KEIN Timer-Pin (Item c unberuehrt) — nur ein Emit-Entprellen.
    - Bei "mehrere physische Klicks bei haengender UI": dasselbe Entprell-Fenster hilft (Doppelklick-Schutz).
    - Bei "Reconnect-Replay neuer click_ids": FE-Dedup greift nicht vollstaendig -> im SUMMARY als BE-Idempotenz-Folgepunkt notieren (BE handle_manual_ewb koennte click_id-de-dupen), NICHT in PIP.1 bauen (separater Punkt, Punkt 27).

    **HARTE EXECUTOR-ANWEISUNG (Cross-AI Executor-Suggestion — Debounce-Platzierung, PFLICHT bei fix-now):**
    Platziere die Debounce-/Dedup-Logik im _triggerEwb-Pfad NACH den UI-Updates, aber VOR dem `socket.emit('manual_ewb', ...)`. Konkrete Reihenfolge im _triggerEwb-Erfolgspfad:
      1. ZUERST die UI aktualisieren — `_renderSlotResult(...)` + slot1-Placeholder + `state.pinActive = true`/`state.pinTyp` (Plan 02) laufen UNVERAENDERT bei JEDEM Klick.
      2. DANN der Debounce-Guard: `if (sameTypInnerhalb800ms) { return; /* nur den Netzwerk-Emit ueberspringen */ }`.
      3. DANN `socket.emit('manual_ewb', {...click_id})`.
    Begruendung (Gemini): so bleibt die UI beim ERSTEN Klick sofort responsiv (Pin gesetzt, Antwort-Placeholder sichtbar, Latenz Punkt 25), und nur der wiederholte NETZWERK-Egress (Doppel-Haiku-Stream + Doppel-Transcript) wird gedrosselt. Der Debounce darf NIEMALS das UI-Update / den Pin-Set ueberspringen — nur das emit. (Das deckt sich mit der bestehenden "Pin wird auch beim entprellten Zweit-Klick korrekt gehalten"-Anforderung unten.)

    KEIN Refactor der Delegation, kein Anfassen des Pin-Pfads selbst (Plan 02) — nur das Emit-Entprellen NACH dem Pin-Set.

## 1. Lokaler Kontext
    _triggerEwb (2247-2289, Plan-02-Stand): _renderSlotResult(0,fake) (2270) -> slot1-Placeholder + pinActive-Set (Plan 02, nach 2277) -> if socket.connected: click_id-Gen + emit (2280-2288). Dedup-Guard direkt VOR dem emit (nach Pin-Set, nach connected-Pruefung). UI-Updates laufen vorher und unbeeinflusst.

## 2. Funktions-Skelett
    _triggerEwb linear; der Dedup ist ein vorgelagerter if(throttle){return;} VOR dem emit, aber NACH _renderSlotResult + pinActive-Set — er darf den Pin-Set (Plan 02) NICHT ueberspringen. Reihenfolge: UI-Update + Pin-Set (frueh) -> Debounce -> emit (spaet). Also: Pin wird auch beim entprellten Zweit-Klick korrekt gehalten; nur der doppelte Stream-Trigger entfaellt. Das ist gewollt (Cross-AI Executor-Suggestion).

## 3. Cross-File-Awareness
    - 'manual_ewb' : FE-Emit (2287) + BE handle_manual_ewb (deepgram ~852). FE-Dedup reduziert die Emits; BE bleibt unveraendert (es sei denn Reconnect-Replay -> separater BE-Punkt).
    - 'click_id' : MP1 (FE) + MP2 (BE) — nach Plan 05 werden die MP-Prints entfernt; click_id im Payload kann fuer Dedup bleiben oder mit raus (entscheiden in Plan 05).
    - 'pinActive' : von Plan 02 gesetzt VOR dem Debounce-Guard — der Guard darf das Set nicht umgehen.

## 4. Edge-Case-Analyse
    - Schnelles bewusstes Doppel-Klick auf ZWEI verschiedene Buttons: unterschiedlicher typ -> Dedup greift nicht (korrekt, beide sind legitim; Pin-Wechsel via Plan 02 Resolver 1).
    - Derselbe Button absichtlich 2x (Berater will neu generieren): das 800ms-Fenster unterdrueckt nur Sub-800ms-Dopplung; eine bewusste Wiederholung nach >800ms geht durch.
    - Entprellter Zweit-Klick: UI/Pin bereits aktualisiert (Punkt 1 der Anweisung), nur der zweite Netzwerk-Emit faellt weg -> keine UI-Regression.
    ### Race-Condition-Audit (4 Pflicht-Fragen)
    1. Gleichzeitiges Feuern: Dedup serialisiert die Emits eines Klicks auf einen. Pin-Set (vor dem Guard) bleibt idempotent + responsiv.
    2. Verbindungsabbruch: Dedup-State ist reiner FE-State; ein Abbruch verliert hoechstens das Fenster (harmlos).
    3. Reconnect-Replay: FE-Dedup faengt FE-seitige Replays; Socket-Re-Emits nach Reconnect sind BE-Empfangs-Sache -> separater Punkt (dokumentiert).
    4. Tab im Hintergrund: das Entprell-Fenster ist kurz; Hintergrund-Tab erzeugt keine Phantom-Klicks.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep manual_ewb'</automated>
  </verify>
  <acceptance_criteria>
    - NUR falls fix-now: nach Deploy + Live-Cold-Call + EIN physischer Knopf-Klick zeigt `inspect.sh logs 600 | grep "BUGB-EWB] MP2"` deutlich WENIGER Recv-Events pro Klick als zuvor (Ziel: 1 pro physischem Klick).
    - NUR falls fix-now: der Debounce-Guard steht im Code NACH `_renderSlotResult`/`pinActive`-Set und VOR `socket.emit('manual_ewb'...)` (Cross-AI Executor-Suggestion verifiziert) — UI bleibt beim ersten Klick sofort responsiv.
    - Falls defer: kein Code-Diff in pip-launcher.js fuer dieses Plan; stattdessen ein klarer Backlog-Eintrag (siehe output).
  </acceptance_criteria>
  <done>Entweder Mehrfach-Feuern per FE-Dedup auf 1 Emit/Klick reduziert (belegt per MP2; Debounce NACH UI-Update, VOR emit), oder bewusst deferred mit Backlog-Eintrag + Begruendung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| FE manual_ewb-Emit -> BE handle_manual_ewb | Mehrfach-Emits erzeugen Doppel-Streams (Kosten) + Mehrfach-Transcript/intent_event (Datenqualitaet) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PIP-10 | Denial of Service | Mehrfach-Feuern -> Doppel-Haiku-Streams (Kosten/Latenz) | mitigate-or-accept | Entscheidung in Task 1; FE-Dedup falls fix-now (Debounce nach UI-Update, vor emit), sonst dokumentierter Folgepunkt |
| T-PIP-11 | Tampering | 16x identische transcript_segments verzerren Trainings-/Judge-Daten | mitigate-or-accept | Gleiche Entscheidung; falls defer, als Datenqualitaets-Folgepunkt notiert (nicht still gedroppt) |
</threat_model>

<verification>
- MP1/MP2-Log-Reconciliation dokumentiert (gleiche vs verschiedene click_id).
- Entscheidung fix-now/defer mit Begruendung + Kosten-/Datenqualitaets-Bewertung im SUMMARY.
- Falls fix-now: MP2 zeigt 1 Recv/Klick; Debounce platziert nach UI-Update + vor emit.
</verification>

<success_criteria>
- Mehrfach-Feuern ist bewusst entschieden, nicht still gedroppt (CONTEXT-Vorgabe erfuellt).
- Falls die Anzeige-Trennung es harmlos macht: explizit mit Begruendung dokumentiert.
- Falls fix-now: UI bleibt beim ersten Klick responsiv, nur Netzwerk-Egress gedrosselt (Cross-AI Executor-Suggestion).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-04-SUMMARY.md`. Pflicht: die getroffene Entscheidung (fix-now/defer) + MP1/MP2-Befund + falls defer ein konkreter Backlog-/Folgephasen-Eintrag (z.B. via /gsd-add-backlog) fuer BE-Idempotenz / Reconnect-Replay-Haertung + falls fix-now Vermerk der Debounce-Platzierung (nach UI-Update, vor emit).
</output>
