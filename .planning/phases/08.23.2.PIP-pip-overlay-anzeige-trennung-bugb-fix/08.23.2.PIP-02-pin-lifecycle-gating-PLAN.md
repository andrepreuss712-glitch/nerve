---
phase: 08.23.2.PIP
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - static/pip-launcher.js
autonomous: false
requirements: [b, c]
complexity: "mittel"
cross_ai_required: true

must_haves:
  truths:
    - "Ein geklickter Vorschlag (Pin) bleibt stehen, bis der Berater einen ANDEREN Button drueckt ODER ihn explizit dismissed (x/Wisch)."
    - "Solange ein Pin aktiv ist, puffert der Auto-Erkenner sein Highlight statt es sofort zu zeigen (Selbst-Trigger-Gating, simpel, ohne Sprachabgleich)."
    - "Der Auto-Erkenner loest NIE einen Pin; es gibt KEINE Timer/keinen Zeitablauf, der einen Pin loest."
    - "Beim Call-Reset wird der Pin geraeumt OHNE ein gepuffertes Auto-Highlight in das sich versteckende DOM zu rendern (isReset-Pfad)."
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "Pin-State (set bei manual-Klick, cleared bei anderem Button / Dismiss / Call-Reset); Gating-Kopplung an state.pinActive; Dismiss-UI (x); _clearPin(isReset)-Flag"
      contains: "pinActive"
  key_links:
    - from: "static/pip-launcher.js _triggerEwb (manual-Klick)"
      to: "state.pinActive = true (Pin gesetzt)"
      via: "Pin-Lifecycle set"
      pattern: "pinActive"
    - from: "static/pip-launcher.js ewb_signal-Handler (Plan 01)"
      to: "Puffern statt Anzeigen waehrend pinActive"
      via: "Gating-Check"
      pattern: "pinActive"
    - from: "Dismiss-Button (x) / anderer Button-Klick"
      to: "state.pinActive = false + _applyBufferedAutoSignal()"
      via: "ereignisbasierter Resolver (nur 2 Loeser, NICHT im Reset-Pfad)"
      pattern: "_applyBufferedAutoSignal"
    - from: "Call-Reset-Pfade (3027/3482)"
      to: "_clearPin(isReset=true) — KEIN _applyBufferedAutoSignal"
      via: "Reset ohne Re-Apply in sich versteckendes DOM"
      pattern: "isReset"
---

<objective>
Pin-Lifecycle (Item c) + Selbst-Trigger-Gating in der einfachsten Form (Item b). Ein geklickter Vorschlag ist ein "Pin", der ereignisbasiert lebt: er persistiert bis (1) der Berater einen ANDEREN Button drueckt oder (2) ihn explizit dismissed (x/Wisch). KEINE Timer, der Auto-Erkenner loest NIE einen Pin. Solange ein Pin aktiv ist, puffert der Auto-Erkenner (aus Plan 01) sein Highlight statt es sofort anzuzeigen — gekoppelt rein an den Pin-Status, OHNE Sprachabgleich (PIP.3, nicht jetzt).

Purpose: Loest den Selbst-Trigger ohne Sprachverarbeitung — die simpelste tragfaehige Form (CLAUDE.md Punkt 27). Zusammen mit Plan 01 (Auto schreibt nie in die Lese-Zone) ist Bug B doppelt abgesichert.

Output: Pin-State-Maschine im FE, Dismiss-Geste (x), Gating-Kopplung des ewb_signal-Puffers, `_clearPin(isReset)`-Flag fuer flicker-freies Call-Reset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-01-anzeige-trennung-PLAN.md
@CLAUDE.md

# Kanonische Design-Vorgabe (Pflicht-Pre-Read):
# C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\NERVE_PiP_Overlay_Entscheidung_Umsetzung.md  (Abschnitt "Pin-/Dismiss-Lifecycle" + "Selbst-Trigger-Fix")

<interfaces>
<!-- Aus Plan 01 bereitgestellt (depends_on:[01], Plan 01 muss live sein): -->
  - state.pinActive (bool) — von Plan 01 als undefined/falsy belassen (NIE true initialisiert); DIESER Plan setzt/cleared ihn echt.
  - ewb_signal-Handler (Plan 01) — prueft bereits state.pinActive und puffert in state.lastAutoSignal.
  - _applyBufferedAutoSignal() (Plan 01) — wendet das juengste gepufferte Auto-Signal an (Highlight/Button).
  - state.lastAutoSignal — Newest-wins-Puffer Groesse 1.

<!-- Verifizierte FE-Anker (aus dem Code gelesen): -->
  static/pip-launcher.js
  - _triggerEwb(typ, btn) (2247) — manual-Klick-Pfad; Ende 2270-2289 setzt slot1-Placeholder + emit('manual_ewb',{slot:1,...}). HIER wird der Pin gesetzt.
  - _wirePipButtons (1783) — document-level Klick-Delegation; .closest('.pip-ewb-btn') (1802) -> _triggerEwb. EIN Listener pro PiP-Fenster (verifiziert robust gegen Re-Render).
  - nlp-ewb-row / .pip-ewb-btn / data-typ — Button-Schema.
  - pip-slot-1 / pip-slot-body-1 / pip-slot-label-1 — die Lese-Zone (Pin-Anzeige).
  - Call-Reset-Pfade: 3027 + 3482 (nlp-ewb-row / pip-section-live verstecken) — hier _clearPin(isReset=true) einhaengen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pin-State setzen (manual-Klick) + ereignisbasierte Resolver (anderer Button / Dismiss), keine Timer, flicker-freies Reset</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js 2247-2289 (_triggerEwb komplett — Pin-Start: Placeholder + manual_ewb-Emit)
    - static/pip-launcher.js 1783-1819 (_wirePipButtons: Klick-Delegation, .closest('.pip-ewb-btn'))
    - static/pip-launcher.js 3020-3030 + 3478-3486 (Call-Reset-Pfade — Pin muss bei Call-Ende cleared werden, OHNE Re-Apply ins sich versteckende DOM)
    - static/pip-launcher.js (Plan-01-Stand) ewb_signal-Handler + state.pinActive (falsy) + _applyBufferedAutoSignal
    - .planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md <decisions> (b) + (c)
    - Nerve-Vault/03 Planung/NERVE_PiP_Overlay_Entscheidung_Umsetzung.md "Pin-/Dismiss-Lifecycle: ereignisbasiert, keine Timer"
    - CLAUDE.md Punkt 14 (Control-Flow + 4 Race-Fragen), Punkt 27 (einfachster Weg), Umlaut-Regel, "keine Inline-Hex/Dark-Mode-Farben"
  </read_first>
  <action>
    Pin = "in der Lese-Zone steht aktuell ein geklickter Vorschlag, der vorgelesen wird". Reiner FE-State (CONTEXT Claude's-Discretion: FE-State reicht; der Pin steuert nur die FE-Anzeige + das FE-Gating, kein BE-Session-State noetig).

    1. Pin setzen: In `_triggerEwb` (2247), an der Stelle des slot1-Placeholders (2273-2277), `state.pinActive = true` und `state.pinTyp = typ` setzen. Das ist der EINZIGE Ort, der einen Pin oeffnet (der Auto-Pfad oeffnet NIE einen Pin — Item c). (LOW-Finding aus Plan 01: `pinActive` wird ERST hier auf true gesetzt — in Plan 01 blieb er falsy.)

    2. Resolver 1 — anderer Button gedrueckt: jeder _triggerEwb-Aufruf setzt pinActive=true + pinTyp=neu. Drueckt der Berater einen anderen Button, uebernimmt die neue Antwort den Pin (pinTyp aktualisiert, neue Antwort rendert in slot 1). Beim Pin-Wechsel KEIN _clearPin (Gating bleibt aktiv) — nur pinTyp neu setzen.

    3. Resolver 2 — expliziter Dismiss (x): einen kleinen Dismiss-Button (id="pip-pin-dismiss", CSS-Klasse statt Inline-Hex, sichtbarer Text/Aria deutsch "Vorschlag schliessen") an der Lese-Zone (pip-slot-1) hinzufuegen, sichtbar nur wenn pinActive. Klick auf x -> `_clearPin()` (OHNE Argument → isReset defaultet auf false → Re-Apply des gepufferten Auto-Signals ist hier gewollt). Wisch-Geste optional (pointer/touch-swipe auf pip-slot-1 -> _clearPin) NUR wenn billig machbar; sonst als PIP.2-Kandidat in SUMMARY notieren. x reicht fuer PIP.1 (Punkt 27).

    4. _clearPin(isReset=false) (NEU — mit Flag-Parameter, Cross-AI MEDIUM): setzt pinActive=false, pinTyp=null, leert pip-slot-body-1 (Text leeren auf Ruhe-Zustand — KEINE Groessenaenderung/kein Einklappen, Design-Vorgabe), versteckt den x-Button.
       **HARTE EXECUTOR-ANWEISUNG (Cross-AI MEDIUM — _clearPin-Seiteneffekt beim Call-Reset, PFLICHT, nicht implizit):**
       `_clearPin` bekommt einen Parameter `isReset` (Default `false`). NUR wenn `isReset === false` (normaler Dismiss / anderer-Button-Pfad) ruft `_clearPin` am Ende `_applyBufferedAutoSignal()` (Plan 01) auf, damit ein waehrend des Pins gepuffertes Auto-Highlight JETZT nachgezogen wird. Bei `isReset === true` (Call-Reset) wird `_applyBufferedAutoSignal()` NICHT aufgerufen — das Pin-State wird nur sauber zurueckgesetzt, KEIN neues Button-Highlight in das sich gerade versteckende/abbauende DOM gerendert.
       Begruendung (Gemini): `_applyBufferedAutoSignal()` rendert ein neues Button-Highlight Sekundenbruchteile bevor die UI versteckt wird → visuelles Flackern oder Fehler, wenn die DOM-Nodes (nlp-ewb-row / pip-section-live) schon teilweise abgeraeumt sind.
       Konkret: `function _clearPin(isReset) { state.pinActive = false; state.pinTyp = null; /* slot1 leeren, x verstecken */; if (!isReset) { _applyBufferedAutoSignal(); } }`. KEIN Timer.

    5. Verdrahtung des x in der Delegation: _wirePipButtons (1787, document-level) um einen Zweig `t.closest('#pip-pin-dismiss')` ergaenzen -> ev.preventDefault + ev.stopPropagation + `_clearPin()` (ohne Argument, isReset=false). Gleiche robuste Delegations-Stelle wie die EWB-Buttons.

    6. Call-Ende: in den bestehenden Reset-Pfaden (3027 / 3482, wo nlp-ewb-row/pip-section-live versteckt werden) `_clearPin(true)` mit aufrufen (isReset=true → KEIN _applyBufferedAutoSignal, siehe Punkt 4), damit pinActive nicht ueber die Call-Grenze haengen bleibt und kein gepuffertes Highlight ins sich versteckende DOM rendert.

    EXPLIZIT NICHT: kein setTimeout/setInterval das pinActive auf false setzt. Kein Auto-Pfad der _clearPin ruft. (Sprach-Loeser = PIP.3.) Kein Refactor-Beifang (Punkt 17) — nur Pin-Lifecycle.

## 1. Lokaler Kontext
    _triggerEwb (2247-2289): baut `fake`-Result -> _renderSlotResult(0, fake) (2270) -> slot1-Placeholder "Variante wird gebaut..." (2273-2277) -> if socket.connected: MP1-clickId + emit('manual_ewb',...) (2280-2288). Kein early return im Erfolgsfall. Pin-Set direkt nach dem slot1-Placeholder (nach 2277).
    _wirePipButtons (1787-1819): document click-Delegation: #nlp-btn-beenden (1795 return) -> .pip-ewb-btn (1802 _triggerEwb return) -> #pip-mic-indicator (1814) -> #nlp-btn-next-call (1821). Neuer #pip-pin-dismiss-Zweig VOR dem .pip-ewb-btn-Zweig (x ist kein .pip-ewb-btn, keine Kollision; klare Trennung).
    Reset-Pfade 3027 / 3482: ['nlp-btn-beenden','nlp-ewb-row','pip-section-live',...].forEach(hide) — _clearPin(true) daneben aufrufen (isReset=true). Diese Pfade verstecken/raeumen genau die DOM-Nodes ab, in die _applyBufferedAutoSignal sonst rendern wuerde → daher isReset=true zwingend (Cross-AI MEDIUM).

## 2. Funktions-Skelett
    _triggerEwb: linear, ein Erfolgspfad. Pin-Set = State-Assignment, keine neue Verzweigung.
    _wirePipButtons: Kette von if(closest){...return}-Bloecken. Additiver Block.
    _clearPin(isReset=false): neue kleine, idempotente Funktion (mehrfach aufrufbar ohne Schaden). Ein einziger `if (!isReset) _applyBufferedAutoSignal()`-Zweig am Ende.

## 3. Cross-File-Awareness
    grep-Belege (Executor verifiziert via grep vor Edit, Punkt 20):
    - 'pinActive' : Plan 01 (falsy Default + ewb_signal-Gating-Check) + hier gesetzt (=true in _triggerEwb) / cleared (=false in _clearPin). Nur static/pip-launcher.js.
    - '_applyBufferedAutoSignal' : Plan 01 (def) + hier (Aufruf in _clearPin NUR wenn !isReset). Nur pip-launcher.js.
    - 'isReset' : neu — der Flag-Parameter von _clearPin. grep muss zeigen: _clearPin() (Dismiss/Delegation, isReset=false) UND _clearPin(true) (beide Reset-Pfade 3027/3482).
    - '_triggerEwb' : def 2247 + Aufruf in Delegation 1810. Nur FE.
    - 'pip-pin-dismiss' : neu — grep muss 0 Vortreffer zeigen.
    - Kein BE-Bezug (Pin ist reiner FE-State). live_session.py NICHT angefasst.

## 4. Edge-Case-Analyse
    - Klick auf denselben Button waehrend Pin aktiv (gleicher typ): pinActive bleibt true, pinTyp unveraendert, Antwort neu gerendert (harmlos).
    - Dismiss ohne aktiven Pin: _clearPin idempotent (pinActive schon false) -> no-op; _applyBufferedAutoSignal hat dann nichts anzuwenden (lastAutoSignal leer) -> harmlos.
    - Pin aktiv, Call endet: Reset-Pfad ruft _clearPin(true) -> Gating sauber zurueckgesetzt, KEIN Re-Apply ins sich versteckende DOM (Cross-AI MEDIUM behoben).
    - Gepuffertes Auto-Signal + sofortiges Call-Ende: isReset=true verwirft das Nachziehen (kein Geister-Highlight in toter UI) — gewollt, lossy.
    ### Race-Condition-Audit (4 Pflicht-Fragen, CLAUDE.md Punkt 14)
    1. Gleichzeitiges Feuern manual vs auto: manual-Klick setzt pinActive=true SYNCHRON im Klick-Handler. JS ist im Tab single-threaded -> ein gleichzeitig ankommendes ewb_signal sieht pinActive=true und puffert (Plan 01). Garantiert: kein Auto-Highlight ueberlagert den frischen Pin. Reihenfolge haelt durch Event-Loop-Serialisierung.
    2. Verbindungsabbruch mid-Stream: Pin-State (FE) ueberlebt einen Socket-Drop. Bei Reconnect bleibt der Pin sichtbar, dismissbar. Kein halber Pin-State.
    3. Reconnect-Replay: alter manual_ewb-Replay -> idempotentes pinActive=true-Set (kein Schaden). Alter ewb_signal -> durch pinActive-Gating gepuffert. Mehrfach-Feuern (Hypothese A) ist fuer die Lese-Zone unkritisch, weil alle manual-Streams in denselben slot 1 schreiben (letzter gewinnt) und KEIN Auto dazwischen schreibt (Plan 01). Pin-Logik bleibt korrekt.
    4. Tab im Hintergrund / Call-Reset waehrend gepuffertem Signal: kein Timer -> kein Pin laeuft ab. Beim Reaktivieren ist der Pin noch da (gewollt: ereignisbasiert). Gepufferte Auto-Signale (Groesse 1) werden erst bei Dismiss/anderem Button nachgezogen — beim Call-Reset (isReset=true) bewusst NICHT (Cross-AI MEDIUM: kein Rendern in sich versteckendes DOM).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep pinActive'; ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep isReset'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh grep "pinActive"` zeigt: Set in _triggerEwb (=true), Clear in _clearPin (=false), Gating-Check im ewb_signal-Handler.
    - `inspect.sh grep "pip-pin-dismiss"` zeigt den x-Button + den Delegations-Zweig.
    - `inspect.sh grep "isReset"` zeigt den Flag-Parameter in _clearPin + `_clearPin(true)` in BEIDEN Reset-Pfaden (3027/3482) + `if (!isReset)` vor `_applyBufferedAutoSignal()` (Cross-AI MEDIUM verifiziert: Reset ruft kein Re-Apply).
    - `inspect.sh grep "setTimeout"` zeigt KEINEN Timer der pinActive/Pin betrifft (Item c: keine Timer).
    - Live (Task 2): geklickter Vorschlag bleibt waehrend Vorlesen stehen; verschwindet NUR bei x oder anderem Button; kein Flackern beim Call-Ende.
  </acceptance_criteria>
  <done>Pin wird nur bei manual-Klick gesetzt, nur durch anderen Button / expliziten Dismiss geloest, nie durch Timer/Auto; Gating-Puffer wird beim Pin-Ende nachgezogen (nur wenn !isReset); bei Call-Ende sauber zurueckgesetzt OHNE Re-Apply ins versteckende DOM (Cross-AI MEDIUM).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-Cold-Call Verifikation (Prod) — Pin haelt, Gating greift, kein Timer-Ablauf, flicker-freies Reset</name>
  <what-built>
    Pin-Lifecycle + Selbst-Trigger-Gating im FE + _clearPin(isReset)-Flag fuer flicker-freies Call-Reset. Deploy via `bash deploy.sh production` (Kein-Local-Dev). Pytest-Gate laeuft serverseitig vor Restart.
  </what-built>
  <how-to-verify>
    1. `bash deploy.sh production`.
    2. Cold-Call mit Test-User auf getnerve.app (Headset-Modal bestaetigen).
    3. EINEN Einwand-Knopf klicken -> Vorschlag erscheint. Antwort LANGSAM vorlesen, dabei bewusst Schluesselwoerter wiederholen (Selbst-Trigger provozieren). Beobachten:
       - Bleibt der Vorschlag waehrend des kompletten Vorlesens stehen (kein Verschwinden, kein Ueberschreiben)? (erwartet: ja, auch ueber >10s ohne Timer-Ablauf)
       - Erscheint eine Auto-Erkennung waehrenddessen NUR als (gepuffertes) Button-Highlight, NICHT als Aenderung in der Lese-Zone? (erwartet: ja)
    4. Dann einen ANDEREN Button druecken -> uebernimmt die neue Antwort den Pin sauber? (erwartet: ja)
    5. Dann das x (Dismiss) druecken -> verschwindet der Vorschlag UND wird ein zuvor gepuffertes Auto-Highlight jetzt sichtbar? (erwartet: ja)
    6. Bei aktivem Pin (mit gepuffertem Auto-Signal) den Call BEENDEN -> verschwindet die Live-UI sauber OHNE kurzes Aufblitzen eines neuen Button-Highlights (Cross-AI MEDIUM)? (erwartet: kein Flackern)
    7. Logs optional: `inspect.sh logs 500 | grep BUGB-EWB`.
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn der Pin haelt (kein Timer-Ablauf), Gating greift, beide Resolver (anderer Button + x) sauber loesen, UND das Call-Ende kein Highlight-Flackern zeigt — sonst beschreibe das Verhalten.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| BE (ewb_signal) -> FE | Auto-Signale, die waehrend eines Pins gepuffert (nicht angezeigt) werden — duerfen den Pin nicht aufloesen |
| Berater-Geste (Klick/x) -> FE Pin-State | Einzige legitime Pin-Resolver (Item c) |
| Call-Reset -> FE DOM-Abbau | Pin-Reset darf kein Highlight in sich abbauendes DOM rendern (Cross-AI MEDIUM) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PIP-05 | Tampering | Auto-Pfad loest faelschlich einen Pin (Bug zurueck) | mitigate | Strukturell: nur _triggerEwb (manual) setzt Pin; nur anderer Button / x loest; kein Auto-Aufruf von _clearPin (grep-belegt) |
| T-PIP-06 | Denial of Service | Timer-Ablauf raeumt Pin mitten im Vorlesen weg | mitigate | KEINE Timer im Pin-Pfad (Item c); grep "setTimeout" zeigt keinen Pin-Bezug |
| T-PIP-07 | Repudiation | Pin haengt ueber Call-Grenze (stale Gating) | mitigate | _clearPin(true) in den Call-Reset-Pfaden (3027/3482) |
| T-PIP-08 | Denial of Service | _applyBufferedAutoSignal rendert in sich abbauendes DOM beim Call-Reset (Flicker/Fehler) | mitigate | _clearPin(isReset)-Flag: bei isReset=true KEIN _applyBufferedAutoSignal (Cross-AI MEDIUM) |
</threat_model>

<verification>
- grep: pinActive Set/Clear/Gating vorhanden; pip-pin-dismiss vorhanden; isReset-Flag + _clearPin(true) in beiden Reset-Pfaden + if(!isReset)-Guard; kein Timer im Pin-Pfad.
- Prod-Live: Pin haelt ueber langes Vorlesen, Gating puffert Auto, beide Resolver loesen sauber, Auto loest nie, Call-Ende ohne Highlight-Flackern.
</verification>

<success_criteria>
- (b) Selbst-Trigger-Gating: bei aktivem Pin puffert der Auto-Erkenner (kein Sprachabgleich), Highlight erst nach Pin-Ende.
- (c) Pin-Lifecycle ereignisbasiert: nur anderer Button / expliziter Dismiss; keine Timer; Auto loest nie.
- Call-Reset raeumt den Pin ohne Re-Apply ins sich versteckende DOM (Cross-AI MEDIUM).
- Live-Re-Test: Vorschlag bleibt waehrend des Vorlesens stabil; kein Flackern beim Call-Ende.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-02-SUMMARY.md`. Pflicht: Pruning-Notiz + Vermerk ob Wisch-Geste gebaut oder als PIP.2 deferred wurde + Bestaetigung dass _clearPin(isReset) im Reset-Pfad kein _applyBufferedAutoSignal ausloest (Cross-AI MEDIUM umgesetzt).
</output>
</task>
</output>
