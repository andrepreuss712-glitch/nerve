---
phase: 08.23.2.PIP
plan: 05
status: code-complete-awaiting-live-verify
completed: 2026-06-30
commit: bd9a0af
---

# Plan 05 — [BUGB-EWB]-Instrumentierung entfernen — SUMMARY

**Commit:** `bd9a0af` — `chore(08.23.2.PIP-05): remove [BUGB-EWB] instrumentation (MP1-MP6)`.

## Entfernt (nach Plan 01/03 verbliebene Messpunkte)
| MP | Ort | Entfernt |
|----|-----|----------|
| MP1 | `static/pip-launcher.js` manual_ewb emit | Print + Kommentar. `click_id` bleibt (de-tagged `_clickId`) fuer deferred Plan 04. |
| MP2 | `deepgram_service.py` manual_ewb RECV | Print + `_bugb_click_id`. |
| MP3 | `claude_service.py` analyse_loop lock READ | Print + Kommentar. Funktionaler `_slot1_busy_until`-Read + Mutex bleiben. |
| MP3+MP4 | `claude_service.py` streame_manual_ewb_variante | Print + `_bugb_busy_manual`. |
| MP5 | `claude_service.py` coldcall_infer pre-parse | Print + Kommentar. |
| MP6 | `app_routes.py` _transcript_entries_to_segments | Print + `_bugb_is_btn`. ts_ms-Logik (base/rel/running) unveraendert. |

(MP4-qa_slot1 + MP3/MP4-keyword waren bereits durch Plan 01 entfernt.)

## NICHT angefasst (Cross-AI MEDIUM #1, PIP.4-Foundation)
Der dormante TTFT-Circuit-Breaker (`_ewb_ttft_history`/`_ewb_fallback_until`/
`_ewb_circuit_lock`) + der `[EWB-TTFT]`-Print bleiben (kein `[BUGB-EWB]`-Tag → nicht im Filter).

## Verifikation
- `grep "BUGB-EWB"` / `grep "_bugb"` in static/ routes/ services/ = **0** Treffer.
- `grep "EWB-TTFT"` / `_ewb_*` in claude_service.py = **weiterhin vorhanden** (Foundation).
- `py_compile` aller 3 Python-Dateien OK.
- **OFFEN (Live, André):** nach Deploy zeigt `inspect.sh logs 300` bei einem Cold-Call
  kein `[BUGB-EWB]`-Rauschen mehr; App laeuft normal.
