---
phase: 08.23.2.PIP
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - static/pip-launcher.js
  - routes/app_routes.py
  - services/deepgram_service.py
  - services/claude_service.py
autonomous: false
requirements: [a, e]
complexity: "trivial"
cross_ai_required: false

must_haves:
  truths:
    - "Alle [BUGB-EWB]-Messpunkte (MP1-MP6) sind aus dem Code entfernt (oder hinter einem Flag), nachdem alle vorherigen Plaene per Live-Logs verifiziert wurden."
    - "Der dormante TTFT-Circuit-Breaker (_ewb_fallback_until/_ewb_ttft_history/_ewb_circuit_lock) wird NICHT als toter Code mit-entfernt — er ist von Plan 01 als PIP.4-Foundation markiert (Cross-AI MEDIUM #1)."
  artifacts:
    - path: "services/claude_service.py"
      provides: "Keine [BUGB-EWB]-Prints mehr"
    - path: "services/deepgram_service.py"
      provides: "Keine [BUGB-EWB]-Prints mehr"
    - path: "routes/app_routes.py"
      provides: "Keine [BUGB-EWB]-Prints mehr"
    - path: "static/pip-launcher.js"
      provides: "Keine [BUGB-EWB]-Logs mehr"
  key_links:
    - from: "alle 4 Dateien"
      to: "sauberer Code ohne Diagnose-Instrumentierung"
      via: "Entfernung der [BUGB-EWB]-Zeilen"
      pattern: "BUGB-EWB"
---

<objective>
End-of-phase Cleanup (CONTEXT-Pflicht): die 6 Instrumentierungs-Messpunkte mit Tag `[BUGB-EWB]` (MP1-MP6, 20 Vorkommen in 4 Dateien), die in der Debug-Session Pass 1 fuer die Diagnose eingebaut wurden, wieder entfernen — als sauberer, eigener Schritt, NACHDEM alle vorherigen Plaene (01-04) ihre Live-Verifikation gegen diese Logs abgeschlossen haben.

Purpose: Diagnose-Prints sind temporaer; sie bleiben nicht im Produktiv-Code (Nudelcode-Praevention, Punkt 3 + 17). Sie waren das Mess-Werkzeug fuer Bug B — nach dem Fix + Verifikation sind sie Ballast (Log-Rauschen).

Output: Code ohne `[BUGB-EWB]`-Instrumentierung in allen 4 Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.23.2.PIP-pip-overlay-anzeige-trennung-bugb-fix/08.23.2.PIP-CONTEXT.md
@.planning/debug/ewb-pip-overwrite-multifire.md
@CLAUDE.md

<interfaces>
<!-- Verifizierte Vorkommen (grep [BUGB-EWB], 20 Treffer / 4 Dateien): -->
  services/claude_service.py     : 8 Treffer (MP3 lock READ analyse_loop ~1534/1539; MP4 slot1 WRITE qa_slot1 ~1647/1659; MP5 coldcall_infer pre-parse ~496; weitere Kommentar-/Print-Zeilen)
  services/deepgram_service.py   : 8 Treffer (MP3 lock READ/SET keyword ~249/254/258; MP4 slot1 WRITE streame_auto_variante ~262/265; MP2 manual_ewb RECV ~852; ggf. MP6-Bezug)
  routes/app_routes.py           : 2 Treffer (MP6 ts_ms-Setzpunkt ~68-75, der Print 75 + Kommentarblock)
  static/pip-launcher.js         : 2 Treffer (MP1 manual_ewb emit ~2281-2286, Kommentar + console.log)

Hinweis: einige [BUGB-EWB]-Stellen wurden durch Plan 01 bereits mit-entfernt (z.B. MP4 in streame_auto_variante / qa_slot1, wenn diese Pfade umgebaut/entfernt wurden). Dieser Plan entfernt die VERBLEIBENDEN Vorkommen — der Executor greppt erneut und entfernt, was noch da ist.

<!-- ABGRENZUNG zum TTFT-Circuit-Breaker (Cross-AI MEDIUM #1) — NICHT mit-prunen: -->
  Der TTFT-Circuit-Breaker in claude_service.py (`_ewb_ttft_history` Z.18, `_ewb_fallback_until` Z.19,
  `_ewb_circuit_lock` Z.20, plus die TTFT-Mess-Prints `[EWB-TTFT]` ~671) ist KEINE [BUGB-EWB]-Instrumentierung.
  Plan 01 (Circuit-Breaker-Removal-Audit, MEDIUM #1) hat streame_auto_variante inkl. dieses dormanten
  Circuit-Breakers als PIP.4-Foundation im Foundation-Code-Register markiert (0 externe Produktiv-Reader,
  grep-belegt). → Dieser Cleanup-Plan fasst diese Symbole + den `[EWB-TTFT]`-Print NICHT an. Nur Zeilen mit
  dem Tag `[BUGB-EWB]` werden entfernt; `[EWB-TTFT]` und die _ewb_*-Globals BLEIBEN.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Alle verbleibenden [BUGB-EWB]-Messpunkte entfernen (4 Dateien)</name>
  <files>static/pip-launcher.js, routes/app_routes.py, services/deepgram_service.py, services/claude_service.py</files>
  <read_first>
    - Aktueller grep-Stand: `grep -rn "BUGB-EWB" static/ routes/ services/` (Executor zieht den frischen Stand NACH Plan 01-04, da einige Stellen schon weg sind)
    - .planning/debug/ewb-pip-overwrite-multifire.md Abschnitt "Instrumentierung Pass 1 — 6 Mess-Punkte" (Inventar MP1-MP6)
    - .planning/phases/08.23.2.PIP-.../08.23.2.PIP-CONTEXT.md Pflicht-Vorgaben "Am Ende: [BUGB-EWB]-Messpunkte wieder entfernen"
    - .planning/phases/08.23.2.PIP-.../08.23.2.PIP-01-anzeige-trennung-PLAN.md Sub-Step `ewb-circuit-breaker-audit` (Cross-AI MEDIUM #1) — die _ewb_*-Globals + der `[EWB-TTFT]`-Print sind PIP.4-Foundation, NICHT mit-prunen
    - .planning/04 Entscheidungen/Foundation-Code-Register.md (Eintrag streame_auto_variante / TTFT-Circuit-Breaker, von Plan 01 gesetzt)
    - CLAUDE.md Punkt 14 (auch beim Entfernen: 30 Zeilen Kontext lesen, Control-Flow), Punkt 17 (NUR die Instrumentierung, kein Beifang)
  </read_first>
  <action>
    Reines Entfernen der Diagnose-Instrumentierung. Pro Vorkommen:
    - Die `[BUGB-EWB]`-`print(...)`/`console.log(...)`-Zeile(n) entfernen.
    - Die zugehoerigen MP-Erklaer-Kommentarbloecke (die nur die Instrumentierung beschreiben, z.B. "[BUGB-EWB] MP3 — LESEN von ...") entfernen.
    - NUR temporaere Diagnose-Hilfsvariablen entfernen, die AUSSCHLIESSLICH fuer den MP-Print existieren (z.B. `_bugb_busy_now`, `_bugbClickId` falls NICHT von Plan 04 fuer click_id-Dedup weitergenutzt — pruefen! Wenn Plan 04 fix-now gewaehlt hat und click_id fuer Dedup braucht, bleibt die click_id-Generierung, nur der MP1-Print geht).
    - Funktionale Code-Zeilen, die zufaellig in einem MP-Kommentar erwaehnt sind, NICHT entfernen (z.B. der echte `slot1_variant_busy_until`-Read im keyword-Pfad — falls Plan 01 ihn nicht schon entfernt hat; aber Plan 01 hat den Lese-Zonen-Lock-Pfad umgebaut, also pruefen was noch lebt).

    **TTFT-Circuit-Breaker NICHT mit-prunen (Cross-AI MEDIUM #1, BLOCKING):** Die Symbole `_ewb_ttft_history` / `_ewb_fallback_until` / `_ewb_circuit_lock` (claude_service.py:18-20) und der `[EWB-TTFT]`-Print (~671) sind KEINE [BUGB-EWB]-Instrumentierung — sie sind der dormante Circuit-Breaker, den Plan 01 als PIP.4-Foundation markiert hat. Auch wenn Plan 01 streame_auto_variante kalt gestellt hat (kein Aufruf mehr) und diese Globals dadurch "tot" aussehen: NICHT entfernen. Der grep-Filter dieses Plans ist `BUGB-EWB` — `[EWB-TTFT]` und `_ewb_*` matchen NICHT und bleiben unangetastet. Falls beim 30-Zeilen-Kontext-Lesen eine _ewb_*-Zeile neben einem MP-Print auftaucht: stehen lassen (Foundation, Foundation-Code-Register).

    Vorgehen je Datei (frischen grep zuerst, da Plan 01 einige Stellen schon entfernt hat):
    1. static/pip-launcher.js (~2281-2286): MP1-Kommentar + console.log entfernen. `_bugbClickId`: bleibt NUR wenn Plan 04 die click_id im Payload fuer Dedup/BE braucht; sonst entfernen + aus dem emit-Payload nehmen (mit BE abstimmen — wenn BE click_id nicht mehr braucht). Im Zweifel click_id im Payload lassen (harmlos), nur den MP1-Print entfernen.
    2. routes/app_routes.py (~68-75): MP6-Kommentarblock + Print entfernen. `_bugb_is_btn` entfernen, falls nur fuer den Print. Die funktionale ts_ms-Logik (_rel/base/running) bleibt unangetastet.
    3. services/deepgram_service.py: MP2 (manual_ewb RECV), MP3 (lock READ/SET keyword), MP4 (slot1 WRITE) Prints + Kommentare entfernen — soweit nach Plan 01 + 03 noch vorhanden.
    4. services/claude_service.py: MP3 (lock READ analyse_loop), MP4 (slot1 WRITE qa_slot1), MP5 (coldcall_infer pre-parse) Prints + Kommentare entfernen — soweit nach Plan 01 noch vorhanden. Die _ewb_*-Globals + `[EWB-TTFT]`-Print NICHT anfassen (siehe oben, MEDIUM #1).

    Alternative (CONTEXT erlaubt "oder hinter Flag"): Planner-Wahl = ENTFERNEN, nicht Flag. Begruendung (Punkt 27): ein Diagnose-Flag fuer einen abgeschlossenen, verifizierten Bug ist ueberfluessige Mechanik; sollte Bug B je wiederkehren, ist die Instrumentierung in der git-Historie (debug-Doc + Commits) jederzeit wieder einbaubar. Kein Dauer-Flag-Ballast.

    Abschluss-Check: `grep -rn "BUGB-EWB" static/ routes/ services/` muss 0 Treffer liefern.

## 1. Lokaler Kontext
    Jede MP-Stelle: ein print/console.log + ein erklaerender Kommentarblock, eingebettet in funktionalen Code. Beim Entfernen 30 Zeilen drumherum lesen (Punkt 14), um nicht versehentlich eine funktionale Zeile (z.B. einen echten emit, einen echten Lock-Read der noch lebt, ODER eine _ewb_*-Circuit-Breaker-Zeile) mitzunehmen. Die MP-Prints sind reine Seiteneffekt-Zeilen (keine Rueckgabe, kein Control-Flow) -> Entfernen aendert den Ablauf nicht.

## 2. Funktions-Skelett
    Keine Funktions-Struktur-Aenderung. Nur print/log-Zeilen + tote Hilfsvariablen raus. Alle return/try/except-Pfade bleiben identisch.

## 3. Cross-File-Awareness
    - 'BUGB-EWB' : die 4 Dateien oben. Nach Entfernen 0 Treffer.
    - 'EWB-TTFT' / '_ewb_fallback_until' / '_ewb_ttft_history' / '_ewb_circuit_lock' : Circuit-Breaker-Foundation (MEDIUM #1) — NICHT anfassen, matchen den BUGB-EWB-Filter NICHT, bleiben erhalten.
    - '_bugbClickId' / 'click_id' : Abstimmung mit Plan-04-Entscheidung (Dedup nutzt click_id ggf. weiter).
    - '_bugb_busy_now' / '_bugb_is_btn' : tote Hilfsvariablen nur fuer Prints -> mit raus.
    - Pruefen dass keine MP-Variable von echtem Code gelesen wird (sollte nicht — sie wurden nur fuer Prints eingefuehrt).

## 4. Edge-Case-Analyse
    - Eine MP-Hilfsvariable wird doch funktional genutzt: dann NICHT entfernen, nur den Print. (grep pro Variable bestaetigt Nutzung.)
    - Eine _ewb_*-Circuit-Breaker-Zeile steht direkt neben einem MP-Print (MEDIUM #1): NUR den MP-Print entfernen, die _ewb_*-Zeile stehen lassen (Foundation).
    - Ein MP-Print steht als einzige Anweisung in einem if/else-Zweig: nach Entfernen darf kein leerer Block entstehen (Python: `pass` einsetzen oder den Zweig sinnvoll auffuellen/entfernen — Control-Flow exakt erhalten).
    ### Race-Condition-Audit (4 Pflicht-Fragen)
    1. Gleichzeitiges Feuern: Entfernen von Log-Zeilen aendert keine Nebenlaeufigkeit (Prints sind nebenwirkungsfrei ausser stdout). Garantierter Zustand unveraendert.
    2. Verbindungsabbruch: irrelevant — keine Zustandslogik beruehrt.
    3. Reconnect-Replay: irrelevant — keine Handler-Logik beruehrt.
    4. Tab im Hintergrund: irrelevant.
    (Begruendet "nicht relevant": dieser Plan entfernt ausschliesslich nebenwirkungsfreie Log-Statements + tote Print-Hilfsvariablen; es wird kein nebenlaeufiger oder zustandsbehafteter Code-Pfad veraendert. Der Circuit-Breaker-Zustand bleibt unberuehrt, MEDIUM #1.)
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "BUGB-EWB"'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh grep "BUGB-EWB"` auf dem deployten Stand liefert 0 Treffer in static/, routes/, services/.
    - **Circuit-Breaker-Erhalt (Cross-AI MEDIUM #1):** `inspect.sh grep "_ewb_fallback_until"` und `inspect.sh grep "EWB-TTFT"` liefern weiterhin Treffer in claude_service.py (der dormante Circuit-Breaker + sein Print wurden NICHT mit-geprunt — Foundation bleibt).
    - `deploy.sh production` Pytest-Gate bleibt gruen (keine funktionale Aenderung).
    - Nach Deploy: `inspect.sh logs 300` zeigt kein `[BUGB-EWB]`-Rauschen mehr bei einem Cold-Call.
  </acceptance_criteria>
  <done>Alle [BUGB-EWB]-Messpunkte entfernt; grep 0 Treffer; TTFT-Circuit-Breaker (MEDIUM #1) als Foundation erhalten; Deploy gruen; Logs sauber.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy + Beleg dass Logs sauber sind</name>
  <what-built>
    Instrumentierung entfernt. Deploy via `bash deploy.sh production`.
  </what-built>
  <how-to-verify>
    1. `bash deploy.sh production` (Pytest-Gate muss gruen sein).
    2. Kurzer Cold-Call mit Test-User (EIN Knopf-Klick).
    3. `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 300'` | grep "BUGB-EWB" -> KEIN Treffer.
    4. Sicherstellen dass die App normal laeuft (Vorschlag erscheint bei Knopf-Klick, kein Funktions-Regress).
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn Deploy gruen, keine [BUGB-EWB]-Logzeilen mehr, App funktioniert normal — sonst Befund posten.</resume-signal>
</task>

</tasks>
