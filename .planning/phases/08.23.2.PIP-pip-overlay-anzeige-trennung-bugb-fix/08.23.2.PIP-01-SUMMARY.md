---
phase: 08.23.2.PIP
plan: 01
status: code-complete-awaiting-live-verify
completed: 2026-06-29
tasks_done: [1, 2]
tasks_pending: [3]
commits:
  - 5072409  # BE Anzeige-Trennung (Task 1)
  - c3adb2e  # FE Source-Gate + ewb_signal-Handler + Coaching-Guard (Task 2)
---

# Plan 01 — Anzeige-Trennung (Bug B Wurzel-Fix) — SUMMARY

**Status:** Code-complete (Task 1 BE + Task 2 FE committet). **Task 3 (Live-Cold-Call-
Verifikation auf Prod) ist ein blocking human-verify-Checkpoint und steht noch aus** —
André deployt (`bash deploy.sh production`) und testet live (siehe Checkpoint unten).
Local == origin/main fuer die 3 Code-Dateien (kein Drift; grep-Anker 2026-06-29 verifiziert).

## Was gebaut wurde

**Kernregel umgesetzt:** Eingabe (Auto-Erkenner / Knoepfe) ist strikt von Ausgabe (Lese-Zone
slot 1) getrennt. Der Auto-Erkenner hat KEINEN Schreibzugriff mehr auf `pip-slot-body-1`.
Damit ist "Antwort wird im Vorlesen ueberschrieben" nicht gemanagt, sondern strukturell
unmoeglich.

### Task 1 — Backend (services/claude_service.py, services/deepgram_service.py)
- **deepgram Keyword-Pfad:** `start_background_task(streame_auto_variante, ...)` (Haiku-Stream
  in slot 1) ersetzt durch ein direktes `ewb_signal`-Emit (`typ=_label or match['keyword']`,
  `known=True`, `source='keyword'`). Der `keyword_einwand_match`-Emit (Slot-0-Instant-Profil-
  Antwort + FE-Highlight) bleibt unveraendert.
- **`_emit_qa_slot1` / `_emit_soft_hint` (analyse_loop/QA):** kein `qa_slot1`/`qa_soft_hint`-Emit
  mehr. In diesem Pfad liegt KEIN sauberes kurzes Einwand-Label vor (nur die `_kat`-Roh-Enum-
  Kategorie ODER der volle Haiku-Absatz / Literal-Hint) → per Blocklist KEIN Signal (lieber
  kein Button als ein Geister-/Paragraph-Button). abstain-/intent_event-Logik (DSGVO/TAXO)
  unangetastet.
- **`streame_manual_ewb_variante`:** alle drei Emits (pip_stream_start/pip_token/pip_token_done)
  tragen jetzt `'source': 'manual_button'` — kennzeichnet den EINZIGEN legitimen Lese-Zonen-
  Schreiber fuer das FE-Source-Gate. Keine weitere Aenderung (Punkt 17).

### Task 2 — Frontend (static/pip-launcher.js)
- **A) Source-Gate (T-PIP-01):** pip_stream_start / pip_token / pip_token_done schreiben slot 1
  NUR bei `d.source === 'manual_button'` — zweite Verteidigungslinie zum BE-Cut. slot 0 unveraendert.
- **B) `ewb_signal`-Handler (NEU):** highlight known / create new + Newest-wins Groesse 1
  (`_applyEwbSignal` / `_applyBufferedAutoSignal`). Eingangs-Guard verwirft `!typ` / `>40 Zeichen` /
  Roh-Enum-Werte. Gating-Hook (`state.pinActive`) fuer Plan 02 vorbereitet — in Plan 01 immer
  falsy (kein `pinActive=true`).
- **C) Coaching-TEXT raus aus slot 1:** `if (slot === 1) return;` als erste Zeile von
  `_showProactiveContent` + `_showProactiveTipp`. Definitionen + Aufruf @2408 + Datenfluss bleiben.
- Legacy-Handler `qa_slot1` / `qa_soft_hint` → bewusste No-Ops (verhindern dass ein verspaeteter
  Legacy-/Replay-Emit die Lese-Zone ueberschreibt).

## Pflicht-Vermerke (aus dem Plan-Output)

### 1. Pruning-Notiz + Lock-Obsoleszenz + ewb_signal-Label-Garantie
- **`streame_auto_variante`:** nicht mehr aufgerufen → als PIP.4-Foundation markiert (NICHT
  geloescht, Body unveraendert — Foundation-Code-Register-Eintrag "PIP.4 — streame_auto_variante").
- **`qa_slot1` / `qa_soft_hint`:** BE-Emit entfernt; FE-Handler zu No-Ops degradiert.
- **`slot1_variant_busy_until`-Lese-Zonen-Lock:** obsolet — kein Setter mehr (qa_slot1-Set
  entfernt, Keyword-Set entfernt, manual setzte ihn nie). Der READ-Guard im analyse_loop-Dispatch
  (claude_service.py:1547) + der MP3-Lese-Block bleiben **inert** stehen (busy_until nie mehr in
  die Zukunft gesetzt → Skip feuert nie) und werden zusammen mit der MP3-Instrumentierung in
  **Plan 05** entfernt. `live_session.py`-Default-Key NICHT angefasst (Foundation).
- **ewb_signal-Label-Garantie (Cross-AI HIGH #1/#2 + LOW):** `typ` ist IMMER ein echtes kurzes
  Label (`_label`/`match['keyword']`) oder es wird gar kein Signal emittiert. Nie ein Absatz, nie
  ein Literal-Hint, nie ein Roh-Enum-Wert. BE-Garantie + FE-Eingangs-Guard (Laenge + Blocklist).

### 2. Circuit-Breaker-Removal-Audit (Cross-AI MEDIUM #1)
grep gegen den Produktiv-Code (2026-06-29):
```
_ewb_fallback_until / _ewb_ttft_history / _ewb_circuit_lock
  → claude_service.py:18-20 (Defs) + 591/595/638/639/661-668 (Body) — ALLE in streame_auto_variante
  → tests/test_ewb_autovar_global_regression.py (Regressions-Test)
  → KEIN externer Produktiv-Reader/-Schreiber.
```
**Ergebnis:** Kalt-Stellen sicher (dormanter Circuit-Breaker ohne lebende Abhaengige).
Foundation-Code-Register-Eintrag "PIP.4 — streame_auto_variante (inkl. dormanter TTFT-Circuit-
Breaker)" gesetzt. Der Regressions-Test bleibt gueltig (Funktion + Globals existieren weiter).

**Konsequenz (no silent cap — explizit gemeldet):** Mit dem Kalt-Stellen entfaellt auch der
`record_suggestion_offer(slot='B', source='auto_variante')`-Capture (claude_service.py:739) — es
wird keine Auto-Variante mehr generiert, also gibt es keine Auto-Suggestion mehr zu erfassen
(inhaerent durch LOCKED-Decision (a): Auto = nur Highlight, keine Generierung). Falls der
TAXO2/DPO-Korpus die `slot='B'`-Auto-Vorschlaege spaeter doch braucht → in PIP.4 reaktivieren.
Der manuelle Knopf-Capture (deepgram_service.py:1007) ist NICHT betroffen.

### 3. Coaching-Signal-Erhalt (André-Direktive)
- **(a) Erhaltene Signale:** Phase / Kaufbereitschaft (kb) / Tempo / Redeanteil — Compute im
  **slot-0-Analyse-Pfad** (phase: claude_service.py:418-423; readiness: compute_readiness_score
  1339 / ls.kaufbereitschaft / score_p4 1407-1419). Diese liegen AUSSERHALB der vier in Task 1
  geprunten Auto-Lese-Zonen-Pfade → unangetastet. Emit-Felder `result.phase` / `result.kb` kommen
  weiter im `pip_token_done`-Payload an.
- **(b) Wo PIP.2 abgreift:** `_showProactiveContent` / `_showProactiveTipp` bekommen `d.result`
  (mit phase/kb) weiterhin uebergeben (Aufruf @2408 bleibt); nur die Text-Ausgabe in slot 1 ist
  geguarded. PIP.2 rendert die Daten Text → Symbol/Rahmenfarbe um.
- **(c) Referenz:** Foundation-Code-Register-Eintrag "PIP.2 — Coaching-Signal-Anzeige".

## Race-Condition-Audit (Punkt 14, 4 Fragen) — siehe Plan; strukturell adressiert
1. Manual vs Auto gleichzeitig: Auto schreibt slot 1 GAR NICHT mehr → kein Ueberschreiben moeglich.
2. Verbindungsabbruch mid-Stream: unveraendert (kein Regress); ewb_signal ist Einzel-Emit.
3. Reconnect-Replay: Source-Gate verwirft auto/source-lose slot-1-Emits; ewb_signal nur Highlight (Newest-wins 1).
4. Tab im Hintergrund: ewb_signal sammeln sich nicht (lastAutoSignal Groesse 1).

## Verifikation (auf Local == Prod-Baseline, grep)
- ✓ `sio.emit('ewb_signal'` in deepgram mit echtem Label (`_ewb_typ`), nicht `text`.
- ✓ Kein `sio.emit('qa_slot1'` / `'qa_soft_hint'` mehr im BE.
- ✓ "Neuer Einwand"-Literal nur noch im Kommentar, kein aktiver Emit.
- ✓ `streame_auto_variante` kein aktiver Caller mehr (nur def + Kommentare).
- ✓ Circuit-Breaker-Globals nur in streame_auto_variante + Regressions-Test.
- ✓ FE: Source-Gate (3×), ewb_signal-Handler mit Guard+Blocklist, pinActive nur READ (nie =true).
- ✓ FE: `_showProactiveContent`/`_showProactiveTipp` Definitionen + Aufruf @2408 vorhanden, slot===1-Guard ergaenzt, result.phase/result.kb-Lesen unveraendert.

## OFFEN — Task 3 (blocking human-verify, André)
Live-Cold-Call auf Prod nach `bash deploy.sh production`:
1. EINEN Einwand-Knopf klicken → Antwort erscheint, langsam vorlesen (Selbst-Trigger provozieren).
2. Erwartet: Vorschlag bleibt STEHEN (kein Ueberschreiben); Auto erscheint NUR als Button-Highlight/
   neuer Button mit ECHTEM KURZEM Label (kein Paragraph, kein Geister-Button, kein Coaching-Text in slot 1).
3. `inspect.sh logs 500 | grep "BUGB-EWB] MP4"` → waehrend aktivem manual-Stream KEIN auto-source-Write.

Resume-Signal: "approved" → weiter; sonst beobachtetes Verhalten + MP4/MP6-Logzeilen melden.
