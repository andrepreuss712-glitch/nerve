---
phase: 04.2-cold-call-und-meeting-modi
plan: 02
subsystem: ui
tags: [mode-selection, consent-flow, ewb-buttons, socket-io, vanilla-js, jinja2]

# Dependency graph
requires:
  - phase: 04.2-cold-call-und-meeting-modi-plan-01
    provides: EWB trigger endpoint /api/ewb_trigger, beenden accepts session_mode, start_live_session handles mode payload

provides:
  - Mode selector overlay on /live before any session starts
  - Consent modal with Vorleseskript for Meeting mode
  - Silent fallback to Cold Call on consent rejection
  - EWB buttons rendered from profile einwaende or DACH fallback list
  - EWB click triggers /api/ewb_trigger and renders response in AI panel
  - Mode badge in header showing Cold Call or Meeting during session
  - socket.emit start_live_session carries {mode} payload to server
  - beenden() sends session_mode in POST body

affects: [deepgram_service, app_routes, live-session, post-call-analytics]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Mode overlay blocks all session activity until user explicitly selects a mode
    - Consent flow: Meeting selection → modal → accept (meeting) or reject (cold_call fallback)
    - EWB buttons populated from profile data at session activation time
    - DSGVO banner deferred to after mode selection, not on socket connect

key-files:
  created: []
  modified:
    - templates/app.html
    - static/app.js

key-decisions:
  - "DSGVO banner deferred from socket.on('connect') to activateSession() — mode overlay must appear first, banner only relevant after session mode confirmed"
  - "activateSession() handles both timer start and DSGVO banner — centralizes post-mode-selection setup"
  - "Socket connect handler guards mic auto-start behind sessionMode check — prevents mic start before mode selected on reconnect"

patterns-established:
  - "Mode overlay pattern: fixed inset overlay with z-index:900 blocks UI until mode chosen"
  - "EWB button pattern: DACH_FALLBACK_EINWAENDE as constant, profile einwaende override via window._profileEinwaende"

requirements-completed: [MODE-01, MODE-03, MODE-04, MODE-05]

# Metrics
duration: 15min
completed: 2026-04-03
---

# Phase 4.2 Plan 02: Cold Call/Meeting Mode Selection Summary

**Mode selector overlay with Cold Call/Meeting cards, consent modal with Vorleseskript, EWB objection buttons from profile or DACH fallback, and mode badge in header — all wired into backend endpoints from Plan 01**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-03T09:20:00Z
- **Completed:** 2026-04-03T09:35:00Z
- **Tasks:** 1 auto + 1 checkpoint (auto-approved)
- **Files modified:** 2

## Accomplishments
- Mode selector overlay appears on /live before any mic access or session start
- Meeting consent modal with Vorleseskript — accept starts Meeting mode, reject silently falls back to Cold Call
- EWB buttons rendered from active profile's einwaende list with DACH fallback (7 standard objection types)
- Mode badge visible in header during session (Cold Call = blue, Meeting = teal)
- socket.emit('start_live_session', {mode}) wired to backend Deepgram configuration
- beenden() sends session_mode for post-call analytics

## Task Commits

Each task was committed atomically:

1. **Task 1: Mode overlay, consent modal, EWB buttons, mode badge (HTML/CSS + JS)** - `be5b06f` (feat)
2. **Task 2: Verify Cold Call and Meeting mode system (checkpoint)** - auto-approved via auto_advance

**Plan metadata:** (pending final commit)

## Files Created/Modified
- `templates/app.html` - Added mode overlay HTML, consent modal HTML, mode badge in header, EWB bar container, profile einwaende data script, CSS for all new components
- `static/app.js` - Added sessionMode state, selectMode/acceptConsent/rejectConsent/activateSession functions, EWB rendering and triggerEwb, modified startMicStream to pass mode, modified beenden to send session_mode, guarded DSGVO banner and mic start behind sessionMode

## Decisions Made
- DSGVO banner moved from socket.on('connect') to activateSession() — the banner should appear after mode selection, not immediately on socket connect while the overlay is still showing
- activateSession() centralizes all post-mode-selection setup: overlay hide, badge update, DSGVO banner, timer start, EWB render, mic start
- Socket connect handler now guards mic auto-start behind `if (sessionMode)` — handles reconnect scenario correctly (mode already set) without triggering mic before overlay is dismissed

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] DSGVO banner timing fix**
- **Found during:** Task 1 (review of socket.on('connect') handler)
- **Issue:** Plan K said to guard DSGVO banner to only show after mode selection, but activateSession() also needed to explicitly trigger the banner since socket is already connected by the time the user picks a mode
- **Fix:** Added DSGVO banner logic directly in activateSession() (not just in socket.on('connect')). The socket.on('connect') handler also guards it behind sessionMode check for reconnect scenarios. This ensures the banner always fires exactly once after mode selection, regardless of socket timing.
- **Files modified:** static/app.js
- **Verification:** Banner logic present in both activateSession() and socket.on('connect') with `_shown` flag preventing double-display
- **Committed in:** be5b06f (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 2 - missing critical timing fix)
**Impact on plan:** Necessary for correct DSGVO banner behavior. The plan's steps K and L only handled the socket.on('connect') path but not the "socket already connected when mode is picked" path.

## Issues Encountered
None beyond the DSGVO banner timing deviation documented above.

## Known Stubs
None — all EWB buttons are wired to /api/ewb_trigger (implemented in Plan 01), mode selection triggers real Deepgram configuration, and session_mode is persisted via beenden().

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Cold Call and Meeting mode infrastructure complete
- Phase 4.2 fully done — both plans (01 backend + 02 frontend) implemented
- Ready for Phase 4 (Payments & Stripe) after Gewerbeanmeldung blocker resolved

---
*Phase: 04.2-cold-call-und-meeting-modi*
*Completed: 2026-04-03*
