---
phase: 08.23.2.METRIK-1
plan: 06
subsystem: auswertungs-anzeige
tags: [d-12, d-14, d-15, d-16, d-17, d-19, gap-c, abwesenheits-anker, rot-vor-gruen]
requires:
  - templates/session_detail.html (Plan 03 — if/elif-Kette, ehrliche Markierung)
  - routes/dashboard.py (Plan 05 — kopfzeile_display / fokus_display in beiden Render-Zweigen)
  - routes/app_routes.py (_cross_context_objections_live, _derive_practice_recommendations)
provides:
  - Auswertungsseite eines LIVE-Anrufs ohne Gesamtnote — kein Hero, kein Vierer-Aufriss, kein
    Trend-Abzeichen, kein "Result: X/100", kein Trainings-Notendurchschnitt
  - Score-Hero mit NEUER typ-Weiche — TRAINING behaelt seine Note (D-17, Gap C)
  - routes/dashboard.py ohne Leser und ohne Namens-Nennung der alten Vierer-Formel
  - _cross_context_objections_live in der Schwester-Form {sessions, focus_match}
  - 5 neue Laufzeit-Tests in tests/test_session_detail_render.py
affects:
  - Plan 07 (macht Form 2 sichtbar + rollt 05/06/07 gemeinsam aus + Schild-Migration 0041)
  - Plan 08 (Abwesenheits-Anker Sollwert 0 auf den Namen der alten Formel ueber routes/ + services/
    — durch diesen Plan in routes/dashboard.py erfuellbar gemacht; zweiter Leser in
    routes/app_routes.py:1375 bleibt und faellt dort)
tech-stack:
  added: []
  patterns:
    - "typ-Weiche als Anzeige-Entscheidung: die Seite entscheidet selbst, ob eine Note angebracht ist"
    - "Beschreiben statt zitieren — ein Kommentar, der einen Namen nennt, laesst ihn seine eigene Loeschung ueberleben"
    - "Abwesenheits-Anker auf Code-FORM, nie auf ein Wort der Prosa (zwei Fehlmessungen belegt)"
    - "Loeschen ohne Neubau: der bestehende Sonst-Zweig traegt den Fall bereits"
key-files:
  created: []
  modified:
    - templates/session_detail.html
    - routes/dashboard.py
    - routes/app_routes.py
    - tests/test_session_detail_render.py
decisions:
  - "Der Score-Hero bekommt eine NEUE typ-Weiche statt ersatzlos zu fallen — sonst stirbt die Trainings-Note still"
  - "Der Vierer-Aufriss faellt GANZ, auch der Live-Zweig (D-16) — sonst bliebe die immer-100%-Redeanteil-Zeile"
  - "Der Trend-Streifen faellt ERSATZLOS, auch fuer Training — keine neutrale Ersatz-Zahl (D-14)"
  - "'Kaufbereitschaft Ende: N/100' bleibt: ehrlich beschriftet, faellt mit dem ERZEUGER in der Folgephase"
  - "FUND: Der Empty-State-Satz in Sektion 13 nannte die geloeschte Note im LIVE-Zweig — gestrichen"
metrics:
  tasks: 3
  commits: 7
  completed: 2026-08-15
---

# Phase 08.23.2.METRIK-1 Plan 06: Die alte Note verlaesst die Live-Seite — Summary

Die Auswertungsseite eines **echten Anrufs** zeigt keine Zahl mehr, die wie eine Note aussieht —
und eine **Trainings-Sitzung** zeigt ihre Note unveraendert, an einer echten Trainings-Sitzung
**belegt statt angenommen**.

## ⛔ Kein Deploy in diesem Plan

**Kein Task hier hat `bash deploy.sh production` aufgerufen**, es gab keinen Neustart und keinen
Deploy-Meta-Schreibvorgang. Zwischen „alte Note raus" (hier) und „Form 2 rein" (Plan 07) haette die
Auswertungsseite ein Loch: keine Note **und** keine Kopfzeile. Ausgerollt wird in **Plan 07, Task 3**,
gemeinsam mit Plan 05 und 07.

⚠ **Was auf dem Server liegt, ausdruecklich benannt:** `templates/session_detail.html`,
`routes/dashboard.py`, `routes/app_routes.py` und `tests/test_session_detail_render.py` wurden per
`scp` nach `/opt/nerve/app` transportiert — sonst waere jeder Triage-Lauf gegen den alten Stand
**blind statt rot** (ROT-LAUF-REZEPT, Bedingung 1). Der laufende Gunicorn haelt weiterhin den alten
Code im Speicher.

## 📌 Punkt 23 — Schild-Schuld: NEIN, dieser Plan erzeugt keine

**Ausdruecklich gesagt statt verschwiegen:** Dieser Plan **entfernt Anzeigen und Berechnungen**. Er
legt keine Spalte an, aendert keine, schreibt keine Daten und aendert die **Bedeutung** keiner
Spalte. `conversation_logs.kb_end` und `conversation_logs.typ` behalten Wortlaut und Bedeutung ihrer
Schilder; `kb_end` bleibt fuer Training die Trainings-Gesamtnote und fuer Live die Kaufbereitschaft —
genau wie das bestehende Schild es beschreibt. **Kein Migrations-Nachzug faellig, kein `alembic/`-Diff.**

⚠ Die **offene** Schild-Schuld aus Plan 05 (`rubric_score.observations_jsonb` hat mit `slow_lane.py`
einen zweiten Schreiber) ist **nicht meine** und wird in **Plan 07 Task 3 (Migration 0041)**
nachgezogen. Sie wurde hier weder angefasst noch geschlossen.

## Was gebaut wurde

| Task | Gate | Ergebnis | Commit |
|---|---|---|---|
| 1 | RED | 4 Tests + `_seed_session` bekommt `typ`/`kb_end`/`redeanteil_avg`/`skript_abdeckung` | `4a75e79` |
| 1 | GREEN | Hero typ-abhaengig, Vierer-Aufriss + Trend-Abzeichen + Kopf-„Result" gefallen | `e12853d` |
| 1 | FIX | Redeanteil-Anker von Prosa auf Code-Form umgestellt (Fehlmessung, s.u.) | `63bdc4f` |
| 2 | — | `score_total` nur fuer Training, alte Formel + `trend_avg` raus | `01ee23a` |
| 3 | RED | `test_kein_trainings_durchschnitt_auf_der_live_seite` (+ Reihenfolge-Fix) | `efd1fe2` / `7afbe40` |
| 3 | GREEN | `_cross_context_objections_live` auf die Schwester-Form, cc-Zweig gefallen | `e837be7` |

**Basis-Commit fuer alle Regions-Anker (verbatim, wie vom Plan verlangt):**
`BASIS = e364262aedf57966dd95518d279ed2404a575a3e`

## ROT-vor-GRUEN — die Laeufe verbatim

Alle ueber `scripts/triage.sh` **auf dem Server**, nie lokal (HART-Regel: kein lokales `pytest`,
kein lokales `python`, kein lokales `python -c`). Alle Laeufe haben den **Pruefkatalog erreicht** —
kein Collection-Error, kein Werkzeug-Ausfall (Rezept-Bedingung 4).

### Lauf A — Task 1, gegen den Stand mit typ-blindem Hero

```
=========================== short test summary info ============================
FAILED tests/test_session_detail_render.py::test_live_zeigt_keine_gesamtnote
FAILED tests/test_session_detail_render.py::test_kein_vierer_aufriss_mehr - A...
FAILED tests/test_session_detail_render.py::test_kein_trend_abzeichen_bei_training
================== 3 failed, 12 passed, 12 warnings in 11.11s ==================
[triage] FEHLER: pytest gegen nerve_test ROT (kein Restart, kein Deploy)
```

Die Fehler im Wortlaut:

```
/opt/nerve/app/tests/test_session_detail_render.py:514: AssertionError: Der Live-Anruf zeigt
weiterhin einen Gesamt-Score — genau die Zahl, die zu 40 % die Kaufbereitschaft des Kunden mass.
    assert 'Gesamt-Score' not in '<!DOCTYPE h...dy>\n</html>'

tests/test_session_detail_render.py:570: AssertionError: Der Vierer-Aufriss steht noch auf der Seite.
    assert 'n-session-detail-breakdown' not in '<!DOCTYPE h...dy>\n</html>'

tests/test_session_detail_render.py:595: AssertionError: Das Trend-Abzeichen erscheint im Training
weiter. ...
    assert 'vs Schnitt letzte 5' not in '<!DOCTYPE h...dy>\n</html>'
```

★ **Ehrlich benannt: `test_training_behaelt_seine_note` war schon VOR dem Bau gruen.** Das ist kein
Mangel, sondern sein Zweck — er ist der **Gap-C-Regressions-Anker**. Er kann per Bauart nicht durch
den Neubau rot werden; er muss rot werden, sobald **spaeter** jemand die Note auch dem Training
wegnimmt. Ein Test, der nur beim Neubau rot sein kann, waere danach wertlos (dieselbe Begruendung
wie bei den zwei „Regeln gegen ein Zuviel" aus Plan 05).

**GRUEN danach:** `15 passed` — alle fuenfzehn Funktionen einzeln mit `PASSED` ausgewiesen.

### Lauf B — Task 3, gegen den Stand mit `avg_score`

Der erste Lauf war rot **auf dem Existenz-Anker** und damit **mehrdeutig** („Sonst-Zweig fehlt" kann
vor dem Umbau auch heissen: der Durchschnitts-Zweig griff korrekt). Die Assertion-Reihenfolge wurde
deshalb umgedreht (`7afbe40`) und der Lauf **neu gezogen** — ein mehrdeutiger ROT-Beleg ist kein
Beleg:

```
/opt/nerve/app/tests/test_session_detail_render.py:667: AssertionError: Die Live-Auswertungsseite
zeigt weiterhin einen Trainings-Notendurchschnitt.
    assert 'Zuletzt im Training: Ø' not in '<!DOCTYPE h...dy>\n</html>'
======================== 1 failed, 13 warnings in 8.30s ========================
[triage] FEHLER: pytest gegen nerve_test ROT (kein Restart, kein Deploy)
```

Der Lauf ist ein **echter** Beleg: er ging durch Login, FORCE-RLS-GUC, Route, den
Empfehlungs-Helfer und den Render — die Seite lieferte HTTP 200, und der Notendurchschnitt stand
sichtbar drin.

**GRUEN danach:** `16 passed` (`tests/test_session_detail_render.py` vollstaendig).

### Gesamtlauf mit den Deploy-Markern

Mit **denselben** Markern wie das Deploy-Tor (`-m "not live and not perf"`, Werkzeug-Divergenz aus
Plan 02):

```
1239 passed, 7 skipped, 5 deselected, 108 warnings in 79.12s (0:01:19)
[triage] pytest gegen nerve_test bestanden — nerve_test wird per trap gedroppt
```

**Die Zahl ist rechenbar, nicht bloss gross:** 1234 (Stand nach Plan 05) + 5 neue Test-Funktionen
(4 aus Task 1, 1 aus Task 3) = **1239**. Die Basis ist nicht gewandert, keine fremde Pruefung ist
nebenbei umgefallen.

## Bau-Regel 20 — die drei Pflicht-greps, VOR den Loeschungen gezogen

⚠ **Entdeckungs-greps ohne Sollwert, kein Abnahme-Gate** (der Plan sagt es ausdruecklich).

### 1. `grep -rn "_calc_call_score" routes/ services/ tests/ --include=*.py`

```
routes/app_routes.py:1007:def _calc_call_score(conv):
routes/app_routes.py:1375:        scores = [_calc_call_score(c) for c in recent]
routes/dashboard.py:742:    from routes.app_routes import _calc_call_score, _derive_practice_recommendations
routes/dashboard.py:764:        #  - Live: _calc_call_score (Gesamt-Score, 4 Komponenten)
routes/dashboard.py:765:        #  - Training: kb_end Fallback (kein _calc_call_score fuer Training)
routes/dashboard.py:786:            # Score mit dem effektiven kb_end berechnen (mini-Shim um _calc_call_score
routes/dashboard.py:792:                score_total = _calc_call_score(conv)
routes/dashboard.py:799:        # Live    -> _calc_call_score ueber letzte 5 typ='live'.
routes/dashboard.py:800:        # Training-> kb_end ueber letzte 5 typ='training' (kein _calc_call_score
routes/dashboard.py:813:                trend_avg = round(sum(_calc_call_score(c) for c in recent) / len(recent))
```

★ **ACHT Vorkommen in `routes/dashboard.py`** — die vom Plan am 14.08. nachgemessene Zahl stimmt.
**Nur DREI davon (`:742`, `:792`, `:813`) sind Code**; fuenf sind Kommentare, und **`:764` schreibt
`_calc_call_score (` mit Leerzeichen, `:765` `_calc_call_score fuer`** — beide entkommen jedem
klammer-gebundenen Anker. Genau sie haetten Plan 08 Task 2 (`grep -rn "_calc_call_score" routes/
services/` → **0**) per Bauart unerfuellbar gemacht, weil `routes/dashboard.py` dort nicht im
files-Block steht.

★ **Der zweite Leser ist belegt und terminiert:** `routes/app_routes.py:1375` innerhalb der
Trend-Route des Anruf-Fensters. **Die Funktion wird deshalb HIER NICHT geloescht** — Definition
(`:1007`) und zweiter Leser fallen gemeinsam in **Plan 08**. Kein Test liest sie.

### 2. `grep -rn "trend_avg" routes/ templates/ --include=*.py --include=*.html`

```
routes/dashboard.py:804:        trend_avg = None
routes/dashboard.py:813:                trend_avg = round(sum(_calc_call_score(c) for c in recent) / len(recent))
routes/dashboard.py:818:                trend_avg = round(sum(_vals) / len(_vals)) if _vals else None
routes/dashboard.py:1057:                trend_avg=trend_avg,
templates/session_detail.html:75:      {# Wave 4 / POLISH-33: Trend-Badge typ-agnostisch — Route liefert trend_avg
templates/session_detail.html:77:      {% if trend_avg is not none %}
templates/session_detail.html:78:        {% set delta = score - trend_avg %}
```

Sieben Treffer, **alle** in den beiden Dateien dieses Plans, **alle** gefallen. Kein Leser ausserhalb.

### 3. `grep -rn "kb_end_effective" routes/ templates/ --include=*.py --include=*.html`

```
routes/app_routes.py:1189/1197/1199/1200/1201   (Ableitung im Empfehlungs-Helfer)
routes/app_routes.py:1206/1209                  (Regel 1 — TRAINING-Zweig, siehe unten)
routes/app_routes.py:1282/1283/1286             (Regel 2 — LIVE-Zweig, "Kaufbereitschaft Ende")
routes/dashboard.py:771/778/781/782/783         (Ableitung in session_detail)
routes/dashboard.py:791/796                     (der Shim — gefallen)
routes/dashboard.py:1062                        (Kontext — BLEIBT, Training braucht ihn)
templates/session_detail.html:51/52             (Kopfbereich — jetzt typ-abhaengig)
templates/session_detail.html:91/92             (Vierer-Aufriss — gefallen)
```

`kb_end_effective` **bleibt berechnet und uebergeben** — Training nutzt es fuer Hero und
Kopfbereichs-Zeile.

### 4. `grep -rn "avg_score" routes/ templates/ static/ --include=*.py --include=*.html --include=*.js` (Task 3)

```
routes/app_routes.py:1112/1124/1134/1135   <- DIESER PLAN (gefallen)
routes/app_routes.py:1354/1374/1377        -> /api/postcall/trend, faellt in PLAN 08
routes/dashboard.py:478                    -> 'avg_score': 0 in einer anderen Route (nicht Scope)
routes/performance.py:102/158/261/262/264/265/469/476   -> PLAN 09
templates/dashboard.html:516/526           -> PLAN 09 (liest kpis.avg_score)
templates/session_detail.html:413/415      <- DIESER PLAN (gefallen)
static/pip-launcher.js:3498/3502           -> PLAN 08 (Anruf-Fenster)
```

⚠ **Der Kartierer hatte fuer `routes/performance.py:158` keinen Leser gefunden — der grep zeigt
ihn:** `templates/dashboard.html:516` liest `k.avg_score`. Die Zaehlung war kein Beweis; **es gibt
einen Leser.** `performance.py` wird in **Plan 09** angefasst, nicht hier.

★ **Kollisions-Gegenprobe ueber Plan-Grenzen (die teuerste Sorte, deshalb ausdruecklich):** Der neue
Docstring in `_cross_context_objections_live` **nennt `avg_score` beschreibend**. Geprueft wurde, ob
ein Folge-Plan darauf einen 0-Anker fuehrt: Plan 09 fuehrt `grep -c "'avg_score'" routes/performance.py`
→ 0 und `grep -c "avg_score" routes/performance.py` — beide **datei-gebunden auf
`routes/performance.py`**. **Keine Kollision.** In Plan 08 kommt `avg_score` als Anker nicht vor.

## Die geprueft-und-begruendet stehengelassenen Stellen (Bau-Regel 20, Zustand 2)

Bau-Regel 20 kennt zwei zulaessige Zustaende — **geloescht** oder **geprueft und mit Begruendung
stehengelassen**. Diese drei sind der zweite:

### 1. `Kaufbereitschaft Ende: {kb_end_effective}/100` (`routes/app_routes.py`, LIVE-Zweig)

```
$ grep -c "Kaufbereitschaft Ende: {kb_end_effective}/100" routes/app_routes.py
1
$ grep -c "ehrlich beschriftet" routes/app_routes.py
1
```

**Sie bleibt.** Sie ist **ehrlich beschriftet** — sie nennt die **Kaufbereitschaft**, also genau das,
was `kb_end` misst, und schreibt sie **nicht** dem Verkaeufer als Leistung zu. D-19 verlangt „kein
LIVE-Anruf zeigt eine **Gesamtnote**"; eine korrekt benannte Kunden-Kennzahl ist keine Note. Sie
faellt mit dem **ERZEUGER** (`kb_*`, eigene Folgephase), nicht hier. Die Begruendung steht **als
Kommentar in derselben Zeile** und hier — damit sie in der **D-19-Abnahme (Plan 09, Schritt 3)** nicht
als Ueberbleibsel gelesen wird.

### 2. `Gesamt-Score unter 50 ({kb_end_effective}/100)` (`routes/app_routes.py:1209`) — im Plan nicht gelistet

⚠ **Vom Ausfuehrer gefunden, ausdruecklich geprueft.** Diese Zeile heisst woertlich „Gesamt-Score"
und zeigt eine `/100`-Zahl — auf den ersten Blick genau die Krankheit dieser Phase.

**Sie steht innerhalb von `if typ == 'training':`** (`routes/app_routes.py:1204`, Block
„Training-spezifische Regeln"). Sie erreicht **keinen** LIVE-Anruf. **D-17 gilt: Training behaelt
seine Bewertung.** Kein Fund, keine Aenderung — aber ohne diesen Eintrag waere sie in Plan 09 als
`/100`-Rest aufgetaucht und haette Sucharbeit gekostet.

### 3. Die zwei verbleibenden `style="--w:` (Farb-HART-Regel, Sonderfall „dynamischer Wert")

```
$ grep -n 'style="--w:' templates/session_detail.html
329:      <div class="n-session-detail-progress"><span style="--w: {{ skript_pct }}%"></span></div>
486:            <div class="n-session-detail-minibar"><span style="--w: {{ ((kat.score or 0) * 10) }}%"></span></div>
$ grep -c 'style="--w:' templates/session_detail.html
2
```

**Zahl exakt wie vorhergesagt: 2.** ⚠ **Die Zeilennummern des Plans (`:313`, `:470`) stimmen NICHT
mehr** — die Datei ist seit der Plan-Messung um 16 Zeilen gewachsen (Plan 03 + dieser Plan). **Die
Fundstellen sind dieselben Stellen:** `:329` ist der **Skript-Fortschritts-Balken** (eine
**Abdeckungs-Quote**, keine Note) und `:486` die **Kategorie-Minibar im Trainings-Zweig** (D-17). Beide
liegen ausserhalb des Scope dieses Plans; sie anzufassen waere ein **Reparatur-Modus-Verstoss**.

## Die `conv.typ == 'training'`-Weichen: vorher und nachher

| | Zahl | Fundstellen (`grep -n`) |
|---|---|---|
| **vorher** (Basis `e364262`) | **7** | `:6` Header-Klasse · `:13` Wiederhol-Knopf · `:22` Badges · `:224` Diagramm-Titel · `:228` Canvas-Kennung · `:233` Diagramm-Zweig · `:337` Skript-Abdeckung |
| **nachher** | **9** | dieselben sieben (verschoben) **plus** die neue Kopfbereichs-Weiche und die neue **Hero**-Weiche |

⚠ **Die Plan-Angabe „belegte Vorher-Zahl 4 (`:13`, `:22`, `:202`, `:306`)" ist eine Fehlmessung** —
gemessen am 2026-08-15 waren es **7**, und keine der vier Zeilennummern trifft. Der Sollwert **>= 6**
ist davon unberuehrt und **erfuellt** (9). **Der Befund aendert aber die Aussagekraft des Ankers: er
war noch weiter davon entfernt, eine vergessene Hero-Weiche zu erkennen, als der Plan annahm.** Genau
deshalb steht der Nachbarschafts-Anker daneben.

## Ausgefuehrte Abnahme-Anker

### Task 1 — `templates/session_detail.html`

| Anker | Soll | Ist | |
|---|---|---|---|
| `grep -c "n-session-detail-breakdown"` | 0 | **0** | ✅ |
| `grep -c "n-session-detail-hero"` (Existenz-Partner) | 1 | **1** | ✅ |
| `grep -c "vs Schnitt letzte 5"` | 0 | **0** | ✅ |
| `grep -c "n-badge n-badge-neutral"` (Existenz-Partner) | >= 1 | **7** | ✅ |
| `grep -c "trend_avg"` | 0 | **0** | ✅ |
| `grep -c "score_total"` (Existenz-Partner) | 1 | **1** | ✅ |
| `grep -c "{% if conv.typ == 'training' %}"` | >= 6 | **9** | ✅ |
| `grep -A2 "{% if conv.typ == 'training' %}" \| grep -c "n-session-detail-hero"` | 1 | **0** | ⚠ **Fehlmessung, siehe unten** |
| `grep -A3 "{% if conv.typ == 'training' %}" \| grep -c "n-session-detail-hero"` (Ersatz) | 1 | **1** | ✅ |
| `grep -B2 "Result: {{ kb_end_effective" \| grep -c "conv.typ == 'training'"` | 1 | **1** | ✅ |
| `grep -c "Result: {{ kb_end_effective"` | 1 | **1** | ✅ |
| `grep -c "def test_live_...\|..._training_behaelt...\|..._kein_vierer...\|..._kein_trend..."` (Testdatei) | 4 | **4** | ✅ |
| `git diff $BASIS -- templates/session_detail.html \| grep "^+" \| grep -c 'style="--w:'` | 0 | **0** | ✅ |
| `git diff $BASIS -- templates/session_detail.html \| grep -c "^-"` (Nicht-Leer-Beleg) | >= 10 | **55** | ✅ |
| `grep -c 'style="--w:'` | 2 | **2** | ✅ |

### Task 2 — `routes/dashboard.py`

| Anker | Soll | Ist | |
|---|---|---|---|
| `grep -c "_calc_call_score("` | 0 | **0** | ✅ |
| `grep -c "_derive_practice_recommendations"` (Existenz-Partner) | >= 2 | **2** | ✅ |
| `grep -c "_calc_call_score"` (**ohne** Klammer, Kommentare zaehlen mit) | 0 | **0** | ✅ |
| `grep -c "Training: eigener Wert, bleibt (D-17)"` (Existenz-Partner) | 1 | **1** | ✅ |
| `grep -c "score_total = kb_end_effective if conv_typ == 'training' else None"` (**Gap-C-Anker**) | 1 | **1** | ✅ |
| `grep -c "trend_avg"` | 0 | **0** | ✅ |
| `grep -c "def session_detail"` (Existenz-Partner) | 1 | **1** | ✅ |
| `grep -rn "trend_avg" templates/ routes/ \| wc -l` | 0 | **1** | ⚠ **Fehlmessung, siehe unten** |
| `grep -rn "trend_avg" templates/ routes/ --include=*.py --include=*.html \| wc -l` (Ersatz) | 0 | **0** | ✅ |
| `grep -c "score_total"` (Existenz-Partner) | >= 2 | **4** | ✅ |

**Zitier-Sperre eingehalten:** Weder der neue Kommentarblock (A0) noch der Shim-Ersatz, der
Trend-Ersatz oder der Import-Kommentar nennt den Funktionsnamen — belegt durch den 0-Anker **ohne**
Klammer. Alle vier beschreiben („die alte Vierer-Formel", „ihr Funktionsname wird hier bewusst NICHT
genannt").

**`recent` vor der Loeschung geprueft** (`grep -n "recent" routes/dashboard.py`): die Abfrage in
`:805` diente **ausschliesslich** dem Trend-Block (`:811-818`). Die uebrigen Treffer (`:135`, `:158`,
`:494-497`, `:537-538`, `:607-608`, `:648-653`) liegen in **anderen Funktionen** und sind unberuehrt.
Die Abfrage ist mit dem Block gefallen.

### Task 3 — `routes/app_routes.py` + `templates/session_detail.html`

| Anker | Soll | Ist | |
|---|---|---|---|
| `grep -c "func.avg(CL.kb_end)" routes/app_routes.py` | 0 | **0** | ✅ |
| `grep -c "def _cross_context_objections_live" routes/app_routes.py` (Existenz-Partner) | 1 | **1** | ✅ |
| `grep -c "Zuletzt im Training" templates/session_detail.html` | 0 | **0** | ✅ |
| `grep -c "Im Training:" templates/session_detail.html` (Existenz-Partner) | >= 1 | **2** | ✅ |
| `grep -c "'sessions': n, 'focus_match': einwand_typ" routes/app_routes.py` | 1 | **1** | ✅ |
| `grep -c "cc.avg_score" templates/session_detail.html` | (Zahl feststellen) | **0** | ✅ siehe unten |
| `grep -c "Kaufbereitschaft Ende: {kb_end_effective}/100" routes/app_routes.py` (Existenz) | 1 | **1** | ✅ |
| `grep -c "ehrlich beschriftet" routes/app_routes.py` | >= 1 | **1** | ✅ |

**Der Training-Zweig ist NICHT angefasst — ausdruecklich festgehalten:** `grep -c "cc.avg_score"`
liefert **0**. Der Executor hat **beide** `cc`-Zweige gelesen, bevor er geloescht hat: der
Live-Zweig (`{% if conv.typ == 'live' %}`) trug die zwei `cc.avg_score`-Vorkommen, der
Training-Zweig darunter (`{% else %}`, „Noch nie im echten Call begegnet" / „Im echten Call: N×
versucht, M× behandelt") **war ohnehin ohne Score** — er liest `cc.sessions` und `cc.n_success`.
`_cross_context_objections_training` lieferte nie einen Score. **Nur der Live-Zweig wurde angefasst.**

## ⚠ DREI Anker-Befunde — falsche Sollwerte, KEIN Code gebogen

### Befund 1 — der Nachbarschafts-Anker war um **eine Zeile** zu kurz (`-A2` statt `-A3`)

```
$ grep -A2 "{% if conv.typ == 'training' %}" templates/session_detail.html | grep -c "n-session-detail-hero"
0
$ grep -A3 "{% if conv.typ == 'training' %}" templates/session_detail.html | grep -c "n-session-detail-hero"
1
```

**Der Plan gibt den Hero-Block VERBATIM vor** — und in genau dieser vorgegebenen Fassung stehen
**zwei** `{% set %}`-Zeilen zwischen der Weiche und der `<section>`. Der Anker `-A2` kann sie
deshalb **per Bauart nie sehen**: der Plan widerspricht sich selbst zwischen `<action>` und
`<acceptance_criteria>`.

★ **Der einzige Weg zum Original-Sollwert waere gewesen, den vom Plan vorgeschriebenen Code
umzustellen, damit ein grep gruen wird** — woertlich der dokumentierte Reflex „**der Anker formt das
Artefakt**". **Gebaut ist der Plan-Code; der Sollwert ist als falsch dokumentiert.** Die Zusage,
die der Anker sichern sollte, ist mit `-A3` **mechanisch belegt** und zusaetzlich
**laufzeit**-belegt durch `test_training_behaelt_seine_note`.

### Befund 2 — `grep -rn "trend_avg" templates/ routes/ | wc -l` zaehlt eine `.pyc`-Datei mit

```
$ grep -rn "trend_avg" templates/ routes/
Binary file routes/__pycache__/dashboard.cpython-311.pyc matches
```

Der einzige verbliebene Treffer ist ein **stales Uebersetzungs-Zwischenprodukt** (`cpython-311`,
Datum 28.07. — der Server faehrt 3.12), also ein **Werkzeug-Artefakt, keine Quelldatei**. Der Anker
ohne `--include`/`-I` misst am Quellcode vorbei. Belastbar sind
`grep -rn ... --include=*.py --include=*.html | wc -l` → **0** und `grep -rnI ... | wc -l` → **0**.
Die Datei wurde **nicht** angefasst (kein Aufraeumen ausserhalb des Auftrags).

### Befund 3 — der Redeanteil-Anker zeigte auf **Prosa**, die legitim woanders steht

Der Plan verlangt „kein `Redeanteil` in der Hero-Region". Als ganzseitige Zeichenketten-Pruefung
gebaut, wurde der Test **nach** dem korrekten Umbau rot:

```
tests/test_session_detail_render.py:571: AssertionError: Die Redeanteil-Zeile lebt weiter ...
    assert 'Redeanteil' not in '<!DOCTYPE h...dy>\n</html>'
```

**Ursache:** `_derive_practice_recommendations` (Regel 3) erzeugt bei Sitzungen **mit**
Speaker-Trennung eine **Uebungs-Empfehlung** zum Redeanteil. Die ist **kein Ueberbleibsel des
Aufrisses**, sondern ein eigener, gewollter Coaching-Hinweis. Der Anker wurde auf die **Code-Form**
der Aufriss-Zeile umgestellt (`Gewichtung 20% im Gesamt-Score`, ihr `title`-Tooltip) **plus** eine
Gegenprobe ueber die drei anderen Gewichtungs-Tooltips — **kein Produktivcode wurde angepasst, um
den Anker zu erfuellen.**

★ **Damit stehen in dieser Phase ELF Instanzen der Klasse „Anker folgt dem Artefakt"** (acht bis
Plan 05, drei hier). Alle elf zeigen auf **Prosa, Zeilenzahlen oder Werkzeug-Ausgabe** statt auf
eine **Code-Form** in der geprueften Datei.

## ★ FUND ueber die Lesestellen des Plans hinaus

### 1. Die achte Lesestelle — `Result: X/100` im Kopfbereich (im Plan enthalten, hier bestaetigt)

`templates/session_detail.html:51-52` zeigte bei **jedem** Anruf `kb_end_effective` als
`Result: N/100`. Bei einem LIVE-Anruf ist das die **Kaufbereitschaft des Kunden**, beschriftet wie
eine Note des Verkaeufers. **Erledigt** — jetzt innerhalb einer `conv.typ == 'training'`-Weiche.

### 2. NEU — eine ZEHNTE Stelle: der Empty-State-Satz in Sektion 13 nannte die geloeschte Note

**Vom Ausfuehrer gefunden, im Plan nicht gelistet, in keiner der neun Lesestellen enthalten.**
Im **LIVE**-Zweig von Sektion 13 („Einzel-Bewertungen folgen") stand woertlich:

> „… werden ab Phase 4.19 (Transkript-Persistierung) verfügbar. **Aktuell siehst du den
> Gesamt-Score und seine 4 Komponenten in Sektion 2.**"

**Zwei Schaeden in einem Satz:**
1. **Er war nach diesem Plan unwahr** (Punkt 23: das Schild wandert mit der Bedeutung) — es gibt
   fuer LIVE weder Gesamt-Score noch vier Komponenten noch Sektion 2.
2. **Er machte den Abwesenheits-Anker dieses Tasks per Bauart unerfuellbar**, weil er den Namen der
   alten Note im LIVE-Zweig weitertrug — `test_live_zeigt_keine_gesamtnote` waere nie gruen geworden.

**Behandlung: ersatzlos gestrichen** (nicht umformuliert — eine Ersatz-Verheissung waere wieder eine
Zusage ohne Deckung), mit Begruendungs-Kommentar an Ort und Stelle. **Aus NEUN Lesestellen sind
damit ZEHN geworden.** Kein Anker auf `_calc_call_score` haette sie gefunden — sie enthielt weder
den Funktionsnamen noch eine Zahl.

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21 + Punkt 26)

**Dieser Plan schreibt keine Daten und aendert kein Schema.** Angefasst (nur **lesend**):
`conversation_logs` (`kb_end`, `kb_verlauf`, `typ`) und `objection_events` (ueber
`_cross_context_objections_live`) — beide ohne RLS.

### `inspect.sh schema conversation_logs` (verbatim, Auszug)

```
                                               Table "public.conversation_logs"
          Column          |            Type             | Collation | Nullable |                    Default
--------------------------+-----------------------------+-----------+----------+-----------------------------------------------
 kb_end                   | integer                     |           |          |
 typ                      | character varying(20)       |           |          |
Indexes:
    "conversation_logs_org_id_fkey" FOREIGN KEY (org_id) REFERENCES organisations(id)
    "conversation_logs_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id)
    TABLE "calls" CONSTRAINT "fk_calls_conversation_log_id" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id)
    TABLE "objection_events" CONSTRAINT "objection_events_conversation_log_id_fkey" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id)
```

**Erwartung bestaetigt:** `kb_end` ist `integer`, **nullable**, **ohne CHECK** auf den Wertebereich.
`typ` ist `varchar(20)`, **nullable**, **ohne CHECK** auf die erlaubten Werte.

### ★ Die real vorkommenden `typ`-Werte (T-METRIK1-06-02) — und ein Befund

```
$ sudo -u postgres psql -d nerve -tAc "SELECT typ, count(*), min(kb_end), max(kb_end)
                                       FROM conversation_logs GROUP BY typ ORDER BY 2 DESC;"
live|78|0|100
```

**Es gibt auf Production genau EINEN `typ`-Wert: `live` (78 Zeilen).** Kein dritter Wert — die
Gefahr „ein dritter Typ faellt durch beide Zweige und hat weder Hero noch Form 2" ist **heute nicht
real**. Sie bleibt **strukturell** moeglich, weil weder DB noch ORM den Wertebereich erzwingen
(kein CHECK, kein Enum); der Code faellt dann in den `else`-Zweig = **Live-Verhalten** (keine Note),
also in die **harmlose** Richtung.

❌ **Der unbequeme Teil desselben Befunds, ausdruecklich:** **Es gibt auf Production NULL Zeilen mit
`typ='training'`.** Die Zusage „die Trainings-Note lebt weiter" ist damit **ausschliesslich** durch
den Laufzeit-Test gegen `nerve_test` belegt — **nicht** an einer echten Trainings-Sitzung aus dem
Bestand. Das ist ein echter Beleg (Login, Route, Render, HTTP 200, Zahlenwert im HTML), aber es ist
**kein Real-Daten-Beleg**. Wer den Gap-C-Punkt im Deploy-Checkpoint von Plan 07 abnimmt, muss dafuer
**erst eine Trainings-Sitzung erzeugen**.

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `conv.kb_end` | `routes/dashboard.py` → Training-Hero | `conversation_logs.kb_end` (integer, nullable, kein CHECK) | live am Call-Ende bzw. von `routes/training.py` am Trainings-Ende; der Anzeige-Pfad laeuft danach. ⚠ **Der Name luegt:** bei `typ='training'` steht dort die **Trainings-Gesamtnote**. | ✅ `inspect.sh schema` oben |
| `conv.kb_verlauf` | gelesen, JSON-geparst | DB-Spalte (Text) | wie oben; kaputter JSON faellt bereits heute auf `conv.kb_end` zurueck | ✅ Bestandslogik unveraendert |
| `conv.typ` | steuert **beide** neuen Weichen | `conversation_logs.typ` (varchar(20), nullable, kein CHECK) | einmalig beim Anlegen. Real vorkommend: **nur `live`**. Dritter Wert faellt in den `else`-Zweig = Live-Verhalten (harmlose Richtung). | ✅ Realdaten-Abfrage oben |
| `avg_score` (`_cross_context_objections_live`) | **entfaellt** | war `AVG(conversation_logs.kb_end)` | n/a | ✅ Loeschung, Bau-Regel-20-grep davor |
| `trend_avg` | **entfaellt** | war eine Berechnung ueber die letzten fuenf Sitzungen | n/a | ✅ Loeschung, Bau-Regel-20-grep davor |

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
Dieser Plan entfernt **Anzeigen und Berechnungen**, keine gespeicherten Daten. `conversation_logs.kb_end`
bleibt befuellt — METRIK-1 entfernt die **Verbraucher**, die Folgephase den **Erzeuger**.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — unwahrer Anzeige-Text, der zugleich den Anker sperrte] Empty-State-Satz in Sektion 13**
- **Found during:** Task 1, beim Ausfuehren der Abnahme-Anker
- **Issue:** siehe „FUND 2" oben — der Satz nannte im LIVE-Zweig die geloeschte Note.
- **Fix:** Schluss-Satz ersatzlos gestrichen, Begruendungs-Kommentar daneben.
- **Files modified:** `templates/session_detail.html`
- **Commit:** `e12853d`

**2. [Rule 1 — Fehlmessung im eigenen Test] Redeanteil-Anker auf Code-Form umgestellt**
- **Found during:** Task 1, GRUEN-Lauf (der Test war **nach** dem korrekten Umbau rot)
- **Issue:** siehe „Befund 3" oben.
- **Fix:** Anker auf den `title`-Tooltip der Aufriss-Zeile + Gegenprobe ueber alle vier
  Gewichtungen. **Kein Produktivcode angepasst.**
- **Files modified:** `tests/test_session_detail_render.py`
- **Commit:** `63bdc4f`

### Abweichungen von Plan-Vorgaben (bewusst, dokumentiert statt still gebogen)

**3. `_seed_session` bekommt VIER neue Parameter statt nur `typ`.**
Der Plan nennt nur den Trainings-Seed. `kb_end` ist noetig, weil der Trainings-Hero sonst leer
rendert und der Zahlen-Anker nichts pruefen koennte; `redeanteil_avg=100` und `skript_abdeckung`
machen `test_kein_vierer_aufriss_mehr` **diskriminierend** — mit ihnen haette der alte Aufriss
sichtbare Werte gezeigt, ohne sie waeren die Zeilen auch vorher schon leer gewesen.

**4. Die Assertion-Reihenfolge in Task 3 wurde umgedreht.**
Der Plan setzt den Existenz-Anker zuerst. Im ROT-Lauf war die Fehlermeldung dadurch **mehrdeutig**
(„Sonst-Zweig fehlt" heisst vor dem Umbau: der Durchschnitts-Zweig griff **korrekt**). Absenz zuerst
liefert einen **eindeutigen** ROT-Beleg; fuer den GRUEN-Zustand ist die Reihenfolge folgenlos, der
Existenz-Anker greift unveraendert.

**5. Der neue Docstring in `_cross_context_objections_live` nennt `avg_score` beschreibend.**
Der Plan schreibt genau diesen Text verbatim vor. Weil „Beschreiben statt Zitieren" in dieser Phase
sonst durchgaengig gilt, wurde die **Kollisions-Gegenprobe ueber alle Folge-Plaene gezogen**
(oben, Pflicht-grep 4): kein Folge-Plan fuehrt `avg_score` als 0-Anker fuer `routes/app_routes.py`.
**Kein Konflikt — aber geprueft, nicht angenommen.**

### Keine Auto-Fixes ausserhalb des Auftrags

Die sieben Commits fassen **ausschliesslich** `templates/session_detail.html`, `routes/dashboard.py`,
`routes/app_routes.py` und `tests/test_session_detail_render.py` an. `.claude/settings.local.json`
und das unversionierte `_design_export/` liegen unveraendert im Arbeitsverzeichnis (aelter als dieser
Plan, gehoeren keinem seiner Tasks) — **bewusst nicht committet**. `routes/__pycache__/` wurde
**nicht** aufgeraeumt, obwohl es einen Anker verfaelschte: Aufraeumen ausserhalb des Auftrags ist ein
Reparatur-Modus-Verstoss.

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Die Seite entscheidet selbst anhand von `conv.typ`, ob eine Note angebracht ist.
2. **Nachvollziehen:** Die Trainings-Note bleibt in `conversation_logs.kb_end` gespeichert und in der
   Admin-Sicht auswertbar (D-18, `routes/admin_ewb.py` **unberuehrt**).
3. **Eingreifen koennen:** Die Weiche ist ein einzeiliges `conv.typ == 'training'` — in Sekunden
   umstellbar, falls Training doch Form 2 bekommen soll (eigene Phase).
4. **Marge:** Eine Zahl, die zu 40 % die Laune des Kunden misst und dem Verkaeufer als Leistung
   ausgewiesen wird, kostet Vertrauen — und Vertrauen ist bei einem Coaching-Produkt das Produkt.

## Grenzen (Punkt-31-Haltung)

Keines der neuen Bauteile ist ein **Waechter** im Sinne von Punkt 31. Die Grenzen gehoeren trotzdem
hierher, weil ihr Schweigen sonst als Beweis gelesen wuerde.

1. **Die fuenf neuen Tests pruefen GENAU EINE Seite** (`/session/<sid>`) und **genau die
   Zeichenketten in ihrem Pruefkatalog**: `Gesamt-Score`, `n-session-detail-hero`, `Result:`,
   `vs Schnitt letzte 5`, `n-session-detail-breakdown`, `Gewichtung 20/30/40/10%`,
   `Zuletzt im Training: Ø`. **Eine Note in anderer Schreibweise wuerde nicht auffallen** — und die
   uebrigen Bildschirme (Anruf-Fenster, Wochen-Dashboard, Sitzungs-Liste) sind **gar nicht im
   Katalog**; die fallen in Plan 08 und 09.
2. **Der Gap-C-Beleg ist ein Test-Beleg, kein Real-Daten-Beleg.** Auf Production gibt es **null**
   `typ='training'`-Zeilen (Abfrage oben).
3. **Der Nachbarschafts-Anker beweist nur die Nachbarschaft im TEXT**, nicht die Schachtelung: er
   sieht nicht, ob die Weiche vor dem `</section>` wieder geschlossen wird. Diese Luecke schliesst
   der Laufzeit-Test, nicht der grep.
4. **`_calc_call_score` lebt weiter** — Definition und zweiter Leser in `routes/app_routes.py`. Dieser
   Plan belegt nur, dass `routes/dashboard.py` sie **weder aufruft noch nennt**. Bis Plan 08 rechnet
   die alte Formel im Anruf-Fenster unveraendert.
5. **Kein Mensch hat die neue Seite gesehen.** Alle Belege sind maschinell.

## Was NICHT bewiesen ist

- **Kein echter Anruf ist durch diesen Code gelaufen** und **kein Mensch hat die Seite angesehen** —
  der Anblick kommt im Deploy-Checkpoint von **Plan 07**.
- **Die Trainings-Sicht ist an keiner echten Trainings-Sitzung geprueft** (null Zeilen auf Prod).
- **Der Rest der Anwendung zeigt die Note weiter:** Anruf-Fenster (Plan 08), Wochen-Dashboard und
  Sitzungs-Liste (Plan 09), Lernkarte als **Fliesstext** (eigener Roadmap-Punkt). „Kein LIVE-Anruf
  zeigt eine Gesamtnote — auf keinem Bildschirm" ist **nach diesem Plan noch nicht erfuellt**; die
  Phasen-Abnahme D-19 liegt in Plan 09.
- **Zwei fremde UAT-Punkte bleiben offen und gehoeren NICHT zu diesem Plan:** der Wirkungs-Beleg des
  Substanz-Tors am echten Anruf (Plan 02) und die drei Browser-Pruefungen (Plan 03). Dieser Plan
  schliesst **keinen** davon.

## Pruning-Notiz (Vault-Regel Nr. 7)

**Geloescht:**
- Score-Hero fuer LIVE (inkl. `{% set score %}` / `{% set score_class %}` fuer Live)
- Trend-Abzeichen **komplett** (beide Typen) — `templates/session_detail.html`
- Vierer-Aufriss **komplett** (`div.n-session-detail-breakdown` + vier Zeilen + vier Minibars)
- `Result: X/100` fuer LIVE (jetzt typ-gebunden)
- Empty-State-Schluss-Satz in Sektion 13 (Fund 2)
- Der Shim um die alte Formel + ihr Import — `routes/dashboard.py`
- Trend-Block **inkl. `recent`-Abfrage** (die nur ihn bediente) + `trend_avg` im Kontext
- `func.avg(CL.kb_end).label('avg_score')` + `avg_score` im Rueckgabe-Dict — `routes/app_routes.py`
- Der mittlere `cc`-Zweig („Zuletzt im Training: Ø …") — der `{% else %}` trug ihn schon

**Bleibt vorerst, mit Begruendung statt stillschweigend:**
- **`_calc_call_score`** (Definition `routes/app_routes.py:1007` + zweiter Leser `:1375`) — faellt in
  **Plan 08**. Belegt durch den Pflicht-grep oben.
- **`kb_end_effective`** in `routes/dashboard.py` — Training braucht es fuer Hero und Kopfzeile.
- **Die drei geprueft-und-begruendeten Stellen** (oben): `Kaufbereitschaft Ende: N/100`,
  `Gesamt-Score unter 50` (Training-Zweig), die zwei `style="--w:`.

**Nichts wurde „fuer spaeter" behalten, ohne einen Termin zu nennen.**

## Threat Flags

Keine. Der Plan fuehrt **keine** neue Netz-Schnittstelle, keinen Auth-Pfad, keinen Datei-Zugriff und
keine Schema-Aenderung ein. Kein `alembic/`-Diff (T-METRIK1-06-03 belegt).

| Threat ID | Umsetzung | Beleg |
|---|---|---|
| T-METRIK1-06-01 (Trainings-Note stirbt still) | NEUE typ-Weiche am Hero + eigener Verhaltens-Test | `test_training_behaelt_seine_note` (Laufzeit, HTTP 200 + Zahlenwert im HTML). **Abnahme am echten Training steht aus** (Plan 07, null Prod-Zeilen). |
| T-METRIK1-06-02 (dritter `conv.typ`-Wert) | Realdaten geprueft: **nur `live`**, kein dritter Wert; `else`-Zweig = Live-Verhalten (harmlose Richtung) | Abfrage oben; kein CHECK in der DB — Grenze benannt |
| T-METRIK1-06-03 (versehentliche Datenloeschung) | ausschliesslich Anzeige-/Berechnungspfade; kein `DELETE`, keine Migration | `git diff $BASIS --name-only`: **4 Dateien**, keine unter `alembic/` |

## Known Stubs

Keine. Es wurde nichts durch einen festen Ersatzwert ersetzt — die Zahlen sind **weg**, nicht durch
Platzhalter ersetzt. Der ausdrueckliche Verzicht auf eine „neutrale Ersatz-Zahl" beim Trend-Streifen
ist eine **Entscheidung** (D-14, im Code begruendet), kein Stub.

⚠ **Bewusst unvollstaendig, nicht vergessen:** `score_total` ist fuer LIVE jetzt `None` und geht
trotzdem in den Template-Kontext. Das ist **kein toter Wert**, sondern die Bedingung dafuer, dass der
Trainings-Zweig denselben Kontext nutzt; kein Template-Zweig liest ihn fuer LIVE (Anker
`grep -c "score_total" templates/session_detail.html` = 1, innerhalb der Trainings-Weiche).

## Self-Check: PASSED

- ✅ **Dateien:** `templates/session_detail.html`, `routes/dashboard.py`, `routes/app_routes.py`,
  `tests/test_session_detail_render.py` existieren (einzeln geprueft).
- ✅ **Commits:** `4a75e79`, `e12853d`, `63bdc4f`, `01ee23a`, `efd1fe2`, `7afbe40`, `e837be7` sind im
  Repo auffindbar und gepusht.
- ✅ **Abnahme:** alle Anker der drei Tasks **ausgefuehrt**, Ergebnisse verbatim oben; die drei
  Fehlmessungen sind als **falsche Sollwerte** dokumentiert, nicht als Code-Abweichung.
- ✅ **Tests:** `1239 passed / 7 skipped / 5 deselected` ueber `scripts/triage.sh` mit den
  Deploy-Markern, mit zwei ROT-Belegen davor.
- ✅ **Kein Deploy, kein Neustart, kein lokaler Lauf** — weder `pytest` noch `python` noch `python -c`.

## Naechster Schritt

**Plan 07** — dort wird Form 2 sichtbar (Kopfzeile, die eine Sache, Aufklapper, „diesmal nichts"),
die Schild-Migration `0041` fuer `observations_jsonb` laeuft, und **Plan 05, 06 und 07 werden
gemeinsam ausgerollt** (`bash deploy.sh production` — agenten-gesperrt, Halt-Punkt fuer Andre).
Im Deploy-Checkpoint mit abzunehmen: **eine echte Trainings-Sitzung** (Gap C an Realdaten) und der
Anblick der notenfreien Live-Seite.
