---
phase: 08.23.2.METRIK-1
plan: 08
subsystem: anruf-fenster
tags: [pip, note-raus, trend-ersatzlos, knopf-aus-der-klammer, pflicht-nachtrag-17-08, ein-aufraeumer]
requires:
  - .planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-07-SUMMARY.md (Form 2 sichtbar, Redeanteil-Norm raus)
  - .planning/DIALOG-GSD-CLAUDIAN.md (Block 17.08. — Quelle des Pflicht-Nachtrags, Task 2b)
provides:
  - static/pip-launcher.js — keine Note im Anruf-Fenster, Trend ersatzlos, Knopf "Auswertung" aus der Noten-Klammer gezogen
  - static/pip-launcher.js — "Naechster Call" ruft den EINEN Aufraeumer; gemerkter Ausgang wird genullt; kein Rueckfall auf den vorigen Ausgang
  - templates/base.html — Noten-Zahl, Etikett und Trend-Streifen samt CSS entfernt
  - routes/app_routes.py — /api/postcall/trend und _calc_call_score geloescht
  - tests/test_pip_keine_note.py — 3 Tests (Route weg, Formel weg, Anruf-Fenster raeumt auf)
affects:
  - Plan 09 (Wochen-Dashboard) — die letzten final_score-Leser stehen dort (templates/dashboard.html)
  - Folgephase ERZEUGER — _calc_process_score/_apply_outcome_modifier/_calc_coaching_score
  - Roadmap 4.0.2 — Redeanteil-Kachel im Anruf-Fenster (baubedingt konstant im Kaltakquise-Modus)
tech-stack:
  added: []
  patterns:
    - "Ein Aufraeumer, ein Besitzer: jeder Weg in den naechsten Anruf RUFT ihn, keiner raeumt selbst (MODULARE BAUWEISE, Leitsatz)"
    - "Kopf-Kommentare zaehlen keine Aufrufer mehr auf — eine still veraltende Liste hat genau diesen Fehler verdeckt"
    - "Kontrollfluss-Vertrag statt Zeilen-grep: Funktionsrumpf per Klammer-Zaehlung ausschneiden, dann pruefen"
    - "Leser-Invariante als Anker: ein Wert, der nur noch geschrieben und nirgends gelesen wird, kann nicht mehr zurueckfallen"
decisions:
  - "Fehlt der Ausgang, wird GAR KEINER angezeigt — ein falscher Ausgang ist schlimmer als keiner (Andre/Claudian 17.08.)"
  - "Der Trend-Streifen faellt ersatzlos — vertagt bis zur Fokus-Serie, nicht gestrichen"
  - "_calc_process_score / _apply_outcome_modifier bleiben: METRIK-1 entfernt VERBRAUCHER, die Folgephase den ERZEUGER"
  - "Der Verhaltens-Test des Browser-Fehlers laeuft als Kontrollfluss-Vertrag — auf dem Ausroll-Server gibt es keine JS-Laufzeit"
metrics:
  tasks: 3 von 4 (Task 3 = Halt-Punkt Andre)
  commits: 5
  completed: 2026-08-18 (Code); Abnahme offen
---

# Phase 08.23.2.METRIK-1 Plan 08: Anruf-Fenster ohne Note — Summary

Das Anruf-Fenster benotet nicht mehr. Die Zaehl-Kacheln bleiben, der Knopf „Auswertung" ist aus der
Noten-Klammer herausgezogen und erscheint jetzt bedingungslos, der Trend-Streifen ist an beiden
Stellen ersatzlos gefallen, und beide Formel-Kopien (`_calc_call_score`, `_calcScore`) samt
`/api/postcall/trend` sind geloescht. Dazu der **Pflicht-Nachtrag vom 17.08.**: nach „Nächster Call"
steht kein Wert des vorigen Anrufs mehr im Fenster — und ein fehlender Ausgang wird lieber gar nicht
angezeigt als falsch.

---

## 🔴 ZUERST: Was hier NICHT bewiesen ist

**Task 3 (`bash deploy.sh production` + Andres Latenz-Abnahme beim Auflegen) ist NICHT gelaufen.**
Der Befehl ist fuer Agenten hart gesperrt (Deny-Regel in `settings.json`). Es fehlen deshalb:

- die **Deploy-Tor-Ausgabe verbatim** (Pflicht-Inhalt laut `<output>`) — **OFFEN**,
- **Andres Latenz-Beobachtung beim Auflegen** (Punkt 3 aus Schritt 2, die eigentliche Zusage aus
  D-13/Punkt 25) — **OFFEN**,
- die Bestaetigung von **Schritt 3** (Knopf in beiden Zustaenden) und **Schritt 3b**
  (zwei Anrufe, „Nächster Call" dazwischen) — **OFFEN**,
- die **Server-Gegenprobe** aus Schritt 4 (`inspect.sh routes`) — **OFFEN**.

Diese vier Punkte sind hier **nicht erfunden und nicht abgehakt**. Der Plan gilt erst mit ihnen als
abgenommen.

### Zustand von Production in diesem Moment (gemessen, nicht vermutet)

```
$ ssh ... 'cd /opt/nerve/app && md5sum static/pip-launcher.js templates/base.html routes/app_routes.py'
c69d039c1a346022ab39d85287f137e9  static/pip-launcher.js      ← == lokaler HEAD (mit Task 1 + 2b)
e8e584b90340ca6816f43f799f068f44  templates/base.html         ← == lokaler HEAD (mit Task 1)
2121b272bfd5355c9ac4134f9df8b957  routes/app_routes.py        ← == lokaler HEAD (mit Task 2)

$ systemctl show nerve -p ActiveEnterTimestamp
ActiveEnterTimestamp=Mon 2026-08-17 05:20:57 UTC     ← VOR allen Commits dieses Plans
```

**Lesart, wichtig fuer die Abnahme:** Die **Dateien** auf Production entsprechen dem heutigen Stand
(`templates/`, `routes/` lagen schon so da; `static/pip-launcher.js` und `tests/test_pip_keine_note.py`
hat dieser Lauf per `scp` nachgezogen — Bedingung 1 des ROT-Lauf-Rezepts: der Code muss auf dem
Server liegen, sonst ist der Lauf blind statt rot). Der **laufende Prozess** ist aelter: gunicorn
wurde seit dem 17.08. 05:20 UTC nicht neu gestartet, `routes/app_routes.py` ist dort also noch die
ALTE Fassung im Speicher. Konkret:

- **Browser-Seite ist schon neu** (statische Dateien werden pro Anfrage von der Platte gelesen):
  wer jetzt einen Anruf faehrt, sieht bereits das Anruf-Fenster ohne Note.
- **Server-Seite ist noch alt**: `/api/postcall/trend` antwortet im laufenden Prozess weiterhin.
  Das stoert nichts — das neue Anruf-Fenster ruft die Route gar nicht mehr auf.
- `bash deploy.sh production` (Task 3, Andre) macht beides deckungsgleich und startet den Dienst neu.

Die zwei per `scp` transportierten Dateien liegen als `root:root 0644` — lesbar fuer den
Test-Lauf als `nerve_app`; `deploy.sh` schreibt sie ohnehin aus dem tar neu.

---

## Was gebaut wurde

| Task | Was | Commit |
|---|---|---|
| 1 | PiP: Note raus, Knopf aus der Klammer, `_calcScore` + `_fetchAndRenderTrend` + Trend-Markup/CSS geloescht | `b8cd77f` |
| 2 (ROT) | `tests/test_pip_keine_note.py` — Route-404 + Laufzeit-Schnittstelle, je mit gepaartem Existenz-Anker | `2db86be` |
| 2 (GRUEN) | `/api/postcall/trend` und `_calc_call_score` geloescht, Abgrenzungs-Kommentar gesetzt | `dff1d66` |
| 2b (ROT) | Verhaltens-/Kontrollfluss-Test fuer „Nächster Call" | `0ac846c` |
| 2b (GRUEN) | `nextCall()` ruft den Aufraeumer · gemerkter Ausgang genullt · Rueckfall entfernt · Kopf-Kommentar nachgezogen | `90876bd` |
| 3 | Ausrollen + Latenz-Abnahme | **OFFEN — Halt-Punkt Andre** |

⚠ **Task 1 und Task 2 wurden in einem frueheren Lauf gebaut.** Dieser Lauf hat Task 2b gebaut und
die Belege fuer Task 1/2 **nachtraeglich** erhoben (frische greps gegen den heutigen Stand +
`git show`). Alles unten, was mit **[nachtraeglich erhoben]** markiert ist, wurde also NICHT vor der
jeweiligen Loeschung gezogen, sondern danach — die Aussagekraft fuer „was war vorher da" ist
entsprechend geringer, und das steht hier ausdruecklich statt still.

---

## Task 2b — der Pflicht-Nachtrag vom 17.08. (dieser Lauf)

### Herkunft

Andre hat vier Anrufe hintereinander gefahren und jeweils „Nächster Call" gedrueckt. Auf allen vier
Bildschirmfotos standen **identische Werte**. Die Datenbank (als `postgres`, Prod) sagte etwas
anderes:

| Anruf | Dauer | `outcome` | `coaching_score` |
|---|---|---|---|
| 20:46 | 55 s | no_interest | **21.845** |
| 20:47 | 44 s | no_interest | — |
| 20:48 | 45 s | no_interest | — |
| 20:50 | 38 s | **(leer)** | — |

Vier Dauern, eine Anzeige. Und beim vierten Anruf stand ein Ausgang auf dem Schirm, den die
Datenbank fuer diesen Anruf gar nicht hatte. **Bestandsfehler, nicht aus dieser Phase** — das
Anruf-Fenster war von METRIK-1 bis Task 1 unberuehrt.

### Die drei Pflicht-greps VOR der Aenderung (Bau-Regel 20, Entdeckung — kein Zielwert)

```
$ grep -n "_resetLiveState" static/pip-launcher.js
2161:    // called _resetLiveState(), this guarantees a clean start for call N+1 regardless
2163:    _resetLiveState();
3008:  function _resetLiveState() {
3060:    console.log('[NerveLauncher] _resetLiveState() complete');
3131:    _resetLiveState();

$ grep -n "state.lastOutcome" static/pip-launcher.js
4288:      summary.textContent = '✓ ' + escHtml(_outcomeLabelDe(json.outcome || state.lastOutcome));
4480:        state.lastOutcome = json.outcome || selectedOutcome;

$ grep -rn "lastOutcome" static/ templates/
static/pip-launcher.js:4288:      summary.textContent = '✓ ' + escHtml(_outcomeLabelDe(json.outcome || state.lastOutcome));
static/pip-launcher.js:4480:        state.lastOutcome = json.outcome || selectedOutcome;
```

**Zur Pflicht-Frage „hat `_resetLiveState()` einen dritten, bisher unbenannten Aufrufer?" —
NEIN.** Es sind genau **zwei** echte Aufrufer (`:2163` in `_showPipLive()`, `:3131` in `endCall()`);
die uebrigen Treffer sind ein Kommentar (`:2161`), die Definition (`:3008`) und eine Log-Zeile
(`:3060`). Der Kopf-Kommentar der Funktion war insofern korrekt — er zaehlte nur die Wege, die sie
**rufen**, und verschwieg den dritten Weg, der sie **nicht** ruft. Genau darin lag der Fehler.

### Gebaut (A/B/C/D)

**A — `nextCall()` ruft den bestehenden Aufraeumer** (kein Nachbau, keine Kopie seiner Logik):

```javascript
    _cleanup();
    // METRIK-1 Pflicht-Nachtrag 17.08.: Dieser Weg raeumte den Nach-Anruf-Zustand NICHT.
    // _cleanup() raeumt Mikrofon, Verbindung, Timer und Slots — den Zustand NACH dem Anruf
    // raeumt der Aufraeumer unten, und der wurde hier nie gerufen. ...
    _resetLiveState();
    open();
```

**B — der gemerkte Ausgang wird in DERSELBEN Nullung mitgeraeumt** (`_resetLiveState()`, direkt
neben `pendingPostcall`/`lastCallId`, nicht daneben).

**C — der Rueckfall auf den Ausgang des vorigen Anrufs faellt weg.** Fehlt der Ausgang, wird jetzt
gar keine Ausgangs-Zeile mehr erzeugt:

```javascript
      if (json.outcome) {
        var summary = _doc.createElement('div');
        summary.className = 'pip-outcome-summary';
        summary.textContent = '✓ ' + escHtml(_outcomeLabelDe(json.outcome));
        sec.appendChild(summary);
      }
```

**D — Kopf-Kommentar nachgezogen.** Er zaehlt keine Aufrufer mehr auf, sondern beschreibt die Regel
(„jeder Weg, der einen neuen Anruf beginnt, ruft IHN") und haelt fest, warum die alte Liste
gefaehrlich war. Eine Liste, die still veraltet, ist der Mechanismus, der diesen Fehler verdeckt hat.

### ROT vor GRUEN — verbatim

Gezogen **vor** A/B/C/D, nach dem ROT-Lauf-Rezept: erst `git commit` + `git push` + `scp` des
Testfiles nach `/opt/nerve/app` (md5 lokal == md5 auf dem Server, `c2e0272…`), dann
`scripts/triage.sh`. **Kein Werkzeug-Ausfall** — das Tor lief bis in die Zusicherung durch
(`Dump-Treue-Katalog-Gate OK`, Test gesammelt, Test ausgefuehrt):

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
    'cd /opt/nerve/app && bash scripts/triage.sh \
     tests/test_pip_keine_note.py::test_naechster_call_raeumt_den_vorigen_anruf_weg -v --tb=long'

=================================== FAILURES ===================================
_______________ test_naechster_call_raeumt_den_vorigen_anruf_weg _______________
...
        # ── ① Der Weg "Naechster Call" ruft den bestehenden Aufraeumer.
>       assert '_resetLiveState()' in rumpf_next, (
            'Der Weg "Naechster Call" raeumt den Nach-Anruf-Zustand nicht weg: er ruft den '
            'Aufraeumer nicht. Mikrofon, Verbindung und Timer werden geraeumt, der Zustand des '
            'vorigen Anrufs bleibt stehen und erscheint im naechsten Anruf-Fenster.'
        )
E       AssertionError: Der Weg "Naechster Call" raeumt den Nach-Anruf-Zustand nicht weg: er ruft den Aufraeumer nicht. Mikrofon, Verbindung und Timer werden geraeumt, der Zustand des vorigen Anrufs bleibt stehen und erscheint im naechsten Anruf-Fenster.
E       assert '_resetLiveState()' in 'function nextCall() {\n    // BUG-11b FIX: null state.pipWindow BEFORE calling .close() so the pagehide\n    // guard (state.pipWindow === pipWindow) always evaluates false for the old window.\n    // BUG-11 r5: callGen++ invalidiert auch noch ausstehende endCall-fetches, deren\n    // .then()-Handler würden sonst _showPostcall in die Live-UI der neuen Session blasen.\n    state.callGen = (state.callGen || 0) + 1;\n    var oldWin = state.pipWindow;\n    state.pipWindow = null;\n    if (oldWin && !oldWin.closed) oldWin.close();\n    _cleanup();\n    open();\n  }'

tests/test_pip_keine_note.py:211: AssertionError
=========================== short test summary info ============================
FAILED tests/test_pip_keine_note.py::test_naechster_call_raeumt_den_vorigen_anruf_weg
======================== 1 failed, 12 warnings in 8.47s ========================
[triage] FEHLER: pytest gegen nerve_test ROT (kein Restart, kein Deploy)
DROP DATABASE
```

Der rote Lauf zeigt den **ganzen Rumpf** von `nextCall()` im Klartext — er ist damit zugleich der
Beleg fuer den Befund („`_cleanup(); open();` und sonst nichts").

### GRUEN — nach der Reparatur, gleicher Befehl, gleicher Server

```
$ ssh ... 'cd /opt/nerve/app && bash scripts/triage.sh \
     tests/test_pip_keine_note.py::test_naechster_call_raeumt_den_vorigen_anruf_weg -v --tb=long'
[triage] Dump-Treue-Katalog-Gate OK: crm-Policies=7, FORCE=5, anon-SELECT-GRANTs=5
[triage] Argumente: tests/test_pip_keine_note.py::test_naechster_call_raeumt_den_vorigen_anruf_weg -v --tb=long
tests/test_pip_keine_note.py::test_naechster_call_raeumt_den_vorigen_anruf_weg PASSED [100%]
======================== 1 passed, 11 warnings in 8.14s ========================
[triage] pytest gegen nerve_test bestanden — nerve_test wird per trap gedroppt
```

Und die **ganze Datei** (alle drei Tests, gegen den heutigen Server-Stand):

```
$ ssh ... 'cd /opt/nerve/app && bash scripts/triage.sh tests/test_pip_keine_note.py -v'
tests/test_pip_keine_note.py::test_postcall_trend_route_existiert_nicht_mehr PASSED [ 33%]
tests/test_pip_keine_note.py::test_calc_call_score_ist_weg PASSED        [ 66%]
tests/test_pip_keine_note.py::test_naechster_call_raeumt_den_vorigen_anruf_weg PASSED [100%]
======================== 3 passed, 11 warnings in 8.86s ========================
```

### Abnahme-Anker Task 2b — mit Ausgaben

| Anker | Sollwert | Ist | |
|---|---|---|---|
| `grep -c "json.outcome \|\| state.lastOutcome" static/pip-launcher.js` | 0 | **0** | ✓ |
| Existenz-Anker `grep -c "json.outcome" static/pip-launcher.js` | ≥ 1 | **4** | ✓ |
| `grep -c "state.lastOutcome = null" static/pip-launcher.js` | ≥ 1 | **1** | ✓ |
| Zeile steht **in** `_resetLiveState()` (`grep -n -A12 "function _resetLiveState"`) | ja | **:3019, direkt nach `pendingPostcall`/`lastCallId`** | ✓ |
| `grep -c "_resetLiveState()" static/pip-launcher.js` | ≥ 4, **nicht** gesunken | vorher **5** → jetzt **6** | ✓ |
| `nextCall()` nullt nichts selbst (`grep -n -A14 "function nextCall"`) | keine eigene Nullung | **nur `_resetLiveState();`** | ✓ |
| Farb-HART: neue Hex-Farbe / neues `style=` im Diff | 0 / 0 | **0 / 0** | ✓ |

---

## Task 1 — Belege [nachtraeglich erhoben]

### Abnahme-Anker

| Anker | Soll | Ist |
|---|---|---|
| `grep -c "function _calcScore" static/pip-launcher.js` | 0 | **0** |
| Existenz: `grep -c "function _renderQuickStats" static/pip-launcher.js` | 1 | **1** |
| `grep -c "_fetchAndRenderTrend" static/pip-launcher.js` | 0 | **0** |
| Existenz: `grep -c "function _renderSparkline" static/pip-launcher.js` | 1 | **1** |
| `grep -c "json.final_score" static/pip-launcher.js` | 0 | **0** |
| Existenz: `grep -c "pipEl('nlp-btn-details')" static/pip-launcher.js` | ≥ 1 | **5** |
| `grep -c "nlp-postcall-trend" templates/base.html` | 0 | **0** |
| Existenz: `grep -c "nlp-btn-details" templates/base.html` | 1 | **1** |
| `grep -c "Fokus-Serie" static/pip-launcher.js` (vertagt, kein Ersatz) | ≥ 1 | **1** |
| Farb-HART im Task-1-Diff (`git show b8cd77f`): neue Hex / `style=` | 0 | **0** |

### Der Token-grep aus Schritt E — und die daraus abgeleitete Entscheidung

```
$ grep -rn "session-score-high\|session-score-mid\|session-score-low" static/ templates/
static/nerve.css:212:  --session-score-high:   #00D4AA;
static/nerve.css:213:  --session-score-mid:    #6B7280;
static/nerve.css:214:  --session-score-low:    #f87171;
static/nerve.css:647:.n-session-score-high{color:var(--session-score-high);}
static/nerve.css:648:.n-session-score-mid{color:var(--session-score-mid);}
static/nerve.css:649:.n-session-score-low{color:var(--session-score-low);}
```

**Entscheidung: die drei Tokens BLEIBEN.** Die Klassen `.n-session-score-*` (`:647-649`) benutzen
sie weiterhin; sie werden ausserhalb des Anruf-Fensters verwendet. Eine Token-Loeschung haette diese
Klassen auf `var(--…)`-Nichts laufen lassen (T-METRIK1-08-04). Die grep-Ausgabe hat entschieden,
nicht die Vermutung. ⚠ Nach Plan 09 (letzte Noten-Anzeige im Wochen-Dashboard) ist dieser grep
**erneut zu ziehen** — dann kann die Antwort anders ausfallen.

### Die sechs `nlp-btn-details`-Stellen — was jede tut

```
$ grep -n "nlp-btn-details" static/pip-launcher.js
1838 · 2191 · 3506 · 3521 · 3540 · 4275
```

| Zeile | Funktion / Zustand | Was sie tut | angefasst? |
|---|---|---|---|
| `:1838` | globaler Klick-Handler | `t.closest('#nlp-btn-details')` → `showDetails()`. Das ist der **Weg zur Auswertungsseite**, kein Ein-/Ausblenden. | nein |
| `:2191` | `_showPipLive()` | blendet den Knopf beim Start des naechsten Anrufs wieder ein, falls der Leer-Zustand ihn versteckt hatte. | nein |
| `:3506` | `_showPostcallLoading()` | **deaktiviert** den Knopf (`disabled = true`), solange keine `conv_id` da ist — sonst landet der Klick auf `/logs` statt auf der Sitzung. **Bleibt bewusst so** (POLISH-23). | nein |
| `:3521` | `_showPostcallEmpty()` | blendet den Knopf **aus** — es gibt nichts zu zeigen. | nein |
| `:3540` | `_showPostcallRaw()` | blendet ein **und** enabled, sobald echter Inhalt da ist. Der Kommentar dort haelt fest, dass es keinen Noten-Bereich mehr gibt, in den `scoreText` geschrieben wuerde; der Parameter unterscheidet weiterhin Lade- von Fuell-Zustand. | Kommentar (Task 1) |
| `:4275` | `_revealScoreAndActions()` | **die eine geaenderte Stelle**: das Einblenden stand INNERHALB der Noten-Klammer und steht jetzt bedingungslos davor. | **ja** |

Damit ist T-METRIK1-08-03 (Loeschung trifft eine der fuenf anderen Stellen) belegt entkraeftet:
fuenf unveraendert, eine bewusst geaendert.

### `.pip-postcall-label` — Entscheidung aus Schritt C

```
$ grep -rn "pip-postcall-label" static/ templates/
(keine Treffer)
```

Die Klasse hat heute **keinen Nutzer mehr** — Etikett-Markup und der zugehoerige JS-Zugriff sind mit
Task 1 gefallen, die CSS-Regel ebenso. Kein Zugriff laeuft ins Leere. ⚠ [nachtraeglich erhoben]:
dieser grep beweist den **Endzustand**, nicht die Reihenfolge („erst greppen, dann entscheiden").

---

## Task 2 — Belege [nachtraeglich erhoben]

### Die vier Bau-Regel-20-greps (Entdeckung — **kein Zielwert, kein Gate**)

```
$ grep -rn "_calc_call_score(" routes/ services/ tests/ --include=*.py
(keine Treffer)

$ grep -rn "_calc_process_score(\|_apply_outcome_modifier(\|_calc_coaching_score(" routes/ services/ tests/ --include=*.py
routes/app_routes.py:845:   _proc_score, _base_bd = _calc_process_score(_conv_bd)
routes/app_routes.py:1032:  def _calc_process_score(conv):
routes/app_routes.py:1070:  def _apply_outcome_modifier(process_score, base_breakdown, outcome):
routes/app_routes.py:1093:  def _calc_coaching_score(conv, outcome):
routes/app_routes.py:1102:      process_score, base_bd = _calc_process_score(conv)
routes/app_routes.py:1103:      return _apply_outcome_modifier(process_score, base_bd, outcome)
routes/app_routes.py:2265:  process_score, base_bd = _calc_process_score(conv_for_score)   # Fallback: Stash-Miss
routes/app_routes.py:2266:  coaching_score_val, final_score_val, breakdown_data = _apply_outcome_modifier(
tests/test_correct_outcome.py: 8 Treffer (_calc_coaching_score)
tests/test_score_split.py:     9 Treffer (_calc_process_score / _apply_outcome_modifier / _calc_coaching_score)

$ grep -rn "final_score" routes/ templates/ static/ tests/
routes/app_routes.py:1040,1074,1075,1081,1082,1087,1090,1100   → ERZEUGER-Innenleben, BLEIBT
routes/app_routes.py:2266,2270,2279                            → Outcome-Korrektur (ERZEUGER), BLEIBT
templates/dashboard.html:910,913,914,916,992,995,996,998       → ⚠ die LETZTEN Anzeige-Leser → PLAN 09
static/                                                        → (keine Treffer — Anruf-Fenster ist sauber)
tests/test_correct_outcome.py:22,30,37,49,61,68                → Tests des ERZEUGERS, bleiben
tests/test_score_split.py:35,70                                → Tests des ERZEUGERS, bleiben

$ grep -rn "postcall/trend" routes/ templates/ static/ tests/
tests/test_pip_keine_note.py:75  (Docstring des Loesch-Belegs)
tests/test_pip_keine_note.py:92  (der GET, der 404 erwartet)
routes/__pycache__/app_routes.cpython-311.pyc  ← Binaertreffer, siehe Anker-Fehlmessung unten
```

**Die zweite Formel ist ausdruecklich geprueft, nicht uebersehen:** die im PiP frueher sichtbare
Zahl war `json.final_score` aus `_calc_process_score` + `_apply_outcome_modifier` — **nicht** aus
`_calc_call_score`. Ein Anker auf `_calc_call_score` allein haette sie nie gefunden. Sie ist im
Anruf-Fenster jetzt weg (`grep -c "json.final_score" static/pip-launcher.js` → 0), im
**Wochen-Dashboard** aber noch da (`templates/dashboard.html`, 8 Treffer) — **Termin: Plan 09**. Der
**ERZEUGER** in `routes/app_routes.py:2265-2279` bleibt bewusst stehen.

### Abnahme-Anker Task 2

| Anker | Soll | Ist |
|---|---|---|
| `grep -c "def _calc_call_score" routes/app_routes.py` | 0 | **0** |
| Existenz: `grep -c "def _calc_process_score" routes/app_routes.py` | 1 | **1** |
| `grep -c "def api_postcall_trend" routes/app_routes.py` | 0 | **0** |
| Existenz: `grep -c "def api_keepalive" routes/app_routes.py` | 1 | **1** |
| `grep -rn "_calc_call_score" routes/ services/ --include=*.py \| wc -l` | 0 | **0** |
| `grep -rn "postcall/trend" routes/ templates/ static/ \| wc -l` | 0 | **1** → siehe Anker-Fehlmessung |
| `grep -c "_calc_call_score" tests/test_pip_keine_note.py` | ≥ 1 | **3** |
| `grep -c "postcall/trend" tests/test_pip_keine_note.py` | ≥ 1 | **2** |
| `grep -rln "_calc_call_score\|postcall/trend" tests/ --include=*.py` | genau 1 Datei | **genau `tests/test_pip_keine_note.py`** — kein stale Bestandstest |
| `grep -c "hasattr(" tests/test_pip_keine_note.py` | 2 | **2** |
| `grep -c "^def test_" tests/test_pip_keine_note.py` | 2 | **3** → siehe Anker-Konflikt |

---

## Anker-Fehlmessungen und -Konflikte (Code wurde NICHT gebogen)

**① `grep -rn "postcall/trend" routes/ templates/ static/ | wc -l` liefert 1 statt 0 — Fehlmessung.**
Der einzige Treffer ist **kein Quelltext**, sondern eine veraltete Bytecode-Datei:
`routes/__pycache__/app_routes.cpython-311.pyc` (grep meldet sie als „Binary file … matches"). Der
Anker durchsucht ein Verzeichnis, in dem auch Kompilate liegen. **Belastbarer Ersatz-Anker**, auf
Quelldateien begrenzt:

```
$ grep -rn "postcall/trend" routes/ templates/ static/ --include=*.py --include=*.html --include=*.js | wc -l
0
```

Der Produktivpfad ist also restlos leer. **Nichts am Code geaendert** — der Anker war zu grob.
(Dieselbe Datei taucht aus demselben Grund im `final_score`-grep auf.)

**② `grep -c "^def test_" tests/test_pip_keine_note.py` liefert 3 statt der in Task 2 geforderten 2
— Anker-Konflikt zwischen Task 2 und Task 2b, nicht Fehlverhalten.** Task 2b (spaeter datiert,
Pflicht-Nachtrag) schreibt ausdruecklich vor, den Verhaltens-Test **in dieselbe Datei** zu
ergaenzen („derselbe Bildschirm; kein neuer Testdatei-Wildwuchs"). Der zaehlende Task-2-Anker kann
danach nicht mehr 2 liefern. **Belastbarer Ersatz-Anker** (namentlich statt zaehlend):

```
$ grep -c "^def test_postcall_trend_route_existiert_nicht_mehr\|^def test_calc_call_score_ist_weg" tests/test_pip_keine_note.py
2
```

Beide Task-2-Tests existieren unveraendert und laufen gruen (siehe Datei-Lauf oben). **Es wurde kein
Test geloescht, um eine Zahl zu treffen.**

**③ `grep -c "_resetLiveState()"` = 6 statt der im Plan angenommenen „Definition + drei Aufrufer".**
Kein Fehler: der Anker zaehlt **Zeilen**, und der Name steht zusaetzlich in einem Kommentar
(`:2161`) und einer Log-Zeile (`:3060`). Wichtig ist die Richtung — vorher 5, jetzt 6: **kein
bestehender Aufrufer ist verlorengegangen.**

---

## Deviations from Plan

**[Rule 3 — blockierend] Der geforderte Browser-Verhaltens-Test ist im Test-Tor nicht herstellbar.**
Task 2b verlangt einen Verhaltens-Test („zwei Anrufe nacheinander…"). Der Fehler sitzt in reinem
Browser-JS in einer IIFE, die nach aussen nur `{ open, close, isActive }` gibt; ein echter
Funktions-Aufruf braucht eine JS-Laufzeit. **Auf dem Ausroll-Server gibt es keine:**

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'which node; node --version'
bash: line 1: node: command not found
```

Lokal auszufuehren ist HART verboten („Kein Local-Dev"). Eine neue Abhaengigkeit (Node, jsdom, ein
JS-Interpreter fuer Python) waere eine Architektur-Aenderung mitten in einem Reparatur-Task.

**Gewaehlter Weg (bewusst benannt, nicht verschwiegen):** der Test prueft den
**Kontrollfluss-Vertrag** an den per Klammer-Zaehlung ausgeschnittenen Funktionsrumpfen — ruft der
Weg „Nächster Call" den EINEN Aufraeumer, raeumt dieser den gemerkten Ausgang mit weg, und hat der
gemerkte Ausgang danach ueberhaupt noch einen **Leser**. Das ist mehr als eine Zeichenketten-Suche
(ein Zeilen-grep kann nicht sagen, **in welcher Funktion** eine Zeile steht — und genau das war hier
die Frage), aber es ist **kein** Laufzeit-Verhalten. Praezedenz im Repo:
`tests/test_meeting_form_dsgvo.py` prueft `static/pip-launcher.js` aus demselben Grund und mit
derselben ausdruecklichen Begruendung; die Test-Qualitaets-Regel laesst diesen Grenzfall mit
Begruendung im Test zu — die Begruendung steht im Kopf des Tests, samt der `node`-Ausgabe.

**Was das fuer die Abnahme heisst:** Der Test faengt jede kuenftige Regression der drei Vertraege.
Er beweist **nicht**, dass der Bildschirm sich richtig verhaelt. **Diesen Beweis liefert
ausschliesslich Schritt 3b von Task 3 (Andre, zwei echte Anrufe).**

**[Anzeige-Verhalten, plan-konform, aber Nebenwirkung ausdruecklich]** Durch C wird bei fehlendem
Ausgang **gar keine** Ausgangs-Zeile mehr erzeugt (statt einer Zeile mit `-`). Das ist genau die
Andre-Entscheidung („lieber gar nichts"), aendert aber die Hoehe des Nach-Anruf-Blocks in diesem
Fall. Kein Layout-Bruch erwartet (der Container schrumpft), aber es ist in Schritt 3b mitzusehen.

**Transport per `scp` statt `deploy.sh`:** `static/pip-launcher.js` und
`tests/test_pip_keine_note.py` wurden auf den Server kopiert, weil das ROT-Lauf-Rezept es
**verlangt** (Bedingung 1: „Der Code muss auf dem Server liegen", sonst ist der Lauf blind statt
rot). Beide Dateien sind committet und gepusht; `deploy.sh` schreibt denselben Inhalt erneut. Kein
Dienst-Neustart, keine Migration, kein Deploy-Meta-Write.

---

## Funde — gemeldet, NICHT mitgefixt

**① `_calc_coaching_score` hat keinen Produktiv-Leser.** (`routes/app_routes.py:1093-1103`) Ein
duenner Wrapper um `_calc_process_score` + `_apply_outcome_modifier`; seine einzigen Aufrufer stehen
in `tests/test_correct_outcome.py` (8) und `tests/test_score_split.py` (1). Nach Bau-Regel 20 ein
Streich- oder Register-Kandidat. **Hier bewusst unangetastet** (Reparatur-Modus). **Vorschlag: in
der ERZEUGER-Folgephase mit erledigen** — dort fallen ohnehin `_calc_process_score` und
`_apply_outcome_modifier`, und die zugehoerigen Tests werden dann im selben Zug retargetiert.

**② Redeanteil-Kachel im Anruf-Fenster — terminiert, nicht hier.** `_renderQuickStats` zeigt
weiterhin „Redeanteil (Vertriebler/Kunde)" (`static/pip-launcher.js:3444,3474`), obwohl der Wert im
Kaltakquise-Modus baubedingt konstant ist — waehrend die **Auswertungsseite** dieselbe Zeile in
Plan 07 gestrichen hat (D-16). Das Anruf-Fenster darf zaehlen, es benotet ja nicht mehr; trotzdem
ist die Kachel dort ohne Aussage. **Termin: Roadmap-Punkt 4.0.2 (Ersatz-Rechnung Redeanteil)** —
ausdruecklich **nicht** in diesem Plan.

**③ NEU (dieser Lauf): der gemerkte Ausgang hat nach C keinen Leser mehr.**
`state.lastOutcome` wird jetzt nur noch **geschrieben** (`:4480` beim Bestaetigen, `:3019` beim
Aufraeumen) und **nirgends gelesen**. Nach Bau-Regel 20 ist er damit ein Streich-Kandidat: das Feld
koennte komplett entfallen. **Hier bewusst NICHT geloescht** — der Plan schreibt genau diese zwei
Schreib-Stellen vor, und eine Loeschung waere eine Aenderung ausserhalb des Fehler-Pfads
(Reparatur-Modus). ⚠ Der Test haelt den Zustand fest: sobald jemand wieder einen **Leser** einbaut,
wird er rot. **Vorschlag: mit der ERZEUGER-Folgephase oder Plan 09 ersatzlos streichen.**

**④ Der laufende Prozess auf Production ist aelter als die Dateien.** Siehe oben — kein Fehler
dieses Plans, aber der Grund, warum Task 3 nicht „nur Formsache" ist.

---

## Pruning-Notiz (was ersatzlos verschwunden ist)

| Weg | Was faellt | Ersatz |
|---|---|---|
| `static/pip-launcher.js` | `_calcScore` (Client-Kopie der Vierer-Formel) | keiner — die Formel ist tot |
| `static/pip-launcher.js` | `_fetchAndRenderTrend` + Aufruf | keiner, **vertagt** bis zur Fokus-Serie |
| `static/pip-launcher.js` | Noten-Anzeige aus `_revealScoreAndActions` | keiner — zaehlen ja, benoten nein |
| `static/pip-launcher.js` | Rueckfall auf den Ausgang des vorigen Anrufs | keiner — lieber gar kein Ausgang |
| `templates/base.html` | Noten-Zahl, Etikett, Trend-Streifen + 6 CSS-Regeln | keiner |
| `routes/app_routes.py` | `api_postcall_trend` (Route + Decorator) | keiner |
| `routes/app_routes.py` | `_calc_call_score` (Vierer-Formel) | keiner |

**Netto:** Task 1 −95/+48 Zeilen, Task 2 −45/+18 Zeilen. Task 2b ist als einziger Schritt additiv
(+25/−7) — er **repariert**, er entfernt keine Faehigkeit.
Nicht geprunt und bewusst so: `_calc_process_score`, `_OUTCOME_MODIFIERS`, `_apply_outcome_modifier`
(**ERZEUGER** von `calls.coaching_score`/`score_breakdown`), `_renderQuickStats`, `_renderSparkline`,
die drei `--session-score-*`-Tokens.

**Bekannter Zwischenzustand:** Das Wochen-Dashboard zeigt zu diesem Zeitpunkt noch eine „Ø
Score"-Kachel und eine Note in der Sitzungs-Liste — **sie fallen in Plan 09.**

---

## Persistenz-Schicht (Bau-Regel 21)

**Keine Tabelle angefasst.** Dieser Plan entfernt Anzeige-Code, eine Route und zwei Formeln; Task 2b
nullt zusaetzlich einen **Browser-seitigen** Zustand. Es wird kein Datum gelesen, geschrieben,
geloescht oder migriert. `calls.coaching_score` / `score_breakdown` werden unveraendert weiter
geschrieben.

---

## Self-Check

**Belegt (mit Ausgabe in diesem Dokument):**
- ✅ ROT vor GRUEN fuer Task 2b, verbatim, ueber `scripts/triage.sh` auf dem Server, mit erreichtem
  Pruefkatalog (kein Werkzeug-Ausfall: `Dump-Treue-Katalog-Gate OK`, Test gesammelt und ausgefuehrt).
- ✅ GRUEN fuer den neuen Test und fuer alle drei Tests der Datei auf dem Server.
- ✅ Alle Abnahme-Anker aus Task 2b (7/7).
- ✅ Alle Abnahme-Anker aus Task 1 (10/10) und Task 2 (11/11, davon 2 mit dokumentiertem
  Ersatz-Anker) — **[nachtraeglich erhoben]**.
- ✅ Die drei Pflicht-greps aus Task 2b **vor** der Aenderung, inkl. der ausdruecklichen Antwort auf
  „dritter Aufrufer?" (nein).
- ✅ Farb-HART-Regel in allen drei Diffs: keine neue Hex-Farbe, kein neues `style=`.
- ✅ Dateien auf Production == lokaler HEAD (md5-Vergleich, oben).

**AUSDRUECKLICH NICHT bewiesen:**
- ❌ **Kein Deploy.** `bash deploy.sh production` ist fuer Agenten gesperrt und wurde nicht
  ausgefuehrt. **Es gibt keine Tor-Ausgabe.** Der Dienst laeuft unveraendert seit 17.08. 05:20 UTC.
- ❌ **Die Latenz-Zusage aus D-13 / Punkt 25 ist nicht abgenommen** — kein Wartekreisel, keine
  zusaetzliche Wartezeit, kein zusaetzlicher Netzwerk-Aufruf beim Auflegen: das kann nur Andre am
  echten Auflegen sehen (Schritt 2, Punkt 1-3).
- ❌ **Schritt 3b (der Kern des Pflicht-Nachtrags) ist nicht abgenommen.** Der neue Test beweist den
  Kontrollfluss, **nicht** den Bildschirm. Ohne zwei echte Anrufe gilt der Nachtrag als **nicht**
  abgenommen.
- ❌ **Schritt 3** (Knopf in bestaetigtem UND unbestaetigtem Zustand) und **Schritt 4**
  (`inspect.sh routes`) stehen aus.
- ❌ **Kein Browser-Verhalten irgendeiner Art** wurde automatisiert geprueft — es gibt im Test-Tor
  keine JS-Laufzeit (`node: command not found`).

**Datei-Existenz-Pruefung:**
```
static/pip-launcher.js                                            FOUND
templates/base.html                                               FOUND
routes/app_routes.py                                              FOUND
tests/test_pip_keine_note.py                                      FOUND
.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-08-SUMMARY.md  FOUND
```

**Commit-Pruefung:** `b8cd77f`, `2db86be`, `dff1d66`, `0ac846c`, `90876bd` — alle in `git log`, alle
nach `origin/main` gepusht.

## Self-Check: PASSED (mit ausdruecklich offener Abnahme — Task 3)
