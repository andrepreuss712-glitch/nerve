---
phase: 08.23.2.METRIK-1
plan: 09
type: execute
wave: 8
depends_on: ["08.23.2.METRIK-1-07", "08.23.2.METRIK-1-08"]
files_modified:
  - routes/performance.py
  - routes/dashboard.py
  - services/coaching_service.py
  - templates/dashboard.html
  - tests/test_wochen_dashboard_keine_note.py
autonomous: false
requirements: [SPEC-R9]
user_setup: []

must_haves:
  truths:
    - "Das Wochen-Dashboard zeigt keine 'Ø Score'-Kachel mehr."
    - "Der Coach-Report zeigt keine zweite Ø-Score-Zahl mehr — weder als Kachel noch im Fallback-Berichtstext."
    - "Der Auftrag an das Modell (report_prompt) enthaelt die Kunden-Kauflaune nicht mehr als Zutat — der erzeugte Berichtstext kann sie deshalb nicht mehr nennen."
    - "Die Begruessung sagt nie mehr 'Dein Score ist diese Woche um N% gestiegen'."
    - "Die Sitzungs-Liste zeigt fuer LIVE-Anrufe keine Note mehr — fuer Trainings-Sitzungen schon."
    - "Keine Ersatz-Zahl tritt an die Stelle der entfernten Werte."
    - "Die uebrigen Kacheln (Sessions, Deals, Umsatz, Streak) sind unveraendert."
    - "Das KB-Trend-Diagramm ist vom Wochen-Dashboard verschwunden — Karte und Chart.js-Block."
    - "Die Wochenmittel-Serie kaufbereitschaft enthaelt nur LIVE-Sitzungen — festgenagelt durch einen Verhaltens-Test, nicht nur durch eine Codezeile."
    - "Die Kaufbereitschafts-Mittelung in routes/dashboard.py mischt keine Trainings-Noten mehr in eine Live-Kennzahl."
    - "Der KI-Auftrag _generate_weekly_summary bekommt den kb_end-Trend nicht mehr als Zutat."
  artifacts:
    - path: "routes/performance.py"
      provides: "kein avg_score, kein score_trend_pct, Sitzungs-Score nur fuer Training"
    - path: "services/coaching_service.py"
      provides: "Fallback-Berichtstext UND report_prompt ohne Ø-Score-Zahl (Zutat entfernt, nicht Text gefiltert). Die DB-Spalte avg_readiness_score bleibt befuellt (ERZEUGER, Folgephase)"
    - path: "templates/dashboard.html"
      provides: "KPI-Reihe ohne Ø-Score-Kachel, Coach-Report ohne Ø-Score-Kachel, Sitzungs-Liste ohne Live-Note, kein final_score-Setter"
    - path: "routes/dashboard.py"
      provides: "typ-Filter fuer die kb_end-Mittelung (Cross-AI-Fund 14.08.) + zwoelfte Lesestelle: der Score-Trend faellt aus dem KI-Auftrag _generate_weekly_summary"
      contains: "_avg_kb_live"
    - path: "tests/test_wochen_dashboard_keine_note.py"
      provides: "Verhaltens-Tests auf die JSON-Antworten beider Routen, auf die live-only-Serie und auf die kb_end-Mittelung"
  key_links:
    - from: "routes/performance.py"
      to: "templates/dashboard.html::renderKpis"
      via: "kpis.avg_score und kpi_trends.avg_score_pct entfallen auf beiden Seiten"
      pattern: "avg_score"
    - from: "routes/performance.py (recent_sessions)"
      to: "templates/dashboard.html::.db2-session-score"
      via: "score nur noch fuer stype == 'training'"
      pattern: "'score'"
---

<objective>
Das Wochen-Dashboard hoert auf, die **Kauflaune der Kunden** als **Leistung des Verkaeufers**
auszuweisen.

**Purpose — der Fund unter dem Fund:** Das Wochen-Dashboard mittelt `kb_end` ueber **Live**-Anrufe
und nennt das Ergebnis **„Ø Score"**. In Live-Anrufen steht dort die **KAUFBEREITSCHAFT DES
KUNDEN**. **Auf dem meistbesuchten Bildschirm steht also die durchschnittliche Kauflaune der
Kunden, beschriftet als Leistung des Verkaeufers** — genau die Krankheit, wegen der diese Phase
existiert, nur nicht dort, wo wir sie gesucht haben.
⛔ **KORRIGIERT 14.08. (Cross-AI-Fund):** Hier stand *„Geprueft und NICHT gefunden: eine
Vermischung von Trainings- und Live-Werten."* **Das galt gemessen nur fuer
`routes/performance.py:56-62`** (`logs_90` filtert dort sauber auf `typ == 'live'`) — und wurde
faelschlich auf die **Klasse** ausgeweitet. **In `routes/dashboard.py` gilt es nicht:** die
`logs`-Abfrage der `/dashboard`-Route filtert **nicht** auf `typ` und mittelt
Live-Kaufbereitschaft mit Trainings-Noten in **einer** Zahl. **➡️ Das wird in dieser Phase
mitkorrigiert — Task 3 dieses Plans.**
**★ Die Lehre, die daraus ins Vault geht:** *Ein Freispruch gilt fuer die Datei, in der er
gemessen wurde — nicht fuer die Klasse.*

**Output:** Wochen-Dashboard ohne Note, Sitzungs-Liste ohne Live-Note, **KB-Trend-Diagramm
gefallen**, **`typ`-Filter in `routes/dashboard.py` korrigiert**, keine Ersatz-Zahl.

## ⛔ D-14 — keine Ersatz-Zahl, und zwar ausdruecklich

An die Stelle der entfernten Werte tritt **nichts**. Eine neutrale Ersatz-Groesse (Anrufe,
Woerter) ist **ausdruecklich verworfen** — sie misst Betriebsamkeit, nicht Fortschritt.
**„Lieber eine ehrliche Leerstelle als eine huebsche Zahl ohne Bedeutung."**
**Nicht gestrichen, vertagt:** die Stelle wird wieder besetzt, sobald die **Fokus-Serie**
existiert („dreimal in Folge angewendet").

## ⚠ Zwei Lesestellen, die in D-12 nicht standen — beim Planen gefunden

D-12 nennt drei `kb_end`-Bildschirme. Beim Kartieren dieses Plans kamen **zwei weitere** dazu,
beide auf dem Wochen-Dashboard:

1. **Die Sitzungs-Liste** (`routes/performance.py`, `recent_sessions` → `'score': l.kb_end or 0`
   → `templates/dashboard.html` `.db2-session-score`). Sie zeigt je Zeile eine farbcodierte Zahl
   — bei **Live**-Zeilen wieder die Kaufbereitschaft, gruen ab 70.
2. **Zwei JavaScript-Bloecke**, die dieselbe Zahl nach einer Outcome-Korrektur mit
   `json.final_score` **ueberschreiben** (`templates/dashboard.html`, im Inline-Korrektur- und
   im Stift-Bearbeiten-Handler).

⚠ **Ohne diese beiden waere das Abnahme-Kriterium „kein LIVE-Anruf zeigt eine Gesamtnote — auf
keinem Bildschirm" NICHT erfuellt**, und **kein** Anker haette angeschlagen: sie zeigen weder auf
`_calc_call_score` noch auf die drei in D-12 genannten Stellen.

## ⚠ Eine VIERTE Stelle — und der Anker, der sie per Bauart nicht gefunden haette

Auf **demselben Bildschirm**, ein Stueck weiter unten, steht im **Coach-Report** eine **zweite**
Ø-Score-Zahl (`templates/dashboard.html:346`):

```html
<div><span style="color:#00D4AA;...">{{ weekly_report.avg_readiness_score }}%</span><br>
<span ...>&Oslash; Score</span></div>
```

Quelle: `services/coaching_service.py:388` / `:401` — `avg_readiness_score=round(avg_kb, 1)`,
ein **Mittelwert ueber `kb_end`**. Also **dieselbe** Zahl mit **derselben** Beschriftung wie die
KPI-Kachel, nur aus einem zweiten Rechenweg. Dazu der **Fallback-Berichtstext**
(`services/coaching_service.py:378`), der dieselbe Zahl in einen Satz giesst:

```python
full_report = f"Diese Woche: {calls_count} Calls, Ø Score {avg_kb:.0f}%."
```

⚠ **Die Anker-Falle, die dieser Plan bis zur Pruefung hatte:** Der Abnahme-Anker lautete
`grep -c "Ø Score" templates/dashboard.html` → **0**. Die KPI-Kachel (`:122`) traegt das
**literale** `Ø Score`, der Coach-Report dagegen die **HTML-Entitaet** `&Oslash; Score`. Nach dem
Umbau haette der Anker also **0 geliefert — waehrend die zweite Zahl weiter auf dem Bildschirm
steht.** Falsch-gruen aus reiner Schreibweisen-Differenz.

**Konsequenz fuer diesen Plan:** Der Anker steht nicht mehr auf einer einzelnen Schreibweise,
sondern auf `grep -n "Score" templates/dashboard.html` **mit vollstaendiger Fundstellen-Liste im
SUMMARY**; die Entitaeten `&Oslash;` und `&#216;` werden **ausdruecklich mitgeprueft**.
Und der Coach-Report ist als **vierte** Stelle in Task 1 (Berichtstext) und Task 2 (Kachel)
mitversorgt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-SPEC.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-CONTEXT.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-07-SUMMARY.md
@CLAUDE.md
@CLAUDE-BAUREGELN-13-14-20.md

<interfaces>
**Stelle 1 — Ø-Score-Kachel**, verbatim `routes/performance.py:100-102`:
```python
        # ── Ø Score (aus kb_end der Live-Sessions) ────────────────────────
        scores = [l.kb_end for l in logs_90 if getattr(l, 'kb_end', None) is not None]
        avg_score = round(sum(scores) / len(scores), 1) if scores else None
```
⚠ **Zwei Routen, zwei Rechnungen — KEIN gemeinsamer Ausgang.** Hier stand bis zur
Plan-Pruefung, diese eine Rechnung habe „zwei Ausgaenge, `:158` und `:469`". Das ist **falsch
und unbelegt** (Punkt 22): `:102` liegt in `api_performance` (`def :47`) und geht **nur** nach
`:158`. Die Dashboard-v2-Route `api_dashboard` (`def :223`) rechnet ihren eigenen Wert bei
`:261` aus `logs_this_week` und gibt **den** bei `:469`/`:476` aus. Die Netto-Edits bleiben
unveraendert — nur die Datenfluss-Behauptung war es nicht wert, geglaubt zu werden.

**Stelle 1a — `api_performance`** (`def :47`): Rechnung `:100-102` → Ausgang `:158`
`'avg_score': avg_score`.

**Stelle 1b — `api_dashboard`** (`def :223`): **eigene** Rechnung `:261` (aus `logs_this_week`,
siehe Stelle 2) → Ausgang `:469` `'avg_score': avg_score` innerhalb `'kpis'` und `:476`
`'avg_score_pct': score_trend_pct` innerhalb `'kpi_trends'`.

**Stelle 2 — Begruessung**, verbatim `routes/performance.py:258-268`:
```python
        # Score-Trend diese Woche vs. letzte Woche
        scores_this = [l.kb_end for l in logs_this_week if l.kb_end]
        scores_last = [l.kb_end for l in logs_last_week if l.kb_end]
        avg_score = round(sum(scores_this) / len(scores_this), 1) if scores_this else None
        avg_score_last = round(sum(scores_last) / len(scores_last), 1) if scores_last else None
        score_trend_pct = None
        if avg_score and avg_score_last and avg_score_last > 0:
            score_trend_pct = round((avg_score - avg_score_last) / avg_score_last * 100)

        if score_trend_pct and score_trend_pct > 0:
            greeting_subtitle = f'Dein Score ist diese Woche um {score_trend_pct}% gestiegen 📈'
        elif streak >= 7:
            ...
```
★ **Das Ersatz-Muster steht direkt darunter**: die Kaskade `streak >= 7` / `streak >= 3` /
„Zeit für eine neue Session" traegt sich selbst. **Der `if`-Zweig faellt einfach weg — kein
Neubau, keine Ersatz-Zahl.**

**Stelle 3 — Sitzungs-Liste**, verbatim `routes/performance.py` (im `recent_sessions`-Aufbau):
```python
            recent_sessions.append({
                'id':           l.id,
                'date_str':     date_str,
                'session_type': stype,
                'score':        l.kb_end or 0,
```
`stype` ist wenige Zeilen darueber bereits bestimmt (`'training'` / `'cold_call'` / `'meeting'`).

**Anzeige-Seite**, verbatim `templates/dashboard.html`:
```html
  <!-- ── Zeile 2: KPI-Reihe (5 Kacheln) ───────────────────────────────────── -->
  <div class="db2-kpi-row">
    <div class="db2-kpi">
      <div class="db2-kpi-value" id="kpi-score">—</div>
      <div class="db2-kpi-label">Ø Score</div>
      <div class="db2-kpi-trend" id="kpi-score-trend"></div>
    </div>
```
```javascript
  setText('kpi-score',   k.avg_score != null ? fNum(k.avg_score) : '—');
  ...
  setHtml('kpi-score-trend',    fTrend(t.avg_score_pct, '%'));
```
```javascript
    var scoreColor = s.score >= 70
      ? 'var(--btn-primary-bg-from, #00D4AA)'
      : (s.score >= 40 ? 'var(--page-text-secondary, #6B7280)' : 'var(--btn-danger-text, #f87171)');
...
    html +=   '<span class="db2-session-score" style="color:' + scoreColor + '">' + (s.score || '—') + '</span>';
```
Und **zweimal** (Inline-Korrektur und Stift-Bearbeiten):
```javascript
          if (json.final_score !== undefined) {
            var scoreSpan = row.querySelector('.db2-session-score');
            if (scoreSpan) {
              scoreSpan.textContent = String(json.final_score);
              scoreSpan.style.color = json.final_score >= 70 ? ... ;
            }
          }
```

**Stelle 4 — Coach-Report-Kachel**, verbatim `templates/dashboard.html:345-348` (die
Ø-Score-Zahl ist die **zweite** von vier Zellen in derselben Flex-Reihe):
```html
        <div><span style="color:#00D4AA;font-size:20px;font-weight:700">{{ weekly_report.calls_count }}</span><br><span style="font-size:12px;color:var(--page-text-secondary)">Calls</span></div>
        <div><span style="color:#00D4AA;font-size:20px;font-weight:700">{{ weekly_report.avg_readiness_score }}%</span><br><span style="font-size:12px;color:var(--page-text-secondary)">&Oslash; Score</span></div>
        <div><span style="font-size:14px">{{ weekly_report.strongest_phase or '&mdash;' }}</span><br>...
```
⚠ Beachte `&Oslash;` — **HTML-Entitaet**, nicht das literale `Ø` der KPI-Kachel. Genau daran
waere ein Schreibweisen-Anker vorbeigelaufen.

**Stelle 4b — Fallback-Berichtstext**, verbatim `services/coaching_service.py:376-379`:
```python
        except Exception as e:
            print(f"[Coach] Weekly report Sonnet failed: {e}")
            full_report = f"Diese Woche: {calls_count} Calls, Ø Score {avg_kb:.0f}%."
            suggested = None
```
`avg_kb` ist der Mittelwert ueber `kb_end` — bei Live-Anrufen die **Kaufbereitschaft des Kunden**.

**Analog fuer das, was BLEIBT:** die Nachbar-Kachel `kpi-sessions` ist eine reine **Zaehl**-Kachel.
„Zaehlen ist erlaubt, benoten nicht." Ebenso bleibt `weekly_report.calls_count` — eine Zaehlung.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: routes/performance.py — Ø-Score, Score-Trend und der Live-Sitzungs-Score fallen</name>

  <files>
routes/performance.py
services/coaching_service.py
tests/test_wochen_dashboard_keine_note.py (NEU)
  </files>

  <read_first>
- `routes/performance.py` — **`:50-170`** (die erste Route inkl. `logs_90`-Filter `:56-63` und
  des JSON-Ausgangs `:143-161`), **`:240-300`** (Begruessung + KPIs) und **`:360-410`**
  (`recent_sessions`) sowie **`:460-500`** (der Dashboard-v2-JSON-Ausgang).
- **Pflicht-grep VOR jeder Loeschung (Bau-Regel 20), Ausgaben ins SUMMARY.**
  ⚠⚠ **NUR ZUM NACHSEHEN, KEIN ZIELWERT.** Diese vier Befehle sind **Entdeckungs**-greps: ihre
  Ausgabe geht ins SUMMARY, sie haben **keinen Sollwert** und sind **kein Abnahme-Gate**. Die
  echten Abnahme-Anker dieses Plans stehen ausschliesslich in den
  `<acceptance_criteria>`-Bloecken und sind dort jeweils auf **eine einzelne Produktivdatei**
  begrenzt.
  ⛔ **Warum das hier ausdruecklich steht:** Im Cross-AI-Durchgang vom 14.08. hat ein
  Gegenleser genau diesen Block fuer einen Anker mit Sollwert 0 gehalten und daraus einen
  „unerfuellbaren Anker" gemeldet (Befund B-1, als **FALSCH-POSITIV** widerlegt). **Wenn ein
  Gegenleser einen Such-Befehl fuer ein Abnahme-Gate haelt, kann ein Ausfuehrer das auch** — und
  ein Ausfuehrer, der einen Entdeckungs-grep auf 0 druecken will, loescht Tests.
  ```bash
  grep -rn "avg_score" routes/ templates/ static/ tests/
  grep -rn "score_trend_pct\|avg_score_pct" routes/ templates/ static/ tests/
  grep -rn "db2-session-score\|\.score" templates/dashboard.html
  grep -rn "avg_readiness_score\|weekly_report" routes/ templates/ services/ static/
  ```
  ⚠ Der Kartierer hat fuer den JSON-Schluessel in der **ersten** Route (`:158`) **keinen** Leser
  gefunden — nur `templates/dashboard.html` liest `kpis.avg_score` aus der **zweiten** Route.
  **Das ist eine Zaehlung, kein Beweis.** Der Executor zieht den grep erneut. Bestaetigt er
  „kein Leser", faellt der Schluessel als **toter JSON-Schluessel** mit weg — und **genau das
  gehoert als Befund ins SUMMARY**, nicht als Nebenbemerkung.
- `services/coaching_service.py` — **`:260-275`** (wo `avg_kb` entsteht), **`:330-360`**
  (`report_prompt` — der **Auftrag an das Modell**, aus dem `report_text` entsteht) und
  **`:340-405`** (`get_or_generate_weekly_report`, der Sonnet-Pfad, der **Fallback-Text**
  `:376-379` und der `CoachingReport`-Schreibblock `:381-395` samt Rueckgabe-Dict `:397-405`).
  ⛔ **`:336` ist die ELFTE Lesestelle und der Grund, warum die Abnahme sonst am Wuerfel des
  Modells haengt.** Die Prompt-Zeile fuettert dem Modell `avg_kb` — denselben Wert wie die
  geloeschte Kachel — und das Modell schreibt daraus Fliesstext, der an
  `templates/dashboard.html:350` (`white-space:pre-line`) **auf demselben Bildschirm direkt
  unter der entfernten Zahl** erscheint. **Kein Anker kann das je fangen**: es ist
  nicht-deterministische Modell-Prosa, die weder `Ø Score` noch `&Oslash;` noch
  `avg_readiness_score` enthaelt (z. B. *„Dein Kaufbereitschafts-Schnitt lag diese Woche bei
  68 %."*). Deshalb faellt die **Zutat**, nicht der Text — siehe Abschnitt F.
  ⚠ **Vor dem Anfassen klaeren:** `avg_kb` wird **auch** in die DB-Spalte
  `coaching_reports.avg_readiness_score` geschrieben (`:388`) und im Rueckgabe-Dict geliefert
  (`:401`). **Beides bleibt** — METRIK-1 entfernt die **Verbraucher**, nicht den Erzeuger
  (dieselbe Linie wie bei `kb_end`). Nur der **Text**, der die Zahl dem Nutzer als Note
  vorlegt, faellt.
  </read_first>

  <action>
**A) Ø-Score-Kachel — beide Routen (`:100-102` → `:158` in `api_performance`; `:261` → `:469`/`:476` in `api_dashboard`).**

Die Berechnung `scores` / `avg_score` **loeschen** und an ihre Stelle:
```python
        # ── METRIK-1 D-12/D-14: "Ø Score" ist ERSATZLOS entfallen. ────────────────────────────
        # Der Wert war der Mittelwert von kb_end ueber die LIVE-Sessions — also die
        # KAUFBEREITSCHAFT DES KUNDEN, beschriftet als Leistung des Verkaeufers. Auf dem
        # meistbesuchten Bildschirm. (In DIESER Datei filtert logs_90 sauber auf typ == 'live',
        # hier wurden also keine Trainings-Noten mit hineingemittelt. Das gilt ausdruecklich
        # NUR fuer diese Datei - in routes/dashboard.py fehlte der Filter und wird in
        # derselben Phase nachgezogen.)
        # ⛔ Eine neutrale Ersatz-Zahl (Anrufe/Woerter) ist ausdruecklich VERWORFEN: sie misst
        # Betriebsamkeit, nicht Fortschritt. Lieber eine ehrliche Leerstelle als eine huebsche
        # Zahl ohne Bedeutung. NICHT gestrichen, VERTAGT — die Stelle wird wieder besetzt,
        # sobald die Fokus-Serie existiert ("dreimal in Folge angewendet").
```
Die JSON-Schluessel `'avg_score'` an **beiden** Stellen entfernen (`:158` in `api_performance`
und `:469` in `api_dashboard`), ebenso `'avg_score_pct'` in `'kpi_trends'` (**`:476`**, nicht
`:477` — die frueher hier stehende Zeilenangabe war um eins daneben).
⚠ **`closing_rate`, `einwand_erfolgsquote`, `deals_*`, `sessions_this_week`, `streak_days`,
`revenue_month` bleiben unangetastet** — das sind Zaehlungen und Quoten, keine Benotung des
Verkaeufers.

**B) Begruessung (`:258-268`).** Die sechs Zeilen der Score-Trend-Rechnung **und** den ersten
`if`-Zweig loeschen. Die Kaskade beginnt danach mit `if streak >= 7:`. Kommentar:
```python
        # METRIK-1 D-12/D-14: Der prozentuale Score-Trend-Satz in der Begruessung ist ERSATZLOS
        # entfallen — er verglich zwei Wochenmittel von kb_end, also die Kauflaune der Kunden,
        # und legte die Veraenderung dem Verkaeufer als Fortschritt vor. Die Streak-Kaskade
        # darunter traegt sich selbst; es wird KEIN Ersatz-Satz gebaut. Vertagt bis zur
        # Fokus-Serie.
        # ⛔ Den entfernten Satz NICHT zitieren: der Loesch-Anker dieser Phase verlangt seinen
        #    Wortlaut 0-mal in dieser Datei. Beschreiben statt zitieren.
```
⚠ **Die Variable `avg_score` in diesem Block wird auch von `score_trend_pct` gebraucht — beide
fallen zusammen.** Der Executor prueft mit `grep -n "avg_score\|avg_score_last\|score_trend_pct"
routes/performance.py`, dass nach der Loeschung **keine** verwaiste Verwendung uebrigbleibt.

**C) Sitzungs-Liste — typ-abhaengig (D-17-Logik, gleiche Begruendung wie beim Hero).**

```python
            recent_sessions.append({
                'id':           l.id,
                'date_str':     date_str,
                'session_type': stype,
                # METRIK-1 D-12/D-19: In LIVE-Zeilen stand hier kb_end — die Kaufbereitschaft des
                # KUNDEN, farbcodiert wie eine Note (gruen ab 70). In TRAINING-Zeilen ist derselbe
                # Wert die echte Trainings-Gesamtnote (routes/training.py schreibt sie nach
                # kb_end) und bleibt deshalb stehen. Dieselbe typ-Weiche wie beim Score-Hero auf
                # der Auswertungsseite. None -> die Anzeige zeigt einen Gedankenstrich.
                'score':        (l.kb_end or 0) if stype == 'training' else None,
```

**D) Tests `tests/test_wochen_dashboard_keine_note.py`** — Verhaltens-Tests auf die
**JSON-Antworten**, nicht auf den Quelltext:
1. `test_kpis_ohne_avg_score` — authentifizierter GET auf die Dashboard-v2-Route; im JSON gibt
   es **kein** `kpis.avg_score` und **kein** `kpi_trends.avg_score_pct`.
   **Gepaarter Existenz-Anker in derselben Funktion:** `kpis.sessions_this_week` **ist**
   vorhanden — sonst waere „fehlt" nicht von „Antwort leer" zu unterscheiden.
2. `test_erste_route_ohne_avg_score` — dasselbe fuer die erste Route; gepaarter Existenz-Anker:
   `closing_rate` ist im JSON vorhanden.
3. `test_begruessung_nennt_keinen_score` — im JSON-Feld `greeting_subtitle` steht **nicht** das
   Wort `Score`; gepaarter Existenz-Anker: `greeting_subtitle` ist **nicht leer**.
4. `test_live_sitzung_ohne_score` — eine geseedete Live-Sitzung mit `kb_end=80` erscheint in
   `recent_sessions` mit `score is None`; gepaarter Existenz-Anker: dieselbe Zeile traegt
   `session_type` und `date_str`.
5. `test_training_sitzung_behaelt_score` — eine geseedete Trainings-Sitzung mit `kb_end=80`
   erscheint mit `score == 80`. **Das ist der Gap-C-Test fuer diese Seite.**

**E) `services/coaching_service.py` — der Fallback-Berichtstext (`:376-379`), die VIERTE
Lesestelle (Textteil).**

Die Zeile
```python
            full_report = f"Diese Woche: {calls_count} Calls, Ø Score {avg_kb:.0f}%."
```
wird ersetzt durch:
```python
            # METRIK-1 D-12/D-19: Die Ø-Score-Zahl ist aus dem Fallback-Satz ENTFALLEN. Sie war
            # der Mittelwert von kb_end ueber die Anrufe der Woche — bei Live-Anrufen also die
            # KAUFBEREITSCHAFT DES KUNDEN, dem Verkaeufer als "Score" vorgelegt. Die Anzahl der
            # Anrufe bleibt: zaehlen ist erlaubt, benoten nicht. Eine Ersatz-Zahl tritt NICHT an
            # ihre Stelle (D-14). Die DB-Spalte coaching_reports.avg_readiness_score wird
            # weiterhin befuellt — METRIK-1 entfernt die Verbraucher, nicht den Erzeuger.
            full_report = f"Diese Woche: {calls_count} Calls."
```
⚠ **`avg_kb` bleibt berechnet** und wird weiter nach `avg_readiness_score` geschrieben (`:388`)
und im Rueckgabe-Dict geliefert (`:401`). Der Executor prueft mit
`grep -n "avg_kb" services/coaching_service.py`, dass die Variable danach **nicht verwaist** ist
— waere sie es, gehoert das als **Fund** ins SUMMARY (dann waere der Erzeuger frueher tot als
gedacht), **nicht** als stillschweigende Zusatz-Loeschung.

**F) `services/coaching_service.py` — der Coach-Report-PROMPT (`:336`), die ELFTE Lesestelle.**

⛔ **Ohne diesen Abschnitt ist die Abnahme dieses Plans nicht messbar.** Die Zeile

```python
- Durchschnittlicher Kaufbereitschafts-Score: {avg_kb:.0f}%
```

steht in `report_prompt` (`:334-341`) und liefert dem Modell **exakt den Wert der geloeschten
Ø-Score-Kachel** — `avg_kb` ist an `:268` als `sum(c.kb_end or 30 for c in calls) / calls_count`
ueber `typ == 'live'` definiert, also die **Kauflaune der Kunden**. Das Modell schreibt daraus
`report_text`; gerendert wird der an `templates/dashboard.html:350` mit `white-space:pre-line`
— **auf demselben Bildschirm, direkt unter der Stelle, an der Task 2 die Zahl entfernt.**

**Die Zeile faellt.** Die uebrigen Prompt-Zeilen (`{calls_count} Calls gefuehrt`, Redeanteil,
staerkste und schwaechste Phase, der `training_context`-Block) bleiben **zeichengleich** stehen:

```python
        report_prompt = f"""Du bist ein Sales-Coach. Erstelle einen kurzen woechentlichen Coach-Report auf Deutsch.

# METRIK-1 D-12/D-19: Aus den Wochendaten ist die Durchschnitts-Kennzahl der Kunden-Kauflaune
# entfernt. Sie stand hier als Zutat fuer den Berichtstext — das Modell hat daraus einen Satz
# gebaut, der dem Verkaeufer eine Kunden-Kennzahl als seine Leistung vorlegte, und zwar als
# freier Fliesstext, den KEIN Loesch-Anker je haette pruefen koennen. Deshalb faellt die
# ZUTAT, nicht der Text. Zaehlungen, Redeanteil und Phasen-Benennungen bleiben.
Wochendaten:
- {calls_count} Calls gefuehrt
- Redeanteil Berater: {avg_talk:.0f}% / Kunde: {avg_talk_kunde:.0f}%
"""
```
⚠ Der Kommentar steht **ueber** dem f-String bzw. als `#`-Zeile ausserhalb des Literals —
**nicht** im Prompt-Text selbst (er soll das Modell nicht erreichen). Der Executor waehlt die
Form, die die Datei sonst benutzt.
⚠ **Der Kommentar nennt weder den entfernten Zeilen-Wortlaut noch den Variablen-Namen im
Prompt-Format.** Sonst ueberlebt die Zeichenkette den Loesch-Anker (dieselbe Falle wie in
Abschnitt B).

⚠ **`avg_kb` auf Verwaisung pruefen — Pflicht, nicht Kuer.** Nach dieser Loeschung und der aus
Abschnitt E bleiben laut Kartierung noch **zwei** Verwendungen: `:388`
(`avg_readiness_score=round(avg_kb, 1)`) und `:401` (Rueckgabe-Dict). **Beide bleiben** — das ist
der **ERZEUGER** (Folgephase). Der Executor belegt das mit
`grep -n "avg_kb" services/coaching_service.py` **nach** der Aenderung und haelt die Ausgabe im
SUMMARY fest. **Findet er dort nur noch die Definition `:268`, ist `avg_kb` verwaist** — dann
faellt die Definition sauber mit (Bau-Regel 20) **und das gehoert als Fund ins SUMMARY**, weil
der Erzeuger dann frueher tot waere als angenommen. **Nicht raten — greppen.**

⚠ Teardown mit dem gemeinsamen `cleanup_rows`-Helfer (CLAUDE.md Test-Cleanup-Regel), FK-Reihenfolge
`calls` ← `conversation_logs` beachten.
  </action>

  <verify>
    <automated>grep -c "avg_score" routes/performance.py</automated>
    <automated>grep -c "score_trend_pct" routes/performance.py</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM, jeweils mit gepaartem Existenz-Anker:**
  - `grep -c "l.kb_end for l in logs_90" routes/performance.py` liefert **0**;
    Existenz-Anker: `grep -c "closing_rate = round(" routes/performance.py` liefert **>= 1**.
    ⚠ **Gemessen sind heute 2** (`:98` und `:294`) — die Quoten-Rechnung steht in **beiden**
    Routen. Ein Soll von genau `1` waere per Bauart unerfuellbar gewesen.
  - `grep -c "score_trend_pct" routes/performance.py` liefert **0**;
    Existenz-Anker: `grep -c "greeting_subtitle = " routes/performance.py` liefert **>= 4**
    (die Streak-Kaskade steht vollstaendig).
  - `grep -c "'avg_score'" routes/performance.py` liefert **0**;
    Existenz-Anker: `grep -c "'sessions_this_week'" routes/performance.py` liefert **>= 1**.
  - **Der Begruessungs-Satz — Code-Form ZUERST, Wortlaut als Zweit-Anker:**
    `grep -c "score_trend_pct" routes/performance.py` liefert **0** (die Rechnung und ihr
    `if`-Zweig sind weg — gemessen am 2026-08-13 steht der Name **5-mal**: `:263`, `:265`,
    `:267`, `:268` **und `:476`**, der Ausgang `'avg_score_pct': score_trend_pct` im
    `kpi_trends`-Block. ⚠ Hier stand „3-mal, `:263`, `:265-267`" — wer die Ist-Zahl vor dem
    Umbau gegenprueft, haette daraus faelschlich einen Fund gemeldet. Der Sollwert **0** war und
    bleibt erfuellbar: `:476` faellt in Abschnitt A mit) **und**
    `grep -c "Dein Score ist diese Woche" routes/performance.py` liefert **0** (der Wortlaut
    selbst — heute `routes/performance.py:268`).
    Existenz-Anker: `grep -c "Tage Streak" routes/performance.py` liefert **>= 2** **und**
    `grep -c "greeting_subtitle = " routes/performance.py` liefert **>= 4** (die Kaskade steht
    vollstaendig).
    ⛔ **Der Wortlaut-Anker ist nur erfuellbar, weil der Erklaer-Kommentar den Satz NICHT
    zitiert.** Beschreiben statt zitieren — sonst ueberlebt der geloeschte Satz seine eigene
    Loeschung als Kommentar.
- `grep -c "if stype == 'training' else None" routes/performance.py` liefert **1**.
- **ELFTE Lesestelle — der Coach-Report-PROMPT (Abschnitt F), der einzige Ort dieser Phase,
  an dem eine Zahl NICHT durch einen Anker auf ihre Ausgabe pruefbar ist:**
  `grep -c "Kaufbereitschafts-Score: {avg_kb" services/coaching_service.py` liefert **0**;
  gepaarter Existenz-Anker: `grep -c "calls_count} Calls gefuehrt" services/coaching_service.py`
  liefert **1** — die uebrigen Prompt-Zeilen stehen unveraendert da, und der grep hat ueberhaupt
  gesucht. Zusaetzlich: `grep -c "Redeanteil Berater: {avg_talk" services/coaching_service.py`
  liefert **1**.
  ⚠ **Warum der Anker auf die Zutat zeigt und nicht auf die Ausgabe:** `report_text` ist
  Modell-Prosa. Ein Anker darauf waere ein Wuerfelwurf. Der einzige deterministisch pruefbare
  Ort ist die Prompt-Zeile.
- **`avg_kb` ist nicht verwaist (oder der Fund ist gemeldet):**
  `grep -n "avg_kb" services/coaching_service.py` steht **mit Ausgabe** im SUMMARY; erwartet
  sind danach **3** Zeilen (`:268` Definition, der `avg_readiness_score`-Schreibblock und das
  Rueckgabe-Dict). Weicht die Zahl ab, ist das ein **Fund** — melden, nicht stillschweigend
  nachloeschen.
- **Vierte Lesestelle, Textteil (`services/coaching_service.py`):**
  `grep -c "Ø Score {avg_kb" services/coaching_service.py` liefert **0**;
  gepaarter Existenz-Anker: `grep -c "avg_readiness_score=round(avg_kb, 1)" services/coaching_service.py`
  liefert **1** — der **Erzeuger** lebt bewusst weiter, und der grep hat ueberhaupt gesucht.
  Zusaetzlich: `grep -c "Diese Woche: {calls_count} Calls." services/coaching_service.py`
  liefert **1** (die Zaehlung bleibt).
- **Keine Ersatz-Zahl:** in den geaenderten Bloecken wird **kein** neuer JSON-Schluessel und
  **keine** neue Berechnung eingefuehrt.
  ⛔ **Regions-Anker NUR gegen einen festen Basis-Commit.** `git diff <datei>` zeigt ausschliesslich
  **ungestagte** Aenderungen gegen HEAD — nach dem `git commit` des Task-Workflows ist die Ausgabe
  **leer**, und jeder darauf gebaute Zaehl-Anker liefert `0`, **ohne irgendetwas gemessen zu haben**
  (blind statt rot, sieht dabei gruen aus — Punkt 31). Deshalb:
  1. **Vor** der ersten Aenderung dieses Plans: `BASIS=$(git rev-parse HEAD)` — der SHA gehoert
     **verbatim ins SUMMARY**.
  2. Anker: `git diff $BASIS -- routes/performance.py | grep "^+" | grep -c "avg_score\|score_trend_pct"`
     liefert **0**.
  3. **Gepaarter Nicht-Leer-Beleg — ohne ihn ist `0` nicht von „nichts gemessen" zu unterscheiden:**
     `git diff $BASIS -- routes/performance.py | grep -c "^-"` liefert **>= 10** (es wurde wirklich
     geloescht) **und** `git diff $BASIS -- routes/performance.py | grep -c "^+"` liefert **>= 5**
     (die Kommentare stehen drin). Liefert einer der beiden **0**, hat der Vergleich gar nicht
     gemessen — **Fund melden, nicht weitermachen.**
- `grep -c "^def test_" tests/test_wochen_dashboard_keine_note.py` liefert **>= 5**, und **jede**
  Funktion enthaelt einen gepaarten Existenz-Anker.
  ⚠ **Kein Gleichheits-Anker auf diese Datei.** Hier stand **genau 5** — unerfuellbar, sobald
  **Task 3** (spaeter im **selben** Plan) drei weitere Funktionen in **dieselbe** Datei legt
  (`test_kb_trend_serie_ist_live_only` und die zwei Tests der kb_end-Mittelung). Ein
  Gleichheits-Soll auf eine Datei, die ein spaeterer Task derselben Welle erweitert, ist per
  Bauart rot.
- Die drei Pflicht-greps stehen **mit Ausgaben** im SUMMARY, insbesondere die Feststellung, ob
  der JSON-Schluessel der ersten Route wirklich keinen Leser hatte.
  </acceptance_criteria>

  <done>
Das Wochen-Dashboard liefert keine Note mehr — weder als Kachel, noch als Trend, noch in der
Begruessung, noch je Live-Sitzung. Trainings-Sitzungen behalten ihre Note.
  </done>
</task>

<task type="auto">
  <name>Task 2: templates/dashboard.html — Kachel raus, Setter raus, zwei final_score-Bloecke raus, KB-Trend-Diagramm raus</name>

  <files>
templates/dashboard.html
  </files>

  <read_first>
- `templates/dashboard.html` — **`:110-140`** (die KPI-Reihe samt dem Kommentar „(5 Kacheln)"),
  **`:339-356`** (der Coach-Report-Block mit der **zweiten** Ø-Score-Zahl in `:346`),
  **`:505-535`** (`renderKpis`), **`:815-855`** (die Sitzungs-Liste), **`:890-930`** und
  **`:975-1010`** (die zwei Outcome-Korrektur-Handler mit `json.final_score`).
- `templates/dashboard.html:364-373` — die Kartenzeile „Langzeit-Fortschritt" mit den **zwei**
  Charts (`chart-kb-trend`, `chart-talk-trend`) und dem Kartentitel `Kaufbereitschafts-Score`.
- `templates/dashboard.html:1154`, **`:1166-1173`** — der Chart.js-Block des KB-Trends: Serie
  `raw.kaufbereitschaft`, y-Achse **fest `min: 0, max: 100`**, JS-Kommentar `// KB Score trend`.
- `static/nerve.css:1870-1885` — `.db2-session-score` und die Nachbar-Klassen; zusaetzlich das
  `db2-two-col`-Raster der Kartenzeile.
- `CLAUDE.md` → HART-Block „Keine hardcoded Farben, keine Inline-Styles".
  </read_first>

  <action>
**A) Die Ø-Score-Kachel loeschen** (der ganze `div.db2-kpi`-Block mit `id="kpi-score"`).
⚠ **Den Kommentar darueber mitziehen:** aus `<!-- ── Zeile 2: KPI-Reihe (5 Kacheln) ── -->`
wird `(4 Kacheln)`. Ein Kommentar, der eine falsche Zahl nennt, ist genau die Sorte Abrieb, die
beim naechsten Leser Zeit kostet.

**B) In `renderKpis` die zwei Setter loeschen:**
`setText('kpi-score', ...)` und `setHtml('kpi-score-trend', ...)`.
⚠ **`setText('kpi-sessions', ...)` und `setHtml('kpi-sessions-trend', ...)` bleiben** — das ist
die Zaehl-Kachel. „Zaehlen ist erlaubt, benoten nicht."

**C) Die Sitzungs-Liste.** Die Farb-Berechnung `scoreColor` **und** das Inline-`style`-Attribut
fallen; die Zelle zeigt bei `null` einen Gedankenstrich:
```javascript
    // METRIK-1 D-12/D-19: Der Score je Zeile kommt nur noch fuer TRAINING-Sitzungen (dort ist
    // kb_end die echte Trainings-Gesamtnote). Bei LIVE stand hier die Kaufbereitschaft des
    // Kunden, farbcodiert wie eine Note — die Farbe faellt mit, denn eine Note faerbt man,
    // eine Zaehlung nicht. Kein Inline-Style mehr (CLAUDE.md Farb-Regel).
    html +=   '<span class="db2-session-score">' + (s.score != null ? s.score : '—') + '</span>';
```
⚠ `(s.score || '—')` war zusaetzlich falsch: bei einem echten Wert `0` haette es den
Gedankenstrich gezeigt. Die neue Form prueft auf `null`.

**D) Die zwei `json.final_score`-Bloecke loeschen** (Inline-Korrektur und Stift-Bearbeiten).
Sie ueberschrieben die Zelle nach einer Outcome-Korrektur mit der Note — an **beiden** Stellen
denselben Block entfernen. Der uebrige Handler (Outcome-Etikett aktualisieren, Punkt entfernen,
Stift wieder anhaengen) **bleibt unveraendert**.
⚠ Die Outcome-Korrektur betrifft nur **Live**-Anrufe; ohne den Block bleibt die Zelle nach der
Korrektur bei ihrem Gedankenstrich — richtig, kein Fehler.
⚠ Der Server liefert `final_score` weiterhin im JSON (**ERZEUGER**, Folgephase). Das ist kein
Widerspruch: hier faellt der **Verbraucher**.

**E) Die Coach-Report-Kachel (`:346`) — die VIERTE Lesestelle (Anzeigeteil).**

Aus der vierzelligen Flex-Reihe (`:345-348`) faellt die **zweite** Zelle ersatzlos weg:
```html
        <div><span style="color:#00D4AA;font-size:20px;font-weight:700">{{ weekly_report.avg_readiness_score }}%</span><br><span style="font-size:12px;color:var(--page-text-secondary)">&Oslash; Score</span></div>
```
Darueber der Kommentar:
```html
      {# METRIK-1 D-12/D-19: Die zweite Durchschnitts-Kachel des Coach-Reports (Mittelwert von
         kb_end, erzeugt in services/coaching_service.py) ist ERSATZLOS entfallen — dieselbe
         Krankheit wie in der KPI-Reihe, hier aber mit der HTML-Entitaet fuer das Durchschnitts-
         Zeichen geschrieben und deshalb von einem Schreibweisen-Anker nicht gefunden. Calls,
         Staerkste Phase und Schwaechste Phase bleiben: das sind eine Zaehlung und zwei
         Benennungen, keine Note. Keine Ersatz-Zahl (D-14).
         ⛔ Weder das literale Durchschnitts-Zeichen noch seine HTML-Entitaet stehen in diesem
         Kommentar — die Loesch-Anker dieses Tasks verlangen beide Schreibweisen 0-mal in
         dieser Datei. Beschreiben statt zitieren. #}
```
⚠ **Die drei Nachbar-Zellen bleiben unangetastet** (`calls_count`, `strongest_phase`,
`weakest_phase`). ⚠ Der Rest des Coach-Reports (`report_text`, `suggested_card`) bleibt ebenfalls
— dort steht ab Task 1 kein Ø-Score-Satz mehr.
⚠ Die hardcoded Farbe `#00D4AA` in den **verbleibenden** Zellen ist Bestand und wird hier
**nicht** mit angefasst (Reparatur-Modus: nur der Fehler-Pfad). Der Executor **meldet sie im
SUMMARY als Fund** samt `grep -n`-Fundstelle, damit sie nicht als uebersehen gilt.

**F) `.db2-session-score` in `static/nerve.css` pruefen (Punkt 20 gilt auch fuer CSS).**
Die Klasse bleibt in Benutzung (Trainings-Zeilen). **Nicht loeschen** — aber der Executor
prueft, ob die Regel Farb-Voreinstellungen enthaelt, die nur fuer die Note gedacht waren, und
haelt das Ergebnis im SUMMARY fest.

**G) 👁 DAS KB-TREND-DIAGRAMM FAELLT — Karte und Chart.js-Block.**

⛔ **Das war bis zum 14.08. ein offener Checkpoint an dieser Stelle. Er ist entschieden und
deshalb kein Checkpoint mehr.** Hier steht das Ergebnis, nicht die Abwaegung.

**Entscheidung: Option (b) — das Diagramm faellt mit.** Beide Gegenleser einig, Andre bestaetigt.
**Drei tragende Gruende:**
1. **Gemini (Kern):** *„Ein Einzelwert bei einem Anruf ist eine Situationsbeschreibung. Ein
   Wochen-Trend auf einem Dashboard ist immer ein Zeugnis."* Die Unterscheidung „der Kartentitel
   nennt ja den Kunden" existiert in der Realitaet eines gestressten Verkaeufers nicht.
2. **Am Code:** Das Diagramm ist heute **schon nicht ehrlich** — jeder Anruf ohne Wert geht als
   **erfundene 30** in den Wochenschnitt ein (`services/coaching_service.py`, fehlender Wert →
   `30`). Eine Kennzahl, die still Ersatzwerte mittelt, verdient die Verteidigung „korrekt
   benannte Kunden-Kennzahl" nur halb.
3. **Doppelstandard:** Der Text-Trendstreifen faellt ersatzlos (D-14). Eine 0-bis-100-Linie mit
   „Score" im Kartentitel danebenstehen zu lassen waere inkonsequent.

⚠ **Das frueher hier gefuehrte Argument „die Quelle stirbt in der direkt folgenden Phase" ist
FALSCH und zurueckgezogen** — zwischen METRIK-1 und der Erzeuger-Phase liegen laut ROADMAP
**drei** Phasen, und das Diagramm wuerde nicht einfrieren, sondern die flache erfundene
30er-Linie weitermalen. **Nicht wiederverwenden.**

**Was faellt:** die Karte (`:366-369`, inkl. Kartentitel und `<canvas id="chart-kb-trend">`) und
der zugehoerige Chart.js-Block (`:1166-1173`, inkl. des JS-Kommentars).
**Was bleibt:** `services/coaching_service.py::get_longterm_data` und der Rueckgabe-Schluessel
`kaufbereitschaft` — das ist der **ERZEUGER**, und METRIK-1 entfernt nur die **Verbraucher**.
⚠ **Der Kaufbereitschafts-VERLAUF JE ANRUF** (`templates/session_detail.html`) **bleibt
ebenfalls** — (b) entfernt die **Wochen-Aggregation**, nicht jede Kaufbereitschafts-Linie.

**Das Raster nachziehen:** Die Kartenzeile „Langzeit-Fortschritt" hat danach nur noch **eine**
Karte (`chart-talk-trend`). Sie wird im bestehenden `db2-two-col`-Raster sauber nachgezogen —
**keine neue CSS-Klasse, keine Farbe, kein Inline-Style** (HART-Regel Farben). Der Executor
kopiert das Muster einer bestehenden Ein-Karten-Zeile derselben Seite, statt eines zu erfinden.

⚠ **Benannte Folge, die in die Bilanz gehoert (nicht hier bauen):** Damit hat der
Datenschluessel `kaufbereitschaft` **null Leser** und wird bei jedem Dashboard-Aufruf umsonst
berechnet. **Benennen + Termin (ERZEUGER-Folgephase)** — Plan 10, Restluecken-Punkt 19, plus
Eintrag in `Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md`. Ohne diesen Vermerk ist
es der naechste „gruen, aber still tot"-Fall.
  </action>

  <verify>
    <automated>grep -c "kpi-score" templates/dashboard.html</automated>
    <automated>grep -c "json.final_score" templates/dashboard.html</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM, jeweils mit gepaartem Existenz-Anker:**
  - `grep -c "kpi-score" templates/dashboard.html` liefert **0**;
    Existenz-Anker: `grep -c "kpi-sessions" templates/dashboard.html` liefert **>= 3**
    (die Zaehl-Kachel lebt vollstaendig — Markup, Setter, Trend).
  - `grep -c "json.final_score" templates/dashboard.html` liefert **0**;
    Existenz-Anker: `grep -c "_outcomeLabelDe(json.outcome" templates/dashboard.html`
    liefert **2** (**beide** Korrektur-Handler existieren weiter, nur ohne den Score-Block).
  - **Beide Schreibweisen ausdruecklich:**
    `grep -c "Ø Score" templates/dashboard.html` liefert **0** **und**
    `grep -c "&Oslash; Score" templates/dashboard.html` liefert **0** **und**
    `grep -c "&#216; Score" templates/dashboard.html` liefert **0**.
    ⚠ **Der erste Anker allein reicht nicht** — die KPI-Kachel trug das literale `Ø`, der
    Coach-Report die Entitaet `&Oslash;`. Ein Anker auf eine einzelne Schreibweise waere
    falsch-gruen gewesen, waehrend die zweite Zahl weiter auf dem Bildschirm steht.
    ⛔ **Alle drei Anker sind nur erfuellbar, weil die beiden Erklaer-Kommentare dieses Tasks
    das Durchschnitts-Zeichen in KEINER Schreibweise enthalten** — weder literal noch als
    Entitaet, auch nicht in Anfuehrungszeichen. Ein „warum-entfernt"-Kommentar, der den
    entfernten Wortlaut zitiert, laesst die Zeichenkette ihre eigene Loeschung ueberleben.
    **Beschreiben statt zitieren.**
  - **Der belastbare Anker ist die vollstaendige Fundstellen-Liste, nicht eine Schreibweise:**
    `grep -n "Score" templates/dashboard.html` liefert **jede** verbleibende Fundstelle, und
    die Liste steht **verbatim im SUMMARY**. **Jede** verbleibende Zeile ist dort einzeln
    „geprueft, bleibt, weil …" begruendet (Bau-Regel 20 — eine Stelle darf stehenbleiben, aber
    nie ungeprueft). Gemessen **vor** dem Task: 4 Fundstellen (`:122` KPI-Kachel, `:346`
    Coach-Report, `:367` Kartentitel `Kaufbereitschafts-Score`, `:1166` JS-Kommentar
    `// KB Score trend`). **Erwartet danach: 0** — `:122` und `:346` fallen in den Abschnitten
    A/E, `:367` und `:1166` in Abschnitt G. Weicht die Zahl ab, ist das ein **Fund**, kein
    Formfehler.
    ⚠ **Hier stand bis zum 14.08. „Erwartet danach: 2"** — mit der Begruendung, `:367` sei
    ehrlich beschriftet und bleibe. **Diese Auslegung ist ueberholt:** das Diagramm faellt
    (Abschnitt G). Wer die alte Zahl stehen laesst, meldet einen Fund, wo eine Entscheidung
    umgesetzt wurde.
    Existenz-Anker: `grep -c "Sessions d. Woche" templates/dashboard.html` liefert **1**.
  - **Das KB-Trend-Diagramm ist weg, die Nachbar-Karte lebt (Abschnitt G):**
    `grep -c "chart-kb-trend" templates/dashboard.html` liefert **0**;
    gepaarter Existenz-Anker: `grep -c "chart-talk-trend" templates/dashboard.html` liefert
    **2** (Markup **und** Chart.js-Block der Nachbar-Karte stehen vollstaendig).
    Zusaetzlich: `grep -c "raw.kaufbereitschaft" templates/dashboard.html` liefert **0**;
    gepaarter Existenz-Anker in **der Erzeuger-Datei**:
    `grep -c "'kaufbereitschaft'" services/coaching_service.py` liefert **>= 1** — der ERZEUGER
    ist ausdruecklich **nicht** mit gefallen.
    ⛔ **Beide 0-Anker sind nur erfuellbar, weil die Erklaer-Kommentare dieses Abschnitts weder
    `chart-kb-trend` noch `raw.kaufbereitschaft` zitieren.** Beschreiben statt zitieren.
  - `grep -c "weekly_report.avg_readiness_score" templates/dashboard.html` liefert **0**;
    gepaarter Existenz-Anker: `grep -c "weekly_report.calls_count" templates/dashboard.html`
    liefert **1** (der Coach-Report lebt vollstaendig, nur ohne die Note).
- `grep -c "(4 Kacheln)" templates/dashboard.html` liefert **1** und
  `grep -c "(5 Kacheln)" templates/dashboard.html` liefert **0**.
- **Farb-HART-Regel:** `grep -c "var scoreColor" templates/dashboard.html` liefert **0**;
  `grep -c "db2-session-score\" style=" templates/dashboard.html` liefert **0**.
  Gepaarter Existenz-Anker: `grep -c "db2-session-score" templates/dashboard.html` liefert
  **1** (die Zelle existiert weiter, nur ohne Inline-Farbe).
- **`static/nerve.css` ist unangetastet — gegen den festen Basis-Commit, nicht gegen HEAD:**
  `git diff $BASIS -- static/nerve.css` ist **leer** (keine Klasse geloescht) — bzw. die Aenderung
  ist im SUMMARY mit dem Punkt-20-grep begruendet.
  ⛔ **`git diff static/nerve.css` ohne Basis waere hier wertlos:** nach dem Commit ist es
  **immer** leer, auch wenn die Datei umgeschrieben wurde. **Gepaarter Nicht-Leer-Beleg, damit
  „leer" etwas heisst:** `git diff $BASIS --stat -- templates/dashboard.html | wc -l` liefert
  **>= 1** — derselbe Vergleich sieht in derselben Ausfuehrung sehr wohl Aenderungen, also ist
  „leer" bei `nerve.css` ein echtes Ergebnis und kein Ausfall des Vergleichs.
  </acceptance_criteria>

  <done>
Die KPI-Reihe hat vier Kacheln und keine Note; die Sitzungs-Liste zeigt eine Zahl nur noch dort,
wo sie den Verkaeufer misst — und die Outcome-Korrektur schreibt keine Note mehr nach.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 🔴 routes/dashboard.py — der fehlende `typ`-Filter + die ZWOELFTE Lesestelle</name>

  <files>
routes/dashboard.py
tests/test_wochen_dashboard_keine_note.py
  </files>

  <read_first>
- `routes/dashboard.py` — **`:443-530`** (die `/dashboard`-Route `index()`: die `logs`-Abfrage
  **`:467-473`**, der `stats`-Block **`:474-500`** samt `avg_kb` **`:486`** und dem
  Trend-Block **`:494-499`**, die zwei Aufrufe **`:524-526`**).
- `routes/dashboard.py:305-383` — **`_generate_weekly_summary`**, besonders die Prompt-Zutat
  **`:344`** `- Trend gegenüber Vorwoche: {stats.get('trend_score', 'neutral')}`.
- `routes/dashboard.py:270-300` — **`_generate_improvement_tip`. NUR LESEN, NICHT ANFASSEN.**
  Siehe Abschnitt D.
- `routes/performance.py:56-63` — der **saubere** `typ == 'live'`-Filter von `logs_90`. Das ist
  die Vorlage und zugleich die Datei, fuer die der Freispruch **gemessen** wurde.
- `.planning/DIALOG-GSD-CLAUDIAN.md` — `### ANTWORT — Claudian — 2026-08-14`, Punkt ⑤ und
  Befund **F-2**. ⚠ Nur zum Nachlesen der Begruendung.
- **Entdeckungs-greps VOR jeder Aenderung (Bau-Regel 20), Ausgaben ins SUMMARY.**
  ⚠⚠ **NUR ZUM NACHSEHEN, KEIN ZIELWERT.** Kein Sollwert, kein Abnahme-Gate — die Anker dieses
  Tasks stehen ausschliesslich in den `<acceptance_criteria>`.
  ```bash
  grep -rn "trend_score" routes/ templates/ static/ services/ tests/
  grep -rn "improvement_tip\|weekly_summary" templates/ routes/
  grep -n "stats\[" routes/dashboard.py
  grep -c "stats\." templates/dashboard.html
  ```
  </read_first>

  <action>
**A) Der Befund, wie er gemessen wurde — damit niemand ihn groesser oder kleiner macht.**

**🔴 Cross-AI-Fund 14.08. (beim Nachmessen eines anderen Befundes entdeckt):** Die `logs`-Abfrage
in `routes/dashboard.py:467-473` filtert **nicht** auf `typ`. Sie laedt Live- **und**
Trainings-Zeilen. Direkt darunter steht

```python
stats['avg_kb'] = round(sum(l.kb_end or 30 for l in logs) / len(logs))
```

In **Trainings**-Sitzungen enthaelt `kb_end` **nicht** die Kaufbereitschaft, sondern die
**Trainings-Gesamtnote** (`routes/admin_ewb.py:72` sagt es woertlich; geschrieben
`training.py:721`). **Die Zeile mittelt also zwei verschiedene Bedeutungen in eine Zahl.**

⛔ **Warum das bis zum 14.08. niemand sah:** Der SPEC stellte an **zwei** Stellen fest, eine
Vermischung Training/Live sei „geprueft und **WIDERLEGT**". Gemessen war das an
`routes/performance.py:56-62` — **einer anderen Datei.** Der Freispruch wurde von einer Datei
auf eine **Klasse** ausgeweitet. **★ Die Lehre: Ein Freispruch gilt fuer die Datei, in der er
gemessen wurde.**

⚠ **Zwei Messwerte, die zum Befund gehoeren und ihn NICHT entkraeften:**
- `grep -c "stats\." templates/dashboard.html` liefert **0** — `stats` erreicht heute **keinen
  Bildschirm** (Dashboard v2 liest die JSON-Route). Der Fehler ist also **kein**
  Anzeige-Verstoss.
- **Aber er erreicht einen bezahlten KI-Aufruf:** `stats['trend_score']` — aus **denselben**
  vermischten `kb_end`-Werten gerechnet (`:494-499`) — steht als Zutat im Auftrag von
  `_generate_weekly_summary` (`:344`), und der ruft Claude. **Das ist die zwoelfte Lesestelle.**

**B) 👁 Die ZWOELFTE Lesestelle: der Score-Trend faellt aus dem KI-Auftrag — und danach ganz.**

**Dieselbe Klasse wie die elfte Lesestelle** (`services/coaching_service.py:336`, Task 1
Abschnitt F): eine Zutat, aus der ein Modell Fliesstext baut. **Kein Anker kann Modell-Prosa
pruefen** — deshalb faellt die **Zutat**, nicht der Text.

1. Die Prompt-Zeile mit dem Trend **entfaellt** aus `_generate_weekly_summary`. Die uebrigen
   Zeilen (`Gespräche geführt`, `Einwand-Erfolgsquote`, `Durchschnittlicher Redeanteil`,
   `Streak`) bleiben **zeichengleich** stehen — das sind Zaehlungen und Quoten, keine Note.
   ⚠ **Der Erklaer-Kommentar zitiert die entfernte Zeile NICHT** (weder Wortlaut noch
   Variablen-Namen im Prompt-Format), sonst ueberlebt die Zeichenkette ihre eigene Loeschung.
   Der Kommentar steht als `#`-Zeile **ausserhalb** des Prompt-Literals — er soll das Modell
   nicht erreichen.
2. **Danach `trend_score` auf Verwaisung pruefen — Pflicht, nicht Kuer** (Bau-Regel 20). Nach
   heutiger Kartierung ist die Prompt-Zeile der **einzige** Leser. Bestaetigt der frisch
   gezogene grep das, fallen im **selben** Commit auch:
   - der Vorbelegungs-Schluessel `'trend_score': 'neutral'` im `stats`-Dict (`:482`) und
   - der Rechen-Block `recent7` / `prev7` / `avg_r` / `avg_p` (`:494-499`) — **eine reine
     `kb_end`-Trend-Rechnung, also genau die Klasse, die D-19 entfernt.**
   Findet der grep einen **weiteren** Leser, ist das ein **Fund**: melden, den Block **stehen
   lassen** und im SUMMARY benennen. **Nicht raten — greppen.**

**C) 🔴 Der `typ`-Filter fuer die Kaufbereitschafts-Mittelung — als pruefbarer Helfer.**

`stats['avg_kb']` bekommt seinen Wert ab jetzt aus einem kleinen **reinen Helfer** direkt ueber
`index()`. **Der Helfer ist kein Selbstzweck:** Ohne ihn ist der Fix nicht testbar, weil `stats`
keinen Ausgang hat, den ein Verhaltens-Test lesen koennte (siehe Abschnitt A) — ein Test auf die
HTTP-Antwort wuerde **nichts** ueber den Wert aussagen. Der Helfer macht aus einer nicht
pruefbaren Inline-Rechnung eine **aufrufbare Funktion mit Rueckgabewert** (Test-Qualitaets-Regel:
Function-Call-Return statt Source-Presence).

```python
def _avg_kb_live(logs):
    """Mittelt kb_end AUSSCHLIESSLICH ueber LIVE-Sitzungen (METRIK-1, Cross-AI-Fund 14.08.).

    Warum der Filter: In Trainings-Sitzungen traegt kb_end NICHT die Kaufbereitschaft des
    Kunden, sondern die Trainings-Gesamtnote (routes/admin_ewb.py sagt es woertlich). Wer
    beides zusammenmittelt, bildet eine Zahl aus zwei verschiedenen Bedeutungen.

    Die Abfrage in index() laedt bewusst weiterhin BEIDE Typen: sie speist auch die
    Gespraechs-Zaehlung und die Einwand-Quote, und die sind vom Fehler nicht betroffen
    (Reparatur-Modus - nur der Fehler-Pfad wird angefasst).

    Rueckgabe: gerundeter Mittelwert oder 0, wenn keine Live-Zeile vorliegt.
    """
    live = [l for l in logs if (getattr(l, 'typ', None) or 'live') == 'live']
    if not live:
        return 0
    return round(sum(l.kb_end or 30 for l in live) / len(live))
```
Im `stats`-Block wird die Inline-Rechnung durch `stats['avg_kb'] = _avg_kb_live(logs)` ersetzt.
⚠ `(getattr(l, 'typ', None) or 'live')` — `typ` ist nullable mit Default `'live'`
(`database/models.py:331`); eine `NULL`-Zeile ist ein **Live**-Anruf und darf nicht wegfallen.
⚠ Das `or 30` bleibt **zeichengleich** erhalten. Es ist ein bekannter Ersatzwert und gehoert zur
`kb_*`-Klasse, die die **ERZEUGER-Folgephase** aufraeumt — **hier nicht mit anfassen**
(Reparatur-Modus). **Als Fund im SUMMARY benennen**, nicht stillschweigend mitkorrigieren.

⛔ **Ausdruecklich NICHT gebaut — und warum:** den `typ`-Filter in die **Abfrage** `:467-473` zu
ziehen waere die groessere Geste, aber sie aendert **drei** unbeteiligte Kennzahlen mit
(`gespraeche`, `einwand_erfolg`, `avg_redeanteil`) — das ist genau das
„wenn-ich-schon-dabei-bin", das der Reparatur-Modus verbietet. **Der Fehler ist die
kb_end-Mittelung, nicht die Abfrage.** Wer es anders will, entscheidet das ausdruecklich; der
Plan trifft die enge Wahl und benennt sie.

**D) ⛔ WAS DIESER TASK NICHT ANFASST — die zwei Verwechslungs-Fallen.**

1. **`_generate_improvement_tip` (`:270-300`) bleibt unangetastet.** Sein
   Kaufbereitschafts-Zweig (`:293-296`) ist ein **toter Ausgang**: `improvement_tip` wird an
   `render_template` uebergeben (`:602`), aber `grep -rn "improvement_tip" templates/` liefert
   **0** Treffer. **Er ist Teil der Vertagung auf Roadmap-Punkt 4.0.2** — zusammen mit dem
   **Redeanteil**-Zweig derselben Funktion.
   ⚠ **Die beiden Zweige sind namentlich vertagt, nicht als Zeilenbereich.** Die frueher
   verwendete Angabe „`dashboard.py:283-300`" hat den Kaufbereitschafts-Zweig still
   mitverschluckt. **Ein Zeilenbereich ist als Anker fuer eine Vertagung genauso zu grob wie
   fuer eine Abnahme.**
2. **Der tote Bestand derselben Datei** (`recent_calls`-Score · Abzeichen „Score ueber 80" ·
   `/api/analytics`) erreicht nach Messung heute **keinen** Bildschirm. **Kein
   Abnahme-Verstoss, kein Auftrag hier** — als **Fund fuer die Tote-Code-Inventur** ins SUMMARY.

**E) Drei Tests in `tests/test_wochen_dashboard_keine_note.py`.**

**E1 — der Regressions-Test zum `typ`-Fund, ERST ROT (Vault-Regel Nr. 1).**
Reihenfolge ist hier Teil der Aufgabe, sonst ist das Rot wertlos:
1. Den Helfer `_avg_kb_live` zuerst mit dem **HEUTIGEN** Verhalten anlegen — **ohne** Filter,
   also `round(sum(l.kb_end or 30 for l in logs) / len(logs))`.
2. `test_avg_kb_ignoriert_trainings_sitzungen` schreiben: zwei Fake-Log-Objekte, eines mit
   `typ='live', kb_end=80`, eines mit `typ='training', kb_end=20`. Erwartet **80**.
3. **Lauf ziehen → ROT**, die Ausgabe zeigt **50** (der Mischwert). **Diese Ausgabe geht
   verbatim ins SUMMARY** — sie ist der Beleg, dass der Fehler wirklich existierte.
4. Erst dann den Filter einziehen → **GRUEN**.
⛔ **Ohne den Rot-Lauf ist der Test ein gruener Test, der nichts beweist.**

**E2 — `test_avg_kb_ohne_typ_gilt_als_live`:** ein Fake-Log mit `typ=None, kb_end=70` →
Ergebnis **70**. **Gepaarter Existenz-Anker in derselben Funktion:** ein zweites Objekt mit
`typ='training'` liegt daneben und wird **nicht** mitgemittelt — sonst waere „70" nicht von
„Filter wirft alles weg und es bleibt zufaellig eines uebrig" zu unterscheiden.

**E3 — `test_kb_trend_serie_ist_live_only`** (gehoert zum ERZEUGER, den Abschnitt G von Task 2
bewusst stehen laesst): geseedet werden **eine** Live-Sitzung mit `kb_end=80` und **eine**
Trainings-Sitzung mit `kb_end=20` in derselben Woche; `get_longterm_data(user_id)` liefert fuer
diese Woche `kaufbereitschaft == [80.0]` — die Trainings-Sitzung fliesst **nicht** ein.
**Gepaarter Existenz-Anker in derselben Funktion:** `labels` ist **nicht leer** — sonst waere
„Training nicht drin" nicht von „gar keine Daten geladen" zu unterscheiden.
⚠ Dass diese Serie live-only ist, haengt heute allein an **einer** Codezeile in
`services/coaching_service.py` und ist nirgends festgenagelt. Der Test gehoert zur **Ratsche des
Test-Netzes** — er bleibt auch dann sinnvoll, wenn das Diagramm gefallen ist, weil der Erzeuger
weiterlebt.

⚠ Teardown mit dem gemeinsamen `cleanup_rows`-Helfer, FK-Reihenfolge `calls` ←
`conversation_logs` (CLAUDE.md Test-Cleanup-Regel).
  </action>

  <verify>
    <automated>grep -c "def _avg_kb_live" routes/dashboard.py</automated>
    <automated>grep -c "trend_score" routes/dashboard.py</automated>
  </verify>

  <acceptance_criteria>
- **🔴 Der `typ`-Filter ist da — Code-FORM, mit gepaartem Existenz-Anker:**
  - `grep -c "def _avg_kb_live" routes/dashboard.py` liefert **1**.
  - `grep -c "stats\['avg_kb'\] = _avg_kb_live(logs)" routes/dashboard.py` liefert **1**.
  - **Abwesenheits-Anker auf die alte, ungefilterte Rechnung:**
    `grep -c "sum(l.kb_end or 30 for l in logs)" routes/dashboard.py` liefert **0**.
    ⚠ Gemessen am 2026-08-14 steht dieses Muster **1-mal** (`:486`); die Varianten mit
    `recent7`/`prev7` fallen mit Abschnitt B. **Gepaarter Existenz-Anker:**
    `grep -c "or 'live') == 'live'" routes/dashboard.py` liefert **1** — der Filter steht
    wirklich da, und der grep hat die Datei ueberhaupt gelesen.
  - **Der ROT-Lauf aus E1 steht verbatim im SUMMARY**, inklusive des Mischwerts **50**.
    ⛔ **Ohne diese Ausgabe gilt der Fix als nicht belegt** — ein Test, der nie rot war, beweist
    nichts (Vault-Regel Nr. 1).
- **ZWOELFTE Lesestelle: die Zutat ist raus, der Auftrag lebt:**
  - `grep -c "Trend gegenüber Vorwoche" routes/dashboard.py` liefert **0**;
    gepaarter Existenz-Anker: `grep -c "Einwand-Erfolgsquote" routes/dashboard.py` liefert
    **1** — die uebrigen Prompt-Zeilen stehen unveraendert, und der grep hat gesucht.
    ⛔ Nur erfuellbar, weil der Erklaer-Kommentar die entfernte Zeile **nicht zitiert**.
  - `grep -c "trend_score" routes/dashboard.py` liefert **0** (Vorbelegung, Rechnung und
    Prompt-Zutat sind gefallen) — **oder** der Executor hat einen weiteren Leser gefunden, den
    Block stehen gelassen und den Fund im SUMMARY benannt. **Beides ist zulaessig, Schweigen
    nicht.**
    Gepaarter Existenz-Anker: `grep -c "'streak': user.streak_count" routes/dashboard.py`
    liefert **1** (der `stats`-Block als Ganzes steht).
- **Die Verwechslungs-Fallen sind NICHT angefasst:**
  `grep -c "die Kaufbereitschaft deiner Kunden endet im Schnitt" routes/dashboard.py` liefert
  **1** (der vertagte Zweig in `_generate_improvement_tip` steht unveraendert) **und**
  `grep -c "def _generate_improvement_tip" routes/dashboard.py` liefert **1**.
  ⚠ **Das ist ein Anker auf ein GLEICHBLEIBEN** — er faengt den Executor ab, der „wenn ich
  schon dabei bin" denkt.
- **Reparatur-Modus belegt:** `git diff $BASIS --stat -- routes/dashboard.py` steht im SUMMARY;
  der Diff beruehrt ausschliesslich `_avg_kb_live` (neu), die eine `avg_kb`-Zeile, den
  `trend_score`-Block und die eine Prompt-Zeile.
- **Die drei Tests existieren und sind Verhaltens-Tests:**
  `grep -c "def test_avg_kb_ignoriert_trainings_sitzungen\|def test_avg_kb_ohne_typ_gilt_als_live\|def test_kb_trend_serie_ist_live_only" tests/test_wochen_dashboard_keine_note.py`
  liefert **3**; `grep -c "inspect.getsource\|open('routes" tests/test_wochen_dashboard_keine_note.py`
  liefert **0**.
- **Die drei Funde stehen im SUMMARY** (nicht gebaut, nur benannt): der Ersatzwert `or 30` · der
  tote Bestand derselben Datei · der bezahlte KI-Aufruf `_generate_weekly_summary`, dessen
  Ausgabe kein Template rendert (`grep -rn "weekly_summary" templates/` → **0**).
  </acceptance_criteria>

  <done>
🔴 Die Kaufbereitschafts-Mittelung auf dem Dashboard mischt keine Trainings-Noten mehr in eine
Live-Kennzahl — belegt durch einen Test, der gegen den ungefixten Stand **rot** war und den
Mischwert zeigte. Der KI-Auftrag des Wochen-Textes bekommt den `kb_end`-Trend nicht mehr als
Zutat, und die Rechnung dahinter ist gefallen. Der vertagte Redeanteil- und der vertagte
Kaufbereitschafts-Zweig sind unangetastet und namentlich benannt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ausrollen + die Abnahme „kein LIVE-Anruf zeigt eine Gesamtnote" (D-19)</name>

  <read_first>
- `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-07-SUMMARY.md` und
  `-08-SUMMARY.md` — welche Bildschirme bereits versorgt sind.
  </read_first>

  <what-built>
Wochen-Dashboard ohne „Ø Score"-Kachel und ohne Score-Trend, Begruessung ohne Score-Satz,
Sitzungs-Liste ohne Note bei Live-Anrufen, keine Nachschreibung nach der Outcome-Korrektur,
**KB-Trend-Diagramm gefallen**, **`typ`-Filter in `routes/dashboard.py` korrigiert** und die
**zwoelfte Lesestelle** (Score-Trend als Zutat im KI-Auftrag) entfernt.
  </what-built>

  <how-to-verify>
**Schritt 1 — Push und Ausrollen (⚠ fuer Agenten gesperrt, Andre fuehrt aus):**
```
git push origin main
bash deploy.sh production
```

**Schritt 2 — Wochen-Dashboard oeffnen.**
1. Die KPI-Reihe hat **vier** Kacheln: Sessions d. Woche · Deals gesamt · Umsatz · Streak.
   **Keine** „Ø Score"-Kachel, **keine** Ersatz-Kachel.
2. Die Begruessung sagt nie „Dein Score ist diese Woche um N% gestiegen" — sie zeigt die
   Streak- bzw. Aktivitaets-Variante.
3. In der Sitzungs-Liste steht bei **Live**-Zeilen ein **Gedankenstrich** in der Score-Spalte,
   bei **Training**-Zeilen die Zahl.
4. Eine Outcome-Korrektur ueber den Stift durchfuehren: das Outcome-Etikett aendert sich, die
   Score-Spalte bleibt beim Gedankenstrich.
5. **Auf DEMSELBEN Bildschirm weiter nach unten scrollen bis „Dein Coach-Report".** Dort stehen
   **drei** Zellen (Calls · Staerkste Phase · Schwaechste Phase) und **keine** Ø-Score-Zahl.
   Der Berichtstext darunter nennt **keine** Ø-Score-Prozentzahl.
   ⚠ **Diese Zeile ist ausdruecklich Teil der Abnahme** — sie ist die Stelle, die ein Anker auf
   eine einzelne Schreibweise uebersehen haette.
6. 👁 **Weiter unten die Kartenzeile „Langzeit-Fortschritt".** Erwartet: **eine** Karte
   (Redeanteil-Verhaeltnis). Die Karte `Kaufbereitschafts-Score` mit der 0-bis-100-Wochenlinie
   ist **weg**, das Raster steht sauber (keine halbe Zeile, kein Loch, keine neue Farbe).

**Schritt 3 — DIE PHASEN-ABNAHME (D-19). Alle vier Bildschirme in EINEM Durchgang.**
Diese Abnahme lautet **nicht** „kein Bildschirm zeigt eine Note" — das waere per Bauart nie
gruen, weil das Training seine Note behaelt (D-17). Sie lautet:
**„Kein LIVE-Anruf zeigt eine Gesamtnote."**

| Bildschirm | Erwartet | Versorgt in |
|---|---|---|
| Auswertungsseite eines Live-Anrufs | kein „Gesamt-Score", kein „Result: …/100", kein Vierer-Aufriss, kein „vs Schnitt letzte 5", kein „Zuletzt im Training: Ø …/100" | Plan 06/07 |
| Anruf-Fenster nach dem Auflegen | keine Zahl, kein „Coaching-Score", kein Trend-Streifen — aber Zaehl-Kacheln und Knopf „Auswertung" | Plan 08 |
| Wochen-Dashboard KPI-Reihe | keine „Ø Score"-Kachel | dieser Plan |
| Wochen-Dashboard Begruessung + Sitzungs-Liste | kein Score-Satz, Gedankenstrich bei Live-Zeilen | dieser Plan |
| Wochen-Dashboard „Langzeit-Fortschritt" | **keine** 0-bis-100-Wochenlinie mehr (KB-Trend-Diagramm gefallen) | dieser Plan, Task 2 G |

**Schritt 4 — Die Gegenprobe, ohne die die Abnahme wertlos waere.**
Eine **Trainings**-Sitzung oeffnen und das Wochen-Dashboard mit mindestens einer
Trainings-Zeile ansehen. **Erwartet:** die Trainings-Note lebt an **beiden** Stellen
(Auswertungsseite-Hero **und** Sitzungs-Liste). **Belegt, nicht angenommen** — mit `conv_id`.

**Schritt 5 — Der Pflicht-grep ueber alle Lesestellen (SIEBEN aus D-12, mit den beim Planen und
in der Pruefung gefundenen inzwischen ZWOELF).**
Zum Abschluss einmal ueber das ganze Repo, Ausgaben **verbatim** ins SUMMARY:
```bash
grep -rn "_calc_call_score(" routes/ services/ static/ templates/ tests/
grep -rn "function _calcScore" static/
grep -rn "final_score" templates/ static/
grep -rn "kb_end" routes/performance.py routes/dashboard.py routes/app_routes.py
grep -n "Score" templates/dashboard.html
grep -rn "Ø Score\|&Oslash;\|&#216;" templates/ static/
```
**Erwartet:** die ersten drei liefern **0** Anzeige-Treffer (bei `final_score` bleiben nur die
**ERZEUGER**-Stellen in `routes/app_routes.py`); der vierte zeigt, welche `kb_end`-Verwendungen
uebrig sind. **`grep -n "Score" templates/dashboard.html` liefert nach Task 2 (inkl. Abschnitt G)
**0** Treffer** — die frueher als „bleiben" gefuehrten `Kaufbereitschafts-Score` und
`// KB Score trend` fallen mit dem Diagramm.

⛔ **DIE ENTSCHEIDUNGSREGEL FUER DEN VIERTEN GREP — sie hatte bis zum 14.08. nur ZWEI Klassen
und haette dadurch eine echte Lesestelle durchgewunken.** Jede verbliebene `kb_end`-Verwendung
wird **genau einer** dieser **DREI** Klassen zugeordnet, namentlich, im SUMMARY:

| Klasse | Bedeutung | Verdikt |
|---|---|---|
| **Training** | die Stelle betrifft `typ == 'training'`; dort ist `kb_end` die echte Trainings-Gesamtnote und misst den Verkaeufer (D-17) | **bleibt** |
| **Nicht-Anzeige** | die Stelle erreicht keinen Verkaeufer-Bildschirm (Erzeuger, Admin-/Datenqualitaets-Sicht D-18, Export-Artefakt, toter Ausgang) | **bleibt, benannt** |
| **🆕 KI-AUFTRAG** | die Stelle steht als **Zutat in einem Prompt**. Ein Modell baut daraus **Fliesstext**, den der Nutzer liest — **kein Anker kann diesen Text je pruefen**, weil er weder `Ø Score` noch einen Variablen-Namen enthaelt (z. B. *„Dein Kaufbereitschafts-Schnitt lag diese Woche bei 68 %."*) | **die ZUTAT faellt, nicht der Text** |

⚠ **Warum die dritte Klasse dazukam:** Ohne sie waere `_generate_weekly_summary`
(`routes/dashboard.py`) als „Nicht-Anzeige" durchgewunken worden — die Ausgabe wird tatsaechlich
von keinem Template gerendert. **Aber die Zutat war eine `kb_end`-Trendzahl in einem bezahlten
Modell-Aufruf**, und genau diese Klasse ist es, die man nicht per grep nachweisen kann. Zwei
Stellen dieser Klasse sind in dieser Phase versorgt: die **elfte** (`coaching_service.py`,
Task 1 F) und die **zwoelfte** (`dashboard.py`, Task 3 B).
⚠ **Eine Stelle, die in KEINE der drei Klassen passt, ist ein Fund** — melden, nicht in die
naechstbeste Spalte schreiben.

**Der Entitaeten-grep liefert GENAU 1 — und dieser eine Treffer ist namentlich benannt.**
Gemessen am 2026-08-13 liefert `grep -rn "Ø Score\|&Oslash;\|&#216;" templates/ static/` **drei**
Treffer: `templates/dashboard.html:122` und `:346` (**beide fallen** mit Task 1/2) sowie
**`templates/training.html:288`** — der **bleibt**:
| Fundstelle | Bleibt, weil |
|---|---|
| `templates/training.html:288` | **Trainings-KPI.** Gespeist aus `/api/training/stats` (`routes/training.py:1112`), dort sauber auf `_CL.typ == 'training'` gefiltert. Im Training ist `kb_end` die echte Trainings-Gesamtnote und misst den Verkaeufer — **D-17: Training behaelt seine Note.** |
⛔ **Die frueher hier stehende Erwartung „0 Treffer mit `Ø Score`-Bedeutung" war kein Sollwert,
sondern ein Urteil.** Sie war nur ueber den Prosa-Zusatz zu retten und haette den Executor
eingeladen, entweder `training.html` mit anzufassen (D-17-Verstoss) oder den Anker
wegzuargumentieren. **Ein Anker nennt eine Zahl und begruendet jeden Rest namentlich** — genau
das Muster, das dieser Plan bei `:367`/`:1166` schon richtig macht.
⚠ Weicht die Zahl von **1** ab oder ist der eine Treffer ein **anderer** als
`templates/training.html:288`, ist das ein **Fund** — melden, nicht anpassen.
**Gepaarter Existenz-Anker:** `grep -rn "def _calc_process_score" routes/` liefert **1** —
der ERZEUGER lebt bewusst weiter, und der grep hat ueberhaupt gesucht.
  </how-to-verify>

  <acceptance_criteria>
- `bash deploy.sh production` gruen; Tor-Ausgabe **verbatim** im SUMMARY.
- Die vier Zeilen aus Schritt 2 sind von Andre bestaetigt.
- **Die Tabelle aus Schritt 3 ist Zeile fuer Zeile abgehakt**, jeweils mit `conv_id` bzw.
  Bildschirmbezug im SUMMARY. **Das ist die zentrale Abnahme der ganzen Phase (D-19).**
- **Die Gegenprobe aus Schritt 4 ist mit `conv_id` belegt** — die Trainings-Note lebt an beiden
  Stellen. Ohne diese Gegenprobe gilt Schritt 3 als **nicht** abgenommen (sonst waere „nichts
  gefunden" nicht von „nichts gerendert" zu unterscheiden).
- Die vier greps aus Schritt 5 stehen **verbatim mit Ausgaben** im SUMMARY, mit gepaartem
  Existenz-Anker.
- **Offene Punkte ausdruecklich benannt:** Requirement 8 (Anwendungs-Pruefung) ist zu diesem
  Zeitpunkt noch offen (Plan 10, letzte Welle, abtrennbar).
  </acceptance_criteria>

  <resume-signal>
Andre antwortet „abgenommen" mit der ausgefuellten Tabelle, oder beschreibt, was abweicht.
  </resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21 + Punkt 26)

### Angefasste Tabellen

- `conversation_logs` (**lesen**: `kb_end`, `typ`, `created_at`, `result`, `einwaende_*`) —
  keine RLS
- `calls` (**lesen**: `outcome`, `outcome_source`) — keine RLS

**Dieser Plan schreibt keine Daten und aendert kein Schema.**

⚠ **Neu mit Task 3:** `conversation_logs` wird zusaetzlich in `routes/dashboard.py::index()`
gelesen — **dieselbe Tabelle, dieselben Spalten** (`kb_end`, `typ`, `created_at`), nur eine
andere Route. **Kein neuer Schreibpfad, keine neue Spalte.**

### inspect.sh-Beleg

Der `conversation_logs`-Auszug wird in **Plan 06, Sektion 5** vom Executor gezogen und dort
verbatim abgelegt. Fuer diesen Plan ist entscheidend, was dort schon geprueft wurde:
`kb_end` ist eine Zahl ohne Wertebereichs-Constraint, und `typ` traegt `live`/`training`.
⚠ **Ergab die Pruefung in Plan 06 einen dritten `typ`-Wert, ist dieser Plan anzupassen** — denn
`stype` wird hier aus `typ` **und** `session_mode` abgeleitet (`training` / `cold_call` /
`meeting`), und die neue Weiche prueft auf `stype == 'training'`. **Der Executor liest den
Ableitungs-Block, bevor er die Weiche setzt.**

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg-thread) + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `l.kb_end` (Ø-Score) | **entfaellt** | DB-Spalte `conversation_logs.kb_end` | war ein Mittelwert ueber 90 Tage — kein Vorauslauf-Risiko, aber inhaltlich falsch beschriftet | ✓ Loeschung, Bau-Regel-20-grep davor |
| `l.kb_end` (Sitzungs-Liste) | gelesen, **nur noch fuer Training** | DB-Spalte | **Live geschrieben** am Sitzungs-Ende. Der Leser laeuft danach. Bei einer gerade laufenden Sitzung ist `kb_end` NULL → `score` ist `None` → Gedankenstrich. Richtig. | ✓ |
| `json.final_score` (Nachschreibung) | **entfaellt** als Verbraucher | erzeugt in der Outcome-Korrektur aus `calls.score_breakdown` | Die **Erzeugung** bleibt (Folgephase). Kein Vorauslauf-Risiko, weil nichts mehr gelesen wird. | ✓ |
| `score_trend_pct` | **entfaellt** | war eine Rechnung ueber zwei Wochenmittel | n/a | ✓ |
| `stats['avg_kb']` (`routes/dashboard.py`) | gelesen, **ab jetzt nur ueber LIVE-Zeilen** (`_avg_kb_live`) | DB-Spalten `conversation_logs.kb_end` + `.typ` | **Live geschrieben** am Sitzungs-Ende, der Dashboard-Aufruf laeuft lange danach. **Kein Vorauslauf-Risiko.** Eine laufende Sitzung hat `kb_end = NULL` → der bestehende Ersatzwert `or 30` greift (unveraendert, als Fund benannt). ⚠ `typ` ist **nullable** mit Default `'live'` — eine NULL-Zeile ist ein Live-Anruf und wird vom Filter **nicht** verworfen. | ✓ `database/models.py:331` |
| `stats['trend_score']` | **entfaellt** samt Rechnung | war eine `kb_end`-Trendrechnung ueber zwei 7-Tage-Fenster | n/a — der einzige Leser (Prompt-Zutat in `_generate_weekly_summary`) faellt im selben Commit. **Findet der Pflicht-grep einen zweiten Leser, bleibt der Block stehen und der Fund wird gemeldet.** | ✓ grep vor der Loeschung |
| `weekly_report.avg_readiness_score` (Coach-Report-Kachel + Fallback-Satz) | **entfaellt als Verbraucher**; der Schreibpfad bleibt | DB-Spalte `coaching_reports.avg_readiness_score` (Float, nullable) | **Batch** — `get_or_generate_weekly_report` erzeugt/liest den Wochenbericht beim Dashboard-Aufruf und cacht ihn in `coaching_reports`. Der Anzeige-Leser laeuft im selben Request **nach** der Erzeugung; ohne Bericht ist `weekly_report` `None` und der ganze Block wird nicht gerendert (bestehender `{% if weekly_report %}`-Zweig). Kein Vorauslauf-Risiko. | ✓ `services/coaching_service.py:214`, `:381-405` |

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
⚠ Ausdruecklich: Dieser Plan entfernt **Anzeigen und Berechnungen**, keine gespeicherten Daten.
`conversation_logs.kb_end` bleibt befuellt — METRIK-1 entfernt die **Verbraucher**, die
Folgephase den **Erzeuger**.

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Die Sitzungs-Liste entscheidet selbst anhand des Sitzungs-Typs, ob eine
   Zahl angebracht ist.
2. **Nachvollziehen:** Alle Werte bleiben gespeichert und in der Admin-Sicht auswertbar (D-18);
   nur die irrefuehrende Anzeige verschwindet.
3. **Eingreifen koennen:** Die Weiche ist ein einzeiliges `stype == 'training'`.
4. **Marge:** Der meistbesuchte Bildschirm hoert auf, die Kauflaune der Kunden als Leistung des
   Verkaeufers auszuweisen — bei einem Coaching-Produkt ist das kein Schoenheitsfehler.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gespeicherte Zahl → meistbesuchter Bildschirm | Eine falsch beschriftete Zahl richtet hier den groessten Schaden an |
| JSON-Vertrag Route → Template | Ein entfernter Schluessel darf das Rendern nicht brechen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-METRIK1-09-01 | Denial of Service | entfernter JSON-Schluessel bricht das Rendern | **mitigate** | Server- **und** Client-Seite fallen im **selben** Commit; `renderKpis` liest `k.avg_score` danach nicht mehr. Verhaltens-Test 1 prueft die Antwort, Schritt 2 der Abnahme das Rendern. |
| T-METRIK1-09-02 | Repudiation | Trainings-Note stirbt in der Sitzungs-Liste mit | **mitigate** | Eigener Verhaltens-Test (`test_training_sitzung_behaelt_score`) **plus** die Gegenprobe in Schritt 4 der Abnahme. |
| T-METRIK1-09-03 | Tampering | `(s.score \|\| '—')` verschluckt eine echte 0 | **mitigate** | Die neue Form prueft auf `null` statt auf Falsy — als Nebenbefund mit korrigiert und im SUMMARY benannt. |
| T-METRIK1-09-04 | Information Disclosure | Sitzungs-Liste zeigt fremde Sitzungen | **accept** | Unveraendert: die Route filtert auf `g.user.id`. Dieser Plan legt keinen neuen Zugang an. |
</threat_model>

<verification>
- Alle `<acceptance_criteria>` der **vier** Tasks sind erfuellt.
- Die Entdeckungs-greps aus Task 1 und Task 3 (**„nur zum Nachsehen, kein Zielwert"**) und die
  Abschluss-greps aus Task 4 stehen mit Ausgaben im SUMMARY.
- **Der ROT-Lauf des `typ`-Regressionstests (Task 3, E1) steht verbatim im SUMMARY** — mit dem
  Mischwert 50.
- Deploy-Tor gruen; die D-19-Tabelle ist Zeile fuer Zeile abgehakt und die Gap-C-Gegenprobe
  belegt.
</verification>

<success_criteria>
- **Kein LIVE-Anruf zeigt eine Gesamtnote — auf keinem der vier Bildschirme**, und das
  Wochen-Dashboard zeigt **keine** 0-bis-100-Wochenlinie mehr.
- 🔴 **Keine Kennzahl mittelt mehr Trainings-Noten und Live-Kaufbereitschaft in eine Zahl** —
  belegt durch einen Test, der gegen den ungefixten Stand rot war.
- **Kein KI-Auftrag dieser Phase bekommt eine `kb_end`-Zahl als Zutat** (elfte und zwoelfte
  Lesestelle).
- Die Trainings-Note lebt auf der Auswertungsseite **und** in der Sitzungs-Liste, an einer
  echten Sitzung belegt.
- Keine Ersatz-Zahl ist an die Stelle der entfernten Werte getreten.
- Alle sieben Lesestellen aus D-12 **plus** die beim Planen und in der Plan-Pruefung gefundenen
  zusaetzlichen Stellen („Result: X/100", Sitzungs-Liste, zwei `final_score`-Nachschreibungen,
  **Coach-Report-Kachel und Coach-Report-Fallbacksatz**) sind versorgt und im SUMMARY einzeln
  aufgefuehrt.
- **Die Fundstellen-Liste `grep -n "Score" templates/dashboard.html` steht verbatim im SUMMARY**,
  jede verbleibende Zeile einzeln begruendet.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-09-SUMMARY.md` anlegen.
**Pflicht-Inhalte:**
- Die Entdeckungs-greps aus Task 1 und Task 3, jeweils als **„nur zum Nachsehen, kein
  Zielwert"** gekennzeichnet, und die Abschluss-greps aus Task 4 — **mit Ausgaben**.
- **Das KB-Trend-Diagramm ist gefallen** (Task 2 G), mit der Begruendung im Wortlaut — und mit
  der ausdruecklichen Feststellung, dass das alte Argument „die Quelle stirbt in der direkt
  folgenden Phase" **falsch und zurueckgezogen** ist. Der Test
  `test_kb_trend_serie_ist_live_only` ist gebaut, **obwohl** das Diagramm faellt (der ERZEUGER
  lebt weiter).
- **`kaufbereitschaft` hat danach null Leser** — benannt, mit Termin (ERZEUGER-Folgephase) und
  Eintrag im `Foundation-Code-Register.md`. Verweis auf Plan 10, Restluecken-Punkt 19.
- 🔴 **Der `typ`-Fund in `routes/dashboard.py`:** ROT-Lauf verbatim (Mischwert 50), der Diff-Stat
  als Beleg des Reparatur-Modus, und die drei benannten Funde (Ersatzwert `or 30` · toter
  Bestand derselben Datei · bezahlter KI-Aufruf ohne Abnehmer).
- **Die Drei-Klassen-Entscheidungsregel aus Schritt 5 ausgefuellt** — jede verbliebene
  `kb_end`-Stelle mit ihrer Klasse.
- Die ausgefuellte D-19-Tabelle mit `conv_id`s und die Gap-C-Gegenprobe.
- Deploy-Tor-Ausgabe verbatim.
- **Die vollstaendige Lesestellen-Bilanz:** die sieben aus D-12 **plus** die beim Planen und in
  der Plan-Pruefung gefundenen zusaetzlichen (`Result: X/100` · Sitzungs-Liste · zwei
  `final_score`-Nachschreibungen · **Coach-Report-Kachel** · **Coach-Report-Fallbacksatz**) — je
  mit dem Plan, der sie versorgt hat.
- **Die `grep -n "Score" templates/dashboard.html`-Fundstellen-Liste verbatim**, jede
  verbleibende Zeile mit „geprueft, bleibt, weil …" begruendet — insbesondere
  `Kaufbereitschafts-Score` (`:367`), das ehrlich beschriftet ist und deshalb bleibt.
- **Die Lehre aus der Plan-Pruefung, ausdruecklich:** Ein Abwesenheits-Anker auf **eine**
  Schreibweise (`Ø Score`) haette **falsch-gruen** geliefert, weil dieselbe Beschriftung an der
  vierten Stelle als HTML-Entitaet (`&Oslash; Score`) steht. Anker gehoeren auf die
  **Fundstellen-Liste** plus alle Schreibweisen, nicht auf ein Zeichen.
- **Fund, terminiert:** Das Wochen-Dashboard coacht weiter „Redeanteil Ziel: unter 40 %"
  (`routes/dashboard.py`) — auf einer Zahl, die im Kaltakquise-Modus konstant 100 % ist, waehrend
  der Bewerter-Auftrag ~55 % nennt. **Termin: Roadmap-Punkt 4.0.2**, nicht hier.
- Pruning-Notiz.
</output>
