---
phase: 08.23.2.METRIK-1
plan: 08
type: execute
wave: 7
depends_on: ["08.23.2.METRIK-1-07"]
files_modified:
  - static/pip-launcher.js
  - templates/base.html
  - routes/app_routes.py
  - tests/test_pip_keine_note.py
autonomous: false
requirements: [SPEC-R9]
user_setup: []

must_haves:
  truths:
    - "Das Anruf-Fenster zeigt nach dem Auflegen keine Note mehr."
    - "Die Zaehl-Kacheln im Anruf-Fenster bleiben unveraendert."
    - "Der Knopf 'Auswertung' erscheint weiterhin — auch ohne Note."
    - "Beim Auflegen entsteht kein Nachladen, kein Wartekreisel und keine zusaetzliche Wartezeit."
    - "Der Trend-Streifen ist an beiden Stellen weg und durch nichts ersetzt."
    - "_calc_call_score und seine Client-Kopie _calcScore existieren nicht mehr."
    - "Nach 'Naechster Call' steht kein Wert des vorigen Anrufs mehr im Anruf-Fenster (Pflicht-Nachtrag 17.08.)."
    - "Fehlt der Ausgang, wird gar keiner angezeigt — nie der des vorigen Anrufs."
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "Score-Anzeige raus, Details-Knopf aus der Klammer gezogen, _calcScore und Trend geloescht"
    - path: "routes/app_routes.py"
      provides: "/api/postcall/trend und _calc_call_score entfernt"
    - path: "tests/test_pip_keine_note.py"
      provides: "Route-Abwesenheit + Existenz-Anker, serverseitig pruefbar"
  key_links:
    - from: "static/pip-launcher.js::_revealScoreAndActions"
      to: "nlp-btn-details"
      via: "Der Knopf wird AUS der final_score-Klammer herausgezogen — sonst verschwindet er mit der Note"
      pattern: "nlp-btn-details"
---

<objective>
Das Anruf-Fenster verliert die Note — und behaelt die Zaehl-Kacheln und den Knopf zur
Auswertung. Der Trend-Streifen faellt ersatzlos, an **beiden** Stellen.

**Purpose:** Der Moment des Auflegens ist der **latenz-empfindlichste ueberhaupt** (Punkt 25).
Die neue Form entsteht erst danach im Hintergrund. Deshalb: **Note raus, Zaehl-Kacheln bleiben,
Knopf „Auswertung" bleibt — kein Nachladen, kein Wartekreisel, keine zusaetzliche Wartezeit.**
**Zaehlen ist erlaubt, benoten nicht.**

**Output:** PiP ohne Note, Formel und Client-Kopie geloescht, Trend-Route weg.

## ★ Der Knopf existiert BEREITS — D-13 ist ein Herausziehen, kein Neubau

`templates/base.html:635` traegt schon
`<button class="pip-btn-details" id="nlp-btn-details">Auswertung</button>`.

Heute wird er **innerhalb** von `if (json.final_score !== undefined && json.final_score !== null)`
eingeblendet (`static/pip-launcher.js:4300-4302`). **Wer nur die Note entfernt, entfernt den
Knopf gleich mit** — dann fuehrt vom Anruf-Fenster kein Weg mehr zur Auswertung.

**Damit ist auch die Farb-HART-Regel automatisch erfuellt:** kein neuer Knopf, kein neues Token,
keine neue Farbe.

## ⚠ Zwei Formeln, nicht eine — und die Zahl im PiP ist die zweite

- `_calc_call_score` (`routes/app_routes.py:1007-1017`) speist `/api/postcall/trend` und
  (bis Plan 06) `routes/dashboard.py`.
- **Die im PiP sichtbare Zahl ist `json.final_score`** — sie kommt aus
  `_calc_process_score` + `_OUTCOME_MODIFIERS` → `_apply_outcome_modifier`
  (`routes/app_routes.py:1021-1090`), geliefert von `/api/postcall_outcome` bzw. der
  Outcome-Korrektur (`:2285-2298`).

⛔ **Ein Anker auf `_calc_call_score` findet diese Zahl NICHT.** Jede Loesch-Pruefung in diesem
Plan braucht **beide** Muster — `_calc_call_score(` **und** `final_score` — jeweils mit
gepaartem Existenz-Anker.

⚠ **`_calc_process_score` und `_apply_outcome_modifier` werden NICHT geloescht.** Sie sind die
**ERZEUGER** von `calls.coaching_score`/`score_breakdown`. METRIK-1 entfernt die **VERBRAUCHER**,
die Folgephase den **ERZEUGER** — nie andersherum; bleiben hier Verbraucher stehen, kann die
Folgephase gar nicht feuern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-SPEC.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-CONTEXT.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-07-SUMMARY.md
@CLAUDE.md
@CLAUDE-BAUREGELN-13-14-20.md

<interfaces>
Die Stelle, an der D-13 haengt — **verbatim** `static/pip-launcher.js:4287-4305`:
```javascript
    // Score aus json.final_score (S-01, kein _calcScore). Haengt NICHT an pendingPostcall.
    if (json.final_score !== undefined && json.final_score !== null) {
      var scoreEl = pipEl('nlp-postcall-score');
      if (scoreEl) {
        scoreEl.style.display = '';
        scoreEl.textContent = String(json.final_score) + '%';
        var _fs = +json.final_score;
        scoreEl.style.color = (_fs >= 70) ? 'var(--session-score-high)'
          : (_fs >= 40) ? 'var(--session-score-mid)'
          : 'var(--session-score-low)';
        var _lbl = scoreEl.parentNode ? scoreEl.parentNode.querySelector('.pip-postcall-label') : null;
        if (_lbl) { _lbl.style.display = ''; _lbl.textContent = 'Coaching-Score'; }
      }
      var detailsBtn = pipEl('nlp-btn-details');
      if (detailsBtn) { detailsBtn.style.display = ''; detailsBtn.disabled = false; }
      // F9: Trend mit dem BESTAETIGTEN final_score fuettern (nicht _calcScore).
      _fetchAndRenderTrend(json.final_score);
    }
```

Die Client-Kopie der Formel — **verbatim** `static/pip-launcher.js:3266-3277`:
```javascript
  function _calcScore(postcall) {
    var kb = postcall.kb_end || 30;
    ...
    return Math.min(100, Math.max(0, Math.round(kb * 0.4 + behandeltRate * 100 * 0.3 + redeScore * 0.2 + skript * 0.1)));
  }
```
⚠ **`_calcScore` hat heute schon keinen Aufrufer mehr** — die drei uebrigen Treffer
(`:3331`, `:3334`, `:4287`) sind **Kommentare**. Der Executor belegt das mit einem frischen
`grep -n "_calcScore" static/pip-launcher.js` **vor** der Loeschung.

Was **BLEIBT** (D-13, „Zaehl-Kacheln bleiben") — `_renderQuickStats` (`:3466-3507`):
Kaufbereitschaft · Dauer · Einwaende · Redeanteil (Vertriebler/Kunde) · Skript-Abdeckung.
Ebenso `_renderSparkline` und der Container `nlp-section-postcall`.

Das PiP-Markup — **verbatim** `templates/base.html:625-635`:
```html
  <div id="nlp-section-postcall" style="display:none;...">
    <div class="pip-postcall-score" id="nlp-postcall-score">--</div>
    <div class="pip-postcall-label">Call Score</div>
    <div class="pip-postcall-trend" id="nlp-postcall-trend"></div>
...
      <button class="pip-btn-details" id="nlp-btn-details">Auswertung</button>
```

Die zu loeschende Route — **verbatim** `routes/app_routes.py:1347-1379`
(`api_postcall_trend`, einziger Aufrufer `static/pip-launcher.js:3494`).

Die zu loeschende Formel — **verbatim** `routes/app_routes.py:1007-1017` (`_calc_call_score`).

── ROT-LAUF-REZEPT — wortgleich in allen zehn Plaenen dieser Phase ─────────────────────────

⚠ **Jeder ROT-vor-GRUEN-Beleg in diesem Plan wird SO gezogen — nicht anders.**

Ein absichtlich roter Lauf ist ueber `bash deploy.sh production` **per Bauart nicht erzeugbar**:
`deploy.sh:222` bricht bei rotem Tor ab (*„kein Restart, kein Deploy"*). Und ein lokales
`pytest` ist **HART verboten** (CLAUDE.md, „Kein Local-Dev — KEIN lokales pytest als
Acceptance-Check"). Ohne ein drittes Werkzeug waere jede ROT-Pflicht dieser Phase
**unausfuehrbar**.

Das Werkzeug existiert und ist ein bleibendes Werkzeug (Ratchet, Andre-Direktive 15.06.):
`scripts/triage.sh` — Server-Triage-Harness. Er provisioniert `nerve_test` **1:1 wie der
deploy.sh-Gate-Block** (pg_dump-Restore + `alembic upgrade head` + 4 DSNs + Whitelist-Guard +
trap-DROP), laeuft dann **NUR die uebergebenen Tests** und **deployt NIE** (kein Restart, kein
Deploy-Meta-Write).

```bash
# ganze Testdatei
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/triage.sh tests/test_X.py -v'

# EINE Funktion — so wird der ROT-Beleg gezogen (nur der eine Test, nicht die Suite)
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/triage.sh tests/test_X.py::test_name -v --tb=long'
```

**Vier Bedingungen, ohne die der Lauf nichts beweist:**
1. **Der Code muss auf dem Server liegen.** `triage.sh` laeuft gegen `/opt/nerve/app` — also
   erst `git commit` + `git push` + Datei-Transport, **dann** triagieren. Ein Triage-Lauf gegen
   den alten Stand ist **blind statt rot** und sieht dabei gruen aus (Punkt 31).
2. **ROT vor GRUEN, nicht danach.** Der rote Lauf wird gezogen, **bevor** die Zeile gefixt bzw.
   der Bestandstest retargetiert wird (Vault-Regel Nr. 1: erst rot, dann fixen).
3. **Ausgabe verbatim ins SUMMARY.** Der Rot-Lauf ist ein **Acceptance-Artefakt**, keine
   Nebenbemerkung.
4. **Werkzeug-Ausfall ist kein ROT-Beleg.** Ein `exit 1` mit der **Fehlermeldung des Tests** IST
   der Beleg. Ein `[triage] FEHLER: pg_dump …` / `… CREATE DATABASE …` / `… Dump-Treue-Katalog-Gate`
   ist ein **Werkzeug-Ausfall** — melden, **nicht** als ROT werten (sonst ist der Beleg eine
   Fehlmessung).

⛔ **KEIN lokales `pytest`, kein lokales `python`, kein lokales `python -c`** als Abnahme —
auch nicht „nur kurz", auch nicht fuer einen reinen Unit-Test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PiP — Note raus, Knopf aus der Klammer ziehen, Trend und _calcScore loeschen</name>

  <files>
static/pip-launcher.js
templates/base.html
  </files>

  <read_first>
- `static/pip-launcher.js` — **`:3260-3350`** (`_calcScore`, `_buildTags`, `_showPostcall`),
  **`:3460-3520`** (`_renderQuickStats`, `_fetchAndRenderTrend`) und **`:4270-4330`**
  (`_revealScoreAndActions`).
- **Alle sechs Fundstellen von `nlp-btn-details`** — `grep -n "nlp-btn-details" static/pip-launcher.js`
  liefert laut Kartierung `:1838`, `:2193`, `:3531`, `:3551`, `:3570`, `:4301`. **Der Executor
  liest jede einzelne**, bevor er etwas aendert: der Knopf wird an mehreren Stellen ein- und
  ausgeblendet, und nur **eine** davon haengt an der Note.
- `templates/base.html:310-340` (die PiP-CSS-Bloecke) und `:620-640` (das PiP-Markup).
- `static/nerve.css:210-215` — die drei Tokens `--session-score-high/mid/low`.
  </read_first>

  <action>
**A) `_revealScoreAndActions` (`:4287-4305`) — die Note faellt, der Knopf wird herausgezogen.**

```javascript
    // ── METRIK-1 D-13: KEINE Note mehr im Anruf-Fenster. ────────────────────────────────────
    // Die Zahl war final_score (kb_end 30 % = Kaufbereitschaft des KUNDEN, Redeanteil 20 % = im
    // Kaltakquise-Modus baubedingt 100 % und damit strukturell 0) mal einem Outcome-Faktor.
    // Zaehlen ist erlaubt, benoten nicht — die Zaehl-Kacheln unten bleiben deshalb unangetastet.
    // Der Knopf "Auswertung" wird BEDINGUNGSLOS eingeblendet: er stand frueher INNERHALB der
    // final_score-Klammer und waere mit der Note verschwunden — dann fuehrte vom Anruf-Fenster
    // kein Weg mehr zur Auswertung. Der Knopf existiert bereits (templates/base.html), es ist
    // ein Herausziehen und kein Neubau (kein neues Token, keine neue Farbe).
    // Punkt 25: KEIN Nachladen, KEIN Wartekreisel, keine zusaetzliche Wartezeit — der Moment des
    // Auflegens ist der latenz-empfindlichste ueberhaupt. Die neue Form entsteht danach im
    // Hintergrund und wird erst auf der Auswertungsseite gelesen.
    var detailsBtn = pipEl('nlp-btn-details');
    if (detailsBtn) { detailsBtn.style.display = ''; detailsBtn.disabled = false; }
```
Der komplette `if (json.final_score !== ...)`-Block **entfaellt**, inklusive
`_fetchAndRenderTrend(json.final_score)`.

⚠ **Der Parameter `json` bleibt in der Signatur** — er wird weiter fuer den Rest der Funktion
gebraucht (`if (!json) return;` steht am Anfang). **Nicht die Funktion umbauen**, nur den Block
entfernen (Reparatur-Modus: nur der Fehler-Pfad wird angefasst).

**B) `_fetchAndRenderTrend` (`:3489-3513`) faellt komplett** (D-14) — Funktion, Aufruf und das
Element:
- Funktion loeschen.
- `templates/base.html:628` (`<div class="pip-postcall-trend" id="nlp-postcall-trend"></div>`)
  loeschen.
- Die CSS-Regeln `.pip-postcall-trend`, `.pip-postcall-trend.up`, `.pip-postcall-trend.down`,
  `.pip-postcall-trend:empty` (`templates/base.html:331-334`) loeschen.
⛔ **Keine Ersatz-Zahl.** Eine neutrale Groesse (Anrufe/Woerter) ist ausdruecklich **verworfen** —
sie misst Betriebsamkeit, nicht Fortschritt. **Nicht gestrichen, vertagt:** der Streifen kommt
zurueck mit der **Fokus-Serie**. Diesen Satz als Kommentar an die Stelle, an der die Funktion
stand.

**C) Score-Element und Etikett** (`templates/base.html:626-627`) loeschen, dazu die CSS-Regeln
`.pip-postcall-score` und `.pip-postcall-label` (`:317-318`) — **aber nur, wenn `.pip-postcall-label`
sonst keinen Nutzer mehr hat.** Der Executor prueft das mit
`grep -n "pip-postcall-label" static/ templates/` **vor** der Loeschung: es gibt weitere
Zugriffe (u. a. `static/pip-launcher.js:3095`), die dann ins Leere liefen. **Zuerst greppen,
dann entscheiden — und die Entscheidung im SUMMARY begruenden.**

**D) `_calcScore` (`:3266-3277`) loeschen** — samt den drei Kommentar-Erwaehnungen (`:3331`,
`:3334`, `:4287`), die auf sie verweisen. **Vorher greppen und belegen**, dass es keinen
Aufrufer gibt (die Kartierung sagt: nur Kommentare).

**E) Die drei Farb-Tokens pruefen (Punkt 20 gilt auch fuer CSS).**
`grep -rn "session-score-high\|session-score-mid\|session-score-low" static/ templates/`
**nach** den Loeschungen. Bleiben Nutzer uebrig (z. B. `static/nerve.css:647-649`
`.n-session-score-*`), bleiben die Tokens. Bleiben **keine**, werden Tokens **und** Klassen im
selben Commit entfernt. **Die grep-Ausgabe entscheidet, nicht die Vermutung** — sie gehoert ins
SUMMARY.

**F) Die uebrigen fuenf `nlp-btn-details`-Stellen NICHT anfassen.** Sie blenden den Knopf in
anderen Zustaenden ein/aus. Der Executor liest jede und haelt im SUMMARY fest, was sie tut —
`grep -n` allein reicht hier nicht, weil eine davon den Knopf **deaktiviert**, und das soll so
bleiben.
  </action>

  <verify>
    <automated>grep -c "function _calcScore\|_fetchAndRenderTrend" static/pip-launcher.js</automated>
    <automated>grep -c "nlp-postcall-score\|nlp-postcall-trend" templates/base.html</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM, jeweils mit gepaartem Existenz-Anker:**
  - `grep -c "function _calcScore" static/pip-launcher.js` liefert **0**;
    Existenz-Anker: `grep -c "function _renderQuickStats" static/pip-launcher.js` liefert **1**
    (die Zaehl-Kacheln leben — D-13).
  - `grep -c "_fetchAndRenderTrend" static/pip-launcher.js` liefert **0**;
    Existenz-Anker: `grep -c "function _renderSparkline" static/pip-launcher.js` liefert **1**.
  - `grep -c "json.final_score" static/pip-launcher.js` liefert **0**;
    Existenz-Anker: `grep -c "pipEl('nlp-btn-details')" static/pip-launcher.js` liefert **>= 1**
    (der Knopf wird weiterhin angefasst — er ist nicht mitgeloescht worden).
  - `grep -c "nlp-postcall-trend" templates/base.html` liefert **0**;
    Existenz-Anker: `grep -c "nlp-btn-details" templates/base.html` liefert **1**.
- **Der Knopf haengt nicht mehr an der Note:** in `_revealScoreAndActions` steht das
  Einblenden **ausserhalb** jeder `final_score`-Bedingung. Pruefbar:
  `grep -n -A3 "var detailsBtn = pipEl('nlp-btn-details')" static/pip-launcher.js` zeigt **keine**
  umschliessende `if (json.final_score`-Zeile im Kontext davor.
- **Keine Ersatz-Zahl (D-14):** an der Stelle, an der `_fetchAndRenderTrend` stand, steht ein
  Kommentar mit dem Wort „vertagt" — und **kein** neuer `textContent`-Zuweiser.
  `grep -c "Fokus-Serie" static/pip-launcher.js` liefert **>= 1**.
- **Farb-HART-Regel:** `git diff static/nerve.css templates/base.html` enthaelt **keine neue**
  Hex-Farbe und **kein neues** `style=`-Attribut. Die Diffs sind reine Loeschungen.
- Der Token-grep aus Schritt E steht **mit Ausgabe** im SUMMARY, samt der daraus abgeleiteten
  Entscheidung.
- Die Beschreibung aller sechs `nlp-btn-details`-Stellen steht im SUMMARY.
  </acceptance_criteria>

  <done>
Das Anruf-Fenster zeigt keine Note mehr, aber weiterhin die Zaehl-Kacheln und einen Knopf, der
zur Auswertung fuehrt — und der Trend-Streifen ist weg, ohne Ersatz.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/postcall/trend und _calc_call_score loeschen — Pflicht-grep auf BEIDE Muster</name>

  <files>
routes/app_routes.py
tests/test_pip_keine_note.py (NEU)
  </files>

  <read_first>
- `routes/app_routes.py:1000-1105` — `_calc_call_score`, `_OUTCOME_MODIFIERS`,
  `_calc_process_score`, `_apply_outcome_modifier`, `_calc_coaching_score`. **Alle fuenf lesen**,
  bevor eine geloescht wird.
- `routes/app_routes.py:1347-1379` — `api_postcall_trend`.
- `routes/app_routes.py:2270-2305` — die Outcome-Korrektur, die `final_score` **erzeugt** und im
  JSON zurueckgibt. ⚠ **Sie bleibt** (ERZEUGER; der letzte Anzeige-Konsument faellt in Plan 09).
- **Pflicht-grep VOR jeder Loeschung (Bau-Regel 20), Ausgaben ins SUMMARY — BEIDE Muster:**
  ⚠⚠ **NUR ZUM NACHSEHEN, KEIN ZIELWERT.** Entdeckungs-greps (Bau-Regel 20): ihre Ausgabe geht
  ins SUMMARY, sie haben **keinen Sollwert** und sind **kein Abnahme-Gate**. Die Abnahme-Anker
  dieses Plans stehen ausschliesslich in den `<acceptance_criteria>` und sind dort jeweils auf
  **eine einzelne Produktivdatei** begrenzt. *(Anlass: Im Cross-AI-Durchgang vom 14.08. hat ein
  Gegenleser einen solchen Block fuer einen Anker mit Sollwert 0 gehalten — Befund B-1, als
  FALSCH-POSITIV widerlegt. Wenn ein Gegenleser einen Such-Befehl fuer ein Gate haelt, kann ein
  Ausfuehrer das auch — und der loescht dann Tests, um die 0 zu erreichen.)*
  ```bash
  grep -rn "_calc_call_score(" routes/ services/ tests/ --include=*.py
  grep -rn "_calc_process_score(\|_apply_outcome_modifier(\|_calc_coaching_score(" routes/ services/ tests/ --include=*.py
  grep -rn "final_score" routes/ templates/ static/ tests/
  grep -rn "postcall/trend" routes/ templates/ static/ tests/
  ```
  ⛔ Ein Anker auf `_calc_call_score` allein findet die PiP-Zahl **nicht** — das ist die Falle
  dieses Plans.
  </read_first>

  <action>
**A) `api_postcall_trend` (`:1347-1379`) loeschen** — die Route **und** den Decorator. Ihr
einziger Aufrufer (`static/pip-launcher.js:3494`) ist in Task 1 gefallen.
An ihre Stelle ein Kommentar:
```python
# ── METRIK-1 D-14: Die Trend-Route des Anruf-Fensters ist ENTFALLEN. ─────────────────────────
# Sie speiste den Vergleichs-Streifen nach dem Auflegen ("diesmal gegenueber dem Schnitt der
# letzten fuenf") auf Basis der alten Vierer-Formel. Eine neutrale Ersatz-Groesse
# (Anrufe/Woerter) ist ausdruecklich VERWORFEN: sie misst Betriebsamkeit, nicht Fortschritt.
# NICHT gestrichen, VERTAGT — der Streifen kommt zurueck, sobald die Fokus-Serie existiert
# ("dreimal in Folge angewendet").
```
⛔ **Der Kommentar nennt weder den Routen-Pfad noch den Funktionsnamen.** Beide sind in diesem
Task Abwesenheits-Anker mit Sollwert **0** ueber `routes/` — ein Kommentar, der sie zitiert,
laesst die Zeichenketten ihre eigene Loeschung ueberleben und macht die Anker unerfuellbar.
**Beschreiben statt zitieren.** Dasselbe gilt fuer den Abgrenzungs-Kommentar in Abschnitt C:
dort duerfen `_calc_process_score`, `_OUTCOME_MODIFIERS` und `_apply_outcome_modifier` sehr wohl
stehen (sie **bleiben** und sind Existenz-Anker) — der **geloeschte** Name darf es nicht.

**B) `_calc_call_score` (`:1007-1017`) loeschen.** Nach Plan 06 (Leser in `routes/dashboard.py`
weg) und Task 2 A (Leser in der Trend-Route weg) hat sie **null** Leser im Produktivpfad.
⚠ **Der Executor belegt das mit dem frischen grep**, nicht mit dieser Zeile. Findet er einen
dritten Leser, ist das ein **Fund** — anhalten und melden, nicht loeschen.
Gibt es Tests, die `_calc_call_score` importieren, werden sie **im selben Task** entfernt oder
retargetiert (Vertragswechsel → stale Test, bekannte Lehre aus AUTH-2 / TAXO).

**C) NICHT loeschen — und warum, als Kommentar an Ort und Stelle:**
```python
# METRIK-1 Abgrenzung: _calc_process_score, _OUTCOME_MODIFIERS und _apply_outcome_modifier
# BLEIBEN. Sie sind die ERZEUGER von calls.coaching_score/score_breakdown (geschrieben in der
# Outcome-Korrektur). METRIK-1 entfernt die VERBRAUCHER, die Folgephase den ERZEUGER — nie
# andersherum: bleiben hier Verbraucher stehen, kann die Folgephase gar nicht feuern.
# Die in dieser Phase entfernte Vierer-Formel wird hier bewusst NICHT beim Namen genannt: ihr
# Name ist ein Loesch-Anker mit Sollwert 0 ueber routes/ und services/.
```

**D) Fund melden, nicht mitfixen:** `_calc_coaching_score` (`:1093-1103`) ist ein duenner
Wrapper, dessen einzige Aufrufer in `tests/test_correct_outcome.py` stehen — **kein
Produktiv-Leser**. Nach Bau-Regel 20 waere das ein Streich- oder Register-Kandidat.
⛔ **Hier NICHT anfassen** (Reparatur-Modus: nur der Fehler-Pfad; „faellt waehrend einer Phase
toter Code auf — nicht in die laufende Phase mischen"). **Als Fund ins SUMMARY**, mit dem
Vorschlag, ihn in der ERZEUGER-Folgephase mit zu erledigen.

**E) Test `tests/test_pip_keine_note.py`** — serverseitig pruefbar, kein Browser noetig:
1. `test_postcall_trend_route_existiert_nicht_mehr` — ein authentifizierter GET auf
   `/api/postcall/trend?n=5` liefert **404**.
   **Gepaarter Existenz-Anker in derselben Funktion:** ein GET auf eine Nachbar-Route, die es
   weiterhin gibt (z. B. `/api/keepalive`, `routes/app_routes.py:1001-1004`), liefert **200** —
   sonst waere „404" nicht von „Test-Client kaputt" oder „nicht eingeloggt" zu unterscheiden.
2. `test_calc_call_score_ist_weg` — `from routes import app_routes` und
   `assert not hasattr(app_routes, '_calc_call_score')`.
   ⚠ **Das ist der einzige zulaessige `hasattr`-Einsatz in dieser Datei** und braucht eine
   Begruendung im Docstring: geprueft wird die **Laufzeit-Schnittstelle** des Moduls, nicht das
   Vorhandensein von Quelltext. **Gepaarter Existenz-Anker in derselben Funktion:**
   `assert hasattr(app_routes, '_calc_process_score')` — der ERZEUGER lebt, und der Test hat
   ueberhaupt ein Modul geladen.
  </action>

  <verify>
    <automated>grep -c "def _calc_call_score\|def api_postcall_trend" routes/app_routes.py</automated>
    <automated>grep -rn "_calc_call_score" routes/ services/ tests/ --include=*.py</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM mit gepaartem Existenz-Anker:**
  - `grep -c "def _calc_call_score" routes/app_routes.py` liefert **0**;
    Existenz-Anker: `grep -c "def _calc_process_score" routes/app_routes.py` liefert **1**
    (der ERZEUGER lebt bewusst weiter).
  - `grep -c "def api_postcall_trend" routes/app_routes.py` liefert **0**;
    Existenz-Anker: `grep -c "def api_keepalive" routes/app_routes.py` liefert **1**.
  - **Produktivpfad restlos leer (Bau-Regel 20 + Punkt 29) — Kommentare und Docstrings
    AUSDRUECKLICH mitgezaehlt:**
    `grep -rn "_calc_call_score" routes/ services/ --include=*.py | wc -l` liefert **0** **und**
    `grep -rn "postcall/trend" routes/ templates/ static/ | wc -l` liefert **0**.
  - **`tests/` ist BEWUSST aus beiden Suchpfaden heraus — und genau deshalb einzeln benannt:**
    Die zwei Tests aus Abschnitt E **muessen** beide Namen tragen, um die Loeschung ueberhaupt
    beweisen zu koennen (ein GET auf den Pfad, ein `hasattr`-Negativ auf die Funktion). Ein
    Suchpfad mit `tests/` waere hier per Bauart unerfuellbar gewesen. Statt dessen namentlich:
    - `grep -c "_calc_call_score" tests/test_pip_keine_note.py` liefert **>= 1** und
      `grep -c "postcall/trend" tests/test_pip_keine_note.py` liefert **>= 1**
      (**Existenz**-Anker: die Loesch-Beweise existieren wirklich).
    - `grep -rln "_calc_call_score\|postcall/trend" tests/ --include=*.py` liefert **genau
      eine** Datei, naemlich `tests/test_pip_keine_note.py`.
      ⚠ **Jede weitere Datei in dieser Liste ist ein stale Bestandstest** — er wird **im selben
      Task** retargetiert oder entfernt (Vertragswechsel, bekannte Lehre aus AUTH-2/TAXO), und
      sein **ROT-Lauf gehoert verbatim ins SUMMARY**, gefuehrt als *„erwartet rot durch
      Vertragswechsel, im selben Task retargetiert"* — nicht als Fund.
      **Der Pflicht-grep vor der Loeschung** (`grep -rn "_calc_call_score" ... tests/`, oben in
      `<read_first>`) liefert genau diese Liste. Ein Ausroll-Punkt an einem stale Test scheitern
      zu lassen, ist der teuerste Weg, das herauszufinden.
- **Die zweite Formel ist ausdruecklich geprueft, nicht uebersehen:** die vier Pflicht-greps
  stehen **mit Ausgaben** im SUMMARY, inklusive der `final_score`-Liste und der Feststellung,
  welche Treffer in **Plan 09** fallen (`templates/dashboard.html`) und welche als **ERZEUGER**
  bleiben (`routes/app_routes.py:2285-2298`).
- `grep -c "^def test_" tests/test_pip_keine_note.py` liefert **2**, und **beide** Funktionen
  enthalten einen gepaarten Existenz-Anker.
- `grep -c "hasattr(" tests/test_pip_keine_note.py` liefert **2** — beide mit Begruendung im
  Docstring (Laufzeit-Schnittstelle, nicht Quelltext-Praesenz).
- `_calc_coaching_score` ist **unveraendert** und als Fund im SUMMARY vermerkt.
  </acceptance_criteria>

  <done>
Beide Formeln sind versorgt: `_calc_call_score` ist restlos geloescht, und die zweite Formel ist
geprueft, benannt und mit Begruendung stehengeblieben — nicht uebersehen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b (PFLICHT-NACHTRAG 17.08.): Das Anruf-Fenster setzt sich beim naechsten Anruf zurueck</name>

  <files>
static/pip-launcher.js
tests/test_pip_keine_note.py
  </files>

  <read_first>
- `.planning/DIALOG-GSD-CLAUDIAN.md` → **letzter** Block
  `### ANTWORT — Claudian — 2026-08-17 (👁 PFLICHT-NACHTRAG AN PLAN 08 …)` — der vollstaendige
  Befund samt Prod-Beleg-Tabelle und Andres Entscheidung. **Er ist die Quelle dieses Tasks.**
- `static/pip-launcher.js:3000-3020` — `_resetLiveState()` (existiert bereits, nullt
  `pendingPostcall` und `lastCallId`; ihr eigener Kopf-Kommentar nennt **zwei** Aufrufer).
- `static/pip-launcher.js:3540-3560` — `nextCall()` (ruft heute nur `_cleanup()` + `open()`).
- `static/pip-launcher.js:4280-4300` — die Anzeige-Stelle mit dem Rueckfall
  `json.outcome || state.lastOutcome`.
- **Pflicht-grep VOR der Aenderung (Bau-Regel 20), Ausgaben ins SUMMARY.**
  ⚠⚠ **NUR ZUM NACHSEHEN, KEIN ZIELWERT** — Entdeckungs-greps haben keinen Sollwert und sind
  kein Abnahme-Gate. Die Abnahme-Anker stehen ausschliesslich in den `<acceptance_criteria>`.
  ```bash
  grep -n "_resetLiveState" static/pip-launcher.js
  grep -n "state.lastOutcome" static/pip-launcher.js
  grep -n "lastOutcome" static/ templates/
  ```
  ⛔ **Der Kopf-Kommentar von `_resetLiveState()` nennt zwei Aufrufer — Kommentare veralten
  still.** Der frische grep entscheidet, nicht der Kommentar. Findet er einen **dritten**
  Aufrufer, ist das ein **Fund**: melden und im SUMMARY festhalten, nicht stillschweigend
  mitaendern.
  </read_first>

  <action>
**Herkunft und Abgrenzung, ausdruecklich:** Das ist ein **Bestandsfehler**, nicht aus dieser
Phase — das Anruf-Fenster war von METRIK-1 bis Task 1 unberuehrt. Er faellt hier trotzdem an,
weil Plan 08 die **Note** entfernt und damit den **auffaelligsten** stehengebliebenen Wert von
selbst verschwinden laesst. **Die anderen bleiben:** Dauer · Kaufbereitschaft · Einwaende ·
Skript-Abdeckung · Ausgang. **Ohne diesen Nachtrag ist der Fehler nach Plan 08 unsichtbarer,
aber nicht weg — und das ist schlimmer als vorher.**

⚠ **Reparatur-Modus (Vault-Regel 5): NUR der Fehler-Pfad wird angefasst.** Keine Umbenennung,
kein Auslagern, kein zweiter Aufraeum-Pfad, keine „wenn ich schon dabei bin"-Verbesserung.

**A) `nextCall()` ruft `_resetLiveState()`.**
Die Funktion **existiert bereits und tut genau das Richtige**. Sie wird nur an einem von drei
Wegen nicht gerufen. ⛔ **Kein Nachbau, keine Kopie ihrer Logik in `nextCall()`** — der Aufruf
wird ergaenzt, sonst entsteht ein **zweiter** Aufraeum-Pfad, der beim naechsten Mal wieder
auseinanderlaeuft. `_cleanup()` raeumt Mikrofon, Verbindung, Timer und Slots — **nicht** den
Nach-Anruf-Zustand; die beiden sind nicht austauschbar.

**B) `state.lastOutcome` wird in `_resetLiveState()` mitgenullt.**
Sie ist heute **nirgends** genullt. Sie gehoert in **dieselbe** Funktion wie `pendingPostcall`
und `lastCallId` — nicht daneben.

**C) Der Rueckfall `json.outcome || state.lastOutcome` faellt weg** (`:4288`).
Fehlt der Ausgang, wird **nichts** angezeigt — **nicht** der Ausgang des vorigen Anrufs.
👁 **Ein falscher Ausgang ist schlimmer als keiner.** Diesen Satz als Begruendung an die Stelle.

**D) Kopf-Kommentar von `_resetLiveState()` nachziehen.** Er nennt heute zwei Aufrufer; nach A
sind es drei. Ein Kommentar, der die Aufrufer falsch auflistet, ist genau der Mechanismus, der
diesen Fehler ueberhaupt erst verdeckt hat. **Beschreiben statt zaehlen**, falls die Liste
erneut veralten kann.

**E) Verhaltens-Test, ERST ROT** — in `tests/test_pip_keine_note.py` ergaenzt (dieselbe Datei,
weil derselbe Bildschirm; kein neuer Testdatei-Wildwuchs).
Der Test prueft das **Verhalten**, nicht die Quelltext-Praesenz (Test-Qualitaets-Regel):
zwei Anrufe nacheinander, der zweite **ohne** bestaetigten Ausgang → im Anruf-Fenster erscheint
**kein** Ausgang und **keine** Zahl aus dem ersten.
⚠ **Der ROT-Lauf wird gegen den heutigen Stand gezogen, BEVOR A/B/C gebaut sind** — nach dem
ROT-LAUF-REZEPT oben (`scripts/triage.sh` auf dem Server, Code vorher transportiert). Ein Test,
der nie rot war, prueft nichts. **Ausgabe verbatim ins SUMMARY.**
  </action>

  <verify>
    <automated>grep -c "state.lastOutcome = null" static/pip-launcher.js</automated>
    <automated>grep -n "_resetLiveState" static/pip-launcher.js</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM, mit gepaartem Existenz-Anker:**
  - `grep -c "json.outcome || state.lastOutcome" static/pip-launcher.js` liefert **0**;
    Existenz-Anker: `grep -c "json.outcome" static/pip-launcher.js` liefert **>= 1**
    (der Server-Wert wird weiterhin gelesen — der Rueckfall ist weg, nicht die Anzeige).
- **Existenz-Anker auf die Reparatur:**
  - `grep -c "state.lastOutcome = null" static/pip-launcher.js` liefert **>= 1**, und die
    Zeile steht **innerhalb** von `_resetLiveState()` — pruefbar ueber
    `grep -n -A12 "function _resetLiveState" static/pip-launcher.js`.
  - `grep -c "_resetLiveState()" static/pip-launcher.js` liefert **>= 4**
    (Definition + drei Aufrufer). ⚠ Sinkt die Zahl gegenueber dem Pflicht-grep aus
    `<read_first>`, ist ein bestehender Aufrufer verlorengegangen — **anhalten**.
- **Kein zweiter Aufraeum-Pfad:** `nextCall()` enthaelt **keine** eigene Nullung von
  `pendingPostcall`, `lastCallId` oder `lastOutcome` — sie ruft die bestehende Funktion.
  Pruefbar ueber `grep -n -A8 "function nextCall" static/pip-launcher.js`.
- **ROT vor GRUEN:** der rote Lauf des neuen Verhaltens-Tests steht **verbatim** im SUMMARY,
  gezogen **vor** der Reparatur, mit erreichtem Pruefkatalog (Werkzeug-Ausfall ist kein
  ROT-Beleg).
- Die drei Pflicht-greps aus `<read_first>` stehen **mit Ausgaben** im SUMMARY — inklusive der
  Feststellung, ob `_resetLiveState()` einen dritten, bisher unbenannten Aufrufer hat.
- **Farb-HART-Regel:** der Diff enthaelt **keine** neue Hex-Farbe und **kein** neues
  `style=`-Attribut.
  </acceptance_criteria>

  <done>
Nach „Nächster Call" steht kein Wert des vorigen Anrufs mehr im Anruf-Fenster — und der Ausgang
wird lieber **gar nicht** angezeigt als falsch.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ausrollen + Latenz-Abnahme beim Auflegen (Punkt 25 / D-13)</name>

  <read_first>
- `CLAUDE.md` → Punkt 25 („Latenz ist ein Dealbreaker") und der HART-Block „Kein Local-Dev".
  </read_first>

  <what-built>
Das Anruf-Fenster zeigt keine Note mehr, behaelt die Zaehl-Kacheln und den Knopf „Auswertung".
Der Trend-Streifen ist an beiden Stellen entfallen, `/api/postcall/trend` und beide
Formel-Kopien (`_calc_call_score`, `_calcScore`) sind geloescht.
  </what-built>

  <how-to-verify>
**Schritt 1 — Push und Ausrollen (⚠ fuer Agenten gesperrt, Andre fuehrt aus):**
```
git push origin main
bash deploy.sh production
```

**Schritt 2 — Der Latenz-Test, und zwar am echten Auflegen (D-13, Punkt 25).**
Einen kurzen Live-Anruf fahren und **auflegen**. Beobachten, im Anruf-Fenster:
1. **Kein Wartekreisel** im ehemaligen Score-Bereich.
2. **Keine spuerbare zusaetzliche Wartezeit**, bis die Kacheln stehen — gefuehlt wie vorher.
3. **Kein Nachladen**: im Browser-Netzwerk-Panel (PiP-Fenster) taucht **kein** Aufruf auf
   `/api/postcall/trend` mehr auf, und es kommt **kein** neuer Aufruf hinzu.
4. Die **Zaehl-Kacheln** stehen wie vorher da: Kaufbereitschaft · Dauer · Einwaende ·
   Redeanteil · Skript-Abdeckung.
5. Der Knopf **„Auswertung"** ist da, ist **klickbar** und fuehrt auf die Auswertungsseite.
6. **Keine Zahl**, die wie eine Note aussieht, und **kein** „Coaching-Score"-Etikett.

**Schritt 3 — Der Knopf in den anderen Zustaenden.**
Einen Anruf beenden **ohne** das Ergebnis zu bestaetigen, und einen **mit** Bestaetigung. In
**beiden** Faellen muss der Knopf „Auswertung" erreichbar sein — er haengt nicht mehr an der Note,
und er darf auch nicht an etwas anderem haengen geblieben sein.

**Schritt 3b — Der Nachtrag am echten Ablauf (Pflicht-Nachtrag 17.08.).**
**Zwei** Anrufe nacheinander fahren und dazwischen **„Nächster Call"** druecken. Der zweite Anruf
bekommt **absichtlich keinen** bestaetigten Ausgang. Im Anruf-Fenster muss nach dem zweiten
Auflegen gelten:
1. **Kein** Ausgang sichtbar (nicht „Kein Interesse" vom ersten Anruf).
2. Dauer, Kaufbereitschaft, Einwaende und Skript-Abdeckung zeigen die Werte des **zweiten**
   Anrufs — oder gar nichts, aber **nie** die des ersten.
⚠ Genau diese Konstellation war der Befund vom 17.08.: vier Anrufe, vier Dauern, **eine** Anzeige.

**Schritt 4 — Server-Gegenprobe.**
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh routes' | grep -i postcall
```
Erwartet: **kein** Eintrag `postcall/trend`. Gepaarter Existenz-Anker: mindestens eine andere
`postcall`-Route ist weiterhin gelistet — sonst beweist die leere Ausgabe nur, dass der Filter
nichts gefunden hat.
⚠ Der Werkzeug-Fund aus der ROADMAP beachten: `inspect.sh routes` war einmal kaputt, weil das
venv unter `/opt/nerve/venv` liegt und nicht unter `$APP_DIR/venv`. **Liefert der Befehl gar
nichts, ist das ein Werkzeug-Problem und kein Beweis** — dann ueber die Browser-Konsole
gegenpruefen (Aufruf auf `/api/postcall/trend` muss 404 liefern).
  </how-to-verify>

  <acceptance_criteria>
- `bash deploy.sh production` gruen; Tor-Ausgabe **verbatim** im SUMMARY.
- **Punkt 3 von Schritt 2 ist ausdruecklich bestaetigt:** kein Wartekreisel, keine zusaetzliche
  Wartezeit, **kein** zusaetzlicher Netzwerk-Aufruf beim Auflegen. **Das ist die
  Latenz-Zusage aus D-13 und Punkt 25** — ohne diese Bestaetigung gilt der Task nicht als
  abgenommen.
- Die Zaehl-Kacheln und der Knopf sind in **beiden** Zustaenden (bestaetigt / unbestaetigt) da.
- **Schritt 3b ist ausdruecklich bestaetigt:** nach „Nächster Call" steht kein Wert und kein
  Ausgang des vorigen Anrufs mehr im Anruf-Fenster. Ohne diese Bestaetigung gilt der
  Pflicht-Nachtrag als **nicht** abgenommen.
- Es ist **keine** Zahl im Anruf-Fenster sichtbar, die eine Qualitaet bewertet.
- `inspect.sh routes` (oder die 404-Gegenprobe) belegt die Abwesenheit von
  `/api/postcall/trend`, **mit gepaartem Existenz-Anker**.
- **Bekannter Zwischenzustand im SUMMARY:** das Wochen-Dashboard zeigt zu diesem Zeitpunkt noch
  eine „Ø Score"-Kachel und eine Note in der Sitzungs-Liste — sie fallen in **Plan 09**.
  </acceptance_criteria>

  <resume-signal>
Andre antwortet „passt" mit den Beobachtungen aus Schritt 2, oder beschreibt, was abweicht.
  </resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21 + Punkt 26)

### Angefasste Tabellen

**Keine.** Dieser Plan entfernt Anzeige-Code, eine Route und zwei Formeln. Es wird kein Datum
gelesen, geschrieben, geloescht oder migriert.

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `json.final_score` | **entfaellt** als Anzeige-Konsument im PiP | erzeugt in `routes/app_routes.py` aus `calls.score_breakdown`/`conversation_logs`-Feldern | Die **Erzeugung** bleibt unveraendert; nur die Anzeige faellt. Kein Vorauslauf-Risiko, weil nichts mehr gelesen wird. | ✓ per Bauart |
| `_calc_call_score(conv)` | **entfaellt** | las `conversation_logs.kb_end/einwaende_*/redeanteil_avg/skript_abdeckung` | n/a | ✓ Loeschung, Bau-Regel-20-grep davor |
| `calls.coaching_score` / `score_breakdown` | **unveraendert geschrieben** | DB-Spalten | Wird weiterhin bei der Outcome-Bestaetigung gesetzt. **METRIK-1 fasst den ERZEUGER nicht an** — die Folgephase tut das. | ✓ ausdrueckliche Abgrenzung |

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
⚠ Ausdruecklich: Dieser Plan entfernt **Anzeigen und Berechnungen**, keine gespeicherten Daten.

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Nichts Neues — im Gegenteil: eine automatische Berechnung, die
   Betriebsamkeit statt Fortschritt mass, faellt weg.
2. **Nachvollziehen:** `calls.coaching_score` und `score_breakdown` bleiben gespeichert; nur die
   irrefuehrende Anzeige verschwindet. Nichts geht verloren.
3. **Eingreifen koennen:** Der Trend-Streifen ist **vertagt, nicht gestrichen** — der Kommentar
   an der Loeschstelle nennt die Bedingung fuer seine Rueckkehr.
4. **Marge:** Eine Note im latenz-empfindlichsten Moment kostet Tempo und Vertrauen und bringt
   nichts, was der Berater steuern koennte.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anruf-Fenster → Netzwerk beim Auflegen | Jeder zusaetzliche Aufruf im latenz-empfindlichsten Moment ist ein Dealbreaker |
| Route-Loeschung → bestehende Clients | Ein alter, gecachter Client koennte die Route noch rufen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-METRIK1-08-01 | Denial of Service | Knopf verschwindet mit der Note | **mitigate** | Der Knopf wird ausdruecklich **aus** der `final_score`-Klammer gezogen; Abnahme-Schritt 3 prueft ihn in **beiden** Zustaenden. |
| T-METRIK1-08-02 | Denial of Service | alter Client ruft die geloeschte Route | **accept** | Der Aufruf liefert 404; `_fetchAndRenderTrend` hatte bereits ein `.catch()`, das den Streifen leerte. Kein Fehlerdialog, kein Absturz. Vor dem Start gibt es zudem nur Andres Test-Konto. |
| T-METRIK1-08-03 | Tampering | Loeschung trifft eine der fuenf anderen `nlp-btn-details`-Stellen | **mitigate** | Der Executor liest alle sechs Stellen und beschreibt sie im SUMMARY; nur die Stelle in `_revealScoreAndActions` wird geaendert. |
| T-METRIK1-08-04 | Information Disclosure | Token-Loeschung bricht eine andere Komponente | **mitigate** | Punkt-20-grep auf `--session-score-*` **vor** der Loeschung; die Ausgabe entscheidet, nicht die Vermutung. |
</threat_model>

<verification>
- Alle `<acceptance_criteria>` der drei Tasks sind erfuellt.
- Die vier Pflicht-greps (Bau-Regel 20) sind **vor** den Loeschungen gezogen und stehen mit
  Ausgaben im SUMMARY — **beide** Muster (`_calc_call_score(` **und** `final_score`).
- Deploy-Tor gruen; die Latenz-Abnahme beim Auflegen ist von Andre bestaetigt.
</verification>

<success_criteria>
- Kein Anruf-Fenster zeigt eine Note; Zaehl-Kacheln und Auswertungs-Knopf sind unveraendert da.
- Beim Auflegen entsteht kein zusaetzlicher Netzwerk-Aufruf, kein Wartekreisel, keine
  zusaetzliche Wartezeit.
- `_calc_call_score`, `_calcScore`, `_fetchAndRenderTrend` und `/api/postcall/trend` existieren
  nicht mehr.
- Die zweite Formel (`_calc_process_score` → `final_score`) ist geprueft, benannt und bewusst
  stehengeblieben.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-08-SUMMARY.md` anlegen.
**Pflicht-Inhalte:**
- Die vier Bau-Regel-20-greps **mit Ausgaben**, vor den Loeschungen gezogen.
- Der Token-grep auf `--session-score-*` und die daraus abgeleitete Entscheidung.
- Die Beschreibung aller sechs `nlp-btn-details`-Stellen.
- Deploy-Tor-Ausgabe verbatim + Andres Latenz-Beobachtung beim Auflegen.
- **Fund, nicht mitgefixt:** `_calc_coaching_score` hat nur Test-Aufrufer — Vorschlag fuer die
  ERZEUGER-Folgephase.
- **Fund, terminiert:** das Anruf-Fenster zeigt weiterhin die Redeanteil-Kachel, die im
  Kaltakquise-Modus baubedingt 100 % ist — waehrend die Auswertungsseite dieselbe Zeile
  gestrichen hat (D-16). **Termin: Roadmap-Punkt 4.0.2**, nicht hier.
- Pruning-Notiz.
</output>
