---
phase: 08.23.2.METRIK-1
plan: 10
type: execute
wave: 9
depends_on: ["08.23.2.METRIK-1-09"]
files_modified:
  - services/fokus_anwendung.py
  - services/slow_lane.py
  - routes/dashboard.py
  - templates/session_detail.html
  - database/models.py
  - alembic/versions/0042_metrik1_anwendung_schild.py
  - tests/test_fokus_anwendung.py
  - tests/test_judge_prompt_fokus_guard.py
autonomous: false
requirements: [SPEC-R8]
user_setup: []

must_haves:
  truths:
    - "Der Fokus des Vor-Anrufs wird im Transkript des Folge-Anrufs nachgezaehlt — ohne KI."
    - "Bei gleicher Eingabe kommt deterministisch dasselbe Ergebnis heraus."
    - "Der Vor-Anruf ist der zeitlich letzte GESPRAECHE (calls.started_at), nicht der zuletzt VERARBEITETE (rubric_score.created_at)."
    - "Ein aelterer Fokus wird ehrlich datiert angezeigt, statt so zu tun, als waere er von gestern."
    - "Der Bewertungs-Auftrag enthaelt weder einen Fokus-Schluessel noch den Anruf-Ausgang — von einem Waechter erzwungen, nicht von Sorgfalt."
    - "Das Ergebnis der Anwendungs-Pruefung ist auf der Auswertungsseite sichtbar — der Fokus-Schluessel hat damit einen Abnehmer."
    - "Das Schild von observations_jsonb nennt den VIERTEN Unterstrich-Schluessel _anwendung — in derselben Aenderung, die ihn einfuehrt (Punkt 23)."
  artifacts:
    - path: "services/fokus_anwendung.py"
      provides: "reine Code-Schicht: Fokus-Kriterium des Vor-Anrufs gegen das TRANSKRIPT des Folge-Anrufs"
      exports: ["pruefe_anwendung"]
    - path: "tests/test_fokus_anwendung.py"
      provides: "Verhaltens-Tests inkl. ROT-vor-GRUEN-Regression auf den Zeit-Anker (F-4, Punkt 26)"
    - path: "tests/test_judge_prompt_fokus_guard.py"
      provides: "Waechter mit RESTLUECKEN-Docstring, Mindest-Soll und ROT-Beleg (Punkt 31)"
    - path: "alembic/versions/0042_metrik1_anwendung_schild.py"
      provides: "COMMENT-ON-Migration fuer observations_jsonb v2 (vierter Unterstrich-Schluessel), Kopf 0041 -> 0042"
      contains: "def _comment"
  key_links:
    - from: "services/slow_lane.py::_judge_step"
      to: "services/fokus_anwendung.py::pruefe_anwendung"
      via: "Vor-Anruf-Fokus aus rubric_score.observations_jsonb['_fokus'] laden (sortiert nach calls.started_at, Punkt 26), gegen das neue Transkript zaehlen"
      pattern: "pruefe_anwendung\\("
    - from: "templates/session_detail.html"
      to: "anwendung_display"
      via: "sichtbarer Abnehmer des Fokus-Schluessels (Bau-Regel 20, kein toter Ausgang)"
      pattern: "anwendung_display"
    - from: "database/models.py::RubricScore.observations_jsonb"
      to: "alembic/versions/0042_metrik1_anwendung_schild.py"
      via: "zeichengleiche Schild-Konstante _SCHILD_RUBRIC_OBSERVATIONS v2 (Punkt 23)"
      pattern: "_SCHILD_RUBRIC_OBSERVATIONS"
---

<objective>
Die Anwendungs-Pruefung: hat der Berater die **eine Sache aus dem letzten Anruf** diesmal
umgesetzt? Gezaehlt wird vom **Code**, gegen das **Transkript** — **ohne jede KI**.

**Purpose:** Ohne Abnehmer waere der Fokus-Schluessel ein **toter Ausgang**. Und weil der
erwartete Fokus in **keinem einzigen KI-Auftrag** steht, ist eine gefaellige Pruefung
**strukturell unmoeglich** — dasselbe Muster wie beim Beobachter, der die NERVE-Vorschlaege
bewusst nicht kennt.

## ⛔ LETZTE WELLE — notfalls abtrennbar (D-24 / SPEC NACHTRAG 1 ④)

**Dieser Plan blockiert nichts.** Er ist die **letzte** Welle der Phase und kann abgetrennt
werden, ohne die Phase aufzuhalten. Alle Abnahme-Kriterien der Requirements 1-7 und 9 sind mit
Plan 09 erfuellt.

**Wird er abgetrennt, wandert er als eigene Mini-Phase direkt hinter METRIK-1** — und die
folgenden zwei Dinge wandern mit:
1. Der Fokus-Schluessel bleibt bis dahin **ohne Abnehmer** (er wird geschrieben, aber nicht
   ausgewertet). Das ist ein bekannter, benannter Zustand — **kein** stiller toter Ausgang.
2. Der Waechter aus Task 1 entfaellt; die Abwesenheit des Fokus-Schluessels im Bewertungs-Auftrag
   ist dann **nur** durch den Verhaltens-Test aus Plan 05
   (`test_prompt_hat_keinen_fokus_schluessel`) gedeckt — schwaecher, aber vorhanden.

## ⛔ KORRIGIERT: verglichen wird gegen das TRANSKRIPT (D-24)

Die urspruengliche Fassung von Requirement 8 liess gegen **die blinde Beobachtung** vergleichen.
Die enthaelt aber **keine Wortlisten-Zaehlungen**, sondern Aussagen zu vier
Verhaltens-Dimensionen. Wer das so baut, baut eine Vergleichs-Schicht, die **ins Leere greift**.

## ⛔ Was hier NICHT gebaut wird

- **Der Trend-Streifen kommt NICHT zurueck.** D-14 sagt ausdruecklich: „Nicht an Requirement 8
  koppeln." Er kehrt mit der **Fokus-Serie** („dreimal in Folge angewendet") zurueck — und die
  Serien-Zaehlung ueber mehrere Anrufe ist **out of scope** (SPEC Boundaries).
- **Keine Serien-Zaehlung.** Dieser Plan vergleicht **genau zwei** Anrufe: den letzten mit
  Fokus und den aktuellen.
- **Kein LLM im Pfad.** Kein `anthropic`-Import, kein zusaetzlicher Aufruf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-SPEC.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-CONTEXT.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-04-SUMMARY.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-05-SUMMARY.md
@CLAUDE.md
@CLAUDE-BAUREGELN-13-14-20.md

<interfaces>
Aus `services/fokus_katalog.py` (Plan 04) — die Zaehl-Maschinerie, die **wiederverwendet** wird:
```python
KATALOG_VERSION = 1
FOKUS_SCHLUESSEL = (...)
NEGATIVE_PHRASES = ((phrase, schwelle, effekt), ...)
ICH_WOERTER / WIR_WOERTER / ICH_SCHWELLE
BUZZWORDS / BUZZWORD_SCHWELLE
REASON_PHRASES
def _normalize(text) -> str
def _zaehle(phrase, norm) -> int
def waehle_fokus(berater_texte, *, firmenname=None) -> dict | None
```

Aus `services/slow_lane.py` (Plan 05) — die Stelle, an der der Fokus entsteht:
```python
    _fokus = waehle_fokus(_berater_texte, firmenname=...)
    _observations['_fokus'] = {'schema': 1, 'katalog_version': ..., 'focus_key': ...,
                               'count': ..., 'satz': ..., 'beleg': ...}
```

Aus `database/models.py` — die Tabelle, aus der der **Vor-Anruf-Fokus** gelesen wird:
`rubric_score` (FORCE RLS, partieller Unique-Index `ux_rubric_score_live_call_id` auf
`call_id WHERE origin='live'`), Spalten `call_id`, `conversation_log_id`, `origin`,
`observations_jsonb`, `created_at`.

Aus `services/slow_lane.py` — die GUC-Klammer, **innerhalb** derer `_judge_step` laeuft:
`set_current_tenant(str(tenant_id))` … `clear_current_tenant()` im `finally`.
⚠ **Damit ist ein Lesezugriff auf `rubric_score` in `_judge_step` erlaubt** — er sieht genau die
Zeilen des eigenen Mandanten. Ausserhalb dieser Klammer liefert die Tabelle **still 0 Zeilen**.

Der Waechter-Bauform-Analog: `tests/test_live_timeout_coverage.py:17-40` (`RESTLUECKEN`) und
`tests/test_session_lock_blocking_calls_guard.py:44-85` (`BEKANNTE GRENZEN`, Mindest-Soll,
`STOP-REGEL`).

Die **Migrations-Vorlage** fuer Schritt E in Task 2 — `alembic/versions/0041_metrik1_observations_schild.py`
(Plan 07), das seinerseits `0040` (Plan 02) und den `_comment`-Helfer aus
`alembic/versions/0039_zeitstempel1_sprechzeiten.py:52-73` kopiert:
```python
def upgrade():
    _comment('COLUMN rubric_score.observations_jsonb', _SCHILD_RUBRIC_OBSERVATIONS)


def downgrade():
    _comment('COLUMN rubric_score.observations_jsonb', _SCHILD_OBSERVATIONS_V1)
```
⚠ Kein `op.execute(` — `_comment()` mit `exec_driver_sql` ist die hauseigene Form seit `0039`
(Doppelpunkt-Falle). Siehe die Anmerkung unter „## Punkt 23 — Abweichung von CLAUDE.md" unten.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/fokus_anwendung.py + der Waechter gegen Fokus im Bewertungs-Auftrag</name>

  <files>
services/fokus_anwendung.py (NEU)
tests/test_fokus_anwendung.py (NEU)
tests/test_judge_prompt_fokus_guard.py (NEU)
  </files>

  <read_first>
- `services/fokus_katalog.py` — **ganze Datei** (Plan 04). Die Zaehl-Funktionen werden
  **wiederverwendet, nicht kopiert**.
- `services/judge_runner.py:236-275` — `_build_judge_prompt`, der Prompt, den der Waechter
  prueft.
- `tests/test_live_timeout_coverage.py:1-45` — **die knappste vollstaendige Waechter-Form**
  (Modul-Docstring mit `RESTLUECKEN`).
- `tests/test_session_lock_blocking_calls_guard.py:40-90` — `BEKANNTE GRENZEN` inkl.
  **Mindest-Soll-Regel** und `STOP-REGEL`.
- `CLAUDE.md` Punkt 31 — die sechs Pflicht-Bestandteile des Restluecken-Absatzes.
  </read_first>

  <action>
**A) `services/fokus_anwendung.py`.**

Modul-Docstring:
```python
# -*- coding: utf-8 -*-
"""METRIK-1 Plan 10 — Anwendungs-Pruefung: wurde die eine Sache aus dem Vor-Anruf umgesetzt?

D-24 (korrigiert 2026-08-13): verglichen wird gegen das TRANSKRIPT des Folge-Anrufs, NICHT gegen
die blinde Beobachtung. Die Beobachtung enthaelt keine Wortlisten-Zaehlungen, sondern Aussagen zu
vier Verhaltens-Dimensionen — eine Vergleichs-Schicht gegen sie greift ins Leere.

KEIN LLM IM PFAD. Der erwartete Fokus steht in KEINEM KI-Auftrag (erzwungen von
tests/test_judge_prompt_fokus_guard.py). Damit ist eine gefaellige Pruefung STRUKTURELL
unmoeglich — gleiches Muster wie beim Beobachter, der die NERVE-Vorschlaege bewusst nicht kennt.
Daher KEIN anthropic-/claude_service-Import in diesem Modul.

Die Zaehl-Regeln werden aus services/fokus_katalog.py WIEDERVERWENDET, nicht kopiert. Eine
zweite Kopie waere genau die Doppel-Wahrheit, die diese Phase abschafft.

pruefe_anwendung ist eine REINE Funktion: kein DB-Zugriff, kein Seiteneffekt, bei gleicher
Eingabe deterministisch dasselbe Ergebnis (SPEC Requirement 8 Acceptance).

NICHT hier: die Serien-Zaehlung ueber mehrere Anrufe ("dreimal in Folge") — sie ist out of scope
(SPEC Boundaries) und bringt den Trend-Streifen zurueck, der ausdruecklich NICHT an Requirement 8
gekoppelt werden soll (D-14).
"""
```

```python
def pruefe_anwendung(vorher_fokus: dict, berater_texte, *, firmenname=None,
                     vorher_gespraechszeit=None, jetzt_gespraechszeit=None) -> dict | None:
    """Zaehlt das Fokus-Kriterium des Vor-Anrufs im Transkript des Folge-Anrufs nach.

    Args:
        vorher_fokus: der '_fokus'-Block des VORHERIGEN Anrufs
            ({'focus_key', 'count', 'katalog_version', ...}). None oder ohne focus_key -> None.
        berater_texte: die BERATER-Segment-Texte des AKTUELLEN Anrufs, EWB-Zeilen bereits
            gefiltert (services/transkript_renderer.segmente_ohne_ewb).
        firmenname: nur fuer die Firmenname-Teilregel; sonst None.
        vorher_gespraechszeit: die SPRECH-Zeit des Vor-Anrufs (calls.started_at, tz-aware).
            Wird AUSSCHLIESSLICH fuer die ehrliche Datierung gebraucht, nie fuer die Zaehlung.
        jetzt_gespraechszeit: die Sprech-Zeit DIESES Anrufs. Beide Zeiten werden hereingereicht,
            damit die Funktion rein und deterministisch bleibt — KEIN datetime.now() im Modul,
            sonst waere test_deterministisch nicht mehr wahr.

    Returns:
        None, wenn es keinen Vor-Fokus gibt (der Normalfall beim ersten Anruf).
        Sonst:
        {'focus_key': str,
         'vorher_count': int|None,      # was der Vor-Anruf gezaehlt hatte
         'jetzt_count': int,            # dieselbe Regel, auf dem neuen Transkript
         'angewendet': bool,            # die Regel feuert JETZT nicht mehr
         'katalog_version': int,
         'vorher_datum': str|None,      # ISO-Tag der SPRECH-Zeit des Vor-Anrufs (F-4)
         'ist_alt': bool}               # aelter als FOKUS_ALTER_TAGE -> die Anzeige datiert ihn

    ENTSCHEIDUNGSREGEL, bewusst einfach (Punkt 27): 'angewendet' ist True, wenn dieselbe Regel
    auf dem neuen Transkript NICHT MEHR ausloest. Sie ist damit dasselbe Ja/Nein, das der
    Katalog schon kennt — keine zweite Schwelle, keine zweite Wahrheit.
    ⚠ Bewusst NICHT "jetzt_count < vorher_count": eine Verbesserung von 9 auf 5 Nennungen von
    'we provide' bleibt eine Verletzung. Wer sie als Erfolg meldet, lobt fuer etwas, das der
    Katalog weiter beanstandet — genau die gefaellige Pruefung, die hier ausgeschlossen ist.

    KATALOG-VERSIONS-BRUCH: Traegt vorher_fokus eine andere katalog_version als der heutige
    Katalog, wird None zurueckgegeben. Sonst verglichen wir gegen eine Regel, die es so nicht
    mehr gibt — und meldeten "angewendet", weil die Regel verschwunden ist.

    EHRLICHE DATIERUNG (F-4, Andre-Entscheidung 14.08.): Liegen mehr als FOKUS_ALTER_TAGE
    zwischen den beiden SPRECH-Zeiten, wird ist_alt True und vorher_datum gesetzt. Die Anzeige
    nennt dann den Tag, statt den Fokus als "deine Sache aus dem letzten Anruf" auszugeben.
    Fehlt eine der beiden Zeiten, ist ist_alt False und vorher_datum None — lieber gar nicht
    datieren als falsch datieren.
    """
```
**A2) 👁 F-4 — der Vor-Anruf ist der zuletzt GESPROCHENE, nicht der zuletzt VERARBEITETE.**

⚠ **Das ist ein Fall von CLAUDE.md Punkt 26 (Zeit-Anker).** Die erste Fassung dieses Plans
liess `_letzter_fokus_des_nutzers` nach `rubric_score.created_at` sortieren — das ist die
**Verarbeitungszeit der Nachbearbeitung**, nicht die Gespraechszeit. Zusammen mit dem fehlenden
Zeitfenster hat das eine sichtbare Fehlfunktion erzeugt: **waren die letzten fuenf Anrufe
deutsch (also ohne Fokus), holt das System einen wochenalten Fokus hervor und legt ihn dem
Nutzer als „deine Sache aus dem letzten Anruf" vor.**

**➡️ ANDRE-ENTSCHEIDUNG 14.08. — zwei Teile, beide sind zu bauen:**
1. **Sortiert wird nach GESPRAECHSZEIT** (`calls.started_at`, tz-aware, indiziert ueber
   `idx_calls_user_time` auf `(user_id, started_at)` — `database/models.py:712, :769`).
2. **Ein aelterer Fokus wird 👁 EHRLICH DATIERT** („deine Sache vom 3. Mai") statt so zu tun,
   als waere er von gestern.

⚠ **Bewusst KEIN hartes Zeitfenster.** Ein alter Fokus wird **datiert**, nicht **verschwiegen** —
das war die Entscheidung. Ein stiller Abschneider haette dieselbe Unehrlichkeit nur in die
andere Richtung: der Nutzer saehe nicht, dass es einen offenen Auftrag gab.

Dazu die Modul-Konstante in `services/fokus_anwendung.py`:
```python
# F-4 (Andre-Entscheidung 14.08.): ab dieser Spanne zwischen den SPRECH-Zeiten wird der
# Vor-Fokus in der Anzeige datiert statt als "letzter Anruf" ausgegeben. Vier-Saeulen-Punkt 3:
# andere Grenze = Ein-Zeilen-Diff.
FOKUS_ALTER_TAGE = 7
```
⚠ **Die Zeiten werden hereingereicht, nicht im Modul geholt** — `pruefe_anwendung` bleibt eine
reine Funktion (kein `datetime.now()`), sonst faellt `test_deterministisch`.

Implementierung: fuer den gegebenen `focus_key` **genau die Teilpruefung** des Katalogs erneut
laufen lassen. Sauberste Form ohne Doppel-Wahrheit: `services/fokus_katalog.py` bekommt in
diesem Task eine kleine Erweiterung —

```python
def prueft_regel(focus_key: str, berater_texte, *, firmenname=None) -> dict | None:
    """Prueft GENAU EINE Katalog-Regel und liefert {'count': int, 'verletzt': bool, 'beleg': str}
    oder None bei unbekanntem Schluessel. waehle_fokus benutzt sie fuer alle Regeln der
    Rangfolge nach; die Anwendungs-Pruefung benutzt sie fuer genau eine.
    """
```
und `waehle_fokus` wird auf `prueft_regel` umgebaut, sodass es **eine** Implementierung je Regel
gibt. ⚠ **Das ist eine Umstellung an bestehendem, getestetem Code** — die Tests aus Plan 04
(`tests/test_fokus_katalog.py`, 15 Funktionen) muessen danach **unveraendert** gruen sein. Ist
das nicht der Fall, ist die Umstellung falsch und wird zurueckgenommen; **kein** Test wird
angepasst, um sie zu retten.

**B) Der Waechter `tests/test_judge_prompt_fokus_guard.py` (Punkt 31).**

Er prueft die **Klasse** „kein Fokus-Schluessel und kein Anruf-Ausgang im Bewertungs-Auftrag" —
nicht den Einzelfall. Aufbau: `_build_judge_prompt` mit gebauten Segmenten aufrufen, den
gerenderten Prompt gegen **alle** Katalog-Schluessel aus `FOKUS_SCHLUESSEL`, gegen die
Schablonen-Signatur (`top reps`) und gegen **alle** Outcome-Werte pruefen.

**Pflicht-Bestandteile im Modul-Docstring, in dieser Reihenfolge (Punkt 31):**
```
RESTLUECKEN
-----------
1. ABGEDECKT: dass der von services/judge_runner.py::_build_judge_prompt gerenderte Prompt
   (System- UND User-Teil) keinen Schluessel aus fokus_katalog.FOKUS_SCHLUESSEL, keine
   Schablonen-Signatur und keinen Wert aus der Outcome-Liste enthaelt.

2. STRUKTURELL UNSICHTBAR: Er sieht den GERENDERTEN PROMPT, nicht das, was das Modell daraus
   macht. Ein Modell, das den Fokus selbst erraet, ist von hier aus nicht zu unterscheiden —
   dagegen steht allein die Bauart (der Fokus wird NACH dem Modell-Aufruf berechnet).
   Ebenso unsichtbar: alles, was den Prompt zur Laufzeit veraendert (Monkeypatch, ein Wrapper
   um _build_judge_prompt, dynamischer Dispatch via getattr) und jeder ZWEITE Prompt-Bauer in
   einer anderen Datei.

3. ZWEISCHNEIDIGE HEURISTIK: Die Suche laeuft auf Kleinschreibung und als Teilstring. Das
   erzeugt FALSCHTREFFER (ein Fachwort, das zufaellig 'discount' enthaelt, macht ihn rot) und
   DURCHRUTSCHER (ein umschriebener Fokus wie 'sag den Grund frueher' enthaelt keinen
   Schluessel und passiert). Die Fehlerrichtung ist bewusst: lieber einmal zu viel rot.

4. FORMAL UNKLAR: Ob der Profil-/Briefing-Block (services/prompt_pipeline.build_profile_context)
   jemals einen Katalog-Begriff enthalten koennte, ist von hier aus nicht entscheidbar — er wird
   aus Nutzer-Daten gebaut. Der Waechter prueft ihn mit, kann aber nicht garantieren, dass eine
   kuenftige Profil-Vorlage keinen englischen Katalog-Begriff einfuehrt.

5. ZWEITE SCHICHT DARUNTER: der Verhaltens-Test
   tests/test_judge_runner.py::test_prompt_hat_keinen_fokus_schluessel (Plan 05) und die
   Bauart selbst (der Fokus entsteht in services/slow_lane.py NACH run_behavior_judge).

6. GEPRUEFT UND GESCHLOSSEN: Die direkteste Form der Fehlerklasse — jemand nimmt den
   Fokus-Schluessel in den Prompt auf, damit das Modell "gezielter" beobachtet — ist
   abgedeckt und wird rot. Ebenso der Anruf-Ausgang. Rest-Kante: eine UMSCHREIBUNG des Fokus
   ohne den Schluessel (siehe Punkt 3).

MINDEST-SOLL (gegen den stillen Ausfall)
---------------------------------------
Der Waechter leitet seine Suchmenge aus FOKUS_SCHLUESSEL ab. Faellt der Import aus oder wird
das Tupel leer, wuerde er BLIND statt ROT und saehe dabei gruen aus. Deshalb:
  assert len(FOKUS_SCHLUESSEL) >= 3
  assert len(user_str) > 200        # der Prompt wurde ueberhaupt gebaut
  assert '[#1 berater' in user_str  # das Transkript wurde ueberhaupt gerendert
Sinkt eine dieser Zahlen legitim: Ursache klaeren und die Zahl MIT BEGRUENDUNG nachziehen —
den Test NIE entfernen und die Zahl NIE stillschweigend senken.

STOP-REGEL
----------
Erwartet sind 0 Treffer. Findet der Waechter einen Katalog-Schluessel oder einen Outcome-Wert im
Prompt, ist das ein FUND und kein Konfigurations-Problem: anhalten, mit Datei und Zeile melden,
NICHT die Ausnahmeliste fuellen und NICHT das Muster aufweichen. Einzige erlaubte Ausnahme ist
ein nachweislicher FALSCH-Treffer, mit '# FALSCH-TREFFER:'-Kommentar, Datei, Zeile und
Begruendung.
```

**⚠ ROT-Beleg VOR GRUEN (Punkt 31, Pflicht-Artefakt):** Der Executor baut den Verstoss
**absichtlich** ein (einen Katalog-Schluessel in den Aufgaben-Text von `_build_judge_prompt`
schreiben), laesst den Waechter **rot** laufen, nimmt die Ausgabe **verbatim** ins SUMMARY, und
**nimmt die Aenderung danach zurueck**. Ein Waechter, der nie rot war, beweist nichts.

**C) Tests `tests/test_fokus_anwendung.py`:**
1. `test_kein_vorfokus_liefert_none`
2. `test_regel_feuert_nicht_mehr_ist_angewendet` — Vor-Fokus `negative_phrases` mit
   `count=5`; neues Transkript mit 2× `we provide` → `angewendet is True`, `jetzt_count == 2`.
3. `test_regel_feuert_weiter_ist_nicht_angewendet` — neues Transkript mit 4× → `False`.
4. `test_teilverbesserung_zaehlt_nicht_als_angewendet` — Vor-Fokus `count=9`, jetzt `5`
   (immer noch ueber der Schwelle) → `angewendet is False`. **Das ist die Regel gegen die
   gefaellige Pruefung.**
5. `test_katalog_versions_bruch_liefert_none` — `vorher_fokus['katalog_version'] = 0` → `None`.
6. `test_deterministisch` — zweimal dieselbe Eingabe → identische Dicts.
7. `test_kein_llm_import` — Laufzeit-Assertion: `services.fokus_anwendung` hat kein Attribut
   `claude_client` und das Modul importiert `anthropic` nicht. *(Als
   `assert 'anthropic' not in sys.modules`-Variante untauglich — stattdessen
   `inspect.getmodule`-freier Weg: `assert not hasattr(fa, 'claude_client')` plus ein
   grep-Anker in den `<acceptance_criteria>` unten. Der Grund gehoert in den Test-Docstring.)*
8. `test_alle_katalog_regeln_sind_pruefbar` — fuer **jeden** Schluessel aus `FOKUS_SCHLUESSEL`
   liefert `prueft_regel` ein Dict (kein `None`). **Faengt einen Katalog-Eintrag ohne
   Anwendungs-Pruefung** — sonst haette genau der einen toten Ausgang.
9. `test_alter_fokus_wird_als_alt_markiert` (F-4) — dieselbe Eingabe zweimal, nur mit anderen
   Sprech-Zeiten: 30 Tage Abstand → `ist_alt is True` und `vorher_datum` ist der ISO-Tag der
   Vor-Sprech-Zeit; 2 Tage Abstand → `ist_alt is False`. **Gepaart in EINER Funktion**, damit
   nicht die eine Haelfte gruen ist, weil die Datierung ueberhaupt nie greift.
10. `test_ohne_zeiten_wird_nicht_datiert` — fehlt eine der beiden Zeiten, ist `ist_alt False`
    und `vorher_datum is None`. **Fehlerrichtung bewusst: lieber gar nicht datieren als falsch
    datieren.**
  </action>

  <verify>
    <automated>grep -c "def pruefe_anwendung" services/fokus_anwendung.py</automated>
    <automated>grep -c "RESTLUECKEN\|MINDEST-SOLL\|STOP-REGEL" tests/test_judge_prompt_fokus_guard.py</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "def pruefe_anwendung" services/fokus_anwendung.py` liefert **1**;
  `grep -c "def prueft_regel" services/fokus_katalog.py` liefert **1**.
- **Kein LLM im Pfad — Abwesenheits-Anker auf Code-FORM:**
  `grep -c "anthropic\|claude_service\|messages.create\|MODEL_" services/fokus_anwendung.py`
  liefert **0**; gepaarter Existenz-Anker:
  `grep -c "from services.fokus_katalog import" services/fokus_anwendung.py` liefert **1**.
- **Keine zweite Kopie der Zaehl-Regeln:**
  `grep -c "NEGATIVE_PHRASES = \|BUZZWORDS = " services/fokus_anwendung.py` liefert **0**;
  gepaarter Existenz-Anker: dieselben Namen kommen in `services/fokus_katalog.py` **je einmal**
  vor.
- **Die Tests aus Plan 04 sind UNVERAENDERT gruen:** `git diff tests/test_fokus_katalog.py` ist
  **leer**. Ist er es nicht, war die `prueft_regel`-Umstellung falsch — zuruecknehmen, **nicht**
  die Tests anpassen.
- Der Waechter hat alle Punkt-31-Abschnitte:
  `grep -c "RESTLUECKEN" tests/test_judge_prompt_fokus_guard.py` liefert **1**;
  `grep -c "MINDEST-SOLL" tests/test_judge_prompt_fokus_guard.py` liefert **1**;
  `grep -c "STOP-REGEL" tests/test_judge_prompt_fokus_guard.py` liefert **1**;
  `grep -c "GEPRUEFT UND GESCHLOSSEN" tests/test_judge_prompt_fokus_guard.py` liefert **1**.
- **Der ROT-Lauf des Waechters steht verbatim im SUMMARY**, mit der Beschreibung des absichtlich
  eingebauten Verstosses und dem Beleg, dass er zurueckgenommen wurde
  (`git diff services/judge_runner.py` ist am Ende **leer** bis auf die Aenderungen aus Plan 05).
- `grep -c "^def test_" tests/test_fokus_anwendung.py` liefert **10** (Stand am Ende von
  Task 1; Task 2 bringt ihn auf 14).
- **Die Datierung ist ein Schalter, kein Literal:** `grep -c "FOKUS_ALTER_TAGE" services/fokus_anwendung.py`
  liefert **>= 2** (Definition + Verwendung).
- **Kein `datetime.now()` im Modul** (sonst ist `test_deterministisch` unwahr):
  `grep -c "datetime.now()\|utcnow()" services/fokus_anwendung.py` liefert **0**; gepaarter
  Existenz-Anker: `grep -c "vorher_gespraechszeit" services/fokus_anwendung.py` liefert **>= 2**.
  </acceptance_criteria>

  <done>
Die Anwendungs-Pruefung ist eine reine, deterministische Code-Schicht ohne KI, sie benutzt
**dieselbe** Zaehl-Implementierung wie der Katalog, und ein Waechter mit benannten Grenzen
verhindert, dass der Fokus jemals in den Bewertungs-Auftrag rutscht.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anschluss + sichtbarer Abnehmer (Bau-Regel 20 — kein toter Ausgang)</name>

  <files>
services/slow_lane.py
routes/dashboard.py
templates/session_detail.html
database/models.py
alembic/versions/0042_metrik1_anwendung_schild.py (NEU)
tests/test_fokus_anwendung.py
  </files>

  <read_first>
- `services/slow_lane.py::_judge_step` im Ist-Stand **nach Plan 05** — insbesondere, wo `_fokus`
  gesetzt wird und wo die GUC-Klammer beginnt und endet (`_call_end_merge:640-678`).
- `routes/dashboard.py:934-1035` — der Preview-Bau und die Kontext-Uebergabe **nach Plan 05**.
- `templates/session_detail.html` — die KI-Karte **nach Plan 07**, insbesondere der Block
  „One thing for next time".
- `database/models.py:901-938` — `RubricScore` (FORCE RLS, partieller Unique-Index) **im
  Ist-Stand nach Plan 07**, insbesondere die Konstante `_SCHILD_RUBRIC_OBSERVATIONS` (Plan 07
  Task 2) — sie nennt heute **drei** Unterstrich-Schluessel und bekommt in Schritt E einen
  **vierten**.
- `alembic/versions/0041_metrik1_observations_schild.py` (Plan 07) — **die unmittelbare
  Vorlage** fuer die Migration in Schritt E: Kopf, `_comment`-Helfer, `upgrade`/`downgrade`-Form.
- `alembic/versions/0039_zeitstempel1_sprechzeiten.py:52-73` — die **Definition** des
  `_comment`-Helfers samt Docstring (der Warnhinweis gehoert an die Definition, nicht an eine
  Aufrufstelle).
- `CLAUDE.md` Punkt 23 — die sechs Schild-Regeln und die **Aktualitaets-Pflicht**.
  </read_first>

  <action>
**A) Vor-Anruf-Fokus laden — innerhalb der bestehenden GUC-Klammer.**

In `services/slow_lane.py::_judge_step`, **nach** der `_fokus`-Berechnung aus Plan 05:

```python
    # ── METRIK-1 Requirement 8 (D-24): Anwendungs-Pruefung gegen das TRANSKRIPT. ─────────────
    # Der zuletzt vergebene Fokus dieses Nutzers wird im Transkript DIESES Anrufs nachgezaehlt.
    # KEIN LLM im Pfad; der erwartete Fokus steht in keinem KI-Auftrag (Waechter
    # tests/test_judge_prompt_fokus_guard.py) — eine gefaellige Pruefung ist damit strukturell
    # unmoeglich.
    # RLS: Dieser Lesezugriff laeuft INNERHALB der M-4-GUC-Klammer des Merge-Gates. Ausserhalb
    # liefert rubric_score STILL 0 Zeilen statt eines Fehlers — dann waere jeder Anruf still
    # "erster Anruf ohne Vor-Fokus". Deshalb hier und nirgendwo sonst.
    from services.fokus_anwendung import pruefe_anwendung

    _vorher, _vorher_zeit = _letzter_fokus_des_nutzers(call, db)
    _anwendung = pruefe_anwendung(_vorher, _berater_texte,
                                  firmenname=_firmenname_fuer_call(call, db),
                                  vorher_gespraechszeit=_vorher_zeit,
                                  jetzt_gespraechszeit=getattr(call, 'started_at', None))
    _observations['_anwendung'] = _anwendung or {
        'schema': 1, 'focus_key': None, 'vorher_count': None,
        'jetzt_count': None, 'angewendet': None,
        'vorher_datum': None, 'ist_alt': False,
    }
```
Der Schluessel wird **immer** geschrieben — ein fehlender Schluessel und ein ehrliches „es gab
keinen Vor-Fokus" sind im Anzeige-Pfad sonst nicht unterscheidbar.

Dazu der Lade-Helfer in `services/slow_lane.py`:
```python
def _letzter_fokus_des_nutzers(call, db) -> tuple:
    """Der '_fokus'-Block des zuletzt GESPROCHENEN Anrufs desselben Nutzers vor diesem Anruf.

    Returns:
        (vorher_fokus: dict|None, vorher_gespraechszeit: datetime|None). Beide None, wenn es
        keinen Vor-Fokus gibt. Die Zeit wird MITGELIEFERT, weil pruefe_anwendung sie fuer die
        ehrliche Datierung braucht und selbst keine DB sehen darf (reine Funktion).

    Join ueber calls.user_id; origin='live'; call_id != dieser Anruf; erste Zeile mit einem
    gesetzten focus_key gewinnt.

    ⚠ ZEIT-ANKER (CLAUDE.md Punkt 26, Befund F-4): sortiert wird nach calls.started_at DESC —
    der SPRECH-Zeit — und NICHT nach rubric_score.created_at. created_at ist die Schreib-Zeit
    der Nachbearbeitung; sie kann in beliebiger Reihenfolge zur Gespraechszeit stehen (ein
    nachtraeglich verarbeiteter alter Anruf steht dort vorn). Wer danach sortiert, legt dem
    Nutzer einen wochenalten Fokus als 'deine Sache aus dem letzten Anruf' vor.
    Der Index dafuer existiert: idx_calls_user_time auf (user_id, started_at).

    ⚠ Nur Zeilen MIT focus_key zaehlen — ein Anruf mit "Nothing flagged this time." ist KEIN
    Vor-Fokus, sondern ein Anruf ohne Auftrag fuers naechste Mal.

    ⚠ KEIN hartes Zeitfenster (Andre-Entscheidung 14.08.): ein alter Fokus wird DATIERT, nicht
    verschwiegen. Begrenzt wird nur der Scan (die juengsten 20 Anrufe), damit die Abfrage nicht
    mit der Historie waechst.

    ⚠ FORCE RLS: nur innerhalb der GUC-Klammer aufrufen. In try/except gekapselt; ein Fehler
    liefert (None, None) und darf die Nachbearbeitung NIE kippen. Der except ruft ZUERST
    db.rollback() (DB-Regel: kein stiller except ohne rollback).
    """
```
Die Filter- und Sortier-Regeln ausgeschrieben, damit der Executor nichts raten muss:
- `Call.user_id == <user_id dieses Anrufs>` und `Call.id != call.id`
- `Call.started_at.isnot(None)` — ein Anruf ohne Sprech-Zeit kann **nicht ehrlich datiert**
  werden und scheidet deshalb aus. ⚠ **Als benannte Grenze ins SUMMARY**, nicht stillschweigend.
- ist `call.started_at` gesetzt, zusaetzlich `Call.started_at < call.started_at` (der Vor-Anruf
  ist der zeitlich fruehere). Ist sie **nicht** gesetzt, entfaellt dieser Filter — dann gewinnt
  schlicht der zuletzt gesprochene Anruf. Auch das gehoert als benannte Grenze ins SUMMARY.
- `order_by(Call.started_at.desc())`, `limit(20)`, dann die erste Zeile mit `focus_key`.
⚠ **Der Executor prueft die Verknuepfung, bevor er sie baut:** `rubric_score` traegt `call_id`
und `conversation_log_id`, aber **keine** `user_id`. Der Weg zum Nutzer laeuft ueber
`calls.user_id`. **Mit `grep -rn` belegen und den gewaehlten Join im SUMMARY nennen** — nicht
raten (Punkt 22).

**B) Route-Kontext.** In `routes/dashboard.py` analog zu `kopfzeile_display`/`fokus_display`
(Plan 05), **mit derselben Form-Garantie** und in **beiden** Zweigen von `_render(_preview_on)`:
```python
                _anw = _obs.get('_anwendung')
                if not isinstance(_anw, dict):
                    _anw = {}
                anwendung_display = {
                    'focus_key': _anw.get('focus_key'),
                    'angewendet': _anw.get('angewendet'),
                    'vorher_count': _anw.get('vorher_count'),
                    'jetzt_count': _anw.get('jetzt_count'),
                    'ist_alt': bool(_anw.get('ist_alt')),
                    'vorher_datum_text': _fokus_datum_text(_anw.get('vorher_datum')),
                }
```

Dazu der kleine Formatierer **in der Anzeige-Schicht** (`routes/dashboard.py`), damit
`fokus_anwendung` rein bleibt und kein Datumsformat kennt:
```python
def _fokus_datum_text(iso_tag):
    """F-4: '2026-05-03' -> 'May 03'. Englisch, weil die Fokus-Texte englisch sind (D-11).
    Bei None oder unlesbarem Wert None — lieber kein Datum als ein falsches."""
```
⚠ **`strftime('%b %d')`** — **kein** `%-d`: die Kurzform ist nicht ueberall vorhanden und
waere ein Absturz statt eines Datums.

**C) 👁 Der sichtbare Abnehmer** in `templates/session_detail.html`, direkt **unter** dem Block
„One thing for next time":
```jinja
      {# METRIK-1 Requirement 8: der Fokus des VOR-Anrufs, gegen DIESES Transkript nachgezaehlt.
         Ohne diese Anzeige waere der Fokus-Schluessel ein toter Ausgang (Bau-Regel 20).
         Englisch, weil der Katalog englisch ist (D-11). Kein hardcoded Hex, keine Inline-Styles. #}
      {% if anwendung_display and anwendung_display.focus_key %}
        <p class="n-observation-text">
          {% if anwendung_display.ist_alt and anwendung_display.vorher_datum_text %}
            {# F-4 (Andre-Entscheidung 14.08.): Ist der Vor-Fokus aelter als FOKUS_ALTER_TAGE,
               wird er DATIERT. "Last time's focus" waere hier schlicht falsch — der Anruf
               dazwischen kann Wochen her sein. #}
            {% if anwendung_display.angewendet %}
              Your focus from {{ anwendung_display.vorher_datum_text }}: applied.
            {% else %}
              Your focus from {{ anwendung_display.vorher_datum_text }}: not yet — it came up {{ anwendung_display.jetzt_count }} times again.
            {% endif %}
          {% else %}
            {% if anwendung_display.angewendet %}
              Last time's focus: applied.
            {% else %}
              Last time's focus: not yet — it came up {{ anwendung_display.jetzt_count }} times again.
            {% endif %}
          {% endif %}
        </p>
      {% endif %}
```
⚠ Gibt es keinen Vor-Fokus, steht dort **nichts** — das ist kein „diesmal nichts"-Fall, sondern
schlicht „es gab keinen Auftrag". Der Unterschied gehoert als Kommentar in den Code.

**D) Tests** (Funktionen 11 bis 14 in `tests/test_fokus_anwendung.py`):
11. `test_anwendung_wird_gespeichert` — `_judge_step` mit gemocktem `run_behavior_judge` und
    abgefangenem `_upsert_rubric_score`; die uebergebenen `observations` tragen `_anwendung`.
12. `test_ohne_vorfokus_steht_der_schluessel_trotzdem` — kein Vor-Anruf → `_anwendung` ist
    vorhanden mit `focus_key is None`.
13. **`test_sortiert_nach_gespraechszeit_nicht_nach_verarbeitungszeit` (F-4, Punkt 26) —
    der Regressions-Test, und er wird ERST ROT GEFAHREN.** Aufbau: zwei fruehere Anrufe
    desselben Nutzers, beide mit gesetztem `focus_key`, **gegenlaeufig** gestellt —
    · **Anruf A:** `calls.started_at` **aelter**, `rubric_score.created_at` **juenger**
    · **Anruf B:** `calls.started_at` **juenger**, `rubric_score.created_at` **aelter**
    Erwartet: `_letzter_fokus_des_nutzers` liefert den Fokus von **B** und dessen Sprech-Zeit.
    ⚠ **ROT vor GRUEN (CLAUDE.md, Regressions-Test bei jedem Bugfix — Pflicht-Artefakt):**
    Der Helfer wird **zuerst** so gebaut, wie die erste Fassung dieses Plans es vorschrieb
    (`order_by(RubricScore.created_at.desc())`). Dagegen laeuft der Test und **muss rot sein**
    (er bekommt A). **Ausgabe verbatim ins SUMMARY.** Erst danach auf `Call.started_at.desc()`
    umstellen. Ohne diesen Rot-Lauf beweist der Test nichts — beide Sortierungen sind gruen,
    solange die Testdaten nicht gegenlaeufig sind.
14. `test_wochenalter_fokus_wird_datiert_angezeigt` (F-4, 👁) — ein Vor-Anruf, dessen
    Sprech-Zeit 30 Tage zurueckliegt; `_anwendung` traegt `ist_alt True` und ein gesetztes
    `vorher_datum`. **Gepaarter Gegen-Fall in derselben Funktion:** ein Vor-Anruf von gestern
    traegt `ist_alt False` — sonst waere „immer datieren" ebenfalls gruen.
Plus **zwei** Render-Tests in `tests/test_session_detail_render.py`:
- `test_anwendung_wird_angezeigt` — Seed mit `_anwendung` (`angewendet=True`, `ist_alt=False`);
  `Last time's focus: applied.` steht im HTML. Gepaarter Existenz-Anker in derselben Funktion:
  `One thing for next time` steht ebenfalls im HTML.
- 👁 `test_alter_fokus_wird_datiert_angezeigt` — Seed mit `ist_alt=True` und
  `vorher_datum='2026-05-03'`; im HTML steht `Your focus from May 03: applied.` und
  **nicht** `Last time's focus`. **Das ist die Zusage aus F-4 am Bildschirm, nicht im
  Docstring.**

**E) Das Schild von `observations_jsonb` nachziehen (Punkt 23) + Migration `0042`.**

⚠ **Dieser Schritt ist Pflicht, nicht Kuer.** Dieser Task fuehrt mit `_anwendung` einen
**vierten** Unterstrich-Schluessel in `observations_jsonb` ein. Das Schild aus Plan 07
(`_SCHILD_RUBRIC_OBSERVATIONS`, Migration `0041`) nennt nur **drei**. Ein Schild, das nicht alle
Schluessel abdeckt, ist laut Punkt 23 **nutzlos — schlimmer als kein Schild**, weil es eine
falsche Vollstaendigkeit vortaeuscht. Der SCHILD-Waechter (`tests/test_schild_guard.py`) faengt
das **nicht**: er prueft nur Vorhandensein und Laenge >= 10, nicht Aktualitaet.

**E1) `database/models.py` — `_SCHILD_RUBRIC_OBSERVATIONS` auf v2.**

Den bestehenden Text (Plan 07) **um genau einen Satz** zum vierten Schluessel erweitern und die
Leser-/Schreiber-Liste nachziehen. Vorschlag, an den Plan-07-Text angehaengt (⚠ **Doppelpunkt-
Falle**: kein Doppelpunkt ohne vorangehendes Wortzeichen, kein Prozentzeichen, keine nackte
Zeilenzahl):

```
... _anwendung traegt ab METRIK-1 Requirement 8 das Ergebnis der Anwendungs-Pruefung mit focus_key, vorher_count, jetzt_count und angewendet; sie zaehlt den Fokus des VOR-Anrufs im TRANSKRIPT dieses Anrufs nach, vollstaendig ohne Modell (services/fokus_anwendung.py), und focus_key NULL bedeutet es gab keinen Vor-Fokus, nicht die Regel wurde erfuellt. Status lebt. Schreibt services/judge_runner.py und services/slow_lane.py; liest routes/dashboard.py und templates/session_detail.html.
```
⚠ Der Satz „Status lebt. Schreibt … liest …" steht danach **genau einmal** am Ende — der aus
Plan 07 wird **ersetzt**, nicht gedoppelt.

**E2) Migration `alembic/versions/0042_metrik1_anwendung_schild.py`.**

`revision = '0042'`, `down_revision = '0041'`. Bauform **exakt wie `0041`/`0040`**:
- Modul-Docstring mit den drei Pflicht-Bestandteilen aus `0039`: **„WARUM ALEMBIC UND NICHT
  `_migrate()`"** (`app._migrate()` returnt auf Postgres frueh — wirkungslos **und** Alt-Muster,
  Punkt 29) · der **beim Planen belegte Prod-Kopf** (`0041` nach Plan 07) · die
  **Ausroll-Reihenfolge** (reine `COMMENT ON`-Migration, keine Spalte, kein Typ → **Migration
  zuerst, dann `deploy.sh production`**, damit der Schild-Waechter gegen den gesetzten Kommentar
  laeuft; die ZEITSTEMPEL-1-Regel „Migration vor Deploy" gilt nur fuer **nullable Spalten** und
  wird hier nicht blind uebertragen).
- Der `_comment(objekt, text)`-Helfer **verbatim** aus `0039:52-73`, **inklusive** des vollen
  Docstrings.
- Fuer `downgrade()` den Plan-07-Text als Konstante `_SCHILD_OBSERVATIONS_V1` **verbatim**
  mitfuehren (er steht in `database/models.py` **vor** dieser Aenderung).

```python
def upgrade():
    _comment('COLUMN rubric_score.observations_jsonb', _SCHILD_RUBRIC_OBSERVATIONS)


def downgrade():
    _comment('COLUMN rubric_score.observations_jsonb', _SCHILD_OBSERVATIONS_V1)
```

⚠ **Der Executor zieht `bash scripts/inspect.sh migrations` erneut** und bricht ab, wenn dort
nicht `0041` steht.
⚠ **Kein `op.execute(`** — siehe „## Punkt 23 — Abweichung von CLAUDE.md" am Ende dieses Plans.
  </action>

  <verify>
    <automated>grep -c "pruefe_anwendung(" services/slow_lane.py</automated>
    <automated>grep -c "anwendung_display" routes/dashboard.py templates/session_detail.html</automated>
    <automated>grep -c "op.execute(" alembic/versions/0042_metrik1_anwendung_schild.py</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "pruefe_anwendung(" services/slow_lane.py` liefert **1** — **damit hat
  `services/fokus_anwendung.py` seinen Produktiv-Aufrufer.**
- `grep -c "def _letzter_fokus_des_nutzers" services/slow_lane.py` liefert **1**.
- **F-4 / Punkt 26 — sortiert wird nach der SPRECH-Zeit. Abwesenheits-Anker auf die
  Code-FORM, mit gepaartem Existenz-Anker:**
  `grep -c "RubricScore.created_at.desc()" services/slow_lane.py` liefert **0**;
  `grep -c "Call.started_at.desc()" services/slow_lane.py` liefert **1**.
  ⚠ Der Beweis ist **nicht** der grep, sondern Test 13 mit seinem ROT-Lauf — der grep faengt
  nur die Rueckkehr des alten Musters.
- **Die Datierung erreicht wirklich den Bildschirm:**
  `grep -c "Your focus from" templates/session_detail.html` liefert **1**; gepaarter
  Existenz-Anker: `grep -c "Last time's focus" templates/session_detail.html` liefert **2**
  (der undatierte Zweig lebt weiter).
- `grep -c "def _fokus_datum_text" routes/dashboard.py` liefert **1**;
  `grep -c "%-d" routes/dashboard.py` liefert **0** (keine nicht-portable Kurzform).
- **Bau-Regel 20 — der Fokus-Schluessel hat einen sichtbaren Abnehmer:**
  `grep -c "anwendung_display" templates/session_detail.html` liefert **>= 2**;
  `grep -c "anwendung_display" routes/dashboard.py` liefert **>= 4** (Initialisierung,
  Befuellung, except-Ruecksetzung, Kontext-Uebergabe).
- **Beide Render-Zweige versorgt:**
  `grep -c "anwendung_display=(anwendung_display if _preview_on else" routes/dashboard.py`
  liefert **1**.
- **Kein Inline-Style, keine Hex-Farbe:** in den neu eingefuegten Template-Zeilen gibt es
  **kein** `style=` und **kein** `#`; gepaarter Existenz-Anker:
  `grep -c "n-observation-text" templates/session_detail.html` liefert **>= 3**.
- Der Join-Weg (`calls.user_id`) ist per `grep -rn` belegt und im SUMMARY genannt.
- `grep -c "^def test_" tests/test_fokus_anwendung.py` liefert **14**.
- **Der ROT-Lauf von `test_sortiert_nach_gespraechszeit_nicht_nach_verarbeitungszeit` gegen die
  `created_at`-Sortierung steht verbatim im SUMMARY**, zusammen mit dem gruenen Lauf danach.
- **Punkt 23 — Schild und Migration existieren wirklich (Schritt E):**
  - `grep -c "comment=_SCHILD_RUBRIC_OBSERVATIONS" database/models.py` liefert **1**.
  - **Schild-Zeichengleichheit ORM/DDL:**
    `grep -c "_anwendung traegt ab METRIK-1 Requirement 8" database/models.py` liefert **1**
    **und** `grep -c "_anwendung traegt ab METRIK-1 Requirement 8" alembic/versions/0042_metrik1_anwendung_schild.py`
    liefert **1**.
  - **Alle VIER Unterstrich-Schluessel stehen im Schild:**
    `grep -c "_compliance traegt das Sicherheits-Hard-Gate" database/models.py` liefert **1**;
    `grep -c "_kopfzeile traegt ab METRIK-1" database/models.py` liefert **1**;
    `grep -c "_fokus traegt die eine Sache" database/models.py` liefert **1**;
    `grep -c "_anwendung traegt ab METRIK-1" database/models.py` liefert **1**.
  - `grep -c "down_revision = '0041'" alembic/versions/0042_metrik1_anwendung_schild.py`
    liefert **1**, und `grep -c "revision = '0042'" alembic/versions/0042_metrik1_anwendung_schild.py`
    liefert **>= 1**.
  - **Doppelpunkt-Falle auf beiden Ebenen entschaerft:**
    `grep -c "exec_driver_sql" alembic/versions/0042_metrik1_anwendung_schild.py` liefert **1**;
    `grep -c "op.execute(" alembic/versions/0042_metrik1_anwendung_schild.py` liefert **0**.
  - `grep -c "_SCHILD_OBSERVATIONS_V1" alembic/versions/0042_metrik1_anwendung_schild.py`
    liefert **>= 2** (Definition + Verwendung in `downgrade`); `grep -c "def downgrade" …`
    liefert **1**.
  - Die erneut gezogene `inspect.sh migrations`-Ausgabe (**erwartet `0041`**, vor der Migration)
    steht im SUMMARY.
  </acceptance_criteria>

  <done>
Der Fokus-Schluessel hat einen Abnehmer: die Anwendungs-Pruefung laeuft, wird gespeichert und ist
auf der Auswertungsseite sichtbar — deterministisch und ohne KI. Und das Schild von
`observations_jsonb` nennt **alle vier** Unterstrich-Schluessel, in derselben Aenderung, die den
vierten einfuehrt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ausrollen + Phasen-Abschluss-Abnahme</name>

  <read_first>
- Alle bisherigen SUMMARYs der Phase (`08.23.2.METRIK-1-01-SUMMARY.md` bis `-09-SUMMARY.md`).
  </read_first>

  <what-built>
Die Anwendungs-Pruefung als reine Code-Schicht, ihr Anschluss in der Slow Lane, die sichtbare
Zeile auf der Auswertungsseite und ein Waechter, der den Fokus dauerhaft aus dem
Bewertungs-Auftrag heraushaelt.
  </what-built>

  <how-to-verify>
**Schritt 0 — Prod-Kopf pruefen, dann Migration `0042` ausfuehren.**
```
git status && git push origin main
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
Erwartet: `0041`. Sonst **STOPP**.
Dann `0042` ausfuehren — **als `postgres`, mit gesetztem `DATABASE_URL`** (sonst faellt Alembic
auf SQLite zurueck; Lehre aus MESSGERAETE-1). Danach:
```
ssh ... 'bash scripts/inspect.sh migrations'          # erwartet: 0042
ssh ... 'bash scripts/inspect.sh schilder rubric_score'
```
Der `schilder`-Auszug geht **verbatim** ins SUMMARY; er muss **alle vier** Unterstrich-Schluessel
nennen.

**Schritt 1 — Ausrollen (⚠ fuer Agenten gesperrt, Andre fuehrt aus):**
```
bash deploy.sh production
```

**Schritt 2 — Der Zwei-Anruf-Test (die eigentliche Abnahme von Requirement 8).**

⚠ **BEKANNTE GRENZE FUER DIESE DREI ANRUFE (F-5) — bitte vor dem ersten Anruf lesen:**
Die Schwaerzung ist **auf Deutsch eingestellt** und **duerfte englische Namen schlechter
erwischen**. ⚠ **Das ist ausdruecklich eine VERMUTUNG (Fable), keine Messung** — sie wurde
nicht nachgeprueft. Fehlerrichtung: ein englischer Personen- oder Firmenname koennte
ungeschwaerzt im gespeicherten Transkript landen.
**➡️ Deshalb: KEINE echten Namen in den Testanrufen** — weder Personen noch Firmen, weder
gesprochen noch im Profil. Erfundene Namen verwenden.
**Die Grenze gehoert samt dieser Kennzeichnung als Vermutung ins SUMMARY** — sie ist ein
Kandidat fuer eine eigene Messung, nicht fuer stilles Vertrauen.
1. **Anruf 1 auf Englisch**, der ein Kriterium sicher ausloest (z. B. fuenfmal „we provide").
   Auswertungsseite oeffnen: unter „One thing for next time" steht der Satz mit der Zahl 5.
2. **Anruf 2 auf Englisch**, denselben Fehler **vermeiden** (hoechstens zweimal „we provide").
   Auswertungsseite oeffnen: darunter steht **„Last time's focus: applied."**
3. **Anruf 3 auf Englisch**, den Fehler **wiederholen** (viermal oder mehr).
   Erwartet: **„Last time's focus: not yet — it came up N times again."** mit der richtigen Zahl.
4. 👁 **Die Datierung (F-4) im echten Betrieb** — ohne 30 Tage Wartezeit nicht am lebenden
   Anruf pruefbar. **Beleg ist deshalb der Render-Test `test_alter_fokus_wird_datiert_angezeigt`
   plus Test 13/14** aus Task 2; **beide Ausgaben ins SUMMARY**, mit dem ausdruecklichen
   Vermerk, dass dieser eine Punkt **nicht** durch die drei Testanrufe abgenommen ist.

**Schritt 3 — Determinismus (SPEC Requirement 8 Acceptance).**
Der Bewerter laeuft mit `temperature=0`, aber das ist **nicht** der Beleg — der Determinismus
der Anwendungs-Pruefung liegt daran, dass **kein LLM im Pfad** ist. Beleg: die Tests 6 und 7 aus
Task 1 (`test_deterministisch`, `test_kein_llm_import`) plus der grep-Anker auf `anthropic`.
**Diese drei Belege gehoeren zusammen ins SUMMARY** — die Zusage „deterministisch" wird sonst mit
„temperature=0" verwechselt, und das waere eine andere, schwaechere Aussage.

**Schritt 4 — Der Waechter ist im Deploy-Tor.**
Die Tor-Ausgabe zeigt `tests/test_judge_prompt_fokus_guard.py` als **gelaufen**. Der **ROT-Lauf**
aus Task 1 steht verbatim im SUMMARY.

**Schritt 5 — Phasen-Abschluss-Bilanz.** Der Executor traegt im SUMMARY zusammen:
- Alle **neun Requirements** mit dem Plan, der sie erfuellt hat.
- Die **vollstaendige Lesestellen-Liste** (die sieben aus D-12 plus die beim Planen und in der
  Plan-Pruefung gefundenen — inklusive Coach-Report-Kachel und Coach-Report-Fallbacksatz aus
  Plan 09).
- **Die Aufloesung des frueheren SPEC-Widerspruchs `:517` — im CODE, nicht in Prosa.**
  SPEC `:517` sagt *„ein Anruf mit ausschliesslich `word_count IS NULL` wird nicht wegen
  Wortmangel abgelehnt"*. Bis zum 13.08. wurde er es faktisch doch: die damalige
  Abschnitts-Bedingung wies ihn ab, mit **demselben** `reason`-String. **Aufgeloest war das nur
  im Plan-Text.** Zwei unabhaengige Gegenleser haben genau das beanstandet (**Gemini B-3 =
  GSD-Plan-Checker W-5** — der belastbarste Befund des Cross-AI-Durchgangs).
  **➡️ Seit dem 14.08. ist der Widerspruch weggebaut statt wegerklaert:**
  (1) die **Abschnitts-Bedingung ist ersatzlos gestrichen** — abgelehnt wird nur noch bei
  weniger als zwanzig gesprochenen Berater-Woertern; (2) **„unbekannte Wortzahl" ist ein
  eigener Programm-Zweig mit eigenem String** (`_TOR_ZWEIG_WORTZAHL_UNBEKANNT` /
  `'wortzahl_unbekannt_durchgelassen'`), der **durchlaesst** (fail-open).
  **Beleg: `tests/test_substanz_tor.py::test_nur_null_wortzahlen_laeuft_ueber_den_eigenen_unbekannt_zweig`**
  (Plan 02, Testfunktion 4) — ein Verhaltens-Test auf den **Rueckgabewert**, kein
  Docstring-Anker.
  ⚠ **Mit in die Bilanz:** Die **EWB-Kippe** aus SPEC NACHTRAG 2 ④ ist mit der
  Abschnitts-Bedingung **ersatzlos entfallen** (die Knopf-Zeile traegt `word_count = NULL` und
  zaehlt bei der Wortsumme ohnehin nicht mit). Der **EWB-FILTER** fuer Bewerter-Auftrag und
  Pruef-Korpus (D-04/D-05, Plan 01) ist davon **unberuehrt** — anderer Grund, andere Stelle.
  Wer beides verwechselt, baut den Filter zurueck.
- Alle **bekannten Restluecken mit Ort und Termin** (siehe `<output>`).
- **Die Fundstellen-Bilanz der Plan-Pruefung R2 — fuenf Stellen, die nirgends benannt sind**
  (`<output>`, Punkte 11-15), jede mit „geprueft, bleibt, weil …". ⚠ **Nur eintragen, nichts
  davon bauen.** Unbenannt heisst: sie kommen beim naechsten Durchgang als Ueberbleibsel wieder
  hoch.
  </how-to-verify>

  <acceptance_criteria>
- `inspect.sh migrations` liefert **0042**; der `inspect.sh schilder rubric_score`-Auszug steht
  **verbatim** im SUMMARY und nennt **alle vier** Unterstrich-Schluessel.
- `bash deploy.sh production` gruen; Tor-Ausgabe **verbatim** im SUMMARY, inklusive des
  Waechters.
- Die drei Anrufe aus Schritt 2 sind gefahren; fuer jeden stehen `conv_id`, der angezeigte
  Fokus-Satz und die Anwendungs-Zeile im SUMMARY.
- Der **ROT-Lauf des Waechters** steht verbatim im SUMMARY, mit dem absichtlich eingebauten
  Verstoss und dem Beleg seiner Ruecknahme.
- Die drei Determinismus-Belege aus Schritt 3 stehen zusammen im SUMMARY, mit dem
  ausdruecklichen Hinweis, dass `temperature=0` **nicht** der Beleg ist.
- Die Phasen-Abschluss-Bilanz aus Schritt 5 ist vollstaendig.
  </acceptance_criteria>

  <resume-signal>
Andre antwortet „abgenommen" mit den drei `conv_id`s, oder beschreibt, was abweicht.
  </resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21 + Punkt 26)

### Angefasste Tabellen

- `rubric_score` (**lesen**: `observations_jsonb` des Vor-Anrufs; **schreiben**:
  `observations_jsonb['_anwendung']`) — **FORCE RLS**
- `calls` (**lesen**: `user_id` fuer den Join **und `started_at` als ZEIT-ANKER**) — keine RLS
- `transcript_segments` (**lesen**, bereits in Plan 05 geladen) — keine RLS

### inspect.sh-Beleg

Der `rubric_score`-Auszug steht **verbatim in Plan 01, Sektion 5** (Zug vom 2026-08-13).
Fuer diesen Plan sind drei Zeilen daraus entscheidend und werden hier ausdruecklich zitiert:
```
 call_id               | uuid                        |           |          |
 origin                | character varying(16)       |           | not null |
 created_at            | timestamp without time zone |           |          |
Indexes:
    "ux_rubric_score_live_call_id" UNIQUE, btree (call_id) WHERE origin::text = 'live'::text
Policies (forced row security enabled):
    POLICY "tenant_isolation"
      USING ((tenant_id = (NULLIF(current_setting('app.tenant_id'::text, true), ''::text))::uuid))
```
⚠ **`rubric_score` traegt KEINE `user_id`.** Der Weg zum Nutzer laeuft ueber `calls.user_id` —
der Executor belegt den Join per `grep -rn` und nennt ihn im SUMMARY (Punkt 22).

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg-thread) + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| Vor-Anruf-`_fokus` | gelesen in `_letzter_fokus_des_nutzers` | `rubric_score.observations_jsonb['_fokus']` (jsonb NOT NULL) | **Batch** (Slow Lane des VORIGEN Anrufs). **Vorauslaufen ist real moeglich:** wird der aktuelle Anruf verarbeitet, bevor die Slow Lane des vorigen fertig war, findet der Leser **keinen** Vor-Fokus und meldet „kein Vor-Fokus". **Fehlerrichtung bewusst:** lieber keine Anwendungs-Zeile als eine falsche. ⚠ Ein Fan-In-Flag wie `audio_health_resolved` waere hier **ueberzogen** — die Anwendungs-Pruefung ist eine Zusatz-Anzeige, kein Tor. **Als bekannte Grenze ins SUMMARY**, nicht stillschweigend. | ✓ inspect.sh + Bestandsmechanik |
| `calls.user_id` | gelesen fuer den Join | DB-Spalte, NOT NULL | zum Anruf-Beginn geschrieben, lange vor der Slow Lane | ✓ |
| **`calls.started_at`** (Sortier-Schluessel) | gelesen in `_letzter_fokus_des_nutzers` | DB-Spalte `calls.started_at` (timestamptz, **nullable**), Index `idx_calls_user_time` | **Zum Anruf-Beginn geschrieben — live, lange vor jeder Nachbearbeitung. Der Leser kann nicht vorauslaufen.** ⚠ **ZEIT-ANKER (CLAUDE.md Punkt 26): sortiert wird nach dieser SPRECH-Zeit, ausdruecklich NICHT nach `rubric_score.created_at`** (der DB-Schreib-Zeit der Nachbearbeitung). Befund F-4: die `created_at`-Sortierung legt dem Nutzer einen wochenalten Fokus als „letzten Anruf" vor. ⚠ Die Spalte ist **nullable** — Zeilen ohne Sprech-Zeit scheiden aus (benannte Grenze, s. Task 2 A). | ✓ `models.py:712, :769` |
| `observations_jsonb['_anwendung']` | geschrieben in `_upsert_rubric_score` | `rubric_score.observations_jsonb` | Batch, unter der M-4-GUC-Klammer. Der Anzeige-Leser laeuft danach oder findet keine Zeile (bestehender Zweig „(d) row-absent"). | ✓ |
| **GUC-Klammer** | `_judge_step` laeuft innerhalb | — | ⚠ **Ohne gesetzte GUC liefert `rubric_score` STILL 0 Zeilen statt eines Fehlers.** Dann waere **jeder** Anruf „erster Anruf ohne Vor-Fokus" — ein falsches Ergebnis ohne jede Fehlermeldung. Deshalb liegt der Lesezugriff ausschliesslich in `_judge_step`. | ✓ `slow_lane.py:640-678` |

| `observations_jsonb`-**Schild** | Migration `0042` (**Task 2, Schritt E**) | `pg_description` | Migration vor Deploy (Schritt 0 der Abnahme) | ✓ Bauform aus `0041` |

**Es wird KEINE Spalte angelegt oder geaendert.**
⚠ **Punkt 23:** Das Schild von `observations_jsonb` (Migration `0041`, Plan 07) nennt drei
Unterstrich-Schluessel. Mit `_anwendung` kommt ein **vierter** dazu — **das Schild wird in
diesem Plan nachgezogen**: **Task 2, Schritt E** baut `_SCHILD_RUBRIC_OBSERVATIONS` v2 in
`database/models.py` **und** die Migration `alembic/versions/0042_metrik1_anwendung_schild.py`
(gleiche Bauform wie `0040`/`0041`, `_comment` mit `exec_driver_sql`, Text zeichengleich).
⚠ **In der ersten Fassung dieses Plans stand `0042` ausschliesslich in Prosa** — kein Task hatte
die Datei in `<files>`, keinen Aktions-Schritt und kein Abnahme-Kriterium, waehrend
`<verification>` sie verlangte. Das ist mit dieser Ueberarbeitung behoben; faellt Schritt E aus,
ist Punkt 23 verletzt **und** die `<verification>` unerfuellbar.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

## Verbindungs-Karte (Punkt 22)

| Neuer Identifier | Kandidat schon vergeben? | Beleg (vom Executor erneut zu ziehen) |
|---|---|---|
| `services/fokus_anwendung.py` | nein | `grep -rn "fokus_anwendung" .` → **0** |
| `pruefe_anwendung` | nein | `grep -rn "pruefe_anwendung" .` → **0** |
| `prueft_regel` | nein | `grep -rn "prueft_regel" .` → **0** |
| `_letzter_fokus_des_nutzers` | nein | `grep -rn "_letzter_fokus" .` → **0** |
| `FOKUS_ALTER_TAGE` (F-4) | nein | `grep -rn "FOKUS_ALTER_TAGE" .` → **0** |
| `_fokus_datum_text` (F-4, Anzeige-Schicht) | nein | `grep -rn "_fokus_datum_text" .` → **0** |
| Sortier-Schluessel `calls.started_at` | **vorhanden, kein neuer Name** | `database/models.py:712` + Index `:769`; **der Executor belegt beides per grep, bevor er sortiert** (Punkt 22) |
| `observations_jsonb`-Schluessel `_anwendung` | frei | belegt sind `_compliance`, `_kopfzeile`, `_fokus` |
| `anwendung_display` | frei | `grep -rn "anwendung_display" .` → **0** |
| Migrations-Revision `0042` | frei | `ls alembic/versions/ \| sort \| tail -3` → Kopf beim Planen `0039`; nach Plan 02 `0040`, nach Plan 07 `0041`. Der Executor zieht `inspect.sh migrations` erneut und bricht bei Abweichung ab |
| `_SCHILD_OBSERVATIONS_V1` | nein | `grep -rn "_SCHILD_OBSERVATIONS_V1" .` → **0** (Plan 07 benutzt `_SCHILD_OBSERVATIONS_ALT` fuer seinen eigenen Rueckweg — **anderer Name, kein Konflikt**) |
| Join-Weg zum Nutzer | **`calls.user_id`** | `rubric_score` hat **keine** `user_id` (inspect.sh oben) — der Executor belegt den Join per grep, statt ihn anzunehmen |

## Punkt 23 — Abweichung von CLAUDE.md, bewusst und gemeldet (I-1)

**CLAUDE.md Punkt 23** nennt unter „Migrations-Stil" woertlich
`op.execute("COMMENT ON TABLE/COLUMN ... IS '...'")` als „hauseigenes Muster aller Migrationen".
**Die Migrationen `0040`, `0041` und `0042` dieser Phase weichen davon bewusst ab:** sie
benutzen den `_comment()`-Helfer mit `exec_driver_sql` und **verbieten** `op.execute(` per
Abnahme-Anker.

**Warum die Abweichung richtig ist:** Ein `COMMENT ON`-Text mit einem Doppelpunkt wird von der
Parameter-Ersetzung als benannter Bindeparameter gelesen — die **Doppelpunkt-Falle**, an der
`0039` (ZEITSTEMPEL-1) haengengeblieben ist. `exec_driver_sql` umgeht die Parameter-Ersetzung
vollstaendig. Die Schild-Texte dieser Phase sind lange Prosa-Saetze; die Wahrscheinlichkeit
eines Doppelpunkts ist hoch.

**Was daraus folgt — nicht fuer diesen Plan, sondern fuer die Regel:** Die Regel sagt weiter das
Gegenteil dessen, was wir tun. Das gehoert **nachgezogen**, aber **nicht von einem Plan-Agenten
und nicht in diesem Plan**. Der Nachtrag steht im Fragen-Kanal
(`.planning/DIALOG-GSD-CLAUDIAN.md`, Eintrag vom 2026-08-13) — **`CLAUDE.md` wird hier nicht
angefasst**.

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Der Code zaehlt selbst nach, ob die eine Sache umgesetzt wurde — niemand
   liest zwei Transkripte nebeneinander.
2. **Nachvollziehen:** Das Ergebnis steht mit Vorher- und Jetzt-Zahl in `observations_jsonb` und
   ist damit spaeter zu einer Serie zusammenzaehlbar.
3. **Eingreifen koennen:** Die Entscheidungsregel ist eine Zeile („die Regel feuert nicht mehr"),
   die Schwellen sind Modul-Konstanten des Katalogs.
4. **Marge:** Der Kreislauf schliesst sich — aus einer Beobachtung wird ein Auftrag und aus dem
   Auftrag eine belegte Rueckmeldung. Das ist der Teil, fuer den man sonst einen Coach braeuchte.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vor-Anruf-Daten (FORCE RLS) → aktueller Anruf | Ein Fokus aus einem fremden Mandanten waere ein Datenleck |
| Anwendungs-Ergebnis → Bildschirm | Ein geschoentes „applied" waere die gefaellige Pruefung, die ausgeschlossen sein soll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-METRIK1-10-01 | Information Disclosure | Vor-Fokus aus fremdem Mandanten | **mitigate** | Der Lesezugriff liegt **innerhalb** der M-4-GUC-Klammer; die FORCE-RLS-Policy `tenant_isolation` filtert auf `app.tenant_id`. Zusaetzlich filtert der Join auf `calls.user_id`. |
| T-METRIK1-10-02 | Repudiation | gefaellige „applied"-Meldung | **mitigate** | Kein LLM im Pfad (grep-Anker + Laufzeit-Test); die Entscheidungsregel ist dieselbe Ja/Nein-Schwelle wie im Katalog; eine Teilverbesserung zaehlt ausdruecklich **nicht** als angewendet (Test 4). |
| T-METRIK1-10-03 | Tampering | Fokus rutscht in den Bewertungs-Auftrag | **mitigate** | Waechter `tests/test_judge_prompt_fokus_guard.py` mit ROT-Beleg, Mindest-Soll und benannten Restluecken (Punkt 31); zweite Schicht ist der Verhaltens-Test aus Plan 05. |
| T-METRIK1-10-04 | Denial of Service | RLS-Fehler kippt die Nachbearbeitung | **mitigate** | `_letzter_fokus_des_nutzers` ist in try/except gekapselt und liefert bei jedem Zweifel `None`. ⚠ **Kein stiller `except` ohne `rollback`** — die DB-Regel aus CLAUDE.md gilt: der `except` auf einer PG-Session ruft zuerst `db.rollback()`, sonst vergiftet ein verschluckter Fehler die laufende Transaktion (`InFailedSqlTransaction`, auf SQLite unsichtbar). |
</threat_model>

<verification>
- Alle `<acceptance_criteria>` der drei Tasks sind erfuellt.
- Der ROT-Lauf des Waechters steht **verbatim** im SUMMARY.
- `git diff tests/test_fokus_katalog.py` ist leer (die Plan-04-Tests blieben unangetastet gruen).
- Migration `0042` (Schild-Nachzug) ist ausgefuehrt; `inspect.sh migrations` liefert **0042**.
- Deploy-Tor gruen; die drei Test-Anrufe sind belegt.
</verification>

<success_criteria>
- Der Fokus des Vor-Anrufs wird im Transkript des Folge-Anrufs nachgezaehlt — ohne KI,
  deterministisch, und mit einer Entscheidungsregel, die eine Teilverbesserung **nicht** als
  Erfolg meldet.
- Der Bewertungs-Auftrag enthaelt weder Fokus-Schluessel noch Anruf-Ausgang — von einem Waechter
  mit benannten Grenzen erzwungen, nicht von Sorgfalt.
- Der Fokus-Schluessel hat einen sichtbaren Abnehmer; es gibt keinen toten Ausgang.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-10-SUMMARY.md` anlegen.
**Pflicht-Inhalte:**
- ROT-Lauf des Waechters **verbatim**, inklusive des absichtlich eingebauten Verstosses.
- Der belegte Join-Weg zum Nutzer.
- **Der ROT-Lauf von `test_sortiert_nach_gespraechszeit_nicht_nach_verarbeitungszeit`
  verbatim** (gegen die `created_at`-Sortierung), mit dem gruenen Lauf danach — und der
  ausdrueckliche Vermerk, dass es sich um **CLAUDE.md Punkt 26** handelt (Sprech-/Ereignis-Zeit
  statt DB-Schreib-Zeit).
- 👁 **Die ehrliche Datierung** mit dem angezeigten Wortlaut (`Your focus from May 03: …`) und
  dem Vermerk, dass sie **nicht** durch die drei Testanrufe abgenommen ist, sondern durch die
  Render-Tests.
- **Die bekannte Grenze F-5** — die Schwaerzung ist auf Deutsch eingestellt und **duerfte**
  englische Namen schlechter erwischen (**ausdruecklich eine Vermutung, keine Messung**);
  in den drei Testanrufen wurden deshalb **keine echten Namen** verwendet.
- **Die zwei benannten Grenzen des Lade-Helfers:** ein Anruf ohne `started_at` scheidet als
  Vor-Fokus aus, und ein aktueller Anruf ohne `started_at` bekommt schlicht den zuletzt
  gesprochenen Vor-Anruf.
- Die drei Test-Anrufe mit `conv_id` und angezeigten Zeilen.
- Die drei Determinismus-Belege, ausdruecklich abgegrenzt von `temperature=0`.
- **Die Phasen-Abschluss-Bilanz:** neun Requirements mit ihrem Plan, die vollstaendige
  Lesestellen-Liste, das Deploy-Protokoll.
- **Die Aufloesung des SPEC-Widerspruchs `:517` vs. D-22 (b)** im Wortlaut aus Schritt 5 — sie
  gehoert in die Bilanz, nicht nur in einen Test-Docstring.
- **Bekannte Restluecken, jede mit Ort und Termin (Ablage-Regel §3b):**
  1. **„Ist es die WICHTIGSTE eine Sache?" bleibt ungeprueft.** Termin: sobald der
     Fokus-Kreislauf laeuft und rund 100 echte Anrufe vorliegen.
  2. **Die Tor-Grenze ist aus dem Zweck hergeleitet, nicht kalibriert.** Termin: Nachjustierung
     nach rund 100 echten Anrufen, auf Basis der in Requirement 2 mitgeschriebenen Messwerte.
  3. **Der Bewerter-Auftrag bleibt auf Deutsch** — ein US-Verkaeufer bekaeme deutsche
     Coaching-Texte. Termin: Englisch-Umbau als eigene Phase.
  4. **Pflicht-Selbsttest, sobald genug echte Anrufe da sind:** schlaegt eine Regel
     ueberdurchschnittlich oft bei ERFOLGREICHEN Anrufen an, ist sie invertiert.
  5. **`[nicht gespeichert]`-Zeilen** tragen echte Wortzahlen bei Platzhalter-Text — ein stark
     geschwaerzter Anruf passiert das Substanz-Tor mit 50 „Woertern", aus denen kein Zitat
     ziehbar ist. Faengt hinten der Zweig „Nicht genug zum Bewerten." ab.
  6. **Die Firmenname-Teilregel schlaegt selten an**, weil der Firmenname im gespeicherten
     Transkript anonymisiert als `[ORG_X]` steht.
  7. **`ICH_SCHWELLE` und `BUZZWORD_SCHWELLE` sind gesetzt, nicht belegt.**
  8. **Der Vor-Fokus kann fehlen**, wenn die Slow Lane des vorigen Anrufs noch nicht durch war.
     Bewusste Fehlerrichtung: lieber keine Anwendungs-Zeile als eine falsche.
  9. **Der Redeanteil-Widerspruch bleibt sichtbar:** das Anruf-Fenster zeigt weiter eine
     Redeanteil-Kachel (im Kaltakquise-Modus baubedingt 100 %), und das Wochen-Dashboard coacht
     weiter „Redeanteil Ziel: unter 40 %". **Vertagt ist NAMENTLICH der Redeanteil-Zweig in
     `routes/dashboard.py::_generate_improvement_tip`, nicht ein Zeilenbereich.**
     **Termin: Roadmap-Punkt 4.0.2.**
     ⛔ **Warum das hier so genau steht (Cross-AI-Fund 14.08.):** SPEC NACHTRAG 2 ⑨ hatte diese
     Vertagung urspruenglich als **Zeilenbereich** `dashboard.py:283-300` geschrieben — und
     damit still einen **zweiten** Befund mitverschluckt, den **Kaufbereitschafts**-Zweig
     derselben Funktion, der zufaellig in denselben Zeilen liegt. **Ein Zeilenbereich ist als
     Anker fuer eine Vertagung genauso zu grob wie fuer eine Abnahme.**
     **Die beiden Zweige sind NICHT dasselbe und duerfen nicht verwechselt werden:**
     · **Redeanteil-Zweig** → vertagt auf 4.0.2 (dieser Punkt).
     · **Kaufbereitschafts-Zweig** derselben Funktion → **ebenfalls vertagt auf 4.0.2**, und
       zwar als **toter Ausgang**: `improvement_tip` wird zwar an `render_template` uebergeben,
       aber `grep -rn "improvement_tip" templates/` liefert **0** Treffer — der Tipp erreicht
       heute keinen Bildschirm. **Nicht** verwechseln mit dem `typ`-Filter, der die
       Kaufbereitschafts-Mittelung speist: **der wird in DIESER Phase korrigiert** (Plan 09,
       Task 3).
  10. **`_calc_coaching_score`** hat nur Test-Aufrufer — Vorschlag fuer die
      ERZEUGER-Folgephase.
  11. **R-1 — `services/coaching_service.py:18` (`POSTCALL_PROMPT`), gefuettert `:78`.** Speist
      `kb_start`/`kb_end` **und** `redeanteil_berater/kunde` in den Lernkarten-LLM — **exakt die
      Struktur der elften Lesestelle** (`:336`): eine Zutat, aus der ein Modell Prosa baut, die
      der Nutzer nach dem Anruf liest. **Bleibt**, weil die Ausgabe Formulierungs-Vorschlaege
      sind und keine Gesamtnote (D-19, nicht im Scope). ⚠ **Benannte Grenze:** kein Anker faengt,
      wenn das Modell schreibt „deine Kaufbereitschaft ging von 30 auf 45". Termin: die
      ERZEUGER-Folgephase (`kb_*`).
      ⛔ **OFFENE WERTUNGSFRAGE — Geminis Gegenposition gehoert hierher, nicht in eine Fussnote
      (Cross-AI 14.08.):** Gemini stuft dieselbe Stelle als **HIGH / „muss weg"** ein. Sein
      Argument aus dem Regeltext: es ist **exakt dieselbe Struktur** wie die elfte Lesestelle
      (`coaching_service.py:336`), die wir sehr wohl entfernen — eine Zutat, aus der ein Modell
      Prosa baut, die der Nutzer nach dem Anruf liest. GSD haelt dagegen mit einer
      Nutzen-Abwaegung: D-19 verlangt „kein LIVE-Anruf zeigt eine **Gesamtnote**", und
      Lernkarten sind Formulierungs-Vorschlaege, keine Note.
      **Beide Lesarten sind vertretbar; die Frage ist NICHT entschieden.** Sie wird hier
      **ehrlich als offen protokolliert** und nicht einseitig weggeschrieben. **Ort:** diese
      Bilanz. **Termin:** Entscheidung spaetestens zu Beginn der ERZEUGER-Folgephase (`kb_*`),
      dort faellt die Stelle ohnehin an.
  12. **R-2 — `services/crm_service.py:46`** (`KAUFBEREITSCHAFT AM ENDE: {kb_end}%`), Aufrufer
      `routes/app_routes.py:369`. Export-/E-Mail-Pfad (CRM-Notiz + Follow-up-Mail), verlangt
      zusaetzlich eine „Abschlusswahrscheinlichkeit". **Bleibt**: ehrlich beschriftete
      Kunden-Kennzahl in einem Kunden-Artefakt — dieselbe Linie wie
      `Kaufbereitschaft Ende: /100` (Plan 06). Termin: ERZEUGER-Folgephase.
      ⚠ **Geminis Position dazu, mit protokolliert (Cross-AI 14.08.):** *„Die Abschaffung der
      Note gilt nicht nach aussen."* GSD-Gegenargument: Der Export ist ein **Kunden**-Artefakt,
      kein Verkaeufer-Bildschirm, faellt also nicht unter D-19. **Hier sind sich beide Seiten am
      Ende einig, dass die Stelle bleibt** — die Uneinigkeit betrifft nur die Einstufung
      (MEDIUM gegen out-of-scope). Ebenso protokolliert: Geminis Widerspruch gegen die
      Einstufung von `routes/app_routes.py:1286` (`_derive_practice_recommendations`,
      A-3 = R-2-Klasse) — auch dort GSD „ehrlich beschriftet, bleibt", Gemini „MEDIUM".
      **Die Stelle bleibt benannt und drin.**
  13. **R-3 — `services/customer_success_service.py:33/71` → `routes/admin_views.py:177` →
      `templates/admin/crm_overview.html:108` (`{{ u.avg_kb }}%`).** Founder-Admin-Sicht,
      aggregiert `kb_end` je Nutzer. **Bleibt**: die **Begruendung** von D-18 traegt („misst
      Datenqualitaet, benotet keinen Verkaeufer") — aber D-18 nennt namentlich nur
      `admin_ewb.py:72`. **Zweite Stelle derselben Klasse, hiermit benannt.**
  14. **R-4 — `templates/dashboard.html:370` „Redeanteil-Verhaeltnis"**
      (`services/coaching_service.py:455`). Plottet genau den Wert, den D-16 aus dem
      Vierer-Aufriss entfernt hat. Steht als **NACHTRAG 2** im Fragen-Kanal; **Ort und Termin
      fehlen noch** — in der Bilanz als **„wartet auf Claudian"** fuehren, nicht als erledigt.
  15. **R-5 — vier Anker sind heute schon 0** (`&#216; Score` · `transkript_renderer` in
      `routes/learning.py` · `TranscriptSegment.call_id` · `speaker == 0`). Drei sind zulaessige
      „Waechter auf neuen Code", einer eine defensive Schreibvariante — **alle vier sind per
      Bauart blind statt rot** und beweisen nur mit gepaartem Existenz-Anker etwas (Punkt 31).
      Plan 02/05 paaren; **Plan 01 paart ebenfalls** (`01-PLAN.md:368-370`:
      `grep -c "TranscriptSegment.conversation_log_id == conv.id" routes/learning.py` → **1**)
      — nachgeprueft in R2, **kein Nachzug noetig**. In die Bilanz gehoert die **Klasse**, nicht
      der Einzelfall: ein 0-Anker ohne Partner ist kein Beweis.
  16. **Die benannte Grenze des Verfahrens selbst (Punkt 31):** „Alle Anker-Messungen dieser
      Phase liefen gegen den **lokalen Arbeitsbaum**, nicht gegen Production (`inspect.sh` nicht
      gezogen) — ein SCP-Hotfix auf Prod, der lokal fehlt, waere unsichtbar." Und: „**Statische
      Anker sehen keine Laufzeit-Pfade**; die Modell-Prosa aus R-1/R-2 ist von keinem grep
      erreichbar."
  17. **Was Gemini nicht pruefen konnte — von ihm selbst benannt und hier uebernommen
      (Cross-AI 14.08.):**
      (a) **Vollstaendigkeit der Bestandstests** — es lief **kein Pytest**. Weitere stale
      Tests, die durch die Vertragswechsel dieser Phase rot werden, koennen unentdeckt sein.
      **Zweite Schicht darunter:** das serverseitige Test-Tor in `deploy.sh production` faengt
      sie beim Ausrollen — rot heisst dort „kein Neustart", also kein Schaden, aber eine
      **Ausroll-Verzoegerung**. Wer eine Welle ausrollt, rechnet damit.
      (b) **SQL-Performance der Aggregationen** (`logs_90`, `logs_this_week`) — **kein
      `EXPLAIN` gegen Production**. Moegliche fehlende Indizes im Dashboard-v2-Lesezugriff sind
      **ungeprueft**. Diese Phase **entfernt** dort Rechnungen, fuegt keine hinzu — das Risiko
      wird also nicht groesser, ist aber auch nicht gemessen.
      (c) **Gemini sieht den Code im Ruhezustand, nicht den Live-Server** (Punkt 24 Teil B).
  18. **⚠ Die benannte Schwaeche des Cross-AI-Durchgangs selbst — NICHT kaschieren:** Es lief
      nur **EINE** externe Sicht (Gemini). `claude` ist die ausfuehrende Umgebung und wurde zur
      Wahrung der Unabhaengigkeit uebersprungen; `codex`, `coderabbit`, `opencode`, `qwen`,
      `cursor` sind auf diesem Rechner **nicht installiert**. **Die „drei Sichten" aus
      CLAUDE.md Punkt 24 Teil B sind damit nur zu zweien besetzt (GSD + Gemini) — das ist eine
      benannte Schwaeche, kein Vollzug.** **Ort/Termin:** wer eine dritte Sicht will, muss
      `codex` oder `opencode` installieren; Entscheidung gehoert in `Nerve-Vault/05 Log`.
  19. **`kaufbereitschaft` hat nach dieser Phase NULL Leser** (`services/coaching_service.py`,
      Rueckgabe-Schluessel von `get_longterm_data`). Mit dem Fall des KB-Trend-Diagramms
      (Plan 09) verliert der Schluessel seinen einzigen Abnehmer und wird bei **jedem**
      Dashboard-Aufruf umsonst berechnet. **Er bleibt** — er gehoert zum **ERZEUGER**, und
      METRIK-1 entfernt nur die **Verbraucher**. **Hiermit benannt, damit er nicht der naechste
      „gruen, aber still tot"-Fall wird. Termin: die ERZEUGER-Folgephase (`kb_*` /
      `coaching_haiku` abschalten).** Zusaetzlich Eintrag in
      `Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md`.
      ⚠ **Bewusst NICHT mit gefallen:** der Kaufbereitschafts-**Verlauf je Anruf**
      (`templates/session_detail.html`) — Plan 09 entfernt nur die **Wochen-Aggregation**,
      nicht jede Kaufbereitschafts-Linie.
  20. **Divergenz-Aufloesung `_compliance` (Cross-AI-Frage (a)) — geschlossen, hier
      protokolliert:** Gemini forderte, die Ausnahme zu streichen (*„Ein Vorwurf ohne Beleg ist
      toxisch"*). **Der Einwand richtete sich gegen den VORWURF, nicht gegen das Flag** — und
      den Vorwurf gibt es nicht mehr: seit Andres Entscheidung vom 14.08. erscheint bei
      fehlendem Beleg ausschliesslich der Satz 👁 **„Dieses Gespräch wurde gemeldet und wird von
      einem NERVE-Mitarbeiter geprüft."** Kein Verdikt, auch nicht abgeschwaecht. **Das Flag
      bleibt gesetzt** (der Sicherheitsbefund ueberlebt den Zitat-Fehler). **Geminis Anliegen
      ist damit erfuellt, ohne die Ausnahme zu streichen** — deshalb wurde hier nichts
      geaendert, sondern die Aufloesung protokolliert. Umgesetzt in **Plan 01, Task 4**.
      ⚠ **Zwei Auflagen, die aus diesem Satz folgen und NICHT vergessen werden duerfen:**
      (a) der Satz ist ein **Versprechen** — der Fall muss in der Gruender-/Diagnose-Sicht als
      **aufrufbarer Einzelfall** landen, sonst ist der Text unwahr; (b) 🔴 **DSGVO, ausserhalb
      dieser Phase:** „Mitarbeiter pruefen Gespraeche" **steht heute nicht** in der
      Datenschutzerklaerung. **Ort: Anwalts-Liste · Termin: zusammen mit den
      DSGVO-Kundenrechten.**
  21. **Divergenz-Aufloesung KB-Trend-Diagramm (Cross-AI-Frage (d)) — geschlossen:** Gemini
      und GSD waren uneinig, Andre hat am 13./14.08. **(b) entschieden — das Diagramm faellt**
      (Plan 09). ⚠ **Richtigstellung, die in die Bilanz gehoert:** Das urspruengliche
      GSD-/Claudian-Hauptargument („die Quelle stirbt in der direkt folgenden Phase") ist
      **falsch und zurueckgezogen** — zwischen METRIK-1 und der Erzeuger-Phase liegen laut
      ROADMAP **drei** Phasen, und das Diagramm wuerde nicht einfrieren, sondern eine **flache
      erfundene 30er-Linie** malen (fehlender Wert → `30`). **Die tragende Begruendung lautet:**
      eine 0-bis-100-Wochenlinie auf dem meistbesuchten Bildschirm wird als **Zeugnis** gelesen,
      und sie mittelt heute schon **erfundene 30er-Werte** mit. **Das alte Argument darf nicht
      wiederverwendet werden.**

- Pruning-Notiz.
</output>
