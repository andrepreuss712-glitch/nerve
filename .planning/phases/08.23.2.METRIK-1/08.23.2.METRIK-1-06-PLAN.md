---
phase: 08.23.2.METRIK-1
plan: 06
type: execute
wave: 5
depends_on: ["08.23.2.METRIK-1-05"]
files_modified:
  - templates/session_detail.html
  - routes/dashboard.py
  - routes/app_routes.py
  - tests/test_session_detail_render.py
autonomous: true
requirements: [SPEC-R9]
user_setup: []

must_haves:
  truths:
    - "Die Auswertungsseite eines LIVE-Anrufs zeigt keine Gesamtnote mehr — weder im Hero, noch im Vierer-Aufriss, noch im Kopfbereich, noch als Trainings-Durchschnitt."
    - "Die Auswertungsseite einer TRAINING-Sitzung zeigt ihre Note weiterhin."
    - "Der Trend-Streifen auf der Auswertungsseite ist ersatzlos weg — ohne Ersatz-Zahl."
    - "Die Redeanteil-Zeile, die im Kaltakquise-Modus immer 100 Prozent zeigt, ist weg."
    - "_calc_call_score hat keinen Leser mehr in routes/dashboard.py — und sein Name steht dort auch in keinem Kommentar mehr (Plan 08 zaehlt Kommentare ausdruecklich mit)."
  artifacts:
    - path: "templates/session_detail.html"
      provides: "Hero nur noch fuer Training, kein Vierer-Aufriss, kein Trend-Abzeichen, kein 'Result: X/100' bei live"
    - path: "routes/dashboard.py"
      provides: "score_total nur fuer Training, kein _calc_call_score-Import, kein trend_avg"
    - path: "routes/app_routes.py"
      provides: "_cross_context_objections_live ohne avg_score (Schwester-Form)"
  key_links:
    - from: "templates/session_detail.html"
      to: "conv.typ == 'training'"
      via: "neue typ-Weiche am Hero — ohne sie stirbt die Trainings-Note still (D-17)"
      pattern: "conv.typ == 'training'"
    - from: "routes/app_routes.py::_cross_context_objections_live"
      to: "templates/session_detail.html"
      via: "Rueckgabe ohne avg_score -> der bestehende else-Zweig traegt die notenfreie Variante"
      pattern: "'sessions': n, 'focus_match'"
---

<objective>
Die alte Note verschwindet von der Auswertungsseite — **ausser im Training**, wo sie tatsaechlich
den Verkaeufer misst.

**Purpose:** Die Note war falsch, weil sie im echten Anruf zu 40 % die **Kaufbereitschaft des
Kunden** mass, die der Verkaeufer nicht steuert, und zu 20 % einen Redeanteil, der im
Kaltakquise-Modus baubedingt **immer 100 %** ist (Term dadurch **immer 0**). Im Training gibt es
keinen Kunden mit Laune, sondern ein festes Szenario mit einer richtigen Antwort — **dort misst
eine Note tatsaechlich den Verkaeufer.**

**Output:** Auswertungsseite ohne Score-Hero, ohne Vierer-Aufriss, ohne Trend-Abzeichen und ohne
den irrefuehrenden Kopfbereichs-Wert — bei Live. Bei Training unveraendert.

## ⛔ D-17 (Gap C) — die Falle dieses Plans

**Der Score-Hero hat heute KEINE typ-Weiche** (`session_detail.html:71-83`): er rendert
`score_total` fuer **beide** Typen. Training schreibt seine Gesamtnote in `kb_end`
(`routes/training.py`) und laeuft durch **denselben** Hero (`routes/dashboard.py:795-796`).

**Wer den Hero blind entfernt, laesst die Trainings-Note still sterben.** Der Hero bekommt
deshalb **neu** eine `{% if conv.typ == 'training' %}`-Weiche — das Muster steht direkt darunter
in derselben Datei (`:89`) und an rund acht weiteren Stellen.

⛔ **Verworfen und nicht wieder aufzunehmen:** Hero auch fuers Training raus · sechs
Kategorie-Noten bleiben (derselbe Verstoss, nur kleinteilig) · Training bekommt Form 2 (eigene
Phase).

## ⛔ Kein Deploy in diesem Plan

Zwischen „alte Note raus" (hier) und „Form 2 rein" (Plan 07) haette die Auswertungsseite ein
Loch: keine Note **und** keine Kopfzeile. Deshalb rollt **Plan 07, Task 3** die Plaene 05, 06
und 07 gemeinsam aus. Commits und Push laufen normal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-SPEC.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-CONTEXT.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-05-SUMMARY.md
@CLAUDE.md
@CLAUDE-BAUREGELN-13-14-20.md

<interfaces>
Der Score-Hero, **verbatim** aus `templates/session_detail.html:68-83` — ⚠ **keine typ-Weiche**:
```jinja
  {# ── Section 2: Score-Hero (W-06: score_total aus Plan 01) ────── #}
  {% set score = score_total %}
  {% set score_class = 'n-session-detail-score--high' if score >= 70 else ('n-session-detail-score--mid' if score >= 60 else 'n-session-detail-score--low') %}
  <section class="n-session-detail-hero" aria-label="Gesamt-Score">
    <div>
      <div class="n-label">Gesamt-Score</div>
      <div class="n-session-detail-score {{ score_class }}">{{ score }}</div>
      {# Wave 4 / POLISH-33: Trend-Badge typ-agnostisch — Route liefert trend_avg
         auch fuer Training (Schnitt ueber kb_end letzte 5 typ='training'). #}
      {% if trend_avg is not none %}
        {% set delta = score - trend_avg %}
        <div class="n-badge {% if delta > 0 %}n-badge-primary{% elif delta < 0 %}n-badge-danger{% else %}n-badge-neutral{% endif %} n-session-detail-trend-badge--spaced">
          {% if delta > 0 %}+{{ delta }} vs Schnitt letzte 5{% elif delta < 0 %}{{ delta }} vs Schnitt letzte 5{% else %}Schnitt letzte 5{% endif %}
        </div>
      {% endif %}
    </div>
```

Der Vierer-Aufriss, **verbatim** `templates/session_detail.html:84-116` (der Live-Zweig
`:89-115` mit vier `n-session-detail-breakdown-row`-Bloecken, darunter die Redeanteil-Zeile
`:107-110`, die im Kaltakquise-Modus **immer 100 %** zeigt).

Die Kopfbereichs-Zeile, **verbatim** `templates/session_detail.html:51-52`:
```jinja
      {# Fix B (07.1 UAT-R1): kb_end_effective = letzter kb_verlauf-Punkt (konsistent mit Chart-Ende). #}
      <span>Result: {{ kb_end_effective if kb_end_effective is not none else '—' }}/100</span>
```

Der Shim, **verbatim** `routes/dashboard.py:785-796` — ⚠ **der `else`-Zweig IST die
Trainings-Note**:
```python
        if conv_typ == 'live':
            # Score mit dem effektiven kb_end berechnen (mini-Shim um _calc_call_score
            # ohne zweite Helper-Variante): temporaer conv.kb_end setzen, Score holen,
            # urspruenglichen Wert zurueckschreiben — Session-Lifecycle bleibt read-only.
            _orig_kb = conv.kb_end
            try:
                conv.kb_end = kb_end_effective
                score_total = _calc_call_score(conv)
            finally:
                conv.kb_end = _orig_kb
        else:
            score_total = kb_end_effective
```

Der Trend-Block, **verbatim** `routes/dashboard.py:804-818` (Live-Zweig `:812-813` mit
`_calc_call_score`, Training-Zweig `:814-818` mit `kb_end`).

Die Schwester-Funktion als Vorlage, `routes/app_routes.py:1141+`:
`_cross_context_objections_training` liefert `{sessions, n_success, focus_match}` — **ohne**
Score. Die Live-Variante wird auf `{sessions, focus_match}` zurueckgebaut.

Die drei Template-Zweige, die das bereits tragen, `templates/session_detail.html:378-389`:
`cc.sessions == 0` → „Noch nie im Training geübt" · `cc.avg_score is not none and >= 70 and
sessions >= 3` → „Zuletzt im Training: Ø N/100 aus M Sessions" · `{% else %}` → „Im Training:
N Sessions". **Der mittlere Zweig faellt weg, der `else` traegt ihn schon.**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hero wird typ-abhaengig, Vierer-Aufriss und Trend-Abzeichen fallen (D-15/D-16/D-14/D-17)</name>

  <files>
templates/session_detail.html
tests/test_session_detail_render.py
  </files>

  <read_first>
- `templates/session_detail.html` — **`:1-200`**. Der Kopfbereich (`:20-56`), der Hero
  (`:68-117`), die KI-Karte (`:119-190`) und die typ-Weichen-Vorbilder (`:89`, `:125`, `:193`,
  `:197`, `:202`).
- `routes/training.py` — die Stelle, an der die Trainings-Gesamtnote in `kb_end` geschrieben
  wird (`grep -n "kb_end" routes/training.py`). **Belegt, dass D-17 real ist.**
- `routes/admin_ewb.py:60-80` — die Admin-Sicht, die `kb_end` ueber die bewerteten Sessions
  spannt. **Sie BLEIBT (D-18)** — sie benotet keinen Verkaeufer, sie misst unsere
  Datenqualitaet.
- `tests/test_session_detail_render.py` — Seed- und `_login`-Muster.
  </read_first>

  <action>
**A) Hero bekommt eine typ-Weiche (D-17) und verliert das Trend-Abzeichen (D-14).**

Die Section `:68-117` wird ersetzt durch:

```jinja
  {# ── Section 2: Score-Hero — NUR NOCH TRAINING (METRIK-1 D-15/D-17) ───────────────────────
     LIVE zeigt keine Gesamtnote mehr: sie mass zu 40 % die Kaufbereitschaft des KUNDEN (die der
     Verkaeufer nicht steuert) und zu 20 % einen Redeanteil, der im Kaltakquise-Modus baubedingt
     100 % ist — der Term war dort IMMER 0. An ihre Stelle tritt die KI-Einschaetzung (Form 2).
     TRAINING BEHAELT SEINE NOTE: dort gibt es keinen Kunden mit Laune, sondern ein festes
     Szenario mit einer richtigen Antwort — die Note misst dort tatsaechlich den Verkaeufer.
     ⚠ Diese typ-Weiche ist NEU. Vorher hatte der Hero KEINE — wer ihn blind entfernt, laesst
     die Trainings-Note still sterben (D-17, Gap C).
     D-14: Das Trend-Abzeichen — der Vergleich gegen den Schnitt der letzten fuenf Sitzungen —
     ist ERSATZLOS entfallen, auch fuer Training.
     ⛔ Sein Anzeige-Text wird hier bewusst NICHT zitiert: er ist in diesem Task ein
     Abwesenheits-Anker mit Sollwert 0 in dieser Datei. Beschreiben statt zitieren. Eine neutrale Ersatz-Zahl (Anrufe/Woerter) ist ausdruecklich VERWORFEN — sie
     misst Betriebsamkeit, nicht Fortschritt. Lieber eine ehrliche Leerstelle als eine huebsche
     Zahl ohne Bedeutung. Der Streifen kommt zurueck, sobald die Fokus-Serie existiert
     ("dreimal in Folge angewendet") — vertagt, nicht gestrichen. #}
  {% if conv.typ == 'training' %}
    {% set score = score_total %}
    {% set score_class = 'n-session-detail-score--high' if score >= 70 else ('n-session-detail-score--mid' if score >= 60 else 'n-session-detail-score--low') %}
    <section class="n-session-detail-hero" aria-label="Gesamt-Score">
      <div>
        <div class="n-label">Gesamt-Score</div>
        <div class="n-session-detail-score {{ score_class }}">{{ score }}</div>
      </div>
    </section>
  {% endif %}
```
⚠ Der **gesamte** `div.n-session-detail-breakdown`-Block (`:84-116`) faellt mit — **einschliesslich
des Live-Zweigs** (D-16: „Gewichtungen raus, Zahlen bleiben" ist ausdruecklich **verworfen**,
weil das die Redeanteil-Zeile stehen liesse, die im Kaltakquise-Modus immer 100 % zeigt).

**B) Der irrefuehrende Kopfbereichs-Wert wird typ-abhaengig (`:51-52`).**

⚠ **Fund beim Planen, ueber die sieben Lesestellen aus D-12 hinaus:** Die Zeile
`Result: {{ kb_end_effective }}/100` steht im Kopfbereich **beider** Typen. Bei einem LIVE-Anruf
zeigt sie die **Kaufbereitschaft des Kunden**, beschriftet als „Result … /100" — also **genau
die Krankheit, wegen der diese Phase existiert**, nur an einer Stelle, an der niemand gesucht
hat. Die ROADMAP nennt sie ausdruecklich „reine Label-Irrefuehrung". Ohne Aenderung waere das
Abnahme-Kriterium **„kein LIVE-Anruf zeigt eine Gesamtnote — auf keinem Bildschirm"** nicht
erfuellt.

```jinja
      {# METRIK-1 D-19: "Result: X/100" zeigte bei LIVE-Anrufen kb_end — die Kaufbereitschaft
         des KUNDEN, beschriftet wie eine Note des Verkaeufers (ROADMAP: "reine
         Label-Irrefuehrung"). Bei Training ist derselbe Wert die echte Trainings-Gesamtnote
         (routes/training.py schreibt sie nach kb_end) und bleibt deshalb stehen. #}
      {% if conv.typ == 'training' %}
        <span>Result: {{ kb_end_effective if kb_end_effective is not none else '—' }}/100</span>
      {% endif %}
```

**C) Tests in `tests/test_session_detail_render.py`:**

1. `test_live_zeigt_keine_gesamtnote` — Seed mit `typ='live'`; im HTML steht **kein**
   `Gesamt-Score`, **kein** `n-session-detail-hero`, **kein** `Result:` und **kein**
   `vs Schnitt letzte 5`. **Gepaarte Existenz-Anker in derselben Funktion:**
   `KI-Einschätzung` **steht** im HTML und der Status ist 200 — sonst waere „nichts gefunden"
   nicht von „Seite nicht gerendert" zu unterscheiden.
2. `test_training_behaelt_seine_note` — Seed mit `typ='training'` und einem `kb_end`-Wert;
   im HTML stehen `Gesamt-Score` **und** der Zahlenwert. ⚠ **Das ist der Gap-C-Test.** Er
   belegt, dass die Trainings-Note **nicht still gestorben** ist.
3. `test_kein_vierer_aufriss_mehr` — im HTML steht **kein** `n-session-detail-breakdown` und
   **kein** `Redeanteil` in der Hero-Region; gepaarter Existenz-Anker: `Dauer:` (Kopfbereich)
   **steht** im HTML.
4. `test_kein_trend_abzeichen_bei_training` — Seed `typ='training'` mit fuenf aelteren
   Trainings-Sitzungen; `vs Schnitt letzte 5` steht **nicht** im HTML, `Gesamt-Score`
   **schon**. **D-14 gilt auch fuer Training.**
  </action>

  <verify>
    <automated>grep -c "Gesamt-Score" templates/session_detail.html</automated>
    <automated>grep -c "vs Schnitt letzte 5\|n-session-detail-breakdown" templates/session_detail.html</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM, jeweils mit gepaartem Existenz-Anker:**
  - `grep -c "n-session-detail-breakdown" templates/session_detail.html` liefert **0**;
    Existenz-Anker: `grep -c "n-session-detail-hero" templates/session_detail.html` liefert **1**.
  - `grep -c "vs Schnitt letzte 5" templates/session_detail.html` liefert **0**;
    Existenz-Anker: `grep -c "n-badge n-badge-neutral" templates/session_detail.html`
    liefert **>= 1** (die uebrigen Abzeichen leben).
  - `grep -c "trend_avg" templates/session_detail.html` liefert **0**;
    Existenz-Anker: `grep -c "score_total" templates/session_detail.html` liefert **1**
    (nur noch im Training-Hero).
- **D-17 mechanisch belegt — auf der NEUEN Code-Form, nicht auf dem nackten Namen:**
  ⚠ Der frueher hier stehende Anker `grep -c "{% if conv.typ == 'training' %}" … >= 2` war
  **heute schon gruen**: gemessen am 2026-08-13 stehen **13** `conv.typ == 'training'`-Weichen in
  der Datei, davon **4** in exakt dieser Schreibweise (`:13`, `:22`, `:202`, `:306`). Ein Anker,
  der ohne die Aenderung gruen ist, kann eine **vergessene Hero-Weiche** nicht erkennen — also
  genau den D-17-Tod, gegen den er gebaut war. Deshalb zaehlt jetzt die **Nachbarschaft**:
  - `grep -c "{% if conv.typ == 'training' %}" templates/session_detail.html` liefert **>= 6**
    (belegte Vorher-Zahl **4**; Hero und Kopfbereichs-Zeile kommen dazu). Die Zahl **vor** und
    **nach** dem Task steht im SUMMARY, jeweils mit `grep -n`-Fundstellen.
  - **Der eigentliche Anker — die Weiche steht unmittelbar VOR dem Hero:**
    `grep -A2 "{% if conv.typ == 'training' %}" templates/session_detail.html | grep -c "n-session-detail-hero"`
    liefert **1**. Steht der Hero **nicht** innerhalb der Weiche, ist die Trainings-Note tot —
    und genau das faengt dieser Anker, waehrend eine blosse Namens-Zaehlung es nicht kann.
    Gepaarter Existenz-Anker: `grep -c "n-session-detail-hero" templates/session_detail.html`
    liefert **1** (der Hero existiert ueberhaupt noch).
  - **Dasselbe fuer die Kopfbereichs-Zeile:**
    `grep -B2 "Result: {{ kb_end_effective" templates/session_detail.html | grep -c "conv.typ == 'training'"`
    liefert **1**.
- `grep -c "Result: {{ kb_end_effective" templates/session_detail.html` liefert **1**, und die
  Zeile steht **innerhalb** einer `conv.typ == 'training'`-Weiche.
- Die vier Tests existieren:
  `grep -c "def test_live_zeigt_keine_gesamtnote\|def test_training_behaelt_seine_note\|def test_kein_vierer_aufriss_mehr\|def test_kein_trend_abzeichen_bei_training" tests/test_session_detail_render.py`
  liefert **4**.
- **Kein Inline-Style, keine Hex-Farbe NEU — Anker auf die geaenderte Region, nicht auf die
  ganze Datei:**
  ⚠ Der frueher hier stehende Anker `grep -c "style=\"--w:" … == 0` war **per Bauart
  unerfuellbar**. Gemessen am 2026-08-13 stehen **6** Vorkommen in der Datei: `:101`, `:105`,
  `:109`, `:113` (der Vierer-Aufriss — **die fallen mit diesem Task**), aber auch **`:313`**
  (Skript-Fortschritts-Balken) und **`:470`** (Trainings-Kategorie-Minibar). **Nach dem Task
  bleiben also 2** — beide ausserhalb des Scope dieses Plans.
  Ein Soll von `0` haette den Executor dazu gebracht, den Skript-Balken und die
  Trainings-Minibar anzufassen: **Reparatur-Modus-Verstoss** (Vault-Regel Nr. 5) und ausserdem
  der dokumentierte Reflex „**der Anker formt das Artefakt**".
  Statt dessen gilt:
  - **Regions-Anker — NUR gegen einen festen Basis-Commit:**
    ⛔ `git diff <datei>` zeigt ausschliesslich **ungestagte** Aenderungen gegen HEAD. Der
    vorgeschriebene Task-Workflow lautet aber *Code-Edit → `git commit` → `git push` →
    `deploy.sh`* — **nach dem Commit ist die Ausgabe leer**, und `| grep "^+" | grep -c ...`
    liefert **0**, **ohne irgendetwas gemessen zu haben**. Das ist „blind statt rot, sieht dabei
    gruen aus" (Punkt 31 — Sperre gegen den stillen Ausfall). Deshalb:
    1. **Vor** der ersten Aenderung dieses Plans: `BASIS=$(git rev-parse HEAD)`, SHA **verbatim
       ins SUMMARY**.
    2. `git diff $BASIS -- templates/session_detail.html | grep "^+" | grep -c "style=\"--w:"`
       liefert **0** — dieser Task fuegt **kein neues** `style="--w:` hinzu.
    3. **Gepaarter Nicht-Leer-Beleg — ohne ihn ist `0` nicht von „nichts gemessen" zu
       unterscheiden:** `git diff $BASIS -- templates/session_detail.html | grep -c "^-"`
       liefert **>= 10** (der Vierer-Aufriss ist wirklich weg). Liefert er **0**, hat der
       Vergleich gar nicht gemessen — **Fund melden, nicht weitermachen.**
  - **Rest-Anker mit Begruendung:** `grep -c "style=\"--w:" templates/session_detail.html`
    liefert **2**, und der Executor belegt mit `grep -n` beide Fundstellen und haelt im SUMMARY
    fest, **warum sie bleiben**: `:313` ist der Skript-Fortschritt (eine **Abdeckungs-Quote**,
    keine Note) und `:470` die Kategorie-Minibar im **Trainings**-Zweig (D-17 — Training behaelt
    seine Bewertung). Beide sind der erlaubte Sonderfall „dynamischer Wert" der Farb-HART-Regel.
    ⚠ Weicht die Zahl von **2** ab, ist das ein **Fund** — melden, nicht anpassen.
  </acceptance_criteria>

  <done>
Ein Live-Anruf zeigt auf der Auswertungsseite keine Zahl mehr, die wie eine Note aussieht — eine
Trainings-Sitzung zeigt ihre Note unveraendert, und das ist an einer echten Trainings-Sitzung
belegt, nicht angenommen.
  </done>
</task>

<task type="auto">
  <name>Task 2: routes/dashboard.py — score_total nur fuer Training, _calc_call_score und trend_avg raus</name>

  <files>
routes/dashboard.py
  </files>

  <read_first>
- `routes/dashboard.py:738-830` — **ganz**: Import (`:742`), der **Kommentarblock `:763-765`**
  (Abschnitt A0 — er nennt die alte Formel beim Namen und faellt durch **jedes** andere Raster),
  `kb_end_effective`-Ableitung
  (`:768-783`), der Shim (`:785-796`), der Trend-Block (`:798-818`).
- `routes/dashboard.py:1000-1035` — der Kontext-Block; `trend_avg` (`:1020`), `score_total`
  (`:1024`), `kb_end_effective` (`:1025`).
- **Pflicht-grep VOR jeder Loeschung (Bau-Regel 20), Ausgaben ins SUMMARY:**
  ```bash
  grep -rn "_calc_call_score" routes/ services/ tests/ --include=*.py
  grep -rn "trend_avg" routes/ templates/ --include=*.py --include=*.html
  grep -rn "kb_end_effective" routes/ templates/ --include=*.py --include=*.html
  ```
  </read_first>

  <action>
**A0) Der Kommentarblock `:763-765` wird beschreibend umgeschrieben — sonst ist Plan 08 per
Bauart unerfuellbar.**

Heute stehen dort zwei Zeilen, die die alte Formel **beim Namen nennen**:
`:764` schreibt sie mit einem **Leerzeichen** vor der Klammer, `:765` mit dem Wort `fuer`
dahinter. Beide entkommen deshalb jedem Anker, der auf `_calc_call_score(` **mit** Klammer
zeigt — und beide bleiben stehen, wenn dieser Task nur den Shim (`:785-796`) und den
Trend-Block (`:798-818`) ersetzt. **Plan 08, Task 2 zaehlt aber `_calc_call_score` ueber
`routes/` und `services/` OHNE Klammer und mit Kommentaren, und `routes/dashboard.py` steht
nicht in seinen `<files>`.** Ohne diesen Abschnitt kann Plan 08 seinen Sollwert 0 nie erreichen.
Dazu waere das Schild falsch (Punkt 23): ein Kommentar, der eine Formel als Live-Weg beschreibt,
die es nicht mehr gibt.

`routes/dashboard.py:763-765` wird ersetzt durch:
```python
        # Phase 07.1 / METRIK-1 D-15/D-17: score_total ist typ-abhaengig.
        #  - Live: die alte Vierer-Formel — entfaellt mit METRIK-1, es gibt keinen Wert mehr.
        #  - Training: eigener Wert, bleibt (D-17).
        # ⛔ Der Name der alten Formel wird hier bewusst NICHT genannt: Plan 08 fuehrt ihn als
        #    Abwesenheits-Anker mit Sollwert 0 ueber routes/ und services/ — Kommentare und
        #    Docstrings zaehlen dort ausdruecklich mit. Beschreiben statt zitieren.
```
⚠ **Gegengeprueft (Fix-Schritt 4):** Im Block `:755-770` steht **keine weitere** Zeichenkette,
die ein Anker irgendeines Plans dieser Phase mit Sollwert 0 fuehrt. Geprueft wurden alle
0-Anker, deren Suchpfad `routes/dashboard.py` einschliesst: `_calc_call_score` (Plan 06/08),
`trend_avg` (Plan 06) und `Leakage-Schutz` (Plan 03, Fundstelle liegt bei `:936`). `kb_end`,
`kb_end_effective` und `Gesamt-Score` sind in **keinem** Plan 0-Anker fuer diese Datei
(`Gesamt-Score` ist auf `templates/session_detail.html` datei-gebunden) — der neue Text
vermeidet sie trotzdem, weil eine Beschreibung sie nicht braucht.

**A) Der Shim faellt, der `else`-Zweig bleibt (D-17).**

`routes/dashboard.py:785-796` wird ersetzt durch:
```python
        # ── METRIK-1 D-15/D-17: score_total gibt es nur noch fuer TRAINING. ──────────────────
        # LIVE bekam bis 2026-08-13 die alte Vierer-Formel (kb_end 40 %, behandelt_rate 30 %,
        # rede_score 20 %, skript 10 %). Drei der vier Bausteine trugen im Kaltakquise-Modus
        # nichts Echtes bei; die 40-%-Komponente war die Kaufbereitschaft des KUNDEN. Die Formel
        # ist damit ohne Leser in dieser Datei — der Import faellt mit.
        # ⛔ Ihr Funktionsname wird hier bewusst NICHT genannt: Plan 08 fuehrt ihn als
        #    Abwesenheits-Anker mit Sollwert 0 ueber routes/ und services/. Ein Kommentar, der
        #    ihn zitiert, macht den Anker der Folge-Welle unerfuellbar.
        # TRAINING behaelt seine Note: kb_end traegt dort die Trainings-Gesamtnote
        # (routes/training.py), nicht die Kaufbereitschaft. Wer diesen Zweig mitloescht, toetet
        # sie still (D-17, Gap C).
        score_total = kb_end_effective if conv_typ == 'training' else None
```

**B) Der Import** (`routes/dashboard.py:742`): `_calc_call_score` entfernen,
`_derive_practice_recommendations` **bleibt**.
⚠ Die Formel selbst wird **hier nicht geloescht** — sie hat noch einen zweiten Leser in
`routes/app_routes.py` (innerhalb der Trend-Route des Anruf-Fensters). Beide fallen gemeinsam in
**Plan 08**. **Als Kommentar an die Import-Zeile**, damit niemand sie fuer vergessen haelt.
⛔ **Dieser Kommentar formuliert BESCHREIBEND** („die alte Vierer-Formel faellt mit ihrem zweiten
Leser in Plan 08") **und nennt den Funktionsnamen nicht** — Plan 08 fuehrt ihn als
Abwesenheits-Anker mit Sollwert **0** ueber `routes/` und `services/`. Ein hier stehengelassener
Namens-Zitat-Kommentar wuerde die Loesch-Pruefung der Folge-Welle unerfuellbar machen, **ohne
dass dieser Plan es je merkt** — Kollisionen ueber Plan-Grenzen hinweg sind die teuerste Sorte.

**C) Der Trend-Block** (`:798-818`) **faellt ersatzlos** — beide Zweige, inklusive der
`recent`-Abfrage, die nur ihn bediente. Ersetzt durch:
```python
        # ── METRIK-1 D-14: Trend-Streifen ERSATZLOS entfallen. ──────────────────────────────
        # Eine neutrale Ersatz-Groesse (Anrufe/Woerter) ist ausdruecklich VERWORFEN: sie misst
        # Betriebsamkeit, nicht Fortschritt. Lieber eine ehrliche Leerstelle als eine huebsche
        # Zahl ohne Bedeutung. NICHT gestrichen, VERTAGT: der Streifen kommt zurueck, sobald die
        # Fokus-Serie existiert ("dreimal in Folge angewendet").
```
⚠ **Die `recent`-Abfrage genau pruefen:** wird sie ausserhalb des Trend-Blocks noch benutzt?
`grep -n "recent" routes/dashboard.py` **vor** der Loeschung ausfuehren und die Ausgabe ins
SUMMARY. Wird sie noch gebraucht, bleibt sie stehen.

**D) Der Kontext-Block** (`:1014-1026`): `trend_avg=trend_avg,` entfernen. `score_total` und
`kb_end_effective` **bleiben** (Training braucht beide).

**E) `kb_end_effective` bleibt berechnet** — Training nutzt es weiter fuer Hero und
Kopfbereichs-Zeile. Der Kommentar `:768-770` („Fix B“, kb_verlauf-Endpunkt) bleibt inhaltlich
gueltig und wird **nicht** angefasst.
⚠ **Der Kommentarblock `:763-765` daruber ist damit NICHT gemeint** — er ist in **Abschnitt A0**
beschreibend neu geschrieben. Wer ihn stehen laesst, macht Plan 08 unerfuellbar.
  </action>

  <verify>
    <automated>grep -c "_calc_call_score\|trend_avg" routes/dashboard.py</automated>
    <automated>grep -rn "_calc_call_score" routes/ --include=*.py</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM mit gepaartem Existenz-Anker:**
  - `grep -c "_calc_call_score(" routes/dashboard.py` liefert **0**;
    Existenz-Anker: `grep -c "_derive_practice_recommendations" routes/dashboard.py`
    liefert **>= 2** (der Nachbar-Import lebt).
  - **Der Name ist restlos raus — OHNE Klammer, Kommentare zaehlen mit (A0):**
    `grep -c "_calc_call_score" routes/dashboard.py` liefert **0**.
    ⚠ Der Klammer-Anker darueber allein reicht **nicht**: gemessen am 2026-08-13 stehen in der
    Datei **7** Vorkommen, davon **vier ohne** anliegende Klammer (`:742` Import, `:764`, `:765`,
    `:786`, `:799`, `:800`) — `:764` schreibt `_calc_call_score (` **mit Leerzeichen**, `:765`
    `_calc_call_score fuer`. Genau diese beiden faengt **kein** klammer-gebundener Anker, und
    genau sie haetten Plan 08, Task 2 (`grep -rn "_calc_call_score" routes/ services/` → **0**)
    per Bauart unerfuellbar gemacht.
    Gepaarte Existenz-Anker: `grep -c "Training: eigener Wert, bleibt (D-17)" routes/dashboard.py`
    liefert **1** (der neue Kommentarblock steht wirklich da, der grep hat also gesucht) **und**
    `grep -c "score_total = kb_end_effective if conv_typ == 'training' else None" routes/dashboard.py`
    liefert **1**.
    ⛔ **Zitier-Sperre:** Weder der neue Kommentarblock aus A0 noch irgendein anderer Kommentar
    dieses Tasks darf den Funktionsnamen enthalten — auch nicht „zur Erklaerung", auch nicht in
    Anfuehrungszeichen. Ein „warum-entfernt"-Kommentar, der den Namen zitiert, laesst ihn seine
    eigene Loeschung ueberleben und bricht die Folge-Welle, **ohne dass dieser Plan es merkt**.
  - `grep -c "trend_avg" routes/dashboard.py` liefert **0**;
    Existenz-Anker: `grep -c "def session_detail" routes/dashboard.py` liefert **1**.
- `grep -c "score_total = kb_end_effective if conv_typ == 'training' else None" routes/dashboard.py`
  liefert **1** — **das ist der Gap-C-Anker (D-17)**.
- **Bau-Regel 20 — die drei Pflicht-greps sind VOR der Loeschung gezogen** und stehen mit
  Ausgaben im SUMMARY. Insbesondere belegt der `_calc_call_score`-grep, dass **noch ein
  Leser** in `routes/app_routes.py` existiert (`/api/postcall/trend`) und die Funktion deshalb
  **hier nicht** geloescht wird.
- `grep -rn "trend_avg" templates/ routes/ | wc -l` liefert **0** — die Variable ist restlos
  raus. Gepaarter Existenz-Anker: `grep -c "score_total" routes/dashboard.py` liefert **>= 2**.
  </acceptance_criteria>

  <done>
Der Live-Zweig der alten Formel hat in `routes/dashboard.py` keinen Aufrufer mehr, der
Trainings-Zweig laeuft unveraendert, und der Trend-Streifen ist ersatzlos weg — ohne Ersatz-Zahl.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lesestelle 7 — 'Zuletzt im Training: Ø 78/100' faellt (D-12)</name>

  <files>
routes/app_routes.py
templates/session_detail.html
tests/test_session_detail_render.py
  </files>

  <read_first>
- `routes/app_routes.py:1110-1138` — `_cross_context_objections_live`, besonders
  `func.avg(CL.kb_end)` (`:1124`) und `avg_score` (`:1134-1135`).
- `routes/app_routes.py:1141+` — `_cross_context_objections_training`, die Schwester **ohne**
  Score. **Das ist die Vorlage.**
- `templates/session_detail.html:377-392` — die drei `cc`-Zweige des Live-Falls.
- **Pflicht-grep VOR der Loeschung:**
  ```bash
  grep -rn "avg_score" routes/ templates/ static/ --include=*.py --include=*.html --include=*.js
  ```
  ⚠ Der Kartierer hat fuer `routes/performance.py:158` **keinen** Leser gefunden. **Das ist eine
  Zaehlung, kein Beweis** — der Executor zieht den grep erneut und schreibt die Ausgabe ins
  SUMMARY. `performance.py` wird in **Plan 09** angefasst, nicht hier.
  </read_first>

  <action>
**A) `routes/app_routes.py::_cross_context_objections_live` auf die Schwester-Form zurueckbauen.**

- `func.avg(CL.kb_end).label('avg_score')` aus der Abfrage entfernen (`:1124`).
- `avg_score` aus dem Rueckgabe-Dict entfernen (`:1134-1135`); die Funktion liefert danach
  `{'sessions': n, 'focus_match': einwand_typ}`.
- Den Docstring anpassen: die Zeilen
  `Returns: {sessions: int, avg_score: float|None, focus_match: str}` und
  `Score basiert auf kb_end (Training), NICHT sterne (SC-1 — Training setzt sterne NICHT).`
  ersetzen durch:
```python
    Returns: {sessions: int, focus_match: str}

    METRIK-1 D-12/Lesestelle 7: der frueher mitgelieferte avg_score war ein Mittelwert ueber
    kb_end und wurde als "Zuletzt im Training: Ø 78/100" gerendert — eine Gesamtnote auf der
    Auswertungsseite eines LIVE-Anrufs. Er ist entfallen; die Schwester-Funktion
    _cross_context_objections_training kam ohne ihn immer schon aus.
```

**B) `templates/session_detail.html`:** der mittlere `{% elif %}`-Zweig (`:382-385`) faellt
**ersatzlos**. Der bestehende `{% else %}`-Zweig („Im Training: N Sessions") traegt den Fall
bereits — **kein Neubau noetig**.
⚠ Nur der **Live**-Zweig (`{% if conv.typ == 'live' %}`, `:379`) ist betroffen. Der
Training-Zweig darunter (`:392+`) bleibt unangetastet — dort ist der Wert die Trainings-Note.
**Der Executor liest beide Zweige, bevor er loescht**, und belegt im SUMMARY, welchen er
angefasst hat.

**C-vorab) Eine `/100`-Zahl, die BLEIBT — geprueft und begruendet, nicht uebersehen
(Bau-Regel 20).**

`routes/app_routes.py:1286` in `_derive_practice_recommendations` erzeugt
```python
                'observation': f'Kaufbereitschaft Ende: {kb_end_effective}/100',
```
Das rendert auf der **Live**-Auswertungsseite eine `/100`-Zahl aus `kb_end`.

**Entscheidung: sie bleibt.** Begruendung, die in **derselben Zeile als Kommentar** und im
SUMMARY steht:
> Sie ist **ehrlich beschriftet** — sie nennt die **Kaufbereitschaft**, also genau das, was
> `kb_end` misst, und schreibt sie **nicht** dem Verkaeufer als Leistung zu. D-19 verlangt „kein
> LIVE-Anruf zeigt eine **Gesamtnote**"; eine korrekt benannte Kunden-Kennzahl ist keine Note.
> Sie faellt mit dem **ERZEUGER** (`kb_*`, eigene Folgephase), nicht hier.

⚠ **Warum das ausdruecklich hier steht:** Ohne diesen Eintrag taucht die Zeile in der
D-19-Abnahme (Plan 09, Schritt 3) als `/100`-Fund auf und wirkt wie ein Ueberbleibsel.
Bau-Regel 20 kennt nur zwei zulaessige Zustaende — **geloescht** oder **geprueft und mit
Begruendung stehengelassen**. Dies ist der zweite.

**Anker dafuer** (in den `<acceptance_criteria>` unten):
`grep -c "Kaufbereitschaft Ende: {kb_end_effective}/100" routes/app_routes.py` liefert **1** —
ein **Existenz**-Anker, damit die Zeile nicht versehentlich mitfaellt.

**C) Test** in `tests/test_session_detail_render.py`:
`test_kein_trainings_durchschnitt_auf_der_live_seite` — Seed `typ='live'` mit einer Empfehlung,
deren `cross_context` frueher den Zweig ausgeloest haette. Assertion: `Zuletzt im Training: Ø`
steht **nicht** im HTML; gepaarter Existenz-Anker: `Im Training:` **oder**
`Noch nie im Training geübt` **steht** im HTML — sonst waere „nichts gefunden" nicht von
„Empfehlungs-Block gar nicht gerendert" zu unterscheiden.
  </action>

  <verify>
    <automated>grep -c "avg_score" routes/app_routes.py</automated>
    <automated>grep -c "Zuletzt im Training" templates/session_detail.html</automated>
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM mit gepaartem Existenz-Anker:**
  - `grep -c "func.avg(CL.kb_end)" routes/app_routes.py` liefert **0**;
    Existenz-Anker: `grep -c "def _cross_context_objections_live" routes/app_routes.py`
    liefert **1**.
  - `grep -c "Zuletzt im Training" templates/session_detail.html` liefert **0**;
    Existenz-Anker: `grep -c "Im Training:" templates/session_detail.html` liefert **>= 1**.
- `grep -c "'sessions': n, 'focus_match': einwand_typ" routes/app_routes.py` liefert **1**.
- **Der Training-Zweig ist NICHT angefasst:** `grep -c "cc.avg_score" templates/session_detail.html`
  liefert die Zahl der **verbleibenden** Vorkommen; der Executor belegt im SUMMARY, welche das
  sind und dass sie zum Training-Zweig gehoeren. Ist die Zahl 0, wird das ausdruecklich
  festgehalten — dann war der Training-Zweig ohnehin ohne Score.
- Der Pflicht-grep auf `avg_score` (ueber `routes/`, `templates/`, `static/`) steht **mit
  Ausgabe** im SUMMARY, samt der Feststellung, welche Treffer in **Plan 09** fallen.
- **Die geprueft-und-begruendet stehengelassene `/100`-Zahl (Bau-Regel 20):**
  `grep -c "Kaufbereitschaft Ende: {kb_end_effective}/100" routes/app_routes.py` liefert **1**
  (Existenz-Anker — sie darf **nicht** mitfallen), und der Kommentar mit der Begruendung steht
  daneben: `grep -c "ehrlich beschriftet" routes/app_routes.py` liefert **>= 1**.
  Die Begruendung steht zusaetzlich im SUMMARY, damit sie in der D-19-Abnahme (Plan 09,
  Schritt 3) nicht als Ueberbleibsel gelesen wird.
- Der neue Test existiert und hat seinen gepaarten Existenz-Anker.
  </acceptance_criteria>

  <done>
Die Auswertungsseite eines Live-Anrufs zeigt keinen Trainings-Notendurchschnitt mehr — und der
Ersatz war bereits gebaut, es musste nichts Neues entstehen.
  </done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21 + Punkt 26)

### Angefasste Tabellen

- `conversation_logs` (**lesen**: `kb_end`, `kb_verlauf`, `typ`) — keine RLS
- `objection_events` (**lesen**, ueber `_cross_context_objections_live`) — keine RLS

**Dieser Plan schreibt keine Daten und aendert kein Schema.**

### inspect.sh-Beleg

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema conversation_logs'
```
⚠ **Der Executor zieht diesen Auszug und fuegt ihn verbatim ins SUMMARY** — der Plan-Autor hat
fuer `rubric_score`, `transcript_segments` und den Migrations-Kopf gezogen (Plan 01/02), fuer
`conversation_logs` steht der Zug aus. Erwartet wird die Bestaetigung, dass `kb_end` eine
**Integer-Spalte ohne CHECK auf den Wertebereich** ist und `typ` die Werte `live`/`training`
traegt. **Weicht das ab, ist das ein Fund** — insbesondere, wenn `typ` weitere Werte kennt: dann
fiele ein dritter Typ durch **beide** Zweige der neuen `conv.typ == 'training'`-Weiche und
haette **weder** Hero **noch** Form 2.

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg-thread) + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `conv.kb_end` | gelesen in `routes/dashboard.py`, gerendert im Training-Hero | DB-Spalte `conversation_logs.kb_end` | **Live geschrieben** am Call-Ende (Live-Pfad) bzw. von `routes/training.py` am Trainings-Ende. Der Anzeige-Pfad laeuft danach. ⚠ **Der Name luegt:** bei `typ='training'` steht dort die **Trainings-Gesamtnote**, nicht die Kaufbereitschaft (`routes/admin_ewb.py` sagt es woertlich). Genau darauf beruht D-17. | ⏳ Executor zieht `inspect.sh schema conversation_logs` |
| `conv.kb_verlauf` | gelesen, JSON-geparst (`:772-781`) | DB-Spalte (Text/JSON) | wie oben; ein kaputter JSON faellt bereits heute auf `conv.kb_end` zurueck | ✓ Bestandslogik unveraendert |
| `conv.typ` | gelesen, steuert **beide** neuen Weichen | DB-Spalte `conversation_logs.typ` | einmalig beim Anlegen. **Kritisch:** ein dritter Wert ausser `live`/`training` faellt durch beide Weichen. Der Executor prueft das gegen die Realdaten (`inspect.sh sample conversation_logs 50`, Spalte `typ`) und schreibt die vorkommenden Werte ins SUMMARY. | ⏳ Executor |
| `avg_score` (`_cross_context_objections_live`) | **entfaellt** | war `AVG(conversation_logs.kb_end)` | n/a | ✓ Loeschung, Bau-Regel-20-grep davor |
| `trend_avg` | **entfaellt** | war eine Berechnung ueber die letzten fuenf Sitzungen | n/a | ✓ Loeschung, Bau-Regel-20-grep davor |

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
⚠ Ausdruecklich: Dieser Plan entfernt **Anzeigen und Berechnungen**, keine gespeicherten Daten.
`conversation_logs.kb_end` bleibt befuellt — METRIK-1 entfernt die **Verbraucher**, die
Folgephase den **Erzeuger**. Nie andersherum: bleiben hier Verbraucher stehen, kann die
Folgephase gar nicht feuern.

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Die Seite entscheidet selbst anhand von `conv.typ`, ob eine Note
   angebracht ist — statt sie fuer beide Welten gleich zu zeigen.
2. **Nachvollziehen:** Die Trainings-Note bleibt in `conversation_logs.kb_end` gespeichert und
   in der Admin-Sicht auswertbar (D-18).
3. **Eingreifen koennen:** Die Weiche ist ein einzeiliges `conv.typ == 'training'` — sie laesst
   sich in Sekunden umstellen, falls Training doch Form 2 bekommen soll (eigene Phase).
4. **Marge:** Eine Zahl, die zu 40 % die Laune des Kunden misst und dem Verkaeufer als Leistung
   ausgewiesen wird, kostet Vertrauen — und Vertrauen ist bei einem Coaching-Produkt das Produkt.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gespeicherte Note → Bildschirm des Beraters | Eine falsch beschriftete Zahl ist ein inhaltlicher Schaden, kein technischer |
| `conv.typ` → Anzeige-Weiche | Ein unerwarteter Wert laesst die Seite in eine Luecke fallen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-METRIK1-06-01 | Repudiation | Trainings-Note stirbt still | **mitigate** | Eigener Verhaltens-Test (`test_training_behaelt_seine_note`) **plus** Abnahme an einer echten Trainings-Sitzung im Deploy-Checkpoint von Plan 07. „Belegt, nicht angenommen" steht woertlich in den SPEC-Abnahmekriterien. |
| T-METRIK1-06-02 | Denial of Service (fachlich) | dritter `conv.typ`-Wert faellt durch beide Weichen | **mitigate** | Der Executor prueft die real vorkommenden Werte per `inspect.sh sample` und haelt sie im SUMMARY fest. Findet er einen dritten Wert, ist das ein **Fund** — melden, nicht wegdruecken. |
| T-METRIK1-06-03 | Information Disclosure | Loeschung entfernt versehentlich Daten | **accept/mitigate** | Es werden ausschliesslich Anzeige- und Berechnungs-Pfade entfernt; kein `DELETE`, keine Migration. Belegt durch `git diff` (keine Datei unter `alembic/`). |
</threat_model>

<verification>
- Alle `<acceptance_criteria>` der drei Tasks sind erfuellt.
- Die drei Pflicht-greps (Bau-Regel 20) sind **vor** den Loeschungen gezogen und stehen mit
  Ausgaben im SUMMARY.
- Der `inspect.sh schema conversation_logs`-Auszug und die real vorkommenden `typ`-Werte stehen
  im SUMMARY.
- **Kein Deploy in diesem Plan** — Plan 07 rollt 05, 06 und 07 gemeinsam aus.
</verification>

<success_criteria>
- Kein Live-Anruf zeigt auf der Auswertungsseite eine Gesamtnote — weder Hero, noch Aufriss,
  noch Kopfbereich, noch Trainings-Durchschnitt, noch Trend-Abzeichen.
- Eine Trainings-Sitzung zeigt ihre Note unveraendert, per Verhaltens-Test belegt.
- `_calc_call_score` hat in `routes/dashboard.py` keinen Aufrufer mehr; der verbleibende Leser
  in `routes/app_routes.py` ist benannt und fuer Plan 08 terminiert.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-06-SUMMARY.md` anlegen.
**Pflicht-Inhalte:**
- Die drei Bau-Regel-20-greps **mit Ausgaben**, vor den Loeschungen gezogen.
- `inspect.sh schema conversation_logs` verbatim + die real vorkommenden `typ`-Werte.
- Die Zahl der `conv.typ == 'training'`-Weichen vor und nach dem Task.
- **Die geprueft-und-begruendet stehengelassene Stelle:** `Kaufbereitschaft Ende: N/100`
  (`routes/app_routes.py`, `_derive_practice_recommendations`) bleibt — ehrlich beschriftet,
  faellt mit dem ERZEUGER in der Folgephase. **Mit grep-Beleg**, damit sie in der D-19-Abnahme
  nicht als Fund gelesen wird.
- **Die zwei verbleibenden `style="--w:`-Vorkommen** (`:313` Skript-Fortschritt, `:470`
  Trainings-Minibar) mit `grep -n`-Fundstellen und der Begruendung, warum sie bleiben.
- **Der Fund ueber die sieben Lesestellen hinaus:** „Result: X/100" im Kopfbereich
  (`session_detail.html:51-52`) war eine **achte** Stelle, die bei Live-Anrufen `kb_end` als
  Note-artige Zahl zeigte. Sie ist mit erledigt und **ausdruecklich als Fund gemeldet**.
- Pruning-Notiz: Score-Hero-Live-Zweig, Vierer-Aufriss, Trend-Abzeichen, `trend_avg`,
  `avg_score` in `_cross_context_objections_live` — jeweils **geloescht**. `_calc_call_score`
  **bleibt vorerst** (zweiter Leser in `routes/app_routes.py`, faellt in Plan 08) — mit
  Begruendung, nicht stillschweigend.
</output>
