---
phase: 08.23.2.METRIK-1
plan: 04
type: execute
wave: 3
depends_on: ["08.23.2.METRIK-1-02"]
files_modified:
  - services/fokus_katalog.py
  - tests/test_fokus_katalog.py
  - .planning/DIALOG-GSD-CLAUDIAN.md
autonomous: false
requirements: [SPEC-R6, SPEC-R7]
user_setup: []

must_haves:
  truths:
    - "Der Code waehlt die eine Sache — kein Modell ist an Auswahl oder Formulierung beteiligt."
    - "Bei gleichem Transkript kommt deterministisch derselbe Fokus heraus."
    - "Der Beleg-Satz ist per Bauart woertlich aus dem Transkript gezogen, nicht geprueft."
    - "Verletzt kein Kriterium, liefert der Katalog ehrlich 'nichts' — nie eine erfundene Sache."
    - "Der Katalog enthaelt genau die vier Wortlisten-Punkte (Sorte A), alle Texte englisch."
    - "Kein Eintrag der Streichliste und kein Eintrag der Sorten B oder C steht im Katalog."
  artifacts:
    - path: "services/fokus_katalog.py"
      provides: "4 Zaehlregeln + feste Rangfolge + 4 englische Satz-Schablonen + Nichts-Ausgang"
      exports: ["KATALOG_VERSION", "FOKUS_SCHLUESSEL", "waehle_fokus"]
      min_lines: 180
    - path: "tests/test_fokus_katalog.py"
      provides: "Zaehlregel-Tests (Wortgrenzen, Platzhalter, Gross/Klein, EWB-Zeile, Schwellen-Kante)"
  key_links:
    - from: "services/fokus_katalog.py"
      to: "services/transkript_renderer.py::ist_ewb_zeile"
      via: "defensiver Zweitfilter gegen die EWB-Knopf-Zeile"
      pattern: "ist_ewb_zeile"
    - from: "services/fokus_katalog.py::waehle_fokus"
      to: "(Aufrufer in Plan 05)"
      via: "Bau-Regel 20: dieser Plan erzeugt bewusst Code ohne Aufrufer — der Aufrufer kommt in Plan 05, DERSELBEN Phase, und dieser Plan wird NICHT einzeln ausgerollt"
      pattern: "waehle_fokus\\("
---

<objective>
Der Fokus-Katalog v1: **vier** Wortlisten-Punkte auf **Englisch**, vom **Code** gezaehlt und
ausgewaehlt, mit dem Beleg-Satz direkt aus dem Transkript und einer von vier festen englischen
Satz-Schablonen.

**Purpose:** Sprachmodelle **zaehlen notorisch schlecht** — ein Modell, das „ab 4×" pruefen soll,
raet. Und der K.-o.-Grund: das erzwungene Antwort-Schema (`judge_runner.py:390` `tool_choice`,
Pflichtfelder `:121`) **muss** einen Schluessel liefern; bei einem guten Anruf ohne Verstoss
muesste das Modell einen **erfinden**. Genau der Fehler, den die Abnahme suchen sollte, waere
einprogrammiert. Der Rechenweg zieht den Beleg-Satz direkt aus dem Transkript — er ist **per
Bauart echt**, nicht per Pruefung.

**Output:** Ein reines Regel-Modul ohne DB, ohne LLM, ohne Seiteneffekte.

## ⛔ Harte Grenzen dieses Plans

- **D-07/D-08: Kein Modell in der Auswahl, keines in der Formulierung, KEIN zusaetzlicher
  KI-Aufruf.** Das Modul importiert nichts aus `anthropic` und nichts aus `claude_service`.
- **D-10: Der „diesmal nichts"-Zweig ist der NORMALFALL**, nicht die Ausnahme — der Katalog ist
  englisch, der gesamte Bestand deutsch. **Niemals eine erfundene Sache, niemals eine leere
  Stelle.**
- **D-11: Der Katalog ist englisch.** Die **Schluessel** bleiben trotzdem ASCII-Identifier
  (CLAUDE.md „ASCII-Pflicht in Code-Identifiern").
- **Sorte B (vier Zeitmasse) und Sorte C (Live-Symbol) werden NICHT gebaut** — sie haben Ort und
  Termin (4.0.2 bzw. 4c). Wer sie „vorsichtshalber" mitnimmt, baut auf Zahlen, die gleich danach
  korrigiert werden.
- **Die Streichliste ist genauso verbindlich wie die Aufnahmeliste:** Fuellwoerter · Weichmacher
  (`"I think"` ist die **bessere** Form) · Tonfall · **Fragenanzahl** (*„zero statistical
  difference"*) · `"Did I catch you at a bad time?"`. **Keiner dieser Punkte kommt in den
  Katalog.**
- **Kein Deploy in diesem Plan.** Das Modul bekommt seinen Aufrufer in **Plan 05** (dieselbe
  Phase, naechste Welle); ausgerollt wird es dort. Bau-Regel 20 („0 Aufrufer = STOPP") ist damit
  nicht verletzt, sondern **bewusst terminiert** — und der Termin steht in dieser Zeile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-SPEC.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-CONTEXT.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md
@CLAUDE.md

<interfaces>
Die Modul-Bauform, die zu kopieren ist — `services/handling_markers.py`:
Wortlisten als Modul-Konstanten, Normalisierung als reine Funktion, Entscheidungs-Kaskade mit
fester Rangfolge, und ein **ehrlicher Nichts-Ausgang** (`return None`).

```python
def _normalize(text: str) -> str:
    """Kleinschreibung + kollabierte Whitespaces. Reine Hilfsfunktion (kein Seiteneffekt)."""
    return re.sub(r"\s+", " ", text.strip().lower())
```

⚠ **NICHT** die `marker in norm`-Form aus `handling_markers._has_label_marker` uebernehmen —
das ist ein Teilstring-Vergleich **ohne** Wortgrenze (`we` wuerde in `week` treffen). D-09
verlangt Wortgrenzen ausdruecklich.

Aus `services/transkript_renderer.py` (Plan 01):
```python
EWB_MARKER = '*ewb button*'
def ist_ewb_zeile(seg) -> bool
def segmente_ohne_ewb(segments) -> list
def render_transkript(segments, *, mit_tags: bool = True) -> str
```

**Die belegten Zahlen (Quelle: `Nerve-Vault/07 Referenz/US-Vertrieb — belegte Zahlen +
Praktiker-Wissen (2026-08-07)`, ueber ROADMAP.md und SPEC Requirement 7):**
- Grund des Anrufs frueh nennen: **2,1×** Erfolg
- `we`/`our` statt `I`/`my`: **+35 % / +55 %**
- Problem-Sprache statt Modewoerter: **16 % gg. 5,5 %**
- Gongs Negativ-Liste (519.000 Gespraeche): `we provide` **ab 4× −22 %** · `discount` **−17 %** ·
  `absolutely`/`perfect` **ab 4× −16 %** · `show you how` **ab 4× −13 %** · eigener Firmenname
  **ab 6× −19 %**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/fokus_katalog.py — vier Zaehlregeln, feste Rangfolge, vier Schablonen</name>

  <files>
services/fokus_katalog.py (NEU)
.planning/DIALOG-GSD-CLAUDIAN.md
  </files>

  <read_first>
- `services/handling_markers.py` — **ganze Datei**. Modul-Docstring (`:1-24`), Wortlisten als
  Konstanten (`:31-60`), `_normalize` + Wortgrenzen-Regex (`:78-94`), die Entscheidungs-Kaskade
  mit `return None` (`:133-144`). **Das ist die Bauform, Punkt fuer Punkt.**
- `services/transkript_renderer.py` (Plan 01) — die drei Export-Funktionen.
- `services/anonymization.py` — genug, um zu sehen, **welche Platzhalter-Form** im gespeicherten
  Text steht (`[PERSON_A]`, `[ORG_C]`, `[LOC_X]`, `[nicht gespeichert]`). Reicht: die
  Ausgabe-Seite mit dem Wortgrenzen-Muster `(?<!\w)...(?!\w)`.
- SPEC Requirement 7 **vollstaendig**, inklusive der **Streichliste**.
- `.planning/DIALOG-GSD-CLAUDIAN.md` — Datei-Ende, um das Format der Eintraege zu sehen.
  </read_first>

  <action>
**A) Modul-Docstring** in der Form von `handling_markers.py:1-24`:

```python
# -*- coding: utf-8 -*-
"""METRIK-1 Plan 04 — Fokus-Katalog v1: die EINE Sache fuers naechste Mal, vom CODE gewaehlt.

Quelle des Inhalts: Nerve-Vault/07 Referenz "US-Vertrieb — belegte Zahlen + Praktiker-Wissen
(2026-08-07)", ueber SPEC Requirement 7. DARAUS gebaut, nicht neu erfunden.

D-07/D-08: Der CODE prueft die vier Zaehlregeln, waehlt nach FESTER Rangfolge, zieht den
belegenden Satz DIREKT aus dem Transkript und setzt die gezaehlte Zahl in eine von vier festen
englischen Satz-Schablonen. KEIN Modell in der Auswahl, KEINES in der Formulierung, KEIN
zusaetzlicher KI-Aufruf. Daher KEIN anthropic-/claude_service-Import in diesem Modul.

WARUM DER CODE ZAEHLT UND NICHT DAS MODELL: Sprachmodelle zaehlen notorisch schlecht. Und das
erzwungene Antwort-Schema des Bewerters (services/judge_runner.py, forced tool_choice mit
Pflichtfeldern) MUSS einen Schluessel liefern — bei einem guten Anruf ohne Verstoss muesste das
Modell einen erfinden. Genau der Fehler, den die Abnahme suchen sollte, waere einprogrammiert.

D-10: Verletzt kein Kriterium, liefert waehle_fokus None — der Aufrufer zeigt dann ehrlich
"Nothing flagged this time.". Auf deutschem Bestand ist das der NORMALFALL, nicht die Ausnahme:
der Katalog ist englisch (D-11), alle bisherigen Anrufe sind deutsch. Niemals eine erfundene
Sache, niemals eine leere Stelle.

D-11: Alle nutzersichtbaren Texte sind ENGLISCH (erste Scheibe des US-Coaching-Gehirns). Die
SCHLUESSEL bleiben ASCII-Identifier (CLAUDE.md ASCII-Pflicht in Code-Identifiern).

NICHT ENTHALTEN und nicht nachtraeglich aufzunehmen:
  - Sorte B (vier Zeitmasse) — Vorbedingung sind die zwei Rechen-Korrekturen aus
    Roadmap-Punkt 4.0.2, der NACH dieser Phase laeuft.
  - Sorte C (Live-Symbol) — Roadmap-Punkt 4c.
  - STREICHLISTE, genauso verbindlich wie die Aufnahmeliste: Fuellwoerter (500.000 Gespraeche,
    null Zusammenhang) · Weichmacher ("I think" ist die BESSERE Form) · Tonfall (aus Text nicht
    messbar) · Fragenanzahl ("zero statistical difference") · "Did I catch you at a bad time?".

waehle_fokus ist eine REINE Funktion: kein DB-Zugriff, kein LLM, kein Seiteneffekt, bei gleicher
Eingabe deterministisch dasselbe Ergebnis.
"""
```

**B) Konstanten.**

```python
KATALOG_VERSION = 1
```

Die vier Schluessel (ASCII):
```python
FOKUS_SCHLUESSEL = (
    'negative_phrases',   # A4 — Gongs Negativ-Liste
    'we_not_i',           # A2 — we/our statt I/my
    'problem_language',   # A3 — Problem-Sprache statt Modewoerter
    'reason_for_call',    # A1 — Grund des Anrufs frueh nennen
)
```
**Diese Reihenfolge IST die Rangfolge** (D-07: „waehlt unter den verletzten nach fester
Rangfolge"). Begruendung als Kommentar darueber: sortiert nach der Groesse des **belegten**
Effekts — `we provide` −22 % ist der staerkste Einzelwert der Negativ-Liste, danach das
we/I-Verhaeltnis (+35 %/+55 %), dann Problem-Sprache (16 % gg. 5,5 %), zuletzt der
Anruf-Grund (2,1×, aber am schwaechsten maschinell fassbar).

Negativ-Liste als Tupel `(phrase, schwelle, effekt_prozent)` — **die Schwellen sind BELEGT,
nicht gesetzt**:
```python
NEGATIVE_PHRASES = (
    ('we provide',   4, -22),
    ('discount',     1, -17),   # ohne "ab N x" in der Quelle -> schon einmal zaehlt
    ('absolutely',   4, -16),
    ('perfect',      4, -16),
    ('show you how', 4, -13),
)
FIRMENNAME_SCHWELLE = 6      # -19 %, belegt
```
`absolutely` und `perfect` teilen sich laut Quelle **eine** Regel („`absolutely`/`perfect` ab 4×").
**Festgelegt: die beiden Zaehlungen werden ADDIERT**, nicht getrennt gewertet — sonst kaeme ein
Anruf mit 3× `absolutely` und 3× `perfect` (zusammen 6) durch, obwohl die Quelle das Verhalten
als eines beschreibt. Als Kommentar in den Code.

```python
ICH_WOERTER = ('i', 'my', 'me', 'mine')
WIR_WOERTER = ('we', 'our', 'us', 'ours')
ICH_SCHWELLE = 4          # GESETZT, nicht belegt — siehe Kommentar
BUZZWORDS = ('solution', 'solutions', 'synergy', 'synergies', 'cutting-edge',
             'innovative', 'world-class', 'best-in-class', 'leverage', 'seamless',
             'game-changer', 'disruptive', 'holistic', 'robust', 'scalable')
BUZZWORD_SCHWELLE = 3     # GESETZT, nicht belegt — siehe Kommentar
REASON_PHRASES = ('the reason for my call', 'the reason i am calling',
                  "the reason i'm calling", 'the reason for the call',
                  'reason for my call')
```

⚠ **Ein Pflicht-Kommentar ueber den beiden gesetzten Schwellen, woertlich sinngemaess:**
> **BELEGT vs. GESETZT — bitte nicht verwechseln.** Belegt aus der US-Referenz sind allein die
> Schwellen der Negativ-Liste (4/4/4/6) und die Effekt-Prozente. `ICH_SCHWELLE` und
> `BUZZWORD_SCHWELLE` sind **von uns gesetzt**, weil die Quelle dort ein Verhaeltnis bzw. eine
> Quote nennt, keine Schwelle. Sie sind Kandidaten fuer die Nachjustierung nach rund hundert
> echten Anrufen — dieselbe Nachjustierung wie beim Substanz-Tor. Wer sie aendert, aendert eine
> **gesetzte** Zahl, keine belegte.

**C) Zaehl-Kern mit Wortgrenzen (D-09).**

```python
def _normalize(text: str) -> str:
    """Kleinschreibung + kollabierte Whitespaces. Reine Hilfsfunktion (kein Seiteneffekt)."""
    return re.sub(r"\s+", " ", (text or '').strip().lower())


def _zaehle(phrase: str, norm: str) -> int:
    """Zaehlt VORKOMMEN einer Phrase mit Wortgrenzen — 'we' trifft NICHT in 'week'.

    (?<!\\w)...(?!\\w) statt \\b: robust auch fuer Phrasen, die mit einem Nicht-Wortzeichen
    beginnen oder enden (z.B. 'cutting-edge'), wo \\b an der falschen Stelle greift. Dieselbe
    Form benutzt der Ausgabe-Pfad der Anonymisierung.

    Gross/Kleinschreibung ist bereits durch _normalize erledigt (D-09).
    """
    if not phrase:
        return 0
    return len(re.findall(r'(?<!\w)' + re.escape(phrase) + r'(?!\w)', norm))
```

**D) `waehle_fokus`.**

```python
def waehle_fokus(berater_texte, *, firmenname=None) -> dict | None:
    """Waehlt die EINE Sache fuers naechste Mal — vom Code, nicht vom Modell (D-07).

    Args:
        berater_texte: Liste der Segment-TEXTE des BERATERS, in Sprech-Reihenfolge.
            Der Aufrufer filtert auf speaker == 'berater' und auf die EWB-Knopf-Zeilen
            (services/transkript_renderer.segmente_ohne_ewb). Dieses Modul filtert die
            EWB-Zeilen SICHERHEITSHALBER ein zweites Mal — ein Knopfdruck ist kein Satz, und
            ein vergessener Filter im Aufrufer darf hier keine Zaehlung erzeugen.
        firmenname: Der eigene Firmenname des Beraters, oder None.
            ⚠ BEKANNTE GRENZE: das gespeicherte Transkript ist anonymisiert; ein erkannter
            Firmenname steht dort als [ORG_X]. Diese Teilregel schlaegt deshalb in der Praxis
            selten an. Sie bleibt trotzdem drin (belegter Effekt -19 %) und ist bei
            durchgerutschten Nennungen wirksam. Ins SUMMARY, nicht stillschweigend.

    Returns:
        None  -> kein Kriterium verletzt (D-10, NORMALFALL). Der Aufrufer zeigt dann
                 "Nothing flagged this time." — niemals eine erfundene Sache, niemals leer.
        dict  -> {'focus_key': str, 'count': int, 'limit': int|None, 'satz': str,
                  'beleg': str, 'katalog_version': int}
                 'beleg' ist WOERTLICH einer der uebergebenen Texte — per Bauart aus dem
                 Transkript gezogen, nicht geprueft (D-07).

    Deterministisch: gleiche Eingabe -> gleiches Ergebnis. Keine Zufallsquelle, keine Zeit,
    kein Modell.
    """
```
Ablauf, **genau in dieser Reihenfolge** (die Rangfolge ist die Reihenfolge der Pruefungen —
die **erste** verletzte Regel gewinnt, danach wird abgebrochen):

1. Texte saeubern: EWB-Zeilen raus (`EWB_MARKER not in text`), leere raus.
   Ist die Liste danach leer → `return None`.
2. `norm_gesamt = _normalize(' '.join(texte))`, und je Text ein `_normalize` fuer die
   Beleg-Suche.
3. **`negative_phrases`** — ueber `NEGATIVE_PHRASES` **in Listen-Reihenfolge** (das ist die
   Rangfolge innerhalb der Regel, sortiert nach Effekt) plus die addierte
   `absolutely`+`perfect`-Zaehlung plus die Firmenname-Teilregel. Erste Teilregel, deren Zahl
   `>= schwelle` ist, gewinnt.
   Beleg: der **erste** Text, in dem die Phrase vorkommt (woertlich, unveraendert).
   Schablone 1:
   `f'You said "{phrase}" {count} times — top reps cap it at {schwelle - 1}.'`
   ⚠ Bei `discount` (Schwelle 1) waere `cap it at 0` — deshalb dort die Variante
   `f'You said "discount" {count} times — top reps let the price stand instead.'`
   Das ist die **zweite** von vier Schablonen. (Also: Schablone 1 = Kappungs-Satz,
   Schablone 2 = Rabatt-Satz.)
4. **`we_not_i`** — `ich = sum(_zaehle(w, norm_gesamt) for w in ICH_WOERTER)`,
   `wir = sum(...WIR_WOERTER...)`. Verletzt, wenn `ich >= ICH_SCHWELLE and ich > wir`.
   Beleg: der erste Text, der ein Wort aus `ICH_WOERTER` enthaelt.
   Schablone 3:
   `f'You said "I" or "my" {ich} times and "we" or "our" {wir} times — top reps flip that ratio.'`
5. **`problem_language`** — `count` = Summe aller `BUZZWORDS`-Treffer; verletzt bei
   `count >= BUZZWORD_SCHWELLE`. Fuer den Satz wird das **haeufigste** Buzzword genannt; bei
   Gleichstand gewinnt das **in `BUZZWORDS` zuerst stehende** (deterministisch, ausdruecklich im
   Kommentar).
   Beleg: der erste Text mit diesem Buzzword.
   Schablone 4:
   `f'You used buzzwords {count} times, "{wort}" most often — top reps name the problem instead.'`
6. **`reason_for_call`** — s. Punkt E.
7. Nichts verletzt → `return None`.

**E) `reason_for_call` — die Regel mit dem Sprach-Riegel.**

Verletzt, wenn **keine** der `REASON_PHRASES` in den **ersten zwei** Berater-Texten vorkommt.
`count` = Anzahl der Berater-Texte vor der ersten Nennung; kommt sie gar nicht vor, ist `count`
die Gesamtzahl der Berater-Texte.
Beleg: der **erste** Berater-Text (der Gespraechs-Einstieg) — er existiert immer, ist woertlich
aus dem Transkript und ist genau die Stelle, um die es geht.
Schablone 5 — ⚠ **das waeren fuenf Schablonen, D-07 nennt vier.** Aufloesung: Schablone 1 und 2
(Kappung / Rabatt) sind **zwei Auspraegungen derselben Negativ-Listen-Schablone**. Gezaehlt wird
**eine Schablone je Fokus-Schluessel** — also **vier**. Der Rabatt-Satz ist die Sonderform,
weil `cap it at 0` unsinnig waere. **Diesen Absatz woertlich in den Modul-Docstring**, damit
niemand spaeter „fuenf Schablonen" fuer einen Verstoss haelt.
```python
f'You spoke {count} times before naming the reason for your call — top reps name it in the first line.'
```

**⚠ SPRACH-RIEGEL — die einzige Regel, die auf deutschem Bestand sonst IMMER anschlagen wuerde:**
`reason_for_call` ist die einzige Regel, die auf **Abwesenheit** prueft. Ohne Riegel wuerde sie
bei **jedem** deutschen Anruf feuern — und damit D-10 aushebeln („der `diesmal nichts`-Zweig ist
der NORMALFALL") und dem Berater eine Sache nennen, die auf Deutsch gar nicht zutrifft.

Deshalb: die Regel feuert **nur**, wenn das Transkript ueberhaupt englisch ist. Geprueft wird das
mit **derselben Maschinerie**, die die anderen Regeln ohnehin brauchen — einer Wortliste, kein
neuer Spracherkenner:
```python
ENGLISCH_ANKER = ('the', 'and', 'you', 'is', 'are', 'to', 'we', 'i', 'for', 'with')
ENGLISCH_MIN_TREFFER = 5
```
```python
def _ist_englisch(norm: str) -> bool:
    """Grobe Sprach-Weiche ueber Funktionswoerter — KEIN Spracherkenner, bewusst simpel.

    Nur die Regel reason_for_call braucht sie: sie ist die einzige Regel, die auf ABWESENHEIT
    prueft und wuerde auf jedem deutschen Anruf feuern. Auf einem deutschen Transkript ist das
    Kriterium nicht VERLETZT, sondern NICHT ANWENDBAR — der Unterschied ist der ganze Punkt.

    Die drei anderen Regeln brauchen den Riegel NICHT: sie zaehlen englische Woerter, kommen auf
    Deutsch also von selbst auf 0 (SPEC NACHTRAG 2 ①).

    GRENZE, ehrlich benannt: ein deutscher Anruf mit vielen englischen Fachwoertern kann den
    Riegel passieren. Fehlerrichtung bewusst — er nennt dann eine zutreffende englische Regel.
    """
    return sum(1 for w in ENGLISCH_ANKER if _zaehle(w, norm) > 0) >= ENGLISCH_MIN_TREFFER
```

**F) Fragen-Kanal-Eintrag SOFORT schreiben und committen** (Vault-Regel Nr. 4). Ans **Ende** von
`.planning/DIALOG-GSD-CLAUDIAN.md`:

```markdown
## FRAGE — 08.23.2.METRIK-1 Plan 04 — 2026-08-13

**Wo ich stehe:** `services/fokus_katalog.py`, Regel `reason_for_call` (Katalog-Punkt A1).

**Die Frage:** Drei der vier Katalog-Punkte zaehlen englische Woerter und kommen auf einem
deutschen Anruf von selbst auf null — genau wie es NACHTRAG 2 ① beschreibt. **A1 ist anders:**
Er prueft, ob der Grund des Anrufs frueh genannt wurde, also auf **Abwesenheit**. Ohne Zusatz
wuerde er bei **jedem** deutschen Anruf feuern und damit den „diesmal nichts"-Zweig (D-10) aus
dem Normalfall in den Ausnahmefall verwandeln — der Berater bekaeme eine englische Regel
vorgehalten, die auf Deutsch gar nicht gilt.

**Optionen:**
1. **(gebaut, meine Empfehlung)** A1 bekommt einen **Sprach-Riegel**: er feuert nur, wenn das
   Transkript ueber eine Funktionswort-Liste als englisch erkannt wird. Auf Deutsch ist das
   Kriterium dann *nicht anwendbar* statt *verletzt*. Kein neuer Spracherkenner — dieselbe
   Wortlisten-Maschinerie wie die anderen Regeln.
2. A1 faellt aus v1 raus; der Katalog hat drei Punkte. SPEC Requirement 7 laesst das
   ausdruecklich zu („wird beim Bau klar, dass auch vier zu viel sind, ueber den Fragen-Kanal
   melden, nicht still entscheiden") — deshalb dieser Eintrag.
3. A1 bleibt ohne Riegel. ⛔ Ich rate ab: er feuert dann auf jedem deutschen Anruf und verdraengt
   den ehrlichen Nichts-Zweig.

**Was blockiert ist:** Nichts. Option 1 ist gebaut und abschaltbar (eine Konstante). Plan 04
laeuft weiter; die Antwort entscheidet nur, ob A1 in v1 bleibt.
```
**Sofort committen.** Dieselbe Frage zusaetzlich als normaler Fliesstext im Terminal ausgeben —
**nie** als interaktives Menue.
  </action>

  <verify>
    <automated>grep -c "def waehle_fokus" services/fokus_katalog.py</automated>
    <automated>grep -c "anthropic\|claude_service\|messages.create" services/fokus_katalog.py</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "def waehle_fokus" services/fokus_katalog.py` liefert **1**;
  `grep -c "def _zaehle\|def _normalize\|def _ist_englisch" services/fokus_katalog.py`
  liefert **3**.
- **D-08 mechanisch belegt — kein Modell im Modul:**
  `grep -c "anthropic\|claude_service\|messages.create\|MODEL_" services/fokus_katalog.py`
  liefert **0**. Gepaarter Existenz-Anker: `grep -c "^import re" services/fokus_katalog.py`
  liefert **1** (die Datei wurde gelesen und hat ueberhaupt Importe).
- **Wortgrenzen statt Teilstring (D-09):**
  `grep -c "(?<!\\\\w)" services/fokus_katalog.py` liefert **>= 1**;
  Abwesenheits-Anker auf die falsche Form:
  `grep -c "phrase in norm\|marker in norm" services/fokus_katalog.py` liefert **0**.
- **Genau vier Fokus-Schluessel:**
  `grep -c "'negative_phrases'\|'we_not_i'\|'problem_language'\|'reason_for_call'" services/fokus_katalog.py`
  liefert **>= 4**, und `FOKUS_SCHLUESSEL` hat **genau 4** Eintraege.
- **Streichliste ist nicht drin — Abwesenheits-Anker mit gepaartem Existenz-Anker:**
  `grep -ci "i think\|did i catch you at a bad time\|filler\|tonfall\|fragenanzahl" services/fokus_katalog.py`
  zaehlt die **Streichlisten-Nennungen im Katalog-Code**. ⚠ Der Modul-Docstring nennt die
  Streichliste ausdruecklich als „nicht enthalten" — deshalb ist der Anker **auf den Code-Teil
  unterhalb des Docstrings** zu ziehen und die Zahl im SUMMARY mit der Fundstelle zu
  begruenden. **Verbindlich pruefbar statt zaehlend:** in `BUZZWORDS`, `NEGATIVE_PHRASES`,
  `ICH_WOERTER`, `WIR_WOERTER` und `REASON_PHRASES` steht **kein** Streichlisten-Eintrag —
  der Executor listet die fuenf Tupel im SUMMARY **vollstaendig** auf.
  ⛔ `grep -ciF` ist **verboten** (stuerzt auf Andres Git-Bash ab und liefert nichts statt `0`).
- **Sorte B und C sind nicht drin:**
  `grep -ci "redeanteil\|sprechtempo\|wpm\|words per minute\|monolog\|pause" services/fokus_katalog.py`
  liefert **0** ausserhalb des „NICHT ENTHALTEN"-Absatzes im Docstring; der Executor belegt das
  mit der Fundstellen-Liste.
- **Alle nutzersichtbaren Texte englisch (D-11):** die vier Schablonen enthalten **keine**
  Umlaute und kein deutsches Wort. Pruefbar:
  `grep -n "top reps" services/fokus_katalog.py` liefert **4** Zeilen (bzw. 5 mit der
  Rabatt-Sonderform) — jede davon ein vollstaendig englischer Satz.
- **Die Schluessel sind ASCII:**
  `grep -c "ä\|ö\|ü\|ß" services/fokus_katalog.py` — Treffer sind **nur** im Docstring/den
  Kommentaren erlaubt, **nicht** in Identifiern oder Schablonen. Der Executor belegt das mit
  `grep -n`.
- Der Fragen-Kanal-Eintrag steht am **Ende** von `.planning/DIALOG-GSD-CLAUDIAN.md` und ist
  **committet**: `grep -c "## FRAGE — 08.23.2.METRIK-1 Plan 04" .planning/DIALOG-GSD-CLAUDIAN.md`
  liefert **1**.
- Der Modul-Docstring enthaelt den Absatz „vier Schablonen, fuenf Saetze" (die Rabatt-Sonderform)
  und den Absatz „BELEGT vs. GESETZT".
  </acceptance_criteria>

  <done>
Ein reines Regel-Modul waehlt bei gleichem Transkript deterministisch dieselbe eine Sache, zieht
den Beleg woertlich aus dem Transkript, formuliert englisch — und sagt ehrlich „nichts", wenn
nichts verletzt ist.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zaehlregel-Tests an gebauten Transkripten — ERST ROT (D-09, Ersatz fuer Kriterium d)</name>

  <files>
tests/test_fokus_katalog.py (NEU)
  </files>

  <read_first>
- `services/fokus_katalog.py` (aus Task 1) — **ganze Datei**.
- `tests/test_judge_runner.py:105-108`, `:169-173` — das Fake-Segment-Muster und die Form
  „Assertions auf den gebauten String".
- SPEC Abnahme-Kriterien, der Kasten **„ERSATZ FUER (d)"**: Wortgrenzen · Platzhalter
  `[PERSON_A]` · Gross-/Kleinschreibung · die EWB-Zeile · die „ab 4×"-Schwelle **exakt auf der
  Kante**.
  </read_first>

  <action>
**Vorgehen: ERST ROT.** Die Tests werden geschrieben und gegen den Stand **vor** Task 1
angesetzt bzw. — wenn Task 1 schon steht — einmal mit absichtlich falscher Erwartung
gegengeprueft, damit belegt ist, dass sie ueberhaupt fehlschlagen **koennen**. Der ROT-Lauf geht
**verbatim** ins SUMMARY (Vault-Regel Nr. 1, und SPEC verlangt es fuer genau diese Tests
ausdruecklich).

**Warum diese Tests das gestrichene Kriterium (d) ersetzen:** (d) haette geprueft, ob das vom
Modell genannte Kriterium im Transkript nachweisbar verletzt ist. Beim Rechenweg prueft sich das
**selbst** — der Code nennt nur, was er selbst gezaehlt hat. Ein Waechter, der immer ja sagt.
Was **nicht** selbstpruefend ist, sind die **Zaehlregeln**. Genau die stehen hier.

Pflicht-Testfunktionen (jede Kante eine eigene Funktion, **keine** Schleife ueber die Kanten):

1. `test_wortgrenze_we_trifft_nicht_in_week` — Text mit `'next week we are'`; die
   `we_not_i`-Zaehlung sieht **ein** `we`, nicht zwei. Assertion auf `_zaehle('we', ...)` == 1.
2. `test_wortgrenze_i_trifft_nicht_in_it` — `_zaehle('i', 'it is inside')` == 0.
3. `test_grossschreibung_egal` — `'We Provide'` und `'we provide'` zaehlen gleich.
4. `test_platzhalter_erzeugt_keine_treffer` — Transkript mit `[PERSON_A]`, `[ORG_C]`,
   `[nicht gespeichert]`; `waehle_fokus` liefert **None** (kein Kriterium verletzt), und
   insbesondere feuert die Firmenname-Teilregel **nicht**, wenn `firmenname='Acme'` uebergeben
   wird, der Name im Text aber als `[ORG_C]` steht.
5. `test_ewb_zeile_zaehlt_nicht` — ein uebergebener Text `'we provide *ewb button*'`, viermal;
   `waehle_fokus` liefert **None**, weil die EWB-Zeilen im Modul defensiv gefiltert werden.
   **Gepaarter Positiv-Test in derselben Funktion:** dieselben vier Texte **ohne** das Suffix
   liefern den Fokus `negative_phrases` — sonst waere „None" nicht von „Funktion tut nichts" zu
   unterscheiden.
6. `test_schwelle_drei_mal_loest_nicht_aus` — `'we provide'` genau **3×** verteilt auf drei
   Texte → `waehle_fokus` liefert **None**.
7. `test_schwelle_vier_mal_loest_aus` — `'we provide'` genau **4×** → `focus_key ==
   'negative_phrases'`, `count == 4`, und der Satz enthaelt woertlich `top reps cap it at 3`.
   **Test 6 und 7 sind die Kante aus D-09 und stehen bewusst als ZWEI Funktionen.**
8. `test_beleg_ist_woertlich_aus_dem_transkript` — der zurueckgegebene `beleg` ist **identisch**
   mit einem der uebergebenen Texte (`assert ergebnis['beleg'] in texte`). **Das ist der Test
   fuer „per Bauart echt, nicht per Pruefung".**
9. `test_deterministisch` — `waehle_fokus` zweimal mit derselben Eingabe → identische Dicts.
10. `test_kein_kriterium_verletzt_liefert_none` — ein sauberer englischer Anruf, der den
    Anruf-Grund im ersten Satz nennt und keine Negativ-Phrase enthaelt → **None**.
    **Das ist der „diesmal nichts"-Normalfall (D-10).**
11. `test_deutscher_anruf_liefert_none` — ein realistischer deutscher Cold-Call-Text
    (mehrere Saetze, keine englischen Woerter) → **None**. **Das ist der Beleg fuer
    NACHTRAG 2 ①** und zugleich fuer den Sprach-Riegel: ohne ihn wuerde
    `reason_for_call` hier feuern.
12. `test_englischer_anruf_ohne_anruf_grund_loest_reason_for_call_aus` — englischer Text ohne
    `the reason for my call`, ohne Negativ-Phrasen, ohne Buzzwords, mit ausgeglichenem we/I →
    `focus_key == 'reason_for_call'`. **Der Gegenbeweis zu 11:** der Riegel sperrt nicht
    generell, er sperrt nur die falsche Sprache.
13. `test_rangfolge_negative_phrases_schlaegt_we_not_i` — ein Text, der **beide** Regeln
    verletzt → `focus_key == 'negative_phrases'`.
14. `test_absolutely_und_perfect_werden_addiert` — 3× `absolutely` + 1× `perfect` → loest aus
    (Summe 4); 2× + 1× → loest **nicht** aus (Summe 3).
15. `test_leere_eingabe_liefert_none` — `waehle_fokus([])` → **None**, kein Fehler.

**Verboten in dieser Datei** (Test-Qualitaets-Regel): `inspect.getsource`, `hasattr`-Existenz-
Pruefungen, Datei-Reads auf `services/fokus_katalog.py`. Alle Assertions sind
**Rueckgabewert**-Assertions.
  </action>

  <verify>
    <automated>grep -c "^def test_" tests/test_fokus_katalog.py</automated>
    <automated>grep -c "inspect.getsource\|hasattr(" tests/test_fokus_katalog.py</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "^def test_" tests/test_fokus_katalog.py` liefert **15**.
- `grep -c "inspect.getsource\|hasattr(\|open('services" tests/test_fokus_katalog.py` liefert
  **0**; gepaarter Existenz-Anker: `grep -c "assert " tests/test_fokus_katalog.py` liefert
  **>= 30**.
- Die Schwellen-Kante steht als **zwei** Funktionen:
  `grep -c "def test_schwelle_drei_mal_loest_nicht_aus\|def test_schwelle_vier_mal_loest_aus" tests/test_fokus_katalog.py`
  liefert **2**.
- `grep -c "ergebnis\['beleg'\] in texte" tests/test_fokus_katalog.py` liefert **>= 1**
  (der Woertlichkeits-Test).
- Der **ROT-Lauf** steht **verbatim** im SUMMARY, mit der Zahl der fehlgeschlagenen Tests
  **vor** dem Fix und `0 failed` danach.
  </acceptance_criteria>

  <done>
Die fuenf Kanten aus D-09 sind einzeln festgenagelt, die „ab 4×"-Schwelle exakt auf der Kante,
und der Beleg-Satz ist als woertlich aus dem Transkript stammend bewiesen — nicht behauptet.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Andre entscheidet ueber A1 (reason_for_call) — Fragen-Kanal-Antwort abwarten</name>

  <read_first>
- `.planning/DIALOG-GSD-CLAUDIAN.md` — **Datei-Ende neu lesen.** Die Antwort kommt asynchron als
  `### ANTWORT — Claudian` bzw. von Andre direkt. **Vor dem naechsten Schritt das Datei-Ende
  erneut lesen** — Antworten kommen nicht ueber das Terminal.
  </read_first>

  <decision>
Bleibt Katalog-Punkt **A1 (`reason_for_call`)** in v1 — und wenn ja, mit dem gebauten
Sprach-Riegel?
  </decision>

  <context>
Drei der vier Katalog-Punkte zaehlen **englische** Woerter und kommen auf einem deutschen Anruf
von selbst auf null — genau wie NACHTRAG 2 ① es beschreibt. **A1 ist anders:** er prueft auf
**Abwesenheit** und wuerde ohne Zusatz bei jedem deutschen Anruf feuern. Damit waere der
„diesmal nichts"-Zweig (D-10) nicht mehr der Normalfall, sondern die Ausnahme — und der Berater
bekaeme eine englische Regel vorgehalten, die auf Deutsch nicht gilt.

SPEC Requirement 7 verlangt fuer genau diesen Fall den Fragen-Kanal: *„wird beim Bau klar, dass
auch vier zu viel sind, ueber den Fragen-Kanal melden, nicht still entscheiden."*
  </context>

  <options>
    <option id="riegel">
      <name>A1 bleibt, mit Sprach-Riegel (gebaut, Empfehlung)</name>
      <pros>Vier Punkte wie festgezurrt · auf Deutsch „nicht anwendbar" statt „verletzt" · kein neuer Spracherkenner, dieselbe Wortlisten-Maschinerie · bei Andres drei englischen Test-Anrufen voll wirksam</pros>
      <cons>Der Riegel ist eine Heuristik: ein deutscher Anruf mit vielen englischen Fachwoertern kann ihn passieren (Fehlerrichtung bewusst — er nennt dann eine zutreffende englische Regel)</cons>
    </option>
    <option id="raus">
      <name>A1 faellt aus v1, Katalog hat drei Punkte</name>
      <pros>Keine Heuristik im Katalog · alle verbleibenden Regeln sind reine Praesenz-Zaehlungen · SPEC laesst es ausdruecklich zu</pros>
      <cons>Der Punkt mit dem **groessten** belegten Einzeleffekt (2,1×) faellt weg · A1 muesste spaeter nachgebaut werden</cons>
    </option>
    <option id="ohne_riegel">
      <name>A1 bleibt ohne Riegel</name>
      <pros>Einfachster Code</pros>
      <cons>⛔ Feuert auf JEDEM deutschen Anruf · verdraengt den ehrlichen „diesmal nichts"-Zweig · widerspricht D-10 direkt</cons>
    </option>
  </options>

  <acceptance_criteria>
- Die Antwort steht als `### ANTWORT` am Ende von `.planning/DIALOG-GSD-CLAUDIAN.md`.
- Bei `raus`: `FOKUS_SCHLUESSEL` hat **3** Eintraege, `reason_for_call`, `REASON_PHRASES`,
  `ENGLISCH_ANKER` und `_ist_englisch` sind **restlos entfernt**
  (`grep -c "reason_for_call\|_ist_englisch" services/fokus_katalog.py` liefert **0**;
  gepaarter Existenz-Anker: `grep -c "def waehle_fokus" services/fokus_katalog.py` liefert
  **1**), und die Tests 12 und 11 sind entsprechend angepasst.
- Bei `riegel`: keine Aenderung; die Entscheidung wird im SUMMARY festgehalten.
- **In jedem Fall** steht die gewaehlte Option mit Begruendung im SUMMARY und in
  `.planning/DIALOG-GSD-CLAUDIAN.md`.
  </acceptance_criteria>

  <resume-signal>
Auswaehlen: `riegel`, `raus` oder `ohne_riegel`.
  </resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21)

**Dieser Plan liest und schreibt KEINE Daten.** `services/fokus_katalog.py` ist eine reine
Transform-Schicht: Eingabe sind Zeichenketten, Ausgabe ist ein Dict oder `None`. Es gibt keinen
DB-Zugriff, keine Datei-I/O, keinen Netzwerk-Aufruf und keinen Modul-Zustand.

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `berater_texte` | Parameter | **keine** — RAM, vom Aufrufer uebergeben | n/a (der Aufrufer in Plan 05 laeuft hinter dem bestehenden `transcript_resolved`-Tor, Punkt 26) | ✓ per Bauart |
| `firmenname` | Parameter | in Plan 05 aus dem Profil aufgeloest | n/a | ✓ per Bauart |
| Rueckgabe-Dict | Rueckgabewert | in Plan 05 nach `observations_jsonb['_fokus']` | Plan 05 | ✓ dort |

Pruefbar: `grep -c "get_session\|db.query\|open(\|requests\.\|socket" services/fokus_katalog.py`
liefert **0**; gepaarter Existenz-Anker: `grep -c "^import re" services/fokus_katalog.py`
liefert **1**.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

## Verbindungs-Karte (Punkt 22)

| Neuer Identifier | Kandidat schon vergeben? | Beleg (vom Executor erneut zu ziehen) |
|---|---|---|
| `services/fokus_katalog.py` | nein | `grep -rn "fokus_katalog" .` → **0** |
| `waehle_fokus` | nein | `grep -rn "waehle_fokus" services/ routes/` → **0** |
| `FOKUS_SCHLUESSEL` / `KATALOG_VERSION` | nein | `grep -rn "FOKUS_SCHLUESSEL\|KATALOG_VERSION" .` → **0** |
| Fokus-Begriff im Bestand | **nicht vorhanden** | SPEC Req 6 „Current: Es gibt keinen Fokus-Begriff im Code" — vom Kartierer per grep bestaetigt. ⚠ **Achtung, Namens-Kollision:** `focus_match` existiert bereits in `routes/app_routes.py` (`_cross_context_objections_live`/`_training`) und bedeutet dort etwas **anderes** (der Einwand-Typ). **Nicht wiederverwenden, nicht verwechseln.** |
| `observations_jsonb`-Schluessel `_fokus` (Plan 05) | frei | belegt ist nur `_compliance` (`judge_runner.py:280-282`) |

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Die Auswahl der einen Sache trifft der Code aus gezaehlten Belegen —
   nicht Andre, und ausdruecklich **nicht** das Modell.
2. **Nachvollziehen:** Der gewaehlte Fokus wird in Plan 05 mit Schluessel, Zahl und Beleg-Satz in
   `observations_jsonb` gespeichert — die Grundlage der spaeteren Serien-Zaehlung.
3. **Eingreifen koennen:** Jede Schwelle ist eine Modul-Konstante; die Rangfolge ist die
   Reihenfolge eines Tupels. Eine Regel abzuschalten heisst, einen Tupel-Eintrag zu entfernen —
   deutlich unter 30 Sekunden.
4. **Marge:** Der Katalog ist die erste Scheibe des US-Coaching-Gehirns. Ohne ihn muesste ein
   Mensch nach jedem Anruf entscheiden, welche eine Sache der Berater beim naechsten Mal anders
   machen soll.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Transkript-Text (anonymisiert) → Zaehlregeln | Nutzer-erzeugter Text als Eingabe einer Regex-Maschine |
| Regel-Ausgabe → Nutzer-Bildschirm | Der erzeugte Satz wird einem Kunden als Coaching gezeigt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-METRIK1-04-01 | Tampering | erfundene „eine Sache" | **mitigate** | Der Satz entsteht ausschliesslich aus einer Schablone plus einer selbst gezaehlten Zahl; der Beleg ist per Bauart ein uebergebener Text (Test 8). Ein Modell ist nicht beteiligt (grep-Anker auf `anthropic`). |
| T-METRIK1-04-02 | Denial of Service | Regex auf sehr langem Transkript | **accept** | Alle Muster sind literale Phrasen ueber `re.escape` — kein Backtracking-Risiko (kein `*`/`+` auf Nutzer-Eingabe). Der Aufruf laeuft in der Slow Lane. |
| T-METRIK1-04-03 | Information Disclosure | Beleg-Satz enthaelt personenbezogene Daten | **accept** | Der Text ist bereits anonymisiert, bevor er in `transcript_segments` steht (Pipeline B). Der Beleg wird nur dem **eigenen** Berater gezeigt — die Route filtert auf `CL.user_id == g.user.id`. |
| T-METRIK1-04-04 | Spoofing | EWB-Knopfdruck erzeugt einen Fokus | **mitigate** | Defensiver Zweitfilter im Modul (`EWB_MARKER`), Test 5 mit gepaartem Positiv-Fall. |
</threat_model>

<verification>
- Alle `<acceptance_criteria>` der drei Tasks sind erfuellt.
- Der ROT-Lauf der Zaehlregel-Tests steht verbatim im SUMMARY.
- Der Fragen-Kanal-Eintrag ist geschrieben **und committet**, und die Antwort ist eingearbeitet.
- **Kein Deploy in diesem Plan** — das Modul geht mit Plan 05 live.
</verification>

<success_criteria>
- `waehle_fokus` liefert bei gleichem Transkript deterministisch dasselbe Ergebnis.
- Der Beleg-Satz ist woertlich einer der uebergebenen Texte.
- Auf einem deutschen Anruf ohne Katalog-Verstoss kommt `None` heraus — der „diesmal
  nichts"-Normalfall.
- Der Katalog enthaelt genau die vier (bzw. nach Andres Entscheidung drei) Wortlisten-Punkte,
  alle Texte englisch, kein Eintrag der Streichliste und keiner der Sorten B oder C.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-04-SUMMARY.md` anlegen.
**Pflicht-Inhalte:**
- ROT-Lauf der Zaehlregel-Tests **verbatim**.
- Die **vollstaendige Auflistung** der fuenf Wortlisten-Tupel als Beleg, dass kein
  Streichlisten-Eintrag und kein Sorte-B/C-Eintrag darin steht.
- Die Entscheidung aus Task 3 mit Begruendung.
- **Bekannte Grenze, ausdruecklich:** die Firmenname-Teilregel schlaegt selten an, weil der
  Firmenname im gespeicherten Transkript anonymisiert als `[ORG_X]` steht.
- **Bekannte Grenze, ausdruecklich:** `ICH_SCHWELLE` und `BUZZWORD_SCHWELLE` sind **gesetzt**,
  nicht belegt — Nachjustierung nach rund hundert echten Anrufen.
- **Restluecke aus dem SPEC, woertlich zu uebernehmen:** „Ist es die WICHTIGSTE eine Sache?"
  bleibt ungeprueft. Termin: sobald der Fokus-Kreislauf laeuft und ~100 echte Anrufe vorliegen.
- Pruning-Notiz: dieser Plan macht nichts obsolet (reiner Neubau).
</output>
