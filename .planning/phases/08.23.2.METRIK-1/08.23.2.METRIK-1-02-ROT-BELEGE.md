# Plan 02 — Ausfuehrungs-Belege (ZWISCHENSTAND, NICHT das SUMMARY)

⚠ **Das ist NICHT `08.23.2.METRIK-1-02-SUMMARY.md`.** Plan 02 ist **nicht abgeschlossen**:
Task 3 (das eine Ausrollen) steht offen und ist ein Halt-Punkt fuer Andre. Diese Datei sichert
die Pflicht-Belege, die spaeter **verbatim** ins SUMMARY gehoeren, damit sie einen
Kontext-Schnitt ueberleben.

Stand: 2026-08-14, HEAD `e70c8a2`, gepusht.

---

## Verbindungs-Karte — frisch gezogen VOR der ersten Aenderung

Entdeckungs-greps ohne Sollwert (kein Abnahme-Gate). **Alle neuen Namen waren frei.**

| Gesuchter Identifier | Treffer vorher |
|---|---|
| `MIN_BERATER_WOERTER` | **0** |
| `_TOR_ZWEIG_` | **0** |
| `wortzahl_unbekannt_durchgelassen` | **0** |
| `MIN_REDEABSCHNITTE` | **0** (wird auch nicht angelegt) |
| `_segments_for_call` (services/ routes/ tests/) | **0** |
| `_mess_sprech_substanz` | **0** |
| `_tor_sprech_substanz` | **0** |
| `too_little_speech` | **0** |

`MIN_HIGH_CONFIDENCE_EVENTS` **vor** der Loeschung — genau die vier erwarteten Leser in
`config.py`/`services/`, **kein vierter Leser**, Loeschung also zulaessig:

```
config.py:305:# MIN_HIGH_CONFIDENCE_EVENTS: D-09 "zu wenig hoch-konfidente Events" — weniger als N Events
config.py:309:MIN_HIGH_CONFIDENCE_EVENTS = int(os.getenv('MIN_HIGH_CONFIDENCE_EVENTS', '3'))
services/slow_lane.py:729:    from config import (AUDIO_HEALTH_GATE_THRESHOLD, MIN_HIGH_CONFIDENCE_EVENTS,
services/slow_lane.py:773:        elif n_high_conf < MIN_HIGH_CONFIDENCE_EVENTS:
tests/test_coaching_score_cutover.py:18:        - < MIN_HIGH_CONFIDENCE_EVENTS hoch-konf.  -> status='not_gradable', KEIN Judge-Call
tests/test_coaching_score_cutover.py:230:    # Gutes Audio, aber nur 1 hoch-konfidentes Event (< MIN_HIGH_CONFIDENCE_EVENTS=3).
```

## Prod-Kopf, read-only erneut gezogen (VOR der Migration)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
 version_num
-------------
 0039
(1 row)
```

→ `down_revision = '0039'` in `alembic/versions/0040_metrik1_schilder.py` ist **belegt**, nicht
geraten.

---

## ROT-Lauf 1 — `tests/test_substanz_tor.py` gegen den ungefixten Stand (Task 1)

Gezogen ueber `scripts/triage.sh` auf dem Server, **bevor** die Funktionen existierten.
Echte Test-Fehler (`AttributeError`), **kein Werkzeug-Ausfall** (Rezept-Bedingung 4).

```
tests/test_substanz_tor.py:240: in test_sprechzeit_unbekannt_ist_none_nicht_null
    mess = sl._mess_sprech_substanz(segments)
           ^^^^^^^^^^^^^^^^^^^^^^^^
E   AttributeError: module 'services.slow_lane' has no attribute '_mess_sprech_substanz'
__________________________ test_audio_tor_hat_vorrang __________________________
tests/test_substanz_tor.py:318: in test_audio_tor_hat_vorrang
    seen = merge_doubles(call, segments=[_berater(2, idx=1)])
tests/test_substanz_tor.py:300: in _lauf
    monkeypatch.setattr(sl, '_segments_for_call', lambda clid, db: segments)
E   AttributeError: <module 'services.slow_lane' from '/opt/nerve/app/services/slow_lane.py'> has no attribute '_segments_for_call'
...
======================= 12 failed, 12 warnings in 8.86s ========================
[triage] FEHLER: pytest gegen nerve_test ROT (kein Restart, kein Deploy)
```

## ROT-Lauf 2 — `tests/test_coaching_score_cutover.py` nach A–C (Task 1, Abschnitt E1)

**Erwartet rot durch Vertragswechsel D-22, im selben Task retargetiert — kein Fund, keine
Regression.** Der Radius war genau der vom Plan vorhergesagte: **nicht nur der eine** Test,
sondern **jeder** Merge-Test, der den Tor-Block erreicht, lief in den ungepatchten Lade-Helfer.

```
tests/test_coaching_score_cutover.py:507: in test_good_audio_not_marked_poor
    sl._call_end_merge({'call_id': call.id})
services/slow_lane.py:925: in _call_end_merge
    segments = _segments_for_call(call.conversation_log_id, read_db)
services/slow_lane.py:426: in _segments_for_call
    .order_by(TranscriptSegment.ts_ms.asc(), TranscriptSegment.id.asc())
E   AttributeError: '_MergeFakeSession' object has no attribute 'order_by'

FAILED tests/test_coaching_score_cutover.py::test_merge_fires_when_ended_and_zero_pending
FAILED tests/test_coaching_score_cutover.py::test_merge_fires_with_terminal_events_only
FAILED tests/test_coaching_score_cutover.py::test_audio_gate_null_health_marks_not_gradable
FAILED tests/test_coaching_score_cutover.py::test_audio_gate_low_health_marks_not_gradable
FAILED tests/test_coaching_score_cutover.py::test_too_few_high_confidence_events_marks_not_gradable
FAILED tests/test_coaching_score_cutover.py::test_daemon_write_sets_tenant_guc
FAILED tests/test_coaching_score_cutover.py::test_daemon_clears_tenant_on_exception
FAILED tests/test_coaching_score_cutover.py::test_merge_skips_when_tenant_id_null
FAILED tests/test_coaching_score_cutover.py::test_daemon_write_failure_requeues_with_cap
FAILED tests/test_coaching_score_cutover.py::test_daemon_write_failure_dead_letters_after_cap
FAILED tests/test_coaching_score_cutover.py::test_daemon_write_failure_not_marked_done
FAILED tests/test_coaching_score_cutover.py::test_daemon_survives_step_exception
FAILED tests/test_coaching_score_cutover.py::test_compute_step_scored_upserts_engine_result
FAILED tests/test_coaching_score_cutover.py::test_no_buffer_sets_resolved_true
FAILED tests/test_coaching_score_cutover.py::test_good_audio_not_marked_poor
================== 16 failed, 22 passed, 12 warnings in 9.36s ==================
```

## ROT-Lauf 3 — der stale Plan-01-Test (Deviation Rule 3, **ein Fund**)

`test_compute_step_scored_upserts_engine_result` war **schon vor diesem Plan** rot und haette
Task 3 blockiert: seit dem Zitat-Pruefer (Plan 01) laeuft das Bewerter-Ergebnis nicht mehr
unveraendert in den UPSERT.

```
tests/test_coaching_score_cutover.py:482: in test_compute_step_scored_upserts_engine_result
    assert captured['observations'] == fake_judge_result['observations_jsonb']
E   AssertionError: assert {'_compliance...ung': [], ...} == {'_compliance...ng': 'Gut.'}]}
E     Differing items:
E     {'bedarfs_ermittlung': []} != {'bedarfs_ermittlung': [{'beleg_zitat': 'Wie geht das?', 'beobachtung': 'Gut.'}]}
E     Left contains 3 more items:
E     {'einwand_behandlung': [], 'gespraechs_eroeffnung': [], 'gespraechsfuehrung': []}
```

## ROT-Lauf 4 — Ablehnungs-Payload (Task 2)

```
tests/test_substanz_tor.py:363: in test_ablehnungs_payload_traegt_alle_messwerte
    assert payload['berater_woerter'] == 12
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
E   KeyError: 'berater_woerter'
```

---

## GRUEN-Laeufe (Server, `triage.sh` gegen `nerve_test`)

```
[triage] Argumente: tests/test_substanz_tor.py tests/test_coaching_score_cutover.py tests/test_judge_runner.py tests/test_beleg_pruefung.py tests/test_beleg_check_founder.py tests/test_session_detail_render.py tests/test_transkript_renderer.py -q
79 passed, 15 warnings in 11.24s
[triage] pytest gegen nerve_test bestanden — nerve_test wird per trap gedroppt
```

**Migration `0040` ist im Wegwerf-`nerve_test` wirklich gelaufen** — die Doppelpunkt-Falle ist
damit ausgeschlossen, ohne dass Production sie gesehen haette:

```
INFO  [alembic.runtime.migration] Running upgrade 0039 -> 0040, rubric_score: Schilder von status und payload_jsonb nachziehen (METRIK-1, Punkt 23)
[triage] Dump-Treue-Katalog-Gate OK: crm-Policies=7, FORCE=5, anon-SELECT-GRANTs=5
```

Schild-Waechter gegen die neuen Kommentare (ORM/DDL-Konsistenz, Punkt 23):

```
[triage] Argumente: tests/test_schild_guard.py -q
2 passed, 11 warnings in 8.59s
```

---

## Abnahme-Anker, ausgefuehrt (Task 1)

| Anker | Soll | Ist |
|---|---|---|
| `def _mess_sprech_substanz` / `_tor_sprech_substanz` / `_segments_for_call` | 1 / 1 / 1 | **1 / 1 / 1** |
| `MIN_REDEABSCHNITTE` (config.py, slow_lane.py) | 0 | **0 / 0** |
| `redeabschnitte <` | 0 | **0** |
| `'redeabschnitte':` (gepaarter Existenz-Anker) | >= 1 | **2** |
| `_TOR_ZWEIG_WORTZAHL_UNBEKANNT = 'wortzahl_unbekannt_durchgelassen'` | 1 | **1** |
| `(None, _TOR_ZWEIG_WORTZAHL_UNBEKANNT)` (zwei fail-open-Kanten) | 2 | **2** |
| `_TOR_ZWEIG_ZU_WENIG` | >= 2 | **2** |
| `elif n_high_conf <` | 0 | **0** |
| `n_high_conf = _count_high_confidence(` | 1 | **1** |
| `MIN_HIGH_CONFIDENCE_EVENTS` (config.py services/ routes/ tests/) | 0 | **0** |
| `AUDIO_HEALTH_GATE_THRESHOLD` in config.py | >= 2 | **2** |
| Audio-Tor-Zeile zeichengleich | 1 | **1** |
| `speaker == 'berater'` / `speaker == 0\|1` | >=1 / 0 | **2 / 0** |
| `TranscriptSegment.call_id` | 0 | **0** |
| `MIN_BERATER_WOERTER` in config.py | >= 1 | **1** |
| `too_few_high_confidence_events` (services/ tests/) | 0 | **0** |
| dito in `templates/session_detail.html` (Anzeige-Zweig bleibt) | 1 | **1** |
| alter Testname weg / neuer da / Umkehrung da | 0 / 1 / 1 | **0 / 1 / 1** |
| `_segments_for_call` im Doppel / `_count_high_confidence` noch da | >=1 / >=1 | **1 / 3** |
| `^def test_` in `tests/test_substanz_tor.py` | 14 | **14** |
| `inspect.getsource\|hasattr(` dort | 0 | **0** |
| `assert ` dort | >= 20 | **30** |
| `wortzahl_unbekannt_durchgelassen` dort / `keine_berater_zeile` dort | >=3 / >=1 | **3 / 2** |

## Abnahme-Anker, ausgefuehrt (Task 2)

| Anker | Soll | Ist |
|---|---|---|
| `'berater_woerter': _mess.get('berater_woerter')` | 1 | **1** |
| `payload={'reason': reason}` (Abwesenheit) | 0 | **0** |
| `status=_STATUS_NOT_GRADABLE` (gepaart) | 1 | **1** |
| `'mess_substanz': mess_substanz` | 1 | **1** |
| `mess_substanz = _mess_sprech_substanz(segments)` | 1 | **1** |
| `_mess_sprech_substanz(` | 2 | **2** |
| `'tor_zweig': _mess.get('tor_zweig')` | 1 | **1** |
| `'schema': 1` (gepaart) | >= 1 | **3** |
| `Bewertungs-Status. Werte judged` in models.py / 0040 | 1 / 1 | **1 / 1** |
| Zeichengleichheit beider Schild-Konstanten ORM vs. DDL | identisch | **identisch** (grep-Vergleich, beide Werte 1 distinkte Zeile) |
| `down_revision = '0039'` | 1 | **1** |
| `def downgrade` / `_SCHILD_STATUS_ALT\|_SCHILD_PAYLOAD_ALT` | 1 / >=4 | **1 / 4** |
| `def _comment` | 1 | **1** |
| `inspect.getsource\|open('services` in den neuen Tests | 0 | **0** |

### Zwei Anker waren FEHLMESSUNGEN des Plans (Anker folgt dem Artefakt)

| Anker | Plan-Soll | Ist | Warum das Kriterium falsch war |
|---|---|---|---|
| `exec_driver_sql` in `0040` | 1 | **3** | Der Plan schreibt im selben Task vor, den `_comment`-Helfer **verbatim inklusive des vollen Docstrings** aus `0039` zu uebernehmen — und dieser Docstring nennt `exec_driver_sql` zweimal. **Gegenprobe: `0039` selbst liefert ebenfalls 3.** Der Sollwert 1 waere nur erreichbar gewesen, indem man den vorgeschriebenen Docstring loescht. |
| `op.execute(` in `0040` | 0 | **1** | Derselbe verbatim uebernommene Docstring warnt woertlich *"NICHT auf op.execute(str) umstellen"*. **Gegenprobe: `0039` liefert ebenfalls 1.** Die **Absicht** des Ankers ist erfuellt: `op.execute` steht ausschliesslich in Prosa, es gibt **null** Aufrufstellen — die einzige DDL-Ausfuehrung ist `op.get_bind().exec_driver_sql(...)` (Zeile 72). |

Beide Sollwerte sind auf `>= 1` bzw. "nur in Prosa, keine Aufrufstelle" richtigzustellen.

---

## NACHLAUF nach dem ersten Deploy-Versuch (2026-08-15) — ein ECHTER Fund

Andres erster `bash deploy.sh production` lief **rot**: `1 failed, 1198 passed, 7 skipped,
5 deselected` an `tests/test_ownership_state_guard.py::test_kennung_aus_url_wird_besitzgeprueft`.
**Kein Neustart, Production unveraendert** — das Tor hat getan, wofuer es da ist.

### ROT-Lauf 5, selbst nachgezogen (verbatim)

```
    def test_kennung_aus_url_wird_besitzgeprueft():
        offen = _verstoesse('KENNUNG_AUS_URL')
>       assert not offen, (
E       AssertionError: Die Kennung kommt hier als URL-Routen-Parameter an, nicht ueber .get() — genau deshalb war sie fuer die Mengen 2-4 unsichtbar.
E           routes/admin_dashboard.py::beleg_check_fall (Zeile 712)
E       assert not [Fund(datei='routes/admin_dashboard.py', funktion='beleg_check_fall', zeile=712, gerufene=frozenset({'query', 'getattr... 'str', 'clear_current_tenant', 'asc', 'set_current_tenant', 'abort'}), server_identitaet=False, org_id_bindung=False)]
```

### Die Entscheidung: (b) — der Waechter hat RICHTIG angeschlagen und die FALSCHE Frage gestellt

Der Besitz-Waechter aus SOFORT-2 hat die **neue** Route aus Plan 01 Task 6 (`70d4e6e`) gefangen.
Das ist **kein staler Test** — er ist das Netz fuer genau solchen neuen Code und hat funktioniert.

**Warum (a) — Route reparieren — falsch waere:** Menge 5 fragt *„gehoert dieses Objekt dem
Anfragenden?"*. Bei `beleg_check_fall` gibt es dazu nichts zu pruefen: das **mandanten-
uebergreifende** Lesen ist der **Zweck** (D-23 Auflage 3 — der Founder muss jeden Anruf
nachpruefen koennen, sonst ist *„wird von einem NERVE-Mitarbeiter geprueft"* ein Versprechen ohne
Deckung). Ein `g.user.id`-Vergleich waere kein Fix, sondern ein **Feature-Bruch** — der Founder
besitzt die Anrufe nicht. Und eine blosse `g.*`-Erwaehnung einzubauen, um den Anker zu treffen,
waere der Anker-formt-das-Artefakt-Fehler.

**Warum die Founder-Weiche hier TRAEGT — belegt, nicht behauptet:** `superadmin_required`
(`services/auth_decorators.py:5-11`) bricht ohne `g.user.is_superadmin` mit `abort(403)` ab,
**bevor** der Rumpf laeuft. Ein fremder Nutzer erreicht die `call_id`-Aufloesung also gar nicht.
Fuer **diese** Route ist das **strenger** als eine `org_id`-Bindung, nicht schwaecher.
**Laufzeit-Beleg statt Prosa:**
`tests/test_beleg_check_founder.py::test_einzelfall_seite_ist_nicht_fuer_jeden` — ein
Nicht-Superadmin bekommt 302/401/403, nie 200.

⛔ **Die Einschraenkung ist der Kern:** Eine Founder-Rolle ist **keine** `org_id`-Bindung. Die
Ausnahme haelt **nur**, weil hier ausschliesslich Founder-Sicht-Daten aufgeloest werden. Deshalb
**kein pauschaler Dekorator-Freibrief**: haette ich `@superadmin_required` generell anerkannt,
waere der Dekorator zum Bypass-Token geworden und jede kuenftige Route, die Founder-Sicht mit
**nutzer-eigenen** Daten mischt, waere stillschweigend mit ausgenommen.

**Gebaut wurde deshalb (Punkt 31):**
1. Ein begruendeter Allowlist-Eintrag **pro Route** (`FALSCH-TREFFER:`-Praefix, Pflicht des
   Waechter-Docstrings).
2. Ein **neuer Hygiene-Test** `test_founder_ausnahmen_tragen_ihre_founder_weiche_noch`: er
   prueft **am AST**, dass die Weiche wirklich noch am Code haengt. Faellt `@superadmin_required`
   irgendwann weg, faellt der Eintrag auf statt still zu decken. Er traegt eine **eigene
   Gegenprobe** (kuenstliche Route mit/ohne Weiche durch dieselbe Ableitung), damit ein Ausfall
   der Ableitung **rot statt blind** wird.
3. **Pruefkatalog erweitert:** `FOUNDER-ONLY-KLASSE` als benannte Grenze in `RESTLUECKEN`, mit
   der **offenen Rest-Kante** — fuer diese eine Route sagt der Sweep jetzt nichts mehr, tragende
   Schicht ist der Verhaltens-Test. Dazu ehrlich vermerkt: die **uebrigen** Allowlist-Grunde
   bleiben Prosa, sie werden nicht nachgeprueft.

Commit `ce32cac`. **GRUEN danach:** `10 passed`, alle Funktionen einzeln `PASSED`; das
Mindest-Soll `MINDEST_KENNUNG_AUS_URL = 8` bleibt unangetastet (gezaehlt wird **vor** dem
Allowlist-Abzug).

### Gegen-Sweep (Auftrag: gibt es einen zweiten Fund derselben Sorte?)

**Nein.** `git diff` ueber Plan 01 + Plan 02 zeigt **genau eine** neue Route im ganzen
`routes/`-Ordner: `@admin_dashboard_bp.route('/beleg-check/<call_id>')`. Weil der Waechter eine
**Namens-Kante** hat (er kennt nur `sid`/`call_id`/`profile_id`), zusaetzlich ein unabhaengiger
Sweep ueber **alle** URL-Parameter-Namen in `routes/*.py`: die uebrigen Parameter in
`admin_dashboard.py` (`rate_id`, `fc_id`, `org_id`) sind **Bestand**, nicht aus dieser Phase, und
tragen **alle** `@superadmin_required` (Zeilen 785, 802, 891, 1013).

### Die BASELINE-AUTO-FIX-Warnung — Ursache gefunden, nicht weggeredet

Die Leak-Warnung ist **weder Plan-01-Defekt noch Folge des Fehlschlags**, sondern eine
**Werkzeug-Divergenz**, und sie beruehrt das Deploy-Tor **nicht**:

- `pytest.ini:5` markiert real-Haiku-Integrationstests als `live`; das Deploy-Tor faehrt
  `-m "not live"` — daher die **5 deselected** in Andres Ausgabe.
- Mein erster Voll-Lauf ueber `triage.sh` gab **keine** Marker mit → die 5 liefen, scheiterten an
  `invalid x-api-key`, und **ihr** Teardown erzeugte die Leak-Warnung.
  Namentlich: `tests/test_phase_classifier.py::test_phase_classifier_integration_real_haiku`.
- Isoliert laeuft `tests/test_beleg_check_founder.py` mit **5 passed** und **ohne** jede
  Leak-Warnung.

**Voll-Lauf mit der Auswahl des Deploy-Tors — die Vorhersage fuer Andres naechsten Versuch:**

```
[triage] Argumente: tests/ -q -m not live and not perf
1200 passed, 7 skipped, 5 deselected, 107 warnings in 76.34s (0:01:16)
[triage] pytest gegen nerve_test bestanden — nerve_test wird per trap gedroppt
```

Keine `BASELINE-AUTO-FIX`-Zeile. Die Arithmetik geht auf: Andres Lauf hatte
`1 failed + 1198 passed`; der Waechter ist jetzt gruen (+1) und der neue Hygiene-Test kommt
hinzu (+1) → **1200 passed, 0 failed**.

---

## Deviations (fuer das spaetere SUMMARY)

**1. [Rule 3 — Blocker] Stale Plan-01-Test `test_compute_step_scored_upserts_engine_result`.**
Rot **vor** diesem Plan, ausgeloest von Plan 01 (Zitat-Pruefer formt `observations` um; der Test
verglich weiter mit dem ROHEN Bewerter-Ergebnis). Er haette das **eine** Ausrollen blockiert —
rotes Tor heisst kein Neustart. Retargetiert: der Test stellt jetzt ein Segment bereit, das das
Beleg-Zitat wirklich enthaelt, und prueft damit den **Normalpfad** statt den Verwurf. Commit
`f149d50`. ⚠ Belegt zugleich, dass Plan 01 diese Testdatei nicht mitgelaufen ist.

**2. `MIN_BERATER_WOERTER` NICHT in die `_call_end_merge`-Importzeile aufgenommen.**
Der Plan sagt "rein". Die Schwelle liest aber `_tor_sprech_substanz` selbst (reine Funktion,
EINE Lese-Stelle); ein Import ohne Leser waere toter Ballast (Punkt 29). Ein Kommentar an der
Importzeile haelt die Entscheidung fest. Kein Abnahme-Anker beruehrt die Stelle.

**3. Die ctx-Zeile `'mess_substanz': mess_substanz` aus Task 2A nach Task 1 vorgezogen.**
Zwei Task-1-Tests (`test_audio_tor_hat_vorrang`, `test_too_little_speech_marks_not_gradable`)
haengen an diesem Schluessel. Waere er in Task 2 geblieben, waere Task 1 **rot committet**
worden. Der Anker `grep -c "'mess_substanz': mess_substanz"` = 1 ist unveraendert erfuellt.

**4. Testfunktion 12 (`test_audio_tor_hat_vorrang`) prueft den ECHTEN Kaskaden-Ablauf.**
Der Plan schlaegt vor, "die Kaskade aus dem Tor-Block nachzubauen". Eine im Test nachgebaute
Kopie bewiese nur, dass der Test rechnen kann — CLAUDE.md-Test-Qualitaets-Regel. Der Test ruft
stattdessen `_call_end_merge` mit Fake-Session und prueft den befuellten `ctx`.

**5. Pruning: die Inline-Segment-Abfrage aus Plan 01 in `_judge_step` ist entfallen.**
Sie war zeichengleich zu `_segments_for_call`. Jetzt EINE Lade-Form fuer beide Leser (Tor und
Pruef-Korpus) — sonst driften Filter/Sortierung auseinander und der Pruef-Korpus waere ein
anderer als der, ueber den das Tor entschieden hat.
`grep -c "TranscriptSegment.conversation_log_id ==" services/slow_lane.py` = **2**
(`_segments_for_call` + eine **andere**, aeltere Abfrage in `_naechste_berater_aussage`, die
zusaetzlich auf `speaker` und `created_at` filtert). ⚠ Der Plan nennt hier Soll **1** — das war
ebenfalls eine Fehlmessung, die Bestandsabfrage ist aelter als diese Phase.

**6. `MIN_REDEABSCHNITTE` wurde nie angelegt** — es gibt nichts zu pruningen, aber der
Verzicht gehoert benannt, damit ihn niemand fuer eine Auslassung haelt.

## Pruning-Notiz

- `MIN_HIGH_CONFIDENCE_EVENTS` **geloescht** (Konstante `config.py` + Prosa-Kommentar + Import
  in `services/slow_lane.py`) — nach dem Umbau **null** Leser im Produktivpfad (Bau-Regel 20).
- `_count_high_confidence` **bleibt**: die Zahl wandert als Messwert `high_conf_events` in die
  Ablehnungs-Zeile. SPEC Req 1 woertlich: Einwand-Momente sind nur noch *ein Signal unter
  mehreren*, nie das alleinige Tor.
- Die Plan-01-Inline-Segmentabfrage in `_judge_step` (siehe Deviation 5).
