---
phase: 08.23.2.METRIK-1
plan: 03
type: execute
wave: 3
depends_on: ["08.23.2.METRIK-1-02"]
files_modified:
  - templates/session_detail.html
  - routes/dashboard.py
  - tests/test_session_detail_render.py
autonomous: false
requirements: [SPEC-R4, SPEC-R1]
user_setup: []

must_haves:
  truths:
    - "Ein Anruf ohne bestaetigtes Ergebnis zeigt die KI-Einschaetzung."
    - "Ein unbestaetigter Anruf ist als solcher ehrlich markiert, statt seine Bewertung zu verstecken."
    - "Ein wegen zu wenig Sprech-Substanz abgewiesener Anruf bekommt die richtige Erklaerung, nicht 'Audio zu schlecht'."
    - "Alte Anrufe mit dem Grund too_few_high_confidence_events zeigen weiterhin ihre alte Erklaerung."
    - "Der Kommentar '(Leakage-Schutz)' steht nicht mehr im Code."
    - "Keine weitere Stelle im Code haengt die neue Rueckmeldung an den Bestaetigungs-Klick."
  artifacts:
    - path: "templates/session_detail.html"
      provides: "if/elif-Kette ohne Anzeige-Sperre, mit drittem Ablehnungs-Zweig und ehrlicher Markierung"
      contains: "too_little_speech"
    - path: "routes/dashboard.py"
      provides: "outcome_confirmed als Markierungs-Flag statt als Sperre — Kommentar richtiggestellt"
    - path: "tests/test_session_detail_render.py"
      provides: "ROT-vor-GRUEN-Beleg fuer die Entkopplung + Zweig-Tests"
  key_links:
    - from: "routes/dashboard.py::session_detail"
      to: "templates/session_detail.html"
      via: "outcome_confirmed wird weiter uebergeben — jetzt als Markierung, nicht als Gate"
      pattern: "outcome_confirmed="
    - from: "templates/session_detail.html"
      to: "rubric_score.payload_jsonb['reason']"
      via: "_reason-Weiche mit drei Zweigen (alt bleibt erhalten)"
      pattern: "_reason == 'too_little_speech'"
---

<objective>
Die Rueckmeldung erscheint, sobald sie fertig ist — nicht erst, wenn jemand das Gespraechsergebnis
bestaetigt. Und der neue Ablehnungs-Grund bekommt seine eigene, wahre Erklaerung.

**Purpose:** Belegt: conv 264 und conv 266 blieben **trotz** Sperre unbestaetigt. Die Sperre auf
der Auswertungsseite hat damit **nie einen Klick erzeugt** — sie versteckt nur Bewertungen. Sie
ist eine **zweite** Instanz einer Regel, deren richtiger Ort das Anruf-Fenster direkt nach dem
Auflegen ist.

**Output:** Auswertungsseite ohne Anzeige-Sperre, mit ehrlicher „Ergebnis nicht bestaetigt"-
Markierung und einem dritten Ablehnungs-Zweig.

## ⛔ Was hier NICHT passiert (D-20)

- **Die Pflicht-Wahl im Anruf-Fenster bleibt unangetastet.** Dort wirkt Andres Mai-Direktive
  („erst Ergebnis, dann Bewertung"), dort ist der Moment richtig. Dieser Plan fasst
  `static/pip-launcher.js` **nicht** an.
- **`calls.outcome` bleibt** — als Korrelations-Etikett, weiterhin browser-getriggert gesetzt.
- **D-21: Der Kommentar „(Leakage-Schutz)" ist ein Etiketten-Irrtum** und wird **richtiggestellt,
  nicht uebernommen**. Schutz des Bewerters ist es belegt **nicht**: der laeuft ergebnis-blind
  (`judge_runner.py:7`) und ist fertig, **bevor** die Sperre ueberhaupt greift
  (`slow_lane.py:593-630`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-SPEC.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-CONTEXT.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Die zu aendernde Kette, **verbatim** aus `templates/session_detail.html:131-149`:
```jinja
    {% if not outcome_confirmed %}
      {# (0) ANZEIGE-Sperre: Beobachtungen erst NACH Outcome-Bestätigung (Leakage-Schutz). #}
      <p>Gesprächsergebnis bestätigen, dann erscheint die Einschätzung.</p>
    {% elif rubric_preview and rubric_preview.status == 'judge_failed' %}
      {# (a) Judge-Fehler (Dead-Letter) #}
      <p>Auswertung fehlgeschlagen — wird automatisch geprüft.</p>
    {% elif rubric_preview and rubric_preview.status == 'not_gradable' %}
      {# (b) Audio-Gate / zu wenig Material #}
      <p>
        {% set _reason = (rubric_preview.payload_jsonb or {}).get('reason') %}
        {% if _reason == 'too_few_high_confidence_events' %}
          Zu wenig auswertbare Momente für eine Einschätzung.
        {% else %}
          Audio zu schlecht für eine Einschätzung.
        {% endif %}
      </p>
    {% elif not rubric_preview %}
      {# (d) row-absent: async-Judge noch nicht durch. #}
      <p>Einschätzung wird im Hintergrund ausgewertet …</p>
    {% else %}
```

Der Kopfkommentar der KI-Karte, **verbatim** aus `templates/session_detail.html:119-124`:
```jinja
  {# ── Section 2b: KI-Einschätzung / Coaching-Hinweis (TAXO2-05) ─────────────────────────────
     Beobachtungen statt Zahl (Soll-Verhalten §6 / Entscheidung.md).
     Edge-Reihenfolge: (0) ANZEIGE-Sperre outcome -> (a) judge_failed -> (b) not_gradable ->
     (d) row-absent -> (e) Beobachtungen + Beleg-Zitate (+ prominente Compliance-Notiz oben,
     Cross-AI-Finding 2). (c) kein-Material-Zweig ist in (e) integriert (leere observations).
     Kein hardcoded Hex, keine Inline-Styles — nur .n-*-Klassen + CSS-Variablen (CLAUDE.md). #}
```

Die Route-Seite, **verbatim** aus `routes/dashboard.py:955-956` und `:1015`:
```python
            if _call_row is not None:
                outcome_confirmed = (_call_row.outcome is not None)
...
                outcome_confirmed=(outcome_confirmed if _preview_on else False),  # TAXO2-05: ANZEIGE-Sperre (calls.outcome IS NOT NULL)
```

Das Seed-Muster, **verbatim** aus `tests/test_session_detail_render.py:95-121` — der Kommentar,
der durch D-20 umgedreht wird:
```python
        # outcome NICHT NULL -> outcome_confirmed=True -> die ANZEIGE-Sperre (0) ist offen,
        # sonst erreicht der Test den Beobachtungs-Zweig gar nicht.
        outcome='meeting_booked',
```

Die verfuegbaren CSS-Klassen (bestehend, **nicht** neu erfinden):
`.n-observation-empty`, `.n-observation-quote`, `.n-observation-text`, `.n-observation-dim`,
`.n-compliance-alert`, `.n-badge`, `.n-badge-neutral`, `.n-label`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anzeige-Sperre faellt, ehrliche Markierung tritt an ihre Stelle (D-20/D-21)</name>

  <files>
templates/session_detail.html
routes/dashboard.py
tests/test_session_detail_render.py
  </files>

  <read_first>
- `templates/session_detail.html` — **`:60-200`**. Die ganze KI-Karte (`:119-188`), die
  Hero-Section davor (`:68-117`) und der Kopfkommentar (`:119-124`).
- `routes/dashboard.py:934-1035` — der Preview-Bau, das Sicherheitsnetz `_render(_preview_on)`
  und der Template-Kontext.
- `tests/test_session_detail_render.py` — **ganze Datei**. Besonders `:50-65` (Teardown mit zwei
  `cleanup_rows`-Aufrufen wegen der FK-Reihenfolge), `:95-121` (Seed) und `:125-138`
  (`_login` mit `tenant_id` — **Pflicht, nicht Kosmetik**: ohne den Session-Eintrag ist die GUC
  leer und `rubric_score` liefert unter FORCE RLS **0 Zeilen**, der Test landet dann still im
  Zweig „(d) row-absent").
- `services/judge_runner.py:1-20` — belegt „ergebnis-blind"; `services/slow_lane.py:593-630` —
  belegt „fertig, bevor die Sperre greift". Beides traegt D-21.
  </read_first>

  <action>
**A) `templates/session_detail.html` — Zweig (0) faellt ersatzlos.**

Die drei Zeilen `:131-133` **loeschen**. Die Kette beginnt danach mit
`{% if rubric_preview and rubric_preview.status == 'judge_failed' %}` (aus `{% elif %}` wird
`{% if %}`).

**An seine Stelle** — **vor** dem `{% if %}`, also direkt hinter der Zeile
`<p class="n-label">KI-Einschätzung / Coaching-Hinweis — keine Note, kein Urteil.</p>`
(`:130`) — die ehrliche Markierung einfuegen:

```jinja
    {% if not outcome_confirmed %}
      {# METRIK-1 D-20: MARKIEREN statt SPERREN. Bis 2026-08-13 stand hier eine Sperre, die die
         Einschätzung bis zur Ergebnis-Bestätigung versteckte. Sie hat belegbar NIE einen Klick
         erzeugt (conv 264 und conv 266 blieben TROTZ Sperre unbestätigt) — sie hat nur
         Bewertungen unsichtbar gemacht.
         D-21: Der frühere Kommentar "(Leakage-Schutz)" war ein Etiketten-Irrtum. Schutz des
         Bewerters ist es belegt NICHT: der läuft ergebnis-blind (services/judge_runner.py) und
         ist fertig, BEVOR die Sperre überhaupt greifen konnte (services/slow_lane.py).
         Der echte Ursprung ist Andrés Mai-Direktive "erst Outcome-Wahl, dann Score" — Zweck
         Daten-Qualität. Diese Direktive bleibt dort in Kraft, wo sie wirkt: bei der
         Pflicht-Wahl im Anruf-Fenster direkt nach dem Auflegen. Unbestätigte Anrufe werden
         hier ehrlich markiert und aus jeder Korrelations-Rechnung herausgenommen. #}
      <p class="n-label">Gesprächsergebnis noch nicht bestätigt — diese Einschätzung zählt nicht in die Auswertung.</p>
    {% endif %}
```
⚠ **Keine neue CSS-Klasse, keine Farbe, kein Inline-Style** — `.n-label` existiert und wird in
derselben Karte schon benutzt (`:130`).
⚠ **Echte Umlaute im sichtbaren Text** („Gesprächsergebnis", „bestätigt"), **ASCII in den
Jinja-Ausdruecken** (`outcome_confirmed`) — die Datei macht das durchgehend richtig.

**B) Kopfkommentar `:119-124` nachziehen.** Er listet die Edge-Reihenfolge auf und wird durch
den Wegfall von (0) **falsch**. Neue Fassung:

```jinja
  {# ── Section 2b: KI-Einschätzung / Coaching-Hinweis (TAXO2-05, METRIK-1) ───────────────────
     Beobachtungen statt Zahl (Soll-Verhalten §6 / Entscheidung.md).
     Edge-Reihenfolge: (a) judge_failed -> (b) not_gradable (drei Gründe) -> (d) row-absent ->
     (e) Beobachtungen + Beleg-Zitate (+ prominente Compliance-Notiz oben, Cross-AI-Finding 2).
     (c) kein-Material-Zweig ist in (e) integriert (leere observations).
     METRIK-1 D-20: Der frühere Zweig (0) — Anzeige-Sperre bis zur Outcome-Bestätigung — ist
     ERSATZLOS entfallen. An seiner Stelle steht oberhalb der Kette eine ehrliche Markierung.
     Kein hardcoded Hex, keine Inline-Styles — nur .n-*-Klassen + CSS-Variablen (CLAUDE.md). #}
```

**C) `routes/dashboard.py` — der Kommentar wird richtiggestellt, der Wert bleibt.**

`outcome_confirmed` wird **weiterhin** berechnet und uebergeben — es ist jetzt ein
**Markierungs**-Flag. Die zwei irrefuehrenden Kommentare anpassen:

`:936-937` (im Block-Kommentar): den Halbsatz
`+ outcome_confirmed (ANZEIGE-Sperre: Beobachtungen erst zeigen, wenn calls.outcome bestaetigt — Leakage-Schutz). Reines Display-Gate via call_id-Join.`
ersetzen durch
`+ outcome_confirmed (METRIK-1 D-20/D-21: MARKIERUNG, keine Sperre mehr — der frueher hier genannte "Leakage-Schutz" war ein Etiketten-Irrtum; der Bewerter ist ergebnis-blind und laeuft vor jeder Anzeige).`

`:1015`: den Zeilenkommentar `# TAXO2-05: ANZEIGE-Sperre (calls.outcome IS NOT NULL)` ersetzen
durch `# METRIK-1 D-20: Markierung "Ergebnis nicht bestaetigt" (calls.outcome IS NOT NULL)`.

⚠ Der Fallback-Wert `False` bei `_preview_on=False` bleibt **unveraendert**: im Fallback-Render
wird die Karte ohnehin ohne Vorschau-Daten gezeichnet, und `False` erzeugt dort dieselbe
Markierung wie ein unbestaetigter Anruf. Das ist die konservative Richtung (lieber einmal zu
viel markiert als eine Bewertung faelschlich als „zaehlt" ausgewiesen) — **im Kommentar
festhalten**.

**D) ROT vor GRUEN — der Beleg fuer die Entkopplung.**

In `tests/test_session_detail_render.py` eine neue Testfunktion ergaenzen, die den bestehenden
Seed **mit `outcome=None`** nachbaut und dieselbe Assertion faehrt wie der Bestandstest auf den
Beobachtungs-Zweig:

```python
def test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis(...):
    """METRIK-1 Requirement 4 / D-20: die KI-Einschaetzung haengt NICHT mehr am Outcome-Klick.

    ROT-vor-GRUEN: Gegen den Stand VOR dieser Phase ist der Test rot — die Sperre (0) fing den
    Request ab und die Seite zeigte 'Gespraechsergebnis bestaetigen, dann erscheint die
    Einschaetzung.'. Ein sauberer Beleg ohne neue Mechanik: derselbe Seed, nur outcome=None.
    """
```
Assertions:
- Der Beleg-Zitat-Text der geseedeten Beobachtung **steht** im HTML.
- Der Satz `Gesprächsergebnis bestätigen, dann erscheint die Einschätzung.` steht **nicht**
  mehr im HTML (Abwesenheits-Anker).
- **Gepaarter Existenz-Anker:** `Gesprächsergebnis noch nicht bestätigt` **steht** im HTML —
  sonst waere „nicht gefunden" nicht von „Seite gar nicht gerendert" zu unterscheiden.
- HTTP-Status ist 200.

⚠ **`_login(client, u, tenant_id)` mit `tenant_id` benutzen** (`:125-138`). Ohne den
Session-Eintrag ist die GUC leer, `rubric_score` liefert unter FORCE RLS 0 Zeilen und der Test
waere aus dem **falschen** Grund gruen bzw. rot.
⚠ Teardown: **zwei** `cleanup_rows`-Aufrufe wegen der FK-Reihenfolge `calls` ←
`conversation_logs` (CLAUDE.md Test-Cleanup-Regel) — das bestehende Muster der Datei
uebernehmen.

Im **Bestandstest** den Seed-Kommentar `:95-121` umdrehen:
```python
        # METRIK-1 D-20: outcome ist hier gesetzt, weil dieser Test den BESTAETIGTEN Fall
        # abbildet — NICHT mehr, weil eine Anzeige-Sperre sonst den Zweig verstellen wuerde.
        # Die Sperre ist ersatzlos entfallen; der unbestaetigte Fall hat einen eigenen Test.
        outcome='meeting_booked',
```
  </action>

  <verify>
    <automated>grep -c "Leakage-Schutz" templates/session_detail.html routes/dashboard.py</automated>
    <automated>grep -c "noch nicht bestätigt" templates/session_detail.html</automated>
    <!-- Serverseitiges Test-Tor: Task 3 dieses Plans. -->
  </verify>

  <acceptance_criteria>
- **Abwesenheits-Anker auf Code-FORM (die Sperre ist wirkungslos, nicht nur umformuliert):**
  `grep -c "{% if not outcome_confirmed %}" templates/session_detail.html` liefert **1**
  (die Markierung) — und `grep -c "Gesprächsergebnis bestätigen, dann erscheint die Einschätzung"
  templates/session_detail.html` liefert **0**.
  Gepaarter Existenz-Anker: `grep -c "rubric_preview.status == 'judge_failed'"
  templates/session_detail.html` liefert **1** (die Kette steht, nur ohne Zweig 0).
- **Die Kette beginnt jetzt mit `{% if %}`, nicht mit `{% elif %}`:**
  `grep -c "{% elif rubric_preview and rubric_preview.status == 'judge_failed' %}"
  templates/session_detail.html` liefert **0**;
  `grep -c "{% if rubric_preview and rubric_preview.status == 'judge_failed' %}"
  templates/session_detail.html` liefert **1**.
- **D-21 — der Etiketten-Irrtum ist raus:**
  `grep -rn "Leakage-Schutz" templates/ routes/ services/ static/ | wc -l` liefert **0**.
  Gepaarter Existenz-Anker: `grep -c "outcome_confirmed" routes/dashboard.py` liefert **>= 3**
  (der Wert wird weiter berechnet und uebergeben — er ist nicht mitgeloescht worden).
- **Keine neue Farbe, kein Inline-Style in der KI-Karte:**
  `grep -c "n-label\">Gesprächsergebnis noch nicht bestätigt" templates/session_detail.html`
  liefert **1**; `grep -n "style=" templates/session_detail.html` zeigt **keinen** neuen Treffer
  innerhalb `:119-190` (der bestehende `style="--w: {{ kb_pct }}%"` in der Hero-Section ist der
  erlaubte Sonderfall „dynamischer Wert" und faellt ohnehin mit Plan 06).
- Der Kopfkommentar (`:119-124`) nennt **keinen** Zweig `(0)` mehr:
  `grep -c "(0) ANZEIGE-Sperre" templates/session_detail.html` liefert **0**.
  ⚠ **Gepaarter Existenz-Anker auf ein CODE-Artefakt, nicht auf Kommentartext:**
  `grep -c "{% if rubric_preview and rubric_preview.status == 'judge_failed' %}"
  templates/session_detail.html` liefert **1** — die `if/elif`-Kette existiert und beginnt jetzt
  mit dem Zweig (a).
  ⚠ Der frueher hier stehende Existenz-Anker `grep -c "Edge-Reihenfolge" … == 1` zeigte auf
  **genau den Kommentartext, den derselbe Plan in Abschnitt B vorschreibt** (Bau-Regel 19
  Punkt 8: ein Anker darf nicht auf Prosa zeigen, die der Plan selbst diktiert — er beweist dann
  nur, dass der Executor abgeschrieben hat, nicht dass die Kette lebt). Der Kommentar wird
  weiterhin gebaut, ist aber **kein Abnahme-Kriterium** mehr.
- Der neue Test existiert:
  `grep -c "def test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis"
  tests/test_session_detail_render.py` liefert **1**, und er benutzt `_login(` mit `tenant_id`.
- Der **ROT-Lauf** des neuen Tests gegen den Stand **vor** der Template-Aenderung steht
  **verbatim** im SUMMARY (Vault-Regel Nr. 1).
  </acceptance_criteria>

  <done>
Ein Anruf ohne bestaetigtes Ergebnis zeigt seine Einschaetzung — und wird dabei ehrlich als
unbestaetigt markiert, statt still versteckt zu werden.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dritter Ablehnungs-Zweig fuer 'too_little_speech' — der alte Zweig bleibt</name>

  <files>
templates/session_detail.html
tests/test_session_detail_render.py
  </files>

  <read_first>
- `templates/session_detail.html:137-146` — die `_reason`-Weiche (nach Task 1 leicht
  verschoben; **vor der Aenderung erneut lesen**, nicht auf die Zeilenzahl verlassen).
- `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-02-SUMMARY.md` — welchen Wert
  `payload_jsonb.reason` traegt (`too_little_speech`) und welche Messwerte danebenstehen.
- `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md` §3.4 — die Einreih-Regeln
  der Kette.
  </read_first>

  <action>
Die innere `_reason`-Weiche bekommt einen **dritten** Zweig. Der Zweig
`too_few_high_confidence_events` **bleibt erhalten** — Alt-Zeilen mit diesem Grund stehen in der
Datenbank und wuerden sonst die falsche Erklaerung („Audio zu schlecht") bekommen. Das
`{% else %}` bleibt „Audio zu schlecht".

```jinja
      <p>
        {% set _reason = (rubric_preview.payload_jsonb or {}).get('reason') %}
        {% if _reason == 'too_little_speech' %}
          {# METRIK-1 Requirement 1: neuer Ablehnungs-Grund des Sprech-Substanz-Tors. #}
          In diesem Gespräch wurde zu wenig gesprochen für eine Einschätzung.
        {% elif _reason == 'too_few_high_confidence_events' %}
          {# ALT-Grund, wird seit METRIK-1 NICHT mehr geschrieben — der Zweig bleibt trotzdem,
             weil Alt-Zeilen mit diesem Grund in der Datenbank stehen. Wer ihn entfernt, gibt
             diesen Anrufen die FALSCHE Erklärung. #}
          Zu wenig auswertbare Momente für eine Einschätzung.
        {% else %}
          Audio zu schlecht für eine Einschätzung.
        {% endif %}
      </p>
```
⚠ **Kein neuer Buchstabe in der Kette** — das ist ein dritter Zweig **innerhalb** von (b), nicht
ein neuer Edge-Case (PATTERNS §3.4).
⚠ **Der neue Zweig steht ZUERST**, damit er nicht vom Alt-Zweig verdeckt werden kann; die
Reihenfolge ist damit „lebender Grund vor Alt-Grund vor Sonst".
⚠ Sichtbarer Text mit **echten Umlauten**, Jinja-Ausdruecke **ASCII**.

**Drei Tests in `tests/test_session_detail_render.py`** (Muster: derselbe Seed wie im
Bestandstest, aber mit einer `rubric_score`-Zeile `status='not_gradable'` und dem jeweiligen
`payload_jsonb`):

1. `test_ablehnungsgrund_zu_wenig_gesprochen_zeigt_eigenen_text` —
   `payload_jsonb = {'reason': 'too_little_speech', 'berater_woerter': 12, 'redeabschnitte': 3, 'sprechzeit_ms': 4100, 'high_conf_events': 0}`.
   Assertion: `In diesem Gespräch wurde zu wenig gesprochen` steht im HTML; `Audio zu schlecht`
   steht **nicht** im HTML.
2. `test_alter_ablehnungsgrund_bleibt_erhalten` —
   `payload_jsonb = {'reason': 'too_few_high_confidence_events'}`.
   Assertion: `Zu wenig auswertbare Momente` steht im HTML. **Das ist der Regressions-Test
   gegen das versehentliche Aufraeumen des Alt-Zweigs.**
3. `test_unbekannter_ablehnungsgrund_faellt_auf_audio_zurueck` —
   `payload_jsonb = {'reason': 'poor_audio_health'}`.
   Assertion: `Audio zu schlecht` steht im HTML.

Alle drei benutzen `_login(client, u, tenant_id)` **mit** `tenant_id` (FORCE-RLS-Falle) und das
bestehende Teardown-Muster mit zwei `cleanup_rows`-Aufrufen.
  </action>

  <verify>
    <automated>grep -c "too_little_speech\|too_few_high_confidence_events" templates/session_detail.html</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "_reason == 'too_little_speech'" templates/session_detail.html` liefert **1**.
- **Der Alt-Zweig lebt (Regressions-Anker):**
  `grep -c "_reason == 'too_few_high_confidence_events'" templates/session_detail.html`
  liefert **1**; `grep -c "Zu wenig auswertbare Momente" templates/session_detail.html`
  liefert **1**.
- `grep -c "Audio zu schlecht" templates/session_detail.html` liefert **1** (der Sonst-Zweig
  steht unveraendert).
- Die drei neuen Tests existieren:
  `grep -c "def test_ablehnungsgrund_zu_wenig_gesprochen_zeigt_eigenen_text\|def test_alter_ablehnungsgrund_bleibt_erhalten\|def test_unbekannter_ablehnungsgrund_faellt_auf_audio_zurueck" tests/test_session_detail_render.py`
  liefert **3**.
- **Kein Source-Presence-False-Green:** `grep -c "inspect.getsource\|open('templates"
  tests/test_session_detail_render.py` liefert **0**; gepaarter Existenz-Anker:
  `grep -c "assert " tests/test_session_detail_render.py` liefert **>= 20**.
  </acceptance_criteria>

  <done>
Ein wegen zu wenig Sprech-Substanz abgewiesener Anruf sagt genau das — und die Anrufe, die noch
mit dem alten Grund in der Datenbank stehen, behalten ihre richtige Erklaerung.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ausrollen + Nachweis, dass NICHTS mehr am Bestaetigungs-Klick haengt</name>

  <read_first>
- `CLAUDE.md` → HART-Block „Kein Local-Dev", Abschnitt „Workflow pro Plan-Task".
- `CLAUDE-BAUREGELN-13-14-20.md` Punkt 20 — Pflicht-grep **immer mit gepaartem Existenz-Anker**.
  </read_first>

  <what-built>
Die Anzeige-Sperre auf der Auswertungsseite ist ersatzlos entfallen und durch eine ehrliche
Markierung ersetzt; der neue Ablehnungs-Grund hat seinen eigenen Zweig; der Alt-Zweig lebt
weiter.
  </what-built>

  <how-to-verify>
**Schritt 1 — Der Pflicht-Nachweis aus SPEC Requirement 4 (grep, VOR dem Deploy).**
Diese vier Befehle ausfuehren und die Ausgaben **verbatim** ins SUMMARY schreiben:

```bash
# (1) Abwesenheits-Anker: kein Gate mehr auf outcome_confirmed ausser der Markierung
grep -rn "outcome_confirmed" templates/ routes/ services/ static/ --include=*.html --include=*.py --include=*.js

# (2) Gepaarter Existenz-Anker DERSELBEN Suche: der Wert wird weiterhin berechnet und uebergeben
grep -c "outcome_confirmed" routes/dashboard.py

# (3) Abwesenheits-Anker auf die zweite moegliche Aufhaengung: calls.outcome als Anzeige-Gate
grep -rn "outcome is not None\|\.outcome and\|if .*\.outcome" templates/session_detail.html

# (4) Gepaarter Existenz-Anker: die KI-Karte wird ueberhaupt gerendert
grep -c "KI-Einschätzung" templates/session_detail.html
```
**Erwartung, am Stand vor dieser Phase gemessen:** `outcome_confirmed` hat **genau einen**
Konsumenten im Template (`session_detail.html:131`) und drei Stellen in `routes/dashboard.py`
(Zuweisung, Berechnung, Uebergabe). **Nach** dieser Phase ist der Template-Treffer die
**Markierung**, kein Gate. Taucht ein **weiterer** Konsument auf, ist das ein **Fund** —
melden, nicht wegdruecken.

**Schritt 2 — Ausrollen (⚠ fuer Agenten gesperrt, Andre fuehrt aus):**
```
git push origin main
bash deploy.sh production
```

**Schritt 3 — Live-Gegenprobe mit dem Test-Konto.**
1. Einen Test-Anruf fahren und das Anruf-Fenster **schliessen, ohne** das Ergebnis zu
   bestaetigen. ⚠ Die **Pflicht-Wahl im Anruf-Fenster ist unveraendert vorhanden** — sie wird
   hier bewusst uebergangen, nicht entfernt.
2. Die Auswertungsseite des Anrufs oeffnen.
   **Erwartet:** die KI-Einschaetzung ist **sichtbar**, und darueber steht
   „Gesprächsergebnis noch nicht bestätigt — diese Einschätzung zählt nicht in die Auswertung."
   **Nicht erwartet:** „Gesprächsergebnis bestätigen, dann erscheint die Einschätzung."
3. Einen **zweiten** Anruf mit bestaetigtem Ergebnis oeffnen. **Erwartet:** die Einschaetzung
   ist sichtbar und die Markierung **fehlt**.
4. Falls ein Anruf mit `status='not_gradable'` und Grund `too_little_speech` vorliegt: die
   Erklaerung lautet „In diesem Gespräch wurde zu wenig gesprochen für eine Einschätzung."
  </how-to-verify>

  <acceptance_criteria>
- Die vier greps aus Schritt 1 stehen **verbatim mit ihren Ausgaben** im SUMMARY — jeder
  Abwesenheits-Anker mit seinem gepaarten Existenz-Anker.
- `bash deploy.sh production` ist gruen; die Tor-Ausgabe (`N passed / 0 failed / 0 error`) steht
  verbatim im SUMMARY.
- Punkt 2 und 3 der Live-Gegenprobe sind von Andre bestaetigt.
- Im SUMMARY steht ausdruecklich: **„Die Pflicht-Wahl im Anruf-Fenster ist unangetastet
  (D-20); `static/pip-launcher.js` wurde in diesem Plan nicht geaendert"** — belegt durch
  `git diff --name-only`, das `static/pip-launcher.js` **nicht** enthaelt.
- **Korrelations-Feststellung im SUMMARY:** D-20 verlangt, unbestaetigte Anrufe „aus jeder
  Korrelations-Rechnung herauszunehmen". Der Executor belegt per grep, **welche**
  Korrelations-Rechnung heute existiert. Steht die Zahl bei **0** (die Uebernahme-/
  Korrelations-Messung ist laut SPEC Boundaries ausdruecklich **out of scope, post-Launch**),
  wird genau das festgehalten: *„Es existiert heute keine Korrelations-Rechnung; die Markierung
  ist der Haken, an dem die spaetere Rechnung sie ausschliesst."* Gepaarter Existenz-Anker
  daneben, damit „0 Treffer" nicht mit „nichts gesucht" verwechselt wird.
  </acceptance_criteria>

  <resume-signal>
Andre antwortet „passt" oder beschreibt, was er sieht.
  </resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21 + Punkt 26)

### Angefasste Tabellen

- `rubric_score` (**lesen**, Anzeige-Pfad) — FORCE RLS
- `calls` (**lesen**: `outcome`) — keine RLS

**Dieser Plan schreibt keine Daten.** Er aendert ausschliesslich Anzeige-Logik.

### inspect.sh-Beleg

Der `rubric_score`-Auszug (FORCE-RLS-Policy, `payload_jsonb jsonb NOT NULL DEFAULT '{}'`,
`status varchar(24) nullable`) steht **verbatim in Plan 01, Sektion 5** — derselbe Zug vom
2026-08-13. Er wird hier nicht ein drittes Mal abgedruckt.

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg-thread) + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `outcome_confirmed` | berechnet in `routes/dashboard.py:956`, gelesen im Template | **KEINE eigene DB-Spalte** — abgeleitet aus `calls.outcome IS NOT NULL` | `calls.outcome` wird **browser-getriggert** gesetzt (`/api/postcall_outcome`, `static/pip-launcher.js`). **Der Leser laeuft dem Schreiber regelmaessig voraus — und genau das ist der Anlass dieses Plans.** Faellt der Browser vor dem Klick weg, bleibt `outcome` NULL. Bisher fuehrte das zu einer versteckten Bewertung; ab jetzt zu einer Markierung. | ✓ `dashboard.py:956` + SPEC-Beleg conv 264/266 |
| `rubric_preview.status` | gelesen im Anzeige-Pfad | DB-Spalte `rubric_score.status` | **Batch** (Slow Lane, `_judge_step`). Der Anzeige-Pfad kann vorauslaufen → er findet **keine Zeile** und faellt in den bestehenden Zweig „(d) row-absent" („wird im Hintergrund ausgewertet"). Dieser Zweig ist **unveraendert** und bleibt der Vorlauf-Faenger. | ✓ `dashboard.py:957-960` |
| `rubric_preview.payload_jsonb['reason']` | gelesen im Anzeige-Pfad | DB-Spalte `rubric_score.payload_jsonb` | Batch, wie oben. Ein unbekannter/fehlender `reason` faellt in den `{% else %}`-Zweig. | ✓ Plan 02 |
| `rubric_score` unter FORCE RLS | gelesen im Request-Pfad | — | Der Request-Pfad setzt die GUC aus der Flask-Session (`app.py`, Mandanten-UUID). **Im Test ist `_login(..., tenant_id)` deshalb Pflicht** — ohne GUC liefert die Tabelle **still 0 Zeilen** und der Test prueft den falschen Zweig. | ✓ `tests/test_session_detail_render.py:125-138` |

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Die Einschaetzung erscheint, sobald sie fertig ist — ohne dass jemand
   einen Klick nachholen muss.
2. **Nachvollziehen:** Ob ein Anruf bestaetigt ist, steht weiterhin in `calls.outcome` und ist
   jetzt **auf dem Bildschirm sichtbar** statt nur als unsichtbare Sperre wirksam.
3. **Eingreifen koennen:** Der Bestaetigungs-Klick bleibt an seinem Platz im Anruf-Fenster und
   kann jederzeit nachgeholt werden — die Markierung verschwindet dann.
4. **Marge:** Anrufe wie conv 264 und conv 266 bekommen ihre Rueckmeldung, statt dauerhaft
   unbewertet zu bleiben. Ohne den Fix ist ein Teil der bezahlten Auswertung schlicht unsichtbar.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `rubric_score` (FORCE RLS) → Auswertungsseite | Mandantenfremde Bewertung darf nie sichtbar werden |
| Anzeige-Zustand → Datenqualitaet des Trainings-Vorrats | Unbestaetigte Ergebnisse duerfen nicht als bestaetigt gelten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-METRIK1-03-01 | Information Disclosure | Wegfall eines Anzeige-Gates | **mitigate** | Das entfallene Gate war **kein** Zugriffsschutz. Der Mandanten- und Nutzerschutz haengt unveraendert an zwei anderen Stellen: `CL.user_id == g.user.id` (`dashboard.py:747-750`) und der FORCE-RLS-Policy auf `rubric_score`. Beide werden nicht angefasst — belegt durch `git diff`. |
| T-METRIK1-03-02 | Repudiation | unbestaetigte Anrufe gelten als bestaetigt | **mitigate** | Ehrliche Markierung im Template + `calls.outcome` bleibt der Wahrheitswert; die Markierung ist der Haken fuer die spaetere Korrelations-Rechnung. |
| T-METRIK1-03-03 | Information Disclosure | Chef sieht Coaching-Inhalte | **accept** | Unveraendert: die Route filtert hart auf `CL.user_id == g.user.id`. Dieser Plan legt keinen neuen Zugang an. |
</threat_model>

<verification>
- Alle `<acceptance_criteria>` der drei Tasks sind erfuellt.
- Der ROT-Lauf des Entkopplungs-Tests steht verbatim im SUMMARY.
- Die vier Pflicht-greps aus SPEC Requirement 4 stehen mit Ausgaben im SUMMARY, jeweils mit
  gepaartem Existenz-Anker.
- Deploy-Tor gruen, Live-Gegenprobe von Andre bestaetigt.
</verification>

<success_criteria>
- Ein Anruf ohne bestaetigtes Ergebnis zeigt Beobachtungen und Beleg-Zitate.
- Der Satz „Gesprächsergebnis bestätigen, dann erscheint die Einschätzung." existiert im Code
  nicht mehr.
- Der Ausdruck „Leakage-Schutz" existiert im Code nicht mehr.
- Der neue Ablehnungs-Grund hat einen eigenen Zweig, der alte lebt weiter.
- `static/pip-launcher.js` ist unangetastet.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-03-SUMMARY.md` anlegen.
**Pflicht-Inhalte:** ROT-Lauf verbatim · die vier Pflicht-greps mit Ausgaben · Deploy-Tor-Ausgabe ·
die Korrelations-Feststellung · Pruning-Notiz (der Zweig (0) ist **geloescht**, nicht als
Foundation stehengelassen — Begruendung: er hat belegbar nie einen Klick erzeugt).
</output>
