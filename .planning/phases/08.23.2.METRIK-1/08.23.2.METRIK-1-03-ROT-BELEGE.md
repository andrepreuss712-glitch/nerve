# 08.23.2.METRIK-1 Plan 03 — ROT-Belege + ausgefuehrte Anker (Zwischenstand)

**Stand:** 2026-08-15, nach Task 1 + Task 2. **Task 3 (Ausrollen + Live-Gegenprobe) ist OFFEN** —
`bash deploy.sh production` ist fuer Agenten gesperrt, Andre fuehrt aus.

Diese Datei fuehrt die Belege waehrend der Ausfuehrung mit, damit sie einen Kontext-Schnitt
ueberleben. Sie wandert nach dem Ausrollen in die SUMMARY.

## Commits

| Task | Gate | Ergebnis | Commit |
|---|---|---|---|
| 1 | RED | Entkopplungs-Test + Gegenprobe, `_seed_session` bekommt `outcome`-Parameter | `b29c57c` |
| 1 | GREEN | Zweig (0) geloescht, ehrliche Markierung, Kopfkommentar + Route-Kommentare | `a72da8b` |
| 2 | RED | drei Zweig-Tests, `_seed_session` bekommt `status` + `payload` | `61a7029` |
| 2 | GREEN | dritter Zweig `too_little_speech`, Alt-Zweig bleibt | `a580461` |

## ROT-Lauf 1 — Task 1, gegen den Stand VOR der Template-Aenderung

Gezogen ueber `scripts/triage.sh` auf dem Server (`/opt/nerve/app`), nachdem **nur die Testdatei**
transportiert war — Template und Route lagen dort noch im alten Zustand. Echter Test-Fehler,
**kein Werkzeug-Ausfall** (Rezept-Bedingung 4), der Lauf erreicht den Pruefkatalog.

```
=================================== FAILURES ===================================
___________ test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis ____________
tests/test_session_detail_render.py:216: in test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis
    assert MARKIERUNG_UNBESTAETIGT in html, (
E   AssertionError: Die ehrliche Markierung fehlt — ohne sie ist ein unbestaetigter Anruf von einem bestaetigten nicht zu unterscheiden.
E   assert 'Gesprächsergebnis noch nicht bestätigt' in '<!DOCTYPE html>\n<html lang="de">\n<head>\n<meta charset="UTF-8">\n<meta name="viewport" content="width=device-width,...onclick="_nerveNavConfirm(true)">Call beenden &amp; verlassen</button>\n    </div>\n  </div>\n</div>\n</body>\n</html>'
```

Erster Lauf mit beiden neuen Tests zusammen:

```
FAILED tests/test_session_detail_render.py::test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis
=================== 1 failed, 1 passed, 12 warnings in 8.45s ===================
[triage] FEHLER: pytest gegen nerve_test ROT (kein Restart, kein Deploy)
```

**Warum 1 passed:** die Gegenprobe `test_bestaetigter_anruf_traegt_die_markierung_nicht` ist gegen
den alten Stand **richtigerweise gruen** (dort gab es die Markierung ueberhaupt nicht). Sie ist der
diskriminierende Partner: sie wird rot, sobald jemand die Markierung fest verdrahtet.

## ROT-Lauf 2 — Task 2, gegen den Stand ohne dritten Zweig

```
=================================== FAILURES ===================================
_________ test_ablehnungsgrund_zu_wenig_gesprochen_zeigt_eigenen_text __________
tests/test_session_detail_render.py:338: in test_ablehnungsgrund_zu_wenig_gesprochen_zeigt_eigenen_text
    assert SATZ_ZU_WENIG_GESPROCHEN in html, (
E   AssertionError: Der neue Ablehnungs-Grund hat keinen eigenen Text — der Anruf bekommt eine fremde Erklaerung.
1 failed, 2 passed, 12 warnings in 8.58s
```

**2 passed ist hier der Zweck, nicht ein Mangel:** `test_alter_ablehnungsgrund_bleibt_erhalten` und
`test_unbekannter_ablehnungsgrund_faellt_auf_audio_zurueck` sind **Regressions**-Anker auf Bestand.
Sie muessen vorher UND nachher gruen sein — waeren sie vorher rot, wuerden sie etwas anderes
messen als „der Alt-Zweig hat ueberlebt".

## GRUEN-Laeufe (Server, `triage.sh` gegen `nerve_test`)

Nach Transport von `templates/session_detail.html` + `routes/dashboard.py` + Testdatei:

```
11 passed, 11 warnings in 10.09s
[triage] pytest gegen nerve_test bestanden — nerve_test wird per trap gedroppt
```

Voll-Lauf mit **denselben Markern wie das Deploy-Tor** (`deploy.sh:221`, `-m "not live and not perf"`) —
die Vorhersage fuer Andres Ausrollen:

```
1220 passed, 7 skipped, 5 deselected, 107 warnings in 79.48s (0:01:19)
[triage] pytest gegen nerve_test bestanden — nerve_test wird per trap gedroppt
```

**Beweis-Kette zur Zahl:** Basis nach Welle 2 = **1200**. +5 aus diesem Plan (2 aus Task 1, 3 aus
Task 2) = 1205. +15 aus Plan 04 (`tests/test_fokus_katalog.py`, auf dem Server bereits vorhanden,
15 Testfunktionen) = **1220**. Die Basis ist nicht gewandert, keine fremde Pruefung ist nebenbei
umgefallen.

⚠ **Werkzeug-Divergenz beachtet** (Lehre aus Plan 02): ohne die Marker `-m "not live and not perf"`
laufen die real-Haiku-Tests mit, sterben an `invalid x-api-key` und ihr Teardown erzeugt
`[BASELINE-AUTO-FIX]`-Warnungen. Das ist kein Cleanup-Defekt. Der Lauf oben traegt die Marker.

## Ausgefuehrte Abnahme-Anker — Task 1

| Anker | Soll | Ist |
|---|---|---|
| `grep -c "Gesprächsergebnis bestätigen, dann erscheint die Einschätzung" templates/session_detail.html` | 0 | **0** |
| `grep -c "rubric_preview.status == 'judge_failed'" templates/session_detail.html` (gepaarter Existenz-Anker) | 1 | **1** |
| `grep -c "{% elif rubric_preview and rubric_preview.status == 'judge_failed' %}" templates/session_detail.html` | 0 | **0** |
| `grep -c "{% if rubric_preview and rubric_preview.status == 'judge_failed' %}" templates/session_detail.html` | 1 | **1** |
| `grep -rn "Leakage-Schutz" templates/ routes/ services/ static/ \| wc -l` | 0 | **0** |
| `grep -c "outcome_confirmed" routes/dashboard.py` (gepaarter Existenz-Anker) | >= 3 | **4** |
| `grep -c "n-label\">Gesprächsergebnis noch nicht bestätigt" templates/session_detail.html` | 1 | **1** |
| `grep -c "(0) ANZEIGE-Sperre" templates/session_detail.html` | 0 | **0** |
| `grep -c "def test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis" tests/test_session_detail_render.py` | 1 | **1** |
| `grep -n "style=" templates/session_detail.html` | kein neuer Treffer in der KI-Karte | **erfuellt** — Treffer nur `:101/105/109/113` (Hero-Minibars) und `:314/338/495` (spaetere Sektionen); die KI-Karte enthaelt **keinen** |

Der neue Test benutzt `_login(client, u, tenant_id)` **mit** `tenant_id` (FORCE-RLS-Falle) und das
bestehende Teardown-Muster mit zwei `cleanup_rows`-Aufrufen (er nutzt dieselbe `tracker`-Fixture).

## Ausgefuehrte Abnahme-Anker — Task 2

| Anker | Soll | Ist |
|---|---|---|
| `grep -c "_reason == 'too_little_speech'" templates/session_detail.html` | 1 | **1** |
| `grep -c "_reason == 'too_few_high_confidence_events'" templates/session_detail.html` | 1 | **1** |
| `grep -c "Zu wenig auswertbare Momente" templates/session_detail.html` | 1 | **1** |
| `grep -c "Audio zu schlecht" templates/session_detail.html` | 1 | **1** |
| `grep -c "def test_ablehnungsgrund_… \| … \| …" tests/test_session_detail_render.py` | 3 | **3** |
| `grep -c "inspect.getsource\|open('templates" tests/test_session_detail_render.py` | 0 | **1** ⚠ siehe unten |
| `grep -c "assert " tests/test_session_detail_render.py` (gepaarter Existenz-Anker) | >= 20 | **35** |

### ⚠ Ein Anker war eine FEHLMESSUNG (Klasse „Abwesenheits-Anker auf Prosa statt Code-Form")

`grep -c "inspect.getsource\|open('templates" tests/test_session_detail_render.py` liefert **1**,
nicht 0. Der Treffer ist der **Modul-Docstring aus einem frueheren Plan** (`:20`), der die
CLAUDE.md-Regel referiert: dort steht sinngemaess, dass die Datei bewusst **keine** solchen
Source-Presence-Tests enthaelt.

**Die Absicht des Kriteriums ist erfuellt, der Anker greift daneben.** Beleg mit dem richtigen
Anker auf die **Code-Form** (Aufrufstelle statt blossem Namen):

```
grep -c "inspect.getsource(\|open('templates" tests/test_session_detail_render.py   -> 0
```

Der einzige Weg zum Sollwert 0 des Original-Ankers waere gewesen, **fremden Bestands-Docstring
umzuschreiben, nur damit ein grep gruen wird** — genau der Fehler „Anker formt das Artefakt". Der
Text bleibt, der Sollwert ist als falsch dokumentiert.

## Task 3, Schritt 1 — die vier Pflicht-Suchen (VOR dem Deploy, ohne Deploy ausfuehrbar)

⚠ Entdeckungs-Suchen ohne Sollwert (so im Plan ausdruecklich markiert), **kein Abnahme-Gate**.

```
$ grep -rn "outcome_confirmed" templates/ routes/ services/ static/ --include=*.html --include=*.py --include=*.js
templates/session_detail.html:133:    {% if not outcome_confirmed %}
routes/dashboard.py:936:        # TAXO2-Plan 05) + outcome_confirmed (METRIK-1 D-20/D-21: MARKIERUNG, keine Sperre mehr —
routes/dashboard.py:944:        outcome_confirmed = False
routes/dashboard.py:957:                outcome_confirmed = (_call_row.outcome is not None)
routes/dashboard.py:1021:                outcome_confirmed=(outcome_confirmed if _preview_on else False),

$ grep -c "outcome_confirmed" routes/dashboard.py
4

$ grep -rn "outcome is not None\|\.outcome and\|if .*\.outcome" templates/session_detail.html
(kein Treffer)

$ grep -c "KI-Einschätzung" templates/session_detail.html
4
```

**Befund:** genau **ein** Template-Konsument — und der ist jetzt die **Markierung**
(`{% if not outcome_confirmed %}` → `<p class="n-label">…</p>`), **kein Gate** mehr: er umschliesst
nur noch einen Absatz, die if/elif-Kette darunter ist davon unabhaengig und beginnt mit
`{% if rubric_preview … 'judge_failed' %}`. **Kein weiterer Konsument** ist aufgetaucht. Die zweite
moegliche Aufhaengung (`calls.outcome` direkt im Template) existiert nicht — und der gepaarte
Existenz-Anker (`KI-Einschätzung` = 4) belegt, dass die Karte ueberhaupt gerendert wird, „kein
Treffer" also nicht „falsche Datei gelesen" heisst.

## Korrelations-Feststellung (Task 3)

D-20 verlangt, unbestaetigte Anrufe „aus jeder Korrelations-Rechnung herauszunehmen".

```
$ grep -rn "group_by\|func.avg\|func.count" services/ routes/ --include=*.py | grep -ic "outcome"
0
$ grep -rn "group_by\|func.avg\|func.count" services/ routes/ --include=*.py | wc -l
25
```

**Es existiert heute keine Korrelations-Rechnung; die Markierung ist der Haken, an dem die
spaetere Rechnung sie ausschliesst.** Die Uebernahme-/Korrelations-Messung ist laut SPEC
Boundaries ausdruecklich **out of scope, post-Launch**. Gepaarter Existenz-Anker daneben: es gibt
**25** Aggregationen im Code — „0 Treffer" heisst hier also „keine davon fasst `outcome` an", nicht
„es wurde nichts gesucht".

Ergaenzend, dieselbe Richtung: die drei Stellen, die `calls.outcome` heute ueberhaupt beruehren,
sind **Setzer** (`routes/learning.py:132/302`, `routes/app_routes.py:2240`) und **Anzeigen**
(`routes/performance.py:389`, `routes/app_routes.py:2198/2299`) — keine rechnet etwas gegen die
Bewertung. Der Bewerter selbst ist ergebnis-blind (`services/judge_runner.py:7`,
`services/rubric_engine.py:30-31`), das traegt D-21.

## Die Pflicht-Wahl im Anruf-Fenster ist unangetastet (D-20)

`static/pip-launcher.js` wurde in diesem Plan **nicht** geaendert — belegt durch:

```
$ git diff --name-only 51bcef6..HEAD
.planning/DIALOG-GSD-CLAUDIAN.md
.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-04-ROT-BELEGE.md
routes/dashboard.py
services/fokus_katalog.py
templates/session_detail.html
tests/test_fokus_katalog.py
tests/test_session_detail_render.py
```

(`services/fokus_katalog.py` + `tests/test_fokus_katalog.py` gehoeren zu **Plan 04** und sind
disjunkt zu diesem Plan.)

## Pruning-Notiz (Vault-Regel Nr. 7)

- **Zweig (0) GELOESCHT, nicht als Foundation stehengelassen.** Begruendung: er hat belegbar nie
  einen Klick erzeugt (conv 264 und conv 266 blieben **trotz** Sperre unbestaetigt) — eine
  Mechanik, die ihren Zweck nachweislich nie erfuellt hat, wird nicht „fuer spaeter" behalten.
  Was bleibt, ist der **Wert** `outcome_confirmed` (4 Stellen in `routes/dashboard.py`) — er traegt
  jetzt die Markierung.
- **Der Alt-Zweig `too_few_high_confidence_events` bleibt bewusst** — das ist **kein** vergessener
  Code: Alt-Zeilen mit diesem Grund stehen in der Datenbank, und wer ihn aufraeumt, gibt genau
  diesen Anrufen die falsche Erklaerung. Festgenagelt von
  `test_alter_ablehnungsgrund_bleibt_erhalten`.

## Was Task 3 noch braucht (Andre)

1. `bash deploy.sh production` — gruen, Tor-Ausgabe verbatim (Vorhersage: **1220 passed**).
2. Live-Gegenprobe Punkt 2 und 3 (unbestaetigter Anruf zeigt Einschaetzung **mit** Markierung;
   bestaetigter Anruf zeigt sie **ohne** Markierung).
