---
phase: 08.23.2.METRIK-1
plan: 02
type: execute
wave: 2
depends_on: ["08.23.2.METRIK-1-01"]
files_modified:
  - config.py
  - services/slow_lane.py
  - database/models.py
  - alembic/versions/0040_metrik1_schilder.py
  - tests/test_coaching_score_cutover.py
  - tests/test_substanz_tor.py
autonomous: false
requirements: [SPEC-R1, SPEC-R2]
user_setup: []

must_haves:
  truths:
    - "Ein Anruf mit 20 Berater-Woertern ohne einen einzigen hoch-konfidenten Moment wird bewertet statt abgewiesen."
    - "Ein Anruf mit 100 Berater-Woertern in EINEM einzigen Redeabschnitt wird NICHT abgewiesen — die Zahl der Redeabschnitte ist keine Tor-Bedingung mehr."
    - "Ein Anruf mit 4 Berater-Woertern wird abgewiesen."
    - "Unbekannte Wortzahl ist ein EIGENER Programm-Zweig mit EIGENEM Zweig-String und laesst durch (fail-open) — im Code belegt, nicht in Prosa."
    - "Eine EWB-Knopf-Zeile (speaker kunde, word_count NULL) kippt einen Anruf NICHT in den Unbekannt-Zweig."
    - "Jede Ablehnungs-Zeile traegt die gezaehlten Messwerte UND den genommenen Tor-Zweig, nicht nur den Grund."
    - "Das Audio-Tor verhaelt sich unveraendert."
    - "Die Schilder von rubric_score.status und rubric_score.payload_jsonb beschreiben den neuen Zustand."
  artifacts:
    - path: "config.py"
      provides: "MIN_BERATER_WOERTER mit ENV-Override und Herleitung aus dem Zweck — EINE Schwelle, keine Abschnitts-Schwelle"
      contains: "MIN_BERATER_WOERTER"
    - path: "services/slow_lane.py"
      provides: "_segments_for_call + _mess_sprech_substanz + Wortzahl-Tor mit eigenem Unbekannt-Zweig + Messwerte im Ablehnungs-Payload"
      contains: "def _mess_sprech_substanz"
    - path: "alembic/versions/0040_metrik1_schilder.py"
      provides: "COMMENT-ON-Migration fuer status + payload_jsonb (Punkt 23), Kopf 0039 -> 0040"
      contains: "def _comment"
    - path: "tests/test_substanz_tor.py"
      provides: "Verhaltens-Tests fuer alle Tor-Kanten inkl. Ein-Abschnitt-Durchlass und den eigenen Unbekannt-Zweig + Messwerte-Payload"
      contains: "_TOR_ZWEIG_WORTZAHL_UNBEKANNT"
  key_links:
    - from: "services/slow_lane.py::_call_end_merge"
      to: "transcript_segments"
      via: "_segments_for_call(call.conversation_log_id, read_db) — GUC-frei, keine RLS"
      pattern: "_segments_for_call\\("
    - from: "services/slow_lane.py::_call_end_merge"
      to: "ctx['mess_substanz']"
      via: "Messwerte werden EINMAL gezaehlt und durchgereicht, nicht zweimal gerechnet"
      pattern: "'mess_substanz'"
    - from: "services/slow_lane.py::_judge_step"
      to: "rubric_score.payload_jsonb"
      via: "payload={'reason': reason, **messwerte}"
      pattern: "'reason': reason"
    - from: "database/models.py::RubricScore"
      to: "alembic/versions/0040_metrik1_schilder.py"
      via: "zeichengleiche Schild-Konstanten (Punkt 23 ORM/DDL-Konsistenz)"
      pattern: "_SCHILD_RUBRIC_STATUS"
---

<objective>
Das Substanz-Tor fragt ab jetzt „wurde ueberhaupt genug gesprochen?" statt „gab es mindestens drei
hoch-konfidente Einwand-Momente?" — und jede Ablehnung haelt fest, **womit** sie begruendet wurde.

**Purpose:** Das alte Tor sperrt 83 % aller Anrufe aus (Messung 2026-08-11 als `postgres`:
Median 0 hoch-konfidente Momente, n = 87). Im Kaltakquise-Modus hoert NERVE nur den Berater und
die automatische Einwand-Erkennung ist kanonisch **abgeschaltet** — Momente entstehen fast nur
per Knopfdruck. Ein Tor, das drei davon verlangt, bestraft strukturell das gute kurze Gespraech.
Das Tor stammt aus der Marker-Notenmaschine, die Einwand-Ereignisse zum **Rechnen** brauchte; der
Bewerter liest das ganze Transkript und braucht sie nicht.

**Output:** Neues Tor (**nur** 20 Berater-Woerter — die Abschnitts-Bedingung ist ersatzlos
gestrichen, Andre-Entscheidung 14.08.), ein **eigener Programm-Zweig** fuer unbekannte Wortzahl,
Messwerte in der Ablehnungs-Zeile, aktualisierte Schilder — und **der eine Ausroll-Punkt** fuer
Plan 01 + Plan 02.

## ⛔ D-01 / D-02 — HIER LIEGT DAS EINE AUSROLLEN

**Gelockte Entscheidung D-01: Zitat-Pruefer (Plan 01, Requirement 3) und Tor-Umbau (dieser Plan,
Requirement 1) gehen in EINEM Ausrollen live.** Das ist die im SPEC ausdruecklich zugelassene
Alternative zu „Pruefer zuerst" und wird hier — wie vom SPEC woertlich verlangt — **ausdruecklich
vermerkt**.

**D-02: Der Zwischenzustand „neues Tor live, Pruefer im naechsten Ausrollen" ist verboten.**
Deshalb: Plan 01 hat **keinen** Deploy-Task; der **einzige** `bash deploy.sh production` dieser
beiden Plaene ist **Task 3 dieses Plans**. Das neue Tor kann per Bauart nicht vor dem Pruefer
live gehen.

**Ausroll-Reihenfolge innerhalb von Task 3:** Migration `0040` ist eine **reine COMMENT-ON-
Migration** — sie legt keine Spalte an und aendert keinen Typ. Sie darf deshalb vor **oder** nach
dem Deploy laufen. Festgelegt: **Migration zuerst, dann `deploy.sh production`**, damit der
Schild-Waechter im Test-Tor gegen den bereits gesetzten Kommentar laeuft.
⚠ Die Regel „nullable Spalten zuerst migrieren" aus ZEITSTEMPEL-1 wird hier **nicht** blind
uebertragen — sie gilt nur fuer neue Spalten. Hier gibt es keine.

## ⛔ ANDRE-ENTSCHEIDUNG 14.08. — DAS TOR HAT NUR NOCH EINE BEDINGUNG

**Ersetzt die frueher hier stehende Prosa-Aufloesung eines SPEC-Widerspruchs. Der Widerspruch
wird nicht mehr wegerklaert, er ist weggebaut.**

**Was gestrichen ist:** die Abschnitts-Bedingung, **ersatzlos**. Abgelehnt wird ab jetzt
**ausschliesslich** bei weniger als `MIN_BERATER_WOERTER` gesprochenen **Berater**-Woertern.
**Anlass (Cross-AI-Fund, HIGH):** Ein sauber am Stueck gesprochener Einstieg von 100 Woertern
ist bei `endpointing=900` (`services/deepgram_service.py:586`) **EIN** Segment und waere
abgelehnt worden — genau der Anruf, fuer den diese Phase existiert.
⛔ **Nicht ODER→UND.** Das war der naheliegende Fix und ist **ausdruecklich verworfen**: UND
liesse **7 Woerter in 3 Abschnitten** durch, und daraus ist kein Zitat ziehbar — ein
zitierfaehiger Satz war die ganze Herleitung der 20. **Die Wortzahl allein macht beides
richtig.**

**Was daraus folgt — und in diesem Plan durchgaengig nachgezogen ist (keine Halb-Migration,
Punkt 29):**

1. **`MIN_REDEABSCHNITTE` wird gar nicht erst angelegt.** Es gibt **eine** Schwelle.
2. **Die Zahl der Redeabschnitte bleibt als DIAGNOSE-Wert** — sie wird weiter gemessen und steht
   im `payload_jsonb` der Ablehnungs-Zeile (Requirement 2, Grundlage der Nachjustierung nach
   ~100 echten Anrufen). Sie entscheidet **nichts**.
3. ✅ **Die EWB-Kippe aus SPEC NACHTRAG 2 ④ entfaellt ersatzlos.** Die Knopf-Zeile traegt
   `word_count = NULL` und zaehlt bei der Wortsumme ohnehin nicht mit; der Sonderfall „ein Satz
   plus ein Knopfdruck = 2 Redeabschnitte" kann per Bauart nicht mehr entstehen.
   ⚠ **Der EWB-FILTER fuer Bewerter-Auftrag und Pruef-Korpus (D-04/D-05, Plan 01) bleibt
   unberuehrt** — er hat einen anderen Grund (ein zitierfaehiger Pseudo-Satz), nicht das Tor.
4. ⚠ **Was BLEIBT und jetzt doppelt wichtig ist: die Ableitung (c2).** Die Unbekannt-Erkennung
   schaut **nur** auf Berater-Zeilen. Ohne sie kippte die EWB-Zeile (`speaker='kunde'`,
   `word_count NULL`) jeden Anruf in den fail-open-Zweig — das Tor waere **per Knopfdruck
   aushebelbar**. Das ist nach der Streichung die **einzige** verbliebene EWB-Kante des Tors
   (Testfunktion 7).

## ⚠ Der NULL-Widerspruch wird im CODE aufgeloest, nicht in Prosa — zwei Gegenleser, ein Befund

**SPEC `:517`** verlangt: *„ein Anruf mit ausschliesslich `word_count IS NULL` wird nicht wegen
Wortmangel abgelehnt."* Bis zum 13.08. wurde er es faktisch doch — nur mit einer anderen
Begruendung unter **demselben** `reason`-String. **Gemini (B-3) und der GSD-Plan-Checker (W-5)
haben das unabhaengig voneinander gefunden**; das ist der belastbarste Befund des
Cross-AI-Durchgangs.

**➡️ Auflage, hier umgesetzt:** „Unbekannte Wortzahl" ist ein **EIGENER Programm-Zweig mit
EIGENEM Zweig-String** — `_TOR_ZWEIG_WORTZAHL_UNBEKANNT` (`'wortzahl_unbekannt_durchgelassen'`)
— und **laesst durch** (fail-open). **Nicht dieselbe Ablehnung unter anderem Namen.** Der Zweig
wird zurueckgegeben, in `mess_substanz['tor_zweig']` festgehalten und in der Ablehnungs-Zeile
mitgeschrieben.

**Beleg im Code, nicht im Text:** die **Verhaltens**-Tests 4 und 5 pruefen den **Rueckgabewert**
`(None, 'wortzahl_unbekannt_durchgelassen')` — kein Source-Presence-Test, kein Docstring-Anker.
Dazu ein Abnahme-Anker auf die **Code-Form** des Zweigs (nicht auf den nackten Namen), mit
gepaartem Existenz-Anker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-SPEC.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-CONTEXT.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-PATTERNS.md
@.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-01-SUMMARY.md
@CLAUDE.md
@CLAUDE-BAUREGELN-13-14-20.md

<interfaces>
Aus `database/models.py::TranscriptSegment` — die Spalten, auf denen das neue Tor rechnet:
```python
conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id', ondelete='CASCADE'), nullable=False)
ts_ms      = Column(Integer, nullable=False)   # Wall-Clock, NUR fuer die Reihenfolge
speaker    = Column(Text, nullable=False)      # CHECK IN ('berater','kunde','system')  <-- DB-Form!
text       = Column(Text, nullable=False)
start_ms   = Column(Integer, nullable=True)    # Deepgram-Audio-Achse
end_ms     = Column(Integer, nullable=True)    # end_ms - start_ms = Sprech-Dauer
word_count = Column(Integer, nullable=True)    # VOR der Schwaerzung gezaehlt; NULL = unbekannt
```

Der heutige Tor-Block, der ersetzt wird (`services/slow_lane.py:623-630`, **verbatim**):
```python
        # ── Audio-Gate D-09 (VOR dem Scoring): events laden + high-conf SELBST zaehlen ──────
        events = _events_for_call(call_id, read_db)
        n_high_conf = _count_high_confidence(events, read_db)
        not_gradable_reason = None
        if call.audio_health_score is None or call.audio_health_score < AUDIO_HEALTH_GATE_THRESHOLD:
            not_gradable_reason = 'poor_audio_health'
        elif n_high_conf < MIN_HIGH_CONFIDENCE_EVENTS:
            not_gradable_reason = 'too_few_high_confidence_events'
```

Die heutige Ablehnungs-Zeile (`services/slow_lane.py:515-527`, **verbatim**):
```python
    reason = ctx.get('not_gradable_reason')
    if reason is not None:
        _upsert_rubric_score(
            db,
            call_id=call.id,
            conversation_log_id=call.conversation_log_id,
            session_mode=mode_key,
            tenant_id=tenant_id,
            status=_STATUS_NOT_GRADABLE,
            payload={'reason': reason},
        )
        db.commit()
        return
```

Der heutige `ctx`-Bau (`services/slow_lane.py:650-657`, **verbatim**):
```python
        ctx = {
            'call': call_w,
            'events': events_w,
            'db': write_db,
            'high_conf': n_high_conf,
            'not_gradable_reason': not_gradable_reason,
            'results': {},
        }
```

Die Lade-Helfer-Form, die `_segments_for_call` kopiert (`services/slow_lane.py:389-395`):
```python
def _events_for_call(call_id, db) -> list:
    """Laedt alle intent_event-Rows eines Calls ... Read-only; calls/intent_event haben KEINE RLS -> GUC-frei."""
    return (db.query(IntentEvent)
              .filter(IntentEvent.call_id == call_id)
              .order_by(IntentEvent.timestamp.asc())
              .all())
```

Der `_comment`-Helfer, der in Migration `0040` **verbatim** zu kopieren ist:
`alembic/versions/0039_zeitstempel1_sprechzeiten.py:52-73`.

── ROT-LAUF-REZEPT — wortgleich in allen zehn Plaenen dieser Phase ─────────────────────────

⚠ **Jeder ROT-vor-GRUEN-Beleg in diesem Plan wird SO gezogen — nicht anders.**

Ein absichtlich roter Lauf ist ueber `bash deploy.sh production` **per Bauart nicht erzeugbar**:
`deploy.sh:222` bricht bei rotem Tor ab (*„kein Restart, kein Deploy"*). Und ein lokales
`pytest` ist **HART verboten** (CLAUDE.md, „Kein Local-Dev — KEIN lokales pytest als
Acceptance-Check"). Ohne ein drittes Werkzeug waere jede ROT-Pflicht dieser Phase
**unausfuehrbar**.

Das Werkzeug existiert und ist ein bleibendes Werkzeug (Ratchet, Andre-Direktive 15.06.):
`scripts/triage.sh` — Server-Triage-Harness. Er provisioniert `nerve_test` **1:1 wie der
deploy.sh-Gate-Block** (pg_dump-Restore + `alembic upgrade head` + 4 DSNs + Whitelist-Guard +
trap-DROP), laeuft dann **NUR die uebergebenen Tests** und **deployt NIE** (kein Restart, kein
Deploy-Meta-Write).

```bash
# ganze Testdatei
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/triage.sh tests/test_X.py -v'

# EINE Funktion — so wird der ROT-Beleg gezogen (nur der eine Test, nicht die Suite)
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/triage.sh tests/test_X.py::test_name -v --tb=long'
```

**Vier Bedingungen, ohne die der Lauf nichts beweist:**
1. **Der Code muss auf dem Server liegen.** `triage.sh` laeuft gegen `/opt/nerve/app` — also
   erst `git commit` + `git push` + Datei-Transport, **dann** triagieren. Ein Triage-Lauf gegen
   den alten Stand ist **blind statt rot** und sieht dabei gruen aus (Punkt 31).
2. **ROT vor GRUEN, nicht danach.** Der rote Lauf wird gezogen, **bevor** die Zeile gefixt bzw.
   der Bestandstest retargetiert wird (Vault-Regel Nr. 1: erst rot, dann fixen).
3. **Ausgabe verbatim ins SUMMARY.** Der Rot-Lauf ist ein **Acceptance-Artefakt**, keine
   Nebenbemerkung.
4. **Werkzeug-Ausfall ist kein ROT-Beleg.** Ein `exit 1` mit der **Fehlermeldung des Tests** IST
   der Beleg. Ein `[triage] FEHLER: pg_dump …` / `… CREATE DATABASE …` / `… Dump-Treue-Katalog-Gate`
   ist ein **Werkzeug-Ausfall** — melden, **nicht** als ROT werten (sonst ist der Beleg eine
   Fehlmessung).

⛔ **KEIN lokales `pytest`, kein lokales `python`, kein lokales `python -c`** als Abnahme —
auch nicht „nur kurz", auch nicht fuer einen reinen Unit-Test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Substanz-Tor auf Sprech-Substanz umbauen (Requirement 1, D-22)</name>

  <files>
config.py
services/slow_lane.py
tests/test_substanz_tor.py (NEU)
tests/test_coaching_score_cutover.py
  </files>

  <read_first>
- `services/slow_lane.py` — **`:377-435`** (die Zaehl- und Lade-Helfer-Formen) und
  **`:577-679`** (`_call_end_merge` **ganz**: die vier Vorbedingungen `:600-619`, die
  GUC-Klammer `:640-678`, der Tor-Block `:623-630`, der `ctx`-Bau `:650-657`).
- `services/slow_lane.py:398-412` — `_count_high_confidence`. ⚠ **Die Vorlage macht genau den
  Fehler, den D-22 (a) verbietet:** `if conf is None: continue` — „unbekannt zaehlt als nicht
  vorhanden". Fuer `word_count` gilt das **Gegenteil**.
- `config.py:299-314` — der Nachbar-Block `AUDIO_HEALTH_GATE_THRESHOLD` /
  `MIN_HIGH_CONFIDENCE_EVENTS`: ENV-Override + Prosa-Kommentar. **Das ist die Form.**
- `database/models.py:966-992` — die drei ZEITSTEMPEL-1-Schilder (`_SCHILD_WORD_COUNT`,
  `_SCHILD_END_MS`, `_SCHILD_TABELLE`) und der `CheckConstraint` auf `speaker`.
  ⚠ `_SCHILD_END_MS` nennt **METRIK-1 namentlich** als den Ort, an dem die Sprech-Dauer
  gerechnet wird — das ist der Vertrag aus ZEITSTEMPEL-1.
- `services/deepgram_service.py:1193-1206` — die EWB-Schreibstelle. Belegt, dass die Knopf-Zeile
  eine **echte** `transcript_segments`-Zeile mit `speaker=1` (DB-Form `'kunde'`) und **ohne**
  Wortzeiten ist.
- `tests/test_judge_runner.py:105-108` und `:169-173` — `_make_segment`, das Fake-Segment-Muster.
- ⚠ **`tests/test_coaching_score_cutover.py` — GANZE DATEI, vor der ersten Aenderung.** Sie
  zementiert das alte Tor an drei Stellen und wird durch diesen Task **rot**:
  - `:18` (Modul-Docstring): `- < MIN_HIGH_CONFIDENCE_EVENTS hoch-konf.  -> status='not_gradable', KEIN Judge-Call`
  - `:229-238`: `test_too_few_high_confidence_events_marks_not_gradable` mit
    `assert seen.get('not_gradable_reason') == 'too_few_high_confidence_events'`
  - `:112-128`: `_install_merge_doubles` patcht `_pending_events`, `_events_for_call` und
    `_count_high_confidence` — **aber nicht `_segments_for_call`**.
  ⚠ **Der Radius ist groesser als der eine Test:** nach Abschnitt C ruft `_call_end_merge` neu
  `_segments_for_call(call.conversation_log_id, read_db)` gegen `_MergeFakeSession`. **Jeder**
  Merge-Test dieser Datei, der den Tor-Block erreicht, laeuft dann in den ungepatchten Helfer.
  Das ist der Grund, warum Abschnitt E in **diesem** Task steht und nicht spaeter.
  </read_first>

  <action>
**A) `config.py` — EINE neue Schwelle direkt hinter `MIN_HIGH_CONFIDENCE_EVENTS`
(`config.py:309`), in der Form des Nachbar-Blocks. ⛔ Es wird KEINE Abschnitts-Schwelle
angelegt (Andre-Entscheidung 14.08.):**

```python
# ── Phase 08.23.2.METRIK-1: Substanz-Tor auf Sprech-Substanz (Requirement 1) ──
# Post-launch tunbar ohne Code-Deploy (Punkt 12, <30s reversibel per ENV).
# HERLEITUNG AUS DEM ZWECK, NICHT AUS DER VERTEILUNG: Eine Rueckmeldung besteht aus einem
#   WOERTLICHEN Zitat; ein zitierfaehiger Satz sind rund 10-15 Woerter. Unter 20 Woertern gibt
#   es schlicht nicht genug Text, um einen besten Moment UND eine Sache fuers naechste Mal
#   herauszuziehen. Dieselbe Zahl kaeme heraus, wenn die heutigen Messwerte andere waeren.
#   Die 87 gespeicherten Anrufe sind ausgedachte Testskripte und tragen KEINE Grenze.
# EINE BEDINGUNG, NICHT ZWEI (Andre-Entscheidung 14.08. nach dem Cross-AI-Durchgang): Bis
#   dahin war zusaetzlich eine Mindestzahl gesprochener Abschnitte vorgesehen. Sie ist
#   ERSATZLOS gestrichen. Ein am Stueck gesprochener Einstieg von 100 Woertern ist bei
#   endpointing=900 (services/deepgram_service.py) EIN Segment und waere abgewiesen worden —
#   genau der Anruf, fuer den diese Phase gebaut wird. Eine UND-Verknuepfung beider
#   Bedingungen liesse umgekehrt 7 Woerter in 3 Abschnitten durch, und daraus ist kein Zitat
#   ziehbar. Die Wortzahl allein macht beides richtig. Die Zahl der Abschnitte wird weiterhin
#   GEMESSEN und in die Ablehnungs-Zeile geschrieben - als Diagnose-Wert, nie als Bedingung.
# FEHLERRICHTUNG BEWUSST GEWAEHLT: Ein zu STRENGES Tor ist teurer als ein zu durchlaessiges —
#   duennes Material faengt die Anzeige bereits ehrlich ab, und verschaerfen kann man spaeter
#   jederzeit, waehrend abgewiesene Anrufe nie eine Rueckmeldung bekommen.
# NACHJUSTIERUNG NACH RUND 100 ECHTEN ANRUFEN, auf Basis der in der Ablehnungs-Zeile
#   mitgeschriebenen Messwerte (Requirement 2). Das ist der Termin, nicht "irgendwann".
MIN_BERATER_WOERTER = int(os.getenv('MIN_BERATER_WOERTER', '20'))
```

**`MIN_HIGH_CONFIDENCE_EVENTS` wird geloescht** — Konstante (`config.py:305-309`) **und**
Import (`services/slow_lane.py:585-586`) im **selben** Commit. Bau-Regel 20: nach dem Umbau hat
die Konstante **null** Leser im Produktivpfad; sie stehenzulassen waere Alt-Muster (Punkt 29).
Die Loeschung gehoert in die Pruning-Notiz des SUMMARY.
⚠ **`_count_high_confidence` bleibt** — die Zahl wandert als Messwert in die Ablehnungs-Zeile
(Task 2). SPEC Req 1 woertlich: „Einwand-Momente sind nur noch **ein Signal unter mehreren**,
nie das alleinige Tor."

**B) `services/slow_lane.py` — zwei neue Helfer, direkt hinter `_count_high_confidence`
(also nach `:412`, vor `_mode_config_for`):**

```python
def _segments_for_call(conversation_log_id, db) -> list:
    """Laedt alle transcript_segments einer Session (Eingang des Substanz-Tors, METRIK-1 Req 1).
    Read-only; transcript_segments hat KEINE RLS -> GUC-frei (models.py TranscriptSegment).

    ⚠ ANDERER SCHLUESSEL als _events_for_call: transcript_segments haengt an
    conversation_log_id, NICHT an call_id (vgl. services/judge_runner.py Segment-Query).
    """
    if conversation_log_id is None:
        return []
    return (db.query(TranscriptSegment)
              .filter(TranscriptSegment.conversation_log_id == conversation_log_id)
              .order_by(TranscriptSegment.ts_ms.asc(), TranscriptSegment.id.asc())
              .all())


def _mess_sprech_substanz(segments) -> dict:
    """METRIK-1 Requirement 1 + 2 (D-22): misst die Sprech-Substanz eines Anrufs.

    Returns:
        {'redeabschnitte': int,           # reine DIAGNOSE-Zahl, KEINE Tor-Bedingung
         'berater_woerter': int|None,     # None = unbekannt (NIE 0 als Ersatz)
         'sprechzeit_ms': int|None,       # None = unbekannt
         'berater_wortzahl_unbekannt': bool}

    DREI REGELN, jede loest eine belegte Falle:

    (a) NULL heisst UNBEKANNT, nie "null Woerter". ⚠ Das ist die UMGEKEHRTE NULL-Semantik zur
        Schwester-Funktion _count_high_confidence, die confidence=None als "zaehlt nicht"
        behandelt. Hier waere das die teure Fehlerrichtung: ein Endergebnis MIT Text, aber
        OHNE Wortobjekte liefert word_count NULL, obwohl gesprochen wurde (Schild
        database/models.py _SCHILD_WORD_COUNT, "Bekannte Kante"). Ein echter 30-Woerter-Anruf
        mit zwei NULL-Zeilen laege sonst unter 20 und wuerde faelschlich abgelehnt.

    (b) redeabschnitte ist eine DIAGNOSE-Zahl, KEINE Bedingung. Gezaehlt werden Zeilen mit
        word_count IS NOT NULL (alle Sprecher) - die Definition bleibt, damit die Zahl in der
        Ablehnungs-Zeile eine feste Bedeutung hat. Sie geht in KEINE Tor-Entscheidung ein:
        die Abschnitts-Bedingung ist am 14.08. ersatzlos gestrichen worden, weil ein am
        Stueck gesprochener 100-Woerter-Einstieg bei endpointing=900 EIN Segment ist.
        Damit entfaellt auch der frueher hier beschriebene Sonderfall "ein Satz plus ein
        Knopfdruck" - er kann per Bauart nicht mehr entstehen.

    (c) Gezaehlt werden nur BERATER-Woerter (speaker == 'berater'). Im Meeting-Modus tragen
        auch Kunden-Zeilen word_count; wer stumpf summiert, laesst ein Meeting durch, in dem
        fast nur der Kunde sprach.

    (c2) ABLEITUNG AUS (c), ausdruecklich festgehalten: die Unbekannt-Erkennung aus (a)
        schaut NUR auf BERATER-Zeilen. Sonst kippt die EWB-Zeile (speaker='kunde',
        word_count NULL) jeden Anruf in den "unbekannt -> durchlassen"-Zweig, und das Tor
        waere per Knopfdruck aushebelbar. Seit die Abschnitts-Bedingung gestrichen ist, ist
        das die EINZIGE verbliebene EWB-Kante des Tors — sie traegt hier allein.

    Reine Funktion: kein DB-Zugriff, kein Seiteneffekt.
    """
```
Implementierungs-Vorgaben:
- `redeabschnitte` = Anzahl der Segmente mit `word_count is not None` (**alle** Sprecher).
  **Reiner Messwert fuer die Ablehnungs-Zeile — er geht in keine Entscheidung ein.**
- `berater` = Segmente mit `speaker == 'berater'`. **Nicht** `speaker == 0` — die Ganzzahl aus
  `deepgram_service.py` ist die **RAM**-Form; die DB-Form ist der String (CheckConstraint
  `ck_transcript_segments_speaker`).
- `berater_woerter` = Summe der `word_count` der Berater-Zeilen mit `word_count is not None`.
  Gibt es **keine** solche Zeile → `None`.
- `berater_wortzahl_unbekannt` = `True`, wenn **mindestens eine** Berater-Zeile
  `word_count is None` hat.
- `sprechzeit_ms` = Summe von `max(0, end_ms - start_ms)` ueber Berater-Zeilen, bei denen
  **beide** Werte nicht `None` sind; keine solche Zeile → `None`.
  ⚠ Das `max(0, ...)` ist kein Schoenheitsfehler: das Schild `_SCHILD_START_MS` nennt
  ueberlappende Deepgram-Endergebnisse ausdruecklich als **UNVERIFIED** und verlangt vom Leser,
  negative Differenzen abzufangen.

Und die Tor-Entscheidung als eigene reine Funktion daneben. **Sie gibt ein PAAR zurueck: den
Ablehnungs-Grund und den genommenen ZWEIG.** Der Zweig ist die Umsetzung der Cross-AI-Auflage —
„unbekannte Wortzahl" ist ein eigener Programm-Weg mit eigenem String, nicht dieselbe
Entscheidung unter anderem Namen:

```python
# ── METRIK-1 Requirement 1: die vier Tor-Zweige. Jeder Weg hat seinen EIGENEN String. ──────
#    'wortzahl_unbekannt_durchgelassen' ist ausdruecklich KEINE Ablehnung unter anderem Namen:
#    er laesst DURCH (fail-open) und ist vom Genug-Zweig unterscheidbar. Zwei unabhaengige
#    Gegenleser haben beanstandet, dass diese Zusage vorher nur in der Prosa stand.
_TOR_ZWEIG_GENUG = 'genug_woerter'
_TOR_ZWEIG_WORTZAHL_UNBEKANNT = 'wortzahl_unbekannt_durchgelassen'
_TOR_ZWEIG_ZU_WENIG = 'zu_wenig_woerter'
_TOR_ZWEIG_KEINE_BERATER_ZEILE = 'keine_berater_zeile'


def _tor_sprech_substanz(mess: dict) -> tuple:
    """Entscheidet ueber die Sprech-Substanz. Liefert (grund, zweig).

    grund: 'too_little_speech' oder None (durchlassen).
    zweig: einer der vier _TOR_ZWEIG_*-Strings. Er sagt, WELCHER Weg genommen wurde, und
           wird in der Ablehnungs-Zeile mitgeschrieben (Requirement 2).

    EINZIGE ABLEHNUNGS-BEDINGUNG IST DIE WORTZAHL (Andre-Entscheidung 14.08.). Die Zahl der
    Redeabschnitte wird gemessen und mitgeschrieben, entscheidet aber nichts: ein am Stueck
    gesprochener 100-Woerter-Einstieg ist bei endpointing=900 EIN Segment und waere sonst
    abgewiesen worden.

    FAIL-OPEN ALS EIGENER ZWEIG: Ist die BEKANNTE Berater-Wortsumme kleiner als die Schwelle
    und ist mindestens eine Berater-Wortzahl unbekannt, ist die Wortbedingung nicht
    entschieden -> der Anruf geht durch, und zwar ueber _TOR_ZWEIG_WORTZAHL_UNBEKANNT, NICHT
    ueber den Genug-Zweig. Der eigene Zweig ist der Unterschied zwischen "wir wissen, dass
    genug gesprochen wurde" und "wir wissen es nicht und lassen im Zweifel durch".

    Reine Funktion: kein DB-Zugriff, kein Seiteneffekt.
    """
```
Kaskade (genau in dieser Reihenfolge, damit die Semantik nachlesbar ist):
1. `berater_woerter is None and berater_wortzahl_unbekannt` (es wurde gesprochen, aber keine
   einzige Zahl ist bekannt) → `(None, _TOR_ZWEIG_WORTZAHL_UNBEKANNT)` — **durchlassen.**
   **Das ist SPEC `:517` woertlich erfuellt, im Code und nicht in der Prosa.**
2. `berater_woerter is None` (gar keine Berater-Zeile, auch keine unbekannte) →
   `('too_little_speech', _TOR_ZWEIG_KEINE_BERATER_ZEILE)`
3. `berater_woerter >= MIN_BERATER_WOERTER` → `(None, _TOR_ZWEIG_GENUG)`
4. `berater_wortzahl_unbekannt` (bekannte Summe zu klein, aber es gibt Unbekanntes) →
   `(None, _TOR_ZWEIG_WORTZAHL_UNBEKANNT)` — **durchlassen**
5. sonst → `('too_little_speech', _TOR_ZWEIG_ZU_WENIG)`

⛔ **Es gibt keinen Zweig auf `redeabschnitte`.** Ein solcher Zweig waere die geloeschte
Bedingung unter anderem Namen.

**C) Den Tor-Block ersetzen** (`services/slow_lane.py:623-630`). Der `if`-Zweig fuer das
Audio-Tor (`:627-628`) bleibt **zeichengleich** stehen (SPEC Req 1: „Das Audio-Gueten-Tor bleibt
unveraendert"). Nur der `elif` (`:629-630`) wird ersetzt:

```python
        # ── Audio-Gate D-09 (VOR dem Scoring): events laden + high-conf SELBST zaehlen ──────
        events = _events_for_call(call_id, read_db)
        n_high_conf = _count_high_confidence(events, read_db)
        # ── METRIK-1 Requirement 1: Sprech-Substanz statt Einwand-Momente ───────────────────
        # Hier stand bis 2026-08-13 das alte Einwand-Momente-Tor: eine Mindest-Schwelle auf der
        # Zahl der hoch-konfidenten Momente, die den Anruf allein durchliess oder abwies. Es
        # sperrte 83 % aller Anrufe aus — im Kaltakquise-Modus hoert NERVE nur den Berater und
        # die automatische Einwand-Erkennung ist kanonisch abgeschaltet, Momente entstehen fast
        # nur per Knopfdruck. n_high_conf bleibt ein Signal unter mehreren (es wandert als
        # Messwert in die Ablehnungs-Zeile), ist aber NIE mehr das alleinige Tor.
        # ⛔ Den alten Ausdruck NICHT zitieren (weder Konstanten-Name noch Vergleich): die
        #    Loesch-Anker dieser Phase verlangen die Zeichenkette 0-mal in dieser Datei.
        #    Beschreiben statt zitieren — sonst ueberlebt sie ihre eigene Loeschung.
        # Das neue Tor hat GENAU EINE Bedingung: die Zahl der gesprochenen Berater-Woerter.
        # Die Zahl der Redeabschnitte wird gemessen und mitgeschrieben, entscheidet aber nichts.
        segments = _segments_for_call(call.conversation_log_id, read_db)
        mess_substanz = _mess_sprech_substanz(segments)
        mess_substanz['high_conf_events'] = n_high_conf
        mess_substanz['tor_zweig'] = None
        not_gradable_reason = None
        if call.audio_health_score is None or call.audio_health_score < AUDIO_HEALTH_GATE_THRESHOLD:
            not_gradable_reason = 'poor_audio_health'
        else:
            not_gradable_reason, mess_substanz['tor_zweig'] = _tor_sprech_substanz(mess_substanz)
```
Import-Zeile `:585-586` anpassen: `MIN_HIGH_CONFIDENCE_EVENTS` raus, `MIN_BERATER_WOERTER`
rein (**nur diese eine** — es gibt keine Abschnitts-Schwelle). `TranscriptSegment` zu den Modul-Importen von
`slow_lane.py` ergaenzen (bzw. lokal importieren, wie es die Datei sonst tut — **die bestehende
Konvention der Datei uebernehmen, nicht eine neue erfinden**).

**Kein Vorauslaufen moeglich:** `_call_end_merge` liest die Segmente erst, nachdem
`transcript_resolved == True` geprueft ist (`slow_lane.py:618-619`). Das ist die bestehende
Punkt-26-Bereitschafts-Naht aus TAXO2-Plan 03 — sie wird hier **benutzt**, nicht neu gebaut.

**D) Neue Tests `tests/test_substanz_tor.py`** — reine Funktions-Tests auf
`_mess_sprech_substanz` + `_tor_sprech_substanz` mit gebauten Fake-Segmenten (Muster
`tests/test_judge_runner.py::_make_segment`, erweitert um `word_count`, `start_ms`, `end_ms`).
**Erst rot laufen lassen** (die Funktionen existieren noch nicht) — der ROT-Lauf gehoert ins
SUMMARY (Vault-Regel Nr. 1).

Pflicht-Testfunktionen (jede Kante eine eigene Funktion, keine Schleife). **Jede prueft den
Rueckgabewert, also (grund, zweig) — nicht nur den Grund. Der Zweig ist der Beleg dafuer, dass
die Wege im CODE getrennt sind und nicht nur in der Prosa:**
1. `test_zwanzig_woerter_ohne_moment_wird_bewertet` — 2 Berater-Zeilen mit `word_count` 10 und
   10, `high_conf_events=0` → `(None, 'genug_woerter')`.
   **Das ist das Abnahme-Kriterium aus SPEC Req 1, als Verhaltens-Test.**
2. **👁 `test_hundert_woerter_in_einem_einzigen_abschnitt_wird_durchgelassen`** — **EINE**
   Berater-Zeile mit `word_count=100` → `(None, 'genug_woerter')`, und `redeabschnitte == 1`.
   **Das ist der Cross-AI-Fund vom 14.08. als Verhaltens-Test** (Einstieg am Stueck ohne
   Atempause, bei `endpointing=900` EIN Segment). ⛔ **Dieser Test ist die Gegenprobe zur
   gestrichenen Abschnitts-Bedingung: waere sie noch da, waere er rot.**
3. `test_vier_woerter_wird_abgelehnt` — 1 Berater-Zeile mit `word_count=4` →
   `('too_little_speech', 'zu_wenig_woerter')`. **SPEC-Abnahme „Ein Anruf mit 4 Woertern wird
   abgelehnt".**
4. **`test_nur_null_wortzahlen_laeuft_ueber_den_eigenen_unbekannt_zweig`** — 2 Berater-Zeilen,
   **beide** `word_count=None` → `(None, 'wortzahl_unbekannt_durchgelassen')`.
   **Das ist SPEC `:517` woertlich erfuellt** — durchgelassen, und zwar ueber einen **eigenen**
   Zweig, nicht ueber den Genug-Zweig. **Der Docstring haelt fest, dass genau dieser Punkt von
   zwei unabhaengigen Gegenlesern (Gemini B-3 / GSD W-5) als „nur in Prosa aufgeloest"
   beanstandet wurde.**
5. `test_gemischte_null_unter_zwanzig_laeuft_ueber_den_eigenen_unbekannt_zweig` — 3
   Berater-Zeilen: `word_count` 8, 6, `None` (bekannte Summe 14 < 20, aber ein NULL vorhanden)
   → `(None, 'wortzahl_unbekannt_durchgelassen')`. **Die fail-open-Kante.**
6. `test_ohne_jede_berater_zeile_wird_abgelehnt` — nur Kunde-Zeilen, keine einzige
   Berater-Zeile → `('too_little_speech', 'keine_berater_zeile')`. **Die Abgrenzung zu 4:
   „gar nichts gesagt" ist nicht dasselbe wie „unbekannt".** Ohne diesen Test waere der
   fail-open-Zweig nicht von einem Durchwinken aller Anrufe zu unterscheiden.
7. `test_ewb_zeile_kippt_nicht_in_den_unbekannt_zweig` — drei Berater-Zeilen mit je
   `word_count=5` (bekannte Summe 15 < 20, **kein** Berater-NULL) plus die EWB-Kunde-Zeile mit
   Text `'Kein Interesse. *ewb button*'` und `word_count=None` →
   `('too_little_speech', 'zu_wenig_woerter')`. **Das ist die Ableitung (c2) und seit der
   Streichung der Abschnitts-Bedingung die EINZIGE verbliebene EWB-Kante des Tors** — ohne sie
   waere das Tor per Knopfdruck in den fail-open-Zweig kippbar.
8. `test_nur_kundenwoerter_werden_nicht_gezaehlt` — 3 Kunde-Zeilen mit `word_count` 40, 40, 40
   und 1 Berater-Zeile mit `word_count=3` → `berater_woerter == 3` und
   `('too_little_speech', 'zu_wenig_woerter')`. **Das ist Regel (c) im Meeting-Modus.**
9. `test_redeabschnitte_sind_diagnose_und_keine_bedingung` — Messwert-Test auf
   `_mess_sprech_substanz`: 1 Berater-Zeile (`word_count=100`) plus 2 Zeilen mit
   `word_count=None` → `redeabschnitte == 1`. **Die Zahl wird korrekt gemessen** — und Test 2
   belegt daneben, dass sie **nichts entscheidet**. Der Docstring nennt beide Haelften.
10. `test_sprechzeit_summiert_nur_berater_und_klemmt_negativ` — Berater-Zeilen mit
    (`start_ms=0`, `end_ms=1000`), (`start_ms=2000`, `end_ms=1500`) → `sprechzeit_ms == 1000`
    (die negative Differenz traegt 0 bei, nicht -500).
11. `test_sprechzeit_unbekannt_ist_none_nicht_null` — Berater-Zeilen ohne `start_ms`/`end_ms` →
    `sprechzeit_ms is None`.
12. `test_audio_tor_hat_vorrang` — Verhaltens-Test auf der Reihenfolge: bei
    `audio_health_score = 0.1` **und** zu wenig Sprech-Substanz lautet der Grund
    `'poor_audio_health'`. *(Als Test auf `_tor_sprech_substanz` nicht abbildbar — hier wird
    die Kaskade aus dem Tor-Block nachgebaut; im Docstring des Tests festhalten, dass das
    Audio-Tor unveraendert vorne steht.)*

**E) Der stale Bestandstest wird im SELBEN Task retargetiert —
`tests/test_coaching_score_cutover.py`.**

⚠ **Warum das hier steht und nicht spaeter:** Dieser Task entfernt genau das Tor, das der Test
festnagelt. Der Test wird dadurch **rot** — und zwar an **Task 3 dieses Plans**, dem **einzigen**
Ausroll-Punkt von Plan 01 **und** Plan 02 (D-01). Ein rotes Test-Tor bedeutet: **kein Neustart,
der alte Stand bleibt live.** Ein stale Test wuerde also das eine Ausrollen der halben Phase
blockieren.

**E1) Den ROT-Lauf ZUERST ziehen.** Nach A–C, **vor** der Anpassung — **nach dem
ROT-LAUF-REZEPT im `<interfaces>`-Block**, also serverseitig ueber `scripts/triage.sh`:
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166   'cd /opt/nerve/app && bash scripts/triage.sh tests/test_coaching_score_cutover.py -v --tb=long'
```
⛔ **Hier stand bis zum 14.08. ein nacktes lokales `pytest tests/… -x`.** Das war ein Verstoss
gegen die HART-Regel „Kein Local-Dev — KEIN lokales pytest als Acceptance-Check" und zugleich der
**einzige** konkrete Testbefehl im ganzen Plansatz. Er ist **ersetzt**, nicht ergaenzt.
⚠ Der Code aus A–C muss dafuer **committet, gepusht und auf dem Server** sein — sonst triagiert
der Lauf gegen den alten Stand und ist **blind statt rot** (Bedingung 1 des Rezepts).
Die Ausgabe geht **verbatim** ins SUMMARY, ausdruecklich gefuehrt als
**„erwartet rot durch Vertragswechsel D-22, im selben Task retargetiert"** — **nicht** als Fund
und **nicht** als Regression. (Vault-Regel Nr. 1: erst rot, dann fixen; der Rot-Lauf ist das
Artefakt.)

**E2) `_install_merge_doubles` (`:112-128`) um den neuen Lade-Helfer erweitern.**
Der Merge ruft ab jetzt `_segments_for_call`; ohne Doppel laeuft er gegen `_MergeFakeSession`.

```python
def _install_merge_doubles(monkeypatch, call, *, pending=0, events=None,
                           high_conf=5, sessions=None, segments=None):
    ...
    monkeypatch.setattr(sl, '_count_high_confidence', lambda evs, db: high_conf)
    # METRIK-1 Req 1 (D-22): der Merge laedt jetzt zusaetzlich die Transkript-Segmente fuer das
    # Sprech-Substanz-Tor. Ohne dieses Doppel liefe die Abfrage gegen _MergeFakeSession und
    # JEDER Merge-Test, der den Tor-Block erreicht, wuerde daran haengen — nicht nur der eine
    # umgestellte Test.
    # Voreinstellung: zwei Berater-Zeilen mit je 15 Woertern => das Tor laesst durch. So bleiben
    # alle Bestands-Tests, die das Tor gar nicht meinen, unveraendert gruen.
    monkeypatch.setattr(sl, '_segments_for_call',
                        lambda clid, db: segments if segments is not None else _durchlass_segmente())
    return sess_list
```
Dazu ein kleiner Helfer `_durchlass_segmente()` in derselben Datei, der zwei Fake-Segmente mit
`speaker='berater'` und `word_count=15` liefert (Muster: `_make_segment` aus
`tests/test_judge_runner.py`).
⚠ **Die Voreinstellung muss DURCHLASSEN.** Ein Doppel, das ablehnt, faerbte die Bestands-Tests
still um — sie pruefen andere Dinge und duerfen sich durch dieses Tor nicht aendern.

**E3) `test_too_few_high_confidence_events_marks_not_gradable` (`:229-238`) auf den neuen
Vertrag umstellen.** Umbenennen in `test_too_little_speech_marks_not_gradable`, mit dem neuen
Grund und mit **gepaartem Existenz-Anker**:

```python
def test_too_little_speech_marks_not_gradable(monkeypatch):
    # METRIK-1 Req 1 / D-22 (Vertragswechsel 2026-08-13): Das alte Einwand-Momente-Tor — die
    # Mindest-Schwelle auf der Zahl hoch-konfidenter Momente — ist ENTFALLEN. Es sperrte 83 %
    # aller Anrufe aus, weil im Kaltakquise-Modus die automatische Einwand-Erkennung kanonisch
    # abgeschaltet ist. An seiner Stelle steht das Sprech-Substanz-Tor. n_high_conf bleibt ein
    # MESSWERT in der Ablehnungs-Zeile, ist aber nie mehr das Tor: high_conf=1 allein lehnt
    # NICHT mehr ab.
    # ⛔ Konstanten-Name und Vergleich werden hier bewusst NICHT zitiert — der Loesch-Anker
    #    dieser Phase verlangt sie 0-mal, auch in tests/.
    call = _make_call(audio_health_score=0.9)
    _install_merge_doubles(monkeypatch, call, pending=0, high_conf=1,
                           segments=[_make_segment(speaker='berater', word_count=3)])
    seen = {}
    monkeypatch.setattr(sl, '_CALL_END_MERGE_STEPS', [lambda ctx: seen.update(ctx)])

    sl._call_end_merge({'call_id': call.id})

    assert seen.get('not_gradable_reason') == 'too_little_speech'
    # Gepaarter Existenz-Anker: der Tor-Block lief ueberhaupt und der Messwert steht daneben —
    # sonst waere "richtiger Grund" nicht von "Merge gar nicht gefeuert" zu unterscheiden.
    assert seen.get('mess_substanz', {}).get('high_conf_events') == 1
```

**E4) Ein zweiter Test daneben — die Umkehrung, ohne die E3 nichts beweist:**
```python
def test_wenig_hoch_konfidente_events_lehnen_NICHT_mehr_ab(monkeypatch):
    # Das eigentliche Abnahme-Kriterium von Requirement 1: 0 Momente, aber Sprech-Substanz da
    # -> der Anruf wird BEWERTET. Ohne diesen Test belegt E3 nur, dass irgendein Grund gesetzt
    # wird, nicht dass das alte Tor wirklich tot ist.
    call = _make_call(audio_health_score=0.9)
    _install_merge_doubles(monkeypatch, call, pending=0, high_conf=0)   # Voreinstellung laesst durch
    ...
    assert seen.get('not_gradable_reason') is None
```

**E5) Den Modul-Docstring (`:18`) nachziehen** — er zementiert das alte Tor in Prosa:
```
        - < MIN_HIGH_CONFIDENCE_EVENTS hoch-konf.  -> status='not_gradable', KEIN Judge-Call
```
wird zu:
```
        - zu wenig Sprech-Substanz (METRIK-1 D-22)  -> status='not_gradable', KEIN Judge-Call
          (die fruehere Drei-Momente-Schwelle auf den hoch-konfidenten Einwand-Momenten ist
           entfallen; die Zahl bleibt als Messwert in der Ablehnungs-Zeile)
        ⛔ Der alte Konstanten-Name wird hier bewusst nicht genannt — er ist in dieser Phase
           restlos geloescht und darf als Zeichenkette nirgends mehr auftauchen.
```

⚠ **Reparatur-Modus (Vault-Regel Nr. 5):** In dieser Datei wird **nur** angefasst, was der
Vertragswechsel mechanisch erzwingt — Docstring `:18`, `_install_merge_doubles`, die eine
umgestellte Testfunktion plus die neue Umkehrung. **Keine** Umbenennungen, **keine**
Import-Sortierung, **keine** „wenn ich schon dabei bin"-Verbesserungen.
  </action>

  <verify>
    <automated>grep -c "def _mess_sprech_substanz\|def _tor_sprech_substanz\|def _segments_for_call" services/slow_lane.py</automated>
    <automated>grep -c "MIN_HIGH_CONFIDENCE_EVENTS" config.py services/slow_lane.py</automated>
    <!-- Serverseitiges Test-Tor: Task 3 dieses Plans (EIN Ausrollen, D-01). -->
  </verify>

  <acceptance_criteria>
- `grep -c "def _mess_sprech_substanz" services/slow_lane.py` liefert **1**;
  `grep -c "def _tor_sprech_substanz" services/slow_lane.py` liefert **1**;
  `grep -c "def _segments_for_call" services/slow_lane.py` liefert **1**.
- **① Die Abschnitts-Bedingung existiert NICHT — Abwesenheits-Anker auf die Code-FORM einer
  Abschnitts-Schwelle, mit gepaartem Existenz-Anker:**
  - `grep -c "MIN_REDEABSCHNITTE" config.py services/slow_lane.py` liefert **0**.
  - `grep -c "redeabschnitte <" services/slow_lane.py` liefert **0** (kein Vergleich auf der
    Abschnitts-Zahl — das ist die Code-Form der geloeschten Bedingung, nicht der nackte Name).
  - Gepaarter Existenz-Anker **derselben Datei**:
    `grep -c "'redeabschnitte':" services/slow_lane.py` liefert **>= 1** — die Zahl wird sehr
    wohl **gemessen** und mitgeschrieben, sie entscheidet nur nichts. Ohne diesen Partner waere
    „0 Treffer" nicht von „Datei gar nicht gelesen" zu unterscheiden.
- **② Der eigene Unbekannt-Zweig ist im CODE da (nicht in der Prosa) — Anker auf die
  Code-Form, nicht auf den nackten Namen:**
  - `grep -c "_TOR_ZWEIG_WORTZAHL_UNBEKANNT = 'wortzahl_unbekannt_durchgelassen'" services/slow_lane.py`
    liefert **1** (die **Definition** des eigenen Zweig-Strings).
  - `grep -c "(None, _TOR_ZWEIG_WORTZAHL_UNBEKANNT)" services/slow_lane.py` liefert **2** — die
    **zwei** fail-open-Kanten (Kaskade 1 und 4). Der Zweig wird an beiden Kanten genommen und
    liefert **kein** `reason`. **Das ist der Beweis „durchlassen", nicht „andere Ablehnung".**
    ⚠ **Der Anker ist bewusst auf die Kanten-FORM gebunden, nicht auf eine Gesamtzahl des
    Namens.** Eine Gesamtzahl waere durch diesen Plan selbst unerfuellbar: der Name steht nach
    dem Bau **viermal** in der Datei (Definition · zwei Kanten · **die Nennung im Docstring von
    `_tor_sprech_substanz`, den dieser Task oben vorschreibt**). Ein Sollwert 3 haette den
    Ausfuehrer falsch-rot laufen lassen — und ihn verleitet, den Docstring-Satz zu loeschen, um
    die Zahl zu treffen. Genau der Anker-formt-das-Artefakt-Fehler, den dieser Plan abstellt.
    ⚠ **Hier stand bis zum 14.08. `grep -c "return None, _TOR_ZWEIG_WORTZAHL_UNBEKANNT" → 2`.**
    Das war ein **„Anker folgt dem Artefakt"-Verstoss**: die Kaskaden-Vorgabe im **selben Task**
    (Schritt B, Punkte 1 und 4) schreibt die Rueckgabe **mit Klammern** vor
    (`` -> `(None, _TOR_ZWEIG_WORTZAHL_UNBEKANNT)` ``). Haelt der Ausfuehrer sich an die
    Vorgabe — was er soll — faellt der alte Anker auf **0**. **Das Kriterium war falsch, nicht
    der Code.** Die Klammer-Form der Kaskade bleibt unveraendert; der Anker ist jetzt
    klammer-unabhaengig.
  - Gepaarter Existenz-Anker: `grep -c "_TOR_ZWEIG_ZU_WENIG" services/slow_lane.py` liefert
    **>= 2** (Definition + Verwendung) — die Ablehnungs-Zweige stehen daneben und sind
    unterscheidbar.
  - **Der belastbare Beleg ist aber der Verhaltens-Test, nicht der grep:** Testfunktionen 4
    und 5 pruefen den **Rueckgabewert** `(None, 'wortzahl_unbekannt_durchgelassen')`.
- **Abwesenheits-Anker auf Code-FORM (das alte Tor ist wirkungslos, nicht nur umbenannt) —
  ZWEI Formen, weil eine allein den Vergleich nur halb trifft:**
  - `grep -c "elif n_high_conf <" services/slow_lane.py` liefert **0** (der Tor-Zweig als
    Code-Form — gemessen am 2026-08-13 steht er 1-mal, `services/slow_lane.py:629`).
  - `grep -c "n_high_conf < MIN_HIGH_CONFIDENCE_EVENTS" services/slow_lane.py` liefert **0**.
  Gepaarter Existenz-Anker: `grep -c "n_high_conf = _count_high_confidence(" services/slow_lane.py`
  liefert **1** — die Zaehlung lebt weiter, nur das Tor nicht.
  ⚠ **Beide Anker sind nur erfuellbar, weil KEIN Kommentar dieses Tasks den alten Ausdruck
  zitiert.** Das ist keine Stil-Frage: ein „warum-entfernt"-Kommentar, der den entfernten
  Wortlaut zitiert, laesst die Zeichenkette ihre eigene Loeschung ueberleben und macht den
  Anker unerfuellbar. **Beschreiben statt zitieren** — an allen drei Stellen dieses Tasks
  (Tor-Block, Testkommentar, Modul-Docstring) ausdruecklich so vorgegeben.
- **Die Konstante ist restlos raus (Bau-Regel 20 + Punkt 29) — `tests/` AUSDRUECKLICH mit:**
  `grep -rn "MIN_HIGH_CONFIDENCE_EVENTS" config.py services/ routes/ tests/ --include=*.py | wc -l`
  liefert **0**.
  ⛔ **Das schliesst Kommentare, Docstrings und Prosa ausdruecklich ein.** Kein Kommentar
  dieses Tasks darf den Konstanten-Namen nennen — auch nicht erklaerend, auch nicht in
  Anfuehrungszeichen. Wer den entfernten Namen zitiert, macht diesen Anker unerfuellbar.
  Gemessen am 2026-08-13 stehen **4** Treffer (`config.py:305` Prosa-Kommentar, `config.py:309`
  Definition, `services/slow_lane.py:585` Import, `:629` Tor) plus **2** in
  `tests/test_coaching_score_cutover.py` (`:18`, `:230`) — alle sechs fallen in diesem Task. Gepaarter Existenz-Anker:
  `grep -c "AUDIO_HEALTH_GATE_THRESHOLD" config.py` liefert **>= 2** (der Nachbar-Eintrag ist
  unangetastet).
  ⚠ **Der Suchpfad enthielt `tests/` in der ersten Fassung dieses Plans NICHT.** Gemessen am
  2026-08-13 liegen dort **zwei** Treffer (`tests/test_coaching_score_cutover.py:18` und `:230`)
  — beide haetten einen ausgeklammerten Suchpfad ueberlebt und das eine Ausrollen (Task 3) an
  einem stale Test scheitern lassen. Ein Pflicht-grep, der den Ort auslaesst, an dem der Vertrag
  festgenagelt ist, prueft die Loeschung nicht zu Ende.
- **Der alte Ablehnungs-Grund wird nicht mehr GESCHRIEBEN, der Anzeige-Zweig bleibt:**
  `grep -rn "too_few_high_confidence_events" services/ tests/ --include=*.py | wc -l` liefert
  **0**; gepaarter Existenz-Anker:
  `grep -c "too_few_high_confidence_events" templates/session_detail.html` liefert **1**
  (Alt-Zeilen stehen in der Datenbank — der **Anzeige**-Zweig bleibt bewusst erhalten, Plan 03).
- **Abschnitt E ist wirklich gebaut (der stale Bestandstest):**
  - `grep -c "def test_too_few_high_confidence_events_marks_not_gradable" tests/test_coaching_score_cutover.py`
    liefert **0**;
    `grep -c "def test_too_little_speech_marks_not_gradable" tests/test_coaching_score_cutover.py`
    liefert **1**;
    `grep -c "def test_wenig_hoch_konfidente_events_lehnen_NICHT_mehr_ab" tests/test_coaching_score_cutover.py`
    liefert **1**.
  - **Das Doppel deckt den neuen Lade-Helfer ab:**
    `grep -c "_segments_for_call" tests/test_coaching_score_cutover.py` liefert **>= 1**;
    gepaarter Existenz-Anker: `grep -c "_count_high_confidence" tests/test_coaching_score_cutover.py`
    liefert **>= 1** (die bestehenden Doppel sind nicht mitgeloescht worden).
  - **Der ROT-Lauf aus E1 steht verbatim im SUMMARY**, gefuehrt als *„erwartet rot durch
    Vertragswechsel D-22, im selben Task retargetiert"* — nicht als Fund.
  - **Reparatur-Modus belegt:** `git diff --stat tests/test_coaching_score_cutover.py` steht im
    SUMMARY; der Diff beruehrt ausschliesslich Docstring `:18`, `_install_merge_doubles`, die
    umgestellte Testfunktion und die neue Umkehrung.
- **Das Audio-Tor ist zeichengleich geblieben (SPEC Req 1):**
  `grep -c "call.audio_health_score is None or call.audio_health_score < AUDIO_HEALTH_GATE_THRESHOLD" services/slow_lane.py`
  liefert **1**.
- **Die DB-Form des Sprechers, nicht die RAM-Form:**
  `grep -c "speaker == 'berater'" services/slow_lane.py` liefert **>= 1**;
  `grep -c "speaker == 0\|speaker == 1" services/slow_lane.py` liefert **0**.
- **Der richtige Fremdschluessel:**
  `grep -c "TranscriptSegment.conversation_log_id ==" services/slow_lane.py` liefert **1**;
  `grep -c "TranscriptSegment.call_id" services/slow_lane.py` liefert **0**.
- `grep -c "MIN_BERATER_WOERTER" config.py` liefert **>= 1**, und die Prosa darueber nennt
  die **Herleitung aus dem Zweck**, den Nachjustier-Termin („rund 100 echten Anrufen") **und**
  die Streichung der Abschnitts-Bedingung samt Begruendung.
- `grep -c "^def test_" tests/test_substanz_tor.py` liefert **12**.
- **👁 Der Cross-AI-Fund ist als Test da:**
  `grep -c "def test_hundert_woerter_in_einem_einzigen_abschnitt_wird_durchgelassen" tests/test_substanz_tor.py`
  liefert **1**, und der Test steht **gruen**.
- **Der eigene Unbekannt-Zweig ist als Verhaltens-Test da:**
  `grep -c "wortzahl_unbekannt_durchgelassen" tests/test_substanz_tor.py` liefert **>= 3**
  (Tests 4, 5 und die Abgrenzung in Test 6 bzw. 7); gepaarter Existenz-Anker:
  `grep -c "keine_berater_zeile" tests/test_substanz_tor.py` liefert **>= 1** — der
  Nachbar-Zweig wird ebenfalls geprueft, „unbekannt" ist also nicht der Sammel-Ausgang.
- `grep -c "inspect.getsource\|hasattr(" tests/test_substanz_tor.py` liefert **0**;
  gepaarter Existenz-Anker: `grep -c "assert " tests/test_substanz_tor.py` liefert **>= 20**.
- Der ROT-Lauf von `tests/test_substanz_tor.py` **gegen den ungefixten Stand** steht
  **verbatim** im SUMMARY (Vault-Regel Nr. 1: erst rot, dann fixen).
  </acceptance_criteria>

  <done>
Ein Anruf mit zwanzig Berater-Woertern wird bewertet, auch wenn er keinen einzigen
hoch-konfidenten Moment hat **und auch dann, wenn er am Stueck in EINEM Redeabschnitt
gesprochen wurde**. Ein Anruf mit vier Woertern faellt raus. Unbekannte Wortzahlen fuehren
**nie** zu einer Ablehnung — sie laufen ueber einen **eigenen** Zweig, der im Rueckgabewert
sichtbar ist. **Und der Bestandstest, der das alte Tor zementierte, prueft die neue Zusage —
das eine Ausrollen in Task 3 kann nicht an einem stale Test scheitern.**
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Messwerte in die Ablehnungs-Zeile + Schilder nachziehen (Requirement 2, Punkt 23)</name>

  <files>
services/slow_lane.py
database/models.py
alembic/versions/0040_metrik1_schilder.py (NEU)
tests/test_substanz_tor.py
  </files>

  <read_first>
- `services/slow_lane.py:488-548` (`_judge_step`, besonders der Ablehnungs-Block `:515-527`) und
  `:640-679` (die GUC-Klammer + der `ctx`-Bau `:650-657`).
- `services/slow_lane.py:437-485` (`_upsert_rubric_score`) — insbesondere `update_cols`
  (`:473-477`): `payload_jsonb` wird **vollstaendig ersetzt**.
- `database/models.py:901-938` — `RubricScore`, besonders die heutigen Schild-Texte von
  `status` (`:921`) und `payload_jsonb` (`:923`). **Beide werden durch diese Phase falsch.**
- `database/models.py:966-971` — die ZEITSTEMPEL-1-Schild-Konstanten. **Das ist die Bauform**:
  Modul-Konstante, zeichengleich zur Migration.
- `alembic/versions/0039_zeitstempel1_sprechzeiten.py` — **ganze Datei**. Besonders der
  `_comment`-Helfer (`:52-73`, Doppelpunkt-Falle) und die `upgrade`/`downgrade`-Form.
- `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-01-SUMMARY.md` — welche Schluessel Plan 01
  im `payload_jsonb` anlegt (`beleg_check`). Das Schild beschreibt **beide** Bereiche.
  </read_first>

  <action>
**A) Messwerte durchreichen, nicht zweimal rechnen.**

`ctx`-Bau (`services/slow_lane.py:650-657`) um einen Schluessel erweitern:

```python
        ctx = {
            'call': call_w,
            'events': events_w,
            'db': write_db,
            'high_conf': n_high_conf,
            # METRIK-1 Requirement 2: die Messwerte, MIT DENEN das Tor entschieden hat, werden
            # durchgereicht statt in _judge_step neu gezaehlt. Zweimal zaehlen hiesse: der
            # gespeicherte Wert koennte vom Wert abweichen, der die Ablehnung ausgeloest hat —
            # und genau dieser Wert ist die Grundlage der Nachjustierung nach ~100 echten Anrufen.
            'mess_substanz': mess_substanz,
            'not_gradable_reason': not_gradable_reason,
            'results': {},
        }
```

**B) Ablehnungs-Payload** (`services/slow_lane.py:515-527`):

```python
    reason = ctx.get('not_gradable_reason')
    if reason is not None:
        # METRIK-1 Requirement 2: die Ablehnungs-Zeile haelt fest, WOMIT sie begruendet wurde.
        # Ohne Zahlen ist spaeteres Nachjustieren unmoeglich — und Nachjustieren ist der ganze
        # Plan (Termin: rund 100 echte Anrufe). NULL heisst UNBEKANNT, nie "null Woerter".
        _mess = ctx.get('mess_substanz') or {}
        _payload = {
            'reason': reason,
            'schema': 1,
            'berater_woerter': _mess.get('berater_woerter'),
            'redeabschnitte': _mess.get('redeabschnitte'),
            'sprechzeit_ms': _mess.get('sprechzeit_ms'),
            'high_conf_events': _mess.get('high_conf_events'),
            # Requirement 1 (Cross-AI-Auflage 14.08.): WELCHER Tor-Zweig genommen wurde.
            # Ohne ihn ist in der Datenbank nicht unterscheidbar, ob eine Ablehnung aus zu
            # wenig bekannten Woertern kam oder daraus, dass gar keine Berater-Zeile da war —
            # und genau diese Unterscheidung traegt die Nachjustierung.
            'tor_zweig': _mess.get('tor_zweig'),
        }
        _upsert_rubric_score(
            db,
            call_id=call.id,
            conversation_log_id=call.conversation_log_id,
            session_mode=mode_key,
            tenant_id=tenant_id,
            status=_STATUS_NOT_GRADABLE,
            payload=_payload,
        )
        db.commit()
        return
```
⚠ Die Messwerte haengen **neben** `'reason'` im selben Dict — der Anzeige-Zweig liest
`(rubric_preview.payload_jsonb or {}).get('reason')` (`templates/session_detail.html:140`)
und bleibt dadurch unveraendert funktionsfaehig.
⚠ Das gilt auch fuer `'poor_audio_health'`: **jede** Ablehnung traegt die Messwerte, nicht nur
die wegen Sprech-Substanz. Mehr Daten, kein Zusatzaufwand.

**C) Schilder nachziehen (Punkt 23, Aktualitaets-Pflicht).**

In `database/models.py`, direkt **ueber** `class RubricScore` (also vor `:901`), zwei neue
Modul-Konstanten in der Form der ZEITSTEMPEL-1-Bloecke (`:966-971`). **Diese Texte sind
zeichengleich in die Migration zu uebernehmen.**

⚠ **Doppelpunkt-Falle:** In den Schild-Texten steht **kein** Doppelpunkt ohne vorangehendes
Wortzeichen und **kein** Prozentzeichen. Datei-Referenzen werden als `datei.py` geschrieben,
**ohne** nackte Zeilenzahl hinter dem Doppelpunkt. Zusaetzlich schuetzt `_comment()` mit
`exec_driver_sql` — beide Ebenen, wie in `0039` gelernt.

```python
# ── Phase 08.23.2.METRIK-1 — Schild-Texte, zeichengleich zu alembic 0040 (Punkt 23) ──
_SCHILD_RUBRIC_STATUS = 'Bewertungs-Status. Werte judged, scored, pending, judge_failed, transcript_not_resolved, not_gradable. NULL bedeutet noch nicht gelaufen. Bei not_gradable steht der Grund in payload_jsonb unter dem Schluessel reason. Ab METRIK-1 gibt es zwei lebende Gruende - poor_audio_health (Audio-Tor, unveraendert) und too_little_speech (Sprech-Substanz-Tor, weniger als zwanzig gesprochene Berater-Woerter; die Zahl der Redeabschnitte ist reiner Messwert und KEINE Bedingung). Welcher Weg genommen wurde, steht daneben im Schluessel tor_zweig. Der Alt-Grund too_few_high_confidence_events wird seit METRIK-1 NICHT mehr geschrieben, steht aber weiter auf Alt-Zeilen in der Datenbank - der Anzeige-Zweig dafuer bleibt deshalb erhalten. Schreibt services/slow_lane.py; liest routes/dashboard.py und templates/session_detail.html.'
_SCHILD_RUBRIC_PAYLOAD = 'Reserve, Training-only-Felder (was_correct, scenario_id, ground_truth_score) und ab METRIK-1 zwei feste Bereiche. Bei not_gradable die Begruendung samt Messwerten - reason, schema, berater_woerter, redeabschnitte, sprechzeit_ms, high_conf_events, tor_zweig. redeabschnitte ist dort ein reiner Diagnose-Wert und keine Bedingung. tor_zweig nennt den genommenen Weg - genug_woerter, zu_wenig_woerter, keine_berater_zeile oder wortzahl_unbekannt_durchgelassen; der letzte laesst bewusst DURCH und erscheint deshalb nur an judged-Zeilen. Eine Zahl NULL heisst dort UNBEKANNT, nie null Woerter; sie ist die Grundlage der Tor-Nachjustierung nach rund hundert echten Anrufen. Bei judged der Zaehler der Zitat-Pruefung unter dem Schluessel beleg_check mit geprueft, treffer, near_miss, verworfen und compliance_beleg_verworfen. Dieser Zaehler ist ein ABSOLUTWERT des Laufs - der Upsert ersetzt payload_jsonb vollstaendig (ON CONFLICT DO UPDATE), ein Wiederholungslauf desselben Anrufs zaehlt daher per Bauart nicht doppelt. Schreibt services/slow_lane.py; liest routes/dashboard.py, templates/session_detail.html und services/beleg_check_counter.py (Prozess-Zaehler der Founder-Sicht).'
```
Dann in `class RubricScore` die beiden `comment=`-Werte auf die Konstanten umstellen:
`status = Column(String(24), nullable=True, comment=_SCHILD_RUBRIC_STATUS)` und
`payload_jsonb = Column(JSON_TYPE, nullable=False, default=dict, server_default='{}', comment=_SCHILD_RUBRIC_PAYLOAD)`.

**D) Migration `alembic/versions/0040_metrik1_schilder.py`.**

`down_revision = '0039'` (**Prod-Kopf am 2026-08-13 belegt gezogen: `0039`** — der Executor
zieht `bash scripts/inspect.sh migrations` **erneut** und bricht ab, wenn dort etwas anderes
steht). `revision = '0040'`.

Modul-Docstring mit den drei Pflicht-Bestandteilen aus `0039`:
- **„WARUM ALEMBIC UND NICHT `_migrate()`"**: `app._migrate()` returnt auf Postgres frueh
  (`app.py`) — wirkungslos **und** Alt-Muster (Punkt 29).
- **Prod-Kopf beim Planen belegt** (`0039`, gezogen 2026-08-13) und vom Executor erneut zu
  ziehen.
- **Ausroll-Reihenfolge ausdruecklich**: Diese Migration legt **keine Spalte** an und aendert
  **keinen Typ** — sie setzt nur `COMMENT ON`. Sie ist damit in beide Richtungen unkritisch;
  festgelegt ist **Migration vor Deploy**, damit der Schild-Waechter im Test-Tor gegen den
  gesetzten Kommentar laeuft. ⚠ Die ZEITSTEMPEL-1-Regel „Migration vor Deploy" gilt nur fuer
  **nullable Spalten** und wird hier nicht blind uebertragen.

`_comment(objekt, text)`-Helfer **verbatim** aus `0039:52-73` uebernehmen (inklusive des vollen
Docstrings zur Doppelpunkt-Falle — der Warnhinweis gehoert an die **Definition**, nicht an eine
Aufrufstelle).

Fuer `downgrade()` die **heutigen** Texte als Konstanten mitfuehren, verbatim aus
`database/models.py`:
```python
_SCHILD_STATUS_ALT = 'Bewertungs-Status: scored|pending|not_gradable (D-09 poor_audio_health). NULL = noch nicht gelaufen.'
_SCHILD_PAYLOAD_ALT = 'Reserve + Training-only-Felder (was_correct, scenario_id, ground_truth_score) ohne spaetere Migration. SPEC Req 1.'
```

```python
def upgrade():
    _comment('COLUMN rubric_score.status', _SCHILD_RUBRIC_STATUS)
    _comment('COLUMN rubric_score.payload_jsonb', _SCHILD_RUBRIC_PAYLOAD)


def downgrade():
    _comment('COLUMN rubric_score.status', _SCHILD_STATUS_ALT)
    _comment('COLUMN rubric_score.payload_jsonb', _SCHILD_PAYLOAD_ALT)
```

**E) Zwei Tests in `tests/test_substanz_tor.py` ergaenzen** (Funktionen 13 und 14):
13. `test_ablehnungs_payload_traegt_alle_messwerte` — baut ein `ctx`-Dict wie `_call_end_merge`
    es baut, ruft `_judge_step` mit einer Fake-Session (Muster: monkeypatch auf
    `_upsert_rubric_score`, um den `payload`-Parameter abzufangen — **Runtime-Assertion auf den
    uebergebenen Wert**, kein Source-Presence-Test) und prueft, dass `payload` die Schluessel
    `reason`, `berater_woerter`, `redeabschnitte`, `sprechzeit_ms`, `high_conf_events` **und
    `tor_zweig`** traegt — `tor_zweig` mit dem Wert `'zu_wenig_woerter'`.
14. `test_unbekannte_messwerte_bleiben_none_nicht_null` — dasselbe mit einem `mess_substanz`,
    dessen `berater_woerter` und `sprechzeit_ms` `None` sind; im `payload` stehen sie als
    `None` (→ `null` im jsonb), **nicht** als `0`.
  </action>

  <verify>
    <automated>grep -c "'mess_substanz'" services/slow_lane.py</automated>
    <automated>grep -c "_SCHILD_RUBRIC_STATUS\|_SCHILD_RUBRIC_PAYLOAD" database/models.py alembic/versions/0040_metrik1_schilder.py</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "'berater_woerter': _mess.get('berater_woerter')" services/slow_lane.py` liefert **1**.
- **Abwesenheits-Anker auf Code-FORM (der alte Ein-Schluessel-Payload ist weg):**
  `grep -c "payload={'reason': reason}" services/slow_lane.py` liefert **0**;
  gepaarter Existenz-Anker: `grep -c "status=_STATUS_NOT_GRADABLE" services/slow_lane.py`
  liefert **1** (der Ablehnungs-Pfad steht unveraendert an seinem Platz).
- `grep -c "'mess_substanz': mess_substanz" services/slow_lane.py` liefert **1** — die
  Messwerte werden durchgereicht, nicht neu gezaehlt. Gepaarter Anker:
  **Anker auf die AUFRUF-Form, nicht auf den Funktionsnamen (B3):**
  `grep -c "mess_substanz = _mess_sprech_substanz(segments)" services/slow_lane.py` liefert **1**
  — **genau eine** Zaehl-Stelle im ganzen Modul. Gepaarter Existenz-Anker:
  `grep -c "_mess_sprech_substanz(" services/slow_lane.py` liefert **2** (die `def`-Zeile aus
  Task 1 **plus** dieser eine Aufruf).
  ⚠ **Ein Soll von 1 auf dem Klammer-Anker waere per Bauart unerfuellbar**, weil `grep` die
  Definitionszeile mitzaehlt. Bestands-Beleg derselben Bauform, gemessen am 2026-08-14:
  `grep -c "_count_high_confidence(" services/slow_lane.py` = **2** (`:398` `def`, `:625` Aufruf).
- **Schild-Zeichengleichheit ORM/DDL (Punkt 23):** die beiden Konstanten-Werte in
  `database/models.py` und `alembic/versions/0040_metrik1_schilder.py` sind identisch.
  Pruefbar: `grep -c "Bewertungs-Status. Werte judged" database/models.py` liefert **1** und
  `grep -c "Bewertungs-Status. Werte judged" alembic/versions/0040_metrik1_schilder.py`
  liefert **1**.
- **Doppelpunkt-Falle entschaerft, auf beiden Ebenen:**
  `grep -c "exec_driver_sql" alembic/versions/0040_metrik1_schilder.py` liefert **1**;
  `grep -c "op.execute(" alembic/versions/0040_metrik1_schilder.py` liefert **0**.
- `grep -c "down_revision = '0039'" alembic/versions/0040_metrik1_schilder.py` liefert **1**,
  und der Executor hat `inspect.sh migrations` **erneut** gezogen; die Ausgabe steht im SUMMARY.
- `grep -c "def downgrade" alembic/versions/0040_metrik1_schilder.py` liefert **1**, und
  `downgrade()` setzt **beide** Alt-Texte zurueck (`grep -c "_SCHILD_STATUS_ALT\|_SCHILD_PAYLOAD_ALT"`
  liefert **>= 4**: je zwei Definitionen und zwei Verwendungen).
- `grep -c "^def test_" tests/test_substanz_tor.py` liefert **14**.
- `grep -c "'tor_zweig': _mess.get('tor_zweig')" services/slow_lane.py` liefert **1**;
  gepaarter Existenz-Anker: `grep -c "'schema': 1" services/slow_lane.py` liefert **1**
  (der Payload-Block als Ganzes steht).
- **Kein Source-Presence-False-Green in den neuen Tests:**
  `grep -c "inspect.getsource\|open('services" tests/test_substanz_tor.py` liefert **0**.
  </acceptance_criteria>

  <done>
Jede Ablehnungs-Zeile traegt Woerter, Abschnitte, Sprechzeit und die Zahl der hoch-konfidenten
Momente als Zahlen (bzw. `null` bei unbekannt). Die Schilder von `status` und `payload_jsonb`
beschreiben den Zustand nach dieser Phase — im selben Ausrollen, ORM und DDL zeichengleich.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: DAS EINE AUSROLLEN — Migration 0040 + deploy.sh production (Plan 01 + Plan 02)</name>

  <read_first>
- `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-01-SUMMARY.md`
- `CLAUDE.md` → HART-Block „Kein Local-Dev — Default ist Production-Server", Abschnitt
  „Workflow pro Plan-Task".
  </read_first>

  <what-built>
**Aus Plan 01 (noch nicht ausgerollt):** gemeinsamer Transkript-Renderer, EWB-Filter im
Bewerter-Auftrag, angeschlossener Zitat-Pruefer mit Drei-Wege-Behandlung und Zaehler,
Founder-Kachel „Verworfene Beleg-Zitate".

**Aus Plan 02:** Substanz-Tor auf **zwanzig Berater-Woerter** (die Abschnitts-Bedingung ist
ersatzlos gestrichen, Andre-Entscheidung 14.08.), ein **eigener** fail-open-Zweig fuer
unbekannte Wortzahl, Messwerte und Tor-Zweig in jeder Ablehnungs-Zeile, Migration `0040` mit den
nachgezogenen Schildern.

**D-01 erfuellt:** Requirement 3 und Requirement 1 gehen mit **diesem einen** Ausrollen
gemeinsam live. **D-02 kann nicht verletzt werden**, weil das Tor vorher nirgends live war.
  </what-built>

  <how-to-verify>
**Schritt 1 — Alles ist committet und gepusht.**
```
git status            # sauber
git log --oneline -8  # die Commits aus Plan 01 UND Plan 02 sind da
git push origin main
```

**Schritt 2 — Prod-Kopf gegenpruefen (VOR der Migration).**
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
Erwartet: `0039`. Steht dort etwas anderes → **STOPP**, `down_revision` in
`alembic/versions/0040_metrik1_schilder.py` klaeren, nicht raten.

**Schritt 3 — Migration `0040` auf Production ausfuehren.**
Als `postgres`-Benutzer mit gesetztem `DATABASE_URL` (sonst faellt Alembic auf SQLite zurueck —
Lehre aus MESSGERAETE-1). Danach erneut:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
Erwartet: `0040`.

**Schritt 4 — Schilder gegenpruefen.**
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schilder rubric_score'
```
Erwartet: die neuen Texte fuer `status` und `payload_jsonb` stehen dort **woertlich** so, wie
sie in `database/models.py` stehen.

**Schritt 5 — DAS Ausrollen. ⚠ Dieser Befehl ist fuer Agenten gesperrt und wird von Andre
ausgefuehrt:**
```
bash deploy.sh production
```
Das Test-Tor laeuft **serverseitig vor dem Neustart**. **Rotes Tor = kein Neustart, der alte
Stand bleibt live** — das ist kein Fehlschlag, sondern der Zweck des Tors.

**Schritt 6 — Live-Gegenprobe mit dem Test-Konto.**
1. Einen kurzen Test-Anruf fahren, der **klar ueber** der Grenze liegt (mehr als 20 Woerter),
   **ohne** einen einzigen EWB-Knopfdruck. ⚠ **Atempausen sind ausdruecklich egal** — die
   Abschnitts-Bedingung existiert nicht mehr; ein Anruf am Stueck ist der Regelfall, den das
   neue Tor durchlassen muss.
2. Die Auswertungsseite des Anrufs oeffnen. Erwartet: die KI-Einschaetzung erscheint
   (Status `judged`), **nicht** „Zu wenig auswertbare Momente".
3. Die zugehoerige `rubric_score`-Zeile **als `postgres` oder mit gesetzter Tenant-GUC**
   ansehen — ⚠ `rubric_score` steht unter FORCE-RLS und liefert ueber `inspect.sh` **still 0
   Zeilen**. Erwartet: `status = 'judged'` und `payload_jsonb` traegt den Schluessel
   `beleg_check` mit `geprueft >= 1`.
4. Founder-Sicht oeffnen: die Kachel „Verworfene Beleg-Zitate" zeigt eine Zahl (auch 0 ist ein
   Ergebnis) und den Detailtext „N von M geprueft".
  </how-to-verify>

  <acceptance_criteria>
- `inspect.sh migrations` liefert **0040**.
- `inspect.sh schilder rubric_score` zeigt die neuen Texte fuer `status` **und**
  `payload_jsonb`.
- `bash deploy.sh production` ist **gruen** durchgelaufen (Test-Tor bestanden, Neustart erfolgt).
  Die Tor-Ausgabe (`N passed / 0 failed / 0 error`) steht **verbatim** im SUMMARY.
- Die `rubric_score`-Zeile des Test-Anrufs traegt `status = 'judged'` und
  `payload_jsonb->'beleg_check'->>'geprueft'` **>= 1** — gemessen **als `postgres` oder mit
  gesetzter Tenant-GUC**, mit gepaartem Existenz-Anker **derselben Schutz-Klasse** (eine zweite
  Abfrage auf `rubric_score`, die bekannt Zeilen liefert). Ein Anker auf `calls` (ohne
  Zeilenschutz) beweist nur, dass das Werkzeug laeuft — er zaehlt hier **nicht**.
- Im SUMMARY steht der Satz: **„D-01 erfuellt — Zitat-Pruefer (Plan 01) und Tor-Umbau (Plan 02)
  sind in EINEM Ausrollen live gegangen. D-02 (Tor live, Pruefer spaeter) war zu keinem
  Zeitpunkt moeglich."**
  </acceptance_criteria>

  <resume-signal>
Andre antwortet „ausgerollt" (plus die Tor-Ausgabe) oder beschreibt, was schiefging.
  </resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Bau-Regel 21 + Punkt 26)

### Angefasste Tabellen

- `transcript_segments` (**lesen**) — **keine** RLS
- `rubric_score` (**schreiben**: `status`, `payload_jsonb`; **Schild-Aenderung**) — FORCE RLS
- `calls` (**lesen**: `audio_health_score`, `conversation_log_id`, `tenant_id`) — keine RLS

### inspect.sh-Beleg (verbatim, gezogen 2026-08-13 gegen Production)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
                                           Table "public.transcript_segments"
       Column        |           Type           | Collation | Nullable |                     Default
---------------------+--------------------------+-----------+----------+-------------------------------------------------
 id                  | bigint                   |           | not null | nextval('transcript_segments_id_seq'::regclass)
 conversation_log_id | integer                  |           | not null |
 ts_ms               | integer                  |           | not null |
 speaker             | text                     |           | not null |
 text                | text                     |           | not null |
 created_at          | timestamp with time zone |           |          | now()
 start_ms            | integer                  |           |          |
 end_ms              | integer                  |           |          |
 word_count          | integer                  |           |          |
Indexes:
    "transcript_segments_pkey" PRIMARY KEY, btree (id)
    "idx_transcript_segments_conv_ts" btree (conversation_log_id, ts_ms)
Check constraints:
    "transcript_segments_speaker_check" CHECK (speaker = ANY (ARRAY['berater'::text, 'kunde'::text, 'system'::text]))
Foreign-key constraints:
    "transcript_segments_conversation_log_id_fkey" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id) ON DELETE CASCADE
```

Der `rubric_score`-Auszug (FORCE-RLS-Policy, partieller Unique-Index, `payload_jsonb jsonb NOT
NULL DEFAULT '{}'`) steht **verbatim in Plan 01, Sektion 5** und wird hier nicht ein zweites Mal
abgedruckt — es ist derselbe Auszug aus demselben Zug.

```
$ ssh ... 'bash scripts/inspect.sh migrations'
 version_num
-------------
 0039
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (live/batch/bg-thread) + kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|
| `word_count` | gelesen in `_mess_sprech_substanz` | DB-Spalte `transcript_segments.word_count` (integer, **nullable**) | **Batch am Call-Ende** (Schild `_SCHILD_TABELLE`: alle `created_at` einer Gruppe identisch). **Vorauslaufen ausgeschlossen** durch das bestehende Punkt-26-Tor `transcript_resolved == True` (`slow_lane.py:618-619`). **Was der Leser sonst laese:** eine leere Liste → keine Berater-Zeile → **jeder** Anruf faelschlich `too_little_speech` / `keine_berater_zeile`. Genau deshalb ist das Tor Pflicht und wird nicht umgangen. | ✓ inspect.sh + `slow_lane.py:613-619` |
| `speaker` | gelesen in `_mess_sprech_substanz` | DB-Spalte `transcript_segments.speaker` (text, CHECK `berater|kunde|system`) | wie oben | ✓ inspect.sh zeigt den CheckConstraint — die DB-Form ist ein **String**, nicht die RAM-Ganzzahl aus `deepgram_service.py` |
| `start_ms` / `end_ms` | gelesen in `_mess_sprech_substanz` | DB-Spalten, beide **nullable** | wie oben. Alt-Anrufe vor ZEITSTEMPEL-1 tragen `NULL` → `sprechzeit_ms is None`. Das ist **kein** Ablehnungsgrund (die Sprechzeit ist reiner Messwert, keine Tor-Bedingung). | ✓ inspect.sh |
| `audio_health_score` | gelesen in `_call_end_merge` | DB-Spalte `calls.audio_health_score` | **bg-thread** `_audio_health_bg`. **Vorauslaufen ausgeschlossen** durch das bestehende Fan-In-Flag `calls.audio_health_resolved` (`slow_lane.py:610-611`, Migration 0027). **Unveraendert** — dieser Plan fasst das Audio-Tor nicht an. | ✓ Bestandsmechanik |
| `mess_substanz` | RAM-Dict, `_call_end_merge` → `ctx` → `_judge_step` | keine | live im selben Faden | ✓ per Bauart |
| `payload_jsonb` (Ablehnung) | geschrieben in `_upsert_rubric_score` | DB-Spalte `rubric_score.payload_jsonb` (jsonb NOT NULL DEFAULT `{}`) | Batch, unter der M-4-GUC-Klammer (`slow_lane.py:640-646`). Leser `routes/dashboard.py:957-960` im Request-Pfad; findet er keine Zeile, greift der bestehende Zweig „(d) row-absent". | ✓ inspect.sh |
| `status` | geschrieben in `_upsert_rubric_score` | DB-Spalte `rubric_score.status` (varchar 24, nullable) | wie oben. **Keine Typ-Aenderung, kein neuer Wertebereich in der DDL** — `too_little_speech` steht in `payload_jsonb.reason`, nicht in `status`. | ✓ inspect.sh |

**Es wird KEINE Spalte angelegt, geloescht oder im Typ geaendert.** Migration `0040` setzt
ausschliesslich `COMMENT ON COLUMN`.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

## Verbindungs-Karte (Punkt 22) — neue Namen, je grep-belegt

⚠⚠ **NUR ZUM NACHSEHEN, KEIN ZIELWERT.** Die greps in diesem Abschnitt sind
**Entdeckungs**-greps: ihre Ausgabe geht ins SUMMARY, sie haben **keinen Sollwert** und sind
**kein Abnahme-Gate**. Die echten Abnahme-Anker dieses Plans stehen ausschliesslich in den
`<acceptance_criteria>`-Bloecken.
⛔ **Warum das ausdruecklich dasteht:** Im Cross-AI-Durchgang vom 14.08. hat ein Gegenleser einen
solchen Such-Befehl fuer einen Anker mit Sollwert 0 gehalten und einen „unerfuellbaren Anker"
gemeldet. **Wenn ein Gegenleser einen Such-Befehl fuer ein Abnahme-Gate haelt, kann ein Ausfuehrer
das auch** — und ein Ausfuehrer, der einen Entdeckungs-grep auf 0 druecken will, loescht Code.
✅ **Gegenrichtung geprueft:** kein Kriterium **mit** Sollwert traegt diese Markierung.


| Neuer Identifier | Kandidat schon vergeben? | Beleg (vom Executor erneut zu ziehen) |
|---|---|---|
| `MIN_BERATER_WOERTER` | nein | `grep -rn "MIN_BERATER_WOERTER" .` → **0** |
| `_TOR_ZWEIG_GENUG` / `_TOR_ZWEIG_WORTZAHL_UNBEKANNT` / `_TOR_ZWEIG_ZU_WENIG` / `_TOR_ZWEIG_KEINE_BERATER_ZEILE` | nein | `grep -rn "_TOR_ZWEIG_" .` → **0** |
| Zweig-Werte `genug_woerter` / `wortzahl_unbekannt_durchgelassen` / `zu_wenig_woerter` / `keine_berater_zeile` | frei | `grep -rn "wortzahl_unbekannt_durchgelassen" .` → **0** |
| ⛔ **`MIN_REDEABSCHNITTE` wird NICHT angelegt** | — | Andre-Entscheidung 14.08.: die Abschnitts-Bedingung ist ersatzlos gestrichen. Wer den Namen im Repo findet, hat eine Halb-Migration gebaut (Punkt 29) |
| `_segments_for_call` | nein | `grep -rn "_segments_for_call" services/ routes/` → **0**; das **Vorbild** `_events_for_call` lebt (`slow_lane.py:389`) |
| `_mess_sprech_substanz` | nein | `grep -rn "_mess_sprech_substanz" .` → **0** |
| `_tor_sprech_substanz` | nein | `grep -rn "_tor_sprech_substanz" .` → **0** |
| Ablehnungs-Grund `too_little_speech` | frei | `grep -rn "too_little_speech" .` → **0**; die bestehenden Gruende sind `poor_audio_health` und `too_few_high_confidence_events` (`slow_lane.py:628`, `:630`) |
| `payload_jsonb`-Schluessel `berater_woerter` / `redeabschnitte` / `sprechzeit_ms` / `high_conf_events` / `tor_zweig` | frei | heute enthaelt der Ablehnungs-Payload **nur** `reason` (`slow_lane.py:524`) |
| `MIN_HIGH_CONFIDENCE_EVENTS` (**Loeschung**) | 2 Verwendungen | `grep -rn "MIN_HIGH_CONFIDENCE_EVENTS" config.py services/ routes/` → **`config.py:309`** (Definition) + **`services/slow_lane.py:585`** (Import) + **`:629`** (Verwendung). **Kein weiterer Leser** → Loeschung zulaessig; der Executor zieht den grep **erneut** und bricht ab, wenn ein vierter Treffer auftaucht |

## Vier-Saeulen-Check (Vault-Regel D)

1. **Automatisieren:** Der Code entscheidet aus gemessenen Wortzahlen, ob ein Anruf genug
   Substanz fuer eine Rueckmeldung hat — statt aus einer Ereigniszahl, die im Kaltakquise-Modus
   strukturell nahe null liegt.
2. **Nachvollziehen:** Jede Ablehnung landet mit ihren Messwerten in `rubric_score.payload_jsonb`,
   nicht nur auf dem Bildschirm. Genau diese Zahlen tragen die Nachjustierung nach ~100 echten
   Anrufen.
3. **Eingreifen koennen:** Die **eine** Schwelle ist ein ENV-Wert (`MIN_BERATER_WOERTER`) —
   Aenderung ohne Code-Deploy, deutlich unter 30 Sekunden. Eine Bedingung weniger heisst auch:
   ein Schalter weniger, an dem man sich vertun kann.
4. **Marge:** Ohne den Umbau bekaemen 83 % aller Anrufe gar keine Rueckmeldung — das Produkt
   liefe, ohne zu liefern.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `transcript_segments` (anonymisiert) → Tor-Entscheidung | Fremd geschriebene Daten aus einem anderen Pfad, gebuendelt am Call-Ende |
| ENV-Schwellen → Tor-Verhalten | Betriebs-Eingabe, die das Verhalten in Produktion aendert |
| Slow-Lane-Daemon → `rubric_score` (FORCE RLS) | Schreiber ohne Request-Kontext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-METRIK1-02-01 | Tampering | EWB-Knopfdruck als Substanz-Beweis | **mitigate** | Ableitung (c2): die Unbekannt-Erkennung schaut **nur** auf Berater-Zeilen — sonst kippte die Knopf-Zeile (`speaker='kunde'`, `word_count NULL`) jeden Anruf in den fail-open-Zweig. **Seit der Streichung der Abschnitts-Bedingung ist das die einzige EWB-Kante des Tors**, festgenagelt von Verhaltens-Test 7. |
| T-METRIK1-02-06 | Tampering | „unbekannt" als Sammel-Ausgang, durch den alles durchrutscht | **mitigate** | Der fail-open-Zweig ist ein **eigener** Zweig mit eigenem String und wird nur genommen, wenn **mindestens eine Berater-Zeile** existiert und ihre Wortzahl unbekannt ist. Der Fall „gar keine Berater-Zeile" hat einen **anderen** Zweig (`keine_berater_zeile`) und lehnt ab — Verhaltens-Test 6 grenzt beide gegeneinander ab. |
| T-METRIK1-02-02 | Denial of Service (fachlich) | zu strenges Tor | **mitigate** | Fehlerrichtung ausdruecklich gewaehlt; „im Zweifel durchlassen" bei unbekannten Wortzahlen; duennes Material faengt der bestehende Zweig „Nicht genug zum Bewerten." ab. |
| T-METRIK1-02-03 | Elevation of Privilege | ENV-Override der Schwellen | **accept** | Wer `MIN_BERATER_WOERTER` setzen kann, hat ohnehin Server-Zugriff. Gleiche Risiko-Klasse wie der bestehende `AUDIO_HEALTH_GATE_THRESHOLD`. |
| T-METRIK1-02-04 | Information Disclosure | Messwerte im `payload_jsonb` | **accept** | Gespeichert werden **Zahlen** (Woerter, Abschnitte, Millisekunden) — kein Transkript-Text, keine personenbezogenen Daten. Die Zeile steht ohnehin unter FORCE RLS. |
| T-METRIK1-02-05 | Tampering | Migration stirbt an der Doppelpunkt-Falle | **mitigate** | `_comment()` mit `exec_driver_sql` **und** Schild-Texte ohne nackte `:NN`-Angaben — beide Ebenen, wie in `0039` gelernt. |
</threat_model>

<verification>
- Alle `<acceptance_criteria>` der drei Tasks sind erfuellt.
- Die frisch gezogenen greps der Verbindungs-Karte stehen mit Zahlen im SUMMARY — insbesondere
  der `MIN_HIGH_CONFIDENCE_EVENTS`-grep **vor** der Loeschung.
- ROT-Laeufe **vor** GRUEN sind verbatim im SUMMARY (Vault-Regel Nr. 1).
- Das Deploy-Tor ist gruen; Migration steht auf `0040`.
</verification>

<success_criteria>
- Das alte Tor `n_high_conf < MIN_HIGH_CONFIDENCE_EVENTS` existiert im Code nicht mehr, die
  Zaehlung `_count_high_confidence` schon.
- **Das Tor hat genau EINE Bedingung** (Wortzahl); eine Abschnitts-Bedingung existiert weder
  in `config.py` noch in `services/slow_lane.py`.
- **Ein Anruf mit 100 Woertern in EINEM Abschnitt kommt durch** — als Verhaltens-Test belegt.
- **„Unbekannte Wortzahl" ist ein eigener Programm-Zweig mit eigenem String und laesst durch**
  — im Code belegt (Rueckgabewert), nicht in der Prosa.
- Alle Kanten aus SPEC Req 1 sind als **Verhaltens**-Tests belegt, nicht als
  Quelltext-Pruefung.
- Jede Ablehnungs-Zeile traegt Messwerte **und** den genommenen Tor-Zweig.
- `rubric_score.status` und `.payload_jsonb` tragen aktuelle Schilder, ORM und DDL zeichengleich.
- **D-01 ist erfuellt und im SUMMARY woertlich vermerkt.**
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-02-SUMMARY.md` anlegen.
**Pflicht-Inhalte:**
- ROT-Lauf von `tests/test_substanz_tor.py` **verbatim**, vor dem Fix gezogen.
- Deploy-Tor-Ausgabe **verbatim** (`N passed / 0 failed / 0 error`).
- `inspect.sh migrations` vor und nach der Migration.
- Der D-01-Satz woertlich (siehe Task 3).
- Die Messung des Test-Anrufs (`status`, `payload_jsonb`), gezogen **als `postgres` oder mit
  gesetzter Tenant-GUC**, mit gepaartem Existenz-Anker derselben Schutz-Klasse.
- **Bekannte Luecke, ausdruecklich ins SUMMARY (SPEC NACHTRAG 2 ④):** `[nicht gespeichert]`-Zeilen
  tragen echte Wortzahlen bei Platzhalter-Text — ein stark geschwaerzter Anruf passiert das Tor
  mit 50 „Woertern", aus denen kein Zitat ziehbar ist. Aufgefangen wird das hinten vom Zweig
  „Nicht genug zum Bewerten."
- **Die Aufloesung des NULL-Widerspruchs, ausdruecklich:** SPEC `:517` ist **im Code** erfuellt
  — ein Anruf mit ausschliesslich `word_count IS NULL` wird ueber den eigenen Zweig
  `wortzahl_unbekannt_durchgelassen` **durchgelassen**, nicht abgelehnt. Dazu der Satz, dass
  dieser Punkt von **zwei unabhaengigen Gegenlesern** (Gemini B-3 = GSD W-5) gefunden wurde und
  vorher nur in Prosa aufgeloest war.
- **Die Streichung der Abschnitts-Bedingung, mit Begruendung** (Cross-AI 14.08.: 100-Woerter-
  Einstieg am Stueck ist EIN Segment; ODER→UND ausdruecklich verworfen, weil es 7 Woerter in 3
  Abschnitten durchliesse) — **und der ausdrueckliche Vermerk, dass die EWB-Kippe damit
  ersatzlos entfaellt, waehrend der EWB-FILTER aus Plan 01 unberuehrt bleibt.**
- Pruning-Notiz: `MIN_HIGH_CONFIDENCE_EVENTS` **geloescht** (Konstante + Import), Begruendung
  Bau-Regel 20 (null Leser nach dem Umbau). **`MIN_REDEABSCHNITTE` wurde nie angelegt** — es
  gibt nichts zu pruningen, aber der Verzicht gehoert benannt, damit ihn niemand fuer eine
  Auslassung haelt.
</output>
