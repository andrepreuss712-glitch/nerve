---
phase: 08.23.2.METRIK-1
plan: 03
subsystem: auswertungs-anzeige
tags: [anzeige-sperre, d-20, d-21, ablehnungs-zweig, too_little_speech, rot-vor-gruen]
requires:
  - templates/session_detail.html (KI-Karte, if/elif-Kette)
  - routes/dashboard.py (session_detail, Preview-Bau + Sicherheitsnetz)
  - .planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-02-SUMMARY.md (payload_jsonb.reason = too_little_speech)
provides:
  - Auswertungsseite ohne Anzeige-Sperre — die Einschaetzung erscheint, sobald sie fertig ist
  - Ehrliche Markierung "Ergebnis noch nicht bestaetigt" als Haken fuer die spaetere Korrelations-Rechnung
  - Dritter Ablehnungs-Zweig too_little_speech; der Alt-Zweig too_few_high_confidence_events lebt weiter
  - Regressions-Test gegen das versehentliche Aufraeumen des Alt-Zweigs
affects:
  - Plan 05+ (die Karte ist ab jetzt fuer JEDEN Anruf sichtbar — auch fuer unbestaetigte)
  - Die spaetere Uebernahme-/Korrelations-Messung (post-Launch): sie schliesst unbestaetigte Anrufe ueber genau diese Markierung aus
tech-stack:
  added: []
  patterns:
    - "MARKIEREN statt SPERREN — eine Regel wirkt dort, wo der Moment richtig ist (Anruf-Fenster), nicht ein zweites Mal auf der Auswertungsseite"
    - "Lebender Grund vor Alt-Grund vor Sonst — Alt-Zweige bleiben, solange Alt-Zeilen in der Datenbank stehen"
    - "Gepaarte Anker: Abwesenheit + Existenz + diskriminierende Gegenprobe im Test"
key-files:
  created: []
  modified:
    - templates/session_detail.html
    - routes/dashboard.py
    - tests/test_session_detail_render.py
decisions:
  - "Zweig (0) ERSATZLOS geloescht, nicht als Foundation stehengelassen — er hat belegbar nie einen Klick erzeugt"
  - "outcome_confirmed bleibt berechnet und uebergeben — als Markierungs-Flag, nicht als Gate"
  - "Der Fallback-Wert False bleibt: lieber einmal zu viel markiert als eine Bewertung faelschlich als 'zaehlt' ausgewiesen"
  - "Der Alt-Zweig too_few_high_confidence_events bleibt und ist per Regressions-Test festgenagelt"
  - "Der Anker auf inspect.getsource war eine Fehlmessung — Sollwert falsch, Bestands-Docstring NICHT umgeschrieben"
metrics:
  tasks: 3
  commits: 6
  completed: 2026-08-15
---

# Phase 08.23.2.METRIK-1 Plan 03: Anzeige-Sperre faellt, dritter Ablehnungs-Zweig — Summary

Die Rueckmeldung erscheint jetzt, sobald sie fertig ist — nicht erst, wenn jemand das
Gespraechsergebnis bestaetigt; und der neue Ablehnungs-Grund sagt endlich, was wirklich los war,
statt „Audio zu schlecht" zu behaupten.

## ❌ WAS NOCH NICHT BELEGT IST — die menschliche Sicht-Pruefung

**Ausgerollt und maschinell abgesichert ist belegt. Dass ein Mensch die neue Anzeige gesehen und
fuer richtig befunden hat, ist NICHT belegt.** Andre macht die drei Browser-Pruefungen, wenn er
das naechste Mal am Rechner ist; sie blockieren die Phase nicht (dieselbe Behandlung wie der
offene Wirkungs-Beleg aus Plan 02).

- ✅ Code live (Deploy-Tor gruen, Exit-Code 0, Neustart erfolgt). Keine Migration noetig — dieser
  Plan aendert ausschliesslich Anzeige-Logik.
- ❌ **Offen (UAT, Andre):**
  1. Test-Anruf fahren, das Anruf-Fenster **ohne** Ergebnis-Bestaetigung schliessen, Auswertungs-
     seite oeffnen → die KI-Einschaetzung ist **sichtbar**, darueber steht „Gesprächsergebnis noch
     nicht bestätigt — diese Einschätzung zählt nicht in die Auswertung."
  2. Zweiter Anruf **mit** bestaetigtem Ergebnis → Einschaetzung sichtbar, Markierung **fehlt**.
  3. Optional, falls ein Anruf mit `too_little_speech` vorliegt → „In diesem Gespräch wurde zu
     wenig gesprochen für eine Einschätzung."

Solange Punkt 1 und 2 offen sind, gilt: **die Kette ist bewiesen, der Anblick nicht.** Die
Laufzeit-Tests pruefen genau diese beiden Faelle gegen echtes gerendertes HTML — was sie **nicht**
pruefen koennen, ist ob es fuer einen Menschen gut aussieht und verstaendlich ist.

## Was gebaut wurde

| Task | Gate | Ergebnis | Commit |
|---|---|---|---|
| 1 | RED | Entkopplungs-Test + diskriminierende Gegenprobe; `_seed_session` bekommt `outcome` | `b29c57c` |
| 1 | GREEN | Zweig (0) geloescht, ehrliche Markierung, Kopfkommentar + Route-Kommentare richtiggestellt | `a72da8b` |
| 2 | RED | drei Zweig-Tests; `_seed_session` bekommt `status` + `payload` | `61a7029` |
| 2 | GREEN | dritter Zweig `too_little_speech`, Alt-Zweig bleibt | `a580461` |
| — | Belege | ROT-Laeufe + ausgefuehrte Anker verbatim | `9b4e322` |
| 3 | — | Ausrollen + Abschluss (diese SUMMARY + STATE) | siehe Fuss |

Die vollstaendigen Laeufe stehen **verbatim** in `08.23.2.METRIK-1-03-ROT-BELEGE.md` (waehrend der
Ausfuehrung gefuehrt, damit sie einen Kontext-Schnitt ueberleben). Hier die Kernstuecke.

## Der Kern: die Sperre war nie eine Regel, sie war ein Vorhang

`conv 264` und `conv 266` blieben **trotz** Sperre unbestaetigt (SPEC-Beleg). Die Sperre hat damit
**nie einen Klick erzeugt** — sie hat ausschliesslich fertige Bewertungen unsichtbar gemacht. Was
bleibt, ist die Regel dort, wo sie wirkt: die **Pflicht-Wahl im Anruf-Fenster** direkt nach dem
Auflegen. Die ist **unangetastet** — `static/pip-launcher.js` wurde in diesem Plan **nicht**
geaendert, belegt durch `git diff --name-only`:

```
$ git diff --name-only 51bcef6..a580461
.planning/DIALOG-GSD-CLAUDIAN.md
.planning/phases/08.23.2.METRIK-1/08.23.2.METRIK-1-04-ROT-BELEGE.md
routes/dashboard.py
services/fokus_katalog.py
templates/session_detail.html
tests/test_session_detail_render.py
```

(`services/fokus_katalog.py` + `tests/test_fokus_katalog.py` gehoeren zu **Plan 04** und sind
disjunkt.)

**D-21 — der Etiketten-Irrtum ist raus.** Die frueher am Zweig klebende Behauptung, es handle sich
um einen Schutz vor durchsickernden Ergebnissen, war falsch: der Bewerter laeuft **ergebnis-blind**
(`services/judge_runner.py:7`, `services/rubric_engine.py:30-31`) und ist **fertig, bevor** die
Sperre ueberhaupt greifen konnte (`services/slow_lane.py`). Der Ausdruck existiert im Code nicht
mehr (Anker unten), und **keiner** der drei neu geschriebenen Kommentare zitiert ihn — beschreiben
statt zitieren, sonst haette er seine eigene Loeschung ueberlebt.

## ROT-vor-GRUEN — die Pflicht-Belege

Alle ueber `scripts/triage.sh` **auf dem Server**, nie lokal (HART-Regel). Fuer den ersten Lauf war
**nur die Testdatei** transportiert — Template und Route lagen dort noch im alten Zustand, der Lauf
war also rot **statt blind** (Rezept-Bedingung 1).

### 1. Task 1 — die Entkopplung

```
=================================== FAILURES ===================================
___________ test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis ____________
tests/test_session_detail_render.py:216: in test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis
    assert MARKIERUNG_UNBESTAETIGT in html, (
E   AssertionError: Die ehrliche Markierung fehlt — ohne sie ist ein unbestaetigter Anruf von einem bestaetigten nicht zu unterscheiden.
E   assert 'Gesprächsergebnis noch nicht bestätigt' in '<!DOCTYPE html>\n<html lang="de">\n<head>\n<meta charset="UTF-8">…'
```

```
FAILED tests/test_session_detail_render.py::test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis
=================== 1 failed, 1 passed, 12 warnings in 8.45s ===================
[triage] FEHLER: pytest gegen nerve_test ROT (kein Restart, kein Deploy)
```

Echter Test-Fehler mit Assertion-Text, **kein Werkzeug-Ausfall** (Rezept-Bedingung 4) und **kein
Collection-Error** — der Lauf erreicht den Pruefkatalog. Das ist die ZEITSTEMPEL-1-Lehre, hier
eingehalten.

**Warum „1 passed" hier kein Mangel ist:** die Gegenprobe
`test_bestaetigter_anruf_traegt_die_markierung_nicht` ist gegen den alten Stand richtigerweise
gruen (dort gab es die Markierung ueberhaupt nicht). Sie ist der **diskriminierende Partner**: sie
wird rot, sobald jemand die Markierung fest verdrahtet. Ohne sie waere „Markierung sichtbar" kein
Beleg, sondern Dekoration.

### 2. Task 2 — der dritte Zweig

```
=================================== FAILURES ===================================
_________ test_ablehnungsgrund_zu_wenig_gesprochen_zeigt_eigenen_text __________
tests/test_session_detail_render.py:338: in test_ablehnungsgrund_zu_wenig_gesprochen_zeigt_eigenen_text
    assert SATZ_ZU_WENIG_GESPROCHEN in html, (
E   AssertionError: Der neue Ablehnungs-Grund hat keinen eigenen Text — der Anruf bekommt eine fremde Erklaerung.
1 failed, 2 passed, 12 warnings in 8.58s
```

**„2 passed" ist hier der Zweck, nicht ein Mangel:** `test_alter_ablehnungsgrund_bleibt_erhalten`
und `test_unbekannter_ablehnungsgrund_faellt_auf_audio_zurueck` sind **Regressions**-Anker auf
Bestand. Sie muessen vorher UND nachher gruen sein — waeren sie vorher rot, wuerden sie etwas
anderes messen als „der Alt-Zweig hat ueberlebt".

### GRUEN

```
11 passed, 11 warnings in 10.09s
[triage] pytest gegen nerve_test bestanden — nerve_test wird per trap gedroppt
```

## Das Ausrollen — Deploy-Tor verbatim

```
[deploy] pytest gegen DATABASE_URL=postgresql://nerve_app@/nerve_test (+ 4 Test-DSNs)
1220 passed, 7 skipped, 5 deselected, 107 warnings in 81.30s (0:01:21)
[deploy] pytest gegen nerve_test bestanden
[deploy] POST-SUITE Baseline-Check OK: alle crm.* Tabellen leer + training.transcript_archive leer (0 Leak-Rows)
[deploy] Tests bestanden
[deploy] Schild-Guard lief im Postgres-Gate gegen nerve_test mit (test_schild_guard.py, NERVE_SCHILD_TEST_DSN=@/nerve_test)
[deploy] Service status: (Restart erfolgt)
[deploy] Done.
```

Exit-Code 0.

**Beweis-Kette zur Zahl — vorhergesagt, dann eingetroffen:** Basis nach Welle 2 = **1200**. +5 aus
diesem Plan (2 aus Task 1, 3 aus Task 2) = 1205. +15 aus Plan 04 (`tests/test_fokus_katalog.py`,
schon auf dem Server) = **1220**. Der Executor hat diese Zahl **vor** dem Deploy ueber einen
`triage.sh`-Voll-Lauf mit **denselben Markern** (`-m "not live and not perf"`, `deploy.sh:221`)
gemessen — `1220 passed, 7 skipped, 5 deselected` — und sie ist exakt so eingetroffen. Die Basis
ist nicht gewandert; keine fremde Pruefung ist nebenbei umgefallen.

⚠ **Werkzeug-Divergenz beachtet** (Lehre aus Plan 02): ohne die Marker laufen die real-Haiku-Tests
mit, sterben an `invalid x-api-key`, und **ihr** Teardown erzeugt `[BASELINE-AUTO-FIX]`-Warnungen.
Das sieht aus wie ein Cleanup-Defekt und ist keiner. Wer `triage.sh` als Deploy-Vorhersage nutzt,
muss dieselben Marker mitgeben — dann stimmt die Zahl auf den Test genau.

## Die vier Pflicht-Suchen aus SPEC Requirement 4 (verbatim)

⚠ Entdeckungs-Suchen ohne Sollwert (so im Plan ausdruecklich markiert) — **kein Abnahme-Gate**.

```
$ grep -rn "outcome_confirmed" templates/ routes/ services/ static/ --include=*.html --include=*.py --include=*.js
templates/session_detail.html:133:    {% if not outcome_confirmed %}
routes/dashboard.py:936:        # TAXO2-Plan 05) + outcome_confirmed (METRIK-1 D-20/D-21: MARKIERUNG, keine Sperre mehr —
routes/dashboard.py:944:        outcome_confirmed = False
routes/dashboard.py:957:                outcome_confirmed = (_call_row.outcome is not None)
routes/dashboard.py:1021:                outcome_confirmed=(outcome_confirmed if _preview_on else False),

$ grep -c "outcome_confirmed" routes/dashboard.py
4

$ grep -rn "outcome is not None\|\.outcome and\|if .*\.outcome" templates/session_detail.html
(kein Treffer)

$ grep -c "KI-Einschätzung" templates/session_detail.html
4
```

**Befund: genau EIN Template-Konsument, und der ist jetzt die Markierung, kein Gate.** Er
umschliesst nur noch einen Absatz; die if/elif-Kette darunter ist davon unabhaengig und beginnt mit
`{% if rubric_preview … 'judge_failed' %}`. **Kein weiterer Konsument** ist aufgetaucht (Erwartung
aus dem Plan: ein Template-Treffer + drei Stellen in `routes/dashboard.py` — es sind vier, weil der
richtiggestellte Block-Kommentar den Namen ebenfalls nennt; das ist kein neuer Konsument, sondern
Prosa). Die **zweite** moegliche Aufhaengung — `calls.outcome` direkt im Template — existiert
nicht. Der gepaarte Existenz-Anker (`KI-Einschätzung` = 4) belegt, dass die Karte ueberhaupt
gerendert wird; „kein Treffer" heisst hier also nicht „falsche Datei gelesen".

## Korrelations-Feststellung (Plan-Auflage)

D-20 verlangt, unbestaetigte Anrufe „aus jeder Korrelations-Rechnung herauszunehmen".

```
$ grep -rn "group_by\|func.avg\|func.count" services/ routes/ --include=*.py | grep -ic "outcome"
0
$ grep -rn "group_by\|func.avg\|func.count" services/ routes/ --include=*.py | wc -l
25
```

**Es existiert heute keine Korrelations-Rechnung; die Markierung ist der Haken, an dem die spaetere
Rechnung sie ausschliesst.** Die Uebernahme-/Korrelations-Messung ist laut SPEC Boundaries
ausdruecklich **out of scope, post-Launch**. Gepaarter Existenz-Anker daneben: es gibt **25**
Aggregationen im Code — „0 Treffer" heisst also „keine davon fasst `outcome` an", nicht „es wurde
nichts gesucht".

Dieselbe Richtung, unabhaengig geprueft: die Stellen, die `calls.outcome` heute ueberhaupt
beruehren, sind **Setzer** (`routes/learning.py:132/302`, `routes/app_routes.py:2240`) und
**Anzeigen** (`routes/performance.py:389`, `routes/app_routes.py:2198/2299`) — keine rechnet etwas
gegen die Bewertung.

## Ausgefuehrte Abnahme-Anker

### Task 1

| Anker | Soll | Ist |
|---|---|---|
| `grep -c "Gesprächsergebnis bestätigen, dann erscheint die Einschätzung" templates/session_detail.html` | 0 | **0** |
| `grep -c "rubric_preview.status == 'judge_failed'" templates/session_detail.html` | 1 | **1** |
| `grep -c "{% elif rubric_preview and rubric_preview.status == 'judge_failed' %}" …` | 0 | **0** |
| `grep -c "{% if rubric_preview and rubric_preview.status == 'judge_failed' %}" …` | 1 | **1** |
| `grep -rn "Leakage-Schutz" templates/ routes/ services/ static/ \| wc -l` | 0 | **0** |
| `grep -c "outcome_confirmed" routes/dashboard.py` | >= 3 | **4** |
| `grep -c "n-label\">Gesprächsergebnis noch nicht bestätigt" templates/session_detail.html` | 1 | **1** |
| `grep -c "(0) ANZEIGE-Sperre" templates/session_detail.html` | 0 | **0** |
| `grep -c "def test_einschaetzung_erscheint_ohne_bestaetigtes_ergebnis" tests/…` | 1 | **1** |
| `grep -n "style=" templates/session_detail.html` | kein neuer Treffer in der KI-Karte | **erfuellt** (Treffer nur `:101/105/109/113` Hero-Minibars und `:314/338/495` spaetere Sektionen) |

Der neue Test benutzt `_login(client, u, tenant_id)` **mit** `tenant_id` (FORCE-RLS-Falle: ohne GUC
liefert `rubric_score` still 0 Zeilen und der Test waere aus dem falschen Grund rot) und die
bestehende `tracker`-Fixture mit **zwei** `cleanup_rows`-Aufrufen (FK-Reihenfolge).

### Task 2

| Anker | Soll | Ist |
|---|---|---|
| `grep -c "_reason == 'too_little_speech'" templates/session_detail.html` | 1 | **1** |
| `grep -c "_reason == 'too_few_high_confidence_events'" templates/session_detail.html` | 1 | **1** |
| `grep -c "Zu wenig auswertbare Momente" templates/session_detail.html` | 1 | **1** |
| `grep -c "Audio zu schlecht" templates/session_detail.html` | 1 | **1** |
| `grep -c "def test_ablehnungsgrund_… \| … \| …" tests/test_session_detail_render.py` | 3 | **3** |
| `grep -c "inspect.getsource\|open('templates" tests/test_session_detail_render.py` | 0 | **1** ⚠ Fehlmessung, siehe unten |
| `grep -c "assert " tests/test_session_detail_render.py` | >= 20 | **35** |

## Deviations from Plan

### EIN Abnahme-Anker war eine FEHLMESSUNG (Klasse „Abwesenheits-Anker auf Prosa statt Code-Form")

`grep -c "inspect.getsource\|open('templates" tests/test_session_detail_render.py` liefert **1**
statt 0. Der Treffer ist der **Modul-Docstring aus einem frueheren Plan** (`:20`), der die
CLAUDE.md-Test-Qualitaets-Regel referiert und dort woertlich festhaelt, dass die Datei bewusst
**keine** Source-Presence-Tests enthaelt.

**Die Absicht des Kriteriums ist erfuellt, der Anker greift daneben.** Beleg mit dem Anker auf die
**Code-Form** (Aufrufstelle statt blossem Namen):

```
$ grep -c "inspect.getsource(\|open('templates" tests/test_session_detail_render.py
0
```

Der einzige Weg zum Sollwert 0 des Original-Ankers waere gewesen, **fremden Bestands-Docstring
umzuschreiben, nur damit ein grep gruen wird** — also der Fehler „Anker formt das Artefakt". Der
Text bleibt unveraendert, der Sollwert ist als falsch dokumentiert. **Das ist die siebte Instanz
dieser Klasse in dieser Phase** — das Muster ist inzwischen so stabil, dass es beim naechsten
Plan-Schreiben eine eigene Pruefung verdient: *zeigt der Anker auf eine Code-Form oder auf Prosa?*

### Kein Auto-Fix noetig

Keine Bugs, keine fehlende kritische Funktionalitaet, keine Blocker (Rules 1-3 kamen nicht zum
Einsatz), keine architektonische Frage (Rule 4). Der Plan lief ansonsten exakt wie geschrieben.

### Bewusste Zusatz-Entscheidungen (dokumentiert statt still gebogen)

1. **Ein zusaetzlicher Test ueber die Plan-Vorgabe hinaus:**
   `test_bestaetigter_anruf_traegt_die_markierung_nicht`. Der Plan verlangt drei Assertions am
   unbestaetigten Fall. Ohne den bestaetigten Gegenfall waere eine fest verdrahtete Markierung
   gruen durchgelaufen — die Markierung waere dann keine Aussage mehr, sondern Dekoration.
2. **`_seed_session` wurde parametrisiert** (`outcome`, `status`, `payload`) statt kopiert. Eine
   zweite Seed-Funktion waere die naechste Stelle gewesen, an der die FORCE-RLS-Falle (Org des
   eingeloggten Nutzers) neu haette stimmen muessen.
3. **Der geflippte Seed-Kommentar steht am Vorgabewert des Parameters**, nicht mehr an einer festen
   Zeile — der Plan formuliert ihn fuer den fest verdrahteten Wert, den es nicht mehr gibt.
4. **Der Fallback-Wert `False` bei `_preview_on=False` wurde begruendet im Code festgehalten**
   (Plan-Auflage): im Fallback-Render wird die Karte ohnehin ohne Vorschau-Daten gezeichnet, und
   `False` erzeugt dort dieselbe Markierung wie ein unbestaetigter Anruf — die konservative
   Richtung.

## Pruning-Notiz (Vault-Regel Nr. 7)

- **Zweig (0) GELOESCHT**, nicht als Foundation stehengelassen. Begruendung: er hat belegbar nie
  einen Klick erzeugt (`conv 264`/`conv 266` blieben **trotz** Sperre unbestaetigt). Eine Mechanik,
  die ihren Zweck nachweislich nie erfuellt hat, wird nicht „fuer spaeter" behalten — kein Eintrag
  ins Foundation-Code-Register.
- **Der WERT `outcome_confirmed` BLEIBT** (4 Stellen in `routes/dashboard.py`) — er traegt jetzt
  die Markierung. Existenz-Anker oben.
- **Der Alt-Zweig `too_few_high_confidence_events` BLEIBT bewusst** — das ist **kein** vergessener
  Code: Alt-Zeilen mit diesem Grund stehen in der Datenbank, und wer ihn aufraeumt, gibt genau
  diesen Anrufen die falsche Erklaerung. Festgenagelt durch
  `test_alter_ablehnungsgrund_bleibt_erhalten`. Loesch-Trigger: erst wenn keine `rubric_score`-Zeile
  mit diesem `reason` mehr existiert — und Call-Logs werden nie geloescht, also praktisch nie.
- **Der Kopfkommentar der KI-Karte wurde nachgezogen**, nicht ergaenzt: die Aufzaehlung der
  Edge-Reihenfolge waere durch den Wegfall von (0) **falsch** geworden. Ein Kommentar, der nicht
  mehr stimmt, ist schlimmer als keiner.

## Grenzen dieses Plans (Punkt-31-Haltung)

Dieser Plan baut **keinen Waechter** — die neuen Tests behaupten nichts ueber eine Fehler**klasse**
im Repo, sie nageln vier konkrete Anzeige-Faelle fest. Trotzdem die ehrlichen Kanten:

- Die Tests pruefen **gerendertes HTML gegen echte DB-Zeilen** (keine Source-Presence). Was sie
  **nicht** sehen: wie es aussieht. Die Markierung nutzt `.n-label` — ob ein Nutzer sie als
  Einordnung und nicht als Fehlermeldung liest, kann nur die offene UAT beantworten.
- Der Anzeige-Zweig liest **ausschliesslich** `payload_jsonb['reason']`. Ein Ablehnungsgrund, der
  kuenftig geschrieben wird **ohne** dass hier ein Zweig entsteht, faellt still auf „Audio zu
  schlecht" — also auf eine **falsche** Aussage. Das ist der Preis des `{% else %}`-Auffangs und
  war schon vorher so; neu ist, dass es jetzt drei statt zwei Zweige sind.
- Der Fall „`rubric_preview` fehlt **und** Ergebnis unbestaetigt" zeigt jetzt die Markierung **plus**
  „Einschätzung wird im Hintergrund ausgewertet …". Das ist gewollt (beide Aussagen sind wahr), war
  aber unter der Sperre nie sichtbar — ein Anblick, den erst die UAT bestaetigen kann.

## Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.

## Known Stubs

Keine. Es wurde nichts mit einem festen Ersatzwert verdrahtet; alle angezeigten Texte haengen an
echten Werten aus `calls.outcome` bzw. `rubric_score.payload_jsonb`.

## Self-Check

**TEILWEISE BESTANDEN — bewusst kein pauschales PASSED.**

- ✅ **Dateien:** alle drei geaenderten Dateien existieren und tragen die Aenderungen (Anker oben
  einzeln ausgefuehrt).
- ✅ **Commits:** alle fuenf Hashes sind im Repo auffindbar (einzeln geprueft), gepusht.
- ✅ **Ausrollen:** Deploy-Tor gruen mit Exit-Code 0, Neustart erfolgt, **1220 passed / 0 failed**
  — die Zahl war **vorhergesagt** und ist eingetroffen.
- ✅ **ROT vor GRUEN:** beide Pflicht-Belege gezogen, verbatim oben, echte Assertion-Fehler statt
  Werkzeug-Ausfall oder Collection-Error.
- ⚠ **Ein Abnahme-Anker war eine Fehlmessung** — als falscher Sollwert dokumentiert, Code
  unveraendert.
- ❌ **NICHT gesehen — die menschliche Sicht-Pruefung im Browser.** Siehe UAT-Block ganz oben. Der
  Plan gilt als **ausgerollt und maschinell belegt**, aber **von keinem Menschen abgenommen**.

## Naechster Schritt

**Welle 3 ist komplett** (Plan 03 + Plan 04). Als naechstes **Welle 4 — Plan 05**: dort bekommt
`waehle_fokus` aus Plan 04 seinen einzigen Aufrufer, das Ergebnis wandert nach
`observations_jsonb['_fokus']`, und `services/fokus_katalog.py` geht damit wirklich live.

Offene UAT-Posten, die **mitlaufen** und nichts blockieren:
1. Plan 02 — Wirkungs-Beleg des Substanz-Tors am echten Anruf.
2. Plan 03 — die drei Browser-Pruefungen oben.
3. Plan 04 — die drei englischen Test-Anrufe.
