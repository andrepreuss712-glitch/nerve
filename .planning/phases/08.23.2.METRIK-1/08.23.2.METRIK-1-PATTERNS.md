# Phase 08.23.2.METRIK-1: Die Rueckmeldung nach dem Anruf abloesen — Pattern Map

**Mapped:** 2026-08-13
**Quelle der Dateiliste:** SPEC.md (9 Requirements, Geltungsreihenfolge NACHTRAG 2 → NACHTRAG 1 →
Grundtext; NACHTRAG 3 in Requirement 9) + CONTEXT.md (D-01..D-24). **Kein RESEARCH.md.**
**Dateien klassifiziert:** 19 (14 geaendert, 4 neu, 1 unveraendert-aber-zu-pruefen)
**Analoge gefunden:** 17 / 19

> ⚠ **Alles unten ist am echten Code gelesen und mit `datei:zeile` belegt.** Wo ich eine
> SPEC-Zeilenangabe nachgemessen und **abweichend** vorgefunden habe, steht das ausdruecklich
> dabei — bitte diese Zahlen nehmen, nicht die aus der SPEC.

---

## 0. Zeilen-Abweichungen gegenueber SPEC/CONTEXT (nachgemessen 2026-08-13)

| SPEC/CONTEXT sagt | Nachgemessen | Bemerkung |
|---|---|---|
| `slow_lane.py:626-630` Tor | **`:627-630`** (`if` audio `:627`, `elif n_high_conf` `:629-630`) | Tor-Block insgesamt `:623-630` |
| `slow_lane.py:524` Ablehnungs-Payload | **`:524`** ✔ (im Block `:515-527`) | passt |
| `slow_lane.py:472-485` UPSERT | **`:472-485`** ✔ (Funktion `:437-485`) | passt |
| `judge_runner.py:247-251` Transkript-Rendering | **`:246-253`** (Schleife `:247-251`, else-Zweig `:252-253`) | der `else`-Zweig gehoert in den gemeinsamen Renderer |
| `judge_runner.py:370-375` Segment-Query | **`:369-375`** ✔ | passt |
| `judge_runner.py:390` `tool_choice` | **`:390`** ✔ | passt |
| `session_detail.html:68-78` Score-Hero | **`:68-83`** (Hero-Section `:71-117`, Trend-Badge `:75-82`) | CONTEXT D-15 (`:68-83`) ist richtig, SPEC-Grundtext (`:68-78`) zu kurz |
| `session_detail.html:84-100` Vierer-Aufriss | **`:84-116`** (`div.n-session-detail-breakdown` `:84`, Live-Zweig `:89-115`) | 4 Rows liegen `:99-114` |
| `session_detail.html:131-133` Anzeige-Sperre | **`:131-133`** ✔ | passt |
| `session_detail.html:151-177` Beobachtungs-Anzeige | **`:150-185`** (Zweig `(e)` ab `:150`, Schleife `:170-184`) | passt sinngemaess |
| `session_detail.html:382` „Zuletzt im Training" | **`:382-385`** ✔ | passt |
| `app_routes.py:1112-1135` | **`:1110-1138`** (`_cross_context_objections_live`, `func.avg(CL.kb_end)` `:1124`) | passt sinngemaess |
| `performance.py:101-102` / `:259-268` | **`:100-102`** / **`:258-268`** ✔ | passt |
| `pip-launcher.js:4287-4302` | **`:4287-4305`** (Trend-Aufruf `:4304` gehoert dazu) | passt |
| `deepgram_service.py:1193-1206` EWB-Schreibstelle | **`:1193-1206`** ✔ (Marker-Doku `:1187-1191`, Alt-Begruendung `:1169-1174`) | CONTEXT D-03/D-06 sind korrekt |

---

## 1. File Classification

| Datei | Neu/Aendern | Rolle | Datenfluss | Naechster Analog | Match |
|---|---|---|---|---|---|
| `services/slow_lane.py` (Tor `:623-630`, `_count_high_confidence` `:398`) | aendern | service (Consumer-Schritt) | batch / event-driven (post-call) | **sich selbst**: `_count_high_confidence` `:398-412` als Zaehl-Helfer-Form; `_events_for_call` `:389-395` als Lade-Helfer-Form | exakt |
| `services/slow_lane.py` (Ablehnungs-Payload `:515-527`) | aendern | service | CRUD (UPSERT) | `_judge_step` `:517-525` selbst | exakt |
| `services/slow_lane.py` (UPSERT-Zaehler D-23 `:437-485`) | aendern | service | CRUD (UPSERT, idempotent) | `_upsert_rubric_score` `:450-485` selbst | exakt |
| `services/judge_runner.py` (EWB-Filter + Renderer-Aufruf `:246-253`, `:369-375`) | aendern | service (LLM-Adapter) | request-response (1 LLM-Call) | sich selbst | exakt |
| **NEU** `services/transkript_renderer.py` (D-05, gemeinsamer Renderer) | neu | utility (reine Funktion) | transform | `services/handling_markers.py` (reine Funktion, kein DB/LLM) + `judge_runner._assemble_markers` `:128-162` | exakt |
| **NEU** `services/fokus_katalog.py` (4 Zaehlregeln, Rangfolge, 4 Schablonen) | neu | utility (Regel-Modul) | transform | **`services/handling_markers.py`** — reines Regel-Modul, Wortlisten als Modul-Konstanten, Wortgrenzen-Regex, reine Funktion ohne Seiteneffekt | exakt |
| **NEU** Beleg-Pruef-Anschluss (Drei-Wege + Zaehler) | neu | service-Schicht in `slow_lane._judge_step` | transform + CRUD | `services/beleg_check.py:23-99` (Bibliothek fertig) + `_parse_judge_output` `:277-326` (Form-Erzwingung) | exakt |
| **NEU** Anwendungs-Pruefung (Req 8, letzte Welle) | neu | utility (Code-Schicht) | batch | `services/handling_markers.py::grade_handling` `:97-145` | rollen-nah |
| `services/beleg_check.py` | **unveraendert** — nur Aufrufer bauen | utility | transform | — | n/a |
| `config.py:305-309` (`MIN_HIGH_CONFIDENCE_EVENTS`) | aendern/ergaenzen | config | — | `AUDIO_HEALTH_GATE_THRESHOLD` `:301-304` (ENV-Override + Prosa-Kommentar) | exakt |
| `templates/session_detail.html` (`:68-117` Hero+Aufriss, `:119-188` KI-Karte, `:375-390`) | aendern | component (Jinja) | request-response | die bestehende `if/elif`-Kette `:131-186` selbst | exakt |
| `routes/dashboard.py:738-820` (`session_detail`) + `:934-999` (Preview-Bau) | aendern | route/controller | request-response | `:942-999` selbst (Form-Garantie-Muster) | exakt |
| `routes/app_routes.py:1007-1017` (`_calc_call_score`) | loeschen | utility | transform | Foundation-Register-Muster `slow_lane.py:551-574` | rollen-nah |
| `routes/app_routes.py:1347-1379` (`/api/postcall/trend`) | loeschen (D-14) | route | request-response | — | n/a |
| `routes/app_routes.py:1110-1138` (`_cross_context_objections_live`) | aendern (kb_end-Mittel raus) | utility | CRUD (read) | `_cross_context_objections_training` `:1141+` (Schwester ohne Score) | exakt |
| `routes/performance.py:100-102`+`:158`, `:258-268`+`:468-477` | aendern | route | request-response | beide Stellen sind Schwestern | exakt |
| `templates/dashboard.html:121-123`, `:516`, `:526` | aendern | component (Jinja+Inline-JS) | request-response | Nachbar-Kachel `kpi-sessions` `:125-128`/`:517` | exakt |
| `static/pip-launcher.js:3266-3277` (`_calcScore`), `:3489-3513` (Trend), `:4276-4305` (Anzeige) | aendern | component (Client) | request-response | `_renderQuickStats` `:3470-3487` (Zaehl-Kacheln, bleiben!) + `nlp-btn-details` (existiert schon) | exakt |
| `database/models.py:901-938` (`RubricScore.payload_jsonb`-Schild) | aendern (Punkt 23) | model | — | `:923` Schild-Text + `:926-929` Schild-Muster | exakt |
| **NEU** `alembic/versions/0040_*.py` (nur falls Spalten/Schilder) | neu | migration | — | **`alembic/versions/0039_zeitstempel1_sprechzeiten.py`** | exakt |
| Tests: Zaehlregeln, Tor-Verhalten, Template-Zweige, Waechter | neu | test | — | `tests/test_session_detail_render.py`, `tests/test_judge_runner.py`, `tests/test_live_timeout_coverage.py` | exakt |

---

## 2. Pattern Assignments

### 2.1 `services/slow_lane.py` — Substanz-Tor (Requirement 1, D-22)

**Analog:** `services/slow_lane.py` selbst — `_count_high_confidence` und `_events_for_call`.

**Helfer-Form, die die neue Wort-/Abschnitts-Zaehlung kopieren soll** (`slow_lane.py:398-412`):

```python
def _count_high_confidence(events, db) -> int:
    """D-09 (F-07, FOLD 26.06.): zaehlt die hoch-konfidenten Events SELBST aus der events-Liste
    (confidence >= Tor-1-Schwelle) — NICHT aus einem compute_rubric-Rueckgabefeld ...
    confidence=None zaehlt NICHT als sicher (Gemini-Skepsis, konsistent zu rubric_engine._sample_size_for)."""
    n = 0
    for ev in (events or []):
        conf = getattr(ev, 'confidence', None)
        if conf is None:
            continue
        gate = _confidence_gate_for(ev, db)
        if conf >= gate:
            n += 1
    return n
```

⚠ **Die Vorlage macht genau den Fehler, den D-22 (a) verbietet:** `confidence is None` → `continue`,
also „unbekannt zaehlt als nicht vorhanden". Fuer `word_count IS NULL` gilt das **Gegenteil**
(„unbekannt → im Zweifel durchlassen"). Die Form kopieren, die **NULL-Semantik umdrehen** und im
Docstring ausdruecklich hinschreiben, warum sie hier anders ist.

**Lade-Helfer-Form** (`slow_lane.py:389-395`) — Vorlage fuer `_segments_for_call`:

```python
def _events_for_call(call_id, db) -> list:
    """Laedt alle intent_event-Rows eines Calls ... Read-only; calls/intent_event haben KEINE RLS -> GUC-frei."""
    return (db.query(IntentEvent)
              .filter(IntentEvent.call_id == call_id)
              .order_by(IntentEvent.timestamp.asc())
              .all())
```

⚠ **Achtung, andere Schluessel:** `transcript_segments` haengt an `conversation_log_id`, **nicht** an
`call_id` — siehe `judge_runner.py:371-372`. Und `transcript_segments` hat **keine** RLS
(`models.py:973-992`), `rubric_score` **hat** FORCE-RLS (`models.py:908-909`).

**Die Tor-Stelle, die ersetzt wird** (`slow_lane.py:623-630`):

```python
        # ── Audio-Gate D-09 (VOR dem Scoring): events laden + high-conf SELBST zaehlen ──────
        events = _events_for_call(call_id, read_db)
        n_high_conf = _count_high_confidence(events, read_db)
        not_gradable_reason = None
        if call.audio_health_score is None or call.audio_health_score < AUDIO_HEALTH_GATE_THRESHOLD:
            not_gradable_reason = 'poor_audio_health'
        elif n_high_conf < MIN_HIGH_CONFIDENCE_EVENTS:
            not_gradable_reason = 'too_few_high_confidence_events'
```

Das Audio-Tor (`:627-628`) bleibt **unveraendert** (SPEC Req 1). Nur der `elif` (`:629-630`) wird
ersetzt. Der Import steht `:585-586` — `MIN_HIGH_CONFIDENCE_EVENTS` dort mitziehen.

**Speaker-Filter (D-22 c) — die Werte stehen fest** (`models.py:989`):

```python
CheckConstraint("speaker IN ('berater', 'kunde', 'system')", name='ck_transcript_segments_speaker'),
```

Also `speaker == 'berater'` filtern, **nicht** `speaker == 0`. Die `speaker=1`-Ganzzahl aus
`deepgram_service.py:1203` ist die **RAM-Form**, nicht die DB-Form.

---

### 2.2 `services/slow_lane.py` — Ablehnungs-Zeile mit Messwerten (Requirement 2)

**Analog + zu aendernde Stelle in einem** (`slow_lane.py:515-527`):

```python
    reason = ctx.get('not_gradable_reason')
    if reason is not None:
        _upsert_rubric_score(
            db,
            call_id=call.id,
            conversation_log_id=call.conversation_log_id,
            session_mode=mode_key,
            tenant_id=tenant_id,
            status=_STATUS_NOT_GRADABLE,
            payload={'reason': reason},
        )
        db.commit()
        return
```

Requirement 2 haengt Woerter/Abschnitte/Sprechzeit **neben** `'reason'` in dasselbe Dict. Die Werte
muessen also im `ctx` liegen — `ctx` wird `:650-657` gebaut (`high_conf`, `not_gradable_reason`);
dort die drei Messwerte anhaengen, nicht neu zaehlen.

**Sprechzeit-Quelle:** `end_ms - start_ms` je Segment (Schild `0039:44`: *„end_ms minus start_ms ist
die Sprech-Dauer des Abschnitts — Grundlage fuer Redeanteil, Sprechtempo, Redeblock-Laenge und
Pausenlaenge (**gerechnet in METRIK-1**, nicht hier)"*). Der Schild-Text nennt diese Phase namentlich
— das ist der Vertrag aus ZEITSTEMPEL-1.

---

### 2.3 `services/slow_lane.py` — Zaehler-UPSERT-Semantik (D-23)

**Analog:** `_upsert_rubric_score` (`slow_lane.py:437-485`). Die Kern-Stelle (`:472-485`):

```python
    stmt = _pg_insert(RubricScore.__table__).values(**values)
    update_cols = {k: stmt.excluded[k] for k in (
        'conversation_log_id', 'session_mode', 'coaching_score', 'is_provisional',
        'measured_weight_pct', 'unmeasured_dimensions', 'dimensions', 'status',
        'tenant_id', 'payload_jsonb', 'observations_jsonb', 'ratings_jsonb',
    )}
    if dimensions_version is not None:
        update_cols['score_schema_version'] = stmt.excluded['score_schema_version']
    stmt = stmt.on_conflict_do_update(
        index_elements=['call_id'],
        index_where=_sa_text("origin = 'live'"),
        set_=update_cols,
    )
    db.execute(stmt)
```

★ **Fuer D-23 ist das die entscheidende Zeile:** `payload_jsonb` steht in `update_cols` →
`stmt.excluded['payload_jsonb']` **ersetzt** die alte Zeile vollstaendig, es wird **nichts addiert**.
Damit ist „nicht doppelt zaehlen" beim Wiederholungslauf **per Bauart erfuellt**, solange der Zaehler
als *Absolutwert des Laufs* geschrieben wird (verworfen · Beinahe-Treffer · geprueft gesamt) und
**nicht** als `payload_jsonb || jsonb_build_object(...)`-Inkrement. Der Plan muss diese Entscheidung
ausdruecklich festhalten (CONTEXT D-23 verlangt genau das).

**GUC-Klammer, die jeden `rubric_score`-Write umgibt** (`slow_lane.py:640-646`) — nicht anfassen,
aber wissen, dass sie da ist:

```python
    set_current_tenant(str(tenant_id))
    write_db = get_session()
    try:
        call_w = write_db.query(Call).filter(Call.id == call_id).first()
```

---

### 2.4 `services/judge_runner.py` + **NEU** gemeinsamer Renderer (D-04/D-05)

**Zu ersetzende Stelle** (`judge_runner.py:244-254`):

```python
    # (2) Getaggtes Transkript in Sprech-Reihenfolge (ts_ms ASC)
    user_lines.append('== TRANSKRIPT (chronologisch, ts_ms ASC) ==')
    if segments:
        for i, seg in enumerate(segments, start=1):
            speaker = getattr(seg, 'speaker', 'unbekannt')
            ts_ms = getattr(seg, 'ts_ms', 0)
            text = getattr(seg, 'text', '')
            user_lines.append(f'[#{i} {speaker} {ts_ms}ms] {text}')
    else:
        user_lines.append('(Keine Transkript-Segmente verfuegbar)')
```

**Lade-Stelle, die den Filter bekommt** (`judge_runner.py:369-375`):

```python
        segments = (
            db.query(TranscriptSegment)
            .filter(TranscriptSegment.conversation_log_id == sid)
            .order_by(TranscriptSegment.ts_ms.asc(), TranscriptSegment.id.asc())
            .all()
        )
```

**Modul-Form fuer den neuen Renderer** — Vorlage `judge_runner._assemble_markers` (`:128-134`):
reine Funktion, `lines = []` … `return '\n'.join(lines)`, Docstring mit `Returns:`-Block, kein
DB-Zugriff im Renderer selbst.

**Der EWB-Marker, gegen den gefiltert wird** — kanonisch dokumentiert
(`deepgram_service.py:1187-1191` + Schreibstelle `:1202-1205`):

```python
                            'ts': datetime.now().strftime('%H:%M:%S'), 'type': 'transcript',
                            'speaker': 1,
                            'text': f"{_anon_typ} *ewb button*",
                            'data': {'ewb_button': True},
```

⚠ **`data={'ewb_button': True}` ist RAM-only und ueberlebt den Persist NICHT** (`:1189-1191`).
Der **einzige** in `transcript_segments` verfuegbare Marker ist das Text-Suffix `*ewb button*`.
Der Filter muss also am `text` haengen, nicht an einem Flag.

⚠ **D-04: nur ZWEI Lese-Stellen filtern.** Die Nutzer-Anzeige `routes/learning.py:628-637` bleibt
**ungefiltert**:

```python
        segments = (db.query(TranscriptSegment)
                    .filter(TranscriptSegment.conversation_log_id == conv.id)
                    .order_by(TranscriptSegment.ts_ms, TranscriptSegment.id)
                    .all())
        return jsonify({
            'conversation_log_id': conv.id,
            'segments': [
                {'ts_ms': s.ts_ms, 'speaker': s.speaker, 'text': s.text}
```

★ **STOLPERSTEIN — ein Bestands-Test wird durch D-04 ROT** (`tests/test_judge_runner.py:166-190`):

```python
def test_transcript_tagged_in_order():
    ...
        _make_segment(2, 'kunde',   1500, 'Kein Interesse. *ewb button*'),
    ...
    # ewb-button-Marker bleibt im Text
    assert '*ewb button*' in user_str
```

Dieser Test zementiert **genau das Gegenteil** von D-04. Er muss im **selben** Plan-Task umgedreht
werden (`assert '*ewb button*' not in user_str` + Existenz-Anker, dass die anderen Zeilen noch da
sind) — sonst ist der Deploy-Gate rot und jemand „repariert" den Filter statt den Test.
*(Lehre aus AUTH-2 / EMAIL-VERIFY: Vertragsaendernde Wellen brechen Bestandstests; die Retargetierung
gehoert in denselben Task, nicht in einen Nachlauf.)*

---

### 2.5 **NEU** `services/fokus_katalog.py` (Requirement 6 + 7, D-07..D-11)

**Analog: `services/handling_markers.py`** — bestes Vorbild im Repo fuer ein reines Regel-Modul.

**Modul-Docstring-Form** (`handling_markers.py:1-24`, gekuerzt):

```python
"""TAXO2-Plan 03, Task 1 — Anker-Marker-Regeln fuer die Einwand-Behandlungs-Note (handling_score 1-3).

Quelle: NERVE TAXO-Geruest (verriegelt) §4 ... + 08.23.2.TAXO2-CONTEXT D-07 (grosszuegige Abstention).
...
v1 ist rein regel-/marker-basiert. KEIN LLM-Urteil (Bau-Regel 1, Zirkelschluss-Schutz; ...). Daher KEIN
anthropic/claude-Import in diesem Modul.

Die Poison-Pill-Behandlung ... liegt NICHT hier, sondern im Aufrufer services/slow_lane.py ...
grade_handling bleibt eine reine Funktion: int|None, keine Seiteneffekte, kein DB-Zugriff.
"""
```

**Wortlisten als Modul-Konstanten** (`handling_markers.py:31-60`, gekuerzt):

```python
# ── Marker-Listen (Konstanten, ASCII-Keys; deutsche Marker-Strings mit Umlauten OK) ──
LABEL_MARKERS = (
    "verstehe ich richtig",
    "wenn ich sie richtig verstehe",
    ...
)
COUNTER_OPENERS = (
    "aber",
    "doch",
    ...
)
MIN_WORDS = 4
```

**Normalisierung + Wortgrenzen-Regex** (`handling_markers.py:78-94`) — genau die Form, die D-09
(Wortgrenzen · Gross/Klein) verlangt:

```python
def _normalize(text: str) -> str:
    """Kleinschreibung + kollabierte Whitespaces. Reine Hilfsfunktion (kein Seiteneffekt)."""
    return re.sub(r"\s+", " ", text.strip().lower())


def _starts_with_counter(norm: str) -> bool:
    """True, wenn die Aussage mit einem sofortigen Gegenargument eroeffnet."""
    for opener in COUNTER_OPENERS:
        # Wort-Grenze: "aber" ja, aber nicht "abermals". Eroeffnung des Satzes.
        if re.match(r"^" + re.escape(opener) + r"\b", norm):
            return True
    return False
```

⚠ `_has_label_marker` (`:92-94`) nutzt **`marker in norm`** — Teilstring **ohne** Wortgrenze. Fuer
den Katalog ist das zu wenig: D-09 verlangt Wortgrenzen ausdruecklich (`we` darf nicht in `week`
treffen). Also die `re.escape(...) + r"\b"`-Form aus `_starts_with_counter` nehmen, **nicht** die
`in`-Form aus `_has_label_marker` — und die Abweichung im Docstring begruenden.

**Entscheidungs-Kaskade mit fester Rangfolge** (`handling_markers.py:133-144`) — Vorbild fuer die
Fokus-Rangfolge und den „diesmal nichts"-Ausgang (D-10):

```python
    # ── Klassifikation ───────────────────────────────────────────────────────────
    if has_label and not starts_counter:
        return 3                       # Anker zuerst, kein sofortiges Dagegen -> GUT
    if (has_label or mirrors_trigger) and starts_counter:
        return 2                       # teils Anerkennung, teils Gegenargument -> MITTEL
    if starts_counter and not has_label:
        return 1                       # sofortiges Wegargumentieren -> SCHLECHT
    if mirrors_trigger and not starts_counter:
        return 3                       # greift den Einwand auf, kein Dagegen -> GUT

    # Weder klarer Anker noch klares Dagegen erkennbar -> grosszuegige Abstention.
    return None
```

★ `return None` **ist** der Vorlaeufer des „Nothing flagged this time."-Zweigs — die Funktion hat
schon heute einen ehrlichen Nichts-Ausgang. Genau so bauen, und den Zweig **im Template** ehrlich
anzeigen (nie leer, nie erfunden).

**Umlaut-Regel:** Der Katalog ist **englisch** (D-11) — also entfaellt die Umlaut-Frage fuer die
Schablonen-Texte. Die **Schluessel** bleiben trotzdem ASCII (CLAUDE.md „ASCII-Pflicht in
Code-Identifiern"), wie `handling_markers` es mit `LABEL_MARKERS`/`MIN_WORDS` vormacht.

---

### 2.6 Zitat-Pruefer anschliessen (Requirement 3, D-05)

**Bibliothek, fertig, unveraendert lassen** (`services/beleg_check.py:23-53`, Kopf):

```python
def beleg_im_transkript(
    zitat: str,
    transkript_text: str,
    *,
    schwelle: float = 0.80,
    near_schwelle: float = 0.60,
) -> tuple:
    """...
    Returns:
        Tuple (ok: bool, score: float, befund: str).
        befund: 'treffer' | 'near_miss' | 'no_match'
    """
```

Die drei Befunde bilden die Drei-Wege-Behandlung 1:1 ab: `treffer` → uebernehmen · `near_miss` →
uebernehmen **und** zaehlen · `no_match` → **ganze Beobachtung** faellt weg.

⚠ **Warum der EWB-Filter VOR dem Pruefer stehen muss** (`beleg_check.py:15-20`):

```python
def _normalisiere(text: str) -> str:
    """Lowercase, Whitespace kollabieren, Satzzeichen entfernen, trim."""
    text = text.lower()
    text = re.sub(r'[^\w\s]', ' ', text)   # Satzzeichen -> Leerzeichen
```

`*ewb button*` verliert hier nur die Sternchen → `ewb button` bleibt als Text stehen → ein Zitat aus
der Knopf-Zeile wuerde **bestaetigt**. Das ist der belegte Grund fuer D-04/D-05.

**Wo der Anschluss hinkommt:** `slow_lane._judge_step` zwischen `run_behavior_judge` (`:532`) und
dem UPSERT (`:537-547`). Vorlage fuer die Form-Erzwingung vor dem Speichern:
`judge_runner._parse_judge_output` (`:293-326`) — baut `observations`/`ratings` neu auf, statt am
LLM-Dict herumzuflicken. Genau dieselbe Bauform fuer das Aussieben nehmen.

**Wo der Zaehler ankommt:** `payload_jsonb` (D-23) — siehe 2.3.

---

### 2.7 `templates/session_detail.html` — die if/elif-Kette (Requirement 3+4+5, D-15/D-16/D-20)

**Die Kette, in die sich der neue Ablehnungs-Grund einreiht** (`session_detail.html:131-149`) —
**verbatim**, das ist die Pflicht-Vorlage aus dem Auftrag:

```jinja
    {% if not outcome_confirmed %}
      {# (0) ANZEIGE-Sperre: Beobachtungen erst NACH Outcome-Bestätigung (Leakage-Schutz). #}
      <p>Gesprächsergebnis bestätigen, dann erscheint die Einschätzung.</p>
    {% elif rubric_preview and rubric_preview.status == 'judge_failed' %}
      {# (a) Judge-Fehler (Dead-Letter) #}
      <p>Auswertung fehlgeschlagen — wird automatisch geprüft.</p>
    {% elif rubric_preview and rubric_preview.status == 'not_gradable' %}
      {# (b) Audio-Gate / zu wenig Material #}
      <p>
        {% set _reason = (rubric_preview.payload_jsonb or {}).get('reason') %}
        {% if _reason == 'too_few_high_confidence_events' %}
          Zu wenig auswertbare Momente für eine Einschätzung.
        {% else %}
          Audio zu schlecht für eine Einschätzung.
        {% endif %}
      </p>
    {% elif not rubric_preview %}
      {# (d) row-absent: async-Judge noch nicht durch. #}
      <p>Einschätzung wird im Hintergrund ausgewertet …</p>
    {% else %}
```

**Drei Aenderungen, alle in dieser Kette:**
1. **D-20:** Zweig `(0)` (`:131-133`) faellt **ersatzlos**; die Kette beginnt dann mit
   `{% if rubric_preview and ... 'judge_failed' %}`. Der Kommentar „(Leakage-Schutz)" wird **nicht**
   uebernommen (D-21, Etiketten-Irrtum) — an seine Stelle gehoert die ehrliche Markierung
   „Ergebnis nicht bestaetigt" (D-20: markieren statt sperren).
2. **Neuer Grund:** Der innere `{% if _reason == ... %}`-Block (`:141-145`) bekommt einen dritten
   Zweig fuer den neuen Ablehnungs-Grund. Der Zweig `too_few_high_confidence_events` **bleibt**
   (SPEC Boundaries: Alt-Zeilen stehen in der DB). Das `{% else %}` bleibt „Audio zu schlecht".
3. **D-15:** Die KI-Karte (`:126-187`) rueckt **vor** die Hero-Section — sie ist das Erste auf der
   Seite.

**Umlaut-Regel im Blick behalten:** Sichtbarer Text mit echten Umlauten („Gesprächsergebnis",
„Einschätzung"), Jinja-Ausdruecke ASCII (`rubric_preview.payload_jsonb`, `_reason`) — die Datei
macht das durchgehend richtig, also nicht umstellen.

**Der ehrliche Leer-Zweig, an den sich „Nothing flagged this time." anlehnt** (`:165-168`):

```jinja
      {% set _has_any_obs = observations_display | selectattr('eintraege') | list | length > 0 %}
      {% if not _has_any_obs and not compliance_verletzt %}
        {# (c) Kein Material — Daten-Substanz-Schutz (NICHT "wegen des Neins"). #}
        <p class="n-observation-empty">Nicht genug zum Bewerten.</p>
```

⚠ **Der Schluessel heisst `eintraege`, NICHT `items`** — `dim.items` loest in Jinja auf die
Dict-**Methode** auf und war ein HTTP-500 (dokumentiert `routes/dashboard.py:985-990`). Fuer die neue
Kopfzeile/„eine Sache" **keinen** Schluesselnamen waehlen, der eine Dict-Methode ist (`items`,
`keys`, `values`, `get`, `update`, `copy`, `pop`).

**Typ-Weiche, die D-17 (Gap C) traegt** (`:125` und `:89`):

```jinja
  {% if conv.typ == 'live' %}
```

Die KI-Karte ist hart auf `live` gestellt (`:125`), der Vierer-Aufriss ebenso (`:89-115`). **Der
Score-Hero selbst (`:71-83`) hat KEINE typ-Weiche** — er rendert `score_total` fuer beide Typen.
Genau daran haengt D-17: der Hero braucht **neu** ein `{% if conv.typ == 'training' %}`, sonst
stirbt die Trainings-Note still. Das Muster dafuer steht direkt darunter in derselben Datei (`:89`)
und an ~8 weiteren Stellen (z.B. `:193`, `:197`, `:202`, `:377`).

**Zu entfernender Trend-Badge (D-14, Stelle 2)** (`:75-82`) — steht **innerhalb** der Hero-Section,
faellt mit ihr; `trend_avg` im Route-Kontext (`dashboard.py:804-818`, `:1020`) dann ebenfalls
entfernen, **auch der Training-Zweig** (`:815-818`), sonst bleibt eine tote Berechnung stehen.

**Dritte Note-Stelle in derselben Datei (D-12/NACHTRAG 3)** (`:382-385`):

```jinja
                {% elif cc.avg_score is not none and cc.avg_score >= 70 and cc.sessions >= 3 %}
                  <div class="n-session-detail-recommendation-cross-context n-session-detail-recommendation-cross-context--success">
                    <i data-lucide="history" class="n-session-detail-icon-sm"></i> Zuletzt im Training: Ø {{ cc.avg_score | round(0, 'floor') }}/100 aus {{ cc.sessions }} Sessions
                  </div>
```

Gespeist aus `app_routes.py:1122-1135` (`func.avg(CL.kb_end)`). ⚠ Hier ist `kb_end` ein
**Trainings**-Wert, also die Trainings-Note — nicht die Kaufbereitschaft. Der Zweig `{% else %}`
(`:386-389`) zeigt bereits die notenfreie Variante („Im Training: N Sessions") — **das ist der
fertige Ersatz**, der `elif`-Zweig faellt einfach weg. Kein Neubau noetig.

---

### 2.8 `routes/dashboard.py` — Route-Kontext (D-15/D-16/D-17)

**Zu entfernen / umzubauen:** `:742` (Import `_calc_call_score`), `:785-796` (Score-Shim),
`:804-818` (`trend_avg`), `:1020` + `:1024-1025` (Template-Kontext).

**Die Shim-Stelle, die mit `_calc_call_score` faellt** (`dashboard.py:785-796`):

```python
        if conv_typ == 'live':
            # Score mit dem effektiven kb_end berechnen (mini-Shim um _calc_call_score
            # ohne zweite Helper-Variante): temporaer conv.kb_end setzen, Score holen,
            # urspruenglichen Wert zurueckschreiben — Session-Lifecycle bleibt read-only.
            _orig_kb = conv.kb_end
            try:
                conv.kb_end = kb_end_effective
                score_total = _calc_call_score(conv)
            finally:
                conv.kb_end = _orig_kb
        else:
            score_total = kb_end_effective
```

⚠ **D-17 haengt genau hier:** der `else`-Zweig (`:795-796`) ist die **Trainings**-Note. Wer den
`if`-Zweig loescht und den `else` mitnimmt, toetet sie. `score_total` muss fuer `typ == 'training'`
weiter berechnet und uebergeben werden.

**Form-Garantie-Muster, das die neuen Felder (Kopfzeile, eine Sache) kopieren sollen**
(`dashboard.py:961-992`, Kern):

```python
            if rubric_preview is not None:
                # FORM-GARANTIE (Fehler-500, 2026-08-01): observations_jsonb ist JSONB — die Form
                # ist in der DB NIRGENDS erzwungen. ... Deshalb die Form HIER erzwingen, wo sie
                # entsteht: der Anzeige-Pfad bekommt garantiert [{name, eintraege: list[dict]}].
                _obs = rubric_preview.observations_jsonb
                if not isinstance(_obs, dict):
                    _obs = {}
                _compliance = _obs.get('_compliance')
                if not isinstance(_compliance, dict):
                    _compliance = {}
```

**Doppeltes Sicherheitsnetz um das Rendern** (`dashboard.py:1001-1007`) — die neuen Kontext-Werte
gehoeren in **beide** Zweige von `_render(_preview_on)` (`:1014-1018`), sonst bricht die Seite beim
Fallback anders als im Normalfall.

**Reservierter Schluessel:** `_compliance` (Unterstrich-Praefix) in `observations_jsonb`
(`judge_runner.py:280-282`). Kopfzeile und Fokus brauchen **eigene** Unterstrich-Schluessel derselben
Bauart (z.B. `_kopfzeile`, `_fokus`) — dann bricht die Dimensions-Schleife `:977-992` nicht, denn sie
iteriert nur ueber `DIMENSIONS`, nicht ueber alle Keys.

---

### 2.9 `routes/app_routes.py` — die Formel und ihre Leser (Requirement 9)

**Zu loeschen** (`app_routes.py:1007-1017`):

```python
def _calc_call_score(conv):
    """Server-seitige Spiegelung der client-seitigen _calcScore()-Formel in pip-launcher.js.
    Gewichtung: kb_end 40%, behandeltRate 30%, redeScore 20%, skript 10%."""
    kb = conv.kb_end if conv.kb_end is not None else 30
    ...
    return min(100, max(0, round(kb * 0.4 + behandelt_rate * 100 * 0.3 + rede_score * 0.2 + skript * 0.1)))
```

Der Docstring nennt die zweite Kopie selbst — `pip-launcher.js:3266` (siehe 2.11).

⚠ **Direkt darunter stehen ZWEI weitere Score-Formeln, die die SPEC nicht nennt:**
`_calc_process_score` (`:1032-1049`, dieselbe Gewichtung leicht anders) und `_OUTCOME_MODIFIERS`
(`:1021-1029`) plus der Kombinator um `:1100-1103`. Deren Ergebnis `final_score` ist genau das, was
das PiP anzeigt (`pip-launcher.js:4288`). **Bau-Regel 20 scharf: vor der Loeschung frisch greppen,
wer `_calc_process_score` / `final_score` liest** — ein Anker auf `_calc_call_score` findet sie
**nicht**, exakt dieselbe Falle wie bei den drei `kb_end`-Bildschirmen.

**Zu loeschen (D-14):** `/api/postcall/trend` (`:1347-1379`) samt seinem einzigen Aufrufer
`pip-launcher.js:3494`.

**Zu aendern:** `_cross_context_objections_live` (`:1110-1138`) — `func.avg(CL.kb_end)` (`:1124`)
und `avg_score` (`:1134-1135`) raus. **Vorlage: die Schwester-Funktion**
`_cross_context_objections_training` (`:1141+`), die schon ohne Score auskommt
(`{sessions, n_success, focus_match}`). Also die Live-Variante auf `{sessions, focus_match}`
zurueckbauen — die Template-Zweige `:378-381` und `:386-389` tragen das bereits.

---

### 2.10 `routes/performance.py` + `templates/dashboard.html` (D-12)

**Stelle 1 — Wochen-Dashboard-Kachel** (`performance.py:100-102`):

```python
        # ── Ø Score (aus kb_end der Live-Sessions) ────────────────────────
        scores = [l.kb_end for l in logs_90 if getattr(l, 'kb_end', None) is not None]
        avg_score = round(sum(scores) / len(scores), 1) if scores else None
```

★ **Der Fund unter dem Fund ist hier zu sehen:** `logs_90` filtert sauber auf `typ == 'live'`
(`:56-63`) — dort steht in `kb_end` die **Kaufbereitschaft des Kunden**, beschriftet als „Ø Score".

**ZWEI Ausgaenge, nicht einer** — die SPEC nennt nur einen:
- `:158` `'avg_score': avg_score` (erste Route)
- `:469` `'avg_score': avg_score` innerhalb `'kpis'` (Dashboard-v2-Route)

⚠ **Fuer `:158` habe ich keinen Leser gefunden**: `grep avg_score` ueber `*.html`/`*.js` liefert nur
`dashboard.html:516/526` (liest `kpis.avg_score` → `:469`), `session_detail.html:382/384` (liest
`cc.avg_score` → `app_routes.py`) und `pip-launcher.js:3498/3502` (liest `/api/postcall/trend`).
**`performance.py:158` sieht damit nach einem toten JSON-Schluessel aus** — der Plan muss das mit
einem **frischen grep + Existenz-Anker** belegen (Bau-Regel 20), nicht auf meine Zaehlung vertrauen.

**Stelle 2 — Begruessung** (`performance.py:258-268`):

```python
        # Score-Trend diese Woche vs. letzte Woche
        scores_this = [l.kb_end for l in logs_this_week if l.kb_end]
        scores_last = [l.kb_end for l in logs_last_week if l.kb_end]
        avg_score = round(sum(scores_this) / len(scores_this), 1) if scores_this else None
        avg_score_last = round(sum(scores_last) / len(scores_last), 1) if scores_last else None
        score_trend_pct = None
        if avg_score and avg_score_last and avg_score_last > 0:
            score_trend_pct = round((avg_score - avg_score_last) / avg_score_last * 100)

        if score_trend_pct and score_trend_pct > 0:
            greeting_subtitle = f'Dein Score ist diese Woche um {score_trend_pct}% gestiegen 📈'
```

**Ersatz-Muster steht direkt darunter** (`:269-279`): Streak-Zweige und
„Zeit für eine neue Session" — der `if`-Zweig faellt einfach weg, die Kaskade traegt sich selbst.
Kein Neubau, keine Ersatz-Zahl (D-14 verbietet sie ausdruecklich).

**Anzeige-Seite** (`dashboard.html:120-124` + `:516` + `:526`):

```html
    <div class="db2-kpi">
      <div class="db2-kpi-value" id="kpi-score">—</div>
      <div class="db2-kpi-label">Ø Score</div>
      <div class="db2-kpi-trend" id="kpi-score-trend"></div>
    </div>
```
```javascript
  setText('kpi-score',   k.avg_score != null ? fNum(k.avg_score) : '—');
  ...
  setHtml('kpi-score-trend',    fTrend(t.avg_score_pct, '%'));
```

**Analog fuer den Umbau:** die Nachbar-Kachel `kpi-sessions` (`:125-129`, `:517`, `:527`) — eine
reine **Zaehl**-Kachel. „Zaehlen ist erlaubt, benoten nicht" (SPEC Constraints). Wird die Kachel
entfernt, muss die 5er-Reihe (`db2-kpi-row`, Kommentar `:118` sagt „5 Kacheln") mitgezaehlt und der
Kommentar nachgezogen werden.

---

### 2.11 `static/pip-launcher.js` (D-13/D-14)

**Zweite Kopie der Formel** (`:3266-3277`) — faellt mit `_calc_call_score`:

```javascript
  function _calcScore(postcall) {
    var kb = postcall.kb_end || 30;
    ...
    return Math.min(100, Math.max(0, Math.round(kb * 0.4 + behandeltRate * 100 * 0.3 + redeScore * 0.2 + skript * 0.1)));
  }
```

**Score-Anzeige + Details-Knopf** (`:4287-4305`) — die Stelle, an der D-13 haengt:

```javascript
    // Score aus json.final_score (S-01, kein _calcScore). Haengt NICHT an pendingPostcall.
    if (json.final_score !== undefined && json.final_score !== null) {
      var scoreEl = pipEl('nlp-postcall-score');
      if (scoreEl) {
        scoreEl.style.display = '';
        scoreEl.textContent = String(json.final_score) + '%';
        var _fs = +json.final_score;
        scoreEl.style.color = (_fs >= 70) ? 'var(--session-score-high)'
          : (_fs >= 40) ? 'var(--session-score-mid)'
          : 'var(--session-score-low)';
        var _lbl = scoreEl.parentNode ? scoreEl.parentNode.querySelector('.pip-postcall-label') : null;
        if (_lbl) { _lbl.style.display = ''; _lbl.textContent = 'Coaching-Score'; }
      }
      var detailsBtn = pipEl('nlp-btn-details');
      if (detailsBtn) { detailsBtn.style.display = ''; detailsBtn.disabled = false; }
      // F9: Trend mit dem BESTAETIGTEN final_score fuettern (nicht _calcScore).
      _fetchAndRenderTrend(json.final_score);
    }
```

★ **Drei Dinge, die der Plan hier wissen muss:**
1. **Der „Auswertung ansehen"-Knopf existiert bereits** — `templates/base.html:635`:
   `<button class="pip-btn-details" id="nlp-btn-details">Auswertung</button>`. D-13 verlangt keinen
   neuen Knopf, sondern **ihn aus der `if (json.final_score ...)`-Klammer herausziehen** — sonst
   verschwindet er zusammen mit der Note. Damit ist auch die Farb-HART-Regel automatisch erfuellt
   (kein neues Token, keine neue Farbe).
2. **Die drei Farb-Zuweisungen `:4294-4296`** sind `var(--session-score-*)`-Tokens, die mit der Note
   sterben — grep, ob sie danach noch Nutzer haben (Punkt 20 gilt auch fuer CSS-Tokens).
3. **`_fetchAndRenderTrend` (`:3489-3513`)** faellt komplett (D-14) samt `nlp-postcall-trend`-Element
   und der Klasse `pip-postcall-trend`/`.up`/`.down`.

**Was BLEIBT (D-13, „Zaehl-Kacheln bleiben")** — `_renderQuickStats`, Ausschnitt `:3480-3486`:

```javascript
        + '<div class="pip-quickstat-split-row">'
          + '<div class="pip-quickstat-split-col"><div class="pip-quickstat-value">' + (hasRede ? redeB + '%' : '–') + '</div><div class="pip-quickstat-sublabel">Vertriebler</div></div>'
```

⚠ Diese Kachel zeigt den **Redeanteil** — im Kaltakquise-Modus baubedingt 100 % (Schild
`0039:46`). D-16 hat die Redeanteil-Zeile auf der Auswertungsseite genau deswegen gestrichen.
Der Widerspruch ist als Roadmap-Punkt **4.0.2** terminiert (SPEC NACHTRAG 2 ⑨) — **hier nicht
mitfixen**, aber im SUMMARY nennen, sonst steht er nach der Phase unbemerkt da.

**Weitere Fundstellen von `nlp-btn-details`**, die der Plan mitpruefen muss (`grep` ausgefuehrt):
`pip-launcher.js:1838`, `:2193`, `:3531`, `:3551`, `:3570`, `:4301` — sechs Stellen, nicht eine.

---

### 2.12 `config.py` (Requirement 1)

**Analog: der Nachbar-Eintrag** (`config.py:299-309`):

```python
# ── Phase 08.23.2.TAXO2 Plan 04: Call-Ende-Merge — Audio-Gate (D-09) + Retry (F-07) ──
# Post-launch tunbar ohne Code-Deploy (Punkt 12, <30s reversibel per ENV).
# AUDIO_HEALTH_GATE_THRESHOLD: calls.audio_health_score liegt auf der 0.0-1.0-Skala
#   (NICHT 0-100!). Unter dieser Schwelle ODER NULL -> Call not_gradable (D-09): ...
AUDIO_HEALTH_GATE_THRESHOLD = float(os.getenv('AUDIO_HEALTH_GATE_THRESHOLD', '0.5'))
# MIN_HIGH_CONFIDENCE_EVENTS: D-09 "zu wenig hoch-konfidente Events" — ...
MIN_HIGH_CONFIDENCE_EVENTS = int(os.getenv('MIN_HIGH_CONFIDENCE_EVENTS', '3'))
```

Neue Schwellen (2 Abschnitte / 20 Woerter) genau so: `int(os.getenv('NAME', '20'))` + Prosa-Kommentar
darueber, der die **Herleitung aus dem Zweck** und den **Nachjustier-Termin** („~100 echte Anrufe",
SPEC Restluecke 2) nennt.

⚠ **`MIN_HIGH_CONFIDENCE_EVENTS` nicht blind loeschen** — Punkt 20 + `slow_lane.py:585-586`
(Import). Entweder mit Foundation-Kommentar stehenlassen oder Import **und** Konstante zusammen im
selben Commit entfernen.

---

### 2.13 Tests

**Analog A — Template-Zweige + Entkopplung (Requirement 3/4/5):** `tests/test_session_detail_render.py`.
Das ist die praezise Vorlage: echte DB-Zeilen, HTTP-Request, Assertion auf HTML.

Seed-Muster (`:95-121`), inklusive des Kommentars, der fuer D-20 **umgedreht** wird:

```python
    db.add(Call(
        id=call_id,
        user_id=u.id,
        tenant_id=tenant_id,
        call_mode='cold_call',
        started_at=_now(), ended_at=_now(),
        transcript_storage='none',
        # outcome NICHT NULL -> outcome_confirmed=True -> die ANZEIGE-Sperre (0) ist offen,
        # sonst erreicht der Test den Beobachtungs-Zweig gar nicht.
        outcome='meeting_booked',
        conversation_log_id=conv.id,
    ))
```

★ **Der Test fuer Requirement 4 ist genau dieser Seed mit `outcome=None`** und derselben
Assertion — vorher rot (Sperre), nachher gruen. Ein sauberer ROT-vor-GRUEN-Beleg ohne neue Mechanik.

**Die RLS-Falle, die der Test schon geloest hat** (`:125-138`) — Pflicht-Vorlage, sonst ist der Test
aus dem falschen Grund gruen/rot:

```python
def _login(client, u, tenant_id):
    """Login MIT `tenant_id` in der Flask-Session.

    PFLICHT, nicht Kosmetik: `app.py:2292` liest die Mandanten-UUID aus der Flask-Session
    ... Ohne diesen Eintrag ist die GUC leer, und `rubric_score` (FORCE RLS) liefert **0 Zeilen** —
    der Route-Pfad landet dann im Zweig „(d) row-absent" ..."""
    with client.session_transaction() as s:
        s['user_id'] = u.id
        s['rolle'] = 'owner'
        s['tenant_id'] = str(tenant_id)
```

Teardown-Muster (`:50-65`): zwei `cleanup_rows`-Aufrufe wegen der FK-Reihenfolge `calls` ←
`conversation_logs` (CLAUDE.md Test-Cleanup-Regel).

**Analog B — Zaehlregel-Tests (D-09, „erst rot"):** `tests/test_judge_runner.py`. Fake-Segmente via
`_make_segment(...)` (`:105-108`, `:169-173`) und Assertions auf den gebauten String — genau die
Form, die „gebaute Transkripte" verlangt. **Die Schwellen-Kante (3× darf nicht, 4× muss) gehoert als
zwei getrennte Testfunktionen** dorthin, nicht als eine mit Schleife.

**Analog C — Abwesenheits-Anker als Runtime-Test** (`tests/test_judge_runner.py:102-119`):

```python
def test_prompt_has_no_outcome():
    """Der Prompt-Text enthaelt KEINE Outcome-Werte — physische Trennung, nicht nur Weglassen."""
    ...
    system_str, user_str = jr._build_judge_prompt(call, events, _fake_profile_briefing(), segments)
    full_prompt = system_str + ' ' + user_str

    for outcome_val in ['meeting_booked', 'no_interest', 'contract_signed', 'callback', 'wrong_person']:
        assert outcome_val not in full_prompt, f"Outcome-Wert '{outcome_val}' im Prompt gefunden!"

    assert 'outcome' not in full_prompt.lower(), "Das Wort 'outcome' ist im Prompt!"
```

★ **Diesem Test fehlt der Existenz-Anker** — er wuerde auch gruen, wenn `_build_judge_prompt` ein
leeres Tupel zurueckgaebe. Requirement 8 verlangt den Anker ausdruecklich. Der neue Test fuer
„kein Fokus-Schluessel im Bewertungs-Auftrag" muss daneben belegen, **dass ueberhaupt gelesen wurde**
— z.B. `assert '[#1 berater 500ms]' in user_str` (Form aus `:179`) oder `assert len(user_str) > 200`.
`test_rubric_at_start_and_end` (`:145-159`) zeigt die gute Variante: es zaehlt (`count >= 2`) statt
nur „nicht vorhanden" zu pruefen.

---

## 3. Shared Patterns (Pflicht-Teil)

### 3.1 Anker-Muster: Abwesenheits-Anker MIT gepaartem Existenz-Anker (Bau-Regel 20)

Im Repo lebt das Muster **in den Plan-Dateien** (`<acceptance_criteria>`), nicht in den Tests. Das
sauberste Vorbild, **verbatim** aus `.planning/phases/08.23.2.ZEITSTEMPEL-1-.../08.23.2.ZEITSTEMPEL-1-01-PLAN.md:285-294`:

```markdown
    - Die Datei enthaelt **keine** der verbotenen Muster (Test-Qualitaets-Regel). Abwesenheits-Anker:
      `grep -c "inspect.getsource\|hasattr(" tests/test_transcript_segments_write.py` liefert **0**;
      gepaarter Existenz-Anker: `grep -c "assert " tests/test_transcript_segments_write.py`
      liefert **24** (11 im Bestand **gemessen**, plus 13 aus der Vorlage oben).
    - **Die gestrichene Mechanik ist restlos raus:**
      `grep -c "seam_before\|_dg_ms_offset\|_dg_last_end_ms\|_dg_seam_pending\|mark_dg_seam\|offset_ms" tests/test_transcript_segments_write.py`
      liefert **0**. Gepaarter Existenz-Anker: `grep -c "word_count" ...` liefert **9**
      (**ausgefuehrt**: 5 Vorkommen in den Dict-Literalen + 4 in den Zusicherungen).
```

Und die Zeilen-vs-Vorkommen-Praezisierung (`:403-408`):

```markdown
      gepaarter Existenz-Anker **auf Zeilen**:
      `grep -c "_extract_word_times(" tests/test_speech_timing_extraction.py` liefert **7**,
      und **auf Vorkommen**:
      `grep -o "_extract_word_times(" tests/test_speech_timing_extraction.py | wc -l` liefert **8**.
      Beide Zahlen sind ausgefuehrt; wer nur eine prueft, prueft die richtige — sie sind
      absichtlich beide genannt, damit die Verwechslung nicht ein siebtes Mal passiert.
```

**Vier Regeln, die aus den Phasen-LEARNINGS gelten und hier scharf sind:**
1. **`grep -c` zaehlt ZEILEN, `grep -o … | wc -l` zaehlt VORKOMMEN.** Immer dazuschreiben, welche.
2. **`grep -ciF` ist verboten** (CLAUDE.md „Acht weitere Vault-Regeln" Nr. 8 — stuerzt auf Andres
   Git-Bash ab und liefert **nichts** statt `0`).
3. **Nie auf Prosa ankern, die derselbe Plan vorschreibt** (SPEC Constraints: „Ein zaehlender Anker
   darf nie auf eine Zeichenkette zeigen, die derselbe Plan als Kommentartext vorschreibt"). Also
   auf **Code-Form** ankern (`_calc_call_score(`), nicht auf den nackten Namen und nicht auf einen
   Kommentar-Satz.
4. **RLS-Klassen-Gleichheit** (SPEC Constraints): ein Existenz-Anker fuer eine `rubric_score`-Pruefung
   muss **selbst** auf eine FORCE-RLS-Tabelle zeigen. Ein Anker auf `calls` (keine RLS) beweist nur,
   dass das Werkzeug laeuft.

**Die sieben (bzw. acht) Lesestellen aus D-12 als konkrete Anker-Liste** — jede braucht ihren
eigenen Abwesenheits-**und** Existenz-Anker, weil ein Anker auf `_calc_call_score` die
`kb_end`-Bildschirme **nicht** findet:

| # | Stelle | Abwesenheits-Anker auf | Gepaarter Existenz-Anker |
|---|---|---|---|
| 1 | `app_routes.py:1375` | `_calc_call_score(` | `def api_postcall_trend` bzw. Nachbar-Route |
| 2-4 | `dashboard.py:742/792/813` | `_calc_call_score` | `def session_detail` |
| 5 | `performance.py:100-102`→`:158`/`:469` | `l.kb_end for l in logs_90` | `closing_rate` (Nachbar-KPI) |
| 6 | `performance.py:258-268` | `score_trend_pct` | `greeting_subtitle` (bleibt) |
| 7 | `app_routes.py:1124` → `session_detail.html:384` | `func.avg(CL.kb_end)` | `_cross_context_objections_live` |
| 8 | `pip-launcher.js:3266` | `function _calcScore` | `function _renderQuickStats` (bleibt) |

---

### 3.2 Alembic-Migrations-Muster + die `COMMENT ON`-Doppelpunkt-Falle

**Analog: `alembic/versions/0039_zeitstempel1_sprechzeiten.py`** (juengste Migration, Kopf `0039`).

**Der Helfer, der zwingend zu kopieren ist** (`0039:52-73`, verbatim):

```python
def _comment(objekt: str, text: str) -> None:
    """COMMENT ON absetzen, ohne dass SQLAlchemy im Schild-Text herumliest.

    NICHT auf op.execute(str) umstellen. Das dreht den String durch sa.text(), und das
    liest jeden Doppelpunkt, dem KEIN Wortzeichen vorausgeht, als Bind-Parameter:
    im Schild stand die Zeilenangabe frei als "in <doppelpunkt>113-118", daraus wurde
    '%(113)s-118' und die Migration starb mit "A value is required for bind parameter
    '113'" (gefangen im Wegwerf-nerve_test am 2026-08-10, bevor Production sie sah).
    Die benachbarte Referenz 'deepgram_service.py:490' ueberlebte, weil dort ein
    Buchstabe vor dem Doppelpunkt steht - die Falle schlaegt also nur bei manchen
    Schreibweisen zu und ist damit genau die Sorte, die beim naechsten Schild wiederkommt.
    Der Schild-Text traegt seither die volle Datei-Referenz statt der nackten Zeilenzahl;
    das allein waere aber nur die Symptom-Haelfte, deshalb zusaetzlich exec_driver_sql.

    exec_driver_sql reicht die Zeichenkette unveraendert an den Treiber durch: keine
    Bind-Parameter-Erkennung, und ohne params auch keine %-Ersetzung durch psycopg2.
    Schild-Texte sind Prosa - sie muessen Doppelpunkte und Prozentzeichen tragen duerfen,
    ohne dass die Migration daran zerbricht.
    """
    op.get_bind().exec_driver_sql(
        "COMMENT ON {} IS '{}'".format(objekt, text.replace("'", "''"))
    )
```

**Schild-Texte als Modul-Konstanten + `upgrade`/`downgrade`** (`0039:43-49`, `:76-90`):

```python
_SCHILD_START_MS = 'Beginn des Abschnitts in ms auf der DEEPGRAM-AUDIO-Achse ...'
# Die Fassung aus 0015 — fuer downgrade() (verbatim aus inspect.sh schilder, 2026-08-10).
_SCHILD_TABELLE_ALT = 'Anonymisierte Transkript-Segmente pro Call ...'

def upgrade():
    op.add_column('transcript_segments', sa.Column('start_ms', sa.Integer(), nullable=True))
    ...
    _comment('COLUMN transcript_segments.start_ms', _SCHILD_START_MS)
    _comment('TABLE transcript_segments', _SCHILD_TABELLE_NEU)


def downgrade():
    _comment('TABLE transcript_segments', _SCHILD_TABELLE_ALT)
    op.drop_column('transcript_segments', 'word_count')
```

**Drei Pflicht-Bestandteile aus dem `0039`-Docstring, die jede neue Migration wieder braucht:**
- **„WARUM ALEMBIC UND NICHT `_migrate()`"** (`0039:21-22`): `app._migrate()` returnt auf Postgres
  frueh (`app.py:140`) — wirkungslos **und** Alt-Muster (Punkt 29).
- **Prod-Kopf beim Planen belegt** (`0039:30-31`): `inspect.sh migrations` → `version_num`, und
  vom Executor **erneut** gezogen.
- **Ausroll-Reihenfolge ausdruecklich** (`0039:24-28`): nullable Spalten → Migration **vor** dem
  Deploy, weil der Alt-Code vorwaerts-kompatibel ist.
  ⚠ **Diese Regel gilt nur fuer nullable Spalten** (Lehre aus ZEITSTEMPEL-1). METRIK-1 fasst
  vermutlich **gar keine Spalte** an — Requirement 2 und D-23 schreiben in das bestehende
  `payload_jsonb` (`models.py:923`). Dann braucht es **keine Struktur-Migration**, aber sehr wohl
  eine **Schild-Migration**: `payload_jsonb` (`:923`) und `status` (`:921`) beschreiben heute nur
  `scored|pending|not_gradable (D-09 poor_audio_health)` bzw. „Reserve + Training-only-Felder" —
  beide Texte werden durch diese Phase **falsch** (Punkt 23 Aktualitaets-Pflicht, `comment=` in
  `models.py` + `COMMENT ON`-Migration im **selben** Commit).

---

### 3.3 Waechter-Muster mit `RESTLUECKEN`-Docstring (Punkt 31)

**Analog A — die knappste vollstaendige Form:** `tests/test_live_timeout_coverage.py:17-40`, verbatim:

```python
RESTLUECKEN
-----------
1. ABGEDECKT: dass jeder messages.create/messages.stream-Aufruf in services/claude_service.py
   und services/qa_pipeline.py ein timeout-Schluesselwort traegt, dass die Stream-Aufrufe die
   ZWEITE Zahl benutzen (LIVE_LLM_STREAM_TIMEOUT_S statt LIVE_LLM_TIMEOUT_S), und dass der
   Modul-Client mit max_retries=0 erzeugt wird.

2. ⚠ ER SIEHT EIN SCHLUESSELWORT, NICHT DEN WERT. `timeout=0.0001` oder `timeout=None` waere
   gruen. Fuer die Stream-Pfade prueft er immerhin den NAMEN der Konstante — nicht deren Zahl.
   Die Zahlen selbst sind benannte Festlegungen in config.py (F-1/F-2) und werden von der
   Abnahme an echten Daten getragen (D-06, Plan 08), nicht von diesem Test.

3. ⚠ ER SAGT NICHTS DARUEBER, OB DER except-ZWEIG ERREICHBAR IST. anthropic.APITimeoutError ist
   eine UNTERKLASSE von anthropic.APIConnectionError — steht das breitere except zuerst, ist
   der Timeout-Zweig unerreichbar und Stufe 1/2 aus D-04 feuern nie, waehrend dieser Waechter
   gruen bleibt. Dagegen steht allein der Runtime-Test aus Plan 06 Task 3.

4. STRUKTURELL UNSICHTBAR: geparst werden NUR die VIER Sweep-Dateien
   (SWEEP_DATEIEN = services/claude_service.py, services/qa_pipeline.py,
   services/adoption_runner.py, services/judge_runner.py). Ein Live-LLM-Pfad in einer FUENFTEN
   Datei taucht hier nie auf ... Ebenso unsichtbar: dynamischer Dispatch via getattr,
   Monkeypatch, ein Wrapper, der messages.create weiterreicht, und jeder Aufruf, der zur
   Laufzeit entsteht.
```

**Analog B — die Zusatz-Abschnitte, die Punkt 31 ebenfalls verlangt:**
`tests/test_session_lock_blocking_calls_guard.py:44-50` (kein Source-Presence-False-Green),
`:66-77` (`BEKANNTE GRENZEN`, inkl. Mindest-Soll-Regel) und `:79-85` (`STOP-REGEL`), verbatim
gekuerzt:

```python
BEKANNTE GRENZEN (ehrlich benannt, nicht heimlich)
--------------------------------------------------
1. Der Sweep sieht nur **direkte** Aufrufe im Block. Ein Helfer, der zwei Ebenen tiefer
   `get_session()` ruft, rutscht durch. ...
2. `_SOLL_MINDESTENS` ist ein **Mindest**wert. Ein weiterer legitimer Rueckbau wuerde den
   Waechter rot machen — dann wird die Zahl **mit Begruendung** nachgezogen, der Test wird
   **nicht** entfernt.
...
STOP-REGEL
----------
Erwartet sind **0** Verstoesse. Findet der Sweep einen echten blockierenden Aufruf unter
dem Riegel, ist das ein **Fund** und kein Konfigurations-Problem: anhalten, mit
`Datei:Zeile` melden, **nicht** die Whitelist fuellen und **nicht** das Muster aufweichen.
Einzige erlaubte Whitelist-Kategorie ist ein nachweislicher **Falsch**-Treffer (Aufruf, der
nur zufaellig so heisst), mit `# FALSCH-TREFFER:`-Kommentar, `Datei:Zeile` und Begruendung.
```

**Pflicht-Bausteine fuer jeden Waechter dieser Phase** (Punkt 31, in derselben Reihenfolge):
`ABGEDECKT` (positiv, ein Satz) · `strukturell unsichtbar` · `zweischneidige Heuristiken`
(beide Richtungen) · `formal UNKLAR` · `ZWEITE SCHICHT DARUNTER` · `GEPRUEFT UND GESCHLOSSEN`
(besonders fuer die direkteste Form der Fehlerklasse) · **Mindest-Soll** gegen den stillen Ausfall ·
**ROT-Lauf verbatim ins SUMMARY**.

⚠ **Fuer diese Phase konkret:** Ein Waechter, der „kein Fokus-Schluessel im Bewertungs-Auftrag"
prueft, muss in seinen Restluecken benennen, dass er den **gerenderten Prompt** sieht und nicht das,
was das Modell daraus macht — und dass er blind wird, wenn `_build_judge_prompt` umbenannt wird
(Mindest-Soll auf die Prompt-Laenge oder auf die Zahl der gerenderten Segmente).

---

### 3.4 Jinja-`if/elif`-Kette in `session_detail.html`

Vollstaendig zitiert in **2.7**. Kurz die Einreih-Regeln:

- **Ein Zweig = ein `{% elif %}` + ein `{# (buchstabe) Erklaerung #}`-Kommentar.** Die Kette ist
  durchbuchstabiert `(0) (a) (b) (d) (e)`, dokumentiert im Kopfkommentar `:119-124`. Der neue
  Ablehnungs-Grund ist **kein neuer Buchstabe**, sondern ein dritter Zweig **innerhalb** von `(b)`
  (`:140-145`) — dort haengt schon die `_reason`-Weiche.
- **Alte Gruende nie loeschen** — `too_few_high_confidence_events` bleibt, weil Alt-Zeilen in der DB
  stehen (SPEC Boundaries + CONTEXT „Established Patterns").
- **Der Kopfkommentar `:119-124` wird mitgezogen** — er listet die Reihenfolge auf und wird durch
  D-20 (Zweig `(0)` faellt) und den neuen Grund **falsch**, wenn man ihn stehen laesst.
- **Keine Inline-Styles, keine Hex-Farben** — der Kopfkommentar sagt es selbst (`:124`):
  *„Kein hardcoded Hex, keine Inline-Styles — nur .n-*-Klassen + CSS-Variablen (CLAUDE.md)."*
  Bestehende Klassen wiederverwenden: `.n-observation-empty`, `.n-observation-quote`,
  `.n-observation-text`, `.n-observation-dim`, `.n-compliance-alert`.
  ⚠ Der Hero verstoesst heute mit `style="--w: {{ kb_pct }}%"` (`:101` u.a.) — das ist der erlaubte
  Sonderfall „dynamischer Wert" und faellt ohnehin mit D-16 weg.

---

## 4. No Analog Found

| Datei/Baustein | Rolle | Datenfluss | Grund |
|---|---|---|---|
| Vier **englische** Satz-Schablonen mit eingesetzter Zahl (D-07) | utility | transform | Es gibt im Repo **keinen** englischen nutzersichtbaren Text — alle Coaching-/Prompt-Texte sind deutsch (`judge_dimensions.py`, `handling_markers.py`, alle Templates). Der Katalog ist die **erste** englische Scheibe (SPEC Req 7). Kein Vorbild → Form frei (Claude's Discretion), aber die Modul-Bauform aus `handling_markers.py` uebernehmen. |
| Anwendungs-Pruefung ueber **zwei** Anrufe (Req 8, letzte Welle) | utility | batch | Kein Bestand liest „Fokus des Vor-Anrufs" — der Begriff existiert im Code nicht (SPEC Req 6 „Current: Es gibt keinen Fokus-Begriff im Code", von mir per grep bestaetigt). Naechster Verwandter ist `_cross_context_objections_live` (`app_routes.py:1110-1138`), das Ueber-Sitzungen-Muster — aber es rechnet mit `kb_end` und wird in dieser Phase gerade abgebaut. **Nicht als Vorlage nehmen.** |

---

## 5. Funde beim Kartieren, die in den Plan gehoeren

1. ★ **`tests/test_judge_runner.py:184` (`assert '*ewb button*' in user_str`) wird durch D-04 ROT.**
   Retargetierung gehoert in denselben Task wie der Filter (siehe 2.4).
2. ★ **Der „Auswertung ansehen"-Knopf existiert bereits** (`templates/base.html:635`,
   `id="nlp-btn-details"`, Beschriftung „Auswertung"). D-13 ist ein **Herausziehen** aus der
   `final_score`-Klammer, kein Neubau — damit auch kein Farb-/Token-Risiko.
3. ⚠ **Zwei weitere Score-Formeln neben `_calc_call_score`:** `_calc_process_score`
   (`app_routes.py:1032-1049`) + `_OUTCOME_MODIFIERS` (`:1021-1029`) → `final_score`, und **das**
   ist die Zahl im PiP (`pip-launcher.js:4288`). Ein Anker auf `_calc_call_score` findet sie nicht —
   dieselbe Falle wie bei den drei `kb_end`-Bildschirmen, nur eine Ebene tiefer.
4. ⚠ **`performance.py:158` `'avg_score'` hat vermutlich keinen Leser** (grep ueber `*.html`/`*.js`:
   nur `dashboard.html:516` liest `kpis.avg_score` → `:469`). Mit frischem grep + Existenz-Anker
   belegen, dann als achte Stelle mit-erledigen oder als tot markieren.
5. ⚠ **`_upsert_rubric_score` ueberschreibt `payload_jsonb` vollstaendig** (`slow_lane.py:473-477`).
   Das erfuellt D-23 („nicht doppelt zaehlen") **nur, wenn** der Zaehler als Absolutwert geschrieben
   wird. Ausdruecklich in den Plan schreiben, sonst baut jemand ein `jsonb ||`-Inkrement.
6. ⚠ **`dashboard.py:795-796` ist die Trainings-Note** (`score_total = kb_end_effective`). Wer den
   `_calc_call_score`-Zweig darueber loescht, muss den `else` **stehen lassen** — sonst stirbt D-17
   still.
7. ⚠ **`_count_high_confidence` behandelt `None` als „zaehlt nicht"** (`slow_lane.py:406-408`).
   D-22 (a) verlangt fuer `word_count` das Gegenteil. Wer die Vorlage kopiert, kopiert die falsche
   NULL-Semantik.
8. ⚠ **Sechs Fundstellen von `nlp-btn-details`** in `pip-launcher.js` (`:1838`, `:2193`, `:3531`,
   `:3551`, `:3570`, `:4301`) — der Knopf wird an mehreren Stellen ein-/ausgeblendet.
9. ⚠ **Redeanteil-Widerspruch bleibt nach der Phase sichtbar** im PiP (`pip-launcher.js:3481-3482`,
   „Vertriebler / Kunde"-Kacheln), obwohl D-16 die Zeile auf der Auswertungsseite streicht.
   Terminiert auf Roadmap 4.0.2 — **gehoert ins SUMMARY**, nicht in diese Phase.
10. ⚠ **Schild-Aktualitaet (Punkt 23):** `models.py:921` (`status`: „scored|pending|not_gradable
    (D-09 poor_audio_health)") und `:923` (`payload_jsonb`: „Reserve + Training-only-Felder") werden
    durch Req 1/2/3 **beide falsch**. Nachziehen im selben Commit — `comment=` **und**
    `COMMENT ON`-Migration.
11. ⚠ **`transcript_segments` hat KEINE RLS, `rubric_score` HAT FORCE-RLS** (`models.py:908-909`).
    Der Tor-Umbau liest RLS-frei (`slow_lane.py:593-594` „GUC-frei"), der Zaehler-Write braucht die
    GUC-Klammer (`:640-646`). Nicht vermischen.

---

## 6. Metadata

**Analog-Suchbereich:** `services/`, `routes/`, `templates/`, `static/`, `database/`, `alembic/versions/`, `tests/`, `config.py`
**Gelesene Produktivdateien (read-only):** `services/slow_lane.py`, `services/judge_runner.py`,
`services/beleg_check.py`, `services/handling_markers.py`, `services/judge_dimensions.py`,
`services/deepgram_service.py`, `routes/dashboard.py`, `routes/app_routes.py`,
`routes/performance.py`, `routes/learning.py`, `routes/admin_ewb.py`, `routes/training.py`,
`templates/session_detail.html`, `templates/dashboard.html`, `static/pip-launcher.js`, `config.py`,
`database/models.py`, `alembic/versions/0039_zeitstempel1_sprechzeiten.py`
**Gelesene Tests:** `tests/test_session_detail_render.py`, `tests/test_judge_runner.py`,
`tests/test_live_timeout_coverage.py`, `tests/test_live_timeout_stufen.py`,
`tests/test_ownership_state_guard.py`, `tests/test_session_lock_blocking_calls_guard.py`
**Nichts geaendert.** Diese Datei ist das einzige Schreib-Ergebnis.
**Pattern extraction date:** 2026-08-13

*Phase: 08.23.2.METRIK-1*
