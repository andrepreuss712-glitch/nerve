---
status: partial
phase: 08.23.2.STT-deepgram-qualitaet-nova3-keyterm-sprecher-label
source: [08.23.2.STT-01-PLAN.md, 08.23.2.STT-02-PLAN.md]
started: 2026-06-05
updated: 2026-06-05
---

## Current Test

[awaiting human testing — Production-Deploy + Test-Calls durch André]

> **HART-Regel:** Diese Phase ist nur auf Production verifizierbar (kein Local-Dev).
> Code ist committet (Commits 3705664…01ce28f). Verifikation = `bash deploy.sh production`
> + Test-User-Calls + `inspect.sh logs`/`sample`-Soll-Ist.

## Vorab — Deploy

### 0. Production-Deploy
command: `bash deploy.sh production`
expected: Test-Gate läuft auf Prod vor Restart (pre-existing-roter `test_ft_seed` blockiert NICHT — Code landet). Bei NEUEN Test-Fehlern: kein Restart, alter Code bleibt live → dann hier STOP + melden.
result: [pending]

## Tests

### 1. nova-3 ist Live-Model (STT-1)
expected: `[DG] LiveOptions: model=nova-3 …` in jeder Connection-Open-Logzeile (cold_call UND meeting). Kein `nova-2` mehr.
verify: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs'`
result: [pending]

### 2. keyterm-Liste wird gebaut + gereicht (STT-2)
expected: `[DG] LiveOptions: … keyterm_count=N` mit **N > 15** bei befülltem Profil (Grundliste 16 + Profil-Terms).
verify: gleiche `inspect.sh logs`-Ausgabe wie #1.
result: [pending]

### 3. SDK-Fallback griff NICHT (Review HIGH — try/except keyterm)
expected: `keyterm_count` ist > 0 (nicht 0) UND keine `[DG] LiveOptions init mit keyterm fehlgeschlagen …`-Zeile im Log. `keyterm_count=0` + Fallback-Zeile = SDK akzeptiert den `keyterm`-Kwarg NICHT → ernst: Call läuft zwar (Fallback), aber keyterm-Effekt fehlt → SDK-Version prüfen.
verify: `inspect.sh logs` nach Fehlerzeile grepen.
result: [pending]

### 4. A1-Serialisierung: repeated-param vs. CSV-Blob (Review MEDIUM)
expected: Die keyterm-Liste geht als **wiederholte** Query-Params an Deepgram (`&keyterm=Einwand&keyterm=Cold%20Call`), NICHT als ein CSV-Blob (`"['Einwand','Cold Call']"`). Bei CSV-Blob brechen mehrwortige Terms ("Cold Call") + Deepgram lehnt evtl. mit 400 ab.
verify: NICHT über das lokale `[DG]`-Dict-Log beweisbar. Entweder (a) Deepgram-Dashboard → Request-Logs (mehrere `keyterm=`-Einträge sichtbar?) ODER (b) SDK-Debug-Logger temporär an (`DeepgramClientOptions(verbose=logging.DEBUG)` / `logging.getLogger('deepgram').setLevel(logging.DEBUG)`), ausgehende Query prüfen, danach zurücknehmen.
result: [pending]

### 5. Fachwort-Erkennung Soll-Ist (STT-2 Wirkung)
expected: In den `[DG] [Berater] …`-Zeilen jetzt korrekt: "Einwände" (vorher "ein, wenn"), "Cold Call" (vorher "Callcalls"), Markenname (vorher Lautmalerei), "Vertriebler" (vorher "Fahrradbetreiber").
verify: André gegen bekannten Pitch im Test-Call.
result: [pending]

### 6. Leeres/fehlendes Profil bricht Call nicht (STT-2 Defensive)
expected: Test-Call mit leerem/ohne Profil startet fehlerfrei; `keyterm_count` = mind. Grundliste (~16), kein Crash.
result: [pending]

### 7. Cold-Call Sprecher-Label = Berater (STT-3)
expected: cold_call: jede finale Zeile `[DG] [Berater] …` statt `[DG] [Unbekannt] …`. Frontend zeigt Berater, nicht SYSTEM/Unbekannt. Persist: `speaker=0` statt NULL.
verify: `inspect.sh logs` + `inspect.sh sample conversation_logs 20` (jüngster cold_call: speaker=0).
result: [pending]

### 8. Meeting-Pfad unverändert (Regression-Schutz STT-3)
expected: meeting-Call: weiterhin korrekte 0/1-Zuordnung (Berater/Kunde) via diarize=True; bestehende roles_confirmed-Heuristik greift. NICHT gebrochen.
result: [pending]

## Summary

total: 9
passed: 0
issues: 0
pending: 9
skipped: 0
blocked: 0

## Gaps
