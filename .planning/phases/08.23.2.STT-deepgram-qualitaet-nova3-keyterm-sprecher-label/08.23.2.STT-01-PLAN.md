---
phase: 08.23.2.STT
plan: 01
type: execute
complexity: 🟡 mittel
wave: 1
depends_on: []
files_modified:
  - services/deepgram_service.py
autonomous: true
requirements:
  - STT-1   # nova-2 -> nova-3 (model + cost-tag + log)
  - STT-2   # keyterm Fachwort-Liste (3-Schichten, build_keyterms, reorder, wiring)

must_haves:
  truths:
    - "Jeder Live-Call (cold_call UND meeting) startet die Deepgram-Connection mit model=nova-3."
    - "Bei befuelltem Profil enthaelt die Deepgram-Connection eine keyterm-Liste (Grundliste + Profil-Terms)."
    - "Bei leerem/fehlendem Profil startet der Call trotzdem fehlerfrei (nur Grundliste als keyterm)."
    - "Die keyterm-Liste wird NIE mit dem Transkript persistiert (nur Transkriptions-Hint)."
    - "Das [DG]-Log zeigt model=nova-3 + keyterm_count=N (Werte selbst NICHT geloggt)."
    - "Die Kosten-Attribution (log_api_cost) taggt 'nova-3' statt 'nova-2'."
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "build_keyterms() Helper + nova-3 LiveOptions(keyterm=...) + Reorder Profil-Load vor Connection-Open"
      contains: "def build_keyterms"
  key_links:
    - from: "handle_start_live_session (Profil-daten+branche geladen)"
      to: "_open_deepgram_connection(..., keyterms=...)"
      via: "build_keyterms(daten, branche, mode) VOR dem Connection-Open"
      pattern: "build_keyterms\\("
    - from: "_open_deepgram_connection(keyterms=...)"
      to: "LiveOptions(keyterm=[...])"
      via: "options_kwargs['keyterm'] = keyterms"
      pattern: "options_kwargs\\['keyterm'\\]"
---

<objective>
Stufe-1-Hebel 1+2 dieser Phase: STT-Qualitaet anheben durch **nova-2 -> nova-3** Model-Swap und eine **3-Schichten keyterm-Fachwort-Liste** (feste Sales-Grundliste + per-Call Profil-Extraktion). Beides MUSS im selben Change landen, weil `keyterm` **nova-3-only** ist.

Purpose: Deepgram erkennt Vertriebs-Fachbegriffe und Profil-spezifische Marken/Branche/Einwand-Begriffe korrekt (vorher: "Einwaende" -> "ein, wenn", "Cold Call" -> "Callcalls", Markenname -> Lautmalerei). keyterm = reiner Erkennungs-Hint, keine neue Datenkategorie, keine Persistenz (DSGVO unveraendert).

Output: `services/deepgram_service.py` mit `build_keyterms()`-Helper, `_open_deepgram_connection`-Signatur `(sid, mode='meeting', keyterms=None)`, `model="nova-3"`, `LiveOptions(keyterm=[...])`, Reorder (Profil-`daten`+`branche` VOR Connection-Open) und konsistentem Cost-Tag + [DG]-Log.

OUT OF SCOPE (Stufe 2, NICHT in diesem Plan): `endpointing`/`utterance_end_ms`-Tuning gegen Verdopplung; `profile_skripte`-Extraktion (eigene DB-Tabelle, NICHT im Session-Cache — Aufwand/Nutzen schlecht); `mode`-differenzierte keyterm-Listen (Param ist durchgereicht aber ungenutzt). `profile_faqs` wird in diesem Plan NICHT extrahiert (siehe Persistenz-Schicht-Verifikation: Cap-Budget geht an Branche+Einwand-Kategorien; FAQ-Kategorien sind niedrigere Prioritaet, deferred).
</objective>

<context>
@.planning/phases/08.23.2.STT-deepgram-qualitaet-nova3-keyterm-sprecher-label/08.23.2.STT-RESEARCH.md
@services/deepgram_service.py
@CLAUDE.md

<interfaces>
<!-- Aus dem live services/deepgram_service.py extrahiert. Executor nutzt diese direkt — keine Codebase-Exploration noetig. -->

Aktuelle Signatur (Z.288):
    def _open_deepgram_connection(sid, mode='meeting'):

Aktueller LiveOptions-Build (Z.310-320):
    options_kwargs = dict(
        model="nova-2",          # <- Z.311 -> "nova-3"
        language="de",
        smart_format=True,
        interim_results=True,
        endpointing=900,
        punctuate=True,
        diarize=is_meeting,
        encoding="linear16",
        sample_rate=SAMPLE_RATE,
    )

Aktueller Connection-Open-Call (Z.421, in handle_start_live_session):
    _open_deepgram_connection(_sid, mode=mode)
    # ACHTUNG: laeuft VOR dem Profil-DB-Load (Z.478-509). build_keyterms braucht das Profil -> Reorder noetig.

Profil-Daten-Quellen (nach DB-Load verfuegbar, Z.486-493):
    _profile_daten2  : dict  (json.loads von Profile.daten)
    # branche steht NICHT in _profile_daten2 garantiert -> DB-Column Profile.branche (kanonisch)
    # Profil-Schema (research-korrigiert):
    #   _profile_daten2['basis']['produktbeschreibung']  -> str (Beschreibungs-Text)
    #   _profile_daten2['basis']['unternehmen']          -> str (Markenname)
    #   _profile_daten2['basis']['branche']              -> str (JSON-Fallback fuer Branche)
    #   _profile_daten2['einwaende_detail']              -> List[dict] mit keys 'kategorie','kurzlabel' (schema v4, top-level)

Cost-Hook (Z.344, in _close_deepgram_connection):
    log_api_cost('deepgram', 'nova-2', user_id=None, ...)  # <- 'nova-2' -> 'nova-3'

[DG]-Log (Z.324):
    print(f"[DG] LiveOptions: model=nova-2, diarize={is_meeting}, smart_format=True")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_keyterms()-Helper + feste Sales-Grundliste anlegen</name>
  <read_first>
    - services/deepgram_service.py (gesamte Datei — Helper wird auf Modul-Ebene eingefuegt, z.B. nach _get_speaker Z.43 oder vor _open_deepgram_connection Z.288)
    - .planning/phases/08.23.2.STT-deepgram-qualitaet-nova3-keyterm-sprecher-label/08.23.2.STT-RESEARCH.md Abschnitt "3-Schichten-keyterm-Design" (exakte Grundliste + exakter build_keyterms-Body) und "keyterm LIMIT" (MAX_KEYTERMS=60, 500-Token-Cap)
  </read_first>
  <action>
Modul-Ebene in `services/deepgram_service.py` (oben bei den anderen Modul-Konstanten / Helpern, vor `_open_deepgram_connection`) einfuegen:

1. `import re` ergaenzen (falls nicht vorhanden — pruefen; sonst nicht doppeln).

2. Feste Sales-Grundliste als Modul-Konstante. CLAUDE.md: dies sind DATEN-Strings (keyterm-Werte, vom Modell "gelesen"), daher echte Umlaute erlaubt — KEINE Identifier:

```python
# Phase 08.23.2.STT — keyterm Layer 2: feste Sales-Grundliste.
# DATEN-Strings (keyterm-Werte) -> echte Umlaute erlaubt (CLAUDE.md User-Data-Regel), KEINE Identifier.
_SALES_KEYTERMS_BASE = [
    "Einwand",
    "Einwandbehandlung",
    "Vorwand",
    "Cold Call",
    "Kaltakquise",
    "Kaufsignal",
    "Kalendereinladung",
    "Vertriebler",
    "Opener",
    "Entscheider",
    "Gatekeeper",
    "Abschluss",
    "Termin",
    "Bedarfsanalyse",
    "Angebot",
    "Nachfassen",
]

MAX_KEYTERMS = 60          # << 500-Token-Cap (Deepgram) mit grossem Sicherheitsabstand
_KEYTERM_MIN_LEN = 4
_STOPWORDS = {"oder", "und", "der", "die", "das", "fuer", "mit", "von", "den", "ein", "eine"}
```

3. `build_keyterms()` exakt wie in RESEARCH.md "Layer 3 — build_keyterms() Design" (Z.182-228) implementieren. Wesentliche Eigenschaften (verbindlich):
   - Signatur: `def build_keyterms(profile_daten: dict, profile_branche: str, mode: str) -> list:`
   - Innere `_add(t)`-Funktion: strip, drop wenn `len < _KEYTERM_MIN_LEN`, lowercase-dedup gegen `seen`-Set, drop wenn in `_STOPWORDS`, hard-stop bei `len(terms) >= MAX_KEYTERMS`.
   - Layer 2 ZUERST einfuegen (Grundliste gewinnt strukturell bei Cap).
   - Layer 3.1 Branche: `_add(profile_branche or basis.get("branche") or "")`.
   - Layer 3.2 Unternehmen/Marke: `_add(basis.get("unternehmen") or "")`.
   - Layer 3.3 Produktbeschreibung-Substantive: `re.findall(r"\b[A-ZÄÖÜ][\wäöüß\-]{3,}\b", produkt)[:10]` -> `_add` je Token.
   - Layer 3.4 Einwand-Kategorien+Kurzlabels: ueber `profile_daten.get("einwaende_detail") or []`[:20], je dict `_add(e.get("kategorie"))` + `_add(e.get("kurzlabel"))`.
   - Defensiv: `if not isinstance(profile_daten, dict): profile_daten = {}`; `basis = ... if isinstance(...dict) else {}`. KEINE Exceptions bei fehlenden Keys.
   - Docstring-Hinweis: "Niemals persistiert — nur Transkriptions-Hint (DSGVO)."
   - `mode`-Param wird in Stufe 1 NICHT im Body genutzt (bewusst durchgereicht fuer kuenftige Differenzierung).

KEINE `profile_skripte`- und KEINE `profile_faqs`-Extraktion in diesem Helper (Stufe-1-Scope, siehe Persistenz-Schicht-Verifikation).
  </action>
  <acceptance_criteria>
    - `services/deepgram_service.py` enthaelt `def build_keyterms(` mit Parametern `profile_daten`, `profile_branche`, `mode`.
    - Enthaelt `_SALES_KEYTERMS_BASE = [` mit allen 16 Grundlisten-Termen (inkl. echte Umlaute wo vorhanden — hier ASCII, aber Datei bleibt UTF-8).
    - Enthaelt `MAX_KEYTERMS = 60`.
    - Enthaelt die Cap-Guard-Zeile `if len(terms) >= MAX_KEYTERMS:` und lowercase-Dedup (`seen`-Set).
    - Enthaelt `einwaende_detail` (top-level Lese-Pfad), `produktbeschreibung`, `unternehmen` als Lese-Keys.
    - Enthaelt KEINE `profile_skripte`- und KEINE `profile_faqs`-Referenz im Helper-Body.
    - `import re` ist im Modul vorhanden.
  </acceptance_criteria>
  <done>build_keyterms() existiert, baut Grundliste-zuerst-dann-Profil, dedupt case-insensitive, capped bei MAX_KEYTERMS, ist gegen leere/fehlende Profile defensiv.</done>
</task>

<task type="auto">
  <name>Task 2: nova-3 + keyterm in _open_deepgram_connection verdrahten (Signatur, LiveOptions, Cost-Tag, Log)</name>
  <read_first>
    - services/deepgram_service.py Z.288-330 (_open_deepgram_connection: Signatur, LiveOptions-Build, [DG]-Log, _session_modes-Set)
    - services/deepgram_service.py Z.333-348 (_close_deepgram_connection: log_api_cost-Hook Z.344)
    - .planning/phases/08.23.2.STT-deepgram-qualitaet-nova3-keyterm-sprecher-label/08.23.2.STT-RESEARCH.md Abschnitt "context7 keyterm-Syntax" (keyterm = Liste von Strings, jeder String 1 (ggf. mehrwortiger) Term; SDK serialisiert zu wiederholten Query-Params) + Assumption A1
  </read_first>
  <action>
Alle Edits in `services/deepgram_service.py`:

1. **Signatur erweitern** (Z.288):
   `def _open_deepgram_connection(sid, mode='meeting'):`
   -> `def _open_deepgram_connection(sid, mode='meeting', keyterms=None):`

2. **Model-Swap** (Z.311) im `options_kwargs = dict(...)`:
   `model="nova-2",` -> `model="nova-3",`

3. **keyterm uebergeben**: NACH dem `if is_meeting: options_kwargs['utterance_end_ms'] = "1000"`-Block (Z.321-322) und VOR `options = LiveOptions(**options_kwargs)` (Z.323) einfuegen:
```python
if keyterms:
    # keyterm ist nova-3-only (siehe RESEARCH context7). Liste von Strings;
    # jeder String = 1 (ggf. mehrwortiger) keyterm. SDK serialisiert zu wiederholten Query-Params.
    options_kwargs['keyterm'] = keyterms
```
   `keyterm` (singular, Live-WS-Pfad) — NICHT `keyterms` (das ist Agent/Flux-Config). KEIN `keywords` setzen (war nie gesetzt, schwaecheres nova-2-Feature).

4. **[DG]-Log umstellen** (Z.324) — NUR Anzahl loggen, NIE die keyterm-Werte (DSGVO):
   `print(f"[DG] LiveOptions: model=nova-2, diarize={is_meeting}, smart_format=True")`
   ->
   `print(f"[DG] LiveOptions: model=nova-3, diarize={is_meeting}, smart_format=True, keyterm_count={len(keyterms) if keyterms else 0}")`

5. **Cost-Tag** (Z.344 in `_close_deepgram_connection`):
   `log_api_cost('deepgram', 'nova-2', user_id=None,` -> `log_api_cost('deepgram', 'nova-3', user_id=None,`

KEINE weiteren Aenderungen an _open_deepgram_connection (diarize-Logik, Event-Wiring bleibt unveraendert — die Cold-Call-Label-Wiring ist Plan 02).
  </action>
  <acceptance_criteria>
    - `services/deepgram_service.py` enthaelt `def _open_deepgram_connection(sid, mode='meeting', keyterms=None):`.
    - Enthaelt `model="nova-3",` und enthaelt KEIN `model="nova-2"` mehr.
    - Enthaelt `options_kwargs['keyterm'] = keyterms` (singular `keyterm`, NICHT `keyterms`-Key; KEIN `keywords`-Key).
    - Enthaelt `keyterm_count=` im [DG]-LiveOptions-Print; das Print enthaelt `model=nova-3`.
    - log_api_cost taggt `'nova-3'` — KEIN `'nova-2'` mehr in der Datei (weder Model-Kwarg noch Cost-Tag noch Log-String).
  </acceptance_criteria>
  <done>nova-3 ist Live-Model; keyterm wird (wenn uebergeben) in LiveOptions gesetzt; Log+Cost-Tag konsistent auf nova-3.</done>
</task>

<task type="auto">
  <name>Task 3: Reorder — Profil-daten+branche VOR _open_deepgram_connection laden und keyterms durchreichen</name>
  <read_first>
    - services/deepgram_service.py Z.407-559 (handle_start_live_session — gesamter Handler: mode-Bestimmung Z.415-419, Connection-Open Z.421, Profil-DB-Load Z.478-509, _load_profile_cache Z.520)
    - .planning/phases/08.23.2.STT-deepgram-qualitaet-nova3-keyterm-sprecher-label/08.23.2.STT-RESEARCH.md Abschnitt "Wiring-Detail" (Reorder-Falle: Z.421 VOR Z.478) + "Anti-Patterns / Landmines" (Reorder-Falle) + "Persistenz-Schicht-Verifikation" (Profile.branche DB-Column)
  </read_first>
  <action>
**Riskantester Edit dieses Plans — sorgfaeltig.** Problem: `_open_deepgram_connection(_sid, mode=mode)` laeuft Z.421 BEVOR das Profil geladen ist (Z.478-509). `build_keyterms` braucht aber `daten`+`branche`. Loesung: einen **Mini-Profil-Load (nur `daten` + `branche`) VOR den Connection-Open vorziehen**, keyterms bauen, durchreichen. Den bestehenden vollen Profil-Load-Block (Z.478-509) UNVERAENDERT lassen (er macht init_session_state, set_profile_for_sid, call-record etc. — NICHT anfassen, nur der keyterm-Mini-Load kommt zusaetzlich davor).

Konkret, in `handle_start_live_session`:

1. Nach der `mode`-Bestimmung (nach Z.420 `print(f"[DG] start_live_session received ...")`) und VOR `_open_deepgram_connection(_sid, mode=mode)` (Z.421) einen Mini-Load + build_keyterms einfuegen:

```python
# Phase 08.23.2.STT: keyterm-Liste VOR Connection-Open ableiten (keyterm ist nova-3-only,
# muss beim LiveOptions-Build vorliegen). Mini-Profil-Load (nur daten+branche) — der volle
# per-SID-Init unten (init_session_state/set_profile_for_sid/call-record) bleibt unveraendert.
_kt_daten = {}
_kt_branche = ''
try:
    from flask import session as _kt_flask_session
    _kt_user_id = _kt_flask_session.get('user_id')
    from database.db import SessionLocal as _SL_kt
    from database.models import User as _User_kt, Profile as _Profile_kt
    _db_kt = _SL_kt()
    try:
        _u_kt = _db_kt.query(_User_kt).filter_by(id=_kt_user_id).first()
        _pid_kt = getattr(_u_kt, 'active_profile_id', None) if _u_kt else None
        if _pid_kt:
            _p_kt = _db_kt.query(_Profile_kt).filter_by(id=_pid_kt).first()
            if _p_kt:
                _kt_branche = getattr(_p_kt, 'branche', None) or ''   # DB-Column-Prioritaet (kanonisch seit 08.19.1)
                if _p_kt.daten:
                    import json as _json_kt
                    _kt_daten = _json_kt.loads(_p_kt.daten) if isinstance(_p_kt.daten, str) else (_p_kt.daten or {})
    finally:
        _db_kt.close()
except Exception as _kt_e:
    print(f"[DG] keyterm-Profil-Load fehlgeschlagen (non-fatal, fallback Grundliste): {_kt_e}")
_keyterms = build_keyterms(_kt_daten, _kt_branche, mode)
```

2. Den Connection-Open-Call (Z.421) erweitern:
   `_open_deepgram_connection(_sid, mode=mode)`
   -> `_open_deepgram_connection(_sid, mode=mode, keyterms=_keyterms)`

**Wichtig — NICHT brechen:**
- Der bestehende Profil-DB-Load-Block Z.478-509 (`_profile_daten2`, `init_session_state`, `set_profile_for_sid`, `_load_profile_cache`, call-record) bleibt **vollstaendig unveraendert**. Der Mini-Load ist additiv und nur fuer keyterms.
- Bei Fehler/leerem Profil ist `_keyterms` mind. die Grundliste (build_keyterms gibt sie immer zurueck) — Call startet trotzdem.
- `import json` ist lokal als `_json_kt` aliassiert (vermeidet Kollision mit dem `_json2` im Block unten).
  </action>
  <acceptance_criteria>
    - `services/deepgram_service.py` enthaelt `_keyterms = build_keyterms(` VOR dem `_open_deepgram_connection(`-Aufruf in handle_start_live_session (Reihenfolge: build_keyterms-Zeile erscheint im File vor der _open_deepgram_connection-Zeile).
    - `_open_deepgram_connection(_sid, mode=mode, keyterms=_keyterms)` ist der Aufruf-Pattern.
    - Der Mini-Load liest `getattr(_p_kt, 'branche', ...)` (DB-Column) und `_p_kt.daten` (JSON).
    - Der bestehende Block `init_session_state(` / `set_profile_for_sid(` / `_load_profile_cache(` ist weiterhin vorhanden und unveraendert (grep zeigt sie noch).
    - Mini-Load ist in try/except gewrappt (`keyterm-Profil-Load fehlgeschlagen` Fallback-Print vorhanden).
  </acceptance_criteria>
  <done>keyterms werden aus dem aktiven Profil abgeleitet und in die Deepgram-Connection gereicht; der bestehende per-SID-Init-Pfad ist intakt; leeres Profil bricht den Call nicht.</done>
</task>

</tasks>

## Persistenz-Schicht-Verifikation (Punkt 21/22)

> Dieser Plan LIEST Profil-Daten (fuer keyterm-Ableitung). Schreibt KEINE Daten (keyterm = Transkriptions-Hint, NICHT persistiert). Beleg-Quelle: RESEARCH.md "Verbindungs-Karte / Persistenz-Schicht-Verifikation" mit grep file:line.

### Angefasste Datenquellen (nur lesend)

| Code-Feld | Lese-Pfad | Persistenz-Schicht | Verifiziert (RESEARCH grep) |
|---|---|---|---|
| `Profile.branche` | Mini-Load `getattr(_p_kt,'branche')` | DB-Column `Profile.branche` (`Column(String(200))`) — kanonisch seit 08.19.1 | ✓ `database/models.py:140` |
| `daten['basis']['unternehmen']` | build_keyterms Layer 3.2 | JSON in `Profile.daten` (`Column(Text)`) | ✓ `services/profile_schema.py:52` |
| `daten['basis']['produktbeschreibung']` | build_keyterms Layer 3.3 | JSON in `Profile.daten` | ✓ `profile_schema.py:53`, gelesen `prompt_pipeline.py:263` |
| `daten['basis']['branche']` (Fallback) | build_keyterms Layer 3.1 | JSON in `Profile.daten` | ✓ `profile_schema.py:54` |
| `daten['einwaende_detail'][].kategorie/.kurzlabel` | build_keyterms Layer 3.4 | JSON top-level in `Profile.daten` (schema v4) | ✓ `profile_schema.py:152`, Felder `:93-103` |
| keyterm-Liste (Output) | -> `LiveOptions(keyterm=...)` | KEINE Persistenz — Request-Query an Deepgram-EU + Log-Count | ✓ kein DB-Write |

### Bewusst NICHT genutzt (ROADMAP-Annahmen waren falsch / Cap-Budget-Entscheidung)

| Quelle | Grund | Beleg |
|---|---|---|
| `Produktname` | Existiert NICHT (in v3->v4 nach `basis.produktbeschreibung` gemerged + gedroppt) | `profile_schema.py:156`, `:341-350` |
| `einwaende` (top-level, alt) | Gedroppt; kanonisch ist `einwaende_detail` | `profile_schema.py:152` |
| `profile_skripte.inhalt` | Eigene DB-Tabelle, NICHT im Session-Cache; Fliesstext-Skripte (Aufwand/Nutzen schlecht) — Stufe-1 SKIP | `models.py:148-158`, `live_session.py:534-585` |
| `profile_faqs.kategorie` | Eigene DB-Tabelle; nur via `_load_profile_cache` als `faqs`-Cache verfuegbar — der keyterm-Mini-Load laeuft VOR dem Cache-Build, ein zusaetzlicher DB-Roundtrip lohnt fuer Stufe 1 nicht. Cap-Budget geht an Branche+Einwand-Kategorien. **DEFERRED** (Stufe 2 / Backlog falls keyterm-Hit-Rate zu niedrig). | `models.py:163-172`, `live_session.py:552-568` |

**Persistenz-Aussage (DSGVO):** Die keyterm-Liste verlaesst den Server ausschliesslich Richtung Deepgram-EU (`api.eu.deepgram.com`, Z.289-293) als Request-Query. Sie wird NICHT in DB, RAM (ueber Connection-Lifetime hinaus) oder Logs (nur `keyterm_count`, NIE Werte) geschrieben. Profil-Daten verlassen den Server ohnehin schon Richtung Deepgram (Audio); keyterm fuegt nur Vokabular-Hints hinzu, keine neue personenbezogene Datenkategorie. Branche/Unternehmensname stammen aus dem **eigenen Profil des Beraters**, nicht des Leads -> kein Lead-PII.

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| NERVE-Server -> Deepgram-EU (api.eu.deepgram.com) | Audio + (neu) keyterm-Vokabular-Hints verlassen den Server. Bereits bestehende Grenze (Audio floss schon). |
| Profile.daten / Profile.branche (DB) -> build_keyterms | Eigene Berater-Profildaten, vom Berater selbst gepflegt. Kein Lead-PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-STT-01 | Information Disclosure | keyterm-Liste -> Deepgram-EU | accept | keyterm = Berater-eigenes Vokabular (Branche/Marke/Sales-Begriffe), KEIN Lead-PII. Gleicher Processor der bereits das Roh-Audio erhaelt -> keine NEUE Daten-Egress-Klasse. EU-Host (DSGVO). Cross-AI-Frage: OK Branche/Unternehmensname des Beraters an Deepgram-EU zu senden? (Erwartung: ja.) |
| T-STT-02 | Information Disclosure | [DG]-Log keyterm | mitigate | Log schreibt NUR `keyterm_count=N`, NIE die keyterm-Werte. Verifiziert in Task 2 acceptance. |
| T-STT-03 | Denial of Service | keyterm 500-Token-Cap | mitigate | `MAX_KEYTERMS=60` hard-cap + `_KEYTERM_MIN_LEN`-Filter. Ohne Cap koennte ein grosses Profil den Request sprengen -> Deepgram-Error -> stiller STT-Ausfall. Cap haelt weit unter 500 Tokens. |
| T-STT-04 | Tampering | Profil-Inhalt als keyterm | accept | Profil ist Berater-eigene Eingabe (kein fremder Input). Capitalized-Token-Heuristik + Stopwords + Dedup begrenzen Muell; schlimmstenfalls suboptimale (harmlose) keyterms. |

Keine neuen High-Severity-Threats. Anonymizer-Pfad (INPUT, Z.91-126 -> anonymize()) bleibt unveraendert.
</threat_model>

<verify>
> Production-only (CLAUDE.md HART). KEIN lokales pytest/server/git-pull. Source-Grep/Code-Lesung lokal als Acceptance erlaubt; Runtime nur auf Production.

  <manual>Source-Grep lokal (Code-Lesung): `build_keyterms` existiert, `model="nova-3"` gesetzt, KEIN `nova-2` mehr in der Datei, `options_kwargs['keyterm']` vorhanden, `_keyterms = build_keyterms(` steht VOR `_open_deepgram_connection(`, log_api_cost taggt 'nova-3'.</manual>
  <manual>Deploy: `bash deploy.sh production` — eingebautes Test-Gate laeuft auf Prod-Server vor Restart. HINWEIS: das Gate enthaelt einen pre-existing-roten `test_ft_seed` — Code-Deploys landen dennoch (bekanntes Pre-existing-Failure, blockiert nicht). Bei NEUEN Test-Fehlern: kein Restart, alter Code bleibt live.</manual>
  <manual>Andre: 1x cold_call + 1x meeting Test-Call mit Test-User (befuelltes Profil).</manual>
  <manual>Claudian zieht [DG]-Roh-Logs:
    `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs'`
    Soll-Ist:
    - Log-Zeile `[DG] LiveOptions: model=nova-3 ... keyterm_count=N` mit N>15 (Grundliste 16 + Profil) bei befuelltem Profil.
    - **A1-Check (MEDIUM):** Erstes `[DG]`-Log + Deepgram-Verhalten bestaetigen dass die keyterm-Liste als wiederholte Query-Params serialisiert wird, NICHT als ein CSV-Blob (sonst brechen mehrwortige Terms wie "Cold Call"). Falls CSV: pro Term einzeln/Tupel nachsteuern.
    - Soll-Ist der `[DG] [Berater] ...`-Zeilen gegen bekannten Pitch: "Einwaende", "Cold Call", Markenname, "Vertriebler" jetzt korrekt erkannt (vorher: "ein, wenn"/"Callcalls"/Lautmalerei/"Fahrradbetreiber").</manual>
  <manual>Bei Bedarf Profil-Real-Daten gegen Prod: `inspect.sh sample profiles 5` / `inspect.sh schema profiles` (bestaetigt schema v4 / einwaende_detail vorhanden — Assumption A2).</manual>
</verify>

<success_criteria>
- nova-3 ist Live-Model fuer ALLE Calls (cold_call + meeting); kein nova-2 mehr im Live-Pfad.
- keyterm-Liste wird pro Call aus Grundliste + aktivem Profil gebaut und in die Connection gereicht (keyterm_count>15 bei befuelltem Profil sichtbar im [DG]-Log).
- Leeres/fehlendes Profil bricht den Call nicht (mind. Grundliste).
- keyterm-Werte werden nirgends persistiert/geloggt (nur Count).
- Cost-Tag = 'nova-3'.
- Bestehender per-SID-Init-Pfad (init_session_state/set_profile_for_sid/call-record/Anonymizer) unveraendert intakt.
</success_criteria>

<output>
Nach Abschluss SUMMARY anlegen: `.planning/phases/08.23.2.STT-deepgram-qualitaet-nova3-keyterm-sprecher-label/08.23.2.STT-01-SUMMARY.md`
</output>
