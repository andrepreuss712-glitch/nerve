---
phase: 08.23.2.STT
plan: 01
subsystem: live-stt
tags: [deepgram, nova-3, keyterm, stt-quality, dsgvo]
requires:
  - services/deepgram_service.py (handle_start_live_session, _open_deepgram_connection)
  - database/models.py (Profile.branche, Profile.daten)
provides:
  - build_keyterms() Helper (Layer-2-Grundliste + Layer-3-Profil-Extraktion)
  - nova-3 als Live-Model fuer alle Calls (cold_call + meeting)
  - keyterm-Fachwort-Liste in LiveOptions mit SDK-Mismatch-Fallback
affects:
  - Live-STT-Erkennungsqualitaet (Vertriebs-Fachbegriffe + Profil-Marken/Branche)
tech-stack:
  added: []
  patterns:
    - "try/except-Fallback um LiveOptions(keyterm=...) gegen strikte SDK-Signatur"
    - "additiver Mini-Profil-Load VOR Connection-Open (Reorder ohne Full-Load-Aenderung)"
    - "lowercase-Dedup + MAX_KEYTERMS-Cap fuer 500-Token-Schutz"
key-files:
  created: []
  modified:
    - services/deepgram_service.py
decisions:
  - "nova-2 -> nova-3 Pflicht-Swap im selben Change (keyterm ist nova-3-only)"
  - "keyterm-Werte werden NIE geloggt/persistiert — nur keyterm_count (DSGVO)"
  - "Komposita-Split (Sales Flow -> Sales+Flow) als Stufe-1-Limitation akzeptiert, nicht gefixt"
  - "DB-Session-in-SocketIO-Smell beim Mini-Load akzeptiert (1x pro Call-Start, gleiches Pattern wie Full-Load)"
metrics:
  duration: ~10min
  tasks: 3
  files: 1
  completed: 2026-06-05
---

# Phase 08.23.2.STT Plan 01: Deepgram-Qualitaet (nova-3 + keyterm) Summary

Live-STT-Qualitaet angehoben durch nova-2->nova-3 Model-Swap und eine 3-Schichten keyterm-Fachwort-Liste (feste Sales-Grundliste + per-Call Profil-Extraktion), verdrahtet in `services/deepgram_service.py` mit SDK-Mismatch-Fallback und Reorder des Profil-Loads vor den Connection-Open.

## Was umgesetzt wurde

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | build_keyterms()-Helper + feste Sales-Grundliste | 3705664 | services/deepgram_service.py |
| 2 | nova-3 + keyterm in _open_deepgram_connection (Signatur, LiveOptions, SDK-Fallback, Cost-Tag, Log) | 4e86798 | services/deepgram_service.py |
| 3 | Reorder — Profil-daten+branche VOR Connection-Open, keyterms durchreichen | dce96d7 | services/deepgram_service.py |

### Task 1
- `import re` ergaenzt (war nicht vorhanden).
- `_SALES_KEYTERMS_BASE` (16 feste Sales-Terms), `MAX_KEYTERMS=60`, `_KEYTERM_MIN_LEN=4`, `_STOPWORDS` als Modul-Konstanten vor `_open_deepgram_connection`.
- `build_keyterms(profile_daten, profile_branche, mode)`: Grundliste zuerst (gewinnt bei Cap), dann Layer-3 (Branche, Unternehmen, Produktbeschreibung-Substantive via Capitalized-Token-Regex, einwaende_detail kategorie/kurzlabel). Lowercase-Dedup via `seen`-Set, `MAX_KEYTERMS`-Cap-Guard, defensiv gegen Nicht-Dict/fehlende Keys. Keine profile_skripte/profile_faqs-Referenz. Komposita-Limitation als Kommentar ueber der Regex dokumentiert (nicht gefixt, REVIEW LOW).

### Task 2
- Signatur `_open_deepgram_connection(sid, mode='meeting', keyterms=None)`.
- `model="nova-3"` (kein `nova-2` mehr in der Datei).
- `options_kwargs['keyterm'] = keyterms` (singular, Live-WS-Pfad) wenn uebergeben.
- **REVIEW HIGH:** try/except um `LiveOptions(**options_kwargs)` — bei SDK-Mismatch `options_kwargs.pop('keyterm', None)` + retry ohne keyterm (kein harter Crash, nova-3 ueberlebt) + `[DG] LiveOptions init mit keyterm fehlgeschlagen`-Print.
- `[DG]`-Log auf `model=nova-3` + `keyterm_count=` (liest aus moeglicherweise gepopptem `options_kwargs`, zeigt 0 bei Fallback; nur Anzahl, nie Werte).
- Cost-Tag `log_api_cost('deepgram', 'nova-3', ...)`.

### Task 3
- Additiver Mini-Profil-Load (nur daten+branche) zwischen `start_live_session received`-Print und `_open_deepgram_connection`-Aufruf.
- `branche` via DB-Column `getattr(_p_kt, 'branche', ...)`, `daten` via JSON-Parse mit `_json_kt`-Alias (keine Kollision mit `_json2` im Full-Load).
- try/except non-fatal: bei Fehler/leerem Profil/fehlendem Request-Context Fallback auf Grundliste, Call startet trotzdem.
- `_open_deepgram_connection(_sid, mode=mode, keyterms=_keyterms)`.
- REVIEW-MEDIUM-Akzeptanz-Kommentar (DB-Session-Smell, 1x pro Call-Start, kein NEUES Architektur-Risiko) vorhanden.
- Bestehender Full-Load-Block (init_session_state/set_profile_for_sid/_load_profile_cache/call-record) vollstaendig unveraendert (verifiziert via grep: Zeilen 622/632/643 intakt, Reihenfolge build_keyterms@542 + open@544 vor Full-Load).

## Deviations from Plan

None - alle 3 Tasks wurden exakt nach Plan-Action-Text ausgefuehrt. `_make_on_message` blieb unangetastet (gehoert Plan 02).

## Threat Surface / DSGVO

Keine neuen Threat-Flags. keyterm-Werte verlassen den Server ausschliesslich Richtung Deepgram-EU als Request-Query (gleicher Processor der bereits Audio erhaelt), werden NICHT in DB/Logs/RAM ueber Connection-Lifetime hinaus geschrieben (nur `keyterm_count`). T-STT-02 (Log-Count statt Werte), T-STT-03 (MAX_KEYTERMS-Cap), T-STT-07 (SDK-Fallback) alle implementiert.

## Known Stubs

Keine. keyterm-Liste ist live aus Grundliste + aktivem Profil gebaut und an die Connection durchgereicht.

## Verifikation (Production-only, NICHT lokal ausgefuehrt)

Source-Grep lokal bestaetigt:
- `def build_keyterms(` mit Parametern profile_daten/profile_branche/mode vorhanden.
- `model="nova-3"` gesetzt, KEIN `nova-2` mehr (grep: 0 Treffer).
- `options_kwargs['keyterm'] = keyterms` + try/except mit `options_kwargs.pop('keyterm', None)` + retry vorhanden.
- `_keyterms = build_keyterms(` (Z.542) steht VOR `_open_deepgram_connection(...keyterms=_keyterms)` (Z.544).
- `log_api_cost('deepgram', 'nova-3', ...)`.
- Full-Load-Block unveraendert (init_session_state/set_profile_for_sid/_load_profile_cache intakt).

Runtime-Verifikation (Deploy `bash deploy.sh production`, Test-Calls, `inspect.sh logs` Soll-Ist, SDK-Fallback-Check) erfolgt durch André/Orchestrator gemaess `<verify>`-Sektion — NICHT in diesem Executor-Lauf.

## Self-Check: PASSED

- Datei `services/deepgram_service.py` existiert und enthaelt alle erwarteten Aenderungen (grep-verifiziert).
- Commits 3705664, 4e86798, dce96d7 vorhanden (git log bestaetigt unten).
- `nova-2`-Grep: 0 Treffer.
- Ordering build_keyterms vor _open_deepgram_connection: bestaetigt.
