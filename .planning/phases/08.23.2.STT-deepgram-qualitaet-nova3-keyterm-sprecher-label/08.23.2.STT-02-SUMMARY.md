---
phase: 08.23.2.STT
plan: 02
subsystem: stt
tags: [deepgram, cold-call, speaker-label, closure-capture]
requires:
  - 01   # services/deepgram_service.py (nova-3, build_keyterms) — gleiche Datei, sequenziell
provides:
  - "_make_on_message(sid, mode) mit cold_call-Hard-Label (speaker=0/Berater)"
  - "Cold-Call emit + persist + sp_name konsistent Berater statt Unbekannt/None"
affects:
  - services/deepgram_service.py
tech-stack:
  added: []
  patterns:
    - "Closure-Capture von mode (call-konstant) statt Hot-Path-Dict-Lookup"
key-files:
  created: []
  modified:
    - services/deepgram_service.py
decisions:
  - "mode via Closure-Capture verdrahtet (kein Dict-Lookup im Hot-Path; T-STT-05 accept)"
  - "cold_call hart speaker=0/roles_confirmed=True -> emit UND persist konsistent 0 (Single-Speaker by design)"
  - "Meeting-Pfad (diarize=is_meeting + roles_confirmed-Heuristik) strikt unveraendert"
metrics:
  duration: "~10 min"
  completed: 2026-06-05
  tasks: 2
  files: 1
---

# Phase 08.23.2.STT Plan 02: Cold-Call Sprecher-Label-Fix Summary

Cold-Call-Transkripte zeigen jetzt korrekt den Berater (speaker=0) statt "Unbekannt"/SYSTEM — `_make_on_message` kennt den `mode` via Closure-Capture und setzt im `cold_call`-Pfad emit, persist und `sp_name` hart auf Berater; der Meeting-Pfad bleibt strikt unveraendert.

## Was umgesetzt wurde

### Task 1 — mode in `_make_on_message` verdrahten (Closure-Capture)
- Signatur `def _make_on_message(sid):` -> `def _make_on_message(sid, mode='meeting'):` (L47).
- Wiring `connection.on(LiveTranscriptionEvents.Transcript, _make_on_message(sid))` -> `_make_on_message(sid, mode)` (L376), `mode` als Parameter von `_open_deepgram_connection` verfuegbar.
- `mode` landet als Closure-Variable; die innere `on_message` liest sie direkt — kein Hot-Path-Dict-Lookup (T-STT-05 accept).
- Commit: `691b421`

### Task 2 — cold_call-Hard-Label im is_final-Zweig + sp_name-Konsistenz
- Label-Block im is_final-Pfad: `if mode == 'cold_call':` vorangestellt (L79) — setzt `sp_label='Berater'`, `emit_speaker=0`, `log_sp=0`, `roles_confirmed=True`. Reihenfolge `cold_call -> elif roles_confirmed -> else`.
- `log_sp`/`roles_confirmed` werden im cold_call-Zweig hart ueberschrieben -> Persist-Zeile `'speaker': log_sp if roles_confirmed else None,` (L123, unveraendert) schreibt nun `speaker=0` statt `None`.
- sp_name-Block: `if mode == 'cold_call': sp_name = 'Berater'` vorangestellt (L175-176), dann `elif roles_confirmed` / `else`.
- Meeting-Zweige (`'Kunde' if log_sp == 1`, `emit_speaker = speaker`, sp_name-Heuristik) und `diarize=is_meeting` (L405) strikt unveraendert.
- Commit: `01ce28f`

## Cross-Layer-Konsistenz (Cold-Call)

| Wert | Vorher | Nachher | Schicht |
|---|---|---|---|
| emit `speaker` | None (-> Frontend SYSTEM) | 0 (-> Frontend Berater) | Socket-Event (kein DB-Write) |
| persist `speaker` | None | 0 (`roles_confirmed=True` + `log_sp=0`) | RAM-Liste `ls.conversation_log` |
| `sp_name` (merge) | 'Sprecher' | 'Berater' | RAM merge-queue |

Kein Schema-Change, keine neue Spalte, kein neuer DB-Write-Pfad — nur der Wert des bestehenden `speaker`-Feldes aendert sich.

## Deviations from Plan

None - plan executed exactly as written. Absolute Zeilennummern aus dem Plan-`<interfaces>`-Block waren durch Plan 01 nach unten verschoben; Code wurde wie instruiert ueber Content-Anker (nicht Zeilennummer) lokalisiert. Plan 01's Additions (build_keyterms, nova-3, keyterm-Reorder) wurden NICHT angefasst.

## Verification (Source-Grep, lokal — kein Local-Exec)

- `def _make_on_message(sid, mode='meeting'):` (L47) + Wiring `_make_on_message(sid, mode)` (L376); kein verbleibender `_make_on_message(sid)`-Aufruf ohne mode.
- `if mode == 'cold_call':` (L79) mit `emit_speaker    = 0` (L82), `log_sp          = 0` (L83), `roles_confirmed = True` (L84); Reihenfolge cold_call -> elif roles_confirmed -> else.
- sp_name cold_call-Zweig `sp_name = 'Berater'` (L175-176) als erster Branch.
- Persist-Zeile `'speaker': log_sp if roles_confirmed else None,` (L123) unveraendert vorhanden.
- Meeting-Zweige (`'Kunde' if log_sp == 1`, `emit_speaker = speaker`) + `diarize=is_meeting` (L405) unveraendert.

Production-Verify (deploy.sh production, cold_call + meeting Test-Call, inspect.sh logs/sample) erfolgt durch André/Orchestrator (CLAUDE.md HART: kein Local-Exec).

## Self-Check: PASSED

- FOUND: services/deepgram_service.py (modified)
- FOUND commit: 691b421 (Task 1)
- FOUND commit: 01ce28f (Task 2)
- FOUND: 08.23.2.STT-02-SUMMARY.md
