---
phase: 08.19.2
plan: 04
type: execute
wave: 4
depends_on:
  - 08.19.2-03
files_modified:
  - templates/profile_editor.html
autonomous: false
requirements: []
must_haves:
  truths:
    - "Each of the 6 sections has a .sec-hint paragraph with the correct text from UI-SPEC Copywriting Contract"
    - "Key fields have .field-desc elements with help text visible below labels"
    - "EWB-preview placeholder notice visible in relevant sections (Kaufsignale, Übergänge in sec-live-signale; Verkaufstechniken in sec-einwaende-antworten)"
    - "Auto-save indicator visible top-right OR existing Save button retained — one clear save mechanism"
    - "Profile editor renders correctly with 17px section headings, no uppercase sec-title"
    - "No JS console errors on page load"
  artifacts:
    - path: "templates/profile_editor.html"
      provides: "Education layer (sec-hint, field-desc, EWB placeholders)"
      contains: "sec-hint"
  key_links:
    - from: "sec-hint paragraphs"
      to: ".sec-hint CSS class"
      via: "nerve.css .sec-hint rule (Plan 01)"
      pattern: "class=\"sec-hint\""
---

<objective>
Add the education layer: per-section .sec-hint hints, per-field .field-desc inline descriptions for key fields, EWB-preview placeholder notices per D-07, and a human verification checkpoint.

Purpose: Implements D-06 (2-level inline help), D-07 (EWB placeholder per section), completing the copywriting contract from UI-SPEC.md. This is the final plan before the phase is verifiable.

Output: All 6 sections have .sec-hint hints. Key fields have .field-desc descriptions. EWB placeholder notices in sections where fields are not yet in the EWB-prompt pipeline. Human verifies the full editor visually.
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/dev/salesnerve/.planning/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- Section hint texts — exact strings from UI-SPEC.md Copywriting Contract -->
sec-branche-firma:    "Diese Grunddaten fließen in jeden EWB-Kontext ein — je präziser, desto besser passt die KI-Antwort."
sec-zielgruppe-neu:   "NERVE nutzt diese Angaben um die Sprache der KI-Antworten auf deinen typischen Kunden anzupassen."
sec-gespraechsleitfaden: "Opener, Pitch und Phasen helfen NERVE zu erkennen wo du im Gespräch stehst und was als nächstes kommt."
sec-einwaende-antworten: "Diese Einwände werden vom Live-Coach erkannt — bei ähnlichen Formulierungen im Gespräch schlägt die KI das passende Gegenargument vor."
sec-live-signale:     "Kaufsignale und Übergänge: aktuell noch nicht in die Live-Pipeline integriert — kommt in Phase 08.20."
sec-ki-verhalten:     "Steuert Ton und Stil aller KI-Ausgaben in Live-Coaching und EWB-Antworten."

<!-- EWB-preview placeholder text from UI-SPEC.md -->
"Live-Preview der KI-Nutzung kommt in Phase 08.20."
→ Applies to: sec-live-signale (Kaufsignale, Übergänge sub-sections), sec-einwaende-antworten (Verkaufstechniken sub-section)
→ NOT needed in sec-branche-firma, sec-zielgruppe-neu, sec-ki-verhalten, sec-gespraechsleitfaden, sec-einwaende-antworten main Einwände (these ARE in the EWB pipeline)
→ HTML: <span class="sec-hint">Live-Preview der KI-Nutzung kommt in Phase 08.20.</span>

<!-- Section heading structure (Plans 01-03 added placeholder comments) -->
<!-- Plan 02 left: <!-- HINT: Plan 04 will add sec-hint here --> -->
<!-- Replace each comment with the actual <p class="sec-hint"> element -->

<!-- .field-desc elements — which fields need them (D-06 minimum set) -->
Key fields requiring .field-desc (max 1 sentence + optional example):
- vi_branche_select: "Wähle die Branche deiner Kunden — bestimmt Sprache und Referenzen der KI."
- Produktbeschreibung / vi_produkt: "Beschreibe dein Produkt so wie du es einem Neukunden erklären würdest. Beispiel: 'CRM-Software für Vertriebsteams ab 5 Personen.'"
- Zielkunde / vi_zielkunde: "Beschreibe deinen idealen Kunden. Beispiel: 'IT-Leiter in mittelständischen Unternehmen mit 50-250 Mitarbeitern.'"
- Hauptschmerzpunkt / Schmerzpunkte fields: "Die größten Probleme deiner Kunden — NERVE priorisiert Gegenargumente danach."
- Opener-Sammlung label: "Dein bevorzugter Gesprächseinstieg. Mehrere Varianten möglich."
- Erlaubnisfrage list: "Frage die du nach dem Opener stellst um Gesprächsbereitschaft zu sichern."
- Pitch list: "Dein Kern-Pitch — 1-2 Sätze warum dein Produkt relevant ist."
- Einwände header: "Häufigste Kundeneinwände. NERVE erkennt ähnliche Formulierungen und schlägt Gegenargumente vor."
- KI-Ton / vi_ton: "Steuert wie die KI formuliert. 'Direkt' = kurz und präzise. 'Empathisch' = verständnisvoll."

<!-- .field-desc HTML pattern: -->
<p class="field-desc">Help text here.</p>
<!-- Position: directly below the <label> element, before the <input>/<textarea>/<select> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sec-hint to all 6 sections + EWB placeholder notices + key field-desc elements</name>
  <files>templates/profile_editor.html</files>
  <read_first>
    - templates/profile_editor.html (read the full content sections for all 6 groups — understand existing label/field structure before inserting .field-desc elements)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-UI-SPEC.md (Copywriting Contract section — section hints table, EWB placeholder text, field-desc interaction contract)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-RESEARCH.md (Q8 mapping table to understand which sub-sections are inside which group)
  </read_first>
  <action>
    **Edit A — Replace sec-hint placeholder comments with actual sec-hint elements**:
    Plan 02 added `<!-- HINT: Plan 04 will add sec-hint here -->` after each section heading. Replace each with the correct `<p class="sec-hint">` element using the exact text from UI-SPEC.md Copywriting Contract (copied above in interfaces block). Use the exact German text — do not paraphrase or shorten.

    If Plan 02's comments are not present (executor may have added the headings differently), find each section's `<h2 class="sec-title">` element and insert the `<p class="sec-hint">` immediately after it.

    All 6 sections receive a sec-hint paragraph:
    1. sec-branche-firma: "Diese Grunddaten fließen in jeden EWB-Kontext ein — je präziser, desto besser passt die KI-Antwort."
    2. sec-zielgruppe-neu: "NERVE nutzt diese Angaben um die Sprache der KI-Antworten auf deinen typischen Kunden anzupassen."
    3. sec-gespraechsleitfaden: "Opener, Pitch und Phasen helfen NERVE zu erkennen wo du im Gespräch stehst und was als nächstes kommt."
    4. sec-einwaende-antworten: "Diese Einwände werden vom Live-Coach erkannt — bei ähnlichen Formulierungen im Gespräch schlägt die KI das passende Gegenargument vor."
    5. sec-live-signale: "Kaufsignale und Übergänge: aktuell noch nicht in die Live-Pipeline integriert — kommt in Phase 08.20."
    6. sec-ki-verhalten: "Steuert Ton und Stil aller KI-Ausgaben in Live-Coaching und EWB-Antworten."

    **Edit B — Add EWB-preview placeholder notices**:
    In the following sub-sections, add `<span class="sec-hint">Live-Preview der KI-Nutzung kommt in Phase 08.20.</span>` as the first child of the sub-section content (after any sub-heading label):
    - Kaufsignale sub-section (inside sec-live-signale)
    - Übergänge sub-section (inside sec-live-signale)
    - Verkaufstechniken sub-section (inside sec-einwaende-antworten)
    
    Do NOT add to: sec-einwaende-antworten Einwände sub-section, sec-branche-firma, sec-zielgruppe-neu, sec-ki-verhalten, sec-gespraechsleitfaden (these fields are actively used in EWB pipeline).

    **Edit C — Add .field-desc to key fields**:
    For each field in the list below, add `<p class="field-desc">...</p>` immediately after the `<label>` element (before the input/select/textarea). Use the help texts from the interfaces block above:

    1. After the `<label>` for vi_branche_select: "Wähle die Branche deiner Kunden — bestimmt Sprache und Referenzen der KI."
    2. After the `<label>` for the product description textarea (vi_produkt or similar): "Beschreibe dein Produkt so wie du es einem Neukunden erklären würdest. Beispiel: 'CRM-Software für Vertriebsteams ab 5 Personen.'"
    3. After the `<label>` for the Zielkunde field (vi_zielkunde or similar): "Beschreibe deinen idealen Kunden. Beispiel: 'IT-Leiter in mittelständischen Unternehmen mit 50-250 Mitarbeitern.'"
    4. After the `<label>` for Opener-Sammlung: "Dein bevorzugter Gesprächseinstieg. Mehrere Varianten möglich."
    5. After the `<label>` for Erlaubnisfrage: "Frage die du nach dem Opener stellst um Gesprächsbereitschaft zu sichern."
    6. After the `<label>` for Pitch: "Dein Kern-Pitch — 1-2 Sätze warum dein Produkt relevant ist."
    7. After the `<label>` for the Einwände section (block header above the einwand list): "Häufigste Kundeneinwände. NERVE erkennt ähnliche Formulierungen und schlägt Gegenargumente vor."
    8. After the `<label>` for KI-Ton (vi_ton or similar): "Steuert wie die KI formuliert. 'Direkt' = kurz und präzise. 'Empathisch' = verständnisvoll."

    If a field label cannot be found (name different from expected), use your best judgment to find the semantically equivalent field. Do not skip a field — add the .field-desc to the closest matching element.

    IMPORTANT: .field-desc elements go BELOW the label, ABOVE the input. Not after the input.
  </action>
  <verify>
    <automated>grep -c "class=\"sec-hint\"" C:/Users/andre/dev/salesnerve/templates/profile_editor.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "class=\"sec-hint\"" returns at least 9 (6 section hints + 3 EWB placeholder notices)
    - grep -c "class=\"field-desc\"" returns at least 8 (one per key field listed above)
    - grep for "Live-Preview der KI-Nutzung" returns exactly 3 results (Kaufsignale, Übergänge, Verkaufstechniken)
    - grep for "HINT: Plan 04" returns 0 results (all placeholder comments replaced)
    - grep for "Diese Grunddaten fließen" returns 1 result (sec-branche-firma hint)
    - grep for "Gesprächsleitfaden" hint text returns 1 result (not typo)
  </acceptance_criteria>
  <done>
    All 6 sections have sec-hint paragraphs. 3 EWB placeholder notices added. At least 8 field-desc elements added to key fields. All placeholder comments removed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete profile_editor.html refactor:
    - Plan 01: nerve.css new variables (--success-green, --danger-red, --info-blue) + 13 new classes
    - Plan 02: 15 sections → 6 groups, sidebar updated, sliders removed, Branche section #1, Wisdom stub
    - Plan 03: CSRF fix for crudList, Erlaubnisfrage+Pitch multi-entry, accordion default-collapsed, inline-style extractions
    - Plan 04: sec-hint for all 6 sections, field-desc for key fields, EWB placeholder notices
  </what-built>
  <how-to-verify>
    Start the Flask dev server: `python app.py` (from C:/Users/andre/dev/salesnerve/)

    Open the profile editor in browser: http://localhost:5000/profiles/{profile_id} (use any existing profile)

    Check these items in order:

    **Layout &amp; Navigation:**
    1. Sidebar shows exactly 6 entries: Branche &amp; Firma, Zielgruppe, Gesprächsleitfaden, Einwände &amp; Antworten, Live-Coaching-Signale, KI-Verhalten
    2. "Gesprächsleitfaden" has correct ä (not Gespraechsleitfaden)
    3. Clicking each sidebar entry smoothly scrolls to the correct section (no section header hidden behind header)

    **Typography &amp; Design:**
    4. Section headings are clearly larger than field labels (17px headings, not 12px)
    5. Section headings are NOT uppercase and NOT teal colored (they should be dark/primary text color)
    6. Each section has a small hint text below the heading (12px, muted color)

    **Branche Section (Section #1):**
    7. Branche dropdown is the first field in Section 1
    8. Wisdom stub box is visible below the dropdown with André's exact text ("Wir haben uns die Arbeit gemacht...")
    9. "Vorschläge laden" button appears disabled (grayed, cursor not-allowed)
    10. Hovering over "Vorschläge laden" shows tooltip "Aktiv ab Phase 08.22"

    **No Sliders:**
    11. Scroll through all sections — confirm NO slider inputs visible anywhere

    **Gesprächsleitfaden Section:**
    12. Erlaubnisfrage shows as a multi-entry list with "+Erlaubnisfrage hinzufügen" button (not a textarea)
    13. Pitch shows as a multi-entry list with "+Pitch hinzufügen" button (not a textarea)
    14. "+Opener hinzufügen" button is present (unchanged)

    **Einwände Section:**
    15. Open DevTools → Network tab
    16. Click "+Skript hinzufügen" — verify the POST request shows X-CSRFToken header and returns 200 (not 400/403)
    17. Add a new Einwand — it should appear collapsed (only header visible, not expanded)
    18. Click on an Einwand header to expand — verify field order: Einwandtext first, then Gegenargument, then Technik, then Metadaten (Kategorie + Kurzlabel)
    19. Verify NO "Intensität" field visible in expanded Einwand

    **Save:**
    20. Make a small change and click the Save button (or trigger auto-save if implemented)
    21. Verify save succeeds (toast or indicator shows "gespeichert")
    22. Reload the page — verify changes persisted
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Describe any issues found (reference item numbers above).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing copy → HTML | sec-hint and field-desc contain user-facing text with Umlaute — must use UTF-8 chars (CLAUDE.md rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.2-08 | Information Disclosure | EWB placeholder mentions "Phase 08.20" | accept | This is intentional transparency per D-07. Low risk — tells users preview is coming, not a security concern |
</threat_model>

<verification>
Checkpoint task above provides full human verification checklist.

Automated smoke check:
- `grep -c "sec-hint" templates/profile_editor.html` returns 9+
- `grep -c "field-desc" templates/profile_editor.html` returns 8+
- `grep "Gespraechsleitfaden" templates/profile_editor.html` returns 0 (typo gone)
- `grep "input type=\"range\"" templates/profile_editor.html` returns 0 (no sliders)
</verification>

<success_criteria>
All 6 sections have sec-hint education text. Key fields have field-desc help text. EWB placeholder notices added to 3 sub-sections. Human verification checkpoint passes all 22 checklist items.

Phase 08.19.2 is complete when:
1. Profile editor loads without JS errors
2. 6 consolidated sections visible in sidebar
3. Branche as Section #1 with Wisdom stub
4. No sliders anywhere
5. +Skript and +Opener work (CSRF fixed)
6. Erlaubnisfrage + Pitch are multi-entry
7. Einwände start collapsed with correct field order
8. 17px section headings (not 12px, not uppercase, not teal)
9. sec-hint and field-desc visible throughout
10. Save mechanism works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-04-SUMMARY.md`
</output>
