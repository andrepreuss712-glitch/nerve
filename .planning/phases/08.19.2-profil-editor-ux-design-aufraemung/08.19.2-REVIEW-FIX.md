---
phase: 08.19.2-profil-editor-ux-design-aufraemung
fixed_at: 2026-04-28T00:00:00Z
review_path: .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-REVIEW.md
iteration: 1
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
---

# Phase 08.19.2: Code Review Fix Report

**Fixed at:** 2026-04-28
**Source review:** `.planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 4
- Fixed: 4
- Skipped: 0

## Fixed Issues

### WR-01: `_pitch_dual_write` / `_erlaubnis_dual_write` silent failure on exception

**Files modified:** `routes/profiles.py`
**Commit:** c952af7
**Applied fix:** Wrapped all four dual-write call sites (`pitch_erstellen`, `pitch_bearbeiten`, `erlaubnis_erstellen`, `erlaubnis_bearbeiten`) in `try/except Exception as _e` blocks that print a `[Profile]`-prefixed error message. The primary `db.commit()` and the success response are unaffected; only the dual-write failure is now observable via stdout logging.

---

### WR-02: `vi_opener` textarea always blank on reload (data-loss appearance)

**Files modified:** `templates/profile_editor.html`
**Commit:** 122477d
**Applied fix:** Removed the `vi_opener` textarea form group from section 3 of the template (4 HTML lines). Also removed the corresponding `opener: document.getElementById('vi_opener').value.trim()` line from `buildAndSubmit()` and the `setVal('vi_opener', DATEN.opener)` line from the populate block. The opener-list CRUD widget directly below remains as the canonical entry point.

---

### WR-03: `_require_own_profile()` — non-obvious caller-must-close-db pattern

**Files modified:** `routes/profiles.py`
**Commit:** 6bf1531
**Applied fix:** Extended the existing docstring with a `WARNING:` paragraph that explicitly states the function opens a DB session internally, the caller receives it, and must close it in a `finally` block. No structural changes to the function or its callers.

---

### WR-04: `crudList()` textarea content injection via partial HTML escaping

**Files modified:** `templates/profile_editor.html`
**Commit:** d50e3e9
**Applied fix:** Replaced the unsafe `innerHTML` textarea string (which only escaped `<`) with a DOM API approach: `document.createElement('textarea')` with `.value = item.inhalt || ''` and `card.insertBefore(inhaltTa, card.querySelector('.crud-actions'))`. The textarea is inserted between the name input and the crud-actions div, preserving the original visual layout. The name `<input>` keeps the existing `&quot;` escaping since it uses a `value` attribute inside innerHTML (safe for that context). The `inhaltTa`, `nameInput`, and `delBtn` variable references all resolve correctly after the change.

---

_Fixed: 2026-04-28_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
