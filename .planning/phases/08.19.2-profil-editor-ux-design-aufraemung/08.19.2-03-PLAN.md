---
phase: 08.19.2
plan: 03
type: execute
wave: 3
depends_on:
  - 08.19.2-02
files_modified:
  - templates/profile_editor.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "crudList() POST/PUT/DELETE requests include X-CSRFToken header — +Skript and +Opener buttons work"
    - "Erlaubnisfrage section has a crudList-managed multi-entry list loading from /profiles/<id>/erlaubnis (not a textarea)"
    - "Pitch section has a crudList-managed multi-entry list loading from /profiles/<id>/pitches (not a textarea)"
    - "crudList('erlaubnis-list', ...) called in init() — erlaubnis items load from /profiles/<id>/erlaubnis"
    - "crudList('pitch-list', ...) called in init() — pitch items load from /profiles/<id>/pitches"
    - "No vi_erlaubnis or vi_pitch element IDs in the template"
    - "buildAndSubmit does NOT include erlaubnis or pitch keys (handled by crudList auto-save)"
    - "addEinwand() generates .einwand-body with class 'collapsed' by default — new einwände start closed"
    - "Einwand sub-fields order: Einwandtext → Gegenargument → Technik → Metadaten (Kategorie + Kurzlabel)"
    - "Intensität select (data-ew='int') removed from addEinwand() template string"
    - "intensitaet key removed from getEinwaende() serialization"
    - "Inline styles at Z.260/263 replaced by class='save-toast'"
    - "Col-header inline styles replaced by .pe-col-grid-2, .pe-col-grid-3, .pe-col-grid-skript classes"
    - "Übergänge .urow border-width set to 2px"
    - "Section anchor scrollIntoView uses 16px offset correction"
    - "base.html has <meta name=\"csrf-token\" content=\"{{ csrf_token() }}\"> (pre-condition verified before adding CSRF logic)"
    - "crudList fetch success handler shows .save-toast for 2 seconds after successful POST or PUT"
  artifacts:
    - path: "templates/profile_editor.html"
      provides: "Interaction fixes, CSRF fix, crudList-based Erlaubnisfrage/Pitch, accordion improvements, save-toast wiring"
      contains: "_getCsrfToken"
  key_links:
    - from: "_getCsrfToken()"
      to: "crudList() fetch calls"
      via: "X-CSRFToken header"
      pattern: "X-CSRFToken.*_getCsrfToken"
    - from: "crudList('erlaubnis-list', '/profiles/'+PROFILE_ID+'/erlaubnis', 'Erlaubnisfrage')"
      to: "erlaubnis_liste GET endpoint"
      via: "fetch to /profiles/<id>/erlaubnis"
      pattern: "crudList.*erlaubnis"
    - from: "crudList('pitch-list', '/profiles/'+PROFILE_ID+'/pitches', 'Pitch')"
      to: "pitch_liste GET endpoint"
      via: "fetch to /profiles/<id>/pitches"
      pattern: "crudList.*pitch"
    - from: "crudList fetch success handler"
      to: ".save-toast element"
      via: "document.querySelector('.save-toast') + setTimeout 2000ms"
      pattern: "save-toast.*setTimeout"
---

<objective>
Fix the +Skript CSRF bug, convert Erlaubnisfrage and Pitch to crudList-based multi-entry (backed by typed ProfileOpener endpoints from Plan 02), fix Einwände accordion default state and sub-field order, remove Intensität select, extract remaining inline styles to CSS classes, fix Übergänge border and scrollIntoView offset, wire save-toast into crudList success handler.

Purpose: Implements D-03 (accordion default closed, Intensität removed), D-05 (multi-entry Erlaubnisfrage+Pitch via ProfileOpener typed endpoints), D-09 (+Skript bug = CSRF fix), D-08 (inline-style cleanup, Übergänge border, scroll offset). Also addresses review findings F-3 (CSRF meta-tag pre-check) and F-4 (save-toast UX feedback for crudList auto-saves). Erlaubnisfrage and Pitch now use the same crudList pattern as Opener — items auto-save via the API, no serialization in buildAndSubmit.

Output: All crudList mutations have CSRF headers. Erlaubnisfrage + Pitch are crudList-managed containers pointing to /profiles/<id>/erlaubnis and /profiles/<id>/pitches. Einwände accordion starts collapsed. Inline styles replaced by CSS classes. save-toast shows for 2 seconds on successful crudList save.
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/dev/salesnerve/.planning/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- CSRF pre-condition (Finding 3 — verified from source): -->
<!-- base.html line 6 already has: <meta name="csrf-token" content="{{ csrf_token() }}"> -->
<!-- This was added in Phase 08.10. Pre-check at start of Task 1 Edit 0 confirms it exists. -->
<!-- If missing (unexpected), add it before proceeding. -->

<!-- CSRF fix pattern — from RESEARCH.md Q3 / Code Examples -->
<!-- Add this function BEFORE crudList() definition (~Z.1259): -->
function _getCsrfToken() {
  var m = document.querySelector('meta[name="csrf-token"]');
  return m ? m.getAttribute('content') : '';
}

<!-- IMPORTANT: The selector MUST use double-quotes around csrf-token inside the attribute  -->
<!-- selector: 'meta[name="csrf-token"]' — single outer quotes, double inner quotes.        -->
<!-- This is the correct DOM attribute selector syntax (Finding 3 CSS selector fix).        -->

<!-- Then in crudList() modify each fetch call: -->
<!-- POST (~Z.1316): headers add 'X-CSRFToken': _getCsrfToken() -->
<!-- PUT  (~Z.1281): headers add 'X-CSRFToken': _getCsrfToken() -->
<!-- DELETE (~Z.1293): headers: { 'X-CSRFToken': _getCsrfToken() } -->

<!-- save-toast wiring in crudList success handler (Finding 4): -->
<!-- In crudList(), find the fetch().then() success handler for POST and PUT calls.         -->
<!-- After the successful response processing (list reload or item update), add:            -->
var toast = document.querySelector('.save-toast');
if (toast) { toast.style.display = 'inline'; setTimeout(function(){ toast.style.display = 'none'; }, 2000); }
<!-- This shows the .save-toast element (added by Plan 01 CSS) for 2 seconds.              -->
<!-- .save-toast CSS class is defined in nerve.css (Plan 01): font-size 13px, --success-green. -->
<!-- The element itself is at Z.260/263 (inline style will be replaced by class in Task 2). -->

<!-- crudList() signature (from profile_editor.html Z.1262): -->
function crudList(containerId, endpoint, label) { ... }
<!-- Existing crudList call for opener (Z.~1441): -->
crudList('opener-list', '/profiles/' + PROFILE_ID + '/opener', 'Opener');

<!-- NEW crudList calls for erlaubnis and pitch (same pattern as opener): -->
crudList('erlaubnis-list', '/profiles/' + PROFILE_ID + '/erlaubnis', 'Erlaubnisfrage');
crudList('pitch-list', '/profiles/' + PROFILE_ID + '/pitches', 'Pitch');
<!-- These are added in init() at the same position as the opener crudList call (Z.~1441). -->
<!-- The endpoints /profiles/<id>/erlaubnis and /profiles/<id>/pitches were created in Plan 02 Task 4. -->

<!-- Erlaubnisfrage HTML container (replaces vi_erlaubnis textarea): -->
<div class="fg erlaubnis-entry">
  <label class="fl">Erlaubnisfrage</label>
  <div id="erlaubnis-list" class="pe-entry-list"></div>
  <button type="button" class="btn-add" id="btn-add-erlaubnis">+ Erlaubnisfrage hinzufügen</button>
</div>

<!-- Pitch HTML container (replaces vi_pitch textarea): -->
<div class="fg pitch-entry">
  <label class="fl">Pitch</label>
  <div id="pitch-list" class="pe-entry-list"></div>
  <button type="button" class="btn-add" id="btn-add-pitch">+ Pitch hinzufügen</button>
</div>
<!-- Note: btn-add-erlaubnis and btn-add-pitch are wired by crudList() internals. -->
<!-- The crudList() function already handles the button click to POST a new item. -->
<!-- Pass the button ID or let crudList handle discovery — check the existing crudList -->
<!-- implementation to see how it wires the add-button (Z.1262-1330). -->

<!-- Einwand accordion — from RESEARCH.md Code Examples Z.726-741 -->
<!-- addEinwand() template string: change 'einwand-body' → 'einwand-body collapsed' -->
<!-- Sub-field order: Einwandtext → Gegenargument → Technik → Metadaten (Kategorie, Kurzlabel) -->
<!-- Remove: <div class="fg"><label class="fl">Intensität</label><select data-ew="int">...</select></div> -->
<!-- getEinwaende() remove: intensitaet: b.querySelector('[data-ew="int"]').value -->

<!-- Inline style locations (RESEARCH.md Q2/D-08): -->
<!-- Z.260, Z.263: style="font-size:13px;color:#22c55e" → class="save-toast" -->
<!-- Z.509, Z.537: col-header display:grid inline → class="pe-col-grid-2" or pe-col-grid-3 -->
<!-- Z.549, Z.561, Z.596: skript col-header → class="pe-col-grid-skript" -->
<!-- Z.515: Skript-Bullets-Hint style="font-size:11px;color:..." → class="skript-hint" -->
<!-- Z.637: color:var(--page-text-muted,#888) → color:var(--page-text-muted) (remove fallback) -->
<!-- Z.676, Z.711: faq-used-count inline style → class="faq-count" -->
<!-- Z.925, Z.954, Z.996: color:#00D4AA in JS template strings → color:var(--input-border-focus) -->

<!-- Übergänge border: find .urow or equivalent selector in template style block or nerve.css -->
<!-- Set border-width to 2px, color to var(--glass-border-hover) -->

<!-- scrollIntoView offset fix (goSec function ~Z.850): -->
<!-- Current: element.scrollIntoView({behavior:'smooth', block:'start'}) -->
<!-- New: Use getBoundingClientRect to calculate target minus 16px offset -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSRF pre-check + fix for crudList + Erlaubnisfrage/Pitch crudList containers + save-toast wiring</name>
  <files>templates/profile_editor.html</files>
  <read_first>
    - templates/base.html (lines 1-15 — verify `<meta name="csrf-token" content="{{ csrf_token() }}">` exists before adding crudList CSRF logic; expected at line 6, added in Phase 08.10)
    - templates/profile_editor.html (read the inline script block Z.758-1494 carefully — locate crudList function at Z.1262, how it wires the add-button (btn-add inside containerId or separate button param), existing crudList call for opener in init(), buildAndSubmit vi_erlaubnis/vi_pitch reads, init() erlaubnis/pitch init calls, fetch POST and PUT success handlers inside crudList)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-RESEARCH.md (Q3 CSRF fix Z.341-374, Q5 Erlaubnisfrage pattern Z.399-426, Code Examples CSRF block Z.686-721)
  </read_first>
  <action>
    **Edit 0 — Verify CSRF meta-tag pre-condition**:
    Before making any other edits, grep base.html for `meta.*csrf-token`.
    Expected result: `<meta name="csrf-token" content="{{ csrf_token() }}">` at line 6 (added Phase 08.10).
    - If present: proceed to Edit A.
    - If missing (unexpected): add `<meta name="csrf-token" content="{{ csrf_token() }}">` to the `<head>`
      block in templates/base.html before proceeding. This is a pre-condition for all crudList CSRF logic.

    **Edit A — Add _getCsrfToken() before crudList()**:
    Find the `function crudList(` definition in the inline script block (~Z.1259). Immediately before it, insert:
    ```javascript
    function _getCsrfToken() {
      var m = document.querySelector('meta[name="csrf-token"]');
      return m ? m.getAttribute('content') : '';
    }
    ```
    IMPORTANT: The selector string MUST be `'meta[name="csrf-token"]'` with double-quotes around
    `csrf-token` inside the attribute selector. Single outer quotes, double inner quotes. This is
    the correct HTML attribute selector syntax (Finding 3 fix).

    **Edit B — Add X-CSRFToken to all three crudList fetch calls**:
    In `crudList()`, find the POST fetch (~Z.1316), PUT fetch (~Z.1281), and DELETE fetch (~Z.1293). For each:
    - POST: change `headers: {'Content-Type': 'application/json'}` → `headers: {'Content-Type': 'application/json', 'X-CSRFToken': _getCsrfToken()}`
    - PUT: same change
    - DELETE: change `headers: {}` or missing headers → `headers: {'X-CSRFToken': _getCsrfToken()}`

    Confirm the fix covers ALL mutating calls. If there are more fetch calls inside crudList, add the header to all of them.

    **Edit C — Wire save-toast in crudList fetch success handler**:
    In `crudList()`, find the `fetch(...).then(...)` success handler for POST and PUT calls (the block
    that runs after a successful response — typically where the list is reloaded or the item is updated).
    After the successful response processing (list reload or item update), add:
    ```javascript
    var toast = document.querySelector('.save-toast');
    if (toast) { toast.style.display = 'inline'; setTimeout(function(){ toast.style.display = 'none'; }, 2000); }
    ```
    This shows the .save-toast element (defined in nerve.css Plan 01, styled at Z.260/263 HTML) for
    2 seconds when a crudList item is successfully saved. Add this snippet to BOTH the POST success
    handler AND the PUT success handler (each has its own .then() block).

    **Edit D — Replace vi_erlaubnis textarea with erlaubnis-list container** (HTML body):
    Find the current `<textarea ...>` for vi_erlaubnis (the Erlaubnisfrage field, ~Z.400). Replace the entire surrounding `<div class="fg ...">` group for erlaubnis with:
    ```html
    <div class="fg erlaubnis-entry">
      <label class="fl">Erlaubnisfrage</label>
      <div id="erlaubnis-list" class="pe-entry-list"></div>
      <button type="button" class="btn-add" id="btn-add-erlaubnis">+ Erlaubnisfrage hinzufügen</button>
    </div>
    ```
    Do NOT add any addErlaubnis() JS function. Do NOT add any getErlaubnisse() function. The crudList() function handles the + button and item rendering automatically.

    **Edit E — Replace vi_pitch textarea with pitch-list container** (HTML body):
    Find the current `<textarea ...>` for vi_pitch (the Pitch field). Replace the entire surrounding `<div class="fg ...">` group for pitch with:
    ```html
    <div class="fg pitch-entry">
      <label class="fl">Pitch</label>
      <div id="pitch-list" class="pe-entry-list"></div>
      <button type="button" class="btn-add" id="btn-add-pitch">+ Pitch hinzufügen</button>
    </div>
    ```
    Do NOT add any addPitch() JS function. Do NOT add any getPitches() function.

    **Edit F — Add crudList calls in init()** (JS script block):
    Find the existing `crudList('opener-list', ...)` call in init() (~Z.1441). Immediately after it, add:
    ```javascript
    crudList('erlaubnis-list', '/profiles/' + PROFILE_ID + '/erlaubnis', 'Erlaubnisfrage');
    crudList('pitch-list', '/profiles/' + PROFILE_ID + '/pitches', 'Pitch');
    ```

    **REMOVE from buildAndSubmit()**: Find and remove any lines that serialize erlaubnis or pitch into the form payload. Specifically:
    - Remove any line containing `erlaubnis:` or `erlaubnisse:` as a key in the submitted JSON
    - Remove any line containing `pitch:` or `pitches:` as a key in the submitted JSON
    Items are auto-saved by crudList via the API — they must NOT also be included in buildAndSubmit.

    **REMOVE from init()**: Remove any lines that read DATEN.erlaubnis, DATEN.erlaubnisse, DATEN.pitch, or DATEN.pitches to populate old textarea or array fields. The crudList calls added in Edit F replace this initialization entirely (crudList fetches items from the API on its own).

    IMPORTANT: Do NOT add `style=` attributes or `element.style.cssText` anywhere in Edits D, E, or F. Layout is handled by the CSS classes from nerve.css (Plan 01): .pe-entry-list, .erlaubnis-entry, .pitch-entry, .btn-add.
  </action>
  <verify>
    <automated>grep -n "_getCsrfToken\|X-CSRFToken\|erlaubnis-list\|pitch-list\|crudList.*erlaubnis\|crudList.*pitch\|save-toast" C:/Users/andre/dev/salesnerve/templates/profile_editor.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'csrf-token' templates/base.html returns at least 1 (pre-condition verified — meta tag exists)
    - grep for "_getCsrfToken" returns at least 4 results (definition + 3 usages in crudList POST/PUT/DELETE)
    - grep for 'meta[name="csrf-token"]' (with double-quotes around csrf-token) in _getCsrfToken function returns 1 result
    - grep for "X-CSRFToken" returns at least 3 results (one per fetch call type)
    - grep for "erlaubnis-list" in HTML returns 1 result (the container div)
    - grep for "pitch-list" in HTML returns 1 result (the container div)
    - grep for "btn-add-erlaubnis" returns 1 result (the HTML button)
    - grep for "btn-add-pitch" returns 1 result (the HTML button)
    - grep for "crudList.*erlaubnis" in init() returns 1 result
    - grep for "crudList.*pitch" in init() returns 1 result
    - grep for "save-toast" in crudList fetch success handler returns at least 1 result (toast wired in POST or PUT success)
    - grep for "setTimeout" near save-toast returns at least 1 result (2000ms dismiss timer)
    - grep for "getErlaubnisse\|getPitches\|addErlaubnis\|addPitch" returns 0 results (no custom functions added)
    - grep for "vi_erlaubnis\|vi_pitch" returns 0 results (no old element IDs anywhere in file)
    - grep for "erlaubnisse:\|pitches:" in buildAndSubmit returns 0 results (not in form submit)
    - grep for "erlaubnis:\|pitch:" as object key in buildAndSubmit returns 0 results (not in form submit)
    - grep for "DATEN.erlaubnis\|DATEN.pitches\|DATEN.pitch" in init() returns 0 results (old init code removed)
    - grep for "style=" in the new erlaubnis-entry and pitch-entry HTML blocks returns 0 results
  </acceptance_criteria>
  <done>
    CSRF meta-tag pre-condition verified. crudList CSRF bug fixed. _getCsrfToken uses correct double-quoted attribute selector. save-toast wired into crudList POST and PUT success handlers (2-second display). Erlaubnisfrage and Pitch replaced by crudList containers backed by /profiles/<id>/erlaubnis and /profiles/<id>/pitches. No custom addErlaubnis/addPitch/getErlaubnisse/getPitches functions. buildAndSubmit does not include erlaubnis or pitch keys. init() does not populate old textarea fields from DATEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Einwände accordion default-collapsed + sub-field reorder + inline-style extractions + Übergänge border + scroll offset</name>
  <files>templates/profile_editor.html</files>
  <read_first>
    - templates/profile_editor.html (read addEinwand() function ~Z.987-1030, getEinwaende() ~Z.1032-1045, _showToast ~Z.1239-1247, goSec() function ~Z.845-860, Übergänge section HTML ~Z.594-607)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-RESEARCH.md (Code Examples Accordion section Z.723-741, Q6 Intensität-Select Z.468-480, Common Pitfalls 5+7, inline-style locations)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-UI-SPEC.md (Interaction Contracts Accordion section, lines 166-184)
  </read_first>
  <action>
    **Edit A — Einwände accordion default-collapsed**:
    In `addEinwand()` template string, find the line creating the `.einwand-body` div:
    - Current: `<div class="einwand-body">`
    - Change to: `<div class="einwand-body collapsed">`
    This ensures new einwände start collapsed (Pitfall 5 fix per RESEARCH.md).

    **Edit B — Einwand sub-field reorder**:
    In the `addEinwand()` template string, reorder the sub-fields inside `.einwand-body` to:
    1. Einwandtext (textarea, full — the main objection text)
    2. Gegenargument (textarea)
    3. Empfohlene Technik (select or text input)
    4. Metadaten block (Kategorie + Kurzlabel in 2-column layout using .pe-col-grid-2)

    The current template string has these fields in a different order — read the current addEinwand() to understand existing order, then reorder to match the specification.

    **Edit C — Remove Intensität select from addEinwand()**:
    Find and remove this block from the addEinwand() template string:
    ```html
    <div class="fg" style="margin:0"><label class="fl">Intensität</label>
      <select class="fs" data-ew="int">
        <option value="mittel" ...>Mittel</option>
        <option value="hoch" ...>Hoch</option>
      </select></div>
    ```
    Also remove from `getEinwaende()`: the line `intensitaet: b.querySelector('[data-ew="int"]').value`

    **Edit D — Inline-style extractions**:
    Apply the following substitutions throughout the template (both HTML and JS template strings):

    1. Save-toast inline styles (~Z.260, Z.263):
       Find `style="font-size:13px;color:#22c55e"` on toast elements → replace with `class="save-toast"` (or add class if element already has style attribute — combine: remove style, add class)

    2. Col-header inline grid styles:
       - `style="display:grid;grid-template-columns:1fr 1fr;gap:8px"` → `class="pe-col-grid-2"`
       - `style="display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:8px"` → `class="pe-col-grid-3"`
       - `style="display:grid;grid-template-columns:36px 1fr 1fr auto auto;gap:8px"` → `class="pe-col-grid-skript"`
       Note: exact column counts may vary slightly from the above — match the actual values in the file, pick the closest class. If a col-header has a unique layout not covered by the 3 classes, leave it as inline style (do not invent new classes).

    3. Skript-Bullets-Hint (~Z.515):
       `style="font-size:11px;color:var(--page-text-muted);margin..."` → `class="skript-hint"` (remove the inline style)

    4. FL-desc fallback (~Z.637):
       `color:var(--page-text-muted,#888)` → `color:var(--page-text-muted)` (remove the `#888` fallback)

    5. FAQ-used-count (~Z.711) and FAQ description (~Z.676):
       Replace inline `font-size` + `color` styles on faq-used-count span → `class="faq-count"`

    6. JS template string colors (~Z.925, Z.954, Z.996):
       `color:#00D4AA` in addPhase() and other JS-generated markup → `color:var(--input-border-focus)`
       Better: remove the inline color and instead add a CSS class. But since these are JS template strings, replacing with `var()` is acceptable.

    **Edit E — Übergänge border-width 2px**:
    Find the Übergänge section (now inside sec-live-signale). Find `.urow` elements or their container. In the template style block or by checking nerve.css, find where `.urow` or `.uebergang-item` border is defined. Set `border-width: 2px` and `border-color: var(--glass-border-hover)`. If the style is inline on the HTML elements, add a class or set in template style block. If defined in nerve.css, update there. Check both locations.

    **Edit F — scrollIntoView with 16px offset in goSec()**:
    Find the `goSec()` function (~Z.845). Current likely: `element.scrollIntoView({behavior:'smooth', block:'start'})`. Replace the scroll call with:
    ```javascript
    var top = el.getBoundingClientRect().top + window.pageYOffset - 16;
    window.scrollTo({top: top, behavior: 'smooth'});
    ```
    This adds a 16px negative offset so the section heading is not hidden behind any sticky header.
    Note: If goSec() has more logic than just scrollIntoView (e.g., active class management), preserve that logic and only replace the scroll call itself.
  </action>
  <verify>
    <automated>grep -n "einwand-body collapsed\|_getCsrfToken\|pe-col-grid\|save-toast\|getBoundingClientRect" C:/Users/andre/dev/salesnerve/templates/profile_editor.html</automated>
  </verify>
  <acceptance_criteria>
    - grep for "einwand-body collapsed" in addEinwand() template string returns 1 result
    - grep for "data-ew=\"int\"" in addEinwand() returns 0 results (Intensität removed)
    - grep for "intensitaet.*data-ew" in getEinwaende() returns 0 results
    - grep for "pe-col-grid" as HTML class returns at least 3 results (col-header replacements)
    - grep for "save-toast" as class (not style=) returns at least 2 results (toast elements Z.260/263 converted)
    - grep for "font-size:13px;color:#22c55e" returns 0 results (old inline toast style fully replaced)
    - grep for "getBoundingClientRect" in goSec function returns 1 result
    - In Übergänge section HTML or CSS: border-width 2px present on row/item element
  </acceptance_criteria>
  <done>
    Einwände accordion starts collapsed. Sub-field order corrected. Intensität removed. Inline styles extracted to CSS classes. Übergänge border 2px. goSec uses 16px scroll offset.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser JS → Flask CRUD endpoints | crudList() POST/PUT/DELETE for opener, pitch, erlaubnis — now all include CSRF token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.2-05 | Spoofing / CSRF | crudList mutations without CSRF token | mitigate | _getCsrfToken() added before crudList, X-CSRFToken header on all mutating fetches |
| T-08.19.2-06 | Tampering | crudList XSS via unescaped content | mitigate | crudList() uses .replace(/</g, '&lt;') on inhalt values — existing pattern preserved |
| T-08.19.2-10 | Tampering | Old vi_erlaubnis / vi_pitch getElementById calls in buildAndSubmit causing runtime error | mitigate | Verify grep confirms 0 references to vi_erlaubnis/vi_pitch anywhere in file after change |
| T-08.19.2-11 | Tampering | save-toast querySelector('.save-toast') returns null if element not in DOM | accept | Guard: `if (toast)` before style assignment — silently no-ops if element absent |
</threat_model>

<verification>
After Plan 03 completes:
- grep -c 'csrf-token' templates/base.html returns at least 1 (pre-condition confirmed)
- In browser DevTools Network tab: clicking +Skript should produce a POST request with X-CSRFToken header and return 200 (not 400/403)
- Clicking +Opener produces POST with CSRF token
- Clicking +Erlaubnisfrage hinzufügen produces POST to /profiles/<id>/erlaubnis with CSRF token
- Clicking +Pitch hinzufügen produces POST to /profiles/<id>/pitches with CSRF token
- After a successful crudList POST or PUT: .save-toast element becomes visible for ~2 seconds then hides
- Erlaubnisfrage section shows crudList container with "+Erlaubnisfrage hinzufügen" button, not a textarea
- Pitch section shows crudList container with "+Pitch hinzufügen" button, not a textarea
- Existing erlaubnis and pitch items load automatically on page load (crudList fetches from API)
- Adding a new Einwand: the card starts collapsed (only header visible)
- Expanding an Einwand: shows Einwandtext first, then Gegenargument, Technik, Metadaten
- No Intensität select visible in Einwand cards
</verification>

<success_criteria>
CSRF meta-tag pre-condition verified (base.html line 6). All crudList mutations send CSRF token with correct querySelector double-quote syntax. crudList POST and PUT success handlers show .save-toast for 2 seconds. Erlaubnisfrage and Pitch are crudList-managed containers backed by typed ProfileOpener endpoints (no custom JS functions, no buildAndSubmit serialization). Einwände accordion starts collapsed with correct sub-field order. Intensität removed. Inline styles extracted. Übergänge border 2px. Scroll offset 16px applied.
</success_criteria>

<output>
After completion, create `.planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-03-SUMMARY.md`
</output>
