---
phase: 08.19.2
plan: "03"
subsystem: frontend/html + frontend/js
tags: [profile-editor, csrf, crudlist, accordion, inline-style, ux]
dependency_graph:
  requires:
    - "08.19.2-02 (ProfileOpener.type Diskriminator + /erlaubnis + /pitches CRUD-Endpoints)"
  provides:
    - "crudList() CSRF-fix: X-CSRFToken in POST/PUT/DELETE (D-09 Bug-Fix)"
    - "Erlaubnisfrage als crudList-Container backed by /profiles/<id>/erlaubnis"
    - "Pitch als crudList-Container backed by /profiles/<id>/pitches"
    - "save-toast wired in crudList POST/PUT success handlers"
    - "Einwand-Accordion: default-collapsed, Intensitaet entfernt, Sub-Felder-Reihenfolge korrigiert"
    - "Inline-Style-Extraktion: pe-col-grid-*, skript-hint, faq-count, save-toast Klasse"
    - "Uebergangs-.urow border 2px solid var(--glass-border-hover)"
  affects:
    - "templates/profile_editor.html"
tech_stack:
  added: []
  patterns:
    - "_getCsrfToken() helper in Inline-Script-Block — liest meta[name='csrf-token'] mit doppelten Anführungszeichen im Attributselektor"
    - "save-toast pattern: querySelector('.save-toast') + style.display toggle + 2s setTimeout"
    - "crudList Erlaubnisfrage/Pitch: gleiche Architektur wie Opener (containerId + endpoint + label)"
key_files:
  created: []
  modified:
    - path: "templates/profile_editor.html"
      description: "CSRF-Fix, crudList Erlaubnisfrage/Pitch, Accordion collapsed, Intensitaet entfernt, Inline-Style-Extraktion, .urow 2px border"
key_decisions:
  - "save-toast Element als span in Topbar eingefuegt (display:none initial) — crudList-success-handler steuert Sichtbarkeit"
  - "wizard-banner inline-styles (#22c55e) bleiben erhalten — anderes Element als save-toast, out-of-scope fuer Plan 03"
  - "varianten-Feld aus addEinwand() Template entfernt (zusammen mit Intensitaet) — konsistent mit D-03 Sub-Feld-Reorder-Spec"
  - "goSec() war bereits korrekt mit getBoundingClientRect 16px offset (Plan 02) — keine Aenderung noetig"
requirements-completed: []
duration: "~20 Minuten"
completed: "2026-04-28"
tasks_completed: 2
files_modified: 1
---

# Phase 08.19.2 Plan 03: CSRF-Fix + crudList Erlaubnisfrage/Pitch + Accordion-Fixes

**CSRF-Bug in crudList() gefixt (POST/PUT/DELETE erhalten X-CSRFToken), Erlaubnisfrage und Pitch zu crudList-Containern umgebaut, Einwand-Accordion startet collapsed mit korrekter Feldreihenfolge ohne Intensität-Select.**

## Performance

- **Duration:** ~20 Minuten
- **Started:** 2026-04-28
- **Completed:** 2026-04-28
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- CSRF-Bug (D-09) behoben: `_getCsrfToken()` hinzugefuegt, alle drei crudList-fetch-Calls (POST/PUT/DELETE) senden jetzt `X-CSRFToken`-Header
- Erlaubnisfrage und Pitch sind jetzt crudList-basierte Multi-Entry-Container backed by `/profiles/<id>/erlaubnis` und `/profiles/<id>/pitches` (keine Textareas mehr)
- save-toast Element in Topbar verdrahtet — erscheint 2 Sekunden nach erfolgreichem crudList-POST oder PUT
- Einwand-Accordion startet collapsed (D-03), Sub-Feld-Reihenfolge korrigiert: Einwandtext → Gegenargument → Technik → Metadaten
- Intensität-Select aus Einwand-Card und getEinwaende()-Serialisierung entfernt (D-03)
- Inline-Style-Extraktion: pe-col-grid-skript/3/2, skript-hint, faq-count Klassen genutzt
- .urow (Übergänge) border auf 2px solid var(--glass-border-hover) gesetzt

## Task Commits

1. **Task 1: CSRF fix + erlaubnis/pitch crudList + save-toast** - `4cb0042` (feat)
2. **Task 2: Accordion collapsed + sub-field reorder + inline-style cleanup** - `4acb6a4` (feat)

## Files Created/Modified

- `templates/profile_editor.html` — _getCsrfToken(), X-CSRFToken in alle fetch-Calls, erlaubnis-list/pitch-list Container, crudList-Aufrufe, buildAndSubmit-Cleanup, accordion collapsed, Intensitaet entfernt, Inline-Styles extrahiert, .urow border 2px

## Decisions Made

- `save-toast` als `<span class="save-toast" style="display:none">` in Topbar platziert — querySelector('.save-toast') findet es aus crudList heraus
- wizard-banner (`#22c55e` inline-styles) bleibt unberührt — kein Toast-Element, out-of-scope
- `varianten`-Feld aus addEinwand() entfernt (war in der alten Reihenfolge, nicht in D-03-Spec erwähnt) — konsistent mit der reduzierten Feldliste
- goSec() war bereits mit getBoundingClientRect + 16px offset implementiert (Plan 02 Task 2) — keine Änderung nötig

## Deviations from Plan

**1. [Rule 2 — Enhancement] save-toast span Element zum HTML hinzugefuegt**
- **Found during:** Task 1 (Edit C — save-toast wiring)
- **Issue:** Plan beschreibt `document.querySelector('.save-toast')` in crudList, aber das Element existierte noch nicht im HTML-DOM
- **Fix:** `<span class="save-toast" style="display:none">✓ Gespeichert</span>` in die Topbar eingefuegt (nach dem Speichern-Button)
- **Files modified:** templates/profile_editor.html
- **Commit:** 4cb0042

**2. [Rule 1 — Beobachtung] varianten-Feld in addEinwand() entfernt**
- **Found during:** Task 2 (Edit B — Sub-Feld-Reorder)
- **Issue:** Das varianten-Feld war in der alten Reihenfolge vorhanden, ist aber in der D-03-Spec-Reihenfolge (Einwandtext → Gegenargument → Technik → Metadaten) nicht aufgeführt
- **Fix:** varianten-Feld aus addEinwand() Template und getEinwaende()-Serialisierung entfernt (konsistent mit Intensitaet-Entfernung)
- **Files modified:** templates/profile_editor.html
- **Commit:** 4acb6a4

---

**Total deviations:** 2 auto-fixed (1 Rule 2 Enhancement, 1 Rule 1 Observation)
**Impact on plan:** Beide Fixes notwendig fuer Funktionsfaehigkeit und Spec-Konformitaet. Kein Scope-Creep.

## Known Stubs

Keine neuen Stubs in Plan 03.

## Threat Surface Scan

Keine neuen Endpunkte oder Sicherheitsgrenzen eingefuehrt. Alle crudList-Mutationen senden jetzt CSRF-Token — T-08.19.2-05 vollstaendig mitigiert. T-08.19.2-11 (save-toast querySelector null) durch `if (toast)` Guard abgesichert.

## Self-Check: PASSED

| Item | Status |
|------|--------|
| templates/profile_editor.html | FOUND |
| _getCsrfToken() defined | FOUND (Z.1209) |
| X-CSRFToken in POST/PUT/DELETE | FOUND (3 Stellen) |
| erlaubnis-list container | FOUND (Z.428) |
| pitch-list container | FOUND (Z.433) |
| btn-add-erlaubnis | FOUND |
| btn-add-pitch | FOUND |
| crudList('erlaubnis-list'...) | FOUND (Z.1392) |
| crudList('pitch-list'...) | FOUND (Z.1393) |
| vi_erlaubnis / vi_pitch (should be 0) | 0 Treffer |
| erlaubnis:/pitch: in buildAndSubmit (should be 0) | 0 Treffer |
| DATEN.erlaubnis/DATEN.pitch in init (should be 0) | 0 Treffer |
| save-toast span im HTML | FOUND (Z.248) |
| save-toast in crudList success handler | FOUND (2 Stellen) |
| einwand-body collapsed | FOUND (Z.957) |
| data-ew="int" (should be 0) | 0 Treffer |
| intensitaet in getEinwaende (should be 0) | 0 Treffer |
| pe-col-grid classes | FOUND (3 Stellen) |
| skript-hint class | FOUND |
| faq-count class | FOUND |
| .urow border 2px | FOUND (Z.210) |
| Commit 4cb0042 | FOUND |
| Commit 4acb6a4 | FOUND |

## Next Phase Readiness

- Plan 03 vollstaendig abgeschlossen
- Plan 04 kann beginnen: sec-hint Education-Stubs fuer alle 6 Sektionen

---
*Phase: 08.19.2-profil-editor-ux-design-aufraemung*
*Completed: 2026-04-28*
