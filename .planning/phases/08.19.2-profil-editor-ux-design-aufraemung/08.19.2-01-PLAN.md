---
phase: 08.19.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/nerve.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "nerve.css :root block contains --success-green, --danger-red, --info-blue"
    - ".sec-title renders at 17px font-size weight 600 without uppercase transform"
    - ".block-lbl renders at 13px font-size weight 600"
    - ".field-desc renders at 12px in --page-text-muted color"
    - ".sec-hint renders at 12px in --page-text-muted color"
    - ".wisdom-stub-box and .wisdom-stub-btn classes exist in nerve.css"
    - "Grid helper classes .pe-col-grid-2, .pe-col-grid-3, .pe-col-grid-skript exist"
    - ".save-toast, .skript-hint, .faq-count, .fl-desc classes exist"
    - "Multi-entry row helpers .pe-entry-list, .pe-entry-row, .pe-entry-input, .pe-entry-textarea, .pe-entry-btn-rm exist"
  artifacts:
    - path: "static/nerve.css"
      provides: "All new CSS variables and classes for 08.19.2"
      contains: "--success-green"
  key_links:
    - from: "static/nerve.css :root"
      to: "templates/profile_editor.html"
      via: "var(--success-green), var(--danger-red), var(--info-blue)"
      pattern: "--success-green"
---

<objective>
Add new CSS variables and all new CSS classes to nerve.css that the profile_editor.html refactor in Plans 02-04 depends on.

Purpose: nerve.css is the single source of truth (CONTEXT.md D-08, CLAUDE.md constraint). No inline styles may be added to the template — all new styling must land here first so Plans 02-04 can reference classes directly.

Output: nerve.css with 3 new :root variables and ~15 new classes. Zero changes to profile_editor.html.
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/dev/salesnerve/.planning/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<!-- Existing nerve.css :root variables (verified, RESEARCH.md Q2):
  --page-text-color: #1a1a1a
  --page-text-secondary: #6B7280
  --page-text-muted: #9CA3AF
  --page-bg: #F0F2F5
  --glass-bg: #FFFFFF
  --glass-border: #E2E8F0       ← use as --border-light
  --glass-border-hover: #CBD5E1 ← use as --border-medium
  --input-border-focus: #00D4AA ← same as --n-accent
  --input-bg: #FFFFFF
  --input-padding: 12px 16px
  --label-color: #1a1a1a

  MISSING (add to :root):
  --success-green: #22c55e
  --danger-red:    #EF4444
  --info-blue:     #4899c8
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new CSS variables to nerve.css :root block</name>
  <files>static/nerve.css</files>
  <read_first>
    - static/nerve.css (full read — locate :root block end, verify no duplicate variable names before inserting)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-RESEARCH.md (Q2 section, lines 306-341 — exact variable list)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-UI-SPEC.md (Color section — semantic state colors table)
  </read_first>
  <action>
    Read nerve.css, find the closing `}` of the `:root { ... }` block (around line 100). Insert the following three variables immediately before the closing `}` of :root:

    ```css
    /* Semantic state colors — added in 08.19.2 */
    --success-green:  #22c55e;   /* Save-Toast, Erfolg-Indikatoren */
    --danger-red:     #EF4444;   /* Delete-Buttons, Fehler-Borders */
    --info-blue:      #4899c8;   /* btn-add Basis-Farbe */
    ```

    Do NOT add --border-light or --border-medium — use existing --glass-border and --glass-border-hover instead (they are semantically equivalent per RESEARCH.md Q2).

    Do NOT modify any existing :root variables.
  </action>
  <verify>
    <automated>grep -n "success-green\|danger-red\|info-blue" C:/Users/andre/dev/salesnerve/static/nerve.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "success-green" nerve.css returns at least 1 (in :root)
    - grep -c "danger-red" nerve.css returns at least 1 (in :root)
    - grep -c "info-blue" nerve.css returns at least 1 (in :root)
    - No duplicate definitions: grep -c "success-green" returns exactly 1
  </acceptance_criteria>
  <done>All three variables present in nerve.css :root. No duplicate definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Add new CSS classes to nerve.css</name>
  <files>static/nerve.css</files>
  <read_first>
    - static/nerve.css (after Task 1 — find the end of the file or a logical section break to append the new classes)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-UI-SPEC.md (Component Inventory section — "New classes to add to nerve.css" table, lines 142-160)
    - .planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-RESEARCH.md (Code Examples section, lines 570-685 — exact CSS with values)
  </read_first>
  <action>
    Append the following CSS block at the END of nerve.css (after all existing rules). Add the comment header shown. Use exact values from UI-SPEC.md and RESEARCH.md Code Examples section:

    ```css
    /* ── Profile Editor 08.19.2 ──────────────────────────────────────────── */

    /* Typography corrections */
    .sec-title {
      font-size: 17px;
      font-weight: 600;
      letter-spacing: 0;
      text-transform: none;
      color: var(--page-text-color);
      margin-bottom: 18px;
      padding-bottom: 8px;
      border-bottom: 1px solid var(--glass-border);
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .block-lbl {
      font-size: 13px;
      font-weight: 600;
      color: var(--page-text-color);
    }

    /* Education inline helpers */
    .field-desc {
      font-size: 12px;
      font-weight: 400;
      color: var(--page-text-muted);
      line-height: 1.5;
      margin-top: 3px;
      margin-bottom: 5px;
      display: block;
    }

    .sec-hint {
      font-size: 12px;
      font-weight: 400;
      color: var(--page-text-muted);
      line-height: 1.5;
      margin-bottom: 16px;
      display: block;
    }

    /* Inline-style extractions */
    .save-toast {
      font-size: 13px;
      color: var(--success-green);
    }

    .skript-hint {
      font-size: 12px;        /* per UI-SPEC.md Component Inventory */
      color: var(--page-text-muted);
      margin: 2px 0 6px 44px;
    }

    .faq-count {
      font-size: 12px;
      color: var(--page-text-secondary);
    }

    .fl-desc {
      font-size: 12px;
      color: var(--page-text-muted);
      margin-top: 4px;
    }

    /* Grid variants — extract col-header inline styles */
    .pe-col-grid-2 {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
    }

    .pe-col-grid-3 {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr auto;
      gap: 8px;
    }

    .pe-col-grid-skript {
      display: grid;
      grid-template-columns: 36px 1fr 1fr auto auto;
      gap: 8px;
    }

    /* Wisdom-Prefill-Stub — Branche section #1 */
    .wisdom-stub-box {
      background: rgba(0, 212, 170, 0.06);
      border: 1px solid var(--glass-border);
      border-radius: 8px;
      padding: 16px;
      margin-top: 16px;
    }

    .wisdom-stub-box p {
      font-size: 13px;
      color: var(--page-text-secondary);
      line-height: 1.6;
      margin: 0 0 12px 0;
    }

    .wisdom-stub-btn {
      border: 1px solid rgba(0, 212, 170, 0.5);
      background: transparent;
      color: var(--input-border-focus);
      border-radius: 6px;
      padding: 8px 16px;
      font-size: 13px;
      cursor: not-allowed;
      opacity: 0.6;
      font-family: inherit;
      display: inline-block;
    }

    /* Section number badge — badge micro exception (outside type scale) */
    .sec-title-num {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 22px;
      height: 22px;
      background: var(--input-border-focus);
      color: #fff;
      border-radius: 50%;
      font-size: 10px;
      font-weight: 600;
      flex-shrink: 0;
    }

    /* Multi-entry wrappers — margin-top via class, not inline style (D-08) */
    .erlaubnis-entry,
    .pitch-entry {
      margin-top: 8px;
    }

    /* Multi-entry row helpers for Erlaubnisfrage/Pitch */
    .pe-entry-list { display: flex; flex-direction: column; gap: 6px; }
    .pe-entry-row { display: flex; gap: 6px; align-items: center; }
    .pe-entry-input { flex: 1; }
    .pe-entry-textarea { flex: 1; min-height: 60px; }
    .pe-entry-btn-rm { align-self: flex-start; }
    ```

    IMPORTANT: Check if `.sec-title` already exists in nerve.css before appending. If it exists, update the existing rule with the new values instead of appending a duplicate. Do the same for `.block-lbl`. Use grep to check first.

    ALSO: Verify that the template `<style>` block in profile_editor.html defines `.sec-title` at 12px — that local override will be removed in Plan 02 (Task 1). The nerve.css rule added here will then take effect.
  </action>
  <verify>
    <automated>grep -n "field-desc\|sec-hint\|wisdom-stub-box\|wisdom-stub-btn\|pe-col-grid-2\|pe-col-grid-skript\|save-toast\|skript-hint\|faq-count\|sec-title-num\|pe-entry-list\|pe-entry-row" C:/Users/andre/dev/salesnerve/static/nerve.css</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "font-size: 17px" nerve.css returns a line inside .sec-title
    - grep -n "text-transform: none" nerve.css returns a line inside .sec-title
    - grep -n "font-size: 12px" nerve.css returns at least 2 lines (.field-desc, .sec-hint)
    - grep -n "font-size: 12px" nerve.css includes .skript-hint (12px per UI-SPEC)
    - grep -n "wisdom-stub-box" nerve.css returns 1 match
    - grep -n "pe-col-grid-skript" nerve.css returns 1 match
    - grep -n "sec-title-num" nerve.css returns 1 match
    - grep -n "erlaubnis-entry" nerve.css returns 1 match
    - grep -n "pe-entry-list" nerve.css returns 1 match
    - grep -n "pe-entry-row" nerve.css returns 1 match
    - No duplicate .sec-title block: grep -c "^.sec-title {" nerve.css returns 1
  </acceptance_criteria>
  <done>
    All 13 new classes present in nerve.css (plus .erlaubnis-entry/.pitch-entry spacing helpers and .pe-entry-* row helpers).
    .sec-title: font-size 17px, font-weight 600, text-transform none.
    .block-lbl: font-size 13px, font-weight 600.
    .field-desc and .sec-hint: font-size 12px, color var(--page-text-muted).
    .skript-hint: font-size 12px (per UI-SPEC.md Component Inventory).
    .wisdom-stub-btn: cursor not-allowed, opacity 0.6.
    .erlaubnis-entry, .pitch-entry: margin-top 8px (for Plan 03 HTML use).
    .pe-entry-list, .pe-entry-row, .pe-entry-input, .pe-entry-textarea, .pe-entry-btn-rm: flex layout helpers for multi-entry rows (no inline styles needed in Plan 03 JS).
    No duplicate class definitions in the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file → browser render | Static asset, no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.2-01 | Tampering | nerve.css duplicate .sec-title | mitigate | Read file first, check for existing .sec-title before appending — prevent specificity conflict |
</threat_model>

<verification>
After Plan 01 completes:
- `grep -c "success-green" static/nerve.css` returns 1 (in :root, not duplicated)
- `grep -n "font-size: 17px" static/nerve.css` returns a line in .sec-title
- `grep -n "wisdom-stub-box" static/nerve.css` returns a match
- `grep -n "pe-col-grid-skript" static/nerve.css` returns a match
- `grep -n "pe-entry-list" static/nerve.css` returns a match
</verification>

<success_criteria>
nerve.css is extended with:
1. Three new :root variables (--success-green, --danger-red, --info-blue)
2. Typography corrections for .sec-title (17px/600) and .block-lbl (13px/600)
3. Education helper classes .field-desc and .sec-hint
4. Inline-style extraction classes (.save-toast, .skript-hint, .faq-count, .fl-desc)
5. Grid helpers (.pe-col-grid-2, .pe-col-grid-3, .pe-col-grid-skript)
6. Wisdom stub classes (.wisdom-stub-box, .wisdom-stub-btn)
7. Section number badge (.sec-title-num)
8. Multi-entry row helpers (.pe-entry-list, .pe-entry-row, .pe-entry-input, .pe-entry-textarea, .pe-entry-btn-rm)

No changes to profile_editor.html. No new CSS files created.
</success_criteria>

<output>
After completion, create `.planning/phases/08.19.2-profil-editor-ux-design-aufraemung/08.19.2-01-SUMMARY.md`
</output>
