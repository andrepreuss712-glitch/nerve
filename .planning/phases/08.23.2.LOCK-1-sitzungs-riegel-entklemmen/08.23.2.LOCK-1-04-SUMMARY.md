---
phase: 08.23.2.LOCK-1
plan: 04
subsystem: live-session-locking
tags: [observability, threading, watchdog, faulthandler, lock-instrumentation]
requires:
  - "08.23.2.LOCK-1-03 (Welle 2 — der Fix: riegel-freier Ton-Weg, begrenzte Erwerbe)"
  - "services/slow_lane.py register_periodic_tick_hook (Bestand, unveraendert)"
provides:
  - "_TracedLock — Halter-Aufzeichnung auf _session_state_lock (Faden-Name, Kennung, Uebernahme-Zeit)"
  - "_lockwatch_tick + register_lockwatch_hook — [LOCKWATCH]-Meldung bei klemmendem Riegel (~30s)"
  - "faulthandler auf SIGUSR1 (all_threads=True, chain=True) — Stapel-Abzug ohne py-spy"
  - "tests/test_lockwatch_watchdog.py — 6 Verhaltens-Tests"
affects:
  - "services/live_session.py (eine Zeile geaendert + zwei angehaengte Bloecke)"
  - "app.py (Hook-Registrierung + faulthandler-Block)"
tech-stack:
  added: []
  patterns:
    - "Aufsatz-Riegel statt Riegel-Ordnung: __enter__/__exit__ ersetzt 1 Zeile, 97 with-Stellen unberuehrt"
    - "Drosselung ueber Zaehler modulo N im bestehenden Slow-Lane-Tick (kein zweiter Timer)"
    - "stdlib faulthandler statt Zusatz-Werkzeug py-spy (Punkt 27)"
key-files:
  created:
    - tests/test_lockwatch_watchdog.py
  modified:
    - services/live_session.py
    - app.py
decisions:
  - "SIGUSR1 statt SIGUSR2 — USR2 loest gunicorn-Arbiter-Re-Exec aus und toetet den Worker (SIG_DFL)"
  - "chain=True explizit — Default ist False; ohne Chain kapern wir gunicorns handle_usr1"
  - "Aufzeichnung bei JEDEM Erwerb (nicht nur bei Konkurrenz) — der minutenlange Halter erwirbt unkonkurriert"
  - "Halter-Felder werden VOR self._lock.release() geloescht (Race gegen den naechsten Halter)"
metrics:
  tasks: 3
  commits: 4
  files-changed: 3
  completed: 2026-07-31
---

# Phase 08.23.2.LOCK-1 Plan 04: Der Wachhund (Welle 3) Summary

Der Sitzungs-Riegel zeichnet ab jetzt auf, **wer** ihn haelt und **seit wann**, ein
`[LOCKWATCH]`-Tick meldet ein Klemmen laenger als 2 Sekunden spaetestens ~30 Sekunden
spaeter mit Faden-Name und Uebernahme-Zeit, und ein vollstaendiger Stapel-Abzug ist ohne
Zusatz-Werkzeug moeglich (`sudo systemctl kill -s SIGUSR1 nerve`) — bei einer einzigen
geaenderten Bestands-Zeile.

## Commits

| # | Hash | Aufgabe | Dateien |
|---|---|---|---|
| 1 | `818804e` | Teil 3 — Aufsatz-Riegel mit Halter-Aufzeichnung (eine Zeile) | `services/live_session.py` |
| 2 | `cee1fe1` | Teil 3 — `[LOCKWATCH]`-Wachhund (30s-Tick, Halter-Name + Uebernahme-Zeit) | `services/live_session.py`, `app.py` |
| 3 | `65acdb5` | Teil 3 — `faulthandler` auf SIGUSR1 (chain=True, NIEMALS USR2) | `app.py` |
| 4 | `f8e1d96` | Verhaltens-Tests fuer Aufsatz-Riegel und Wachhund | `tests/test_lockwatch_watchdog.py` |

Vier atomare Commits, wie im Plan gefordert. `git log -1 --stat` zeigte je genau die oben
gelisteten Dateien (Commit 2 planmaessig zwei).

## Task 1 — Aufsatz-Riegel

### Kompatibilitaets-grep VOR der Aenderung (Pflicht, verbatim)

```
$ grep -rn "_session_state_lock\." services/ routes/ tests/ scripts/ app.py
services/deepgram_service.py:580:                  f"Deepgram-Lausch-Faden haengt vermutlich am _session_state_lock. Wir "
services/deepgram_service.py:914:        if ls._session_state_lock.acquire(timeout=ls._LOCK_PROBE_TIMEOUT_S):
services/deepgram_service.py:918:                ls._session_state_lock.release()
services/live_session.py:171:    got = _session_state_lock.acquire(timeout=timeout)
services/live_session.py:173:        _session_state_lock.release()
services/live_session.py:217:# ALLE 6 Writer (deepgram:2, claude:4) schreiben per-SID unter _session_state_lock.
services/live_session.py:254:# BEIDE Reader (app_routes:244 + claude:206) jetzt per-SID unter _session_state_lock.
services/live_session.py:559:    if _session_state_lock.acquire(timeout=_LOCK_PROBE_TIMEOUT_S):
services/live_session.py:567:            _session_state_lock.release()
services/live_session.py:578:    if _session_state_lock.acquire(timeout=_LOCK_PROBE_TIMEOUT_S):
services/live_session.py:582:            _session_state_lock.release()
services/live_session.py:626:    if not _session_state_lock.acquire(timeout=_LOCK_PROBE_TIMEOUT_S):
services/live_session.py:640:        _session_state_lock.release()
services/live_session.py:1046:    - Pop aus _session_state[sid]['_merge_pending'][key] unter _session_state_lock.
services/live_session.py:1145:    LATENZ (Punkt 25, HART): NUR ein list.append unter _session_state_lock.
Binary file services/__pycache__/deepgram_service.cpython-311.pyc matches
Binary file services/__pycache__/live_session.cpython-311.pyc matches
tests/test_persid_04_merge_pending.py:235:        "GENAU EIN Lock = _session_state_lock."
tests/test_session_lock_blocking_calls_guard.py:428:            if ls._session_state_lock.acquire(timeout=2.0):
tests/test_session_lock_blocking_calls_guard.py:432:                    ls._session_state_lock.release()
tests/test_session_lock_blocking_calls_guard.py:457:                if not _session_state_lock.acquire(timeout=2.0):
tests/test_session_lock_blocking_calls_guard.py:463:                    _session_state_lock.release()
tests/test_session_lock_blocking_calls_guard.py:467:                if ls._session_state_lock.acquire(timeout=2.0):
tests/test_session_lock_blocking_calls_guard.py:477:                if not _session_state_lock.acquire(timeout=0.05):
tests/test_session_lock_blocking_calls_guard.py:483:                    _session_state_lock.release()
Binary file tests/__pycache__/... (weitere stale .pyc)
```

**Befund = Erwartung.** Genau die fuenf echten Nutzungen aus Plan 03
(`wait_session_state_lock_free` :171/:173, `stash_ended_session` :626/:640,
`pop_session_state` 2× :559/:567 und :578/:582, plus cross-modul
`handle_disconnect` in `deepgram_service.py:914/:918`) — durchgehend
`acquire(timeout=…)` + `release()`. Restliche Treffer sind Kommentare, Test-Code und
stale `.pyc`. **Keine exotische Methode** (`.locked()`, `acquire()` ohne Timeout,
`with`-fremde Nutzung) im Bestand — `_TracedLock` bietet `acquire`/`release`/`locked`/
`__enter__`/`__exit__` und deckt damit alles ab.

### Ist-Stand der Zeilennummern (Plan nannte 224-227, tatsaechlich)

Die Riegel-Definition lag bei **293** (`_session_state_lock = threading.Lock()`), der
Abschnitts-Kommentar bei 291, `_per_sid_lock` bei 289, `_ended_snapshots_lock` bei 287 —
also gegenueber dem Plan-Stand (224-227) um ~66 Zeilen verschoben. Die **Struktur** war
identisch; geaendert wurde trotzdem exakt die eine Zeile.

### Anker (alle geprueft)

| Anker | Soll | Ist |
|---|---|---|
| `class _TracedLock:` | 1 | **1** |
| `_session_state_lock = _TracedLock('_session_state_lock')` | 1 | **1** |
| `_session_state_lock = threading.Lock()` | 0 | **0** |
| `_per_sid_lock = threading.Lock()` | 1 | **1** |
| `def acquire(self, blocking: bool = True, timeout: float = -1) -> bool:` | 1 | **1** |
| `def __enter__(self):` / `def __exit__(self, exc_type, exc, tb):` / `def locked(self) -> bool:` | je 1 | **je 1** |
| `REIHENFOLGE IST LOAD-BEARING` | 1 | **1** |
| `KEIN RLock` | ≥1 | **1** |
| `__slots__ = ('_lock', '_name', 'holder_thread', 'holder_ident',` | 1 | **1** |
| `time.time()` innerhalb des Klassen-Blocks | 0 | **0** |
| `RLock(` in der ganzen Datei | 0 | **0** |

### Laeufe

```
$ python -m pytest tests/test_live_session_ghost_sid.py tests/test_persid_concurrency.py \
                   tests/test_persid_04_merge_pending.py tests/test_no_live_global_state.py \
                   tests/test_per_sid_migration.py -q
26 passed, 1 skipped in 1.43s
```

Waechter-2-Zaehlung nach Task 1 **unveraendert** wie gefordert:
`services/live_session.py: 25 (with=22, try/finally=3)`, `services/deepgram_service.py:
22 (with=21, try/finally=1)`, SUMME **101**. Der Aufsatz-Riegel wird korrekt **nicht**
als ueberwachter Block mitgezaehlt (sein internes `self._lock.release()` traegt den
Attribut-Namen `_lock`).

## Task 2 — Der Wachhund

### Anker

| Anker | Datei | Soll | Ist |
|---|---|---|---|
| `[LOCKWATCH] _session_state_lock >2s belegt` | `live_session.py` | 1 | **1** |
| `_LOCKWATCH_ACQUIRE_TIMEOUT_S = 2.0` | `live_session.py` | 1 | **1** |
| `_LOCKWATCH_EVERY_N_TICKS = 6` | `live_session.py` | 1 | **1** |
| `Faden=` / `Uebernahme=` | `live_session.py` | je 1 | **je 1** |
| `sudo systemctl kill -s SIGUSR1 nerve` | `live_session.py` | 1 | **2** ⚠ (siehe Abweichung 1) |
| `pass    # P-3: im Erfolgsfall unter dem Riegel NICHTS tun` | `live_session.py` | 1 | **1** |
| `_session_state_lock.release()` | `live_session.py` | ≥5 (genau 5) | **5** |
| `def register_lockwatch_hook` | `live_session.py` | 1 | **1** |
| `_lockwatch_tick in _PERIODIC_TICK_HOOKS` | `live_session.py` | 1 | **1** |
| `[LOCKWATCH] Herzschlag:` | `live_session.py` | 1 | **1** |
| `register_lockwatch_hook` | `app.py` | ≥1 | **1** |

### Zeilennummern-Belege (`app.py`, nach dem Task-2-Insert)

| Anker | Zeile | Bedingung |
|---|---|---|
| `if _os_guard.environ.get('NERVE_TESTING') != '1':` (erster Treffer) | **2410** | — |
| `_reg_lockwatch()` | **2430** | > 2410 ✓ (**innerhalb** des Test-Guards) |
| `threading.Thread(target=slow_lane_consumer, daemon=True).start()` | **2434** | 2430 < 2434 ✓ (**vor** dem Consumer-Start → das Start-Beleg-Log zaehlt ihn mit) |

`git status --porcelain services/slow_lane.py services/deepgram_service.py routes/
database/ alembic/` → **leer**. Die Registry wurde nicht geaendert, der Hook haengt sich an.

### Waechter 2 nach Task 2 — steigt planmaessig um genau einen Block (verbatim)

```
[LOCK-1 Waechter 2] Ist-Zaehlung der ueberwachten Riegel-Bloecke:
  routes/app_routes.py: 4 (with=4, try/finally=0)
  routes/learning.py: 2 (with=2, try/finally=0)
  services/claude_service.py: 41 (with=41, try/finally=0)
  services/cost_tracker.py: 2 (with=2, try/finally=0)
  services/deepgram_service.py: 22 (with=21, try/finally=1)
  services/einwand_keyword_matcher.py: 2 (with=2, try/finally=0)
  services/live_session.py: 26 (with=22, try/finally=4)
  services/prompt_pipeline.py: 3 (with=3, try/finally=0)
  SUMME: 102 (with=97, try/finally=5) in 8 Dateien
5 passed in 0.46s
```

**`services/live_session.py: 26 (with=22, try/finally=4)`, SUMME 102** — exakt wie im
Plan gefordert. Der Wachhund wird also **mitgezaehlt und mitbewacht**; die deliberate
False-Green-Falle (Wachhund waere nicht als `try/finally`-Block erkannt worden) hat
**nicht** zugeschnappt. `_SOLL_MINDESTENS` wurde **nicht** angefasst.

```
$ python -m pytest tests/test_no_live_global_state.py tests/test_live_session_ghost_sid.py \
                   tests/test_persid_concurrency.py -q
17 passed in 0.37s
```

## Task 3 — `faulthandler` auf SIGUSR1 + Verhaltens-Tests

### Anker `app.py`

| Anker | Soll | Ist |
|---|---|---|
| `NIEMALS SIGUSR2` | 1 | **1** |
| `_fh.register(signal.SIGUSR1, all_threads=True, chain=True)` | 1 | **1** |
| `grep -c "SIGUSR2" app.py` | ≥1 | **2** (die zwei vorgeschriebenen Warn-Zeilen) |
| `signal.SIGUSR2` | 0 | **0** (nur Prosa, kein Aufruf) |
| `[LOCKWATCH] faulthandler auf SIGUSR1 registriert` | 1 | **1** |
| `[LOCKWATCH] faulthandler NICHT registriert` | 1 | **1** |
| `AttributeError) as _fh_e` | 1 | **1** |
| `inspect.sh logs 3000` | ≥1 | **2** |
| `^import signal` | 1 Treffer | **1** (Zeile 5, kein Doppel-Import) |

**Reihenfolge-Beleg:** `_fh.register(signal.SIGUSR1` steht bei **2537**,
`signal.signal(signal.SIGTERM, _graceful_sigterm)` bei **2497** → 2537 > 2497 ✓, der
neue Block steht **nach** dem SIGTERM-Block und **vor** dem `# ────`-Trenner.
Der korrigierte Plan-Anker (nach der SIGTERM-`print`-Zeile, vor dem Trenner) wurde
befolgt; die absoluten Zeilennummern lagen wegen des Task-2-Inserts (+11 Zeilen) und
Bestands-Drift bei **2508/2509** statt der im Plan genannten 2491/2492 — die
**Struktur** stimmte exakt, deshalb wurde per Text-Anker eingefuegt statt per Zeilennummer.
`python -c "import ast; ast.parse(...)"` auf `app.py` → OK.

### Anker Test-Datei

| Anker | Soll | Ist |
|---|---|---|
| `LOCKWATCH-testhalter` | ≥2 | **2** |
| `acquire(timeout=0.2)` | ≥1 | **2** |
| `P-3` | ≥1 | **2** |
| `pytest.mark` | 0 | **0** |
| `inspect.getsource` | 0 | **0** |

### `tests/test_lockwatch_watchdog.py` — verbatim

```
$ python -m pytest tests/test_lockwatch_watchdog.py -q --durations=5
......                                                                   [100%]
============================= slowest 5 durations =============================
0.21s call     tests/test_lockwatch_watchdog.py::test_lockwatch_tick_meldet_klemmenden_riegel
0.20s call     tests/test_lockwatch_watchdog.py::test_tracedlock_acquire_timeout_kehrt_mit_false_zurueck

(3 durations < 0.005s hidden.  Use -vv to show these durations.)
6 passed in 0.47s
```

**6 passed** — wie gefordert (der Plan-Checker hatte den fehlenden sechsten Test
`test_lockwatch_tick_drosselt` nachgetragen; er ist gebaut).

## Gesamt-Bilanz aller vier Waechter (verbatim)

```
$ python -m pytest tests/test_audio_path_lock_free_guard.py \
                   tests/test_session_lock_deadlock_guard.py \
                   tests/test_session_lock_blocking_calls_guard.py \
                   tests/test_lockwatch_watchdog.py -q -s
.[DG] audio_chunk #1 (sid=lock1-w3-unbekannt-002, bytes=100, type=bytes)
.[PiP] manual_ewb (sid=lock1-w1-ewb-001): zu_teuer
[LOCKWATCH] manual_ewb abgebrochen: _session_state_lock >2s belegt (sid=lock1-w1-ewb-001)
.[PiP] manual_ewb (sid=lock1-w1-ewb-001): zu_teuer
.ss.
[LOCK-1 Waechter 2] Ist-Zaehlung der ueberwachten Riegel-Bloecke:
  routes/app_routes.py: 4 (with=4, try/finally=0)
  routes/learning.py: 2 (with=2, try/finally=0)
  services/claude_service.py: 41 (with=41, try/finally=0)
  services/cost_tracker.py: 2 (with=2, try/finally=0)
  services/deepgram_service.py: 22 (with=21, try/finally=1)
  services/einwand_keyword_matcher.py: 2 (with=2, try/finally=0)
  services/live_session.py: 26 (with=22, try/finally=4)
  services/prompt_pipeline.py: 3 (with=3, try/finally=0)
  SUMME: 102 (with=97, try/finally=5) in 8 Dateien
..........
15 passed, 2 skipped in 3.78s
```

Die **2 skipped** sind die bekannten `api_beenden`-Haelften aus Waechter 1, die lokal
uebersprungen werden und im `deploy.sh`-Gate auf Prod laufen.

Bestand rund um Riegel und Nebenlaeufigkeit:

```
$ python -m pytest tests/test_live_session_ghost_sid.py tests/test_persid_concurrency.py \
                   tests/test_persid_04_merge_pending.py tests/test_no_live_global_state.py \
                   tests/test_per_sid_migration.py tests/test_stabil1_beenden_guard.py \
                   tests/test_mode_switch_event.py tests/test_deepgram_send_failure_log.py -q
34 passed, 9 skipped in 1.30s
```

Nichts angefasst, was nicht angefasst werden durfte:

```
$ git status --porcelain database/ alembic/ static/ templates/ deploy/ requirements.txt services/slow_lane.py
(leer)
```

## Abweichungen vom Plan

### 1. `sudo systemctl kill -s SIGUSR1 nerve` in `live_session.py` → **2** statt geforderter **1**

- **Gefunden bei:** Task 2, Anker-Pruefung.
- **Befund (Kriterium NICHT aufgeweicht, gemeldet):** die zweite Fundstelle ist
  **Bestand aus Welle 2** — `stash_ended_session` (`live_session.py:710`, Commit
  `9d5619a`, Teil 2c) druckt denselben Abzug-Befehl in seiner Riegel-Proben-Meldung.
  Mein Wachhund traegt genau **eine** Fundstelle bei (`:1543`).
- **Bewertung:** das Plan-Kriterium `→ 1` wurde gegen den Stand **vor** Plan 03
  formuliert und ist am Ist-Stand nicht erfuellbar, ohne eine korrekte Bestands-Zeile zu
  loeschen. Beide Vorkommen sind gewollt und inhaltlich richtig. **Keine Code-Aenderung,
  keine Kriterien-Aenderung** — Befund gemeldet.

### 2. `LOCKWATCH-testhalter` stand zunaechst nur **1×** in der Test-Datei

- **Gefunden bei:** Task 3, Anker-Pruefung (Soll ≥2, Ist 1).
- **Ursache:** ich hatte den Halter-Namen als Konstante `_HALTER_NAME` gefuehrt und in
  der Assertion die Konstante verwendet — das waere ausserdem eine Assertion **gegen sich
  selbst** gewesen (Konstante auf beiden Seiten).
- **Fix (Rule 1 — Code geaendert, nicht das Kriterium):** die Assertion prueft jetzt das
  **Literal** `'LOCKWATCH-testhalter'`, mit Kommentar zur Begruendung. Anker danach: 2.

### 3. Zeilennummern der Plan-Anker verschoben (kein Eingriff noetig)

- `live_session.py`: Riegel-Definition lag bei **293** statt 227 (Plan-Stand).
- `app.py`: SIGTERM-`print` endete bei **2508** statt 2491, Trenner bei **2509** statt
  2492 (Bestands-Drift + 11 Zeilen aus meinem eigenen Task-2-Insert).
- **Umgang:** alle Einfuegungen ueber eindeutige **Text-Anker** statt Zeilennummern; die
  vom Plan beschriebene Struktur (Nachbar-Zeilen, Reihenfolge) wurde vorher jeweils
  verifiziert und stimmte exakt. Keine inhaltliche Abweichung.

Keine weiteren Abweichungen. Keine Rule-4-Situation, keine Auth-Gates.

## Punkt-Pflichten

- **Punkt 23 (Schild): nicht anwendbar** — geprueft, nicht angenommen: keine neue
  Tabelle, keine neue Spalte, kein neuer/entfallender Leser- oder Schreiber-Pfad einer
  Tabelle. Der Wachhund liest **keine** Datenbank, der Aufsatz-Riegel arbeitet
  ausschliesslich im RAM, `faulthandler` schreibt nach stderr. **Keine Alembic-Migration**,
  `database/models.py` unveraendert.
- **Punkt 21/22/26:** Skip-OK (kein DB-/Schema-/Route-Pfad).
- **Punkt 25 (Latenz):** benannt im Plan und als Docstring im Code — ~1-2 µs pro Erwerb,
  grob **1,8 ms pro Minute und Anruf** (Budget < 5 ms/min/Anruf). Der 10-Hz-Ton-Weg nimmt
  diesen Riegel seit Welle 2 **gar nicht mehr**. Es bleibt eine **Abschaetzung**, keine
  Messung (`[ASSUMED]` A6 der RESEARCH) — Cross-AI-Punkt.
- **Punkt 28:** die Halter-Felder und die drei Wachhund-Zaehler sind veraenderlicher
  Modul-Zustand, beschreiben aber **den Riegel/den Prozess** — keine sid, keine `user_id`,
  keine `org_id`. Begruendung steht im Klassen- bzw. Block-Docstring;
  `tests/test_no_live_global_state.py` bleibt gruen.
- **Umlaut-Regel:** User-facing-Text gibt es hier nicht; Log-Zeilen und Kommentare sind
  bewusst ASCII (`Uebernahme`, `Pruefungen`) — Journal-sicher, keine Code-Identifier mit
  Umlauten.

## Was Andre nach dem Deploy im Journal sehen MUSS

`bash scripts/inspect.sh logs 200` — **drei** Beleg-Zeilen. Fehlt eine davon, ist der
entsprechende Teil **stumm** und damit wirkungslos:

1. `[SLOW] call-end-steps registriert: … | periodic-hooks: 2`  ← **2** statt 1
2. `[LOCKWATCH] Wachhund registriert: alle ~30s eine Probe mit 2.0s Zeitlimit`
3. `[LOCKWATCH] faulthandler auf SIGUSR1 registriert (all_threads=True, chain=True) — Abzug: sudo systemctl kill -s SIGUSR1 nerve, lesen: bash scripts/inspect.sh logs 3000`

**Test-Anruf:** Anruf starten, sprechen, einen EWB-Knopf druecken, auflegen. Erwartet:
Transkript, Auswertung und `nova-3`-Kostenzeile sind da, `[Beenden]` antwortet, und
**keine** `[LOCKWATCH] … belegt`-Zeile im Journal.

**Optionaler Einmal-Beleg (empfohlen):** `sudo systemctl kill -s SIGUSR1 nerve`, dann
`bash scripts/inspect.sh logs 3000` — der Abzug muss im Journal stehen und der Dienst
danach **weiterlaufen** (`inspect.sh service-status`). Das ist der Beweis, dass die
Signal-Wahl richtig war. **Niemals SIGUSR2.**

## Executor-Mandat (was NICHT getan wurde)

Kein `deploy.sh`, kein tar-Upload, kein `systemctl`, kein Neustart, **keine SSH-Befehle
auf den Prod-Server**, keine Migration. `git push origin main` wurde ausgefuehrt
(Andre-Direktive: Push ≠ Deploy; ueberschreibt das `KEIN git push` des Plan-Textes).
Lokaler pytest ist **Vorabsignal**, nicht Abnahme.

## Naechster Schritt

Die Phase ist **code-vollstaendig** (4/4 Plans). Vor dem Deploy steht **Cross-AI-Review
(PFLICHT bei 🟡 + Live-Pfad, CONTEXT `<decisions>`)** — insbesondere fuer die
Latenz-Abschaetzung des Aufsatz-Riegels und die Punkt-27-Begruendung der Riegel-Probe.
Danach Uebergabe an Andre: `bash deploy.sh production` → Journal-Beleg (drei Zeilen) →
Test-Anruf.

## Self-Check: PASSED

- `services/live_session.py` FOUND, `app.py` FOUND, `tests/test_lockwatch_watchdog.py` FOUND
- Commits `818804e`, `cee1fe1`, `65acdb5`, `f8e1d96` FOUND in `git log`
