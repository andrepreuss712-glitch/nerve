# Phase 08.23.2.LOCK-1: Sitzungs-Riegel entklemmen — Context

**Gathered:** 2026-07-30
**Status:** Ready for planning
**Source:** André-Direktive im `/gsd-insert-phase`-Auftrag (2026-07-30) — vollständig gelockt, KEIN discuss-phase nötig

<domain>
## Phase Boundary

**IN SCOPE — genau drei Teile, nicht mehr:**
1. `get_sid_paused` (`services/live_session.py:105-108`) nimmt den globalen `_session_state_lock` nicht mehr.
2. Das Auflegen (`handle_stop_live_session` → `_close_deepgram_connection` → Deepgram `finish()`) kann nicht mehr unbegrenzt warten.
3. Ein Wachhund meldet, wenn der Riegel doch klemmt — inklusive Faden-Name und Übernahme-Zeit — plus `faulthandler` für einen Stapel-Abzug ohne Zusatz-Werkzeug.

Dazu die drei Pflicht-Wächter (siehe `<decisions>`).

**OUT OF SCOPE:**
- Kein Refactor der ~60 anderen `with _session_state_lock:`-Stellen (CLAUDE.md Punkt 17). Sie sind auditiert und unauffällig.
- Keine Umstellung auf feingranulare per-sid-Riegel als Architektur-Umbau. Das wäre ein eigener Brocken.
- Keine Suche nach dem ursprünglichen Riegel-Halter durch statische Analyse — Fable hat das erschöpfend gemacht, Ergebnis: nicht auffindbar. Teil 3 findet ihn zur Laufzeit.
- Kein Deploy, kein Push. Claudian fährt Deploy + Test-Anruf.
</domain>

<decisions>
## Implementation Decisions

### Beweislage — gelockt, NICHT neu diagnostizieren

Die Wurzel ist **am laufenden Prozess** per `py-spy dump` (PID 2335884) bewiesen, nicht erschlossen.
Abzug: `root@178.104.82.166:/root/dump_2026-07-30_lock-deadlock.txt`.

**Anruf 30.07., sid `5Y-0MFlm_ITb1cupAAAB`:**
```
09:27:56  letzte Coaching-Zeile
--- ab hier absolute Stille ---
09:28:07  manual_ewb "Hat Partner"  -> kein Start, keine Fehlermeldung
09:29:11 / 09:29:55 / 09:30:07     -> dito (3 weitere Klicks)
09:30:18  [Beenden] ENTRY          -> und dann nichts mehr
```
Falsifikation bestanden: ab 09:28:07 NULL Treffer für `[Claude-1]`, `[Claude-2]`, `[KW]`, `[MOMENT]`, `[COUNTERPART]`.

**Frame-Häufigkeit im Abzug:** 1415× `get_sid_paused` → `live_session.py:107` (`with _session_state_lock:`); davon 1414× aus `handle_audio_chunk` (`deepgram_service.py:864`), 4× `handle_manual_ewb` (`:966`), 1× `api_beenden` (`app_routes.py:171`), 1× `handle_stop_live_session` (`:845`), 1× `coaching_loop`→`get_anonymisierer` (`live_session.py:313`), 1× `analyse_loop` (`claude_service.py:1322`). **Niemand hält den Riegel mit sichtbarem Python-Frame — alle 1416 warten.** Keine anonymization/gliner/torch-Frames.

### Wurzel 1 — globaler Riegel im 10-Hz-Takt

`get_sid_paused` nimmt denselben globalen Riegel wie Analyse, Coaching, Umschalter, Knopfdruck und Auflegen — bei **jedem** Ton-Brocken, also 10×/Sekunde (100ms-Frames, `deepgram_service.py:864`). Klemmt er einmal, stirbt die ganze Sitzung, und ohne Wächter: stumm.

**Gelockt:** Der Zugriff braucht den Riegel **nicht**. Die Funktion ist durchgängig mit `.get()`-Defaults geschrieben; riegel-freies Lesen kann höchstens einen um Millisekunden veralteten Ja/Nein-Wert liefern (harmlos — der nächste Brocken kommt in 100ms), niemals einen Fehler oder kaputte Daten.

### Wurzel 2 — echte Umklammerung beim Auflegen

Thread-2284: `_wait_for_tstate_lock` → `join` → `finish` (`deepgram/clients/common/v1/abstract_sync_websocket.py:468`) → `_close_deepgram_connection` (`deepgram_service.py:548`) → `handle_stop_live_session` (`:845`).
`finish()` wartet per `join()` **unbegrenzt** auf den Lausch-Faden, der in `on_message` → `get_sid_paused` am Riegel steht. Zwei warten aufeinander.

**Folge:** kein `conversation_logs`-Eintrag, kein Transkript, keine `nova-3`-Kostenzeile — und **kein 504 im App-Log** (die Anfrage endet nicht, sie bricht nur beim Browser ab; `gunicorn --timeout 120` greift bei blockierten Arbeits-Fäden nicht, weil der Herzschlag vom Haupt-Faden kommt).

### ⚠ Zwei Nachträge aus RESEARCH.md (2026-07-30) — überschreiben Annahmen in diesem Dokument

Diese Korrekturen stammen aus der Recherche am echten Code, nicht aus der Direktive. Wer plant oder baut, folgt **den Korrekturen**, nicht den überholten Stellen weiter unten:

1. **Es sind 98, nicht ~60 `with _session_state_lock:`-Stellen** (`claude_service.py` 40, `deepgram_service.py` 21, `live_session.py` 26, `app_routes.py` 4, `cost_tracker.py` 2, `prompt_pipeline.py` 2, `learning.py` 2, `einwand_keyword_matcher.py` 1). Wächter 2 muss über **alle** sweepen.
2. **Teil 1 allein löst die Verklemmung NICHT auf** — statisch widerlegt, nicht vermutet. Der Lausch-Faden nimmt den Riegel auf dem `on_message`-Weg **13-mal weiter**, nachdem `get_sid_paused` riegel-frei ist; Kronzeuge `services/deepgram_service.py:94` im `result.is_final`-Zweig, also bei jeder finalisierten Zeile. Die Abwägung in Teil 2 ist damit **entschieden: Weg (c)** — aber als **zwei disjunkte Defekte**, nicht als doppelte Absicherung desselben Defekts. Punkt 27 bleibt gewahrt.

### Ehrlich offen — bewusst so stehengelassen

**WER den Riegel ursprünglich nahm, ist UNBEKANNT.** Fable hat alle ~60 `with _session_state_lock:`-Stellen auditiert (`deepgram_service`, `claude_service`, `live_session`, `app_routes`, `learning`, `cost_tracker`, `prompt_pipeline`, `einwand_keyword_matcher`): jede ist ein kurzer RAM-Block, kein DB/Netz/LLM/emit unter dem Riegel, kein rohes `.acquire()`. Statisch nicht auffindbar.
Das ändert den Fix nicht — Wurzel 1+2 sind unabhängig davon falsch gebaut. **Teil 3 benennt den Halter beim nächsten Auftreten.**

### Teil 1 🟢 — `get_sid_paused` riegel-frei

- Riegel-Nahme entfernen, Docstring auf „bewusst riegel-frei" mit der Begründung (ms-veralteter Ja/Nein-Wert ist harmlos, nächster Brocken in 100ms).
- **PFLICHT-PRÜFUNG VORHER:** greppen, ob es **weitere** Riegel-Nahmen im Ton-Weg gibt. Bekannt: `handle_audio_chunk` nimmt zusätzlich `_sessions_lock` — **anderer Riegel, ok**; `_chunk_counts` ist riegel-frei. Ergebnis des greps belegen.

### Teil 2 🟡 — Auflegen mit Zeitlimit: ERST MESSEN, DANN BAUEN

**Drei Wege gegeneinander abwägen, nicht blind bauen:**
- **(a)** `finish()` in eigenem Faden mit Zeitlimit ~5s, danach ohne ihn weiter + tote Verbindung aus dem Verzeichnis nehmen.
- **(b)** Prüfen, ob **Teil 1 das Problem schon auflöst** — der Lausch-Faden hängt dann nicht mehr am Sitzungs-Riegel. Dann ist (a) evtl. unnötig.
- **(c)** Beides.

**Gelockte Haltung:** Nicht doppelt absichern, wo einer reicht (CLAUDE.md Punkt 27 / Leitsatz 2). **Ergebnis der Abwägung in `.planning/DIALOG-GSD-CLAUDIAN.md` dokumentieren** — mit Begründung, nicht als Behauptung.

### Teil 3 🟡 — Wachhund

- Periodischer Tick alle 30s, **analog zum bestehenden `[SLOW] requeue_pending`-Muster**, probiert `lock.acquire(timeout=2)`.
- Bei Fehlschlag: Log-Zeile `[LOCKWATCH] _session_state_lock >2s belegt` + **Faden-Name** + **Übernahme-Zeit**.
- Ergänzend `faulthandler` mit Signal-Auslöser, damit ein Stapel-Abzug **ohne Zusatz-Werkzeug** möglich ist. Hintergrund: `py-spy` musste am 30.07. erst installiert werden und liegt jetzt nur im venv, **nicht in `requirements.txt`**.

### Wächter — Pflicht, Test-Netz-Ratsche

1. **VERKLEMMUNGS-TEST, ERST ROT.** Test-Faden hält `_session_state_lock`; danach müssen `handle_manual_ewb` **UND** `api_beenden` innerhalb N Sekunden **MIT FEHLER** zurückkehren. Heute hängen sie ewig → **der Test MUSS am alten Stand rot sein, Rot-Beleg verbatim in den Commit.** Ein Test, der von Anfang an grün ist, beweist nichts.
2. **STATISCHE SPERRE.** Test, der in jedem `with _session_state_lock:`-Block blockierende Aufrufe verbietet: `get_session`, `SessionLocal`, `messages.create`/`messages.stream`, `sio.emit`, `requests.`, `sleep`, `join`.
3. **TON-WEG RIEGEL-FREI.** Regressions-Test, dass `get_sid_paused` den Sitzungs-Riegel nicht nimmt.

### Prozess-Regeln (gelockt)

- **CLAUDE.md Punkt 14** — vor jedem Insert 30 Zeilen davor/danach, Control-Flow, Cross-File-grep, Edge-Cases + die vier Race-Fragen. **Hier besonders relevant**, weil der Fix selbst Nebenläufigkeit anfasst.
- **CLAUDE.md Punkt 28** — Live-Pfad, pro-sid. Kein neuer modul-globaler veränderlicher Zustand für pro-Sitzungs-Daten (gilt auch für den Wachhund-Zustand).
- **CLAUDE.md Punkt 17** — kein Refactor nebenbei.
- **CLAUDE.md Punkt 25** — Latenz. Der Ton-Weg ist die schnelle Bahn; Teil 1 macht ihn schneller, Teil 3 darf ihn nicht belasten (30s-Tick, nicht pro Brocken).
- Atomare Commits.
- **Fragen und Entscheidungen ans ENDE von `.planning/DIALOG-GSD-CLAUDIAN.md`** — NICHT als interaktives Menü (André am Handy).
- **`autonomous: false`.** Nach dem Bau **NICHT deployen** — Deploy-Gate melden, Claudian fährt Deploy + Test-Anruf.
- **Cross-AI PFLICHT vor Execute** (🟡 + Live-Pfad).
- **Multi-Segment-Gotcha:** Pfade hardcoden, `gsd-tools`/`gsd-sdk`-Phasenauflösung umgehen, STATE/ROADMAP hand-editieren.

### Claude's Discretion

- Aufteilung in Plans und Wellen.
- Wo genau der Wachhund-Tick aufgehängt wird (bestehender Scheduler vs. eigener Daemon-Faden) — solange dem `[SLOW] requeue_pending`-Muster gefolgt wird.
- Welches Signal `faulthandler` nutzt (`SIGUSR1` naheliegend auf Linux).
- Konkreter Wert für „N Sekunden" im Verklemmungs-Test.
- Exakte Formulierung der Docstrings und Schutz-Kommentare.
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Betroffener Code
- `services/live_session.py` — `get_sid_paused:105-108` (Wurzel 1), `get_anonymisierer:313`, `_session_state` + `_session_state_lock`
- `services/deepgram_service.py` — `handle_audio_chunk:864` (10-Hz-Aufrufer), `handle_manual_ewb:966`, `handle_stop_live_session:845`, `_close_deepgram_connection:548`
- `routes/app_routes.py` — `api_beenden:171`
- `services/claude_service.py` — `analyse_loop:1322`

### Muster zum Nachbauen
- Das bestehende `[SLOW] requeue_pending`-Tick-Muster (Vorlage für den Wachhund-Tick) — im Code lokalisieren, nicht raten
- `deploy/nerve.service` — Faden-/Worker-Konfiguration (`--threads`), relevant für die Verklemmungs-Betrachtung

### Projekt-Regeln
- `./CLAUDE.md` — Punkt 14 (Pre-Insert-Control-Flow-Audit, 4 Schichten), Punkt 17, Punkt 25 (Latenz), Punkt 27 (einfachster tragfähiger Weg), Punkt 28 (kein globaler Live-Zustand), „HART: Kein Local-Dev" (lokales pytest ist **kein** Acceptance-Check)
- `routes/CLAUDE.md` — Routes-spezifische Regeln
- `.planning/DIALOG-GSD-CLAUDIAN.md` — Ziel für Fragen/Abwägungen (ans Ende anhängen)
</canonical_refs>

<specifics>
## Specific Ideas

- Der Log-Präfix ist auf `[LOCKWATCH]` festgelegt, die Zeile auf `[LOCKWATCH] _session_state_lock >2s belegt` + Faden-Name + Übernahme-Zeit.
- Der Wachhund-Tick nutzt `lock.acquire(timeout=2)` — ein Probier-Erwerb, kein Blockieren.
- `py-spy` liegt im venv, **nicht** in `requirements.txt`. Ein Neuaufsetzen des Servers verliert es. Der `faulthandler`-Teil existiert genau deshalb.
- Der py-spy-Abzug ist das **einzige Exemplar** des Beweises und liegt nur unter `/root` auf dem Prod-Server.
</specifics>

<deferred>
## Deferred Ideas

- Architektur-Umbau auf feingranulare per-sid-Riegel (statt EINEM globalen `_session_state_lock`) — eigener Brocken, nicht hier.
- Aufräumen der ~60 übrigen `with _session_state_lock:`-Stellen — durch Wächter 2 ab jetzt gegen Verschlechterung gesichert, aber kein Umbau in dieser Phase.
- `py-spy` in `requirements.txt` aufnehmen — nicht entschieden; `faulthandler` löst den Bedarf ohne zusätzliche Abhängigkeit.
</deferred>

---

*Phase: 08.23.2.LOCK-1-sitzungs-riegel-entklemmen*
*Context gathered: 2026-07-30 aus der André-Direktive (Insert-Auftrag) — kein discuss-phase*
