---
phase: 08.23.2.LOCK-1
plan: 01
subsystem: live-session
tags: [tests, deadlock, session-state-lock, erst-rot, waechter, tdd]
requires:
  - services/live_session.py (_session_state, _session_state_lock, get_sid_paused)
  - services/deepgram_service.py (register_audio_handlers, handle_audio_chunk, handle_manual_ewb)
  - routes/app_routes.py (api_beenden)
  - tests/conftest.py (client, cleanup_rows, _baseline_cleanup_guard)
provides:
  - "Waechter 1 — Verklemmungs-Test fuer handle_manual_ewb + POST /api/beenden (2 Prueflinge + 2 gepaarte Frei-Fall-Kontrollen)"
  - "Waechter 3 — Ton-Weg-riegel-frei-Test (get_sid_paused + handle_audio_chunk, gepaarte Wert-Assertion)"
  - "Fixture-Muster held_session_state_lock (Halter-Faden + Nachzuegler-Anmeldung + 30s-Notbremse)"
  - "DIALOG-Eintrag mit der Teil-2-Abwaegung (Weg c) und der 102-Bloecke-Korrektur"
affects:
  - "deploy.sh-Gate — ab jetzt ABSICHTLICH ROT bis Plan 03 die Riegel-Proben baut"
  - "Plan 02 (Waechter 2, AST), Plan 03 (Fix), Plan 04 (Wachhund)"
tech-stack:
  added: []
  patterns:
    - "ERST ROT: Verhaltens-Waechter vor dem Fix, Rot-Ausgabe verbatim in der Commit-Message"
    - "Halter-Faden haelt einen Produktiv-Riegel; Pruefling laeuft in Daemon-Faden mit join(timeout)"
    - "Gepaarte Positiv-Assertion neben jeder Negativ-Assertion (Anti-Stub in beide Richtungen)"
    - "Aufraeum-Fixture VOR der Halter-Fixture in der Parameter-Liste -> Abbau ZULETZT, Riegel dann frei"
key-files:
  created:
    - tests/test_audio_path_lock_free_guard.py
    - tests/test_session_lock_deadlock_guard.py
  modified:
    - .planning/DIALOG-GSD-CLAUDIAN.md
decisions:
  - "Rot-Beleg lokal ist ein Ermittlungs-Lauf, keine Abnahme (HART-Regel Kategorie Lesen/Ermitteln)"
  - "_cleanup_sid wanderte aus dem test-lokalen finally in die Fixture ewb_sid_aufraeumen — sonst 30s statt 10s Laufzeit"
  - "Kein pytest-Marker auf beiden Waechtern, damit sie im deploy.sh-Gate mitlaufen"
  - "Rot-Beleg II (api_beenden-Haelfte) wird NICHT vom Executor gefahren — Uebergabe an Claudian (SSH-Mandat)"
metrics:
  tasks: 3
  commits: 3
  duration: ~35min
  completed: 2026-07-31
---

# Phase 08.23.2.LOCK-1 Plan 01: Netz vor dem Fix (zwei Waechter + DIALOG) Summary

Zwei Verhaltens-Waechter haengen am unveraenderten Code und sind **absichtlich rot**: sie
beweisen, dass Knopfdruck und Ton-Weg am globalen `_session_state_lock` stumm haengen bleiben —
mit gepaarten Positiv-Assertionen, damit ein Stub-Fix in Plan 03 nicht gruen werden kann.

## Commits

| Task | Commit | Datei | Inhalt |
|---|---|---|---|
| 1 | `e1249a4` | `tests/test_audio_path_lock_free_guard.py` | Waechter 3 — Ton-Weg riegel-frei (ERST ROT) |
| 2 | `b0e8b75` | `tests/test_session_lock_deadlock_guard.py` | Waechter 1 — Verklemmungs-Test manual_ewb + api_beenden (ERST ROT) |
| 3 | `cac90ae` | `.planning/DIALOG-GSD-CLAUDIAN.md` | DIALOG — Teil-2-Abwaegung (c) + fuenf Punkte (6 Eintraege, +100 Zeilen, 0 Loeschungen) |

Kein Produktiv-Code angefasst: `git status --porcelain services/ routes/ app.py static/ templates/ database/ alembic/` ist **leer**.
Kein Deploy, kein Push, kein SSH.

---

## Rot-Beleg I-a — Waechter 3 (verbatim)

`python -m pytest tests/test_audio_path_lock_free_guard.py -q --durations=5`

```
>       assert not c.is_alive(), (
            f"get_sid_paused kehrte mit gehaltenem _session_state_lock nicht innerhalb "
            f"{_PRUEFLING_TIMEOUT_S}s zurueck — die Funktion nimmt den globalen Riegel "
            f"(live_session.py:105-108). Das ist Wurzel 1 aus Phase 08.23.2.LOCK-1.")
E       AssertionError: get_sid_paused kehrte mit gehaltenem _session_state_lock nicht innerhalb 2.0s zurueck — die Funktion nimmt den globalen Riegel (live_session.py:105-108). Das ist Wurzel 1 aus Phase 08.23.2.LOCK-1.
E       assert not True
E        +  where True = is_alive()
E        +    where is_alive = <Thread(LOCK1-pruefling-get-sid-paused, started daemon 2844)>.is_alive

tests\test_audio_path_lock_free_guard.py:137: AssertionError
_________ test_handle_audio_chunk_kehrt_mit_gehaltenem_riegel_zurueck _________
>       assert not c.is_alive(), (
            f"handle_audio_chunk kehrte mit gehaltenem _session_state_lock nicht innerhalb "
            f"{_PRUEFLING_TIMEOUT_S}s zurueck — der 10-Hz-Ton-Weg haengt an "
            f"deepgram_service.py:864 (ls.get_sid_paused). 1415 von 1416 blockierten Rahmen "
            f"im py-spy-Abzug vom 30.07. standen genau dort.")
E       AssertionError: handle_audio_chunk kehrte mit gehaltenem _session_state_lock nicht innerhalb 2.0s zurueck — der 10-Hz-Ton-Weg haengt an deepgram_service.py:864 (ls.get_sid_paused). 1415 von 1416 blockierten Rahmen im py-spy-Abzug vom 30.07. standen genau dort.
E       assert not True
E        +  where True = is_alive()
E        +    where is_alive = <Thread(LOCK1-pruefling-audio-chunk, started daemon 29812)>.is_alive

tests\test_audio_path_lock_free_guard.py:166: AssertionError
---------------------------- Captured stdout call -----------------------------
[DG] audio_chunk #1 (sid=lock1-w3-unbekannt-002, bytes=100, type=bytes)
============================= slowest 5 durations =============================
2.40s call     tests/test_audio_path_lock_free_guard.py::test_handle_audio_chunk_kehrt_mit_gehaltenem_riegel_zurueck
2.01s call     tests/test_audio_path_lock_free_guard.py::test_get_sid_paused_kehrt_mit_gehaltenem_riegel_zurueck

(3 durations < 0.005s hidden.  Use -vv to show these durations.)
=========================== short test summary info ===========================
FAILED tests/test_audio_path_lock_free_guard.py::test_get_sid_paused_kehrt_mit_gehaltenem_riegel_zurueck
FAILED tests/test_audio_path_lock_free_guard.py::test_handle_audio_chunk_kehrt_mit_gehaltenem_riegel_zurueck
2 failed in 4.53s
```

**Ergebniszeile: `2 failed in 4.53s`** — beide Prueflinge rot, wie gefordert.
Laengste Laufzeit **2.40s** (Kriterium: Modul unter 15s — erfuellt, Gesamtlauf 4.53s).

> Hinweis zur Darstellung: Die Windows-Konsole (cp1252) zeigt die Gedankenstriche der
> Assertion-Meldungen als Ersatzzeichen. Im Quelltext und auf dem Server-Terminal stehen echte
> Gedankenstriche; oben sind sie wiederhergestellt, der Rest ist unveraendert.

---

## Rot-Beleg I-b — Waechter 1, `manual_ewb`-Haelfte (verbatim)

`python -m pytest tests/test_session_lock_deadlock_guard.py -q --durations=5`

```
>       assert not c.is_alive(), (
            f"handle_manual_ewb kehrte mit gehaltenem _session_state_lock nicht innerhalb "
            f"{_PRUEFLING_TIMEOUT_S}s zurueck. Vier Riegel-Nahmen liegen im synchronen Pfad: "
            f"deepgram_service.py:966, :982 (get_anonymisierer), :1011, :1027. "
            f"Am 30.07. hingen genau so vier Klicks stumm (09:28:07/09:29:11/09:29:55/09:30:07).")
E       AssertionError: handle_manual_ewb kehrte mit gehaltenem _session_state_lock nicht innerhalb 10.0s zurueck. Vier Riegel-Nahmen liegen im synchronen Pfad: deepgram_service.py:966, :982 (get_anonymisierer), :1011, :1027. Am 30.07. hingen genau so vier Klicks stumm (09:28:07/09:29:11/09:29:55/09:30:07).
E       assert not True
E        +  where True = is_alive()
E        +    where is_alive = <Thread(LOCK1-pruefling-A, started daemon 17172)>.is_alive

tests\test_session_lock_deadlock_guard.py:176: AssertionError
---------------------------- Captured stdout call -----------------------------
[PiP] manual_ewb (sid=lock1-w1-ewb-001): zu_teuer
============================= slowest 5 durations =============================
10.21s call     tests/test_session_lock_deadlock_guard.py::test_manual_ewb_kehrt_mit_fehler_zurueck
5.00s teardown tests/test_session_lock_deadlock_guard.py::test_manual_ewb_kehrt_mit_fehler_zurueck
0.55s call     tests/test_session_lock_deadlock_guard.py::test_manual_ewb_laeuft_mit_freiem_riegel_normal_durch

(2 durations < 0.005s hidden.  Use -vv to show these durations.)
=========================== short test summary info ===========================
FAILED tests/test_session_lock_deadlock_guard.py::test_manual_ewb_kehrt_mit_fehler_zurueck
1 failed, 1 passed, 2 skipped in 15.96s
```

**Ergebniszeile woertlich: `1 failed, 1 passed, 2 skipped in 15.96s`.**
**Lokal ist das ein Vorabsignal, die Abnahme dieser Phase ist das `deploy.sh production`-Gate
nach Plan 04.**

Aufschluesselung — damit `1 failed` **nicht** als halber Waechter gelesen und der gruene
Kontrolltest „repariert" wird:

| Test | Ergebnis | Rolle |
|---|---|---|
| `test_manual_ewb_kehrt_mit_fehler_zurueck` | **failed** | DER Rot-Beleg (Verklemmung) |
| `test_manual_ewb_laeuft_mit_freiem_riegel_normal_durch` | **passed** | Frei-Fall-Kontrolle — **soll** heute gruen sein |
| `test_api_beenden_kehrt_mit_fehler_zurueck` | **skipped** | kein `TEST_DATABASE_URL` (`conftest.py:824-827`) |
| `test_api_beenden_mit_freiem_riegel_ist_kein_state_locked` | **skipped** | dito |

Laengster Test **10.21s** (Kriterium: <= 12s — erfuellt). Der 5.00s-Teardown ist der
Nachzuegler-Join: im ROT-Fall steht Pruefling A noch am Riegel und laeuft nach der Freigabe
weiter — genau dafuer existiert die Anmeldung `held_session_state_lock(c)`. Nach Plan 03
kehrt A sofort zurueck und der Posten verschwindet.

### Geprueft: Fixture-Abbau-Reihenfolge

Von Hand kontrolliert, alle vier Signaturen:

```
def test_manual_ewb_kehrt_mit_fehler_zurueck(ewb_sid_aufraeumen, held_session_state_lock)
def test_manual_ewb_laeuft_mit_freiem_riegel_normal_durch(ewb_sid_aufraeumen)
def test_api_beenden_kehrt_mit_fehler_zurueck(client, throwaway, held_session_state_lock)
def test_api_beenden_mit_freiem_riegel_ist_kein_state_locked(client, throwaway)
```

`held_session_state_lock` steht in **keiner** Signatur vor `throwaway` — es steht ueberall
**zuletzt**, wird also **zuerst** abgebaut. Damit ist der Riegel frei, bevor der
`throwaway`-Teardown `_clear_leaked_sessions_for_user` ruft (das denselben Riegel nimmt).

---

## ⚠ OFFENE UEBERGABE — Rot-Beleg II

**Rot-Beleg II steht aus — Claudian faehrt `tests/test_session_lock_deadlock_guard.py` direkt
auf dem Prod-Server (Befehlsblock in `<erst_rot_pflicht>`, **kein** `deploy.sh`). Entscheidend:
`test_api_beenden_kehrt_mit_fehler_zurueck` muss FAILED sein,
`test_api_beenden_mit_freiem_riegel_ist_kein_state_locked` muss PASSED sein.**

- **Wer:** Claudian (SSH-Mandat). Der Executor hat den Lauf **nicht** gefahren und hat kein
  SSH-Mandat — das ist so vorgesehen, kein Versaeumnis.
- **Wogegen:** frisch provisionierte Wegwerf-`nerve_test`, Arbeits-Abzug des App-Baums nach
  `/tmp`; `/opt/nerve/app` wird nur **gelesen**. Kein tar-Upload, kein `systemctl`, keine
  Aenderung an `nerve`.
- **Nachzutragen ins SUMMARY:** die Ausgabe **verbatim** unter der Ueberschrift
  „Rot-Beleg II (Server-Lauf, alter Stand)" — **inklusive der `git-stand`-Ausgabe aus
  Schritt (2a')**, damit schwarz auf weiss steht, gegen welchen Stand gezogen wurde.
- **Erwartet:** `2 failed, 2 passed`.
- **STOP-Fall:** `test_api_beenden_kehrt_mit_fehler_zurueck` PASSED **oder** SKIPPED →
  Konstruktion falsch, melden statt weiterbauen. **Ein `skipped` ist kein Beleg.**
- **Zeitfenster:** genau jetzt. Nach Plan 03 ist der alte Stand weg und der Beleg nicht mehr
  nachholbar.

Solange dieser Beleg fehlt, gilt Waechter 1 als **nur halb bewiesen**.

---

## Zustand nach Plan 01

- **Das Deploy-Tor ist ab jetzt ROT** und bleibt es bis zum Abschluss von Plan 03
  (`deploy.sh:222` bricht bei rotem Gate ab). In diesem Fenster ist kein Not-Hotfix deploybar,
  ohne das Gate zu umgehen. Wellen 1-3 gehoeren in **eine** Sitzung (Cross-AI B7).
- Regressions-Kontrolle unveraendert gruen:
  `python -m pytest tests/test_per_sid_migration.py tests/test_live_session_ghost_sid.py tests/test_stabil1_beenden_guard.py tests/test_mode_switch_event.py tests/test_no_live_global_state.py -q`
  → **`14 passed, 9 skipped in 1.40s`**. Kein Bestands-Test durch die neuen Fixtures gestoert,
  nichts haengt.
- **Punkt 23 nicht anwendbar (kein DB-Pfad angefasst).** Keine Tabelle, keine Spalte, kein
  Lese-/Schreib-Pfad; die `throwaway`-Fixture ist Test-Infrastruktur mit vollstaendigem
  Teardown. Punkt 21/22/26 ebenfalls Skip-OK.

## Naechste Schritte

1. Claudian: Rot-Beleg II fahren, Ausgabe hier nachtragen.
2. Plan 02 (Waechter 2, AST — Welle 1, parallel).
3. Plan 03 (Welle 2: Teil 1 + zwei Riegel-Proben + Teil 2 + Teil 2c) — macht Waechter 1 und 3 gruen.
4. Plan 04 (Welle 3: Wachhund). Deploy + Test-Anruf erst danach, durch Claudian.

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `_cleanup_sid` im test-lokalen `finally` blockierte am gehaltenen Riegel**

- **Found during:** Task 2, erster Rot-Lauf.
- **Issue:** Der Plan schrieb `_cleanup_sid(_SID_EWB)` als Teardown „im `finally` bzw. am
  Test-Ende". `_cleanup_sid` nimmt selbst den `_session_state_lock` — der zu diesem Zeitpunkt
  noch vom Halter-Faden gehalten wird (die Fixture wird erst **nach** dem Test-Koerper
  abgebaut). Der Test blockierte deshalb im eigenen `finally` bis zur 30s-Notbremse:
  gemessen **30.01s call** statt der erwarteten 10s. Das verletzte das Abnahme-Kriterium
  „laengster Test <= 12s" und haette im Gate wie ein halber Haenger ausgesehen.
- **Fix:** `_cleanup_sid` in eine eigene Fixture `ewb_sid_aufraeumen` verschoben, die in der
  Parameter-Liste **vor** `held_session_state_lock` steht und damit **zuletzt** abgebaut wird —
  exakt das Muster, das der Plan fuer `geseedeter_state` in Task 1 selbst vorgibt (Schicht 4
  Punkt 1). Danach: **10.21s call**, Kriterium erfuellt, Rot-Beleg unveraendert.
- **Files modified:** `tests/test_session_lock_deadlock_guard.py`
- **Commit:** `b0e8b75` (im Commit-Body dokumentiert)

Sonst keine Abweichungen. Insbesondere: kein `→ 0`-Abnahme-Kriterium nachtraeglich ergaenzt,
kein Schutz-Kommentar geloescht, keine Assertion abgeschwaecht.

## Abnahme-Kriterien — Nachweis

**Waechter 3 (`tests/test_audio_path_lock_free_guard.py`)**

| Kriterium | Soll | Ist |
|---|---|---|
| `grep -cF "assert ergebnis == [True, False]"` | 1 | 1 |
| `grep -cF "_HOLDER_NOTBREMSE_S = 30.0"` | 1 | 1 |
| `grep -cF "release.wait(timeout=_HOLDER_NOTBREMSE_S)"` | 1 | 1 |
| `grep -cF "daemon=True"` | >= 3 | 3 |
| `grep -cF "held_session_state_lock(c)"` | >= 2 | 2 |
| `grep -cF "inspect.getsource"` | 0 | 0 |
| `grep -cE "^import pytest$"` | 1 | 1 |
| `grep -c "pytest.mark"` | 0 | 0 |
| `git log -1 --stat` Dateien | 1 | 1 |

**Waechter 1 (`tests/test_session_lock_deadlock_guard.py`)**

| Kriterium | Soll | Ist |
|---|---|---|
| `grep -cF "'reason') == 'state_locked'"` | >= 1 | 1 |
| `grep -cF "r.status_code == 503"` | 1 | 1 |
| `grep -cF "not mock_sio.start_background_task.called"` | 1 | 1 |
| `grep -cF "assert mock_sio.start_background_task.called"` | 1 | 1 |
| `grep -cF "'reason') == 'no_session'"` | 1 | 1 |
| `grep -cF "held_session_state_lock)"` | >= 2 | 2 |
| `grep -cF "trg_audit_log_immutable"` | >= 2 | 3 |
| `grep -cF "held_session_state_lock(c)"` | >= 2 | 2 |
| `grep -cF "[LOCK1-TEST] org"` | 1 | 1 |
| `grep -c "pytest.mark"` | 0 | 0 |
| `git log -1 --stat` Dateien | 1 | 1 |

**DIALOG (`.planning/DIALOG-GSD-CLAUDIAN.md`)**

| Kriterium | Soll | Ist |
|---|---|---|
| `grep -cF "LOCK-1 — GSD — 2026-07-30"` | 1 | 1 |
| `grep -cF "ZWEI DISJUNKTE DEFEKTE"` | >= 1 | 1 |
| `grep -cF "deepgram_service.py:94"` | >= 1 | 1 |
| `grep -cF "state_locked"` | >= 1 | 3 |
| `grep -cF "102"` | >= 1 | 12 |
| `tail -120 \| grep -cE "^\*\*[1-6]\."` | 6 | 6 |
| Menue-Muster in neuer Sektion | 0 | 0 |
| Eintrag am Dateiende | ja | ja (`tail -4` endet in Punkt 6) |
| `git show --stat` | 1 Datei, nur Hinzufuegungen | 1 Datei, +100 / -0 |

## Known Stubs

Keine. Beide Waechter sind vollstaendig verdrahtete Verhaltens-Tests; der einzige bewusst
unerfuellte Zustand ist die **absichtliche Rot-Faerbung** bis Plan 03.

## Threat Flags

Keine neue Angriffsflaeche: keine Route, kein Endpunkt, kein Auth-Pfad, keine Schema-Aenderung.
`T-LOCK1-03` (der Waechter selbst haengt die Suite) ist adressiert — Freigabe im
Fixture-`finally`, 30s-Notbremse, alle Faeden `daemon=True`, Laufzeit gemessen.
`T-LOCK1-04` (Test-Daten) ist adressiert — `[LOCK1-TEST]`-Wegwerf-Org, vollstaendiger Teardown
mit scoped `audit_log`-Trigger-Bypass und `cleanup_rows`, nie Baseline id=1.

## Self-Check: PASSED

Alle drei Dateien existieren auf der Platte, alle drei Commit-Hashes (`e1249a4`, `b0e8b75`,
`cac90ae`) sind in `git log` auffindbar.
