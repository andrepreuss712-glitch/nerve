---
phase: 06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/pip-launcher.js
  - templates/base.html
autonomous: true
requirements: [PIP-03, PIP-04]
requirements_addressed: [D-01, D-02, D-03]

must_haves:
  truths:
    - "EWB-Buttons im PiP zeigen lesbare Labels (z.B. 'Zu teuer', 'Kein Bedarf'), nicht '[object Object]'"
    - "Beide KI-Antwort-Slots (A und B) starten leer mit Platzhalter 'Warte auf Gesprächsinhalt...' — der Opener belegt Slot A NICHT mehr"
    - "Der Opener-Text erscheint als erster Block im Teleprompter und wird wie jeder andere Skript-Block dargestellt"
    - "Beide KI-Slots und der Teleprompter zeigen eine schlanke Custom-Scrollbar (4px) statt der hässlichen Browser-Default-Scrollbar"
    - "Backend-KI-Position-Erkennung kennt den Opener als Block 0 (Phase 0 = Opener) im gesendeten skript_bloecke-Array"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "EWB-Label-Fix mit kategorie-first Fallback-Chain, Opener→Teleprompter-Block-0-Integration, skript_bloecke-Array enthält Opener"
      contains: "e.kategorie || e.typ || e.name || e.einwand"
    - path: "templates/base.html"
      provides: "Custom-Scrollbar-CSS für .pip-ki-slot und .pip-teleprompter (konsistent), EWB-Row flex-wrap:nowrap"
      contains: ".pip-ki-slot::-webkit-scrollbar"
  key_links:
    - from: "_renderEwbButtons (static/pip-launcher.js ~line 972)"
      to: "profileDaten.einwaende objects with .kategorie field"
      via: "typeof-check + priorisierte Fallback-Chain"
      pattern: "e\\.kategorie \\|\\| e\\.typ"
    - from: "_initTeleprompter (static/pip-launcher.js ~line 1173)"
      to: "state.teleprompterBlocks[0] = openerText"
      via: "Resolve openerText aus state._editedOpenerText / selectedOpenerId / profileDaten.opener, prepend vor skriptBlocks"
      pattern: "openerText \\? \\[openerText\\]\\.concat\\(skriptBlocks\\)"
    - from: "_startAudio (static/pip-launcher.js ~line 771)"
      to: "socket.emit('start_live_session', { skript_bloecke: [opener, ...blocks] })"
      via: "Opener als Block 0 im Array, damit KI-Position-Erkennung Phase 0 erkennt"
      pattern: "skript_bloecke.*opener"
---

<objective>
Phase 06 UAT identifizierte 3 funktionale Bugs im neu gebauten PiP-Live-Fenster. Dieser Plan behebt alle drei: BUG-1 (EWB-Buttons zeigen [object Object]), BUG-2 (hässliche Browser-Default-Scrollbar in KI-Slots), BUG-3 (Opener blockiert Slot A dauerhaft und verhindert dass das Dual-Slot-Konzept sichtbar wird).

Purpose: Bevor Farbschema, Layout-Rotation, Mic-Indikator und Slider-Redesign angegangen werden, müssen die funktionalen Defekte behoben sein — sonst überlagern sich Design- und Funktionsregressionen im nächsten UAT.

Output: Ein PiP, in dem alle 5 EWB-Buttons mit Labels erscheinen, beide Slots leer beginnen, der Opener als erster Teleprompter-Block angezeigt wird und alle Scroll-Bereiche eine konsistente 4px-Custom-Scrollbar haben.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider/06.1-CONTEXT.md
@.planning/phases/06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider/06.1-PATTERNS.md
@.planning/phases/06-pip-komplett-rebuild-neues-layout-claude-streaming-skript-te/06-03-SUMMARY.md
@static/pip-launcher.js
@templates/base.html
@templates/profile_editor.html

<interfaces>
<!-- Data-Shape für einwaende (aus profile_editor.html lines 830-838 + routes/profiles.py lines 80-96) -->
<!-- WICHTIG: Profile-Editor speichert Objekte mit Shape {kategorie, intensitaet, einwand, varianten, gegenargument, technik} -->
<!-- Wizard speichert ggf. Strings. Beide müssen unterstützt werden. -->

profileDaten.einwaende kann eine Liste sein mit Einträgen vom Typ:
- string: "Zu teuer" (Wizard-Format)
- object: { kategorie: "Zu teuer", intensitaet: 3, einwand: "...", varianten: [...], gegenargument: "...", technik: "..." } (Profile-Editor-Format)
- object: { typ: "Zu teuer", name: "..." } (älteres Format, falls vorhanden)

Fallback-Priorität für Button-Label:
typeof e === 'string' ? e : (e.kategorie || e.typ || e.name || e.einwand || '')

Opener-Resolution-Chain (aus _showPipLive lines 953-962):
openerText =
  state._editedOpenerText ||
  (state.selectedOpenerId && state.openerItems.find(o => o.id === state.selectedOpenerId)?.inhalt) ||
  state.profileDaten?.opener ||
  ''

Teleprompter-Block-Parsing (aus _initTeleprompter line 1192):
skriptBlocks = inhalt.split(/\n\n+/).filter(b => b.trim())
blocks = openerText ? [openerText].concat(skriptBlocks) : skriptBlocks
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BUG-1 — EWB-Label-Fallback-Chain reparieren (D-01)</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeilen 970-990 (aktuelle _renderEwbButtons-Funktion)
    - .planning/phases/06.1-.../06.1-PATTERNS.md Abschnitt "BUG-1: EWB Labels render [object Object]"
    - templates/profile_editor.html Zeilen 820-840 (getEinwaende() Ausgabeformat zum Verständnis der Datenstruktur)
    - .planning/phases/06.1-.../06.1-CONTEXT.md D-01 Beschreibung
  </read_first>
  <behavior>
    - Test 1: Wenn state.profileDaten.einwaende = [{kategorie:'Zu teuer', intensitaet:3}, {kategorie:'Kein Bedarf'}] → Buttons haben Labels "Zu teuer" und "Kein Bedarf", NICHT "[object Object]"
    - Test 2: Wenn einwaende = ["Zu teuer", "Kein Bedarf"] (String-Format) → Buttons haben die Strings als Label (Backward-Compat)
    - Test 3: Wenn ein Eintrag gar kein Label-Feld hat → Button wird übersprungen (leerer String), NICHT "[object Object]"
    - Test 4: Alle 5 Buttons (nicht 2) werden gerendert, wenn 5 einwaende vorhanden sind — slice(0, 5) bleibt
    - Test 5: escHtml() wird weiterhin auf den ermittelten Label-String angewendet (XSS-Schutz bleibt)
  </behavior>
  <action>
In `static/pip-launcher.js` die Funktion `_renderEwbButtons` (ca. Zeilen 972-986) anpassen.

**Konkret — Zeilen 977-981 ersetzen:**

Alt:
```javascript
var html = einwaende.slice(0, 5).map(function (e) {
  var typ = typeof e === 'string' ? e : (e.typ || e.name || String(e));
  return '<button class="pip-ewb-btn" data-typ="' + escHtml(typ) + '">' + escHtml(typ) + '</button>';
}).join('');
```

Neu (per D-01 PATTERNS.md Fix-Pattern):
```javascript
var html = einwaende.slice(0, 5).map(function (e) {
  var typ = typeof e === 'string'
    ? e
    : (e.kategorie || e.typ || e.name || e.einwand || '');
  if (!typ) return '';  // skip rather than render [object Object]
  return '<button class="pip-ewb-btn" data-typ="' + escHtml(typ) + '">' + escHtml(typ) + '</button>';
}).join('');
```

**Wichtig:**
- `e.kategorie` MUSS an erster Stelle der Objekt-Fallback-Chain stehen (Profile-Editor-Format)
- Leere Labels werden übersprungen (return '') statt "[object Object]" zu rendern
- `.slice(0, 5)` bleibt — D-11 will alle 5 Buttons
- `escHtml(typ)` bleibt doppelt (einmal data-typ, einmal Label) — XSS-Schutz
- Keine anderen Änderungen an der Funktion
  </action>
  <verify>
    <automated>grep -n "e.kategorie || e.typ || e.name || e.einwand" static/pip-launcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "e.kategorie || e.typ || e.name || e.einwand" static/pip-launcher.js` findet exakt diese Zeile in _renderEwbButtons
    - `grep "String(e)" static/pip-launcher.js` findet KEINEN Match mehr in _renderEwbButtons (alter broken fallback entfernt)
    - `grep "if (!typ) return ''" static/pip-launcher.js` findet den Skip-Guard
    - `.slice(0, 5)` ist noch vorhanden (grep findet weiterhin ".slice(0, 5)")
  </acceptance_criteria>
  <done>EWB-Buttons im PiP-Live rendern lesbare Labels aus kategorie-Feld; kein "[object Object]" mehr sichtbar</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BUG-3 — Opener aus Slot A entfernen und als Teleprompter-Block-0 integrieren (D-03)</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeilen 950-970 (_showPipLive Opener-Resolution und Slot-0-Befüllung)
    - static/pip-launcher.js Zeilen 1173-1225 (_initTeleprompter Block-Parsing)
    - static/pip-launcher.js Zeilen 740-790 (_startAudio skript_bloecke-Array Konstruktion Zeile ~771)
    - .planning/phases/06.1-.../06.1-PATTERNS.md Abschnitt "BUG-3: Opener-Integration in Teleprompter"
    - .planning/phases/06.1-.../06.1-CONTEXT.md D-03 + specifics "Opener-Integration im Teleprompter"
    - .planning/phases/06-.../06-CONTEXT.md D-13 (KI-Position-Erkennung — darf nicht brechen)
  </read_first>
  <behavior>
    - Test 1: Nach Call-Start ist `pip-slot-body-0` leer bzw. zeigt "Warte auf Gesprächsinhalt..." (nicht mehr den Opener-Text)
    - Test 2: Nach Call-Start ist `pip-slot-label-0` NICHT mehr auf "OPENER" gesetzt (Slot A ist neutral)
    - Test 3: state.teleprompterBlocks[0] === openerText (aufgelöst via gleiche Chain wie vorher in _showPipLive)
    - Test 4: state.teleprompterBlocks.length === (Anzahl Skript-Blöcke + 1) wenn ein Opener existiert
    - Test 5: Wenn kein Opener existiert (leer), wird state.teleprompterBlocks nur aus skriptBlocks bestehen (kein leerer Block-0)
    - Test 6: `start_live_session` Socket-Emit enthält skript_bloecke mit Opener an Position 0
    - Test 7: tp-block-active (CSS-Highlight) beim ersten Block zeigt den Opener-Text
  </behavior>
  <action>
Drei koordinierte Änderungen in `static/pip-launcher.js`:

**Änderung 1 — Slot-0-Opener-Zuweisung entfernen (in `_showPipLive`, ca. Zeilen 953-966):**

Alt (aktuell):
```javascript
// Show opener in slot 0 (replaces old nlp-opener-text)
var openerText = 'Warte auf Gespr\u00e4chsinhalt...';
if (state._editedOpenerText) {
  openerText = state._editedOpenerText;
} else if (state.selectedOpenerId && state.openerItems.length > 0) {
  var selOp = state.openerItems.find(function (o) { return o.id === state.selectedOpenerId; });
  if (selOp) openerText = selOp.inhalt;
} else if (state.profileDaten && state.profileDaten.opener) {
  openerText = state.profileDaten.opener;
}
var slot0Body = pipEl('pip-slot-body-0');
if (slot0Body) slot0Body.textContent = openerText;
var slot0Label = pipEl('pip-slot-label-0');
if (slot0Label && openerText !== 'Warte auf Gespr\u00e4chsinhalt...') slot0Label.textContent = 'OPENER';
```

Neu:
```javascript
// D-03: Opener wandert in den Teleprompter als Block 0 — Slot A bleibt leer für erste KI-Antwort
// (keine Slot-0-Zuweisung mehr; beide Slots starten mit "Warte auf Gesprächsinhalt..." Default-Markup)
```

Entferne die 14 Zeilen komplett. Der Default-Platzhalter steht bereits im HTML (templates/base.html pip-slot-body-0).

**Änderung 2 — Opener als Block 0 in `_initTeleprompter` (ca. Zeilen 1173-1195):**

Direkt VOR der Zeile `var blocks = inhalt.split(/\n\n+/)...` einfügen:

```javascript
// D-03: Opener als erster Teleprompter-Block (Phase 0 = Opener für KI-Position-Erkennung)
var openerText = '';
if (state._editedOpenerText) {
  openerText = state._editedOpenerText;
} else if (state.selectedOpenerId && state.openerItems && state.openerItems.length > 0) {
  var selOp = state.openerItems.find(function (o) { return o.id === state.selectedOpenerId; });
  if (selOp) openerText = selOp.inhalt;
} else if (state.profileDaten && state.profileDaten.opener) {
  openerText = state.profileDaten.opener;
}
```

Und direkt danach die Zeile `var blocks = inhalt.split(/\n\n+/).filter(function (b) { return b.trim(); });` ersetzen durch:

```javascript
var skriptBlocks = inhalt.split(/\n\n+/).filter(function (b) { return b.trim(); });
var blocks = openerText ? [openerText].concat(skriptBlocks) : skriptBlocks;
```

Der bestehende Block `state.teleprompterBlocks = blocks;` bleibt unverändert — er nimmt jetzt das erweiterte Array.

**Änderung 3 — `start_live_session` Socket-Emit skript_bloecke erweitern (in `_startAudio` ca. Zeile 771):**

Die Stelle, an der `skript_bloecke` für den Socket-Emit gebaut wird (aktuell `skript_bloecke = skriptInhalt.split(/\n\n+/)...`), muss die gleiche Opener-Prepend-Logik anwenden. Konkret:

Alt (ca. Zeile 771):
```javascript
var skript_bloecke = skriptInhalt ? skriptInhalt.split(/\n\n+/).filter(function (b) { return b.trim(); }) : [];
```

Neu:
```javascript
// D-03: Opener als Block 0, damit KI-Position-Erkennung (skript_position) Phase 0 = Opener kennt
var skriptBloeckeRaw = skriptInhalt ? skriptInhalt.split(/\n\n+/).filter(function (b) { return b.trim(); }) : [];
var openerFuerKi = '';
if (state._editedOpenerText) openerFuerKi = state._editedOpenerText;
else if (state.selectedOpenerId && state.openerItems) {
  var selOp2 = state.openerItems.find(function (o) { return o.id === state.selectedOpenerId; });
  if (selOp2) openerFuerKi = selOp2.inhalt;
} else if (state.profileDaten && state.profileDaten.opener) {
  openerFuerKi = state.profileDaten.opener;
}
var skript_bloecke = openerFuerKi ? [openerFuerKi].concat(skriptBloeckeRaw) : skriptBloeckeRaw;
```

**Wichtig:**
- Die Opener-Resolution-Chain (3 Kandidaten: editedOpenerText → selectedOpenerId → profileDaten.opener) muss in Änderung 2 UND 3 identisch sein
- Kein Fallback auf "Warte auf Gesprächsinhalt..." mehr — wenn kein Opener, dann kein Block-0
- Slot-0 HTML-Default (templates/base.html) ist bereits "Warte auf Gesprächsinhalt..." — bleibt unverändert
  </action>
  <verify>
    <automated>grep -n "state.teleprompterBlocks = blocks" static/pip-launcher.js && grep -n "openerText ? \[openerText\]" static/pip-launcher.js && grep -c "pip-slot-label-0.*OPENER" static/pip-launcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pip-slot-body-0" static/pip-launcher.js` findet KEINE Opener-Zuweisung mehr (keine `slot0Body.textContent = openerText` in _showPipLive)
    - `grep "pip-slot-label-0.*OPENER" static/pip-launcher.js` findet 0 Matches (Label-Zuweisung entfernt)
    - `grep "openerText ? \[openerText\]\.concat" static/pip-launcher.js` findet mindestens 1 Match (in _initTeleprompter)
    - `grep "openerFuerKi ? \[openerFuerKi\]\.concat" static/pip-launcher.js` findet 1 Match (in _startAudio)
    - `grep -n "D-03" static/pip-launcher.js` findet mindestens 3 Kommentar-Marker (Änderung 1, 2, 3)
  </acceptance_criteria>
  <done>Slot A startet leer nach Call-Start; Opener erscheint als oberster, aktiver Teleprompter-Block; Backend empfängt Opener als skript_bloecke[0]</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: BUG-2 — Custom-Scrollbar für KI-Slots und Teleprompter konsistent einführen (D-02)</name>
  <files>templates/base.html</files>
  <read_first>
    - templates/base.html Zeilen 185-272 (kompletter PiP-&lt;style&gt;-Block inline)
    - templates/base.html Zeilen 204-206 (existierende Teleprompter-Scrollbar als Analog)
    - .planning/phases/06.1-.../06.1-PATTERNS.md Abschnitt "D-02 / BUG-2: Custom Scrollbar for Slots AND Teleprompter"
    - .planning/phases/06.1-.../06.1-CONTEXT.md D-02
  </read_first>
  <behavior>
    - Test 1: `.pip-ki-slot::-webkit-scrollbar` hat width:4px (WebKit browsers: Chrome, Edge, Safari)
    - Test 2: `.pip-ki-slot::-webkit-scrollbar-thumb` hat background:rgba(0,0,0,0.15) (dunkel für hellen Body in Plan 02 kommend; funktioniert jetzt schon auf dunklem Body okay — wird in Plan 02 nicht erneut angefasst)
    - Test 3: `.pip-teleprompter::-webkit-scrollbar-thumb` wird auch auf rgba(0,0,0,0.15) gesetzt (vorher rgba(255,255,255,0.15) — aber Teleprompter wird sowieso hell in Plan 02, also passt das schon hier)
    - Test 4: Firefox bekommt `scrollbar-width:thin` und `scrollbar-color:rgba(0,0,0,0.15) transparent` auf BEIDE Container
    - Test 5: Browser-Default-Scrollbar ist bei langem Text in Slot B nicht mehr sichtbar
  </behavior>
  <action>
In `templates/base.html` innerhalb des PiP-`<style>`-Blocks (Zeilen 185-272) die Scrollbar-Regeln konsolidieren.

**Konkret — die bestehenden Zeilen 204-206 (`.pip-teleprompter::-webkit-scrollbar*`) ERSETZEN durch den konsolidierten Block. Hintergrund-Farbe wird bereits auf den hellen Body vorbereitet (rgba(0,0,0,0.15)) — Plan 02 baut darauf auf.**

Alt (Zeilen 204-206):
```css
.pip-teleprompter::-webkit-scrollbar{width:4px}
.pip-teleprompter::-webkit-scrollbar-track{background:transparent}
.pip-teleprompter::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.15);border-radius:2px}
```

Neu (gleiche Stelle, beide Selektoren kombiniert):
```css
/* BUG-2: konsistente schlanke 4px-Scrollbar für Slots + Teleprompter (auf hellem Body aus Plan 02) */
.pip-ki-slot::-webkit-scrollbar,
.pip-teleprompter::-webkit-scrollbar{width:4px}
.pip-ki-slot::-webkit-scrollbar-track,
.pip-teleprompter::-webkit-scrollbar-track{background:transparent}
.pip-ki-slot::-webkit-scrollbar-thumb,
.pip-teleprompter::-webkit-scrollbar-thumb{background:rgba(0,0,0,0.15);border-radius:2px}
/* Firefox */
.pip-ki-slot{scrollbar-width:thin;scrollbar-color:rgba(0,0,0,0.15) transparent}
.pip-teleprompter{scrollbar-width:thin;scrollbar-color:rgba(0,0,0,0.15) transparent}
```

**Wichtig:**
- Farbe ist bereits `rgba(0,0,0,0.15)` (für hellen Body aus Plan 02); auf aktuellem dunklem Body leicht sichtbar, aber die Scrollbar ist so dezent (4px, 15% Opacity), dass es im Wave-1-Zustand keine visuelle Regression darstellt. Plan 02 invertiert den Body in der nächsten Wave.
- `.pip-ki-slot` hat bereits `overflow-y:auto` (Zeile 193-194) — keine Änderung nötig.
- Keine anderen Regeln im `<style>`-Block anfassen.
  </action>
  <verify>
    <automated>grep -c "pip-ki-slot::-webkit-scrollbar" templates/base.html && grep -c "scrollbar-color:rgba(0,0,0,0.15)" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pip-ki-slot::-webkit-scrollbar" templates/base.html` findet mindestens 3 Matches (scrollbar, track, thumb)
    - `grep "pip-ki-slot.*scrollbar-width:thin" templates/base.html` findet 1 Match (Firefox)
    - `grep "rgba(255,255,255,0.15)" templates/base.html` findet 0 Matches im ersetzten Block (alte Farbe entfernt) — andere Matches anderswo sind okay
    - `grep "BUG-2" templates/base.html` findet den Kommentar-Marker
  </acceptance_criteria>
  <done>Beide KI-Slots und Teleprompter zeigen identische 4px-Custom-Scrollbar in WebKit UND Firefox; keine hässliche Default-Scrollbar mehr sichtbar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile-Daten → PiP-DOM | einwaende-Objekte können frei-definierte Strings enthalten (User-Content) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Tampering (XSS via einwand-Labels) | _renderEwbButtons in static/pip-launcher.js | mitigate | escHtml(typ) bleibt erhalten für beide Einsatzstellen (data-typ UND innerHTML-Label). BUG-1-Fix ändert nur die Extraktion, nicht den HTML-Escape. |
| T-06.1-02 | Information Disclosure (Opener-Text an Backend) | _startAudio skript_bloecke Socket-Emit | accept | Opener ist Teil des aktiven Profils und geht bereits heute an den Backend-Claude-Service als Teil des Profil-Kontexts. Aufnahme ins skript_bloecke-Array ist kein zusätzlicher Leak. |
</threat_model>

<verification>
- Lokaler Smoke-Test: `python app.py` starten, in Chrome /live öffnen, Modus wählen, PiP starten, Setup durchlaufen
- In PiP-Live: EWB-Buttons-Reihe prüfen — 5 Buttons mit Labels wie "Zu teuer", "Kein Bedarf"
- Slot A (links) und Slot B (rechts) zeigen beide "Warte auf Gesprächsinhalt..." direkt nach Call-Start
- Teleprompter (unten) zeigt als ersten Block den Opener-Text in voller Farbe + teal-Border links (active-state)
- Slot-Text lang machen (DevTools, längeren String in innerHTML) → Custom-Scrollbar statt Browser-Default
- `grep "D-03" static/pip-launcher.js` zeigt 3+ Marker (Änderungen 1, 2, 3)
- `grep "BUG-2" templates/base.html` zeigt den Kommentar-Marker
</verification>

<success_criteria>
- Alle 3 Bugs aus UAT behoben: EWB-Labels lesbar, Slot A frei für erste KI-Antwort, konsistente schlanke Scrollbar
- Keine Regression bei existierendem Streaming-Flow: Tokens erscheinen weiterhin in Slot A bei erstem Einwand
- Backend KI-Position-Erkennung kennt Opener als Phase 0 (skript_bloecke[0])
- Alle `<acceptance_criteria>` grep-Commands liefern die dokumentierten Matches
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider/06.1-01-SUMMARY.md` erstellen (Template: was wurde gefixt, welche Zeilen, wie verifiziert).
</output>
