---
phase: 06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider
plan: 03
type: execute
wave: 3
depends_on: [06.1-02]
files_modified:
  - templates/base.html
  - static/pip-launcher.js
autonomous: true
requirements: [PIP-01]
requirements_addressed: [D-13, D-14, D-15, D-16]

must_haves:
  truths:
    - "Im PiP-Header erscheint rechts neben Badge/Timer ein Mic-Indikator mit 4 animierten Balken"
    - "Die Balken-Höhe folgt in Echtzeit dem Audio-Input-Pegel (WebAudio AnalyserNode) mit ~16 fps ohne Performance-Probleme"
    - "Aktiver Zustand: Balken teal (#00D4AA), animiert; Muted-Zustand: Balken grau (#6B7280) + flach bei 15% Höhe + Button-Opacity 0.6"
    - "Click auf den Mic-Indikator toggled den Mute-State via MediaStreamTrack.enabled (Stream bleibt alive, keine Deepgram-Reconnect-Kosten)"
    - "Mute-Toggle killt nicht die Session — stop_live_session wird NICHT emittet; Deepgram bleibt verbunden und empfängt silent audio"
    - "Cleanup bei _stopMic: rAF-Loop wird cancelled und AnalyserNode disconnected"
  artifacts:
    - path: "templates/base.html"
      provides: "Mic-Indikator-Button-Markup im Header (#pip-mic-indicator mit 4 pip-mic-bar-0..3 Spans) + Mic-Indikator-CSS mit aktiv/muted States"
      contains: "pip-mic-indicator"
    - path: "static/pip-launcher.js"
      provides: "AnalyserNode parallel zum Worklet-Node im _startAudio, _startMicLevelLoop rAF-Tick, _updateMicBarsDom, _toggleMicMute, _updateMicIndicatorState, Wire-up in _wirePipButtons, Cleanup in _stopMic, state-Fields (micAnalyser, micLevelRafId, micMuted), Show-Toggle in _showPipLive"
      contains: "createAnalyser"
  key_links:
    - from: "_startAudio (static/pip-launcher.js)"
      to: "source.connect(analyser) parallel zu source.connect(workletNode)"
      via: "AnalyserNode mit fftSize 256 + smoothingTimeConstant 0.7"
      pattern: "createAnalyser"
    - from: "pip-mic-indicator click handler"
      to: "MediaStreamTrack.enabled toggle"
      via: "_toggleMicMute() setzt tracks[0].enabled = !current"
      pattern: "tracks\\[0\\]\\.enabled"
    - from: "_startMicLevelLoop rAF-Tick (60ms throttle = ~16 fps)"
      to: "pip-mic-bar-0..3 DOM-Elemente via .style.height"
      via: "Uint8Array Buffer geteilt in N=4 Buckets, gemittelt, normalisiert 0-1"
      pattern: "getByteFrequencyData"
---

<objective>
Phase 06 baute das PiP ohne Mic-Indikator — der Berater sieht nicht, ob das Mikro wirklich Ton aufnimmt. Im Cold Call ist das kritisch: kein visuelles Feedback = kein Vertrauen = kein Live-Use.

Purpose: Dieser Plan fügt dem PiP-Header einen 4-Balken-Audio-Level-Indikator hinzu, der in Echtzeit die Input-Amplitude visualisiert. Zusätzlich dient er als klickbarer Mute-Toggle (für Niesen/Husten ohne CRM-Wechsel) — technisch via `MediaStreamTrack.enabled=false`, sodass die Deepgram-Verbindung nicht abbricht und der Reconnect-Overhead vermieden wird.

Output: Ein Header-Element, das den Live-Mic-Status sofort erkennbar macht (grün animiert = hört zu, grau flach = stumm) und per Klick zwischen beiden Zuständen wechselt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider/06.1-CONTEXT.md
@.planning/phases/06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider/06.1-PATTERNS.md
@.planning/phases/06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider/06.1-02-SUMMARY.md
@templates/base.html
@static/pip-launcher.js
@services/deepgram_service.py

<interfaces>
<!-- Web Audio API Referenz (MDN AnalyserNode) -->
audioCtx.createAnalyser() → AnalyserNode
  .fftSize = 256 (liefert frequencyBinCount = 128)
  .smoothingTimeConstant = 0.7 (glatte Transitions)
  .getByteFrequencyData(Uint8Array) → füllt Buffer mit 0-255 pro Bin

<!-- MediaStream-Track-Mute-Pattern -->
state.micStream.getAudioTracks()[0].enabled = false
  → Track produziert Silence (0-bytes), bleibt aber alive
  → getUserMedia muss NICHT erneut aufgerufen werden
  → Deepgram empfängt leere Chunks, bleibt verbunden
  → Reconnect-Kosten: 0

<!-- State-Additions (module-level state object in pip-launcher.js lines 8-42) -->
state.micAnalyser: AnalyserNode | null
state.micLevelRafId: number | null  // requestAnimationFrame ID
state.micMuted: boolean  // Mirror der track.enabled-Inversion

<!-- DOM-IDs der neuen Elemente -->
#pip-mic-indicator (button, role=button, aria-label, title)
#pip-mic-bar-0, #pip-mic-bar-1, #pip-mic-bar-2, #pip-mic-bar-3 (spans innerhalb)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mic-Indikator-Markup + CSS in base.html (D-13, D-14)</name>
  <files>templates/base.html</files>
  <read_first>
    - templates/base.html Zeilen 284-294 (aktuelles PiP-Header-Markup)
    - templates/base.html PiP-&lt;style&gt;-Block (aus Plan 02 jetzt mit hellem Body + dunklem Header)
    - .planning/phases/06.1-.../06.1-PATTERNS.md Abschnitt "PiP-Header Markup (Mic-Indikator + Slider Redesign)" + "Mic-Indikator CSS"
    - .planning/phases/06.1-.../06.1-CONTEXT.md D-13, D-14
  </read_first>
  <behavior>
    - Test 1: `<button id="pip-mic-indicator" class="pip-mic-indicator">` existiert innerhalb #pip-header, BEFORE #pip-opacity-slider
    - Test 2: Button enthält genau 4 `<span class="pip-mic-bar" id="pip-mic-bar-N">` (N=0..3)
    - Test 3: Button hat `title="Mikrofon stumm schalten"` und `aria-label="Mic Toggle"`
    - Test 4: Initiales `style="display:none"` ist gesetzt (wird erst in _showPipLive eingeschaltet)
    - Test 5: .pip-mic-indicator CSS — display:inline-flex, align-items:flex-end, height:20px, width:28px, cursor:pointer, border:none, background:transparent, border-radius:4px
    - Test 6: .pip-mic-bar CSS — width:4px, background:#00D4AA, border-radius:1px, height:15% (idle), transition: height 60ms linear
    - Test 7: .pip-mic-indicator.pip-mic-muted .pip-mic-bar → background:#6B7280 (grau)
    - Test 8: .pip-mic-indicator.pip-mic-muted → opacity:0.6
    - Test 9: .pip-mic-indicator:hover → background:rgba(0,212,170,0.12)
  </behavior>
  <action>
Zwei Änderungen in `templates/base.html`:

**Änderung A — Header-Markup (ca. Zeilen 284-294):**

Alt:
```html
<div class="pip-header" id="pip-header">
  <div style="display:flex;align-items:center;gap:8px">
    <span class="pip-mode-badge" id="nlp-mode-badge">Cold Call</span>
    <span class="pip-timer" id="nlp-timer">00:00</span>
  </div>
  <div style="display:flex;align-items:center;gap:8px">
    <span class="pip-opacity-label" id="pip-opacity-label" style="display:none">BG</span>
    <input type="range" id="pip-opacity-slider" min="10" max="100" step="5" value="100" style="display:none">
  </div>
</div>
```

Neu (Mic-Indikator-Button VOR dem Slider einfügen; gap auf 12px erhöhen; optional opacity-label entfernen ist OKAY wenn im Flow nicht mehr gebraucht — aber nicht in diesem Task, Plan 04 nimmt sich den Slider vor):
```html
<div class="pip-header" id="pip-header">
  <div style="display:flex;align-items:center;gap:8px">
    <span class="pip-mode-badge" id="nlp-mode-badge">Cold Call</span>
    <span class="pip-timer" id="nlp-timer">00:00</span>
  </div>
  <div style="display:flex;align-items:center;gap:12px">
    <!-- D-13: Mic-Indikator — 4 animierte Balken, klickbar als Mute-Toggle -->
    <button class="pip-mic-indicator" id="pip-mic-indicator" title="Mikrofon stumm schalten" aria-label="Mic Toggle" style="display:none">
      <span class="pip-mic-bar" id="pip-mic-bar-0"></span>
      <span class="pip-mic-bar" id="pip-mic-bar-1"></span>
      <span class="pip-mic-bar" id="pip-mic-bar-2"></span>
      <span class="pip-mic-bar" id="pip-mic-bar-3"></span>
    </button>
    <span class="pip-opacity-label" id="pip-opacity-label" style="display:none">BG</span>
    <input type="range" id="pip-opacity-slider" min="10" max="100" step="5" value="100" style="display:none">
  </div>
</div>
```

**Änderung B — CSS-Block ergänzen (innerhalb des bestehenden `<style>`-Blocks, nach den Header-Regeln):**

Direkt nach der `.pip-header`-Regel (aus Plan 02 mit background:#0D1117) einfügen:

```css
/* D-13: Mic-Indikator — 4 Balken, klickbar als Mute-Toggle */
.pip-mic-indicator{display:inline-flex; align-items:flex-end; gap:2px;
  height:20px; width:28px; padding:2px 3px; background:transparent; border:none;
  cursor:pointer; border-radius:4px; transition:background 150ms, opacity 200ms}
.pip-mic-indicator:hover{background:rgba(0,212,170,0.12)}
.pip-mic-bar{display:inline-block; width:4px; background:#00D4AA;
  border-radius:1px; height:15%;
  transition:height 60ms linear, background 200ms}
/* D-14: Muted-State — Balken grau + flach + Button-Opacity */
.pip-mic-indicator.pip-mic-muted .pip-mic-bar{background:#6B7280}
.pip-mic-indicator.pip-mic-muted{opacity:0.6}
```

**Wichtig:**
- Button-Element (kein `<div>`) — semantisch korrekt für klickbar + Keyboard-Accessibility (Enter/Space).
- `aria-label` und `title` beide gesetzt (Screen-Reader + Tooltip).
- `display:none` initial, weil der Indikator erst im Live-Zustand sichtbar sein soll (Task 2 schaltet ein).
- Die 4 Spans haben fixe IDs `pip-mic-bar-0` bis `pip-mic-bar-3` — pip-launcher.js referenziert diese direkt per pipEl().
  </action>
  <verify>
    <automated>grep -c 'id="pip-mic-bar-' templates/base.html && grep -c "pip-mic-indicator.pip-mic-muted" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'id="pip-mic-indicator"' templates/base.html` findet 1 Match (Button)
    - `grep 'id="pip-mic-bar-' templates/base.html` findet 4 Matches (0, 1, 2, 3)
    - `grep 'aria-label="Mic Toggle"' templates/base.html` findet 1 Match
    - `grep ".pip-mic-indicator.pip-mic-muted" templates/base.html` findet mindestens 2 Matches (opacity + bar-bg)
    - `grep "background:#00D4AA" templates/base.html` findet mindestens 1 Match im .pip-mic-bar-Kontext (weitere Matches elsewhere okay)
    - `grep "background:#6B7280" templates/base.html` findet mindestens 1 Match (muted bar)
    - `grep 'class="pip-mic-indicator"' templates/base.html` findet 1 Match (button class)
  </acceptance_criteria>
  <done>Header hat Mic-Indikator-Button (initial display:none) mit 4 Balken-Spans und vollständigen CSS-Regeln für idle/muted States</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WebAudio AnalyserNode + rAF-Loop + Mute-Toggle in pip-launcher.js (D-15, D-16)</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeilen 8-42 (state-Objekt — neue Fields ergänzen)
    - static/pip-launcher.js Zeilen 743-784 (_startAudio WebAudio-Graph-Setup — AnalyserNode parallel einhängen)
    - static/pip-launcher.js Zeilen 1309-1316 (_stopMic Cleanup — rAF-Cancel + Analyser-Disconnect ergänzen)
    - static/pip-launcher.js Zeilen 857-869 (_wirePipButtons — Mic-Click wire-up hinzufügen)
    - static/pip-launcher.js Zeilen 940-965 (_showPipLive — Mic-Indikator einschalten)
    - .planning/phases/06.1-.../06.1-PATTERNS.md Abschnitt "Mic-Indikator with Audio-Level-Bars" + "Mute-Toggle Click Handler"
    - .planning/phases/06.1-.../06.1-CONTEXT.md D-15, D-16
    - services/deepgram_service.py (zum Check, dass Mute via track.enabled=false keine Session beendet)
  </read_first>
  <behavior>
    - Test 1: Nach _startAudio gibt es state.micAnalyser als AnalyserNode mit fftSize=256, smoothingTimeConstant=0.7
    - Test 2: AnalyserNode ist parallel zum workletNode an `source` connected (keine Unterbrechung des bestehenden Worklet-Pfads)
    - Test 3: state.micLevelRafId ist nach _startAudio gesetzt (truthy)
    - Test 4: _updateMicBarsDom setzt height-Style auf alle 4 pip-mic-bar-N Elemente bei jedem Tick
    - Test 5: rAF-Tick ist auf ~16 fps gedrosselt (ts - lastTick >= 60ms Guard)
    - Test 6: Click auf #pip-mic-indicator toggled state.micMuted
    - Test 7: Bei Mute: alle tracks[i].enabled = false; beim Unmute: alle tracks[i].enabled = true
    - Test 8: Bei Mute: _stopMic wird NICHT aufgerufen, stop_live_session wird NICHT emittet (Deepgram-Connection bleibt)
    - Test 9: Bei Mute: alle 4 Balken bekommen height:15% (flach)
    - Test 10: .pip-mic-indicator.pip-mic-muted Klasse wird korrekt getoggelt
    - Test 11: _stopMic cancelled rAF (state.micLevelRafId = null nach Call) und disconnected AnalyserNode
    - Test 12: In _showPipLive wird pip-mic-indicator auf display:inline-flex gesetzt, sobald Live-Zustand erreicht ist
  </behavior>
  <action>
Fünf koordinierte Änderungen in `static/pip-launcher.js`:

**Änderung 1 — State-Fields ergänzen (im state-Objekt ca. Zeilen 8-42):**

Innerhalb des bestehenden `var state = { ... }` Blocks die folgenden drei Fields hinzufügen:

```javascript
micAnalyser: null,
micLevelRafId: null,
micMuted: false,
```

**Änderung 2 — AnalyserNode im _startAudio einhängen (ca. Zeilen 745-760):**

Nach der Zeile `var source = audioCtx.createMediaStreamSource(state.micStream);` und VOR `source.connect(workletNode);` folgendes einfügen (der bestehende worklet-Pfad bleibt unverändert darunter):

```javascript
// D-16: AnalyserNode parallel zum Worklet — für Mic-Level-Bars, stört Worklet-Streaming nicht
var analyser = audioCtx.createAnalyser();
analyser.fftSize = 256;
analyser.smoothingTimeConstant = 0.7;
source.connect(analyser);
state.micAnalyser = analyser;
state.micMuted = false;
_startMicLevelLoop();
```

**Änderung 3 — _startMicLevelLoop + _updateMicBarsDom + _toggleMicMute + _updateMicIndicatorState als neue Funktionen (unterhalb von _startAudio, außerhalb):**

```javascript
function _startMicLevelLoop() {
  var analyser = state.micAnalyser;
  if (!analyser) return;
  var buffer = new Uint8Array(analyser.frequencyBinCount);
  var lastTick = 0;
  var N = 4;
  var perBar = Math.floor(buffer.length / N);
  function tick(ts) {
    if (!state.micAnalyser) return;  // gestoppt
    if (ts - lastTick >= 60) {  // ~16 fps Drosselung (D-16)
      lastTick = ts;
      analyser.getByteFrequencyData(buffer);
      var bars = new Array(N);
      for (var i = 0; i < N; i++) {
        var sum = 0;
        for (var j = 0; j < perBar; j++) sum += buffer[i * perBar + j];
        bars[i] = (sum / perBar) / 255;  // normalisiert 0..1
      }
      _updateMicBarsDom(bars);
    }
    state.micLevelRafId = requestAnimationFrame(tick);
  }
  state.micLevelRafId = requestAnimationFrame(tick);
}

function _updateMicBarsDom(bars) {
  // Bei Mute: Balken bleiben flach (15%), nicht animiert
  if (state.micMuted) return;
  for (var i = 0; i < bars.length; i++) {
    var el = pipEl('pip-mic-bar-' + i);
    if (el) el.style.height = (Math.max(0.15, bars[i]) * 100) + '%';
  }
}

function _toggleMicMute() {
  if (!state.micStream) return;
  var tracks = state.micStream.getAudioTracks();
  if (!tracks.length) return;
  var nowMuted = tracks[0].enabled;  // wenn aktuell enabled → wir muten
  tracks.forEach(function (t) { t.enabled = !nowMuted; });
  state.micMuted = nowMuted;
  _updateMicIndicatorState();
  console.log('[NerveLauncher] Mic ' + (state.micMuted ? 'muted' : 'unmuted') + ' (track.enabled toggle, no session restart)');
}

function _updateMicIndicatorState() {
  var indicator = pipEl('pip-mic-indicator');
  if (!indicator) return;
  indicator.classList.toggle('pip-mic-muted', !!state.micMuted);
  if (state.micMuted) {
    for (var i = 0; i < 4; i++) {
      var b = pipEl('pip-mic-bar-' + i);
      if (b) b.style.height = '15%';
    }
  }
}
```

**Änderung 4 — Cleanup in _stopMic (ca. Zeilen 1309-1316):**

Direkt am Anfang von _stopMic, VOR dem existierenden Code, ergänzen:

```javascript
if (state.micLevelRafId) { cancelAnimationFrame(state.micLevelRafId); state.micLevelRafId = null; }
if (state.micAnalyser) { try { state.micAnalyser.disconnect(); } catch(e){} state.micAnalyser = null; }
state.micMuted = false;
```

**Änderung 5 — Wire-up in _wirePipButtons (ca. Zeile 857-869):**

Innerhalb der Wire-up-Funktion ergänzen (neben den anderen onclick-Bindings):

```javascript
var micBtn = pipEl('pip-mic-indicator');
if (micBtn) micBtn.onclick = function () { _toggleMicMute(); };
```

**Änderung 6 — Show-Toggle in _showPipLive (ca. Zeile 940-965, neben dem Slider-Show-Pattern):**

Ergänzen an der Stelle, wo der Opacity-Slider sichtbar geschaltet wird:

```javascript
var micBtnShow = pipEl('pip-mic-indicator');
if (micBtnShow) micBtnShow.style.display = 'inline-flex';
```

**Wichtig:**
- Die AnalyserNode wird NUR an `source` connected, NICHT an `audioCtx.destination` — wir wollen keinen Audio-Echo, nur Level-Readings.
- Der bestehende `source.connect(workletNode)` und `workletNode.connect(audioCtx.destination)` bleibt unverändert.
- Bei Mute: `tracks.forEach(t => t.enabled = false)` → Deepgram empfängt silent Chunks, stop_live_session wird NICHT gesendet.
- Cleanup in _stopMic MUSS VOR dem workletNode.disconnect() stehen, damit der rAF keine disconnected AnalyserNode mehr adressiert.
- `pipEl()` verwenden (nicht `document.getElementById`) — DOM lebt im PiP-Document-Fenster.
- Logging via `console.log('[NerveLauncher] ...')` folgt bestehendem Pattern.
  </action>
  <verify>
    <automated>grep -c "createAnalyser" static/pip-launcher.js && grep -c "_toggleMicMute" static/pip-launcher.js && grep -c "_startMicLevelLoop" static/pip-launcher.js && grep -c "micLevelRafId" static/pip-launcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "createAnalyser" static/pip-launcher.js` findet 1 Match (in _startAudio)
    - `grep "state.micAnalyser = analyser" static/pip-launcher.js` findet 1 Match
    - `grep "fftSize = 256" static/pip-launcher.js` findet 1 Match
    - `grep "smoothingTimeConstant = 0.7" static/pip-launcher.js` findet 1 Match
    - `grep "function _startMicLevelLoop" static/pip-launcher.js` findet 1 Match
    - `grep "function _updateMicBarsDom" static/pip-launcher.js` findet 1 Match
    - `grep "function _toggleMicMute" static/pip-launcher.js` findet 1 Match
    - `grep "function _updateMicIndicatorState" static/pip-launcher.js` findet 1 Match
    - `grep "tracks.forEach" static/pip-launcher.js` findet mindestens 1 Match in _toggleMicMute
    - `grep "cancelAnimationFrame(state.micLevelRafId)" static/pip-launcher.js` findet 1 Match in _stopMic
    - `grep "micBtn.onclick = function" static/pip-launcher.js` findet 1 Match in _wirePipButtons
    - `grep "pip-mic-indicator.*display = 'inline-flex'" static/pip-launcher.js` findet 1 Match in _showPipLive
    - `grep "stop_live_session" static/pip-launcher.js` findet KEINEN neuen Match innerhalb _toggleMicMute (Mute darf Session nicht beenden)
    - State-Fields: `grep "micAnalyser: null" static/pip-launcher.js` UND `grep "micLevelRafId: null" static/pip-launcher.js` UND `grep "micMuted: false" static/pip-launcher.js` finden je 1 Match
  </acceptance_criteria>
  <done>Mic-Indikator pulsiert live mit Audio-Level; Click toggled zwischen aktiv (teal Bars) und muted (grau + flach) ohne Session-Abbruch; Cleanup beim _stopMic vollständig</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mute-Button click → MediaStreamTrack | Client-seitige Mute-Entscheidung; Server erhält entweder Audio oder Stille, kein Bypass möglich |
| AudioContext → AnalyserNode | Rein client-seitige Visualisierung; keine externen Datenflüsse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-04 | Information Disclosure (Audio leak nach Mute) | MediaStreamTrack.enabled = false | mitigate | `track.enabled = false` garantiert laut WebRTC-Spec null-bytes-Audio bis enabled wieder true wird. Mute-State spiegelt den Stream-Zustand 1:1 (state.micMuted = !tracks[0].enabled). Audio-chunks an Deepgram enthalten damit Silence während Mute. Kein Daten-Leak möglich. |
| T-06.1-05 | Denial of Service (rAF-Loop leak nach PiP close) | _startMicLevelLoop | mitigate | Cleanup in _stopMic cancelled rAF explizit (cancelAnimationFrame + analyser.disconnect + state-nulls). pagehide-Handler ruft _stopMic auf bei PiP-Close. Keine dangling refs. |
</threat_model>

<verification>
- `python app.py` starten, /live öffnen, PiP starten, Call beginnen (Mic-Consent erlauben)
- Header zeigt Mic-Indikator rechts neben Timer, 4 Balken in teal
- Ins Mikro sprechen/klopfen → Balken animieren sichtbar in Echtzeit
- Auf Mic-Indikator klicken → Balken werden grau und flach (15% alle); Indikator-Button wird leicht transparent (opacity 0.6)
- Im DevTools: `state.micStream.getAudioTracks()[0].enabled === false` nach Mute
- Deepgram-Connection bleibt alive — im Backend-Log KEIN "stop_live_session" beim Mute
- Erneut klicken → Balken werden wieder teal und animieren mit Audio-Level
- Call beenden → Cleanup in _stopMic disconnected AnalyserNode, cancelled rAF
- Keine Performance-Probleme in devtools Performance-Profile (rAF ~16 fps, kein CPU-Spike)
</verification>

<success_criteria>
- Mic-Indikator sichtbar im Live-Zustand, versteckt im Setup
- Echtzeit-Animation der Balken bei Sprache
- Click-to-Mute funktioniert ohne Session-Abbruch (Deepgram-Connection bleibt)
- Muted-State visuell klar erkennbar (grau + flach + reduziert opacity)
- Cleanup bei _stopMic vollständig (keine memory/CPU leaks)
- Alle grep-acceptance_criteria matchen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06.1-.../06.1-03-SUMMARY.md` (Mic-Indikator-Architektur, WebAudio-Graph-Diagramm verbal, Mute-Technik-Wahl Track.enabled vs track.stop).
</output>
