---
phase: 06.1-pip-uat-fixes-bugs-farben-proportionen-mic-indikator-slider
plan: "02"
subsystem: pip-live
tags: [layout, css, pip, teleprompter, ewb, light-mode, uat]

dependency_graph:
  requires: [06.1-01]
  provides:
    - PiP-Default-Groesse 480x760 px
    - Heller slate-50 Body mit dunklem Header
    - Teleprompter oben (60%), EWB-Pills Mitte (10%), KI-Slots unten (30%)
    - EWB-Buttons als horizontale nowrap-Pill-Reihe mit text-overflow:ellipsis
    - iOS-style Transparenz-Slider (140px, Filled-Portion in Teal)
  affects:
    - static/pip-launcher.js
    - templates/base.html

tech_stack:
  added: []
  patterns:
    - CSS rgba() mit CSS-Custom-Property fuer alpha-controlled Transparenz (--pip-bg-alpha)
    - linear-gradient fuer iOS-style Slider-Filled-Portion via --pip-slider-pct
    - flex:0 0 N% fuer fixe Split-Proportionen in einem column-flex-Container

key_files:
  created: []
  modified:
    - static/pip-launcher.js
    - templates/base.html

decisions:
  - "PiP-Fenster oeffnet standardmaessig mit 480x760 px statt 380x440"
  - "Body-Hintergrund ist slate-50 (#F8FAFC) statt dark (#06060a) — heller Body fuer besseren Kontrast ueber CRM"
  - "Header bekommt explizites #0D1117 statt var(--page-bg) damit er unabhaengig vom Body-Scheme dunkel bleibt"
  - "Zone-Divider auf rgba(0,0,0,0.08) statt rgba(255,255,255,0.08) — invertiertes Farbschema"
  - "EWB-Pills erhalten flex:1 1 0 + min-width:0 + text-overflow:ellipsis statt flex-wrap:wrap"

metrics:
  duration: "~10 Minuten"
  completed: "2026-04-15"
  tasks_completed: 2
  files_modified: 2
---

# Phase 06.1 Plan 02: Layout-Rebuild — Teleprompter oben, heller Body, 480x760 Summary

PiP-Farbschema invertiert (heller slate-50 Body, dunkler #0D1117 Header) und Split-Layout rotiert: Teleprompter dominiert oben mit 60%, EWB-Pills in einer horizontalen Pill-Reihe in der Mitte (10%), KI-Slots als weisse Cards unten (30%). Default-Fenstergroesse von 380x440 auf 480x760 erhoehen.

## Ergebnisse

### Task 1: PiP-Default-Size 480x760 + slate-50 Body-BG in JS (D-10, D-05)

**Aenderungen (`static/pip-launcher.js`):**

1. **requestWindow-Geometrie (Zeile 715):**
   - Alt: `{ width: 380, height: 440 }`
   - Neu: `{ width: 480, height: 760 }`

2. **PiP-Document-Body-Background (Zeile 819):**
   - Alt: `body.style.background = '#06060a'`
   - Neu: `body.style.background = '#F8FAFC';  // D-05: slate-50 light body`

**Commit:** `e974eb0`

---

### Task 2: Split-Layout-Rotation + helles Farbschema komplett in base.html (D-04 bis D-12)

**CSS-Aenderungen (`templates/base.html` Style-Block):**

| Regel | Vorher | Nachher |
|-------|--------|---------|
| `.pip-header` background | `var(--page-bg,#06060a)` | `#0D1117` (explizit, D-04) |
| `.pip-header` border-bottom | `var(--glass-border,#E2E8F0)` | `rgba(255,255,255,0.08)` |
| `.pip-live-split` background | `rgba(6,6,10,var(--pip-bg-alpha,1))` | `rgba(248,250,252,var(--pip-bg-alpha,1))` (D-05) |
| `.pip-ki-zone` flex | `flex:0 0 55%` | `flex:0 0 30%` (D-09) |
| `.pip-teleprompter` flex | `flex:1` | `flex:0 0 60%` (D-09) |
| `.pip-ewb-row` | `flex-wrap:wrap` | `flex:0 0 10%;flex-wrap:nowrap;overflow:hidden` (D-09, D-11) |
| `.pip-ki-slot` border | `rgba(255,255,255,0.08)` | `#E2E8F0` (D-08) |
| `.pip-ki-slot` background | `rgba(255,255,255,0.04)` | `#FFFFFF` (D-08) |
| `.pip-slot-label` color | `rgba(255,255,255,0.4)` | `#6B7280` (D-08) |
| `.pip-slot-body` color | `#e8ecf4` | `#1a1a1a` (D-08) |
| `.pip-zone-divider` background | `rgba(255,255,255,0.08)` | `rgba(0,0,0,0.08)` |
| `.tp-block` color | `rgba(255,255,255,0.4)` | `rgba(26,26,26,0.4)` (D-06) |
| `.tp-block-active` color | `#e8ecf4` | `#1a1a1a` (D-06) |
| `.tp-block-active` background | `rgba(0,212,170,0.06)` | `rgba(0,212,170,0.08)` (D-06) |
| `.pip-ewb-btn` | `background:none;color:var(--page-text-secondary)` | `background:#FFFFFF;color:#1a1a1a;flex:1 1 0;min-width:0;text-overflow:ellipsis` (D-11) |
| `#pip-opacity-slider` | `width:80px` | `width:140px;linear-gradient Filled-Portion` (D-17) |

**Markup-Rotation (`#pip-section-live`):**

Alt (KI oben 55%, Teleprompter unten 45%):
```
pip-ki-zone (mit pip-ewb-row darin)
pip-zone-divider
pip-teleprompter
```

Neu (D-09 — Teleprompter oben 60%, EWB Mitte 10%, KI unten 30%):
```
pip-teleprompter
pip-zone-divider
pip-ewb-row (eigenstaendige Zone)
pip-zone-divider
pip-ki-zone (nur Slots, kein EWB mehr)
```

Alle Slot-IDs (pip-slot-0, pip-slot-label-0, pip-slot-body-0, pip-slot-1 etc.) unveraendert — pip-launcher.js referenziert diese IDs direkt.

**Commit:** `1e783a6`

---

## Deviationen vom Plan

Keine — Plan wurde exakt wie geschrieben ausgefuehrt. BUG-2-Scrollbar-Regeln aus Plan 01 blieben unveraendert (konsistente rgba(0,0,0,0.15) Farbe bereits in Plan 01 auf hellen Body vorbereitet).

## Sicherheits-Review (T-06.1-03)

`--pip-bg-alpha` CSS-Custom-Property weiterhin unveraendert durch Slider-Clamp in `_initOpacitySlider` kontrolliert. Keine neuen Exfiltrations-Vektoren eingefuehrt. CSS-Farbwerte sind hard-coded Hexwerte ohne User-Input.

## Known Stubs

Keine — alle Farbwerte, Flex-Basis und Markup-Struktur vollstaendig implementiert. EWB-Buttons werden dynamisch per `_renderEwbButtons` in pip-launcher.js befuellt (unveraendert, BUG-1 bereits in Plan 01 behoben).

## Threat Flags

Keine neuen Angriffsvektoren eingefuehrt.

## Self-Check: PASSED

- `static/pip-launcher.js` geaendert: ja (Commit e974eb0)
- `templates/base.html` geaendert: ja (Commit 1e783a6)
- `grep "width: 480, height: 760"` → 1 Match (Zeile 715)
- `grep "#F8FAFC"` → 1 Match (Zeile 819)
- `grep "rgba(248,250,252,var(--pip-bg-alpha"` → 1 Match
- `grep "flex:0 0 60%"` → 1 Match (pip-teleprompter)
- `grep "flex:0 0 10%"` → 1 Match (pip-ewb-row)
- `grep "flex:0 0 30%"` → 1 Match (pip-ki-zone)
- `grep "flex-wrap:nowrap"` → 1 Match
- `grep "text-overflow:ellipsis"` → 1 Match
- `grep "background:#0D1117"` → 1 Match (pip-header)
- `grep "color:rgba(26,26,26,0.4)"` → 1 Match (tp-block)
- `grep "border-left-color:#00D4AA"` → 1 Match (tp-block-active)
- DOM-Reihenfolge: pip-teleprompter (Z.333) → pip-ewb-row (Z.341) → pip-ki-zone (Z.347)
- Alle 2 Commits vorhanden: e974eb0, 1e783a6
