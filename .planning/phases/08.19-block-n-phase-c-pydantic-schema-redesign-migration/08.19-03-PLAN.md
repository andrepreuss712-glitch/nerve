---
phase: 08.19
plan: 03
type: execute
wave: 3
depends_on:
  - 08.19-01
  - 08.19-02
files_modified:
  - routes/profiles.py
  - services/precall_service.py
  - static/pip-launcher.js
autonomous: true
requirements:
  - D-01
  - D-04
  - D-05

must_haves:
  truths:
    - "wizard_create() schreibt schema_version=2 und zielkunde.unternehmensgroesse in daten JSON"
    - "wizard_create() schreibt KEIN opener/pitch in daten JSON (D-01) — Wizard hat kein opener/pitch Formular-Feld (Code-Check-Befund)"
    - "Neue Profile via wizard_create() haben KEINEN ProfileOpener-Eintrag — das ist by design (kein Opener-Input im Wizard, User ergaenzt via /profiles/<pid>/opener Route)"
    - "bearbeiten() GET laeuft _migrate_profile_data() durch VOR Weiterverarbeitung"
    - "bearbeiten() POST schreibt consent_text in BEIDE: profiles.consent_text UND daten.meta.consent_text (D-04)"
    - "bearbeiten() POST ruft _migrate_profile_data() auf dem eingehenden daten_json VOR ProfileSchema.model_validate() auf (Finding 2 — verhindert false-positive 400 durch Altlasten)"
    - "bearbeiten() POST faengt ValidationError ab und gibt HTTP 400 + Flash-Message zurueck statt 500 (Finding 3)"
    - "precall_service.py liest opener/pitch aus ProfileOpener-Tabelle, NICHT aus profil_daten.get('opener')"
    - "pip-launcher.js Consent-Modal liest consent_text aus daten.meta.consent_text mit Fallback auf top-level consent_text (D-04 read-path)"
  artifacts:
    - path: "routes/profiles.py"
      provides: "wizard_create() mit schema_version + unternehmensgroesse + dual-write; bearbeiten() mit _migrate_profile_data chain + ValidationError-Handler"
      contains: "_migrate_profile_data"
    - path: "services/precall_service.py"
      provides: "opener/pitch aus ProfileOpener-Tabelle gelesen"
      contains: "ProfileOpener"
    - path: "static/pip-launcher.js"
      provides: "consent_text read-path: meta.consent_text bevorzugt, Fallback auf top-level (D-04)"
      contains: "meta.consent_text"
  key_links:
    - from: "routes/profiles.py wizard_create()"
      to: "daten JSON"
      via: "json.dumps({'schema_version': 2, 'zielkunde': {'unternehmensgroesse': ...}})"
      pattern: "schema_version.*2"
    - from: "routes/profiles.py bearbeiten() POST"
      to: "_migrate_profile_data() + ProfileSchema.model_validate()"
      via: "strip legacy fields before strict validation — Finding 2 + Finding 3"
      pattern: "_migrate_profile_data\\|ValidationError"
    - from: "services/precall_service.py"
      to: "ProfileOpener"
      via: "db.query(ProfileOpener).filter_by(profile_id=...)"
      pattern: "ProfileOpener"
    - from: "static/pip-launcher.js _showConsentModal()"
      to: "daten.meta.consent_text"
      via: "state.profileDaten.meta.consent_text || state.profileDaten.consent_text || CONSENT_DEFAULT_TEXT"
      pattern: "meta.*consent_text"
---

<objective>
Read/Write-Pfad anpassen: (1a) `routes/profiles.py` Imports + wizard_create() + bearbeiten() GET auf neues Schema umstellen; (1b) bearbeiten() POST consent_text dual-write mit exaktem Code — Reviews v2: _migrate_profile_data() VOR Validation (Finding 2) + ValidationError abfangen (Finding 3); (2) `services/precall_service.py` opener/pitch-Read auf ProfileOpener-Tabelle umstellen; (3) `static/pip-launcher.js` consent_text read-path auf `meta.consent_text` umstellen (D-04 read-side).

Purpose: Nach Schema-Definition (Plan 01) und DB-Migration (Plan 02) muss der schreibende und lesende Code die neuen Strukturen nutzen. Kritisch: precall_service.py las opener/pitch aus daten-JSON (jetzt geloescht). DSGVO-kritisch: pip-launcher.js Consent-Modal las consent_text aus top-level daten-JSON (jetzt in daten.meta.consent_text). Reviews v2 adressiert zusaetzlich: Legacy-Daten im POST-Handler wuerden ohne _migrate_profile_data() die extra='forbid' Validation als false-positive 400 triggern; ValidationError ohne Catch wuerde 500 liefern.

Code-Check-Befund (Reviews v2 Finding 1 — wizard_create opener/pitch):
  Der Wizard (routes/profiles.py Zeilen 91-153) hat KEIN opener/pitch Formular-Feld.
  Das Template (templates/profile_wizard.html) hat KEIN opener/pitch Eingabefeld.
  Opener/pitch wird exklusiv ueber dedizierte Routen verwaltet:
    GET/POST /profiles/<pid>/opener — schreibt direkt in ProfileOpener-Tabelle.
  Konsequenz: wizard_create() macht KEINEN ProfileOpener-INSERT (keine Daten vorhanden).
  Neue Profile starten ohne Opener-Eintrag — by design.
  Graceful degradation in precall_service: kein Opener = kein Opener-Block im Briefing.
  User ergaenzt Opener manuell via Profil-Editor nach Wizard-Abschluss.

Output: ~40 Zeilen geaendert in routes/profiles.py, ~15 Zeilen geaendert in services/precall_service.py, ~3 Zeilen geaendert in static/pip-launcher.js
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-CONTEXT.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-01-SUMMARY.md
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-02-SUMMARY.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/CLAUDE.md
@C:/Users/andre/dev/salesnerve/routes/CLAUDE.md

<interfaces>
<!-- Blueprint-Name aus routes/profiles.py Zeile 10 (verifiziert): -->
<!-- profiles_bp = Blueprint('profiles', __name__, url_prefix='/profiles') -->
<!-- Blueprint-Name ist 'profiles' — url_for('profiles.bearbeiten', pid=...) -->
<!-- NICHT 'profiles_bp' — das ist der Python-Variablenname, nicht der Blueprint-Name -->

<!-- wizard_create() aktueller daten-Dict (routes/profiles.py Zeilen 118-132) -->
```python
daten = json.dumps({
    'basis': {
        'produktbeschreibung': produkt,
        'zielkunden': zielkunden,
        'einwaende': einwaende_list,
        'phasen': [],
    },
    'ki': {'anrede': 'Sie'},
    'meta': {'firma': firma, 'rolle': rolle},
}, ensure_ascii=False)
```
<!-- Nach Aenderung muss schema_version=2, zielkunde.unternehmensgroesse drin sein -->
<!-- opener/pitch werden NICHT hinzugefuegt — Wizard hat kein opener/pitch Formular-Feld -->
<!-- Kein ProfileOpener-INSERT in wizard_create() — by design (Code-Check-Befund) -->

<!-- bearbeiten() Route-Signatur (routes/profiles.py Zeile 156-158, verifiziert): -->
<!-- @profiles_bp.route('/<int:pid>/edit', methods=['GET', 'POST']) -->
<!-- def bearbeiten(pid):  <-- Parameter heisst 'pid', nicht 'id' -->
<!-- url_for korrekt: url_for('profiles.bearbeiten', pid=p.id) -->

<!-- bearbeiten() GET read pattern (routes/profiles.py Zeilen 187-193) -->
```python
try:
    daten = json.loads(p.daten) if p.daten else {}
except Exception:
    daten = {}
# Phase 08.5 Korrektur 1: migrate tabu_begriffe to list-of-objects on editor load
daten = migrate_tabu_begriffe(daten)
```
<!-- Nach 08.19: _migrate_profile_data() VOR migrate_tabu_begriffe() -->

<!-- bearbeiten() POST: aktueller Stand (routes/profiles.py Zeile ~168-179, verifiziert) -->
```python
daten_json = request.form.get('daten_json', p.daten or '{}')
try:
    json.loads(daten_json)
except Exception:
    daten_json = p.daten or '{}'
p.name    = request.form.get('name', p.name).strip()
p.branche = _normalize_branche(request.form.get('branche', p.branche or ''))
p.daten   = daten_json
p.consent_text = request.form.get('consent_text', p.consent_text or '').strip() or None
db.commit()
```
<!-- Reviews v2: _migrate_profile_data() VOR Validation (Finding 2), ValidationError catch (Finding 3) -->

<!-- precall_service.py aktueller opener/pitch-Read (Zeilen 150-155) -->
```python
opener = profil_daten.get('opener', '')
pitch = profil_daten.get('pitch', '')
if opener:
    profile_parts.append(f"Opener: {opener}")
if pitch:
    profile_parts.append(f"Pitch: {pitch}")
```
<!-- MUSS ersetzt werden durch ProfileOpener-Tabellen-Query -->

<!-- ProfileOpener-Query-Pattern (aus routes/profiles.py Zeilen 337-338) -->
```python
items = db.query(ProfileOpener).filter_by(profile_id=pid).order_by(
    ProfileOpener.sortierung, ProfileOpener.id
).all()
```

<!-- pip-launcher.js aktueller consent_text Read (Zeile 678) -->
```javascript
var text = (state.profileDaten && state.profileDaten.consent_text)
  ? state.profileDaten.consent_text
  : CONSENT_DEFAULT_TEXT;
```
<!-- profileDaten ist das volle daten-JSON aus der DB -->
<!-- Nach Migration ist consent_text in daten.meta.consent_text, nicht top-level -->
<!-- D-04 read-path: bevorzuge meta.consent_text, Fallback auf top-level (Uebergangssicherheit) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: routes/profiles.py — Import + wizard_create() + bearbeiten() GET anpassen (Code-Check-Befund dokumentiert)</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/routes/profiles.py (komplett — wizard_create Zeilen 91-153, bearbeiten GET Zeilen 156-200)
    - C:/Users/andre/dev/salesnerve/templates/profile_wizard.html (grep fuer 'opener' und 'pitch' — Verifikation dass kein Formular-Feld existiert)
    - C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-01-SUMMARY.md
  </read_first>

  <files>routes/profiles.py</files>

  <action>
Aenderung 1 — Imports hinzufuegen (am Anfang des Files nach bestehenden Imports):
```python
from services.profile_schema import _migrate_profile_data, ProfileSchema
from pydantic import ValidationError
```

Aenderung 2 — wizard_create(): daten-Dict erweitern.
Das bestehende `daten = json.dumps({...}, ensure_ascii=False)` (Zeilen 118-132) ersetzen durch:
```python
# Phase 08.19: schema_version=2 + zielkunde.unternehmensgroesse (D-02, D-03, D-07)
# Code-Check-Befund (Reviews v2): Wizard hat KEIN opener/pitch Formular-Feld.
# wizard_create() macht KEINEN ProfileOpener-INSERT — keine Opener-Daten vorhanden.
# Neue Profile starten ohne Opener-Eintrag (by design).
# User ergaenzt Opener via Profil-Editor -> /profiles/<pid>/opener Route.
# Graceful degradation: precall_service ohne Opener liefert kein Opener-Block im Briefing.
unternehmensgroesse = request.form.get('unternehmensgroesse', '').strip() or None

daten = json.dumps({
    'schema_version': 2,
    'basis': {
        'produktbeschreibung': produkt,
        'zielkunden': zielkunden,
        'einwaende': einwaende_list,
        'phasen': [],
    },
    'zielkunde': {
        'unternehmensgroesse': unternehmensgroesse,
    },
    'ki': {
        'anrede': 'Sie',
    },
    'meta': {
        'firma': firma,
        'rolle': rolle,
    },
    # opener/pitch werden NICHT in daten gespeichert (D-01) — canonical: ProfileOpener-Tabelle
    # Kein ProfileOpener-INSERT hier: Wizard hat kein opener/pitch Eingabefeld (Code-Check-Befund)
}, ensure_ascii=False)
```

Aenderung 3 — bearbeiten() GET: `_migrate_profile_data()` VOR `migrate_tabu_begriffe()` einfuegen.
Die bestehende Zeile:
```python
daten = migrate_tabu_begriffe(daten)
```
Ersetzen durch:
```python
# Phase 08.19: Schema v1 -> v2 Migration (idempotent) VOR Tabu-Migration
daten = _migrate_profile_data(daten)
daten = migrate_tabu_begriffe(daten)
```
  </action>

  <acceptance_criteria>
    - `grep -n "from services.profile_schema import" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt 1 Treffer mit `_migrate_profile_data` und `ProfileSchema`
    - `grep -n "from pydantic import ValidationError" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt 1 Treffer
    - `grep -n "schema_version" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 1 Treffer im wizard_create-Dict
    - `grep -n "unternehmensgroesse" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 1 Treffer (request.form.get + daten-Dict)
    - `grep -n "_migrate_profile_data" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 1 Treffer (bearbeiten GET)
    - Reviews v2 Code-Check-Befund: `grep -n "opener\|pitch" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 0 Treffer fuer Formular-Felder (nur Kommentare OK) — Wizard hat weiterhin KEIN opener/pitch Eingabefeld (D-01 Enforcement)
    - Reviews v2 Code-Check-Befund: `grep -n "ProfileOpener" C:/Users/andre/dev/salesnerve/routes/profiles.py | grep "wizard_create"` gibt 0 Treffer — wizard_create() macht KEINEN ProfileOpener-INSERT (by design, kein opener/pitch Input vorhanden)
    - `python -c "from routes.profiles import profiles_bp"` laeuft ohne ImportError
    - Nach wizard_create() via Flask test_client: `ProfileOpener.query.filter_by(profile_id=new_profile_id).count() == 0` — korrekt, da kein opener-Input im Wizard
  </acceptance_criteria>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import app, json
from services.profile_schema import _migrate_profile_data, ProfileReadSchema
from database.db import get_session
from database.models import Profile, ProfileOpener

# Runtime-Test 1: Migration-Chain (GET-Path) — v1 -> v2 korrekt
v1_daten = {'opener': 'X', 'zielgruppe': {'alter': '30', 'vorwissen': 'hoch'}}
result = _migrate_profile_data(v1_daten.copy())
assert result.get('schema_version') == 2, 'Migration setzt schema_version=2'
assert 'opener' not in result, 'Migration entfernt opener-Key'

# Runtime-Test 2: wizard_create() Behavioral-Test via Flask test_client POST
# Prueft dass erstellte Profile schema_version=2 + zielkunde.unternehmensgroesse enthalten
# UND keinen ProfileOpener-Eintrag haben (Code-Check-Befund: kein opener-Input im Wizard)
with app.app.test_client() as client:
    client.post('/auth/login', data={'email': 'demo@getnerve.app', 'password': 'demo'}, follow_redirects=True)
    db = get_session()
    count_before = db.query(Profile).count()
    db.close()

    client.post('/profiles/wizard', data={
        'firma': 'TestGmbH08',
        'produkt': 'Testprodukt fuer Pytest',
        'zielkunden': 'KMU-Einkauf',
        'rolle': 'Sales',
        'unternehmensgroesse': '<10',
        'branche': 'it',
    }, follow_redirects=True)

db = get_session()
try:
    for p in db.query(Profile).all():
        daten = json.loads(p.daten or '{}')
        if daten.get('schema_version') == 2:
            assert 'zielkunde' in daten, f'Profil {p.id}: zielkunde-Key fehlt in v2-Profil'
            assert 'opener' not in daten, f'Profil {p.id}: opener darf nicht in wizard-Profil sein (D-01)'
            ug = daten['zielkunde'].get('unternehmensgroesse')
            print(f'Profil {p.id}: schema_version=2, unternehmensgroesse={ug} OK')
            # Code-Check-Befund: Neue Profile ueber Wizard haben keinen ProfileOpener-Eintrag
            opener_count = db.query(ProfileOpener).filter_by(profile_id=p.id).count()
            # Koennte 0 sein (neues Profil ohne Opener) oder >0 (migriertes Profil mit Opener)
            # Fuer TestGmbH08-Profil (neu ueber Wizard): 0 erwartet
            print(f'Profil {p.id}: ProfileOpener-Eintraege: {opener_count} (0 fuer neue Wizard-Profile erwartet)')
finally:
    db.close()

# Runtime-Test 3: Schema-Konformitaet des wizard_create Outputs
test_daten = {
    'schema_version': 2,
    'basis': {'produktbeschreibung': 'Test', 'zielkunden': 'KMU', 'einwaende': [], 'phasen': []},
    'zielkunde': {'unternehmensgroesse': '<10'},
    'ki': {'anrede': 'Sie'},
    'meta': {'firma': 'TestGmbH', 'rolle': 'Verkaeufer'},
}
validated = ProfileReadSchema.model_validate(test_daten)
assert validated.zielkunde.unternehmensgroesse == '<10', 'unternehmensgroesse Enum-Wert validiert'
assert validated.schema_version == 2
print('Task 1a: Migration-Chain, wizard_create Behavioral-Test und Schema-Konformitaet OK')
print('Code-Check-Befund bestaetigt: wizard_create() macht keinen ProfileOpener-INSERT (by design)')
"
    </automated>
  </verify>

  <done>
    - Import `from services.profile_schema import _migrate_profile_data, ProfileSchema` am Dateianfang vorhanden
    - Import `from pydantic import ValidationError` am Dateianfang vorhanden
    - wizard_create() schreibt schema_version=2 und zielkunde.unternehmensgroesse in daten
    - wizard_create() schreibt kein opener/pitch in daten (D-01)
    - wizard_create() macht KEINEN ProfileOpener-INSERT (kein opener-Input im Wizard — Code-Check-Befund, by design)
    - Code-Check-Befund als Kommentar im wizard_create()-Block dokumentiert (5 Zeilen Erklaerung)
    - bearbeiten() GET ruft _migrate_profile_data() VOR migrate_tabu_begriffe() auf
    - `python -c "from routes.profiles import profiles_bp"` laeuft ohne ImportError
  </done>
</task>

<task type="auto">
  <name>Task 1b: routes/profiles.py — bearbeiten() POST: migrate-before-validate + ValidationError-Handler + consent_text dual-write (Reviews v2)</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/routes/profiles.py (bearbeiten() POST-Branch, Zeilen ~156-195 — exakten Stand nach Task 1a lesen)
    - C:/Users/andre/dev/salesnerve/routes/CLAUDE.md (url_for-Konventionen: Blueprint-Name 'profiles', Route-Parameter 'pid')
  </read_first>

  <files>routes/profiles.py</files>

  <action>
DSGVO-kritisch (D-04) + Reviews v2 (Finding 2 + Finding 3): bearbeiten() POST muss eingehende Legacy-Daten strippen (Finding 2), ValidationError abfangen (Finding 3) und consent_text in BEIDE Locations schreiben (D-04).

url_for-Verifikation (PFLICHT per routes/CLAUDE.md):
- Blueprint-Name: 'profiles' (Zeile 10: `profiles_bp = Blueprint('profiles', __name__, ...)`)
- Route-Funktion: `bearbeiten(pid)` (Zeile 158: `def bearbeiten(pid):`)
- Korrekt: `url_for('profiles.bearbeiten', pid=p.id)`
- FALSCH waere: `url_for('profiles_bp.bearbeiten', id=p.id)` — 'profiles_bp' ist Python-Variablenname, nicht Blueprint-Name; 'id' ist falscher Parameter-Name

Finde im POST-Branch von bearbeiten() die Zeilen (aktueller Stand Zeilen ~168-179):
```python
p.daten   = daten_json
p.consent_text = request.form.get('consent_text', p.consent_text or '').strip() or None
db.commit()
```

Ersetze diese durch (exakte Implementierung — Reviews v2, keine Alternativen):
```python
# Phase 08.19 Reviews v2: consent_text dual-write (D-04) + Finding 2 (migrate before validate) + Finding 3 (ValidationError 400)

# Schritt 1: neuen consent_text aus Form lesen
_new_consent = request.form.get('consent_text', p.consent_text or '').strip() or None

# Schritt 2: daten_json aus Form parsen (daten_json wurde bereits oben aus request.form gelesen)
try:
    _daten_neue = json.loads(daten_json) if daten_json else {}
except Exception:
    _daten_neue = {}

# Schritt 2b: Finding 2 — v1->v2 Altlasten-Stripping VOR Write-Validation
# Entfernt opener/pitch + B2C-Felder bevor extra='forbid' validiert (verhindert false-positive 400)
_daten_neue = _migrate_profile_data(_daten_neue)

# Schritt 3: Finding 3 — Pydantic Write-Validation, ValidationError -> HTTP 400 (kein 500)
try:
    ProfileSchema.model_validate(_daten_neue)
except ValidationError as _ve:
    flash(f"Profil-Daten ungültig ({_ve.error_count()} Fehler) — bitte Eingabe prüfen.", 'error')
    db.rollback()
    return redirect(url_for('profiles.bearbeiten', pid=p.id))

# Schritt 4: consent_text in daten.meta schreiben (JSON-Side des dual-write)
if not isinstance(_daten_neue.get('meta'), dict):
    _daten_neue['meta'] = {}
_daten_neue['meta']['consent_text'] = _new_consent or ''  # NULL -> '' (Finding 4)

# Schritt 5: aktualisiertes daten-JSON in DB schreiben
p.daten = json.dumps(_daten_neue, ensure_ascii=False)

# Schritt 6: DB-Column setzen (bleibt in 08.19 erhalten — DB-Column-Drop in spaeterer Phase)
p.consent_text = _new_consent

db.commit()
```

WICHTIG — Variable `daten_json` Pruefung: Sie muss bereits VOR dieser Sequenz aus `request.form.get('daten_json', p.daten or '{}')` gelesen worden sein. Lese den aktuellen Code-Zustand nach Task 1a, um sicherzustellen dass `daten_json` vor Schritt 2 gesetzt ist. Falls der bestehende Code `daten_json` bereits definiert und validiert hat (try/except json.loads), beginne ab Schritt 1 ohne Redundanz.

Die Zeile `p.daten = daten_json` (rohe Zuweisung ohne consent_text-Einbettung) MUSS durch obige Sequenz ersetzt werden.

HINWEIS: Der log_action()-Aufruf und der AJAX-Response-Check (X-Requested-With) bleiben unveraendert nach db.commit() — ersetze NUR den Block von `p.daten = daten_json` bis `db.commit()`.
  </action>

  <acceptance_criteria>
    - `grep -n "_migrate_profile_data" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 2 Treffer (GET-Branch + POST-Branch)
    - `grep -n "ProfileSchema.model_validate" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 1 Treffer (POST-Branch Validation)
    - `grep -n "except ValidationError" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 1 Treffer (Finding 3 Handler)
    - `grep -n "profiles.bearbeiten" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 1 Treffer — url_for-Aufruf nutzt Blueprint-Name 'profiles' (nicht 'profiles_bp')
    - `grep -n "profiles_bp.bearbeiten" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt 0 Treffer — falscher Blueprint-Name 'profiles_bp' darf nicht vorkommen
    - `grep -n "url_for.*bearbeiten.*id=" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt 0 Treffer — Parameter muss 'pid' sein, nicht 'id'
    - `grep -n "meta.*consent_text\|_daten_neue\[.meta.\]" C:/Users/andre/dev/salesnerve/routes/profiles.py` gibt mindestens 1 Treffer (D-04 dual-write JSON-Side)
    - `python -c "from routes.profiles import profiles_bp"` laeuft ohne ImportError
  </acceptance_criteria>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import app, json
from database.db import get_session
from database.models import Profile
from services.profile_schema import _migrate_profile_data, ProfileSchema
from pydantic import ValidationError

# Runtime-Test 1: Finding 2 — Legacy-Daten mit opener/pitch werden korrekt gestripped VOR Validation
# Simuliert eingehenden POST-Body mit Altlasten (Frontend hat noch v1-Daten geschickt)
_legacy_daten = {
    'opener': 'Guten Tag, ich rufe wegen...',
    'pitch': 'Wir bieten...',
    'basis': {'produktbeschreibung': 'Test', 'zielkunden': 'KMU', 'einwaende': [], 'phasen': []},
    'ki': {'anrede': 'Sie'},
    'meta': {'firma': 'TestGmbH', 'rolle': 'Sales'},
}
_migrated = _migrate_profile_data(_legacy_daten.copy())
# Nach Migration: opener/pitch weg, schema_version=2 — ProfileSchema akzeptiert das
try:
    ProfileSchema.model_validate(_migrated)
    print('Finding 2: Legacy-Daten nach _migrate_profile_data() bestehen ProfileSchema.model_validate() — kein false-positive 400')
except ValidationError as e:
    raise AssertionError(f'Finding 2 FAILED: _migrate_profile_data() stripping unvollstaendig: {e}')

# Runtime-Test 2: Finding 3 — ValidationError wird korrekt abgefangen (kein 500)
_invalid_daten = {'schema_version': 2, 'basis': 'KEIN_DICT_SONDERN_STRING'}  # ungueltig
try:
    ProfileSchema.model_validate(_invalid_daten)
    print('WARNUNG: Ungueltige Daten wurden akzeptiert — pruefe ProfileSchema Definition')
except ValidationError as e:
    print(f'Finding 3: ValidationError korrekt raised mit {e.error_count()} Fehlern — wird in Route als 400 behandelt')

# Runtime-Test 3: consent_text dual-write Logik (D-04)
db = get_session()
try:
    p = db.query(Profile).first()
    if not p:
        print('Kein Profil in DB — skip runtime test')
    else:
        _test_consent = 'Test-Vorlesetext fuer DSGVO'
        _daten_dict = json.loads(p.daten or '{}')
        _daten_dict = _migrate_profile_data(_daten_dict)
        if not isinstance(_daten_dict.get('meta'), dict):
            _daten_dict['meta'] = {}
        _daten_dict['meta']['consent_text'] = _test_consent
        _serialized = json.dumps(_daten_dict, ensure_ascii=False)
        _reparsed = json.loads(_serialized)
        assert _reparsed.get('meta', {}).get('consent_text') == _test_consent, \
            f'meta.consent_text nicht korrekt gesetzt: {_reparsed.get(\"meta\", {})}'
        print(f'D-04 dual-write Logic OK: meta.consent_text = {_reparsed[\"meta\"][\"consent_text\"][:30]}...')
finally:
    db.close()

# url_for Verifikation: Blueprint-Name 'profiles', Route-Parameter 'pid'
# Behavioral test via Flask test_request_context() — prueft ob url_for('profiles.bearbeiten', pid=1)
# aufloest ohne BuildError (nicht nur ob Code-String existiert — kein SOURCE-PRESENCE-FALSE-GREEN)
with app.app.test_request_context():
    from flask import url_for as _uf
    _resolved = _uf('profiles.bearbeiten', pid=1)
    assert 'profiles' in _resolved or 'bearbeiten' in _resolved or '/1/' in _resolved, \
        f"url_for('profiles.bearbeiten', pid=1) resolved to unexpected path: {_resolved}"
    print(f'Blueprint url_for resolves correctly: {_resolved}')

# Import-Test
from routes.profiles import profiles_bp
print('Task 1b: Finding 2 + Finding 3 + D-04 dual-write + url_for verifiziert, Blueprint importiert OK')
"
    </automated>
  </verify>

  <done>
    - bearbeiten() POST ruft _migrate_profile_data() auf _daten_neue VOR ProfileSchema.model_validate() (Finding 2)
    - bearbeiten() POST faengt ValidationError ab und redirectet mit flash() zu bearbeiten() GET (HTTP 400, kein 500) (Finding 3)
    - url_for('profiles.bearbeiten', pid=p.id) — Blueprint-Name 'profiles' (nicht 'profiles_bp'), Parameter 'pid' (nicht 'id')
    - bearbeiten() POST liest daten_json aus Form, parst als JSON-Dict
    - Setzt _daten_neue['meta']['consent_text'] = _new_consent or '' (JSON-Side, NULL -> '') (Finding 4)
    - Schreibt p.daten = json.dumps(_daten_neue) mit consent_text eingebettet
    - Schreibt p.consent_text = _new_consent (DB-Column-Side, bleibt in 08.19)
    - Kein Datenverlust: alle anderen Felder in daten_neue bleiben erhalten
    - `python -c "from routes.profiles import profiles_bp"` laeuft ohne ImportError
  </done>
</task>

<task type="auto">
  <name>Task 2: services/precall_service.py — opener/pitch Read-Pfad auf ProfileOpener-Tabelle umstellen</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/services/precall_service.py (Zeilen 140-160: aktueller opener/pitch-Read)
    - C:/Users/andre/dev/salesnerve/database/models.py (Zeilen 161-168: ProfileOpener-Modell)
  </read_first>

  <files>services/precall_service.py</files>

  <action>
Das Problem: precall_service.py liest `profil_daten.get('opener', '')` und `profil_daten.get('pitch', '')`. Nach der 08.19-Migration sind diese Keys aus daten-JSON geloescht. Das wuerde stille leere Strings liefern — falsch.

Graceful-degradation-Semantik (Code-Check-Befund): Neue Profile via wizard_create() haben KEINEN ProfileOpener-Eintrag (Wizard hat kein opener-Eingabefeld). profile_id=None oder Profile ohne Eintraege liefern leeren Opener-Block im Briefing — kein Crash, kein False-Error. Das ist das korrekte Verhalten fuer neue Profile.

Aenderung 1 — Import hinzufuegen:
```python
from database.db import get_session
from database.models import ProfileOpener
```

Aenderung 2 — `recherche_firma()` Signatur: `profile_id=None` Parameter hinzufuegen (optional, backward-compatible):
```python
def recherche_firma(firmenname, ansprechpartner=None, branche=None, profil_daten=None, user_id=None, profile_id=None):
```

Aenderung 3 — `_generiere_briefing()` Signatur: `profile_id=None` Parameter hinzufuegen:
```python
def _generiere_briefing(firmenname, ansprechpartner, branche, suchergebnisse, profil_daten=None, user_id=None, profile_id=None):
```

Aenderung 4 — `recherche_firma()` Aufruf an `_generiere_briefing()`: `profile_id=profile_id` weitergeben.

Aenderung 5 — In `_generiere_briefing()`: Die Zeilen mit `profil_daten.get('opener', '')` und `profil_daten.get('pitch', '')` ersetzen durch ProfileOpener-Tabellen-Query:
```python
# Phase 08.19: opener/pitch aus ProfileOpener-Tabelle lesen (D-01)
# profil_daten.get('opener') ist nach Migration leer — canonical: profile_opener-Tabelle
# Graceful degradation: kein profile_id oder keine Eintraege = kein Opener-Block (kein Crash)
# Neue Profile via wizard_create() haben 0 ProfileOpener-Eintraege — das ist by design
if profile_id:
    try:
        _db = get_session()
        try:
            _opener_rows = _db.query(ProfileOpener).filter_by(
                profile_id=profile_id
            ).order_by(ProfileOpener.sortierung, ProfileOpener.id).all()
            for _op_row in _opener_rows:
                if _op_row.inhalt:
                    _label = _op_row.name or 'Opener'
                    profile_parts.append(f"{_label}: {_op_row.inhalt}")
        finally:
            _db.close()
    except Exception as _e:
        print(f"[PreCall] ProfileOpener-Query fuer Profil {profile_id} fehlgeschlagen: {_e}")
# Wenn profile_id=None: kein Opener im Briefing (graceful degradation, kein Crash)
```

Die alten Zeilen:
```python
opener = profil_daten.get('opener', '')
pitch = profil_daten.get('pitch', '')
if opener:
    profile_parts.append(f"Opener: {opener}")
if pitch:
    profile_parts.append(f"Pitch: {pitch}")
```
werden VOLLSTAENDIG ERSETZT durch den ProfileOpener-Query-Block oben.
  </action>

  <acceptance_criteria>
    - `grep -n "from database.models import.*ProfileOpener\|from database.models import ProfileOpener" C:/Users/andre/dev/salesnerve/services/precall_service.py` gibt mindestens 1 Treffer
    - `grep -n "profile_id" C:/Users/andre/dev/salesnerve/services/precall_service.py` gibt mindestens 2 Treffer (recherche_firma Signatur + _generiere_briefing Signatur)
    - `grep -n "profil_daten.get.*opener\|profil_daten.get.*pitch" C:/Users/andre/dev/salesnerve/services/precall_service.py` gibt 0 Treffer (alter Read-Pfad vollstaendig entfernt)
    - `grep -n "ProfileOpener" C:/Users/andre/dev/salesnerve/services/precall_service.py` gibt mindestens 2 Treffer (Import + Query)
    - `python -c "from services.precall_service import recherche_firma; import inspect; assert 'profile_id' in inspect.signature(recherche_firma).parameters"` laeuft ohne Fehler
  </acceptance_criteria>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
from services.precall_service import recherche_firma
import inspect

# Runtime-Test 1: Signatur hat profile_id Parameter
sig = inspect.signature(recherche_firma)
params = list(sig.parameters.keys())
assert 'profile_id' in params, f'profile_id fehlt in recherche_firma Signatur: {params}'
print(f'Signatur OK: {params}')

# Runtime-Test 2: Aufruf mit profile_id=None crasht nicht
# (kein Opener im Briefing bei None — graceful degradation per D-01 und Code-Check-Befund)
try:
    result, error = recherche_firma('TestFirma GmbH', profile_id=None)
    print(f'recherche_firma(profile_id=None): result-type={type(result)}, error={error!r:.50}')
except Exception as e:
    # Nur Brave-API-Fehler sind OK — ImportError oder AttributeError waeren Bugs
    if 'Brave' in str(e) or 'API' in str(e) or 'request' in str(e).lower() or 'connect' in str(e).lower():
        print(f'recherche_firma: expected API-Fehler (kein Crash): {e!r:.80}')
    else:
        raise AssertionError(f'Unerwarteter Fehler in recherche_firma: {e}')

# Runtime-Test 3: ProfileOpener direkt aus database.models importierbar
from database.models import ProfileOpener as _PO
print('ProfileOpener importierbar aus database.models: OK')
print('Task 2: alle Runtime-Tests bestanden')
print('Graceful degradation bestaetigt: profile_id=None liefert leeren Opener-Block (kein Crash)')
"
    </automated>
  </verify>

  <done>
    - precall_service.py importiert ProfileOpener und get_session
    - recherche_firma() hat profile_id=None Parameter (backward-compatible)
    - _generiere_briefing() hat profile_id=None Parameter
    - opener/pitch aus profil_daten.get() Zeilen sind entfernt und durch ProfileOpener-Query ersetzt
    - Wenn profile_id=None: kein Crash, kein Opener im Briefing (graceful degradation, dokumentiert)
    - Neue Profile via wizard_create() haben 0 ProfileOpener-Eintraege — precall_service liefert leeren Opener-Block — korrekt
    - `python -c "from services.precall_service import recherche_firma"` laeuft ohne ImportError
  </done>
</task>

<task type="auto">
  <name>Task 3: static/pip-launcher.js — consent_text read-path auf meta.consent_text (D-04)</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/static/pip-launcher.js (Zeilen 665-690: _showConsentModal() Funktion)
  </read_first>

  <files>static/pip-launcher.js</files>

  <action>
DSGVO-kritisch (D-04 read-side): Der Consent-Modal in pip-launcher.js liest aktuell:
```javascript
var text = (state.profileDaten && state.profileDaten.consent_text)
  ? state.profileDaten.consent_text
  : CONSENT_DEFAULT_TEXT;
```

`state.profileDaten` ist das volle daten-JSON aus der DB. Nach der 08.19-Migration liegt `consent_text` in `daten.meta.consent_text`, nicht mehr als Top-Level-Key. Ohne diese Aenderung zeigt der Consent-Modal immer den CONSENT_DEFAULT_TEXT — auch wenn der User einen eigenen Text konfiguriert hat.

Ersetze die 3 Zeilen durch (D-04 read-path: meta.consent_text bevorzugen, Fallback fuer Uebergangssicherheit):
```javascript
// Phase 08.19: D-04 read-path — consent_text jetzt in daten.meta.consent_text
// Fallback auf top-level consent_text fuer Profile die noch nicht migriert sind
var _meta = state.profileDaten && state.profileDaten.meta;
var text = (_meta && _meta.consent_text)
  ? _meta.consent_text
  : ((state.profileDaten && state.profileDaten.consent_text)
      ? state.profileDaten.consent_text
      : CONSENT_DEFAULT_TEXT);
```

Exakte Zeile die ersetzt wird (Zeile ~678):
```
var text = (state.profileDaten && state.profileDaten.consent_text)
  ? state.profileDaten.consent_text
  : CONSENT_DEFAULT_TEXT;
```

HINWEIS: `meta` ist ein JS-Variablenname (kein Umlaut), `consent_text` ist ein Dict-Key (kein Umlaut) — ASCII-Pflicht gilt hier korrekt. Nur der Kommentar darf Umlaute enthalten.
  </action>

  <acceptance_criteria>
    - `grep -n "meta.consent_text\|_meta" C:/Users/andre/dev/salesnerve/static/pip-launcher.js` gibt mindestens 2 Treffer (var _meta + _meta.consent_text Nutzung)
    - `grep -n "CONSENT_DEFAULT_TEXT" C:/Users/andre/dev/salesnerve/static/pip-launcher.js` gibt mindestens 1 Treffer (letzter Fallback noch vorhanden)
    - Alter top-level-only Read-Pfad (3-zeilige Form ohne meta) ist durch neue 4-zeilige Fallback-Kette ersetzt
    - `python -c "open('static/pip-launcher.js', encoding='utf-8').read()"` laeuft ohne UnicodeDecodeError (Encoding-Integritaet)
  </acceptance_criteria>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
# Runtime-Verifikation: JS-Datei laedt korrekt + Logik ist semantisch korrekt
with open('static/pip-launcher.js', encoding='utf-8') as _f:
    _js = _f.read()

# Pruefe dass alter top-level-only read-path nicht mehr alleinig vorhanden ist
_old_pattern = 'state.profileDaten.consent_text\n  ? state.profileDaten.consent_text\n  : CONSENT_DEFAULT_TEXT'
assert _old_pattern not in _js, 'Alter top-level-only consent_text read-path noch vorhanden (D-04 nicht implementiert)'

# Pruefe dass meta.consent_text im neuen read-path vorkommt
assert 'meta.consent_text' in _js or '.meta &&' in _js, 'Neuer meta.consent_text read-path fehlt'

# Pruefe Fallback-Kette: CONSENT_DEFAULT_TEXT muss noch vorhanden sein (Fallback)
assert 'CONSENT_DEFAULT_TEXT' in _js, 'CONSENT_DEFAULT_TEXT Fallback entfernt — darf nicht passieren'

print('pip-launcher.js: D-04 read-path korrekt implementiert')
print('  - meta.consent_text wird bevorzugt')
print('  - Fallback auf top-level consent_text fuer nicht-migrierte Profile')
print('  - CONSENT_DEFAULT_TEXT als letzter Fallback erhalten')
"
    </automated>
  </verify>

  <done>
    - pip-launcher.js _showConsentModal() liest consent_text aus daten.meta.consent_text (bevorzugt)
    - Fallback auf top-level daten.consent_text fuer nicht-migrierte Profile (Uebergangssicherheit)
    - Letzter Fallback: CONSENT_DEFAULT_TEXT (unveraendert, bleibt erhalten)
    - Consent-Modal zeigt nach Migration den vom User konfigurierten Text aus daten.meta.consent_text
    - Kein DSGVO-Risiko: Modal funktioniert in allen Zustaenden (migriert, nicht migriert, kein Profil)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard_create() Form-Input → daten JSON | unternehmensgroesse aus Form ist unvalidierter String — Whitelist-Pruefung per ProfileSchema-Enum fehlt noch auf Route-Ebene (wird in UI-Chip-Select erzwungen) |
| bearbeiten() POST daten_json → DB | daten_json kommt als Form-String, wird als JSON geparsed — JSON-Injection-Risiko minimal (User editiert eigenes Profil) |
| bearbeiten() POST Legacy-Daten → ProfileSchema | Finding 2: _migrate_profile_data() stripping verhindert dass Altlasten (opener/pitch) extra='forbid' Validation triggern |
| precall_service._generiere_briefing() | profile_id kommt vom Aufrufer (Caller-Kontrolle) — kein user_id-Ownership-Check fuer ProfileOpener-Query |
| pip-launcher.js consent_text → DOM | consent_text wird als textContent gesetzt (nicht innerHTML) — kein XSS-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19-08 | Tampering | wizard_create unternehmensgroesse | mitigate | Wizard-UI erzwingt Enum-Chip-Select. Schiefe Werte: ProfileSchema.model_validate() wuerde bei Write-Validation fehlschlagen wenn in 08.20 integriert — akzeptabel fuer 08.19 (UI-First) |
| T-08.19-09 | DSGVO | consent_text dual-write POST | mitigate | consent_text wird nur innerhalb desselben Profils kopiert (org_id-isoliert). Keine neue Exposition. |
| T-08.19-10 | Elevation of Privilege | precall_service ProfileOpener-Query | accept | precall_service laeuft bereits in authentifiziertem Kontext (login_required Route). profile_id aus ls.state ist org-isoliert via Live-Session-Init. IDOR-Risiko minimal Pre-Launch. |
| T-08.19-11 | DSGVO | pip-launcher consent_text read-path | mitigate | D-04 read-path bevorzugt meta.consent_text (nach Migration), Fallback auf top-level (vor Migration). Keine Version liefert leeren Consent-Text wenn User einen konfiguriert hat. |
| T-08.19-12 | Spoofing | bearbeiten() POST ValidationError-Handler | mitigate | Finding 3: ValidationError wird als 400 + Flash zurueckgegeben statt 500. db.rollback() verhindert Partial-Write. redirect() zu bearbeiten() GET nutzt korrekten Blueprint-Namen 'profiles' und Parameter 'pid'. |
</threat_model>

<verification>
```bash
cd C:/Users/andre/dev/salesnerve

# 1. Import-Tests
python -c "from routes.profiles import profiles_bp; print('profiles_bp OK')"
python -c "from services.precall_service import recherche_firma; print('precall_service OK')"

# 2. wizard_create schreibt schema_version + unternehmensgroesse, kein opener
grep -n "schema_version\|unternehmensgroesse\|zielkunde" routes/profiles.py | head -10

# 3. _migrate_profile_data in bearbeiten() GET und POST
grep -n "_migrate_profile_data" routes/profiles.py

# 4. Finding 2 + Finding 3 in bearbeiten() POST
grep -n "ProfileSchema.model_validate\|except ValidationError" routes/profiles.py

# 5. url_for Blueprint-Name Verifikation
grep -n "url_for.*bearbeiten" routes/profiles.py
# Soll 'profiles.bearbeiten' zeigen, NICHT 'profiles_bp.bearbeiten'

# 6. consent_text dual-write meta
grep -n "meta.*consent_text\|_daten_neue" routes/profiles.py | head -5

# 7. precall_service hat keine alten opener/pitch-Reads mehr
grep -n "profil_daten.get.*opener\|profil_daten.get.*pitch" services/precall_service.py
# (soll 0 Ergebnisse geben)

# 8. ProfileOpener im precall_service
grep -n "ProfileOpener" services/precall_service.py

# 9. pip-launcher.js D-04 read-path
grep -n "meta.consent_text\|_meta" static/pip-launcher.js | head -5

# 10. Code-Check-Befund: Wizard hat kein opener/pitch Formular-Feld
grep -n "opener\|pitch" templates/profile_wizard.html
# (soll 0 Treffer fuer Formular-Felder — nur Kommentare OK)

# 11. pytest Baseline
python -m pytest -x -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- `python -c "from routes.profiles import profiles_bp"` keine ImportError
- `python -c "from services.precall_service import recherche_firma"` keine ImportError
- `grep "_migrate_profile_data" routes/profiles.py` gibt 2+ Treffer (import + bearbeiten GET + bearbeiten POST)
- `grep "except ValidationError" routes/profiles.py` gibt mindestens 1 Treffer (Finding 3)
- `grep "url_for.*profiles.bearbeiten" routes/profiles.py` gibt 1+ Treffer (korrekt: 'profiles' nicht 'profiles_bp', 'pid' nicht 'id')
- `grep "profiles_bp.bearbeiten" routes/profiles.py` gibt 0 Treffer (falscher Blueprint-Name nicht vorhanden)
- `grep "schema_version" routes/profiles.py` gibt 1+ Treffer (in wizard_create daten-Dict)
- `grep "profil_daten.get.*opener" services/precall_service.py` gibt 0 Treffer (alter Read-Pfad entfernt)
- `grep "ProfileOpener" services/precall_service.py` gibt 2+ Treffer (Import + Query)
- `grep "meta.consent_text\|_meta.*consent" static/pip-launcher.js` gibt 1+ Treffer (D-04 read-path)
- pytest gibt keine neuen Failures
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-03-SUMMARY.md` erstellen.
</output>
