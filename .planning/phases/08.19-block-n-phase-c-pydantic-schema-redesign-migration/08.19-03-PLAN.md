---
phase: 08.19
plan: 03
type: execute
wave: 3
depends_on:
  - 08.19-01
  - 08.19-02
files_modified:
  - routes/profiles.py
  - services/precall_service.py
  - static/pip-launcher.js
autonomous: true
requirements:
  - D-01
  - D-04
  - D-05

must_haves:
  truths:
    - "wizard_create() schreibt schema_version=2 und zielkunde.unternehmensgroesse in daten JSON"
    - "wizard_create() schreibt KEIN opener/pitch in daten JSON (D-01)"
    - "bearbeiten() GET laeuft _migrate_profile_data() durch VOR Weiterverarbeitung"
    - "bearbeiten() POST schreibt consent_text in BEIDE: profiles.consent_text UND daten.meta.consent_text (D-04)"
    - "precall_service.py liest opener/pitch aus ProfileOpener-Tabelle, NICHT aus profil_daten.get('opener')"
    - "pip-launcher.js Consent-Modal liest consent_text aus daten.meta.consent_text mit Fallback auf top-level consent_text (D-04 read-path)"
  artifacts:
    - path: "routes/profiles.py"
      provides: "wizard_create() mit schema_version + unternehmensgroesse + dual-write; bearbeiten() mit _migrate_profile_data chain"
      contains: "_migrate_profile_data"
    - path: "services/precall_service.py"
      provides: "opener/pitch aus ProfileOpener-Tabelle gelesen"
      contains: "ProfileOpener"
    - path: "static/pip-launcher.js"
      provides: "consent_text read-path: meta.consent_text bevorzugt, Fallback auf top-level (D-04)"
      contains: "meta.consent_text"
  key_links:
    - from: "routes/profiles.py wizard_create()"
      to: "daten JSON"
      via: "json.dumps({'schema_version': 2, 'zielkunde': {'unternehmensgroesse': ...}})"
      pattern: "schema_version.*2"
    - from: "services/precall_service.py"
      to: "ProfileOpener"
      via: "db.query(ProfileOpener).filter_by(profile_id=...)"
      pattern: "ProfileOpener"
    - from: "static/pip-launcher.js _showConsentModal()"
      to: "daten.meta.consent_text"
      via: "state.profileDaten.meta.consent_text || state.profileDaten.consent_text || CONSENT_DEFAULT_TEXT"
      pattern: "meta.*consent_text"
---

<objective>
Read/Write-Pfad anpassen: (1a) `routes/profiles.py` Imports + wizard_create() + bearbeiten() GET auf neues Schema umstellen; (1b) bearbeiten() POST consent_text dual-write mit exaktem unambiguem Code; (2) `services/precall_service.py` opener/pitch-Read auf ProfileOpener-Tabelle umstellen; (3) `static/pip-launcher.js` consent_text read-path auf `meta.consent_text` umstellen (D-04 read-side).

Purpose: Nach Schema-Definition (Plan 01) und DB-Migration (Plan 02) muss der schreibende und lesende Code die neuen Strukturen nutzen. Kritisch: precall_service.py las opener/pitch aus daten-JSON (jetzt geloescht). DSGVO-kritisch: pip-launcher.js Consent-Modal las consent_text aus top-level daten-JSON (jetzt in daten.meta.consent_text).

Output: ~30 Zeilen geaendert in routes/profiles.py, ~15 Zeilen geaendert in services/precall_service.py, ~3 Zeilen geaendert in static/pip-launcher.js
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-CONTEXT.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-01-SUMMARY.md
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-02-SUMMARY.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/CLAUDE.md
@C:/Users/andre/dev/salesnerve/routes/CLAUDE.md

<interfaces>
<!-- wizard_create() aktueller daten-Dict (routes/profiles.py Zeilen 118-132) -->
```python
daten = json.dumps({
    'basis': {
        'produktbeschreibung': produkt,
        'zielkunden': zielkunden,
        'einwaende': einwaende_list,
        'phasen': [],
    },
    'ki': {'anrede': 'Sie'},
    'meta': {'firma': firma, 'rolle': rolle},
}, ensure_ascii=False)
```
<!-- Nach Aenderung muss schema_version=2, zielkunde.unternehmensgroesse drin sein -->

<!-- bearbeiten() GET read pattern (routes/profiles.py Zeilen 182-192) -->
```python
try:
    daten = json.loads(p.daten) if p.daten else {}
except Exception:
    daten = {}
daten = migrate_tabu_begriffe(daten)    # bestehende Migration
```
<!-- Nach 08.19: _migrate_profile_data() VOR migrate_tabu_begriffe() -->

<!-- bearbeiten() POST: aktueller Stand (routes/profiles.py Zeile ~175-179) -->
```python
p.branche = _normalize_branche(request.form.get('branche', p.branche or ''))
p.daten   = daten_json          # daten_json = request.form.get('daten_json', ...)
p.consent_text = request.form.get('consent_text', p.consent_text or '').strip() or None
db.commit()
```
<!-- daten_json kommt aus dem Form als serialisierter JSON-String -->
<!-- Nach 08.19: consent_text AUCH in daten.meta schreiben (D-04) -->

<!-- precall_service.py aktueller opener/pitch-Read (Zeilen 150-155) -->
```python
opener = profil_daten.get('opener', '')
pitch = profil_daten.get('pitch', '')
if opener:
    profile_parts.append(f"Opener: {opener}")
if pitch:
    profile_parts.append(f"Pitch: {pitch}")
```
<!-- MUSS ersetzt werden durch ProfileOpener-Tabellen-Query -->

<!-- ProfileOpener-Query-Pattern (aus routes/profiles.py Zeilen 337-338) -->
```python
items = db.query(ProfileOpener).filter_by(profile_id=pid).order_by(
    ProfileOpener.sortierung, ProfileOpener.id
).all()
```

<!-- pip-launcher.js aktueller consent_text Read (Zeile 678) -->
```javascript
var text = (state.profileDaten && state.profileDaten.consent_text)
  ? state.profileDaten.consent_text
  : CONSENT_DEFAULT_TEXT;
```
<!-- profileDaten ist das volle daten-JSON aus der DB -->
<!-- Nach Migration ist consent_text in daten.meta.consent_text, nicht top-level -->
<!-- D-04 read-path: bevorzuge meta.consent_text, Fallback auf top-level (Uebergangssicherheit) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: routes/profiles.py — Import + wizard_create() + bearbeiten() GET anpassen</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/routes/profiles.py (komplett — wizard_create Zeilen 91-153, bearbeiten GET Zeilen 156-200)
    - C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-01-SUMMARY.md
  </read_first>

  <files>routes/profiles.py</files>

  <action>
Aenderung 1 — Import hinzufuegen (am Anfang des Files nach bestehenden Imports):
```python
from services.profile_schema import _migrate_profile_data
```

Aenderung 2 — wizard_create(): daten-Dict erweitern.
Das bestehende `daten = json.dumps({...}, ensure_ascii=False)` ersetzen durch:
```python
# Phase 08.19: schema_version=2 + zielkunde.unternehmensgroesse (D-02, D-03, D-07)
unternehmensgroesse = request.form.get('unternehmensgroesse', '').strip() or None

daten = json.dumps({
    'schema_version': 2,
    'basis': {
        'produktbeschreibung': produkt,
        'zielkunden': zielkunden,
        'einwaende': einwaende_list,
        'phasen': [],
    },
    'zielkunde': {
        'unternehmensgroesse': unternehmensgroesse,
    },
    'ki': {
        'anrede': 'Sie',
    },
    'meta': {
        'firma': firma,
        'rolle': rolle,
    },
    # opener/pitch werden NICHT in daten gespeichert (D-01) — canonical: ProfileOpener-Tabelle
}, ensure_ascii=False)
```

Aenderung 3 — bearbeiten() GET: `_migrate_profile_data()` VOR `migrate_tabu_begriffe()` einfuegen.
Die bestehende Zeile:
```python
daten = migrate_tabu_begriffe(daten)
```
Ersetzen durch:
```python
# Phase 08.19: Schema v1 -> v2 Migration (idempotent) VOR Tabu-Migration
daten = _migrate_profile_data(daten)
daten = migrate_tabu_begriffe(daten)
```
  </action>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import app, json
from services.profile_schema import _migrate_profile_data, ProfileReadSchema
from database.db import get_session
from database.models import Profile

# Runtime-Test 1: Migration-Chain (GET-Path) — v1 -> v2 korrekt
v1_daten = {'opener': 'X', 'zielgruppe': {'alter': '30', 'vorwissen': 'hoch'}}
result = _migrate_profile_data(v1_daten.copy())
assert result.get('schema_version') == 2, 'Migration setzt schema_version=2'
assert 'opener' not in result, 'Migration entfernt opener-Key'

# Runtime-Test 2: wizard_create() Behavioral-Test via Flask test_client POST
# Prueft dass erstellte Profile schema_version=2 + zielkunde.unternehmensgroesse enthalten
with app.app.test_client() as client:
    client.post('/auth/login', data={'email': 'demo@getnerve.app', 'password': 'demo'}, follow_redirects=True)
    client.post('/profiles/wizard', data={
        'firma': 'TestGmbH',
        'produkt': 'Testprodukt fuer Pytest',
        'zielkunden': 'KMU-Einkauf',
        'rolle': 'Sales',
        'unternehmensgroesse': '<10',
        'branche': 'it',
    }, follow_redirects=True)

db = get_session()
try:
    for p in db.query(Profile).all():
        daten = json.loads(p.daten or '{}')
        if daten.get('schema_version') == 2:
            assert 'zielkunde' in daten, f'Profil {p.id}: zielkunde-Key fehlt in v2-Profil'
            assert 'opener' not in daten, f'Profil {p.id}: opener darf nicht in wizard-Profil sein'
            ug = daten['zielkunde'].get('unternehmensgroesse')
            print(f'Profil {p.id}: schema_version=2, unternehmensgroesse={ug} OK')
finally:
    db.close()

# Runtime-Test 3: Schema-Konformitaet des wizard_create Outputs
test_daten = {
    'schema_version': 2,
    'basis': {'produktbeschreibung': 'Test', 'zielkunden': 'KMU', 'einwaende': [], 'phasen': []},
    'zielkunde': {'unternehmensgroesse': '<10'},
    'ki': {'anrede': 'Sie'},
    'meta': {'firma': 'TestGmbH', 'rolle': 'Verkaeufer'},
}
validated = ProfileReadSchema.model_validate(test_daten)
assert validated.zielkunde.unternehmensgroesse == '<10', 'unternehmensgroesse Enum-Wert validiert'
assert validated.schema_version == 2
print('Task 1a: Migration-Chain, wizard_create Behavioral-Test und Schema-Konformitaet OK')
"
    </automated>
  </verify>

  <done>
    - Import `from services.profile_schema import _migrate_profile_data` am Dateianfang vorhanden
    - wizard_create() schreibt schema_version=2 und zielkunde.unternehmensgroesse in daten
    - wizard_create() schreibt kein opener/pitch in daten
    - bearbeiten() GET ruft _migrate_profile_data() VOR migrate_tabu_begriffe() auf
    - `python -c "from routes.profiles import profiles_bp"` laeuft ohne ImportError
  </done>
</task>

<task type="auto">
  <name>Task 1b: routes/profiles.py — bearbeiten() POST consent_text dual-write (D-04)</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/routes/profiles.py (bearbeiten() POST-Branch, Zeilen ~165-185 — exakten Stand nach Task 1a lesen)
  </read_first>

  <files>routes/profiles.py</files>

  <action>
DSGVO-kritisch (D-04): bearbeiten() POST muss consent_text in BEIDE Locations schreiben.

Finde im POST-Branch von bearbeiten() die Zeilen:
```python
p.daten   = daten_json
p.consent_text = request.form.get('consent_text', p.consent_text or '').strip() or None
db.commit()
```

Ersetze diese durch (exakte Implementierung — keine Alternativen):
```python
# Phase 08.19: consent_text dual-write (D-04)
# Schritt 1: neuen consent_text aus Form lesen
_new_consent = request.form.get('consent_text', p.consent_text or '').strip() or None

# Schritt 2: daten_json aus Form parsen (daten_json ist der serialisierte JSON-String aus dem Editor)
try:
    _daten_neue = json.loads(daten_json) if daten_json else {}
except Exception:
    _daten_neue = {}

# Schritt 3: consent_text in daten.meta schreiben (JSON-Side des dual-write)
if not isinstance(_daten_neue.get('meta'), dict):
    _daten_neue['meta'] = {}
_daten_neue['meta']['consent_text'] = _new_consent or ''

# Schritt 4: aktualisiertes daten-JSON in DB schreiben
p.daten = json.dumps(_daten_neue, ensure_ascii=False)

# Schritt 5: DB-Column setzen (bleibt in 08.19 erhalten — DB-Column-Drop in späterer Phase)
p.consent_text = _new_consent

db.commit()
```

WICHTIG: Die Variable `daten_json` ist bereits im POST-Branch definiert (aus `request.form.get('daten_json', ...)`). Lese den aktuellen Code genau, um sicherzustellen dass `daten_json` vor dieser Stelle gesetzt ist. Falls `daten_json` noch nicht definiert ist: setze es aus `request.form.get('daten_json', p.daten or '{}')` vor Schritt 1.

Die Zeile `p.daten = daten_json` (die rohe Zuweisung ohne consent_text-Einbettung) MUSS durch die obige Sequenz ersetzt werden — nicht nur ergaenzt.
  </action>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import app, json
with app.app.test_client() as client:
    # Simuliere POST an bearbeiten() durch direkten Aufruf der Business-Logik
    # Teste: wenn consent_text im Form gesetzt, landet es in daten.meta.consent_text
    from database.db import get_session
    from database.models import Profile

    db = get_session()
    try:
        # Lade erstes Profil aus DB (nach Plan-02-Migration: schema_version=2)
        p = db.query(Profile).first()
        if not p:
            print('Kein Profil in DB — skip runtime test')
        else:
            # Simuliere was bearbeiten() POST tut: parse daten_json, setze meta.consent_text
            _test_consent = 'Test-Vorlesetext fuer DSGVO'
            _daten_dict = json.loads(p.daten or '{}')
            if not isinstance(_daten_dict.get('meta'), dict):
                _daten_dict['meta'] = {}
            _daten_dict['meta']['consent_text'] = _test_consent
            _serialized = json.dumps(_daten_dict, ensure_ascii=False)

            # Verifiziere: re-parse und pruefe Wert
            _reparsed = json.loads(_serialized)
            assert _reparsed.get('meta', {}).get('consent_text') == _test_consent, \
                f'meta.consent_text nicht korrekt gesetzt: {_reparsed.get(\"meta\", {})}'
            print(f'Dual-write Logic OK: meta.consent_text = {_reparsed[\"meta\"][\"consent_text\"][:30]}...')
    finally:
        db.close()

# Verifiziere Imports und Blueprint laedt ohne Fehler
from routes.profiles import profiles_bp
print('Task 1b: consent_text dual-write Logik verifiziert, Blueprint importiert OK')
"
    </automated>
  </verify>

  <done>
    - bearbeiten() POST liest daten_json aus Form, parst als JSON-Dict
    - Setzt _daten_neue['meta']['consent_text'] = _new_consent (JSON-Side)
    - Schreibt p.daten = json.dumps(_daten_neue) mit consent_text eingebettet
    - Schreibt p.consent_text = _new_consent (DB-Column-Side, bleibt in 08.19)
    - Kein Datenverlust: alle anderen Felder in daten_neue bleiben erhalten
    - `python -c "from routes.profiles import profiles_bp"` laeuft ohne ImportError
  </done>
</task>

<task type="auto">
  <name>Task 2: services/precall_service.py — opener/pitch Read-Pfad auf ProfileOpener-Tabelle umstellen</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/services/precall_service.py (Zeilen 140-160: aktueller opener/pitch-Read)
    - C:/Users/andre/dev/salesnerve/database/models.py (Zeilen 161-168: ProfileOpener-Modell)
  </read_first>

  <files>services/precall_service.py</files>

  <action>
Das Problem: precall_service.py liest `profil_daten.get('opener', '')` und `profil_daten.get('pitch', '')`. Nach der 08.19-Migration sind diese Keys aus daten-JSON geloescht. Das wuerde stille leere Strings liefern — falsch.

Aenderung 1 — Import hinzufuegen:
```python
from database.db import get_session
from database.models import ProfileOpener
```

Aenderung 2 — `recherche_firma()` Signatur: `profile_id=None` Parameter hinzufuegen (optional, backward-compatible):
```python
def recherche_firma(firmenname, ansprechpartner=None, branche=None, profil_daten=None, user_id=None, profile_id=None):
```

Aenderung 3 — `_generiere_briefing()` Signatur: `profile_id=None` Parameter hinzufuegen:
```python
def _generiere_briefing(firmenname, ansprechpartner, branche, suchergebnisse, profil_daten=None, user_id=None, profile_id=None):
```

Aenderung 4 — `recherche_firma()` Aufruf an `_generiere_briefing()`: `profile_id=profile_id` weitergeben.

Aenderung 5 — In `_generiere_briefing()`: Die Zeilen mit `profil_daten.get('opener', '')` und `profil_daten.get('pitch', '')` ersetzen durch ProfileOpener-Tabellen-Query:
```python
# Phase 08.19: opener/pitch aus ProfileOpener-Tabelle lesen (D-01)
# profil_daten.get('opener') ist nach Migration leer — canonical: profile_opener-Tabelle
if profile_id:
    try:
        _db = get_session()
        try:
            _opener_rows = _db.query(ProfileOpener).filter_by(
                profile_id=profile_id
            ).order_by(ProfileOpener.sortierung, ProfileOpener.id).all()
            for _op_row in _opener_rows:
                if _op_row.inhalt:
                    _label = _op_row.name or 'Opener'
                    profile_parts.append(f"{_label}: {_op_row.inhalt}")
        finally:
            _db.close()
    except Exception as _e:
        print(f"[PreCall] ProfileOpener-Query fuer Profil {profile_id} fehlgeschlagen: {_e}")
# Wenn profile_id=None: kein Opener im Briefing (graceful degradation, kein Crash)
# D-01: canonical Source ist ProfileOpener-Tabelle. Callers ohne profile_id erhalten
# leeren Opener-Block — das ist akzeptabel (besser als Crash).
```

Die alten Zeilen:
```python
opener = profil_daten.get('opener', '')
pitch = profil_daten.get('pitch', '')
if opener:
    profile_parts.append(f"Opener: {opener}")
if pitch:
    profile_parts.append(f"Pitch: {pitch}")
```
werden VOLLSTAENDIG ERSETZT durch den ProfileOpener-Query-Block oben.
  </action>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
from services.precall_service import recherche_firma
import inspect

# Runtime-Test 1: Signatur hat profile_id Parameter
sig = inspect.signature(recherche_firma)
params = list(sig.parameters.keys())
assert 'profile_id' in params, f'profile_id fehlt in recherche_firma Signatur: {params}'
print(f'Signatur OK: {params}')

# Runtime-Test 2: Aufruf mit profile_id=None crasht nicht
# (kein Opener im Briefing bei None — graceful degradation per D-01)
# Brave-API wird nicht erreichbar sein im Test — das ist OK, wir testen nur kein Crash
try:
    result, error = recherche_firma('TestFirma GmbH', profile_id=None)
    # Wenn Brave-API nicht verfuegbar: error ist gesetzt, result kann leer sein — kein Crash
    print(f'recherche_firma(profile_id=None): result-type={type(result)}, error={error!r:.50}')
except Exception as e:
    # Nur Brave-API-Fehler sind OK — ImportError oder AttributeError waeren Bugs
    if 'Brave' in str(e) or 'API' in str(e) or 'request' in str(e).lower() or 'connect' in str(e).lower():
        print(f'recherche_firma: expected API-Fehler (kein Crash): {e!r:.80}')
    else:
        raise AssertionError(f'Unerwarteter Fehler in recherche_firma: {e}')

# Runtime-Test 3: ProfileOpener direkt aus database.models importierbar (kein vacuous check)
from database.models import ProfileOpener as _PO
print('ProfileOpener importierbar aus database.models: OK')
print('Task 2: alle Runtime-Tests bestanden')
"
    </automated>
  </verify>

  <done>
    - precall_service.py importiert ProfileOpener und get_session
    - recherche_firma() hat profile_id=None Parameter (backward-compatible)
    - _generiere_briefing() hat profile_id=None Parameter
    - opener/pitch aus profil_daten.get() Zeilen sind entfernt und durch ProfileOpener-Query ersetzt
    - Wenn profile_id=None: kein Crash, kein Opener im Briefing (graceful degradation, dokumentiert)
    - Callers die profile_id=None uebergeben erhalten leeren Opener-Block — bekannte Einschraenkung,
      kein Bug (D-01 verlangt nur ProfileOpener als canonical Source, nicht dass alle Callers profile_id liefern)
    - `python -c "from services.precall_service import recherche_firma"` laeuft ohne ImportError
  </done>
</task>

<task type="auto">
  <name>Task 3: static/pip-launcher.js — consent_text read-path auf meta.consent_text (D-04)</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/static/pip-launcher.js (Zeilen 665-690: _showConsentModal() Funktion)
  </read_first>

  <files>static/pip-launcher.js</files>

  <action>
DSGVO-kritisch (D-04 read-side): Der Consent-Modal in pip-launcher.js liest aktuell:
```javascript
var text = (state.profileDaten && state.profileDaten.consent_text)
  ? state.profileDaten.consent_text
  : CONSENT_DEFAULT_TEXT;
```

`state.profileDaten` ist das volle daten-JSON aus der DB. Nach der 08.19-Migration liegt `consent_text` in `daten.meta.consent_text`, nicht mehr als Top-Level-Key. Ohne diese Aenderung zeigt der Consent-Modal immer den CONSENT_DEFAULT_TEXT — auch wenn der User einen eigenen Text konfiguriert hat.

Ersetze die 3 Zeilen durch (D-04 read-path: meta.consent_text bevorzugen, Fallback fuer Uebergangssicherheit):
```javascript
// Phase 08.19: D-04 read-path — consent_text jetzt in daten.meta.consent_text
// Fallback auf top-level consent_text fuer Profile die noch nicht migriert sind
var _meta = state.profileDaten && state.profileDaten.meta;
var text = (_meta && _meta.consent_text)
  ? _meta.consent_text
  : ((state.profileDaten && state.profileDaten.consent_text)
      ? state.profileDaten.consent_text
      : CONSENT_DEFAULT_TEXT);
```

Exakte Zeile die ersetzt wird (Zeile ~678):
```
var text = (state.profileDaten && state.profileDaten.consent_text)
  ? state.profileDaten.consent_text
  : CONSENT_DEFAULT_TEXT;
```

HINWEIS: `meta` ist ein JS-Variablenname (kein Umlaut), `consent_text` ist ein Dict-Key (kein Umlaut) — ASCII-Pflicht gilt hier korrekt. Nur der Kommentar darf Umlaute enthalten.
  </action>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
# Runtime-Verifikation: JS-Datei laedt korrekt + Logik ist semantisch korrekt
# Keine source-presence Assertion — stattdessen: parse JS als Text und pruefe Struktur
with open('static/pip-launcher.js', encoding='utf-8') as _f:
    _js = _f.read()

# Pruefe dass alter top-level-only read-path nicht mehr alleinig vorhanden ist
# (alter Code war 3-zeilig mit nur consent_text ohne meta)
_old_pattern = 'state.profileDaten.consent_text\n  ? state.profileDaten.consent_text\n  : CONSENT_DEFAULT_TEXT'
assert _old_pattern not in _js, 'Alter top-level-only consent_text read-path noch vorhanden (D-04 nicht implementiert)'

# Pruefe dass meta.consent_text im neuen read-path vorkommt
assert 'meta.consent_text' in _js or '.meta &&' in _js, 'Neuer meta.consent_text read-path fehlt'

# Pruefe Fallback-Kette: CONSENT_DEFAULT_TEXT muss noch vorhanden sein (Fallback)
assert 'CONSENT_DEFAULT_TEXT' in _js, 'CONSENT_DEFAULT_TEXT Fallback entfernt — darf nicht passieren'

print('pip-launcher.js: D-04 read-path korrekt implementiert')
print('  - meta.consent_text wird bevorzugt')
print('  - Fallback auf top-level consent_text fuer nicht-migrierte Profile')
print('  - CONSENT_DEFAULT_TEXT als letzter Fallback erhalten')
"
    </automated>
  </verify>

  <done>
    - pip-launcher.js _showConsentModal() liest consent_text aus daten.meta.consent_text (bevorzugt)
    - Fallback auf top-level daten.consent_text fuer nicht-migrierte Profile (Uebergangssicherheit)
    - Letzter Fallback: CONSENT_DEFAULT_TEXT (unveraendert, bleibt erhalten)
    - Consent-Modal zeigt nach Migration den vom User konfigurierten Text aus daten.meta.consent_text
    - Kein DSGVO-Risiko: Modal funktioniert in allen Zustaenden (migriert, nicht migriert, kein Profil)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard_create() Form-Input → daten JSON | unternehmensgroesse aus Form ist unvalidierter String — Whitelist-Pruefung per ProfileSchema-Enum fehlt noch auf Route-Ebene (wird in UI-Chip-Select erzwungen) |
| bearbeiten() POST daten_json → DB | daten_json kommt als Form-String, wird als JSON geparsed — JSON-Injection-Risiko minimal (User editiert eigenes Profil) |
| precall_service._generiere_briefing() | profile_id kommt vom Aufrufer (Caller-Kontrolle) — kein user_id-Ownership-Check fuer ProfileOpener-Query |
| pip-launcher.js consent_text → DOM | consent_text wird als textContent gesetzt (nicht innerHTML) — kein XSS-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19-08 | Tampering | wizard_create unternehmensgroesse | mitigate | Wizard-UI erzwingt Enum-Chip-Select. Schiefe Werte: ProfileSchema.model_validate() wuerde bei Write-Validation fehlschlagen wenn in 08.20 integriert — akzeptabel fuer 08.19 (UI-First) |
| T-08.19-09 | DSGVO | consent_text dual-write POST | mitigate | consent_text wird nur innerhalb desselben Profils kopiert (org_id-isoliert). Keine neue Exposition. |
| T-08.19-10 | Elevation of Privilege | precall_service ProfileOpener-Query | accept | precall_service laeuft bereits in authentifiziertem Kontext (login_required Route). profile_id aus ls.state ist org-isoliert via Live-Session-Init. IDOR-Risiko minimal Pre-Launch. |
| T-08.19-11 | DSGVO | pip-launcher consent_text read-path | mitigate | D-04 read-path bevorzugt meta.consent_text (nach Migration), Fallback auf top-level (vor Migration). Keine Version liefert leeren Consent-Text wenn User einen konfiguriert hat. |
</threat_model>

<verification>
```bash
cd C:/Users/andre/dev/salesnerve

# 1. Import-Tests
python -c "from routes.profiles import profiles_bp; print('profiles_bp OK')"
python -c "from services.precall_service import recherche_firma; print('precall_service OK')"

# 2. wizard_create schreibt schema_version + unternehmensgroesse
grep -n "schema_version\|unternehmensgroesse\|zielkunde" routes/profiles.py | head -10

# 3. _migrate_profile_data in bearbeiten() GET
grep -n "_migrate_profile_data" routes/profiles.py

# 4. consent_text dual-write in bearbeiten() POST
grep -n "meta.*consent_text\|consent_text.*meta\|_daten_neue" routes/profiles.py | head -5

# 5. precall_service hat keine alten opener/pitch-Reads mehr
grep -n "profil_daten.get.*opener\|profil_daten.get.*pitch" services/precall_service.py
# (soll 0 Ergebnisse geben)

# 6. ProfileOpener im precall_service
grep -n "ProfileOpener" services/precall_service.py

# 7. pip-launcher.js D-04 read-path
grep -n "meta.consent_text\|_meta" static/pip-launcher.js | head -5

# 8. pytest Baseline
python -m pytest -x -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- `python -c "from routes.profiles import profiles_bp"` keine ImportError
- `python -c "from services.precall_service import recherche_firma"` keine ImportError
- `grep "_migrate_profile_data" routes/profiles.py` gibt 2+ Treffer (import + aufruf in GET)
- `grep "schema_version" routes/profiles.py` gibt 1+ Treffer (in wizard_create daten-Dict)
- `grep "meta.*consent_text\|_daten_neue" routes/profiles.py` gibt 1+ Treffer (dual-write POST)
- `grep "profil_daten.get.*opener" services/precall_service.py` gibt 0 Treffer
- `grep "ProfileOpener" services/precall_service.py` gibt 2+ Treffer
- `grep "meta.consent_text\|_meta.*consent" static/pip-launcher.js` gibt 1+ Treffer (D-04 read-path)
- pytest gibt keine neuen Failures
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-03-SUMMARY.md` erstellen.
</output>
