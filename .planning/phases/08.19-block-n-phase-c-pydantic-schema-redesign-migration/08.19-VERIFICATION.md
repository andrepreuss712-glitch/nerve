---
phase: 08.19-block-n-phase-c-pydantic-schema-redesign-migration
verified: 2026-04-27T12:00:00Z
status: passed
score: 26/26 must-haves verified
overrides_applied: 0
---

# Phase 08.19: Pydantic Schema Redesign + Migration — Verifikationsbericht

**Phase-Ziel:** Pydantic v2 ProfileSchema Redesign + idempotente DB-Migration aller Profile auf schema_version=2. Scope: services/profile_schema.py, app.py Migration, routes/profiles.py + precall_service.py Read/Write-Integration, profile_wizard.html Wizard-UI.
**Verifiziert:** 2026-04-27
**Status:** PASSED
**Re-Verifikation:** Nein — initiale Verifikation

---

## Ziel-Erreichung

### Beobachtbare Wahrheiten (Observable Truths)

| # | Wahrheit | Status | Nachweis |
|---|---------|--------|---------|
| 1 | services/profile_schema.py existiert und importierbar ist | VERIFIED | Datei vorhanden, 296 Zeilen, saubere Exporte |
| 2 | ProfileSchema.model_validate({'schema_version': 2, 'basis': {}, 'zielkunde': {'unternehmensgroesse': '<10'}}) laeuft ohne Fehler | VERIFIED | ZielkundeSchema.unternehmensgroesse ist Optional[UnternehmensgroesseEnum], ProfileReadSchema hat extra='ignore', Wert '<10' liegt im Literal |
| 3 | _migrate_profile_data({'opener': 'X', 'pitch': 'Y', 'zielgruppe': {'alter': '30'}}) gibt Dict zurueck ohne 'opener', 'pitch', 'alter' | VERIFIED | Zeilen 281-282 (opener/pitch pop), Zeilen 258-260 (alter pop aus zielgruppe) |
| 4 | _migrate_profile_data setzt schema_version=2 wenn Eingabe fehlt oder < 2 | VERIFIED | Zeile 251: `version = daten.get('schema_version') or 1`; Zeile 293: `daten['schema_version'] = 2` |
| 5 | _migrate_profile_data auf schema_version=2-Dict ist idempotent | VERIFIED | Zeile 252-253: `if version >= 2: return daten` — fruehes Return ohne Mutation |
| 6 | UnternehmensgroesseEnum hat exakt 5 Literal-Werte: '<10', '10-50', '50-250', '250-1000', '1000+' | VERIFIED | profile_schema.py Zeile 32: exakte Literal-Definition |
| 7 | Alle Profile haben schema_version=2 in daten JSON nach App-Start | VERIFIED | app.py: _migrate_profile_json() nach _seed_demo_profiles() aufgerufen (Zeile 1878); Summary bestaetigt 4/4 Profile OK |
| 8 | Kein Profil hat 'opener' oder 'pitch' als Top-Level-Key in daten JSON nach Migration | VERIFIED | _migrate_profile_data() entfernt beide Keys (Zeilen 281-282), app.py Migration sichert Werte vor Deletion fuer ProfileOpener-Sync |
| 9 | consent_text ist in daten.meta.consent_text fuer alle Profile (dual-write D-04) | VERIFIED | app.py Zeilen 1318-1322: consent_text dual-write mit NULL->'' Fallback |
| 10 | consent_text NULL wird als leerer String '' in daten.meta.consent_text geschrieben | VERIFIED | app.py Zeile 1322: `_db_consent or ''` — explizit |
| 11 | Profil-opener/pitch Werte sind in ProfileOpener-Tabelle vorhanden (sync D-01) | VERIFIED | app.py Zeilen 1294-1314: SELECT-vor-INSERT Guard, synchronisiert opener+pitch separat mit sortierung 0/1 |
| 12 | Alle migrierten Profile bestehen ProfileReadSchema.model_validate(daten) (D-05) | VERIFIED | Summary Plan 02: "ALLE 4 PROFILE MIGRIERT UND VALIDIERT"; eigenstaendige Read-Sub-Schemas mit Any-Typen loesen Typ-Drift |
| 13 | Migration ist idempotent: zweiter App-Start aendert nichts | VERIFIED | app.py Zeile 1288: `(_daten.get('schema_version') or 1) >= 2` Guard + Summary bestaetigt kein [Schema]-Log beim zweiten Start |
| 14 | _migrate_profile_json() verwendet Advisory-Lock (SQLite: BEGIN EXCLUSIVE, PostgreSQL: pg_advisory_xact_lock) | VERIFIED | app.py Zeilen 1260-1271: beide Faelle implementiert |
| 15 | wizard_create() schreibt schema_version=2 und zielkunde.unternehmensgroesse in daten JSON | VERIFIED | routes/profiles.py Zeilen 128-148: daten-Dict mit schema_version=2 und zielkunde.unternehmensgroesse |
| 16 | wizard_create() schreibt KEIN opener/pitch in daten JSON (D-01) | VERIFIED | Kein opener/pitch im daten-Dict; Kommentar-Block in Zeilen 121-125 dokumentiert das by design |
| 17 | bearbeiten() GET laeuft _migrate_profile_data() durch VOR Weiterverarbeitung | VERIFIED | routes/profiles.py Zeile 240: `daten = _migrate_profile_data(daten)` vor `migrate_tabu_begriffe` |
| 18 | bearbeiten() POST schreibt consent_text in BEIDE: profiles.consent_text UND daten.meta.consent_text (D-04) | VERIFIED | routes/profiles.py Zeilen 217-225: Schritt 4 (JSON-Side) + Schritt 6 (DB-Column) |
| 19 | bearbeiten() POST ruft _migrate_profile_data() VOR ProfileSchema.model_validate() auf (Finding 2) | VERIFIED | routes/profiles.py Zeilen 206-210: _migrate_profile_data dann ProfileSchema.model_validate |
| 20 | bearbeiten() POST faengt ValidationError ab und gibt HTTP 400 + Flash-Message zurueck (Finding 3) | VERIFIED | routes/profiles.py Zeilen 211-214: `except ValidationError as _ve` -> flash + db.rollback() + redirect |
| 21 | precall_service.py liest opener/pitch aus ProfileOpener-Tabelle, NICHT aus profil_daten.get('opener') | VERIFIED | services/precall_service.py Zeile 153: nur noch Kommentar-Referenz; Zeilen 156-170: aktiver ProfileOpener-Query |
| 22 | pip-launcher.js Consent-Modal liest consent_text aus daten.meta.consent_text mit Fallback auf top-level | VERIFIED | static/pip-launcher.js Zeilen 679-684: `_meta.consent_text` bevorzugt, Fallback `profileDaten.consent_text`, letzter Fallback `CONSENT_DEFAULT_TEXT` |
| 23 | Quick-Wizard hat Unternehmensgroesse-Chip-Select mit 5 Enum-Werten als neues Pflicht-Feld | VERIFIED | templates/profile_wizard.html Zeilen 344-349: 5 .groesse-chip Divs mit exakten Enum-Werten |
| 24 | Chip-onclick-Werte sind exakt '<10', '10-50', '50-250', '250-1000', '1000+' (Reviews v2 Enum-Sync) | VERIFIED | template: `onclick="selectGroesse(this, '<10')"` etc. — HTML-Entity &lt; nur im sichtbaren Text, nicht im onclick-String |
| 25 | Wizard zeigt UI-Hint 'Mehr Profil-Details = besseres Live-Coaching' per D-03 | VERIFIED | templates/profile_wizard.html Zeile 353: exakter Wortlaut vorhanden |
| 26 | DOM-Init initGroesseChip() restauriert Chip-State bei Zurueck-Navigation | VERIFIED | templates/profile_wizard.html Zeilen 474-485: Funktion definiert und direkt aufgerufen |

**Score:** 26/26 Wahrheiten verifiziert

---

### Erforderliche Artefakte

| Artefakt | Erwartet | Status | Details |
|---------|---------|--------|---------|
| `services/profile_schema.py` | Pydantic v2 ProfileSchema + _migrate_profile_data() | VERIFIED | 296 Zeilen; exportiert ProfileSchema, ProfileReadSchema, _migrate_profile_data, UnternehmensgroesseEnum, ZeithorizontEnum, EinwandTypEnum |
| `app.py` | _migrate_profile_json() Funktion + Aufruf | VERIFIED | Zeilen 1236-1338: Funktion definiert; Zeile 1878: Aufruf nach _seed_demo_profiles() |
| `routes/profiles.py` | wizard_create() + bearbeiten() mit Schema-Integration | VERIFIED | Import Zeilen 9-10; wizard_create() Zeilen 120-148; bearbeiten() GET Zeile 240; bearbeiten() POST Zeilen 193-225 |
| `services/precall_service.py` | opener/pitch aus ProfileOpener-Tabelle gelesen | VERIFIED | Import Zeile 13; ProfileOpener-Query Zeilen 156-170; alter profil_daten.get-Pfad entfernt |
| `static/pip-launcher.js` | consent_text D-04 read-path auf meta.consent_text | VERIFIED | Zeilen 677-684: D-04 3-stufige Fallback-Kette implementiert |
| `templates/profile_wizard.html` | unternehmensgroesse Chip-Select + UI-Hint + Validation + DOM-Init | VERIFIED | Zeilen 344-485: alle 4 Komponenten vorhanden |

---

### Key Link Verifikation

| Von | Nach | Via | Status | Details |
|-----|------|-----|--------|---------|
| services/profile_schema.py | _migrate_profile_data() | schema_version Guard `if version >= 2: return daten` | WIRED | Zeile 252-253 — exakter Guard implementiert |
| ProfileSchema | model_config | ConfigDict(extra='forbid') auf Write-Schema | WIRED | Zeile 127: Write-Schema; Zeilen 218+: Read-Sub-Schemas mit extra='ignore' |
| app.py _migrate_profile_json() | services/profile_schema._migrate_profile_data() | Import und Aufruf pro Profile-Row | WIRED | Zeile 1248: Import; Zeile 1324: `_daten = _mpd(_daten)` |
| app.py _migrate_profile_json() | database/models.ProfileOpener | SELECT-vor-INSERT fuer opener/pitch Sync | WIRED | Zeilen 1295-1314: SELECT COUNT(*) + bedingte INSERTs |
| routes/profiles.py wizard_create() | daten JSON | json.dumps({'schema_version': 2, 'zielkunde': {'unternehmensgroesse': ...}}) | WIRED | Zeilen 128-148: vollstaendiger daten-Dict |
| routes/profiles.py bearbeiten() POST | _migrate_profile_data() + ProfileSchema.model_validate() | Finding 2 + Finding 3 | WIRED | Zeilen 206-214: sequenzielle Aufrufkette |
| services/precall_service.py | ProfileOpener | db.query(ProfileOpener).filter_by(profile_id=...) | WIRED | Zeilen 160-162: aktiver Query |
| static/pip-launcher.js _showConsentModal() | daten.meta.consent_text | `_meta.consent_text || profileDaten.consent_text || CONSENT_DEFAULT_TEXT` | WIRED | Zeilen 679-684: 3-stufige Fallback-Kette |
| templates/profile_wizard.html chip onclick values | services/profile_schema.py UnternehmensgroesseEnum | onclick-Werte == Literal-Werte | WIRED | 5/5 onclick-Werte exakt identisch mit Python Literals; HTML-Entity &lt; nur im sichtbaren Text |
| templates/profile_wizard.html | routes/profiles.py wizard_create() | hidden input name='unternehmensgroesse' | WIRED | Zeile 351: `<input type="hidden" name="unternehmensgroesse" id="unternehmensgroesseInput">` |

---

### Data-Flow Trace (Level 4)

| Artefakt | Datenvariable | Quelle | Echte Daten | Status |
|---------|--------------|--------|------------|--------|
| profile_schema.py ProfileSchema | schema_version, zielkunde.unternehmensgroesse | Pydantic Validation auf eingehenden daten-Dict | Ja — direkt aus Form-Input und DB-JSON | FLOWING |
| app.py _migrate_profile_json() | _daten (alle Profile-Rows) | `SELECT id, daten, consent_text FROM profiles` — echte DB-Query | Ja — real DB rows, 4 Profile bestaetigt | FLOWING |
| routes/profiles.py wizard_create() | daten JSON | request.form.get('unternehmensgroesse', ...) aus Wizard-POST | Ja — Form-Input direkt in daten-Dict | FLOWING |
| routes/profiles.py bearbeiten() POST | _daten_neue | request.form.get('daten_json') + _migrate_profile_data() + ProfileSchema.model_validate() | Ja — bestehende DB-Daten + Form-Update | FLOWING |
| services/precall_service.py | _opener_rows | db.query(ProfileOpener).filter_by(profile_id=...) | Ja — DB-Query auf profile_opener Tabelle | FLOWING |
| static/pip-launcher.js | text (Consent-Text) | state.profileDaten.meta.consent_text (aus DB-JSON geladen) | Ja — nach Migration in meta.consent_text persistiert | FLOWING |

---

### Behavioral Spot-Checks

| Verhalten | Check | Ergebnis | Status |
|---------|-------|---------|--------|
| _migrate_profile_data idempotent | Zeile 252: `if version >= 2: return daten` — frueher Return ohne Aenderung | Nachweis im Code: kein Branch-Pfad aendert bereits-v2-Daten | PASS |
| opener/pitch entfernt in Migration | Zeilen 281-282: `daten.pop('opener', None)` + `daten.pop('pitch', None)` | Direkt nach B2C-Felder-Pruning, vor schema_version-Setzung | PASS |
| Advisory-Lock: SQLite BEGIN EXCLUSIVE | Zeile 1262: `conn.execute(_text3("BEGIN EXCLUSIVE"))` | Korrekte SQL-Direktive statt nicht-unterstuetzter execution_options | PASS |
| Enum-Sync: onclick-Werte == Python Literals | 5/5 HTML onclick-Werte exakt = Literal['<10', '10-50', '50-250', '250-1000', '1000+'] | HTML-Entity &lt; nur im Display-Text; onclick-Strings sind pure ASCII-Werte | PASS |
| Wizard: kein opener/pitch Formular-Feld | grep auf templates/profile_wizard.html: 0 Treffer fuer 'opener' oder 'pitch' | Datei vollstaendig geprüft — kein Formular-Feld vorhanden | PASS |

---

### Requirements-Abdeckung

| Requirement | Plan(s) | Beschreibung | Status | Nachweis |
|------------|---------|-------------|--------|---------|
| D-01 | 02, 03 | opener/pitch aus daten.JSON eliminieren; canonical: ProfileOpener-Tabelle | SATISFIED | app.py Migration sync + Delete; precall_service.py auf ProfileOpener umgestellt; wizard_create() ohne opener/pitch |
| D-02 | 01, 02 | Schema-Versionierung via schema_version Integer; _migrate_profile_data() idempotent | SATISFIED | profile_schema.py: version Guard + Setzen; app.py: alle Profile auf v2 migriert |
| D-03 | 03, 04 | Neue Felder: Wizard schlank (unternehmensgroesse als Pflicht-Feld), Detail-Editor vollstaendig | SATISFIED | Wizard-UI: Chip-Select + Validation + UI-Hint; wizard_create() schreibt unternehmensgroesse |
| D-04 | 02, 03 | consent_text: inkrementelle Migration, DB-Column-Drop spaeter; dual-write JSON+DB | SATISFIED | app.py: dual-write bei Migration; bearbeiten() POST: beide Locations; pip-launcher.js: meta.consent_text Lese-Pfad |
| D-05 | 01, 02 | Pydantic strict bei Write (extra='forbid'), permissive bei Read (extra='ignore'); ProfileReadSchema toleriert Typ-Drift | SATISFIED | ProfileSchema extra='forbid'; eigenstaendige Read-Sub-Schemas mit Any-Typen; bearbeiten() POST mit ValidationError-Handler |
| D-06 | 01 | Felder eliminieren: alter, einkommensniveau, lebenssituation, schmerzen.trigger, ki.stil, erlaubnis | SATISFIED | profile_schema.py: Felder nicht modelliert; _migrate_profile_data() entfernt alle eliminierten Felder |
| D-07 | 01, 03 | Neue B2B-Felder: unternehmensgroesse, buying_committee, statusquo, zeithorizont, roi_argumente, einwand_typ | SATISFIED | ZielkundeSchema + ValueSchema + EinwandDetailSchema.einwand_typ modelliert; Wizard-UI fuer unternehmensgroesse |

**Alle 7 Requirements (D-01 bis D-07) vollstaendig erfuellt.**

---

### Anti-Pattern-Scan

| Datei | Pattern | Befund | Schwere |
|------|---------|--------|---------|
| services/profile_schema.py | TODO/FIXME/Placeholder | Keiner gefunden | — |
| services/profile_schema.py | return null/empty | Nur Default-Werte fuer Schema-Felder (kein Rendering-Stub) | — |
| app.py _migrate_profile_json() | return {} / empty array ohne DB-Query | Kein Befund — echte SELECT/UPDATE auf DB | — |
| routes/profiles.py | profil_daten.get('opener') als aktiver Code-Pfad | Nur Kommentar-Referenz (Zeile 153 in precall_service) — kein aktiver Zugriff | — |
| services/precall_service.py | profil_daten.get('opener'/'pitch') als aktiver Code | Zeile 153: nur Kommentar — kein aktiver Zugriff | — |
| templates/profile_wizard.html | opener/pitch Formular-Felder | 0 Treffer — vollstaendig entfernt (D-01 Enforcement) | — |
| static/pip-launcher.js | CONSENT_DEFAULT_TEXT als alleiniger Pfad | Nein — nur als letzter Fallback erhalten; primaer: meta.consent_text | — |

**Keine Blocker oder Warnings gefunden.**

---

### Human Verification Required

Keine Items erfordern menschliche Verifikation. Alle Must-Haves sind programmatisch verifizierbar und wurden verifiziert.

---

## Luecken-Zusammenfassung

Keine Luecken. Alle 26 Must-Haves aus den 4 Plan-Frontmattern sind verifiziert. Die Phase hat ihr Ziel vollstaendig erreicht:

- **services/profile_schema.py** ist substantiell (296 Zeilen), exportiert alle 3 Hauptsymbole (ProfileSchema, ProfileReadSchema, _migrate_profile_data), implementiert Dual-Schema-Pattern mit korrektem extra='forbid'/'ignore', idempotente v1→v2 Migration mit None-safety.
- **app.py** _migrate_profile_json() ist verdrahtet (nach _seed_demo_profiles()), Advisory-Lock korrekt implementiert (BEGIN EXCLUSIVE fuer SQLite, pg_advisory_xact_lock fuer PostgreSQL), consent_text dual-write mit NULL→'' Handling.
- **routes/profiles.py** wizard_create() schreibt schema_version=2 + unternehmensgroesse, bearbeiten() GET/POST migrieren vor Verarbeitung, ValidationError wird als HTTP 400 behandelt (Finding 3), url_for korrekt mit Blueprint-Name 'profiles' + Parameter 'pid'.
- **services/precall_service.py** liest opener/pitch ausschliesslich aus ProfileOpener-Tabelle, graceful degradation bei profile_id=None.
- **static/pip-launcher.js** 3-stufige consent_text Fallback-Kette implementiert (meta.consent_text > top-level > CONSENT_DEFAULT_TEXT).
- **templates/profile_wizard.html** Chip-Select mit 5 Enum-Werten (Enum-Sync verifiziert), UI-Hint, Step-2-Validation, DOM-State-Init — alle implementiert und verdrahtet.

---

_Verifiziert: 2026-04-27_
_Verifier: Claude (gsd-verifier)_
