---
phase: 08.19-block-n-phase-c-pydantic-schema-redesign-migration
fixed_at: 2026-04-27T14:46:20Z
review_path: .planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-REVIEW.md
iteration: 1
fix_scope: critical_warning
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 08.19: Code Review Fix Report

**Fixed at:** 2026-04-27T14:46:20Z
**Source review:** `.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 5
- Fixed: 5
- Skipped: 0

## Fixed Issues

### CR-01: Missing CSRF token on save-to-profile PUT request

**Files modified:** `static/pip-launcher.js`
**Commit:** a12ff2a
**Applied fix:** Added `'X-CSRFToken': getCsrfToken()` to the headers object in the `lnr-save-yes` onclick fetch call inside `_showSaveToProfileDialog`. The header is now consistent with all other mutating fetches in the file.

### WR-01: `wizard_create()` writes unternehmensgroesse to DB without Pydantic validation

**Files modified:** `routes/profiles.py`
**Commit:** 20930d4
**Applied fix:** Inserted a `ProfileSchema.model_validate(json.loads(daten))` call with a `ValidationError` catch immediately after the `json.dumps(...)` block that builds the wizard daten dict, before the DB session is opened. On validation failure, flashes a user-facing error and redirects back to `profiles.wizard_page`.

### WR-02: `runPrecall()` client-side min-length mismatch with backend

**Files modified:** `static/pip-launcher.js`
**Commit:** 78697f2
**Applied fix:** Changed `firma.trim().length < 2` to `firma.trim().length < 3` and updated the error message text from "mind. 2 Zeichen" to "mind. 3 Zeichen" to match the backend constraint in `precall_service.py`.

### WR-03: Silent data destruction in `bearbeiten()` when `json.loads` fails

**Files modified:** `routes/profiles.py`
**Commit:** 8871261
**Applied fix:** Replaced the silent `except Exception: _daten_neue = {}` fallback with a proper abort: flash error message, `db.rollback()`, and redirect to `profiles.bearbeiten` for the same profile. Prevents empty dict from silently overwriting existing profile data.

### WR-04: `_showConsentModal` silently no-ops when DOM elements are missing

**Files modified:** `static/pip-launcher.js`
**Commit:** 1c8b85e
**Applied fix:** Replaced the bare `return` on the missing-elements guard with a `console.error` log plus `callback('cancelled')` before returning. This ensures the caller (`startCall()`) always receives a callback response and the UI does not appear frozen.

---

_Fixed: 2026-04-27T14:46:20Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
