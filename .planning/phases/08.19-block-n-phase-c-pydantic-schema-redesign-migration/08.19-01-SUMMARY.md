---
phase: "08.19"
plan: "01"
subsystem: "services"
tags: ["pydantic", "schema", "migration", "profile", "tdd"]
dependency_graph:
  requires: []
  provides:
    - "services/profile_schema.py: ProfileSchema, ProfileReadSchema, _migrate_profile_data"
  affects:
    - "routes/profiles.py (naechste Plans: Write-Pfad)"
    - "services/claude_service.py (naechste Plans: Read-Pfad)"
    - "app.py _migrate() (naechste Plans: DB-Migration)"
tech_stack:
  added:
    - "pydantic v2 ConfigDict(extra='forbid'/'ignore')"
    - "Literal types fuer UnternehmensgroesseEnum, ZeithorizontEnum, EinwandTypEnum"
  patterns:
    - "Write-Schema (extra='forbid') + Read-Schema (extra='ignore') — Dual-Schema-Pattern"
    - "Idempotenter schema_version Guard: if version >= 2: return daten"
key_files:
  created:
    - "services/profile_schema.py (207 Zeilen)"
    - "tests/test_08_19_01_profile_schema.py (21 TDD Tests)"
  modified: []
decisions:
  - "zielgruppe.vorwissen + zielgruppe.entscheidungsverhalten bleiben in zielgruppe.* (minimaler Migration-Impact — claude_service._build_coaching_prompt liest explizit pdata.get('zielgruppe', {}))"
  - "UnternehmensgroesseEnum Literal-Werte kanonisch: '<10', '10-50', '50-250', '250-1000', '1000+' (Plan 04 HTML-Chips muessen exakt matchen)"
  - "branche bleibt als String in basis.branche (DB-Column-Konsolidierung spaeter in 08.20)"
metrics:
  duration: "3 Minuten"
  completed: "2026-04-27"
  tasks: 1
  files_created: 2
  files_modified: 0
---

# Phase 08.19 Plan 01: ProfileSchema + Migration Summary

**One-liner:** Pydantic v2 ProfileSchema mit Dual-Schema-Pattern (extra='forbid'/'ignore'), 6 neuen B2B-Feldern, 6 eliminierten B2C-Feldern und idempotenter schema_version-2-Migration.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| RED  | TDD: Failing Tests ProfileSchema | 08c2b51 | tests/test_08_19_01_profile_schema.py |
| GREEN | feat: ProfileSchema + _migrate_profile_data | 317c0a2 | services/profile_schema.py |

## Implemented

`services/profile_schema.py` (207 Zeilen) mit:

**Schemas:**
- `ProfileSchema` — Write-Schema (extra='forbid'), wirft ValidationError bei unbekannten Feldern
- `ProfileReadSchema` — Read-Schema (extra='ignore'), ignoriert Drift-Felder aus alten Profilen
- Sub-Schemas: `BasisSchema`, `ZielgruppeSchema`, `ZielkundeSchema`, `ValueSchema`, `KiSchema`, `SchmerzenSchema`, `MetaSchema`, `EinwandDetailSchema`

**Enums:**
- `UnternehmensgroesseEnum = Literal['<10', '10-50', '50-250', '250-1000', '1000+']` (Reviews v2 kanonisch)
- `ZeithorizontEnum = Literal['sofort', '3_monate', '6_monate', 'kein_druck']`
- `EinwandTypEnum = Literal['echt', 'vorschiebe', 'unbekannt']`

**Neue B2B-Felder (D-07):** unternehmensgroesse, buying_committee, statusquo, zeithorizont, roi_argumente, einwand_typ

**Eliminierte Felder (D-06):** alter, einkommensniveau, lebenssituation, schmerzen.trigger, ki.stil, erlaubnis

**Migration:** `_migrate_profile_data()` — idempotent, v1→v2, entfernt eliminierte Felder, mergt ki.stil→ki.ton, setzt schema_version=2

## TDD Gate Compliance

- **RED:** test(08.19-01) commit 08c2b51 — 21 failing tests (ImportError, services/profile_schema.py noch nicht vorhanden)
- **GREEN:** feat(08.19-01) commit 317c0a2 — alle 21 Tests gruen nach Implementierung
- **REFACTOR:** Kein Refactor-Bedarf (Code war direkt sauber aus Plan-Spec)

## Deviations from Plan

Keine — Plan exakt ausgefuehrt.

## Threat Model Coverage

| Threat | Status |
|--------|--------|
| T-08.19-01: Idempotenz-Guard `if version >= 2: return daten` | Implementiert — verhindert Doppel-Migration |
| T-08.19-13: Enum-Sync UI-Backend | Implementiert — UnternehmensgroesseEnum ist kanonische Quelle, in Acceptance Criteria verifiziert |
| T-08.19-02: extra='forbid' ValidationError als HTTP-400 | Schema bereit — HTTP-400-Surfacing folgt in Plan 03 |

## Known Stubs

Keine — Plan 01 ist reine Schema-Definition, keine UI-Anbindung in diesem Plan.

## Self-Check: PASSED
