---
phase: 08.19
plan: 04
type: execute
wave: 3
depends_on:
  - 08.19-01
  - 08.19-02
files_modified:
  - templates/profile_wizard.html
autonomous: true
requirements:
  - D-03
  - D-07

must_haves:
  truths:
    - "Quick-Wizard hat Unternehmensgroesse-Chip-Select mit 5 Enum-Werten als neues Pflicht-Feld"
    - "Unternehmensgroesse-Wert wird als Hidden-Input 'unternehmensgroesse' an wizard_create() POST gesendet"
    - "Wizard zeigt UI-Hint 'Mehr Profil-Details = besseres Live-Coaching' per D-03-Spec"
    - "Wizard-Validation verhindert Submit ohne ausgewaehlte Unternehmensgroesse"
  artifacts:
    - path: "templates/profile_wizard.html"
      provides: "unternehmensgroesse Chip-Select + UI-Hint + Validation"
      contains: "unternehmensgroesse"
      min_lines: 5
  key_links:
    - from: "templates/profile_wizard.html"
      to: "routes/profiles.py wizard_create()"
      via: "hidden input name='unternehmensgroesse' im wizard-form POST"
      pattern: "name=\"unternehmensgroesse\""
---

<objective>
Wizard-UI anpassen: `templates/profile_wizard.html` bekommt ein neues Pflicht-Feld `unternehmensgroesse` als Chip-Select (5 Enum-Werte), einen UI-Hint und Validation.

Purpose: Neues Pflicht-Feld aus D-03/D-07 muss im Quick-Wizard sichtbar sein damit neue Profile direkt schema_version=2-konform erstellt werden. Chip-Select erzwingt Enum-Whitelist (keine Freieingabe).

Output: ~30-50 neue Zeilen in profile_wizard.html (neues Formularfeld + JS-Handler + Validation + UI-Hint)
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-CONTEXT.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-03-SUMMARY.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/CLAUDE.md

<interfaces>
<!-- PATTERNS.md chip-select Pattern aus BRANCHE-Selector (profile_wizard.html Zeilen 267-319) -->
<!-- Das exakt gleiche .einwand-chip Pattern fuer single-select verwenden -->

<!-- Bestehende Wizard-Steps in profile_wizard.html (aus PATTERNS.md): -->
<!-- Step 1: Einwaende-Auswahl  -->
<!-- Step 2: Produkt & Firma (firma, produkt, branche, zielkunden) -->
<!-- Step 3: Rolle & submit -->

<!-- Einzufuegendes neues Feld in Step 2, nach 'Zielkunden': -->
```html
<label class="form-label">Typische Unternehmensgröße deiner Zielkunden</label>
<div class="einwand-grid" id="groesseGrid">
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '<10')">&lt;10 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '10-50')">10–50 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '50-250')">50–250 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '250-1000')">250–1000 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '1000+')">1000+ MA</div>
</div>
<input type="hidden" name="unternehmensgroesse" id="unternehmensgroesseInput">
```

<!-- UI-Hint per D-03 (exakter Wortlaut, Umlaute erlaubt in user-facing text): -->
```html
<p style="font-size:0.8rem;color:var(--page-text-secondary);margin-top:16px;">
  Mehr Profil-Details = besseres Live-Coaching. Du kannst auch später ergänzen unter Profil → Tiefe.
</p>
```

<!-- JS Handler (analog zur bestehenden selectBranche() oder selectEinwand()-Logik): -->
```javascript
function selectGroesse(el, value) {
  document.querySelectorAll('.groesse-chip').forEach(function(c) {
    c.classList.remove('selected');
  });
  el.classList.add('selected');
  document.getElementById('unternehmensgroesseInput').value = value;
}
```

<!-- Validation: validateSubmit() oder nextStep()-Funktion muss unternehmensgroesse pruefen -->
<!-- Pattern: analog bestehender Branche-Validation wenn vorhanden, sonst: -->
```javascript
// In der Funktion die Step 2 -> Step 3 Transition steuert (nextStep oder validateStep2):
var groesse = document.getElementById('unternehmensgroesseInput').value;
if (!groesse) {
  // Flash-artige Markierung des groesseGrid
  document.getElementById('groesseGrid').style.outline = '2px solid var(--n-accent)';
  return false;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: profile_wizard.html — unternehmensgroesse Chip-Select + UI-Hint + Validation</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/templates/profile_wizard.html (komplett — Step-Struktur, chip-Pattern, JS-Handlers, Validation)
  </read_first>

  <files>templates/profile_wizard.html</files>

  <action>
Lese profile_wizard.html komplett. Verstehe:
1. Welcher Step ist "Produkt & Firma" (hat firma/produkt/branche/zielkunden Felder)?
2. Wo ist das bestehende `.einwand-chip` oder Chip-Select-Pattern implementiert?
3. Wo ist die Validation / nextStep-Funktion?

Dann fuege ein:

A) Im Produkt-&-Firma-Step, NACH dem Zielkunden-Eingabefeld: das unternehmensgroesse Chip-Select-Block mit 5 Chips + hidden input:
```html
<!-- Phase 08.19: Unternehmensgroesse Pflicht-Feld (D-03, D-07) -->
<label class="form-label">Typische Unternehmensgröße deiner Zielkunden <span style="color:var(--n-accent)">*</span></label>
<div class="einwand-grid" id="groesseGrid">
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '<10')">&lt;10 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '10-50')">10–50 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '50-250')">50–250 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '250-1000')">250–1000 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '1000+')">1000+ MA</div>
</div>
<input type="hidden" name="unternehmensgroesse" id="unternehmensgroesseInput">
```

B) Direkt danach den UI-Hint (exakter Wortlaut per D-03, Umlaute sind user-facing text — erlaubt):
```html
<p style="font-size:0.8rem;color:var(--page-text-secondary);margin-top:16px;">
  Mehr Profil-Details = besseres Live-Coaching. Du kannst auch später ergänzen unter Profil → Tiefe.
</p>
```

C) Im `<script>`-Block: `selectGroesse()` JS-Funktion hinzufuegen:
```javascript
function selectGroesse(el, value) {
  document.querySelectorAll('.groesse-chip').forEach(function(c) {
    c.classList.remove('selected');
  });
  el.classList.add('selected');
  document.getElementById('unternehmensgroesseInput').value = value;
}
```

D) Validation im Step 2 → Step 3 Uebergang (in der nextStep- oder validateStep-Funktion fuer Step 2):
Finde die Funktion die den Uebergang von Step 2 nach Step 3 kontrolliert (nextStep(), showStep() o.ae.).
Fuege VOR dem Uebergang ein:
```javascript
// Phase 08.19: unternehmensgroesse ist Pflicht-Feld (D-03)
var _groesse = document.getElementById('unternehmensgroesseInput').value;
if (!_groesse) {
  var _grid = document.getElementById('groesseGrid');
  if (_grid) _grid.style.boxShadow = '0 0 0 2px var(--n-accent)';
  return;
}
```

WICHTIG: Umlaute in HTML-Content (label-Text, Hint-Text) sind erlaubt (user-facing text). 
ASCII-Pflicht gilt nur fuer id="...", name="...", class="...", JS-Variablen-Namen.
Korrekt: `<label class="form-label">Unternehmensgröße</label>` — Umlaut im Text OK
Falsch waere: `id="unternehmensgroesse"` mit ae statt oe — aber id ist ASCII hier (kein Umlaut)

CSS fuer `.einwand-chip` und `.selected`-State sind vermutlich bereits im Template oder nerve.css vorhanden (vom bestehenden Einwand-Chip-Pattern). Keine neuen CSS-Klassen noetig wenn `.einwand-chip` und `.einwand-grid` bereits existieren.
  </action>

  <acceptance_criteria>
    - `grep -n "unternehmensgroesse" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt mindestens 3 Treffer (hidden input name, id, JS-Funktion)
    - `grep -n "groesse-chip" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 5 Treffer (5 Chip-Divs)
    - `grep -n "selectGroesse" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt mindestens 2 Treffer (onclick-Attribut und Funktionsdefinition)
    - `grep -n "Mehr Profil-Details" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 1 Treffer (UI-Hint per D-03)
    - `grep -n "&lt;10\|10-50\|50-250\|250-1000\|1000+" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 5 Treffer (5 Enum-Werte)
    - HTML-Datei ist valides HTML (keine unclosed tags) — `python -c "from html.parser import HTMLParser; p=HTMLParser(); p.feed(open('templates/profile_wizard.html').read())"` ohne Exception
  </acceptance_criteria>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
src = open('templates/profile_wizard.html', encoding='utf-8').read()
assert 'unternehmensgroesse' in src, 'unternehmensgroesse-Input fehlt'
assert 'groesse-chip' in src, 'groesse-chip CSS-Klasse fehlt'
assert 'selectGroesse' in src, 'selectGroesse JS-Funktion fehlt'
assert 'Mehr Profil-Details' in src, 'UI-Hint fehlt (D-03)'
assert 'groesseGrid' in src, 'groesseGrid ID fehlt'
# Alle 5 Enum-Werte vorhanden
assert '10-50' in src, '10-50 Chip fehlt'
assert '50-250' in src, '50-250 Chip fehlt'
assert '250-1000' in src, '250-1000 Chip fehlt'
assert '1000+' in src, '1000+ Chip fehlt'
print('profile_wizard.html: alle Checks OK')
"
    </automated>
  </verify>

  <done>
    - unternehmensgroesse Chip-Select mit 5 Enum-Werten im Step 2 vorhanden
    - Hidden input name="unternehmensgroesse" id="unternehmensgroesseInput" vorhanden
    - selectGroesse() JS-Funktion vorhanden
    - UI-Hint "Mehr Profil-Details = besseres Live-Coaching..." vorhanden (D-03 exakter Wortlaut)
    - Validation im Step 2 → Step 3 Uebergang: kein Submit ohne Auswahl
    - HTML parsierbar ohne Exception
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wizard HTML Form → wizard_create() POST | unternehmensgroesse-Wert kommt als Form-String — Enum-Validation erfolgt im Backend durch wizard_create() (request.form.get) und optionale ProfileSchema-Validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19-11 | Tampering | Chip-Select hidden input | mitigate | Client-side JS erzwingt Enum-Werte via onclick-Handler. Backend-Validation: wizard_create() liest den Wert, ProfileSchema-Validation in 08.20 wuerde ungueltige Werte ablehnen. Pre-Launch akzeptable TOFU. |
| T-08.19-12 | Information Disclosure | UI-Hint Text | accept | Hint-Text ist oeffentlich sichtbar — enthaelt keine sensitiven Informationen, nur Produktinformation. |
</threat_model>

<verification>
```bash
cd C:/Users/andre/dev/salesnerve

# 1. Alle Pflicht-Elemente vorhanden
grep -c "groesse-chip" templates/profile_wizard.html
# (soll 5 sein — 5 Chips)

grep -n "selectGroesse\|unternehmensgroesse\|Mehr Profil-Details" templates/profile_wizard.html

# 2. HTML valide
python -c "
from html.parser import HTMLParser
p = HTMLParser()
p.feed(open('templates/profile_wizard.html', encoding='utf-8').read())
print('HTML parsiert OK')
"

# 3. App laeuft (Flask kann Template rendern)
python -c "
import app
with app.app.test_client() as c:
    c.post('/auth/login', data={'email': 'test@test.com', 'password': 'test'}, follow_redirects=True)
print('App-Start OK')
"
```
</verification>

<success_criteria>
- `grep "groesse-chip" templates/profile_wizard.html | wc -l` gibt 5 (5 Chip-Divs)
- `grep "Mehr Profil-Details" templates/profile_wizard.html` gibt 1 Treffer
- `grep "selectGroesse" templates/profile_wizard.html` gibt 2+ Treffer
- HTML-Parser laeuft ohne Exception
- Wizard-Page rendert ohne Jinja2-Fehler (python -c import app test_client)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-04-SUMMARY.md` erstellen.
</output>
