---
phase: 08.19
plan: 04
type: execute
wave: 3
depends_on:
  - 08.19-01
  - 08.19-02
files_modified:
  - templates/profile_wizard.html
autonomous: true
requirements:
  - D-03
  - D-07

must_haves:
  truths:
    - "Quick-Wizard hat Unternehmensgroesse-Chip-Select mit 5 Enum-Werten als neues Pflicht-Feld"
    - "Unternehmensgroesse-Wert wird als Hidden-Input 'unternehmensgroesse' an wizard_create() POST gesendet"
    - "Chip-onclick-Werte sind exakt: '<10', '10-50', '50-250', '250-1000', '1000+' — matchen UnternehmensgroesseEnum Literal-Werte aus Plan 01 (Reviews v2 Enum-Sync)"
    - "Wizard zeigt UI-Hint 'Mehr Profil-Details = besseres Live-Coaching' per D-03-Spec"
    - "Wizard-Validation verhindert Submit ohne ausgewaehlte Unternehmensgroesse (verifiziert via Flask POST ohne unternehmensgroesse — kein 500-Fehler, kein neues Profil)"
    - "DOM-Init: Wenn hidden-Input beim Laden einen Wert hat (z.B. bei Zurueck-Navigation), wird der entsprechende Chip initial als active markiert (Reviews v2 DOM-State-Init)"
  artifacts:
    - path: "templates/profile_wizard.html"
      provides: "unternehmensgroesse Chip-Select + UI-Hint + Validation + DOM-Init"
      contains: "unternehmensgroesse"
      min_lines: 5
  key_links:
    - from: "templates/profile_wizard.html"
      to: "routes/profiles.py wizard_create()"
      via: "hidden input name='unternehmensgroesse' im wizard-form POST"
      pattern: "name=\"unternehmensgroesse\""
    - from: "templates/profile_wizard.html chip onclick values"
      to: "services/profile_schema.py UnternehmensgroesseEnum"
      via: "onclick-Werte muessen exakt mit Literal-Werten uebereinstimmen — kein silent 400"
      pattern: "selectGroesse.*<10|10-50|50-250|250-1000|1000\\+"
---

<objective>
Wizard-UI anpassen: `templates/profile_wizard.html` bekommt ein neues Pflicht-Feld `unternehmensgroesse` als Chip-Select (5 Enum-Werte), einen UI-Hint, Validation und DOM-State-Initialisierung.

Purpose: Neues Pflicht-Feld aus D-03/D-07 muss im Quick-Wizard sichtbar sein damit neue Profile direkt schema_version=2-konform erstellt werden. Chip-Select erzwingt Enum-Whitelist (keine Freieingabe). Reviews v2: DOM-Init-Check stellt sicher dass bei Zurueck-Navigation oder Page-Reload der korrekte Chip als active dargestellt wird.

Reviews v2 Enum-Sync-Pflicht: Die Chip-onclick-Werte MUESSEN exakt den UnternehmensgroesseEnum Literal-Werten aus `services/profile_schema.py` entsprechen:
  '<10', '10-50', '50-250', '250-1000', '1000+'
Abweichung (z.B. 'unter_10', 'lt10', '< 10') erzeugt silent HTTP-400 beim wizard_create() POST da ProfileSchema.model_validate() den Wert ablehnt.

Output: ~30-50 neue Zeilen in profile_wizard.html (neues Formularfeld + JS-Handler + DOM-Init + Validation + UI-Hint)
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-CONTEXT.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-03-SUMMARY.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/CLAUDE.md

<interfaces>
<!-- PATTERNS.md chip-select Pattern aus BRANCHE-Selector (profile_wizard.html Zeilen 267-319) -->
<!-- Das exakt gleiche .einwand-chip Pattern fuer single-select verwenden -->

<!-- Bestehende Wizard-Steps in profile_wizard.html (aus PATTERNS.md): -->
<!-- Step 1: Einwaende-Auswahl  -->
<!-- Step 2: Produkt & Firma (firma, produkt, branche, zielkunden) -->
<!-- Step 3: Rolle & submit -->

<!-- Einzufuegendes neues Feld in Step 2, nach 'Zielkunden': -->
```html
<label class="form-label">Typische Unternehmensgröße deiner Zielkunden <span style="color:var(--n-accent)">*</span></label>
<div class="einwand-grid" id="groesseGrid">
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '<10')">&lt;10 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '10-50')">10–50 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '50-250')">50–250 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '250-1000')">250–1000 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '1000+')">1000+ MA</div>
</div>
<input type="hidden" name="unternehmensgroesse" id="unternehmensgroesseInput">
```

<!-- Reviews v2 Enum-Sync-Verifikation: onclick-Werte exakt wie UnternehmensgroesseEnum Literals -->
<!--
  Python Literal       | HTML onclick-Wert  | Angezeigter Text
  '<10'                | '<10'              | '&lt;10 MA'
  '10-50'              | '10-50'            | '10–50 MA'
  '50-250'             | '50-250'           | '50–250 MA'
  '250-1000'           | '250-1000'         | '250–1000 MA'
  '1000+'              | '1000+'            | '1000+ MA'
  ACHTUNG: '<10' muss exakt so in onclick stehen. HTML-Entity &lt; nur im sichtbaren Text.
-->

<!-- UI-Hint per D-03 (exakter Wortlaut, Umlaute erlaubt in user-facing text): -->
```html
<p style="font-size:0.8rem;color:var(--page-text-secondary);margin-top:16px;">
  Mehr Profil-Details = besseres Live-Coaching. Du kannst auch später ergänzen unter Profil → Tiefe.
</p>
```

<!-- JS Handler (analog zur bestehenden selectBranche() oder selectEinwand()-Logik): -->
```javascript
function selectGroesse(el, value) {
  document.querySelectorAll('.groesse-chip').forEach(function(c) {
    c.classList.remove('selected');
  });
  el.classList.add('selected');
  document.getElementById('unternehmensgroesseInput').value = value;
}
```

<!-- Reviews v2: DOM-Init fuer Chip-State-Restaurierung bei Zurueck-Navigation / Page-Reload -->
<!-- Beim DOMContentLoaded oder Step-2-Show: pruefen ob hidden-Input bereits Wert hat -->
<!-- Falls ja: entsprechenden Chip als 'selected' markieren -->
```javascript
// Reviews v2: DOM-State-Init — Chip-State restaurieren bei Page-Reload oder Zurueck-Navigation
function initGroesseChip() {
  var existing = document.getElementById('unternehmensgroesseInput');
  if (!existing || !existing.value) return;
  var val = existing.value;
  document.querySelectorAll('.groesse-chip').forEach(function(c) {
    if (c.getAttribute('onclick') && c.getAttribute('onclick').indexOf("'" + val + "'") !== -1) {
      c.classList.add('selected');
    }
  });
}
// Aufruf: entweder beim DOMContentLoaded oder wenn Step 2 angezeigt wird
```

<!-- Validation: validateSubmit() oder nextStep()-Funktion muss unternehmensgroesse pruefen -->
<!-- Pattern: analog bestehender Branche-Validation wenn vorhanden, sonst: -->
```javascript
// In der Funktion die Step 2 -> Step 3 Transition steuert (nextStep oder validateStep2):
var _groesse = document.getElementById('unternehmensgroesseInput').value;
if (!_groesse) {
  var _grid = document.getElementById('groesseGrid');
  if (_grid) _grid.style.boxShadow = '0 0 0 2px var(--n-accent)';
  return;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: profile_wizard.html — unternehmensgroesse Chip-Select + UI-Hint + Validation + DOM-Init (Reviews v2)</name>
  <read_first>
    - C:/Users/andre/dev/salesnerve/templates/profile_wizard.html (komplett — Step-Struktur, chip-Pattern, JS-Handlers, Validation, DOMContentLoaded)
  </read_first>

  <files>templates/profile_wizard.html</files>

  <action>
Lese profile_wizard.html komplett. Verstehe:
1. Welcher Step ist "Produkt & Firma" (hat firma/produkt/branche/zielkunden Felder)?
2. Wo ist das bestehende `.einwand-chip` oder Chip-Select-Pattern implementiert?
3. Wo ist die Validation / nextStep-Funktion?
4. Gibt es einen DOMContentLoaded-Handler oder Step-Show-Callback wo DOM-Init-Code hingehoert?

Dann fuege ein:

A) Im Produkt-&-Firma-Step, NACH dem Zielkunden-Eingabefeld: das unternehmensgroesse Chip-Select-Block mit 5 Chips + hidden input.

Reviews v2 Enum-Sync-Pflicht: onclick-Werte MUESSEN exakt den Python Literal-Werten entsprechen.
HTML-Entity-Regel: '&lt;' nur im sichtbaren Text-Content, NICHT im onclick-String.
```html
<!-- Phase 08.19: Unternehmensgroesse Pflicht-Feld (D-03, D-07) -->
<!-- Reviews v2 Enum-Sync: onclick-Werte exakt = UnternehmensgroesseEnum Literals aus profile_schema.py -->
<label class="form-label">Typische Unternehmensgröße deiner Zielkunden <span style="color:var(--n-accent)">*</span></label>
<div class="einwand-grid" id="groesseGrid">
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '<10')">&lt;10 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '10-50')">10–50 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '50-250')">50–250 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '250-1000')">250–1000 MA</div>
  <div class="einwand-chip groesse-chip" onclick="selectGroesse(this, '1000+')">1000+ MA</div>
</div>
<input type="hidden" name="unternehmensgroesse" id="unternehmensgroesseInput">
```

B) Direkt danach den UI-Hint (exakter Wortlaut per D-03, Umlaute sind user-facing text — erlaubt):
```html
<p style="font-size:0.8rem;color:var(--page-text-secondary);margin-top:16px;">
  Mehr Profil-Details = besseres Live-Coaching. Du kannst auch später ergänzen unter Profil → Tiefe.
</p>
```

C) Im `<script>`-Block: `selectGroesse()` JS-Funktion hinzufuegen:
```javascript
function selectGroesse(el, value) {
  document.querySelectorAll('.groesse-chip').forEach(function(c) {
    c.classList.remove('selected');
  });
  el.classList.add('selected');
  document.getElementById('unternehmensgroesseInput').value = value;
}
```

D) Reviews v2 DOM-Init: `initGroesseChip()` Funktion hinzufuegen und beim geeigneten Zeitpunkt aufrufen.
Geeigneter Aufruf-Zeitpunkt (waehle das passende aus dem bestehenden Template):
- Option 1: Im `DOMContentLoaded`-Handler wenn vorhanden — `initGroesseChip()` appenden
- Option 2: In der `showStep(n)`- oder `nextStep()`-Funktion wenn Step 2 angezeigt wird — Aufruf wenn n==2
- Option 3: Als letztes Statement im `<script>`-Block nach Funktionsdefinitionen

```javascript
// Reviews v2: DOM-State-Init — Chip-State restaurieren bei Page-Reload oder Zurueck-Navigation
function initGroesseChip() {
  var existing = document.getElementById('unternehmensgroesseInput');
  if (!existing || !existing.value) return;
  var val = existing.value;
  document.querySelectorAll('.groesse-chip').forEach(function(c) {
    if (c.getAttribute('onclick') && c.getAttribute('onclick').indexOf("'" + val + "'") !== -1) {
      c.classList.add('selected');
    }
  });
}
```

Aufrufposition: NACH der Funktionsdefinition, entweder im DOMContentLoaded-Block oder als direkter Aufruf `initGroesseChip();` am Ende des Script-Blocks.

E) Validation im Step 2 → Step 3 Uebergang (in der nextStep- oder validateStep-Funktion fuer Step 2):
Finde die Funktion die den Uebergang von Step 2 nach Step 3 kontrolliert (nextStep(), showStep() o.ae.).
Fuege VOR dem Uebergang ein:
```javascript
// Phase 08.19: unternehmensgroesse ist Pflicht-Feld (D-03)
var _groesse = document.getElementById('unternehmensgroesseInput').value;
if (!_groesse) {
  var _grid = document.getElementById('groesseGrid');
  if (_grid) _grid.style.boxShadow = '0 0 0 2px var(--n-accent)';
  return;
}
```

WICHTIG: Umlaute in HTML-Content (label-Text, Hint-Text) sind erlaubt (user-facing text).
ASCII-Pflicht gilt nur fuer id="...", name="...", class="...", JS-Variablen-Namen.
Korrekt: `<label class="form-label">Unternehmensgröße</label>` — Umlaut im Text OK
Korrekt: `id="groesseGrid"` — 'ss' statt 'ß' im Identifier, kein Umlaut

CSS fuer `.einwand-chip` und `.selected`-State sind vermutlich bereits im Template oder nerve.css vorhanden (vom bestehenden Einwand-Chip-Pattern). Keine neuen CSS-Klassen noetig wenn `.einwand-chip` und `.einwand-grid` bereits existieren.
  </action>

  <acceptance_criteria>
    - `grep -n "unternehmensgroesse" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt mindestens 3 Treffer (hidden input name, id, JS-Funktion)
    - `grep -n "groesse-chip" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 5 Treffer (5 Chip-Divs)
    - `grep -n "selectGroesse" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt mindestens 6 Treffer (5 onclick-Attribute + 1 Funktionsdefinition)
    - `grep -n "Mehr Profil-Details" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 1 Treffer (UI-Hint per D-03)
    - `grep -n "initGroesseChip" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt mindestens 2 Treffer (Funktionsdefinition + Aufruf — Reviews v2 DOM-Init)
    - Reviews v2 Enum-Sync: `grep -n "selectGroesse.*<10\|selectGroesse.*10-50\|selectGroesse.*50-250\|selectGroesse.*250-1000\|selectGroesse.*1000+" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 5 Treffer (exakte Enum-Werte als onclick-Argumente)
    - HTML-Datei ist parsierbar: `python -c "from html.parser import HTMLParser; p=HTMLParser(); p.feed(open('templates/profile_wizard.html', encoding='utf-8').read())"` ohne Exception
    - Code-Check-Befund-Verifikation: `grep -n "opener\|pitch" C:/Users/andre/dev/salesnerve/templates/profile_wizard.html` gibt 0 Treffer fuer Formular-Felder — Wizard hat weiterhin KEIN opener/pitch Eingabefeld (D-01 Enforcement)
  </acceptance_criteria>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import app

# Check 1: HTML-Parser (keine broken tags — strukturelle Integritaet)
from html.parser import HTMLParser
with open('templates/profile_wizard.html', encoding='utf-8') as _f:
    _html = _f.read()
_parser = HTMLParser()
_parser.feed(_html)
print('HTML-Parser: keine Exception, Template strukturell valide')

# Check 2: Reviews v2 Enum-Sync-Verifikation
# Chip-onclick-Werte muessen exakt den Python UnternehmensgroesseEnum Literals entsprechen
_required_enum_values = ['<10', '10-50', '50-250', '250-1000', '1000+']
for _enum_val in _required_enum_values:
    _onclick_pattern = f\"selectGroesse(this, '{_enum_val}')\"
    assert _onclick_pattern in _html, f'Enum-Sync FEHLER: onclick-Wert fuer {_enum_val!r} nicht gefunden (Reviews v2)'
print(f'Enum-Sync OK: alle 5 onclick-Werte exakt = UnternehmensgroesseEnum Literals: {_required_enum_values}')

# Check 3: DOM-Init-Funktion vorhanden (Reviews v2)
assert 'initGroesseChip' in _html, 'initGroesseChip() Funktion fehlt (Reviews v2 DOM-State-Init)'
print('DOM-Init OK: initGroesseChip() vorhanden')

# Check 4: Code-Check-Befund — kein opener/pitch Formular-Feld
import re
_form_opener = re.search(r'<input[^>]*name=[\"\\']opener[\"\\'|][^>]*>', _html)
_form_pitch  = re.search(r'<input[^>]*name=[\"\\']pitch[\"\\'|][^>]*>', _html)
assert _form_opener is None, 'Wizard hat opener-Formular-Feld — D-01 Verletzung!'
assert _form_pitch is None,  'Wizard hat pitch-Formular-Feld — D-01 Verletzung!'
print('Code-Check-Befund OK: Wizard hat kein opener/pitch Formular-Feld (D-01 Enforcement)')

# Check 5: Flask test_client GET — Jinja2 rendert das Template ohne Fehler
with app.app.test_client() as client:
    client.post('/auth/login', data={'email': 'demo@getnerve.app', 'password': 'demo'}, follow_redirects=True)
    resp = client.get('/profiles/wizard')
    assert resp.status_code in (200, 302), f'Wizard-GET: unerwarteter Status {resp.status_code}'
    if resp.status_code == 200:
        assert b'groesse-chip' in resp.data, 'groesse-chip fehlt im gerenderten Wizard (Jinja2-Rendering-Test)'
        assert b'unternehmensgroesse' in resp.data, 'unternehmensgroesse-Input fehlt im Rendering'
        assert b'Mehr Profil-Details' in resp.data, 'UI-Hint fehlt im gerenderten Wizard (D-03)'
        assert b'initGroesseChip' in resp.data, 'initGroesseChip fehlt im gerenderten Wizard (Reviews v2 DOM-Init)'
        print('Flask GET /profiles/wizard: alle Pflicht-Elemente im Rendering vorhanden')

# Check 6: Flask test_client POST ohne unternehmensgroesse — kein 500-Crash
with app.app.test_client() as client2:
    client2.post('/auth/login', data={'email': 'demo@getnerve.app', 'password': 'demo'}, follow_redirects=True)
    post_resp = client2.post('/profiles/wizard', data={
        'firma': 'ValidierungTest GmbH',
        'produkt': 'Testprodukt',
        'zielkunden': 'KMU',
        'rolle': 'Sales',
        # unternehmensgroesse absichtlich NICHT gesendet
        'branche': 'it',
    }, follow_redirects=False)
    assert post_resp.status_code != 500, f'POST ohne unternehmensgroesse: 500 Server Error (Crash!)'
    print(f'Flask POST ohne unternehmensgroesse: Status {post_resp.status_code} (kein 500-Crash) OK')

print('Task 1: alle Checks bestanden (HTML-Parser, Enum-Sync, DOM-Init, Code-Check-Befund, Flask-Rendering, Validation)')
"
    </automated>
  </verify>

  <done>
    - unternehmensgroesse Chip-Select mit 5 Enum-Werten im Step 2 vorhanden
    - Hidden input name="unternehmensgroesse" id="unternehmensgroesseInput" vorhanden
    - selectGroesse() JS-Funktion vorhanden
    - UI-Hint "Mehr Profil-Details = besseres Live-Coaching..." vorhanden (D-03 exakter Wortlaut)
    - Validation im Step 2 → Step 3 Uebergang: kein Submit ohne Auswahl (boxShadow-Flash)
    - Reviews v2 DOM-Init: initGroesseChip() definiert und aufgerufen — restauriert Chip-State bei Reload/Zurueck-Navigation
    - Reviews v2 Enum-Sync: onclick-Werte '<10', '10-50', '50-250', '250-1000', '1000+' exakt gleich wie UnternehmensgroesseEnum Literals in profile_schema.py
    - HTML parsierbar ohne Exception
    - Kein opener/pitch Formular-Feld im Wizard (D-01 Enforcement, Code-Check-Befund bestaetigt)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wizard HTML Form → wizard_create() POST | unternehmensgroesse-Wert kommt als Form-String — Enum-Validation erfolgt im Backend durch wizard_create() (request.form.get) und optionale ProfileSchema-Validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19-14 | Tampering | Chip-Select hidden input + Enum-Sync | mitigate | Client-side JS erzwingt Enum-Werte via onclick-Handler. Reviews v2: onclick-Werte sind exakt die Python Literal-Werte — kein Silent-400 durch Enum-Mismatch. Backend ProfileSchema.model_validate() lehnt ungueltige Werte ab. |
| T-08.19-15 | Information Disclosure | UI-Hint Text | accept | Hint-Text ist oeffentlich sichtbar — enthaelt keine sensitiven Informationen, nur Produktinformation. |
| T-08.19-16 | Tampering | DOM-State-Init bei Page-Reload | mitigate | initGroesseChip() liest Chip-Wert aus hidden-Input und restauriert visual state — kein Sicherheitsrisiko (nur UI-State). Wert kommt aus eigenem DOM, nicht aus URL-Parametern. |
</threat_model>

<verification>
```bash
cd C:/Users/andre/dev/salesnerve

# 1. Alle Pflicht-Elemente vorhanden
grep -c "groesse-chip" templates/profile_wizard.html
# (soll 5 sein — 5 Chips)

grep -n "selectGroesse\|unternehmensgroesse\|Mehr Profil-Details\|initGroesseChip" templates/profile_wizard.html

# 2. Reviews v2 Enum-Sync-Verifikation
grep -n "selectGroesse.*<10\|selectGroesse.*10-50\|selectGroesse.*50-250\|selectGroesse.*250-1000\|selectGroesse.*1000+" templates/profile_wizard.html
# (soll 5 Zeilen zeigen — alle 5 Enum-Werte als onclick-Argumente)

# 3. Code-Check-Befund: kein opener/pitch Formular-Feld
grep -n "opener\|pitch" templates/profile_wizard.html
# (soll 0 Treffer fuer Formular-Felder geben — D-01 Enforcement)

# 4. HTML valide
python -c "
from html.parser import HTMLParser
p = HTMLParser()
p.feed(open('templates/profile_wizard.html', encoding='utf-8').read())
print('HTML parsiert OK')
"

# 5. App laeuft (Flask kann Template rendern)
python -c "
import app
with app.app.test_client() as c:
    c.post('/auth/login', data={'email': 'demo@getnerve.app', 'password': 'demo'}, follow_redirects=True)
    r = c.get('/profiles/wizard')
    print(f'Wizard GET: {r.status_code}')
"
```
</verification>

<success_criteria>
- `grep "groesse-chip" templates/profile_wizard.html | wc -l` gibt 5 (5 Chip-Divs)
- `grep "Mehr Profil-Details" templates/profile_wizard.html` gibt 1 Treffer
- `grep "selectGroesse" templates/profile_wizard.html` gibt 6+ Treffer (5 onclick + 1 Funktionsdefinition)
- `grep "initGroesseChip" templates/profile_wizard.html` gibt 2+ Treffer (Funktionsdefinition + Aufruf — Reviews v2 DOM-Init)
- Reviews v2 Enum-Sync: `grep "selectGroesse.*<10\|10-50\|50-250\|250-1000\|1000+" templates/profile_wizard.html` gibt 5 Zeilen (alle Enum-Werte exakt)
- HTML-Parser laeuft ohne Exception
- Wizard-Page rendert ohne Jinja2-Fehler
- `grep "opener\|pitch" templates/profile_wizard.html` gibt 0 Treffer fuer Formular-Felder (D-01)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19-block-n-phase-c-pydantic-schema-redesign-migration/08.19-04-SUMMARY.md` erstellen.
</output>
