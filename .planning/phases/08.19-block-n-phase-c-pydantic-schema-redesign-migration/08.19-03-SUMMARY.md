---
phase: "08.19"
plan: "03"
subsystem: "routes/profiles.py + services/precall_service.py + static/pip-launcher.js"
tags: ["pydantic", "schema", "migration", "read-write-path", "dsgvo", "consent", "opener"]
dependency_graph:
  requires:
    - "08.19-01: services/profile_schema.py (ProfileSchema, ProfileReadSchema, _migrate_profile_data)"
    - "08.19-02: app.py _migrate_profile_json() (alle Profile auf schema_version=2)"
  provides:
    - "routes/profiles.py: wizard_create() mit schema_version=2 + zielkunde.unternehmensgroesse"
    - "routes/profiles.py: bearbeiten() GET mit _migrate_profile_data() Chain"
    - "routes/profiles.py: bearbeiten() POST mit migrate-before-validate + ValidationError 400 + consent_text dual-write"
    - "services/precall_service.py: opener/pitch aus ProfileOpener-Tabelle (nicht aus daten-JSON)"
    - "static/pip-launcher.js: consent_text D-04 read-path (meta.consent_text bevorzugt)"
  affects:
    - "routes/profiles.py (Plan 04: Wizard-UI unternehmensgroesse HTML-Chip-Select)"
    - "services/claude_service.py (Plan 08.20: Pipeline-Re-Wire mit neuen Schema-Feldern)"
tech_stack:
  added:
    - "pydantic ValidationError catch in bearbeiten() POST (Finding 3)"
    - "ProfileOpener DB-Query in precall_service._generiere_briefing()"
  patterns:
    - "_migrate_profile_data() VOR Validation (Finding 2) — verhindert false-positive 400 durch Altlasten"
    - "consent_text dual-write: profiles.consent_text (DB-Column) + daten.meta.consent_text (JSON)"
    - "D-04 read-path: meta.consent_text bevorzugt, Fallback top-level, letzter Fallback CONSENT_DEFAULT_TEXT"
    - "Graceful degradation: profile_id=None in precall_service -> kein Opener-Block, kein Crash"
key_files:
  created: []
  modified:
    - "routes/profiles.py (+52 Zeilen: imports + wizard_create + bearbeiten GET + bearbeiten POST)"
    - "services/precall_service.py (+25 Zeilen, -9 Zeilen: imports + Signaturen + ProfileOpener-Query)"
    - "static/pip-launcher.js (+8 Zeilen, -4 Zeilen: _showConsentModal D-04 read-path)"
decisions:
  - "wizard_create() macht KEINEN ProfileOpener-INSERT — Wizard hat kein opener/pitch Eingabefeld (Code-Check-Befund, by design)"
  - "bearbeiten() POST: _migrate_profile_data() VOR ProfileSchema.model_validate() (Finding 2) — Altlasten-Stripping verhindert false-positive 400"
  - "ValidationError -> flash + db.rollback() + redirect HTTP 400 statt 500 (Finding 3)"
  - "consent_text dual-write: NULL -> '' in daten.meta (Finding 4), DB-Column-Drop in spaeterer Phase"
  - "precall_service profile_id=None -> leerer Opener-Block (graceful degradation, kein Crash)"
metrics:
  duration: "3 Minuten"
  completed: "2026-04-27"
  tasks: 4
  files_created: 0
  files_modified: 3
---

# Phase 08.19 Plan 03: Read/Write-Pfad Anpassung Summary

**One-liner:** Read/Write-Pfad auf schema_version=2 umgestellt — wizard_create() schreibt zielkunde.unternehmensgroesse, bearbeiten() POST validiert via Pydantic mit migrate-before-validate, precall_service liest opener aus ProfileOpener-Tabelle, pip-launcher nutzt meta.consent_text (D-04 DSGVO-kritisch).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1a | profiles.py imports + wizard_create() + bearbeiten() GET | 0bed0a2 | routes/profiles.py |
| 1b | bearbeiten() POST: migrate-before-validate + ValidationError 400 + consent_text dual-write | 4eb2ed5 | routes/profiles.py |
| 2 | precall_service opener/pitch Read-Pfad auf ProfileOpener-Tabelle | 4675300 | services/precall_service.py |
| 3 | pip-launcher.js consent_text D-04 read-path auf meta.consent_text | ee9fa3c | static/pip-launcher.js |

## Implemented

### routes/profiles.py

**Task 1a — Imports + wizard_create() + bearbeiten() GET:**
- `from services.profile_schema import _migrate_profile_data, ProfileSchema`
- `from pydantic import ValidationError`
- `wizard_create()`: `schema_version=2`, `zielkunde.unternehmensgroesse` aus Form, kein opener/pitch (D-01)
- Code-Check-Befund als 6-Zeilen-Kommentar dokumentiert: Wizard hat kein opener/pitch Eingabefeld, kein ProfileOpener-INSERT by design
- `bearbeiten() GET`: `_migrate_profile_data(daten)` VOR `migrate_tabu_begriffe(daten)`

**Task 1b — bearbeiten() POST (Reviews v2):**
- Finding 2: `_migrate_profile_data(_daten_neue)` VOR `ProfileSchema.model_validate()` — Altlasten-Stripping verhindert false-positive 400
- Finding 3: `except ValidationError` -> `flash(..., 'error')` + `db.rollback()` + `redirect(url_for('profiles.bearbeiten', pid=p.id))` — kein 500
- D-04: `_daten_neue['meta']['consent_text'] = _new_consent or ''` (JSON-Side, NULL -> '')
- `p.consent_text = _new_consent` (DB-Column-Side, bleibt in 08.19)
- url_for korrekt: `'profiles.bearbeiten'` (nicht 'profiles_bp'), Parameter `pid` (nicht 'id')

### services/precall_service.py

- Import: `from database.db import get_session`, `from database.models import ProfileOpener`
- `recherche_firma()` + `_generiere_briefing()`: `profile_id=None` Parameter (backward-compatible)
- Alter `profil_daten.get('opener', '')` / `profil_daten.get('pitch', '')` Block vollstaendig entfernt
- Ersetzt durch ProfileOpener-Query: `db.query(ProfileOpener).filter_by(profile_id=...).order_by(sortierung, id).all()`
- Graceful degradation: `profile_id=None` -> kein Opener-Block, kein Crash

### static/pip-launcher.js

- `_showConsentModal()`: D-04 read-path mit 3-stufiger Fallback-Kette:
  1. `_meta.consent_text` (bevorzugt — nach Migration)
  2. `state.profileDaten.consent_text` (Fallback — vor Migration, Uebergangssicherheit)
  3. `CONSENT_DEFAULT_TEXT` (letzter Fallback — unveraendert)

## Deviations from Plan

Keine — Plan exakt ausgefuehrt.

## Verification Results

Alle Acceptance Criteria bestanden:

```
profiles_bp OK
precall_service OK
_migrate_profile_data: 2 Treffer in routes/profiles.py (GET + POST)
ProfileSchema.model_validate: 1 Treffer (POST-Branch)
except ValidationError: 1 Treffer (Finding 3)
url_for('profiles.bearbeiten', pid=...): korrekt, kein profiles_bp.bearbeiten
meta.consent_text: vorhanden in routes/profiles.py + pip-launcher.js
profil_daten.get('opener'): nur in Kommentar, kein aktiver Code
ProfileOpener: Import + Query in precall_service.py
CONSENT_DEFAULT_TEXT: erhalten als letzter Fallback
D-04 semantisch verifiziert: neuer Pfad bevorzugt, alter Pfad als Fallback, kein Datenverlust
```

**pytest:** 313 passing — identisch mit Baseline (20 pre-existierende Failures in test_08_13_01_config_constants, test_ab_stats, test_exchange_rates, test_qa_pipeline — unveraendert seit 08.19-02).

## Threat Model Coverage

| Threat | Status |
|--------|--------|
| T-08.19-08: wizard_create unternehmensgroesse Tampering | Akzeptiert — UI-Chip-Select erzwingt Enum, ProfileSchema validiert bei Write |
| T-08.19-09: DSGVO consent_text dual-write | Implementiert — consent_text nur innerhalb desselben Profils kopiert |
| T-08.19-10: precall_service ProfileOpener-Query | Akzeptiert — laeuft in authentifiziertem Kontext, org-isoliert via Live-Session |
| T-08.19-11: DSGVO pip-launcher consent_text read-path | Implementiert — D-04 3-stufige Fallback-Kette, kein leerer Consent-Text moeglich |
| T-08.19-12: bearbeiten() POST ValidationError-Handler | Implementiert — 400 + Flash + db.rollback() + redirect, kein 500 |

## Known Stubs

Keine — alle Read/Write-Pfade sind vollstaendig verdrahtet.

## Threat Flags

Keine neuen Netzwerk-Endpoints oder Auth-Pfade durch diesen Plan.

## Self-Check: PASSED
