# Phase 08.20.3: Briefing-Lebenszyklus + KI-Skript-Personalisierung — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-30
**Phase:** 08.20.3-briefing-lebenszyklus-ki-skript-verschmelzung
**Areas discussed:** Modus C Dropdown, Vorher/Nachher-Modal, PiP-Briefing-Tab, Plan-Splitting

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Modus C Dropdown | SPEC sagt 'Modal oder Inline-Dropdown' — noch nicht entschieden. | ✓ |
| Vorher/Nachher-Modal | Pseudo-Step vs. DOM-Overlay | ✓ |
| PiP-Briefing-Tab | DOM-Anbindung in app.js | ✓ |
| Plan-Splitting | Wave-Struktur + Test-Strategie | ✓ |

User wählte alle 4 Bereiche + lieferte vorab ein detailliertes Vault-Briefing mit eigenen Positionen zu allen Bereichen.

---

## Modus C Dropdown

| Option | Description | Selected |
|--------|-------------|----------|
| KEIN eigenes Dropdown in Step 4 | C-Button → setzt briefingModus='C' → Step 5. Step 5 bekommt zweiten Action-Button "✨ Personalisieren + Call ▶" | ✓ |
| Auswahl-Modal über Step-4-Footer | SPEC Req 5 Variante A — Modal öffnet sich über dem Footer mit Dropdown | |
| Inline-Dropdown im Step-4-Footer | SPEC Req 5 Variante B — Dropdown klappt unter den 3 Buttons auf | |

**User's choice:** Kein eigenes Dropdown in Step 4 — Step-5-Opener-Auswahl wiederverwenden.
**Notes:** Begründung: Keine Duplikation, kein State-Sync-Drift zwischen zwei Selects. SPEC-"oder"-Formulierung lässt Spielraum. Disabled-State für C-Button in Step 4 bleibt (laut SPEC Req 2). Plan-Phase klärt Skript vs. Opener Priorität.

---

## Lade-State während Sonnet-Call

| Option | Description | Selected |
|--------|-------------|----------|
| renderStep4b() — eigener Pseudo-Step | Konsistent mit Step-3-Pattern (Briefing-Recherche). Back-Button abbricht Call. | ✓ |
| Inline-Spinner im Personalisieren-Button | Kein Step-Wechsel, kein Cancel-Pfad | |
| Overlay auf Step-5-Content | DOM-Komplexer | |

**User's choice:** renderStep4b() — eigener Pseudo-Step.
**Notes:** AbortController für Cancel-Pfad. State-Flow: Step 5 → Step 4b → Step 4c.

---

## Vorher/Nachher-Modal

| Option | Description | Selected |
|--------|-------------|----------|
| renderStep4c() Pseudo-Step | Konsistent mit Step-Architektur, "Zurück"-Logik stabil | ✓ |
| DOM-Overlay | Eigene DOM-Mgmt-Logik, Step-Stack-Pattern gebrochen | |

**User's choice:** renderStep4c() Pseudo-Step.
**Notes:** R7-Lesson: Step-State-Mgmt verhindert Zurück-Workflow-Bugs. Side-by-Side vs. gestackt via CSS-@media (Claude-Discretion). "Original nutzen" → briefingModus='A' + step=5.

---

## mdToHtml() Cross-File-Sharing

| Option | Description | Selected |
|--------|-------------|----------|
| window.mdToHtml in pip-launcher.js exposen | Einzeiler, konsistent mit Commit f1916e2 | ✓ |
| Duplizieren in app.js | Drift-Risk | |
| Externe helpers.js | Overhead, Premature-Abstraction | |

**User's choice:** window.mdToHtml expose.
**Notes:** Script-Reihenfolge pip-launcher.js VOR app.js (base.html grep Pflicht). XSS-Sanitization-Test Pflicht. Kommentar-Block am Ende der IIFE dokumentiert alle window-Exports.

---

## PiP-Briefing-Tab Position + Verhalten

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-collapse bei neuer EWB-Antwort | Trigger: pip_token_done — mit briefingTabExpandedAtStreamStart Guard | ✓ |
| Education-Hint beim ersten Modus-B-Call | localStorage marker, 3s auto-expand | ✓ |
| Kein Auto-Verhalten | KISS | |

**User's choice:** Auto-collapse (mit Guard) + Education-Hint.
**Notes:** Tab-Position: oben im PiP-Window, unter Header, vor pip-anrede-row. Nicht rendern wenn briefing.firmenname null/leer. localStorage-Key: `nerve.seen_briefing_tab_intro`. Timing: 500ms delay → 300ms open → 3000ms → 300ms close.

---

## Plan-Splitting + Wave-Struktur

| Option | Description | Selected |
|--------|-------------|----------|
| 4 Plans + Wave-Struktur | Wave 1 (Plan 3+4 parallel) → Wave 2 (Plan 1) → Wave 3 (Plan 2) | ✓ |
| Weniger Plans | — | |

**User's choice:** 4 Plans + Wave-Struktur wie angegeben.
**Notes:** Plan 1: Step-4-Footer + Step-4b + Step-4c + Step-5-Button. Plan 2: generate_personalized_skript() + Route + Save. Plan 3: DB-Migration + Cap + DSGVO. Plan 4: PiP-Tab + window.mdToHtml.

---

## Test-Konsolidierung

| Option | Description | Selected |
|--------|-------------|----------|
| 1 konsolidierte test-Datei | tests/test_08_20_3.py mit 6 Klassen | ✓ |
| 4 separate Test-Dateien | Granularer aber mehr Navigation | |

**User's choice:** 1 konsolidiertes File.
**Notes:** 6 pytest-Klassen: TestUIFooter, TestKIPersonalize, TestDBMigration, TestPiPBriefingTab, TestCapEnforcement, TestRollback. ~25-35 Tests insgesamt. Real-Daten-Validation für DB-Migration Pflicht.

---

## Claude's Discretion

- CSS-Implementierung des collapsible Tabs (max-height vs. height transition)
- AbortController-Implementierungsdetail für Cancel-Pfad
- Exakte Prompt-Formulierung in generate_personalized_skript()
- Side-by-Side vs. gestapeltes Layout in Step 4c (CSS-@media-Breakpoint-Wert)

## Deferred Ideas

- Multi-Modus B+C gleichzeitig
- Modus D (manuelle Skript-Bearbeitung) — Andre-Decision 2026-04-30 raus
- Cross-Validation Education-Hint (localStorage counter)
- helpers.js Migration für window-Exports
