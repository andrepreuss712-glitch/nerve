---
phase: 08.20.3-briefing-lebenszyklus-ki-skript-verschmelzung
plan: "01"
subsystem: ui
tags: [pip-launcher, javascript, flask, optgroup, modus-selector, abortcontroller]

requires:
  - phase: 08.20.3-03
    provides: DB-Migration parent_id + is_personalized + briefing_source_firma columns in profile_opener
  - phase: 08.20.3-04
    provides: state keys briefingModus/_personalizedSkriptText/_personalizeAbortController + renderStep() pre-check

provides:
  - renderStep4() 3-button Modus-Selector footer (A=primary, B=ghost, C=ghost/disabled)
  - renderStep4b() KI-Personalisierungs-Ladescreen with AbortController cancel
  - renderStep4c() Vorher/Nachher comparison panels with escHtml XSS protection
  - _savePersonalizedAndStartCall() POST /api/precall/personalize/save with cap_exceeded handling
  - _showCapSubModal() inline delete+re-save flow returning to step 4c on cancel
  - renderStep5() opener dropdown with 3-section optgroup grouping (current-firma / standard / other)
  - Conditional "Personalisieren + Call" button in Step 5 when openerItems.length > 0
  - api_launcher_init + api_launcher_profile now return is_personalized + briefing_source_firma per opener

affects:
  - 08.20.3-02 (precall_service.py KI-backend + /api/precall/personalize routes — consumes renderStep4b fetch)
  - 08.20.3-05 (Call-Flow Integration — briefingModus state now set)

tech-stack:
  added: []
  patterns:
    - optgroup-grouped select for categorized dropdown items (personalized/standard/other)
    - AbortController pattern for cancellable fetch calls (renderStep4b)
    - Pseudo-step string keys ('4b'/'4c') pre-check before switch() in renderStep()

key-files:
  created: []
  modified:
    - static/pip-launcher.js
    - routes/app_routes.py

key-decisions:
  - "State keys + renderStep() pre-check already added by Plan 04 — skipped re-add (deviation)"
  - "briefingText save-back preserved in all 3 Modus A/B/C handlers (not just old accept handler)"
  - "opGroupOther sorted by id desc (newer first) for better UX in dropdown section 3"

patterns-established:
  - "Optgroup pattern: is_personalized + briefing_source_firma fields partition openers into 3 sections"
  - "Auto-select: if exactly 1 opGroupCurrent item exists, state.selectedOpenerId set automatically"

requirements-completed: [Req1, Req2, Req3, Req5, Req7, Req13]

duration: 35min
completed: 2026-05-01
---

# Phase 08.20.3 Plan 01: Step-4-Footer + Pseudo-Steps + Step-5-Dropdown-Grouping Summary

**3-button Modus-Selector A/B/C in Step 4, renderStep4b/4c pseudo-steps with AbortController, _savePersonalizedAndStartCall with cap_exceeded, optgroup opener dropdown in Step 5**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-05-01T08:00:00Z
- **Completed:** 2026-05-01T08:35:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- `renderStep4()` footer vollständig ersetzt: 3 Buttons (Modus A primary, B ghost, C ghost/disabled wenn keine Opener), alle setzen `state.briefingModus` und navigieren zu Step 5
- `renderStep4b()` KI-Ladescreen mit AbortController-Cancel, POST `/api/precall/personalize` mit CSRF-Token; bei Abbruch zurück zu Step 5 ohne DB-Write
- `renderStep4c()` Vorher/Nachher-Vergleich mit `escHtml()` (XSS-Mitigation T-08203-01-01), "Original nutzen" setzt `briefingModus='A'`
- `_savePersonalizedAndStartCall()` + `_showCapSubModal()` implementiert inkl. `cap_exceeded`-Handling und Delete+Re-Save-Flow
- `renderStep5()` Opener-Dropdown auf optgroup-Struktur umgestellt (3 Sektionen: personalisiert für aktuelle Firma / Standard / andere personalisierte), Auto-Select bei genau 1 Match
- `api_launcher_init()` + `api_launcher_profile()` geben jetzt `is_personalized` + `briefing_source_firma` pro Opener zurück

## Task Commits

1. **Task 1: Add briefing_source_firma + is_personalized to opener API responses** — `f58d9ea` (feat)
2. **Task 2: Replace renderStep4() footer with 3-button Modus-Selector** — `d7d6b20` (feat)
3. **Task 3: renderStep4b/4c/_savePersonalized/_showCapSubModal + Step5 optgroup** — `4466448` (feat)

## Files Created/Modified

- `static/pip-launcher.js` — renderStep4() Footer, renderStep4b(), renderStep4c(), _savePersonalizedAndStartCall(), _showCapSubModal(), renderStep5() optgroup + personalize button
- `routes/app_routes.py` — api_launcher_init() + api_launcher_profile() opener dicts erweitert

## Decisions Made

- **briefingText save-back in allen 3 Modus-Handlern beibehalten:** Der alte `lnr-step4-accept` Handler speicherte den bearbeiteten Briefing-Text via textarea zurück ins state-Objekt. Diese Logik wurde in alle 3 neuen Handler (A/B/C) übernommen, da der User die Analyse noch in renderStep4() bearbeiten kann.
- **opGroupOther nach id desc sortiert:** Neuere personalisierte Items (höhere id) erscheinen oben in Sektion 3, da diese wahrscheinlich relevanter sind.

## Deviations from Plan

### Skipped Sub-Edits (per Kontext-Instruktion)

**1. [Plan-Kontext — Intentional Skip] State Keys + renderStep() pre-check bereits von Plan 04 vorhanden**
- **Found during:** Task 2 pre-read (Z.66-71 und Z.153-155)
- **Issue:** Plan 01 Task 2 beschreibt das Hinzufügen von 4 State Keys und dem renderStep() pre-check — Plan 04 hat diese bereits committed (Commit-Message "Phase 08.20.3: Modus-B Briefing Tab state")
- **Fix:** Sub-Edits übersprungen; nur launcher-actions div und onclick-Handler ersetzt wie vom Kontext vorgegeben
- **Verification:** `grep "briefingTabExpandedAtStreamStart\|_personalizedSkriptText\|state.step === '4b'" static/pip-launcher.js` gibt 3+ Treffer

---

**Total deviations:** 1 intentional skip (Plan-Kontext-Instruktion befolgt)
**Impact on plan:** Kein Impact — alle Acceptance Criteria erfüllt.

## Issues Encountered

Keine. Tests: `python -m pytest tests/test_08_20_3.py -x -q` → 23 passed, 5 skipped.

## Known Stubs

Keine. Die neuen Funktionen sind vollständig verdrahtet. `renderStep4b()` ruft `/api/precall/personalize` auf (wird in Plan 02 implementiert) — bis Plan 02 committed ist, gibt der Endpoint 404 zurück, was als Fehlermeldung in `lnr-4b-error` angezeigt wird (kein stiller Fehler).

## Threat Flags

Keine neuen Threat-Surfaces außerhalb des Plans. Alle T-08203-01-0x Mitigations umgesetzt:
- T-01 (XSS): `escHtml()` auf beiden Vorher/Nachher-Panels
- T-02 (CSRF): `getCsrfToken()` in allen 3 fetch-Calls
- T-06 (XSS optgroup label): `escHtml(currentFirma)` in optgroup label

## Next Phase Readiness

- **Plan 02** (KI-Backend + Flask-Routes `/api/precall/personalize` + `/api/precall/personalize/save`) kann jetzt implementiert werden — alle Frontend-Seiten sind bereit
- **Plan 05** (Call-Flow Integration) kann `state.briefingModus` lesen — wird jetzt korrekt gesetzt

---
*Phase: 08.20.3-briefing-lebenszyklus-ki-skript-verschmelzung*
*Completed: 2026-05-01*
