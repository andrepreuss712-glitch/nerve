---
status: feature_incomplete
phase: 08.20.3-briefing-lebenszyklus-ki-skript-verschmelzung
source: [08.20.3-VERIFICATION.md]
started: 2026-05-01T08:13:14Z
updated: 2026-05-01T00:00:00Z
decision: "Modus A + Modus B approved. Modus C (KI-Skript-Personalisierung) deferred nach Block O → Phase 08.20.4. Andre-Decision 2026-05-01."
---

## Current Test

[awaiting human testing]

## Tests

### 1. Modus-B: PiP Briefing-Tab im Live-Call
expected: Nach Briefing in Step 4 auf "📋 Briefing während Call sichtbar lassen" klicken → Call starten → PiP zeigt eingeblendeten #pip-briefing-tab mit Firmenname-Titel, per Klick aufklappbar (300px max-height), CSS-Transition 200ms. Auto-Collapse: Tab war offen → pip_token_done kommt → Tab klappt automatisch zu.
result: [pending]

### 2. Modus-C End-to-End: KI-Personalisierung (echter Call)
expected: Step 4 → "✨ Skript auf diesen Lead personalisieren" → Step 5 → Opener-Dropdown mit optgroup-Sektionen (✨ Personalisiert für [Firma] / 📋 Standard-Opener / 📦 Andere) → "✨ Personalisieren + Call ▶" klicken → renderStep4b() Ladescreen erscheint (~5-10s KI-Call) → renderStep4c() Vorher/Nachher-Vergleich → "Personalisiert nutzen + Call ▶" → neuer ProfileOpener in DB mit is_personalized=True + briefing_source_firma gesetzt → Call startet.
result: [pending]

### 3. Modus-C Abbrechen in Step 4b
expected: Während KI-Ladescreen "Abbrechen" klicken → AbortController.abort() feuert → kein Fehler angezeigt → Zurück zu Step 5, kein DB-Write, kein neuer ProfileOpener erstellt.
result: [pending]

### 4. Cap Sub-Modal (>= 20 personalisierte Items)
expected: Nach >= PERSONALIZED_SCRIPTS_CAP (20) personalisierten Items im Profil → nächster Save-Versuch → _showCapSubModal() zeigt Liste der personalisierten Items mit Alter (Wochen) → Auswahl + "Auswahl löschen + Personalisieren →" → atomarer Delete+Insert → Call startet. "Abbrechen" → zurück zu Vorher/Nachher.
result: [pending]

### 5. Profile-Editor Opener Filter-Toggle
expected: Profil-Editor → Opener-Sektion zeigt 3 Buttons (Alle anzeigen / Nur Standard / Nur personalisiert). "Nur personalisiert" → Cap-Status erscheint ("X von 20 Slots belegt"). Personalisierte Items haben ✨-Badge + read-only Name/Inhalt-Felder. "Löschen" funktioniert für personalisierte Items.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
