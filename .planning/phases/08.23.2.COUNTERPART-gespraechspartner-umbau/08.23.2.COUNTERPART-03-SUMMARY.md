---
phase: 08.23.2.COUNTERPART
plan: 03
subsystem: test-netz / ratsche
tags: [guard, static-sweep, ast, postgres, check-constraint, anti-regression]
requires:
  - "state['counterpart'] als einziger Gespraechspartner-Zustand (Plan 01)"
  - "handle_toggle_counterpart als einziger Umschalter (Plan 01)"
  - "Wortschatz-Freiheit in services/ routes/ static/ templates/ (Plan 01 + 02)"
  - "Fixture schild_guard_pg_conn (tests/conftest.py:949)"
provides:
  - "Waechter 2 — Wortschatz-Sperre: 5 verbotene Bezeichner in 4 Verzeichnissen, __pycache__/.pyc doppelt ausgeschlossen"
  - "Waechter 3 — Ein-Schreiber-Sperre (AST) mit (Datei, Funktion)-Whitelist + 2 Laufzeit-Seed-Tests"
  - "Waechter R3 — CHECK-Constraint-Paritaet ORM-Metadata vs. pg_get_constraintdef"
  - "DIALOG-Eintrag mit den vier zurueckgestellten Entscheidungen"
affects:
  - "Plan 04 (Welle 3) — muss models.py auf 9+ Werte ziehen, sonst bleibt R3 rot"
  - "deploy.sh-Gate — nimmt alle drei Waechter automatisch mit (pytest tests/), keine Gate-Aenderung"
tech-stack:
  added: []
  patterns:
    - "Statischer Sweep mit doppeltem Kompilat-Ausschluss (Verzeichnisname + Endungs-Whitelist)"
    - "Suchmuster fragmentiert zusammengesetzt ('contact' + '_category'), damit der Waechter sich nicht selbst findet"
    - "Falsifizierbarkeits-Selbsttest neben jedem Sweep (der Sweep muss echte Dateien sehen)"
    - "pg_catalog statt information_schema — nicht privilegien-maskiert, kein False-Negative"
key-files:
  created:
    - tests/test_counterpart_vocabulary_guard.py
    - tests/test_counterpart_single_writer.py
    - tests/test_call_events_check_constraint_parity.py
  modified:
    - .planning/DIALOG-GSD-CLAUDIAN.md
decisions:
  - "Der Wortschatz-Waechter bekam einen DRITTEN Test (test_guard_skips_compiled_bytecode) — der __pycache__-Ausschluss ist explizit gepruefte Zusicherung, nicht Beifang"
  - "R3-Waechter NICHT abgeschwaecht, obwohl er bis Plan 04 rot ist (models.py 7 Werte, DB 9) — Expand/Contract, die Deklaration zieht der DB nach"
  - "Kein Schutz-Kommentar geloescht, um einen grep gruen zu bekommen — 6 Kriterien sind wegen plan-eigener Kommentare zahlen-abweichend, alle dokumentiert"
  - "Kein Schild-Nachzug noetig (Punkt 23): nur lesender Katalog-Zugriff im Test, keine neue Tabelle/Spalte/Pfad"
metrics:
  tasks: 3
  commits: 3
  duration: ~40min
  completed: 2026-07-30
---

# Phase 08.23.2.COUNTERPART Plan 03: Die Ratsche einrasten Summary

Drei laufende Wächter ersetzen drei einmalige greps: der alte Wortschatz kann nicht zurückkommen, `counterpart` kann keinen dritten Schreiber bekommen, und eine Drift zwischen ORM-Deklaration und echter Datenbank macht ab jetzt einen Test rot statt ein Jahr lang unbemerkt zu liegen.

## Was gebaut wurde

| Task | Inhalt | Commit | Dateien |
|---|---|---|---|
| 1 | Wächter 2 — Wortschatz-Sperre (3 Tests) | `2bd8a0d` | 1 |
| 2 | Wächter 3 — Ein-Schreiber-Sperre AST (4 Tests) | `d8fa30c` | 1 |
| 3 | Wächter R3 — CHECK-Paritäts-Sperre (3 Tests) + DIALOG | `cd06ca7` | 2 |

### Task 1 — Wortschatz-Sperre

`tests/test_counterpart_vocabulary_guard.py`, Sweep über `services/ routes/ static/ templates/` (`.py`/`.js`/`.html`) auf fünf fragmentiert zusammengesetzte Bezeichner: `current_mode`, `contact_category`, `contactCategory`, `currentMode`, `manual_mode_toggle`.

**Das blosse Wort `gatekeeper` steht bewusst NICHT im Muster** — es ist ein gültiger `counterpart`-Wert und zusätzlich ein völlig unbeteiligter Outcome-Wert im Bestand. Beleg geprüft: `grep -c gatekeeper_blocked templates/dashboard.html` → **5** (Zeilen 792, 877, 881, 956, 960), und der Wächter bleibt grün. `grep -c "^    'gatekeeper'," tests/test_counterpart_vocabulary_guard.py` → **0**.

### Task 2 — Ein-Schreiber-Sperre

`tests/test_counterpart_single_writer.py`, AST-Sweep über `services/` + `routes/`. Jede `ast.Assign`/`ast.AugAssign` mit Subscript-Target und Slice-Konstante `'counterpart'` wird gegen die Whitelist `{('services/deepgram_service.py', 'handle_toggle_counterpart')}` gehalten. Die innerste umschliessende Funktion wird gemeldet — belegt an der realen Closure (`handle_toggle_counterpart` liegt in `register_audio_handlers`, `deepgram_service.py:1122`; der Schreiber steht auf `:1149` und wird korrekt der inneren Funktion zugeordnet, nicht der äusseren).

Der Init-Default ist ein **Dict-Literal** (`live_session.py:406`) und für einen AST-Subscript-Sweep unsichtbar. Deshalb zwei **Laufzeit**-Tests statt Textsuche: `init_session_state(sid,1,1)` → `gatekeeper`, `init_session_state(sid,1,1,mode='meeting')` → `decision_maker` (Meeting-Regressions-Wächter).

### Task 3 — CHECK-Paritäts-Sperre + DIALOG

`tests/test_call_events_check_constraint_parity.py` diffft die `event_type`-Werteliste der ORM-`CheckConstraint` gegen `pg_get_constraintdef(c.oid)` aus `pg_catalog.pg_constraint`. Mengen-Vergleich, nicht String-Vergleich (Postgres normalisiert zu `ANY (ARRAY[...::text])`). „Constraint nicht gefunden" ist ein **Fehler**, kein Skip.

Die bestehende Fixture `schild_guard_pg_conn` (`tests/conftest.py:949`) wird wiederverwendet — **keine neue Fixture, kein neuer DSN, keine `deploy.sh`-Änderung**. Beleg, dass das Gate sie automatisch mitnimmt:

```
deploy.sh:219    NERVE_SCHILD_TEST_DSN="postgresql://nerve_app@/${TEST_DB}" \
deploy.sh:221    /opt/nerve/venv/bin/pytest tests/ --tb=short -q -m "not live and not perf"
```

Der DIALOG-Eintrag hält die vier zurückgestellten Punkte schriftlich fest (Event-Umbenennung als Plan-04-Auftrag, Meeting-Knopf-Frage an André, modus-blindes `current_phase_name`, die zwei Alt-Reste, die volle Achse-A-Umbenennung).

---

## Falsifizierbarkeits-Rot-Läufe (Pflicht, verbatim)

### Wächter 2 — temporäres `# contact_category` in `services/mode_strategy.py`

```
E       AssertionError: Alter Gespraechspartner-Wortschatz zurueck in services/routes/static (1 Treffer):
E           services/mode_strategy.py:176  [contact_category]  # contact_category
E
E         Phase 08.23.2.COUNTERPART: der Gespraechspartner heisst counterpart ('gatekeeper' | 'decision_maker'), die Anruf-Art heisst call_type ('cold_call' | 'meeting'). Keine Wort-Ueberlappung.
E       assert not ['services/mode_strategy.py:176  [contact_category]  # contact_category']

tests\test_counterpart_vocabulary_guard.py:87: AssertionError
=========================== short test summary info ===========================
FAILED tests/test_counterpart_vocabulary_guard.py::test_forbidden_counterpart_vocabulary_absent
1 failed, 2 passed in 0.13s
```

Danach `git checkout -- services/mode_strategy.py` → `git status --porcelain services/mode_strategy.py` **leer**, erneuter Lauf **3 passed**.

### Wächter 3 — temporärer dritter Schreiber in `services/live_session.py`

```
E       AssertionError: Neuer counterpart-Schreiber ausserhalb der Whitelist:
E           services/live_session.py  in _tmp_falsifizierbarkeit()
E
E         Alle gefundenen Schreiber:
E           services/deepgram_service.py:1149  in handle_toggle_counterpart()
E           services/live_session.py:329  in _tmp_falsifizierbarkeit()
E
E         Ein-Schreiber-Sperre (Phase 08.23.2.COUNTERPART): counterpart entsteht nur im Init-Default (live_session.init_session_state) und im Toggle-Handler.
E       assert not {('services/live_session.py', '_tmp_falsifizierbarkeit')}

tests\test_counterpart_single_writer.py:83: AssertionError
=========================== short test summary info ===========================
FAILED tests/test_counterpart_single_writer.py::test_counterpart_has_exactly_one_assigning_writer
1 failed, 3 passed in 0.39s
```

Danach `git checkout -- services/live_session.py` → `git status --porcelain services/live_session.py` **leer**, erneuter Lauf **4 passed**.

### Wächter R3 — Falsifizierbarkeit ohne DB

Ein echter PG-Rot-Lauf ist unter dem Executor-Mandat nicht erzeugbar (kein Deploy, kein SSH, keine Prod-Verbindung). Der DB-freie `test_parser_detects_drift` belegt den Diff-Kern stattdessen synthetisch: `frozenset{'a','b'}` (DB-Form mit `::text`-Casts) vs. `frozenset{'a'}` (ORM-Form) → Differenz `{'b'}` wird erkannt. **passed.**

---

## ⚠ Bekannt ROT bis Plan 04 — der Wächter wurde NICHT abgeschwächt

```
$ python -m pytest tests/test_call_events_check_constraint_parity.py -q
E       AssertionError: ORM-CHECK-Werteliste hat nur 7 Werte: ['audio_health', 'consent_optin',
        'objection_detected', 'phase_change', 'reaction', 'suggestion_shown', 'transcript_chunk']
        — erwartet werden mindestens die 9 seit Migration 0004 gueltigen event_type-Werte.
E       assert 7 >= 9
tests\test_call_events_check_constraint_parity.py:54: AssertionError
1 failed, 1 passed, 1 skipped in 0.08s
```

Das Akzeptanz-Kriterium des Plans lautet „**2 passed, 1 skipped**". Ist-Stand: **1 failed, 1 passed, 1 skipped.**

**Ursache — kein Bug im Wächter, sondern der geplante Expand/Contract-Zwischenzustand.** `database/models.py:785` deklariert weiterhin 7 `event_type`-Werte, während `alembic/versions/0004_add_mode_events_to_call_events.py` die echte Datenbank seit 2026-05-21 auf 9 gesetzt hat (`mode_switch` + `mode_initial`). Genau diese Drift ist der Grund, warum der Wächter gebaut wurde — er zeigt sie beim ersten Lauf sofort an.

**Der Plan hat diesen Nebeneffekt in `<offene_annahmen>` Punkt 3 nur für den PG-Test bedacht** („zwischen Welle 2 und Plan 04 ist der PG-Test rot; ungefährlich, weil er lokal skippt"). Übersehen wurde, dass `test_orm_declaration_is_parsable` **immer** läuft und dieselbe 9-Werte-Erwartung trägt — er ist damit auch lokal rot, nicht nur im Gate.

**Entscheidung (Mandats-konform):** Der Wächter bleibt **unverändert bei `>= 9`**. Ihn auf `>= 7` zu senken oder den Test zu skippen wäre das Abschwächen einer Ratsche, um einen Zwischenstand grün zu bekommen — genau das Anti-Muster, gegen das diese Phase gebaut wird. `database/models.py` wird in **Plan 04** gemeinsam mit der Migration auf `counterpart_initial`/`counterpart_switch` gezogen; dann sind ORM und DB auf denselben 9 Werten und alle drei Tests grün.

**Auflage für Claudian / Plan 04:** Nicht deployen, bevor Plan 04 gelaufen ist. Das `deploy.sh production`-Gate wäre in diesem Fenster rot — was korrekt ist und den Deploy zu Recht blockiert.

---

## Anker-Tabelle aus `<anti_gaming_warnung>` — alle 12 geprüft

| Anker | Datei | Soll | Ist |
|---|---|---|---|
| `steht bewusst im STABILEN Block` | `services/prompt_pipeline.py` | 1 | ✅ 1 |
| `ZWEI ACHSEN` | `services/mode_strategy.py` | 1 | ✅ 1 |
| `Hoerbarkeits-Achse` | `services/mode_strategy.py` | ≥ 2 | ✅ 2 |
| `reason='mode_downgrade'` | `services/deepgram_service.py` | 1 | ✅ 1 |
| `no_active_session` | `services/deepgram_service.py` | 1 | ✅ 1 |
| `nicht reentrant` | `services/live_session.py` | ≥ 1 | ✅ 1 |
| `nicht reentrant` | `services/claude_service.py` | ≥ 1 | ✅ 1 |
| `ACHSE A (call_type)` | `services/live_session.py` | 1 | ✅ 1 |
| `'counterpart' fehlt` | `services/prompt_pipeline.py` | 1 | ✅ 1 |
| `_counterpart_default = 'decision_maker' if mode == 'meeting' else 'gatekeeper'` | `services/live_session.py` | 1 | ✅ 1 |
| `data-counterpart="gatekeeper"` | `templates/base.html` | 1 | ✅ 1 |
| `data-mode-button="gatekeeper"` | `static/pip-launcher.js` / `static/nerve.css` | je 1 | ✅ 1 / 1 |

**Kein Anker wurde gelöscht, umformuliert oder geopfert.**

---

## Abweichungen vom Plan

### Auto-behoben

**1. [Rule 2 — fehlende kritische Zusicherung] Dritter Test im Wortschatz-Wächter: `test_guard_skips_compiled_bytecode`**

- **Gefunden bei:** Task 1 (Orchestrator-Befund vor Plan-Start bestätigt)
- **Problem:** Ein roher `grep` über `services/ static/ routes/` liefert genau einen Treffer der alten Bezeichner — `routes/__pycache__/app_routes.cpython-311.pyc`, kompilierter Bytecode aus der Zeit vor dem Umbau (verifiziert: `grep -rl "contact_category\|current_mode" routes/__pycache__/` → 1 Datei). Der Plan schliesst `__pycache__` zwar aus, aber nur **beiläufig** — kein Test würde merken, wenn der Filter beim nächsten Refactor herausfällt und der Wächter dann auf Vergangenheit statt auf Quelltext rot wird.
- **Fix:** Zweiter unabhängiger Filter (`_SKIP_DIR_NAMES` per Verzeichnisname **plus** Endungs-Whitelist `.py/.js/.html`, die `.pyc`/`.pyo` ohnehin ausschliesst) und ein eigener Test, der beide Zusicherungen assertiert. Begründung steht im Modul-Docstring.
- **Folge fürs Akzeptanz-Kriterium:** `python -m pytest tests/test_counterpart_vocabulary_guard.py -q` liefert **3 passed** statt der geplanten **2 passed**. Der zusätzliche Test ist additiv, kein Ersatz.
- **Datei:** `tests/test_counterpart_vocabulary_guard.py`

**2. [Rule 1 — Robustheit] Fehlermeldung des AST-Wächters direkt aus `extra` formatiert**

- **Gefunden bei:** Task 2
- **Problem:** Der Plan-Vorschlag baut die Verstoss-Liste über String-Zerlegung der bereits formatierten `details`-Zeilen (`d.split(':')[0]`, `d.split(' in ')[-1].rstrip('()')`) und filtert damit gegen `extra`. Das ist funktionsfähig, aber brüchig (Doppel-Zerlegung eines Anzeige-Strings, um eine Menge zu filtern, die bereits strukturiert vorliegt).
- **Fix:** Direkt aus der Menge formatiert (`f'{rel}  in {fn}()' for rel, fn in sorted(extra)`) und die vollständige `details`-Liste als Kontext zusätzlich ausgegeben. Der Rot-Lauf oben zeigt beide Blöcke.
- **Datei:** `tests/test_counterpart_single_writer.py`

### Dokumentiert, NICHT „gefixt" — grep-Kriterien treffen plan-eigene Kommentare

Die Orchestrator-Regel gilt: **kein Schutz-Kommentar wird gelöscht oder umformuliert, um einen grep grün zu bekommen.** Sechs Kriterien zählen Text mit, den der Plan selbst wörtlich vorschreibt. Alle mit exakter Fundstelle:

| Kriterium | Soll | Ist | Sole/zusätzliche Fundstelle | Absicht erfüllt? |
|---|---|---|---|---|
| `grep -cF "handle_toggle_counterpart" tests/test_counterpart_single_writer.py` | 1 | **2** | `:26` = Whitelist-Eintrag (gemeint), `:5` = plan-vorgeschriebener Docstring „2. handle_toggle_counterpart in deepgram_service" | ✅ die Whitelist nennt die Funktion |
| `grep -cF "pg_get_constraintdef" …parity.py` | 1 | **3** | `:73` = das SQL (gemeint), `:16` + `:32` = plan-vorgeschriebene Docstrings | ✅ |
| `grep -c "information_schema" …parity.py` | 0 im Code | **1** | `:14` — der vom Plan als **Pflicht** bezeichnete Begründungs-Kommentar. Im Code (SQL) kommt es nicht vor. | ✅ |
| `grep -cF "privilegien-maskiert" …parity.py` | 1 | **2** | `:15` (Docstring, gemeint) + `:85` (plan-vorgeschriebene Assertion-Meldung) | ✅ |
| `grep -cF "schild_guard_pg_conn" …parity.py` | 1 | **2** | `:68` Fixture-Parameter + `:70` `.cursor()` — beide plan-vorgeschrieben; **keine** neue Fixture, kein neuer DSN | ✅ |
| `grep -cF "MODE_TRANSITION_AUTO"` / `"verify_corpus_gate.py:33"` in DIALOG | je 1 | **je 2** | neuer Eintrag `:1520` / `:1524`; zweiter Treffer `:1487` / `:1488` ist **Bestand** (Cross-AI-Review-Notiz Punkt 7 vom 2026-07-28) | ✅ die zwei Alt-Reste stehen schriftlich |

Keine dieser Zeilen wurde angefasst.

### Bewusst NICHT angefasst (Punkt 17)

- `config/phase_transitions.py` und `scripts/verify_corpus_gate.py` — nur dokumentiert. `git status --porcelain config/phase_transitions.py scripts/verify_corpus_gate.py` → **leer**.
- `database/models.py` — in diesem Plan **nur gelesen**. `git diff HEAD~1 --stat | grep -c "database/models.py"` → **0**. Der Werteliste-Nachzug gehört zu Plan 04.
- `deploy.sh` — `git diff HEAD~1 --stat | grep -c "deploy.sh"` → **0**.
- `alembic/versions/` — `git diff HEAD~1 --stat | grep -c alembic` → **0**. Keine Migration angelegt, keine ausgeführt.

---

## Persistenz-Schicht (Punkt 21 / Punkt 23)

**Schild-Pflicht: kein Nachzug nötig.** Dieser Plan legt keine Tabelle und keine Spalte an und ändert keinen Lese-/Schreib-Pfad der Daten. Der einzige DB-Kontakt ist ein **lesender Katalog-Zugriff** im Test (`pg_catalog.pg_constraint`, read-only, `autocommit=True` in der bestehenden Fixture). Der Tabellen-Kommentar an `call_events` (`database/models.py:789`) und der Spalten-Kommentar an `event_type` (`:780`) bleiben unverändert korrekt.

**Kein SSH, keine `inspect.sh`-Ausführung.** Das Executor-Mandat untersagt jede Production-Verbindung. Der code-seitige Beleg für die 9 DB-Werte ist `alembic/versions/0004_add_mode_events_to_call_events.py` (bereits in Plan 01 verbatim zitiert; keine spätere Migration fasst den Constraint an). **Offene Auflage bleibt bei Claudian:** `inspect.sh constraints call_events` vor dem Deploy einmal ziehen — der R3-Wächter macht diese Prüfung ab Plan 04 dauerhaft überflüssig.

---

## Verifikation (lokal = Vorabsignal, NICHT Abnahme)

```
$ python -m pytest tests/test_counterpart_toggle_roundtrip.py \
                   tests/test_counterpart_vocabulary_guard.py \
                   tests/test_counterpart_single_writer.py \
                   tests/test_call_events_check_constraint_parity.py -q
1 failed, 13 passed, 1 skipped in 0.75s
    FAILED test_call_events_check_constraint_parity.py::test_orm_declaration_is_parsable
    (bekannt, Plan-04-Fenster — siehe oben)

$ python -m pytest tests/test_mode_switch_event.py tests/test_mode_initial_db.py \
                   tests/test_live_session_gatekeeper.py tests/test_session_state_phase_c.py \
                   tests/test_phase_classifier.py tests/test_counterpart_consumers.py \
                   tests/test_h1_latte.py tests/test_callid_race_close.py \
                   tests/test_mode_registry.py tests/test_no_live_global_state.py -q
55 passed, 1 warning in 7.63s

$ grep -rn "current_mode\|contact_category\|contactCategory\|currentMode\|manual_mode_toggle" \
       services/ static/ routes/ templates/ --include=*.py --include=*.js --include=*.html
(keine Ausgabe, exit 1)

$ node --check static/pip-launcher.js        → exit 0
$ python -c "import database.models"         → exit 0
```

Weitere Akzeptanz-Greps:

| Anker | Soll | Ist |
|---|---|---|
| `'current' + 'Mode'` im Wächter | 1 | ✅ 1 |
| `'manual' + '_mode_toggle'` im Wächter | 1 | ✅ 1 |
| `'templates'` im Wächter | 1 | ✅ 1 |
| `'.html'` im Wächter | 1 | ✅ 1 |
| `^    'gatekeeper',` im Wächter | 0 | ✅ 0 |
| `gatekeeper_blocked` in `templates/dashboard.html` | 5 | ✅ 5 (Wächter bleibt grün) |
| `ast.AugAssign` in Wächter 3 | 1 | ✅ 1 |
| `08.23.2.COUNTERPART — was gebaut wurde …` im DIALOG | 1 | ✅ 1 |
| `113 Prod-Altzeilen` im DIALOG | 1 | ✅ 1 |
| `git log -1 --stat` Task 1 / 2 / 3 | 1 / 1 / 2 Dateien | ✅ 1 / 1 / 2 |

**ABNAHME bleibt das `bash deploy.sh production`-Gate** (CLAUDE.md HART-Regel) plus der Test-Anruf — und zwar **nach Plan 04**, nicht nach diesem Plan. Nicht gepusht, nicht deployt, keine Migration ausgeführt, kein SSH, keine Production-Verbindung.

---

## Diese Phase ist NICHT fertig

Es folgt **Plan 04 (Welle 3)**: Event-Umbenennung `mode_initial`/`mode_switch` → `counterpart_initial`/`counterpart_switch` inkl. Alembic-Migration, UPDATE der 113 Prod-Altzeilen und `models.py` auf die neuen Werte. **Erst danach** sind alle drei Wächter grün und die Phase deploy-fähig.

## Known Stubs

Keine. Alle drei Wächter sind vollständig verdrahtet und laufen im Gate mit.

## Threat Flags

Keine. Kein neuer Netzwerk-Endpunkt, kein Auth-Pfad, keine Schema-Änderung, kein neues Datei-Zugriffsmuster. Der einzige neue DB-Kontakt ist ein read-only Katalog-Query über eine bestehende Fixture, die nur gegen `nerve_test` verbindet.

## Self-Check: PASSED

Dateien:
- `tests/test_counterpart_vocabulary_guard.py` — FOUND
- `tests/test_counterpart_single_writer.py` — FOUND
- `tests/test_call_events_check_constraint_parity.py` — FOUND
- `.planning/DIALOG-GSD-CLAUDIAN.md` — FOUND (40 Zeilen ergänzt)

Commits (verifiziert via `git log --oneline -3`):
- `2bd8a0d` — FOUND
- `d8fa30c` — FOUND
- `cd06ca7` — FOUND

Nicht angefasst (verifiziert abwesend im Diff):
- `database/models.py` — 0 Treffer im Diff
- `deploy.sh` — 0 Treffer im Diff
- `alembic/versions/` — 0 Treffer im Diff
- `config/phase_transitions.py`, `scripts/verify_corpus_gate.py` — `git status --porcelain` leer
