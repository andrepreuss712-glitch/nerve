---
phase: 08.23.2.COUNTERPART
plan: 01
subsystem: live-session
tags: [socketio, session-state, refactor, race-condition, tdd]
requires:
  - services/live_session.py (_session_state, _session_state_lock, close_moment)
  - services/deepgram_service.py (register_audio_handlers)
provides:
  - "state['counterpart'] als EINZIGER Gespraechspartner-Zustand ('gatekeeper' | 'decision_maker')"
  - "ls.get_counterpart(sid) — Lese-Helfer Achse B"
  - "Socket-Event toggle_counterpart (Browser -> Server, ohne Wert)"
  - "Socket-Event counterpart_changed (Server -> Browser, source: session_init | manual)"
  - "Socket-Event counterpart_toggle_ack ({ok:true} | {ok:false,error:'no_active_session'})"
  - "mode_initial-Payload: call_type + counterpart getrennt"
  - "mode_switch-Payload: call_type + old_counterpart + new_counterpart"
affects:
  - static/pip-launcher.js (hoert noch auf geloeschte Events — Plan 02)
  - services/prompt_pipeline.py (liest noch contact_category — Plan 02)
  - services/claude_service.py (Phasenmodell — Plan 02)
tech-stack:
  added: []
  patterns:
    - "Server-autoritativer Toggle: Befehl ohne Wert rein, Server rechnet das Gegenteil, Echo raus"
    - "Race-Ablehnung statt Race-Rateln: kein setdefault in nicht-initialisierten State"
    - "Read-Compute-Write in EINEM _session_state_lock-Block"
key-files:
  created:
    - tests/test_counterpart_toggle_roundtrip.py
  modified:
    - services/live_session.py
    - services/deepgram_service.py
    - tests/test_mode_switch_event.py
    - tests/test_mode_initial_db.py
    - tests/test_live_session_gatekeeper.py
    - tests/test_session_state_phase_c.py
decisions:
  - "Init-Default haengt an der Anruf-Art: meeting -> decision_maker, sonst gatekeeper (Meeting-Regression geschlossen)"
  - "KEIN get_call_type()-Helfer gebaut — Achse A behaelt 9 Direktleser + Kommentar-Vertrag (Cross-AI 2026-07-28)"
  - "Toggle ohne Session-State wird abgelehnt (ok:false), nicht gepuffert"
  - "DB-Event-Namen mode_initial/mode_switch bleiben in Welle 1 unveraendert (Umbenennung = Plan 04)"
metrics:
  tasks: 3
  commits: 3
  duration: ~45min
  completed: 2026-07-30
---

# Phase 08.23.2.COUNTERPART Plan 01: Server-autoritativer Gespraechspartner Summary

Der Gesprächspartner liegt jetzt in genau einem Server-Schlüssel (`state['counterpart']`), und der Toggle ist umgedreht: der Browser schickt einen Befehl ohne Wert, der Server rechnet das Gegenteil aus seinem eigenen Zustand und schickt das Ergebnis zurück.

## Was gebaut wurde

| Task | Inhalt | Commit |
|---|---|---|
| 1 | Hin-und-Zurück-Wächter (5 Tests), zweifach ROT belegt | `8bedfb5` |
| 2 | `counterpart` als EIN Zustands-Ort + getrennte `mode_initial`-Payload + `get_counterpart` | `2037c8d` |
| 3 | `handle_toggle_counterpart` ersetzt `handle_manual_mode_toggle`, Init-Sync umgestellt | `de49c10` |

### Task 1 — der Wächter

`tests/test_counterpart_toggle_roundtrip.py` (5 Tests, alle Runtime-Verhalten, kein Source-Presence-Check):

1. `test_toggle_counterpart_roundtrip_returns_to_gatekeeper` — DER Wächter. Assertion nach Klick 1 (`decision_maker`) UND nach Klick 2 (`gatekeeper`) — die erste Assertion ist die Falsifizierbarkeit.
2. `test_toggle_counterpart_ignores_payload` — Server-Autorität, Alt-Payload wirkungslos.
3. `test_toggle_without_session_state_is_refused` — Race-Wächter A (SID unbekannt).
4. `test_toggle_before_init_does_not_create_state` — Race-Wächter B (SID da wie nach `connect`, kein `'state'`-Sub-Dict).
5. `test_successful_toggle_emits_exactly_one_counterpart_changed` — Erfolgs-Emit-Wächter, gezählt statt „mindestens einer".

### Task 2 — ein Zustands-Ort

- `init_session_state`: `contact_category` + `current_mode` → `counterpart`, Default an die Anruf-Art gekoppelt (`_counterpart_default = 'decision_maker' if mode == 'meeting' else 'gatekeeper'`).
- Vertrags-Kommentar `ACHSE A (call_type)` an der `'mode'`-Definition; **kein** `get_call_type()`.
- Neuer Helfer `get_counterpart(sid)` mit Lock-Warnung („nicht reentrant") im Docstring.
- `create_call_for_sid`: `mode_initial`-Payload trägt `call_type` + `counterpart` getrennt; Event-Name unverändert.

### Task 3 — Toggle umgedreht

- Alt-Handler `manual_mode_toggle` vollständig gelöscht (0 Treffer in `services/`).
- Neuer `toggle_counterpart`: kein `setdefault`, Ablehnung mit `counterpart_toggle_ack {'ok': False, 'error': 'no_active_session'}` und **ohne** `counterpart_changed`.
- Phase-Reset, `close_moment(reason='mode_downgrade')`, Skip-Guard und non-fatale DB-Fehlerbehandlung 1:1 übernommen.
- Init-Sync emittiert `counterpart_changed` (`source: 'session_init'`, `call_type` aus der lokalen Variable `mode`).
- TAXO1-07-Achsen-Kommentar auf den neuen Wortschatz gezogen (umformuliert, nicht gelöscht).

## Rot-Belege (verbatim, Task 1)

### ROT-BELEG A — der neue Vertrag fehlt im Alt-Stand

```
$ python -m pytest tests/test_counterpart_toggle_roundtrip.py -x -q
event_name = 'toggle_counterpart'

    def _extract_handler(event_name):
        registered = {}

        mock_sio = MagicMock()
        mock_sio.on = lambda event: (lambda fn: registered.__setitem__(event, fn) or fn)

        from services.deepgram_service import register_audio_handlers
        register_audio_handlers(mock_sio)

>       return registered[event_name], mock_sio
               ^^^^^^^^^^^^^^^^^^^^^^
E       KeyError: 'toggle_counterpart'

tests/test_counterpart_toggle_roundtrip.py:35: KeyError
=========================== short test summary info ===========================
FAILED tests/test_counterpart_toggle_roundtrip.py::test_toggle_counterpart_roundtrip_returns_to_gatekeeper
1 failed in 0.51s
```

### ROT-BELEG B — der echte Bug am Alt-Handler (temporärer Beleg-Lauf, nicht committet)

```
$ python -m pytest tmp_altstand_beleg.py -x -q
                handler({'category': 'target'})   # Klick 2 - der Browser rechnet aus seiner
                                                  # veralteten Kopie WIEDER 'target'
            st = ls._session_state[sid]['state']
>           assert st['current_mode'] == 'gatekeeper', (
                f"Verklemmt: nach zwei Klicks steht current_mode={st['current_mode']!r}, "
                f"contact_category={st['contact_category']!r} - erwartet waere gatekeeper.")
E               AssertionError: Verklemmt: nach zwei Klicks steht current_mode='cold_call', contact_category='target' - erwartet waere gatekeeper.
E               assert 'cold_call' == 'gatekeeper'
E
E                 - gatekeeper
E                 + cold_call

tmp_altstand_beleg.py:26: AssertionError
---------------------------- Captured stdout call -----------------------------
[Phase-C] manual_mode_toggle sid=altstand-beleg-001 category=target new_mode=cold_call
[pip] mode_switch: call_id not set, skip event
[Phase-C] manual_mode_toggle sid=altstand-beleg-001 category=target new_mode=cold_call
[pip] mode_switch: call_id not set, skip event
=========================== short test summary info ===========================
FAILED tmp_altstand_beleg.py::test_altstand_zweiter_klick_kippt_nicht_zurueck
1 failed in 0.62s
```

Die Beleg-Datei wurde vor dem Commit gelöscht (`test -f tmp_altstand_beleg.py` → nicht vorhanden). Beide Belege stehen verbatim in der Commit-Message von `8bedfb5`.

## Persistenz-Schicht (Punkt 21 / Punkt 23)

### `inspect.sh`-Pflicht-Beleg — NICHT ausgeführt (Executor-Mandat)

Der Plan verlangt vor Task 2:

```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema call_events'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh constraints call_events'
```

Das Executor-Mandat dieses Laufs untersagt ausdrücklich jeden SSH-Zugriff auf Production („do not touch production, do not ssh anywhere"). Der Beleg wurde daher **nicht** gezogen. Ersatzweise der code-seitige Beleg aus der Migration, die den Constraint zuletzt gesetzt hat — `alembic/versions/0004_add_mode_events_to_call_events.py:19-34`:

```python
_NEW_CONSTRAINT = (
    "event_type IN ("
    "'transcript_chunk', 'suggestion_shown', 'reaction', 'phase_change', "
    "'audio_health', 'objection_detected', 'consent_optin', "
    "'mode_switch', 'mode_initial'"
    ")"
)
_CONSTRAINT_NAME = 'ck_call_events_event_type'
```

9 Werte inklusive `mode_switch` + `mode_initial`, wie im Plan erwartet. `grep -rn "mode_initial\|mode_switch" alembic/versions/` zeigt: **keine spätere Migration** fasst diesen Constraint noch an, 0004 ist der letzte Schreiber.

**Restrisiko:** code-seitiger Beleg ≠ Live-Beleg. Es bleibt theoretisch möglich, dass Production vom Migrations-Stand abweicht. Das Risiko ist für **diesen** Plan gering, weil hier **kein** `event_type`-Wert geändert wird — nur die JSON-Payload. **Offene Auflage für Claudian:** die beiden `inspect.sh`-Befehle vor dem Deploy der Welle einmal ausführen; weichen sie ab → Plan 03 Task 3 anpassen (so im Plan vorgesehen).

### Schild (Punkt 23)

`call_events` bekommt **keine** neue Spalte und keinen neuen Leser/Schreiber-Pfad — nur die Payload-Struktur innerhalb der bestehenden JSON-Spalte ändert sich. Der bestehende Tabellen-Kommentar (`database/models.py:789`) bleibt korrekt. **Kein Schild-Nachzug nötig.**

## Abweichungen vom Plan

### Auto-behoben

**1. [Rule 3 – Blocker] Beleg-Dateiname ohne führenden Punkt**
- **Gefunden bei:** Task 1, Schritt 3
- **Problem:** `.tmp_altstand_beleg.py` (führender Punkt) lässt sich von pytest nicht sammeln: `TypeError: the 'package' argument is required to perform a relative import for '.tmp_altstand_beleg'`.
- **Fix:** Datei als `tmp_altstand_beleg.py` angelegt, Lauf durchgeführt, danach gelöscht. Nicht committet.
- **Dateien:** keine (temporär)

**2. [Dokumentiert, nicht „gefixt"] Akzeptanz-Kriterium widerspricht dem Plan-eigenen Docstring**
- **Betrifft:** Task 3, Kriterium `grep -cF "setdefault('state'" services/deepgram_service.py` → **0**
- **Konflikt:** Der vom Plan **wörtlich vorgeschriebene** Handler-Docstring enthält den Satz „KEIN `setdefault('state', {})` — das war der Bug." Der grep zählt diesen Schutz-Kommentar mit → Ist-Wert **1**, nicht 0.
- **Entscheidung:** Der Schutz-Kommentar bleibt stehen. Ihn zu löschen, nur um einen grep grün zu bekommen, ist genau das Anti-Muster, gegen das die Anti-Gaming-Anker dieser Phase gebaut wurden. **Die Absicht des Kriteriums ist erfüllt:** im ausgeführten Code existiert kein `setdefault` mehr, der einzige Treffer steht im Docstring (`services/deepgram_service.py:1133`). Der Wächter dafür ist verhaltensbasiert: `test_toggle_before_init_does_not_create_state`.
- **Empfehlung für Plan 03:** das Kriterium auf einen Code-Anker umstellen, z. B. `grep -c "^\s*st = ls\._session_state\[sid\]\.setdefault" ` → 0.

**3. [Rule 2 – Vollständigkeit] `get_counterpart` vor `init_session_state` platziert**
Der Plan sagt „direkt oberhalb von `init_session_state`". So umgesetzt; keine funktionale Abweichung, nur als Beleg für die Reihenfolge vermerkt.

### Bewusst NICHT angefasst

- `services/gatekeeper.py`, `calls.call_mode`, `_session_state[sid]['mode']`, der TEMPO-1-Prompt-Cache-Bust (`prompt_pipeline.py:599-604`) — alle unberührt, kein Diff.
- `services/prompt_pipeline.py` taucht in keinem Commit dieses Plans auf (`git show --stat HEAD | grep -c prompt_pipeline` → 0).

## Bekannter Zwischenstand — Welle 1 ist NICHT abnahmefähig

Dies ist der im Plan angekündigte „test-grün und live kaputt"-Stand. Konkret offen (alles Plan 02):

| Ort | Zustand nach Plan 01 |
|---|---|
| `static/pip-launcher.js:2563,2576,3894` | sendet `manual_mode_toggle`, hört auf `contact_category_update`/`manual_mode_toggle_ack` — alle drei Events existieren serverseitig nicht mehr → **Knopf tot** |
| `services/prompt_pipeline.py:659` | liest `_sub.get('contact_category') or 'gatekeeper'` → Schlüssel weg → **still immer Sekretär-Rolle**, auch beim Entscheider |
| `services/claude_service.py` | Phasenmodell noch nicht auf `(call_type, counterpart)` umgestellt |
| `services/mode_strategy.py:31,91`, `routes/app_routes.py:2030` | Kommentare nennen noch die abgeschafften Wörter (Wortschatz-Sperre, Plan 03) |

**Kein Deploy vor Abschluss von Plan 02.** Nicht gepusht, nicht deployt, keine Migration ausgeführt.

## Verifikation (lokal = Vorabsignal, NICHT Abnahme)

```
$ python -m pytest tests/test_counterpart_toggle_roundtrip.py tests/test_mode_switch_event.py \
    tests/test_mode_initial_db.py tests/test_live_session_gatekeeper.py \
    tests/test_session_state_phase_c.py tests/test_callid_race_close.py -q
21 passed in 0.59s
```

Akzeptanz-Greps (alle erfüllt, ausser dem in Abweichung 2 dokumentierten):

| Anker | Soll | Ist |
|---|---|---|
| `'contact_category'\|'current_mode'` in `live_session.py` | 0 | 0 |
| `'counterpart':           _counterpart_default,` | 1 | 1 |
| `_counterpart_default = 'decision_maker' if mode == 'meeting' else 'gatekeeper'` | 1 | 1 |
| `def get_counterpart(sid: str) -> str:` | 1 | 1 |
| `def get_call_type` in `services/` | 0 | 0 |
| `ACHSE A (call_type)` | 1 | 1 |
| `'counterpart': _counterpart_init,` / `'call_type': _call_type_init,` | 1 / 1 | 1 / 1 |
| `event_type='mode_initial'` / `event_type='mode_switch'` | 1 / 1 | 1 / 1 |
| `nicht reentrant` | ≥1 | 1 |
| `manual_mode_toggle` in `deepgram_service.py` | 0 | 0 |
| `contact_category\|current_mode` in `deepgram_service.py` | 0 | 0 |
| `@sio.on('toggle_counterpart')` | 1 | 1 |
| `'error': 'no_active_session'` | 1 | 1 |
| `sio.emit('counterpart_changed'` | 2 | 2 |
| `reason='mode_downgrade'` | 1 | 1 |
| `broadcast=True` | 0 | 0 |
| `setdefault('state'` | 0 | **1 (nur Docstring — siehe Abweichung 2)** |
| `^def test_` in Wächter-Datei | 5 | 5 |
| `len(_changed) == 1` / `_acks[0].args[1] == {'ok': True}` | 1 / 1 | 1 / 1 |
| `'state' not in` in Wächter-Datei | ≥1 | 1 |
| `tmp_altstand_beleg.py` existiert | nein | nein |

**Abnahme bleibt das `bash deploy.sh production`-Gate** (CLAUDE.md) — nach Plan 02, von Claudian gefahren.

## Threat Flags

Keine. Es entsteht kein neuer Netzwerk-Endpunkt, kein neuer Auth-Pfad, keine Schema-Änderung. Das ersetzte Socket-Event ist **restriktiver** als sein Vorgänger: der Client kann den Zielwert nicht mehr bestimmen (Payload wird ignoriert), und der Alt-Handler-Pfad, der bei fehlender SID fälschlich `ok:True` meldete, ist geschlossen. Alle Emits bleiben `room=sid`, kein Broadcast.

## Self-Check: PASSED

- `tests/test_counterpart_toggle_roundtrip.py` — FOUND
- `services/live_session.py` — FOUND
- `services/deepgram_service.py` — FOUND
- `tests/test_mode_switch_event.py` — FOUND
- `tests/test_mode_initial_db.py` — FOUND
- `tests/test_live_session_gatekeeper.py` — FOUND
- `tests/test_session_state_phase_c.py` — FOUND
- Commit `8bedfb5` — FOUND
- Commit `2037c8d` — FOUND
- Commit `de49c10` — FOUND
- `tmp_altstand_beleg.py` — korrekt ABWESEND
