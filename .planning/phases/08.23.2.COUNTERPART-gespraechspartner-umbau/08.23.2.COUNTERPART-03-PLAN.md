---
phase: 08.23.2.COUNTERPART
plan: 03
type: execute
wave: 2
depends_on: ["08.23.2.COUNTERPART-01", "08.23.2.COUNTERPART-02"]
files_modified:
  - tests/test_counterpart_vocabulary_guard.py
  - tests/test_counterpart_single_writer.py
  - tests/test_call_events_check_constraint_parity.py
  - .planning/DIALOG-GSD-CLAUDIAN.md
autonomous: false
requirements: [COUNTERPART-W3]
complexity: 🟢

must_haves:
  truths:
    - "Der alte Wortschatz kann nicht zurückkommen: ein Test macht jeden neuen Treffer von `current_mode`/`contact_category`/`contactCategory`/`currentMode`/`manual_mode_toggle` in services/, routes/, static/, templates/ rot."
    - "`counterpart` hat genau zwei Schreiber: den Init-Default und den Toggle-Handler — durch einen AST-Sweep erzwungen."
    - "Eine Drift zwischen der ORM-CHECK-Deklaration und dem echten Constraint in Postgres macht ab jetzt einen Test rot — nicht erst den nächsten Zufallsfund."
  artifacts:
    - path: "tests/test_counterpart_vocabulary_guard.py"
      provides: "Wächter 2 — Wortschatz-Sperre"
    - path: "tests/test_counterpart_single_writer.py"
      provides: "Wächter 3 — Ein-Schreiber-Sperre (AST)"
    - path: "tests/test_call_events_check_constraint_parity.py"
      provides: "Wächter R3 — CHECK-Constraint-Parität models.py ↔ echte DB"
      contains: "pg_get_constraintdef"
  key_links:
    - from: "tests/test_counterpart_single_writer.py"
      to: "services/deepgram_service.py (handle_toggle_counterpart)"
      via: "AST-Whitelist (Datei, Funktion)"
      pattern: "handle_toggle_counterpart"
---

<objective>
Die Ratsche einrasten lassen. Zwei statische Wächter sorgen dafür, dass der abgerissene
Zustand nicht durch die Hintertür zurückkommt — plus ein Ein-Zeilen-Nachzug an einer
Deklaration, die seit Migration 0004 von der echten Datenbank abweicht.

Purpose: Ein Umbau ohne Ratsche ist ein Pflaster mit besserem Namen. Die Wächter sind der
Grund, warum dieser Bug nicht ein sechstes Mal wiederkommt.

Output: Wächter 2 (Wortschatz-Sperre), Wächter 3 (Ein-Schreiber-Sperre), CHECK-Nachzug,
und die zurückgestellten Entscheidungen schriftlich im DIALOG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-CONTEXT.md
@.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-RESEARCH.md
@.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-01-PLAN.md
@CLAUDE.md
</context>

<anti_gaming_warnung>
## Der Wächter darf nicht durch Aufräumen grün werden

Ein grep-basiertes Abnahme-Kriterium lädt dazu ein, die Treffer wegzuräumen statt die Sache zu
lösen — insbesondere Schutz-Kommentare, die die verbotenen Wörter nur *erwähnen*.

**Regel für diesen Plan:** Die Wortschatz-Sperre wird **nicht** dadurch grün gemacht, dass
Kommentare gelöscht werden. Alle betroffenen Kommentare wurden in Welle 1 bewusst
**umformuliert** (Plan 01 Task 3 Punkt C, Plan 02 Task 1 Punkt B). Dieser Plan prüft deshalb
zusätzlich **positive Anker**: die Schutz-Kommentare müssen weiterhin da sein.

| Anker (muss existieren) | Datei | Prüfung |
|---|---|---|
| `steht bewusst im STABILEN Block` | `services/prompt_pipeline.py` | `grep -cF` → 1 |
| `ZWEI ACHSEN` | `services/mode_strategy.py` | `grep -cF` → 1 |
| `Hoerbarkeits-Achse` | `services/mode_strategy.py` | `grep -cF` → ≥ 2 |
| `reason='mode_downgrade'` | `services/deepgram_service.py` | `grep -cF` → 1 |
| `no_active_session` | `services/deepgram_service.py` | `grep -cF` → 1 |
| `nicht reentrant` | `services/live_session.py`, `services/claude_service.py` | `grep -cF` → je ≥ 1 |
| `ACHSE A (call_type)` | `services/live_session.py` | `grep -cF` → 1 (Kommentar-Vertrag Achse A) |
| `'counterpart' fehlt` | `services/prompt_pipeline.py` | `grep -cF` → 1 (Zaun um den stillen Fallback) |
| `_counterpart_default = 'decision_maker' if mode == 'meeting' else 'gatekeeper'` | `services/live_session.py` | `grep -cF` → 1 (Meeting-Regression geschlossen) |
| `data-counterpart="gatekeeper"` | `templates/base.html` | `grep -cF` → 1 (Markup umgestellt, nicht nur altes Wort gelöscht) |
| `data-mode-button="gatekeeper"` | `static/pip-launcher.js`, `static/nerve.css` | `grep -cF` → je 1 (EWB-Attribut bewusst NICHT mit umbenannt) |

⚠ **Die neuen Muster `currentMode` und `manual_mode_toggle` sind Bezeichner-Sperren, keine
Wort-Sperren.** Das blosse Wort `gatekeeper` darf **NIE** in `_FORBIDDEN` landen (siehe Task 1) —
es ist ein legitimer `counterpart`-Wert und ausserdem ein unbeteiligter Outcome-Wert im Bestand
(`gatekeeper_blocked` in `templates/dashboard.html`).

Wer einen dieser Anker löscht, um einen grep grün zu bekommen, hat den Plan gebrochen.
</anti_gaming_warnung>

<bewusst_nicht_angefasst>
## Bewusst nicht angefasst (Punkt 17) — dokumentiert, nicht repariert

Zwei Alt-Reste vermischen weiterhin beide Achsen. Sie werden **nur schriftlich festgehalten**
(hier und im DIALOG, Task 3), **keine Code-Änderung**:

| Fundstelle | Was | Warum liegen lassen |
|---|---|---|
| `config/phase_transitions.py:90-101` | `MODE_TRANSITION_AUTO` mischt `gatekeeper`/`cold_call`/`meeting` in denselben Tupeln | **derzeit toter Code**, kein Live-Leser. Ein Umbau wäre Refactor nebenbei. Wer ihn reaktiviert, muss ihn vorher auf die zwei getrennten Achsen ziehen. |
| `scripts/verify_corpus_gate.py:33` | `VALID_MODES = {'cold_call', 'meeting', 'gatekeeper'}` — dieselbe Vermischung in einer Prüf-Konstante | Skript ausserhalb des Live-Pfads; kein Zusammenhang mit dem Umschalter. |

Beide liegen **ausserhalb** des Wortschatz-Sperre-Scopes (`services/`, `routes/`, `static/`,
`templates/`) — der Wächter aus Task 1 wird von ihnen also nicht rot, und das ist Absicht.
`git status --porcelain` muss für beide Dateien **leer** bleiben.
</bewusst_nicht_angefasst>

<tasks>

<task type="auto">
  <name>Task 1: Wächter 2 — Wortschatz-Sperre als Test</name>

  <read_first>
    - `tests/test_no_live_global_state.py` Zeilen 1-60 und 265-320 — das hauseigene Muster für
      statische Sweep-Wächter (Doc-Block mit der „kein Source-Presence-False-Green"-Begründung,
      `Path`-Iteration, Verstoss-Liste mit `Datei:Zeile`, aussagekräftige Fehlermeldung).
    - `services/live_session.py`, `services/deepgram_service.py`, `services/prompt_pipeline.py`,
      `services/mode_strategy.py`, `routes/app_routes.py`, `static/pip-launcher.js`,
      `templates/base.html` — jeweils nur per
      `grep -n "current_mode\|contact_category\|contactCategory\|currentMode\|manual_mode_toggle"`
      prüfen, dass Welle 1 wirklich sauber ist. **Nicht ganze Dateien lesen.**
    - `templates/dashboard.html` — nur `grep -n "gatekeeper_blocked"` (5 Treffer erwartet:
      `:792,877,881,956,960`). Das ist der **Beleg**, warum das blosse Wort `gatekeeper` NICHT
      ins Suchmuster darf: es ist dort ein Anruf-Ergebnis („Gatekeeper blockt"), kein Zustand.
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
Neue Datei, kein Insert. Umschliessender Kontext ist pytest; keine Fixtures, kein State,
kein Netz, keine DB. Ausgangspfad: `assert` oder Rückkehr.

### Schicht 2 — Funktions-Skelett
```
test_forbidden_vocabulary_absent()
  → fuer jedes Verzeichnis in (services/, routes/, static/):
       fuer jede Datei mit Endung .py/.js (rekursiv, __pycache__ ausgeschlossen):
           **[hier der Eingriff: Zeilen gegen die drei verbotenen Woerter pruefen]**
  → Verstoss-Liste sammeln (Datei:Zeile:Wort)
  → assert not violations, <Meldung mit allen Treffern>
```
Ersetzt wird: nichts. Unberührt: alles.

### Schicht 3 — Cross-File-Awareness (grep-belegt)

| Symbol | Leser VOR dem Plan | Leser NACH dem Plan |
|---|---|---|
| `current_mode` | nach Welle 1: **0** in services/ static/ routes/ | dieser Wächter (nur als Suchmuster, gebaut aus Fragmenten) |
| `contact_category` | nach Welle 1: **0** ebenda | dito |
| `contactCategory` | nach Welle 1: **0** ebenda | dito |
| `currentMode` | `static/pip-launcher.js:2637` (`state.currentMode = mode`) — vom alten Unterstrich-Muster **nicht** gefangen; nach Welle 1: 0 | dito |
| `manual_mode_toggle` | `static/pip-launcher.js:2576,2578,3894` (Event + `_ack`); nach Welle 1: 0 | dito |
| `templates/` | HEUTE **0 Treffer** aller verbotenen Bezeichner (verifiziert 2026-07-28) — aber das Knopf-Markup liegt dort (`base.html:586`) und wird in Welle 1 umgebaut | **vorbeugend** im Sweep-Scope, damit die Sperre gültig bleibt, wenn das Markup später wieder angefasst wird |
| `tests/` | enthält die Wörter noch in `test_init_session_state_has_no_legacy_keys` (Absenz-Assertion) | **bewusst NICHT im Sweep-Scope** — die Absenz-Assertion muss die Wörter nennen dürfen |

### Schicht 4 — Edge-Case- und RACE-Analyse
1. **Selbst-Treffer:** Der Wächter enthält die verbotenen Wörter selbst. Er darf sich nicht
   selbst finden → er scannt nur `services/`, `routes/`, `static/`, **nicht** `tests/`.
   Zusätzlich werden die Suchmuster aus Fragmenten zusammengesetzt
   (`'contact' + '_category'`), damit auch ein versehentlich erweiterter Scope die Datei nicht
   rot macht.
2. **`__pycache__`:** `.pyc`-Dateien enthalten die Wörter als Bytes (heute belegt: 4 Treffer).
   Der Sweep filtert auf `.py`/`.js` und überspringt jedes Verzeichnis namens `__pycache__`.
3. **Minified/Fremd-Assets in `static/`:** `static/vendor/` enthält `chart.umd.min.js`,
   `lucide.min.js`, `socket.io.min.js`. Vorab geprüft (2026-07-28): **kein** Treffer eines der
   fünf Muster. Sollte ein künftiges Fremd-Asset zufällig treffen, wird die Datei explizit in
   `_IGNORED_FILES` aufgenommen (mit Begründungs-Kommentar), **nicht** das Muster aufgeweicht.
6. **⚠ Die Falle beim Erweitern des Musters:** Wer `gatekeeper` als Suchwort aufnimmt „um
   gründlicher zu sein", färbt sofort unbeteiligten Bestand rot —
   `templates/dashboard.html:792,877,881,956,960` nutzt `gatekeeper_blocked` als **Outcome-Wert**
   (Anruf-Ergebnis), und `gatekeeper` ist ausserdem ein **gültiger `counterpart`-Wert**. Verboten
   sind die alten **Zustands-/Event-Bezeichner**, nicht das Vokabular. Diese Abgrenzung steht als
   Kommentar im Wächter selbst.
4. **Encoding:** `read_text(encoding='utf-8', errors='ignore')` — `nerve.css`/JS enthalten
   Umlaute; ein UnicodeDecodeError darf den Wächter nicht kippen.
5. **Kein Source-Presence-False-Green:** Der Test prüft ein **verbotenes** Muster, nicht die
   Existenz von erwünschtem Code. Kommt das Muster zurück, wird er rot; verschwindet Code,
   wird er grüner — nie umgekehrt. Das ist der dokumentierte Regex-Grenzfall der CLAUDE.md
   Test-Qualitäts-Regel; die Begründung gehört in den Modul-Docstring.
  </punkt14_audit>

  <files>tests/test_counterpart_vocabulary_guard.py</files>

  <action>
Neue Datei `tests/test_counterpart_vocabulary_guard.py`:

```python
"""Waechter 2 (Wortschatz-Sperre) — Phase 08.23.2.COUNTERPART.

Der Gespraechspartner hatte drei Namen an sieben Orten. Nach dem Abriss heisst er
ueberall 'counterpart'. Dieser Sweep macht jede Rueckkehr der alten Zustands- und
Event-BEZEICHNER in services/, routes/, static/ und templates/ rot.

Abgrenzung (wichtig): verboten sind BEZEICHNER, nicht Vokabular. 'gatekeeper' bleibt ein
gueltiger counterpart-WERT und ist ausserdem ein voellig unbeteiligter Outcome-Wert im
Bestand (gatekeeper_blocked in templates/dashboard.html). Wer 'gatekeeper' ins Muster
aufnimmt, faerbt die halbe Codebasis rot — genau das ist hier NICHT gemeint.

Scope-Entscheidung: tests/ ist NICHT im Sweep. Die Absenz-Assertion in
test_live_session_gatekeeper.py muss die alten Schluesselnamen nennen duerfen.

KEIN Source-Presence-False-Green (CLAUDE.md Test-Qualitaets-Regel): geprueft wird ein
VERBOTENES Muster, nicht die Existenz erwuenschten Codes. Ein False-Green kann nicht
entstehen — kehrt das Muster zurueck, roetet es den Test. Dokumentierter Regex-Grenzfall;
eine Mock-bare Funktion, die 'kein Code nutzt dieses Wort mehr' beweisen koennte,
existiert nicht.
"""
from pathlib import Path

_ROOT = Path(__file__).parent.parent
# templates/ ist mit im Scope, weil das Knopf-Markup dort liegt (base.html:586) und in
# Welle 1 umgebaut wurde. HEUTE steht dort keiner der verbotenen Bezeichner (verifiziert
# 2026-07-28: 0 Treffer) — der Scan ist VORBEUGEND (haelt die Sperre gueltig, wenn das
# Markup spaeter wieder angefasst wird), nicht Loch-schliessend.
_SCAN_DIRS = ('services', 'routes', 'static', 'templates')
_SCAN_SUFFIXES = ('.py', '.js', '.html')

# VERBOTEN sind die alten ZUSTANDS-/EVENT-BEZEICHNER — NICHT das Vokabular.
# NIEMALS das blosse Wort 'gatekeeper' aufnehmen: gueltiger counterpart-Wert +
# unbeteiligter Outcome-Wert 'gatekeeper_blocked' (templates/dashboard.html:792,877,
# 881,956,960). Eine solche "Verschaerfung" faerbt die halbe Codebasis rot.
# Fragmentiert zusammengesetzt, damit diese Datei sich nicht selbst findet,
# falls der Scope je erweitert wird.
_FORBIDDEN = (
    'current' + '_mode',
    'contact' + '_category',
    'contact' + 'Category',
    'current' + 'Mode',         # Browser-Kopie (pip-launcher.js:2637) — Camel-Case ohne
                                # Unterstrich, vom alten Muster NICHT gefangen
    'manual' + '_mode_toggle',  # geloeschtes Socket-Event (deckt auch _ack ab)
)

# Fremd-Assets, die zufaellig treffen wuerden. Leer lassen, solange nichts noetig ist —
# JEDER Eintrag braucht eine Begruendung im Kommentar.
_IGNORED_FILES: frozenset = frozenset()


def _iter_source_files():
    for d in _SCAN_DIRS:
        base = _ROOT / d
        if not base.is_dir():
            continue
        for path in base.rglob('*'):
            if '__pycache__' in path.parts:
                continue
            if path.suffix not in _SCAN_SUFFIXES or not path.is_file():
                continue
            if path.relative_to(_ROOT).as_posix() in _IGNORED_FILES:
                continue
            yield path


def test_forbidden_counterpart_vocabulary_absent():
    violations = []
    for path in _iter_source_files():
        rel = path.relative_to(_ROOT).as_posix()
        text = path.read_text(encoding='utf-8', errors='ignore')
        for line_no, line in enumerate(text.splitlines(), start=1):
            for word in _FORBIDDEN:
                if word in line:
                    violations.append(f'{rel}:{line_no}  [{word}]  {line.strip()[:100]}')
    assert not violations, (
        'Alter Gespraechspartner-Wortschatz zurueck in services/routes/static '
        f'({len(violations)} Treffer):\n  ' + '\n  '.join(violations)
        + '\n\nPhase 08.23.2.COUNTERPART: der Gespraechspartner heisst counterpart '
          "('gatekeeper' | 'decision_maker'), die Anruf-Art heisst call_type "
          "('cold_call' | 'meeting'). Keine Wort-Ueberlappung."
    )


def test_guard_actually_scans_something():
    """Falsifizierbarkeit: der Sweep muss echte Dateien sehen.

    Ohne diesen Test waere ein kaputter Pfad ein stiller Gruen-Macher (0 Dateien
    gescannt = 0 Verstoesse = gruen).
    """
    files = list(_iter_source_files())
    assert len(files) >= 20, f'Sweep sieht nur {len(files)} Dateien — Pfad-Logik kaputt?'
    names = {p.name for p in files}
    for expected in ('live_session.py', 'deepgram_service.py', 'pip-launcher.js',
                     'base.html'):
        assert expected in names, f'{expected} nicht im Sweep-Scope'
```

Ein Commit: `test(08.23.2.COUNTERPART): Waechter 2 — Wortschatz-Sperre`
  </action>

  <verify>
    <automated>python -m pytest tests/test_counterpart_vocabulary_guard.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_counterpart_vocabulary_guard.py -q` → **2 passed**.
    - `grep -rn "current_mode\|contact_category\|contactCategory\|currentMode\|manual_mode_toggle" services/ static/ routes/ templates/ --include=*.py --include=*.js --include=*.html` → **0 Treffer**.
    - `grep -cF "'current' + 'Mode'" tests/test_counterpart_vocabulary_guard.py` → **1** und
      `grep -cF "'manual' + '_mode_toggle'" tests/test_counterpart_vocabulary_guard.py` → **1**
      (die zwei nachgezogenen Muster sind wirklich drin).
    - `grep -cF "'templates'" tests/test_counterpart_vocabulary_guard.py` → **1** und
      `grep -cF "'.html'" tests/test_counterpart_vocabulary_guard.py` → **1**.
    - `grep -c "^    'gatekeeper'," tests/test_counterpart_vocabulary_guard.py` → **0**
      (das blosse Wort `gatekeeper` ist NICHT ins Muster gerutscht).
    - `grep -rn "gatekeeper_blocked" templates/dashboard.html | wc -l` → **5**, und
      `python -m pytest tests/test_counterpart_vocabulary_guard.py -q` bleibt grün
      (Beleg, dass der Wächter unbeteiligten Bestand in Ruhe lässt).
    - Alle positiven Anker aus `<anti_gaming_warnung>` treffen mit den dort genannten Zahlen zu
      (alle sechs Zeilen einzeln prüfen und die Ergebnisse ins SUMMARY schreiben).
    - **Falsifizierbarkeits-Nachweis (Pflicht, im SUMMARY dokumentieren):** temporär
      `# contact_category` als Kommentar in `services/mode_strategy.py` einfügen,
      `python -m pytest tests/test_counterpart_vocabulary_guard.py -q` laufen lassen →
      muss **ROT** mit `services/mode_strategy.py:<n>` sein; Ausgabe verbatim ins SUMMARY,
      dann die Zeile wieder **entfernen** und erneut grün laufen lassen.
      `git status --porcelain services/mode_strategy.py` → **leer** vor dem Commit.
    - `git log -1 --stat` zeigt genau 1 Datei.
  </acceptance_criteria>

  <done>Die Wortschatz-Sperre ist ein laufender Test, nicht ein einmaliger grep — und sie ist nachweislich falsifizierbar.</done>
</task>

<task type="auto">
  <name>Task 2: Wächter 3 — Ein-Schreiber-Sperre (AST)</name>

  <read_first>
    - `tests/test_no_live_global_state.py` Zeilen 240-320 — der AST-Sweep (`ast.walk`,
      `ast.Assign`, `ast.Subscript`, Verstoss-Liste mit Datei:Zeile).
    - `services/deepgram_service.py` — nur die Stelle `grep -n "st\['counterpart'\]"`
      (die einzige erlaubte Zuweisung).
    - `services/live_session.py` — nur `grep -n "'counterpart'" ` (der Init-Default als
      Dict-Literal-Schlüssel, **keine** Subscript-Zuweisung).
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
Neue Datei, kein Insert. Reiner statischer Sweep, keine Fixtures, kein State.

### Schicht 2 — Funktions-Skelett
```
test_counterpart_has_exactly_one_assigning_writer()
  → fuer jede .py-Datei in services/ + routes/ (ohne __pycache__):
       ast.parse(quelle)
       fuer jede FunctionDef: fuer jeden Assign-Knoten darin:
           **[hier der Eingriff: Target ist Subscript mit Slice-Konstante 'counterpart'?]**
               → (relpath, funktionsname, zeile) sammeln
  → gefundene (Datei, Funktion)-Paare gegen _ALLOWED_WRITERS vergleichen
  → assert gleich

test_init_seeds_counterpart()          # Laufzeit, nicht statisch
  → init_session_state(sid, 1, 1) aufrufen
  → assert _session_state[sid]['state']['counterpart'] == 'gatekeeper'
```
Ersetzt wird: nichts.

### Schicht 3 — Cross-File-Awareness (grep-belegt)

| Symbol | Schreiber VOR dem Plan | Schreiber NACH dem Plan |
|---|---|---|
| `state['counterpart']` (Dict-Literal-Seed) | `services/live_session.py` `init_session_state` (Welle 1) | unverändert — genau einer |
| `st['counterpart'] = …` (Subscript-Assign) | `services/deepgram_service.py` `handle_toggle_counterpart` (Welle 1) | unverändert — genau einer |
| Payload-Dicts mit Schlüssel `'counterpart'` | `live_session.create_call_for_sid`, `deepgram_service` (2 Emits) | **keine State-Schreiber** — der AST-Sweep sieht sie korrekt nicht (Dict-Literal ≠ Subscript-Assign) |

### Schicht 4 — Edge-Case- und RACE-Analyse
1. **Der Init-Default ist ein Dict-Literal**, keine Subscript-Zuweisung — der AST-Sweep findet
   ihn nicht. Deshalb der zweite, **Laufzeit**-Test: er beweist die Existenz des Seeds durch
   Aufruf, nicht durch Textsuche.
2. **Zuweisung ausserhalb einer Funktion** (Modul-Ebene) würde der FunctionDef-Iteration
   entgehen → zusätzlich über den ganzen Modul-AST laufen und Treffer ausserhalb jeder
   Funktion als Verstoss mit Funktionsname `'<modul-ebene>'` melden.
3. **Verschachtelte Closures** (`register_audio_handlers` → `handle_toggle_counterpart`): der
   Sweep muss die **innerste** umschliessende Funktion melden, sonst passt der
   Whitelist-Eintrag nicht. Umsetzung: über `ast.walk` alle `FunctionDef` sammeln und den
   Assign-Knoten der Funktion mit der **grössten** `lineno ≤ assign.lineno` und
   `end_lineno ≥ assign.lineno` zuordnen.
4. **Erweiterte Zuweisungen** (`st['counterpart'] += …`) sind `ast.AugAssign` — ebenfalls
   erfassen, sonst gäbe es ein Schlupfloch.
5. **`setattr`/dynamische Keys** kann kein statischer Sweep sehen. Bekannte Grenze, im
   Docstring benennen (wie `test_api_rate_coverage.py` seine Grenze benennt) — nicht
   überversprechen.
6. **Kein Source-Presence-False-Green:** verbotenes Muster + ein echter Laufzeit-Test.
  </punkt14_audit>

  <files>tests/test_counterpart_single_writer.py</files>

  <action>
Neue Datei `tests/test_counterpart_single_writer.py`:

```python
"""Waechter 3 (Ein-Schreiber-Sperre) — Phase 08.23.2.COUNTERPART.

'counterpart' darf nur an ZWEI Stellen entstehen:
  1. Init-Default in live_session.init_session_state (Dict-Literal beim Anlegen des States)
  2. handle_toggle_counterpart in deepgram_service (der einzige Umschalter)

Alles andere waere ein dritter Schreiber — genau die Konstellation, aus der der
urspruengliche Bug entstanden ist (Zustand an sieben Orten).

Grenze dieses Waechters (bewusst benannt, nicht ueberversprochen): ein statischer Sweep
sieht kein setattr() und keinen dynamisch gebauten Schluessel. Er faengt das reale
Muster (st['counterpart'] = ...), nicht jede denkbare Umgehung.

KEIN Source-Presence-False-Green: Test 1 prueft ein VERBOTENES Muster (Schreiber ausserhalb
der Whitelist), Test 2 ruft die echte Funktion auf und assertiert die State-Mutation.
"""
import ast
from pathlib import Path

_ROOT = Path(__file__).parent.parent
_SCAN_DIRS = ('services', 'routes')
_KEY = 'counter' + 'part'

# (relativer Pfad, umschliessende Funktion) — die EINZIGEN erlaubten Subscript-Schreiber.
_ALLOWED_WRITERS = frozenset({
    ('services/deepgram_service.py', 'handle_toggle_counterpart'),
})


def _enclosing_function(func_nodes, lineno):
    best = None
    for fn in func_nodes:
        if fn.lineno <= lineno <= (fn.end_lineno or fn.lineno):
            if best is None or fn.lineno > best.lineno:
                best = fn
    return best.name if best is not None else '<modul-ebene>'


def _iter_py_files():
    for d in _SCAN_DIRS:
        base = _ROOT / d
        if not base.is_dir():
            continue
        for path in base.rglob('*.py'):
            if '__pycache__' in path.parts:
                continue
            yield path


def _collect_writers():
    found = set()
    details = []
    for path in _iter_py_files():
        rel = path.relative_to(_ROOT).as_posix()
        try:
            tree = ast.parse(path.read_text(encoding='utf-8', errors='ignore'))
        except SyntaxError:
            continue
        funcs = [n for n in ast.walk(tree)
                 if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
        for node in ast.walk(tree):
            targets = []
            if isinstance(node, ast.Assign):
                targets = node.targets
            elif isinstance(node, ast.AugAssign):
                targets = [node.target]
            for tgt in targets:
                if (isinstance(tgt, ast.Subscript)
                        and isinstance(tgt.slice, ast.Constant)
                        and tgt.slice.value == _KEY):
                    fname = _enclosing_function(funcs, node.lineno)
                    found.add((rel, fname))
                    details.append(f'{rel}:{node.lineno}  in {fname}()')
    return found, details


def test_counterpart_has_exactly_one_assigning_writer():
    found, details = _collect_writers()
    extra = found - _ALLOWED_WRITERS
    missing = _ALLOWED_WRITERS - found
    assert not extra, (
        'Neuer counterpart-Schreiber ausserhalb der Whitelist:\n  '
        + '\n  '.join(d for d in details if (d.split(':')[0],
                                             d.split(' in ')[-1].rstrip('()')) in extra)
        + '\n\nEin-Schreiber-Sperre (Phase 08.23.2.COUNTERPART): counterpart entsteht nur '
          'im Init-Default (live_session.init_session_state) und im Toggle-Handler.'
    )
    assert not missing, (
        f'Erwarteter counterpart-Schreiber fehlt: {sorted(missing)}. '
        'Gefunden wurde:\n  ' + '\n  '.join(details)
    )


def test_init_session_state_seeds_counterpart():
    """Laufzeit-Beleg fuer den zweiten erlaubten Schreiber (Dict-Literal, AST-unsichtbar)."""
    import services.live_session as ls
    sid = 'test-counterpart-single-writer-001'
    try:
        ls.init_session_state(sid, 1, 1)
        with ls._session_state_lock:
            st = ls._session_state[sid]['state']
        assert st['counterpart'] == 'gatekeeper', (
            f"Init-Default (cold_call) muss 'gatekeeper' sein, war: {st['counterpart']!r}")
    finally:
        with ls._session_state_lock:
            ls._session_state.pop(sid, None)


def test_init_session_state_meeting_seeds_decision_maker():
    """Meeting-Regressions-Waechter: zu einem Termin sitzt man beim Entscheider.

    Ohne diese Kopplung liefe JEDER Meeting-Anruf bis zum ersten manuellen Toggle still
    im 4-Phasen-Sekretaersmodell (Cross-AI-Fund 2026-07-28).
    """
    import services.live_session as ls
    sid = 'test-counterpart-single-writer-002'
    try:
        ls.init_session_state(sid, 1, 1, mode='meeting')
        with ls._session_state_lock:
            st = ls._session_state[sid]['state']
        assert st['counterpart'] == 'decision_maker', (
            f"Meeting muss beim Entscheider starten, war: {st['counterpart']!r}")
    finally:
        with ls._session_state_lock:
            ls._session_state.pop(sid, None)


def test_guard_actually_scans_something():
    files = list(_iter_py_files())
    assert len(files) >= 15, f'Sweep sieht nur {len(files)} Dateien — Pfad-Logik kaputt?'
```

Ein Commit: `test(08.23.2.COUNTERPART): Waechter 3 — Ein-Schreiber-Sperre fuer counterpart`
  </action>

  <verify>
    <automated>python -m pytest tests/test_counterpart_single_writer.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_counterpart_single_writer.py -q` → **4 passed**
      (inkl. Meeting-Regressions-Wächter).
    - `grep -cF "handle_toggle_counterpart" tests/test_counterpart_single_writer.py` → **1**
      (die Whitelist nennt die Funktion, nicht nur die Datei).
    - `grep -cF "ast.AugAssign" tests/test_counterpart_single_writer.py` → **1**
      (das `+=`-Schlupfloch ist zu).
    - **Falsifizierbarkeits-Nachweis (Pflicht, im SUMMARY dokumentieren):** temporär in
      `services/live_session.py` in einer beliebigen Funktion die Zeile
      `_session_state[sid]['state']['counterpart'] = 'gatekeeper'` einfügen →
      `python -m pytest tests/test_counterpart_single_writer.py -q` muss **ROT** werden und
      `services/live_session.py` als unerlaubten Schreiber melden; Ausgabe verbatim ins SUMMARY,
      Zeile wieder entfernen, erneut grün laufen lassen.
      `git status --porcelain services/live_session.py` → **leer** vor dem Commit.
    - `git log -1 --stat` zeigt genau 1 Datei.
  </acceptance_criteria>

  <done>`counterpart` hat nachweislich genau zwei Schreiber, und ein dritter würde den Deploy-Gate rot machen.</done>
</task>

<task type="auto">
  <name>Task 3: Wächter R3 — CHECK-Constraint-Parität models.py ↔ echte DB + zurückgestellte Entscheidungen</name>

  <read_first>
    - `database/models.py` Zeilen 774-790 (`CallEvent`, insbesondere die `CheckConstraint`-Zeile
      `:785` und der Tabellen-Kommentar `:789`) — **nur lesen, in diesem Plan NICHT ändern.**
    - `alembic/versions/0004_add_mode_events_to_call_events.py` (vollständig, ~30 Zeilen) —
      die Migration, die `mode_switch`/`mode_initial` in der echten DB ergänzt hat, während die
      ORM-Deklaration bei 7 Werten stehen blieb. **Das ist die Drift, die dieser Wächter ab jetzt
      fängt.**
    - `tests/test_schild_guard.py` Zeilen 1-55 — das hauseigene Muster für real-PG-Wächter
      (Fixture `schild_guard_pg_conn`, Skip ohne DSN, „kein SQLite-Fallback"-Begründung).
    - `tests/conftest.py` — nur `grep -n "schild_guard_pg_conn" -A 20` (die Fixture selbst:
      `NERVE_SCHILD_TEST_DSN`, psycopg2, `pytest.skip` ohne DSN).
    - `deploy.sh` — nur `grep -n "NERVE_SCHILD_TEST_DSN\|pytest tests/"` (Beleg, dass der DSN im
      Production-Gate gesetzt ist und `pytest tests/` die neue Datei automatisch mitnimmt —
      **keine** Deploy-Änderung nötig).
    - `.planning/DIALOG-GSD-CLAUDIAN.md` — nur das **Ende** (letzte ~40 Zeilen), um Schreibstil und
      Format der bestehenden Einträge zu treffen.
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
Neue Testdatei, kein Insert in Bestandscode. Umschliessender Kontext ist pytest. Ein Test hängt an
der Fixture `schild_guard_pg_conn` (skippt ohne `NERVE_SCHILD_TEST_DSN`), zwei Tests laufen immer.
`database/models.py` wird in diesem Plan **nur gelesen**, nicht geändert — der Nachzug der
Werteliste passiert in **Plan 04 (Welle 3)** gemeinsam mit der Migration, damit Deklaration und
echte DB im selben Schritt auf dieselben Werte gehen (Expand/Contract, AUTH-2-Lehre: die
Deklaration darf der DB nachziehen, nie vorauseilen).

### Schicht 2 — Funktions-Skelett
```
_parse_values(sql)                 # rein: "event_type IN ('a', 'b')" -> frozenset{'a','b'}
_orm_values()                      # Laufzeit: CallEvent.__table__.constraints -> _parse_values

test_check_constraint_parity(schild_guard_pg_conn)     # real-PG
  → pg_get_constraintdef aus pg_catalog holen
  → _parse_values(db_def) vs _orm_values()
  → assert Mengengleichheit, Fehlermeldung nennt beide Differenzmengen

test_parser_detects_drift()        # synthetisch, laeuft IMMER -> Falsifizierbarkeit ohne DB
test_orm_declaration_is_parsable() # Laufzeit, laeuft IMMER
```
Ersetzt wird: nichts. Unberührt: alles.

### Schicht 3 — Cross-File-Awareness (grep-belegt)

| Symbol | Leser VOR dem Plan | Leser NACH dem Plan |
|---|---|---|
| `ck_call_events_event_type` (ORM) | `Base.metadata.create_all` (SQLite-Testpfade) | zusätzlich dieser Wächter (liest die Deklaration zur Laufzeit, nicht als Text) |
| `ck_call_events_event_type` (echte DB) | Postgres, gesetzt durch Migration 0004 (**9 Werte**) | zusätzlich dieser Wächter (read-only `pg_catalog`) |
| `schild_guard_pg_conn` | `tests/test_schild_guard.py` | zusätzlich diese Datei (dieselbe Fixture, kein neuer DSN, keine `deploy.sh`-Änderung) |
| `database/models.py` | — | **in diesem Plan nur gelesen**; geändert wird sie in Plan 04 |

### Schicht 4 — Edge-Case- und RACE-Analyse
1. **Warum `pg_catalog` und nicht `information_schema.check_constraints`:** `information_schema`
   ist **privilegien-maskiert** — es zeigt einer Rolle nur, worauf sie Rechte hat. Der Wächter
   läuft als `nerve_app` (peer-auth, wie der Schild-Guard). Eine leere Zeilenmenge wäre dann ein
   **False-Negative** (bekannte Falle: FORCE-RLS-/Privileg-Masken haben hier schon einmal
   „kein Row"-Fehlschlüsse erzeugt). `pg_catalog.pg_constraint` +
   `pg_get_constraintdef(oid)` ist für jede Rolle lesbar und liefert **dieselbe** Information.
   Deshalb prüft der Test zusätzlich explizit, dass **eine** Constraint-Zeile gefunden wurde —
   „nicht gefunden" ist ein Fehler, nie ein stilles Grün.
2. **Zeitpunkt im Wellen-Ablauf:** Zwischen Welle 2 (dieser Plan) und Plan 04 sind ORM (7 Werte)
   und DB (9 Werte) **noch** verschieden — der PG-Test wäre in diesem Fenster ROT. Das ist
   ungefährlich, weil (a) er lokal ohne DSN **skippt** und (b) in diesem Fenster **nicht deployt
   wird** (`autonomous: false`, Claudian fährt den Deploy erst nach Plan 04). Beim Deploy-Gate am
   Phasen-Ende sind beide Seiten auf den neuen Werten → grün.
3. **Reihenfolge im Gate:** `deploy.sh` fährt `alembic upgrade` auf `nerve_test` **vor** `pytest`
   — die Plan-04-Migration ist also angewandt, wenn dieser Test läuft. Keine Sonderbehandlung.
4. **Der Wächter prüft Mengen, nicht Reihenfolge/Formatierung.** `pg_get_constraintdef` schreibt
   den Ausdruck normalisiert um (`::text`-Casts, `ANY (ARRAY[...])`) — deshalb wird über
   `re.findall(r"'([^']+)'", …)` extrahiert und als `frozenset` verglichen, nicht per String-Gleichheit.
   Sonst wäre der Test dauerhaft rot ohne echte Drift.
5. **Kein Source-Presence-False-Green:** Test 1 vergleicht zwei **Laufzeit**-Quellen
   (ORM-Metadata vs. Postgres-Katalog). Test 2 ist ein reiner Parser-/Diff-Test mit synthetischen
   Eingaben — er beweist die Falsifizierbarkeit **ohne** DB und ohne Server-Zugriff (HART-Regel:
   der Executor deployt nicht, kann also keinen echten Rot-Lauf gegen Postgres erzeugen).
6. **Punkt 23 (Schild):** keine neue Tabelle, keine neue Spalte, kein neuer Leser/Schreiber-Pfad
   der Daten — nur ein lesender Katalog-Zugriff im Test. **Kein Schild-Nachzug nötig**, im SUMMARY
   explizit vermerken.
  </punkt14_audit>

  <files>tests/test_call_events_check_constraint_parity.py, .planning/DIALOG-GSD-CLAUDIAN.md</files>

  <action>
**Schritt 1 — Neue Datei `tests/test_call_events_check_constraint_parity.py`:**

```python
"""Waechter R3 (CHECK-Constraint-Paritaet) — Phase 08.23.2.COUNTERPART.

database/models.py deklarierte 7 event_type-Werte, die echte Datenbank kannte 9
(Migration 0004 vom 2026-05-21 ergaenzte mode_switch + mode_initial, die ORM-Deklaration
hinkte seitdem hinterher). Die Drift lag ueber ein Jahr unbemerkt herum und ist erst in
dieser Phase per Zufallsfund aufgefallen.

Ein einmaliger inspect.sh-Blick ist KEIN Waechter: bei der naechsten Constraint-Aenderung
entsteht dieselbe Drift wieder. Dieser Test diffft die Werteliste der ORM-CheckConstraint
gegen den echten Constraint in Postgres und laeuft im deploy.sh-production-Gate mit
(pytest tests/ dort mit gesetztem NERVE_SCHILD_TEST_DSN). Lokal ohne DSN: SKIP, nie ein
SQLite-Fallback (der wuerde die Frage gar nicht beantworten koennen).

Warum pg_catalog statt information_schema.check_constraints: information_schema ist
privilegien-maskiert und liefert der Rolle nerve_app ggf. LEER -> False-Negative.
pg_catalog.pg_constraint + pg_get_constraintdef ist fuer jede Rolle lesbar und traegt
dieselbe Information. "Constraint nicht gefunden" ist deshalb ein FEHLER, kein Skip.

KEIN Source-Presence-False-Green (CLAUDE.md Test-Qualitaets-Regel): Test 1 vergleicht zwei
LAUFZEIT-Quellen (SQLAlchemy-Metadata vs. Postgres-Katalog). Test 2 ist ein reiner
Parser-/Diff-Test mit synthetischen Eingaben und belegt die Falsifizierbarkeit ohne DB.
"""
import re

_CONSTRAINT_NAME = 'ck_call_events_event_type'
_TABLE = 'call_events'


def _parse_values(sql: str) -> frozenset:
    """Zieht die einfach-gequoteten Literale aus einer CHECK-Definition.

    Vergleicht MENGEN, nicht Text: pg_get_constraintdef normalisiert den Ausdruck
    (::text-Casts, ANY (ARRAY[...])) — ein String-Vergleich waere dauerhaft rot
    ohne echte Drift.
    """
    return frozenset(re.findall(r"'([^']+)'", sql or ''))


def _orm_values() -> frozenset:
    """Werteliste aus der ORM-Deklaration (Laufzeit, ueber die SQLAlchemy-Metadata)."""
    from sqlalchemy import CheckConstraint
    from database.models import CallEvent
    for c in CallEvent.__table__.constraints:
        if isinstance(c, CheckConstraint) and c.name == _CONSTRAINT_NAME:
            return _parse_values(str(c.sqltext))
    raise AssertionError(
        f'CheckConstraint {_CONSTRAINT_NAME!r} fehlt in der ORM-Deklaration von CallEvent '
        '— entweder umbenannt oder geloescht. Beides ist ein Befund, kein Testfehler.')


def test_orm_declaration_is_parsable():
    """Laeuft immer: die ORM-Deklaration existiert und traegt eine nicht-leere Werteliste."""
    vals = _orm_values()
    assert len(vals) >= 9, (
        f'ORM-CHECK-Werteliste hat nur {len(vals)} Werte: {sorted(vals)} — '
        'erwartet werden mindestens die 9 seit Migration 0004 gueltigen event_type-Werte.')


def test_parser_detects_drift():
    """Falsifizierbarkeit ohne DB: der Diff-Kern erkennt eine Abweichung wirklich."""
    db = _parse_values("CHECK (event_type::text = ANY (ARRAY['a'::text, 'b'::text]))")
    orm = _parse_values("event_type IN ('a')")
    assert db == frozenset({'a', 'b'})
    assert db != orm, 'Der Parser wuerde eine echte Drift nicht sehen — Waechter wertlos.'
    assert (db - orm) == frozenset({'b'})


def test_check_constraint_matches_db(schild_guard_pg_conn):
    """Der eigentliche Waechter: ORM-Deklaration == echter Constraint in Postgres."""
    cur = schild_guard_pg_conn.cursor()
    cur.execute(
        """
        SELECT pg_get_constraintdef(c.oid)
        FROM pg_constraint c
        JOIN pg_class t ON t.oid = c.conrelid
        JOIN pg_namespace n ON n.oid = t.relnamespace
        WHERE c.conname = %s AND t.relname = %s AND n.nspname = 'public'
        """,
        (_CONSTRAINT_NAME, _TABLE),
    )
    rows = cur.fetchall()
    assert len(rows) == 1, (
        f'Erwartet genau EINEN Constraint {_CONSTRAINT_NAME!r} auf public.{_TABLE}, '
        f'gefunden: {len(rows)}. "Nicht gefunden" ist ein Befund, kein Skip '
        '(pg_catalog ist nicht privilegien-maskiert).')
    db_vals = _parse_values(rows[0][0])
    orm_vals = _orm_values()
    assert db_vals == orm_vals, (
        'DRIFT zwischen ORM-Deklaration und echter Datenbank:\n'
        f'  nur in models.py : {sorted(orm_vals - db_vals)}\n'
        f'  nur in der DB    : {sorted(db_vals - orm_vals)}\n'
        f'  DB-Definition    : {rows[0][0]}\n\n'
        'Regel (AUTH-2-Expand/Contract): die Deklaration zieht der DB NACH, nie voraus. '
        'Wer eine CHECK-Werteliste aendert, aendert BEIDES im selben Schritt — '
        'Migration und models.py.')
```

**Schritt 2 — `.planning/DIALOG-GSD-CLAUDIAN.md` ans Ende anhängen** (Format der bestehenden
Einträge übernehmen, KEIN interaktives Menü — André liest vom Handy):

```markdown
## 08.23.2.COUNTERPART — was gebaut wurde und was bewusst liegen bleibt (2026-07-28, Planung nach Cross-AI)

**GEBAUT (Plan 04, Welle 3): die Event-Umbenennung.** `mode_initial`/`mode_switch` heissen ab
dieser Phase `counterpart_initial`/`counterpart_switch`. Alte Namen raus, die **113 Prod-Altzeilen**
(72 + 41) werden per UPDATE mitgezogen, `models.py` steht auf den NEUEN Werten, Deploy-Reihenfolge
Migration → Code. Ein Rollback ist gutartig, weil beide Event-Writer non-fatal sind
(`live_session.py:747`, `deepgram_service.py:1194`): alter Code + neuer Constraint → der Event
wird verworfen, der Anruf laeuft weiter. Kein Halb-Zustand, keine „alten Werte im Constraint
belassen"-Variante.

**LIEGT BEWUSST LIEGEN — 1: Der Toggle-Knopf wird bei `call_type == 'meeting'` weiterhin NICHT
ausgeblendet.** Der CSS-Selektor `[data-mode="meeting"]`, der das tun sollte, war seit seiner
Entstehung tot (kein Schreiber setzte je den Wert `meeting`). Er wurde beim Attribut-Umbau entfernt,
statt einen neuen Signalweg zu bauen — dafuer gibt es keine Entscheidung und es waere Refactor
nebenbei. **Frage an André: soll der Gespraechspartner-Knopf im Meeting verschwinden?** Wenn ja:
kleiner eigener Schritt (der Server schickt `call_type` bereits im `counterpart_changed`-Echo mit,
der Browser muesste ihn nur auswerten).

**LIEGT BEWUSST LIEGEN — 2: `current_phase_name` bleibt modus-blind.** `claude_service.py` schreibt
den Phasen-Namen immer aus der Cold-Call-Labelliste (`_PHASE_NAMES`), unabhaengig vom gewaehlten
Phasenmodell. Das Phasenmodell selbst waehlt jetzt korrekt nach `(call_type, counterpart)` — nur
dieses eine Anzeige-Label hinkt nach. Eigener Befund aus der Kartierung.

**LIEGT BEWUSST LIEGEN — 3: zwei Alt-Reste, nur dokumentiert, nicht angefasst** (Punkt 17):
- `config/phase_transitions.py:90-101` — `MODE_TRANSITION_AUTO` mischt weiter beide Achsen
  (`gatekeeper`/`cold_call`/`meeting` in denselben Tupeln). Derzeit **toter Code**; er wird von
  keinem Live-Pfad gelesen. Wer ihn reaktiviert, muss ihn vorher auf die zwei getrennten Achsen
  umbauen.
- `scripts/verify_corpus_gate.py:33` — dieselbe Achsen-Vermischung in einer Pruef-Konstante
  (`VALID_MODES = {'cold_call', 'meeting', 'gatekeeper'}`).
Beide liegen ausserhalb des Wortschatz-Sperre-Scopes (`services/`, `routes/`, `static/`,
`templates/`) und bleiben in dieser Phase unberuehrt.

**LIEGT BEWUSST LIEGEN — 4: die vollstaendige Umbenennung von Achse A.** Der Speicher-Schluessel
`_session_state[sid]['mode']` behaelt seinen Namen (9 Direktleser, MODE_REGISTRY/mode_strategy
sitzen darauf). Er hat jetzt einen Kommentar-Vertrag an der Definition. Ein Lese-Helfer
`get_call_type()` wurde bewusst **nicht** gebaut — ein Helfer mit einem Aufrufer neben neun
Direktlesern waere dasselbe „der-Name-luegt"-Muster eine Ebene hoeher. Die volle Umbenennung
fuehrt André als Backlog-Eintrag.
```

Ein Commit: `test(08.23.2.COUNTERPART): Waechter R3 — CHECK-Constraint-Paritaet models.py vs. echte DB`
(die DIALOG-Ergänzung geht im selben Commit mit).
  </action>

  <verify>
    <automated>python -m pytest tests/test_call_events_check_constraint_parity.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_call_events_check_constraint_parity.py -q` →
      **2 passed, 1 skipped** (lokal ohne `NERVE_SCHILD_TEST_DSN` skippt der PG-Test —
      das ist der gewollte Zustand, kein Mangel).
    - `grep -cF "pg_get_constraintdef" tests/test_call_events_check_constraint_parity.py` → **1**.
    - `grep -c "information_schema" tests/test_call_events_check_constraint_parity.py` → **0**
      im Code; der **Kommentar** im Docstring, warum nicht, ist Pflicht:
      `grep -cF "privilegien-maskiert" tests/test_call_events_check_constraint_parity.py` → **1**.
    - `grep -cF "schild_guard_pg_conn" tests/test_call_events_check_constraint_parity.py` → **1**
      (bestehende Fixture wiederverwendet — **keine** neue Fixture, **kein** neuer DSN,
      **keine** `deploy.sh`-Änderung).
    - `git diff HEAD~1 --stat | grep -c "deploy.sh"` → **0**.
    - `git diff HEAD~1 --stat | grep -c "database/models.py"` → **0**
      (der Werteliste-Nachzug gehört zu Plan 04, nicht hierher).
    - `ls alembic/versions/ | wc -l` **unverändert** gegenüber Plan-Start —
      `git diff HEAD~1 --stat | grep -c alembic` → **0**.
    - `grep -cF "08.23.2.COUNTERPART — was gebaut wurde und was bewusst liegen bleibt" .planning/DIALOG-GSD-CLAUDIAN.md` → **1**.
    - `grep -cF "113 Prod-Altzeilen" .planning/DIALOG-GSD-CLAUDIAN.md` → **1**.
    - `grep -cF "MODE_TRANSITION_AUTO" .planning/DIALOG-GSD-CLAUDIAN.md` → **1** und
      `grep -cF "verify_corpus_gate.py:33" .planning/DIALOG-GSD-CLAUDIAN.md` → **1**
      (die zwei Alt-Reste stehen schriftlich).
    - `git status --porcelain config/phase_transitions.py scripts/verify_corpus_gate.py` → **leer**
      (dokumentiert, **nicht** angefasst).
    - `git log -1 --stat` zeigt genau 2 Dateien.
  </acceptance_criteria>

  <done>Eine Drift zwischen ORM-CHECK-Deklaration und echter Datenbank macht ab jetzt einen Test rot — und die vier zurückgestellten Punkte stehen schriftlich im DIALOG statt in einem Kopf.</done>
</task>

</tasks>

<verification>
## Phasen-Abschluss-Prüfung (lokal = Vorabsignal, NICHT Abnahme)

```bash
# Die vier Waechter
python -m pytest tests/test_counterpart_toggle_roundtrip.py \
                 tests/test_counterpart_vocabulary_guard.py \
                 tests/test_counterpart_single_writer.py \
                 tests/test_call_events_check_constraint_parity.py -q
# Die mitgezogenen Bestands-Tests
python -m pytest tests/test_mode_switch_event.py tests/test_mode_initial_db.py \
                 tests/test_live_session_gatekeeper.py tests/test_session_state_phase_c.py \
                 tests/test_phase_classifier.py tests/test_counterpart_consumers.py \
                 tests/test_h1_latte.py tests/test_callid_race_close.py \
                 tests/test_mode_registry.py tests/test_no_live_global_state.py -q
# Wortschatz-Sperre als roher grep (muss 0 sein)
grep -rn "current_mode\|contact_category\|contactCategory\|currentMode\|manual_mode_toggle" \
     services/ static/ routes/ templates/ --include=*.py --include=*.js --include=*.html
node --check static/pip-launcher.js
python -c "import database.models"
```

**ABNAHME (HART, CLAUDE.md):** Kein lokales pytest ist die Abnahme. Die Abnahme ist das
`bash deploy.sh production`-Gate (läuft auf dem Prod-Server vor dem Restart; bei Test-Fehler
kein Restart, alter Code bleibt live) plus ein Test-Anruf im Browser mit dem Test-User:
Knopf klicken → Anzeige kippt auf `Entscheider` → nochmal klicken → kippt zurück auf
`Sekretär` → viermal hintereinander, ohne zu verklemmen.

**KEIN Deploy, KEIN `git push`, KEINE Migration-Ausführung durch den Executor.**
`autonomous: false` — nach diesem Plan ANHALTEN.

⚠ **Diese Phase ist nach Plan 03 NICHT fertig.** Es folgt **Plan 04 (Welle 3)**: die
Event-Umbenennung inkl. Alembic-Migration (`counterpart_initial`/`counterpart_switch`, 113
Altzeilen per UPDATE, `models.py` auf die NEUEN Werte). Der Test-Anruf oben ist die Abnahme der
**gesamten** Phase, nicht dieses Plans.
</verification>

<success_criteria>
- Wächter 2 und 3 sind als falsifizierbar nachgewiesen (je ein dokumentierter Rot-Lauf);
  Wächter R3 belegt seine Falsifizierbarkeit über den DB-freien Parser-/Diff-Test
  (ein echter PG-Rot-Lauf ist ohne Deploy nicht erzeugbar — HART-Regel).
- `grep -rn "current_mode\|contact_category\|contactCategory\|currentMode\|manual_mode_toggle" services/ static/ routes/ templates/` → 0.
- Kein Schutz-Kommentar wurde geopfert, um einen grep grün zu bekommen (Anker-Tabelle geprüft).
- **In diesem Plan** keine Alembic-Migration und keine `models.py`-Änderung (beides gehört zu
  Plan 04), kein Deploy, kein Push.
- Die zurückgestellten Entscheidungen stehen im DIALOG, nicht nur im SUMMARY.
</success_criteria>

<offene_annahmen>
1. **Der reale `ck_call_events_event_type` enthält 9 Werte** (RESEARCH-Beleg über Migration 0004).
   Der R3-Wächter verifiziert das ab jetzt bei **jedem** Deploy-Gate-Lauf statt einmalig.
2. **`pg_catalog` statt `information_schema`:** bewusste Abweichung von der Review-Formulierung.
   `information_schema.check_constraints` ist privilegien-maskiert und könnte der Rolle `nerve_app`
   eine leere Menge liefern (False-Negative). `pg_get_constraintdef` trägt dieselbe Information und
   ist nicht maskiert; der Test behandelt „Constraint nicht gefunden" ausdrücklich als Fehler.
3. **Zwischen Welle 2 und Plan 04 ist der PG-Test rot** (ORM 7 vs. DB 9). Ungefährlich: lokal
   skippt er, und in diesem Fenster wird nicht deployt. Plan 04 zieht beide Seiten gleichzeitig
   auf die neuen Werte.
2. **`tests/` ist bewusst nicht im Wortschatz-Sperre-Scope.** Die Absenz-Assertion
   `test_init_session_state_has_no_legacy_keys` muss die alten Namen nennen dürfen, sonst
   könnte niemand mehr prüfen, dass sie weg sind.
3. **Der Ein-Schreiber-Wächter sieht kein `setattr`/keinen dynamischen Schlüssel.** Bewusst
   benannte Grenze — er fängt das reale Muster, nicht jede denkbare Umgehung.
</offene_annahmen>

<output>
Nach Abschluss: `.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-03-SUMMARY.md`
mit: den beiden Falsifizierbarkeits-Rot-Läufen (Task 1 + Task 2) verbatim, dem Skip-Nachweis des
R3-Wächters (`2 passed, 1 skipped`), der geprüften Anker-Tabelle aus `<anti_gaming_warnung>`,
und dem Vermerk zur Schild-Pflicht (kein Nachzug nötig, Punkt 23).

Danach ANHALTEN. **Es folgt Plan 04 (Welle 3)** — erst danach Übergabe an Claudian für
Migration + Deploy-Gate + Test-Anruf.
</output>
