---
phase: 08.23.2.COUNTERPART
plan: 03
type: execute
wave: 2
depends_on: ["08.23.2.COUNTERPART-01", "08.23.2.COUNTERPART-02"]
files_modified:
  - tests/test_counterpart_vocabulary_guard.py
  - tests/test_counterpart_single_writer.py
  - database/models.py
  - .planning/DIALOG-GSD-CLAUDIAN.md
autonomous: false
requirements: [COUNTERPART-W3]
complexity: 🟢

must_haves:
  truths:
    - "Der alte Wortschatz kann nicht zurückkommen: ein Test macht jeden neuen Treffer von `current_mode`/`contact_category`/`contactCategory` in services/, static/, routes/ rot."
    - "`counterpart` hat genau zwei Schreiber: den Init-Default und den Toggle-Handler — durch einen AST-Sweep erzwungen."
    - "Die ORM-Deklaration des `call_events`-CHECK-Constraints stimmt wieder mit der echten Datenbank überein."
  artifacts:
    - path: "tests/test_counterpart_vocabulary_guard.py"
      provides: "Wächter 2 — Wortschatz-Sperre"
    - path: "tests/test_counterpart_single_writer.py"
      provides: "Wächter 3 — Ein-Schreiber-Sperre (AST)"
    - path: "database/models.py"
      provides: "CHECK-Deklaration mit allen 9 Event-Typen"
      contains: "mode_initial"
  key_links:
    - from: "tests/test_counterpart_single_writer.py"
      to: "services/deepgram_service.py (handle_toggle_counterpart)"
      via: "AST-Whitelist (Datei, Funktion)"
      pattern: "handle_toggle_counterpart"
---

<objective>
Die Ratsche einrasten lassen. Zwei statische Wächter sorgen dafür, dass der abgerissene
Zustand nicht durch die Hintertür zurückkommt — plus ein Ein-Zeilen-Nachzug an einer
Deklaration, die seit Migration 0004 von der echten Datenbank abweicht.

Purpose: Ein Umbau ohne Ratsche ist ein Pflaster mit besserem Namen. Die Wächter sind der
Grund, warum dieser Bug nicht ein sechstes Mal wiederkommt.

Output: Wächter 2 (Wortschatz-Sperre), Wächter 3 (Ein-Schreiber-Sperre), CHECK-Nachzug,
und die zurückgestellten Entscheidungen schriftlich im DIALOG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-CONTEXT.md
@.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-RESEARCH.md
@.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-01-PLAN.md
@CLAUDE.md
</context>

<anti_gaming_warnung>
## Der Wächter darf nicht durch Aufräumen grün werden

Ein grep-basiertes Abnahme-Kriterium lädt dazu ein, die Treffer wegzuräumen statt die Sache zu
lösen — insbesondere Schutz-Kommentare, die die verbotenen Wörter nur *erwähnen*.

**Regel für diesen Plan:** Die Wortschatz-Sperre wird **nicht** dadurch grün gemacht, dass
Kommentare gelöscht werden. Alle betroffenen Kommentare wurden in Welle 1 bewusst
**umformuliert** (Plan 01 Task 3 Punkt C, Plan 02 Task 1 Punkt B). Dieser Plan prüft deshalb
zusätzlich **positive Anker**: die Schutz-Kommentare müssen weiterhin da sein.

| Anker (muss existieren) | Datei | Prüfung |
|---|---|---|
| `steht bewusst im STABILEN Block` | `services/prompt_pipeline.py` | `grep -cF` → 1 |
| `ZWEI ACHSEN` | `services/mode_strategy.py` | `grep -cF` → 1 |
| `Hoerbarkeits-Achse` | `services/mode_strategy.py` | `grep -cF` → ≥ 2 |
| `reason='mode_downgrade'` | `services/deepgram_service.py` | `grep -cF` → 1 |
| `no_active_session` | `services/deepgram_service.py` | `grep -cF` → 1 |
| `nicht reentrant` | `services/live_session.py`, `services/claude_service.py` | `grep -cF` → je ≥ 1 |

Wer einen dieser Anker löscht, um einen grep grün zu bekommen, hat den Plan gebrochen.
</anti_gaming_warnung>

<tasks>

<task type="auto">
  <name>Task 1: Wächter 2 — Wortschatz-Sperre als Test</name>

  <read_first>
    - `tests/test_no_live_global_state.py` Zeilen 1-60 und 265-320 — das hauseigene Muster für
      statische Sweep-Wächter (Doc-Block mit der „kein Source-Presence-False-Green"-Begründung,
      `Path`-Iteration, Verstoss-Liste mit `Datei:Zeile`, aussagekräftige Fehlermeldung).
    - `services/live_session.py`, `services/deepgram_service.py`, `services/prompt_pipeline.py`,
      `services/mode_strategy.py`, `routes/app_routes.py`, `static/pip-launcher.js` — jeweils nur
      per `grep -n "current_mode\|contact_category\|contactCategory"` prüfen, dass Welle 1
      wirklich sauber ist. **Nicht ganze Dateien lesen.**
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
Neue Datei, kein Insert. Umschliessender Kontext ist pytest; keine Fixtures, kein State,
kein Netz, keine DB. Ausgangspfad: `assert` oder Rückkehr.

### Schicht 2 — Funktions-Skelett
```
test_forbidden_vocabulary_absent()
  → fuer jedes Verzeichnis in (services/, routes/, static/):
       fuer jede Datei mit Endung .py/.js (rekursiv, __pycache__ ausgeschlossen):
           **[hier der Eingriff: Zeilen gegen die drei verbotenen Woerter pruefen]**
  → Verstoss-Liste sammeln (Datei:Zeile:Wort)
  → assert not violations, <Meldung mit allen Treffern>
```
Ersetzt wird: nichts. Unberührt: alles.

### Schicht 3 — Cross-File-Awareness (grep-belegt)

| Symbol | Leser VOR dem Plan | Leser NACH dem Plan |
|---|---|---|
| `current_mode` | nach Welle 1: **0** in services/ static/ routes/ | dieser Wächter (nur als Suchmuster, gebaut aus Fragmenten) |
| `contact_category` | nach Welle 1: **0** ebenda | dito |
| `contactCategory` | nach Welle 1: **0** ebenda | dito |
| `tests/` | enthält die Wörter noch in `test_init_session_state_has_no_legacy_keys` (Absenz-Assertion) | **bewusst NICHT im Sweep-Scope** — die Absenz-Assertion muss die Wörter nennen dürfen |

### Schicht 4 — Edge-Case- und RACE-Analyse
1. **Selbst-Treffer:** Der Wächter enthält die verbotenen Wörter selbst. Er darf sich nicht
   selbst finden → er scannt nur `services/`, `routes/`, `static/`, **nicht** `tests/`.
   Zusätzlich werden die Suchmuster aus Fragmenten zusammengesetzt
   (`'contact' + '_category'`), damit auch ein versehentlich erweiterter Scope die Datei nicht
   rot macht.
2. **`__pycache__`:** `.pyc`-Dateien enthalten die Wörter als Bytes (heute belegt: 4 Treffer).
   Der Sweep filtert auf `.py`/`.js` und überspringt jedes Verzeichnis namens `__pycache__`.
3. **Minified/Fremd-Assets in `static/`:** falls dort Fremdbibliotheken liegen, könnten sie
   zufällig `contactCategory` enthalten. Der Sweep meldet Datei:Zeile — bei einem Fremd-Treffer
   wird die Datei explizit in `_IGNORED_FILES` aufgenommen (mit Begründungs-Kommentar),
   **nicht** das Muster aufgeweicht.
4. **Encoding:** `read_text(encoding='utf-8', errors='ignore')` — `nerve.css`/JS enthalten
   Umlaute; ein UnicodeDecodeError darf den Wächter nicht kippen.
5. **Kein Source-Presence-False-Green:** Der Test prüft ein **verbotenes** Muster, nicht die
   Existenz von erwünschtem Code. Kommt das Muster zurück, wird er rot; verschwindet Code,
   wird er grüner — nie umgekehrt. Das ist der dokumentierte Regex-Grenzfall der CLAUDE.md
   Test-Qualitäts-Regel; die Begründung gehört in den Modul-Docstring.
  </punkt14_audit>

  <files>tests/test_counterpart_vocabulary_guard.py</files>

  <action>
Neue Datei `tests/test_counterpart_vocabulary_guard.py`:

```python
"""Waechter 2 (Wortschatz-Sperre) — Phase 08.23.2.COUNTERPART.

Der Gespraechspartner hatte drei Namen an sieben Orten. Nach dem Abriss heisst er
ueberall 'counterpart'. Dieser Sweep macht jede Rueckkehr des alten Wortschatzes in
services/, routes/ und static/ rot.

Scope-Entscheidung: tests/ ist NICHT im Sweep. Die Absenz-Assertion in
test_live_session_gatekeeper.py muss die alten Schluesselnamen nennen duerfen.

KEIN Source-Presence-False-Green (CLAUDE.md Test-Qualitaets-Regel): geprueft wird ein
VERBOTENES Muster, nicht die Existenz erwuenschten Codes. Ein False-Green kann nicht
entstehen — kehrt das Muster zurueck, roetet es den Test. Dokumentierter Regex-Grenzfall;
eine Mock-bare Funktion, die 'kein Code nutzt dieses Wort mehr' beweisen koennte,
existiert nicht.
"""
from pathlib import Path

_ROOT = Path(__file__).parent.parent
_SCAN_DIRS = ('services', 'routes', 'static')
_SCAN_SUFFIXES = ('.py', '.js')

# Fragmentiert zusammengesetzt, damit diese Datei sich nicht selbst findet,
# falls der Scope je erweitert wird.
_FORBIDDEN = (
    'current' + '_mode',
    'contact' + '_category',
    'contact' + 'Category',
)

# Fremd-Assets, die zufaellig treffen wuerden. Leer lassen, solange nichts noetig ist —
# JEDER Eintrag braucht eine Begruendung im Kommentar.
_IGNORED_FILES: frozenset = frozenset()


def _iter_source_files():
    for d in _SCAN_DIRS:
        base = _ROOT / d
        if not base.is_dir():
            continue
        for path in base.rglob('*'):
            if '__pycache__' in path.parts:
                continue
            if path.suffix not in _SCAN_SUFFIXES or not path.is_file():
                continue
            if path.relative_to(_ROOT).as_posix() in _IGNORED_FILES:
                continue
            yield path


def test_forbidden_counterpart_vocabulary_absent():
    violations = []
    for path in _iter_source_files():
        rel = path.relative_to(_ROOT).as_posix()
        text = path.read_text(encoding='utf-8', errors='ignore')
        for line_no, line in enumerate(text.splitlines(), start=1):
            for word in _FORBIDDEN:
                if word in line:
                    violations.append(f'{rel}:{line_no}  [{word}]  {line.strip()[:100]}')
    assert not violations, (
        'Alter Gespraechspartner-Wortschatz zurueck in services/routes/static '
        f'({len(violations)} Treffer):\n  ' + '\n  '.join(violations)
        + '\n\nPhase 08.23.2.COUNTERPART: der Gespraechspartner heisst counterpart '
          "('gatekeeper' | 'decision_maker'), die Anruf-Art heisst call_type "
          "('cold_call' | 'meeting'). Keine Wort-Ueberlappung."
    )


def test_guard_actually_scans_something():
    """Falsifizierbarkeit: der Sweep muss echte Dateien sehen.

    Ohne diesen Test waere ein kaputter Pfad ein stiller Gruen-Macher (0 Dateien
    gescannt = 0 Verstoesse = gruen).
    """
    files = list(_iter_source_files())
    assert len(files) >= 20, f'Sweep sieht nur {len(files)} Dateien — Pfad-Logik kaputt?'
    names = {p.name for p in files}
    for expected in ('live_session.py', 'deepgram_service.py', 'pip-launcher.js'):
        assert expected in names, f'{expected} nicht im Sweep-Scope'
```

Ein Commit: `test(08.23.2.COUNTERPART): Waechter 2 — Wortschatz-Sperre`
  </action>

  <verify>
    <automated>python -m pytest tests/test_counterpart_vocabulary_guard.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_counterpart_vocabulary_guard.py -q` → **2 passed**.
    - `grep -rn "current_mode\|contact_category\|contactCategory" services/ static/ routes/ --include=*.py --include=*.js` → **0 Treffer**.
    - Alle positiven Anker aus `<anti_gaming_warnung>` treffen mit den dort genannten Zahlen zu
      (alle sechs Zeilen einzeln prüfen und die Ergebnisse ins SUMMARY schreiben).
    - **Falsifizierbarkeits-Nachweis (Pflicht, im SUMMARY dokumentieren):** temporär
      `# contact_category` als Kommentar in `services/mode_strategy.py` einfügen,
      `python -m pytest tests/test_counterpart_vocabulary_guard.py -q` laufen lassen →
      muss **ROT** mit `services/mode_strategy.py:<n>` sein; Ausgabe verbatim ins SUMMARY,
      dann die Zeile wieder **entfernen** und erneut grün laufen lassen.
      `git status --porcelain services/mode_strategy.py` → **leer** vor dem Commit.
    - `git log -1 --stat` zeigt genau 1 Datei.
  </acceptance_criteria>

  <done>Die Wortschatz-Sperre ist ein laufender Test, nicht ein einmaliger grep — und sie ist nachweislich falsifizierbar.</done>
</task>

<task type="auto">
  <name>Task 2: Wächter 3 — Ein-Schreiber-Sperre (AST)</name>

  <read_first>
    - `tests/test_no_live_global_state.py` Zeilen 240-320 — der AST-Sweep (`ast.walk`,
      `ast.Assign`, `ast.Subscript`, Verstoss-Liste mit Datei:Zeile).
    - `services/deepgram_service.py` — nur die Stelle `grep -n "st\['counterpart'\]"`
      (die einzige erlaubte Zuweisung).
    - `services/live_session.py` — nur `grep -n "'counterpart'" ` (der Init-Default als
      Dict-Literal-Schlüssel, **keine** Subscript-Zuweisung).
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
Neue Datei, kein Insert. Reiner statischer Sweep, keine Fixtures, kein State.

### Schicht 2 — Funktions-Skelett
```
test_counterpart_has_exactly_one_assigning_writer()
  → fuer jede .py-Datei in services/ + routes/ (ohne __pycache__):
       ast.parse(quelle)
       fuer jede FunctionDef: fuer jeden Assign-Knoten darin:
           **[hier der Eingriff: Target ist Subscript mit Slice-Konstante 'counterpart'?]**
               → (relpath, funktionsname, zeile) sammeln
  → gefundene (Datei, Funktion)-Paare gegen _ALLOWED_WRITERS vergleichen
  → assert gleich

test_init_seeds_counterpart()          # Laufzeit, nicht statisch
  → init_session_state(sid, 1, 1) aufrufen
  → assert _session_state[sid]['state']['counterpart'] == 'gatekeeper'
```
Ersetzt wird: nichts.

### Schicht 3 — Cross-File-Awareness (grep-belegt)

| Symbol | Schreiber VOR dem Plan | Schreiber NACH dem Plan |
|---|---|---|
| `state['counterpart']` (Dict-Literal-Seed) | `services/live_session.py` `init_session_state` (Welle 1) | unverändert — genau einer |
| `st['counterpart'] = …` (Subscript-Assign) | `services/deepgram_service.py` `handle_toggle_counterpart` (Welle 1) | unverändert — genau einer |
| Payload-Dicts mit Schlüssel `'counterpart'` | `live_session.create_call_for_sid`, `deepgram_service` (2 Emits) | **keine State-Schreiber** — der AST-Sweep sieht sie korrekt nicht (Dict-Literal ≠ Subscript-Assign) |

### Schicht 4 — Edge-Case- und RACE-Analyse
1. **Der Init-Default ist ein Dict-Literal**, keine Subscript-Zuweisung — der AST-Sweep findet
   ihn nicht. Deshalb der zweite, **Laufzeit**-Test: er beweist die Existenz des Seeds durch
   Aufruf, nicht durch Textsuche.
2. **Zuweisung ausserhalb einer Funktion** (Modul-Ebene) würde der FunctionDef-Iteration
   entgehen → zusätzlich über den ganzen Modul-AST laufen und Treffer ausserhalb jeder
   Funktion als Verstoss mit Funktionsname `'<modul-ebene>'` melden.
3. **Verschachtelte Closures** (`register_audio_handlers` → `handle_toggle_counterpart`): der
   Sweep muss die **innerste** umschliessende Funktion melden, sonst passt der
   Whitelist-Eintrag nicht. Umsetzung: über `ast.walk` alle `FunctionDef` sammeln und den
   Assign-Knoten der Funktion mit der **grössten** `lineno ≤ assign.lineno` und
   `end_lineno ≥ assign.lineno` zuordnen.
4. **Erweiterte Zuweisungen** (`st['counterpart'] += …`) sind `ast.AugAssign` — ebenfalls
   erfassen, sonst gäbe es ein Schlupfloch.
5. **`setattr`/dynamische Keys** kann kein statischer Sweep sehen. Bekannte Grenze, im
   Docstring benennen (wie `test_api_rate_coverage.py` seine Grenze benennt) — nicht
   überversprechen.
6. **Kein Source-Presence-False-Green:** verbotenes Muster + ein echter Laufzeit-Test.
  </punkt14_audit>

  <files>tests/test_counterpart_single_writer.py</files>

  <action>
Neue Datei `tests/test_counterpart_single_writer.py`:

```python
"""Waechter 3 (Ein-Schreiber-Sperre) — Phase 08.23.2.COUNTERPART.

'counterpart' darf nur an ZWEI Stellen entstehen:
  1. Init-Default in live_session.init_session_state (Dict-Literal beim Anlegen des States)
  2. handle_toggle_counterpart in deepgram_service (der einzige Umschalter)

Alles andere waere ein dritter Schreiber — genau die Konstellation, aus der der
urspruengliche Bug entstanden ist (Zustand an sieben Orten).

Grenze dieses Waechters (bewusst benannt, nicht ueberversprochen): ein statischer Sweep
sieht kein setattr() und keinen dynamisch gebauten Schluessel. Er faengt das reale
Muster (st['counterpart'] = ...), nicht jede denkbare Umgehung.

KEIN Source-Presence-False-Green: Test 1 prueft ein VERBOTENES Muster (Schreiber ausserhalb
der Whitelist), Test 2 ruft die echte Funktion auf und assertiert die State-Mutation.
"""
import ast
from pathlib import Path

_ROOT = Path(__file__).parent.parent
_SCAN_DIRS = ('services', 'routes')
_KEY = 'counter' + 'part'

# (relativer Pfad, umschliessende Funktion) — die EINZIGEN erlaubten Subscript-Schreiber.
_ALLOWED_WRITERS = frozenset({
    ('services/deepgram_service.py', 'handle_toggle_counterpart'),
})


def _enclosing_function(func_nodes, lineno):
    best = None
    for fn in func_nodes:
        if fn.lineno <= lineno <= (fn.end_lineno or fn.lineno):
            if best is None or fn.lineno > best.lineno:
                best = fn
    return best.name if best is not None else '<modul-ebene>'


def _iter_py_files():
    for d in _SCAN_DIRS:
        base = _ROOT / d
        if not base.is_dir():
            continue
        for path in base.rglob('*.py'):
            if '__pycache__' in path.parts:
                continue
            yield path


def _collect_writers():
    found = set()
    details = []
    for path in _iter_py_files():
        rel = path.relative_to(_ROOT).as_posix()
        try:
            tree = ast.parse(path.read_text(encoding='utf-8', errors='ignore'))
        except SyntaxError:
            continue
        funcs = [n for n in ast.walk(tree)
                 if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
        for node in ast.walk(tree):
            targets = []
            if isinstance(node, ast.Assign):
                targets = node.targets
            elif isinstance(node, ast.AugAssign):
                targets = [node.target]
            for tgt in targets:
                if (isinstance(tgt, ast.Subscript)
                        and isinstance(tgt.slice, ast.Constant)
                        and tgt.slice.value == _KEY):
                    fname = _enclosing_function(funcs, node.lineno)
                    found.add((rel, fname))
                    details.append(f'{rel}:{node.lineno}  in {fname}()')
    return found, details


def test_counterpart_has_exactly_one_assigning_writer():
    found, details = _collect_writers()
    extra = found - _ALLOWED_WRITERS
    missing = _ALLOWED_WRITERS - found
    assert not extra, (
        'Neuer counterpart-Schreiber ausserhalb der Whitelist:\n  '
        + '\n  '.join(d for d in details if (d.split(':')[0],
                                             d.split(' in ')[-1].rstrip('()')) in extra)
        + '\n\nEin-Schreiber-Sperre (Phase 08.23.2.COUNTERPART): counterpart entsteht nur '
          'im Init-Default (live_session.init_session_state) und im Toggle-Handler.'
    )
    assert not missing, (
        f'Erwarteter counterpart-Schreiber fehlt: {sorted(missing)}. '
        'Gefunden wurde:\n  ' + '\n  '.join(details)
    )


def test_init_session_state_seeds_counterpart():
    """Laufzeit-Beleg fuer den zweiten erlaubten Schreiber (Dict-Literal, AST-unsichtbar)."""
    import services.live_session as ls
    sid = 'test-counterpart-single-writer-001'
    try:
        ls.init_session_state(sid, 1, 1)
        with ls._session_state_lock:
            st = ls._session_state[sid]['state']
        assert st['counterpart'] == 'gatekeeper', (
            f"Init-Default muss 'gatekeeper' sein, war: {st['counterpart']!r}")
    finally:
        with ls._session_state_lock:
            ls._session_state.pop(sid, None)


def test_guard_actually_scans_something():
    files = list(_iter_py_files())
    assert len(files) >= 15, f'Sweep sieht nur {len(files)} Dateien — Pfad-Logik kaputt?'
```

Ein Commit: `test(08.23.2.COUNTERPART): Waechter 3 — Ein-Schreiber-Sperre fuer counterpart`
  </action>

  <verify>
    <automated>python -m pytest tests/test_counterpart_single_writer.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_counterpart_single_writer.py -q` → **3 passed**.
    - `grep -cF "handle_toggle_counterpart" tests/test_counterpart_single_writer.py` → **1**
      (die Whitelist nennt die Funktion, nicht nur die Datei).
    - `grep -cF "ast.AugAssign" tests/test_counterpart_single_writer.py` → **1**
      (das `+=`-Schlupfloch ist zu).
    - **Falsifizierbarkeits-Nachweis (Pflicht, im SUMMARY dokumentieren):** temporär in
      `services/live_session.py` in einer beliebigen Funktion die Zeile
      `_session_state[sid]['state']['counterpart'] = 'gatekeeper'` einfügen →
      `python -m pytest tests/test_counterpart_single_writer.py -q` muss **ROT** werden und
      `services/live_session.py` als unerlaubten Schreiber melden; Ausgabe verbatim ins SUMMARY,
      Zeile wieder entfernen, erneut grün laufen lassen.
      `git status --porcelain services/live_session.py` → **leer** vor dem Commit.
    - `git log -1 --stat` zeigt genau 1 Datei.
  </acceptance_criteria>

  <done>`counterpart` hat nachweislich genau zwei Schreiber, und ein dritter würde den Deploy-Gate rot machen.</done>
</task>

<task type="auto">
  <name>Task 3: CHECK-Deklaration nachziehen + zurückgestellte Entscheidungen schriftlich festhalten</name>

  <read_first>
    - `database/models.py` Zeilen 774-790 (`CallEvent`, insbesondere die `CheckConstraint`-Zeile
      `:785` und der Tabellen-Kommentar `:789`).
    - `alembic/versions/0004_add_mode_events_to_call_events.py` (vollständig, ~30 Zeilen) —
      die Migration, die `mode_switch`/`mode_initial` in der echten DB ergänzt hat.
    - Der `inspect.sh constraints call_events`-Output aus Plan 01 (steht im
      `08.23.2.COUNTERPART-01-SUMMARY.md`). **Wenn er dort fehlt: jetzt selbst ziehen**, siehe
      Action Schritt 1.
    - `.planning/DIALOG-GSD-CLAUDIAN.md` — nur das **Ende** (letzte ~40 Zeilen), um den
      Schreibstil und das Format der bestehenden Einträge zu treffen.
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
`database/models.py:784-790` ist ein `__table_args__`-Tupel: eine `CheckConstraint`, drei
`Index`, ein Kommentar-Dict. Kein Kontrollfluss, kein try/except. Wirkung der Änderung: **nur
auf `create_all`** (also auf die SQLite-Test-Pfade) — die echte Postgres-Datenbank ist von
`models.py` unberührt, dort regiert Migration 0004.

### Schicht 2 — Funktions-Skelett
```
class CallEvent(Base):
    __table_args__ = (
        **[hier der Eingriff: die 7-Werte-Liste des CHECK wird auf 9 ergaenzt]**
        Index(...) x3,                       [unberuehrt]
        {'comment': '...'},                  [unberuehrt]
    )
```
Ersetzt wird: die Wertliste innerhalb des `CheckConstraint`-Strings. Unberührt: Constraint-Name
`ck_call_events_event_type`, alle Indizes, das Tabellen-Schild, jede andere Klasse.

### Schicht 3 — Cross-File-Awareness (grep-belegt)

| Symbol | Leser VOR dem Plan | Leser NACH dem Plan |
|---|---|---|
| `ck_call_events_event_type` (ORM-Deklaration) | `Base.metadata.create_all` (SQLite-Testpfade) | unverändert dieselben |
| `ck_call_events_event_type` (echte DB) | Postgres, gesetzt durch Migration 0004 (**9 Werte**) | **unverändert** — dieser Task fasst die DB NICHT an |
| `event_type='mode_initial'` | `services/live_session.py` | unverändert |
| `event_type='mode_switch'` | `services/deepgram_service.py` | unverändert |

### Schicht 4 — Edge-Case- und RACE-Analyse
1. **Keine Migration, kein Deploy, kein `alembic upgrade`.** Dieser Task ändert eine
   **Deklaration**, damit sie die Realität wieder beschreibt. Wer hier eine Migration schreibt,
   hat den Plan gebrochen.
2. **Expand/Contract-Lehre (AUTH-2):** `models.py` ist der ORM-**Leser** der DB. Die
   Deklaration darf der echten DB nachziehen, nie vorauseilen. Deshalb ist der
   `inspect.sh constraints`-Beleg Vorbedingung.
3. **Wenn der reale Constraint die zwei Werte NICHT enthält:** dann wäre die Deklaration
   korrekt und die Prod-DB abweichend — in dem Fall **STOP**, `models.py` nicht anfassen,
   Befund in den DIALOG schreiben.
4. **SQLite-Nebenwirkung:** Tests, die `call_events`-Zeilen mit `mode_switch`/`mode_initial`
   gegen eine per `create_all` erzeugte SQLite-DB schreiben, würden heute am CHECK scheitern.
   Der Nachzug schliesst diese latente Falle — deshalb ist er fachlich zugehörig und kein
   Refactor nebenbei.
5. **Schild-Pflicht (Punkt 23):** keine neue Spalte, kein neuer Leser/Schreiber-Pfad → der
   Tabellen-Kommentar `:789` bleibt korrekt. Explizit im SUMMARY vermerken.
  </punkt14_audit>

  <files>database/models.py, .planning/DIALOG-GSD-CLAUDIAN.md</files>

  <action>
**Schritt 1 — Beleg (Pflicht, VOR der Änderung):**
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/inspect.sh constraints call_events'
```
Output verbatim ins SUMMARY. **Nur wenn** der reale `ck_call_events_event_type` sowohl
`mode_switch` als auch `mode_initial` enthält, weiter mit Schritt 2. Sonst: STOP, Befund in
den DIALOG (Schritt 3), `models.py` unangetastet lassen.

**Schritt 2 — `database/models.py`,** die `CheckConstraint`-Zeile ersetzen durch:
```python
        # Werte-Liste gleichgezogen mit der echten DB (Migration 0004 ergaenzte
        # mode_switch + mode_initial; die ORM-Deklaration hinkte seitdem hinterher).
        # Reine Deklaration — wirkt nur auf create_all (SQLite-Testpfade), NICHT auf
        # Postgres. Phase 08.23.2.COUNTERPART.
        CheckConstraint(
            "event_type IN ('transcript_chunk', 'suggestion_shown', 'reaction', "
            "'phase_change', 'audio_health', 'objection_detected', 'consent_optin', "
            "'mode_switch', 'mode_initial')",
            name='ck_call_events_event_type'),
```
**Nichts anderes** in `database/models.py` anfassen.

**Schritt 3 — `.planning/DIALOG-GSD-CLAUDIAN.md` ans Ende anhängen** (Format der bestehenden
Einträge übernehmen, KEIN interaktives Menü — André liest vom Handy):

```markdown
## 08.23.2.COUNTERPART — bewusst zurückgestellt (2026-07-28, Planung)

Der Umbau ist durch; drei Dinge wurden **absichtlich nicht** gebaut. Jeweils mit Begründung,
damit später niemand rätselt:

**1. Umbenennung der DB-Events `mode_initial`/`mode_switch` → `counterpart_initial`/`counterpart_switch`.**
Der Punkt-20-grep hat 0 Leser belegt, die Umbenennung wäre also erlaubt. Sie braucht aber eine
Alembic-Migration (CHECK-Constraint) plus eine Entscheidung über **113 echte Prod-Zeilen**
(72 + 41) mit den alten Namen: mitziehen, stehenlassen oder als historisch markieren?
In dieser Phase wurde nur die **Payload** sauber getrennt (`call_type` + `counterpart` als
getrennte Felder) — der Kern des Problems. Die Umbenennung ist reine Kosmetik an einem Feld,
das niemand liest. **Vorschlag: als eigene Mini-Phase, wenn ohnehin eine Migration ansteht.**

**2. Der Toggle-Knopf wird bei `call_type == 'meeting'` weiterhin NICHT ausgeblendet.**
Der CSS-Selektor `[data-mode="meeting"]`, der das tun sollte, war seit seiner Entstehung tot
(kein Schreiber setzte je den Wert `meeting`). Er wurde beim Attribut-Umbau entfernt, statt
einen neuen Signalweg zu bauen — dafür gibt es keine Entscheidung und es wäre Refactor
nebenbei. **Frage an André: soll der Gesprächspartner-Knopf im Meeting-Modus verschwinden?**
Wenn ja: kleiner eigener Schritt (Server schickt `call_type` bereits im `counterpart_changed`-Echo
mit, der Browser müsste ihn nur auswerten).

**3. `current_phase_name` bleibt modus-blind.** `claude_service.py` schreibt den Phasen-Namen
immer aus der Cold-Call-Labelliste (`_PHASE_NAMES`), unabhängig vom gewählten Phasenmodell.
Das Phasenmodell selbst wählt jetzt korrekt nach `(call_type, counterpart)` — nur dieses eine
Anzeige-Label hinkt nach. Eigener Befund aus der Kartierung, nicht Teil dieses Umbaus.
```

Ein Commit: `chore(08.23.2.COUNTERPART): CHECK-Deklaration an die echte DB angeglichen + zurueckgestellte Entscheidungen dokumentiert`
  </action>

  <verify>
    <automated>python -c "import database.models" && python -m pytest tests/test_mode_initial_db.py tests/test_mode_switch_event.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "'mode_switch', 'mode_initial')" database/models.py` → **1**.
    - `grep -cF "ck_call_events_event_type" database/models.py` → **1** (Constraint-Name
      unverändert, nicht versehentlich dupliziert).
    - `git diff HEAD~1 -- database/models.py | grep -c "^[+-]"` → **≤ 14**
      (nur der CheckConstraint-Block + Kommentar; kein Kollateral-Diff).
    - `ls alembic/versions/ | wc -l` **unverändert** gegenüber Plan-Start — **keine neue
      Migration** wurde angelegt.
    - `git diff HEAD~1 --stat | grep -c alembic` → **0**.
    - `grep -cF "08.23.2.COUNTERPART — bewusst zurückgestellt" .planning/DIALOG-GSD-CLAUDIAN.md` → **1**.
    - `grep -cF "113 echte Prod-Zeilen" .planning/DIALOG-GSD-CLAUDIAN.md` → **1**.
    - `python -c "import database.models"` → exit 0.
    - Der `inspect.sh constraints call_events`-Output steht verbatim im SUMMARY.
  </acceptance_criteria>

  <done>Die ORM-Deklaration beschreibt wieder die echte Datenbank, keine Migration wurde angelegt, und die drei zurückgestellten Entscheidungen stehen schriftlich im DIALOG statt in einem Kopf.</done>
</task>

</tasks>

<verification>
## Phasen-Abschluss-Prüfung (lokal = Vorabsignal, NICHT Abnahme)

```bash
# Die drei Waechter
python -m pytest tests/test_counterpart_toggle_roundtrip.py \
                 tests/test_counterpart_vocabulary_guard.py \
                 tests/test_counterpart_single_writer.py -q
# Die mitgezogenen Bestands-Tests
python -m pytest tests/test_mode_switch_event.py tests/test_mode_initial_db.py \
                 tests/test_live_session_gatekeeper.py tests/test_session_state_phase_c.py \
                 tests/test_phase_classifier.py tests/test_counterpart_consumers.py \
                 tests/test_h1_latte.py tests/test_callid_race_close.py \
                 tests/test_mode_registry.py tests/test_no_live_global_state.py -q
# Wortschatz-Sperre als roher grep (muss 0 sein)
grep -rn "current_mode\|contact_category\|contactCategory" services/ static/ routes/ \
     --include=*.py --include=*.js
node --check static/pip-launcher.js
python -c "import database.models"
```

**ABNAHME (HART, CLAUDE.md):** Kein lokales pytest ist die Abnahme. Die Abnahme ist das
`bash deploy.sh production`-Gate (läuft auf dem Prod-Server vor dem Restart; bei Test-Fehler
kein Restart, alter Code bleibt live) plus ein Test-Anruf im Browser mit dem Test-User:
Knopf klicken → Anzeige kippt auf `Entscheider` → nochmal klicken → kippt zurück auf
`Sekretär` → viermal hintereinander, ohne zu verklemmen.

**KEIN Deploy, KEIN `git push`, KEINE Migration-Ausführung durch den Executor.**
`autonomous: false` — nach diesem Plan ANHALTEN und an Claudian übergeben.
</verification>

<success_criteria>
- Alle drei Wächter laufen und sind einzeln als falsifizierbar nachgewiesen (je ein
  dokumentierter Rot-Lauf).
- `grep -rn "current_mode\|contact_category\|contactCategory" services/ static/ routes/` → 0.
- Kein Schutz-Kommentar wurde geopfert, um einen grep grün zu bekommen (Anker-Tabelle geprüft).
- Keine neue Alembic-Migration, kein Deploy, kein Push.
- Die zurückgestellten Entscheidungen stehen im DIALOG, nicht nur im SUMMARY.
</success_criteria>

<offene_annahmen>
1. **Der reale `ck_call_events_event_type` enthält 9 Werte** (RESEARCH-Beleg über
   Migration 0004). Task 3 verifiziert das per `inspect.sh`, bevor er die Deklaration ändert.
   Falls nicht: STOP statt Anpassen.
2. **`tests/` ist bewusst nicht im Wortschatz-Sperre-Scope.** Die Absenz-Assertion
   `test_init_session_state_has_no_legacy_keys` muss die alten Namen nennen dürfen, sonst
   könnte niemand mehr prüfen, dass sie weg sind.
3. **Der Ein-Schreiber-Wächter sieht kein `setattr`/keinen dynamischen Schlüssel.** Bewusst
   benannte Grenze — er fängt das reale Muster, nicht jede denkbare Umgehung.
</offene_annahmen>

<output>
Nach Abschluss: `.planning/phases/08.23.2.COUNTERPART-gespraechspartner-umbau/08.23.2.COUNTERPART-03-SUMMARY.md`
mit: den beiden Falsifizierbarkeits-Rot-Läufen (Task 1 + Task 2) verbatim, dem
`inspect.sh constraints call_events`-Output, der geprüften Anker-Tabelle aus
`<anti_gaming_warnung>`, und dem Vermerk zur Schild-Pflicht (kein Nachzug nötig, Punkt 23).

Danach ANHALTEN. Übergabe an Claudian: Deploy-Gate + Test-Anruf.
</output>
