---
phase: 08.19.4
plan: "04"
subsystem: routes/app_routes, routes/profiles, services/live_session, services/claude_service, services/prompt_pipeline, tests
tags: [dsgvo, per-sid, session-scoping, d-05, route-migration, compat-cleanup, tests]
dependency_graph:
  requires:
    - 08.19.4-01
    - 08.19.4-02
    - 08.19.4-03
  provides:
    - routes/app_routes.py: kein ls.set_active_profile / ls.get_active_profile mehr
    - routes/profiles.py: aktivieren schreibt nur noch in DB; loeschen bereinigt User.active_profile_id
    - services/live_session.py: set_active_profile / get_active_profile / _legacy_profile_* geloescht
    - tests/test_session_scoping.py: DSGVO-Pflicht-Tests (7 pass, 1 skipped)
  affects:
    - routes/app_routes.py
    - routes/profiles.py
    - services/live_session.py
    - services/claude_service.py
    - services/prompt_pipeline.py
tech_stack:
  added: []
  patterns:
    - D-05: DB ist Single Source of Truth fuer active_profile_id — flask_session convenience-only
    - Per-SID profile lookup via get_profile_for_sid(sid) — kein globaler Fallback mehr
    - DSGVO-Isolation-Tests: Runtime-Behavior-Assertions (keine Source-Presence-Checks)
key_files:
  created:
    - tests/test_session_scoping.py
  modified:
    - routes/app_routes.py
    - routes/profiles.py
    - services/live_session.py
    - services/claude_service.py
    - services/prompt_pipeline.py
decisions:
  - "D-05 abgeschlossen: HTTP-Routes schreiben active_profile_id nur noch in DB (User.active_profile_id); flask_session convenience-only"
  - "loeschen-Route setzt User.active_profile_id=None in DB beim Loeschen des aktiven Profils (T-08.19.4-10 Mitigation)"
  - "_active_profile_id() bevorzugt g.user.active_profile_id (DB) ueber Cookie-Wert"
  - "deprecated else-Zweige in claude_service._build_coaching_prompt und _qa_pipeline_dispatch entfernt"
  - "deprecated else-Zweig in prompt_pipeline.build_profile_context entfernt"
  - "set_active_profile() / get_active_profile() / _legacy_profile_* aus live_session.py geloescht"
metrics:
  duration: ~25min
  completed_date: "2026-04-29"
  tasks_completed: 2
  files_modified: 5
  files_created: 1
---

# Phase 08.19.4 Plan 04: D-05 Route-Migration + DSGVO-Tests Summary

D-05 Route-Caller-Migration abgeschlossen: alle `ls.set_active_profile()` und `ls.get_active_profile()` Aufrufe aus `routes/` entfernt; deprecated `_legacy_profile_*` Variablen und Compat-Wrapper aus `live_session.py` geloescht; 7 DSGVO-Pflicht-Tests in `tests/test_session_scoping.py` geschrieben und bestanden.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | D-05 route callers migrated, deprecated compat wrappers deleted | edca234 | routes/app_routes.py, routes/profiles.py, services/live_session.py, services/claude_service.py, services/prompt_pipeline.py |
| 2 | DSGVO-Pflicht-Tests SID-Isolation (TestPerSidProfileIsolation) | 560ba20 | tests/test_session_scoping.py |

## What Was Built

### Task 1 — Route-Migration + Compat-Wrapper-Deletion

**routes/app_routes.py (~97):**
- `ls.get_active_profile()` ersetzt durch DB-Lookup via `SessionLocal()` mit `_pid = flask_session.get(...) or getattr(g.user, 'active_profile_id', None)`
- Fallback `pdata = {}` wenn kein aktives Profil gesetzt

**routes/app_routes.py (~766, api_set_profile):**
- `ls.set_active_profile(p.name, daten, profile_id=p.id)` entfernt
- `u.active_profile_id = p.id; db.commit()` bleibt (autoritativer DB-Write)
- `flask_session['active_profile_id'] = p.id` bleibt mit D-05-Kommentar (convenience-only)
- `ls.load_learning_cards(g.user.id)` bleibt (unabhaengiges Feature)

**routes/profiles.py (_active_profile_id helper):**
- Bevorzugt jetzt `g.user.active_profile_id` (DB-geladen vom auth-Decorator) ueber Cookie-Wert
- Fallback auf `flask_session.get('active_profile_id')` wenn g.user nicht verfuegbar

**routes/profiles.py (aktivieren-Route):**
- `ls_mod.set_active_profile(p.name, daten, profile_id=p.id)` entfernt
- DB-Write bleibt, `flask_session` bleibt mit D-05-Kommentar

**routes/profiles.py (loeschen-Route, T-08.19.4-10 Mitigation):**
- Setzt `u.active_profile_id = None` in DB wenn geloeschtes Profil das aktive war
- Verhindert stale FK-Referenz in User-Tabelle

**services/claude_service.py:**
- `_build_coaching_prompt`: deprecated `else`-Zweig mit `get_active_profile()` entfernt; `('', {})` Fallback wenn `sid=None`
- `_qa_pipeline_dispatch`: ebenso deprecated `else`-Zweig entfernt

**services/prompt_pipeline.py:**
- `build_profile_context`: deprecated `else`-Zweig mit `get_active_profile()` entfernt; HTTP-Caller ohne SID erhalten leeren Profil-Kontext (korrekt — kein WS-Kontext)

**services/live_session.py:**
- `_legacy_profile_lock`, `_legacy_profile_data`, `_legacy_profile_name` geloescht
- `set_active_profile()` Funktion geloescht
- `get_active_profile()` Funktion geloescht
- Ersetzt durch einzeiligen Kommentar: `# set_active_profile / get_active_profile deleted Phase 08.19.4 D-04/D-05`

### Task 2 — DSGVO-Pflicht-Tests

`tests/test_session_scoping.py` mit Klasse `TestPerSidProfileIsolation` (7 Tests, alle gruen):

| Test | Was wird geprueft |
|------|-------------------|
| test_two_sids_independent_profiles | Zwei SIDs haben isolierte Profile — keine Cross-Contamination |
| test_disconnect_cleanup | pop_session_state raumt _per_sid_profile, _session_state, _per_sid_transcript auf |
| test_tier1_isolation | user_id und org_id pro SID isoliert (Tier-1 DSGVO-Keys) |
| test_unknown_sid_fallback | get_profile_for_sid gibt ('', {}) fuer unbekannte SID zurueck |
| test_init_session_state_required_keys | Alle 9 DSGVO-Pflicht-Keys werden bei init erstellt |
| test_concurrent_sid_writes_no_cross_contamination | Thread-Safety: 50 Writes pro SID ohne Interferenz |
| test_no_module_global_profile_on_import | active_profile_data/name Globals existieren nicht mehr |

Latency-Scaffold (`test_latency_scaffold_n_sessions`) als `@pytest.mark.skip` fuer manuelle post-launch Messung.

## Checkpoint: Human Verification Required

Task 3 ist ein `checkpoint:human-verify` Gate. Die automatischen Aufgaben (Tasks 1+2) sind abgeschlossen.

**Was zu verifizieren ist:**

1. Volle Test-Suite ohne Regressionen:
   ```
   python -m pytest tests/ -x -q
   ```

2. Nur DSGVO-Tests:
   ```
   python -m pytest tests/test_session_scoping.py -v
   ```
   Erwartet: 7 pass, 1 skipped

3. Keine Legacy-Caller mehr:
   ```
   grep -rn "get_active_profile()\|set_active_profile(" services/ routes/
   ```
   Erwartet: 0 Ergebnisse (nur Kommentare in live_session.py)

4. _load_initial_profile weg:
   ```
   grep -c "_load_initial_profile" app.py
   ```
   Erwartet: 0

5. transcript_buffer.append nur im Fallback-Zweig:
   ```
   grep -n "transcript_buffer.append" services/live_session.py
   ```
   Erwartet: genau 1 Treffer (else-Zweig in _flush_segment)

6. Flask-App startet ohne Fehler:
   ```
   python app.py
   ```
   Erwartet: kein "[Init] Aktives Profil geladen" Log mehr

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Functionality] Deprecated else-Zweige in claude_service und prompt_pipeline entfernt**
- **Found during:** Task 1 nach Route-Migration
- **Issue:** Plan-Spec verlangte "0 results" fuer `grep -rn "get_active_profile()" services/ routes/` — die deprecated else-Zweige in `_build_coaching_prompt`, `_qa_pipeline_dispatch` und `build_profile_context` wuerden diesen Check nicht bestehen. Plan 03 SUMMARY dokumentierte explizit: "deprecated else-Zweig bleibt fuer Plan 04 (loescht ihn)"
- **Fix:** Alle drei else-Zweige entfernt; HTTP-Caller ohne SID erhalten leere Profile (`('', {})`) — korrekt, da HTTP-Routes nach D-05 keinen WS-SID-Kontext haben
- **Files modified:** services/claude_service.py, services/prompt_pipeline.py
- **Commit:** edca234

## Verification Results

```
grep -rn "ls\.set_active_profile\|ls_mod\.set_active_profile" routes/
-> 2 Treffer (beide Kommentare: "# D-05: ... kein ls.set_active_profile() mehr")

grep -rn "ls\.get_active_profile()\b" routes/ services/
-> 0 Treffer

grep -rn "def set_active_profile\|def get_active_profile" services/live_session.py
-> 0 Treffer

grep -n "_legacy_profile" services/live_session.py
-> 0 Treffer

python -c "import py_compile; [py_compile.compile(f, doraise=True) for f in ['routes/app_routes.py', 'routes/profiles.py', 'services/live_session.py', 'services/claude_service.py', 'services/prompt_pipeline.py']]; print('OK')"
-> OK

python -m pytest tests/test_session_scoping.py -v
-> 7 passed, 1 skipped
```

## Known Stubs

Keine. Alle D-05 Ziele vollstaendig umgesetzt.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| T-08.19.4-10 mitigated | routes/profiles.py | loeschen-Route setzt User.active_profile_id=None in DB — stale FK-Referenz verhindert |
| T-08.19.4-11 accepted | routes/app_routes.py, routes/profiles.py | flask_session Cookie ist convenience-only (D-05). Autoritaet liegt in DB. Cookie-Manipulation kann keine Profile-Impersonation verursachen |
| T-08.19.4-12 mitigated | services/, routes/ | grep bestaetigt 0 aktive get_active_profile()-Caller ausserhalb von live_session.py |

## Self-Check: PASSED

- [x] routes/app_routes.py: `ls.get_active_profile()` Aufruf weg (DB-Lookup stattdessen)
- [x] routes/app_routes.py: `ls.set_active_profile()` Aufruf weg
- [x] routes/profiles.py: `ls_mod.set_active_profile()` Aufruf weg
- [x] routes/profiles.py: loeschen-Route setzt User.active_profile_id=None in DB
- [x] routes/profiles.py: `_active_profile_id()` bevorzugt g.user.active_profile_id
- [x] services/live_session.py: `def set_active_profile` nicht vorhanden
- [x] services/live_session.py: `def get_active_profile` nicht vorhanden
- [x] services/live_session.py: `_legacy_profile_*` nicht vorhanden
- [x] tests/test_session_scoping.py: existiert
- [x] pytest tests/test_session_scoping.py: 7 pass, 1 skipped
- [x] Commits edca234 und 560ba20 vorhanden
