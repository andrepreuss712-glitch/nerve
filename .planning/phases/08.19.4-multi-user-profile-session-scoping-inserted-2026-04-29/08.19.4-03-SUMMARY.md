---
phase: 08.19.4
plan: "03"
subsystem: claude_service / deepgram_service / live_session / prompt_pipeline
tags: [dsgvo, per-sid, session-scoping, multi-user, analyse-loop, coaching-loop, transcript-buffer]
dependency_graph:
  requires:
    - 08.19.4-01
    - 08.19.4-02
  provides:
    - services/live_session.py:_per_sid_transcript
    - services/claude_service.py:analyse_loop (SID-iteration)
    - services/claude_service.py:coaching_loop (SID-iteration)
    - services/claude_service.py:_build_coaching_prompt(sid)
    - services/claude_service.py:_qa_pipeline_dispatch(sid)
  affects:
    - services/live_session.py
    - services/deepgram_service.py
    - services/claude_service.py
    - services/prompt_pipeline.py
tech_stack:
  added: []
  patterns:
    - SID snapshot pattern: list(_session_state.keys()) — lock released before Claude calls
    - Per-SID transcript buffer: _per_sid_transcript dict (analog _per_sid_profile)
    - SID liveness check after every Claude API call
key_files:
  created: []
  modified:
    - services/live_session.py
    - services/deepgram_service.py
    - services/claude_service.py
    - services/prompt_pipeline.py
decisions:
  - "analyse_loop und coaching_loop: O(N) sequentielle SID-Iteration akzeptiert fuer EA (max 50 User) — Migration zu ThreadPoolExecutor in Phase 08.19.5"
  - "_bof_count in coaching_loop liest aus _session_state[sid]['_bof_count'] statt Modul-Global"
  - "_build_coaching_prompt(sid=None): deprecated else-Zweig bleibt fuer Plan 04 (loescht ihn)"
  - "transcript_buffer Modul-Global bleibt als Fallback-Zweig in _flush_segment fuer pre-08.19.4 Reconnect-Edge-Cases"
  - "coaching_buffer bleibt global shared — volle Isolation in Phase 08.19.5"
metrics:
  duration: ~35min
  completed_date: "2026-04-29"
  tasks_completed: 3
  files_modified: 4
---

# Phase 08.19.4 Plan 03: SID-Loop Migration + Per-SID Transcript Buffer Summary

Per-SID `_per_sid_transcript` Buffer implementiert (D-02 Tier 2 PFLICHT); `analyse_loop` und `coaching_loop` iterieren ueber alle aktiven SIDs statt einen globalen State zu lesen; alle 7 `get_active_profile()` Service-Caller auf `get_profile_for_sid(sid)` migriert; SID-Liveness-Checks nach jedem Claude-API-Call.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Per-SID transcript_buffer + _flush_segment + deepgram _merge_pending | 3ccd06d | services/live_session.py, services/deepgram_service.py |
| 2 | Rebuild analyse_loop SID-iteration; migrate get_active_profile callers in claude_service | 4bc3522 | services/claude_service.py |
| 3 | Migrate deepgram/prompt_pipeline callers; rebuild coaching_loop SID-iteration | cc598e8 | services/deepgram_service.py, services/prompt_pipeline.py, services/claude_service.py |

## What Was Built

### Task 1 — live_session.py + deepgram_service.py

- `_per_sid_transcript: dict = {}` + `_per_sid_transcript_lock` in live_session.py
- `_flush_segment`: liest `pending.get('sid')` und schreibt in `_per_sid_transcript[sid].setdefault`
- Fallback else-Zweig schreibt in `transcript_buffer` fuer pre-migration Reconnect-Edge-Cases
- `pop_session_state`: raeumt auch `_per_sid_transcript[sid]` auf
- `_make_on_message`: `'sid': sid` key in `_merge_pending` Entry hinzugefuegt

### Task 2 — claude_service.py (analyse_loop)

- `analyse_loop`: snapshots `list(_session_state.keys())`, Lock freigegeben vor Claude-Calls
- SKALIERUNG-Kommentar (D-03 Pflicht) direkt nach dem Snapshot
- Liest `_per_sid_transcript[sid]` statt globalem `transcript_buffer`
- `analysiert_bisher` aus `sid_state` gelesen statt Modul-Global
- SID-Liveness-Checks nach `analysiere_mit_claude` und `_qa_pipeline_dispatch`
- `get_active_profile()` bei ~978 und ~1060 auf `get_profile_for_sid(sid)` migriert
- `_build_coaching_prompt(sid=None)`: neuer `sid`-Parameter, `get_profile_for_sid` wenn sid vorhanden
- `_qa_pipeline_dispatch(sid=None)`: liest `user_id`, `active_profile_id` aus `_session_state[sid]`; `get_profile_for_sid(sid)` statt `get_active_profile()`

### Task 3 — deepgram_service.py + prompt_pipeline.py + coaching_loop

- `deepgram_service._make_on_message`: Keyword-Match-Caller auf `get_profile_for_sid(sid)`
- `deepgram_service.handle_manual_ewb`: auf `get_profile_for_sid(_sid)` migriert
- `prompt_pipeline.build_profile_context(sid=None)`: neuer `sid`-Parameter; `get_profile_for_sid` wenn sid vorhanden, `get_active_profile()` als deprecated Fallback fuer HTTP-Caller
- `coaching_loop`: SID-Iteration identisch zu `analyse_loop` — `list(_session_state.keys())`
- `coaching_loop`: `_bof_count` liest/schreibt aus `_session_state[sid]` statt Modul-Global `ls._bof_count`
- `coaching_loop`: SID-Liveness-Check nach `analysiere_coaching`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Einrueckungsfehler nach erstem analyse_loop Edit**
- **Found during:** Task 2 (erster Edit-Versuch)
- **Issue:** Der erste analyse_loop-Edit liess den Rest des try-Blocks falsch eingerueckt ausserhalb des for-Loops — `print(f"[Claude-1] Ergebnis...")` waere auf falscher Indentation-Ebene und haette SyntaxError verursacht.
- **Fix:** Vollstaendiger Ersatz des gesamten try-Block-Koerpers mit korrekter 16-Space-Einrueckung innerhalb `for sid in active_sids:`.
- **Files modified:** services/claude_service.py
- **Commit:** 4bc3522

## Verification Results

```
python -m py_compile services/live_session.py services/deepgram_service.py services/claude_service.py services/prompt_pipeline.py
-> compile OK

grep -cn "get_active_profile()" services/claude_service.py
-> 2 (beide sind deprecated fallback else-Zweige fuer sid=None)

grep -cn "get_active_profile()" services/deepgram_service.py
-> 1 (nur Kommentar-Zeile 474, kein Funktionsaufruf)

grep -cn "get_active_profile()" services/prompt_pipeline.py
-> 2 (Docstring + deprecated else-Zweig fuer HTTP-Caller)

grep -n "active_sids = list(ls._session_state" services/claude_service.py
-> 833 (analyse_loop)
-> 1374 (coaching_loop)

grep -n "SKALIERUNG" services/claude_service.py
-> 827 (im analyse_loop)

grep -n "gone during|gone —" services/claude_service.py
-> 877 analyse_loop nach analysiere_mit_claude
-> 886 analyse_loop nach qa_dispatch
-> 1412 coaching_loop nach analysiere_coaching

grep -n "_per_sid_transcript" services/live_session.py
-> 235 (dict declaration)
-> 236 (lock)
-> 289 (pop in pop_session_state)
-> 380-381 (setdefault write in _flush_segment)

transcript_buffer.append in live_session.py
-> 387 (nur in else-Fallback-Zweig von _flush_segment)

get_profile_for_sid combined count:
-> claude_service.py: 4
-> deepgram_service.py: 2
-> prompt_pipeline.py: 2
Total: 8 Treffer (Plan verlangte 7+)
```

## Known Stubs

Keine. coaching_buffer ist noch global shared (kein per-SID) — das ist eine dokumentierte Partial-Fix-Entscheidung, keine unfertige Implementierung. Volle coaching_buffer-Isolation ist fuer Phase 08.19.5 vorgesehen.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| T-08.19.4-07 mitigated | services/claude_service.py | analyse_loop emittiert mit room=sid (zielgerichtet) — SID-Liveness-Check vor emit |
| T-08.19.4-08 mitigated | services/live_session.py | _per_sid_transcript isoliert Transkript per SID; _flush_segment liest sid aus _merge_pending |
| T-08.19.4-09 mitigated | services/claude_service.py | Lock nur fuer Snapshot gehalten, vor Claude-Calls freigegeben |

## Self-Check: PASSED

- [x] services/live_session.py: `_per_sid_transcript: dict = {}` vorhanden
- [x] services/live_session.py: `_per_sid_transcript_lock` vorhanden
- [x] services/live_session.py: `_per_sid_transcript.setdefault` in _flush_segment
- [x] services/live_session.py: `_per_sid_transcript.pop` in pop_session_state
- [x] services/deepgram_service.py: `'sid': sid` in _merge_pending (Zeile 108)
- [x] services/claude_service.py: `active_sids = list(ls._session_state.keys())` an 2 Stellen
- [x] services/claude_service.py: `SKALIERUNG` Kommentar in analyse_loop
- [x] services/claude_service.py: 3 Liveness-Checks (`gone during` / `gone —`)
- [x] services/claude_service.py: `def _build_coaching_prompt(sid: str = None)`
- [x] services/claude_service.py: `def _qa_pipeline_dispatch(..., sid: str = None)`
- [x] Compile OK fuer alle 4 Dateien
- [x] Commits 3ccd06d, 4bc3522, cc598e8 vorhanden
