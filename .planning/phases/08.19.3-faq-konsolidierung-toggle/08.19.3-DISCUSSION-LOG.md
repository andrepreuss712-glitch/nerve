# Phase 08.19.3: FAQ-Konsolidierung mit Toggle — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-28
**Phase:** 08.19.3-faq-konsolidierung-toggle
**Areas discussed:** daten.fragen Cleanup, FAQ Context Block, Toggle Visual, Idempotenz-Prüfung

---

## daten.fragen Cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| Leeres Array lassen | daten.fragen = [] nach Migration. Sicher: profile_schema.py und alle .get('fragen', []) Aufrufe crashen nicht. 08.19.1 räumt den Key dann sauber raus. | ✓ |
| Key komplett entfernen | Key wird aus JSON-Dict gelöscht. Sauberer, aber riskant falls irgendwo noch KeyError möglich ist. | |

**User's choice:** Leeres Array `[]` lassen
**Notes:** 08.19.1 kommt danach und macht die saubere Schema-Kalibrierung.

---

## FAQ Context Block — Welche FAQs

| Option | Description | Selected |
|--------|-------------|----------|
| Alle FAQs (beide Modi) | ALLE profile_faqs, unabhängig von mode, fließen als Kontext in den Prompt. | ✓ |
| Nur ki_generated FAQs | Literal-FAQs weglassen — inkonsistent mit Phase-Brief. | |

**User's choice:** Alle FAQs (beide Modi)

---

## FAQ Context Block — Format

| Option | Description | Selected |
|--------|-------------|----------|
| Q+A-Block mit Intro-Label | Häufig gestellte Fragen + Muster-Antworten:\n- F: [frage_muster]\n  A: [antwort] | ✓ |
| Inline in beweise-Block | FAQs als Teil des beweise-Blocks — vermischt Konzepte. | |

**User's choice:** Q+A-Block mit separatem Intro-Label

---

## Toggle Visual

| Option | Description | Selected |
|--------|-------------|----------|
| Custom CSS Toggle Switch | label+checkbox Two-State. Links "Wortwörtlich" (grau), rechts "KI generiert" (teal). Persist via PUT /faqs/<id>. | ✓ |
| Select-Dropdown pro FAQ | Einfacher, aber schlechtere UX — nicht sofort klar welcher Mode aktiv ist. | |

**User's choice:** Custom CSS Toggle Switch

---

## Idempotenz-Prüfung

| Option | Description | Selected |
|--------|-------------|----------|
| Exact + TRIM | SELECT EXISTS WHERE frage_muster = trim(f) AND profile_id = p.id. Einfach, ausreichend. | ✓ |
| Case-insensitive LOWER() | Übertrieben für manuell eingetragene Texte. | |

**User's choice:** Exact + TRIM

---

## FAQ Context Block — Position

| Option | Description | Selected |
|--------|-------------|----------|
| Nach beweise, vor Anrede | Natürliche Reihenfolge: Basis → Formulierungen → Beweise → FAQ → Anrede. Gut für 08.20-Refactor. | ✓ |
| Am Ende nach Anrede | FAQ fällt ans Ende — schlechter für LLM-Gewichtung (Lost-in-Middle). | |

**User's choice:** Nach beweise-Block, vor Anrede-Lock

---

## Punkt 5 — match_faq bei leerer literal-Liste (via Notes entschieden)

**User's choice:** Bestehender None-Return-Pfad bleibt unverändert. Caller führt dann Generic-EWB-Pfad (Standard-Pipeline) aus. Kein expliziter Fallback nötig — bestehende qa_pipeline-Architektur greift natürlich.

**Edge-Case bestätigt:** Alle FAQs auf `ki_generated` → match_faq() returnt None → KI antwortet aus Profil-Kontext-Wissen (FAQs sind als Q+A-Block im Prompt, aber kein expliziter Literal-Hint). Akzeptabel weil seltener Fall + KI-Generierung ist Default-Pfad.

---

## Claude's Discretion

- CSS-Details des Toggle-Switches (Breite, Animation, Übergänge)
- Exakte Position des Toggles innerhalb der FAQ-Card
- Ob mode-Label neben Toggle als Text oder nur als Tooltip

## Deferred Ideas

- `daten.fragen`-Key komplett entfernen + Schema-Kalibrierung → Phase 08.19.1
- build_profile_context() vollständiger Reihenfolge-Refactor → Phase 08.20
