---
phase: 08.19.3
plan: 04
type: execute
wave: 3
depends_on: ["08.19.3-02", "08.19.3-03"]
files_modified:
  - templates/profile_editor.html
  - static/profile_editor.js
autonomous: false
requirements: []

must_haves:
  truths:
    - "sec-fragen Block (DOM-ID fragen-list, addFrage() Button) ist vollständig aus HTML entfernt"
    - "fragen: getFragen() ist aus dem Speicher-Payload entfernt"
    - "Jede FAQ-Card im tpl-faq-row Template enthält einen Toggle-Switch (Wortwörtlich / KI generiert)"
    - "Toggle zeigt teal (#00D4AA) im ki_generated-Zustand, grau im literal-Zustand"
    - "Toggle-Change persistiert sofort via PUT an /profiles/api/profile/faqs/<id> mit X-CSRFToken"
    - "Bei PUT-Fehler (!r.ok oder Network-Fehler): Toggle wird visuell zurückgesetzt + Toast wird angezeigt"
    - "FAQ-Datenbank-Sektion bleibt in Sektion ④ Einwände & Antworten"
    - "Toggle ist disabled (opacity 0.5) während fetch läuft — wird in .then() UND .catch() wieder enabled"
    - "Neues FAQ-Toggle startet disabled (opacity 0.5) wenn data-faq-id leer — wird nach POST-Success mit returned ID enabled"
  artifacts:
    - path: "templates/profile_editor.html"
      provides: "sec-fragen entfernt, tpl-faq-row mit Toggle-Widget"
      contains: "faq-mode-toggle"
    - path: "static/profile_editor.js"
      provides: "renderFaqRow() setzt Toggle-State, wire change-Event mit optimistic revert + disabled-during-fetch + ghost-toggle-guard"
      contains: "faq-mode-toggle"
  key_links:
    - from: "static/profile_editor.js renderFaqRow()"
      to: "PUT /profiles/api/profile/faqs/<id>"
      via: "modeChk addEventListener change → fetch PUT mit mode-Payload + disabled during fetch + revert on failure"
      pattern: "faq-mode-toggle.*change"
    - from: "templates/profile_editor.html tpl-faq-row"
      to: "static/profile_editor.js renderFaqRow()"
      via: ".faq-mode-toggle class — querySelector-Binding"
      pattern: "faq-mode-toggle"
    - from: "static/profile_editor.js persistFaq() POST success"
      to: "static/profile_editor.js renderFaqRow() ghost-toggle"
      via: "data-faq-id setzen + toggle.disabled = false nach POST-Response"
      pattern: "data-faq-id.*data\\.id"
---

<objective>
Frontend: (1) "Häufige Fragen"-Sektion und `getFragen()`-Payload-Zeile entfernen (D-13, D-14). (2) Toggle-Switch pro FAQ-Card in `tpl-faq-row` einbauen (D-15). (3) Toggle-State aus `faq.mode` in `renderFaqRow()` setzen, Change-Event via sofortigem PUT persistieren (D-16, D-17). (4) Bei PUT-Fehler: optimistisches Update visuell zurücksetzen + Toast anzeigen. (5) Toggle während fetch disabled (opacity 0.5) — in .then() UND .catch() wieder enabled. (6) Ghost-Toggle: neues FAQ startet mit disabled Toggle (kein data-faq-id) — nach POST-Success wird Toggle enabled. Dann Human-Verify-Checkpoint.

Purpose: Konsolidierung zu einer einzigen FAQ-Sektion mit sichtbarem Mode-Toggle pro Eintrag; robuste Fehlerbehandlung verhindert stumm falsch angezeigten Toggle-State; disabled-during-fetch verhindert race conditions bei schnellem Klicken.
Output: Bereinigte profile_editor.html ohne sec-fragen-Duplikat + mode-fähige FAQ-Cards mit optimistic-revert + disabled-during-fetch + ghost-toggle-guard.
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19.3-faq-konsolidierung-toggle/08.19.3-PATTERNS.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md

<interfaces>
<!-- sec-fragen Block to REMOVE (profile_editor.html lines ~479–484): -->
```html
<!-- ehemals sec-fragen -->
<div style="margin-top:24px">
  <div class="sec-title" style="font-size:14px;margin-bottom:12px">Häufige Fragen</div>
  <div id="fragen-list"></div>
  <button type="button" class="btn-primary btn-add-item" onclick="addFrage()">+ Frage hinzufügen</button>
</div>
```

<!-- tpl-faq-row template (profile_editor.html lines ~500–534): -->
```html
<template id="tpl-faq-row">
  <div class="faq-row block" data-faq-id="">
    <div class="block-hd faq-hd" style="cursor:pointer">...</div>
    <div class="faq-fields acc-body collapsed">
      <div class="frow frow-2" ...><!-- frage + antwort textareas --></div>
      <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap">
        <div class="fg" ...>
          <label class="fl">Kategorie</label>
          <select class="faq-kategorie fs">...</select>
        </div>
        <!-- Toggle gehoert HIER rein (nach Kategorie-div) -->
      </div>
    </div>
  </div>
</template>
```

<!-- Speicher-Payload line to REMOVE (profile_editor.html line ~1163): -->
```javascript
fragen: getFragen(),   // ← D-14: diese Zeile entfernen
```

<!-- renderFaqRow() current (profile_editor.js lines ~75–115): -->
```javascript
function renderFaqRow(faq) {
  if (!faqTpl || !faqsContainer) return;
  var frag = faqTpl.content.cloneNode(true);
  var row = frag.querySelector('.faq-row');
  row.setAttribute('data-faq-id', faq.id || '');
  row.querySelector('.faq-frage').value = faq.frage_muster || '';
  row.querySelector('.faq-antwort').value = faq.antwort || '';
  row.querySelector('.faq-kategorie').value = faq.kategorie || 'Sonstiges';
  // ...accordion + autosave wiring...
  faqsContainer.appendChild(frag);
}
```

<!-- persistFaq() POST success handler (profile_editor.js lines ~178–184): -->
```javascript
      .then(function (r) { return r.ok ? r.json() : Promise.reject(r.status); })
      .then(function (data) {
        if (data && data.id) row.setAttribute('data-faq-id', String(data.id));
      })
      .catch(function (e) { console.warn('[FAQ] create failed', e); })
      .finally(function () { row.removeAttribute('data-faq-creating'); });
```

<!-- CSRF pattern (profile_editor.js lines ~61–64): -->
```javascript
function getCsrfToken() {
  var m = document.querySelector('meta[name="csrf-token"]');
  return m ? m.getAttribute('content') : '';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: profile_editor.html — sec-fragen entfernen, fragen-Payload entfernen, Toggle-DOM in tpl-faq-row</name>
  <files>templates/profile_editor.html</files>
  <read_first>
    - templates/profile_editor.html (gezielt lesen: Zeilen ~466–540 für Sektion ④ und tpl-faq-row Template; Zeilen ~970–1030 für addFrage()/getFragen() JS-Funktionen; Zeilen ~1155–1175 für Speicher-Payload; grep "fragen-list" und "addFrage" für exakte Positionen)
  </read_first>
  <action>
**Entfernung 1 — sec-fragen Block (D-13):**

Den gesamten Block (kommentiert mit "ehemals sec-fragen") entfernen:
```html
<!-- ehemals sec-fragen -->
<div style="margin-top:24px">
  <div class="sec-title" style="font-size:14px;margin-bottom:12px">Häufige Fragen</div>
  <div id="fragen-list"></div>
  <button type="button" class="btn-primary btn-add-item" onclick="addFrage()">+ Frage hinzufügen</button>
</div>
```

Exakt diesen Block aus dem HTML entfernen. Den umliegenden Sektion-④-Kontext (FAQ-Datenbank-Sektion) NICHT anfassen — die bleibt (D-18).

**Entfernung 2 — fragen: getFragen() im Speicher-Payload (D-14):**

In der JS-Sektion der Seite (Speicher-Payload, ca. Zeile 1163):
Die Zeile `fragen: getFragen(),` entfernen.
Wenn `fragen` der letzte Key vor einer `}` ist, auf ein vorangehendes Komma achten — dieses ebenfalls entfernen um gültiges JS zu behalten.

**Entfernung 3 — JS-Funktionen addFrage() und getFragen() entfernen:**

`addFrage()` und `getFragen()` vollständig aus dem `<script>`-Block entfernen (ca. Zeilen 978–1021 in PATTERNS.md Beschreibung). Exakte Grenzen via grep ermitteln: `grep -n "function addFrage\|function getFragen" templates/profile_editor.html`. Alle Zeilen beider Funktionen entfernen.

**Addition — Toggle-Widget in tpl-faq-row Template (D-15, D-17):**

Im `<template id="tpl-faq-row">` nach dem Kategorie-`<div>` (und innerhalb des flex-containers `style="display:flex;align-items:center;gap:12px;flex-wrap:wrap"`) folgendes Toggle-Widget einfügen:

```html
<div class="fg faq-mode-wrap" style="margin:0;flex:0 0 auto">
  <label class="fl">Antwort-Modus</label>
  <label class="faq-mode-label" style="display:flex;align-items:center;gap:8px;cursor:pointer">
    <span class="faq-mode-text" style="font-size:12px;color:var(--page-text-muted)">Wortwörtlich</span>
    <span class="faq-mode-track" style="position:relative;display:inline-block;width:40px;height:22px;background:var(--page-text-muted,#888);border-radius:11px;transition:background 0.2s">
      <input type="checkbox" class="faq-mode-toggle" style="opacity:0;width:0;height:0;position:absolute">
      <span class="faq-mode-thumb" style="position:absolute;top:3px;left:3px;width:16px;height:16px;background:#fff;border-radius:50%;transition:transform 0.2s;pointer-events:none"></span>
    </span>
    <span class="faq-mode-text-ki" style="font-size:12px;color:var(--page-text-muted)">KI generiert</span>
  </label>
  <p class="field-desc" style="margin:4px 0 0;font-size:11px">
    <span class="faq-mode-desc"></span>
  </p>
</div>
```

WICHTIG CSS: Die `.faq-mode-track` Background-Farbe und der `.faq-mode-thumb` Transform werden via JS in `renderFaqRow()` gesteuert (kein CSS :checked Pseudo-Selektor nötig, da wir JS verwenden). JS setzt `.style.background` und `.style.transform` basierend auf dem toggle-State.

WICHTIG User-Text (CLAUDE.md Regel): HTML-Content zwischen Tags benutzt echte Umlaute — "Wortwörtlich", "KI generiert" sind korrekt. CSS-Klassen und IDs: ASCII (.faq-mode-toggle, .faq-mode-track etc.).
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import subprocess, sys

def grep_count(pattern, filepath):
    r = subprocess.run(['grep', '-c', pattern, filepath], capture_output=True, text=True)
    return int(r.stdout.strip()) if r.returncode in (0, 1) else -1

# fragen-list must be gone
count_fragen = grep_count('fragen-list', 'templates/profile_editor.html')
assert count_fragen == 0, f'fragen-list still present ({count_fragen} matches)'
print('fragen-list removed: OK')

# addFrage and getFragen must be gone
count_addfrage = grep_count('addFrage', 'templates/profile_editor.html')
assert count_addfrage == 0, f'addFrage still present ({count_addfrage} matches)'
print('addFrage removed: OK')

count_getfragen = grep_count('getFragen', 'templates/profile_editor.html')
assert count_getfragen == 0, f'getFragen still present ({count_getfragen} matches)'
print('getFragen removed: OK')

# Toggle classes must be present in template
count_toggle = grep_count('faq-mode-toggle', 'templates/profile_editor.html')
assert count_toggle >= 1, f'faq-mode-toggle not found in template'
print(f'faq-mode-toggle present: OK ({count_toggle} matches)')

count_track = grep_count('faq-mode-track', 'templates/profile_editor.html')
assert count_track >= 1, f'faq-mode-track not found'
print(f'faq-mode-track present: OK')

count_thumb = grep_count('faq-mode-thumb', 'templates/profile_editor.html')
assert count_thumb >= 1, f'faq-mode-thumb not found'
print(f'faq-mode-thumb present: OK')

print('Plan 04 Task 1 HTML verify: PASSED')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fragen-list" templates/profile_editor.html` → kein Treffer
    - `grep -n "addFrage\|getFragen" templates/profile_editor.html` → kein Treffer
    - `grep -n "faq-mode-toggle" templates/profile_editor.html` → mindestens 1 Treffer (in tpl-faq-row)
    - `grep -n "faq-mode-track" templates/profile_editor.html` → mindestens 1 Treffer
    - `grep -n "faq-mode-thumb" templates/profile_editor.html` → mindestens 1 Treffer
  </acceptance_criteria>
  <done>
    - `id="fragen-list"` ist nicht mehr in profile_editor.html vorhanden
    - `addFrage()` Funktion und Button sind entfernt
    - `getFragen()` Funktion ist entfernt
    - `fragen: getFragen()` ist aus dem Speicher-Payload entfernt
    - `tpl-faq-row` Template enthält `.faq-mode-toggle` checkbox + `.faq-mode-track` span + `.faq-mode-thumb` span
    - Kein Syntax-Fehler im HTML (Browser-DevTools zeigen keine Parse-Fehler — geprüft im Human-Verify Checkpoint)
  </done>
</task>

<task type="auto">
  <name>Task 2: profile_editor.js — renderFaqRow() Toggle-State + Change-Event mit optimistic revert + disabled-during-fetch + ghost-toggle-guard</name>
  <files>static/profile_editor.js</files>
  <read_first>
    - static/profile_editor.js (vollständig lesen — renderFaqRow() Funktion exakt, persistFaq() Payload und POST success handler, getCsrfToken() Definition, alle bestehenden event-Wirings; nach showToast/toast-Funktion suchen: grep -n "showToast\|toast\|Toast" static/profile_editor.js)
  </read_first>
  <action>
<!-- Plan v4: Finding 4 — disabled-during-fetch; Finding 5 — ghost-toggle-guard -->

In `renderFaqRow(faq)` nach der Zeile `row.querySelector('.faq-kategorie').value = faq.kategorie || 'Sonstiges';` folgende Blöcke einfügen:

**Block 1 — Toggle-State setzen + Ghost-Toggle-Guard + optimistisches Change-Event mit Revert + disabled-during-fetch (D-15, D-16, Finding 4, Finding 5):**

```javascript
  // ── Phase 08.19.3 D-15: mode toggle state ────────────────────────────
  var modeChk = row.querySelector('.faq-mode-toggle');
  var modeTrack = row.querySelector('.faq-mode-track');
  var modeThumb = row.querySelector('.faq-mode-thumb');
  var modeDesc = row.querySelector('.faq-mode-desc');
  var faqId = row.getAttribute('data-faq-id');
  var isKi = (faq.mode || 'ki_generated') === 'ki_generated';

  function _setModeVisual(ki) {
    if (modeTrack) modeTrack.style.background = ki ? '#00D4AA' : '#888';
    if (modeThumb) modeThumb.style.transform = ki ? 'translateX(18px)' : 'translateX(0)';
    if (modeDesc) modeDesc.textContent = ki
      ? 'KI generiert = KI nutzt deine Antwort als Wissen und formuliert situationsabhängig.'
      : 'Wortwörtlich = KI spielt deine Antwort exakt so aus wenn die Frage erkannt wird.';
  }

  if (modeChk) {
    modeChk.checked = isKi;
    _setModeVisual(isKi);

    // Ghost-toggle guard: disable toggle until FAQ has DB id (prevents PUT on undefined id).
    // Toggle is re-enabled in persistFaq() POST-success handler after data-faq-id is set.
    if (!faqId) {
      modeChk.disabled = true;
      if (modeTrack) modeTrack.style.opacity = '0.5';
    }

    // ── D-16: sofortiges PUT beim Toggle-Change mit optimistic revert ─────
    modeChk.addEventListener('change', function() {
      var currentFaqId = row.getAttribute('data-faq-id');
      if (!currentFaqId) return;  // ghost-toggle guard (belt-and-suspenders)

      // Capture old state BEFORE the optimistic update (the state before this change event)
      var prevChecked = !modeChk.checked;  // old state = inverse of new checked value
      var newMode = modeChk.checked ? 'ki_generated' : 'literal';

      // Optimistic update: apply visual change immediately
      _setModeVisual(modeChk.checked);

      // Disable toggle during fetch to prevent rapid-fire + race conditions
      modeChk.disabled = true;
      if (modeTrack) modeTrack.style.opacity = '0.5';

      function _revertAndNotify() {
        // Revert checkbox and visual to previous state
        modeChk.checked = prevChecked;
        _setModeVisual(prevChecked);
        // Show toast — use existing toast function if available, else alert as fallback
        var _toastFn = (typeof showToast === 'function') ? showToast
                     : (typeof window.showToast === 'function') ? window.showToast
                     : null;
        if (_toastFn) {
          _toastFn('Speichern fehlgeschlagen — bitte erneut versuchen', 'error');
        } else {
          alert('Speichern fehlgeschlagen — bitte erneut versuchen');
        }
      }

      fetch('/profiles/api/profile/faqs/' + currentFaqId, {
        method: 'PUT',
        credentials: 'same-origin',
        headers: {
          'Content-Type': 'application/json',
          'X-CSRFToken': getCsrfToken()
        },
        body: JSON.stringify({ mode: newMode })
      }).then(function(r) {
        // Re-enable always (success path)
        modeChk.disabled = false;
        if (modeTrack) modeTrack.style.opacity = '';
        if (!r.ok) {
          console.warn('[FAQ] mode update failed', r.status);
          _revertAndNotify();
        }
      }).catch(function(e) {
        // Re-enable always (error path)
        modeChk.disabled = false;
        if (modeTrack) modeTrack.style.opacity = '';
        console.warn('[FAQ] mode update error', e);
        _revertAndNotify();
      });
    });
  }
```

WICHTIG Reihenfolge: `prevChecked = !modeChk.checked` wird BEVOR `_setModeVisual()` gecaptured — das ist der Zustand vor dem Toggle-Change. Nach dem change-Event hat `modeChk.checked` bereits den neuen Wert, daher ist `!modeChk.checked` der alte Wert.

WICHTIG disabled-during-fetch: `modeChk.disabled = true` + `opacity = '0.5'` BEVOR fetch aufgerufen wird. In BEIDEN Pfaden (.then UND .catch) wieder `disabled = false` + `opacity = ''` setzen — nicht nur im Fehlerfall. Das verhindert dass rapid-fire clicks mehrere PUTs in-flight haben.

WICHTIG Ghost-Toggle: Der `if (!faqId)` Block setzt den Toggle initial disabled (opacity 0.5). Der `change` handler liest `data-faq-id` im Moment des Events nochmal via `row.getAttribute('data-faq-id')` (nicht die einmalig gecaptured `faqId` Variable) — so kann der Toggle nach POST-Success korrekt feuern.

WICHTIG Toast-Funktion: Zuerst `static/profile_editor.js` nach `showToast` oder `toast` greppen. Falls eine Funktion existiert, in `_revertAndNotify()` direkt aufrufen. Falls nicht vorhanden: `alert()` als Fallback ist korrekt.

WICHTIG `_setModeVisual` ist eine inner-closure-Funktion (var im renderFaqRow-Scope) — kein globaler Namespace-Conflict.

WICHTIG CSRF: `getCsrfToken()` ist bereits in profile_editor.js definiert. Kein Re-Import nötig.

WICHTIG persistFaq()-Payload: `persistFaq()` sendet nur `frage_muster`, `antwort`, `kategorie` — `mode` NICHT hinzufügen (D-16: mode wird separat via Toggle-Change persistiert, nicht via blur-triggered save).

**Block 2 — Ghost-Toggle nach POST-Success enablen:**

In der bestehenden `persistFaq()` POST-success-Kette (`.then(function(data) { if (data && data.id) row.setAttribute('data-faq-id', String(data.id)); })`):

Diese Zeile ERSETZEN durch:
```javascript
      .then(function (data) {
        if (data && data.id) {
          row.setAttribute('data-faq-id', String(data.id));
          // Ghost-toggle guard: enable toggle now that DB id exists
          var _tog = row.querySelector('.faq-mode-toggle');
          var _trk = row.querySelector('.faq-mode-track');
          if (_tog) {
            _tog.disabled = false;
            if (_trk) _trk.style.opacity = '';
            // If server returned mode, sync toggle state
            if (data.mode) {
              _tog.checked = data.mode === 'ki_generated';
              // _setModeVisual is scoped to renderFaqRow — re-apply via track/thumb directly
              var _thm = row.querySelector('.faq-mode-thumb');
              var _dsc = row.querySelector('.faq-mode-desc');
              var _isKi = data.mode === 'ki_generated';
              if (_trk) _trk.style.background = _isKi ? '#00D4AA' : '#888';
              if (_thm) _thm.style.transform = _isKi ? 'translateX(18px)' : 'translateX(0)';
              if (_dsc) _dsc.textContent = _isKi
                ? 'KI generiert = KI nutzt deine Antwort als Wissen und formuliert situationsabhängig.'
                : 'Wortwörtlich = KI spielt deine Antwort exakt so aus wenn die Frage erkannt wird.';
            }
          }
        }
      })
```

WICHTIG Scope: `_setModeVisual` ist eine Closure in `renderFaqRow()` — sie ist in `persistFaq()` nicht verfügbar. Daher direktes Inline-Update der track/thumb/desc Elemente in der POST-success-Closure.

WICHTIG: `persistFaq()` wird als eigenständige Funktion ausgeführt (nicht innerhalb von `renderFaqRow`). Daher `.faq-mode-toggle`, `.faq-mode-track`, `.faq-mode-thumb`, `.faq-mode-desc` via `row.querySelector(...)` abfragen — `row` ist der `.faq-row` DOM-Node der in `persistFaq()` als Parameter verfügbar ist.

Keine anderen Änderungen in profile_editor.js.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
from app import app
from database.db import engine, SessionLocal
from database.models import ProfileFaq, Profile, Organisation
from sqlalchemy import text

# End-to-end runtime test: POST new FAQ, then PUT mode toggle, assert DB state
client = app.test_client()

# Route is auth-guarded — verify no 500 on unauthenticated requests (smoke test)
r_get = client.get('/profiles/api/profile/1/faqs')
assert r_get.status_code in (302, 401, 403), f'GET unexpected status {r_get.status_code}'
print('GET /faqs auth guard: OK (no 500)', r_get.status_code)

r_put = client.put('/profiles/api/profile/faqs/1', json={'mode': 'literal'}, headers={'X-CSRFToken': 'test'})
assert r_put.status_code in (302, 400, 401, 403), f'PUT unexpected {r_put.status_code}'
print('PUT /faqs mode=literal auth guard: OK', r_put.status_code)

r_put_inv = client.put('/profiles/api/profile/faqs/1', json={'mode': 'invalid'}, headers={'X-CSRFToken': 'test'})
assert r_put_inv.status_code in (302, 400, 401, 403), f'PUT invalid mode unexpected {r_put_inv.status_code}'
print('PUT /faqs mode=invalid guard: OK', r_put_inv.status_code)

r_post = client.post('/profiles/api/profile/1/faqs', json={'frage_muster': 'Test', 'antwort': 'Test'})
assert r_post.status_code in (302, 400, 401, 403, 404), f'POST unexpected {r_post.status_code}'
print('POST /faqs auth guard: OK', r_post.status_code)

# Direct ORM test: simulate disabled-during-fetch behavior via DB round-trip
# Seed a test row, PUT mode via ORM, verify DB reflects change (tests the DB path the PUT route uses)
with engine.connect() as c:
    c.execute(text(\"\"\"INSERT OR IGNORE INTO profile_faqs (id, profile_id, frage_muster, antwort, kategorie, used_count, mode)
        VALUES (99902, 1, 'Toggle test row', 'Answer', 'Sonstiges', 0, 'ki_generated')\"\"\"))
    c.commit()

db = SessionLocal()
try:
    row = db.query(ProfileFaq).filter_by(id=99902).first()
    assert row.mode == 'ki_generated', f'Initial mode wrong: {row.mode}'
    row.mode = 'literal'
    db.commit()
    db.refresh(row)
    assert row.mode == 'literal', f'After toggle PUT: mode not updated: {row.mode}'
    # Revert back (optimistic revert scenario)
    row.mode = 'ki_generated'
    db.commit()
    db.refresh(row)
    assert row.mode == 'ki_generated', f'After revert: mode wrong: {row.mode}'
    print('Toggle ORM round-trip (ki_generated → literal → revert): OK')
finally:
    db.close()

with engine.connect() as c:
    c.execute(text('DELETE FROM profile_faqs WHERE id=99902'))
    c.commit()

# Note: JS disabled-during-fetch and ghost-toggle visual behavior verified in Task 3 human-verify checkpoint
# (no headless browser available in this stack; these are UI interaction tests, not server-side logic)
print('Plan 04 Task 2 verify: PASSED (JS interaction tests: see Task 3 human-verify)')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "prevChecked" static/profile_editor.js` → mindestens 1 Treffer mit `prevChecked = !modeChk.checked`
    - `grep -n "_revertAndNotify" static/profile_editor.js` → mindestens 2 Treffer (definition + calls in .then und .catch)
    - `grep -n "modeChk.checked = prevChecked" static/profile_editor.js` → mindestens 1 Treffer (checkbox revert)
    - `grep -n "_setModeVisual(prevChecked)" static/profile_editor.js` → mindestens 1 Treffer (visual revert)
    - `grep -n "Speichern fehlgeschlagen" static/profile_editor.js` → mindestens 1 Treffer (failure message)
    - `grep -n "X-CSRFToken" static/profile_editor.js` → mindestens 1 Treffer im mode toggle fetch
    - `grep -n "modeChk.disabled = true" static/profile_editor.js` → mindestens 1 Treffer (vor fetch)
    - `grep -n "modeChk.disabled = false" static/profile_editor.js` → mindestens 2 Treffer (.then UND .catch Pfad)
    - `grep -n "opacity.*0.5" static/profile_editor.js` → mindestens 2 Treffer (ghost-guard + disabled-during-fetch)
    - `grep -n "_tog.disabled = false" static/profile_editor.js` → mindestens 1 Treffer (POST-success ghost-enable)
    - `renderFaqRow()` setzt `.faq-mode-toggle` checked-State basierend auf `faq.mode`
    - `.faq-mode-track` background: `#00D4AA` wenn ki_generated, `#888` wenn literal
    - `.faq-mode-thumb` transform: `translateX(18px)` wenn ki_generated, `translateX(0)` wenn literal
    - `persistFaq()` payload-Objekt enthält kein `mode`-Feld
    - Toggle startet disabled (opacity 0.5) wenn `data-faq-id` leer (neues FAQ vor POST)
    - Toggle ist disabled + opacity 0.5 während fetch läuft (Finding 4)
    - Toggle wird in .then() UND .catch() wieder enabled (beide Pfade, Finding 4)
    - Nach POST-Success: `data-faq-id` gesetzt + toggle enabled + mode-State aus Response gesetzt (Finding 5)
  </acceptance_criteria>
  <done>
    - `renderFaqRow()` setzt `.faq-mode-toggle` checked-State basierend auf `faq.mode`
    - `.faq-mode-track` background: `#00D4AA` wenn ki_generated, `#888` wenn literal
    - `.faq-mode-thumb` transform: `translateX(18px)` wenn ki_generated, `translateX(0)` wenn literal
    - `prevChecked = !modeChk.checked` wird vor `_setModeVisual()` gecaptured (alter Zustand vor Toggle)
    - Optimistisches Update: `_setModeVisual(modeChk.checked)` sofort beim Change
    - Toggle disabled (opacity 0.5) BEVOR fetch gestartet wird — in .then() UND .catch() wieder enabled (Finding 4)
    - Bei `!r.ok`: `_revertAndNotify()` setzt checkbox + visual zurück + zeigt Toast/Alert "Speichern fehlgeschlagen — bitte erneut versuchen"
    - Bei catch (Network-Fehler): `_revertAndNotify()` identisch, toggle re-enabled
    - Ghost-Toggle: Toggle startet disabled (opacity 0.5) wenn `data-faq-id` leer (Finding 5)
    - Ghost-Toggle POST-Success: `data-faq-id` gesetzt, toggle enabled, mode-State aus `data.mode` gesetzt (Finding 5)
    - Toggle `change` Event feuert sofortigen `fetch PUT` mit `{mode: newMode}` und `X-CSRFToken`-Header
    - `persistFaq()` payload enthält kein `mode`-Feld
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Plan 01: profile_faqs.mode-Spalte migriert (ALTER TABLE + Backfill nur beim Erstlauf + daten.fragen Migration mit granularem Error-Logging)
    - Plan 02: match_faq() filtert nur literal-FAQs; build_profile_context() hat FAQ Q+A-Block (max 20, sortiert by used_count DESC)
    - Plan 03: GET /faqs gibt mode zurück; PUT /faqs validiert + speichert mode; POST /faqs akzeptiert mode (default 'ki_generated'), gibt mode in Response; partial PUT explizit verifiziert
    - Plan 04: sec-fragen Sektion entfernt; tpl-faq-row Toggle-Widget eingebaut; renderFaqRow() setzt Toggle-State + Change-Event mit optimistic revert + disabled-during-fetch + ghost-toggle-guard; POST-success enabled Toggle mit DB-ID
  </what-built>
  <how-to-verify>
    1. App starten: `python app.py` (oder Server neu starten)
    2. Startup-Log prüfen: `[DB] Migration: added profile_faqs.mode` ODER `[DB] Migration: profile_faqs.mode skip (...)` — kein silent Fehler
    3. Browser: https://getnerve.app/profiles/<id>/editor öffnen
    4. Sektion ④ "Einwände & Antworten" aufrufen
    5. PRÜFEN: "Häufige Fragen"-Subsection ist NICHT mehr sichtbar (kein "+ Frage hinzufügen" Button)
    6. PRÜFEN: FAQ-Datenbank-Sektion ist noch vorhanden (bleibt)
    7. Eine bestehende FAQ-Card öffnen (Accordion aufklappen)
    8. PRÜFEN: Toggle-Switch "Wortwörtlich / KI generiert" ist sichtbar
    9. PRÜFEN: Neue FAQs (aus daten.fragen migriert) zeigen Toggle in KI-generiert-Position (teal)
    10. PRÜFEN: Bestehende profile_faqs (vor Migration) zeigen Toggle in Wortwörtlich-Position (grau)
    11. Toggle umschalten → prüfen ob teal/grau korrekt wechselt
    12. Seite neu laden → Toggle-State bleibt erhalten (persistiert via PUT)
    13. Browser-DevTools: kein JS-Fehler in Console
    14. PRÜFEN disabled-during-fetch: Toggle schnell hin und her klicken → Toggle ist kurz disabled (grayed out) während Network-Request läuft
    15. OPTIONAL (Revert-Test): DevTools → Network → Offline setzen → Toggle umschalten → Toast "Speichern fehlgeschlagen" erscheint + Toggle kehrt zur alten Position zurück → Offline aufheben
    16. OPTIONAL (Ghost-Toggle-Test): Neues FAQ via "+ FAQ hinzufügen" erstellen → Toggle ist disabled/grayed solange FAQ noch nicht gespeichert; nach erstem Blur (POST) wird Toggle enabled
  </how-to-verify>
  <resume-signal>
    "approved" wenn Punkte 1–13 bestätigt (Punkte 14–16 optional aber empfohlen), oder Issue-Beschreibung wenn Fehler gefunden.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PUT /profiles/api/profile/faqs/<id> | Toggle-Change sendet mode via fetch; CSRF-Token Pflicht |
| Template tpl-faq-row | Gerendert aus Server-Daten; faq.mode kommt aus GET-Response |
| persistFaq() POST-success | data.id + data.mode kommen aus Server-Response; werden direkt in DOM gesetzt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.3-14 | CSRF | Toggle fetch PUT | mitigate | X-CSRFToken Header aus getCsrfToken() in jedem Toggle-Change-Fetch (D-16, Polish-Cycle 5 Lesson) |
| T-08.19.3-15 | Tampering | mode-Wert im JS | accept | JS sendet 'ki_generated' oder 'literal' (hardcoded aus modeChk.checked); Server validiert via Whitelist (Plan 03) |
| T-08.19.3-16 | Denial of Service | Toggle rapid-fire | mitigate | Toggle disabled während fetch läuft — verhindert mehrere in-flight PUTs; re-enabled in .then() UND .catch() (Finding 4) |
| T-08.19.3-17 | Information Disclosure | JS-console.warn | accept | `console.warn('[FAQ] mode update failed', r.status)` — kein sensitiver Inhalt in Log-Message |
| T-08.19.3-18 | Tampering | Ghost-Toggle PUT ohne ID | mitigate | `if (!currentFaqId) return;` guard im change-Handler + toggle initial disabled — kein PUT auf undefined FAQ-ID möglich |
</threat_model>

<verification>
1. `grep -n "fragen-list" templates/profile_editor.html` → keine Treffer
2. `grep -n "addFrage\|getFragen" templates/profile_editor.html` → keine Treffer
3. `grep -n "faq-mode-toggle" templates/profile_editor.html` → mindestens ein Treffer (in tpl-faq-row)
4. `grep -n "faq-mode-toggle" static/profile_editor.js` → mindestens ein Treffer (in renderFaqRow)
5. `grep -n "X-CSRFToken" static/profile_editor.js` → mindestens ein Treffer im mode-toggle Fetch
6. `grep -n "prevChecked" static/profile_editor.js` → mindestens ein Treffer
7. `grep -n "_revertAndNotify" static/profile_editor.js` → mindestens zwei Treffer (definition + calls)
8. `grep -n "Speichern fehlgeschlagen" static/profile_editor.js` → mindestens ein Treffer
9. `grep -n "modeChk.disabled = true" static/profile_editor.js` → mindestens ein Treffer (vor fetch)
10. `grep -n "modeChk.disabled = false" static/profile_editor.js` → mindestens zwei Treffer (.then + .catch)
11. `grep -n "_tog.disabled = false" static/profile_editor.js` → mindestens ein Treffer (POST-success)
12. Browser-Test: Checkpoint human-verify bestätigt alle 13 Pflichtpunkte
</verification>

<success_criteria>
- sec-fragen Sektion (fragen-list, addFrage, getFragen) vollständig entfernt
- tpl-faq-row enthält Toggle-Widget (.faq-mode-toggle, .faq-mode-track, .faq-mode-thumb)
- renderFaqRow() setzt Toggle-State aus faq.mode (teal = ki_generated, grau = literal)
- Toggle-Change: optimistisches Update + sofortiger PUT mit X-CSRFToken
- Toggle disabled (opacity 0.5) WÄHREND fetch läuft — re-enabled in .then() UND .catch() (Finding 4)
- Toggle-Change Fehlerfall: prevChecked vor Update gecaptured, _revertAndNotify() setzt checkbox + visual zurück + zeigt Toast/Alert
- Ghost-Toggle: neues FAQ startet mit disabled Toggle (kein data-faq-id) — POST-success enabled Toggle + setzt mode aus Response (Finding 5)
- Human-Verify Checkpoint bestätigt alle 13 Pflicht-UI-Punkte
- pytest bleibt grün
- git push origin main nach Phase-Abschluss (CLAUDE.md Git-Regel)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19.3-faq-konsolidierung-toggle/08.19.3-04-SUMMARY.md` erstellen.
</output>
