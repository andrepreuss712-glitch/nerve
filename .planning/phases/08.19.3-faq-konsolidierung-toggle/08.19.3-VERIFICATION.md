---
phase: 08.19.3-faq-konsolidierung-toggle
verified: 2026-04-29T00:00:00Z
status: human_needed
score: 17/17 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open profile editor in browser, navigate to Sektion 4, open an existing FAQ card and verify the Toggle-Switch (Wortwörtlich / KI generiert) is visible and functional"
    expected: "Toggle shows teal (#00D4AA) for ki_generated mode and grey (#888) for literal mode; toggling persists via PUT (reload retains state); sec-fragen block with addFrage button is completely absent"
    why_human: "Visual appearance, toggle interaction, PUT persistence via real browser session with CSRF token, and disabled-during-fetch race-condition behavior cannot be verified without a headless browser or live session"
  - test: "Verify disabled-during-fetch: rapidly click toggle back and forth during a slow network"
    expected: "Toggle becomes opacity 0.5 and unclickable during the in-flight PUT; re-enables in both success and error paths"
    why_human: "Requires real browser interaction with network throttling; cannot simulate timing in static code analysis"
  - test: "Ghost-toggle test: add a new FAQ, verify toggle is disabled/grayed until first blur triggers POST-success"
    expected: "Toggle disabled (opacity 0.5) on new row before POST; after POST-success with returned id, toggle enables and shows correct mode from server response"
    why_human: "Requires live browser interaction with real POST round-trip; cannot verify in static analysis"
---

# Phase 08.19.3: FAQ-Konsolidierung mit Toggle — Verification Report

**Phase Goal:** Consolidate FAQ sources (daten.fragen + profile_faqs) into a single profile_faqs table with a mode column (literal vs ki_generated), expose mode in API, filter match_faq() to literal-only, inject FAQ Q&A block in Claude prompts, and add a toggle UI in the profile editor to switch modes.
**Verified:** 2026-04-29
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | profile_faqs Tabelle hat eine mode-Spalte VARCHAR(20) NOT NULL DEFAULT 'ki_generated' | VERIFIED | `app.py` line 700: `ALTER TABLE profile_faqs ADD COLUMN mode VARCHAR(20) NOT NULL DEFAULT 'ki_generated'` |
| 2  | Alle bestehenden profile_faqs-Rows (vor Migration) haben mode='literal' (Backfill) | VERIFIED | `app.py` lines 699–710: Backfill `UPDATE profile_faqs SET mode='literal' WHERE mode='ki_generated'` inside same try/except as ALTER TABLE — runs only on first start |
| 3  | Alle daten.fragen-Eintraege sind als profile_faqs-Rows mit mode='ki_generated' migriert | VERIFIED | `_migrate_fragen_to_faqs()` function at `app.py` line 914: loops all profiles, inserts with `mode='ki_generated'`, idempotency via SELECT EXISTS |
| 4  | daten.fragen-Key ist nach Migration ein leeres Array [] (nicht entfernt) | VERIFIED | `app.py` line ~985: `_daten['fragen'] = []` set after migration loop |
| 5  | ProfileFaq ORM-Modell hat mode-Attribut | VERIFIED | `database/models.py` line 159: `mode = Column(String(20), nullable=False, default='ki_generated')` |
| 6  | match_faq() erhaelt nur mode='literal' FAQs (nie ki_generated) | VERIFIED | `services/claude_service.py` line 1258–1259: `_faqs_all = _qa_load_faqs(...)` then `_faqs = [f for f in _faqs_all if f.get('mode') == 'literal']` |
| 7  | Bei leerem literal-Filter gibt match_faq() None zurueck und Standard-EWB greift | VERIFIED | Line 1260: `match_faq(...) if _faqs else None` — no call when list empty |
| 8  | build_profile_context() enthaelt FAQ Q+A-Block fuer ALLE FAQs (beide Modi) | VERIFIED | `services/prompt_pipeline.py` lines 179–205: block queries ALL profile_faqs without mode filter |
| 9  | FAQ Q+A-Block erscheint nach beweise-Block, vor Ton/Anrede | VERIFIED | beweise block ends line ~177; FAQ block at line 179; Ton/Stil block at line 207 |
| 10 | FAQ Q+A-Block in build_profile_context() ist auf max 20 FAQs limitiert (sortiert by used_count DESC) | VERIFIED | `prompt_pipeline.py` line 194: `.order_by(_PF.used_count.desc()).limit(20)` |
| 11 | GET /profiles/api/profile/<id>/faqs liefert mode-Feld in jedem FAQ-Objekt | VERIFIED | `routes/profiles.py` line 725: `'mode': r.mode or 'ki_generated'` in list comprehension |
| 12 | PUT /profiles/api/profile/faqs/<id> akzeptiert mode-Feld und validiert es (nur 'literal' oder 'ki_generated') | VERIFIED | `routes/profiles.py` lines 801–805: `if 'mode' in data:` → whitelist check → `row.mode = m` |
| 13 | POST /profiles/api/profile/<id>/faqs akzeptiert mode-Feld (optional, default 'ki_generated'), validiert und gibt mode in Response | VERIFIED | `routes/profiles.py` lines 748–749, 762, 767: `mode_val` parsing with default, whitelist, `mode=mode_val` in constructor, `'mode': row.mode` in 201 response |
| 14 | sec-fragen Block (DOM-ID fragen-list, addFrage() Button) ist vollstaendig aus HTML entfernt | VERIFIED | Grep `fragen-list`, `addFrage`, `getFragen` in `templates/profile_editor.html` returns no matches |
| 15 | Jede FAQ-Card im tpl-faq-row Template enthaelt einen Toggle-Switch (Wortwoertlich / KI generiert) | VERIFIED | `templates/profile_editor.html` line 531: `.faq-mode-toggle` checkbox inside `.faq-mode-track` span with `.faq-mode-thumb` — all three classes present |
| 16 | Toggle zeigt teal (#00D4AA) im ki_generated-Zustand, grau im literal-Zustand | VERIFIED | `static/profile_editor.js` line 93: `modeTrack.style.background = ki ? '#00D4AA' : '#888'` |
| 17 | Toggle-Change persistiert sofort via PUT an /profiles/api/profile/faqs/<id> mit X-CSRFToken | VERIFIED | `static/profile_editor.js` lines 142–149: `fetch('/profiles/api/profile/faqs/' + currentFaqId, {method:'PUT', headers: {'X-CSRFToken': getCsrfToken()}, body: JSON.stringify({mode: newMode})})` |

**Score:** 17/17 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `database/models.py` | ProfileFaq.mode ORM column | VERIFIED | Line 159: `mode = Column(String(20), nullable=False, default='ki_generated')` |
| `app.py` | idempotent ALTER TABLE + Backfill + daten.fragen migration in `_migrate()` | VERIFIED | Lines 698–710 (Block A+B), `_migrate_fragen_to_faqs()` defined at line 914 and called at line 911 |
| `services/claude_service.py` | `_qa_load_faqs()` returns mode field, `_qa_pipeline_dispatch()` filters on literal | VERIFIED | Line 1152: `'mode': r.mode` in dict; lines 1258–1260: `_faqs_all` + literal filter |
| `services/prompt_pipeline.py` | FAQ Q+A-Block in `build_profile_context()` | VERIFIED | Lines 179–205: full try/except block with cap, order_by, format |
| `routes/profiles.py` | mode-Feld in GET-Response, PUT-Handler and POST-Handler | VERIFIED | Lines 725, 748–749, 762, 767, 801–805 |
| `templates/profile_editor.html` | sec-fragen removed, tpl-faq-row with Toggle-Widget | VERIFIED | No fragen-list/addFrage/getFragen matches; faq-mode-toggle at line 531 |
| `static/profile_editor.js` | renderFaqRow() sets toggle state, change-event with optimistic revert + disabled-during-fetch + ghost-toggle-guard | VERIFIED | Lines 84–166 (renderFaqRow toggle block), lines 264–286 (persistFaq ghost-enable) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app.py _migrate()` | `profile_faqs.mode` | ALTER TABLE + advisory-lock pattern | WIRED | Line 700 ALTER TABLE; single try/except with backfill at line 705 |
| `app.py _migrate()` | `daten.fragen → profile_faqs` | advisory-lock loop with idempotency SELECT EXISTS | WIRED | `_migrate_fragen_to_faqs()` called at line 911 |
| `services/claude_service.py _qa_pipeline_dispatch()` | `match_faq()` | mode='literal' filter before call | WIRED | Lines 1258–1260: explicit `[f for f in _faqs_all if f.get('mode') == 'literal']` |
| `services/prompt_pipeline.py build_profile_context()` | `database/models.py ProfileFaq` | DB-Query all FAQs (max 20, order by used_count DESC) | WIRED | Lines 188–203: SessionLocal + `filter_by(profile_id=_faq_profile_id).order_by(_PF.used_count.desc()).limit(20)` |
| `routes/profiles.py api_faqs_list()` | `ProfileFaq.mode` | `r.mode` in list comprehension | WIRED | Line 725 |
| `routes/profiles.py api_faqs_update()` | `ProfileFaq.mode` | whitelist validation + `row.mode = m` | WIRED | Lines 801–805 |
| `routes/profiles.py api_faqs_create()` | `ProfileFaq.mode` | mode_val from POST body + `ProfileFaq(mode=mode_val)` | WIRED | Lines 748–749, 762, 767 |
| `static/profile_editor.js renderFaqRow()` | `PUT /profiles/api/profile/faqs/<id>` | modeChk addEventListener change → fetch PUT with mode-payload + disabled during fetch + revert on failure | WIRED | Lines 112–165 |
| `templates/profile_editor.html tpl-faq-row` | `static/profile_editor.js renderFaqRow()` | `.faq-mode-toggle` class — querySelector binding | WIRED | HTML line 531; JS line 85: `row.querySelector('.faq-mode-toggle')` |
| `static/profile_editor.js persistFaq() POST success` | ghost-toggle enable | `data-faq-id` set + `toggle.disabled = false` after POST-response | WIRED | Lines 266–285 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `services/prompt_pipeline.py build_profile_context()` | `_faqs` (list of ProfileFaq) | `SessionLocal` query on `profile_faqs` with `filter_by(profile_id=...)` | Yes — ORM query against live DB | FLOWING |
| `services/claude_service.py _qa_pipeline_dispatch()` | `_faqs` (literal-only list) | `_qa_load_faqs()` + list comprehension filter | Yes — filtered from DB query result | FLOWING |
| `routes/profiles.py api_faqs_list()` | FAQ list with mode field | `db.query(ProfileFaq).filter_by(profile_id=p.id)` | Yes — ORM query, `r.mode` from DB column | FLOWING |
| `static/profile_editor.js renderFaqRow()` | `faq.mode` toggle state | GET /faqs response `'mode'` field from server | Yes — server returns DB value | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED for server startup (app import) — running `python -c "import app"` would initialize the DB migration, which is a stateful operation against the live DB. The SUMMARY.md documents that all migration verifications were run during execution and passed. The static code analysis above confirms all code paths are correctly wired.

| Behavior | Result | Status |
|----------|--------|--------|
| `database/models.py` ProfileFaq has mode attribute | Confirmed via grep: line 159 | PASS |
| `app.py` ALTER TABLE + Backfill in single try/except | Confirmed via grep: lines 699–710 | PASS |
| `_migrate_fragen_to_faqs()` called in `_migrate()` | Confirmed: line 911 call + line 914 definition | PASS |
| mode filter in `_qa_pipeline_dispatch()` | Confirmed: lines 1258–1260 | PASS |
| FAQ Q+A block in `build_profile_context()` | Confirmed: lines 179–205 | PASS |
| mode field in all three profile FAQ API endpoints | Confirmed: lines 725, 748–767, 801–805 | PASS |
| sec-fragen removed from HTML | Confirmed: no grep matches for fragen-list/addFrage/getFragen | PASS |
| Toggle widget in tpl-faq-row | Confirmed: faq-mode-toggle, faq-mode-track, faq-mode-thumb all present | PASS |
| Toggle change-event wired with CSRF, disabled-during-fetch, optimistic revert | Confirmed: lines 112–165 | PASS |
| Ghost-toggle guard + POST-success enable | Confirmed: lines 106–109 (guard), 267–285 (enable) | PASS |

### Requirements Coverage

No `requirements:` field populated in any of the four plan files — requirements coverage not applicable for this phase.

### Anti-Patterns Found

No blockers identified. Reviewed all 6 modified files:

- `database/models.py`: Clean ORM column addition, no stubs
- `app.py`: Migration blocks have explicit logging (no silent pass); per-entry errors log profile_id and frage preview; outer advisory-lock body has `app.logger.error + raise`
- `services/claude_service.py`: `_qa_load_faqs()` returns real DB data; mode filter is a real list comprehension, not a stub
- `services/prompt_pipeline.py`: FAQ block is in a `try/except` (MUST NOT raise); reads from real DB query; no hardcoded empty returns
- `routes/profiles.py`: Whitelist validation present on all three endpoints; no `except Exception: pass` patterns
- `templates/profile_editor.html`: sec-fragen block cleanly removed; toggle widget DOM is substantive
- `static/profile_editor.js`: Toggle change-event fully wired; `_revertAndNotify()` called in both `.then()` and `.catch()` paths; ghost-toggle guard present

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None found | — | — | — |

### Human Verification Required

Three items require browser-based human testing (toggle visual behavior, persistence, race-condition guards):

#### 1. Toggle Visual + Persistence Test

**Test:** Open `https://getnerve.app/profiles/<id>/editor` → Sektion ④ → open an existing FAQ card accordion
**Expected:**
- Toggle switch "Wortwörtlich / KI generiert" is visible with label "Antwort-Modus"
- FAQs migrated from daten.fragen show teal (ki_generated); pre-existing profile_faqs show grey (literal)
- Clicking toggle changes color immediately (optimistic update)
- Page reload after toggle: state persists (PUT was saved to DB)
- "Häufige Fragen" sub-section with addFrage button is absent from the page
**Why human:** Visual rendering and PUT persistence via real authenticated browser session with live CSRF token

#### 2. Disabled-During-Fetch Race Condition Test

**Test:** Open profile editor, open a FAQ card, rapidly click the toggle back and forth
**Expected:** Toggle becomes grey/opacity 0.5 and non-clickable during the in-flight PUT request; re-enables after network response
**Why human:** Requires real browser interaction with network throttling (DevTools → Network → Slow 3G); cannot simulate fetch timing in static analysis

#### 3. Ghost-Toggle Test (New FAQ)

**Test:** Click "+ FAQ hinzufügen", observe the toggle before typing/saving
**Expected:** Toggle is disabled (opacity 0.5) on new row before first blur triggers POST; after successful POST (first blur away from frage/antwort fields), toggle enables and shows correct mode from server response
**Why human:** Requires live browser interaction with real POST round-trip; cannot verify DOM state transitions in static analysis

### Gaps Summary

No gaps. All 17 must-haves are verified in the codebase. Human verification is required for the toggle UI interaction behaviors (visual rendering, persistence confirmation, race-condition guards) which cannot be assessed through static code analysis alone.

---

_Verified: 2026-04-29_
_Verifier: Claude (gsd-verifier)_
