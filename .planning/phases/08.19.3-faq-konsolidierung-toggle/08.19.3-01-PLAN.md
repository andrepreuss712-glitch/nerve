---
phase: 08.19.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - database/models.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "profile_faqs Tabelle hat eine mode-Spalte VARCHAR(20) NOT NULL DEFAULT 'ki_generated'"
    - "Alle bestehenden profile_faqs-Rows (vor Migration) haben mode='literal' (Backfill)"
    - "Alle daten.fragen-Eintraege sind als profile_faqs-Rows mit mode='ki_generated' migriert"
    - "daten.fragen-Key ist nach Migration ein leeres Array [] (nicht entfernt)"
    - "ProfileFaq ORM-Modell hat mode-Attribut"
  artifacts:
    - path: "app.py"
      provides: "idempotente ALTER TABLE + Backfill + daten.fragen Migration in _migrate()"
      contains: "ADD COLUMN mode VARCHAR(20)"
    - path: "database/models.py"
      provides: "ProfileFaq.mode ORM-Spalte"
      contains: "mode = Column(String(20)"
  key_links:
    - from: "app.py _migrate()"
      to: "profile_faqs.mode"
      via: "ALTER TABLE + advisory-lock pattern"
      pattern: "ADD COLUMN mode VARCHAR\\(20\\)"
    - from: "app.py _migrate()"
      to: "daten.fragen → profile_faqs"
      via: "advisory-lock loop mit idempotency SELECT EXISTS"
      pattern: "fragen.*ki_generated"
---

<objective>
Schema-Migration: `mode`-Spalte zu `profile_faqs` hinzufügen, bestehende Rows auf `literal` backfillen, und alle `daten.fragen`-Einträge verlustfrei nach `profile_faqs` migrieren.

Purpose: Basisvoraussetzung für alle folgenden Plans — Backend-Logik und Frontend setzen mode-Feld voraus.
Output: Laufende DB-Migration in `_migrate()`, ORM-Attribut in `database/models.py`.
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19.3-faq-konsolidierung-toggle/08.19.3-PATTERNS.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md

<interfaces>
<!-- Existing ProfileFaq ORM (database/models.py lines 150–158): -->
```python
class ProfileFaq(Base):
    __tablename__ = 'profile_faqs'
    id           = Column(Integer, primary_key=True)
    profile_id   = Column(Integer, ForeignKey('profiles.id'), nullable=False)
    frage_muster = Column(Text, nullable=False)
    antwort      = Column(Text, nullable=False)
    kategorie    = Column(String(100), nullable=True)
    created_at   = Column(DateTime, default=utcnow)
    used_count   = Column(Integer, default=0, nullable=False)
```

<!-- Advisory-Lock pattern (app.py lines ~1261–1287): -->
```python
_is_sqlite = str(engine.url).startswith('sqlite')
with engine.connect() as conn:
    if _is_sqlite:
        try:
            conn.execute(_text3("BEGIN EXCLUSIVE"))
        except Exception as _e:
            print(f"[DB] _migrate_xxx: BEGIN EXCLUSIVE failed: {_e}")
            return
    else:
        try:
            conn.execute(_text3("SELECT pg_advisory_xact_lock(819)"))
        except Exception as _e:
            print(f"[DB] _migrate_xxx: pg_advisory_xact_lock failed: {_e}")
            return
    # migration body ...
    try:
        conn.commit()
    except Exception:
        pass
```

<!-- profile_faqs CREATE TABLE (app.py lines ~681–696): -->
```python
try:
    conn.execute(text("""
        CREATE TABLE IF NOT EXISTS profile_faqs (
            id INTEGER PRIMARY KEY,
            profile_id INTEGER NOT NULL REFERENCES profiles(id),
            frage_muster TEXT NOT NULL,
            antwort TEXT NOT NULL,
            kategorie VARCHAR(100),
            created_at DATETIME,
            used_count INTEGER DEFAULT 0 NOT NULL
        )
    """))
    conn.commit()
    print("[DB] Migration: created profile_faqs table")
except Exception as _e:
    print(f"[DB] Migration: profile_faqs skip ({_e})")
```

<!-- Profile JSON daten column (database/models.py): Profile.daten is TEXT JSON, accessed via json.loads -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileFaq ORM — mode-Spalte hinzufügen</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py (vollständige Datei lesen — ORM-Modell-Definitionen, utcnow-Definition, Column-Import-Pattern)
  </read_first>
  <action>
In `database/models.py` die `ProfileFaq`-Klasse um die `mode`-Spalte erweitern.

Nach der `used_count`-Zeile einfügen:
```python
mode = Column(String(20), nullable=False, default='ki_generated')
```

Import-Check: `String` ist bereits via `from sqlalchemy import Column, Integer, String, ...` importiert (analog zu anderen Modellen in der Datei). Falls `String` noch nicht importiert ist, zu den bestehenden SQLAlchemy-Importen hinzufügen.

Namensregel: `mode` ist ein Python-Attribut/DB-Spalte — ASCII, kein Umlaut (CLAUDE.md Regel).
  </action>
  <verify>
    <automated>python -c "from database.models import ProfileFaq; print(hasattr(ProfileFaq, 'mode'))"</automated>
  </verify>
  <done>
    - database/models.py enthält `mode = Column(String(20), nullable=False, default='ki_generated')` in der ProfileFaq-Klasse
    - `hasattr(ProfileFaq, 'mode')` gibt True zurück
  </done>
</task>

<task type="auto">
  <name>Task 2: app.py _migrate() — ALTER TABLE + Backfill + daten.fragen Migration</name>
  <files>app.py</files>
  <read_first>
    - app.py (gezielt lesen: _migrate()-Funktion vollständig, speziell Zeilen ~680–724 für profile_faqs CREATE TABLE, Zeilen ~1261–1295 für Advisory-Lock-Pattern, Zeilen ~180–185 für idempotentes ALTER TABLE Pattern, Zeilen ~247 für session_mode DEFAULT-Analog)
    - database/models.py (Profile-Modell: daten-Spalte, id-Spalte — für die Migration-Loop)
  </read_first>
  <action>
In `app.py` in der `_migrate()`-Funktion drei Blöcke hinzufügen. Reihenfolge: (A) nach dem profile_faqs CREATE TABLE Block, aber vor anderen späteren Migrations.

**Block A — Idempotentes ALTER TABLE (D-01):**
```python
# ── Phase 08.19.3 D-01: profile_faqs.mode ────────────────────────────────
try:
    conn.execute(text("ALTER TABLE profile_faqs ADD COLUMN mode VARCHAR(20) NOT NULL DEFAULT 'ki_generated'"))
    conn.commit()
    print("[DB] Migration: added profile_faqs.mode")
except Exception:
    pass  # column already exists — idempotent
```

**Block B — Backfill bestehende Rows auf literal (D-02):**
Direkt nach Block A (in der selben connection-Session, wenn möglich, oder in separatem try-Block):
```python
# ── Phase 08.19.3 D-02: Backfill existing profile_faqs mode='literal' ────
try:
    conn.execute(text("UPDATE profile_faqs SET mode='literal' WHERE mode='ki_generated'"))
    conn.commit()
    print("[DB] Migration: backfilled profile_faqs.mode='literal' for existing rows")
except Exception as _be:
    print(f"[DB] Migration: profile_faqs backfill skip ({_be})")
```

WICHTIG D-02-Timing: Der Backfill läuft NACH dem ALTER TABLE. Das ist korrekt: er setzt alle Rows die nach ALTER TABLE den DEFAULT 'ki_generated' erhalten haben auf 'literal' um. Neue Rows (nach dieser Migration) erhalten via DEFAULT 'ki_generated' — was korrekt ist. Der Backfill trifft also nur die zum Migrations-Zeitpunkt existierenden Rows.

**Block C — daten.fragen → profile_faqs Migration (D-03, D-04, D-05):**
Als separate Funktion `_migrate_fragen_to_faqs()` definieren und in `_migrate()` aufrufen. Diese Funktion verwendet das Advisory-Lock-Pattern (analog zu `_migrate_profile_json()` lines ~1261–1295):

```python
def _migrate_fragen_to_faqs():
    """Idempotente Migration: daten.fragen Eintraege -> profile_faqs rows (mode='ki_generated').
    D-03: Exact-Match + TRIM Idempotenz-Check.
    D-04: daten.fragen key auf [] setzen (nicht entfernen).
    D-05: Laeuft gegen alle Profile (Andre's Profil 6 + System-Profil 7 inkl.).
    MUST NOT raise.
    """
    import json as _json
    _is_sqlite = str(engine.url).startswith('sqlite')
    try:
        with engine.connect() as _conn:
            if _is_sqlite:
                try:
                    _conn.execute(text("BEGIN EXCLUSIVE"))
                except Exception as _e:
                    print(f"[DB] _migrate_fragen_to_faqs: BEGIN EXCLUSIVE failed: {_e}")
                    return
            else:
                try:
                    _conn.execute(text("SELECT pg_advisory_xact_lock(81930)"))
                except Exception as _e:
                    print(f"[DB] _migrate_fragen_to_faqs: pg_advisory_xact_lock failed: {_e}")
                    return

            # Load all profiles
            _profiles = _conn.execute(text("SELECT id, daten FROM profiles")).fetchall()
            _migrated = 0
            _skipped = 0
            for _row in _profiles:
                _pid = _row[0]
                try:
                    _daten = _json.loads(_row[1] or '{}')
                except Exception:
                    continue
                _fragen = _daten.get('fragen') or []
                if not _fragen:
                    continue
                _changed = False
                for _f in _fragen:
                    _frage = (_f.get('frage') or '').strip() if isinstance(_f, dict) else str(_f).strip()
                    _antwort = (_f.get('antwort') or '').strip() if isinstance(_f, dict) else ''
                    if not _frage:
                        continue
                    # Idempotenz: Exact Match + TRIM (D-03)
                    _exists = _conn.execute(
                        text("SELECT EXISTS(SELECT 1 FROM profile_faqs WHERE profile_id=:pid AND frage_muster=trim(:frage))"),
                        {'pid': _pid, 'frage': _frage}
                    ).scalar()
                    if _exists:
                        _skipped += 1
                        continue
                    _conn.execute(
                        text("""INSERT INTO profile_faqs (profile_id, frage_muster, antwort, kategorie, created_at, used_count, mode)
                                VALUES (:pid, :frage, :antwort, 'Sonstiges', datetime('now'), 0, 'ki_generated')"""),
                        {'pid': _pid, 'frage': _frage, 'antwort': _antwort}
                    )
                    _migrated += 1
                    _changed = True
                # D-04: daten.fragen key auf [] setzen (nicht entfernen)
                if _changed or _fragen:
                    _daten['fragen'] = []
                    _conn.execute(
                        text("UPDATE profiles SET daten=:daten WHERE id=:pid"),
                        {'daten': _json.dumps(_daten, ensure_ascii=False), 'pid': _pid}
                    )
            _conn.commit()
            print(f"[DB] _migrate_fragen_to_faqs: migrated={_migrated} skipped={_skipped}")
    except Exception as _e:
        print(f"[DB] _migrate_fragen_to_faqs: error ({_e})")
```

Aufruf am Ende von `_migrate()`:
```python
_migrate_fragen_to_faqs()
```

WICHTIG Reihenfolge in `_migrate()`: Block A (ALTER TABLE mode) MUSS vor dem Aufruf von `_migrate_fragen_to_faqs()` stehen, damit die mode-Spalte existiert wenn die INSERT-Statements laufen.

WICHTIG Advisory-Lock-Nummer: 81930 ist neu (nicht Kollision mit 819 aus `_migrate_profile_json`).
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import app
from database.db import engine
from sqlalchemy import text
with engine.connect() as c:
    cols = [row[1] for row in c.execute(text('PRAGMA table_info(profile_faqs)')).fetchall()]
    print('Columns:', cols)
    assert 'mode' in cols, 'mode column missing'
    print('mode column: OK')
"</automated>
  </verify>
  <done>
    - app.py enthält `ALTER TABLE profile_faqs ADD COLUMN mode VARCHAR(20) NOT NULL DEFAULT 'ki_generated'` in try/except Block
    - app.py enthält `UPDATE profile_faqs SET mode='literal' WHERE mode='ki_generated'` Backfill nach dem ALTER TABLE
    - `_migrate_fragen_to_faqs()` Funktion ist definiert und wird in `_migrate()` aufgerufen
    - Starten der App via `python -c "import app"` bringt keine Exception
    - `PRAGMA table_info(profile_faqs)` zeigt `mode`-Spalte
    - `[DB] Migration: added profile_faqs.mode` oder `[DB] Migration: profile_faqs backfill...` in Startup-Log
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.py _migrate() | Läuft beim App-Start — kein User-Input, nur interne DB-Operationen |
| daten.fragen JSON | Aus DB geladen — kann None, leeres Array, nicht-dict-Einträge enthalten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.3-01 | Tampering | daten.fragen Migration Loop | mitigate | isinstance(_f, dict) Check vor .get()-Zugriff — verhindert Crash bei String-Einträgen; try/except pro Profil verhindert partial-failure-Cascade |
| T-08.19.3-02 | Denial of Service | BEGIN EXCLUSIVE Lock | accept | Advisory-Lock verhindert parallele Migration; SQLite single-writer ist akzeptabel für Solo-User Setup |
| T-08.19.3-03 | Tampering | Backfill UPDATE | accept | `WHERE mode='ki_generated'` ist idempotent; zweiter Lauf setzt keine bereits-literal Rows um |
| T-08.19.3-04 | Information Disclosure | print([DB] Migration log) | accept | Startup-Log ist server-seitig, kein User-facing Leak |
</threat_model>

<verification>
Nach App-Start (python -c "import app") oder Serverstart:

1. `PRAGMA table_info(profile_faqs)` gibt Row mit `name=mode` zurück
2. Alle bestehenden profile_faqs-Rows haben `mode='literal'` (Backfill verifizieren: `SELECT COUNT(*) FROM profile_faqs WHERE mode='ki_generated' AND id < (SELECT MIN(id) FROM profile_faqs WHERE created_at > datetime('now','-1 minute'))` = 0)
3. Alle daten.fragen-Einträge (Profile mit nicht-leerem fragen-Array) sind als profile_faqs mit mode='ki_generated' insertet
4. Profile.daten.fragen ist [] nach Migration
5. hasattr(ProfileFaq, 'mode') = True
</verification>

<success_criteria>
- database/models.py ProfileFaq hat `mode = Column(String(20), nullable=False, default='ki_generated')`
- app.py _migrate() enthält idempotentes ALTER TABLE für profile_faqs.mode
- app.py _migrate() enthält Backfill UPDATE mode='literal' für Bestandsrows
- `_migrate_fragen_to_faqs()` ist definiert und aufgerufen, migiert daten.fragen → profile_faqs mit mode='ki_generated'
- App startet ohne Exception
- pytest bleibt grün (keine Breaking Changes)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19.3-faq-konsolidierung-toggle/08.19.3-01-SUMMARY.md` erstellen.
</output>
