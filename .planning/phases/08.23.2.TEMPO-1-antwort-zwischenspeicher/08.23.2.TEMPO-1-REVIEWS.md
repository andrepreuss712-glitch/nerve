---
phase: 08.23.2.TEMPO-1
reviewers: [gemini, claude-fable-5]
reviewed_at: 2026-07-21
plans_reviewed: [08.23.2.TEMPO-1-01-PLAN.md, 08.23.2.TEMPO-1-02-PLAN.md, 08.23.2.TEMPO-1-03-PLAN.md]
verdict: NICHT execute-reif — 2× HIGH + 1× MEDIUM (verifiziert)
---

# Cross-AI Plan Review — Phase 08.23.2.TEMPO-1

**Verfügbare CLIs:** gemini ✓, claude ✓ (als Fable 5, anderes Modell = echte Zweitmeinung).
codex / opencode / qwen / cursor / coderabbit: nicht installiert.

## Gemini Review

Hallo, hier ist dein Cross-AI Plan Review für die Phase `08.23.2.TEMPO-1`.

### 0. Antworten auf den Spezial-Auftrag

Hier sind die Antworten auf deine drei expliziten Prüfaufträge:

**1. Falscher Beleg-Query (`context_tag='pip_stream'`)**

**Verdikt: OK**

Die Pläne haben diese Korrektur **überall und exzellent** umgesetzt.

-   **Fundstellen:**
    -   `08.23.2.TEMPO-1-03-PLAN.md`, `<objective>`: Das Problem wird explizit als Falle benannt und der korrekte Query `context_tag IN ('ewb','qa')` wird vorgestellt.
    -   `08.23.2.TEMPO-1-03-PLAN.md`, `Task 3`: Der `psql`-Befehl zur Verifikation verwendet den korrekten Query.
    -   `08.23.2.TEMPO-1-03-PLAN.md`, `Task 4`: Die Aktualisierung der `ROADMAP.md` beinhaltet die explizite Korrektur des Beleg-Queries und eine Erklärung, warum `pip_stream` falsch war.
    -   `08.23.2.TEMPO-1-03-PLAN.md`, `acceptance_criteria` für Task 4: Es wird sogar per `grep` geprüft, dass `pip_stream` in der `ROADMAP.md` nur noch in nicht-normativen Kontexten (als Teil der Messungs-Beschreibung) vorkommt.

Es wurde keine Stelle gefunden, an der der veraltete `pip_stream`-Tag noch als Verifikations-Kriterium verwendet wird.

**2. Wiederkehrende Fehlerklasse (widersprüchliche Abnahmekriterien)**

**Verdikt: OK**

Ich habe jedes `grep`- und Verifikations-Kriterium in allen drei Plänen gegen den Ziel-Zustand des Codes geprüft und **keine Widersprüche** dieser Art gefunden. Der Plan-Autor zeigt ein außergewöhnlich hohes Bewusstsein für dieses Problem.

-   **Beispiele für gute Praktiken, die hier angewendet wurden:**
    -   **Code-Anker statt Roh-Strings:** In `08.23.2.TEMPO-1-01-PLAN.md`, Task 2, wird ein komplexer `grep`-Ausdruck verwendet, der zwischen Code-Definitionen (`=`) bzw. Lesezugriffen (`config.`) und reinen Erwähnungen in Kommentaren unterscheidet. Dadurch wird verhindert, dass das Kriterium fälschlicherweise fehlschlägt, weil ein neuer Kommentar die entfernten Schalter zu Erklärungszwecken nennt.
    -   **Präzise `grep`-Flags:** In `08.23.2.TEMPO-1-03-PLAN.md`, Task 2, wird die Notwendigkeit des `-F` (fixed string) Flags für den `grep` auf dem Server korrekt identifiziert, um die `[`-Zeichen in `_block['cache_control']` korrekt zu interpretieren. Ein Fehlen dieses Details hätte zu einem falschen Fehler nach dem Deploy geführt.
    -   **Konsistenz:** Alle Kriterien, die das Entfernen von Code betreffen (z.B. Plan 01, Task 1, `_CACHE_MIN_CHARS`), prüfen korrekt auf `0 Treffer`. Alle Kriterien, die das Hinzufügen von Code betreffen (z.B. Plan 02, Task 1), prüfen auf eine exakte Anzahl von Treffern an der richtigen Stelle.

Die Pläne sind in dieser Hinsicht vorbildlich und scheinen aus früheren Fehlern gelernt zu haben.

**3. `cache_control`-Zuordnung**

**Verdikt: OK**

Die im Plan `08.23.2.TEMPO-1-02-PLAN.md`, Task 1 vorgeschriebene Implementierung ist **wasserdicht**.

-   **Robustheit der Implementierung:**
    -   Die Lösung ersetzt die fehleranfällige List-Comprehension durch eine `for`-Schleife. Dies stellt sicher, dass der `_layer`-Marker verarbeitet wird, **bevor** er verworfen wird.
    -   Die Zuordnung erfolgt über einen Wert-Vergleich (`b.get('_layer') == 'stable'`), **nicht** über einen zerbrechlichen Listen-Index (`content[0]`).
    -   Ein `_cache_gesetzt`-Flag stellt sicher, dass maximal ein `cache_control`-Breakpoint pro Request gesetzt wird, selbst wenn die Quell-Logik mehrere stabile Blöcke liefern würde.
    -   Der Fall eines leeren Stabil-Blocks wird korrekt gehandhabt: er wird übersprungen, und der Volatil-Block (der dann auf Index 0 rutscht) erhält **keinen** Marker.
-   **Testabdeckung:** Die neuen Tests in Plan 02, Task 2 (`test_cache_marker_...`) decken exakt diese kritischen Punkte ab, insbesondere `test_cache_marker_folgt_layer_nicht_index`, der die Index-Falle absichert.

Die Implementierung ist durchdacht, robust gegen die beschriebenen Risiken und durch spezifische Tests abgesichert.

---

### 1. Summary

Die vorliegenden Pläne sind von außergewöhnlich hoher Qualität. Sie sind extrem detailliert, transparent und zeigen ein tiefes Verständnis für die technischen Details und potenziellen Risiken. Die Planung geht weit über eine reine To-do-Liste hinaus und stellt eine umfassende Ausführungs-Spezifikation dar, die proaktiv aus vergangenen Fehlern lernt (z.B. widersprüchliche Verifikations-Kriterien) und robuste, testgestützte Lösungen vorschreibt. Die Aufteilung in drei Wellen (Code-Entfernung, Implementierung, Verifikation) ist logisch und minimiert Risiken.

### 2. Strengths

-   **Proaktives Risikomanagement:** Die Pläne identifizieren und mitigieren bekannte und potenzielle Fehlerquellen explizit (z.B. die Index-Falle bei der `cache_control`-Zuordnung, Cache-Poisoning durch die `session_anrede`, falsche `grep`-Kriterien).
-   **Testgetriebene Entwicklung:** Die Implementierung der Kernlogik (Plan 02) wird untrennbar mit der Erstellung einer umfassenden Test-Suite verknüpft, die nicht nur den Happy Path, sondern gezielt die kritischsten Edge Cases absichert.
-   **Präzise & widerspruchsfreie Verifikation:** Die Akzeptanzkriterien sind sorgfältig formuliert, um den tatsächlichen Code-Zustand zu prüfen und nicht durch Nebeneffekte (wie Kommentare) fälschlicherweise fehlzuschlagen.
-   **Operative Sicherheit:** Die Integration eines dedizierten Rollback-Schalters (`CACHE_ANTWORT`) und ein detaillierter Deploy- und Verifikationsplan (Plan 03) mit Vorher-Nachher-Messungen zeugen von hoher Professionalität im Umgang mit einem Live-System.
-   **Hervorragende Dokumentation:** Die Begründung für jede Entscheidung ("Warum EIN Schalter?") ist direkt im Code und in den Plänen verankert, was zukünftige Wartung massiv erleichtert.

### 3. Concerns

Ich habe keine Bedenken mit dem Schweregrad HIGH oder MEDIUM. Der einzige Punkt ist vernachlässigbar klein:

-   **(LOW) Geringfügige Ungenauigkeit bei Doku-Verifikation:** In `08.23.2.TEMPO-1-01-PLAN.md`, Task 2, prüft das Akzeptanzkriterium `grep -rn "CACHE_ANTWORT" .planning/codebase/` >= 3 nur die Anwesenheit des Keywords in den Doku-Dateien, nicht den korrekten Inhalt. Dies ist ein sehr geringes Risiko, da der Plan den exakten Zieltext vorschreibt.

### 4. Suggestions

Zur weiteren Verbesserung der Robustheit des unter "Concerns" genannten Punktes schlage ich vor, das Akzeptanzkriterium wie folgt zu präzisieren:

-   **Fundstelle:** `08.23.2.TEMPO-1-01-PLAN.md`, Task 2, `<acceptance_criteria>`
-   **Aktuelles Kriterium:** `grep -rn "CACHE_ANTWORT" .planning/codebase/` liefert **>= 3 Treffer**
-   **Verbesserungsvorschlag:** Ersetzen durch drei spezifischere Prüfungen, die den Inhalt verifizieren. Dies stellt sicher, dass nicht nur der Name, sondern der korrekte Kontext in der Doku gelandet ist.
    ```bash
    grep -q "Single ENV switch `CACHE_ANTWORT`" .planning/codebase/ARCHITECTURE.md && \
    grep -q "`CACHE_ANTWORT = true`" .planning/codebase/INTEGRATIONS.md && \
    grep -q "Prompt caching: `CACHE_ANTWORT=true`" .planning/codebase/STACK.md
    # test $? -eq 0
    ```

### 5. Risk Assessment

**Risk: LOW**

Das Gesamtrisiko der Phase ist als sehr gering einzuschätzen. Die Gründe dafür sind:
1.  **Scope:** Die Änderung ist auf einen kleinen, klar definierten Teil der Codebase beschränkt.
2.  **Rollback-Fähigkeit:** Ein dedizierter, ENV-basierter Schalter (`CACHE_ANTWORT`) ermöglicht eine sofortige Deaktivierung der neuen Logik ohne einen erneuten Deploy.
3.  **Testabdeckung:** Die neue Kernlogik ist durch eine Reihe von spezifischen, automatisierten Tests abgesichert, die die wichtigsten Risiken (falsche Marker-Zuordnung, Cache-Vergiftung) abdecken.
4.  **Verifikation:** Ein mehrstufiger Verifikationsplan für die Produktionsumgebung stellt sicher, dass sowohl die technische Funktion (Cache-Read-Logs) als auch die Performance (TTFT) und die Stabilität (Fehler-Logs) nachgewiesen werden.

Die Pläne sind ein Musterbeispiel für eine sorgfältige und risikoarme Implementierung einer Performance-Optimierung an einem kritischen Pfad des Systems. Ich gebe eine klare Empfehlung zur Umsetzung.

---

## Claude (Fable 5) Review

# Cross-AI Plan Review — Phase 08.23.2.TEMPO-1

Ich habe alle referenzierten Code-Stellen real gelesen (`prompt_pipeline.py:440-687`, `claude_service.py:1-30/510-880`, `qa_pipeline.py:36-505`, `config.py:60-115`, beide Test-Dateien komplett, `app.py:1095-1175`, ROADMAP:2386-2423) und jedes grep-Kriterium gegen den Ziel-Zustand nach Abschluss ALLER Tasks geprüft.

## 0. Spezial-Auftrag

### 0.1 Falscher Beleg-Query (`pip_stream`) — **Verdikt: OK**

Die Korrektur ist überall übernommen. Geprüfte Stellen:

- **Plan 03, must_haves + Task 3 Acceptance:** durchgängig `context_tag IN ('ewb','qa')`; explizites Verbot „nicht den String `pip_stream` als Beleg-Kriterium". ✓
- **Plan 03, Task 4:** korrigiert die einzige echte Fundstelle `ROADMAP.md:2411` (verifiziert: dort steht heute die Beleg-Pflicht **ohne** context_tag) und whitelisted die zwei legitimen Mess-/Nebenfund-Treffer `:2394`/`:2413` präzise. ✓
- **CONTEXT.md D2** und beide Bau-Pläne nennen `pip_stream` nur noch als widerlegtes Label, nie als Prüfkriterium. ✓
- Die Cost-Hooks schreiben real `context_tag='ewb'` (`claude_service.py:705/858`) und `'qa'` (`qa_pipeline.py:493`) — der Query passt zum Code. Zusätzlich verifiziert: für **alle drei geloggten Modell-Strings** (`haiku-4-5`, `sonnet-4-5`, `claude-sonnet-4-5`) existieren `per_1k_cache_read_tokens`-Raten im Seed (`app.py:1110/1121/1125`) — der W3-Skip verschluckt den Beleg also nicht, obwohl der Knopf-Pfad das falsche Modell loggt (Nebenfund korrekt erfasst).

Kosmetik (LOW): Der Ersatztext in Plan 03 Task 4 A behauptet „Der **früher hier genannte** `context_tag='pip_stream'`" — laut Plan-03-Objective selbst nennt `:2411` gar keinen context_tag; `pip_stream` stand nur in `:2394`. Der Korrektur-Text schreibt damit eine kleine Un-Wahrheit in die ROADMAP.

### 0.2 Grep-Kriterien vs. Ziel-Code — **Verdikt: FEHLER (2× HIGH, 1× MEDIUM)** — die Fehlerklasse lebt noch

**FEHLER 1 (HIGH) — Plan 01, Task 1: das Kriterium löscht den eigenen Erklär-Kommentar.**
Der vorgeschriebene Ersatz-Kommentar in `claude_service.py` enthält wörtlich:
```
# CACHE_ANALYSE-Zweig verglich zusaetzlich Zeichen gegen eine Token-Grenze.
```
Das Abnahme-Kriterium desselben Tasks verlangt aber:
`grep -rn "CACHE_ANALYSE" --include=*.py services/ routes/ nerve_rt/` → **0 Treffer**.
Nach korrekt ausgeführtem Task ist der Zähler **1**. Der Executor wird — genau wie im Plan selbst als Falle beschrieben — den Erklär-Kommentar löschen, um grün zu werden. Ironie: Task 2 desselben Plans löst das identische Problem für config.py bereits vorbildlich per CODE-Anker-Regex; Task 1 hat den Roh-String-grep behalten.

**FEHLER 2 (HIGH) — Plan 01, Task 1: gleicher Widerspruch beim Schalter-Namen.**
Derselbe Ersatz-Kommentar endet mit:
```
# answer_system_content, Schalter config.CACHE_ANTWORT).
```
Kriterium: `grep -n "config.CACHE_ANTWORT" services/claude_service.py` → **0 Treffer** („Gilt zum Zeitpunkt dieses Plans"). Real: **1 Treffer** sofort nach Task 1. Folgefehler kaskadieren: die Fußnote im selben Kriterium („nach Plan 02 ist der Roh-String-Zähler dort **2**") und **Plan 02 Task 3** (`grep -c "CACHE_ANTWORT" services/claude_service.py` == **2**) sind beide falsch — der echte Endstand ist **3** (Plan-01-Kommentar + die zwei ersetzten Kommentare A)1/A)2).

**FEHLER 3 (MEDIUM) — Plan 03, Task 2: das spät-scheiternde Kriterium — falsche Zahl NACH dem Prod-Deploy.**
Die Gegenprobe behauptet: „Der Roh-String-Zaehler `grep -c cache_control services/prompt_pipeline.py` ergibt beim korrekt gebauten Stand **4**." Das stimmt nur nach Plan 02 **Task 1**. Plan 02 **Task 3** (A)5 + A)6) fügt zwei weitere `cache_control`-Kommentarzeilen hinzu (`build_answer_context`-Docstring `:584` + Zeilen-Kommentar `:619`). Endstand: **6** Zeilen. Wer die Gegenprobe nach dem Deploy fährt und 6 statt 4 sieht, hat exakt den false-red-Reflex, vor dem der Plan drei Absätze lang warnt. (Der bindende Anker `grep -c "cache_control.*ephemeral"` == 2 bleibt korrekt — die neuen Kommentare enthalten kein „ephemeral". Auch die „vier Zeilen"-Zähler-Regel in Plan 02 Task 1 gilt nur bis Task 3 — sollte als „Stand nach Task 1" etikettiert werden.)

**Alle übrigen grep-Kriterien geprüft und konsistent** — u.a.: die CODE-Anker-Regexe in Plan 01 Task 2 + Verify-Block matchen den neuen config-Kommentar und den `hasattr`-Wächter-Test korrekt NICHT (verifiziert gegen Zeilenanfang/`config.`-Prefix); `grep -c "cache_control.*ephemeral"` == 2 hält über alle Tasks; `_CACHE_MIN_CHARS`/`Cache-Check` haben repo-weit keine weiteren Treffer (verifiziert); `_system_prompt` hat keinen Leser außerhalb des gelöschten Blocks (verifiziert: nur `:526-534`); die ROADMAP-Plan-Liste existiert genau 1× (Zähler-Kriterium in Plan 03 Task 4 trägt); `-F`-Hinweis beim Prod-grep ist korrekt und wichtig (`['cache_control']` wäre ohne `-F` eine Zeichenklasse); Doku-Treffer in `.planning/codebase/` exakt die 4 genannten Stellen (verifiziert), `.env.example`/`deploy.sh` sauber.

### 0.3 cache_control-Zuordnung trotz Marker-Verwurf — **Verdikt: OK, wasserdicht**

Gegen den realen Code geprüft (`prompt_pipeline.py:618-621` liefert `_layer` hart codiert; `:683` verwirft ihn heute):

- Die vorgeschriebene Schleife liest `b.get('_layer') == 'stable'` **am Quell-Block**, bevor der neue Content-Dict ohne Marker gebaut wird — der Marker geht nicht verloren, sondern wird genau an der letzten Stelle konsumiert, an der er existiert (A3 erfüllt).
- **Kein Index-Raten:** Zuordnung rein wertbasiert. Fällt der leere Stabil-Block durch den `if not _text: continue`-Filter, rutscht der Volatil-Block zwar auf Index 0, matcht aber nie `== 'stable'` → kann den Marker **nicht** bekommen (A5 erfüllt; Test 3 nagelt es fest, inkl. Pflicht-Beleg, dass der Test gegen eine Index-Variante rot zeigt — sehr gut).
- `_cache_gesetzt` deckelt auf genau 1 Breakpoint, auch bei hypothetisch mehreren stabilen Blöcken (A4); Reihenfolge-robust.
- Fallback (`'Verstehen und helfen…'`) wird nach der Schleife neu gebaut, ohne Marker (A6).
- Schalter: `import config as _cfg` lazy + `getattr` — funktioniert mit `monkeypatch.setattr(config, 'CACHE_ANTWORT', False)`, weil der lazy Import dasselbe Modul-Objekt aus `sys.modules` liefert; Modul-Vertrag „side-effect-free beim Import" bleibt gewahrt (kein `^import config`, verifiziert: existiert heute nicht).
- QA-Pfad: Tabu-Block wird **nach** der Rückgabe hinten angehängt (`qa_pipeline.py:420-421`) — der Cache-Prefix endet am markierten Stabil-Block, Produkt-Verbote bestimmen ihn nicht mit. Korrekt.
- Anti-Cache-Poison-Filter `:474-481` existiert wie behauptet und filtert exakt die Sektion-7-Anredezeile; Sektion-9-Anrede (`:452`) liegt hinter dem `## PreCall-Briefing`-Split-Anker → volatil. E3-Test-Design (Byte-Gleichheit über zwei SIDs, In-Memory nach dem Muster von `test_anti_cache_poison_real_split:129-163`) ist tragfähig, inkl. der Anti-False-Green-Assertions (`'## KI-Verhalten' in stable_a`, `startswith('Anrede: Du')` statt Teilstring).

## 1. Summary

Die Pläne sind ungewöhnlich gründlich, die Kern-Implementierung (A3/A5-Schleife) ist korrekt konstruiert und die drei Spezial-Risiken (pip_stream, Cache-Poison, Marker-Verlust) sind sauber adressiert. Aber die vom Nutzer benannte wiederkehrende Fehlerklasse — grep-Kriterien, die dem vom selben Plan vorgeschriebenen Code widersprechen — ist **nicht ausgerottet**: Plan 01 Task 1 enthält zwei solcher Selbst-Widersprüche (der vorgeschriebene Erklär-Kommentar nennt `CACHE_ANALYSE` und `config.CACHE_ANTWORT`, die Abnahme verlangt 0 Treffer für genau diese Strings), mit Kaskade in Plan 02 Task 3 (Zähler 2 statt real 3). Zusätzlich enthält Plan 03 Task 2 eine spät-scheiternde falsche Zahl (Roh-Zähler „4" statt real 6 — geprüft **nach** dem Prod-Deploy), die exakt den false-red-Rollback-Reflex auslösen kann, vor dem der Plan selbst warnt. Alle drei sind mechanisch fixbar; danach sind die Pläne execute-reif.

## 2. Strengths

- Die Marker-Logik (wertbasiert, `_cache_gesetzt`, Fallback ohne Marker, Schalter an EINER Stelle) ist gegen alle sechs LOCKED-Entscheidungen konsistent und gegen den realen Code verifizierbar.
- Test-Netz ist echtes Runtime-Verhalten (keine Source-Presence), inkl. Pflicht-Nachweis, dass der Index-Fallen-Test die Falle wirklich fängt.
- Beleg-Führung in Plan 03 antizipiert die drei realen Stolpersteine korrekt: „Hooks schreiben nur bei >0", RLS-False-Negative (psql als postgres), Write-vor-Read (mindestens zwei Antworten). Der Zwei-Antworten-Zwang ist sachlich richtig.
- Der Nebenfund `TEMPO1-KNOPF-MODELLNAME-FALSCH` ist real (verifiziert: `claude_service.py:846/856` loggt hart `'haiku-4-5'` bei `MODEL_PIP_VARIANTE=claude-sonnet-4-5`) und korrekt als Backlog statt Scope-Creep behandelt. Wichtig: Raten für `haiku-4-5`-Cache-Reads existieren (`app.py:1110`), der Beleg-Query funktioniert trotz des Falsch-Namens.
- TTFT-Baseline **vor** dem Deploy zu sichern (journald-Rotation!) ist genau richtig sequenziert; „n<5 = nicht belastbar, nicht schönreden" ist die richtige Disziplin.
- Wellen-Reihenfolge sauber: Schalter entsteht (W1) vor Verdrahtung (W2) vor Deploy (W3), gleiche Dateien nie parallel.

## 3. Concerns

- **HIGH — Plan 01 Task 1, Acceptance-Kriterium 2:** `grep -rn "CACHE_ANALYSE" … services/` == 0 widerspricht dem im selben Task vorgeschriebenen Kommentar („`CACHE_ANALYSE`-Zweig"). Executor löscht den Kommentar oder das Kriterium bleibt rot.
- **HIGH — Plan 01 Task 1, Acceptance-Kriterium 6:** `grep -n "config.CACHE_ANTWORT" services/claude_service.py` == 0 widerspricht dem vorgeschriebenen Kommentar („Schalter config.CACHE_ANTWORT"). Kaskade: Fußnote „danach 2" und Plan 02 Task 3 `grep -c "CACHE_ANTWORT" … == 2` sind beide falsch (real: 1 nach Plan 01, 3 nach Plan 02).
- **MEDIUM — Plan 03 Task 2, Gegenprobe (spät-scheiternd, nach Prod-Deploy):** „Roh-String-Zaehler ergibt 4" — real **6** nach Plan 02 Task 3 (A)5 `:584` + A)6 `:619` fügen je eine `cache_control`-Kommentarzeile hinzu). Kann den grundlosen Rollback triggern.
- **LOW — Plan 02 Task 1, Zähler-Regel:** „nennt cache_control in vier Zeilen" gilt nur bis Task 3 desselben Plans; als „Stand nach Task 1" etikettieren.
- **LOW — Plan 03 Task 4:** Ersatztext „Der früher hier genannte `context_tag='pip_stream'`" ist für `:2411` faktisch falsch (dort stand nie ein context_tag) — Formulierung anpassen, sonst schreibt die Korrektur eine neue Un-Wahrheit.
- **LOW — Plan 02 Task 3:** Nummerierung der „SECHS Stellen" läuft 1,2,3,5,6,4 — Verwechslungsrisiko für den Executor, rein kosmetisch.
- **LOW — Plan 03 Objective:** Doppelter Satz („…`nerve_rt/`). `pip_stream` ist ein Mess-Label, kein DB-Wert." zweimal hintereinander) — Copy-Paste-Rest.

## 4. Suggestions

1. **Plan 01 Task 1, Kriterium 2 ersetzen durch CODE-Anker** (Muster aus Task 2 übernehmen):
   ```
   grep -rnE "(config|_cfg)\.CACHE_ANALYSE|^CACHE_ANALYSE[[:space:]]*=" --include=*.py services/ routes/ nerve_rt/
   ```
   liefert **0 Treffer**. (Der Erklär-Kommentar nennt den Namen absichtlich — gesucht ist die Abwesenheit von Definition und Leser, nicht des Namens. Gleiche Begründung wie in Task 2 bereits formuliert.)
2. **Plan 01 Task 1, Kriterium 6 ersetzen** durch den ohnehin in Plan 02 definierten dauerhaften Anker:
   ```
   grep -nE "getattr\([_a-z]*cfg, ?'CACHE_ANTWORT'|if +config\.CACHE_ANTWORT" services/claude_service.py
   ```
   == **0 Treffer** (gilt dauerhaft, nicht nur „zum Zeitpunkt dieses Plans"). Fußnote „Roh-String-Zaehler danach 2" streichen oder auf **3** korrigieren.
3. **Plan 02 Task 3, Kriterium 4 korrigieren:** `grep -c "CACHE_ANTWORT" services/claude_service.py` == **3** „(die zwei ersetzten Kommentarzeilen A)1/A)2 **plus** der Plan-01-Kommentar im Analyse-Pfad — kein Code)".
4. **Plan 03 Task 2, Gegenprobe korrigieren:** „…`grep -c cache_control …` ergibt beim korrekt gebauten Stand **6** (vier Zeilen aus Plan 02 Task 1 + die zwei Kommentar-Nachzüge aus Task 3 A)5/A)6)" — oder die Roh-Zahl ganz streichen und nur die zwei stabilen Anker behalten (`_block['cache_control'] …` == 1, `cache_control.*ephemeral` == 2).
5. **Plan 03 Task 4, Ersatztext präzisieren:** statt „Der früher hier genannte `context_tag='pip_stream'`" → „Die Beleg-Zeile nannte früher keinen `context_tag`; das aus der Messung kursierende `pip_stream` ist ein Mess-Label und existiert nicht als DB-Wert."

## 5. Risk Assessment

**MEDIUM.** Der eigentliche Code-Eingriff ist klein, wertbasiert korrekt konstruiert, per-User-gescoped (Cache-Poison-Filter real vorhanden und ab jetzt test-gesichert) und hat einen echten 30-Sekunden-Rollback (`CACHE_ANTWORT=false`). Latenz-Risiko ist minimal und wird gemessen statt behauptet. Das Restrisiko liegt nicht im Laufzeit-Verhalten, sondern in der Abnahme-Mechanik: Zwei HIGH-Widersprüche würden den Executor zum Löschen von Schutz-Kommentaren verleiten, und die falsche Post-Deploy-Zahl (6 statt 4) könnte einen unnötigen Rollback auslösen. Nach den fünf Korrekturen oben: **LOW** — dann sind die Pläne execute-reif.

---

## Consensus Summary

Die beiden Reviewer sind **nicht** einer Meinung — und der Widerspruch ist auflösbar:
Gemini hat rein aus dem Plan-Text geurteilt („OK, vorbildlich"), Fable hat den realen Code gelesen
und die grep-Zähler gegen den Ziel-Zustand nach ALLEN Tasks nachgerechnet. **Der Orchestrator hat
alle drei Fable-Befunde am Plan-Text und am Code nachverifiziert — sie sind echt.** Gemini ist an
genau der Fehlerklasse vorbeigelaufen, die der Spezial-Auftrag suchen sollte.

### Verifizierte Befunde (Orchestrator-Gegenprobe, nicht nur Reviewer-Behauptung)

**HIGH-1 — Plan 01, Task 1: Kriterium löscht seinen eigenen Erklär-Kommentar.**
`08.23.2.TEMPO-1-01-PLAN.md:149` schreibt den Kommentar `# CACHE_ANALYSE-Zweig verglich zusaetzlich …`
vor. `:177` verlangt `grep -rn "CACHE_ANALYSE" --include=*.py services/ routes/ nerve_rt/` == **0 Treffer**.
Nach korrekt ausgeführtem Task ist der Zähler **1** → der Executor löscht den Schutz-Kommentar, um
grün zu werden. (Task 2 desselben Plans, `:301`, macht es mit CODE-Anker-Regex bereits richtig.)

**HIGH-2 — Plan 01, Task 1: derselbe Widerspruch beim Schalter-Namen, mit Kaskade.**
`:151` schreibt `# answer_system_content, Schalter config.CACHE_ANTWORT).` vor; `:181` verlangt
`grep -n "config.CACHE_ANTWORT" services/claude_service.py` == **0 Treffer**. Real: **1**.
Folgefehler: die Fußnote `:184` („danach ist der Roh-String-Zaehler dort **2**") und
`08.23.2.TEMPO-1-02-PLAN.md:541` (`grep -c "CACHE_ANTWORT" services/claude_service.py` == **2**)
sind beide falsch — echter Endstand ist **3** (Plan-01-Kommentar + A)1 + A)2).

**MEDIUM-3 — Plan 03, Task 2: spät-scheiternde Zahl, geprüft NACH dem Prod-Deploy.**
`08.23.2.TEMPO-1-03-PLAN.md:208-209` behauptet, der Roh-String-Zähler
`grep -c cache_control services/prompt_pipeline.py` ergebe beim korrekt gebauten Stand **4**.
Verifiziert: heute enthält die Datei den String in 2 Zeilen (`:670-671`); Plan 02 Task 1 baut auf 4,
Plan 02 Task 3 A)5 (`:584` Docstring) und A)6 (`:619` Zeilen-Kommentar) ersetzen Text OHNE
„cache_control" durch Text MIT „cache_control" → Endstand **6**. Wer die Gegenprobe nach dem Deploy
fährt und 6 statt 4 sieht, hat exakt den false-red-Rollback-Reflex, vor dem der Plan selbst warnt.
Entschärfend: die Zeile ist ausdrücklich als „**kein** Abbruch-Kriterium" etikettiert und der
bindende Anker (`cache_control.*ephemeral` == 2) bleibt korrekt.

### Spezial-Auftrag — Antworten

1. **`pip_stream` → `context_tag IN ('ewb','qa')`: OK (beide Reviewer einig).** Korrektur ist
   durchgängig übernommen, auch in den Abnahmekriterien (Plan 03 must_haves, Task 3, Task 4 inkl.
   Whitelist der zwei legitimen Mess-Treffer in der ROADMAP). Fable verifizierte zusätzlich am Code,
   dass die Cost-Hooks real `'ewb'` (`claude_service.py:705/858`) und `'qa'` (`qa_pipeline.py:493`)
   schreiben. Einziger Rest (LOW): der Ersatztext in Plan 03 Task 4 A behauptet, `ROADMAP.md:2411`
   habe früher `pip_stream` genannt — dort stand nie ein `context_tag`. Formulierung anpassen,
   sonst schreibt die Korrektur eine neue Un-Wahrheit in die ROADMAP.

2. **Wiederkehrende Fehlerklasse: NICHT ausgerottet.** Drei Widersprüche leben noch (HIGH-1,
   HIGH-2, MEDIUM-3 oben). MEDIUM-3 ist genau der vom Nutzer beschriebene Typ „zeigt erst NACH
   erfolgreichem Prod-Deploy rot". Gemini hat hier ein falsches „OK" geliefert — nicht als
   Entlastung werten.

3. **`cache_control`-Zuordnung trotz Marker-Verwurf (`prompt_pipeline.py:683`): OK, wasserdicht
   (beide Reviewer einig, Fable am Code verifiziert).** Die vorgeschriebene `for`-Schleife liest
   `b.get('_layer') == 'stable'` am QUELL-Block, bevor der marker-freie Content-Dict gebaut wird —
   der Marker wird an der letzten Stelle konsumiert, an der er existiert. Kein Index-Raten.
   Fällt der Stabil-Block leer durch den `if not _text: continue`-Filter, rutscht der Volatil-Block
   zwar auf Index 0, matcht aber nie `== 'stable'` → kann den Marker **nicht** bekommen.
   `_cache_gesetzt` deckelt auf genau 1 Breakpoint; der Fallback wird ohne Marker neu gebaut.
   Der Anti-Cache-Poison-Filter (`:474-481`) existiert wie behauptet und filtert die Sektion-7-Anrede.

### Agreed Strengths

- Marker-Logik wertbasiert statt index-basiert, gegen alle LOCKED-Entscheidungen konsistent.
- Test-Netz prüft Runtime-Verhalten statt Source-Presence, inkl. Pflicht-Nachweis, dass der
  Index-Fallen-Test die Falle wirklich fängt.
- Echter Rollback ohne Deploy (`CACHE_ANTWORT=false`) + Wellen-Reihenfolge sauber sequenziert
  (Schalter W1 → Verdrahtung W2 → Deploy W3, gleiche Dateien nie parallel).
- Beleg-Führung in Plan 03 antizipiert RLS-False-Negative (psql als postgres), „Hooks schreiben nur
  bei >0" und Write-vor-Read (mindestens zwei Antworten) korrekt.
- Nebenfund `TEMPO1-KNOPF-MODELLNAME-FALSCH` real und korrekt als Backlog statt Scope-Creep behandelt.

### Divergent Views

| Punkt | Gemini | Fable 5 | Gegenprobe |
|---|---|---|---|
| Grep-Widersprüche | „keine gefunden, vorbildlich" | 2× HIGH + 1× MEDIUM | **Fable bestätigt** |
| Risk | LOW | MEDIUM (nach Fix: LOW) | **MEDIUM** |
| Doku-grep `CACHE_ANTWORT >= 3` | LOW-Bedenken, Inhalt statt Name prüfen | nicht bemängelt | optional |

### Fix-Liste vor Execute (mechanisch, 5 Edits)

1. **Plan 01 Task 1, Kriterium 2** ersetzen durch CODE-Anker (Muster aus Task 2):
   `grep -rnE "(config|_cfg)\.CACHE_ANALYSE|^CACHE_ANALYSE[[:space:]]*=" --include=*.py services/ routes/ nerve_rt/` == 0
2. **Plan 01 Task 1, Kriterium 6** ersetzen durch den dauerhaften Anker:
   `grep -nE "getattr\([_a-z]*cfg, ?'CACHE_ANTWORT'|if +config\.CACHE_ANTWORT" services/claude_service.py` == 0;
   Fußnote „danach 2" streichen oder auf **3** korrigieren.
3. **Plan 02 Task 3, Kriterium 4:** `grep -c "CACHE_ANTWORT" services/claude_service.py` == **3**.
4. **Plan 03 Task 2, Gegenprobe:** Roh-Zähler auf **6** korrigieren — oder die Roh-Zahl ersatzlos
   streichen und nur die zwei stabilen Anker behalten (empfohlen).
5. **Plan 03 Task 4, Ersatztext:** „Die Beleg-Zeile nannte früher keinen `context_tag`; das aus der
   Messung kursierende `pip_stream` ist ein Mess-Label und existiert nicht als DB-Wert."

Optional (LOW): Plan 02 Task 1 „vier Zeilen" als „Stand nach Task 1" etikettieren; Nummerierung
„SECHS Stellen" läuft 1,2,3,5,6,4; doppelter Satz in Plan 03 Objective.

**Gesamt-Risiko: MEDIUM** — nicht im Laufzeit-Verhalten (Eingriff klein, per-User gescoped,
30-Sekunden-Rollback), sondern in der Abnahme-Mechanik. Nach den 5 Korrekturen: LOW, execute-reif.
