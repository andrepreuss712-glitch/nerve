---
phase: 08.23.2.TEMPO-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/claude_service.py
  - services/qa_pipeline.py
  - config.py
  - tests/test_08_13_01_config_constants.py
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
autonomous: false
requirements: [T1-C1, T1-C2, T1-C3, T1-C4, T1-C5, T1-C6, T1-B1, T1-B2, T1-B3]
complexity: "🟡 — reine Entfernung von totem Code + EIN neuer Konfig-Schalter. Kein Live-Verhalten geaendert (der entfernte Zweig war beweisbar ein No-Op). Deshalb Welle 1."

# ★★ NICHT auto-advance. Cross-AI (Claudian-Pre-Execute-Audit + Fable/Gemini-Gegencheck) VOR Bau.
#    Deploy faehrt Claudian (Plan 03), nicht dieser Plan.

must_haves:
  truths:
    - "Der Analyse-Pfad (analysiere_mit_claude) laeuft weiterhin UNCACHED — system ist danach ein reiner String (SYSTEM_PROMPT_BASE), kein Block-Array."
    - "Es gibt im gesamten Repo keinen Konsumenten und keine Definition von CACHE_ANALYSE, CACHE_EWB, CACHE_QA mehr."
    - "Es gibt im gesamten Repo keine Konstante _CACHE_MIN_CHARS mehr (weder in claude_service.py noch in qa_pipeline.py)."
    - "config.py definiert genau EINEN Cache-Schalter: CACHE_ANTWORT, ENV-overridable, default True."
    - "tests/test_08_13_01_config_constants.py prueft CACHE_ANTWORT (Default True, Typ bool, ENV-Override) statt der drei abgeloesten Schalter — die Test-Klasse ist nicht ersatzlos geloescht."
    - "Das Test-Gate (pytest -m 'not live and not perf') ist gruen; die Baseline-Zahl sinkt nicht unter den Stand vor diesem Plan minus der 4 ersetzten Tests. VERBINDLICH ist der Server-Lauf im Pytest-Gate von deploy.sh production (Plan 03, Task 2) — nicht ein lokaler Lauf."
    - "Die Projekt-Doku (.planning/codebase/ARCHITECTURE.md, INTEGRATIONS.md, STACK.md) beschreibt keine der drei abgeloesten ENV-Schalter mehr als lebend — sie nennt CACHE_ANTWORT."
  artifacts:
    - path: "config.py"
      provides: "EIN ehrlicher Rollback-Schalter CACHE_ANTWORT (default true) fuer den gemeinsamen Antwort-System-Prompt; die drei alten Schalter sind entfernt"
      contains: "CACHE_ANTWORT"
    - path: "services/claude_service.py"
      provides: "Analyse-Pfad ohne toten Cache-Zweig; _system = SYSTEM_PROMPT_BASE als String"
      contains: "_system = SYSTEM_PROMPT_BASE"
    - path: "services/qa_pipeline.py"
      provides: "Waisen-Konstante _CACHE_MIN_CHARS entfernt"
    - path: "tests/test_08_13_01_config_constants.py"
      provides: "Nachgezogener Test-Contract fuer CACHE_ANTWORT (Punkt 18)"
      contains: "CACHE_ANTWORT"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Projekt-Doku auf den Ist-Stand: EIN Schalter CACHE_ANTWORT statt der drei abgeloesten"
      contains: "CACHE_ANTWORT"
  key_links:
    - from: "config.CACHE_ANTWORT"
      to: "services/prompt_pipeline.answer_system_content (Plan 02, Welle 2)"
      via: "lazy import config in der Marker-Verzweigung"
      pattern: "CACHE_ANTWORT"
---

<objective>
**Welle 1: den toten Cache-Apparat entfernen und EINEN ehrlichen Schalter an seine Stelle setzen.**

Heute stehen in `config.py` drei Cache-Schalter, von denen **keiner** wirkt:
- `CACHE_ANALYSE` steuert einen Zweig, der nie greifen kann (`SYSTEM_PROMPT_BASE` = 6.398 Zeichen;
  Haiku 4.5 verlangt 4.096 **Tokens** ≈ 16.000 Zeichen — und der Vergleich in
  `claude_service.py:528` stellt ohnehin **Zeichen gegen eine Token-Grenze**).
- `CACHE_EWB` und `CACHE_QA` haben **null Konsumenten** im gesamten Repo (grep-belegt: nur `config.py`
  + die Tests, die sie pruefen).

An ihre Stelle tritt **ein** Schalter, `CACHE_ANTWORT` — der Rollback-Pfad fuer die Aktivierung,
die Plan 02 baut (CLAUDE.md Punkt 12, Saeule 3: jede automatische Entscheidung braucht einen
Eingriffs-Pfad, 30 Sekunden statt Deploy).

**Warum EIN Schalter und nicht zwei (B2, LOCKED):** alle drei Antwort-Pfade (Auto/EWB, Knopf, QA)
ziehen seit TAXO3-P1-02 **denselben** zusammengebauten Text aus `answer_system_content()` **und**
laufen auf **demselben** Modell (`claude-sonnet-4-5`: `config.py:74` MODEL_EWB, `:75` MODEL_QA,
`:93` MODEL_PIP_AUTOVAR, `:94` MODEL_PIP_VARIANTE). Gleiches Modell + gleicher Prefix = **ein**
Cache-Eintrag. Zwei getrennte Schalter wuerden vorgaukeln, man koenne die Pfade einzeln steuern —
kann man nicht: schaltet man einen aus, haelt der andere den Speicher trotzdem warm. Das ist die
„der Name luegt"-Klasse (Regel vom 2026-07-03).

**Der Analyse-Pfad bleibt bewusst uncached** (C3). Das ist keine Vergesslichkeit, sondern das
Ergebnis von Fund 1: dort lohnt es sich nachweislich nicht.

**★ STOP-Signal:** dieser Plan aktiviert **nichts** und baut **keine** Cache-Logik. Er entfernt und
legt einen Schalter an. Wer hier anfaengt, `cache_control` zu setzen → **STOP**, das ist Plan 02.
</objective>

<context>
@.planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-CONTEXT.md

**Pflicht-Pre-Read (in dieser Reihenfolge):**
1. `08.23.2.TEMPO-1-CONTEXT.md` — Abschnitte **B** (Schalter), **C** (toter Code), `<deferred>`
2. `.planning/DIALOG-GSD-CLAUDIAN.md:259-322` — die beiden Pre-Check-Belege im Volltext
3. `CLAUDE.md` — Punkt 12 (Eingriffs-Pfad), Punkt 14 (tote Pfade), Punkt 18 (Test-Contract)

**Grep-Beleg zum Ausgangsstand (2026-07-21, verifiziert):**
```
config.py:100-105          Kommentare + Definitionen der drei Schalter
services/claude_service.py:10-11    stale Kommentar + _CACHE_MIN_CHARS
services/claude_service.py:527-534  der tote Cache-Zweig
services/qa_pipeline.py:48-49       Waisen-Konstante (0 Verwendungen in der Datei)
tests/test_08_13_01_config_constants.py:104-123  die vier Tests auf die alten Schalter
```
Ausserhalb dieser Stellen: **kein** weiterer Treffer fuer `CACHE_EWB|CACHE_QA|CACHE_ANALYSE|_CACHE_MIN_CHARS`.

**Latenz-Budget (CLAUDE.md Punkt 25):** dieser Plan **entfernt** Code aus dem Analyse-Pfad
(ein `if` + ein `len()` + ein `print` weniger pro Analyse-Call). Erwarteter Latenz-Effekt: 0 bis
minimal positiv. Kein neuer Aufruf, kein neuer Import im Hot-Path.
</context>

<tasks>

### Task 1 — Tote Cache-Mechanik entfernen (claude_service.py + qa_pipeline.py)

<read_first>
- `services/claude_service.py` — Zeilen 1-30 (Kopf, `_CACHE_MIN_CHARS`) UND 512-572
  (`analysiere_mit_claude` komplett, inkl. Cost-Hook danach)
- `services/qa_pipeline.py` — Zeilen 36-52 (Import-Kopf + Konstanten)
- `08.23.2.TEMPO-1-CONTEXT.md` Abschnitt **C** (C3/C4/C5) — die Begruendung, warum das ein No-Op ist
- `CLAUDE.md` Punkt 14 (tote Pfade entfernen, nicht auskommentieren)
</read_first>

<action>
**A) `services/claude_service.py` — Kopf (heute Zeilen 10-11):**

Diese beiden Zeilen **ersatzlos loeschen**:
```python
# Anthropic minimum: 1024 tokens ≈ 4096 chars
_CACHE_MIN_CHARS = 4096
```
(C5: der Kommentar ist fuer Haiku 4.5 falsch — dort sind es 4.096 **Tokens**, nicht 1.024.
C4: die Konstante hat nach B) keinen Leser mehr.)

**B) `services/claude_service.py` — der Cache-Zweig in `analysiere_mit_claude` (heute Zeilen 526-535):**

Heutiger Stand:
```python
    _system_prompt = SYSTEM_PROMPT_BASE
    # ── Phase 08.13: Prompt-Caching Analyse-Loop (CACHE_ANALYSE=False default) ──
    if config.CACHE_ANALYSE and len(_system_prompt) >= _CACHE_MIN_CHARS:
        _system = [{"type": "text", "text": _system_prompt, "cache_control": {"type": "ephemeral"}}]
    else:
        _system = _system_prompt  # String, kein Cache
    if config.CACHE_ANALYSE:
        print(f"[Cache-Check] analyse system_prompt: {len(_system_prompt)} chars, "
              f"threshold {_CACHE_MIN_CHARS}, cache={'on' if len(_system_prompt) >= _CACHE_MIN_CHARS else 'off'}")
    # ──────────────────────────────────────────────────────────────────────────
```

Ziel-Stand (exakt diese Zeilen, inkl. Begruendungs-Kommentar):
```python
    # TEMPO-1 (C3): der Analyse-Pfad bleibt bewusst UNCACHED. SYSTEM_PROMPT_BASE ist
    # ~6.400 Zeichen; Haiku 4.5 verlangt als cachebaren Prefix 4.096 TOKENS (~16.000
    # Zeichen) — ein cache_control haette hier nie gegriffen. Der frueher hier stehende
    # CACHE_ANALYSE-Zweig verglich zusaetzlich Zeichen gegen eine Token-Grenze.
    # Gecacht wird ausschliesslich der ANTWORT-Prompt (services/prompt_pipeline.py,
    # answer_system_content, Schalter config.CACHE_ANTWORT).
    _system = SYSTEM_PROMPT_BASE
```
Der `messages.create(...)`-Aufruf darunter bleibt **unveraendert** (`system=_system`) — er bekommt
jetzt garantiert einen String statt eines Block-Arrays.

**Wichtig:** die lokale Variable heisst danach `_system`; `_system_prompt` existiert nicht mehr.
Pruefen, dass es **keinen** weiteren Leser von `_system_prompt` in dieser Funktion gibt (es gibt
keinen — der einzige stand in dem geloeschten `print`).

**C) `services/qa_pipeline.py` — Waisen-Konstante (heute Zeilen 48-49):**

Diese beiden Zeilen **ersatzlos loeschen**:
```python
# Anthropic minimum: 1024 tokens ≈ 4096 chars
_CACHE_MIN_CHARS = 4096
```
Sie hat in `qa_pipeline.py` **null** Verwendungen (grep-belegt) — reine Kopier-Waise aus 08.13.

**Nicht anfassen in diesem Task:** die Kommentare in `claude_service.py:613-619` / `:772-776` und
`qa_pipeline.py:413-418` / `:439-442`, die „Phase 1: PLAIN Bloecke ohne cache_control" sagen. Die
werden erst mit der Aktivierung falsch und werden in **Plan 02, Task 3** nachgezogen.
</action>

<acceptance_criteria>
- `grep -rn "_CACHE_MIN_CHARS" --include=*.py .` liefert **0 Treffer**
- `grep -rnE "(config|_cfg)\.CACHE_ANALYSE|^CACHE_ANALYSE[[:space:]]*=" --include=*.py services/ routes/ nerve_rt/`
  liefert **0 Treffer**
  (**CODE-Anker, nicht Roh-String:** der oben vorgeschriebene Ersatz-Kommentar nennt „der frueher
  hier stehende CACHE_ANALYSE-Zweig …" **absichtlich**. Ein Roh-String-grep faende genau diesen
  Schutz-Kommentar und wuerde den Executor dazu bringen, ihn zu loeschen, nur um gruen zu werden.
  Gesucht ist die Abwesenheit von **Definition und Leser**, nicht die des Namens — gleiches Muster
  wie Task 2 dieses Plans, der es bereits richtig macht.)
- `grep -rn "Cache-Check" --include=*.py .` liefert **0 Treffer**
- `grep -n "_system = SYSTEM_PROMPT_BASE" services/claude_service.py` liefert **genau 1 Treffer**
- `grep -n "_system_prompt" services/claude_service.py` liefert **0 Treffer**
- `grep -nE "getattr\([_a-z]*cfg, ?'CACHE_ANTWORT'|if +config\.CACHE_ANTWORT" services/claude_service.py`
  liefert **0 Treffer** — und zwar **dauerhaft**, nicht nur „zum Zeitpunkt dieses Plans".
  Fachlich: der Schalter gehoert NICHT in den Analyse-Pfad (B4 — eine Verzweigung, nicht drei).
  (**CODE-Anker, nicht Roh-String:** der oben vorgeschriebene Ersatz-Kommentar nennt „Schalter
  config.CACHE_ANTWORT" absichtlich, und Plan 02 Task 3 A) ergaenzt in derselben Datei zwei weitere
  KOMMENTAR-Erwaehnungen. Ein Roh-String-Zaehler waere hier ein bewegliches Ziel und wuerde den
  Executor dazu bringen, Schutz-Kommentare zu loeschen.)
- `python -c "import ast,sys; ast.parse(open('services/claude_service.py',encoding='utf-8').read())"` exit 0
- `python -c "import ast,sys; ast.parse(open('services/qa_pipeline.py',encoding='utf-8').read())"` exit 0
</acceptance_criteria>

**Commit:** `refactor(TEMPO-1-01): toten CACHE_ANALYSE-Zweig + _CACHE_MIN_CHARS entfernt (Analyse bleibt uncached)`

---

### Task 2 — config.py: drei tote Schalter raus, EIN ehrlicher rein

<read_first>
- `config.py` — Zeilen 70-110 (MODEL-Block + der CACHE-Toggle-Block, den dieser Task ersetzt)
- `08.23.2.TEMPO-1-CONTEXT.md` Abschnitt **B** (B1-B4) — insbesondere die B2-Begruendung, die
  woertlich in den Kommentar wandert
- `CLAUDE.md` Punkt 12 (Eingriffs-Pfad / Rollback ohne Deploy)
</read_first>

<action>
`config.py` Zeilen 99-105 (der gesamte Block „Phase 08.13: CACHE-Toggles") wird **ersetzt**.

Heutiger Stand:
```python
# ── Phase 08.13: CACHE-Toggles (ENV-overridable, per Decision 3) ───────────────
# CACHE_EWB: EWB-Generation cachen (System-Prompt gross genug — default an)
# CACHE_QA: QA-Response cachen (System-Prompt gross genug — default an)
# CACHE_ANALYSE: Analyse-Loop cachen (System-Prompt kuerzer — default AUS)
CACHE_EWB     = os.getenv("CACHE_EWB",     "true").lower()  == "true"
CACHE_QA      = os.getenv("CACHE_QA",      "true").lower()  == "true"
CACHE_ANALYSE = os.getenv("CACHE_ANALYSE", "false").lower() == "true"
```

Ziel-Stand (exakt dieser Block):
```python
# ── TEMPO-1: EIN Cache-Schalter fuer den gemeinsamen Antwort-System-Prompt ────
# Abgeloest: CACHE_EWB / CACHE_QA (hatten null Konsumenten) und CACHE_ANALYSE
# (steuerte einen Zweig, der nie greifen konnte — SYSTEM_PROMPT_BASE ~6.400 Zeichen
# gegen 4.096 TOKENS Mindest-Prefix bei Haiku 4.5).
#
# Warum EIN Schalter und nicht drei: alle drei Antwort-Pfade (Auto/EWB, Knopf, QA)
# ziehen seit TAXO3-P1-02 DENSELBEN Text aus prompt_pipeline.answer_system_content()
# und laufen auf DEMSELBEN Modell (MODEL_EWB/MODEL_QA/MODEL_PIP_AUTOVAR/
# MODEL_PIP_VARIANTE = claude-sonnet-4-5, siehe oben). Gleiches Modell + gleicher
# Prefix = EIN Cache-Eintrag. Getrennte Schalter wuerden vorgaukeln, man koenne die
# Pfade einzeln steuern — schaltet man einen aus, haelt der andere den Speicher
# trotzdem warm.
#
# Wirkt an genau EINER Stelle: services/prompt_pipeline.py, answer_system_content()
# (cache_control auf dem _layer='stable'-Block). Rollback ohne Deploy:
# CACHE_ANTWORT=false in /etc/nerve/.env eintragen und den Dienst neu starten
# (systemctl restart nerve). ACHTUNG: /opt/nerve/app/.env existiert auf Prod NICHT
# (deploy.sh:54 schliesst ./.env vom tar-Deploy aus, :42-45 prueft /etc/nerve/.env).
# Der ANALYSE-Pfad (claude_service.analysiere_mit_claude) ist bewusst uncached und
# wird von diesem Schalter NICHT beruehrt.
CACHE_ANTWORT = os.getenv("CACHE_ANTWORT", "true").lower() == "true"
```

**Zwischenzustand ist beabsichtigt:** nach diesem Task hat `CACHE_ANTWORT` noch keinen Leser —
Plan 02 (Welle 2) verdrahtet ihn. Beide Wellen gehen mit **einem** Deploy live (Plan 03), es
entsteht also kein toter Schalter auf Production.

**D) Projekt-Doku nachziehen — sonst dokumentiert die Doku drei Schalter, die es nicht gibt.**

Das ist dieselbe „der Name luegt"-Klasse, die dieser Plan im Code gerade behebt: `.planning/codebase/`
beschreibt die drei Schalter als lebende ENV-Schalter, **inklusive der durch diese Phase widerlegten
Begruendung** „EWB system prompt cached (large enough, >4096 chars)". Vier Stellen, konkret:

1. `.planning/codebase/ARCHITECTURE.md:207-208` — der Block **Prompt Caching (Anthropic)**, heute:
   ```markdown
   - EWB and QA system prompts use Anthropic prompt caching (`cache_control: {"type": "ephemeral"}`) when prompt length > 4096 chars (`_CACHE_MIN_CHARS`)
   - Toggled per module via `CACHE_EWB` / `CACHE_QA` env vars; `CACHE_ANALYSE=false` by default (analyse_loop prompt too short)
   ```
   → ersetzen durch:
   ```markdown
   - The shared answer system prompt (`prompt_pipeline.answer_system_content`) carries `cache_control: {"type": "ephemeral"}` on the `_layer='stable'` block only — one breakpoint per request (TEMPO-1)
   - Single ENV switch `CACHE_ANTWORT` (default `true`); rollback without deploy. The analyse loop (`claude_service.analysiere_mit_claude`) stays deliberately UNCACHED — `SYSTEM_PROMPT_BASE` is ~6.400 chars, far below the 4.096-TOKEN minimum prefix
   - The previous per-module cache toggles and the char-vs-token threshold constant were removed in TEMPO-1 (no consumers / the comparison could never fire)
   ```

2. `.planning/codebase/INTEGRATIONS.md:39-43` — der Block **Prompt caching (Anthropic cache_control)**,
   heute vier Zeilen (`CACHE_EWB = true`, `CACHE_QA = true`, `CACHE_ANALYSE = false`, „Minimum cache
   threshold: 4096 chars (`_CACHE_MIN_CHARS` …)")
   → ersetzen durch:
   ```markdown
   - `CACHE_ANTWORT = true` — the shared answer system prompt (Auto/EWB, button, QA) is cached on its stable block; ENV-overridable, rollback without deploy
   - Marker sits in `services/prompt_pipeline.py` (`answer_system_content`), on the `_layer='stable'` block only — never on the volatile block
   - Analyse loop deliberately uncached (prompt below the model's minimum cacheable prefix of 4.096 TOKENS)
   ```
   **Achtung:** die Behauptung „large enough, >4096 chars" ist die von dieser Phase widerlegte —
   sie darf nicht in anderer Formulierung ueberleben.

3. `.planning/codebase/STACK.md:77` — heute die Zeile
   ```markdown
     - Prompt caching: `CACHE_EWB=true`, `CACHE_QA=true`, `CACHE_ANALYSE=false`
   ```
   → ersetzen durch
   ```markdown
     - Prompt caching: `CACHE_ANTWORT=true` (single switch, stable answer prefix; analyse loop uncached)
   ```

4. `.planning/codebase/STACK.md:146` — die ENV-Tabellenzeile
   ```markdown
   | `CACHE_EWB/QA/ANALYSE` | Prompt caching toggles | `true/true/false` |
   ```
   → ersetzen durch
   ```markdown
   | `CACHE_ANTWORT` | Answer-prompt caching toggle (stable prefix) | `true` |
   ```

**★ Eingrenzung (WICHTIG):** angefasst werden **ausschliesslich** `.planning/codebase/`, `.env.example`
und `deploy.sh`. `.env.example` und `deploy.sh` wurden geprueft: **kein Treffer**, dort ist nichts zu
tun. **Historische Phasen-Dokumente (08.13, 08.20, TAXO3, …) NICHT anfassen** — die sind Protokoll
eines vergangenen Zustands und bleiben unveraendert.
</action>

<acceptance_criteria>
- `grep -c "CACHE_ANTWORT = os.getenv(\"CACHE_ANTWORT\", \"true\").lower() == \"true\"" config.py` == **1**
- `grep -rnE "^(CACHE_EWB|CACHE_QA|CACHE_ANALYSE)[[:space:]]*=|(config|_cfg)\.(CACHE_EWB|CACHE_QA|CACHE_ANALYSE)" --include=*.py . | grep -v "^\./tests/"` liefert **0 Treffer**
  (**CODE-Anker, nicht Roh-String:** der oben vorgeschriebene Ziel-Kommentar in `config.py` nennt
  „Abgeloest: CACHE_EWB / CACHE_QA … und CACHE_ANALYSE" woertlich — ein Roh-String-grep wuerde
  **4 Zeilen** finden und den Executor dazu bringen, genau diesen Erklaer-Kommentar zu loeschen.
  Gesucht ist die Abwesenheit von **Definition und Leser**, nicht die des Namens.)
- `grep -n "MODEL_PIP_AUTOVAR" config.py` liefert weiterhin Treffer (der MODEL-Block ist **nicht** angefasst)
- **(entfaellt als Abnahme — CLAUDE.md HART verbietet lokale Smoke-Tests via `python -c "import ..."`.)**
  Default `True` und der ENV-Override `false` werden von Task 3 als echte Tests abgedeckt
  (`test_cache_antwort_default_true`, `test_cache_antwort_env_override_false`); verbindliches Tor ist
  das Pytest-Gate in `deploy.sh production` (Plan 03, Task 2). Wer lokal ein Vorab-Signal will, darf
  `python -c "import config; ..."` laufen lassen — es ist **kein Abnahme-Kriterium**.
- `grep -rn "CACHE_EWB\|CACHE_QA\|CACHE_ANALYSE\|_CACHE_MIN_CHARS" .planning/codebase/ .env.example deploy.sh`
  liefert **0 Treffer** (Doku-Nachzug D; **nur** dieser Suchraum — historische Phasen-Dokumente
  bleiben unangetastet). **Achtung:** auch der neue Ersatztext darf die drei Namen **nicht** wieder
  nennen — deshalb steht dort „the previous per-module cache toggles" statt einer Aufzaehlung.
- `grep -rn "CACHE_ANTWORT" .planning/codebase/` liefert **>= 3 Treffer** (ARCHITECTURE, INTEGRATIONS, STACK)
- `grep -rn "large enough, >4096 chars" .planning/codebase/` liefert **0 Treffer**
  (die von dieser Phase widerlegte Begruendung ist weg, nicht nur umformuliert)
</acceptance_criteria>

**Commit:** `feat(TEMPO-1-01): CACHE_ANTWORT als EINZIGER Cache-Schalter (drei tote Schalter entfernt) + Projekt-Doku nachgezogen`

---

### Task 3 — Test-Contract nachziehen (Punkt 18: ersetzen, nicht loeschen)

<read_first>
- `tests/test_08_13_01_config_constants.py` — Zeilen 1-40 (Kopf + `fresh_config()`-Helfer)
  UND 104-140 (`TestCacheBooleans` + `TestEnvOverride` als Muster fuer den ENV-Override-Test)
- `CLAUDE.md` Punkt 18 / Test-Contract-Regel: Vertraege aendern sich → Tests werden NACHGEZOGEN
- `CLAUDE.md` Abschnitt „Test-Qualitaets-Regel" — der Test muss Runtime-Verhalten pruefen
  (Attribut-Wert nach `importlib.reload`), keine Source-Presence
</read_first>

<action>
`tests/test_08_13_01_config_constants.py`, Klasse `TestCacheBooleans` (heute Zeilen 104-123) wird
**ersetzt** — nicht geloescht. Die vier Tests
(`test_cache_ewb_default_true`, `test_cache_qa_default_true`, `test_cache_analyse_default_false`,
`test_cache_booleans_are_bool_type`) werden durch die `CACHE_ANTWORT`-Aequivalente ersetzt:

```python
class TestCacheBooleans:
    """TEMPO-1: EIN Cache-Schalter (CACHE_ANTWORT) statt der drei abgeloesten
    CACHE_EWB/CACHE_QA/CACHE_ANALYSE. Der Vertrag hat sich geaendert, der Test wird
    nachgezogen (CLAUDE.md Punkt 18) — nicht ersatzlos entfernt."""

    def test_cache_antwort_default_true(self):
        cfg = fresh_config()
        assert cfg.CACHE_ANTWORT is True

    def test_cache_antwort_is_bool_type(self):
        cfg = fresh_config()
        assert isinstance(cfg.CACHE_ANTWORT, bool)

    def test_cache_antwort_env_override_false(self, monkeypatch):
        """Rollback-Pfad ohne Deploy (CLAUDE.md Punkt 12): ENV CACHE_ANTWORT=false."""
        monkeypatch.setenv('CACHE_ANTWORT', 'false')
        import importlib
        import config
        importlib.reload(config)
        assert config.CACHE_ANTWORT is False
        monkeypatch.delenv('CACHE_ANTWORT', raising=False)
        importlib.reload(config)
        assert config.CACHE_ANTWORT is True

    def test_abgeloeste_schalter_sind_weg(self):
        """Die drei alten Schalter duerfen NICHT wieder auftauchen — sonst kehrt die
        'zwei Schalter fuer einen Cache-Eintrag'-Luege zurueck (B2)."""
        cfg = fresh_config()
        for _tot in ('CACHE_EWB', 'CACHE_QA', 'CACHE_ANALYSE'):
            assert not hasattr(cfg, _tot), f'{_tot} lebt wieder — TEMPO-1 rueckgaengig gemacht?'
```

**Zum ENV-Override-Test:** das Cleanup-Muster (delenv + reload + Re-Assert) ist Pflicht, sonst
vergiftet der Reload nachfolgende Tests im selben Prozess — genau so macht es der bestehende
`TestEnvOverride.test_model_ewb_env_override` in derselben Datei (Zeilen 129-134). Dessen
Cleanup-Zeilen vor dem Schreiben lesen und spiegeln.

**Kein Source-Presence-Test:** `test_abgeloeste_schalter_sind_weg` prueft ein **Modul-Attribut zur
Laufzeit** (`hasattr` auf dem frisch geladenen Config-Modul), nicht den Dateiinhalt — das ist die
erlaubte Form (Runtime-API-Schnittstelle), keine `getsource`/`open().read()`-Pruefung.
</action>

<acceptance_criteria>
- `grep -c "CACHE_ANTWORT" tests/test_08_13_01_config_constants.py` >= **6**
- `grep -n "cfg.CACHE_EWB is True\|cfg.CACHE_QA is True\|cfg.CACHE_ANALYSE is False" tests/test_08_13_01_config_constants.py` liefert **0 Treffer**
- `grep -n "class TestCacheBooleans" tests/test_08_13_01_config_constants.py` liefert **1 Treffer**
  (die Klasse existiert weiter — Vertrag nachgezogen, nicht geloescht)
- `pytest tests/test_08_13_01_config_constants.py -q` exit **0** — **Vorab-Signal, nicht Abnahme**
- `pytest -m "not live and not perf" -q` exit **0** — **Vorab-Signal, nicht Abnahme**

**★ Abnahme-Etikett (CLAUDE.md HART: „KEIN lokales `pytest` als Acceptance-Check"):** die beiden
lokalen Laeufe oben sind ein **Vorab-Signal**. **Verbindliches Tor ist das Pytest-Gate in
`deploy.sh production` (Plan 03, Task 2).** Die Test-Zahl (Baseline ~916 passed; 4 Tests raus,
4 Tests rein — sie darf nicht **sinken**) wird **gegen den Server-Lauf** verglichen, **nicht** gegen
einen lokalen: lokal skippen die real-PG-Tests (Schild-Guard, Baseline-Cleanup-Waechter), ein
lokaler Zahlenvergleich gegen eine Server-Baseline traegt nicht.
</acceptance_criteria>

**★ Fussnote fuer den Pre-Execute-Audit (Rest-Risiko, W-4 aus dem Plan-Check):** liegt auf Prod eine
manuell angelegte `/opt/nerve/app/.env` mit `CACHE_ANTWORT=false` (Rollback-Zustand), wuerde
`test_cache_antwort_default_true` im Deploy-Gate rot und blockierte damit jeden weiteren Deploy.
Das ist eine **Live-Pruefung**, kein Plan-Edit — im Claudian-Pre-Execute-Audit fahren:
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'ls -la /opt/nerve/app/.env'
```
Existiert die Datei, im Test einen Guard ergaenzen (`pytest.skip` bei gesetztem ENV-Override).
Erwartung nach `deploy.sh:54` (`./.env` vom tar ausgeschlossen): sie existiert **nicht**.

**Commit:** `test(TEMPO-1-01): Test-Contract auf CACHE_ANTWORT nachgezogen (Punkt 18)`

</tasks>

<verify>
  <automated>pytest -m "not live and not perf" -q   # VORAB-SIGNAL, keine Abnahme (siehe unten)</automated>
  <automated>grep -rnE "_CACHE_MIN_CHARS|^(CACHE_EWB|CACHE_QA|CACHE_ANALYSE)[[:space:]]*=|(config|_cfg)\.(CACHE_EWB|CACHE_QA|CACHE_ANALYSE)" --include=*.py . | grep -v "^\./tests/" ; test $? -eq 1</automated>
  <manual>Kein Deploy in diesem Plan. Deploy + Live-Beleg fahren gebuendelt in Plan 03.</manual>
  <manual>★ Lokales pytest ist hier VORAB-SIGNAL, nicht Abnahme (CLAUDE.md HART: kein lokales pytest als
  Acceptance-Check). Verbindliches Tor ist das Pytest-Gate in `deploy.sh production` (Plan 03, Task 2).
  Die Test-Zahl wird gegen den Server-Lauf verglichen, nicht gegen einen lokalen — lokal skippen die
  real-PG-Tests (Schild-Guard, Baseline-Cleanup-Waechter), die Zahlen sind nicht vergleichbar.</manual>
  <manual>★ Warum der grep-Ausdruck auf CODE-Anker greift und nicht auf die Roh-Bezeichner: der neue
  Kommentarblock in `config.py` (Task 2) und der Waechter-Test `test_abgeloeste_schalter_sind_weg`
  (Task 3) NENNEN die drei abgeloesten Namen absichtlich. Ein Roh-String-grep wuerde diese
  Schutz-Stellen als Verstoss melden — und der Executor wuerde genau den Schutz loeschen.</manual>
</verify>

<success_criteria>
- Alle `must_haves.truths` erfuellt (grep-belegt, nicht behauptet)
- Test-Gate gruen, Test-Zahl nicht gesunken
- Der Analyse-Pfad ist unveraendert im Verhalten: `system=` bekommt einen String, exakt wie vorher
  (der entfernte Zweig war beweisbar nie aktiv — `CACHE_ANALYSE` default `false`)
- **Nichts aktiviert.** Kein `cache_control` irgendwo neu gesetzt.
</success_criteria>

## 1. Lokaler Kontext (Punkt 14 Schicht 1)

- `analysiere_mit_claude` (`claude_service.py:512-572`): einziger Ausgangspfad ist der `return` am Ende;
  der geloeschte Block hat **keine** Verzweigung ausser der Zuweisung an `_system`. Kein `try` umschliesst
  ihn, kein `finally` haengt daran.
- `qa_pipeline.py:48-49`: Modul-Ebene, keine Funktion, kein Aufrufer.
- `config.py:99-105`: Modul-Ebene, wird beim Import einmal ausgewertet.

## 2. Funktions-Skelett

`analysiere_mit_claude(neuer_text, kontext, sid)`:
`user_msg` bauen → **[hier der Eingriff]** `_system` setzen → `claude_client.messages.create(system=_system, …)`
→ Cost-Hook (`try/except`, unberuehrt) → `if not msg.content: return {}` → `_parse_json(...)`.
Der Eingriff ersetzt **genau** die Zuweisung, nichts davor und nichts danach.

## 3. Cross-File-Awareness (Punkt 14 Schicht 3, grep-belegt)

| Symbol | Leser vor diesem Plan | Nach diesem Plan |
|---|---|---|
| `_CACHE_MIN_CHARS` | `claude_service.py:528,534` (dieselbe Datei), `qa_pipeline.py:49` (Definition ohne Leser) | entfaellt |
| `config.CACHE_ANALYSE` | `claude_service.py:528,532` | entfaellt |
| `config.CACHE_EWB` / `config.CACHE_QA` | **niemand** ausser `tests/test_08_13_01_config_constants.py` | entfaellt |
| `config.CACHE_ANTWORT` (neu) | noch niemand → Plan 02 verdrahtet | `prompt_pipeline.answer_system_content` |

## 4. Edge-Case-Analyse

1. **`system=` erwartet jetzt garantiert einen String** — vorher konnte es (bei `CACHE_ANALYSE=true`)
   ein Block-Array sein. Da der Schalter default `false` ist und auf Prod nicht gesetzt ist
   (`inspect.sh env-keys` zeigt ihn nicht), ist der Live-Zustand **identisch**. Trotzdem im
   Pre-Execute-Audit gegen `inspect.sh env-keys` gegenpruefen: steht `CACHE_ANALYSE` doch in der
   Prod-`.env`, aendert dieser Plan Live-Verhalten → melden, nicht stillschweigend bauen.
2. **`CACHE_ANTWORT` faelschlich als „true"-String gelesen:** `os.getenv(...).lower() == "true"`
   liefert immer einen echten `bool` — der Typ-Test in Task 3 nagelt das fest.
3. **Reload-Vergiftung im Test-Prozess:** `importlib.reload(config)` ohne Cleanup wuerde nachfolgende
   Tests beeinflussen → Cleanup-Muster aus `TestEnvOverride` gespiegelt (Task 3).
4. **Ein zweiter Konsument von `CACHE_EWB` in nicht-Python-Dateien:** vor dem Commit **eingegrenzt**
   pruefen —
   ```bash
   grep -rn "CACHE_EWB\|CACHE_QA\|CACHE_ANALYSE\|_CACHE_MIN_CHARS" .planning/codebase/ .env.example deploy.sh
   ```
   Treffer bereinigen (Task 2 D). **NICHT** repo-weit greppen: ein `grep -rn … . --exclude-dir=.git`
   trifft zusaetzlich ~20 **historische** Phasen-Dokumente (08.13, 08.20, TAXO3). Die sind Protokoll
   eines vergangenen Zustands und werden **NICHT angefasst**. Stand 2026-07-21: `.env.example` und
   `deploy.sh` haben **keinen** Treffer — dort ist nichts zu tun.

### Race-Condition-Audit (Punkt 14, vier Pflicht-Fragen)
1. **Gleichzeitiges Feuern:** n/a — reine Konstanten-/Zweig-Entfernung, kein geteilter Zustand.
2. **Verbindungsabbruch mid-Operation:** n/a.
3. **Wiederverbinden mit altem Zustand:** n/a.
4. **Tab im Hintergrund:** n/a — kein Frontend.

### Mehr-Nutzer-Pflicht (Punkt 28)
Kein modul-globaler veraenderlicher Zustand angefasst. `CACHE_ANTWORT` ist eine **unveraenderliche
Konstante** (Import-Zeit gesetzt, nie geschrieben) — genau der Whitelist-Fall.

### Async-Bereitschafts-Naht (Punkt 26)
Keine Daten gelesen, die ein anderer Pfad schreibt. n/a.

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

**Angefasste Tabellen: KEINE.** Dieser Plan aendert ausschliesslich Modul-Konstanten und einen
Prompt-Zusammenbau-Zweig. Es wird keine DB-Spalte gelesen oder geschrieben, kein `getattr`/`setattr`
auf Model-Objekten, keine Migrations-Annahme.

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Verifiziert? |
|---|---|---|---|---|
| `config.CACHE_ANTWORT` | gelesen (ab Plan 02) | Prozess-RAM, aus ENV bei Import | App-Start | ✓ kein DB-Bezug |

Der Beleg-Pfad ueber `api_cost_log` gehoert zu **Plan 03** und ist dort verifiziert.

## 6. Punkt 23 — Schild-Pflicht

Keine Tabelle/Spalte angefasst → **kein Schild-Nachzug noetig** (Skip-OK laut Punkt 23:
reine Code-Aenderung ohne DB-Bezug).

<output>
Nach Abschluss: `.planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-01-SUMMARY.md`
</output>

