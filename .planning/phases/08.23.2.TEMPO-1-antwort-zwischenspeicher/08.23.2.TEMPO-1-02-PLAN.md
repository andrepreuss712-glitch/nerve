---
phase: 08.23.2.TEMPO-1
plan: 02
type: execute
wave: 2
depends_on: ["08.23.2.TEMPO-1-01"]
files_modified:
  - services/prompt_pipeline.py
  - tests/test_build_answer_context.py
  - services/claude_service.py
  - services/qa_pipeline.py
autonomous: false
requirements: [T1-A1, T1-A2, T1-A3, T1-A4, T1-A5, T1-A6, T1-B4, T1-E3, T1-F2]
complexity: "🟡 — beruehrt den LIVE-Antwort-Pfad (Auto/EWB, Knopf, QA). Cross-AI PFLICHT vor Execute. Sequenziell nach Plan 01, weil CACHE_ANTWORT dort entsteht und dieselben zwei Dateien angefasst werden."

# ★★ NICHT auto-advance. Cross-AI (Claudian-Pre-Execute-Audit + Fable/Gemini-Gegencheck) VOR Bau.
#    Deploy faehrt Claudian (Plan 03), nicht dieser Plan.

must_haves:
  truths:
    - "answer_system_content() setzt cache_control auf GENAU EINEN Content-Block — den, dessen Quell-Block _layer == 'stable' traegt."
    - "Die Zuordnung laeuft ueber den _layer-WERT, nicht ueber einen Listen-Index: ist der Stabil-Block leer und faellt weg, bekommt der Volatil-Block KEIN cache_control."
    - "Der Volatil-Block traegt nie cache_control (sonst Cache-Write pro Request statt Read)."
    - "Der Fail-open-Fallback ('Verstehen und helfen, nie druecken.') traegt kein cache_control."
    - "Bei config.CACHE_ANTWORT=False traegt kein einziger Block cache_control — der Rollback wirkt ohne Deploy."
    - "Pro Request entsteht hoechstens 1 cache_control-Breakpoint (Anthropic-Maximum 4)."
    - "Der Anti-Cache-Poison-Filter ist per Test festgenagelt: gleicher User, zwei SIDs mit verschiedener session_anrede -> der Stabil-Block ist BYTE-GLEICH."
    - "Kein Kommentar im Repo behauptet mehr 'Phase 1: PLAIN Bloecke ohne cache_control' / 'Caching = Phase 2/Plan 04' — die Kommentare beschreiben den aktivierten Zustand."
  artifacts:
    - path: "services/prompt_pipeline.py"
      provides: "cache_control-Marker auf dem _layer='stable'-Block in answer_system_content(), geschaltet ueber config.CACHE_ANTWORT"
      contains: "cache_control"
    - path: "tests/test_build_answer_context.py"
      provides: "Marker-Tests (stable markiert / volatile nicht / Schalter aus / Index-Falle / Fallback) + E3-Byte-Gleichheits-Test des Anti-Cache-Poison-Filters"
      contains: "def test_cache_marker_"
  key_links:
    - from: "services/prompt_pipeline.py build_answer_context (_layer='stable')"
      to: "services/prompt_pipeline.py answer_system_content (cache_control)"
      via: "Auswertung des _layer-Wertes VOR dem Verwerfen des Markers"
      pattern: "_layer.*stable"
    - from: "services/prompt_pipeline.py answer_system_content"
      to: "Anthropic API (system=)"
      via: "claude_service.py:621 / :779 / qa_pipeline.py:418"
      pattern: "answer_system_content"
    - from: "services/prompt_pipeline.py:474-481 (Anti-Cache-Poison-Filter)"
      to: "Stabilitaet des Cache-Prefix"
      via: "tests/test_build_answer_context.py Byte-Gleichheits-Test"
      pattern: "WICHTIG: Nutze konsequent"
---

<objective>
**Welle 2: den Marker setzen. Das ist der eigentliche Zweck der Phase — und er ist eine Handvoll Zeilen.**

Die Struktur steht seit TAXO3-P1-01: `build_answer_context()` liefert zwei Bloecke,
`{'_layer': 'stable'}` und `{'_layer': 'volatile'}` (`services/prompt_pipeline.py:618-621`).
Es fehlt **ausschliesslich** `cache_control: {"type": "ephemeral"}` auf dem stabilen.

**★ Der Fund, der diesen Plan von einem Einzeiler unterscheidet (A3/A5, LOCKED):**
`answer_system_content()` baut die Content-Bloecke heute so (`prompt_pipeline.py:683`):
```python
content = [{'type': 'text', 'text': b['text']} for b in blocks if b.get('text')]
```
Diese Zeile **wirft den `_layer`-Marker weg**. Danach existiert die Layer-Information nicht mehr —
`claude_service` und `qa_pipeline` bekommen nur noch eine Liste ohne Layer-Info. Der Marker muss
**genau hier** ausgewertet werden, bevor er verloren geht.

Und er muss ueber den **Wert** `_layer == 'stable'` zugeordnet werden, **nicht** ueber einen
Listen-Index: der Filter `if b.get('text')` kann den Stabil-Block entfernen (leerer Text), dann
rutscht der Volatil-Block auf Index 0 — und `content[0]['cache_control'] = …` wuerde den
**per-SID wechselnden** Block cachen. Das ist die teuerste denkbare Fehlbedienung: ein Cache-Write
bei jedem Request statt eines Reads.

**Warum das Tempo bringt und nicht Geld:** dieser Pfad ist 1,8 % der erfassten Kosten. Der Gewinn
ist die **Antwort-Latenz** (CLAUDE.md Punkt 25) — der Berater haengt sonst im Gespraech. Prod-Messung:
avg **7.437 Input-Tokens** pro Antwort-Call (Sonnet-4.5-Minimum: 1.024 → 7× drueber) bei
**~8-9 Antwort-Calls pro Anruf** (Break-even bei 5-Min-TTL: 2 Calls).

**★ STOP-Signale (aus `<deferred>` des CONTEXT.md):**
- **Kein** Circuit-Breaker-Umbau, **kein** Cache-Pre-Warming (TAXO3-Alt-Plan-04 bleibt Landkarte).
- **Kein** Judge-Transkript-Cap (es existiert kein Messwert — `context_tag='judge'` hat 0 Zeilen).
- **Kein** Modellwechsel.
- **Kein** `cache_control` auf dem Analyse-Pfad (der bleibt uncached, siehe Plan 01).
- **Keine** Reparatur der Cache-Segmentierung durch `Rolle: {role_goal}` (F1) — sie ist inhaltlich
  korrekt und kostet nur einen Aufschlag beim Kategorie-Wechsel.
Wer hier eines davon anfaengt → **STOP, melden, deferren.**
</objective>

<context>
@.planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-CONTEXT.md

**Pflicht-Pre-Read (in dieser Reihenfolge):**
1. `08.23.2.TEMPO-1-CONTEXT.md` — Abschnitte **A** (A1-A6), **E** (E1-E4), **F** (F1-F3), `<deferred>`
2. `.planning/DIALOG-GSD-CLAUDIAN.md:289-317` — Pre-Check 2 + Fund 3 (der `_layer`-Verlust)
3. `services/prompt_pipeline.py:576-687` — `build_answer_context` + `derive_answer_params` +
   `answer_system_content` am Stueck
4. `services/prompt_pipeline.py:462-484` — der Anti-Cache-Poison-Filter (E3, ab jetzt cache-tragend)
5. `tests/test_build_answer_context.py:127-164` — `test_anti_cache_poison_real_split`, das Muster
   fuer den neuen Byte-Gleichheits-Test (In-Memory-`_session_state`, kein DB)
6. `CLAUDE.md` Punkt 25 (Latenz), Punkt 18 (Test-Contract)

**Interfaces (aus dem Code gezogen — der Executor braucht dafuer keine Suche):**
```python
# services/prompt_pipeline.py:618-621  — die Quelle des Markers
return [
    {'role': 'system', 'text': stabil_block,  '_layer': 'stable'},
    {'role': 'system', 'text': volatil_block, '_layer': 'volatile'},
]

# services/prompt_pipeline.py:665-686 — die Verbrauchsstelle
def answer_system_content(sid: str | None, *, is_auto_triggered: bool,
                          primary_intent: str | None = None,
                          confidence: float | None = None) -> list[dict]: ...

# Die drei Konsumenten (Rueckgabe-Vertrag bleibt: list[dict] im Anthropic-Content-Format)
services/claude_service.py:621   _system_autovar = _asc_auto(sid, is_auto_triggered=True, primary_intent=primary_intent)
services/claude_service.py:779   _system_manual  = _asc_manual(sid, is_auto_triggered=False, primary_intent=typ, confidence=1.0)
services/qa_pipeline.py:418      _system = answer_system_content(sid, is_auto_triggered=False, confidence=float(confidence))
services/qa_pipeline.py:420-421  _system = _system + [{'type': 'text', 'text': tabu_block}]   # Tabu-Block HINTEN, ohne cache_control
```

**Latenz-Budget (CLAUDE.md Punkt 25 — Pflicht-Angabe):**
- Zusatz-Aufwand pro Aufruf: eine Schleife ueber **2** Bloecke, ein `dict`-Insert, ein lazy
  `import config` (bereits in `sys.modules`). **< 0,1 ms**, kein I/O, kein DB, kein Netz.
- Erwarteter Effekt auf die **TTFT**: negativ (schneller), weil Anthropic den gecachten Prefix nicht
  neu verarbeitet. **Gate:** TTFT darf nach dem Deploy nicht schlechter sein als vorher — der
  Vorher/Nachher-Vergleich ist **Plan 03, Task 1/3** (Waechter b, `[EWB-TTFT]`, PERSID Req 10).
</context>

<tasks>

### Task 1 — Der Marker: cache_control auf den `_layer == 'stable'`-Block

<read_first>
- `services/prompt_pipeline.py` — Zeilen **576-687** am Stueck (`build_answer_context`,
  `derive_answer_params`, `answer_system_content`). Ohne diesen Block ist der Task nicht sicher machbar.
- `services/prompt_pipeline.py` — Zeilen **1-20** (Modul-Docstring: „Side-effect-free beim Import:
  keine I/O … Alle DB-Imports sind LAZY innerhalb der Funktionen") — deshalb wird `config` **lazy in
  der Funktion** importiert, nicht am Modulkopf.
- `services/qa_pipeline.py` — Zeilen **413-422** (der Konsument haengt den Tabu-Block HINTEN an; der
  Marker muss davor sitzen, sonst waere der Cache-Prefix von den Produkt-Verboten abhaengig)
- `08.23.2.TEMPO-1-CONTEXT.md` Abschnitt **A** (A1-A6) — die sechs LOCKED-Entscheidungen
</read_first>

<action>
**Einzige Aenderung: `services/prompt_pipeline.py`, Funktion `answer_system_content` (heute :665-686).**

Heutiger Rumpf-Schluss (Zeilen 683-686):
```python
    content = [{'type': 'text', 'text': b['text']} for b in blocks if b.get('text')]
    if not content:
        content = [{'type': 'text', 'text': 'Verstehen und helfen, nie druecken.'}]
    return content
```

Ziel-Stand (exakt so, inkl. Kommentare — sie sind der Schutz gegen das versehentliche
Wieder-Herausreissen):
```python
    # ── TEMPO-1: cache_control auf den STABILEN Prefix (A1-A6) ────────────────
    # Hier — und nur hier — ist die Layer-Information noch da: die Zeile darunter
    # verwirft den _layer-Marker, den build_answer_context (:618-621) gesetzt hat.
    # ZUORDNUNG UEBER DEN _layer-WERT, NIE ueber einen Listen-Index (A5): der Filter
    # 'if b.get("text")' kann den Stabil-Block entfernen — dann rutscht der VOLATILE
    # Block auf Index 0 und ein index-basierter Marker wuerde den per-SID wechselnden
    # Block cachen (Cache-Write pro Request statt Read, das teuerste Gegenteil).
    # Genau 1 Breakpoint pro Request (A4; Anthropic-Maximum 4) — _cache_gesetzt sorgt
    # dafuer, auch falls build_answer_context spaeter mehrere stabile Bloecke liefert.
    # Rollback ohne Deploy: ENV CACHE_ANTWORT=false (config.py, CLAUDE.md Punkt 12).
    try:
        import config as _cfg
        _cache_an = bool(getattr(_cfg, 'CACHE_ANTWORT', True))
    except Exception as _cfg_e:
        # Live-Loop-Garantie: nie raise. Ohne Config -> Standard-Verhalten (an).
        print(f"[Pipeline] CACHE_ANTWORT nicht lesbar, Standard an: {type(_cfg_e).__name__}")
        _cache_an = True

    content = []
    _cache_gesetzt = False
    for b in blocks:
        _text = b.get('text')
        if not _text:
            continue
        _block = {'type': 'text', 'text': _text}
        if _cache_an and not _cache_gesetzt and b.get('_layer') == 'stable':
            _block['cache_control'] = {'type': 'ephemeral'}
            _cache_gesetzt = True
        content.append(_block)
    if not content:
        # A6: der Stoerfall-Fallback ist weit unter jedem Cache-Minimum -> KEIN cache_control.
        content = [{'type': 'text', 'text': 'Verstehen und helfen, nie druecken.'}]
    return content
```

**Der Docstring der Funktion (heute :668-673) wird mitgezogen** — er sagt heute woertlich
„PHASE 1: PLAIN Content-Bloecke … OHNE cache_control — Caching … ist Phase 2 (Plan 04)".
Das ist nach dieser Aenderung **falsch**. Ersetzen durch:
```python
    """System-Content-Liste (Anthropic-Format) aus build_answer_context fuer die Live-Pfade.

    TEMPO-1: der STABILE Block (build_answer_context _layer='stable') traegt
    cache_control {'type':'ephemeral'} — genau EIN Breakpoint pro Request. Der volatile
    Block traegt bewusst KEINEN (sonst Cache-Write pro Request statt Read). Schalter:
    config.CACHE_ANTWORT (default an, ENV-Rollback ohne Deploy).
    primary_intent/confidence: explizit uebergeben (z.B. geklickter typ, QA-confidence)
    ODER aus per-SID abgeleitet (None -> derive). Fail-open: nie leer, nie raise.
    """
```

**Was NICHT angefasst wird:**
- `build_answer_context` (:576-621) bleibt Zeile fuer Zeile unveraendert — der `_layer`-Marker
  existiert bereits, er wird nur endlich gelesen.
- Der Anti-Cache-Poison-Filter (:474-481) bleibt unveraendert — er wird in Task 2 **festgenagelt**,
  nicht umgebaut.
- Die Rueckgabe bleibt `list[dict]` im Anthropic-Content-Format; alle drei Konsumenten reichen sie
  unveraendert an `system=` weiter. **Keine Signatur-Aenderung.**
</action>

<acceptance_criteria>
- `grep -c "cache_control" services/prompt_pipeline.py` == **1**
- `grep -n "_layer') == 'stable'" services/prompt_pipeline.py` liefert **genau 1 Treffer**
- `grep -n "content\[0\]" services/prompt_pipeline.py` liefert **0 Treffer** (keine Index-Zuordnung, A5)
- `grep -n "CACHE_ANTWORT" services/prompt_pipeline.py` liefert **genau 1 Treffer**
  (EINE Verzweigung, nicht drei — B4)
- `grep -n "PHASE 1: PLAIN" services/prompt_pipeline.py` liefert **0 Treffer**
- `grep -n "^import config" services/prompt_pipeline.py` liefert **0 Treffer**
  (der Import bleibt lazy in der Funktion — Modul-Vertrag „side-effect-free beim Import")
- `python -c "import ast; ast.parse(open('services/prompt_pipeline.py',encoding='utf-8').read())"` exit 0
- `pytest tests/test_build_answer_context.py -q` exit 0 (die **bestehenden** Tests bleiben gruen —
  der Rueckgabe-Vertrag ist unveraendert)
</acceptance_criteria>

**Commit:** `feat(TEMPO-1-02): cache_control auf den stabilen Antwort-Prefix (Zuordnung ueber _layer, nicht Index)`

---

### Task 2 — Die fuenf Tests, die das festnageln (inkl. E3-Byte-Gleichheit)

<read_first>
- `tests/test_build_answer_context.py` — **komplett** (187 Zeilen): Kopf-Docstring (Runtime-Verhalten,
  kein Source-Presence), `test_block_form_stable_before_volatile` (:43-54),
  `test_anti_cache_poison_real_split` (:129-163) als Muster fuer In-Memory-`_session_state`
- `services/prompt_pipeline.py:462-484` — der Filter, den Test 5 festnagelt
- `services/prompt_pipeline.py:631-686` — `derive_answer_params` (Test 1-4 muessen es monkeypatchen
  oder ein leeres `_session_state` stellen, sonst laeuft der Test in den DB-Pfad)
- `CLAUDE.md` Abschnitt „Test-Qualitaets-Regel": Assertion auf **Rueckgabewerte**, kein
  `inspect.getsource`, kein `open().read()`
</read_first>

<action>
`tests/test_build_answer_context.py` wird um **einen Abschnitt mit fuenf Tests** erweitert
(bestehende Datei erweitern, keine neue anlegen — sie ist genau die Heimat dieser Funktion).
Alle fuenf pruefen **Rueckgabewerte** eines echten Funktionsaufrufs.

```python
# ── TEMPO-1: cache_control-Marker auf dem stabilen Prefix ────────────────────

def _blocks(stable_text='STABIL', volatile_text='VOLATIL'):
    """Zwei-Block-Liste im Format von build_answer_context (:618-621)."""
    return [
        {'role': 'system', 'text': stable_text, '_layer': 'stable'},
        {'role': 'system', 'text': volatile_text, '_layer': 'volatile'},
    ]


def test_cache_marker_auf_stabilem_block(monkeypatch):
    """A1/A2: genau der stabile Block traegt cache_control, der volatile nicht."""
    import services.prompt_pipeline as pp
    monkeypatch.setattr(pp, 'build_answer_context', lambda **k: _blocks())
    monkeypatch.setattr(pp, 'derive_answer_params', lambda sid: {
        'user_id': 1, 'role': 'interessent', 'mode': 'cold_call',
        'primary_intent': None, 'confidence': None})
    content = pp.answer_system_content('sid-1', is_auto_triggered=True)
    assert len(content) == 2
    assert content[0]['text'] == 'STABIL'
    assert content[0]['cache_control'] == {'type': 'ephemeral'}
    assert 'cache_control' not in content[1]          # A2: Volatil NIE markiert


def test_cache_marker_genau_ein_breakpoint(monkeypatch):
    """A4: hoechstens EIN cache_control pro Request (Anthropic-Maximum 4)."""
    import services.prompt_pipeline as pp
    monkeypatch.setattr(pp, 'build_answer_context', lambda **k: [
        {'text': 'S1', '_layer': 'stable'},
        {'text': 'S2', '_layer': 'stable'},
        {'text': 'V', '_layer': 'volatile'},
    ])
    monkeypatch.setattr(pp, 'derive_answer_params', lambda sid: {
        'user_id': 1, 'role': 'interessent', 'mode': 'cold_call',
        'primary_intent': None, 'confidence': None})
    content = pp.answer_system_content('sid-1', is_auto_triggered=False)
    assert sum(1 for c in content if 'cache_control' in c) == 1


def test_cache_marker_folgt_layer_nicht_index(monkeypatch):
    """A5 — die teuerste denkbare Fehlbedienung: faellt der leere Stabil-Block weg,
    darf der VOLATILE Block (Index 0!) KEIN cache_control bekommen. Sonst schreibt
    jeder Request einen neuen Cache-Eintrag statt einen zu lesen."""
    import services.prompt_pipeline as pp
    monkeypatch.setattr(pp, 'build_answer_context', lambda **k: [
        {'text': '', '_layer': 'stable'},            # leer -> wird gefiltert
        {'text': 'NUR_VOLATIL', '_layer': 'volatile'},
    ])
    monkeypatch.setattr(pp, 'derive_answer_params', lambda sid: {
        'user_id': 1, 'role': 'interessent', 'mode': 'cold_call',
        'primary_intent': None, 'confidence': None})
    content = pp.answer_system_content('sid-1', is_auto_triggered=False)
    assert len(content) == 1
    assert content[0]['text'] == 'NUR_VOLATIL'
    assert 'cache_control' not in content[0]


def test_cache_marker_schalter_aus(monkeypatch):
    """B1/B3: CACHE_ANTWORT=False -> kein einziger Block traegt cache_control
    (Rollback-Pfad ohne Deploy, CLAUDE.md Punkt 12)."""
    import config
    import services.prompt_pipeline as pp
    monkeypatch.setattr(config, 'CACHE_ANTWORT', False)
    monkeypatch.setattr(pp, 'build_answer_context', lambda **k: _blocks())
    monkeypatch.setattr(pp, 'derive_answer_params', lambda sid: {
        'user_id': 1, 'role': 'interessent', 'mode': 'cold_call',
        'primary_intent': None, 'confidence': None})
    content = pp.answer_system_content('sid-1', is_auto_triggered=False)
    assert all('cache_control' not in c for c in content)


def test_cache_marker_fallback_ohne_marker(monkeypatch):
    """A6: der Stoerfall-Fallback ist weit unter jedem Cache-Minimum -> kein Marker."""
    import services.prompt_pipeline as pp
    monkeypatch.setattr(pp, 'build_answer_context', lambda **k: [
        {'text': '', '_layer': 'stable'}, {'text': '', '_layer': 'volatile'}])
    monkeypatch.setattr(pp, 'derive_answer_params', lambda sid: {
        'user_id': 1, 'role': 'interessent', 'mode': 'cold_call',
        'primary_intent': None, 'confidence': None})
    content = pp.answer_system_content(None, is_auto_triggered=False)
    assert len(content) == 1
    assert content[0]['text'] == 'Verstehen und helfen, nie druecken.'
    assert 'cache_control' not in content[0]
```

**Und der wichtigste Test (E3) — der Filter ist ab jetzt cache-tragend:**

```python
def test_stabil_block_byte_gleich_ueber_zwei_sids(monkeypatch):
    """E3 (LOCKED): der Anti-Cache-Poison-Filter (prompt_pipeline.py:474-481) haelt den
    Cache am Leben. Reisst eine spaetere Aenderung ihn heraus, stirbt der Cache LAUTLOS:
    der Prefix waere pro Anruf verschieden -> Cache-Write statt Read, Kosten steigen,
    KEINE Fehlermeldung. Deshalb: gleicher User, zwei SIDs mit VERSCHIEDENER
    session_anrede -> der Stabil-Block muss BYTE-GLEICH sein."""
    import threading
    import services.live_session as ls
    import services.prompt_pipeline as pp

    pdata = {'ki': {'ansprache': 'Sie', 'ton': 'freundlich'}, 'basis': {'unternehmen': 'ACME'}}
    _cache = {'opener_content': '', 'faqs': [], 'profile_branche': ''}
    _state = {
        'sid-A': {'_profile_cache': dict(_cache), 'session_anrede': 'Sie', 'vorwissen_level': 'hoch'},
        'sid-B': {'_profile_cache': dict(_cache), 'session_anrede': 'Du',  'vorwissen_level': 'niedrig'},
    }
    monkeypatch.setattr(ls, 'get_profile_for_sid', lambda s: ('', pdata), raising=False)
    monkeypatch.setattr(ls, 'get_briefing_for_sid',
                        lambda s: f'Briefing fuer {s}', raising=False)   # volatil, pro SID verschieden
    monkeypatch.setattr(ls, '_session_state', _state, raising=False)
    monkeypatch.setattr(ls, '_session_state_lock', threading.Lock(), raising=False)

    stable_a, volatile_a = pp._profile_blocks(user_id=1, mode='cold_call', sid='sid-A')
    stable_b, volatile_b = pp._profile_blocks(user_id=1, mode='cold_call', sid='sid-B')

    assert stable_a == stable_b, (
        'Stabil-Block unterscheidet sich pro SID — der Cache-Prefix ist vergiftet, '
        'der Cache stirbt lautlos. Anti-Cache-Poison-Filter (prompt_pipeline.py:474-481) pruefen.')
    assert stable_a != ''                    # kein leerer False-Green
    assert volatile_a != volatile_b          # die per-SID-Unterschiede sind wirklich VOLATIL gelandet
    assert 'Du' in volatile_b and 'Du' not in stable_b
```

**Fallstrick beim Schreiben (aus `test_anti_cache_poison_real_split` uebernehmen):** ohne
`_profile_cache` mit `opener_content=''` faellt `build_profile_context` in den DB-Pfad — der Test
waere dann nicht mehr rein/In-Memory. Das Cache-Dict pro SID **kopieren** (`dict(_cache)`), nicht
dasselbe Objekt teilen.
</action>

<acceptance_criteria>
- `grep -c "def test_cache_marker_" tests/test_build_answer_context.py` == **4**
- `grep -c "def test_stabil_block_byte_gleich_ueber_zwei_sids" tests/test_build_answer_context.py` == **1**
- `grep -n "getsource\|open(.*).read()" tests/test_build_answer_context.py` liefert **0 Treffer**
  (keine Source-Presence-Tests — CLAUDE.md Test-Qualitaets-Regel)
- `pytest tests/test_build_answer_context.py -q` exit **0**, mindestens **5** neue Tests gelaufen
- **Wirksamkeits-Beleg (Pflicht, in die SUMMARY):** `test_cache_marker_folgt_layer_nicht_index`
  wird gegen eine testweise index-basierte Variante (`content[0]['cache_control'] = …`) laufen
  gelassen und MUSS dabei **rot** sein. Danach die Variante verwerfen. Ein Test, der die Falle
  nicht faengt, ist kein Netz.
</acceptance_criteria>

**Commit:** `test(TEMPO-1-02): Marker-Tests + Byte-Gleichheit des Cache-Prefix ueber zwei SIDs (E3)`

---

### Task 3 — Kommentar-Wahrheit nachziehen + Stale-Test-Sichtung + volles Gate

<read_first>
- `services/claude_service.py` — Zeilen **613-621** (Auto/EWB-Pfad, Kommentar „Phase 1: PLAIN
  Bloecke ohne cache_control") UND **772-780** (Knopf-Pfad, gleicher Kommentar)
- `services/claude_service.py` — Zeilen **13-22** und **622-630** (Circuit-Breaker: `_model_autovar`
  kippt im Stoerfall auf Haiku) — dorthin gehoert der F2-Kommentar
- `services/qa_pipeline.py` — Zeilen **413-418** und **439-442** (zwei Kommentar-Bloecke, die
  „Caching = Phase 2/Plan 04" behaupten)
- `tests/test_heiler_resolved.py` — Zeilen **60-100** (mockt `answer_system_content` **Rueckgabewert**)
- `tests/test_per_sid_migration.py:118-140` und `tests/test_qa_pipeline.py:190-200`
  (beide patchen `answer_system_content`)
- `CLAUDE.md` Punkt 18 (Stale-Test-Contract) + Punkt 14 (kein Kommentar, der luegt)
</read_first>

<action>
**A) Kommentar-Wahrheit — vier Stellen, die nach Task 1 falsch sind.**

Ein Kommentar, der den Gegenteil-Zustand behauptet, ist schlimmer als kein Kommentar: der naechste
Leser glaubt ihm und reisst den Marker heraus.

1. `services/claude_service.py:617` (Auto/EWB) — heute endet der Kommentarblock mit:
   `# analyse_loop: per-SID). Phase 1: PLAIN Bloecke ohne cache_control (Caching = Phase 2).`
   → ersetzen durch:
   `# analyse_loop: per-SID). TEMPO-1: der stabile Prefix traegt cache_control (Schalter config.CACHE_ANTWORT).`

2. `services/claude_service.py:776` (Knopf) — heute:
   `# Phase 1: PLAIN Content-Bloecke ohne cache_control (Caching = Phase 2/Plan 04).`
   → ersetzen durch:
   `# TEMPO-1: der stabile Prefix traegt cache_control (Schalter config.CACHE_ANTWORT).`

3. `services/qa_pipeline.py:416` — heute:
   `# Phase 1: PLAIN Content-Bloecke ohne cache_control (Caching = Phase 2/Plan 04).`
   → ersetzen durch:
   `# TEMPO-1: der stabile Prefix traegt cache_control (Schalter config.CACHE_ANTWORT).`

4. `services/qa_pipeline.py:439-442` — der Block
   `# TAXO3 P1-02: cache_control-Layering ENTFERNT (Phase 2/Plan 04 baut es korrekt auf dem
   stabilen Prefix wieder auf). …`
   → ersetzen durch:
   ```python
        # TEMPO-1: das cache_control-Layering sitzt jetzt in answer_system_content auf dem
        # stabilen Prefix — NICHT hier. Der Tabu-Block wird bewusst OHNE cache_control
        # HINTEN angehaengt: er ist produkt-/profilabhaengig und darf den Cache-Prefix
        # nicht mitbestimmen. anrede kommt aus dem Profil-Kontext (Volatil-Block).
   ```

**B) F2-Kommentar (LOCKED, „gehoert in einen Kommentar"):** an den Circuit-Breaker-Fallback in
`services/claude_service.py` (bei `_model_autovar = 'claude-haiku-4-5-20251001'`, heute :628)
folgende Zeile **ergaenzen** (der Code bleibt unveraendert — kein Circuit-Breaker-Umbau, das ist
ausdruecklich deferred):
```python
        # TEMPO-1/F2: Anthropic-Caches sind PRO MODELL gekeyt. Nach diesem Fallback laeuft
        # der Antwort-Prompt auf einem EIGENEN Cache-Eintrag (erster Call zahlt Aufschlag).
        # Das ist erwartet, kein Defekt — nicht als Cache-Fehler diagnostizieren.
```

**C) Stale-Test-Sichtung (Pflicht, Punkt 18).** Der Rueckgabe-Vertrag von `answer_system_content`
aendert sich **nicht** (weiterhin `list[dict]`), es kommt nur ein optionaler Schluessel dazu. Erwartung:
alle drei Kandidaten bleiben gruen. **Das ist zu belegen, nicht anzunehmen:**
- `tests/test_heiler_resolved.py:79` mockt `answer_system_content.return_value` mit einer
  Ein-Block-Liste ohne `cache_control` → prueft den TTFT-Waechter, nicht den Cache. Bleibt gueltig.
- `tests/test_per_sid_migration.py:133` und `tests/test_qa_pipeline.py:194` patchen die Funktion mit
  einem `raise` → pruefen Fail-Verhalten. Bleiben gueltig.
Alle drei Dateien einzeln laufen lassen und das Ergebnis in die SUMMARY schreiben. **Falls einer
gruen bleibt, obwohl er den geaenderten Vertrag prueft, ist genau das der Befund** — dann
retargeten (nicht loeschen, nicht „gruen machen").

**D) Volles Gate.** `pytest -m "not live and not perf"` muss gruen sein und die Test-Zahl muss
gegenueber dem Stand nach Plan 01 um die **5 neuen** Tests **gestiegen** sein.
</action>

<acceptance_criteria>
- `grep -rn "PLAIN Bloecke ohne cache_control\|PLAIN Content-Bloecke ohne cache_control" --include=*.py services/` liefert **0 Treffer**
- `grep -rn "Caching = Phase 2\|Phase 2/Plan 04" --include=*.py services/` liefert **0 Treffer**
- `grep -c "CACHE_ANTWORT" services/claude_service.py` == **2** (zwei Kommentar-Erwaehnungen, **kein** Code)
- `grep -n "config.CACHE_ANTWORT" services/claude_service.py services/qa_pipeline.py | grep -v "#"` liefert **0 Treffer** (B4: der Schalter wirkt an EINER Stelle, nicht an dreien)
- `grep -n "TEMPO-1/F2" services/claude_service.py` liefert **1 Treffer**
- `pytest tests/test_heiler_resolved.py tests/test_per_sid_migration.py tests/test_qa_pipeline.py -q` exit **0**
- `pytest -m "not live and not perf" -q` exit **0**, Test-Zahl >= (Stand nach Plan 01) + 5
</acceptance_criteria>

**Commit:** `docs(TEMPO-1-02): Kommentare auf den aktivierten Zustand nachgezogen + F2-Cache-pro-Modell-Hinweis`

</tasks>

<verify>
  <automated>pytest tests/test_build_answer_context.py -q</automated>
  <automated>pytest -m "not live and not perf" -q</automated>
  <automated>grep -c "cache_control" services/prompt_pipeline.py   # muss 1 sein</automated>
  <manual>Kein Deploy in diesem Plan. Deploy + Live-Beleg (api_cost_log + TTFT vorher/nachher) fahren in Plan 03.</manual>
</verify>

<success_criteria>
- Alle `must_haves.truths` erfuellt und per Test belegt (nicht per Behauptung)
- `test_cache_marker_folgt_layer_nicht_index` hat gegen die index-basierte Variante **rot** gezeigt
- Rueckgabe-Vertrag von `answer_system_content` unveraendert (`list[dict]`); alle drei Konsumenten
  ungeaendert im Aufruf
- Kein Kommentar im Repo behauptet noch den deaktivierten Zustand
- **Nichts** aus der `<deferred>`-Liste angefasst
</success_criteria>

## 1. Lokaler Kontext (Punkt 14 Schicht 1)

`answer_system_content` (`prompt_pipeline.py:665-686`) hat **einen** Ausgangspfad: das `return content`
am Ende. Kein `try` umschliesst die Content-Schleife heute; der neue `try` umschliesst
**ausschliesslich** den Config-Lookup, damit die Live-Loop-Garantie („Keine der Funktionen wirft
Exceptions", Modul-Docstring :13) erhalten bleibt. Der `if not content:`-Fallback bleibt der letzte
Schutz vor einer leeren `system=`-Liste.

## 2. Funktions-Skelett

```
answer_system_content(sid, *, is_auto_triggered, primary_intent=None, confidence=None)
  → derive_answer_params(sid)                     [unveraendert]
  → _intent / _conf aufloesen                     [unveraendert]
  → build_answer_context(...)  -> [stable, volatile] mit _layer   [unveraendert]
  → ★ EINGRIFF: Schleife statt List-Comprehension, cache_control bei _layer=='stable'
  → if not content: Fallback OHNE Marker          [unveraendert bis auf Kommentar]
  → return content
```

## 3. Cross-File-Awareness (Punkt 14 Schicht 3, grep-belegt)

| Konsument | Stelle | Was ankommt | Risiko |
|---|---|---|---|
| `claude_service.streame_auto_variante` | `:620-621` → `system=_system_autovar` (`:640`) | Liste mit 1 markiertem Block | keins — Anthropic akzeptiert `cache_control` pro Block. **Hinweis:** dieser Pfad ist seit PIP-01 **dormant** (0 Produktiv-Caller, nur Tests) → der Live-Beleg kommt aus Knopf + QA |
| `claude_service.streame_manual_ewb_variante` | `:778-780` → `system=_system_manual` (`:808`) | dito | keins |
| `qa_pipeline.generate_qa_response` | `:418` → `+ [tabu_block]` (`:420-421`) → `system=_system` (`:449`) | markierter Stabil-Block **vorne**, Tabu-Block **hinten** ohne Marker | korrekt: der Cache-Prefix endet vor dem Tabu-Block |

**Cache-Read-Hooks (nichts zu bauen, D1 verifiziert):** `claude_service.py:700-709` (`context_tag='ewb'`),
`claude_service.py:853-862` (`context_tag='ewb'`), `qa_pipeline.py:488-497` (`context_tag='qa'`).

## 4. Edge-Case-Analyse

1. **Stabil-Block leer** (kein Profil, `_profile_blocks` fail-open `('','')`, Paradigma trotzdem da):
   dann ist der Stabil-Block **nicht** leer (Paradigma + Grounding stehen drin). Ist er es doch,
   greift Test 3 (`test_cache_marker_folgt_layer_nicht_index`) — kein Marker auf dem Volatilen.
2. **Stabil-Block unter dem Cache-Minimum** (1.024 Tokens bei Sonnet 4.5): Anthropic ignoriert den
   Marker dann still. Prod-Messung sagt avg 7.437 Input-Tokens — 7× drueber. Kein Fehlerfall,
   nur ein ausbleibender Nutzen. **Nicht** durch eine Laengen-Pruefung im Code „absichern" — genau
   dieser Zeichen-gegen-Token-Vergleich war der Defekt, den Plan 01 entfernt.
3. **`CACHE_ANTWORT=false` auf Prod gesetzt:** Marker faellt weg, alles laeuft wie heute. Genau der
   gewollte Rollback (Test 4).
4. **Rolle kippt mitten im Anruf** (F1, `contact_category` gatekeeper→target): der Stabil-Block
   enthaelt `Rolle: {role_goal}` → Prefix aendert sich → **ein** Miss + **ein** Write. Inhaltlich
   korrekt, akzeptiert, **nicht reparieren**.
5. **Circuit-Breaker-Fallback auf Haiku** (F2): eigener Cache pro Modell → erster Call nach dem
   Fallback zahlt Aufschlag. Als Kommentar dokumentiert (Task 3B), **kein** Umbau.
6. **Anthropic-SDK akzeptiert `cache_control` nicht** (Versions-Drift): dann wirft
   `messages.create/stream` — das waere ein **lauter** Fehler in `inspect.sh logs-errors`, kein
   stiller. Plan 03 prueft genau darauf.

### Race-Condition-Audit (Punkt 14, vier Pflicht-Fragen)
1. **Gleichzeitiges Feuern (zwei Sessions parallel):** `answer_system_content` ist rein
   funktional — jeder Aufruf baut seine eigene `content`-Liste, `_cache_gesetzt` ist eine lokale
   Variable. Kein geteilter Zustand.
2. **Verbindungsabbruch mid-Operation:** unveraendert gegenueber heute (Retry-Logik im Knopf-Pfad).
3. **Wiederverbinden mit altem Zustand:** der Cache ist serverseitig bei Anthropic und rein
   inhaltsbasiert — ein Reconnect kann keinen falschen Eintrag treffen.
4. **Tab im Hintergrund:** kein Frontend beruehrt.

### Mehr-Nutzer-Pflicht (Punkt 28)
**Kritisch und geprueft:** der Cache-Prefix ist **pro User** gescoped, weil der Stabil-Block den
Profil-Stabilteil (Sek. 1-7) enthaelt (E4). Zwei Nutzer bekommen verschiedene Prefixe → verschiedene
Cache-Eintraege. Kein modul-globaler veraenderlicher Zustand angefasst; `_cache_gesetzt` ist
funktions-lokal. Der einzige per-Anruf-Inhalt im Stabil-Teil waere die Anrede — genau die haelt der
Anti-Cache-Poison-Filter draussen, und genau das nagelt Test E3 fest.

### Async-Bereitschafts-Naht (Punkt 26)
`answer_system_content` liest `_session_state[sid]` ueber `derive_answer_params` — **unveraendert**
gegenueber heute, mit dem bestehenden fail-open (`None` → Default-Intent). Dieser Plan aendert am
Lese-Zeitpunkt nichts.

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

**Angefasste Tabellen: KEINE.** Der Plan aendert ausschliesslich den Prompt-Zusammenbau im RAM.

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann Leser vorauslaufen? |
|---|---|---|---|---|
| `_session_state[sid]['state']['primary_intent']` | gelesen in `derive_answer_params` | Prozess-RAM (per-SID) | live durch TAXO1-Emit | ja — **bestehendes**, unveraendertes fail-open (`None` → Default-Intent) |
| `_session_state[sid]['_profile_cache']` | gelesen in `build_profile_context` | Prozess-RAM (per-User, ueber den Anruf stabil) | bei Session-Start | nein (Fallback: DB-Pfad) |
| `api_cost_log` (Cache-Read-Zeilen) | **nur in Plan 03 gelesen** | Postgres-Tabelle | **synchron** im Cost-Hook direkt nach dem LLM-Call (kein Batch, kein Hintergrund-Thread) | nein — der Beleg-Query laeuft nach dem Test-Anruf |

Keine neue DB-Spalte, kein `getattr`/`setattr` auf Model-Objekten, keine Migration.

## 6. Punkt 23 — Schild-Pflicht

Keine Tabelle/Spalte angefasst; die Leser/Schreiber von `api_cost_log` aendern sich **nicht**
(die Hooks existieren unveraendert, sie bekommen nur endlich Werte `> 0`). **Kein Schild-Nachzug
noetig.**

<output>
Nach Abschluss: `.planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-02-SUMMARY.md`
</output>
