---
phase: 08.23.2.TEMPO-1
plan: 03
type: execute
wave: 3
depends_on: ["08.23.2.TEMPO-1-01", "08.23.2.TEMPO-1-02"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-VERIFICATION.md
autonomous: false
requirements: [T1-D1, T1-D2, T1-D3, T1-V1, T1-V2, T1-V3]
complexity: "🟡 — kein Code. Deploy + Beleg-Fuehrung am LIVE-Antwort-Pfad. Claudian faehrt den Deploy mit dem Zwei-Tore-Netz; Andre faehrt den Test-Anruf."

# ★★ NICHT auto-advance. Dieser Plan startet erst, wenn Cross-AI (Claudian-Pre-Execute-Audit +
#    Fable/Gemini-Gegencheck) fuer Plan 01+02 durch ist UND beide gebaut + lokal gruen sind.

must_haves:
  truths:
    - "Es existiert eine TTFT-Zahlenreihe VOR dem Deploy (aus den Prod-Logs, [EWB-TTFT]-Zeilen) — sonst ist 'nicht schlechter als vorher' unbelegbar."
    - "Das Deploy-Gate (deploy.sh production, eingebautes Pytest) war vollstaendig gruen; ohne gruenes Gate kein Restart."
    - "Nach einem Test-Anruf zeigt api_cost_log mindestens eine Zeile mit unit_type='per_1k_cache_read_tokens', units > 0 und context_tag IN ('ewb','qa'). PFLICHT-BEDINGUNG (Claudian-Audit): der Test-Anruf laeuft mit einem GROSSEN Profil (Prod id 6, 17.949 Zeichen) — bei id 8 (1.340 Zeichen) liegt der Stabil-Block an/unter der 1.024-Token-Mindestgroesse und es entsteht korrekterweise WEDER eine Write- NOCH eine Read-Zeile (Marker still ignoriert, Fable A3); das waere ein Fehlalarm, kein Defekt."
    - "Die TTFT nach dem Deploy ist nicht schlechter als die Baseline davor (CLAUDE.md Punkt 25)."
    - "inspect.sh logs-errors zeigt keine neue Fehlerklasse (insb. keinen Anthropic-Fehler zu cache_control)."
    - "ROADMAP.md und STATE.md sind von Hand nachgezogen (Multi-Segment-Gotcha: gsd-tools loesen 08.23.2.TEMPO-1 falsch auf)."
  artifacts:
    - path: ".planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-VERIFICATION.md"
      provides: "Die drei Belege im Volltext: TTFT vorher/nachher, api_cost_log-Zeilen (verbatim), logs-errors-Stand"
      contains: "per_1k_cache_read_tokens"
    - path: ".planning/ROADMAP.md"
      provides: "TEMPO-1-Eintrag auf COMPLETE + Beleg-Query ergaenzt (context_tag IN ('ewb','qa'); die Zeile nannte nie einen context_tag)"
      contains: "context_tag IN ('ewb','qa')"
  key_links:
    - from: "services/prompt_pipeline.py answer_system_content (cache_control)"
      to: "api_cost_log-Zeile unit_type='per_1k_cache_read_tokens'"
      via: "bestehende Cost-Hooks claude_service.py:700-709 / :853-862 / qa_pipeline.py:488-497"
      pattern: "per_1k_cache_read_tokens"
    - from: "[EWB-TTFT]-Logzeile (claude_service.py:817, Waechter b / PERSID Req 10)"
      to: "Latenz-Gate 'nicht schlechter als vorher'"
      via: "journalctl-Vergleich vorher/nachher"
      pattern: "EWB-TTFT"
---

<objective>
**Welle 3: beweisen, dass der Speicher wirklich greift — und dass die Antwort dadurch nicht langsamer wurde.**

Dieser Plan schreibt **keine Zeile Code**. Er fuehrt drei Belege, und ohne alle drei gilt die Phase
nicht als fertig.

**★ Die Falle, gegen die dieser Plan gebaut ist (D2/D3, LOCKED):** die Cost-Hooks schreiben nur bei
`> 0`. Ein fehlender Marker sieht deshalb aus wie „**keine Zeile**", nicht wie ein Fehler. Und der
Beleg-Query im ROADMAP-Text traegt nicht: die Beleg-Zeile (heute `ROADMAP.md:2411`) nennt ueberhaupt
**keinen** `context_tag`, und das aus der Erstmessung kursierende `pip_stream` ist ein
**Mess-Label, kein DB-Wert** — diesen Wert schreibt der Code **nirgends** (Enumeration aller `context_tag=`/`call_site=`-Literale in `services/`,
`routes/`, `nerve_rt/`). Wer danach sucht, findet
garantiert nichts und haelt eine **funktionierende** Aktivierung faelschlich fuer gescheitert.
Die Erstmessung selbst ist bereits korrigiert (ROADMAP Fund 2, heute `:2394`, „KORRIGIERT
2026-07-21" — gueltig sind `ewb` avg 3.190 / `qa` avg 3.570 Input-Tokens, „3× drueber statt 7×").

Gueltig ist ausschliesslich:
```sql
unit_type='per_1k_cache_read_tokens' AND units > 0 AND context_tag IN ('ewb','qa')
```

**★ Und der eigentliche Erfolgsnachweis ist die TTFT, nicht der Geldbetrag.** Dieser Pfad ist 1,8 %
der erfassten Kosten — der Nutzen der Phase ist **Tempo** (CLAUDE.md Punkt 25). Die TTFT-Messung
existiert bereits (Waechter b, PERSID Req 10, `claude_service.py:817`, Logzeile
`[EWB-TTFT] {ms}ms model=… sid=… path=manual`). Sie muss **vor** dem Deploy abgelesen werden —
danach ist die Baseline weg.

**Wichtig fuer die Erwartungshaltung beim Test-Anruf:** der Auto/EWB-Pfad
(`streame_auto_variante`) ist seit PIP-01 **dormant** (0 Produktiv-Caller, nur Tests). Der Beleg
kommt daher realistisch aus dem **Knopf-Pfad** (`context_tag='ewb'`) und dem **QA-Pfad**
(`context_tag='qa'`). Ein Test-Anruf muss also mindestens **zwei** Antworten ausloesen — der erste
Call fuellt den Cache (`per_1k_cache_write_tokens`), erst der zweite liest ihn
(`per_1k_cache_read_tokens`). **Ein einzelner Antwort-Call ist kein gueltiger Beleg.**
</objective>

<context>
@.planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-CONTEXT.md

**Pflicht-Pre-Read:**
1. `08.23.2.TEMPO-1-CONTEXT.md` — Abschnitte **D** (Beleg-Pfad) und `<verification>`
2. `.planning/DIALOG-GSD-CLAUDIAN.md:264-288` — Pre-Check 1 (Hook-Tabelle) + die Query-Korrektur
3. `.planning/ROADMAP.md:2386-2418` — der Phasen-Eintrag, der am Ende nachgezogen wird
4. `CLAUDE.md` — „HART: Kein Local-Dev", Punkt 25 (Latenz), Punkt 30

**Prod-Zugang (memorierte Fakten, nicht raten):**
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'
```
`inspect.sh` kann **kein** freies SQL (Whitelist: schema/sample/count/tables/migrations/columns/
constraints/alembic-current/routes/logs/logs-errors/health/git-stand/service-status/grep/env-keys/
schilder). Der exakte Beleg-Query laeuft daher als `postgres` per peer-auth.

⚠ **RLS-False-Negative-Falle (memoriert):** FORCE-RLS-Tabellen lesen fuer `nerve_app` **ohne**
Tenant-GUC als **leer**. Ein leerer `inspect.sh`-Read ist **kein** Abwesenheitsbeweis. Deshalb wird
der entscheidende Query als `postgres` gefahren.
</context>

<tasks>

### Task 1 — TTFT-Baseline VOR dem Deploy sichern (sonst ist sie weg)

<read_first>
- `services/claude_service.py:799-818` — der TTFT-Waechter (b): `_t_stream_start` unmittelbar vor
  dem Stream-Kontext, Ausgabe `print(f"[EWB-TTFT] {_ttft_ms:.0f}ms model=… sid=… path=manual")`
- `services/claude_service.py:645-657` — die zweite `[EWB-TTFT]`-Quelle (Auto-Pfad, dormant)
- `CLAUDE.md` Punkt 25 (Latenz-Budget, Balance-Absatz)
</read_first>

<action>
**Reine Lese-Operation gegen Production, VOR dem Deploy.** Danach existiert die Baseline nicht mehr
in vergleichbarer Form.

```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  "journalctl -u nerve --since '14 days ago' --no-pager | grep -F '[EWB-TTFT]' | tail -200"
```
(Falls die Unit anders heisst: `bash scripts/inspect.sh service-status` zeigt den Namen. Alternativ
`bash scripts/inspect.sh logs 2000 | grep -F '[EWB-TTFT]'`.)

Aus den Treffern folgende Kennzahlen bilden und **verbatim** in
`08.23.2.TEMPO-1-VERIFICATION.md` festhalten:
- **n** (Anzahl der Messwerte) und der Zeitraum
- **Median** und **p90** der Millisekunden-Werte, getrennt nach `path=manual` und (falls vorhanden)
  dem Auto-Pfad
- die verwendeten Modelle (`model=` im Log) — ein Modellwechsel im Zeitraum wuerde den Vergleich
  verfaelschen

**Wenn n < 5:** das ist ein Befund, kein Grund weiterzugehen. Dann steht in der VERIFICATION
ausdruecklich „Baseline zu duenn (n=X)", und der Nachher-Vergleich wird als **nicht belastbar**
markiert statt still als Erfolg verbucht. Ein erfundener Vergleich ist schlimmer als ein fehlender.

**★ ZWEITE Baseline im selben Zug: die Fehler-Stichprobe VOR dem Deploy.**
`must_haves.truths` verlangt „**keine neue** Fehlerklasse" — ohne Vorher-Stichprobe ist „neu"
nicht messbar, und jede phasenfremde Anthropic-Stoerung (`overloaded_error`, Rate-Limit, Timeout)
saehe nach dem Deploy wie ein Befund aus und wuerde einen grundlosen Rollback ausloesen.
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs-errors 300'
```
Output **verbatim** in `08.23.2.TEMPO-1-VERIFICATION.md` unter der Ueberschrift
`Fehler-Baseline (vor Deploy)`. Die **Fehlerarten** daraus auflisten (welche Klassen kommen im
Bestand schon vor) — genau gegen diese Liste misst Task 2 sein Abbruch-Kriterium.

**Kein Deploy in diesem Task.**
</action>

<acceptance_criteria>
- `08.23.2.TEMPO-1-VERIFICATION.md` existiert und enthaelt den String `TTFT-Baseline (vor Deploy)`
- Die Datei enthaelt mindestens eine verbatim kopierte `[EWB-TTFT]`-Logzeile (nicht nur eine Zahl)
- Die Datei nennt **n**, **Median**, **p90** und den Zeitraum
- Die Datei nennt den `model=`-Wert aus den Logzeilen
- Die Datei enthaelt den Abschnitt `Fehler-Baseline (vor Deploy)` mit dem **verbatim**
  `logs-errors 300`-Output **und** einer Auflistung der darin bereits vorkommenden Fehlerarten
  (Bestandsrauschen) — ohne diese Liste ist „keine **neue** Fehlerklasse" in Task 2/3 nicht messbar
- `git log --oneline -1` zeigt den Baseline-Commit **vor** dem Deploy-Commit
</acceptance_criteria>

**Commit:** `docs(TEMPO-1-03): TTFT-Baseline vor dem Deploy gesichert`

---

### Task 2 — Deploy mit dem Zwei-Tore-Netz (Claudian faehrt)

<read_first>
- `deploy.sh` — der Abschnitt mit dem eingebauten Pytest-Gate und dem Waisen-Prune
  (`grep -n "pytest\|prune\|Manifest" deploy.sh`)
- `CLAUDE.md` — „HART: Kein Local-Dev" inkl. der Ueberschreibung 2026-06-01 (nur Production,
  kein Staging, kein `git pull` auf Prod)
- SUMMARY von Plan 01 und Plan 02 (lokale Gate-Ergebnisse)
</read_first>

<action>
**Tor 1 — vor dem Deploy pruefen, ob auf Prod Code liegt, der NICHT in origin/main ist**
(SCP-Hotfix-Falle; der tar-Push wuerde ihn wegbuegeln):
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh git-stand'
```
Abweichung → **STOP**, melden, nicht deployen.

**Tor 2 — der Deploy:**
```bash
bash deploy.sh production
```
Das eingebaute Pytest-Gate laeuft **auf dem Server vor dem Restart**. Bei Test-Fehler: kein Restart,
alter Code bleibt live. Das Gate muss **vollstaendig** gruen sein — inklusive
`tests/test_build_answer_context.py`, `tests/test_08_13_01_config_constants.py`,
Schild-Guard und Baseline-Cleanup-Waechter.

**Direkt nach dem Restart — Rauchtest, bevor irgendwer anruft:**
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh health'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs-errors 200'
```
**Abbruch-Kriterium:** taucht in `logs-errors` ein Anthropic-Fehler auf, der `cache_control`,
`system` oder `invalid_request_error` nennt, ist der Marker falsch geformt → **sofort Rollback**
(das ist genau der Grund, warum es den Schalter gibt), dann melden. **Nicht** live am Code
herumprobieren.

★ **Der Rollback laeuft ueber `/etc/nerve/.env`, NICHT ueber `/opt/nerve/app/.env`** — letztere
existiert auf Prod **nicht** (`deploy.sh:54` schliesst `./.env` vom tar-Deploy aus, `:42-45` prueft
`/etc/nerve/.env` auf dem Ziel-Server). Wer die App-lokale Datei editiert, aendert eine Datei, die
der Dienst nie liest, und schliesst falsch, der Schalter wirke nicht.
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "echo 'CACHE_ANTWORT=false' >> /etc/nerve/.env && systemctl restart nerve"
```
Gegenprobe: `ssh … 'cd /opt/nerve/app && bash scripts/inspect.sh env-keys'` → `CACHE_ANTWORT`
muss danach gelistet sein. Ist er es nicht, hat der Eintrag die falsche Datei getroffen.

⚠ **Bestandsrauschen loest KEINEN Rollback aus.** Andere Anthropic-Fehlerarten
(`overloaded_error`, Rate-Limit, Timeout) sind **nur** dann ein Befund, wenn sie in der
Fehler-Baseline aus Task 1 **nicht** vorkamen.

**Und ein Beleg, dass der neue Stand wirklich liegt** — auf den **aktiven Code** pruefen, nicht auf
den Roh-String:
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  "cd /opt/nerve/app && grep -cF \"_block['cache_control'] = {'type': 'ephemeral'}\" services/prompt_pipeline.py"
```
Muss **1** ergeben.

⚠ **NIE `grep -c cache_control …` verwenden — auch nicht „nur informativ".** `grep -c` zaehlt
**Zeilen**, und der korrekt gebaute Stand enthaelt den Roh-String in mehreren Kommentar- und
Docstring-Zeilen (Plan 02 Task 1 legt vier an, Task 3 A)5/A)6 kommen dazu). **Jede feste Zahl an
dieser Stelle ist ein bewegliches Ziel** — und diese Gegenprobe laeuft **nach dem Prod-Deploy**, wo
ein falsches Rot genau den grundlosen Rollback ausloest, vor dem dieser Plan warnt. Deshalb steht
hier **keine Roh-Zahl**. Gueltig sind ausschliesslich die zwei stabilen Anker:
`grep -cF "_block['cache_control'] = {'type': 'ephemeral'}" services/prompt_pipeline.py` == **1**
und `grep -c "cache_control.*ephemeral" services/prompt_pipeline.py` == **2** (Code + Docstring —
die Kommentar-Nachzuege aus Plan 02 Task 3 enthalten kein „ephemeral" und verschieben diese Zahl nicht).
</action>

<acceptance_criteria>
- `inspect.sh git-stand` vor dem Deploy dokumentiert (verbatim in der VERIFICATION)
- `bash deploy.sh production` exit **0**, Pytest-Gate-Ausgabe (`N passed`) verbatim in der VERIFICATION
- `inspect.sh health` liefert nach dem Restart HTTP-OK
- `inspect.sh logs-errors 200` enthaelt **keine** Zeile mit `cache_control` oder
  `invalid_request_error` (das sind die phasen-eigenen Fehlerklassen — jede davon ist ein Befund)
- Sonstige Anthropic-Fehlerarten (`overloaded_error`, Rate-Limit, Timeout, …) sind **nur** dann ein
  Befund, wenn sie in der **Fehler-Baseline aus Task 1** nicht vorkamen. **Bestandsrauschen loest
  KEINEN Rollback aus** — ein pauschales „`anthropic` + `error`" waere ein false-red nach dem
  Prod-Deploy und damit genau die teuerste Fehlerklasse dieser Phase
- Der Prod-seitige `grep -cF "_block['cache_control'] = {'type': 'ephemeral'}" services/prompt_pipeline.py`
  ergibt **1** (CODE-Anker; `-F` ist Pflicht — ohne `-F` waere `['cache_control']` eine
  BRE-Zeichenklasse und der Zaehler liefert 0 statt 1)
- `grep -c "cache_control.*ephemeral" services/prompt_pipeline.py` ergibt **2** (Code + Docstring;
  die Kommentar-Nachzuege aus Plan 02 Task 3 enthalten kein „ephemeral")
- **Ein Roh-String-Zaehler `grep -c cache_control …` ist KEIN Kriterium** — weder bindend noch
  informativ. Seine Zahl bewegt sich mit jedem Kommentar-Nachzug, und ein falsches Rot **nach** dem
  Prod-Deploy loest genau den grundlosen Rollback aus, vor dem dieser Plan warnt.
</acceptance_criteria>

**Commit:** `chore(TEMPO-1-03): Deploy production (Zwei-Tore) — Antwort-Cache aktiv`

---

### Task 3 — 🔴 CHECKPOINT: Test-Anruf + die drei Belege (Andre + Claudian)

<read_first>
- `08.23.2.TEMPO-1-CONTEXT.md` `<verification>` — die drei Pflicht-Belege
- `services/claude_service.py:853-862` und `services/qa_pipeline.py:488-497` — die Hooks, die
  die Beleg-Zeilen schreiben (bestaetigen: sie schreiben **nur bei > 0**)
- Task 1 dieser Datei — die Baseline-Zahlen zum Vergleich
</read_first>

<action>
**Dieser Task ist ein Checkpoint (`autonomous: false`).** Ohne den Test-Anruf gibt es keinen Beleg.

**Was Andre tut:** ein Test-Anruf mit dem Test-User, bei dem **mindestens zwei** Antworten entstehen
(zweimal der EWB-Knopf, oder Knopf + eine QA-Antwort). Grund: der erste Call **schreibt** den Cache
(`per_1k_cache_write_tokens`), erst der zweite **liest** ihn (`per_1k_cache_read_tokens`).
Ein einzelner Antwort-Call ist kein gueltiger Beleg — und genau dieses Missverstaendnis wuerde eine
funktionierende Aktivierung als gescheitert erscheinen lassen.

**Beleg 1 — der Cache greift** (als `postgres`, wegen der RLS-False-Negative-Falle):
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "sudo -u postgres psql -d nerve -c \"
SELECT created_at, provider, model, unit_type, units, cost_eur, context_tag, call_site
FROM api_cost_log
WHERE unit_type IN ('per_1k_cache_read_tokens','per_1k_cache_write_tokens')
  AND context_tag IN ('ewb','qa')
  AND created_at > now() - interval '2 hours'
ORDER BY created_at DESC LIMIT 40;\""
```
**Bestanden, wenn:** mindestens eine Zeile `unit_type='per_1k_cache_read_tokens'` mit `units > 0`
und `context_tag IN ('ewb','qa')`. Die Write-Zeilen sind der erwartete Begleiter (erster Call pro
Cache-Fenster, ~1,25× Aufschlag — bei ~8-9 Antwort-Calls pro Anruf weit ueber dem Break-even von 2).

**Wenn nur Write-Zeilen und keine Read-Zeile da sind:** entweder lagen die Calls mehr als 5 Minuten
auseinander (TTL abgelaufen) oder der Prefix hat sich zwischen den Calls geaendert (F1: Rollenwechsel
gatekeeper→target). Beides ist **kein Defekt** — Anruf mit zwei zeitnahen Antworten wiederholen und
die Rolle konstant halten. Erst wenn auch das keine Read-Zeile bringt, ist es ein echter Befund.

**★ DRITTE URSACHE (Claudian-Pre-Execute-Audit 2026-07-21, im Plan bisher NICHT abgedeckt) —
das getestete Profil ist zu klein.** Die Mindestgroesse fuer einen cachebaren Prefix gilt fuer den
**STABIL-Block allein**, NICHT fuer den Gesamt-Input. Die Prod-Messung (3.190 / 3.570 Tokens) ist
der **gesamte** Input inkl. User-Message und Kontext — der Stabil-Block ist nur ein Teil davon.
Prod hat aktuell **drei** Profile (`SELECT id, length(daten::text) FROM profiles`):
**id 6 = 17.949 Zeichen · id 7 = 7.788 · id 8 = 1.340**. Bei id 8 (Erstprofil-Platzhalter aus
AUTH-2) liegt der Stabil-Block sehr wahrscheinlich **unter 1.024 Tokens** → Anthropic cached
schlicht nicht: **kein Fehler, keine Fehlermeldung, nur kein Nutzen.**
★★ **KORREKTUR dieser Ergaenzung (Fable-Bestand-Audit 2026-07-21, Befund A3 — Claudian hatte sie
falsch einsortiert):** liegt der Stabil-Block UNTER dem Minimum, wird der Marker **still ignoriert**
→ `cache_creation_input_tokens = 0` → die Hooks schreiben nur bei `> 0` → es entsteht **weder eine
Write- NOCH eine Read-Zeile**. Der Fall gehoert damit **NICHT** in den Zweig „nur Write-Zeilen",
sondern in den Zweig **„Gar keine Zeile"** (unten) — dessen Standard-Diagnose („der Marker kommt
nicht an") waere hier **falsch** und fuehrt genau in den Fehlalarm, den diese Ergaenzung verhindern
soll. Deshalb steht die Profil-Groesse dort ab jetzt als **erster** Pruefpunkt.

★ **Fable-Nachrechnung am Code (praeziser als Claudians Schaetzung):** Fixanteil = Paradigma
(`answer_paradigm.py:28-49`, ~1.515 Zeichen) + Rollen-Ziel (~160) + Grounding (~330) ≈ 2.000 Zeichen
≈ **600-700 Tokens** — der Fixtext allein reisst die Schwelle also **nie**. Profil id 8 liefert
~1.000-1.100 Zeichen Sektionstext ≈ ~350 Tokens → **Summe ≈ 950-1.050 Tokens, also DIREKT AN der
1.024-Grenze**, nicht klar darunter. Claudians „vermutlich drunter" ist vertretbar, aber unscharf;
die Vorschrift „Test mit id 6" bleibt richtig, weil sie den Beleg **schwellenunabhaengig** macht.

→ **Der Test-Anruf MUSS mit einem grossen Profil gefahren werden (id 6).** Wird er mit id 8
gefahren, bleibt der Beleg-Query korrekt **komplett leer** — und ein **funktionierender Bau wuerde
faelschlich als kaputt eingestuft und ggf. zurueckgerollt.** Das ist dieselbe
Post-Deploy-Fehlalarm-Klasse wie der von Fable gefundene Roh-Zaehler-Befund, nur eine Ebene tiefer.
→ **Ehrliche Erwartung fuers Protokoll:** bei frischen/kleinen Profilen greift das Caching nicht.
Der Nutzen skaliert mit der Profil-Groesse. Das ist Absicht des Anbieters, kein Mangel unseres Baus.

**Wenn gar keine Zeile da ist:** der Marker kommt nicht an. Dann pruefen (in dieser Reihenfolge):
(a) `CACHE_ANTWORT` in `/etc/nerve/.env` auf `false`? (`inspect.sh env-keys`; **nicht**
`/opt/nerve/app/.env` — die existiert auf Prod nicht),
(b) liegt der neue Code wirklich (Task 2, der **Code-Anker**-grep `_block['cache_control'] = …`,
**nicht** `grep -c cache_control`),
(c) `inspect.sh logs-errors`.

**Beleg 2 — die TTFT ist nicht schlechter** (CLAUDE.md Punkt 25):
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  "journalctl -u nerve --since '1 hour ago' --no-pager | grep -F '[EWB-TTFT]'"
```
Median und p90 gegen die Baseline aus Task 1 stellen. **Bestanden, wenn:** der Median nicht hoeher
ist als die Baseline. **Erwartung:** deutlich niedriger — das ist der eigentliche Zweck der Phase.
Bei duenner Nachher-Stichprobe (n < 5) ausdruecklich als „nicht belastbar" markieren, **nicht**
schoenreden.

**Beleg 3 — keine neue Fehlerklasse:**
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs-errors 300'
```

**Alle drei Belege verbatim** in `08.23.2.TEMPO-1-VERIFICATION.md` (Output kopieren, nicht
zusammenfassen — Vault-Regel „Sicht/Beleg").

**Rollback-Pfad, falls Beleg 2 rot ist** (TTFT schlechter): `CACHE_ANTWORT=false` in
**`/etc/nerve/.env`** (NICHT `/opt/nerve/app/.env` — die existiert auf Prod nicht, `deploy.sh:54`
schliesst `./.env` vom tar aus) + Restart. 30 Sekunden, kein Deploy:
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "echo 'CACHE_ANTWORT=false' >> /etc/nerve/.env && systemctl restart nerve"
```
Gegenprobe: `inspect.sh env-keys` muss `CACHE_ANTWORT` danach listen. Genau dafuer existiert der
Schalter.
</action>

<acceptance_criteria>
- `08.23.2.TEMPO-1-VERIFICATION.md` enthaelt den verbatim psql-Output mit mindestens einer Zeile
  `per_1k_cache_read_tokens` / `units > 0` / `context_tag` in (`ewb`,`qa`)
- Die Datei enthaelt den String `context_tag IN ('ewb','qa')` und **nicht** den String `pip_stream`
  als Beleg-Kriterium
- Die Datei enthaelt einen Abschnitt `TTFT-Vergleich` mit Baseline-Median/p90 **und**
  Nachher-Median/p90 und einem expliziten Urteil (bestanden / nicht belastbar / rot)
- Die Datei enthaelt den verbatim `logs-errors`-Output
- Bei rotem Beleg 2: der Rollback (`CACHE_ANTWORT=false`) ist dokumentiert und der Befund gemeldet —
  die Phase gilt dann **nicht** als complete
</acceptance_criteria>

**Commit:** `docs(TEMPO-1-03): Live-Belege (cache_read-Zeilen, TTFT vorher/nachher, logs-errors)`

---

### Task 4 — ROADMAP + STATE von Hand nachziehen (Multi-Segment-Gotcha)

<read_first>
- `.planning/ROADMAP.md:2386-2425` — der TEMPO-1-Eintrag im **aktuellen** Stand. ★ Andre hat die
  ROADMAP am 2026-07-21 selbst korrigiert (Commit **69248b8**, gepusht, beide Roadmaps deckungsgleich)
  — **Ist-Stand lesen, keine Zeilennummer und keinen Alt-Text raten:**
  • `:2394` (Fund 2) traegt die **korrigierte Messung** („KORRIGIERT 2026-07-21", `pip_stream` als
    toter Tag benannt, gueltige Zahlen `ewb` avg 3.190 / `qa` avg 3.570, „3× drueber statt 7×")
    — **das war die Fundstelle der Falschaussage, nicht `:2411`. Nicht anfassen.**
  • `:2411` ist die **Beleg-Pflicht-Zeile**. Sie nannte **nie** einen `context_tag` — hier wird der
    gueltige Beleg-Query **ergaenzt**, nicht ein alter korrigiert.
  • `:2413` traegt „**Nebenfund ZURUECKGEZOGEN 2026-07-21**" — der `session_id`-Nebenfund ist
    gegenstandslos. **Nicht anfassen, nicht in den Backlog eintragen.**
- `.planning/STATE.md` — Kopf-`status`-Feld (Muster: wie KOSTEN-1 dort steht)
- Memory-Regel „Multi-segment phase ID gotcha": `gsd-tools`/`gsd-sdk` loesen `08.23.2.TEMPO-1`
  **falsch** auf → Pfade hardcoden, Dateien von Hand editieren
</read_first>

<action>
**A) `.planning/ROADMAP.md` — die Beleg-Pflicht-Zeile um den gueltigen Query ERGAENZEN** (heute
`:2411`; Zeilennummer im Ist-Stand verifizieren, nicht raten). Heute steht dort sinngemaess
„`api_cost_log` zeigt `unit_type='per_1k_cache_read_tokens'` mit `units > 0` fuer einen Antwort-Pfad.
Der Logging-Hook existiert bereits (Muster `claude_service.py:556ff`) — **pruefen, ob er auch am
EWB/QA-Pfad haengt**, sonst mit anlegen."

★ **Diese Zeile nannte NIE einen `context_tag`.** Der Ersatztext darf deshalb **nicht** behaupten,
hier haette frueher `pip_stream` gestanden — das waere eine neue Un-Wahrheit an genau der Stelle,
die eine Un-Wahrheit ausraeumen soll. Die falsche Erstmessung stand in Fund 2 (`:2394`) und ist
dort **bereits korrigiert** (Commit 69248b8). Ersetzen durch:
```markdown
**Beleg-Pflicht nach Deploy (Test-Anruf, PFLICHT) — Beleg-Query ergaenzt (Pre-Check 2026-07-21):**
`api_cost_log` zeigt `unit_type='per_1k_cache_read_tokens' AND units > 0 AND context_tag IN ('ewb','qa')`
— das sind die lebenden Antwort-Pfade. `pip_stream` ist ein **Mess-Label und existiert nicht als
DB-Wert** (Tag tot seit 16. April); die Erstmessung darueber ist in Fund 2 oben bereits korrigiert.
Wer nach `pip_stream` sucht, findet garantiert nichts und haelt eine funktionierende Aktivierung
faelschlich fuer gescheitert. Die Cache-Hooks haengen **bereits an allen drei** Antwort-Pfaden
(`claude_service.py:700-709` / `:853-862`, `qa_pipeline.py:488-497`) — es war nichts anzulegen.
Zusaetzlich: TTFT nicht schlechter als vorher (Waechter b, `claude_service.py:817`).
```

**Nicht anfassen:** die korrigierte Mess-Zeile (Fund 2, heute `:2394`) und die Rueckzugs-Notiz
(heute `:2413`). Beide sind bereits richtig. Und: die **alten** Zahlen (7.437 Input-Tokens,
„7× drueber") gehen **nicht** in die ROADMAP — gueltig sind die korrigierten aus Fund 2
(`ewb` avg 3.190 / `qa` avg 3.570, „3× drueber").

**B) Den Phasen-Eintrag auf den Ist-Stand setzen:** Marker `🟡` → Status-Zeile mit dem Ergebnis
(`✅ COMPLETE + LIVE <Datum>, git_head <sha>` bzw. bei rotem Beleg: `GEBAUT, BELEG OFFEN`) plus die
die Plan-Liste.

⚠ **Die Plan-Liste EXISTIERT bereits** — heute `ROADMAP.md:2422-2424` (Zeilennummer im Ist-Stand verifizieren), angelegt beim Planen, drei Zeilen
im Zustand `- [ ]`. Sie wird **in-place** auf `- [x]` umgestellt und um das Ergebnis ergaenzt.
**KEINE zweite Liste anlegen** — sonst steht jeder Plan-Dateiname zweimal in der ROADMAP, der
Zaehler unten wird **2**, und das Abnahme-Kriterium schlaegt fehl, obwohl der Task korrekt lief:
```
- [x] 08.23.2.TEMPO-1-00-PLAN.md — W0 Prefix-Stabilitaet (Sentinel/Kopie/order_by) + 4 Waechter
- [x] `08.23.2.TEMPO-1-01-PLAN.md` (W1) — toten Cache-Apparat entfernt, CACHE_ANTWORT als EINZIGER Schalter
- [x] `08.23.2.TEMPO-1-02-PLAN.md` (W2) — cache_control auf dem stabilen Antwort-Prefix (_layer-WERT, nicht Index) + Tests
- [x] `08.23.2.TEMPO-1-03-PLAN.md` (W3) — Deploy + drei Live-Belege
```

**C) `.planning/STATE.md`:** das `status`-Feld im Kopf nachziehen — nach dem Muster des
KOSTEN-1-Eintrags: was belegt ist, was offen ist, git_head, und die zwei Erkenntnisse, die ueber
diese Phase hinaus gelten:
1. **der `_layer`-Marker-Verlust** in `answer_system_content` (eine List-Comprehension hat eine
   bewusst gelegte Foundation-Markierung stillschweigend verworfen — Foundation-Marker muessen dort
   gelesen werden, wo sie noch existieren),
2. **der falsche Beleg-Query** (`pip_stream` war ein Mess-Label, kein DB-Wert — ein Beleg-Kriterium
   gehoert gegen die Literale im Code verifiziert, nicht gegen die Erinnerung).

**D) Nebenfunde — beide sind bereits erledigt bzw. erfasst. HIER IST NICHTS EINZUTRAGEN.**

⚠ **ZURUECKGEZOGEN (nicht eintragen):** der frueher gefuehrte `session_id`-Nebenfund
(„`pip_stream` schreibt keine `session_id`") ist **gegenstandslos** — ein toter Tag kann nichts
fehlen lassen, und die lebenden Pfade `ewb`/`qa` schreiben `session_id=sid` korrekt
(`claude_service.py:705/858`, `qa_pipeline.py:493`). Die ROADMAP fuehrt das bereits als
„Nebenfund ZURUECKGEZOGEN 2026-07-21" (heute `:2413`). **Kein Backlog-Eintrag, keine
„nur der …"-Sonderbehandlung beim Test-Anruf.**

⚠ **BEREITS ERFASST (nicht doppelt eintragen):** der Knopf-Pfad loggt seine Kosten hart als
`log_api_cost('anthropic', 'haiku-4-5', …)` (`claude_service.py:846-862`), obwohl er auf
`config.MODEL_PIP_VARIANTE` = `claude-sonnet-4-5` laeuft — die Zeilen landen unter dem **falschen
Modell** und damit zu **billig** (CLAUDE.md Punkt 30). Dieser Fund (`TEMPO1-KNOPF-MODELLNAME-FALSCH`)
steht **bereits** in der ROADMAP als eigene Folge-Phase **08.23.2.KOSTEN-1.1 „Modellnamen-Wahrheit
in der Kosten-Erfassung"** (heute `:2428ff`, Herkunfts-Zeile `:2430`). **Nicht erneut eintragen** —
ein zweiter Eintrag macht den Zaehler unten **2** und das Kriterium rot, obwohl der Task korrekt lief.
In dieser Phase bewusst nicht angefasst (Scope eng); relevant bleibt nur: es faelschte den
Kosten-Vergleich, wenn jemand den Cache-Nutzen in Euro nachrechnen will.
</action>

<acceptance_criteria>
- `grep -n "pip_stream" .planning/ROADMAP.md` — jeder verbleibende Treffer ist eine **legitime
  nicht-normative Erwaehnung**. Inhaltlich pruefen, **nicht** ueber feste Zeilennummern (die
  verschieben sich beim naechsten ROADMAP-Edit):
  (a) die **korrigierte Mess-Zeile** (Fund 2, „KORRIGIERT 2026-07-21" — nennt `pip_stream` als den
      toten Tag, dessen Erstmessung verworfen wurde);
  (b) die **Rueckzugs-Notiz** („Nebenfund ZURUECKGEZOGEN 2026-07-21");
  (c) der neu ergaenzte Erklaer-Satz „`pip_stream` ist ein Mess-Label und existiert nicht als DB-Wert".
  **Kein** Treffer darf als Beleg-/Pruef-Kriterium formuliert sein — genau das ist die Abnahme.
- `grep -c "context_tag IN ('ewb','qa')" .planning/ROADMAP.md` >= **1**
- `grep -c "08.23.2.TEMPO-1-01-PLAN.md" .planning/ROADMAP.md` == **1** — genau EINER, weil die
  bestehende Zeile `ROADMAP.md:2421` **in-place** von `- [ ]` auf `- [x]` gesetzt wird. Wird der
  Zaehler **2**, wurde faelschlich eine zweite Plan-Liste angelegt (siehe Warnung in Teil B)
- `grep -c "TEMPO-1" .planning/STATE.md` >= **1**
- `grep -c "TEMPO1-KNOPF-MODELLNAME-FALSCH" .planning/ROADMAP.md` == **1** — das ist der
  **bestehende** Treffer in der Herkunfts-Zeile von Phase 08.23.2.KOSTEN-1.1 (heute `:2430`).
  Der Task legt **keinen** neuen Eintrag an; wird der Zaehler **2**, wurde faelschlich doppelt
  eingetragen (siehe Warnung in Teil D).
- Kein `gsd-tools`/`gsd-sdk`-Aufruf in diesem Task (Multi-Segment-Gotcha)
- `git push origin main` gelaufen (CLAUDE.md Git-Regel: am Sessionende pushen)
</acceptance_criteria>

**Commit:** `docs(TEMPO-1-03): ROADMAP + STATE nachgezogen, Beleg-Query korrigiert, Backlog-Nebenfund`

</tasks>

<verify>
  <automated>bash deploy.sh production   # eingebautes Pytest-Gate auf dem Server, vor dem Restart</automated>
  <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "cd /opt/nerve/app && grep -cF \"_block['cache_control'] = {'type': 'ephemeral'}\" services/prompt_pipeline.py"   # muss 1 sein (CODE-Anker, -F = FIXED STRING: OHNE -F ist ['cache_control'] eine BRE-ZEICHENKLASSE und der Zaehler liefert 0 statt 1; ein grep -c auf den blossen Roh-String cache_control zaehlt Kommentar-/Docstring-Zeilen mit, bewegt sich mit jedem Nachzug und ist deshalb KEIN Kriterium — beides waere ein falsches Rot NACH dem Prod-Deploy)</automated>
  <manual>Test-Anruf mit MINDESTENS ZWEI Antworten → psql-Query auf api_cost_log (per_1k_cache_read_tokens, units>0, context_tag IN ('ewb','qa')) + [EWB-TTFT]-Vergleich gegen die Baseline aus Task 1 + inspect.sh logs-errors</manual>
</verify>

<success_criteria>
- Alle drei Belege aus `<verification>` des CONTEXT.md liegen **verbatim** in
  `08.23.2.TEMPO-1-VERIFICATION.md` — Beleg, nicht Zusicherung
- Die TTFT ist nicht schlechter als die Baseline (oder der Vergleich ist ehrlich als „nicht
  belastbar" markiert)
- ROADMAP + STATE von Hand nachgezogen, der falsche `pip_stream`-Beleg ist korrigiert
- Rollback-Pfad ist erprobt bekannt: `CACHE_ANTWORT=false` in **`/etc/nerve/.env`** + Restart,
  30 Sekunden, kein Deploy (die App-lokale `/opt/nerve/app/.env` existiert auf Prod nicht)
</success_criteria>

## 1. Lokaler Kontext (Punkt 14 Schicht 1)

Kein Code-Pfad geaendert. Der einzige Eingriff in ein laufendes System ist der Deploy selbst
(`deploy.sh production`), der sein eigenes Pytest-Gate **vor** dem Restart faehrt: bei rotem Gate
kein Restart, alter Code bleibt live.

## 2. Funktions-Skelett

n/a — dieser Plan schreibt keinen Code. Der Ablauf ist:
Baseline lesen → Tor 1 (git-stand) → Tor 2 (deploy.sh production) → Rauchtest → Checkpoint
(Test-Anruf) → drei Belege → Doku.

## 3. Cross-File-Awareness (Punkt 14 Schicht 3)

- Beleg-schreibende Stellen (**unveraendert**, nur endlich mit Werten `> 0`):
  `services/claude_service.py:700-709` (`ewb`), `:853-862` (`ewb`), `services/qa_pipeline.py:488-497` (`qa`).
- Beleg-lesende Stellen: nur dieser Plan (psql read-only) + das Founder-Dashboard.
- `deploy.sh`: faehrt zusaetzlich den Waisen-Prune (DEPLOY-PRUNE) — dieser Plan legt **keine** neuen
  Dateien auf dem Server an, also nichts zu whitelisten.

## 4. Edge-Case-Analyse

1. **Nur Write-Zeilen, keine Read-Zeile:** TTL (5 Min) abgelaufen oder Prefix-Wechsel durch F1
   (Rollenwechsel). Kein Defekt → Anruf mit zwei zeitnahen Antworten und konstanter Rolle
   wiederholen. (**NICHT** die Profil-Groesse — die erzeugt gar keine Zeile, siehe Punkt 2.)
2. **Gar keine Zeile:** ZUERST **Profil-Groesse** pruefen (Fable A3): liegt der Stabil-Block unter
   1.024 Tokens, wird der Marker still ignoriert → weder Write- noch Read-Zeile, **kein Defekt**.
   Test mit Profil id 6 wiederholen. **Erst danach**: Marker kommt nicht an → `env-keys`
   (CACHE_ANTWORT=false? **und kein `MODEL_PIP_*`/`MODEL_QA`-Override auf Haiku** — dort gilt
   4.096 statt 1.024 Tokens, Fable A4), Code-Stand,
   `logs-errors` — in dieser Reihenfolge.
3. **Leerer Read als „Beweis" fuer Abwesenheit:** die RLS-False-Negative-Falle. Deshalb faehrt der
   entscheidende Query als `postgres`, nicht als `nerve_app`.
4. **TTFT-Baseline zu duenn (n < 5):** ehrlich als „nicht belastbar" markieren statt einen
   Erfolg zu behaupten.
5. **Modellwechsel im Baseline-Zeitraum:** wuerde den TTFT-Vergleich verfaelschen → der `model=`-Wert
   wird deshalb mitprotokolliert (Task 1).
6. **Anthropic lehnt `cache_control` ab:** waere ein **lauter** Fehler in `logs-errors` →
   Abbruch-Kriterium in Task 2, Rollback per Schalter.

### Race-Condition-Audit (Punkt 14)
1. **Gleichzeitiges Feuern:** zwei parallele Anrufe waehrend des Belegs → verschiedene User =
   verschiedene Cache-Prefixe (E4). Der Beleg-Query filtert nach Zeitfenster → beim Test-Anruf
   sicherstellen, dass **nur** der Test-User telefoniert, sonst ist die Zuordnung unscharf.
   (Die `ewb`/`qa`-Hooks schreiben `session_id=sid` korrekt — der frueher notierte
   `session_id`-Nebenfund ist **zurueckgezogen**, siehe Task 4 D.)
2. **Verbindungsabbruch mid-Deploy:** `deploy.sh` restartet erst nach gruenem Gate.
3./4. n/a.

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

**Angefasste Tabellen:** `api_cost_log` — **ausschliesslich lesend**.

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt (Punkt 26) | Kann der Leser vorauslaufen? |
|---|---|---|---|---|
| `api_cost_log.unit_type` / `.units` / `.context_tag` / `.call_site` | gelesen (psql, dieser Plan) | Postgres-Tabelle `public.api_cost_log` | **synchron** im Cost-Hook direkt nach dem LLM-Call (`claude_service.py:853-862`, `qa_pipeline.py:488-497`) — **kein** Batch, **kein** Hintergrund-Thread | **nein** — der Query laeuft nach dem Test-Anruf. Einziger Sonderfall: der Hook schreibt nur bei `> 0`, „keine Zeile" ist also mehrdeutig (D3) → deshalb die dreistufige Diagnose in Task 3 |
| `[EWB-TTFT]`-Messwerte | gelesen (journalctl) | **journald**, nicht DB — RAM/Disk des Dienstes | live beim ersten Token | **ja**: Log-Rotation kann die Baseline abschneiden → deshalb Task 1 **vor** dem Deploy |

Der Schema-Beleg fuer `api_cost_log` ist in
`.planning/phases/08.23.2.KOSTEN-1-kosten-erfassung-dichtmachen/…-CONTEXT.md` gezogen worden
(dieselbe Tabelle, unveraendert seit KOSTEN-1). Vor dem Beleg-Query trotzdem einmal
`inspect.sh schema api_cost_log` fahren und den Output in die VERIFICATION legen — die
Spaltennamen im Query duerfen nicht geraten sein.

## 6. Punkt 23 — Schild-Pflicht

`api_cost_log` wird **nur gelesen**; die Schreiber (die drei Cost-Hooks) aendern sich **nicht** —
sie liefern lediglich erstmals Werte `> 0` fuer `per_1k_cache_read_tokens`. Der Lese-/Schreib-Pfad
der Tabelle bleibt damit unveraendert → **kein Schild-Nachzug faellig**. Falls das
`api_cost_log`-Schild den Cache-Unit-Typ heute gar nicht erwaehnt: das im Zuge von Task 4 pruefen
(`inspect.sh schilder api_cost_log`) und, falls es irrefuehrend ist, als Backlog-Punkt notieren —
**nicht** stillschweigend darueber hinweggehen (Schild-Aktualitaets-Pflicht).

<output>
Nach Abschluss: `.planning/phases/08.23.2.TEMPO-1-antwort-zwischenspeicher/08.23.2.TEMPO-1-03-SUMMARY.md`
plus `08.23.2.TEMPO-1-VERIFICATION.md` (die drei Belege im Volltext)
</output>
