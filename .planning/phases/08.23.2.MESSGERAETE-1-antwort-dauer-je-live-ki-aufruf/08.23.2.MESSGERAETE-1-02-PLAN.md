---
phase: 08.23.2.MESSGERAETE-1
plan: 02
type: execute
wave: 2
depends_on: ["08.23.2.MESSGERAETE-1-01"]
files_modified:
  - services/cost_tracker.py
  - services/claude_service.py
  - services/qa_pipeline.py
  - services/live_session.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Alle ACHT produktiven Live-LLM-Pfade (sieben in claude_service.py + classify_utterance in qa_pipeline.py) schreiben ihre reine API-Dauer als latency_ms in api_cost_log"
    - "Kein Mess-Anker misst Fremdarbeit mit: wo ein Argument des API-Aufrufs erst zur Aufrufzeit gebaut wird, wird es VOR den Anker gezogen"
    - "Die beiden Streaming-Pfade schreiben zusaetzlich ttft_ms (Zeit bis zum ersten Token)"
    - "Eine API-Antwort geht genau EINMAL in Ø/p50/p95 ein — nur die input-Token-Buchung traegt die Dauer"
    - "Die Messung verlangsamt den Live-Loop nicht und kann ihn nicht zum Absturz bringen"
    - "Die vorhandenen latency_e/latency_c bleiben unangetastet und landen NICHT in latency_ms"
    - "Die Pruning-Entscheidung zur logs/-Textdatei-Aggregation steht sichtbar im Code"
  artifacts:
    - path: "services/claude_service.py"
      provides: "sieben Messpunkte + zwei TTFT-Werte an den Cost-Hooks (der achte liegt in qa_pipeline.py)"
      contains: "_t_api_start"
    - path: "services/qa_pipeline.py"
      provides: "achter Messpunkt (qa_classifier, D-10)"
      contains: "_t_api_start"
    - path: "services/live_session.py"
      provides: "Pruning-Entscheidung D-09 als Code-Kommentar an der Latenz-Statistik"
      contains: "MESSGERAETE-1"
  key_links:
    - from: "services/claude_service.py"
      to: "services/cost_tracker.py::log_api_cost"
      via: "Keyword latency_ms= an der per_1k_input_tokens-Buchung"
      pattern: "latency_ms=_latency_ms"
    - from: "services/claude_service.py"
      to: "api_cost_log.ttft_ms"
      via: "Keyword ttft_ms= an den beiden Streaming-Buchungen"
      pattern: "ttft_ms=int\\(_ttft_ms\\)"
---

**Komplexitaet: 🟡** — Cross-AI-Review (`/gsd-review --gemini --claude`) ist Pflicht VOR Execute.

<objective>
Die eigentliche Messung: an allen **acht** produktiven Live-KI-Aufrufen wird die **reine API-Dauer**
genommen und an `log_api_cost` durchgereicht. Die beiden Streaming-Pfade liefern zusaetzlich die Zeit
bis zum ersten Token. Dazu die sichtbare Antwort auf die Pruning-Notiz.

Purpose: ohne diese Zahlen ist jede Tempo-Aussage eine Behauptung. Und sie muessen **die richtige
Sache** messen — die vorhandenen `latency_e`/`latency_c` messen etwas anderes (D-02) und werden
ausdruecklich nicht wiederverwendet.

Output: sieben Messpunkte in `services/claude_service.py` + einer in `services/qa_pipeline.py`,
zwei TTFT-Werte, ein begruendeter
Pruning-Kommentar in `services/live_session.py`, gruener Waechter aus Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-CONTEXT.md
@.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-01-SUMMARY.md
@CLAUDE.md
@services/claude_service.py
@services/cost_tracker.py
@tests/test_live_latency_coverage.py
</context>

<interfaces>
<!-- Alles, was der Executor an Vertraegen braucht. KEIN Erkunden noetig. -->

`services/claude_service.py` importiert `time` bereits auf Modul-Ebene (Z.3) → `time.monotonic()`
ist ueberall verfuegbar. Die funktionslokalen Aliase (`import time as _time_autovar` in
`streame_auto_variante`, `from time import monotonic as _monotonic` in
`streame_manual_ewb_variante`) binden **lokale Namen** und verdecken das Modul-`time` NICHT.

Signatur des Ziels (`services/cost_tracker.py:162-174`) — `latency_ms` ist bereits Keyword-Parameter,
`ttft_ms` kommt in Task 1 dieses Plans dazu:
```python
def log_api_cost(provider, model, user_id, units, unit_type, *, org_id=None, session_id=None,
                 context_tag=None, latency_ms=None, call_site=None) -> None
```

Bestehende TTFT-Berechnung, die bisher nur geprintet wird:
- `streame_auto_variante`: `_t_stream_start = _time_autovar.monotonic()` (`:853`) →
  `_ttft_ms = (_time_autovar.monotonic() - _t_stream_start) * 1000` (`:862`), zusaetzlich vom
  **EWB-Circuit-Breaker** verwendet (`_ewb_ttft_history.append(_ttft_ms)`, `:865`).
- `streame_manual_ewb_variante`: `_t_stream_start = _monotonic()` (`:1029`, **pro Retry-Versuch neu**) →
  `_ttft_ms = (_monotonic() - _t_stream_start) * 1000` (`:1041`).
</interfaces>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
- `public.api_cost_log` (schreibend, ueber `services/cost_tracker.py::log_api_cost`) — Spalten
  `latency_ms` (existiert auf Prod, siehe Plan 01) und `ttft_ms` (legt Plan 01 an).
- `services/live_session.py` wird **nur um einen Kommentar** ergaenzt — keine Tabelle betroffen.

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `_latency_ms` (neu) | geschrieben in 7 Live-Pfaden | DB-Spalte `api_cost_log.latency_ms` (integer, nullable) | ✓ inspect.sh in Plan 01 |
| `_ttft_ms` | bisher nur `print`; ab jetzt geschrieben in 2 Streaming-Pfaden | DB-Spalte `api_cost_log.ttft_ms` — **wird von Plan 01 angelegt** | ✓ Abhaengigkeit `depends_on: 01` |
| `latency_e` / `latency_c` | `live_session`-Puffer → `app_routes.py:385` Datei-Write | **KEINE DB-Spalte** — RAM + `logs/`-Textdatei | ✓ D-02: wird NICHT angefasst |
| `_ewb_ttft_history` | RAM, Circuit-Breaker | modul-globale `deque`, tenant-neutral (nur Millisekunden-Zahlen) | ✓ unveraendert, kein neuer globaler Zustand |

### Schreib-Zeitpunkt / Punkt 26
`log_api_cost` committet synchron im selben Aufruf (`cost_tracker.py:233`). Die Werte entstehen
unmittelbar davor im selben Thread. Kein Batch, kein Hintergrund-Job, kein Voraus-Laufen moeglich.

### Punkt 28 — kein globaler Live-Zustand
Alle neuen Variablen (`_t_api_start`, `_latency_ms`, `_ttft_ms`) sind **funktionslokal**. Es wird
kein modul-globaler veraenderlicher Zustand angelegt. `_ewb_ttft_history` bleibt unveraendert (steht
bereits begruendet auf der Whitelist als tenant-neutraler Zaehler).

## Vier Saeulen (Bau-Regel D)
- **Automatisieren:** die Messung laeuft bei jedem Aufruf ohne Zutun mit.
- **Nachvollziehen:** Tabelle `api_cost_log`, nicht Bildschirm/Textdatei.
- **Eingreifen:** Plan 03 macht sichtbar, welche Frage-Sorte teuer/langsam ist.
- **Marge:** Grundlage der Preis-Untergrenze.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ttft_ms in log_api_cost + die SECHS blockierenden Live-Pfade messen (inkl. Hoist bei analysiere_coaching)</name>

  <read_first>
    - `services/cost_tracker.py` VOLLSTAENDIG (insb. Z.162-241: Signatur, `db.add(ApiCostLog(...))`,
      der „darf NIEMALS raisen"-Rahmen)
    - `services/claude_service.py` Z.405-471 (`phase_classify`), Z.491-548 (`coldcall_infer`),
      Z.551-608 (`analysiere_mit_claude` / `live_haiku`), Z.733-786 (`analysiere_und_klassifiziere` /
      `live_haiku_merged`), Z.1112-1142 (`analysiere_coaching` / `coaching_haiku`)
    - `services/claude_service.py` Z.1-8 (Modul-Importe — `time` ist da)
    - `tests/test_live_latency_coverage.py` (der Waechter aus Plan 01 — er definiert den Vertrag,
      den dieser Task erfuellen muss)
    - `services/claude_service.py` Z.170-232 (`_build_coaching_prompt` VOLLSTAENDIG — insb. `ls.get_profile_for_sid(sid)` `:173` und `with ls._session_state_lock:` `:221`; das ist die Fremdarbeit, die NICHT ins Messfenster darf)
    - `services/qa_pipeline.py` Z.281-348 (`classify_utterance`: `messages.create` `:302`, Cost-Hook `:328-341`, `context_tag='qa_classifier'`)
    - CONTEXT D-10 (qa_classifier mitmessen) und `<specifics>` Punkt 14 (Mess-Anker darf kein Argument mitmessen — BLOCKER, beide Reviewer unabhaengig)
    - CONTEXT D-01 (was gemessen wird), D-02 (was NICHT wiederverwendet wird), D-07 (Doppelzaehlung)
    - CLAUDE.md → Punkt 25 (Latenz-Neutralitaet) und Punkt 30 (Cost-Hook darf nie raisen)
  </read_first>

  <files>services/cost_tracker.py, services/claude_service.py, services/qa_pipeline.py</files>

  <behavior>
    - `log_api_cost(..., ttft_ms=123)` schreibt 123 in `ApiCostLog.ttft_ms`; ohne das Keyword bleibt die Spalte NULL.
    - Jeder der sechs blockierenden Live-Pfade (fuenf in `claude_service.py`, `classify_utterance` in `qa_pipeline.py`) uebergibt an der `per_1k_input_tokens`-Buchung ein `latency_ms`, das der Dauer des `messages.create`-Aufrufs entspricht.
    - Output-Buchungen und Cache-Buchungen derselben Antwort uebergeben WEDER `latency_ms` NOCH `ttft_ms`.
    - Bei `analysiere_coaching` liegt der Prompt-Bau (`_build_coaching_prompt`) VOLLSTAENDIG vor dem Anker: die gemessene Zahl enthaelt weder den Profil-Load noch eine Wartezeit am `_session_state_lock`.
    - `qa_classifier` (`services/qa_pipeline.py::classify_utterance`) schreibt seine Dauer genauso wie die fuenf Pfade in `claude_service.py`.
    - Ein Fehler beim Messen bricht den Live-Aufruf nicht ab (der Wert bleibt None, der Call laeuft weiter).
  </behavior>

  <action>
**(a) `services/cost_tracker.py` — `ttft_ms` durchreichen.** Zwei Stellen, sonst nichts:

1. Signatur (nach `latency_ms`, vor `call_site`):
```python
    latency_ms: int | None = None,    # NEU: Phase 08.13
    ttft_ms: int | None = None,       # NEU: Phase 08.23.2.MESSGERAETE-1 (D-03, nur Streaming)
    call_site: str | None = None,     # NEU: Phase 08.13
```
2. Im `db.add(ApiCostLog(...))`-Block (Z.230f.), analog:
```python
                latency_ms=latency_ms,    # NEU: Phase 08.13
                ttft_ms=ttft_ms,          # NEU: Phase 08.23.2.MESSGERAETE-1
                call_site=call_site,      # NEU: Phase 08.13
```
Der Docstring bekommt eine Zeile dazu:
`ttft_ms: Zeit bis zum ersten Token (nur Streaming-Pfade, nur an der input-Token-Buchung)`.
**Sonst nichts an cost_tracker.py** — kein Wrapper, kein Framework, keine Umbenennung.

**(b) Das Mess-Muster.** In JEDER der sechs blockierenden Funktionen exakt so, mit dem kanonischen
Variablennamen `_t_api_start` (der Waechter aus Plan 01 kennt ihn):

```python
    # ── MESSGERAETE-1 (D-01): reine API-Dauer. Zwischen _t_api_start und _latency_ms
    # steht NUR der API-Aufruf — kein Prompt-Bau, kein Parsing, kein Emit, kein Cost-Hook.
    # Punkt 25 (Latenz-Neutralitaet): das sind zwei monotonic()-Lesungen, kein Netz, keine DB.
    _t_api_start = time.monotonic()
    msg = claude_client.messages.create(       # <- der Aufruf selbst bleibt Zeichen fuer Zeichen
        model=...,                             #    wie er ist: Modell, max_tokens, system,
        max_tokens=...,                        #    messages werden NICHT angefasst.
        system=...,                            #    ⚠ ABER: siehe (b2) — steht in einem dieser
        messages=[...],                        #    Argumente ein FUNKTIONSAUFRUF, wird er VOR
    )                                          #    den Anker gezogen, nicht hier gelassen.
    _latency_ms = int((time.monotonic() - _t_api_start) * 1000)
```

⚠ **Steht der `create` in einem `try:`-Block, gehoert der Anker MIT HINEIN** — direkt vor die
`resp = ...`-Zeile, nicht davor vor das `try:`. Betrifft `classify_phase` (`:422/:423`) und
`infer_customer_state` (`:499/:500`). Steht er ausserhalb, greift die Regex-Acceptance unten
nicht, und der Executor faengt an, die Regex „passend zu machen" statt den Code.

```

Und in der Cost-Hook-Buchung mit `unit_type='per_1k_input_tokens'` (und **nur** dort) das Keyword
ergaenzen:
```python
            log_api_cost('anthropic', 'haiku-4-5', user_id=None,
                         units=in_tok/1000.0, unit_type='per_1k_input_tokens',
                         context_tag='<unveraendert>', session_id=sid,
                         latency_ms=_latency_ms)
```

**(b2) HOIST-PFLICHT bei `analysiere_coaching` — eigener Schritt, KEIN Kommentar.**

Der Aufruf lautet heute (`services/claude_service.py:1120-1125`):

```python
    msg = claude_client.messages.create(
        model=config.MODEL_COACHING,
        max_tokens=200,
        system=_build_coaching_prompt(sid=sid),      # ← Funktionsaufruf IM Argument
        messages=[{"role": "user", "content": user_msg}]
    )
```

Python wertet Argumente **nach** der davorstehenden Anweisung aus. Stuende der Anker unmittelbar
davor, laege `_build_coaching_prompt` **im Messfenster** — und die Funktion laedt das
Verkaufsprofil (`ls.get_profile_for_sid(sid)`, `:173`) und **nimmt `ls._session_state_lock`**
(`:221-222`), den zentralen Live-Riegel. Bei Lock-Contention koennte die Zahl **beliebig** luegen.
`coaching_haiku` ist mit 132 Prod-Buchungen in 21 Tagen die zweitgroesste Sorte — kein Randfall.

**Der Zielzustand, woertlich:**

```python
    # ── MESSGERAETE-1 (Punkt 14, Cross-AI-BLOCKER 2026-08-03): Prompt-Bau VOR den Anker.
    # _build_coaching_prompt laedt das Profil und nimmt ls._session_state_lock. Bliebe der
    # Aufruf im Argument, wuerde Python ihn NACH _t_api_start auswerten und die Lock-Wartezeit
    # landete in latency_ms — die Zahl haette dann den falschen Namen (D-02-Fehlerklasse).
    # KEIN Waechter dieser Phase kann das sehen; dieser Hoist ist die einzige Verteidigung.
    _system_coaching = _build_coaching_prompt(sid=sid)
    _t_api_start = time.monotonic()
    msg = claude_client.messages.create(
        model=config.MODEL_COACHING,
        max_tokens=200,
        system=_system_coaching,
        messages=[{"role": "user", "content": user_msg}]
    )
    _latency_ms = int((time.monotonic() - _t_api_start) * 1000)
```

**Die anderen sechs Pfade brauchen KEINEN Hoist** — am Code einzeln geprueft (Cross-AI Fable,
2026-08-03): `classify_phase` (`prompt` `:414` vorab) · `infer_customer_state` (`:498`) ·
`analysiere_mit_claude` (`_system` `:571`, `user_msg` `:552`) · `analysiere_und_klassifiziere`
(`_MERGED_SYSTEM` ist Modul-Konstante `:640`) · `streame_auto_variante` (`_system_autovar` `:835`
vor dem Anker) · `streame_manual_ewb_variante` (`_system_manual` `:996` vor der Retry-Schleife).
Bei `classify_utterance` (b3) sind `system_prompt` und `user_msg` ebenfalls vorab gebaut
(`:292`/`:297`).

**Regel fuer alle acht:** steht in einem Argument des API-Aufrufs ein Funktionsaufruf, wird er in
eine eigene Zeile VOR den Anker gezogen. Ein Attributzugriff (`config.MODEL_X`) oder eine
Konstante bleibt, wo er ist.


**Die sechs Stellen konkret** — fuenf in `services/claude_service.py`, eine in `services/qa_pipeline.py`
(Funktionsname → `context_tag` → Zeile des `messages.create` im Ist-Stand; die Zeilennummern
verschieben sich beim Bearbeiten, die Funktionsnamen nicht):

| Funktion | `context_tag` | `messages.create` | input-Buchung | Besonderheit |
|---|---|---|---|---|
| `classify_phase` (`def` bei `:387`) | `phase_classify` | `:423` | `:435` | `create` steht im `try:` — Anker mit hinein |
| `infer_customer_state` | `coldcall_infer` | `:500` | `:512` | `create` steht im `try:` — Anker mit hinein |
| `analysiere_mit_claude` | `live_haiku` | `:572` | `:585` | rollback-only (Dormanz, siehe Plan 04) |
| `analysiere_und_klassifiziere` | `live_haiku_merged` | `:750` | `:764` | — |
| `analysiere_coaching` | `coaching_haiku` | `:1120` | `:1133` | **⚠ Hoist noetig, siehe (b2)** |
| `classify_utterance` (**services/qa_pipeline.py**) | `qa_classifier` | `:302` | `:333` | eigene Datei, D-10; rollback-only (Dormanz) |

**(b3) `qa_classifier` mitmessen — `services/qa_pipeline.py::classify_utterance` (D-10, GELOCKT).**

Warum diese Datei ueberhaupt: `classify_utterance` ist der **Rollback-Zwilling** von
`analysiere_mit_claude`. Bei `MERGE_ANALYSE_QA=0` feuern beide **gemeinsam** bei jedem Tick
(`claude_service.py:1216` + `:1813`). Ohne diesen Schritt waere der Rollback-Pfad **halb**
gemessen — und der Waechter saehe es nie, wenn er nur `claude_service.py` parste. Genau solche
handverlesenen Datei-Grenzen sind der Beleg-Fall dieser Phase.

Dasselbe Muster wie (b), an `services/qa_pipeline.py:302`:

```python
        _t_api_start = time.monotonic()
        msg = claude_client.messages.create(
            model=config.MODEL_ANALYSE,
            max_tokens=150,
            system=system_prompt,          # :292 vorab gebaut — kein Hoist noetig
            messages=[{"role": "user", "content": user_msg}]   # :297 vorab
        )
        _latency_ms = int((time.monotonic() - _t_api_start) * 1000)
```

`time` ist in `qa_pipeline.py` ggf. noch nicht importiert — dann `import time` zu den
Modul-Importen ergaenzen (kein funktionslokaler Import: der laege im heissen Pfad).

Und in der `per_1k_input_tokens`-Buchung (`:333`) `latency_ms=_latency_ms` ergaenzen.
Die Output-Buchung (`:336`) bleibt unveraendert (D-07).

⚠ **`generate_qa_response` (`qa_pipeline.py:443`) wird NICHT angefasst** — toter Code, repo-weit
null Produktiv-Aufrufer, per `tests/test_h1_qakill.py` als Runtime-Waechter festgenagelt (D-10,
beide Reviewer einig). Er steht mit Begruendung auf der Allowlist des Waechters (Plan 01 Task 2).
Ihn zu messen waere Wegwerf-Arbeit.


**(c) D-07 — Doppelzaehlung. ENTSCHEIDUNG UND BEGRUENDUNG (der Empfehlung aus D-07 wird gefolgt):**
`latency_ms` wird **ausschliesslich an der `per_1k_input_tokens`-Buchung** gesetzt.
Die Output-Buchung und die beiden Cache-Buchungen (`per_1k_cache_read_tokens`,
`per_1k_cache_write_tokens` in `analysiere_mit_claude` und `analysiere_und_klassifiziere`) bleiben
**unveraendert ohne** `latency_ms`/`ttft_ms`.

Begruendung, die als Kommentar **einmal** ueber die Buchung in `analysiere_mit_claude` gehoert
(dort gibt es alle vier Buchungsarten, also ist es die lehrreichste Stelle):

```python
        # ── MESSGERAETE-1 / D-07: die Dauer haengt NUR an der input-Token-Buchung ──────────
        # log_api_cost laeuft pro API-ANTWORT 2-4x (input, output, ggf. cache-read, cache-write).
        # Traegt jede dieser Zeilen dieselbe latency_ms, zaehlt EINE Antwort 2-4x in Ø/p50/p95 —
        # und der Mittelwert kippt in Richtung der Antworten, die zufaellig Cache-Zeilen erzeugt
        # haben. Die input-Buchung ist die einzige, die bei JEDER Antwort GENAU EINMAL existiert;
        # deshalb traegt sie die Dauer, und der Leser (routes/admin_dashboard.py) filtert auf
        # latency_ms IS NOT NULL. Cache-/Output-Zeilen bleiben bewusst NULL — das ist keine
        # Luecke, das IST die Entzerrung. tests/test_live_latency_coverage.py haelt beide
        # Richtungen fest (input MUSS, alle anderen DUERFEN NICHT).
```

**(d) Was ausdruecklich NICHT passiert (D-02, Andre-Falle 1):**
- `latency_e` (`claude_service.py:1181`→`:1326`) und `latency_c` (`:1985`→`:2010`) werden **nicht
  gelesen, nicht kopiert, nicht umgeleitet**. Sie messen ab dem ersten Puffer-Eintrag inklusive
  QA-Dispatch — ein anderes Mass. Wer sie in `latency_ms` schreibt, erzeugt ein Feld, dessen Name
  luegt.
- Es wird **kein** neuer Kontextmanager / Decorator / Timing-Helfer eingefuehrt. Zwei Zeilen
  `monotonic()` pro Pfad sind der einfachste tragfaehige Weg (Punkt 27); ein Wrapper um den
  Anthropic-Client waere genau das Framework, das der Bauplan seit KOSTEN-1 verbietet.
- `coaching_haiku` wird **nicht** abgeschaltet und **nicht** verschmolzen (eigene Phase nach
  METRIK-1). Es wird nur gemessen wie die anderen sieben.
  </action>

  <verify>
    <automated>grep -c "_t_api_start = time.monotonic()" services/claude_service.py</automated>
    <automated>grep -c "_t_api_start = time.monotonic()" services/qa_pipeline.py</automated>
    <automated>grep -c "ttft_ms" services/cost_tracker.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "_t_api_start = time.monotonic()" services/claude_service.py` == 5 (nach Task 2: 7)
    - `grep -c "_t_api_start = time.monotonic()" services/qa_pipeline.py` == 1
    - `grep -c "_latency_ms = int((time.monotonic() - _t_api_start) \* 1000)" services/claude_service.py` == 5 (nach Task 2: 7)
    - `grep -c "_latency_ms = int((time.monotonic() - _t_api_start) \* 1000)" services/qa_pipeline.py` == 1
    - `grep -c "latency_ms=_latency_ms" services/claude_service.py` == 5 (nach Task 2: 7)
    - `grep -c "latency_ms=_latency_ms" services/qa_pipeline.py` == 1
    - **R1/Hoist, greppbar am Code-Anker (nicht am Kommentar):** `grep -cF "_system_coaching = _build_coaching_prompt(sid=sid)" services/claude_service.py` == 1 **und** `grep -cF "system=_system_coaching," services/claude_service.py` == 1 **und** `grep -F "system=_build_coaching_prompt(sid=sid)," services/claude_service.py | wc -l` == 0 (der Aufruf steht nicht mehr im Argument)
    - **Kein Argument mehr, das erst zur Aufrufzeit gebaut wird** — AST-Gegenprobe ueber beide Dateien: `python -c "import ast,pathlib; bad=[]; [bad.extend((f, n.lineno) for n in ast.walk(ast.parse(pathlib.Path(f).read_text(encoding='utf-8'))) if isinstance(n, ast.Call) and getattr(getattr(n,'func',None),'attr','') in ('create','stream') and getattr(getattr(getattr(n,'func',None),'value',None),'attr','')=='messages' and any(isinstance(k.value, ast.Call) for k in n.keywords)) for f in ('services/claude_service.py','services/qa_pipeline.py')]; print('OK' if not bad else 'ARGUMENT-AUFRUF in '+str(bad))"` gibt `OK`
    - `grep -c "ttft_ms" services/cost_tracker.py` == 3 (Signatur, Docstring-Zeile, `db.add`-Kwarg)
    - `grep -cF "unit_type='per_1k_output_tokens'" services/claude_service.py` bleibt unveraendert gegenueber dem Stand vor diesem Task (Buchungen wurden ergaenzt, nicht umgebaut)
    - **D-07-Gegenprobe:** `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('services/claude_service.py').read_text(encoding='utf-8')+chr(10)+pathlib.Path('services/qa_pipeline.py').read_text(encoding='utf-8')); bad=[n.lineno for n in ast.walk(t) if isinstance(n,ast.Call) and getattr(getattr(n,'func',None),'id','')=='log_api_cost' and any(k.arg=='unit_type' and isinstance(k.value,ast.Constant) and k.value.value!='per_1k_input_tokens' for k in n.keywords) and any(k.arg in ('latency_ms','ttft_ms') for k in n.keywords)]; print('OK' if not bad else 'DOPPELZAEHLUNG in Zeilen '+str(bad))"` gibt `OK`
    - `grep -c "latency_e\|latency_c" services/claude_service.py` ist **unveraendert** gegenueber dem Stand vor diesem Task (D-02: nicht angefasst)
    - `grep -cF "MESSGERAETE-1 / D-07: die Dauer haengt NUR an der input-Token-Buchung" services/claude_service.py` == 1
    - Zwischen `_t_api_start = time.monotonic()` und dem `messages.create(` steht in allen fuenf
      Faellen **keine** weitere Anweisung: `python -c "import re,pathlib; s=pathlib.Path('services/claude_service.py').read_text(encoding='utf-8'); print(len(re.findall(r'_t_api_start = time\.monotonic\(\)\n\s*(?:msg|resp) = claude_client\.messages\.create\(', s)))"` gibt `5`
  </acceptance_criteria>
    - Dasselbe fuer `qa_pipeline.py`: dieselbe Regex gegen `services/qa_pipeline.py` gibt `1`

  <done>
`log_api_cost` kennt `ttft_ms`; die sechs blockierenden Live-Pfade (fuenf in `claude_service.py`, `classify_utterance` in `qa_pipeline.py`) messen die reine API-Dauer und
uebergeben sie an der input-Token-Buchung; Output-/Cache-Buchungen tragen die Dauer nicht;
`latency_e`/`latency_c` sind unberuehrt.
Bei `analysiere_coaching` liegt der Prompt-Bau nachweislich VOR dem Anker.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Die zwei Streaming-Pfade — latency_ms bis zum letzten, ttft_ms bis zum ersten Token</name>

  <read_first>
    - `services/claude_service.py` Z.789-960 (`streame_auto_variante`, komplett: Circuit-Breaker,
      Token-Schleife, Cost-Hook nach `stream.get_final_message()`)
    - `services/claude_service.py` Z.960-1110 (`streame_manual_ewb_variante`, komplett: **Retry-Loop
      Z.1023-1058**, `_t_stream_start` pro Versuch neu Z.1029, `break` bei Erfolg Z.1046, Cost-Hook
      in einem SEPARATEN try-Block Z.1059ff.)
    - CONTEXT D-03 (Streaming braucht zwei Zahlen) und Punkt 11 (Retry-Semantik)
    - CONTEXT Punkt 7 (`pip_autovar` ist der zweite Streaming-Pfad — die ROADMAP kannte ihn nicht)
    - CLAUDE.md → Punkt 25 (Latenz), Punkt 30 (nie raisen), Punkt 31
  </read_first>

  <files>services/claude_service.py</files>

  <behavior>
    - `pip_autovar` und `pip_variante` uebergeben an der input-Token-Buchung sowohl `latency_ms` (Stream-Ende) als auch `ttft_ms` (erstes Token).
    - Bei einem Retry im Manual-Pfad beziehen sich beide Zahlen auf den ERFOLGREICHEN Versuch — Backoff-Zeit und Fehlversuche sind NICHT enthalten.
    - Bricht der Stream ab, bleiben beide Werte None und der Cost-Hook laeuft trotzdem durch (bzw. wird gar nicht erreicht) — kein neuer Absturzpfad.
    - Der EWB-Circuit-Breaker verhaelt sich unveraendert (gleiche TTFT-Semantik, gleicher Startpunkt).
  </behavior>

  <action>
**(a) `streame_auto_variante` (`context_tag='pip_autovar'`).**

1. VOR dem `try:` (also vor Z.852) initialisieren, damit der Cost-Hook die Namen immer kennt:
```python
    _ttft_ms = None
    _latency_ms = None
```
2. `_t_stream_start` **umbenennen zu `_t_api_start`** und auf das Modul-`time` umstellen — EIN Anker
   fuer beide Zahlen (das ist kein Beifang, sondern die Voraussetzung dafuer, dass `ttft_ms` und
   `latency_ms` denselben Nullpunkt haben):
```python
        # ── MESSGERAETE-1 (D-01/D-03): EIN Nullpunkt fuer beide Zahlen.
        # ttft_ms = bis zum ERSTEN Token, latency_ms = bis zum LETZTEN (Stream-Ende).
        # Bewusste Abweichung von "nichts dazwischen": im Stream liegen die per-Token-
        # sio.emit-Aufrufe zwangslaeufig INNERHALB des Messfensters — das ist der Stream,
        # nicht Nachverarbeitung. Ein Emit ist ein RAM-Append im SocketIO-Puffer (µs),
        # kein Netz-Roundtrip; die Zahl bleibt aussagekraeftig.
        _t_api_start = time.monotonic()
        with claude_client.messages.stream(
```
   Innerhalb der Token-Schleife entsprechend:
   `_ttft_ms = (time.monotonic() - _t_api_start) * 1000`
   (die Circuit-Breaker-Logik bleibt **wortgleich** — sie liest denselben `_ttft_ms`).
3. UNMITTELBAR nach dem Ende des `with`-Blocks (also direkt vor `cleaned_display = full_text.strip()`):
```python
        _latency_ms = int((time.monotonic() - _t_api_start) * 1000)
```
4. In der `per_1k_input_tokens`-Buchung (`:910`):
```python
                log_api_cost('anthropic', _model_autovar, user_id=None,
                             units=in_tok/1000.0, unit_type='per_1k_input_tokens',
                             context_tag='pip_autovar', session_id=sid,
                             latency_ms=_latency_ms,
                             ttft_ms=int(_ttft_ms) if _ttft_ms is not None else None)
```
   Output- und Cache-Buchungen (`:913`, `:920`, `:924`) bleiben **unveraendert** (D-07).

**(b) `streame_manual_ewb_variante` (`context_tag='pip_variante'`) — mit Retry-Loop.**

1. VOR der `while attempts < 3`-Schleife (also vor Z.1023):
```python
    # ── MESSGERAETE-1 (Punkt 11): bei Retries zaehlt die Dauer des ERFOLGREICHEN Versuchs.
    # Beide Variablen werden PRO VERSUCH neu gesetzt; ein Fehlversuch samt Backoff faellt
    # damit aus der Zahl heraus. Begruendung: latency_ms soll die API-Dauer messen, nicht
    # Anthropics Ueberlast. Wer die Ueberlast messen will, zaehlt 529er — das ist eine
    # andere Frage und gehoert nicht in ein Feld namens "latency".
    _ttft_ms = None
    _latency_ms = None
```
2. Innerhalb der Schleife `_t_stream_start = _monotonic()` (Z.1029) ersetzen durch
   `_t_api_start = time.monotonic()`, und **direkt darueber** `_ttft_ms = None` setzen (Reset pro
   Versuch, neben dem bestehenden `_first_token_manual = True`). Der bestehende Kommentarblock
   Z.1025-1027 **bleibt erhalten**, aber die Erwaehnung von `_t_stream_start` **darin** wird auf
   `_t_api_start` nachgezogen und um den MESSGERAETE-1-Bezug ergaenzt. (Sonst widerspraeche der
   Kommentar dem Acceptance-Kriterium „0 Treffer auf `_t_stream_start`" — und der billigste Weg zu
   Gruen waere, den Schutz-Kommentar zu loeschen statt ihn nachzuziehen.)
3. In der Token-Schleife: `_ttft_ms = (time.monotonic() - _t_api_start) * 1000` (der bestehende
   `print(f"[EWB-TTFT] ...")` bleibt unveraendert — die Textdatei-/Log-Ausgabe ist kein
   Doppel-Buchhaltungsproblem, sondern die Live-Diagnose beim Test-Anruf).
4. **Direkt NACH dem `with`-Block und VOR dem `break`** (Z.1046) — Einrueckung eindeutig:
   dieselbe Ebene wie das `break` (12 Leerzeichen), **nicht** eine Ebene tiefer im `with`:

```python
                    full_text += token
                    sio.emit('pip_token', {...}, room=sid)
            # ← hier endet der with-Block (Stream geschlossen = letztes Token durch)
            _latency_ms = int((time.monotonic() - _t_api_start) * 1000)
            break  # Erfolg — raus aus Retry-Loop
```

   Beide Einrueckungen waeren fachlich vertretbar (der Unterschied ist der Schliess-Vorgang des
   Kontextmanagers, Mikrosekunden); die Vorgabe ist eindeutig, damit der Executor nicht raten
   muss. Entscheidend ist nur: **innerhalb** des `try` des erfolgreichen Versuchs, damit ein
   Fehlversuch die Zahl nicht setzt. Die lokalen Aliase `_time`/`_monotonic` bleiben bestehen
   (sie werden vom Backoff-`sleep` genutzt) — nur der Mess-Anker geht auf `time.monotonic()`.


5. In der `per_1k_input_tokens`-Buchung (`:1076`) analog zu (a)4, mit
   `context_tag='pip_variante'` und `_model_variante`.
   Output-/Cache-Buchungen (`:1079`, `:1086`, `:1090`) bleiben unveraendert.

**(c) Waechter gruen fahren.** Der Waechter aus Plan 01 muss jetzt vollstaendig gruen sein. Der
Lauf erfolgt serverseitig im Deploy-Tor (Plan 04) — fuer die Zwischenpruefung reicht der
statische AST-Check aus den Acceptance-Kriterien, weil der Waechter selbst ein reiner
Datei-Sweep ist.

**(d) Was NICHT passiert:** `_ewb_ttft_history`, der Circuit-Breaker-Schwellwert
(`EWB_SONNET_FALLBACK_TTFT_MS`) und das Fallback-Verhalten werden **nicht** angefasst. Keine
Aenderung an `max_tokens`, Prompts, Modellwahl oder Emit-Reihenfolge. Kein zusaetzlicher `sleep`,
kein zusaetzlicher DB-/Netz-Zugriff im Stream (Punkt 25).
  </action>

  <verify>
    <automated>grep -c "ttft_ms=" services/claude_service.py</automated>
    <automated>grep "_t_stream_start" services/claude_service.py | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "_t_api_start = time.monotonic()" services/claude_service.py` == 7 (5 aus Task 1 + 2 Streams)
    - `grep -c "latency_ms=_latency_ms" services/claude_service.py` == 7
    - `grep -c "ttft_ms=int(_ttft_ms) if _ttft_ms is not None else None" services/claude_service.py` == 2
    - `grep "_t_stream_start" services/claude_service.py | wc -l` == 0 (EIN Anker, kein zweiter Nullpunkt — inklusive der Erwaehnung im Kommentar Z.1025)
    - `grep -cF "bei Retries zaehlt die Dauer des ERFOLGREICHEN Versuchs" services/claude_service.py` == 1
    - `grep -cF "ttft_ms = bis zum ERSTEN Token, latency_ms = bis zum LETZTEN" services/claude_service.py` == 1
    - `grep -c "_ewb_ttft_history" services/claude_service.py` == 3 (unveraendert: Definition `:15`, `append` `:865`, `_history_snap = list(...)` `:867`)
    - `grep -cF "EWB_SONNET_FALLBACK_TTFT_MS" services/claude_service.py` == 1 (Circuit-Breaker unangetastet)
    - D-07-Gegenprobe aus Task 1 gibt weiterhin `OK`
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('services/claude_service.py').read_text(encoding='utf-8')); print('SYNTAX OK')"` gibt `SYNTAX OK`
    - **Kein lokales `pytest`** (CLAUDE.md HART, keine Ausnahmen). Die Zwischenpruefung dieses Tasks
      leisten die AST-/grep-Kriterien oben — sie pruefen exakt dieselben vier Bedingungen wie
      `tests/test_live_latency_coverage.py`, nur direkt. Der **einzige verbindliche Waechter-Lauf**
      ist das Deploy-Tor in Plan 04 (`deploy.sh production` faehrt
      `/opt/nerve/venv/bin/pytest tests/ -m "not live and not perf"` serverseitig, inklusive dieser
      Datei, VOR dem Neustart).
  </acceptance_criteria>

  <done>
Beide Streaming-Pfade schreiben zwei getrennte Zahlen aus EINEM Nullpunkt; bei Retries zaehlt der
erfolgreiche Versuch; der Circuit-Breaker verhaelt sich unveraendert; der Waechter aus Plan 01 ist
gruen.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pruning-Notiz D-09 sichtbar beantworten (logs/-Textdatei-Aggregation)</name>

  <read_first>
    - `services/live_session.py` Z.1370-1450 (die Latenz-Aggregation: `latenzen_einwand`,
      `latenzen_coaching`, `laengste_latenz` und der Block `LATENZ-STATISTIKEN` ab Z.1433)
    - `routes/app_routes.py` Z.365-390 (`_build_log_content` → Datei-Write nach `logs/`)
    - CONTEXT D-09 (die Frage) + D-02 (warum „obsolet" hier NICHT automatisch wahr ist)
    - CLAUDE.md → „Pruning-Notiz — Pflichtteil JEDER Phasen-SUMMARY" (Bau-Regel 7)
    - CLAUDE.md → „Zwei unverhandelbare Daten-Schranken" (Call-Logs sind heilig)
  </read_first>

  <files>services/live_session.py</files>

  <action>
**ENTSCHEIDUNG: Die `logs/`-Textdatei-Aggregation BLEIBT STEHEN. Sie wird NICHT entfernt.**

Begruendung (gehoert genauso in die SUMMARY):

1. **Sie misst etwas anderes.** Die Aggregation summiert `latency_e`/`latency_c` — also
   „wie lange lag der Satz im Puffer + KI + Nachverarbeitung" (D-02). `api_cost_log.latency_ms`
   misst die **reine API-Dauer**. Das eine ersetzt das andere nicht; es sind zwei verschiedene
   Fragen („warum fuehlt sich der Live-Loop langsam an?" vs. „wie lange braucht die API?").
   Sie werden erst zusammen aussagekraeftig: die Differenz der beiden ist die Wartezeit, die NICHT
   an Anthropic liegt — genau die Zahl, die eine spaetere Tempo-Phase braucht.
2. **„Obsolet" waere hier eine Behauptung, keine Feststellung.** Genau davor warnt D-09
   ausdruecklich.
3. **Sie ist Teil des Call-Log-Textes**, nicht eine Nebenausgabe. Call-Logs sind laut den
   unverhandelbaren Daten-Schranken heilig; das Entfernen einer Sektion aus einem laufenden
   Protokoll-Format ist eine eigene Entscheidung, kein Beifang dieser Phase (Reparatur-Modus-Regel:
   nur was die Aufgabe braucht).
4. **Doppelfuehrung liegt nicht vor**, weil es zwei verschiedene Masse sind — die Gefahr, vor der
   D-09 schuetzt, tritt hier nicht ein. Sie traete ein, wenn jemand spaeter `latency_e` in
   `latency_ms` kippt; genau das verbietet D-02 und der Kommentar unten nagelt es fest.

**Umsetzung:** Ein Kommentarblock unmittelbar VOR `lines.append("  LATENZ-STATISTIKEN")`
(`services/live_session.py:1435`):

```python
    # ── MESSGERAETE-1 (D-09, Pruning-Entscheidung 2026-08-03): BLEIBT BEWUSST STEHEN ────────
    # Diese Statistik aggregiert latency_e/latency_c — das Mass "erster Puffer-Eintrag bis
    # nach der Nachverarbeitung", inklusive Wartezeit und QA-Dispatch. Seit MESSGERAETE-1
    # steht in api_cost_log.latency_ms die REINE API-Dauer. Das sind ZWEI VERSCHIEDENE ZAHLEN,
    # keine Doppelfuehrung: ihre Differenz ist genau die Wartezeit, die NICHT an der API liegt.
    # ⚠ Wer diese Werte je in api_cost_log.latency_ms schreibt, erzeugt ein Feld, dessen Name
    #   luegt (D-02). Diese Statistik bleibt in der logs/-Textdatei und NUR dort.
```

**Sonst nichts an `live_session.py`.** Kein Refactor, keine Umbenennung, kein Entfernen. Der Diff
dieses Tasks besteht aus genau diesem Kommentarblock.

**(b) Punkt-18-Gegenprobe: Bestandstests gegen den geaenderten Vertrag.**
Diese Phase aendert den Vertrag von `log_api_cost` (neues Keyword) und von `ApiCostLog` (neue
Spalte) — vertragsaendernde Wellen brechen erfahrungsgemaess Bestandstests, und ein lokaler
AST-/grep-Check ist kein Abnahme-Ersatz. Deshalb ausdruecklich gegenpruefen, **welche Tests den
Vertrag assertieren**:

```bash
grep -rln "log_api_cost\|ApiCostLog\|EWB-TTFT\|_ewb_ttft_history" tests/ --include="*.py"
```

Bekannter Stand aus der Planung (2026-08-03, alle **unkritisch** — muss trotzdem nachgemessen
werden, nicht abgeschrieben):
- `tests/test_08_13_01_cost_tracker_extension.py` — prueft nur, DASS `latency_ms`/`call_site` in
  Signatur und Modell existieren. Ein zusaetzliches Keyword bricht das nicht.
- `tests/test_cost_model_truth.py` — AST-Sweep ueber das **zweite** Argument (Modellname). Neue
  Keywords sind ihm gleichgueltig.
- `tests/test_cost_hook_coverage.py` — Datei-Granularitaet auf `log_api_cost(`. Unveraendert.
- `tests/test_heiler_resolved.py` — prueft den `[EWB-TTFT]`-`print`. Der bleibt woertlich stehen
  (Plan 02 Task 2 (b)3) → ueberlebt die Umbenennung von `_t_stream_start`.
- `tests/test_no_live_global_state.py` — Whitelist enthaelt `_ewb_ttft_history`; es kommt kein
  neuer globaler Zustand dazu (alle neuen Variablen sind funktionslokal).

**Das Ergebnis dieser Gegenprobe gehoert mit Dateinamen in den Commit-Text** — „keine
Bestandstests betroffen" ist nur dann eine Aussage, wenn dabeisteht, welche geprueft wurden.
Bricht doch einer: nachziehen ist Teil DIESES Tasks, nicht Aufgabe des Deploy-Tors.
  </action>

  <verify>
    <automated>grep -c "MESSGERAETE-1 (D-09, Pruning-Entscheidung" services/live_session.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "MESSGERAETE-1 (D-09, Pruning-Entscheidung 2026-08-03): BLEIBT BEWUSST STEHEN" services/live_session.py` == 1
    - `grep -cF "ZWEI VERSCHIEDENE ZAHLEN" services/live_session.py` == 1
    - `grep -cF "LATENZ-STATISTIKEN" services/live_session.py` == 1 (die Sektion existiert weiterhin — nichts geloescht)
    - `grep -c "latenzen_einwand\|latenzen_coaching" services/live_session.py` ist **unveraendert** gegenueber dem Stand vor diesem Task
    - `git diff --stat services/live_session.py` zeigt **nur Additionen** (`0 deletions`)
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('services/live_session.py').read_text(encoding='utf-8')); print('SYNTAX OK')"` gibt `SYNTAX OK`
    - **Punkt 18:** der Commit-Text nennt die gegengeprueften Bestandstest-Dateien namentlich
      (mindestens `test_08_13_01_cost_tracker_extension.py`, `test_cost_model_truth.py`,
      `test_cost_hook_coverage.py`, `test_heiler_resolved.py`, `test_no_live_global_state.py`)
      und das Ergebnis je Datei
  </acceptance_criteria>

  <done>
Die Pruning-Frage aus D-09 ist sichtbar und begruendet beantwortet — im Code, nicht nur im Plan —
und die Aggregation ist unveraendert erhalten.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live-Loop → Anthropic-API | Der heisse Pfad; jede zusaetzliche Anweisung wirkt direkt auf die spuerbare Antwortzeit |
| Live-Loop → `api_cost_log` | Der Cost-Hook schreibt aus dem Live-Thread in die DB; er darf den Anruf nie abbrechen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MESS-06 | Denial of Service | Messung im Live-Pfad (`services/claude_service.py`, 7 Stellen) | mitigate | Nur zwei `time.monotonic()`-Lesungen pro Aufruf, keine DB-, Netz- oder Lock-Operation, kein `sleep`. CLAUDE.md Punkt 25. Als Acceptance greppbar: keine neue Anweisung zwischen `_t_api_start` und dem API-Aufruf. |
| T-MESS-07 | Denial of Service | Cost-Hook wirft wegen des neuen Keywords | mitigate | Die neuen Keywords stehen **innerhalb** des bestehenden `try/except` der Cost-Hooks (Punkt 30: darf nie raisen). `_latency_ms`/`_ttft_ms` werden VOR dem `try` auf `None` initialisiert → kein `NameError` auf Fehlerpfaden. `log_api_cost` selbst faengt weiterhin alles ab (`cost_tracker.py:239`). |
| T-MESS-08 | Tampering | Falscher Nullpunkt → Zahl luegt | mitigate | EIN Anker `_t_api_start` pro Aufruf, `_t_stream_start` verschwindet (greppbar: 0 Treffer). Regex-Acceptance prueft, dass zwischen Anker und `messages.create` nichts steht. Der Waechter kann das NICHT pruefen (steht als RESTLUECKE 2 in seinem Docstring) — die zweite Schicht ist die Abnahme an echten Daten (Plan 04). |
| T-MESS-09 | Information Disclosure | Neue Werte in der DB | accept | `latency_ms`/`ttft_ms` sind reine Millisekunden-Zahlen ohne Personenbezug. Es wird KEIN Transkript, KEIN Prompt und KEIN Antworttext zusaetzlich persistiert. Die DSGVO-Lage von `api_cost_log` bleibt unveraendert. |
| T-MESS-10 | Tampering | Doppelzaehlung ueber Cache-/Output-Buchungen | mitigate | `latency_ms`/`ttft_ms` nur an der input-Buchung (D-07); AST-Gegenprobe in den Acceptance-Kriterien; `test_live_latency_coverage.py` haelt beide Richtungen dauerhaft fest. |
| T-MESS-11 | Repudiation | Retry-Semantik unklar → spaetere Fehldeutung der Zahlen | mitigate | Die Bedeutung („Dauer des erfolgreichen Versuchs, ohne Backoff") steht als Code-Kommentar an der Stelle selbst und ist greppbares Acceptance-Kriterium. |
</threat_model>

<verification>
1. `grep -c "_t_api_start = time.monotonic()" services/claude_service.py` == 7
2. `grep -c "latency_ms=_latency_ms" services/claude_service.py` == 7
3. `grep -c "ttft_ms=int(_ttft_ms) if _ttft_ms is not None else None" services/claude_service.py` == 2
4. `grep "_t_stream_start" services/claude_service.py | wc -l` == 0
5. D-07-AST-Gegenprobe gibt `OK` (keine Dauer an Output-/Cache-Buchungen)
6. Waechter-Lauf ausschliesslich serverseitig im Deploy-Tor (Plan 04) - **kein lokales `pytest`** (CLAUDE.md HART, keine Ausnahmen)
7. `git diff --stat services/live_session.py` → nur Additionen
8. `grep -c "latency_e\|latency_c" services/claude_service.py` unveraendert (D-02)
</verification>

<success_criteria>
- Alle acht Live-Pfade uebergeben `latency_ms` an der input-Token-Buchung
- Beide Streaming-Pfade uebergeben zusaetzlich `ttft_ms` aus demselben Nullpunkt
- Keine Output-/Cache-Buchung traegt eine Dauer (D-07)
- Retry-Semantik ist im Code begruendet
- `latency_e`/`latency_c` unangetastet; Pruning-Entscheidung steht im Code
- Der Waechter aus Plan 01 ist gruen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-02-SUMMARY.md`
— inklusive der Pruning-Entscheidung im Wortlaut (Bau-Regel 7).
</output>
