---
phase: 08.23.2.MESSGERAETE-1
reviewers: [gemini, fable]
reviewed_at: 2026-08-03
plans_reviewed:
  - 08.23.2.MESSGERAETE-1-01-PLAN.md
  - 08.23.2.MESSGERAETE-1-02-PLAN.md
  - 08.23.2.MESSGERAETE-1-03-PLAN.md
  - 08.23.2.MESSGERAETE-1-04-PLAN.md
plan_commit: 086b3ab
---

# Cross-AI Plan Review — Phase 08.23.2.MESSGERAETE-1

**Reviewer:** Gemini CLI (Repo-Zugriff) · Fable via claude-CLI (`--model claude-fable-5`, Repo-Zugriff).
Codex/OpenCode/Qwen/Cursor/CodeRabbit sind auf diesem Rechner nicht installiert.
Die claude-CLI wurde bewusst auf **Fable** gesetzt — die orchestrierende Instanz ist Opus,
ein Opus-Review waere keine zweite Sicht gewesen.

Beiden Reviewern wurden die drei Befunde aus Claudians Plan-Gegenlesen **zur Gegenpruefung**
vorgelegt, nicht zur Bestaetigung.

---

## ★ Entscheidungen dieser Runde (fuer den Replan bindend)

| # | Punkt | Ergebnis |
|---|---|---|
| **A** | BEFUND 1 — Coaching-Anker misst Fremdarbeit mit | **BESTAETIGT von beiden, unabhaengig. BLOCKER.** Fix: Hoist als expliziter Plan-Schritt. |
| **B** | BEFUND 2 — „sieben produktive Pfade" ist falsch | **BESTAETIGT von beiden am Code (Aufrufer-Suche, nicht Kommentare).** Truths + Abnahme korrigieren. |
| **C** | BEFUND 3 — Zwei-Tabellen-Rework | **Umsetzungsweg geklaert** (beide schlagen eine Single-List vor, unterschiedlicher Ort — siehe Divergenz D-2). |
| **D** | PUNKT 4 — `qa_pipeline.py` | **Widerspruch aufgeloest — siehe „Schiedsspruch" unten. Gemini hat sich geirrt.** |
| **E** | PUNKT 5 — sproede CSS-Regel | **BESTAETIGT von beiden**, zwei kompatible Ersatz-Kriterien vorgeschlagen. |
| **F** | PUNKT 6 — Farben/Tokens | **Plan 03 ist sauber**: null Farben, null `style=`, null Tokens. `var(--accent)` kommt nirgends vor. |

---

## ★★ Schiedsspruch PUNKT 4 — am Code entschieden (Gemini widerlegt)

Die beiden Reviewer widersprachen sich, deshalb hier die Entscheidung mit Beleg:

- **Gemini:** `classify_utterance` werde „aktiv aus dem `analyse_loop` gerufen" → achter produktiver
  Live-Call, Schweregrad HOCH.
- **Fable:** `classify_utterance` sei **rollback-only**, exakt wie `live_haiku` → MITTEL.

**Fable hat recht.** Beleg (`services/claude_service.py`):

```
:1211  if config.MERGE_ANALYSE_QA == '1':
:1214      _qa_section = merged.get('qa', {})   # fail-open {} bei Truncation
:1215  else:
:1217      _qa_section = None                   # Signal fuer den eigenen classify_utterance-Call
...
:1802  if qa_section is not None:               # ← {} ist NICHT None
:1813  else:  _qa_result = classify_utterance(...)
```

Bei Truncation entsteht `{}`, und `{}` **ist nicht `None`** — der Merge-Zweig greift, `classify_utterance`
wird **nicht** gerufen. Der `else`-Zweig ist ausschliesslich ueber `MERGE_ANALYSE_QA != '1'` erreichbar,
und `config.py:80` liest die Env **beim Import** (Default `'1'`) → Umschalten braucht einen Neustart.
Prod-Beleg deckt sich: `qa_classifier` nur am 23.07. (Rollback-Schalter-Test).

**`generate_qa_response`** (`qa_pipeline.py:443`, `context_tag='qa_response'`): repo-weite Aufrufer-Suche
zeigt **null** Produktiv-Call-Sites — nur Definition, Kommentare und Tests; `tests/test_h1_qakill.py`
nagelt das als Runtime-Waechter fest. **Toter Code, kein fehlender Messpfad.** Beide Reviewer einig.

**Die Konsequenz bleibt trotzdem bestehen** (und ist Fables eigentlicher Fund, M-2):
`qa_classifier` ist der **Rollback-Zwilling** von `live_haiku`. Zieht jemand den Schalter, feuern beide
**gemeinsam** bei jedem Tick (`:1216` + `:1813`) — `live_haiku` dann **mit** Dauer, `qa_classifier`
**ohne**, und der Waechter sieht es nie, weil er nur `claude_service.py` parst.
Die Phase wuerde also einen Rollback-Pfad messen und seinen Zwilling auslassen.

---

## Gemini Review

### Zusammenfassung
Der Gesamtplan ist solide, durchdacht und verfolgt mit dem Waechter-Konzept und der „Erst-ROT"-Strategie
einen robusten Ansatz. Die Trennung von `latency_ms` und `ttft_ms` ist konzeptionell sauber, Migrations-
und Deploy-Plaene sind umsichtig. Der Review deckt **zwei kritische Luecken (HOCH)** auf, die das Kernziel —
lueckenlose und korrekte Messung aller Live-Pfade — untergraben haetten. Nach Korrektur: Risiko **NIEDRIG**,
ohne Korrektur **HOCH**.

### BEFUND 1 — BESTAETIGT
`system=_build_coaching_prompt(sid=sid)` steht innerhalb des `messages.create`-Aufrufs; die Zeitmessung
wuerde den Prompt-Aufbau miterfassen. Die Funktion ist teuer (`ls.get_profile_for_sid(sid)`).
Die anderen sechs Pfade sind sauber.

### BEFUND 2 — BESTAETIGT
`grep` nach `streame_auto_variante` findet **keine** Aufrufe im produktiven Code (`routes/`, `services/`) →
dormant. `config.py` + `claude_service.py:1209-1216` bestaetigen: `analysiere_mit_claude` ist nur der
Rollback-Pfad fuer `MERGE_ANALYSE_QA=0`. **De facto fuenf aktive Live-Pfade.**
Zusatzargument: der TTFT-Nachweis haengt damit an einem **einzigen** Pfad — ein spezifischer Bug dort
wuerde das Zwei-Spalten-Konzept faelschlich bestaetigen oder widerlegen.

### BEFUND 3 — Umsetzungsvorschlag
Konstante `LIVE_CONTEXT_TAGS: set[str]` in `routes/admin_dashboard.py` als alleinige Wahrheitsquelle;
**eine** DB-Abfrage, Split in Python, zwei Listen ans Template. Neue unbekannte Tags landen automatisch
und sicher in „Uebrige Kosten". Der Waechter `tests/test_live_latency_coverage.py` soll die Liste
**importieren** und pruefen, dass seine AST-Funde eine Teilmenge davon sind — das erzwingt Synchronitaet
zwischen Waechter und Leser.

### PUNKT 5 — Ersatz-Kriterium
`START`/`END`-Kommentarpaar im CSS, dann:
```bash
sed -n '/MESSGERAETE-1.*START/,/MESSGERAETE-1.*END/p' static/admin_dashboard.css \
  | grep -E "#[0-9A-Fa-f]{3,8}|rgba?\(|var\(--" | wc -l   # == 0
```

### PUNKT 6
`var(--accent)` ist als **Phantom-Token** bestaetigt — weder in `static/nerve.css` noch in
`static/admin_dashboard.css` definiert. Plan 03 verwendet es erfreulicherweise nicht.

### Staerken
„Erst ROT, dann GRUEN" · AST-basierter Waechter statt Handliste · saubere `latency_ms`/`ttft_ms`-Trennung ·
umsichtige Migration/Deploy-Plaene (`inspect.sh`, `_migrate()`-Falle) · erzwungene sichtbare
Pruning-Entscheidung (D-09).

### Bedenken
- **(HOCH)** Unbemessener Call in `services/qa_pipeline.py:classify_utterance` — *siehe Schiedsspruch: Schweregrad ueberbewertet, der Pfad ist rollback-only.*
- **(HOCH)** Fehlerhafter Mess-Anker in `analysiere_coaching`.
- **(MITTEL)** TTFT-Nachweis stuetzt sich auf `pip_autovar`, der nachweislich dormant ist.
- **(MITTEL)** `grep -cF` sei „laut ROADMAP instabil". — **⚠ FEHLLESUNG, nicht uebernehmen:** die Projektlehre
  schreibt `-F` fuer Literale mit `[ ]` gerade **vor**. Dieser Vorschlag wuerde eine korrekte Regel kaputtmachen.
- **(NIEDRIG)** Kein `context_tag`-Filter in Plan 03.

### Risiko-Gesamturteil: **MITTEL**

---

## Fable Review

> Alle Zeilennummern am realen Repo-Stand nachgelesen, nicht aus den Plaenen uebernommen.

### BEFUND 1 — BESTAETIGT, BLOCKER, betrifft **genau einen** der sieben Pfade

`_build_coaching_prompt` (`:170-230`) ruft `ls.get_profile_for_sid(sid)` (`:173`) und nimmt
`ls._session_state_lock` (`:221-222`). Weil `_session_state_lock` der **zentrale Live-Riegel** ist,
kann die Zahl bei Lock-Contention **beliebig** luegen — kein Randfall bei 132 Prod-Buchungen.

**Einzelpruefung aller sieben Pfade** (Argument fuer Argument):

| # | Pfad | teure Auswertung im Argument? | Beleg |
|---|---|---|---|
| 1 | `classify_phase` (`:423`) | Nein — `prompt` `:414` vorab | sauber |
| 2 | `infer_customer_state` (`:500`) | Nein — `prompt` `:498` vorab | sauber |
| 3 | `analysiere_mit_claude` (`:572`) | Nein — `_system` `:571`, `user_msg` `:552` vorab | sauber |
| 4 | `analysiere_und_klassifiziere` (`:750`) | Nein — `_MERGED_SYSTEM` Modul-Konstante (`:640`) | sauber |
| 5 | `streame_auto_variante` (`:854`) | Nein — `_system_autovar` `:835` vor dem Anker | sauber |
| 6 | `streame_manual_ewb_variante` (`:1031`) | Nein — `_system_manual` `:996` vor der Retry-Schleife | sauber |
| 7 | `analysiere_coaching` (`:1123`) | **JA** — Profil-Load **+ `_session_state_lock`** | **BLOCKER** |

**Wichtig fuer den Replan:** Plan 02 (b) zeigt das Muster `messages.create(...unveraendert...)` —
„unveraendert" ist fuer Pfad 7 **genau falsch**. Der Hoist muss ein **expliziter Schritt** sein, sonst
baut der Executor die Falle wortgetreu nach. Nach dem Hoist greift die Regex-Acceptance weiter, die Zahl 5
bleibt korrekt.

**Zusatzbeobachtung (M-3):** bei `classify_phase`/`infer_customer_state` steht der `create` **im `try:`-Block** —
der Anker muss mit hinein, sonst schlaegt die Regex-Acceptance falsch-rot an.

### BEFUND 2 — BESTAETIGT
`streame_auto_variante`: Treffer nur in Definition (`:789`), Kommentaren (`:564`, `:801`, `:967`,
`deepgram_service.py:302/:311`) und Tests. Der frueher einzige Produktiv-Caller (Keyword-Pipe) ist entfernt —
`deepgram_service.py:300-313` emittiert nur noch `ewb_signal`. **0 Produktiv-Aufrufer.**
`analysiere_mit_claude`: einziger Produktiv-Aufruf `:1216`, nur im `else`-Zweig von `:1211`.
`config.py:80` liest die Env **beim Import** → Umschalten braucht Neustart.

**Die Umformulierung reisst keine neue Luecke** — der statische Waechter (MINDEST 7) haelt die dormanten
Pfade weiterhin fest; nur die **Daten**-Abnahme kann sie strukturell nicht sehen. Zwei Auflagen:

1. **Plan-04-Truths korrigieren.** Task 3 ist mit „jede **im Anruf vorgekommene** Live-Sorte" bereits
   richtig gescoped; Frontmatter-Truth und die „falls ausgeloest"-Erwaehnung von `pip_autovar` muessen
   nachgezogen werden. In die SUMMARY gehoert: *„live_haiku + pip_autovar: Messung eingebaut und statisch
   bewacht, an echten Daten NICHT belegt (dormant/rollback-only)."*
2. **Reicht `pip_variante` allein als TTFT-Beweis?** Fuer das, was D-06 beweisen soll — **ja**:
   ein Pfad, der beide Spalten schreibt, plus `avg_ttft < avg_lat`, beweist Schema, Schreiber, Leser und
   die Zwei-Spalten-Trennung Ende-zu-Ende. Was er **nicht** beweist: dass der `pip_autovar`-Einbau korrekt
   *misst* (nur, DASS er das Keyword traegt). Gehoert namentlich zu RESTLUECKE 2.

### BEFUND 3 — WO die eine Liste lebt

**Empfehlung: Modul-Konstante in `services/cost_tracker.py`** — `LIVE_LLM_CONTEXT_TAGS: dict[str, str]`
(Tag → deutsches Label) plus `CACHE_CONTEXT_TAGS = {'analyse': …, 'ewb': …}`.

- `cost_tracker.py` **besitzt** die `context_tag`-Semantik bereits (einziger Schreiber) und ist import-leicht —
  `admin_dashboard.py` nutzt sie fuer Query-Split **und** Beschriftung. Ein Pflegeort.
- **Der Waechter darf sie NICHT importieren** (Plan-01-Acceptance „0 Treffer auf `import services`" — richtig so).
  Loesung ohne zweiten Pflegeort: der Waechter liest die Konstante **per AST aus der Datei**
  (`ast.literal_eval` auf das Dict-Literal) und bekommt einen **fuenften Test**
  `test_live_tag_liste_ist_synchron`: die AST-abgeleiteten `context_tag`-Literale aus `claude_service.py`
  muessen exakt `set(LIVE_LLM_CONTEXT_TAGS) − CACHE_TAGS` sein. Der Waechter ist damit selbst der
  Drift-Schutz — der Geist von D-05. (`def test_ == 4` wird `== 5`.)
- Cache-Tag-Verortung am Code bestaetigt: `:597/:601/:775/:779` → `analyse`, `:922/:926/:1088/:1092` → `ewb`.

**Unbekannter neuer Tag → faellt still in Tabelle 2.** Fuer Nicht-LLM-Kosten gewuenscht. Fuer einen neuen
Live-LLM-Pfad **in `claude_service.py`** kann es nicht still passieren (Waechter + Sync-Test greifen).
**Echte Restluecke:** ein neuer Live-Pfad in einer **anderen Datei** landet sichtbar in Tabelle 2, aber
ohne Dauer und **ohne Alarm** — gehoert namentlich in RESTLUECKE 3.

**Folge fuer Plan 03 (Rework):** Query bleibt eine Summen-Query (Split in Python), Template bekommt die
zweite Tabelle ohne Dauer-Spalten, `_LATENZ_LABELS` wandert nach `cost_tracker`. **Die grep-Acceptance-Zahlen
von Plan 03 (`by_context_tag == 2`, `fcd-ktag >= 11`, `def test_ >= 7`) stimmen danach nicht mehr und muessen
mit dem Rework neu gezaehlt werden — nicht der Executor soll sie „passend machen".**

### PUNKT 5 — Ersatz-Kriterium (mit Leerlauf-Schutz)
```bash
awk '/MESSGERAETE-1: Live-KI je Frage-Sorte/{f=1;next} /\/MESSGERAETE-1/{f=0} f' \
  static/admin_dashboard.css | grep -E '#[0-9A-Fa-f]{3,8}|rgba?\(|var\(--' | wc -l   # == 0
```
Plus **Existenz-Anker** gegen den Leerlauf-Ausfall (Punkt-31-Logik im Kleinen — ein leerer awk-Ausschnitt
saehe sonst gruen aus): dieselbe Extraktion `| grep -c "fcd-ktag-num"` == 1.

### PUNKT 6
Plan-03-Schnipsel: **null** Farben, null `style=`, null Tokens. `.fcd-table` existiert
(`admin_dashboard.css:186-225`), die neuen Klassen erben vollstaendig; die ganze Datei ist token-frei —
„gar kein Token statt falsches Token" ist hier die sauberste Wahl. Die drei Bestandsverstoesse sind exakt
wie behauptet (`_tab_ausgaben.html:90`, `:101`, `:109`), `grep -c "00D4AA" == 2` stimmt.
**Der Waechter kann den Anker-Fehler strukturell nie sehen** (er sieht nur das Keyword), und die
Plausibilitaets-Probe in Plan 04 Task 3 (c) wuerde ihn **nicht** fangen — die Coaching-Zahl waere um
Lock-/Profilbau-Millisekunden zu hoch, aber nicht „offensichtlich unsinnig". **Der Hoist ist die einzige
echte Verteidigung.**

### Staerken
D-07-Entzerrung **doppelt vernagelt** (statisch + an echten Daten), inkl. der Einsicht, dass
`COUNT(latency_ms)=COUNT(*)` auf Gesamt-Ebene *gar nicht eintreten darf* · Waechter folgt Punkt 31
vollstaendig · Retry-Semantik sauber (erfolgreicher Versuch, Reset pro Attempt) · D-09-Antwort inhaltlich
richtig · **kein Timing-Framework** (Punkt 27) — zwei `monotonic()`-Zeilen statt Client-Wrapper.

Zur Faktengrundlage: „praktisch jede Zeilennummer, die ich nachgeschlagen habe, stimmt" — alle 7 Cost-Hook-Paare,
beide Cache-Tag-Bloecke, `_ewb_ttft_history` exakt 3, `EWB_SONNET_FALLBACK_TTFT_MS` exakt 1, Schild-Schreiber-Liste
deckungsgleich mit dem realen Caller-Grep ueber 15 Produktiv-Dateien.

### Bedenken
- **H-1** Coaching-Anker (Befund 1) — ohne Hoist liefert die zweitteuerste Sorte **ab Tag 1 eine luegende Zahl**.
- **H-2** Plan-04-Truths verlangen fuer **alle** Live-Sorten `COUNT(latency_ms)=COUNT(*)` — strukturell
  unerfuellbar fuer die dormanten. Sonst endet die Abnahme im „fertig ohne Beleg"-Graubereich, den die
  Phase gerade abschaffen soll.
- **M-1** Plan-03-Rework + Single-List + AST-Sync-Test; Acceptance-Zahlen neu ableiten.
- **M-2** `qa_classifier` als Rollback-Zwilling — mitmessen + Sweep auf `qa_pipeline.py` (empfohlen,
  Minuten-Aufwand, `MINDEST_LIVE_PFADE = 8`) **oder** namentlich in RESTLUECKE 3.
- **M-3** Anker muss bei `classify_phase`/`infer_customer_state` **in** den `try:`-Block.
- **N-1** Plan 01 Verification Punkt 2 („keinen `_migrate`-Bezug") widerspricht dem vorgeschriebenen
  Docstring, der `_migrate()` mehrfach nennt → praezisieren auf „kein Aufruf/Import".
- **N-2** Einrueckung des `_latency_ms`-Snippets vor dem `break` ist ambivalent (im `with` vs. danach) →
  eindeutige Vorgabe spart eine Executor-Entscheidung.
- **N-3** Exit-Code-Falle systematisch geprueft: **kein** Plan enthaelt eine nackte `grep -c X == 0`-Acceptance.
  Nachgezaehlte Fix-Zahlen stimmen am heutigen Code.

### Risiko-Gesamturteil: **MITTEL**
Nichts gefaehrdet Daten oder Live-Betrieb (nullable Spalte, Migration-vor-Code, Cost-Hook raist nie,
zwei `monotonic()`-Reads). MITTEL statt NIEDRIG aus **einem** Grund: H-1 wuerde das Kernprodukt der Phase —
eine Zahl, der man trauen kann — fuer die zweitgroesste Sorte von Anfang an beschaedigen, **und zwar so,
dass keiner der eingebauten Waechter es je melden wuerde.** Mit Hoist, Truth-Korrekturen und Plan-03-Rework
ist die Phase execute-reif.

---

## Consensus Summary

### Uebereinstimmende Staerken (beide Reviewer)
- **„Erst ROT, dann GRUEN"** — verhindert blinde Tests, die nichts pruefen.
- **AST-abgeleitete Pfadliste statt Handliste** (D-05) — beide nennen es ausdruecklich als die
  strukturelle Staerke der Phase.
- **Saubere `latency_ms`/`ttft_ms`-Trennung** (D-03) — konzeptionell richtig und fuer Streaming noetig.
- **Migration-vor-Code + Deploy-Umsicht** — Fable nennt die Einsicht, dass ein „neuer Code, alte DB"-Fenster
  **jede** Kostenzeile still verloren haette, „den wichtigsten Einzelsatz der Phase".
- **Sichtbar getroffene D-09-Pruning-Entscheidung** statt stillem Weiterlaufen.

### Uebereinstimmende Bedenken (Prioritaet)
1. **BLOCKER — Coaching-Anker misst `_build_coaching_prompt` + `_session_state_lock` mit.**
   Beide unabhaengig bestaetigt. Beide betonen: **kein Waechter dieser Phase kann das je sehen.**
   Fix: Hoist als **expliziter Plan-Schritt** (Plan 02 Task 1) — das aktuelle Muster
   „`messages.create(...unveraendert...)`" ist fuer Pfad 7 genau falsch.
2. **BLOCKER — Dormanz-Ehrlichkeit.** `live_haiku` (rollback-only) und `pip_autovar` (0 Produktiv-Aufrufer)
   koennen `COUNT(latency_ms) = COUNT(*)` strukturell nie erfuellen. Truths auf „im Anruf vorgekommene
   Sorten" umformulieren, Dormanz-Vermerk in die SUMMARY. Der Einbau selbst bleibt richtig.
3. **MITTEL — Plan-03-Rework** (Zwei-Tabellen-Entscheidung). Beide fordern **eine** Liste als Wahrheitsquelle,
   von der Waechter, Query und Beschriftung leben. Acceptance-Zahlen danach **neu zaehlen**.
4. **MITTEL — CSS-Pruefregel** ist sproede; zwei kompatible Ersatzvorschlaege liegen vor.
5. **MITTEL — `qa_classifier`** bleibt als Rollback-Zwilling ungemessen und fuer den Waechter unsichtbar.

### Divergente Sichten
- **D-1 · Schweregrad von `qa_pipeline.py`:** Gemini HOCH („aktiv gerufen"), Fable MITTEL („rollback-only").
  **→ Am Code entschieden: Fable hat recht** (siehe Schiedsspruch oben). `{}` ist nicht `None`.
  Der Handlungsbedarf bleibt, die Dringlichkeit sinkt.
- **D-2 · Wo die Single-List lebt:** Gemini sagt `routes/admin_dashboard.py` (Waechter **importiert** sie),
  Fable sagt `services/cost_tracker.py` (Waechter liest sie **per AST**, kein Import).
  **→ Fables Variante ist die einzige, die mit der bestehenden Plan-01-Acceptance „0 Treffer auf
  `import services`" vertraeglich ist.** Geminis Variante wuerde diese Regel brechen.
- **D-3 · `grep -cF`:** Gemini haelt es fuer „laut ROADMAP instabil" und will es ersetzen.
  **→ Fehllesung, NICHT uebernehmen.** Die Projektlehre schreibt `-F` fuer Literale mit `[ ]` gerade vor;
  Fable hat die Exit-Code-Frage systematisch geprueft und keinen Verstoss gefunden.

### Nicht bestaetigt / entkraeftet
- „Achter **produktiver** Live-Call in `qa_pipeline.py`" (Gemini) — **entkraeftet**, rollback-only.
- „`generate_qa_response` fehlt als Messpfad" — **entkraeftet**, toter Code ohne Aufrufer,
  durch `tests/test_h1_qakill.py` als Runtime-Waechter festgenagelt.
- Exit-Code-1-Falle in den aktuellen Plaenen — **systematisch geprueft, nicht vorhanden** (Fable N-3).

---

## Was in den Replan muss (`/gsd-plan-phase 08.23.2.MESSGERAETE-1 --reviews`)

| Prio | Plan | Aenderung |
|---|---|---|
| **BLOCKER** | 02 Task 1 | `_system_coaching = _build_coaching_prompt(sid=sid)` **vor** `_t_api_start` — als **eigener Schritt**, nicht als Kommentar. „unveraendert" im Muster fuer Pfad 7 streichen. |
| **BLOCKER** | 04 | Truths auf „jede **im Anruf vorgekommene** Live-Sorte"; `pip_autovar` aus „falls ausgeloest" entfernen; SUMMARY-Pflichtsatz zur Dormanz beider Pfade. |
| HOCH | 02 Task 1 | Executor-Hinweis: Anker **in** den `try:`-Block bei `classify_phase`/`infer_customer_state` (M-3). |
| HOCH | 03 | Zwei-Tabellen-Rework; `LIVE_LLM_CONTEXT_TAGS` + `CACHE_CONTEXT_TAGS` in `services/cost_tracker.py`; `_LATENZ_LABELS` dorthin verschieben; **alle grep-Zahlen neu zaehlen**. |
| HOCH | 01 | Fuenfter Waechter-Test `test_live_tag_liste_ist_synchron` (AST-Read, **kein** Import); `def test_` == 5. |
| MITTEL | 01/02 | `qa_classifier` mitmessen + AST-Sweep auf `qa_pipeline.py` (`MINDEST_LIVE_PFADE = 8`) **oder** namentlich in RESTLUECKE 3. **Andre-Entscheidung noetig.** |
| MITTEL | 03 Task 2 | CSS-End-Marker + awk-Kriterium **plus Existenz-Anker** gegen den leeren Ausschnitt. |
| MITTEL | 01 | RESTLUECKE 3 um „neuer Live-Pfad in anderer Datei → sichtbar in Tabelle 2, aber ohne Dauer und ohne Alarm" ergaenzen; RESTLUECKE 2 um „pip_autovar-Einbau ist nur keyword-, nicht datenbelegt". |
| NIEDRIG | 01 | Verification Punkt 2 praezisieren auf „kein Aufruf/Import von `_migrate`" (N-1). |
| NIEDRIG | 02 | Einrueckung des `_latency_ms`-Snippets eindeutig vorgeben (N-2). |
| — | — | **Geminis `grep -cF`-Vorschlag NICHT umsetzen** (D-3). |
