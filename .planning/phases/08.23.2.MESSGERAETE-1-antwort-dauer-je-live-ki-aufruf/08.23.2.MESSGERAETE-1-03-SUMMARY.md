---
phase: 08.23.2.MESSGERAETE-1
plan: 03
subsystem: routes
tags: [founder-dashboard, cost-tracking, latency, aggregation, percentiles, css, dsgvo]

# Dependency graph
requires:
  - phase: 08.23.2.MESSGERAETE-1-01
    provides: "LIVE_LLM_CONTEXT_TAGS + CACHE_CONTEXT_TAGS (die EINE Liste, D-11), ORM-Spalte ttft_ms, Migration 0036 (geschrieben, nicht ausgefuehrt)"
  - phase: 08.23.2.MESSGERAETE-1-02
    provides: "Acht Live-Pfade schreiben latency_ms, zwei Streaming-Pfade zusaetzlich ttft_ms — nur an der input-Token-Buchung (D-07)"
  - phase: 04.7.2
    provides: "ausgaben_page + .fcd-table-Muster + _parse_period (Zeitraum-Wahl)"
provides:
  - "Endpunkt /admin/dashboard/ausgaben liefert zusaetzlich live_ki und uebrige_kosten"
  - "Zwei reine Hilfsfunktionen _perzentil + _aggregiere_kosten_nach_tag (ohne DB testbar)"
  - "Zwei Tabellen im Tab Ausgaben: Live-KI je Frage-Sorte (8 Spalten) + Uebrige Kosten (3 Spalten)"
  - "tests/test_messgeraete1_leser.py — neun deterministische Tests, laufen im Deploy-Tor mit"
affects: [08.23.2.MESSGERAETE-1 Plan 04 (Migration 0036 + Deploy + Abnahme an echten Daten + Sichtpruefung)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Perzentile in Python (Nearest-Rank) statt percentile_cont in SQL — kein Postgres-Dialekt-Zwang im Leser (Punkt 27)"
    - "Zwei Abfragen statt einer: Kosten ueber ALLE Zeilen, Dauer nur ueber latency_ms IS NOT NULL (D-07)"
    - "Split gegen die EINE Liste in Python statt per context_tag-Filter im SQL — ein unbekannter Tag bleibt sichtbar statt lautlos zu verschwinden"
    - "Neues CSS mit Start- UND Ende-Marker begrenzen, damit die Farb-Pruefregel exakt schneidet, plus Existenz-Anker gegen den Leerlauf-Ausfall (Punkt-31-Logik im Kleinen)"

key-files:
  created:
    - tests/test_messgeraete1_leser.py
  modified:
    - routes/admin_dashboard.py
    - templates/admin/_tab_ausgaben.html
    - static/admin_dashboard.css

key-decisions:
  - "Kein neuer Endpunkt: die zwei Auswertungen haengen an ausgaben_page und erben damit @login_required + @superadmin_required unveraendert (T-MESS-12, Anzahl Route-Dekoratoren 17 vor und nach diesem Plan)"
  - "Kein zweites Label-Dict (D-11): Query-Split UND Beschriftung kommen aus services/cost_tracker.py::LIVE_LLM_CONTEXT_TAGS; _LATENZ_LABELS existiert repoweit weiterhin nicht"
  - "Klassen-Praefix fcd-* statt n-*: die Founder-Admin-Flaeche fuehrt durchgehend fcd-* (.fcd-table, .fcd-ausgaben, .fcd-chart-card). Ein zweites Praefix auf derselben Flaeche waere genau der Wildwuchs, den D-08 verhindern soll — CONTEXT D-08 wurde dazu am 03.08. korrigiert"
  - "Die drei Bestands-Farbverstoesse in _tab_ausgaben.html werden NICHT angefasst (Reparatur-Modus, kein Beifang) — sie stehen unten als Fund"
  - "Kein pytest in diesem Plan, weder lokal (CLAUDE.md HART) noch auf Prod (das waere ungetesteter Produktiv-Code auf dem Live-Server, in falscher Reihenfolge vor Migration 0036). Der einzige verbindliche Lauf ist das Deploy-Tor in Plan 04"

patterns-established:
  - "Zaehl-Anker gegen Prosa-Falschtreffer praezisieren statt die Prosa zu kuerzen (dritter Fall dieser Phase)"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-08-04
---

# Phase 08.23.2.MESSGERAETE-1 Plan 03: Der Leser Summary

**`/admin/dashboard/ausgaben` gruppiert Kosten und Dauer jetzt nach `context_tag` und liefert das Ergebnis als zwei getrennte Listen — `live_ki` mit Buchungen/Kosten/Antworten/Ø-p50-p95-Dauer/Ø-TTFT und `uebrige_kosten` ohne Dauer-Spalten; der Tab „Ausgaben" rendert beide als `.fcd-table` ohne ein einziges `style`-Attribut, ohne Farbwert und ohne neues Token.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-04T07:14:00Z
- **Completed:** 2026-08-04T07:26:00Z
- **Tasks:** 2
- **Files modified:** 4 (1 neu, 3 geaendert) — rein additiv, `git diff --numstat d0c12c6..HEAD` zeigt **null Loeschungen** in allen vier Dateien

## Accomplishments

- **Das tote Feld hat einen Leser.** `latency_ms` liegt seit Phase 08.13 im Schema und wurde von niemandem gelesen — genau der Grund, warum monatelang niemand bemerkt hat, dass nichts drinsteht. Ab jetzt haengt eine Ansicht daran.
- **Zwei Tabellen statt einer.** In Prod existieren 33 verschiedene `context_tag`-Werte; nur zehn sind Live-KI. `stt` (die mengenmaessig groesste Kostenposition), `stripe_fee`, `training_*`, `precall`, `judge`, `crm`, `postcall_coach` landen in „Übrige Kosten" statt mit „—" in jeder Dauer-Spalte unter einer Live-KI-Ueberschrift zu stehen.
- **Eine API-Antwort geht genau einmal in die Statistik ein** (D-07). `buchungen` und `antworten` stehen bewusst als zwei Spalten nebeneinander und werden im Hinweistext erklaert — sie sind nicht dasselbe.
- **Die Zuordnung hat genau einen Pflegeort** (D-11). Query-Split und Beschriftung lesen dieselbe Konstante; `grep "_LATENZ_LABELS" routes/admin_dashboard.py | wc -l` → `0`.
- **Null Farbverstoesse im neuen Code** — marker-begrenzt belegt, inklusive Existenz-Anker gegen den Leerlauf-Ausfall.

## Task Commits

1. **Task 1: Aggregation nach `context_tag` im Ausgaben-Endpunkt (inkl. Perzentilen, D-07-Filter)** — `bee2850` (feat)
2. **Task 2: ZWEI Tabellen im Tab „Ausgaben" — ohne eine einzige hardcoded Farbe** — `43f3b7c` (feat)

## Files Created/Modified

- `routes/admin_dashboard.py` (+120/-0) — Modul-Import der EINEN Liste, `_OHNE_HERKUNFT`, `_perzentil`, `_aggregiere_kosten_nach_tag` (alle drei unter `COST_DATA_COMPLETE_SINCE`), das Abfrage-Paar plus zwei `jsonify`-Keys in `ausgaben_page`. Keine neue Route.
- `templates/admin/_tab_ausgaben.html` (+51/-0) — zwei Ueberschriften mit Hinweistext, zwei `.fcd-table`-Tabellen, zwei Render-Bloecke in `load()` plus der `ms()`-Formatierer. Eingefuegt direkt nach der EÜR-Kategorien-Tabelle, also ganz oben im Tab.
- `static/admin_dashboard.css` (+17/-0) — `.fcd-ktag-num` / `.fcd-ktag-head` / `.fcd-ktag-hint`, ausschliesslich Ausrichtung, Ziffern-Laufweite, Schriftgroesse und Abstand. Zwischen Start- und Ende-Marker.
- `tests/test_messgeraete1_leser.py` (neu, 116 Zeilen) — die neun im Plan vorgeschriebenen Tests, keiner gestrichen.

## Statische Abnahme (verbatim gemessen, kein `pytest` — siehe Begruendung unten)

| Kriterium | Soll | Ist |
|---|---|---|
| `ast.parse` beider Python-Dateien | `SYNTAX OK` | `SYNTAX OK` |
| `grep -cF "from services.cost_tracker import LIVE_LLM_CONTEXT_TAGS, CACHE_CONTEXT_TAGS"` | 1 | 1 |
| `grep -cF "def _perzentil("` / `"def _aggregiere_kosten_nach_tag("` | 1 / 1 | 1 / 1 |
| `grep -cF "live_ki, uebrige_kosten = _aggregiere_kosten_nach_tag(ktag_summen, ktag_latenzen)"` | 1 | 1 |
| `grep -cF "'live_ki': live_ki,"` / `"'uebrige_kosten': uebrige_kosten,"` | 1 / 1 | 1 / 1 |
| `grep -cF "ApiCostLog.latency_ms.isnot(None)"` (D-07-Filter) | 1 | 1 |
| `grep -cF "'antworten': len(lat)"` | 1 | 1 |
| `grep "_LATENZ_LABELS" \| wc -l` (D-11, kein zweiter Pflegeort) | 0 | 0 |
| `grep -cF "if schluessel in LIVE_LLM_CONTEXT_TAGS:"` | 1 | 1 |
| `grep "ApiCostLog.context_tag.in_\|context_tag ==" \| wc -l` (Punkt 12) | 0 | 0 |
| `grep -cF "Kein user_id, kein org_id, kein session_id"` (DSGVO im Code) | 1 | 1 |
| `grep -c "@admin_dashboard_bp.route"` **vor und nach** diesem Plan | 17 / 17 | 17 / 17 |
| `grep -E "percentile_cont *\(\|\.percentile_cont" \| wc -l` (kein Dialekt-Zwang) | 0 | 0 |
| `grep -c "def test_" tests/test_messgeraete1_leser.py` | 9 | 9 |
| `grep -E "@pytest.mark.(live\|perf)" \| wc -l` | 0 | 0 |
| `grep -cF 'id="fcd-ktag-body"'` / `'id="fcd-uebrige-body"'` | 1 / 1 | 1 / 1 |
| `grep -cF "(d.live_ki||[])"` / `"(d.uebrige_kosten||[])"` | 1 / 1 | 1 / 1 |
| `grep -n "ktag\|uebrige" … \| grep "style=" \| wc -l` (Inline-Styles) | 0 | 0 |
| `grep -n "ktag\|uebrige" … \| grep -E "#[0-9A-Fa-f]{3,8}\|rgba?\(" \| wc -l` | 0 | 0 |
| **CSS marker-begrenzt**, `awk … \| grep -E '#…\|rgba?(\|var(--' \| wc -l` | 0 | 0 |
| **Existenz-Anker** derselben Extraktion `\| grep -c "fcd-ktag-num"` | 1 | 1 |
| `grep -cF "ENDE MESSGERAETE-1-BLOCK" static/admin_dashboard.css` | 1 | 1 |
| `git diff static/nerve.css \| wc -l` (Datei unberuehrt) | 0 | 0 |
| `git diff static/admin_dashboard.css \| grep "^+.*--[a-z-]*:" \| wc -l` (kein Token) | 0 | 0 |
| `grep -c "00D4AA" templates/admin/_tab_ausgaben.html` (Bestand unberuehrt) | 2 | 2 |
| `grep -cF ".fcd-ktag-num" static/admin_dashboard.css` | 1 | 1 |
| `grep -c "fcd-ktag" templates/admin/_tab_ausgaben.html` | >= 13 | 15 |

Die marker-begrenzte CSS-Extraktion wurde **echt gefahren**, nicht nur gelesen — genau das war der
Anlass der Plan-Korrektur `803c41d` (der alte Ende-Marker `/MESSGERAETE-1` kam als Substring im
eigenen Kommentartext vor, das `awk`-Fenster brach nach zwei Zeilen ab, die Farbpruefung lief
falsch-gruen und der Existenz-Anker rot). Mit `ENDE MESSGERAETE-1-BLOCK` schneidet sie korrekt:
Farbtreffer `0` **und** Existenz-Anker `1` — beide Zahlen zusammen beweisen erst, dass sie
ueberhaupt etwas gelesen hat.

## Warum in diesem Plan kein Testlauf stattfand

Der Plan schreibt es ausdruecklich vor, und beide Alternativen sind versperrt:

- **Lokal** verbietet es CLAUDE.md (HART, keine Ausnahmen). Die Testdatei importiert
  `routes.admin_dashboard` und damit Flask-Blueprint, Decorators und Models — ein echter
  App-Import, kein „nur ein Unit-Test".
- **Auf Prod** waere ein Zwischenlauf schlimmer: er muesste `routes/admin_dashboard.py` — eine
  Produktiv-Quelldatei — ungetestet auf den Live-Server legen. Prod hat kein `.git`
  (tar-deployed), es gibt keinen Rueckhol-Befehl, und `inspect.sh git-stand` kann die Abweichung
  mangels `.git` nicht erkennen. Ein `deploy.sh production` an dieser Stelle wuerde Plan 03
  ausrollen, **bevor** Migration `0036` drauf ist — genau die Reihenfolge, die Plan 04 als
  zwingend festlegt.

→ Der einzige verbindliche Lauf ist das **Deploy-Tor in Plan 04**
(`/opt/nerve/venv/bin/pytest tests/ -m "not live and not perf"` serverseitig vor dem Neustart).
`tests/test_messgeraete1_leser.py` traegt bewusst keinen `live`/`perf`-Marker und ist damit
abgedeckt. Damit bleibt Plan 01 Task 2 der **einzige** `scp`-auf-Prod-Vorgang der ganzen Phase.

## Fund: drei Bestands-Farbverstoesse in `_tab_ausgaben.html` — bewusst NICHT angefasst

Reparatur-Modus, kein Beifang. Alle drei sind aelter als diese Phase und liegen ausserhalb des
Aufgaben-Pfads; sie stehen hier als Kandidat fuer eine spaetere Aufraeum-Mini-Phase.

| # | Stelle (Stand nach diesem Plan) | Verstoss |
|---|---|---|
| 1 | `templates/admin/_tab_ausgaben.html:118` | `style="…background:#00D4AA;…"` in der Anbieter-Balken-Zeile — Hex-Literal **und** Inline-Style. Teal ist die gueltige Markenfarbe, gehoert aber ueber `var(--btn-primary-bg-from)` statt als Literal. |
| 2 | `templates/admin/_tab_ausgaben.html:152` | `backgroundColor:'#00D4AA'` in der Chart.js-Konfiguration — Hex-Literal in JS. |
| 3 | `templates/admin/_tab_ausgaben.html:160` | `style="background:rgba(239,68,68,.1);"` bei veralteten Raten — Inline-Style mit Rot-Wert, das Rot existiert nirgends als Token. |

Zusaetzlich, derselben Klasse, aber nur beobachtet: die fuenf bestehenden `<h4 style="margin-top:24px;">`
in derselben Datei. Die **neuen** Ueberschriften kopieren das bewusst nicht — dafuer gibt es
`.fcd-ktag-head`.

Keine dieser Stellen ist im Diff dieses Plans. `grep -c "00D4AA"` liefert unveraendert `2`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `percentile_cont`-Anker auf die Aufruf-Form praezisiert statt den Plan-Kommentar zu kuerzen**
- **Found during:** Task 1 (Acceptance-Lauf)
- **Issue:** Das Kriterium lautet `grep "percentile_cont" routes/admin_dashboard.py | wc -l == 0`. Nach Task 1 stand dort `2` — beide Treffer stammen aus dem **woertlich vom Plan vorgegebenen** Begruendungs-Kommentar („Perzentile werden in PYTHON gerechnet, nicht per `percentile_cont` in SQL … `percentile_cont` ist Postgres-Dialekt"). Der Plan erzeugt seinen eigenen Falschtreffer; der billige Weg zu Gruen waere gewesen, die Begruendung zu loeschen.
- **Fix:** Der Kommentar bleibt **unveraendert**. Die Gegenprobe prueft stattdessen, was das Kriterium meint — kein Dialekt-Zwang im ausfuehrbaren Code: `grep -E "percentile_cont *\(|\.percentile_cont" routes/admin_dashboard.py | wc -l` → **0**. Zur Absicherung gegengeprueft, dass der Ausgangsstand (`git show HEAD:routes/admin_dashboard.py`) ebenfalls `0` Treffer auf `percentile_cont` hatte, die zwei also nachweislich aus dem neuen Kommentar stammen.
- **Files modified:** keine (reine Pruef-Methodik)
- **Commit:** `bee2850`

**2. [Rule 3 - Blocking] Marker-Erwaehnung im Test-Docstring ohne Dekorator-Schreibweise formuliert**
- **Found during:** Task 1 (Acceptance-Lauf)
- **Issue:** Das Kriterium `grep -E "@pytest.mark.(live|perf)" tests/test_messgeraete1_leser.py | wc -l == 0` lieferte `1`. Ursache war **kein** Marker, sondern der Satz im Modul-Docstring, der erklaert, **warum** diese Datei bewusst keinen traegt — er nannte die Marker in Dekorator-Schreibweise.
- **Fix:** Dieselbe Aussage ohne die Dekorator-Schreibweise: „Bewusst OHNE die Marker \"live\" und \"perf\": das Deploy-Tor faehrt mit `-m \"not live and not perf\"` und waere fuer diese Datei sonst blind." Die Erklaerung bleibt vollstaendig erhalten; der Anker ist eindeutig.
- **Files modified:** `tests/test_messgeraete1_leser.py`
- **Verification:** Anker → `0`
- **Commit:** `bee2850`

Beide Faelle sind dieselbe Klasse wie in Plan 01 (`UPDATE`) und Plan 02 (`latency_c`, Streaming-Semantik-Satz) — **dritter und vierter Fall dieser Phase**: eine greppbare Abnahme-Regel kollidiert mit der Prosa, die sie schuetzen soll. Der Reflex „Text an den Anker anpassen" wurde in keinem der Faelle bedient; entweder wurde der Anker praezisiert oder die Formulierung geaendert, ohne eine Zusicherung zu verlieren.

**Total deviations:** 2 auto-fixed (beide Rule 3, beide betrafen Zaehl-Anker, keine Substanz). Kein Scope-Creep, keine Datei ausserhalb der vier geplanten angefasst, keine Zeile geloescht.

## Issues Encountered

- Beim ersten Acceptance-Lauf meldeten die Anker `id="fcd-ktag-body"` und `id="fcd-uebrige-body"` faelschlich `0`. Ursache war **keine** fehlende Zeile, sondern verschachteltes Quoting im Pruef-Skript (`grep -cF '…\"…\"…'` innerhalb einer `echo "…$(…)"`-Huelle). Direkt gefahren liefern beide `1`. Notiert, weil derselbe Huellen-Fehler in einer Abnahme leicht als „fehlt" fehlgedeutet wird — Anker mit eingebetteten Anfuehrungszeichen besser ohne `echo`-Huelle fahren.

## Known Stubs

Keine. Beide Tabellen sind an echte Daten verdrahtet; es gibt keinen hartkodierten Platzhalter,
keine leere Liste als Datenquelle und keinen „kommt spaeter"-Text. Zwei beabsichtigte, in der
Plan-Reihenfolge begruendete Bedingungen bleiben bis Plan 04 offen:

- **Migration `0036` ist auf Prod nicht ausgefuehrt** (Prod steht auf `0035`). Bis dahin wuerde die
  Abfrage auf `ttft_ms` dort ins Leere greifen — deshalb ist die Reihenfolge in Plan 04 bindend:
  erst Migration, dann Code. Diesen Plan auszurollen ist ausdruecklich **nicht** Teil von Plan 03.
- **Die TTFT-Spalte bleibt bei allen Sorten ausser `pip_autovar`/`pip_variante` dauerhaft leer**
  (nur Streaming-Pfade messen sie). Das ist kein Defekt, sondern D-03 — der Hinweistext ueber der
  Tabelle sagt es woertlich („„bis 1. Wort" gibt es nur bei den beiden Stream-Pfaden").

## Pruning-Notiz (Bau-Regel 7)

- **Nichts wurde durch diesen Plan obsolet.** Der Diff ist rein additiv (`git diff --numstat` ueber
  beide Commits: `120/0`, `51/0`, `17/0`, `116/0` — null Loeschungen).
- **`_LATENZ_LABELS` gibt es weiterhin nicht** und wurde auch nicht angelegt (CONTEXT D-11,
  korrigiert am 03.08.: es „wandert" nichts, die Liste wurde in Plan 01 neu angelegt).
- Die drei Bestands-Farbverstoesse oben sind **kein** Pruning-Kandidat dieses Plans, sondern ein
  benannter Fund fuer eine eigene Mini-Phase — bewusst stehengelassen, nicht stilles Legacy.

## Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.

Dieser Plan liest ausschliesslich; er schreibt keine einzige Tabelle.

## Threat Flags

Keine. Die angefasste Flaeche entspricht dem `<threat_model>` des Plans:

- **T-MESS-12 (Elevation of Privilege):** mitigiert. Kein neuer Endpunkt; die Anzahl der
  `@admin_dashboard_bp.route`-Dekoratoren ist **17 vor und 17 nach** diesem Plan. Der Zugriffsschutz
  (`@login_required` + `@superadmin_required` → `abort(403)` ohne `is_superadmin`) ist unveraendert
  geerbt.
- **T-MESS-13 (Information Disclosure, DSGVO):** mitigiert. Die Rueckgabe enthaelt ausschliesslich
  `context_tag`, Label, Anzahlen, Kosten und Millisekunden — kein `user_id`, kein `org_id`, kein
  `session_id`, kein Transkript. Die Begruendung steht als Kommentar im Code und ist greppbar
  (`grep -cF "Kein user_id, kein org_id, kein session_id"` → `1`). Die Zeilen-Ansicht, die
  Personenbezug haette, wurde bewusst nicht gebaut.
- **T-MESS-14:** unveraendert `accept` — das Founder-Cost-Dashboard ist per Zweck
  organisationsuebergreifend und nur fuer den Founder erreichbar.
- **T-MESS-15 (XSS ueber `innerHTML`):** mitigiert. Alle Textwerte laufen durch den vorhandenen
  `esc()`-Helfer, alle Zahlen durch `fmt()`/`ms()` (beide `Number(...)`). `context_tag` ist
  serverseitig auf `String(32)` begrenzt und stammt aus Code-Literalen, nicht aus Nutzereingaben.
- **T-MESS-16 (DoS):** mitigiert. Die Latenz-Abfrage laedt **zwei Integer-Spalten**, begrenzt auf
  einen Monat (`_parse_period`, `abort(400)` bei Formatfehler), Index auf `created_at` existiert.
  Kein `SELECT *`, keine Text-Spalte.
- **T-MESS-17 (Repudiation):** mitigiert. Der Hinweistext unter beiden Ueberschriften erklaert
  „Antworten" vs. „Buchungen", dass „bis 1. Wort" nur fuer die zwei Stream-Pfade existiert und
  dass die Zwischenspeicher-Zeilen absichtlich Striche tragen.

## User Setup Required

Keine.

## Next Phase Readiness

**Bereit fuer Plan 04 (Migration + Deploy + Abnahme an echten Daten).** Was Plan 04 mitnehmen muss:

- **Bindende Reihenfolge:** `alembic current` messen → Migration `0036` ausfuehren (als DB-User
  `postgres`, peer-auth) → Gegenprobe, dass `ttft_ms` da ist → `deploy.sh production` → Test-Anruf →
  D-06-Abfrage. Dieser Plan ist **nicht** ausgerollt.
- **Der Testlauf dieser Datei gehoert dem Deploy-Tor.** `tests/test_messgeraete1_leser.py` ist noch
  nie gefahren worden — weder lokal noch auf Prod. Sollte das Tor rot werden, liegt der Fehler
  hier und nicht am Bestand.
- **Sichtpruefung ist Teil des Test-Anrufs:** Tab „Ausgaben" oeffnen, oben stehen zwei Tabellen.
  Erwartung nach einem Test-Anruf: die im Anruf vorgekommenen Live-Sorten haben `antworten > 0` und
  Zahlen in Ø/p50/p95; `pip_variante` zusaetzlich eine Zahl unter „Ø bis 1. Wort".
- **Dormanz-Ehrlichkeit (CONTEXT Punkt 13):** `live_haiku`, `pip_autovar` und `qa_classifier`
  erscheinen in Tabelle 1 mit `antworten == 0`. Das ist **kein** Defekt des Lesers — sie sind
  dormant bzw. rollback-only und koennen an echten Daten strukturell nicht belegt werden.
- **Restluecke, die sichtbar bleibt:** ein kuenftiger Live-LLM-Pfad in einer anderen Datei als
  `claude_service.py`/`qa_pipeline.py` erscheint in Tabelle 2 „Übrige Kosten" — sichtbar, aber ohne
  Dauer und ohne Alarm. Gehoert namentlich in RESTLUECKE 3 der Phasen-SUMMARY.

## Self-Check: PASSED

- Alle behaupteten Dateien existieren: `tests/test_messgeraete1_leser.py` (neu),
  `routes/admin_dashboard.py`, `templates/admin/_tab_ausgaben.html`,
  `static/admin_dashboard.css` — jeweils `FOUND`.
- Beide Task-Commits in `git log` auffindbar: `bee2850`, `43f3b7c` — jeweils `FOUND`.
- Kein Commit dieses Plans enthaelt Datei-Loeschungen
  (`git diff --diff-filter=D --name-only` ueber beide Commits: leer).
- `.planning/STATE.md` und `.planning/ROADMAP.md` wurden **nicht** veraendert — die schreibt der
  Orchestrator am Phasen-Ende.
- Die zwei phasenfremden Arbeitsbaum-Eintraege (`.claude/settings.local.json`, `_design_export/`)
  sind unberuehrt und ungestaged geblieben.

---
*Phase: 08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf*
*Completed: 2026-08-04*
