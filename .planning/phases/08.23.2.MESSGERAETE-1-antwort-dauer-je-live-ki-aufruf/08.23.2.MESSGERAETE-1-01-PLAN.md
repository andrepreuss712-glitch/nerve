---
phase: 08.23.2.MESSGERAETE-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - alembic/versions/0036_messgeraete1_ttft_ms.py
  - tests/test_live_latency_coverage.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Die Tabelle api_cost_log hat eine Spalte ttft_ms, die getrennt von latency_ms existiert"
    - "Das Punkt-23-Schild von api_cost_log nennt die neue Spalte, alle Schreiber und den neuen Leser"
    - "Ein Waechter leitet die produktiven Live-LLM-Pfade per AST aus services/claude_service.py ab und ist NACHWEISLICH ROT, solange die Messung fehlt"
    - "Der Waechter faellt ROT statt blind, wenn seine Ableitung ausfaellt (Mindest-Soll 7 Pfade)"
  artifacts:
    - path: "alembic/versions/0036_messgeraete1_ttft_ms.py"
      provides: "Spalte ttft_ms + COMMENT-Nachzug auf Head 0035"
      contains: "down_revision = '0035'"
    - path: "database/models.py"
      provides: "ORM-Spalte ttft_ms + aktualisiertes __table_args__-Schild"
      contains: "ttft_ms"
    - path: "tests/test_live_latency_coverage.py"
      provides: "AST-abgeleiteter Waechter mit RESTLUECKEN-Absatz"
      min_lines: 120
  key_links:
    - from: "tests/test_live_latency_coverage.py"
      to: "services/claude_service.py"
      via: "ast.parse ueber die Datei, Suche nach messages.create/messages.stream"
      pattern: "ast\\.parse"
    - from: "alembic/versions/0036_messgeraete1_ttft_ms.py"
      to: "database/models.py"
      via: "wortgleicher Schild-Text in Migration und __table_args__"
      pattern: "COMMENT ON TABLE api_cost_log"
---

<objective>
Das Fundament der Phase: die zweite Zahl bekommt ihre eigene Spalte, das Tabellen-Schild waechst mit,
und der Waechter, der die Messung dauerhaft festnagelt, wird gebaut **und nachweislich ROT** gefahren —
bevor irgendeine Messung eingebaut wird.

Purpose: `ttft_ms` (Zeit bis zum ersten Token) darf niemals in `latency_ms` (Dauer bis zum letzten Token)
gekippt werden — das ist der „Name luegt"-Fehler aus D-02/D-03. Und ein Waechter, der nie rot war,
beweist nichts (CLAUDE.md Punkt 31 + Bau-Regel „Regressions-Test bei jedem Bugfix").

Output: Alembic-Migration `0036`, ORM-Spalte + Schild-Nachzug, Waechter-Datei mit ROT-Beleg.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-CONTEXT.md
@CLAUDE.md
@database/models.py
@alembic/versions/0034_kosten1_apirate_schilder.py
@alembic/versions/0035_counterpart_event_rename.py
@tests/test_cost_hook_coverage.py
@services/claude_service.py
</context>

<interfaces>
<!-- Vertraege, die der Executor braucht. Aus dem Code extrahiert — KEIN Erkunden noetig. -->

Aus `services/cost_tracker.py:162-174` (Signatur steht bereits, wird NICHT geaendert ausser um ttft_ms):
```python
def log_api_cost(
    provider: str, model: str, user_id: int | None, units: float, unit_type: str, *,
    org_id: int | None = None, session_id: str | None = None, context_tag: str | None = None,
    latency_ms: int | None = None, call_site: str | None = None,
) -> None:
```

Aus `database/models.py:515-536` (Ist-Stand):
```python
class ApiCostLog(Base):
    __tablename__ = 'api_cost_log'
    __table_args__ = ({'comment': 'Jeder API-Call mit eingefrorenem Wechselkurs und Rate (Founder Cost Dashboard, steuerlich korrekt). Status: lebt. Schreibt API-Call-Wrapper in services/; liest Founder-Cost-Dashboard.'},)
    ...
    context_tag = Column(String(32), nullable=True, comment='Kontext-Tag des Calls')
    latency_ms  = Column(Integer, nullable=True, comment='API-Latenz in ms')
    call_site   = Column(String(50), nullable=True, comment='Code-Aufrufstelle')
```

Die SIEBEN produktiven Live-Pfade in `services/claude_service.py` (CONTEXT Punkt 7, am Code
gegengeprueft 2026-08-03 — Zeilennummern koennen sich verschieben, die Funktionsnamen nicht):

| # | Funktion | Aufruf | context_tag | Art |
|---|---|---|---|---|
| 1 | `klassifiziere_phase` (`:423`) | `messages.create` | `phase_classify` | blockierend |
| 2 | `infer_customer_state` (`:500`) | `messages.create` | `coldcall_infer` | blockierend |
| 3 | `analysiere_mit_claude` (`:572`) | `messages.create` | `live_haiku` | blockierend |
| 4 | `analysiere_und_klassifiziere` (`:750`) | `messages.create` | `live_haiku_merged` | blockierend |
| 5 | `streame_auto_variante` (`:854`) | `messages.stream` | `pip_autovar` | **streaming** |
| 6 | `streame_manual_ewb_variante` (`:1031`) | `messages.stream` | `pip_variante` | **streaming** |
| 7 | `analysiere_coaching` (`:1120`) | `messages.create` | `coaching_haiku` | blockierend |

⚠ Der Funktionsname von #1 ist per `grep -n "def .*phase" services/claude_service.py` zu bestaetigen,
bevor er in eine Fehlermeldung geschrieben wird — der Waechter leitet ihn ohnehin selbst ab.
</interfaces>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
- `public.api_cost_log` (Schema-Aenderung: neue Spalte `ttft_ms`; COMMENT-Nachzug)

### inspect.sh-Beleg (gezogen 2026-08-03, Production, read-only)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema api_cost_log'
                                           Table "public.api_cost_log"
     Column      |            Type             | Collation | Nullable |                 Default
-----------------+-----------------------------+-----------+----------+------------------------------------------
 id              | integer                     |           | not null | nextval('api_cost_log_id_seq'::regclass)
 created_at      | timestamp without time zone |           | not null |
 provider        | character varying(32)       |           | not null |
 model           | character varying(64)       |           | not null |
 user_id         | integer                     |           |          |
 org_id          | integer                     |           |          |
 units           | numeric(14,4)               |           | not null |
 unit_type       | character varying(32)       |           | not null |
 rate_applied    | numeric(12,8)               |           | not null |
 rate_currency   | character varying(3)        |           | not null |
 fx_rate_applied | numeric(10,6)               |           | not null |
 cost_eur        | numeric(12,6)               |           | not null |
 session_id      | character varying(64)       |           |          |
 context_tag     | character varying(32)       |           |          |
 latency_ms      | integer                     |           |          |
 call_site       | character varying(50)       |           |          |
Indexes:
    "api_cost_log_pkey" PRIMARY KEY, btree (id)
    "ix_api_cost_log_created_at" btree (created_at)
    "ix_api_cost_log_org_id" btree (org_id)
    "ix_api_cost_log_provider" btree (provider)
    "ix_api_cost_log_session_id" btree (session_id)
    "ix_api_cost_log_user_id" btree (user_id)
Foreign-key constraints:
    "api_cost_log_org_id_fkey" FOREIGN KEY (org_id) REFERENCES organisations(id)
    "api_cost_log_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id)
```

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
 version_num
-------------
 0035
(1 row)
```

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schilder api_cost_log'
── Schild: api_cost_log (Tabelle) ─────────────────────────────────
 public | Jeder API-Call mit eingefrorenem Wechselkurs und Rate (Founder Cost Dashboard, steuerlich
          korrekt). Status: lebt. Schreibt API-Call-Wrapper in services/; liest Founder-Cost-Dashboard.
── Spalten-Schilder ───────────────────────────────────────
 ... context_tag | Kontext-Tag des Calls
     latency_ms  | API-Latenz in ms
     call_site   | Code-Aufrufstelle
```

### ⚠ KORREKTUR zu CONTEXT-Punkt 9 (belegt, hat Vorrang)
CONTEXT Punkt 9 sagt: *„Migration 0035 (COUNTERPART) ist auf Prod noch NICHT ausgefuehrt."*
**Das stimmt nicht mehr.** `inspect.sh migrations` liefert `version_num = 0035` (gemessen 2026-08-03).
Folge: `alembic upgrade head` zieht **nur 0036** hoch. Plan 04 prueft `alembic current` trotzdem
**vor** dem Upgrade — falls Prod wider Erwarten auf 0034 steht, laufen 0035 **und** 0036, und die
COUNTERPART-Deploy-Reihenfolge (Migration → Code) gilt dann zusaetzlich.

### Cross-Layer-Konsistenz-Tabelle

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `latency_ms` | wird ab Plan 02 geschrieben, ab Plan 03 gelesen | DB-Spalte `api_cost_log.latency_ms` (integer, nullable) | ✓ inspect.sh oben |
| `ttft_ms` | wird ab Plan 02 geschrieben, ab Plan 03 gelesen | **existiert noch NICHT** → diese Migration legt sie an | ✓ inspect.sh: fehlt |
| `context_tag` | wird bereits geschrieben, ab Plan 03 gelesen | DB-Spalte `api_cost_log.context_tag` (varchar 32, nullable) | ✓ inspect.sh oben |
| `latency_e` / `latency_c` | RAM + `logs/`-Textdatei | **KEINE DB-Spalte** (`live_session.py`-Puffer → `app_routes.py:385` Datei-Write) | ✓ D-02, wird NICHT angefasst |

### Schreib-Zeitpunkt / kann der Leser vorauslaufen? (Punkt 26)
`log_api_cost` schreibt **synchron mit eigenem `db.commit()`** im selben Aufruf
(`services/cost_tracker.py:217-233`). Es gibt keinen Hintergrund-Thread und keinen Batch-Write.
Der Leser (Plan 03) ist eine Admin-Seite, die Zeitraeume der Vergangenheit liest — er kann dem
Schreiber strukturell nicht vorauslaufen. **Keine Fan-In-Naht noetig.**

## Vier Saeulen (Bau-Regel D)
- **Automatisieren:** die Spalte + der Waechter sorgen dafuer, dass die Messung nicht wieder still verschwindet.
- **Nachvollziehen:** `api_cost_log`, nicht Bildschirm/Textdatei.
- **Eingreifen:** die Ansicht aus Plan 03 macht sichtbar, welche Frage-Sorte teuer/langsam ist.
- **Marge:** ohne diese Zahlen ist die Preis-Untergrenze nicht berechenbar.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

<tasks>

<task type="auto">
  <name>Task 1: Spalte ttft_ms + Punkt-23-Schild-Nachzug (Alembic 0036, NICHT _migrate)</name>

  <read_first>
    - `database/models.py` Z.513-537 (ApiCostLog inkl. `__table_args__`-Schild — die zu aendernde Stelle)
    - `alembic/versions/0034_kosten1_apirate_schilder.py` (VOLLSTAENDIG — das hauseigene Muster fuer
      COMMENT-Migrationen: `op.execute("COMMENT ON ... IS '...'")` + `_comment`-Helfer mit
      `replace("'", "''")`-Escaping; KEIN autogenerate-Round-Trip)
    - `alembic/versions/0035_counterpart_event_rename.py` Z.26-31 (aktueller Head: `revision = '0035'`)
    - CLAUDE.md → Punkt 23 (Tabellen-Dokumentations-Pflicht + Schild-AKTUALITAETS-Pflicht)
    - CONTEXT Punkt 8 + 10 (`_migrate()` ist der falsche Weg; Schild muss mitwachsen)
    - Abschnitt „5. Persistenz-Schicht-Verifikation" oben in DIESEM Plan (inspect.sh-Beleg)
  </read_first>

  <files>database/models.py, alembic/versions/0036_messgeraete1_ttft_ms.py</files>

  <action>
**(a) ORM-Spalte in `database/models.py`.** Direkt UNTER `latency_ms` (Z.535), VOR `call_site`:

```python
    latency_ms  = Column(Integer, nullable=True, comment='Reine API-Dauer in ms bis zum LETZTEN Token. Nur an der input-Token-Buchung gesetzt (D-07: eine API-Antwort zaehlt genau einmal), Cache-/Output-Buchungen bleiben NULL. NICHT identisch mit latency_e/latency_c aus live_session (die enthalten Puffer-Wartezeit + QA-Dispatch).')
    ttft_ms     = Column(Integer, nullable=True, comment='Zeit bis zum ERSTEN Token in ms. Nur Streaming-Pfade (pip_autovar, pip_variante), nur an der input-Token-Buchung; bei blockierenden Aufrufen immer NULL. Getrennte Spalte, weil latency_ms bis zum letzten Token misst — beide Bedeutungen in EINE Spalte zu kippen waere der Name-luegt-Fehler (D-03).')
    call_site   = Column(String(50), nullable=True, comment='Code-Aufrufstelle')
```

Achtung: der `comment=` von `latency_ms` wird dabei **praeziser gefasst** (vorher nur „API-Latenz in
ms"). Das ist Punkt-23-Aktualitaetspflicht, kein Beifang: die Spalte bekommt in dieser Phase zum
ersten Mal eine Bedeutung, und die alte Beschreibung liesse die D-07-Regel offen.

**(b) Tabellen-Schild in `__table_args__` (Z.520) ERSETZEN durch — wortgleich:**

```
Jeder API-Call mit eingefrorenem Wechselkurs und Rate (Founder Cost Dashboard, steuerlich korrekt). Status: lebt. Schreibt ausschliesslich services/cost_tracker.py log_api_cost (gerufen aus services/claude_service.py sieben Live-Pfaden, coaching_service.py, precall_service.py, qa_pipeline.py, deepgram_service.py, training_service.py, crm_service.py, judge_runner.py, adoption_runner.py, outcome_service.py, routes/dashboard.py, routes/payments.py, routes/training.py, nerve_rt/services/session_manager.py); liest routes/admin_dashboard.py (Founder-Dashboard: Tab Ausgaben inkl. Live-KI-Auswertung je context_tag, CSV-Export) + services/eur_calculator.py (EUER). latency_ms/ttft_ms tragen die Dauer NUR an der input-Token-Buchung (D-07).
```

**(c) Neue Datei `alembic/versions/0036_messgeraete1_ttft_ms.py`** nach dem Muster von `0034`:

```python
"""ttft_ms fuer api_cost_log + Schild-Nachzug (Punkt 23)

Revision ID: 0036
Revises: 0035
Create Date: 2026-08-03

Phase 08.23.2.MESSGERAETE-1. Streaming-Pfade brauchen ZWEI Zahlen (D-03): latency_ms misst
bis zum LETZTEN Token, ttft_ms bis zum ERSTEN. Beide Bedeutungen in eine Spalte zu kippen
waere derselbe "Name luegt"-Fehler, der latency_e/latency_c unbrauchbar macht (D-02).

WARUM ALEMBIC UND NICHT _migrate(): app._migrate() RETURNT AUF POSTGRES FRUEH (app.py:140).
Die dortigen ALTER TABLE api_cost_log ADD COLUMN latency_ms/call_site (app.py:943-955) sind
der Legacy-SQLite-Pfad und auf Prod TOT — dokumentiert im Code selbst (app.py:955-962: der
Phase-08.14-ApiRate-Seed war aus genau diesem Grund auf Prod wirkungslos). Ein zusaetzlicher
_migrate()-Eintrag wird BEWUSST NICHT angelegt: es gibt keinen lokalen Dev-Pfad mehr
(CLAUDE.md "Kein Local-Dev"), und _migrate() ist das Alt-Muster (Punkt 29 — nie das
Alt-Muster fuer neuen Code kopieren).

Prod-Stand bei Planung (inspect.sh migrations, 2026-08-03): version_num = 0035.
"""
from alembic import op
import sqlalchemy as sa

revision = '0036'
down_revision = '0035'
branch_labels = None
depends_on = None

_TABELLE_NEU = (...)   # wortgleich zum __table_args__-Text aus (b)
_TABELLE_ALT = (
    'Jeder API-Call mit eingefrorenem Wechselkurs und Rate (Founder Cost Dashboard, '
    'steuerlich korrekt). Status: lebt. Schreibt API-Call-Wrapper in services/; '
    'liest Founder-Cost-Dashboard.'
)
_TTFT_SCHILD = (...)      # wortgleich zum comment= von ttft_ms aus (a)
_LATENCY_SCHILD_NEU = (...)  # wortgleich zum comment= von latency_ms aus (a)
_LATENCY_SCHILD_ALT = 'API-Latenz in ms'


def _comment(objekt: str, text: str) -> None:
    op.execute("COMMENT ON {} IS '{}'".format(objekt, text.replace("'", "''")))


def upgrade():
    op.add_column('api_cost_log', sa.Column('ttft_ms', sa.Integer(), nullable=True))
    _comment('COLUMN api_cost_log.ttft_ms', _TTFT_SCHILD)
    _comment('COLUMN api_cost_log.latency_ms', _LATENCY_SCHILD_NEU)
    _comment('TABLE api_cost_log', _TABELLE_NEU)


def downgrade():
    _comment('TABLE api_cost_log', _TABELLE_ALT)
    _comment('COLUMN api_cost_log.latency_ms', _LATENCY_SCHILD_ALT)
    op.drop_column('api_cost_log', 'ttft_ms')
```

Die `...`-Platzhalter sind vom Executor mit den EXAKTEN Texten aus (a)/(b) zu fuellen — wortgleich,
sonst behauptet das DB-Schild etwas anderes als models.py (Punkt 23).

**KEINE Aenderung an `app.py`.** Kein `_migrate()`-Eintrag. Kein Backfill alter Zeilen
(D-02-Finanzamt-Linie: die Vergangenheit wird markiert, nicht umgeschrieben).
  </action>

  <verify>
    <automated>grep -c "ttft_ms" database/models.py</automated>
    <automated>grep -c "down_revision = '0035'" alembic/versions/0036_messgeraete1_ttft_ms.py</automated>
    <automated>grep -c "_migrate" alembic/versions/0036_messgeraete1_ttft_ms.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ttft_ms" database/models.py` >= 1
    - `grep -cF "ttft_ms     = Column(Integer, nullable=True, comment=" database/models.py` == 1
    - `grep -cF "down_revision = '0035'" alembic/versions/0036_messgeraete1_ttft_ms.py` == 1
    - `grep -cF "revision = '0036'" alembic/versions/0036_messgeraete1_ttft_ms.py` == 1
    - `grep -cF "op.add_column('api_cost_log', sa.Column('ttft_ms'" alembic/versions/0036_messgeraete1_ttft_ms.py` == 1
    - `grep -cF "COMMENT ON" alembic/versions/0036_messgeraete1_ttft_ms.py` == 1 (nur im `_comment`-Helfer, kein Copy-Paste-Wildwuchs)
    - `grep -c "log_api_cost" alembic/versions/0036_messgeraete1_ttft_ms.py` == 0
    - `grep -rn "ttft_ms" app.py | wc -l` == 0 — **kein** `_migrate()`-Eintrag (CONTEXT Punkt 8, CLAUDE.md Punkt 29)
    - Der Schild-Text in `database/models.py` `__table_args__` und der Text in `_TABELLE_NEU` in der
      Migration sind **zeichengleich**: `python -c "import re,pathlib; m=pathlib.Path('database/models.py').read_text(encoding='utf-8'); a=pathlib.Path('alembic/versions/0036_messgeraete1_ttft_ms.py').read_text(encoding='utf-8'); print('OK' if 'Schreibt ausschliesslich services/cost_tracker.py log_api_cost' in m and 'Schreibt ausschliesslich services/cost_tracker.py log_api_cost' in a else 'DIVERGENZ')"` gibt `OK`
    - `ls alembic/versions/ | grep -c "^0036_"` == 1 (genau eine 0036, kein zweiter Head)
  </acceptance_criteria>

  <done>
`api_cost_log` hat im ORM die Spalte `ttft_ms` mit Schild, das Tabellen-Schild nennt Schreiber
(cost_tracker als einzigen direkten Schreiber) und Leser (admin_dashboard + eur_calculator), und
Migration `0036` sitzt auf `down_revision = '0035'`. `app.py` ist unberuehrt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Waechter bauen und ERST ROT laufen lassen (AST-abgeleitet, mit Mindest-Soll und RESTLUECKEN)</name>

  <read_first>
    - `tests/test_cost_hook_coverage.py` VOLLSTAENDIG (Haus-Muster: Docstring mit „BEWUSSTE GRENZEN",
      kommentierte Allowlist, zweistufiger Aufbau, Mindest-Soll-Gedanke)
    - `tests/test_cost_model_truth.py` Z.60-75 + Z.200-212 (Muster fuer AST-Erkennung von
      `log_api_cost`-Calls UND fuer den Mindest-Soll-Test „Scanner hat nichts gefunden = kaputt")
    - `services/claude_service.py` Z.405-445, Z.490-520, Z.551-608, Z.733-790, Z.789-930, Z.1000-1110,
      Z.1112-1143 — die sieben Live-Pfade im Ist-Zustand (OHNE Messung; das ist der ROT-Zustand)
    - CLAUDE.md → Punkt 31 VOLLSTAENDIG (die sechs Pflicht-Bestandteile des RESTLUECKEN-Absatzes,
      der ROT-Beleg als Acceptance-Artefakt, das Mindest-Soll gegen den stillen Ausfall)
    - CONTEXT D-05 + Punkt 7 (Liste aus dem Code ableiten; der Beleg, warum Handpflege scheitert)
  </read_first>

  <files>tests/test_live_latency_coverage.py</files>

  <action>
Neue Datei `tests/test_live_latency_coverage.py`. Reiner statischer AST-Sweep — **keine DB, kein
Netz, kein App-Import**, damit er auch ohne laufende Umgebung fahrbar ist.

**Ableitung (D-05, KEINE handgepflegte Liste):**
1. `ast.parse` auf `services/claude_service.py`.
2. Alle `ast.FunctionDef` sammeln, in deren Rumpf ein `ast.Call` vorkommt, dessen `func` ein
   `ast.Attribute` mit `attr in {'create', 'stream'}` ist und dessen `value` ein `ast.Attribute`
   mit `attr == 'messages'` ist. Ergebnis = `LIVE_FUNCS`.
3. Je Funktion alle `log_api_cost`-Calls sammeln (Erkennung wie `_is_log_api_cost` in
   `tests/test_cost_model_truth.py:64-69` — `ast.Name` ODER `ast.Attribute`).
4. Je Buchung das Keyword `unit_type` auslesen (nur `ast.Constant`-Strings).

**Vier Pruefungen — genau diese, in dieser Reihenfolge:**

`test_mindestens_sieben_live_pfade_gefunden`
> Sperre gegen den stillen Ausfall (Punkt 31). `MINDEST_LIVE_PFADE = 7`. Faellt die AST-Ableitung
> aus (Umbau, Umbenennung, kaputter Parser), waere der Waechter sonst **blind statt rot** und saehe
> gruen aus. Fehlermeldung muss woertlich enthalten: „Die Zahl NIE stillschweigend senken — Ursache
> klaeren und mit Begruendung nachziehen."

`test_input_buchung_traegt_latency_ms`
> Fuer JEDE Funktion in `LIVE_FUNCS`: die Buchung mit `unit_type='per_1k_input_tokens'` muss das
> Keyword `latency_ms=` tragen. Fehlt sie ganz → ebenfalls rot (eine Live-Funktion ohne
> Input-Token-Buchung ist selbst ein Defekt). Fehlermeldung nennt Funktionsname + Zeilennummer.

`test_streaming_pfade_tragen_zusaetzlich_ttft_ms`
> Fuer jede Funktion aus `LIVE_FUNCS`, die einen `messages.stream`-Call enthaelt: die
> `per_1k_input_tokens`-Buchung muss ZUSAETZLICH `ttft_ms=` tragen. Mindest-Soll: `>= 2`
> Streaming-Funktionen (`streame_auto_variante`, `streame_manual_ewb_variante`) — CONTEXT Punkt 7,
> die ROADMAP-Liste kannte nur eine.

`test_nur_die_input_buchung_traegt_die_dauer`
> D-07-Doppelzaehlungs-Sperre. Buchungen mit `unit_type` in
> `{'per_1k_output_tokens', 'per_1k_cache_read_tokens', 'per_1k_cache_write_tokens'}` duerfen
> WEDER `latency_ms=` NOCH `ttft_ms=` tragen. Ohne diese Pruefung wandert die Dauer irgendwann per
> Copy-Paste an die Output-Buchung und jede API-Antwort zaehlt doppelt in Ø/p50/p95.

**Modul-Docstring — Pflicht-Abschnitte (Punkt 31, alle sechs Bestandteile):**

```
RESTLUECKEN
-----------
1. ABGEDECKT: dass in jeder Funktion von services/claude_service.py, die messages.create oder
   messages.stream ruft, die input-Token-Buchung ein latency_ms-Keyword traegt (Streaming
   zusaetzlich ttft_ms) — und dass Output-/Cache-Buchungen es NICHT tragen.

2. ⚠ DIE ZENTRALE LUECKE, ausdruecklich benannt: der Waechter prueft, DASS gemessen wird —
   NICHT, DASS DIE ZAHL STIMMT. Er sieht ein Keyword im Syntaxbaum. Ob `_t_api_start`
   unmittelbar vor dem API-Aufruf steht oder 40 Zeilen frueher (dann misst man Prompt-Bau mit),
   kann er strukturell nicht beurteilen — AST-Nachbarschaft ist kein Beweis fuer
   Ausfuehrungs-Nachbarschaft (try/except, with, Retry-Schleifen verschieben die Reihenfolge).
   Er wuerde `latency_ms=0` genauso gruen abnicken wie den richtigen Wert.

3. STRUKTURELL UNSICHTBAR: nur services/claude_service.py wird geparst. Ein achter Live-Pfad in
   einer anderen Datei taucht hier nie auf. Ebenso unsichtbar: dynamischer Dispatch (getattr),
   Monkeypatch, ein Wrapper, der log_api_cost weiterreicht.

4. HEURISTIK, zweischneidig: unit_type wird nur als ast.Constant-String gelesen. Eine Buchung
   mit unit_type=_VARIABLE rutscht LAUTLOS DURCH (Durchrutscher). Umgekehrt wuerde eine
   Hilfsfunktion, die messages.create nur erwaehnt ohne sie zu rufen, faelschlich als Live-Pfad
   gefordert (Falschtreffer).

5. GEPRUEFT UND GESCHLOSSEN:
   - Doppelzaehlung ueber Cache-/Output-Buchungen (D-07): geschlossen durch
     test_nur_die_input_buchung_traegt_die_dauer.
   - Stiller Ausfall der Ableitung: geschlossen durch MINDEST_LIVE_PFADE = 7.
   - Streaming mit nur EINER Zahl (D-03): geschlossen durch
     test_streaming_pfade_tragen_zusaetzlich_ttft_ms.
   - Rest-Kante: ein Live-Pfad, der GAR KEINEN Cost-Hook hat, faellt hier ueber die fehlende
     input-Buchung rot — die vollstaendige Hook-Abdeckung bleibt aber Aufgabe von
     tests/test_cost_hook_coverage.py (W2 aus KOSTEN-1).

6. ZWEITE SCHICHT DARUNTER: die Abnahme an ECHTEN Prod-Daten (D-06, Plan 04) —
   SELECT context_tag, COUNT(*), COUNT(latency_ms) ... mit der Forderung
   COUNT(latency_ms) = COUNT(*) je Live-Sorte. Plus der Leser im Founder-Dashboard (Plan 03):
   eine Frage-Sorte mit "0 Antworten" oder einer offensichtlich unsinnigen Ø-Dauer faellt dort
   ins Auge. Ein gruener Waechter ist ausdruecklich KEIN Ersatz fuer beides.
```

**Danach: ROT-Beleg ziehen (VOR jeder Aenderung an claude_service.py).**
Der Waechter ist eine reine Datei-Analyse und braucht keine DB. Er wird gegen den **auf Production
deployten** Code gefahren — dort ist die Messung noch nicht drin, also muss er fallen:

```bash
# 1) Stand auf Prod bestaetigen (der Waechter prueft DIESEN Code)
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh git-stand'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "cd /opt/nerve/app && grep -c 'latency_ms=' services/claude_service.py"   # erwartet: 0

# 2) NUR die Waechter-Datei hochlegen (kein Deploy — die Messung fehlt ja noch)
scp -i ~/.ssh/nerve_vps tests/test_live_latency_coverage.py \
    root@178.104.82.166:/opt/nerve/app/tests/test_live_latency_coverage.py

# 3) ROT-Lauf — Ausgabe VERBATIM sichern
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_live_latency_coverage.py -q --tb=short'

# 4) Datei wieder abraeumen (sonst faellt sie dem Waisen-Prune von deploy.sh zur Last)
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'rm -f /opt/nerve/app/tests/test_live_latency_coverage.py'
```

Erwartung Schritt 3: `test_input_buchung_traegt_latency_ms` und
`test_streaming_pfade_tragen_zusaetzlich_ttft_ms` **FAILED**, mit einer Liste von **sieben** bzw.
**zwei** Funktionsnamen. `test_mindestens_sieben_live_pfade_gefunden` muss dabei **gruen** sein —
sonst ist der Sweep kaputt, nicht der Code.

Die verbatim-Ausgabe aus Schritt 3 gehoert in den Commit-Body **und** spaeter in die SUMMARY
(Punkt 31: der ROT-Lauf ist ein Acceptance-Artefakt, keine Nebenbemerkung).

**Was NICHT erlaubt ist**, wenn der Waechter spaeter anschlaegt: Allowlist fuellen, Muster
aufweichen, Mindest-Soll senken. Einzige zulaessige Ausnahme ist der nachweisliche Falsch-Treffer
mit `# FALSCH-TREFFER:`-Kommentar, `Datei:Zeile` und Begruendung.
  </action>

  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_live_latency_coverage.py -q --tb=short'</automated>
    <!-- MUSS ROT sein. Ein gruener Lauf an dieser Stelle bedeutet: der Waechter prueft nichts. -->
  </verify>

  <acceptance_criteria>
    - `grep -c "ast.parse" tests/test_live_latency_coverage.py` >= 1 (Ableitung per AST, nicht per Hand)
    - `grep -cF "MINDEST_LIVE_PFADE = 7" tests/test_live_latency_coverage.py` == 1
    - `grep -c "def test_" tests/test_live_latency_coverage.py` == 4
    - `grep -cF "RESTLUECKEN" tests/test_live_latency_coverage.py` == 1
    - `grep -cF "NICHT, DASS DIE ZAHL STIMMT" tests/test_live_latency_coverage.py` == 1
    - `grep -cF "ZWEITE SCHICHT DARUNTER" tests/test_live_latency_coverage.py` == 1
    - `grep -cF "GEPRUEFT UND GESCHLOSSEN" tests/test_live_latency_coverage.py` == 1
    - `grep -cF "per_1k_cache_read_tokens" tests/test_live_latency_coverage.py` >= 1 (D-07-Sperre ist im Pruefkatalog)
    - `grep -c "import services\|from services\|import app\|get_session" tests/test_live_latency_coverage.py` == 0 (reiner Datei-Sweep, kein App-Import)
    - Der ROT-Lauf aus Schritt 3 endet mit mindestens `2 failed`, und die Fehlermeldung von
      `test_input_buchung_traegt_latency_ms` listet **7** Funktionsnamen. Ausgabe verbatim im
      Commit-Body.
    - `test_mindestens_sieben_live_pfade_gefunden` ist im ROT-Lauf **PASSED** (sonst: Sweep kaputt → Abbruch, nicht weiterbauen)
    - Nach Schritt 4: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'ls /opt/nerve/app/tests/test_live_latency_coverage.py'` meldet „No such file"
  </acceptance_criteria>

  <done>
`tests/test_live_latency_coverage.py` existiert, leitet die sieben Live-Pfade per AST ab, hat ein
Mindest-Soll und einen vollstaendigen RESTLUECKEN-Absatz nach Punkt 31 — und ist gegen den heutigen
Prod-Code **nachweislich rot** gelaufen, mit verbatim gesicherter Ausgabe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migrations-Runner → Postgres | Laeuft auf Prod als DB-User `postgres` (peer-auth) — hoechste Rechte, DDL auf einer Tabelle mit Finanzdaten |
| Repo → Prod-Dateisystem | `scp` einer einzelnen Test-Datei nach `/opt/nerve/app/tests/` fuer den ROT-Beleg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MESS-01 | Tampering | `_comment()` in `alembic/versions/0036_messgeraete1_ttft_ms.py` | mitigate | Schild-Texte sind **Modul-Konstanten**, nie Laufzeit-Eingaben; `_comment` escaped Apostrophe via `text.replace("'", "''")` (Muster `0034`). Keine f-String-Interpolation von Variablen in das SQL. |
| T-MESS-02 | Denial of Service | `ALTER TABLE api_cost_log ADD COLUMN` auf Prod | accept | `ADD COLUMN ... NULL` ohne Default ist in PG11+ eine reine Katalog-Aenderung (kein Table-Rewrite, kein langer ACCESS-EXCLUSIVE-Halt). Die Tabelle hat ~4-stellige Zeilenzahl. Risiko vernachlaessigbar. |
| T-MESS-03 | Tampering | Migration schreibt Bestandsdaten um | mitigate | `upgrade()` enthaelt **kein** UPDATE/Backfill — greppbar als Acceptance (`grep -c "UPDATE " ... == 0`). D-02-Finanzamt-Linie: alte Zeilen werden nie rueckwirkend korrigiert. |
| T-MESS-04 | Elevation of Privilege | `scp` einer Test-Datei nach `/opt/nerve/app/tests/` | mitigate | Die Datei ist ein reiner Datei-Sweep ohne App-Import (per Acceptance greppbar: 0 Treffer auf `import services`/`get_session`), wird ausschliesslich mit `pytest` gelesen und in Schritt 4 wieder geloescht. Kein Neustart des Dienstes, kein Eingriff in laufenden Code. |
| T-MESS-05 | Repudiation | Waechter ohne ROT-Beleg | mitigate | Der verbatim-ROT-Lauf ist Acceptance-Kriterium und wandert in Commit-Body + SUMMARY (Punkt 31). Ohne ihn gilt der Waechter als unbewiesen. |
</threat_model>

<verification>
1. `grep -c "ttft_ms" database/models.py` >= 1 und `grep -rn "ttft_ms" app.py | wc -l` == 0
2. Migration `0036` sitzt auf `0035`, hat genau ein `op.add_column`, kein `UPDATE`, keinen `_migrate`-Bezug
3. Schild-Text ist zwischen `models.py` und `0036` zeichengleich
4. Waechter existiert, hat 4 Tests, Mindest-Soll 7, RESTLUECKEN-Absatz mit allen sechs Punkt-31-Bestandteilen
5. ROT-Beleg gegen den Prod-Code liegt verbatim vor (>= 2 failed, 7 gelistete Funktionen, Mindest-Soll-Test gruen)
6. **KEIN Deploy in diesem Plan.** `deploy.sh production` waere hier rot (der Waechter ist absichtlich rot) — das Ausrollen macht Plan 04, nach Plan 02.
</verification>

<success_criteria>
- `ttft_ms` existiert im ORM und in Migration `0036` (Head `0035`), mit Punkt-23-Schild
- Das Tabellen-Schild von `api_cost_log` nennt Schreiber und Leser vollstaendig und aktuell
- Der Waechter leitet die Pfade aus dem Code ab, hat ein Mindest-Soll und benennt seine Grenzen
- Der ROT-Beleg ist gezogen und gesichert
- `app.py` ist unberuehrt (kein `_migrate()`-Eintrag)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-01-SUMMARY.md`
— inklusive der **verbatim** ROT-Ausgabe des Waechters.
</output>
