---
phase: 08.23.2.MESSGERAETE-1
plan: 02
subsystem: services
tags: [latency, cost-tracking, streaming, ast, anthropic, live-loop]

# Dependency graph
requires:
  - phase: 08.23.2.MESSGERAETE-1-01
    provides: "DB-Spalte ttft_ms (Migration 0036, noch nicht ausgefuehrt), ORM-Spalte, LIVE_LLM_CONTEXT_TAGS/CACHE_CONTEXT_TAGS, der ROTE Waechter"
  - phase: 08.23.2.KOSTEN-1
    provides: "log_api_cost als einziger Schreiber von api_cost_log, Cost-Hook-Muster (Punkt 30)"
provides:
  - "Alle ACHT produktiven Live-LLM-Pfade schreiben ihre reine API-Dauer als latency_ms"
  - "Beide Streaming-Pfade schreiben zusaetzlich ttft_ms aus DEMSELBEN Nullpunkt (D-03)"
  - "log_api_cost kennt das Keyword ttft_ms und reicht es an ApiCostLog durch"
  - "D-09-Pruning-Entscheidung als Code-Kommentar an der logs/-Latenz-Aggregation"
  - "tests/test_live_latency_coverage.py GRUEN (6 passed, serverseitig gefahren)"
affects: [08.23.2.MESSGERAETE-1 Plan 03 (Leser im Founder-Dashboard), Plan 04 (Migration 0036 + Deploy + Abnahme an echten Daten)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Mess-Anker als zwei nackte time.monotonic()-Lesungen inline statt Kontextmanager/Decorator (Punkt 27 + Punkt 25)"
    - "Hoist-Pflicht: ein Funktionsaufruf im Argument des API-Aufrufs wird VOR den Anker gezogen, weil Python Argumente NACH der davorstehenden Anweisung auswertet (Punkt 14)"
    - "Eine API-Antwort traegt die Dauer genau EINMAL — an der per_1k_input_tokens-Buchung (D-07)"
    - "Serverseitiger Waechter-Lauf ohne Deploy: Quelldateien in ein Scratch-Verzeichnis auf Prod, pytest dagegen, Verzeichnis abraeumen"

key-files:
  created: []
  modified:
    - services/cost_tracker.py
    - services/claude_service.py
    - services/qa_pipeline.py
    - services/live_session.py

key-decisions:
  - "D-07 umgesetzt wie empfohlen: latency_ms/ttft_ms haengen AUSSCHLIESSLICH an der input-Token-Buchung; Output- und Cache-Buchungen bleiben NULL — die Begruendung steht als Kommentar ueber der Buchung in analysiere_mit_claude (der Stelle mit allen vier Buchungsarten)"
  - "Der Hoist bei analysiere_coaching ist gebaut: _system_coaching = _build_coaching_prompt(sid=sid) steht VOR dem Anker. Ohne ihn laege Profil-Load + Nahme von ls._session_state_lock im Messfenster; kein Waechter dieser Phase kann das sehen"
  - "_t_stream_start in beiden Streaming-Pfaden aufgeloest zugunsten des EINEN Ankers _t_api_start auf dem Modul-time — Voraussetzung dafuer, dass ttft_ms und latency_ms denselben Nullpunkt haben (0 Treffer auf _t_stream_start)"
  - "Retry-Semantik (Punkt 11): _latency_ms wird im Manual-Pfad erst NACH dem with-Block und VOR dem break gesetzt, _ttft_ms pro Versuch zurueckgesetzt — ein Fehlversuch samt Backoff faellt aus der Zahl heraus"
  - "D-09: die logs/-Textdatei-Aggregation bleibt stehen (siehe Pruning-Notiz), Diff ist rein additiv"
  - "Die D-02-Gegenprobe laeuft wortgenau (grep -cw), weil der vom Plan woertlich vorgegebene D-07-Kommentar den Dateinamen test_live_latency_coverage.py enthaelt und damit einen Substring-Falschtreffer auf latency_c erzeugt"

patterns-established:
  - "Waechter-Beweis ohne Deploy: scp der geaenderten Quelldateien nach /tmp/<gate>/services + tests, pytest serverseitig, Gegenprobe (Zaehler im Scratch vs. im deployten Stand), dann rm -rf"

requirements-completed: []

# Metrics
duration: 41min
completed: 2026-08-04
---

# Phase 08.23.2.MESSGERAETE-1 Plan 02: Die Messung einbauen Summary

**Alle acht produktiven Live-KI-Aufrufe (sieben in `claude_service.py`, `classify_utterance` in `qa_pipeline.py`) klammern ihren `messages.create`/`.stream` jetzt zwischen zwei `time.monotonic()`-Lesungen und reichen die reine API-Dauer an der input-Token-Buchung durch; die zwei Streaming-Pfade liefern aus demselben Nullpunkt zusaetzlich `ttft_ms` — der Waechter aus Plan 01 ist damit von `3 failed` auf `6 passed` gedreht.**

## Performance

- **Duration:** 41 min
- **Started:** 2026-08-04T10:05:00Z
- **Completed:** 2026-08-04T10:46:00Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- **Acht Messpunkte statt fuenf.** `phase_classify`, `coldcall_infer`, `live_haiku`, `live_haiku_merged`, `coaching_haiku`, `pip_autovar`, `pip_variante` (alle `services/claude_service.py`) und `qa_classifier` (`services/qa_pipeline.py`). Die zwei Pfade, die die ROADMAP-Liste nicht kannte (`live_haiku`, `pip_autovar`), und der Rollback-Zwilling aus einer anderen Datei (`qa_classifier`, D-10) sind mit dabei.
- **Der Hoist bei `analysiere_coaching` ist gebaut** — die einzige Verteidigung gegen einen Messwert, der bei Lock-Contention beliebig luegt, und die einzige Stelle dieser Phase, die kein Waechter pruefen kann.
- **Zwei Zeit-Bedeutungen, zwei Spalten, EIN Nullpunkt.** `_t_stream_start` ist in beiden Streaming-Pfaden verschwunden; `ttft_ms` und `latency_ms` beziehen sich jetzt nachweislich auf denselben Startzeitpunkt (`grep _t_stream_start | wc -l` → `0`).
- **Doppelzaehlung strukturell ausgeschlossen** (D-07) — AST-Gegenprobe ueber beide Dateien: keine einzige Output- oder Cache-Buchung traegt eine Dauer.
- **Der Waechter ist gruen, serverseitig belegt** — und die Gegenprobe zeigt, dass er dabei wirklich den neuen Stand gelesen hat (7 Treffer im Scratch vs. 0 im deployten Stand).

## Task Commits

1. **Task 1: `ttft_ms` in `log_api_cost` + die sechs blockierenden Live-Pfade (inkl. Hoist)** — `640a998` (feat)
2. **Task 2: Die zwei Streaming-Pfade — `latency_ms` bis zum letzten, `ttft_ms` bis zum ersten Token** — `75993f7` (feat)
3. **Task 3: Pruning-Notiz D-09 sichtbar beantwortet** — `6b0ef48` (docs)

## Files Created/Modified

- `services/cost_tracker.py` — Keyword `ttft_ms` an genau drei Stellen: Signatur, Docstring-Zeile, `db.add(ApiCostLog(...))`. Sonst nichts: kein Wrapper, kein Framework, keine Umbenennung.
- `services/claude_service.py` — sieben Mess-Anker, zwei TTFT-Durchreichungen, der Hoist bei `analysiere_coaching`, der D-07-Begruendungsblock, der Retry-Semantik-Block. `latency_e`/`latency_c`, `_ewb_ttft_history`, `EWB_SONNET_FALLBACK_TTFT_MS` und der `[EWB-TTFT]`-`print` unveraendert.
- `services/qa_pipeline.py` — `import time` auf Modul-Ebene (kein Import im heissen Pfad), achter Mess-Anker in `classify_utterance`, `latency_ms` an der input-Buchung. `generate_qa_response` **nicht** angefasst (toter Code, steht begruendet auf der Waechter-Allowlist).
- `services/live_session.py` — **nur** der D-09-Kommentarblock. `git diff --numstat` → `7 0` (sieben Additionen, null Loeschungen).

## Waechter-Lauf — GRUEN (Acceptance-Artefakt, CLAUDE.md Punkt 31)

Gefahren **auf dem Production-Server**, ohne Deploy und ohne Dienst-Neustart: die drei geaenderten Quelldateien plus der Waechter wurden in ein Scratch-Verzeichnis kopiert (der Waechter ist ein reiner Datei-Sweep und leitet seine Wurzel aus dem eigenen Dateipfad ab), dort gefahren, danach abgeraeumt.

```
$ scp services/claude_service.py services/qa_pipeline.py services/cost_tracker.py \
      root@178.104.82.166:/tmp/mg1_gate/services/
$ scp tests/test_live_latency_coverage.py root@178.104.82.166:/tmp/mg1_gate/tests/

$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
    'cd /tmp/mg1_gate && /opt/nerve/venv/bin/pytest tests/test_live_latency_coverage.py -v --tb=short -p no:cacheprovider'

============================= test session starts ==============================
platform linux -- Python 3.12.3, pytest-9.0.3, pluggy-1.6.0 -- /opt/nerve/venv/bin/python3
rootdir: /tmp/mg1_gate
plugins: anyio-4.13.0
collecting ... collected 6 items

tests/test_live_latency_coverage.py::test_mindestens_acht_live_pfade_gefunden PASSED [ 16%]
tests/test_live_latency_coverage.py::test_input_buchung_traegt_latency_ms PASSED [ 33%]
tests/test_live_latency_coverage.py::test_streaming_pfade_tragen_zusaetzlich_ttft_ms PASSED [ 50%]
tests/test_live_latency_coverage.py::test_nur_die_input_buchung_traegt_die_dauer PASSED [ 66%]
tests/test_live_latency_coverage.py::test_live_tag_liste_ist_synchron PASSED [ 83%]
tests/test_live_latency_coverage.py::test_allowlist_ist_begruendet_und_lebt PASSED [100%]

============================== 6 passed in 0.17s ===============================
```

Gegenprobe, dass der Sweep **den neuen Stand** gelesen hat und nicht zufaellig den deployten (sonst waere „gruen" bedeutungslos):

```
$ ssh ... 'echo "scratch: $(grep -c "latency_ms=_latency_ms" /tmp/mg1_gate/services/claude_service.py)";
           echo "deployt: $(grep -c "latency_ms=_latency_ms" /opt/nerve/app/services/claude_service.py)"'
scratch (neuer Stand): 7
deployt (alter Stand): 0
```

Danach abgeraeumt:

```
$ ssh ... 'rm -rf /tmp/mg1_gate && ls /tmp/mg1_gate'
ls: cannot access '/tmp/mg1_gate': No such file or directory
```

Der ROT-Beleg desselben Waechters gegen denselben deployten Stand steht in der SUMMARY von Plan 01 (`3 failed, 3 passed`). Rot → gruen ist damit ueber beide Plans belegt, jeweils serverseitig.

## Statische Abnahme (die vier Bedingungen direkt, ohne Waechter)

| Kriterium | Soll | Ist |
|---|---|---|
| `grep -c "_t_api_start = time.monotonic()" services/claude_service.py` | 7 | 7 |
| `grep -c "_t_api_start = time.monotonic()" services/qa_pipeline.py` | 1 | 1 |
| `grep -c "latency_ms=_latency_ms" services/claude_service.py` | 7 | 7 |
| `grep -c "latency_ms=_latency_ms" services/qa_pipeline.py` | 1 | 1 |
| `grep -c "ttft_ms=int(_ttft_ms) if _ttft_ms is not None else None"` | 2 | 2 |
| `grep "_t_stream_start" \| wc -l` | 0 | 0 |
| `grep -c "ttft_ms" services/cost_tracker.py` | 3 | 3 |
| Hoist-Anker: `_system_coaching = _build_coaching_prompt(sid=sid)` / `system=_system_coaching,` / alter Aufruf im Argument | 1 / 1 / 0 | 1 / 1 / 0 |
| AST: kein Funktionsaufruf mehr in einem Argument von `messages.create/stream` | `OK` | `OK` |
| AST D-07: keine Output-/Cache-Buchung traegt `latency_ms`/`ttft_ms` | `OK` | `OK` |
| Regex „nichts zwischen Anker und `create`" (claude_service / qa_pipeline) | 5 / 1 | 5 / 1 |
| `unit_type='per_1k_output_tokens'` unveraendert | 7 | 7 |
| `_ewb_ttft_history` / `EWB_SONNET_FALLBACK_TTFT_MS` unveraendert | 3 / 1 | 3 / 1 |
| `latency_e`/`latency_c` **wortgenau** unveraendert (D-02) | 6 | 6 |
| `latenzen_einwand`/`latenzen_coaching` unveraendert | 11 | 11 |
| `git diff --numstat services/live_session.py` | nur Additionen | `7 0` |
| `ast.parse` aller vier Dateien | `SYNTAX OK` | `SYNTAX OK` |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] D-02-Gegenprobe auf wortgenaues Greppen umgestellt statt den vorgegebenen Kommentar zu kuerzen**
- **Found during:** Task 1 (Acceptance-Lauf)
- **Issue:** Das Kriterium lautet „`grep -c "latency_e\|latency_c" services/claude_service.py` ist unveraendert" (Ausgangswert 6). Nach Task 1 stand dort 7. Ursache ist **kein** Code: der vom Plan **woertlich vorgegebene** D-07-Kommentarblock enthaelt den Satz „tests/test_live_latency_coverage.py haelt beide Richtungen fest" — und `test_live_laten**cy_c**overage` enthaelt den Substring `latency_c`. Der Plan erzeugt seinen eigenen Falschtreffer.
- **Fix:** Der Kommentar bleibt **unveraendert** (er ist woertliche Plan-Vorgabe und traegt die D-07-Begruendung). Die Gegenprobe laeuft stattdessen wortgenau: `grep -cw 'latency_e\|latency_c'` → **6**, und dasselbe Kommando gegen `git show HEAD:services/claude_service.py` → **6**. Identisch, also sind `latency_e`/`latency_c` nachweislich unangetastet. Der belegte Anti-Pattern-Reflex („Text an den Anker anpassen statt umgekehrt", CLAUDE.md Werkzeug-Falle) wurde bewusst nicht bedient.
- **Files modified:** keine (reine Pruef-Methodik)
- **Commit:** `640a998`

**2. [Rule 3 - Blocking] Semantik-Kommentar im Manual-Streaming-Pfad umformuliert, damit ein Zaehl-Anker eindeutig bleibt**
- **Found during:** Task 2
- **Issue:** Die Acceptance verlangt `grep -cF "ttft_ms = bis zum ERSTEN Token, latency_ms = bis zum LETZTEN"` == **1**. Der Satz stand nach dem ersten Entwurf in **beiden** Streaming-Pfaden (Zaehler 2). Der billige Weg zu Gruen waere gewesen, den Schutz-Kommentar irgendwo zu loeschen.
- **Fix:** Der Satz steht woertlich genau einmal — in `streame_auto_variante`, wo ihn der Plan vorgibt. Im Manual-Pfad steht dieselbe Aussage in eigenen Worten **plus** einem Verweis auf die ausgeschriebene Stelle („die Zeit bis zum ersten Token und die bis zum Stream-Ende (Semantik siehe `streame_auto_variante`, dort einmal ausgeschrieben)"). Keine Information verloren, Anker eindeutig.
- **Files modified:** `services/claude_service.py`
- **Commit:** `75993f7`

### Abweichung von der Plan-Annahme (Task 3, Punkt-18-Gegenprobe)

Der Plan nannte fuenf Bestandstest-Dateien mit dem Zusatz „muss trotzdem nachgemessen werden, nicht abgeschrieben". Nachgemessen: `grep -rln "log_api_cost\|ApiCostLog\|EWB-TTFT\|_ewb_ttft_history" tests/` liefert **14** Dateien, nicht 5. Alle 14 wurden einzeln geprueft; die vollstaendige Bewertung je Datei steht im Commit-Text von `6b0ef48`. **Ergebnis unveraendert: kein Bestandstest ist nachzuziehen.** Die neun zusaetzlichen Dateien rufen `log_api_cost` entweder nur auf (neues Keyword hat Default `None`) oder assertieren einen anderen Vertrag.

**Total deviations:** 2 auto-fixed (beide Rule 3, beide betrafen Zaehl-Anker und nicht die Substanz) + 1 praezisierte Gegenprobe. Kein Scope-Creep, keine Semantik-Aenderung, keine Datei ausserhalb der vier geplanten angefasst.

## Issues Encountered

- **Ein `_monotonic`-Alias in `streame_manual_ewb_variante` ist jetzt ungenutzt.** `from time import monotonic as _monotonic` (`services/claude_service.py:1067`) hatte genau zwei Nutzer — beide waren die Mess-Stellen, die jetzt auf dem Modul-`time` liegen. Der Plan schreibt ausdruecklich „die lokalen Aliase `_time`/`_monotonic` bleiben bestehen"; `_time` wird vom Backoff-`sleep` weiterhin gebraucht, `_monotonic` nicht mehr. **Bewusst stehengelassen** (Reparatur-Modus: kein Beifang, ausdrueckliche Plan-Vorgabe) und unten in der Pruning-Notiz als benannter Rest gefuehrt — nicht als stilles Legacy.

## Known Stubs

Keine. Die Messung ist an allen acht Pfaden vollstaendig verdrahtet. Zwei beabsichtigte, in der Plan-Reihenfolge begruendete Luecken bleiben bis Plan 03/04 offen:
- Die Spalte `ttft_ms` hat **noch keinen Leser** — das ist Plan 03.
- Migration `0036` ist geschrieben, aber **nicht ausgefuehrt**; Prod steht weiter auf `version_num = 0035`. Das Ausrollen ist Plan 04. **Reihenfolge dort ist bindend: erst Migration, dann Code** — sonst schreibt `log_api_cost` gegen eine Spalte, die auf Prod nicht existiert. (Der Deploy-Gate-Testlauf selbst ist davon unabhaengig sicher: `deploy.sh` baut `nerve_test` per `pg_dump`-Restore **und faehrt `alembic upgrade head` dagegen**, `deploy.sh:187` — dort existiert `ttft_ms` also im selben Lauf.)

## Pruning-Notiz (Bau-Regel 7)

**1. Die `logs/`-Textdatei-Aggregation der Latenzen (`services/live_session.py`, Block `LATENZ-STATISTIKEN`; Ausgabe `routes/app_routes.py`) — BLEIBT STEHEN, begruendet.** Wortlaut der Entscheidung, wie sie jetzt im Code steht:

1. **Sie misst etwas anderes.** Die Aggregation summiert `latency_e`/`latency_c` — „wie lange lag der Satz im Puffer + KI + Nachverarbeitung" (D-02). `api_cost_log.latency_ms` misst die **reine API-Dauer**. Das eine ersetzt das andere nicht; es sind zwei verschiedene Fragen („warum fuehlt sich der Live-Loop langsam an?" vs. „wie lange braucht die API?"). Sie werden erst zusammen aussagekraeftig: die **Differenz** der beiden ist die Wartezeit, die NICHT an Anthropic liegt — genau die Zahl, die eine spaetere Tempo-Phase braucht.
2. **„Obsolet" waere hier eine Behauptung, keine Feststellung.** Genau davor warnt D-09 ausdruecklich.
3. **Sie ist Teil des Call-Log-Textes**, nicht eine Nebenausgabe. Call-Logs sind laut den unverhandelbaren Daten-Schranken heilig; eine Sektion aus einem laufenden Protokoll-Format zu entfernen ist eine eigene Entscheidung, kein Beifang dieser Phase.
4. **Doppelfuehrung liegt nicht vor**, weil es zwei verschiedene Masse sind — die Gefahr, vor der D-09 schuetzt, tritt hier nicht ein. Sie traete ein, wenn jemand spaeter `latency_e` in `latency_ms` kippt; genau das verbietet D-02, und der neue Kommentar im Code nagelt es fest.

**2. `from time import monotonic as _monotonic` (`services/claude_service.py:1067`) — bleibt stehen, ist aber ab jetzt ungenutzt.** Der Plan schreibt den Erhalt der lokalen Aliase ausdruecklich vor. Kandidat fuer die naechste Aufraeum-Mini-Phase; kein Verhalten haengt daran (`_time` fuer den Backoff-`sleep` bleibt gebraucht).

**3. Nichts geloescht.** Diese Phase hat keinen Code obsolet gemacht.

## Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.

## Threat Flags

Keine. Die angefasste Flaeche entspricht dem `<threat_model>` des Plans:

- **T-MESS-06 (DoS im Live-Pfad):** eingehalten. Pro Aufruf zwei `time.monotonic()`-Lesungen, keine DB-, Netz- oder Lock-Operation, kein `sleep`. Greppbar belegt ueber die Regex-Acceptance (zwischen Anker und `messages.create` steht keine weitere Anweisung, 5 bzw. 1 Treffer). Einzige bewusste Ausnahme sind die Streaming-Pfade, wo die per-Token-`sio.emit`-Aufrufe zwangslaeufig im Messfenster liegen — das ist der Stream selbst, im Code begruendet.
- **T-MESS-07 (Cost-Hook wirft):** eingehalten. `_latency_ms`/`_ttft_ms` werden in beiden Streaming-Pfaden **vor** dem `try` bzw. **vor** der Retry-Schleife auf `None` initialisiert → kein `NameError` auf Fehlerpfaden; die neuen Keywords stehen innerhalb der bestehenden `try/except`-Rahmen; `log_api_cost` faengt weiterhin alles ab.
- **T-MESS-08 (falscher Nullpunkt):** mitigiert, soweit statisch moeglich — EIN Anker, `_t_stream_start` verschwunden, Regex-Acceptance gruen, Hoist gebaut. Die zweite Schicht bleibt die Abnahme an echten Daten in Plan 04.
- **T-MESS-09 (Information Disclosure):** unveraendert `accept` — es werden ausschliesslich Millisekunden-Zahlen zusaetzlich persistiert, kein Transkript, kein Prompt, kein Antworttext.
- **T-MESS-10 (Doppelzaehlung):** geschlossen, AST-Gegenprobe `OK`.
- **T-MESS-11 (Retry-Semantik):** geschlossen, Bedeutung steht als greppbarer Kommentar an der Stelle selbst.

## User Setup Required

Keine.

## Next Phase Readiness

**Bereit fuer Plan 03 (Leser im Founder-Dashboard).** Was Plan 03 und 04 aus diesem Plan mitnehmen muessen:

- Der Leser filtert auf **`latency_ms IS NOT NULL`** — Cache- und Output-Zeilen sind bewusst NULL (D-07). Das ist keine Luecke, das ist die Entzerrung.
- `ttft_ms` ist **nur** bei `pip_autovar` und `pip_variante` je befuellt. Bei allen anderen Sorten bleibt die TTFT-Spalte dauerhaft leer — so beschriften, dass es nicht als Defekt gelesen wird.
- **Dormanz-Ehrlichkeit fuer die Abnahme in Plan 04 (CONTEXT Punkt 13):** `live_haiku`, `pip_autovar` und `qa_classifier` sind **Messung eingebaut und statisch bewacht, an echten Daten aber NICHT belegbar** (dormant bzw. rollback-only, Umschalten braucht Neustart). `COUNT(latency_ms) = COUNT(*)` kann fuer sie strukturell nie eintreten. Die Forderung aus D-06 gilt deshalb fuer **jede im Test-Anruf vorgekommene** Live-Sorte, nicht fuer alle acht.
- Der TTFT-Nachweis an echten Daten haengt an **einem** Pfad (`pip_variante`).
- **Plan 04, bindende Reihenfolge:** `alembic current` messen → Migration `0036` ausfuehren (als DB-User `postgres`, peer-auth) → Gegenprobe, dass die Spalte da ist → `deploy.sh production` → Test-Anruf → D-06-Abfrage.

## Self-Check: PASSED

- Alle vier geaenderten Dateien existieren: `services/cost_tracker.py`, `services/claude_service.py`, `services/qa_pipeline.py`, `services/live_session.py` — jeweils `FOUND`.
- Alle drei Task-Commits in `git log` auffindbar: `640a998`, `75993f7`, `6b0ef48` — jeweils `FOUND`.
- Kein Commit dieses Plans enthaelt Datei-Loeschungen (`git diff --diff-filter=D` ueber alle drei Commits: leer).
- `.planning/STATE.md` und `.planning/ROADMAP.md` wurden **nicht** veraendert (schreibt der Orchestrator am Phasen-Ende).
- Die phasenfremden Arbeitsbaum-Eintraege (`.claude/settings.local.json`, `_design_export/`) sind unberuehrt und ungestaged geblieben.

---
*Phase: 08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf*
*Completed: 2026-08-04*
