---
phase: 08.23.2.MESSGERAETE-1
plan: 03
type: execute
wave: 2
depends_on: ["08.23.2.MESSGERAETE-1-01"]   # Migration 0036 + LIVE_LLM_CONTEXT_TAGS (D-11)
files_modified:
  - routes/admin_dashboard.py
  - templates/admin/_tab_ausgaben.html
  - static/admin_dashboard.css
  - tests/test_messgeraete1_leser.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Andre sieht im Founder-Dashboard ZWEI Tabellen: Live-KI je Frage-Sorte (mit Dauer + TTFT) und Uebrige Kosten (ohne Dauer-Spalten)"
    - "Die Zuordnung kommt aus GENAU EINER Liste (services/cost_tracker.py::LIVE_LLM_CONTEXT_TAGS) — nicht doppelt gepflegt"
    - "Ein kuenftig unbekannter context_tag faellt still in Tabelle 2, nicht unter die Live-KI-Ueberschrift"
    - "Eine API-Antwort geht genau EINMAL in Ø/p50/p95 ein (Filter latency_ms IS NOT NULL)"
    - "Der Zeitraum ist waehlbar und folgt dem bestehenden _parse_period-Muster"
    - "Die Ansicht liegt hinter demselben Zugriffsschutz wie der Rest des Founder-Dashboards"
    - "Die Ansicht enthaelt keine fest eingetragenen Farbwerte und keine neuen Farb-Tokens"
  artifacts:
    - path: "routes/admin_dashboard.py"
      provides: "Zwei-Wege-Aggregation nach context_tag inkl. Perzentilen"
      contains: "uebrige_kosten"
    - path: "templates/admin/_tab_ausgaben.html"
      provides: "Zwei Tabellen im Tab Ausgaben: Live-KI je Frage-Sorte + Uebrige Kosten"
      contains: "fcd-uebrige-body"
    - path: "tests/test_messgeraete1_leser.py"
      provides: "Tests fuer Perzentil-Helfer und Aggregation (deterministisch, ohne DB)"
      min_lines: 60
  key_links:
    - from: "templates/admin/_tab_ausgaben.html"
      to: "/admin/dashboard/ausgaben"
      via: "bestehender fetch in load(), neue Felder live_ki + uebrige_kosten"
      pattern: "uebrige_kosten"
    - from: "routes/admin_dashboard.py"
      to: "api_cost_log.latency_ms"
      via: "SQLAlchemy-Query mit isnot(None)-Filter"
      pattern: "latency_ms"
---

**Komplexitaet: 🟡** — Cross-AI-Review (`/gsd-review --gemini --claude`) ist Pflicht VOR Execute.

<objective>
Der Leser. Ohne ihn ist die ganze Phase wertlos — genau sein Fehlen hat `latency_ms` und `call_site`
seit Phase 08.13 zu toten Feldern gemacht: Spalte da, Schreiber teilweise da, **kein Leser** → niemand
merkt, dass nichts drinsteht.

Purpose: sichtbar machen, welche Frage-Sorte teuer und welche langsam ist. Das ist die Saeule
„Eingreifen" — ohne sie kann Andre keine Tempo- oder Kosten-Entscheidung treffen.

Output: zwei neue Auswertungen (`live_ki` + `uebrige_kosten`) im bestehenden Ausgaben-Endpunkt, zwei Tabellen im Tab
„Ausgaben", eine Handvoll deterministischer Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-CONTEXT.md
@CLAUDE.md
@routes/CLAUDE.md
@routes/admin_dashboard.py
@templates/admin/_tab_ausgaben.html
@static/admin_dashboard.css
</context>

<interfaces>
<!-- Alles, was der Executor braucht. KEIN Erkunden noetig. -->

Zugriffsschutz (steht bereits, wird uebernommen — `routes/admin_dashboard.py:323-326`):
```python
@admin_dashboard_bp.route('/ausgaben')
@login_required
@superadmin_required
def ausgaben_page():
```
`superadmin_required` (`services/auth_decorators.py:5-11`) prueft `g.user.is_superadmin`, sonst `abort(403)`.

Zeitraum-Muster (`routes/admin_dashboard.py:25-39`):
```python
def _parse_period(period_str):
    """Parst 'YYYY-MM' in (start_date, end_date_exclusive). Fallback: aktueller Monat.
    Bei ungueltigem Format: abort(400)."""
```

Ist-Rueckgabe von `ausgaben_page` (`:398-407`) — die neue Auswertung kommt als ZUSAETZLICHER Key dazu:
```python
        return jsonify({
            'period': ..., 'by_provider': ..., 'api_total': ..., 'fixed_costs': ...,
            'fixed_total': ..., 'grand_total': ..., 'daily_30': ..., 'api_rates': ...,
        })
```

Relevante ORM-Spalten (`database/models.py`, `ttft_ms` legt Plan 01 an):
```python
class ApiCostLog(Base):
    created_at, provider, model, cost_eur, session_id
    context_tag = Column(String(32), nullable=True)
    latency_ms  = Column(Integer, nullable=True)
    ttft_ms     = Column(Integer, nullable=True)   # NEU aus Plan 01
```

Die EINE Liste (Plan 01 Task 1 (d), `services/cost_tracker.py`) — dieser Plan **importiert** sie,
er definiert sie NICHT:
```python
LIVE_LLM_CONTEXT_TAGS: dict[str, str]   # 10 Eintraege: 8 Live-Tags + 'analyse' + 'ewb'
CACHE_CONTEXT_TAGS: tuple[str, ...]     # ('analyse', 'ewb') — Teilmenge davon
```
`LIVE_LLM_CONTEXT_TAGS` **ist** die Menge fuer Tabelle 1. Alles andere geht in Tabelle 2.
In Prod existieren **33** verschiedene `context_tag`-Werte (`stt` = Deepgram-Minuten und damit die
mengenmaessig groesste Kostenposition, `stripe_fee`, `training_*`, `precall`, `judge`, `crm`,
`postcall_coach` …). Ohne diese Trennung landeten sie alle unter „Live-KI je Frage-Sorte", rund
zwei Dutzend davon ohne Label und mit „—" in jeder Dauer-Spalte.


Die Live-`context_tag`s mit echten Prod-Zahlen (21 Tage, 2026-07-23 – 2026-08-02):
`live_haiku_merged` 152 · `coaching_haiku` 132 · `phase_classify` 16 · `coldcall_infer` 16 ·
`pip_variante` 14. Dazu ab MESSGERAETE-1 auch `live_haiku` und `pip_autovar`.

Vorhandene CSS-Klassen im Founder-Dashboard (`static/admin_dashboard.css:186-225`):
`.fcd-table`, `.fcd-table th`, `.fcd-table td`, `.fcd-table tfoot th`. Buttons app-weit: `.n-btn`,
`.n-btn-ghost`.
</interfaces>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
- `public.api_cost_log` — **nur lesend**.

### inspect.sh-Beleg
Siehe Plan 01, Abschnitt „5. Persistenz-Schicht-Verifikation" (verbatim `schema api_cost_log`,
gezogen 2026-08-03 gegen Production). Bestaetigt: `context_tag varchar(32) NULL`,
`latency_ms integer NULL`, `call_site varchar(50) NULL`, Index auf `created_at`.
**`ttft_ms` existiert dort noch nicht** — sie kommt aus Migration `0036` (Plan 01). Dieser Plan
haengt deshalb hart an Plan 01 (`depends_on`).

### Cross-Layer-Konsistenz-Tabelle

| Gelesenes Datum | Lese-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `context_tag` | neue Aggregation | DB-Spalte `api_cost_log.context_tag` | ✓ inspect.sh (Plan 01) |
| `cost_eur` | `func.sum` | DB-Spalte `api_cost_log.cost_eur` numeric(12,6) | ✓ inspect.sh (Plan 01) |
| `latency_ms` | Perzentil-Berechnung | DB-Spalte `api_cost_log.latency_ms` integer | ✓ inspect.sh (Plan 01) |
| `ttft_ms` | Ø-Berechnung | DB-Spalte, **erst nach Migration 0036** vorhanden | ✓ `depends_on: 01` |
| „Frage-Sorte" | Anzeige-Label | **KEINE eigene Spalte** — identisch mit `context_tag` (Punkt 21: der Anzeige-Name ist kein zweites Feld) | ✓ |

### Schreib-Zeitpunkt / kann der Leser vorauslaufen? (Punkt 26)
`api_cost_log`-Zeilen werden **synchron** von `services/cost_tracker.py::log_api_cost` geschrieben
(eigene Session, `db.commit()` im selben Aufruf, `cost_tracker.py:217-233`). Kein Batch-Write, kein
Hintergrund-Thread. Der Leser ist eine Admin-Ansicht auf einen abgeschlossenen Zeitraum — er kann
dem Schreiber strukturell nicht vorauslaufen. **Zeit-Anker:** die Ansicht filtert auf
`created_at` — hier ist das korrekt, weil `created_at` bei synchronem Write **gleich** dem
Ereigniszeitpunkt ist (nicht der Batch-Falle aus Punkt 26 unterworfen).

## Vier Saeulen (Bau-Regel D)
- **Automatisieren:** die Auswertung rechnet selbst, Andre klickt nur den Monat.
- **Nachvollziehen:** liest `api_cost_log`, nicht eine Textdatei.
- **Eingreifen:** die Tabelle zeigt in einem Blick, welche Frage-Sorte teuer/langsam ist — die
  Entscheidung „abschalten/zusammenlegen" der Folgephase haengt daran.
- **Marge:** Kosten je Frage-Sorte ist die Eingangsgroesse der Preis-Untergrenze.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggregation nach context_tag im Ausgaben-Endpunkt (inkl. Perzentilen, D-07-Filter)</name>

  <read_first>
    - `routes/admin_dashboard.py` Z.1-60 (Blueprint, Decorators, `_parse_period`, `COST_DATA_COMPLETE_SINCE`)
    - `routes/admin_dashboard.py` Z.312-410 (`ausgaben_page` komplett — die zu erweiternde Funktion,
      inkl. `try/finally: db.close()`-Muster und der `jsonify`-Rueckgabe)
    - `routes/admin_dashboard.py` Z.803-835 (CSV-Export — er liest `context_tag` bereits, dient als
      Beleg, dass das Feld befuellt ist)
    - `database/models.py` Z.515-537 (ApiCostLog nach Plan 01, mit `ttft_ms`)
    - CONTEXT D-04 (was die Ansicht zeigen muss) und D-07 (warum `latency_ms IS NOT NULL` gefiltert wird)
    - `services/cost_tracker.py` Z.185-238 (Beleg fuer den synchronen Write — Punkt 26)
  </read_first>

  <files>routes/admin_dashboard.py, tests/test_messgeraete1_leser.py</files>

  <behavior>
    - `_perzentil([10, 20, 30, 40], 0.5)` == 20 und `_perzentil([10, 20, 30, 40], 0.95)` == 40 (Nearest-Rank, deterministisch)
    - `_perzentil([], 0.5)` is None (kein ZeroDivisionError, kein Absturz auf leeren Sorten)
    - `_perzentil([42], 0.95)` == 42
    - `_aggregiere_live_ki` zaehlt aus 4 Buchungen einer Antwort (input+output+cache-read+cache-write) genau EINE Antwort, weil nur die input-Zeile ein latency_ms traegt
    - Eine Sorte ohne jede Dauer (nur Cache-/Output-Zeilen) erscheint mit `antworten == 0` und allen Dauer-Feldern None — sie verschwindet NICHT aus der Liste
    - Buchungen mit `context_tag is None` landen unter dem Label `(ohne Herkunft)`
    - Sortierung: absteigend nach Kosten
  </behavior>

  <action>
**(a) Import + zwei reine Hilfsfunktionen** in `routes/admin_dashboard.py`, direkt unter
`COST_DATA_COMPLETE_SINCE` (Z.50). Rein, ohne DB — damit ohne Datenbank testbar:

```python
from services.cost_tracker import LIVE_LLM_CONTEXT_TAGS, CACHE_CONTEXT_TAGS

# ── Phase 08.23.2.MESSGERAETE-1 — Leser fuer die Live-KI-Messung ──────────────────────────
# ★ D-11: die Zuordnung "welcher context_tag ist Live-KI" kommt aus GENAU EINER Liste in
# services/cost_tracker.py. Hier wird sie NUR gelesen — keine zweite Liste, kein Duplikat.
# tests/test_live_latency_coverage.py::test_live_tag_liste_ist_synchron faellt rot, wenn die
# Liste von den Code-Funden abweicht.
#
# Perzentile werden in PYTHON gerechnet, nicht per percentile_cont in SQL. Grund (Punkt 27,
# einfachster tragfaehiger Weg): percentile_cont ist Postgres-Dialekt; die Tests laufen teils
# ohne Postgres, und eine Dialekt-Weiche im Leser waere mehr Mechanik als das Problem gross
# ist. Es werden nur ZWEI Integer-Spalten des gewaehlten Monats geladen (bei den heutigen
# Groessenordnungen — 152 Buchungen der teuersten Sorte in 21 Tagen — sind das dreistellige
# bis niedrig vierstellige Zahlen pro Monat).
_OHNE_HERKUNFT = '(ohne Herkunft)'


def _perzentil(werte_sortiert, q):
    """Nearest-Rank-Perzentil auf einer AUFSTEIGEND sortierten Liste. None bei leerer Liste.

    Bewusst Nearest-Rank (kein Interpolieren): das Ergebnis ist immer ein tatsaechlich
    gemessener Wert, keine Rechengroesse zwischen zwei Messungen. Bei zwei Dutzend Werten
    pro Sorte ist Interpolation ohnehin Scheingenauigkeit.
    """
    if not werte_sortiert:
        return None
    import math
    rang = max(1, math.ceil(q * len(werte_sortiert)))
    return int(werte_sortiert[rang - 1])


def _aggregiere_kosten_nach_tag(summen_rows, latenz_rows):
    """Teilt die Kosten in ZWEI Listen: (live_ki, uebrige_kosten).

    summen_rows: Iterable von (context_tag, anzahl_buchungen, summe_cost_eur) — ALLE Tags
    latenz_rows: Iterable von (context_tag, latency_ms, ttft_ms) — NUR Zeilen mit
                 latency_ms IS NOT NULL

    ★ D-07 (Doppelzaehlung): log_api_cost laeuft pro API-ANTWORT 2-4x (input, output, ggf.
    cache-read, cache-write). Die Dauer haengt deshalb NUR an der input-Token-Buchung
    (services/claude_service.py + services/qa_pipeline.py, MESSGERAETE-1 Plan 02). Dieser Leser
    filtert auf latency_ms IS NOT NULL und zaehlt damit jede API-Antwort GENAU EINMAL in
    Ø/p50/p95 — waehrend 'buchungen' bewusst weiterhin ALLE Zeilen zaehlt (das ist die
    Kosten-Sicht). Deshalb stehen 'buchungen' und 'antworten' als ZWEI Spalten nebeneinander;
    sie sind nicht dasselbe und duerfen nie zusammengefasst werden.

    ★ Punkt 12: Tabelle 2 bekommt bewusst KEINE Dauer-Felder. Ein neuer, unbekannter Tag faellt
    still dorthin — fuer Nicht-LLM-Kosten (stt, stripe_fee, training_*) ist das gewuenscht.
    Fuer einen neuen LIVE-Pfad waere es eine Luecke; die faengt der Waechter (Sync-Test), solange
    der Pfad in claude_service.py oder qa_pipeline.py steht. Steht er woanders: RESTLUECKE 3.

    ★ DSGVO/Mandanten: die Rueckgabe enthaelt ausschliesslich context_tag, Anzahlen, Kosten und
    Millisekunden. Kein user_id, kein org_id, kein session_id, kein Transkript. Eine nach
    context_tag gruppierte Aggregation ueber alle Mandanten ist damit personenbezugsfrei; eine
    Zeilen-Ansicht waere es nicht und wird deshalb NICHT gebaut.
    """
    roh = {}
    for tag, anzahl, summe in summen_rows:
        schluessel = tag or _OHNE_HERKUNFT
        e = roh.setdefault(schluessel, {'buchungen': 0, 'kosten_eur': 0.0, '_lat': [], '_ttft': []})
        e['buchungen'] += int(anzahl or 0)
        e['kosten_eur'] += float(summe or 0)
    for tag, lat, ttft in latenz_rows:
        schluessel = tag or _OHNE_HERKUNFT
        e = roh.setdefault(schluessel, {'buchungen': 0, 'kosten_eur': 0.0, '_lat': [], '_ttft': []})
        e['_lat'].append(int(lat))
        if ttft is not None:
            e['_ttft'].append(int(ttft))

    live_ki, uebrige_kosten = [], []
    for schluessel, e in roh.items():
        kosten = round(e['kosten_eur'], 4)
        if schluessel in LIVE_LLM_CONTEXT_TAGS:
            lat = sorted(e['_lat'])
            ttft = e['_ttft']
            live_ki.append({
                'context_tag': schluessel,
                'label': LIVE_LLM_CONTEXT_TAGS[schluessel],
                'nur_cache': schluessel in CACHE_CONTEXT_TAGS,
                'buchungen': e['buchungen'],
                'kosten_eur': kosten,
                'antworten': len(lat),
                'latenz_avg_ms': int(sum(lat) / len(lat)) if lat else None,
                'latenz_p50_ms': _perzentil(lat, 0.5),
                'latenz_p95_ms': _perzentil(lat, 0.95),
                'ttft_avg_ms': int(sum(ttft) / len(ttft)) if ttft else None,
            })
        else:
            uebrige_kosten.append({
                'context_tag': schluessel,
                'label': schluessel,
                'buchungen': e['buchungen'],
                'kosten_eur': kosten,
            })
    live_ki.sort(key=lambda r: r['kosten_eur'], reverse=True)
    uebrige_kosten.sort(key=lambda r: r['kosten_eur'], reverse=True)
    return live_ki, uebrige_kosten
```

**(b) In `ausgaben_page`** — nach dem `rates_out`-Block (Z.396) und VOR dem `return jsonify(...)`:

```python
        # ── MESSGERAETE-1 (D-04 + Punkt 12): zwei Tabellen aus EINER Abfrage-Paarung ──────
        # Zwei Abfragen statt einer: die Kosten-Summe braucht ALLE Zeilen, die Dauer-Statistik
        # nur die mit gesetztem latency_ms (D-07). Eine gemeinsame Abfrage wuerde entweder
        # Kosten unterschlagen oder Dauern doppelt zaehlen. Der Split Live-KI/Uebrige passiert
        # in Python gegen LIVE_LLM_CONTEXT_TAGS — kein context_tag-Filter im SQL, damit ein
        # unbekannter Tag sichtbar bleibt statt lautlos zu verschwinden.
        ktag_summen = (db.query(ApiCostLog.context_tag,
                                func.count(ApiCostLog.id),
                                func.sum(ApiCostLog.cost_eur))
                         .filter(ApiCostLog.created_at >= start,
                                 ApiCostLog.created_at < end)
                         .group_by(ApiCostLog.context_tag).all())
        ktag_latenzen = (db.query(ApiCostLog.context_tag,
                                  ApiCostLog.latency_ms,
                                  ApiCostLog.ttft_ms)
                           .filter(ApiCostLog.created_at >= start,
                                   ApiCostLog.created_at < end,
                                   ApiCostLog.latency_ms.isnot(None)).all())
        live_ki, uebrige_kosten = _aggregiere_kosten_nach_tag(ktag_summen, ktag_latenzen)
```
und im `jsonify` als zwei zusaetzliche Keys:
```python
            'live_ki': live_ki,
            'uebrige_kosten': uebrige_kosten,
```

`func` ist in der Funktion bereits importiert (`from sqlalchemy import func`, Z.328); `ApiCostLog`
ebenfalls (Z.327). Neu ist nur der Modul-Import aus (a) und `math` lokal in `_perzentil`.

**(c) Kein neuer Endpunkt, keine neue Route.** Der Zugriffsschutz (`@login_required` +
`@superadmin_required`) wird damit unveraendert **geerbt** — die sicherste und zugleich
einfachste Variante (Punkt 27). Die Zeitraum-Wahl kommt gratis aus dem bestehenden
`_parse_period`-Muster (D-04-Anforderung „Zeitraum waehlbar" erfuellt).

**(d) `_LATENZ_LABELS` wird NICHT hier angelegt.** Die Labels leben in
`LIVE_LLM_CONTEXT_TAGS` (Plan 01). Ein zweites Label-Dict in `admin_dashboard.py` waere genau
der zweite Pflegeort, den D-11 verbietet.


**(e) Neue Testdatei `tests/test_messgeraete1_leser.py`** — deterministisch, ohne DB, ohne
App-Kontext, mit **genau diesen neun** Tests (der Executor darf keinen streichen):

1. `test_perzentil_p50` — `_perzentil([10, 20, 30, 40], 0.5) == 20`
2. `test_perzentil_p95` — `_perzentil([10, 20, 30, 40], 0.95) == 40`
3. `test_perzentil_leer_ist_none` — `_perzentil([], 0.5) is None` (kein ZeroDivisionError)
4. `test_perzentil_einzelwert` — `_perzentil([42], 0.95) == 42`
5. `test_eine_antwort_zaehlt_einmal` — vier Buchungen einer Antwort (input+output+cache-read+
   cache-write), nur die input-Zeile traegt `latency_ms` → `antworten == 1`, `buchungen == 4`
6. `test_sorte_ohne_dauer_verschwindet_nicht` — eine Live-Sorte nur mit Cache-/Output-Zeilen
   erscheint mit `antworten == 0` und allen Dauer-Feldern `None`
7. `test_unbekannter_tag_faellt_in_tabelle_zwei` — `'stt'` und `'stripe_fee'` landen in
   `uebrige_kosten`, **nicht** in `live_ki`; ihre Dicts haben **keine** Dauer-Schluessel
8. `test_cache_tags_sind_tabelle_eins_mit_flag` — `'analyse'`/`'ewb'` landen in `live_ki` mit
   `nur_cache is True` und `antworten == 0`
9. `test_ohne_context_tag_bekommt_label` — `context_tag=None` landet als `(ohne Herkunft)` in
   `uebrige_kosten`; beide Listen sind absteigend nach Kosten sortiert

Test-Docstring nennt die Grenze: *„Prueft die Rechnung und den Split, nicht die Abfrage — ob die
SQLAlchemy-Filter die richtigen Zeilen liefern, beweist erst die Abnahme an echten Prod-Daten
(D-06, Plan 04)."*


  </action>

  <verify>
    <!-- Rein statisch. Kein `scp`, kein `pytest` — weder lokal (CLAUDE.md HART) noch auf
         Prod (das waere ungetesteter Produktiv-Code auf dem Live-Server). Der EINZIGE
         verbindliche Testlauf dieser Datei ist das Deploy-Tor in Plan 04. -->
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/admin_dashboard.py').read_text(encoding='utf-8')); ast.parse(pathlib.Path('tests/test_messgeraete1_leser.py').read_text(encoding='utf-8')); print('SYNTAX OK')"</automated>
    <automated>grep -c "def test_" tests/test_messgeraete1_leser.py</automated>
    <automated>grep -cF "ApiCostLog.latency_ms.isnot(None)" routes/admin_dashboard.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "from services.cost_tracker import LIVE_LLM_CONTEXT_TAGS, CACHE_CONTEXT_TAGS" routes/admin_dashboard.py` == 1
    - `grep -cF "def _perzentil(" routes/admin_dashboard.py` == 1
    - `grep -cF "def _aggregiere_kosten_nach_tag(" routes/admin_dashboard.py` == 1
    - `grep -cF "live_ki, uebrige_kosten = _aggregiere_kosten_nach_tag(ktag_summen, ktag_latenzen)" routes/admin_dashboard.py` == 1
    - `grep -cF "'live_ki': live_ki," routes/admin_dashboard.py` == 1
    - `grep -cF "'uebrige_kosten': uebrige_kosten," routes/admin_dashboard.py` == 1
    - `grep -cF "ApiCostLog.latency_ms.isnot(None)" routes/admin_dashboard.py` == 1 (D-07-Filter)
    - `grep -cF "'antworten': len(lat)" routes/admin_dashboard.py` == 1
    - **D-11, kein zweiter Pflegeort:** `grep "_LATENZ_LABELS" routes/admin_dashboard.py | wc -l` == 0
      und `grep -cF "if schluessel in LIVE_LLM_CONTEXT_TAGS:" routes/admin_dashboard.py` == 1
    - **Punkt 12, kein SQL-Filter auf context_tag** (ein unbekannter Tag muss sichtbar bleiben):
      `grep "ApiCostLog.context_tag.in_\|context_tag ==" routes/admin_dashboard.py | wc -l` == 0
    - `grep -cF "Kein user_id, kein org_id, kein session_id" routes/admin_dashboard.py` == 1 (DSGVO-Begruendung im Code)
    - `grep -c "@admin_dashboard_bp.route" routes/admin_dashboard.py` ist **unveraendert** gegenueber dem Stand vor diesem Task (keine neue Route → geerbter Zugriffsschutz)
    - `grep "percentile_cont" routes/admin_dashboard.py | wc -l` == 0 (kein Dialekt-Zwang)
    - `grep -c "def test_" tests/test_messgeraete1_leser.py` == 9 (die neun Tests aus (e), keiner gestrichen)
    - **Kein `pytest` in diesem Task — weder lokal noch auf Prod.**
      Lokal verbietet es CLAUDE.md (HART, keine Ausnahmen; der Test importiert
      `routes.admin_dashboard` und damit Flask-Blueprint + Models, also einen echten App-Import).
      Ein Zwischenlauf **auf** Prod scheidet ebenfalls aus: er muesste `routes/admin_dashboard.py`
      — eine PRODUKTIV-Quelldatei — ungetestet auf den Live-Server legen. Prod hat kein `.git`
      (tar-deployed), es gibt also keinen Rueckhol-Befehl, und `inspect.sh git-stand` kann die
      Abweichung mangels `.git` **nicht** erkennen. Ein `deploy.sh production` an dieser Stelle
      wuerde Plan 03 ausrollen, BEVOR Plan 02 und Migration `0036` drauf sind — genau die
      Reihenfolge, die Plan 04 als zwingend festlegt.
      → **Der einzige verbindliche Lauf ist das Deploy-Tor in Plan 04**
      (`deploy.sh production` faehrt `/opt/nerve/venv/bin/pytest tests/ -m "not live and not perf"`
      serverseitig VOR dem Neustart und deckt `tests/test_messgeraete1_leser.py` mit ab).
      Damit bleibt **Plan 01 Task 2 der einzige `scp`-auf-Prod-Vorgang der ganzen Phase**
      — eine reine Testdatei, mit `rm -f` und greppbarem Abraeum-Nachweis.
    - Die Tests tragen keine `@pytest.mark.live`/`perf`-Marker (sonst schliesst das Tor sie per
      `-m "not live and not perf"` aus und waere fuer diese Datei blind):
      `grep -E "@pytest.mark.(live|perf)" tests/test_messgeraete1_leser.py | wc -l` == 0
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/admin_dashboard.py').read_text(encoding='utf-8')); print('SYNTAX OK')"` gibt `SYNTAX OK`
  </acceptance_criteria>

  <done>
`/admin/dashboard/ausgaben` liefert zusaetzlich `live_ki` (Buchungen, Kosten, Antworten,
Ø/p50/p95 Dauer, Ø TTFT je Live-Frage-Sorte) und `uebrige_kosten` (alle anderen Tags, ohne
Dauer-Spalten) — jede API-Antwort genau einmal gezaehlt, die Zuordnung aus GENAU EINER Liste,
hinter dem bestehenden Superadmin-Schutz, ohne Personenbezug.
  </done>
</task>

<task type="auto">
  <name>Task 2: ZWEI Tabellen im Tab „Ausgaben" — ohne eine einzige hardcoded Farbe</name>

  <read_first>
    - `templates/admin/_tab_ausgaben.html` VOLLSTAENDIG (Muster: `<h4>` + `.fcd-table` + `<tbody id="...">`,
      die `load()`-Funktion, `esc()`/`fmt()`-Helfer, der Lazy-Load-Observer am Ende)
    - `static/admin_dashboard.css` Z.185-226 (`.fcd-table`-Regeln — das vorhandene Tabellen-Muster)
    - `static/nerve.css` Z.14-135 (die real existierenden Farb-Variablen; **`var(--accent)` ist ein
      Phantom-Token und existiert NICHT** — vor Verwendung eines Tokens dort greppen)
    - CLAUDE.md → „⛔ HART: Keine hardcoded Farben, keine Inline-Styles in UI-Komponenten" VOLLSTAENDIG
    - CONTEXT D-08 (die Stil-Fesseln dieser Phase)
  </read_first>

  <files>templates/admin/_tab_ausgaben.html, static/admin_dashboard.css</files>

  <action>
**(a) Markup.** Neue Sektion in `templates/admin/_tab_ausgaben.html`, eingefuegt **direkt nach dem
`</table>` der EÜR-Kategorien** (Z.9) — also ganz oben im Tab, weil sie die Frage beantwortet, die
Andre wirklich hat:

```html
  <h4 class="fcd-ktag-head">Live-KI je Frage-Sorte</h4>
  <p class="fcd-ktag-hint">
    Dauer = reine API-Zeit. „Antworten" zählt jede KI-Antwort genau einmal;
    „Buchungen" zählt alle Kostenzeilen (Ein-/Ausgabe-Token, Zwischenspeicher).
    „bis 1. Wort" gibt es nur bei den beiden Stream-Pfaden.
    Die Zeilen „Zwischenspeicher …" sind reine Cache-Token-Buchungen ohne eigene
    KI-Antwort — dort stehen bei den Dauer-Spalten absichtlich Striche.
  </p>
  <table class="fcd-table">
    <thead>
      <tr>
        <th>Frage-Sorte</th><th>Buchungen</th><th>Kosten</th><th>Antworten</th>
        <th>Ø Dauer</th><th>p50</th><th>p95</th><th>Ø bis 1. Wort</th>
      </tr>
    </thead>
    <tbody id="fcd-ktag-body"></tbody>
  </table>

  <h4 class="fcd-ktag-head">Übrige Kosten</h4>
  <p class="fcd-ktag-hint">
    Alles, was keine Live-KI-Frage ist: Spracherkennung, Zahlungsgebühren, Training,
    Vorbereitung, Nachbereitung. Hier gibt es bewusst keine Dauer-Spalten.
  </p>
  <table class="fcd-table">
    <thead><tr><th>Herkunft</th><th>Buchungen</th><th>Kosten</th></tr></thead>
    <tbody id="fcd-uebrige-body"></tbody>
  </table>
```

**(b) Rendern.** In `load()`, nach dem `by_provider`-Block, im vorhandenen Stil:

```javascript
    // MESSGERAETE-1: zwei Tabellen. ms-Formatierung deutsch, "—" wo nichts gemessen wurde.
    const ms = v => (v === null || v === undefined) ? '—'
                    : Number(v).toLocaleString('de-DE') + ' ms';
    document.getElementById('fcd-ktag-body').innerHTML =
      (d.live_ki||[]).map(k => `
        <tr>
          <td>${esc(k.label)}</td>
          <td class="fcd-ktag-num">${esc(k.buchungen)}</td>
          <td class="fcd-ktag-num">${fmt(k.kosten_eur)}</td>
          <td class="fcd-ktag-num">${esc(k.antworten)}</td>
          <td class="fcd-ktag-num">${ms(k.latenz_avg_ms)}</td>
          <td class="fcd-ktag-num">${ms(k.latenz_p50_ms)}</td>
          <td class="fcd-ktag-num">${ms(k.latenz_p95_ms)}</td>
          <td class="fcd-ktag-num">${ms(k.ttft_avg_ms)}</td>
        </tr>`).join('');
    document.getElementById('fcd-uebrige-body').innerHTML =
      (d.uebrige_kosten||[]).map(k => `
        <tr>
          <td>${esc(k.label)}</td>
          <td class="fcd-ktag-num">${esc(k.buchungen)}</td>
          <td class="fcd-ktag-num">${fmt(k.kosten_eur)}</td>
        </tr>`).join('');
```

**(c) CSS.** In `static/admin_dashboard.css`, unmittelbar nach dem `.fcd-table`-Block (nach Z.225).
⚠ **Der End-Marker ist Pflicht**, nicht Kosmetik — die Abnahme-Regel unten schneidet genau
zwischen Start- und End-Marker aus. Ohne ihn liefe sie in den Folgeblock (der ab Z.227 ein
`color:#0D1117` enthaelt) und schluege bei jeder harmlosen Umformatierung falsch-rot an:

```css
/* ── MESSGERAETE-1: Live-KI je Frage-Sorte ──────────────────────────── */
/* Bewusst NUR Ausrichtung und Ziffern-Laufweite. KEINE Farbe, KEIN neues Token:
   die Tabellen erben Rahmen, Hintergrund und Schriftfarbe komplett von .fcd-table.
   Der /MESSGERAETE-1-Marker unten begrenzt die Farb-Pruefregel dieses Plans. */
.fcd-ktag-num {
  text-align: right;
  font-variant-numeric: tabular-nums;
}
.fcd-ktag-head {
  margin-top: 24px;
}
.fcd-ktag-hint {
  font-size: 12px;
  margin: 0 0 8px;
}
/* ── /MESSGERAETE-1 ──────────────────────────────────────────────────── */
```

**(d) Die Stil-Fesseln aus D-08, wortwoertlich einzuhalten:**
- **Kein einziges `style="..."`-Attribut** im neuen Markup und in den neuen JS-Template-Strings.
  Der `margin-top:24px`, den die bestehenden `<h4>` inline tragen, wird fuer die neuen
  Ueberschriften **nicht kopiert** — dafuer gibt es `.fcd-ktag-head`.
- **Keine Farbe.** Weder Hex, noch `rgb(`, noch `var(--...)`. Die neuen Tabellen brauchen schlicht
  keine — sie erben alles von `.fcd-table`. Damit ist auch die Token-Frage erledigt: es wird kein
  Token verwendet, also auch kein Phantom-Token wie `var(--accent)`.
- **Kein neues Farb-Token** in `nerve.css` oder `admin_dashboard.css`.
- **Klassen-Praefix:** die Founder-Admin-Flaeche fuehrt durchgehend `fcd-*` (`.fcd-table`,
  `.fcd-ausgaben`, `.fcd-chart-card`); app-weite Buttons heissen `.n-btn`/`.n-btn-ghost`. D-08
  verlangt „Layout und Farben **ausschliesslich** aus den vorhandenen Mustern in
  `routes/admin_dashboard.py` + zugehoerigem Template" — das vorhandene Muster hier ist `fcd-*`.
  Deshalb `fcd-ktag-*`/`fcd-uebrige-*` und **nicht** `n-ktag-*`: ein zweites Praefix auf derselben
  Flaeche waere genau der Wildwuchs, den die Regel verhindern soll. **Diese Abweichung von der
  `.n-*`-Formulierung in D-08 ist bewusst und hier begruendet.** Neue Buttons gibt es keine.
- Die **bestehenden** Verstoesse in dieser Datei (`background:#00D4AA` in der Anbieter-Balken-Zeile
  Z.90, `backgroundColor:'#00D4AA'` im Chart Z.101, `style="background:rgba(239,68,68,.1)"` bei
  stale Rates Z.109) werden **NICHT** angefasst — Reparatur-Modus: nur die Aufgabe, kein Beifang.
  Sie gehoeren als Fund in die SUMMARY (Kandidat fuer eine spaetere Aufraeum-Mini-Phase), nicht in
  diesen Diff.
  </action>

  <verify>
    <automated>grep -n "ktag\|uebrige" templates/admin/_tab_ausgaben.html | grep "style=" | wc -l</automated>
    <automated>awk '/MESSGERAETE-1: Live-KI je Frage-Sorte/{f=1;next} /\/MESSGERAETE-1/{f=0} f' static/admin_dashboard.css | grep -c "fcd-ktag-num"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF 'id="fcd-ktag-body"' templates/admin/_tab_ausgaben.html` == 1
    - `grep -cF 'id="fcd-uebrige-body"' templates/admin/_tab_ausgaben.html` == 1
    - `grep -cF "(d.live_ki||[])" templates/admin/_tab_ausgaben.html` == 1
    - `grep -cF "(d.uebrige_kosten||[])" templates/admin/_tab_ausgaben.html` == 1
    - **Keine Inline-Styles im neuen Block:** `grep -n "ktag\|uebrige" templates/admin/_tab_ausgaben.html | grep "style=" | wc -l` == 0
    - **Keine hardcoded Farbe im neuen Block:** `grep -n "ktag\|uebrige" templates/admin/_tab_ausgaben.html | grep -E "#[0-9A-Fa-f]{3,8}|rgba?\(" | wc -l` == 0
    - **Keine Farbe im neuen CSS — marker-begrenzt statt `grep -A14`** (der Folgeblock ab
      `admin_dashboard.css:227` enthaelt bei `:235` ein `color:#0D1117`; eine Zeilen-Zaehlung waere
      sproede und der billigste Weg zu Gruen waere, Zeilen aus dem Folgeblock zu loeschen):
      ```bash
      awk '/MESSGERAETE-1: Live-KI je Frage-Sorte/{f=1;next} /\/MESSGERAETE-1/{f=0} f' \
        static/admin_dashboard.css | grep -E '#[0-9A-Fa-f]{3,8}|rgba?\(|var\(--' | wc -l
      ```
      == 0
    - **Existenz-Anker gegen den Leerlauf-Ausfall** (Punkt-31-Logik im Kleinen: ein leerer
      awk-Ausschnitt — weil ein Marker fehlt oder umbenannt wurde — saehe sonst gruen aus):
      dieselbe Extraktion `| grep -c "fcd-ktag-num"` == 1
    - `grep -cF "/* ── /MESSGERAETE-1" static/admin_dashboard.css` == 1 (der End-Marker existiert)
    - **Kein neues Token:** `git diff static/nerve.css | wc -l` == 0 (Datei unberuehrt) und
      `git diff static/admin_dashboard.css | grep "^+.*--[a-z-]*:" | wc -l` == 0
    - **Bestandsverstoesse unberuehrt:** `grep -c "00D4AA" templates/admin/_tab_ausgaben.html` == 2 (unveraendert)
    - `grep -cF ".fcd-ktag-num" static/admin_dashboard.css` == 1
    - `grep -c "fcd-ktag" templates/admin/_tab_ausgaben.html` >= 13 (2 Ueberschriften, 2 Hinweise,
      tbody-id, getElementById, 8 Zellen Tabelle 1, 2 Zellen Tabelle 2 — Zeilen, nicht Vorkommen)
  </acceptance_criteria>

  <done>
Der Tab „Ausgaben" zeigt oben **zwei** Tabellen: „Live-KI je Frage-Sorte" mit acht Spalten
(gerendert aus `live_ki`) und darunter „Übrige Kosten" mit drei Spalten (aus `uebrige_kosten`,
ohne Dauer) — ohne ein einziges `style`-Attribut, ohne Farbwert, ohne neues Token, im vorhandenen
`.fcd-table`-Muster, mit Start- und End-Marker im CSS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/admin/dashboard/ausgaben` | Founder-Admin-Flaeche; Aufrufer koennte ein beliebiger eingeloggter Nutzer sein |
| Aggregation → Mandantendaten | `api_cost_log` traegt `user_id`, `org_id`, `session_id` — eine unvorsichtige Ansicht wuerde Mandanten-Daten mischen |
| JSON → DOM | Werte aus der DB landen in `innerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MESS-12 | Elevation of Privilege | Neue Auswertung im Ausgaben-Endpunkt | mitigate | **Kein neuer Endpunkt.** Die Daten haengen an `ausgaben_page`, das bereits `@login_required` + `@superadmin_required` traegt (`services/auth_decorators.py:5-11` → `abort(403)` ohne `is_superadmin`). Acceptance: Anzahl `@admin_dashboard_bp.route`-Dekoratoren unveraendert. |
| T-MESS-13 | Information Disclosure | Mandanten-/DSGVO-Betrachtung der Gruppierung | mitigate | Die Rueckgabe enthaelt **nur** `context_tag`, Anzahlen, Kosten und Millisekunden — **kein** `user_id`, `org_id`, `session_id`, kein Transkript. Aggregat statt Zeilen-Ansicht ist die bewusste Entscheidung; die Zeilen-Ansicht (die Personenbezug haette) wird nicht gebaut. Begruendung als Code-Kommentar + greppbares Acceptance-Kriterium. |
| T-MESS-14 | Information Disclosure | Kosten-Gesamtsicht ueber alle Organisationen | accept | Das Founder-Cost-Dashboard ist per Zweck organisationsuebergreifend (EUER, Deckungsbeitrag) und nur fuer den Founder erreichbar. Keine Aenderung der bestehenden Lage. |
| T-MESS-15 | Tampering (XSS) | `innerHTML` mit DB-Werten | mitigate | Alle Textwerte laufen durch den vorhandenen `esc()`-Helfer (`_tab_ausgaben.html:51`); Zahlen durch `fmt()`/`ms()` (beide `Number(...)`). `context_tag` ist zudem serverseitig auf `String(32)` begrenzt und stammt aus Code-Literalen, nicht aus Nutzereingaben. |
| T-MESS-16 | Denial of Service | Perzentil-Berechnung laedt Zeilen in den Speicher | mitigate | Die Abfrage laedt **zwei Integer-Spalten** und ist auf einen Monat begrenzt (`_parse_period`, `abort(400)` bei Formatfehler); Index auf `created_at` existiert. Groessenordnung heute: dreistellig pro Monat. Kein `SELECT *`, keine Text-Spalte. |
| T-MESS-17 | Repudiation | Zahl im Dashboard ohne erklaerte Bedeutung | mitigate | Der Hinweistext unter der Ueberschrift erklaert „Antworten" vs. „Buchungen" und dass „bis 1. Wort" nur fuer Stream-Pfade existiert — sonst wird die NULL-Spalte spaeter als Defekt fehlgedeutet. |
</threat_model>

<verification>
1. Statische Pruefung: `ast.parse` beider Dateien OK, `grep -c "def test_" tests/test_messgeraete1_leser.py` >= 7 — der Testlauf selbst gehoert dem Deploy-Tor in Plan 04 (kein `pytest` in diesem Task, weder lokal noch auf Prod)
2. `grep -cF "live_ki, uebrige_kosten = _aggregiere_kosten_nach_tag(ktag_summen, ktag_latenzen)" routes/admin_dashboard.py` == 1 und beide jsonify-Keys vorhanden
3. `grep -cF "ApiCostLog.latency_ms.isnot(None)" routes/admin_dashboard.py` == 1
4. Anzahl der `@admin_dashboard_bp.route`-Dekoratoren unveraendert (kein neuer, ungeschuetzter Endpunkt)
5. `grep -n "ktag\|uebrige" templates/admin/_tab_ausgaben.html | grep "style=" | wc -l` == 0
6. Marker-begrenzte CSS-Farbpruefung == 0 **und** Existenz-Anker (`| grep -c "fcd-ktag-num"`) == 1
7. `git diff static/nerve.css | wc -l` == 0
8. Sichtprüfung ist Teil des Test-Anrufs in Plan 04 (Tab „Ausgaben" oeffnen)
</verification>

<success_criteria>
- `live_ki` liefert je Live-Frage-Sorte: Buchungen, Kosten, Antworten, Ø/p50/p95 Dauer, Ø TTFT;
  `uebrige_kosten` liefert alle anderen Tags ohne Dauer-Spalten
- Die Zuordnung kommt aus GENAU EINER Liste (D-11), nicht aus einem zweiten Label-Dict
- Jede API-Antwort geht genau einmal in die Dauer-Statistik ein (D-07)
- Zeitraum waehlbar ueber das bestehende `_parse_period`-Muster
- Zugriffsschutz geerbt, keine personenbezogenen Felder in der Ausgabe
- Null hardcoded Farben, null Inline-Styles, null neue Tokens im neuen Code
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-03-SUMMARY.md`
— inklusive des Funds „drei Bestands-Farbverstoesse in `_tab_ausgaben.html`, bewusst nicht angefasst".
</output>
