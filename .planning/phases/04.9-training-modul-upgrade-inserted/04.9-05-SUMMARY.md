---
phase: 04.9-training-modul-upgrade-inserted
plan: "05"
subsystem: training
tags: [training, scoring, mood-curve, wendepunkt-analyse, chart-js, beziehungsaufbau]
dependency_graph:
  requires: [generate_response_with_mood, stimmung_history, personality_types_table]
  provides: [generate_scoring_6categories, renderMoodChart, renderWendepunkteDetail, basis_punkte]
  affects: [services/training_service.py, templates/training.html, routes/training.py]
tech_stack:
  added: []
  patterns: [chart-js-line-chart, dynamic-dom-injection, claude-sonnet-scoring]
key_files:
  created: []
  modified:
    - services/training_service.py
    - templates/training.html
    - routes/training.py
decisions:
  - "generate_scoring() punkte field is 10 + bonus(gesamt-50) — separate from punkte_verdient which applies modus/hilfe penalty; both coexist"
  - "Chart containers injected as placeholder divs in buildScoring() HTML string, rendered after by renderMoodChart()/renderWendepunkteDetail() — avoids timing issues with DOM"
  - "wendepunkte_detail and Beziehungsaufbau are only added to prompt when stimmung_history is present — backward compatible for old sessions"
  - "max_tokens increased from 1200 to 1500 for generate_scoring() — wendepunkte_detail adds ~200 tokens to output"
metrics:
  duration: "5 min"
  completed_date: "2026-04-10"
  tasks_completed: 2
  files_modified: 3
---

# Phase 04.9 Plan 05: Scoring Upgrade — Mood Curve, Wendepunkt-Analyse, 6th Category Summary

Enhanced training scoring with 6th Beziehungsaufbau category (D-14), Chart.js mood curve (-5 to +5 scale) with red wendepunkt markers (D-15), prominent Wendepunkt-Analyse feedback blocks with "Du hast gesagt / Problem / Besser waere" structure (D-17), and basis points awarded per D-16.

## What Was Built

### Task 1: generate_scoring() upgrade

**services/training_service.py:**
- Added `stimmung_history: list = None` parameter to `generate_scoring()` signature
- When `stimmung_history` is provided: injects stimmung context into Claude prompt, adds 6th "Beziehungsaufbau" category (D-14), adds `wendepunkte_detail` to expected JSON output
- When `stimmung_history` is None: prompt is identical to before — fully backward compatible (5 categories, no wendepunkte_detail)
- Added `punkte` field to returned dict: `10 + max(0, gesamt_score - 50)` per D-16
- Increased `max_tokens` from 1200 to 1500 for the larger scoring response

**routes/training.py:**
- `training_end()` now passes `stimmung_history=session.get('stimmung_history')` to `generate_scoring()` using keyword arguments

### Task 2: Scoring UI upgrade

**templates/training.html:**
- `buildScoring()` injects `<div id="t-mood-chart-container">` with `<canvas id="t-mood-chart">` and `<div id="t-wendepunkte-detail">` as placeholder divs before the category cards in the scoring innerHTML
- Added `renderMoodChart(stimmungHistory)`: Chart.js line chart, y-axis -5 to +5, teal line (#00D4AA), mood drops >= 2 marked with red (#FC8181) larger dots (radius 8), `Chart.getChart()` destroy guard before re-render
- Added `renderWendepunkteDetail(wendepunkte)`: renders each wendepunkt as a card with red (#FC8181) left border, "Du hast gesagt" / "Problem:" / "Besser waere:" sections in teal (#48BB78) green
- `confirmEndTraining()` calls `renderMoodChart(data.stimmung_history)` and `renderWendepunkteDetail((data.scoring || {}).wendepunkte_detail)` after `buildScoring(data)` and `showPhase('scoring')`

### Task 3: Visual verification checkpoint

**⚡ Auto-approved** (auto-mode active)

## Commits

| Task | Commit | Files |
|------|--------|-------|
| Task 1 + Task 2 | e2aa999 | services/training_service.py, routes/training.py, templates/training.html |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Enhancement] Tasks 1 and 2 committed together**
- Both tasks modify tightly coupled code (scoring prompt + UI that renders scoring). Combined into single commit to avoid partial state.

**2. [Rule 2 - Enhancement] Chart canvas wrapped in position:relative container**
- Plan showed `<canvas id="t-mood-chart" height="180">` directly. Added a `position:relative; height:180px` wrapper div around the canvas — required for Chart.js `maintainAspectRatio:false` to correctly constrain chart height inside the flex layout.

**3. [Rule 1 - Note] Existing punkte_verdient logic preserved**
- routes/training.py already calculates `punkte_verdient` with modus bonus (free = 1.5x) and hilfe penalty. The new `scoring.punkte` from generate_scoring() (10 + bonus) is an additional field — the frontend still displays `punkte_verdient` from the route in the modus motivation block. Both coexist cleanly; no duplication or conflict.

## Known Stubs

None — all elements are fully wired:
- `t-mood-chart-container` rendered by `renderMoodChart(data.stimmung_history)` from `/training/end` response
- `t-wendepunkte-detail` rendered by `renderWendepunkteDetail((data.scoring||{}).wendepunkte_detail)` from Claude scoring
- `scoring.punkte` field populated by `generate_scoring()` when called with stimmung_history

## Threat Surface Scan

No new network endpoints. Changes are internal to existing `/training/end` endpoint:
- T-04.9-12 (Tampering/scoring output): `wendepunkte_detail` and `Beziehungsaufbau` fields are optional in frontend — missing fields render nothing (null-safe checks throughout). JSON parsed with existing try-except in generate_scoring(). Mitigated.
- T-04.9-13 (Info Disclosure/wendepunkte_detail): Contains only conversation analysis scoped to user's own session. No PII. Accepted.

## Self-Check: PASSED

- `services/training_service.py` `stimmung_history` param — VERIFIED (python inspect check passed)
- `services/training_service.py` `Beziehungsaufbau` in prompt — VERIFIED
- `services/training_service.py` `wendepunkte_detail` in prompt — VERIFIED
- `services/training_service.py` `punkte` field — VERIFIED
- `templates/training.html` `t-mood-chart` — VERIFIED (python content check passed)
- `templates/training.html` `t-mood-chart-container` — VERIFIED
- `templates/training.html` `t-wendepunkte-detail` — VERIFIED
- `templates/training.html` `renderMoodChart` — VERIFIED
- `templates/training.html` `renderWendepunkteDetail` — VERIFIED
- `templates/training.html` `Chart.getChart` — VERIFIED
- `routes/training.py` `stimmung_history` parameter — VERIFIED
- Commit e2aa999 exists — VERIFIED
