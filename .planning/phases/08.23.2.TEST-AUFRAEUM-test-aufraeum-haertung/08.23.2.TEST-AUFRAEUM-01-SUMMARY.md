---
phase: 08.23.2.TEST-AUFRAEUM-test-aufraeum-haertung
plan: 01
status: code-complete-pending-deploy-gate
executed: 2026-07-07
files_modified:
  - tests/conftest.py
commits:
  - 75f6bb7  # Task 1 — scoped DISABLE/ENABLE trigger bypass (Cause A)
  - 5a3e53d  # Task 3 — coach_id cut + docstring fix + crm doc (Cause C+D)
  - a3b7e4b  # Task 5 — snapshot except -> WARN (Cause E)
---

# SUMMARY — 08.23.2.TEST-AUFRAEUM Plan 01 (Test-Cleanup-Haertung)

**Status:** Code-complete, 3 atomische Commits auf `main`, `git diff` zeigt AUSSCHLIESSLICH
`tests/conftest.py`. **Deploy-Gate-Acceptance NOCH OFFEN** — der `bash deploy.sh production`
pytest-Gate (die einzige gueltige Acceptance, HART Kein-Local-Dev) wird von André **supervised**
gefahren (STOP-vor-Deploy). Lokal nur Syntax (`py_compile` OK) + Plan-Acceptance-Greps (alle grün) —
**kein Acceptance**.

## Pre-Execute-Diskrepanz-Check (Plan „Bei Diskrepanz: STOP + Replan")

Alle vier Code-Anker gegen die Verbindungs-Karte v2 live-verifiziert — **KEINE Diskrepanz**:
- `text` auf Modulebene importiert (`conftest.py:7`), nicht `_sa_text` (lokal in `_fk_safe_delete_rows`). ✓
- try/except-Block im Guard exakt wie im Plan (ex-`conftest.py:626-643`). ✓
- FALSCHE Docstring exakt wie beschrieben (ex-`conftest.py:166-170`). ✓
- Stiller Snapshot-`except: continue` exakt wie beschrieben (ex-`conftest.py:393-395`). ✓
- `_CLEANUP_FK_ORDER`: `public.tenant_orgs` (Z.131) VOR `public.organisations` (Z.132) — Order korrekt.

## Cause A (Task 1) — audit_log Trigger-Bypass

Der Guard-Auto-Delete ist jetzt in **scoped DISABLE → (DELETE) → ENABLE** gewickelt (drei getrennte
committete TX, ENABLE im `finally`). `has_audit_log = 'public.audit_log' in leaked_by_tbl` gated
DISABLE/ENABLE auf betroffenes audit_log (kein Overhead sonst). Greps: DISABLE==1, ENABLE==1,
has_audit_log-Guard==1. **Gate-Lauf-Beleg (null audit_log + null orgs/users [BASELINE-AUTO-FIX]) +
Prod-tgenabled='O': OFFEN — im supervised deploy.sh-Gate zu verifizieren.**

## Cause B (Task 2) — FK-Order tenant_orgs-vor-organisations: VERIFY-ONLY, kein Code-Change

Order verifiziert **korrekt**, KEIN Code-Change: `_CLEANUP_FK_ORDER` hat `public.tenant_orgs` (Z.131)
vor `public.organisations` (Z.132); `_DERIVED_FK_ORDER` stammt aus `derive_baseline_tables`
(FK-Graph-Traversierung Kind-vor-Eltern) — `tenant_orgs.legacy_org_id -> organisations` macht
tenant_orgs zum Kind → kommt vor der Eltern-Tabelle. Kein Post-Sort-Override eingesetzt (waere toter
Code, Punkt 27). **Truth #2 („null Mutual-FK-Warnung") ist durch TASK 1 (Trigger-Kaskade) erfuellt,
NICHT durch Task 2 (verify-only).**

## Task 3 — coach_id-Zyklus + Docstring + crm-DOKU

- **Teil 1 (coach_id-Kappung):** `UPDATE public.organisations SET coach_id = NULL WHERE id::text = ANY(...)`
  im `if 'public.organisations' in leaked_by_tbl:`-Zweig, EIGENE committende TX, VOR dem Delete-Block.
  Nur coach_id, nur geleakte org-ids — kein generisches FK-Nulling (Gemini). Grep: `UPDATE public.organisations
  SET coach_id = NULL`==1. Heute No-Op (register/login setzt kein coach_id); `coach.py:105` instanziiert
  den Zyklus kuenftig real.
- **Teil 2 (Docstring):** FALSCHE Zeile („ein echter Hard-Stall ... ohne DEFERRABLE gar nicht einfuegbar")
  entfernt und durch die korrekte Beschreibung ersetzt (echter Zyklus IST ohne DEFERRABLE einfuegbar,
  Retry loest ihn nicht → Kante muss vor dem Delete gekappt werden). Grep: false-Zeile==0.
- **Teil 3 (crm-DOKU):** `CRM-CROSS-SCHEMA-BLOCKER-KLASSE`-Absatz in die D-G04-SCOPE-Notiz aufgenommen;
  referenziert den fail-closed `deploy.sh` POST-SUITE-crm-leak-Check (`scripts/_crm_leak_count.py`).
  **crm ist DOKUMENTIERT + fail-closed durch deploy.sh POST-SUITE abgesichert, NICHT in-suite abgeraeumt
  (Runde-2-Trim: doppelt-latent + in-suite-Clearing waere schaedlich — wuerde den Backstop zu warn-only
  degradieren).** Grep: `CRM-CROSS-SCHEMA-BLOCKER-KLASSE`==1, `DELETE FROM crm`==0, kein neues
  `set_config('app.tenant_id'` im Guard.

## Task 5 — Snapshot-except laut

Stiller `except Exception: continue` in `_snapshot_public_tables` auf `[BASELINE-SNAPSHOT]`-WARN gehoben;
`continue`-Verhalten (best-effort) bleibt. Grep: `BASELINE-SNAPSHOT`==1.

## Prod-Unversehrtheit

`git diff` zeigt AUSSCHLIESSLICH `tests/conftest.py` — keine Prod-Migration, kein `deploy.sh`-Zuwachs,
kein `_schema_introspect.py`-Change, kein `alembic/`. Punkt 17 gewahrt (kein Guard-/Traversierungs-Rewrite,
nur gezieltes ADD-Handling + Doku-Notiz).

## OFFEN (supervised, André) — Deploy-Gate = Acceptance

1. `bash deploy.sh production` — der eingebaute pytest-Gate (`-m "not live and not perf"` gegen nerve_test)
   MUSS gruen laufen mit NULL `[BASELINE-AUTO-FIX]`-Zeilen fuer audit_log/organisations/users/tenant_orgs.
2. `tests/test_audit_log_immutable.py` bleibt gruen (Trigger feuert — kein False-Green).
3. Prod-Trigger byte-identisch: `sudo -u postgres psql -d nerve -c "SELECT tgenabled FROM pg_trigger
   WHERE tgname='trg_audit_log_immutable'"` → `O`.
4. deploy.sh POST-SUITE crm-leak-Check bleibt fail-closed gruen (crm.* == 0).
