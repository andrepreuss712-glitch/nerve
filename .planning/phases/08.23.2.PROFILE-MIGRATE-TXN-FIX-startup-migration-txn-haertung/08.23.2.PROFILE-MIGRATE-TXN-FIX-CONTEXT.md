# 08.23.2.PROFILE-MIGRATE-TXN-FIX — CONTEXT (locked decisions, REPLANNED via --reviews 2026-07-13)

**Promoted from Backlog `STARTUP-BATCH-MIGRATION-TXN` 2026-07-13 (André).** Cross-AI (Fable + Gemini) durch → **BLOCKER + Nachbesserungen** → dieser Replan. Siehe `…-REVIEWS.md`. Cross-AI **PFLICHT** war erledigt; nach diesem Replan: kurze Re-Review auf die neue Deploy-/Test-Naht, dann Execute (🔴 Startup-Migration + TX-Semantik).

## ★ KORRIGIERTE Prämisse (Cross-AI BLOCKER, Code+Prod-verifiziert)

**Der v4-Batch (app.py:989-1028) läuft auf Postgres NIE.** Er liegt INNERHALB `_migrate()`, und `_migrate()` returned auf PG früh (app.py:140-142). Live-Beleg: „_migrate skipped on Postgres" + keine „[Schema] Batch-Migration"-Zeile. **Die ursprüngliche Prämisse „v4-Batch läuft vor _migrate_profile_json auf Prod" war FALSCH.** Auf Prod laufen als Startup-Migration NUR:
- `_data_migrate()` (app.py:1361-1374, Aufruf :1376) — kein PG-Skip.
- `_migrate_profile_json()` (app.py:1379, Aufruf :2039) — kein PG-Skip; Advisory-Lock 819 (Multi-Worker).

## Root-Cause (Fable, verifiziert)

`_migrate_profile_json()` fährt bei jedem Start `ALTER TABLE profile_opener ADD COLUMN type …` (:1423) in `try/except: pass` **OHNE rollback**. Die Spalte `type` existiert (models.py `ProfileOpener.type` server_default='opener' → jede create_all-DB inkl. US-Umzug hat sie; Prod-verifiziert) → **DuplicateColumn** vergiftet die Postgres-Transaktion → Folge-Statements (ProfileOpener-sync :1454, UPDATE :1504) sterben still als `InFailedSqlTransaction`. Sichtbar nur bei einem versions-losen Profil. Postgres-only. Fingerabdruck `"Profile ?"` im Log.

## ★ Cross-Layer-Fund (Punkt 21/22)
`profiles` hat KEINE Spalte `schema_version` — die Version lebt AUSSCHLIESSLICH im `daten`-JSON (`daten` = Text, kein JSONB). Guard (a) MUSS `schema_version` aus dem `daten`-JSON parsen (`SELECT daten` → `json.loads`), NICHT via Spalten-SELECT / `daten::json->>`.

## ★ Prod-Realität (Claudian, Punkt 21): v2/v3-Profile stecken fest
`_migrate_profile_json` skipt `schema_version >= 2` (:1446) → verarbeitet NUR versions-lose/v1-Profile. Da der v4-Batch (der v3→v4 macht) auf PG tot ist, werden **v2/v3-Profile auf Prod NIE auf v4 gehoben**. Guard (a) macht das sichtbar (s.u.); das echte Heben ist Teil der DEFERRED Konsolidierung.

## Scope = REFRAME auf die 2 prod-laufenden Routinen (LOCKED, post-Review)

1. **`_migrate_profile_json` — Mine raus (TXN-01):** totes ALTER (:1421-1427) + Step B (:1429-1435) **ersatzlos** entfernen (NICHT `IF NOT EXISTS` — nerve_app hat auf Prod keine ALTER-Rechte, app.py:136 → IF NOT EXISTS scheitert an Permissions = leisere Mine).
2. **`_migrate_profile_json` — rollback-Hygiene (TXN-02):** `conn.rollback()` in JEDEN verbleibenden swallowing-except auf der conn (:1474, :1492, :1511). Das ist Entgiftung UND Pro-Profil-Isolation in einem (ein Fehler rollt nur sein Statement, die Schleife läuft weiter).
3. **`_data_migrate` — Beifang (TXN-03, Fable P7):** `conn.rollback()` in die 2 `except:pass` (:1367-1368, :1373-1374) — dieselbe Mine, kürzer.
4. **Guard (a) am PG-Pfad (TXN-04):** ans ENDE von `_migrate_profile_json` — Re-Check aller Profile, `schema_version` aus `daten`-JSON: version-los/v1 nach dem Lauf → CRITICAL „MIGRATION FAILED"; v2/v3 → WARN „stuck, siehe PROFILE-MIGRATE-CONSOLIDATE".
5. **Stale-Doku (TXN-05):** Docstring (:1380-1385) + Print (:1509) „schema_version=2" → auf das reale v4-Verhalten korrigieren. + 1-Zeilen-Marker am toten v4-Batch, dass er auf PG nie läuft (Verweis Konsolidierung).
6. **CLAUDE.md-Regel (TXN-06):** „nie `except:pass`/stiller except auf einer PG-conn/session ohne rollback". Der **grep-Wächter ist RAUS** (AST-Analyse, kein grep — over-engineered, beide AIs) → Backlog `TXN-ROLLBACK-GUARD-AST`.
7. **Erst-Rot-Wächter (TXN-07):** real-PG gegen `_migrate_profile_json` (NICHT den toten v4-Batch). Rot gegen HEAD (DuplicateColumn-Poison → v1-Profil bleibt hängen), grün nach Fix. Fixture-Import-Reihenfolge beachten (conftest patcht `dbmod.engine` VOR `import app`).

## DROPPED aus aktivem Scope (post-Review, dokumentiert)
- **Guard (b) (v4→älter-Ping-Pong):** auf Prod ein PHANTOM — kein konkurrierender v4-Batch; Multi-Worker-`_migrate_profile_json`-gegen-sich ist via Advisory-Lock 819 + `:1446`-Skip idempotent (beide Läufe produzieren v4 = benign). Punkt 27 → DROP. Die optionale **CAS-UPDATE** (`… WHERE daten=:snapshot`, Fable P4/Gemini) ist damit moot.
- **v4-Batch `begin_nested`-Savepoint (Task 3 alt):** dead-on-PG → RAUS. (Warnung für die Konsolidierung: falls je in einem PG-Pfad reaktiviert, im except NUR loggen+continue, NIEMALS `_db.rollback()` — das rollt die Außen-TX + verwirft alle migrierten Profile, Fable-Fund 1b.)

## DEFERRED (Backlog `PROFILE-MIGRATE-CONSOLIDATE`, NICHT diese Phase)
Die zwei Pfade konsolidieren (Opener-Sync transaktionssicher in EINEN lebenden PG-Pfad ziehen, `_migrate_profile_json` löschen, v2/v3→v4 auf Prod tatsächlich heben) — behebt auch den stillen Opener/Pitch-Datenverlust. Guard (a) macht die v2/v3-Lücke bis dahin sichtbar.

## Verify=Production (CLAUDE.md HART)
Deploy-Gate (server-side pytest nerve_test — Poison ist PG-only, SQLite wäre False-Green) + Live-Verify (Claudian): Restart → `journalctl` KEIN `[Schema]`/`InFailedSqlTransaction`/`"Profile ?"` mehr. inspect.sh read-only für profile_opener-Schema + Profile-Sample.
