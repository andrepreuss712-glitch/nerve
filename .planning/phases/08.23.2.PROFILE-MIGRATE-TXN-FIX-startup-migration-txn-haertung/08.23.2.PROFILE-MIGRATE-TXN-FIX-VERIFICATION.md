---
phase: 08.23.2.PROFILE-MIGRATE-TXN-FIX
verified: 2026-07-14T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 08.23.2.PROFILE-MIGRATE-TXN-FIX Verification Report

**Phase Goal:** Härtet die auf Prod laufenden Startup-Profil-Migrationsroutinen (`_migrate_profile_json` + `_data_migrate`) gegen die Postgres-Transaktions-Vergiftung (totes `ALTER TABLE profile_opener ADD COLUMN type` → DuplicateColumn/Permission in `except: pass` ohne rollback → `InFailedSqlTransaction`-Kette → versions-lose Profile bleiben unmigriert).
**Verified:** 2026-07-14
**Status:** passed
**Re-verification:** No — initial verification (no prior VERIFICATION.md in phase dir)

## Goal Achievement

### Observable Truths (from PLAN must_haves.truths)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | Start vergiftet keine PG-Transaktion mehr — totes `ALTER TABLE profile_opener ADD COLUMN type` (+ Step B) ERSATZLOS raus | ✓ VERIFIED | `grep "ADD COLUMN type" app.py` = **0 Treffer**. app.py:1440-1450 = Löschmarker-Kommentar an Stelle der ex-Mine; kein `IF NOT EXISTS`-Rest. Kein ALTER auf profile_opener im Body. |
| 2 | Kein swallowing conn-except in `_migrate_profile_json` ODER `_data_migrate` ohne `conn.rollback()` | ✓ VERIFIED | `_migrate_profile_json`: rollback in allen 3 sync/UPDATE-excepts (app.py:1489, 1508, 1529) + Guard-a-except (1549). `_data_migrate`: beide ex-`except:pass` jetzt `conn.rollback()` (app.py:1381, 1387). Advisory-Lock/SELECT-excepts `return` sauber (keine conn-Weiterverwendung, 1425/1431/1437). |
| 3 | Guard (a) am Ende `_migrate_profile_json` loggt <LATEST-Profile: v1/versions-los = CRITICAL, v2/v3 = WARN/stuck | ✓ VERIFIED | app.py:1532-1550. `_cv <= 1` → `[Schema] CRITICAL MIGRATION FAILED …` (1545); `_cv < _LATEST` → `[Schema] WARN … stuck` (1547). |
| 4 | `profiles` hat KEINE schema_version-Spalte — Guard liest schema_version aus dem daten-JSON | ✓ VERIFIED | Guard-a liest `SELECT id, daten FROM profiles` (app.py:1539) + `json.loads(...).get('schema_version')` (1541). `grep "SELECT schema_version FROM profiles" app.py` = **0**. |
| 5 | Alle Änderungen zielen auf PROD-laufende Routinen; v4-Batch PG-tot, unverändert außer 1-Zeilen-Marker | ✓ VERIFIED | `_migrate()` PG-early-return app.py:133ff; v4-Batch (app.py:989-995) trägt nur `# HINWEIS (TXN-05): laeuft auf Postgres NIE … Dieser Batch bleibt UNVERAENDERT (PG-tot)` — keine Logik-Änderung. |

**Score:** 5/5 truths verified

### Required Artifacts (from PLAN must_haves.artifacts)

| Artifact | Expected (`contains`) | Status | Details |
| -------- | --------------------- | ------ | ------- |
| `app.py` | `conn.rollback()` | ✓ VERIFIED | Mine raus + rollback in jedem conn-except (`_migrate_profile_json` 3× + Guard-a + `_data_migrate` 2×), Guard-a, Stale-Doku-Fix, v4-Batch-Marker. AST-parse OK. |
| `tests/test_profile_migrate_txn.py` | `_migrate_profile_json` | ✓ VERIFIED | Existiert; 2 real-PG-Verhaltenstests importieren + rufen `_migrate_profile_json()` (Zeilen 66, 100). Substantiv (128 Z., DB-Insert/Read-Assertions), gewired (Test-Gate-Suite). AST-parse OK. |
| `CLAUDE.md` | `InFailedSqlTransaction` | ✓ VERIFIED | Neue DB-Regel „Nie stiller except auf einer PG-Connection/Session ohne rollback" enthält `InFailedSqlTransaction` + `rollback()`-Pflicht + AST-Guard→Backlog-Verweis. |

### Key Link Verification (from PLAN must_haves.key_links)

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `_migrate_profile_json` ALTER | keine DuplicateColumn-Vergiftung | totes ALTER + Step B ersatzlos entfernt | ✓ WIRED | `pattern: profile_opener` — kein `ALTER … profile_opener ADD COLUMN` mehr; Löschmarker app.py:1440. Nachfolgende opener/erlaubnis-Sync-INSERTs (1476/1483/1502) und finales UPDATE (1521) laufen auf entgifteter conn. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `_migrate_profile_json` | profiles.daten (schema_version) | `SELECT id, daten … FROM profiles` + `_mpd()` UPDATE | Ja — v1→v4 UPDATE mit echtem `_json.dumps` (app.py:1520-1524) | ✓ FLOWING |
| Guard (a) | `_cv` je Profil | `SELECT id, daten FROM profiles` re-read (1539) | Ja — echte Re-Query nach Lauf, JSON-parse | ✓ FLOWING |
| profile_opener sync | is_personalized | roher INSERT (1476/1483/1502) | Ja — TXN-09 liefert `is_personalized=false` explizit (NOT NULL, kein server_default, models.py:203) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| app.py/profile_schema.py/test parse | `python -c "import ast; ast.parse(...)"` (×3) | `SYNTAX OK` | ✓ PASS |
| Erst-Rot-Guard rot→grün, Full-Suite | Production-Deploy-Gate (real-PG nerve_test, supervised 2026-07-14) | Guard rot gegen pre-fix, dann grün; **944 passed** | ✓ PASS (authoritative runtime evidence) |
| Live-Restart sauber | journalctl since restart (supervised) | Kein `[Schema]`-Error / `InFailedSqlTransaction` / `"Profile ?"` / NotNullViolation / CRITICAL; login 302 | ✓ PASS (authoritative runtime evidence) |

_Local pytest/deploy NOT re-run (CLAUDE.md HART „Kein Local-Dev"). Runtime pass/fail taken from the already-passed supervised Production gate; code-level verification via Read/Grep + AST-parse only._

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| TXN-01 | 01 | Totes ALTER + Step B ersatzlos raus | ✓ SATISFIED | `grep "ADD COLUMN type" app.py`=0; Marker app.py:1440-1450 |
| TXN-02 | 01 | rollback in jedem conn-except von `_migrate_profile_json` | ✓ SATISFIED | app.py:1489, 1508, 1529, 1549 |
| TXN-03 | 01 | rollback in beide `_data_migrate`-excepts | ✓ SATISFIED | app.py:1381, 1387 |
| TXN-04 | 01 | Guard (a) am Funktions-Ende (CRITICAL/WARN, JSON-Read) | ✓ SATISFIED | app.py:1532-1550 |
| TXN-05 | 01 | Stale „schema_version=2"-Doku → v4/LATEST; v4-Batch-Marker | ✓ SATISFIED | app.py:1395, 1526-1527, 990-992; profile_schema.py:256-260, 274 |
| TXN-06 | 01 | CLAUDE.md-Regel „kein stiller except auf PG-conn ohne rollback"; grep-Guard→Backlog | ✓ SATISFIED | CLAUDE.md DB-Regel (InFailedSqlTransaction+rollback); backlog.md:41 `TXN-ROLLBACK-GUARD-AST` |
| TXN-07 | 01 | Erst-Rot real-PG-Wächter gegen `_migrate_profile_json` (≥2 Tests, valid runtime) | ✓ SATISFIED | test_profile_migrate_txn.py, 2 Tests, DB-Insert/Read-Assertions, cleanup_rows, keine Source-Presence-Muster |
| TXN-08 | 01 | rollback in die 2 zufällig-entschärften Verletzer (Heiler-BootSweep + `_seed_ewb_scenarios`), owns/close unverändert | ✓ SATISFIED | `_db_boot.rollback()` app.py:2317; `_seed_ewb_scenarios` echter `db.rollback()`+`raise` app.py:1340-1344; `if owns:` (1302/1346) unverändert |
| TXN-09 | 01 (gap-closure, `5c2ee37`) | is_personalized in alle 3 opener-sync raw INSERTs (poison-maskierter NotNullViolation) | ✓ SATISFIED | app.py:1476/1483/1502 liefern `is_personalized … false`; models.py:203 `nullable=False, default=False` (kein server_default) bestätigt Root-Cause |

Keine orphaned Requirements: REQUIREMENTS.md-Datei nicht vorhanden für diese Phase; alle 8 PLAN-IDs + TXN-09 durch Code belegt.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| services/profile_schema.py | 11-14 | Modul-Exports-Header-Docstring sagt weiterhin „v1 -> v2 -> v3 upgrade … gibt schema_version=3 zurueck" (Verhalten geht bis v4) | ℹ️ Info | Rein kosmetischer Doku-Rest. PLAN/Fable-P5d zielten explizit auf den `_migrate_profile_data`-Docstring (:256-262) — der IST auf v4 korrigiert (:258-260). Dieser zweite, im Modul-Header stehende stale Satz war nicht im PLAN-Scope. Kein Verhaltens- oder Goal-Impact. Optional-Nachzug (z.B. via PROFILE-MIGRATE-CONSOLIDATE). |

Test-Datei-Sweep: `grep "getsource|hasattr|open(|subprocess|pytest.mark.live|pytest.mark.perf|.count("` → **0 Treffer** (kein Source-Presence-False-Green, kein live/perf-Marker; entspricht CLAUDE.md Test-Qualitäts-Regel). Kein `TODO/FIXME/PLACEHOLDER`/leere-Handler-Muster in den geänderten Migrationsroutinen.

### Cross-AI / Locked-Decision Compliance (CONTEXT + REVIEWS)

| Locked Decision (REVIEWS) | Honored? | Evidence |
| ------------------------- | -------- | -------- |
| BLOCKER Reframe: Fix nur auf PG-laufende Routinen, v4-Batch-Änderungen raus | ✓ | Nur `_migrate_profile_json` + `_data_migrate` geändert; v4-Batch nur Marker |
| ALTER ersatzlos raus (nicht IF NOT EXISTS) — nerve_app ohne ALTER-Recht | ✓ | app.py:1446-1447 begründet; 0 ALTER-Treffer |
| Guard (a) an PG-Pfad statt tot in v4-Batch | ✓ | app.py:1532 am Ende `_migrate_profile_json` |
| Guard (b) Ping-Pong-Race als Phantom DROP (Punkt 27) | ✓ | Kein Guard-b/CAS-UPDATE im Code |
| grep-Wächter → Backlog `TXN-ROLLBACK-GUARD-AST` (kein grep-Guard in Phase) | ✓ | backlog.md:41; kein grep-Guard-Test |
| TXN-08 `_seed_ewb_scenarios`: echter rollback+re-raise, owns/close unverändert | ✓ | app.py:1340-1347 |

### Human Verification Required

Keine. Das autonomous:false Task-7 Production-Deploy-Gate ist bereits supervised gefahren und GRÜN (944 passed, Live-Restart clean, git_head 787b5a2 pushed) — authoritative runtime evidence, nicht erneut nötig.

### Gaps Summary

Keine goal-blockierenden Gaps. Alle 5 Observable Truths, alle 3 Artefakte (exists/substantive/wired/data-flowing), der Key-Link und alle 9 Requirements (TXN-01..09) sind code-belegt; das reale Production-Gate + Live-Verify sind bereits grün. Einziger Befund ist ein Info-Level Doku-Rest (profile_schema.py:11-14 Modul-Header, außerhalb des PLAN-Scopes) ohne Verhaltens- oder Zielwirkung.

---

_Verified: 2026-07-14_
_Verifier: Claude (gsd-verifier)_
