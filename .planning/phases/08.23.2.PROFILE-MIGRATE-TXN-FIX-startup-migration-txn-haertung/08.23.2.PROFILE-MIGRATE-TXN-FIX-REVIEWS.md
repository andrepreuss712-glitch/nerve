# Phase 08.23.2.PROFILE-MIGRATE-TXN-FIX — Cross-AI Review (Fable + Gemini)

**Reviewed:** 2026-07-13
**Reviewers:** Fable + Gemini (3 Sichten, Punkt 24 — Startup-Migration + TX-Semantik)
**Verdikt:** BLOCKER + Nachbesserungen → REPLANNED (dieser Replan). Alle Funde Code+Prod-verifiziert (Claudian).

---

## ★ BLOCKER (Fable) — der v4-Batch ist tot auf Postgres

Der v4-Batch (app.py:989-1028) liegt INNERHALB `_migrate()`, und `_migrate()` returned auf Postgres früh (app.py:140-142, `if db_url.startswith('postgresql'): … return`) → läuft auf Prod **NIE**. Live-Beleg: 15:54-Restart-Log „_migrate skipped on Postgres" + KEINE „[Schema] Batch-Migration"-Zeile. **Folge:** Task-3 (begin_nested-Savepoint) + Guard (a) im v4-Batch waren tote-Prod-Änderungen. Die CONTEXT-Prämisse „v4-Batch läuft vor _migrate_profile_json auf Prod" war FALSCH — auf PG läuft NUR `_migrate_profile_json` (:2039).

**Claudian-Verifikation (Code):** app.py:140-142 PG-return bestätigt; v4-Batch (993-1028) liegt in `_migrate()` → PG-dead. Bestätigt.

## Disposition der Funde (→ in diesen Replan eingearbeitet)

| # | Fund | Disposition |
|---|------|-------------|
| BLOCKER | v4-Batch tot auf PG | **Reframe:** Fix zielt NUR auf die 2 prod-laufenden Routinen (`_migrate_profile_json` + `_data_migrate`). v4-Batch-Änderungen (Task 3 begin_nested, Guard a dort) RAUS. |
| P7 (Fable) BEIFANG | `_data_migrate` (:1361-1374) läuft auf Prod (kein PG-Skip, :1376), gleiche `except:pass`-ohne-rollback-Mine (2×) | **NEU Task 3:** conn.rollback() in beide excepts (2 Zeilen). |
| P3 (Fable, decisive) | ALTER: RAUS statt defensiv behalten — nerve_app hat auf Prod keine ALTER-Rechte (app.py:136) → `IF NOT EXISTS` scheitert an Permissions = leisere Mine. `type` in models.py (server_default) → jede create_all-DB hat sie. | ALTER + Step B **ersatzlos raus** (nicht IF-NOT-EXISTS). |
| Guard (a) | im toten v4-Batch platziert | **An PG-Pfad verschoben:** ans Ende von `_migrate_profile_json`. |
| Guard (b) | Ping-Pong-Race ist auf Prod ein PHANTOM (kein konkurrierender v4-Batch; Multi-Worker via Advisory-Lock 819 + :1446-Skip idempotent) | **DROP aus aktivem Scope** (Punkt 27). Rationale in CONTEXT. CAS-UPDATE (P4/P9) damit moot. |
| Fund 1b (Fable) | falls begin_nested behalten: `_db.rollback()` im except rollt die Außen-TX + verwirft ALLE migrierten Profile | moot — begin_nested/v4-Batch-Änderung ist raus. Als Warnung für die spätere Konsolidierung notiert. |
| P-Doku | Docstring `_migrate_profile_json` (:1380-1385) + Print (:1509) sagen „schema_version=2" (stale, Verhalten ist v4) | Task 4: korrigieren. |
| Task 5b grep-Wächter | „except-ohne-rollback" ist AST-Analyse, kein grep → over-engineered (beide AIs) | **→ Backlog `TXN-ROLLBACK-GUARD-AST`.** CLAUDE.md-Regel bleibt. |
| P5 (Fable) PASS | Erst-Rot-Test (Task 1) ist korrekt | behalten; jetzt explizit gegen `_migrate_profile_json` ausgerichtet. Fixture-Import-Reihenfolge beachten (conftest patcht `dbmod.engine` VOR `import app`). |

## Zusätzlicher Claudian-Fund (beim Reframe, Punkt 21)
`_migrate_profile_json` SKIPT `schema_version >= 2` (:1446) → auf Prod (wo der v4-Batch tot ist)
werden **v2/v3-Profile NIE auf v4 gehoben**. Guard (a) macht genau das sichtbar (verbleibende
<LATEST nach dem Lauf) — und trennt „v1/versions-los nach Lauf = echter Migrations-Ausfall" von
„v2/v3 = bekannte Lücke des toten Batches → PROFILE-MIGRATE-CONSOLIDATE".

## Nächster Schritt
Nach diesem Replan: erneut kurz Cross-AI auf die neue Deploy-/Test-Naht (Reframe), dann
Claudian-Pre-Execute + Execute.

---

## Pre-Execute-Audit (Claudian + Fable-Re-Review, 2026-07-13) — BAUBEREIT

**Live-Check (Prod, read-only):** Step-B-Waisen = 0 (ALTER+Step-B-Löschen gefahrlos); alle 3
Prod-Profile bereits **v4** (kein v2/v3 → Guard a feuert aktuell nicht, nur latent); profile_opener
type-Verteilung sauber (10 opener / 3 pitch / 3 erlaubnis). → **baubereit.**

**3 kleine Beifänge in den Plan gefaltet (KEIN Replan):**
1. **TXN-01 (Fable P5a):** Lösch-Kommentar dokumentiert, dass das gelöschte ALTER der EINZIGE
   `type`-Nachrüster für Alt-SQLite war → per Kein-Local-Dev + create_all/models.py hat jede
   Prod-/create_all-DB die Spalte → Löschen sicher. → Task 2.
2. **TXN-05 (Fable P5d):** stale Docstring `services/profile_schema.py:256-262` mitkorrigieren
   (sagt „v1→v2→v3 … >=3 unverändert", Verhalten geht bis v4). → Task 4 + files_modified.
3. **TXN-08 (Fable P1, GSD-Entscheidung: INCLUDE):** `rollback()` in die 2 zufällig-durch-close()
   entschärften Regel-Verletzer — Heiler-BootSweep (app.py:~2276, `_db_boot.rollback()`) +
   `_seed_ewb_scenarios` (interne Session, rollback vor close ODER Caveat-Notiz). Rationale: die neue
   CLAUDE.md-Regel (TXN-06) soll ab Tag 1 keine bekannten Verletzer haben. Punkt 27: minimal, kein
   Refactor der owns/close-Mechanik. → neuer Task 6.

**Notiz für PROFILE-MIGRATE-CONSOLIDATE (deferred):** Advisory-Lock 819 ist xact-scoped (fällt beim
ersten commit) → theoretischer opener-Doppel-INSERT-Race; die Verschärfung gehört in CONSOLIDATE
(im Backlog vermerkt).

**Deploy:** supervised, Claudian fährt Zwei-Tore (Erst-Rot der Wächter gegen den ungefixten Stand → Grün).
