---
phase: 08.23.2.PROFILE-MIGRATE-TXN-FIX
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - services/profile_schema.py
  - tests/test_profile_migrate_txn.py
  - CLAUDE.md
  - .planning/backlog.md
autonomous: false
requirements: [TXN-01, TXN-02, TXN-03, TXN-04, TXN-05, TXN-06, TXN-07, TXN-08]
complexity: "🔴 komplex (Startup-Migration + Postgres-Transaktions-Semantik → Cross-AI PFLICHT; REPLANNED via --reviews)"

must_haves:
  truths:
    - "Der Start vergiftet keine Postgres-Transaktion mehr: das tote ALTER TABLE profile_opener ADD COLUMN type (Spalte existiert, nerve_app hat keine ALTER-Rechte) ist ERSATZLOS raus"
    - "Kein swallowing-except auf der conn in _migrate_profile_json ODER _data_migrate ohne conn.rollback()"
    - "Guard (a) am PG-laufenden Pfad (Ende _migrate_profile_json) loggt verbleibende <LATEST-Profile: v1/versions-los = MIGRATION FAILED, v2/v3 = stuck (Konsolidierung)"
    - "profiles hat KEINE schema_version-Spalte — Guard liest schema_version aus dem daten-JSON"
    - "Alle Änderungen zielen auf PROD-laufende Routinen (_migrate_profile_json + _data_migrate); der v4-Batch ist PG-tot und wird NICHT verändert (nur 1-Zeilen-Marker)"
  artifacts:
    - path: "app.py"
      provides: "Mine raus + rollback in jedem conn-except (_migrate_profile_json + _data_migrate) + Guard a + Doku-Korrektur"
      contains: "conn.rollback()"
    - path: "tests/test_profile_migrate_txn.py"
      provides: "Erst-Rot Regressions-Waechter gegen _migrate_profile_json: v1-Profil überlebt ohne InFailedSqlTransaction -> v4"
      contains: "_migrate_profile_json"
    - path: "CLAUDE.md"
      provides: "Regel: nie except:pass auf PG-conn/session ohne rollback"
      contains: "InFailedSqlTransaction"
  key_links:
    - from: "_migrate_profile_json ALTER (:1423)"
      to: "keine DuplicateColumn-Vergiftung"
      via: "totes ALTER + Step B ersatzlos entfernt"
      pattern: "profile_opener"
---

<objective>
Härtet die AUF PROD LAUFENDEN Startup-Profil-Migrationsroutinen gegen die Postgres-Transaktions-
Vergiftung, die bei jedem Start ein versions-loses Profil unmigriert lässt und `[Schema]`-Fehler wirft.

**★ REPLANNED via --reviews (Cross-AI BLOCKER, siehe REVIEWS.md):** Der v4-Batch (app.py:989-1028)
liegt in `_migrate()`, das auf Postgres früh returned (app.py:140-142) → läuft auf Prod NIE.
Der ursprüngliche Fix (begin_nested-Savepoint + Guard a im v4-Batch) war tote-Prod-Änderung.
**Reframe:** der Fix zielt auf die zwei PG-laufenden Routinen — `_migrate_profile_json` (:1379,
Aufruf :2039) und `_data_migrate` (:1361, Aufruf :1376).

**Root-Cause (Fable+Claudian, Code+Prod-verifiziert):** `_migrate_profile_json()` fährt bei jedem
Start `ALTER TABLE profile_opener ADD COLUMN type …` (:1423) in `try/except: pass` OHNE rollback.
Die Spalte `type` existiert (models.py `ProfileOpener.type` server_default) UND nerve_app hat keine
ALTER-Rechte → **DuplicateColumn / Permission-Error** vergiftet die conn → Folge-Statements
(ProfileOpener-sync :1454, UPDATE :1504) sterben still als `InFailedSqlTransaction`. Postgres-only.

**★ Cross-Layer (Punkt 21/22):** `profiles` hat KEINE Spalte `schema_version` (lebt im `daten`-JSON,
Text). Guard (a) liest JSON, kein Spalten-SELECT.

Purpose: kein vergifteter Start mehr, kein stiller Migrations-Ausfall, Regress sichtbar.
Output: app.py-Härtung (2 Routinen) + Erst-Rot-Waechter (gegen _migrate_profile_json) + CLAUDE.md-Regel.
Konsolidierung + v2/v3→v4-Heben DEFERRED (Backlog `PROFILE-MIGRATE-CONSOLIDATE`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

★ Multi-Segment-Phase-ID-Gotcha ([[project_nerve_multisegment_phase_ids]]): phase_dir HART =
`.planning/phases/08.23.2.PROFILE-MIGRATE-TXN-FIX-startup-migration-txn-haertung`.
SUMMARY = `08.23.2.PROFILE-MIGRATE-TXN-FIX-01-SUMMARY.md`. KEIN gsd-tools/gsd-sdk (nicht installiert)
→ plain `git`-Commits, Tracking manuell.

★ HART: Kein Local-Dev. Verify=Production. Tests im deploy.sh-Pytest-Gate auf nerve_test (real-PG;
die Vergiftung ist Postgres-only → SQLite wäre False-Green). Live-Verify = human checkpoint (autonomous:false).

★ Cross-AI-Runde 1 durch (BLOCKER → dieser Replan). NACH diesem Replan: kurze Re-Review auf die
neue Deploy-/Test-Naht (Reframe), DANN Claudian-Pre-Execute + Execute.
</execution_context>

<context>
@.planning/phases/08.23.2.PROFILE-MIGRATE-TXN-FIX-startup-migration-txn-haertung/08.23.2.PROFILE-MIGRATE-TXN-FIX-CONTEXT.md
@.planning/phases/08.23.2.PROFILE-MIGRATE-TXN-FIX-startup-migration-txn-haertung/08.23.2.PROFILE-MIGRATE-TXN-FIX-REVIEWS.md
@salesnerve/CLAUDE.md

<interfaces>
<!-- Alle per INHALT re-ankern (grep frisch, Zeilen driften). Anker 2026-07-13: -->

<!-- _migrate() PG-Skip (app.py:140-142): `if db_url.startswith('postgresql'): print(...); return`
     → ALLES in _migrate() (inkl. v4-Batch 989-1028) ist PG-tot. NICHT anfassen (nur Marker). -->

<!-- _data_migrate (app.py:1361-1374, Aufruf :1376) — LÄUFT auf Prod, 2× except:pass ohne rollback: -->
```python
with engine.connect() as conn:
    try:
        conn.execute(text("UPDATE organisations SET name='NERVE Alpha' WHERE name='SalesNerve Alpha'"))
        conn.commit()
    except Exception:
        pass            # :1367-1368  → conn.rollback() fehlt
    try:
        conn.execute(text("UPDATE organisations SET billing_email=… WHERE …"))
        conn.commit()
    except Exception:
        pass            # :1373-1374  → conn.rollback() fehlt
```

<!-- _migrate_profile_json (app.py:1379, Aufruf :2039) — LÄUFT auf Prod. Advisory-Lock 819 (:1410). -->
<!-- Die MINE (:1421-1435): Step A ALTER type (:1423, except:pass :1426) + Step B backfill (:1431, except:pass :1434). -->
<!-- conn-except OHNE rollback: opener-sync :1473-1474, erlaubnis-sync :1491-1492, UPDATE :1510-1511. -->
<!-- Idempotenz-Skip :1446 `if (_daten.get('schema_version') or 1) >= 2: continue` → NUR v1/versions-los. -->
<!-- _mpd (:1500) bringt auf v4; Print :1509 "schema_version=2" ist STALE. Docstring :1380-1385 auch. -->

<!-- ProfileOpener.type: database/models.py (server_default='opener') → existiert in jeder create_all-DB. -->
<!-- profiles: KEINE schema_version-Spalte — nur daten(Text-JSON). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (TXN-07): Erst-Rot Regressions-Waechter gegen _migrate_profile_json (real-PG)</name>
  <read_first>
    - app.py:1379-1511 (_migrate_profile_json — `grep -n "def _migrate_profile_json" app.py`)
    - app.py:1421-1435 (Mine), :1446 (Skip), :1500-1511 (UPDATE)
    - tests/conftest.py:791-838 (client-Fixture PG nerve_test; ★ patcht dbmod.engine VOR `import app` — Import-Reihenfolge im Test beachten, Fable P5), :240-310 (cleanup_rows)
    - `bash scripts/inspect.sh schema profile_opener` (Spalte type existiert auf nerve_test? — sonst zündet die Mine nicht = False-Green)
    - database/models.py Profile + ProfileOpener
  </read_first>
  <behavior>
    VALID Runtime-Verhalten (real-PG nerve_test, KEIN Source-Presence):
    - Test A (Kern, Erst-Rot): Insert EIN versions-loses Profil (daten OHNE schema_version, mit
      `opener`-Text). profile_opener.type existiert (Precondition). `_migrate_profile_json()` aufrufen.
      ERWARTET nach Fix: Profil auf v4 (`json.loads(daten)['schema_version'] == LATEST_SCHEMA_VERSION`)
      UND ProfileOpener-Row für das Profil (opener synced). GEGEN HEAD: die Mine (:1423) vergiftet die
      conn → sync/UPDATE sterben (InFailedSqlTransaction) → Profil bleibt versions-los → Test A ROT.
    - Test B (rollback-Isolation): ein Profil mit einem Sync-Fehler-Auslöser (falls billig
      konstruierbar) blockiert NICHT die Migration eines zweiten, gesunden Profils im selben Lauf
      (Loop läuft nach conn.rollback() weiter). Falls nicht billig deterministisch: mind. assert,
      dass zwei gesunde v1-Profile in EINEM `_migrate_profile_json()`-Lauf BEIDE v4 werden.
    - Kein `InFailedSqlTransaction`: der Aufruf wirft nicht + ein Folge-SELECT auf der Fixture-Session
      funktioniert.
    Cleanup via cleanup_rows (Profile + profile_opener), uuid-suffixed Namen. KEIN live/perf-Marker.
    ★ Import-Reihenfolge: `_migrate_profile_json` erst NACH der Fixture-Engine-Umbindung importieren
    (analog `from app import app` in conftest:815) — sonst bindet app an die falsche Engine.
  </behavior>
  <action>
    Neue Datei tests/test_profile_migrate_txn.py. `client`/`db_from_client`-Fixture (PG nerve_test).
    Versions-loses Profil: `Profile(org_id=…, name=f'migtxn-{uuid}', daten=json.dumps({'opener':'Hallo'}))`
    (KEIN schema_version). `from app import _migrate_profile_json` INNERHALB des Tests (nach Fixture).
    Assertion via frischem SELECT auf profiles.daten + profile_opener (COUNT WHERE profile_id).
    ★ Committe test ROT zuerst: `test(08.23.2.PROFILE-MIGRATE-TXN-FIX-01): ERST-ROT _migrate_profile_json txn-poison guard`.
    Doku im Commit: Test A rot gegen HEAD (DuplicateColumn vergiftet → Profil bleibt v1).
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: Test A/B nach Task 2 grün. Vor Task 2: Test A rot (dokumentiert).</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_" tests/test_profile_migrate_txn.py` zeigt >=2 Tests
    - `grep -ni "_migrate_profile_json\|schema_version\|InFailedSqlTransaction" tests/test_profile_migrate_txn.py` vorhanden
    - `grep -n "cleanup_rows" tests/test_profile_migrate_txn.py` vorhanden
    - KEIN getsource/open(/hasattr/grep auf Quelldateien; KEIN live/perf-Marker
  </acceptance_criteria>
  <done>v1-Profil überlebt _migrate_profile_json (→v4) ohne InFailedSqlTransaction; zweites gesundes Profil ebenfalls migriert; Test A rot gegen HEAD.</done>
</task>

<task type="auto">
  <name>Task 2 (TXN-01/02): _migrate_profile_json — Mine raus + rollback in jedem conn-except</name>
  <read_first>
    - app.py:1421-1435 (Step A/B — `grep -n "Add type column to profile_opener" app.py`)
    - app.py:1452-1474 (opener-sync), :1476-1492 (erlaubnis-sync), :1502-1511 (UPDATE) — die 3 conn-excepts
    - app.py:136 (nerve_app keine ALTER-Rechte — begründet „ersatzlos raus", nicht IF NOT EXISTS)
  </read_first>
  <action>
    (a) **TXN-01 Mine ersatzlos raus:** Step A (ALTER :1421-1427) + Step B (Backfill :1429-1435)
        LÖSCHEN. NICHT `IF NOT EXISTS` (nerve_app hat keine ALTER-Rechte → scheitert an Permissions =
        leisere Mine, Fable P3). Die Spalte existiert (models.py ProfileOpener.type). Kommentar:
        „TXN-01: totes ALTER (Spalte existiert, nerve_app ohne ALTER-Recht) entfernt — war die
        Transaktions-Mine (DuplicateColumn/Permission → InFailedSqlTransaction-Kette)."
        ★ Fable P5a im Lösch-Kommentar dokumentieren: dieses ALTER war der EINZIGE `type`-Nachrüster
        für eine Alt-SQLite-DB VOR Einführung der Spalte. Per „Kein Local-Dev" (Prod = create_all/PG) +
        `ProfileOpener.type` server_default (models.py) trägt JEDE Prod-/create_all-DB die Spalte bereits
        → Löschen gefahrlos. Pre-Execute-Audit (Claudian 2026-07-13): Step-B-Waisen=0, type-Verteilung
        sauber (10 opener / 3 pitch / 3 erlaubnis), alle 3 Prod-Profile bereits v4.
    (b) **TXN-02 rollback-Hygiene:** in JEDEN verbleibenden conn-except (opener-sync :1473-1474,
        erlaubnis-sync :1491-1492, UPDATE :1510-1511) `conn.rollback()` VOR dem print:
        ```python
        except Exception as _e:
            conn.rollback()   # TXN-02: conn nach verschlucktem Fehler nicht vergiftet lassen
            print(f"[Schema] Profil {_pid}: … failed: {_e}")
        ```
        Das ist Entgiftung UND Pro-Profil-Isolation (Fehler rollt nur sein Statement, Loop läuft weiter).
    Committe: `fix(08.23.2.PROFILE-MIGRATE-TXN-FIX-01): remove dead profile_opener ALTER mine + rollback in every conn except (TXN-01/02)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate: Test A grün (kein InFailedSqlTransaction mehr)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ADD COLUMN type VARCHAR" app.py` liefert 0 Treffer (Mine ersatzlos raus, KEIN IF NOT EXISTS)
    - `grep -cn "conn.rollback()" app.py` — in _migrate_profile_json >=3× (je verbleibendem except)
    - KEIN `except Exception` in _migrate_profile_json auf conn OHNE folgendes `conn.rollback()`
    - `grep -n "TXN-01\|TXN-02" app.py` vorhanden
  </acceptance_criteria>
  <done>Mine weg; jeder conn-except hat rollback; _migrate_profile_json vergiftet die Transaktion nicht mehr.</done>
</task>

<task type="auto">
  <name>Task 3 (TXN-03, Fable P7 Beifang): _data_migrate — rollback in beide except:pass</name>
  <read_first>
    - app.py:1361-1374 (_data_migrate — `grep -n "def _data_migrate" app.py`; Aufruf :1376, kein PG-Skip)
  </read_first>
  <action>
    In `_data_migrate` (:1364-1374) beide `except Exception: pass` um `conn.rollback()` ergänzen:
    ```python
    except Exception:
        conn.rollback()   # TXN-03: dieselbe Mine wie _migrate_profile_json (Fable P7)
    ```
    (Läuft auf Prod, kein PG-Skip — dieselbe except:pass-ohne-rollback-Klasse, kürzer.)
    Committe: `fix(08.23.2.PROFILE-MIGRATE-TXN-FIX-01): rollback in _data_migrate swallowing excepts (TXN-03, Fable P7)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün (kein Test-Bruch; Startup sauber)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TXN-03" app.py` vorhanden
    - in _data_migrate KEIN `except Exception: pass` mehr ohne `conn.rollback()`
  </acceptance_criteria>
  <done>_data_migrate lässt keine vergiftete conn zurück.</done>
</task>

<task type="auto">
  <name>Task 4 (TXN-04/05): Guard (a) am Ende von _migrate_profile_json + Stale-Doku-Korrektur</name>
  <read_first>
    - app.py:1436-1511 (Schleifen-Ende — Ziel für Guard a NACH der for-Schleife, VOR Funktions-Ende)
    - app.py:1380-1385 (Docstring), :1509 (Print) — stale „schema_version=2"
    - app.py:989-1028 (v4-Batch — nur 1-Zeilen-Marker „PG-tot", NICHT verändern)
    - services/profile_schema.py:255-262 (stale Docstring „v1->v2->v3 … >=3 unveraendert") + :271 (>=4 return) + LATEST_SCHEMA_VERSION (=4)
  </read_first>
  <action>
    (a) **TXN-04 Guard (a)** ans ENDE von `_migrate_profile_json` (nach der for-Schleife, innerhalb
        des `with engine.connect()`-Blocks): alle Profile erneut lesen, `schema_version` AUS DEM
        daten-JSON parsen (KEINE profiles.schema_version-Spalte!):
        ```python
        from services.profile_schema import LATEST_SCHEMA_VERSION as _LATEST
        for _cid, _cdaten in conn.execute(_text3("SELECT id, daten FROM profiles")).fetchall():
            try:
                _cv = (_json3.loads(_cdaten) if _cdaten else {}).get('schema_version') or 1
            except Exception:
                _cv = 1
            if _cv <= 1:
                print(f"[Schema] CRITICAL MIGRATION FAILED Profil {_cid}: schema_version={_cv} nach _migrate_profile_json")
            elif _cv < _LATEST:
                print(f"[Schema] WARN Profil {_cid}: schema_version={_cv} stuck (<{_LATEST}) — v4-Batch PG-tot, siehe PROFILE-MIGRATE-CONSOLIDATE")
        ```
        (Nutze `current_app.logger.critical/warning` falls im Modul-Kontext verfügbar; sonst print mit
        `[Schema] CRITICAL`/`[Schema] WARN`-Präfix.)
    (b) **TXN-05 Stale-Doku:** Print :1509 „migriert auf schema_version=2" → „migriert auf
        schema_version={aktuell/LATEST}" (Verhalten ist v4). Docstring :1380-1385 „schema_version=2"
        analog auf v4 korrigieren. + 1-Zeilen-Marker am v4-Batch (app.py:~989):
        „# HINWEIS: läuft auf Postgres NIE (_migrate() early-return :140) — nur SQLite/lokal.
        Migration auf Prod: _migrate_profile_json. Konsolidierung → Backlog PROFILE-MIGRATE-CONSOLIDATE."
        ★ Fable P5d: ZUSÄTZLICH die stale Docstring in `services/profile_schema.py:256-262` mitkorrigieren
        (sagt „v1 -> v2 -> v3 … Wenn >= 3: unveraendert" — das Verhalten geht bis v4; der `if version >= 4:
        return`-Guard :271 + der v3→v4-Block :410-446 belegen v4). Analog zu den app.py-Stale-Docs auf v4
        aktualisieren (Wert-Semantik, KEIN Code-Change an der Migration selbst).
    Committe: `fix(08.23.2.PROFILE-MIGRATE-TXN-FIX-01): regression guard (a) at _migrate_profile_json end + stale-doc fixes app.py+profile_schema.py (TXN-04/05)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün; Guard a loggt (falls <LATEST-Profile) ohne zu brechen</automated>
  </verify>
  <acceptance_criteria>
    - `grep -niE "MIGRATION FAILED|stuck" app.py` im Ende von _migrate_profile_json vorhanden (Guard a)
    - Guard a liest `SELECT id, daten` + json-parse (KEIN `SELECT schema_version FROM profiles`)
    - `grep -n "schema_version=2" app.py` — stale Print/Docstring korrigiert (0 irreführende Treffer)
    - `grep -ni "v1 -> v2 -> v3" services/profile_schema.py` — Docstring auf v4 korrigiert (kein stale >=3-Endzustand mehr)
    - `grep -n "PROFILE-MIGRATE-CONSOLIDATE\|_migrate() early-return\|Postgres NIE" app.py` (v4-Batch-Marker) vorhanden
    - `grep -n "TXN-04\|TXN-05" app.py` vorhanden
  </acceptance_criteria>
  <done>Guard a loggt verbleibende <LATEST-Profile (v1=CRITICAL, v2/v3=WARN/stuck, JSON-basiert); stale v2-Doku korrigiert; v4-Batch als PG-tot markiert.</done>
</task>

<task type="auto">
  <name>Task 5 (TXN-06): CLAUDE.md-Regel (grep-Wächter → Backlog)</name>
  <read_first>
    - CLAUDE.md (Regel-Nummerierung — nächster „Punkt N" bei den DB-/Test-Regeln)
  </read_first>
  <action>
    (a) **TXN-06 Regel** in CLAUDE.md (salesnerve) ergänzen:
        „**Nie `except: pass`/stiller except auf einer Postgres-Connection/Session ohne `rollback`.**
        Ein verschluckter Fehler auf einer laufenden Transaktion vergiftet sie → alle Folge-Statements
        sterben still als `InFailedSqlTransaction` (Postgres-only, auf SQLite unsichtbar). Jeder
        `except` auf einer `conn`/`session`, der den Fehler nicht re-raist, MUSS zuerst
        `conn.rollback()`/`session.rollback()` rufen. Anlass: PROFILE-MIGRATE-TXN-FIX 2026-07-13
        (totes ALTER → DuplicateColumn → InFailedSqlTransaction-Kette bei jedem Start)."
    (b) **grep-Wächter RAUS (Cross-AI):** „except-ohne-rollback" ist AST-Analyse, kein grep →
        over-engineered (Fable + Gemini). Als Backlog-Eintrag `TXN-ROLLBACK-GUARD-AST` in
        .planning/backlog.md vermerken (AST-basierter Wächter, wenn je gebaut). KEIN grep-Guard in dieser Phase.
    Committe: `docs(08.23.2.PROFILE-MIGRATE-TXN-FIX-01): CLAUDE.md rule — no silent except on PG conn without rollback + backlog AST-guard (TXN-06)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Doku-only, kein Test-Bruch</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ni "InFailedSqlTransaction" CLAUDE.md` vorhanden
    - `grep -ni "rollback" CLAUDE.md` im neuen Regel-Kontext vorhanden
    - `grep -n "TXN-ROLLBACK-GUARD-AST" .planning/backlog.md` vorhanden (grep-Guard vertagt)
  </acceptance_criteria>
  <done>Regel in CLAUDE.md; AST-grep-Wächter bewusst als Backlog vertagt (Cross-AI-Entscheidung).</done>
</task>

<task type="auto">
  <name>Task 6 (TXN-08, Fable P1): Regel-Konsistenz — rollback in die 2 zufällig-entschärften Verletzer</name>
  <read_first>
    - app.py:1333-1341 (_seed_ewb_scenarios-Aufruf-Wrapper :1337-1340 + das `if owns: db.close()` :1334-1335) — die interne Session sitzt IN `_seed_ewb_scenarios`; `grep -n "def _seed_ewb_scenarios" app.py` + deren Session-Handling lesen
    - app.py:2270-2280 (Heiler-BootSweep: `_db_boot` mit `except … as _e_bs: print(...)` :2276-2277 + `finally: _db_boot.close()`)
  </read_first>
  <action>
    ★ Diese 2 Stellen VERURSACHEN den aktuellen Bug NICHT (durch `close()` zufällig entschärft) — sie
    werden nur zur Konsistenz mit der NEUEN CLAUDE.md-Regel (TXN-06) gefixt, damit die Regel ab Tag 1
    keine bekannten Verletzer hat (+ kein False-Positive falls je der AST-Wächter kommt). Punkt 27:
    minimal, KEIN Refactor.
    (a) **Heiler-BootSweep (app.py:~2276):** im `except … as _e_bs:`-Block VOR dem print/finally ein
        `_db_boot.rollback()` ergänzen (direkter Session-Zugriff, sauber 1 Zeile).
    (b) **_seed_ewb_scenarios:** die Session, die im Fehlerfall NICHT zurückgerollt wird, absichern —
        in `_seed_ewb_scenarios` selbst ein `db.rollback()` im Fehlerpfad VOR `db.close()` (try/except
        ODER try/finally, je nach vorhandener Struktur; der Aufruf-Wrapper :1337-1340 bleibt als
        Call-Site-Guard). Falls die Funktion KEINEN sauberen 1-3-Zeilen-Rollback-Punkt hergibt
        (verschachtelt/ownership-`owns`-Logik), STATTDESSEN nur eine Regel-Caveat-Notiz an der Stelle
        setzen (`# TXN-08: rollback via close() entschärft; sauberer Rollback → PROFILE-MIGRATE-CONSOLIDATE`)
        und im SUMMARY vermerken — NICHT die owns/close-Mechanik umbauen (Über-Engineering-Grenze).
    Committe: `fix(08.23.2.PROFILE-MIGRATE-TXN-FIX-01): rollback in 2 accidentally-defused conn/session excepts for rule consistency (TXN-08, Fable P1)`.
  </action>
  <verify>
    <automated>bash deploy.sh production  # Pytest-Gate grün (kein Verhaltens-Change; nur rollback-Hygiene)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_db_boot.rollback()" app.py` vorhanden (Heiler-BootSweep gefixt)
    - `grep -n "TXN-08" app.py` vorhanden (im _seed_ewb_scenarios-Pfad: entweder rollback ODER dokumentierte Caveat-Notiz)
    - KEIN Umbau der `owns`/`close()`-Ownership-Mechanik (nur rollback ergänzt ODER Caveat notiert)
  </acceptance_criteria>
  <done>Beide zufällig-entschärften Verletzer rollback-konsistent zur neuen Regel (oder sauber als Caveat notiert); kein Refactor.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: Deploy + Live-Verify (Restart, journalctl sauber, kein Poison)</name>
  <what-built>
    Mine raus + rollback-Hygiene (_migrate_profile_json + _data_migrate) + Guard a + Stale-Doku +
    Erst-Rot-Waechter + CLAUDE.md-Regel. Alle committet + gepusht. Der v4-Batch (PG-tot) blieb
    unverändert (nur Marker). Erwartung: sauberer Start, kein versions-loses Profil hängt, kein Poison.
  </what-built>
  <how-to-verify>
    ★ Verify=Production (Claudian fährt). Reihenfolge:
    1. `bash deploy.sh production` — Pytest-Gate (real-PG nerve_test) grün: der Erst-Rot-Waechter
       (Test A) muss VOR den Fixes rot gewesen sein und ist jetzt grün.
    2. Service-Restart → `journalctl`/inspect.sh logs: KEIN `[Schema]`-Fehler, KEIN
       `InFailedSqlTransaction`, KEIN `"Profile ?"` mehr. Guard-a-Zeilen (falls <LATEST-Profile)
       erscheinen als bewusste WARN/CRITICAL (kein Crash).
    3. `bash scripts/inspect.sh sample profiles 20` (bzw. sudo -u postgres): versions-lose Profile
       sind auf v4 gehoben; verbleibende v2/v3 sind bekannt (Guard-a-WARN → Konsolidierung).
    André/Claudian bestätigt: Start sauber, kein Transaktions-Vergiftungs-Log, Guard a nur informativ.
  </how-to-verify>
  <resume-signal>Type "migrate-txn-live" wenn Start sauber (kein InFailedSqlTransaction/[Schema]-Fehler/"Profile ?"), versions-lose Profile v4 — oder beschreibe Abweichungen.</resume-signal>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `profiles` (LESEN daten-JSON in Guard a + Sync-Loop; SCHREIBEN daten via bestehendem UPDATE). **KEINE Schema-Änderung.**
- `profile_opener` (LESEN COUNT + SCHREIBEN INSERT im Sync; das tote ALTER auf `type` wird ENTFERNT). **KEINE Schema-Änderung** (Spalte `type` existiert, models.py server_default).
- `organisations` (SCHREIBEN in _data_migrate — bestehende Einmal-Renames, nur rollback ergänzt). **Keine Schema-Änderung.**

### Beleg (Executor zitiert via inspect.sh / models.py — NICHT raten)
```
profiles: id/org_id/name/branche/daten(Text-JSON)/… → KEINE Spalte schema_version. Version lebt im daten-JSON.
profile_opener.type: models.py ProfileOpener.type server_default='opener' → in jeder create_all-DB vorhanden.
  nerve_app OHNE ALTER-Recht (app.py:136) → ALTER = Permission/DuplicateColumn = Mine → ersatzlos raus.
_migrate() PG-early-return (app.py:140-142) → v4-Batch (989-1028) PG-tot, NICHT verändert.
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt / Leser-Voraus? (Punkt 26) | Verifiziert? |
|---|---|---|---|---|
| `schema_version` | gelesen in Guard a + :1446-Skip; geschrieben via daten-UPDATE | **JSON-Key IM `profiles.daten` (Text), NICHT Spalte** | Start-Zeit, PG: NUR _migrate_profile_json (v4-Batch tot). v1→v4; v2/v3 bleiben (Guard a WARN) | ✓ models.py + app.py:1009/1446 |
| `profile_opener.type` | totes ALTER (entfernt) + INSERT im Sync | DB-Spalte (server_default) | existiert → ALTER = Mine | ✓ models.py + app.py:136 |
| _data_migrate UPDATEs | 2× UPDATE organisations (Einmal-Rename) | organisations | Start-Zeit; rollback im except ergänzt | ✓ app.py:1361-1374 |

### Async-Naht (Punkt 26)
Auf PG läuft als Profil-Migration nur `_migrate_profile_json` (v4-Batch tot). Multi-Worker: Advisory-Lock 819 + `:1446`-Skip machen konkurrierende Läufe idempotent (Guard b als Race-Schutz war ein Phantom → DROPPED). Kein fremder Schreiber läuft dem Guard-a-Read voraus (läuft am Ende derselben Funktion).

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Startup-Migrationsprozess → profiles/profile_opener/organisations | Kein User-Input; Risiko = Selbst-Vergiftung der Transaktion + stiller Datenausfall. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-MIGTXN-01 | Denial of Service | totes ALTER → DuplicateColumn/Permission → InFailedSqlTransaction | mitigate | Mine ersatzlos raus (TXN-01) + rollback in jedem conn-except (TXN-02). |
| T-MIGTXN-02 | Tampering | ein Sync-Fehler blockiert Folge-Profile im Loop | mitigate | conn.rollback() je except (TXN-02) → Fehler isoliert, Loop läuft weiter. |
| T-MIGTXN-03 | DoS | _data_migrate vergiftet conn (2× except:pass) | mitigate | rollback (TXN-03, Fable P7). |
| T-MIGTXN-04 | Repudiation | stille Regression (Profil bleibt <LATEST) unbemerkt | mitigate | Guard a (TXN-04) am PG-Pfad: CRITICAL (v1) / WARN (v2/v3-stuck) aus daten-JSON. |
| T-MIGTXN-05 | DoS (Klasse) | künftiger except:pass auf PG-conn vergiftet erneut | mitigate | CLAUDE.md-Regel (TXN-06); AST-Wächter → Backlog. |
</threat_model>

<verification>
- Mine (ALTER :1423 + Step B) ersatzlos raus; kein IF NOT EXISTS.
- Jeder conn-except in _migrate_profile_json UND _data_migrate hat conn.rollback().
- Guard a am Ende von _migrate_profile_json: <LATEST-Profile geloggt (v1=CRITICAL, v2/v3=WARN), JSON-Read.
- v4-Batch unverändert, als PG-tot markiert; stale „v2"-Doku korrigiert.
- Erst-Rot-Waechter real-PG gegen _migrate_profile_json (Test A rot gegen HEAD, grün nach Fix); Import-Reihenfolge beachtet.
- CLAUDE.md-Regel gesetzt; grep-Wächter → Backlog.
- Live: Start sauber (kein InFailedSqlTransaction/[Schema]/„Profile ?"), versions-lose Profile v4.
</verification>

<success_criteria>
- Die 2 prod-laufenden Routinen vergiften keine Postgres-Transaktion mehr; kein versions-loses Profil bleibt hängen.
- Regression sichtbar (Guard a); profiles.schema_version-Nicht-Spalte respektiert (JSON-Read).
- v4-Batch (PG-tot) unverändert; kein Scope in totem Code.
- Cross-AI-Re-Review auf die Reframe-Naht vor Execute (🔴).
- Konsolidierung + v2/v3→v4-Heben sauber DEFERRED (Backlog PROFILE-MIGRATE-CONSOLIDATE).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.PROFILE-MIGRATE-TXN-FIX-startup-migration-txn-haertung/08.23.2.PROFILE-MIGRATE-TXN-FIX-01-SUMMARY.md`
</output>
