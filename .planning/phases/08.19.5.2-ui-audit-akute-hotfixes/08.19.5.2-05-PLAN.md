---
phase: 08.19.5.2
plan: "05"
type: execute
wave: 2
depends_on:
  - "08.19.5.2-02"
  - "08.19.5.2-03"
files_modified:
  - static/pip-launcher.js
  - templates/base.html
autonomous: true
requirements:
  - REQ-08.19.5.2-5
  - REQ-08.19.5.2-6

must_haves:
  truths:
    - "Nav zeigt 'Profile' (nicht 'Profil') in der Hauptnavigation"
    - "localStorage nerve_pip_was_active wird gesetzt wenn Call aktiv war bei pagehide"
    - "Re-Launch-Banner erscheint auf Zielseite wenn nerve_pip_was_active === '1'"
    - "Architekturentscheidung (PiP Re-Launch vs. seamless) ist im Code dokumentiert"
    - "SPEC.md Fallback-Acceptance erfuellt: Re-Launch-Flow implementiert + Begruendung dokumentiert"
  artifacts:
    - path: templates/base.html
      provides: "Nav-Label 'Profile' + PiP Re-Launch Banner"
      contains: "n-nav-label\">Profile"
    - path: static/pip-launcher.js
      provides: "localStorage nerve_pip_was_active in pagehide-Handler"
      contains: "nerve_pip_was_active"
  key_links:
    - from: "pip-launcher.js pagehide-Handler"
      to: "localStorage nerve_pip_was_active"
      via: "setItem bei aktivem state.micStarted"
      pattern: "nerve_pip_was_active"
    - from: "base.html inline script"
      to: "pip-relaunch-banner div"
      via: "localStorage check on page load"
      pattern: "pip-relaunch-banner"

cross_ai_finding_medium_3: |
  MEDIUM-3 (Cross-AI 2026-05-03): PiP Re-Launch State-Loss fuer v1 akzeptiert (SPEC-Fallback).
  Mic-Session wird bei Navigation unterbrochen — User muss neu starten via Banner.
  Dokumentieren in SUMMARY als akzeptiertes Verhalten. Kein seamless-PiP moeglich
  (window.documentPictureInPicture.requestWindow() erfordert User-Gesture).
  Block-J-Mini-Add: "PiP-State-Persistierung via activeSessionId in localStorage fuer spaetere Polish-Phase."
---

<objective>
Wave 2b — PiP Re-Launch-Flow + Nav-Label-Fix: Zwei einfache Fixes in denselben Files.

Fix A: Nav-Label "Profil" → "Profile" in base.html (2 Zeilen — title-Attribut + span-Inhalt)

Fix B: PiP Re-Launch-Flow via localStorage:
1. pip-launcher.js: Bei Navigation weg (pagehide) + activem Call → localStorage.setItem('nerve_pip_was_active', '1')
2. pip-launcher.js: Bei normalem Call-Ende → localStorage.removeItem('nerve_pip_was_active')
3. base.html: Inline-Script prueft beim Laden ob Flag gesetzt → zeigt Re-Launch-Banner
4. Banner-Klick: NerveLauncher.open() (User-Gesture) → PiP neu starten

Architektur-Entscheidung (dokumentiert im Code):
window.documentPictureInPicture.requestWindow() benoetigt User-Gesture — kein programmatischer PiP-Neustart
bei Navigation moeglich. Re-Launch-Flow ist bewusster Kompromiss. SPEC.md Fallback-Acceptance.

Cross-AI MEDIUM-3: PiP State-Loss v1 akzeptiert. In SUMMARY dokumentieren + Block-J-Mini-Add.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.2-ui-audit-akute-hotfixes\08.19.5.2-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.2-ui-audit-akute-hotfixes\08.19.5.2-SPEC.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\STATE.md

<interfaces>
<!-- Fix A: base.html Nav-Label -->
<!-- Zeile ca. 54-56 (verifiziert): -->
<!--   <a class="n-nav-item ..." href="/profiles" title="Profil"> -->
<!--     <i data-lucide="user" class="nav-icon-lucide"></i> -->
<!--     <span class="n-nav-label">Profil</span>    ← AENDERN zu Profile -->
<!--   </a> -->
<!-- Zwei Stellen: title="Profil" → title="Profile", >Profil< → >Profile< -->

<!-- Fix B: pip-launcher.js pagehide-Handler -->
<!-- Zeile ca. 1579-1594: pipWindow.addEventListener('pagehide', function () { ... }) -->
<!-- state.micStarted: boolean — true wenn Mic aktiv und Call laeuft -->
<!-- endCall / _cleanup: Wo state.micStarted auf false zurueckgesetzt wird -->

<!-- Fix B: base.html Re-Launch-Banner -->
<!-- WICHTIG: CSRF-Meta-Tag ist BEREITS in base.html:6 vorhanden (<meta name="csrf-token" ...>) -->
<!-- Kein zweites meta-Tag hinzufuegen — Erbe aus base.html funktioniert schon. -->
<!-- Banner kommt kurz vor </body>: position:fixed, bottom, click → NerveLauncher.open() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav-Label Profil → Profile in base.html</name>
  <files>templates/base.html</files>
  <read_first>
    - templates/base.html (Zeilen 50-65: Nav-Bereich vollstaendig lesen)
  </read_first>
  <action>
Lese templates/base.html, Zeilen 50-65.

Finde den Nav-Link fuer /profiles. Aendere:
1. `title="Profil"` → `title="Profile"`
2. `<span class="n-nav-label">Profil</span>` → `<span class="n-nav-label">Profile</span>`

Das sind genau 2 Zeilen-Aenderungen.

Commit: `fix(08.19.5.2): Nav-Label Profil → Profile in base.html`
  </action>
  <verify>
    <automated>grep -n "n-nav-label.*Profile" "C:\Users\andre\dev\salesnerve\templates\base.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "n-nav-label.*Profile" templates/base.html` zeigt 1 Treffer (mit 'e' am Ende)
    - `grep -n "n-nav-label.*Profil[^e]" templates/base.html` zeigt 0 Treffer (kein altes "Profil" ohne e)
    - `grep -n 'title="Profile"' templates/base.html` zeigt Treffer am profiles-Nav-Link
    - Datei wurde committed
  </acceptance_criteria>
  <done>Hauptnavigation zeigt "Profile" auf jeder Seite.</done>
</task>

<task type="auto">
  <name>Task 2: PiP Re-Launch-Flow — localStorage Flag + Banner</name>
  <files>static/pip-launcher.js, templates/base.html</files>
  <read_first>
    - static/pip-launcher.js (Zeilen 27-71: state-Objekt lesen — insbesondere state.micStarted)
    - static/pip-launcher.js (Zeilen 1570-1600: pagehide-Handler vollstaendig lesen)
    - static/pip-launcher.js (grep nach "endCall\|_cleanup\|micStarted.*false" — Zeilen finden wo Call beendet wird)
    - templates/base.html (Ende des body-Tags finden — Zeile von </body>)
  </read_first>
  <action>
**Architektur-Entscheidung (Cross-AI MEDIUM-3, D-12):**
window.documentPictureInPicture.requestWindow() erfordert User-Gesture (Chrome-Spec).
"PiP ueberlebt Navigation nahtlos" ist mit dieser API unmoeglich.
Implementierung: localStorage Re-Launch-Flow (v1 akzeptiert als SPEC.md Fallback-Acceptance).

**pip-launcher.js — pagehide-Handler erweitern:**

Finde den pagehide-Handler (Zeile ca. 1579). Fuege OBEN im Handler-Block (vor der bestehenden Logik) einen Architektur-Kommentar ein und dann das Flag-Setting:

```javascript
// PiP Re-Launch Architecture (08.19.5.2 D-12):
// window.documentPictureInPicture.requestWindow() erfordert User-Gesture (Chrome-Spec).
// "PiP ueberlebt Navigation nahtlos" ist mit dieser API unmoeglich.
// Implementierung: localStorage-Flag + Re-Launch-Banner auf Zielseite (v1, SPEC.md Fallback-Acceptance).
if (state.micStarted) {
  try { localStorage.setItem('nerve_pip_was_active', '1'); } catch(e) {}
}
```

**pip-launcher.js — Flag beim Call-Ende loeschen:**

Suche nach der Stelle wo state.micStarted auf false gesetzt wird ODER wo der Call sauber beendet wird (grep: `micStarted.*false\|endCall\|_cleanup`). Dort ergaenzen:

```javascript
try { localStorage.removeItem('nerve_pip_was_active'); } catch(e) {}
```

Falls es mehrere Stellen gibt wo micStarted = false gesetzt wird: nur im Haupt-Cleanup erganzen (nicht mehrfach hinzufuegen falls eine Stelle alles abdeckt).

**base.html — Re-Launch-Banner:**

Finde das `</body>`-Tag. Fuege DIREKT DAVOR ein:

```html
<!-- PiP Re-Launch Banner — sichtbar wenn PiP waehrend Navigation aktiv war -->
<div id="pip-relaunch-banner" style="display:none;position:fixed;bottom:16px;left:50%;transform:translateX(-50%);background:#1a1a2e;border:1px solid #00D4AA;border-radius:8px;padding:10px 18px;color:#e0e0e0;font-size:13px;z-index:9999;cursor:pointer;box-shadow:0 4px 16px rgba(0,0,0,0.4)" onclick="this.style.display='none'; try{localStorage.removeItem('nerve_pip_was_active')}catch(e){}; if(window.NerveLauncher){NerveLauncher.open();}">
  &#9654; Live-Assistent war aktiv — hier klicken zum Fortfahren
</div>
<script>
(function() {
  try {
    if (localStorage.getItem('nerve_pip_was_active') === '1') {
      var b = document.getElementById('pip-relaunch-banner');
      if (b) b.style.display = 'block';
    }
  } catch(e) {}
})();
</script>
```

WICHTIG: Kein zweites CSRF-Meta-Tag hinzufuegen — base.html:6 hat bereits `<meta name="csrf-token" content="{{ csrf_token() }}">`. Der Banner braucht dieses Tag nicht.

Commit (beide Dateien zusammen):
`fix(08.19.5.2): PiP Re-Launch-Flow via localStorage + Banner (D-12, SPEC Fallback-Acceptance)`
  </action>
  <verify>
    <automated>grep -n "nerve_pip_was_active" "C:\Users\andre\dev\salesnerve\static\pip-launcher.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nerve_pip_was_active" static/pip-launcher.js` zeigt mindestens 2 Treffer (setItem + removeItem)
    - `grep -n "nerve_pip_was_active" templates/base.html` zeigt 2 Treffer (localStorage-Check + removeItem im onclick)
    - `grep -n "pip-relaunch-banner" templates/base.html` zeigt Treffer (Banner-div existiert)
    - `grep -n "NerveLauncher.open" templates/base.html` zeigt Treffer (Banner-Click-Handler)
    - `grep -n "Re-Launch Architecture\|User-Gesture\|unmoeglich" static/pip-launcher.js` zeigt Treffer (Architektur-Dokumentation)
    - Beide Dateien in einem Commit
  </acceptance_criteria>
  <done>Bei PiP-Navigation: localStorage-Flag gesetzt. Auf Zielseite erscheint Re-Launch-Banner. User-Klick oeffnet PiP neu. Architekturentscheidung im Code dokumentiert (SPEC.md Fallback-Acceptance).</done>
</task>

</tasks>

<verification>
```bash
# Fix A — Nav-Label
grep -n "n-nav-label.*Profile" templates/base.html
# Expected: 1 Treffer

# Fix A — kein altes Profil ohne e
grep -n "n-nav-label.*Profil[^e]" templates/base.html
# Expected: 0 Treffer

# Fix B — localStorage Flag
grep -n "nerve_pip_was_active" static/pip-launcher.js
# Expected: 2+ Treffer

# Fix B — Banner
grep -n "pip-relaunch-banner" templates/base.html
# Expected: 1 Treffer

# Fix B — Architektur-Dokumentation
grep -n "Re-Launch Architecture\|User-Gesture" static/pip-launcher.js
# Expected: 1+ Treffer
```
</verification>

<success_criteria>
- Hauptnavigation zeigt "Profile" auf jeder Seite
- PiP-Navigation setzt localStorage-Flag
- Re-Launch-Banner erscheint auf Zielseite
- Architekturentscheidung und SPEC.md Fallback-Acceptance im Code dokumentiert
- Block-J-Mini-Add in SUMMARY: PiP-State-Persistierung via activeSessionId (fuer spaetere Polish-Phase)
- 2 atomare Commits (1 Nav + 1 PiP+Banner)
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.19.5.2-ui-audit-akute-hotfixes/08.19.5.2-05-SUMMARY.md`

Inhalt:
- Commit-Hashes (Nav-Label + PiP-Bundle)
- Verifikations-Output der grep-Checks
- Cross-AI MEDIUM-3 Disposition: PiP State-Loss v1 akzeptiert (SPEC Fallback-Acceptance) — nahtloses PiP nicht moeglich ohne User-Gesture
- Block-J-Mini-Add: "PiP-State-Persistierung via activeSessionId in localStorage fuer spaetere Polish-Phase (wave noch nicht geplant)"
</output>
