---
phase: 08.19.5.2
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/profiles_list.html
  - routes/profiles.py
autonomous: true
requirements:
  - REQ-08.19.5.2-2

must_haves:
  truths:
    - "Wizard-Modal-HTML (div.wiz-overlay + div.wiz-modal) ist vollstaendig aus profiles_list.html entfernt"
    - "Alle wiz-* CSS-Klassen die nur vom Wizard genutzt werden sind entfernt"
    - "openWizard() / closeWizard() / submitWizard() JS-Funktionen sind entfernt"
    - "POST /profiles/wizard gibt 302 Redirect zu /profiles zurueck"
    - "Der '+ Neues Profil' Button existiert noch, aber oeffnet keinen Wizard mehr — er verlinkt zu /profiles/new oder ist entfernt"
    - "Profile-Editor-Weg bleibt voll funktionsfaehig (nicht anfassen)"
  artifacts:
    - path: templates/profiles_list.html
      provides: "Kein Wizard-Modal, kein wiz-* JS-Code"
      contains: "Neues Profil"
    - path: routes/profiles.py
      provides: "wizard_create() gibt redirect('profiles.liste') zurueck"
      contains: "redirect.*profiles.liste"
  key_links:
    - from: "POST /profiles/wizard"
      to: "redirect /profiles"
      via: "302 Redirect in wizard_create()"
      pattern: "redirect.*profiles"

pruning_note: |
  Macht NICHT: Profile-Editor veraendern — der bleibt wie er ist.
  Macht NICHT: Wizard-Backend-Logik tief aufraumen — nur Route auf 302 umstellen.
  Macht NICHT: onboarding_done Flag anfassen — das ist Plan 03.
  Scope: Nur Wizard-Modal aus UI + Route auf 302.
---

<objective>
Wave 1a — Profil-Wizard komplett aus UI entfernen. Der Wizard ist aktuell kaputt (POST gibt 400) und wird laut Andre-Decision 2026-05-03 nicht repariert sondern entfernt. User legen Profile ab jetzt nur noch ueber den Profile-Editor an.

Drei Schritte:
1. Wizard-Modal-HTML aus profiles_list.html entfernen (div.wiz-overlay + gesamter Inhalt)
2. Wizard-JavaScript aus profiles_list.html entfernen (openWizard, closeWizard, submitWizard + wiz-* CSS)
3. POST /profiles/wizard Route auf 302 Redirect zu /profiles umstellen

Der "Neues Profil" Button auf der profiles_list.html Seite MUSS noch existieren — er kann entweder auf /profiles/new (Editor) verlinken oder den naechsten leeren Editor-Slot oeffnen. Pruefe ob /profiles/new existiert, sonst Button-Text auf "Profil im Editor anlegen" aendern und href auf /profiles setzen (User sieht dann die Liste mit Erstellen-Option ueber Editor).

Output: Kein Wizard im UI mehr. POST /profiles/wizard gibt 302 zurueck.

Pruning: Wizard-Backend-Logik (wizard_create() Funktion) bleibt als Stub mit Redirect — kein Deep-Delete der Funktion weil sie als API-Endpoint bekannt ist.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.2-ui-audit-akute-hotfixes\08.19.5.2-CONTEXT.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\STATE.md
@C:\Users\andre\dev\salesnerve\routes\CLAUDE.md

<interfaces>
<!-- profiles_list.html Wizard-Trigger (verifiziert 2026-05-03): -->
<!-- Zeile 102: <button class="n-btn n-btn-primary" onclick="openWizard()">+ Neues Profil</button> -->
<!-- Zeile 153: onclick="openWizard()" (zweite Trigger-Stelle — leere Liste Karte) -->
<!-- Zeile 168: <div class="wiz-overlay" id="wiz-overlay" ...> — Beginn des Modals -->

<!-- CSS Zeilen 23-102: alle .wiz-* Styles — nur Wizard relevant, koennen raus -->

<!-- routes/profiles.py: -->
<!-- @profiles_bp.route('/wizard', methods=['POST']) -->
<!-- def wizard_create(): — Zeilen 98-188 -->
<!-- Pruefe ob auch GET /profiles/wizard existiert (wizard_page) -->

<!-- url_for verifiziert: -->
<!-- url_for('profiles.liste') → profiles_bp Blueprint-Name 'profiles', Funktion liste() -->
<!-- url_for('dashboard.index') → dashboard_bp Blueprint-Name 'dashboard', Funktion index() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard-Modal + JS + CSS aus profiles_list.html entfernen</name>
  <files>templates/profiles_list.html</files>
  <read_first>
    - templates/profiles_list.html (vollstaendig lesen — Zeilen 1-420 oder bis Ende)
  </read_first>
  <action>
Lese profiles_list.html vollstaendig.

**Schritt 1 — Wizard-Modal-HTML entfernen:**
Finde div.wiz-overlay (Zeile ca. 168) bis zum schliessenden `</div>` des Modals. Entferne den gesamten Block inklusive aller verschachtelten divs. Das sind alle wiz-body, wiz-header, wiz-footer, wiz-step Bloecke.

**Schritt 2 — Wizard-CSS entfernen:**
Finde alle .wiz-* CSS-Regeln im `<style>`-Block (Zeilen ca. 23-102 im style-Block). Entferne sie. Beachte: Nur wiz-* Klassen entfernen, keine anderen Styles anfassen.

**Schritt 3 — Wizard-JS entfernen:**
Finde und entferne diese Funktionen aus dem Script-Block:
- openWizard()
- closeWizard()
- goToStep() (Wizard-Navigationsfunktion, falls vorhanden)
- submitWizard()
- Alle Helfer die NUR vom Wizard genutzt werden (getObjRows, getPhaseRows falls nur im Wizard-Context)
- Beachte: Nur entfernen wenn die Funktion AUSSCHLIESSLICH vom Wizard genutzt wird. Falls getObjRows() auch ausserhalb genutzt wird — behalten.

**Schritt 4 — Wizard-Trigger-Buttons anpassen:**
Finde alle Stellen mit onclick="openWizard()":
- Zeile 102: `<button ... onclick="openWizard()">+ Neues Profil</button>`
  → Aendern zu: `<a href="/profiles" class="n-btn n-btn-primary">+ Neues Profil</a>`
  (oder wenn /profiles/new als Editor-URL existiert: href="/profiles/new")
- Zeile ca. 153: Zweite Trigger-Stelle (leere Karte onclick)
  → onclick entfernen oder href="/profiles" setzen

Pruefe via grep ob /profiles/new als Route existiert:
```bash
grep -n "'/new'\|/profiles/new" routes/profiles.py
```
Wenn nicht: href="/profiles" nehmen.

Commit: `fix(08.19.5.2): Profil-Wizard Modal aus profiles_list.html entfernt`
  </action>
  <verify>
    <automated>grep -c "wiz-overlay\|openWizard\|submitWizard" "C:\Users\andre\dev\salesnerve\templates\profiles_list.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wiz-overlay\|openWizard\|submitWizard" templates/profiles_list.html` gibt 0 zurueck
    - `grep -c "wiz-modal\|wiz-header\|wiz-body\|wiz-footer" templates/profiles_list.html` gibt 0 zurueck
    - "Neues Profil" Button oder Link existiert noch (nur ohne Wizard-Trigger)
    - Datei wurde committed
  </acceptance_criteria>
  <done>Kein Wizard-Modal, kein Wizard-JS, kein Wizard-CSS in profiles_list.html. Neues-Profil-Button noch vorhanden aber ohne Modal-Trigger.</done>
</task>

<task type="auto">
  <name>Task 2: POST /profiles/wizard auf 302 Redirect umstellen</name>
  <files>routes/profiles.py</files>
  <read_first>
    - routes/profiles.py (Zeilen 95-200: wizard_create() und eventuelle wizard_page() vollstaendig lesen)
  </read_first>
  <action>
Lese routes/profiles.py, Zeilen 95-200.

**wizard_create() umstellen:**
Ersetze den gesamten Body der wizard_create() Funktion durch:

```python
@profiles_bp.route('/wizard', methods=['GET', 'POST'])
@login_required
def wizard_create():
    # Profil-Wizard entfernt (Andre-Decision 2026-05-03) — User legt Profile ueber Editor an.
    # Stub bleibt als 302 fuer eventuelle Bookmarks oder externe Links.
    return redirect(url_for('profiles.liste'))
```

Falls GET und POST in separaten Routen definiert sind (wizard_page GET + wizard_create POST): beide auf dieselbe Stub-Funktion reduzieren oder beide in eine combined Route zusammenfassen.

Wichtig: url_for('profiles.liste') — verifiziere dass 'liste' die korrekte Funktion in profiles_bp ist:
```bash
grep -n "def liste\|def profil" routes/profiles.py | head -5
```

Commit: `fix(08.19.5.2): /profiles/wizard Route auf 302 Redirect umgestellt`
  </action>
  <verify>
    <automated>grep -n "def wizard" "C:\Users\andre\dev\salesnerve\routes\profiles.py"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "redirect.*profiles\|redirect.*liste" routes/profiles.py` zeigt Treffer in wizard_create()
    - `grep -n "request.form\|get_json\|daten_dict\|ProfileSchema" routes/profiles.py` zeigt KEINEN Treffer mehr in wizard_create() Zeilen-Bereich (Logik entfernt)
    - Datei wurde committed
  </acceptance_criteria>
  <done>POST (und ggf. GET) /profiles/wizard gibt 302 Redirect zu /profiles zurueck. Kein Wizard-Processing-Code mehr aktiv.</done>
</task>

</tasks>

<verification>
```bash
# Kein Wizard-HTML in Template
grep -c "wiz-overlay\|openWizard\|submitWizard" templates/profiles_list.html
# Expected: 0

# Route gibt Redirect
grep -n "redirect.*profiles\|redirect.*liste" routes/profiles.py
# Expected: Treffer in wizard_create()

# Commits vorhanden
git log --oneline -3 | grep "08.19.5.2"
```
</verification>

<success_criteria>
- Wizard-Modal komplett aus profiles_list.html entfernt (0 wiz-* Referenzen)
- POST /profiles/wizard gibt 302 zurueck
- Profile-Editor bleibt unveraendert
- 2 atomare Commits: Template + Route
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.19.5.2-ui-audit-akute-hotfixes/08.19.5.2-02-SUMMARY.md`

Inhalt:
- Commit-Hashes beider Commits
- Anzahl entfernter HTML-Zeilen (Wizard-Modal)
- Anzahl entfernter JS-Funktionen
- Verifikations-Output der grep-Checks
</output>
