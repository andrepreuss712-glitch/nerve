---
phase: 08.19.5.2
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/onboarding.py
  - routes/auth.py
autonomous: true
requirements:
  - REQ-08.19.5.2-3

must_haves:
  truths:
    - "Alle Onboarding-Routen (/, /complete, /create_profile) geben 302 Redirect zu /dashboard zurueck"
    - "login_required Gate in auth.py leitet NICHT mehr zu onboarding.wizard weiter"
    - "Neue User landen nach Login direkt im Dashboard"
    - "Onboarding-Route-Stub ist als bewusstes Uebergangskonstrukt dokumentiert (kein vergessenes Legacy)"
    - "onboarding_done DB-Spalte bleibt unveraendert (kein Schema-Change)"
  artifacts:
    - path: routes/onboarding.py
      provides: "Alle Routen geben redirect(dashboard.index) zurueck"
      contains: "redirect.*dashboard.index"
    - path: routes/auth.py
      provides: "Onboarding-Gate deaktiviert mit Kommentar"
      contains: "Onboarding 2.0"
  key_links:
    - from: "login_required gate auth.py"
      to: "dashboard.index (nicht onboarding.wizard)"
      via: "Gate-Bedingung entfernt / deaktiviert"
      pattern: "Onboarding 2.0"

pruning_note: |
  Macht NICHT: onboarding_done Spalte aus DB entfernen — bleibt als Legacy-Feld.
  Macht NICHT: onboarding.html Template loeschen — bleibt als Referenz fuer Onboarding 2.0.
  Macht NICHT: app.py Blueprint-Registration anfassen.
  Bewusstes Uebergangskonstrukt: onboarding_bp bleibt registriert mit 302-Stubs.
  Onboarding 2.0: Wird in einer kuenftigen Phase neu konzipiert (neues Onboarding-Erlebnis
  das zum MVP-Scope aus Phase 08.19.5.3 passt). Die Stub-Routen verhindern 404-Fehler
  falls irgendwo noch Links auf /onboarding existieren.
---

<objective>
Wave 1b — Onboarding-Wizard deaktivieren: Neue User landen nach Login direkt im Dashboard.

Zwei Eingriffe:
1. auth.py: Onboarding-Gate (Zeile 73-74) deaktivieren — der Redirect zu onboarding.wizard entfaellt
2. routes/onboarding.py: Alle drei Route-Handler als 302-Stubs zu /dashboard umbauen

Kein Template-Delete, kein DB-Schema-Change, kein Blueprint-Unregister. Bewusstes Uebergangskonstrukt — Onboarding 2.0 kommt in einer kuenftigen Phase (kein vergessenes Legacy, dokumentiert im Code).

Output: Neuer User registriert sich → login_required Gate leitet NICHT nach /onboarding → Dashboard direkt.

Pruning: onboarding.html Template bleibt (Referenz). onboarding_bp bleibt registriert (verhindert 404 bei alten Links). onboarding_done Spalte bleibt (Legacy — Onboarding 2.0 kann sie nutzen oder entfernen).
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.2-ui-audit-akute-hotfixes\08.19.5.2-CONTEXT.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\STATE.md
@C:\Users\andre\dev\salesnerve\routes\CLAUDE.md

<interfaces>
<!-- auth.py Zeile 67-74 (verifiziert 2026-05-03): -->
<!--   onboarding_done = bool(getattr(user, 'onboarding_done', True)) -->
<!--   if not onboarding_done and not request.path.startswith('/onboarding'): -->
<!--       return redirect(url_for('onboarding.wizard')) -->
<!-- Diese beiden Zeilen (73-74) muessen deaktiviert werden -->

<!-- onboarding.py Routen (verifiziert 2026-05-03): -->
<!--   @onboarding_bp.route('/') → def wizard() — Zeile 165 -->
<!--   @onboarding_bp.route('/complete', methods=['POST']) → def complete() — Zeile 186 -->
<!--   @onboarding_bp.route('/create_profile', methods=['POST']) → def create_profile_from_template() — Zeile 207 -->

<!-- url_for Verifikation: -->
<!-- url_for('dashboard.index') → dashboard_bp Name 'dashboard', Funktion index() — verifiziert in CLAUDE.md -->
<!-- url_for('onboarding.wizard') → onboarding_bp Name 'onboarding', Funktion wizard() — bleibt im Code als Referenz (Stub) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: auth.py Onboarding-Gate deaktivieren</name>
  <files>routes/auth.py</files>
  <read_first>
    - routes/auth.py (Zeilen 60-85: login_required vollstaendig lesen)
  </read_first>
  <action>
Lese routes/auth.py, Zeilen 60-85.

Finde den Block:
```python
if not onboarding_done and not request.path.startswith('/onboarding'):
    return redirect(url_for('onboarding.wizard'))
```

Ersetze durch:
```python
# Onboarding 2.0 (08.19.5.2 Andre-Decision 2026-05-03): Gate deaktiviert.
# Neue User landen direkt im Dashboard. onboarding.py Routen sind 302-Stubs.
# Onboarding 2.0 wird in einer kuenftigen Phase neu konzipiert (kein vergessenes Legacy).
# onboarding_done DB-Spalte bleibt als Legacy-Feld unveraendert.
```

Beachte: Die Variable `onboarding_done` wird danach nicht mehr benutzt. Falls sie nirgendwo sonst in der Funktion verwendet wird, kann auch die Zeile `onboarding_done = bool(getattr(user, 'onboarding_done', True))` entfernt werden (Zeile ca. 67). Pruefe vorher ob onboarding_done noch anderswo in der Funktion referenziert wird.

Commit: `fix(08.19.5.2): auth.py Onboarding-Gate deaktiviert — neue User direkt ins Dashboard`
  </action>
  <verify>
    <automated>grep -n "onboarding.wizard\|onboarding_done" "C:\Users\andre\dev\salesnerve\routes\auth.py"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "return redirect.*onboarding.wizard" routes/auth.py` zeigt KEINEN Treffer (kein aktiver Redirect mehr)
    - `grep -n "Onboarding 2.0" routes/auth.py` zeigt Treffer (Kommentar vorhanden)
    - Datei wurde committed
  </acceptance_criteria>
  <done>login_required leitet keine User mehr zu /onboarding weiter. Kommentar erklaert Onboarding 2.0 als kuenftige Phase.</done>
</task>

<task type="auto">
  <name>Task 2: onboarding.py Routen als 302-Stubs umbauen</name>
  <files>routes/onboarding.py</files>
  <read_first>
    - routes/onboarding.py (vollstaendig lesen: Zeilen 165 bis Ende — alle drei Route-Handler)
  </read_first>
  <action>
Lese routes/onboarding.py ab Zeile 165 (alle Route-Handler).

Ersetze den Body JEDER der drei Route-Funktionen durch einen 302-Stub.

**wizard() Route (GET /):**
```python
@onboarding_bp.route('/')
@login_required
def wizard():
    # Onboarding 2.0 Stub (08.19.5.2 Andre-Decision 2026-05-03).
    # Neue User landen direkt im Dashboard. Onboarding 2.0 in kuenftiger Phase.
    # Stub verhindert 404 bei alten Bookmarks oder Auth-Redirects.
    return redirect(url_for('dashboard.index'))
```

**complete() Route (POST /complete):**
```python
@onboarding_bp.route('/complete', methods=['POST'])
@login_required
def complete():
    # Onboarding 2.0 Stub — siehe wizard() oben.
    return redirect(url_for('dashboard.index'))
```

**create_profile_from_template() Route (POST /create_profile):**
```python
@onboarding_bp.route('/create_profile', methods=['POST'])
@login_required
def create_profile_from_template():
    # Onboarding 2.0 Stub — siehe wizard() oben.
    return redirect(url_for('dashboard.index'))
```

Alle Helper-Funktionen und BRANCHE_TEMPLATES Dict am Anfang der Datei:
- BRANCHE_TEMPLATES Dict BEHALTEN (Referenz fuer Onboarding 2.0)
- Import-Statements bereinigen: Entferne Imports die nur von den alten Handler-Bodies genutzt wurden und jetzt unused sind (z.B. render_template, make_response, json, Profile-Modell-Imports falls nur in den alten Bodies). BEACHTE: Nur entfernen was wirklich unused ist — pruefe jeden Import.

Commit: `fix(08.19.5.2): onboarding.py Routen als 302-Stubs umgebaut (Onboarding 2.0 kuenftig)`
  </action>
  <verify>
    <automated>grep -n "render_template.*onboarding\|onboarding\.html" "C:\Users\andre\dev\salesnerve\routes\onboarding.py"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "render_template.*onboarding\|onboarding\.html" routes/onboarding.py` zeigt KEINEN Treffer (kein Template-Render mehr)
    - `grep -n "redirect.*dashboard.index" routes/onboarding.py` zeigt mindestens 3 Treffer (eine pro Route)
    - `grep -n "Onboarding 2.0 Stub" routes/onboarding.py` zeigt mindestens 1 Treffer (Dokumentation)
    - BRANCHE_TEMPLATES Dict noch vorhanden (`grep -n "BRANCHE_TEMPLATES" routes/onboarding.py` zeigt Treffer)
    - Datei wurde committed
  </acceptance_criteria>
  <done>Alle drei Onboarding-Routen geben 302 Redirect zu /dashboard zurueck. BRANCHE_TEMPLATES bleibt fuer Onboarding 2.0. Stubs sind als bewusstes Uebergangskonstrukt dokumentiert.</done>
</task>

</tasks>

<verification>
```bash
# Gate deaktiviert
grep -n "return redirect.*onboarding.wizard" routes/auth.py
# Expected: 0 Treffer

# Kommentar vorhanden
grep -n "Onboarding 2.0" routes/auth.py
# Expected: 1+ Treffer

# Stubs in onboarding.py
grep -n "redirect.*dashboard.index" routes/onboarding.py
# Expected: 3 Treffer

# Kein Template-Render mehr
grep -n "render_template.*onboarding" routes/onboarding.py
# Expected: 0 Treffer
```
</verification>

<success_criteria>
- login_required leitet keine User nach /onboarding weiter
- Alle 3 Onboarding-Routen geben 302 zu /dashboard zurueck
- BRANCHE_TEMPLATES Dict bleibt fuer Onboarding 2.0
- Stub ist als bewusstes Uebergangskonstrukt dokumentiert (nicht als vergessenes Legacy)
- 2 atomare Commits: auth.py + onboarding.py
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.19.5.2-ui-audit-akute-hotfixes/08.19.5.2-03-SUMMARY.md`

Inhalt:
- Commit-Hashes beider Commits
- Bestätigung dass Gate deaktiviert + Stubs aktiv
- Hinweis: Onboarding 2.0 klar dokumentiert als kuenftige Phase (nicht vergessenes Legacy)
</output>
