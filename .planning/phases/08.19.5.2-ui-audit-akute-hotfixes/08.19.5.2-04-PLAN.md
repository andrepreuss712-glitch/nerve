---
phase: 08.19.5.2
plan: "04"
type: execute
wave: 2
depends_on:
  - "08.19.5.2-02"
  - "08.19.5.2-03"
files_modified:
  - templates/dashboard.html
autonomous: true
requirements:
  - REQ-08.19.5.2-4

must_haves:
  truths:
    - "Klick auf Session-Row in Dashboard navigiert zu /session/<id>"
    - "Session-Row zeigt cursor:pointer beim Hover"
    - "s.id wird direkt als Integer interpoliert (kein esc() noetig)"
  artifacts:
    - path: templates/dashboard.html
      provides: "db2-session-row mit onclick-Handler"
      contains: "db2-session-row.*onclick"
  key_links:
    - from: "dashboard.html db2-session-row"
      to: "/session/<s.id>"
      via: "onclick location.href"
      pattern: "location\\.href.*session.*s\\.id"
---

<objective>
Wave 2a — Dashboard Session-Row onclick hinzufuegen: Ein-Zeilen-Fix.

Session-Rows in der renderSessions() Funktion haben keinen onclick-Handler — Klick tut nichts. Fix: onclick="location.href='/session/' + s.id" + style="cursor:pointer" auf das db2-session-row div.

Das Pattern ist konsistent mit renderRecommendations() 20 Zeilen tiefer im selben File.

Output: Jede Session-Row ist klickbar und navigiert zu /session/<id>.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.2-ui-audit-akute-hotfixes\08.19.5.2-CONTEXT.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\STATE.md

<interfaces>
<!-- dashboard.html Zeile 729 (verifiziert via Audit): -->
<!-- html += '<div class="db2-session-row">'; — FEHLT onclick -->

<!-- Vorbild-Pattern (renderRecommendations(), ca. Zeile 749): -->
<!-- html += '<div class="db2-reco-item"' + (r.url ? ' onclick="location.href=\'' + esc(r.url) + '\'" style="cursor:pointer"' : '') + '>'; -->

<!-- s.id ist Integer aus Backend-JSON — kein esc() noetig (kein XSS-Risiko bei Integer) -->
<!-- Route verifiziert: /session/<int:sid> existiert in routes/dashboard.py -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session-Row onclick-Handler hinzufuegen</name>
  <files>templates/dashboard.html</files>
  <read_first>
    - templates/dashboard.html (Zeilen 725-760: renderSessions() und renderRecommendations() vollstaendig lesen)
  </read_first>
  <action>
Lese templates/dashboard.html, Zeilen 725-760.

Finde Zeile ca. 729:
```javascript
html += '<div class="db2-session-row">';
```

Ersetze durch:
```javascript
html += '<div class="db2-session-row" onclick="location.href=\'/session/' + s.id + '\'" style="cursor:pointer">';
```

Das ist der vollstaendige Fix. Eine Zeile aendern.

Commit: `fix(08.19.5.2): Dashboard Session-Row onclick-Handler (REQ-4)`
  </action>
  <verify>
    <automated>grep -n "db2-session-row.*onclick" "C:\Users\andre\dev\salesnerve\templates\dashboard.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "db2-session-row.*onclick" templates/dashboard.html` zeigt 1 Treffer mit location.href und s.id
    - `grep -n "cursor:pointer" templates/dashboard.html` zeigt Treffer im Session-Row-div
    - Datei wurde committed
  </acceptance_criteria>
  <done>Session-Rows im Dashboard sind klickbar. Klick navigiert zu /session/<id>.</done>
</task>

</tasks>

<verification>
```bash
grep -n "db2-session-row.*onclick" templates/dashboard.html
# Expected: 1 Treffer

grep -n "cursor:pointer" templates/dashboard.html
# Expected: Treffer im db2-session-row div
```
</verification>

<success_criteria>
- Klick auf Session-Row → Browser navigiert zu /session/<id>
- cursor:pointer sichtbar beim Hover
- 1 atomarer Commit
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.19.5.2-ui-audit-akute-hotfixes/08.19.5.2-04-SUMMARY.md`

Inhalt:
- Commit-Hash
- Verifikations-Output
- Kurze Bestaetigung: 1 Zeile geaendert, Pattern konsistent mit renderRecommendations()
</output>
