---
phase: 08.19.5.2
plan: "02"
subsystem: profiles
tags: [wizard-removal, ui-cleanup, route-stub]
dependency_graph:
  requires: []
  provides: [wizard-removed, profiles-list-clean]
  affects: [templates/profiles_list.html, routes/profiles.py]
tech_stack:
  added: []
  patterns: [302-redirect-stub]
key_files:
  created: []
  modified:
    - templates/profiles_list.html
    - routes/profiles.py
decisions:
  - "Wizard komplett entfernt (Andre-Decision 2026-05-03) statt repariert"
  - "/profiles/new als Ziel fuer 'Neues Profil' Button (Route existiert)"
  - "wizard_create() als kombinierter GET+POST Stub behalten fuer Bookmarks"
metrics:
  duration: ~10min
  completed: 2026-05-03
---

# Phase 08.19.5.2 Plan 02: Profil-Wizard Entfernung Summary

**One-liner:** Wizard-Modal (HTML/CSS/JS, ~310 Zeilen) aus profiles_list.html entfernt und /profiles/wizard auf 302-Redirect-Stub umgestellt.

## Commits

| Task | Commit | Beschreibung |
|------|--------|--------------|
| Task 1 | `ac280a7` | fix(08.19.5.2): Profil-Wizard Modal aus profiles_list.html entfernt |
| Task 2 | `b0bef14` | fix(08.19.5.2): /profiles/wizard Route auf 302 Redirect umgestellt |

## Was wurde getan

### Task 1 — profiles_list.html bereinigt

**Entfernte HTML-Zeilen:** ~90 Zeilen (div.wiz-overlay + alle verschachtelten wiz-* Divs, Steps 1-3, Footer)

**Entfernte CSS-Regeln:** ~70 Zeilen (.wiz-overlay, .wiz-modal, .wiz-header, .wiz-header-top, .wiz-title, .wiz-close, .wiz-progress, .wiz-dot, .wiz-pline, .wiz-step-label, .wiz-body, .wiz-footer, .wiz-sub, .wfg, .wfl, .wfi/.wfs/.wfta, .whint, .obj-list/.obj-row, .btn-obj-rm, .btn-add-obj, .btn-load-tmpl, .phase-list/.phase-row, .phase-num, .wiz-vorlage-row)

**Entfernte JS-Funktionen (13 Stück):**
- openWizard(), closeWizard()
- wizUpdateUI(), wizNext(), wizBack()
- addObjRow(), loadBrancheEinwaende(), getObjRows()
- addPhaseRow(), renumPhaseRows(), loadPhaseVorlage(), getPhaseRows()
- submitWizard()

**Entfernte JS-Konstanten:** BRANCHE_EW, PHASE_VORLAGEN, let wizStep

**Button-Anpassung:**
- `<button onclick="openWizard()">` → `<a href="/profiles/new">` (Route existiert: GET/POST /profiles/new)
- Dashed-Karte: `onclick="openWizard()"` entfernt, zu `<a href="/profiles/new">` umgewandelt

**Netto-Änderung:** -316 Zeilen, +4 Zeilen (git diff)

### Task 2 — routes/profiles.py Wizard-Stub

**Entfernt:** wizard_page() (GET, 3 Zeilen) + wizard_create() (POST, ~95 Zeilen Verarbeitungslogik)

**Ersetzt durch:** Kombinierter GET+POST Stub (5 Zeilen):
```python
@profiles_bp.route('/wizard', methods=['GET', 'POST'])
@login_required
def wizard_create():
    # Profil-Wizard entfernt (Andre-Decision 2026-05-03)
    return redirect(url_for('profiles.liste'))
```

**Netto-Änderung:** -96 Zeilen, +4 Zeilen (git diff)

## Verifikation

```
grep -c "wiz-overlay|openWizard|submitWizard" templates/profiles_list.html
→ 0  ✓

grep -n "redirect.*profiles.liste" routes/profiles.py (Zeile 96)
→ Treffer in wizard_create()  ✓

"Neues Profil" Link existiert noch: <a href="/profiles/new">  ✓
Profile-Editor (routes/profiles.py) nicht angefasst  ✓
```

## Deviationen vom Plan

Keine — Plan exakt wie beschrieben ausgeführt. `/profiles/new` existierte bereits als Route, daher direkt als href verwendet (kein Fallback auf `/profiles` nötig).

## Threat Flags

Keine neuen Threat-Surfaces eingeführt. Wizard-Route wurde nicht gelöscht sondern als 302-Stub belassen — keine offenen Endpunkte entfernt, nur Verarbeitungslogik.

## Self-Check: PASSED

- [x] templates/profiles_list.html: 0 wiz-* Referenzen
- [x] routes/profiles.py: wizard_create() gibt redirect(url_for('profiles.liste')) zurück
- [x] Commit ac280a7 existiert
- [x] Commit b0bef14 existiert
- [x] "Neues Profil" Link auf /profiles/new vorhanden
- [x] Profile-Editor-Routen unverändert
