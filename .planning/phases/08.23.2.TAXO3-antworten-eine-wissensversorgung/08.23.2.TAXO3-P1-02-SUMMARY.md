---
phase: 08.23.2.TAXO3
plan: P1-02
status: LIVE + verifiziert (deployed 2026-07-01, Live-Test Call 5d5f4bdd — Andre: "echter Sprung, kein Muell")
completed_code: 2026-07-01
test_anruf: true (DONE — Call 5d5f4bdd, Knopf + QA exerziert)
deploy: DONE (Prod, Commits bis ab8143c gepusht)
commits:
  - 47f0ede feat: Live-Cutover — 3 Antwort-Pfade auf build_answer_context
  - da3b2b0 test: Stale-Test-Triage fuer den Cutover
  - 237320c docs: SUMMARY (Cutover code-complete) + P1-NOTES
  - 9786498 fix: Antwort-Pfade max_tokens 250/400/200 -> 500 (Headroom gegen mid-Satz-Clipping)
  - 7ed799a feat: Paradigma-Nachschaerfung — Anti-Formelhaftigkeit (Regel 10+11)
  - ab8143c fix: Paradigma auf 10 Regeln konsolidiert (Anti-Bloat-Test 6-10)
---

# SUMMARY — P1-02: Live-Cutover der 3 Antwort-Pfade auf das Paradigma

## Was gebaut wurde (Code-complete — Deploy/Test-Anruf ausstehend)

Der Paradigma-Reset ist **im Code scharf geschaltet** (Req 1/2/3/5/6/7). Alle Live-Antwort-Pfade
ziehen ihren System-Prompt aus der EINEN Quelle `build_answer_context` (P1-01).

| Pfad | Datei:Zeile | Änderung |
|---|---|---|
| **QA** | `qa_pipeline.py:418` (`generate_qa_response`) | System-Prompt aus `answer_system_content`; `_SYSTEM_PROMPT_QA` + `"...zwingt"` + `"Frag nach:"`-Beispiele gelöscht |
| **Manual/Knopf** | `claude_service.py:776` (`streame_manual_ewb_variante`) | System-Prompt aus `answer_system_content`; `standard_ga`-Vorlage raus |
| **Auto** | `claude_service.py:618` (`streame_auto_variante`) | `_ewb_autovar_system` gelöscht; Few-Shot raus; neuer `primary_intent`-Param |
| Wiring | `prompt_pipeline.py` | `answer_system_content()` + `derive_answer_params()` (Deviation, s.u.) |

## (a) Pruning-Notiz — gelöscht

- **`_ewb_autovar_system`** (kontext-armer 14-Token-Auto-Prompt) — 0 live Definitionen (grep-belegt).
- **`_SYSTEM_PROMPT_QA`** (QA-Selbstbau inkl. drückerischem `"...die den Kunden zur Konkretisierung
  ZWINGT"` + hartcodierte `"Frag nach:"`-Beispielsätze) — gelöscht.
- **`standard_ga`**-Injektion (Manual Few-Shot-Vorlage) + **"Hinterlegte Einwand-Gegenargumente
  (als Orientierung)"**-Injektion (Auto Few-Shot) — gelöscht.
- Ersetzt durch: Paradigma + Rollen-Ziel + Grounding-Regel aus `build_answer_context`.
- **Low-Conf-"Frag nach:"-Format-Kontrakt** (FE erwartet das Präfix für Rückfragen) **strukturell
  erhalten** (user_msg-Hint + Präfix-Safety-Net in `generate_qa_response`) — das ist ein FE-Kontrakt,
  keine drückerische Formulierung.

## (b) Punkt-26-Beleg — Schreibstelle vor Leser (Async-Naht)

- **Schreiber:** `claude_service.py:1008-1009` (`analysiere_mit_claude`, Medium-Lane): setzt
  `_med_st['primary_intent'] = _it` + `['confidence']` per-SID **VOR dem Antwort-Trigger** (IL-2,
  TAXO1-Live).
- **Leser:** `prompt_pipeline.derive_answer_params(sid)` liest `_session_state[sid]['state']
  ['primary_intent']` (+ confidence) zum Antwort-Zeitpunkt, unter `_session_state_lock`.
- **Voraus-Lauf-Schutz:** läuft der Leser dem Schreiber voraus → `primary_intent = None` →
  `build_answer_context` nutzt `_DEFAULT_INTENT_HINT` (fail-open) — **NIE ein falscher Intent**.

## (c) TTFT-Basislinie — PENDING (Test-Anruf)

⏸ Kommt aus dem Pflicht-Test-Anruf nach Deploy (`inspect.sh logs 500 | grep "EWB-TTFT"`). Der
Auto-Prompt wächst (vorher 14 Token → jetzt voller Profil-Kontext); **NUR messen, nicht festzurren**
(D-06). Phase 2 (Plan 04) macht Tempo via Caching. Slot A <1s unverändert überbrückt.

## (d) Unverändert (Beleg)

- **Modell:** `config.MODEL_PIP_AUTOVAR` / `MODEL_PIP_VARIANTE` / `MODEL_QA` unangetastet.
- **Circuit-Breaker:** `_ewb_fallback_until` / `_ewb_ttft_history` / `_ewb_circuit_lock` +
  `global _ewb_fallback_until`-Deklaration unverändert (grep: 14 Referenzen intakt).
- **Caching:** Phase 1 gibt PLAIN Content-Blöcke OHNE `cache_control` — cache_control-Layering auf
  dem stabilen Prefix ist Phase 2/Plan 04 (verhindert Cache-Poison aus P1-01 nicht zu re-injizieren).
- **Slot A** (lokales Sofort-Netz) NICHT angefasst; `source=manual_button`-Emits intakt.

## ⚠️ DEVIATION (Re-Grep-Befund, Punkt 20) — für Claudian-Audit + Test-Anruf

**`streame_auto_variante` ist seit PIP-01 (2026-06-30) DORMANT** — **0 Produktiv-Caller**
(grep `streame_auto_variante(` in `services/ routes/`: nur `tests/`). Der deepgram-Keyword-Pfad
ruft ihn nicht mehr (emittiert nur `ewb_signal`, um einen bestehenden Button aufleuchten zu lassen).

Konsequenzen:
1. **`deepgram_service.py` wurde NICHT geändert** — es gibt keinen Call-Site für die geplante
   `match['matched_label']`-Durchreichung. Der `primary_intent`-Param ist trotzdem angelegt (für
   Reaktivierung + analyse_loop-Trigger).
2. Die Umlenkung von `streame_auto_variante` auf `build_answer_context` hält den dormanten Pfad
   **paradigma-korrekt für eine spätere Reaktivierung** (Meeting-Auto), löscht aber vor allem den
   Müll-String (Req 1/2 erfüllt).
3. **Der Live-Test-Anruf kann nur Knopf + QA exerzieren, NICHT Auto[Meeting]** — der Auto-Pfad
   feuert aktuell nirgends. Task-4-Schritt 5 ("Meeting-Auto trägt Profil-Stimme") ist derzeit nicht
   live testbar. Empfehlung: Auto-Reaktivierung als eigene spätere Scheibe bewerten.

**Anrede-Enforcement (Watch-Item):** durch P1-01s Anti-Cache-Poison-Fix ist die forcierte
Sektion-7-Anrede-Zeile ("WICHTIG: Nutze konsequent {anrede}-Form") aus dem Stabil-Block entfernt;
Anrede kommt jetzt nur noch als "Anrede: X" aus dem Volatil-Block (Sek. 9) + Paradigma-Regel 5. Falls
der Test-Anruf Anrede-Drift zeigt → forcierte Anrede-Zeile in den Volatil-Block aufnehmen (kleiner Fix).

## Stale-Test-Triage (Vertrags-ändernde Welle)

- `test_per_sid_migration::test_ewb_variante_propagates_profile_context_error` → monkeypatcht jetzt
  `answer_system_content` (war `build_profile_context`, nach Cutover No-Op).
- `test_qa_pipeline::test_fail_closed_on_exception` → patcht `answer_system_content`.
- Grün ohne Änderung: `test_qa_pipeline_rueckfrage` (Frag-nach-Präfix-Mechanik erhalten),
  `test_ewb_autovar_global_regression` (UnboundLocal-Guard, `global` unangetastet),
  `test_anon_live_vs_stored` (Anzeige/Storage-Logik unangetastet).
- `ast.parse` grün für alle 5 geänderten Dateien.

## ✅ ABGESCHLOSSEN (2026-07-01) — deployed + live-verifiziert + nachgeschärft

1. **Claudian-Pre-Execute-Audit** über den Cutover-Diff: GRÜN.
2. `bash deploy.sh production`: GRÜN (Commits bis ab8143c gepusht).
3. **André Live-Test-Anruf (Call 5d5f4bdd, Knopf + QA):** Antworten **deutlich besser** — Gatekeeper
   respektvoll (bittet um Durchstellen, kein Trick), diagnostische Sachfragen statt Gegenargument,
   Sie durchgehalten, sauberer Vorlese-Text. **André: „echter Sprung, kein Müll."**

## Nach dem Live-Test — Formelhaftigkeits-Befund + Regel-Nachschärfung (live)

- **Befund (André-Bauchgefühl → Gemini am kompletten Call, blind):** drei Tics — „Verstehe"-Syndrom
  (11/14 Antworten öffnen so), „kurz"-Krücke (unterwürfig), Sandwich-Muster (Bestätigung+Pitch+Frage
  jedes Mal). „Zu teuer" = ROI-Bingo. GUT (nicht kaputtmachen): Gatekeeper-Umgang, echte Sachfragen,
  Tonalität.
- **Fix (André-freigegeben, auf 10 Regeln konsolidiert, Commits 7ed799a/ab8143c):** Regel 10 = bei
  harten Einwänden (Preis/Zeit/Wettbewerb/kein Bedarf) KEIN Gegenargument/ROI/Pitch → offene
  diagnostische Frage; Öffnungs-Anti-Tic (nicht immer „Verstehe", kein „kurz") in Regel 5 gefaltet.
- **VERWORFEN:** „ein Fakt statt drei" (André: Pitch braucht mehrere Eckpfeiler kurz).
- **max_tokens** aller 3 Antwort-Pfade 250/400/200 → **500** (Commit 9786498, kein mid-Satz-Clipping;
  Länge steuert das Paradigma, nicht die Kappe).
- **Watch (noch offen):** Regel-Nachschärfung ist live, aber **noch nicht re-getestet** (André:
  „nicht viel passiert, Test jetzt unnötig") — bei nächstem Test prüfen, ob „Verstehe"/ROI zurückgehen.
- **TTFT-Basislinie:** nicht festgezurrt (Tempo = Phase 2/Caching). Auto[Meeting] weiterhin dormant.

## ★ Nächste Scheibe (b): Gesprächs-Gedächtnis + „Freie-Antwort"-Knopf

Die „Verstehe"-Wiederholung entsteht aus der **Isolation jeder Antwort** (NERVE hat kein Gedächtnis).
Echte Lösung = Gesprächs-Gedächtnis (Vorturns inkl. eigener Vorantworten + Live-Transkript), kein
Verbot. Frage-Paradigma öffnet eine Schleife → im Cold-Call via „Freie-Antwort"-Knopf, im Meeting
automatisch. Scopen mit Drei-Wege + Modell/Latenz-Frage. Details: Vault `05 Log` HANDOFF 2026-07-01.
