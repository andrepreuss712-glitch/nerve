---
phase: 08.23.2.TAXO3
plan: P1-01
type: execute
wave: 1
complexity: 🔴
depends_on: []
files_modified:
  - services/answer_paradigm.py        # NEU
  - services/prompt_pipeline.py
  - tests/test_build_answer_context.py  # NEU
autonomous: false
risiko: moderat
test_anruf: false   # reine Logik, keine Live-Pfad-Änderung — Unit-Test genügt
requirements: ["SPEC Req 1 (Funktion)", "SPEC Req 2 (Text-Quelle)", "SPEC Req 3 (Rollen-Config)", "SPEC Req 5 (EIN Intent — Signatur)", "SPEC Req 6 (Modus/Konfidenz-Parameter)", "SPEC Req 7 (Grounding-Regel-Text)"]

must_haves:
  truths:
    - "NEU services/answer_paradigm.py: der NERVE-Standard als Text-in-Code — PARADIGMA (6–10 Verhaltensregeln, unsichtbares Geruest, KEINE Beispielsaetze), 3 ROLLEN-Ziele (gatekeeper/interessent/meeting), GROUNDING-Regel, intent→hinweis-Map (Ziel/Register + 1–2 Hebel je Intent, KEINE fertigen Saetze). Inhalt = die von André per D-01 freigegebenen Texte (echte Umlaute, User-facing Wirkung)."
    - "NEU load_answer_config(user_id, sid) in prompt_pipeline.py: DIE EINE Andock-Stelle (Weg 3). Gibt HEUTE den NERVE-Standard aus answer_paradigm.py zurueck. Dokumentierter Naht-Punkt: spaeter klinkt hier die Coach-Tuer-DB ein — build_answer_context wird dafuer NICHT umgebaut. Foundation-Register-Eintrag gesetzt."
    - "NEU build_answer_context(*, user_id, sid, primary_intent, mode, confidence, role, is_auto_triggered) -> list[dict]: gibt eine LISTE von System-Bloecken zurueck. STABIL-Block (Paradigma + Rollen-Ziel + Profil-Persona-Stabilteil + Grounding-Regel) VORNE, VOLATIL-Block (aktueller Intent + Modus/Konfidenz-Register + volatiler Profilteil Briefing/Lead) HINTEN. Strukturell cache-faehig getrennt — Caching NICHT aktiviert (Phase 2)."
    - "build_answer_context wrappt das bestehende build_profile_context (prompt_pipeline.py:111) — NICHT neu bauen. Der REALE Aufbau hat 9 Sektionen (2026-07-01 verifiziert, Cross-AI Gemini HIGH): 1–7 STABIL (Branche/Basis/Zielkunde/Schmerzen/Einwaende/Phasen/KI-Verhalten), 8 PreCall-Briefing + 9 Lead-Kontext VOLATIL (per-SID). Stabilteil (Sek. 1–7) in den Stabil-Block, Volatilteil (Sek. 8–9) in den Volatil-Block. Split NICHT ueber Sektions-NUMMER, sondern ueber Text-Anker '## PreCall-Briefing' (erste volatile Sektion) — es gibt KEINE eigenen Sektionen '8 FAQ/9 Tabu' (Alt-Annahme war falsch)."
    - "ANTI-CACHE-POISON (Cross-AI Gemini HIGH, verifiziert prompt_pipeline.py:402/241/465): die per-SID 'anrede' (_resolve_anrede priorisiert session_anrede) ist heute DOPPELT eingebacken — stabil in Sektion 7 (KI-Verhalten, :402) UND volatil in Sektion 9 (Lead-Kontext, :452). Der Stabil-Block MUSS die Anrede-Zeile aus Sektion 7 auslassen; Anrede erscheint NUR im Volatil-Block (Sek. 9). Kein per-SID-Datum (anrede/briefing/lead) im Stabil-Block (SPEC Req 9: keine fluechtigen Daten im Cache-Prefix)."
    - "Modus + Konfidenz + Rolle steuern das Verhalten als PARAMETER (Register-Text aus answer_paradigm.py), KEIN if mode==-Zweig der einen eigenen Prompt baut. Genau EIN primary_intent-Parameter (kein Top-2)."
    - "build_answer_context MUST NOT raise: fehlt Config/Profil → fail-open Minimal-Block (Paradigma-Standard). Kein DB-Call im Hot-Path (load_answer_config ist heute reiner Speicher-Zugriff; falls je gecacht, wie _profile_cache)."
    - "KEINE Live-Pfad-Änderung in diesem Plan (claude_service unangetastet). Nur die neue Funktion + Config + Test entstehen. Live-Cutover = P1-02."
  artifacts:
    - path: "services/answer_paradigm.py"
      provides: "NERVE_PARADIGM (list[str] 6–10 Regeln), ROLE_GOALS (dict gatekeeper/interessent/meeting -> Ziel-Text), GROUNDING_RULE (str), INTENT_HINTS (dict intent_key -> {register, hebel}); Getter get_answer_config() -> dict"
      contains: "NERVE_PARADIGM"
    - path: "services/prompt_pipeline.py"
      provides: "load_answer_config(user_id, sid) (Andock-Stelle, heute answer_paradigm-Default); build_answer_context(...) -> list[dict] (Block-Split stabil/volatil); Block-Split-Helfer um build_profile_context (return_blocks; Split am Text-Anker '## PreCall-Briefing', Anrede-Zeile aus Sek.7-Stabilteil entfernt)"
      contains: "def build_answer_context"
    - path: "tests/test_build_answer_context.py"
      provides: "Pure-logic: Block-Listen-Form (>=2 Bloecke), Stabil-vor-Volatil, fail-open (kein raise ohne Profil/Config), EIN-Intent (Signatur nimmt genau einen), unbekannter Intent-Key -> Default-Hinweis (kein KeyError), Rolle/Modus/Konfidenz als Parameter im Register-Text, Grounding-Regel im Block, KEIN Few-Shot/Beispielsatz-String im Output, ANTI-CACHE-POISON (kein per-SID-Datum anrede/briefing/lead im Stabil-Block), Split am Text-Anker '## PreCall-Briefing'"
  key_links:
    - from: "build_answer_context"
      to: "load_answer_config (Andock-Stelle, Weg 3)"
      via: "config = load_answer_config(user_id, sid)"
      pattern: "load_answer_config"
    - from: "load_answer_config"
      to: "answer_paradigm.get_answer_config (NERVE-Standard heute; Coach-DB spaeter)"
      via: "fail-open Default"
      pattern: "get_answer_config"
    - from: "build_answer_context"
      to: "build_profile_context (Bestand, prompt_pipeline.py:111)"
      via: "Wrapper mit Block-Split stabil/volatil"
      pattern: "build_profile_context"
---

<objective>
Baue das Fundament des Paradigma-Resets OHNE Live-Risiko: (1) die neue Datei
`services/answer_paradigm.py` mit dem NERVE-Standard-Text (Paradigma-Regeln + 3 Rollen-Ziele +
Grounding-Regel + Intent-Hinweise — die von André freigegebenen Texte, D-01); (2) die EINE
Andock-Stelle `load_answer_config(user_id, sid)` (Weg 3 — heute Datei-Default, später Coach-Tür-DB
ohne Umbau); (3) die neue `build_answer_context(...)`-Funktion als dünner Wrapper um das bestehende
`build_profile_context()`, die eine **Liste von System-Blöcken** (stabil vorne / volatil hinten,
cache-fähig angelegt, Caching NICHT aktiv) zurückgibt und Rolle/Modus/Konfidenz/EIN-Intent als
Parameter nimmt. Plus Unit-Test.

Purpose: die EINE Quelle der Antwort-Versorgung entsteht testbar, bevor die riskante Live-Umlenkung
(P1-02) sie scharf schaltet. Kein Antwort-Pfad wird in diesem Plan geändert.

Output: answer_paradigm.py + load_answer_config + build_answer_context live im Code (aber noch von
keinem Live-Pfad aufgerufen), Unit-Test grün. KEIN Test-Anruf (keine Live-Wirkung).

NICHT in diesem Plan: Umlenkung der 3 Antwort-Pfade (P1-02), Löschen des Auto-Müll-Strings (P1-02),
Caching-Aktivierung (Phase 2), Coach-Tür-DB (deferred), product_facts-Tabelle (deferred).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT
`.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/
gsd-verifier NICHT aufrufen (lesen den Basis-Dir 08.23.2). STATE/ROADMAP hand-editieren.

**Verify:** Dieser Plan ist reine Logik ohne Live-Anbindung → Unit-Test genügt (Ausnahme von der
„jede Welle Test-Anruf"-Regel, D-05: „reine Schema/Logik-Addition ohne Live-Anbindung darf
überspringen"). Trotzdem Deploy über Production (`bash deploy.sh production`) damit der Code auf Prod
liegt, wenn P1-02 ihn scharf schaltet — aber KEIN Live-Verhalten ändert sich.

**UTF-8-Regel:** Der PROMPT-TEXT-INHALT in answer_paradigm.py (Paradigma-Regeln, Rollen-Ziele,
Grounding-Regel) ist USER-FACING-Wirkung (die KI liest ihn, der Berater hört das Ergebnis) → echte
Umlaute (ä/ö/ü/ß). Code-Identifier / dict-keys ASCII (`gatekeeper`, `interessent`, `meeting`).

**Latenz (Punkt 25):** build_answer_context darf den Hot-Path nicht mit DB bremsen. load_answer_config
ist heute reiner Speicher-Zugriff (Konstanten aus answer_paradigm.py) — 0 DB. Falls die Coach-Tür
später hier einklinkt: dann per-User cachen wie build_profile_context (`_profile_cache`). Block-Split
ist strukturell cache-fähig, aber Caching wird hier NICHT aktiviert (Phase 2).
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-P1-NOTES.md

<interfaces>
<!-- Verifizierte Anker (Code gemappt 2026-07-01, file:line). -->

build_profile_context (BESTAND, prompt_pipeline.py:111 — WRAPPEN, nicht neu bauen):
  def build_profile_context(user_id, mode='cold_call', sid=None, profile_id=None) -> str
  - Hot-Path (sid + _profile_cache): <5ms, 0 DB. Fallback (sid=None): leichte DB-Query.
  - REALE Sektionen (2026-07-01 gegen prompt_pipeline.py verifiziert, Cross-AI Gemini HIGH-Fix —
    das Modul hat NUR 9 Sektionen, NICHT 11; es gibt KEINE eigenen Sektionen "8 FAQ / 9 Tabu"):
      1 Branche (:243), 2 Basis (:258), 3 Zielkunde (:291), 4 Schmerzen (:319), 5 Einwaende (:368),
      6 Phasen (:384), 7 KI-Verhalten (:399) = STABIL.
      8 ## PreCall-Briefing (:433, _briefing per-SID) + 9 ## Lead-Kontext (:442,
      session_anrede/vorwissen_level per-SID) = VOLATIL.
  - CACHE-POISON-FALLE (Gemini HIGH): 'anrede' wird bei :241 via _resolve_anrede(ls,ki,sid) (:465,
    priorisiert per-SID session_anrede :495) berechnet und DOPPELT eingebacken — STABIL in Sektion 7
    (:402 `Anrede: {anrede}. WICHTIG...`) UND volatil in Sektion 9 (:452). Bleibt sie in Sek. 7,
    leakt per-SID-Anrede in den (Phase-2-)Cache-Prefix (verstoesst SPEC Req 9).
  → Block-Split: Schwester-Funktion `_profile_blocks(...)` ODER Parameter `return_blocks=True`, die
    (stabil_text, volatil_text) getrennt liefert. Trenn-Regel: STABIL = alles VOR dem Text-Anker
    '## PreCall-Briefing'; VOLATIL = ab '## PreCall-Briefing' bis Ende. NICHT ueber hartcodierte
    Sektions-Nummern splitten (Plan-Kommentare != Code). ZUSAETZLICH: die Anrede-Zeile (:402) wird
    aus dem stabil_text ENTFERNT (Anrede nur im volatil_text via Sek. 9 halten).

Rollen-/Modus-Achsen (mode_strategy.py:28-33 Caveat, gemappt):
  - Hoerbarkeit: _session_state[sid]['mode'] = 'cold_call' | 'meeting' (am Call-Start gesetzt).
  - Kontakt/Rolle: _session_state[sid]['state']['contact_category'] + ['current_mode']
    = 'gatekeeper' (Default) | 'target'  (umschaltbar). DAS ist die Rolle fuer das Rollen-Register.
  → role-Ableitung (Parameter, in P1-02 durchgereicht): mode=='meeting' -> 'meeting';
    sonst contact_category ('gatekeeper' -> gatekeeper-Ziel, 'target' -> interessent-Ziel).
    In DIESEM Plan nur die Signatur + Register-Lookup; das echte Durchreichen macht P1-02.

primary_intent-Quelle (TAXO1, gemappt — nur Signatur hier):
  - _session_state[sid]['state']['primary_intent'] (per-SID, TAXO1-Live). EIN Wert. In P1-02
    durchgereicht; hier nur der Parameter + fail-open Default bei None.

KEIN neues DB-Schema in diesem Plan. Punkt 21/22 (Persistenz/Verbindungs-Karte) daher schlank:
  build_answer_context LIEST nur bestehende, in TAXO1 verifizierte per-SID-RAM-Werte + Profil-Cache;
  es SCHREIBT nichts, legt keine Tabelle/Spalte an. (Volle Persistenz-Karte greift erst, wenn die
  Coach-Tuer-DB an der Andock-Stelle einklinkt — eigene spaetere Scheibe.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: services/answer_paradigm.py — der NERVE-Standard (André-freigegebener Text, D-01)</name>
  <files>services/answer_paradigm.py</files>
  <read_first>
    - Nerve-Vault/03 Planung/Antwort-Wissensversorgung für NERVE Entscheidungsreife Empfehlung.md
      Sektion „Empfohlene System-Prompt-Architektur" (Paradigma-Gerüst 6–10 Regeln, Rollen-Register)
      + „Recommendations Phase 1"
    - Nerve-Vault/04 Entscheidungen/NERVE TAXO-Gerüst (verriegelt).md §4.5-KORREKTUR (Stimme aus
      Stil-Deskriptoren statt Beispiele) + §4 (Antwort-Paradigma, Rollen-Grundlage)
    - CONTEXT.md D-01 (Regeln + 3 Rollen-Texte werden André vor Execute freigegeben) + D-02 (3 harte Tabus)
    - salesnerve/CLAUDE.md Umlaut-Regel (User-facing Text = echte Umlaute; Identifier = ASCII)
  </read_first>
  <action>
    NEUE Datei `services/answer_paradigm.py` mit dem NERVE-Standard als Konstanten. Der TEXT-INHALT ist
    der von André per D-01 freigegebene Wortlaut (siehe DRAFT unten — geht VOR Execute als Freigabe-Schritt
    an André, „klingt das nach mir?"). Struktur:

    ```python
    # services/answer_paradigm.py
    # NERVE-Standard-Antwort-Konfiguration (Text-in-Code, Weg 3).
    # USER-FACING WIRKUNG -> echte Umlaute im Text. dict-keys/Identifier ASCII.
    # Quelle: Gong/Voss-Recherche destilliert (Verhaltensregeln, KEINE Beispielsaetze).

    NERVE_PARADIGM: list[str] = [ ... 6–10 Regeln ... ]        # unsichtbares Geruest
    ROLE_GOALS: dict[str, str] = {'gatekeeper': ..., 'interessent': ..., 'meeting': ...}
    GROUNDING_RULE: str = ...                                   # nur aus Kontext; fehlt Fakt -> sagen + Follow-up
    INTENT_HINTS: dict[str, dict] = { '<intent_key>': {'register': ..., 'hebel': ...}, ... }
    _DEFAULT_INTENT_HINT: dict = {'register': ..., 'hebel': ...}  # fail-open fuer unbekannten Intent

    def get_answer_config() -> dict:
        """Der NERVE-Standard als ein Dict. Reiner Speicher-Zugriff, kein I/O."""
        return {'paradigm': NERVE_PARADIGM, 'roles': ROLE_GOALS,
                'grounding': GROUNDING_RULE, 'intent_hints': INTENT_HINTS,
                'default_intent_hint': _DEFAULT_INTENT_HINT}
    ```

    **INTENT_HINTS-Keys** = die TAXO1-intent_type-Taxonomie (echter_einwand / vorwand / reflexeinwand /
    kaufsignal / aufschub / info_frage / gatekeeper / wettbewerber_referenz / hard_opt_out / commitment /
    meta_kommunikation). Pro Key NUR {register: Ziel/Haltung, hebel: 1–2 Verhaltens-Hebel} — Ziel + Hebel
    aus dem Paradigma, NIE fertige Antwortsätze (Cliché-Anker, SPEC Req 2 + §4.5). Unbekannter Key →
    `_DEFAULT_INTENT_HINT` (kein KeyError).

    HARTE REGELN (SPEC Req 2 + D-02):
    - KEINE Beispiel-Antwortsätze irgendwo in dieser Datei (auch keine „so könnte man antworten"-Muster).
    - KEINE Technik-Vokabel im TEXT der vorgelesen wird (kein „Reframe"/„ANKER"/„Einwand überwinden").
      Verhaltensanweisung ja („benenne die Sorge vorsichtig, dann offene Frage"), Vokabel nein.
    - Die 3 harten Tabus (D-02) sind in NERVE_PARADIGM eingebaut: (a) kein falsches Produkt-Versprechen,
      (b) kein Druck-/Dringlichkeits-Sprech, (c) keine Mini-Romane (kurz + lieber Frage als Monolog).

    ── DRAFT für D-01-Freigabe (André editiert/bestätigt VOR Execute — dies ist mein Vorschlag) ──

    NERVE_PARADIGM (9 Regeln, unsichtbares Gerüst):
    1. Ziel ist verstehen und echt helfen — nie argumentieren, überreden oder drücken.
    2. Benenne die wahrscheinliche Sorge des Kunden vorsichtig; behaupte nie, du wüsstest was er denkt
       (kein Gedankenlesen).
    3. Bevorzuge eine kurze, offene Frage gegenüber einem Gegenargument — die Frage verlagert das
       Nachdenken zum Kunden.
    4. Halte es kurz: zwei, drei Sätze zum lauten Vorlesen; lieber eine Frage als ein Monolog.
    5. Bleib in der Stimme des Verkäufers (Stil aus dem Profil); keine Fachwörter, keine Floskeln.
    6. Antworte nur aus dem gegebenen Wissen. Fehlt ein Fakt, sag das ehrlich und schlage ein Follow-up
       vor — nie etwas erfinden.
    7. Kein Druck- oder Dringlichkeits-Sprech („nur heute", „letzte Chance", „Sie verpassen sonst").
    8. Kein falsches Produkt-Versprechen — behaupte nichts über das Produkt, das nicht in den
       hinterlegten Fakten steht.
    9. Bei als heikel/reguliert markierten Themen: vorsichtiger antworten, nie über den belegten Fakt
       hinausgehen.

    ROLE_GOALS:
    - gatekeeper: „Respekt und Ehrlichkeit. Sei knapp, sag ehrlich worum es beim Anruf geht, und bitte
      um Durchstellen oder um Rat, wer der richtige Ansprechpartner ist. Kein Einwand-Überwinden, kein
      Trick, kein ‚ich weiß, Sie wollen nur Ihren Chef schützen'."
    - interessent: „Verstehe zuerst die eigentliche Sorge, dann gib genau einen relevanten Fakt oder
      Nutzen dazu. Diagnostizieren vor Antworten."
    - meeting: „Geh tiefer; beide Seiten sind hörbar. Antworte konkret auf das, was der Kunde
      tatsächlich gesagt hat."

    GROUNDING_RULE: „Antworte ausschließlich aus dem hier gegebenen Wissen. Fehlt ein Fakt, sag offen
    dass du ihn nicht hast, und schlage eine kurze Rückfrage oder ein Follow-up vor. Erfinde nie Zahlen,
    Eigenschaften oder Versprechen. Ist ein Thema als heikel/reguliert markiert, bleib besonders
    vorsichtig und nah am belegten Fakt."
  </action>
  <verify><automated>python -c "import ast; ast.parse(open('services/answer_paradigm.py',encoding='utf-8').read())"</automated></verify>
  <acceptance_criteria>
    - Datei parst; get_answer_config() gibt ein Dict mit paradigm(list)/roles(3 keys)/grounding(str)/intent_hints(dict)/default_intent_hint.
    - grep: KEIN Few-Shot/Beispielsatz-Block; keine Technik-Vokabel im vorzulesenden Text; 3 Tabus (D-02) in NERVE_PARADIGM erkennbar.
    - Der Text entspricht dem von André per D-01 freigegebenen Wortlaut (Freigabe-Schritt VOR Execute).
  </acceptance_criteria>
  <done>answer_paradigm.py existiert mit dem freigegebenen NERVE-Standard; keine Beispielsätze; 3 Rollen + Grounding + Intent-Hinweise als Text.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: load_answer_config (Andock-Stelle) + build_answer_context (Block-Liste) in prompt_pipeline.py</name>
  <files>services/prompt_pipeline.py</files>
  <read_first>
    - services/prompt_pipeline.py 111–462 (build_profile_context: 9 REALE Sektionen — 1–7 stabil, 8 PreCall-Briefing + 9 Lead-Kontext volatil; Anrede :402 in Sek.7 = per-SID-Falle — der Wrap-Kandidat)
    - services/answer_paradigm.py (Task 1 — get_answer_config)
    - salesnerve/CLAUDE.md Punkt 14 (Control-Flow bei Insert in build_profile_context), Punkt 25 (Latenz), Punkt 27 (einfachster Weg)
  </read_first>
  <action>
    **A) load_answer_config(user_id, sid) — DIE Andock-Stelle (Weg 3):**
    ```python
    def load_answer_config(user_id: int, sid: str | None = None) -> dict:
        """Andock-Stelle fuer die Antwort-Konfiguration (Weg 3, Foundation).
        HEUTE: der NERVE-Standard aus answer_paradigm.get_answer_config() (reiner Speicher-Zugriff).
        SPAETER (Coach-Tuer, eigene Scheibe): hier laedt die training.method_packs/pack_assignments-DB
        (pack_assignment -> pack -> stiller NERVE-Default) — build_answer_context wird dafuer NICHT
        umgebaut. Foundation-Code-Register: 'TAXO3 answer-config Andock-Stelle'.
        Fail-open: jede Stoerung -> NERVE-Standard (nie raise, Live-Loop-Garantie)."""
        try:
            from services.answer_paradigm import get_answer_config
            return get_answer_config()
        except Exception as _e:
            print(f"[AnswerConfig] load fail-open -> minimal default: {type(_e).__name__}")
            return {'paradigm': ['Verstehen und helfen, nie druecken.'], 'roles': {},
                    'grounding': 'Nur aus gegebenem Wissen; fehlt der Fakt -> sagen + Follow-up.',
                    'intent_hints': {}, 'default_intent_hint': {'register': '', 'hebel': ''}}
    ```

    **B) Block-Split von build_profile_context (Punkt 14 — Insert in bestehende Funktion):**
    Erweitere build_profile_context um `return_blocks: bool = False` ODER lege eine schmale
    Schwester-Funktion `_profile_blocks(user_id, mode, sid, profile_id) -> tuple[str, str]` an, die
    (stabil_text = Sektion 1–7, volatil_text = Sektion 8–9) getrennt liefert. **Kein Verhalten der
    bestehenden Aufrufer ändern** (Default return_blocks=False → alter String-Rückgabewert unverändert
    inkl. der heutigen Anrede-Zeile in Sek. 7; die heutigen Aufrufer streame_manual/generate_qa_response
    bleiben bis P1-02 unberührt).
    Control-Flow: build_profile_context hat einen linearen Aufbau + `'\n'.join(lines)` am Ende (:462);
    KEIN early-return-Pfad betroffen. **Trenn-Regel (Gemini HIGH-Fix, gegen Code verifiziert):**
    - Split-Grenze = der Index der Zeile `'## PreCall-Briefing'` in der `lines`-Liste (Text-Anker),
      NICHT eine hartcodierte Sektions-Nummer (das Modul hat 9 Sektionen: Briefing=8/Lead=9; die
      Alt-Annahme "Sektion 10/11" war falsch). Alles davor = stabil_text, ab dort = volatil_text.
    - **ANTI-CACHE-POISON:** die per-SID Anrede-Zeile in Sektion 7 (:402,
      `Anrede: {anrede}. WICHTIG: Nutze konsequent {anrede}-Form...`) MUSS aus dem stabil_text raus
      (Gemini HIGH: _resolve_anrede priorisiert per-SID session_anrede). Umsetzung im
      return_blocks=True-Pfad: die Anrede-Zeile in Sek. 7 NICHT anhängen (Anrede bleibt ausschliesslich
      volatil in Sek. 9 :452). Der Alt-String-Pfad (return_blocks=False) behält sie unverändert.
    - Fallback: fehlt der '## PreCall-Briefing'-Anker (z.B. sid=None-Pfad ohne volatile Sektionen) →
      volatil_text = '' , alles = stabil_text (fail-open, kein raise).

    **C) build_answer_context — die EINE Funktion (Block-Liste):**
    ```python
    def build_answer_context(*, user_id: int, sid: str | None, primary_intent: str | None,
                             mode: str = 'cold_call', confidence: float | None = None,
                             role: str = 'interessent', is_auto_triggered: bool = False) -> list[dict]:
        """EINE Quelle des Antwort-System-Prompts fuer Auto/Knopf/QA (Req 1).
        Rueckgabe: LISTE von System-Bloecken (stabil vorne / volatil hinten) — cache-faehig
        STRUKTURELL angelegt, Caching NICHT aktiviert (Phase 2). MUST NOT raise (fail-open)."""
        cfg = load_answer_config(user_id, sid)
        stabil, volatil = _profile_blocks(user_id, mode, sid)   # Bestand gewrappt
        # STABIL-Block: Paradigma (6–10 Regeln) + Rollen-Ziel + Grounding + Profil-Stabilteil
        role_goal = cfg['roles'].get(role) or cfg['roles'].get('interessent', '')
        stabil_block = "\n".join([
            *cfg['paradigm'],
            f"Rolle: {role_goal}",
            cfg['grounding'],
            stabil,
        ])
        # VOLATIL-Block: EIN Intent-Hinweis + Modus/Konfidenz-Register + volatiler Profilteil
        hint = cfg['intent_hints'].get(primary_intent) or cfg['default_intent_hint']
        conf_register = _confidence_register(mode, confidence, is_auto_triggered)  # Text, kein if-Prompt-Zweig
        volatil_block = "\n".join([
            f"Erkannter Punkt: {primary_intent or '(unklar)'} — {hint.get('register','')}; {hint.get('hebel','')}",
            conf_register,
            volatil,
        ])
        return [
            {'role': 'system', 'text': stabil_block, '_layer': 'stable'},   # _layer -> Phase-2-Caching-Marker
            {'role': 'system', 'text': volatil_block, '_layer': 'volatile'},
        ]
    ```
    `_confidence_register(mode, confidence, is_auto_triggered)` liefert EINEN kurzen Register-Text
    (z.B. Cold-Call/niedrige Konfidenz → „unsicheres Signal, vorsichtiger + kürzer + eher eine Frage";
    Meeting/hohe Konfidenz → „mehr Substanz"), rein aus Parametern — KEIN `if mode==`-Zweig, der einen
    ganzen eigenen Prompt zusammenbaut (Req 6). Der Aufrufer (P1-02) reicht die Werte durch.

    **KEIN Aufruf aus einem Live-Pfad in diesem Plan** — build_answer_context existiert, wird aber erst
    in P1-02 von streame_auto/manual/QA benutzt. (Punkt 20: grep zeigt in diesem Plan 0 Live-Aufrufer —
    bewusst, P1-02 verdrahtet.)

## 1. Lokaler Kontext
    build_profile_context (prompt_pipeline.py:111–462): linear, sammelt `lines`, `return '\n'.join(lines)`
    @462. Kein early-return zwischen Sektion 9 und 10. Split an der 9/10-Grenze ist eine reine
    Listen-Teilung, kein Control-Flow-Eingriff. Bestehende Aufrufer bekommen bei return_blocks=False den
    identischen String zurück.

## 2. Funktions-Skelett
    load_answer_config: try -> get_answer_config; except -> Minimal-Default. Ein Ausgang je Zweig.
    build_answer_context: linear, kein raise (jeder Teil fail-open: fehlt cfg-Key -> .get mit Default;
    fehlt Profil -> _profile_blocks liefert leere Strings). Rueckgabe immer eine 2-Element-Liste.

## 3. Cross-File-Awareness
    - 'build_profile_context' : prompt_pipeline.py:111 (def) + claude_service.py:800 + qa_pipeline.py:425
      (Aufrufer). return_blocks-Default=False -> Aufrufer UNVERAENDERT (Beweis: grep zeigt kein Aufrufer
      mit return_blocks=True in diesem Plan; die kommen in P1-02).
    - 'build_answer_context' : NEU. grep muss 0 Live-Aufrufer zeigen (nur Test) — P1-02 verdrahtet.
    - 'get_answer_config' / 'answer_paradigm' : nur Task 1 + load_answer_config.

## 4. Edge-Case-Analyse
    - primary_intent None ODER unbekannter Key -> default_intent_hint (kein KeyError).
    - role unbekannt -> Fallback interessent-Ziel.
    - Profil fehlt (sid ohne Cache) -> _profile_blocks liefert ('','') -> Bloecke tragen nur
      Paradigma/Grounding (fail-open, kein raise).
    - confidence None -> _confidence_register neutraler Default-Text.
    - Split-Anker '## PreCall-Briefing' fehlt (kein volatiler Teil, z.B. sid=None) -> volatil_text=''
      + alles im stabil_text (fail-open). Anrede-Zeile aus Sek. 7 trotzdem NICHT im Stabil-Block
      (per-SID -> gehoert nie in den Cache-Prefix, auch nicht wenn Sek. 9 fehlt).
    ### Race-Condition-Audit (Punkt 14, 4 Fragen)
    1. Gleichzeitiges Feuern: build_answer_context ist ZUSTANDSLOS (liest nur, schreibt nichts) ->
       parallele Aufrufe unkritisch. (Das Durchreichen der per-SID-Werte macht P1-02; dort greift Punkt 26.)
    2. Verbindungsabbruch: reine Funktion, kein Halbzustand.
    3. Reconnect-Replay: liest zum Aufruf-Zeitpunkt frische per-SID-Werte (in P1-02) — hier ohne Live-Bezug.
    4. Tab-Hintergrund: irrelevant (kein Timer/State).
    („Nicht relevant" belastbar: reine, zustandslose Assembly-Funktion ohne Schreib-Pfad.)
  </action>
  <verify><automated>python -c "import ast; ast.parse(open('services/prompt_pipeline.py',encoding='utf-8').read())"</automated></verify>
  <acceptance_criteria>
    - grep: `def load_answer_config` + `def build_answer_context` in prompt_pipeline.py; build_answer_context ruft load_answer_config + build_profile_context (bzw. _profile_blocks).
    - grep: 0 Live-Aufrufer von build_answer_context (nur Test) — bewusst, P1-02 verdrahtet.
    - Bestehende build_profile_context-Aufrufer unverändert (return_blocks default False).
  </acceptance_criteria>
  <done>load_answer_config (Andock-Stelle) + build_answer_context (Block-Liste stabil/volatil) existieren, fail-open, ohne Live-Aufrufer; Block-Split von build_profile_context ohne Verhaltensänderung der Alt-Aufrufer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: tests/test_build_answer_context.py — Pure-Logic</name>
  <files>tests/test_build_answer_context.py</files>
  <action>
    Reine Logik-Tests (Runtime-Verhalten, kein Source-Presence — CLAUDE.md Test-Regel):
    - build_answer_context gibt eine Liste mit >=2 Blöcken; Block[0]._layer=='stable' VOR Block[1]._layer=='volatile'.
    - fail-open: Aufruf ohne Profil (sid=None, kein Cache) + unbekannter primary_intent → kein raise,
      Blöcke enthalten das Paradigma + Grounding-Regel.
    - EIN Intent: Signatur nimmt genau EIN primary_intent (kein list/top-2); unbekannter Intent-Key →
      default_intent_hint (kein KeyError).
    - Rolle als Parameter: role='gatekeeper' → Stabil-Block enthält das Gatekeeper-Ziel-Wort
      („Durchstellen"/„Respekt"); role unbekannt → interessent-Fallback.
    - Modus/Konfidenz als Parameter: niedrige confidence im cold_call → Register-Text signalisiert
      „vorsichtiger/kürzer" (Assertion auf den conf_register-Text), OHNE dass ein mode-spezifischer
      Prompt-Zweig existiert.
    - Grounding-Regel im Output (Assertion: GROUNDING-Kernsatz in einem Block).
    - KEIN Few-Shot/Beispielsatz: Assertion dass kein Block eine „Beispiel:"-/Muster-Antwort-Sektion trägt.
    - ANTI-CACHE-POISON (Gemini HIGH): _profile_blocks-Test mit einem Profil, das eine per-SID
      session_anrede setzt (z.B. „Du") + ein PreCall-Briefing + Lead-Kontext → Assertion: der
      Stabil-Block enthält WEDER die session_anrede noch Briefing/Lead; der Volatil-Block enthält
      Anrede + Briefing + Lead. Beweist: kein per-SID-Datum im (Phase-2-)Cache-Prefix (SPEC Req 9).
    - Split-Anker: Assertion dass der Stabil-Block VOR „## PreCall-Briefing" endet und der Volatil-Block
      mit „## PreCall-Briefing" beginnt (Text-Anker, nicht Sektions-Nummer).
    Monkeypatch build_profile_context/_profile_blocks auf feste Test-Strings (kein DB, kein Live).
    Für den Anti-Cache-Poison-Test _profile_blocks NICHT wegmonkeypatchen — echten Split über
    Test-`lines` mit realer Sektions-7-Anrede-Zeile + '## PreCall-Briefing'-Anker prüfen.
  </action>
  <verify><automated>bash deploy.sh production  # server-seitiges Pytest-Gate laeuft test_build_answer_context mit</automated></verify>
  <acceptance_criteria>
    - Test grün im Deploy-Gate (server-seitig). Deckt Block-Form, fail-open, EIN-Intent, Rolle/Modus/Konfidenz-Parameter, Grounding, kein-Few-Shot, ANTI-CACHE-POISON (kein per-SID-Datum/keine Anrede im Stabil-Block) + Split am Text-Anker „## PreCall-Briefing".
  </acceptance_criteria>
  <done>Unit-Test grün; die neue Funktion ist verhaltensgeprüft, bevor P1-02 sie live schaltet.</done>
</task>

</tasks>

<verification>
- grep: answer_paradigm.py (NERVE-Standard, keine Beispielsätze) + load_answer_config (Andock-Stelle) + build_answer_context (Block-Liste) existieren; 0 Live-Aufrufer von build_answer_context (P1-02 verdrahtet).
- Unit-Test grün im Prod-Deploy-Gate (reine Logik, kein Test-Anruf nötig — keine Live-Wirkung).
- CACHE-SICHERHEIT (Cross-AI Gemini HIGH-Fix): Block-Split trennt am Text-Anker „## PreCall-Briefing" (Stabil=Sek.1–7, Volatil=Sek.8–9); die per-SID Anrede-Zeile (Sek.7 :402) ist NICHT im Stabil-Block; Test beweist kein per-SID-Datum (anrede/briefing/lead) im Cache-Prefix (SPEC Req 9).
- Foundation-Code-Register-Eintrag „TAXO3 answer-config Andock-Stelle (Weg 3)" gesetzt.
- KEIN neues DB-Schema; kein Live-Antwort-Pfad geändert (Beleg: claude_service.py unverändert in diesem Plan).
</verification>

<success_criteria>
- Die EINE build_answer_context() existiert testbar (Block-Liste stabil/volatil, cache-fähig angelegt, Caching aus).
- Der Block-Split ist cache-SAUBER: kein per-SID-Datum (Anrede/Briefing/Lead) im Stabil-Block; Split am Text-Anker „## PreCall-Briefing", 9-Sektionen-Modell korrekt (Cross-AI Gemini HIGH-Fix, 2026-07-01).
- Der NERVE-Standard-Text (Paradigma/Rollen/Grounding) liegt als freigegebener Text-in-Code vor (D-01).
- Weg-3-Andock-Stelle steht + ist als Foundation dokumentiert (Coach-Tür klinkt später ohne Umbau ein).
- Kein Live-Verhalten geändert — der riskante Cutover ist sauber auf P1-02 isoliert.
</success_criteria>

<output>
SUMMARY: `08.23.2.TAXO3-P1-01-SUMMARY.md`. Pflicht: (a) Foundation-Register-Eintrag-Verweis
(Andock-Stelle); (b) Bestätigung 0 Live-Aufrufer von build_answer_context (Cutover = P1-02);
(c) der finale, von André freigegebene Paradigma/Rollen-Text (D-01) als Beleg.
</output>
