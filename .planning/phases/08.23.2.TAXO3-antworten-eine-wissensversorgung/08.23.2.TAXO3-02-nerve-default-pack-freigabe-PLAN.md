---
phase: 08.23.2.TAXO3
plan: 02
type: execute
wave: 2
complexity: 🔴
depends_on: [01]
files_modified:
  - alembic/versions/XXXX_seed_nerve_default_method_pack.py
  - tests/test_nerve_default_pack_seed.py
autonomous: false
requirements: ["SPEC Req 2", "SPEC Req 3", "SPEC Req 4 (Default-Inhalt)"]
risiko: daten-seed
test_anruf: false
user_setup: []

must_haves:
  truths:
    - "Genau EIN NERVE-Default method_pack (owner='NERVE', schema_version=1, is_active) liegt in public.method_packs auf Production — der stille Default-Fallback (E2), wenn keine/abgelaufene Zuweisung greift"
    - "Der content-JSONB enthält: 6-10 unsichtbare Paradigma-Regeln (verstehen+diagnostizieren+helfen+rollen-angemessen, KEIN bekämpfen→Reframe→Close), die 3 harten Tabus (D-02), die 3 Rollen-Texte (gatekeeper=Respekt+Ehrlichkeit / interessent=Sorge+Fakt / meeting=tiefer), Intent→Technik-Hebel — ALLES als Stil-Deskriptoren, KEINE Few-Shot-Beispielsätze (Req 2, §4.5-KORREKTUR)"
    - "Der destillierte Text wurde André VOR dem Seed als fertiger Block zur Freigabe vorgelegt (D-01 'klingt das nach mir?') und freigegeben — bevor er in die DB geht"
    - "Der Default ist standalone vollwertig (Rangfolge User-Stimme > Paket > NERVE-Default; NERVE-Default allein funktioniert)"
  artifacts:
    - path: "alembic/versions/XXXX_seed_nerve_default_method_pack.py"
      provides: "Idempotenter Seed (ON CONFLICT/IF NOT EXISTS) des EINEN NERVE-Default method_pack mit dem André-freigegebenen content-JSONB"
      contains: "INSERT INTO method_packs"
    - path: "tests/test_nerve_default_pack_seed.py"
      provides: "Genau-EIN-Default-Test + content-Struktur-Test (paradigm_rules>=6, 3 roles, 3 taboos, KEIN examples-Key)"
  key_links:
    - from: "Andre-Freigabe (D-01 Text-Block)"
      to: "public.method_packs.content (der EINE NERVE-Default)"
      via: "Seed-Migration nach Freigabe"
      pattern: "paradigm_rules"
    - from: "build_answer_context (Plan 03, Fallback)"
      to: "der EINE NERVE-Default method_pack"
      via: "stiller Default wenn keine/abgelaufene pack_assignment"
      pattern: "owner.*NERVE"
---

> RISIKO-BANNER: **daten-seed.** KEIN Code-Pfad, KEIN Live-Antwort-Pfad — reiner Daten-Insert (EIN Paket) NACH André-Freigabe. KEIN Test-Anruf (RESEARCH §Wave-Cut W0b — Daten, kein Live-Anbindung). ABER: der content ist die WURZEL des Paradigma-Resets (Req 2 Haupt-Müll-Ursache) — der Freigabe-Schritt ist die Qualitäts-Sicherung.

> ⚠️ **D-01-FREIGABE-GATE (autonomous:false):** Die 6-10 Paradigma-Regeln + 3 Rollen-Texte + 3 Tabus werden von Claude aus den Quell-Docs DESTILLIERT und André als fertiger Text vorgelegt ("klingt das nach mir?"). André formuliert NICHT selbst — sein Bauch-Check ist die Sicherheits-Schicht. Erst NACH Freigabe geht der Text in die DB. Quelle = Stil-Deskriptoren, NIE Few-Shot-Beispielsätze (Req 2 + §4.5-KORREKTUR Punkt 1).

> ⚠️ **CROSS-AI-GATE (🔴):** Geplant, nicht sofort ausgeführt. Der destillierte Paradigma-Text geht zusätzlich durch Cross-AI (Gemini-Gegenleser: klingt das manipulativ? bekämpfen-Restspuren? Few-Shot-Anker versteckt?) BEVOR André-Freigabe — die 3 Sichten (Punkt 24) auf den Ton-Kern.

<objective>
Destilliere den NERVE-Default method_pack-Inhalt (Paradigma-Gerüst + Rollen-Register + Tabus) aus den verriegelten Quell-Docs, lege ihn André zur Freigabe vor (D-01), und seede dann genau EIN Paket in `public.method_packs`. Das ist der **stille Default-Fallback** (E2) und gleichzeitig der **Paradigma-Reset-Kern** (Req 2): die Daten, aus denen `build_answer_context()` (Plan 03) den Ton zieht — verstehen statt bekämpfen, rollen-angemessen, ohne Few-Shot-Anker.

Purpose: Plan 03 (Ton-Welle) lädt das Paket und baut daraus den System-Prompt. Ohne den freigegebenen Default-Inhalt hätte build_answer_context kein Paradigma. Der Inhalt ist Daten (kein Code) → eigene Welle, eigener Freigabe-Schritt (D-01, Anti-Abrieb D-06).
Output: EIN NERVE-Default method_pack live (André-freigegeben), Seed-Migration (idempotent), Struktur-Test.

KEINE build_answer_context (Plan 03). KEIN Live-Pfad. KEINE echten Produkt-Fakten (product_facts bleibt leer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen. STATE/ROADMAP hand-editieren.

**Verify = Production only:** `bash deploy.sh production` + `inspect.sh`. Seed-Migration läuft wie alle public-Tabellen-Migrationen (normaler Alembic-Lauf — public.method_packs ist App-Eigentum nach Plan 01, OQ-1 = Option B; KEIN postgres-Sonderweg, der war nur fürs training-Schema nötig). KEIN lokales pytest als Acceptance.

**Migrations-Revision (HART):** down_revision = Plan-01-Migration (method_packs muss existieren). Beim Execute `inspect.sh migrations` lesen.

**UTF-8-Regel:** content-JSONB ist USER-FACING-Wirkung (wird Teil der vorgelesenen Antwort) → echte Umlaute im Text-Inhalt (ä/ö/ü/ß) erlaubt/erwünscht. JSONB-KEYS (paradigm_rules/roles/taboos/ziel/hebel) ASCII.

**Quell-Docs liegen im Vault (außerhalb Repo):** `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\`. Der Executor liest sie zum Destillieren; sie sind NICHT im git-Repo.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md

<interfaces>
<!-- content-JSONB-Form (E3, aus Plan 01 — KEIN examples-Feld). -->
{
  "schema_version": 1,
  "paradigm_rules": [ "<6-10 Verhaltens-Regeln, Stil-Deskriptoren, unsichtbar>" ],
  "taboos": [
    "<D-02a kein falsches Produkt-Versprechen — fehlt der Fakt: sagen + Follow-up>",
    "<D-02b kein Druck-/Drang-Sprech ('nur heute'/'letzte Chance'), auch nicht beim Gatekeeper>",
    "<D-02c keine Mini-Romane — kurz genug zum Vorlesen, lieber eine offene Frage als Monolog>"
  ],
  "roles": {
    "gatekeeper":  {"ziel": "Respekt + Ehrlichkeit (knapp, um Durchstellen/Rat bitten, KEIN Einwand-Ueberwinden, KEIN 'ich weiss, Sie wollen Ihren Chef schuetzen'-Klang)", "hebel": ["..."]},
    "interessent": {"ziel": "Sorge diagnostizieren + relevanter Fakt", "hebel": ["..."]},
    "meeting":     {"ziel": "tiefer, beide Seiten hoerbar", "hebel": ["..."]}
  },
  "intents": { "<intent_type>": {"ziel": "...", "hebel": ["..."], "fact_ids": []} }
}
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `public.method_packs` (INSERT, EIN Default) — existiert nach Plan 01

### inspect.sh-Beleg
```
$ inspect.sh count method_packs   →  0   (Plan 01 lieferte leer; beim Execute neu ziehen — muss 0 sein VOR Seed)
$ inspect.sh schema method_packs  →  (Plan-01-Schema: id|name|owner|schema_version|content(jsonb)|is_active|valid_until|created_at)
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Schreib-/Lese-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `content.paradigm_rules` | gelesen build_answer_context (Plan 03) | DB public.method_packs.content JSONB | ⬜ Task 3 Struktur-Test |
| `content.roles.{gatekeeper,interessent,meeting}` | Rollen-Lookup (Plan 03, Req 3) | content JSONB | ⬜ Task 3 |
| `content.taboos` (D-02) | Tabu-Anweisung im Prompt (Plan 03) | content JSONB | ⬜ Task 3 |
| `owner='NERVE'` (Default-Marker) | Fallback-Selektion (Plan 03) | DB method_packs.owner | ⬜ Task 3 genau-EIN-Test |

## Verbindungs-Karte (Punkt 22)
- **method_packs.content** = Plan-01-Schema (JSONB, KEIN examples-Feld). Dieser Plan füllt EINE Zeile (owner='NERVE', is_active). grep `grep -rn "owner.*NERVE\|method_packs" services/ routes/` == 0 Live-Leser (Loader = Plan 03).
- **Genau EIN Default:** vor Seed `inspect.sh count method_packs` == 0; nach Seed == 1; idempotent (re-run fügt nicht doppelt ein — ON CONFLICT auf einem stabilen Schlüssel, z.B. name='NERVE-Default-v1' UNIQUE ODER WHERE NOT EXISTS).

<tasks>

<task type="auto">
  <name>Task 1: Paradigma + Rollen + Tabus aus Quell-Docs destillieren (Text-Artefakt, NOCH NICHT in DB)</name>
  <read_first>
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\04 Entscheidungen\NERVE TAXO-Geruest (verriegelt).md §4.5-KORREKTUR (Stimme aus Stil-Deskriptoren) + §4 (Antwort-Paradigma, Rollen-Register-Grundlage) + §0.1
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\Antwort-Wissensversorgung fuer NERVE Entscheidungsreife Empfehlung.md (Paradigma-Reset Gong/Voss, Rollen-Register Gatekeeper Respekt+Ehrlichkeit)
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md <decisions> D-01/D-02 (verriegelte Rollen-Ziele + 3 Tabus)
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md Req 2/3
  </read_first>
  <action>
    Destilliere (NICHT erfinden — aus den Quell-Docs ableiten) und schreibe als fertiges Text-Artefakt in `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-DEFAULT-PACK-DRAFT.md`:
    - **6-10 Paradigma-Regeln** (unsichtbare Verhaltensanweisungen, Stil-Deskriptoren): „verstehen + diagnostizieren + helfen + rollen-angemessen". Drehung WEG von „Einwand bekämpfen → Reframe → Gegenargument → Close". Technik (Gong/Voss-Haltung) als Verhaltensanweisung („benenne die wahrscheinliche Sorge vorsichtig, dann stelle eine offene Frage"), NIE als Vokabel in der Antwort. KEINE Beispielsätze, KEINE Gegenargumente-als-Vorlage.
    - **3 harte Tabus (D-02):** (a) kein falsches Produkt-Versprechen (fehlt der Fakt → sagen + Follow-up); (b) kein Druck-/Drang-Sprech (keine „nur heute"/„letzte Chance", auch nicht Gatekeeper); (c) keine Mini-Romane (kurz zum Vorlesen, lieber offene Frage als Monolog).
    - **3 Rollen-Texte (verriegelte Ziele, Req 3):** gatekeeper = Respekt + Ehrlichkeit (knapp, um Durchstellen/Rat bitten, KEIN Einwand-Überwinden, KEIN „ich weiß, Sie wollen Ihren Chef schützen"-Klang); interessent = Sorge diagnostizieren + relevanter Fakt; meeting = tiefer, beide Seiten hörbar. Je Rolle ziel + 1-2 Verhaltens-Hebel (Deskriptoren).
    - Format: lesbarer deutscher Klartext (für André) PLUS die finale JSONB-Struktur (für den Seed). Beide im Draft.
    Schreibe deutlich dazu: „KEINE Few-Shot-Beispielsätze enthalten — Stimme kommt aus Profil-Stil-Deskriptoren (Plan 03) + diesem Paradigma."
  </action>
  <verify>
    <automated>grep -c "paradigm_rules\|gatekeeper\|interessent\|meeting" .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-DEFAULT-PACK-DRAFT.md</automated>
  </verify>
  <acceptance_criteria>
    - DRAFT enthält 6-10 Paradigma-Regeln (Klartext + JSONB), 3 Tabus, 3 Rollen-Texte mit verriegelten Zielen
    - KEINE Few-Shot-Beispielsätze / keine „so antwortest du:"-Muster (manuelle Sicht + grep kein „Beispiel:"/„z.B. antworte")
    - gatekeeper-Text trägt KEIN Überreden / kein „ich weiß, Sie wollen Ihren Chef schützen"-Klang
    - Drehung belegt: kein „bekämpfen/Reframe/Close" als Anweisung (verstehen/diagnostizieren/helfen)
  </acceptance_criteria>
  <done>Destillierter Default-Pack-Text als Draft-Artefakt (Klartext + JSONB), noch nicht in DB, noch nicht freigegeben.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: André-Freigabe des Paradigma/Rollen-Texts (D-01 'klingt das nach mir?')</name>
  <what-built>
    Der destillierte NERVE-Default-Inhalt (6-10 Paradigma-Regeln + 3 Tabus + 3 Rollen-Texte) als fertiger Klartext-Block in 08.23.2.TAXO3-DEFAULT-PACK-DRAFT.md. ZUSÄTZLICH: Cross-AI-Gegenlesung (Gemini) auf manipulativen Klang / bekämpfen-Restspuren / versteckte Few-Shot-Anker ist erfolgt und die Findings sind eingearbeitet (Punkt 24, 3 Sichten).
  </what-built>
  <how-to-verify>
    1. Lies den Klartext-Block in 08.23.2.TAXO3-DEFAULT-PACK-DRAFT.md.
    2. Prüfe pro Rolle: klingt das nach DIR (André)? Gatekeeper respektvoll-ehrlich (nicht überredend)? Interessent diagnostisch? Meeting tiefer?
    3. Prüfe die Tabus: kein Druck-Sprech, keine Mini-Romane, kein falsches Produkt-Versprechen — passt das zu NERVEs Ton?
    4. Prüfe: keine Beispielsätze als „Vorlage" (das wäre der alte Cliché-Anker).
  </how-to-verify>
  <resume-signal>Tippe "freigegeben" ODER beschreibe konkret, was anders klingen soll (Claude überarbeitet den Draft und legt erneut vor — Schleife bis Freigabe).</resume-signal>
  <acceptance_criteria>
    - André-Freigabe explizit dokumentiert (SUMMARY + Nerve-Vault/05 Log): freigegebener Text-Stand + Datum
    - Cross-AI-Gegenlesung (Gemini) auf Ton dokumentiert (Findings + Disposition)
    - KEIN Seed (Task 3) vor dieser Freigabe
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Seed-Migration — genau EIN NERVE-Default method_pack (idempotent) + Struktur-Test</name>
  <files>alembic/versions/XXXX_seed_nerve_default_method_pack.py, tests/test_nerve_default_pack_seed.py</files>
  <read_first>
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-DEFAULT-PACK-DRAFT.md (der FREIGEGEBENE content — verbatim in den Seed)
    - alembic/versions/0013_*.py (training-Seed/Daten-Migrations-Muster, idempotent)
    - ssh ... 'bash scripts/inspect.sh migrations' (echter HEAD = down_revision; method_packs muss existieren)
  </read_first>
  <action>
    Seed-Migration (als postgres). `down_revision` = Plan-01-Migration (beim Execute via inspect.sh bestätigen).
    upgrade():
    - `INSERT INTO public.method_packs (name, owner, schema_version, content, is_active) VALUES ('NERVE-Default-v1', 'NERVE', 1, '<FREIGEGEBENES JSONB>'::jsonb, true)` — idempotent: `WHERE NOT EXISTS (SELECT 1 FROM public.method_packs WHERE name='NERVE-Default-v1')` ODER ON CONFLICT (falls name-UNIQUE). content = der von André freigegebene JSONB aus dem Draft (verbatim, mit echten Umlauten im Text-Inhalt, ASCII-Keys).
    downgrade(): `DELETE FROM public.method_packs WHERE name='NERVE-Default-v1' AND owner='NERVE'`.

    Test `tests/test_nerve_default_pack_seed.py` (Runtime-Behavior):
    - `test_exactly_one_nerve_default`: nach Seed `SELECT count(*) FROM public.method_packs WHERE owner='NERVE' AND is_active` == 1.
    - `test_default_content_structure`: content hat `paradigm_rules` (len >= 6), `taboos` (len == 3), `roles` mit Keys gatekeeper/interessent/meeting (je `ziel`), und KEINEN Key `examples`/`beispiele`/`musterantworten` (E3).
    - `test_seed_idempotent`: Seed-Logik zweimal → weiterhin genau 1 (kein Doppel).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u nerve_app NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest tests/test_nerve_default_pack_seed.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh count method_packs` == 1 nach Seed; `WHERE owner='NERVE' AND is_active` == 1
    - test_exactly_one_nerve_default + test_default_content_structure + test_seed_idempotent grün
    - content enthält paradigm_rules(>=6) + taboos(3) + roles(gatekeeper/interessent/meeting mit ziel), KEIN examples-Key
    - Seed-content == André-freigegebener Draft-Stand (verbatim-Abgleich im SUMMARY)
  </acceptance_criteria>
  <done>Genau EIN André-freigegebener NERVE-Default live, idempotent geseedet, Struktur-Test grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| method_pack content (JSONB) → vorgelesene Antwort (Plan 03) | der Default-Inhalt wird Teil dessen, was NERVE laut sagt — Ton-/Manipulations-Fläche |
| Seed-Migration (normaler Alembic-Lauf, public-App-Eigentum) → public.method_packs | Daten-Insert ins public-Schema (OQ-1 b, kein Sonderrecht, kein training-Bezug) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3-02-01 | T (Tampering / Ton) | Default-Paradigma enthält manipulativen/bekämpfen-Klang → NERVE klingt manipulativ | mitigate | D-01 André-Freigabe ("klingt das nach mir?") + Cross-AI-Gegenlesung (Gemini) auf Ton VOR Seed. Acceptance: kein bekämpfen/Reframe/Close, Gatekeeper respektvoll. |
| T-TAXO3-02-02 | T (Tampering) | versteckter Few-Shot-Anker (Gegenargument-als-Vorlage) im content → Cliché-Output (Req 2) | mitigate | E3-Schema hat KEIN examples-Feld; Task-1-Acceptance + Task-3 test_default_content_structure prüfen kein examples-Key; Stil = Deskriptoren. |
| T-TAXO3-02-03 | T (Doppel-Seed) | mehrfacher Default → Fallback-Ambiguität (welches Paket gewinnt?) | mitigate | Idempotenter Seed (WHERE NOT EXISTS/ON CONFLICT); test_exactly_one_nerve_default + test_seed_idempotent. |
| T-TAXO3-02-04 | I | content speichert versehentlich echte Produkt-Fakten/PII | accept | Default ist Paradigma/Rollen (kein Produkt-Fakt); product_facts bleibt leer (Req 7 nur Struktur). André-Freigabe fängt Versehen. |
</threat_model>

<verification>
- `inspect.sh count method_packs` == 1 (genau EIN Default).
- content-Struktur: paradigm_rules>=6, taboos==3, roles{gatekeeper,interessent,meeting}, KEIN examples (test).
- André-Freigabe + Cross-AI-Ton-Gegenlesung im SUMMARY + 05-Log dokumentiert.
- KEIN Live-Pfad berührt; KEIN Test-Anruf (Daten-Welle, RESEARCH §Wave-Cut W0b).
</verification>

<success_criteria>
- EIN NERVE-Default method_pack live, André-freigegeben (D-01), standalone vollwertig, kein Few-Shot-Anker, idempotent.
- Paradigma-Reset-Kern als Daten bereit für Plan 03 (Ton-Welle).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-02-SUMMARY.md` (inkl. André-Freigabe-Stand + Datum, Cross-AI-Ton-Findings, `inspect.sh count method_packs`==1, content-Struktur-Beleg, echter Migrations-Revision).
</output>
