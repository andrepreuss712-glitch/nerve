---
phase: 08.23.2.TAXO3
plan: P1-02
type: execute
wave: 2
complexity: 🔴
depends_on: [P1-01]
files_modified:
  - services/claude_service.py
  - services/qa_pipeline.py
  - services/deepgram_service.py
autonomous: false
risiko: riskant
test_anruf: true   # lenkt ALLE Live-Antwort-Pfade um -> Pflicht-Test-Anruf + TTFT-Basislinie (D-05)
requirements: ["SPEC Req 1 (Cutover)", "SPEC Req 2 (Müll+Few-Shot löschen)", "SPEC Req 3 (Rolle durchreichen)", "SPEC Req 5 (EIN Intent)", "SPEC Req 6 (Modus/Konfidenz-Parameter, Auto-Trigger bleibt)", "SPEC Req 7 (Grounding-Regel live)"]

must_haves:
  truths:
    - "streame_auto_variante (claude_service.py:572) + streame_manual_ewb_variante (:752) + generate_qa_response (qa_pipeline.py:389, via _qa_pipeline_dispatch) beziehen ihren System-Prompt aus build_answer_context() (P1-01) — EINE Quelle (Req 1)."
    - "Der kontext-arme _ewb_autovar_system-String (claude_service.py:622) ist GELÖSCHT; der Auto-Pfad ist nicht mehr kontext-arm (Req 1/2). (Auto-Pfad feuert nach PIP 2026-06-30 nur noch im Meeting — build_answer_context gilt trotzdem.)"
    - "Few-Shot/Anker RAUS: die Auto-Injektion 'Hinterlegte Einwand-Gegenargumente (als Orientierung)' (auto user_msg ~:603) UND die manual standard_ga-Injektion (:771) sind entfernt (Req 2). Die einwaende-Liste bleibt Profil-Kontext, ist aber KEINE Vorlage/Intent-Quelle mehr."
    - "Genau EIN deterministischer primary_intent geht ans LLM (Req 5, kein Top-2): keyword-Trigger → der gematchte match['matched_label'] (vom deepgram-Caller durchgereicht); analyse_loop/QA → _session_state[sid]['state']['primary_intent'] (TAXO1-Live). KEINE 'erste-aus-einwaende-Liste'-Heuristik."
    - "Modus + Konfidenz + Rolle gehen als PARAMETER in build_answer_context (Req 3/6). role aus _session_state[sid]['state']['contact_category']/mode abgeleitet; is_auto_triggered=True KONSTANT im Auto-Pfad (beide Trigger), False im Manual-Pfad. KEIN if mode==-Prompt-Zweig."
    - "Grounding-Regel ist live im zusammengebauten Prompt (Req 7). KEINE Live-Web-Recherche (grep). product_facts-Tabelle NICHT gebaut (deferred); die Grounding-Regel referenziert die vorhandene Profil-FAQ."
    - "Auto-Trigger-Verhalten bleibt (nur der kontext-arme Prompt ist ersetzt, nicht das Selber-Feuern). Modell/Caching UNVERÄNDERT (= Phase 2). Slot A unverändert lokal <1s."
    - "Async-Bereitschaft (Punkt 26): primary_intent wird zum Antwort-Zeitpunkt gelesen — belegt dass der Schreiber (analysiere_mit_claude/keyword) VOR dem Antwort-Aufruf geschrieben hat; laeuft der Leser voraus → fail-open Default-Intent, NIE falscher Intent."
    - "Pflicht-Test-Anruf (Auto[Meeting]+Knopf+QA): alle tragen Profil-Persona+Stimme; Gatekeeper-Antwort respektvoll; keine Technik-Vokabel; kein manipulativer Klang; Grounding greift (fehlt Fakt → Rückfrage). TTFT-BASISLINIE gemessen ([EWB-TTFT]) — NICHT festgezurrt (Phase 2)."
  artifacts:
    - path: "services/claude_service.py"
      provides: "streame_auto_variante + streame_manual_ewb_variante ziehen System-Prompt aus build_answer_context; _ewb_autovar_system gelöscht; Few-Shot-Injektionen raus; primary_intent/mode/confidence/role/is_auto_triggered durchgereicht"
      contains: "build_answer_context"
    - path: "services/qa_pipeline.py"
      provides: "generate_qa_response zieht System-Prompt aus build_answer_context statt _SYSTEM_PROMPT_QA-Selbstbau; die 'zwingt'-Formulierung (drückerisch) entfernt; Grounding-Regel kommt aus build_answer_context"
      contains: "build_answer_context"
    - path: "services/deepgram_service.py"
      provides: "Keyword-Pfad reicht match['matched_label'] als primary_intent an streame_auto_variante durch (deterministisch, Req 5)"
      contains: "matched_label"
  key_links:
    - from: "streame_auto_variante / streame_manual_ewb_variante / generate_qa_response"
      to: "build_answer_context() (P1-01, EINE Quelle)"
      via: "System-Prompt-Bezug statt Eigen-Assembly"
      pattern: "build_answer_context"
    - from: "deepgram Keyword-Pfad (match['matched_label']) + _session_state[sid]['state']['primary_intent']"
      to: "build_answer_context(primary_intent=...) (EIN Intent, Req 5)"
      via: "deterministische Durchreichung"
      pattern: "primary_intent"
    - from: "_session_state[sid]['state']['contact_category'] + ['mode']"
      to: "build_answer_context(role=..., mode=...) (Rolle/Modus als Parameter, Req 3/6)"
      via: "Parameter-Ableitung, kein Code-Zweig"
      pattern: "contact_category"
---

> 🔴 **CROSS-AI-GATE:** Dieser Plan lenkt ALLE Live-Antwort-Pfade um + löscht den Auto-Müll + dreht das
> Paradigma. Gemini-Cross-AI (Punkt 24) + Claudian-Pre-Execute-Audit PFLICHT vor Execute. Deploy + Test-
> Anruf erst nach Clearance.

> 🔎 **VOR-EXECUTE-RE-GREP (Anker, Stand 2026-07-01 gemappt):**
> `grep -n "_ewb_autovar_system\|def streame_auto_variante\|def streame_manual_ewb_variante\|standard_ga\|Hinterlegte Einwand-Gegenargumente\|_SYSTEM_PROMPT_QA\|primary_intent\|matched_label\|contact_category" services/claude_service.py services/qa_pipeline.py services/deepgram_service.py`
> — Zeilen abgleichen (Stand: claude :572/:622/:752/:771/:603; qa_pipeline :78/:389/:431; primary_intent per-SID). PIP 2026-06-30 hat den deepgram-Keyword-Block (Cold-Call-Gate) angefasst → Anker neu ziehen.

> ⚠️ **ANTI-ABRIEB + LATENZ (Punkt 25, ehrlich):** Der Auto-Pfad bekommt jetzt VOLLEN Profil-Kontext
> (vorher 14-Token-Prompt). Das VERGRÖSSERT den Auto-Prompt → höhere TTFT auf dem Auto-Pfad, SOLANGE
> Caching aus ist (Phase 2 aktiviert es). Abgefedert: (1) Cold-Call-Auto ist seit PIP AUS → Auto feuert
> nur im Meeting; (2) Knopf + QA hatten schon vollen Kontext; (3) Slot A <1s überbrückt. **Phase 1 MISST
> die TTFT-Basislinie, zurrt sie NICHT fest** (D-06). Modell/Caching bleiben unverändert — kein Sonnet-
> Umbau, kein cache_control-Layering hier.

<objective>
Schalte den Paradigma-Reset scharf: lenke die 3 Live-Antwort-Pfade (Auto, Knopf, QA) auf die EINE
`build_answer_context()` aus P1-01, lösche den kontext-armen Auto-Müll-String + die Few-Shot-Anker,
reiche genau EINEN primary_intent + Modus/Konfidenz/Rolle als Parameter durch, und bring die Grounding-
Regel live. Das ist der größte Hebel gegen den „Müll" (Req 1+2+3+5+6+7).

Purpose: kein Antwort-Pfad mehr ohne Profil-Stimme; Grund-Paradigma dreht von „bekämpfen/überzeugen"
auf „verstehen/diagnostizieren/helfen/rollen-angemessen"; Technik unsichtbar; EIN Intent; Modus/Rolle/
Konfidenz als Parameter; Grounding gegen Halluzination.

Output: 3 Pfade ziehen aus build_answer_context; _ewb_autovar_system + Few-Shot tot; EIN Intent
deterministisch; Rolle/Modus/Konfidenz Parameter; Grounding live; Pflicht-Test-Anruf-Beleg + TTFT-
Basislinie.

NICHT hier: Caching/Sonnet/Circuit-Breaker-Umbau (Phase 2); Slot-B-Dedup (deferred); Coach-Tür-DB
(deferred); product_facts-Tabelle (deferred); Slot A (unverändert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT `.../08.23.2.TAXO3-antworten-eine-wissensversorgung/`.
gsd-tools/gsd-code-review/gsd-verifier NICHT aufrufen. STATE/ROADMAP hand-editieren.

**Verify = Production only (CLAUDE.md HART):** `bash deploy.sh production` (server-seitiges Pytest-Gate)
+ **Test-Anruf im Browser als Test-User auf getnerve.app** (D-05 Pflicht: Auto[Meeting]+Knopf+QA) +
`inspect.sh logs` für TTFT + Daten-Fluss-Beleg (alle Pfade tragen Profil-Stimme). KEIN lokales pytest
als Acceptance.

**UTF-8:** Prompt-Text = echte Umlaute; Code-Identifier ASCII.

**Latenz (Punkt 25):** Auto-Prompt wächst → TTFT messen (nicht festzurren). load_answer_config/build_answer_context
= 0 DB im Hot-Path (P1-01). Slot A lokal <1s NICHT anfassen (Bau-Regel 1).
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-P1-NOTES.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-P1-01-answer-config-build-context-PLAN.md

<interfaces>
<!-- Anker gemappt 2026-07-01 (file:line, Re-Grep vor Execute Pflicht). -->

Auto-Pfad — claude_service.py streame_auto_variante(neuer_text, einwaende, kontext, sid, slot=1, trigger="analyse_loop"):
  - def :572; System-Prompt _ewb_autovar_system :622 (14 Token, kontext-arm) → LÖSCHEN, ersetzt durch build_answer_context.
  - user_msg baut ~:603 aus einwaende-Liste + "Hinterlegte Einwand-Gegenargumente (als Orientierung)" → Few-Shot-Zeile RAUS (Req 2). einwaende bleibt Profil-Kontext (kommt jetzt via build_answer_context/Profil), NICHT als Vorlage.
  - Modell config.MODEL_PIP_AUTOVAR (:635) + TTFT-Circuit-Breaker (:631-666) UNVERÄNDERT lassen (Phase 2).
  - NEUER Keyword-Param `primary_intent: str | None = None` anhängen (deepgram reicht match['matched_label']; analyse_loop-Trigger → per-SID primary_intent). is_auto_triggered=True KONSTANT (beide Trigger auto).

Manual-Pfad — claude_service.py streame_manual_ewb_variante(typ, profile_einwand, kontext, sid, slot=1):
  - def :752; System-Prompt heute via build_profile_context :800 → auf build_answer_context heben (role/mode/confidence=hoch/is_auto_triggered=False, primary_intent = der geklickte typ als Label).
  - user_msg standard_ga-Injektion :771 (Profil-Gegenargument als Vorlage) → RAUS (Req 2 Few-Shot). Der geklickte typ bleibt als primary_intent-Label.

QA-Pfad — qa_pipeline.py generate_qa_response(utterance, category, profile_data, anrede, confidence, ...):
  - def :389; System-Prompt _SYSTEM_PROMPT_QA :78-93 (enthält "die den Kunden zur Konkretisierung ZWINGT" = drückerisch) → auf build_answer_context heben; die "zwingt"-Formulierung + die "Frag nach:"-Beispiel-Zeilen (:85-88) entfallen (Grounding-Regel + Paradigma ersetzen sie). confidence ist schon vorhanden → als Parameter durchreichen. Aufrufer _qa_pipeline_dispatch (claude_service.py:~1551) reicht primary_intent + mode + role durch.
  - HINWEIS: generate_qa_response bekommt user_id/sid (für build_answer_context) — sid ist im dispatch vorhanden (_active_sid).

primary_intent-Quelle (TAXO1-Live, Punkt 26 Async-Naht):
  - keyword: deepgram match['matched_label'] — synchron im selben Event, kein Versatz.
  - analyse_loop/QA: _session_state[sid]['state']['primary_intent'] — von analysiere_mit_claude VOR dem
    Antwort-Aufruf im selben SID gesetzt (TAXO1). Re-Grep bestätigt Schreibstelle vor Leser. Leser voraus
    → None → fail-open Default-Intent (NIE falscher Intent).

role-Ableitung (Parameter, kein Zweig):
  - _session_state[sid]['mode']=='meeting' → role='meeting'.
  - sonst _session_state[sid]['state']['contact_category']: 'gatekeeper' → role='gatekeeper'; 'target' → role='interessent'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: QA-Pfad — generate_qa_response auf build_answer_context; drückerische Formulierung raus</name>
  <files>services/qa_pipeline.py, services/claude_service.py</files>
  <read_first>
    - services/qa_pipeline.py 78-93 (_SYSTEM_PROMPT_QA, "zwingt" + "Frag nach:"-Beispiele) + 389-527 (generate_qa_response: confidence-Branch, build_profile_context :425, System-Prompt-Assembly :431)
    - services/claude_service.py ~1485-1707 (_qa_pipeline_dispatch: reicht confidence, hat sid; hier primary_intent+mode+role beschaffen)
    - services/prompt_pipeline.py build_answer_context (P1-01)
    - CLAUDE.md Punkt 14 (Insert), Punkt 25 (Latenz)
  </read_first>
  <action>
    generate_qa_response holt den System-Prompt aus build_answer_context (role/mode/confidence/primary_intent
    durchgereicht) statt _SYSTEM_PROMPT_QA selbst zu bauen. Die drückerische Zeile ("die den Kunden zur
    Konkretisierung ZWINGT") + die "Frag nach:"-Beispiel-Zeilen (:85-88 Few-Shot-artig) entfallen — die
    Grounding-Regel (fehlt Fakt → sagen + Rückfrage vorschlagen) + das Paradigma (offene Frage statt Druck)
    ersetzen sie inhaltlich sauberer. confidence bleibt als Parameter (niedrig → Register „vorsichtiger",
    über build_answer_context, KEIN eigener Prompt-Zweig). _qa_pipeline_dispatch beschafft primary_intent
    (per-SID) + mode + role und reicht sie an generate_qa_response → build_answer_context durch.
    _SYSTEM_PROMPT_QA als Konstante entfernen ODER als reine Fallback-Minimalvorlage kennzeichnen (bevorzugt
    entfernen, Pruning-Notiz). Die 4-Werte-Kategorie (classify) bleibt unangetastet (TAXO1).
## 1. Lokaler Kontext / ## 2. Funktions-Skelett / ## 3. Cross-File-Awareness / ## 4. Edge-Case-Analyse
    (Executor füllt beim Bau gemäß Re-Grep; Kern: generate_qa_response linear, ein LLM-Call :468; Insert
    ersetzt nur die System-Prompt-Quelle, kein early-return betroffen. Race: QA läuft im analyse_loop-Thread;
    primary_intent per-SID synchron vor Aufruf (Punkt 26). confidence None → neutraler Register-Default.)
  </action>
  <verify><automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "build_answer_context"'</automated></verify>
  <acceptance_criteria>
    - grep: generate_qa_response bezieht System-Prompt aus build_answer_context; _SYSTEM_PROMPT_QA-Selbstbau + "zwingt"-Formulierung + "Frag nach:"-Beispiele weg.
    - confidence als Parameter (niedrig → vorsichtiges Register), kein mode-spezifischer Prompt-Zweig.
  </acceptance_criteria>
  <done>QA-Antwort kommt aus der EINEN Quelle; drückerische Formulierung + Few-Shot-Beispiele raus; Grounding live.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Manual-Pfad — streame_manual_ewb_variante auf build_answer_context; standard_ga-Vorlage raus</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py 752-901 (streame_manual_ewb_variante: build_profile_context :800, standard_ga user_msg :771, drei Emits — PIP hat source=manual_button ergänzt, NICHT anfassen)
    - build_answer_context (P1-01)
  </read_first>
  <action>
    System-Prompt via build_answer_context (role aus contact_category/mode, mode, confidence=hoch [Knopf =
    bewusster Klick], is_auto_triggered=False, primary_intent = der geklickte typ als Label). Die
    standard_ga-Injektion in user_msg (:771, Profil-Gegenargument als „Vorlage") RAUS (Req 2 Few-Shot) — das
    Standard-Gegenargument steckt ohnehin im Profil-Kontext (via build_answer_context), nicht als
    Nachbau-Vorlage. Die drei Emits (pip_stream_start/pip_token/pip_token_done inkl. `source: manual_button`
    aus PIP) UNVERÄNDERT. Modell/Retry unverändert (Phase 2). Punkt-14-Sektionen beim Bau füllen (Insert
    ersetzt nur System-Prompt-Quelle + entfernt eine user_msg-Zeile; kein Emit/Exit-Pfad betroffen).
  </action>
  <verify><automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "standard_ga"'</automated></verify>
  <acceptance_criteria>
    - grep: streame_manual_ewb_variante zieht System-Prompt aus build_answer_context; standard_ga-Vorlage-Injektion weg; source=manual_button-Emits intakt.
  </acceptance_criteria>
  <done>Knopf-Antwort aus der EINEN Quelle; kein standard_ga-Nachbau-Anker; Profil-Stimme + Paradigma live.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Auto-Pfad — _ewb_autovar_system löschen; build_answer_context; EIN Intent durchreichen (deepgram)</name>
  <files>services/claude_service.py, services/deepgram_service.py</files>
  <read_first>
    - services/claude_service.py 572-749 (streame_auto_variante: _ewb_autovar_system :622 LÖSCHEN, "Hinterlegte Einwand-Gegenargumente" ~:603 RAUS, TTFT-Circuit-Breaker :631-666 NICHT anfassen — Phase 2)
    - services/deepgram_service.py Keyword-Pfad (nach PIP: mode=='cold_call' → return; im Meeting emit ewb_signal + ruft Auto-Variante? Re-Grep: streame_auto_variante-Aufruf-Stelle + match['matched_label'])
    - build_answer_context (P1-01); CLAUDE.md Punkt 26 (Async-Naht primary_intent), Punkt 14
  </read_first>
  <action>
    _ewb_autovar_system (:622) LÖSCHEN; streame_auto_variante zieht den System-Prompt aus build_answer_context
    (is_auto_triggered=True KONSTANT; role/mode/confidence/primary_intent durchgereicht). Die „Hinterlegte
    Einwand-Gegenargumente (als Orientierung)"-Zeile im user_msg RAUS (Req 2). NEUER Param
    `primary_intent: str | None = None`. deepgram Keyword-Pfad reicht `match['matched_label']` als
    primary_intent durch (deterministisch, Req 5); analyse_loop-Trigger → `_session_state[sid]['state'].get('primary_intent')`
    unter Lock. **Punkt 26:** primary_intent wird zum Aufruf-Zeitpunkt gelesen; Schreibstelle
    (analysiere_mit_claude/keyword) liegt VOR dem Antwort-Aufruf (Re-Grep-Beleg in SUMMARY); Leser voraus →
    None → Default-Intent (nie falscher Intent). TTFT-Circuit-Breaker + Modell UNVERÄNDERT (Phase 2).
    **PIP-Kontext:** Cold-Call-Auto ist aus (deepgram mode=='cold_call' → return); dieser Pfad feuert nur im
    Meeting — build_answer_context gilt trotzdem (role='meeting'). Punkt-14-Sektionen beim Bau füllen.
  </action>
  <verify><automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh grep "_ewb_autovar_system"'</automated></verify>
  <acceptance_criteria>
    - grep: `_ewb_autovar_system` = 0 Treffer (gelöscht); streame_auto_variante zieht aus build_answer_context; „Hinterlegte Einwand-Gegenargumente"-Zeile weg; primary_intent-Param + deepgram match['matched_label']-Durchreichung vorhanden.
    - grep: TTFT-Circuit-Breaker (_ewb_fallback_until/_ewb_ttft_history) + Modell-Konstanten UNVERÄNDERT.
  </acceptance_criteria>
  <done>Auto-Müll tot; Auto zieht aus der EINEN Quelle; EIN deterministischer Intent; Circuit-Breaker/Modell unangetastet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-Test-Anruf (Prod) — Paradigma greift, alle 3 Pfade tragen Stimme, TTFT-Basislinie</name>
  <what-built>
    3 Antwort-Pfade auf build_answer_context; Auto-Müll + Few-Shot tot; EIN Intent; Rolle/Modus/Konfidenz
    Parameter; Grounding live. Deploy via `bash deploy.sh production` (server-seitiges Pytest-Gate).
  </what-built>
  <how-to-verify>
    1. `bash deploy.sh production` (Pytest-Gate grün).
    2. Cold-Call (Test-User): EINEN Einwand-Knopf klicken → Antwort lesen. Erwartet: Profil-Stimme, kein
       Technik-Vokabel („Reframe"/„überwinden"), kein manipulativer Klang, kurz + eher eine offene Frage.
    3. Gatekeeper-Fall provozieren (Rolle gatekeeper): Antwort respektvoll/ehrlich, KEIN Überreden, kein
       „ich weiß, Sie wollen Ihren Chef schützen".
    4. Grounding prüfen: einen Einwand zu einem NICHT hinterlegten Fakt → NERVE sagt ehrlich „nicht hinterlegt"
       + schlägt Rückfrage/Follow-up vor (erfindet nichts).
    5. Meeting-Modus: Auto-Antwort trägt jetzt Profil-Stimme (nicht mehr generisch).
    6. `inspect.sh logs 500 | grep "EWB-TTFT"` → TTFT-Basislinie notieren (Auto-Prompt ist größer; NUR messen,
       nicht festzurren — Phase 2 fixt Tempo via Caching).
  </how-to-verify>
  <resume-signal>Schreibe "approved" wenn: alle 3 Pfade Profil-Stimme tragen + kein Technik-Vokabel/kein Druck + Gatekeeper respektvoll + Grounding greift (kein Erfinden). Sonst: die schlechte Antwort + Kontext (Rolle/Intent) + TTFT-Zeilen posten.</resume-signal>
</task>

</tasks>

<verification>
- grep (Prod): build_answer_context ist EINE Quelle für Auto+Knopf+QA; `_ewb_autovar_system`=0; standard_ga-Vorlage + „Hinterlegte Einwand-Gegenargumente" + „zwingt"/„Frag nach:"-Beispiele weg.
- grep: EIN primary_intent (kein Top-2); role/mode/confidence als Parameter (kein `if mode==`-Prompt-Zweig); Auto-Trigger + Circuit-Breaker + Modell unverändert; kein Web-Recherche-Call; keine product_facts-Tabelle.
- Live-Test-Anruf (D-05): Auto[Meeting]+Knopf+QA tragen Profil-Stimme; Gatekeeper respektvoll; keine Technik-Vokabel; Grounding greift. TTFT-Basislinie dokumentiert (nicht festgezurrt).
- Punkt 26: SUMMARY belegt Schreibstelle primary_intent VOR Leser (fail-open Default bei Voraus-Lauf).
</verification>

<success_criteria>
- (Req 1/2) Alle 3 Pfade aus EINER Quelle; kontext-armer Auto-Prompt + Few-Shot-Anker tot; Paradigma gedreht.
- (Req 3) Gatekeeper-Antwort respektvoll/ehrlich (Live-Test) — Rolle config-getrieben (answer_paradigm.py, Weg 3).
- (Req 5) genau EIN Intent ans LLM. (Req 6) Modus/Konfidenz/Rolle Parameter; Auto-Trigger bleibt.
- (Req 7) Grounding-Regel live; kein Erfinden; keine Live-Web-Recherche; product_facts deferred.
- Latenz: TTFT-Basislinie gemessen (nicht festgezurrt); Slot A <1s unverändert; Caching = Phase 2.
</success_criteria>

<output>
SUMMARY: `08.23.2.TAXO3-P1-02-SUMMARY.md`. Pflicht: (a) Pruning-Notiz (_ewb_autovar_system + _SYSTEM_PROMPT_QA
+ Few-Shot-Anker gelöscht); (b) Punkt-26-Beleg (primary_intent-Schreibstelle vor Leser, fail-open); (c)
TTFT-Basislinie-Zahlen je Pfad (Auto/Knopf/QA) + Vermerk „nicht festgezurrt, Phase 2 = Caching"; (d)
Bestätigung Modell/Circuit-Breaker unverändert + Slot A unverändert.
</output>
