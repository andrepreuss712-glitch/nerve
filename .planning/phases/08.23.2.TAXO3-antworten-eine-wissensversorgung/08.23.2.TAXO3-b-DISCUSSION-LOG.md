# Phase 08.23.2.TAXO3 Scheibe (b) — Discussion Log

> **Audit trail only.** Do not use as input to planning/execution agents.
> Decisions are captured in `08.23.2.TAXO3-b-CONTEXT.md`.

**Date:** 2026-07-02
**Phase/Slice:** 08.23.2.TAXO3 — (b) Gesprächs-Gedächtnis + Continuation
**Pflicht-Pre-Read done:** NERVE Gedächtnis + Continuation — Entscheidung + Bau-Vorgabe.md · NERVE Konstrukt - Soll-Verhalten.md §2/§5
**Areas discussed:** Erinnerungs-Umfang · Unsicherheits-Verhalten · Modell-Wahl · Knopf-Platzierung

---

## Erinnerung (Umfang + was NERVE sieht)

| Option | Beschreibung | Selected |
|--------|--------------|----------|
| Letzte 3 Züge, nur NERVE selbst | Eigene letzten 3 Antworten + Öffner-Liste. Kleinste/schnellste. | ✓ (als Größe) |
| Letzte 5 Züge, nur NERVE selbst | Etwas mehr Gedächtnis, minimal langsamer | |
| Inkl. Berater-Aussagen | Reichster Kontext, längster Prompt | |

**User's choice:** Option 1 als Größe fürs allgemeine Fenster. **Refinement:** ZWEI getrennte Bedürfnisse — (1) Anti-Wiederholung braucht nur NERVEs eigene Vorantworten (kleines Fenster); (2) Continuation-Knopf braucht die letzte(n) Berater-Aussage(n)/gespiegeltes Echo als EIGENEN Eingang beim Knopfdruck, NICHT via Fenster-Vergrößerung. `triggering_text` auf Ausreichen für (1) prüfen. TTFT gegen Fenster messen, klein starten ist Absicht.

## Bei Unsicherheit (gespiegeltes Echo unklar)

| Option | Beschreibung | Selected |
|--------|--------------|----------|
| Nur Rückfrage-Empfehlung | Keine geratene Weiterführung, „Frag nach: X oder Y?" | ✓ |
| Weiterführung + Rückfrage-Hinweis | Geratene Weiterführung, als unsicher markiert | |
| Gestaffelt nach Sicherheit | Sicher → Weiterführung, unklar → Rückfrage | (später) |

**User's choice:** Option 1. Risiko-Asymmetrie (unnötige Rückfrage billig, falsche Weiterführung teuer). Ask-back auf dem gesenkten Konfidenz-Wert (G1) aufsetzen → spätere Staffelung = nur Schwellenwert-Dreh, kein Rebuild. Schwelle jetzt konservativ.

## Starkes Modell (live) für den Continuation-Knopf

| Option | Beschreibung | Selected |
|--------|--------------|----------|
| Ja, starkes Modell für diesen Knopf | Sonnet live, Latenz via Vorladen/Caching lösen | ✓ |
| Erst mit schnellem Modell testen | Haiku zuerst, nur bei zu schwach hochstufen | |

**User's choice:** Option 1. **Refinements:** (1) Test misst TTFT des STARKEN Modells, nicht „ist schwach gut genug"; bei Latenz-Problem starkes Modell schneller machen, nie zurückfallen. (2) Gilt nur für den Continuation-Knopf. **Zusatz:** Modell config-getrieben (`MODEL_CONTINUATION`, kein Hardcode, kein Redeploy) UND durch DIESELBE eine Modell-Aufruf-Naht wie die anderen Antwort-Pfade — volle Anbieter-Abstraktion deferred (eigene Phase), aber die eine Naht jetzt vorbereiten.

## Knopf-Platzierung + Auslösung

| Option | Beschreibung | Selected |
|--------|--------------|----------|
| Immer sichtbar bei den Einwand-Knöpfen | Dauerhaft, stabil, kein Show/Hide | ✓ |
| Erscheint erst nach einer NERVE-Frage | Zustandsabhängig | |

**User's choice:** Option 1 (dauerhaft sichtbar, kein Show/Hide → vermeidet Bug-B-Klasse). **Refinements:** (1) Knopf respektiert das bestehende Gesprächs-Start-Gate (feuert nicht vor erkanntem Call; danach deterministisch). (2) Finale UI-Politur → PIP.2, jetzt nur funktional stabil.

## Claude's Discretion (⚙️ Code-Innerei)
Ringpuffer-Mechanik (per-SID RAM), Öffner-Listen-Ableitung, Gespiegelt-Marker-Ablage + schema_version (nach Persistenz-Schicht-Verifikation), Transkript-Zugriff des Continuation-Knopfes.

## Deferred Ideas
Account-Wissen über mehrere Gespräche · manueller Wissens-Eingang B1 · Kunden-Übersicht + async Post-Call-Zusammenfassung · zwei Zeiten · volle LLM-Anbieter-Schicht + EU-Residenz · gestaffeltes Unsicherheits-Verhalten · finale Knopf-UI (PIP.2) · Prompt-Caching (TAXO3 Phase 2).
