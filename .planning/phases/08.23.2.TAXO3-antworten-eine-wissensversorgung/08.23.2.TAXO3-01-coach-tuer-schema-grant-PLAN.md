---
phase: 08.23.2.TAXO3
plan: 01
type: execute
wave: 1
complexity: 🔴
depends_on: []
files_modified:
  - database/models.py
  - alembic/versions/XXXX_method_packs_pack_assignments_product_facts.py
  - tests/test_method_pack_schema.py
autonomous: false
requirements: ["SPEC Req 4", "SPEC Req 7 (Schema-Anteil)"]
risiko: schema-addition
test_anruf: false
user_setup: []

must_haves:
  truths:
    - "training.method_packs + training.pack_assignments + training.product_facts existieren auf Production (leer/EIN-Default), Owner postgres, mit narrow GRANT USAGE+SELECT an nerve_app (OQ-1 Variante a, NACH André+Gemini-Freigabe)"
    - "nerve_app (Live-App-Rolle) kann die 3 neuen Tabellen LESEN — bewiesen als die ECHTE nerve_app-DB-Rolle auf Prod (peer-auth: OS-User nerve_app verbindet als DB-Rolle nerve_app via NERVE_..._DSN=postgresql://nerve_app@/nerve, NICHT postgres-then-SET-ROLE) → `SELECT … FROM training.method_packs` erfolgreich; der bestehende training-Wall (DPO-Tabellen) bleibt gesperrt → `SELECT … FROM training.preference_pairs` permission denied (DSGVO/DPO-Isolation intakt). SET-ROLE-Simulation nur als Sekundaer-Signal."
    - "method_packs trägt schema_version, workspace_id (nullable), owner (default 'NERVE'), valid-Versionierung; pack_assignments trägt user_id/workspace_id → pack_id + valid_until (nullable=unbefristet); KEINE FK ins crm-Schema; KEIN Beispielsatz-Feld im JSONB-Schema"
    - "product_facts trägt intent-Tag + id + reguliert/riskant-Flag (Req 7 Andock-Struktur, leer) — KEINE crm-FK, KEINE echten Fakten eingepflegt"
    - "SCHILD-Guard läuft server-seitig GRÜN für alle 3 Tabellen + alle nicht-trivialen Spalten"
    - "Kein Live-Pfad liest die Tabellen in dieser Welle (Loader kommt Plan 03/W1) — reine Schema-Tür"
  artifacts:
    - path: "database/models.py"
      provides: "ORM MethodPack + PackAssignment + ProductFact (training-Schema, JSONB-Inhalt, schema_version, workspace_id, valid_until, Schilder)"
      contains: "class MethodPack"
    - path: "alembic/versions/XXXX_method_packs_pack_assignments_product_facts.py"
      provides: "CREATE 3 training-Tabellen (als postgres) + narrow GRANT USAGE+SELECT an nerve_app + COMMENTs"
      contains: "GRANT SELECT"
    - path: "tests/test_method_pack_schema.py"
      provides: "Schema-Smoke + nerve_app-Lese-Recht-Proof (ECHTE nerve_app-Rolle peer-auth, beide Seiten: method_packs OK / preference_pairs FAIL) + KEIN-crm-FK-Test + JSONB-roundtrip"
  key_links:
    - from: "database/models.py MethodPack/PackAssignment/ProductFact"
      to: "training.method_packs / training.pack_assignments / training.product_facts"
      via: "Alembic-Migration als postgres"
      pattern: "method_packs"
    - from: "nerve_app (Live-App-Rolle, build_answer_context Plan 03)"
      to: "training.method_packs / pack_assignments (SELECT)"
      via: "narrow GRANT USAGE ON SCHEMA training + GRANT SELECT (OQ-1 a)"
      pattern: "GRANT SELECT ON .* TO nerve_app"
    - from: "pack_assignments.pack_id"
      to: "method_packs.id"
      via: "FK innerhalb training-Schema (KEINE crm-FK)"
      pattern: "ForeignKey"
---

> 🛑 **PRE-BUILD-GATE OQ-1 (Punkt 22 Schema-Entscheidung) — [NEEDS ANDRÉ+GEMINI vor Execute].**
> Die Schema-Frage „WO leben method_packs + pack_assignments?" ist NICHT silently gelockt. Verifiziert gegen Prod (RESEARCH §Persistenz, verbatim psql): `nerve_app` hat `permission denied for schema training`; `has_schema_privilege('nerve_app','training','USAGE')=f`; training-Owner=postgres, granted nur an nerve_anon_worker. CONTEXT.md sagt „Owner nerve_app" — **falsch gegen Prod.** `build_answer_context()` (Plan 03) läuft live als nerve_app und MUSS lesen.
> **Drei Optionen, am Cross-AI-Review (PFLICHT, 🔴) zu bestätigen BEVOR dieser Plan executet:**
> - **(a) narrow GRANT** (Empfehlung RESEARCH OQ-1): Tabellen bleiben im `training`-Schema (honoriert SPEC Req 4 + Coach-Daten-Isolation) + `GRANT USAGE ON SCHEMA training` + `GRANT SELECT ON training.method_packs, training.pack_assignments, training.product_facts TO nerve_app`. DPO-Tabellen (preference_pairs/transcript_archive) bleiben nerve_anon_worker-only → Wand intakt.
> - **(b) public-Schema:** einfachster Live-Zugriff, weicht von „training-Schema"-Vorgabe ab.
> - **(c) neues `coach`-Schema** mit eigenem Grant.
> **Dieser Plan ist auf (a) geschrieben.** Falls André+Gemini (b) oder (c) wählen → Tabellen-Schema + GRANT-Block + Loader-Pfad (Plan 03) entsprechend umstellen. `autonomous:false` — Checkpoint-Task 0 hält für die Bestätigung.

> RISIKO-BANNER: **schema-addition.** Reine Tür (3 leere/EIN-Default Tabellen + Grant + Schild). KEIN Live-Pfad berührt, KEIN Loader (Plan 03/W1), KEIN Test-Anruf (RESEARCH §Wave-Cut W0, TAXO1 D-03 Auflage 1) — aber SCHILD-Guard GRÜN Pflicht.

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Geplant, nicht sofort ausgeführt. Deploy erst nach Cross-AI-Clearance (PFLICHT) inkl. OQ-1-Schema-Entscheidung + 3-Wege-Interlock TAXO1/2/3 (intent_event-Klebstoff).

> 🛡️ **DSGVO/DPO-ISOLATIONS-GATE (Reviewer MEDIUM-Finding):** Der narrow GRANT ist die einzige security-sensitive Fläche dieser Phase — ein fehlerhafter Grant könnte die DPO-Daten-Isolation (preference_pairs/transcript_archive) aufweichen. Der Grant-Proof (Task 3) wird deshalb gegen die **ECHTE nerve_app-DB-Rolle auf Production** gefahren (peer-auth, OS-User nerve_app → DB-Rolle nerve_app via `NERVE_..._DSN=postgresql://nerve_app@/nerve`), NICHT als `postgres`-then-`SET ROLE`-Simulation (die kann echte Rollen-Attribut-/peer-auth-Unterschiede maskieren). Der Proof MUSS BEIDE Seiten explizit zeigen: `SELECT training.method_packs` → erfolgreich (Grant greift), `SELECT training.preference_pairs` → permission denied (DPO-Wall intakt). Die SET-ROLE-Variante bleibt nur als Sekundär-Signal.

> 🔎 **VOR-EXECUTE-RE-GREP + RE-VERIFY (RESEARCH Valid until 2026-06-19):** Beim Execute neu ziehen: `inspect.sh migrations` (echter HEAD = down_revision; TAXO1/2 legen 0016+ an) + `ssh ... sudo -u postgres psql nerve -c "SELECT has_schema_privilege('nerve_app','training','USAGE');"` (bestätigen: weiterhin `f` vor dem Grant) + `grep -rn "method_packs\|pack_assignments\|product_facts\|MethodPack" services/ routes/ database/` == 0 (Tabellen wirklich neu).

<objective>
Baue die **Coach-Tür** (E1–E3): die 3 Daten-Tabellen `training.method_packs`, `training.pack_assignments`, `training.product_facts` — LEER bzw. EIN NERVE-Default (Inhalt kommt Plan 02/W0b). Reine Schema-Addition + der **narrow GRANT** (OQ-1 a), der dem Live-Loop (`build_answer_context`, Plan 03) Lese-Zugriff gibt, ohne die DPO-Wand zu durchlöchern. Schild (Punkt 23) auf alle 3 Tabellen + jede nicht-triviale Spalte.

Purpose: Ohne diese Tabellen + Grant kann `build_answer_context()` (Plan 03) das Intent→Technik-Mapping nicht über `pack_assignments → method_packs` laden (SPEC Req 4). Risk-low Schema-Addition ZUERST (Wellen-Schnitt, kein Big-Bang), bevor irgendein Live-Pfad sie liest.
Output: 3 training-Tabellen live auf Prod (leer), narrow GRANT aktiv, ORM-Models, Migration, Schema-Smoke + nerve_app-Lese-Proof (echte Rolle, peer-auth, beide Seiten), SCHILD-Guard GRÜN.

KEIN Loader. KEINE build_answer_context. KEIN Live-Pfad berührt. KEINE echten Produkt-Fakten (Req 7 = nur Struktur).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Phase-Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen (lesen sonst Basis-Dir `08.23.2`). STATE/ROADMAP hand-editieren. SUMMARY ins hardcodierte Verzeichnis.

**Verify = Production only (CLAUDE.md HART):** `bash deploy.sh production` (server-seitiges Pytest-Gate vor Restart) + `inspect.sh` gegen Prod. KEIN lokales pytest als Acceptance, KEIN `git pull` auf Prod. **Migration als `postgres`-User** (kann training-Schema schreiben + GRANT erteilen; nerve_app kann es nicht). SCHILD-Guard MANUELL server-seitig (`tests/test_schild_guard.py`, OS-User nerve_app peer-auth, NERVE_SCHILD_TEST_DSN). **Grant-Proof ebenfalls als OS-User nerve_app (peer-auth) — siehe DSGVO-Gate oben + Task 3.**

**Migrations-Revision (HART, DEPENDS-ON-TAXO1/2):** Prod-HEAD = 0015 (Stand SCHILD). TAXO1/2 legen 0016+ an. Revision NICHT hardcoden — beim Execute `inspect.sh migrations` lesen, down_revision = dann-HEAD, Datei auf echte nächste Nummer benennen.

**UTF-8-Regel:** Umlaute nur in user-facing Text. DB-Spalten/dict-keys/COMMENT-Identifier ASCII (ae/oe/ue/ss). COMMENT-Text-Strings dürfen Umlaute tragen (UTF-8 DB).

**SSH-Pattern:** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'`. Privilege-maskierte training-Sicht: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql nerve -c "..."'`. **Echte-nerve_app-Rolle (Grant-Proof, peer-auth):** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u nerve_app NERVE_..._DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest ...'` (gleiches Muster wie der SCHILD-Guard auf Prod).
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md

<interfaces>
<!-- Muster zum Kopieren — bewiesen aus dem Code/Prod. KEINE Codebase-Exploration nötig. -->

training-Tabelle als postgres anlegen, KEIN ALTER OWNER nerve_app (würde DPO-Wand-Logik aufweichen) — stattdessen narrow GRANT (OQ-1 a):
```
-- in upgrade(), als postgres:
op.execute("GRANT USAGE ON SCHEMA training TO nerve_app")
op.execute("GRANT SELECT ON training.method_packs TO nerve_app")
op.execute("GRANT SELECT ON training.pack_assignments TO nerve_app")
op.execute("GRANT SELECT ON training.product_facts TO nerve_app")
-- KEIN GRANT INSERT/UPDATE/DELETE an nerve_app (Coach-Admin-Schreibpfad = Post-Launch/v2).
-- DPO-Tabellen (preference_pairs/transcript_archive) bekommen KEINEN nerve_app-Grant → Wand bleibt.
```

Echte-nerve_app-Rolle-Grant-Proof (peer-auth, gleiches Muster wie SCHILD-Guard auf Prod — Reviewer-Vorgabe):
```
# OS-User nerve_app verbindet als DB-Rolle nerve_app (peer-auth), NICHT postgres-then-SET-ROLE:
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && sudo -u nerve_app NERVE_GRANTPROOF_DSN=postgresql://nerve_app@/nerve \
   /opt/nerve/venv/bin/pytest tests/test_method_pack_schema.py::test_nerve_app_can_read_method_packs -x'
# Der Test oeffnet die DSN (echte nerve_app-Rolle) und prueft BEIDE Seiten:
#   SELECT count(*) FROM training.method_packs;      -> erfolgreich (Grant greift)
#   SELECT count(*) FROM training.preference_pairs;  -> permission denied (DPO-Wall intakt)
```

Versions-Lade-Muster (Vorbild für Plan-03-Loader, hier NUR Schema): services/prompt_pipeline.py:30 resolve_prompt_version — ENV-Override → Cache → DB-Variants → fail-open. Plan-03-Loader analog: pack_assignments(user/workspace, valid_until) → method_packs → abgelaufen/keine → stiller NERVE-Default.

COMMENT-Schild-Muster (Migration 0015, hauseigenes op.execute):
```
op.execute("COMMENT ON TABLE training.method_packs IS '<Zweck>. Status: lebt. Schreibt <admin-pfad post-launch>; liest services/prompt_pipeline.py (build_answer_context, Plan 03).'")
op.execute("COMMENT ON COLUMN training.method_packs.<col> IS '<...>'")
```

profile_faqs-Schema (Req 7 Andock-Referenz, RESEARCH §Persistenz verbatim) — product_facts ist die NEUE intent-getaggte Struktur, NICHT profile_faqs (das bleibt FAQ-Fuzzy-Match):
```
profile_faqs: id|profile_id(FK profiles)|frage_muster|antwort|kategorie|created_at|used_count|mode
```

method_pack JSONB-Form (E3, ⚙️ Discretion — KEIN Beispielsatz-Feld):
```json
{
  "schema_version": 1,
  "paradigm_rules": ["<6-10 Verhaltens-Regeln, unsichtbar>"],
  "taboos": ["<3 harte Tabus D-02>"],
  "roles": {
    "gatekeeper":   {"ziel": "<Respekt+Ehrlichkeit>", "hebel": ["<1-2 Verhaltens-Hebel>"]},
    "interessent":  {"ziel": "<Sorge diagnostizieren + relevanter Fakt>", "hebel": [...]},
    "meeting":      {"ziel": "<tiefer, beide Seiten hörbar>", "hebel": [...]}
  },
  "intents": { "<intent_type>": {"ziel": "...", "hebel": [...], "fact_ids": [<optional product_facts.id-Verweise>]} }
}
```
(Inhalt → Plan 02/W0b destilliert + André-Freigabe. Hier nur: JSONB-Spalte akzeptiert diese Form, KEIN Feld `examples`/`beispiele`/`musterantworten`.)
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `training.method_packs` (CREATE) — existiert NICHT (Beleg unten)
- `training.pack_assignments` (CREATE) — existiert NICHT
- `training.product_facts` (CREATE) — existiert NICHT
- `training` (Schema, GRANT-Ziel) — existiert (Owner postgres)

### inspect.sh / psql-Beleg (VERBATIM aus RESEARCH §Persistenz, 2026-06-12)
```
$ ssh ... sudo -u postgres psql nerve -c "SET ROLE nerve_app; SELECT count(*) FROM training.preference_pairs;"
SET
ERROR:  permission denied for schema training

$ ssh ... sudo -u postgres psql nerve -c "SELECT has_schema_privilege('nerve_app','training','USAGE');"
 nerve_app_usage
-----------------
 f

$ \dn+ training
   Name   |  Owner   |      Access privileges
----------+----------+------------------------------
 training | postgres | postgres=UC/postgres        +
          |          | nerve_anon_worker=U/postgres

$ \dt training.*
 training | preference_pairs   | table | postgres
 training | transcript_archive | table | postgres

$ inspect.sh migrations   →  0015   (TAXO1/2 heben auf 0016+; beim Execute neu lesen)
```
**Beim Execute neu ziehen:** `inspect.sh migrations` (echter HEAD = down_revision) + `has_schema_privilege('nerve_app','training','USAGE')` (muss VOR dem Grant noch `f` sein).

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `MethodPack.content` (JSONB) | gelesen build_answer_context (Plan 03, nerve_app) | DB training.method_packs.content JSONB | ⬜ Task 3 Schema + echte-Rolle-Lese-Proof |
| `MethodPack.schema_version` | gelesen Loader (Plan 03) | DB feste Spalte SmallInt | ⬜ Task 3 |
| `PackAssignment.pack_id → method_packs.id` | Loader-Join (Plan 03) | DB FK INNERHALB training (KEINE crm-FK) | ⬜ Task 3 KEIN-crm-FK-Test |
| `PackAssignment.valid_until` (nullable) | Fallback-Logik (Plan 03) | DB Timestamp nullable | ⬜ Task 3 |
| `ProductFact.intent_tag / risk_flag` | gelesen Grounding (Plan 03, leer in W0) | DB training.product_facts | ⬜ Task 3 |
| nerve_app USAGE+SELECT auf 3 Tabellen | build_answer_context Live-Read | GRANT (Catalog) | ⬜ Task 2/3 echte-Rolle-Proof (peer-auth): method_packs OK / preference_pairs denied |

## Verbindungs-Karte (Punkt 22)
- **method_packs/pack_assignments/product_facts** existieren NICHT (psql oben) → reine Neu-Tabellen, kein bestehender Schreiber/Leser. `grep -rn "method_packs\|pack_assignments\|product_facts\|MethodPack\|PackAssignment\|ProductFact" services/ routes/ database/` == 0 vor diesem Plan (Beleg Task 0).
- **Schema-Ort (OQ-1, [NEEDS ANDRÉ+GEMINI]):** training-Schema (a, narrow GRANT) — NICHT silently gelockt. nerve_app↔training heute gesperrt (psql verbatim oben). Plan auf (a); Checkpoint-Task 0 hält für Bestätigung.
- **GRANT-Begründung bewiesen:** nerve_app=`permission denied for schema training` + USAGE=`f`. Ohne USAGE+SELECT kann build_answer_context (Plan 03) NICHT lesen → JEDE Antwort fiele auf Default/crash (Pitfall 1). DPO-Tabellen bekommen KEINEN Grant → Coach-Daten-Isolation hält. **Grant-Proof gegen echte nerve_app-Rolle (peer-auth), beide Seiten (DSGVO-Gate).**
- **KEINE crm-FK (Coach-harte-Linie):** pack_assignments.pack_id → method_packs.id (innerhalb training). workspace_id/user_id sind reine Werte-Spalten (KEIN FK in crm.accounts/users). Beleg Task 3 KEIN-crm-FK-Test.
- **Migrations-Rolle (A1):** Migration läuft als postgres — 0015 (SCHILD) hat training erfolgreich kommentiert → postgres kann training schreiben + GRANT erteilen. Beim Execute DSN-Rolle der Migration bestätigen.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint 0: OQ-1 Schema-Ort bestätigen (André + Gemini, Punkt 22) — VOR jedem Tabellen-Lock</name>
  <decision>WO leben method_packs + pack_assignments + product_facts? Der Plan ist auf (a) narrow GRANT geschrieben — final bestätigen ODER auf (b)/(c) umstellen.</decision>
  <context>
    Verifiziert gegen Prod (psql verbatim, Persistenz-Sektion oben): nerve_app hat `permission denied for schema training`, USAGE=`f`, training-Owner=postgres (nur nerve_anon_worker granted). CONTEXT.md „Owner nerve_app" ist FALSCH gegen Prod. build_answer_context (Plan 03) liest live als nerve_app. Diese Entscheidung ist Punkt-22-pflichtig (Schema-/Rollen-Entscheidung) UND Teil des 🔴-Cross-AI-Reviews.
  </context>
  <options>
    <option id="a-narrow-grant">
      <name>(a) training-Schema + narrow GRANT USAGE+SELECT auf NUR die 3 neuen Tabellen (Empfehlung)</name>
      <pros>Honoriert SPEC Req 4 („training-Schema") + Coach-Daten-Isolation; DPO-Tabellen bleiben dicht (kein Grant); minimaler Eingriff; postgres-Migration kann GRANT erteilen.</pros>
      <cons>Live-App-Rolle bekommt eine schmale Tür ins training-Schema (auf 3 Tabellen begrenzt, SELECT-only).</cons>
    </option>
    <option id="b-public">
      <name>(b) public-Schema</name>
      <pros>Einfachster Live-Zugriff (nerve_app ist public-Owner), kein Grant nötig.</pros>
      <cons>Weicht von SPEC Req 4 „training-Schema"-Vorgabe ab; mischt Coach-Daten in public.</cons>
    </option>
    <option id="c-coach-schema">
      <name>(c) neues coach-Schema + eigener Grant</name>
      <pros>Saubere Trennung Coach-Domäne; eigene Grant-Politik.</pros>
      <cons>Neues Schema = mehr Infra (env.py include_schemas, Guard-Erweiterung); Over-Engineering für „nur die Tür".</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md §Open Questions OQ-1 + §Common Pitfalls Pitfall 1
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md <decisions> (Coach-harte-Linie)
    - ssh ... 'sudo -u postgres psql nerve -c "\dn+ training"' (Live-Owner/Grants neu ziehen)
  </read_first>
  <resume-signal>Wähle: a-narrow-grant (Default, Plan unverändert), b-public, oder c-coach-schema. Bei b/c: Executor stellt Tabellen-Schema-Präfix + GRANT-Block + Plan-03-Loader-Pfad entsprechend um.</resume-signal>
  <acceptance_criteria>
    - André+Gemini-Entscheidung im SUMMARY + Nerve-Vault/05 Log dokumentiert (a/b/c + Begründung)
    - Bei (a): GRANT-Block bleibt wie unten; bei (b)/(c): Abweichungen notiert BEVOR Task 1 läuft
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 0: Neuheit beweisen (Punkt 22 grep==0) + Wellen-Schnitt-Beleg</name>
  <read_first>
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md §Code-Insights (Andock-Punkte)
  </read_first>
  <action>
    Führe den Neuheits-grep aus und schreibe das Ergebnis in die SUMMARY (Task 0): `grep -rn "method_packs\|pack_assignments\|product_facts\|MethodPack\|PackAssignment\|ProductFact" services/ routes/ database/ alembic/` — erwartet 0 aktive Treffer (Migration-Datei dieses Plans ausgenommen). Belegt: reine Neu-Tabellen, kein bestehender Schreiber/Leser. Plus: `inspect.sh migrations` (echter HEAD = down_revision für Task 2) + `sudo -u postgres psql nerve -c "SELECT has_schema_privilege('nerve_app','training','USAGE');"` (muss `f` sein) verbatim in SUMMARY.
  </action>
  <verify>
    <automated>grep -rc "MethodPack\|PackAssignment\|ProductFact" services/ routes/ database/ | grep -v ':0' | grep -v 'models.py'</automated>
  </verify>
  <acceptance_criteria>
    - grep-Beleg in SUMMARY: 0 aktive method_packs/pack_assignments/product_facts-Referenzen vor diesem Plan (außer der neuen Migration)
    - `inspect.sh migrations`-Output verbatim in SUMMARY (echter HEAD)
    - `has_schema_privilege('nerve_app','training','USAGE')` == `f` verbatim in SUMMARY (Grant noch nicht erteilt)
  </acceptance_criteria>
  <done>Neuheit + Migrations-HEAD + Pre-Grant-Zustand belegt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: ORM MethodPack + PackAssignment + ProductFact in database/models.py (training-Schema, JSONB, Schilder)</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py (training-Tabellen-Stil falls ORM-Vorbild da; sonst public-Tabellen-Stil + __table_args__={'schema':'training'}-Muster)
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md §Standard Stack (method_pack JSONB-Form E3) + §Security Domain (workspace_id, KEINE crm-FK)
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-Stil als Schild-Vorlage)
  </read_first>
  <behavior>
    - MethodPack instanziierbar mit name+owner='NERVE'+schema_version=1+content={} ; ASCII-Spaltennamen
    - content (JSONB) akzeptiert die E3-Form (paradigm_rules/taboos/roles/intents) UND lehnt KEIN Feld ab — aber das Schema sieht KEIN `examples`-Feld vor (E3: kein Beispielsatz-Feld)
    - PackAssignment.valid_until nullable (NULL = unbefristet); pack_id FK auf method_packs.id (training-intern)
    - KEINE FK in crm.* (workspace_id/user_id sind reine Integer/UUID-Werte ohne ForeignKey nach crm)
    - ProductFact.intent_tag + risk_flag (bool) + content JSONB; leer in W0
  </behavior>
  <action>
    Drei NEUE Klassen (neue Klassen → Punkt-14-Control-Flow nicht anwendbar; Punkt 21 oben erfüllt). Alle mit `__table_args__ = {'schema': 'training'}` (bei (a)/training; bei (b)/(c) Präfix anpassen nach Checkpoint 0).

    `class MethodPack(Base)` `__tablename__='method_packs'`:
    - `id = Column(Integer/UUID_TYPE, primary_key=True)` (Stil wie bestehende Models).
    - `name = Column(String(120), nullable=False, comment="Name des Methoden-Pakets (Intent→Technik-Mapping). Beim Launch genau EIN Paket = NERVE-Default. E1.")`
    - `owner = Column(String(32), nullable=False, server_default='NERVE', comment="Eigentuemer des Pakets. Vorerst immer NERVE (Coach-Feature = Post-Launch). E1.")`
    - `schema_version = Column(SmallInteger, nullable=False, default=1, comment="Format-Version des content-JSONB fuer spaetere Bumps (E1).")`
    - `content = Column(JSON, nullable=False, server_default='{}', comment="Paradigma-Regeln (unsichtbar) + Tabus (D-02) + Rollen-Register (gatekeeper/interessent/meeting, je ziel+hebel, Req 3) + Intent→Technik-Mapping (Req 4) + optionale fact_ids. KEIN Beispielsatz-Feld (E3, Req 2). Inhalt: Plan 02 + Andre-Freigabe.")`
    - `is_active = Column(Boolean, nullable=False, default=True)` (is_*-Flag → L-04 trivial, kein Schild)
    - `valid_until = Column(DateTime, nullable=True, comment="Gueltig-bis der Paket-Version (NULL = unbefristet). Versionierung E1; Fallback-Logik in Plan 03.")`
    - `created_at = Column(DateTime, default=utcnow)` (trivial).
    - Tabellen-Schild: "Methoden-Paket (Coach-Tuer E1): Intent→Technik-Mapping + Paradigma + Rollen-Register + Tabus als JSONB, versioniert. Owner vorerst NERVE. Status: lebt (neu, TAXO3). Schreibt <Coach-Admin Post-Launch>; liest services/prompt_pipeline.py (build_answer_context, Plan 03). KEINE crm-FK (Coach-Daten-Isolation)."

    `class PackAssignment(Base)` `__tablename__='pack_assignments'`:
    - `id`, `created_at` (trivial).
    - `pack_id = Column(<PK-Typ>, ForeignKey('training.method_packs.id'), nullable=False, index=True, comment="Welches Paket diese Zuweisung aktiviert. FK INNERHALB training (E2).")`
    - `user_id = Column(Integer, nullable=True, index=True, comment="Ziel-User der Zuweisung (KEINE FK in crm/public.users — Coach-Daten-Isolation). NULL = workspace-weit. E2.")`
    - `workspace_id = Column(Integer, nullable=True, index=True, comment="Ziel-Workspace (KEINE crm-FK). E2.")`
    - `valid_until = Column(DateTime, nullable=True, comment="Gueltig-bis (NULL = unbefristet). Abgelaufen/keine Zuweisung → stiller NERVE-Default (Plan 03, nie Loesch-Aktion). E2.")`
    - Tabellen-Schild: "Paket-Zuweisung (Coach-Tuer E2): user/workspace → method_pack mit valid_until. Abgelaufen/keine → stiller Default-Fallback (Plan 03). Status: lebt (neu, TAXO3). Schreibt <Coach-Admin Post-Launch>; liest services/prompt_pipeline.py (Plan 03). KEINE crm-FK."

    `class ProductFact(Base)` `__tablename__='product_facts'`:
    - `id`, `created_at` (trivial).
    - `intent_tag = Column(String(64), nullable=False, index=True, comment="Intent-Tag, an dem dieser Fakt andockt (Req 7). Verbindet Fakt mit der Taxonomie.")`
    - `risk_flag = Column(Boolean, nullable=False, default=False, comment="reguliert/riskant-Markierung (Req 7) — Grounding-Regel behandelt riskante Fakten vorsichtiger.")`
    - `content = Column(JSON, nullable=False, server_default='{}', comment="Strukturierter Produktfakt (Req 7). NUR Struktur in TAXO3 — KEINE echten Fakten eingepflegt (Andre-Entscheidung). Befuellen = spaetere Phase.")`
    - `workspace_id = Column(Integer, nullable=True, index=True, comment="Ziel-Workspace (KEINE crm-FK).")`
    - Tabellen-Schild: "Produktfakten-Andock (Req 7, NUR Struktur): intent-getaggte Fakten + IDs + reguliert/riskant-Flag fuer die Grounding-Regel. Leeres Regal — Befuellen spaetere Phase. Status: Reserve/Foundation (neu, TAXO3). Schreibt <spaetere Phase>; liest services/prompt_pipeline.py (Grounding, Plan 03). KEINE crm-FK, KEIN Live-Web-Call."

    Importiere fehlende Typen (Boolean/SmallInteger/JSON/ForeignKey/DateTime) konsistent zum models.py-Stil.
  </action>
  <verify>
    <automated>grep -c "class MethodPack\|class PackAssignment\|class ProductFact" database/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class MethodPack\|class PackAssignment\|class ProductFact" database/models.py` == 3
    - `grep "'schema': 'training'" database/models.py` deckt alle 3 ab (bei Option a; bei b/c entsprechend)
    - `grep "ForeignKey('training.method_packs.id')" database/models.py` == 1 (pack_id, training-intern)
    - KEINE crm-FK: `grep -E "ForeignKey\('crm\.|ForeignKey\(\"crm\." database/models.py` deckt KEINE der 3 neuen Klassen ab (manuelle Sicht auf die 3 Klassen-Blöcke)
    - KEIN Beispielsatz-Feld: `grep -iE "examples|beispiel|musterantwort" database/models.py` deckt die 3 neuen Klassen NICHT ab (E3)
    - `grep -c "comment=" database/models.py` deckt name/owner/schema_version/content/valid_until (MethodPack) + pack_id/user_id/workspace_id/valid_until (PackAssignment) + intent_tag/risk_flag/content/workspace_id (ProductFact) ab
    - Alle Spaltennamen ASCII (keine Umlaute in Identifiern)
  </acceptance_criteria>
  <done>3 ORM-Models vollständig (training-Schema, JSONB-Inhalt, KEINE crm-FK, KEIN Beispielsatz-Feld), Schilder gesetzt, keine Schreiber.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic-Migration (CREATE 3 training-Tabellen als postgres + narrow GRANT + COMMENTs)</name>
  <files>alembic/versions/XXXX_method_packs_pack_assignments_product_facts.py</files>
  <read_first>
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-op.execute-Stil)
    - alembic/versions/0013_*.py ODER eine training-berührende Migration (training-Schema-CREATE + GRANT-Muster wie nerve_anon_worker-Grant aus G/MEET Welle 3)
    - ssh ... 'bash scripts/inspect.sh migrations' (echter HEAD = down_revision)
  </read_first>
  <action>
    Neue Migration. `down_revision` = aktueller Alembic-HEAD (beim Execute via `inspect.sh migrations` lesen — TAXO1/2 haben 0016+ angelegt; NICHT hardcoden). Datei auf echte nächste Nummer benennen.
    upgrade() (läuft als postgres):
    1. `op.create_table('method_packs', ..., schema='training')` + `'pack_assignments'` (FK pack_id → training.method_packs.id) + `'product_facts'` — alle Spalten aus Task 1 (DDL-Parität). JSONB-Spalten `JSONB NOT NULL DEFAULT '{}'`.
    2. Indizes: pack_assignments(pack_id, user_id, workspace_id), product_facts(intent_tag, workspace_id).
    3. **OQ-1 (a) narrow GRANT (als postgres):**
       `op.execute("GRANT USAGE ON SCHEMA training TO nerve_app")`
       `op.execute("GRANT SELECT ON training.method_packs TO nerve_app")`
       `op.execute("GRANT SELECT ON training.pack_assignments TO nerve_app")`
       `op.execute("GRANT SELECT ON training.product_facts TO nerve_app")`
       — KEIN INSERT/UPDATE/DELETE an nerve_app. KEIN Grant an die DPO-Tabellen.
       (Bei Checkpoint-0 (b)/(c): Schema-Präfix + Grant-Block entsprechend; bei (b)/public ggf. ALTER OWNER nerve_app statt Grant.)
    4. KEINE crm-FK (Coach-harte-Linie). KEIN ALTER OWNER der training-Tabellen auf nerve_app (Owner bleibt postgres — Schreibpfad ist Post-Launch-Admin).
    5. Schilder: `COMMENT ON TABLE training.<t>` + `COMMENT ON COLUMN` für jede nicht-triviale Spalte (Texte aus Task-1-comment=). op.execute-Stil (0015). Umlaute in COMMENT-Strings OK.
    downgrade(): symmetrisch (REVOKE Grants, DROP 3 Tabellen mit schema='training'; USAGE-Grant nur zurücknehmen wenn keine anderen training-Grants für nerve_app existieren — defensiv: `REVOKE SELECT ON ...` + die 3 DROP TABLE; USAGE-REVOKE optional kommentiert, da andere Phasen es brauchen könnten).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "op.create_table\('method_packs'|op.create_table\('pack_assignments'|op.create_table\('product_facts'" alembic/versions/*method_packs*.py` == 3 + alle mit `schema='training'`
    - `grep "GRANT USAGE ON SCHEMA training TO nerve_app" alembic/versions/*method_packs*.py` == 1
    - `grep -c "GRANT SELECT ON training\." alembic/versions/*method_packs*.py` == 3
    - `grep -E "GRANT (INSERT|UPDATE|DELETE).*nerve_app" alembic/versions/*method_packs*.py` == 0 (SELECT-only)
    - `grep -iE "preference_pairs|transcript_archive" alembic/versions/*method_packs*.py` == 0 (DPO-Tabellen NICHT angefasst → Wand bleibt)
    - `grep -c "COMMENT ON COLUMN training\." alembic/versions/*method_packs*.py` >= 10 + 3 COMMENT ON TABLE
    - Nach Migration als postgres: `inspect.sh schilder method_packs` zeigt Tabelle + Spalten-Schilder (echter-Rolle-Lese-Proof folgt in Task 3, peer-auth)
  </acceptance_criteria>
  <done>Migration reversibel, 3 training-Tabellen live (leer), narrow GRANT aktiv (nerve_app liest, DPO-Wand intakt), Schilder gesetzt.</done>
</task>

<task type="auto">
  <name>Task 3: Schema-Smoke + nerve_app-Lese-Proof (ECHTE Rolle peer-auth, beide Seiten) + KEIN-crm-FK-Test + SCHILD-Guard GRÜN</name>
  <files>tests/test_method_pack_schema.py</files>
  <read_first>
    - tests/test_schild_guard.py + tests/conftest.py (Guard-Mechanik, pg_description, NERVE_SCHILD_TEST_DSN, peer-auth nerve_app-DSN-Muster)
    - .planning/phases/08.23.2.G-MEET-* SET-ROLE-/Rollen-Worker-Proof-Test (Vorbild für rollen-basierte psql-Assertions) falls auffindbar; sonst Muster aus tests/ für rollen-basierte Assertions
  </read_first>
  <action>
    Schreibe `tests/test_method_pack_schema.py` (Runtime-Behavior, KEIN Source-Presence — CLAUDE.md Test-Regel):
    - `test_method_pack_insert_roundtrip`: lege MethodPack (name, owner='NERVE', content={paradigm_rules:[...], roles:{...}}) an → commit → query zurück → assert content-Keys + owner default. Räumt auf (DELETE).
    - `test_pack_assignment_fallback_shape`: PackAssignment mit valid_until=NULL (unbefristet) + pack_id → assert FK greift, valid_until None. Plus eine Zeile mit valid_until in der Vergangenheit (Plan 03 wird sie als abgelaufen behandeln — hier nur Schema-Akzeptanz). Cleanup.
    - **`test_no_crm_foreign_key` (Coach-harte-Linie, Pflicht):** via `inspect.sh constraints` ODER psql `\d training.pack_assignments`/`product_facts` → assert KEINE FK referenziert `crm.*`. (Runtime: query information_schema.table_constraints/referential_constraints, assert kein referenced_table im crm-Schema.)
    - **`test_nerve_app_can_read_method_packs` (OQ-1-Grant-Proof, DSGVO-Gate, Pflicht — gegen die ECHTE nerve_app-DB-Rolle, NICHT postgres-then-SET-ROLE):**
      - Der Test öffnet eine eigene Verbindung als die **echte nerve_app-Rolle** über die per-ENV gereichte DSN (peer-auth auf Prod: `NERVE_GRANTPROOF_DSN=postgresql://nerve_app@/nerve`, gesetzt vom OS-User nerve_app im verify-Befehl). Wenn die DSN fehlt (lokal/SQLite) → `pytest.skip` (kein False-Green, wie der SCHILD-Guard).
      - **BEIDE Seiten explizit (Reviewer-Vorgabe):**
        - `SELECT count(*) FROM training.method_packs;` → **erfolgreich** (kein permission denied) — Grant greift.
        - `SELECT count(*) FROM training.preference_pairs;` → **permission denied** (psycopg-Exception InsufficientPrivilege ODER vergleichbar abfangen + assert) — DPO-Wall / DSGVO-Isolation intakt.
      - PRIMÄR-Proof ist diese echte-Rolle-Verbindung. Die `sudo -u postgres psql; SET ROLE nerve_app; ...`-Variante darf zusätzlich als SEKUNDÄR-Signal mitlaufen, ersetzt aber NICHT den echte-Rolle-Proof (kann Rollen-Attribut-/peer-auth-Unterschiede maskieren).
      - Kommentar im Test: warum echte-Rolle-Verbindung (peer-auth) statt SET-ROLE-Simulation + warum kein Function-Mock — das ist eine echte Runtime-/Permission-Assertion (Grant-Verhalten der echten Rolle), KEIN Source-Presence (CLAUDE.md Test-Regel).
    - `test_method_pack_no_examples_field`: assert das ORM-Model hat keine Spalte/JSONB-Konvention `examples` (E3) — via inspect.signature/Tabellen-Spalten-Liste (Runtime-API-Check, OK per CLAUDE.md).
    SCHILD-Guard: nach Deploy server-seitig MANUELL (OS-User nerve_app, NERVE_SCHILD_TEST_DSN) — muss alle 3 Tabellen + nicht-triviale Spalten GRÜN melden.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u nerve_app NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve NERVE_GRANTPROOF_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest tests/test_schild_guard.py tests/test_method_pack_schema.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh schema method_packs` (+ pack_assignments + product_facts) zeigt alle Spalten; alle 3 im training-Schema
    - SCHILD-Guard GRÜN: alle 3 Tabellen + nicht-triviale Spalten haben Schild >= 10 Zeichen (pg_description)
    - test_method_pack_insert_roundtrip + test_pack_assignment_fallback_shape grün server-seitig
    - **test_nerve_app_can_read_method_packs grün als ECHTE nerve_app-Rolle (peer-auth, NICHT SET-ROLE): `SELECT training.method_packs` erfolgreich (Grant greift) UND `SELECT training.preference_pairs` permission denied (DPO-Wall / DSGVO-Isolation intakt). Beide Seiten im SUMMARY verbatim belegt.**
    - **test_no_crm_foreign_key grün: KEINE FK referenziert crm.* (Coach-Daten-Isolation)**
    - test_method_pack_no_examples_field grün (E3, kein Beispielsatz-Feld)
    - `inspect.sh count method_packs` == 0 nach Cleanup (W0 = leer; EIN NERVE-Default kommt Plan 02)
  </acceptance_criteria>
  <done>3 Tabellen schema-verifiziert, nerve_app-Lese-Recht bewiesen als ECHTE Rolle (peer-auth, beide Seiten: method_packs OK / preference_pairs denied → DPO-Wand intakt), KEINE crm-FK, Guard grün, kein Schreiber im Live-Pfad.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Migration (postgres) → training-Schema + GRANT | privilegierter DDL/Grant-Pfad; öffnet eine schmale Tür für nerve_app |
| nerve_app (Live-App) → training.method_packs/pack_assignments/product_facts | Live-Loop liest hier (Plan 03); darf NICHT die DPO-Tabellen erreichen |
| method_pack content (JSONB) | wird später in vorgelesene Antwort gegeben (Plan 03) — Prompt-Injection-Fläche |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3-01-01 | I (Info Disclosure) | breiter Grant durchlöchert DPO-Wand (nerve_app liest preference_pairs) | mitigate | NUR `GRANT USAGE` + `GRANT SELECT` auf die 3 NEUEN Tabellen; KEIN Grant an preference_pairs/transcript_archive. Test test_nerve_app_can_read_method_packs prüft beide Seiten gegen die ECHTE nerve_app-Rolle (peer-auth): neu lesbar, DPO (preference_pairs) weiter `permission denied` (DSGVO-Gate). |
| T-TAXO3-01-02 | I (Info Disclosure) | Coach liest Kundendaten via FK ins crm | mitigate | KEINE crm-FK; user_id/workspace_id reine Werte. Test test_no_crm_foreign_key. |
| T-TAXO3-01-03 | E (Elevation) | nerve_app bekommt versehentlich INSERT/UPDATE auf method_packs (Coach-Schreibpfad ist Post-Launch) | mitigate | Grant ist SELECT-only; Acceptance grep `GRANT (INSERT|UPDATE|DELETE).*nerve_app` == 0. Schreiben = postgres/Admin (v2). |
| T-TAXO3-01-04 | T (Tampering) | method_pack/product_fact JSONB-Inhalt injiziert später Prompt-Anweisung in vorgelesene Antwort | accept (in W0) | In W0 leer/EIN-Default (von NERVE/André freigegeben). Grounding-Regel + apply_tabu_filter (Bestand) greifen erst in Plan 03; hier nur Schema. |
| T-TAXO3-01-05 | T (Tampering) | content nimmt beliebigen JSONB (kein DB-CHECK) | accept | Validierung Python-seitig im Loader (Plan 03, fail-open Default). JSONB ist Datenfeld, ORM parametrisiert. Niedriges Risiko (kein Live-User-Input direkt). |
</threat_model>

<verification>
- `inspect.sh schema method_packs|pack_assignments|product_facts` (verbatim in SUMMARY): alle Spalten, training-Schema, JSONB-Inhalt.
- SCHILD-Guard GRÜN server-seitig (3 Tabellen + Spalten beschildert).
- **nerve_app-Lese-Proof gegen die ECHTE nerve_app-Rolle (peer-auth, NICHT SET-ROLE): `SELECT … training.method_packs` → erfolgreich (Grant greift); `SELECT … training.preference_pairs` → permission denied (DPO-Wall / DSGVO-Isolation intakt). Beide Seiten verbatim in SUMMARY.**
- KEINE crm-FK (test + inspect.sh constraints).
- `inspect.sh count method_packs|pack_assignments|product_facts` == 0 (kein Schreiber in W0; EIN Default → Plan 02).
- `grep -rn "method_packs\|MethodPack" services/ routes/` == 0 aktive Leser (Loader kommt Plan 03/W1).
- OQ-1-Entscheidung (a/b/c) im SUMMARY + 05-Log dokumentiert.
</verification>

<success_criteria>
- 3 training-Tabellen live (leer), narrow GRANT aktiv (OQ-1 a, nach André+Gemini), nerve_app liest als ECHTE Rolle (peer-auth) / DPO-Wand intakt (preference_pairs weiter denied), KEINE crm-FK, KEIN Beispielsatz-Feld, alle nicht-trivialen Spalten beschildert (Guard grün).
- Risk-low Schema-Addition: KEIN Test-Anruf (reine Tür, RESEARCH §Wave-Cut W0). KEIN Live-Pfad berührt.
- OQ-1-Schema-Entscheidung bewusst getroffen (Checkpoint 0), nicht silently gelockt.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-01-SUMMARY.md` (inkl. verbatim `inspect.sh schema` der 3 Tabellen + count=0 + SCHILD-Guard-GRÜN-Output + echte-nerve_app-Rolle-Lese-Proof BEIDER Seiten [method_packs OK / preference_pairs denied, peer-auth] + OQ-1-Entscheidung + echter Migrations-Revision).
</output>
</content>
</invoke>
