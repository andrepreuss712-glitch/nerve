---
phase: 08.23.2.TAXO3
plan: 01
type: execute
wave: 1
complexity: 🔴
depends_on: []
files_modified:
  - database/models.py
  - alembic/versions/XXXX_method_packs_pack_assignments_product_facts.py
  - tests/test_method_pack_schema.py
autonomous: true
requirements: ["SPEC Req 4", "SPEC Req 7 (Schema-Anteil)"]
risiko: schema-addition
test_anruf: false
user_setup: []

must_haves:
  truths:
    - "public.method_packs + public.pack_assignments + public.product_facts existieren auf Production (leer/EIN-Default), im public-Schema wie die normalen App-Tabellen (OQ-1 = Option B, André 2026-06-12) — KEIN Tabellen-Eintrag im training-Schema, KEIN GRANT der Rolle nerve_app ins training-Schema"
    - "nerve_app (Live-App-Rolle) kann die 3 neuen public-Tabellen LESEN wie jede andere App-Tabelle (kein Grant nötig — public-Eigentum) — bewiesen als die ECHTE nerve_app-DB-Rolle auf Prod (peer-auth: OS-User nerve_app verbindet als DB-Rolle nerve_app via NERVE_..._DSN=postgresql://nerve_app@/nerve, NICHT postgres-then-SET-ROLE)"
    - "Die DPO-Wand bleibt ABSOLUT: nerve_app hat WEITERHIN NULL Zugriff aufs training-Schema (`permission denied for schema training` bleibt wahr) — die Migration fügt KEINEN training-Grant hinzu (Blast-Radius-Schutz: kein Weg vom Live-Pfad in den DPO-Tresor)"
    - "method_packs trägt schema_version, workspace_id (nullable), owner (default 'NERVE'), valid-Versionierung; pack_assignments trägt user_id/workspace_id → pack_id + valid_until (nullable=unbefristet); KEINE FK ins crm-Schema; KEIN Beispielsatz-Feld im JSONB-Schema"
    - "product_facts trägt intent-Tag + id + reguliert/riskant-Flag (Req 7 Andock-Struktur, leer) — KEINE crm-FK, KEINE echten Fakten eingepflegt"
    - "SCHILD-Guard läuft server-seitig GRÜN für alle 3 Tabellen + alle nicht-trivialen Spalten"
    - "Kein Live-Pfad liest die Tabellen in dieser Welle (Loader kommt Plan 03/W1) — reine Schema-Tür"
  artifacts:
    - path: "database/models.py"
      provides: "ORM MethodPack + PackAssignment + ProductFact (public-Schema, JSONB-Inhalt, schema_version, workspace_id, valid_until, Schilder)"
      contains: "class MethodPack"
    - path: "alembic/versions/XXXX_method_packs_pack_assignments_product_facts.py"
      provides: "CREATE 3 public-Tabellen (wie normale App-Tabellen, KEIN training-Schema, KEIN GRANT) + COMMENTs"
      contains: "op.create_table('method_packs'"
    - path: "tests/test_method_pack_schema.py"
      provides: "Schema-Smoke + nerve_app-Lese-Recht-Proof (ECHTE nerve_app-Rolle peer-auth: public.method_packs lesbar / training-Schema VERWEIGERT) + KEIN-crm-FK-Test + KEIN-training-Grant-Test + JSONB-roundtrip"
  key_links:
    - from: "database/models.py MethodPack/PackAssignment/ProductFact"
      to: "public.method_packs / public.pack_assignments / public.product_facts"
      via: "Alembic-Migration (normaler public-Tabellen-Weg, kein Sonderrecht)"
      pattern: "method_packs"
    - from: "nerve_app (Live-App-Rolle, build_answer_context Plan 03)"
      to: "public.method_packs / public.pack_assignments (SELECT)"
      via: "public-Schema natives App-Zugriffsrecht (KEIN GRANT, OQ-1 b)"
      pattern: "method_packs"
    - from: "pack_assignments.pack_id"
      to: "method_packs.id"
      via: "FK innerhalb public-Schema (KEINE crm-FK)"
      pattern: "ForeignKey('method_packs.id')"
---

> ✅ **OQ-1 RESOLVED — Option B (public-Schema), André-Entscheidung 2026-06-12.**
> Die Schema-Frage „WO leben method_packs + pack_assignments + product_facts?" ist ENTSCHIEDEN: **public-Schema** (App-Seite, wo die normalen App-Tabellen liegen), **KEIN GRANT der Rolle nerve_app ins training-Schema**.
> **Begründung (André, verbatim Sinn):** Blast-Radius. Falls der Live-Pfad (PiP/WebSocket) je kompromittiert wird, darf darüber KEIN Weg in den DPO-Trainings-Tresor existieren. Die Live-Tür ist während des Calls eh offen → keine zweite Tür in den Tresor bohren. Die DPO-Wand bleibt absolut: nerve_app hat NULL Zugriff auf training.
> **SPEC-Abweichung (dokumentiert):** SPEC Req 4 schreibt wörtlich „training-Schema". Diese André-OQ-1-Entscheidung (Option B) **überstimmt** die Wortwahl bewusst — die Coach-Daten-Isolation wird durch die ABWESENHEIT jeglichen training-Zugriffs sogar STÄRKER gewahrt als durch einen narrow Grant. SPEC.md (locked) bleibt unberührt; diese Notiz lenkt den Executor.

> RISIKO-BANNER: **schema-addition.** Reine Tür (3 leere/EIN-Default Tabellen im public-Schema + Schild). KEIN Live-Pfad berührt, KEIN Loader (Plan 03/W1), KEIN Test-Anruf (RESEARCH §Wave-Cut W0, TAXO1 D-03 Auflage 1), KEIN GRANT, KEIN training-Eingriff — aber SCHILD-Guard GRÜN Pflicht.

> 🛡️ **DSGVO/DPO-ISOLATIONS-GATE (Blast-Radius, André OQ-1 b):** Der Grant-Proof (Task 3) läuft als die **ECHTE** nerve_app-DB-Rolle auf Prod (peer-auth `postgresql://nerve_app@/nerve`, OS-User nerve_app — NICHT `postgres`-then-`SET ROLE`, das die echten Rollen-Rechte maskieren kann). Er MUSS BEIDE Seiten beweisen: (1) `SELECT … FROM public.method_packs` → **erfolgreich** (natives public-App-Recht, kein Grant nötig); (2) `SELECT … FROM training.preference_pairs` → **permission denied for schema training** (DPO-Wand UNANGETASTET — keine neue Tür). Plus: die Migration enthält NULL `GRANT … training … nerve_app`-Zeilen (kein neuer Tresor-Zugang).

> ⚠️ **CROSS-AI-GATE (🔴-Phase):** Cross-AI-Review erfolgt (REVIEWS.md, Gemini). OQ-1 ist jetzt durch André entschieden (Option B) — der frühere „[NEEDS ANDRÉ+GEMINI]"-Checkpoint entfällt. Deploy weiterhin erst nach 3-Wege-Interlock TAXO1/2/3 (intent_event-Klebstoff) + TAXO1-04-I-4-Fix (Phasen-Orchestrierung, nicht dieser Plan).

> 🔎 **VOR-EXECUTE-RE-GREP + RE-VERIFY (RESEARCH Valid until 2026-06-19):** Beim Execute neu ziehen: `inspect.sh migrations` (echter HEAD = down_revision; TAXO1/2 legen 0016+ an) + `ssh ... sudo -u postgres psql nerve -c "SELECT has_schema_privilege('nerve_app','training','USAGE');"` (bestätigen: weiterhin `f` — die Wand steht und BLEIBT, dieser Plan ändert daran NICHTS) + `grep -rn "method_packs\|pack_assignments\|product_facts\|MethodPack" services/ routes/ database/` == 0 (Tabellen wirklich neu) + Migrations-User-Konvention der bestehenden public-Tabellen bestätigen (normaler Alembic-Lauf, KEIN postgres-Sonderweg, KEIN GRANT).

<objective>
Baue die **Coach-Tür** (E1–E3): die 3 Daten-Tabellen `public.method_packs`, `public.pack_assignments`, `public.product_facts` — LEER bzw. EIN NERVE-Default (Inhalt kommt Plan 02/W0b). Reine Schema-Addition im **public-Schema** (wie die normalen App-Tabellen), die dem Live-Loop (`build_answer_context`, Plan 03) nativen Lese-Zugriff gibt — **OHNE** die DPO-Wand auch nur zu berühren (OQ-1 = Option B, André 2026-06-12: kein training-Grant, Blast-Radius-Schutz). Schild (Punkt 23) auf alle 3 Tabellen + jede nicht-triviale Spalte.

Purpose: Ohne diese Tabellen kann `build_answer_context()` (Plan 03) das Intent→Technik-Mapping nicht über `pack_assignments → method_packs` laden (SPEC Req 4). Risk-low Schema-Addition ZUERST (Wellen-Schnitt, kein Big-Bang), bevor irgendein Live-Pfad sie liest. Public-Schema → nerve_app liest nativ (kein Grant), training bleibt für nerve_app dicht.
Output: 3 public-Tabellen live auf Prod (leer), ORM-Models, Migration (kein Grant), Schema-Smoke + nerve_app-Lese-Proof (public lesbar / training verweigert), SCHILD-Guard GRÜN.

KEIN Loader. KEINE build_answer_context. KEIN Live-Pfad berührt. KEINE echten Produkt-Fakten (Req 7 = nur Struktur). KEIN training-Grant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Phase-Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen (lesen sonst Basis-Dir `08.23.2`). STATE/ROADMAP hand-editieren. SUMMARY ins hardcodierte Verzeichnis.

**Verify = Production only (CLAUDE.md HART):** `bash deploy.sh production` (server-seitiges Pytest-Gate vor Restart) + `inspect.sh` gegen Prod. KEIN lokales pytest als Acceptance, KEIN `git pull` auf Prod. **Migration im public-Schema läuft wie alle bestehenden App-Tabellen (normaler Alembic-Lauf) — KEIN postgres-Sonderweg, KEIN GRANT** (der postgres-only-Sonderweg galt nur fürs training-Schema, das hier NICHT mehr angefasst wird). Beim Execute die Migrations-User-Konvention der bestehenden public-Tabellen bestätigen. SCHILD-Guard MANUELL server-seitig (`tests/test_schild_guard.py`, OS-User nerve_app peer-auth, NERVE_SCHILD_TEST_DSN).

**Migrations-Revision (HART, DEPENDS-ON-TAXO1/2):** Prod-HEAD = 0015 (Stand SCHILD). TAXO1/2 legen 0016+ an. Revision NICHT hardcoden — beim Execute `inspect.sh migrations` lesen, down_revision = dann-HEAD, Datei auf echte nächste Nummer benennen.

**UTF-8-Regel:** Umlaute nur in user-facing Text. DB-Spalten/dict-keys/COMMENT-Identifier ASCII (ae/oe/ue/ss). COMMENT-Text-Strings dürfen Umlaute tragen (UTF-8 DB).

**SSH-Pattern:** `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh <cmd>'`. Privilege-maskierte training-Sicht (nur fürs Beweisen der intakten Wand): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql nerve -c "..."'`. ECHTE nerve_app-Rolle (Grant-Proof): `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u nerve_app NERVE_GRANTPROOF_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest ...'`.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md

<interfaces>
<!-- Muster zum Kopieren — bewiesen aus dem Code/Prod. KEINE Codebase-Exploration nötig. -->

public-Tabellen anlegen wie die bestehenden App-Tabellen — KEIN schema='training', KEIN GRANT, KEIN ALTER OWNER-Sonderweg (OQ-1 b):
```
-- in upgrade(), normaler Alembic-Lauf (public default):
op.create_table('method_packs', ... )            # KEIN schema='training'
op.create_table('pack_assignments', ... )        # FK pack_id -> method_packs.id (public)
op.create_table('product_facts', ... )
-- KEIN GRANT-Block. KEIN training-Eingriff. nerve_app liest public nativ (App-Eigentum), wie calls/conversation_logs.
-- DPO-Tabellen (training.preference_pairs/transcript_archive) bleiben UNANGETASTET → Wand absolut.
```

Versions-Lade-Muster (Vorbild für Plan-03-Loader, hier NUR Schema): services/prompt_pipeline.py:30 resolve_prompt_version — ENV-Override → Cache → DB-Variants → fail-open. Plan-03-Loader analog: pack_assignments(user/workspace, valid_until) → method_packs → abgelaufen/keine → stiller NERVE-Default.

COMMENT-Schild-Muster (Migration 0015, hauseigenes op.execute — public, kein training-Präfix):
```
op.execute("COMMENT ON TABLE method_packs IS '<Zweck>. Status: lebt. Schreibt <admin-pfad post-launch>; liest services/prompt_pipeline.py (build_answer_context, Plan 03).'")
op.execute("COMMENT ON COLUMN method_packs.<col> IS '<...>'")
```

profile_faqs-Schema (Req 7 Andock-Referenz, RESEARCH §Persistenz verbatim) — product_facts ist die NEUE intent-getaggte Struktur, NICHT profile_faqs (das bleibt FAQ-Fuzzy-Match):
```
profile_faqs: id|profile_id(FK profiles)|frage_muster|antwort|kategorie|created_at|used_count|mode
```

method_pack JSONB-Form (E3, ⚙️ Discretion — KEIN Beispielsatz-Feld):
```json
{
  "schema_version": 1,
  "paradigm_rules": ["<6-10 Verhaltens-Regeln, unsichtbar>"],
  "taboos": ["<3 harte Tabus D-02>"],
  "roles": {
    "gatekeeper":   {"ziel": "<Respekt+Ehrlichkeit>", "hebel": ["<1-2 Verhaltens-Hebel>"]},
    "interessent":  {"ziel": "<Sorge diagnostizieren + relevanter Fakt>", "hebel": [...]},
    "meeting":      {"ziel": "<tiefer, beide Seiten hörbar>", "hebel": [...]}
  },
  "intents": { "<intent_type>": {"ziel": "...", "hebel": [...], "fact_ids": [<optional product_facts.id-Verweise>]} }
}
```
(Inhalt → Plan 02/W0b destilliert + André-Freigabe. Hier nur: JSONB-Spalte akzeptiert diese Form, KEIN Feld `examples`/`beispiele`/`musterantworten`.)
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `public.method_packs` (CREATE) — existiert NICHT (Beleg unten)
- `public.pack_assignments` (CREATE) — existiert NICHT
- `public.product_facts` (CREATE) — existiert NICHT
- `training` (Schema) — **NICHT angefasst** (OQ-1 b: kein Grant, kein Eingriff; nur zur Beweisführung gelesen, dass die Wand steht)

### inspect.sh / psql-Beleg (VERBATIM aus RESEARCH §Persistenz, 2026-06-12)
```
$ ssh ... sudo -u postgres psql nerve -c "SET ROLE nerve_app; SELECT count(*) FROM training.preference_pairs;"
SET
ERROR:  permission denied for schema training          <- bleibt so (Wand absolut, dieser Plan aendert NICHTS daran)

$ ssh ... sudo -u postgres psql nerve -c "SELECT has_schema_privilege('nerve_app','training','USAGE');"
 nerve_app_usage
-----------------
 f                                                       <- bleibt `f` (kein Grant hinzugefuegt)

$ \dn+ training
   Name   |  Owner   |      Access privileges
----------+----------+------------------------------
 training | postgres | postgres=UC/postgres        +
          |          | nerve_anon_worker=U/postgres   <- nerve_app NICHT in der Liste, bleibt so

$ inspect.sh migrations   ->  0015   (TAXO1/2 heben auf 0016+; beim Execute neu lesen)
```
**Beim Execute neu ziehen:** `inspect.sh migrations` (echter HEAD = down_revision) + `has_schema_privilege('nerve_app','training','USAGE')` (muss `f` BLEIBEN — keine neue Tür) + nach Migration: `SET ROLE nerve_app; SELECT count(*) FROM public.method_packs;` → 0 (public nativ lesbar, kein Grant nötig).

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `MethodPack.content` (JSONB) | gelesen build_answer_context (Plan 03, nerve_app) | DB public.method_packs.content JSONB | ⬜ Task 3 Schema + nerve_app-Lese-Proof |
| `MethodPack.schema_version` | gelesen Loader (Plan 03) | DB feste Spalte SmallInt | ⬜ Task 3 |
| `PackAssignment.pack_id → method_packs.id` | Loader-Join (Plan 03) | DB FK INNERHALB public (KEINE crm-FK) | ⬜ Task 3 KEIN-crm-FK-Test |
| `PackAssignment.valid_until` (nullable) | Fallback-Logik (Plan 03) | DB Timestamp nullable | ⬜ Task 3 |
| `ProductFact.intent_tag / risk_flag` | gelesen Grounding (Plan 03, leer in W0) | DB public.product_facts | ⬜ Task 3 |
| nerve_app liest public / NICHT training | build_answer_context Live-Read + DPO-Wand | public nativ (App-Eigentum) / training verweigert | ⬜ Task 3 nerve_app-Rolle-Proof BEIDE Seiten |

## Verbindungs-Karte (Punkt 22)
- **method_packs/pack_assignments/product_facts** existieren NICHT (psql oben) → reine Neu-Tabellen, kein bestehender Schreiber/Leser. `grep -rn "method_packs\|pack_assignments\|product_facts\|MethodPack\|PackAssignment\|ProductFact" services/ routes/ database/` == 0 vor diesem Plan (Beleg Task 0).
- **Schema-Ort (OQ-1 = Option B, André 2026-06-12):** public-Schema (wo die normalen App-Tabellen liegen). nerve_app liest public nativ (App-Eigentum, KEIN Grant). training bleibt für nerve_app dicht (Blast-Radius-Schutz). SPEC-Req-4-Wortlaut „training-Schema" bewusst überstimmt (André).
- **KEIN training-Grant (bewiesen-Pflicht):** nerve_app=`permission denied for schema training` + USAGE=`f` BLEIBT so. Die Migration fügt KEINE `GRANT … training …`-Zeile hinzu (Task-2-Acceptance grep == 0). DPO-Tabellen unangetastet → Coach-Daten-Isolation absolut.
- **KEINE crm-FK (Coach-harte-Linie):** pack_assignments.pack_id → method_packs.id (innerhalb public). workspace_id/user_id sind reine Werte-Spalten (KEIN FK in crm.accounts/users). Beleg Task 3 KEIN-crm-FK-Test.
- **Migrations-Rolle:** public-Tabellen werden wie alle App-Tabellen per normalem Alembic-Lauf angelegt (kein postgres-Sonderweg, der war nur fürs training-Schreibrecht nötig). Beim Execute die Migrations-User-Konvention der bestehenden public-Tabellen bestätigen.

<tasks>

<task type="auto">
  <name>Task 0: Neuheit beweisen (Punkt 22 grep==0) + Wellen-Schnitt-Beleg + DPO-Wand-Ausgangszustand</name>
  <read_first>
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md §Code-Insights (Andock-Punkte)
  </read_first>
  <action>
    Führe den Neuheits-grep aus und schreibe das Ergebnis in die SUMMARY (Task 0): `grep -rn "method_packs\|pack_assignments\|product_facts\|MethodPack\|PackAssignment\|ProductFact" services/ routes/ database/ alembic/` — erwartet 0 aktive Treffer (Migration-Datei dieses Plans ausgenommen). Belegt: reine Neu-Tabellen, kein bestehender Schreiber/Leser. Plus: `inspect.sh migrations` (echter HEAD = down_revision für Task 2) + `sudo -u postgres psql nerve -c "SELECT has_schema_privilege('nerve_app','training','USAGE');"` (muss `f` sein — der Ausgangszustand der Wand; dieser Plan lässt ihn `f`) verbatim in SUMMARY.
  </action>
  <verify>
    <automated>grep -rc "MethodPack\|PackAssignment\|ProductFact" services/ routes/ database/ | grep -v ':0' | grep -v 'models.py'</automated>
  </verify>
  <acceptance_criteria>
    - grep-Beleg in SUMMARY: 0 aktive method_packs/pack_assignments/product_facts-Referenzen vor diesem Plan (außer der neuen Migration)
    - `inspect.sh migrations`-Output verbatim in SUMMARY (echter HEAD)
    - `has_schema_privilege('nerve_app','training','USAGE')` == `f` verbatim in SUMMARY (Wand-Ausgangszustand — bleibt `f`, kein Grant)
  </acceptance_criteria>
  <done>Neuheit + Migrations-HEAD + DPO-Wand-Ausgangszustand (`f`, bleibt) belegt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: ORM MethodPack + PackAssignment + ProductFact in database/models.py (public-Schema, JSONB, Schilder)</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py (bestehende public-App-Tabellen-Stil — z.B. ConversationLog/Call — als Vorbild; KEIN __table_args__-schema, public ist Default)
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md §Standard Stack (method_pack JSONB-Form E3) + §Security Domain (workspace_id, KEINE crm-FK)
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-Stil als Schild-Vorlage)
  </read_first>
  <behavior>
    - MethodPack instanziierbar mit name+owner='NERVE'+schema_version=1+content={} ; ASCII-Spaltennamen
    - content (JSONB) akzeptiert die E3-Form (paradigm_rules/taboos/roles/intents) UND lehnt KEIN Feld ab — aber das Schema sieht KEIN `examples`-Feld vor (E3: kein Beispielsatz-Feld)
    - PackAssignment.valid_until nullable (NULL = unbefristet); pack_id FK auf method_packs.id (public-intern)
    - KEINE FK in crm.* (workspace_id/user_id sind reine Integer/UUID-Werte ohne ForeignKey nach crm)
    - ProductFact.intent_tag + risk_flag (bool) + content JSONB; leer in W0
    - KEIN __table_args__ = {'schema': 'training'} (OQ-1 b — public default, wie die anderen App-Tabellen)
  </behavior>
  <action>
    Drei NEUE Klassen (neue Klassen → Punkt-14-Control-Flow nicht anwendbar; Punkt 21 oben erfüllt). **KEIN `__table_args__ = {'schema': ...}`** — public ist Default, genau wie die bestehenden App-Tabellen (Call/ConversationLog).

    `class MethodPack(Base)` `__tablename__='method_packs'`:
    - `id = Column(Integer/UUID_TYPE, primary_key=True)` (Stil wie bestehende Models).
    - `name = Column(String(120), nullable=False, comment="Name des Methoden-Pakets (Intent→Technik-Mapping). Beim Launch genau EIN Paket = NERVE-Default. E1.")`
    - `owner = Column(String(32), nullable=False, server_default='NERVE', comment="Eigentuemer des Pakets. Vorerst immer NERVE (Coach-Feature = Post-Launch). E1.")`
    - `schema_version = Column(SmallInteger, nullable=False, default=1, comment="Format-Version des content-JSONB fuer spaetere Bumps (E1).")`
    - `content = Column(JSON, nullable=False, server_default='{}', comment="Paradigma-Regeln (unsichtbar) + Tabus (D-02) + Rollen-Register (gatekeeper/interessent/meeting, je ziel+hebel, Req 3) + Intent→Technik-Mapping (Req 4) + optionale fact_ids. KEIN Beispielsatz-Feld (E3, Req 2). Inhalt: Plan 02 + Andre-Freigabe.")`
    - `is_active = Column(Boolean, nullable=False, default=True)` (is_*-Flag → L-04 trivial, kein Schild)
    - `valid_until = Column(DateTime, nullable=True, comment="Gueltig-bis der Paket-Version (NULL = unbefristet). Versionierung E1; Fallback-Logik in Plan 03.")`
    - `created_at = Column(DateTime, default=utcnow)` (trivial).
    - Tabellen-Schild: "Methoden-Paket (Coach-Tuer E1): Intent→Technik-Mapping + Paradigma + Rollen-Register + Tabus als JSONB, versioniert. Owner vorerst NERVE. Status: lebt (neu, TAXO3, public-Schema OQ-1-b). Schreibt <Coach-Admin Post-Launch>; liest services/prompt_pipeline.py (build_answer_context, Plan 03). KEINE crm-FK (Coach-Daten-Isolation)."

    `class PackAssignment(Base)` `__tablename__='pack_assignments'`:
    - `id`, `created_at` (trivial).
    - `pack_id = Column(<PK-Typ>, ForeignKey('method_packs.id'), nullable=False, index=True, comment="Welches Paket diese Zuweisung aktiviert. FK INNERHALB public (E2).")`
    - `user_id = Column(Integer, nullable=True, index=True, comment="Ziel-User der Zuweisung (KEINE FK in crm/users — Coach-Daten-Isolation). NULL = workspace-weit. E2.")`
    - `workspace_id = Column(Integer, nullable=True, index=True, comment="Ziel-Workspace (KEINE crm-FK). E2.")`
    - `valid_until = Column(DateTime, nullable=True, comment="Gueltig-bis (NULL = unbefristet). Abgelaufen/keine Zuweisung → stiller NERVE-Default (Plan 03, nie Loesch-Aktion). E2.")`
    - Tabellen-Schild: "Paket-Zuweisung (Coach-Tuer E2): user/workspace → method_pack mit valid_until. Abgelaufen/keine → stiller Default-Fallback (Plan 03). Status: lebt (neu, TAXO3, public). Schreibt <Coach-Admin Post-Launch>; liest services/prompt_pipeline.py (Plan 03). KEINE crm-FK."

    `class ProductFact(Base)` `__tablename__='product_facts'`:
    - `id`, `created_at` (trivial).
    - `intent_tag = Column(String(64), nullable=False, index=True, comment="Intent-Tag, an dem dieser Fakt andockt (Req 7). Verbindet Fakt mit der Taxonomie.")`
    - `risk_flag = Column(Boolean, nullable=False, default=False, comment="reguliert/riskant-Markierung (Req 7) — Grounding-Regel behandelt riskante Fakten vorsichtiger.")`
    - `content = Column(JSON, nullable=False, server_default='{}', comment="Strukturierter Produktfakt (Req 7). NUR Struktur in TAXO3 — KEINE echten Fakten eingepflegt (Andre-Entscheidung). Befuellen = spaetere Phase.")`
    - `workspace_id = Column(Integer, nullable=True, index=True, comment="Ziel-Workspace (KEINE crm-FK).")`
    - Tabellen-Schild: "Produktfakten-Andock (Req 7, NUR Struktur): intent-getaggte Fakten + IDs + reguliert/riskant-Flag fuer die Grounding-Regel. Leeres Regal — Befuellen spaetere Phase. Status: Reserve/Foundation (neu, TAXO3, public). Schreibt <spaetere Phase>; liest services/prompt_pipeline.py (Grounding, Plan 03). KEINE crm-FK, KEIN Live-Web-Call."

    Importiere fehlende Typen (Boolean/SmallInteger/JSON/ForeignKey/DateTime) konsistent zum models.py-Stil.
  </action>
  <verify>
    <automated>grep -c "class MethodPack\|class PackAssignment\|class ProductFact" database/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class MethodPack\|class PackAssignment\|class ProductFact" database/models.py` == 3
    - KEIN training-Schema: `grep -n "'schema': 'training'" database/models.py` deckt KEINE der 3 neuen Klassen ab (public default)
    - `grep "ForeignKey('method_packs.id')" database/models.py` == 1 (pack_id, public-intern)
    - KEINE crm-FK: `grep -E "ForeignKey\('crm\.|ForeignKey\(\"crm\." database/models.py` deckt KEINE der 3 neuen Klassen ab (manuelle Sicht auf die 3 Klassen-Blöcke)
    - KEIN Beispielsatz-Feld: `grep -iE "examples|beispiel|musterantwort" database/models.py` deckt die 3 neuen Klassen NICHT ab (E3)
    - `grep -c "comment=" database/models.py` deckt name/owner/schema_version/content/valid_until (MethodPack) + pack_id/user_id/workspace_id/valid_until (PackAssignment) + intent_tag/risk_flag/content/workspace_id (ProductFact) ab
    - Alle Spaltennamen ASCII (keine Umlaute in Identifiern)
  </acceptance_criteria>
  <done>3 ORM-Models vollständig (public-Schema, JSONB-Inhalt, KEINE crm-FK, KEIN Beispielsatz-Feld), Schilder gesetzt, keine Schreiber.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic-Migration (CREATE 3 public-Tabellen wie App-Tabellen, KEIN Grant, KEIN training-Eingriff + COMMENTs)</name>
  <files>alembic/versions/XXXX_method_packs_pack_assignments_product_facts.py</files>
  <read_first>
    - alembic/versions/0015_table_column_comments_schilder.py (COMMENT-op.execute-Stil)
    - eine bestehende public-Tabellen-CREATE-Migration als Vorbild (normaler public-create_table, KEIN schema-arg, KEIN Grant)
    - ssh ... 'bash scripts/inspect.sh migrations' (echter HEAD = down_revision)
  </read_first>
  <action>
    Neue Migration. `down_revision` = aktueller Alembic-HEAD (beim Execute via `inspect.sh migrations` lesen — TAXO1/2 haben 0016+ angelegt; NICHT hardcoden). Datei auf echte nächste Nummer benennen.
    upgrade() (normaler Alembic-Lauf, public default):
    1. `op.create_table('method_packs', ...)` + `'pack_assignments'` (FK pack_id → method_packs.id) + `'product_facts'` — alle Spalten aus Task 1 (DDL-Parität), **KEIN `schema='training'`** (public default, wie alle App-Tabellen). JSONB-Spalten `JSONB NOT NULL DEFAULT '{}'`.
    2. Indizes: pack_assignments(pack_id, user_id, workspace_id), product_facts(intent_tag, workspace_id).
    3. **KEIN GRANT-Block. KEIN training-Eingriff. KEIN ALTER OWNER-Sonderweg.** nerve_app liest public nativ (App-Eigentum). Die DPO-Tabellen (training.*) werden NICHT angefasst → Wand absolut (OQ-1 b, Blast-Radius).
    4. KEINE crm-FK (Coach-harte-Linie).
    5. Schilder: `COMMENT ON TABLE method_packs` + `COMMENT ON COLUMN` für jede nicht-triviale Spalte (Texte aus Task-1-comment=). op.execute-Stil (0015), public (kein training-Präfix). Umlaute in COMMENT-Strings OK.
    downgrade(): symmetrisch (DROP 3 Tabellen, public; KEINE Grant-Revokes nötig, da kein Grant erteilt wurde).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "op.create_table\('method_packs'|op.create_table\('pack_assignments'|op.create_table\('product_facts'" alembic/versions/*method_packs*.py` == 3
    - KEIN training-Schema: `grep -E "schema='training'|schema=\"training\"" alembic/versions/*method_packs*.py` == 0
    - **KEIN GRANT: `grep -ciE "GRANT" alembic/versions/*method_packs*.py` == 0** (kein Grant überhaupt; insbesondere KEIN training-Grant)
    - **KEIN training-Eingriff: `grep -iE "training\.|SCHEMA training" alembic/versions/*method_packs*.py` == 0** (DPO-Wand unangetastet)
    - `grep -iE "preference_pairs|transcript_archive" alembic/versions/*method_packs*.py` == 0 (DPO-Tabellen NICHT angefasst)
    - `grep -c "COMMENT ON COLUMN method_packs\.\|COMMENT ON COLUMN pack_assignments\.\|COMMENT ON COLUMN product_facts\." alembic/versions/*method_packs*.py` >= 10 + 3 COMMENT ON TABLE
    - Nach Migration: `inspect.sh schilder method_packs` zeigt Tabelle + Spalten-Schilder; `sudo -u postgres psql -c "SET ROLE nerve_app; SELECT count(*) FROM public.method_packs;"` → 0 (public nativ lesbar)
  </acceptance_criteria>
  <done>Migration reversibel, 3 public-Tabellen live (leer), KEIN Grant, training-Wand unangetastet, Schilder gesetzt.</done>
</task>

<task type="auto">
  <name>Task 3: Schema-Smoke + nerve_app-Rolle-Proof (public lesbar / training verweigert) + KEIN-crm-FK + KEIN-training-Grant + SCHILD-Guard GRÜN</name>
  <files>tests/test_method_pack_schema.py</files>
  <read_first>
    - tests/test_schild_guard.py + tests/conftest.py (Guard-Mechanik, pg_description, NERVE_SCHILD_TEST_DSN, peer-auth-nerve_app-Muster)
    - eine bestehende rollen-basierte psql-Assertion in tests/ als Vorbild (echte nerve_app-Rolle via DSN, nicht SET ROLE)
  </read_first>
  <action>
    Schreibe `tests/test_method_pack_schema.py` (Runtime-Behavior, KEIN Source-Presence — CLAUDE.md Test-Regel):
    - `test_method_pack_insert_roundtrip`: lege MethodPack (name, owner='NERVE', content={paradigm_rules:[...], roles:{...}}) an → commit → query zurück → assert content-Keys + owner default. Räumt auf (DELETE).
    - `test_pack_assignment_fallback_shape`: PackAssignment mit valid_until=NULL (unbefristet) + pack_id → assert FK greift, valid_until None. Plus eine Zeile mit valid_until in der Vergangenheit (Plan 03 wird sie als abgelaufen behandeln — hier nur Schema-Akzeptanz). Cleanup.
    - **`test_no_crm_foreign_key` (Coach-harte-Linie, Pflicht):** via `inspect.sh constraints` ODER psql `\d pack_assignments`/`product_facts` → assert KEINE FK referenziert `crm.*`. (Runtime: query information_schema.table_constraints/referential_constraints, assert kein referenced_table im crm-Schema.)
    - **`test_nerve_app_can_read_method_packs` (OQ-1-b DSGVO/DPO-Proof, Pflicht — ECHTE nerve_app-Rolle, peer-auth, NICHT SET-ROLE):** Öffne eine Verbindung **AS the real nerve_app role** (DSN aus ENV `NERVE_GRANTPROOF_DSN=postgresql://nerve_app@/nerve`; `pytest.skip` wenn ENV fehlt, damit lokal kein False-Green). Assert BEIDE Seiten:
      (1) `SELECT count(*) FROM public.method_packs` → **erfolgreich** (natives public-App-Recht, kein Grant nötig).
      (2) `SELECT count(*) FROM training.preference_pairs` → wirft **permission denied for schema training** (DPO-Wand absolut, keine neue Tür).
      Dokumentiere mit Kommentar im Test, warum echte Rolle statt Function-Mock: testet das echte Rollen-Runtime-Verhalten (Grant-Abwesenheit + public-Zugriff), kein Source-Presence. SET-ROLE-Variante (`sudo -u postgres … SET ROLE nerve_app`) NUR als optionales Sekundär-Signal, NICHT als Primär-Proof.
    - **`test_migration_adds_no_training_grant` (Blast-Radius, Pflicht):** der EINZIGE assert ist das echte Runtime-Signal — server-seitig `has_schema_privilege('nerve_app','training','USAGE')` ist weiterhin `f` (DPO-Wand-Invariante: kein neuer training-Zugang). KEIN Migrations-Datei-grep als Assert (Source-Presence-Falle vermeiden, CLAUDE.md Test-Regel) — der Datei-grep darf höchstens als erklärender Kommentar im Test stehen, nie als eigenständige Assertion. Begründung im Kommentar: prüft die echte Rollen-Privilege-Invariante, nicht Code-Existenz.
    - `test_method_pack_no_examples_field`: assert das ORM-Model hat keine Spalte/JSONB-Konvention `examples` (E3) — via inspect.signature/Tabellen-Spalten-Liste (Runtime-API-Check, OK per CLAUDE.md).
    SCHILD-Guard: nach Deploy server-seitig MANUELL (OS-User nerve_app, NERVE_SCHILD_TEST_DSN) — muss alle 3 Tabellen + nicht-triviale Spalten GRÜN melden.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && sudo -u nerve_app NERVE_SCHILD_TEST_DSN=postgresql://nerve_app@/nerve NERVE_GRANTPROOF_DSN=postgresql://nerve_app@/nerve /opt/nerve/venv/bin/pytest tests/test_schild_guard.py tests/test_method_pack_schema.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `inspect.sh schema method_packs` (+ pack_assignments + product_facts) zeigt alle Spalten; alle 3 im public-Schema
    - SCHILD-Guard GRÜN: alle 3 Tabellen + nicht-triviale Spalten haben Schild >= 10 Zeichen (pg_description)
    - test_method_pack_insert_roundtrip + test_pack_assignment_fallback_shape grün server-seitig
    - **test_nerve_app_can_read_method_packs grün als ECHTE nerve_app-Rolle (peer-auth, NICHT SET-ROLE): `SELECT public.method_packs` erfolgreich (public nativ) UND `SELECT training.preference_pairs` permission denied (DPO-Wand absolut). Beide Seiten im SUMMARY verbatim belegt.**
    - **test_migration_adds_no_training_grant grün: einziger Assert = `has_schema_privilege('nerve_app','training','USAGE')` == `f` bleibt (Runtime-Privilege-Invariante). Die „Migration enthält 0 GRANT-Zeilen"-Prüfung ist separat das Task-2-Migrations-Acceptance (grep auf die Build-Artefakt-Datei), NICHT Teil dieses Runtime-Tests.**
    - **test_no_crm_foreign_key grün: KEINE FK referenziert crm.* (Coach-Daten-Isolation)**
    - test_method_pack_no_examples_field grün (E3, kein Beispielsatz-Feld)
    - `inspect.sh count method_packs` == 0 nach Cleanup (W0 = leer; EIN NERVE-Default kommt Plan 02)
  </acceptance_criteria>
  <done>3 public-Tabellen schema-verifiziert, nerve_app-Rolle-Proof (public lesbar / training absolut dicht), kein neuer Grant, KEINE crm-FK, Guard grün, kein Schreiber im Live-Pfad.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Migration → public-Schema | normaler App-DDL-Pfad; legt 3 public-Tabellen an, KEIN Sonderrecht, KEIN training-Eingriff |
| nerve_app (Live-App) → public.method_packs/pack_assignments/product_facts | Live-Loop liest hier (Plan 03); natives public-App-Recht (kein Grant) |
| nerve_app (Live-App) → training-Schema | MUSS dicht bleiben (absolute DPO-Wand); dieser Plan öffnet KEINE Tür |
| method_pack content (JSONB) | wird später in vorgelesene Antwort gegeben (Plan 03) — Prompt-Injection-Fläche |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3-01-01 | I (Info Disclosure) | Live-Pfad-Kompromiss erreicht über die Coach-Tabellen den DPO-Tresor (training) | mitigate | **OQ-1 b (Blast-Radius):** Tabellen im public-Schema, KEIN GRANT der Rolle nerve_app ins training-Schema. nerve_app behält NULL training-Zugriff (`permission denied`, USAGE=`f` bleibt). Test test_nerve_app_can_read_method_packs (beide Seiten) + test_migration_adds_no_training_grant beweisen die unangetastete Wand. Stärker als ein narrow Grant: es existiert gar keine Tür. |
| T-TAXO3-01-02 | I (Info Disclosure) | Coach liest Kundendaten via FK ins crm | mitigate | KEINE crm-FK; user_id/workspace_id reine Werte. Test test_no_crm_foreign_key. |
| T-TAXO3-01-03 | T (Tampering) | method_pack/product_fact JSONB-Inhalt injiziert später Prompt-Anweisung in vorgelesene Antwort | accept (in W0) | In W0 leer/EIN-Default (von NERVE/André freigegeben). Grounding-Regel + apply_tabu_filter (Bestand) greifen erst in Plan 03; hier nur Schema. |
| T-TAXO3-01-04 | T (Tampering) | content nimmt beliebigen JSONB (kein DB-CHECK) | accept | Validierung Python-seitig im Loader (Plan 03, fail-open Default). JSONB ist Datenfeld, ORM parametrisiert. Niedriges Risiko (kein Live-User-Input direkt). |
</threat_model>

<verification>
- `inspect.sh schema method_packs|pack_assignments|product_facts` (verbatim in SUMMARY): alle Spalten, public-Schema, JSONB-Inhalt.
- SCHILD-Guard GRÜN server-seitig (3 Tabellen + Spalten beschildert).
- **DPO/Blast-Radius-Proof (OQ-1 b): nerve_app-Rolle (peer-auth) — `SELECT public.method_packs` erfolgreich; `SELECT training.preference_pairs` → permission denied (Wand absolut). Migration enthält 0 GRANT-Zeilen; `has_schema_privilege('nerve_app','training','USAGE')` == `f` bleibt.**
- KEINE crm-FK (test + inspect.sh constraints).
- `inspect.sh count method_packs|pack_assignments|product_facts` == 0 (kein Schreiber in W0; EIN Default → Plan 02).
- `grep -rn "method_packs\|MethodPack" services/ routes/` == 0 aktive Leser (Loader kommt Plan 03/W1).
- OQ-1 = Option B (public) im SUMMARY + 05-Log dokumentiert (André-Entscheidung + Blast-Radius-Begründung).
</verification>

<success_criteria>
- 3 public-Tabellen live (leer), nerve_app liest public nativ (kein Grant), training-Wand absolut dicht (kein neuer Zugang), KEINE crm-FK, KEIN Beispielsatz-Feld, alle nicht-trivialen Spalten beschildert (Guard grün).
- Risk-low Schema-Addition: KEIN Test-Anruf (reine Tür, RESEARCH §Wave-Cut W0). KEIN Live-Pfad berührt. KEIN training-Eingriff.
- OQ-1-Schema-Entscheidung = Option B (André 2026-06-12, Blast-Radius) umgesetzt + dokumentiert.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-01-SUMMARY.md` (inkl. verbatim `inspect.sh schema` der 3 public-Tabellen + count=0 + SCHILD-Guard-GRÜN-Output + nerve_app-Rolle-Proof BEIDE Seiten [public lesbar / training verweigert] + Beleg kein neuer training-Grant + OQ-1-Option-B-Dokumentation + echter Migrations-Revision).
</output>
