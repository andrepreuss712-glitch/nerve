---
phase: 08.23.2.TAXO3
slice: b
plan: b-03
type: execute
wave: 2
komplexitaet: "🔴 (neuer Live-Antwort-Pfad, starkes Modell, Live-Antwort-Latenz)"
depends_on: [b-01]
files_modified:
  - config.py
  - services/claude_service.py
  - services/deepgram_service.py
  - static/js/pip-launcher.js
  - templates/live.html
  - tests/test_taxo3b_continuation.py
autonomous: false
decision_ids: [D-05, D-06, D-07, D-08, D-10, D-11, D-16, RV-1, RV-2, RV-4]
user_setup: []

must_haves:
  truths:
    - "Der 'Freie Antwort'-Knopf führt aus dem Gesprächs-Kontext weiter — Eingang = die letzten Berater-Aussagen (gespiegeltes Kunden-Echo) aus per-SID RAM (_session_state[sid]['analysiert_bisher']), NIE aus der DB (RV-1)."
    - "Der Knopf nutzt das STARKE Modell via config-Schalter MODEL_CONTINUATION (D-05/D-07) durch die EINE gemeinsame Modell-Aufruf-Naht answer_system_content (kein bespoke Pfad, D-07)."
    - "Echte TTFT wird im Test-Anruf gemessen (Pflicht-Akzeptanzkriterium, RV-2/D-06) und ein DEDIZIERTER Continuation-TTFT-Circuit-Breaker (eigener _continuation_fallback_until + eigene TTFT-Historie, KEINE Kopplung an den EWB-Breaker) liefert bei zu langsamem Anlauf eine sichere generische Rückfrage statt Hänger (RV-2 Notnetz) — NIE einen Haiku-Rückfall."
    - "Bei semantisch unklarem gespiegeltem Echo entscheidet das Continuation-LLM selbst per erzwungenem JSON {\"action\":\"ask_clarification\"} vs. {\"action\":\"continue\",\"text\":...} (RV-4/D-08) — KEINE geratene Weiterführung."
    - "Das gespiegelte Kunden-Echo wird als intent_event mit build_mirror_payload (is_mirrored_echo, gesenkte confidence) + speaker_role='berater' geschrieben (b-01-Vertrag, D-16) — nie ein harter Kunden-Fakt."
    - "Der Knopf ist dauerhaft sichtbar bei den bestehenden Einwand-Knöpfen (D-10) und respektiert das Gesprächs-Start-Gate — feuert NICHT vor erkanntem Gespräch (D-11 — konkret: _durable_call_id(state['call_id']) is not None), sonst deterministische Antwort (kein Confidence-Gate pro Antwort)."
  artifacts:
    - path: "config.py"
      provides: "MODEL_CONTINUATION-Schalter (starkes Modell, Override ohne Redeploy)"
      contains: "MODEL_CONTINUATION"
    - path: "services/claude_service.py"
      provides: "streame_continuation_antwort() — starkes Modell, RAM-Eingang, DEDIZIERTER TTFT-Circuit-Breaker (_continuation_fallback_until + eigene deque), JSON-Bailout"
      contains: "_continuation_fallback_until"
    - path: "services/deepgram_service.py"
      provides: "Continuation-Socket-Handler handle_freie_antwort + Mirror-Marker-Emit (build_mirror_payload)"
      contains: "build_mirror_payload"
    - path: "static/js/pip-launcher.js"
      provides: "Dauerhaft sichtbarer 'Freie Antwort'-Knopf + Start-Gate-Respekt"
      contains: "freie_antwort"
  key_links:
    - from: "streame_continuation_antwort"
      to: "_session_state[sid]['analysiert_bisher']"
      via: "RAM-Read der letzten Berater-Aussagen zum Klick (RV-1)"
      pattern: "analysiert_bisher"
    - from: "streame_continuation_antwort"
      to: "config.MODEL_CONTINUATION via answer_system_content"
      via: "gemeinsame Modell-Aufruf-Naht (D-07)"
      pattern: "answer_system_content"
    - from: "streame_continuation_antwort"
      to: "eigener _continuation_fallback_until + _continuation_ttft_history"
      via: "isolierter Continuation-Breaker (RV-2, KEINE _ewb_*-Kopplung)"
      pattern: "_continuation_fallback_until"
    - from: "Continuation-Handler"
      to: "intent_event.payload_jsonb (is_mirrored_echo)"
      via: "emit_intent_event(extra_payload=build_mirror_payload(...))"
      pattern: "build_mirror_payload"
---

<objective>
(B) Der "Freie Antwort"-Knopf: der Berater spiegelt die Kunden-Antwort kurz laut -> drückt den Knopf
-> NERVE führt aus dem Gesprächs-Kontext weiter (schließt die Frage-Schleife, die der Cold-Call sonst
nicht schließen kann, weil NERVE den Kunden nicht hört). Plus (C) Gespiegelt-Marker + (D) Knopf-Gate.

Purpose: Continuation = härteste Inferenz (aus holprigem, einseitigem Echo die wahrscheinliche
Kunden-Aussage erschließen) -> BEWUSST das STARKE Modell (D-05). Der bewusste Knopfdruck landet in
der natürlichen Gesprächspause ("schnell genug" = "landet in der Pause", nicht "<300ms").

Kritische Naht (Konsens-Kernbotschaft, RV-1/HIGH-1/Punkt 26): der Knopf liest das gespiegelte Echo
aus per-SID RAM (analysiert_bisher — LIVE/inkrementell befuellt, in b-01 bewiesen), NIE aus
transcript_segments (batch am Call-Ende -> leer). Der bestehende Knopf-Pfad (deepgram_service.py:987)
liest schon exakt so — dieser Plan folgt derselben Naht.

Modell-Disziplin (D-06/D-07/RV-2): starkes Modell via config MODEL_CONTINUATION durch die EINE
gemeinsame Naht answer_system_content (KEIN bespoke Modell-Call-Pfad). TTFT wird im Test-Anruf
GEMESSEN (Pflicht); Latenz-Problem -> Tempo-Arbeit (Phase-2-Caching/kleinerer Input/Streaming), NIE
Rückfall aufs schwache Modell. Notnetz: DEDIZIERTER Continuation-TTFT-Circuit-Breaker -> sichere
generische Rückfrage.

**RV-2 Breaker-ISOLATION (BLOCKER-Fix, kanonisch):** Der Continuation-Breaker teilt sich KEINEN
Zustand mit dem bestehenden EWB-Breaker (_ewb_fallback_until / _ewb_ttft_history). Grund (am Code
verifiziert 2026-07-02): der EWB-Breaker setzt bei aktivem Fenster _model_autovar='claude-haiku-4-5-...'
(claude_service.py:625) und sampelt jede TTFT in die GETEILTE _ewb_ttft_history-deque (:645). Würde
Continuation denselben Zustand nutzen, entstünden ZWEI Fehler: (a) ein aktives EWB-Haiku-Fenster würde
Continuation am FALSCHEN Signal gaten, und (b) langsame Continuation-TTFT-Samples würden in
_ewb_ttft_history landen und den LIVE-EWB-Pfad fälschlich auf Haiku kippen (Cross-Kontamination).
RV-2 verbietet JEDEN Haiku-Rückfall für Continuation — der Breaker liefert die sichere generische
Rückfrage, NIE einen Schwachmodell-Call. Daher: ein EIGENER _continuation_fallback_until (monotonic)
+ eine EIGENE _continuation_ttft_history-deque, strikt getrennt.

Unsicherheit (RV-4/D-08): das Continuation-LLM entscheidet per erzwungenem JSON selbst
(ask_clarification vs. continue) — kein Raten. Ask-back setzt auf der gesenkten Marker-confidence (G1)
auf -> spaeter nur ein Schwellenwert-Dreh (D-09), kein Rebuild.

Output: MODEL_CONTINUATION-Schalter; streame_continuation_antwort() (RAM-Eingang, starkes Modell via
gemeinsame Naht, DEDIZIERTER TTFT-Circuit-Breaker, JSON-Bailout); Mirror-Marker-Emit (b-01-Vertrag,
D-16); dauerhaft sichtbarer Knopf + Start-Gate-Respekt (D-10/D-11); Pflicht-Test-Anruf mit TTFT-Messung.

**Scope-Note (kein Split jetzt):** b-03 ist am oberen Rand des Kontext-Budgets (FE+BE, 6 Files). Bleibt
zusammen — der FE-Knopf (Task 3) ist der EINZIGE Split-Punkt, falls der Execute-Kontext degradiert
(dann Task 3 in einen Folge-Plan b-04 auslagern). JETZT NICHT splitten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-b-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-b-REVIEWS.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-b-01-persistenz-marker-eventform-PLAN.md

<interfaces>
<!-- Aus dem Repo verifiziert 2026-07-02. -->

config.py:93-94 — Muster der PIP-Modell-Schalter (schon Sonnet):
```python
MODEL_PIP_AUTOVAR = os.getenv("MODEL_PIP_AUTOVAR", "claude-sonnet-4-5")
MODEL_PIP_VARIANTE = os.getenv("MODEL_PIP_VARIANTE", "claude-sonnet-4-5")
```

services/claude_service.py:740 streame_manual_ewb_variante(typ, profile_einwand, kontext, sid, slot)
— STRUKTURELLES VORBILD. Nutzt bereits die gemeinsame Naht (:775-777):
```python
from services.prompt_pipeline import answer_system_content as _asc_manual
_system_manual = _asc_manual(sid, is_auto_triggered=False, primary_intent=typ, confidence=1.0)
with claude_client.messages.stream(model=config.MODEL_PIP_VARIANTE, max_tokens=500,
     system=_system_manual, messages=[{'role':'user','content': user_msg}]) as stream:
    for token in stream.text_stream: ...  # emit pip_token
```

services/claude_service.py:18-20 — der BESTEHENDE EWB-TTFT-Circuit-Breaker (NUR als STRUKTUR-Vorbild
lesen — NICHT wiederverwenden, siehe RV-2-Isolation unten):
```python
_ewb_ttft_history: deque = deque(maxlen=5)   # last 5 TTFT ms — GEHOERT DEM EWB-PFAD
_ewb_fallback_until: float = 0.0             # monotonic ts — EWB-Haiku-Fenster
_ewb_circuit_lock = threading.Lock()
```
Trip-Pfad (:642-654): _ttft_ms messen -> _ewb_ttft_history.append -> bei >=3/5 über Schwelle
_ewb_fallback_until setzen; bei aktivem Fenster _model_autovar='claude-haiku-4-5-...' (:625).
⛔ RV-2 (BLOCKER): dieser Zustand ist EWB-EXKLUSIV. Continuation baut das MUSTER nach, aber mit
EIGENEN Symbolen (_continuation_ttft_history / _continuation_fallback_until / _continuation_circuit_lock)
UND ohne jeden Haiku-Zweig (der Continuation-Breaker liefert die generische Rückfrage, kein Modell-Call).

services/deepgram_service.py:864 handle_manual_ewb(data, sid) — Socket-Handler-Vorbild.
:987 kontext = " ".join(ls._session_state.get(_sid,{}).get('analysiert_bisher', [])[-20:])
  <- EXAKT die RV-1 RAM-Quelle. :919-927 emit_intent_event-Aufruf-Vorbild.

services/live_session.py:529-537 _durable_call_id(raw) — DAS Gesprächs-Start-Gate (D-11, am Code
verifiziert 2026-07-02):
```python
def _durable_call_id(raw):
    # None/'__call_pending__' -> None ; durable UUID-str bleibt
    return raw if (raw and raw != '__call_pending__') else None
```
Ein Gespräch ist LIVE erkannt <=> `_durable_call_id(_session_state[sid]['state'].get('call_id'))` is not None.
Vorher steht call_id auf None (nie gesetzt) oder auf dem Sentinel '__call_pending__' (create_call_for_sid:581).
Der EWB-Knopf respektiert exakt diese Naht: deepgram_service.py:902 `_btn_cid = ls._durable_call_id(_btn_st.get('call_id'))`.
HINWEIS: es gibt KEIN separates call_active-Flag — die durable-call_id-Präsenz IST das Gate.

services/intent_event_writer.py (b-01) — build_mirror_payload(*, mirrored_text, lowered_confidence)
+ MIRROR_SCHEMA_VERSION; emit_intent_event(..., extra_payload=build_mirror_payload(...)).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MODEL_CONTINUATION-Schalter + streame_continuation_antwort (RV-1 RAM, D-05/D-07 Naht, RV-2 ISOLIERTER Circuit-Breaker, RV-4 JSON-Bailout)</name>
  <files>config.py, services/claude_service.py, tests/test_taxo3b_continuation.py</files>
  <read_first>
    - config.py:74,93-97 (MODEL_* Muster + Sonnet-Default + os.getenv-Override)
    - services/claude_service.py:740-867 (streame_manual_ewb_variante — VOLLES Vorbild: Naht, Streaming, Retry, Cost-Hook)
    - services/claude_service.py:18-20 + :619-654 (EWB-TTFT-Circuit-Breaker — NUR Struktur-Vorbild; der Zustand _ewb_ttft_history/_ewb_fallback_until ist EWB-EXKLUSIV, siehe :625 Haiku-Zweig + :645 geteilte deque)
    - services/prompt_pipeline.py:680-699 (answer_system_content — die gemeinsame Naht, D-07)
    - CONTEXT D-06/D-07/RV-2/RV-4 (Modell-Disziplin, Breaker-Isolation, JSON-Bailout)
  </read_first>
  <behavior>
    - Test 1 (RV-1 RAM-Quelle): streame_continuation_antwort(sid) liest den Eingang aus
      _session_state[sid]['analysiert_bisher'] (gemockt) — NICHT aus einer DB-Query. Ein Test mit
      leerem analysiert_bisher + befuellter (Anti-)transcript_segments-Mock beweist: es wird RAM gelesen.
    - Test 2 (D-07 Naht): der Modell-Aufruf nutzt config.MODEL_CONTINUATION UND answer_system_content
      (gemockt/gepatcht) — kein bespoke System-Prompt-Selbstbau.
    - Test 3 (RV-4 Bailout): bei LLM-Antwort {"action":"ask_clarification","frage":"X oder Y?"} gibt
      streame_continuation_antwort eine Rückfrage-Empfehlung im "Frag nach:"-Format zurueck (kein
      geratener Continuation-Text); bei {"action":"continue","text":"..."} gibt es den Continuation-Text.
    - Test 4 (RV-2 Breaker-Kurzschluss): ist _continuation_fallback_until aktiv (monotonic in der
      Zukunft), liefert der Pfad die sichere generische Rückfrage OHNE Modell-Aufruf (claude_client.messages.stream
      wird NICHT aufgerufen — via Mock-Assertion assert_not_called) — Notnetz, kein Hänger, kein Haiku.
    - Test 5 (RV-2 Breaker-ISOLATION — der BLOCKER-Beweis, KEINE Cross-Kontamination):
      (a) Ein getripptes Continuation-Fenster (_continuation_fallback_until in der Zukunft) liefert die
          generische Rückfrage UND setzt/berührt WEDER _ewb_fallback_until NOCH _ewb_ttft_history
          (Assertion: beide unverändert vor/nach dem Call).
      (b) Umgekehrt: ein aktives EWB-Fenster (_ewb_fallback_until in der Zukunft) beeinflusst
          streame_continuation_antwort NICHT — der Continuation-Pfad feuert normal (Breaker NICHT als
          getrippt gewertet), weil er NUR _continuation_fallback_until liest.
      (c) Ein langsamer Continuation-TTFT-Sample landet in _continuation_ttft_history, NICHT in
          _ewb_ttft_history (Assertion: _ewb_ttft_history-Länge unverändert nach einem Continuation-Call).
    - Cleanup: keine DB-Rows in diesem Task (reiner Antwort-Pfad); falls ein Test committet, cleanup_rows.
  </behavior>
  <action>
1. config.py — neuer Schalter neben den PIP-Modellen:
   `MODEL_CONTINUATION = os.getenv("MODEL_CONTINUATION", "claude-sonnet-4-5")`
   (starkes Modell Default, Override ohne Redeploy — D-07 (1); Rollback-Kommentar wie MODEL_PIP_AUTOVAR).
   INFO (nicht regressen): Sonnet-Default ist korrekt (Punkt 25 + die bestehenden Live-Sonnet-Pfade
   MODEL_EWB/MODEL_QA/MODEL_PIP_* + Haiku-Breaker) — NICHT auf Haiku ändern.
2. services/claude_service.py — DEDIZIERTE Continuation-Breaker-Infra neben (NICHT geteilt mit) der
   EWB-Infra (:18-20) anlegen (RV-2 BLOCKER-Fix, ISOLATION):
   ```python
   _continuation_ttft_history: deque = deque(maxlen=5)   # eigene deque — NIE _ewb_ttft_history
   _continuation_fallback_until: float = 0.0             # eigener monotonic ts — NIE _ewb_fallback_until
   _continuation_circuit_lock = threading.Lock()          # eigener Lock
   ```
   KEINE Wiederverwendung von _ewb_* — sonst (a) EWB-Haiku-Fenster gated Continuation falsch und
   (b) Continuation-Samples kippen den LIVE-EWB-Pfad auf Haiku (Cross-Kontamination). Der
   Continuation-Breaker hat KEINEN Haiku-Zweig (D-06: der Breaker liefert eine Rückfrage, NIE Haiku).
3. services/claude_service.py — neue Funktion `streame_continuation_antwort(sid, slot=1)` nach dem
   Vorbild streame_manual_ewb_variante, ABER:
   - **Eingang aus RAM (RV-1):** `kontext = " ".join(ls._session_state.get(sid,{}).get('analysiert_bisher', [])[-N:])`
     — die letzten Berater-Aussagen inkl. dem laut gespiegelten Kunden-Echo. KEIN DB-Read. (Der
     Aufrufer/Handler in Task 2 reicht sid; die RAM-Lese-Stelle lebt hier oder im Handler — EINE Stelle.)
   - **Modell via gemeinsame Naht (D-07 (2)):** System-Prompt aus
     `answer_system_content(sid, is_auto_triggered=False, primary_intent='continuation', confidence=<gesenkt>)`;
     Modell = `config.MODEL_CONTINUATION`. KEIN neuer bespoke Modell-Call-Pfad — dieselbe
     claude_client.messages.stream-Naht wie der Knopf.
   - **RV-4 JSON-Bailout:** die user_msg zwingt das Modell zu erzwungenem JSON:
     `{"action":"continue","text":"..."}` ODER `{"action":"ask_clarification","frage":"Frag nach: X oder Y?"}`.
     Parse die (gestreamte oder final) Antwort: action=continue -> Continuation-Text streamen;
     action=ask_clarification -> die "Frag nach:"-Rückfrage ausgeben (das bestehende FE-Frag-nach-
     Format, P1-02 strukturell erhalten). Ungueltiges JSON -> sichere generische Rückfrage (fail-safe).
   - **RV-2 DEDIZIERTER TTFT-Circuit-Breaker (ISOLIERT, KEIN _ewb_*):** VOR dem Modell-Call unter
     _continuation_circuit_lock prüfen `time.monotonic() < _continuation_fallback_until`; ist das
     Fenster aktiv -> KEIN Modell-Aufruf, sondern SOFORT die sichere generische Rückfrage
     ("Frag nach, was genau der Kunde meint — dann helfe ich gezielt."). Beim Streaming den ersten-Token-
     TTFT messen (Muster :642-654), aber in `_continuation_ttft_history` (NICHT _ewb_ttft_history);
     bei >=3/5 über Schwelle `_continuation_fallback_until` setzen + [CONT-TTFT]-Log. Eigener
     ENV-Schwellen-Key (z.B. CONTINUATION_FALLBACK_TTFT_MS). NIE Rückfall aufs schwache Modell (D-06) —
     der Breaker liefert eine Rückfrage, NICHT Haiku. Er berührt _ewb_fallback_until/_ewb_ttft_history NIE.
   - Emits: pip_stream_start/pip_token/pip_token_done wie der Knopf (source='continuation').
   - Fail-open: jede Stoerung -> sichere generische Rückfrage, nie raise, Live-Loop-Garantie.
4. Der eigentliche intent_event/Mirror-Emit passiert im Handler (Task 2) — diese Funktion ist der
   Antwort-Pfad.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <!-- Prod-Gate faehrt tests/test_taxo3b_continuation.py (Behavior: RAM-Quelle, Naht, JSON-Bailout, Breaker-Kurzschluss, Breaker-ISOLATION). HART: kein Local-Pytest. Live-TTFT kommt im Test-Anruf (Task 3). -->
  </verify>
  <acceptance_criteria>
    - grep: `grep -n "MODEL_CONTINUATION" config.py services/claude_service.py` zeigt Schalter + Nutzung.
    - grep: `grep -n "_continuation_fallback_until\|_continuation_ttft_history\|_continuation_circuit_lock" services/claude_service.py`
      zeigt die DEDIZIERTE Breaker-Infra (eigene Symbole).
    - grep (ISOLATIONS-BEWEIS, statisch): innerhalb der neuen Funktion streame_continuation_antwort
      taucht KEIN `_ewb_fallback_until` und KEIN `_ewb_ttft_history` auf — der Continuation-Pfad liest/
      schreibt AUSSCHLIESSLICH die _continuation_*-Symbole (via Code-Review + gezieltem grep der Funktion belegen).
    - grep: `grep -n "answer_system_content\|analysiert_bisher\|ask_clarification" services/claude_service.py`
      zeigt gemeinsame Naht + RAM-Quelle + JSON-Bailout in der neuen Funktion.
    - Test (Behavior, kein source-presence): RAM-Read (nicht DB), Naht-Nutzung, action=continue vs.
      ask_clarification, Breaker-Kurzschluss (Test 4), Breaker-ISOLATION (Test 5 a/b/c: getripptes
      Continuation-Fenster berührt _ewb_* NICHT; aktives EWB-Fenster gated Continuation NICHT;
      Continuation-Sample landet nicht in _ewb_ttft_history).
    - Prod-Deploy-Gate GRUEN.
  </acceptance_criteria>
  <done>streame_continuation_antwort liest RAM (RV-1), nutzt MODEL_CONTINUATION via gemeinsame Naht (D-05/D-07), hat JSON-Bailout (RV-4) + DEDIZIERTEN, ISOLIERTEN TTFT-Circuit-Breaker (RV-2 — eigener _continuation_fallback_until/_continuation_ttft_history, KEINE _ewb_*-Kopplung, kein Haiku-Rückfall), fail-open, Behavior-getestet inkl. Isolations-Beweis.</done>
</task>

<task type="auto">
  <name>Task 2: Continuation-Socket-Handler handle_freie_antwort + Mirror-Marker-Emit + Start-Gate (RV-1, C/D-16, D-11)</name>
  <files>services/deepgram_service.py, tests/test_taxo3b_continuation.py</files>
  <read_first>
    - services/deepgram_service.py:864-1030 (handle_manual_ewb — Handler-Vorbild: mode-Quelle, get_or_open_moment, emit_intent_event, _run-Thread, RAM-kontext:987, record_suggestion_offer)
    - services/deepgram_service.py:894-927 (der EWB-Handler liest _btn_cid = ls._durable_call_id(_btn_st.get('call_id')) unter _session_state_lock — GENAU das Gate, das der Continuation-Handler auch respektiert)
    - services/live_session.py:529-537 (_durable_call_id — DAS Start-Gate; None/'__call_pending__' = kein Gespräch) + :581 (Sentinel-Zustand)
    - services/intent_event_writer.py (b-01: build_mirror_payload + MIRROR_SCHEMA_VERSION)
    - CONTEXT D-11 (Gesprächs-Start-Gate: feuert NICHT vor erkanntem Call — vorher ehrliche "noch kein Gespräch erkannt"; sonst deterministisch, KEIN Confidence-Gate pro Antwort)
  </read_first>
  <action>
1. Neuer Socket-Handler `handle_freie_antwort(data=None, sid=None)` (registriert wie handle_manual_ewb):
   - **Start-Gate (D-11) — KONKRETER Identifier (Punkt 22, am Code verifiziert):** lies unter
     `ls._session_state_lock` das per-SID `state.get('call_id')` und wende `ls._durable_call_id(...)`
     an — EXAKT wie der EWB-Pfad (deepgram_service.py:902). Ist das Ergebnis `None` (call_id nie gesetzt
     ODER Sentinel '__call_pending__') -> es ist KEIN Gespräch erkannt -> emittiere eine ehrliche
     "noch kein Gespräch erkannt"-Antwort (kein Modell-Aufruf, keine geratene Fortsetzung). Ist die
     durable call_id gesetzt (Call live erkannt) -> DETERMINISTISCH weiter (KEIN Confidence-Gate pro
     Antwort — der alte smalltalk_none-Unterdrueckungs-Bug darf NICHT zurueckkommen, D-11).
   - Ruft in einem _run-Thread `streame_continuation_antwort(_sid, slot=...)` (Task 1) auf.
2. **Mirror-Marker-Emit (C/D-16, b-01-Vertrag):** analog zum EWB-intent_event-Emit (:919-927), aber:
   - `speaker_role='berater'` (D-16: Single-Speaker cold_call — das laut gespiegelte Kunden-Echo bleibt
     eine BERATER-Aussage, NIE ein harter Kunden-Fakt).
   - `confidence=<gesenkt>` (G1, z.B. 0.4 — der spaeter tunbare Ask-back-Hebel, D-09).
   - `extra_payload=build_mirror_payload(mirrored_text=<anonymisiertes Echo oder None>, lowered_confidence=<gesenkt>)`
     (is_mirrored_echo=True + mirror_schema_version, RV-3, KEINE Migration).
   - call_id/interaction_id/mode/user_id/org_id wie der EWB-Emit (get_or_open_moment, _durable_call_id).
   - triggering_text = anonymisiert (anonymize_output + Sentinel->None), wie der EWB-Pfad.
   - try/except: der Emit crasht den Live-Loop nie (bestehende Disziplin).
3. record_suggestion_offer(slot=..., source='continuation', model=config.MODEL_CONTINUATION, ...) wie
   der Knopf-Pfad (latenz-neutral RAM-Append) — damit die Continuation-Antwort in (A)s Vorantworten-
   Ring landet (Kohärenz-Kette).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <!-- Prod-Gate faehrt die Handler-Tests (Start-Gate-Verhalten via _durable_call_id, Mirror-Emit schreibt is_mirrored_echo + speaker_role='berater' + gesenkte confidence). Danach Live-Test (Task 3). Committende Tests raeumen intent_event-Rows via cleanup_rows. -->
  </verify>
  <acceptance_criteria>
    - grep: `grep -n "handle_freie_antwort\|build_mirror_payload\|speaker_role='berater'\|_durable_call_id" services/deepgram_service.py` zeigt Handler + Mirror-Emit + D-16-Rolle + das konkrete Start-Gate.
    - Test (Behavior): Handler mit call_id=None (bzw. '__call_pending__') -> ehrliche
      "noch kein Gespräch erkannt"-Antwort, KEIN Modell-Aufruf (streame_continuation_antwort NICHT
      aufgerufen); Handler mit durable call_id gesetzt -> ruft streame_continuation_antwort
      deterministisch (kein per-Antwort-Confidence-Gate).
    - Test (DB-Behavior): der Mirror-Emit schreibt eine intent_event-Zeile mit
      payload_jsonb->>'is_mirrored_echo'='true', speaker_role='berater', gesenkter confidence; Query
      findet sie; Test raeumt sie weg (cleanup_rows).
    - Nach einem manuellen Test-Emit: `ssh ... 'bash scripts/inspect.sh sample intent_event 5'` zeigt
      KEINE Test-Mirror-Zeilen (Baseline-Sauberkeit).
    - Prod-Deploy-Gate GRUEN inkl. SCHILD-Guard. Das Tabellen-Schild nennt deepgram_service.py als
      Mirror/Continuation-Schreiber bereits (in b-01 gesetzt) — prüfen dass es stimmt; sonst nachziehen (Punkt 23).
  </acceptance_criteria>
  <done>Continuation-Handler respektiert das Start-Gate über _durable_call_id(state['call_id']) (D-11, konkreter Identifier), ruft den Antwort-Pfad, schreibt den Gespiegelt-Marker (is_mirrored_echo + speaker_role='berater' + gesenkte confidence, b-01-Vertrag/D-16), landet in (A)s Vorantworten-Ring; Rows aufgeraeumt; Schild aktuell.</done>
</task>

<task type="auto">
  <name>Task 3: Dauerhaft sichtbarer 'Freie Antwort'-Knopf im PiP (D-10/D-12) + Verdrahtung</name>
  <files>static/js/pip-launcher.js, templates/live.html</files>
  <read_first>
    - static/js/pip-launcher.js (bestehende Einwand-Knopf-Render-Stelle + der manual_ewb-Emit ans Backend; grep nach 'manual_ewb' / dem Knopf-Container)
    - templates/live.html (PiP-Knopf-Bereich / EWB-Button-Container)
    - CONTEXT D-10 (dauerhaft sichtbar, stabil bei den bestehenden Knöpfen, KEIN zustandsabhängiges Ein-/Ausblenden — vermeidet Bug-B-Klasse) + D-12 (finale Politur = PIP.2, JETZT nur funktional stabil, NICHT überdesignen)
  </read_first>
  <action>
1. Fuege einen "Freie Antwort"-Knopf DAUERHAFT sichtbar bei den bestehenden Einwand-Knöpfen ein
   (D-10 — KEIN zustandsabhängiges Ein-/Ausblenden, stabile Anzeige wie PIP.1). Label: "Freie Antwort"
   (User-facing, echte Umlaute erlaubt); Mikrocopy-Hinweis kurz "Kunden-Antwort in EINEM Satz spiegeln,
   dann drücken" (Bau-Vorgabe §6a — genauer finaler Wortlaut = PIP.2, D-12: NICHT überdesignen).
   Code-Identifier ASCII: id/class/data-typ = z.B. `freie_antwort` / `data-action="freie_antwort"`.
2. onClick -> emittiere das bestehende Socket-Event-Muster ans Backend (analog manual_ewb) mit dem
   neuen Handler-Namen (handle_freie_antwort aus Task 2) — z.B. socket.emit('freie_antwort', {...}).
3. Der Knopf schreibt in die bestehende Lese-Zone (slot 1, source-Gate wie manual_button — pruefe das
   FE-Source-Gate pip-launcher.js, damit 'continuation' als legitime Quelle in die Lese-Zone darf,
   analog 'manual_button').
4. KEINE finale optische Politur (D-12 -> PIP.2): funktional stabil reicht.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <!-- FE-Deploy; kein Pytest-FE (HART: FE-Test = Live-Browser mit Test-User im Checkpoint). -->
  </verify>
  <acceptance_criteria>
    - grep: `grep -n "freie_antwort" static/js/pip-launcher.js templates/live.html` zeigt Knopf + Emit.
    - Der Knopf ist im DOM dauerhaft (kein show/hide an Call-State gebunden) — sichtbar im Live-Checkpoint.
    - Prod-Deploy GRUEN.
  </acceptance_criteria>
  <done>Dauerhaft sichtbarer 'Freie Antwort'-Knopf bei den Einwand-Knöpfen, emittiert an handle_freie_antwort, schreibt in die Lese-Zone (source='continuation'); keine Über-Politur (PIP.2 deferred).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Der komplette Continuation-Knopf ist live: dauerhaft sichtbarer "Freie Antwort"-Knopf -> liest die
letzten Berater-Aussagen aus per-SID RAM (RV-1) -> starkes Modell (MODEL_CONTINUATION) via gemeinsame
Naht -> JSON-Bailout (continue vs. ask_clarification) -> Gespiegelt-Marker (is_mirrored_echo,
speaker_role='berater', gesenkte confidence) in intent_event. DEDIZIERTER Continuation-TTFT-Circuit-
Breaker (isoliert von EWB) als Notnetz.
  </what-built>
  <how-to-verify>
1. Test-User, Live-Cold-Call auf https://getnerve.app.
2. **Start-Gate (D-11):** VOR erkanntem Gespräch den "Freie Antwort"-Knopf druecken -> es MUSS eine
   ehrliche "noch kein Gespräch erkannt"-Antwort kommen, KEINE geratene Fortsetzung.
3. **Continuation (B):** Call live erkannt. Sprich eine Berater-Frage; spiegle eine (fiktive)
   Kunden-Antwort laut in EINEM Satz; druecke "Freie Antwort" -> NERVE fuehrt sinnvoll aus dem Kontext
   weiter (nutzt das gespiegelte Echo). Deterministisch (jeder Druck antwortet — KEIN smalltalk_none-
   Unterdrueckungs-Bug).
4. **Unsicherheit (RV-4/D-08):** spiegle ein UNKLARES/widerspruechliches Echo -> NERVE gibt eine
   "Frag nach: X oder Y?"-Rückfrage-Empfehlung statt einer selbstbewusst-falschen Weiterführung.
5. **TTFT-Messung (RV-2/D-06, PFLICHT-Akzeptanzkriterium):** nach dem Anruf
   `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 500'`
   | grep -E "CONT-TTFT|EWB-TTFT" — echte TTFT des STARKEN Modells fuer den Continuation-Pfad ablesen
   (der Continuation-Breaker loggt [CONT-TTFT], der EWB-Breaker [EWB-TTFT] — die zwei sind getrennt).
   Landet die Antwort in der natuerlichen Gesprächspause (D-05)? Reisst sie die Pause -> Auslöser =
   Tempo-Arbeit (Phase-2-Caching, Plan 04), NIE Rückfall aufs schwache Modell. Continuation-Breaker
   getriggert? -> kam eine generische Rückfrage statt Hänger (und der EWB-Pfad blieb unbeeinflusst)?
6. **Mirror-Marker (C/D-16):**
   `ssh ... 'bash scripts/inspect.sh sample intent_event 10'` (oder als postgres mit Tenant-GUC, siehe
   RLS-Regel) -> die Continuation-Zeile traegt is_mirrored_echo=true, speaker_role='berater', gesenkte
   confidence. (Test-User-Rows: danach ggf. aufraeumen.)
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn: Start-Gate haelt, Continuation sinnvoll+deterministisch, Unsicherheit->Rückfrage, TTFT gemessen+in der Pause (oder Tempo-Fix-Bedarf notiert), Continuation-Breaker isoliert (EWB unbeeinflusst), Mirror-Marker korrekt. Sonst beschreibe das Problem.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Berater-Sprache (gespiegeltes Echo) -> Continuation-Prompt | untrusted Text geht in den STARKEN-Modell-Prompt (Prompt-Injection-Flaeche) |
| analysiert_bisher RAM -> Continuation-Eingang | Live-Transkript-RAM haelt Real-Text (ANON-LIVE-ANSWER-Entscheidung) |
| Continuation-LLM-Output (JSON) -> FE/Vorlese-Text | Modell-Output ist untrusted (JSON-Parse) |
| Mirror-Marker -> intent_event (Trainings-Fundament) | mis-attribuiertes Echo koennte Training vergiften |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3b-08 | Tampering (Trainings-Vergiftung) | Mirror-Marker speaker_role | mitigate | D-16: speaker_role='berater' erzwungen (Task 2 + b-01-Vertrag/Test) — das gespiegelte Echo ist NIE ein harter Kunden-Fakt. |
| T-TAXO3b-09 | Elevation/Injection | gespiegeltes Echo im Continuation-Prompt | mitigate | Echo geht als user-content (Daten), nicht als System-Instruktion; System-Prompt aus der gemeinsamen Naht (Grounding-Regel + Paradigma). Erzwungenes JSON begrenzt die Output-Form. |
| T-TAXO3b-10 | Information Disclosure (PII) | RAM-Real-Text + Mirror-Payload | mitigate | analysiert_bisher = anonymisierte Live-Version (ANON-LIVE-ANSWER); build_mirror_payload schreibt Roh-Echo NICHT (nur Flags); triggering_text via anonymize_output. DSGVO-Posture in SUMMARY belegen. |
| T-TAXO3b-11 | DoS (Latenz-Hänger) | starkes Modell live | mitigate | DEDIZIERTER Continuation-TTFT-Circuit-Breaker (RV-2, isoliert von EWB) -> sichere generische Rückfrage statt Hänger; TTFT-Messung Pflicht-Akzeptanz. |
| T-TAXO3b-12 | Tampering | ungueltiges LLM-JSON | mitigate | Parse-fail -> sichere generische Rückfrage (fail-safe), nie roher Modell-Müll vorgelesen. |
| T-TAXO3b-13 | Tampering (Breaker-Cross-Kontamination) | geteilter EWB/Continuation-Breaker-Zustand | mitigate | RV-2-Isolation: eigener _continuation_fallback_until + _continuation_ttft_history; Test 5 beweist keine Kopplung (Continuation-Sample kippt nie den LIVE-EWB-Pfad auf Haiku, EWB-Fenster gated Continuation nicht). |
</threat_model>

<verification>
- Prod-Deploy-Gate GRUEN inkl. tests/test_taxo3b_continuation.py + SCHILD-Guard.
- RAM-Quelle (RV-1) im Test bewiesen; kein DB-Read fuer das Echo.
- MODEL_CONTINUATION via gemeinsame Naht answer_system_content (D-07); kein bespoke Pfad.
- Continuation-TTFT-Breaker ISOLIERT (RV-2): eigene Symbole, Test 5 beweist keine _ewb_*-Kopplung.
- Start-Gate via _durable_call_id(state['call_id']) (D-11, konkreter Identifier).
- Live-Test-Checkpoint: Start-Gate haelt, Continuation deterministisch+sinnvoll, RV-4-Rückfrage,
  TTFT gemessen (Pflicht), Breaker-Notnetz + isoliert, Mirror-Marker korrekt (is_mirrored_echo/berater/gesenkt).
</verification>

<success_criteria>
- Continuation-Knopf: RAM-Eingang (RV-1), starkes Modell via gemeinsame Naht (D-05/D-07), JSON-Bailout (RV-4).
- DEDIZIERTER, ISOLIERTER TTFT-Circuit-Breaker (RV-2: eigener _continuation_fallback_until/_continuation_ttft_history, kein Haiku-Rückfall, keine EWB-Kopplung — Test 5 beweist es).
- Gespiegelt-Marker geschrieben (is_mirrored_echo + gesenkte confidence + speaker_role='berater', b-01-Vertrag/D-16).
- Dauerhaft sichtbarer Knopf + Start-Gate-Respekt via _durable_call_id (D-10/D-11), keine Über-Politur (D-12/PIP.2).
- TTFT im Test-Anruf GEMESSEN (Pflicht-Akzeptanz, RV-2/D-06); Latenz-Problem -> Tempo-Arbeit, nie schwaches Modell.
- Rows aufgeraeumt; Schild aktuell.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-b-03-SUMMARY.md`
(inkl. `## 5. Persistenz-Schicht-Verifikation` fuer den Mirror-Emit + gemessene TTFT-Basislinie + DSGVO-Posture
+ Beleg der Breaker-Isolation).
Multi-Segment-Gotcha: Pfad handhardcoden, STATE/ROADMAP handeditieren, plain git commit.
</output>
</output>
