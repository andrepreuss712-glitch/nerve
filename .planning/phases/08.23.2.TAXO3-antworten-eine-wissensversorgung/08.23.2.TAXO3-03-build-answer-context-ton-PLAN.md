---
phase: 08.23.2.TAXO3
plan: 03
type: execute
wave: 3
complexity: 🔴
depends_on: [01, 02]
files_modified:
  - services/prompt_pipeline.py
  - services/claude_service.py
  - services/deepgram_service.py
  - tests/test_build_answer_context.py
autonomous: false
requirements: ["SPEC Req 1", "SPEC Req 2", "SPEC Req 3", "SPEC Req 4 (Loader)", "SPEC Req 5", "SPEC Req 6", "SPEC Req 7 (Grounding-Regel)"]
risiko: riskant
test_anruf: true
user_setup: []

must_haves:
  truths:
    - "EINE build_answer_context() liefert den Antwort-System-Prompt als LISTE von System-Bloecken (stabil/volatil strukturell getrennt, cache-faehig angelegt — aber Caching noch NICHT aktiviert, das macht Plan 04/W2) fuer ALLE 3 Pfade (Auto, manueller Knopf, QA)"
    - "Der kontext-arme _ewb_autovar_system-String (claude_service.py:560) ist GELOESCHT; der Auto-Pfad (streame_auto_variante) zieht den System-Prompt aus build_answer_context (Auto-Muell tot, Req 1)"
    - "KEIN Few-Shot-Anker mehr: die 'Hinterlegte Einwand-Gegenargumente (als Orientierung)'-Injektion (auto user_msg :547) UND die standard_ga-Injektion (manual user_msg :678) sind raus (Req 2)"
    - "Paradigma + Rollen-Register + Intent→Technik kommen aus dem method_pack (pack_assignments → method_packs → stiller NERVE-Default, Req 4 Loader); Rolle/Modus/Konfidenz sind PARAMETER (kein if mode==-Prompt-Zweig, Req 3/6); genau EIN DETERMINISTISCHER primary_intent geht ans LLM (Req 5, M-5): keyword-Trigger → der gematchte `match['matched_label']` (vom Caller durchgereicht); analyse_loop-Trigger → `_session_state[sid]['state']['primary_intent']` (IL-2); KEINE „erste-aus-einwaende-Liste"-Heuristik mehr"
    - "Grounding-Regel im Prompt (Antwort nur aus gegebenem Kontext; fehlt der Fakt → sagen + Follow-up, Req 7); product_facts leer angedockt"
    - "Auto-Trigger-Verhalten bleibt erhalten (nur der kontext-arme Prompt ist ersetzt); build_answer_context MUST NOT raise (fail-open Minimal-Block, Live-Loop-Garantie)"
    - "Die SUMMARY.md enthaelt eine explizit benannte Sektion `## Vertrag fuer Plan 05 — interaction_id-Quelle` (formaler Cross-Wave-Handoff): verbatim IL-2-Re-Grep-Ergebnis + aufgeloeste per-SID-Lage der interaction_id (z.B. _session_state[sid]['interaction_id'] unter _session_state_lock nach I-4) + Granularitaet (ereignis-getriebenes Einwand-Fenster, nicht roh line_id) + Bestaetigung dass TAXO1-04 Blocker I-4 gefixt+deployed ist — Plan 05 liest GENAU diese Sektion"
    - "Test-Anruf bestaetigt: Auto + Knopf + QA tragen alle Profil-Persona + Voice; Gatekeeper-Antwort respektvoll; keine Technik-Vokabel; TTFT-Basislinie gemessen"
  artifacts:
    - path: "services/prompt_pipeline.py"
      provides: "build_answer_context(*, user_id, sid, primary_intent, mode, confidence, is_auto_triggered, role) -> list[dict] (Wrapper um build_profile_context, method_pack-Loader, Block-Split stabil/volatil strukturell); load_method_pack() (pack_assignments→method_packs→Default, fail-open)"
      contains: "def build_answer_context"
    - path: "services/claude_service.py"
      provides: "Auto-Pfad + Manual-Pfad ziehen System-Prompt aus build_answer_context; _ewb_autovar_system geloescht; Few-Shot-Injektionen raus"
      contains: "build_answer_context"
    - path: "tests/test_build_answer_context.py"
      provides: "Pure-logic: Block-Liste-Form, fail-open (kein raise bei fehlendem Pack), EIN-Intent, unbekannter Intent-Key → Default-Technik-Block (M-5 Keyspace b), Rolle/Modus als Parameter, Grounding-Regel im Block, KEIN Few-Shot-String"
  key_links:
    - from: "streame_auto_variante (Auto) + streame_manual_ewb_variante (Knopf) + _qa_pipeline_dispatch (QA)"
      to: "build_answer_context() (EINE Quelle)"
      via: "System-Prompt-Bezug statt Eigen-Assembly"
      pattern: "build_answer_context"
    - from: "build_answer_context"
      to: "public.method_packs / pack_assignments (Plan 01 Tabellen, Plan 02 Default)"
      via: "load_method_pack(user_id, workspace_id) fail-open NERVE-Default"
      pattern: "load_method_pack"
    - from: "build_answer_context"
      to: "build_profile_context (Bestand, prompt_pipeline.py:111)"
      via: "Wrapper-Aufruf (Block-Split stabil/volatil)"
      pattern: "build_profile_context"
    - from: "primary_intent / mode / confidence (TAXO1 intent_event)"
      to: "build_answer_context-Parameter (EIN Intent, kein Code-Zweig)"
      via: "Parameter-Uebergabe"
      pattern: "primary_intent"
    - from: "IL-2-Re-Grep interaction_id-Quelle"
      to: "08.23.2.TAXO3-03-SUMMARY.md `## Vertrag fuer Plan 05 — interaction_id-Quelle` (Plan 05 liest sie)"
      via: "formaler Cross-Wave-Vertrag in der SUMMARY"
      pattern: "Vertrag fuer Plan 05"
---

> 🛑 **DEPENDS-ON-TAXO1 + VOR-EXECUTE-RE-GREP (IL-2, RESEARCH Valid until 2026-06-19):** build_answer_context konsumiert `primary_intent` + `confidence` + `mode` aus dem TAXO1 intent_event-Vertrag. VOR Execute: `grep -n "primary_intent\|intent_type\|_session_modes\|CLASSIFIER_CONFIDENCE_THRESHOLD\|interaction_id" services/live_session.py services/deepgram_service.py services/claude_service.py` — bestätigen, WO der per-SID primary_intent/mode/confidence nach TAXO1-Execute liegt (module-global vs `_session_state[sid]`). build_answer_context an die dann-gültige Quelle hängen. **Dieser Re-Grep liefert auch die interaction_id-Quelle, die Plan 05/W3 braucht — sie wird als FORMALER `## Vertrag fuer Plan 05 — interaction_id-Quelle` in die SUMMARY geschrieben (siehe Task 2 + `<output>`), Plan 05 liest GENAU diese Sektion.**

> 🤝 **VERTRAG FÜR PLAN 05 (Reviewer-Vorgabe, formaler Cross-Wave-Handoff):** Der IL-2-Re-Grep ermittelt die verifizierte interaction_id-Quelle. Diese darf NICHT nur beiläufig in der Verifikationsliste auftauchen — sie wird als klar überschriebene Sektion `## Vertrag fuer Plan 05 — interaction_id-Quelle` in 08.23.2.TAXO3-03-SUMMARY.md festgehalten, mit: (a) verbatim IL-2-Re-Grep-Ergebnis, (b) aufgelöste per-SID-Lage der interaction_id-Quelle (z.B. `_session_state[sid]['interaction_id']` unter `_session_state_lock` nach I-4-Fix), (c) Granularität (ereignis-getriebenes Einwand-Fenster, NICHT roh line_id), (d) Bestätigung dass TAXO1-04-Blocker I-4 gefixt+deployed ist. Plan 05 Task 0 liest GENAU diese Sektion als Pre-Build-Gate-Input.

> 🔎 **VOR-EXECUTE-RE-GREP (Auto/Manual-Anker, RESEARCH Stand 2026-06-12 verifiziert):** `grep -n "_ewb_autovar_system\|def streame_auto_variante\|def streame_manual_ewb_variante\|build_profile_context\|standard_ga\|Hinterlegte Einwand-Gegenargumente" services/claude_service.py` — Zeilen abgleichen (Stand: :515/:560/:657/:705/:547/:678). TAXO1-06 (v2-modular) hat ggf. denselben Bereich angefasst — **TAXO3 LÖST v2-modular AB (IL-4), baut NICHT darauf auf.**

> RISIKO-BANNER: **riskant.** Lenkt ALLE 3 Live-Antwort-Pfade auf EINE Funktion um, LÖSCHT den Auto-Müll-String + Few-Shot-Anker, dreht das Paradigma. → **Test-Anruf PFLICHT (D-05: Auto + Knopf + QA, Daten-Fluss-Beweis Profil-Stimme; Gatekeeper respektvoll; keine Technik-Vokabel) + TTFT-Basislinie messen.**

> ⚠️ **CROSS-AI-GATE (🔴):** Geplant, nicht sofort ausgeführt. Deploy + Test-Anruf erst nach Cross-AI-Clearance + 3-Wege-Interlock TAXO1/2/3.

> ⚠️ **ANTI-ABRIEB (D-06):** Dieser Plan (Ton) legt den Block-Split stabil/volatil STRUKTURELL an (build_answer_context gibt eine LISTE zurück, volatile Daten in einem eigenen Block), aktiviert das Caching aber NICHT (kein cache_control-Layering, kein Pre-Warming, kein Circuit-Breaker-Umbau). Plan 04 (Tempo) aktiviert/misst NUR — build_answer_context wird in Plan 04 NICHT grob neu gebaut. **Latenz-Budget W1:** Slot A <1s lokal (unverändert); KI-Antwort „in die Pause" (Sonnet ungecacht ~0,8-1,5s erwartbar — hier nur Ton, TTFT-Basislinie messen, NICHT festzurren).

<objective>
Baue die EINE `build_answer_context()` als dünnen Wrapper um das bestehende `build_profile_context()` (Anti-Abrieb D-06): sie holt den Profil-Kontext, lädt das method_pack (Paradigma + Rollen + Intent→Technik aus Plan 01/02), reicht EINEN primary_intent + mode/confidence/role als Parameter durch, hängt die Grounding-Regel an — und gibt eine **Liste von System-Blöcken** zurück (stabil/volatil strukturell getrennt, cache-fähig angelegt). Dann: Auto-Pfad + Manual-Pfad auf den Wrapper heben, den `_ewb_autovar_system`-Müll-String + die Few-Shot-Anker löschen. Das ist der größte Hebel gegen den Müll (Req 1+2+3+4+5+6+7).

Purpose: Kein Antwort-Pfad mehr ohne Profil-Stimme; Paradigma-Reset (verstehen statt bekämpfen); Technik unsichtbar; EIN Intent; Modus/Konfidenz als Parameter. Plan 04 (Tempo) aktiviert dann das Caching auf der hier angelegten Block-Struktur.
Output: build_answer_context + load_method_pack live; Auto/Manual/QA ziehen daraus; Müll-String + Few-Shot tot; Grounding-Regel drin; Test-Anruf-Beleg + TTFT-Basislinie; `## Vertrag fuer Plan 05` in der SUMMARY.

KEINE Caching-Aktivierung (Plan 04). KEIN Circuit-Breaker-Umbau (Plan 04). KEIN Slot-B-Dedup (Plan 05). Slot A unverändert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen. STATE/ROADMAP hand-editieren.

**Verify = Production only (CLAUDE.md HART):** `bash deploy.sh production` (server-seitiges Pytest-Gate) + **Test-Anruf im Browser als Test-User auf getnerve.app** (D-05 Pflicht) + `inspect.sh logs` für TTFT/Daten-Fluss-Beleg. KEIN lokales pytest als Acceptance, KEIN lokaler Server.

**UTF-8-Regel:** Prompt-Text-Inhalt (Grounding-Regel, Rollen-Anweisung) ist USER-FACING-Wirkung → echte Umlaute. Code-Identifier/dict-keys ASCII.

**Latenz (Punkt 25):** Slot A bleibt lokal <1s (Bau-Regel 1, NICHT anfassen). build_answer_context darf den Hot-Path nicht mit DB-Calls bremsen — load_method_pack per-SID/per-User CACHEN (wie build_profile_context _profile_cache), 0 DB im Hot-Loop. TTFT-Basislinie messen ([EWB-TTFT] Bestand), NICHT festzurren (Plan 04).
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md

<interfaces>
<!-- Verifizierte Andock-Punkte (grep file:line, RESEARCH + diese Session bestaetigt). KEINE Codebase-Exploration noetig. -->

build_profile_context (BESTAND, prompt_pipeline.py:111 — wrappen, NICHT neu bauen):
  def build_profile_context(user_id: int, mode='cold_call', sid=None, profile_id=None) -> str
  - Sektion 1-7 + FAQ + Tabu = STABIL; return '\n'.join(lines) :462
  - Sektion 8 PreCall-Briefing (:433 get_briefing_for_sid) + Sektion 9 Lead/Anrede (:446 session_anrede) = VOLATIL
  - Anrede-Caveat: Anrede-Konsistenz-Anweisung :402 (STABIL bleibt) vs per-SID _sid_anrede :451 (VOLATIL)
  → Block-Split: build_profile_context um Parameter `return_blocks=True` erweitern ODER Schwester-Funktion,
    die (stabiler_text, volatiler_text) getrennt liefert. Sektion 1-7+FAQ+Tabu = stabil; 8+9 = volatil.

Auto-Pfad (claude_service.py:515 streame_auto_variante):
  - Signatur :515 `streame_auto_variante(neuer_text, einwaende, kontext, sid, slot=1, trigger="analyse_loop")` → NEUEN Keyword-Param `primary_intent: str | None = None` anhängen (M-5)
  - :533 baut heute aus der ganzen einwaende-Liste → die Liste bleibt profile-Kontext, ist aber KEINE Intent-Quelle mehr (Req 5/M-5)
  - :547 user_msg injiziert "Hinterlegte Einwand-Gegenargumente (als Orientierung)" → RAUS (Req 2 Few-Shot)
  - :560 _ewb_autovar_system = "Du bist ein erfahrener Sales-Coach..." → LOESCHEN (Req 1)
  - :578 trigger="keyword"/"analyse_loop" → BEIDE automatisch → `is_auto_triggered = True` KONSTANT (NICHT aus trigger ableiten; Gemini-BLOCKER-Fix). Nur Manual-Pfad = False. (Req 6/9; Circuit-Breaker = Plan 04)
  - **primary_intent-Auflösung in streame_auto_variante (M-5, deterministisch):**
      if trigger == "keyword":   _pi = primary_intent          # vom deepgram-Caller (match['matched_label'])
      else:  # analyse_loop      with ls.state_lock: _pi = ls.state... bzw. _session_state[sid]['state'].get('primary_intent')  # IL-2
      # _pi None → generischer Default-Intent (fail-open), NIE „erste aus einwaende"
  - Keyword-Caller: services/deepgram_service.py:243-246 reicht `primary_intent=match['matched_label']` durch (match in Scope :219)

primary_intent per-SID-Quelle (analyse_loop, IL-2-Vertrag von TAXO1-04, VOR-EXECUTE-RE-GREP bestätigen):
  - TAXO1-04 schreibt `_session_state[sid]['state']['primary_intent']` (=intent_type) + `['confidence']` unter `state_lock` VOR dem Antwort-Trigger (_qa_pipeline_dispatch, claude_service.py:907); init_session_state seedet beide =None.
  - Re-Grep: `grep -n "'primary_intent'\|'confidence'" services/live_session.py services/claude_service.py` → bestätigt Write-vor-Trigger + Seed. Liegt NICHT → BLOCK (TAXO1-04 nicht executet, Wave-Reihenfolge verletzt).

Manual-Pfad (claude_service.py:657 streame_manual_ewb_variante):
  - :705 from services.prompt_pipeline import build_profile_context as _bpc_manual; _ewb_manual_system = _bpc_manual(...)
    → auf build_answer_context umstellen (gleiche EINE Quelle)
  - :678 user_msg injiziert standard_ga (profile_einwand.gegenargument) → RAUS (Req 2 Few-Shot)

Anthropic SDK system-Block-Liste (Context7 /anthropics/anthropic-sdk-python, RESEARCH §Standard Stack — HIER nur STRUKTUR anlegen, cache_control kommt Plan 04):
  system = [
    {"type": "text", "text": SALES_PARADIGMA_GERUEST},   # stabil (method_pack paradigm_rules+roles+taboos)
    {"type": "text", "text": SELLER_PROFILE_STABLE},     # stabil (build_profile_context Sektion 1-7+FAQ+Tabu)
    {"type": "text", "text": PRODUCT_FAQ_GROUNDING},     # stabil (Grounding-Regel + product_facts leer)
    {"type": "text", "text": VOLATILE_BRIEFING_LEAD},    # volatil (Sektion 8+9 — Plan 04: KEIN cache_control)
  ]
  (In W1: Liste-Form + Reihenfolge. cache_control-Keys NICHT setzen — das ist Plan 04.)

method_pack-Loader-Muster (Vorbild resolve_prompt_version prompt_pipeline.py:30):
  def load_method_pack(user_id, workspace_id=None) -> dict:
    # ENV-Override → per-(user) Cache → pack_assignments(valid_until > now OR NULL) → method_packs
    # → abgelaufen/keine → stiller NERVE-Default (owner='NERVE', is_active). MUST NOT raise (fail-open {}).
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `public.method_packs` (LESEN, nerve_app) — Plan 01 + Default Plan 02
- `public.pack_assignments` (LESEN, nerve_app) — Plan 01
- `public.product_facts` (LESEN, leer) — Plan 01
- `profiles.daten` (LESEN, via build_profile_context Bestand — KEINE Aenderung)

### inspect.sh-Beleg
```
$ SET ROLE nerve_app; SELECT count(*) FROM public.method_packs;   →  1  (Plan 02 Default; public ist natives App-Recht — KEIN Grant noetig, OQ-1 b)
$ inspect.sh schema method_packs   →  (Plan-01: id|name|owner|schema_version|content jsonb|is_active|valid_until|created_at)
$ inspect.sh schema pack_assignments  →  (Plan-01: id|pack_id FK|user_id|workspace_id|valid_until|created_at)
```
**Beim Execute:** echte-nerve_app-Rolle-Lese-Proof neu (public nativ lesbar, KEIN Grant — Plan 01 OQ-1 b; training bleibt dicht) + `inspect.sh count method_packs` == 1 (Default da).

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `method_pack['paradigm_rules'/'roles'/'taboos'/'intents']` | gelesen build_answer_context (nerve_app, gecacht) | DB public.method_packs.content JSONB | ⬜ Task 1 Loader-Test |
| `pack_assignments.valid_until` | Fallback-Selektion load_method_pack | DB Timestamp nullable | ⬜ Task 1 fail-open-Test |
| `primary_intent` (analyse_loop) | gelesen aus _session_state[sid]['state'] (IL-2, von TAXO1-04 vor Trigger geschrieben) | RAM `_session_state[sid]['state']['primary_intent']` unter state_lock | ⬜ VOR-Execute-Re-Grep (IL-2): Write-vor-Trigger + Seed |
| `primary_intent` (keyword) | match['matched_label'] vom deepgram-Caller durchgereicht (M-5, deterministisch) | RAM (Funktions-Parameter, kein geteilter State) | ⬜ Task 1b: grep deepgram-Call-Site reicht es durch |
| `mode` / `confidence` | Parameter aus TAXO1 (_session_modes per-SID) | RAM per-SID | ⬜ VOR-Execute-Re-Grep |
| `interaction_id` (fuer Plan 05) | IL-2-Re-Grep ermittelt die Quelle | RAM `_session_state[sid]['interaction_id']` (per-SID nach I-4) | ⬜ Task 2 → `## Vertrag fuer Plan 05` |
| Profil-Persona (Sektion 1-9) | build_profile_context (Bestand, _profile_cache) | DB profiles.daten (gecacht RAM) | ✓ Bestand prompt_pipeline.py:111 |
| product_facts (Grounding, leer) | gelesen build_answer_context | DB public.product_facts (leer) | ⬜ Task 2 |

## Verbindungs-Karte (Punkt 22)
- **build_answer_context** existiert NICHT (grep==0, RESEARCH + Session bestätigt) → Neu-Funktion in prompt_pipeline.py (neben build_profile_context).
- **build_profile_context** = EINE Quelle (1 Def :111, kein Duplikat) → wrappen, nicht ersetzen (Anti-Abrieb). Block-Split additiv (return_blocks ODER Schwester-Fn).
- **_ewb_autovar_system** lebt (:560, grep bestätigt) → wird GELÖSCHT (Req 1). Auto-Pfad zieht künftig aus build_answer_context.
- **standard_ga / "Hinterlegte Einwand-Gegenargumente"** leben (:678/:547) → Few-Shot-Anker, RAUS (Req 2).
- **method_pack-Quelle (Req 4):** kein hardcoded Mapping — load_method_pack über pack_assignments→method_packs→Default. grep nach Execute: kein hardcoded Intent→Technik-Mapping in Python.
- **gatekeeper.py ist NICHT der Rollen-Träger** (No-op-Stub, Pitfall 4) — Rollen-Register lebt im method_pack-JSONB + build_answer_context-Lookup (`pack['roles'][role]`).
- **FAQ-Embedding ist KEIN RAG** (Pitfall 5): qa_pipeline.py _get_embedding_model = FAQ-Fuzzy-Match (match_faq, threshold 0.75), bleibt. Req-7-„kein RAG" = kein KB-Retrieval.
- **interaction_id-Quelle (für Plan 05):** der IL-2-Re-Grep ermittelt die per-SID-Lage (`_session_state[sid]['interaction_id']` nach I-4) → wird als `## Vertrag fuer Plan 05 — interaction_id-Quelle` in die SUMMARY geschrieben (Task 2).
- **primary_intent-Quelle (M-5, TAXO3-Fund, deterministisch — ZWEI Pfade, KEINE Liste):** keyword-Trigger → `match['matched_label']` (der Matcher löst GENAU EINEN profile-einwand auf, einwand_keyword_matcher.py:166; vom deepgram-Caller :245 als `primary_intent=` durchgereicht). analyse_loop-Trigger → `_session_state[sid]['state']['primary_intent']` (IL-2, von TAXO1-04 vor Trigger geschrieben). KEIN „erste/höchste aus der einwaende-Liste" (widerspräche REQ-5 = genau EIN Intent).
- **KEYSPACE-ENTSCHEIDUNG (M-5, Gemini-Fund verifiziert — Option b, bewusst):** `method_pack['intents']` ist nach §1-`<intent_type>` gekeyt (TAXO3-02:90). Der keyword-`matched_label` ist eine **Profil-Kategorie** (Preis/Zeit/…), KEIN §1-intent_type → `pack['intents'].get(matched_label, default)` zieht beim Keyword-Pfad IMMER den Default-Technik-Block. **Das ist gewollt:**
    - **(a) Profil-Kategorie→intent_type mappen ist VERWORFEN:** die Abbildung ist NICHT 1:1 — „zu teuer" kann `echter_einwand` ODER `vorwand` sein. Ein hartcodiertes Mapping müsste raten → würde genau die Nicht-Determinismus/Falsch-Klassifikation wieder einführen, die M-5 + REQ-5 eliminieren, und die Aufgabe des LLM-Klassifikators (Medium-Lane) schlecht duplizieren.
    - **(b) GEWÄHLT — keyword-Fast-Lane nutzt bewusst den Default-Technik-Block:** Die Fast-Lane feuert auf dem **Interim-Transcript VOR der Klassifikation** (Speed-Pfad). Paradigma-Reset, Rollen-Register, Profil-Stimme, Grounding gelten weiterhin voll (kommen aus `pack['paradigm_rules']`/`['roles'][role]`/`['taboos']`/Profil — NICHT aus `['intents']`); NUR der intent-SPEZIFISCHE Technik-Hebel fällt auf den generischen „verstehen+diagnostizieren"-Default. Der `matched_label` geht trotzdem ans LLM als Sorge-Kontext (REQ-5: genau EIN Intent benannt). Die **intent-präzise** Technik ist dem klassifizierten §1-intent_type-Pfad (analyse_loop) vorbehalten, der `pack['intents'][intent_type]` trifft.
    - **Naht zu REQ-8/Plan 05:** WELCHER Pfad pro Aussage Slot B besetzt (deterministische Single-Source-pro-Fenster) ist Plan 05 — diese Entscheidung legt nur fest, dass der Keyword-Pfad den generischen Technik-Block korrekt-by-design bekommt, NICHT crasht/falsch-mappt.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_answer_context + load_method_pack in prompt_pipeline.py (Wrapper, Block-Split strukturell, fail-open)</name>
  <files>services/prompt_pipeline.py, tests/test_build_answer_context.py</files>
  <read_first>
    - services/prompt_pipeline.py:111-462 (build_profile_context — Sektion 1-9, return '\n'.join :462, Anrede-Caveat :402/:451) + :30-69 (resolve_prompt_version Loader-Muster)
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md §Architecture Patterns 1-3 (Wrapper, Block-Split, Rollen-Register als Daten) + §Standard Stack (SDK-Block-Liste)
    - VOR-EXECUTE-RE-GREP-Ergebnis (IL-2): wo liegt primary_intent/mode/confidence per-SID nach TAXO1
    - inspect.sh schema method_packs + pack_assignments (Plan-01-Schema)
  </read_first>
  <behavior>
    - build_answer_context(*, user_id, sid, primary_intent, mode='cold_call', confidence=1.0, is_auto_triggered=False, role='interessent') -> list[dict]
    - Rueckgabe ist eine LISTE von {"type":"text","text":...}-Bloecken (KEINE cache_control-Keys in W1) in fester Reihenfolge: [paradigma_stabil, profil_stabil, grounding_stabil, volatil_briefing_lead]
    - fail-open: fehlt das method_pack / wirft der Loader → Minimal-Block (NERVE-Default ODER leerer Paradigma-Block), KEIN raise (Live-Loop-Garantie, wie build_profile_context)
    - genau EIN primary_intent fliesst in den Prompt (kein Top-2-Array)
    - mode/confidence/role als Parameter — KEIN if mode==-Zweig der einen eigenen Prompt baut; Lookup pack['roles'][role] + pack['modes'/'intents']
    - load_method_pack(user_id, workspace_id=None) -> dict: pack_assignments(valid_until>now OR NULL)→method_packs→Default; fail-open {}; per-user gecacht (0 DB im Hot-Loop)
    - Grounding-Regel-Text im grounding_stabil-Block (Antwort nur aus Kontext; fehlt Fakt → sagen + Follow-up)
  </behavior>
  <action>
    1. **Block-Split (Anti-Abrieb):** Erweitere build_profile_context um `return_blocks: bool = False` ODER baue eine schmale Schwester-Funktion `build_profile_blocks(user_id, mode, sid, profile_id) -> tuple[str, str]`, die (stabiler_text [Sektion 1-7+FAQ+Tabu], volatiler_text [Sektion 8 Briefing + Sektion 9 Lead/Anrede]) GETRENNT liefert. Die Anrede-Konsistenz-Anweisung (:402) bleibt im STABILEN Teil; der per-SID-Anrede-WERT (:451) im VOLATILEN. KEINE Logik-Änderung an den Sektionen selbst — nur die join-Grenze verschieben (additiv, alter String-Pfad bleibt für HTTP-Aufrufer per Default).
    2. **load_method_pack(user_id, workspace_id=None) -> dict:** Muster resolve_prompt_version (:30): ENV-Override (NERVE_DEFAULT_METHOD_PACK?) → per-user-Cache → `SELECT content FROM public.pack_assignments JOIN method_packs ... WHERE (valid_until IS NULL OR valid_until > now()) AND is_active` → bei keiner/abgelaufen: `SELECT content FROM method_packs WHERE owner='NERVE' AND is_active LIMIT 1` (stiller Default). `try/except → return {}` (fail-open, MUST NOT raise). Cache wie _profile_cache (per-SID/per-user, 0 DB im Hot-Loop).
    3. **build_answer_context(...) -> list[dict]:** ruft build_profile_blocks (stabil/volatil) + load_method_pack. Baut die 4-Block-Liste:
       - Block 1 (paradigma_stabil): aus pack['paradigm_rules'] + pack['taboos'] + pack['roles'][role] (ziel+hebel) + pack['intents'][primary_intent] (Req 3/4/5). KEINE Beispielsätze. Konfidenz dämpft die Sicherheit als Anweisung (niedrige confidence → „formuliere vorsichtiger/fragender", Req 6) — als Text, kein Code-Zweig.
       - Block 2 (profil_stabil): der stabile build_profile_blocks-Teil (Sektion 1-7+FAQ+Tabu).
       - Block 3 (grounding_stabil): Grounding-Regel (Req 7: „Antworte nur aus dem gegebenen Kontext. Fehlt ein Produkt-Fakt, sage das ehrlich und schlage ein Follow-up vor — erfinde nichts.") + product_facts-Andock (leer in W0, intent-getaggt geladen wenn vorhanden).
       - Block 4 (volatil): der volatile build_profile_blocks-Teil (Sektion 8 Briefing + Sektion 9 Lead/Anrede) + der aktuelle primary_intent + Echo/Transkript-Platzhalter (vom Aufrufer gefüllt).
       Reihenfolge fest (stabil vorne, volatil hinten). KEINE cache_control-Keys (Plan 04). fail-open: Loader-Fehler → Block 1 = Minimal-Paradigma, kein raise.
    Test `tests/test_build_answer_context.py` (pure-logic, Function-Call-Return):
    - test_returns_block_list: Rückgabe ist list, jeder Eintrag {"type":"text","text":str}, len==4, KEIN cache_control-Key (W1).
    - test_fail_open_no_pack: load_method_pack monkeypatched → {} → build_answer_context wirft NICHT, liefert Minimal-Block.
    - test_single_intent: nur primary_intent im Paradigma-Block, kein Top-2-Array.
    - test_unknown_intent_uses_default_technique (M-5 Keyspace, Option b): primary_intent=<Profil-Kategorie nicht in pack['intents']> → Block 1 nutzt den generischen Default-Technik-Hebel (kein KeyError, kein leerer Block) UND enthält weiterhin Paradigma + Rolle + Grounding (die kommen NICHT aus pack['intents']); ein bekannter §1-intent_type → der intent-spezifische Hebel erscheint. Beweist: keyword-Pfad bricht nicht + bekommt bewusst Default, analyse_loop-Pfad trifft präzise.
    - test_role_mode_are_parameters: verschiedene role/mode → unterschiedlicher Block-1-Text via pack-Lookup, KEIN if mode==-Zweig (Struktur-Beleg: gleiche Funktion, Parameter wählen Lookup).
    - test_grounding_rule_present: Block 3 enthält die Grounding-Regel.
    - test_no_few_shot: kein „Hinterlegte Einwand-Gegenargumente"/standard_ga-Muster in irgendeinem Block.
    - test_volatile_last: Briefing/Lead nur im letzten Block (Plan-04-Cache-Prefix-Schutz strukturell schon richtig).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_build_answer_context.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def build_answer_context" services/prompt_pipeline.py` == 1; `grep -c "def load_method_pack" services/prompt_pipeline.py` == 1
    - build_answer_context-Signatur trägt primary_intent, mode, confidence, is_auto_triggered, role (grep der def-Zeile)
    - test_returns_block_list + test_fail_open_no_pack + test_single_intent + test_unknown_intent_uses_default_technique + test_role_mode_are_parameters + test_grounding_rule_present + test_no_few_shot + test_volatile_last grün
    - KEIN cache_control-Key in build_answer_context (`grep "cache_control" services/prompt_pipeline.py` deckt build_answer_context NICHT ab — Plan 04)
    - Block-Reihenfolge stabil-vorne/volatil-hinten (Briefing/Lead im letzten Block — test_volatile_last)
  </acceptance_criteria>
  <done>build_answer_context + load_method_pack live (Wrapper, Block-Split strukturell, fail-open, EIN Intent, Rolle/Modus Parameter, Grounding-Regel), noch nicht in die 3 Pfade verdrahtet.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-Pfad + Manual-Pfad auf build_answer_context heben; _ewb_autovar_system + Few-Shot-Anker LOESCHEN (Punkt 14 Control-Flow) + `## Vertrag fuer Plan 05` schreiben</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py:515-604 (streame_auto_variante: :533 einwaende-Liste, :547 Few-Shot-Injektion, :560 _ewb_autovar_system, :562 system-Block, :578 trigger, :592-604 TTFT-Log)
    - services/claude_service.py:657-720 (streame_manual_ewb_variante: :678 standard_ga, :705 build_profile_context-Import, :716 cache-Block)
    - services/claude_service.py:1240-1334 (_qa_pipeline_dispatch / _emit_qa_slot1 — QA-Pfad; NUR System-Prompt-Bezug umstellen, Slot-B-Dedup = Plan 05)
    - services/deepgram_service.py:218-246 (Keyword-Fast-Lane: `match = matcher.match_with_dedup(...)`, `match['matched_label']`/`match['profile_einwand']`, der `streame_auto_variante(..., "keyword")`-Aufruf :243-246) — **die deterministische keyword-primary_intent-Quelle (M-5)**. Zeilen können nach TAXO1-03 (editiert dieselbe Datei, per-SID kw_fired) verschoben sein → vor Edit re-greppen: `grep -n "streame_auto_variante\|matched_label" services/deepgram_service.py`.
    - services/einwand_keyword_matcher.py:148-171 (match_with_dedup-Rückgabe-Form `{'profile_einwand', 'matched_label'}`, Feld-Prioritaet kurzlabel>kategorie>typ) — belegt, dass matched_label deterministisch EIN Wert ist
    - tests/test_build_answer_context.py (aus Task 1)
    - IL-2-Re-Grep-Ergebnis (primary_intent per-SID-Quelle für analyse_loop + interaction_id-Quelle für `## Vertrag fuer Plan 05`)
  </read_first>
  <action>
    **Punkt-14-Control-Flow-Audit (eingebettet):** Diese Änderung ersetzt 3 Eigen-Assemblies durch EINEN Aufruf. Race-Fragen: build_answer_context ist read-only (kein State-Write), load_method_pack-Cache thread-safe (per-user dict + Lock wie _profile_cache). Auto/Manual/QA laufen in verschiedenen Threads → jeder ruft build_answer_context unabhängig, kein geteilter Mutable-State. Auto-Trigger-Verhalten (Selbst-Feuern) UNVERÄNDERT — nur der Prompt-Bezug wechselt.

    1. **Auto-Pfad (streame_auto_variante) — DETERMINISTISCHE primary_intent-Quelle (M-5, REQ-5: genau EIN deterministischer Intent, KEINE „erste-aus-Liste"-Heuristik):**
       - **`is_auto_triggered = True` KONSTANT (Gemini-BLOCKER-Fix):** streame_auto_variante ist der AUTOMATISCHE Pfad — BEIDE Trigger (`keyword` UND `analyse_loop`) sind selbst-gefeuert, KEIN manueller Button. `is_auto_triggered` darf NICHT aus dem trigger abgeleitet werden (das alte `(trigger != "keyword")` war invertiert → der Keyword-Pfad würde wie der manuelle Knopf behandelt → der TTFT-Circuit-Breaker [Plan 04] griffe NIE für automatische Keyword-Treffer → feuerte immer Sonnet, auch im Latenz-Spike → sabotiert die Latenz-Schranke, Punkt 25). Nur `streame_manual_ewb_variante` (Button) setzt `is_auto_triggered=False`. (Req 6/9; Circuit-Breaker-NUTZUNG = Plan 04, hier nur das Flag durchreichen.)
       - **primary_intent wird NICHT mehr aus der `einwaende`-Liste geraten (:533).** streame_auto_variante bekommt einen NEUEN Parameter `primary_intent: str | None = None` und löst ihn pro Trigger-Pfad EINDEUTIG auf:
         - **`trigger == "keyword"` (Fast-Lane):** Der gematchte Keyword-Intent IST die Quelle (deterministisch — der Matcher hat GENAU EINEN Treffer aufgelöst). Der Aufrufer in `services/deepgram_service.py:245` reicht `match['matched_label']` (die aufgelöste profile-einwand-Kategorie, Feld-Prioritaet kurzlabel>kategorie>typ, `einwand_keyword_matcher.py:151/166`) als `primary_intent` durch. **NICHT** `_session_state[sid]['state']['primary_intent']` lesen — die Medium-Lane (die das per IL-2 setzt) ist bei einem Keyword-Match evtl. noch gar nicht durchgelaufen (Fast-Lane feuert vor classify), der RAM-Wert wäre stale/None.
         - **`trigger == "analyse_loop"` (Medium-Lane):** primary_intent kommt aus `_session_state[sid]['state']['primary_intent']` (IL-2-Live-Übergabe, von TAXO1-04 VOR dem Trigger geschrieben — unter `state_lock` lesen). Ist er `None` (Race: analyse_loop feuert bevor die Medium-Lane primary_intent setzte) → **fail-open auf einen neutralen Default-Intent** (z.B. `'unklar'`/`None`→generisches Register im method_pack), **NICHT** „erste aus der Liste".
       - Genau EINE dieser beiden Quellen liefert pro Aufruf den EINEN primary_intent → an `build_answer_context(primary_intent=...)`. Die `einwaende`-Liste bleibt nur noch als profile-Kontext-Material relevant, ist aber KEINE Intent-Quelle mehr (REQ-5).
       - **Keyspace (bewusste Entscheidung — Option b, NICHT mappen):** `method_pack['intents']` ist nach §1-`<intent_type>` gekeyt. keyword-`matched_label` = Profil-Kategorie → `pack['intents'].get(matched_label, <generischer Default>)` zieht beim Keyword-Pfad **bewusst** den Default-Technik-Block (Paradigma/Rolle/Stimme/Grounding gelten weiter; nur der intent-spezifische Hebel ist generisch). Mapping Profil-Kategorie→intent_type ist VERWORFEN (nicht 1:1, „zu teuer"=echter_einwand|vorwand → Raten → Nicht-Determinismus zurück). analyse_loop-`primary_intent` IST ein §1-intent_type → trifft `pack['intents'][intent_type]` präzise. Begründung in der Verbindungs-Karte; im SUMMARY als bewusste Entscheidung festhalten.
       - `_ewb_autovar_system = "..."` (:560) **LÖSCHEN.** `_system_autovar = build_answer_context(user_id=_user_id, sid=sid, primary_intent=..., mode=..., confidence=..., is_auto_triggered=True, role=...)` (Liste; True konstant — Auto-Pfad). Der `if config.CACHE_EWB ...`-Block (:561-564) bleibt als Hülle, gibt die Liste durch — cache_control-Layering kommt Plan 04 (W1: Liste ohne cache_control ODER der bestehende EINE cache_control-Block bleibt unverändert über Block 1, KEINE Schichtung).
       - Few-Shot-Injektion „Hinterlegte Einwand-Gegenargumente (als Orientierung)" (:547) im user_msg **ERSATZLOS LÖSCHEN** (Req 2). user_msg trägt nur noch Echo/Transkript + den EINEN Intent (keine Vorlage).
       - [EWB-TTFT]-Log (:604) BLEIBT (Basislinie messen).
    1b. **Keyword-Call-Site (services/deepgram_service.py, M-5 deterministische Quelle):** Im Keyword-Fast-Lane-Pfad den `streame_auto_variante(text, einwaende, _kontext, sid, 1, "keyword")`-Aufruf (:243-246) um den gematchten Intent erweitern → `streame_auto_variante(text, einwaende, _kontext, sid, 1, "keyword", primary_intent=match['matched_label'])` (per Keyword-Arg, damit die Reihenfolge stabil bleibt). `match` ist hier in Scope (:219). KEINE weitere Logik im on_message-Handler — nur der EINE Wert durchgereicht. (analyse_loop-Caller in claude_service.py reicht KEIN primary_intent → streame_auto_variante liest dort `_session_state[sid]['state']['primary_intent']`, siehe Schritt 1.)
    2. **Manual-Pfad (streame_manual_ewb_variante):**
       - :705 `build_profile_context`-Import/Aufruf → `build_answer_context(...)` (gleiche EINE Quelle; role/mode/confidence aus Kontext; is_auto_triggered=False — Knopf probiert immer Sonnet, aber Circuit-Breaker = Plan 04).
       - standard_ga-Injektion (:678) im user_msg **ERSATZLOS LÖSCHEN** (Req 2).
       - cache-Block (:716) gibt die Liste durch (Schichtung Plan 04).
    3. **QA-Pfad (_qa_pipeline_dispatch):** System-Prompt-Bezug auf build_answer_context umstellen (EINE Quelle, Req 1). Slot-B-Emit/Dedup NICHT anfassen (Plan 05).
    Alle 3 Pfade ziehen jetzt aus build_answer_context. Auto-Müll-String + beide Few-Shot-Anker tot.

    4. **`## Vertrag fuer Plan 05 — interaction_id-Quelle` (formaler Cross-Wave-Handoff, Reviewer-Vorgabe):** Schreibe in die SUMMARY.md eine klar überschriebene Sektion mit GENAU diesem Titel, die enthält:
       - (a) das **verbatim IL-2-Re-Grep-Ergebnis** (die grep-n-Ausgabe zu `interaction_id`/`_session_state_lock`/etc.).
       - (b) die **aufgelöste per-SID-Lage** der interaction_id-Quelle (z.B. `_session_state[sid]['interaction_id']` unter `_session_state_lock` nach dem I-4-Fix) — NICHT module-global.
       - (c) die **Granularität**: ereignis-getriebenes Einwand-Fenster (TAXO1-04-B3, André), NICHT roh line_id.
       - (d) **Bestätigung**, dass TAXO1-04-Blocker I-4 gefixt UND deployed ist (sonst hier als „noch offen" markieren — Plan 05 Task 0 blockt dann).
       Plan 05 Task 0 liest GENAU diese Sektion als Pre-Build-Gate-Input.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_ewb_autovar_system" services/claude_service.py` == 0 (Müll-String GELÖSCHT, Req 1)
    - `grep -c "Hinterlegte Einwand-Gegenargumente" services/claude_service.py` == 0 (Auto-Few-Shot raus, Req 2)
    - `grep -n "standard_ga" services/claude_service.py` zeigt KEINE user_msg-Injektion mehr im Manual-Pfad (Req 2)
    - `grep -c "build_answer_context" services/claude_service.py` >= 3 (Auto + Manual + QA ziehen daraus, Req 1)
    - `grep "build_profile_context" services/claude_service.py` zeigt KEINEN direkten Aufruf mehr im Manual-Pfad (über build_answer_context gewrappt)
    - `grep -c "\[EWB-TTFT\]" services/claude_service.py` >= 1 (Basislinie-Log bleibt)
    - KEIN if mode==-Prompt-Zweig neu eingefügt (Req 6, manuelle Sicht)
    - **M-5 (deterministische primary_intent-Quelle):** `grep -n "primary_intent" services/claude_service.py services/deepgram_service.py` zeigt: streame_auto_variante-Signatur trägt `primary_intent`-Param; keyword-Pfad reicht `match['matched_label']` durch (deepgram_service.py); analyse_loop-Pfad liest `_session_state[sid]['state']['primary_intent']`. KEINE „einwaende[0]"/„erste-aus-Liste"-Intent-Ableitung (manuelle Sicht + grep: kein `einwaende[0]` als Intent-Quelle)
    - **is_auto_triggered (Gemini-BLOCKER-Fix):** `grep -n "is_auto_triggered" services/claude_service.py` zeigt in streame_auto_variante `is_auto_triggered = True` (konstant) bzw. den build_answer_context-Aufruf mit `is_auto_triggered=True` — KEIN `(trigger != "keyword")` o.ä. trigger-Ableitung; nur streame_manual_ewb_variante setzt False (manuelle Sicht + grep: kein `trigger != "keyword"`-Muster für is_auto_triggered)
    - **SUMMARY.md enthält die Sektion `## Vertrag fuer Plan 05 — interaction_id-Quelle` mit allen vier Teilen (a verbatim Re-Grep / b per-SID-Lage / c Granularität Einwand-Fenster / d I-4-gefixt+deployed-Bestätigung)**
  </acceptance_criteria>
  <done>3 Pfade auf EINE build_answer_context gehoben, Müll-String + Few-Shot-Anker tot, Auto-Trigger erhalten, TTFT-Log bleibt, `## Vertrag fuer Plan 05` in der SUMMARY (formaler Handoff).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Test-Anruf W1 (Ton) — Daten-Fluss + Paradigma + TTFT-Basislinie (D-05 Pflicht)</name>
  <what-built>
    build_answer_context als EINE Quelle für Auto + Knopf + QA; Müll-String + Few-Shot tot; Paradigma-Reset aus method_pack; Grounding-Regel; EIN Intent; Rolle/Modus als Parameter. Deployed auf Production (bash deploy.sh production grün, Pytest-Gate server-seitig).
  </what-built>
  <how-to-verify>
    1. getnerve.app als Test-User, Live-Call starten (cold_call).
    2. **Auto-Pfad:** Einwand sprechen → Auto-Variante (Slot B) feuert weiter (Auto-Trigger erhalten) UND trägt Profil-Persona + Voice (nicht mehr der generische „Sales-Coach"-Ton).
    3. **Knopf-Pfad:** EWB-Button → Antwort trägt ebenfalls Profil-Stimme.
    4. **QA-Pfad:** QA-Antwort trägt Profil-Stimme.
    5. **Paradigma:** Antworten enthalten KEINE Technik-Vokabel („Reframe"/„ANKER"/„Einwand überwinden") und keinen manipulativen Klang. Gatekeeper-Szenario → respektvoll/ehrlich, kein Überreden.
    6. **TTFT-Basislinie:** Claudian zieht `inspect.sh logs` → `[EWB-TTFT] <ms>`-Zeilen notieren (Basislinie, ungecacht — NICHT festzurren, Plan 04 misst gecacht).
    7. Anrede-Konsistenz prüfen (Sie/Du korrekt trotz Block-Split).
  </how-to-verify>
  <resume-signal>Tippe "bestätigt" wenn alle 3 Pfade Profil-Stimme tragen + kein manipulativer Klang + Gatekeeper respektvoll + TTFT notiert. Sonst: konkrete Abweichung beschreiben (Claude fixt + re-deployt).</resume-signal>
  <acceptance_criteria>
    - Live-Beleg: Auto + Knopf + QA tragen alle Profil-Persona + Voice (kein generischer Pfad mehr)
    - Live-Beleg: keine Technik-Vokabel, kein manipulativer Klang; Gatekeeper respektvoll/ehrlich
    - Auto-Trigger feuert weiter (Log-Beleg trigger=keyword/analyse_loop)
    - TTFT-Basislinie aus [EWB-TTFT] dokumentiert (SUMMARY) — eigene Messung, nicht interpoliert
    - Anrede-Konsistenz intakt (Block-Split hat sie nicht zerrissen)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| method_pack content (JSONB) → build_answer_context → vorgelesene Antwort | Prompt-Injection-Fläche (Pack-Inhalt steuert Ton) |
| Profil/Pack-Inhalt → Live-LLM-Antwort | Halluzination/falsches Versprechen-Fläche |
| TAXO1 intent_event (primary_intent/mode/confidence/interaction_id) → build_answer_context-Parameter + Plan-05-Vertrag | Vertrags-Naht (per-SID-Quelle, IL-2) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3-03-01 | T (Tampering) | Prompt-Injection via method_pack/Profil-Inhalt in vorgelesene Antwort | mitigate | Grounding-Regel (Block 3) + bestehender apply_tabu_filter (claude_service.py:1349) bleibt aktiv; pack-Inhalt ist NERVE-freigegeben (Plan 02). |
| T-TAXO3-03-02 | T (Tampering / Halluzination) | falsches Produkt-Versprechen (erfundener Fakt) | mitigate | D-02(a) Tabu + Grounding-Regel: fehlt der Fakt → sagen + Follow-up, nicht erfinden. product_facts leer → kein erfundener Fakt. |
| T-TAXO3-03-03 | D (DoS) | build_answer_context wirft im Live-Loop → jede Antwort crasht/faellt auf Default | mitigate | fail-open (MUST NOT raise, Minimal-Block); test_fail_open_no_pack; load_method_pack try/except → {}. |
| T-TAXO3-03-04 | I (Info Disclosure / DSGVO) | Anonymisierung des Antwort-Outputs bricht beim Pfad-Umbau | mitigate | anonymize_output (claude_service.py:613) UNVERÄNDERT — nur System-Prompt-Bezug wechselt, Output-Pipeline bleibt. Test-Anruf prüft. |
| T-TAXO3-03-05 | T | Konfidenz wird ignoriert → übersichere Antwort bei niedriger Konfidenz | mitigate | confidence als Parameter dämpft den Block-1-Text (vorsichtiger formulieren); Live-Test prüft. |
| T-TAXO3-03-06 | T (Vertrags-Drift) | Plan 05 klammert an falsche interaction_id-Quelle (module-global statt per-SID) weil der Handoff nur beiläufig dokumentiert ist | mitigate | Formaler `## Vertrag fuer Plan 05 — interaction_id-Quelle` in der SUMMARY (Task 2) mit verbatim Re-Grep + per-SID-Lage + Granularität + I-4-Status; Plan 05 Task 0 liest GENAU diese Sektion als Gate-Input. |
</threat_model>

<verification>
- `grep -c "_ewb_autovar_system" services/claude_service.py` == 0; `grep -c "Hinterlegte Einwand-Gegenargumente" ...` == 0; standard_ga-Injektion raus (Req 1/2).
- `grep -c "build_answer_context" services/claude_service.py` >= 3 (3 Pfade, Req 1).
- **M-5: deterministische primary_intent-Quelle — keyword→`match['matched_label']` (deepgram-Caller), analyse_loop→`_session_state[sid]['state']['primary_intent']` (IL-2); KEINE „erste-aus-Liste"-Heuristik (grep: kein `einwaende[0]` als Intent; streame_auto_variante-Signatur trägt `primary_intent`). Keyspace: keyword-`matched_label`∉`pack['intents']` → bewusst Default-Technik-Block (siehe Verbindungs-Karte), kein Mapping.**
- **is_auto_triggered = True KONSTANT in streame_auto_variante (Gemini-BLOCKER-Fix: beide Auto-Trigger; nicht aus trigger ableiten); nur Manual-Pfad False (grep: kein `trigger != "keyword"` für is_auto_triggered). Sonst greift der TTFT-Breaker [Plan 04] nie für Keyword-Treffer.**
- pytest test_build_answer_context.py grün (Block-Liste, fail-open, EIN deterministischer Intent, Rolle/Modus Parameter, Grounding, kein Few-Shot, volatil hinten).
- Test-Anruf (D-05): Auto+Knopf+QA tragen Profil-Stimme; keine Technik-Vokabel; Gatekeeper respektvoll; Auto-Trigger feuert; TTFT-Basislinie notiert.
- KEIN cache_control-Layering in build_answer_context (Plan 04); Anti-Abrieb belegt.
- **`## Vertrag fuer Plan 05 — interaction_id-Quelle` in der SUMMARY vorhanden + vollständig (verbatim IL-2-Re-Grep + per-SID-Lage + Einwand-Fenster-Granularität + I-4-gefixt+deployed). Plan 05 liest GENAU diese Sektion.**
</verification>

<success_criteria>
- EINE build_answer_context für alle 3 Pfade; Auto-Müll + Few-Shot tot; Paradigma-Reset aus method_pack; Grounding-Regel; EIN Intent; Modus/Konfidenz/Rolle als Parameter (kein Code-Zweig); fail-open.
- Test-Anruf bestätigt Daten-Fluss + Paradigma + Gatekeeper-Respekt; TTFT-Basislinie gemessen.
- Block-Split strukturell angelegt (cache-fähig), Caching NICHT aktiviert (Plan 04) — Anti-Abrieb D-06 gewahrt.
- Formaler `## Vertrag fuer Plan 05 — interaction_id-Quelle` in der SUMMARY (Cross-Wave-Handoff verriegelt).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-03-SUMMARY.md` (inkl. grep-Belege Müll-String/Few-Shot==0 + build_answer_context>=3, pytest-Beleg, Test-Anruf-Protokoll [3 Pfade Profil-Stimme + Gatekeeper + keine Technik-Vokabel], TTFT-Basislinie). **Pflicht-Sektion mit genau dem Titel `## Vertrag fuer Plan 05 — interaction_id-Quelle`** mit (a) verbatim IL-2-Re-Grep-Ergebnis, (b) aufgelöster per-SID-Lage der interaction_id-Quelle (`_session_state[sid]['interaction_id']` unter `_session_state_lock` nach I-4), (c) Granularität (ereignis-getriebenes Einwand-Fenster, nicht roh line_id), (d) Bestätigung TAXO1-04-Blocker I-4 gefixt+deployed. Plan 05 Task 0 liest GENAU diese Sektion.
</output>
</content>
