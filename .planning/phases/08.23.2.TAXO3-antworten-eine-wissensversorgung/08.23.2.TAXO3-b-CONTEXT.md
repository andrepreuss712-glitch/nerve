# Phase 08.23.2.TAXO3 — Scheibe (b): Gesprächs-Gedächtnis + Continuation — Context

**Gathered:** 2026-07-02
**Status:** Ready for planning
**Slice:** (b) — eigene Scheibe innerhalb TAXO3, NACH P1-01/P1-02 (Paradigma-Reset, LIVE).
**Komplexität:** 🔴 (berührt Live-Antwort-Pfad → Cross-AI Pflicht vor Execute).

> **Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT
> `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/
> gsd-code-review/gsd-verifier lösen die ID `08.23.2.TAXO3` falsch auf → Pfade hand-hardcoden,
> STATE/ROADMAP hand-editieren, plain git committen.

<domain>
## Phase Boundary

Diese Scheibe liefert zwei zusammenhängende Verhaltensweisen (eine Wurzel: jede Antwort
entsteht heute isoliert):

1. **(A) Live-Kurzzeit-Gedächtnis** — NERVE sieht im laufenden Cold-Call seine eigenen letzten
   Antworten + eine „schon-benutzte-Öffner"-Liste und variiert den Einstieg (löst die
   „Verstehe"-Wiederholung an der Wurzel = Kontext-Problem, KEIN Sampling-Problem).
2. **(B) Continuation via „Freie Antwort"-Knopf** — der Berater spiegelt die Kunden-Antwort kurz
   laut → drückt den Knopf → NERVE führt aus dem Gesprächs-Kontext weiter (schließt die Frage-
   Schleife, die der Cold-Call sonst nicht schließen kann, weil NERVE den Kunden nicht hört).
3. **(C) Gespiegelt-Marker + gesenkte Konfidenz** — die vom Berater gespiegelte Kunden-Aussage
   wird intern als „gespiegelt/unsicher" geführt, NIE als harter Fakt (Schweigen ≠ Bestätigung).
4. **Ereignis-Form jetzt konkret + versioniert festzurren** (der eine nicht-nachholbare Neuzugang
   ist der Gespiegelt-Marker) + **Verlustfrei-Check** für das bestehende Cold-Call-PreCall-Briefing.

**Cold-Call-first.** Meeting-Modus + kontenübergreifendes Account-Wissen sind NICHT Teil dieser
Scheibe (siehe Deferred).
</domain>

<decisions>
## Implementation Decisions

### ⭐ Review-Verschärfungen (2026-07-02, Gemini-Cross-AI + André — LOCKED, überschreiben wo sie schärfer sind)

- **RV-1 (aus HIGH-1, schärft D-02/D-03) — AN DEN ANFANG DES PLANS:** Der Continuation-Knopf liest das
  gespiegelte Kunden-Echo + die letzten Berater-Aussagen aus dem **laufenden per-SID-Arbeitsspeicher**
  (`_session_state[sid]`, RAM), **NICHT aus der DB** (`transcript_segments` werden erst am Call-Ende
  gebündelt geschrieben → DB-Read greift ins Leere). Gleiche Klasse wie HANDLING-TIMING/Audio-Race —
  **Async-Bereitschafts-Naht (Punkt 22/26)**. Persistenz-Schicht-Verifikation belegt die RAM-Quelle.
- **RV-2 (aus HIGH-2, schärft D-05/D-06) — starkes Modell BLEIBT, mit hartem Gate:** (a) **Latenz-
  Messung im Test-Anruf = Pflicht-Akzeptanzkriterium** (echte TTFT messen, nicht annehmen). (b) Reißt
  die Messung die Gesprächspause → Auslöser = **Tempo-Arbeit** (Phase-2-Caching / kleinerer Input /
  Streaming), **NIE Rückfall aufs schwache Modell**. (c) **Notnetz Pflicht: TTFT-Circuit-Breaker** —
  bricht das Modell zu langsam an, kommt eine sichere generische Rückfrage statt Hänger (deckt sich
  mit Ask-back-bei-Unsicherheit RV-4).
- **RV-3 (aus MEDIUM, schärft D-13/D-14) — Marker OHNE Migration:** der Gespiegelt-Marker ist ein
  **strukturierter, versionierter Eintrag in `payload_jsonb`** (konsistent mit dem dort schon
  liegenden `speaker_role`) — **KEINE dedizierte Spalte, keine Migration**. „First-class" = durch
  typisierten, dokumentierten Vertrag + `schema_version` garantiert erkennbar/queryable, NICHT „in
  Prosa vergraben".
- **RV-4 (aus Vorschlag 5, schärft D-08/D-09) — semantische Unsicherheit:** ob das gespiegelte Echo
  klar genug ist, entscheidet das **Continuation-LLM selbst** (expliziter Bailout) — NICHT primär eine
  rohe STT-Konfidenz. **AUSGABE-Mechanik durch RV-7 aktualisiert: Text-Präfix-Kontrakt
  (`ASK_CLARIFICATION: `) statt erzwungenem JSON** — die ENTSCHEIDUNGS-Anweisung selbst („Im Zweifel
  ask_clarification, NICHT raten") und der Test des Entscheids BLEIBEN unverändert. Die gesenkte
  Marker-Konfidenz (G1) bleibt der zweite, tunbare Hebel.
- **RV-5 (Divergenz — André gegen Gemini): „verbrannte Öffner"-Liste BEHALTEN, schlank** (~5 letzte
  Öffner als Negativliste, aus den letzten Antworten abgeleitet, keine Pflege-Mechanik). Gegen
  Geminis „streichen": ohne Liste kippt NERVE in ein NEUES Floskel-Muster (In-Context-Selbst-
  verstärkung) — begründeter Recherche-Befund (Bau-Vorgabe §5), kein Over-Engineering.

### ⭐⭐ Runde-2 Review-Verschärfungen (2026-07-02, Gemini R2 + André — LOCKED)

Runde-2-Cross-AI (Gemini R2, gegen die post-RV-1..RV-5-Pläne) fand zwei HIGH + zwei MEDIUM + ein LOW
+ zwei Reviewer-Notes. Diese verschärfen/patchen die bestehenden Pläne (b-02, b-03); b-01 blieb
ohne R2-Fund. Wo sie schärfer sind, überschreiben sie.

- **RV-6 (aus HIGH-1, schärft RV-1/D-02 in b-03) — Audio/Click-Race, Async-Bereitschafts-Naht (Punkt 26):**
  RV-1 hat bewiesen, dass der Continuation-Knopf RAM (`analysiert_bisher`) statt DB liest — aber NICHT,
  dass RAM zum Klick-Zeitpunkt BEFÜLLT ist. Der Berater spricht das Echo und drückt SOFORT den Knopf;
  der Socket.IO-Klick reist quasi instant, Deepgram-STT braucht ~300–500ms bis das Echo an
  `_session_state[sid]['analysiert_bisher']` (RAM) angehängt ist. Der Leser (Knopf) kann dem Schreiber
  (Deepgram-Append) VORAUSLAUFEN → das starke Modell führt aus dem VORHERIGEN Turn weiter.
  **Fix (b-03):** eine **Bereitschafts-Naht** statt blindem fixem Sleep (Punkt 26/27). Bevorzugt:
  warte bis seit dem letzten STT-Append eine kurze Ruhe verstrichen ist ODER bis ein NEUER Append seit
  dem Knopf-Intent kam, mit **Timeout-Fallback (fail-open → generische Rückfrage)**. Ein knapper
  Debounce (~500ms) in `pip-launcher.js` vor dem `freie_antwort`-Emit ist als pragmatischer erster
  tragfähiger Weg (Punkt 27) akzeptabel, WENN die Race benannt ist und der Timeout-Fallback greift.
  Pflicht: neue Persistenz-Schicht-Verifikation-Zeile (Schreib-Zeitpunkt = STT-Append ~300–500ms nach
  Sprechen; „kann Leser vorauslaufen? JA") + Behavior-Test (verspäteter Append → Handler wartet/re-liest
  → sieht Echo; Timeout → fail-open).

- **RV-7 (aus HIGH-2, MECHANIK-SWAP in b-03) — Streaming statt erzwungenem JSON, Präfix-Kontrakt:**
  Erzwungenes JSON (`{"action":...}`) UND Token-Streaming für TTFT (Punkt 25) widersprechen sich —
  rohes JSON zu streamen sendet literale Klammern an UI/TTS → kaputtes Rendering. **Nur die AUSGABE-
  Mechanik tauscht:** statt JSON ein **Text-Präfix-Kontrakt** — das Modell beginnt bei Unsicherheit
  EXAKT mit `ASK_CLARIFICATION: ` gefolgt von der Rückfrage; sonst streamt es die Weiterführung normal.
  Backend buffert die ersten Tokens; erkennt es das Präfix → bricht ab + emittiert die generische/
  „Frag nach"-Rückfrage; sonst streamt es normal (TTFT bleibt, kein JSON-Parse-Akrobatik). Ungültig/
  leer → fail-safe generische Rückfrage. **HART — was BLEIBT (André-Direktive):** die verbatim RV-4-
  Entscheidungs-ANWEISUNG („Im Zweifel ask_clarification, NICHT raten"), die Gespiegelt/unsicher-
  Markierung im Prompt, der TEST des Entscheids (b-03 Test 3b — ambiguer Echo MUSS ask_clarification,
  klarer Echo MUSS continue), UND die Runtime-Assembly-Assertion. Nur die Mechanik (JSON → Präfix) und
  die Handling-Assertion (Präfix-Erkennung statt `action`-Parse) ändern sich, NICHT die Anweisung.

- **RV-8 (aus MEDIUM, schärft RV-2-Breaker in b-03) — Python-global-Scope-Falle:**
  `_continuation_fallback_until` in `streame_continuation_antwort` NEU zuweisen OHNE
  `global _continuation_fallback_until` erzeugt eine lokale Var → der Breaker trippt global NIE.
  **Fix (b-03 Task 1):** `global _continuation_fallback_until` bei jeder Neuzuweisung erzwingen
  (die deque `_continuation_ttft_history` braucht für `.append` KEIN global — dokumentieren). Behavior-
  Test: nach simuliertem Trip liest ein zweiter Call das global gesetzte Fenster (Breaker greift wirklich global).

- **RV-9 (aus MEDIUM, schärft D-01/RV-5 in b-02) — Interpunktion in verbrannten Öffnern:**
  `derive_burned_openers` extrahiert das erste Wort OHNE Interpunktion zu strippen → „Verstehe," und
  „Verstehe." gelten als verschiedene Öffner → Dedup versagt, Tic bleibt. **Fix (b-02 Task 1):**
  Interpunktion strippen (regex `[^\w\s]`) + lowercasing VOR Vergleich/Dedup. Test-Case:
  `["Verstehe, das…","Verstehe. Sie…","verstehe!"]` → EIN Öffner nach Normalisierung.

- **RV-Note-Low (aus LOW, in b-03) — Empty-Context-Guard:** Knopf am Call-Start gedrückt (Call live,
  aber noch keine Sprache) → `analysiert_bisher` leer → starkes Modell könnte einen Opener
  halluzinieren. **Fix:** ist der Kontext NACH der Bereitschafts-Naht (RV-6) leer/zu kurz → SOFORT die
  generische „Frag nach"-Rückfrage OHNE LLM-Call. Eigener Guard, NICHT das Start-Gate D-11 (call_id) —
  der Call kann live sein, aber noch keine Sprache. Behavior-Test dafür.

- **RV-Note-A (Reviewer-Note, in b-03) — statischen Isolations-grep demoten:** die Breaker-Isolations-
  Akzeptanz nutzte einen statischen grep („KEIN `_ewb_fallback_until` in der Funktion") als BEWEIS.
  Per Test-Qualitäts-Regel = Source-Presence-Check. **Fix:** grep zu ADVISORY/Lint demoten; **Test 5
  (Behavior — EWB-State vor/nach unberührt) ist DAS Gate.**

- **RV-Note-B (Reviewer-Note, in b-03) — PII-Posture-Zeile:** explizite Zeile in b-03s Persistenz-
  Schicht-Verifikation, die verifiziert, dass ANON-LIVE-ANSWER für den Live-RAM-Buffer
  `analysiert_bisher` (der das starke Modell speist) TATSÄCHLICH hält (grep/inspect-Beleg oder klarer
  Verweis auf die ANON-LIVE-ANSWER-Naht), DSGVO-Säule.

### (A) Live-Kurzzeit-Gedächtnis — Anti-Wiederholung

- **D-01 (🟢 André; präzisiert durch RV-5):** Das allgemeine Antwort-Gedächtnis zeigt NERVE seine
  **eigenen letzten ~3 Antworten** + eine **schlanke „verbrannte Öffner"-Liste** (~5 letzte Öffner
  als Negativliste, aus den letzten Antworten abgeleitet, keine Pflege-Mechanik). Liste BEHALTEN
  (gegen Geminis Streichen — verhindert Kippen in ein NEUES Floskel-Muster). Klein starten ist
  Absicht, kein Kompromiss (Bau-Vorgabe §7).
- **D-02 (🟢 André, KERN-ARCHITEKTUR):** **ZWEI GETRENNTE Bedürfnisse, NICHT in EIN Fenster
  werfen.**
  - **Bedürfnis 1 — Alltags-Anti-Wiederholung** („Verstehe"-Syndrom): braucht NUR NERVEs eigene
    Vorantworten → das kleine ~3-Antworten-Fenster im allgemeinen Antwort-Kontext (VOLATIL-Teil).
    Berater-Aussagen kommen NICHT ins allgemeine Fenster (Latenz; Tempo-Sparen ist noch aus).
  - **Bedürfnis 2 — Continuation-Knopf**: braucht die letzte(n) **Berater-Aussage(n)**, speziell das
    laut gespiegelte Kunden-Echo, als **EIGENEN Eingang beim Knopfdruck** — aus dem **per-SID
    Live-Arbeitsspeicher** (`_session_state[sid]`, RAM), **NICHT aus der DB** (siehe RV-1: Batch-Write
    am Call-Ende → DB-Read greift ins Leere) und NICHT indem das allgemeine Fenster vergrößert wird.
- **D-03 (Prüf-Auftrag an Plan-Author):** Der unmittelbare Auslöser-Wortlaut (`triggering_text`)
  ist ohnehin schon im Antwort-Kontext → prüfen, ob der für Bedürfnis 1 bereits reicht (dann noch
  weniger neuer Kontext nötig).
- **D-04 (🟢 André, Latenz-Disziplin, Punkt 25):** TTFT gegen Fenster-Größe **messen** (A/B),
  Fenster NUR vergrößern wenn die Kohärenz **messbar** leidet. Anti-Floskel = Kontext, nicht am
  Zufalls-Regler drehen (`repetition_penalty` NIE > 1,2; siehe Bau-Vorgabe §5). Falle: ein Modell,
  das seine Vorantworten sieht, kann in ein NEUES Muster kippen → deshalb die „verbrannte Öffner"-
  Liste als Negativliste, nicht nur „sei anders".

### (B) „Freie Antwort"-Knopf (Continuation)

- **D-05 (🟢 André):** Der Knopf nutzt bewusst das **STARKE Modell** (Continuation = härteste
  Inferenz: aus holprigem, einseitigem Echo die wahrscheinliche Kunden-Aussage erschließen). Der
  bewusste Knopfdruck landet in der natürlichen Gesprächspause → „schnell genug" = „landet in der
  Pause", nicht „unter 300ms".
- **D-06 (🟢 André, Modell-Wahl-Disziplin):** (1) Im Test-Anruf wird die **TTFT/Latenz des STARKEN
  Modells** gemessen — NICHT „ist das schwache gut genug". Bei Latenz-Problem: das starke Modell
  schneller machen (kleinerer Continuation-Input, Streaming, Phase-2-Caching) — NIE aufs schwache
  Modell zurückfallen. (2) Das gilt NUR für diesen Continuation-Knopf; kontextarme Lookups/
  Klassifikation (Standard-Einwand-Erkennung, Auto/Keyword) bleiben auf dem schnellen Modell.
  **(3) Pflicht-Gate (RV-2):** echte TTFT-Messung im Test-Anruf ist Akzeptanzkriterium + ein
  TTFT-Circuit-Breaker als Notnetz (zu langsamer Anlauf → sichere generische Rückfrage statt Hänger).
- **D-07 (🟢 André, Tauschbarkeit — Punkt 12 Override-Margin):**
  - **JETZT bauen:** der Knopf holt sein Modell aus der **Konfiguration** (eigener `MODEL_*`-Schalter
    wie die anderen Antwort-Pfade, z.B. `MODEL_CONTINUATION`) — NICHT hardcoden; umschaltbar ohne
    Redeploy.
  - **JETZT bauen (Naht-Disziplin):** der Knopf-Modell-Aufruf läuft durch **DIESELBE EINE
    Modell-Aufruf-Naht** wie die anderen Antwort-Pfade (KEIN neuer bespoke Modell-Call-Pfad) →
    damit die spätere Anbieter-Schicht diese eine Naht umschließt und der Anbieter-Wechsel für den
    Knopf ohne Rebuild mitkommt.
  - **NICHT jetzt bauen (Über-Bau vermeiden):** die volle Anbieter-Abstraktion
    (Anthropic ↔ Gemini ↔ GPT ↔ eigene KI) = separate Roadmap-Phase „LLM-Anbieter-Schicht +
    EU-Residenz" (pre-launch Pflicht, siehe DSGVO-Analyse Ist-Stand).

### (C) Gespiegelt-Marker + Unsicherheits-Verhalten

- **D-08 (🟢 André):** Bei unklarem/widersprüchlichem gespiegeltem Echo gibt NERVE **KEINE geratene
  Weiterführung**, sondern eine **Rückfrage-Empfehlung** an den Berater („Frag nach: X oder Y?").
  Grund = Risiko-Asymmetrie: unnötige Rückfrage ist billig; eine selbstbewusst-falsche
  Weiterführung, die der Berater live vorliest, ist teuer. (Option „geratene Weiterführung als
  Default" ist ausgeschlossen — verletzt Soll-Verhalten §2 C.)
- **D-09 (🟢 André, baubar-erweiterbar):** Die Ask-back-Entscheidung setzt auf dem **Konfidenz-Wert**
  auf, der für gespiegelte Aussagen ohnehin gesenkt wird (G1). So ist das spätere Hochstufen auf
  „gestaffelt nach Sicherheit" nur ein **Schwellenwert-Dreh, kein Rebuild**. Schwelle jetzt
  konservativ (im Zweifel Rückfrage), später mit Messung tunen.

### (D) Knopf-Platzierung + Gate

- **D-10 (🟢 André):** Der „Freie Antwort"-Knopf ist **dauerhaft sichtbar**, stabil bei den
  bestehenden Einwand-Knöpfen — KEIN zustandsabhängiges Ein-/Ausblenden (vermeidet die Bug-B-Klasse
  „erscheint/verschwindet im Live-Fenster"; passt zur stabilen-Anzeige-Entscheidung PIP.1 + zum
  manuellen Cold-Call-Modell).
- **D-11 (🟢 André, Gate-Respekt):** Der Knopf feuert **NICHT vor erkanntem Gespräch** — er
  respektiert das bestehende **Gesprächs-Start-Gate** (Soll-Verhalten §2): vor erkanntem Call →
  ehrliche „noch kein Gespräch erkannt"-Antwort statt geratener Fortsetzung. Sobald der Call live
  erkannt ist: Knopf gedrückt → **deterministische Antwort** (KEIN Confidence-Gate pro Antwort —
  der alte `smalltalk_none`-Unterdrückungs-Bug darf nicht zurückkommen).
- **D-12 (Deferred-Verweis):** Die finale optische Platzierung/Politur gehört zu **PIP.2**
  (UI-Restruktur, eigene spätere Scheibe). Für JETZT: funktional stabil bei den vorhandenen Knöpfen,
  nicht überdesignen.

### Ereignis-Form (Pflicht jetzt festzurren — §4/§5, überwiegend ⚙️ Code-Innerei)

- **D-13 (LOCKED, Bau-Vorgabe §4 + Soll-Verhalten §5 + G1; präzisiert durch RV-3):** Der
  **Gespiegelt-Marker** ist der eine nicht-nachträglich-rekonstruierbare Neuzugang. Ablage
  **ENTSCHIEDEN (RV-3):** ein **strukturierter, versionierter Eintrag in `payload_jsonb`**
  (z.B. `is_mirrored_echo: true`, konsistent mit dem dort schon liegenden `speaker_role`) +
  **gesenkte `confidence`** — **KEINE dedizierte Spalte, keine Migration**. „First-class" =
  durch typisierten, dokumentierten Vertrag + `schema_version` garantiert erkennbar/queryable
  (nicht „in Prosa vergraben"). Harte Anforderung bleibt: **trennbar + versioniert + queryable**.
- **D-14 (LOCKED):** **`schema_version` + disziplinierte `payload_jsonb`-Struktur** — neue Felder im
  Reserve-Feld nur mit Versionsnummer + Doku (Gemini-Warnung: `payload_jsonb` = Segen bei Disziplin,
  Fluch ohne).
- **D-15 (LOCKED, Verlustfrei-Check):** Verifizieren, dass sich das bestehende Cold-Call-PreCall-
  Briefing später als `source=manual`-Ereignis in dieselbe Form überführen lässt (sonst doch
  Abrieb) — Form-Check jetzt, Bau von B1 später.
- **D-16 (LOCKED, Speaker-Zuordnung §2):** Cold-Call = Single-Speaker → Gesprochenes ist IMMER der
  Berater; die Berater-Paraphrase des Kunden-Einwands bleibt eine **Berater**-Aussage. Der
  Gespiegelt-Marker (D-13) ist die saubere Trennung „vom Berater gespiegeltes Kunden-Echo" vs.
  „echt gehörte Kunden-Aussage" (letztere gibt es im Cold-Call nicht) — verhindert Trainings-
  Vergiftung.

### Claude's Discretion (⚙️ Code-Innerei — die 3 Sichten + Audits regeln das)
- Exakte Ablage-Mechanik des kleinen ~3-Antworten-Ringpuffers (per-SID RAM in `_session_state[sid]`).
- Ableitung der „verbrannte Öffner"-Liste aus den letzten Antworten (z.B. erste Wörter/Einstieg).
- Genaue Ablage-Form von Gespiegelt-Marker + `schema_version` (D-13/D-14) nach Persistenz-
  Schicht-Verifikation.
- Wie der Continuation-Knopf die letzte(n) Berater-Aussage(n) aus dem Live-Transkript zieht.
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Bau-Vorgabe + Soll-Verhalten (Pflicht-Pre-Read, außerhalb des Repos im Vault)
- `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\NERVE Gedächtnis + Continuation — Entscheidung + Bau-Vorgabe.md` — **DIE kanonische Bau-Vorgabe für Scheibe (b).** §2 (Entscheidung A/B/C), §4 (Form konkret), §5 (Anti-Formelhaftigkeit = Kontext), §6 (Mirror-Trick + Guardrails), §7 (Latenz + Modell), §9 (die 3 größten Fehler).
- `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\04 Entscheidungen\NERVE Konstrukt - Soll-Verhalten.md` §2 (Cold-Call, insb. der ★-Punkt „Gesprächs-Gedächtnis + Continuation A/B/C" + „knüpft an statt zu wiederholen" + „wiederkehrender Einwand → tiefer" + „Gesprächs-Start-Gate" + „Sprecher-Zuordnung") + §5 (Gesprächs-Verständnis / Ereignis-Form).

### Belege (Recherche-Hintergrund)
- `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\Nerve_Architektur-Analyse_Zwei_Gedächtnis-Arten.md`
- `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\architektur für kontenübergreifendes account wissen.md` (nur Form-Tür, NICHT jetzt bauen)
- `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\04 Entscheidungen\NERVE TAXO-Gerüst (verriegelt).md` — §3 (intent_event-Vertrag), §4.5 (EINE build_answer_context, Grounding).

### Repo — Code-Anker (Ist-Stand, am Code verifiziert 2026-07-02)
- `services/prompt_pipeline.py` — `build_answer_context()` (P1-01/02, LIVE): STABIL/VOLATIL-Split;
  hier dockt (A) im VOLATIL-Teil an. `answer_system_content()`/`derive_answer_params()` = die
  Wiring-Naht der Live-Pfade (P1-02).
- `services/claude_service.py` — `streame_manual_ewb_variante` (Knopf-Antwort-Pfad, Vorbild für den
  neuen Continuation-Knopf); `_med_st['primary_intent']`/`confidence` per-SID (~:1008, TAXO1-Schreiber).
- `services/deepgram_service.py` — Cold-Call-Keyword-Pfad + Gesprächs-Start-Gate-Kontext.
- `database/models.py` — `IntentEvent` (:777, append-only, `payload_jsonb`+`interaction_id`+
  `confidence`+`mode`+`timestamp`; `speaker_role` in payload) + `SuggestionReaction` (:831, NERVEs
  eigene Ausgaben, **Call-Ende-Flush** — NICHT live, darum braucht (A) einen eigenen Live-Ringpuffer).
- `services/live_session.py` — `_session_state[sid]` (per-SID State, `analysiert_bisher`, `_briefing`,
  `anonymisierer`) — Ort des Live-Ringpuffers für (A).
- `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-P1-01-SUMMARY.md`
  + `…-P1-02-SUMMARY.md` — was P1 gebaut hat (Foundation für (b)).
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `build_answer_context()` (P1-01, LIVE): der VOLATIL-Block ist der Andock-Punkt für (A). Enthält
  heute Intent-Hinweis + Konfidenz-Register + volatilen Profilteil — noch KEIN Gesprächsverlauf.
- `streame_manual_ewb_variante` (Knopf-Pfad, Haiku): strukturelles Vorbild für den neuen
  Continuation-Pfad — ABER neues Modell (stark) via config-Schalter durch die gemeinsame Naht (D-07).
- `intent_event` + `payload_jsonb` + `interaction_id`: die Speicher-Seite steht bereits (append-only
  Ereignis-Log). Es fehlt fast nichts außer Gespiegelt-Marker + schema_version (D-13/D-14).
- Gesprächs-Start-Gate (Soll-Verhalten §2, per-SID „call_active"/Opener-Erkennung): der Knopf hängt
  sich dran (D-11) — Erkennung ist TAXO1-Familie/per-SID-Fundament.

### Established Patterns
- Per-SID RAM-State (`_session_state[sid]`) für flüchtige Live-Daten (Ringpuffer für (A) gehört hierhin).
- Config-getriebene Modell-Schalter (`MODEL_*`) + Override ohne Redeploy (Punkt 12) — D-07 folgt dem.
- Anonymisierung VOR Speicherung (DSGVO); Cascade-Ketten via `call_id` FK.

### Integration Points
- (A) → VOLATIL-Teil von `build_answer_context` (kleiner Ringpuffer + Öffner-Liste).
- (B) Continuation-Knopf → neuer Backend-Pfad (Vorbild `streame_manual_…`) + FE-Knopf im PiP,
  Eingang = letzte Berater-Aussage(n) aus dem Live-Transkript; Modell via gemeinsame Naht (D-07).
- (C)/Form → `intent_event` (Gespiegelt-Marker + gesenkte confidence + schema_version).
</code_context>

<specifics>
## Specific Ideas
- „Freie Antwort"-Knopf-Label + Mikrocopy: der Mirror-Trick lebt von kurzen Echos — UI-Mikrocopy
  „in EINEM kurzen Satz spiegeln" (Bau-Vorgabe §6a). Genauer Wortlaut = Bau/PIP.2.
- Continuation-Antwort ist **Anfänger-Hilfe** — muss NICHT 100% zuverlässig sein (Bau-Vorgabe §2 B).
- Rückfrage-Format „Frag nach: X oder Y?" knüpft an das bestehende QA-Rückfrage-Muster an (P1-02
  hat den „Frag nach:"-Format-Kontrakt strukturell erhalten).
</specifics>

<deferred>
## Deferred Ideas (Tür offen halten via Form, NICHT jetzt bauen)
- **Kontenübergreifendes Account-Wissen** (Kunden-Akte über mehrere Gespräche) — mit Meeting-Modus.
- **Manueller Wissens-Eingang mode-übergreifend** (`source=manual` als gleichrangige Quelle, B1) —
  Meeting-Infrastruktur; Cold-Call hat mit dem PreCall-Briefing schon einen manuellen Eingang.
- **Abgeleitete Kunden-Übersicht + async Post-Call-Zusammenfassung** (frisch aus Roh-Events).
- **Zwei Zeiten** (galt-wann vs. erfahren-wann) auf der Account-Fakten-Ebene.
- **Volle LLM-Anbieter-Abstraktion** (Anthropic↔Gemini↔GPT↔eigene KI) + EU-Residenz — eigene
  pre-launch Pflicht-Phase (nur die EINE Modell-Aufruf-Naht wird jetzt vorbereitet, D-07).
- **„gestaffelt nach Sicherheit"** für das Unsicherheits-Verhalten — später Schwellenwert-Dreh (D-09).
- **Finale UI-Platzierung/Politur des Knopfes** — PIP.2 (D-12).
- **Prompt-Caching** (Tempo für das starke Modell) — TAXO3 Phase 2 / Plan 04.
</deferred>

---

*Phase: 08.23.2.TAXO3 — Scheibe (b) Gedächtnis + Continuation*
*Context gathered: 2026-07-02*
</content>
</invoke>
