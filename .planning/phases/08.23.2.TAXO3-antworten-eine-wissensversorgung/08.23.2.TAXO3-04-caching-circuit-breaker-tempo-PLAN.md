---
phase: 08.23.2.TAXO3
plan: 04
type: execute
wave: 4
complexity: 🔴
depends_on: [03]
files_modified:
  - services/prompt_pipeline.py
  - services/claude_service.py
  - services/live_session.py
  - tests/test_answer_cache_layering.py
autonomous: false
requirements: ["SPEC Req 9"]
risiko: riskant
test_anruf: true
user_setup: []

must_haves:
  truths:
    - "build_answer_context schichtet jetzt cache_control AKTIV: der stabile Prefix (Paradigma 1h-ttl + Profil-stabil + Grounding) traegt cache_control:ephemeral; der volatile Block (Briefing/Lead/Intent/Echo) traegt KEIN cache_control (Cache-Miss-Falle vermieden, Req 9)"
    - "Cache-Pre-Warming beim Call-Start (Hintergrund-Task, blockiert den Call-Start NICHT) waermt NUR den global-konstanten Paradigma-Block (1h-ttl, ueber alle Sessions identisch) vor — DEDUPLIZIERT per module-globalem Zeit-Guard (`_paradigm_prewarm_until`): bei N parallelen Session-Starts feuert HOECHSTENS EIN Prewarm-Sonnet-Call pro ttl-Fenster, NICHT N (H-5: kein 50x-Rate-Limit/Kosten). Per-User Profil/Grounding (5m-ttl) werden NICHT prewarmt — sie waermen sich am ersten echten Call, Slot A ueberbrueckt die erste Antwort. Prewarm-Call ist System-attribuiert (kein B-B org_id-Roulette)"
    - "Circuit-Breaker-Wrapper nutzt is_auto_triggered: Auto downgradet bei TTFT-Spike auf Haiku, manueller Knopf probiert IMMER Sonnet (auf bestehender EWB_SONNET_FALLBACK_TTFT_MS-Infra)"
    - "Der Breaker-State (_ewb_ttft_history-Read → 3/5-Schwellen-Check → _ewb_fallback_until-Write) ist durch den BESTEHENDEN module-globalen `_ewb_circuit_lock` (threading.Lock, claude_service.py:20) geschuetzt — die GESAMTE Read-Check-Write-Sequenz ist atomar (deque.append allein ist atomar, die Sequenz NICHT); die NEUE Routing-Read (is_auto_triggered and _ewb_fallback_until > now()) wird unter DENSELBEN Lock gezogen. KEIN zweiter paralleler Lock (Korrektur: _ewb_circuit_lock existiert bereits). Plain Lock reicht (getrennte with-Bloecke, keine Re-Entrancy); RLock-Upgrade nur bei nachgewiesenem Nesting"
    - "Test-Anruf belegt mit EIGENER TTFT-Messung: Sonnet gecacht trifft das Latenz-Budget (~0,5-0,9s Ziel, eigene Zahl vor Festzurren); cache_read_input_tokens > 0; Log-Beleg Knopf→Sonnet, Auto-Spike→Haiku"
    - "build_answer_context ist NICHT grob neu gebaut (Anti-Abrieb D-06) — nur die in Plan 03 angelegte Block-Struktur bekommt cache_control + Pre-Warming + Breaker-Wrapper"
  artifacts:
    - path: "services/prompt_pipeline.py"
      provides: "cache_control-Layering auf der Block-Liste (stabil cached / volatil ungecacht); ttl 1h fuer Paradigma-Geruest, 5m fuer Profil"
      contains: "cache_control"
    - path: "services/claude_service.py"
      provides: "Circuit-Breaker-Wrapper mit is_auto_triggered (Auto→Haiku bei Spike, Knopf→immer Sonnet); is_auto_triggered-Routing-Read unter den BESTEHENDEN `_ewb_circuit_lock` (KEIN zweiter Lock); dedupliziertes System-Prewarm; cache_read-Beleg-Logging"
      contains: "with _ewb_circuit_lock"
    - path: "services/live_session.py"
      provides: "Cache-Pre-Warming bei Session-Start (Background-Task, nicht-blockierend)"
      contains: "prewarm"
    - path: "tests/test_answer_cache_layering.py"
      provides: "Pure-logic: stabiler Block traegt cache_control, volatiler NICHT; Breaker-Routing (auto+spike→Haiku, knopf→Sonnet)"
  key_links:
    - from: "build_answer_context Block-Liste (Plan 03)"
      to: "Anthropic messages.stream(system=[stabil_cached, ..., volatil])"
      via: "cache_control:ephemeral auf stabilen Bloecken (Context7-verifiziert)"
      pattern: "cache_control"
    - from: "is_auto_triggered (Plan 03 Parameter)"
      to: "Circuit-Breaker (EWB_SONNET_FALLBACK_TTFT_MS, _ewb_fallback_until unter _ewb_circuit_lock)"
      via: "Routing Auto→Haiku bei Spike / Knopf→Sonnet"
      pattern: "is_auto_triggered"
    - from: "Breaker-Read-Check-Write (_ewb_ttft_history → 3/5 → _ewb_fallback_until) + NEUE Routing-Read"
      to: "_ewb_circuit_lock (BESTAND, threading.Lock :20)"
      via: "bestehender Lock um die gesamte Sequenz + neue Routing-Read (kein zweiter Lock)"
      pattern: "_ewb_circuit_lock"
    - from: "Session-Start (live_session)"
      to: "Cache-Pre-Warming des stabilen Prefix"
      via: "Background-Task (nicht-blockierend)"
      pattern: "prewarm"
---

> 🔎 **VOR-EXECUTE-RE-GREP (Caching-Infra, RESEARCH Stand verifiziert):** `grep -n "CACHE_EWB\|cache_control\|EWB_SONNET_FALLBACK_TTFT_MS\|_ewb_ttft_history\|_ewb_fallback_until\|_ewb_circuit_lock\|\[EWB-TTFT\]" services/claude_service.py config.py` — bestätigen: EINE cache_control-Block-Stelle (:562/:716), TTFT-Breaker-Infra (:18-19, :592-604), Threshold-ENV (:596). Plan 03 hat build_answer_context auf Block-Liste umgestellt — die Hülle (:561-564/:716) gibt sie durch.
>
> 🔧 **KORREKTUR (Prod-Code-Verifikation 2026-06-13): Es EXISTIERT bereits ein Breaker-Lock — `_ewb_circuit_lock = threading.Lock()` (claude_service.py:20), und er umschließt die Read-Check-Write-Sequenz schon (:571 Routing-Read, :594-601 Feed mit deque-append + 3/5-Check + `_ewb_fallback_until`-Write).** Die frühere Annahme „erwartet: NICHT → lege `_ewb_breaker_lock` an" war FALSCH. → **Dieser Plan legt KEINEN zweiten parallelen Lock an** (zwei Locks auf denselben State = Deadlock-/Inkonsistenz-Risiko, hebt die Atomarität auf). Stattdessen: den bestehenden `_ewb_circuit_lock` WIEDERVERWENDEN und um die NEUE is_auto_triggered-Routing-Read erweitern. Plain `Lock` reicht (keine Re-Entrancy nötig — keine verschachtelten Acquires desselben Locks im selben Thread; die :571/:594-Stellen sind getrennte `with`-Blöcke). **Upgrade auf `RLock` NUR falls der is_auto_triggered-Routing-`with`-Block einen anderen `with _ewb_circuit_lock`-Block re-entrant umschließt** — beim Execute via Re-Grep prüfen; Default = Lock behalten. Überall unten wo „`_ewb_breaker_lock`"/„`threading.RLock()`" stand, gilt jetzt „`_ewb_circuit_lock`" (Bestand) — siehe Task 2.

> RISIKO-BANNER: **riskant.** Aktiviert das Caching-Layering + Circuit-Breaker-Routing auf dem Live-Antwort-Pfad. → **Test-Anruf PFLICHT + EIGENE TTFT-Messung (D-05/Req 9 Constraint: Hersteller-Zahlen interpoliert): Sonnet gecacht ins Budget, cache_read>0, Knopf→Sonnet/Auto-Spike→Haiku belegen.**

> ⚠️ **CROSS-AI-GATE (🔴):** Geplant, nicht sofort ausgeführt. Deploy + Test-Anruf erst nach Cross-AI-Clearance.

> 🔒 **RACE-GATE (Reviewer LOW-Finding, sauber eliminierbar — KORRIGIERT 2026-06-13):** Der Breaker liest `_ewb_ttft_history`, prüft die 3/5-Schwelle und schreibt `_ewb_fallback_until` — `deque.append` allein ist atomar, die Read-Check-Write-SEQUENZ NICHT. **Der dafür nötige Lock EXISTIERT bereits: `_ewb_circuit_lock` (threading.Lock, :20) umschließt die Sequenz schon (:571/:594-601).** Dieser Plan zieht die NEUE Routing-Read (`is_auto_triggered and _ewb_fallback_until > now()`) unter DENSELBEN Lock — **KEIN zweiter `_ewb_breaker_lock`** (zwei Locks auf denselben State = Atomarität aufgehoben, Race zurück). Plain Lock reicht (keine Re-Entrancy: getrennte `with`-Blöcke); RLock-Upgrade nur bei nachgewiesenem Nesting. Das eliminiert die Race vollständig.

> ⚠️ **ANTI-ABRIEB (D-06):** build_answer_context wird NICHT neu gebaut — die in Plan 03 angelegte 4-Block-Struktur bekommt NUR cache_control-Keys (stabil) + bleibt-ungecacht (volatil). Pre-Warming + Breaker-Wrapper sind additiv. Wenn der Re-Grep zeigt, dass Plan 03 die Struktur sauber gelegt hat, ist dieser Plan klein.

> **LATENZ-BUDGET (Punkt 25, Dealbreaker):** Slot A lokal <1s (unverändert). Sonnet gecacht Ziel ~0,5-0,9s TTFT — **eigene [EWB-TTFT]-Messung Pflicht VOR Festzurren** (Cached-Vendor-Zahlen sind interpoliert/UNVERIFIED, RESEARCH A4 LOW-confidence). Pre-Warming darf den Call-Start NICHT blockieren (Background-Task). BALANCE: das STARKE Modell (Sonnet) schnell machen, NICHT aufs schwache (Haiku) ausweichen — Haiku bleibt NUR Auto-Spike-Fallback.
>
> 🟠 **H-5-ENTSCHEIDUNG — Prewarm bedingt + dedupliziert + System-attribuiert (Interlock-Fund, entschieden 2026-06-13):**
> **Problem (H-5):** Naiver Plan = ein echter Sonnet-Call pro Session-Start. Bei 50 parallelen Calls = 50 Extra-Sonnet-Calls → (a) Anthropic-Rate-Limit-Druck, (b) Kosten für reine Cache-Warm-Calls, (c) org_id-Roulette (Background-Thread ohne Request-Context trifft den B-B-„erste-beliebige-Session"-Resolver → Fehl-Verbuchung), (d) die 5m-ttl-Blöcke (Profil/Grounding) sind oft schon kalt-abgelaufen bevor der erste Einwand kommt → Prewarm vergebens.
> **Entscheidung (warum diese, nicht „ganz weglassen" und nicht „naiv per Session"):** REQ-9 verlangt „Cache-Pre-Warming beim Call-Start" (locked) — also NICHT streichen, aber präzisieren auf die EINZIGE Variante, die Nutzen ohne die vier Risiken bringt:
> 1. **Nur der global-konstante Paradigma-Block (1h-ttl)** wird prewarmt — er ist über ALLE Sessions/User identisch (NERVE-Default-Gerüst). Per-User Profil/Grounding (5m) werden bewusst NICHT prewarmt: unzuverlässig (Einwand kann >5min später kommen) und Slot A überbrückt die erste Antwort ohnehin (Bau-Regel 1). Der erste echte Call wärmt die per-User-Blöcke für alle Folge-Einwände im selben Call (dort liegt der echte Latenz-Gewinn).
> 2. **Dedupliziert** per module-globalem `_paradigm_prewarm_until`-Zeit-Guard (unter Lock): N parallele Session-Starts lösen HÖCHSTENS EINEN Prewarm-Call pro ttl-Fenster aus. In Steady-State mit Traffic hält laufender Verkehr den 1h-Block ohnehin warm → Prewarm feuert real nur im Cold-Window direkt nach Deploy/Restart. Das ist genau die richtige Dosis.
> 3. **System-attribuiert:** der Prewarm-Call ist KEINE user-zurechenbare Inferenz → KEIN log_api_cost über den B-B-Resolver; entweder gar nicht kosten-getrackt oder mit explizitem System/NERVE-Sentinel getaggt (kein org_id-Roulette). Cross-Link: B-B-Resolver-Fix in TAXO1-03.
> **Akzeptiertes Trade-off:** die allererste Slot-B-Antwort pro frisch-deploytem Cold-Window kann ungecacht laufen (~0,8-1,5s) — Slot A (<1s lokal) überbrückt sie; Verkäufer pausieren nach Einwänden ohnehin (SPEC Background). Wenn Checkpoint 3 zeigt, dass selbst MIT Slot-A die erste Antwort spürbar stört → Follow-up: globalen Prozess-Warm beim App-Bootstrap (NICHT per-Session) erwägen. NICHT in W2.

<objective>
Aktiviere das Tempo: schichte cache_control auf die in Plan 03 angelegte Block-Struktur (stabiler Prefix gecacht, volatil ungecacht — Cache-Miss-Falle vermieden), wärme NUR den global-konstanten Paradigma-Block beim Call-Start vor (nicht-blockierend, dedupliziert, System-attribuiert — H-5), und baue den Circuit-Breaker-Wrapper mit `is_auto_triggered` (Auto→Haiku bei TTFT-Spike, Knopf→immer Sonnet) auf die bestehende `EWB_SONNET_FALLBACK_TTFT_MS`-Infra — geschützt durch den BESTEHENDEN `_ewb_circuit_lock` (kein zweiter Lock). Das macht das starke Modell (Sonnet) schnell genug für die natürliche Verkäufer-Pause (Req 9).

Purpose: Qualität (Plan 03) + Tempo (dieser Plan) = Balance (Punkt 25). Sonnet gecacht fällt in die Pause; Slot A überbrückt; Auto weicht nur bei echtem Spike auf Haiku aus.
Output: cache_control-Layering aktiv; dedupliziertes System-Prewarm (H-5); is_auto_triggered-Breaker unter dem bestehenden _ewb_circuit_lock; Test-Anruf mit eigener TTFT-Messung + cache_read-Beleg.

KEINE build_answer_context-Neubau (Plan 03). KEIN Slot-B-Dedup (Plan 05). Slot A unverändert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen. STATE/ROADMAP hand-editieren.

**Verify = Production only:** `bash deploy.sh production` + **Test-Anruf als Test-User auf getnerve.app** + `inspect.sh logs` für [EWB-TTFT] + cache_read. KEIN lokales pytest als Acceptance.

**Context7 (Pflicht für SDK-Caching):** Anthropic Python SDK system-Block-Liste mit cache_control — verifiziert (RESEARCH §Standard Stack + Context7 /anthropics/anthropic-sdk-python): `system=[{"type":"text","text":...,"cache_control":{"type":"ephemeral","ttl":"1h"|"5m"}}, ...]`. ttl "1h" fürs konstante Gerüst, "5m" (default) fürs Profil, KEIN cache_control auf dem volatilen Block. cache_read_input_tokens im usage-Objekt belegt den Treffer.

**UTF-8-Regel:** Code-Identifier ASCII; Prompt-Text echte Umlaute.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md

<interfaces>
<!-- Context7-verifiziert (/anthropics/anthropic-sdk-python) + Bestand (RESEARCH). -->

cache_control-Layering (Plan 04 setzt die Keys auf die Plan-03-Block-Liste):
  system = [
    {"type":"text","text": PARADIGMA_GERUEST,  "cache_control":{"type":"ephemeral","ttl":"1h"}},  # stabil, konstant
    {"type":"text","text": PROFIL_STABLE,       "cache_control":{"type":"ephemeral"}},             # stabil, 5m default
    {"type":"text","text": GROUNDING_FAQ,       "cache_control":{"type":"ephemeral"}},             # stabil
    {"type":"text","text": VOLATIL_BRIEFING_LEAD_INTENT_ECHO},                                      # KEIN cache_control
  ]
  # max 4 cache_control-Breakpoints (API-Limit) — wir nutzen 3 (volatil ungecacht).
  # Treffer-Beleg: response.usage.cache_read_input_tokens > 0 nach dem 2. Call.

Bestehende TTFT-Breaker-Infra (claude_service.py:18-19, :592-604 — erweitern, NICHT neu bauen):
  _ewb_ttft_history (deque), _ewb_fallback_until (timestamp)
  EWB_SONNET_FALLBACK_TTFT_MS (ENV default 1500); 3/5-ueber-Schwelle → fallback_until = now+30s → Haiku
  [EWB-TTFT] {ms} model={model} sid={sid}  (:604 — Beleg-Log)

BESTAND (NICHT neu anlegen — Korrektur-Banner oben): der Breaker-Lock existiert schon:
  _ewb_circuit_lock = threading.Lock()   # claude_service.py:20, module-global, neben _ewb_ttft_history/_ewb_fallback_until
  # umschliesst BEREITS: (1) Feed :594-601 (deque-append + 3/5-Check + _ewb_fallback_until-Write)
  #                      (2) Read :571 (_cb_in_fallback)
  # Plan 04: WIEDERVERWENDEN + um die NEUE is_auto_triggered-Routing-Read erweitern. KEIN zweiter Lock.

is_auto_triggered-Routing (Plan 03 reicht das Flag durch; unter dem BESTEHENDEN _ewb_circuit_lock gelesen):
  with _ewb_circuit_lock:
      if is_auto_triggered and _ewb_fallback_until > now():  model = HAIKU   # Auto weicht bei Spike aus
      else:                                                   model = SONNET # Knopf IMMER Sonnet
  # Knopf (is_auto_triggered=False) ignoriert das Fenster logisch — liest aber konsistent unter demselben Lock.
  # Plain Lock reicht (keine Re-Entrancy: getrennte with-Bloecke, kein verschachteltes Acquire). RLock NUR falls Re-Grep ein Nesting zeigt.

Pre-Warming (H-5: bedingt + dedupliziert + nur Paradigma-Block + System-attribuiert):
  _paradigm_prewarm_until = 0.0          # module-global Zeit-Guard (unter Lock), neben den Breaker-Globals
  _paradigm_prewarm_lock = threading.Lock()
  # live_session Session-Start (init_session_state :299, user_id+org_id in Scope):
  with _paradigm_prewarm_lock:
      if monotonic() < _paradigm_prewarm_until:  return   # schon gewarmt in diesem ttl-Fenster → KEIN Call
      _paradigm_prewarm_until = monotonic() + PREWARM_DEDUP_WINDOW_S  # z.B. 3000s (<1h-ttl, mit Sicherheitsmarge)
  start_background_task(prewarm_answer_cache)   # EIN minimaler Sonnet-Call, NUR der konstante Paradigma-Block (Block 1)
  # - KEINE per-User Profil/Grounding-Warmung (5m unzuverlässig; Slot A überbrückt; erster echter Call wärmt sie)
  # - System-attribuiert: KEIN log_api_cost über B-B-Resolver (Cache-Warm, keine user-Inferenz)
  # - try/except still, NIE den Call-Start blockieren (Background)
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
> Dieser Plan ändert KEINE DB-Tabelle (reines Prompt-Caching + Modell-Routing + Background-Task). Punkt 21 Skip-OK für DB-Schicht — ABER der Cache-State (RAM) + Breaker-State (RAM) ist Runtime-State (Punkt 14 Race-Audit unten Pflicht).

### Cross-Layer-Konsistenz-Tabelle (Runtime-State, kein DB)
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `_ewb_ttft_history` / `_ewb_fallback_until` | Breaker liest/schreibt unter dem BESTEHENDEN _ewb_circuit_lock (:20) + neue Routing-Read dazu | RAM module-global (Bestand) + Lock (Bestand) | ⬜ Task 2 Race-Audit (Lock wraps read-check-write + Routing-Read; kein zweiter Lock) |
| `_paradigm_prewarm_until` (H-5 Prewarm-Dedup) | Prewarm-Guard liest/schreibt unter _paradigm_prewarm_lock | RAM module-global (neu) + Lock (neu) | ⬜ Task 1 test_prewarm_dedup |
| Prompt-Cache-Treffer | Anthropic-seitig (ephemeral 5m/1h) | Vendor-Cache (kein lokaler State) | ⬜ Task 3 cache_read>0 |
| Pre-Warm-Background-Task | live_session Session-Start | RAM (Background-Thread) | ⬜ Task 1 nicht-blockierend |
| method_pack-Cache (Plan 03) | load_method_pack per-user | RAM (Bestand Plan 03) | ✓ Plan 03 |

## Verbindungs-Karte (Punkt 22)
- **cache_control-Stelle** lebt EINMAL (:562/:716, grep) → Plan 03 hat sie auf Block-Liste-Durchreichen umgestellt; Plan 04 setzt die Keys (stabil) + lässt volatil ungecacht. Kein neuer Cache-Manager (Don't-Hand-Roll: Vendor-Caching).
- **TTFT-Breaker** lebt (_ewb_ttft_history/_ewb_fallback_until :18-19, `_ewb_circuit_lock = threading.Lock()` :20, grep) → erweitern um is_auto_triggered-Routing unter dem BESTEHENDEN `_ewb_circuit_lock` (Bestand, kein Neubau, KEIN zweiter Lock — Korrektur-Banner).
- **Pre-Warming** ist neu → live_session Session-Start (init_session_state :299), start_background_task (Bestand-Muster wie streame_auto_variante-Spawn). **H-5: dedupliziert (`_paradigm_prewarm_until`-Guard, höchstens 1 Call/ttl-Fenster), nur konstanter Paradigma-Block, System-attribuiert (kein B-B org_id-Roulette).** grep nach Execute: KEIN per-Session-unbedingter Sonnet-Call, KEIN log_api_cost im Prewarm-Pfad.
- **Sonnet ist NICHT neu** (Pitfall 6): config.py:49-69 alle Sonnet seit langem; André-Freigabe 2026-06-12. Req 9 = Schichtung + Breaker-Verfeinerung, KEINE Modell-Umstellung.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache_control-Layering aktivieren + Pre-Warming (build_answer_context-Bloecke, live_session Session-Start)</name>
  <files>services/prompt_pipeline.py, services/live_session.py</files>
  <read_first>
    - services/prompt_pipeline.py (build_answer_context aus Plan 03 — die 4-Block-Liste; HIER cache_control auf Block 1-3 setzen, Block 4 NICHT)
    - services/claude_service.py:561-564, :716-717 (cache-Huelle, die die Liste durchreicht)
    - services/live_session.py (Session-Start-Stelle + start_background_task-Muster)
    - RESEARCH §Standard Stack (Context7-Block-Liste) + §Open Questions OQ-3 (Pre-Warming)
  </read_first>
  <behavior>
    - build_answer_context-Block 1 (Paradigma-Gerüst) trägt cache_control ttl="1h"; Block 2 (Profil-stabil) + Block 3 (Grounding) tragen cache_control (5m default); Block 4 (volatil) trägt KEIN cache_control
    - max 4 Breakpoints eingehalten (3 genutzt)
    - prewarm_answer_cache() läuft als Background-Task, blockiert den Session-Start NICHT, schreibt KEINEN State der den Live-Loop blockiert
    - **H-5: Prewarm ist dedupliziert (module-globaler `_paradigm_prewarm_until`-Zeit-Guard unter Lock → höchstens EIN Sonnet-Call pro ttl-Fenster, NICHT pro Session), wärmt NUR den global-konstanten Paradigma-Block (Block 1, 1h-ttl), und ist System-attribuiert (KEIN log_api_cost über den B-B-Resolver). Per-User Profil/Grounding werden NICHT prewarmt.**
    - fail-open bleibt (Plan 03): Caching-Fehler → ungecachter Fallback, kein raise
  </behavior>
  <action>
    1. **cache_control-Layering (prompt_pipeline.py build_answer_context):** Setze auf die in Plan 03 gebaute Block-Liste die cache_control-Keys: Block 1 `{"type":"ephemeral","ttl":"1h"}` (Paradigma-Gerüst konstant), Block 2 + Block 3 `{"type":"ephemeral"}` (5m default, Profil/Grounding), Block 4 (Briefing/Lead/Intent/Echo) **KEIN cache_control** (Cache-Miss-Falle vermeiden, Req 9 Constraint). Nur >= _CACHE_MIN_CHARS (4096) cachen (Bestand-Guard). Context7-Syntax verbatim.
    2. **Pre-Warming (H-5: bedingt + dedupliziert + nur Paradigma-Block + System-attribuiert):** Lege module-global `_paradigm_prewarm_until = 0.0` + `_paradigm_prewarm_lock = threading.Lock()` an (neben den Breaker-Globals). In init_session_state (live_session.py:299, user_id+org_id in Scope) beim Session-Start:
       ```
       with _paradigm_prewarm_lock:
           if _time.monotonic() < _paradigm_prewarm_until:  _skip = True
           else:  _paradigm_prewarm_until = _time.monotonic() + PREWARM_DEDUP_WINDOW_S; _skip = False
       if not _skip:  sio.start_background_task(prewarm_answer_cache)
       ```
       `prewarm_answer_cache()` = EIN minimaler Sonnet-Call mit NUR dem global-konstanten Paradigma-Block (Block 1 aus build_answer_context — KEINE per-User Profil/Grounding/Briefing-Daten, die sind 5m-ttl + user-spezifisch → unzuverlässig + Slot-A überbrückt). **System-attribuiert: KEIN log_api_cost** (Cache-Warm, keine user-Inferenz; falls Tracking gewünscht → expliziter System/NERVE-Sentinel, NIE der B-B-„erste-Session"-Resolver). `PREWARM_DEDUP_WINDOW_S` ENV-default ~3000 (<1h-ttl, Sicherheitsmarge). **NIE den Call-Start blockieren** (Background, try/except → still). **Begründung (H-5) im SUMMARY festhalten** (warum bedingt/dedupliziert/system-attribuiert statt naiv-per-Session).
    Test `tests/test_answer_cache_layering.py` (pure-logic):
    - test_stable_blocks_have_cache_control: Block 1-3 tragen cache_control; Block 1 ttl=="1h".
    - test_volatile_block_no_cache_control: Block 4 (Briefing/Lead) trägt KEIN cache_control (Cache-Miss-Schutz).
    - test_max_four_breakpoints: <= 4 cache_control-Keys.
    - test_prewarm_non_blocking: prewarm wirft nicht / läuft im Background (monkeypatched Anthropic-Call, kein Block).
    - **test_prewarm_dedup: zwei aufeinanderfolgende Session-Starts innerhalb des ttl-Fensters → der Prewarm-Sonnet-Call (monkeypatched, Zähler) feuert HÖCHSTENS EINMAL, nicht zweimal (H-5: kein N-fach-Call). Monotonic-Clock monkeypatchen (kein echtes Sleep).**
    - **test_prewarm_no_cost_track: prewarm_answer_cache ruft KEIN log_api_cost mit dem B-B-Resolver (monkeypatched log_api_cost → assert not-called ODER nur mit System-Sentinel-org_id).**
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_answer_cache_layering.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cache_control" services/prompt_pipeline.py` deckt build_answer_context-Block 1-3 ab (>=3 in der Block-Liste)
    - `grep -E "ttl.*1h" services/prompt_pipeline.py` == vorhanden (Paradigma-Gerüst 1h)
    - test_stable_blocks_have_cache_control + test_volatile_block_no_cache_control + test_max_four_breakpoints + test_prewarm_non_blocking + test_prewarm_dedup + test_prewarm_no_cost_track grün
    - `grep -c "prewarm" services/live_session.py` >= 1; Pre-Warming als Background-Task (start_background_task)
    - **H-5: `grep -n "_paradigm_prewarm_until\|_paradigm_prewarm_lock" services/...` zeigt den Dedup-Guard (höchstens 1 Call/ttl-Fenster); `grep` belegt prewarm_answer_cache wärmt NUR den konstanten Paradigma-Block (kein per-User-Profil-Aufruf) UND ruft KEIN log_api_cost über den B-B-Resolver (System-attribuiert)**
    - Block 4 (volatil) hat KEIN cache_control (test_volatile_block_no_cache_control)
  </acceptance_criteria>
  <done>cache_control-Layering aktiv (stabil cached/volatil ungecacht), Pre-Warming nicht-blockierend, fail-open erhalten.</done>
</task>

<task type="auto">
  <name>Task 2: Circuit-Breaker-Wrapper mit is_auto_triggered unter dem BESTEHENDEN _ewb_circuit_lock (Punkt 14 Race-Audit Pflicht; KEIN zweiter Lock)</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py:18-20 (_ewb_ttft_history/_ewb_fallback_until + `_ewb_circuit_lock = threading.Lock()` :20 — Bestand, WIEDERVERWENDEN) + :571 (Read unter Lock) + :594-604 (Breaker-Heuristik 3/5 unter Lock, [EWB-TTFT]) — Re-Grep bestätigt: Lock existiert, kein neuer nötig
    - services/claude_service.py (streame_auto_variante is_auto_triggered aus Plan 03 + streame_manual_ewb_variante is_auto_triggered=False)
    - config.py:49-69 (Sonnet/Haiku-Modelle) + EWB_SONNET_FALLBACK_TTFT_MS
  </read_first>
  <action>
    **Punkt-14-Control-Flow + Race-Audit (eingebettet, Pflicht — Breaker-State ist module-global RAM):**
    - **BESTEHENDEN Lock WIEDERVERWENDEN (KEIN zweiter Lock — Korrektur-Banner oben):** `_ewb_circuit_lock = threading.Lock()` existiert bereits (claude_service.py:20) und umschließt die Read-Check-Write-Sequenz schon (:571 Read `_cb_in_fallback`, :594-601 Feed deque-append + 3/5-Check + `_ewb_fallback_until`-Write). NICHT `_ewb_breaker_lock` neu anlegen — zwei Locks auf denselben State heben die Atomarität auf (Race kommt zurück) und riskieren Deadlock. Stattdessen: die NEUE is_auto_triggered-Routing-Read unter DENSELBEN `_ewb_circuit_lock` ziehen.
    - **Plain Lock reicht (kein RLock-Upgrade default):** die Acquires sind getrennte `with`-Blöcke (Routing-Read VOR dem stream-Call; Feed NACH dem ersten Token) — KEIN verschachteltes Acquire desselben Locks im selben Thread → keine Re-Entrancy nötig. **RLock-Upgrade NUR falls der Re-Grep zeigt, dass die Routing-`with`-Klammer einen anderen `with _ewb_circuit_lock`-Block umschließt** (Nesting). Default: `threading.Lock` (Bestand) behalten. Entscheidung im SUMMARY notieren.
    - **Was der Lock umschließt (Bestand + NEU):** Die GESAMTE Read-Check-Write-Sequenz des Breaker-State ist NICHT atomar (deque.append allein schon, die Sequenz `_ewb_ttft_history-Read → 3/5-Schwellen-Check → _ewb_fallback_until-Write` NICHT). Unter `_ewb_circuit_lock`:
      1. den **Breaker-Feed** (bestehende 3/5-Heuristik :594-601, schreibt `_ewb_fallback_until`) — bleibt wie Bestand unter dem Lock.
      2. die **Routing-Read** (`if is_auto_triggered and _ewb_fallback_until > now()`) — NEU ebenfalls unter `with _ewb_circuit_lock:`, damit Lese- und Schreibseite konsistent sind (heute liest :571 nur `_cb_in_fallback` unter dem Lock; die is_auto_triggered-Verzweigung kommt dazu).
    - Wer liest/schreibt: Auto-Pfad (analyse_loop-Thread) + Manual-Pfad (Button-Thread) gleichzeitig. Beide gehen über `_ewb_circuit_lock`. Der manuelle/Knopf-Pfad liest `_ewb_fallback_until` logisch nicht (probiert immer Sonnet) — falls er aus Konsistenz-Gründen unter dem Lock liest, unbedenklich (billiger Lock); Entscheidung (liest-er/liest-er-nicht) im SUMMARY notieren.

    **Routing:** Vor dem messages.stream-Call:
    ```
    with _ewb_circuit_lock:
        if is_auto_triggered and _ewb_fallback_until > _now():
            model = config.HAIKU_MODEL   # Auto weicht bei aktivem Spike-Fenster aus
            _log("[EWB-BREAKER] auto-downgrade→haiku sid=%s", sid)
        else:
            model = config.SONNET_MODEL  # Knopf IMMER Sonnet (is_auto_triggered=False); Auto ohne Spike auch
    ```
    - Die bestehende 3/5-über-Schwelle-Heuristik (:594-601) füttert `_ewb_fallback_until` weiter (Bestand) — bereits unter `_ewb_circuit_lock` (Read-Check-Write atomar). is_auto_triggered entscheidet NUR, ob das Fenster honoriert wird.
    - [EWB-TTFT]-Log (:604) bleibt + ergänze ein [EWB-BREAKER]-Log, das model + is_auto_triggered + sid zeigt (Beleg Knopf→Sonnet, Auto-Spike→Haiku).
    - Manual-Pfad (streame_manual_ewb_variante, is_auto_triggered=False): probiert IMMER Sonnet.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_auto_triggered" services/claude_service.py` deckt das Breaker-Routing ab (Auto vs Knopf)
    - `grep -c "_ewb_breaker_lock\|threading.RLock()" services/claude_service.py` == 0 (KEIN zweiter Lock angelegt — Korrektur); `grep "_ewb_circuit_lock = threading.Lock()" services/claude_service.py` == 1 (Bestand unverändert)
    - `grep -c "with _ewb_circuit_lock" services/claude_service.py` >= 3 (Bestand :571/:594 + NEUE Routing-Read)
    - `grep -E "_ewb_fallback_until" services/claude_service.py` zeigt: jede Read/Write-Stelle des Breaker-State liegt innerhalb eines `with _ewb_circuit_lock:`-Blocks
    - `grep "\[EWB-BREAKER\]" services/claude_service.py` >= 1 (Beleg-Log model+is_auto_triggered)
    - Race-Audit in SUMMARY dokumentiert: der BESTEHENDE _ewb_circuit_lock umschließt die GESAMTE Read-Check-Write-Sequenz (_ewb_ttft_history-Read + 3/5-Check + _ewb_fallback_until-Write) UND die NEUE Routing-Read; idempotenter Downgrade; Lock-Typ-Entscheidung (Plain Lock behalten / RLock nur bei Nesting) + Knopf-liest-unter-Lock-oder-nicht notiert
    - Manual-Pfad routet IMMER auf Sonnet (manuelle Sicht)
  </acceptance_criteria>
  <done>Circuit-Breaker-Wrapper mit is_auto_triggered (Auto→Haiku bei Spike, Knopf→Sonnet) unter dem BESTEHENDEN _ewb_circuit_lock (kein zweiter Lock), Race-Audit gemacht, Beleg-Logging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Test-Anruf W2 (Tempo) — eigene TTFT-Messung + cache_read + Breaker-Beleg (D-05/Req 9 Pflicht)</name>
  <what-built>
    cache_control-Layering aktiv (stabil cached, volatil ungecacht); dedupliziertes System-Prewarm (nur Paradigma-Block); Circuit-Breaker mit is_auto_triggered (Auto→Haiku bei Spike, Knopf→Sonnet) unter dem bestehenden _ewb_circuit_lock. Deployed auf Production.
  </what-built>
  <how-to-verify>
    1. getnerve.app als Test-User, Live-Call (cold_call). Mehrere Einwände, damit Pre-Warm + Cache-Treffer greifen.
    2. **Eigene TTFT-Messung (Pflicht VOR Festzurren, Req 9 Constraint):** Claudian zieht `inspect.sh logs` → `[EWB-TTFT] <ms> model=<sonnet> sid=...`-Zeilen. Sonnet GECACHT sollte das Budget treffen (~0,5-0,9s Ziel — die eigene Zahl notieren, NICHT die interpolierte Vendor-Zahl annehmen). Wenn deutlich über Budget → umdesignen, NICHT „später optimieren" (Punkt 25 Dealbreaker).
    3. **cache_read-Beleg:** in den Logs cache_read_input_tokens > 0 nach dem 2. Call (Cache greift). Wenn 0 trotz mehrerer Calls → Cache-Miss-Falle (volatiles Datum im Prefix?) → Block-Split prüfen.
    4. **Breaker-Beleg:** Knopf-Pfad → `[EWB-BREAKER] ...→sonnet` (Knopf immer Sonnet). Auto-Pfad bei künstlichem TTFT-Spike (oder im Log-Verlauf) → `[EWB-BREAKER] auto-downgrade→haiku`.
    5. Slot A weiter <1s lokal; Antwort streamt zügig in die Pause.
  </how-to-verify>
  <resume-signal>Tippe "bestätigt" wenn: eigene Sonnet-gecacht-TTFT im Budget + cache_read>0 + Knopf→Sonnet/Auto-Spike→Haiku belegt. Sonst: Abweichung beschreiben (Cache-Miss / TTFT über Budget → Claude redesignt).</resume-signal>
  <acceptance_criteria>
    - Eigene [EWB-TTFT]-Messung dokumentiert (SUMMARY): Sonnet gecacht im Latenz-Budget (~0,5-0,9s Ziel) — eigene Zahl, nicht interpoliert
    - cache_read_input_tokens > 0 nach 2. Call (Log-Beleg) — Cache-Layering greift, kein flüchtiges Datum im Prefix
    - Log-Beleg: Knopf → Sonnet; Auto bei TTFT-Spike → Haiku ([EWB-BREAKER])
    - Slot A <1s lokal (unverändert); KI-Antwort streamt in die Pause
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| volatiles Datum (Briefing/Lead) → Cache-Prefix | Cache-Miss-Falle: ungecacht halten, sonst Invalidierung + TTFT-Spike |
| Breaker-State (RAM, module-global) → Auto+Knopf-Threads | Race auf _ewb_fallback_until (Read-Check-Write) → BESTEHENDER _ewb_circuit_lock (kein zweiter Lock) |
| Prewarm-Background-Task (N parallele Session-Starts) → ein Sonnet-Call | H-5: Dedup-Guard _paradigm_prewarm_until → höchstens 1 Call/ttl-Fenster; System-attribuiert |
| Pre-Warm-Background-Task → Call-Start | darf den Live-Call-Start nicht blockieren |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3-04-01 | D (DoS / Latenz) | volatiles Briefing/Lead im Cache-Prefix → Cache-Miss bei jedem Wechsel → TTFT-Spike → Breaker feuert dauernd | mitigate | Block 4 trägt KEIN cache_control (test_volatile_block_no_cache_control); cache_read>0-Live-Beleg. |
| T-TAXO3-04-02 | T (Race) | gleichzeitiger Auto+Knopf-Zugriff auf _ewb_fallback_until (Read-Check-Write nicht atomar) → inkonsistentes Routing | mitigate | **Der BESTEHENDE `_ewb_circuit_lock` (threading.Lock, :20) umschließt die GESAMTE Read-Check-Write-Sequenz (_ewb_ttft_history-Read + 3/5-Check + _ewb_fallback_until-Write, Bestand :594-601) UND die NEUE Routing-Read. KEIN zweiter paralleler Lock (Korrektur: würde Atomarität aufheben). Acceptance: grep `_ewb_circuit_lock = threading.Lock()` ==1 + `with _ewb_circuit_lock` >=3 + KEIN `_ewb_breaker_lock`. Eliminiert die Race vollständig.** |
| T-TAXO3-04-03 | D (DoS) | Pre-Warm-Task blockiert/verlangsamt den Call-Start | mitigate | Background-Task (start_background_task), try/except still, nie synchron im Start-Pfad (test_prewarm_non_blocking). |
| T-TAXO3-04-04 | D (Latenz) | Sonnet gecacht trifft das Budget NICHT (interpolierte Zahl war falsch) | mitigate | Eigene TTFT-Messung Pflicht VOR Festzurren (Checkpoint 3); über Budget → umdesignen statt „später". |
| T-TAXO3-04-05 | T | Auto-Pfad weicht NIE auf Haiku aus (Spike-Fenster ignoriert) → langsame Auto-Antworten | mitigate | is_auto_triggered honoriert _ewb_fallback_until (unter _ewb_circuit_lock); [EWB-BREAKER]-Log belegt den Downgrade. |
| T-TAXO3-04-06 | D (DoS / Kosten) | H-5: naiver Prewarm = 1 Sonnet-Call pro Session-Start → 50 parallele Calls = 50 Extra-Calls (Rate-Limit + Kosten + org_id-Roulette) | mitigate | **Dedup-Guard `_paradigm_prewarm_until` (unter Lock) → höchstens 1 Prewarm-Call/ttl-Fenster; nur global-konstanter Paradigma-Block (per-User-Blöcke 5m → nicht prewarmt, Slot-A überbrückt); System-attribuiert (kein log_api_cost über B-B-Resolver). Acceptance: test_prewarm_dedup + test_prewarm_no_cost_track.** |
| T-TAXO3-04-07 | I (Info / Kosten-Fehlverbuchung) | H-5/B-B: Prewarm-Call (Background, kein Request-Context) trifft den „erste-beliebige-Session"-org_id-Resolver → falsche Kosten-Zuordnung | mitigate | Prewarm ist System-attribuiert: KEIN log_api_cost (Cache-Warm, keine user-Inferenz) ODER expliziter System/NERVE-Sentinel-org_id. Cross-Link: B-B-Resolver-Fix in TAXO1-03. |
</threat_model>

<verification>
- pytest test_answer_cache_layering.py grün (stabil cached / volatil ungecacht / <=4 Breakpoints / prewarm non-blocking / **prewarm-dedup / prewarm-no-cost-track**).
- `grep cache_control services/prompt_pipeline.py` deckt Block 1-3 ab, ttl=1h auf Block 1; Block 4 ohne.
- `grep is_auto_triggered services/claude_service.py` deckt Breaker-Routing ab; [EWB-BREAKER]-Log.
- **`grep "_ewb_circuit_lock = threading.Lock()" services/claude_service.py` == 1 (Bestand); `grep -c "with _ewb_circuit_lock" services/claude_service.py` >= 3 (Read-Check-Write + Routing-Read); `grep -c "_ewb_breaker_lock\|threading.RLock()" services/claude_service.py` == 0 (KEIN zweiter Lock).**
- **H-5: `grep "_paradigm_prewarm_until" services/...` zeigt den Dedup-Guard; Prewarm wärmt nur den konstanten Paradigma-Block + ruft KEIN log_api_cost über den B-B-Resolver (System-attribuiert).**
- Test-Anruf (D-05/Req 9): eigene Sonnet-gecacht-TTFT im Budget; cache_read>0; Knopf→Sonnet/Auto-Spike→Haiku.
- build_answer_context NICHT grob neu gebaut (Anti-Abrieb D-06) — nur cache_control-Keys + Pre-Warm + Breaker additiv.
- Race-Audit Breaker-State im SUMMARY (bestehender _ewb_circuit_lock wraps read-check-write + Routing-Read; kein zweiter Lock).
</verification>

<success_criteria>
- Caching geschichtet (stabil gecacht, volatil ungecacht, kein flüchtiges Datum im Prefix); Pre-Warming nicht-blockierend, dedupliziert + System-attribuiert (H-5); is_auto_triggered-Breaker (Auto→Haiku/Knopf→Sonnet) unter dem bestehenden _ewb_circuit_lock.
- Eigene TTFT-Messung belegt Sonnet gecacht im Budget (nicht interpoliert); cache_read>0.
- Race-Condition im Breaker vollständig eliminiert (bestehender _ewb_circuit_lock um Read-Check-Write + Routing-Read, KEIN zweiter Lock); Prewarm-Explosion eliminiert (Dedup-Guard, H-5); Anti-Abrieb D-06 gewahrt (build_answer_context nicht neu gebaut).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-04-SUMMARY.md` (inkl. pytest-Beleg, eigene [EWB-TTFT]-Sonnet-gecacht-Zahl + Budget-Abgleich, cache_read>0-Log, [EWB-BREAKER]-Beleg Knopf→Sonnet/Auto-Spike→Haiku, Race-Audit Breaker-State [bestehender _ewb_circuit_lock wraps read-check-write + Routing-Read, Lock-Typ-Entscheidung Plain-Lock/RLock], **H-5-Prewarm-Begründung [bedingt/dedupliziert/nur-Paradigma-Block/System-attribuiert] + test_prewarm_dedup + test_prewarm_no_cost_track**).
</output>
</content>
