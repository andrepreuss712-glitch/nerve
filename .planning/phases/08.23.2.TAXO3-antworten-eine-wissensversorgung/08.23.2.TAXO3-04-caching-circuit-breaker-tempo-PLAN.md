---
phase: 08.23.2.TAXO3
plan: 04
type: execute
wave: 4
complexity: 🔴
depends_on: [03]
files_modified:
  - services/prompt_pipeline.py
  - services/claude_service.py
  - services/live_session.py
  - tests/test_answer_cache_layering.py
autonomous: false
requirements: ["SPEC Req 9"]
risiko: riskant
test_anruf: true
user_setup: []

must_haves:
  truths:
    - "build_answer_context schichtet jetzt cache_control AKTIV: der stabile Prefix (Paradigma 1h-ttl + Profil-stabil + Grounding) traegt cache_control:ephemeral; der volatile Block (Briefing/Lead/Intent/Echo) traegt KEIN cache_control (Cache-Miss-Falle vermieden, Req 9)"
    - "Cache-Pre-Warming beim Call-Start (Hintergrund-Task, blockiert den Call-Start NICHT) waermt den stabilen Prefix vor"
    - "Circuit-Breaker-Wrapper nutzt is_auto_triggered: Auto downgradet bei TTFT-Spike auf Haiku, manueller Knopf probiert IMMER Sonnet (auf bestehender EWB_SONNET_FALLBACK_TTFT_MS-Infra)"
    - "Test-Anruf belegt mit EIGENER TTFT-Messung: Sonnet gecacht trifft das Latenz-Budget (~0,5-0,9s Ziel, eigene Zahl vor Festzurren); cache_read_input_tokens > 0; Log-Beleg Knopf→Sonnet, Auto-Spike→Haiku"
    - "build_answer_context ist NICHT grob neu gebaut (Anti-Abrieb D-06) — nur die in Plan 03 angelegte Block-Struktur bekommt cache_control + Pre-Warming + Breaker-Wrapper"
  artifacts:
    - path: "services/prompt_pipeline.py"
      provides: "cache_control-Layering auf der Block-Liste (stabil cached / volatil ungecacht); ttl 1h fuer Paradigma-Geruest, 5m fuer Profil"
      contains: "cache_control"
    - path: "services/claude_service.py"
      provides: "Circuit-Breaker-Wrapper mit is_auto_triggered (Auto→Haiku bei Spike, Knopf→immer Sonnet); cache_read-Beleg-Logging"
      contains: "is_auto_triggered"
    - path: "services/live_session.py"
      provides: "Cache-Pre-Warming bei Session-Start (Background-Task, nicht-blockierend)"
      contains: "prewarm"
    - path: "tests/test_answer_cache_layering.py"
      provides: "Pure-logic: stabiler Block traegt cache_control, volatiler NICHT; Breaker-Routing (auto+spike→Haiku, knopf→Sonnet)"
  key_links:
    - from: "build_answer_context Block-Liste (Plan 03)"
      to: "Anthropic messages.stream(system=[stabil_cached, ..., volatil])"
      via: "cache_control:ephemeral auf stabilen Bloecken (Context7-verifiziert)"
      pattern: "cache_control"
    - from: "is_auto_triggered (Plan 03 Parameter)"
      to: "Circuit-Breaker (EWB_SONNET_FALLBACK_TTFT_MS, _ewb_fallback_until)"
      via: "Routing Auto→Haiku bei Spike / Knopf→Sonnet"
      pattern: "is_auto_triggered"
    - from: "Session-Start (live_session)"
      to: "Cache-Pre-Warming des stabilen Prefix"
      via: "Background-Task (nicht-blockierend)"
      pattern: "prewarm"
---

> 🔎 **VOR-EXECUTE-RE-GREP (Caching-Infra, RESEARCH Stand verifiziert):** `grep -n "CACHE_EWB\|cache_control\|EWB_SONNET_FALLBACK_TTFT_MS\|_ewb_ttft_history\|_ewb_fallback_until\|\[EWB-TTFT\]" services/claude_service.py config.py` — bestätigen: EINE cache_control-Block-Stelle (:562/:716), TTFT-Breaker-Infra (:18-19, :592-604), Threshold-ENV (:596). Plan 03 hat build_answer_context auf Block-Liste umgestellt — die Hülle (:561-564/:716) gibt sie durch.

> RISIKO-BANNER: **riskant.** Aktiviert das Caching-Layering + Circuit-Breaker-Routing auf dem Live-Antwort-Pfad. → **Test-Anruf PFLICHT + EIGENE TTFT-Messung (D-05/Req 9 Constraint: Hersteller-Zahlen interpoliert): Sonnet gecacht ins Budget, cache_read>0, Knopf→Sonnet/Auto-Spike→Haiku belegen.**

> ⚠️ **CROSS-AI-GATE (🔴):** Geplant, nicht sofort ausgeführt. Deploy + Test-Anruf erst nach Cross-AI-Clearance.

> ⚠️ **ANTI-ABRIEB (D-06):** build_answer_context wird NICHT neu gebaut — die in Plan 03 angelegte 4-Block-Struktur bekommt NUR cache_control-Keys (stabil) + bleibt-ungecacht (volatil). Pre-Warming + Breaker-Wrapper sind additiv. Wenn der Re-Grep zeigt, dass Plan 03 die Struktur sauber gelegt hat, ist dieser Plan klein.

> **LATENZ-BUDGET (Punkt 25, Dealbreaker):** Slot A lokal <1s (unverändert). Sonnet gecacht Ziel ~0,5-0,9s TTFT — **eigene [EWB-TTFT]-Messung Pflicht VOR Festzurren** (Cached-Vendor-Zahlen sind interpoliert/UNVERIFIED, RESEARCH A4 LOW-confidence). Pre-Warming darf den Call-Start NICHT blockieren (Background-Task). BALANCE: das STARKE Modell (Sonnet) schnell machen, NICHT aufs schwache (Haiku) ausweichen — Haiku bleibt NUR Auto-Spike-Fallback.

<objective>
Aktiviere das Tempo: schichte cache_control auf die in Plan 03 angelegte Block-Struktur (stabiler Prefix gecacht, volatil ungecacht — Cache-Miss-Falle vermieden), wärme den stabilen Prefix beim Call-Start vor (nicht-blockierend), und baue den Circuit-Breaker-Wrapper mit `is_auto_triggered` (Auto→Haiku bei TTFT-Spike, Knopf→immer Sonnet) auf die bestehende `EWB_SONNET_FALLBACK_TTFT_MS`-Infra. Das macht das starke Modell (Sonnet) schnell genug für die natürliche Verkäufer-Pause (Req 9).

Purpose: Qualität (Plan 03) + Tempo (dieser Plan) = Balance (Punkt 25). Sonnet gecacht fällt in die Pause; Slot A überbrückt; Auto weicht nur bei echtem Spike auf Haiku aus.
Output: cache_control-Layering aktiv; Pre-Warming; is_auto_triggered-Breaker; Test-Anruf mit eigener TTFT-Messung + cache_read-Beleg.

KEINE build_answer_context-Neubau (Plan 03). KEIN Slot-B-Dedup (Plan 05). Slot A unverändert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen. STATE/ROADMAP hand-editieren.

**Verify = Production only:** `bash deploy.sh production` + **Test-Anruf als Test-User auf getnerve.app** + `inspect.sh logs` für [EWB-TTFT] + cache_read. KEIN lokales pytest als Acceptance.

**Context7 (Pflicht für SDK-Caching):** Anthropic Python SDK system-Block-Liste mit cache_control — verifiziert (RESEARCH §Standard Stack + Context7 /anthropics/anthropic-sdk-python): `system=[{"type":"text","text":...,"cache_control":{"type":"ephemeral","ttl":"1h"|"5m"}}, ...]`. ttl "1h" fürs konstante Gerüst, "5m" (default) fürs Profil, KEIN cache_control auf dem volatilen Block. cache_read_input_tokens im usage-Objekt belegt den Treffer.

**UTF-8-Regel:** Code-Identifier ASCII; Prompt-Text echte Umlaute.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md

<interfaces>
<!-- Context7-verifiziert (/anthropics/anthropic-sdk-python) + Bestand (RESEARCH). -->

cache_control-Layering (Plan 04 setzt die Keys auf die Plan-03-Block-Liste):
  system = [
    {"type":"text","text": PARADIGMA_GERUEST,  "cache_control":{"type":"ephemeral","ttl":"1h"}},  # stabil, konstant
    {"type":"text","text": PROFIL_STABLE,       "cache_control":{"type":"ephemeral"}},             # stabil, 5m default
    {"type":"text","text": GROUNDING_FAQ,       "cache_control":{"type":"ephemeral"}},             # stabil
    {"type":"text","text": VOLATIL_BRIEFING_LEAD_INTENT_ECHO},                                      # KEIN cache_control
  ]
  # max 4 cache_control-Breakpoints (API-Limit) — wir nutzen 3 (volatil ungecacht).
  # Treffer-Beleg: response.usage.cache_read_input_tokens > 0 nach dem 2. Call.

Bestehende TTFT-Breaker-Infra (claude_service.py:18-19, :592-604 — erweitern, NICHT neu bauen):
  _ewb_ttft_history (deque), _ewb_fallback_until (timestamp)
  EWB_SONNET_FALLBACK_TTFT_MS (ENV default 1500); 3/5-ueber-Schwelle → fallback_until = now+30s → Haiku
  [EWB-TTFT] {ms} model={model} sid={sid}  (:604 — Beleg-Log)

is_auto_triggered-Routing (Plan 03 reicht das Flag durch):
  if is_auto_triggered and _ewb_fallback_until > now():  model = HAIKU   # Auto weicht bei Spike aus
  else:                                                   model = SONNET # Knopf IMMER Sonnet
  # Knopf (is_auto_triggered=False) ignoriert _ewb_fallback_until — probiert immer Sonnet.

Pre-Warming (live_session Session-Start — Background, nicht-blockierend):
  start_background_task(prewarm_answer_cache, sid, user_id)  # dummy/minimal Sonnet-Call mit stabilem Prefix
  # OQ-3: Andock = Session-Start. Latenz-Vorbehalt: NIE den Call-Start blockieren.
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
> Dieser Plan ändert KEINE DB-Tabelle (reines Prompt-Caching + Modell-Routing + Background-Task). Punkt 21 Skip-OK für DB-Schicht — ABER der Cache-State (RAM) + Breaker-State (RAM) ist Runtime-State (Punkt 14 Race-Audit unten Pflicht).

### Cross-Layer-Konsistenz-Tabelle (Runtime-State, kein DB)
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `_ewb_ttft_history` / `_ewb_fallback_until` | Breaker liest/schreibt (thread-safe?) | RAM module-global (Bestand) | ⬜ Task 2 Race-Audit |
| Prompt-Cache-Treffer | Anthropic-seitig (ephemeral 5m/1h) | Vendor-Cache (kein lokaler State) | ⬜ Task 3 cache_read>0 |
| Pre-Warm-Background-Task | live_session Session-Start | RAM (Background-Thread) | ⬜ Task 1 nicht-blockierend |
| method_pack-Cache (Plan 03) | load_method_pack per-user | RAM (Bestand Plan 03) | ✓ Plan 03 |

## Verbindungs-Karte (Punkt 22)
- **cache_control-Stelle** lebt EINMAL (:562/:716, grep) → Plan 03 hat sie auf Block-Liste-Durchreichen umgestellt; Plan 04 setzt die Keys (stabil) + lässt volatil ungecacht. Kein neuer Cache-Manager (Don't-Hand-Roll: Vendor-Caching).
- **TTFT-Breaker** lebt (_ewb_ttft_history/_ewb_fallback_until :18-19, grep) → erweitern um is_auto_triggered-Routing (Bestand, kein Neubau).
- **Pre-Warming** ist neu → live_session Session-Start, start_background_task (Bestand-Muster wie streame_auto_variante-Spawn).
- **Sonnet ist NICHT neu** (Pitfall 6): config.py:49-69 alle Sonnet seit langem; André-Freigabe 2026-06-12. Req 9 = Schichtung + Breaker-Verfeinerung, KEINE Modell-Umstellung.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache_control-Layering aktivieren + Pre-Warming (build_answer_context-Bloecke, live_session Session-Start)</name>
  <files>services/prompt_pipeline.py, services/live_session.py</files>
  <read_first>
    - services/prompt_pipeline.py (build_answer_context aus Plan 03 — die 4-Block-Liste; HIER cache_control auf Block 1-3 setzen, Block 4 NICHT)
    - services/claude_service.py:561-564, :716-717 (cache-Huelle, die die Liste durchreicht)
    - services/live_session.py (Session-Start-Stelle + start_background_task-Muster)
    - RESEARCH §Standard Stack (Context7-Block-Liste) + §Open Questions OQ-3 (Pre-Warming)
  </read_first>
  <behavior>
    - build_answer_context-Block 1 (Paradigma-Gerüst) trägt cache_control ttl="1h"; Block 2 (Profil-stabil) + Block 3 (Grounding) tragen cache_control (5m default); Block 4 (volatil) trägt KEIN cache_control
    - max 4 Breakpoints eingehalten (3 genutzt)
    - prewarm_answer_cache(sid, user_id) läuft als Background-Task, blockiert den Session-Start NICHT, schreibt KEINEN State der den Live-Loop blockiert
    - fail-open bleibt (Plan 03): Caching-Fehler → ungecachter Fallback, kein raise
  </behavior>
  <action>
    1. **cache_control-Layering (prompt_pipeline.py build_answer_context):** Setze auf die in Plan 03 gebaute Block-Liste die cache_control-Keys: Block 1 `{"type":"ephemeral","ttl":"1h"}` (Paradigma-Gerüst konstant), Block 2 + Block 3 `{"type":"ephemeral"}` (5m default, Profil/Grounding), Block 4 (Briefing/Lead/Intent/Echo) **KEIN cache_control** (Cache-Miss-Falle vermeiden, Req 9 Constraint). Nur >= _CACHE_MIN_CHARS (4096) cachen (Bestand-Guard). Context7-Syntax verbatim.
    2. **Pre-Warming (live_session Session-Start):** `prewarm_answer_cache(sid, user_id)` als Background-Task (`start_background_task`, Bestand-Muster) beim Session-Start: ein minimaler Sonnet-Call mit NUR dem stabilen Prefix (Block 1-3), damit der Vendor-Cache warm ist, wenn der erste echte Einwand kommt. **NIE den Call-Start blockieren** (Background, try/except → still). OQ-3: Andock = Session-Start-Stelle in live_session.
    Test `tests/test_answer_cache_layering.py` (pure-logic):
    - test_stable_blocks_have_cache_control: Block 1-3 tragen cache_control; Block 1 ttl=="1h".
    - test_volatile_block_no_cache_control: Block 4 (Briefing/Lead) trägt KEIN cache_control (Cache-Miss-Schutz).
    - test_max_four_breakpoints: <= 4 cache_control-Keys.
    - test_prewarm_non_blocking: prewarm wirft nicht / läuft im Background (monkeypatched Anthropic-Call, kein Block).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_answer_cache_layering.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cache_control" services/prompt_pipeline.py` deckt build_answer_context-Block 1-3 ab (>=3 in der Block-Liste)
    - `grep -E "ttl.*1h" services/prompt_pipeline.py` == vorhanden (Paradigma-Gerüst 1h)
    - test_stable_blocks_have_cache_control + test_volatile_block_no_cache_control + test_max_four_breakpoints + test_prewarm_non_blocking grün
    - `grep -c "prewarm" services/live_session.py` >= 1; Pre-Warming als Background-Task (start_background_task)
    - Block 4 (volatil) hat KEIN cache_control (test_volatile_block_no_cache_control)
  </acceptance_criteria>
  <done>cache_control-Layering aktiv (stabil cached/volatil ungecacht), Pre-Warming nicht-blockierend, fail-open erhalten.</done>
</task>

<task type="auto">
  <name>Task 2: Circuit-Breaker-Wrapper mit is_auto_triggered (Punkt 14 Race-Audit Pflicht)</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py:18-22 (_ewb_ttft_history/_ewb_fallback_until Bestand) + :569-604 (Breaker-Heuristik 3/5, [EWB-TTFT])
    - services/claude_service.py (streame_auto_variante is_auto_triggered aus Plan 03 + streame_manual_ewb_variante is_auto_triggered=False)
    - config.py:49-69 (Sonnet/Haiku-Modelle) + EWB_SONNET_FALLBACK_TTFT_MS
  </read_first>
  <action>
    **Punkt-14-Control-Flow + Race-Audit (eingebettet, Pflicht — Breaker-State ist module-global RAM):**
    - Wer liest/schreibt _ewb_fallback_until / _ewb_ttft_history? Auto-Pfad (analyse_loop-Thread) + Manual-Pfad (Button-Thread) gleichzeitig. → Lese/Schreibe unter dem bestehenden Lock (oder atomar; prüfen ob _ewb_ttft_history thread-safe — deque.append ist atomar, aber das 3/5-Read+fallback_until-Write braucht Konsistenz). Falls kein Lock: minimaler Lock um die Entscheidung. RLock-Frage prüfen (hält der Aufrufer schon einen Lock?).
    - Race: zwei Auto-Calls lesen gleichzeitig fallback_until → beide downgraden konsistent (idempotent, kein Schaden). Button-Thread ignoriert fallback_until → liest ihn gar nicht (kein Race auf dem Knopf-Pfad).

    **Routing:** Vor dem messages.stream-Call:
    ```
    if is_auto_triggered and _ewb_fallback_until > _now():
        model = config.HAIKU_MODEL   # Auto weicht bei aktivem Spike-Fenster aus
        _log("[EWB-BREAKER] auto-downgrade→haiku sid=%s", sid)
    else:
        model = config.SONNET_MODEL  # Knopf IMMER Sonnet (is_auto_triggered=False); Auto ohne Spike auch
    ```
    - Die bestehende 3/5-über-Schwelle-Heuristik (:592-604) füttert _ewb_fallback_until weiter (Bestand). is_auto_triggered entscheidet NUR, ob das Fenster honoriert wird.
    - [EWB-TTFT]-Log (:604) bleibt + ergänze ein [EWB-BREAKER]-Log, das model + is_auto_triggered + sid zeigt (Beleg Knopf→Sonnet, Auto-Spike→Haiku).
    - Manual-Pfad (streame_manual_ewb_variante, is_auto_triggered=False): probiert IMMER Sonnet — kein fallback_until-Read.
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_auto_triggered" services/claude_service.py` deckt das Breaker-Routing ab (Auto vs Knopf)
    - `grep -E "_ewb_fallback_until" services/claude_service.py` zeigt: nur Auto-Pfad liest es fürs Routing (Knopf ignoriert es)
    - `grep "\[EWB-BREAKER\]" services/claude_service.py` >= 1 (Beleg-Log model+is_auto_triggered)
    - Race-Audit in SUMMARY dokumentiert (Lock-Haltung Breaker-State, RLock-Frage, idempotente Downgrade)
    - Manual-Pfad routet IMMER auf Sonnet (kein fallback_until-Read; manuelle Sicht)
  </acceptance_criteria>
  <done>Circuit-Breaker-Wrapper mit is_auto_triggered (Auto→Haiku bei Spike, Knopf→Sonnet), Race-Audit gemacht, Beleg-Logging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Test-Anruf W2 (Tempo) — eigene TTFT-Messung + cache_read + Breaker-Beleg (D-05/Req 9 Pflicht)</name>
  <what-built>
    cache_control-Layering aktiv (stabil cached, volatil ungecacht); Pre-Warming beim Call-Start; Circuit-Breaker mit is_auto_triggered (Auto→Haiku bei Spike, Knopf→Sonnet). Deployed auf Production.
  </what-built>
  <how-to-verify>
    1. getnerve.app als Test-User, Live-Call (cold_call). Mehrere Einwände, damit Pre-Warm + Cache-Treffer greifen.
    2. **Eigene TTFT-Messung (Pflicht VOR Festzurren, Req 9 Constraint):** Claudian zieht `inspect.sh logs` → `[EWB-TTFT] <ms> model=<sonnet> sid=...`-Zeilen. Sonnet GECACHT sollte das Budget treffen (~0,5-0,9s Ziel — die eigene Zahl notieren, NICHT die interpolierte Vendor-Zahl annehmen). Wenn deutlich über Budget → umdesignen, NICHT „später optimieren" (Punkt 25 Dealbreaker).
    3. **cache_read-Beleg:** in den Logs cache_read_input_tokens > 0 nach dem 2. Call (Cache greift). Wenn 0 trotz mehrerer Calls → Cache-Miss-Falle (volatiles Datum im Prefix?) → Block-Split prüfen.
    4. **Breaker-Beleg:** Knopf-Pfad → `[EWB-BREAKER] ...→sonnet` (Knopf immer Sonnet). Auto-Pfad bei künstlichem TTFT-Spike (oder im Log-Verlauf) → `[EWB-BREAKER] auto-downgrade→haiku`.
    5. Slot A weiter <1s lokal; Antwort streamt zügig in die Pause.
  </how-to-verify>
  <resume-signal>Tippe "bestätigt" wenn: eigene Sonnet-gecacht-TTFT im Budget + cache_read>0 + Knopf→Sonnet/Auto-Spike→Haiku belegt. Sonst: Abweichung beschreiben (Cache-Miss / TTFT über Budget → Claude redesignt).</resume-signal>
  <acceptance_criteria>
    - Eigene [EWB-TTFT]-Messung dokumentiert (SUMMARY): Sonnet gecacht im Latenz-Budget (~0,5-0,9s Ziel) — eigene Zahl, nicht interpoliert
    - cache_read_input_tokens > 0 nach 2. Call (Log-Beleg) — Cache-Layering greift, kein flüchtiges Datum im Prefix
    - Log-Beleg: Knopf → Sonnet; Auto bei TTFT-Spike → Haiku ([EWB-BREAKER])
    - Slot A <1s lokal (unverändert); KI-Antwort streamt in die Pause
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| volatiles Datum (Briefing/Lead) → Cache-Prefix | Cache-Miss-Falle: ungecacht halten, sonst Invalidierung + TTFT-Spike |
| Breaker-State (RAM, module-global) → Auto+Knopf-Threads | Race auf _ewb_fallback_until |
| Pre-Warm-Background-Task → Call-Start | darf den Live-Call-Start nicht blockieren |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3-04-01 | D (DoS / Latenz) | volatiles Briefing/Lead im Cache-Prefix → Cache-Miss bei jedem Wechsel → TTFT-Spike → Breaker feuert dauernd | mitigate | Block 4 trägt KEIN cache_control (test_volatile_block_no_cache_control); cache_read>0-Live-Beleg. |
| T-TAXO3-04-02 | T (Race) | gleichzeitiger Auto+Knopf-Zugriff auf _ewb_fallback_until → inkonsistentes Routing | mitigate | Race-Audit (Task 2): Knopf liest fallback_until gar nicht; Auto-Downgrade idempotent; Lock um die Entscheidung wenn nötig. |
| T-TAXO3-04-03 | D (DoS) | Pre-Warm-Task blockiert/verlangsamt den Call-Start | mitigate | Background-Task (start_background_task), try/except still, nie synchron im Start-Pfad (test_prewarm_non_blocking). |
| T-TAXO3-04-04 | D (Latenz) | Sonnet gecacht trifft das Budget NICHT (interpolierte Zahl war falsch) | mitigate | Eigene TTFT-Messung Pflicht VOR Festzurren (Checkpoint 3); über Budget → umdesignen statt „später". |
| T-TAXO3-04-05 | T | Auto-Pfad weicht NIE auf Haiku aus (Spike-Fenster ignoriert) → langsame Auto-Antworten | mitigate | is_auto_triggered honoriert _ewb_fallback_until; [EWB-BREAKER]-Log belegt den Downgrade. |
</threat_model>

<verification>
- pytest test_answer_cache_layering.py grün (stabil cached / volatil ungecacht / <=4 Breakpoints / prewarm non-blocking).
- `grep cache_control services/prompt_pipeline.py` deckt Block 1-3 ab, ttl=1h auf Block 1; Block 4 ohne.
- `grep is_auto_triggered services/claude_service.py` deckt Breaker-Routing ab; [EWB-BREAKER]-Log.
- Test-Anruf (D-05/Req 9): eigene Sonnet-gecacht-TTFT im Budget; cache_read>0; Knopf→Sonnet/Auto-Spike→Haiku.
- build_answer_context NICHT grob neu gebaut (Anti-Abrieb D-06) — nur cache_control-Keys + Pre-Warm + Breaker additiv.
- Race-Audit Breaker-State im SUMMARY.
</verification>

<success_criteria>
- Caching geschichtet (stabil gecacht, volatil ungecacht, kein flüchtiges Datum im Prefix); Pre-Warming nicht-blockierend; is_auto_triggered-Breaker (Auto→Haiku/Knopf→Sonnet).
- Eigene TTFT-Messung belegt Sonnet gecacht im Budget (nicht interpoliert); cache_read>0.
- Anti-Abrieb D-06 gewahrt (build_answer_context nicht neu gebaut).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-04-SUMMARY.md` (inkl. pytest-Beleg, eigene [EWB-TTFT]-Sonnet-gecacht-Zahl + Budget-Abgleich, cache_read>0-Log, [EWB-BREAKER]-Beleg Knopf→Sonnet/Auto-Spike→Haiku, Race-Audit Breaker-State).
</output>
