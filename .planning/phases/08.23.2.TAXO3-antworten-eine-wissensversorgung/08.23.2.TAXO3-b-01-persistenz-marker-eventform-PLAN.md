---
phase: 08.23.2.TAXO3
slice: b
plan: b-01
type: execute
wave: 1
komplexitaet: "🔴 (Live-Antwort-Pfad-Vertrag + intent_event-Schreiber + Schild)"
depends_on: []
files_modified:
  - services/intent_event_writer.py
  - database/models.py
  - alembic/versions/00XX_taxo3b_mirrored_marker_schild.py
  - tests/test_taxo3b_mirrored_marker.py
autonomous: true
decision_ids: [RV-1, RV-3, D-13, D-14, D-15, D-16, D-08, D-09]
user_setup: []

must_haves:
  truths:
    - "Der Continuation-Knopf-Datenquelle ist BEWIESEN per-SID RAM (_session_state[sid]['analysiert_bisher']), NICHT die DB — Persistenz-Schicht-Verifikation belegt Schreib-Zeitpunkt (live/inkrementell) vs. transcript_segments (batch am Call-Ende)."
    - "Ein vom Berater gespiegeltes Kunden-Echo laesst sich als typisierter, versionierter payload_jsonb-Eintrag (is_mirrored_echo=true + mirror_schema_version) in intent_event ablegen — OHNE Migration einer neuen Spalte."
    - "Der Gespiegelt-Marker traegt eine gesenkte confidence und bleibt speaker_role='berater' (Single-Speaker cold_call, D-16) — nie ein harter Kunden-Fakt."
    - "Das payload_jsonb-Feld ist queryable + trennbar (nicht in Prosa vergraben) — ein SQL-Filter WHERE payload_jsonb->>'is_mirrored_echo'='true' findet die Zeilen."
    - "Das Tabellen-Schild von intent_event nennt services/deepgram_service.py als den neuen Continuation/Mirror-Schreiber (Punkt 23 Aktualitaet) — festgezurrt, keine Konditionale."
  artifacts:
    - path: "services/intent_event_writer.py"
      provides: "Typisierter Mirror-Payload-Vertrag (build_mirror_payload) + mirror_schema_version-Konstante"
      contains: "is_mirrored_echo"
    - path: "database/models.py"
      provides: "Aktualisiertes intent_event-Tabellen-Schild + payload_jsonb-Spalten-Kommentar (nennt is_mirrored_echo/mirror_schema_version + deepgram_service.py als Continuation/Mirror-Schreiber)"
      contains: "is_mirrored_echo"
    - path: "alembic/versions/00XX_taxo3b_mirrored_marker_schild.py"
      provides: "COMMENT-ONLY Migration (Schild-Update, KEINE Spalte)"
      contains: "COMMENT ON TABLE"
  key_links:
    - from: "services/deepgram_service.py handle_freie_antwort / Continuation-Mirror-Emit (b-03)"
      to: "intent_event.payload_jsonb"
      via: "emit_intent_event(extra_payload=build_mirror_payload(...))"
      pattern: "extra_payload="
    - from: "Continuation-Knopf-Backend (b-03)"
      to: "_session_state[sid]['analysiert_bisher']"
      via: "RAM-Read zum Klick-Zeitpunkt (KEIN DB-Read)"
      pattern: "analysiert_bisher"
---

<objective>
Die Fundament-Scheibe: BEWEISE die Live-Datenquelle des Continuation-Knopfs (per-SID RAM, RV-1)
und zurre die Ereignis-Form fuer den einen nicht-nachholbaren Neuzugang fest — den Gespiegelt-Marker
(D-13/D-14) — als typisierten, versionierten payload_jsonb-Vertrag OHNE Migration (RV-3).

Purpose: Der zentrale Konsens-Review-Befund (HIGH-1 / Punkt 26): der Knopf MUSS aus per-SID RAM lesen,
NIE aus transcript_segments (batch-written ~25-58s spaeter am Call-Ende -> DB-Read greift ins Leere).
Diese Naht wird HIER bewiesen, BEVOR b-03 den Knopf baut. Zusaetzlich wird die Ablage-Form
(Marker + gesenkte confidence + schema_version) festgezurrt, weil die Form der eine Neuzugang ist,
den man nicht nachtraeglich rekonstruieren kann (D-13).

Output: build_mirror_payload() (typisierter Payload-Vertrag) + mirror_schema_version in
intent_event_writer.py; aktualisiertes intent_event-Schild (models.py + COMMENT-only Migration);
Test der den Payload-Vertrag + die Queryability beweist; Verlustfrei-Check des PreCall-Briefings (D-15).

Dieser Plan schreibt NOCH KEINEN Mirror-Event live (das macht b-03 beim Continuation-Emit in
deepgram_service.py) — er liefert den VERTRAG + den Beweis, dass b-03 ihn aus RAM speisen kann, UND
setzt das Schild schon vertrags-korrekt (nennt deepgram_service.py als Mirror-Schreiber, Punkt 23).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-b-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-b-REVIEWS.md

<interfaces>
<!-- Aus dem Repo verifiziert 2026-07-02 — direkt nutzen, keine Codebase-Exploration noetig. -->

services/intent_event_writer.py — emit_intent_event(...) unterstuetzt bereits extra_payload:
```python
def emit_intent_event(*, session_id, mode, intent_type, source, speaker_role, speaker_id,
                      phase=None, confidence=None, user_id=None, org_id=None, call_id,
                      interaction_id=None, inference_basis=None, abstained=False,
                      is_simulation=False, origin_type='human_live', triggering_text=None,
                      extra_payload=None) -> int:
    ...
    payload = { 'source':..., 'taxonomy_version': TAXONOMY_VERSION, 'speaker_role': speaker_role,
                'speaker_id': speaker_id, 'triggering_text': triggering_text, ... }
    if extra_payload:
        for _k, _v in extra_payload.items():
            payload[_k] = _v          # <- HIER dockt der Mirror-Marker an, KEINE Migration
    ...
    payload_jsonb=payload
```

database/models.py:777 IntentEvent — payload_jsonb (models.py:795) ist nullable=False, default=dict.
Der Spalten-Kommentar listet bereits: source, inference_basis, taxonomy_version, abstained,
speaker_role, speaker_id, is_simulation, origin_type, triggering_text, ... (Reserve-Feld,
disziplinierter Vertrag). __table_args__ comment (models.py:797) = das Tabellen-Schild; nennt heute
Schreiber: einwand_keyword_matcher.py, claude_service.py, deepgram_service.py (EWB-Knopf-Emit),
slow_lane.py. -> deepgram_service.py steht schon drin; b-03 legt den Mirror-Emit ebenfalls dort ab
(handle_freie_antwort), daher wird der Schreiber-Eintrag HIER um den Continuation/Mirror-Zusatz erweitert.

Bestehender EWB-Knopf-Emit (deepgram_service.py:919-927) — Vorbild fuer den Mirror-Emit in b-03:
```python
emit_intent_event(session_id=_sid, mode=_btn_mode, intent_type='echter_einwand',
    phase=_btn_phase, source='ui_asserted', inference_basis='ui_button',
    confidence=1.0, speaker_role='kunde', speaker_id='local',   # <- Knopf-Ausnahme
    user_id=_btn_uid, org_id=_btn_oid, interaction_id=_btn_iid,
    call_id=_btn_cid, triggering_text=_btn_trig)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistenz-Schicht-Verifikation + RV-1 RAM-Quellen-Beweis (Punkt 21/22/26)</name>
  <read_first>
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema intent_event' (Live-Schema, payload_jsonb-Spalte, KEINE neue Spalte noetig)
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments' (belegt: NICHT die Continuation-Quelle)
    - ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schilder intent_event' (aktuelles Schild)
    - services/claude_service.py:930-943 (analysiert_bisher wird LIVE/inkrementell im analyse_loop appended, per-SID RAM, _existing[-200:])
    - services/deepgram_service.py:987 (kontext = analysiert_bisher[-20:] — der bestehende Knopf liest schon RAM, KEIN DB-Read)
    - services/live_session.py:47,419-420 (analysiert_bisher per-SID init) + :872,893 (global-Reset-Pfad = Legacy)
  </read_first>
  <action>
Erstelle im Plan-Autor-Output NICHT hier — sondern schreibe die Pflicht-Sektion `## 5. Persistenz-Schicht-Verifikation`
in die SUMMARY dieses Plans (bzw. in einen Kommentar-Block am Kopf von intent_event_writer.py, falls
knapper). Inhalt (Punkt 21 + Punkt 26):

1. Angefasste Tabelle: `intent_event` (SCHREIBEN via payload_jsonb-Vertrag, KEINE neue Spalte).
2. inspect.sh schema intent_event Output VERBATIM einfuegen (Beleg: payload_jsonb existiert, nullable=False).
3. Cross-Layer-Konsistenz-Tabelle mit der PFLICHT-Spalte "Schreib-Zeitpunkt + kann Leser vorauslaufen?":

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann Leser vorauslaufen? |
|---|---|---|---|---|
| gespiegeltes Kunden-Echo (Continuation-Eingang) | b-03 liest zum Klick | `_session_state[sid]['analysiert_bisher']` RAM | LIVE/inkrementell (claude_service.py:942, analyse_loop, per neuem Segment) | NEIN — RAM ist zum Klick befuellt; leer nur bei Call-Start (dann Fallback-Text) |
| gespiegeltes Kunden-Echo (Marker-Ablage) | b-03 schreibt beim Emit | `intent_event.payload_jsonb` (is_mirrored_echo) | LIVE beim Klick (insert-only) | n/a (Schreiber, kein Leser) |
| ❌ ANTI-QUELLE transcript_segments | NICHT nutzen | DB | BATCH am Call-Ende (~25-58s spaeter, alle created_at identisch) | JA — greift ins Leere → VERBOTEN als Continuation-Quelle |

4. FAZIT festhalten: Continuation-Eingang = per-SID RAM `analysiert_bisher[-N:]` (exakt der bestehende
   Knopf-Pfad deepgram_service.py:987). KEIN neuer DB-Read. Damit ist RV-1 / HIGH-1 / Punkt 26 aufgeloest,
   BEVOR b-03 baut.

Diese Task produziert KEINEN Code-Change — sie ist der Beweis-Schritt (Artefakt = die Sektion in der SUMMARY).
  </action>
  <acceptance_criteria>
    - Die SUMMARY dieses Plans enthaelt eine `## 5. Persistenz-Schicht-Verifikation`-Sektion mit
      VERBATIM inspect.sh-Output (nicht zusammengefasst) fuer intent_event.
    - Die Cross-Layer-Tabelle enthaelt die Spalte "Schreib-Zeitpunkt" + "kann Leser vorauslaufen?"
      fuer jedes gelesene/geschriebene Datum (Punkt 26).
    - transcript_segments ist explizit als ANTI-Quelle (batch, VERBOTEN) markiert.
    - grep-Beleg zitiert: `grep -n "analysiert_bisher" services/claude_service.py` zeigt den
      Live-Append (:942) UND `grep -n "analysiert_bisher" services/deepgram_service.py` zeigt den
      bestehenden RAM-Read (:987).
  </acceptance_criteria>
  <done>Die RAM-Quelle des Continuation-Knopfs ist mit inspect.sh + grep bewiesen; transcript_segments als batch-Anti-Quelle ausgeschlossen; b-03 kann auf dem Beweis bauen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typisierter Mirror-Payload-Vertrag + mirror_schema_version (RV-3, D-13/D-14, D-16, D-09)</name>
  <files>services/intent_event_writer.py, tests/test_taxo3b_mirrored_marker.py</files>
  <read_first>
    - services/intent_event_writer.py:32-118 (emit_intent_event-Signatur + payload-Bau + extra_payload-Merge)
    - services/intent_event_writer.py Kopf (TAXONOMY_VERSION-Konstante, Import-Muster)
    - tests/test_taxo3b_mirrored_marker.py (existiert noch nicht — neu)
  </read_first>
  <behavior>
    - Test 1: build_mirror_payload(mirrored_text='...', lowered_confidence=0.35) gibt ein dict zurueck
      mit is_mirrored_echo=True, mirror_schema_version==CURRENT (int, z.B. 1), mirror_confidence==0.35,
      speaker_role NICHT im Rueckgabe-dict ueberschrieben (bleibt Aufrufer-Sache, D-16 = 'berater').
    - Test 2: emit_intent_event(..., speaker_role='berater', confidence=0.35,
      extra_payload=build_mirror_payload(...)) schreibt eine Zeile, deren payload_jsonb
      is_mirrored_echo=True + mirror_schema_version + speaker_role='berater' enthaelt (D-16:
      gespiegeltes Echo bleibt Berater-Aussage, NIE harter Kunden-Fakt).
    - Test 3 (Queryability, D-14): nach dem Insert findet eine Query
      WHERE payload_jsonb->>'is_mirrored_echo' = 'true' die Zeile (trennbar, nicht in Prosa vergraben).
    - Test 4 (Versionierung): mirror_schema_version ist eine benannte Konstante (MIRROR_SCHEMA_VERSION),
      kein Magic-Literal — build_mirror_payload liest sie.
    - Cleanup: der committende Test raeumt seine intent_event-Rows via cleanup_rows (conftest.py) weg.
  </behavior>
  <action>
Fuege in services/intent_event_writer.py hinzu (Bau-Regel: einfachster tragfaehiger Weg, Punkt 27 —
KEINE neue Spalte, KEINE neue Tabelle, nur ein typisierter Payload-Helfer):

1. Konstante `MIRROR_SCHEMA_VERSION = 1` (mit Doku-Kommentar: neue Mirror-Payload-Felder NUR mit
   Versions-Bump + Doku, D-14 payload-Disziplin).
2. Funktion `build_mirror_payload(*, mirrored_text: str | None, lowered_confidence: float) -> dict`:
   gibt `{'is_mirrored_echo': True, 'mirror_schema_version': MIRROR_SCHEMA_VERSION,
   'mirror_confidence': float(lowered_confidence)}` zurueck. Docstring: "D-13/RV-3 — typisierter,
   versionierter payload_jsonb-Vertrag fuer das vom Berater laut gespiegelte Kunden-Echo. KEINE
   Migration. D-16: der Aufrufer setzt speaker_role='berater' (Single-Speaker cold_call) — dieses
   Echo ist eine BERATER-Aussage, markiert gespiegelt, NIE ein harter Kunden-Fakt (Trainings-
   Vergiftungs-Schutz). D-09: lowered_confidence ist der spaeter tunbare Ask-back-Hebel."
   `mirrored_text` wird NICHT roh in den Marker geschrieben (PII-Schutz, Threat-Model) — es dient
   dem Aufrufer als Beleg-Referenz; wenn spaeter denormalisiert benoetigt, laeuft es durch
   anonymize_output wie triggering_text. Fuer JETZT nur die Marker-Flags im Payload.
3. Der Merge-Pfad (extra_payload) existiert bereits (:116-118) — build_mirror_payload liefert exakt
   die extra_payload-Keys.

KEINE Aenderung an der emit_intent_event-Signatur (der extra_payload-Kanal reicht — Punkt 27).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <!-- Das Prod-Deploy-Gate faehrt tests/test_taxo3b_mirrored_marker.py server-seitig gegen nerve_test-Postgres; bei rot kein Restart (alter Code bleibt live). HART: kein Local-Pytest als Acceptance. -->
  </verify>
  <acceptance_criteria>
    - grep: `grep -n "def build_mirror_payload\|MIRROR_SCHEMA_VERSION" services/intent_event_writer.py`
      zeigt beide (Funktion + Konstante).
    - Das Prod-Deploy-Gate (bash deploy.sh production) ist GRUEN inkl. der 4 neuen Tests
      (Behavior-Assertions gegen echtes Postgres, KEINE source-presence-Checks).
    - inspect.sh belegt nach einem manuellen Test-Insert die Queryability:
      `ssh ... 'bash scripts/inspect.sh sample intent_event 5'` zeigt keine Mirror-Zeilen (Tests
      raeumen auf) — Baseline-Sauberkeit (Cleanup-Regel).
  </acceptance_criteria>
  <done>build_mirror_payload + MIRROR_SCHEMA_VERSION existieren; ein Mirror-Marker ist typisiert, versioniert, queryable, speaker_role='berater'; Tests gruen im Prod-Gate; Rows aufgeraeumt.</done>
</task>

<task type="auto">
  <name>Task 3: Schild-Update (Punkt 23) COMMENT-only Migration + Verlustfrei-Check PreCall-Briefing (D-15)</name>
  <files>database/models.py, alembic/versions/00XX_taxo3b_mirrored_marker_schild.py</files>
  <read_first>
    - database/models.py:795 (payload_jsonb-Spalten-Kommentar — Feld-Liste) + :796-798 (__table_args__ Tabellen-Schild, Schreiber-Liste — deepgram_service.py steht schon drin fuer den EWB-Knopf-Emit)
    - ssh ... 'bash scripts/inspect.sh migrations' (aktuelle Alembic-Head-Revision fuer down_revision)
    - Eine bestehende COMMENT-Migration als Muster (z.B. Migration 0015 SCHILD / das hauseigene op.execute("COMMENT ON ...")-Muster, Punkt 23 Migrations-Stil)
    - CONTEXT D-15: PreCall-Briefing muss sich spaeter als source=manual-Ereignis in DIESELBE Form ueberfuehren lassen (Form-Check jetzt, Bau spaeter)
  </read_first>
  <action>
1. models.py:795 — erweitere den payload_jsonb-Spalten-Kommentar um die neuen Vertrags-Felder:
   `is_mirrored_echo, mirror_schema_version, mirror_confidence` (mit kurzem Zusatz: "TAXO3-b:
   vom Berater gespiegeltes Kunden-Echo, versionierter Marker RV-3, KEINE Migration").
2. models.py:797 __table_args__ Tabellen-Schild — der Mirror-Emit liegt in b-03 in
   `services/deepgram_service.py` (handle_freie_antwort, neben dem bestehenden EWB-Knopf-Emit). Das
   Schild nennt daher deepgram_service.py als Mirror/Continuation-Schreiber (FESTGEZURRT — KEINE
   Konditionale mehr; am b-03-Plan verankert, Punkt 23 Aktualitaets-Pflicht). deepgram_service.py
   steht bereits in der Schreiber-Liste (EWB-Knopf-Emit) -> den vorhandenen Eintrag um den
   Continuation/Mirror-Zusatz ERWEITERN, nicht doppelt listen:
   "...; services/deepgram_service.py (EWB-Knopf-Emit + Continuation/Mirror-Marker-Emit handle_freie_antwort, TAXO3-b)..."
   Das Schild wird HIER schon vertrags-korrekt gesetzt, damit b-03 nur noch den Aufruf ergaenzt und
   das Schild bereits stimmt (b-03 Task 2 prueft die Aktualitaet + zieht bei Abweichung im selben
   Commit nach — Belt-and-Suspenders).
3. Neue Alembic-Migration `00XX_taxo3b_mirrored_marker_schild.py` — REIN COMMENT (RV-3: KEINE
   Spalte, KEINE Schema-Aenderung): `op.execute("COMMENT ON COLUMN intent_event.payload_jsonb IS '...'")`
   + `op.execute("COMMENT ON TABLE intent_event IS '...'")` mit dem aktualisierten Text (1:1 zum
   models.py-comment). down_revision = aktueller Head (aus inspect.sh migrations). downgrade() setzt
   den alten Kommentar zurueck.
4. Verlustfrei-Check D-15 (Form-Check, KEIN Bau): dokumentiere in der SUMMARY, dass das bestehende
   Cold-Call-PreCall-Briefing (services/live_session.py _briefing, freier Text) sich spaeter als
   intent_event mit source='manual' + payload_jsonb (speaker_role, triggering_text=Briefing-Kernaussage,
   is_mirrored_echo=False) in DIESELBE Form ueberfuehren laesst — KEIN Feld fehlt in der aktuellen
   Ereignis-Form. Falls doch ein Feld fehlte -> hier FLAG (dann waere die Form nicht verlustfrei).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
    <!-- Prod-Deploy-Gate faehrt die Migration (Alembic upgrade head) + den SCHILD-Guard (test_schild_guard.py gegen pg_description). Bei rotem Schild-Guard kein Restart. -->
  </verify>
  <acceptance_criteria>
    - ssh ... 'bash scripts/inspect.sh schilder intent_event' zeigt NACH Deploy den neuen
      payload_jsonb-Kommentar (mit is_mirrored_echo/mirror_schema_version) + deepgram_service.py als
      Continuation/Mirror-Schreiber im Tabellen-Schild.
    - ssh ... 'bash scripts/inspect.sh schema intent_event' zeigt UNVERAENDERTE Spaltenliste (Beweis:
      KEINE neue Spalte, nur COMMENT — RV-3).
    - Der SCHILD-Guard (test_schild_guard.py) ist im Prod-Gate GRUEN.
    - Die SUMMARY enthaelt den D-15-Verlustfrei-Check (Briefing -> source=manual-Form, kein fehlendes Feld).
  </acceptance_criteria>
  <done>Schild aktuell (nennt neue Felder + deepgram_service.py als Mirror-Schreiber), COMMENT-only Migration deployed, KEINE Spalte, Schild-Guard gruen, D-15 Form-Check dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Berater-Sprache (STT) -> analysiert_bisher RAM | Gespiegeltes Kunden-Echo ist untrusted Text (kann PII / Injection-Muster tragen) |
| build_mirror_payload -> intent_event.payload_jsonb | Marker-Flags gehen in die Single-Source-Ereignis-Tabelle (Trainings-Fundament) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3b-01 | Tampering (Trainings-Vergiftung) | Mirror-Marker speaker_role | mitigate | D-16: build_mirror_payload-Docstring + Test 2 erzwingen speaker_role='berater'; das gespiegelte Echo ist NIE ein harter Kunden-Fakt. Test beweist es. |
| T-TAXO3b-02 | Information Disclosure (PII) | mirrored_text im Payload | mitigate | build_mirror_payload schreibt den Roh-Echo-Text NICHT in den Marker (nur Flags + gesenkte confidence). Falls spaeter denormalisiert: durch anonymize_output wie triggering_text (bestehender Vertrag). |
| T-TAXO3b-03 | Repudiation/Integritaet | mirror_schema_version | mitigate | Versionierte Konstante (D-14) — neue Felder nur mit Bump; Marker bleibt trennbar/queryable, nicht in Prosa. |
| T-TAXO3b-04 | Tampering | payload_jsonb-Disziplin | accept | payload_jsonb ist Reserve-Feld; Risiko = un-disziplinierte Felder. Akzeptiert durch typisierten build_mirror_payload-Vertrag (kein freies Dict-Schreiben im Live-Pfad). |
</threat_model>

<verification>
- Prod-Deploy-Gate GRUEN inkl. tests/test_taxo3b_mirrored_marker.py + SCHILD-Guard.
- inspect.sh schema intent_event: KEINE neue Spalte (RV-3 bewiesen).
- inspect.sh schilder intent_event: Schild nennt neue Felder + deepgram_service.py als Continuation/Mirror-Schreiber.
- Persistenz-Schicht-Verifikation-Sektion in SUMMARY: RAM-Quelle bewiesen, transcript_segments als batch-Anti-Quelle ausgeschlossen.
</verification>

<success_criteria>
- build_mirror_payload + MIRROR_SCHEMA_VERSION existieren, typisiert, versioniert (RV-3/D-13/D-14).
- Mirror-Marker ist queryable (WHERE payload_jsonb->>'is_mirrored_echo') + speaker_role='berater' (D-16).
- RV-1 RAM-Quelle des Continuation-Knopfs BEWIESEN (Punkt 26), transcript_segments ausgeschlossen.
- Schild aktuell (nennt deepgram_service.py als Mirror-Schreiber, festgezurrt) + COMMENT-only Migration (Punkt 23, RV-3 keine Spalte).
- D-15 Verlustfrei-Check dokumentiert (kein fehlendes Feld).
- Rows aufgeraeumt (Cleanup-Regel).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-b-01-SUMMARY.md`
(inkl. der Pflicht-Sektion `## 5. Persistenz-Schicht-Verifikation` + D-15-Form-Check).
Multi-Segment-Gotcha: Pfad handhardcoden, STATE/ROADMAP handeditieren, plain git commit.
</output>
</output>
