---
phase: 08.23.2.TAXO3
plan: 05
type: execute
wave: 5
complexity: 🔴
depends_on: [03]
files_modified:
  - services/claude_service.py
  - static/pip-launcher.js
  - tests/test_slot_b_dedup.py
autonomous: false
requirements: ["SPEC Req 8"]
risiko: riskant
test_anruf: true
user_setup: []

must_haves:
  truths:
    - "Slot B (pip-slot-1) bekommt fuer dieselbe Kunden-Aussage genau EINE KI-Variante — deterministisch dedupliziert per interaction_id (Moment-Klammer aus TAXO1), NICHT per roher line_id (fragmentiert per Deepgram-Endpointing) und NICHT per nicht-deterministischem slot1_variant_busy_until-Mutex"
    - "Die Stichwort-Doppelung in Slot B (keyword_einwand_match-Variante) ist gestrichen — die KI-Antwort (qa_slot1) gewinnt unten (D-04); die schnelle Stichwort-Antwort bleibt OBEN in Slot A unveraendert (Slot A NICHT gekillt)"
    - "FE (pip-launcher.js) dedupliziert Slot-B-Render per interaction_id (kein Doppel-Render bei langsamem Sprecher)"
    - "Der bestehende slot1_variant_busy_until-Mutex ist durch den interaction_id-Dedup ERSETZT (D-04), nicht doppelt belassen"
    - "Test-Anruf: langsamer Einwand (Deepgram splittet in 2 line_ids) → genau 1 Slot-B-Variante; Slot A feuert weiter sofort + unabhaengig"
  artifacts:
    - path: "services/claude_service.py"
      provides: "Slot-B-Emit dedupliziert per interaction_id (eine Variante pro Moment); keyword-Doppelung aus Slot B raus; Mutex ersetzt"
      contains: "interaction_id"
    - path: "static/pip-launcher.js"
      provides: "FE-Render-Dedup Slot B per interaction_id (kein Doppel-Render)"
      contains: "interaction_id"
    - path: "tests/test_slot_b_dedup.py"
      provides: "Pure-logic: zwei Emits gleicher interaction_id → eine Variante; verschiedene interaction_id → zwei; Slot A unabhaengig"
  key_links:
    - from: "Slot-B-Emit (qa_slot1 + ex-keyword_einwand_match)"
      to: "Dedup per interaction_id (TAXO1 get_or_create_interaction_id-Quelle)"
      via: "interaction_id-Schluessel statt slot1_variant_busy_until-Mutex"
      pattern: "interaction_id"
    - from: "Backend Slot-B-Emit"
      to: "FE pip-launcher.js Render-Dedup"
      via: "interaction_id im Emit-Payload durchgereicht"
      pattern: "interaction_id"
    - from: "Slot A (keyword-Sofort-Antwort)"
      to: "bleibt unveraendert lokal (D-03/Bau-Regel 1)"
      via: "NICHT angefasst"
      pattern: "pip-slot-0"
---

> 🛑 **PRE-BUILD-GATE IL-1/OQ-2 (TAXO1-Interlock — BLOCKIEREND, RESEARCH §Interlock + §Open Questions):** Dieser Plan baut den Dedup-Mechanismus per `interaction_id`. Die Quelle der interaction_id ist in TAXO1 NOCH NICHT festgezurrt (offene Drift):
> - TAXO1-04 verankert die Moment-Klammer ursprünglich am **module-globalen** state-Dict; der I-4-Blocker (TAXO1-04-PLAN:62-72) zeigt: das ist falsch (Anruf-Vermischung B-1) → die Klammer wandert **per-SID** nach `_session_state[sid]` und der Anker-Teil **in TAXO1-03**; die Granularität (B-3) ist auf ein **ereignis-getriebenes Einwand-Fenster** entschieden (André), NICHT starr line_id.
> - **TAXO3 darf den Dedup NICHT bauen, bevor (a) der TAXO1-04-Blocker I-4 gefixt ist UND (b) die interaction_id-Quelle (module-global vs per-SID, Granularität) in TAXO1-03/-04 entschieden + deployed ist.** Sonst klammert TAXO3 an die falsche Quelle.
> - **VOR-EXECUTE-RE-GREP-GATE (Pflicht):** `grep -n "get_or_create_interaction_id\|interaction_id\|_session_state_lock\|state_lock\|kw_fired_for_line" services/live_session.py services/claude_service.py services/intent_event_writer.py` — bestätigen, WO die finale interaction_id-Quelle liegt (per-SID `_session_state[sid]['interaction_id']` erwartet nach I-4-Fix). **Plan 03 hat diese Quelle im IL-2-Re-Grep schon ermittelt → aus 08.23.2.TAXO3-03-SUMMARY lesen.** Wenn die Quelle noch module-global / nicht deployed → STOP, dieser Plan kann nicht executen.

> RISIKO-BANNER: **riskant.** Ändert die Slot-B-Schreib-Logik (Doppel-Emit-Fix, Mutex-Ersatz) + FE-Render. → **Test-Anruf PFLICHT (D-05): langsamer Einwand → genau 1 Slot-B-Variante; Slot A feuert weiter.**

> ⚠️ **CROSS-AI-GATE (🔴):** Geplant, nicht sofort ausgeführt. Deploy + Test-Anruf erst nach Cross-AI-Clearance + TAXO1-Interlock-Auflösung.

> **REIHENFOLGE (RESEARCH §Wave-Cut W3-Vorbehalt):** W3 hängt am TAXO1-Interlock. Falls die Execute-Reihenfolge es erzwingt, KANN W3 vor W2 (Plan 04) laufen — aber NUR nach Interlock-Auflösung. depends_on ist [03] (build_answer_context muss stehen), NICHT [04] — Tempo und Dedup sind unabhängig.

> **LATENZ-BUDGET (Punkt 25):** unverändert. Der Dedup ist deterministisch + lokal (kein LLM, kein Netz-Call) → kein Latenz-Aufschlag. Slot A bleibt <1s lokal (NICHT anfassen).

<objective>
Fixe das D3-Doppel-Emit: in Slot B (pip-slot-1) schreiben heute Stichwort-Pfad (`keyword_einwand_match`) UND QA-Pipeline (`qa_slot1`) für dieselbe Aussage — der `slot1_variant_busy_until`-Mutex fängt es nur zufällig. Künftig: die KI-Antwort gewinnt unten (D-04), die Stichwort-Doppelung in Slot B wird gestrichen (die Stichwort-Antwort steht bereits OBEN in Slot A), und der Dedup läuft **deterministisch per interaction_id** (Moment-Klammer aus TAXO1 — die EINE-ID-pro-Kundenmoment, robust gegen Deepgram-Fragmentierung). FE dedupliziert den Render. Slot A bleibt unverändert (D-03, Bau-Regel 1).

Purpose: eine Aussage → genau eine Slot-B-Variante, kein Flackern, deterministisch statt Mutex-Zufall (Req 8). Oben Tempo (Stichwort), unten Qualität (KI).
Output: interaction_id-Dedup im Backend-Emit + FE-Render; Mutex ersetzt; keyword-Doppelung aus Slot B raus; Test-Anruf-Beleg.

KEINE Slot-A-Änderung. KEINE build_answer_context-Änderung (Plan 03). KEIN Caching (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Multi-Segment-ID-Gotcha (HART):** Verzeichnis EXAKT `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/`. gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier NICHT aufrufen. STATE/ROADMAP hand-editieren.

**Verify = Production only:** `bash deploy.sh production` + **Test-Anruf als Test-User auf getnerve.app** (langsamer Sprecher-Szenario) + `inspect.sh logs`. KEIN lokales pytest als Acceptance. node --check für pip-launcher.js (Syntax) lokal OK (kein Ausführen).

**UTF-8-Regel:** Code-Identifier/JS-Object-Keys ASCII; user-facing Text echte Umlaute.
</execution_context>

<context>
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-SPEC.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-CONTEXT.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-RESEARCH.md
@.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-03-SUMMARY.md

<interfaces>
<!-- Verifizierte Andock-Punkte (grep file:line, RESEARCH + Session). interaction_id-Quelle: aus Plan-03-SUMMARY (IL-2-Re-Grep) + dem PRE-BUILD-Re-Grep oben. -->

Slot-B-Doppel-Emit (claude_service.py — RESEARCH §Code-Insights, Session bestätigt):
  - _emit_qa_slot1(text) :1323 → emit 'qa_slot1' :1325 (KI-Antwort, gewinnt unten D-04)
  - streame_auto_variante → emit 'pip_token' slot:1 (Auto-Variante, KI)
  - keyword_einwand_match-Pfad schreibt ebenfalls in Slot B → DOPPELUNG (D-04: raus aus Slot B, bleibt in Slot A)
  - Guard heute: slot1_variant_busy_until (:1267/:1275/:1331/:1334 per-SID :1331 + global-Fallback :1334) — Mutex, ERSETZEN

interaction_id-Quelle (TAXO1, nach I-4-Fix per-SID — aus Plan-03-SUMMARY/Re-Grep):
  get_or_create_interaction_id(line_id) → EINE uuid4 pro Kundenmoment (Einwand-Fenster, ereignis-getrieben)
  geteilt von Fast-Keyword + Medium-Haiku + Button derselben Aussage
  Lage: _session_state[sid]['interaction_id'] unter _session_state_lock (per-SID, nach I-4)
  # NICHT roh line_id (fragmentiert per Deepgram-Endpointing, TAXO1-04-B3)

FE Slot B (static/pip-launcher.js — RESEARCH: pip-launcher.js:2488 kein line_id-Dedup):
  Slot B = pip-slot-1; Slot A = pip-slot-0 (NICHT anfassen)
  Render-Dedup: interaction_id im Emit-Payload → FE merkt sich zuletzt gerenderte interaction_id pro Slot-B,
  überschreibt/ignoriert Doppel-Emit gleicher interaction_id (kein neues DOM-Element).
</interfaces>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
> Kein DB-Schema-Änderung (reines Emit-/Render-Dedup + Mutex-Ersatz). interaction_id liegt im RAM per-SID (TAXO1) + optional DB intent_event.interaction_id (TAXO1 Plan 01). Punkt 21 DB-Skip-OK; Runtime-State-Audit (Punkt 14) unten Pflicht.

### Cross-Layer-Konsistenz-Tabelle
| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `interaction_id` (Dedup-Key) | gelesen im Slot-B-Emit | RAM `_session_state[sid]['interaction_id']` (TAXO1, per-SID nach I-4) + DB intent_event.interaction_id | ⬜ PRE-BUILD-Re-Grep (Quelle bestätigen) |
| `slot1_variant_busy_until` (Mutex) | heute Slot-B-Guard | RAM per-SID :1331 + global :1334 | ⬜ Task 1 (ERSETZT durch interaction_id-Dedup) |
| Slot-B gerenderte interaction_id | FE-Render-Dedup | RAM Browser (pip-launcher.js state) | ⬜ Task 2 |
| Slot A (keyword) | unverändert lokal | RAM (Bestand) | ✓ NICHT angefasst |

## Verbindungs-Karte (Punkt 22)
- **interaction_id ist der richtige Key, NICHT line_id** (Pitfall 3, TAXO1-04-B3): line_id fragmentiert per Deepgram-Endpointing → 2 ids pro Moment. interaction_id = EINE-ID-pro-Moment-Klammer. **Quelle = TAXO1 (per-SID nach I-4) — VOR-Execute-Re-Grep bestätigt sie.**
- **slot1_variant_busy_until lebt** (:1267/:1275/:1331/:1334, grep) → wird durch interaction_id-Dedup ERSETZT (D-04), nicht doppelt belassen. grep nach Execute: Mutex als Slot-B-Guard weg (oder nur noch als unschädlicher Rest).
- **keyword_einwand_match in Slot B lebt** → Doppelung, raus aus Slot B (D-04). Slot A behält die Stichwort-Antwort (Bau-Regel 1, NICHT killen).
- **FE hat heute KEINEN line_id-Dedup** (pip-launcher.js:2488, grep) → interaction_id-Render-Dedup neu.

<tasks>

<task type="auto">
  <name>Task 0: interaction_id-Quelle bestaetigen (PRE-BUILD-Re-Grep-Gate, IL-1/OQ-2) + Mutex-Inventar</name>
  <read_first>
    - .planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-03-SUMMARY.md (IL-2-Re-Grep-Ergebnis: interaction_id-Quelle)
    - .planning/phases/08.23.2.TAXO1-verstehen-fundament-erkennung/08.23.2.TAXO1-04-live-cutover-taxonomie-k4-PLAN.md (I-4-Blocker-Disposition: per-SID-Anker wandert in TAXO1-03, Einwand-Fenster-Granularität)
    - services/claude_service.py:1240-1334 (_emit_qa_slot1, slot1_variant_busy_until-Stellen)
  </read_first>
  <action>
    GATE: Führe den PRE-BUILD-Re-Grep aus und schreibe das Ergebnis in die SUMMARY (Task 0):
    `grep -n "get_or_create_interaction_id\|interaction_id\|_session_state_lock\|state_lock\|kw_fired_for_line\|slot1_variant_busy_until" services/live_session.py services/claude_service.py services/intent_event_writer.py`
    - Bestätige: (a) TAXO1-04-Blocker I-4 ist gefixt (interaction_id per-SID, `_session_state[sid]['interaction_id']`, NICHT module-global) UND deployed (inspect.sh grep auf Prod-Code optional). (b) Die Granularität ist das ereignis-getriebene Einwand-Fenster (nicht roh line_id).
    - **Wenn die Quelle noch module-global / nicht deployed / I-4 offen → STOP + in SUMMARY als BLOCKED markieren, NICHT bauen.**
    - Inventarisiere alle slot1_variant_busy_until-Stellen (welche werden durch den Dedup ersetzt, welche bleiben unschädlich).
  </action>
  <verify>
    <automated>grep -c "interaction_id" services/live_session.py</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY enthält den PRE-BUILD-Re-Grep verbatim + die Feststellung: interaction_id-Quelle = per-SID (I-4 gefixt) ODER BLOCKED
    - Bei BLOCKED: Plan stoppt, keine Code-Änderung, Eskalation an André (Interlock noch offen)
    - Mutex-Inventar (slot1_variant_busy_until-Stellen) in SUMMARY
  </acceptance_criteria>
  <done>interaction_id-Quelle bestätigt (per-SID, I-4 gefixt) ODER Plan sauber als BLOCKED gestoppt.</done>
</task>

<task type="auto">
  <name>Task 1: Backend Slot-B-Dedup per interaction_id; keyword-Doppelung raus; Mutex ersetzen (Punkt 14 Control-Flow + Race)</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py:1240-1334 (_qa_pipeline_dispatch/_emit_qa_slot1, slot1_variant_busy_until :1267/:1275/:1331/:1334)
    - services/claude_service.py:515-600 (streame_auto_variante pip_token slot:1)
    - Task-0-Ergebnis (interaction_id-Quelle per-SID)
  </read_first>
  <action>
    **Punkt-14-Control-Flow + Race-Audit (eingebettet, Pflicht — Slot-B-Emit läuft in mehreren Threads: analyse_loop-Daemon + keyword-Pfad + Button):**
    - Dedup-State: pro-SID „zuletzt emittierte interaction_id für Slot B" (`_session_state[sid]['slot_b_emitted_iid']` ODER ein Set pro SID). Lese/Schreibe unter `_session_state_lock` (per-SID, konsistent mit TAXO1 I-4). RLock-Frage: hält der Aufrufer (analyse_loop) den Lock schon? (TAXO1-04-Review-Punkt — lock-free Helper ODER RLock).
    - **Dedup-Regel:** Vor jedem Slot-B-Emit (qa_slot1 + pip_token slot:1): hole die aktuelle interaction_id (Moment-Klammer). Wenn für DIESE interaction_id schon eine Slot-B-Variante emittiert wurde → SKIP (deterministisch, keine zweite Variante). Sonst: emit + interaction_id als emittiert markieren.
    - **keyword-Doppelung raus (D-04):** der keyword_einwand_match-Pfad schreibt NICHT mehr in Slot B (qa_slot1 gewinnt unten). Die Stichwort-Antwort bleibt in Slot A (pip-slot-0, NICHT anfassen).
    - **Mutex ersetzen:** slot1_variant_busy_until als Slot-B-Guard entfernen/ersetzen (:1267/:1275/:1331/:1334). Der interaction_id-Dedup IST der neue Guard (deterministisch). Falls eine Mutex-Stelle anderswo (nicht Slot-B-Dedup) unschädlich gebraucht wird → in SUMMARY begründen, sonst weg.
    - interaction_id in den Emit-Payload aufnehmen (für FE-Render-Dedup, Task 2).
    - Memory-Cleanup: bei /api/beenden den Dedup-State pro-SID poppen (kein Leak — TAXO1-04-Folgefehler-Hinweis).
  </action>
  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interaction_id" services/claude_service.py` deckt den Slot-B-Dedup ab (Lookup + Markierung + Payload)
    - keyword_einwand_match schreibt NICHT mehr in Slot B (manuelle Sicht + grep: keine Slot-1/qa_slot1-Emit im keyword-Pfad)
    - slot1_variant_busy_until als Slot-B-Guard ersetzt (`grep -n "slot1_variant_busy_until" services/claude_service.py` zeigt nur noch unschädliche/keine Slot-B-Guard-Nutzung — in SUMMARY begründet)
    - interaction_id im Slot-B-Emit-Payload (FE braucht ihn)
    - Race-Audit in SUMMARY (Lock-Haltung, RLock-Frage, Dedup-State-Cleanup bei /api/beenden)
  </acceptance_criteria>
  <done>Slot-B deterministisch per interaction_id dedupliziert, keyword-Doppelung raus, Mutex ersetzt, Race-Audit gemacht, Cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: FE-Render-Dedup Slot B per interaction_id (pip-launcher.js) + Pure-Logic-Test</name>
  <files>static/pip-launcher.js, tests/test_slot_b_dedup.py</files>
  <read_first>
    - static/pip-launcher.js:2488 + Slot-B-Render-Handler (qa_slot1/pip_token slot:1 → pip-slot-1; Slot A pip-slot-0 NICHT anfassen)
    - services/claude_service.py (interaction_id im Emit-Payload aus Task 1)
  </read_first>
  <action>
    **FE-Render-Dedup (pip-launcher.js):** im Slot-B-Socket-Handler (qa_slot1/pip_token slot:1): lies `interaction_id` aus dem Payload. Halte `state.slotBRenderedIid` (zuletzt gerenderte interaction_id). Bei gleicher interaction_id → KEIN neues DOM-Element (überschreibe den bestehenden Slot-B-Inhalt ODER ignoriere den Doppel-Emit). Bei neuer interaction_id → neuer Slot-B-Render. Slot A (pip-slot-0) NICHT anfassen — feuert weiter sofort + unabhängig.
    node --check static/pip-launcher.js (Syntax, lokal OK).

    **Pure-Logic-Test `tests/test_slot_b_dedup.py`** (Backend-Dedup-Logik, Function-Call/State-Mutation — KEIN Source-Presence):
    - test_same_interaction_id_one_variant: zwei Slot-B-Emit-Versuche mit gleicher interaction_id → nur EINER geht durch (zweiter SKIP).
    - test_different_interaction_id_two_variants: zwei verschiedene interaction_ids → zwei Varianten.
    - test_slot_a_independent: Slot-A-Pfad (keyword) wird vom Slot-B-Dedup NICHT geblockt (feuert weiter).
    - test_dedup_state_cleanup: nach Session-Ende (/api/beenden-Simulation) ist der per-SID-Dedup-State weg (kein Leak).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_slot_b_dedup.py -x' && node --check static/pip-launcher.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interaction_id" static/pip-launcher.js` >= 1 (FE-Render-Dedup)
    - `grep "pip-slot-0" static/pip-launcher.js` zeigt KEINE Änderung am Slot-A-Render (manuelle Sicht: Slot A unangetastet)
    - test_same_interaction_id_one_variant + test_different_interaction_id_two_variants + test_slot_a_independent + test_dedup_state_cleanup grün
    - node --check static/pip-launcher.js OK (Syntax)
  </acceptance_criteria>
  <done>FE dedupliziert Slot B per interaction_id, Slot A unangetastet, Pure-Logic-Tests grün.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Test-Anruf W3 (Dedup) — langsamer Einwand → genau 1 Slot-B; Slot A feuert weiter (D-05 Pflicht)</name>
  <what-built>
    Slot-B-Dedup deterministisch per interaction_id (Backend-Emit + FE-Render); keyword-Doppelung aus Slot B raus; Mutex ersetzt. Deployed auf Production.
  </what-built>
  <how-to-verify>
    1. getnerve.app als Test-User, Live-Call (cold_call).
    2. **Langsamer-Sprecher-Szenario (der D3-Auslöser):** einen Einwand MIT Sprechpause sprechen (Deepgram splittet ihn in 2 line_ids) → in Slot B (unten) erscheint genau EINE KI-Variante (kein Doppel-Emit, kein Flackern).
    3. **Slot A unabhängig:** die Stichwort-Antwort (oben, Slot A) feuert weiter sofort + lokal — unverändert. Slot A ist NICHT leer und NICHT gekillt.
    4. **Verschiedene Momente:** zwei verschiedene Einwände → zwei verschiedene Slot-B-Varianten (Dedup blockt nur den GLEICHEN Moment, nicht verschiedene).
    5. `inspect.sh logs` → interaction_id pro Moment (Beleg: gleiche id bei der gesplitteten Zeile, verschiedene id bei neuem Einwand).
  </how-to-verify>
  <resume-signal>Tippe "bestätigt" wenn: langsamer Einwand → genau 1 Slot-B; verschiedene Einwände → verschiedene Slot-B; Slot A feuert weiter sofort. Sonst: Abweichung beschreiben (Doppel-Emit / Slot A weg → Claude fixt).</resume-signal>
  <acceptance_criteria>
    - Live-Beleg: dieselbe Aussage (auch gesplittet) → genau EINE Slot-B-Variante (kein Doppel-Emit)
    - Live-Beleg: Dedup deterministisch per interaction_id (Log: gleiche id bei gesplitteter Zeile)
    - Live-Beleg: Slot A feuert weiter sofort + unabhängig (nicht gekillt, nicht leer)
    - Verschiedene Momente → verschiedene Slot-B-Varianten (Dedup nicht zu aggressiv)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| interaction_id-Quelle (TAXO1 per-SID) → Slot-B-Dedup | Vertrags-Naht; falsche Quelle (module-global/line_id) = falsche Dedup |
| Slot-B-Emit (mehrere Threads) → Dedup-State (RAM per-SID) | Race auf den Dedup-State |
| interaction_id im Emit-Payload → FE-Render | Backend→FE-Durchreichung |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-TAXO3-05-01 | T (Tampering / falsche Dedup) | Dedup an line_id statt interaction_id → fragmentiert (2 Varianten bei Sprechpause) | mitigate | interaction_id (Moment-Klammer) als Key; PRE-BUILD-Re-Grep bestätigt per-SID-Quelle; Live-Test langsamer Sprecher. |
| T-TAXO3-05-02 | T (Race) | gleichzeitiger Slot-B-Emit (analyse_loop + Button) → beide sehen „noch nicht emittiert" → 2 Varianten | mitigate | Dedup-State unter _session_state_lock (per-SID), RLock-Frage geprüft (Task 1 Race-Audit); Mark-then-emit atomar. |
| T-TAXO3-05-03 | D (Verfügbarkeit) | Slot A wird beim Dedup-Umbau versehentlich gekillt (D-03-Verletzung) | mitigate | Slot A (pip-slot-0) NICHT angefasst; test_slot_a_independent + Live-Test prüfen. |
| T-TAXO3-05-04 | I (Memory-Leak) | Dedup-State pro-SID wird bei Session-Ende nicht aufgeräumt | mitigate | Cleanup bei /api/beenden (pop per-SID); test_dedup_state_cleanup. |
| T-TAXO3-05-05 | T | Dedup zu aggressiv → verschiedene Momente teilen eine id → echte 2. Variante unterdrückt | mitigate | Granularität = ereignis-getriebenes Einwand-Fenster (TAXO1 I-4, André); Live-Test „verschiedene Einwände → verschiedene Slot-B". |
</threat_model>

<verification>
- PRE-BUILD-Re-Grep (Task 0): interaction_id-Quelle per-SID (I-4 gefixt) ODER Plan BLOCKED.
- `grep interaction_id services/claude_service.py + static/pip-launcher.js` deckt Backend-Dedup + FE-Render-Dedup ab.
- keyword-Doppelung aus Slot B raus; slot1_variant_busy_until als Slot-B-Guard ersetzt.
- pytest test_slot_b_dedup.py grün (eine id→eine Variante, verschiedene→zwei, Slot A unabhängig, Cleanup); node --check OK.
- Test-Anruf (D-05): langsamer Einwand → genau 1 Slot-B; Slot A feuert weiter; verschiedene Momente → verschiedene Varianten.
- Race-Audit Dedup-State im SUMMARY.
</verification>

<success_criteria>
- Slot B: eine Aussage → genau eine KI-Variante, deterministisch per interaction_id (kein Mutex-Zufall, kein line_id-Fragment); keyword-Doppelung raus; Slot A unverändert.
- FE dedupliziert den Render; Cleanup bei Session-Ende.
- TAXO1-Interlock (I-4 + interaction_id-Quelle) vor Bau bestätigt — nicht an falscher Quelle geklammert.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.TAXO3-antworten-eine-wissensversorgung/08.23.2.TAXO3-05-SUMMARY.md` (inkl. PRE-BUILD-Re-Grep-Beleg [interaction_id-Quelle per-SID], grep-Belege Backend+FE-Dedup + keyword-Doppelung raus + Mutex ersetzt, pytest+node-Beleg, Test-Anruf-Protokoll [langsamer Einwand→1 Slot-B, Slot A unabhängig], Race-Audit).
</output>
