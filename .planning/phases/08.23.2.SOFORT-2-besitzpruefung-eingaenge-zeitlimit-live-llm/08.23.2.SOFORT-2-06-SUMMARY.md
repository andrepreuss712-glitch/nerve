---
phase: 08.23.2.SOFORT-2
plan: 06
subsystem: live-llm
tags: [timeout, max-retries, live-pfad, stream, httpx, stale-tests, d-01, r-6, r-10]

requires:
  - phase: 08.23.2.SOFORT-2-05
    provides: "die sechs Konstanten in config.py + der Welle-2-Waechter tests/test_live_timeout_coverage.py + der ROT-Beleg (3 failed / 2 passed)"
  - phase: 08.23.2.SOFORT-2-01
    provides: "Zahlen-Tafel ZT-15 (MINDEST_LIVE_PFADE = 10) / ZT-16 (2) / ZT-17 (11 gesamt, 10 angefasst)"
provides:
  - "timeout=httpx.Timeout(...) an ZEHN von elf Live-LLM-Aufrufen — acht blockierend (LIVE_LLM_TIMEOUT_S), zwei Stream (LIVE_LLM_STREAM_TIMEOUT_S)"
  - "max_retries=0 am Modul-Client services/claude_service.py — Worst Case 1 x Limit statt 3 x"
  - "zwei umgeschriebene Bestandstests: test_daemon_pfad_mit_zeitlimit (Runtime-Wert-Pruefung) + test_modul_client_hat_keinen_retry"
  - "alle Selbstverweise im http_llm_client-Docstring nennen Namen statt Zeilennummern (D-01)"
affects:
  - "08.23.2.SOFORT-2-07 (Zaehler + sichtbarer Hinweis — braucht den Timeout-Zweig im AEUSSEREN except)"
  - "08.23.2.SOFORT-2-08 (Deploy + GRUEN-Lauf + D-06-Messung gegen die MESSGERAETE-1-Tabelle)"

tech-stack:
  added: []
  patterns:
    - "EIN Mechanismus, ZWEI Zahlen: derselbe httpx-Parameter wirkt blockierend als Gesamt-Limit, im Stream als TTFT-plus-Stillstands-Limit — ohne die Gesamtdauer zu kappen"
    - "max_retries am Modul-Client statt with_options() auf Modul-Ebene — sonst laufen 91 monkeypatch.setattr in 16 Testdateien am Fake vorbei"
    - "Stale-Tests werden UMGESCHRIEBEN, nicht geloescht: das Test-Netz ist eine Ratsche"
    - "Selbstverweise auf NAMEN statt Zeilennummern (D-01) — der Import in Task 2 haette den heute richtigen (:27) still falsch gemacht"

key-files:
  created:
    - .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-06-SUMMARY.md
  modified:
    - services/claude_service.py
    - services/qa_pipeline.py
    - services/adoption_runner.py
    - services/judge_runner.py
    - tests/test_stabil1_http_llm_timeout.py
    - tests/test_medium_lane_intent_event_live.py

key-decisions:
  - "Zehn Pfade, nicht acht — die Zahlen-Tafel (ZT-15/ZT-17) ist die Quelle, nicht die Prosa; adoption_runner und judge_runner sind mitgefixt (R-10)"
  - "Die Stream-Pfade tragen die ZWEITE Zahl (8 s) und werden ausdruecklich NICHT an der Gesamtdauer gekappt"
  - "Beide Zeilennummern-Verweise im http_llm_client-Docstring fallen weg — auch der heute richtige (:27), weil import httpx ihn auf :28 schiebt"
  - "Kein Timeout-except-Zweig in diesem Plan gebaut (Plan 07) — und wenn, dann in den AEUSSEREN except (Plan-05-Befund)"
  - "Kein Deploy, kein lokaler pytest-Lauf (CLAUDE.md 'HART: Kein Local-Dev') — der GRUEN-Lauf ist Plan 08"

requirements-completed: []

duration: ~35min
completed: 2026-08-06
---

# Phase 08.23.2.SOFORT-2 Plan 06: Der Einbau — timeout= an zehn Live-Aufrufen + max_retries=0 Summary

**Kein Live-LLM-Aufruf laeuft mehr ohne Zeitlimit: acht blockierende Aufrufe tragen `LIVE_LLM_TIMEOUT_S`, die zwei Stream-Pfade die zweite Zahl `LIVE_LLM_STREAM_TIMEOUT_S` — ohne die Gesamtdauer zu kappen —, der Modul-Client wird mit `max_retries=0` erzeugt, und die zwei Bestandstests, die diese Vertragsaenderung bricht, sind umgeschrieben statt geloescht. AST-Beleg ueber alle vier Sweep-Dateien: `AUFRUFE 10 / OHNE_TIMEOUT []`.**

## Performance

- **Duration:** ~35 min
- **Tasks:** 3 / 3
- **Files modified:** 6 · **created:** 1 (diese SUMMARY)

## Commits

| Task | Commit | Inhalt |
|---|---|---|
| 1 | `0cdfd78` | `max_retries=0` am Modul-Client + Erklaerblock (R-6 benannt) + alle drei Selbstverweise auf Namen (auch in der Testdatei) |
| 2 | `de15b45` | `timeout=` an zehn von elf Aufrufen + `import httpx` in allen vier Sweep-Dateien |
| 3 | `9210273` | die zwei Bestandstests umgeschrieben + Modul-Docstring nachgezogen + Gegenprobe auf weitere strikte Fakes |

## Task 1 — `max_retries=0` und die drei Selbstverweise

`services/claude_service.py`: der Modul-Client heisst jetzt

```python
claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY, max_retries=0)
```

Der vierzehnzeilige Erklaerblock steht **unter** der Zeile (Konstruktor bleibt Kopfzeile seines Blocks), nennt die Worst-Case-Rechnung `3 x 12 s + Backoff ~= 38 s`, den Grund fuer den Modul-Client statt eines zweiten Clients (Import-Zeit-Falle mit 91 Monkeypatches in 16 Testdateien) und **die Nebenwirkung R-6** namentlich.

**⚠ R-6 (bewusst, nicht neu entschieden):** `services/judge_runner.py` und `services/adoption_runner.py` benutzen `claude_client` direkt und verlieren damit ihre zwei SDK-Retries. Beide sind Post-Call-Batch hinter `slow_lane` mit eigener Fehlerbehandlung — ein verlorener Retry bedeutet eine fehlende Bewertungszeile, keinen gebrochenen Anruf (Threat-Register T-SOF2-59, disposition `accept`). Der Fund steht im Code-Kommentar **und** in `08.23.2.SOFORT-2-FUNDE.md`.

**D-01 — alle drei Selbstverweise nennen jetzt Namen:**

| Verweis | vorher | nachher |
|---|---|---|
| Modul-Client im `http_llm_client`-Docstring | `(:27)` — **heute richtig** | `claude_client, das Modul-Global direkt ueber dieser Funktion` |
| Stream-Aufrufe im `http_llm_client`-Docstring | `:812/:980` — veraltet (Ist: :898/:1090) | `streame_auto_variante / streame_manual_ewb_variante` |
| Stream-Aufrufe in `test_stabil1_http_llm_timeout.py` | `:812/:980` — veraltet | dieselben zwei Funktionsnamen (einziger zulaessiger Beifang) |

Auch der **heute richtige** `(:27)` faellt weg: Task 2 fuegt `import httpx` ganz oben ein und schiebt den Konstruktor auf `:28`. Ein `grep -cF "(:27)"`-Kriterium waere danach **gruen auf einer Luege** geblieben — genau die Fehlerklasse, gegen die D-01 antritt.

### Verifikation (alle Anker gruen)

```
ast.parse services/claude_service.py                                        -> SYNTAX OK
grep -cF "claude_client = anthropic.Anthropic(api_key=..., max_retries=0)"  -> 1
grep -c  "anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)$"                  -> 0   (Anker: "anthropic.Anthropic(" -> 1)
grep -c  ":812/:980" (beide Dateien, ":0$"-Zaehlung)                        -> 2   (Anker: streame_manual_ewb_variante -> 4)
grep -cF "der Modul-Client (claude_client, das Modul-Global direkt ueber"   -> 1
grep -c  "(:27)"                                                            -> 0   (Anker: claude_client -> 14)
AST CLIENT_ZUWEISUNGEN                                                      -> 1
grep -c  "R-6"                                                              -> 1
```

## Task 2 — `timeout=` an ZEHN von elf Aufrufen

`import httpx` in **allen vier** Sweep-Dateien ergaenzt — `services/adoption_runner.py` und `services/judge_runner.py` hatten ihn nicht; ohne ihn waere der Modul-Import an einem `NameError` gestorben und das Deploy-Tor rot gefallen, bevor irgendein Test laeuft.

| # | Datei::Funktion | Art | Zahl |
|---|---|---|---|
| 1 | `claude_service.py::classify_phase` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 2 | `claude_service.py::infer_customer_state` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 3 | `claude_service.py::analysiere_mit_claude` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 4 | `claude_service.py::analysiere_und_klassifiziere` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 5 | `claude_service.py::streame_auto_variante` | **Stream** | `LIVE_LLM_STREAM_TIMEOUT_S` |
| 6 | `claude_service.py::streame_manual_ewb_variante` | **Stream** | `LIVE_LLM_STREAM_TIMEOUT_S` |
| 7 | `claude_service.py::analysiere_coaching` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 8 | `qa_pipeline.py::classify_utterance` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 9 | `qa_pipeline.py::generate_qa_response` | toter Code | ⛔ **NICHT angefasst** (Waechter-Allowlist) |
| 10 | `adoption_runner.py::run_adoption_judge` | blockierend (R-10) | `LIVE_LLM_TIMEOUT_S` |
| 11 | `judge_runner.py::run_behavior_judge` | blockierend (R-10) | `LIVE_LLM_TIMEOUT_S` |

⚠ **WORST CASE BLOCKIEREND = 5 s Verbindung + 12 s Lesen ≈ 17 s** — httpx zaehlt `connect` und `read` **nacheinander**, nicht als Gesamtbudget. Fuer die Stream-Pfade: 5 + 8 = **13 s bis zum ersten Token**. Wer das Gesamt-Budget senken will, senkt **beide** Zahlen, nicht nur die auffaellige. **D-06 (Plan 08) misst gegen genau diese Zahl.**

**Die Stream-Pfade haengen bewusst NICHT an der Gesamtdauer.** `read` gilt bei httpx pro Datenblock; beim Stream ist jedes Token ein Datenblock. Der Wert wirkt damit als TTFT- **plus** Stillstands-Grenze — eine lange, aber fliessende Antwort laeuft unbegrenzt weiter. Die Rest-Kante (`read` kappt auch eine Pause MITTEN in der Antwort; es gibt keine Messung der Token-Abstaende) steht als Kommentar an der Stelle, wie im Plan verlangt (T-SOF2-53, disposition `accept`).

**Nicht passiert (alle fuenf Verbote eingehalten):** kein `timeout` am toten `generate_qa_response`; `_t_api_start` unveraendert an seiner Stelle (7x); die `try`-Bloecke um beide `with`-Bloecke bleiben **aussen**; kein `finally: stream.close()`; keine Uhr im Token-Loop; `log_api_cost` unangetastet.

### Verifikation (alle Anker gruen)

```
AST ueber ALLE VIER Sweep-Dateien:   AUFRUFE 10   OHNE_TIMEOUT []
grep -cF "...LIVE_LLM_TIMEOUT_S, connect=..."   claude_service.py   -> 5
grep -cF "...LIVE_LLM_STREAM_TIMEOUT_S, connect=..." claude_service.py -> 2
grep -cF "...LIVE_LLM_TIMEOUT_S, connect=..."   qa_pipeline.py      -> 1
grep -cF "...LIVE_LLM_TIMEOUT_S, connect=..."   adoption_runner.py  -> 1
grep -cF "...LIVE_LLM_TIMEOUT_S, connect=..."   judge_runner.py     -> 1
grep -cE "^import httpx"  cs/qa/adoption/judge                      -> 1 / 1 / 1 / 1
AST generate_qa_response:            GEFUNDEN 1   MIT_TIMEOUT []
Beifang (stream.close/Uhr/break)                                    -> 0   (Anker: ^+ -> 33)
log_api_cost im Diff                                                -> 0   (Anker: Bestand 31)
_t_api_start = time.monotonic()                                     -> 7
ast.parse alle vier Dateien                                         -> OK
```

Die `33` und die `31` neben den Nullen sind der Punkt: eine Abwesenheits-`0` ohne Existenz-Anker waere nicht von „der grep sieht die Datei gar nicht" unterscheidbar.

## Task 3 — Die zwei Stale-Tests umgeschrieben

**(a0) Der Modul-Docstring behauptete woertlich das Gegenteil.** In `tests/test_stabil1_http_llm_timeout.py` stand in der Aufzaehlung „Daemon-Pfad (analysiere_mit_claude) bekommt KEIN Zeitlimit". Nach dem Umbau ist das falsch — und es **bricht keinen Test**, faellt also sonst nie auf. Ersetzt durch die zwei Zeilen aus dem Plan (Zeitlimit AM AUFRUF + `max_retries=0`).

**(a) `test_daemon_pfad_ohne_zeitlimit` → `test_daemon_pfad_mit_zeitlimit`.** Der alte Test verankerte „Daemon-Pfad darf `with_options()` NICHT aufrufen" — nach dieser Phase eine **falsche Aussage, die zufaellig gruen geblieben waere** (wir rufen tatsaechlich kein `with_options`). Der neue Test faengt die `messages.create`-Argumente an einem Fake ab und prueft den **Wert**:

- `'timeout' in aufgefangen`
- `_t.read == config.LIVE_LLM_TIMEOUT_S`
- `_t.connect == 5.0`

Das ist die **Runtime**-Ergaenzung zum statischen Waechter: der sieht ein Schluesselwort im Syntaxbaum, dieser sieht den Wert, der wirklich ankommt.

**Neu dazu: `test_modul_client_hat_keinen_retry`** — Runtime-Gegenprobe am erzeugten Objekt (`claude_client.max_retries == 0`). `test_modul_client_bleibt_unveraendert` bleibt unveraendert bestehen (seine Aussage ueber `with_options()` als Kopie ist weiterhin richtig).

**(b) `tests/test_medium_lane_intent_event_live.py`** — die strikte Fake-Signatur `def create(self, *, model, max_tokens, system, messages):` waere an `TypeError: create() got an unexpected keyword argument 'timeout'` gestorben. Jetzt mit `**kwargs` und einem Kommentar, der den Grund nennt.

### (c) Die Gegenprobe auf weitere strikte Fakes — Ergebnis

Der Plan-Sweep (`tests/test_*.py`, `def create`/`def stream` ohne `**kwargs`) meldet:

```
keine weiteren strikten Fakes
```

**Zusaetzlich breiter gefahren** (nicht nur `glob`, sondern `rglob` inkl. Unterordner; zusaetzlich `async def` und `lambda`-Zuweisungen an `create`/`stream`):

```
TREFFER 0
```

Die zwei im Plan namentlich als „wahrscheinlich unberuehrt" gefuehrten Dateien einzeln nachgesehen statt angenommen:
- `tests/test_qa_pipeline_rueckfrage.py:108` und `:143` — beide bereits `def create(self, **kwargs)`, vertragen das zusaetzliche Schluesselwort.
- `tests/test_anon_live_vs_stored.py:103` — patcht `cs.claude_client` mit einem `_FakeClient`, dessen `create` ebenfalls `**kwargs` traegt (im breiten Sweep mitgeprueft, 0 Treffer).

### Verifikation (alle Anker gruen)

```
def test_daemon_pfad_mit_zeitlimit                 -> 1   (alter Name -> 0)
_t.read == config.LIVE_LLM_TIMEOUT_S               -> 1
"bekommt KEIN Zeitlimit"                           -> 0   (Anker: "setzt sein Zeitlimit AM AUFRUF" -> 2)
claude_service_module.claude_client.max_retries==0 -> 1
def test_ (Datei gesamt)                           -> 6   (>= 4; test_modul_client_bleibt_unveraendert -> 1)
def create(..., **kwargs) im medium-lane-Fake      -> 1   (alte strikte Signatur -> 0)
Diff der ZWEI Dateien: -def test_ -> 1  /  +def test_ -> 2
ast.parse beide Testdateien                        -> OK
```

## Decisions Made

- **Zehn statt acht.** Das `must_haves`-Frontmatter von Plan 05 nennt noch die `8`; die Zahlen-Tafel (ZT-15 = 10, ZT-17 = 11 gesamt / 10 angefasst) ist die Quelle. Gebaut wurden **zehn**, und die AST-Abnahme laeuft ueber alle **vier** Sweep-Dateien — waere sie nur ueber zwei gelaufen, haette sie `AUFRUFE 8` gemeldet und waere wirkungslos gewesen.
- **Der Erklaerkommentar steht nur einmal ausgeschrieben** (bei `classify_phase` bzw. `streame_auto_variante`), an den uebrigen Stellen als Ein-Zeiler mit Verweis auf den Namen der ausgeschriebenen Stelle. Vierzehn identische Bloecke waeren dieselbe „eine Aussage an N Orten"-Falle, gegen die diese Phase antritt.
- **Kein Timeout-`except`-Zweig gebaut.** Der gehoert nach Plan 07 — und laut Plan-05-Befund in den **aeusseren** `except`, nicht neben den Kosten-Hook (die Ausnahme ueberspringt in `streame_auto_variante` die Zeilen ab dem `with` bis hinter den Hook, inklusive `pip_token_done` und `record_suggestion_offer`).
- **Kein lokaler pytest-Lauf.** CLAUDE.md „HART: Kein Local-Dev" — lokal nur `ast.parse`, AST-Sweeps und `grep`. Der GRUEN-Lauf der Suite laeuft serverseitig im Deploy-Tor: **Plan 08**.

## Deviations from Plan

### 1. [Beobachtung, kein Eingriff] Ein Existenz-Anker liefert 2 statt der im Plan notierten 1

Das Abnahme-Kriterium in Task 3 lautet `grep -c "setzt sein Zeitlimit AM AUFRUF" == 1`. Gemessen: **2**. Grund: der Plan schreibt diesen Wortlaut an **zwei** Stellen vor — im Modul-Docstring (a0) und in der ersten Zeile des Docstrings der neuen Funktion (a). Beide Texte sind woertlich aus dem Plan uebernommen. Der Zweck des Kriteriums ist ausdruecklich ein **Existenz-Anker** („der zweite Wert beweist, dass die Datei ueberhaupt gelesen wurde") — der ist mit 2 erfuellt. **Keine Zahl nachgezogen, kein Text an den Anker angepasst.** Die `== 1` im Plan ist schlicht eine Zaehl-Ungenauigkeit gegen den eigenen vorgeschriebenen Text.

### 2. [Erweiterung, Rule 2] Die Fake-Gegenprobe breiter gefahren als vorgeschrieben

Der Plan-Sweep deckt `tests/test_*.py` (nur oberste Ebene) und nur `ast.FunctionDef` ab. Zusaetzlich gefahren: `rglob` (Unterordner), `AsyncFunctionDef` und `lambda`-Zuweisungen an `create`/`stream`. Ergebnis beide Male 0 — die Erweiterung aendert nichts am Befund, macht ihn aber belastbar statt „im gepruefften Ausschnitt sauber". Kein Code geaendert.

### 3. [mechanisch] Der Erklaerkommentar aus dem Plan-Beispiel steht nicht an allen zehn Stellen

Siehe „Decisions Made". Die vorgeschriebene **Code**-Zeile (`timeout=httpx.Timeout(...)`) steht ueberall exakt wie im Plan — nur der mehrzeilige Begruendungstext ist einmal ausgeschrieben und sonst referenziert.

## Testing

- **Kein lokaler pytest-Lauf** (CLAUDE.md „HART: Kein Local-Dev"). Lokal gefahren: `ast.parse` auf allen sechs geaenderten Dateien, der AST-Aufruf-Sweep ueber alle vier Sweep-Dateien, der AST-Check auf `generate_qa_response`, der AST-Client-Zuweisungs-Check und saemtliche `grep`-Anker aus den drei `<acceptance_criteria>`-Bloecken.
- **Der Welle-2-Waechter** (`tests/test_live_timeout_coverage.py`, Plan 05, ROT belegt mit 3 failed / 2 passed) prueft exakt die Bedingungen, die hier eingebaut wurden. Seine drei roten Pruefungen sind durch diesen Plan adressiert: `timeout` an jedem Live-Aufruf (AST: `OHNE_TIMEOUT []`), `LIVE_LLM_STREAM_TIMEOUT_S` an den zwei Stream-Knoten, `max_retries=0` am Modul-Client. **Der GRUEN-Lauf als Beleg gehoert in Plan 08** — hier gibt es keinen, weil er lokal nicht zaehlen wuerde.
- **Kein Deploy.** Ausdrueckliche Plan-Vorgabe: Stufe 1/2 aus D-04 (Zaehler + sichtbarer Hinweis) fehlt noch. Ein Deploy jetzt wuerde das Zeitlimit **ohne** die Sichtbarkeit ausrollen — also genau den stillen Ausfall herstellen, den D-04 verhindern soll.

## Known Stubs

Keine. Der fehlende Timeout-`except`-Zweig (Anzeige/Zaehler) ist die ausdrueckliche Grenze dieses Plans (Plan 07), kein Stub — der Aufruf bricht heute mit `anthropic.APITimeoutError` ab und wird von der bestehenden Fehlerbehandlung gefangen wie jede andere Ausnahme.

## Pruning-Notiz (Vault-Regel 7)

Durch diesen Plan wurde **nichts** obsolet.

- `test_daemon_pfad_ohne_zeitlimit` ist nicht geloescht, sondern **umgeschrieben** — die Sperre bleibt, ihre Aussage ist umgedreht. Das Test-Netz ist eine Ratsche.
- `test_modul_client_bleibt_unveraendert` bleibt gueltig: `with_options()` liefert weiterhin eine Kopie, der Modul-Client wird nicht mutiert — das schuetzt die HTTP-Pfade unabhaengig von `max_retries`.
- `http_llm_client()` und `config.HTTP_LLM_*` bleiben vollstaendig noetig: sie decken die **HTTP-Request-Thread**-Pfade ab (eigene Zahlen wegen nginx-60s), nicht die Live-Bahn.
- `tests/test_live_latency_coverage.py` bleibt: es prueft, **dass gemessen wird**; der Welle-2-Waechter prueft, **dass begrenzt wird**.

## Threat Flags

Keine neue Angriffsflaeche. Es kommt keine Route, kein Eingang, kein Schema und kein Auth-Pfad hinzu — die Aenderung verkuerzt ausschliesslich, wie lange ein bestehender ausgehender Aufruf haengen darf. Die im Threat-Register gefuehrten Punkte sind damit adressiert:

| Threat | Status |
|---|---|
| T-SOF2-50 (haengender blockierender Aufruf blockiert alle Gespraeche) | mitigiert — AST `OHNE_TIMEOUT []` bei `AUFRUFE 10` |
| T-SOF2-51 (`max_retries=2` verdreifacht den Worst Case) | mitigiert — `max_retries=0` + Runtime-Gegenprobe |
| T-SOF2-52 (haengender Stream blockiert den PiP) | mitigiert — `LIVE_LLM_STREAM_TIMEOUT_S` |
| T-SOF2-53 (Stream-Limit kappt legitime Pause) | `accept` — Rest-Kante im Code benannt, Messung ist D-06 |
| T-SOF2-54 (`log_api_cost` verfaelscht) | mitigiert — Diff `^[+-].*log_api_cost` == 0 bei Bestand 31 |
| T-SOF2-55 (Stale-Test bleibt falsch-gruen) | mitigiert — umgeschrieben, beide Richtungen belegt |
| T-SOF2-56 (`_t_api_start` verrutscht) | mitigiert — Anker unveraendert (7x) |
| T-SOF2-57 (`try` im `with` faengt TTFT-Timeout nicht) | mitigiert — `try`-Position unangetastet, am Code nachgesehen |
| T-SOF2-58 (toter Pfad bekommt Limit) | mitigiert — `GEFUNDEN 1 MIT_TIMEOUT []` |
| T-SOF2-59 (Runner verlieren Retries) | `accept` — **R-6**, im Code-Kommentar und in FUNDE.md |

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.** `api_cost_log` ist ausdruecklich nicht angefasst (F-5a).

## Was der naechste Plan wissen muss

1. **Der Timeout-Zweig (Plan 07) gehoert in den AEUSSEREN `except`** von `streame_auto_variante` — die Ausnahme aus dem Token-Loop verlaesst den `with`-Block und ueberspringt alles bis hinter den Kosten-Hook, inklusive `pip_token_done` und `record_suggestion_offer`. In `streame_manual_ewb_variante` wird der `try:` hinter dem Retry-Loop bei einem Timeout **nie betreten**; gefangen wird im `except` des Retry-Loops (kein 529 → `raise`).
2. **`anthropic.APITimeoutError` ist eine UNTERKLASSE von `anthropic.APIConnectionError`** — wer beide faengt, faengt sie in der richtigen Reihenfolge (Restluecke 3 des Waechters).
3. **Die Zahl fuer D-06 lautet 17 s blockierend / 13 s bis zum ersten Token** (`connect` + `read`, nacheinander). Gegen sie wird in Plan 08 gemessen.
4. **Kein Deploy ist erfolgt.** Der Code liegt committet auf `main` (HEAD `9210273`), nicht gepusht, nicht ausgerollt. Prod laeuft weiter auf dem Welle-1-Stand (`alembic_version = 0038`).
5. **`.planning/ROADMAP.md` und `.planning/STATE.md` wurden in diesem Plan bewusst NICHT angefasst** (Auftrags-Vorgabe — der Orchestrator fuehrt das Tracking). Nachgeprueft: `git log --name-only -3 | grep -c "ROADMAP.md\|STATE.md"` → **0**.

## Self-Check: PASSED

- `services/claude_service.py` — FOUND (`max_retries=0`, 5+2 timeout-Zeilen, `ast.parse` sauber)
- `services/qa_pipeline.py` — FOUND (1 timeout-Zeile, toter Pfad unangetastet)
- `services/adoption_runner.py` — FOUND (1 timeout-Zeile + `import httpx`)
- `services/judge_runner.py` — FOUND (1 timeout-Zeile + `import httpx`)
- `tests/test_stabil1_http_llm_timeout.py` — FOUND (6 Tests, alter Name fort, neuer Name da)
- `tests/test_medium_lane_intent_event_live.py` — FOUND (`**kwargs` im Fake)
- Commit `0cdfd78` — FOUND · Commit `de15b45` — FOUND · Commit `9210273` — FOUND
- `.planning/ROADMAP.md` / `.planning/STATE.md` in keinem der drei Commits — BELEGT (0 Treffer)
