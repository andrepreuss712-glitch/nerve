---
phase: 08.23.2.SOFORT-2
round: 6
reviewers: [claude-cli, claudian-nachpruefung]
reviewers_failed: [gemini]
reviewed_at: 2026-08-05
verdict: baureif (nach Einarbeitung von R6-1 und R6-2)
plans_reviewed: [01, 02, 03, 04, 05, 06, 07, 08, 09]
supersedes: [08.23.2.SOFORT-2-REVIEWS-R1.md, 08.23.2.SOFORT-2-REVIEWS-R2-R3.md]
---

# Cross-AI Plan Review — Phase 08.23.2.SOFORT-2 — RUNDE 6 (LETZTE)

> Runden 1-3 liegen in `-REVIEWS-R1.md` und `-REVIEWS-R2-R3.md`.
> **Gemini ist ausgefallen** (HTTP 429, Guthaben aufgebraucht) — zum dritten Mal in Folge.
> Diese Runde lief mit **einer** externen Sicht (Claude CLI, eigene Sitzung, am Code) plus
> einem unabhaengigen Claudian-Durchgang. **Das ist keine Modell-Vielfalt und wird hier
> nicht als solche gemeldet.**

## Abbruch-Kriterium (Andre-Vorgabe fuer diese Runde)

Nach dieser Runde wird gebaut — behoben werden nur Funde der Klassen
**(a)** Datenpreisgabe/-verlust · **(b)** Bau bricht mechanisch ab ·
**(c)** Beweis-Anker nachweislich wirkungslos. Alles andere wird **notiert**.

**Ergebnis: zwei qualifizierende Funde, beide behoben. Acht Funde notiert.**

| Fund | Klasse | Status |
|---|---|---|
| **R6-1** — Plan 06 baut R-10 nicht, obwohl Plan 05s Waechter ihn fordert | **(b)** | ✅ behoben |
| **R6-2** — Plan 05 Task 3 ROT-Erwartung „acht" nach der ZT-15-Erhoehung unerfuellbar | **(c)** | ✅ behoben |
| R6-3 … R6-10 | NOTIEREN | Ablageort je Fund benannt |

---

# Erster Durchgang — Claude CLI (eigene Sitzung, am Code)


## 1. Kurzfazit

Der Beweis-Apparat traegt jetzt. Ich habe die fuenf abgeleiteten Mengen, beide Regel-Fassungen, die Helfer-Anerkennung, die Allowlist-Ziele, die `try`-Lage in `classify_phase`/`infer_customer_state`, die Modul-Ebene von `services/claude_service.py` und die Antwortform von `api_beenden` **am Code nachgefahren** — und alle Zahlen der Zahlen-Tafel (ZT-1…ZT-6, ZT-7, ZT-15, ZT-16, ZT-17) sowie die neun Marker-Zuordnungen aus Plan 01 Task 2 stimmen exakt. Die drei Anlaeufe an `methodik_uebertragen`/`test_b02` sind beim vierten Mal richtig gelandet: die enge `org_id`-Bindungs-Regel meldet **genau eine** Funktion rot, und `resp.json['call_id'] is None` ist der einzige Anker in `api_beenden`, der vor dem Fix wirklich falsch und nach dem Fix wirklich richtig ist. **Genau ein Fund bleibt, und es ist der von S4 vorhergesagte:** die Erhoehung ZT-15 8→10 / ZT-17 9/8→10/11 wurde in Plan 01 und Plan 05 (Waechter) nachgezogen, in **Plan 06 (Einbau)** und **Plan 05 Task 3 (ROT-Erwartung)** aber nicht — dort steht ueberall noch acht. Folge: der Waechter fordert zehn, der Bau liefert acht, das Deploy-Tor in Plan 08 faellt rot. Mechanischer Abbruch, Klasse (b).

---

## 2. S1 — Die neun Marker-Zeilen, jede einzeln am Code

Ableitung gemessen (`ast.parse` ueber `routes/*.py`, heutiger Arbeitsstand):

```
ZUSTAND (5)  api_beenden, _load_beenden_state, api_gatekeeper_phrases,
             learning::api_postcall_analysis, learning::api_postcall_outcome
SID (2)      api_gatekeeper_phrases, api_precall_research
CALLID (5)   api_beenden, api_calls_latest_outcome, crm_export::save_meeting,
             learning::api_postcall_analysis, learning::api_postcall_outcome
PROFILEID(6) / URL (8)  — siehe S2
```

| Pruefung | Markierung | Ist-Verhalten am Code | Urteil |
|---|---|---|---|
| `test_mindestens_erwartete_pruefpunkte_gefunden` | keine | roh **5/2/5/6/8** ≥ Soll 3/2/5/6/8 → PASSED | ✅ gruen, richtiger Grund |
| `test_zustands_aufloesungen_…` | `rot_vor_fix` | 5 − 1 Allowlist = **4** Funktionen, keiner der drei Helfer existiert → FAILED | ✅ rot |
| `test_sid_aus_der_anfrage_…` | `rot_vor_fix` | 2 Funktionen, kein Helfer → FAILED; nennt `api_precall_research` (N-01) | ✅ rot |
| `test_call_id_aus_der_anfrage_…` | `rot_vor_fix` | 5 − 1 Allowlist = **4**, darunter `save_meeting` (R-7) → FAILED | ✅ rot |
| `test_profile_id_aus_der_anfrage_…` | `rot_vor_fix` | Regel angewandt: **genau eine** rot (`methodik_uebertragen`), `training_end` allowlistet | ✅ rot, siehe S2 |
| `test_kennung_aus_url_…` | keine | alle 8 gruen (5× Server-Identitaet, 3× ueber `_require_own_profile`) | ✅ gruen |
| `test_regeln_fangen_ein_kuenstliches_leck` | keine | prueft die Regel, nicht den Bestand | ⚠ siehe R6-3 |
| `test_helfer_existieren_und_pruefen_user_id` | `rot_vor_fix` | `sid_belongs_to` existiert heute nicht → FAILED. `_require_own_profile` liegt auf `routes/profiles.py:602` und traegt `g.org.id` → Teil (b) der Anerkennung greift | ✅ rot |
| `test_allowlist_ist_begruendet_und_lebt` | keine | alle drei Ziele leben: `_load_beenden_state:211`, `api_calls_latest_outcome:2107`, `training_end:605` | ✅ gruen |

Der GRUEN-Lauf geht ebenfalls auf: nach Plan 03 fallen die zwei `learning.py`-Funktionen aus `ZUSTAND` heraus (der Ersatz-Block enthaelt kein `ls._session_state` mehr), `api_beenden` behaelt seinen Stufe-2-Scan **und** bekommt den Helfer → **exakt 3**, wie ZT-2 und Plan 04s I-1-Kriterium verlangen.

## 3. S2 — Die enge `org_id`-Regel: wie viele Falsch-Treffer wirklich?

Regel implementiert wie spezifiziert ((a) `ast.Compare` `…org_id` gegen Server-Identitaet **oder** (b) `filter_by/filter/get/query` mit `org_id=<Server-Identitaet>`, Konstruktoren zaehlen nicht), plus Helfer-Bewusstheit. Ergebnis ueber **alle 14** Funktionen der Mengen 4 und 5:

```
PROFILEID:  api_set_profile GRUEN(b) · methodik_uebertragen ROT · api_profile_preview_context GRUEN(a)
            training_start GRUEN(a) · training_end ROT · api_training_personality_generate GRUEN(b)
URL:        alle 8 GRUEN (api_faqs_list/_create/api_tabu_update NUR ueber _require_own_profile)
```

**Genau ein Falsch-Treffer, `routes/training.py::training_end`** — mehr nicht. Damit ist ZT-7 = 3 richtig, und die Begruendung stimmt am Code: `training_start` erfuellt (a) — hier ist der W-1-Korrekturvermerk im Plan („(b) `filter_by(id=profile_id, org_id=g.org.id)` — `training.py:165`") **selbst leicht daneben**, es greift (a); das Urteil GRUEN bleibt richtig, kein Anker haengt daran (NOTIEREN, R6-4). `training_end` traegt zwar `g.org.id` **und** `g.user.id` im Rumpf — aber nur im `CLog(...)`-Konstruktor, also genau der Fall, den die Verschaerfung ausschliesst. Ohne die enge Fassung waere `training_end` faelschlich gruen **und** `methodik_uebertragen` ebenfalls (es traegt `coach_id=g.user.id`): die Regel dreht beide Richtungen korrekt.

Gegenprobe zur Helfer-Anerkennung: `routes/profiles.py::_rolle` traegt **keine** Server-Identitaet — er faellt durch Teil (b) und waere auch als Eintrag kein Freibrief. Die Anerkennung leitet wirklich ab.

## 4. S3 — `methodik_uebertragen` / `test_b02`, vierter Anlauf

**`test_b02`:** Die Antwort von `api_beenden` ist verifiziert `{'ok','filename','postcall','conv_id','call_id'}` (`routes/app_routes.py:946-952`), `call_id` = `str(_phase_d_call_id)`. Vor dem Fix uebernimmt `:275-276` die **gepostete** Kennung ungeprueft → `call_id == str(call_id_von_B)` → FAILED. Nach Plan 03 Task 3(c) haengt die Zuweisung hinter `ls.call_belongs_to(...)`; A hat keine eigene Sitzung, Stufe 2 findet nichts, `_bs` bleibt `None` → `call_id: None` → PASSED. Der Session-los-Guard `:234` greift nicht (`_posted_call_id` ist gesetzt), die `ConversationLog` fuer A entsteht unbedingt (`:419-457`) → `conv_id` ist **nicht** `None`, die zweite Streichung war ebenfalls richtig. **Der Anker beisst in beide Richtungen — erstmals.**

**R-8:** `routes/coach.py:171-174` traegt den Kommentar „Coach must own source profile's org" ohne Pruefung; `api_my_profiles` (`:204`) belegt, dass die eigenen Profile ueber `org_id=g.org.id` laufen — der Fix `src.org_id != g.org.id` ist semantisch richtig, und die Festlegung auf `g.org.id` (statt Entweder-Oder) ist am Code belegt. Der Waechter faengt ihn seit dem Replan **selbst** (S2), zusaetzlich zum Verhaltenstest. Zwei Schichten statt einer Namens-Schwarzliste — das ist der Ausweg aus dem Dreier-Muster, nicht der vierte Versuch derselben Sorte.

## 5. S4 — Nachzieh-Luecken: **hier sitzt der Fund**

Gemessen ueber alle vier Sweep-Dateien: **11** `messages.*`-Aufrufe, davon 10 anzufassen — ZT-15 = 10 und ZT-17 = 11/10 sind korrekt. `services/adoption_runner.py:281` und `services/judge_runner.py:382` existieren, importieren `claude_client` (`:35` / `:32`) und haben heute **kein** `timeout`. Plan 05 zieht sie korrekt in `SWEEP_DATEIEN`, setzt `MINDEST_LIVE_PFADE = 10` und nennt sie namentlich im `RESTLUECKEN`-Absatz.

**Plan 06 zieht nicht nach — an vier Stellen:**

- `files_modified` (Zeile 7-11) listet `services/adoption_runner.py` / `services/judge_runner.py` **nicht**
- Task 2, Import-Absatz: „Ganz oben in `services/claude_service.py` **und** `services/qa_pipeline.py`"
- Task 2, Kern-Anweisung (Zeile 396): „Die Ergaenzung an den **SIEBEN** blockierenden bzw. Stream-Aufrufen in `claude_service.py` **und der EINEN** in `qa_pipeline.py`"
- Acceptance: AST-Pruefung ueber nur zwei Dateien → `AUFRUFE 8`; die drei `grep`-Zaehler 5/2/1; `^import httpx` nur fuer zwei Dateien. **Kein einziger Anker beruehrt die zwei Runner-Dateien.** `<done>` sagt „Alle **acht** produktiven Live-LLM-Aufrufe".

Nur die Schnittstellen-Tabelle (Zeilen 95-96) und der Task-**Titel** sagen zehn. Ein Executor, der dem Aktions-Block folgt — wie der Plan-Satz es ueberall verlangt —, baut acht.

**Und Plan 05 Task 3 haengt am selben Nagel:** die ROT-Erwartung sagt „`test_jeder_live_aufruf_traegt_ein_zeitlimit` FAILED, listet **acht** Funktionen", das Acceptance-Kriterium „listet **acht** Funktionsnamen (sieben aus `claude_service.py` + `classify_utterance`)". Der Waechter listet nach dem Allowlist-Abzug **zehn**. Der Plan schreibt dazu selbst: „Weicht die Verteilung ab, wird **nicht weitergebaut**."

Weitere gepruefte Zahlen sind sauber: ZT-7 = 3 (`^ *'routes/.*::` == 3), ZT-2/3/4/5/6, `except anthropic.APITimeoutError` == 6 (4 Loop-Zweige + 2 Durchreicher, Plan 07 und Plan 08 stimmen ueberein), `_snap_s3` == 3 und `consume_ended_session_by_call_id` == 2 (per Zeilenfortsetzung — die R3-Korrektur war richtig), `pytest.ini` traegt genau `security|live|perf` mit 4er-Einzug.

## 6. S5 — Ausgaenge, die weder Verstoss noch Erfolg sind

- **`SKIPPED` / `ERROR` / Collection-Fehler:** abgedeckt, in beide Richtungen, mit dem F-B2-Umgebungsblock als Ursachen-Behandlung. ✅
- **Test wird gar nicht eingesammelt:** der Tabellen-Abgleich gegen `--collect-only` faengt das **nicht** (fehlt er ueberall, faellt nichts auf). Geschlossen wird es an anderer Stelle: Plan 01 Task 3 verankert **jeden** der zehn Testnamen einzeln per `grep -cF "def test_…" == 1` und zusaetzlich per AST-Dekorator-Abbild. Ein Tippfehler im Namen faellt dort. ✅
- **Lauf-Abbruch (KeyboardInterrupt / Prozess-Tod):** kein Summary, unvollstaendige Tabelle → der Abgleich schlaegt an. ✅
- **`xfail` / `xpass`:** in **keiner** Zeile der Verstoss-Tabelle. Kein Test dieser Phase traegt `xfail`, `pytest.ini` setzt kein `xfail_strict` — es ist eine theoretische Kante, kein offener Ausgang. → NOTIEREN (R6-5).

## 7. S6 — Freie Sicht: was dieser Plan-Satz nicht faengt

- **Der Waechter sieht Anwesenheit, nicht Wirkung.** `if sid_belongs_to(...): pass` bleibt gruen — steht ehrlich im `RESTLUECKEN`-Absatz, getragen vom Verhaltenstest.
- **`build_profile_context(user_id=A, sid=B)`** prueft weiterhin selbst nicht, ob beide zusammenpassen. Der zweite Aufrufer waere wieder offen, sobald er nicht in `routes/` sitzt. Steht als Rest-Kante im Threat-Register — richtig benannt, ungeloest.
- **Der Founder-Zaehler ist prozess-lokal.** „Soll 0" ist bei mehreren Gunicorn-Workern eine Aussage pro Worker. Das Label sagt es; die Abnahme in Plan 08 liest **einen** Prozess.
- **Die grosse Gegenfrage:** vier Waechter, zwei Verhaltenstests, ein Browser-Durchgang — aber **kein** Test faehrt zwei Sitzungen *gleichzeitig*. CLAUDE.md Punkt 28 verlangt fuer Live-Pfad-Aenderungen ausdruecklich einen deterministischen Zwei-Mandanten-Nebenlaeufigkeits-Test. Diese Phase aendert den Live-Pfad (Welle 2) und begruendet nirgends, warum sie ohne auskommt. Meine Lesart: sie darf es, weil Welle 2 keinen neuen Zustand einfuehrt — aber die Begruendung fehlt, und genau solche Luecken wachsen.

---

## 8. Befunde

### R6-1 — **(b)** · BLOCKER · Plan 06 (+ Plan 05 Task 3)
**Plan 06 baut R-10 nicht, obwohl der Waechter aus Plan 05 ihn fordert.**
Beleg: `08.23.2.SOFORT-2-06-PLAN.md:7-11` (`files_modified` ohne die zwei Runner), `:396` („SIEBEN … und der EINEN"), Acceptance-Block (AST ueber zwei Dateien → `AUFRUFE 8`, `grep`-Zaehler 5/2/1, `^import httpx` nur zweimal), `<done>` („Alle acht"). Dagegen `08.23.2.SOFORT-2-05-PLAN.md:401,406,557`: `SWEEP_DATEIEN` mit vier Dateien, `MINDEST_LIVE_PFADE = 10`. Am Code gemessen: 11 Aufrufstellen, `adoption_runner.py:281` und `judge_runner.py:382` ohne `timeout`, kein `httpx`-Import in `adoption_runner.py`.
**Wirkung:** `test_jeder_live_aufruf_traegt_ein_zeitlimit` bleibt nach Plan 06 rot → `deploy.sh production` (Plan 08) bricht am Test-Tor ab, kein Neustart. Der Bau steht.
**Fix (mechanisch, vier Zeilen):** in Plan 06 (a) `files_modified` um beide Runner ergaenzen, (b) „SIEBEN … EINEN" → „acht in `claude_service.py`/`qa_pipeline.py` plus je einen in `adoption_runner.py` und `judge_runner.py`", inkl. `import httpx` dort, (c) den AST-Anker auf dieselben vier Dateien erweitern (`AUFRUFE 10`), zwei `grep`-Zaehler `timeout=httpx.Timeout(config.LIVE_LLM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S),` == 1 je Runner-Datei ergaenzen, `<done>` auf zehn.

### R6-2 — **(c)** · BLOCKER · Plan 05 Task 3
**Die ROT-Erwartung ist unerfuellbar und wuerde bei korrektem Bau den ABBRUCH ausloesen.**
Beleg: `08.23.2.SOFORT-2-05-PLAN.md:63` („listet **acht** Funktionen") und `:94-95` („listet **acht** Funktionsnamen (sieben aus `services/claude_service.py` + `classify_utterance`)"). Der Sweep leitet ueber vier Dateien ab: 11 Fundstellen − 1 Allowlist = **zehn** Funktionen.
**Wirkung:** Der Anker ist nach der ZT-15-Erhoehung nachweislich wirkungslos — er misst eine Menge, die es nicht mehr gibt, und der einzige vom Plan angebotene Weg zurueck zu Gruen ist, die Zahl zu biegen. Genau die Fehlerklasse, gegen die der Umbau antrat.
**Fix:** `acht` → **zehn**, Klammer-Erlaeuterung auf „sieben aus `claude_service.py` + `classify_utterance` + `run_adoption_judge` + `run_behavior_judge`". `3 failed / 2 passed` bleibt richtig (fuenf Tests). Ebenso den Funktionsnamen `test_mindestens_acht_live_pfade_gefunden` in `test_mindestens_erwartete_live_pfade_gefunden` umbenennen — sonst zementiert der Name die alte Zahl (er steht in Plan 05 und Plan 08 je einmal).

### R6-3 — **NOTIEREN** · Plan 01 Task 2, `test_regeln_fangen_ein_kuenstliches_leck`
Der Plan teilt **die Mengen-Ableitung** in `_mengen_aus_quelltext` / `_mengen_aus_routes` (R2-5), lagert die **Regel** (org_id-Bindung / Server-Identitaet / Helfer-Anerkennung) aber nicht aus. Der Selbsttest muss sie also entweder aus einer geteilten Funktion ziehen — oder sie nachbauen. Baut er sie nach, prueft er eine Kopie und ist als einziger ROT-Beleg von Menge 5 wertlos: exakt der R2-5-Fehler, eine Ebene hoeher. **Ablageort:** Ein-Satz-Vorgabe fuer den Executor beim Bau von Task 2 (und Vermerk im 01-SUMMARY): *die Regel-Anwendung sitzt in einer eigenen Funktion `_verstoesse(mengen, quelltext_baum)`, die sowohl die vier Produktiv-Pruefungen als auch der Selbsttest rufen.* Kein Plan-Zyklus noetig.

### R6-4 — **NOTIEREN** · Plan 01 Task 2, Gegenprobe-Tabelle Menge 4, Zeile 4
Der Beleg fuer `training_start` nennt „(b) `filter_by(id=profile_id, org_id=g.org.id)` — `training.py:165`". Gemessen greift dort **(a)** (ein `ast.Compare`), nicht (b). Urteil GRUEN stimmt, kein Anker haengt daran. **Ablageort:** 09-SUMMARY / Fussnote beim Bau.

### R6-5 — **NOTIEREN** · Plan 02 Task 1, Verstoss-Tabelle
`xfail` / `xpass` haben keine Urteils-Zeile. Heute unerreichbar (kein Test dieser Phase traegt den Marker, `pytest.ini` setzt kein `xfail_strict`). **Ablageort:** eine Zeile in der Tabelle beim Bau ergaenzen — *„irgendwas + XFAIL/XPASS → VERSTOSS"* —, sonst 02-SUMMARY.

### R6-6 — **NOTIEREN** · Plan 01 Task 1, DIALOG-Text (`:424`) und `<done>` (`:543`)
Beide sagen noch „**ACHT** nicht gefixte Funde", waehrend die Tafel, der Abschnitt-2-Kopf, die Acceptance-Anker und Plan 08 durchgehend **sieben** sagen (ZT-11). `:424` liegt im woertlich zu uebernehmenden DIALOG-Eintrag — das Artefakt wuerde acht behaupten und sieben auflisten. Kein Anker bricht. **Ablageort:** Korrektur beim Bau von Task 1.

### R6-7 — **NOTIEREN** · Plan 01 Task 3, Seam-Tabelle, Zeile 2
Die Begruendung nennt „`api_beenden` durchlaeuft `analysiere_postcall` (Sonnet) … `routes/app_routes.py:450`". `analysiere_postcall` kommt in `routes/app_routes.py` **nicht vor** (0 Treffer); `:450` liegt im `ConversationLog(...)`-Konstruktor. Ebenso ist der `generate_crm_export`-Aufruf (`:351`) in ein `try/except` gehuellt — eine Ausnahme dort stoppt den Fluss **nicht**, der Kostengrund (echter Sonnet-Aufruf bei jedem Deploy) bleibt aber vollstaendig gueltig. Alle vier Seams sind trotzdem richtig gefordert. **Ablageort:** 01-SUMMARY.

### R6-8 — **NOTIEREN** · Plan 01 Task 3, Seam-Begruendung `recherche_firma`
Die Begruendung („bricht ohne `BRAVE_SEARCH_API_KEY` ab, **bevor** sie schreibt") ist wackelig: `services/precall_service.py` ruft `set_briefing_for_sid` an **zwei** Stellen (`:236` und `:273`), die Key-Pruefung sitzt bei `:248-249` — also zwischen beiden. Der Seam bleibt richtig und Pflicht (er garantiert den Schreibversuch), nur die Herleitung trifft nicht ganz. **Ablageort:** 01-SUMMARY.

### R6-9 — **NOTIEREN** · Plan 02 Task 1, ROT-Lauf
`pytest.ini` wird nicht mit hochgelegt; auf Production fehlt `rot_vor_fix` noch → der ROT-Lauf erzeugt `PytestUnknownMarkWarning` je markiertem Test. Funktional harmlos (`-m rot_vor_fix` selektiert auch unregistrierte Marker, `--strict-markers` ist nicht gesetzt), aber es ist genau das Rauschen, das Plan 01 mit der Registrierung vermeiden wollte. **Ablageort:** ein Satz im 02-SUMMARY („Warnungen erwartet, `pytest.ini` geht erst mit Plan 04 raus").

### R6-10 — **NOTIEREN** · phasenweit, Punkt 28
Kein deterministischer Zwei-Mandanten-**Nebenlaeufigkeits**-Test, obwohl Welle 2 den Live-Pfad anfasst und CLAUDE.md Punkt 28 ihn fuer Live-Pfad-Aenderungen als Verify-Pflicht nennt. Vertretbar (kein neuer geteilter Zustand; der per-sid-Zaehler ist getestet), aber **nirgends begruendet**. **Ablageort:** ein Absatz im 08-SUMMARY unter den Grenzen.

---

## 9. Was dieser Plan-Satz NICHT faengt

1. **Ob das Ergebnis einer Besitzpruefung beachtet wird** — nur, dass sie gerufen wird (benannt).
2. **Kennungen mit anderem Namen** (`pid`, `event_id`, `ziel_org_id`) — Katalog-Grenze, per Gegen-Sweep als heute sauber belegt, aber offen fuer neuen Code (benannt).
3. **Socket.IO** (`services/deepgram_service.py:963/:978`, R-9) — faellt durch alle fuenf Mengen, gemeldet, eigene Mini-Phase.
4. **Den WERT der Zeitlimits** — nur ihre Anwesenheit; getragen allein von D-06 (ein einziger Test-Anruf, ±40 %-Band).
5. **Token-Abstaende im Stream** — es gibt keine Messung; die 8-s-Stillstands-Grenze ist begruendet geschaetzt, nicht belegt (ehrlich als A3 offengelegt).
6. **Einen fuenften Live-LLM-Pfad ausserhalb der vier Sweep-Dateien** — und, siehe R6-1, aktuell nicht einmal die vierte Datei im Bau.
7. **Kein DB-Netz unter `public.calls`** — der `WHERE`-Filter ist die einzige Kontrolle; die Waechter pruefen ihn statisch und einmalig im Verhalten, nicht dauerhaft zur Laufzeit.

---

noch ein Zyklus, weil (b)

---

# Zweiter Durchgang — Claudian-Nachpruefung (unabhaengig, am Code gemessen)

Dieser Durchgang lief **vor** dem Lesen des Claude-CLI-Reviews und hat die zwei
mechanisch zaehlbaren Schwerpunkte per eigenem AST-Skript nachgefahren, statt sie zu
schaetzen.

## S2 — Die enge `org_id`-Regel: wie viele Falsch-Treffer wirklich?

Regel implementiert wie in Plan 01 spezifiziert ((a) `ast.Compare` mit `org_id` gegen eine
Server-Identitaet, **oder** (b) Lese-/Filter-Aufruf mit `org_id=<Server-Identitaet>`;
Konstruktoren zaehlen **nicht**), angewandt auf **alle sechs** Funktionen der Menge 4:

```
GRUEN  routes/app_routes.py::api_set_profile
       [b] :1343  db.query(ProfileModel).filter_by(id=pid, org_id=g.org.id)
ROT    routes/coach.py::methodik_uebertragen
       (kein org_id-Treffer im Rumpf - die zwei org_id=ziel_org haengen an einem
        ANFRAGE-Wert, nicht an g.*)
GRUEN  routes/profiles.py::api_profile_preview_context
       [a] :814  profile.org_id != g.user.org_id
GRUEN  routes/training.py::training_start
       [a] :208  TrainingScenario.org_id == g.org.id
       [b] :165  db.query(Profile).filter_by(id=profile_id, org_id=g.org.id)
ROT    routes/training.py::training_end
       [KONSTRUKTOR] :712  CLog(user_id=g.user.id, org_id=g.org.id, ...)
GRUEN  routes/training.py::api_training_personality_generate
       [b] :927  db.query(Profile).filter_by(id=pid, org_id=g.user.org_id)
```

**Ergebnis: genau ZWEI rot — ein echter Fund (`methodik_uebertragen` = R-8) und ein
Falsch-Treffer (`training_end`, allowlistet).** Damit ist **ZT-7 = 3 richtig** und Plan 02s
Zeile „6 − 1 Allowlist = 5 geprueft, davon 1 Verstoss" stimmt exakt.

⚠ **Die Regel dreht beide Richtungen korrekt:** ohne die enge Fassung waere `training_end`
faelschlich gruen (Konstruktor-Schreibvorgang als Besitznachweis gelesen) **und**
`methodik_uebertragen` ebenfalls (es traegt `coach_id=g.user.id`). Das war genau der
Doppelfehler aus den ersten drei Anlaeufen.

## Menge 5 (ZT-6 = 8) mit der helfer-bewussten Regel

```
GRUEN  app_routes.py::api_calls_correct_outcome   [identitaet] :2176 g.user.id
GRUEN  dashboard.py::session_detail               [identitaet] :749  g.user.id
GRUEN  profiles.py::api_faqs_list                 [helfer]     :623  _require_own_profile(...)
GRUEN  profiles.py::api_faqs_create               [helfer]     :664  _require_own_profile(...)
GRUEN  profiles.py::api_tabu_update               [helfer]     :775  _require_own_profile(...)
GRUEN  profiles.py::skript_bearbeiten             [identitaet] :273  g.org.id
GRUEN  profiles.py::skript_loeschen               [identitaet] :299  g.org.id
GRUEN  training.py::training_scenarios_delete     [identitaet] :1087 g.org.id
```

**Alle acht gruen, null Falsch-Treffer.** Damit ist belegt: (1) die Helfer-Bewusstheit aus
F-B1 wirkt — die drei `_require_own_profile`-Aufrufer waeren ohne sie **faelschlich rot**;
(2) die Marker-Zeile „`test_kennung_aus_url_wird_besitzgeprueft` traegt **keine**
Markierung" ist richtig, weil Menge 5 am Bestand gruen ist.

**Antwort auf S2 in einem Satz: genau EIN Falsch-Treffer insgesamt, und er ist begruendet
allowlistet.** Kein weiterer Anker reisst.

## S1 — Die neun Marker-Zeilen

Unabhaengig nachgefahren, deckungsgleich mit dem Claude-CLI-Ergebnis. Besonders geprueft:

- Zeile 1 (`…mindestens_erwartete_pruefpunkte…`, **keine** Markierung): Roh-Zahlen vor dem
  Fix **5/2/5/6/8** gegen Mindest-Soll **3/2/5/6/8** — mit `>=` gruen. Richtig, und der
  Grund stimmt (die Soll-Werte sind die **Nach**-Fix-Werte, damit die Pruefung ihren eigenen
  Fix ueberlebt).
- Zeilen 2/3/4/8 (`rot_vor_fix`): die drei Besitz-Helfer existieren zum ROT-Zeitpunkt
  **nicht** (Plan 02 Task 1 laeuft vor Task 2) → zwangslaeufig rot. **Aus dem richtigen
  Grund:** die Fehlermeldung nennt die fehlende Besitzpruefung, nicht einen Importfehler.
- Zeile 5 (`…profile_id…`, `rot_vor_fix`): rot wegen `methodik_uebertragen` — oben am Code
  belegt.
- Zeile 6 (`…kennung_aus_url…`, **keine**): gruen, oben am Code belegt.
- Zeile 9 (`…allowlist_ist_begruendet_und_lebt`, **keine**): alle drei Ziele existieren
  (`_load_beenden_state`, `api_calls_latest_outcome`, `training_end`) → gruen.

## S4 — Nachzieh-Luecken: der Fund ist meiner

**R6-1 und R6-2 sind beide Nachzieh-Luecken der R-10-Aufnahme, die ich selbst gemacht
habe.** Ich hatte in Plan 06 die Schnittstellen-Tabelle (Zeilen 10/11) und den Task-Titel
geaendert, aber **nicht** den Aktions-Block, `files_modified`, die Acceptance-Anker und
`<done>` — und in Plan 05 Task 3 nicht die ROT-Erwartung.

**Wirkung, wenn es so gebaut worden waere:** Plan 05 baut einen Waechter, der **zehn**
Aufrufe mit Zeitlimit fordert; Plan 06 haette **acht** gebaut; das Deploy-Tor in Plan 08
faellt rot; der Bau steht. Genau Klasse **(b)**. Und Plan 05 Task 3 haette den ABBRUCH
ausgeloest, weil die ROT-Erwartung eine Menge misst, die es nicht mehr gibt — Klasse **(c)**.

**Das ist die dritte Wiederholung derselben Fehlerklasse in dieser Phase** (ZT-7, ZT-11,
jetzt ZT-15/ZT-17). Die Tafel-Regel *„wer eine Zahl aendert, greppt danach ueber alle
Plan-Dateien"* ist offensichtlich nicht dadurch durchsetzbar, dass sie im Plan steht.
**Lehre fuer den Bau, nicht fuer einen weiteren Zyklus:** jede Zahlen-Aenderung wird ab
sofort mit dem Grep **im selben Arbeitsschritt** abgeschlossen, nicht danach.

Nach der Korrektur zusaetzlich gefunden und mitgezogen: vier weitere Fundstellen der alten
Zahl (`05:584`, `05:704`, `05:742`, `08:260`), die der Reviewer nicht genannt hatte.

---

# Was behoben wurde (nur die zwei qualifizierenden Funde)

**R6-1 (b) — Plan 06:**
`files_modified` um `services/adoption_runner.py` + `services/judge_runner.py` ergaenzt ·
Kern-Anweisung von „SIEBEN … und der EINEN" auf „ZEHN Aufrufe" mit beiden Runnern
namentlich · **`import httpx` in beiden Runnern ausdruecklich gefordert** (heute hat ihn
keiner von beiden — ohne ihn stirbt der Modul-Import) · AST-Anker auf **alle vier**
Sweep-Dateien erweitert (`AUFRUFE 10`) · zwei neue `grep`-Anker fuer das Zeitlimit je
Runner-Datei · zwei neue `grep`-Anker fuer `^import httpx` · `<done>`, Threat-Model-Zeile
und Verification auf zehn.

**R6-2 (c) — Plan 05 Task 3:**
ROT-Erwartung „listet acht Funktionen" → **zehn**, an beiden Stellen ·
`test_mindestens_acht_live_pfade_gefunden` → **`test_mindestens_erwartete_live_pfade_gefunden`**
(der alte Name zementierte die Zahl im Bezeichner) · drei weitere Fundstellen der alten
Zahl in Plan 05 und eine in Plan 08 mitgezogen.

# Was NOTIERT wird (nicht behoben, mit Ablageort)

| Fund | Inhalt | Ablageort |
|---|---|---|
| **R6-3** | Die **Regel-Anwendung** ist nicht ausgelagert (nur die Mengen-Ableitung). Der Selbsttest muss sie sonst nachbauen und prueft dann eine Kopie — R2-5 eine Ebene hoeher | Ein-Satz-Vorgabe beim Bau von Plan 01 Task 2: Regel in `_verstoesse(mengen, baum)`, die Produktiv-Pruefungen **und** Selbsttest rufen; Vermerk im 01-SUMMARY |
| **R6-4** | Beleg fuer `training_start` nennt (b), am Code greift (a). Urteil GRUEN stimmt, kein Anker haengt daran | Fussnote beim Bau, 01-SUMMARY |
| **R6-5** | `xfail`/`xpass` haben keine Urteils-Zeile in der Verstoss-Tabelle. Heute unerreichbar (kein Test traegt den Marker, kein `xfail_strict`) | Eine Zeile in der Tabelle beim Bau ergaenzen, sonst 02-SUMMARY |
| **R6-6** | Plan 01 DIALOG-Text + `<done>` sagen noch „acht nicht gefixte Funde", Tafel/Anker sagen sieben (ZT-11). Kein Anker bricht | Korrektur beim Bau von Task 1 |
| **R6-7** | Seam-Begruendung nennt `analysiere_postcall` in `app_routes.py` — kommt dort nicht vor; `generate_crm_export` steht in `try/except`. **Alle vier Seams bleiben richtig gefordert**, nur die Herleitung trifft nicht | 01-SUMMARY |
| **R6-8** | Seam-Begruendung `recherche_firma`: `set_briefing_for_sid` wird an **zwei** Stellen gerufen (`:236`, `:273`), die Key-Pruefung sitzt dazwischen (`:248`). Seam bleibt Pflicht, Herleitung wackelt | 01-SUMMARY |
| **R6-9** | ROT-Lauf erzeugt `PytestUnknownMarkWarning` (die `pytest.ini` mit `rot_vor_fix` geht erst mit Plan 04 raus). Funktional harmlos | Ein Satz im 02-SUMMARY |
| **R6-10** | **Kein deterministischer Zwei-Mandanten-Nebenlaeufigkeits-Test**, obwohl Welle 2 den Live-Pfad anfasst und CLAUDE.md Punkt 28 ihn dafuer verlangt. Vertretbar (Welle 2 fuehrt keinen neuen geteilten Zustand ein), aber **nirgends begruendet** | Ein Absatz im 08-SUMMARY unter „Grenzen" |

⚠ **R6-10 ist der wertvollste der notierten Funde.** Er ist kein Fehler im Plan, sondern
eine **unbegruendete Auslassung** gegenueber einer Projekt-Hartregel. Beim naechsten
Live-Pfad-Bau ist er die erste Frage.

---

# Urteil

Beide qualifizierenden Funde sind eingearbeitet, die acht uebrigen sind mit Ablageort
notiert. Die Zahlen-Gegenprobe ueber alle neun Plan-Dateien ist sauber (ZT-7 = 3,
ZT-11 = 7, ZT-15 = 10, ZT-16 = 2, ZT-17 = 11/10), alle neun Frontmatter validieren, alle
neun `<threat_model>`-Bloecke sind intakt.

**baureif**
