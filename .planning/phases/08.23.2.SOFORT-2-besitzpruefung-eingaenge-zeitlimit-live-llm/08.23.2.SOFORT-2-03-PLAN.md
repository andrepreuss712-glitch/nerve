---
phase: 08.23.2.SOFORT-2
plan: 03
type: execute
wave: 3
depends_on: ["08.23.2.SOFORT-2-02"]
files_modified:
  - routes/app_routes.py
  - routes/learning.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Eine geposteten fremde call_id loest in /api/beenden keinen fremden Zustand mehr auf"
    - "Eine fremde sid im Query-String liefert leere Template-Variablen statt fremder Kundendaten"
    - "POST /api/precall/research mit fremder sid antwortet 404 und schreibt NICHTS in die fremde Sitzung"
    - "Der outcome_ready-Emit erreicht nur den PiP des eigenen Beraters"
    - "Der calls-UPDATE filtert an BEIDEN Stellen auf user_id — der zweite, unabhaengige Weg zu einer geposteten call_id ist mitgeschlossen"
  artifacts:
    - path: "routes/app_routes.py"
      provides: "B-01, B-02, B-03, N-01 geschlossen"
      contains: "resolve_own_sid_by_call_id"
    - path: "routes/learning.py"
      provides: "N-02, N-03 geschlossen"
      contains: "resolve_own_sid_by_call_id"
  key_links:
    - from: "routes/app_routes.py"
      to: "services/live_session.py"
      via: "ls.resolve_own_sid_by_call_id / ls.sid_belongs_to"
      pattern: "sid_belongs_to|resolve_own_sid_by_call_id"
    - from: "routes/app_routes.py"
      to: "public.calls"
      via: "SQLAlchemy-Filter mit _CallModel.user_id == g.user.id"
      pattern: "_CallModel\\.user_id == g\\.user\\.id"
---

**Komplexitaet: 🔴** — Cross-AI-Review ist Pflicht VOR Execute.

<objective>
Der Fix. Sechs Eingaenge, fuenf davon ueber den Helfer aus Plan 02, einer (B-01) als
DB-Filter.

Purpose: Die Ghost-SID-Guards im Repo pruefen **Lebendigkeit**, nicht **Eigentuemerschaft**.
Nach diesem Plan gibt es in `routes/` keine Zustands- oder Anruf-Aufloesung mehr, die eine
Kennung aus der Anfrage ohne Besitz-Vergleich verwendet.

Output: `routes/app_routes.py` (4 Stellen), `routes/learning.py` (2 Stellen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-CONTEXT.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-RESEARCH.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-02-SUMMARY.md
@CLAUDE.md
@routes/CLAUDE.md
@routes/app_routes.py
@routes/learning.py
@services/live_session.py
</context>

## Scope-Begruendung — warum SECHS Eingaenge HIER und zwei weitere in Plan 09

Der Roadmap-Eintrag nennt B-01/B-02/B-03. Der Pflicht-Sweep aus D-02 fand drei weitere Eingaenge
**derselben Fehlerklasse**, und der Cross-AI-Replan vom 2026-08-05 zwei weitere (R-7, R-8).
Entscheidung: **alle acht werden gefixt** (Zahlen-Tafel ZT-10 in Plan 01) — die sechs
RAM-/`calls`-Faelle **hier**, die zwei DB-Fremdreferenz-Faelle in **Plan 09** (andere Dateien,
andere Schicht, deshalb ein eigener Plan statt eines fuenf-Task-Ungetuems). Begruendung, Fund
fuer Fund:

| Fund | gefixt? | Begruendung |
|---|---|---|
| **N-01** `POST /api/precall/research` | **JA** | Der einzige Fund mit **Schreibzugriff in eine fremde LAUFENDE Sitzung**: Konto A schreibt beliebigen Text in `_session_state[sid_von_B]['_briefing']`, und dieser Text geht als Abschnitt „PreCall-Briefing" in den System-Prompt **jedes** Live-LLM-Aufrufs von B (`services/prompt_pipeline.py`). Das ist Prompt-Injektion in ein laufendes fremdes Verkaufsgespraech. Im selben Aufruf liest A ueber `build_profile_context(user_id=<A>, sid=<B>)` Bs Profil, Opener und FAQs und bekommt sie in der Antwort zurueck. **Schwerer als B-01 bis B-03 zusammen.** Ihn zu melden und offen zu lassen, waehrend drei schwaechere Tueren geschlossen werden, waere nicht erklaerbar — und der Fix ist **eine Bedingung**, dieselbe Groessenordnung wie B-03. |
| **N-02 / N-03** `outcome_ready`-Emit | **JA** | Zwei **identische** Zeilen in `routes/learning.py`. Der `calls`-UPDATE direkt darueber filtert bereits korrekt auf `Call.user_id == g.user.id`; nur die Raum-Auswahl darunter nicht — derselbe Zwei-Niveau-Fehler wie in B-03. Die Verwaltung einer Folgephase kostet mehr als der Fix. Zudem: der Fix ist **derselbe Helfer**, den B-02 ohnehin bekommt. |
| **R-7** `routes/crm_export.py::save_meeting` — geposteten `call_id` wird ungeprueft `Meeting.call_id` | **JA — in Plan 09** | ⚠ **Die urspruengliche Vertagung ist am 2026-08-05 von Andre aufgehoben worden.** Begruendung: die Spalte hat heute **keinen Leser**, also heute kein Leck — und genau deshalb ist Vertagen die **teure** Variante: ein nur-einfuegender Haken **ohne** Pruefung produziert **stillen Muell**, der spaeter nicht mehr von echten Daten unterscheidbar ist (Vault-Leitplanke 4: *verpasste Felder sind fuer immer weg*). Die befuerchtete Schicht-Huerde ist gemessen und klein: `public.calls` traegt **keine** RLS und `nerve_app` hat `SELECT` darauf (Production, 2026-08-05). ⛔ **In diesem Plan wird `routes/crm_export.py` trotzdem NICHT angefasst** — er gehoert zu Plan 09. Steht die Datei im Diff dieses Plans, ist das ein Reparatur-Modus-Verstoss |
| **R-8** `routes/coach.py::methodik_uebertragen` — geposteten `profile_id` wird ungeprueft zur Quelle einer Profil-Kopie | **JA — in Plan 09** | Neu gefunden beim Nachziehen des Waechters (Menge `PROFILEID_AUS_ANFRAGE`). Der Kommentar im Code sagt „Coach must own source profile's org" — **die Pruefung fehlt**, geprueft wird nur die **Ziel**-Org. Heute unerreichbar (0 Coaches / 0 Zuweisungen auf Production, gemessen 2026-08-05), Fix = **eine Bedingung**. ⛔ `routes/coach.py` wird in **diesem** Plan nicht angefasst |
| **R-1 bis R-6** (toter Code, `sid` im Query-String, Farb-Verstoss, veraltete Selbstverweise, Pfad-Traversal-Kante, Retry-Verlust der Post-Call-Runner) | **NEIN** | Reparatur-Modus: andere Fehlerklassen bzw. unerreichbarer Code. **Gemeldet mit Ablageort** in `08.23.2.SOFORT-2-FUNDE.md` Abschnitt 2 + DIALOG-Eintrag (Plan 01 Task 1). |

⚠ **P-4 — die Falle, die diesen Plan dreiteilt:** `routes/app_routes.py:275-276`
(`if not _phase_d_call_id and _posted_call_id: _phase_d_call_id = _posted_call_id`) ist ein
**zweiter, unabhaengiger** Weg, wie eine gepostete Fremd-`call_id` zum `calls`-UPDATE wird — er
laeuft an der SID-Aufloesung komplett vorbei. **Wer nur B-02 fixt, hat B-01 offen gelassen und
haelt es fuer erledigt.** Deshalb ist B-01 ein eigener Task (Task 3) mit eigenem Beweis.

## Antwortform je Eingang — BENANNTE FESTLEGUNG

D-05/D-06 sagen „403/404". RESEARCH §1.6 mahnt zu Recht: ein Test, der „403 **oder** 404"
akzeptiert, prueft weniger als er scheint. **Deshalb legt dieser Plan je Eingang genau EIN
Verhalten fest.**

| Eingang | Verhalten bei Fremdbesitz | Begruendung |
|---|---|---|
| **N-01** `/api/precall/research` | **HTTP 404**, `{'error': 'Unbekannte Sitzung'}` | Der Client schickt die `sid` **bewusst** mit. Sie still zu verwerfen wuerde ihm ein Briefing liefern, das in seinem eigenen PiP nie ankommt — ein stiller Ausfall fuer den ehrlichen Nutzer. **404 statt 403**, weil 403 bestaetigt, dass die Kennung existiert. |
| **B-03** `/api/gatekeeper/phrases` | **HTTP 200**, `variables` bleiben leer | Die `sid` ist dort **optional** (Rueckfall auf `flask_session.get('sid')`), und die Funktion hat bereits einen sauberen Leerpfad: Platzhalter bleiben stehen. Ein 404 wuerde legitime Aufrufe ohne aktive Sitzung brechen. |
| **B-02** `/api/beenden` Stufe 1 | **kein Statuswechsel** — `_beenden_sid` bleibt `None`, Stufe 2 (eigener `user_id`-Scan) greift | Der Endpunkt hat bereits eine korrekte zweite Stufe. Ein 404 waere eine Verhaltensaenderung fuer den Normalfall. |
| **B-01** `calls`-UPDATE | **kein Statuswechsel** — 0 Zeilen, kein UPDATE, **laute** Protokollzeile | Der Endpunkt hat andere Aufgaben (Log speichern, Antwort bauen). Heute laeuft er bei `_call_row is None` **still** durch; das bleibt, wird aber protokolliert. |
| **N-02 / N-03** `outcome_ready` | **kein Statuswechsel** — der Emit unterbleibt | Der bestehende `else`-Zweig („KEINE aktive SID → SKIP emit") ist bereits der richtige Leerpfad. |

⚠ **Abweichung vom CONTEXT, ausdruecklich benannt:** D-05/D-06 verlangen woertlich „403/404
erwarten". **Nur N-01 liefert einen 404.** Die anderen fuenf setzen „Fremdzugriff wirkt wie nicht
vorhanden" um — das ist gegenueber 403 **staerker** (kein Existenz-Leck) und gegenueber 404
verhaltens-neutral fuer legitime Nutzer. Der Verhaltens-Waechter aus Plan 01 Task 3 prueft
entsprechend **pro Eingang** die hier festgelegte Aussage, nicht pauschal einen Statuscode.

<interfaces>
<!-- Aus dem Code extrahiert (2026-08-04, alle Zeilen per grep -n belegt) — KEIN Erkunden noetig. -->

**Der Helfer aus Plan 02** (`services/live_session.py`):
```python
def sid_belongs_to(sid, user_id) -> bool
def resolve_own_sid_by_call_id(call_id, user_id)   # -> sid | None
```

**In `routes/app_routes.py` ist `live_session` als `ls` importiert** — belegt durch die
bestehenden Aufrufe `ls._session_state_lock` / `ls._session_state`. In `routes/learning.py`
ebenso (`ls._session_state_lock`). **Kein neuer Import noetig** — vor dem Bau per
`grep -n "import services.live_session as ls\|from services import live_session as ls" routes/app_routes.py routes/learning.py`
bestaetigen.

**Ist-Zustand B-02** (`routes/app_routes.py:181-195`):
```python
        _my_uid = g.user.id
        # Stufe 1: exakte Aufloesung via posted call_id (S3)
        if _posted_call_id:
            with ls._session_state_lock:
                for _s, _sd in ls._session_state.items():
                    if _sd.get('state', {}).get('call_id') and \
                       str(_sd['state']['call_id']) == str(_posted_call_id):
                        _beenden_sid = _s
                        break
            # Auch im Snapshot suchen (B1 — fuer den Fall dass :779 schon gestasht hat)
            if _beenden_sid is None:
                _snap_s3 = ls.consume_ended_session_by_call_id(_posted_call_id) \
                    if hasattr(ls, 'consume_ended_session_by_call_id') else None
                if _snap_s3 is not None:
                    _beenden_sid = _snap_s3
```
⚠ Der `_snap_s3`-Zweig ist **toter Code**: `grep -rn "def consume_ended_session_by_call_id" services/`
→ **0 Treffer**, `hasattr` ist immer `False`. Er wird **nicht angefasst** (Reparatur-Modus,
gemeldet als R-1).

**Ist-Zustand N-01** (`routes/app_routes.py:1562-1572`):
```python
    sid = (data.get('sid') or '').strip() or None
    user_id = g.user.id if g.user else None
    profile_id = flask_session.get('active_profile_id')

    briefing, error = recherche_firma(
        firmenname, ansprechpartner, branche,
        profil_daten=profil_daten, user_id=user_id, profile_id=profile_id, sid=sid,
    )
```

**Ist-Zustand B-03** (`routes/app_routes.py:2076-2085`):
```python
        sid = request.args.get('sid') or flask_session.get('sid')
        variables = {'branche': '', 'detail': '', 'vorname': '', 'nachname': ''}
        if sid:
            from services.live_session import _session_state, _session_state_lock
            with _session_state_lock:
                st = _session_state.get(sid, {}).get('state', {})
                briefing = st.get('active_profile_data') or st.get('_briefing') or {}
            if isinstance(briefing, dict):
                variables['branche'] = briefing.get('branche', '') or ''
                variables['detail'] = briefing.get('detail', '') or briefing.get('produkt', '') or ''
```

**Ist-Zustand N-02/N-03** (`routes/learning.py:148-153` und `:315-320`, **wortgleich**):
```python
                    _sid_for_emit = None
                    with ls._session_state_lock:
                        for _sid, _sd in ls._session_state.items():
                            if str(_sd.get('state', {}).get('call_id')) == str(_posted_call_id):
                                _sid_for_emit = _sid
                                break
```

**Ist-Zustand B-01** (`routes/app_routes.py:782` und `:827-829`):
```python
            _call_row = _db_calls.query(_CallModel).filter(_CallModel.id == _phase_d_call_id).first()
...
                _row_heal = _db_heal.query(_CallModel).filter(
                    _CallModel.id == _phase_d_call_id
                ).first()
```
**Das korrekte Vorbild steht 20 Zeilen darueber** (`:759-764`, DB-Fallback) und in
`_audio_health_bg` (`_CallAH.user_id == user_id_val`).
</interfaces>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
- `public.calls` — **schreibend** (B-01, zwei UPDATE-Stellen; geaendert wird die
  `WHERE`-Bedingung, nicht die geschriebenen Felder)
- `public.conversation_logs` — **lesend**, mittelbar ueber B-02 (`conversation_log_id`)
- `public.profiles` — **lesend**, mittelbar ueber N-01 (`build_profile_context`)
- `_session_state` (RAM) — lesend + schreibend, ueber den Helfer

### inspect.sh-Beleg (Production, read-only, 2026-08-04) — verbatim

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
                                    Table "public.calls"
        Column         |            Type             | Collation | Nullable |    Default
-----------------------+-----------------------------+-----------+----------+---------------
 id                    | uuid                        |           | not null |
 tenant_id             | uuid                        |           |          |
 account_id            | uuid                        |           |          |
 contact_id            | uuid                        |           |          |
 user_id               | integer                     |           | not null |
 call_mode             | text                        |           | not null |
 call_type             | text                        |           |          |
 started_at            | timestamp with time zone    |           |          |
 ended_at              | timestamp with time zone    |           |          |
 transcript_storage    | text                        |           |          |
 transcript_expires_at | timestamp with time zone    |           |          |
 call_summary          | text                        |           |          |
 outcome               | text                        |           |          |
 audio_health_score    | double precision            |           |          |
 coaching_score        | double precision            |           |          |
 meddpicc_extracted    | jsonb                       |           |          |
 created_at            | timestamp without time zone |           |          |
 conversation_log_id   | integer                     |           |          |
 outcome_confidence    | double precision            |           |          |
 outcome_note          | text                        |           |          |
 outcome_source        | text                        |           |          |
 followup_intent       | text                        |           | not null | 'none'::text
 score_breakdown       | jsonb                       |           |          |
 score_schema_version  | smallint                    |           | not null | '1'::smallint
 audio_health_resolved | boolean                     |           | not null | false
 transcript_resolved   | boolean                     |           | not null | false
Indexes:
    "calls_pkey" PRIMARY KEY, btree (id)
    "idx_calls_account_time" btree (account_id, started_at)
    "idx_calls_mode_outcome" btree (call_mode, outcome) WHERE outcome IS NOT NULL
    "idx_calls_user_time" btree (user_id, started_at)
Foreign-key constraints:
    "calls_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id)
    "fk_calls_conversation_log_id" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id)
```

Der zusaetzliche Filter `user_id = ?` ist **kein Index-Problem**: `idx_calls_user_time` liegt auf
`(user_id, started_at)`, und der Primaerschluessel-Zugriff ueber `id` bleibt die fuehrende
Bedingung. Latenz-Wirkung: **null** (der Filter wird auf einer bereits per PK gefundenen Zeile
ausgewertet).

### Cross-Layer-Konsistenz-Tabelle

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|---|
| `calls.user_id` | **neu gelesen** als Filter (B-01, 2 Stellen) | DB-Spalte `public.calls.user_id` (integer, **not null**) | beim Anlegen des Calls (`create_call_for_sid`) | **Nein** — `not null`, existiert ab Zeile 1 | ✓ inspect.sh oben |
| `calls.ended_at` / `conversation_log_id` / `call_mode` / `score_breakdown` / `transcript_resolved` | geschrieben (unveraendert) | DB-Spalten, alle vorhanden | `api_beenden` | — (Schreiber) | ✓ inspect.sh oben |
| `_session_state[sid]['user_id']` | gelesen ueber den Helfer | **RAM**, keine DB-Spalte | synchron in `init_session_state` beim Verbindungsaufbau | **Nein** | ✓ Plan 02, Abschnitt 5 |
| `_session_state[sid]['_briefing']` | vor dem Fix von N-01 **fremd beschreibbar** | **RAM**, keine DB-Spalte | `set_briefing_for_sid` aus `precall_service` | — | ✓ `grep -n "def set_briefing_for_sid" services/live_session.py` → `:418` |

### ⚠ Kein DB-Netz darunter
`public.calls`, `public.conversation_logs` und `public.profiles` haben auf Production **KEINE
RLS** (belegt per `psql` als `postgres`, 2026-08-04 — nur `crm.*` traegt `rls=true force=true`).
**Der Filter im Query ist die einzige Kontrolle.** Faellt er wieder raus, faengt darunter nichts.

### Punkt 23 (Schild) — geprueft, NICHT faellig
Das Schild von `public.calls` nennt `routes/app_routes.py (api_beenden)` bereits als Schreiber
(verbatim in Plan 01, Abschnitt 5). Diese Phase fuegt **keinen neuen Schreiber**, **keinen neuen
Leser** hinzu und aendert den **Mechanismus** nicht — nur die `WHERE`-Bedingung. Schild bleibt
unveraendert.

## Vier Saeulen (Bau-Regel D)
- **Automatisieren:** Die Besitzpruefung entscheidet, nicht Andre.
- **Nachvollziehen:** B-01 protokolliert den abgelehnten Fremdzugriff **laut** (heute: stiller
  Durchlauf); der Helfer protokolliert die fuenf Zustands-Faelle.
- **Eingreifen:** Die Antwortform je Eingang ist eine benannte Festlegung in diesem Plan.
- **Marge:** Eine Datentrennungs-Panne vor dem EA-Launch kostet mehr als jede Handarbeit.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

<tasks>

<task type="auto">
  <name>Task 1: routes/app_routes.py — B-02, N-01, B-03 ueber den Helfer</name>

  <read_first>
    - `routes/app_routes.py` Z.175-220 (B-02 im Ist-Zustand **plus** das korrekte Stufe-2-Vorbild
      `:198-201`, das hier NICHT angefasst wird — es ist die Vorlage), Z.1534-1580 (N-01),
      Z.2053-2100 (B-03)
    - `services/live_session.py` — die zwei neuen Funktionen aus Plan 02 (Signatur + Rueckgabe
      bei Fremdbesitz)
    - `services/precall_service.py` — die Kette `recherche_firma(sid=…)` →
      `set_briefing_for_sid` → `build_profile_context(user_id=…, sid=…)`; per
      `grep -n "set_briefing_for_sid\|build_profile_context" services/precall_service.py`
      lokalisieren (D-01 — die Zeilennummern aus der RESEARCH sind eine Momentaufnahme)
    - `routes/CLAUDE.md` (Blueprint-Namen, `url_for`-Verifikation)
    - Der Abschnitt „Antwortform je Eingang — BENANNTE FESTLEGUNG" **oben in diesem Plan**
    - CLAUDE.md → „Acht weitere Vault-Regeln", Punkt 5 (Reparatur-Modus: nur der Fehler)
  </read_first>

  <files>routes/app_routes.py</files>

  <action>
**(a) B-02 — Stufe-1-SID-Aufloesung in `api_beenden`.**
Such-Muster: `grep -n "str(_sd\['state'\]\['call_id'\]) == str(_posted_call_id)" routes/app_routes.py`

Den `with ls._session_state_lock:`-Block samt Schleife (Ist: `:184-189`) **ersetzen** durch:

```python
        # Stufe 1: exakte Aufloesung via posted call_id (S3)
        if _posted_call_id:
            # SOFORT-2 B-02: Der frueher hier stehende Scan verglich NUR die call_id und
            # traf damit auch fremde Sitzungen (Lebendigkeit statt Eigentuemerschaft).
            # Der Helfer vergleicht zusaetzlich user_id; ein fremder Treffer zaehlt wie
            # kein Treffer, und Stufe 2 (eigener user_id-Scan, unveraendert darunter) greift.
            _beenden_sid = ls.resolve_own_sid_by_call_id(_posted_call_id, _my_uid)
```

Der `_snap_s3`-Zweig (Ist: `:191-195`) bleibt **unveraendert** stehen — toter Code, gemeldet als
R-1. Die Stufe-2-Schleife (`:197-203`) bleibt **unveraendert** — sie filtert bereits korrekt.

**(b) N-01 — `api_precall_research`.**
Such-Muster: `grep -n "sid = (data.get('sid')" routes/app_routes.py`

Die Zeile `sid = (data.get('sid') or '').strip() or None` **ersetzen** durch:

```python
    sid = (data.get('sid') or '').strip() or None
    user_id = g.user.id if g.user else None
    # SOFORT-2 N-01: die sid aus dem Anfrage-Koerper wandert ueber recherche_firma in
    # set_briefing_for_sid — dessen Guard prueft nur, ob die Sitzung LEBT. Ohne diese
    # Bedingung schreibt ein fremdes Konto beliebigen Text in den _briefing-Schluessel einer
    # laufenden Sitzung; der Text landet in Abschnitt 8 des System-Prompts JEDES Live-Aufrufs
    # dieser Sitzung. Im selben Aufruf laese build_profile_context das fremde Profil aus und
    # gaebe es dem Anfragenden zurueck.
    # 404 (nicht 403): ein 403 wuerde bestaetigen, dass die Kennung existiert.
    if sid and not ls.sid_belongs_to(sid, user_id):
        return jsonify({'error': 'Unbekannte Sitzung'}), 404
```

und die **bisherige** Zeile `user_id = g.user.id if g.user else None` direkt darunter
**entfernen** (sie ist jetzt nach oben gewandert — kein doppeltes Setzen).

⚠ Reihenfolge: die Pruefung steht **vor** dem `recherche_firma(...)`-Aufruf. Danach waere sie
wirkungslos, denn der Schreibzugriff passiert **in** `recherche_firma`.

⚠ `ls` muss in dieser Funktion erreichbar sein. Per
`grep -n "^import services.live_session as ls\|^from services import live_session as ls" routes/app_routes.py`
pruefen. Fehlt der Modul-Import auf Datei-Ebene, im Funktionsrumpf `import services.live_session as ls`
ergaenzen — **wie es die Datei an anderen Stellen ohnehin tut** (Muster:
`from services.live_session import _session_state, _session_state_lock` in
`api_gatekeeper_phrases`).

**(c) B-03 — `api_gatekeeper_phrases`.**
Such-Muster: `grep -n "request.args.get('sid')" routes/app_routes.py`

Den `if sid:`-Block (Ist: `:2078-2085`) **ersetzen** durch:

```python
        # SOFORT-2 B-03: Die DB-Abfrage darueber filtert korrekt auf Phrase.user_id — die
        # Zustands-Aufloesung darunter tat es nicht. Zwei Vertrauensniveaus in einer Funktion.
        # Antwortform (benannte Festlegung, Plan 03): KEIN Statuswechsel. Die sid ist hier
        # optional (Rueckfall auf flask_session), und die Funktion hat bereits einen sauberen
        # Leerpfad — die Platzhalter bleiben stehen. Ein 404 wuerde legitime Aufrufe ohne
        # aktive Sitzung brechen.
        import services.live_session as _ls_gk
        if sid and _ls_gk.sid_belongs_to(sid, g.user.id):
            with _ls_gk._session_state_lock:
                st = _ls_gk._session_state.get(sid, {}).get('state', {})
                briefing = st.get('active_profile_data') or st.get('_briefing') or {}
            if isinstance(briefing, dict):
                variables['branche'] = briefing.get('branche', '') or ''
                variables['detail'] = briefing.get('detail', '') or briefing.get('produkt', '') or ''
```

Der bisherige lokale Import `from services.live_session import _session_state, _session_state_lock`
faellt damit weg (er wird durch `_ls_gk` ersetzt) — der AST-Waechter fordert einen Helfer-Aufruf
**im selben Funktionsrumpf**, und `_ls_gk.sid_belongs_to(...)` erfuellt das.

**⛔ Reparatur-Modus — was in diesem Task NICHT passiert:**
- Der tote `_snap_s3`-Zweig wird nicht entfernt (R-1)
- Die `sid` bleibt im Query-String (R-2)
- Die veralteten Selbstverweise in Kommentaren (`:252`, `:266`, `:229-231`, `:821`) bleiben
  stehen (R-4) — sie sind historische Erzaehlungen, keine Wegweiser
- Keine Umbenennung, kein Import-Sortieren
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/app_routes.py').read_text(encoding='utf-8')); print('SYNTAX OK')"</automated>
  </verify>

  <acceptance_criteria>
    - Alle drei Fixes sind da (Existenz-Anker, je == 1):
      `grep -cF "_beenden_sid = ls.resolve_own_sid_by_call_id(_posted_call_id, _my_uid)" routes/app_routes.py`,
      `grep -cF "if sid and not ls.sid_belongs_to(sid, user_id):" routes/app_routes.py`,
      `grep -cF "if sid and _ls_gk.sid_belongs_to(sid, g.user.id):" routes/app_routes.py`
    - Die 404-Festlegung fuer N-01 steht im Code:
      `grep -cF "return jsonify({'error': 'Unbekannte Sitzung'}), 404" routes/app_routes.py` == 1
    - ⛔ **Es gibt KEIN 403 in den drei geaenderten Pfaden** — Abwesenheits-Pruefung MIT
      Existenz-Anker daneben:
      `git diff -U0 routes/app_routes.py | grep -c "^+.*), 403"` == 0
      **und** `git diff -U0 routes/app_routes.py | grep -c "^+"` >= 15
      (der zweite Wert beweist, dass der Diff ueberhaupt gelesen wurde — eine `0` allein waere
      nicht von „kein Diff vorhanden" unterscheidbar)
    - ⛔ Der alte, besitzlose Scan ist weg — Abwesenheits-Pruefung MIT Existenz-Anker:
      `grep -c "str(_sd\['state'\]\['call_id'\]) == str(_posted_call_id)" routes/app_routes.py` == 0
      **und** `grep -c "_posted_call_id" routes/app_routes.py` >= 3
      (die Variable lebt weiter — nur der besitzlose Vergleich ist fort)
    - Die drei Marker-Kommentare stehen im Code (der AST-Waechter sieht sie nicht, der naechste
      Leser schon): `grep -c "SOFORT-2 B-02\|SOFORT-2 N-01\|SOFORT-2 B-03" routes/app_routes.py` == 3
    - Die Stufe-2-Aufloesung ist **unangetastet** (sie war schon richtig):
      `grep -cF "if st.get('user_id') == _my_uid]" routes/app_routes.py` == 1
    - Reparatur-Modus: der tote `_snap_s3`-Zweig steht unangetastet da — **beide Zahlen am
      echten Dateiinhalt nachgemessen (2026-08-04), nicht geschaetzt**:
      `grep -c "consume_ended_session_by_call_id" routes/app_routes.py` == **2**
      **und** `grep -c "_snap_s3" routes/app_routes.py` == **3**
      (⚠ **frueher stand hier `== 1`. Das war bei voellig korrektem Bau rot.** Der Aufruf ist
      ueber eine **Zeilenfortsetzung** verteilt — `:192` traegt `_snap_s3 = ls.consume_ended_
      session_by_call_id(_posted_call_id) \`, `:193` traegt `if hasattr(ls, 'consume_ended_
      session_by_call_id') else None`. Der Name steht auf **zwei** Zeilen, und `grep -c`
      zaehlt Zeilen, nicht Treffer. Der einzige Weg zu einer `1` waere gewesen, genau den
      toten Zweig anzufassen, den dieses Kriterium schuetzen soll. `_snap_s3` steht auf
      **drei** Zeilen: Zuweisung `:192`, Pruefung `:194`, Verwendung `:195`.
      ⚠ **Wer diese Zahlen anfasst, misst vorher — nicht schaetzen, und Zeilenfortsetzungen
      mit `\` gehoeren mitgelesen.**)
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/app_routes.py').read_text(encoding='utf-8')); print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
B-02, N-01 und B-03 laufen ueber den Helfer aus Plan 02. `/api/precall/research` antwortet bei
fremder `sid` mit **404** und ruft `recherche_firma` gar nicht erst auf.
`/api/gatekeeper/phrases` liefert bei fremder `sid` leere Variablen statt fremder Kundendaten.
Der tote `_snap_s3`-Zweig und die alten Selbstverweise sind unangetastet.
  </done>
</task>

<task type="auto">
  <name>Task 2: routes/learning.py — N-02 und N-03, zwei wortgleiche Stellen</name>

  <read_first>
    - `routes/learning.py` Z.120-172 (`api_postcall_analysis`: der `calls`-UPDATE darueber
      filtert **korrekt** auf `Call.user_id == g.user.id` — das ist das Vorbild in derselben
      Funktion; der Emit darunter tut es nicht) und Z.290-340 (`api_postcall_outcome`,
      wortgleicher Aufbau)
    - `services/live_session.py` — `resolve_own_sid_by_call_id` aus Plan 02
    - `static/pip-launcher.js` — der `outcome_ready`-Empfaenger und die Client-Milderung
      (`if (state.lastCallId && String(state.lastCallId) !== String(d.call_id)) return;`);
      per `grep -n "outcome_ready" static/pip-launcher.js` lokalisieren. **Nur lesen** — sie ist
      eine Client-Pruefung und ersetzt keinen Serverschutz
    - Der Abschnitt „Antwortform je Eingang" oben in diesem Plan
  </read_first>

  <files>routes/learning.py</files>

  <action>
Such-Muster: `grep -n "for _sid, _sd in ls._session_state.items()" routes/learning.py`
→ **zwei** Treffer erwartet (Ist: `:150` und `:317`).

An **beiden** Stellen den Block

```python
                    _sid_for_emit = None
                    with ls._session_state_lock:
                        for _sid, _sd in ls._session_state.items():
                            if str(_sd.get('state', {}).get('call_id')) == str(_posted_call_id):
                                _sid_for_emit = _sid
                                break
```

**ersetzen** durch — an beiden Stellen **wortgleich**:

```python
                    # SOFORT-2 N-02/N-03: Raum-Auswahl ueber den Besitz-Helfer, nicht ueber
                    # einen besitzlosen Zustands-Scan. Der calls-UPDATE oben filtert bereits
                    # korrekt auf Call.user_id — nur die Raum-Auswahl hier tat es nicht
                    # (derselbe Zwei-Niveau-Fehler wie in B-03). Folge vorher: ein fremdes
                    # Konto konnte dem PiP eines anderen Beraters mitten im Gespraech eine
                    # Abschluss-Maske mit selbst gewaehltem Ergebnis einblenden.
                    # Der bestehende else-Zweig darunter ("KEINE aktive SID - SKIP emit") ist
                    # bereits der richtige Leerpfad — deshalb KEIN Statuswechsel.
                    _sid_for_emit = ls.resolve_own_sid_by_call_id(_posted_call_id, g.user.id)
```

Alles darunter (`if _sid_for_emit:` … `_payload` … `emit(..., room=_sid_for_emit)` …
`except Exception as _e_em:`) bleibt **unveraendert**.

⚠ **Erst pruefen, dann ersetzen:** die zwei Bloecke sind laut `grep` wortgleich, aber der
umgebende Einzug kann abweichen. Beide Stellen einzeln lesen (nicht per Suchen-und-Ersetzen ueber
die ganze Datei), damit der Einzug stimmt.

⚠ `g.user.id` ist in beiden Funktionen erreichbar (beide tragen `@login_required` und der
`calls`-UPDATE darueber benutzt bereits `g.user.id`) — per
`grep -n "g.user.id" routes/learning.py` bestaetigen.

**⛔ Reparatur-Modus:** die Client-Milderung in `static/pip-launcher.js` wird **nicht**
angefasst. Sie bleibt als zweite Schicht bestehen, ersetzt aber keinen Serverschutz.
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/learning.py').read_text(encoding='utf-8')); print('SYNTAX OK')"</automated>
  </verify>

  <acceptance_criteria>
    - Beide Stellen laufen ueber den Helfer:
      `grep -cF "_sid_for_emit = ls.resolve_own_sid_by_call_id(_posted_call_id, g.user.id)" routes/learning.py` == 2
    - ⛔ Der besitzlose Scan ist an **beiden** Stellen weg — Abwesenheits-Pruefung MIT
      Existenz-Anker daneben:
      `grep -c "for _sid, _sd in ls._session_state.items()" routes/learning.py` == 0
      **und** `grep -c "_sid_for_emit" routes/learning.py` >= 6
      (der zweite Wert beweist, dass die Emit-Bloecke noch existieren — eine `0` allein waere
      auch von „Bloecke versehentlich geloescht" nicht unterscheidbar)
    - Der Emit selbst ist unveraendert:
      `grep -cF "_sio_phase_d.emit('outcome_ready', _payload, room=_sid_for_emit)" routes/learning.py` == 2
    - Der Marker-Kommentar steht zweimal:
      `grep -c "SOFORT-2 N-02/N-03" routes/learning.py` == 2
    - Reparatur-Modus: nur diese eine Datei im Diff:
      `git status --porcelain routes/learning.py static/pip-launcher.js | wc -l` == 1
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/learning.py').read_text(encoding='utf-8')); print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
Der `outcome_ready`-Emit waehlt seinen SocketIO-Raum an beiden Stellen ueber
`resolve_own_sid_by_call_id` — aus dem eigenen Kontext, nie aus der Anfrage. Der bestehende
Leerpfad („keine aktive SID → SKIP emit") traegt jetzt auch den Fremdbesitz-Fall.
  </done>
</task>

<task type="auto">
  <name>Task 3: B-01 — der calls-UPDATE bekommt user_id, an BEIDEN Stellen</name>

  <read_first>
    - `routes/app_routes.py` Z.752-790 — **beide** Dinge in einem Blick: der DB-Fallback
      `:755-770` filtert **korrekt** (`_CallModel_fb.user_id == g.user.id`), der Haupt-UPDATE
      `:782` darunter **nicht**. Das Vorbild und der Fehler stehen 20 Zeilen auseinander
    - `routes/app_routes.py` Z.818-836 — der Heiler-UPDATE (zweite Stelle)
    - `routes/app_routes.py` Z.265-280 — ⚠ **`:275-276` ist der ZWEITE, UNABHAENGIGE Weg**, wie
      eine gepostete Fremd-`call_id` zu `_phase_d_call_id` wird. Er laeuft an der SID-Aufloesung
      aus Task 1 komplett vorbei. **Ohne diesen Task waere B-01 offen und wuerde fuer erledigt
      gehalten** (RESEARCH P-4)
    - `routes/app_routes.py` — `_audio_health_bg`, per `grep -n "_CallAH.user_id" routes/app_routes.py`
      lokalisieren: das zweite korrekte Vorbild in derselben Datei
    - Der Abschnitt „5. Persistenz-Schicht-Verifikation" **oben in diesem Plan**
      (inspect.sh-Beleg + „keine RLS auf public.calls")
  </read_first>

  <files>routes/app_routes.py</files>

  <action>
Such-Muster: `grep -n "_CallModel.id == _phase_d_call_id" routes/app_routes.py`
→ **zwei** Treffer erwartet (Ist: `:782` und `:828`).

**(a) Haupt-UPDATE** (Ist: `:782`). Die Zeile

```python
            _call_row = _db_calls.query(_CallModel).filter(_CallModel.id == _phase_d_call_id).first()
            if _call_row is not None:
```

**ersetzen** durch:

```python
            # SOFORT-2 B-01: Besitzpruefung. Vorher filterte dieser UPDATE NUR auf die id —
            # eine geposteter fremde call_id (zweiter Weg: der Rueckfall weiter oben, der
            # _posted_call_id direkt uebernimmt) setzte damit ended_at, conversation_log_id
            # (fremder Anruf zeigte mein Transkript), call_mode, score_breakdown und
            # transcript_resolved auf einem fremden Anruf — und stiess die Bewertungs-Strecke
            # darauf an. Vorbild: der DB-Fallback ~20 Zeilen darueber und _audio_health_bg
            # filtern beide korrekt.
            # ⚠ public.calls hat auf Production KEINE RLS (psql als postgres, 2026-08-04) —
            # dieser WHERE-Filter ist die EINZIGE Kontrolle. Faellt er raus, faengt nichts.
            _call_row = (_db_calls.query(_CallModel)
                         .filter(_CallModel.id == _phase_d_call_id,
                                 _CallModel.user_id == g.user.id)
                         .first())
            if _call_row is None:
                # Vorher lief dieser Fall STILL durch. Ein abgelehnter Fremdzugriff ist ein
                # Ereignis, kein Nichts.
                print(f'[SOFORT-2] calls-UPDATE uebersprungen: call_id={_phase_d_call_id} '
                      f'gehoert nicht user_id={g.user.id} (oder existiert nicht)')
            if _call_row is not None:
```

⚠ Der bestehende `if _call_row is not None:`-Block darunter bleibt **vollstaendig unveraendert**
(Einzug beibehalten). Es wird **kein** `else` gebaut und **kein** Block umgehaengt — Reparatur-Modus.

**(b) Heiler-UPDATE** (Ist: `:827-829`). Die Zeilen

```python
                _row_heal = _db_heal.query(_CallModel).filter(
                    _CallModel.id == _phase_d_call_id
                ).first()
```

**ersetzen** durch:

```python
                _row_heal = _db_heal.query(_CallModel).filter(
                    _CallModel.id == _phase_d_call_id,
                    _CallModel.user_id == g.user.id,   # SOFORT-2 B-01: zweite Stelle
                ).first()
```

**(c) Der zweite Weg wird NICHT abgeschnitten — sondern harmlos.**
`:275-276` (`if not _phase_d_call_id and _posted_call_id: _phase_d_call_id = _posted_call_id`)
bleibt **unveraendert**. Begruendung, die als Kommentar direkt darueber eingefuegt wird:

```python
        # SOFORT-2 B-01 (P-4): Dieser Rueckfall uebernimmt die GEPOSTETE call_id ungeprueft —
        # er ist ein zweiter, von der SID-Aufloesung unabhaengiger Weg. Er bleibt bewusst
        # stehen (er traegt den legitimen Reconnect-Fall), ist aber nicht mehr gefaehrlich:
        # jeder Verbraucher von _phase_d_call_id, der in public.calls schreibt, filtert seit
        # dieser Phase zusaetzlich auf user_id. Wer hier je einen dritten Verbraucher
        # anhaengt, muss denselben Filter mitbringen.
```

**Das ist die bewusste Entscheidung dieses Tasks:** die Besitzpruefung sitzt **am Schreibzugriff**,
nicht an der Herkunft der Kennung. Ein Filter an der Herkunft haette den legitimen
Reconnect-Fall gebrochen (`_bs` leer, Client kennt seine `call_id` noch).

**⛔ Reparatur-Modus:** kein weiterer `calls`-Zugriff in dieser Datei wird angefasst. Der
DB-Fallback und `_audio_health_bg` filtern bereits korrekt und bleiben unberuehrt.
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/app_routes.py').read_text(encoding='utf-8')); print('SYNTAX OK')"</automated>
    <automated>grep -c "_CallModel.user_id == g.user.id" routes/app_routes.py</automated>
  </verify>

  <acceptance_criteria>
    - Beide UPDATE-Stellen filtern jetzt auf `user_id` (Existenz-Anker):
      `grep -c "_CallModel.user_id == g.user.id" routes/app_routes.py` == 2
    - ⛔ Kein `calls`-Filter mehr **nur** auf `id` — Abwesenheits-Pruefung MIT Existenz-Anker.
      Der AST-Check zaehlt `.filter(...)`-Aufrufe auf `_CallModel`, deren Argumentliste den
      Namen `user_id` **nicht** enthaelt (Kommentare sind fuer den AST unsichtbar — genau
      deshalb AST statt grep, denn der oben vorgeschriebene Erklaertext enthaelt „`_CallModel.id`"):
      ```
      python -c "import ast,pathlib; t=ast.parse(pathlib.Path('routes/app_routes.py').read_text(encoding='utf-8')); f=[c for c in ast.walk(t) if isinstance(c,ast.Call) and isinstance(c.func,ast.Attribute) and c.func.attr=='filter' and '_CallModel.' in ast.unparse(c)]; ohne=[ast.unparse(c)[:70] for c in f if 'user_id' not in ast.unparse(c)]; print('FILTER', len(f), 'OHNE_USERID', ohne)"
      ```
      gibt `OHNE_USERID []` bei `FILTER >= 2`
      — die Zahl neben der leeren Liste ist der Existenz-Anker: `OHNE_USERID []` bei
      `FILTER 0` waere bedeutungslos
    - Der abgelehnte Fremdzugriff wird protokolliert:
      `grep -cF "[SOFORT-2] calls-UPDATE uebersprungen" routes/app_routes.py` == 1
    - Der zweite Weg steht noch da und traegt jetzt seine Begruendung:
      `grep -cF "_phase_d_call_id = _posted_call_id" routes/app_routes.py` == 1
      **und** `grep -c "SOFORT-2 B-01 (P-4)" routes/app_routes.py` == 1
    - Die zwei korrekten Bestands-Vorbilder sind unangetastet:
      `grep -c "_CallModel_fb.user_id == g.user.id" routes/app_routes.py` == 1
      **und** `grep -c "_CallAH.user_id == user_id_val" routes/app_routes.py` == 1
    - ⛔ Kein Backfill, kein Massen-UPDATE — Abwesenheits-Pruefung MIT Existenz-Anker:
      `git diff -U0 routes/app_routes.py | grep -c "^+.*\.update("` == 0
      **und** `git diff --stat routes/app_routes.py | grep -c "app_routes.py"` == 1
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('routes/app_routes.py').read_text(encoding='utf-8')); print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
Beide `calls`-UPDATE-Stellen filtern auf `_CallModel.user_id == g.user.id`. Ein abgelehnter
Fremdzugriff wird laut protokolliert statt still durchzulaufen. Der zweite, unabhaengige Weg zu
einer geposteten `call_id` bleibt fuer den legitimen Reconnect erhalten und traegt jetzt die
Begruendung, warum er ungefaehrlich ist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Client → `POST /api/beenden` | `call_id` aus dem Anfrage-Koerper steuert einen Schreibzugriff auf `public.calls` |
| Browser/Client → `POST /api/precall/research` | `sid` aus dem Anfrage-Koerper steuert einen **Schreibzugriff** in fremden RAM-Zustand und einen Lesezugriff auf ein fremdes Profil |
| Browser/Client → `GET /api/gatekeeper/phrases` | `sid` aus dem Query-String steuert einen Lesezugriff auf fremden RAM-Zustand |
| Browser/Client → `POST /api/postcall_analysis` \| `/api/postcall_outcome` | `call_id` aus dem Anfrage-Koerper steuert die Wahl eines **fremden SocketIO-Raums** |
| Flask-Endpunkt → `public.calls` | **KEINE RLS** — der `WHERE`-Filter ist die einzige Kontrolle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SOF2-20 | Tampering | `api_beenden` → `calls`-UPDATE (B-01, beide Stellen) | mitigate | `_CallModel.user_id == g.user.id` in **beiden** Filtern (Task 3 a/b); AST-Acceptance `OHNE_USERID []`; laute Protokollzeile statt stillem Durchlauf |
| T-SOF2-21 | Tampering | `_phase_d_call_id` aus `_posted_call_id` (P-4, zweiter Weg) | mitigate | Der Weg bleibt (legitimer Reconnect), die Kontrolle wandert an den **Schreibzugriff**. Als Kommentar mit Merksatz fuer den naechsten Verbraucher verankert |
| T-SOF2-22 | Information Disclosure | `api_beenden` Stufe-1-SID-Aufloesung (B-02) | mitigate | `resolve_own_sid_by_call_id` — ein fremder Treffer zaehlt wie kein Treffer; Stufe 2 (eigener `user_id`-Scan) faengt den legitimen Fall auf |
| T-SOF2-23 | Tampering | `recherche_firma(sid=…)` → `set_briefing_for_sid` (N-01 Schreib-Pfad) | mitigate | `sid_belongs_to` **vor** dem `recherche_firma`-Aufruf, 404. Prompt-Injektion in eine fremde laufende Sitzung ist damit geschlossen |
| T-SOF2-24 | Information Disclosure | `build_profile_context(user_id=A, sid=B)` (N-01 Lese-Pfad) | mitigate | Derselbe Guard — der Aufruf findet nicht mehr statt. **Rest-Kante:** `build_profile_context` selbst prueft weiterhin nicht, ob `user_id` und `sid` zusammenpassen. Gemeldet, nicht gefixt (Reparatur-Modus): ein zweiter Aufrufer mit fremder `sid` waere wieder offen — der AST-Waechter faengt ihn nur, wenn er in `routes/` sitzt |
| T-SOF2-25 | Spoofing | `outcome_ready`-Emit in fremden Raum (N-02/N-03) | mitigate | Raum aus dem eigenen Kontext ableiten. Die Client-Milderung in `static/pip-launcher.js` bleibt als zweite Schicht, ersetzt aber keinen Serverschutz |
| T-SOF2-26 | Information Disclosure | Antwortform verraet die Existenz einer fremden Kennung | mitigate | Benannte Festlegung: **404** nur bei N-01, sonst „wirkt wie nicht vorhanden". Nirgends 403 — als Diff-Acceptance greppbar |
| T-SOF2-27 | Denial of Service | Der zusaetzliche `user_id`-Filter verlangsamt `api_beenden` | accept | `idx_calls_user_time` liegt auf `(user_id, started_at)`; der Zugriff laeuft weiterhin ueber den Primaerschluessel. Latenz-Wirkung null (Punkt 25) |
| T-SOF2-28 | Denial of Service | Der Fix bricht den legitimen Reconnect-Fall | mitigate | `test_eigener_zugriff_funktioniert_weiter` (Plan 01 Task 3) plus die Browser-Gegenprobe mit dem **eigenen** Konto (Plan 04 Task 3). Ein Fix, der einfach alles verweigert, faellt dort rot |
| T-SOF2-29 | Repudiation | Ein abgelehnter Fremdzugriff bleibt unsichtbar | mitigate | `[SOFORT-2] calls-UPDATE uebersprungen: …` (B-01) und `[SOFORT-2] Fremdzugriff abgelehnt: …` (Helfer, Plan 02) |
</threat_model>

<verification>
1. Alle sechs Eingaenge sind geschlossen — je ein Existenz-Anker im Code, je eine
   Abwesenheits-Pruefung des Alt-Musters **mit** Existenz-Anker daneben
2. Der AST-Check auf `_CallModel`-Filter gibt `OHNE_USERID []` bei `FILTER >= 2`
3. Nirgends 403 im Diff; genau ein 404 (N-01, benannte Festlegung)
4. Die drei korrekten Bestands-Muster (Stufe-2-Scan, DB-Fallback, `_audio_health_bg`) sind
   unangetastet
5. Reparatur-Modus eingehalten: toter `_snap_s3`-Zweig, `sid` im Query-String, Client-Milderung
   und die veralteten Selbstverweise sind **nicht** angefasst (alle als R-1/R-2/R-4 gemeldet)
6. **KEIN Deploy in diesem Plan** — das macht Plan 04, zusammen mit der Zwei-Konten-Gegenprobe
</verification>

<success_criteria>
- Sechs Eingaenge geschlossen, fuenf ueber **einen** Helfer, einer als DB-Filter an **zwei** Stellen
- Der zweite, unabhaengige Weg zu B-01 (`:275-276`) ist erkannt, bewertet und dokumentiert —
  nicht uebersehen (P-4)
- Die Antwortform je Eingang ist eine benannte Festlegung, kein Zufall
- Kein legitimer Zugriff wird gebrochen (Gegenprobe-Test)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-03-SUMMARY.md`
</output>
