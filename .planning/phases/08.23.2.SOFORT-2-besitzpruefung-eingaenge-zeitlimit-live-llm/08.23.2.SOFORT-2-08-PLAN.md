---
phase: 08.23.2.SOFORT-2
plan: 08
type: execute
wave: 8
depends_on: ["08.23.2.SOFORT-2-07"]
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Welle 2 ist ausgerollt und das serverseitige Test-Tor war dabei gruen"
    - "Der Welle-2-Waechter ist auf Production GRUEN — derselbe Test, der in Plan 05 rot war"
    - "Ein echter Test-Anruf hat stattgefunden und die api_cost_log-Messwerte liegen unveraendert im bisherigen Bereich"
    - "Der Founder-Zaehler steht nach dem Test-Anruf auf 0 — kein legitimer Aufruf wurde neu gekappt"
    - "Alle Funde dieser Phase — gefixte wie nicht gefixte — sind in einem durablen Artefakt und im DIALOG-Kanal"
  artifacts:
    - path: ".planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-08-SUMMARY.md"
      provides: "GRUEN-Beleg verbatim, D-06-SELECT-Ausgabe, Pruning-Notiz"
      min_lines: 60
  key_links:
    - from: "bash deploy.sh production"
      to: "tests/test_live_timeout_coverage.py"
      via: "serverseitiges Test-Tor vor dem Neustart"
      pattern: "test_live_timeout"
    - from: "echter Test-Anruf"
      to: "public.api_cost_log"
      via: "SELECT context_tag, AVG(latency_ms), AVG(ttft_ms) nach dem Anruf"
      pattern: "api_cost_log"
---

**Komplexitaet: 🔴** — Cross-AI-Review ist Pflicht VOR Execute.

<objective>
Welle 2 geht live und wird **an echten Daten** abgenommen (D-06): ein echter Test-Anruf, und die
Messwerte in `api_cost_log` muessen **unveraendert im bisherigen Bereich** liegen — der Beleg,
dass kein legitimer Aufruf neu gekappt wird.

Purpose: Ein gruener Waechter beweist seinen Pruefkatalog. Er sagt **nichts** darueber, ob die
Zahlen 12 s und 8 s richtig gewaehlt sind. Nur ein echter Anruf sagt das.

Output: Deploy, GRUEN-Beleg, D-06-SELECT-Ausgabe, Pruning-Notiz, Phasen-Abschluss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-CONTEXT.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-05-SUMMARY.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-06-SUMMARY.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-07-SUMMARY.md
@.planning/phases/08.23.2.MESSGERAETE-1-antwort-dauer-je-live-ki-aufruf/08.23.2.MESSGERAETE-1-04-SUMMARY.md
@CLAUDE.md
@deploy.sh
</context>

## Die Vergleichsgrundlage (D-06) — und ihr Messumstand

| `context_tag` | Ø `latency_ms` | Ø `ttft_ms` |
|---|---|---|
| `live_haiku_merged` (Analyse + QA) | **1988 ms** | — |
| `coaching_haiku` (Coaching-Frage) | **2714 ms** | — |
| `phase_classify` (Phasen-Erkennung) | **1742 ms** | — |
| `coldcall_infer` (Cold-Call-Ableitung) | **1642 ms** | — |
| `pip_variante` (Stream) | — | **1035 ms** |

⚠ **Messumstand:** Diese Werte stammen aus einem Anruf **mit Headset** (MESSGERAETE-1,
2026-08-04). Der Vergleichs-Anruf soll unter **denselben** Bedingungen laufen, sonst vergleicht
man zwei verschiedene Dinge.

⚠ **`latency_ms` traegt bei den Stream-Pfaden eine andere Bedeutung** als bei den blockierenden:
dort ist die Auslieferung an den Browser mit im Messfenster (`sio.emit` innerhalb,
`async_mode='threading'`). **Fuer die TTFT-Grenze ist `ttft_ms` die richtige Groesse** — beim
Vergleich nicht verwechseln.

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
- `public.api_cost_log` — **ausschliesslich lesend** (die D-06-Abnahme). Kein `INSERT`, kein
  `UPDATE`, keine Migration, kein Backfill.

### inspect.sh-Beleg (Production, read-only, 2026-08-04) — verbatim

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema api_cost_log'
                                           Table "public.api_cost_log"
     Column      |            Type             | Collation | Nullable |                 Default
-----------------+-----------------------------+-----------+----------+------------------------------------------
 id              | integer                     |           | not null | nextval('api_cost_log_id_seq'::regclass)
 created_at      | timestamp without time zone |           | not null |
 provider        | character varying(32)       |           | not null |
 model           | character varying(64)       |           | not null |
 user_id         | integer                     |           |          |
 org_id          | integer                     |           |          |
 units           | numeric(14,4)               |           | not null |
 unit_type       | character varying(32)       |           | not null |
 rate_applied    | numeric(12,8)               |           | not null |
 rate_currency   | character varying(3)        |           | not null |
 fx_rate_applied | numeric(10,6)               |           | not null |
 cost_eur        | numeric(12,6)               |           | not null |
 session_id      | character varying(64)       |           |          |
 context_tag     | character varying(32)       |           |          |
 latency_ms      | integer                     |           |          |
 call_site       | character varying(50)       |           |          |
 ttft_ms         | integer                     |           |          |
Indexes:
    "ix_api_cost_log_created_at" btree (created_at)
```

⚠ Die Spalte `ttft_ms` kam mit Migration **0036** (MESSGERAETE-1). Der Executor zieht diesen
Output **vor** dem Deploy erneut und legt ihn verbatim in die SUMMARY — falls `ttft_ms` fehlt,
steht Prod auf einem aelteren Migrations-Stand und die D-06-Abnahme kann so nicht laufen
(**STOP und melden**, keine Migration in dieser Phase nachziehen).

### Cross-Layer-Konsistenz-Tabelle

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|---|
| `api_cost_log.latency_ms` | **nur gelesen** (D-06-SELECT) | DB-Spalte, integer nullable | `log_api_cost` schreibt **synchron mit eigenem `commit()`** im selben Aufruf — kein Hintergrund-Thread, kein Batch | ⚠ **ja, kurz:** der SELECT laeuft nach dem Anruf. Deshalb wird er **erst nach dem sichtbaren Ende** des Anrufs gefahren (`ended_at` gesetzt, Abschluss-Maske erschienen), nicht waehrenddessen | ✓ Muster aus MESSGERAETE-1 Plan 04 |
| `api_cost_log.ttft_ms` | nur gelesen | DB-Spalte (Migration 0036) | dito, nur an der input-Token-Buchung der Stream-Pfade | dito | ✓ inspect.sh oben |
| `_live_timeout_counts` (Founder) | gelesen im Dashboard | **RAM, pro Prozess** | synchron | ja, harmlos — die Aussage ist „0 oder nicht 0" | ✓ Plan 07 |

### Punkt 23 (Schild) — geprueft, NICHT faellig
Diese Phase aendert weder Schema noch Lese-/Schreib-**Pfad** von `api_cost_log`: kein neuer
Schreiber, kein neuer Leser, kein geaenderter Mechanismus. Ein Timeout bucht **nichts** (F-5a),
also entsteht auch kein neuer Schreibfall, den das Schild nennen muesste.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

<tasks>

<task type="auto">
  <name>Task 1: Deploy Welle 2 + GRUEN-Beleg</name>

  <read_first>
    - `deploy.sh` VOLLSTAENDIG — das serverseitige Test-Tor vor dem Neustart, die
      Waisen-Prune-Stufe (Manifest-Diff, Cap N=30, Dry-Run-first, Quarantaene) und die
      Schild-Guard-Stufe
    - `.planning/phases/08.23.2.SOFORT-2-.../08.23.2.SOFORT-2-05-SUMMARY.md` — der ROT-Lauf
      (3 failed / 2 passed), gegen den der GRUEN-Lauf gestellt wird
    - `.planning/phases/08.23.2.SOFORT-2-.../08.23.2.SOFORT-2-06-SUMMARY.md` und `-07-SUMMARY.md`
      — was gebaut wurde und welche Bestandstests umgeschrieben wurden
    - CLAUDE.md → „HART: Kein Local-Dev", Abschnitt „Workflow pro Plan-Task"
  </read_first>

  <files>(keine Repo-Datei — dieser Task rollt aus und erzeugt den GRUEN-Beleg)</files>

  <action>
**(a) Vorbedingungen pruefen — drei Dinge, jedes mit Existenz-Anker daneben:**

```bash
# 1) Kein SCP-Hotfix auf Prod, der vom tar-Deploy weggebuegelt wuerde
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/inspect.sh git-stand'

# 2) Die ROT-Lauf-Datei aus Plan 05 ist abgeraeumt (sonst prunt deploy.sh sie als Waise)
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'ls /opt/nerve/app/tests/test_live_timeout_coverage.py 2>&1'     # erwartet: No such file

# 3) Migrations-Stand: ttft_ms muss existieren, sonst kann D-06 nicht abnehmen
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/inspect.sh schema api_cost_log'   # erwartet: ttft_ms ist dabei
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/inspect.sh migrations'            # erwartet: >= 0036
```

⚠ **Diese Phase bringt KEINE Migration mit.** Steht Prod unter `0036`, ist das ein Blocker fuer
die D-06-Abnahme, **nicht** ein Anlass, hier eine Migration nachzuziehen (Reparatur-Modus).

**(b) Commit + Push + Deploy:**

```bash
git add -A
git commit -m "feat(08.23.2.SOFORT-2): Zeitlimit auf den Live-LLM-Aufrufen, gestaffeltes Verhalten (Welle 2)"
git push origin main
bash deploy.sh production
```

**(c) GRUEN-Beleg — die Spiegelung des ROT-Laufs aus Plan 05:**

```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_live_timeout_coverage.py \
     tests/test_live_timeout_stufen.py tests/test_stabil1_http_llm_timeout.py -q --tb=short'
```

Erwartet: **alles passed**, insbesondere die fuenf aus `test_live_timeout_coverage.py`, die in
Plan 05 mit 3 failed / 2 passed endeten. Ausgabe **verbatim** in die SUMMARY, direkt unter den
ROT-Lauf (Punkt 31: erst das Paar ROT→GRUEN beweist etwas).

**(d) Sicht auf die Protokolle nach dem Neustart:**

```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh service-status'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs-errors 80'
```

⚠ **`[SOFORT-2] Zeitueberschreitung` unmittelbar nach dem Deploy waere ein WARNZEICHEN**, kein
Erfolg: es hiesse, dass ein Limit bereits im Ruhebetrieb reisst. Dann sind 12 s bzw. 8 s zu
knapp gewaehlt — zurueckrollen oder per ENV (`LIVE_LLM_TIMEOUT_S`) hochsetzen, **ohne Deploy**,
und die Ursache klaeren.

**Bei rotem Test-Tor:** kein Neustart, der alte Stand bleibt live. Das ist der Zweck des Tors.
  </action>

  <verify>
    <automated>bash deploy.sh production</automated>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_live_timeout_coverage.py tests/test_live_timeout_stufen.py tests/test_stabil1_http_llm_timeout.py -q --tb=short'</automated>
  </verify>

  <acceptance_criteria>
    - `inspect.sh schema api_cost_log` auf Prod enthaelt `ttft_ms` (sonst STOP)
    - `inspect.sh migrations` meldet `>= 0036`
    - `deploy.sh production` endet mit gruenem Test-Tor und durchgefuehrtem Neustart
    - Der gezielte Lauf endet **ohne failed** — die Spiegelung von 3 failed / 2 passed
    - Der Einbau ist auf dem Server angekommen (Existenz-Anker auf Production):
      `ssh … 'cd /opt/nerve/app && grep -c "LIVE_LLM_TIMEOUT_S" services/claude_service.py'` >= 5
      **und** `ssh … 'cd /opt/nerve/app && grep -c "max_retries=0" services/claude_service.py'` >= 1
      **und** `ssh … 'cd /opt/nerve/app && grep -c "except anthropic.APITimeoutError:" services/claude_service.py'` == **6**
      (vier enge Zweige in den zwei Loops **plus** die zwei Durchreiche-Zweige in
      `classify_phase`/`infer_customer_state` — siehe Plan 07 Task 1 (c1). Eine `4` hier
      bedeutet: die zwei Durchreicher fehlen, und die Loop-Zweige fuer `classify_phase`/
      `infer_customer_state` sind **toter Code**. Das ist ein **Blocker**, kein Rundungsfehler)
    - ⛔ Der alte Konstruktor ist auf Prod fort — Abwesenheits-Pruefung MIT Existenz-Anker:
      `ssh … 'cd /opt/nerve/app && grep -c "anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)$" services/claude_service.py'` == 0
      **und** `ssh … 'cd /opt/nerve/app && grep -c "anthropic.Anthropic(" services/claude_service.py'` == 1
    - `inspect.sh service-status` meldet den Dienst als laufend
    - `inspect.sh logs-errors 80` zeigt **keine** `[SOFORT-2] Zeitueberschreitung`-Zeile im
      Ruhebetrieb und keine neue Fehlerklasse
    - GRUEN-Lauf steht **verbatim** in der SUMMARY, direkt unter dem ROT-Lauf aus Plan 05
  </acceptance_criteria>

  <done>
Welle 2 ist live. Dieselben Tests, die vor dem Einbau 3 failed / 2 passed lieferten, sind gruen —
auf dem Server, gegen den ausgerollten Code. Keine Zeitueberschreitung im Ruhebetrieb.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-06 — echter Test-Anruf, dieselben Bedingungen wie bei der Messung</name>

  <read_first>
    - `.planning/phases/08.23.2.MESSGERAETE-1-.../08.23.2.MESSGERAETE-1-04-SUMMARY.md` — wie der
      Vergleichs-Anruf damals gefahren wurde (Headset, Ablauf, welcher `context_tag` wann
      entsteht) **und** die Doppelbedeutung von `latency_ms` bei den Stream-Pfaden
    - Der Abschnitt „Die Vergleichsgrundlage (D-06)" **oben in diesem Plan**
    - `08.23.2.SOFORT-2-05-PLAN.md`, Abschnitt „DIE VIER BENANNTEN FESTLEGUNGEN" — die Zahlen,
      gegen die hier gemessen wird
  </read_first>

  <what-built>
Auf `https://app.getnerve.app` ist live:
- Alle acht produktiven Live-LLM-Aufrufe tragen ein Zeitlimit — sechs blockierende **12 s**,
  zwei Stream-Aufrufe **8 s** (TTFT plus Stillstand, **nicht** die Gesamtdauer).
- Der Modul-Client hat `max_retries=0` — der Worst Case ist 1 × Limit statt 3 ×.
- Bei einer Zeitueberschreitung: Runde still uebersprungen, protokolliert, **kein Retry**.
- Ab **drei** Ueberschreitungen in Folge fuer dieselbe Sitzung: eine ruhige Meldung in der
  PiP-Kopfzeile — *„Die Live-Erkennung antwortet gerade nicht."*
- Im Founder-Dashboard steht eine neue Karte **„Live-KI Zeitüberschreitungen"** mit Soll **0**.

**Was das alles nicht beweist:** ob 12 s und 8 s richtig gewaehlt sind. Ein Waechter beweist
seinen Pruefkatalog. Nur ein echter Anruf sagt, ob ein **legitimer** Aufruf jetzt gekappt wird.
  </what-built>

  <how-to-verify>
**Vorbereitung — dieselben Bedingungen wie bei der Vergleichsmessung**
1. **Headset aufsetzen.** Die Vergleichswerte stammen aus einem Anruf mit Headset; ohne
   dieselbe Bedingung vergleicht man zwei verschiedene Dinge.
2. Auf `https://app.getnerve.app` mit dem Test-Konto einloggen.

**Der Anruf**
3. Einen Anruf starten (Modus **Cold Call**, damit auch `coldcall_infer` und `phase_classify`
   feuern). Zwei bis drei Minuten sprechen, so dass:
   - mehrere Analyse-Runden laufen (`live_haiku_merged`),
   - mindestens eine Coaching-Frage kommt (`coaching_haiku`),
   - mindestens **einmal** der EWB-Knopf gedrueckt wird → Stream-Antwort im PiP
     (`pip_variante` — der einzige Pfad, der `ttft_ms` an echten Daten belegt).
4. Den Anruf sauber beenden (Knopf „Beenden"), bis die Abschluss-Maske erscheint.

**Was waehrend des Anrufs zu beobachten ist**

| # | Beobachtung | Erwartet | Wenn es ANDERS ist |
|---|---|---|---|
| 1 | Die PiP-Antworten kommen wie gewohnt | **Kein** Unterschied zu vorher spuerbar | Latenz-Regression → Blocker (Punkt 25) |
| 2 | Die Meldung *„Die Live-Erkennung antwortet gerade nicht."* | erscheint **NICHT** | Sie erscheint → ein legitimer Aufruf wird gekappt. **Blocker.** `LIVE_LLM_TIMEOUT_S` per ENV hochsetzen (ohne Deploy) und Ursache klaeren |
| 3 | Die EWB-Antwort im PiP | laeuft **vollstaendig** durch, bricht nicht mitten im Satz ab | Abbruch → `LIVE_LLM_STREAM_TIMEOUT_S` ist zu knapp (P-10) |
| 4 | Eine **Gespraechspause** von >10 s einlegen und danach weitersprechen | Die Meldung erscheint **NICHT** (P-7: ein Takt ohne LLM-Versuch zaehlt nicht) | Sie erscheint → der Zaehler zaehlt Takte statt Versuche. **Blocker** |

**Nach dem Anruf — im Founder-Dashboard**
5. Die Karte **„Live-KI Zeitüberschreitungen"** ansehen. Erwartet: **0**.
   Steht dort eine Zahl, ist mindestens ein legitimer Aufruf gekappt worden — dann die
   Funktionsnamen notieren und mitschicken.

**Antwort:**
- Punkte 1-5 wie erwartet → `abgenommen`
- Sonst → beschreiben, **welche** Beobachtung abgewichen ist, plus die Zahl aus der
  Dashboard-Karte
  </how-to-verify>

  <verify>
    <!-- ⛔ B5: EINE `0` allein beweist nichts — sie heisst „sauber" ODER „nichts gelesen"
         (falscher Pfad, leeres Journal, Neustart, SSH-Fehler). Das frueher hier stehende
         `|| true` hat den SSH-Fehler zusaetzlich maskiert. Deshalb: das Protokoll-Fenster
         EINMAL holen und drei Zahlen daraus lesen — zwei Existenz-Anker und die Abwesenheit.
         Plan 04 macht es an derselben Stelle bereits richtig (dort `>= 3` als positiver Beleg). -->
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 200' > sof2_logfenster.txt; echo "ZEILEN $(wc -l < sof2_logfenster.txt)"; echo "ANKER_BEENDEN $(grep -F "[Beenden] ENTRY" sof2_logfenster.txt | wc -l)"; echo "SOFORT2 $(grep -F "SOFORT-2" sof2_logfenster.txt | wc -l)"</automated>
    <!-- Erwartet: ZEILEN >= 50, ANKER_BEENDEN >= 1, SOFORT2 == 0.
         ZEILEN/ANKER_BEENDEN beweisen, dass das Fenster wirklich gelesen wurde UND dass der
         Test-Anruf darin liegt ([Beenden] ENTRY steht in routes/app_routes.py:103 und wird
         bei jedem Beenden gedruckt). Erst dann traegt die 0.
         `| wc -l` statt `grep -c`, weil `grep -c` bei 0 Treffern mit Status 1 endet — das
         war der Grund fuer das alte `|| true`, und genau das hat den Fehler maskiert. -->
    <automated>rm -f sof2_logfenster.txt</automated>
  </verify>

  <acceptance_criteria>
    - Der Test-Anruf hat stattgefunden, mit Headset, Modus Cold Call, mit mindestens einem
      EWB-Knopfdruck (sonst fehlt `ttft_ms` in der Abnahme)
    - Die Stufe-2-Meldung ist waehrend des Anrufs **nicht** erschienen
    - Die Gespraechspause hat **nichts** ausgeloest (P-7 an echten Daten geprueft)
    - Die EWB-Antwort lief vollstaendig durch (P-10 an echten Daten geprueft)
    - Die Dashboard-Karte „Live-KI Zeitüberschreitungen" steht auf **0**
    - ⛔ **Keine `[SOFORT-2]`-Zeile im Protokoll — Abwesenheits-Pruefung MIT ZWEI
      Existenz-Ankern** (B5; eine nackte `0` ist nicht von „nichts gelesen" unterscheidbar):
      `ZEILEN` >= 50 **und** `ANKER_BEENDEN` >= 1 **und** `SOFORT2` == 0.
      ⚠ Ist `ANKER_BEENDEN` == 0, liegt der Test-Anruf **nicht** im gelesenen Fenster — dann
      ist die `0` wertlos; Fenster vergroessern (`inspect.sh logs 600`) und neu lesen, **nicht**
      als bestanden werten
    - Alle fuenf Beobachtungen stehen einzeln in der SUMMARY, mit Ergebnis
  </acceptance_criteria>

  <resume-signal>Antworte `abgenommen` oder beschreibe die abweichende Beobachtung.</resume-signal>

  <done>
Ein echter Anruf unter denselben Bedingungen wie die Vergleichsmessung ist gelaufen. Kein
legitimer Aufruf wurde gekappt, keine Meldung erschien, die Gespraechspause loeste nichts aus,
und der Founder-Zaehler steht auf 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Die D-06-Messung + Phasen-Abschluss</name>

  <read_first>
    - `.planning/phases/08.23.2.MESSGERAETE-1-.../08.23.2.MESSGERAETE-1-04-SUMMARY.md` — der
      D-06-SELECT, der dort schon einmal gefahren wurde. **Dieselbe Abfrage, damit die Zahlen
      vergleichbar sind** — nicht eine neu erfundene
    - `.planning/phases/08.23.2.SOFORT-2-.../08.23.2.SOFORT-2-FUNDE.md` (aus Plan 01) — die
      Fund-Meldung, die hier abschliessend ergaenzt wird
    - CLAUDE.md → „Acht weitere Vault-Regeln", Punkt 7 (**Pruning-Notiz ist Pflichtteil JEDER
      Phasen-SUMMARY**) und „Vier Vault-Regeln", Punkt B (Roadmap-Sync ueber die DIALOG-Datei)
    - Der Abschnitt „Die Vergleichsgrundlage (D-06)" oben in diesem Plan
  </read_first>

  <files>.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md, .planning/DIALOG-GSD-CLAUDIAN.md</files>

  <action>
**(a) Der D-06-SELECT** — auf Production, read-only, als `postgres`:

```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "sudo -u postgres psql -d nerve -c \"
SELECT context_tag,
       COUNT(*)                              AS buchungen,
       COUNT(latency_ms)                     AS mit_dauer,
       ROUND(AVG(latency_ms))                AS avg_latency_ms,
       ROUND(AVG(ttft_ms))                   AS avg_ttft_ms,
       MAX(latency_ms)                       AS max_latency_ms
  FROM api_cost_log
 WHERE unit_type = 'per_1k_input_tokens'
   AND created_at >= NOW() - INTERVAL '30 minutes'
 GROUP BY context_tag
 ORDER BY context_tag;\""
```

Die Ausgabe **verbatim** in die SUMMARY, direkt neben die Vergleichstabelle.

**Die Abnahme-Bedingung — was „unveraendert im bisherigen Bereich" heisst:**

| Pruefung | Bedingung | Wenn verletzt |
|---|---|---|
| **Kein legitimer Aufruf gekappt** | `max_latency_ms` liegt bei jedem blockierenden Tag **unter** 12000 | ein Aufruf hat das Limit gestreift → Limit hochsetzen (ENV) und neu messen |
| **Ø unveraendert** | Jeder Ø liegt innerhalb **±40 %** des Vergleichswerts (Analyse 1988 / Coaching 2714 / Phase 1742 / Coldcall 1642) | ausserhalb → Ursache klaeren, **bevor** die Phase geschlossen wird |
| **TTFT unveraendert** | `avg_ttft_ms` fuer `pip_variante` innerhalb ±40 % von 1035 ms | dito |
| **Die Messung ist nicht leer** | `buchungen > 0` fuer mindestens **vier** Live-Tags **und** `mit_dauer = buchungen` | ⚠ **Eine leere oder unvollstaendige Ergebnismenge ist KEINE Abnahme.** Sie bedeutet „nichts gemessen", nicht „alles gut" — das ist der Existenz-Anker dieser Pruefung |

⚠ Das **±40 %-Band** ist bewusst weit: ein einzelner Test-Anruf hat eine kleine Stichprobe, und
die Vergleichswerte sind selbst Ø ueber eine Handvoll Buchungen. Es faengt eine
**Groessenordnungs**-Abweichung, nicht Rauschen. Eine engere Grenze waere Schein-Genauigkeit.

**(b) `08.23.2.SOFORT-2-FUNDE.md` abschliessend ergaenzen** — ein neuer Abschnitt am Ende:

```markdown
## 5. Stand am Phasen-Ende (2026-08-XX)

| Fund | Status |
|---|---|
| B-01 / B-02 / B-03 / N-01 / N-02 / N-03 | **geschlossen und live** (Welle 1, Plan 03/04) |
| R-1 bis R-6 | **gemeldet, offen** — Backlog-Promotion ueber den DIALOG-Eintrag |
| **R-7** `routes/crm_export.py::save_meeting` (fremde `call_id` an eigenen Termin) | **gemeldet, offen — 🔴 echtes Loch.** Backlog-Promotion ueber den DIALOG-Eintrag **und** dauerhaft sichtbar als `GEMELDET-NICHT-GEFIXT`-Eintrag in `tests/test_ownership_state_guard.py`. Bewusst nicht in dieser Phase: der Fix braucht eine `public.calls`-Besitzabfrage in einem bisher rein RLS-gedeckten `crm`-Blueprint |

**Zwei Waechter-Paare halten die Fehlerklassen fest:**
- Besitzpruefung: `tests/test_ownership_state_guard.py` (abgeleitet, drei Mengen) +
  `tests/test_ownership_endpoints.py` (Verhalten, sechs Eingaenge)
- Zeitlimit: `tests/test_live_timeout_coverage.py` (abgeleitet, inkl. `max_retries`) +
  `tests/test_live_timeout_stufen.py` (Verhalten, Erreichbarkeit + Schwelle)

**Was KEIN Waechter sieht** (Punkt 31, aus den vier `RESTLUECKEN`-Absaetzen zusammengefasst):
- ob das ERGEBNIS des Besitz-Helfers beachtet wird (nur, dass er gerufen wird)
- der DB-Fall B-01 im statischen Sweep (dafuer traegt der Verhaltenstest)
- der WERT der Zeitlimits (nur, dass eines gesetzt ist)
- ein Live-LLM-Pfad in einer dritten Datei
- ⚠ **es gibt KEINE RLS unter `public.calls`** — der Filter im Code ist die einzige Kontrolle
```

**(c) DIALOG-Eintrag ans Dateiende** (Roadmap-Sync, CLAUDE.md „Vier Vault-Regeln" Punkt B):

```markdown
## ROADMAP-SYNC — 08.23.2.SOFORT-2 — <Datum> — ABGESCHLOSSEN

Phase 08.23.2.SOFORT-2 ist live. Welle 1 (Besitzpruefung, **sechs** Eingaenge statt der drei aus
dem Roadmap-Eintrag) und Welle 2 (Zeitlimit, 12 s blockierend / 8 s Stream, max_retries=0,
gestaffeltes Verhalten mit Schwelle 3) sind ausgerollt und an echten Daten abgenommen.

**Bitte in `Nerve-Vault/01 Roadmap.md` nachziehen** — und in `.planning/ROADMAP.md`
(ich habe sie auftragsgemaess nicht angefasst): Phase als abgeschlossen markieren, Scope-Notiz
„sechs statt drei Eingaenge".

**Weiterhin offen als Backlog-Kandidaten:** R-1 bis R-7 aus
`08.23.2.SOFORT-2-FUNDE.md` Abschnitt 2.
```

**(d) Pruning-Notiz** — Pflichtteil jeder Phasen-SUMMARY (CLAUDE.md „Acht weitere
Vault-Regeln", Punkt 7). In `08.23.2.SOFORT-2-08-SUMMARY.md`:

| Stelle | obsolet geworden? | Entscheidung |
|---|---|---|
| Der besitzlose `_session_state`-Scan in `routes/app_routes.py` und `routes/learning.py` (3×) | ja | **geloescht** (durch den Helfer ersetzt) |
| `test_daemon_pfad_ohne_zeitlimit` | ja, die Aussage wurde falsch | **umgeschrieben** zu `test_daemon_pfad_mit_zeitlimit` |
| `consume_ended_session_by_call_id`-Zweig (`routes/app_routes.py`) | war schon vorher tot | **bleibt** — Reparatur-Modus, gemeldet als R-1 |
| `_showAudioWarning` / `.pip-audio-warn` | nein, weiter in Benutzung | **bleibt** — der Farbverstoss darin ist R-3 |
| `services/claude_service.py::http_llm_client` | nein | **bleibt** — deckt die HTTP-Request-Pfade, die diese Phase nicht anfasst |

⛔ **`.planning/ROADMAP.md` und `.planning/STATE.md` werden auch in diesem Plan NICHT
angefasst.** Der Nachzug laeuft ueber den DIALOG-Kanal.
  </action>

  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "sudo -u postgres psql -d nerve -c \"SELECT context_tag, COUNT(*) AS buchungen, COUNT(latency_ms) AS mit_dauer, ROUND(AVG(latency_ms)) AS avg_latency_ms, ROUND(AVG(ttft_ms)) AS avg_ttft_ms, MAX(latency_ms) AS max_latency_ms FROM api_cost_log WHERE unit_type = 'per_1k_input_tokens' AND created_at >= NOW() - INTERVAL '30 minutes' GROUP BY context_tag ORDER BY context_tag;\""</automated>
  </verify>

  <acceptance_criteria>
    - Die SELECT-Ausgabe steht **verbatim** in der SUMMARY, neben der Vergleichstabelle
    - ⛔ **Existenz-Anker fuer die Messung selbst:** mindestens **vier** Live-`context_tag` mit
      `buchungen > 0`, und fuer jeden gilt `mit_dauer = buchungen`.
      **Eine leere Ergebnismenge ist eine FEHLGESCHLAGENE Abnahme, kein Erfolg** — sie bedeutet
      „nichts gemessen", nicht „alles gut"
    - `pip_variante` hat `avg_ttft_ms` **nicht NULL** (der EWB-Knopfdruck aus Task 2 ist der
      einzige Weg dorthin)
    - Kein blockierender Tag hat `max_latency_ms >= 12000`
    - Jeder Ø liegt innerhalb ±40 % seines Vergleichswerts (Analyse 1988 / Coaching 2714 /
      Phase 1742 / Coldcall 1642 / TTFT 1035)
    - `08.23.2.SOFORT-2-FUNDE.md` hat jetzt **fuenf** Abschnitte:
      `grep -c "^## " …/08.23.2.SOFORT-2-FUNDE.md` == 5
    - Der Abschluss-DIALOG-Eintrag existiert:
      `grep -c "ROADMAP-SYNC — 08.23.2.SOFORT-2 — .* — ABGESCHLOSSEN" .planning/DIALOG-GSD-CLAUDIAN.md` == 1
    - Die Pruning-Notiz steht in der SUMMARY:
      `grep -c "Pruning-Notiz" …-08-SUMMARY.md` == 1
      **und** `grep -cE "^\| .* \| (ja|nein|war schon vorher tot) \|" …-08-SUMMARY.md` >= 5
    - ⛔ ROADMAP/STATE unberuehrt — Abwesenheits-Pruefung MIT Existenz-Anker:
      `git status --porcelain .planning/ROADMAP.md .planning/STATE.md | wc -l` == 0
      **und** `git status --porcelain .planning/DIALOG-GSD-CLAUDIAN.md | wc -l` == 1
    - `git push origin main` ist gelaufen (Git-Regel: nach jeder abgeschlossenen Phase pushen)
  </acceptance_criteria>

  <done>
Die D-06-Messung liegt vor: die Ø-Werte nach dem Einbau stehen neben den Vergleichswerten von
MESSGERAETE-1, keiner ist gekappt, und die Ergebnismenge ist nachweislich **nicht leer**. Alle
Funde — gefixte wie offene — haben ihren durablen Ablageort, die Pruning-Notiz ist geschrieben,
und der Roadmap-Nachzug ist ueber den DIALOG-Kanal angestossen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → Prod-Dateisystem (tar-Deploy) | `deploy.sh production` ueberschreibt `/opt/nerve/app` |
| Agent → Production-Datenbank | **nur lesend** (der D-06-SELECT), als `postgres` |
| App → Anthropic-API | Ab jetzt mit Zeitlimit — der Test-Anruf prueft, ob es legitime Aufrufe kappt |
| Browser (Test-Konto) → Production | Der echte Anruf erzeugt echte Anruf-Daten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SOF2-80 | Denial of Service | Ein zu knappes Limit kappt legitime Aufrufe und macht das Produkt schlechter statt besser | mitigate | Der echte Test-Anruf (Task 2) + der D-06-SELECT mit `max_latency_ms < 12000` und dem ±40 %-Band. **Notausgang ohne Deploy:** `LIVE_LLM_TIMEOUT_S` / `LIVE_LLM_STREAM_TIMEOUT_S` per ENV |
| T-SOF2-81 | Denial of Service | Das Stream-Limit kappt eine legitime Antwort mitten im Satz (P-10) | mitigate | Beobachtung 3 im Test-Anruf (EWB-Antwort laeuft vollstaendig durch) — die einzige Pruefung, die das an echten Daten sagt |
| T-SOF2-82 | Spoofing | Stufe 2 feuert in einer Gespraechspause (P-7) | mitigate | Beobachtung 4: bewusst eine Pause >10 s einlegen. **Der Runtime-Test allein reicht nicht** — er zaehlt Aufrufe, nicht Takte im echten Loop |
| T-SOF2-83 | Repudiation | Eine leere Ergebnismenge wird als „alles gut" gelesen | mitigate | Ausdrueckliches Acceptance-Kriterium: mindestens vier Tags mit `buchungen > 0` und `mit_dauer = buchungen`. **Eine leere Menge ist eine fehlgeschlagene Abnahme** |
| T-SOF2-84 | Tampering | tar-Deploy buegelt einen SCP-Hotfix weg | mitigate | `inspect.sh git-stand` vor dem Deploy |
| T-SOF2-85 | Tampering | Die Waisen-Prune-Stufe quarantaeniert die ROT-Lauf-Datei | mitigate | Vorbedingung 2 prueft „No such file" vor dem Deploy |
| T-SOF2-86 | Information Disclosure | Der D-06-SELECT liest Produktivdaten | accept | Read-only, keine Gespraechsinhalte — nur `context_tag`, Zaehlungen und Dauern. Der Test-Anruf ist Andres eigener |
| T-SOF2-87 | Repudiation | Nicht gefixte Funde verschwinden mit dem Phasen-Ende | mitigate | Abschnitt 5 in `08.23.2.SOFORT-2-FUNDE.md` + der Abschluss-DIALOG-Eintrag mit ausdruecklicher Backlog-Bitte (D-02) |
| T-SOF2-88 | Repudiation | Obsoleter Code bleibt unbegruendet stehen (Nudelcode-Wurzel) | mitigate | **Pruning-Notiz** als Pflicht-Acceptance: je Stelle **geloescht** ODER **bleibt, weil …** |
| T-SOF2-89 | Denial of Service | Prod steht unter Migration 0036, `ttft_ms` fehlt, und die Abnahme laeuft ins Leere | mitigate | Vorbedingung 3 prueft `schema api_cost_log` und `migrations` **vor** dem Deploy; bei Fehlen: STOP, **keine** Migration in dieser Phase nachziehen |
</threat_model>

<verification>
1. Vorbedingungen geprueft: kein Hotfix-Verlust, ROT-Lauf-Datei abgeraeumt, Migration >= 0036
2. `deploy.sh production` gruen, Neustart erfolgt, Dienst laeuft
3. GRUEN-Lauf ohne failed — die Spiegelung des ROT-Laufs (3 failed / 2 passed) aus Plan 05
4. Einbau auf dem Server angekommen (vier Existenz-Anker), alter Konstruktor fort
   (Abwesenheit **mit** Existenz-Anker)
5. Echter Test-Anruf mit Headset gelaufen; fuenf Beobachtungen einzeln belegt, inklusive der
   bewussten Gespraechspause
6. D-06-SELECT verbatim, Ergebnismenge nachweislich **nicht leer**, kein Ø ausserhalb ±40 %,
   kein `max_latency_ms >= 12000`
7. Founder-Zaehler auf 0, keine `[SOFORT-2]`-Zeile im Protokoll eines normalen Anrufs
8. `FUNDE.md` hat fuenf Abschnitte, Abschluss-DIALOG-Eintrag steht, Pruning-Notiz geschrieben
9. `ROADMAP.md`/`STATE.md` unveraendert; `git push origin main` gelaufen
</verification>

<success_criteria>
- Beide Wellen sind live und **an echten Daten** abgenommen (D-06 vollstaendig)
- Kein legitimer Aufruf wird neu gekappt — belegt durch Messwerte, nicht durch Zuversicht
- Jede der vier Waechter-Dateien hat einen ROT→GRUEN-Beleg als Paar in den SUMMARYs
- Alle **sieben** nicht gefixten Funde haben einen Ablageort und eine Backlog-Bitte — R-7
  ausdruecklich als **echtes, offenes Loch** ausgewiesen, nicht als Falschtreffer
- Die Pruning-Notiz laesst keine Code-Stelle unbegruendet stehen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-08-SUMMARY.md`
— mit dem verbatim GRUEN-Lauf, der D-06-SELECT-Ausgabe neben der Vergleichstabelle, den fuenf
Beobachtungen des Test-Anrufs und der Pruning-Notiz.

**Damit ist Phase 08.23.2.SOFORT-2 abgeschlossen.**
</output>
