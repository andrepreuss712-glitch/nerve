---
phase: 08.23.2.SOFORT-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md
  - .planning/DIALOG-GSD-CLAUDIAN.md
  - tests/test_ownership_state_guard.py
  - tests/test_ownership_endpoints.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Jeder Sweep-Fund dieser Phase hat einen durablen Ablageort — auch die, die NICHT gefixt werden"
    - "Ein AST-Waechter leitet die Besitz-Pruefpunkte in routes/ SELBST aus dem Code ab — FUENF Mengen: Zustands-Aufloesung, sid-aus-Anfrage, call_id-aus-Anfrage, profile_id-aus-Anfrage, Kennung-aus-URL"
    - "Der Waechter sieht auch Kennungen, die als URL-Routen-Parameter ankommen — nicht nur solche aus .get()"
    - "Ein Verhaltens-Waechter fragt mit Konto A acht Ressourcen von Konto B an und erwartet je Eingang ein festgelegtes Verhalten"
    - "Beide Waechter fallen ROT statt blind, wenn ihre Ableitung ausfaellt (eigenes Mindest-Soll auf JEDER der fuenf Mengen)"
    - "Die zwei neuen Regeln beweisen an einem KUENSTLICHEN Leck, dass sie beissen — am Bestand sind sie gruen"
    - "Beide Waechter benennen im Modul-Docstring, was ihr Pruefkatalog NICHT sieht (RESTLUECKEN, Punkt 31)"
  artifacts:
    - path: ".planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md"
      provides: "D-02-Meldung aller Funde inkl. der NICHT gefixten, mit Ablageort"
      min_lines: 45
    - path: "tests/test_ownership_state_guard.py"
      provides: "AST-Sweep ueber routes/*.py mit FUENF Mindest-Soll-Zahlen, Selbsttest und RESTLUECKEN"
      min_lines: 200
    - path: "tests/test_ownership_endpoints.py"
      provides: "Verhaltens-Waechter Konto A gegen Konto B, ACHT Eingaenge"
      min_lines: 220
  key_links:
    - from: "tests/test_ownership_state_guard.py"
      to: "routes/app_routes.py"
      via: "ast.parse ueber routes/*.py, Suche nach _session_state / .get('sid') / .get('call_id')"
      pattern: "ast\\.parse"
    - from: "tests/test_ownership_endpoints.py"
      to: "routes/learning.py"
      via: "Flask-Test-Client mit zwei Konten, Assertion auf ausbleibenden outcome_ready-Emit"
      pattern: "outcome_ready"
---

**Komplexitaet: 🔴** (Sicherheit + Live-Pfad) — Cross-AI-Review (`/gsd-review --phase 08.23.2.SOFORT-2 --all`) ist **Pflicht VOR Execute**.

<objective>
Der Anfang der Sicherheits-Welle: **erst melden, dann messen, dann fixen.** Dieser Plan schreibt
alle sechs Funde des Sweeps durabel fest (D-02 — ein nicht gemeldeter Fund sieht aus wie „gibt es
nicht") und baut die **zwei** Waechter, die die Fehlerklasse dauerhaft festnageln. Es wird in
diesem Plan **nichts gefixt** — der ROT-Lauf kommt in Plan 02, der Fix in Plan 03.

Purpose: D-05 verlangt, dass die Pruefpunkt-Liste **aus dem Code abgeleitet** wird. Ein Waechter,
der die sechs Stellen aus der RESEARCH.md abschreibt, beweist nur seinen eigenen Pruefkatalog
(Punkt 31, wortwoertlich der LOCK-2-Fall).

Output: `08.23.2.SOFORT-2-FUNDE.md`, ein DIALOG-Eintrag, zwei Waechter-Dateien.
</objective>

## ⚠ ZAHLEN-TAFEL — die EINZIGE Quelle fuer alle Zahlen dieser Phase

⚠ **Diese Tafel steht genau EINMAL, hier.** Alle anderen Plaene verweisen mit
`Zahlen-Tafel ZT-x` darauf, statt die Zahl zu wiederholen. Das ist die Antwort auf die
„eine Zahl an vier Orten"-Falle, die diese Phase bereits dreimal einen Blocker gekostet hat.
**Wer eine Zahl aendert, aendert sie HIER und greppt danach `Zahlen-Tafel` ueber alle
Plan-Dateien.**

| ID | Bedeutung | Wert | Herkunft |
|---|---|---|---|
| ZT-1 | `ZUSTANDS_FUNKTIONEN` **vor** dem Fix | **5** | `ast.parse` ueber `routes/*.py`, gemessen 2026-08-05 |
| ZT-2 | `ZUSTANDS_FUNKTIONEN` **nach** dem Fix = Mindest-Soll | **3** | dieselbe Ableitung gegen die Ersetzungen aus Plan 03 Task 2 |
| ZT-3 | `SID_AUS_ANFRAGE` (vor = nach) = Mindest-Soll | **2** | gemessen 2026-08-05 |
| ZT-4 | `CALLID_AUS_ANFRAGE` (vor = nach) = Mindest-Soll | **5** | gemessen 2026-08-05 |
| ZT-5 | `PROFILEID_AUS_ANFRAGE` (vor = nach) = Mindest-Soll | **6** | gemessen 2026-08-05 (neue Menge) |
| ZT-6 | `KENNUNG_AUS_URL` (vor = nach) = Mindest-Soll | **8** | gemessen 2026-08-05 (neue Menge) |
| ZT-7 | Allowlist-Eintraege (beide `FALSCH-TREFFER:`) | **2** | Task 2 |
| ZT-8 | Tests in `tests/test_ownership_state_guard.py` | **9** | Task 2 |
| ZT-9 | Tests in `tests/test_ownership_endpoints.py` | **10** | Task 3 |
| ZT-10 | In dieser Phase GEFIXTE Eingaenge | **8** | B-01, B-02, B-03, N-01, N-02, N-03, **R-7**, **R-8** |
| ZT-11 | GEMELDETE, nicht gefixte Funde | **6** | R-1 bis R-6 |
| ZT-12 | ROT-Lauf (Plan 02 Task 1) | **12 failed / 7 passed** | Herleitung direkt darunter |
| ZT-13 | GRUEN-Lauf (Plan 04 Task 2 / Plan 09) | **19 passed** | ZT-8 + ZT-9 |
| ZT-14 | Browser-Proben der D-06-Gegenprobe (Plan 04 Task 3) | **9** = 6 Fremdzugriff + 3 Gegenproben | Plan 04 Task 3 |

**Herleitung von ZT-12 — die einzige Stelle, an der sie steht:**

| Datei | FAILED | PASSED |
|---|---|---|
| `test_ownership_state_guard.py` (ZT-8 = 9) | **4** — `…zustands…`, `…sid…`, `…call_id…`, `…helfer_existieren…` | **5** — Mindest-Soll, Allowlist, `…profile_id…`, `…url…`, Selbsttest |
| `test_ownership_endpoints.py` (ZT-9 = 10) | **8** — die acht Eingaenge aus ZT-10 | **2** — Fixture-Blindheit, Eigenzugriff |
| **Summe** | **12** | **7** |

⚠ **ZT-5 und ZT-6 tragen im ROT-Lauf nichts bei:** die zwei neuen Mengen sind am Bestand
**gruen** (jede ihrer Funktionen vergleicht bereits gegen eine Server-Identitaet). Ihr
ROT-Beleg ist deshalb der **Selbsttest gegen ein kuenstliches Leck** (Task 2) — nicht ein
kaputter Produktionspfad. Ein Waechter, der nie rot war, beweist nichts (Punkt 31); ein
Waechter, der nur deshalb gruen ist, weil der Bestand sauber ist, muss das **belegen**.


<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-CONTEXT.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-RESEARCH.md
@CLAUDE.md
@tests/test_live_latency_coverage.py
@routes/app_routes.py
@routes/learning.py
</context>

<interfaces>
<!-- Vertraege, die der Executor braucht. Aus dem Code extrahiert (2026-08-04, per grep belegt). -->

**Die SECHS Eingaenge — Ist-Zeilen am Arbeitsstand, per `grep -n` am 2026-08-04 bestaetigt.**
⚠ D-01: Zeilen verschieben sich, die **Such-Muster** nicht. Der Waechter leitet die Menge ohnehin
selbst ab; diese Tabelle dient der Orientierung, NICHT als Pflegeliste.

| ID | Datei:Zeile | Such-Muster | Art |
|---|---|---|---|
| B-01 | `routes/app_routes.py:782` + `:827-829` | `grep -n "_CallModel.id == _phase_d_call_id" routes/app_routes.py` | SCHREIBEND (DB) |
| B-02 | `routes/app_routes.py:187` | `grep -n "str(_posted_call_id)" routes/app_routes.py` | LESEND (Zustand) |
| B-03 | `routes/app_routes.py:2076` | `grep -n "request.args.get('sid')" routes/app_routes.py` | LESEND (Zustand) |
| N-01 | `routes/app_routes.py:1562` | `grep -n "sid = (data.get('sid')" routes/app_routes.py` | SCHREIBEND **und** LESEND (Zustand) |
| N-02 | `routes/learning.py:150` | `grep -n "for _sid, _sd in ls._session_state.items()" routes/learning.py` | SCHREIBEND (fremde Anzeige) |
| N-03 | `routes/learning.py:317` | dasselbe Muster, zweiter Treffer | SCHREIBEND (fremde Anzeige) |

**Das korrekte Vorbild steht in derselben Datei** (nicht erfinden — ablesen):
```python
# routes/app_routes.py:198-201 — die Stufe-2-SID-Aufloesung filtert RICHTIG:
with ls._session_state_lock:
    _cands = [(s, (st.get('session_start_time') or 0.0))
              for s, st in ls._session_state.items()
              if st.get('user_id') == _my_uid]

# routes/app_routes.py:759-764 — der DB-Fallback filtert RICHTIG:
_open_fb = (_db_fb.query(_CallModel_fb)
            .filter(_CallModel_fb.user_id == g.user.id,
                    _CallModel_fb.ended_at.is_(None), ...)
```

**Der Helfer, den Plan 02 baut** (dieser Waechter prueft gegen genau diese zwei Namen):
```python
# services/live_session.py  (kommt in Plan 02 — hier nur der Vertrag)
def sid_belongs_to(sid: str, user_id: int) -> bool
def resolve_own_sid_by_call_id(call_id, user_id: int) -> 'str | None'
```

**Waechter-Vorbild:** `tests/test_live_latency_coverage.py` — reiner Datei-Sweep per `ast.parse`,
**kein App-Import**, Allowlist mit Pflicht-Begruendung, `MINDEST_LIVE_PFADE = 8`,
`RESTLUECKEN`-Absatz mit allen sechs Punkt-31-Bestandteilen. **Mechanik dort abkupfern.**
</interfaces>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
**Dieser Plan liest und schreibt KEINE Produktivtabelle.** Er legt zwei Test-Dateien und zwei
Dokumente an. Der Verhaltens-Waechter aus Task 3 schreibt in die **Test**-Datenbank `nerve_test`
(`public.users`, `public.organisations`, `public.calls`) und raeumt seine Zeilen im Teardown ueber
`cleanup_rows` aus `tests/conftest.py` weg (Test-Cleanup-Regel, `_baseline_cleanup_guard`).

### inspect.sh-Beleg (Production, read-only, gezogen 2026-08-04)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
                                    Table "public.calls"
        Column         |            Type             | Collation | Nullable |    Default
-----------------------+-----------------------------+-----------+----------+---------------
 id                    | uuid                        |           | not null |
 tenant_id             | uuid                        |           |          |
 account_id            | uuid                        |           |          |
 contact_id            | uuid                        |           |          |
 user_id               | integer                     |           | not null |
 call_mode             | text                        |           | not null |
 call_type             | text                        |           |          |
 started_at            | timestamp with time zone    |           |          |
 ended_at              | timestamp with time zone    |           |          |
 transcript_storage    | text                        |           |          |
 transcript_expires_at | timestamp with time zone    |           |          |
 call_summary          | text                        |           |          |
 outcome               | text                        |           |          |
 audio_health_score    | double precision            |           |          |
 coaching_score        | double precision            |           |          |
 meddpicc_extracted    | jsonb                       |           |          |
 created_at            | timestamp without time zone |           |          |
 conversation_log_id   | integer                     |           |          |
 outcome_confidence    | double precision            |           |          |
 outcome_note          | text                        |           |          |
 outcome_source        | text                        |           |          |
 followup_intent       | text                        |           | not null | 'none'::text
 score_breakdown       | jsonb                       |           |          |
 score_schema_version  | smallint                    |           | not null | '1'::smallint
 audio_health_resolved | boolean                     |           | not null | false
 transcript_resolved   | boolean                     |           | not null | false
Indexes:
    "calls_pkey" PRIMARY KEY, btree (id)
    "idx_calls_user_time" btree (user_id, started_at)
Foreign-key constraints:
    "calls_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id)
```

### ⚠ Es gibt KEIN DB-Netz unter Welle 1 — belegt, nicht behauptet

| Tabelle | in dieser Phase | RLS auf Prod? | Folge |
|---|---|---|---|
| `public.calls` | schreibend (B-01) | **NEIN** | Der `WHERE`-Filter im Query ist die **einzige** Kontrolle |
| `public.conversation_logs` | lesend (ueber B-02) | **NEIN** | dito |
| `public.profiles` | lesend (ueber N-01) | **NEIN** | dito |
| `crm.*` | — | ja, `force=true` | gedeckt, nicht betroffen |
| `_session_state` (RAM) | lesend + schreibend | keine DB-Schicht | ausschliesslich App-Kontrolle |

Beleg (Production, `psql` als `postgres`, RESEARCH §1.4-C/§1.5): nur `crm.account_memory`,
`crm.accounts`, `crm.contacts`, `crm.meetings`, `crm.user_preferences` tragen `rls=true
force=true`. `public.calls`/`conversation_logs`/`profiles` stehen **nicht** in der Liste.
Bewusst als `postgres` gefahren — ein leerer Lesevorgang als `nerve_app` waere bei
FORCE-RLS-Tabellen ein Falsch-Negativ.

**Das ist der Grund, warum diese Phase nicht optional ist:** faellt der Filter im Code, faengt
darunter nichts.

### Punkt 23 (Schild) — geprueft, NICHT faellig

```
$ ssh ... 'bash scripts/inspect.sh schilder calls'   (Auszug, 2026-08-04)
 public | Zentraler Call-Datensatz der neuen Architektur (UUID-PK, Outcome/Coaching/
          Transkript-Storage). Status: lebt (neue Architektur Phase 08.23.2.A+).
          Schreibt/liest services/+routes/ der neuen Call-Pipeline; audio_health_resolved
          geschrieben routes/app_routes.py (api_beenden/_audio_health_bg), gelesen
          services/slow_lane.py ...; transcript_resolved geschrieben routes/app_routes.py
          (api_beenden), gelesen services/slow_lane.py ...
```

Das Schild nennt `routes/app_routes.py (api_beenden)` bereits als Schreiber. Diese Phase aendert
**kein Schema**, fuegt **keinen** neuen Schreiber und **keinen** neuen Leser hinzu und aendert den
**Mechanismus** nicht — nur die `WHERE`-Bedingung eines bestehenden Schreibers. **Schild bleibt
unveraendert.** (RESEARCH-Annahme A5 damit geprueft statt angenommen.)

### Schreib-Zeitpunkt / kann der Leser vorauslaufen? (Punkt 26)
Keine neue Lese-Schreib-Naht. Der Besitz-Vergleich liest `_session_state[sid]['user_id']`, das
`init_session_state()` **synchron beim Verbindungsaufbau** setzt — vor jedem Endpunkt-Aufruf, der
die `sid` kennen kann. Kein Hintergrund-Thread, kein Batch-Write, keine Fan-In-Naht noetig.

## Vier Saeulen (Bau-Regel D)
- **Automatisieren:** Die Besitzpruefung entscheidet, nicht Andre. Die Waechter halten sie fest.
- **Nachvollziehen:** B-01 protokolliert den abgelehnten Fremdzugriff (Plan 03); die Funde stehen
  in `08.23.2.SOFORT-2-FUNDE.md` statt nur im Terminal.
- **Eingreifen:** Die Antwortform je Eingang steht als benannte Festlegung in Plan 03, nicht in
  einer Bedingung vergraben.
- **Marge:** Eine Datentrennungs-Panne vor dem EA-Launch kostet mehr als jede Handarbeit, die
  hier gespart wird.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

<tasks>

<task type="auto">
  <name>Task 1: D-02-Pflicht — ALLE Funde durabel melden, auch die nicht gefixten</name>

  <read_first>
    - `.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-RESEARCH.md` §1.2, §1.3, §1.4, §2.3
      (die Fund-Tabellen und die Entlastungen — Quelle des Inhalts)
    - `.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-CONTEXT.md` D-02 (die Pflicht selbst)
    - `.planning/DIALOG-GSD-CLAUDIAN.md` — die letzten 40 Zeilen, um das Eintrags-Format zu sehen
      (`## FRAGE — <Phase> — <Datum>` / `## ROADMAP-SYNC — <Phase> — <Datum>`)
    - CLAUDE.md → „Vier Vault-Regeln", Punkt B (Roadmap-Sync ueber die DIALOG-Datei) und
      „Acht weitere Vault-Regeln", Punkt 4 (Fragen-Kanal) sowie Punkt 5 (Reparatur-Modus)
  </read_first>

  <files>.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md, .planning/DIALOG-GSD-CLAUDIAN.md</files>

  <action>
**(a) Neue Datei `08.23.2.SOFORT-2-FUNDE.md` im Phasenverzeichnis** mit genau diesen vier
Abschnitten (Ueberschriften woertlich uebernehmen):

```markdown
# Phase 08.23.2.SOFORT-2 — Sweep-Funde (D-02-Meldung)

**Sweep-Stand:** 2026-08-04, Repo `e27b8ca`. 168 Route-Handler, davon 118 mit extern
gesteuerter Eingabe. Vier unabhaengige Suchwege (AST-Decorator-Sweep, AST-DB-Ketten ohne
Besitz-Bedingung, `_session_state`-grep, Kennung-aus-Anfrage-grep) plus manuelle Sicht auf
alle 76 Verdachtsfaelle des DB-Wegs.

## 1. In dieser Phase GEFIXT (ACHT Eingaenge — Zahlen-Tafel ZT-10)

| ID | Stelle (Such-Muster) | Art | Was ein Fremdzugriff bewirkt |
|---|---|---|---|
| B-01 | `_CallModel.id == _phase_d_call_id` in `routes/app_routes.py` (zwei Stellen) | SCHREIBEND DB | `ended_at`, `conversation_log_id` (fremder Anruf zeigt mein Transkript), `call_mode`, `score_breakdown`, `transcript_resolved`; stoesst zusaetzlich die Bewertungs-Strecke auf dem fremden Anruf an |
| B-02 | `str(_posted_call_id)` in `routes/app_routes.py` (Stufe-1-SID-Aufloesung) | LESEND Zustand | fremdes Transkript, fremdes Briefing, fremde Sprachstatistik, fremde Painpoints, fremde Phasen-Abdeckung |
| B-03 | `request.args.get('sid')` in `routes/app_routes.py` | LESEND Zustand | `branche`/`detail` aus dem laufenden Fremdgespraech, im Antwort-JSON zurueckgegeben |
| N-01 | `sid = (data.get('sid')` in `routes/app_routes.py` | SCHREIBEND **und** LESEND Zustand | **Prompt-Injektion in ein fremdes laufendes Verkaufsgespraech** (`_briefing` geht in Abschnitt 8 jedes Live-Prompts) **plus** Rueckgabe von Bs Profil-Kontext an A |
| N-02 | `for _sid, _sd in ls._session_state.items()` in `routes/learning.py` (1. Treffer) | SCHREIBEND fremde Anzeige | `outcome_ready`-Emit in den PiP eines fremden Beraters, mitten im Gespraech |
| N-03 | dasselbe Muster, 2. Treffer | SCHREIBEND fremde Anzeige | dito |
| **R-7** | `call_id = data.get('call_id') or None` → `Meeting(… call_id=call_id …)` in `routes/crm_export.py::save_meeting` | SCHREIBEND DB (**Fremdreferenz**) | Konto A haengt einen **fremden** Anruf an seinen **eigenen** Termin. Die RLS auf `crm.*` schuetzt die **Zeile** mandantenweise, **nicht die Fremdreferenz**. Der Code-Kommentar benennt selbst, was daran haengt: „Firma ist der Schluessel, der den Termin spaeter dem richtigen Briefing zuordnet" |
| **R-8** | `profile_id = data.get('profile_id')` → `src = db.get(Profile, profile_id)` in `routes/coach.py::methodik_uebertragen` | LESEND + KOPIEREND DB | Ein Coach kopiert ein **fremdes** Profil (Methodik, Einwaende, Gegenargumente, Skripte) in eine Organisation, auf die er zugewiesen ist, und liest es dort. Der Kommentar im Code sagt woertlich „Coach must own source profile's org" — **die Pruefung fehlt.** Die vorhandene Pruefung deckt nur die **Ziel**-Org (`CoachAssignment`), nicht die **Quelle** |

### Warum R-7 doch gefixt wird (Andre-Entscheidung 2026-08-05, hebt die Vertagung auf)

Die erste Fassung dieses Plans hat R-7 als „gemeldet, nicht gefixt" geparkt — mit der
Begruendung, der Fix brauche eine neue Schicht-Abhaengigkeit (`crm`-Blueprint liest
`public.calls`). **Diese Vertagung ist aufgehoben.** Andres Begruendung, sinngemaess:

> Die Spalte hat **heute keinen Leser** — deshalb heute kein Leck. Genau deshalb ist Vertagen
> die **teure** Variante: ein nur-einfuegender Haken **ohne** Pruefung produziert **stillen
> Muell**, der spaeter nicht mehr von echten Daten unterscheidbar ist. Vault-Leitplanke 4 /
> Tueroeffner-Regel: **verpasste Felder sind fuer immer weg, es gibt kein Nachtragen.**

Die befuerchtete Schicht-Huerde ist **gemessen und klein** (Belege in Plan 09, Abschnitt 5):
`public.calls` traegt **keine** RLS und `nerve_app` hat `SELECT` darauf (`pg_class` +
`has_table_privilege`, Production 2026-08-05) — ein leerer Lesevorgang dort ist also ein
**echter** Abwesenheitsbeweis und **kein** FORCE-RLS-Falsch-Negativ.

### Warum R-8 mitgefixt wird

R-8 ist beim Nachziehen des Waechters aufgefallen (neue Menge `PROFILEID_AUS_ANFRAGE`) und ist
**dieselbe Fehlerklasse**: eine Kennung aus dem Anfrage-Koerper loest eine DB-Zeile auf, ohne
gegen die Identitaet des Anfragenden geprueft zu werden. Der Roadmap-Text dieser Phase nennt
`profile_id` ausdruecklich. Heute ist der Pfad **unerreichbar** — auf Production stehen
**0 Coaches und 0 Coach-Zuweisungen** (gemessen 2026-08-05, `SELECT count(*) FILTER (WHERE
is_coach)` = 0, `coach_assignments` = 0) — und der Fix ist **eine Bedingung**. Nach Andres
Massstab („melden statt bauen nur, wenn die Pruefung teurer ist als eine Zeile") wird er
gebaut. ⚠ **Der Waechter faengt R-8 NICHT** (er sieht `coach_id=g.user.id` und haelt die
Funktion fuer geprueft) — das steht als Durchrutscher im `RESTLUECKEN`-Absatz.

**Warum acht und nicht die drei aus der Roadmap:** siehe „Scope-Begruendung" in Plan 03 und
Plan 09. Kurz: identische Fehlerklasse; fuenf gehen durch **denselben** Helfer, R-7 durch
einen dritten Helfer derselben Familie, R-8 ist eine Bedingung.

## 2. GEMELDET, in dieser Phase NICHT gefixt (Reparatur-Modus — Zahlen-Tafel ZT-11)

| # | Fund | Warum nicht hier | Ablageort |
|---|---|---|---|
| R-1 | `consume_ended_session_by_call_id` ist **toter Code** (`grep -rn "def consume_ended_session_by_call_id" services/` → 0 Treffer, `hasattr` immer False) | Unerreichbarer Zweig; ihn zu „fixen" waere Beifang. **Aber:** wer die Funktion je nachruestet, baut B-02 exakt nach — sie gehoert von Tag 1 mit `user_id`-Bedingung gebaut | Backlog-Kandidat via DIALOG-Eintrag (b) |
| R-2 | Die `sid` wandert im **Query-String** (`static/pip-launcher.js` `fetch('/api/gatekeeper/phrases?sid=' + …)`) → landet im nginx-Zugriffsprotokoll und in der Browser-Historie (ASVS V3) | Andere Fehlerklasse (Sitzungsverwaltung, nicht Besitzpruefung). Der Besitz-Fix macht die erlangte `sid` wertlos — das Protokoll-Leck bleibt | dito |
| R-3 | `static/pip-launcher.js::_showAudioWarning` schreibt `stroke="#f59e0b"` als Hex-Literal in ein Inline-Attribut — Bestands-Verstoss gegen die Farb-Regel | Reparatur-Modus. ⚠ **Nicht abkupfern:** der neue PiP-Hinweis aus Welle 2 nimmt ein Token aus `static/nerve.css` | dito |
| R-4 | Veraltete Selbstverweise in `routes/app_routes.py` (u.a. „Vorher lasen `:193-207`", „Muster: `_audio_health_bg`-finally, `app_routes.py:~810`" — Ist: `:865`+) | Historische Erzaehlungen, keine Wegweiser. Umschreiben waere Reparatur-Modus-Verstoss | dito |
| R-5 | `routes/admin_views.py` `<path:rel>` → `send_from_directory` ist eine **Pfad-Traversal**-Frage (Guard vorhanden: `if '..' in rel or rel.startswith('/')`) | Andere Fehlerklasse, `@superadmin_required` | dito |
| R-6 | Nach `max_retries=0` am Modul-Client (Welle 2, Plan 06) verlieren **`services/judge_runner.py`** und **`services/adoption_runner.py`** ihre SDK-Retries | Bewusste Nebenwirkung der Welle-2-Entscheidung, siehe Plan 06 Task 1. Beide sind Post-Call-Batch hinter `slow_lane` mit eigener Fehlerbehandlung | dito |

## 3. GEPRUEFT UND ENTLASTET (Belege, keine Behauptung)

Ohne diesen Abschnitt kann der naechste Leser nicht unterscheiden, ob eine nicht genannte
Stelle **geprueft** oder bloss **nicht bedacht** war (Punkt 31 Nr. 6).

- **22 Handler unter `@superadmin_required`** (`routes/admin_dashboard.py` 17,
  `routes/admin_ewb.py` 2, `routes/logs_routes.py` 2, `routes/waitlist.py` 2,
  `routes/admin_views.py` 1) — Founder-Flaeche, einziger Nutzer ist Andre.
- **`routes/profiles.py:621`, `:647`, `:745`** — gedeckt durch `_require_own_profile()`
  (`routes/profiles.py:614`: `p.org_id != g.org.id`). Werkzeug-Falschtreffer.
- **`routes/crm_export.py:44/65/98/108/122`** — gedeckt durch Postgres-RLS
  (`crm.* rls=true force=true`, Mandant aus `app.tenant_id`, unangemeldet → `None` → 0 Zeilen).
- **`routes/dashboard.py:754/761`, `routes/learning.py:85/254/622`,
  `routes/app_routes.py:533/575/601/797/2217`, `routes/settings.py:208`** — loesen ueber eine
  **bereits besitzgepruefte Elternzeile** auf. Abgeleitete Kennungen, keine Eingabe.
- **Die gesamte WebSocket-Flaeche** (`services/deepgram_service.py`, 12 `@sio.on`-Handler) —
  keiner nimmt eine `sid` aus der Nachricht, alle benutzen `request.sid`;
  `handle_connect` weist unangemeldete Verbindungen ab. **Diese Flaeche ist sauber** — das ist
  ein Ergebnis, kein Nicht-Ergebnis.
- **`routes/auth.py:375`, `routes/oauth.py:183/219`, `routes/waitlist.py:13/61/83`** — bewusst
  oeffentlich bzw. token-getrieben (kein `g.user` vorhanden ist der Zweck).
- **Kein Blueprint-weiter Besitz-Hook:** `app.py:2283-2308` ist der einzige `before_request`
  und setzt ausschliesslich `g.user`/`g.org`/`g.tenant_id`. **Es gibt keine unsichtbare
  Deckung**, auf die man sich berufen koennte — ausser der RLS auf `crm.*`.

## 4. Grenze dieses Sweeps (ehrlich, Punkt 31)

Die AST-Wege lesen **String-Literale und Namen im Syntaxbaum**. Eine Besitz-Bedingung ueber eine
Variable oder einen Helfer zaehlt als „keine Bedingung" (Falschtreffer — so entstanden die 8
Fehlalarme in Abschnitt 3). Umgekehrt wuerde eine Bedingung, die `user_id` nur **erwaehnt** ohne
zu filtern, als „vorhanden" durchrutschen (Durchrutscher). **Beide Richtungen sind hier durch
manuelle Sicht auf jeden der 76 Verdachtsfaelle geschlossen** — nicht durch das Werkzeug.
Strukturell unsichtbar bleiben: dynamischer Dispatch, alles was erst zur Laufzeit entsteht.
```

**(b) Eintrag ans ENDE von `.planning/DIALOG-GSD-CLAUDIAN.md`** anhaengen (nicht einfuegen,
nicht ersetzen), damit Claudian die SECHS nicht gefixten Funde in die Vault-Roadmap/den Backlog
promotet:

```markdown
## ROADMAP-SYNC — 08.23.2.SOFORT-2 — 2026-08-05

Phase 08.23.2.SOFORT-2 fixt **ACHT** Eingaenge statt der drei aus dem Roadmap-Eintrag
(B-01/B-02/B-03 + N-01 `/api/precall/research` + N-02/N-03 `outcome_ready`-Emit in
`routes/learning.py` + **R-7** `routes/crm_export.py::save_meeting` + **R-8**
`routes/coach.py::methodik_uebertragen`). Begruendung: Plan 03 „Scope-Begruendung" und Plan 09.

**R-7 war zuerst vertagt — Andre hat das am 2026-08-05 aufgehoben.** Grund: die Spalte
`crm.meetings.call_id` hat heute keinen Leser, also heute kein Leck; genau deshalb ist Vertagen
teuer — ein nur-einfuegender Haken ohne Pruefung produziert stillen Muell, der spaeter nicht
mehr von echten Daten unterscheidbar ist (Vault-Leitplanke 4, „verpasste Felder sind fuer immer
weg"). **R-8 ist neu** und beim Nachziehen des Waechters aufgefallen (Menge
`PROFILEID_AUS_ANFRAGE`): `methodik_uebertragen` prueft die Ziel-Org, nicht die Quell-Org — ein
Coach koennte ein fremdes Profil kopieren. Heute unerreichbar (0 Coaches / 0 Zuweisungen auf
Production, gemessen), Fix = eine Bedingung.

**Bitte als Backlog-Eintrag promoten (D-02: gemeldet, nicht gefixt):** R-1 bis R-6 aus
`.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`,
Abschnitt 2. Kurz:
- R-1 toter `consume_ended_session_by_call_id`-Zweig (wer ihn nachruestet, baut B-02 nach)
- R-2 `sid` im Query-String → nginx-Zugriffsprotokoll (ASVS V3)
- R-3 Hex-Literal `#f59e0b` in `_showAudioWarning` (Farb-Regel-Bestandsverstoss)
- R-4 vier veraltete Selbstverweise in `routes/app_routes.py`
- R-5 Pfad-Traversal-Kante `routes/admin_views.py` (Guard vorhanden, andere Fehlerklasse)
- R-6 `judge_runner`/`adoption_runner` verlieren ihre SDK-Retries durch `max_retries=0`

**Zusaetzlich zu melden (kein Fix in dieser Phase, kein Backlog-Eintrag noetig):** der
Einladungsweg `routes/organisations.py` setzt **kein** `is_test_user` auf neu eingeladenen
Konten (`grep is_test_user routes/organisations.py` → 0 Treffer). Ein per Einladung erzeugtes
Testkonto zaehlt damit als **echter** Nutzer in Kennzahlen, DPO-Korpus-Filter und spaeterer
Abrechnung. Diese Phase setzt das Flag fuer ihre Gegenprobe **von Hand** (Plan 04 Task 1);
den Einladungsweg dauerhaft zu reparieren gehoert in eine eigene Mini-Phase.

**Ich aendere `.planning/ROADMAP.md` in dieser Phase NICHT** (Auftrags-Vorgabe).
```

⛔ **`.planning/ROADMAP.md` und `.planning/STATE.md` werden in diesem Plan NICHT angefasst.**
  </action>

  <verify>
    <automated>test -f .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md && echo VORHANDEN</automated>
    <automated>grep -c "^## " .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^## " .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` == 4
      (die vier Pflicht-Abschnitte)
    - Alle ACHT gefixten Fund-IDs stehen in Abschnitt 1 — Existenz-Anker (alle >= 1):
      `grep -c "| B-01 |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`, `grep -c "| B-02 |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`, `grep -c "| B-03 |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`,
      `grep -c "| N-01 |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`, `grep -c "| N-02 |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`, `grep -c "| N-03 |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`,
      `grep -c "| \*\*R-7\*\* |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`, `grep -c "| \*\*R-8\*\* |" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md`
      (⚠ R-7/R-8 stehen **fett** in Abschnitt 1 — genau daran unterscheidet der Anker sie von
      den Zeilen in Abschnitt 2, die mit `| R-n |` beginnen)
    - Die SECHS nicht gefixten Funde stehen in Abschnitt 2 (Zahlen-Tafel ZT-11):
      `grep -cE "^\| R-[1-6] \|" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` == 6
    - ⛔ **R-7 steht NICHT MEHR in Abschnitt 2** — Abwesenheits-Pruefung MIT Existenz-Anker:
      `grep -cE "^\| R-7 \|" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` == 0
      **und** `grep -cE "^\| R-6 \|" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` == 1
      (der zweite Wert beweist, dass der Anker in dieser Datei ueberhaupt greift — ohne ihn
      waere die `0` nicht von „Datei nicht gelesen" unterscheidbar)
    - Jeder nicht gefixte Fund hat einen Ablageort (die Spalte ist nie leer):
      `grep -cE "^\| R-[1-6] \|.*\|.*\|.*[A-Za-z].*\|$" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` == 6
    - Die Aufhebung der R-7-Vertagung ist begruendet festgehalten (Existenz-Anker):
      `grep -c "Diese Vertagung ist aufgehoben" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` >= 1 **und**
      `grep -c "verpasste Felder sind fuer immer weg" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` >= 1
    - R-8 ist als eigener Fund mit Beleg ausgewiesen:
      `grep -c "methodik_uebertragen" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` >= 1 **und**
      `grep -c "0 Coaches" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-FUNDE.md` >= 1
    - Der DIALOG-Eintrag existiert und nennt R-1 bis R-6:
      `grep -c "ROADMAP-SYNC — 08.23.2.SOFORT-2" .planning/DIALOG-GSD-CLAUDIAN.md` == 1
      und `grep -cE "^- R-[1-6] " .planning/DIALOG-GSD-CLAUDIAN.md` == 6
    - Der `is_test_user`-Befund am Einladungsweg ist gemeldet (M-2 aus dem Cross-AI-Review):
      `grep -c 'is_test_user' .planning/DIALOG-GSD-CLAUDIAN.md` >= 1
      (⚠ **einfache** Anfuehrungszeichen, **keine Backticks im Muster**: in doppelten
      Anfuehrungszeichen fuehrt Bash den Inhalt zwischen Backticks als Kommando aus)
    - ⛔ Beweis, dass ROADMAP/STATE unberuehrt sind — **Abwesenheits-Pruefung MIT Existenz-Anker
      daneben** (eine `0` allein koennte auch „git sieht die Dateien nicht" heissen):
      `git status --porcelain .planning/ROADMAP.md .planning/STATE.md | wc -l` == 0
      **und** `git status --porcelain .planning/DIALOG-GSD-CLAUDIAN.md | wc -l` == 1
  </acceptance_criteria>

  <done>
`08.23.2.SOFORT-2-FUNDE.md` existiert mit vier Abschnitten, listet die **acht** gefixten (ZT-10)
und die **sechs** nicht gefixten Funde (ZT-11) je mit Ablageort, begruendet die Aufhebung der
R-7-Vertagung, benennt die Entlastungen mit Beleg und die Grenze des Sweeps. Der DIALOG-Eintrag steht am Dateiende. `ROADMAP.md` und `STATE.md` sind unveraendert.
  </done>
</task>

<task type="auto">
  <name>Task 2: AST-Waechter — die Pruefpunkte werden ABGELEITET, nicht abgeschrieben</name>

  <read_first>
    - `tests/test_live_latency_coverage.py` VOLLSTAENDIG — das Haus-Muster: `REPO_ROOT`,
      `_own_nodes()`, Allowlist mit Pflicht-Begruendung, `MINDEST_LIVE_PFADE = 8`,
      `RESTLUECKEN`-Absatz, **kein App-Import**
    - `routes/app_routes.py` Z.175-220 (B-02 + das korrekte Stufe-2-Vorbild), Z.1534-1580 (N-01),
      Z.2053-2100 (B-03) — der Ist-Zustand, den der Waechter rot sehen muss
    - `routes/learning.py` Z.140-172 und Z.310-340 (N-02/N-03 im Ist-Zustand)
    - CLAUDE.md → Punkt 31 VOLLSTAENDIG (die sechs Pflicht-Bestandteile des RESTLUECKEN-Absatzes,
      der ROT-Beleg als Acceptance-Artefakt, das Mindest-Soll gegen den stillen Ausfall)
    - CONTEXT D-05 (Liste aus dem Code ableiten — Handpflege ist ausdruecklich verboten)
    - RESEARCH §1.6 (warum EIN Helfer statt fuenf Einzel-Bedingungen) und §6.2 (die ehrliche
      Grenze: der DB-Fall B-01 ist per AST nicht zuverlaessig pruefbar)
  </read_first>

  <files>tests/test_ownership_state_guard.py</files>

  <action>
Neue Datei `tests/test_ownership_state_guard.py`. **Reiner statischer AST-Sweep ueber
`routes/*.py`** — keine DB, kein Netz, kein App-Import, damit er gegen jeden ausgecheckten oder
ausgerollten Stand fahrbar ist (das ist die Voraussetzung fuer den ROT-Lauf in Plan 02).

**Die FUENF abgeleiteten Mengen** (per `ast.parse` ueber alle `routes/*.py`, je Funktion).
⚠ **Zwei davon sind neu** (Menge 4 und 5) — sie schliessen die zwei Blindstellen, die der
Cross-AI-Replan vom 2026-08-05 belegt hat: der Roadmap-Text dieser Phase verlangt woertlich
„eine `sid`/`call_id`/`profile_id` aus Request-Body, Query-String **oder URL**", der Waechter
sah bis dahin weder `profile_id` noch URL-Routen-Parameter.

1. `ZUSTANDS_FUNKTIONEN` — jede `ast.FunctionDef`, in deren **eigenem** Rumpf (nicht in
   verschachtelten Funktionen; `_own_nodes`-Muster) ein `ast.Attribute` oder `ast.Name` mit dem
   Bezeichner `_session_state` vorkommt. **Kommentare zaehlen nicht** — das ist der ganze Grund
   fuer AST statt grep (`routes/app_routes.py` enthaelt 8 reine Kommentar-Erwaehnungen).
2. `SID_AUS_ANFRAGE` — jede Funktion, deren Rumpf einen `ast.Call` auf ein Attribut `get`
   enthaelt, dessen erstes Argument die Konstante `'sid'` ist (deckt
   `request.args.get('sid')`, `data.get('sid')`, `request.form.get('sid')`).
3. `CALLID_AUS_ANFRAGE` — dasselbe mit der Konstante `'call_id'`.
4. **NEU** `PROFILEID_AUS_ANFRAGE` — dasselbe mit der Konstante `'profile_id'`.
5. **NEU** `KENNUNG_AUS_URL` — jede Funktion, deren Route-Dekorator-String einen Platzhalter
   der Form `[variable]` oder `[konverter:variable]` (in Flask-Schreibweise mit spitzen
   Klammern) traegt, dessen Variablenname in `{'sid', 'call_id', 'profile_id'}`
   liegt **und** zugleich ein Argument der dekorierten Funktion ist. Erkennung: die
   String-Konstanten aus `fn.decorator_list` (nur `ast.Call`-Dekoratoren) mit
   `re.findall(r'<(?:[^:<>]+:)?([A-Za-z_][A-Za-z0-9_]*)>', s)` auslesen und gegen
   `{a.arg for a in fn.args.args}` schneiden.
   ⚠ **Diese Menge war die eigentliche Blindstelle:** kommt die Kennung ueber die URL, landet
   sie als **Funktions-Argument** im Rumpf — es gibt kein `.get()`, das die Mengen 2-4 sehen
   koennten.

**ZWEI Forderungen, nicht eine — und der Unterschied ist bewusst:**

| Mengen | Forderung | Warum genau diese |
|---|---|---|
| 1, 2, 3 | mindestens ein `ast.Call` auf einen der **drei benannten Besitz-Helfer** im eigenen Rumpf | Diese drei Mengen loesen **Live-Zustand** oder **`public.calls`** auf. Dafuer gibt es seit Plan 02 genau **einen** Ort mit **drei** benannten Funktionen — eine strenge, namens-scharfe Forderung ist hier moeglich |
| 4, 5 | mindestens **eine Server-Identitaet** im eigenen Rumpf: `g.user.id`, `g.org.id`, `g.tenant_id` oder `g.user.org_id` | Die `profile_id`- und URL-Pfade loesen ihre Zeilen ueber **viele verschiedene** Wege auf (`_require_own_profile()`, `filter_by(org_id=g.org.id)`, `filter(CL.user_id == g.user.id)`). Eine Helfer-Pflicht wuerde **vierzehn** heute korrekte Funktionen zum Umbau zwingen — das waere ein Reparatur-Modus-Verstoss. Die schwaechere Regel ist ein **Netz fuer NEUEN Code, der gar nichts prueft**; ihre Grenze steht ausdruecklich im `RESTLUECKEN`-Absatz |

```python
HELFER = ('sid_belongs_to', 'resolve_own_sid_by_call_id', 'call_belongs_to')
SERVER_IDENTITAET = ('g.user.id', 'g.org.id', 'g.tenant_id', 'g.user.org_id')
```
Erkennung der Helfer wie `_is_log_api_cost` im Vorbild: `ast.Name` mit passendem `id` ODER
`ast.Attribute` mit passendem `attr` (deckt `ls.sid_belongs_to(...)` und `sid_belongs_to(...)`).
Erkennung der Server-Identitaet: `ast.unparse(node)` jedes `ast.Attribute` im eigenen Rumpf
gegen `SERVER_IDENTITAET` vergleichen.

⚠ **`call_belongs_to` ist der dritte Helfer** (Plan 02 Task 2). Er beantwortet dieselbe Frage
fuer eine **DB-Zeile** in `public.calls` statt fuer den RAM-Zustand und wird von R-7
(`routes/crm_export.py::save_meeting`, Plan 09) gerufen. **Deshalb faellt der frueher hier
stehende `GEMELDET-NICHT-GEFIXT:`-Allowlist-Eintrag fuer `save_meeting` ersatzlos weg** — er
war ausdruecklich als „wird geloescht, wenn R-7 gebaut ist" formuliert, und R-7 wird gebaut.

**Allowlist** — Datei::Funktion → GRUND, Pflicht-Begruendung >= 20 Zeichen, und jeder Eintrag
muss auf eine **existierende** Funktion zeigen (Muster `test_allowlist_ist_begruendet_und_lebt`).
**Jeder Eintrag beginnt mit genau einem von ZWEI Praefixen** — die Unterscheidung ist der
ganze Punkt (Punkt 31: „Allowlist fuellen" ist die verbotene Abkuerzung, ein **benannter,
begruendeter** Ausnahmefall ist es nicht):

| Praefix | Bedeutung | Erlaubt wann |
|---|---|---|
| `FALSCH-TREFFER:` | Die Ableitung hat die Funktion eingesammelt, obwohl dort **kein Loch** ist — die Besitzpruefung sitzt nachweislich woanders | Beleg mit `Datei:Zeile` Pflicht |
| `GEMELDET-NICHT-GEFIXT:` | Dort ist ein **echtes** Loch, es wird in dieser Phase bewusst **nicht** geschlossen | Nur mit R-Nummer + Verweis auf `08.23.2.SOFORT-2-FUNDE.md` Abschnitt 2 |

Sie startet mit genau **ZWEI** Eintraegen (Zahlen-Tafel ZT-7) — **beide `FALSCH-TREFFER:`,
keiner `GEMELDET-NICHT-GEFIXT:`**. Am Code abgeleitet (2026-08-05, `ast.parse` ueber
`routes/*.py` mit exakt den fuenf Mengen-Definitionen oben):

```python
ALLOWLIST: dict[str, str] = {
    'routes/app_routes.py::_load_beenden_state':
        'FALSCH-TREFFER: verschachtelte Helfer-Funktion in api_beenden '
        '(routes/app_routes.py:211). Sie liest ls._session_state.get(_sid) in :216, bekommt '
        '_sid aber als ARGUMENT aus dem Aufruf-Kontext — und der ist nach dem Fix (Plan 03 '
        'Task 1a) bereits besitzgeprueft: _beenden_sid stammt aus resolve_own_sid_by_call_id '
        'im eigenen Rumpf von api_beenden. Ein Helfer-Aufruf hier waere die zweite Pruefung '
        'derselben Sache. ACHTUNG: der Eintrag faellt weg, sobald _load_beenden_state einen '
        'zweiten Aufrufer bekaeme — der Hygiene-Test meldet ihn dann nicht, deshalb steht es '
        'hier im Klartext.',
    'routes/app_routes.py::api_calls_latest_outcome':
        'FALSCH-TREFFER: nimmt call_id aus dem Query-String (routes/app_routes.py:2116), '
        'filtert den Query aber bereits korrekt auf den Besitzer — :2126 '
        '"q = db_lo.query(Call).filter(Call.user_id == g.user.id)", die call_id verengt nur '
        'zusaetzlich (:2128). Eine fremde call_id liefert 0 Zeilen. Ein Helfer-Aufruf waere '
        'eine zweite Pruefung derselben Sache.',
}
```

⚠ **Vor dem Uebernehmen beide Ziele am Code gegenpruefen** (D-01 — genau diese Phase lebt
davon, keiner Angabe zu trauen):
```bash
grep -n "def _load_beenden_state" routes/app_routes.py        # erwartet: 211
grep -n "def api_calls_latest_outcome" routes/app_routes.py   # erwartet: 2107
```
Weicht eine Zeile ab, ist das kein Grund den Eintrag zu streichen — nur die Zeilenzahl in der
Begruendung nachziehen. Faellt ein **Name** ins Leere, meldet `test_allowlist_ist_begruendet_und_lebt`
das ohnehin rot.

⛔ **`routes/crm_export.py::save_meeting` steht NICHT in der Allowlist** (R-7 wird gebaut, Plan 09).
⛔ **`routes/app_routes.py::_audio_health_bg` steht NICHT in der Allowlist.** Der urspruengliche
Plan hatte ihn dort — er ist **wirkungslos**: `_audio_health_bg` (`routes/app_routes.py:865`)
bekommt `call_id_val` als Argument und liest weder `_session_state` noch `.get('sid')`/
`.get('call_id')`; er steht damit in **keiner** der fuenf Mengen. Ein Allowlist-Eintrag auf eine
Funktion ausserhalb aller Mengen sieht aus wie Deckung, deckt aber nichts.

**NEUN Pruefungen (Zahlen-Tafel ZT-8), genau diese, in dieser Reihenfolge:**

`test_mindestens_erwartete_pruefpunkte_gefunden`
> Sperre gegen den stillen Ausfall (Punkt 31), **FUENF** Mindest-Soll-Zahlen auf **fuenf**
> abgeleiteten Mengen — **je Menge eine eigene Zahl**, nie eine Sammelzahl, sonst deckt eine
> wachsende Menge den Ausfall einer anderen zu.
> Die fuenf Zahlen zaehlen die Menge **VOR** dem Abzug der Allowlist. Das ist Absicht: sonst
> koennte ein wachsender Allowlist-Bestand das Schrumpfen einer Menge zudecken.
> ```python
> # Werte: Zahlen-Tafel ZT-2 / ZT-3 / ZT-4 / ZT-5 / ZT-6 im Plan 01.
> # Am Code abgeleitet 2026-08-05 (ast.parse ueber routes/*.py), NICHT geschaetzt.
> # Zaehlung VOR Allowlist-Abzug.
> #
> # ⚠ DIE ZAHLEN MUESSEN DEN EIGENEN FIX UEBERLEBEN. Ein Mindest-Soll auf einer Menge, die
> # der Fix VERKLEINERT, macht sich selbst dauerhaft rot und zwingt den naechsten Leser zu genau
> # der stillschweigenden Senkung, die die Fehlermeldung verbietet. Deshalb steht hier jeweils
> # der Wert NACH dem Fix; im ROT-Lauf (vor dem Fix) sind die Mengen groesser und die Pruefung
> # ist wegen >= trotzdem PASSED.
> MINDEST_ZUSTANDS_FUNKTIONEN   = 3   # ZT-2. NACH dem Fix: api_beenden (der Stufe-2-Scan bleibt),
>                                     # _load_beenden_state (verschachtelt!), api_gatekeeper_phrases.
>                                     # api_postcall_analysis und api_postcall_outcome loesen
>                                     # _session_state nach Plan 03 Task 2 NICHT MEHR SELBST auf.
>                                     # Vor dem Fix: 5 (ZT-1).
> MINDEST_SID_AUS_ANFRAGE       = 2   # ZT-3. api_gatekeeper_phrases, api_precall_research
> MINDEST_CALLID_AUS_ANFRAGE    = 5   # ZT-4. api_beenden, api_calls_latest_outcome, save_meeting,
>                                     # api_postcall_analysis, api_postcall_outcome
> MINDEST_PROFILEID_AUS_ANFRAGE = 6   # ZT-5. api_set_profile, methodik_uebertragen,
>                                     # api_profile_preview_context, training_start, training_end,
>                                     # api_training_personality_generate
> MINDEST_KENNUNG_AUS_URL       = 8   # ZT-6. api_calls_correct_outcome, session_detail,
>                                     # api_faqs_list, api_faqs_create, api_tabu_update,
>                                     # skript_bearbeiten, skript_loeschen,
>                                     # training_scenarios_delete
> ```
> ⚠ **`_load_beenden_state` ist eine VERSCHACHTELTE Funktion** (`routes/app_routes.py:211`,
> definiert im Rumpf von `api_beenden`). Das Haus-Muster sammelt mit `ast.walk` — verschachtelte
> `FunctionDef` sind also **drin**, waehrend `_own_nodes` nur den Rumpf-Umfang begrenzt. Wer die
> Menge auf Top-Level-Funktionen verengt, macht verschachtelte Zustands-Leser **unsichtbar**.
> Deshalb bleibt `ast.walk`.
> Fehlermeldung muss woertlich enthalten: `Die Zahl NIE stillschweigend senken — Ursache
> klaeren und mit Begruendung nachziehen.`

`test_zustands_aufloesungen_gehen_durch_den_besitz_helfer`
> Fuer jede Funktion in `ZUSTANDS_FUNKTIONEN` minus Allowlist: mindestens ein Helfer-Aufruf im
> eigenen Rumpf. Fehlermeldung nennt `Datei::Funktion (Zeile N)` und den Satz: „Ein
> Ghost-SID-Guard prueft LEBENDIGKEIT, nicht Eigentuemerschaft — das ist die Fehlerklasse
> dieser Phase."

`test_sid_aus_der_anfrage_wird_besitzgeprueft`
> Analog fuer `SID_AUS_ANFRAGE`. Das ist die Pruefung, die N-01 faengt — dort steht die
> Zustands-Aufloesung nicht in `routes/`, sondern wandert ueber `recherche_firma(sid=…)` in
> `services/precall_service.py`. **Ohne diese zweite Menge waere N-01 fuer den Waechter
> unsichtbar.**

`test_call_id_aus_der_anfrage_wird_besitzgeprueft`
> Analog fuer `CALLID_AUS_ANFRAGE`. Faengt B-01, B-02, N-02, N-03 **und R-7**
> (`save_meeting` — seit dem Wegfall seines Allowlist-Eintrags steht er in der Fehlerliste,
> bis Plan 09 ihn ueber `call_belongs_to` fuehrt).

`test_profile_id_aus_der_anfrage_wird_besitzgeprueft`  ← **NEU**
> Fuer jede Funktion in `PROFILEID_AUS_ANFRAGE` minus Allowlist: mindestens eine
> Server-Identitaet im eigenen Rumpf. **Eigenes Mindest-Soll (ZT-5), eigene Fehlermeldung.**
> Fehlermeldung woertlich: „Eine profile_id aus der Anfrage loest eine DB-Zeile auf, ohne dass
> im Rumpf ueberhaupt eine Server-Identitaet vorkommt — das ist dieselbe Fehlerklasse."
> ⚠ Diese Pruefung ist am Bestand **gruen**; ihr ROT-Beleg ist der Selbsttest unten.

`test_kennung_aus_url_wird_besitzgeprueft`  ← **NEU**
> Analog fuer `KENNUNG_AUS_URL`. **Eigenes Mindest-Soll (ZT-6).** Fehlermeldung woertlich:
> „Die Kennung kommt hier als URL-Routen-Parameter an, nicht ueber .get() — genau deshalb war
> sie fuer die Mengen 2-4 unsichtbar." Ebenfalls am Bestand **gruen**; ROT-Beleg unten.

`test_regeln_fangen_ein_kuenstliches_leck`  ← **NEU, der ROT-Beleg der zwei neuen Regeln**
> ⚠ **Ohne diesen Test beweisen die zwei neuen Mengen nichts.** Sie sind am Bestand gruen —
> ein Waechter, der nie rot war, ist wertlos (Punkt 31). Der Test parst einen **inline im Test
> definierten** Quelltext-String (kein Produktionscode, keine Datei) und prueft, dass die
> Ableitung + Regel ihn **flaggt**:
> ```python
> LECK = '''
> @app.route('/api/profile/<int:profile_id>/leak', methods=['POST'])
> @login_required
> def leck_url(profile_id):
>     row = db.query(Profile).filter_by(id=profile_id).first()
>     return jsonify(row.daten)
>
> @app.route('/api/leak2', methods=['POST'])
> @login_required
> def leck_body():
>     pid = request.get_json().get('profile_id')
>     row = db.query(Profile).filter_by(id=pid).first()
>     return jsonify(row.daten)
> '''
> ```
> Erwartung: `leck_url` landet in `KENNUNG_AUS_URL`, `leck_body` in `PROFILEID_AUS_ANFRAGE`,
> und **beide** fallen durch die Server-Identitaets-Regel (kein `g.`-Vergleich im Rumpf).
> Zusaetzlich die Gegenprobe im selben Test: derselbe Code **mit**
> `filter_by(id=profile_id, org_id=g.org.id)` wird **nicht** geflaggt — sonst waere die Regel
> ein Dauer-Alarm und wuerde weggeklickt.
> Das ist ein **Runtime-Test** (die Ableitungs-Funktion wird aufgerufen, ihr Rueckgabewert
> geprueft), kein Source-Presence-Test.

`test_helfer_existieren_und_pruefen_user_id`
> Der Waechter darf nicht gegen Namen pruefen, die es nicht gibt (sonst waere er nach einer
> Umbenennung still gruen, weil die Mengen leer laufen). Per `ast.parse` ueber
> `services/live_session.py`: **alle DREI** Namen aus `HELFER` existieren als `ast.FunctionDef`,
> **und** im Rumpf jedes der drei kommt der Bezeichner `user_id` vor. Fehlt einer → rot mit dem
> Hinweis auf Plan 02.

`test_allowlist_ist_begruendet_und_lebt`
> Muster wortgleich aus `tests/test_live_latency_coverage.py`: Begruendung >= 20 Zeichen **und**
> das Ziel existiert noch (per `ast.walk`, damit auch die verschachtelte Funktion
> `_load_beenden_state` als „lebt" gilt). Ein Eintrag ins Leere weitet den Sweep still auf und
> sieht dabei gruen aus.
>
> **Zusaetzlich — die Praefix-Pflicht** (das ist der Unterschied zwischen einem begruendeten
> Ausnahmefall und dem verbotenen „Allowlist fuellen", Punkt 31):
> - jede Begruendung beginnt mit `FALSCH-TREFFER:` **oder** `GEMELDET-NICHT-GEFIXT:`
> - jede `GEMELDET-NICHT-GEFIXT:`-Begruendung enthaelt eine `R-<Ziffer>`-Nummer **und** die
>   Zeichenkette `FUNDE.md`
> - Fehlermeldung woertlich: `Ein Eintrag ohne Praefix ist eine stille Ausnahme — entweder
>   ist es ein Falsch-Treffer (dann belegen) oder ein Loch (dann als R-Nummer melden).`

**Modul-Docstring — `RESTLUECKEN`-Absatz mit allen SECHS Punkt-31-Bestandteilen** (Wortlaut
uebernehmen, Kern-Saetze woertlich):

```
RESTLUECKEN
-----------
1. ABGEDECKT: FUENF Mengen aus routes/*.py. Mengen 1-3 (loest _session_state auf / liest 'sid'
   bzw. 'call_id' aus der Anfrage) muessen einen der DREI benannten Besitz-Helfer aus
   services/live_session.py rufen. Mengen 4-5 (liest 'profile_id' aus der Anfrage / bekommt
   sid|call_id|profile_id als URL-Routen-Parameter) muessen mindestens eine Server-Identitaet
   (g.user.id / g.org.id / g.tenant_id / g.user.org_id) im eigenen Rumpf tragen.

2. ⚠ DIE ZENTRALE LUECKE: der Waechter sieht, DASS geprueft wird — NICHT, DASS SEIN ERGEBNIS
   BEACHTET WIRD. `if sid_belongs_to(sid, uid): pass` waere gruen. Ebenso sieht er nicht, ob die
   Pruefung VOR oder NACH der Verwendung der Kennung steht (AST-Nachbarschaft ist kein Beweis
   fuer Ausfuehrungs-Reihenfolge). Dagegen schuetzt allein der Verhaltens-Waechter
   tests/test_ownership_endpoints.py.

3. ⚠ DER DB-FALL IST STRUKTURELL UNSICHTBAR: B-01 ist ein fehlender `user_id`-Filter in einem
   SQLAlchemy-Query. Ein AST-Sweep kann „hat einen Besitz-Filter" nicht zuverlaessig
   entscheiden — genau daran scheiterte der Sweep dieser Phase (8 von 84 Verdachtsfaellen waren
   Fehlalarme, weil die Bedingung ueber eine Variable oder einen Helfer lief). Fuer B-01 ist der
   Zwei-Konten-Verhaltenstest die TRAGENDE Schicht, dieser Sweep hoechstens eine zweite.

4. STRUKTURELL UNSICHTBAR, weiteres: geparst wird NUR routes/*.py. Ein Endpunkt, der ausserhalb
   dieses Ordners registriert wird, taucht hier nie auf. Ebenso: dynamischer Dispatch via
   getattr, Callbacks, Registry-Hooks, alles was erst zur Laufzeit entsteht. Und: die Mengen 2-4
   erkennen `.get('sid')` nur mit LITERALEM Schluessel — ein `.get(_KEY)` rutscht lautlos durch.
   INDEX-ZUGRIFF (data['call_id'], request.get_json()['sid']) wird ebenfalls NICHT gesehen;
   gemessen 2026-08-05: HEUTE 0 Fundorte in routes/, also aktuell kein Loch — aber eine offene
   Kante fuer neuen Code.

5. HEURISTIK, zweischneidig — in BEIDE Richtungen:
   - `.get('sid')` trifft JEDES `get` mit diesem Schluessel, auch auf einem Dict, das nichts mit
     der Anfrage zu tun hat (Falschtreffer; Beispiel: routes/training.py::training_end liest
     session.get('profile_id') aus dem Trainings-Zustand, nicht aus der Anfrage).
   - Der URL-Platzhalter `<int:sid>` heisst in routes/dashboard.py eine ConversationLog-Id und in
     routes/profiles.py eine Skript-Id — NAMENSGLEICH, aber kein Sitzungs-Bezug (Falschtreffer;
     beide sind trotzdem korrekt besitzgeprueft und fallen deshalb nicht auf).
   - ⚠ DURCHRUTSCHER der Server-Identitaets-Regel, belegt: routes/coach.py::methodik_uebertragen
     ENTHAELT `coach_id=g.user.id` — aber der Vergleich gilt der ZIEL-Organisation, nicht dem
     QUELL-Profil. Die Regel sieht „irgendeine Identitaets-Erwaehnung", nicht „Vergleich gegen
     das richtige Objekt". Dieser Fund (R-8) wurde VON HAND gefunden und in Plan 09 gefixt —
     NICHT von diesem Waechter. Wer sich auf sein Gruen verlaesst, verlaesst sich auf zu wenig.

6. GEPRUEFT UND GESCHLOSSEN:
   - Die direkteste Form der Fehlerklasse — eine _session_state-Aufloesung in routes/ ganz ohne
     Besitz-Vergleich — ist abgedeckt durch test_zustands_aufloesungen_gehen_durch_den_besitz_helfer.
     Rest-Kante: siehe Punkt 2 (Ergebnis wird nicht beachtet).
   - Die Weitergabe einer fremden sid an einen SERVICE (N-01, recherche_firma) ist abgedeckt
     durch die zweite Menge SID_AUS_ANFRAGE — obwohl in routes/ dort gar keine
     _session_state-Zeile steht.
   - Die Fremdreferenz in eine EIGENE Zeile (R-7, crm.meetings.call_id) ist abgedeckt durch die
     dritte Menge plus den dritten Helfer call_belongs_to.
   - Kennungen aus dem URL-Pfad sind abgedeckt durch die fuenfte Menge (vorher: strukturell
     unsichtbar).
   - Stiller Ausfall der Ableitung: geschlossen durch FUENF getrennte Mindest-Soll-Zahlen.
   - Umbenennung der Helfer: geschlossen durch test_helfer_existieren_und_pruefen_user_id.
   - Dass die zwei neuen Regeln ueberhaupt beissen: geschlossen durch
     test_regeln_fangen_ein_kuenstliches_leck (sie sind am Bestand gruen).
   - NICHT geschlossen: der DB-Fall (Punkt 3) und der Durchrutscher aus Punkt 5.

ZWEITE SCHICHT DARUNTER
-----------------------
tests/test_ownership_endpoints.py (Verhalten, Konto A gegen Konto B) und die Gegenprobe mit zwei
Konten im Browser (D-06, Plan 04). ⚠ Eine DRITTE Schicht gibt es NICHT: public.calls,
public.conversation_logs und public.profiles haben auf Production KEINE RLS (belegt per psql als
postgres: pg_class.relrowsecurity = f, 2026-08-05). Der Filter im Code ist die einzige Kontrolle.

WAS NICHT ERLAUBT IST, wenn dieser Waechter anschlaegt: die Allowlist fuellen, das Muster
aufweichen oder ein Mindest-Soll senken. Einzige zulaessige Ausnahme ist der nachweisliche
Falsch-Treffer, mit `# FALSCH-TREFFER:`-Kommentar, Datei:Zeile und Begruendung.
```

**KEIN Fix in diesem Task.** Der Waechter ist absichtlich rot — der Beleg dafuer wird in Plan 02
Task 1 gezogen.
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('tests/test_ownership_state_guard.py').read_text(encoding='utf-8')); print('SYNTAX OK')"</automated>
    <!-- Der ROT-Lauf gegen den Prod-Stand ist ein EIGENER Schritt (Plan 02 Task 1) —
         hier wird bewusst NICHT auf gruen geprueft. -->
  </verify>

  <acceptance_criteria>
    - `grep -c "ast.parse" tests/test_ownership_state_guard.py` >= 1 (Ableitung per AST)
    - `grep -c "def test_" tests/test_ownership_state_guard.py` == 9  (Zahlen-Tafel ZT-8)
    - Die FUENF Mindest-Soll-Zahlen stehen als eigene Konstanten (je == 1) — Werte aus der
      Zahlen-Tafel ZT-2/ZT-3/ZT-4/ZT-5/ZT-6, **eine Zahl je Menge, keine Sammelzahl**:
      `grep -cF "MINDEST_ZUSTANDS_FUNKTIONEN   = 3" tests/test_ownership_state_guard.py`
      `grep -cF "MINDEST_SID_AUS_ANFRAGE       = 2" tests/test_ownership_state_guard.py`
      `grep -cF "MINDEST_CALLID_AUS_ANFRAGE    = 5" tests/test_ownership_state_guard.py`
      `grep -cF "MINDEST_PROFILEID_AUS_ANFRAGE = 6" tests/test_ownership_state_guard.py`
      `grep -cF "MINDEST_KENNUNG_AUS_URL       = 8" tests/test_ownership_state_guard.py`
      (⚠ **Wer hier eine Zahl aendert, aendert zuerst die Zahlen-Tafel in Plan 01** und greppt
      danach `Zahlen-Tafel` ueber alle Plan-Dateien. Die Zahl lebt an EINEM Ort)
    - `grep -cF "HELFER = ('sid_belongs_to', 'resolve_own_sid_by_call_id', 'call_belongs_to')" tests/test_ownership_state_guard.py` == 1
      (**drei** Namen — `call_belongs_to` ist der Helfer fuer R-7)
    - `grep -cF "SERVER_IDENTITAET = ('g.user.id', 'g.org.id', 'g.tenant_id', 'g.user.org_id')" tests/test_ownership_state_guard.py` == 1
    - Die zwei neuen Mengen existieren namentlich (je == 1):
      `grep -cF "PROFILEID_AUS_ANFRAGE" tests/test_ownership_state_guard.py` >= 2
      `grep -cF "KENNUNG_AUS_URL" tests/test_ownership_state_guard.py` >= 2
    - Der Selbsttest der zwei neuen Regeln existiert:
      `grep -cF "def test_regeln_fangen_ein_kuenstliches_leck" tests/test_ownership_state_guard.py` == 1
    - ⛔ **Der R-7-Allowlist-Eintrag ist FORT** — Abwesenheits-Pruefung MIT Existenz-Anker.
      ⚠ **Die Anker zielen auf die ZEILENFORM eines Allowlist-Eintrags, NICHT auf das Wort.**
      Das ist der Unterschied, an dem diese Phase schon dreimal gescheitert ist: `save_meeting`
      und `GEMELDET-NICHT-GEFIXT:` **muessen** im Waechter vorkommen — ersteres im
      `MINDEST_CALLID_AUS_ANFRAGE`-Kommentar (oben vorgeschrieben), letzteres als Literal in der
      Praefix-Pruefung (ohne das Literal ist die Pruefung nicht baubar). Verboten ist allein,
      dass sie als **Eintrag** in der Allowlist stehen:
      `grep -cE "^ *'routes/crm_export.py::save_meeting'" tests/test_ownership_state_guard.py` == 0
      **und** `grep -cE "^ +'GEMELDET-NICHT-GEFIXT:" tests/test_ownership_state_guard.py` == 0
      **und** `grep -cE "^ *'routes/.*::" tests/test_ownership_state_guard.py` == 2
      (die letzte Zahl ist der **Zaehl-Anker fuer ZT-7** — genau zwei Allowlist-**Schluessel** —
      und zugleich der Existenz-Anker fuer die zwei Abwesenheits-Pruefungen darueber: eine `0`
      bei null Schluesseln waere bedeutungslos)
    - Die Praefix-Pflicht ist wirklich gebaut — **getrennt** vom ZT-7-Zaehler, weil beide
      Literale an mehreren Orten stehen **muessen** (zwei Allowlist-Begruendungen, der
      RESTLUECKEN-Satz, die Praefix-Pruefung selbst):
      `grep -c "FALSCH-TREFFER:" tests/test_ownership_state_guard.py` >= 3
      **und** `grep -c "GEMELDET-NICHT-GEFIXT:" tests/test_ownership_state_guard.py` >= 1
      (⚠ **kein Widerspruch zum Anker darueber**: dort ist die **Zeilenform eines Eintrags**
      verboten, hier wird das **Literal in der Praefix-Pruefung** verlangt)
    - `grep -c "api_calls_latest_outcome" tests/test_ownership_state_guard.py` == **2**
      (einmal als Allowlist-Schluessel, einmal im `MINDEST_CALLID_AUS_ANFRAGE`-Kommentar —
      **beide** sind im Action-Block oben ausdruecklich vorgeschrieben. Eine `1` hier waere
      bei korrektem Bau rot)
    - `grep -cF "RESTLUECKEN" tests/test_ownership_state_guard.py` == 1
    - `grep -cF "ZWEITE SCHICHT DARUNTER" tests/test_ownership_state_guard.py` == 1
    - `grep -cF "GEPRUEFT UND GESCHLOSSEN" tests/test_ownership_state_guard.py` == 1
    - `grep -cF "Die Zahl NIE stillschweigend senken" tests/test_ownership_state_guard.py` == 1
    - `grep -cF "NICHT, DASS SEIN ERGEBNIS" tests/test_ownership_state_guard.py` == 1
      (die zentrale Luecke ist ausdruecklich benannt)
    - Der Durchrutscher (R-8-Klasse) steht im Pruefkatalog, nicht nur im Plan:
      `grep -cF "DURCHRUTSCHER der Server-Identitaets-Regel" tests/test_ownership_state_guard.py` == 1
      **und** `grep -c "methodik_uebertragen" tests/test_ownership_state_guard.py` >= 1
    - Der Index-Zugriff steht als offene Kante mit seiner Messung:
      `grep -cF "HEUTE 0 Fundorte in routes/" tests/test_ownership_state_guard.py` == 1
    - `grep -cF "KEINE RLS" tests/test_ownership_state_guard.py` == 1
      (das fehlende DB-Netz steht im Docstring, nicht nur im Plan)
    - ⛔ Reiner Datei-Sweep — **Abwesenheits-Pruefung MIT Existenz-Anker daneben**:
      `grep -E "^from services|^import services|^from app import|get_session\(" tests/test_ownership_state_guard.py | wc -l` == 0
      **und** `grep -c "^import ast" tests/test_ownership_state_guard.py` == 1
    - Der Waechter laesst sich fehlerfrei parsen:
      `python -c "import ast,pathlib; ast.parse(pathlib.Path('tests/test_ownership_state_guard.py').read_text(encoding='utf-8')); print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
`tests/test_ownership_state_guard.py` existiert, leitet **fuenf** Pruefpunkt-Mengen per AST aus
`routes/*.py` ab (inklusive der URL-Routen-Parameter und `profile_id`), hat **fuenf** getrennte
Mindest-Soll-Zahlen, beweist mit einem kuenstlichen Leck, dass die zwei neuen Regeln beissen,
prueft die Existenz der **drei** Helfer gegen `services/live_session.py`, und benennt im
`RESTLUECKEN`-Absatz alle sechs Punkt-31-Bestandteile — inklusive der ausdruecklichen Aussage,
dass der DB-Fall B-01 und der Durchrutscher aus R-8 fuer ihn strukturell unsichtbar sind.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verhaltens-Waechter — Konto A fragt ACHTMAL eine Ressource von Konto B</name>

  <read_first>
    - `tests/conftest.py` VOLLSTAENDIG — die Fixtures fuer `nerve_test`, der `cleanup_rows`-Helfer
      und `_baseline_cleanup_guard` (Test-Cleanup-Regel: committende Tests raeumen ihre Rows weg)
    - `tests/test_medium_lane_intent_event_live.py` Z.80-110 — Muster fuer einen Fake-Client mit
      `messages.create`/`with_options` (wird hier gebraucht, damit `/api/precall/research` keinen
      echten LLM-Aufruf macht)
    - `routes/app_routes.py` Z.99-130 (`api_beenden`-Kopf, `@login_required`),
      Z.1534-1580 (`api_precall_research`), Z.2053-2100 (`api_gatekeeper_phrases`)
    - `routes/learning.py` Z.20-60 (`api_postcall_analysis`-Kopf) und Z.198-235
      (`api_postcall_outcome`-Kopf) — woher `_posted_call_id` kommt
    - Der Abschnitt „Antwortform je Eingang (BENANNTE FESTLEGUNG)" in
      `08.23.2.SOFORT-2-03-PLAN.md` — **die Erwartungswerte dieses Tests stehen dort**, nicht hier
    - CONTEXT D-05 (pro gefundenem Eingang ein Test) und D-06 (Abnahme an echten Daten)
  </read_first>

  <files>tests/test_ownership_endpoints.py</files>

  <action>
Neue Datei `tests/test_ownership_endpoints.py`. **Runtime-Test, kein Source-Presence-Test**
(Test-Qualitaets-Regel): jeder Fall macht einen echten HTTP-Request ueber den Flask-Test-Client
und prueft **Antwort oder Zustandsaenderung**, nie den Quelltext.

**Aufbau — eine gemeinsame Fixture `zwei_konten`:**
- Legt zwei Organisationen und je einen User an (`A` und `B`), beide `is_test_user=True`.
- Legt fuer `B` einen offenen `calls`-Datensatz an (`user_id=B.id`, `ended_at=None`,
  `call_mode='cold_call'`) und merkt sich seine UUID als `call_id_von_B`.
- Legt in `services.live_session._session_state` eine Sitzung `sid_von_B` an, mit mindestens:
  `{'user_id': B.id, 'org_id': B.org_id, 'state': {'call_id': call_id_von_B,
    'active_profile_data': {'branche': 'GEHEIM-B', 'detail': 'GEHEIM-DETAIL-B'}},
    '_briefing': 'GEHEIM-BRIEFING-B'}`.
  ⚠ Der genaue Aufbau ist per `grep -n "def init_session_state" services/live_session.py` und
  Lesen der Funktion zu bestaetigen — **nicht raten** (D-01 gilt auch fuer Datenstrukturen).
- Loggt Konto **A** ein (Session-Cookie ueber den bestehenden Login-Weg der Test-Suite; Muster
  aus `tests/conftest.py`).
- Teardown: `_session_state` leeren **und** `cleanup_rows` fuer `calls`, `users`,
  `organisations` (Baseline-Sauberkeit, sonst schlaegt `_baseline_cleanup_guard` an).

⚠ **Diese Fixture braucht KEINE zwei einloggbaren Browser-Konten.** Der Browser-Teil von D-06
haengt an einer eigenen Voraussetzung (Plan 04 Task 1) und ist von diesem Test **getrennt**.

**ACHT Faelle — Erwartung je Eingang wie in Plan 03 bzw. Plan 09 festgelegt:**

| Test | Eingang | Aufruf als Konto A | Erwartung |
|---|---|---|---|
| `test_b01_fremder_call_wird_nicht_beendet` | B-01 | `POST /api/beenden` mit `{'call_id': call_id_von_B}` | Die Zeile von B ist danach **unveraendert**: `ended_at IS NULL` **und** `conversation_log_id IS NULL` (frisch aus der DB nachgelesen, nicht aus dem ORM-Cache — `db.expire_all()` davor) |
| `test_b02_fremdes_briefing_nicht_im_antwort_json` | B-02 | derselbe Aufruf | Die Antwort enthaelt **nirgends** die Zeichenkette `GEHEIM-BRIEFING-B` (`assert 'GEHEIM-BRIEFING-B' not in resp.get_data(as_text=True)`) |
| `test_b03_gatekeeper_liefert_keine_fremden_variablen` | B-03 | `GET /api/gatekeeper/phrases?sid=<sid_von_B>` | Status **200**, aber `resp.json['variables']['branche'] == ''` **und** `['detail'] == ''` — und `GEHEIM-B` kommt im ganzen Antwort-Text nicht vor |
| `test_n01_fremde_sitzung_wird_abgelehnt` | N-01 | `POST /api/precall/research` mit `{'firmenname': 'Testfirma GmbH', 'sid': sid_von_B}` | Status **404**. Zusaetzlich: `_session_state[sid_von_B]['_briefing']` ist danach **unveraendert** `'GEHEIM-BRIEFING-B'` (der Schreib-Pfad ist die eigentliche Gefahr) |
| `test_n02_kein_outcome_emit_in_fremden_raum` | N-02 | `POST /api/postcall_analysis` mit `{'call_id': call_id_von_B, …}` | Der gepatchte SocketIO-Emit wurde **nicht** mit `room=sid_von_B` gerufen (`emit`-Mock einsammeln, `room`-kwarg pruefen) |
| `test_n03_kein_outcome_emit_in_fremden_raum_outcome` | N-03 | `POST /api/postcall_outcome` mit `{'call_id': call_id_von_B, …}` | dito |
| `test_r7_fremde_call_id_wird_nicht_an_termin_gehaengt` ← **NEU** | R-7 | `POST /crm/meetings` mit `{'firma': 'Testfirma GmbH', 'call_id': str(call_id_von_B)}` | Status **403**. **Und — das ist der eigentliche Beweis:** in `crm.meetings` existiert danach **keine** Zeile mit `call_id = call_id_von_B` (frisch aus der DB gezaehlt). Gegenprobe im selben Test: derselbe Aufruf **ohne** `call_id` liefert `ok=True` (der Fix darf den Normalfall nicht brechen) |
| `test_r8_fremdes_profil_nicht_uebertragbar` ← **NEU** | R-8 | `POST /coach/methodik/uebertragen` mit `{'profile_id': profil_von_B.id, 'ziel_org_id': org_von_A.id}` als **Coach A** | Status **404**. Zusaetzlich: in `profiles` existiert danach **keine** Zeile mit `org_id = org_von_A.id` und `name = profil_von_B.name` — der Inhalt ist nicht kopiert worden |

**Was die Fixture `zwei_konten` fuer die zwei neuen Faelle ZUSAETZLICH braucht** (alles im
selben Teardown wegraeumen):

1. **Fuer R-7:** Konto A braucht einen **Mandanten**. `save_meeting` liest `g.tenant_id`
   (`routes/crm_export.py:133`) und antwortet ohne ihn mit **403 „Kein Mandant"** — dann waere
   der Test **gruen ohne den Fix zu pruefen**, der klassische Falsch-Beweis.
   ⚠ **Pflicht-Gegenprobe im Test:** der Aufruf **ohne** `call_id` muss `ok=True` liefern.
   Nur damit ist belegt, dass die 403 aus der **Besitzpruefung** kam und nicht aus dem
   fehlenden Mandanten.
   Wie `tenant_id` in die Sitzung kommt: per
   `with client.session_transaction() as sess: sess['tenant_id'] = <tenant_uuid_von_A>`.
   Die Test-Organisation legt der Trigger `trg_mk_tenant_org` automatisch in `tenant_orgs` an —
   **Muster woertlich abkupfern** aus `tests/test_meeting_save_rls.py::_new_tenant`
   (dort steht das INSERT + das Zurueck-Lesen der Trigger-erzeugten `tenant_orgs.id`).
2. **Fuer R-7-Cleanup:** `crm.meetings`, `crm.contacts`, `crm.accounts` gehoeren in den
   Teardown (FK-Reihenfolge: Contacts vor Accounts). ⚠ **Das ist nicht optional:** die
   POST-SUITE-Stufe in `deploy.sh` prueft, dass **jede** `crm.*`-Tabelle danach **0 Zeilen**
   hat. Muster: `tests/test_meeting_save_rls.py`, Fixture `tenants_ab`.
   ⚠ **FORCE-RLS-Falle:** `crm.*` traegt `rls=true force=true`. Ein Lese- oder Loeschvorgang
   **ohne** gesetzte `app.tenant_id`-GUC sieht dort **null Zeilen** — das ist **kein**
   Abwesenheitsbeweis. Sowohl die Zaehl-Assertion als auch das Aufraeumen laufen deshalb
   **innerhalb** einer Transaktion mit `SELECT set_config('app.tenant_id', %s, true)`.
   (`public.calls` ist davon **nicht** betroffen: `relrowsecurity = f`, gemessen 2026-08-05.)
3. **Fuer R-8:** ein Konto **A als Coach** (`is_coach=True`, und `session['is_coach'] = True`
   ueber `client.session_transaction()` — `coach_required` liest die Flask-Sitzung, nicht die
   DB, siehe `routes/coach.py:14-20`), eine `CoachAssignment(coach_id=A.id, org_id=A.org_id,
   aktiv=True)` **auf die eigene Org** (damit die Ziel-Pruefung besteht und wirklich die
   **Quell**-Pruefung getestet wird) und ein `Profile` in **Bs** Org.
   ⚠ **Ohne die Zuweisung waere der Test gruen aus dem falschen Grund** (403 „Kein Zugriff auf
   Ziel-Organisation" statt 404 aus der Quell-Pruefung) — deshalb wird auf **404** geprueft,
   nicht auf „irgendein Fehler".
   Teardown: `coach_assignments`, `profiles` mit `cleanup_rows`.

**Zwei Pflicht-Zusatztests gegen den stillen Ausfall (Punkt 31):**

`test_fixture_baut_wirklich_eine_fremde_sitzung`
> ⚠ **Ohne diesen Test sind alle acht oben wertlos.** Wenn die Fixture die Sitzung von B gar
> nicht anlegt, sind alle Assertions trivial erfuellt und der Waechter ist **blind statt rot,
> sieht dabei aber gruen aus.** Der Test prueft **positiv**: `sid_von_B` steht in
> `_session_state`, `_session_state[sid_von_B]['user_id'] == B.id`,
> `_session_state[sid_von_B]['_briefing'] == 'GEHEIM-BRIEFING-B'`, die `calls`-Zeile von B
> existiert mit `user_id == B.id` und `ended_at IS NULL`, **das Profil von B existiert in Bs
> Org**, und **A und B sitzen in VERSCHIEDENEN Organisationen** (`A.org_id != B.org_id`) —
> der letzte Punkt ist die Voraussetzung dafuer, dass R-8 ueberhaupt etwas beweist.

`test_eigener_zugriff_funktioniert_weiter`
> Die Gegenprobe. Konto **B** (eigener Login) ruft `GET /api/gatekeeper/phrases?sid=<sid_von_B>`
> und bekommt `variables['branche'] == 'GEHEIM-B'`. **Ohne diesen Test wuerde ein Fix, der
> einfach ALLE Zugriffe verweigert, gruen durchgehen** — und das Produkt waere kaputt.

**Modul-Docstring mit `RESTLUECKEN`-Absatz** (Punkt 31, alle sechs Bestandteile). Kern-Saetze
woertlich:

```
RESTLUECKEN
-----------
1. ABGEDECKT: dass Konto A ueber die ACHT bekannten Eingaenge weder Daten von Konto B liest
   noch dessen Zustand, DB-Zeilen oder Fremdreferenzen veraendert — geprueft am VERHALTEN
   (HTTP-Antwort, DB-Zeile, Emit-Raum), nicht am Quelltext.

2. STRUKTURELL UNSICHTBAR: dieser Test kennt genau die acht Eingaenge, die der Sweep vom
   2026-08-04/05 gefunden hat. Ein NEUNTER Eingang, der spaeter entsteht, taucht hier NIE auf.
   Genau dagegen steht der abgeleitete Sweep tests/test_ownership_state_guard.py — die beiden
   Waechter decken bewusst zwei verschiedene Fehlerarten ab und ersetzen einander NICHT.

3. HEURISTIK, zweischneidig: die Lecks werden ueber MARKIERTE Zeichenketten (GEHEIM-*) gesucht.
   Ein Leck, das die Daten umformt (zusammenfasst, uebersetzt, in einen Vektor kippt), rutscht
   lautlos durch (Durchrutscher). Umgekehrt wuerde ein legitimer Antworttext, der zufaellig
   'GEHEIM-B' enthaelt, faelschlich rot melden (Falschtreffer) — deshalb sind die Marker
   bewusst unnatuerliche Zeichenketten.

4. UNKLAR: ob die Fixture den Live-Zustand strukturgleich nachbaut. Der Aufbau von
   _session_state[sid] ist per grep aus services/live_session.py::init_session_state
   uebernommen; weicht die echte Struktur ab, koennte ein Fall gruen sein, obwohl der Endpunkt
   in Produktion anders laeuft. Dagegen steht die Gegenprobe mit zwei Konten im Browser (D-06).
   EBENFALLS UNKLAR: ob der Test-Login denselben tenant_id-Weg nimmt wie der echte Login. Die
   Fixture setzt tenant_id explizit in die Sitzung; weicht der Produktionsweg ab, prueft der
   R-7-Fall eine andere Konstellation als die Wirklichkeit.

5. GEPRUEFT UND GESCHLOSSEN:
   - Der DB-Fall B-01, den der AST-Sweep strukturell NICHT sehen kann: hier abgedeckt durch
     test_b01_fremder_call_wird_nicht_beendet (Zeile frisch aus der DB nachgelesen).
   - Der R-8-Durchrutscher, den die Server-Identitaets-Regel des AST-Sweeps NICHT sieht: hier
     abgedeckt durch test_r8_fremdes_profil_nicht_uebertragbar.
   - Die Fremdreferenz R-7, die keine fremde ZEILE anfasst und deshalb von RLS nicht gedeckt
     ist: hier abgedeckt durch test_r7_fremde_call_id_wird_nicht_an_termin_gehaengt, samt
     Gegenprobe ohne call_id (sonst waere eine 403 aus fehlendem Mandanten ein Falsch-Beweis).
   - Blindheit der Fixture: geschlossen durch test_fixture_baut_wirklich_eine_fremde_sitzung.
   - Ueberschiessender Fix ("verweigere einfach alles"): geschlossen durch
     test_eigener_zugriff_funktioniert_weiter.

6. ZWEITE SCHICHT DARUNTER: die Gegenprobe mit zwei Konten im Browser (D-06, Plan 04 Task 3).
   ⚠ Eine DRITTE Schicht gibt es NICHT: public.calls hat auf Production KEINE RLS. Fuer crm.*
   gibt es sie (force=true) — sie schuetzt aber die ZEILE, nicht die FREMDREFERENZ darin (R-7).
```

**KEIN Fix in diesem Task.** Acht der zehn Tests sind absichtlich rot; die zwei
Zusatztests muessen **gruen** sein (sonst ist die Fixture kaputt, nicht der Code).
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('tests/test_ownership_endpoints.py').read_text(encoding='utf-8')); print('SYNTAX OK')"</automated>
    <!-- Der ROT-Lauf auf dem Server ist ein EIGENER Schritt: Plan 02 Task 1. -->
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_" tests/test_ownership_endpoints.py` == 10  (Zahlen-Tafel ZT-9:
      acht Eingaenge + zwei Zusatztests gegen den stillen Ausfall)
    - Alle acht Fall-Namen existieren (je == 1):
      `grep -cF "def test_b01_fremder_call_wird_nicht_beendet" tests/test_ownership_endpoints.py`,
      `grep -cF "def test_b02_fremdes_briefing_nicht_im_antwort_json" …`,
      `grep -cF "def test_b03_gatekeeper_liefert_keine_fremden_variablen" …`,
      `grep -cF "def test_n01_fremde_sitzung_wird_abgelehnt" …`,
      `grep -cF "def test_n02_kein_outcome_emit_in_fremden_raum" …`,
      `grep -cF "def test_n03_kein_outcome_emit_in_fremden_raum_outcome" …`,
      `grep -cF "def test_r7_fremde_call_id_wird_nicht_an_termin_gehaengt" …`,
      `grep -cF "def test_r8_fremdes_profil_nicht_uebertragbar" …`
    - `grep -cF "def test_fixture_baut_wirklich_eine_fremde_sitzung" tests/test_ownership_endpoints.py` == 1
    - `grep -cF "def test_eigener_zugriff_funktioniert_weiter" tests/test_ownership_endpoints.py` == 1
    - `grep -cF "GEHEIM-BRIEFING-B" tests/test_ownership_endpoints.py` >= 2
      (Marker wird gesetzt UND geprueft)
    - Die R-7-Gegenprobe ohne `call_id` existiert — sonst waere eine 403 aus fehlendem
      Mandanten ein Falsch-Beweis:
      `grep -c "ohne call_id" tests/test_ownership_endpoints.py` >= 1
    - Die FORCE-RLS-Behandlung fuer `crm.*` ist im Test verankert, nicht nur im Plan:
      `grep -c "set_config('app.tenant_id'" tests/test_ownership_endpoints.py` >= 1
    - `grep -cF "cleanup_rows" tests/test_ownership_endpoints.py` >= 1
      (Test-Cleanup-Regel: committende Tests raeumen ihre Rows weg)
    - Der crm-Teil raeumt **alle drei** Tabellen (deploy.sh POST-SUITE verlangt 0 Zeilen je
      `crm.*`-Tabelle): `grep -c "crm.meetings\|crm.contacts\|crm.accounts" tests/test_ownership_endpoints.py` >= 3
    - `grep -cF "RESTLUECKEN" tests/test_ownership_endpoints.py` == 1
    - `grep -cF "ZWEITE SCHICHT DARUNTER" tests/test_ownership_endpoints.py` == 1
    - `grep -cF "GEPRUEFT UND GESCHLOSSEN" tests/test_ownership_endpoints.py` == 1
    - ⛔ Kein Source-Presence-False-Green (Test-Qualitaets-Regel) — **Abwesenheits-Pruefung MIT
      Existenz-Anker daneben**:
      `grep -E "inspect\.getsource|open\('routes|subprocess\.run\(\['grep'" tests/test_ownership_endpoints.py | wc -l` == 0
      **und** `grep -c "client.post\|client.get" tests/test_ownership_endpoints.py` >= 8
      (der zweite Wert beweist, dass es echte HTTP-Requests gibt — ohne ihn ist die erste `0`
      nicht von „nichts gelesen" unterscheidbar)
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('tests/test_ownership_endpoints.py').read_text(encoding='utf-8')); print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
`tests/test_ownership_endpoints.py` existiert, prueft alle **acht** Eingaenge am **Verhalten**
mit zwei echten Test-Konten in **zwei verschiedenen Organisationen**, hat die zwei
Pflicht-Zusatztests gegen Fixture-Blindheit und ueberschiessende Fixes, raeumt seine Zeilen in
`public.*` **und** `crm.*` weg und benennt seine Grenzen im `RESTLUECKEN`-Absatz.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Client → Flask-Endpunkt (`routes/`) | Hier ueberquert eine **vom Angreifer gewaehlte** `sid`/`call_id` die Grenze. `@login_required` beweist nur „irgendein Konto", nicht „dieses Objekt gehoert dir" |
| Flask-Endpunkt → `_session_state` (RAM) | Kein DB-Layer darunter. Der Guard `if sid not in _session_state` prueft **Lebendigkeit**, nicht Eigentuemerschaft |
| Flask-Endpunkt → `public.calls` (Postgres) | **KEINE RLS** (belegt per `psql` als `postgres`, 2026-08-04). Der `WHERE`-Filter im Query ist die einzige Kontrolle |
| Repo → Prod-Dateisystem | `scp` von zwei Test-Dateien nach `/opt/nerve/app/tests/` fuer den ROT-Beleg (Plan 02) |
| Test-Suite → `nerve_test` (Postgres) | Task 3 committet Zeilen in `users`/`organisations`/`calls` und muss sie wegraeumen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SOF2-01 | Information Disclosure | `routes/app_routes.py` Stufe-1-SID-Aufloesung (B-02) und `api_gatekeeper_phrases` (B-03) | mitigate | Besitz-Helfer `sid_belongs_to`/`resolve_own_sid_by_call_id` (Plan 02) + AST-Waechter Menge 1/2 + Verhaltenstest mit `GEHEIM-*`-Markern |
| T-SOF2-02 | Tampering | `routes/app_routes.py` `calls`-UPDATE ohne `user_id` (B-01) | mitigate | `_CallModel.user_id == g.user.id` in **beide** Filter (Plan 03 Task 3) + `test_b01_fremder_call_wird_nicht_beendet` (die tragende Schicht — AST sieht das nicht) |
| T-SOF2-03 | Tampering | `POST /api/precall/research` schreibt `_briefing` in fremde laufende Sitzung (N-01) | mitigate | 404 bei Fremdbesitz (Plan 03 Task 1) + AST-Waechter Menge 2 (`SID_AUS_ANFRAGE`) — die einzige Menge, die N-01 ueberhaupt sichtbar macht |
| T-SOF2-04 | Spoofing | `outcome_ready`-Emit in fremden SocketIO-Raum (N-02/N-03) | mitigate | Raum aus dem eigenen Kontext ableiten (`resolve_own_sid_by_call_id`), nie aus der Anfrage (Plan 03 Task 2) |
| T-SOF2-05 | Elevation of Privilege | Ghost-SID-Guard `if sid not in _session_state` als vermeintliche Sicherung | mitigate | Der `RESTLUECKEN`-Absatz beider Waechter benennt Lebendigkeit≠Eigentuemerschaft ausdruecklich; der AST-Waechter fordert den **Helfer**, nicht irgendeine Bedingung |
| T-SOF2-06 | Repudiation | Waechter ohne ROT-Beleg | mitigate | Der ROT-Lauf ist ein **eigener Schritt** (Plan 02 Task 1), seine Ausgabe wandert **verbatim** in Commit-Body und SUMMARY (Punkt 31, D-05) |
| T-SOF2-07 | Information Disclosure | Antwortform verraet Existenz einer fremden Kennung (403 vs 404) | mitigate | Benannte Festlegung pro Eingang in Plan 03 („Antwortform je Eingang"): **404** bei N-01, sonst „wirkt wie nicht vorhanden". Nie 403 |
| T-SOF2-08 | Information Disclosure | `sid` im Query-String → nginx-Zugriffsprotokoll, Browser-Historie (ASVS V3) | accept | Andere Fehlerklasse (Sitzungsverwaltung). Nach dem Besitz-Fix ist eine erlangte `sid` wertlos. **Gemeldet als R-2** in `08.23.2.SOFORT-2-FUNDE.md`, Ablageort DIALOG-Eintrag |
| T-SOF2-08b | Tampering | `routes/crm_export.py::save_meeting` — geposteten `call_id` wird ungeprueft `Meeting.call_id` (R-7) | mitigate | ⚠ **Disposition am 2026-08-05 von `accept` auf `mitigate` geaendert** (Andre hebt die Vertagung auf). RLS auf `crm.*` schuetzt die Zeile, **nicht die Fremdreferenz** — A kann einen fremden Anruf an seinen eigenen Termin haengen. **Fix in Plan 09** ueber den dritten Helfer `call_belongs_to`; erfasst wird der Eingang von Menge 3 (`CALLID_AUS_ANFRAGE`), der Allowlist-Eintrag ist **geloescht**. Verhaltenstest: `test_r7_fremde_call_id_wird_nicht_an_termin_gehaengt` |
| T-SOF2-09 | Denial of Service | Test-Zeilen in `nerve_test` bleiben liegen und roten die Baseline | mitigate | `cleanup_rows` im Teardown (Test-Cleanup-Regel); `_baseline_cleanup_guard` erzwingt es |
| T-SOF2-08c | Information Disclosure | `routes/coach.py::methodik_uebertragen` — geposteten `profile_id` waehlt die QUELLE einer Profil-Kopie (R-8) | mitigate | **Fix in Plan 09** (`src.org_id != g.org.id` → 404). ⚠ **Kein Waechter faengt diesen Fall:** die Funktion enthaelt `coach_id=g.user.id`, die Server-Identitaets-Regel haelt sie deshalb fuer geprueft — der Vergleich gilt aber der ZIEL-Org. Als **Durchrutscher** im `RESTLUECKEN`-Absatz benannt; Verhaltenstest `test_r8_fremdes_profil_nicht_uebertragbar` |
| T-SOF2-09b | Repudiation | Die Allowlist waechst still und deckt echte Loecher zu | mitigate | Praefix-Pflicht (`FALSCH-TREFFER:` / `GEMELDET-NICHT-GEFIXT:`) in `test_allowlist_ist_begruendet_und_lebt`; `GEMELDET-NICHT-GEFIXT` braucht R-Nummer + `FUNDE.md`-Verweis. Zusaetzlich zaehlt das Mindest-Soll **vor** dem Allowlist-Abzug, damit eine wachsende Allowlist keine schrumpfende Menge zudecken kann |
| T-SOF2-10 | Elevation of Privilege | `scp` von Test-Dateien nach `/opt/nerve/app/tests/` | mitigate | Beide Dateien werden nur von `pytest` gelesen, kein Dienst-Neustart; der AST-Waechter hat **keinen App-Import** (per Acceptance greppbar); nach dem ROT-Lauf wieder abgeraeumt (Plan 02) |
</threat_model>

<verification>
1. `08.23.2.SOFORT-2-FUNDE.md` hat vier Abschnitte, **acht** gefixte (ZT-10) und **sechs**
   nicht gefixte Funde (ZT-11) je mit Ablageort, plus Entlastungen und Sweep-Grenze
2. DIALOG-Eintrag steht am Dateiende, nennt R-1 bis R-6 und meldet den `is_test_user`-Befund
   am Einladungsweg
3. `.planning/ROADMAP.md` und `.planning/STATE.md` sind laut `git status --porcelain` unveraendert
4. `tests/test_ownership_state_guard.py`: **9** Tests (ZT-8), **fuenf** Mindest-Soll-Zahlen,
   drei Helfer-Namen, Selbsttest gegen ein kuenstliches Leck, kein App-Import,
   `RESTLUECKEN` mit allen sechs Punkt-31-Bestandteilen inkl. „der DB-Fall ist strukturell
   unsichtbar" und dem belegten Durchrutscher `methodik_uebertragen`
5. `tests/test_ownership_endpoints.py`: **10** Tests (ZT-9: 8 Eingaenge +
   Fixture-Blindheits-Test + Gegenprobe), Runtime-Assertions statt Source-Presence,
   `cleanup_rows` im Teardown, `crm.*` unter gesetzter Tenant-GUC geraeumt
6. Die Allowlist hat genau **zwei** Eintraege, **beide** `FALSCH-TREFFER:` (ZT-7) — der
   frueher dort geparkte R-7-Eintrag ist fort
7. **KEIN Deploy und KEIN Fix in diesem Plan.** `bash deploy.sh production` waere hier rot —
   das ist der Zweck (Plan 02 zieht den Beleg, Plan 03 + Plan 09 fixen, Plan 04 rollt aus)
</verification>

<success_criteria>
- Jeder Fund hat einen durablen Ablageort (D-02 erfuellt, auch fuer die nicht gefixten)
- Beide Waechter leiten ihre Pruefpunkte ab bzw. pruefen Verhalten — keiner schreibt die
  RESEARCH-Liste ab (D-05)
- **Jede der fuenf abgeleiteten Mengen hat ihr EIGENES Mindest-Soll** (Punkt 31: rot statt blind)
- Die zwei neuen Mengen belegen an einem kuenstlichen Leck, dass sie beissen
- Beide `RESTLUECKEN`-Absaetze enthalten alle sechs Pflicht-Bestandteile, inklusive
  `GEPRUEFT UND GESCHLOSSEN`, `ZWEITE SCHICHT DARUNTER` und dem belegten Durchrutscher
- `ROADMAP.md`/`STATE.md` unveraendert
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-01-SUMMARY.md`
— mit der Liste der gemeldeten Funde und dem Vermerk, dass der ROT-Beleg in Plan 02 folgt.
</output>
