---
phase: 08.23.2.SOFORT-2
plan: 06
type: execute
wave: 6
depends_on: ["08.23.2.SOFORT-2-05"]
files_modified:
  - services/claude_service.py
  - services/qa_pipeline.py
  - tests/test_stabil1_http_llm_timeout.py
  - tests/test_medium_lane_intent_event_live.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Kein Live-LLM-Aufruf laeuft mehr ohne Zeitlimit"
    - "Die Stream-Pfade bekommen die ZWEITE Zahl und werden NICHT an der Gesamtdauer gekappt"
    - "Der Modul-Client wird mit max_retries=0 erzeugt — der Worst Case ist 1 x Limit, nicht 3 x"
    - "Die zwei Bestandstests, die diese Vertragsaenderung bricht, sind UMGESCHRIEBEN — nicht geloescht und nicht falsch-gruen stehengelassen"
    - "Alle Selbstverweise im http_llm_client-Docstring nennen Namen statt Zeilennummern: :812/:980 UND (:27) sind fort (D-01)"
  artifacts:
    - path: "services/claude_service.py"
      provides: "timeout= an sieben Aufrufen + max_retries=0 am Modul-Client"
      contains: "max_retries=0"
    - path: "services/qa_pipeline.py"
      provides: "timeout= an classify_utterance"
      contains: "LIVE_LLM_TIMEOUT_S"
    - path: "tests/test_stabil1_http_llm_timeout.py"
      provides: "umgeschriebener Daemon-Pfad-Test: MIT Zeitlimit, OHNE Retry"
      contains: "LIVE_LLM_TIMEOUT_S"
  key_links:
    - from: "services/claude_service.py"
      to: "config.py"
      via: "config.LIVE_LLM_TIMEOUT_S / config.LIVE_LLM_STREAM_TIMEOUT_S am messages-Aufruf"
      pattern: "LIVE_LLM_(STREAM_)?TIMEOUT_S"
---

**Komplexitaet: 🔴** (Live-Pfad) — Cross-AI-Review ist Pflicht VOR Execute.

<objective>
Der Einbau. Ein Mechanismus, zwei Zahlen, neun Aufruf-Stellen — plus die eine Zeile am
Modul-Client, ohne die das Limit im Worst Case dreimal so lange haelt wie es behauptet.

Purpose: Ohne Limit greifen die SDK-Vorgaben (`read=600`, `max_retries=2`) → Worst Case ~30 min
fuer EINEN Aufruf. Weil `analyse_loop`/`coaching_loop` sequentiell ueber alle SIDs iterieren,
stehen in dieser Zeit **alle** Gespraeche still. Der `try/except` faengt Ausnahmen, **nicht
Haenger**; der Lock-Wachhund ueberwacht den **Riegel**, nicht die Schleife.

Output: `timeout=` an neun Stellen, `max_retries=0` am Modul-Client, zwei umgeschriebene Tests,
zwei nachgezogene Selbstverweise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-CONTEXT.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-RESEARCH.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-05-SUMMARY.md
@CLAUDE.md
@config.py
@services/claude_service.py
@services/qa_pipeline.py
@tests/test_stabil1_http_llm_timeout.py
</context>

<interfaces>
<!-- Aus dem Code extrahiert (2026-08-04, per grep -n belegt) — KEIN Erkunden noetig. -->

★ **NEUN gesamt, ACHT angefasst — Zahlen-Tafel ZT-17 (Plan 01).**
⚠ **C-3:** bis zum Replan stand hier *"Die NEUN Aufruf-Stellen"*, waehrend der Task-Titel
weiter unten *"an den ACHT produktiven Aufrufen"* sagte — aufgeloest erst dreissig Zeilen
spaeter (`generate_qa_response` ist die neunte und bleibt unangetastet). **Wer nur den
Task-Titel liest, baut acht und der Waechter zaehlt neun.** Deshalb steht die Aufteilung jetzt
an EINEM Ort (ZT-17) und der Task-Titel nennt sie ausdruecklich: *"acht von neun"*.

**Die NEUN Aufruf-Stellen — davon ACHT angefasst (ZT-17). ⚠ D-01: Zeilen wandern, Funktionsnamen nicht — jede Stelle per
Funktionsnamen suchen, nie per Zeilennummer.**

| # | Datei | Funktion (Such-Muster) | Aufruf | Art | Zahl |
|---|---|---|---|---|---|
| 1 | `services/claude_service.py` | `grep -n "def classify_phase"` | `messages.create` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 2 | `services/claude_service.py` | `grep -n "def infer_customer_state"` | `messages.create` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 3 | `services/claude_service.py` | `grep -n "def analysiere_mit_claude"` | `messages.create` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 4 | `services/claude_service.py` | `grep -n "def analysiere_und_klassifiziere"` | `messages.create` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 5 | `services/claude_service.py` | `grep -n "def streame_auto_variante"` | **`messages.stream`** | **Stream** | `LIVE_LLM_STREAM_TIMEOUT_S` |
| 6 | `services/claude_service.py` | `grep -n "def streame_manual_ewb_variante"` | **`messages.stream`** | **Stream** | `LIVE_LLM_STREAM_TIMEOUT_S` |
| 7 | `services/claude_service.py` | `grep -n "def analysiere_coaching"` | `messages.create` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 8 | `services/qa_pipeline.py` | `grep -n "def classify_utterance"` | `messages.create` | blockierend | `LIVE_LLM_TIMEOUT_S` |
| 9 | `services/qa_pipeline.py` | `grep -n "def generate_qa_response"` | `messages.create` | **toter Code** | ⛔ **NICHT anfassen** — steht auf der Waechter-Allowlist |

**Ist-Stand des Modul-Clients** (`services/claude_service.py:27`, per
`grep -n "anthropic.Anthropic(" services/claude_service.py` bestaetigt):
```python
claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)
```

**Ist-Stand `http_llm_client`** (`:30-43`) — **beide** Zweige setzen `max_retries` ausdruecklich.
Das ist der Beleg, dass `max_retries=0` am Modul-Client die HTTP-Pfade **nicht** beruehrt:
```python
    if long_running:
        return claude_client.with_options(
            timeout=config.HTTP_LLM_TIMEOUT_LONG_S, max_retries=0)
    return claude_client.with_options(
        timeout=config.HTTP_LLM_TIMEOUT_S, max_retries=config.HTTP_LLM_MAX_RETRIES)
```

**Ist-Stand der Docstring-Selbstverweise** (`services/claude_service.py:33-34`):
```
    Phase 08.23.2.STABIL-1: der Modul-Client (:27) bleibt bewusst ohne timeout —
    ein globales Limit wuerde die messages.stream-Aufrufe (:812/:980) kappen.
```
⚠ **`:27` ist HEUTE korrekt** (per `grep -n "anthropic.Anthropic(" services/claude_service.py` →
`27:claude_client = ...`), **`:812/:980` ist veraltet** (Ist: `:898`/`:1090`).
**Die D-01-Tabelle im CONTEXT NICHT abschreiben** — sie stand an dieser Zeile selbst einmal
falsch (`:28`), und wer ihr folgte, haette einen **richtigen** Verweis „korrigiert".

⛔ **Trotzdem fallen in Task 1 BEIDE Zeilennummern-Verweise weg, auch der heute richtige.**
Der Grund steht in **Task 2 desselben Plans**: dort wird `import httpx` ganz oben ergaenzt, und
der Konstruktor wandert damit auf `:28`. Ein `(:27)` im Docstring waere danach **still falsch**
— waehrend ein `grep -cF`-Kriterium darauf **weiterhin gruen** bliebe. Genau diese Kombination
(gruener Haken auf einer Luege) ist die Fehlerklasse, gegen die D-01 antritt. Deshalb: Verweise
auf **Namen** umstellen, keine `== 27`-Abnahme.

**Zwei Stale-Tests, die diese Vertragsaenderung bricht** — Belege:
- `tests/test_stabil1_http_llm_timeout.py`, `test_daemon_pfad_ohne_zeitlimit`:
  `assert mock_with_options.called is False, "Daemon-Pfad darf with_options() (Zeitlimit) NICHT aufrufen"`
  → bliebe **zufaellig gruen** (wir rufen kein `with_options`), waehrend die Aussage **falsch**
  wird. Genau die Klasse Fehler, die still gruen aussieht.
- `tests/test_medium_lane_intent_event_live.py`: `def create(self, *, model, max_tokens, system, messages):`
  → strikte Signatur ohne `**kwargs` → `TypeError: create() got an unexpected keyword argument 'timeout'`.

**Der Anthropic-SDK-Vertrag** (`anthropic 0.86.0`, auf Prod per `pip show anthropic` bestaetigt):
```python
# messages.create(..., timeout: float | httpx.Timeout | None | NotGiven = NOT_GIVEN)
# messages.stream(..., timeout: float | httpx.Timeout | None | NotGiven = NOT_GIVEN)
# anthropic.Anthropic(api_key=..., max_retries: int = 2, timeout: float | Timeout = DEFAULT_TIMEOUT)
# httpx.Timeout(<read/write/pool>, connect=<connect>)
```
</interfaces>

## Latenz-Budget (CLAUDE.md Punkt 25 — Pflicht, weil dieser Plan den Live-Antwort-Pfad anfasst)

| Was | Wirkung auf die Antwort-Latenz | Beleg |
|---|---|---|
| `timeout=httpx.Timeout(X, connect=config.LLM_CONNECT_TIMEOUT_S)` als Aufruf-Argument | **null** — der Wert reist als Feld im Anfrage-Objekt mit (`anthropic/_base_client.py`: `timeout=… if isinstance(options.timeout, NotGiven) else options.timeout`). Kein zusaetzlicher Aufruf, kein Netz, keine Objekt-Kopie | RESEARCH §3.1 |
| `max_retries=0` am Modul-Client | **null im Erfolgsfall.** Im Fehlerfall **schneller**: kein Backoff-Schlaf, kein zweiter Versuch | RESEARCH §3.4 |
| `httpx.Timeout(...)`-Objekt je Aufruf erzeugen | ~µs, reine Datenklasse | httpx `_config.py` |
| **Was NICHT gebaut wird** | eine Uhr im Token-Loop haette pro Token einen `monotonic()`-Vergleich gekostet — **und waere unnoetig** (§3.1) | Punkt 27 |

**Budget:** Die gemessenen Ø-Werte aus MESSGERAETE-1 duerfen sich durch diesen Plan **nicht
messbar** aendern. **Die Messung dazu ist D-06 (Plan 08):** ein echter Test-Anruf, `api_cost_log`
gegen die Tabelle Analyse 1988 / Coaching 2714 / Phase 1742 / Coldcall 1642 / TTFT 1035 ms.

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
**Keine.** ⛔ **`log_api_cost` wird ausdruecklich NICHT angefasst** (Festlegung F-5a aus Plan 05:
bei einer Zeitueberschreitung wird **nichts** gebucht — es sind keine Token angefallen, und
`latency_ms = <Limit>` wuerde genau die Statistik verfaelschen, an der D-06 abnimmt).
Kein Schema, keine Migration, kein Backfill. **Punkt 23 (Schild) nicht faellig** — kein neuer
Schreiber, kein neuer Leser, kein geaenderter Mechanismus auf `api_cost_log`.

### Cross-Layer-Konsistenz-Tabelle

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|---|
| `config.LIVE_LLM_TIMEOUT_S` / `_STREAM_TIMEOUT_S` | **neu gelesen** an neun Aufrufen | Modul-Konstante `config.py` | Prozess-Start | nein | ✓ Plan 05 Task 1 |
| `api_cost_log.latency_ms` / `ttft_ms` | **unveraendert** — der Hook liegt hinter bzw. innerhalb des Aufrufs und wird von der Ausnahme uebersprungen | DB-Spalten | synchron | nein | ✓ Plan 05 F-5a, am Code nachgefahren |

## Vier Saeulen (Bau-Regel D)
- **Automatisieren:** Der Abbruch entscheidet der Code nach einer benannten Zahl, nicht ein
  Mensch, der einen haengenden Prozess bemerkt.
- **Nachvollziehen:** Der Zaehler kommt in Plan 07 (`api_cost_log` bleibt bewusst sauber).
- **Eingreifen:** `LIVE_LLM_TIMEOUT_S` / `LIVE_LLM_STREAM_TIMEOUT_S` per ENV **ohne Code-Deploy** — aber **wirksam erst nach einem Prozess-Neustart** (`config.py` liest beim Start, F-N7.3).
- **Marge:** Ein haengender Aufruf legt heute alle gleichzeitigen Gespraeche still — das ist
  beim EA-Launch ein Ausfall pro Nutzer, nicht pro Aufruf.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

<tasks>

<task type="auto">
  <name>Task 1: max_retries=0 am Modul-Client + die zwei veralteten Selbstverweise</name>

  <read_first>
    - `services/claude_service.py` Z.20-45 — der Modul-Client `:27`, der Docstring `:33-34` und
      **beide** `with_options`-Zweige von `http_llm_client` (der Beleg, dass die HTTP-Pfade
      `max_retries` ausdruecklich setzen und deshalb unberuehrt bleiben)
    - `config.py` — der `HTTP_LLM_*`-Block: `HTTP_LLM_MAX_RETRIES` steht dort seit STABIL-1 auf
      `0`, mit einer ausfuehrlichen Begruendung. **Dieselbe Entscheidung, anderer Ort** — der
      Kommentar dort ist die Vorlage
    - RESEARCH §2.1 **und** §Open Questions (⚠ **die CONTEXT-D-01-Tabelle war an der
      `:27`-Zeile selbst falsch, inzwischen korrigiert** — und
      ⛔ **die dortige RESOLVED-Notiz zu „Erklaerblock unter der Zeile, damit `:27` gueltig
      bleibt" ist ueberholt:** Task 2 verschiebt den Konstruktor durch `import httpx` auf
      `:28`, deshalb faellt der Zeilennummern-Verweis in dieser Revision ganz weg) und
      §2.3 (die Liste der veralteten Selbstverweise + welche mitkorrigiert werden duerfen)
    - RESEARCH §3.4 (`max_retries` gilt pro Versuch) und §3.7 (warum Modul-Client statt
      zweitem Client — die Import-Zeit-Falle mit 91 Monkeypatches)
    - Der Abschnitt „F-5 (b)" in `08.23.2.SOFORT-2-05-PLAN.md` (die Entscheidung samt der
      gemeldeten Nebenwirkung R-6)
  </read_first>

  <files>services/claude_service.py</files>

  <action>
**(a) Modul-Client.** Such-Muster: `grep -n "anthropic.Anthropic(" services/claude_service.py`

Die Zeile

```python
claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)
```

**ersetzen** durch — ⚠ **der Erklaerblock steht UNTER der Zeile, nicht darueber**:

```python
claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY, max_retries=0)
# ↑ SOFORT-2 (D-03/D-04): max_retries=0. Das SDK wiederholt bei Zeitueberschreitung, 408, 409,
# 429 und >=500 — und ehrt dabei einen Retry-After-Header. Das Limit gilt PRO VERSUCH, nicht
# kumulativ: mit der SDK-Vorgabe 2 haelt ein 12-s-Limit im Worst Case 3 x 12 s + Backoff ~= 38 s.
# Ein Waechter, der nur `timeout=` prueft, bleibt dabei GRUEN — deshalb steht max_retries
# ausdruecklich im Pruefkatalog von tests/test_live_timeout_coverage.py.
# HIER am Modul-Client und nicht als zweiter Client: (1) es deckt auch kuenftige Live-Pfade,
# (2) http_llm_client() setzt max_retries in BEIDEN Zweigen selbst und bleibt unberuehrt,
# (3) ein Modul-Level with_options(...) waere beim IMPORT ausgewertet — die 91 vorhandenen
# monkeypatch.setattr(cs, 'claude_client', ...) in 16 Testdateien wuerden ihn nicht mehr
# erreichen und still am Fake vorbeilaufen (falsche Greens, genau die Fehlerklasse dieser Phase).
# BEWUSSTE NEBENWIRKUNG, gemeldet als R-6: services/judge_runner.py und
# services/adoption_runner.py benutzen claude_client direkt und verlieren ihre SDK-Retries.
# Beide sind Post-Call-Batch hinter slow_lane mit eigener Fehlerbehandlung.
# Dieselbe Entscheidung wie config.HTTP_LLM_MAX_RETRIES = 0 (STABIL-1), an einem zweiten Ort.
# Der Block steht BEWUSST unter der Zeile, damit der Konstruktor die erste Zeile des Blocks
# bleibt und nicht unter vierzehn Kommentarzeilen verschwindet.
```

⚠ **Warum unter und nicht darueber:** damit der Konstruktor die Kopfzeile seines eigenen
Erklaerblocks bleibt — man findet ihn dort, wo man ihn sucht. **Es haengt aber ausdruecklich
KEIN Abnahme-Kriterium mehr an seiner Zeilennummer** (siehe (b) und die frueher hier stehende
`== 27`-Pruefung, die in dieser Revision entfernt wurde): Task 2 dieses Plans fuegt `import httpx`
ganz oben ein und schiebt den Konstruktor damit ohnehin auf `:28`. **Eine Zeilennummer als
Anker ist genau das, was D-01 abschafft — diese Phase zementiert keine neue.**

**(b) BEIDE Selbstverweise im Docstring von `http_llm_client` — auf NAMEN statt Zeilennummern.**
Such-Muster: `grep -n ":812/:980\|(:27)" services/claude_service.py`

⚠ **Diese Revision hat hier umgesteuert.** Frueher stand hier: *„NUR `:812/:980` wird angefasst,
`:27` bleibt“* — mit einem Abnahme-Kriterium auf `grep -n "anthropic.Anthropic("` == `27`.
Das war eine Falle: **Task 2 dieses Plans fuegt `import httpx` ganz oben ein und schiebt den
Konstruktor auf `:28`.** Der Verweis `(:27)` waere danach falsch, sein `grep -cF`-Kriterium
aber weiterhin gruen — ein gruener Haken auf einer Luege, und die `== 27`-Pruefung waere
unerfuellbar. **Deshalb faellt die Zeilennummer ganz weg**, in beiden Verweisen. Das ist
dieselbe Behandlung, die `:812/:980` ohnehin bekommt, und es ist die einzige Variante, die die
Fehlerklasse beseitigt statt sie um eine Zeile zu verschieben (D-01).

Die zwei Zeilen

```
    Phase 08.23.2.STABIL-1: der Modul-Client (:27) bleibt bewusst ohne timeout —
    ein globales Limit wuerde die messages.stream-Aufrufe (:812/:980) kappen.
```

**ersetzen** durch:

```
    Phase 08.23.2.STABIL-1: der Modul-Client (claude_client, das Modul-Global direkt ueber
    dieser Funktion) bleibt bewusst ohne timeout — ein globales Limit wuerde die
    messages.stream-Aufrufe (streame_auto_variante / streame_manual_ewb_variante) kappen.
    Zeilen wandern — deshalb hier durchgehend Namen statt Zeilennummern. Der Modul-Client
    steht als einzige claude_client-Zuweisung direkt ueber dieser Funktion; die
    Stream-Aufrufe lagen am 2026-08-04 auf :898 und :1090.
    ⚠ SOFORT-2: der Modul-Client hat inzwischen max_retries=0 (aber weiterhin KEIN globales
    timeout) — die Live-Aufrufe setzen ihr Zeitlimit je Aufruf, mit zwei Zahlen.
```

**(c) Derselbe veraltete Verweis in `tests/test_stabil1_http_llm_timeout.py`.**
Such-Muster: `grep -n ":812/:980" tests/test_stabil1_http_llm_timeout.py`
Dort im Docstring von `test_modul_client_bleibt_unveraendert` ebenfalls durch die
Funktionsnamen ersetzen. **Das ist der einzige zulaessige Beifang** (D-01: die zwei
Stream-Verweise liegen im angefassten Fehler-Pfad).

**⛔ Reparatur-Modus — NICHT mitkorrigiert:** die vier historischen Selbstverweise in
`routes/app_routes.py` (gemeldet als R-4). Sie sind Erzaehlungen („vorher stand hier…"), keine
Wegweiser.
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; ast.parse(pathlib.Path('services/claude_service.py').read_text(encoding='utf-8')); print('SYNTAX OK')"</automated>
    <automated>grep -c "max_retries=0" services/claude_service.py</automated>
  </verify>

  <acceptance_criteria>
    - Der Modul-Client hat `max_retries=0` (Existenz-Anker):
      `grep -cF "claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY, max_retries=0)" services/claude_service.py` == 1
    - ⛔ Der alte Konstruktor ist weg — Abwesenheits-Pruefung MIT Existenz-Anker daneben:
      `grep -c "anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)$" services/claude_service.py` == 0
      **und** `grep -c "anthropic.Anthropic(" services/claude_service.py` == 1
      (der zweite Wert beweist, dass es genau einen Konstruktor gibt — eine `0` allein waere
      nicht von „Datei nicht gelesen" unterscheidbar, und `>1` waere ein zweiter Client)
    - ⛔ Der veraltete Verweis ist weg — Abwesenheits-Pruefung MIT Existenz-Anker:
      `grep -c ":812/:980" services/claude_service.py tests/test_stabil1_http_llm_timeout.py | grep -c ":0$"` == 2
      **und** `grep -c "streame_manual_ewb_variante" services/claude_service.py` >= 2
    - ⛔ **KEIN Verweis im Docstring haengt mehr an einer Zeilennummer** — der Verweis nennt den
      **Namen** (D-01; Task 2 dieses Plans verschiebt den Konstruktor durch `import httpx`
      ohnehin auf `:28`, eine `== 27`-Abnahme waere danach unerfuellbar und ein `(:27)` im
      Kommentar waere still falsch geworden):
      `grep -cF "der Modul-Client (claude_client, das Modul-Global direkt ueber" services/claude_service.py` == 1
      **und** — Abwesenheits-Pruefung MIT Existenz-Anker daneben —
      `grep -c "(:27)" services/claude_service.py` == 0
      **und** `grep -c "claude_client" services/claude_service.py` >= 5
      (der dritte Wert beweist, dass die Datei ueberhaupt gelesen wurde — eine `0` allein waere
      nicht von „Datei nicht gefunden“ unterscheidbar)
    - Der Konstruktor bleibt die **Kopfzeile** seines Erklaerblocks (Lesbarkeit, kein
      Zeilennummern-Anker) — per AST statt per `grep -n`, damit kein Import die Abnahme kippt:
      `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('services/claude_service.py').read_text(encoding='utf-8')); print('CLIENT_ZUWEISUNGEN', sum(1 for n in ast.walk(t) if isinstance(n,ast.Assign) and any(getattr(x,'id',None)=='claude_client' for x in n.targets)))"`
      gibt `CLIENT_ZUWEISUNGEN 1`
    - Die Nebenwirkung ist im Code benannt, nicht nur im Plan:
      `grep -c "R-6" services/claude_service.py` == 1
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('services/claude_service.py').read_text(encoding='utf-8')); print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
Der Modul-Client wird mit `max_retries=0` erzeugt, die Entscheidung samt Nebenwirkung steht als
Kommentar an der Stelle. **Alle drei Selbstverweise im Docstring von `http_llm_client` nennen
jetzt Namen statt Zeilennummern** — die zwei veralteten Stream-Verweise (`:812/:980`) und der
bisher korrekte `(:27)`, den Task 2 durch den `httpx`-Import ohnehin ungueltig gemacht haette.
  </done>
</task>

<task type="auto">
  <name>Task 2: timeout= an ACHT VON NEUN Aufrufen — zwei Zahlen, ein Mechanismus</name>

  <read_first>
    - `services/claude_service.py` — **alle sieben produktiven Aufruf-Stellen**, je 10 Zeilen
      davor und danach. Per `grep -n "messages.create\|messages.stream" services/claude_service.py`
      lokalisieren. ⚠ Besonders: die zwei `messages.stream`-Stellen, wo `_t_api_start` und die
      `_ttft_ms`-Messung aus MESSGERAETE-1 stehen — **der Mess-Anker darf nicht verrutschen**
    - `services/qa_pipeline.py` Z.290-320 (`classify_utterance` — die achte Stelle) und
      Z.440-460 (`generate_qa_response` — die **neunte**, die NICHT angefasst wird)
    - `config.py` — die Konstanten aus Plan 05 Task 1
    - RESEARCH §3.1 (⭐ **der Kern:** `timeout=` ist NIE ein Gesamt-Limit, `read` gilt pro
      Datenblock — deshalb wirkt derselbe Parameter beim blockierenden Aufruf faktisch als
      Gesamt-Limit und beim Stream als TTFT-plus-Stillstands-Limit **ohne** die Gesamtdauer zu
      kappen) und §3.2 (`httpx.Timeout(X, connect=config.LLM_CONNECT_TIMEOUT_S)` — `connect` nicht anfassen, 5 s ist
      bereits scharf)
    - RESEARCH §3.6 (der Kontext-Manager-Fallstrick: die HTTP-Anfrage laeuft in `__enter__`,
      ein TTFT-Timeout fliegt also **an der `with`-Zeile selbst**; beide Call-Sites haben ihr
      `try` bereits **aussen** — das muss so bleiben)
    - CLAUDE.md → Punkt 25 (Latenz-Budget) und Punkt 14 (kein Funktionsaufruf im Argument —
      der Hoist-Blocker aus MESSGERAETE-1)
  </read_first>

  <files>services/claude_service.py, services/qa_pipeline.py</files>

  ★ **F-N7.1 — `connect` ist jetzt zentral, und der Worst Case hat eine Zahl.**
  Das frueher hier stehende Literal `connect=5.0` stand an **neun** Aufruf-Stellen ohne
  Zentrale — dieselbe "eine Zahl an N Orten"-Falle, gegen die die Zahlen-Tafel gebaut wurde,
  nur in Welle 2. Es wird durch `config.LLM_CONNECT_TIMEOUT_S` ersetzt (Plan 05 Task 1, F-5).
  **Der Wert bleibt 5.0** — Reparatur-Modus, er bekommt nur einen Ort.

  ⚠ **WORST CASE BLOCKIEREND = 5 s Verbindung + 12 s Lesen ≈ 17 s.** Diese Zahl stand bis zum
  Replan **nirgends** im Plan-Satz, obwohl **D-06 gegen sie misst**. httpx zaehlt `connect` und
  `read` **nacheinander**, nicht als Gesamtbudget. Fuer die Stream-Pfade: 5 + 8 = **13 s** bis
  zum ersten Token. Wer das Gesamt-Budget senken will, senkt **beide** Zahlen — nicht nur die
  auffaellige. Die 17 s gehoeren **wortgleich** in die SUMMARY und in die D-06-Abnahme
  (Plan 08).

  <action>
**Der Import.** Ganz oben in `services/claude_service.py` und `services/qa_pipeline.py`
sicherstellen, dass `httpx` verfuegbar ist:
```python
import httpx
```
Per `grep -n "^import httpx" services/claude_service.py services/qa_pipeline.py` pruefen. `httpx`
ist als Abhaengigkeit von `anthropic` ohnehin installiert (Prod-Fassung 0.28.1, per
`ssh … pip show httpx` bestaetigen). `config` ist in beiden Dateien bereits importiert
(`grep -n "^import config"`).

**Die Ergaenzung an den SIEBEN blockierenden bzw. Stream-Aufrufen in `services/claude_service.py`
und der EINEN in `services/qa_pipeline.py`.**

Fuer die **fuenf blockierenden** Aufrufe (`classify_phase`, `infer_customer_state`,
`analysiere_mit_claude`, `analysiere_und_klassifiziere`, `analysiere_coaching`) und den einen in
`qa_pipeline.py` (`classify_utterance`): als **letztes** Schluesselwort-Argument ergaenzen —

```python
        timeout=httpx.Timeout(config.LIVE_LLM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S),
```

Fuer die **zwei Stream**-Aufrufe (`streame_auto_variante`, `streame_manual_ewb_variante`):

```python
            timeout=httpx.Timeout(config.LIVE_LLM_STREAM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S),
```

**Beispiel, wie es danach aussieht** (`classify_phase`, Einzug an die Fundstelle anpassen):

```python
        _t_api_start = time.monotonic()
        resp = claude_client.messages.create(
            model=config.MODEL_PHASE_CLASSIFY,
            max_tokens=60,
            messages=[{'role': 'user', 'content': prompt}],
            # SOFORT-2 (D-03): EIN Mechanismus, ZWEI Zahlen. `timeout=` geht 1:1 an httpx,
            # wo `read` PRO DATENBLOCK gilt. Eine blockierende Antwort kommt als EIN Koerper —
            # damit wirkt der Wert hier faktisch wie ein Gesamt-Limit. connect=config.LLM_CONNECT_TIMEOUT_S ist der
            # bewaehrte SDK-Wert und bleibt scharf.
            timeout=httpx.Timeout(config.LIVE_LLM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S),
        )
```

Und fuer einen Stream (`streame_auto_variante`):

```python
        _t_api_start = time.monotonic()
        with claude_client.messages.stream(
            model=_model_autovar,
            max_tokens=500,
            system=_system_autovar,
            messages=[{'role': 'user', 'content': user_msg}],
            # SOFORT-2 (D-03): derselbe Parameter, die ZWEITE Zahl. Beim Stream ist jedes Token
            # ein eigener Datenblock — `read` heisst hier "nie laenger als X Sekunden Stille".
            # Der ERSTE Block muss innerhalb von X kommen => X ist automatisch die TTFT-Grenze.
            # Die GESAMTDAUER wird dadurch NICHT gekappt: eine lange, aber fliessende Antwort
            # laeuft unbegrenzt weiter. Genau das verlangt D-03.
            # ⚠ Rest-Kante: X kappt auch eine Pause MITTEN in der Antwort. Deshalb ist
            # LIVE_LLM_STREAM_TIMEOUT_S (8 s) bewusst weit ueber dem gemessenen TTFT-Ø (1035 ms).
            timeout=httpx.Timeout(config.LIVE_LLM_STREAM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S),
        ) as stream:
```

**FUENF Dinge, die dabei NICHT passieren duerfen:**

1. ⛔ **`services/qa_pipeline.py::generate_qa_response` wird NICHT angefasst.** Toter Code, steht
   auf der Waechter-Allowlist. Ein `timeout=` dort waere Wegwerf-Arbeit und wuerde den
   Allowlist-Eintrag fragwuerdig machen.
2. ⛔ **Der Mess-Anker `_t_api_start` bleibt, wo er ist** — unmittelbar vor dem Aufruf. Der
   MESSGERAETE-1-Waechter (`tests/test_live_latency_coverage.py`) prueft das nicht, aber die
   Zahlen wuerden luegen. Das `timeout`-Argument ist ein reiner Konstanten-Zugriff plus ein
   `httpx.Timeout(...)`-Konstruktor (µs) — **kein Funktionsaufruf, der teuren Prompt-Bau
   ausloest** (Punkt 14 / der Hoist-Blocker aus MESSGERAETE-1).
3. ⛔ **Das `try` um die `with`-Bloecke bleibt AUSSEN.** Die HTTP-Anfrage laeuft in `__enter__` —
   ein Verbindungs- oder TTFT-Timeout fliegt **an der `with`-Zeile selbst**, nicht im
   Schleifenrumpf. Ein `try` **im** `with` faengt ihn nicht. Beide Call-Sites haben es bereits
   aussen; das nicht umbauen.
4. ⛔ **Kein `finally: stream.close()`.** `__exit__` schliesst den Stream auch bei Ausnahme oder
   `break` (SDK `lib/streaming/_messages.py`). Zusaetzliche Mechanik ohne Nutzen.
5. ⛔ **Keine Uhr im Token-Loop.** D-03 verlangt sie nicht und §3.1 zeigt, dass sie unnoetig
   ist (Punkt 27).

**Und:** `log_api_cost` wird **nicht** angefasst (F-5a). Kein neuer Aufruf, kein neuer Tag.
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ('services/claude_service.py','services/qa_pipeline.py')]; print('SYNTAX OK')"</automated>
    <automated>grep -c "LIVE_LLM_TIMEOUT_S\|LIVE_LLM_STREAM_TIMEOUT_S" services/claude_service.py services/qa_pipeline.py</automated>
  </verify>

  <acceptance_criteria>
    - Die zwei Zahlen sitzen an den richtigen Stellen (Existenz-Anker):
      `grep -cF "timeout=httpx.Timeout(config.LIVE_LLM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S)," services/claude_service.py` == 5
      `grep -cF "timeout=httpx.Timeout(config.LIVE_LLM_STREAM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S)," services/claude_service.py` == 2
      `grep -cF "timeout=httpx.Timeout(config.LIVE_LLM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S)," services/qa_pipeline.py` == 1
    - ⛔ **Kein produktiver Aufruf ohne `timeout`** — als **AST**-Pruefung, nicht als grep
      (die vom Plan vorgeschriebenen Erklaerkommentare enthalten die Woerter `timeout`,
      `messages.stream` und `messages.create`; ein grep-Zaehler traefe sie mit — der AST sieht
      Kommentare nicht):
      ```
      python -c "import ast,pathlib; tot={'generate_qa_response'}; res=[];
      exec('''
      for p in ('services/claude_service.py','services/qa_pipeline.py'):
          t=ast.parse(pathlib.Path(p).read_text(encoding=\"utf-8\"))
          for fn in [n for n in ast.walk(t) if isinstance(n,ast.FunctionDef)]:
              if fn.name in tot: continue
              for c in ast.walk(fn):
                  if isinstance(c,ast.Call) and isinstance(c.func,ast.Attribute) and c.func.attr in (\"create\",\"stream\") and isinstance(c.func.value,ast.Attribute) and c.func.value.attr==\"messages\":
                      res.append((p,fn.name,\"timeout\" in [k.arg for k in c.keywords]))
      '''); print('AUFRUFE', len(res), 'OHNE_TIMEOUT', [r[:2] for r in res if not r[2]])"
      ```
      gibt `AUFRUFE 8` und `OHNE_TIMEOUT []`
      — die `8` ist der Existenz-Anker: `OHNE_TIMEOUT []` bei `AUFRUFE 0` waere bedeutungslos
    - ⛔ Der tote Pfad ist **unangetastet** — Abwesenheits-Pruefung MIT Existenz-Anker:
      `python -c "import ast,pathlib; t=ast.parse(pathlib.Path('services/qa_pipeline.py').read_text(encoding='utf-8')); f=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='generate_qa_response']; print('GEFUNDEN',len(f),'MIT_TIMEOUT',[k.arg for x in f for c in ast.walk(x) if isinstance(c,ast.Call) for k in c.keywords if k.arg=='timeout'])"`
      gibt `GEFUNDEN 1 MIT_TIMEOUT []`
    - `httpx` ist importiert:
      `grep -c "^import httpx" services/claude_service.py` == 1
      **und** `grep -c "^import httpx" services/qa_pipeline.py` == 1
    - ⛔ Keine Uhr im Token-Loop, kein `stream.close()`-Beifang — Abwesenheits-Pruefung MIT
      Existenz-Anker:
      `git diff -U0 services/claude_service.py | grep -cE "^\+.*(stream\.close\(\)|while .*monotonic|break  # timeout)"` == 0
      **und** `git diff -U0 services/claude_service.py | grep -c "^+"` >= 20
    - ⛔ `log_api_cost` unangetastet (F-5a):
      `git diff -U0 services/claude_service.py services/qa_pipeline.py | grep -c "^[+-].*log_api_cost"` == 0
      **und** `grep -c "log_api_cost" services/claude_service.py` >= 10
      (der zweite Wert beweist, dass die Hooks noch da sind)
    - Der Mess-Anker steht weiterhin unmittelbar vor dem Aufruf:
      `grep -c "_t_api_start = time.monotonic()" services/claude_service.py` == 7
    - `python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ('services/claude_service.py','services/qa_pipeline.py')]; print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
Alle acht produktiven Live-LLM-Aufrufe tragen ein Zeitlimit: die sechs blockierenden
`LIVE_LLM_TIMEOUT_S`, die zwei Stream-Aufrufe `LIVE_LLM_STREAM_TIMEOUT_S` — beide als
`httpx.Timeout(X, connect=config.LLM_CONNECT_TIMEOUT_S)`. Der tote Pfad, der Mess-Anker, die `try`-Position um die
`with`-Bloecke und `log_api_cost` sind unangetastet. Keine Uhr im Token-Loop.
  </done>
</task>

<task type="auto">
  <name>Task 3: Die ZWEI Stale-Tests umschreiben — nicht loeschen, nicht falsch-gruen lassen</name>

  <read_first>
    - `tests/test_stabil1_http_llm_timeout.py` VOLLSTAENDIG — besonders
      `test_daemon_pfad_ohne_zeitlimit` (die Behauptung, die diese Phase **absichtlich** falsch
      macht) und `test_modul_client_bleibt_unveraendert` (bleibt gueltig, nur der Docstring
      wurde in Task 1 nachgezogen)
    - `tests/test_medium_lane_intent_event_live.py` — der `_FakeMessages`-Block mit der strikten
      Signatur `def create(self, *, model, max_tokens, system, messages):`. Per
      `grep -n "def create(self" tests/test_medium_lane_intent_event_live.py` lokalisieren
    - `tests/test_qa_pipeline_rueckfrage.py` und `tests/test_anon_live_vs_stored.py` — **nur
      pruefen**, ob ihre Fakes das zusaetzliche `timeout=` vertragen (RESEARCH §5.2/§5.3 haelt
      sie fuer unberuehrt, aber „wahrscheinlich" ist kein Beleg)
    - CLAUDE.md → „Acht weitere Vault-Regeln", Punkt 1 (Regressions-Test bei jedem Fix, erst rot)
      und die Test-Qualitaets-Regel (Integration-Assertion statt Source-Presence)
    - RESEARCH §5.1 und §5.2 (die Belege mit `Datei:Zeile`)
  </read_first>

  <files>tests/test_stabil1_http_llm_timeout.py, tests/test_medium_lane_intent_event_live.py</files>

  <action>
**(a0) ZUERST: der Modul-Docstring derselben Datei — Zeile 6.**
Such-Muster: `grep -n "bekommt KEIN Zeitlimit" tests/test_stabil1_http_llm_timeout.py`

Dort steht in der Aufzaehlung, was die Datei prueft:

```
- Daemon-Pfad (analysiere_mit_claude) bekommt KEIN Zeitlimit
```

**ersetzen** durch:

```
- Daemon-Pfad (analysiere_mit_claude) setzt sein Zeitlimit AM AUFRUF
  (config.LIVE_LLM_TIMEOUT_S, Phase 08.23.2.SOFORT-2 D-03 — bis dahin bewusst OHNE Limit)
- Der Modul-Client wird mit max_retries=0 erzeugt (SOFORT-2 D-04)
```

⚠ **Warum das kein Beifang ist:** die Zeile liegt in **derselben Datei**, die dieser Task
ohnehin umschreibt, und sie behauptet nach dem Umbau **woertlich das Gegenteil des
Ist-Zustands**. Sie bricht dabei **keinen Test** — genau deshalb faellt sie sonst nie auf. Eine
stehengelassene Falschaussage im Modul-Docstring ist dieselbe Fehlerklasse, gegen die diese
Phase antritt (der naechste Leser glaubt der Angabe statt dem Code, D-01).

**(a) `test_daemon_pfad_ohne_zeitlimit` UMSCHREIBEN.**

⚠ **Der Grund, warum Loeschen und Stehenlassen beide falsch waeren:** Der Test verankert
woertlich „Daemon-Pfad darf `with_options()` (Zeitlimit) NICHT aufrufen". Nach dieser Phase ist
diese Aussage **falsch** — der Daemon-Pfad hat ein Zeitlimit. Er wuerde aber **zufaellig gruen
bleiben**, weil wir tatsaechlich kein `with_options` rufen. Ein gruener Test, der das Gegenteil
des Ist-Zustands behauptet, ist schlimmer als kein Test.

Die Funktion **ersetzen** durch:

```python
def test_daemon_pfad_mit_zeitlimit():
    """analysiere_mit_claude (Daemon-Pfad analyse_loop) setzt sein Zeitlimit AM AUFRUF.

    Phase 08.23.2.SOFORT-2 (D-03) hat die STABIL-1-Aussage umgedreht: der Daemon-Pfad bekam
    frueher bewusst KEIN Zeitlimit (die Vorgaengerfassung dieses Tests nagelte genau das fest).
    Ohne Limit greifen die SDK-Vorgaben (read=600 s, max_retries=2) — und weil analyse_loop
    sequentiell ueber alle SIDs iteriert, stehen in dieser Zeit ALLE Gespraeche still.

    Der Weg ist ein anderer als bei den HTTP-Pfaden: kein with_options()-Client, sondern das
    timeout-Schluesselwort am messages.create-Aufruf selbst (RESEARCH §3.7). Dieser Test ist
    die RUNTIME-Ergaenzung zum statischen tests/test_live_timeout_coverage.py — der sieht ein
    Schluesselwort im Syntaxbaum, dieser hier sieht den WERT, der wirklich ankommt.
    """
    fake_msg = _fake_response('{}')
    aufgefangen = {}

    class _FakeMessages:
        def create(self, **kwargs):
            aufgefangen.update(kwargs)
            return fake_msg

    class _FakeClient:
        messages = _FakeMessages()

    with patch.object(claude_service_module, 'claude_client', _FakeClient()):
        analysiere_mit_claude(neuer_text="Testsatz", kontext="", sid=None)

    assert 'timeout' in aufgefangen, \
        "Daemon-Pfad ruft messages.create OHNE timeout — ohne Limit greift read=600 s"
    _t = aufgefangen['timeout']
    # httpx.Timeout traegt die Lese-Grenze in .read und den Verbindungsaufbau in .connect
    assert _t.read == config.LIVE_LLM_TIMEOUT_S, \
        f"Daemon-Pfad benutzt {_t.read}s statt config.LIVE_LLM_TIMEOUT_S ({config.LIVE_LLM_TIMEOUT_S}s)"
    assert _t.connect == 5.0, "connect soll der scharfe SDK-Wert 5.0 bleiben"


def test_modul_client_hat_keinen_retry():
    """Der Modul-Client wird mit max_retries=0 erzeugt — der Worst Case ist 1 x Limit.

    Ohne diese Zeile gilt das Limit PRO VERSUCH: mit der SDK-Vorgabe 2 haelt ein 12-s-Limit
    im Worst Case 3 x 12 s + Backoff. Der statische Waechter prueft das ebenfalls; hier steht
    die Runtime-Gegenprobe am wirklich erzeugten Objekt.
    """
    assert claude_service_module.claude_client.max_retries == 0
```

⚠ `config` muss in der Testdatei importiert sein — per `grep -n "^import config" tests/test_stabil1_http_llm_timeout.py`
pruefen (die vorhandenen Tests benutzen bereits `config.HTTP_LLM_TIMEOUT_LONG_S`, also ist er da).

⚠ **`test_modul_client_bleibt_unveraendert` bleibt bestehen** — seine Aussage („`with_options`
liefert eine Kopie, der Modul-Client wird nicht mutiert") ist unveraendert richtig und
schuetzt weiterhin die HTTP-Pfade.

**(b) `tests/test_medium_lane_intent_event_live.py` — die strikte Fake-Signatur.**
Such-Muster: `grep -n "def create(self, \*, model, max_tokens, system, messages):" tests/test_medium_lane_intent_event_live.py`

**ersetzen** durch:

```python
        def create(self, *, model, max_tokens, system, messages, **kwargs):
            # SOFORT-2: **kwargs, weil die Live-Aufrufe seit D-03 zusaetzlich ein
            # timeout=httpx.Timeout(...) uebergeben. Ohne diese Ergaenzung stirbt der Fake an
            # TypeError: create() got an unexpected keyword argument 'timeout'.
            captured['system'] = system
            return _FakeMsg()
```

**(c) Die drei „wahrscheinlich unberuehrten" Dateien nachpruefen statt annehmen.**
`tests/test_qa_pipeline_rueckfrage.py`, `tests/test_anon_live_vs_stored.py` und alle weiteren
Treffer aus `grep -rln "claude_client" tests/*.py` daraufhin ansehen, ob ihre Fakes ein
zusaetzliches Schluesselwort vertragen:

```bash
python - <<'PY'
import ast, pathlib
for p in sorted(pathlib.Path('tests').glob('test_*.py')):
    t = ast.parse(p.read_text(encoding='utf-8'))
    for fn in [n for n in ast.walk(t) if isinstance(n, ast.FunctionDef) and n.name in ('create', 'stream')]:
        if fn.args.kwarg is None:
            print(f'STRIKT: {p}:{fn.lineno} def {fn.name}(...) ohne **kwargs')
PY
```
Jede Fundstelle, die einen der **acht produktiven** Live-Pfade faket, bekommt `**kwargs`.
Fundstellen, die etwas anderes faken, bleiben unangetastet (Reparatur-Modus).
**Das Ergebnis dieser Suche — inklusive „keine weiteren Treffer" — gehoert in die SUMMARY.**

⛔ **Kein Test wird geloescht.** Ein geloeschter Test ist eine geloeschte Sperre; das Test-Netz
ist eine Ratsche (CLAUDE.md).
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ('tests/test_stabil1_http_llm_timeout.py','tests/test_medium_lane_intent_event_live.py')]; print('SYNTAX OK')"</automated>
    <!-- Der GRUEN-Lauf der ganzen Suite laeuft serverseitig im Deploy-Tor: Plan 08. -->
  </verify>

  <acceptance_criteria>
    - Der Stale-Test ist **umgeschrieben**, nicht geloescht — beide Richtungen belegt:
      `grep -cF "def test_daemon_pfad_mit_zeitlimit" tests/test_stabil1_http_llm_timeout.py` == 1
      **und** `grep -c "def test_daemon_pfad_ohne_zeitlimit" tests/test_stabil1_http_llm_timeout.py` == 0
    - Er prueft den **Wert**, nicht nur die Anwesenheit:
      `grep -cF "_t.read == config.LIVE_LLM_TIMEOUT_S" tests/test_stabil1_http_llm_timeout.py` == 1
    - ⛔ **Der Modul-Docstring behauptet nicht mehr das Gegenteil** (W2) —
      Abwesenheits-Pruefung MIT Existenz-Anker:
      `grep -c "bekommt KEIN Zeitlimit" tests/test_stabil1_http_llm_timeout.py` == 0
      **und** `grep -c "setzt sein Zeitlimit AM AUFRUF" tests/test_stabil1_http_llm_timeout.py` == 1
      (der zweite Wert beweist, dass die Datei ueberhaupt gelesen wurde — eine `0` allein waere
      nicht von „falscher Pfad" unterscheidbar)
    - Die Runtime-Gegenprobe fuer `max_retries` existiert:
      `grep -cF "claude_service_module.claude_client.max_retries == 0" tests/test_stabil1_http_llm_timeout.py` == 1
    - Die Datei hat einen Test **mehr** als vorher (umgeschrieben + ergaenzt, nichts verloren):
      `grep -c "def test_" tests/test_stabil1_http_llm_timeout.py` >= 4
      **und** `grep -cF "def test_modul_client_bleibt_unveraendert" tests/test_stabil1_http_llm_timeout.py` == 1
    - Der strikte Fake vertraegt jetzt `timeout`:
      `grep -cF "def create(self, *, model, max_tokens, system, messages, **kwargs):" tests/test_medium_lane_intent_event_live.py` == 1
      **und** `grep -c "def create(self, \*, model, max_tokens, system, messages):" tests/test_medium_lane_intent_event_live.py` == 0
    - Die Nachpruefung aus (c) ist gelaufen und ihr Ergebnis steht in der SUMMARY:
      `grep -cE "STRIKT:|keine weiteren strikten Fakes" .planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-06-SUMMARY.md` >= 1
    - ⛔ Kein Test geloescht — Abwesenheits-Pruefung MIT Existenz-Anker.
      ⚠ **Die Zaehlung geht ausdruecklich NUR ueber die zwei Dateien dieses Tasks**, nicht ueber
      `tests/` als Ganzes: Plan 05 legt `tests/test_live_timeout_coverage.py` mit fuenf
      `def test_` an; zaehlte man `tests/` pauschal, kaeme 7 statt 2 heraus und der Task fiele
      falsch-rot aus. (Plan 05 Schritt 6 committet die Datei zwar vorher — aber das Kriterium
      soll auch dann halten, wenn jemand die Reihenfolge aendert.)
      `git diff -U0 -- tests/test_stabil1_http_llm_timeout.py tests/test_medium_lane_intent_event_live.py | grep -cE "^-\s*def test_"` == 1
      (**genau eine**: `test_daemon_pfad_ohne_zeitlimit`, ersetzt durch zwei neue)
      **und** `git diff -U0 -- tests/test_stabil1_http_llm_timeout.py tests/test_medium_lane_intent_event_live.py | grep -cE "^\+\s*def test_"` == 2
    - `python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ('tests/test_stabil1_http_llm_timeout.py','tests/test_medium_lane_intent_event_live.py')]; print('OK')"` gibt `OK`
  </acceptance_criteria>

  <done>
Die zwei Bestandstests, die diese Vertragsaenderung bricht, sind umgeschrieben statt geloescht
oder falsch-gruen stehengelassen. `test_daemon_pfad_mit_zeitlimit` prueft jetzt den wirklich
uebergebenen **Wert** (die Runtime-Ergaenzung zum statischen Waechter), und
`test_modul_client_hat_keinen_retry` nagelt `max_retries=0` am erzeugten Objekt fest. Die Suche
nach weiteren strikten Fakes ist gelaufen und belegt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Anthropic-API | Die Grenze, an der ein Haenger entsteht. Ab hier ist die Antwortzeit fremdbestimmt |
| `config.py` (ENV) → Live-Pfad | Die zwei Zahlen kommen aus der Umgebung des Prod-Servers |
| Modul-Client → alle Verbraucher | `max_retries=0` wirkt auf **jeden** Nutzer von `claude_client`, auch ausserhalb des Live-Pfads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SOF2-50 | Denial of Service | Ein haengender `messages.create` blockiert `analyse_loop` und damit **alle** gleichzeitigen Gespraeche (~30 min Worst Case) | mitigate | `timeout=httpx.Timeout(LIVE_LLM_TIMEOUT_S, connect=config.LLM_CONNECT_TIMEOUT_S)` an allen sechs blockierenden Aufrufen; per **AST**-Acceptance `OHNE_TIMEOUT []` bei `AUFRUFE 8` belegt |
| T-SOF2-51 | Denial of Service | `max_retries=2` verdreifacht den Worst Case; ein reiner `timeout=`-Waechter bleibt dabei gruen | mitigate | `max_retries=0` am Modul-Client + `test_modul_client_hat_keinen_retry` (Runtime) + `test_modul_client_hat_max_retries_null` (statisch) |
| T-SOF2-52 | Denial of Service | Ein haengender **Stream** blockiert den PiP des Beraters | mitigate | `LIVE_LLM_STREAM_TIMEOUT_S` als TTFT- plus Stillstands-Grenze. **Die Gesamtdauer bleibt frei** (D-03) |
| T-SOF2-53 | Denial of Service | Das Stream-Limit kappt eine legitime lange Antwort (P-10) | accept | Faktor 7,7× ueber dem gemessenen TTFT-Ø; Rest-Kante im Code-Kommentar benannt; die Messung dazu ist D-06 (Plan 08). **Es gibt keine Messung der Token-Abstaende** — das ist als A3 offengelegt, nicht wegdefiniert |
| T-SOF2-54 | Repudiation | Die MESSGERAETE-1-Statistik wird durch Timeout-Buchungen verfaelscht | mitigate | F-5a: `log_api_cost` unangetastet — als Diff-Acceptance greppbar (`^[+-].*log_api_cost` == 0) **mit** Existenz-Anker daneben |
| T-SOF2-55 | Repudiation | Ein Stale-Test behauptet weiter das Gegenteil und bleibt dabei gruen | mitigate | `test_daemon_pfad_ohne_zeitlimit` **umgeschrieben** statt geloescht; die Acceptance belegt beide Richtungen (alter Name 0, neuer Name 1) |
| T-SOF2-56 | Tampering | Der Mess-Anker `_t_api_start` verrutscht und die MESSGERAETE-1-Zahlen luegen | mitigate | Ausdrueckliches Verbot in der Action + Acceptance `_t_api_start = time.monotonic()` == 7. Das `timeout`-Argument ist ein Konstanten-Zugriff plus Datenklassen-Konstruktor, **kein** Funktionsaufruf mit Prompt-Bau (Punkt 14) |
| T-SOF2-57 | Elevation of Privilege | Ein `try` **im** `with` faengt den TTFT-Timeout nicht (die Anfrage laeuft in `__enter__`) | mitigate | Ausdrueckliches Verbot, die `try`-Position umzubauen; beide Call-Sites haben es bereits aussen (RESEARCH §3.6) |
| T-SOF2-58 | Denial of Service | Der tote `generate_qa_response` bekommt ein Limit und der Allowlist-Eintrag wird fragwuerdig | mitigate | Ausdruecklich ausgenommen; AST-Acceptance `GEFUNDEN 1 MIT_TIMEOUT []` |
| T-SOF2-59 | Denial of Service | `judge_runner` / `adoption_runner` verlieren ihre Retries (Nebenwirkung von `max_retries=0`) | accept | Post-Call-Batch hinter `slow_lane` mit eigener Fehlerbehandlung; ein verlorener Retry bedeutet eine fehlende Bewertungszeile, keinen gebrochenen Anruf. **Gemeldet als R-6** — im Code-Kommentar **und** in `08.23.2.SOFORT-2-FUNDE.md` |
</threat_model>

<verification>
1. AST-Pruefung: `AUFRUFE 8`, `OHNE_TIMEOUT []` — kein produktiver Live-Aufruf ohne Zeitlimit
2. Fuenf + eins blockierende Aufrufe tragen `LIVE_LLM_TIMEOUT_S`, zwei Stream-Aufrufe
   `LIVE_LLM_STREAM_TIMEOUT_S`
3. `claude_client` wird mit `max_retries=0` erzeugt; genau **ein** Konstruktor in der Datei
4. **Kein Selbstverweis im Docstring haengt mehr an einer Zeilennummer** — `(:27)` ist fort
   (`grep -c "(:27)"` == 0), der veraltete `:812/:980` ist an beiden Stellen fort, beide sind
   durch Namen ersetzt
5. Der tote Pfad, der Mess-Anker, die `try`-Position und `log_api_cost` sind unangetastet
6. Zwei Stale-Tests umgeschrieben, keiner geloescht (Diff: 1 entfernte, 2 neue `def test_`)
7. **KEIN Deploy in diesem Plan** — Stufe 1/2 aus D-04 fehlt noch (Plan 07); ein Deploy hier
   wuerde das Zeitlimit ohne die Sichtbarkeit ausrollen, also genau den stillen Ausfall
   herstellen, den D-04 verhindern will
</verification>

<success_criteria>
- Kein Live-LLM-Aufruf laeuft mehr ohne Zeitlimit — per AST belegt, nicht per grep
- Zwei Zahlen, ein Mechanismus, keine Uhr im Token-Loop (Punkt 27)
- Der Worst Case ist 1 × Limit, nicht 3 × (`max_retries=0`)
- Die Vertragsaenderung hinterlaesst keinen falsch-gruenen Test
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-06-SUMMARY.md`
— inklusive der AST-Ausgabe (`AUFRUFE 8 / OHNE_TIMEOUT []`) und des Ergebnisses der Suche nach
weiteren strikten Fakes.
</output>
