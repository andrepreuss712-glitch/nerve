---
phase: 08.23.2.SOFORT-2
plan: 02
type: execute
wave: 2
depends_on: ["08.23.2.SOFORT-2-01"]
files_modified:
  - services/live_session.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Der ROT-Lauf beider Welle-1-Waechter gegen den heute ausgerollten Prod-Code liegt VERBATIM vor — vor jeder Zeile Fix"
    - "Es gibt GENAU EINEN Ort, an dem 'gehoert diese Kennung mir?' beantwortet wird: services/live_session.py"
    - "Der Helfer vergleicht user_id, nicht Lebendigkeit — und gibt bei Fremdbesitz dasselbe zurueck wie bei unbekannter Kennung"
    - "Der Helfer nimmt den _session_state_lock genau einmal und ruft in seinem Rumpf keine Funktion, die ihn erneut nimmt"
  artifacts:
    - path: "services/live_session.py"
      provides: "sid_belongs_to + resolve_own_sid_by_call_id als EINZIGER Besitz-Pruefpunkt"
      contains: "def sid_belongs_to"
  key_links:
    - from: "services/live_session.py"
      to: "_session_state"
      via: "with _session_state_lock, Vergleich auf user_id"
      pattern: "user_id"
---

**Komplexitaet: 🔴** — Cross-AI-Review ist Pflicht VOR Execute.

<objective>
Zwei Dinge, die zusammengehoeren: der **ROT-Beleg** (das Acceptance-Artefakt, ohne das die
Waechter nichts beweisen) und der **eine Helfer**, gegen den beide Waechter pruefen.

Purpose: D-05 verlangt „ERST ROT". Ein Waechter, der nie rot war, beweist nichts (Punkt 31).
Und RESEARCH §1.6: fuenf der sechs Eingaenge fragen inhaltlich dasselbe — sie bekommen **eine**
Antwort an **einer** Stelle, sonst hat der AST-Waechter keinen ableitbaren Pruefpunkt und faellt
auf die handgepflegte Liste zurueck, die D-05 ausdruecklich verbietet.

Output: verbatim gesicherter ROT-Lauf, zwei neue Funktionen in `services/live_session.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-CONTEXT.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-RESEARCH.md
@.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-01-SUMMARY.md
@CLAUDE.md
@services/live_session.py
</context>

<interfaces>
<!-- Aus dem Code extrahiert (2026-08-04, per grep belegt) — KEIN Erkunden noetig. -->

**Der Zustand, gegen den geprueft wird** (`services/live_session.py:373-374`):
```python
_session_state: dict = {}       # {sid: {key: value, ...}}
_session_state_lock = _TracedLock('_session_state_lock')
```

**Das korrekte Vorbild, direkt im Repo** (`routes/app_routes.py:198-201`) — der Vergleich, den
der Helfer kapselt:
```python
with ls._session_state_lock:
    _cands = [(s, (st.get('session_start_time') or 0.0))
              for s, st in ls._session_state.items()
              if st.get('user_id') == _my_uid]
```

**Das FALSCHE Muster, das der Helfer abloest** (`services/live_session.py:418-425`,
`set_briefing_for_sid`) — der Ghost-SID-Guard prueft **Lebendigkeit**:
```python
with _session_state_lock:
    if sid not in _session_state:      # prueft LEBENDIGKEIT
        _logger.debug(...)
        return
    _session_state[sid]['_briefing'] = briefing_text or ''
```

**Die Nachbarn, neben denen die neuen Funktionen stehen** (Ist-Zeilen, per `grep -n "^def "
services/live_session.py` am 2026-08-04):
`set_profile_for_sid:389` · `get_profile_for_sid:402` · `set_briefing_for_sid:418` ·
`get_briefing_for_sid:428` · `init_session_state:503` · `pop_session_state:655` ·
`resolve_call_id_for_sid:886`

⚠ **Riegel-Falle (CLAUDE.md Punkt 31, LOCK-2):** `_session_state_lock` ist ein `_TracedLock`,
**kein** `RLock`. Wer ihn haelt und darin eine Funktion ruft, die ihn erneut nimmt, verklemmt
sich selbst. `get_briefing_for_sid`, `set_briefing_for_sid`, `get_or_open_moment` und
`record_suggestion_offer` nehmen ihn alle. **Im Rumpf der neuen Helfer wird KEINE davon
gerufen** — nur direkter Dict-Zugriff.
</interfaces>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 + Punkt 26)

### Angefasste Tabellen
**Keine.** Dieser Plan aendert ausschliesslich RAM-Zustands-Zugriff in `services/live_session.py`.
Kein `SELECT`, kein `INSERT`, kein `UPDATE`, keine Migration.

### Cross-Layer-Konsistenz-Tabelle

| Datum | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann der Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|---|
| `_session_state[sid]['user_id']` | gelesen vom neuen Helfer | **RAM**, keine DB-Spalte | **synchron** in `init_session_state(sid, user_id, org_id, …)` beim Verbindungsaufbau | **Nein** — die `sid` existiert fuer einen Endpunkt erst, nachdem `init_session_state` sie angelegt hat | ✓ `grep -n "def init_session_state" services/live_session.py` → `:503`, Signatur traegt `user_id` als **Pflicht**-Argument |
| `_session_state[sid]['state']['call_id']` | gelesen vom neuen Helfer | **RAM** | `create_call_for_sid` bzw. `get_or_open_moment` waehrend des Anrufs | **Ja, theoretisch** — bevor der Call angelegt ist, gibt es keine `call_id`. Folge: der Helfer findet nichts und gibt `None` → der Aufrufer faellt auf seinen bestehenden Leerpfad zurueck. **Das ist der gewuenschte fail-closed-Ausgang**, kein Defekt | ✓ `grep -n "def create_call_for_sid" services/live_session.py` → `:899` |

### Punkt 23 (Schild)
**Nicht faellig.** Keine Tabelle, keine Spalte, kein geaenderter Lese-/Schreib-Pfad einer Tabelle.

## Vier Saeulen (Bau-Regel D)
- **Automatisieren:** Der Besitz-Vergleich passiert an einer Stelle, nicht fuenfmal von Hand.
- **Nachvollziehen:** Der Helfer protokolliert einen **abgelehnten** Fremdzugriff (nicht jeden
  Aufruf — sonst ist das Log im Live-Betrieb unbrauchbar).
- **Eingreifen:** Ein Umbau der Regel ist eine Funktion, nicht sechs Stellen.
- **Marge:** Erspart bei jedem neuen Endpunkt die Frage „wie war das nochmal".

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

<tasks>

<task type="auto">
  <name>Task 1: ROT-LAUF — beide Waechter gegen den ausgerollten Prod-Code, Ausgabe verbatim sichern</name>

  <read_first>
    - `.planning/phases/08.23.2.SOFORT-2-.../08.23.2.SOFORT-2-01-SUMMARY.md` (was Plan 01 gebaut hat)
    - `tests/test_ownership_state_guard.py` und `tests/test_ownership_endpoints.py` VOLLSTAENDIG
      (die zwei Dateien, die jetzt rot fahren muessen — inkl. der zwei Tests, die dabei GRUEN
      sein muessen)
    - CLAUDE.md → „HART: Kein Local-Dev" (der ROT-Lauf laeuft auf dem Server, nicht lokal) und
      Punkt 31 („ROT-Beleg vor GRUEN", Acceptance-Artefakt)
    - `.planning/phases/08.23.2.MESSGERAETE-1-.../08.23.2.MESSGERAETE-1-01-PLAN.md` Task 2,
      Abschnitt „Danach: ROT-Beleg ziehen" — die exakte scp/pytest/rm-Mechanik dieses Projekts
  </read_first>

  <files>(keine Repo-Datei — dieser Task erzeugt ein Acceptance-Artefakt fuer Commit-Body und SUMMARY)</files>

  <action>
⛔ **Dieser Schritt laeuft VOR jeder Fix-Zeile.** Wer erst fixt und dann rot fahren will, hat den
Beleg fuer immer verloren.

**Kein `deploy.sh`.** Die Waechter sind absichtlich rot — ein Deploy waere hier ein rotes
Test-Tor und wuerde den Neustart verweigern (was richtig ist, aber nicht der Zweck). Stattdessen
werden **nur die zwei Test-Dateien** einzeln hochgelegt, gefahren und wieder abgeraeumt.

```bash
# 1) Prod-Stand bestaetigen — der Waechter prueft GENAU diesen Code
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && bash scripts/inspect.sh git-stand'

# 2) Gegenprobe, dass der Fix dort WIRKLICH noch fehlt (sonst waere ein gruener Lauf
#    kein Beweis, sondern ein Hinweis auf einen bereits vorhandenen Fix):
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  "cd /opt/nerve/app && grep -c 'sid_belongs_to' services/live_session.py"   # erwartet: 0
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  "cd /opt/nerve/app && grep -c 'ls._session_state.items()' routes/learning.py"  # erwartet: 2

# 3) NUR die zwei Waechter-Dateien hochlegen (KEIN Deploy)
scp -i ~/.ssh/nerve_vps tests/test_ownership_state_guard.py \
    root@178.104.82.166:/opt/nerve/app/tests/test_ownership_state_guard.py
scp -i ~/.ssh/nerve_vps tests/test_ownership_endpoints.py \
    root@178.104.82.166:/opt/nerve/app/tests/test_ownership_endpoints.py

# 4) ROT-LAUF — Ausgabe VERBATIM sichern (Commit-Body + SUMMARY)
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_ownership_state_guard.py \
     tests/test_ownership_endpoints.py -q --tb=short'

# 5) Beide Dateien wieder abraeumen (sonst faellt der Waisen-Prune von deploy.sh darueber)
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
  'rm -f /opt/nerve/app/tests/test_ownership_state_guard.py \
         /opt/nerve/app/tests/test_ownership_endpoints.py'
```

**Erwartung in Schritt 4 — genau lesen, nicht nur „ist rot" abhaken:**

| Test | erwartet | Bedeutung, wenn es ANDERS ist |
|---|---|---|
| `test_zustands_aufloesungen_gehen_durch_den_besitz_helfer` | **FAILED**, listet **VIER**: `api_beenden`, `api_gatekeeper_phrases`, `api_postcall_analysis`, `api_postcall_outcome` | gruen → der Sweep findet die Funktionen nicht (Ableitung kaputt) |
| `test_sid_aus_der_anfrage_wird_besitzgeprueft` | **FAILED**, listet **ZWEI**: `api_gatekeeper_phrases`, `api_precall_research` | gruen → N-01 ist fuer den Waechter unsichtbar, Menge 2 greift nicht |
| `test_call_id_aus_der_anfrage_wird_besitzgeprueft` | **FAILED**, listet **DREI**: `api_beenden` + die zwei Handler aus `routes/learning.py` (`api_postcall_analysis`, `api_postcall_outcome`) | gruen → Menge 3 greift nicht |
| `test_helfer_existieren_und_pruefen_user_id` | **FAILED** (die Helfer gibt es noch nicht) | gruen → jemand hat schon gefixt, der ROT-Beleg ist wertlos |
| `test_b01` … `test_n03` (sechs Faelle) | **FAILED** | gruen → die Fixture baut die Fremd-Sitzung nicht |
| `test_mindestens_erwartete_pruefpunkte_gefunden` | **PASSED** | rot → der Sweep ist kaputt, **nicht** der Code → **ABBRUCH**, nicht weiterbauen |
| `test_allowlist_ist_begruendet_und_lebt` | **PASSED** | rot → der Allowlist-Eintrag zeigt ins Leere → **ABBRUCH** |
| `test_fixture_baut_wirklich_eine_fremde_sitzung` | **PASSED** | rot → die Fixture ist kaputt, alle sechs Faelle waeren wertlos → **ABBRUCH** |
| `test_eigener_zugriff_funktioniert_weiter` | **PASSED** | rot → die Fixture oder der Login-Weg stimmt nicht → **ABBRUCH** |

Also: **10 failed, 4 passed**. Weicht die Verteilung ab, wird **nicht weitergebaut** — erst die
Ursache klaeren.

**Woher die Zahlen 4 / 2 / 3 kommen — am Code nachgerechnet, nicht geschaetzt** (2026-08-04,
`ast.parse` ueber `routes/*.py` gegen den ungefixten Stand):

| Menge | roh | minus Allowlist | bleibt im ROT-Lauf |
|---|---|---|---|
| `ZUSTANDS_FUNKTIONEN` | **5** (`api_beenden`, `_load_beenden_state`, `api_gatekeeper_phrases`, `api_postcall_analysis`, `api_postcall_outcome`) | −1 (`_load_beenden_state`) | **4** |
| `SID_AUS_ANFRAGE` | **2** | −0 | **2** |
| `CALLID_AUS_ANFRAGE` | **5** (`api_beenden`, `api_calls_latest_outcome`, `save_meeting`, `api_postcall_analysis`, `api_postcall_outcome`) | −2 (`api_calls_latest_outcome`, `save_meeting`) | **3** |

⚠ **`test_mindestens_erwartete_pruefpunkte_gefunden` prueft die ROHEN Zahlen** (vor
Allowlist-Abzug) gegen das Mindest-Soll **3 / 2 / 5** aus Plan 01. Im ROT-Lauf meldet er
**5 / 2 / 5** — also **PASSED**, weil `>=` geprueft wird und die erste Menge hier noch groesser
ist als das Soll. **Das ist Absicht:** das Soll steht auf dem Wert NACH dem Fix (Plan 03 Task 2
nimmt `api_postcall_analysis`/`api_postcall_outcome` aus `ZUSTANDS_FUNKTIONEN` heraus), damit
derselbe Waechter beide Laeufe haelt und sich nicht durch den Erfolg seiner eigenen Reparatur
dauerhaft rot macht.

⚠ **Was ein ABBRUCH ist und was nicht:** meldet er im ROT-Lauf z.B. `ZUSTANDS_FUNKTIONEN=4`,
dann liest der Sweep die verschachtelte Funktion `_load_beenden_state` nicht mit (`ast.walk`
durch `tree.body` ersetzt?) — **ABBRUCH**, den Sweep reparieren. Dasselbe bei jeder anderen
Abweichung von **5 / 2 / 5** in diesem Lauf. Die `3` im Mindest-Soll ist dagegen **kein**
Abbruch-Signal, sondern der erwartete Wert nach Welle 1.

⛔ **Wenn eine der drei Fehlermeldungen mehr Funktionen listet als hier steht, wird die Allowlist
NICHT erweitert.** Ein zusaetzlicher Treffer heisst: der Sweep hat einen Eingang gefunden, den
Plan 03 nicht fixt. Dann gilt D-02 — melden (neue R-Nummer in `08.23.2.SOFORT-2-FUNDE.md`) und
erst danach entscheiden, ob `FALSCH-TREFFER:` oder `GEMELDET-NICHT-GEFIXT:` zutrifft. Die
Allowlist stillschweigend zu fuellen ist der Punkt-31-Verstoss, gegen den diese Phase antritt.

Die Ausgabe aus Schritt 4 wandert **verbatim** (nicht zusammengefasst, nicht gekuerzt) in den
Commit-Body **und** in `08.23.2.SOFORT-2-02-SUMMARY.md`. Punkt 31: der ROT-Lauf ist ein
Acceptance-Artefakt, keine Nebenbemerkung.
  </action>

  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && grep -c "sid_belongs_to" services/live_session.py'</automated>
    <!-- MUSS 0 sein: beweist, dass der ROT-Lauf gegen den ungefixten Stand faehrt. -->
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/pytest tests/test_ownership_state_guard.py tests/test_ownership_endpoints.py -q --tb=short'</automated>
    <!-- MUSS ROT sein (10 failed / 4 passed). Ein gruener Lauf an dieser Stelle bedeutet:
         die Waechter pruefen nichts. -->
  </verify>

  <acceptance_criteria>
    - Schritt 2 liefert `0` fuer `sid_belongs_to` in `services/live_session.py` auf Prod
      **und** `2` fuer `ls._session_state.items()` in `routes/learning.py` auf Prod
      (Existenz-Anker neben der Abwesenheits-Pruefung: die `2` beweist, dass der `grep` auf dem
      Server ueberhaupt Treffer liefern kann — ohne sie waere die `0` nicht von
      „falscher Pfad / Datei fehlt" unterscheidbar)
    - Der ROT-Lauf endet mit **`10 failed`** und **`4 passed`**
    - Diese vier sind im ROT-Lauf **PASSED**: `test_mindestens_erwartete_pruefpunkte_gefunden`,
      `test_allowlist_ist_begruendet_und_lebt`, `test_fixture_baut_wirklich_eine_fremde_sitzung`,
      `test_eigener_zugriff_funktioniert_weiter`
    - Die Fehlermeldung von `test_call_id_aus_der_anfrage_wird_besitzgeprueft` nennt
      **drei** Funktionen (`api_beenden` + die zwei Handler aus `routes/learning.py`)
    - Die Fehlermeldung von `test_zustands_aufloesungen_gehen_durch_den_besitz_helfer` nennt
      **vier** Funktionen — und **nicht** `_load_beenden_state` (der steht als
      `FALSCH-TREFFER` in der Allowlist; taucht er trotzdem auf, greift der Allowlist-Abzug
      nicht und der spaetere GRUEN-Lauf in Plan 04 kaeme nie zustande)
    - `test_mindestens_erwartete_pruefpunkte_gefunden` ist **PASSED** und seine Zahlen sind
      **5 / 2 / 5** (roh, vor Allowlist-Abzug) — abweichende Rohzahlen sind ein **ABBRUCH**,
      nicht ein Anlass, das Mindest-Soll zu senken
    - Die Fehlermeldung von `test_sid_aus_der_anfrage_wird_besitzgeprueft` nennt
      **`api_precall_research`** — das ist der Beleg, dass N-01 vom Waechter erfasst wird
    - Schritt 5 raeumt ab:
      `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'ls /opt/nerve/app/tests/test_ownership_state_guard.py'`
      meldet „No such file"
    - Die verbatim-Ausgabe steht im Commit-Body (`git log -1 --format=%B | grep -c "failed"` >= 1)
  </acceptance_criteria>

  <done>
Beide Welle-1-Waechter sind gegen den heute auf Production ausgerollten Code nachweislich rot
gelaufen — mit der erwarteten Verteilung 10 failed / 4 passed, verbatim gesichert. Die
Test-Dateien sind vom Server wieder abgeraeumt. **Es ist noch keine Zeile gefixt.**
  </done>
</task>

<task type="auto">
  <name>Task 2: Der EINE Besitz-Helfer in services/live_session.py</name>

  <read_first>
    - `services/live_session.py` Z.373-380 (`_session_state` + `_session_state_lock` — der
      Zustand), Z.389-440 (`set_profile_for_sid` bis `get_briefing_for_sid` — der Ghost-SID-Guard
      im Ist-Zustand; **direkt darunter kommen die neuen Funktionen**), Z.503-560
      (`init_session_state` — wo `user_id` gesetzt wird), Z.886-900 (`resolve_call_id_for_sid` —
      das naechstverwandte Vorbild fuer eine Aufloesungs-Funktion)
    - `routes/app_routes.py` Z.196-203 — das **korrekte** Filter-Muster, das hier gekapselt wird
    - CLAUDE.md → Punkt 28 (kein modul-globaler veraenderlicher Zustand fuer pro-Nutzer-Daten),
      Punkt 27 (einfachster tragfaehiger Weg), Punkt 31 (LOCK-2: ein Riegel-Halter darf keine
      Funktion rufen, die denselben Riegel nimmt)
    - RESEARCH §1.6 (Begruendung fuer EINEN Helfer statt fuenf Bedingungen) und §1.1
      (Ausnutzbarkeit — warum 404 statt 403)
  </read_first>

  <files>services/live_session.py</files>

  <action>
**Einfuegeort:** unmittelbar **nach** `get_briefing_for_sid` (Ist: `:428-431`) und **vor** dem
Kommentarblock `# ── Per-SID AnrufAnonymisierer Cache (D-06 Phase 08.23.2.B) ──`.
Per `grep -n "def get_briefing_for_sid" services/live_session.py` gegenpruefen (D-01).

**Der einzufuegende Block — woertlich:**

```python
# ── Phase 08.23.2.SOFORT-2 — Besitzpruefung (D-02: Lebendigkeit ist NICHT Eigentuemerschaft) ──
# Die Ghost-SID-Guards ueber diesem Block (set_profile_for_sid, set_briefing_for_sid) pruefen
# `if sid not in _session_state` — also ob die Sitzung LEBT. Sie beantworten NICHT die Frage
# "gehoert sie dem Anfragenden?". Das ist eine ganze Fehlerklasse, kein Einzelfall: sechs
# Endpunkte in routes/ nahmen eine sid/call_id aus der Anfrage und loesten damit fremden Zustand
# oder fremde DB-Zeilen auf.
#
# Diese zwei Funktionen sind der EINE Ort, an dem die Frage beantwortet wird. Der AST-Waechter
# tests/test_ownership_state_guard.py fordert, dass jede Zustands-Aufloesung in routes/ durch
# eine von ihnen laeuft — deshalb duerfen ihre Namen nicht ohne Nachzug im Waechter wandern.
#
# ⚠ RIEGEL (Punkt 31 / LOCK-2): _session_state_lock ist KEIN RLock. Im Rumpf dieser beiden
# Funktionen wird deshalb KEINE andere live_session-Funktion gerufen, die den Riegel nimmt
# (get_briefing_for_sid, set_briefing_for_sid, get_or_open_moment, record_suggestion_offer
# nehmen ihn alle) — nur direkter Dict-Zugriff. Wer das aendert, verklemmt den Live-Pfad.
#
# ⚠ ANTWORTFORM: beide geben bei FREMDBESITZ exakt dasselbe zurueck wie bei einer UNBEKANNTEN
# Kennung (False bzw. None). Das ist Absicht — eine Unterscheidung waere ein Existenz-Leck
# (ASVS V7). Die Endpunkte leiten daraus 404 ab, nie 403.

def sid_belongs_to(sid, user_id) -> bool:
    """True, wenn die Sitzung `sid` dem Nutzer `user_id` gehoert.

    False bei: unbekannter sid, fehlendem user_id im Zustand, fremdem Besitzer, sid/user_id None.
    Kein Unterschied zwischen "gibt es nicht" und "gehoert dir nicht" (Existenz-Leck).
    """
    if not sid or user_id is None:
        return False
    with _session_state_lock:
        eintrag = _session_state.get(sid)
        besitzer = eintrag.get('user_id') if isinstance(eintrag, dict) else None
    if besitzer is None:
        return False
    if besitzer != user_id:
        # Bewusst NUR der abgelehnte Fall wird protokolliert. Jeden Aufruf zu loggen waere im
        # Live-Betrieb Rauschen; ein abgelehnter Fremdzugriff ist ein Ereignis.
        _logger.warning(
            f"[SOFORT-2] Fremdzugriff abgelehnt: sid={sid!r} gehoert user_id={besitzer}, "
            f"angefragt von user_id={user_id}"
        )
        return False
    return True


def resolve_own_sid_by_call_id(call_id, user_id):
    """Die sid der EIGENEN lebenden Sitzung zu dieser `call_id` — oder None.

    Loest den Muster-Fehler aus B-02/N-02/N-03 an einer Stelle: der Scan ueber _session_state
    vergleicht die call_id UND den Besitzer. Ein Treffer, der jemand anderem gehoert, zaehlt
    wie kein Treffer.
    """
    if call_id is None or user_id is None:
        return None
    gesucht = str(call_id)
    treffer_fremd = False
    with _session_state_lock:
        for _s, _sd in _session_state.items():
            if not isinstance(_sd, dict):
                continue
            _cid = (_sd.get('state') or {}).get('call_id')
            if _cid is None or str(_cid) != gesucht:
                continue
            if _sd.get('user_id') == user_id:
                return _s
            treffer_fremd = True
    if treffer_fremd:
        _logger.warning(
            f"[SOFORT-2] Fremdzugriff abgelehnt: call_id={gesucht} gehoert einer fremden Sitzung, "
            f"angefragt von user_id={user_id}"
        )
    return None
```

**Fuenf Dinge, die dabei bewusst so sind:**

1. **Kein `RLock`, kein zweiter Riegel, kein Cache.** Punkt 27 — der einfachste tragfaehige Weg.
   Beide Funktionen sind ein Dict-Lookup bzw. ein Scan ueber die aktiven Sitzungen (im
   Solo-/EA-Betrieb einstellige Anzahl). **Latenz-Budget (Punkt 25): < 1 ms**, kein Netz, keine
   DB — und keine der beiden liegt im Token-Pfad, sondern in HTTP-Endpunkten.
2. **`resolve_own_sid_by_call_id` gibt beim ersten eigenen Treffer zurueck** und laeuft sonst
   durch. Ein fremder Treffer bricht die Schleife **nicht** ab — sonst wuerde ein fremder Call
   mit derselben `call_id` die eigene Sitzung verdecken.
3. **`str()`-Vergleich**, weil `call_id` mal UUID-Objekt, mal String ist (Vorbild:
   `routes/app_routes.py` verglich bereits `str(...) == str(...)`).
4. **`_logger`** statt `print` — die Datei benutzt bereits `_logger`
   (per `grep -n "_logger" services/live_session.py` bestaetigen; falls nicht vorhanden, das
   dort uebliche `print`-Muster nehmen, **nicht** ein neues Logging einfuehren).
5. **Kein modul-globaler Zustand** (Punkt 28): beide Funktionen sind zustandslos.

**Was dieser Task NICHT tut:** kein Aufrufer wird umgestellt. Die sechs Eingaenge kommen in
Plan 03. Nach diesem Task ist `test_helfer_existieren_und_pruefen_user_id` gruen, die anderen
neun bleiben rot.
  </action>

  <verify>
    <automated>python -c "import ast,pathlib; t=ast.parse(pathlib.Path('services/live_session.py').read_text(encoding='utf-8')); n=[x.name for x in ast.walk(t) if isinstance(x,ast.FunctionDef)]; print('sid_belongs_to' in n, 'resolve_own_sid_by_call_id' in n)"</automated>
    <automated>grep -c "def sid_belongs_to" services/live_session.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "def sid_belongs_to(sid, user_id) -> bool:" services/live_session.py` == 1
    - `grep -cF "def resolve_own_sid_by_call_id(call_id, user_id):" services/live_session.py` == 1
    - Beide vergleichen wirklich `user_id` (nicht nur Existenz) — Code-Anker, nicht Prosa:
      `grep -cF "if besitzer != user_id:" services/live_session.py` == 1
      und `grep -cF "if _sd.get('user_id') == user_id:" services/live_session.py` == 1
    - Der abgelehnte Fremdzugriff wird protokolliert:
      `grep -c "\[SOFORT-2\] Fremdzugriff abgelehnt" services/live_session.py` == 2
    - ⛔ **Riegel-Selbstverklemmung ausgeschlossen** — Abwesenheits-Pruefung MIT Existenz-Anker.
      Im Rumpf der zwei neuen Funktionen darf keine Funktion vorkommen, die
      `_session_state_lock` erneut nimmt:
      ```
      python -c "import ast,pathlib; src=pathlib.Path('services/live_session.py').read_text(encoding='utf-8'); t=ast.parse(src); f=[x for x in ast.walk(t) if isinstance(x,ast.FunctionDef) and x.name in ('sid_belongs_to','resolve_own_sid_by_call_id')]; verboten={'get_briefing_for_sid','set_briefing_for_sid','get_or_open_moment','record_suggestion_offer','get_anonymisierer','get_profile_for_sid','set_profile_for_sid'}; ruft=[c.func.id for x in f for c in ast.walk(x) if isinstance(c,ast.Call) and isinstance(c.func,ast.Name)]; print('FUNKTIONEN', len(f), 'VERBOTENE', sorted(set(ruft)&verboten))"
      ```
      gibt `FUNKTIONEN 2 VERBOTENE []`
      — die `2` ist der Existenz-Anker: eine leere Verbotsliste bei `FUNKTIONEN 0` waere
      bedeutungslos
    - Kein neuer Modul-Global (Punkt 28), Existenz-Anker daneben:
      `git diff -U0 services/live_session.py | grep -cE "^\+[A-Za-z_][A-Za-z0-9_]* *= *(\{\}|\[\])"` == 0
      **und** `git diff --stat services/live_session.py | grep -c "live_session.py"` == 1
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('services/live_session.py').read_text(encoding='utf-8')); print('OK')"` gibt `OK`
    - Nur EINE Datei im Diff (Reparatur-Modus):
      `git status --porcelain services/ routes/ | wc -l` == 1
  </acceptance_criteria>

  <done>
`services/live_session.py` hat `sid_belongs_to` und `resolve_own_sid_by_call_id` direkt unter den
bestehenden per-sid-Zugriffen. Beide vergleichen `user_id`, geben bei Fremdbesitz dasselbe
zurueck wie bei unbekannter Kennung, protokollieren nur den abgelehnten Fall und rufen im Rumpf
keine Funktion, die `_session_state_lock` erneut nimmt. Kein Aufrufer ist umgestellt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flask-Endpunkt → `_session_state` (RAM) | Der neue Helfer ist ab Plan 03 der **einzige** Uebergang. Kein DB-Layer darunter |
| Repo → Prod-Dateisystem | `scp` von zwei Test-Dateien fuer den ROT-Beleg, danach `rm` |
| Live-Thread → `_session_state_lock` | Der Helfer nimmt den Riegel. Ein zweiter Zugriff im selben Halter waere eine Selbstverklemmung (LOCK-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SOF2-11 | Information Disclosure | `sid_belongs_to` / `resolve_own_sid_by_call_id` unterscheiden „gibt es nicht" von „gehoert dir nicht" | mitigate | Beide geben in **beiden** Faellen denselben Wert zurueck (`False`/`None`). Ausdruecklich im Kommentarblock verankert; die Endpunkte leiten daraus 404 ab, nie 403 |
| T-SOF2-12 | Denial of Service | Selbstverklemmung: der Helfer haelt `_session_state_lock` und ruft eine Funktion, die ihn erneut nimmt (LOCK-2-Fehlerklasse) | mitigate | Nur direkter Dict-Zugriff im Rumpf; als **AST-Acceptance** greppbar (`VERBOTENE []` bei `FUNKTIONEN 2`), nicht als Kommentar-Zusicherung |
| T-SOF2-13 | Denial of Service | Scan ueber `_session_state` bei vielen Sitzungen im Live-Pfad | accept | Beide Funktionen laufen in HTTP-Endpunkten, nicht im Token-Pfad. Solo/EA: einstellige Sitzungszahl. Latenz-Budget < 1 ms, kein Netz, keine DB (Punkt 25) |
| T-SOF2-14 | Repudiation | Ein abgelehnter Fremdzugriff bleibt unsichtbar | mitigate | `_logger.warning('[SOFORT-2] Fremdzugriff abgelehnt: …')` an beiden Stellen, mit beiden `user_id`. Bewusst **nur** der abgelehnte Fall — jeden Aufruf zu loggen waere Rauschen |
| T-SOF2-15 | Information Disclosure | Die Log-Zeile selbst leakt Daten | accept | Sie enthaelt nur `sid`, `call_id` und zwei numerische `user_id` — keine Gespraechsinhalte, keine Namen, keine E-Mail |
| T-SOF2-16 | Tampering | Ein fremder Call mit derselben `call_id` verdeckt die eigene Sitzung | mitigate | Die Schleife bricht bei einem fremden Treffer **nicht** ab, sondern laeuft weiter (ausdruecklich in der Action begruendet) |
| T-SOF2-17 | Repudiation | ROT-Lauf nachtraeglich „erzeugt" statt vorher gezogen | mitigate | Schritt 2 belegt per `grep` auf Prod, dass der Fix dort fehlt (`0` fuer `sid_belongs_to`) — mit Existenz-Anker daneben (`2` fuer das Alt-Muster in `routes/learning.py`) |
</threat_model>

<verification>
1. ROT-Lauf liegt verbatim vor: **10 failed / 4 passed**, die vier Sperren gegen den stillen
   Ausfall sind PASSED, `api_precall_research` steht namentlich in der Fehlermeldung
2. Die zwei Test-Dateien sind vom Prod-Server wieder abgeraeumt
3. `sid_belongs_to` und `resolve_own_sid_by_call_id` existieren in `services/live_session.py`
   und vergleichen `user_id`
4. Der AST-Check auf Riegel-Selbstverklemmung gibt `FUNKTIONEN 2 VERBOTENE []`
5. Kein neuer Modul-Global, genau eine geaenderte Datei (Reparatur-Modus)
6. **KEIN Deploy in diesem Plan** — neun der vierzehn Tests sind weiterhin absichtlich rot
</verification>

<success_criteria>
- Der ROT-Beleg ist gezogen, verbatim gesichert und liegt **vor** der ersten Fix-Zeile (D-05)
- Die Frage „gehoert diese Kennung mir?" hat genau einen Ort im Code (RESEARCH §1.6)
- Fremdbesitz und unbekannte Kennung sind von aussen ununterscheidbar
- Keine Selbstverklemmung des `_session_state_lock` (Punkt 31 / LOCK-2)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.SOFORT-2-besitzpruefung-eingaenge-zeitlimit-live-llm/08.23.2.SOFORT-2-02-SUMMARY.md`
— **inklusive der verbatim ROT-Ausgabe beider Waechter** (Acceptance-Artefakt, Punkt 31).
</output>
