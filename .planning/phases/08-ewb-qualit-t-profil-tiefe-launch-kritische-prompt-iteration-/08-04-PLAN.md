---
phase: 08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - templates/profile_editor.html
  - templates/_tooltip.html
  - templates/_beispiel_profil_modal.html
  - static/nerve.css
  - routes/profiles.py
autonomous: false
requirements:
  - EWB-04
  - EWB-07
  - EWB-08
addressed_decisions: [D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-16, D-17, D-18, D-19, D-20, D-21]

must_haves:
  truths:
    - "Profile-Editor UI hat 4 neue Eingabefelder: eigene_formulierungen (Textarea), beweise (Textarea), branche_kontext (Textarea), ton (Select + Flex-Escape)"
    - "branche Freitext-Input ist durch Select-Enum mit 8 Werten + 'Bitte waehlen' ersetzt"
    - "zusatz Label lautet 'Spezielle Anweisungen an NERVE' (DB-Key unveraendert)"
    - "Alle 4 neuen Felder an 3 Stellen implementiert: HTML-Input + buildAndSubmit() JS + Populate-Handler (verhindert Pitfall 1 Wholesale-Replace)"
    - "Tooltip-Pattern auf 3-Block-Struktur umgestellt (Was rein soll / Beispiel / Nicht verwechseln mit) via Jinja-Macro in _tooltip.html"
    - "i-Button mindestens 16x16px, Hover-State, Keyboard-accessible (tabindex, role)"
    - "Read-Only Beispiel-Profil-Modal im Profile-Editor, Link-Text 'Sieh dir ein ausgefuelltes Beispiel an'"
    - "Content in ALLEN Tooltips folgt D-20 Anti-Pattern-Regel: keine NERVE/Andre/echte-Firmen-Beispiele, nur fiktive Platzhalter"
    - "routes/profiles.py POST-Handler validiert branche gegen Enum-Whitelist (Fallback 'sonstiges' statt 400)"
    - "Human-Checkpoint: Claudian-Review bestaetigt Tooltip-Anti-Pattern-Konformitaet (D-21 Launch-Gate)"
  artifacts:
    - path: "templates/_tooltip.html"
      provides: "Jinja-Macro tip3(was_rein, beispiel, nicht_verwechseln) + Dispaly-JS"
      min_lines: 15
    - path: "templates/_beispiel_profil_modal.html"
      provides: "Read-Only Modal mit fiktivem Demo-Profil (D-19)"
      min_lines: 60
    - path: "templates/profile_editor.html"
      provides: "6 Profil-Felder live, tip3-Macro importiert, Modal included"
      contains: "{% import '_tooltip.html' as tooltip %}"
    - path: "static/nerve.css"
      provides: "Tooltip + Modal + i-Button CSS (>=16px)"
      contains: ".tip-icon"
    - path: "routes/profiles.py"
      provides: "branche-Whitelist-Validation im POST-Handler"
      contains: "VALID_BRANCHE"
  key_links:
    - from: "templates/profile_editor.html buildAndSubmit()"
      to: "daten.basis.eigene_formulierungen / .beweise / .branche_kontext"
      via: "value.split + trim + filter, .value.trim() fuer String-Felder"
      pattern: "eigene_formulierungen.*split"
    - from: "templates/profile_editor.html init()"
      to: "DATEN.basis.*"
      via: "setVal + join + JSON.parse"
      pattern: "setVal\\('vi_eigene_formulierungen'"
    - from: "routes/profiles.py bearbeiten()"
      to: "VALID_BRANCHE set"
      via: "Whitelist-Check vor p.branche = ..."
      pattern: "VALID_BRANCHE"
---

<objective>
Wave 4 UI: Profile-Editor komplett umgebaut — 4 neue Felder (2 neu, 2 rebuilt, 1 erweitert, 1 relabeled), 3-Block-Tooltip-System, i-Button >=16px, Read-Only Beispiel-Profil-Modal, Anti-Pattern-konforme Content. Abschluss mit human-checkpoint: Claudian-Review der Tooltip-Texte (D-21 Launch-Gate).

Purpose: Ohne diese Aenderungen bleibt die neue Prompt-Pipeline (Plan 02/03) leer — der Live-Prompt liest die neuen Felder aber Profile-Editor schreibt sie nicht. KRITISCH: Wholesale-JSON-Replace (RESEARCH Pitfall 1) zwingt alle 4 Felder an 3 Stellen einzubauen. Anti-Pattern-Regel (D-20) ist Launch-Gate, NICHT algorithmisch pruefbar → Human-Checkpoint.

Output:
- 6 Profile-Editor-Felder live
- 2 neue Jinja-Partials (_tooltip.html, _beispiel_profil_modal.html)
- CSS-Upgrade fuer Tooltip + Modal + i-Button (>=16px)
- Server-side branche-Enum-Validation
- Claudian-Reviewed Tooltip-Content
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-CONTEXT.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-RESEARCH.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-PATTERNS.md
@templates/profile_editor.html
@routes/profiles.py
@static/nerve.css
@templates/app.html
@CLAUDE.md

<interfaces>
**Bestehende Tooltip-Pattern in profile_editor.html (aus RESEARCH Focus Area 9):**
- Line 120-141: CSS `.tip-icon` + `#g-tip` + `#g-tip-inner`, Groesse 14×14px (UNTER D-18 Spec)
- Line 625-627: JS-Helper `function ti(text) { return '<i class="tip-icon" data-tip="${esc(text)}">?</i>'; }`
- Line 629-657: mouseover/mouseout-Handler, nutzt `inner.textContent=icon.dataset.tip`

**Bestehende Profile-Editor Struktur (aus PATTERNS):**
- `buildAndSubmit()` line 948-1007 — baut komplettes JSON-Objekt aus DOM-Inputs
- `init()` / populate-Handler line 1082-1148 — liest DATEN dict und setzt DOM-Values
- Wholesale-Replace: alle 3 Stellen muessen gepflegt werden

**routes/profiles.py bearbeiten POST (line 121-155):**
```python
# Aktuell (wholesale):
daten_json = request.form.get('daten_json', p.daten or '{}')
p.branche = request.form.get('branche', p.branche or '').strip()
p.daten = daten_json
```

**Consent-Modal-Pattern analog fuer Beispiel-Profil-Modal (templates/app.html line 698-712):**
```html
<div class="consent-overlay" id="consentOverlay">
  <div class="consent-box">
    <div class="consent-title">...</div>
    <div class="consent-actions">...</div>
  </div>
</div>
```

**Enum-Werte (D-09) — strikt ASCII:**
```python
VALID_BRANCHE = {'saas_b2b', 'maschinenbau', 'versicherung', 'finanzprodukte',
                 'immobilien', 'coaching', 'beratung', 'sonstiges', ''}
```

**ton-Select Default (D-10):** 4 Stile + 'eigener_stil'-Flex-Escape:
- `Direkt/Klartext`, `Beratend/Sanft`, `Enthusiastisch/Begeistert`, `Analytisch/Zahlenorientiert`, `eigener_stil`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: templates/_tooltip.html + _beispiel_profil_modal.html + nerve.css Tooltip/Modal Styles</name>
  <files>templates/_tooltip.html, templates/_beispiel_profil_modal.html, static/nerve.css</files>
  <read_first>
    - templates/profile_editor.html Zeilen 115-145 (bestehende CSS .tip-icon + #g-tip), Zeilen 620-660 (JS ti()-Helper + mouseover-Handler), Zeilen 720-820 (Beispiel-Feldstruktur)
    - templates/app.html Zeilen 698-712 (consent-overlay Pattern als Modal-Analog)
    - static/nerve.css (Farben-Tokens: --n-border, --n-accent, Card-Styles Phase 7)
    - 08-PATTERNS.md §"templates/_tooltip.html" Zeilen 205-291, §"templates/_beispiel_profil_modal.html" Zeilen 293-355
    - 08-CONTEXT.md D-16, D-17, D-18, D-19, D-20
    - 08-RESEARCH.md Focus Area 9 (Zeilen 547-590) komplett
    - CLAUDE.md Umlaut-Regel Zeilen 103-141
  </read_first>
  <action>
    **Erstelle templates/_tooltip.html** (Jinja-Macro-Partial):

    ```jinja
    {#
      templates/_tooltip.html — Phase 08 D-16 3-Block-Tooltip-Pattern.

      Usage in einem Template:
        {% import '_tooltip.html' as tooltip %}
        <label>Feldname {{ tooltip.tip3(
          'Was rein soll — 1-2 Saetze',
          'Beispiel — kurz, konkret, fiktive Platzhalter',
          'Nicht verwechseln mit: Feld A, Feld B'
        ) }}</label>

      Content-Regel (D-20): NUR fiktive Platzhalter (Firma XY, Anna S., Branche Maschinenbau).
      KEINE NERVE-spezifischen, Andre-persoenlichen oder echten-Firmen-Beispiele.
    #}
    {% macro tip3(was_rein, beispiel, nicht_verwechseln) -%}
    <i class="tip-icon"
       data-tip-was="{{ was_rein|e }}"
       data-tip-bsp="{{ beispiel|e }}"
       data-tip-nvm="{{ nicht_verwechseln|e }}"
       tabindex="0"
       role="button"
       aria-label="Hilfe zu diesem Feld">?</i>
    {%- endmacro %}
    ```

    **Erstelle templates/_beispiel_profil_modal.html** (Read-Only Modal mit fiktivem Demo):

    ```jinja
    {#
      templates/_beispiel_profil_modal.html — Phase 08 D-19.
      Read-Only-Modal mit kompletten fiktiven Beispiel-Profil.
      D-20 Content-Regel: KEINE NERVE/Andre/echte-Firmen.
    #}
    <div class="beispiel-overlay" id="beispielOverlay" style="display:none"
         onclick="if(event.target===this)closeBeispiel()"
         role="dialog" aria-labelledby="beispielTitle" aria-modal="true">
      <div class="beispiel-box">
        <div class="beispiel-header">
          <div id="beispielTitle" class="beispiel-title">Beispiel-Profil: Anna S. (Firma XY GmbH)</div>
          <button type="button" class="beispiel-close" onclick="closeBeispiel()" aria-label="Schliessen">&times;</button>
        </div>
        <div class="beispiel-content">
          <section>
            <h3>Basis</h3>
            <p><strong>Unternehmen:</strong> Firma XY GmbH</p>
            <p><strong>Produkt:</strong> Branchensoftware fuer mittelstaendische Maschinenbauer</p>
            <p><strong>Preismodell:</strong> 12 Monate Laufzeit, ab 850 EUR/Monat pro Lizenz</p>
            <p><strong>USPs:</strong> 1) Deutsche Server, 2) eigene Implementierungsberatung, 3) Schulung inklusive</p>
          </section>
          <section>
            <h3>Branche + Kontext</h3>
            <p><strong>Branche:</strong> Maschinenbau</p>
            <p><strong>Branchen-Kontext:</strong> Mittelstand mit 50-300 MA. Typisches Pain: veraltete Excel-Prozesse in der Produktion, keine Echtzeit-Sicht auf Auftragsbestand. Haeufiger Einwand: "Wir haben schon eine Loesung" — meist ein altes ERP das niemand mehr aktiv pflegt.</p>
          </section>
          <section>
            <h3>Eigene Formulierungen</h3>
            <ul>
              <li>"Darf ich fragen, was Sie aktuell einsetzen?"</li>
              <li>"Was stoert Sie da am meisten?"</li>
              <li>"Haben Sie da eine konkrete Zahl?"</li>
            </ul>
          </section>
          <section>
            <h3>Beweise</h3>
            <ul>
              <li>Firma Z GmbH (40 MA, Maschinenbau): nach 3 Monaten 15% schnellere Auftragsabwicklung</li>
              <li>Kundenzitat: "Das erste Mal, dass wir wirklich sehen wo wir stehen."</li>
              <li>Durchschnittlicher ROI bei unseren Kunden: 2,3x innerhalb 12 Monaten</li>
            </ul>
          </section>
          <section>
            <h3>Stil (Ton)</h3>
            <p><strong>Direkt/Klartext</strong> — kurze Saetze, wenige Fuellworte, direkt zur Sache.</p>
          </section>
          <section>
            <h3>Spezielle Anweisungen an NERVE</h3>
            <p>Immer siezen. Fachbegriffe aus Maschinenbau-Jargon erlaubt (Werkzeugmaschine, CAD, CNC). Keine Anglizismen wie "Solution" — stattdessen "Loesung".</p>
          </section>
          <section>
            <h3>Einwaende + Gegenargumente (Auszug)</h3>
            <ul>
              <li><strong>"Zu teuer":</strong> "Verstehe. Rechne mal: Sie zahlen heute wie viel fuer Excel-Lizenzen plus die Stunden die Ihr Team mit Fehler-Korrekturen verbringt? Firma Z hat das erste Mal berechnet — 480 EUR pro Woche fuer Fehler-Korrekturen. Wir wuerden das auf unter 100 EUR senken."</li>
              <li><strong>"Haben schon was aehnliches":</strong> "Darf ich fragen, was Sie einsetzen? Und wann wurde das zuletzt aktualisiert?"</li>
            </ul>
          </section>
        </div>
      </div>
    </div>
    <script>
    function closeBeispiel() {
      const el = document.getElementById('beispielOverlay');
      if (el) el.style.display = 'none';
    }
    function openBeispiel() {
      const el = document.getElementById('beispielOverlay');
      if (el) el.style.display = 'flex';
    }
    </script>
    ```

    WICHTIG zu Content: Jede Firma, Person, Zahl in diesem Modal ist FIKTIV. Kein NERVE, kein Andre, keine Sparkasse Iserlohn, keine "87% unserer Kunden". Alles generisch und neutral. Falls Task 3 Claudian-Review dieses auch mitprueft.

    **Ergaenze static/nerve.css** am Ende (oder nach bestehenden `.tip-icon`-Rules falls schon in nerve.css) folgende Blocke:

    ```css
    /* ── Phase 08 D-18: Tooltip-Icon >=16px ────────────────────────── */
    .tip-icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 16px; height: 16px; min-width: 16px;
      margin-left: 6px;
      border: 1px solid var(--n-border, #2D3748);
      border-radius: 50%;
      background: #F3F4F6;
      color: #1C2333;
      font-size: 11px;
      font-weight: 700;
      cursor: help;
      user-select: none;
      vertical-align: middle;
    }
    .tip-icon:hover,
    .tip-icon:focus {
      background: var(--n-accent, #00D4AA);
      color: #0D1117;
      outline: none;
    }

    /* Tooltip-Container (global singleton) — 3-Block-Pattern */
    #g-tip {
      position: fixed;
      display: none;
      background: #0D1117;
      color: #E4E4E7;
      padding: 12px 14px;
      border-radius: 8px;
      max-width: 360px;
      font-size: 13px;
      line-height: 1.45;
      z-index: 9999;
      box-shadow: 0 4px 16px rgba(0,0,0,0.35);
      pointer-events: none;
    }
    #g-tip .tip-block { margin-bottom: 8px; }
    #g-tip .tip-block:last-child { margin-bottom: 0; }
    #g-tip .tip-block strong { color: var(--n-accent, #00D4AA); }

    /* ── Phase 08 D-19: Beispiel-Profil-Modal ──────────────────────── */
    .beispiel-overlay {
      position: fixed; inset: 0;
      background: rgba(0,0,0,0.55);
      z-index: 9998;
      display: flex; align-items: center; justify-content: center;
    }
    .beispiel-box {
      background: #FFFFFF;
      max-width: 760px;
      max-height: 86vh;
      width: 90%;
      border-radius: 12px;
      box-shadow: 0 8px 40px rgba(0,0,0,0.3);
      display: flex; flex-direction: column;
    }
    .beispiel-header {
      display: flex; justify-content: space-between; align-items: center;
      padding: 16px 20px; border-bottom: 1px solid var(--n-border, #E5E7EB);
    }
    .beispiel-title { font-weight: 600; font-size: 16px; color: #111827; }
    .beispiel-close {
      background: transparent; border: 0; font-size: 24px; line-height: 1;
      cursor: pointer; color: #6B7280; padding: 0 4px;
    }
    .beispiel-close:hover { color: #111827; }
    .beispiel-content {
      padding: 20px; overflow-y: auto; flex: 1; color: #1F2937; font-size: 14px; line-height: 1.55;
    }
    .beispiel-content section { margin-bottom: 20px; }
    .beispiel-content section h3 {
      font-size: 14px; font-weight: 700; color: var(--n-accent, #00D4AA);
      margin: 0 0 6px 0; text-transform: uppercase; letter-spacing: 0.5px;
    }
    .beispiel-content ul { margin: 6px 0 0 20px; padding: 0; }
    .beispiel-content li { margin-bottom: 4px; }

    /* Link-Trigger im Editor */
    .beispiel-link {
      display: inline-block;
      color: var(--n-accent, #00D4AA);
      font-size: 13px;
      font-weight: 500;
      text-decoration: underline;
      cursor: pointer;
      margin-top: 4px;
    }
    .beispiel-link:hover { color: #009b7b; }
    ```

    Umlaut-Regel: User-facing (HTML-Content, CSS comments) mit echten Umlauten (where relevant). CSS-Klassen ASCII (`.beispiel-overlay` nicht `.beispiel-uberlagerung`, `.tip-icon` nicht `.tip-icon-hilfe`).
  </action>
  <verify>
    <automated>test -f templates/_tooltip.html && test -f templates/_beispiel_profil_modal.html && grep -q "macro tip3" templates/_tooltip.html && grep -q "data-tip-was\|data-tip-bsp\|data-tip-nvm" templates/_tooltip.html && grep -q "Firma XY\|Anna S." templates/_beispiel_profil_modal.html && grep -q "NERVE\|Andre\|Sparkasse" templates/_beispiel_profil_modal.html && echo "ERROR: D-20 anti-pattern content found" && exit 1 || echo "OK: D-20 content clean"; grep -q "width: 16px" static/nerve.css && grep -q "beispiel-overlay" static/nerve.css && echo "OK: css blocks present"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f templates/_tooltip.html` → exit 0
    - `grep -nE "macro tip3\(was_rein, beispiel, nicht_verwechseln\)" templates/_tooltip.html` → exit 0
    - `grep -n "data-tip-was\|data-tip-bsp\|data-tip-nvm" templates/_tooltip.html` → 3 matches
    - `grep -n "tabindex\|role" templates/_tooltip.html` → 2 matches (Accessibility)
    - `test -f templates/_beispiel_profil_modal.html` → exit 0
    - `wc -l templates/_beispiel_profil_modal.html` → >= 60
    - `grep -n "Firma XY\|Anna S." templates/_beispiel_profil_modal.html` → exit 0 (fiktive Platzhalter)
    - D-20 Anti-Pattern-Check: `grep -niE "NERVE|Preu[a-z]*|Andre|Sparkasse|Iserlohn|87%" templates/_beispiel_profil_modal.html` → NO matches (wenn matches: fail, D-20-Content-Regel verletzt)
    - `grep -nE "closeBeispiel|openBeispiel" templates/_beispiel_profil_modal.html` → >= 2 matches (JS-Helpers)
    - `grep -nE "^\s*\.tip-icon\s*\{" static/nerve.css` → exit 0
    - `grep -nE "width:\s*16px|min-width:\s*16px" static/nerve.css` → >= 2 matches (D-18 >=16px)
    - `grep -nE "^\s*\.beispiel-overlay\s*\{" static/nerve.css` → exit 0
    - `grep -nE "^\s*#g-tip\s" static/nerve.css` → exit 0
    - `grep -nE "\.tip-block\b" static/nerve.css` → exit 0 (3-Block-Struktur im Tooltip-CSS)
  </acceptance_criteria>
  <done>2 neue Partial-Templates + CSS-Upgrade committed. i-Button >=16px, Tooltip-CSS hat tip-block-Pattern fuer 3-Block-Display, Modal-CSS matched Consent-Modal-Design, Beispiel-Profil-Content frei von D-20 Anti-Patterns.</done>
</task>

<task type="auto">
  <name>Task 2: templates/profile_editor.html — 6 Felder + 3-Block-Tooltips + Modal-Link + routes/profiles.py Enum-Validation</name>
  <files>templates/profile_editor.html, routes/profiles.py</files>
  <read_first>
    - templates/profile_editor.html KOMPLETT (1200+ Zeilen) — insbesondere:
      - Zeilen 115-145 (alte Tooltip-CSS — wird obsolet durch nerve.css-Block aus Task 1, aber NICHT loeschen noch)
      - Zeilen 620-657 (JS `ti()`-Helper + mouseover-Handler — MUSS fuer 3-Block umgebaut werden)
      - Zeilen 720-820 (Basis-Sektion — eigene_formulierungen + beweise + branche_kontext + branche-Select hier einreihen)
      - Zeilen 791-870 (einwaende-Sektion — Referenz-Pattern)
      - Zeilen 860-940 (ki-Sektion — ton-Select + zusatz-Label hier aendern)
      - Zeilen 948-1007 (buildAndSubmit — JS-Builder)
      - Zeilen 1082-1148 (init + populate-Handler)
    - routes/profiles.py Zeilen 121-155 (bearbeiten POST-Handler)
    - templates/_tooltip.html + templates/_beispiel_profil_modal.html (aus Task 1)
    - 08-CONTEXT.md D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-16, D-17 (Abgrenzungs-Matrix)
    - 08-RESEARCH.md Focus Area 6 "Profile JSON-Merge-Pattern" Zeilen 414-434, Focus Area 7 "branche-Enum" Zeilen 436-493
    - 08-PATTERNS.md §"templates/profile_editor.html" Zeilen 732-866, §"routes/profiles.py" Zeilen 1001-1029
    - scripts/migrate_branche_to_enum.py (Plan 03 — VALID_ENUMS Set) fuer Quellcodes-Konsistenz
    - CLAUDE.md Umlaut-Regel
  </read_first>
  <action>
    **Edit-Block A — Jinja-Macro-Import + Modal-Include am Template-Top:**

    Direkt unter dem `{% extends ... %}`-Statement in profile_editor.html:
    ```jinja
    {% import '_tooltip.html' as tooltip %}
    ```

    Innerhalb des `{% block content %}` (vor dem schliessenden `{% endblock %}`):
    ```jinja
    {% include '_beispiel_profil_modal.html' %}
    ```

    Am passenden Platz im Editor (z.B. neben der Header-Ueberschrift des Editors):
    ```jinja
    <a href="#" class="beispiel-link" onclick="openBeispiel();return false"
       aria-label="Beispiel-Profil anzeigen">Sieh dir ein ausgefuelltes Beispiel an</a>
    ```

    **Edit-Block B — Basis-Sektion mit 4 neuen Feldern (D-07, D-08, D-09, D-11):**

    In der Basis-Sektion (typisch nach Unternehmen/Produkt/Preismodell) einreihen:

    ```html
    <!-- D-09 branche Enum-Select (ERSETZT Freitext-Input wenn vorhanden) -->
    <div class="fg">
      <label class="fl">Branche {{ tooltip.tip3(
        'Die Haupt-Branche in der du verkaufst — waehle den naechsten Wert aus der Liste.',
        'Beispiel: Maschinenbau, wenn du Software fuer produzierende Unternehmen verkaufst.',
        'Branchen-Kontext (tiefe Pain-Points) / Unternehmen (dein eigenes) / Zielgruppe (Ansprechpartner)'
      ) }}</label>
      <select class="fs" id="vi_branche_select" name="branche">
        <option value="">-- Bitte waehlen --</option>
        <option value="saas_b2b">SaaS B2B</option>
        <option value="maschinenbau">Maschinenbau</option>
        <option value="versicherung">Versicherung</option>
        <option value="finanzprodukte">Finanzprodukte</option>
        <option value="immobilien">Immobilien</option>
        <option value="coaching">Coaching</option>
        <option value="beratung">Beratung</option>
        <option value="sonstiges">Sonstiges</option>
      </select>
    </div>

    <!-- D-11 branche_kontext Textarea -->
    <div class="fg">
      <label class="fl">Branchen-Kontext {{ tooltip.tip3(
        'Jargon, typische Pain-Points, was in der Branche funktioniert vs. nicht. Hilft NERVE deine Einwaende praeziser zu beantworten.',
        'Mittelstand 50-300 MA. Typisches Pain: veraltete Excel-Prozesse. Haeufiger Einwand: "Wir haben schon was."',
        'Branche (nur Enum-Wert) / Zielgruppe (Ansprechpartner-Profil) / Unternehmen (dein eigenes)'
      ) }}</label>
      <textarea class="fta" id="vi_branche_kontext" style="min-height:60px"
                placeholder="Jargon, Pain-Points, typische Einwaende der Branche ..."></textarea>
    </div>

    <!-- D-07 eigene_formulierungen Textarea (multi-line) -->
    <div class="fg">
      <label class="fl">Eigene Formulierungen {{ tooltip.tip3(
        'Saetze die du im Call wortwoertlich sagst — Claude imitiert deinen Stil statt generisches Vertriebs-Sprech. Eine Formulierung pro Zeile.',
        'Darf ich fragen, was Sie aktuell einsetzen?\nWas stoert Sie da am meisten?\nHaben Sie da eine konkrete Zahl?',
        'Stil (ton = allgemeine Art) / Gegenargumente (einwaende-spezifisch) / Spezielle Anweisungen (zusatz = Regeln)'
      ) }}</label>
      <textarea class="fta" id="vi_eigene_formulierungen" style="min-height:84px"
                placeholder="Eine Formulierung pro Zeile ..."></textarea>
    </div>

    <!-- D-08 beweise Textarea (multi-line) -->
    <div class="fg">
      <label class="fl">Beweise {{ tooltip.tip3(
        'Zahlen, Kundenzitate, Fallstudien die Claude im Baustein "Beweis" einsetzt. Eine pro Zeile.',
        'Firma Z: 15% mehr Abschluesse in 3 Monaten.\nKundenzitat: "Das erste Mal, dass wir sehen wo wir stehen."\n2,3x ROI im Durchschnitt.',
        'USPs (eigene Produkt-Vorteile) / Konsequenz (was passiert ohne Produkt) / Branchen-Kontext (Pain-Points)'
      ) }}</label>
      <textarea class="fta" id="vi_beweise" style="min-height:84px"
                placeholder="Eine pro Zeile — Zahlen, Zitate, Fallstudien ..."></textarea>
    </div>
    ```

    **Edit-Block C — ki-Sektion: ton-Select + Flex-Escape + zusatz-Label (D-10, D-12):**

    Suche die existing `ton`/`ki_ton`-Zeile in der ki-Sektion. Ersetze durch:

    ```html
    <!-- D-10 ton Select mit Flex-Escape -->
    <div class="fg">
      <label class="fl">Stil (Ton) {{ tooltip.tip3(
        'Wie soll NERVE sprechen? Das ist ein allgemeiner Stil, der sich auf ALLE Antworten auswirkt.',
        'Direkt/Klartext = kurz, wenig Fuellworte. Beratend/Sanft = empathisch, viele Fragen. Eigener Stil: frei formuliert.',
        'Techniken aktiv (konkrete Methoden) / Spezielle Anweisungen (konkrete Do/Don\'t-Regeln) / Eigene Formulierungen (ganze Saetze)'
      ) }}</label>
      <select class="fs" id="vi_ton_select" onchange="toggleTonFlex()">
        <option value="">-- Bitte waehlen --</option>
        <option value="Direkt/Klartext">Direkt/Klartext</option>
        <option value="Beratend/Sanft">Beratend/Sanft</option>
        <option value="Enthusiastisch/Begeistert">Enthusiastisch/Begeistert</option>
        <option value="Analytisch/Zahlenorientiert">Analytisch/Zahlenorientiert</option>
        <option value="eigener_stil">Eigener Stil ...</option>
      </select>
      <input type="text" class="fi" id="vi_ton_flex" style="display:none;margin-top:6px"
             placeholder="Beschreibe deinen eigenen Stil ..." />
    </div>
    ```

    Suche die existing `zusatz`/`ki_zusatz`-Label-Zeile. Ersetze das Label (DB-Key bleibt `zusatz`):

    ```html
    <!-- D-12 zusatz nur Label-Aenderung, DB-Key unveraendert -->
    <div class="fg">
      <label class="fl">Spezielle Anweisungen an NERVE {{ tooltip.tip3(
        'Konkrete Regeln fuer den KI-Assistenten. Do/Dont. Jargon-Erlaubnis, Tabu-Woerter, Anrede-Ausnahmen.',
        'Immer siezen. Fachbegriffe aus Maschinenbau-Jargon erlaubt. Keine Anglizismen — stattdessen deutsche Begriffe.',
        'Techniken verboten (Methoden-Blacklist) / Stil (allgemeiner Ton) / Eigene Formulierungen (ganze Saetze)'
      ) }}</label>
      <textarea class="fta" id="vi_zusatz" style="min-height:72px"
                placeholder="Regeln, Do / Dont, Jargon-Erlaubnis ..."></textarea>
    </div>
    ```

    **Edit-Block D — buildAndSubmit() JS-Builder ergaenzen:**

    Suche die daten.basis-Definition in buildAndSubmit (~line 954-961). Ergaenze:
    ```javascript
    daten.basis.eigene_formulierungen =
        document.getElementById('vi_eigene_formulierungen').value
            .split('\n').map(s => s.trim()).filter(Boolean);
    daten.basis.beweise =
        document.getElementById('vi_beweise').value
            .split('\n').map(s => s.trim()).filter(Boolean);
    daten.basis.branche_kontext =
        document.getElementById('vi_branche_kontext').value.trim();
    ```

    Suche die daten.ki-Definition (~line 997-1003). Ersetze ton-Block durch Flex-Logic:
    ```javascript
    daten.ki = daten.ki || {};
    (function() {
      const sel = document.getElementById('vi_ton_select').value;
      if (sel === 'eigener_stil') {
        daten.ki.ton = document.getElementById('vi_ton_flex').value.trim();
      } else {
        daten.ki.ton = sel;
      }
    })();
    // zusatz bleibt wie bisher (DB-Key unveraendert):
    daten.ki.zusatz = document.getElementById('vi_zusatz').value.trim();
    ```

    **Edit-Block E — init()/populate-Handler ergaenzen:**

    Suche die setVal-Block in init (~line 1082-1148). Ergaenze:
    ```javascript
    const basis = (DATEN && DATEN.basis) || {};
    setVal('vi_eigene_formulierungen', (basis.eigene_formulierungen || []).join('\n'));
    setVal('vi_beweise', (basis.beweise || []).join('\n'));
    setVal('vi_branche_kontext', basis.branche_kontext || '');

    // branche Top-Level (NICHT in daten.basis — separate Profile.branche Column):
    const brancheSelect = document.getElementById('vi_branche_select');
    if (brancheSelect && PROFILE_BRANCHE) {  // PROFILE_BRANCHE muss vom Jinja-Context als Variable bereitgestellt werden
      brancheSelect.value = PROFILE_BRANCHE;
    }

    // ton Select + Flex-Escape:
    const KNOWN_TONS = ['Direkt/Klartext', 'Beratend/Sanft',
                         'Enthusiastisch/Begeistert', 'Analytisch/Zahlenorientiert'];
    const tonSelect = document.getElementById('vi_ton_select');
    const tonFlex = document.getElementById('vi_ton_flex');
    const currentTon = (DATEN.ki || {}).ton || '';
    if (tonSelect && tonFlex) {
      if (KNOWN_TONS.indexOf(currentTon) >= 0) {
        tonSelect.value = currentTon;
        tonFlex.style.display = 'none';
      } else if (currentTon) {
        tonSelect.value = 'eigener_stil';
        tonFlex.value = currentTon;
        tonFlex.style.display = 'block';
      }
    }

    function toggleTonFlex() {
      const sel = document.getElementById('vi_ton_select');
      const flex = document.getElementById('vi_ton_flex');
      if (sel && flex) {
        flex.style.display = (sel.value === 'eigener_stil') ? 'block' : 'none';
      }
    }
    window.toggleTonFlex = toggleTonFlex;  // sichtbar fuer onclick
    ```

    WICHTIG: `PROFILE_BRANCHE` muss vom Template ueber Jinja bereitgestellt werden — suche am Skript-Anfang wie DATEN injiziert wird (`const DATEN = {{ profile.daten|safe }};`) und ergaenze:
    ```javascript
    const PROFILE_BRANCHE = "{{ profile.branche|default('', true)|e }}";
    ```

    **Edit-Block F — JS Tooltip-3-Block Display-Handler umbauen:**

    Suche in profile_editor.html die mouseover/mouseout-Handler (~Zeile 629-657). Ersetze den Content-Assignment-Part VOLLSTAENDIG durch den folgenden Dual-Mode-Handler (3-Block NEU + Legacy-Fallback). Dies ist die EINZIGE Version — es gibt keine separate Stand-alone-3-Block-Variante, da `ti()`-Helper (Zeile 625-627) weiter fuer bestehende 1-Satz-Aufrufe (addEinwand etc.) benoetigt wird.

    WICHTIG zur `ti()`-Helper: Die alte `ti(text)`-Funktion bleibt unangefasst — sie setzt `data-tip` (alt, 1-Satz). NEUE Aufrufe nutzen `{{ tooltip.tip3(...) }}` Jinja-Macro und setzen `data-tip-was` / `data-tip-bsp` / `data-tip-nvm`. Der Display-Handler prueft zuerst die 3-Block-Attribute und faellt bei absent zurueck auf `data-tip`:

    ```javascript
    inner.innerHTML = '';
    if (icon.dataset.tipWas || icon.dataset.tipBsp || icon.dataset.tipNvm) {
      // 3-Block-Modus (neue Tooltips nach D-16)
      const sections = [
        {label: 'Was rein soll', value: icon.dataset.tipWas || ''},
        {label: 'Beispiel', value: icon.dataset.tipBsp || ''},
        {label: 'Nicht verwechseln mit', value: icon.dataset.tipNvm || ''},
      ];
      sections.forEach(s => {
        if (!s.value) return;
        const block = document.createElement('div');
        block.className = 'tip-block';
        block.innerHTML = '<strong>' + s.label + ':</strong> ';
        const span = document.createElement('span');
        span.textContent = s.value;
        block.appendChild(span);
        inner.appendChild(block);
      });
    } else {
      // Legacy 1-Satz-Fallback (alte ti() Aufrufe via data-tip)
      inner.textContent = icon.dataset.tip || '';
    }
    ```

    **Edit-Block G — routes/profiles.py Enum-Whitelist-Validation:**

    In routes/profiles.py bearbeiten()-POST-Handler (~Zeile 134-141) ergaenze:
    ```python
    # Phase 08 D-09: branche Enum-Whitelist
    VALID_BRANCHE = {'saas_b2b', 'maschinenbau', 'versicherung', 'finanzprodukte',
                     'immobilien', 'coaching', 'beratung', 'sonstiges', ''}
    branche_raw = (request.form.get('branche') or p.branche or '').strip()
    if branche_raw and branche_raw not in VALID_BRANCHE:
        # Fallback statt 400 — konservativ: sonstiges + (wenn daten.basis.branche_kontext leer) Originaltext merge
        branche_raw = 'sonstiges'
    p.branche = branche_raw
    ```

    Alle Aenderungen halten Wholesale-Replace-Pattern intakt — `p.daten = daten_json` bleibt.

    Umlaut-Regel:
    - HTML-Content (Labels, Tooltips, Placeholders, Modal-Content) → **echte Umlaute** (Gespräch, Einwände, ausgefüllt, stört, Sätze).
    - CSS-Klassen + HTML-IDs + JS-Variables → ASCII (vi_eigene_formulierungen, vi_ton_flex, n-ewb-btn).
    - Enum-Werte (saas_b2b, etc.) strikt lowercase ASCII.

    **Content-Check Phase D-20:** Alle Tooltip-Texte in diesem Edit-Block verwenden NUR fiktive Platzhalter (Firma Z, Anna S., "Wir haben schon was."). Keine NERVE-spezifischen Beispiele, keine echte-Firmen-Namen, keine André-persoenlichen Tonfaelle ("Guck mal, du kennst das ..."). Task 3 Claudian-Review prueft nochmals.
  </action>
  <verify>
    <automated>grep -q "tooltip.tip3" templates/profile_editor.html && grep -q "vi_eigene_formulierungen" templates/profile_editor.html && grep -q "vi_beweise" templates/profile_editor.html && grep -q "vi_branche_kontext" templates/profile_editor.html && grep -q "vi_branche_select" templates/profile_editor.html && grep -q "vi_ton_select" templates/profile_editor.html && grep -q "eigener_stil" templates/profile_editor.html && grep -q "Spezielle Anweisungen an NERVE" templates/profile_editor.html && grep -q "VALID_BRANCHE" routes/profiles.py && grep -q "beispielOverlay\|openBeispiel" templates/profile_editor.html && echo "OK: all field hooks + enum + modal trigger found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "{% import '_tooltip.html' as tooltip %}" templates/profile_editor.html` → exit 0
    - `grep -n "{% include '_beispiel_profil_modal.html' %}" templates/profile_editor.html` → exit 0
    - `grep -n "tooltip.tip3(" templates/profile_editor.html` → >= 6 Aufrufe (branche, branche_kontext, eigene_formulierungen, beweise, ton, zusatz — Tooltips fuer alle neuen/geaenderten Felder)
    - `grep -nE 'id="vi_(eigene_formulierungen|beweise|branche_kontext|branche_select|ton_select|ton_flex|zusatz)"' templates/profile_editor.html` → 7 matches
    - `grep -n "eigene_formulierungen.*split" templates/profile_editor.html` → exit 0 (Builder)
    - `grep -nE "setVal\('vi_(eigene_formulierungen|beweise|branche_kontext)'" templates/profile_editor.html` → 3 matches (Populate)
    - `grep -n "Spezielle Anweisungen an NERVE" templates/profile_editor.html` → exit 0 (D-12 Label)
    - `grep -n "Sieh dir ein ausgefuelltes Beispiel\|Sieh dir ein ausgefülltes Beispiel" templates/profile_editor.html` → exit 0 (Modal-Link)
    - `grep -n "toggleTonFlex" templates/profile_editor.html` → >= 2 matches
    - `grep -n "data-tip-was" templates/profile_editor.html` → >= 1 match (im Display-Handler)
    - `grep -n "VALID_BRANCHE" routes/profiles.py` → exit 0
    - `grep -n "saas_b2b.*maschinenbau.*versicherung.*finanzprodukte.*immobilien.*coaching.*beratung.*sonstiges" routes/profiles.py` → Menge/Set enthaelt alle 8 Enum-Werte (oder mit Line-Breaks: `grep -c "'saas_b2b'\|'maschinenbau'\|'versicherung'\|'finanzprodukte'\|'immobilien'\|'coaching'\|'beratung'\|'sonstiges'" routes/profiles.py` → >= 8)
    - Import-Smoke: `python -c "from routes.profiles import profiles_bp"` → exit 0
    - 3-Block-Tooltip-Count: `grep -c "data-tip-was" templates/profile_editor.html` >= 6 (6+ neue 3-Block-Tooltips)
    - W-8 ton-Flex-Integritaet: `grep -nE "'eigener_stil'" templates/profile_editor.html` → genau 3 Fundstellen zulaessig: (1) `<option value="eigener_stil">` (Zeile ~487), (2) `if (sel === 'eigener_stil')` in buildAndSubmit (Zeile ~528), (3) `tonSelect.value = 'eigener_stil'` in populate-Handler (Zeile ~564) bzw. toggleTonFlex (Zeile ~574). KEINE weitere Fundstelle, insbesondere nicht in einer Zuweisung zu `daten.ki.ton`.
    - W-8 buildAndSubmit branch-Coverage: `grep -nE "daten\.ki\.ton = '?eigener_stil'?|daten\.ki\.ton\s*=\s*'eigener_stil'" templates/profile_editor.html` → exit 1 (der Literal-String darf NIEMALS direkt als ton-Wert geschrieben werden)
    - W-8 buildAndSubmit sichert empty-flex-Fallback: Der else-Branch bei `sel === 'eigener_stil'` mit leerem `vi_ton_flex` fuehrt NICHT dazu dass `daten.ki.ton === 'eigener_stil'` persistiert wird. Expliziter Trim-Check: `document.getElementById('vi_ton_flex').value.trim()` liefert dann `''` → `daten.ki.ton = ''` (leer, nicht die Sentinel-String). Dokumentiert im Task 2 Edit-Block D Kommentar.
  </acceptance_criteria>
  <done>Profile-Editor hat alle 6 Profil-Felder mit 3-Block-Tooltips, Modal-Link, ton-Select mit Flex-Escape, zusatz neu gelabelt, routes/profiles.py validiert branche gegen Enum-Whitelist. Wholesale-Pattern intakt (JS baut alle 3 Stellen), kein Field-Verlust bei Re-Save moeglich.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-Checkpoint — Claudian-Review aller Tooltip-Texte (D-21 Launch-Gate)</name>
  <files>templates/profile_editor.html, templates/_tooltip.html, templates/_beispiel_profil_modal.html</files>
  <action>Human review only — keine automatisierten Edits. Task 2 hat bereits den Content implementiert. Dieser Checkpoint pausiert Execution bis User "approved" oder eine Fix-Liste zurueckgibt. Der Executor zeigt dem User den how-to-verify-Block und wartet auf resume-signal.</action>
  <verify>
    <automated>grep -q "tooltip.tip3" templates/profile_editor.html && grep -q "data-tip-was" templates/profile_editor.html && grep -q "openBeispiel" templates/profile_editor.html && echo "precondition OK — waiting for human approval"</automated>
  </verify>
  <done>User antwortet mit "approved" nach Durchlaufen aller Verifikations-Schritte. Sonst werden Findings als Fix-Liste zurueckgegeben und Task 2 erneut ausgefuehrt (inline-Korrektur).</done>
  <what-built>
    In Task 2 wurden ~6-10 neue 3-Block-Tooltips (je 3 Bloecke = 18-30 Text-Strings) sowie 1 Beispiel-Profil-Modal mit 6-8 Sektionen Content implementiert. Alle Content-Strings sollen D-20 Anti-Pattern-Regel folgen:
    - KEINE NERVE-spezifischen Inhalte
    - KEINE Andre-persoenlichen Tonfaelle ("Guck mal, du kennst das...", "im 10. Call...")
    - KEINE echte-Firmen-Namen (Sparkasse Iserlohn, BMW, ...)
    - KEINE angebliche NERVE-Statistiken ("87% unserer Kunden", "2,3x ROI bei NERVE-Kunden")
    - NUR fiktive Platzhalter (Firma XY, Anna S., Firma Z GmbH, "Branche Maschinenbau")
    - Bei Stil-Bandbreite: mehrere Beispiele ueber verschiedene Stile
  </what-built>
  <how-to-verify>
    **Schritt 1 — Starte die App und oeffne den Profile-Editor:**
    ```bash
    python app.py
    # Browser: http://localhost:5000/profiles/edit/<deinProfilId>
    ```

    **Schritt 2 — Durchlaufe jedes neue Feld mit Mouse-Hover auf i-Button:**
    Liste der zu reviewenden Tooltips (aus Task 2):
    - [ ] Branche (Enum-Select) — 3 Bloecke
    - [ ] Branchen-Kontext (Textarea) — 3 Bloecke
    - [ ] Eigene Formulierungen — 3 Bloecke
    - [ ] Beweise — 3 Bloecke
    - [ ] Stil (Ton) — 3 Bloecke
    - [ ] Spezielle Anweisungen an NERVE — 3 Bloecke

    Pro Tooltip pruefe:
    - [ ] Block 1 "Was rein soll" ist klar und nicht laenger als 1-2 Saetze
    - [ ] Block 2 "Beispiel" hat nur fiktive Platzhalter (Firma XY, Anna S., Firma Z)
    - [ ] Block 3 "Nicht verwechseln mit" grenzt zu 2-4 aehnlichen Feldern ab
    - [ ] Kein NERVE, kein Andre, keine echte Firma, keine angebliche NERVE-Stat
    - [ ] Umlaute (ae/oe/ue/ss) werden korrekt als echte Umlaute dargestellt ODER einheitlich als ae-Ersatz (konsistent, nicht gemischt)

    **Schritt 3 — Oeffne das Beispiel-Profil-Modal:**
    - [ ] Klicke "Sieh dir ein ausgefuelltes Beispiel an"
    - [ ] Modal oeffnet sich zentriert, weisse Kachel, schwarzer Backdrop
    - [ ] 6-8 Sektionen sichtbar (Basis, Branche+Kontext, Eigene Formulierungen, Beweise, Stil, Spezielle Anweisungen, Einwaende)
    - [ ] Jede Sektion hat nur fiktive Beispiele (Firma XY GmbH, Firma Z GmbH, Anna S.)
    - [ ] Schliessen per X oder Outside-Click funktioniert
    - [ ] ESC-Taste schliesst Modal (optional, nice-to-have)

    **Schritt 4 — UI-Check fuer i-Button >=16px:**
    - [ ] Browser-DevTools → Inspect Element auf einem `.tip-icon` → width/height jeweils 16px
    - [ ] Hover-State zeigt sichtbare Farbaenderung (teal Akzent)
    - [ ] Click oder Tab-Focus zeigt keyboard-accessible Outline

    **Schritt 5 — Anti-Pattern-Scan:**
    Durchsuche visuell alle geaenderten Stellen. Wenn du findest:
    - "NERVE" im Tooltip-Content oder Modal → muss entfernt werden
    - "87%" oder aehnliche Stat-Zahlen ohne fiktive Quelle → muss durch "Firma Z: 15% ..." ersetzt werden
    - Echte Firmen-Namen (Sparkasse, BMW, ...) → muss durch "Firma XY" ersetzt werden
    - Andre-Tonfall ("Guck mal", "du kennst das") → muss neutralisiert werden

    **Schritt 6 — Save-Test (Pitfall 1 Regression):**
    - [ ] Fuelle alle 4 neuen Felder aus (eigene_formulierungen: 2 Zeilen, beweise: 2 Zeilen, branche_kontext: 1 Satz, branche: Maschinenbau, ton: Direkt/Klartext, zusatz: 1 Satz)
    - [ ] Save → Success-Flash
    - [ ] Reload der Seite
    - [ ] ALLE 4 Felder sind noch befuellt (keine Pitfall-1-Daten-Verlust)
    - [ ] Bei Ton eigener_stil → Flex-Input erscheint dynamisch, Wert bleibt nach Save
  </how-to-verify>
  <resume-signal>Type "approved" wenn alle 6 Tooltip-Sets + Modal + Save-Roundtrip passen. Sonst: kurze Liste welche Stellen zu fixen sind (Feld-Name + Block-Nummer) — planner adressiert per inline-Fix in Task 2 Re-Run.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Profile-Editor-POST | User-Input fuer 6 Felder, wholesale-JSON-Replace. |
| Jinja-Template ↔ data-tip-Attribute | Tooltip-Content wird escaped via `|e` — keine HTML-Injection. |
| Client-JS ↔ ton-Flex-Escape | "eigener_stil"-Wert wird in DB gespeichert — muss Whitelist-less bleiben (Freitext). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | User submittet ungueltigen branche-Wert im POST | mitigate | routes/profiles.py VALID_BRANCHE-Set validiert. Fallback auf 'sonstiges' statt 400. |
| T-08-04-02 | Info Disclosure | Tooltip-Content leakt NERVE-Interna oder echte Kunden-Daten | mitigate | D-20 Anti-Pattern-Regel. Task 3 Human-Checkpoint Claudian-Review VOR Launch. |
| T-08-04-03 | Tampering | XSS via eigene_formulierungen oder beweise in Profile-Editor-Render | mitigate | Jinja autoescape ist aktiv (Default in Flask-Template-Config). `|e` fuer data-tip-Attribute explizit in _tooltip.html. `textContent` statt innerHTML fuer 3-Block-Display-Handler (keine HTML-Injection ueber User-Content). |
| T-08-04-04 | Integrity | Modal-Include wird mehrfach injiziert auf gleicher Seite | accept | Modal-ID "beispielOverlay" ist HTML-ID-uniq. Include-Statement einmal pro Seite. Risk minimal. |
| T-08-04-05 | Denial | Profile mit sehr grossen eigene_formulierungen-Arrays (>1MB) | accept | JSON-Spalte profiles.daten ist TEXT-Typ ohne Laenge-Limit. Praktisch unrealistisch. Launch-Gate: Form-Size-Limit in Flask (MAX_CONTENT_LENGTH=5MB aus Phase 4.7 reicht). |
| T-08-04-06 | Info Disclosure | Template-File _beispiel_profil_modal.html enthaelt Demo-Daten | accept | Alle Daten sind FIKTIV (Firma XY, Anna S.) — keine echten Personal-Daten. D-20 Compliant. |
</threat_model>

<verification>
Alle 3 Tasks muessen gruen sein (Task 3 blocking Human-Checkpoint):
1. `test -f templates/_tooltip.html && test -f templates/_beispiel_profil_modal.html` — Partial-Files existieren
2. `grep -c "tooltip.tip3(" templates/profile_editor.html` >= 6 — mindestens 6 neue Tooltip-Aufrufe
3. `grep -c "data-tip-was" templates/profile_editor.html` >= 6 — 3-Block-Pattern im HTML
4. `grep -n "VALID_BRANCHE" routes/profiles.py` → Enum-Whitelist aktiv
5. Browser-Smoke: Profile-Editor laedt, 6 neue Felder sichtbar, Modal oeffnet + schliesst
6. Save-Roundtrip: Felder werden nach Reload nicht verloren (Pitfall 1 Regression-Check)
7. Claudian-Review: alle Tooltip-Content-Strings bestanden D-20 Anti-Pattern-Check (Human-Approval)
</verification>

<success_criteria>
- [ ] templates/_tooltip.html Macro exportiert tip3(was_rein, beispiel, nicht_verwechseln)
- [ ] templates/_beispiel_profil_modal.html enthaelt 6-8 Sektionen mit ausschliesslich fiktiven Inhalten
- [ ] static/nerve.css hat neue Blocks .tip-icon (>=16px), #g-tip + .tip-block, .beispiel-overlay, .beispiel-link
- [ ] templates/profile_editor.html: 6 Felder (branche Select, branche_kontext Textarea, eigene_formulierungen Textarea, beweise Textarea, ton Select+Flex, zusatz Label-Update) live
- [ ] templates/profile_editor.html: Modal-Link + Modal-Include integriert
- [ ] buildAndSubmit() schreibt alle 4 neuen JSON-Keys, populate-Handler liest sie
- [ ] routes/profiles.py VALID_BRANCHE-Set validiert Submit
- [ ] 3-Block-Display-Handler im Tooltip-JS unterstuetzt BEIDE Modi (alt data-tip + neu data-tip-was/bsp/nvm) ohne Legacy-Tooltips zu brechen
- [ ] D-20 Anti-Pattern-Compliance durch Human-Checkpoint bestaetigt
- [ ] Save-Roundtrip verifiziert (Felder bleiben nach Reload befuellt)
- [ ] Umlaut-Regel: User-facing Text (Labels, Tooltips, Modal) mit echten Umlauten, Code-Identifier ASCII
</success_criteria>

<output>
Summary: `.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-04-SUMMARY.md`

Muss dokumentieren:
- Exakte Tooltip-Content-Count (wie viele tip3-Aufrufe = Count aus grep)
- Screenshots-URLs oder Paths falls User Screenshots beim Checkpoint angehaengt hat
- Claudian-Review-Findings: Welche Content-Strings mussten geaendert werden?
- Offene Frage: Sollen die alten 1-Satz-Tooltips (via ti()-Helper) auf 3-Block migriert werden, oder bleibt Legacy-Fallback aktiv?
- Regression-Check: Hat die Populate-Handler-Ergaenzung bestehende Felder (produktbeschreibung, usps, etc.) unveraendert gelassen?
</output>
</content>
</invoke>