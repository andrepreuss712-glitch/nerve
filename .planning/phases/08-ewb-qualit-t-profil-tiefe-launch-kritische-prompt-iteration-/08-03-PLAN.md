---
phase: 08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - services/claude_service.py
  - scripts/migrate_branche_to_enum.py
  - .env.example
  - deploy/nerve.service
  - tests/test_branche_migration.py
autonomous: true
requirements:
  - EWB-05
  - EWB-09
  - EWB-13
  - EWB-17
addressed_decisions: [D-09, D-24, D-25]

must_haves:
  truths:
    - "services/claude_service.py analysiere_mit_claude() und analysiere_mit_claude_streaming() nutzen resolve_prompt_version + build_ewb_prompt statt _build_system_prompt() fuer das 'ewb'-Modul"
    - "Der neue Resolver-Cache aus prompt_pipeline.py ist aktiv; der Legacy-Cache _ACTIVE_PROMPT_CACHE bleibt fuer die 4 anderen Module unberuehrt"
    - "scripts/migrate_branche_to_enum.py --dry-run zeigt Heuristik-Mapping fuer existierende Profile, --run persistiert in DB"
    - "Freitext-Werte die nicht Enum-matchen landen in Enum='sonstiges' UND Originaltext wird in daten.basis.branche_kontext gespeichert (kein Daten-Verlust)"
    - ".env.example dokumentiert PROMPT_EWB_VERSION_OVERRIDE Kommentar"
    - "deploy/nerve.service hat Kommentar-Hinweis auf ENV-Override"
    - "test_branche_migration.py deckt Heuristik-Mapping + sonstiges-Fallback ab"
  artifacts:
    - path: "services/claude_service.py"
      provides: "EWB-Integration — analysiere_mit_claude + analysiere_mit_claude_streaming nutzen Phase 08 Pipeline"
      contains: "from services.ewb_pipeline import build_ewb_prompt"
    - path: "scripts/migrate_branche_to_enum.py"
      provides: "Standalone-Skript mit --dry-run und --run Modus fuer Heuristik-Migration"
      min_lines: 100
    - path: ".env.example"
      provides: "ENV-Doku Phase 08"
      contains: "PROMPT_EWB_VERSION_OVERRIDE"
    - path: "deploy/nerve.service"
      provides: "Phase 08 ENV-Kommentar"
      contains: "Phase 08"
    - path: "tests/test_branche_migration.py"
      provides: "Heuristik-Mapping + Fallback-Tests"
      min_lines: 80
  key_links:
    - from: "services/claude_service.py analysiere_mit_claude"
      to: "services.ewb_pipeline.build_ewb_prompt"
      via: "system=build_ewb_prompt(anrede=..., version=ewb_version, user_id=user_id)"
      pattern: "system=build_ewb_prompt"
    - from: "services/claude_service.py"
      to: "services.prompt_pipeline.resolve_prompt_version"
      via: "ewb_version = resolve_prompt_version('ewb', user_id)"
      pattern: "resolve_prompt_version\\('ewb'"
    - from: "scripts/migrate_branche_to_enum.py"
      to: "profiles table + profiles.daten JSON"
      via: "UPDATE profiles SET branche=<enum>; merge Originaltext in daten.basis.branche_kontext"
      pattern: "UPDATE profiles SET branche"
---

<objective>
Wave 3 Integration: claude_service.py Live-EWB-Pfad umstellen auf Phase 08 Pipeline + branche-Heuristik-Migration als Standalone-Skript + ENV-Override Dokumentation. Dieser Plan ist der "Hot-Swap"-Moment: Nach Deploy routet der Live-EWB-Call ueber prompt_pipeline+ewb_pipeline statt ueber _build_system_prompt.

Purpose: Ohne diese Integration bleibt Plan 02 isoliert Infrastruktur ohne Effekt. Die 4 anderen Module (assistant_live, coaching_live, objection_trigger, api_frage, training_persona) bleiben bewusst auf dem Legacy-Pfad (siehe RESEARCH Open Question 2 Empfehlung: Scope minimieren). Branche-Migration separiert als Skript weil einmalig ausgefuehrt und Dev-Inspektion erfordert.

Output:
- 2 Call-Sites in claude_service.py umgestellt (analysiere_mit_claude + analysiere_mit_claude_streaming)
- 1 Standalone-Migrations-Skript mit dry-run/run
- 2 Config-Dateien mit Phase-08-ENV-Doku
- 1 neuer Test-File mit Heuristik-Tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-CONTEXT.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-RESEARCH.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-PATTERNS.md
@services/prompt_pipeline.py
@services/ewb_pipeline.py
@services/claude_service.py
@database/models.py
@scripts/
@.env.example
@deploy/nerve.service
@tests/test_einwand_keyword_matcher.py
@CLAUDE.md

<interfaces>
**Bestehende call-sites in services/claude_service.py (aus RESEARCH Focus Area 5):**

analysiere_mit_claude (line 633-661):
```python
msg = claude_client.messages.create(
    model="claude-haiku-4-5-20251001",
    max_tokens=400,
    system=_build_system_prompt(),  # ← ersetzen durch build_ewb_prompt(...)
    messages=[{"role": "user", "content": user_msg}]
)
```

analysiere_mit_claude_streaming (line 664-744):
```python
# aehnlicher Call mit stream=True
# system=_build_system_prompt() → wird durch build_ewb_prompt(...) ersetzt
```

**Phase 08 Pipeline Kontrakte (aus Plan 02):**
```python
from services.prompt_pipeline import resolve_prompt_version
from services.ewb_pipeline import build_ewb_prompt
import services.live_session as ls

user_id = ls.state.get('user_id') or 0
ewb_version = resolve_prompt_version('ewb', user_id)
system_prompt = build_ewb_prompt(
    profile_data=None,  # wird von build_profile_context selbst geladen
    anrede=ls.state.get('session_anrede') or 'Sie',
    version=ewb_version,
    user_id=user_id,
)
```

**Heuristik-Map aus RESEARCH Focus Area 7 (Zeilen 447-475):**
| Enum | Keywords |
|------|----------|
| saas_b2b | saas, software, b2b, cloud, platform, api |
| maschinenbau | maschinenbau, industrie, produktion, fertigung, engineering, anlagenbau |
| versicherung | versicherung, assekuranz, policen |
| finanzprodukte | finanz, investment, anlage, kapital, bank, fonds |
| immobilien | immobilien, makler (wenn nicht mit 'versicher' kombiniert), bau, grundstuck, wohnung |
| coaching | coaching, coach, training, mentor |
| beratung | beratung, consulting, berater, consultant |
| sonstiges | FALLBACK — Originaltext in daten.basis.branche_kontext |

**Match-Priority (wichtig bei mehrdeutigen Strings):**
1. saas_b2b vor beratung ("IT-Beratung" → beratung ist OK wenn kein saas-Keyword)
2. finanzprodukte vor beratung ("Finanzberatung" → finanzprodukte)
3. versicherung vor finanzprodukte (Versicherung spezifischer)
4. Erster Match in obiger Tabellenreihenfolge gewinnt

**Umlaut-Normalisierung (fuer Match):** explizite Map ä→ae, ö→oe, ü→ue, ß→ss, dann lowercase strip.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/claude_service.py — EWB-Pfad auf Phase 08 Pipeline umstellen</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py Zeilen 1-30 (Imports), Zeilen 95-120 (_ACTIVE_PROMPT_CACHE + get_active_prompt_version — bleibt unangefasst fuer 4 andere Module), Zeilen 258-394 (_build_system_prompt — NICHT LOESCHEN, bleibt fuer Non-EWB-Module), Zeilen 630-750 (analysiere_mit_claude + analysiere_mit_claude_streaming)
    - services/prompt_pipeline.py (aus Plan 02) — Interface resolve_prompt_version
    - services/ewb_pipeline.py (aus Plan 02) — Interface build_ewb_prompt
    - services/live_session.py (state-Struktur, state_lock)
    - **W-6 user_id-Wiring (KRITISCH, bereits vorhanden):** services/deepgram_service.py Zeile 310-343 — `handle_start_live_session` setzt in Zeile 338 bereits `ls.state['user_id'] = user_id` (innerhalb des FT-Logging-Blocks). Dieser Key wird beim Call-Start aus `flask_session.get('user_id')` ueber `SessionLocal().query(User).first()` geladen. Plan 03 kann sich darauf verlassen. **Keine zusaetzliche Wiring-Aufgabe noetig.** Falls W-6-Check in der Zukunft fehlschlaegt (z.B. deepgram_service.py refactored): Fallback-Logik in Edit 2 unten schreibt bei `user_id=0` eine Warning-Log und faellt auf `variants[0]` zurueck (= v1-legacy, sicherer Default).
    - 08-RESEARCH.md Focus Area 5 "Router-Integration Caching" + Pitfall 3 "A/B-Cache Single-Variant-Sticky"
    - 08-PATTERNS.md §"services/claude_service.py (MODIFY — 3 integration points)" Zeilen 697-728
    - 08-CONTEXT.md Code-Anker Zeile 164 (`analysiere_mit_claude` line 633, Streaming-Variante line 664)
  </read_first>
  <behavior>
    - Test 1: Nach Integration laeuft Unit-Test der bestehenden EWB-Tests noch (keine Regression im Call-Interface)
    - Test 2: build_ewb_prompt wird aufgerufen mit user_id aus ls.state.get('user_id')
    - Test 3: ewb_version wird ueber resolve_prompt_version('ewb', user_id) aufgeloest
    - Test 4: Der Legacy-Cache _ACTIVE_PROMPT_CACHE wird fuer EWB NICHT mehr geschrieben (optional Check — nicht strikt aber praeferiert)
    - Test 5: analysiere_mit_claude_streaming nutzt das neue Pattern analog
    - Test 6: Wenn user_id=0 (anonymous/test): Pipeline wirft keine Exception
    - Test 7: Import-Smoke: `from services.claude_service import analysiere_mit_claude` funktioniert ohne DB-Hit beim Import
  </behavior>
  <action>
    Aendere services/claude_service.py an 2 konkreten Call-Sites. **Kein Loeschen** bestehenden Codes — nur Swap der `system=`-Argumente.

    **Edit 1 — Imports am Datei-Anfang (nach bestehenden Imports, typ. Zeile ~15):**
    Ergaenze:
    ```python
    # Phase 08: EWB-Pipeline (A/B-Routing + Baustein-Struktur)
    from services.prompt_pipeline import resolve_prompt_version
    from services.ewb_pipeline import build_ewb_prompt
    ```

    **Edit 2 — analysiere_mit_claude (Zeile ~633-661):**
    Lokalisiere die Stelle wo `system=_build_system_prompt()` aufgerufen wird. Ersetze den `system=`-Parameter durch:
    ```python
    # Phase 08: EWB-Pipeline statt Legacy _build_system_prompt
    import services.live_session as ls
    _user_id = (ls.state.get('user_id') if hasattr(ls, 'state') else None) or 0
    if not _user_id:
        # W-6 Safety-Net: user_id sollte von deepgram_service.handle_start_live_session
        # Zeile 338 gesetzt sein. Falls leer: Warning + Fallback auf variants[0] (= v1-legacy
        # sicherer Default, keine A/B-Zuweisung). Verhindert dass A/B-Router in degraded state
        # alle Anfragen auf dieselbe Variante routet.
        print("[Phase08] WARN: ls.state['user_id'] leer — faellt auf v1-legacy zurueck")
    _ewb_version = resolve_prompt_version('ewb', _user_id)
    _anrede = (ls.state.get('session_anrede') if hasattr(ls, 'state') else None) or 'Sie'
    _system_prompt = build_ewb_prompt(
        profile_data=None,
        anrede=_anrede,
        version=_ewb_version,
        user_id=_user_id,
    )
    msg = claude_client.messages.create(
        model="claude-haiku-4-5-20251001",
        max_tokens=400,
        system=_system_prompt,
        messages=[{"role": "user", "content": user_msg}]
    )
    ```
    WICHTIG: Alle anderen Parameter des messages.create-Calls (model, max_tokens, messages) bleiben identisch.

    **Edit 3 — analysiere_mit_claude_streaming (Zeile ~664-744):**
    Exakt dasselbe Pattern in der Streaming-Variante. Suche nach dem `system=_build_system_prompt()`-Aufruf innerhalb dieser Funktion und swap mit obiger Struktur (Zeilen `_user_id`, `_ewb_version`, `_anrede`, `_system_prompt` koennen am Funktionsanfang stehen, `system=_system_prompt` im Streaming-Call).

    **Edit 4 — Kommentar-Marker fuer Grep-Auffindbarkeit (direkt vor Edit 2 und Edit 3):**
    ```python
    # ── Phase 08 EWB-Pipeline Integration ──────────────────────────────────────
    ```

    WICHTIG:
    - `_build_system_prompt()` BLEIBT in der Datei (wird von anderen Modulen wie Lernkarten-Matching oder anderen Flows genutzt — RESEARCH Open Question 2).
    - `get_active_prompt_version()` (Zeile 100-115) bleibt UNANGEFASST — 4 andere Module (assistant_live, coaching_live, objection_trigger, api_frage, training_persona) nutzen es weiterhin.
    - `_ACTIVE_PROMPT_CACHE` (Zeile 97) bleibt. Es wird nur fuer `ewb` nicht mehr konsultiert (neue prompt_pipeline hat eigenen Cache).
    - try/except um die Pipeline-Aufrufe ist NICHT noetig — build_ewb_prompt hat eigenen Fallback. Wenn die neue Pipeline unerwartet crasht, crasht der EWB-Call — das ist gewollt (kein silent Fallback auf altes _build_system_prompt, weil das inkonsistente A/B-Zuordnung verursachen wuerde).

    Sonnet-Regel (CLAUDE.md): model bleibt "claude-haiku-4-5-20251001" (Haiku, nicht Sonnet — Live-Loop-Constraint!). Diesen String NICHT aendern.

    Umlaut-Regel: Python-Code-Identifier (_ewb_version, _user_id, etc.) ASCII. Kommentare duerfen Umlaute.

    **WICHTIG fuer die Behavior-Tests 1-7:** Dieser Task modifiziert bestehenden Code ohne neue Unit-Tests zu erstellen — die Pipeline-Tests aus Plan 02 decken bereits build_ewb_prompt + resolve_prompt_version ab. Der Import-Smoke-Test ist die kritische Regression-Pruefung.
  </action>
  <verify>
    <automated>python -c "from services.claude_service import analysiere_mit_claude, analysiere_mit_claude_streaming; print('OK: imports valid')" && python -c "import services.claude_service as cs; assert hasattr(cs, '_build_system_prompt'), '_build_system_prompt should still exist for legacy modules'; assert hasattr(cs, 'get_active_prompt_version'), 'get_active_prompt_version should still exist'; print('OK: legacy symbols preserved')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from services.prompt_pipeline import resolve_prompt_version" services/claude_service.py` → exit 0
    - `grep -n "from services.ewb_pipeline import build_ewb_prompt" services/claude_service.py` → exit 0
    - `grep -n "Phase 08 EWB-Pipeline Integration" services/claude_service.py` → exit 0 (Marker)
    - `grep -nE "resolve_prompt_version\('ewb'" services/claude_service.py` → >= 2 matches (analysiere_mit_claude + analysiere_mit_claude_streaming)
    - `grep -n "system=_system_prompt" services/claude_service.py` → >= 2 matches
    - `grep -n "build_ewb_prompt" services/claude_service.py` → >= 2 matches
    - `grep -n "_ACTIVE_PROMPT_CACHE" services/claude_service.py` → >= 1 match (nicht geloescht)
    - `grep -n "def _build_system_prompt" services/claude_service.py` → exit 0 (nicht geloescht)
    - `grep -n "def get_active_prompt_version" services/claude_service.py` → exit 0 (nicht geloescht)
    - `grep -n "claude-haiku-4-5-20251001" services/claude_service.py` → >= 2 matches (Haiku-Model unveraendert)
    - `grep -n "claude-sonnet\|claude-opus" services/claude_service.py` → NO live-loop calls (Sonnet nur Post-Call OK, aber nicht in den 2 edited Call-Sites)
    - Import-Smoke: `python -c "from services.claude_service import analysiere_mit_claude"` → exit 0
    - Existing Test-Regression: `pytest tests/test_ft_write_hooks.py -x` → exit 0 (die Tests monkeypatchen _ACTIVE_PROMPT_CACHE; sollte trotz Neuer Pipeline weiter gruen bleiben)
  </acceptance_criteria>
  <done>analysiere_mit_claude und analysiere_mit_claude_streaming nutzen Phase 08 Pipeline via resolve_prompt_version + build_ewb_prompt. Legacy-Funktionen (_build_system_prompt, get_active_prompt_version) nicht geloescht. Import ohne Side-Effect, existing Tests gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/migrate_branche_to_enum.py + Tests (D-09 Heuristik-Migration)</name>
  <files>scripts/migrate_branche_to_enum.py, tests/test_branche_migration.py</files>
  <read_first>
    - database/models.py Zeile 121-140 (Profile Model — branche ist Top-Level String(200) Column, NICHT in daten JSON)
    - database/db.py (SessionLocal Pattern)
    - 08-RESEARCH.md Focus Area 7 "branche-Enum-Migration-Heuristik" Zeilen 436-493, inkl. Heuristik-Map Zeilen 447-456, Match-Priority Zeilen 458-462, Umlaut-Normalisierung Zeilen 465-478, Fallback-Rule Zeilen 491-492
    - 08-PATTERNS.md §"tests/test_branche_migration.py" Zeilen 468-502
    - tests/test_einwand_keyword_matcher.py Zeilen 1-60 (Analog: pure-function test mit dict-fixtures)
    - CLAUDE.md (DB-Spalten ASCII, User-facing-Text mit Umlauten — hier reine Code-Paths also ASCII)
    - 08-CONTEXT.md D-09, Concerns Zeile 205 "branche-Migration kann User-Daten zerstoeren wenn Freitext nicht sauber gemappt wird"
  </read_first>
  <behavior>
    - Test 1: _normalize_branche ersetzt ae/oe/ue/ss aus ä/ö/ü/ß, lowercased und trim
    - Test 2: _map_branche_to_enum('SaaS-Plattform B2B') == 'saas_b2b'
    - Test 3: _map_branche_to_enum('Werkzeugmaschinen') == 'maschinenbau'
    - Test 4: _map_branche_to_enum('Industrieversicherung') == 'versicherung' (NICHT finanzprodukte — versicherung-priority)
    - Test 5: _map_branche_to_enum('Finanzberatung') == 'finanzprodukte' (finanzprodukte vor beratung)
    - Test 6: _map_branche_to_enum('Immobilienmakler') == 'immobilien'
    - Test 7: _map_branche_to_enum('Performance-Coach') == 'coaching'
    - Test 8: _map_branche_to_enum('Exotisches Feld') == 'sonstiges'
    - Test 9: _map_branche_to_enum('') == 'sonstiges' (empty string Fallback)
    - Test 10: _migrate_profile_branche(profile_dict_with_freetext) setzt Enum-Wert UND merged Originaltext in daten.basis.branche_kontext
    - Test 11: Wenn daten.basis.branche_kontext schon belegt: Originaltext wird APPENDED mit Trennzeichen (kein Overwrite)
    - Test 12: Wenn Enum-Wert bereits gesetzt (idempotent): Skip ohne Aenderung
  </behavior>
  <action>
    **Erstelle Verzeichnis falls noetig:** `mkdir -p scripts` (falls nicht existiert)

    **Erstelle scripts/migrate_branche_to_enum.py** (Standalone CLI-Skript):

    ```python
    #!/usr/bin/env python3
    """
    scripts/migrate_branche_to_enum.py — Phase 08 D-09 Data-Migration.

    Ersetzt Freitext-Branche-Werte in profiles.branche durch Enum-Werte und
    konserviert den Originaltext in profiles.daten['basis']['branche_kontext'].

    Heuristik: Keyword-Match mit Prioritaets-Reihenfolge (siehe HEURISTIC_MAP).
    Nicht-matchende Werte → 'sonstiges' + Originaltext in branche_kontext.

    Usage:
      python scripts/migrate_branche_to_enum.py --dry-run   # zeigt nur Plan, schreibt nichts
      python scripts/migrate_branche_to_enum.py --run       # schreibt in DB

    Idempotent: Schon-Enum-Werte werden skipped (kein Doppel-Mapping).
    """
    from __future__ import annotations
    import argparse
    import json
    import sys


    VALID_ENUMS = {
        'saas_b2b', 'maschinenbau', 'versicherung', 'finanzprodukte',
        'immobilien', 'coaching', 'beratung', 'sonstiges',
    }


    # Reihenfolge = Prioritaet. Erster Match gewinnt.
    # Alle keyword-Strings sind bereits normalisiert (lowercase, ae/oe/ue/ss).
    HEURISTIC_MAP = [
        ('saas_b2b',       ['saas', 'b2b', 'software', 'cloud', 'platform', 'api']),
        ('versicherung',   ['versicher', 'assekuranz', 'policen']),  # VOR finanzprodukte
        ('finanzprodukte', ['finanz', 'investment', 'anlage', 'kapital', 'bank', 'fonds']),
        ('maschinenbau',   ['maschinenbau', 'industrie', 'produktion', 'fertigung',
                             'engineering', 'anlagenbau', 'werkzeugmaschin']),
        ('immobilien',     ['immobilien', 'makler', 'grundstuck', 'wohnung']),
        ('coaching',       ['coaching', 'coach', 'mentor']),
        ('beratung',       ['beratung', 'consulting', 'berater', 'consultant']),
    ]


    def _normalize_branche(s: str) -> str:
        """Umlaut-Ersatz + lowercase + strip. NICHT fuer Display — nur Match."""
        if not s:
            return ''
        return (s.lower()
                .replace('\u00e4', 'ae')
                .replace('\u00f6', 'oe')
                .replace('\u00fc', 'ue')
                .replace('\u00df', 'ss')
                .strip())


    def _map_branche_to_enum(freitext: str) -> str:
        """Heuristik-Mapping Freitext → Enum. Fallback 'sonstiges'."""
        if not freitext:
            return 'sonstiges'
        if freitext in VALID_ENUMS:
            return freitext  # Idempotent — schon Enum
        norm = _normalize_branche(freitext)
        for enum, keywords in HEURISTIC_MAP:
            for kw in keywords:
                if kw in norm:
                    return enum
        return 'sonstiges'


    def _migrate_profile_branche(profile_id: int, original: str, daten: dict) -> tuple[str, dict]:
        """Liefert (enum_value, updated_daten). Merged Originaltext in daten.basis.branche_kontext.

        - Wenn schon Enum → (original, daten unveraendert), idempotent.
        - Sonst: (mapped_enum, daten mit branche_kontext-Merge)
        - Wenn branche_kontext bereits belegt: APPENDED mit ' | ' Separator.
        """
        if original in VALID_ENUMS:
            return (original, daten)

        new_enum = _map_branche_to_enum(original)
        basis = daten.get('basis') or {}
        existing = basis.get('branche_kontext', '').strip() if isinstance(basis.get('branche_kontext'), str) else ''
        if existing:
            basis['branche_kontext'] = f'{existing} | {original}'.strip()
        else:
            basis['branche_kontext'] = original.strip()
        daten['basis'] = basis
        return (new_enum, daten)


    def _run(dry_run: bool = True) -> int:
        """Execute migration. Returns count of profiles migrated (or 'to-migrate' in dry-run)."""
        try:
            from database.db import SessionLocal
            from database.models import Profile
        except ImportError as e:
            print(f"[MIGRATE] Import failed (run from repo root): {e}", file=sys.stderr)
            return -1

        db = SessionLocal()
        count = 0
        try:
            profiles = db.query(Profile).all()
            print(f"[MIGRATE] scanning {len(profiles)} profiles ...")
            for p in profiles:
                original = (p.branche or '').strip()
                if not original:
                    continue
                if original in VALID_ENUMS:
                    continue  # Already migrated
                try:
                    daten = json.loads(p.daten) if p.daten else {}
                except Exception:
                    daten = {}
                new_enum, new_daten = _migrate_profile_branche(p.id, original, daten)
                print(f"[MIGRATE] profile_id={p.id} "
                      f"'{original[:40]}' → enum='{new_enum}', "
                      f"kontext_preserved='{new_daten.get('basis', {}).get('branche_kontext', '')[:40]}'")
                if not dry_run:
                    p.branche = new_enum
                    p.daten = json.dumps(new_daten, ensure_ascii=False)
                count += 1
            if not dry_run:
                db.commit()
                print(f"[MIGRATE] committed {count} profile updates")
            else:
                print(f"[MIGRATE] DRY-RUN — would migrate {count} profiles")
        finally:
            db.close()
        return count


    def _main():
        parser = argparse.ArgumentParser()
        parser.add_argument('--dry-run', action='store_true', help='Preview without writing')
        parser.add_argument('--run', action='store_true', help='Execute migration')
        args = parser.parse_args()
        if not args.dry_run and not args.run:
            parser.error('Specify --dry-run or --run')
        _run(dry_run=args.dry_run)


    if __name__ == '__main__':
        _main()
    ```

    **Erstelle tests/test_branche_migration.py** mit 12 Tests:

    ```python
    """Phase 08 tests for scripts/migrate_branche_to_enum.py heuristic + fallback."""
    import pytest
    import sys, os
    sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

    from scripts.migrate_branche_to_enum import (
        _normalize_branche, _map_branche_to_enum, _migrate_profile_branche,
        VALID_ENUMS, HEURISTIC_MAP,
    )


    def test_normalize_umlaute():
        assert _normalize_branche('Grundstueck') == 'grundstueck'
        # Unicode ae/oe/ue/ss:
        assert _normalize_branche('Grundst\u00fcck') == 'grundstueck'
        assert _normalize_branche('F\u00fcr Unternehmen') == 'fuer unternehmen'
        assert _normalize_branche('  TRIM  ') == 'trim'


    def test_normalize_empty():
        assert _normalize_branche('') == ''
        assert _normalize_branche(None) == ''


    def test_heuristic_saas_b2b():
        assert _map_branche_to_enum('SaaS-Plattform B2B') == 'saas_b2b'
        assert _map_branche_to_enum('cloud software') == 'saas_b2b'


    def test_heuristic_maschinenbau():
        assert _map_branche_to_enum('Werkzeugmaschinen') == 'maschinenbau'
        assert _map_branche_to_enum('Maschinenbau Mittelstand') == 'maschinenbau'
        assert _map_branche_to_enum('Anlagenbau') == 'maschinenbau'


    def test_heuristic_versicherung_vor_finanz():
        """'Industrieversicherung' darf nicht als finanzprodukte klassifiziert werden."""
        assert _map_branche_to_enum('Industrieversicherung') == 'versicherung'
        assert _map_branche_to_enum('Versicherungsmakler') == 'versicherung'


    def test_heuristic_finanzberatung_prio():
        """'Finanzberatung' ist finanzprodukte (nicht beratung)."""
        assert _map_branche_to_enum('Finanzberatung') == 'finanzprodukte'


    def test_heuristic_immobilien():
        assert _map_branche_to_enum('Immobilienmakler') == 'immobilien'
        assert _map_branche_to_enum('Grundstueckshandel') == 'immobilien'


    def test_heuristic_coaching():
        assert _map_branche_to_enum('Performance-Coach') == 'coaching'
        assert _map_branche_to_enum('Coaching') == 'coaching'


    def test_heuristic_beratung_ohne_finanz():
        """Reine 'Beratung' ohne finanz-Keyword → beratung."""
        assert _map_branche_to_enum('Unternehmensberatung') == 'beratung'


    def test_heuristic_fallback_sonstiges():
        assert _map_branche_to_enum('Exotisches Feld') == 'sonstiges'
        assert _map_branche_to_enum('') == 'sonstiges'
        assert _map_branche_to_enum(None) == 'sonstiges'


    def test_idempotent_already_enum():
        """Wenn branche bereits Enum-Wert: kein Remap."""
        assert _map_branche_to_enum('saas_b2b') == 'saas_b2b'
        assert _map_branche_to_enum('maschinenbau') == 'maschinenbau'


    def test_migrate_preserves_originaltext_in_branche_kontext():
        daten = {'basis': {}}
        enum_val, out = _migrate_profile_branche(1, 'Exotisches Feld', daten)
        assert enum_val == 'sonstiges'
        assert out['basis']['branche_kontext'] == 'Exotisches Feld'


    def test_migrate_appends_to_existing_branche_kontext():
        """Wenn branche_kontext schon belegt: append mit ' | ' separator, kein Overwrite."""
        daten = {'basis': {'branche_kontext': 'Bestehender Text'}}
        enum_val, out = _migrate_profile_branche(1, 'Neuer Freitext', daten)
        assert 'Bestehender Text' in out['basis']['branche_kontext']
        assert 'Neuer Freitext' in out['basis']['branche_kontext']
        assert '|' in out['basis']['branche_kontext']


    def test_migrate_skips_if_already_enum():
        daten = {'basis': {'branche_kontext': 'Some context'}}
        enum_val, out = _migrate_profile_branche(1, 'saas_b2b', daten)
        assert enum_val == 'saas_b2b'
        # daten unveraendert
        assert out['basis']['branche_kontext'] == 'Some context'
    ```

    Umlaut-Regel: Python-Code-Identifier ASCII (`_map_branche_to_enum`, nicht `_branche_zu_enum`). String-Literals in HEURISTIC_MAP sind normalisierte Keywords (ae/oe/ue/ss) — Absicht ist Match-Robustheit, nicht User-Display.
  </action>
  <verify>
    <automated>test -f scripts/migrate_branche_to_enum.py && python -c "from scripts.migrate_branche_to_enum import _normalize_branche, _map_branche_to_enum, _migrate_profile_branche, VALID_ENUMS, HEURISTIC_MAP; print('OK: imports valid')" && pytest tests/test_branche_migration.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/migrate_branche_to_enum.py` → exit 0
    - `wc -l scripts/migrate_branche_to_enum.py` → >= 100
    - `grep -nE "^def (_normalize_branche|_map_branche_to_enum|_migrate_profile_branche|_run|_main)" scripts/migrate_branche_to_enum.py` → 5 matches
    - `grep -n "VALID_ENUMS" scripts/migrate_branche_to_enum.py` → exit 0
    - `grep -n "HEURISTIC_MAP" scripts/migrate_branche_to_enum.py` → exit 0
    - `grep -n "dry.run\|--run" scripts/migrate_branche_to_enum.py` → >= 2 matches (CLI-Flags)
    - `grep -c "^def test_" tests/test_branche_migration.py` → >= 12
    - `pytest tests/test_branche_migration.py -x -v` → exit 0 (alle 12+ Tests)
    - Dry-Run-Test: `python scripts/migrate_branche_to_enum.py --dry-run` → Exit 0 (auch wenn keine Profile existieren)
    - Standalone-Import: `python -c "from scripts.migrate_branche_to_enum import _map_branche_to_enum; assert _map_branche_to_enum('SaaS') == 'saas_b2b'"` → exit 0
  </acceptance_criteria>
  <done>scripts/migrate_branche_to_enum.py existiert mit CLI (dry-run/run), 3 pure Hilfsfunktionen + 2 Orchestrator. tests/test_branche_migration.py deckt alle 8 Enums + Fallback + Append-Logic ab. Idempotenz verifiziert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: .env.example + deploy/nerve.service — ENV-Override Dokumentation (D-25)</name>
  <files>.env.example, deploy/nerve.service</files>
  <read_first>
    - .env.example (existing pattern Zeilen 1-25, Phase-04.7 ENV-Doku-Style)
    - deploy/nerve.service (systemd-Unit, EnvironmentFile-Konfiguration Zeile ~18)
    - 08-RESEARCH.md Assumptions Log A5 Zeile 950 ("André schreibt ENV-Vars in /etc/systemd/system/nerve.service")
    - 08-PATTERNS.md §".env.example + deploy/nerve.service" Zeilen 1084-1114
    - 08-CONTEXT.md D-24, D-25
  </read_first>
  <behavior>
    - Test 1: .env.example hat PROMPT_EWB_VERSION_OVERRIDE= (leer als Default)
    - Test 2: .env.example hat erklärenden Kommentar mit Use-Cases (leer=A/B aktiv, gesetzt=forciert)
    - Test 3: deploy/nerve.service hat Kommentar-Zeile die auf Phase 08 verweist (ohne ENV-Wert in der Unit selbst)
    - Test 4: Keine sensiblen Defaults (kein API-Key oder Secret Literal)
  </behavior>
  <action>
    **Edit 1 — .env.example:**

    Fuege am Ende der Datei (nach letztem bestehendem Block) diesen Block an:

    ```
    # --- Phase 08: EWB A/B Prompt-Routing (optional) ---
    # Leer = A/B-Routing aktiv (deterministisch user_id % len(active_variants) fuer module='ewb').
    # Gesetzt = forciert ALLE User auf die angegebene Variante (Safety-Net).
    # Use-Cases:
    #   - Emergency-Rollback ohne Code-Deploy: PROMPT_EWB_VERSION_OVERRIDE=v1-legacy
    #   - Saubere UAT vor Launch: PROMPT_EWB_VERSION_OVERRIDE=v2-modular
    #   - Debug-Support fuer Solo-User (Andre): manuell zwischen v1/v2 umschalten
    # Format: Version-String analog prompt_versions.version (z.B. 'v1-legacy', 'v2-modular').
    # Unbekannter Wert: resolve_prompt_version gibt den String 1:1 zurueck; ewb_pipeline
    # faellt via _load_prompt_template auf _FALLBACK_V1_PROMPT zurueck (kein Crash).
    PROMPT_EWB_VERSION_OVERRIDE=
    ```

    **Edit 2 — deploy/nerve.service:**

    Finde die bestehende `EnvironmentFile=/etc/nerve/.env` Zeile (typisch um Zeile 18). Ergaenze KURZ VOR dieser Zeile einen Kommentar (nur Hint, Wert kommt aus .env selbst):

    ```ini
    # Phase 08: Optional PROMPT_EWB_VERSION_OVERRIDE fuer A/B-Override (siehe .env.example)
    # Leer = A/B-Routing aktiv; gesetzt = forciert alle User auf angegebene Variante.
    EnvironmentFile=/etc/nerve/.env
    ```

    Wenn `EnvironmentFile=/etc/nerve/.env` bereits eine Kommentar-Zeile davor hat, den neuen Kommentar direkt darunter oder direkt davor einfuegen (nicht doppeln).

    WICHTIG:
    - KEINE `Environment=PROMPT_EWB_VERSION_OVERRIDE=...` Zeile in nerve.service direkt — der Wert wird aus /etc/nerve/.env gelesen und soll NICHT ins Repo committed werden.
    - Falls deploy/nerve.service nicht existiert: Task skipping mit Log-Meldung (es ist ein VPS-Deployment-File, kann in manchen Repo-States abwesend sein). Dann nur .env.example bearbeiten.

    Umlaut-Regel: Kommentare duerfen Umlaute haben. Im ENV-Var-Namen (PROMPT_EWB_VERSION_OVERRIDE) strict ASCII.
  </action>
  <verify>
    <automated>grep -n "PROMPT_EWB_VERSION_OVERRIDE" .env.example && grep -n "Phase 08" .env.example && grep -q "^PROMPT_EWB_VERSION_OVERRIDE=" .env.example && echo "OK env example" && { if [ -f deploy/nerve.service ]; then grep -q "Phase 08" deploy/nerve.service && echo "OK deploy nerve.service"; else echo "OK: deploy/nerve.service absent — skipped"; fi; }</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PROMPT_EWB_VERSION_OVERRIDE" .env.example` → exit 0
    - `grep -n "^PROMPT_EWB_VERSION_OVERRIDE=$" .env.example` → exit 0 (leer, kein Default-Wert)
    - `grep -n "Phase 08" .env.example` → exit 0 (Header-Marker)
    - `grep -n "Emergency-Rollback\|A/B-Routing" .env.example` → exit 0 (Use-Cases dokumentiert)
    - Wenn `test -f deploy/nerve.service`: `grep -n "Phase 08" deploy/nerve.service` → exit 0
    - Wenn `test -f deploy/nerve.service`: `grep -n "^Environment=PROMPT_EWB_VERSION_OVERRIDE" deploy/nerve.service` → exit 1 (Wert NICHT in Unit-File committed — kommt aus EnvironmentFile)
    - Keine neuen Secrets oder API-Keys in .env.example (spot-check: `grep -nE "=.{10,}" .env.example | grep -v "^#" | grep -iE "(key|secret|password|token)=[A-Za-z0-9]{10,}"` → NO matches mit echten Werten)
  </acceptance_criteria>
  <done>.env.example dokumentiert PROMPT_EWB_VERSION_OVERRIDE mit leer-default und Use-Cases. deploy/nerve.service hat Phase-08-Kommentar-Hint (oder wurde begruendet skipped wenn File fehlt). Keine Secret-Leaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live-Loop ↔ EWB-Pipeline | Jeder Haiku-EWB-Call durchlaeuft Plan-08-Pipeline. Crash hier = Live-Feature kaputt. |
| migrate_branche_to_enum.py ↔ profiles table | Standalone-Skript mit DESTRUKTIVEM UPDATE auf profiles.branche + profiles.daten. Nur manuell ausgefuehrt. |
| .env ↔ Prozess | PROMPT_EWB_VERSION_OVERRIDE ist Ops-Control, nicht User-Request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Denial | ewb_pipeline-Crash im Live-Loop → EWB-Feature tot | mitigate | build_ewb_prompt hat eingebauten Fallback auf _FALLBACK_V1_PROMPT (Plan 02). resolve_prompt_version faellt auf 'unknown' bei DB-Error. Pipeline laedt lazy — kein Import-Crash. Plus: claude_service.py-Call wird durch bestehende try/except umrahmt (service-layer-pattern). |
| T-08-03-02 | Tampering | migrate_branche_to_enum.py schreibt falsche Heuristik (datenverlustend) | mitigate | `--dry-run` Default-Mode, explizites `--run` noetig. Originaltext IMMER in daten.basis.branche_kontext konserviert (appended wenn schon belegt). Tests decken 12 Scenarios. Plus: Plan 01 Backup-File database/nerve.db.bak_pre_v08_01 ist Rollback-Path. |
| T-08-03-03 | Info Disclosure | ENV-Wert PROMPT_EWB_VERSION_OVERRIDE in Log-Output | accept | Variable ist Version-String, kein Secret. Plan 02 logged expliziten `[Pipeline] cache hit` statt raw Value (siehe T-08-02-01). .env.example dokumentiert NICHT-sensiblen Wert. |
| T-08-03-04 | Tampering | User-controlled string in profiles.branche nach Migration | mitigate | Nach Migration ist Wert garantiert aus VALID_ENUMS (Whitelist). Plan 04 Profile-Editor-POST-Handler (Plan 04) reinforced die Whitelist auf Submit-Pfad. |
| T-08-03-05 | Integrity | Concurrent migrate_branche + Profile-Editor-Save Race | accept | Migration wird offline gefahren (deploy-time, nicht im Live-Traffic). Risk minimal. Plus: idempotent — zweiter Run is no-op auf bereits-Enum-Werten. |
</threat_model>

<verification>
Nach dem Plan muessen ALLE folgenden Befehle gruen sein:
1. `python -c "from services.claude_service import analysiere_mit_claude, analysiere_mit_claude_streaming"` — keine Import-Fehler
2. `pytest tests/test_branche_migration.py -x -v` — 12+ Tests gruen
3. `python scripts/migrate_branche_to_enum.py --dry-run` — laeuft ohne Crash
4. `grep -n "PROMPT_EWB_VERSION_OVERRIDE" .env.example` — exit 0
5. `pytest tests/test_prompt_pipeline.py tests/test_ewb_pipeline.py -x` — Plan-02-Tests bleiben gruen (Regression-Check)

Live-Smoke-Test (nach Deploy, nicht Teil des Plans):
- App starten → EWB-Button im PiP druecken → EWB-Response erscheint (Pipeline wird aktiv)
- Log-Check: `grep "\[EWB\] v" logs/gunicorn.log` → muss nach erstem EWB-Call einen Eintrag geben
- `grep "\[Pipeline\] variants loaded" logs/gunicorn.log` → erster Aufruf loadet Variants
</verification>

<success_criteria>
- [ ] services/claude_service.py: 2 Call-Sites swappen system= auf build_ewb_prompt (+ 2 Imports + 1 Kommentar-Marker)
- [ ] Legacy-Symbole _build_system_prompt + get_active_prompt_version + _ACTIVE_PROMPT_CACHE NICHT entfernt
- [ ] scripts/migrate_branche_to_enum.py Standalone-CLI mit dry-run/run
- [ ] tests/test_branche_migration.py mit >=12 Tests gruen
- [ ] .env.example dokumentiert PROMPT_EWB_VERSION_OVERRIDE mit Use-Cases
- [ ] deploy/nerve.service hat Phase-08-Kommentar-Hint (wenn File existiert)
- [ ] Kein Import-Side-Effect in Code-Changes
- [ ] Existing Tests (test_ft_write_hooks.py etc.) bleiben gruen (Regression)
- [ ] Model bleibt Haiku in beiden edited Call-Sites (Sonnet-Regel CLAUDE.md)
</success_criteria>

<output>
Summary: `.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-03-SUMMARY.md`

Muss dokumentieren:
- Zeilen-Ranges in claude_service.py wo Edits gemacht wurden
- Ob dry-run ueber echte DB ausgefuehrt wurde + Count der betroffenen Profile
- Falls deploy/nerve.service fehlte: Hinweis in Summary fuer Operator
- Offene Frage: Wurde Migration bereits mit --run ausgefuehrt, oder bleibt das fuer Deploy-Tag offen?
- Regression-Test-Ergebnis: test_ft_write_hooks.py (monkeypatchet alten Cache) muss nach dieser Aenderung noch gruen sein
</output>
</content>
</invoke>