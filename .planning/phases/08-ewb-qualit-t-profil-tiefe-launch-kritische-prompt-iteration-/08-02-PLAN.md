---
phase: 08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - services/prompt_pipeline.py
  - services/ewb_pipeline.py
  - app.py
  - tests/test_prompt_pipeline.py
  - tests/test_ewb_pipeline.py
autonomous: true
requirements:
  - EWB-09
  - EWB-10
  - EWB-11
  - EWB-12
  - EWB-13
  - EWB-14
  - EWB-18
  - EWB-19
addressed_decisions: [D-15, D-23, D-24, D-25, D-26, D-40, D-41, D-42, D-43, D-44, D-45, D-47]

must_haves:
  truths:
    - "services/prompt_pipeline.py exportiert resolve_prompt_version, build_profile_context, log_pipeline_event, invalidate_resolver_cache"
    - "resolve_prompt_version liest ENV-Override zuerst, dann deterministisches mod-Routing, Fallback 'unknown'"
    - "Cache-Key ist (module, user_id) — zwei user_ids landen auf verschiedenen Varianten wenn N>1"
    - "services/ewb_pipeline.py exportiert build_ewb_prompt(profile_data, anrede, version)"
    - "prompt_versions hat 2 Rows fuer module='ewb': v1-legacy (is_default=1), v2-modular (is_default=0), beide is_active=1"
    - "v2-modular Prompt-Text enthaelt D-15 wortwoertlich + D-47 Active-Listening-Block + Baustein-Struktur"
    - "Unit-Tests test_prompt_pipeline.py + test_ewb_pipeline.py gruen via pytest"
  artifacts:
    - path: "services/prompt_pipeline.py"
      provides: "Shared-Utils"
      exports: ["resolve_prompt_version", "build_profile_context", "log_pipeline_event", "invalidate_resolver_cache"]
      min_lines: 120
    - path: "services/ewb_pipeline.py"
      provides: "EWB-specific Assembly"
      exports: ["build_ewb_prompt"]
      min_lines: 60
    - path: "app.py"
      provides: "_seed_ewb_v2 Seed-Funktion + Call im Startup"
      contains: "_seed_ewb_v2"
    - path: "tests/test_prompt_pipeline.py"
      provides: "Unit-Tests Resolver + Profile-Context"
      min_lines: 100
    - path: "tests/test_ewb_pipeline.py"
      provides: "Integration-Test build_ewb_prompt + Seed"
      min_lines: 60
  key_links:
    - from: "services/prompt_pipeline.py"
      to: "prompt_versions table"
      via: "SessionLocal Query mit filter_by(module, is_active=True).order_by(version)"
      pattern: "PromptVersion.*is_active=True.*order_by"
    - from: "services/ewb_pipeline.py"
      to: "services/prompt_pipeline.py"
      via: "Import build_profile_context"
      pattern: "from services.prompt_pipeline import"
    - from: "app.py"
      to: "prompt_versions"
      via: "_seed_ewb_v2 mit idempotentem existing-row-check"
      pattern: "_seed_ewb_v2"
---

<objective>
Wave 2 Pipeline: Shared-Utils-Modul + EWB-spezifisches Assembly-Modul + v2-modular Prompt-Seed, vollstaendig unit-getestet. KEINE Integration in claude_service.py (das passiert in Plan 03). Dieser Plan liefert die Pipeline-Bausteine isoliert, inkl. Tests.

Purpose: Ohne prompt_pipeline.py kein A/B-Routing. Ohne ewb_pipeline.py und v2-Seed fehlt die neue Baustein-Struktur. Kritisch: Caching muss (module, user_id)-Key haben (Pitfall 3 aus RESEARCH) — sonst bricht A/B fuer multi-user.

Output:
- 2 neue Service-Module (side-effect-free beim Import)
- 2 neue Test-Files
- 1 Seed-Funktion + Call in app.py
- 2 neue Rows in prompt_versions nach App-Start
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-CONTEXT.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-RESEARCH.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-PATTERNS.md
@docs/phase-08-training-vs-live-prompt-gap.md
@services/claude_service.py
@app.py
@database/models.py
@tests/conftest.py
@tests/test_ft_seed.py
@tests/test_ft_write_hooks.py
@CLAUDE.md

<interfaces>
services/prompt_pipeline.py (NEW):
```python
def resolve_prompt_version(module: str, user_id: int) -> str:
    """Priority: ENV override > user_id % len(variants) > 'unknown'. Cache per (module, user_id)."""

def build_profile_context(user_id: int, mode: str = 'cold_call') -> str:
    """Standardized context string: Produkt, USPs, branche, branche_kontext,
    eigene_formulierungen, beweise, Anrede (Session > Profile > 'Sie')."""

def log_pipeline_event(event_type: str, module: str, data: dict) -> None:
    """Modul-agnostisches Logging. Swallows errors."""

def invalidate_resolver_cache() -> None:
    """Clear caches after prompt_versions change."""
```

services/ewb_pipeline.py (NEW):
```python
def build_ewb_prompt(profile_data: Optional[dict] = None,
                     anrede: str = 'Sie',
                     version: str = 'v1-legacy',
                     user_id: int = 0) -> str:
    """Assemble complete EWB system prompt. Uses build_profile_context + prompt_versions text."""
```

Existing (do NOT touch): _ACTIVE_PROMPT_CACHE (claude_service.py line 97) bleibt fuer 4 Legacy-Module.

Seed-Signature (analog _seed_prompt_versions at app.py line 601-644):
```python
def _seed_ewb_v2(db=None):
    """Idempotent seed: 2 rows for module='ewb' (v1-legacy is_default=1, v2-modular is_default=0)."""
```

Test Fixtures (tests/conftest.py lines 41-51):
- db_session: in-memory SQLite
- client: Flask test client
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/prompt_pipeline.py + Unit-Tests (Shared-Utils, D-40)</name>
  <files>services/prompt_pipeline.py, tests/test_prompt_pipeline.py</files>
  <read_first>
    - services/claude_service.py Zeilen 1-120 (Imports + _ACTIVE_PROMPT_CACHE + get_active_prompt_version als Analog)
    - services/claude_service.py Zeilen 258-394 (_build_system_prompt — Referenz für build_profile_context)
    - tests/conftest.py Zeilen 1-98 (db_session fixture)
    - tests/test_ft_write_hooks.py Zeilen 1-90 (monkeypatch-Pattern + Fake-Session)
    - tests/test_ft_seed.py Zeilen 1-35 (Seed-Assert-Pattern)
    - 08-RESEARCH.md Focus Area 5 (Router-Integration Caching) + Code Examples Zeilen 817-867
    - 08-PATTERNS.md §"services/prompt_pipeline.py" Zeilen 35-117
    - 08-CONTEXT.md D-23, D-24, D-40, D-43, D-45
    - database/models.py (PromptVersion, Profile Models fuer build_profile_context)
    - CLAUDE.md (Umlaut-Regel)
  </read_first>
  <behavior>
    - Test 1: ENV-Override `PROMPT_EWB_VERSION_OVERRIDE=foo` → returns 'foo', ignoriert DB
    - Test 2: 2 aktive Variants v1-legacy + v2-modular, user_id=0 → 'v1-legacy' (sortiert alphabetisch)
    - Test 3: user_id=1 → 'v2-modular'
    - Test 4: Cache-Per-User: user_id=0 und user_id=1 bekommen unterschiedliche Cache-Entries
    - Test 5: Empty prompt_versions → returns 'unknown'
    - Test 6: invalidate_resolver_cache() leert beide Caches
    - Test 7: build_profile_context liest eigene_formulierungen + beweise + branche_kontext
    - Test 8: build_profile_context Anrede-Resolution: session_override > profile > 'Sie'
    - Test 9: Fehlendes Profil → leerer String (kein Crash)
    - Test 10: log_pipeline_event schluckt Exception (MUST NOT raise)
  </behavior>
  <action>
    Erstelle services/prompt_pipeline.py mit 4 exportierten Funktionen, side-effect-free beim Import.

    **Modul-Header und Imports:**
    ```python
    """
    services/prompt_pipeline.py
    Phase 08 Shared Prompt-Pipeline-Utilities.
    """
    from __future__ import annotations
    import os
    from typing import Any
    ```

    **Modul-Level Caches:**
    ```python
    _RESOLVER_CACHE: dict = {}      # {(module, user_id): version_string}
    _VARIANTS_CACHE: dict = {}      # {module: [version_string, ...]}
    ```

    **resolve_prompt_version(module, user_id):** Implementierung exakt nach RESEARCH Code Examples Zeilen 817-867:
    - STEP 1: `env_key = f'PROMPT_{module.upper()}_VERSION_OVERRIDE'`; wenn `os.environ.get(env_key)` truthy → return direkt
    - STEP 2: cache_key = (module, user_id); wenn in _RESOLVER_CACHE → return cached
    - STEP 3: Wenn module nicht in _VARIANTS_CACHE: SessionLocal() → query PromptVersion filter_by(module, is_active=True).order_by(PromptVersion.version).all() → Liste der version-Strings, Fallback ['unknown']. try/finally db.close(). Exception → ['unknown'] + print `[Pipeline] variants load failed ...`
    - STEP 4: variants = _VARIANTS_CACHE[module]; resolved = variants[user_id % len(variants)]; _RESOLVER_CACHE[cache_key] = resolved; return resolved.

    **build_profile_context(user_id, mode='cold_call'):**
    - lines = []
    - try: `import services.live_session as ls; _, pdata = ls.get_active_profile()`; except: pdata = None
    - if not pdata: return '' (Aufrufer kombiniert mit Fallback)
    - basis = pdata.get('basis', {}) or {}; ki = pdata.get('ki', {}) or {}
    - Conditionals: Unternehmen, Produkt, USPs (siehe Focus Area 1 Zeilen 165-176 fuer genaue Format-Strings)
    - NEU Phase 08 D-09/D-11: Profil-Branche-Column (separate Query oder via ls.state['profile_id']) + basis.get('branche_kontext')
    - Phase 08 D-07: if basis.get('eigene_formulierungen'): lines.append header + list items als `- "..."`
    - Phase 08 D-08: if basis.get('beweise'): lines.append header + list items
    - Anrede-Resolution: ls.state.get('session_anrede') > ki.get('ansprache') > 'Sie' → lines.append(`Anrede: {anrede}. WICHTIG: Nutze konsequent {anrede}-Form. Wechsle NIEMALS innerhalb einer Antwort zwischen Du und Sie.`) — **D-15 wortwoertlich**
    - return '\n'.join(lines)

    **log_pipeline_event(event_type, module, data):**
    - try: from services.finetune_logging import log_ft_event; log_ft_event(phase=f'phase_08_{event_type}', model=data.get('model','haiku'), ...)
    - except Exception as e: print(f"[Pipeline] log_pipeline_event failed module={module}: {e}"); no raise

    **invalidate_resolver_cache():**
    - _RESOLVER_CACHE.clear(); _VARIANTS_CACHE.clear(); print("[Pipeline] resolver cache invalidated")

    WICHTIG:
    - Alle DB-Imports LAZY innerhalb der Funktionen (`from database.db import SessionLocal`) — sonst Import-Side-Effects.
    - Logging-Prefix `[Pipeline]` konsistent zu CLAUDE.md Logging-Pattern.
    - try/finally db.close() zwingend (Pattern S1 aus 08-PATTERNS.md).
    - D-15 Constraint-Phrase "Wechsle NIEMALS" muss WORTWOERTLICH im Code erscheinen (keine Paraphrase).

    **Erstelle tests/test_prompt_pipeline.py** mit 10 Tests:

    ```python
    """Phase 08 unit tests for services/prompt_pipeline.py."""
    import os
    import pytest
    import services.prompt_pipeline as pp
    from database.models import PromptVersion


    @pytest.fixture(autouse=True)
    def _reset_caches(monkeypatch):
        monkeypatch.setattr(pp, '_RESOLVER_CACHE', {})
        monkeypatch.setattr(pp, '_VARIANTS_CACHE', {})
        for k in list(os.environ.keys()):
            if k.startswith('PROMPT_') and k.endswith('_VERSION_OVERRIDE'):
                monkeypatch.delenv(k, raising=False)


    def _seed_variants(db_session, module='ewb', versions=('v1-legacy', 'v2-modular')):
        for v in versions:
            db_session.add(PromptVersion(
                module=module, version=v, prompt_text=f'text-{v}',
                is_active=True, is_default=(v == versions[0]),
                changelog=f'test-{v}',
            ))
        db_session.commit()


    class _Fake:
        def __init__(self, real): self._r = real
        def query(self, *a, **k): return self._r.query(*a, **k)
        def add(self, *a, **k): return self._r.add(*a, **k)
        def commit(self): return self._r.commit()
        def close(self): pass


    def _bind(monkeypatch, db_session):
        monkeypatch.setattr('database.db.SessionLocal', lambda: _Fake(db_session))


    def test_env_override_first_check(monkeypatch):
        monkeypatch.setenv('PROMPT_EWB_VERSION_OVERRIDE', 'v-override')
        assert pp.resolve_prompt_version('ewb', user_id=42) == 'v-override'


    def test_deterministic_routing_even_user(db_session, monkeypatch):
        _seed_variants(db_session)
        _bind(monkeypatch, db_session)
        assert pp.resolve_prompt_version('ewb', user_id=0) == 'v1-legacy'


    def test_deterministic_routing_odd_user(db_session, monkeypatch):
        _seed_variants(db_session)
        _bind(monkeypatch, db_session)
        assert pp.resolve_prompt_version('ewb', user_id=1) == 'v2-modular'


    def test_cache_per_user_key(db_session, monkeypatch):
        _seed_variants(db_session)
        _bind(monkeypatch, db_session)
        r0 = pp.resolve_prompt_version('ewb', user_id=0)
        r1 = pp.resolve_prompt_version('ewb', user_id=1)
        assert r0 != r1
        assert ('ewb', 0) in pp._RESOLVER_CACHE
        assert ('ewb', 1) in pp._RESOLVER_CACHE


    def test_no_variants_returns_unknown(db_session, monkeypatch):
        _bind(monkeypatch, db_session)
        assert pp.resolve_prompt_version('nonexistent_module', user_id=0) == 'unknown'


    def test_invalidate_resolver_cache_clears_both():
        pp._RESOLVER_CACHE[('ewb', 0)] = 'cached'
        pp._VARIANTS_CACHE['ewb'] = ['x', 'y']
        pp.invalidate_resolver_cache()
        assert pp._RESOLVER_CACHE == {}
        assert pp._VARIANTS_CACHE == {}


    def test_build_profile_context_no_active_profile(monkeypatch):
        import sys
        class _LSMock:
            @staticmethod
            def get_active_profile(): return (None, None)
            state = {}
        monkeypatch.setitem(sys.modules, 'services.live_session', _LSMock)
        assert pp.build_profile_context(user_id=1) == ''


    def test_build_profile_context_includes_phase_08_fields(monkeypatch):
        import sys
        class _LSMock:
            state = {}
            @staticmethod
            def get_active_profile():
                return (1, {
                    'basis': {
                        'unternehmen': 'Firma XY',
                        'produktbeschreibung': 'Testprodukt',
                        'usps': ['U1', 'U2'],
                        'branche_kontext': 'Maschinenbau-Mittelstand',
                        'eigene_formulierungen': ['Darf ich fragen, was Sie einsetzen?'],
                        'beweise': ['Firma Z: 15% mehr Abschluesse in 3 Monaten'],
                    },
                    'ki': {'ton': 'Direkt/Klartext'},
                })
        monkeypatch.setitem(sys.modules, 'services.live_session', _LSMock)
        out = pp.build_profile_context(user_id=1)
        assert 'Firma XY' in out
        assert 'Testprodukt' in out
        assert 'Maschinenbau-Mittelstand' in out
        assert 'Darf ich fragen, was Sie einsetzen?' in out
        assert '15% mehr Abschluesse' in out


    def test_build_profile_context_anrede_session_override_wins(monkeypatch):
        import sys
        class _LSMock:
            state = {'session_anrede': 'Du'}
            @staticmethod
            def get_active_profile():
                return (1, {'basis': {}, 'ki': {'ansprache': 'Sie'}})
        monkeypatch.setitem(sys.modules, 'services.live_session', _LSMock)
        out = pp.build_profile_context(user_id=1)
        assert 'Anrede: Du.' in out
        assert 'Wechsle NIEMALS' in out


    def test_log_pipeline_event_swallows_errors(monkeypatch):
        def _raise(*a, **k): raise RuntimeError('DB down')
        monkeypatch.setattr('services.finetune_logging.log_ft_event', _raise)
        # Should not raise:
        pp.log_pipeline_event('assistant', 'ewb', {'model': 'haiku'})
    ```

    Umlaut-Regel: User-facing Prompt-Output ("Anrede: ...", "WICHTIG: ...") mit echten Umlauten wo sie stehen (z.B. "Uebersicht" vs "Übersicht" — hier ae-Ersatz OK weil der String in einem Prompt landet der an Haiku API geht; Haiku interpretiert beides). Python-Code-Identifier (Cache-Keys, Funktionsnamen) ASCII.
  </action>
  <verify>
    <automated>python -c "import services.prompt_pipeline as pp; assert callable(pp.resolve_prompt_version); assert callable(pp.build_profile_context); assert callable(pp.log_pipeline_event); assert callable(pp.invalidate_resolver_cache); assert pp._RESOLVER_CACHE == {}; assert pp._VARIANTS_CACHE == {}; print('OK')" && pytest tests/test_prompt_pipeline.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f services/prompt_pipeline.py` → exit 0
    - `wc -l services/prompt_pipeline.py` → >= 120
    - `grep -nE "^def (resolve_prompt_version|build_profile_context|log_pipeline_event|invalidate_resolver_cache)" services/prompt_pipeline.py` → 4 matches
    - `grep -n "_RESOLVER_CACHE.*dict" services/prompt_pipeline.py` → exit 0
    - `grep -nE '\(module, user_id\)' services/prompt_pipeline.py` → exit 0 (W-7: Cache-Key MUSS das 2-Tuple `(module, user_id)` sein — verhindert Cross-User-Variant-Leakage, siehe Risk R2 in RESEARCH Pitfalls)
    - `grep -n "_ACTIVE_PROMPT_CACHE" services/prompt_pipeline.py` → exit 1 (W-7: neuer Resolver-Cache MUSS unabhaengig vom Legacy-Cache aus services/claude_service.py:97 sein — keine Referenz, kein Import, kein Write)
    - `grep -n "PROMPT_.*VERSION_OVERRIDE" services/prompt_pipeline.py` → exit 0
    - `grep -n "user_id % len" services/prompt_pipeline.py` → exit 0
    - `grep -n "Wechsle NIEMALS" services/prompt_pipeline.py` → exit 0 (D-15 wortwoertlich)
    - `grep -n "\[Pipeline\]" services/prompt_pipeline.py` → exit 0
    - `test -f tests/test_prompt_pipeline.py` → exit 0
    - `grep -c "^def test_" tests/test_prompt_pipeline.py` → >= 10
    - `pytest tests/test_prompt_pipeline.py -x -v` → exit 0
    - `python -c "import services.prompt_pipeline"` → exit 0 (no import side-effect)
  </acceptance_criteria>
  <done>prompt_pipeline.py mit 4 Funktionen + 2 Caches. 10 Tests gruen. Keine Side-Effects. ENV-Override + Routing + Cache + Anrede-Resolution + D-15 Phrase alle verifiziert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: services/ewb_pipeline.py + _seed_ewb_v2 + Integration-Test (D-41, D-47)</name>
  <files>services/ewb_pipeline.py, app.py, tests/test_ewb_pipeline.py</files>
  <read_first>
    - services/prompt_pipeline.py (neu aus Task 1)
    - services/claude_service.py Zeilen 9-30 (SYSTEM_PROMPT_BASE als v1-Legacy Referenz-Text), Zeilen 258-394 (_build_system_prompt Analog-Pattern)
    - app.py Zeilen 601-644 (_seed_prompt_versions Pattern mit existing-row-check)
    - docs/phase-08-training-vs-live-prompt-gap.md (aus Plan 01 — v2-Prompt SOLL-Struktur)
    - 08-PATTERNS.md §"services/ewb_pipeline.py" Zeilen 121-201, §"app.py _migrate()" Block 5 _seed_ewb_v2 Pattern Zeilen 669-692
    - 08-RESEARCH.md Focus Area 1 (Gap-Matrix), Focus Area 4 "Seed fuer v2-Variante" Zeilen 322-332
    - 08-CONTEXT.md D-15 (Anrede-Phrase), D-41 (ewb_pipeline.py Scope), D-42 (NICHT apply_tabu_filter in 08), D-47 (Active-Listening-Block), <specifics> Zeile 215 (Baustein-Template: Anker/Reframe/Beweis/Ueberleitung, 45 Woerter, niemals apologetisch)
    - tests/conftest.py
  </read_first>
  <behavior>
    - Test 1: build_ewb_prompt(profile_data={}, anrede='Sie', version='v1-legacy') liefert Non-Empty-String mit v1-Seed-Text
    - Test 2: version='v2-modular' → Output enthaelt 'Anker', 'Reframe', 'Beweis', 'Ueberleitung', 'Active Listening', '45 Woerter'
    - Test 3: anrede='Du' → Output enthaelt 'Anrede: Du.' UND 'Wechsle NIEMALS'
    - Test 4: Unbekannte version → Fallback auf _FALLBACK_V1_PROMPT (len > 50, enthaelt 'NERVE' oder 'Vertrieb')
    - Test 5: _seed_ewb_v2 legt 2 Rows an, zweiter Call idempotent (bleibt bei 2)
    - Test 6: v1-legacy: is_default=True; v2-modular: is_default=False; beide is_active=True
  </behavior>
  <action>
    **Erstelle services/ewb_pipeline.py:**

    ```python
    """
    services/ewb_pipeline.py
    Phase 08 EWB-Modul-spezifische Prompt-Assembly.

    Exports:
      - build_ewb_prompt(profile_data, anrede, version, user_id) -> str

    Laedt Prompt-Text aus prompt_versions DB; Fallback _FALLBACK_V1_PROMPT.
    """
    from __future__ import annotations
    from typing import Optional

    from services.prompt_pipeline import build_profile_context


    _FALLBACK_V1_PROMPT = (
        "Du bist NERVE, ein Vertriebs-KI-Assistent im Live-Call.\n\n"
        "Wenn ein Einwand kommt, liefere EINE konkrete, sofort vorlesbare "
        "Gegenargumentation in 2-3 Saetzen. Kein Fachjargon, keine Floskeln. "
        "Ende mit Gegenfrage.\n"
    )


    def build_ewb_prompt(profile_data: Optional[dict] = None,
                         anrede: str = 'Sie',
                         version: str = 'v1-legacy',
                         user_id: int = 0) -> str:
        """Assemble EWB system prompt fuer Haiku-Call."""
        template_text = _load_prompt_template(version)

        # Kontext aus Shared-Utils (Session-Override/Profile-Default)
        context_block = build_profile_context(user_id=user_id)
        if not context_block:
            # Fallback fuer Unit-Tests oder leere Session: Anrede-Constraint manuell
            context_block = (
                f'Anrede: {anrede}. WICHTIG: Nutze konsequent {anrede}-Form. '
                f'Wechsle NIEMALS innerhalb einer Antwort zwischen Du und Sie.'
            )

        parts = [template_text, '\n--- AKTIVES VERKAUFSPROFIL ---', context_block]
        prompt = '\n'.join(parts)
        print(f"[EWB] v{version} assembled user_id={user_id} len={len(prompt)}")
        return prompt


    def _load_prompt_template(version: str) -> str:
        """Load prompt_text fuer module='ewb' + version aus prompt_versions."""
        try:
            from database.db import SessionLocal
            from database.models import PromptVersion
            db = SessionLocal()
            try:
                row = (db.query(PromptVersion)
                       .filter_by(module='ewb', version=version, is_active=True)
                       .first())
                if row and row.prompt_text:
                    return row.prompt_text
            finally:
                db.close()
        except Exception as e:
            print(f"[EWB] template load failed version={version}: {e}")
        return _FALLBACK_V1_PROMPT
    ```

    **Ergaenze app.py** mit `_seed_ewb_v2(db=None)` Funktion (nach `_seed_prompt_versions` definieren):

    ```python
    def _seed_ewb_v2(db=None):
        """Phase 08 D-26: Seed 2 prompt_versions Rows fuer module='ewb'.
        Idempotent via existing-row-check. v1-legacy ist_default=True, v2-modular ist_default=False."""
        from database.db import SessionLocal
        from database.models import PromptVersion

        V1_LEGACY_TEXT = (
            "Du bist NERVE, ein Vertriebs-KI-Assistent im Live-Call.\n\n"
            "Wenn ein Einwand kommt, liefere EINE konkrete, sofort vorlesbare "
            "Gegenargumentation in 2-3 Saetzen. Kein Fachjargon, keine Floskeln "
            "wie 'Ich verstehe vollkommen'. Ende mit Gegenfrage.\n"
        )

        V2_MODULAR_TEXT = (
            "Du bist NERVE, ein Vertriebs-KI-Assistent im Live-Call.\n\n"
            "## Bausteine (Reihenfolge einhalten)\n"
            "1. ANKER: Kurz bestaetigen was der Kunde gesagt hat (kein 'Ich verstehe'-Floskel).\n"
            "2. REFRAME: Perspektivwechsel - stelle den Einwand in einen neuen Kontext.\n"
            "3. KERN-GEGENARGUMENT + BEWEIS: Ein konkretes Argument plus ein Beweis-Element "
            "(Zahlen, Fallstudie, Kundenzitat aus dem Profil).\n"
            "4. UEBERLEITUNG: Gegenfrage oder Alternativ-Close, der den Dialog zurueckholt.\n\n"
            "## Active Listening Block (D-47)\n"
            "- Reagiere auf konkrete Phrasen des Kunden, nicht auf Kategorien.\n"
            "- Wenn der Kunde korrigiert: korrigiere dich explizit ('Danke fuer die Klarstellung, ...').\n"
            "- Wenn der Kunde ein Detail nennt: spiegele es zurueck bevor du argumentierst.\n"
            "- Bilde NIEMALS Hypothesen ueber Bedarf ohne Signal - frage nach.\n"
            "- Beachte Geschlechts-Hinweise im Vornamen und halte sie konsistent.\n\n"
            "## Harte Regeln\n"
            "- Max 45 Woerter pro Antwort.\n"
            "- NIEMALS apologetisch ('Ich verstehe, dass ...', 'Tut mir leid').\n"
            "- Anrede-Constraint aus Kontext-Block strikt einhalten.\n"
            "- Niemals Floskeln wie 'Das ist eine gute Frage'.\n"
        )

        owns = db is None
        if owns:
            db = SessionLocal()
        try:
            for version, ptext, is_default in [
                ('v1-legacy', V1_LEGACY_TEXT, True),
                ('v2-modular', V2_MODULAR_TEXT, False),
            ]:
                exists = (db.query(PromptVersion)
                          .filter_by(module='ewb', version=version)
                          .first())
                if exists:
                    continue
                db.add(PromptVersion(
                    module='ewb', version=version, prompt_text=ptext,
                    is_active=True, is_default=is_default,
                    changelog=f'Phase 08 Seed ({version})',
                ))
            db.commit()
            print("[DB] Seed v08: module='ewb' v1-legacy + v2-modular seeded (idempotent)")
        finally:
            if owns:
                db.close()
    ```

    Rufe `_seed_ewb_v2()` in app.py **nach _migrate()** auf (gleiche Stelle wie _seed_prompt_versions oder direkt danach). Wrapp in try/except non-fatal:
    ```python
    try:
        _seed_ewb_v2()
    except Exception as e:
        print(f"[DB] _seed_ewb_v2 failed (non-fatal): {e}")
    ```

    **Erstelle tests/test_ewb_pipeline.py** mit 6 Tests:

    ```python
    """Phase 08 tests for services/ewb_pipeline.py + _seed_ewb_v2."""
    import pytest
    import services.ewb_pipeline as ep
    from database.models import PromptVersion


    def _seed_ewb_variants(db_session):
        for v, text, default in [
            ('v1-legacy', 'v1-legacy-text CONTENT', True),
            ('v2-modular',
             ('v2 with Anker Reframe Beweis Ueberleitung '
              'Active Listening max 45 Woerter NIEMALS apologetisch'),
             False),
        ]:
            db_session.add(PromptVersion(
                module='ewb', version=v, prompt_text=text,
                is_active=True, is_default=default,
                changelog=f'test-{v}',
            ))
        db_session.commit()


    class _Fake:
        def __init__(self, real): self._r = real
        def query(self, *a, **k): return self._r.query(*a, **k)
        def add(self, *a, **k): return self._r.add(*a, **k)
        def commit(self): return self._r.commit()
        def close(self): pass


    def _bind(monkeypatch, db_session):
        monkeypatch.setattr('database.db.SessionLocal', lambda: _Fake(db_session))


    def test_build_ewb_prompt_v1_legacy(db_session, monkeypatch):
        _seed_ewb_variants(db_session)
        _bind(monkeypatch, db_session)
        out = ep.build_ewb_prompt(profile_data={}, anrede='Sie',
                                   version='v1-legacy', user_id=0)
        assert 'v1-legacy-text CONTENT' in out
        assert 'Anrede: Sie' in out


    def test_build_ewb_prompt_v2_modular_bausteine(db_session, monkeypatch):
        _seed_ewb_variants(db_session)
        _bind(monkeypatch, db_session)
        out = ep.build_ewb_prompt(profile_data={}, anrede='Du',
                                   version='v2-modular', user_id=1)
        for keyword in ['Anker', 'Reframe', 'Beweis', 'Ueberleitung',
                         'Active Listening', '45 Woerter']:
            assert keyword in out, f'missing: {keyword}'


    def test_build_ewb_prompt_anrede_du(db_session, monkeypatch):
        _seed_ewb_variants(db_session)
        _bind(monkeypatch, db_session)
        out = ep.build_ewb_prompt(anrede='Du', version='v1-legacy')
        assert 'Anrede: Du' in out
        assert 'Wechsle NIEMALS' in out


    def test_build_ewb_prompt_fallback_unknown_version(db_session, monkeypatch):
        _seed_ewb_variants(db_session)
        _bind(monkeypatch, db_session)
        out = ep.build_ewb_prompt(anrede='Sie', version='nonexistent')
        assert len(out) > 50
        assert 'NERVE' in out or 'Vertrieb' in out


    def test_seed_ewb_v2_idempotent(db_session):
        from app import _seed_ewb_v2
        _seed_ewb_v2(db=db_session)
        c1 = db_session.query(PromptVersion).filter_by(module='ewb').count()
        _seed_ewb_v2(db=db_session)
        c2 = db_session.query(PromptVersion).filter_by(module='ewb').count()
        assert c1 == c2 == 2


    def test_seed_ewb_v2_default_flags(db_session):
        from app import _seed_ewb_v2
        _seed_ewb_v2(db=db_session)
        v1 = db_session.query(PromptVersion).filter_by(module='ewb', version='v1-legacy').first()
        v2 = db_session.query(PromptVersion).filter_by(module='ewb', version='v2-modular').first()
        assert v1 is not None and v2 is not None
        assert v1.is_default is True
        assert v2.is_default is False
        assert v1.is_active is True and v2.is_active is True
    ```

    Umlaut-Regel: V2_MODULAR_TEXT Prompt-String ist API-Input fuer Haiku — ae/oe/ue/ss ASCII-Ersatz ist OK (und verhindert Encoding-Fallen in .py-Files gefaehrlich positionierter Umlaute). Python-Code-Identifier strikt ASCII.
  </action>
  <verify>
    <automated>python -c "from services.ewb_pipeline import build_ewb_prompt; assert callable(build_ewb_prompt); print('OK ewb_pipeline')" && python -c "from app import _seed_ewb_v2; assert callable(_seed_ewb_v2); print('OK seed')" && pytest tests/test_ewb_pipeline.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test -f services/ewb_pipeline.py` → exit 0
    - `wc -l services/ewb_pipeline.py` → >= 60
    - `grep -nE "^def build_ewb_prompt" services/ewb_pipeline.py` → exit 0
    - `grep -n "_FALLBACK_V1_PROMPT" services/ewb_pipeline.py` → exit 0
    - `grep -n "\[EWB\]" services/ewb_pipeline.py` → exit 0
    - `grep -n "from services.prompt_pipeline import build_profile_context" services/ewb_pipeline.py` → exit 0
    - `grep -nE "^def _seed_ewb_v2" app.py` → exit 0
    - `grep -n "ANKER\|REFRAME\|KERN-GEGENARGUMENT\|UEBERLEITUNG" app.py` → >= 4 matches (V2_MODULAR_TEXT Bausteine)
    - `grep -n "Active Listening" app.py` → exit 0 (D-47)
    - `grep -n "45 Woerter" app.py` → exit 0
    - `grep -n "NIEMALS apologetisch" app.py` → exit 0
    - `grep -n "_seed_ewb_v2()" app.py` → >= 1 Call-Site (ausserhalb der Definition)
    - `test -f tests/test_ewb_pipeline.py` → exit 0
    - `grep -c "^def test_" tests/test_ewb_pipeline.py` → >= 6
    - `pytest tests/test_ewb_pipeline.py -x -v` → exit 0
  </acceptance_criteria>
  <done>ewb_pipeline.py baut EWB-Prompt, _seed_ewb_v2 legt idempotent 2 prompt_versions-Rows an, Integration-Tests alle gruen, Baustein-Struktur + Active Listening + 45-Woerter-Rule + NIEMALS-apologetisch im Seed-Text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python-Code ↔ ENV-Vars | `PROMPT_EWB_VERSION_OVERRIDE` ist Ops-Control — nicht User-Request. |
| prompt_pipeline.resolve_prompt_version ↔ DB | Read-only Query auf prompt_versions, kein User-Input. |
| ewb_pipeline._load_prompt_template ↔ DB | Read-only. Prompt-Text wird direkt an Claude API gesendet — kein Sanitize noetig (Claude ist System-Prompt-Sink, nicht SQL-Sink). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | `PROMPT_EWB_VERSION_OVERRIDE` ENV leak in Prozess-Logs | mitigate | `resolve_prompt_version` logged nicht den Override-Wert, nur "cache hit"-Events. Pattern ist konsistent zu `[Pipeline] variants loaded module=X count=N`. Kein env-Wert in print(). |
| T-08-02-02 | Spoofing | Client-Request forced einen Variant via POST-Body | accept | Router ist server-side, user_id kommt aus `g.user.id` (Session), nicht aus Request-Body. Plan 03 Integration-Stelle MUSS das bestaetigen. |
| T-08-02-03 | Denial | _VARIANTS_CACHE-Load-Fehler bei DB-Crash blockiert Live-Loop | mitigate | try/except um SessionLocal()-Call, fallback auf `['unknown']`. resolve_prompt_version wirft nie Exception. log_pipeline_event schluckt Exception. |
| T-08-02-04 | Elevation | Admin-Code (invalidate_resolver_cache) callable aus App-Context | accept | Funktion ist public, aber nur Entwickler-Interface. Kein HTTP-Endpunkt, kein User-Facing. Plan 06 Admin-Page nutzt es optional. |
| T-08-02-05 | Info Disclosure | Prompt-Text (V2_MODULAR_TEXT) enthaelt proprietaere Struktur | accept | Prompt ist Teil des Produkt-IP, aber im Code-Repo (andre's Privat-Repo) mit dem gesamten System dokumentiert. Kein Extra-Leak-Risiko. |
</threat_model>

<verification>
Beide Tasks muessen gruen sein:
1. pytest tests/test_prompt_pipeline.py → alle 10 Tests (Resolver-ENV, -Routing, -Cache, -Context, -Anrede, -Logging)
2. pytest tests/test_ewb_pipeline.py → alle 6 Tests (v1/v2, anrede, fallback, seed-idempotent, default-flags)
3. Import-Smoke: `python -c "import services.prompt_pipeline; import services.ewb_pipeline; print('OK')"` — keine Import-Side-Effects

Nach Deploy von Plan 02 (aber vor Plan 03):
- App-Startup muss `_seed_ewb_v2` ausfuehren
- Manuelle Verifikation: `sqlite3 database/nerve.db "SELECT module, version, is_active, is_default FROM prompt_versions WHERE module='ewb'"` → 2 Zeilen
</verification>

<success_criteria>
- [ ] services/prompt_pipeline.py mit 4 Funktionen committed
- [ ] services/ewb_pipeline.py mit 1 Hauptfunktion committed
- [ ] app.py _seed_ewb_v2 definiert und in Startup aufgerufen (nach _migrate)
- [ ] tests/test_prompt_pipeline.py 10 Tests gruen
- [ ] tests/test_ewb_pipeline.py 6 Tests gruen
- [ ] Nach App-Startup: prompt_versions hat 2 Rows fuer module='ewb'
- [ ] D-15 Phrase "Wechsle NIEMALS" wortwoertlich in prompt_pipeline.py und ewb_pipeline.py Fallback-Block
- [ ] D-47 Active-Listening-Block im V2_MODULAR_TEXT enthalten
- [ ] Keine Modifikationen an services/claude_service.py (die Integration kommt in Plan 03)
- [ ] Keine Import-Side-Effects (verifiziert via python -c "import ...")
</success_criteria>

<output>
Summary: `.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-02-SUMMARY.md`

Muss dokumentieren:
- Tatsaechliche Lines-of-Code beider neuer Module
- Test-Count pro Datei und Gesamt-Runtime
- Tatsaechliche Zeilennummer in app.py wo _seed_ewb_v2 aufgerufen wird
- Ob _FALLBACK_V1_PROMPT jemals gegriffen hat (sollte nicht — nur wenn Seed fehlschlaegt)
- Interface-Contract fuer Plan 03 Consumer (claude_service.py): `from services.ewb_pipeline import build_ewb_prompt; ewb_version = resolve_prompt_version('ewb', user_id); system_prompt = build_ewb_prompt(anrede=..., version=ewb_version, user_id=user_id)`
</output>
</content>
</invoke>