---
phase: 08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/models.py
  - app.py
  - docs/phase-08-training-vs-live-prompt-gap.md
autonomous: true
requirements:
  - EWB-01
  - EWB-02
  - EWB-06
  - EWB-10
addressed_decisions: [D-01, D-02, D-14, D-26, D-46]

must_haves:
  truths:
    - "objection_events.success kann NULL sein (3-State akzeptiert TRUE/FALSE/NULL)"
    - "Alt-Daten aus POLISH-38.1 (created_at < 2026-04-22 UTC) haben success=NULL nach Migration"
    - "Pre-Migration Backup der SQLite-DB existiert unter database/nerve.db.bak_pre_v08_01"
    - "conversation_logs.anrede Column existiert (VARCHAR(10), nullable)"
    - "prompt_versions.is_default Column existiert, vorhandene is_active=1 Rows haben is_default=1 (backfill)"
    - "docs/phase-08-training-vs-live-prompt-gap.md dokumentiert die 7 Profile-Feld-Gaps zwischen _build_system_prompt IST und v2-modular SOLL"
  artifacts:
    - path: "database/models.py"
      provides: "ObjectionEvent.success nullable, ConversationLog.anrede, PromptVersion.is_default"
      contains: "nullable=True"
    - path: "app.py"
      provides: "Phase 08 migration block + audit_log marker + DB backup"
      contains: "migration_v08_01"
    - path: "docs/phase-08-training-vs-live-prompt-gap.md"
      provides: "Gap-Matrix Profile-Feld → System-Prompt"
      min_lines: 40
    - path: "database/nerve.db.bak_pre_v08_01"
      provides: "Safety backup before destructive D-02 reset"
  key_links:
    - from: "app.py _migrate()"
      to: "objection_events table"
      via: "Table-Rebuild (CREATE new → INSERT SELECT → DROP old → RENAME)"
      pattern: "CREATE TABLE objection_events_new"
    - from: "app.py _migrate()"
      to: "conversation_logs.anrede"
      via: "ALTER TABLE ADD COLUMN"
      pattern: "ALTER TABLE conversation_logs ADD COLUMN anrede"
    - from: "app.py _migrate()"
      to: "audit_log"
      via: "insert marker row mit action=migration_v08_01"
      pattern: "migration_v08_01_reset_success_polish38_1"
---

<objective>
Wave 1 Foundation: DB-Schema-Änderungen für Phase 08 — 3 Migrations-Blöcke + Training-vs-Live-Prompt-Gap-Matrix als Planner-Input für Wave 2. Alle Migrations idempotent (Re-Run-Safe), destruktive Migration (D-02) mit Pre-Backup und audit_log-Marker.

Purpose: Ohne diese Migrations können weder A/B-Routing (prompt_versions.is_default), noch POLISH-55-Rating (success NULL), noch PreCall-Anrede-Override (conversation_logs.anrede) funktionieren. Gap-Matrix dient Wave 2 als Spec für v2-modular Prompt-Text.

Output:
- 3 Migrations-Blöcke in app.py _migrate()
- 3 Model-Änderungen in database/models.py
- Backup-File database/nerve.db.bak_pre_v08_01
- Gap-Matrix docs/phase-08-training-vs-live-prompt-gap.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-CONTEXT.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-RESEARCH.md
@.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-PATTERNS.md
@database/models.py
@app.py
@CLAUDE.md

<interfaces>
<!-- Key types and existing patterns the executor will touch -->

ObjectionEvent (database/models.py line 342-350):
```python
class ObjectionEvent(Base):
    __tablename__ = 'objection_events'
    id                  = Column(Integer, primary_key=True)
    user_id             = Column(Integer, ForeignKey('users.id'), nullable=False)
    org_id              = Column(Integer, ForeignKey('organisations.id'), nullable=True)
    conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id'), nullable=False)
    einwand_typ         = Column(String(100), nullable=False)
    success             = Column(Boolean, default=False, nullable=False)   # ← Phase 08 D-01 → nullable=True
    created_at          = Column(DateTime, default=utcnow, nullable=False)
```

ConversationLog (database/models.py line 231-283):
```python
class ConversationLog(Base):
    __tablename__ = 'conversation_logs'
    # ... ~30 existing columns ...
    # NEW Phase 08 D-14 (append after line 283):
    anrede              = Column(String(10), nullable=True)  # PreCall-Override Du/Sie
```

PromptVersion (database/models.py line 463-474):
```python
class PromptVersion(Base):
    __tablename__ = 'prompt_versions'
    __table_args__ = (UniqueConstraint('version', 'module', name='uq_prompt_version_module'),)
    id          = Column(Integer, primary_key=True)
    version     = Column(String(50), nullable=False)
    module      = Column(String(50), nullable=False)
    prompt_text = Column(Text, nullable=False)
    changelog   = Column(Text)
    is_active   = Column(Boolean, default=False, nullable=False)
    # NEW Phase 08 D-26:
    is_default  = Column(Boolean, default=False, nullable=False)
    created_at  = Column(DateTime, default=utcnow)
```

_migrate() pattern (app.py lines 90-530) — idempotent ADD COLUMN via try/except:
```python
for col, typedef in [
    ('anrede', 'VARCHAR(10)'),
]:
    try:
        conn.execute(text(f'ALTER TABLE conversation_logs ADD COLUMN {col} {typedef}'))
        conn.commit()
        print(f"[DB] Migration: added conversation_logs.{col}")
    except Exception:
        pass
```

Training-EWB-Prompt Gap-Matrix (RESEARCH Focus Area 1) — Profile-Felder die _build_system_prompt() IST vs. SOLL liest:
- `branche` column → IST NICHT gelesen (legacy column), SOLL gelesen
- `branche_kontext` → existiert nicht, SOLL gelesen (D-11)
- `eigene_formulierungen` → existiert nicht, SOLL gelesen (D-07)
- `beweise` → existiert nicht, SOLL gelesen (D-08)
- Anrede-Constraint → IST "Kundenansprache: X (immer einhalten)", SOLL "Anrede: X. WICHTIG: Nutze konsequent. Wechsle NIEMALS" (D-15)
- Baustein-Struktur → IST keine, SOLL Anker/Reframe/Beweis/Überleitung
- Active-Listening-Block → IST keiner, SOLL D-47 (gegen POLISH-35/36/37)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: database/models.py Model-Änderungen + Alembic-freies Marker-Pattern</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py (Zeilen 100-150 Profile, 231-283 ConversationLog, 342-360 ObjectionEvent, 463-474 PromptVersion)
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-CONTEXT.md (D-01, D-14, D-26)
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-RESEARCH.md Focus Area 2, Focus Area 4 (Zeilen 191-332)
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-PATTERNS.md §"database/models.py (MODIFY)" (Zeilen 563-589)
    - CLAUDE.md (Umlaut-Regel: DB-Spalten ASCII → anrede nicht Anrede, is_default nicht ist_default)
  </read_first>
  <behavior>
    - Test 1: ObjectionEvent.__table__.columns['success'].nullable == True (per D-01)
    - Test 2: ConversationLog.__table__.columns['anrede'] existiert, String-Type, nullable=True (per D-14)
    - Test 3: PromptVersion.__table__.columns['is_default'] existiert, Boolean, default=False, nullable=False (per D-26)
    - Test 4: Bestehende UniqueConstraint uq_prompt_version_module bleibt intakt
  </behavior>
  <action>
    Führe exakt 3 in-place Edits an database/models.py durch:

    **Edit 1 — ObjectionEvent.success nullable (D-01):**
    Zeile ~349 in models.py: Ersetze
    ```python
    success             = Column(Boolean, default=False, nullable=False)
    ```
    durch
    ```python
    # Phase 08 D-01: 3-state (TRUE=Erfolg, FALSE=Kein Erfolg, NULL=Uebersprungen/Unbekannt)
    success             = Column(Boolean, default=None, nullable=True)
    ```

    **Edit 2 — ConversationLog.anrede (D-14):**
    In der ConversationLog-Klasse (ab Zeile 231) direkt VOR der Definition der letzten Column (oder vor dem `def __repr__` falls vorhanden, sonst als letzte Column im Definitionsblock nach Zeile 283) folgende Column ergänzen:
    ```python
    # Phase 08 D-14: PreCall-Anrede-Override (Du/Sie pro Session). Fallback: Profile.daten.ki.ansprache.
    anrede              = Column(String(10), nullable=True)
    ```
    Werte-Constraint wird in Wave 5 im Write-Hook (routes/app_routes.py) erzwungen (Whitelist ['Du', 'Sie']), nicht auf DB-Ebene — siehe Plan 05 Threat Model.

    **Edit 3 — PromptVersion.is_default (D-26):**
    Zeile ~471 in models.py nach `is_active = Column(...)` und vor `created_at = Column(...)` ergänzen:
    ```python
    # Phase 08 D-26: A/B-Default-Fallback (wenn get_active_prompt_version() single-lookup macht).
    # Bei 2+ aktiven Varianten pro module: exakt 1 Row hat is_default=True.
    is_default  = Column(Boolean, default=False, nullable=False)
    ```

    KEINE Änderungen an bestehenden Spalten außer Edit 1 (nullable toggle). KEIN Entfernen von UniqueConstraint.

    Implementierung der Migrations-Block-Logik (Table-Rebuild für success + ALTER ADD COLUMN für anrede/is_default) passiert in Task 2 — hier NUR Model-Änderungen.

    Umlaut-Regel (CLAUDE.md): Spalten-Namen bleiben ASCII (`anrede`, `is_default`). Kommentare dürfen Umlaute haben.
  </action>
  <verify>
    <automated>python -c "from database.models import ObjectionEvent, ConversationLog, PromptVersion; assert ObjectionEvent.__table__.columns['success'].nullable == True, 'ObjectionEvent.success not nullable'; assert 'anrede' in ConversationLog.__table__.columns, 'ConversationLog.anrede missing'; assert ConversationLog.__table__.columns['anrede'].nullable == True, 'anrede not nullable'; assert 'is_default' in PromptVersion.__table__.columns, 'PromptVersion.is_default missing'; assert PromptVersion.__table__.columns['is_default'].nullable == False, 'is_default not non-null'; print('OK: 3 model changes verified')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "success.*=.*Column(Boolean.*nullable=True" database/models.py` → exit 0 (ObjectionEvent.success nullable)
    - `grep -nE "anrede\s*=\s*Column\(String\(10\)" database/models.py` → exit 0 (ConversationLog.anrede existiert)
    - `grep -nE "is_default\s*=\s*Column\(Boolean.*nullable=False" database/models.py` → exit 0 (PromptVersion.is_default existiert)
    - `grep -c "Phase 08 D-0[1-9]\|Phase 08 D-1[0-9]\|Phase 08 D-2[0-9]" database/models.py` → ≥3 (Kommentar-Marker für 3 Edits)
    - `python -c "from database.models import ObjectionEvent; print(ObjectionEvent.__table__.columns['success'].nullable)"` → `True`
    - `python -c "from database.models import ConversationLog; print('anrede' in ConversationLog.__table__.columns)"` → `True`
    - `python -c "from database.models import PromptVersion; print('is_default' in PromptVersion.__table__.columns)"` → `True`
    - Keine Änderungen an UniqueConstraint: `grep "uq_prompt_version_module" database/models.py` → exit 0
  </acceptance_criteria>
  <done>Alle 3 Column-Änderungen in database/models.py committed, Import lädt ohne Fehler, alle Acceptance-Criteria grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app.py _migrate() 3 Migrations-Blöcke + Pre-Backup + audit_log Marker</name>
  <files>app.py, database/nerve.db.bak_pre_v08_01</files>
  <read_first>
    - app.py Zeilen 88-650 (bestehende _migrate()-Struktur inkl. ADD COLUMN Pattern + _seed_prompt_versions)
    - database/models.py (AuditLog Definition — für Marker-Row)
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-RESEARCH.md Focus Area 2 "Migration" Zeilen 870-907, Focus Area 4 "Migration" Zeilen 317-321, "Risks/Rollback Plan §Risk 1" Zeilen 1060-1067
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-PATTERNS.md §"app.py _migrate() (MODIFY)" Zeilen 592-693 inkl. Pre-Migration DB-Backup Snippet
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-CONTEXT.md (D-01, D-02, D-14, D-26)
    - STATE.md Quick Task 260421-kwm (POLISH-38.1 Commit `585f567` Timestamp 2026-04-21 15:11:10 +0200 → Cutoff konservativ 2026-04-22 00:00:00 UTC)
  </read_first>
  <behavior>
    - Test 1: Nach _migrate() gibt PRAGMA table_info(objection_events) für Spalte 'success' notnull=0 (nullable)
    - Test 2: Alt-Data objection_events-Rows mit created_at < 2026-04-22 haben success=NULL
    - Test 3: conversation_logs hat die Spalte 'anrede' (PRAGMA table_info)
    - Test 4: prompt_versions hat die Spalte 'is_default', alle bestehenden is_active=1 Rows haben is_default=1 (backfill)
    - Test 5: Pre-Migration Backup database/nerve.db.bak_pre_v08_01 existiert und ist >0 bytes
    - Test 6: audit_log hat eine Row mit action='migration_v08_01_reset_success_polish38_1'
    - Test 7: Idempotenz — zweiter Aufruf von _migrate() wirft keine Exception
  </behavior>
  <action>
    Ergänze in app.py `_migrate()` genau 4 neue Blöcke NACH dem bestehenden prompt_versions-Block (Zeile ~360 suchen nach `# ── prompt_versions ` oder vor `_seed_prompt_versions`).

    Jeder Block ist try/except-swallow-Pattern konsistent zu bestehenden Migrations (Pitfall 2 vermeiden — SQLite kann kein ALTER COLUMN, daher Table-Rebuild).

    **Block A — Pre-Migration DB-Backup (CRITICAL vor D-02):**
    ```python
    # ── Phase 08: Pre-Migration DB-Backup vor destruktiver D-02-Migration ──────
    try:
        import shutil
        import os as _os_bk
        _db_path = 'database/nerve.db'
        _backup_path = 'database/nerve.db.bak_pre_v08_01'
        if _os_bk.path.exists(_db_path) and not _os_bk.path.exists(_backup_path):
            shutil.copy(_db_path, _backup_path)
            print(f"[DB] Phase 08 backup created: {_backup_path}")
    except Exception as e:
        print(f"[DB] Phase 08 backup skipped (non-fatal): {e}")
    ```

    **Block B — ObjectionEvent.success Nullable (Table-Rebuild, D-01):**
    ```python
    # ── Phase 08 D-01: objection_events.success → NULLABLE (Table-Rebuild) ─────
    try:
        rows = conn.execute(text("PRAGMA table_info(objection_events)")).fetchall()
        # r = (cid, name, type, notnull, dflt_value, pk)
        success_is_notnull = any(r[1] == 'success' and r[3] == 1 for r in rows)
        if success_is_notnull:
            conn.execute(text("""
                CREATE TABLE objection_events_new (
                    id INTEGER PRIMARY KEY,
                    user_id INTEGER NOT NULL,
                    org_id INTEGER,
                    conversation_log_id INTEGER NOT NULL,
                    einwand_typ VARCHAR(100) NOT NULL,
                    success BOOLEAN,
                    created_at DATETIME NOT NULL,
                    FOREIGN KEY(user_id) REFERENCES users(id),
                    FOREIGN KEY(org_id) REFERENCES organisations(id),
                    FOREIGN KEY(conversation_log_id) REFERENCES conversation_logs(id)
                )
            """))
            conn.execute(text("INSERT INTO objection_events_new SELECT * FROM objection_events"))
            conn.execute(text("DROP TABLE objection_events"))
            conn.execute(text("ALTER TABLE objection_events_new RENAME TO objection_events"))
            conn.commit()
            print("[DB] Migration v08_01: objection_events.success -> NULLABLE (table rebuilt)")
    except Exception as e:
        print(f"[DB] Phase 08 objection_events rebuild skipped: {e}")
    ```

    **Block C — Alt-Daten-Reset POLISH-38.1 (D-02, destruktiv):**
    Cutoff-Timestamp wortwörtlich `'2026-04-22 00:00:00'` (per Open Question 1 in RESEARCH + git log verifiziert Commit 585f567 vom 2026-04-21 15:11).
    ```python
    # ── Phase 08 D-02: Reset POLISH-38.1 Alt-Daten auf NULL (DESTRUKTIV) ───────
    # Cutoff = 2026-04-22 00:00:00 UTC (POLISH-38.1 Commit 585f567 war 2026-04-21 15:11 +0200).
    # Marker-Row in audit_log zur Nachvollziehbarkeit.
    try:
        result = conn.execute(text("""
            UPDATE objection_events SET success = NULL
            WHERE created_at < '2026-04-22 00:00:00'
        """))
        reset_count = result.rowcount if hasattr(result, 'rowcount') else -1
        conn.commit()
        print(f"[DB] Migration v08_01: Reset {reset_count} POLISH-38.1 success-Werte auf NULL")
        # Marker in audit_log — NICHT fatal wenn fehlschlägt
        try:
            import json as _json_mig
            conn.execute(text("""
                INSERT INTO audit_log (user_id, action, entity_type, entity_id, details, created_at)
                VALUES (NULL, 'migration_v08_01_reset_success_polish38_1', 'objection_events', NULL, :det, CURRENT_TIMESTAMP)
            """), {'det': _json_mig.dumps({'reset_count': reset_count, 'cutoff_utc': '2026-04-22 00:00:00'})})
            conn.commit()
        except Exception as _me:
            print(f"[DB] audit_log marker insert skipped: {_me}")
    except Exception as e:
        print(f"[DB] Phase 08 success reset skipped: {e}")
    ```

    **Block D — conversation_logs.anrede Column (D-14):**
    ```python
    # ── Phase 08 D-14: conversation_logs.anrede (PreCall Du/Sie Override) ──────
    for col, typedef in [('anrede', 'VARCHAR(10)')]:
        try:
            conn.execute(text(f'ALTER TABLE conversation_logs ADD COLUMN {col} {typedef}'))
            conn.commit()
            print(f"[DB] Migration: added conversation_logs.{col}")
        except Exception:
            pass
    ```

    **Block E — prompt_versions.is_default Column + Backfill (D-26):**
    ```python
    # ── Phase 08 D-26: prompt_versions.is_default + backfill is_active → default ──
    try:
        conn.execute(text("ALTER TABLE prompt_versions ADD COLUMN is_default BOOLEAN DEFAULT 0"))
        conn.commit()
        print("[DB] Migration: added prompt_versions.is_default")
        # Backfill: bestehende is_active=1 Rows bekommen is_default=1 (single-variant-semantik bleibt erhalten)
        conn.execute(text("UPDATE prompt_versions SET is_default = 1 WHERE is_active = 1"))
        conn.commit()
        print("[DB] Migration v08: prompt_versions.is_default backfilled from is_active=1")
    except Exception:
        pass
    ```

    **Reihenfolge enforced:** A (Backup) MUSS vor B (Rebuild) und C (Reset) stehen. Reihenfolge A→B→C→D→E.

    Alle 5 Blöcke werden sequenziell in _migrate() ergänzt, hinter bestehenden Blöcken (NACH prompt_versions-Block, VOR `_seed_prompt_versions(db=...)`-Call falls vorhanden). Jeder Block hat einen Marker-Kommentar `# ── Phase 08 ...` zur schnellen grep-Identifikation.

    Die existierenden Migrations-Blöcke bleiben unberührt. Kein Entfernen, nur Appendieren.

    Umlaut-Regel: Kommentare dürfen Umlaute, SQL-Identifier (Tabellen, Spalten) bleiben ASCII. Log-Prefixes `[DB]` konsistent zu CLAUDE.md line 155.
  </action>
  <verify>
    <automated>python -c "
from database.db import SessionLocal, engine
from sqlalchemy import text
db = SessionLocal()
try:
    # PRAGMA-Check für objection_events.success nullable
    rows = db.execute(text('PRAGMA table_info(objection_events)')).fetchall()
    success_col = [r for r in rows if r[1] == 'success']
    assert success_col and success_col[0][3] == 0, f'objection_events.success still NOT NULL: {success_col}'
    # anrede column in conversation_logs
    cl_cols = [r[1] for r in db.execute(text('PRAGMA table_info(conversation_logs)')).fetchall()]
    assert 'anrede' in cl_cols, f'anrede missing: {cl_cols}'
    # is_default in prompt_versions
    pv_cols = [r[1] for r in db.execute(text('PRAGMA table_info(prompt_versions)')).fetchall()]
    assert 'is_default' in pv_cols, f'is_default missing: {pv_cols}'
    # Backfill check
    active_not_default = db.execute(text('SELECT COUNT(*) FROM prompt_versions WHERE is_active = 1 AND (is_default IS NULL OR is_default = 0)')).scalar()
    assert active_not_default == 0, f'{active_not_default} is_active rows missing is_default=1 backfill'
    # Backup file
    import os
    assert os.path.exists('database/nerve.db.bak_pre_v08_01'), 'Backup file missing'
    # Audit-log marker
    marker_count = db.execute(text(\"SELECT COUNT(*) FROM audit_log WHERE action = 'migration_v08_01_reset_success_polish38_1'\")).scalar()
    assert marker_count >= 1, f'audit_log marker missing (found {marker_count})'
    print('OK: all 5 migration blocks verified')
finally:
    db.close()
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 08 D-01.*objection_events.success" app.py` → exit 0 (Block B marker)
    - `grep -n "Phase 08 D-02.*Reset POLISH-38.1" app.py` → exit 0 (Block C marker)
    - `grep -n "Phase 08 D-14.*conversation_logs.anrede" app.py` → exit 0 (Block D marker)
    - `grep -n "Phase 08 D-26.*prompt_versions.is_default" app.py` → exit 0 (Block E marker)
    - `grep -n "migration_v08_01_reset_success_polish38_1" app.py` → exit 0 (audit marker action string)
    - `grep -n "nerve.db.bak_pre_v08_01" app.py` → exit 0 (Backup Path Block A)
    - `grep -n "2026-04-22 00:00:00" app.py` → exit 0 (Cutoff-Timestamp Block C)
    - `grep -n "CREATE TABLE objection_events_new" app.py` → exit 0 (Table-Rebuild SQL)
    - `ls -la database/nerve.db.bak_pre_v08_01` → file exists, size > 0
    - Python-Verify-Block oben → exit 0 (5 assertions grün)
    - Idempotenz: `python -c "from app import _migrate; _migrate(); _migrate()"` → zweiter Run wirft keine Exception (Swallow-Pattern)
  </acceptance_criteria>
  <done>app.py _migrate() enthält alle 5 Blöcke (A-E), Backup-File existiert, audit_log-Marker vorhanden, PRAGMA-Check bestätigt 3 Schema-Änderungen, Re-Run von _migrate() ist fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 3: docs/phase-08-training-vs-live-prompt-gap.md (Profile-Feld → System-Prompt Wiring-Matrix, D-46)</name>
  <files>docs/phase-08-training-vs-live-prompt-gap.md</files>
  <read_first>
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-RESEARCH.md Focus Area 1 (Zeilen 158-190) komplette Gap-Matrix
    - services/claude_service.py Zeilen 258-394 (_build_system_prompt() — die Quelle für "IST"-Spalte)
    - services/training_service.py Zeilen 499-619 (KUNDEN_PROMPT_TEMPLATE / build_customer_prompt — Referenz für Training-Prompt-Struktur)
    - database/models.py Zeile 121-140 (Profile-Model, inkl. branche-Column die NICHT in _build_system_prompt gelesen wird)
    - .planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-CONTEXT.md (D-07, D-08, D-11, D-15, D-46, D-47)
  </read_first>
  <action>
    Erstelle docs/phase-08-training-vs-live-prompt-gap.md als Markdown-Doc mit diesen Sektionen (KEINE v2-Prompt-Implementierung hier — das passiert in Wave 2):

    **Struktur:**
    ```markdown
    # Phase 08: Training-EWB-Prompt vs. Live-EWB-Prompt Gap-Analyse

    **Erstellt:** 2026-04-22 (Wave 1 Plan 01 Task 3)
    **Quelle:** RESEARCH Focus Area 1 + Direkte Code-Inspektion
    **Ziel:** Input für Wave 2 Plan 02 (v2-modular Prompt-Text in prompt_versions)

    ## 1. Scope

    Dieser Gap-Report dokumentiert welche Profil-Felder der heutige Live-EWB-Prompt
    (`services/claude_service.py` line 258-394 `_build_system_prompt()`) liest,
    welche das v2-modular Prompt-Template (Vault `NERVE Phase 08 EWB-Qualität.md`
    Teil 4) lesen SOLL, und welche Änderungen Wave 2 Plan 02 an
    `services/ewb_pipeline.py` vornehmen MUSS.

    ## 2. Gap-Matrix

    [Tabelle analog RESEARCH Focus Area 1 — Live-Prompt IST vs. Vault-Template SOLL,
     mit 14 Zeilen:]
    - Produkt-Beschreibung, USPs, Zielgruppe (keine Gap)
    - branche (KRITISCHE LÜCKE — Profile.branche legacy column wird NICHT gelesen)
    - branche_kontext (neu, D-11)
    - eigene_formulierungen (neu, D-07)
    - beweise (neu, D-08)
    - Anrede-Constraint (GAP — IST nur "einhalten", SOLL "NIEMALS wechseln" D-15)
    - Baustein-Struktur Anker/Reframe/Beweis/Überleitung (STRUKTUR-LÜCKE)
    - Active-Listening-Block (D-47, neu)
    - 45-Wort-Constraint (neu)
    - Niemals-apologetisch-Regel (verstärken)
    - Lernkarten-Matching (Live-only, bleibt)
    - Gegenfrage-Pflicht (keine Gap)

    ## 3. Konsequenzen für Wave 2 Plan 02

    3 Änderungen in `services/ewb_pipeline.py` build_ewb_prompt():
    1. Lese `profile.branche` (Top-Level-Column) UND `daten.basis.branche_kontext`.
    2. Lese `daten.basis.eigene_formulierungen` (List) + `daten.basis.beweise` (List).
    3. Ersetze alten Anrede-Output durch D-15 wortwörtlichen Text.

    ## 4. v2-Prompt-Struktur (Ziel)

    [Baustein-Reihenfolge in v2-modular:]
    - Kontext-Block (Profil-Kontext-String aus build_profile_context())
    - Bausteine (4): Anker → Reframe → Kern-Gegenargument + Beweis → Überleitung/Alt-Close
    - Active-Listening-Block (D-47)
    - Harte Regeln (max 45 Wörter, keine Apologetik, Anrede konstant)

    ## 5. Anti-Regression-Checks

    - `profile.branche` muss in Live-Prompt sichtbar sein (grep nach Branche:|branche: in generated prompt)
    - Anrede-Text muss "Wechsle NIEMALS" enthalten
    - bestehende USPs/Zielgruppe/Konsequenz-Ausgaben dürfen NICHT entfernt werden (nur ergänzt)
    ```

    Konkrete Inhalte: Die Gap-Matrix-Tabelle MUSS alle 14 Zeilen aus RESEARCH Focus Area 1 enthalten, mit identischer Kategorisierung (Gap/Lücke/keine/neu/verstärken). Die "Konsequenzen"-Sektion MUSS die 3 konkreten Code-Stellen (Zeile-Ranges in services/claude_service.py) nennen, die Wave 2 anfassen wird.

    KEINE v2-Prompt-Text-Implementation — der kommt in Wave 2. Dies ist Planning-Doc.

    Umlaut-Regel: User-facing Doku-Text mit echten Umlauten erlaubt (kein Code). Code-Snippets/Identifier bleiben ASCII.
  </action>
  <verify>
    <automated>test -f docs/phase-08-training-vs-live-prompt-gap.md && wc -l docs/phase-08-training-vs-live-prompt-gap.md | awk '{if($1 < 40) exit 1}' && grep -q "Gap-Matrix" docs/phase-08-training-vs-live-prompt-gap.md && grep -q "_build_system_prompt" docs/phase-08-training-vs-live-prompt-gap.md && grep -q "branche_kontext" docs/phase-08-training-vs-live-prompt-gap.md && grep -q "eigene_formulierungen" docs/phase-08-training-vs-live-prompt-gap.md && grep -q "beweise" docs/phase-08-training-vs-live-prompt-gap.md && grep -q "Wechsle NIEMALS" docs/phase-08-training-vs-live-prompt-gap.md && grep -q "Active-Listening" docs/phase-08-training-vs-live-prompt-gap.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/phase-08-training-vs-live-prompt-gap.md` → exit 0
    - `wc -l docs/phase-08-training-vs-live-prompt-gap.md` → ≥ 40 Zeilen (substantial content)
    - `grep -c "^| " docs/phase-08-training-vs-live-prompt-gap.md` → ≥ 14 (Gap-Matrix-Tabelle mit allen 14 Zeilen aus RESEARCH Focus Area 1)
    - `grep -n "Gap-Matrix\|branche_kontext\|eigene_formulierungen\|beweise\|Wechsle NIEMALS\|Active-Listening\|Anker.*Reframe.*Beweis" docs/phase-08-training-vs-live-prompt-gap.md` → alle 7 Keywords gefunden
    - `grep -n "services/ewb_pipeline.py\|services/claude_service.py" docs/phase-08-training-vs-live-prompt-gap.md` → exit 0 (konkrete Code-Stellen genannt)
    - Dokument referenziert explizit D-07, D-08, D-11, D-15, D-46, D-47
  </acceptance_criteria>
  <done>Gap-Matrix-Doku existiert mit ≥40 Zeilen + ≥14 Tabellen-Zeilen, nennt alle 3 Änderungs-Code-Stellen, fungiert als Spec-Input für Wave 2 Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration ↔ DB-Dateisystem | _migrate() schreibt DESTRUKTIV in production DB via raw SQL — kein User-Input, aber Pre-Backup ist Rollback-Gate. |
| Backfill UPDATE ↔ prompt_versions | `is_default = 1 WHERE is_active = 1` ändert bestehende Daten — Integritäts-Risiko bei gleichzeitiger Admin-Modification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | D-02 destruktiver UPDATE auf objection_events.success | mitigate | Pre-Backup `database/nerve.db.bak_pre_v08_01` VOR UPDATE (Block A in Task 2). audit_log-Marker-Row mit action='migration_v08_01_reset_success_polish38_1' für Forensik. |
| T-08-01-02 | Integrity | Table-Rebuild verliert Daten wenn INSERT SELECT unvollständig | mitigate | Ganze Rebuild-Transaktion in einem try-Block. Bei Exception → Backup-File bleibt verfügbar für manuelle Recovery. Rowcount-Logging in print-Output zur visuellen Kontrolle. |
| T-08-01-03 | Denial | Concurrent _migrate()-Run bei Prozess-Parallelism | accept | Migrations laufen 1× beim App-Startup (single-threaded vor Server-Bind). Flask-Dev-Server + Gunicorn hot-reload beide single-worker. Kein Lock nötig. |
| T-08-01-04 | Information Disclosure | Backup-File database/nerve.db.bak_pre_v08_01 wird in Repo eingecheckt | mitigate | Keine Git-Tracking: prüfen ob .gitignore `database/*.db` und `database/*.bak*` schon enthält. Falls nicht, in Task 2 Acceptance ergänzen. Backup ist NUR lokal auf VPS. |
| T-08-01-05 | Tampering | Cutoff-Timestamp '2026-04-22 00:00:00' als String-Literal ohne Validierung | accept | Hardcoded Literal im Code, kein User-Input. Dokumentiert als "konservativer Cutoff" in Kommentar + docs. POLISH-38.1 Commit 585f567 vom 2026-04-21 15:11 +0200 ist 2026-04-21 13:11 UTC, also < Cutoff. |
</threat_model>

<verification>
Alle 3 Tasks müssen atomar grün werden vor Wave 2:
1. `python -c "from database.models import ObjectionEvent, ConversationLog, PromptVersion; ..."` → Model-Schema korrekt.
2. PRAGMA-Check auf live DB → Schema auf Disk ist konsistent zum Model (migrations lief).
3. docs/phase-08-training-vs-live-prompt-gap.md existiert mit ≥40 Zeilen.

Idempotenz-Smoke-Test:
```bash
python -c "from app import _migrate; _migrate()"  # erster Lauf — migriert alles
python -c "from app import _migrate; _migrate()"  # zweiter Lauf — schweigt (alles existiert schon)
```
</verification>

<success_criteria>
- [ ] database/models.py: 3 Column-Änderungen (success nullable, anrede, is_default) committed
- [ ] app.py _migrate(): 5 neue Blöcke (A-E) committed, alle mit `# ── Phase 08 ...` Markern
- [ ] database/nerve.db.bak_pre_v08_01 existiert (>0 bytes) — Rollback-Path verfügbar
- [ ] audit_log hat ≥1 Row mit action='migration_v08_01_reset_success_polish38_1'
- [ ] docs/phase-08-training-vs-live-prompt-gap.md ≥40 Zeilen, ≥14 Matrix-Zeilen
- [ ] Re-Run von _migrate() wirft keine Exception (Idempotenz)
- [ ] Umlaut-Regel eingehalten: Kommentare mit Umlauten OK, SQL-/Python-Identifier ASCII
- [ ] Keine bestehenden Migrations-Blöcke in _migrate() entfernt oder modifiziert
</success_criteria>

<output>
Summary: `.planning/phases/08-ewb-qualit-t-profil-tiefe-launch-kritische-prompt-iteration-/08-01-SUMMARY.md`

Muss dokumentieren:
- Tatsächlicher reset_count aus Block C (wie viele POLISH-38.1 Alt-Daten auf NULL gesetzt wurden)
- Tatsächlicher backfill-count aus Block E (wie viele is_active=1 → is_default=1 übertragen)
- Backup-File-Größe (sanity check)
- Offene Frage für Wave 2: Sollen die 4 Legacy-Module (`assistant_live`, `coaching_live`, `objection_trigger`, `api_frage`, `training_persona`) in Phase 08 mitmigrieren (is_default=1 bleibt) oder als Risiko dokumentiert (siehe RESEARCH Open Question 2 — Empfehlung: minimalen Scope halten, nur ewb migriert)
</output>
</content>
</invoke>