---
phase: 03.1-frontend-redesign
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - templates/dashboard.html
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Dashboard page uses NERVE glass panels instead of old #1a1a2e cards"
    - "Stats circles use NERVE KPI cards with design system colors"
    - "Activity feed uses .n-feed-item components"
    - "Quick action buttons use .n-btn-* classes"
    - "All dashboard-specific inline styles replaced with nerve.css classes where applicable"
  artifacts:
    - path: "templates/dashboard.html"
      provides: "Dashboard page using NERVE design system components"
      contains: "n-glass"
  key_links:
    - from: "templates/dashboard.html"
      to: "static/nerve.css"
      via: "inherits from base.html link"
      pattern: "n-glass|n-kpi|n-feed-item"
---

<objective>
Redesign the Dashboard page to use NERVE Design System components.

Purpose: The dashboard is the most-seen page after login. Migrating it to the NERVE design system gives users an immediate visual upgrade with glass panels, proper typography, and consistent component language.

Output: Updated `templates/dashboard.html`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-frontend-redesign/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-redesign/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dashboard inline CSS with NERVE design system classes</name>
  <files>templates/dashboard.html</files>
  <read_first>
    - templates/dashboard.html (current full file — understand all sections: greeting, nudge, usage, stats, level, heatmap, ROI, recent, quick actions, toasts)
    - static/nerve.css (confirm .n-glass, .n-kpi, .n-feed-item, .n-badge-*, .n-btn-*, .n-metric classes exist)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\07 Anhänge\NERVE-Design-System.html (reference for component usage patterns)
    - static/app.js (NOT needed for dashboard — dashboard.html has its own inline JS, app.js is live-session only)
  </read_first>
  <action>
    Rewrite templates/dashboard.html {% block extra_style %} and {% block content %} to use NERVE components. Preserve ALL Jinja2 logic, variables, and conditionals exactly.

    **{% block extra_style %} changes:**
    Remove ALL dashboard-specific CSS classes that now have NERVE equivalents. Keep ONLY styles for dashboard-specific elements that have no NERVE equivalent (stat circles SVG animation, heatmap grid, nudge card, toast animations, level card progress bar). Specifically:

    - REMOVE: .stat-card (use .n-glass), .activity-card/.quick-card (use .n-glass), .usage-card/.roi-card (use .n-glass), .level-card/.heatmap-card (use .n-glass)
    - KEEP (modified to work with NERVE tokens): .stat-circle, .stat-circle-bg, .stat-circle-fg, .stat-value (SVG circle animation — no NERVE equivalent)
    - KEEP: .heat-0 through .heat-3 (heatmap cell colors — update to use NERVE teal: heat-3 should be #2dd4a8 instead of #E8B040)
    - KEEP: .nudge-card (unique component — update colors: border-left use #2dd4a8, CTA button use .n-btn-primary)
    - KEEP: .toast, .toast-wrap, @keyframes slideIn (toast notification)
    - KEEP: .level-bar-wrap, .level-bar-fill (update fill color to #2dd4a8)

    Update remaining dashboard-specific CSS to use NERVE design tokens:
    - Background colors: #1a1a2e -> var(--glass-bg) or use .n-glass class
    - Text colors: #6b6b80 -> var(--page-text-muted), #b8b0a0 -> var(--page-text-secondary), #f0ebe0 -> var(--page-text-color)
    - Gold accent: #E8B040 -> #2dd4a8 (NERVE primary teal) where it's used as accent, keep gold for KI/AI-related elements
    - Border colors: #1a1a2e -> rgba(255,255,255,0.06)
    - Font references: remove 'DM Sans', rely on body font from nerve.css (Inter)

    **{% block content %} class replacements:**

    | Old Class | New Class | Where |
    |-----------|-----------|-------|
    | .stat-card | .n-glass (add to div) | Stats circles row |
    | .stat-label | .n-kpi-label | Stat circle labels |
    | .activity-card | .n-glass | Recent activities section |
    | .quick-card | .n-glass | Quick actions section |
    | .usage-card | .n-glass | Usage widget |
    | .roi-card | .n-glass | ROI tracker |
    | .level-card | .n-glass | Level & achievements |
    | .heatmap-card | .n-glass | Activity heatmap |
    | .ea-banner | .n-glass (with custom border) | Early access banner |
    | .section-title | .n-kpi-label (reuse for section labels) | Section headers |
    | .badge.badge-teal | .n-badge .n-badge-primary | Status badges |
    | .badge.badge-gray | .n-badge .n-badge-neutral | Category badges |
    | .quick-btn | .n-btn .n-btn-ghost | Quick action buttons |
    | .greeting-streak | keep class, update colors in extra_style | Streak badge |
    | .activity-item | .n-feed-item | Recent activity rows |
    | .activity-time | .n-feed-time | Activity timestamps |
    | .activity-desc | .n-feed-text | Activity descriptions |

    For the ROI tracker grid, usage bars, and stat circles: keep the custom HTML structure but wrap sections in .n-glass panels.

    **Color migration in inline styles:**
    - `style="color:#E8B040"` for accent highlights -> `style="color:var(--page-text-color)"` or use `.text-accent` helper
    - `style="color:#22c55e"` for positive values -> `.text-success`
    - `style="color:#6b6b80"` -> `.text-muted`
    - `style="background:#E8B040"` for progress bars -> `background:#2dd4a8`
    - Usage bar fill colors: keep the conditional Jinja2 logic but update #E8B040 -> #2dd4a8 for normal state

    **PRESERVE exactly:**
    - All `{% if %}` / `{% for %}` / `{% endif %}` blocks
    - All `{{ variable }}` template expressions
    - All JavaScript in `<script>` tags (circle animation, heatmap generation, nudge logic, toast logic)
    - The SVG circle structure for stat circles
    - All data-* attributes and element IDs used by JavaScript
  </action>
  <verify>
    <automated>grep -c "n-glass\|n-kpi-label\|n-feed-item\|n-btn.*n-btn-ghost\|n-badge" templates/dashboard.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/dashboard.html contains `n-glass` (at least 6 occurrences: stats cards, level, heatmap, ROI, activity, quick actions, usage)
    - templates/dashboard.html contains `n-kpi-label`
    - templates/dashboard.html contains `n-feed-item`
    - templates/dashboard.html contains `n-btn` with `n-btn-ghost` (quick action buttons)
    - templates/dashboard.html contains `n-badge`
    - templates/dashboard.html still contains `{% if usage %}` (usage widget conditional preserved)
    - templates/dashboard.html still contains `{% for log in recent_logs` (activity loop preserved)
    - templates/dashboard.html still contains `{{ stats.avg_kb }}` (template variables preserved)
    - templates/dashboard.html still contains `animate('circle-kb'` (JS animation preserved)
    - templates/dashboard.html still contains `id="heatmap-container"` (heatmap JS target preserved)
    - {% block extra_style %} is significantly smaller (most card/section styles removed, only SVG circles, heatmap cells, nudge, toast, level bar remain)
  </acceptance_criteria>
  <done>Dashboard page renders with NERVE glass panels, teal accents, Inter font, and consistent badge/button/feed components. All Jinja2 logic and JavaScript preserved.</done>
</task>

</tasks>

<verification>
1. Visit /dashboard — page renders with dark glass panels, not flat #1a1a2e backgrounds
2. Stats circles animate correctly
3. Heatmap renders
4. Quick action buttons show ghost button styling
5. Recent activities show feed-item dots
6. All Jinja2 variables render (no template errors)
</verification>

<success_criteria>
- Dashboard uses .n-glass panels for all card sections
- No #1a1a2e hardcoded backgrounds remain in the extra_style block (all migrated to NERVE tokens)
- All interactive elements (buttons, badges) use .n-btn-* and .n-badge-* classes
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-frontend-redesign/03.1-02-SUMMARY.md`
</output>
