---
phase: 03.1-frontend-redesign
plan: 06
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - templates/profile_wizard.html
  - templates/onboarding.html
autonomous: true
requirements:
  - UI-07
must_haves:
  truths:
    - "Profile wizard uses NERVE design tokens for colors and typography"
    - "Onboarding uses NERVE design tokens for colors and typography"
    - "Form inputs match NERVE input styling"
    - "Buttons use NERVE button styling"
    - "All wizard/onboarding JS logic preserved (step navigation, branche selection, einwand chips)"
  artifacts:
    - path: "templates/profile_wizard.html"
      provides: "Profile wizard using NERVE design system"
      contains: "var(--page-bg)"
    - path: "templates/onboarding.html"
      provides: "Onboarding using NERVE design system"
      contains: "var(--page-bg)"
  key_links:
    - from: "templates/profile_wizard.html"
      to: "static/nerve.css"
      via: "link tag (standalone page)"
      pattern: "nerve\\.css"
---

<objective>
Redesign the Profile Wizard and Onboarding pages to use NERVE Design System tokens.

Purpose: These are standalone pages (they do NOT extend base.html) that new users see during first setup. Consistent NERVE styling here creates a polished first impression.

Output: Updated `templates/profile_wizard.html` and `templates/onboarding.html`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-frontend-redesign/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-redesign/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate profile_wizard.html to NERVE design tokens</name>
  <files>templates/profile_wizard.html</files>
  <read_first>
    - templates/profile_wizard.html (FULL file — standalone page extending base.html via {% extends "base.html" %}, has a `<style>` block inside {% block content %}, uses CSS custom properties like var(--color-amber), var(--color-border) that are currently UNDEFINED unless defined elsewhere)
    - static/nerve.css (source of available custom properties)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\07 Anhänge\NERVE-Design-System.html
  </read_first>
  <action>
    profile_wizard.html extends base.html BUT has its own `<style>` block inside {% block content %}. It references CSS variables like `var(--color-amber)`, `var(--color-border)`, `var(--color-bg)`, `var(--color-card)`, `var(--color-text-primary)`, `var(--color-text-secondary)`, `var(--font-headline)`, `var(--font-body)`, `var(--radius-md)`, `var(--radius-lg)`, `var(--space-xs)`, `var(--space-sm)`, `var(--space-md)`, `var(--space-lg)` — these are NOT defined in the current system.

    **Step 1: Replace all undefined CSS variables with NERVE equivalents:**

    | Old Variable | NERVE Replacement |
    |-------------|-------------------|
    | `var(--color-amber)` | `#2dd4a8` (NERVE primary teal, not amber) |
    | `var(--color-bg)` | `var(--page-bg)` |
    | `var(--color-card)` | `var(--glass-bg)` |
    | `var(--color-border)` | `rgba(255,255,255,0.08)` |
    | `var(--color-text-primary)` | `var(--page-text-color)` |
    | `var(--color-text-secondary)` | `var(--page-text-secondary)` |
    | `var(--font-headline)` | `var(--page-font)` |
    | `var(--font-body)` | `var(--page-font)` |
    | `var(--radius-md)` | `12px` (NERVE input radius) |
    | `var(--radius-lg)` | `var(--glass-radius)` or `20px` |
    | `var(--space-xs)` | `4px` |
    | `var(--space-sm)` | `8px` |
    | `var(--space-md)` | `16px` |
    | `var(--space-lg)` | `24px` |

    Also update `color-mix(in srgb, var(--color-amber) 8%, transparent)` references to use `rgba(45,212,168,0.08)` (teal tint).

    **Step 2: Update component styling:**

    - `.branche-card.selected` border-color: `#2dd4a8`
    - `.branche-card:hover` border-color: `#2dd4a8`
    - `.einwand-chip.selected` border-color: `#2dd4a8`, background: `rgba(45,212,168,0.08)`
    - `.einwand-chip:hover` border-color: `#2dd4a8`
    - `.progress-dot.active` / `.progress-dot.completed`: background `#2dd4a8`, border-color `#2dd4a8`
    - `.progress-line.completed`: background `#2dd4a8`
    - `.btn-primary`: background `#2dd4a8`, match `.n-btn-primary` styling (gradient from `#2dd4a8` to `#20b090`)
    - `.btn-secondary`: match `.n-btn-ghost` styling
    - `.form-input:focus` / `.form-textarea:focus`: border-color `#2dd4a8`

    **Step 3: Move the `<style>` block** from inside {% block content %} to {% block extra_style %} (proper Jinja2 pattern).

    **PRESERVE exactly:**
    - All Jinja2 blocks and template variables
    - Form action: `{{ url_for('profiles.wizard_create') }}`
    - All JavaScript: nextStep, prevStep, updateNav, updateProgress, selectBranche, toggleEinwand, validateSubmit
    - All element IDs: step-1/2/3, dot-1/2/3, line-1/2, brancheInput, einwaendeInput
    - All onclick handlers
    - Hidden input fields
  </action>
  <verify>
    <automated>grep -c "var(--page-bg)\|var(--page-text\|2dd4a8\|var(--glass" templates/profile_wizard.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/profile_wizard.html contains `var(--page-bg)` (at least 1 occurrence)
    - templates/profile_wizard.html contains `#2dd4a8` (at least 3 occurrences for teal accent)
    - templates/profile_wizard.html does NOT contain `var(--color-amber)` (undefined variable replaced)
    - templates/profile_wizard.html does NOT contain `var(--font-headline)` (undefined variable replaced)
    - templates/profile_wizard.html still contains `selectBranche` (JS function preserved)
    - templates/profile_wizard.html still contains `toggleEinwand` (JS function preserved)
    - templates/profile_wizard.html still contains `wizard_create` (form action preserved)
  </acceptance_criteria>
  <done>Profile wizard uses NERVE design tokens, teal accents for selections, no undefined CSS variables remain</done>
</task>

<task type="auto">
  <name>Task 2: Migrate onboarding.html to NERVE design tokens</name>
  <files>templates/onboarding.html</files>
  <read_first>
    - templates/onboarding.html (FULL file — standalone page with own DOCTYPE, full `<style>` block. Multi-step onboarding: welcome, branche selection, company info, experience level, mic test, first profile creation)
    - static/nerve.css (source of NERVE tokens)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\07 Anhänge\NERVE-Design-System.html
  </read_first>
  <action>
    onboarding.html is a STANDALONE page (own `<!DOCTYPE html>`, NOT extending base.html). This is intentional for the fullscreen onboarding experience.

    **Step 1: Add nerve.css link** in `<head>`:
    ```html
    <link rel="stylesheet" href="{{ url_for('static', filename='nerve.css') }}">
    ```
    Add Lucide script tag.
    Replace Google Fonts link (Playfair + DM Sans) — nerve.css handles fonts.

    **Step 2: Token migration in `<style>` block:**

    Replace hardcoded colors:
    - `background:#0c0c18` -> `background:var(--page-bg)`
    - `color:#f0ebe0` -> `color:var(--page-text-color)`
    - `color:#8888a8` / `#6b6b80` -> `color:var(--page-text-muted)`
    - `border:... #2e2e48` / `#1a1a2e` -> `border:... rgba(255,255,255,0.08)` / `rgba(255,255,255,0.06)`
    - `#E8B040` -> `#2dd4a8` for primary actions (progress dots, button bg, active states, focus borders)
    - `background:#1a1a2e` -> `var(--glass-bg)` or `rgba(255,255,255,0.04)`
    - `rgba(232,176,64,...)` -> `rgba(45,212,168,...)` (teal tint equivalents)
    - `font-family:'DM Sans'` -> remove (inherits Inter from nerve.css body)
    - `font-family:'Playfair Display'` -> use `var(--page-font)` for logo

    **Step 3: Component updates:**

    - `.progress-dot.active` / `.progress-dot.done`: `#E8B040` -> `#2dd4a8`
    - `.progress-line.done`: `#E8B040` -> `#2dd4a8`
    - `.branche-card.selected`: border `#E8B040` -> `#2dd4a8`, background tint `rgba(45,212,168,0.08)`
    - `.branche-card:hover`: border `#E8B04066` -> `rgba(45,212,168,0.4)`
    - `.level-card.selected`: border `#E8B040` -> `#2dd4a8`, background `rgba(45,212,168,0.08)`
    - `.level-card:hover`: `#E8B04066` -> `rgba(45,212,168,0.4)`
    - `.form-input:focus` / `.form-select:focus` / `.form-textarea:focus`: `#E8B040` -> `#2dd4a8`
    - Logo `.logo-n`: border/color `#E8B040` -> `#2dd4a8`
    - Logo `.logo-sub`: color `#E8B040` -> `#2dd4a8`
    - `.profile-result`: border `#E8B04033` -> `rgba(45,212,168,0.2)`, color `#E8B040` -> `#2dd4a8`
    - Navigation buttons: `.nav-btn-primary` background `#E8B040` -> gradient `linear-gradient(135deg,#2dd4a8,#20b090)`
    - `.how-num`: background/border/color from gold to teal
    - `.play-icon`: border/background from gold tints to teal tints
    - `.video-placeholder:hover`: border from gold to teal
    - Mic button: `#E8B040` -> `#2dd4a8`

    **Step 4: Update logo:**
    - `.logo-n` border/color: `#E8B040` -> `#2dd4a8`
    - `.logo-word` font: `'DM Sans'` -> remove (inherits Inter)
    - `.logo-sub` color: `#E8B040` -> `#2dd4a8`

    **PRESERVE exactly:**
    - All Jinja2: `{{ url_for() }}`, form actions, CSRF tokens if any
    - All JavaScript: step navigation, branche/level selection, mic test, profile creation
    - All element IDs used by JS
    - Step show/hide mechanism (.step.active toggle)
    - Microphone permission test logic
    - The standalone page structure (own DOCTYPE, not extending base.html)
  </action>
  <verify>
    <automated>grep -c "nerve.css\|var(--page-bg)\|2dd4a8\|var(--page-text\|var(--glass" templates/onboarding.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/onboarding.html contains `nerve.css` link
    - templates/onboarding.html contains `var(--page-bg)` (at least 2 occurrences)
    - templates/onboarding.html contains `#2dd4a8` (at least 5 occurrences replacing #E8B040)
    - templates/onboarding.html does NOT contain `font-family:'DM Sans'` in style block
    - templates/onboarding.html does NOT contain `font-family:'Playfair Display'` in style block
    - templates/onboarding.html still contains `<!DOCTYPE html>` (standalone page structure)
    - templates/onboarding.html still contains step navigation JS (nextStep/prevStep or equivalent)
    - templates/onboarding.html still contains microphone test logic (getUserMedia or similar)
  </acceptance_criteria>
  <done>Onboarding page uses NERVE design tokens, teal accents throughout, Inter font, consistent with rest of the app</done>
</task>

</tasks>

<verification>
1. Visit the profile wizard page — renders with teal accents, progress dots, NERVE-styled inputs
2. Branche selection highlights with teal border
3. Einwand chips highlight with teal
4. Form submission works
5. Visit onboarding — renders with teal logo, progress dots, NERVE colors
6. All onboarding steps navigate correctly
7. Mic test works
</verification>

<success_criteria>
- Both wizard and onboarding pages use NERVE teal instead of old gold accent
- No undefined CSS variables remain
- All interactive JS preserved
- Visual consistency with the rest of the app
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-frontend-redesign/03.1-06-SUMMARY.md`
</output>
