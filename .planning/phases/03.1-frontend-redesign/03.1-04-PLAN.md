---
phase: 03.1-frontend-redesign
plan: 04
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - templates/training.html
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Training setup page uses NERVE glass panels for scenario/difficulty cards"
    - "Pill tabs use .n-pill / .n-pill-active for mode selection"
    - "Buttons use .n-btn-* classes"
    - "Chat phase uses NERVE tokens for colors"
    - "All training JS logic preserved (scenario selection, chat, voice, timer)"
  artifacts:
    - path: "templates/training.html"
      provides: "Training page using NERVE design system"
      contains: "n-glass"
  key_links:
    - from: "templates/training.html"
      to: "static/nerve.css"
      via: "inherits from base.html"
      pattern: "n-glass|n-pill|n-btn"
---

<objective>
Redesign the Training page to use NERVE Design System components.

Purpose: The training page has three phases (setup, call screen, chat). Migrating to NERVE design system gives consistent styling for scenario cards, difficulty selection, and the chat interface.

Output: Updated `templates/training.html`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-frontend-redesign/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-redesign/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate training.html inline CSS to NERVE design tokens and components</name>
  <files>templates/training.html</files>
  <read_first>
    - templates/training.html (FULL file — three-phase training UI: setup with difficulty cards and scenario selector, call screen with ring animation, chat with messages, voice input, help suggestions, scoring)
    - static/nerve.css (confirm .n-glass, .n-pill, .n-btn-*, .n-badge-*, .n-input classes exist)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\07 Anhänge\NERVE-Design-System.html (reference for component patterns)
  </read_first>
  <action>
    training.html extends base.html and uses {% block extra_style %} for its CSS.

    **{% block extra_style %} changes — token migration:**

    Replace hardcoded colors with NERVE tokens throughout the inline CSS:
    - `background:#0c0c18` -> `background:var(--page-bg)`
    - `color:#f0ebe0` -> `color:var(--page-text-color)`
    - `color:#6b6b80` -> `color:var(--page-text-muted)`
    - `border:... #1a1a2e` -> `border:... rgba(255,255,255,0.06)`
    - `#E8B040` (accent) -> `#2dd4a8` for primary actions (call button, active states), keep `#d4a853` for KI/AI elements
    - `background:#1a1a2e` -> `var(--glass-bg)` or `rgba(255,255,255,0.04)`
    - Remove `font-family` references (inherits Inter from body)

    **Specific class migrations:**

    | Old Training Class | Migration Strategy |
    |-------------------|-------------------|
    | `.t-diff-card` | Add `.n-glass` alongside, update active state border to #2dd4a8 |
    | `.t-diff-card.t-diff-active` | border-color: #2dd4a8 (was #E8B040) |
    | `.t-call-btn` | Replace with `.n-btn .n-btn-primary .n-btn-lg` class list |
    | `.t-avatar` | Add `.n-avatar .n-avatar-md` alongside existing class |
    | `.t-bubble-in` | Update background to `var(--glass-bg)`, border-radius keep custom |
    | `.t-bubble-out` | border-left: 3px solid #d4a853 (KI accent), background with accent tint |
    | `.t-help-btn` | Add `.n-btn .n-btn-accent .n-btn-sm` |
    | `.t-ptt-btn` | Update background: #2dd4a8 (was #E8B040) |
    | `.t-ptt-btn.t-recording` | Keep red recording state (#e05c5c) |
    | `.t-text-input` | Add `.n-input` alongside, or update to match .n-input styling |
    | `.t-timer` | color: #2dd4a8 (was #E8B040) |
    | `.t-msg-name` | color: #d4a853 (KI accent for sender labels) |

    **Keep all training-specific classes** — they handle the unique three-phase UI, call screen animation, chat layout, voice recording. Only update their color values.

    **{% block content %} class additions:**

    Add NERVE classes alongside existing training classes where applicable:
    - Difficulty cards: add `n-glass` to `.t-diff-card` divs
    - Form selects: update `class="form-select"` to include `.n-input` characteristics
    - Section labels: `.t-section-label` can be styled similar to `.n-kpi-label`

    **Color updates in HTML inline styles:**
    - `color:#E8B040` -> `color:#2dd4a8` for primary accent
    - `color:#e05c5c` -> keep (danger color, same in NERVE system)
    - `background:#E8B040` -> `background:#2dd4a8` for CTA elements

    **PRESERVE exactly:**
    - All Jinja2 blocks: `{% for scenario in scenarios %}`, `{% if %}`, `{{ var }}`
    - All JavaScript functions: scenario selection, difficulty selection, chat logic, voice recording, timer, help suggestions, scoring
    - All element IDs used by JavaScript
    - All data-* attributes
    - The t-overlay / t-hidden toggle mechanism
    - Call screen ring animation (@keyframes pulse-ring)
    - Recording animation (@keyframes pulse-rec)
    - Chat message animation (@keyframes msg-in)
    - The ElevenLabs voice playback logic
    - Training mode selection (frei/gefuehrt)
  </action>
  <verify>
    <automated>grep -c "n-glass\|n-btn\|--page-bg\|--page-text\|--glass-bg\|2dd4a8" templates/training.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/training.html contains `n-glass` (at least 1 occurrence on difficulty cards)
    - templates/training.html contains `var(--page-bg)` (at least 2 occurrences)
    - templates/training.html contains `#2dd4a8` (NERVE teal replacing old gold accents)
    - templates/training.html still contains `{% for scenario in scenarios %}` (scenario loop preserved)
    - templates/training.html still contains `t-overlay` (phase transition mechanism preserved)
    - templates/training.html still contains `t-ptt-btn` (voice recording button preserved)
    - templates/training.html still contains `pulse-ring` (call animation preserved)
    - templates/training.html does NOT contain `font-family` in its extra_style block (inherits from nerve.css)
  </acceptance_criteria>
  <done>Training page uses NERVE design tokens for all colors, glass panels for cards, teal accents for primary actions, all training JS logic preserved</done>
</task>

</tasks>

<verification>
1. Visit /training — setup phase renders with glass-styled difficulty cards
2. Difficulty cards highlight with teal border on selection
3. Call button shows teal primary styling
4. Start a training — chat phase renders correctly with messages
5. Voice recording button works (teal, turns red when recording)
6. Scoring and post-training summary render
</verification>

<success_criteria>
- Training page visually matches NERVE design language
- All three training phases (setup, call, chat) render correctly
- Zero JS breakage in scenario selection, chat, or voice recording
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-frontend-redesign/03.1-04-SUMMARY.md`
</output>
