---
phase: 03.1-frontend-redesign
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - templates/app.html
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "Live-Session page uses NERVE design tokens for colors and typography"
    - "AI coaching panel uses .n-ai-panel class with gold border"
    - "Live badge uses .n-live-badge component"
    - "All existing JS class references in static/app.js still work (msg, sp0, sp1, coaching-card, etc.)"
  artifacts:
    - path: "templates/app.html"
      provides: "Live-Session page using NERVE design system"
      contains: "n-ai-panel"
  key_links:
    - from: "templates/app.html"
      to: "static/app.js"
      via: "JS class references must be preserved"
      pattern: "msg|sp0|sp1|coaching-card"
---

<objective>
Redesign the Live-Session page (app.html) to use NERVE Design System tokens and components.

Purpose: The live-session page is the core product interface. It is a standalone page (does NOT extend base.html) with its own full HTML document. The redesign must preserve all JavaScript interactions in static/app.js.

Output: Updated `templates/app.html`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-frontend-redesign/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-redesign/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app.html inline CSS to use NERVE design tokens and add nerve.css link</name>
  <files>templates/app.html</files>
  <read_first>
    - templates/app.html (FULL file — this is a standalone page, not extending base.html, with extensive inline CSS and complex structure: header, profile bar, phases bar, transcript panel, coaching panel, compact mode, post-call overlay, DSGVO banner)
    - static/app.js (CRITICAL — scan ALL classList/className references. These classes MUST be preserved: .msg, .sp0, .sp1, .interim, .final, .speaker-tag, .row-header, .row-swap-btn, .coaching-card, .painpoint, .tip, .paused, .visible, .kb-value, .ai-card, .kein-einwand, .einwand-typ, .typ-kein, .withdrawn, .spinner, .stern, .active, .hover, .checked, .frage-antwort-card, .ns-item, .bullet-item, .einwand-row, .kb-bar-item, .postcallOverlay, .frageOverlay, .copied, .removing)
    - static/nerve.css (confirm it exists with NERVE component classes)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\07 Anhänge\NERVE-Design-System.html (reference for .n-live-badge, .n-ai-panel, .n-btn-* usage)
  </read_first>
  <action>
    app.html is a STANDALONE page (not {% extends 'base.html' %}). It has its own `<head>`, `<body>`, and full `<style>` block. This is intentional for the live-session fullscreen experience.

    **Step 1: Add nerve.css link in `<head>`** (after the Socket.IO script):
    ```html
    <link rel="stylesheet" href="{{ url_for('static', filename='nerve.css') }}">
    ```
    Also add Lucide script tag.
    Replace the Google Fonts link (Playfair + DM Sans) with nothing (nerve.css handles fonts via @import).

    **Step 2: Update inline `<style>` block — token migration:**

    Replace hardcoded color values with CSS custom property references throughout the inline `<style>`:
    - `background:#0c0c18` -> `background:var(--page-bg)`
    - `color:#f0ebe0` -> `color:var(--page-text-color)`
    - `color:#6b6b80` / `#444466` -> `color:var(--page-text-muted)`
    - `border:... #1a1a2e` -> `border:... rgba(255,255,255,0.06)`
    - `color:#E8B040` (accent) -> keep as `#d4a853` (NERVE KI accent) for AI-related elements, use `#2dd4a8` for primary actions
    - `background:#1a1a2e` (cards/panels) -> `background:var(--glass-bg)` or use rgba(255,255,255,0.04)
    - `font-family:'DM Sans'` -> remove (inherits from body via nerve.css)
    - `font-family:'Playfair Display'` -> remove (logo handled differently)

    **Step 3: Replace specific components in HTML with NERVE classes:**

    | Element | Old Pattern | New Pattern |
    |---------|-------------|-------------|
    | Header buttons | `.btn` / `.btn-pause` / `.btn-log` | Add `.n-btn` alongside existing classes |
    | "Beenden" button | `.btn-beenden` | `.n-btn .n-btn-danger` (keep .btn-beenden for specific overrides) |
    | Live status dot + label | Custom `.dot` + `.status` | Add `.n-live-badge` wrapper around existing dot/status |
    | AI coaching panel | `.ai-panel` wrapper | Add `.n-ai-panel` class to the coaching panel container |
    | Back link | `.back-link` | `.n-btn .n-btn-ghost .n-btn-sm` |
    | Kaufbereitschaft badge | Custom `.kb-display` | Keep as-is (unique component) |
    | Phase buttons | `.phase-btn` / `.phase-btn.active` | Add `.n-pill` / `.n-pill-active` alongside existing classes |

    **CRITICAL — DO NOT RENAME/REMOVE these JS-referenced classes:**
    `.msg`, `.sp0`, `.sp1`, `.interim`, `.final`, `.speaker-tag`, `.row-header`, `.row-swap-btn`,
    `.coaching-card`, `.painpoint`, `.tip`, `.paused`, `.visible`, `.kb-value`, `.ai-card`,
    `.kein-einwand`, `.einwand-typ`, `.typ-kein`, `.withdrawn`, `.spinner`, `.stern`, `.active`,
    `.hover`, `.checked`, `.frage-antwort-card`, `.ns-item`, `.bullet-item`, `.einwand-row`,
    `.kb-bar-item`, `.postcallOverlay`, `.frageOverlay`, `.copied`, `.removing`,
    `.dot` (header live indicator), `#st`, `#tr`, `#ai`, `#wc`

    Strategy: ADD new .n-* classes alongside existing ones. Do not remove any class that app.js references.

    **Step 4: Update logo** in header:
    - `.nav-n` border/color: #E8B040 -> #2dd4a8
    - `.nav-word` font: use Inter instead of DM Sans

    **Step 5: Keep all compact mode (.kompakt) styles** — they are critical for the live-session UX. Just update their color tokens.

    **Preserve exactly:**
    - ALL Jinja2 blocks ({% if %}, {% for %}, {{ var }})
    - ALL element IDs (used extensively by app.js and inline JS)
    - Socket.IO script tag and all inline `<script>` blocks
    - DSGVO banner structure and its .visible toggling
    - Post-call overlay structure (postcallOverlay)
    - Frage overlay structure (frageOverlay)
    - All data-* attributes
  </action>
  <verify>
    <automated>grep -c "nerve.css\|--page-bg\|--page-text\|--glass-bg\|n-btn\|n-ai-panel\|n-pill\|n-live" templates/app.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/app.html contains `nerve.css` link
    - templates/app.html contains `var(--page-bg)` (at least 3 occurrences replacing hardcoded #0c0c18)
    - templates/app.html contains `var(--page-text-color)` or `var(--page-text-muted)` (at least 3 occurrences)
    - templates/app.html still contains `class="msg` (JS-referenced class preserved)
    - templates/app.html still contains `class="coaching-card` (JS-referenced class preserved)
    - templates/app.html still contains `id="tr"` and `id="ai"` (JS targets preserved)
    - templates/app.html still contains `id="postcallOverlay"` or `postcallOverlay` (overlay preserved)
    - templates/app.html still contains `socket.io` script tag
    - templates/app.html still contains `kompakt` (compact mode references preserved)
    - templates/app.html does NOT contain `font-family:'DM Sans'` in its `<style>` block
  </acceptance_criteria>
  <done>Live-Session page uses NERVE design tokens for all colors/typography, adds .n-ai-panel and .n-live-badge components, preserves all JS class references and Jinja2 logic</done>
</task>

</tasks>

<verification>
1. Visit /live — page renders with NERVE dark background and teal accents
2. All buttons render and are clickable (pause, beenden, log, frage)
3. Socket.IO connects (check browser console)
4. Compact mode toggle works
5. AI coaching panel has gold-bordered NERVE styling
6. Post-call overlay opens/closes
</verification>

<success_criteria>
- app.html uses NERVE CSS custom properties for colors
- All JS-referenced classes preserved (zero app.js breakage)
- Live-Session visual language matches NERVE design system
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-frontend-redesign/03.1-03-SUMMARY.md`
</output>
