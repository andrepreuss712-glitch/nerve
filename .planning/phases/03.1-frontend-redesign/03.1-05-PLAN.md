---
phase: 03.1-frontend-redesign
plan: 05
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - templates/profile_editor.html
  - templates/profiles_list.html
autonomous: true
requirements:
  - UI-05
  - UI-06
must_haves:
  truths:
    - "Profile editor uses NERVE inputs (.n-input) and labels (.n-label) for form fields"
    - "Profile list uses NERVE glass cards for each profile"
    - "Badges use .n-badge-* classes"
    - "Buttons use .n-btn-* classes"
    - "Profile wizard modal in profiles_list.html preserves all JS logic"
  artifacts:
    - path: "templates/profile_editor.html"
      provides: "Profile editor using NERVE design system"
      contains: "n-input"
    - path: "templates/profiles_list.html"
      provides: "Profiles list using NERVE design system"
      contains: "n-glass"
  key_links:
    - from: "templates/profiles_list.html"
      to: "static/nerve.css"
      via: "inherits from base.html"
      pattern: "n-glass|n-btn|n-badge"
---

<objective>
Redesign the Profile Editor and Profiles List pages to use NERVE Design System components.

Purpose: These two pages handle profile management — the list view shows all profiles as cards, and the editor provides a detailed form. Both get glass panels, NERVE inputs, and consistent button styling.

Output: Updated `templates/profile_editor.html` and `templates/profiles_list.html`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-frontend-redesign/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-redesign/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate profiles_list.html to NERVE design system</name>
  <files>templates/profiles_list.html</files>
  <read_first>
    - templates/profiles_list.html (FULL file — profile cards grid, wizard modal with 3 steps, objection/phase inputs, JS for wizard navigation and submission)
    - static/nerve.css (confirm .n-glass, .n-btn-*, .n-badge-*, .n-input classes)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\07 Anhänge\NERVE-Design-System.html (component reference)
  </read_first>
  <action>
    profiles_list.html extends base.html and uses {% block extra_style %}.

    **{% block extra_style %} changes — token migration:**

    Replace hardcoded colors:
    - `#0c0c18` -> `var(--page-bg)`
    - `#1a1a2e` -> `rgba(255,255,255,0.04)` for backgrounds, `rgba(255,255,255,0.06)` for borders
    - `#f0ebe0` -> `var(--page-text-color)`
    - `#6b6b80` -> `var(--page-text-muted)`
    - `#E8B040` -> `#2dd4a8` for primary actions, `#d4a853` for KI accents
    - `#e05c5c` -> keep (danger)
    - `#444466` -> `var(--page-text-muted)`
    - `#2e2e48` -> `rgba(255,255,255,0.08)`

    **Specific class migrations:**

    | Old Class | New Class | Notes |
    |-----------|-----------|-------|
    | `.profile-card` | Add `.n-glass` alongside | Glass panel for each profile |
    | `.profile-card.active-card` | Keep, update border-color to `#2dd4a8` | Active profile highlight |
    | `.btn.btn-primary` | `.n-btn .n-btn-primary` | "Aktivieren" and "Neues Profil" buttons |
    | `.btn.btn-secondary` | `.n-btn .n-btn-ghost` | "Bearbeiten" button |
    | `.btn.btn-danger` | `.n-btn .n-btn-danger` | "Loschen" button |
    | `.btn.btn-sm` | Add `.n-btn-sm` | Size modifier |
    | `.badge.badge-teal` | `.n-badge .n-badge-primary` | "Aktiv" badge |
    | `.badge.badge-gray` | `.n-badge .n-badge-neutral` | Branche badge |
    | `.wiz-overlay` / `.wiz-modal` | Update colors, keep structure | Wizard modal |
    | `.wfi` (wizard input) | Add `.n-input` alongside | Wizard form inputs |
    | `.wfs` (wizard select) | Add `.n-input` alongside | Wizard selects |
    | `.wfta` (wizard textarea) | Add `.n-input` alongside | Wizard textareas |
    | `.wfl` (wizard label) | Add `.n-label` alongside | Wizard labels |
    | `.wiz-dot.active` | Update colors: `#E8B040` -> `#2dd4a8` | Step progress dots |
    | `.wiz-dot.done` | Update background: `#E8B040` -> `#2dd4a8` | Completed steps |
    | `.wiz-pline.done` | Update background: include `#2dd4a8` | Progress lines |

    **{% block content %} changes:**

    - Profile card divs: add `n-glass` to class list
    - Button classes: replace `.btn .btn-primary` with `.n-btn .n-btn-primary`, etc.
    - Badge classes: replace `.badge .badge-teal` with `.n-badge .n-badge-primary`
    - "Neues Profil" dashed card: update border colors in inline styles to use `rgba(255,255,255,0.08)` and hover to `#2dd4a8`
    - Wizard inputs: add `.n-input` alongside `.wfi`

    **PRESERVE exactly:**
    - All Jinja2: `{% for p in profiles %}`, `{% if active_id %}`, `{% if session.get('rolle') %}`
    - All wizard JavaScript: openWizard, closeWizard, wizNext, wizBack, submitWizard, addObjRow, loadBrancheEinwaende, addPhaseRow, loadPhaseVorlage
    - All element IDs: wiz-overlay, wdot-1/2/3, wline-1/2, wstep-1/2/3, w-name, w-branche, etc.
    - BRANCHE_EW and PHASE_VORLAGEN data objects
    - Form submission to /profiles/wizard endpoint
  </action>
  <verify>
    <automated>grep -c "n-glass\|n-btn.*n-btn-primary\|n-badge.*n-badge-primary\|n-input\|2dd4a8" templates/profiles_list.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/profiles_list.html contains `n-glass` (profile cards)
    - templates/profiles_list.html contains `n-btn-primary` (action buttons)
    - templates/profiles_list.html contains `n-badge-primary` (active badge) or `n-badge`
    - templates/profiles_list.html contains `n-input` (wizard form inputs)
    - templates/profiles_list.html still contains `{% for p in profiles %}` (profile loop)
    - templates/profiles_list.html still contains `openWizard()` (wizard JS)
    - templates/profiles_list.html still contains `submitWizard()` (submit function)
    - templates/profiles_list.html still contains `BRANCHE_EW` (objection templates)
    - Wizard step dot colors updated to #2dd4a8
  </acceptance_criteria>
  <done>Profiles list shows glass-styled profile cards, NERVE buttons and badges, wizard modal uses NERVE inputs with teal accent colors</done>
</task>

<task type="auto">
  <name>Task 2: Migrate profile_editor.html to NERVE design system</name>
  <files>templates/profile_editor.html</files>
  <read_first>
    - templates/profile_editor.html (FULL file — complex editor with topbar, sidebar sections, main scroll area with form groups for: Grundlagen, Gesprach, Einwande, Phasen, KI-Einstellungen, Erweitert. Has extensive inline CSS and JS)
    - static/nerve.css (confirm .n-input, .n-label, .n-glass, .n-btn-* exist)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\07 Anhänge\NERVE-Design-System.html
  </read_first>
  <action>
    profile_editor.html extends base.html and has a large {% block extra_style %} block.

    **{% block extra_style %} changes — token migration:**

    Replace hardcoded colors throughout:
    - `#0c0c18` -> `var(--page-bg)`
    - `#1a1a2e` -> `rgba(255,255,255,0.06)` for borders
    - `#f0ebe0` -> `var(--page-text-color)`
    - `#6b6b80` -> `var(--page-text-muted)`
    - `#E8B040` -> `#2dd4a8` for primary interactions (focus borders, active states, section title color)
    - `#444466` -> `var(--page-text-muted)`
    - `#2e2e48` -> `rgba(255,255,255,0.08)`
    - `#b0b0c8`, `#c0c0d5` -> `var(--page-text-secondary)`
    - `#0d0d14` backgrounds -> `rgba(255,255,255,0.02)`
    - Remove font-family declarations (inherits Inter from nerve.css)

    **Class updates in CSS:**

    | Old Class | Migration |
    |-----------|-----------|
    | `.fi`, `.fs`, `.fta` (form inputs) | Keep classes, update focus border-color to `var(--input-border-focus)` (#2dd4a8) |
    | `.fl` (form label) | Keep class, update color to `var(--label-color)` |
    | `.save-btn` | Update bg to `#2dd4a8` (was #E8B040) |
    | `.editor-topbar .back-link` | Keep, or add `.n-btn .n-btn-ghost .n-btn-sm` |
    | `.sec-title` | Update color to `#2dd4a8`, `.sec-title-num` background update |
    | `.sec-link.active` | Update color and border-left-color to `#2dd4a8` |
    | `.tip-icon` | Update hover colors to match NERVE tokens |
    | `.editor-sidebar` | Update background to `rgba(255,255,255,0.02)` |

    **{% block content %} changes:**

    Minimal HTML changes — the editor's form structure is complex and should mostly stay. Key updates:
    - Save button: keep `.save-btn` class (CSS updated above)
    - Add `.n-glass` to `.editor-layout` or section containers if it makes visual sense
    - Back link: update class to include `.n-btn .n-btn-ghost .n-btn-sm`

    **PRESERVE exactly:**
    - All Jinja2: `{% if profile %}`, `{{ profile.name }}`, form action URLs
    - All JavaScript: auto-save, section scrolling, sidebar highlighting, einwand/phase CRUD, tooltip toggles
    - All element IDs and data-* attributes
    - The `.g-content { overflow: hidden }` override at the top (needed for editor layout)
    - Editor sidebar section navigation mechanism
    - All form field names (they map to backend processing)
  </action>
  <verify>
    <automated>grep -c "var(--page-bg)\|var(--page-text\|var(--input-border\|2dd4a8\|var(--glass\|var(--label" templates/profile_editor.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/profile_editor.html contains `var(--page-bg)` (at least 2 occurrences)
    - templates/profile_editor.html contains `#2dd4a8` (replacing #E8B040 for accent color, at least 3 occurrences)
    - templates/profile_editor.html contains `var(--page-text-muted)` or `var(--page-text-color)` (at least 2)
    - templates/profile_editor.html still contains `{% if profile %}` (conditional preserved)
    - templates/profile_editor.html still contains `.save-btn` (save button class preserved)
    - templates/profile_editor.html still contains `.sec-link` (sidebar navigation preserved)
    - templates/profile_editor.html does NOT contain `font-family` in its extra_style (inherits from nerve.css)
  </acceptance_criteria>
  <done>Profile editor uses NERVE design tokens for all colors, teal accent on focus/active states, consistent with rest of the app</done>
</task>

</tasks>

<verification>
1. Visit /profiles — list shows glass-styled cards, teal active badge, NERVE buttons
2. Click "Neues Profil" — wizard opens with teal step indicators, NERVE-styled inputs
3. Complete wizard — submission works, redirect to editor
4. Visit /profiles/{id}/edit — editor renders with teal accents, all sections scrollable
5. Save button works
6. Sidebar section navigation works (clicking section name scrolls to it)
</verification>

<success_criteria>
- Both profile pages use NERVE design tokens
- Profiles list wizard modal fully functional
- Profile editor form submission and auto-save work
- Visual consistency with dashboard and training pages
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-frontend-redesign/03.1-05-SUMMARY.md`
</output>
