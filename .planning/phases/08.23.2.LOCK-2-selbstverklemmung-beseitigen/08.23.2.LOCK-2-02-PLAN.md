---
phase: 08.23.2.LOCK-2
plan: 02
type: execute
wave: 2
depends_on: ["08.23.2.LOCK-2-01"]
files_modified: []
autonomous: false
requirements: [LOCK2-ROTBELEG]
complexity: 🟡

must_haves:
  truths:
    - "Der erweiterte Waechter war am ALTEN Stand (vor dem Fix) nachweislich ROT — der Lauf wurde WIRKLICH gefahren, nicht behauptet."
    - "Der Treffer lautet `services/claude_service.py:2076` und steht woertlich im SUMMARY."
    - "Es ist GENAU EIN Treffer — mehr waere ein Widerspruch zum gesetzten Fable-Ergebnis und ein STOP-Grund, weniger hiesse die Erweiterung greift nicht."
    - "Der Lauf hat `/opt/nerve/app` nur GELESEN: kein `deploy.sh`, kein tar-Upload nach /opt, kein Restart, keine Aenderung an der Produktions-DB."
    - "Die GEMESSENE Ist-Zahl der abgeleiteten Nehmer aus der `[LOCK-2]`-Ausgabe steht im SUMMARY — damit der naechste Umbau einen gemessenen Bezugspunkt hat statt einer Ueberlieferung."
  artifacts:
    - path: ".planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md"
      provides: "Rot-Beleg verbatim (Abnahme-Artefakt, kein Nebensatz)"
      contains: "claude_service.py:2076"
  key_links:
    - from: "tests/test_session_lock_blocking_calls_guard.py"
      to: "services/claude_service.py:2076"
      via: "pytest-Lauf am alten Stand auf dem Prod-Server (Claudian)"
      pattern: "claude_service.py:2076"
---

<objective>
**Ein Waechter, der nie rot war, beweist nichts.** Dieser Plan zieht den Rot-Beleg — am
**alten** Stand, **vor** dem Fix, mit dem Treffer `services/claude_service.py:2076`.

Purpose: Ohne diesen Beleg ist unklar, ob der Waechter aus Plan 01 die Fehlerklasse wirklich
sieht oder nur zufaellig nichts findet. Genau derselbe Zweifel hat in LOCK-1 den Rot-Beleg II
erzwungen — dort war ein `skipped` fast als Beweis durchgegangen.

Output: Der Rot-Beleg **verbatim** im SUMMARY. Das ist ein **Acceptance-Artefakt**, keine
Nebenbemerkung.

**Dieser Plan aendert KEINEN Code.** Der Executor faehrt nichts — er uebergibt an Claudian und
traegt das Ergebnis nach.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-CONTEXT.md
@.planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-01-PLAN.md
@CLAUDE.md
</context>

<wer_faehrt_was>
## ⚠ Das Deploy-Tor ist JETZT rot — Wellen 1-3 in EINER Sitzung

Seit dem Commit von Plan 01 ist `test_keine_erneute_riegel_nahme_unter_dem_riegel`
**absichtlich rot** (der Fix kommt erst in Plan 03), und `deploy.sh:222` bricht bei rotem Gate
ab: **in diesem Fenster ist kein Not-Hotfix deploybar, ohne das Gate zu umgehen.** Dieser Plan
ist einer der beiden **blocking** Checkpoints der Phase — also genau eine der zwei Stellen, an
denen die Sitzung real reissen kann. Wer hier unterbrechen muss, haelt im SUMMARY fest, dass
das Deploy-Tor bis zum Abschluss von Plan 03 rot ist. **Das ist keine Deploy-Erlaubnis** — in
diesem Fenster wird gar nicht deployt, auch nicht „einmal zum Testen".

## Der Executor hat KEIN SSH-Mandat

Der Executor **faehrt diesen Lauf nicht**. Er verbindet sich nicht auf den Server, er faehrt
kein `deploy.sh`, er faehrt keinen lokalen pytest (CLAUDE.md „Kein Local-Dev", ohne Ausnahme).

Er tut genau zwei Dinge:
1. **Task 1:** haelt an und uebergibt an Claudian, mit dem fertigen Befehlsblock und den
   Erwartungswerten.
2. **Task 2:** traegt die von Claudian gelieferte Ausgabe **verbatim** ins SUMMARY und wertet
   sie gegen die Abweichungs-Regel aus.

## KEIN `deploy.sh production` (Fortschreibung der LOCK-1-Entscheidung)

`deploy.sh:79-80` laedt das tar **vor** dem Gate hoch — Produktion bekaeme Dateien auf die
Platte, ohne dass wir den Upload brauchen. **Damit gibt es in dieser Phase KEINE Ausnahme von
der Kein-Deploy-Regel.** Der Beweis kommt aus einem direkten pytest-Lauf auf dem Server, bei
dem `/opt/nerve/app` ausschliesslich **gelesen** wird.
</wer_faehrt_was>

<rot_beleg_lauf>
## Der Lauf — fertig zum Einfuegen (Claudian faehrt ihn)

### Was „alter Stand" hier genau heisst

Der Beleg wird gegen den **lokalen HEAD nach Plan 01** gezogen — also: Waechter gebaut, Fix
**nicht** gebaut. Das ist bewusst **nicht** der Prod-Baum: der Prod-Baum liegt hinter dem
lokalen Stand (COUNTERPART `da7834e` ist gebaut, aber nicht deployt), und die Trefferzeile
**2076** ist die Zeile **dieses** Standes. Ein Beleg gegen einen anderen Baum wuerde eine
andere Zeilennummer melden und damit genau die Aussage kaputt machen, die er tragen soll.

`git archive HEAD` liefert diesen Stand deterministisch und ohne `.git`, `logs/` oder
Arbeitsverzeichnis-Rauschen.

### Vier Punkte, ohne die der Lauf nichts bewiese (aus LOCK-1 uebernommen, dort belegt)

1. **`nerve_test` existiert zwischen zwei Deploys NICHT.** `deploy.sh:169-170` legt einen
   `trap cleanup EXIT`, der die DB am Ende **immer** droppt. Sie wird fuer diesen Lauf frisch
   provisioniert (Muster `deploy.sh:173-187`) und danach wieder gedroppt.
   **Der Waechter selbst braucht keine DB** — er ist ein reiner AST-Sweep. Die DB existiert
   nur, damit `tests/conftest.py` (Modul-Import `database.db` + autouse-Fixtures) sauber laedt.
2. **`pytest /tmp/<datei>` findet die Fixtures nicht.** `tests/conftest.py:14` leitet
   `_REPO_ROOT` aus dem eigenen Pfad ab. Deshalb ein **vollstaendiger Arbeits-Abzug** nach
   `/tmp`, nicht nur die Testdatei.
3. **`DATABASE_URL` allein reicht nicht.** Die Fixtures lesen **`TEST_DATABASE_URL`**
   (`tests/conftest.py:824-827`). Fehlt sie, wird **uebersprungen** statt rot — und ein
   `skipped` ist genau der Nicht-Beweis, gegen den dieser ganze Abschnitt gebaut ist.
   Aus demselben Grund wird **`NERVE_SCHILD_TEST_DSN`** mitgesetzt (dieselbe Wegwerf-DB, kostet
   nichts): sie gehoert zur Standard-Belegung des Gates, und eine fehlende Belegung erzeugt
   Skips, die die Ergebnis-Zeile schwerer lesbar machen.
4. **Kein `-x`.** Mit `-x` braeche der Lauf beim ersten Fehlschlag ab und die uebrigen
   Ergebnisse fehlten. Stattdessen `-rA -s`, damit **jedes** Ergebnis namentlich in der Ausgabe
   steht **und** die `print`-Zaehlungen (`[LOCK-1 Waechter 2]`, `[LOCK-2]`) sichtbar sind.

### Befehlsblock

```bash
# (1) Den ALTEN Stand (= HEAD nach Plan 01: Waechter gebaut, Fix NICHT gebaut) als tar nach
#     /tmp auf den Server. KEIN deploy.sh, KEIN Schreibzugriff auf /opt/nerve/app.
git archive --format=tar HEAD | ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cat > /tmp/lock2-red.tar'

# (2) Alles Weitere auf dem Server
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'bash -s' <<'FERTIG'
set -e
TEST_DB=nerve_test
cleanup() { sudo -u postgres psql -c "DROP DATABASE IF EXISTS \"$TEST_DB\";" >/dev/null 2>&1 || true; }
trap cleanup EXIT

# (2a) Wegwerf-DB nach dem Muster deploy.sh:173-187 — Prod-DB `nerve` wird NUR GELESEN.
sudo -u postgres psql -c "DROP DATABASE IF EXISTS \"$TEST_DB\";"
sudo -u postgres psql -c "CREATE DATABASE \"$TEST_DB\" OWNER postgres;"
sudo -u postgres bash -c "set -o pipefail; pg_dump --schema-only nerve | psql -v ON_ERROR_STOP=1 -d $TEST_DB"
sudo -u postgres bash -c "set -o pipefail; pg_dump --data-only --table=alembic_version nerve | psql -v ON_ERROR_STOP=1 -d $TEST_DB"

# (2b) Arbeits-Abzug aus (1). /opt/nerve/app wird NICHT angefasst.
rm -rf /tmp/lock2-red && mkdir -p /tmp/lock2-red
tar xf /tmp/lock2-red.tar -C /tmp/lock2-red
chown -R nerve_app:nerve_app /tmp/lock2-red

# (2b') Gegenprobe VOR dem Lauf: steht der Verstoss im Abzug wirklich in Zeile 2076?
#       Erwartet drei Zeilen: 1355, 2076, 2090 — nur 2076 liegt IM Riegel-Block :2062.
grep -n "_anon_cache = ls.get_anonymisierer(sid)" /tmp/lock2-red/services/claude_service.py

# (2c) pytest als nerve_app, Env-Belegung wie das Gate (deploy.sh:214-221), OHNE -x
sudo -u nerve_app env TEST_DB="$TEST_DB" SECRET_KEY="lock2-red-$RANDOM$RANDOM" bash -c '
  cd /tmp/lock2-red && \
  DATABASE_URL="postgresql://nerve_app@/${TEST_DB}" \
  TEST_DATABASE_URL="postgresql://nerve_app@/${TEST_DB}" \
  NERVE_APP_TEST_DSN="postgresql://nerve_app@/${TEST_DB}" \
  NERVE_SCHILD_TEST_DSN="postgresql://nerve_app@/${TEST_DB}" \
  /opt/nerve/venv/bin/pytest tests/test_session_lock_blocking_calls_guard.py --tb=long -q -rA -s
' || true

# (2d) Abzug wegraeumen; die Wegwerf-DB nimmt der trap
rm -rf /tmp/lock2-red /tmp/lock2-red.tar
FERTIG
```

**Was dabei NICHT passiert:** kein `deploy.sh`, kein tar-Upload nach `/opt`, kein
`systemctl restart`, keine Schreiboperation in `/opt/nerve/app`, keine Aenderung an der
Produktions-DB `nerve` (nur ein lesendes `pg_dump --schema-only`). Der laufende Dienst merkt
nichts.

**Ruecklauf-Variante, falls der Lauf schon bei Collection/Fixtures scheitert** (nicht an einer
Assertion, sondern z.B. an einer fehlenden Tabelle): den Alembic-Schritt aus LOCK-1 zwischen
(2a) und (2b) einschieben und **einmal** wiederholen —
```bash
sudo -u postgres bash -c "cd /tmp/lock2-red && DATABASE_URL=postgresql://postgres@/$TEST_DB /opt/nerve/venv/bin/alembic upgrade head"
```
Er ist hier bewusst **nicht** im Standardweg: der lokale Baum enthaelt Migration 0035
(COUNTERPART, geschrieben aber auf Prod **nicht** ausgefuehrt), und ein AST-Waechter braucht
kein Schema. Scheitert auch die Wiederholung: **STOP und melden** — ein Fehler in der
Test-Umgebung ist **kein** Rot-Beleg.

**Wo ein Fehler STOP bedeutet und wo nicht:** ein **Fixture-/Collection-Fehler in der
Guard-Datei selbst** macht den Lauf wertlos → **STOP**. Ein **Skip in fremden Dateien** (z.B.
weil eine dort erwartete DSN nicht belegt ist) beruehrt die Aussage dieses Belegs **nicht** —
gefahren wird ohnehin nur `tests/test_session_lock_blocking_calls_guard.py`. Entscheidend
bleibt: der Rot-Beleg selbst darf weder `skipped` noch `error` sein.
</rot_beleg_lauf>

<erwartung_und_abweichungs_regel>
## Erwartet

| Test | erwartet |
|---|---|
| `test_keine_erneute_riegel_nahme_unter_dem_riegel` | **FAILED** — **das ist der Rot-Beleg** |
| `test_nehmer_ableitung_faellt_nicht_still_aus` | PASSED (Boden **≥ 45**; erwartete Ist-Zahl **47**) |
| `test_transitivitaet_greift_ueber_zwei_ebenen` | PASSED |
| `test_verschachtelte_defs_haften_nicht_fuer_den_aeusseren` | PASSED |
| `test_argument_referenz_ist_kein_treffer` | PASSED |
| `test_zyklus_im_aufrufgraph_terminiert` | PASSED |
| `test_erneute_nahme_meldet_harmlose_aufrufe_nicht` | PASSED |
| `test_namensbasierter_zweitpass_faengt_methoden_aufrufe` | PASSED |
| `test_alias_aufloesung_zielt_auf_die_datei_nicht_nur_den_namen` | PASSED |
| die **fuenf LOCK-1-Tests** | PASSED (Zaehlung: Summe **≥ 101**) |

Also: **1 failed, 13 passed** (5 Bestand + 9 neue = 14 Tests; die Bestands-Zahl ist
nachgezaehlt — es sind **fuenf** LOCK-1-Tests, nicht vier).

**Wichtig fuer den menschlich bewerteten Teil:** eine andere Zaehl-Zeile ist **nur dann** eine
Anomalie, wenn sie **mehr als einen** Fehlschlag oder einen `skipped`/`error` zeigt. Eine
abweichende **Gesamt**-Zahl allein ist kein STOP-Grund — sie waere nur ein Hinweis darauf,
dass in Plan 01 mehr oder weniger Tests entstanden sind als geplant.

Die Fehlermeldung des roten Tests muss **genau eine** Treffer-Zeile enthalten, und die muss
lauten:

```
  services/claude_service.py:2076  ->  services/live_session.py::get_anonymisierer
```

## Die Abweichungs-Regel (D-2, nicht verhandelbar)

| Befund | Bedeutung | Was zu tun ist |
|---|---|---|
| genau `:2076`, sonst nichts | Soll-Zustand | weiter zu Plan 03 |
| **MEHR** als `:2076` | **Widerspruch zum gesetzten Fable-Ergebnis** | **STOP.** Im DIALOG melden (`.planning/DIALOG-GSD-CLAUDIAN.md`), mit jeder zusaetzlichen Trefferzeile und ~10 Zeilen Umfeld-Code. **Nicht stillschweigend mitfixen**, nicht whitelisten. André/Claudian entscheiden, ob der Zusatzfund in diese Phase gehoert oder ein eigener Brocken ist. |
| **WENIGER**/nichts | die Erweiterung greift **nicht** | **STOP.** Den Waechter reparieren (Plan 01 erneut), **nicht** den Fix vorziehen. Ein Waechter, der den einen bekannten Fall nicht sieht, sieht auch den naechsten nicht. |
| `test_nehmer_ableitung_faellt_nicht_still_aus` FAILED | Ableitung liefert < 45 (Boden) | **STOP.** Die Zahl **nicht** senken — erst klaeren, ob das AST-Muster nicht greift. |
| Test PASSED, aber Ist-Zahl **unter 47** | ein Nehmer ist verschwunden — legitim (Rueckbau) oder Ableitungs-Schwaeche | **Kein automatischer STOP**, aber die Zahl **muss** im SUMMARY stehen und die Differenz zu 47 wird benannt. Der Boden 45 faengt den Einbruch, nicht den Einzelfall. |
| ein LOCK-1-Test FAILED | Regression durch die Erweiterung | **STOP.** Plan 01 nachbessern. |

**Ein `skipped` ist kein Beleg. Ein `error` ist kein Beleg.**

**Zeitfenster:** genau **hier** (Plan 01 gebaut, Plan 03 noch nicht). Danach ist der alte Stand
weg und der Beleg nicht mehr nachholbar.
</erwartung_und_abweichungs_regel>

<punkt_23_hinweis>
## Punkt 21 / 22 / 23 / 25 / 26 — nicht anwendbar

Kein Code, keine Tabelle, keine Migration, kein Live-Pfad wird geaendert. Der Lauf ist
lesend. `nerve_test` ist eine Wegwerf-DB und wird vom `trap` wieder gedroppt (Test-Cleanup-Regel
erfuellt, weil die ganze Datenbank verschwindet).
</punkt_23_hinweis>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Rot-Beleg ziehen (CLAUDIAN faehrt den Lauf, nicht der Executor)</name>

  <read_first>
    - `<rot_beleg_lauf>` in diesem Plan — der vollstaendige Befehlsblock.
    - `<erwartung_und_abweichungs_regel>` in diesem Plan — die Soll-Ergebnisse und die vier
      STOP-Faelle.
    - `.planning/phases/08.23.2.LOCK-1-sitzungs-riegel-entklemmen/08.23.2.LOCK-1-01-PLAN.md`
      Zeilen 71-223 (`<erst_rot_pflicht>`) — das Muster, aus dem dieser Lauf abgeleitet ist,
      inklusive der vier Funde („`nerve_test` existiert nicht", „Fixtures aus `/tmp`",
      „`TEST_DATABASE_URL` statt `DATABASE_URL`", „kein `-x`").
    - `deploy.sh` Zeilen 165-225 — die Gate-Provisionierung und das Gate-Kommando, aus dem die
      Env-Belegung stammt.
  </read_first>

  <what-built>
Plan 01 hat `tests/test_session_lock_blocking_calls_guard.py` um die LOCK-2-Erweiterung
ergaenzt (AST-Nehmer-Ableitung, Fixpunkt-Transitivitaet, namensbasierter Zweitpass,
Selbst-Tests). **Der Fix an `services/claude_service.py:2076` ist NICHT gebaut** — das ist
der Punkt: der Waechter muss an diesem Stand rot werden.
  </what-built>

  <how-to-verify>
1. Den Befehlsblock aus `<rot_beleg_lauf>` **unveraendert** fahren (Claudian, SSH-Mandat).
2. Die **vollstaendige** Ausgabe sichern — inklusive der `grep -n`-Zeile aus Schritt (2b') und
   der `[LOCK-2]`-Nehmer-Zaehlung aus `-s`.
3. Gegen `<erwartung_und_abweichungs_regel>` pruefen:
   - `test_keine_erneute_riegel_nahme_unter_dem_riegel` → **FAILED**
   - **genau eine** Treffer-Zeile, und sie enthaelt `services/claude_service.py:2076`
   - alle uebrigen Tests der Datei → **PASSED**
4. Bei jeder Abweichung: die passende STOP-Zeile der Tabelle befolgen, **nicht** weiterbauen.
  </how-to-verify>

  <acceptance_criteria>
    - Die Ausgabe des Laufs liegt **verbatim** vor (kein Auszug, keine Zusammenfassung).
    - Sie enthaelt die Zeichenkette `claude_service.py:2076`.
    - Sie enthaelt `test_keine_erneute_riegel_nahme_unter_dem_riegel` mit dem Ergebnis
      **FAILED** (nicht `skipped`, nicht `error`).
    - Die Zaehl-Zeile `1 failed, 13 passed` — **massgeblich ist**: exakt **ein** Fehlschlag,
      **kein** Skip, **kein** Error. Eine abweichende Gesamtzahl allein ist **keine** Anomalie
      und **kein** STOP-Grund (sie haengt nur daran, wie viele Selbst-Tests Plan 01 tatsaechlich
      angelegt hat).
    - Die `[LOCK-2]`-Ausgabe nennt eine Nehmer-Summe **≥ 45** (erwartet: **47**) — die Zahl wird
      notiert, sie kommt ins SUMMARY.
    - Die Gegenprobe aus (2b') zeigt **drei** Fundstellen (`1355`, `2076`, `2090`) — davon
      liegt nur `2076` im Riegel-Block `:2062`.
    - Auf dem Server ist **nichts** geblieben: `/tmp/lock2-red*` weg, `nerve_test` gedroppt,
      `/opt/nerve/app` unveraendert.
  </acceptance_criteria>

  <resume-signal>Ausgabe des Laufs verbatim zurueckgeben, oder „STOP: &lt;Abweichung&gt;"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rot-Beleg verbatim ins SUMMARY + Abweichung auswerten</name>

  <read_first>
    - Die von Claudian in Task 1 gelieferte Ausgabe.
    - `<erwartung_und_abweichungs_regel>` in diesem Plan.
    - `.planning/phases/08.23.2.LOCK-1-sitzungs-riegel-entklemmen/08.23.2.LOCK-1-01-SUMMARY.md`
      — wie ein Rot-Beleg dort dokumentiert wurde (Ueberschrift, Verbatim-Block, Bewertung).
  </read_first>

  <files>.planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md</files>

  <action>
Das SUMMARY dieses Plans schreiben. **Kein Code**, kein Commit an `services/` oder `tests/`.

Pflicht-Abschnitte:

1. **`## Rot-Beleg (alter Stand, Server-Lauf durch Claudian)`** — die Ausgabe **verbatim** in
   einem Code-Block. Nicht kuerzen, nicht zusammenfassen, nicht „sinngemaess". Inklusive der
   `grep -n`-Gegenprobe aus Schritt (2b') und der `[LOCK-2]`-Nehmer-Zaehlung.

2. **`## Die Trefferzeile`** — die eine Zeile woertlich herausgehoben:
   ```
   services/claude_service.py:2076  ->  services/live_session.py::get_anonymisierer
   ```
   plus in einem Satz, **warum** das der Selbstverklemmer ist: der Riegel `:2062` ist gehalten,
   `get_anonymisierer` (`services/live_session.py:456-461`) nimmt denselben Riegel, und der ist
   **kein RLock** (`services/live_session.py:308-310`).

3. **`## Warum es nur manchmal knallte`** — der Aufruf liegt in einem Zweig, der nur greift wenn
   Painpoint erkannt (`:2059`) UND Session lebt (`:2064`) UND **KEIN** Duplikat (`:2068`
   else-Zweig). Bei Duplikat knallt es nicht. Latente Bombe, am 31.07. gezuendet.

4. **`## Abweichungs-Auswertung`** — explizit beantworten:
   - Wurde **genau ein** Treffer gemeldet? (ja/nein)
   - Falls **mehr**: jede zusaetzliche Zeile auflisten, **STOP** vermerken und einen Eintrag in
     `.planning/DIALOG-GSD-CLAUDIAN.md` anlegen (Ueberschrift
     `### BEFUND — GSD — 2026-XX-XX (LOCK-2 Rot-Lauf: Zusatztreffer ueber dem Fable-Ergebnis)`)
     mit Trefferzeile + ~10 Zeilen Umfeld je Fund. **Plan 03 nicht starten.**
   - Falls **weniger/nichts**: **STOP**, Plan 01 nachbessern, Plan 03 nicht starten.
   - Die abgeleitete **Nehmer-Zahl** aus der `[LOCK-2]`-Ausgabe **woertlich** nennen (Summe
     **und** die Aufschluesselung pro Datei) und gegen den gemessenen Planungs-Stand **47**
     sowie den Boden **45** stellen. Diese Zahl ist der **gemessene Bezugspunkt fuer den
     naechsten Umbau** — sie ersetzt die Ueberlieferung, an der diese Phase sich schon einmal
     verhoben hat („48" war eine gerundete Angabe, kein Messwert).

5. **`## Was NICHT passiert ist`** — kein `deploy.sh`, kein tar-Upload nach `/opt`, kein
   Restart, keine Aenderung an der Prod-DB, kein lokaler pytest.

Ein Commit: `docs(08.23.2.LOCK-2): Rot-Beleg am alten Stand — Treffer claude_service.py:2076 (Server-Lauf, Claudian)`
  </action>

  <verify>
    <automated>grep -cF "claude_service.py:2076" .planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "claude_service.py:2076" .planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md` → **≥ 2**
      (einmal im Verbatim-Block, einmal im Abschnitt „Die Trefferzeile")
    - `grep -cF "## Rot-Beleg (alter Stand, Server-Lauf durch Claudian)" .planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md` → **1**
    - `grep -cF "## Abweichungs-Auswertung" .planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md` → **1**
    - `grep -cF "test_keine_erneute_riegel_nahme_unter_dem_riegel" .planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md` → **≥ 1**
    - `grep -ciF "FAILED" .planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md` → **≥ 1**
    - `grep -cF "[LOCK-2]" .planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md` → **≥ 1**
      (die **gemessene** Nehmer-Zaehlung steht im SUMMARY — ohne sie hat der naechste Umbau
      wieder nur eine Ueberlieferung statt einer Messung)
    - `git status --porcelain services/ routes/ tests/ app.py database/ alembic/` → **leer**
      (dieser Plan fasst **keinen** Code an)
    - `git log -1 --stat` zeigt genau **1** Datei (das SUMMARY)
  </acceptance_criteria>

  <done>Der Rot-Beleg liegt verbatim im SUMMARY, die Trefferzeile `services/claude_service.py:2076` steht woertlich da, und die Abweichungs-Frage („genau einer?") ist explizit beantwortet — nicht implizit angenommen.</done>
</task>

</tasks>

<verification>
- Das SUMMARY enthaelt einen Verbatim-Block mit `claude_service.py:2076`.
- Kein Code-Commit in diesem Plan: `git diff --name-only HEAD~1` nennt **nur** das SUMMARY.
- Plan 03 startet **nur**, wenn die Abweichungs-Auswertung „genau ein Treffer, `:2076`" ergibt.
</verification>

<success_criteria>
Der Waechter war rot, bevor der Fix kam — belegt, nicht behauptet. Und es war **genau der eine**
Fund, den Fables Vollscan vorhergesagt hat.
</success_criteria>

<output>
`.planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-02-SUMMARY.md`
</output>
