---
phase: 08.23.2.LOCK-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_session_lock_blocking_calls_guard.py
autonomous: true
requirements: [LOCK2-WAECHTER]
complexity: 🟡

must_haves:
  truths:
    - "Ein Aufruf unter `_session_state_lock`, der den Riegel selbst wieder nimmt — direkt oder ueber beliebig viele Zwischenstufen — macht ab jetzt einen Test rot."
    - "Die Liste der riegel-nehmenden Funktionen wird per AST AUS DEM CODE abgeleitet, nicht gepflegt: eine neue Nehmer-Funktion ist automatisch mitgeschuetzt."
    - "Verschachtelte `def`s haften nicht fuer den aeusseren Rumpf: `api_beenden` gilt NICHT als Nehmer, nur weil `_load_beenden_state` in ihm definiert ist."
    - "Die Transitivitaet greift beweisbar: ein synthetischer 2-Ebenen-Fall (Riegel-Block -> Helfer -> Riegel) wird gefunden, ein Zyklus laesst den Sweep terminieren statt haengen."
    - "Eine Funktion, die nur als ARGUMENT weitergereicht wird (`threading.Timer(..., ls._flush_segment)`), ist kein Treffer — nur Aufruf-Positionen werden gemeldet."
    - "Faellt die Nehmer-Ableitung still aus (0 oder zu wenige abgeleitete Nehmer), wird der Test ROT statt blind gruen."
    - "Die fuenf bestehenden Tests der Datei bleiben unveraendert gruen; die LOCK-1-Zaehlungen werden nicht angefasst."
    - "Der namensbasierte Zweitpass und die Alias-Aufloesung sind je durch einen GEPAARTEN Selbst-Test belegt (Treffer + Gegenprobe) — nicht nur durch den einen realen Fund, der mit Plan 03 verschwindet."
  artifacts:
    - path: "tests/test_session_lock_blocking_calls_guard.py"
      provides: "Waechter 2 + LOCK-2-Erweiterung: erneute Riegel-Nahme unter gehaltenem Riegel (AST-Nehmer-Ableitung, Fixpunkt-Transitivitaet, namensbasierter Zweitpass)"
      contains: "_erneute_nahmen_finden"
  key_links:
    - from: "tests/test_session_lock_blocking_calls_guard.py"
      to: "services/*.py + routes/*.py"
      via: "AST-Call-Graph mit Fixpunkt-Iteration ueber die abgeleitete Nehmer-Menge"
      pattern: "while geaendert"
---

<objective>
Die Fehler**klasse** dicht machen, die am 31.07. eine Produktions-Sitzung getoetet hat:
**unter einem gehaltenen `_session_state_lock` wird eine Funktion gerufen, die denselben
Riegel selbst nimmt.** `threading.Lock` ist nicht reentrant — der Faden blockiert sich selbst,
fuer immer, und sieht dabei im Abzug aus wie ein Opfer.

Purpose: Waechter 2 aus LOCK-1 lief ueber 102 Bloecke und fand NULL Verstoesse — sein
Verbots-Set kannte `get_session`, `SessionLocal`, `messages.create/stream`, `sio.emit`,
`requests.*`, `sleep`, `join`, **aber nicht die erneute Riegel-Nahme**. Genau die eine Klasse,
die hier zuschlaegt, fehlte. Sein gruenes Ergebnis war wahr und wertlos zugleich.

Output: `tests/test_session_lock_blocking_calls_guard.py` — **erweitert**, nicht ersetzt.

**Dieser Plan baut den Waechter. Er faehrt KEINEN Beleg-Lauf** (CLAUDE.md: kein Local-Dev).
Der Rot-Beleg ist Plan 02 und wird von **Claudian** auf dem Prod-Server gefahren.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-CONTEXT.md
@CLAUDE.md
</context>

<gesetztes_vorwissen>
## Der Sweep ist ERLEDIGT — dieser Plan sucht NICHTS

Der systematische Durchgang (Teil 2 des Auftrags) wurde **vor** der Planung von Fable
gefahren: AST-Vollscan aller `.py`, beide Riegel-Formen, alias-fest, Call-Graph mit
Fixpunkt-Transitivitaet, namensbasierter Zweitpass, **223 Riegel-Bloecke gesichtet**.

**GENAU EIN produktiver Fund:** `services/claude_service.py:2062` (Riegel) →
`:2076` `ls.get_anonymisierer(sid)`.

**ENTLASTET, nicht anfassen** (einzeln geprueft): `claude_service.py:1284` · `:1355` ·
`:2090` (vorbildlich, dient als Referenz-Muster) · `:942` · `deepgram_service.py:141` ·
`:285` · `:796` · `handle_disconnect` · `stash_ended_session:742` · `deepgram_service.py:254`
(`threading.Timer(…, ls._flush_segment)` ist nur eine **Referenz**) · alle Bloecke in
`learning.py`, `app_routes.py`, `prompt_pipeline.py` (reine Dict-Snapshots).

**Korrektur am urspruenglichen Auftrag:** `get_sid_paused` ist **kein Nehmer mehr** — seit
LOCK-1 Teil 1 bewusst riegel-frei (`services/live_session.py:105-138`, „NICHT WIEDER EINEN
RIEGEL EINBAUEN"). Nicht als Nehmer behandeln, keinen Riegel einbauen.

**Kein Task dieses Plans darf „alle Faelle systematisch suchen" enthalten.** Der Waechter
reproduziert dieses Ergebnis maschinell — er erhebt es nicht neu.

**Umfang der echten Nehmer-Liste — NACHGEMESSEN am Code** (Planungs-Lauf 2026-07-31: der
in Task 1 spezifizierte Algorithmus, gefahren ueber `_SCAN_DIRS` = `services/` + `routes/`,
68 Dateien): **47 produktive Nehmer** — `live_session.py` (21), `deepgram_service.py` (10),
`claude_service.py` (6), `app_routes.py` (3, darunter das **verschachtelte**
`_load_beenden_state:215`), `cost_tracker.py` (2), `learning.py` (2), `prompt_pipeline.py` (2),
`einwand_keyword_matcher.match_with_dedup` (1, Methode). Transitiv gefaehrlich: **99
Funktionen** ueber `_SCAN_DIRS`.

**Korrektur an zwei ueberlieferten Zahlen** (beide waren nie Messwerte dieses Sweeps):
* **„48 Nehmer"** war eine gerundete Angabe — Fables eigene Aufschluesselung summiert sich
  ebenfalls auf **47**. Ein Mindest-Soll von 48 waere unerfuellbar gewesen und haette die
  Phase an genau der Stelle verklemmt, an der sie eine Verklemmung beseitigt.
* **„389 transitiv gefaehrliche Funktionen"** stammen aus Fables **Vollscan ueber alle `.py`**
  (inkl. `app.py`, `database/`, `tests/`) — also aus einem **weiteren** Bereich als
  `_SCAN_DIRS`. Fuer diesen Waechter gelten die **99**.

Beide Zahlen sind **kein** Abnahme-Kriterium. Kriterium ist allein `_SOLL_NEHMER_MINDESTENS`
(siehe Task 1) — und die exakte Ist-Zahl, die jeder Lauf selbst ausdruckt.
</gesetztes_vorwissen>

<das_deploy_tor_ist_ab_hier_rot>
## ⚠ Wellen 1-3 in EINER Sitzung durchziehen

Ab dem Commit dieses Plans ist `test_keine_erneute_riegel_nahme_unter_dem_riegel`
**absichtlich rot** (der Fix kommt erst in Plan 03), und `deploy.sh:222` bricht bei rotem Gate
ab. **In diesem Fenster ist kein Not-Hotfix deploybar, ohne das Gate zu umgehen.**

Deshalb: Plan 01 → Plan 02 (Rot-Beleg, Claudian) → Plan 03 (Fix + Gruen-Beleg) in **einer**
Sitzung. Wer unterbrechen muss, haelt im SUMMARY fest, dass das Deploy-Tor bis zum Abschluss
von Plan 03 rot ist.

**Das ist keine Deploy-Erlaubnis.** In diesem Fenster wird **gar nicht** deployt — auch nicht
„einmal zum Testen".
</das_deploy_tor_ist_ab_hier_rot>

<punkt_23_hinweis>
## Punkt 21 / 22 / 23 / 26 — nicht anwendbar

Dieser Plan legt keine Tabelle/Spalte an, aendert keinen Lese-/Schreib-Pfad einer Tabelle,
fasst weder `database/models.py` noch eine Migration an und liest keine fremd-geschriebenen
Daten. Kein Schild-Nachzug, keine Persistenz-Schicht-Verifikation, keine
Verbindungs-Karte, keine Async-Bereitschafts-Naht noetig.

**Punkt 25 (Latenz):** kein Produktiv-Code in diesem Plan — Latenz unveraendert.
**Punkt 27 (einfachster tragfaehiger Weg):** die Alternative „gepflegte Liste riegel-nehmender
Helfer" waere **einfacher zu schreiben** und **falsch**: sie veraltet und erzeugt genau die
Luecke, die diese Phase schliesst (der Kommentar bei `claude_service.py:1441` ist der Beleg —
Wissen als Kommentar statt als Waechter). Die AST-Ableitung ist der einfachste **tragfaehige**
Weg, nicht der einfachste.
**Punkt 28/29:** kein modul-globaler Zustand, kein kopiertes Alt-Muster.
</punkt_23_hinweis>

<tasks>

<task type="auto">
  <name>Task 1: Nehmer-Ableitung + Fixpunkt-Transitivitaet + Verstoss-Sweep</name>

  <read_first>
    - `tests/test_session_lock_blocking_calls_guard.py` **vollstaendig** (500 Zeilen). Das ist
      die Basis, die ERWEITERT wird — nicht ersetzt. Insbesondere zum Wiederverwenden:
      `_ist_session_state_lock:136` (Alias-feste Riegel-Erkennung ueber den Attribut-Namen),
      `_sammle_bloecke:147` (with-Form), `_sammle_try_bloecke:168` (begrenzter Erwerb),
      `_riegel_region:185` (was WIRKLICH unter dem Riegel laeuft), `_python_dateien:250`,
      `_baum_oder_fehler:263`, `_SCAN_DIRS:94`. Die **fuenf** bestehenden Tests
      (`:272`, `:301`, `:366`, `:407`, `:449`) bleiben **unveraendert** — es sind **fuenf**,
      nicht vier (nachgezaehlt: `grep -c "^def test_"` → **5**). Alle Zaehl-Kriterien dieses
      Plans rechnen mit fuenf.
    - `services/claude_service.py` Zeilen 2050-2100 — der Riegel-Block `:2062`, der Verstoss
      `:2076` (`_anon_cache = ls.get_anonymisierer(sid)`) und das vorbildliche Gegenbeispiel
      `:2090` (**derselbe Aufruf, aber ausserhalb des Blocks**). Das ist der EINE Treffer, den
      der Waechter am alten Stand melden muss — und der Beleg, dass die Regions-Logik `:2090`
      NICHT mitnimmt.
    - `services/claude_service.py` Zeilen 1338-1360 — `:1355` ist **derselbe Quelltext**
      (`_anon_cache = ls.get_anonymisierer(sid)`), liegt aber **hinter** dem `with`-Block
      `:1341-1347`. Zweiter Beleg, dass die Regions-Logik trennt. Wichtig fuer die
      Abnahme-Kriterien in Plan 03.
    - `services/live_session.py` Zeilen 442-473 — `init_anonymisierer` (schreibt
      `_session_state[sid]['anonymisierer']`), `get_anonymisierer:456` und
      `get_counterpart:464`. **Beide nehmen den Riegel selbst** → beide muessen von der
      Ableitung als Nehmer erkannt werden.
    - `services/live_session.py` Zeilen 105-138 — `get_sid_paused`, seit LOCK-1 **riegel-frei**.
      Darf **nicht** als Nehmer herauskommen. Gegenprobe fuer die Ableitung.
    - `services/live_session.py` Zeilen 291-345 — die `_TracedLock`-Klasse. Ihre Methoden
      `acquire`/`release` operieren auf `self._lock` (Attribut-Name `_lock`, **nicht**
      `_session_state_lock`) → `_ist_session_state_lock` sagt korrekt Nein, die Klasse selbst
      wird **nicht** zum Nehmer. Plus der woertliche „KEIN RLock"-Design-Zwang (`:308-310`).
    - `services/claude_service.py` Zeilen 934-945 — `import services.live_session as _ls_av`
      (`:936`) als funktions**lokaler** Lazy-Import. Die Alias-Aufloesung muss auch solche
      Importe sehen, nicht nur Modul-Ebene.
    - `services/live_session.py` Zeilen 1106-1118 — `ist_painpoint_duplikat`, wird bei
      `claude_service.py:2065` **unter** dem Riegel gerufen und ist **riegel-frei**. Gegenprobe:
      dieser Aufruf darf **kein** Treffer werden.
    - `services/live_session.py` Zeilen 847-868 — `_durable_call_id` (lock-frei) und
      `resolve_call_id_for_sid` (Nehmer) mit dem woertlich dokumentierten Muster
      „die 4 Live-emit-Aufrufer halten `_session_state_lock` bereits, ein Re-Lock waere ein
      Deadlock". Das ist die im Code bereits gelebte Regel, die der Waechter erzwingt.
    - `CLAUDE.md` Test-Qualitaets-Regel (Abschnitt „Integration-Assertion vs.
      Source-Presence-False-Green") und der HART-Abschnitt „Kein Local-Dev".
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
Erweiterung einer bestehenden Testdatei. Kein Produktiv-Code, kein Import aus `services.*`,
keine DB, kein Netz, keine Nebenlaeufigkeit. Der Sweep liest Quelltext und parst ihn mit `ast`.

### Schicht 2 — Funktions-Skelett (neu, unterhalb des LOCK-1-Bestands)
```
_ist_riegel_acquire(call)            -> <riegel>.acquire(...)  (nutzt _ist_session_state_lock)
_eigene_knoten(fn)                   -> Rumpf OHNE verschachtelte def/class-Ruempfe
_nimmt_riegel_selbst(fn)             -> with-Riegel ODER .acquire im EIGENEN Rumpf
_modul_schluessel(datei)             -> 'services/live_session.py' <-> 'services.live_session'
_aliase_und_importe(baum, dateien)   -> (alias->zieldatei, lokalname->(zieldatei,name))
_funktionen_der_datei(baum)          -> [(name, knoten)] inkl. Methoden + verschachtelter defs
_aufloesen(func, datei, aliase, importe, lokale) -> (zieldatei|None, name|None)
_graph_bauen(baeume)                 -> (gefaehrlich, gefaehrliche_namen, nehmer, pro_datei)
                                        Fixpunkt-Iteration, KEINE Rekursion
_erneute_nahmen_finden(baeume)       -> [(datei, zeile, meldename)]
test_keine_erneute_riegel_nahme_unter_dem_riegel()
test_nehmer_ableitung_faellt_nicht_still_aus()
```
Ersetzt wird: **nichts**. Unberuehrt: alle LOCK-1-Funktionen und alle fuenf LOCK-1-Tests.

### Schicht 3 — Cross-File-Awareness (belegt)

| Symbol | Wo | Konsequenz |
|---|---|---|
| `_ist_session_state_lock` | `tests/…guard.py:136` | **Wiederverwenden.** Eine zweite Alias-Logik waere der Anfang der Divergenz — dieselbe Lehre wie beim `try/finally`-Zweig aus LOCK-1. |
| `_riegel_region` | `tests/…guard.py:185` | **Wiederverwenden.** Sie trennt `:2076` (drin) von `:2090` (draussen) — genau die Trennung, auf der der ganze Rot-Beleg steht. |
| Lazy-Importe im **Funktions**rumpf | `claude_service.py:936` (`_ls_av`), `deepgram_service.py:572` (`ls_module`), `prompt_pipeline.py`/`einwand_keyword_matcher.py` (`_ls`) | Die Alias-Sammlung laeuft ueber `ast.walk(baum)`, **nicht** nur ueber `baum.body`. Sonst faende sie genau die fuenf Alias-Faelle nicht, die schon LOCK-1 zum AST gezwungen haben. |
| `from services.anonymization import anonymize_output` | `claude_service.py:2075`, funktionslokal | `ast.ImportFrom` → **Funktions**-Import, nicht Modul-Alias. Aufloesung auf `(services/anonymization.py, anonymize_output)`. Die Datei hat 0 Riegel-Bloecke (LOCK-1-Zaehlung) → kein Treffer. |
| `einwand_keyword_matcher.match_with_dedup` | Methode | Fuer Aufrufer nur als `matcher.match_with_dedup(...)` sichtbar — Empfaenger nicht aufloesbar. Faengt der **namensbasierte Zweitpass**. |
| `_load_beenden_state` | `routes/app_routes.py:215`, **verschachtelt** in `api_beenden` | Muss selbst Nehmer sein, aber `api_beenden` darf es **nicht** dadurch werden. Deshalb `_eigene_knoten`. |
| `_TracedLock.acquire/.release` | `services/live_session.py` | Operieren auf `self._lock` → kein Nehmer. Und die Aufrufe `ls._session_state_lock.acquire()/.release()` in Riegel-Regionen loesen auf `(live_session.py, 'acquire'/'release')` auf — dort existiert nur die `_TracedLock`-Methode, die kein Nehmer ist → kein Selbst-Treffer. |
| `tests/` | **nicht** in `_SCAN_DIRS` | Die synthetischen Quelltexte des Selbst-Tests (Task 2) koennen nicht in den Datei-Sweep geraten. |
| `app.py`, `database/`, `alembic/` | ausserhalb `_SCAN_DIRS` | **Benannte Restluecke** (Task 2, Docstring): Kanten aus Modulen ausserhalb des Sweep-Bereichs sind unsichtbar. |

### Schicht 4 — Edge-Case-Analyse

1. **Terminierung.** Fixpunkt-Iteration ueber eine endliche, nur wachsende Menge → terminiert
   garantiert, auch bei Zyklen (`A ruft B, B ruft A`). Rekursion ueber den Call-Graph wuerde
   bei Zyklen ohne Besuchs-Markierung endlos laufen. **Deshalb `while geaendert:`, keine
   Rekursion** — D-3 Punkt 2, nicht verhandelbar.
2. **Verschachtelte `def`s.** `ast.walk(fn)` wuerde in verschachtelte Funktionen absteigen und
   `api_beenden` faelschlich zum Nehmer machen (`_load_beenden_state` haelt dort einen Riegel).
   `_eigene_knoten` steigt an jeder `FunctionDef`/`AsyncFunctionDef`/`ClassDef` **nicht** ab.
   Die verschachtelte Funktion selbst wird trotzdem als eigene Funktion erfasst und ist Nehmer.
3. **Nur Aufruf-Positionen, keine Argument-Referenzen.** Geprueft wird ausschliesslich
   `knoten.func` eines `ast.Call`. `threading.Timer(…, ls._flush_segment)` reicht
   `_flush_segment` nur als **Wert** weiter — der Callback feuert spaeter auf dem Timer-Faden
   **ohne** Riegel. Wuerden Argument-Referenzen mitgemeldet, waere das ein Dauer-Falschtreffer
   (`deepgram_service.py:254`). D-3 Punkt 4.
4. **Namensbasierter Zweitpass — zweischneidig, aber noetig.** Ist der Empfaenger nicht
   aufloesbar (`matcher.match_with_dedup`, `self.foo`, `obj.bar`), wird der **blosse
   Methodenname** gegen die Menge der gefaehrlichen Namen geprueft. Das faengt echte Faelle und
   kann bei Namensgleichheit falsch anschlagen. Ausnahmen **ausschliesslich** ueber die
   bestehende `# FALSCH-TREFFER:`-Regel mit `Datei:Zeile` und Begruendung — **nie** durch
   Aufweichen des Musters. Als Restluecke benannt (Task 2).
5. **dict-/str-Methodennamen.** Unter dem Riegel stehen massenhaft `d.get(...)`,
   `.append(...)`, `.pop(...)`, `.copy()`. Ein Treffer entstuende nur, wenn eine **gefaehrliche
   Funktion genauso hiesse**. Unter den 47 Nehmern ist kein solcher Name; die einzige theoretisch
   moegliche Kollision ist `index` (Fable: praktisch ausgeschlossen, formal UNKLAR). **Kein
   Sonderfilter** (Punkt 27) — tritt der Fall auf, ist es ein `# FALSCH-TREFFER:` MIT Begruendung
   und ein Abweichungs-Melder nach Plan 02. Als Restluecke benannt.
6. **Der Riegel-Erwerb selbst.** `ls._session_state_lock.acquire(...)` / `.release()` innerhalb
   einer Riegel-Region loest auf `(live_session.py, 'acquire'/'release')` auf. Beide Namen
   gehoeren zu `_TracedLock`-Methoden, die **keine** Nehmer sind → kein Treffer. Kein
   Sonderfall noetig; das ist geprueft, nicht gehofft.
7. **Ein Nehmer, der sich selbst ruft.** Der aeussere Rahmen ist selbst Nehmer; ein
   rekursiver Selbstaufruf **unter** dem Riegel waere ein echter Deadlock und wird zu Recht
   gemeldet. Kein Sonderfall.
8. **Encoding / `__pycache__` / Syntaxfehler.** Alles bereits durch `_python_dateien` und
   `_baum_oder_fehler` aus LOCK-1 geregelt — **wiederverwenden**, nicht neu bauen.
9. **Die gefaehrlichste Stelle: still-blind statt rot.** Faellt die Nehmer-Ableitung aus
   (falsches AST-Muster, leerer Alias-Satz), findet der Sweep 0 Verstoesse und sieht **gruen
   aus**. Dagegen drei Riegel: (a) `_SOLL_NEHMER_MINDESTENS = 45` — ein Boden **unter** dem
   gemessenen Ist-Stand 47, der den Einbruch faengt, ohne bei jeder legitimen Rueckbau-Aenderung
   zu blockieren; (b) der synthetische 2-Ebenen-Selbst-Test aus Task 2 beweist, dass die
   Transitivitaet ueberhaupt greift; (c) die gepaarten Selbst-Tests fuer den **namensbasierten
   Zweitpass** und die **Alias-Aufloesung** (Task 2) beweisen, dass diese beiden Wege ueberhaupt
   verdrahtet sind — sonst haengen sie einzig am realen Treffer `:2076`, und **der verschwindet
   mit Plan 03**. **Alle drei sind Pflicht, keiner ersetzt den anderen.**
10. **Kein Source-Presence-False-Green.** Der Sweep prueft ein **verbotenes Muster**, nicht die
    Existenz erwuenschten Codes: verschwindet Code, wird er gruener; kommt das Muster zurueck,
    wird er rot — nie umgekehrt. Das ist der in CLAUDE.md dokumentierte Grenzfall (Waechter-Klasse
    wie `test_no_live_global_state.py` / `test_schild_guard.py`). **Begruendung gehoert in den
    Docstring** (Task 2).
  </punkt14_audit>

  <files>tests/test_session_lock_blocking_calls_guard.py</files>

  <action>
Die bestehende Datei `tests/test_session_lock_blocking_calls_guard.py` **erweitern**. Nichts
loeschen, nichts umbenennen, `_SOLL_MINDESTENS`/`_SOLL_SUMME_MINDESTENS` **nicht** anfassen.
Alles Neue kommt **unterhalb** des LOCK-1-Bestands unter einer eigenen Ueberschrift:

```python
# ══ LOCK-2: erneute Riegel-Nahme unter gehaltenem Riegel ══════════════════════
```

**Alle neuen Bezeichner sind ASCII** (CLAUDE.md-Umlaut-Regel: Code-Identifier ohne Umlaute).

### 1. Konstanten

```python
# Mindest-Soll fuer die ABGELEITETE Nehmer-Menge. Faellt die Ableitung still aus (falsches
# AST-Muster, leerer Alias-Satz), findet der Sweep 0 Verstoesse und SIEHT GRUEN AUS. Diese
# Zahl macht ihn dann rot statt blind.
#
# IST-STAND 2026-07-31 (nachgemessen ueber services/ + routes/): 47 abgeleitete Nehmer —
# live_session 21, deepgram_service 10, claude_service 6, app_routes 3, cost_tracker 2,
# learning 2, prompt_pipeline 2, einwand_keyword_matcher 1.
# Fables urspruenglich genannte "48" war eine GERUNDETE Angabe, keine Messung: ihre eigene
# Aufschluesselung ergibt ebenfalls 47.
#
# WARUM 45 UND NICHT 47: der Zweck ist "rot statt blind", nicht exakte Buchfuehrung. Ein
# stiller Total-Ausfall der Ableitung liefert 0 bis eine Handvoll Nehmer — den faengt 45
# genauso zuverlaessig wie 47. Ein Boden von 47 waere dagegen bei JEDER legitimen Entfernung
# eines Nehmers rot; genau das ist real passiert (LOCK-1 hat get_sid_paused bewusst
# riegel-frei gemacht). Der Waechter waere dann ein Blockierer statt ein Netz. Der Puffer
# folgt derselben Logik wie _SOLL_MINDESTENS weiter oben ("Tiefpunkt der Kurve, nicht
# heutiger Ist-Stand").
# Die EXAKTE Ist-Zahl druckt jeder Lauf selbst ([LOCK-2]-Ausgabe unter -s) und sie gehoert
# ins SUMMARY. Sinkt sie: Ursache klaeren und MIT BEGRUENDUNG nachziehen — nie stillschweigend
# senken, nie den Test entfernen.
_SOLL_NEHMER_MINDESTENS = 45

# Falsch-Treffer-Ausnahmen NUR fuer die LOCK-2-Erweiterung. HEUTE LEER — jeder Eintrag
# braucht einen '# FALSCH-TREFFER:'-Kommentar mit Datei:Zeile und Begruendung.
# Ein ECHTER Fund gehoert NICHT hierher, sondern gemeldet (STOP-Regel im Docstring).
_ERNEUTE_NAHME_FALSCH_TREFFER = frozenset()   # {(datei, zeile, name), ...}
```

### 2. Nehmer-Erkennung (D-3 Punkt 1)

```python
def _ist_riegel_acquire(call):
    """True fuer <riegel>.acquire(...) — Alias-fest ueber DIESELBE Riegel-Erkennung wie der
    with-Zweig (_ist_session_state_lock). Zweite Form der Riegel-Nahme seit LOCK-1 Plan 03."""
    f = call.func
    return (isinstance(f, ast.Attribute) and f.attr == 'acquire'
            and _ist_session_state_lock(f.value))


def _eigene_knoten(fn_knoten):
    """Alle Knoten im Rumpf von fn_knoten OHNE die Ruempfe verschachtelter def/class.

    WARUM (D-3 Punkt 1): ast.walk wuerde in verschachtelte Funktionen absteigen und
    routes/app_routes.py:api_beenden faelschlich zum Riegel-Nehmer machen, weil das darin
    definierte _load_beenden_state:215 einen Riegel haelt. Die verschachtelte Funktion wird
    separat als EIGENE Funktion erfasst und ist dort zu Recht Nehmer.
    """
    ergebnis = []

    def _lauf(knoten):
        for kind in ast.iter_child_nodes(knoten):
            if isinstance(kind, (ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)):
                continue
            ergebnis.append(kind)
            _lauf(kind)

    _lauf(fn_knoten)
    return ergebnis


def _nimmt_riegel_selbst(fn_knoten):
    """Nehmer = im EIGENEN Rumpf liegt ein Riegel-`with` ODER ein <riegel>.acquire().
    Nichts hartkodiert: eine neue riegel-nehmende Funktion ist damit automatisch geschuetzt.
    Eine gepflegte Liste wuerde veralten und dieselbe Luecke neu erzeugen."""
    for k in _eigene_knoten(fn_knoten):
        if isinstance(k, ast.With) and any(
                _ist_session_state_lock(i.context_expr) for i in k.items):
            return True
        if isinstance(k, ast.Call) and _ist_riegel_acquire(k):
            return True
    return False
```

### 3. Modul-/Alias-Aufloesung (D-3 Punkt 2)

Modul-Schluessel ist **durchgaengig der relative posix-Pfad** (`'services/live_session.py'`) —
dieselbe Schreibweise wie `_SOLL_MINDESTENS`, damit Meldungen und Zaehlungen zusammenpassen.

```python
def _punkt_name(datei):
    """'services/live_session.py' -> 'services.live_session'"""
    return datei[:-3].replace('/', '.')


def _aliase_und_importe(baum, bekannte_module):
    """(modul_aliase, funktions_importe) fuer EINE Datei.

    modul_aliase:    alias-Name        -> ziel-DATEI   (deckt ls / _ls / _ls_av / ls_module)
    funktions_importe: lokaler Name    -> (ziel-DATEI, funktions-Name)

    ast.walk statt baum.body: die riskantesten Aliase sind FUNKTIONSLOKALE lazy-Importe gegen
    Modul-Zyklen (services/claude_service.py:936 `import services.live_session as _ls_av`).
    Ein Sammler nur auf Modul-Ebene faende genau die fuenf Faelle nicht, die schon LOCK-1 zum
    AST gezwungen haben.
    """
    aliase, importe = {}, {}
    for knoten in ast.walk(baum):
        if isinstance(knoten, ast.Import):
            for a in knoten.names:
                if a.name in bekannte_module:
                    aliase[a.asname or a.name.split('.')[-1]] = bekannte_module[a.name]
        elif isinstance(knoten, ast.ImportFrom):
            if not knoten.module:
                continue
            for a in knoten.names:
                voll = f'{knoten.module}.{a.name}'
                if voll in bekannte_module:
                    # `from services import live_session as ls` -> Modul, kein Symbol
                    aliase[a.asname or a.name] = bekannte_module[voll]
                elif knoten.module in bekannte_module:
                    importe[a.asname or a.name] = (bekannte_module[knoten.module], a.name)
    return aliase, importe
```

`bekannte_module` ist `{ _punkt_name(datei): datei }` ueber **alle** Dateien aus
`_python_dateien()` — einmal gebaut, an alle Dateien durchgereicht.

### 4. Funktionen + Kanten einer Datei

```python
def _funktionen_der_datei(baum):
    """[(name, knoten)] fuer JEDE def/async def im Baum — inkl. Methoden und verschachtelter
    defs. Der Name ist der BLOSSE Funktionsname (ohne Klasse): fuer Aufrufer ist eine Methode
    ohnehin nur als `obj.name(...)` sichtbar, und genau darauf setzt der Zweitpass auf."""
    return [(k.name, k) for k in ast.walk(baum)
            if isinstance(k, (ast.FunctionDef, ast.AsyncFunctionDef))]


def _aufloesen(func, datei, aliase, importe, lokale_namen):
    """(ziel_datei|None, name|None) fuer den AUFGERUFENEN Ausdruck eines ast.Call.

    ziel_datei is None + name gesetzt  ->  nicht aufloesbar, geht in den namensbasierten
                                           Zweitpass (D-3 Punkt 3).
    """
    if isinstance(func, ast.Name):
        if func.id in lokale_namen:
            return datei, func.id
        if func.id in importe:
            return importe[func.id]
        return None, func.id
    if isinstance(func, ast.Attribute):
        v = func.value
        if isinstance(v, ast.Name) and v.id in aliase:
            return aliase[v.id], func.attr
        return None, func.attr
    return None, None


def _kanten_der_funktion(fn_knoten, datei, aliase, importe, lokale_namen):
    """(aufgeloeste_ziele, namens_ziele) aus dem EIGENEN Rumpf.

    NUR Aufruf-Positionen (knoten.func) — KEINE Argument-Referenzen (D-3 Punkt 4). Sonst
    wuerde threading.Timer(..., ls._flush_segment) (services/deepgram_service.py:254) ein
    Dauer-Falschtreffer: dort wird die Funktion nur als WERT weitergereicht, der Callback
    feuert spaeter auf dem Timer-Faden OHNE Riegel.
    """
    ziele, namen = set(), set()
    for k in _eigene_knoten(fn_knoten):
        if not isinstance(k, ast.Call):
            continue
        zdatei, zname = _aufloesen(k.func, datei, aliase, importe, lokale_namen)
        if zname is None:
            continue
        if zdatei is None:
            namen.add(zname)
        else:
            ziele.add((zdatei, zname))
    return ziele, namen
```

**Wichtig:** `_eigene_knoten` auch hier — die Kanten einer verschachtelten Funktion gehoeren
ihr selbst, nicht dem aeusseren Rahmen. Ruft der aeussere Rahmen die verschachtelte Funktion
auf, entsteht die Kante ueber den **Aufruf** (`lokale_namen` enthaelt auch verschachtelte
Namen) — genau richtig.

### 5. Fixpunkt (D-3 Punkt 2)

```python
def _graph_bauen(baeume):
    """(gefaehrlich, gefaehrliche_namen, nehmer, nehmer_pro_datei) fuer {datei: baum}.

    FIXPUNKT-ITERATION, KEINE REKURSION: die Menge waechst monoton ueber eine endliche
    Grundmenge, terminiert also garantiert — auch bei Zyklen (A ruft B, B ruft A). Eine
    Rekursion ueber den Call-Graph liefe dort ohne Besuchs-Markierung endlos.
    Beliebige Tiefe ist damit gratis: der Kegel aus 99 transitiv gefaehrlichen Funktionen
    (Ist-Stand 2026-07-31 ueber _SCAN_DIRS) entsteht von selbst.
    """
    bekannte_module = {_punkt_name(d): d for d in baeume}
    alle = {}          # (datei, name) -> (ziele, namens_ziele)
    nehmer = set()
    nehmer_pro_datei = {}
    for datei, baum in baeume.items():
        aliase, importe = _aliase_und_importe(baum, bekannte_module)
        funktionen = _funktionen_der_datei(baum)
        lokale_namen = {n for n, _ in funktionen}
        for name, knoten in funktionen:
            schluessel = (datei, name)
            alle[schluessel] = _kanten_der_funktion(
                knoten, datei, aliase, importe, lokale_namen)
            if _nimmt_riegel_selbst(knoten):
                nehmer.add(schluessel)
                nehmer_pro_datei[datei] = nehmer_pro_datei.get(datei, 0) + 1

    gefaehrlich = set(nehmer)
    gefaehrliche_namen = {name for _d, name in gefaehrlich}
    geaendert = True
    while geaendert:
        geaendert = False
        for schluessel, (ziele, namens_ziele) in alle.items():
            if schluessel in gefaehrlich:
                continue
            if (ziele & gefaehrlich) or (namens_ziele & gefaehrliche_namen):
                gefaehrlich.add(schluessel)
                gefaehrliche_namen.add(schluessel[1])
                geaendert = True
    return gefaehrlich, gefaehrliche_namen, nehmer, nehmer_pro_datei
```

### 6. Verstoss-Sweep

```python
def _erneute_nahmen_finden(baeume):
    """[(datei, zeile, meldename)] — Aufrufe unter dem Riegel, die den Riegel selbst
    (direkt oder transitiv) wieder nehmen. Nutzt DIESELBEN Regions-Helfer wie LOCK-1
    (_sammle_bloecke / _sammle_try_bloecke / _riegel_region) — eine zweite Regions-Logik
    waere der Anfang der Divergenz.

    Dass die Regionslogik trennt, ist am Produktiv-Code belegt: services/claude_service.py:2076
    liegt IM Block :2062 (Treffer), :2090 und :1355 liegen DAHINTER (kein Treffer) — derselbe
    Quelltext, andere Position.
    """
    gefaehrlich, gefaehrliche_namen, _n, _p = _graph_bauen(baeume)
    bekannte_module = {_punkt_name(d): d for d in baeume}
    treffer = []
    for datei, baum in baeume.items():
        aliase, importe = _aliase_und_importe(baum, bekannte_module)
        lokale_namen = {n for n, _ in _funktionen_der_datei(baum)}
        regionen = [[b] for b in _sammle_bloecke(baum)]
        regionen += [_riegel_region(t) for t in _sammle_try_bloecke(baum)]
        for region in regionen:
            for wurzel in region:
                for k in ast.walk(wurzel):
                    if not isinstance(k, ast.Call):
                        continue
                    zdatei, zname = _aufloesen(
                        k.func, datei, aliase, importe, lokale_namen)
                    if zname is None:
                        continue
                    ist_treffer = ((zdatei, zname) in gefaehrlich if zdatei
                                   else zname in gefaehrliche_namen)
                    if ist_treffer:
                        melde = (f'{zdatei or "?"}::{zname}')
                        treffer.append((datei, getattr(k, 'lineno', 0), melde))
    return sorted(set(treffer))
```

### 7. Die zwei Tests

```python
def test_keine_erneute_riegel_nahme_unter_dem_riegel():
    baeume = {}
    fehler = []
    for datei, pfad in _python_dateien():
        baum, f = _baum_oder_fehler(pfad)
        if baum is None:
            fehler.append((datei, 0, f'SYNTAX ({f})'))
            continue
        baeume[datei] = baum
    verstoesse = [t for t in _erneute_nahmen_finden(baeume)
                  if t not in _ERNEUTE_NAHME_FALSCH_TREFFER] + fehler
    assert not verstoesse, (
        "Erneute Riegel-Nahme unter gehaltenem _session_state_lock — der Faden blockiert "
        "SICH SELBST (threading.Lock ist NICHT reentrant, und das ist Absicht: "
        "services/live_session.py:308-310):\n" +
        "\n".join(f"  {d}:{z}  ->  {n}" for d, z, n in verstoesse) +
        "\n\nNICHT die Whitelist fuellen und KEIN RLock. Den Wert direkt aus dem SCHON "
        "GEHALTENEN State lesen (Muster: services/live_session.py:859-863 und "
        "services/claude_service.py:1440-1442) oder den Aufruf VOR den Block ziehen "
        "(Muster: services/claude_service.py:2090).")


def test_nehmer_ableitung_faellt_nicht_still_aus():
    baeume = {}
    for datei, pfad in _python_dateien():
        baum, _f = _baum_oder_fehler(pfad)
        if baum is not None:
            baeume[datei] = baum
    _g, _gn, nehmer, pro_datei = _graph_bauen(baeume)
    print('\n[LOCK-2] Abgeleitete Riegel-NEHMER (nichts hartkodiert):')
    for datei in sorted(pro_datei):
        print(f'  {datei}: {pro_datei[datei]}')
    print(f'  SUMME: {len(nehmer)} Nehmer, davon transitiv gefaehrlich: {len(_g)}')
    assert len(nehmer) >= _SOLL_NEHMER_MINDESTENS, (
        f"Die Nehmer-Ableitung liefert nur {len(nehmer)} Funktionen, erwartet mindestens "
        f"{_SOLL_NEHMER_MINDESTENS} (gemessener Ist-Stand 2026-07-31: 47). Entweder greift das "
        f"AST-Muster nicht mehr (dann ist der Waechter BLIND, nicht gruen), oder Nehmer "
        f"wurden legitim zurueckgebaut (dann die Zahl MIT BEGRUENDUNG nachziehen, den Test "
        f"NICHT entfernen).")
```

### 8. Was NICHT passiert

- **Kein lokaler pytest-Lauf** (CLAUDE.md „Kein Local-Dev"). Der Beleg-Lauf ist Plan 02,
  gefahren von Claudian auf dem Prod-Server.
- **Kein `deploy.sh`**, kein SSH, keine Migration, kein Produktiv-Code.
- **Kein RLock**, kein neuer Helfer in `services/`, keine Aenderung an
  `get_anonymisierer`/`get_counterpart` (das ist Plan 03, und dort nur ein Kommentar).

Ein Commit: `test(08.23.2.LOCK-2): Waechter 2 erweitert — erneute Riegel-Nahme unter gehaltenem Riegel (AST-Nehmer-Ableitung + Fixpunkt)`
  </action>

  <verify>
    <automated>grep -cF "def _erneute_nahmen_finden" tests/test_session_lock_blocking_calls_guard.py</automated>
  </verify>

  <acceptance_criteria>
    **Kein pytest in diesem Task** — der Beleg-Lauf ist Plan 02 (Claudian, Prod-Server).
    Alle Kriterien sind statisch pruefbar.

    - `grep -cF "def _nimmt_riegel_selbst" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def _eigene_knoten" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def _graph_bauen" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def _erneute_nahmen_finden" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def _aliase_und_importe" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "_SOLL_NEHMER_MINDESTENS = 45" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (Mindest-Soll existiert; **45** ist der bewusst gewaehlte Boden unter dem gemessenen
      Ist-Stand **47** — Begruendung im Kommentar an der Konstante)
    - `grep -cF "_ERNEUTE_NAHME_FALSCH_TREFFER = frozenset()" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (die LOCK-2-Whitelist startet **leer**)
    - `grep -cF "while geaendert:" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (Fixpunkt-Iteration statt Rekursion — D-3 Punkt 2)
    - `grep -cF "ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (verschachtelte `def`s werden ausgenommen — D-3 Punkt 1)
    - `grep -cF "_ist_session_state_lock(f.value)" tests/test_session_lock_blocking_calls_guard.py` → **2**
      (war 1 in LOCK-1 fuer `_ist_lock_release_aufruf`, jetzt zusaetzlich `_ist_riegel_acquire`
      — die Alias-Erkennung ist **wiederverwendet**, nicht ein zweites Mal geschrieben)
    - `grep -cF "_riegel_region(t)" tests/test_session_lock_blocking_calls_guard.py` → **≥ 1**
      (die LOCK-1-Regions-Logik ist wiederverwendet, nicht nachgebaut)
    - `grep -cF "isinstance(k, ast.Call)" tests/test_session_lock_blocking_calls_guard.py` → **≥ 4**
      (nur Aufruf-Knoten werden betrachtet. **≥ 2 waere kein Kriterium gewesen:** der
      LOCK-1-Bestand erfuellt es heute schon exakt — `:179` und `:199`. Der neue Code bringt
      drei weitere Vorkommen mit: `_nimmt_riegel_selbst`, `_kanten_der_funktion`,
      `_erneute_nahmen_finden`.)
    - `grep -cF "_aufloesen(" tests/test_session_lock_blocking_calls_guard.py` → **≥ 3**
      (Definition + beide Aufruf-Stellen — haengt **ausschliesslich** am neuen Code)
    - **Der namensbasierte Zweitpass ist verdrahtet** (D-3 Punkt 3 — ohne diese beiden Anker der
      einzige Konstruktionspunkt ohne Abnahme; er kann fehlen, ohne dass irgendetwas rot wird,
      weil der eine reale Treffer `:2076` ueber die **Alias**-Aufloesung laeuft):
      `grep -cF "namens_ziele & gefaehrliche_namen" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (der Fixpunkt zieht die Namens-Kanten mit),
      `grep -cF "else zname in gefaehrliche_namen" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (der Sweep wertet unaufloesbare Empfaenger ueber den blossen Namen aus)
    - `grep -cF "def test_keine_erneute_riegel_nahme_unter_dem_riegel" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def test_nehmer_ableitung_faellt_nicht_still_aus" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - **Der LOCK-1-Bestand ist unangetastet:**
      `grep -cF "_SOLL_SUMME_MINDESTENS = 101" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "'services/live_session.py': 25" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "def test_keine_blockierenden_aufrufe_unter_dem_sitzungs_riegel" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "def test_sweep_erreicht_alle_bekannten_bloecke" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "def test_sweep_erkennt_verbotene_aufrufe_in_allen_alias_schreibweisen" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "def test_sweep_meldet_harmlose_aufrufe_nicht" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "def test_sweep_erkennt_die_begrenzten_erwerbe_aus_plan_03" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (**alle fuenf** LOCK-1-Tests, nicht vier)
    - `grep -c "^import services\.\|^from services" tests/test_session_lock_blocking_calls_guard.py` → **0**
      (der Waechter laedt die App nicht — er parst nur Quelltext; bewusst auf Modul-Ebene
      verankert, weil Docstring und synthetische Quelltexte die Woerter enthalten duerfen)
    - `grep -c "pytest.mark" tests/test_session_lock_blocking_calls_guard.py` → **0**
      (`deploy.sh:221` faehrt `-m "not live and not perf"`)
    - `grep -c "RLock" tests/test_session_lock_blocking_calls_guard.py` → **0** im Code;
      eine Erwaehnung im Docstring ist erlaubt und zaehlt hier nicht (das Kriterium wird gegen
      `grep -c "= *threading.RLock\|RLock()" tests/test_session_lock_blocking_calls_guard.py` → **0** gemessen)
    - `git status --porcelain services/ routes/ app.py database/ alembic/ CLAUDE.md` → **leer**
      (dieser Plan fasst KEINEN Produktiv-Code an)
    - `git log -1 --stat` zeigt genau **1** geaenderte Datei
  </acceptance_criteria>

  <done>Der Waechter leitet die Nehmer-Menge per AST aus dem Code ab, verfolgt Aufrufe transitiv per Fixpunkt und meldet jede erneute Riegel-Nahme unter gehaltenem Riegel mit `Datei:Zeile` — ohne dass irgendwo eine Helfer-Liste gepflegt wird.</done>
</task>

<task type="auto">
  <name>Task 2: Selbst-Tests + Docstring mit benannten Restluecken</name>

  <read_first>
    - Die in Task 1 erweiterte Datei `tests/test_session_lock_blocking_calls_guard.py` —
      insbesondere die Signaturen von `_graph_bauen` und `_erneute_nahmen_finden`. Der
      Selbst-Test faehrt **genau diese** Funktionen, keinen Nachbau.
    - Derselben Datei Zeilen 340-364 — `_analysiere_quelle` / `_zaehle_bloecke` aus LOCK-1:
      das hauseigene Muster „synthetischer Quelltext statt verunreinigter Produktiv-Datei",
      inklusive der Begruendung im Modul-Docstring (Zeilen 52-64). Dieses Muster wird
      fortgesetzt, nicht neu erfunden.
    - Derselben Datei Zeilen 1-86 — der Modul-Docstring, der um den LOCK-2-Abschnitt
      erweitert wird (ANLASS, WARUM AST, RESTLUECKEN, STOP-REGEL).
    - `services/live_session.py` Zeilen 1518-1541 — der LOCKWATCH-Wachhund. Das ist die
      **zweite Schicht** unter dem Waechter und gehoert namentlich in den Restluecken-Absatz:
      was der statische Sweep nicht sieht, meldet der Wachhund zur Laufzeit.
    - `routes/app_routes.py` Zeilen 205-225 — `api_beenden` mit dem **verschachtelten**
      `_load_beenden_state:215`. Das ist der reale Fall, den der Verschachtelungs-Selbst-Test
      nachbaut.
    - `services/deepgram_service.py` Zeilen 248-260 — `threading.Timer(…, ls._flush_segment)`.
      Das ist der reale Fall, den der Argument-Referenz-Selbst-Test nachbaut.
  </read_first>

  <punkt14_audit>
### Schicht 1 — Lokaler Kontext
Weitere Tests in derselben Datei. Kein Produktiv-Code, keine DB, kein Netz, keine
Nebenlaeufigkeit.

### Schicht 2 — Funktions-Skelett
```
_erneute_nahme_in_quelle(quelltext, datei='services/synthetisch.py') -> [(datei, zeile, name)]
    ast.parse(dedent(quelltext)) -> {datei: baum} -> _erneute_nahmen_finden(...)
    (DIESELBE Funktion wie der Datei-Sweep, kein Mini-Detektor)
_erneute_nahme_in_quellen(quellen: dict[datei, quelltext]) -> [(datei, zeile, name)]
    Mehr-Datei-Variante. Noetig fuer die Alias-Aufloesung: die laeuft per Definition ueber
    Modul-Grenzen (Alias in Datei A zeigt auf Datei B) und ist mit EINER Datei nicht pruefbar.

test_transitivitaet_greift_ueber_zwei_ebenen()
test_verschachtelte_defs_haften_nicht_fuer_den_aeusseren()
test_argument_referenz_ist_kein_treffer()
test_zyklus_im_aufrufgraph_terminiert()
test_erneute_nahme_meldet_harmlose_aufrufe_nicht()
test_namensbasierter_zweitpass_faengt_methoden_aufrufe()      <- D-3 Punkt 3
test_alias_aufloesung_zielt_auf_die_datei_nicht_nur_den_namen()  <- Alias-Weg
```

### Schicht 3 — Cross-File-Awareness
Der Selbst-Test importiert nichts; die synthetischen Quelltexte liegen in `tests/`, das
**nicht** in `_SCAN_DIRS` steht → kein Selbst-Treffer im Datei-Sweep. Der Datei-Name im
synthetischen Aufruf (`services/synthetisch.py`) existiert nicht auf der Platte und wird nur
als **Schluessel** verwendet — `_python_dateien()` wird dabei nicht befragt.

### Schicht 4 — Edge-Case-Analyse
1. **Der Selbst-Test darf die Helfer nicht nachbauen.** Sonst prueft er eine Kopie und der
   echte Sweep bleibt unbewiesen (LOCK-1-Lehre, dort woertlich so entschieden).
2. **Einrueckung.** `textwrap.dedent` (bereits importiert), sonst `IndentationError` und der
   Test waere aus dem falschen Grund rot.
3. **Der 2-Ebenen-Fall ist die eigentliche Falsch-Gruen-Sperre.** Ohne ihn ist nicht
   unterscheidbar, ob die Transitivitaet greift oder ob es zufaellig keinen 2-Ebenen-Fall im
   Repo gibt. Er prueft **genau eine** Meldung an der Aufruf-Zeile des Helfers.
4. **Der Verschachtelungs-Fall prueft eine ABWESENHEIT.** Ein Test, der nur Anwesenheit prueft,
   liesse die `api_beenden`-Falschhaftung durch. Deshalb gepaart: der aeussere Rahmen ist
   **kein** Nehmer, die verschachtelte Funktion **schon**.
5. **Der Zyklus-Test ist ein Terminierungs-Beweis.** Er ist gruen, wenn er ueberhaupt
   zurueckkehrt. Damit ein Haenger nicht die ganze Suite blockiert, bleibt der Graph winzig
   (zwei Funktionen) — bei einer Rekursion ohne Besuchsmarkierung waere es ein sofortiger
   `RecursionError`, also rot statt haengend. Das ist die Absicht.
6. **Restluecken gehoeren in den Docstring, nicht ins Vergessen** (D-5). Ein Waechter beweist
   nur, was in seinem Pruefkatalog steht.
7. **Der namensbasierte Zweitpass ist sonst der EINZIGE Konstruktionspunkt ohne Beleg.** Die
   fuenf Tests oben fahren ausschliesslich **aufloesbare** Kanten (lokale Namen im selben
   Modul). Laesst der Executor `namens_ziele`/`gefaehrliche_namen` weg oder verdrahtet sie
   falsch, bleibt **alles gruen — auch der Rot-Beleg**, denn der eine reale Treffer (`:2076`)
   laeuft ueber die **Alias**-Aufloesung, nicht ueber den Namenspass. Das ist exakt die
   „blind statt rot"-Luecke, gegen die diese Phase gebaut wird. Deshalb ein eigener,
   **gepaarter** Test (bekannter Methodenname → 1 Treffer, unbekannter → 0).
8. **Die Alias-Aufloesung haengt heute nur am realen Treffer — und der verschwindet mit
   Plan 03.** Danach bewacht nichts mehr, ob `_ls_av.get_anonymisierer()` unter einem Riegel
   ueberhaupt noch erkannt wird. Der synthetische Fall braucht **zwei** Dateien (Alias in A →
   Ziel in B), deshalb `_erneute_nahme_in_quellen`. Die Gegenprobe zeigt derselbe Alias auf
   eine **riegel-freie** gleichnamige Funktion → **0** Treffer: das beweist, dass die
   Aufloesung auf die **Datei** zielt und nicht bloss auf den Namen.
  </punkt14_audit>

  <files>tests/test_session_lock_blocking_calls_guard.py</files>

  <action>
Dieselbe Datei erweitern (kein neuer Dateiname).

### 1. Helfer

```python
def _erneute_nahme_in_quelle(quelltext, datei='services/synthetisch.py'):
    """Faehrt die LOCK-2-Erweiterung gegen einen synthetischen Quelltext — bewusst ueber
    DIESELBE Funktion wie der Datei-Sweep (_erneute_nahmen_finden). Ein nachgebauter
    Mini-Detektor wuerde nur sich selbst beweisen (LOCK-1-Lehre)."""
    return _erneute_nahme_in_quellen({datei: quelltext})


def _erneute_nahme_in_quellen(quellen):
    """Mehr-Datei-Variante. Die Alias-Aufloesung laeuft per Definition ueber Modul-Grenzen
    (ein funktionslokales `import services.live_session as _ls_av` in Datei A zeigt auf Datei
    B) — mit nur EINER synthetischen Datei ist sie gar nicht pruefbar, weil `bekannte_module`
    dann nur diese eine Datei kennt."""
    return _erneute_nahmen_finden(
        {d: ast.parse(textwrap.dedent(q)) for d, q in quellen.items()})
```

### 2. `test_transitivitaet_greift_ueber_zwei_ebenen()`

Synthetischer Fall **Riegel-Block → Helfer → Riegel** (D-3 Punkt 5):

```python
    quelle = """
        def _tief():
            with ls._session_state_lock:
                pass

        def _mittel(sid):
            return _tief()

        def oben(sid):
            with ls._session_state_lock:
                x = _mittel(sid)
    """
```
Erwartung: **genau 1** Treffer, und zwar an der Zeile des `_mittel(sid)`-Aufrufs (nicht an der
`_tief()`-Zeile — die steht nicht unter einem fremden Riegel, sie IST der Riegel).
Assertion gepaart:
```python
    treffer = _erneute_nahme_in_quelle(quelle)
    assert len(treffer) == 1, f"Transitivitaet greift NICHT: {treffer!r}"
    assert treffer[0][2].endswith('::_mittel'), treffer
```
Plus die **Gegenprobe im selben Test**: dieselbe Quelle ohne `with ls._session_state_lock:`
in `oben` → **0** Treffer (sonst waere unklar, ob der Treffer aus der Region oder aus dem
Helfer stammt).

### 3. `test_verschachtelte_defs_haften_nicht_fuer_den_aeusseren()`

Nachbau von `routes/app_routes.py:api_beenden` / `_load_beenden_state:215` (D-3 Punkt 1):

```python
    quelle = """
        def api_beenden():
            def _load_beenden_state(sid):
                with ls._session_state_lock:
                    return 1
            return _load_beenden_state('x')

        def ruft_nur_den_aeusseren():
            with ls._session_state_lock:
                api_beenden()
    """
```
Zwei gepaarte Assertionen:
- `api_beenden` **ist** trotzdem gefaehrlich — aber **transitiv** (weil es
  `_load_beenden_state` **ruft**), nicht weil der Riegel in seinem Rumpf steht. Der Treffer in
  `ruft_nur_den_aeusseren` ist also **erwartet** und korrekt: **1** Treffer.
- Der **Nehmer**-Status haengt am eigenen Rumpf: `_graph_bauen` auf einer Variante **ohne** den
  `return _load_beenden_state('x')` liefert `api_beenden` **nicht** in `nehmer` **und nicht** in
  `gefaehrlich`:
```python
    quelle_ohne_aufruf = """
        def api_beenden():
            def _load_beenden_state(sid):
                with ls._session_state_lock:
                    return 1
            return 0
    """
    baeume = {'services/synthetisch.py': ast.parse(textwrap.dedent(quelle_ohne_aufruf))}
    gefaehrlich, _gn, nehmer, _p = _graph_bauen(baeume)
    assert ('services/synthetisch.py', 'api_beenden') not in nehmer
    assert ('services/synthetisch.py', 'api_beenden') not in gefaehrlich
    assert ('services/synthetisch.py', '_load_beenden_state') in nehmer
```
Das ist der Kern: **der aeussere Rahmen haftet nicht fuer den Riegel im verschachtelten
Rumpf — er haftet nur fuer das, was er selbst ruft.**

### 4. `test_argument_referenz_ist_kein_treffer()`

Nachbau von `services/deepgram_service.py:254` (D-3 Punkt 4):

```python
    quelle = """
        def _flush_segment(sid):
            with ls._session_state_lock:
                pass

        def plant_timer(sid):
            with ls._session_state_lock:
                threading.Timer(2.0, _flush_segment).start()
    """
```
Erwartung: **0** Treffer fuer `_flush_segment` (nur Wert-Uebergabe, der Callback feuert
spaeter ohne Riegel). Gepaarte Gegenprobe im selben Test: dieselbe Quelle mit
`_flush_segment(sid)` **als Aufruf** → **genau 1** Treffer. Ohne diese Paarung waere nicht
unterscheidbar, ob der Detektor korrekt trennt oder nur nichts findet.

### 5. `test_zyklus_im_aufrufgraph_terminiert()`

```python
    quelle = """
        def a():
            return b()

        def b():
            return a()

        def nimmt():
            with ls._session_state_lock:
                return a()
    """
```
Erwartung: der Aufruf kehrt zurueck (Terminierungs-Beweis) und liefert **0** Treffer —
weder `a` noch `b` nimmt je einen Riegel. Variante im selben Test: `b()` bekommt
zusaetzlich `with ls._session_state_lock: pass` → **1** Treffer auf `a`, ebenfalls
terminierend. Das beweist beides: Terminierung **und** dass der Zyklus die Gefahr
weitertraegt.

### 6. `test_erneute_nahme_meldet_harmlose_aufrufe_nicht()`

Jeweils **0** Treffer:
- `d.get('k', 0)` und `liste.append(x)` unter dem Riegel (dict-/list-Methoden, keine Nehmer)
- ein Aufruf einer riegel-freien Funktion unter dem Riegel (Nachbau von
  `ls.ist_painpoint_duplikat`, `services/live_session.py:1106`)
- derselbe Nehmer-Aufruf **ausserhalb** jedes Riegel-Blocks (Nachbau von
  `services/claude_service.py:2090` — das vorbildliche Muster **darf nicht** rot werden)
- ein Nehmer-Aufruf **nach** dem `release()` im `finally` (Nachbau von
  `stash_ended_session:742` → `pop_session_state`, entlastet)

### 7. `test_namensbasierter_zweitpass_faengt_methoden_aufrufe()`

**Der Anker fuer D-3 Punkt 3.** Realer Fall: `einwand_keyword_matcher.match_with_dedup` ist
fuer jeden Aufrufer nur als `matcher.match_with_dedup(...)` sichtbar — der Empfaenger ist
**nicht aufloesbar**, nur der Zweitpass ueber den blossen Namen faengt ihn.

```python
    quelle = """
        class EinwandMatcher:
            def match_with_dedup(self, text):
                with _ls._session_state_lock:
                    return 1

        def oben(matcher, text):
            with ls._session_state_lock:
                return matcher.match_with_dedup(text)
    """
```
Erwartung: **genau 1** Treffer, an der Zeile des `matcher.match_with_dedup(text)`-Aufrufs, und
der Meldename endet auf `::match_with_dedup` (die Datei ist `?`, weil der Empfaenger nicht
aufloesbar war — genau das ist der Beleg, dass der **Namens**pass gefeuert hat und nicht die
Alias-Aufloesung).

**Gepaarte Gegenprobe im selben Test:** dieselbe Quelle mit `matcher.unbekannte_methode(text)`
statt des bekannten Namens → **0** Treffer. Ohne diese Paarung waere nicht unterscheidbar, ob
der Zweitpass gezielt trifft oder pauschal jeden Attribut-Aufruf meldet.

### 8. `test_alias_aufloesung_zielt_auf_die_datei_nicht_nur_den_namen()`

Die Alias-Aufloesung (`ls` / `_ls` / `_ls_av` / `ls_module`) ist heute **nur** durch den einen
realen Treffer belegt — und **der verschwindet mit Plan 03**. Danach waere sie unbewacht.

```python
    ziel = """
        def get_anonymisierer(sid):
            with _session_state_lock:
                return 1
    """
    rufer = """
        def coaching_loop(sid):
            import services.live_session as _ls_av
            with _ls_av._session_state_lock:
                return _ls_av.get_anonymisierer(sid)
    """
    treffer = _erneute_nahme_in_quellen({
        'services/live_session.py': ziel,
        'services/claude_service.py': rufer,
    })
```
Erwartung: **genau 1** Treffer, Meldename
`services/live_session.py::get_anonymisierer` — das ist der **funktionslokale** Lazy-Import aus
`services/claude_service.py:936`, synthetisch nachgebaut.

**Gepaarte Gegenprobe im selben Test:** derselbe Rufer, aber
`import services.harmlos as _ls_av`, wobei `services/harmlos.py` eine **riegel-freie**
Funktion **gleichen Namens** enthaelt — und `services/live_session.py` mit dem gefaehrlichen
Namensvetter **weiterhin im Baum-Satz liegt**. Erwartung: **0** Treffer. Das ist der
eigentliche Beweis: die Aufloesung zielt auf die **Datei**, auf die der Alias zeigt, nicht
bloss auf den Namen — sonst waere hier faelschlich gemeldet worden.

### 9. Modul-Docstring erweitern

Unterhalb des LOCK-1-Texts ein neuer Abschnitt (die Ueberschriften woertlich, sie sind
Abnahme-Anker):

```
LOCK-2: ERNEUTE RIEGEL-NAHME UNTER GEHALTENEM RIEGEL
----------------------------------------------------
ANLASS: Anruf vom 31.07. Der LOCKWATCH-Wachhund aus LOCK-1 Teil 3 meldete beim ERSTEN
Einsatz auf Produktion `_session_state_lock >2s belegt | Faden='Thread-3 (coaching_loop)'
… gehalten=133.2s` — und zum ersten Mal stand der HALTER namentlich im Log statt nur seine
Opfer. Wurzel: services/claude_service.py:2062 nimmt den Riegel, :2076 ruft
ls.get_anonymisierer(sid), das denselben Riegel NOCHMAL nimmt. threading.Lock ist NICHT
reentrant (und das ist Absicht, services/live_session.py:308-310: „Ein RLock wuerde diesen
Design-Zwang lautlos aufloesen") -> der Faden blockiert SICH SELBST, dauerhaft. Im py-spy-
Abzug sieht ein Selbstverklemmer aus wie ein Opfer: er steht wartend in acquire(), wie alle
anderen auch.

WARUM DIE LOCK-1-HAELFTE DAS NICHT FAND: ihr Verbots-Set kennt get_session, SessionLocal,
messages.create/stream, sio.emit, requests.*, sleep, join — aber nicht die erneute
Riegel-Nahme. Genau die eine Klasse fehlte. Der Waechter war nicht falsch, er war
unvollstaendig; sein gruenes Ergebnis war wahr und wertlos zugleich.

WARUM DIE NEHMER-LISTE ABGELEITET UND NICHT GEPFLEGT WIRD: eine gepflegte Liste veraltet und
erzeugt genau dieselbe Luecke noch einmal. Der Kommentar bei services/claude_service.py:1441
ist der Beleg — dort stand das Wissen als Kommentar statt als Waechter, und bei
get_anonymisierer hat es niemand gelesen.

RESTLUECKEN — ein Waechter beweist nur, was in seinem Pruefkatalog steht (CLAUDE.md Punkt 31):
1. Dynamischer Dispatch: getattr(...)(...), Callbacks, Monkeypatch, Registry-Hooks sind fuer
   einen statischen Sweep unsichtbar.
2. Die Namens-Heuristik ist zweischneidig: sie faengt matcher.match_with_dedup, kann aber bei
   Namensgleichheit falsch anschlagen — und ein anders benannter Wrapper rutscht durch.
3. Kanten aus Modulen ausserhalb _SCAN_DIRS (app.py, database/, alembic/) werden nicht verfolgt.
4. dict-/str-Methodennamen sind NICHT ausgefiltert. Eine Kollision entstuende nur, wenn eine
   gefaehrliche Funktion wie eine dict-/str-Methode hiesse; unter den 47 abgeleiteten Nehmern
   ist das heute nicht der Fall. Die einzige theoretisch moegliche Kollision ist `index` —
   praktisch ausgeschlossen, formal UNKLAR.
ZWEITE SCHICHT DARUNTER: der LOCKWATCH-Wachhund (services/live_session.py:1518-1541) meldet
zur LAUFZEIT, was der statische Sweep nicht sieht. Er hat diesen Fund geliefert.

STOP-REGEL: erwartet sind 0 Verstoesse. Ein echter Fund wird mit Datei:Zeile gemeldet — NICHT
whitelistet, NICHT durch ein RLock aufgeloest, NICHT durch Aufweichen des Musters.
```

Ein Commit: `test(08.23.2.LOCK-2): Selbst-Tests fuer Transitivitaet, Verschachtelung, Argument-Referenz, Zyklus, Namens-Zweitpass, Alias-Aufloesung + Restluecken im Docstring`
  </action>

  <verify>
    <automated>grep -cF "def test_transitivitaet_greift_ueber_zwei_ebenen" tests/test_session_lock_blocking_calls_guard.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "def _erneute_nahme_in_quelle(quelltext" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (Muster bewusst bis `(quelltext` verlaengert: `grep -F` matcht Praefixe, und
      `def _erneute_nahme_in_quellen(` enthaelt `def _erneute_nahme_in_quelle` — das kuerzere
      Muster ergaebe 2 und waere damit unerfuellbar.)
    - `grep -cF "_erneute_nahmen_finden(" tests/test_session_lock_blocking_calls_guard.py` → **≥ 3**
      (Definition + Datei-Sweep + Selbst-Test-Helfer — der Selbst-Test faehrt den **echten**
      Detektor, keinen Nachbau)
    - `grep -cF "def test_transitivitaet_greift_ueber_zwei_ebenen" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def test_verschachtelte_defs_haften_nicht_fuer_den_aeusseren" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def test_argument_referenz_ist_kein_treffer" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def test_zyklus_im_aufrufgraph_terminiert" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - `grep -cF "def test_erneute_nahme_meldet_harmlose_aufrufe_nicht" tests/test_session_lock_blocking_calls_guard.py` → **1**
    - **Die zwei Wege, die sonst unbewacht blieben** (Namenspass hat keinen realen Beleg, die
      Alias-Aufloesung verliert ihren mit Plan 03):
      `grep -cF "def test_namensbasierter_zweitpass_faengt_methoden_aufrufe" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "def test_alias_aufloesung_zielt_auf_die_datei_nicht_nur_den_namen" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "def _erneute_nahme_in_quellen" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (die Mehr-Datei-Variante existiert — ohne sie ist der Alias-Fall gar nicht baubar),
      `grep -cF "match_with_dedup" tests/test_session_lock_blocking_calls_guard.py` → **≥ 2**
      (der reale unaufloesbare Empfaenger ist nachgebaut, nicht nur behauptet),
      `grep -cF "import services.live_session as _ls_av" tests/test_session_lock_blocking_calls_guard.py` → **≥ 1**
      (der funktionslokale Lazy-Import aus `services/claude_service.py:936` ist nachgebaut)
    - `grep -cF "_load_beenden_state" tests/test_session_lock_blocking_calls_guard.py` → **≥ 3**
      (der reale Verschachtelungs-Fall ist woertlich nachgebaut, nicht nur behauptet)
    - `grep -cF "threading.Timer" tests/test_session_lock_blocking_calls_guard.py` → **≥ 1**
      (die Argument-Referenz aus `services/deepgram_service.py:254` ist nachgebaut)
    - **Docstring-Anker (Restluecken sind benannt, nicht verschwiegen — D-5):**
      `grep -cF "RESTLUECKEN" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "Dynamischer Dispatch" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "ZWEITE SCHICHT DARUNTER" tests/test_session_lock_blocking_calls_guard.py` → **1**,
      `grep -cF "services/live_session.py:1518-1541" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (der LOCKWATCH-Wachhund ist als zweite Schicht namentlich genannt)
    - `grep -cF "CLAUDE.md Punkt 31" tests/test_session_lock_blocking_calls_guard.py` → **1**
      (Verweis auf die Regel, die Plan 04 verankert)
    - `grep -cF "STOP-REGEL" tests/test_session_lock_blocking_calls_guard.py` → **≥ 1**
    - `grep -c "^import services\.\|^from services" tests/test_session_lock_blocking_calls_guard.py` → **0**
    - `git status --porcelain services/ routes/ app.py database/ alembic/ CLAUDE.md` → **leer**
    - `git log -1 --stat` zeigt genau **1** geaenderte Datei
  </acceptance_criteria>

  <done>Der Waechter beweist an synthetischem Quelltext, dass seine Transitivitaet greift, dass verschachtelte `def`s nicht auf den aeusseren Rahmen durchschlagen, dass eine Argument-Referenz kein Treffer ist und dass ein Zyklus ihn nicht haengen laesst — und benennt im Docstring, was er NICHT sieht.</done>
</task>

</tasks>

<verification>
- Die Datei enthaelt nach beiden Tasks **14 Test-Funktionen**: die **fuenf** aus LOCK-1
  (unveraendert: `test_keine_blockierenden_aufrufe_unter_dem_sitzungs_riegel`,
  `test_sweep_erreicht_alle_bekannten_bloecke`,
  `test_sweep_erkennt_verbotene_aufrufe_in_allen_alias_schreibweisen`,
  `test_sweep_meldet_harmlose_aufrufe_nicht`,
  `test_sweep_erkennt_die_begrenzten_erwerbe_aus_plan_03`)
  + die **neun** neuen: `test_keine_erneute_riegel_nahme_unter_dem_riegel`,
  `test_nehmer_ableitung_faellt_nicht_still_aus`,
  `test_transitivitaet_greift_ueber_zwei_ebenen`,
  `test_verschachtelte_defs_haften_nicht_fuer_den_aeusseren`,
  `test_argument_referenz_ist_kein_treffer`, `test_zyklus_im_aufrufgraph_terminiert`,
  `test_erneute_nahme_meldet_harmlose_aufrufe_nicht`,
  `test_namensbasierter_zweitpass_faengt_methoden_aufrufe`,
  `test_alias_aufloesung_zielt_auf_die_datei_nicht_nur_den_namen`
  (`grep -c "^def test_" tests/test_session_lock_blocking_calls_guard.py` → **14**;
  Bestand vorher nachgezaehlt: **5**, nicht 4).
- Kein Produktiv-Code angefasst: `git diff --name-only HEAD~2` nennt **nur**
  `tests/test_session_lock_blocking_calls_guard.py`.
- **Kein pytest-Lauf in diesem Plan.** Der Beleg ist Plan 02.
</verification>

<success_criteria>
Der Waechter existiert, leitet die Nehmer-Menge aus dem Code ab, ist transitiv per Fixpunkt,
hat einen namensbasierten Zweitpass, meldet nur Aufruf-Positionen, faellt bei stiller
Ableitungs-Schwaeche rot statt blind — und seine Restluecken stehen benannt im Docstring.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-01-SUMMARY.md`

**Pflicht-Inhalt:**
- Was gebaut wurde, mit den neuen Funktionsnamen.
- **Offener Uebergabe-Punkt, woertlich:** „Rot-Beleg steht aus — Claudian faehrt
  `tests/test_session_lock_blocking_calls_guard.py` direkt auf dem Prod-Server (Befehlsblock
  in Plan 02 `<rot_beleg_lauf>`, **kein** `deploy.sh`). Entscheidend:
  `test_keine_erneute_riegel_nahme_unter_dem_riegel` muss **FAILED** sein, mit **genau einem**
  Treffer, und der muss `services/claude_service.py:2076` lauten."
- Der Hinweis, dass das **Deploy-Tor ab diesem Commit rot** ist, bis Plan 03 gebaut ist.
</output>
