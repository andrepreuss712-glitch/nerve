---
phase: 08.23.2.LOCK-2
plan: 01
subsystem: tests/waechter
tags: [lock, deadlock, ast-guard, deploy-gate]
requires: [tests/test_session_lock_blocking_calls_guard.py (LOCK-1-Bestand)]
provides:
  - "Waechter-Erweiterung: erneute Riegel-Nahme unter gehaltenem _session_state_lock (Call-Pfad + direkter Pfad)"
  - "_erneute_nahmen_finden / _graph_bauen / _direkte_erneute_nahmen als wiederverwendbare AST-Helfer"
affects:
  - "deploy.sh:221-222 Test-Gate (ab diesem Commit ROT, siehe unten)"
tech-stack:
  added: []
  patterns: ["AST-Call-Graph mit Fixpunkt-Iteration", "synthetischer Quelltext als Selbst-Test"]
key-files:
  created: []
  modified: [tests/test_session_lock_blocking_calls_guard.py]
decisions:
  - "_SOLL_NEHMER_MINDESTENS = 45 (Boden unter dem gemessenen Ist-Stand 47) — 'rot statt blind', kein Buchhaltungs-Anker"
  - "Keine Positions-Ausnahme im try-Pfad: eine echte Wieder-Nahme als erste Anweisung waere sonst still verschluckt worden"
  - "Der neue Test ist ABSICHTLICH rot bis Plan 03 (Rot-Beleg vor Fix, D-2)"
metrics:
  tasks: 2
  commits: 2
  files-changed: 1
  completed: 2026-07-31
---

# Phase 08.23.2.LOCK-2 Plan 01: Waechter 2 erweitern — Summary

Waechter 2 leitet die Menge der riegel-nehmenden Funktionen jetzt **per AST aus dem Code ab**
(nichts gepflegt, nichts hartkodiert), verfolgt Aufrufe **transitiv per Fixpunkt** und meldet
jede erneute Nahme des `_session_state_lock` unter bereits gehaltenem Riegel mit `Datei:Zeile`
— plus einen zweiten Pfad fuer die **direkte** Wieder-Nahme, die ueber den Call-Pfad
strukturell unsichtbar waere.

## Was gebaut wurde

Genau **eine** Datei angefasst: `tests/test_session_lock_blocking_calls_guard.py`.
Der komplette LOCK-1-Bestand (Konstanten, Helfer, alle **fuenf** Tests) ist **unveraendert**;
alles Neue liegt unterhalb der Ueberschrift
`# ══ LOCK-2: erneute Riegel-Nahme unter gehaltenem Riegel ══`.

### Task 1 — Nehmer-Ableitung + Fixpunkt + Verstoss-Sweep (Commit `01715c4`)

| Neue Funktion | Aufgabe |
|---|---|
| `_ist_riegel_acquire` | `<riegel>.acquire(...)` — nutzt **dieselbe** Alias-Erkennung `_ist_session_state_lock` wie der with-Zweig (keine zweite Alias-Logik) |
| `_eigene_knoten` | Rumpf **ohne** verschachtelte `def`/`class` — damit `api_beenden` nicht fuer `_load_beenden_state:215` haftet |
| `_nimmt_riegel_selbst` | Nehmer = Riegel-`with` **oder** `.acquire()` im eigenen Rumpf |
| `_punkt_name` / `_aliase_und_importe` | Modul-Schluessel + Alias-/Import-Aufloesung, per `ast.walk` (faengt die **funktionslokalen** Lazy-Importe `_ls_av` / `ls_module` / `_ls`) |
| `_funktionen_der_datei` / `_aufloesen` / `_kanten_der_funktion` | Call-Kanten, **nur** Aufruf-Positionen — keine Argument-Referenzen |
| `_graph_bauen` | Fixpunkt-Iteration (`while geaendert:`), **keine** Rekursion → terminiert bei Zyklen garantiert |
| `_direkte_erneute_nahmen` | **Pfad B**: verschachteltes Riegel-`with` + `<riegel>.acquire()` innerhalb einer gehaltenen Region, mit Identitaets-Ausschluss der Regions-Wurzel |
| `_erneute_nahmen_finden` | beide Pfade, ueber die **LOCK-1**-Regions-Helfer `_sammle_bloecke` / `_sammle_try_bloecke` / `_riegel_region` |

Neue Konstanten: `_SOLL_NEHMER_MINDESTENS = 45`, `_ERNEUTE_NAHME_FALSCH_TREFFER = frozenset()`
(startet leer), `_MELDE_WITH` / `_MELDE_ACQUIRE` (Meldenamen der direkten Wieder-Nahme,
bewusst von den Call-Treffern `<datei>::<funktion>` unterscheidbar — Plan 02 wertet die
Trefferzeilen aus).

Neue Tests: `test_keine_erneute_riegel_nahme_unter_dem_riegel`,
`test_nehmer_ableitung_faellt_nicht_still_aus`.

### Task 2 — Selbst-Tests + Restluecken im Docstring (Commit `8a65ad5`)

`_erneute_nahme_in_quelle` / `_erneute_nahme_in_quellen` fahren den **echten** Detektor
(`_erneute_nahmen_finden`) gegen synthetischen Quelltext — kein nachgebauter Mini-Detektor.
Die Mehr-Datei-Variante ist noetig, weil die Alias-Aufloesung per Definition ueber
Modul-Grenzen laeuft.

Acht neue Selbst-Tests, **alle gepaart** (Treffer **und** Gegenprobe):

1. `test_transitivitaet_greift_ueber_zwei_ebenen` — Block → Helfer → Riegel
2. `test_verschachtelte_defs_haften_nicht_fuer_den_aeusseren` — `api_beenden` ist transitiv
   gefaehrlich, aber **kein Nehmer**
3. `test_argument_referenz_ist_kein_treffer` — `threading.Timer(2.0, _flush_segment)`
4. `test_zyklus_im_aufrufgraph_terminiert` — Terminierungs-Beweis + Gefahr wandert weiter
5. `test_erneute_nahme_meldet_harmlose_aufrufe_nicht` — vier entlastete Realfaelle
6. `test_namensbasierter_zweitpass_faengt_methoden_aufrufe` — `matcher.match_with_dedup`
7. `test_alias_aufloesung_zielt_auf_die_datei_nicht_nur_den_namen` — Gegenprobe mit
   gleichnamiger riegel-freier Funktion in anderer Datei
8. `test_direkte_erneute_nahme_wird_gefangen` — **sechs** Faelle, Erwartung
   1 · 0 · 1 · 0 · 1 · 1

Der Modul-Docstring hat einen LOCK-2-Abschnitt bekommen: ANLASS (der `[LOCKWATCH]`-Fund vom
31.07.), WARUM DIE LOCK-1-HAELFTE DAS NICHT FAND, WARUM DIE NEHMER-LISTE ABGELEITET WIRD,
**RESTLUECKEN** (7 benannt), **GEPRUEFT UND GESCHLOSSEN** (die direkteste Form der
Fehlerklasse ist nicht offen gelassen), **ZWEITE SCHICHT DARUNTER** (LOCKWATCH-Wachhund
`services/live_session.py:1518-1541`), STOP-REGEL.

Die Datei enthaelt jetzt **15** Test-Funktionen (5 LOCK-1 unveraendert + 10 neu).

## ⚠ Der neue Test ist ABSICHTLICH ROT — und das Deploy-Tor ist es damit auch

`test_keine_erneute_riegel_nahme_unter_dem_riegel` **muss** am jetzigen Stand **FAILEN**.
Das ist kein Defekt, sondern der Rot-Beleg (D-2: „ein Waechter, der nie rot war, beweist
nichts"). Der Fix an `services/claude_service.py:2076` kommt erst in **Plan 03**.

**Ab Commit `01715c4` faerbt dieser Test das Deploy-Gate (`deploy.sh:221-222`) rot.**
In diesem Fenster ist **kein** Deploy moeglich, ohne das Gate zu umgehen — und genau das
passiert nicht. Wellen 1 → 2 → 3 sind in **einer** Sitzung durchzuziehen.

## Offener Uebergabe-Punkt (woertlich)

> Rot-Beleg steht aus — Claudian faehrt `tests/test_session_lock_blocking_calls_guard.py`
> direkt auf dem Prod-Server (Befehlsblock in Plan 02 `<rot_beleg_lauf>`, **kein**
> `deploy.sh`). Entscheidend: `test_keine_erneute_riegel_nahme_unter_dem_riegel` muss
> **FAILED** sein, mit **genau einem** Treffer, und der muss `services/claude_service.py:2076`
> lauten.

Zusatz aus D-2: meldet der Waechter **mehr als** `:2076`, ist das ein Widerspruch zum
gesetzten Fable-Ergebnis → STOP + im DIALOG melden, nicht stillschweigend mitfixen. Meldet er
**weniger/nichts**, greift die Erweiterung nicht → Waechter reparieren.

## Gemessene Ist-Zahl der abgeleiteten Nehmer

**Nicht erhoben — und bewusst nicht geraten.** Sie erfordert einen Python-Lauf, und
CLAUDE.md („Kein Local-Dev": kein lokales pytest, keine lokalen Smoke-Tests via `python -c`)
verbietet den lokal. Die Zahl ist **von Claudian im Beleg-Lauf (Plan 02) zu erheben** — der
Test druckt sie unter `-s` selbst aus:

```
[LOCK-2] Abgeleitete Riegel-NEHMER (nichts hartkodiert):
  <datei>: <n>
  SUMME: <n> Nehmer, davon transitiv gefaehrlich: <n>
```

Planungs-Messwert zum Vergleich (2026-07-31, Planner **und** Plan-Checker unabhaengig):
**47** Nehmer, **99** transitiv gefaehrlich ueber `_SCAN_DIRS`. `_SOLL_NEHMER_MINDESTENS = 45`
liegt bewusst darunter.

## Abnahme — alle Kriterien des Plans, gemessen

Ausschliesslich `grep`/`git` (Plan: „Kein pytest in diesem Task").

### Task 1

| Anker | Soll | Ist |
|---|---|---|
| `def _nimmt_riegel_selbst` | 1 | **1** |
| `def _eigene_knoten` | 1 | **1** |
| `def _graph_bauen` | 1 | **1** |
| `def _erneute_nahmen_finden` | 1 | **1** |
| `def _direkte_erneute_nahmen` | 1 | **1** |
| `_direkte_erneute_nahmen(baum)` | ≥ 2 | **2** |
| `_MELDE_WITH = '<direkte erneute Nahme (with)>'` | 1 | **1** |
| `_MELDE_ACQUIRE = '<direkte erneute Nahme (.acquire)>'` | 1 | **1** |
| `_MELDE_WITH` | ≥ 3 | **6** |
| `if k is wurzel:` | 1 | **1** |
| `and pos > 0` | 0 | **0** |
| `for anweisung in _riegel_region(tknoten):` | 1 | **1** |
| `def _aliase_und_importe` | 1 | **1** |
| `_SOLL_NEHMER_MINDESTENS = 45` | 1 | **1** |
| `_ERNEUTE_NAHME_FALSCH_TREFFER = frozenset()` | 1 | **1** |
| `while geaendert:` | 1 | **1** |
| `ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef` | 1 | **1** |
| `_ist_session_state_lock(f.value)` | 2 | **2** |
| `_riegel_region(t)` | ≥ 1 | **1** |
| `isinstance(k, ast.Call)` | ≥ 4 | **7** |
| `_aufloesen(` | ≥ 3 | **3** |
| `namens_ziele & gefaehrliche_namen` | 1 | **1** |
| `else zname in gefaehrliche_namen` | 1 | **1** |
| `def test_keine_erneute_riegel_nahme_unter_dem_riegel` | 1 | **1** |
| `def test_nehmer_ableitung_faellt_nicht_still_aus` | 1 | **1** |
| LOCK-1-Bestand: `_SOLL_SUMME_MINDESTENS = 101` | 1 | **1** |
| LOCK-1-Bestand: `'services/live_session.py': 25` | 1 | **1** |
| LOCK-1-Bestand: alle **fuenf** Test-Namen | je 1 | **je 1** |
| `^import services\.\|^from services` | 0 | **0** |
| `pytest.mark` | 0 | **0** |
| `= *threading.RLock\|RLock()` | 0 | **0** |
| `git status --porcelain services/ routes/ app.py database/ alembic/ CLAUDE.md` | leer | **leer** |
| `git log -1 --stat` | 1 Datei | **1 Datei** |

### Task 2

| Anker | Soll | Ist |
|---|---|---|
| `def _erneute_nahme_in_quelle(quelltext` | 1 | **1** |
| `_erneute_nahmen_finden(` | ≥ 3 | **3** |
| `def _erneute_nahme_in_quellen` | 1 | **1** |
| `def test_transitivitaet_greift_ueber_zwei_ebenen` | 1 | **1** |
| `def test_verschachtelte_defs_haften_nicht_fuer_den_aeusseren` | 1 | **1** |
| `def test_argument_referenz_ist_kein_treffer` | 1 | **1** |
| `def test_zyklus_im_aufrufgraph_terminiert` | 1 | **1** |
| `def test_erneute_nahme_meldet_harmlose_aufrufe_nicht` | 1 | **1** |
| `def test_namensbasierter_zweitpass_faengt_methoden_aufrufe` | 1 | **1** |
| `def test_alias_aufloesung_zielt_auf_die_datei_nicht_nur_den_namen` | 1 | **1** |
| `match_with_dedup` | ≥ 2 | **8** |
| `import services.live_session as _ls_av` | ≥ 1 | **3** |
| `def test_direkte_erneute_nahme_wird_gefangen` | 1 | **1** |
| `_MELDE_ACQUIRE` | ≥ 3 | **6** |
| `Die Regions-WURZEL meldet sich selbst` | 1 | **1** |
| `regions-EROEFFNENDE Erwerb meldet sich selbst` | 1 | **1** |
| `_load_beenden_state` | ≥ 3 | **8** |
| `threading.Timer` | ≥ 1 | **4** |
| `RESTLUECKEN` | 1 | **1** |
| `Dynamischer Dispatch` | 1 | **1** |
| `ZWEITE SCHICHT DARUNTER` | 1 | **1** |
| `services/live_session.py:1518-1541` | 1 | **1** |
| `Konstruktor-/Property-Durchrutscher` | 1 | **1** |
| `FALSCH-TREFFER (konservativ)` | 1 | **1** |
| `GEPRUEFT UND GESCHLOSSEN` | 1 | **1** |
| `CLAUDE.md Punkt 31` | 1 | **1** |
| `STOP-REGEL` | ≥ 1 | **2** |
| `^import services\.\|^from services` | 0 | **0** |
| `git status --porcelain services/ …` | leer | **leer** |
| `git log -1 --stat` | 1 Datei | **1 Datei** |

### Plan-Verifikation

| Kriterium | Soll | Ist |
|---|---|---|
| `grep -c "^def test_"` | 15 | **15** |
| `git diff --name-only HEAD~2 HEAD` | nur die Testdatei | **nur `tests/test_session_lock_blocking_calls_guard.py`** |
| pytest-Lauf in diesem Plan | keiner | **keiner** |

**Alle Abnahme-Kriterien des Plans sind erfuellt. Keine Zahl wurde aufgeweicht, kein Anker
umformuliert, kein Warn-/Schutztext geloescht.**

## Abweichungen vom Plan

**Eine, kosmetisch, selbst verursacht und korrigiert:**

**[Rule 1 — Bug] Doppelter `CLAUDE.md Punkt 31`-Verweis**
- **Gefunden in:** Task 2, beim Pruefen des Ankers `grep -cF "CLAUDE.md Punkt 31"` → soll **1**
- **Ursache:** Ich hatte im Task-1-Docstring von `_direkte_erneute_nahmen` „(Punkt 31)"
  (so im Plan) versehentlich als „(CLAUDE.md Punkt 31)" geschrieben — damit waere der Anker
  auf 2 gestiegen.
- **Fix:** zurueck auf den Plan-Wortlaut „(Punkt 31)". Der Verweis steht jetzt genau einmal,
  im Kopf des RESTLUECKEN-Abschnitts. **Kein** Warn-/Schutztext geloescht.
- **Commit:** `8a65ad5`

**Zeichensatz-Anker:** kein einziger Anker ist zeichensatz-bedingt rot geworden. Die
ASCII-Verengung aus Commit `7ede4c2` hat gehalten — alle 60+ `grep -F`-Muster liefen unter
Git-Bash/Windows sauber durch.

**Nicht angefasst (wie vorgeschrieben):** kein Produktiv-Code, kein Deploy, kein SSH, kein
`deploy.sh`, kein lokales pytest, kein RLock, kein Eintrag in einer Whitelist.

## Nicht-Ziele dieses Plans

- Der **Fix** an `services/claude_service.py:2076` → **Plan 03**
- Die **Warnkommentare** an `get_anonymisierer` / `get_counterpart` → **Plan 03**
- Der **CLAUDE.md-Punkt 31** (Restluecken-Regel) → **Plan 04**
- Der **Rot-Beleg-Lauf** auf Prod → **Plan 02 (Claudian)**

## Naechster Schritt

**Plan 02 — Rot-Beleg auf dem Prod-Server (Claudian).** Erst danach Plan 03 (Fix + Gruen).

## Self-Check: PASSED

- `tests/test_session_lock_blocking_calls_guard.py` — vorhanden
- `.planning/phases/08.23.2.LOCK-2-selbstverklemmung-beseitigen/08.23.2.LOCK-2-01-SUMMARY.md` — vorhanden
- Commit `01715c4` — vorhanden
- Commit `8a65ad5` — vorhanden
