---
phase: 08.23.2.MEHRNUTZER-REST-1
plan: 03
wave: 2
status: complete
completed: 2026-08-06
requirements: [MEHRNUTZER-REST-1-ROT-BELEG]
files_modified: []
---

# Plan 03 — ROT-Lauf ausgerollt, rote Tor-Ausgabe verbatim belegt

**Kein Code wurde in diesem Plan geaendert.** Welle 1 (Plan 01 + Plan 02) ging als reine
Test-Aenderung auf `origin/main` (`002f578..dcdcdf6`, Code-Diff gegen `services/`, `routes/`,
`app.py`, `database/`, `alembic/` = **leer**), danach `bash deploy.sh production`.

Das Tor ist **ROT** gelaufen und hat den Neustart geblockt. Das ist der Zweck dieses Plans
(CLAUDE.md Punkt 31: *„Ein Waechter, der nie rot war, beweist nichts"*; „Acht weitere
Vault-Regeln" Nr. 1: *Rot-Lauf vor dem Fix, Ausgabe verbatim*).

## ROT-Beleg (vor dem Fix)

Serverseitig, `/opt/nerve/venv/bin/pytest tests/ --tb=short -q -m "not live and not perf"`
gegen echtes Postgres `nerve_test`, 2026-08-06:

```
=================================== FAILURES ===================================
_______________ test_verschiedene_conv_ids_blockieren_sich_nicht _______________
tests/test_lernkarten_lock_pro_conv.py:200: in test_verschiedene_conv_ids_blockieren_sich_nicht
    assert erfolg.get('A') is True and erfolg.get('B') is True, (
E   AssertionError: Zwei verschiedene conv_id (4711/4712) waren NICHT gleichzeitig im kritischen Abschnitt: {'A': False, 'B': True}. Der Riegel in services/coaching_service.py:59 ist prozessweit — ein zweiter Nutzer wartet im Worst Case config.HTTP_LLM_TIMEOUT_LONG_S = 45 s (config.py:134) und belegt dabei einen der 64 gthread-Threads (deploy/nerve.service:35-36). Der Fix ist ein Riegel PRO conv_id, NICHT das ersatzlose Entfernen des Riegels.
E   assert (False is True)
E    +  where False = <built-in method get of dict object at 0x7a7cc7d2c700>('A')
E    +    where <built-in method get of dict object at 0x7a7cc7d2c700> = {'A': False, 'B': True}.get
________________ test_kein_modul_globaler_riegel_um_netz_aufruf ________________
tests/test_no_live_global_state.py:862: in test_kein_modul_globaler_riegel_um_netz_aufruf
    assert not verstoesse, (
E   AssertionError: 1 modul-globale(r) Riegel um einen Netz-/LLM-Aufruf:
E       services/coaching_service.py:84  [http_llm_client, messages.create]
E     
E     Punkt 28 (Mehr-Nutzer): ein prozessweiter Riegel um einen Netz-Aufruf serialisiert ALLE Nutzer ueber die volle Zeitlimit-Dauer.
E     NICHT den Riegel ersatzlos entfernen (der Duplikatschutz haengt daran) und NICHT die Falsch-Treffer-Liste fuellen. Den Riegel pro Schluessel fuehren (Muster: services/coaching_service.py _analysis_lock_for) ODER den Netz-Aufruf AUS dem Riegel-Block herausziehen.
E   assert not ['services/coaching_service.py:84  [http_llm_client, messages.create]']
=========================== short test summary info ============================
FAILED tests/test_lernkarten_lock_pro_conv.py::test_verschiedene_conv_ids_blockieren_sich_nicht
FAILED tests/test_no_live_global_state.py::test_kein_modul_globaler_riegel_um_netz_aufruf
2 failed, 1140 passed, 7 skipped, 5 deselected, 103 warnings in 86.04s (0:01:26)
[SLOW] flush_to_db: 0 items drained, 0 persisted
[deploy] FEHLER: pytest gegen nerve_test ROT — kein Restart, kein Deploy
DROP DATABASE
```

Hausform (vgl. `08.23.2.SOFORT-2-08-SUMMARY.md:78`):

```
ROT:   2 failed, 1140 passed, 7 skipped, 5 deselected, 103 warnings in 86.04s
GRUEN: (Plan 04) erwartet 1142 passed, 7 skipped, 5 deselected
```

## Warum die Zahl 1140 Teil des Belegs ist

`rot` allein waere kein Beweis — es koennte auch heissen, dass die Datei gar nicht eingesammelt
wurde. Die Rechnung geht auf:

| Posten | Zahl |
|---|---|
| Basis, letztes gruenes Tor (SOFORT-2, 06.08.) | 1133 |
| neu aus Plan 01 | +3 |
| neu aus Plan 02 | +6 |
| davon absichtlich rot (`rot_vor_fix`) | −2 |
| **erwartet und gemessen** | **1140** |

Die Basis ist **nicht** gewandert. Genau die zwei vorhergesagten Pruefungen sind rot, keine
dritte — kein stale Bestands-Test, keine gedriftete Soll-Zahl, kein Kollateralschaden in
anderen Dateien.

## Der Waechter ist aus dem richtigen Grund rot

Gemeldet wird **genau eine** deduplizierte Stelle:
`services/coaching_service.py:84  [http_llm_client, messages.create]` — der prozessweite
`_analysis_lock`, dessen `with`-Block den Sonnet-Aufruf umschliesst. Kein Treffer in einer
anderen Datei, kein Sammel-Rauschen.

## ⚠ Ein Abnahme-Kriterium des Plans ist so nicht messbar

Plan 03 verlangt, die Zeilen `[MEHRNUTZER-REST-1 Riegel-Waechter] Ist-Zaehlung`
(Riegel-Namen-Anzahl + SUMME) aus der Tor-Ausgabe zu belegen. **Sie stehen dort nicht** —
und koennen dort nicht stehen: die `print`-Aufrufe liegen in
`test_riegel_sweep_erreicht_alle_bekannten_bloecke` (`tests/test_no_live_global_state.py:890-895`),
und dieser Test ist **bestanden**. Unter `-q` ohne `-s` faengt pytest die Ausgabe bestandener
Tests ab; sichtbar wird sie nur bei einem Fehlschlag.

**Der Beleg gegen den stillen Ausfall der Ableitung existiert trotzdem — er hat nur eine andere
Form:** Genau dieser Test traegt die Mindest-Soll-Zusicherungen (`:912`, `>= 140` auf der Summe,
plus Namens- und Je-Datei-Soll). Waere die Riegel-Ableitung ausgefallen, waere er **rot**
geworden — dann stuenden dort 3 failed, nicht 2. Dass er unter den 1140 bestandenen liegt,
**ist** der Beweis, dass die Ableitung intakt ist. Die konkreten Zahlen (23 Namen / SUMME 143)
sind damit belegt-durch-Nichtunterschreitung, nicht abgelesen.

Das ist eine Praezisierung des Plan-Kriteriums, keine Abweichung im Bau: das Mindest-Soll nach
Punkt 31 („Sperre gegen den stillen Ausfall") tut genau, was es soll.

## Der Neustart wurde geblockt

`deploy.sh` bricht bei rotem Tor **vor** dem Neustart ab (`[deploy] FEHLER: pytest gegen
nerve_test ROT — kein Restart, kein Deploy`). Der alte Stand laeuft unveraendert auf
`app.getnerve.app` weiter. Production wurde durch diesen Plan **nicht** veraendert.

⚠ **Solange Plan 04 nicht durch ist, blockiert dieses rote Tor jeden anderen Deploy.**

## Werkzeug-Fund: `deploy.sh` ist fuer den Agenten gesperrt

`bash deploy.sh production` faellt unter eine Deny-Regel in `~/.claude/settings.json:34-37`
(`Bash(bash deploy.sh:*)` u.a.). Die Deny schlaegt die Allow-Regel in `settings.local.json:232`.
Der Lauf wurde deshalb von Andre selbst gefahren (`! bash deploy.sh production`), die Ausgabe
kam ueber den Hintergrund-Kanal zurueck. **Das beantwortet die offene Frage aus der
Sitzungs-Notiz: die Deny greift.** Fuer kuenftige Phasen heisst das: der Deploy-Schritt ist
immer ein Halt-Punkt fuer den Menschen, nie ein Agenten-Schritt.

## Was NICHT belegt ist

- Kein Live-Test im Browser — es gibt nichts Neues zu sehen, der alte Stand ist live.
- Der GRUEN-Lauf steht aus (Plan 04). Erst er beweist, dass der Fix beide roten Pruefungen
  dreht, **ohne** eine dritte zu brechen.

## Pruning-Notiz

Keine Code-Stelle ist durch diesen Plan obsolet geworden — es wurde kein Code geaendert.
