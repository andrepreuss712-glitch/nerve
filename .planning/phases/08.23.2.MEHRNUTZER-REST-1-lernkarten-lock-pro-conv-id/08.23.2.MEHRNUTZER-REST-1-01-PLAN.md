---
phase: 08.23.2.MEHRNUTZER-REST-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_lernkarten_lock_pro_conv.py
autonomous: true
requirements: [MEHRNUTZER-REST-1-REG-A, MEHRNUTZER-REST-1-REG-B, MEHRNUTZER-REST-1-REG-C]
komplexitaet: mittel

must_haves:
  truths:
    - "Ein Test beweist, dass zwei verschiedene conv_id gleichzeitig im kritischen Abschnitt sein koennen (heute unmoeglich -> ROT)."
    - "Ein Test beweist, dass zwei parallele Requests derselben conv_id weiterhin nur EINEN Satz Karten erzeugen."
    - "Ein dritter Test beweist, dass die Apparatur Duplikate ueberhaupt sehen kann (riegellose Mini-Funktion -> 6 Karten)."
    - "Kein Test misst eine Wanduhr; jedes wait()/join() traegt ein eigenes timeout."
  artifacts:
    - path: "tests/test_lernkarten_lock_pro_conv.py"
      provides: "Regressionstest-Netz fuer den Riegel pro conv_id"
      contains: "rot_vor_fix"
      min_lines: 180
  key_links:
    - from: "tests/test_lernkarten_lock_pro_conv.py"
      to: "services.coaching_service.generate_postcall_analysis"
      via: "direkter Aufruf der ECHTEN Funktion in zwei Faden"
      pattern: "generate_postcall_analysis\\("
    - from: "tests/test_lernkarten_lock_pro_conv.py"
      to: "services.claude_service.claude_client"
      via: "patch.object(..., 'with_options', return_value=fake_client)"
      pattern: "patch\\.object\\(.*with_options"
---

<objective>
Das Regressionstest-Netz dieser Phase bauen — **ohne** den Fix. Drei Bausteine in **einer** neuen
Datei `tests/test_lernkarten_lock_pro_conv.py`:

- **(a)** zwei **verschiedene** `conv_id` sind **gleichzeitig** im kritischen Abschnitt.
  Gegen den ungefixten Stand **ROT** (Marker `rot_vor_fix`).
- **(b)** zwei parallele Requests **derselben** `conv_id` erzeugen weiterhin **EINEN** Satz Karten
  (3 Karten, 1 LLM-Aufruf). **Ausdruecklich KEIN ROT-Beleg** — der heutige globale Riegel schuetzt
  genauso. Der Test ist der **Gegenpol gegen den falschen Fix** („Riegel ersatzlos raus").
- **(c)** dieselbe Apparatur gegen eine im Testmodul definierte, absichtlich **riegellose**
  Mini-Funktion → **6** Karten. Beweist, dass (b) Duplikate ueberhaupt sehen **kann**.

Purpose: CLAUDE.md „Acht weitere Vault-Regeln" Nr. 1 (Regressionstest ROT vor GRUEN) + Roadmap
Test-Netz-Ratsche Punkt 1. Ein Test, der nie rot war, beweist nichts.
Output: eine neue Test-Datei. **Kein Produktiv-Code wird in diesem Plan angefasst.**

Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-RESEARCH.md

<interfaces>
<!-- Alles, was der Executor braucht. KEINE Codebase-Suche noetig. -->

Die zu testende Funktion (`services/coaching_service.py:49-141`, Signatur bleibt unveraendert):

```python
generate_postcall_analysis(conv_id, user_id, einwaende, painpoints,
                           kb_start, kb_end, redeanteil_berater,
                           redeanteil_kunde, dauer_sek,
                           skript_abdeckung, ga_details,
                           kaufsignale=None, profile_data=None)
```

Ihr Ablauf **heute** (die Zeilen, die dieser Test angreift):

```python
  8  _analysis_lock = threading.Lock()          # Modul-global, prozessweit
 59  with _analysis_lock:                       # umschliesst DEN GANZEN RUMPF
 61      from database.db import get_session    # funktionslokaler Import -> patchbar
 63      db_check = get_session()
 65      existing = db_check.query(LearningCard).filter_by(call_id=conv_id).count()
 66      if existing > 0: return []              # Duplikat-Pfad
 84      response = http_llm_client(long_running=True).messages.create(...)
113      vorschlaege = result.get('vorschlaege', [])[:3]
116      db = get_session()
118-129     for v in vorschlaege: db.add(LearningCard(user_id=…, call_id=conv_id, …))
130      db.commit()
136      db.close()
138      return vorschlaege
```

Der **einzige** funktionierende Mock-Angriffspunkt fuer den LLM (Vorbild
`tests/test_stabil1_http_llm_timeout.py:57-79`, laeuft heute gruen):

```python
import services.claude_service as claude_service_module
from services.coaching_service import generate_postcall_analysis

fake_client = MagicMock()
fake_client.messages.create.return_value = fake_msg
with patch.object(claude_service_module.claude_client, 'with_options',
                  return_value=fake_client), \
     patch('database.db.get_session', return_value=fake_db):
    generate_postcall_analysis(conv_id=1, user_id=1, einwaende=[], painpoints=[],
                               kb_start=0, kb_end=30, redeanteil_berater=50,
                               redeanteil_kunde=50, dauer_sek=60,
                               skript_abdeckung=0, ga_details=[])
```

Warum genau so (RESEARCH §3.1):
- `coaching_service` importiert `http_llm_client` beim Modul-Import, aber `http_llm_client` liest
  zur **Laufzeit** `claude_client.with_options(...)` (`services/claude_service.py:62-64`) → das
  Patchen des Modul-Clients wirkt durch.
- `patch.object(..., 'with_options', ...)` statt `setattr(cs, 'claude_client', ...)`: robust gegen
  Testreihenfolge (`tests/test_heiler_resolved.py:103-105` warnt, dass `cs.claude_client` je nach
  Reihenfolge schon ein `_FakeAnthropic` ist).
- `patch('database.db.get_session', ...)` wirkt, weil `coaching_service` `get_session`
  **funktionslokal** importiert (`:61`, `:116`).
- `usage=None` auf der Fake-Antwort haelt die Kosten-Haken inaktiv (`coaching_service.py:93 if u:`)
  → **kein** echter DB-Write, **kein** `cleanup_rows`, **kein** real-PG, **keine** Fixture.

Rendezvous-Vorbild (`tests/test_session_lock_deadlock_guard.py:110-132`):

```python
held = threading.Event()
h = threading.Thread(target=_halte, daemon=True, name='LOCK1-holder')
h.start()
assert held.wait(timeout=_HELD_READY_S), 'Halter-Faden hat den Riegel nicht genommen'
...
h.join(timeout=5.0)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-Modul anlegen — Docstring, Konstanten, Fake-Apparatur</name>

  <read_first>
    - `tests/test_stabil1_http_llm_timeout.py` (ganz, 158 Z.) — Vorbild fuer Modul-Docstring
      (`:1-20`: „Kein Netz, kein echter Anthropic-Call, keine committenden DB-Writes -> kein
      cleanup_rows noetig"), fuer `_fake_response` (`:31-37`) und fuer das Mock-Muster (`:57-79`).
    - `tests/test_session_lock_deadlock_guard.py:95-135` — Vorbild fuer Event-Rendezvous,
      `daemon=True`, benannte Faden, `join(timeout=…)`, Notbremse-Kommentar zu `deploy.sh:221`.
    - `services/coaching_service.py:49-141` — der Ablauf, den die Fake-DB nachstellen muss
      (`count()` bei `:65`, `add()` bei `:129`, `commit()` bei `:130`, `rollback()` bei `:133`,
      `close()` bei `:136`).
    - `pytest.ini` — der Marker `rot_vor_fix` existiert bereits, **nicht** neu anlegen.
  </read_first>

  <files>tests/test_lernkarten_lock_pro_conv.py</files>

  <behavior>
    - `_FakeDB` liefert bei `query(...).filter_by(call_id=X).count()` die Anzahl bereits
      **committeter** Karten mit `call_id == X`.
    - `_FakeDB.add()` legt in einem **fadeneigenen** Zwischenspeicher ab; erst `commit()` uebernimmt
      in die geteilte Liste. Zwei Faden vermischen ihre ungeschriebenen Karten also nicht.
    - `_fake_response('{"vorschlaege": []}')` traegt `usage=None`.
  </behavior>

  <action>
Neue Datei `tests/test_lernkarten_lock_pro_conv.py` anlegen. Inhalt dieses Tasks (die drei Tests
kommen in Task 2 und 3 dazu):

```python
"""Phase 08.23.2.MEHRNUTZER-REST-1 — Riegel PRO conv_id statt EIN globaler Riegel.

Der Befund (Roadmap + RESEARCH §1): services/coaching_service.py:8 haelt einen
modul-globalen threading.Lock(), und :59 umschliesst mit ihm den GANZEN Rumpf von
generate_postcall_analysis — inklusive des Sonnet-Aufrufs auf :84, dessen Zeitlimit
config.HTTP_LLM_TIMEOUT_LONG_S = 45.0 s betraegt (config.py:134). Zwei gleichzeitige
Anruf-Enden laufen damit hintereinander statt nebeneinander.

Was diese Datei prueft — und was NICHT:
  (a) test_verschiedene_conv_ids_blockieren_sich_nicht
      ROT-BELEG. Marker rot_vor_fix. Gegen den ungefixten Stand MUSS er failen.
  (b) test_gleiche_conv_id_erzeugt_nur_einen_satz_karten
      KEIN ROT-BELEG (RESEARCH §3.5 / Falle 6). Der heutige globale Riegel schuetzt
      exakt genauso. Dieser Test ist der GEGENPOL gegen den FALSCHEN Fix
      ("Riegel ersatzlos raus, die DB-Pruefung dahinter schuetzt schon") — der ist am
      Code widerlegt: learning_cards hat KEINEN Unique-Constraint auf call_id
      (database/models.py:629-631) und kann auch keinen bekommen (bis zu 3 Karten
      pro Call by design, coaching_service.py:113).
  (c) test_riegellose_variante_erzeugt_doppelte_karten
      FALSIFIZIERBARKEIT. Dieselbe Apparatur gegen eine hier im Testmodul definierte,
      absichtlich riegellose Mini-Funktion -> 6 statt 3 Karten. Ohne diesen Test
      waere (b) eine Behauptung: er beweist, dass die Apparatur Duplikate SEHEN kann.
      Muster "synthetischer Quelltext statt verunreinigter Produktiv-Datei", wie
      tests/test_session_lock_blocking_calls_guard.py:432-441.

KEINE Wanduhr-Messung. "Keine Serialisierung" wird ueber ein threading.Event-Rendezvous
bewiesen ("sind beide gleichzeitig im kritischen Abschnitt?"), nicht ueber
"parallel < 2x seriell" — das wuerde auf dem geteilten VPS unter Deploy-Last die
Maschine messen statt den Code. Deshalb auch KEIN perf-Marker: der wuerde den Test aus
dem Deploy-Tor werfen (-m "not live and not perf") und die Ratsche entwerten.

Jedes wait()/join() traegt ein EIGENES timeout: deploy.sh:221 hat KEINEN pytest-Timeout,
ein haengender Test haengt den gesamten Deploy (tests/test_session_lock_deadlock_guard.py:118-119).

Kein Netz, kein echter Anthropic-Call, keine committenden DB-Writes -> kein cleanup_rows
noetig, keine db_session/client-Fixture, kein TEST_DATABASE_URL. usage=None auf der
Fake-Antwort haelt die Cost-Hooks inaktiv (coaching_service.py:93 "if u:").
"""
import json
import threading
from types import SimpleNamespace
from unittest.mock import patch, MagicMock

import pytest

import services.claude_service as claude_service_module
from services.coaching_service import generate_postcall_analysis

# ── Zeitgrenzen ───────────────────────────────────────────────────────────────
# (a): wird nur im ROT-Lauf voll ausgeschoepft. Im gefixten Stand kehren beide wait()
#      in Millisekunden zurueck. 5.0 s ist lang genug gegen jeden Scheduler-Ruckler
#      auf dem geteilten VPS und kurz genug fuers Tor (RESEARCH §3.4).
_RENDEZVOUS_S = 5.0
# (b)/(c): wird im KORREKTEN Stand JEDES MAL voll ausgeschoepft (der zweite Faden steht
#      dann am conv_id-Riegel und meldet sich nie). Das ist die dauerhafte Tor-Kosten
#      dieses Gegenpols.
#      SIZING-BELEG (nicht geraten): die Konstante muss NUR die Faden-Startzeit im
#      RIEGELLOSEN Fall ueberbieten — und genau die misst Test (c) empirisch. Dort
#      kommen beide Faden in Millisekunden am Rendezvous an und (c) sieht seine 6 Karten.
#      0.5 s ist damit durch (c) belegt und spart 1,5 s pro Deploy-Tor gegenueber 2.0.
#      Sollte (c) je flattern, ist DAS der Beleg zum Nachziehen — kein Bauchgefuehl.
_GEGENPOL_S = 0.5
# Notbremse fuer das Einsammeln der Faden.
_JOIN_S = 20.0

_DREI_VORSCHLAEGE = json.dumps({'vorschlaege': [
    {'category': 'einwand_preis', 'original_suggestion': f'Satz {i}',
     'alternative_1': 'a1', 'alternative_2': 'a2', 'lernziel': 'Lernziel'}
    for i in range(3)
]})


def _fake_response(text='{"vorschlaege": []}'):
    """Stand-in fuer ein anthropic.types.Message. usage=None haelt die Cost-Hooks
    inaktiv (coaching_service.py:93 "if u:") -> keine echte DB-Session noetig."""
    return SimpleNamespace(content=[SimpleNamespace(text=text)], usage=None)


def _zweig():
    """'A' oder 'B' — abgeleitet aus dem Faden-Namen (Muster
    tests/test_session_lock_deadlock_guard.py:172 'LOCK1-pruefling-A')."""
    return threading.current_thread().name[-1]


# ── Gefaelschte DB-Session mit ECHTEM Zustand ────────────────────────────────
class _FakeQuery:
    def __init__(self, db):
        self._db = db
        self._kw = {}

    def filter_by(self, **kw):
        self._kw = kw
        return self

    def count(self):
        cid = self._kw.get('call_id')
        with self._db.sperre:
            return sum(1 for k in self._db.karten if k == cid)


class _FakeDB:
    """MagicMock reicht hier NICHT: (b)/(c) muessen zaehlen, wie viele Karten wirklich
    entstanden sind. add() legt fadeneigen ab, commit() uebernimmt in die geteilte
    Liste — sonst vermischten zwei Faden ihre ungeschriebenen Karten.

    Committet nichts in eine echte DB -> kein cleanup_rows (tests/conftest.py:263)."""

    def __init__(self):
        self.karten = []              # committete call_id-Werte
        self.sperre = threading.Lock()
        self._offen = {}              # faden-name -> [call_id, ...]

    def query(self, *_a, **_kw):
        return _FakeQuery(self)

    def add(self, card):
        self._offen.setdefault(threading.current_thread().name, []).append(card.call_id)

    def commit(self):
        n = threading.current_thread().name
        with self.sperre:
            self.karten.extend(self._offen.pop(n, []))

    def rollback(self):
        self._offen.pop(threading.current_thread().name, None)

    def close(self):
        pass


def _starte_paar(ziel, argumente):
    """Zwei Daemon-Faden 'MEHRNUTZER-A'/'MEHRNUTZER-B', gestartet und zurueckgegeben.
    daemon=True + join(timeout=) sind Pflicht (deploy.sh:221 ohne pytest-Timeout)."""
    faeden = []
    for name, arg in zip(('MEHRNUTZER-A', 'MEHRNUTZER-B'), argumente):
        t = threading.Thread(target=ziel, args=(arg,), daemon=True, name=name)
        faeden.append(t)
    for t in faeden:
        t.start()
    return faeden


def _sammle_ein(faeden):
    for t in faeden:
        t.join(timeout=_JOIN_S)
    for t in faeden:
        assert not t.is_alive(), (
            f'Faden {t.name} kehrte nicht innerhalb {_JOIN_S}s zurueck — '
            f'ein haengender Faden haengt den Deploy (deploy.sh:221 ohne pytest-Timeout).')


def _rufe_analyse(conv_id):
    generate_postcall_analysis(
        conv_id=conv_id, user_id=1, einwaende=[], painpoints=[],
        kb_start=0, kb_end=30, redeanteil_berater=50, redeanteil_kunde=50,
        dauer_sek=60, skript_abdeckung=0, ga_details=[],
    )
```

**Bau-Regel 17 / Reparatur-Modus:** In diesem Task wird **ausschliesslich** diese neue Datei
angelegt. Kein Produktiv-Code, keine andere Test-Datei, keine `pytest.ini`-Aenderung
(`rot_vor_fix` existiert bereits).
  </action>

  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/test_lernkarten_lock_pro_conv.py',encoding='utf-8').read())"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f tests/test_lernkarten_lock_pro_conv.py` → 0
    - `grep -c "class _FakeDB" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "usage=None" tests/test_lernkarten_lock_pro_conv.py` → mindestens 1
    - `grep -c "_RENDEZVOUS_S = 5.0" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -cF "_GEGENPOL_S = 0.5" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "daemon=True" tests/test_lernkarten_lock_pro_conv.py` → mindestens 1
    - `grep -c "cleanup_rows" tests/test_lernkarten_lock_pro_conv.py` → mindestens 1
      (der Docstring MUSS sagen, warum keines gebraucht wird)
    - `grep -c "@pytest.mark.perf" tests/test_lernkarten_lock_pro_conv.py` → 0
    - `grep -c "time.time()" tests/test_lernkarten_lock_pro_conv.py` → 0
    - `git diff --name-only -- tests/test_lernkarten_lock_pro_conv.py` nennt die Datei
    - **kein** Produktiv-Code im Diff:
      `git diff --name-only | grep -c "^services/\|^routes/\|^app.py"` → 0
      ⚠ **Nicht** `git diff --name-only` ohne Pfad-Filter pruefen: Plan 01 und Plan 02 laufen
      in Welle 1 **parallel im selben Arbeitsverzeichnis**. Die Datei des jeweils anderen Plans
      steht dann immer mit im Diff — ein "ausschliesslich"-Kriterium waere falsch-rot.
  </acceptance_criteria>

  <done>
    Datei existiert, parst, enthaelt Docstring + Konstanten + `_FakeDB`/`_FakeQuery` +
    `_fake_response` + `_starte_paar`/`_sammle_ein`/`_rufe_analyse`. Noch kein Test darin.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test (a) — der ROT-Beleg: zwei verschiedene conv_id gleichzeitig im kritischen Abschnitt</name>

  <read_first>
    - die in Task 1 angelegte Datei `tests/test_lernkarten_lock_pro_conv.py`
    - `services/coaching_service.py:59-84` — der Riegel und der LLM-Aufruf, an dem das
      Rendezvous haengt.
    - `tests/test_stabil1_http_llm_timeout.py:57-79` — das Mock-Paar verbatim.
    - `pytest.ini` — Marker-Zeile `rot_vor_fix` (Wortlaut fuer den Kommentar).
  </read_first>

  <files>tests/test_lernkarten_lock_pro_conv.py</files>

  <behavior>
    - Faden A und Faden B rufen `generate_postcall_analysis` mit **verschiedenen** `conv_id`
      (4711 / 4712) auf.
    - Im gefaelschten `messages.create` setzt jeder Faden sein eigenes Event und wartet auf das
      des anderen (`timeout=_RENDEZVOUS_S`).
    - **Heute (globaler Riegel):** B kommt nie ueber `coaching_service.py:59` hinaus → A's `wait`
      liefert nach 5 s `False` → **ROT**. Das ist eine Blockade, kein Rennen — deterministisch.
    - **Nach dem Fix:** beide nehmen ihren eigenen conv_id-Riegel, beide `wait` liefern `True`.
  </behavior>

  <action>
Diesen Test an die Datei anhaengen:

```python
# ══ (a) ROT-BELEG ═════════════════════════════════════════════════════════════

@pytest.mark.rot_vor_fix
def test_verschiedene_conv_ids_blockieren_sich_nicht():
    """Zwei VERSCHIEDENE conv_id muessen GLEICHZEITIG im kritischen Abschnitt sein koennen.

    Gegen den ungefixten Stand (services/coaching_service.py:59 "with _analysis_lock:")
    ist das unmoeglich: Faden A haelt den prozessweiten Riegel und steht im gefaelschten
    LLM-Aufruf, Faden B kommt nie ueber :59 hinaus. A's wait() laeuft in den Timeout,
    erfolg['A'] ist False -> ROT. Das ist eine BLOCKADE, kein Rennen; der Test flattert
    also nicht.

    Nach dem Fix nehmen beide ihren eigenen conv_id-Riegel, beide Events sind gesetzt,
    beide wait() kehren mit True zurueck — typischerweise in Millisekunden.

    Bewusst KEINE Zeitmessung: "parallel < 2x seriell" wuerde auf dem geteilten VPS die
    Maschine messen statt den Code (RESEARCH §3.4 / Falle 4).
    """
    drin = {'A': threading.Event(), 'B': threading.Event()}
    erfolg = {}

    def _fake_create(**_kwargs):
        wer = _zweig()
        anderer = 'B' if wer == 'A' else 'A'
        drin[wer].set()
        erfolg[wer] = drin[anderer].wait(timeout=_RENDEZVOUS_S)
        return _fake_response()

    fake_client = MagicMock()
    fake_client.messages.create.side_effect = _fake_create

    fake_db = MagicMock()
    fake_db.query.return_value.filter_by.return_value.count.return_value = 0

    with patch.object(claude_service_module.claude_client, 'with_options',
                      return_value=fake_client), \
         patch('database.db.get_session', return_value=fake_db):
        faeden = _starte_paar(_rufe_analyse, (4711, 4712))
        _sammle_ein(faeden)

    assert erfolg.get('A') is True and erfolg.get('B') is True, (
        f"Zwei verschiedene conv_id (4711/4712) waren NICHT gleichzeitig im kritischen "
        f"Abschnitt: {erfolg!r}. Der Riegel in services/coaching_service.py:59 ist "
        f"prozessweit — ein zweiter Nutzer wartet im Worst Case "
        f"config.HTTP_LLM_TIMEOUT_LONG_S = 45 s (config.py:134) und belegt dabei einen "
        f"der 64 gthread-Threads (deploy/nerve.service:35-36). Der Fix ist ein Riegel "
        f"PRO conv_id, NICHT das ersatzlose Entfernen des Riegels.")
```

⚠ Der Marker `@pytest.mark.rot_vor_fix` ist **Pflicht** und wird vom Tor **nicht** deselektiert
(`deploy.sh:221` faehrt `-m "not live and not perf"`). Der Test laeuft also mit und faerbt das Tor
in Welle 1 rot — **das ist gewollt** (Roadmap Test-Netz-Ratsche Punkt 1).
  </action>

  <verify>
    <automated>python -c "import ast,sys; s=open('tests/test_lernkarten_lock_pro_conv.py',encoding='utf-8').read(); ast.parse(s); assert 'rot_vor_fix' in s and 'test_verschiedene_conv_ids_blockieren_sich_nicht' in s"</automated>
    <!-- Abnahme ist NICHT lokal: das Tor auf dem Server entscheidet (Plan 03). -->
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_verschiedene_conv_ids_blockieren_sich_nicht" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "@pytest.mark.rot_vor_fix" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -cF "drin[anderer].wait(timeout=_RENDEZVOUS_S)" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "side_effect = _fake_create" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -cF "patch.object(claude_service_module.claude_client, 'with_options'" tests/test_lernkarten_lock_pro_conv.py` → mindestens 1
    - `grep -c "4711" tests/test_lernkarten_lock_pro_conv.py` → mindestens 1 (verschiedene conv_id)
    - **kein** `wait(` ohne `timeout=`: `grep -cF ".wait()" tests/test_lernkarten_lock_pro_conv.py` → 0
  </acceptance_criteria>

  <done>
    Test (a) steht in der Datei, traegt `rot_vor_fix`, benutzt beidseitiges Event-Rendezvous mit
    Timeout und ruft die **echte** `generate_postcall_analysis` in zwei benannten Daemon-Faden.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test (b) Gegenpol + Test (c) Falsifizierbarkeit</name>

  <read_first>
    - die in Task 1/2 gebaute Datei `tests/test_lernkarten_lock_pro_conv.py`
    - `services/coaching_service.py:59-138` — insbesondere die Reihenfolge
      count (`:65`) → LLM (`:84`) → `add`/`commit` (`:129-130`). Das Rendezvous sitzt zwischen
      count und commit; genau dadurch ist (c) deterministisch.
    - `tests/test_session_lock_blocking_calls_guard.py:432-441` — Muster „synthetischer
      Quelltext statt verunreinigter Produktiv-Datei" (Begruendung, warum (c) eine
      Mini-Funktion IM TESTMODUL benutzt und NICHT den Produktiv-Riegel entfernt).
    - `database/models.py:628-641` — `LearningCard`, insbesondere: `__table_args__` enthaelt
      **nur** den Schild-Kommentar, **keinen** `UniqueConstraint`; `call_id` ist
      `nullable=True` und **nicht** unique.
  </read_first>

  <files>tests/test_lernkarten_lock_pro_conv.py</files>

  <behavior>
    - (b): beide Faden, **gleiche** `conv_id=4711`. Ergebnis MUSS sein: `len(fake_db.karten) == 3`
      und `len(aufrufe) == 1`.
    - (c): dieselbe Apparatur gegen `_riegellos_erzeuge_karten` → `len(fake_db.karten) == 6`.
    - Das Rendezvous liegt in beiden Faellen **nach** der count-Abfrage und **vor** dem Schreiben.
      Damit ist (c) deterministisch: A schreibt erst, nachdem B gezaehlt hat.
  </behavior>

  <action>
Diese zwei Tests anhaengen:

```python
# ══ (b) GEGENPOL — KEIN ROT-BELEG ═════════════════════════════════════════════

def test_gleiche_conv_id_erzeugt_nur_einen_satz_karten():
    """Zwei parallele Requests DERSELBEN conv_id erzeugen weiterhin EINEN Satz Karten.

    ⚠ EHRLICHKEIT (RESEARCH §3.5 / Falle 6): Dieser Test ist gegen den HEUTIGEN Stand
    GRUEN. Er ist KEIN ROT-Beleg und darf im SUMMARY nicht als solcher gefuehrt werden —
    der globale Riegel schuetzt exakt genauso. Er ist der GEGENPOL gegen den FALSCHEN
    Fix ("Riegel ersatzlos raus"): learning_cards hat KEINEN Unique-Constraint auf
    call_id (database/models.py:629-631) und kann auch keinen bekommen (bis zu 3 Karten
    pro Call by design, coaching_service.py:113). Ohne Riegel lesen beide Faden die 0
    und schreiben beide.

    Warum das deterministisch beisst: das Rendezvous sitzt IM gefaelschten LLM-Aufruf —
    also NACH der count-Abfrage (:65) und VOR dem Schreiben (:129-130). Faden A schreibt
    erst, nachdem B sich gemeldet hat; B meldet sich erst, nachdem B gezaehlt hat. Im
    riegellosen Fall zaehlen also beide zwingend 0 (-> Test (c) belegt genau das).
    Im korrekten Fall steht B am conv_id-Riegel, A laeuft nach _GEGENPOL_S weiter,
    schreibt 3, gibt frei; B zaehlt dann 3 und ueberspringt den Sonnet-Aufruf.
    Die _GEGENPOL_S sind die dauerhaften Tor-Kosten dieses Gegenpols.
    """
    fake_db = _FakeDB()
    drin = {'A': threading.Event(), 'B': threading.Event()}
    aufrufe = []
    aufrufe_sperre = threading.Lock()

    def _fake_create(**_kwargs):
        with aufrufe_sperre:
            aufrufe.append(_zweig())
        wer = _zweig()
        anderer = 'B' if wer == 'A' else 'A'
        drin[wer].set()
        drin[anderer].wait(timeout=_GEGENPOL_S)
        return _fake_response(_DREI_VORSCHLAEGE)

    fake_client = MagicMock()
    fake_client.messages.create.side_effect = _fake_create

    with patch.object(claude_service_module.claude_client, 'with_options',
                      return_value=fake_client), \
         patch('database.db.get_session', return_value=fake_db):
        faeden = _starte_paar(_rufe_analyse, (4711, 4711))
        _sammle_ein(faeden)

    assert len(fake_db.karten) == 3, (
        f"Duplikatschutz gebrochen: {len(fake_db.karten)} Karten fuer conv_id=4711 statt 3. "
        f"Der Riegel darf NICHT ersatzlos entfallen — die count()-Pruefung in "
        f"services/coaching_service.py:65 ist die EINZIGE Duplikat-Sperre im Prozess.")
    assert len(aufrufe) == 1, (
        f"Der Sonnet-Aufruf lief {len(aufrufe)}x statt 1x ({aufrufe!r}). Das ist die "
        f"schaerfere der beiden Aussagen: sie faellt auch dann, wenn die Karten zufaellig "
        f"ueberschrieben statt verdoppelt wuerden.")


# ══ (c) FALSIFIZIERBARKEIT ════════════════════════════════════════════════════

def _riegellos_erzeuge_karten(conv_id, db, drin):
    """Absichtlich RIEGELLOSE Mini-Fassung derselben count-dann-schreib-Logik.

    Steht bewusst IM TESTMODUL (Muster "synthetischer Quelltext",
    tests/test_session_lock_blocking_calls_guard.py:432-441): so laesst sich beweisen,
    dass die Apparatur Duplikate ueberhaupt SEHEN kann, OHNE Produktiv-Code anzufassen
    und ohne Rueckbau-Risiko. Sie bildet coaching_service.py:65 / :129-130 nach.
    """
    class _Karte:
        def __init__(self, call_id):
            self.call_id = call_id

    if db.query(None).filter_by(call_id=conv_id).count() > 0:
        return
    wer = _zweig()
    anderer = 'B' if wer == 'A' else 'A'
    drin[wer].set()
    drin[anderer].wait(timeout=_GEGENPOL_S)
    for _ in range(3):
        db.add(_Karte(conv_id))
    db.commit()


def test_riegellose_variante_erzeugt_doppelte_karten():
    """Anti-Stub: OHNE Riegel entstehen 6 Karten. Damit ist bewiesen, dass Test (b)
    Duplikate sehen KANN und nicht bloss immer gruen ist."""
    fake_db = _FakeDB()
    drin = {'A': threading.Event(), 'B': threading.Event()}

    faeden = _starte_paar(lambda cid: _riegellos_erzeuge_karten(cid, fake_db, drin),
                          (4711, 4711))
    _sammle_ein(faeden)

    assert len(fake_db.karten) == 6, (
        f"Die Apparatur sieht Duplikate NICHT: riegellos entstanden {len(fake_db.karten)} "
        f"statt 6 Karten. Solange das so ist, beweist Test (b) nichts.")
```
  </action>

  <verify>
    <automated>python -c "import ast; s=open('tests/test_lernkarten_lock_pro_conv.py',encoding='utf-8').read(); ast.parse(s); assert s.count('def test_')==3"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_" tests/test_lernkarten_lock_pro_conv.py` → 3
    - `grep -c "def test_gleiche_conv_id_erzeugt_nur_einen_satz_karten" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "def test_riegellose_variante_erzeugt_doppelte_karten" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "len(fake_db.karten) == 3" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "len(fake_db.karten) == 6" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -c "len(aufrufe) == 1" tests/test_lernkarten_lock_pro_conv.py` → 1
    - `grep -cF "KEIN ROT-Beleg" tests/test_lernkarten_lock_pro_conv.py` → mindestens 1
      (die Ehrlichkeits-Aussage MUSS im Docstring stehen)
    - **Kein** Produktiv-Code angefasst: `git diff --name-only | grep -c "^services/\|^routes/"` → 0
  </acceptance_criteria>

  <done>
    Drei Tests in einer Datei. (a) traegt `rot_vor_fix`, (b) ist explizit als Gegenpol und
    NICHT-ROT-Beleg dokumentiert, (c) beweist die Falsifizierbarkeit von (b).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/api/postcall_cards` | untrusted `conv_id` aus dem JSON-Rumpf (`routes/learning.py:383`) |
| Nutzer A ↔ Nutzer B (ein Prozess) | 64 gthread-Threads teilen sich einen Modul-Namensraum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MEHRNUTZER-01 | Denial of Service | `services/coaching_service.py:59` | mitigate | Der prozessweite Riegel serialisiert alle Nutzer ueber einen 45-s-Aufruf. Wird in Plan 04 durch einen Riegel pro `conv_id` ersetzt; Test (a) dieses Plans ist der Nachweis. |
| T-MEHRNUTZER-02 | Tampering | `learning_cards` (Duplikate) | mitigate | Duplikatschutz bleibt (Test (b)); Falsifizierbarkeit ueber Test (c). |
| T-MEHRNUTZER-03 | Information Disclosure | Test-Apparatur | accept | Der Test faelscht LLM und DB vollstaendig; es verlassen keine Daten den Prozess, es wird nichts persistiert. |
| T-MEHRNUTZER-04 | Denial of Service | Deploy-Tor | mitigate | Jedes `wait()`/`join()` traegt ein eigenes `timeout` — `deploy.sh:221` hat keinen pytest-Timeout (RESEARCH Falle 5). |
</threat_model>

<verification>
- `tests/test_lernkarten_lock_pro_conv.py` existiert, parst, enthaelt genau 3 Tests.
- Genau **ein** Test traegt `@pytest.mark.rot_vor_fix`.
- Kein `perf`/`live`-Marker, keine Wanduhr-Messung, kein `wait()` ohne `timeout`.
- Kein Produktiv-Code und keine andere Test-Datei im Diff.
- **Abnahme ist NICHT lokal** (CLAUDE.md HART: Kein Local-Dev). Der scharfe Lauf gegen das
  serverseitige Tor passiert in **Plan 03** und muss dort **ROT** sein.
</verification>

<success_criteria>
- Drei Tests geschrieben, davon einer als ROT-Beleg markiert.
- Der Gegenpol (b) ist im Code selbst als **kein** ROT-Beleg dokumentiert (Falle 6).
- Die Falsifizierbarkeit (c) benutzt eine Mini-Funktion **im Testmodul**, nicht den Produktiv-Code.
- Diff enthaelt ausschliesslich `tests/test_lernkarten_lock_pro_conv.py`.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-01-SUMMARY.md` anlegen.
**Nicht** deployen — der ROT-Lauf gehoert zu Plan 03 (Welle 2), damit beide Test-Bausteine in
**einem** roten Lauf belegt werden.
</output>
