---
phase: 08.23.2.MEHRNUTZER-REST-1
plan: 03
type: execute
wave: 2
depends_on: ["08.23.2.MEHRNUTZER-REST-1-01", "08.23.2.MEHRNUTZER-REST-1-02"]
files_modified: []
autonomous: false
requirements: [MEHRNUTZER-REST-1-ROT-BELEG]
komplexitaet: mittel

must_haves:
  truths:
    - "Beide neuen Test-Bausteine sind gegen den UNGEFIXTEN Stand nachweislich ROT gelaufen."
    - "Die rote Tor-Ausgabe steht verbatim in einer Commit-Message und im SUMMARY."
    - "Das rote Tor hat den Neustart geblockt — der alte Stand ist noch live."
  artifacts:
    - path: ".planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md"
      provides: "ROT-Beleg verbatim (Acceptance-Artefakt nach CLAUDE.md Punkt 31)"
      contains: "failed"
  key_links:
    - from: "deploy.sh production"
      to: "pytest tests/ --tb=short -q -m 'not live and not perf'"
      via: "serverseitiges Test-Tor vor dem Restart"
      pattern: "deploy.sh"
---

<objective>
Den **ROT-Lauf** ziehen. Beide Wellen-1-Bausteine (Plan 01 Regressionstest, Plan 02 Waechter)
gehen in **einem** Commit auf den Server und muessen dort **failen** — gegen den **ungefixten**
`services/coaching_service.py`.

Purpose: CLAUDE.md „Acht weitere Vault-Regeln" Nr. 1 + Punkt 31: *„Ein Waechter, der nie rot war,
beweist nichts. Der Rot-Lauf ist ein **Acceptance-Artefakt** (Ausgabe verbatim ins SUMMARY), keine
Nebenbemerkung — und er wird **vor** dem Fix gezogen, nicht danach."*

Output: die verbatim rote Tor-Ausgabe, festgehalten in einer Commit-Message und im SUMMARY.
**Kein Code wird in diesem Plan geaendert.**

⚠ **Das rote Tor ist kein Fehlschlag, sondern der Zweck dieses Plans.** `deploy.sh` bricht bei
rotem Tor **vor** dem Neustart ab; der alte Stand bleibt live. Genau so soll es sein.

Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-RESEARCH.md
@.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-01-SUMMARY.md
@.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-02-SUMMARY.md

<erwartungswerte>
Das Tor faehrt serverseitig `/opt/nerve/venv/bin/pytest tests/ --tb=short -q -m "not live and not perf"`
gegen echtes Postgres `nerve_test` (`deploy.sh:208/221`) — **kein xdist, kein pytest-Timeout**.

Letztes gruenes Tor (Phase 08.23.2.SOFORT-2, 2026-08-06):

```
1133 passed, 7 skipped, 5 deselected
```

Diese Phase legt **9** neue Pruefungen an: 3 aus Plan 01 + 6 aus Plan 02.

| Lauf | Erwartung |
|---|---|
| **ROT (dieser Plan)** | `2 failed, 1140 passed, 7 skipped, 5 deselected` |
| GRUEN (Plan 04) | `1142 passed, 7 skipped, 5 deselected` |

Die **zwei** erwarteten Fehlschlaege — und **nur** diese zwei:

1. `tests/test_lernkarten_lock_pro_conv.py::test_verschiedene_conv_ids_blockieren_sich_nicht`
   → `erfolg` enthaelt mindestens ein `False`. Faden B kommt nie ueber
   `services/coaching_service.py:59` hinaus. Kostet ~5 s (`_RENDEZVOUS_S`).
2. `tests/test_no_live_global_state.py::test_kein_modul_globaler_riegel_um_netz_aufruf`
   → meldet **genau eine** deduplizierte Stelle:
   `services/coaching_service.py:84  [http_llm_client, messages.create]`

**Beide tragen `rot_vor_fix`.** Der Marker ist **nicht** deselektiert (`-m "not live and not perf"`
nennt ihn nicht) — die Pruefungen laufen also mit und faerben das Tor rot. Das ist gewollt.

⚠ Die Nehmer-Zahl ist **Teil des Belegs**: „1140 passed" beweist, dass die neun neuen Pruefungen
wirklich mitgelaufen sind. „rot" allein waere kein Beleg.
</erwartungswerte>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Welle-1-Commit und Push</name>

  <read_first>
    - `git status` und `git diff --stat` — es duerfen **ausschliesslich**
      `tests/test_lernkarten_lock_pro_conv.py` und `tests/test_no_live_global_state.py`
      (plus die beiden SUMMARY-Dateien der Plaene 01/02) im Diff stehen.
    - `CLAUDE.md`, Abschnitt „HART: Kein Local-Dev" — Workflow pro Plan-Task:
      Edit → commit → push → `bash deploy.sh production`.
  </read_first>

  <files>(keine Code-Datei — nur git)</files>

  <action>
Pruefen, dass **kein** Produktiv-Code im Diff steht:

```bash
git status --porcelain
git diff --stat HEAD
```

Erwartet: nur `tests/test_lernkarten_lock_pro_conv.py`, `tests/test_no_live_global_state.py`
und `.planning/phases/08.23.2.MEHRNUTZER-REST-1-*/…-SUMMARY.md`.
**Steht `services/coaching_service.py` im Diff → STOP.** Der Fix gehoert zu Plan 04; er darf
nicht in diesem Commit sein, sonst gibt es keinen ROT-Lauf und der ganze Beleg ist wertlos.

Dann committen und pushen:

```bash
git add tests/test_lernkarten_lock_pro_conv.py tests/test_no_live_global_state.py \
        .planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/
git commit -m "test(08.23.2.MEHRNUTZER-REST-1): Welle 1 — Regressionsnetz + Riegel-Waechter, ROT vor Fix

Beide Bausteine gehen ABSICHTLICH gegen den ungefixten Stand:
- test_verschiedene_conv_ids_blockieren_sich_nicht (rot_vor_fix)
- test_kein_modul_globaler_riegel_um_netz_aufruf (rot_vor_fix)

Der Fix (services/coaching_service.py:8/:59) folgt in Welle 3.
Das rote Tor blockt den Neustart — der alte Stand bleibt live. Das ist der Zweck."
git push origin main
```
  </action>

  <verify>
    <automated>git log -1 --stat | grep -E "coaching_service" ; test $? -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --name-only --format=` listet **nicht** `services/coaching_service.py`
    - `git log -1 --name-only --format= | grep -c "tests/test_lernkarten_lock_pro_conv.py"` → 1
    - `git log -1 --name-only --format= | grep -c "tests/test_no_live_global_state.py"` → 1
    - `git status --porcelain | grep -c "^ M services/"` → 0
    - `git log origin/main -1 --format=%H` == `git log -1 --format=%H` (gepusht)
  </acceptance_criteria>

  <done>
    Ein Commit mit **beiden** Test-Dateien und **ohne** Produktiv-Code liegt auf `origin/main`.
  </done>
</task>

<task type="auto">
  <name>Task 2: `bash deploy.sh production` — ROT erwartet, Ausgabe verbatim sichern</name>

  <read_first>
    - `deploy.sh`, Zeilen ~200-240 — der pytest-Abschnitt (`:208` DSN, `:221` das
      pytest-Kommando, `:225-239` POST-SUITE-Baseline-Check). Vorher lesen, damit die
      Ausgabe richtig eingeordnet wird.
    - der Abschnitt `<erwartungswerte>` oben — die zwei erwarteten Fehlschlaege und die Zahlen.
  </read_first>

  <files>(keine — Deploy-Lauf)</files>

  <action>
```bash
bash deploy.sh production
```

**Die vollstaendige Ausgabe des pytest-Abschnitts abfangen und VERBATIM aufbewahren** — mindestens:
- die Zusammenfassungszeile (`… failed, … passed, … skipped, … deselected in …s`)
- die beiden `FAILED …`-Zeilen mit Testnamen
- den Ausschnitt der Assert-Meldung von
  `test_kein_modul_globaler_riegel_um_netz_aufruf`, der `services/coaching_service.py:84` nennt
- die `[MEHRNUTZER-REST-1 Riegel-Waechter] Ist-Zaehlung`-Zeilen (Riegel-Namen-Anzahl + SUMME) —
  sie belegen, dass die Ableitung **nicht** ausgefallen ist

Nicht zusammenfassen, nicht paraphrasieren, nicht kuerzen. Der verbatim Text ist das
Acceptance-Artefakt.

**Drei Abweichungen und was sie bedeuten:**

| Beobachtung | Bedeutung | Was tun |
|---|---|---|
| **0 failed** (Tor gruen) | Mindestens einer der beiden Bausteine prueft nichts. | STOP. Nicht weiterbauen. Ursache im Test finden (haeufigster Kandidat: das Rendezvous liegt nicht im gefaelschten `messages.create`, oder der Marker wurde deselektiert). Zurueck zu Plan 01/02. |
| **3+ failed** | Ein dritter Test ist stale (Punkt 18) oder eine Soll-Zahl ist gedriftet. | Den dritten Fehlschlag benennen. Ist es `test_riegel_sweep_erreicht_alle_bekannten_bloecke` → Ist-Zaehlung aus dem Log ablesen, Soll-Tabelle in Plan 02 **mit Begruendung** nachziehen (`services/coaching_service.py` in diesem Plan **nie** antasten — der Nachzug 1→3 gehoert in Plan 04 Task 1 (4), zusammen mit dem Fix). Ist es ein Bestands-Test → Punkt 18 Stale-Test-Contract, im SUMMARY benennen. |
| **Der Lauf haengt** | Ein `wait()`/`join()` ohne `timeout` (`deploy.sh:221` hat **keinen** pytest-Timeout). | Deploy abbrechen, Timeouts in `tests/test_lernkarten_lock_pro_conv.py` nachziehen. RESEARCH Falle 5. |

Erwartet ist, dass `deploy.sh` **vor** dem Neustart abbricht. Gegenprobe, dass der alte Stand
weiterlaeuft:

```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
    'cd /opt/nerve/app && bash scripts/inspect.sh service-status'
```
  </action>

  <verify>
    <automated>bash deploy.sh production; test $? -ne 0</automated>
    <!-- ROT IST das Erfolgssignal dieses Plans. Das nachgestellte `test $? -ne 0` dreht den
         Exit-Code um, damit ein Executor-Harness den roten Lauf NICHT als Task-Fehlschlag
         wertet und anfaengt zu "reparieren" — das wuerde den ROT-Beleg zerstoeren.
         Gruenes Tor (Exit 0) laesst dieses Kommando fehlschlagen: genau richtig, denn dann
         prueft mindestens einer der beiden Bausteine nichts (siehe Abweichungs-Tabelle). -->
  </verify>

  <acceptance_criteria>
    - Die Tor-Ausgabe enthaelt woertlich `2 failed`
    - Die Tor-Ausgabe nennt `test_verschiedene_conv_ids_blockieren_sich_nicht`
    - Die Tor-Ausgabe nennt `test_kein_modul_globaler_riegel_um_netz_aufruf`
    - Die Assert-Meldung nennt woertlich `services/coaching_service.py:84`
    - Die Zahl der bestandenen Pruefungen ist **1140** (= 1133 Basis + 9 neu − 2 rot).
      Weicht sie ab, ist die Basis gewandert → im SUMMARY mit neuer Basis begruenden.
    - Die Ist-Zaehlung meldet `abgeleitete Riegel-Namen: 23` (oder mehr) und `SUMME: 143`
      (oder mehr) — sonst ist die Ableitung ausgefallen
    - `bash scripts/inspect.sh service-status` zeigt den Dienst weiterhin laufend (alter Stand)
  </acceptance_criteria>

  <done>
    Der ROT-Lauf ist gezogen, die Ausgabe liegt verbatim vor, der Neustart wurde geblockt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ROT-Beleg festschreiben — Commit-Message, SUMMARY, Fragen-Kanal</name>

  <read_first>
    - die verbatim Ausgabe aus Task 2
    - `.planning/phases/08.23.2.SOFORT-2-*/08.23.2.SOFORT-2-08-SUMMARY.md:78` und `:110` —
      die Hausform des ROT/GRUEN-Belegs:
      ```
      ROT:   2 failed, 1131 passed, 7 skipped, 5 deselected in 76.16s
      GRUEN: 1133 passed, 7 skipped, 5 deselected, 103 warnings in 74.61s
      ```
    - `CLAUDE.md` Nr. 4 (Fragen-Kanal): Eintrag ans **Ende** von
      `.planning/DIALOG-GSD-CLAUDIAN.md`, **sofort committen**, **zusaetzlich** als
      normaler Fliesstext im Terminal — **nie** als interaktives Menue.
  </read_first>

  <what-built>
    Der ROT-Lauf ist gezogen. Beide Test-Bausteine sind gegen den ungefixten Stand rot;
    `deploy.sh` hat den Neustart geblockt, der alte Stand ist weiterhin live.
  </what-built>

  <action>
1. **SUMMARY schreiben** (`…-03-SUMMARY.md`) mit der verbatim roten Ausgabe unter einer
   Ueberschrift `## ROT-Beleg (vor dem Fix)`. Zusaetzlich die Ist-Zaehlung des Waechters
   (Riegel-Namen + SUMME) — sie beweist, dass die Ableitung nicht ausgefallen ist.

2. **ROT-Beleg als Commit festschreiben** (der Beleg entsteht zeitlich nach dem Test-Commit,
   deshalb ein Folge-Commit — `main` wird nicht force-gepusht):

```bash
git add .planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/
git commit -m "docs(08.23.2.MEHRNUTZER-REST-1): ROT-Beleg Welle 1 verbatim

$(<verbatim Tor-Ausgabe: Zusammenfassungszeile + beide FAILED-Zeilen +
   die Assert-Zeile mit services/coaching_service.py:84>)

Punkt 31: der Rot-Lauf ist ein Acceptance-Artefakt, kein Nebensatz."
git push origin main
```

3. **Fragen-Kanal bedienen** — die vier offenen Auslegungsfragen aus RESEARCH §11 ans **Ende**
   von `.planning/DIALOG-GSD-CLAUDIAN.md` schreiben (`## FRAGE — 08.23.2.MEHRNUTZER-REST-1 —
   <Datum>`), sofort committen und **zusaetzlich als Fliesstext im Terminal** zeigen:
   - Variante A (Riegel-Erkennung ueber Aufrufnamen) ist umgesetzt — gilt das als Aufweichen
     oder als Erweiterung? (Meine Lesart: Erweiterung, die bewachte Menge wird groesser.)
   - Schild `learning_cards` (`database/models.py:628`) zitiert stale Zeilennummern (`:170`,
     der zweite Leser steht auf `:200`) und driftet durch den Fix um eine weitere Zeile.
     Geplant: **nicht** anfassen (waere eine Alembic-Revision, die Roadmap erwartet keine),
     als Folgefund notiert.
   - Weite Riegel-Ableitung `endswith('Lock')` (143 statt 40 bewachte Bloecke) — umgesetzt.
   - Rendezvous 5,0 s im ROT-Lauf + 0,5 s dauerhaft im Gegenpol-Test — akzeptabel?
     (Die 0,5 s sind kein Bauchwert: Test (c) misst die Faden-Startzeit im riegellosen Fall
     empirisch und kommt in Millisekunden an — er ist der Sizing-Beleg.)
  </action>

  <how-to-verify>
    1. `.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md`
       oeffnen: steht dort die **verbatim** Tor-Ausgabe (nicht paraphrasiert)?
    2. `git log -2 --format=%s%n%b` — enthaelt der ROT-Beleg-Commit die Zusammenfassungszeile
       mit `2 failed` und die Zeile mit `services/coaching_service.py:84`?
    3. `tail -40 .planning/DIALOG-GSD-CLAUDIAN.md` — stehen die vier Fragen dort?
    4. Die App auf `https://app.getnerve.app` laeuft weiterhin (alter Stand, Neustart geblockt).
  </how-to-verify>

  <acceptance_criteria>
    - `grep -c "2 failed" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md` → mindestens 1
    - `grep -cF "services/coaching_service.py:84" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md` → mindestens 1
    - `grep -c "ROT-Beleg" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md` → mindestens 1
    - `git log -1 --format=%B | grep -c "failed"` → mindestens 1
    - `grep -c "FRAGE — 08.23.2.MEHRNUTZER-REST-1" .planning/DIALOG-GSD-CLAUDIAN.md` → 1
    - `git status --porcelain` → leer (alles committet und gepusht)
  </acceptance_criteria>

  <resume-signal>
    „freigegeben" tippen — dann geht es mit Plan 04 (dem Fix) weiter. Oder Abweichungen
    beschreiben (z.B. eine Antwort auf eine der vier Fragen).
  </resume-signal>
</task>

</tasks>

<verification>
- Genau **zwei** Pruefungen sind rot, und zwar die beiden mit `rot_vor_fix`.
- Der Waechter ist aus dem **richtigen** Grund rot: `services/coaching_service.py:84`, **eine**
  deduplizierte Stelle, kein Kollateralschaden in anderen Dateien.
- Die Ist-Zaehlung belegt eine intakte Riegel-Ableitung (≥23 Namen, SUMME ≥143).
- Der Neustart wurde geblockt; der alte Stand ist live.
- ROT-Ausgabe verbatim in SUMMARY **und** Commit-Message.
</verification>

<success_criteria>
- `2 failed, 1140 passed, 7 skipped, 5 deselected` (oder begruendete Abweichung der Basis).
- Der ROT-Beleg ist ein Artefakt im Repo, nicht nur eine Terminal-Erinnerung.
- Vier Auslegungsfragen im Fragen-Kanal, committet, zusaetzlich als Fliesstext gezeigt.
- **Kein** Produktiv-Code angefasst.
</success_criteria>

<output>
`.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md`
mit dem verbatim ROT-Beleg.
</output>
