---
phase: 08.23.2.MEHRNUTZER-REST-1
plan: 04
type: execute
wave: 3
depends_on: ["08.23.2.MEHRNUTZER-REST-1-03"]
files_modified:
  - services/coaching_service.py
  - tests/test_no_live_global_state.py   # NUR die Soll-Zahl je Datei (1 -> 3), Task 1 (4)
autonomous: false
requirements: [MEHRNUTZER-REST-1-FIX, MEHRNUTZER-REST-1-GRUEN-BELEG, MEHRNUTZER-REST-1-DOKU]
komplexitaet: mittel

must_haves:
  truths:
    - "Verschiedene conv_id blockieren sich nicht mehr gegenseitig."
    - "Der Duplikatschutz ist exakt gleich stark: die count()-Pruefung liegt weiterhin INNERHALB des Riegels."
    - "Die Riegel-Ablage waechst nicht ueber die Zeit — sie ist so gross wie die Zahl gerade laufender Analysen."
    - "Der Waechter ist nach dem Fix gruen UND sieht den neuen Riegel weiterhin (Datei-Soll im selben Commit von 1 auf 3 nachgezogen)."
    - "Der per-conv_id-Riegel wird vom Waechter GEZAEHLT, aber NICHT als Verstoss gemeldet — sonst waere das gruene Tor unerreichbar."
    - "Der prozessweite _analysis_lock ist geloescht, nicht stehengelassen."
  artifacts:
    - path: "services/coaching_service.py"
      provides: "Riegel pro conv_id mit Nehmer-Zaehler"
      contains: "_analysis_lock_for"
  key_links:
    - from: "services/coaching_service.py generate_postcall_analysis"
      to: "_analysis_lock_for(conv_id)"
      via: "with-Block um den gesamten Rumpf"
      pattern: "with _analysis_lock_for\\(conv_id\\):"
    - from: "_analysis_lock_for"
      to: "_conv_locks / _conv_locks_guard"
      via: "Nehmer-Zaehler hoch vor acquire, runter im finally unter dem Ablage-Riegel"
      pattern: "_conv_locks_guard"
---

<objective>
Der Fix. `services/coaching_service.py:8` (`_analysis_lock = threading.Lock()`) und
`:59` (`with _analysis_lock:`) werden durch einen Riegel **pro `conv_id`** ersetzt.

Der Duplikatschutz bleibt **exakt gleich stark**: die `count()`-Abfrage auf
`LearningCard.call_id` bleibt **innerhalb** des neuen Riegels. Der Eingriff ist klein und
mechanisch: **eine** Zeile im Funktionsrumpf, der Ersatz der Kopfzeile, ein Import, ein
Context-Manager am Dateiende — und **ein Nachzug einer einzigen Soll-Zahl** im Waechter
(`tests/test_no_live_global_state.py`, `services/coaching_service.py: 1 -> 3`, siehe Task 1 (4)).

Purpose: Roadmap-Goal — *„Nach dieser Phase blockieren sich verschiedene Anrufe nicht mehr
gegenseitig, waehrend der Duplikatschutz exakt gleich stark bleibt."* Heute wartet ein zweiter
Nutzer im Worst Case `config.HTTP_LLM_TIMEOUT_LONG_S = 45.0` s (`config.py:134`) und belegt dabei
einen der 64 gthread-Threads (`deploy/nerve.service:35-36`).

Output: gefixter Produktiv-Code, GRUENES Tor, GRUEN-Beleg gegen den ROT-Beleg aus Plan 03.

Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.

**Latenz-Budget (CLAUDE.md Punkt 25):** Diese Phase **senkt** Latenz. Der neue Ablage-Riegel wird
ausschliesslich fuer Dict-Operationen gehalten (µs) — **nie** ueber einem `get_session()`,
`messages.create`, `sleep` oder `emit`. Genau die Bau-Vorschrift, die
`tests/test_session_lock_blocking_calls_guard.py:158-166` fuer `_session_state_lock` durchsetzt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-RESEARCH.md
@.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md

<warum_referenz_gezaehlt>
<!-- Die Roadmap verlangt diese Begruendung AUSDRUECKLICH. Sie gehoert in den Commit und
     ins SUMMARY, nicht nur hierher. -->

Drei Kandidaten gegen das Memory-Leak, das eine Ablage `conv_id -> Lock` ohne Aufraeumen
darstellt:

| Variante | Bewertung |
|---|---|
| **(a) Nehmer-Zaehler, Aufraeumen im `finally`** — **GEWAEHLT** | Obergrenze = Anzahl gleichzeitig laufender Analysen ≤ 64 (Thread-Zahl, `deploy/nerve.service:35-36`). Nach dem letzten Verlassen ist der Eintrag weg. **Kein** Wachstum ueber die Zeit. Kosten: ein zusaetzlicher Ablage-Riegel + zwei kurze kritische Abschnitte, ~15 Zeilen. |
| (b) Deckel-/LRU-Ablage (z.B. max 512) | Kuerzer zu schreiben — aber eine Verdraengung, **waehrend** ein Riegel gehalten wird, ist **exakt** das Duplikatschutz-Loch, das diese Phase verhindern soll. Bei 512 unwahrscheinlich, aber es ist eine **stille Bedingung statt einer Garantie**. **ABGELEHNT.** |
| (c) `WeakValueDictionary` | Braucht trotzdem einen Guard um „holen-oder-anlegen" (sonst Race) — also gleicher Aufwand, aber die Korrektheit haengt zusaetzlich am GC-Zeitpunkt. Schwerer zu beweisen. **ABGELEHNT.** |
| (d) Nie aufraeumen | Genau das Leak, das die Roadmap verbietet. **ABGELEHNT.** |

**Der Entscheidungsgrund in einem Satz:** (a) ist die einzige Variante, bei der der Duplikatschutz
**nicht von einer Groessen-Annahme abhaengt** — er bleibt beweisbar exakt so stark wie heute, und
genau das ist der erklaerte Kern-Anspruch der Phase.
</warum_referenz_gezaehlt>

<die_vier_fallen>
<!-- RESEARCH §2.3. Die Reihenfolge IST der ganze Trick. -->

1. **Zaehler HOCH vor `acquire()`, unter dem Ablage-Riegel.** Wuerde der Zaehler erst **nach** dem
   `acquire()` erhoeht, koennte ein Wartender existieren, dessen Eintrag ein anderer Faden mit
   Zaehler 0 loescht → ein dritter Faden legt einen **neuen** Riegel unter demselben Key an →
   **zwei Faden derselben `conv_id` laufen gleichzeitig** → Duplikatschutz-Loch. Genau die
   Fehlerklasse, die diese Phase nicht erzeugen darf.
2. **Zaehler RUNTER unter dem Ablage-Riegel** (nicht daneben) — `-= 1` und der `== 0`-Test sind
   sonst nicht atomar zueinander. Der GIL garantiert das **nicht** in Verbindung mit dem
   folgenden Vergleich.
3. **`release()` VOR dem Zaehler-Dekrement.** Erwerb: Ablage → frei → conv. Freigabe: conv frei →
   Ablage. So wird der Ablage-Riegel **nie** gehalten, waehrend der conv-Riegel gehalten wird →
   keine Rangfolge-Inversion.
4. **`is eintrag`-Vergleich beim Loeschen.** Streng genommen nicht noetig (ein Eintrag mit
   Zaehler > 0 kann nicht ersetzt werden), aber billig und macht die Invariante sichtbar.

**Zusaetzlich — Falle 3 der Phase (`conv_id` als `int` und als `str`):** **drei** Eingaenge lesen
`data.get('conv_id')` **ohne Cast** — `routes/learning.py:23`, `:211` und `:383`
(nachgezaehlt 2026-08-06; der dritte Leser auf `:211` stuetzt die Entscheidung zusaetzlich).
JSON liefert mal `5`, mal `"5"`. Ohne
`key = str(conv_id)` bekaemen beide **verschiedene** Riegel, obwohl `filter_by(call_id=…)` in
Postgres **denselben** Datensatz meint — ein realer, heute vom globalen Riegel verdeckter
Fehlerkandidat. **Entscheidung: `key = str(conv_id)`.** Kein Sonderfall fuer `None`/falsy:
`None` wird zu `'None'`, alle None-Aufrufe teilen sich einen Riegel. Das ist die **fail-safe**
Richtung — mehr Serialisierung, nie weniger Schutz. Ein `if not conv_id: <ohne Riegel weiter>`
waere eine echte Schwaechung und ist abzulehnen.
</die_vier_fallen>

<alle_sieben_ausgaenge>
<!-- RESEARCH §1.2. Ein finally deckt sie RESTLOS ab; eine Loesung, die nur die return-Pfade
     behandelt, waere an E6/E7 falsch. -->

| # | Zeile (heute) | Art |
|---|---|---|
| E1 | `:68` | `return []` Duplikat-Pfad — **die kritischste**, haeufigster Nicht-Fehler-Ausgang |
| E2 | `:111` | `return []` kein JSON in der Antwort |
| E3 | `:134` | `return []` DB-Persist fehlgeschlagen (inneres `try :117`) |
| E4 | `:138` | `return vorschlaege` Erfolgspfad |
| E5 | `:141` | `return []` `except Exception` des aeusseren `try :83` |
| E6 | `:61-70` | **durchschlagende Ausnahme** — `get_session()` auf `:63` liegt **vor** dem `try :64` (Pool-Timeout, `database/db.py:31 pool_timeout=10`) |
| E7 | `:72-82` | **durchschlagende Ausnahme** — `POSTCALL_PROMPT.format(...)` steht **ausserhalb** des `try :83` |
</alle_sieben_ausgaenge>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Der Fix — Riegel-Ablage mit Nehmer-Zaehler + eine Zeile im Rumpf</name>

  <read_first>
    - `services/coaching_service.py:1-10` (Kopf: `import threading` auf `:3`,
      `from datetime import …` auf `:4`, `import config` auf `:5`,
      `from services.claude_service import claude_client, http_llm_client` auf `:6`,
      `_analysis_lock = threading.Lock()` auf `:8`).
    - `services/coaching_service.py:49-141` — der gesamte Rumpf. Nur **eine** Zeile darin
      (`:59`) wird geaendert.
    - `services/coaching_service.py` Dateiende — nach `get_longterm_data`; dorthin kommt der
      Context-Manager.
    - `tests/test_no_live_global_state.py`, Abschnitt „Pruefpunkt 6" (aus Plan 02) —
      insbesondere `_RIEGEL_KONSTRUKTOR_ENDUNG`, `_RIEGEL_FABRIK_ENDUNGEN`,
      `_ist_per_schluessel_riegel_ausdruck` und `_RIEGEL_SOLL_JE_DATEI`. Der Fix muss zu dieser
      Ableitung passen, sonst faellt er aus der eigenen Bewachung.
    - `tests/test_stabil1_http_llm_timeout.py:57-79` — dieser Bestands-Test ruft die **echte**
      Funktion und **muss** nach dem Umbau gruen bleiben (Punkt 18, beste Fruehwarnung).
  </read_first>

  <files>services/coaching_service.py</files>

  <action>
**Vier Aenderungen, sonst nichts** (Bau-Regel 17 / Reparatur-Modus) — drei in
`services/coaching_service.py`, eine einzelne Zahl in `tests/test_no_live_global_state.py`.

**(1) Kopf** — `from contextlib import contextmanager` ergaenzen (nach `from datetime import …`,
`:4`) und `:8` ersetzen:

```python
# ALT (Zeile 8):
_analysis_lock = threading.Lock()

# NEU (zwei Zeilen, ZWEI GETRENNTE ZUWEISUNGEN — nicht als Tupel!):
_conv_locks = {}                       # conv_id(str) -> [threading.Lock, Nehmer-Zaehler]
_conv_locks_guard = threading.Lock()   # schuetzt NUR die Ablage; NIE ueber einem Netz-Aufruf
```

⚠ **`_conv_locks_guard` MUSS eine eigene, einfache Zuweisung sein.** Ein Einzeiler
`_conv_locks, _conv_locks_guard = {}, threading.Lock()` waere eine Tupel-Zuweisung — die
Riegel-Ableitung des Waechters (`tests/test_no_live_global_state.py`,
`_riegel_namen_einer_datei`) sieht dann **keinen** Riegel-Namen mehr. Die Namensmenge fiele von
23 auf 22 und `test_riegel_sweep_erreicht_alle_bekannten_bloecke` wuerde rot. (Mit der obigen
Form bleibt sie bei 23: `_analysis_lock` faellt weg, `_conv_locks_guard` kommt hinzu.)

Die Zeilendrift betraegt damit **+2** — der Schild-Kommentar auf `database/models.py:628` zitiert
`coaching_service.py:65`, das wird `:67`. **Nicht** nachziehen (siehe Task 3, Folgefund).

**(2) Der Rumpf** — genau **eine** Zeile:

```python
# ALT (Zeile 59):
    with _analysis_lock:

# NEU:
    with _analysis_lock_for(conv_id):
```

Der gesamte uebrige Rumpf (`:60-141`) bleibt **buchstabengleich**: keine Neu-Einrueckung, keine
Umbenennung, keine Import-Sortierung, keine Nebenverbesserung. Insbesondere bleibt die
Duplikat-Pruefung (`count()` auf `LearningCard.call_id`, heute `:65`) **innerhalb** des Riegels —
sie ist die **einzige** Duplikat-Sperre im Prozess (`learning_cards` hat keinen
Unique-Constraint auf `call_id`, `database/models.py:629-631`, und kann auch keinen bekommen:
bis zu 3 Karten pro Call by design, `:113`).

**(3) Der Context-Manager ans Dateiende** (nach `get_longterm_data`). Python loest den Namen erst
zur Aufrufzeit auf — die Definitionsreihenfolge im Modul ist irrelevant, und so bleiben die Zeilen
49-141 bis auf `:59` unveraendert:

```python
# ── Riegel PRO conv_id (Phase 08.23.2.MEHRNUTZER-REST-1) ─────────────────────
# Ersetzt den frueher prozessweiten _analysis_lock. Der hielt waehrend des ganzen
# Sonnet-Aufrufs (config.HTTP_LLM_TIMEOUT_LONG_S = 45 s, config.py:134) ALLE Nutzer
# auf; ein zweiter wartete bis 45 s, ein dritter bis 90 s, und jeder Wartende belegte
# einen der 64 gthread-Threads (deploy/nerve.service:35-36).
#
# WARUM NEHMER-ZAEHLER statt Deckel-/LRU-Ablage (Roadmap verlangt die Begruendung):
# Eine Verdraengung, WAEHREND ein Riegel gehalten wird, ist exakt das
# Duplikatschutz-Loch, das diese Phase verhindern soll. Der Zaehler ist die einzige
# Variante, bei der der Schutz nicht von einer Groessen-Annahme abhaengt. Die Ablage
# bleibt so gross wie die Zahl der GERADE LAUFENDEN Analysen (<= 64) und ist danach
# leer — kein Wachstum ueber die Zeit.
#
# _conv_locks_guard wird AUSSCHLIESSLICH fuer Dict-Operationen gehalten: kein
# get_session, kein messages.create, kein sleep, kein emit. (Dieselbe Bau-Vorschrift,
# die tests/test_session_lock_blocking_calls_guard.py:158-166 fuer _session_state_lock
# durchsetzt.)
@contextmanager
def _analysis_lock_for(conv_id):
    """Riegel pro conv_id. Duplikatschutz identisch stark, verschiedene Anrufe
    blockieren sich nicht mehr.

    key = str(conv_id): routes/learning.py:23, :211 und :383 lesen conv_id OHNE Cast aus
    dem JSON — 5 und "5" bekaemen sonst VERSCHIEDENE Riegel, obwohl
    filter_by(call_id=...) in Postgres denselben Datensatz meint. Kein Sonderfall fuer
    None/falsy: None wird zu 'None' und teilt sich einen Riegel. Fail-safe Richtung —
    mehr Serialisierung, nie weniger Schutz.
    """
    key = str(conv_id)
    with _conv_locks_guard:                   # (1) holen/anlegen + Zaehler HOCH
        eintrag = _conv_locks.get(key)        #     HOCH vor acquire(): sonst koennte
        if eintrag is None:                   #     ein Wartender wegge-raeumt werden
            eintrag = [threading.Lock(), 0]   #     und ein Dritter legte einen ZWEITEN
            _conv_locks[key] = eintrag        #     Riegel unter demselben Key an
        eintrag[1] += 1
    riegel = eintrag[0]
    riegel.acquire()                          # (2) warten AUSSERHALB des Ablage-Riegels
    try:
        yield
    finally:
        riegel.release()                      # (3) erst freigeben ...
        with _conv_locks_guard:               # (4) ... dann Zaehler RUNTER, unter der
            eintrag[1] -= 1                   #     Ablage: '-= 1' und '== 0' sind sonst
            if eintrag[1] == 0 and _conv_locks.get(key) is eintrag:
                del _conv_locks[key]          #     nicht atomar zueinander
```

Das `finally` deckt **alle sieben** Ausgaenge aus dem Riegel-Block ab (E1-E7, siehe
`<alle_sieben_ausgaenge>`) — einschliesslich der beiden durchschlagenden Ausnahme-Regionen
(`get_session()` auf `:63` **vor** dem `try :64`, und `POSTCALL_PROMPT.format(...)` auf `:72-82`
**ausserhalb** des `try :83`). Eine Loesung, die nur die `return`-Pfade behandelt, waere dort
falsch.

**(4) Eine einzige Zahl im Waechter nachziehen** — `tests/test_no_live_global_state.py`:

```python
# ALT:
    'services/coaching_service.py': 1,

# NEU:
    'services/coaching_service.py': 3,   # 1x _analysis_lock_for + 2x _conv_locks_guard
                                         # (Phase MEHRNUTZER-REST-1: Soll HOCHgezogen, nicht
                                         #  gesenkt — bei 1 wuerden die beiden Ablage-Riegel
                                         #  allein den Eintrag erfuellen und er koennte den
                                         #  Ausfall von Variante A nicht mehr melden.)
```

⚠ **Das ist ein HOCHziehen, kein Senken.** Punkt 31 verbietet ausdruecklich nur das *stille
Senken*; ein Nachzug nach oben **mit Begruendung** ist genau der vorgeschriebene Weg. Der Fix
macht aus **einem** Riegel-Block **drei** (1x `with _analysis_lock_for(conv_id):` + 2x
`with _conv_locks_guard:` im Context-Manager — AST-nachsimuliert am gepatchten Baum:
`services/coaching_service.py: 3 (with=3, try/finally=0)`, `SUMME: 145`). Bliebe der Eintrag
bei 1, waere er ab sofort **wertlos**: schon die zwei Ablage-Riegel allein erfuellen ihn, und
ein Ausfall von Variante A (der neue conv_id-Riegel faellt aus der Bewachung) wuerde **nicht
mehr gemeldet**. Genau die Punkt-31-Fehlerklasse „gruen und trotzdem blind".

⚠ **Nicht schon in Plan 02 vorziehen:** vor dem Fix hat die Datei genau **einen** Block.
Ein Soll von 3 in Welle 1 wuerde den ROT-Lauf (Plan 03) mit **drei** statt zwei Fehlschlaegen
roeten und den Beleg unbrauchbar machen. Die Zahl gehoert deshalb in **diesen** Commit.

Sonst **nichts** an dieser Datei: keine weitere Soll-Zahl, kein Katalog-Text, keine Whitelist,
kein Test.

**Was NICHT angefasst wird** (Bau-Regel 17, Roadmap-Verbotsliste):
- `services/slow_lane.py`, `app.py:2434` (Fund 2) — bleibt offen.
- `services/anonymization.py` (Fund 3) — bleibt offen.
- Der tote HTTP-Eingang `/api/postcall_analysis` (`routes/learning.py:18`) — bleibt stehen.
- Kein DB-seitiger Riegel / advisory lock. Kein Prompt, kein Schema, keine Alembic-Revision.
- Keine sonstige Verbesserung an `coaching_service.py`.
  </action>

  <verify>
    <automated>python -c "import ast; s=open('services/coaching_service.py',encoding='utf-8').read(); ast.parse(s); assert '_analysis_lock =' not in s and 'with _analysis_lock_for(conv_id):' in s and 'from contextlib import contextmanager' in s; t=open('tests/test_no_live_global_state.py',encoding='utf-8').read(); assert \"'services/coaching_service.py': 3\" in t"</automated>
    <!-- Scharfe Abnahme: Task 2, das Tor auf dem Server. -->
  </verify>

  <acceptance_criteria>
    - `grep -cF "_analysis_lock = threading.Lock()" services/coaching_service.py` → 0
      (das alte Modul-Global ist **geloescht**, nicht stehengelassen)
    - `grep -cF "with _analysis_lock:" services/coaching_service.py` → 0
    - `grep -cF "with _analysis_lock_for(conv_id):" services/coaching_service.py` → 1
    - `grep -c "^_conv_locks = " services/coaching_service.py` → 1
    - `grep -c "^_conv_locks_guard = threading" services/coaching_service.py` → 1
      (**eigene** Zeile, **keine** Tupel-Zuweisung — sonst faellt die Riegel-Ableitung von 23 auf 22)
    - `grep -cF "from contextlib import contextmanager" services/coaching_service.py` → 1
    - `grep -cF "key = str(conv_id)" services/coaching_service.py` → 1
    - `grep -cF "riegel.release()" services/coaching_service.py` → 1
    - Soll-Nachzug im Waechter (Task 1 (4)):
      `grep -cF "'services/coaching_service.py': 3," tests/test_no_live_global_state.py` → 1
      und `grep -cF "'services/coaching_service.py': 1," tests/test_no_live_global_state.py` → 0
    - **Nur diese eine Zahl** im Waechter geaendert:
      `git diff --numstat tests/test_no_live_global_state.py` zeigt **1** entfernte und
      **≤ 6** hinzugefuegte Zeilen (die Wertzeile plus die vierzeilige Punkt-31-Begruendung
      des Hochziehens). ⚠ Die Begruendungs-Kommentare sind **pflichtig** — sie zu loeschen,
      um eine kleinere Zahl zu treffen, ist genau der Abrieb, den diese Phase verhindert.
    - Reihenfolge belegen (Falle 3): `riegel.release()` steht **vor** dem zweiten
      `with _conv_locks_guard:` —
      `python -c "s=open('services/coaching_service.py',encoding='utf-8').read(); assert s.index('riegel.release()') < s.rindex('with _conv_locks_guard:')"`
    - Duplikatschutz weiterhin **innerhalb** des Riegels:
      `python -c "s=open('services/coaching_service.py',encoding='utf-8').read(); assert s.index('with _analysis_lock_for(conv_id):') < s.index('filter_by(call_id=conv_id).count()')"`
    - Diff-Groesse (Bau-Regel 17): `git diff --numstat services/coaching_service.py` zeigt
      **≤ 55** hinzugefuegte und **≤ 4** entfernte Zeilen. Mehr → begruenden.
      (Der in diesem Task vorgeschriebene Code ergibt gemessen 51 hinzugefuegte / 2 entfernte
      Zeilen — der grosse Teil ist der pflichtige `WARUM NEHMER-ZAEHLER`-Kommentarblock, den
      die Roadmap ausdruecklich verlangt. Nicht wegkuerzen, um eine Zahl zu treffen.)
    - Kein anderer Produktiv-Code im Diff:
      `git diff --name-only | grep -c "^services/\|^routes/\|^app.py"` → 1
      (`tests/` ist kein Produktiv-Code und faellt bewusst nicht unter diesen Filter — die
      einzige erlaubte Test-Aenderung ist der Soll-Nachzug aus (4))
    - Keine Migration angelegt: `git status --porcelain alembic/` → leer
  </acceptance_criteria>

  <done>
    `_analysis_lock` existiert nicht mehr; `generate_postcall_analysis` nimmt einen Riegel pro
    `conv_id`; die `count()`-Pruefung liegt weiterhin darin; der Ablage-Riegel wird nur fuer
    Dict-Operationen gehalten; das Datei-Soll im Waechter steht auf 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ausrollen — GRUEN erwartet, Nehmer-Zahl als Beleg</name>

  <read_first>
    - `.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md`
      — die verbatim ROT-Ausgabe, gegen die der GRUEN-Lauf gestellt wird.
    - `deploy.sh` Zeilen ~200-240 — pytest-Abschnitt + POST-SUITE-Baseline-Check.
    - `CLAUDE.md` „HART: Kein Local-Dev" — lokales `pytest` ist **kein** Abnahme-Signal.
  </read_first>

  <files>(keine — Commit + Deploy)</files>

  <action>
Committen mit der Begruendung im Text (Roadmap verlangt die schriftliche Begruendung der
Ablage-Wahl), pushen, ausrollen:

```bash
git add services/coaching_service.py tests/test_no_live_global_state.py
git commit -m "fix(08.23.2.MEHRNUTZER-REST-1): Riegel pro conv_id statt prozessweitem _analysis_lock

Der Riegel hielt den gesamten Rumpf von generate_postcall_analysis inkl. des
Sonnet-Aufrufs (config.HTTP_LLM_TIMEOUT_LONG_S = 45 s). N gleichzeitige Anruf-Enden
liefen hintereinander, jeder Wartende belegte einen der 64 gthread-Threads.

Ablage-Lebensdauer: Nehmer-Zaehler mit Aufraeumen im finally. Begruendung (von der
Roadmap ausdruecklich verlangt): eine Deckel-/LRU-Ablage koennte den Riegel einer NOCH
LAUFENDEN conv_id verdraengen und genau den Duplikatschutz aushebeln, den diese Phase
erhalten soll. Der Zaehler ist die einzige Variante, bei der der Schutz nicht von einer
Groessen-Annahme abhaengt. Ablage <= Zahl gleichzeitig laufender Analysen (<= 64),
danach leer.

Duplikatschutz unveraendert: die count()-Pruefung auf LearningCard.call_id liegt
weiterhin INNERHALB des Riegels. learning_cards hat keinen Unique-Constraint auf
call_id (database/models.py:629-631) und kann auch keinen bekommen (bis zu 3 Karten
pro Call by design).

key = str(conv_id): routes/learning.py:23, :211 und :383 lesen conv_id ohne Cast —
5 und '5' bekaemen sonst verschiedene Riegel bei demselben Datensatz.

Waechter-Soll services/coaching_service.py 1 -> 3 nachgezogen (HOCH, nicht gesenkt):
der Fix macht aus einem Riegel-Block drei (1x _analysis_lock_for + 2x _conv_locks_guard).
Bei 1 wuerden die zwei Ablage-Riegel den Eintrag allein erfuellen und ein Ausfall von
Variante A bliebe unbemerkt — Punkt 31, gruen und trotzdem blind.

ROT-Beleg des Vorlaufs siehe 08.23.2.MEHRNUTZER-REST-1-03-SUMMARY.md."
git push origin main
bash deploy.sh production
```

**Die vollstaendige Tor-Ausgabe verbatim sichern**, mindestens:
- die Zusammenfassungszeile
- die Zeilen `[MEHRNUTZER-REST-1 Riegel-Waechter] Ist-Zaehlung` (Riegel-Namen + SUMME +
  `services/coaching_service.py: 3`)

**Vier Beobachtungen und was sie bedeuten:**

| Beobachtung | Bedeutung | Was tun |
|---|---|---|
| `1142 passed`, 0 failed | Soll-Zustand. | Weiter zu Task 3. |
| `test_riegel_sweep_erreicht_alle_bekannten_bloecke` rot mit `services/coaching_service.py: 2 (with=2)` statt 3 | **Falle 1: der Waechter ist blind statt gruen.** `with _analysis_lock_for(conv_id):` wird nicht als Riegel-Block erkannt — gezaehlt werden nur die beiden `_conv_locks_guard`-Bloecke. **Genau dafuer wurde das Soll auf 3 gezogen**; bei 1 waere dieser Ausfall unsichtbar geblieben. | Variante A pruefen (`_RIEGEL_FABRIK_ENDUNGEN` / `_ist_per_schluessel_riegel_ausdruck` in `tests/test_no_live_global_state.py`). **Nicht** das Datei-Soll senken — das verbietet `tests/test_session_lock_blocking_calls_guard.py:200-207`. |
| `test_kein_modul_globaler_riegel_um_netz_aufruf` **weiterhin rot** mit `services/coaching_service.py:8x` | Die **Melde-Seite** trennt nicht zwischen gemeinsamem und per-Schluessel-Riegel: der Fabrik-Block wird faelschlich als Verstoss gemeldet, obwohl er die Loesung ist. | `_ist_gemeinsamer_riegel_ausdruck` / `_ist_per_schluessel_riegel_ausdruck` in `tests/test_no_live_global_state.py` pruefen (Plan 02, Task 1/2). **Nicht** `_FALSCH_TREFFER_RIEGEL` fuellen und **nicht** den Netz-Aufruf aus dem Riegel ziehen — beides ist ausdruecklich verboten. |
| Riegel-Namen-Anzahl `22` statt 23 | `_conv_locks_guard` wurde als Tupel zugewiesen. | Auf zwei getrennte Zuweisungen umstellen (Task 1). |
| `test_gleiche_conv_id_erzeugt_nur_einen_satz_karten` rot (6 Karten / 2 Aufrufe) | **Der Duplikatschutz ist gebrochen** — vermutlich Falle 2 (Dekrement ausserhalb des Ablage-Riegels) oder Falle 3 (`str()` fehlt). | STOP, nicht deployen. Reihenfolge (1)-(4) aus Task 1 woertlich gegenpruefen. |
| `tests/test_stabil1_http_llm_timeout.py::test_lang_stufe_ohne_retry` rot | Der Umbau hat Signatur/Ablauf verbogen (Punkt 18, beste Fruehwarnung). | Rumpf gegen `git diff` pruefen — er muss ausser `:59` buchstabengleich sein. |
  </action>

  <verify>
    <automated>bash deploy.sh production</automated>
  </verify>

  <acceptance_criteria>
    - Die Tor-Ausgabe enthaelt `0 failed` bzw. keine `failed`-Angabe und **1142 passed**
      (= 1133 Basis + 9 neu). Abweichung → mit gewanderter Basis begruenden.
    - Die Tor-Ausgabe enthaelt `7 skipped, 5 deselected`
    - Die Ist-Zaehlung nennt `services/coaching_service.py: 3` (with=3, try/finally=0) —
      1x `with _analysis_lock_for(conv_id):` + 2x `with _conv_locks_guard:`; der neue Riegel ist
      damit **weiterhin bewacht**. (AST-nachsimuliert am gepatchten Baum; die Gesamt-`SUMME`
      steigt entsprechend von 143 auf 145.)
    - Die Ist-Zaehlung nennt `abgeleitete Riegel-Namen: 23` (oder mehr) und enthaelt
      `_conv_locks_guard`
    - **Keine** `[BASELINE-AUTO-FIX]`-Warnung in der Ausgabe
    - Der POST-SUITE-Check ist gruen (jede `crm.*`-Tabelle 0 Rows,
      `training.transcript_archive` 0)
    - Dienst neu gestartet:
      `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh service-status'`
      zeigt `active (running)`
  </acceptance_criteria>

  <done>
    Tor gruen mit belegter Nehmer-Zahl; der Waechter sieht den neuen Riegel weiterhin; der Dienst
    laeuft mit dem neuen Stand.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Abschluss-Doku — ROT/GRUEN gegenuebergestellt, Pruning-Notiz, Folgefunde</name>

  <read_first>
    - der ROT-Beleg aus `…-03-SUMMARY.md` und die GRUEN-Ausgabe aus Task 2.
    - `CLAUDE.md` „Acht weitere Vault-Regeln" Nr. 7 (Pruning-Notiz ist **Pflichtteil** jeder
      Phasen-SUMMARY: pro obsolet gewordener Stelle **geloescht** ODER *bleibt als Foundation
      fuer Phase Y, weil …*).
    - `database/models.py:628` — der `learning_cards`-Schild-Kommentar mit den Zeilennummern.
    - `CLAUDE.md` „Vier Vault-Regeln" B (Roadmap-Sync) und Nr. 4 (Fragen-Kanal).
  </read_first>

  <what-built>
    Der Riegel laeuft pro `conv_id`, das Tor ist gruen, der neue Riegel ist weiterhin bewacht.
  </what-built>

  <action>
Das Phasen-SUMMARY (`…-04-SUMMARY.md`) mit diesen Pflichtteilen schreiben:

1. **ROT/GRUEN gegenuebergestellt**, in der Hausform (Vorbild
   `.planning/phases/08.23.2.SOFORT-2-*/08.23.2.SOFORT-2-08-SUMMARY.md:78/:110`):
   ```
   ROT:   2 failed, 1140 passed, 7 skipped, 5 deselected in …s
   GRUEN: 1142 passed, 7 skipped, 5 deselected in …s
   ```
   Dazu **verbatim** die Waechter-Meldung des ROT-Laufs
   (`services/coaching_service.py:84  [http_llm_client, messages.create]`).

2. **Ehrlichkeits-Absatz (RESEARCH §3.5 / Falle 6):** ausdruecklich festhalten, dass
   `test_gleiche_conv_id_erzeugt_nur_einen_satz_karten` **kein** ROT-Beleg ist — er war vor
   **und** nach dem Fix gruen. Sein Beweiswert liegt im Falsifizierbarkeits-Test
   `test_riegellose_variante_erzeugt_doppelte_karten` (riegellos → 6 Karten). Ihn als ROT-Beleg
   zu fuehren, waere die Behauptung eines Beweises, den es nicht gibt.

3. **Pruning-Notiz (Pflicht):**
   - `_analysis_lock` (`services/coaching_service.py:8`) — **geloescht**. 0 externe Leser
     (`grep -rn "_analysis_lock"` → nur Definition und die eine Nutzung).
   - Waechter-Soll `services/coaching_service.py` — **von 1 auf 3 hochgezogen**, mit
     Begruendung (nicht gesenkt, nicht entfernt). Punkt-31-konform dokumentiert.
   - Der tote HTTP-Eingang `/api/postcall_analysis` (`routes/learning.py:18`) — **bleibt
     bewusst stehen**, ausserhalb des Scopes dieser Phase, notiert fuer die naechste
     Tote-Code-Inventur (Bau-Regel 3c/3d).

4. **Waechter-Restluecken ins SUMMARY** (CLAUDE.md Punkt 31 verlangt sie **auch** dort, nicht
   nur im Docstring): die **fuenfzehn** Punkte aus `_RIEGEL_PRUEFKATALOG` in Kurzform,
   insbesondere „ein Helfer eine Ebene tiefer ist unsichtbar" (Punkt 1), „eine Fabrik, die ihr
   Argument ignoriert und doch einen gemeinsamen Riegel liefert, rutscht durch" (Punkt 8, die
   **neue** Luecke dieser Phase) und „fuer den neuen conv_id-Riegel gibt es **keine**
   Laufzeit-Schicht" (Punkt 11).

5. **Folgefunde** (nicht bauen, notieren):
   - **`learning_cards`-Schild stale** (`database/models.py:628`): `:170` war **schon vor**
     dieser Phase falsch (der zweite Leser steht auf `:200`), und `:65` ist durch die
     +2-Zeilendrift dieser Phase zu `:67` geworden. Bewusst **nicht** nachgezogen: das waere
     eine `COMMENT ON TABLE`-Alembic-Revision, und die Roadmap erwartet fuer diese Phase
     ausdruecklich **keine** Migration. Punkt 23 loest bei *„neuer Schreiber, geaenderter
     Mechanismus, neue Konsum-Stelle"* aus — nichts davon trifft zu. **Auslegung, Konfidenz
     MEDIUM** → gehoert in den Fragen-Kanal.
   - **Fund (2)** `services/slow_lane.py:145` + `app.py:2434` — bleibt offen.
   - **Fund (3)** `services/anonymization.py:19` + `:524-534` — bleibt offen.
   - Multi-Worker: ein Prozess-Riegel traegt dort **nicht** mehr; gehoert zu Fund (2).

6. **Roadmap-Sync (CLAUDE.md „Vier Vault-Regeln" B):** `.planning/ROADMAP.md` auf abgeschlossen
   setzen **und** einen Hinweis
   `## ROADMAP-SYNC — 08.23.2.MEHRNUTZER-REST-1 — <Datum>` ans Ende von
   `.planning/DIALOG-GSD-CLAUDIAN.md` schreiben, damit Claudian `Nerve-Vault/01 Roadmap.md`
   nachzieht.

7. Alles committen und pushen (`git push origin main` — Git-Regel „Immer pushen").
  </action>

  <how-to-verify>
    1. `…-04-SUMMARY.md` oeffnen: stehen ROT und GRUEN **verbatim** gegenuebergestellt?
    2. Steht der Ehrlichkeits-Absatz dort („(b) ist kein ROT-Beleg")?
    3. Steht die Pruning-Notiz mit **beiden** Eintraegen dort?
    4. `tail -30 .planning/DIALOG-GSD-CLAUDIAN.md` — steht dort der ROADMAP-SYNC-Hinweis und die
       Schild-Frage?
    5. **Fachliche Gegenprobe (optional, kein Gate):** einen Anruf beenden und pruefen, dass
       Lernkarten weiterhin genau einmal entstehen. Der einzige lebende Aufrufer ist
       fire-and-forget nach Call-Ende (`static/pip-launcher.js:3174`) — die Phase beruehrt
       **keinen** Live-Pfad, ein Test-Anruf ist deshalb kein Pflicht-Gate.
  </how-to-verify>

  <acceptance_criteria>
    - `grep -c "ROT:" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-04-SUMMARY.md` → mindestens 1
    - `grep -c "GRUEN:" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-04-SUMMARY.md` → mindestens 1
    - `grep -cF "kein ROT-Beleg" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-04-SUMMARY.md` → mindestens 1
    - `grep -c "Pruning-Notiz" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-04-SUMMARY.md` → 1
    - `grep -cF "RESTLUECKEN" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-04-SUMMARY.md` → mindestens 1
    - `grep -cF "database/models.py:628" .planning/phases/08.23.2.MEHRNUTZER-REST-1-*/08.23.2.MEHRNUTZER-REST-1-04-SUMMARY.md` → mindestens 1
    - `grep -c "ROADMAP-SYNC — 08.23.2.MEHRNUTZER-REST-1" .planning/DIALOG-GSD-CLAUDIAN.md` → 1
    - `database/models.py` **nicht** angefasst: `git diff HEAD~5 --name-only | grep -c "^database/"` → 0
    - `git status --porcelain` → leer; `git log origin/main -1 --format=%H` == `git log -1 --format=%H`
  </acceptance_criteria>

  <resume-signal>
    „abgenommen" tippen — Phase ist fertig. Oder Abweichungen beschreiben.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/api/postcall_cards` | untrusted `conv_id` (`routes/learning.py:23`, `:211`, `:383` — alle drei ohne Typ-Cast) |
| Nutzer A ↔ Nutzer B (ein gunicorn-Worker) | 64 gthread-Threads teilen `_conv_locks` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MEHRNUTZER-09 | Denial of Service | `_conv_locks` (unbegrenztes Wachstum) | mitigate | Nehmer-Zaehler + `del` im `finally` bei 0. Obergrenze = gleichzeitig laufende Analysen ≤ 64. |
| T-MEHRNUTZER-10 | Tampering | Duplikatschutz-Loch durch Race in der Ablage | mitigate | Zaehler HOCH **vor** `acquire()` unter dem Ablage-Riegel; Dekrement + `== 0`-Test **unter** dem Ablage-Riegel; `is eintrag`-Vergleich beim Loeschen. |
| T-MEHRNUTZER-11 | Tampering | `conv_id` als `int` vs. `str` | mitigate | `key = str(conv_id)` — sonst zwei Riegel fuer denselben Datensatz. |
| T-MEHRNUTZER-12 | Denial of Service | Rangfolge-Inversion zwischen Ablage- und conv-Riegel | mitigate | Erwerb Ablage→frei→conv, Freigabe conv→Ablage; der Ablage-Riegel wird nie gehalten, waehrend der conv-Riegel gehalten wird. |
| T-MEHRNUTZER-13 | Elevation of Privilege | Riegel traegt bei Multi-Worker nicht | accept | Ausdruecklich ausserhalb des Scopes (Roadmap: gehoert zu Fund (2) / Anforderung 4c). Heute laeuft **ein** Worker (`deploy/nerve.service:35-36`). Im SUMMARY als offene Kante benannt. |
| T-MEHRNUTZER-14 | Repudiation | Waechter nach dem Fix blind | mitigate | Datei-Soll `services/coaching_service.py` auf **3** hochgezogen (Task 1 (4)) + `test_riegel_erkennung_erfasst_context_manager_aufruf` (Plan 02, alle vier Richtungen). |
| T-MEHRNUTZER-15 | Repudiation | Waechter nach dem Fix dauerhaft ROT (gruenes Tor unerreichbar) | mitigate | Trennung Zaehl-/Melde-Seite: per-Schluessel-Riegel zaehlt, meldet nicht (Plan 02, `_ist_per_schluessel_riegel_ausdruck`). Die dadurch geoeffnete Restluecke steht als Punkt 8 im `_RIEGEL_PRUEFKATALOG`. |
</threat_model>

<verification>
- `_analysis_lock` existiert nirgends mehr (`grep -rn "_analysis_lock\b" services/ routes/ app.py` → 0,
  `_analysis_lock_for` ausgenommen).
- Die `count()`-Pruefung liegt weiterhin **innerhalb** des Riegels.
- Diff nur in `services/coaching_service.py`; keine Migration, kein Schema, kein Prompt.
- Tor gruen mit **1142 passed** und `services/coaching_service.py: 3` in der Ist-Zaehlung
  (SUMME 145). Die ROT-Zahlen aus Plan 03 (`2 failed, 1140 passed`) bleiben davon unberuehrt —
  vor dem Fix hat die Datei genau **einen** Riegel-Block und der Sweep meldet genau **eine**
  deduplizierte Stelle.
- ROT- und GRUEN-Beleg im SUMMARY gegenuebergestellt.
</verification>

<success_criteria>
- Verschiedene `conv_id` blockieren sich nicht mehr (Test (a) gruen, war rot).
- Duplikatschutz exakt gleich stark (Test (b) gruen, Falsifizierbarkeit ueber Test (c) belegt).
- Riegel-Ablage waechst nicht ueber die Zeit — Begruendung schriftlich im Commit und SUMMARY.
- Der Waechter ist gruen **und** sieht den neuen Riegel (Datei-Soll im selben Commit auf 3
  hochgezogen, mit Begruendung in Commit und SUMMARY).
- Pruning-Notiz, Restluecken und drei Folgefunde dokumentiert; Roadmap-Sync angestossen.
</success_criteria>

<output>
`.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-04-SUMMARY.md`
</output>
