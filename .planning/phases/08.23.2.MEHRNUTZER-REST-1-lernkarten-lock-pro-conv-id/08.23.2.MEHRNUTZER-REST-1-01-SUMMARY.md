---
phase: 08.23.2.MEHRNUTZER-REST-1
plan: 01
subsystem: tests
tags: [regression-net, threading, lock, coaching_service, rot-vor-gruen]
requires: []
provides:
  - "tests/test_lernkarten_lock_pro_conv.py — Regressionstest-Netz fuer den Riegel pro conv_id"
  - "ROT-Beleg-Kandidat fuer den Tor-Lauf in Plan 03"
affects:
  - "services/coaching_service.py (NUR als Testobjekt — nicht veraendert; Fix kommt in Plan 04)"
tech-stack:
  added: []
  patterns:
    - "Event-Rendezvous statt Wanduhr-Messung (keine perf-Marker, kein time.time())"
    - "Fake-DB mit echtem Zustand (fadeneigenes add(), commit() uebernimmt in geteilte Liste)"
    - "patch.object(claude_client, 'with_options') statt setattr — reihenfolge-robust"
    - "synthetischer riegelloser Quelltext IM TESTMODUL als Anti-Stub-Beleg"
key-files:
  created:
    - "tests/test_lernkarten_lock_pro_conv.py (300 Zeilen, 3 Tests)"
  modified: []
decisions:
  - "_GEGENPOL_S = 0.5 s statt 2.0 s — die Groesse ist durch Test (c) empirisch belegt, nicht geraten; spart 1,5 s pro Deploy-Tor"
  - "Test (b) traegt KEINEN rot_vor_fix-Marker und ist im Docstring ausdruecklich als NICHT-ROT-Beleg gekennzeichnet (Ehrlichkeit, RESEARCH Falle 6)"
  - "Kein perf-Marker — der wuerde den Test aus dem Deploy-Tor (-m 'not live and not perf') werfen und die Ratsche entwerten"
metrics:
  duration: "~25 min"
  completed: "2026-08-06"
  tasks: 3
  commits: 3
  files: 1
---

# Phase 08.23.2.MEHRNUTZER-REST-1 Plan 01: Lernkarten-Riegel pro conv_id — Regressionsnetz Summary

Drei Tests in einer neuen Datei, die den prozessweiten Riegel um den 45-Sekunden-Sonnet-Aufruf
als Mehrnutzer-Blockade festnageln — ohne den Fix und ohne eine Zeile Produktiv-Code.

## Was gebaut wurde

`tests/test_lernkarten_lock_pro_conv.py` (neu, 300 Zeilen):

| Baustein | Test | Rolle |
|---|---|---|
| (a) | `test_verschiedene_conv_ids_blockieren_sich_nicht` | **ROT-BELEG**, Marker `rot_vor_fix`. Zwei verschiedene `conv_id` (4711/4712) muessen gleichzeitig im kritischen Abschnitt sein koennen. Gegen `coaching_service.py:59` unmoeglich. |
| (b) | `test_gleiche_conv_id_erzeugt_nur_einen_satz_karten` | **GEGENPOL, KEIN ROT-Beleg.** Zwei parallele Requests derselben `conv_id` → 3 Karten, 1 Sonnet-Aufruf. Schuetzt gegen den falschen Fix „Riegel ersatzlos raus". |
| (c) | `test_riegellose_variante_erzeugt_doppelte_karten` | **FALSIFIZIERBARKEIT.** Dieselbe Apparatur gegen eine riegellose Mini-Funktion im Testmodul → 6 Karten. Beweist, dass (b) Duplikate ueberhaupt sehen kann. |

Apparatur: `_FakeDB`/`_FakeQuery` mit echtem Zustand (kein blosser MagicMock — (b)/(c) muessen
zaehlen), `_fake_response(usage=None)` haelt die Cost-Hooks inaktiv, `_starte_paar`/`_sammle_ein`
mit `daemon=True` und `join(timeout=_JOIN_S)`.

## Ausdrueckliche Nicht-Behauptungen

- **(b) ist gegen den heutigen Stand GRUEN.** Er ist **kein** ROT-Beleg. Der heutige globale
  Riegel schuetzt gegen Duplikate exakt genauso gut wie der geplante per-`conv_id`-Riegel.
  Wer diesen Test als Beleg fuer den Fix fuehrt, taeuscht sich. Er ist reine Absicherung
  **gegen den falschen Fix**.
- **Diese Datei wurde NICHT ausgefuehrt.** Kein lokales `pytest` (CLAUDE.md HART: Kein Local-Dev).
  Lokal geprueft wurde ausschliesslich der Syntaxbaum (`ast.parse`) plus die Anker-greps aus den
  Acceptance-Kriterien. **Die Aussage „(a) ist rot" ist damit hergeleitet, nicht gemessen** —
  gemessen wird sie im Deploy-Tor in **Plan 03**.
- Das Zeitverhalten ist **nicht** gemessen: kein `time.time()`, kein `perf`-Marker, keine
  „parallel < 2x seriell"-Arithmetik. Bewiesen wird ausschliesslich „beide gleichzeitig drin?"
  ueber ein `threading.Event`-Rendezvous.

## Tasks & Commits

| Task | Name | Commit |
|---|---|---|
| 1 | Test-Modul anlegen — Docstring, Konstanten, Fake-Apparatur | `49b6b2f` |
| 2 | Test (a) — ROT-Beleg, zwei conv_id gleichzeitig | `353b246` |
| 3 | Test (b) Gegenpol + Test (c) Falsifizierbarkeit | `36ef630` |

## Verifikation

| Kriterium | Ergebnis |
|---|---|
| `ast.parse` | OK |
| `grep -c "def test_"` | 3 |
| `@pytest.mark.rot_vor_fix` | 1 (nur Test a) |
| `@pytest.mark.perf` / `time.time()` | 0 / 0 |
| `.wait()` ohne `timeout=` | 0 |
| `_RENDEZVOUS_S = 5.0` / `_GEGENPOL_S = 0.5` | 1 / 1 |
| `len(fake_db.karten) == 3` / `== 6` / `len(aufrufe) == 1` | 1 / 1 / 1 |
| `KEIN ROT-Beleg` im Docstring | 1 |
| Produktiv-Code im Diff (`^services/`, `^routes/`, `^app.py`) | 0 |
| Geloeschte Dateien in den 3 Commits | 0 |
| Diff `002f578..36ef630` | ausschliesslich `tests/test_lernkarten_lock_pro_conv.py` |
| Zeilen | 300 (min_lines 180) |

## Deviations from Plan

Keine — der Plan wurde exakt wie geschrieben ausgefuehrt. Die Code-Bloecke aus
`<action>` sind verbatim uebernommen.

## Known Stubs

Keine. Die riegellose Mini-Funktion `_riegellos_erzeuge_karten` ist **kein** Stub, sondern ein
absichtlicher synthetischer Gegenbeweis-Quelltext im Testmodul (Muster
`tests/test_session_lock_blocking_calls_guard.py:432-441`) — sie hat einen Konsumenten (Test c)
und eine Assertion.

## Naechster Schritt

Plan 02 (Welle 1, parallel): Pruefpunkt in `tests/test_no_live_global_state.py`.
Danach **Plan 03 (Welle 2)**: `bash deploy.sh production` — der ROT-Lauf, in dem beide
Test-Bausteine in **einem** roten Tor-Lauf belegt werden. Erwartete Tor-Zahlen laut Roadmap:
ROT `2 failed, 1140 passed`. Der Fix folgt erst in **Plan 04**.

## Self-Check: PASSED

- `tests/test_lernkarten_lock_pro_conv.py` → FOUND
- Commit `49b6b2f` → FOUND
- Commit `353b246` → FOUND
- Commit `36ef630` → FOUND
