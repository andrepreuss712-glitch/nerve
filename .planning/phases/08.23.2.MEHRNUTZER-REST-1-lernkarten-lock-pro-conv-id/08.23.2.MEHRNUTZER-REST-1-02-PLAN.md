---
phase: 08.23.2.MEHRNUTZER-REST-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_no_live_global_state.py
autonomous: true
requirements: [MEHRNUTZER-REST-1-WAECHTER, MEHRNUTZER-REST-1-SOLL, MEHRNUTZER-REST-1-KATALOG]
komplexitaet: mittel

must_haves:
  truths:
    - "Ein statischer AST-Sweep ueber services/*.py + routes/*.py meldet jeden modul-globalen Riegel, dessen Block einen LLM-/Netz-Aufruf umschliesst."
    - "Gegen den ungefixten Stand meldet er genau EINE Stelle: services/coaching_service.py:84."
    - "Der Waechter erkennt `with _analysis_lock_for(conv_id):` als bewachten Block — er wird nach dem Fix nicht blind."
    - "Faellt die Riegel-Ableitung aus, wird der Waechter ROT statt still gruen (Mindest-Soll auf Namen, Summe und je Datei)."
    - "Pruefkatalog und benannte Restluecken stehen im selben Commit im Docstring."
  artifacts:
    - path: "tests/test_no_live_global_state.py"
      provides: "Neuer Pruefpunkt: modul-globaler Riegel um einen Netz-/LLM-Aufruf"
      contains: "_RIEGEL_SOLL_JE_DATEI"
  key_links:
    - from: "tests/test_no_live_global_state.py"
      to: "services/coaching_service.py:84"
      via: "AST-Sweep, empfaenger-gebundener Netz-Katalog"
      pattern: "_NETZ_MESSAGES_METHODEN"
    - from: "tests/test_no_live_global_state.py"
      to: "services/live_session.py:374 (_TracedLock)"
      via: "weite Riegel-Ableitung endswith('Lock')"
      pattern: "_RIEGEL_KONSTRUKTOR_ENDUNG"
---

<objective>
Einen **neuen Pruefpunkt** in `tests/test_no_live_global_state.py` bauen: *modul-globaler Riegel,
der einen Netz-/LLM-Aufruf umschliesst.* Der bestehende Sweep dieser Datei kennt heute
ausschliesslich `ls.<attr> = …` und `ls.state[…] = …` (`:246-262`) — **Riegel sind gar keine
gepruefte Musterklasse**. Es ist also ein neuer Pruefpunkt, **keine** Verzeichnis-Erweiterung
(der Sweep laeuft ueber `services/` + `routes/` bereits, `:283` / `:339`).

Purpose: Roadmap Test-Netz-Ratsche Punkt 2+3 + CLAUDE.md Punkt 31. Der Waechter muss gegen den
**ungefixten** Stand ROT sein, sonst prueft er nichts — und er darf nach dem Fix **nicht blind**
werden (das ist der wichtigste Einzelpunkt der Phase, RESEARCH §4.6 / Falle 1).

Output: ein neuer Abschnitt **am Dateiende**. Die vier bestehenden Tests werden **nicht**
angefasst; Blast-Radius null.

Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-RESEARCH.md

<interfaces>
<!-- Die Bausteine, die 1:1 aus tests/test_session_lock_blocking_calls_guard.py uebernommen
     werden. RESEARCH §7: nicht neu erfinden — der __pycache__-Ausschluss und
     "Syntaxfehler wird rot statt still uebersprungen" sind teuer erkaufte Details. -->

Aus `tests/test_session_lock_blocking_calls_guard.py`:

```python
# :342-352 — Datei-Walker
def _python_dateien():
    ergebnis = []
    for d in _SCAN_DIRS:
        for p in (_ROOT / d).rglob('*.py'):
            if '__pycache__' in p.parts:
                continue
            ergebnis.append((p.relative_to(_ROOT).as_posix(), p))
    return sorted(ergebnis)

# :355-361 — Syntaxfehler wird ROT, nicht still uebersprungen
def _baum_oder_fehler(pfad):
    try:
        return ast.parse(pfad.read_text(encoding='utf-8')), None
    except (SyntaxError, UnicodeDecodeError) as e:
        return None, f'{type(e).__name__}: {e}'

# :239-247 — with-Bloecke, bei denen IRGENDEIN Kontext-Ausdruck der Riegel ist
# :260-274 — try-Bloecke, deren finally den Riegel FREIGIBT (verankert an der Freigabe)
# :277-295 — _riegel_region: body + orelse + except-Ruempfe + finally VOR dem release
# :299-318 — _ist_verbotener_aufruf: EMPFAENGER-gebunden (requests.<x> ueber f.value.id,
#            messages.create/stream ueber f.value.attr == 'messages')
```

⚠ Diese Datei (`test_no_live_global_state.py`) importiert `ast` bereits auf `:27`, benutzt es
aber nirgends. Der neue Abschnitt ist der erste AST-Nutzer darin. `import textwrap` fehlt und
muss ergaenzt werden (fuer die Selbst-Tests gegen synthetischen Quelltext).

⚠ **Falle 7 (RESEARCH §8):** `_WHITELIST` (`:98-183`) enthaelt bereits Riegel-Namen
(`state_lock`, `_per_sid_lock`, …) — aber als **erlaubte Zuweisungsziele** fuer `ls.<attr> =`.
Wer sie fuer den neuen Pruefpunkt mitbenutzt, schaltet ihn fuer die halbe Riegel-Menge still ab.
**Eigene Konstanten mit eigenem Praefix `_RIEGEL_` / `_NETZ_`, nichts Bestehendes mitbenutzen.**
</interfaces>

<messwerte>
<!-- AST-gemessen am Stand 2026-08-06 (RESEARCH §4.3/§4.4). Diese Zahlen gehen VERBATIM
     in die Soll-Tabelle. -->

Modul-globale Riegel (**weite** Ableitung, `endswith('Lock')`):

```
services/anonymization.py    3   :21 _error_lock  :29 _nlp_lock  :51 _gliner_lock
services/claude_service.py   1   :18 _ewb_circuit_lock
services/coaching_service.py 1   :8  _analysis_lock
services/deepgram_service.py 1   :22 _sessions_lock
services/live_session.py    15   :19 :49 :53 :66 :72 :83 :185 :223 :237 :245 :287 :289
                                 :374 (_session_state_lock = _TracedLock(...)) :380 :386
services/precall_service.py  1   :23 _cache_lock
services/qa_pipeline.py      1   :90 _MODEL_LOCK  (= _threading.Lock())
routes/training.py           1   :42 _sessions_lock
=> 24 Zuweisungen, 23 DISTINKTE Namen (_sessions_lock kommt in zwei Dateien vor)
```

Ueberwachte Bloecke je Datei `(with, try/finally)` — SUMME **143**:

```
routes/app_routes.py            (3, 0)      services/deepgram_service.py   (26, 1)
routes/training.py              (9, 0)      services/einwand_keyword_matcher.py (2, 0)
services/anonymization.py       (5, 0)      services/live_session.py       (37, 4)
services/claude_service.py     (47, 0)      services/precall_service.py     (2, 0)
services/coaching_service.py    (1, 0)      services/prompt_pipeline.py     (3, 0)
services/cost_tracker.py        (2, 0)      services/qa_pipeline.py         (1, 0)
```

VERSTOESSE **heute** (der ROT-Grund, genau die richtige Stelle):

```
services/coaching_service.py:84  [_analysis_lock]  messages.create
services/coaching_service.py:84  [_analysis_lock]  http_llm_client()
```

⚠ Zwei Melde-Zeilen fuer **eine** Stelle (beides im selben Ausdruck auf Zeile 84) →
je `(datei, zeile)` **deduplizieren**, sonst behauptet die Meldung zwei Verstoesse, wo einer ist.

Variante A kollidiert heute mit **0** bestehenden Bloecken — nachgemessen:
`grep -rE "with [A-Za-z_][A-Za-z0-9_.]*_lock(_for)?\(" services/ routes/` → keine Treffer.
Die einzige Funktion im Sweep-Bereich mit passender Endung ist
`services/exchange_rates.py:100 def _acquire_worker_lock()` — sie wird nirgends in einem `with`
benutzt und liefert ein `bool`, keinen Riegel. Gehoert als zweischneidige Heuristik in die
Restluecken.
</messwerte>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AST-Bausteine, Riegel-Ableitung (weit + Variante A), Netz-Katalog, Soll-Tabelle</name>

  <read_first>
    - `tests/test_session_lock_blocking_calls_guard.py:154-360` — die Bausteine, die uebernommen
      werden: `_SCAN_DIRS`, `_ist_session_state_lock` (`:228-236`), `_sammle_bloecke` (`:239-247`),
      `_ist_lock_release_aufruf` (`:251-257`), `_sammle_try_bloecke` (`:260-274`),
      `_riegel_region` (`:277-295`), `_ist_verbotener_aufruf` (`:299-318`),
      `_verbotene_aufrufe_in` (`:321-338`), `_python_dateien` (`:342-352`),
      `_baum_oder_fehler` (`:355-361`), `_SOLL_MINDESTENS`/`_SOLL_SUMME_MINDESTENS` (`:208-219`).
    - `tests/test_no_live_global_state.py:1-40` (Docstring + Importe, `ast` ist auf `:27`
      importiert aber ungenutzt) und `:98-183` (`_WHITELIST` — **nicht** mitbenutzen, Falle 7).
    - `services/live_session.py:292` (`class _TracedLock`) und `:374`
      (`_session_state_lock = _TracedLock('_session_state_lock')`) — der Grund fuer die **weite**
      Ableitung.
    - `services/coaching_service.py:8` und `:84` — die Stelle, die heute rot werden MUSS.
  </read_first>

  <files>tests/test_no_live_global_state.py</files>

  <behavior>
    - `_riegel_namen_gesamt()` liefert heute **≥ 23** distinkte Namen, darunter `_session_state_lock`
      (aus `_TracedLock`) und `_analysis_lock`.
    - `_ist_riegel_ausdruck(<Name aus der Menge>)` → True; `_ist_riegel_ausdruck(<ast.Call mit
      Funktionsnamen auf `_lock`/`_lock_for` endend>)` → True.
    - `_ist_netz_aufruf` meldet `<x>.messages.create/stream`, `requests.<x>`, `httpx.<x>`,
      nacktes `http_llm_client(...)` — und meldet `d.get('k')` **nicht**.
  </behavior>

  <action>
`import textwrap` zu den Importen von `tests/test_no_live_global_state.py` ergaenzen (`ast` ist
bereits da, `:27`). Dann **ans Dateiende** — nach `test_pending_migration_is_empty` (`:377-392`) —
diesen Abschnitt anhaengen. **Die vier bestehenden Tests und `_WHITELIST` bleiben unberuehrt.**

```python
# ══════════════════════════════════════════════════════════════════════════════
# Pruefpunkt 6 (Phase 08.23.2.MEHRNUTZER-REST-1): modul-globaler Riegel, der einen
# Netz-/LLM-Aufruf umschliesst.
#
# Eigene Konstanten mit eigenem Praefix — _WHITELIST oben wird BEWUSST NICHT
# mitbenutzt: sie listet erlaubte ZUWEISUNGSZIELE fuer `ls.<attr> =` und enthaelt
# dabei Riegel-Namen (state_lock, _per_sid_lock, ...). Wer sie hier mitbenutzte,
# schaltete diesen Pruefpunkt fuer die halbe Riegel-Menge still ab.
# ══════════════════════════════════════════════════════════════════════════════

_RIEGEL_SCAN_DIRS = ('services', 'routes')

# WEITE Riegel-Ableitung: jede Modul-Ebenen-Zuweisung, deren rechte Seite ein Aufruf
# ist, dessen Name auf 'Lock' endet. Faengt threading.Lock(), threading.RLock(),
# _threading.Lock() UND _TracedLock('_session_state_lock')
# (services/live_session.py:374). Ein enges Kriterium "threading.Lock()" wuerde
# ausgerechnet den wichtigsten Riegel des Projekts STILL uebersehen und die
# ueberwachte Blockmenge von 143 auf 40 senken — genau die Punkt-31-Fehlerklasse.
_RIEGEL_KONSTRUKTOR_ENDUNG = 'Lock'

# VARIANTE A (Phase MEHRNUTZER-REST-1, der wichtigste Einzelpunkt): ein Aufruf,
# dessen Funktionsname auf '_lock' oder '_lock_for' endet, gilt ebenfalls als
# Riegel-Ausdruck. OHNE das faellt der in dieser Phase gebaute
# `with _analysis_lock_for(conv_id):` aus der eigenen Bewachung — er ist ein
# ast.Call, kein Riegel-Name -> das Datei-Soll coaching_service.py: 1 muesste auf 0,
# und "ein Eintrag mit Soll 0 kann nie fehlschlagen"
# (tests/test_session_lock_blocking_calls_guard.py:200-207). Der Waechter waere
# gruen ABER BLIND. Das ist eine ERWEITERUNG der Bewachung, kein Aufweichen:
# die Menge der bewachten Bloecke wird groesser, nie kleiner.
# Gemessen 2026-08-06: kollidiert mit 0 bestehenden Bloecken.
_RIEGEL_FABRIK_ENDUNGEN = ('_lock', '_lock_for')

# Netz-/LLM-Katalog. BEWUSST ENG und EMPFAENGER-GEBUNDEN: ein Katalog ueber
# Methodennamen (z.B. 'get') produzierte allein durch dict.get(...) unter Riegeln
# neun Falsch-Treffer (precall_service.py:219/:285, deepgram_service.py:184/:898,
# routes/training.py:420/:482/:582/:609).
_NETZ_EMPFAENGER_MODULE = frozenset({'requests', 'httpx'})
_NETZ_MESSAGES_METHODEN = frozenset({'create', 'stream'})
_NETZ_NACKTE_NAMEN = frozenset({'http_llm_client'})

# ── Mindest-Soll (CLAUDE.md Punkt 31: Sperre gegen den stillen Ausfall) ───────
# Faellt eine abgeleitete Menge aus, faende der Sweep 0 Verstoesse und SAEHE GRUEN AUS.
# Sinkt eine Zahl: Ursache klaeren und MIT BEGRUENDUNG nachziehen — den Test NIE
# entfernen, die Zahl NIE stillschweigend senken.
_RIEGEL_SOLL_NAMEN_MINDESTENS = 23      # heute exakt 23 distinkte Namen
_RIEGEL_SOLL_SUMME_MINDESTENS = 140     # heute 143 (138 with + 5 try/finally), Puffer 3

# Stand 2026-08-06 (AST-gemessen). services/coaching_service.py: 1 ist der WICHTIGSTE
# Eintrag: er sichert, dass der in dieser Phase gebaute conv_id-Riegel auch NACH dem
# Fix noch bewacht ist.
_RIEGEL_SOLL_JE_DATEI = {
    'routes/app_routes.py': 3,
    'routes/training.py': 9,
    'services/anonymization.py': 5,
    'services/claude_service.py': 47,
    'services/coaching_service.py': 1,
    'services/cost_tracker.py': 2,
    'services/deepgram_service.py': 27,
    'services/einwand_keyword_matcher.py': 2,
    'services/live_session.py': 41,
    'services/precall_service.py': 2,
    'services/prompt_pipeline.py': 3,
    'services/qa_pipeline.py': 1,
}

# Falsch-Treffer-Ausnahmen. HEUTE LEER. Jeder Eintrag braucht einen
# '# FALSCH-TREFFER:'-Kommentar mit Datei:Zeile und Begruendung. Ein ECHTER Fund
# gehoert NICHT hierher, sondern gemeldet und behoben.
_FALSCH_TREFFER_RIEGEL = frozenset()    # {(datei, zeile), ...}


# ── Riegel-Ableitung ─────────────────────────────────────────────────────────
def _riegel_namen_einer_datei(baum):
    """Modul-globale Riegel-Namen EINER Datei.

    Nur baum.body (echte Modul-Ebene), nicht ast.walk: ein Riegel in einer Funktion
    oder als Klassen-Attribut ist pro-Instanz und bewusst ausserhalb.
    """
    namen = set()
    for knoten in baum.body:
        if not isinstance(knoten, ast.Assign) or not isinstance(knoten.value, ast.Call):
            continue
        f = knoten.value.func
        aufruf = f.attr if isinstance(f, ast.Attribute) else getattr(f, 'id', '')
        if not aufruf.endswith(_RIEGEL_KONSTRUKTOR_ENDUNG):
            continue
        for ziel in knoten.targets:
            if isinstance(ziel, ast.Name):
                namen.add(ziel.id)
    return namen


def _riegel_namen_gesamt():
    """Vereinigung ueber alle Dateien im Sweep-Bereich. Ueber den NAMEN, nicht ueber
    den Empfaenger — so muss keine Alias-Liste gepflegt werden (Begruendung:
    tests/test_session_lock_blocking_calls_guard.py:229-231)."""
    namen = set()
    for _datei, pfad in _riegel_python_dateien():
        baum, _f = _riegel_baum_oder_fehler(pfad)
        if baum is not None:
            namen |= _riegel_namen_einer_datei(baum)
    return namen


def _ist_riegel_ausdruck(expr, namen):
    """Riegel-Ausdruck: Name/Attribut aus der abgeleiteten Menge (faengt `x`, `ls.x`,
    `_ls.x`, `modul.x` — ueber den ATTRIBUT-Namen), ODER (Variante A) ein Aufruf,
    dessen Funktionsname auf _lock/_lock_for endet."""
    if isinstance(expr, ast.Attribute) and expr.attr in namen:
        return True
    if isinstance(expr, ast.Name) and expr.id in namen:
        return True
    if isinstance(expr, ast.Call):
        f = expr.func
        aufruf = f.attr if isinstance(f, ast.Attribute) else getattr(f, 'id', '')
        if aufruf.endswith(_RIEGEL_FABRIK_ENDUNGEN):
            return True
    return False


def _riegel_with_bloecke(baum, namen):
    """Jedes ast.With, bei dem IRGENDEIN Kontext-Ausdruck ein Riegel ist —
    auch `with anderer, _session_state_lock:`."""
    return [k for k in ast.walk(baum)
            if isinstance(k, ast.With)
            and any(_ist_riegel_ausdruck(i.context_expr, namen) for i in k.items)]


def _ist_riegel_freigabe(call, namen):
    f = call.func
    return (isinstance(f, ast.Attribute) and f.attr == 'release'
            and _ist_riegel_ausdruck(f.value, namen))


def _riegel_try_bloecke(baum, namen):
    """Jedes ast.Try, dessen finally-Zweig einen Riegel FREIGIBT. Verankert an der
    FREIGABE, nicht am Erwerb: das release() markiert das Ende der Region eindeutig,
    waehrend der Erwerb je nach Form eine Zeile hoeher oder im if-Test steht.
    Heute 5 solche Regionen (deepgram_service.py 1, live_session.py 4). Der Sammler
    kommt trotzdem mit: sonst fiele ein kuenftiger Umbau von `with` auf
    `acquire`/`try` STILL aus der Bewachung."""
    return [k for k in ast.walk(baum)
            if isinstance(k, ast.Try)
            and any(isinstance(n, ast.Call) and _ist_riegel_freigabe(n, namen)
                    for anw in k.finalbody for n in ast.walk(anw))]


def _riegel_region(try_knoten, namen):
    """Die Anweisungen, die WIRKLICH unter dem Riegel laufen: body + orelse +
    except-Ruempfe + finally-Anweisungen VOR dem release. Ab dem release ist frei."""
    region = list(try_knoten.body) + list(try_knoten.orelse)
    for behandler in try_knoten.handlers:
        region.extend(behandler.body)
    for anw in try_knoten.finalbody:
        if any(isinstance(n, ast.Call) and _ist_riegel_freigabe(n, namen)
               for n in ast.walk(anw)):
            break
        region.append(anw)
    return region


# ── Netz-/LLM-Pruefung (genau EINE, fuer beide Formen) ───────────────────────
def _ist_netz_aufruf(call):
    """Melde-Name des Netz-/LLM-Aufrufs, sonst None. EMPFAENGER-gebunden."""
    f = call.func
    if isinstance(f, ast.Name):
        return f.id if f.id in _NETZ_NACKTE_NAMEN else None
    if isinstance(f, ast.Attribute):
        if (f.attr in _NETZ_MESSAGES_METHODEN
                and isinstance(f.value, ast.Attribute) and f.value.attr == 'messages'):
            return 'messages.' + f.attr
        if (isinstance(f.value, ast.Name)
                and f.value.id in _NETZ_EMPFAENGER_MODULE):
            return f'{f.value.id}.{f.attr}'
    return None


def _netz_aufrufe_in(knoten_oder_liste):
    """ast.walk geht auch in verschachtelte with/try/if-Bloecke: ein messages.create
    zwei Ebenen tief im selben Riegel-Block ist genauso schaedlich."""
    knoten = (knoten_oder_liste if isinstance(knoten_oder_liste, list)
              else [knoten_oder_liste])
    treffer = []
    for wurzel in knoten:
        for n in ast.walk(wurzel):
            if isinstance(n, ast.Call):
                name = _ist_netz_aufruf(n)
                if name:
                    treffer.append((getattr(n, 'lineno', 0), name))
    return treffer


# ── Datei-Sweep (uebernommen aus test_session_lock_blocking_calls_guard.py) ──
def _riegel_python_dateien():
    """__pycache__ wird uebersprungen — .pyc ist kein Quelltext, und stale Bytecode war
    in Phase COUNTERPART-03 der einzige verbliebene Falsch-Treffer. rglob statt glob:
    Unterordner von services//routes/ sind damit mit abgedeckt (heute existieren keine)."""
    ergebnis = []
    for d in _RIEGEL_SCAN_DIRS:
        for p in (_ROOT / d).rglob('*.py'):
            if '__pycache__' in p.parts:
                continue
            ergebnis.append((p.relative_to(_ROOT).as_posix(), p))
    return sorted(ergebnis)


def _riegel_baum_oder_fehler(pfad):
    """Kein errors='ignore': ein Dekodier-Fehler soll auffallen, nicht still einen
    Block verschlucken."""
    try:
        return ast.parse(pfad.read_text(encoding='utf-8')), None
    except (SyntaxError, UnicodeDecodeError) as e:
        return None, f'{type(e).__name__}: {e}'


def _riegel_zaehlung(baum, namen):
    """(anzahl_with, anzahl_try) fuer eine Datei."""
    return len(_riegel_with_bloecke(baum, namen)), len(_riegel_try_bloecke(baum, namen))
```

**Bau-Regel 17:** ausschliesslich anhaengen + eine Import-Zeile. Keine Umbenennung, keine
Sortierung, keine Aenderung an `_WHITELIST`, `_PENDING_MIGRATION` oder den vier bestehenden Tests.
  </action>

  <verify>
    <automated>python -c "import ast; s=open('tests/test_no_live_global_state.py',encoding='utf-8').read(); ast.parse(s); assert 'import textwrap' in s and '_RIEGEL_SOLL_JE_DATEI' in s"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "_RIEGEL_KONSTRUKTOR_ENDUNG = 'Lock'" tests/test_no_live_global_state.py` → 1
    - `grep -c "_RIEGEL_FABRIK_ENDUNGEN = ('_lock', '_lock_for')" tests/test_no_live_global_state.py` → 1
    - `grep -c "_RIEGEL_SOLL_NAMEN_MINDESTENS = 23" tests/test_no_live_global_state.py` → 1
    - `grep -c "_RIEGEL_SOLL_SUMME_MINDESTENS = 140" tests/test_no_live_global_state.py` → 1
    - `grep -c "'services/coaching_service.py': 1," tests/test_no_live_global_state.py` → 1
    - `grep -c "'services/live_session.py': 41," tests/test_no_live_global_state.py` → 1
    - `grep -c "def _ist_netz_aufruf" tests/test_no_live_global_state.py` → 1
    - `grep -c "import textwrap" tests/test_no_live_global_state.py` → 1
    - Bestehende Tests unangetastet:
      `grep -c "def test_no_new_live_global_state_writes\|def test_pending_migration_is_empty\|def test_killed_module_globals_absent\|def test_deprecated_globals_not_newly_written" tests/test_no_live_global_state.py` → 4
    - `_WHITELIST` unveraendert: `git diff -U0 tests/test_no_live_global_state.py | grep -c "^-.*_WHITELIST"` → 0
  </acceptance_criteria>

  <done>
    Der Abschnitt steht am Dateiende, die Datei parst, alle Konstanten und Helfer existieren,
    nichts Bestehendes wurde veraendert.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Die fuenf Pruefungen — Sweep (ROT-Beleg), Mindest-Soll, drei Selbst-Tests</name>

  <read_first>
    - der in Task 1 angehaengte Abschnitt in `tests/test_no_live_global_state.py`
    - `tests/test_session_lock_blocking_calls_guard.py:364-430` — Melde-Format des Sweeps
      (`datei:zeile [muster]`, **ein** `assert not verstoesse` am Ende, Handlungsanweisung im
      Text) und die Ist-Zaehlung-Ausgabe ins Gate-Log (`:409-416`).
    - `tests/test_session_lock_blocking_calls_guard.py:432-460` — `_analysiere_quelle` /
      `_zaehle_bloecke`: Selbst-Tests fahren **denselben** Sweep gegen synthetischen Quelltext.
      Ein nachgebauter Mini-Detektor wuerde nur sich selbst beweisen.
    - `pytest.ini` — Marker `rot_vor_fix`.
    - `services/coaching_service.py:8,59,84` — die Stelle, die heute gemeldet werden MUSS.
  </read_first>

  <files>tests/test_no_live_global_state.py</files>

  <behavior>
    - Sweep gegen den **ungefixten** Stand meldet genau **eine** deduplizierte Stelle:
      `services/coaching_service.py:84`.
    - Soll-Test: ≥23 Namen, Summe ≥140, jede Datei ≥ Tabellenwert. Faellt die Ableitung aus,
      wird er ROT statt still gruen.
    - Selbst-Test 1 (positiv): synthetischer Quelltext mit `_x_lock = threading.Lock()` +
      `with _x_lock:` + `client.messages.create(...)` → 1 Treffer.
    - Selbst-Test 2 (negativ): derselbe Riegel um `d.get('k')` und `', '.join(xs)` → 0 Treffer.
    - Selbst-Test 3 (**L3-Beweis**): `with _analysis_lock_for(conv_id):` wird als bewachter Block
      **gezaehlt** und ein `messages.create` darin **gemeldet**.
  </behavior>

  <action>
Diese fuenf Pruefungen anhaengen:

```python
def _riegel_analysiere_quelle(quelltext, namen=None):
    """Faehrt DENSELBEN Sweep gegen synthetischen Quelltext (Muster
    tests/test_session_lock_blocking_calls_guard.py:439-449). Die Schnipsel stehen in
    tests/, das NICHT im Sweep-Bereich liegt -> kein Selbst-Treffer.
    namen=None: Riegel-Namen aus dem Schnipsel selbst ableiten."""
    baum = ast.parse(textwrap.dedent(quelltext))
    if namen is None:
        namen = _riegel_namen_einer_datei(baum)
    treffer = []
    for block in _riegel_with_bloecke(baum, namen):
        treffer.extend(_netz_aufrufe_in(block))
    for tblock in _riegel_try_bloecke(baum, namen):
        treffer.extend(_netz_aufrufe_in(_riegel_region(tblock, namen)))
    return treffer


def _riegel_zaehle_bloecke_in_quelle(quelltext, namen=None):
    baum = ast.parse(textwrap.dedent(quelltext))
    if namen is None:
        namen = _riegel_namen_einer_datei(baum)
    return (len(_riegel_with_bloecke(baum, namen))
            + len(_riegel_try_bloecke(baum, namen)))


@pytest.mark.rot_vor_fix
def test_kein_modul_globaler_riegel_um_netz_aufruf():
    """Kein modul-globaler Riegel darf einen Netz-/LLM-Aufruf umschliessen.

    Die Fehlerklasse: ein prozessweiter Riegel um einen Aufruf mit
    config.HTTP_LLM_TIMEOUT_LONG_S = 45 s Zeitlimit serialisiert ALLE Nutzer. Solo
    getestet unsichtbar; zwei gleichzeitige Anruf-Enden warten hintereinander und
    belegen dabei je einen der 64 gthread-Threads (deploy/nerve.service:35-36).

    ROT-Beleg (Marker rot_vor_fix, CLAUDE.md Punkt 31): gegen den ungefixten Stand
    MUSS diese Pruefung failen — mit services/coaching_service.py:84.

    KEIN Source-Presence-False-Green (CLAUDE.md Test-Qualitaets-Regel): geprueft wird
    ein VERBOTENES Muster, nicht die Existenz erwuenschten Codes. Verschwindet der
    Aufruf aus dem Riegel, wird der Test gruener; kommt einer hinzu, roetet er.
    """
    namen = _riegel_namen_gesamt()
    gefunden = {}
    for datei, pfad in _riegel_python_dateien():
        baum, fehler = _riegel_baum_oder_fehler(pfad)
        if baum is None:
            # Nicht verschlucken: sichtbar rot statt still uebersprungen.
            gefunden[(datei, 0)] = {f'SYNTAX ({fehler})'}
            continue
        treffer = []
        for block in _riegel_with_bloecke(baum, namen):
            treffer.extend(_netz_aufrufe_in(block))
        for tblock in _riegel_try_bloecke(baum, namen):
            treffer.extend(_netz_aufrufe_in(_riegel_region(tblock, namen)))
        for zeile, muster in treffer:
            if (datei, zeile) in _FALSCH_TREFFER_RIEGEL:
                continue
            # Dedup je (datei, zeile): messages.create UND http_llm_client() stehen im
            # selben Ausdruck; zwei Melde-Zeilen behaupteten zwei Verstoesse statt einem.
            gefunden.setdefault((datei, zeile), set()).add(muster)

    verstoesse = [f"{d}:{z}  [{', '.join(sorted(m))}]"
                  for (d, z), m in sorted(gefunden.items())]
    assert not verstoesse, (
        f"{len(verstoesse)} modul-globale(r) Riegel um einen Netz-/LLM-Aufruf:\n  "
        + "\n  ".join(verstoesse)
        + "\n\nPunkt 28 (Mehr-Nutzer): ein prozessweiter Riegel um einen Netz-Aufruf "
          "serialisiert ALLE Nutzer ueber die volle Zeitlimit-Dauer.\n"
          "NICHT den Riegel ersatzlos entfernen (der Duplikatschutz haengt daran) und "
          "NICHT die Falsch-Treffer-Liste fuellen. Den Riegel pro Schluessel fuehren "
          "(Muster: services/coaching_service.py _analysis_lock_for) ODER den "
          "Netz-Aufruf AUS dem Riegel-Block herausziehen.")


def test_riegel_sweep_erreicht_alle_bekannten_bloecke():
    """Sperre gegen den stillen Ausfall (CLAUDE.md Punkt 31).

    Faellt die Riegel-Ableitung aus, ist die Namensmenge leer, der Sweep faende 0
    Verstoesse und SAEHE GRUEN AUS. Diese Pruefung macht daraus ROT.
    """
    namen = _riegel_namen_gesamt()
    ist, aufteilung = {}, {}
    for datei, pfad in _riegel_python_dateien():
        baum, _f = _riegel_baum_oder_fehler(pfad)
        if baum is None:
            continue
        n_with, n_try = _riegel_zaehlung(baum, namen)
        if n_with or n_try:
            ist[datei] = n_with + n_try
            aufteilung[datei] = (n_with, n_try)

    print('\n[MEHRNUTZER-REST-1 Riegel-Waechter] Ist-Zaehlung:')
    print(f'  abgeleitete Riegel-Namen: {len(namen)} -> {sorted(namen)}')
    for datei in sorted(ist):
        n_with, n_try = aufteilung[datei]
        print(f'  {datei}: {ist[datei]} (with={n_with}, try/finally={n_try})')
    print(f'  SUMME: {sum(ist.values())} in {len(ist)} Dateien')

    assert len(namen) >= _RIEGEL_SOLL_NAMEN_MINDESTENS, (
        f"Unter-Ableitung: nur {len(namen)} modul-globale Riegel-Namen gefunden, "
        f"erwartet mindestens {_RIEGEL_SOLL_NAMEN_MINDESTENS}. Entweder ist die "
        f"Ableitung kaputt (dann ist der Waechter BLIND, nicht gruen) oder Riegel "
        f"wurden legitim entfernt (dann die Zahl MIT BEGRUENDUNG nachziehen, den Test "
        f"NICHT entfernen). Gefunden: {sorted(namen)}")

    zu_wenig = {d: (ist.get(d, 0), soll) for d, soll in _RIEGEL_SOLL_JE_DATEI.items()
                if ist.get(d, 0) < soll}
    assert not zu_wenig, (
        f"Unter-Sweep je Datei: {zu_wenig}. services/coaching_service.py ist der "
        f"wichtigste Eintrag — faellt er auf 0, ist der conv_id-Riegel dieser Phase "
        f"aus der eigenen Bewachung gefallen (Variante A / _RIEGEL_FABRIK_ENDUNGEN "
        f"greift nicht mehr).")

    assert sum(ist.values()) >= _RIEGEL_SOLL_SUMME_MINDESTENS, (
        f"Unter-Sweep ueber die Summe: {sum(ist.values())} ueberwachte Bloecke, "
        f"erwartet mindestens {_RIEGEL_SOLL_SUMME_MINDESTENS}. Gezaehlt werden BEIDE "
        f"Formen (with + try/finally).")


# ── Selbst-Tests gegen synthetischen Quelltext ───────────────────────────────

def test_riegel_sweep_beisst_gegen_synthetischen_quelltext():
    """Positiv-Beleg: der Sweep meldet die Fehlerklasse tatsaechlich — in beiden
    Erwerbsformen und ueber Alias-Schreibweisen."""
    with_form = """
        import threading
        _x_lock = threading.Lock()

        def f(c):
            with _x_lock:
                r = c.messages.create(model='m')
                return r
    """
    try_form = """
        import threading
        _y_lock = threading.Lock()

        def g(c):
            _y_lock.acquire()
            try:
                return c.messages.stream(model='m')
            finally:
                _y_lock.release()
    """
    requests_form = """
        import threading
        _z_lock = threading.Lock()

        def h():
            with _z_lock:
                return requests.post('https://x')
    """
    nackter_aufruf = """
        import threading
        _w_lock = threading.Lock()

        def i():
            with _w_lock:
                return http_llm_client(long_running=True)
    """
    traced_huelle = """
        _session_state_lock = _TracedLock('_session_state_lock')

        def j(c):
            with _session_state_lock:
                return c.messages.create(model='m')
    """
    for name, quelle in (('with', with_form), ('try/finally', try_form),
                         ('requests', requests_form), ('nackt', nackter_aufruf),
                         ('_TracedLock', traced_huelle)):
        assert _riegel_analysiere_quelle(quelle), (
            f"Der Sweep sieht die Fehlerklasse in der Form '{name}' NICHT — "
            f"solange das so ist, beweist sein Gruen nichts.")


def test_riegel_sweep_meldet_harmloses_nicht():
    """Negativ-Beleg: der enge, empfaenger-gebundene Katalog produziert keine
    Falsch-Treffer. Ein Katalog ueber Methodennamen ('get', 'join') haette allein
    durch dict.get(...) unter Riegeln neun Falsch-Treffer im Bestand."""
    harmlos = """
        import threading
        _a_lock = threading.Lock()

        def f(d, xs, c):
            with _a_lock:
                v = d.get('k')
                s = ', '.join(xs)
                n = create(model='m')          # nacktes create, KEIN messages.create
                return v, s, n
    """
    assert _riegel_analysiere_quelle(harmlos) == [], (
        f"Falsch-Treffer im harmlosen Quelltext: "
        f"{_riegel_analysiere_quelle(harmlos)!r}. Der Katalog MUSS "
        f"empfaenger-gebunden bleiben.")

    ausserhalb = """
        import threading
        _b_lock = threading.Lock()

        def f(c):
            with _b_lock:
                daten = {'k': 1}
            return c.messages.create(model='m')   # AUSSERHALB des Riegels
    """
    assert _riegel_analysiere_quelle(ausserhalb) == [], (
        "Ein Netz-Aufruf AUSSERHALB des Riegel-Blocks darf nicht gemeldet werden — "
        "sonst waere der Waechter unbrauchbar laut.")


def test_riegel_erkennung_erfasst_context_manager_aufruf():
    """DER WICHTIGSTE SELBST-TEST DIESER PHASE (Variante A / _RIEGEL_FABRIK_ENDUNGEN).

    Nach dem Fix heisst es in services/coaching_service.py nicht mehr
    `with _analysis_lock:` sondern `with _analysis_lock_for(conv_id):` — ein ast.Call,
    kein Riegel-Name. OHNE Variante A faellt dieser Block aus der Zaehlung, das
    Datei-Soll coaching_service.py: 1 muesste auf 0, und der Waechter waere GRUEN ABER
    BLIND. Genau das verbietet
    tests/test_session_lock_blocking_calls_guard.py:200-207 ("ein Eintrag mit Soll 0
    kann nie fehlschlagen"). Dieser Test nagelt die Erweiterung fest.
    """
    nach_dem_fix = """
        import threading
        _conv_locks = {}
        _conv_locks_guard = threading.Lock()

        def f(conv_id, c):
            with _analysis_lock_for(conv_id):
                return c.messages.create(model='m')
    """
    assert _riegel_zaehle_bloecke_in_quelle(nach_dem_fix) == 1, (
        "`with _analysis_lock_for(conv_id):` wird NICHT als bewachter Block gezaehlt. "
        "Damit faellt der in dieser Phase gebaute Riegel aus der eigenen Bewachung "
        "und das Datei-Soll services/coaching_service.py: 1 muesste auf 0 — der "
        "Waechter waere gruen ABER BLIND (RESEARCH §4.6, Falle 1). "
        "_RIEGEL_FABRIK_ENDUNGEN pruefen.")
    assert _riegel_analysiere_quelle(nach_dem_fix), (
        "Ein messages.create UNTER `with _analysis_lock_for(conv_id):` wird nicht "
        "gemeldet — die Bewachung des neuen Riegels ist nur scheinbar da.")

    # Gegenprobe: ein gewoehnlicher Aufruf ist KEIN Riegel-Ausdruck.
    kein_riegel = """
        def f(c):
            with open('x') as fh:
                return c.messages.create(model='m')
    """
    assert _riegel_zaehle_bloecke_in_quelle(kein_riegel) == 0, (
        "`with open(...)` wird faelschlich als Riegel-Block gezaehlt — die "
        "Endungs-Heuristik ist zu weit.")
```
  </action>

  <verify>
    <automated>python -c "import ast; s=open('tests/test_no_live_global_state.py',encoding='utf-8').read(); ast.parse(s); assert s.count('def test_')==9"</automated>
    <!-- 4 bestehende + 5 neue. Scharfe Abnahme: Plan 03 (Tor auf dem Server). -->
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_" tests/test_no_live_global_state.py` → 9 (4 bestehende + 5 neue)
    - `grep -c "@pytest.mark.rot_vor_fix" tests/test_no_live_global_state.py` → 1
    - `grep -c "def test_kein_modul_globaler_riegel_um_netz_aufruf" tests/test_no_live_global_state.py` → 1
    - `grep -c "def test_riegel_sweep_erreicht_alle_bekannten_bloecke" tests/test_no_live_global_state.py` → 1
    - `grep -c "def test_riegel_erkennung_erfasst_context_manager_aufruf" tests/test_no_live_global_state.py` → 1
    - `grep -cF "_analysis_lock_for(conv_id)" tests/test_no_live_global_state.py` → mindestens 1
    - `grep -c "gefunden.setdefault((datei, zeile), set())" tests/test_no_live_global_state.py` → 1 (Dedup)
    - `grep -c "_WHITELIST" tests/test_no_live_global_state.py` → unveraendert gegenueber
      `git show HEAD:tests/test_no_live_global_state.py | grep -c "_WHITELIST"`
  </acceptance_criteria>

  <done>
    Fuenf neue Pruefungen stehen in der Datei; genau eine traegt `rot_vor_fix`; der L3-Selbst-Test
    (`test_riegel_erkennung_erfasst_context_manager_aufruf`) existiert und assertiert **beides**:
    Blockzaehlung == 1 **und** Meldung des `messages.create` darunter.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pruefkatalog + RESTLUECKEN + GEPRUEFT UND GESCHLOSSEN (CLAUDE.md Punkt 31)</name>

  <read_first>
    - `tests/test_session_lock_blocking_calls_guard.py:104-152` — die Musterform der Abschnitte
      `RESTLUECKEN`, `GEPRUEFT UND GESCHLOSSEN`, `ZWEITE SCHICHT DARUNTER`, `STOP-REGEL`.
    - `tests/test_no_live_global_state.py:1-25` — der bestehende Modul-Docstring (hat **keinen**
      RESTLUECKEN-Abschnitt, der Test ist aelter als Punkt 31). Er wird **nicht** umgeschrieben;
      der neue Katalog gehoert in den Abschnitts-Docstring des neuen Pruefpunkts.
    - `services/anonymization.py:36,62` und `services/qa_pipeline.py:97` — die drei Lazy-Model-
      Loads, die der Katalog bewusst **nicht** meldet.
    - `services/live_session.py:1518-1541` — der LOCKWATCH-Wachhund (die zweite Schicht, die es
      **nur** fuer `_session_state_lock` gibt).
  </read_first>

  <files>tests/test_no_live_global_state.py</files>

  <action>
Unmittelbar **unter** der Abschnitts-Ueberschrift „Pruefpunkt 6" aus Task 1 (also **vor**
`_RIEGEL_SCAN_DIRS`) diesen Docstring als Modul-Ebenen-String einfuegen. Ein Gruen ohne Katalog ist
keine Aussage (CLAUDE.md Punkt 31) — und die Roadmap fordert ihn ausdruecklich als Punkt 3 der
Test-Netz-Ratsche.

```python
_RIEGEL_PRUEFKATALOG = """PRUEFPUNKT 6 — modul-globaler Riegel um einen Netz-/LLM-Aufruf.
(Phase 08.23.2.MEHRNUTZER-REST-1, CLAUDE.md Punkt 31)

WAS DER KATALOG ABDECKT (positiv, in einem Satz):
Ein auf MODUL-EBENE definierter Riegel, dessen `with`-Block — oder dessen
`acquire`/`try`/`finally`-Region — einen im Block SICHTBAR aufgerufenen LLM- oder
HTTP-Aufruf enthaelt: <x>.messages.create/stream, requests.*, httpx.*,
http_llm_client(...) — ueber alle .py-Dateien in services/ und routes/.

WAS ER STRUKTURELL NICHT SEHEN KANN:
1. EIN HELFER EINE EBENE TIEFER. Steht unter dem Riegel `self._frag_das_modell()` und
   der Netzaufruf liegt IN dieser Funktion, sieht der Sweep nichts. Das ist dieselbe
   Restluecke wie bei tests/test_session_lock_blocking_calls_guard.py:114 und die
   WAHRSCHEINLICHSTE kuenftige Rueckkehr der Fehlerklasse.
2. Dynamischer Dispatch (getattr(...)()), Callbacks, Monkeypatch, Registry-Hooks.
3. Riegel, die NICHT auf Modul-Ebene entstehen — Klassen-Attribute wie
   services/anonymization.py:197, services/einwand_keyword_matcher.py:219,
   services/live_session.py:331. Bewusst ausserhalb, weil pro-Instanz.
4. Alles ausserhalb von services/ + routes/: app.py, database/, nerve_rt/, scripts/.
   ⚠ nerve_rt/ ist damit UNBEWACHT und hat bereits eine eigene HART-Regel (CLAUDE.md,
   fehlende Anonymisierung) — dieser Waechter deckt sie NICHT mit ab.

WO DIE HEURISTIKEN ZWEISCHNEIDIG SIND (beide Richtungen):
5. Riegel-Erkennung ueber den NAMEN: `_sessions_lock` existiert in zwei Dateien
   (services/deepgram_service.py:22, routes/training.py:42). Eine gleichnamige LOKALE
   Variable in einer dritten Datei gaelte als Riegel -> Richtung FALSCH-TREFFER (laut,
   harmlos). Umgekehrt rutscht ein Riegel durch, der ueber eine FUNKTION statt einer
   Zuweisung entsteht.
6. Konstruktor-Kriterium endswith('Lock') faengt threading.Lock/RLock und _TracedLock,
   aber NICHT eine Huelle namens z.B. `Mutex` oder `make_lock()`.
7. VARIANTE A (endswith('_lock','_lock_for') auf AUFRUFE) haelt den conv_id-Riegel in
   der Bewachung, macht aber jeden gleich benannten Aufruf zu einem Riegel-Ausdruck.
   Beleg fuer die Richtung: services/exchange_rates.py:100 `_acquire_worker_lock()`
   liefert ein bool, keinen Riegel — wird heute in keinem `with` benutzt (gemessen
   2026-08-06: 0 Treffer), gaelte dort aber als Riegel-Block. Richtung FALSCH-TREFFER
   (laut), nicht Durchrutscher.
8. messages.create/stream sind an den Empfaenger `messages` gebunden; ein LLM-SDK mit
   anderer Aufrufform (z.B. client.complete(...)) rutscht durch.

WAS FORMAL UNKLAR BLEIBT:
9. Ob ein Lazy-Model-Load unter Riegel als "Netzaufruf" zu werten ist:
   services/anonymization.py:62 (_gliner_lock -> GLiNER.from_pretrained, :67),
   services/qa_pipeline.py:97 (_MODEL_LOCK -> SentenceTransformer, :101),
   services/anonymization.py:36 (_nlp_lock -> spacy.load, :38). Alle drei sind
   Double-Checked-Locking, EINMAL pro Prozess beim Kaltstart, kein pro-Nutzer-Aufruf —
   deshalb BEWUSST NICHT im Katalog. from_pretrained KANN beim allerersten Mal einen
   Download ausloesen; die Klasse ist damit OFFEN, nicht geschlossen. Sie stehen
   ABSICHTLICH NICHT in _FALSCH_TREFFER_RIEGEL: es sind keine Falsch-Treffer, der
   Katalog erfasst sie schlicht nicht.

WELCHE ZWEITE SCHICHT DARUNTER LIEGT:
10. Fuer _session_state_lock: der LOCKWATCH-Wachhund (services/live_session.py:1518-1541,
    Laufzeit) plus tests/test_session_lock_blocking_calls_guard.py (breiteres Verbots-Set).
    Fuer ALLE UEBRIGEN modul-globalen Riegel — darunter der neue conv_id-Riegel in
    services/coaching_service.py — gibt es KEINE Laufzeit-Schicht. Dieser statische
    Sweep ist dort die einzige Schicht.

GEPRUEFT UND GESCHLOSSEN:
11. Die DIREKTESTE Form der Fehlerklasse — ein modul-globaler Riegel, der im selben
    Block sichtbar messages.create/stream oder requests.*/httpx.* umschliesst — ist
    GEFANGEN. Belegt doppelt: durch test_riegel_sweep_beisst_gegen_synthetischen_quelltext
    (fuenf Formen, inkl. try/finally und _TracedLock-Huelle) UND durch den ROT-Lauf
    dieser Phase gegen services/coaching_service.py:84.
12. Die Form NACH dem Fix — `with _analysis_lock_for(conv_id):` — ist EBENFALLS gefangen
    (Variante A), belegt durch test_riegel_erkennung_erfasst_context_manager_aufruf.
    Ohne diesen Punkt waere der Waechter nach dem Fix gruen ABER BLIND.
13. _session_state_lock ist von diesem Pruefpunkt erfasst (weite Ableitung ueber
    _TracedLock), war aber schon vorher durch
    tests/test_session_lock_blocking_calls_guard.py mit breiterem Verbots-Set gedeckt.
    Doppel-Deckung, keine Luecke.
14. Unterordner von services//routes/ sind ueber rglob mit abgedeckt (heute existieren
    keine) — anders als bei den vier aelteren Sweeps dieser Datei, die glob('*.py')
    benutzen.

STOP-REGEL: erwartet sind 0 Verstoesse. Ein echter Fund wird mit Datei:Zeile gemeldet —
NICHT in _FALSCH_TREFFER_RIEGEL geschoben, NICHT durch Aufweichen des Netz-Katalogs
aufgeloest, und NICHT durch ersatzloses Entfernen des Riegels (daran haengt der
Duplikatschutz: learning_cards hat keinen Unique-Constraint auf call_id,
database/models.py:629-631).
"""
```

Zusaetzlich: eine Pruefung, die den Katalog gegen stilles Verschwinden sichert.

```python
def test_riegel_pruefkatalog_ist_vollstaendig():
    """Ein Waechter ohne benannte Grenzen erzeugt falsche Sicherheit (Punkt 31).
    Diese Pruefung haelt die sechs Pflicht-Bestandteile im Dokument fest."""
    for ueberschrift in ('WAS DER KATALOG ABDECKT',
                         'WAS ER STRUKTURELL NICHT SEHEN KANN',
                         'WO DIE HEURISTIKEN ZWEISCHNEIDIG SIND',
                         'WAS FORMAL UNKLAR BLEIBT',
                         'WELCHE ZWEITE SCHICHT DARUNTER LIEGT',
                         'GEPRUEFT UND GESCHLOSSEN',
                         'STOP-REGEL'):
        assert ueberschrift in _RIEGEL_PRUEFKATALOG, (
            f"Pflicht-Abschnitt '{ueberschrift}' fehlt im Pruefkatalog "
            f"(CLAUDE.md Punkt 31).")
```
  </action>

  <verify>
    <automated>python -c "import ast; s=open('tests/test_no_live_global_state.py',encoding='utf-8').read(); ast.parse(s); [s.index(x) for x in ('WAS DER KATALOG ABDECKT','GEPRUEFT UND GESCHLOSSEN','STOP-REGEL','ZWEITE SCHICHT')]"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "_RIEGEL_PRUEFKATALOG" tests/test_no_live_global_state.py` → mindestens 2
      (Definition + Pruefung)
    - `grep -cF "WAS DER KATALOG ABDECKT" tests/test_no_live_global_state.py` → 1
    - `grep -cF "GEPRUEFT UND GESCHLOSSEN" tests/test_no_live_global_state.py` → 1
    - `grep -cF "WELCHE ZWEITE SCHICHT DARUNTER LIEGT" tests/test_no_live_global_state.py` → 1
    - `grep -cF "STOP-REGEL" tests/test_no_live_global_state.py` → 1
    - `grep -c "def test_riegel_pruefkatalog_ist_vollstaendig" tests/test_no_live_global_state.py` → 1
    - `grep -c "def test_" tests/test_no_live_global_state.py` → 10 (4 bestehende + 6 neue)
    - `grep -cF "_gliner_lock" tests/test_no_live_global_state.py` → mindestens 1
      (die bewusste Nicht-Abdeckung MUSS benannt sein)
    - `_FALSCH_TREFFER_RIEGEL` ist leer geblieben:
      `grep -c "_FALSCH_TREFFER_RIEGEL = frozenset()" tests/test_no_live_global_state.py` → 1
  </acceptance_criteria>

  <done>
    Der Pruefkatalog steht im selben Commit wie der Waechter, nennt alle sechs Punkt-31-Pflichtteile,
    benennt die drei Lazy-Model-Loads als bewusst nicht abgedeckt (und **nicht** als Falsch-Treffer)
    und sagt ausdruecklich, dass fuer den neuen conv_id-Riegel **keine** Laufzeit-Schicht existiert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entwickler → `services/` + `routes/` | neuer Code kann die Fehlerklasse zurueckbringen |
| Waechter → sein eigener Pruefkatalog | ein gruener Waechter wird als Beweis gelesen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-MEHRNUTZER-05 | Repudiation | Waechter-Gruen ohne Katalog | mitigate | `_RIEGEL_PRUEFKATALOG` + `test_riegel_pruefkatalog_ist_vollstaendig` (Task 3). |
| T-MEHRNUTZER-06 | Denial of Service | ausgefallene Riegel-Ableitung | mitigate | Drei Mindest-Soll-Anker (Namen ≥23, Summe ≥140, je Datei) → ROT statt still gruen. |
| T-MEHRNUTZER-07 | Tampering | `_FALSCH_TREFFER_RIEGEL` | mitigate | Bleibt leer; STOP-REGEL im Katalog verbietet das Fuellen bei echten Funden. |
| T-MEHRNUTZER-08 | Elevation of Privilege | `_WHITELIST`-Mitbenutzung (Falle 7) | mitigate | Eigene `_RIEGEL_`/`_NETZ_`-Konstanten; Acceptance-Kriterium prueft, dass `_WHITELIST` unveraendert ist. |
</threat_model>

<verification>
- `tests/test_no_live_global_state.py` parst; 10 Tests (4 bestehende + 6 neue).
- Genau **ein** neuer Test traegt `rot_vor_fix`.
- `_WHITELIST` / `_PENDING_MIGRATION` / die vier bestehenden Tests unveraendert.
- Der L3-Selbst-Test existiert und prueft **beides** (Blockzaehlung **und** Meldung).
- **Abnahme ist NICHT lokal.** Der scharfe Lauf gegen das serverseitige Tor ist **Plan 03**;
  dort MUSS `test_kein_modul_globaler_riegel_um_netz_aufruf` mit
  `services/coaching_service.py:84` failen.
- ⚠ Falls im ROT-Lauf **zusaetzlich** `test_riegel_sweep_erreicht_alle_bekannten_bloecke` failt,
  ist eine der Soll-Zahlen gegenueber dem Stand 2026-08-06 gedriftet (eine andere Phase hat in
  `services/`/`routes/` gebaut). Dann: Ist-Zaehlung aus dem Gate-Log ablesen, Tabelle **mit
  Begruendung** nachziehen — **nicht** den Test entfernen und **nicht** `coaching_service.py: 1`
  antasten.
</verification>

<success_criteria>
- Neuer Pruefpunkt am Dateiende, Blast-Radius null an den bestehenden Tests.
- Weite Riegel-Ableitung (`endswith('Lock')`) erfasst `_session_state_lock` aus `_TracedLock`.
- Variante A erfasst `with _analysis_lock_for(conv_id):` — durch einen eigenen Selbst-Test belegt.
- Drei Mindest-Soll-Anker vorhanden, `services/coaching_service.py: 1` darunter.
- Pruefkatalog mit allen sechs Punkt-31-Bestandteilen im selben Commit.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.MEHRNUTZER-REST-1-lernkarten-lock-pro-conv-id/08.23.2.MEHRNUTZER-REST-1-02-SUMMARY.md` anlegen.
**Nicht** deployen — der ROT-Lauf gehoert zu Plan 03 (Welle 2).
</output>
