---
phase: 08.23.2.CALLID
plan: 02
type: execute
wave: 2
complexity: "🔴"
depends_on: ["08.23.2.CALLID-01"]
deploy: 1
files_modified:
  - services/deepgram_service.py
  - services/live_session.py
  - tests/test_callid_race_close.py
autonomous: true
requirements: [CI-2]
user_setup: []

must_haves:
  truths:
    - "Nach dem Fix ist die call_id im per-SID-Zustand durable, BEVOR die Detection (on_message → Fast/Medium/Button-emit) für dieselbe sid feuern kann (Ordering-Gate)"
    - "Die Call-Anlage bleibt EINZIG in start_live_session (Single-Owner) — NIE aus dem Detection/emit-Thread (kein Lazy-Create)"
    - "Kein intent_event wird im Call-Start-Fenster mit call_id=NULL geschrieben (Race geschlossen, kein verlorener erster Einwand)"
    - "Latenz: das Gate läuft im Start-/Hintergrund-Pfad, NICHT in der Live-Antwort-Bahn (Punkt 25)"
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "Ordering-Garantie: call_id durable vor Detection-Freischaltung (Reorder ODER Gate, je research_first)"
      contains: "create_call_for_sid"
  key_links:
    - from: "services/deepgram_service.py handle_start_live_session"
      to: "durable call_id VOR _open_deepgram_connection-Wirkung (Detection)"
      via: "Reorder (create_call_for_sid vor connection-open) ODER Per-SID-Ready-Gate/Buffer"
      pattern: "create_call_for_sid"
---

<objective>
Schließt die Call-Start-Race (CI-2, Locked = Option 2 Ordering-Gate / Single-Owner). RESEARCH §3 belegt am Code: `_open_deepgram_connection` öffnet bei deepgram_service.py:572, `create_call_for_sid` läuft erst bei :712 — die Verbindung (= Detection-Freischaltung) steht, BEVOR die call_id durable ist; der Quell-Kommentar :51-52 dokumentiert die Race. Detection kann also vor durable call_id feuern → ohne Gate verlöre der erste Einwand seine call_id (NULL → Slow-Lane fail-closed, RESEARCH §4).

Locked (André/Claudian/Gemini): Call-Anlage bleibt Single-Owner in start_live_session (NIE aus dem Detection-Thread). KEIN „nur defensiv" (Datenverlust). **research_first entscheidet die schlankste Mechanik:**
- **bevorzugt (Reorder):** `init_session_state` + `create_call_for_sid` VOR `_open_deepgram_connection` ziehen → call_id durable bevor die Verbindung steht → harte sequenzielle Garantie, kein Puffer.
- **Fallback (Per-SID-Puffer):** nur wenn der Reorder nachweislich unsicher ist (Vorwärts-Abhängigkeit), frühe Emits pro sid puffern + flushen sobald `resolve_call_id_for_sid` durable liefert.

Output: Ordering-Gate (eine der zwei Mechaniken, research_first-belegt) + Race-Close-Integrationstest. Defensiver Backstop = Plan 03 (Netz, nicht Ersatz).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
PRE-EXECUTE: Plan 01 (resolve_call_id_for_sid + Pflicht-Param) MUSS gebaut sein. kein Local-Dev. Multi-Segment-Gotcha: Pfade hardcoden, gsd-tools/gsd-verifier umgehen.
</execution_context>

<context>
@.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (research_first): Reorder-Sicherheit belegen → Mechanik wählen</name>
  <files>services/deepgram_service.py</files>
  <read_first>
    - services/deepgram_service.py:519-572 (Handler-Kopf: mode :539, keyterms :548-570, _open_deepgram_connection :572)
    - services/deepgram_service.py:595-732 (per-SID-Init-Block: init_session_state :646, anonymizer/profile/cache :662-694, create_call_for_sid :712)
    - services/deepgram_service.py:49-52 (on_message-Race-Kommentar) + on_message-Body (was liest der Detection-Pfad aus _session_state?)
    - RESEARCH §3 (Sequenz + beide Mechanik-Optionen)
  </read_first>
  <action>
    PFLICHT-VORARBEIT (André-Direktive): am echten Code belegen, ob `init_session_state` + `create_call_for_sid` VOR `_open_deepgram_connection` gezogen werden können, OHNE etwas zu brechen:
    - Braucht `_open_deepgram_connection`/`build_keyterms` etwas, das init_session_state erst NACH :572 setzt? (`mode` ist bei :539 schon gesetzt; keyterms :548-570 laden eigenständig — prüfen, ob sonst etwas aus dem Init-Block in den Connection-Open-Pfad fließt.)
    - Liest der on_message-Detection-Pfad per-SID-State, der ohne init_session_state fehlt? (heute schon Race-anfällig laut :51-52 — Reorder verbessert das generell.)
    ENTSCHEIDUNG dokumentieren (im SUMMARY): Reorder sicher → Reorder bauen (Task 2a). Reorder unsicher (konkrete Abhängigkeit benannt) → Per-SID-Puffer bauen (Task 2b). KEINE der beiden ohne Beleg.
  </action>
  <verify>
    <automated>grep -n "_open_deepgram_connection\|init_session_state\|create_call_for_sid" services/deepgram_service.py</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY benennt die gewählte Mechanik + den grep/Code-Beleg (welche Abhängigkeiten geprüft, Ergebnis)
    - Single-Owner bestätigt: create_call_for_sid wird NICHT in on_message/Detection-Thread gerufen (grep: create_call_for_sid nur in start_live_session)
  </acceptance_criteria>
  <done>Mechanik (Reorder vs Puffer) am Code belegt gewählt — kein Blind-Bau.</done>
</task>

<task type="auto">
  <name>Task 2a (falls Reorder gewählt): call_id durable VOR _open_deepgram_connection</name>
  <files>services/deepgram_service.py</files>
  <read_first>
    - services/deepgram_service.py:595-732 (Init-Block, der vor :572 muss) + :519-572 (Ziel-Einfügepunkt)
    - services/live_session.py:529-590 (create_call_for_sid)
  </read_first>
  <action>
    `init_session_state` (+ die für create_call_for_sid nötige Vorarbeit) und `create_call_for_sid` VOR `_open_deepgram_connection(:572)` ziehen, sodass `_session_state[sid]['state']['call_id']` durable ist, BEVOR die Verbindung steht. Reihenfolge bewahren: was `_open_deepgram_connection`/keyterms wirklich brauchen (mode :539, keyterms :570) bleibt davor. Die idempotente Fast-Path-Guard (:706-718) + Reconnect-Pfad unverändert lassen. Profil/anonymizer/cache dürfen — falls sie die Verbindung nicht blockieren müssen — an ihrer Stelle bleiben; NUR call_id muss vor der Verbindung durable sein. Bei DB-Fehler (create_call_for_sid None) bleibt das Verhalten wie heute (Skip-Guard + Log) — der defensive Backstop (Plan 03) fängt das downstream.
    AUDIO-DROP-NOTIZ (im SUMMARY dokumentieren — Gemini-Review-Fund 2): das Vorziehen der Call-Anlage VOR `_open_deepgram_connection` bedeutet, dass `audio_chunk`-Events, die feuern bevor die Deepgram-Connection offen ist (`_deepgram_sessions[sid]` noch None), von `handle_audio_chunk` verworfen werden → die ersten ~50–200 ms Audio können gedroppt werden. BEWUSST AKZEPTIERT: VoIP-Gesprächsanlauf ist i.d.R. Stille/Rauschen, und Detection läuft ohnehin erst nach offener Connection — kein Einwand-Verlust. Keine echte Vorwärts-Abhängigkeit (mode :539 + keyterms :570 bleiben vor dem Connection-Open).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_callid_race_close.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `create_call_for_sid` steht im Handler VOR `_open_deepgram_connection` (grep-Zeilenordnung)
    - create_call_for_sid NICHT im Detection-Thread (Single-Owner, grep)
    - bestehende Start-/Reconnect-Tests grün (kein Regress im Call-Start)
    - SUMMARY enthält die Audio-Drop-Notiz (erste ~50–200 ms `audio_chunk` vor offener Connection können verworfen werden — bewusst akzeptiert)
  </acceptance_criteria>
  <done>call_id durable vor Detection-Freischaltung; harte sequenzielle Garantie.</done>
</task>

<task type="auto">
  <name>Task 2b (NUR falls Reorder unsicher): Per-SID-Puffer früher Emits</name>
  <files>services/live_session.py, services/deepgram_service.py</files>
  <read_first>
    - services/live_session.py:205-215 (_session_state) + resolve_call_id_for_sid (Plan 01)
    - RESEARCH §3 (Puffer-Variante)
  </read_first>
  <action>
    NUR bauen, wenn Task 1 den Reorder als unsicher belegt. Per-SID-RAM-Puffer: feuert ein Emit für eine sid, deren call_id noch nicht durable ist (`resolve_call_id_for_sid` → None), wird der Emit-Auftrag pro sid gepuffert (NICHT verworfen, NICHT mit NULL geschrieben). Sobald create_call_for_sid die durable call_id setzt (Single-Owner, start_live_session-Thread), wird der Puffer für die sid mit der jetzt-durable call_id geflusht. Puffer läuft im Hintergrund/Start-Pfad (Punkt 25, kein Live-Antwort-Belang). Obergrenze + Timeout (kein unbegrenztes Wachstum); Überlauf → defensiver Backstop (Plan 03, 'failed' + Alarm).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_callid_race_close.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - Früher Emit ohne durable call_id wird gepuffert (nicht NULL geschrieben, nicht verworfen)
    - Flush nach durable call_id schreibt mit korrekter call_id; Call-Anlage bleibt Single-Owner
    - Puffer-Obergrenze/Timeout vorhanden (kein Leak)
  </acceptance_criteria>
  <done>Frühe Emits überleben race-frei mit korrekter call_id; Single-Owner gewahrt.</done>
</task>

<task type="auto">
  <name>Task 3: Race-Close-Integrationstest</name>
  <files>tests/test_callid_race_close.py</files>
  <read_first>
    - tests/test_callid_threading.py (Plan 01) + die gewählte Mechanik (Task 2a/2b)
    - RESEARCH §3/§8
  </read_first>
  <action>
    Test stellt die Call-Start-Race nach: ein Emit-Versuch im Fenster „Verbindung offen, call_id noch nicht durable". Assert (mechanik-abhängig): Reorder → call_id ist bei jedem möglichen Emit bereits durable (kein NULL-Emit möglich, da Sequenz erzwungen); Puffer → früher Emit wird gepuffert + nach durable call_id mit korrekter call_id geschrieben (kein NULL, kein Verlust). Echte Effekt-Assertion (kein Source-Presence). Skip ohne TEST_DATABASE_URL wo DB nötig.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_callid_race_close.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - Test beweist: KEIN intent_event mit call_id=NULL im Start-Fenster (V-CI-3)
    - Test grün server-seitig
  </acceptance_criteria>
  <done>Race-Close integrationsbelegt (kein NULL-Emit), nicht nur „Helper existiert".</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `intent_event` (SCHREIBEN über die emit-Pfade; Gate stellt call_id sicher) — Schema unverändert.
- `calls` (LESEN/Anlage via create_call_for_sid, vor-verlegt) — keine Schema-Änderung.
- `_session_state` (RAM): call_id-Durability ist der Gate-Kern.
### Cross-Layer-Konsistenz
| Datum | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| durable `state['call_id']` | create_call_for_sid (Single-Owner) → resolve_call_id_for_sid | RAM `_session_state` → DB `intent_event.call_id` | ✓ RESEARCH §2/§3 |
| Detection-Freischaltung | _open_deepgram_connection :572 vs create_call_for_sid :712 | on_message-Thread (RAM) | ✓ RESEARCH §3 (Race belegt) |

<verification>
- Mechanik am Code belegt gewählt (Task 1).
- Reorder: create_call_for_sid vor _open_deepgram_connection (grep-Ordnung). / Puffer: früher Emit gepuffert+geflusht.
- Single-Owner: create_call_for_sid nicht im Detection-Thread (grep).
- Integrationstest: kein NULL-call_id-Emit im Start-Fenster (V-CI-3).
</verification>

<success_criteria>
- call_id durable vor Detection (Ordering-Gate), Single-Owner gewahrt, kein NULL-Emit, kein verlorener erster Einwand.
- Latenz: Start-/Hintergrund-Pfad (Punkt 25 ✓).
- VALIDATION V-CI-3 nach grünem Gate auf green.
</success_criteria>

<output>
`.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-02-SUMMARY.md` (gewählte Mechanik + Beleg + Audio-Drop-Notiz beim Reorder dokumentieren).
</output>
