---
phase: 08.23.2.CALLID
plan: 03
type: execute
wave: 2
complexity: "🔴"
depends_on: ["08.23.2.CALLID-01"]
deploy: 1
files_modified:
  - services/slow_lane.py
  - tests/test_slowlane_callid_defensive.py
autonomous: true
requirements: [CI-4]
user_setup: []

must_haves:
  truths:
    - "Ist call_id/Tenant für ein Slow-Lane-Item nicht ermittelbar, setzt _persist_event_ref handling_status='failed' (kein abstain_log-Versuch, kein Endlos-Re-Queue) UND loggt einen LAUTEN Alarm"
    - "Nach dem Fix (Plan 01/02) darf dieser Pfad praktisch nie feuern; tut er's = sichtbarer Alarm (Regression/Race), nicht still"
    - "flush_to_db (slow_lane.py:328) setzt vor dem abstain_log-Write dieselbe A1-set_current_tenant-Klammer wie der Consumer-Loop — kein fail-closed-Verlust beim SIGTERM/atexit-Flush bei gültiger call_id"
  artifacts:
    - path: "services/slow_lane.py"
      provides: "Defensiver Backstop (failed+Alarm statt Endlos-Loop) + flush_to_db A1-GUC-Klammer"
      contains: "handling_status = 'failed'"
  key_links:
    - from: "services/slow_lane.py flush_to_db"
      to: "set_current_tenant vor dem abstain_log-Write (A1, wie Consumer-Loop)"
      via: "Per-Item-Tenant ermitteln → set → write → clear im finally"
      pattern: "set_current_tenant"
---

<objective>
Defensives Netz + zweiter Schreibpfad (CI-4). Zwei Teile:
1. **Backstop:** Ist für ein Slow-Lane-Item kein call_id/Tenant ermittelbar (Rest-Race/Regression nach Plan 01/02), markiert `_persist_event_ref` das Event `handling_status='failed'` (ABGESCHLOSSEN, NICHT 'pending') — KEIN abstain_log-INSERT-Versuch (der fail-closed würde, RESEARCH §4) und damit KEIN Endlos-Re-Queue durch H-3 — PLUS ein LAUTER Alarm-Log. Nach dem Fix darf das praktisch nie feuern; tut es das, ist es sichtbar (Regression/Race), nicht still.
2. **flush_to_db-Klammer:** `flush_to_db` (slow_lane.py:328) committet abstain_log-Writes OHNE Tenant-GUC (RESEARCH §5, Asymmetrie zum Consumer-Loop :399) → beim SIGTERM/atexit-Flush wird ein abstain_log-INSERT auch bei gültiger call_id fail-closed abgewiesen. Dieselbe A1-`set_current_tenant`-Klammer wie der Consumer-Loop nachrüsten.

Output: Backstop in _persist_event_ref + A1-Klammer in flush_to_db + Tests (failed-Pfad / flush-mit-GUC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
PRE-EXECUTE: Plan 01 gebaut (resolve-Naht). Plan 02 (Race-Close) ist der primäre Fix — dieser Backstop ist das Netz, KEIN Ersatz. kein Local-Dev. Multi-Segment-Gotcha.
</execution_context>

<context>
@.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-RESEARCH.md

<interfaces>
_persist_event_ref (slow_lane.py:199-266): 'failed'-Setzpunkte :235-237 (Tor-1 low-conf) / :244-250 (Poison-Pill); abstain-Zweig :259-266 `db.add(AbstainLog(... tenant_id=_tenant_id_for(ev,db) ...))`. _tenant_id_for :161-169 (`if not ev.call_id: return None`).
Consumer-Loop A1-Klammer (slow_lane.py:385-410, TENANT-FOUND): separate Read-Session → set_current_tenant → Schreib-Session → clear im finally.
flush_to_db (slow_lane.py:328-348): drain → _persist_event_ref pro Item → EIN db.commit(), OHNE set_current_tenant. Aufgerufen aus request_shutdown/SIGTERM/atexit (Bau-Regel 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Defensiver Backstop in _persist_event_ref (failed + Alarm statt Endlos-Loop)</name>
  <files>services/slow_lane.py</files>
  <read_first>
    - services/slow_lane.py:199-266 (_persist_event_ref, Status-Setzpunkte + abstain-Zweig)
    - services/slow_lane.py:161-183 (_tenant_id_for / _tenant_id_for_item — None bei fehlender call_id)
    - RESEARCH §4 (die fail-closed-Kette) + §0 (Backstop = primäres Netz, lauter Alarm)
  </read_first>
  <action>
    Im abstain-Zweig (vor `db.add(AbstainLog(...))`, :259-266): den Tenant ermitteln (`_tenant_id_for(ev, db)`). Ist er None (kein call_id ODER kein calls.tenant_id ermittelbar) → NICHT `db.add(AbstainLog(...))` (der INSERT würde fail-closed gegen FORCE RLS scheitern → rollback → 'pending' → H-3-Endlos). Stattdessen: `ev.handling_status = 'failed'` (ABGESCHLOSSEN, aus der pending-Arbeitsliste raus) + LAUTER Alarm-Log (z.B. `print(f"[CALLID-ALARM] slow-lane: tenant/call_id nicht ermittelbar für event_id={ev.event_id} (call_id={ev.call_id!r}) → 'failed' (Race/Regression NACH dem Fix — untersuchen). Kein abstain_log, kein Re-Queue.")`) und `return` (kein abstain_log). 
    WICHTIG: nur den abstain-Zweig betreffen — scored/Tor-1-failed/Poison-failed unverändert. Der Backstop greift NUR wenn ein Event abstainen würde, aber tenant nicht auflösbar ist. Damit ist 'failed' terminal (F-01: H-3 re-queued nur 'pending').
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_slowlane_callid_defensive.py tests/test_slow_lane_flush.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - abstain-Zweig: tenant None → `handling_status='failed'` + `[CALLID-ALARM]`-Log + return (KEIN db.add(AbstainLog)) (grep + Test)
    - 'pending' wird NICHT gesetzt (terminal, kein H-3-Re-Queue)
    - scored/Tor-1/Poison-Pfade unverändert (grep-Diff)
  </acceptance_criteria>
  <done>Nicht-auflösbarer Abstain wird terminal 'failed' + laut alarmiert — kein Endlos-Loop, kein stiller Verlust.</done>
</task>

<task type="auto">
  <name>Task 2: flush_to_db A1-set_current_tenant-Klammer</name>
  <files>services/slow_lane.py</files>
  <read_first>
    - services/slow_lane.py:328-348 (flush_to_db, Sammel-Commit ohne GUC)
    - services/slow_lane.py:385-410 (Consumer-Loop A1-Klammer als Muster)
    - RESEARCH §5
  </read_first>
  <action>
    flush_to_db so umbauen, dass jeder abstain_log-fähige Write unter gesetztem Tenant-GUC läuft — dieselbe A1-Klammer wie der Consumer-Loop, PRO ITEM (kein Sammel-Commit über Tenant-Grenzen): pro gedraintem Item separate Read-Session → `_tenant_id_for_item(item, _read_db)` → close → `set_current_tenant(str(_tid))` falls nicht None → Schreib-Session → `_persist_event_ref(item, db)` + commit → `clear_current_tenant()` im finally + close. Per-Item-Fehler isolieren (ein Item killt den Flush für andere nicht) UND EXPLIZIT loggen — nicht still schlucken (Gemini-Review-Fund 3): z.B. `print(f"[SLOW] flush_to_db item-Fehler event_id={getattr(item,'event_id',None)}: {e!r} — übersprungen, Flush läuft weiter")`. Ein einzelner Fehlschlag darf im Shutdown-Log nicht den erfolgreichen Flush der übrigen Items verdecken (kein `except: pass`). Damit ist flush_to_db symmetrisch zum Consumer-Loop. (Der Backstop aus Task 1 greift hier identisch: nicht-auflösbar → 'failed', kein fail-closed-Crash.)
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_slowlane_callid_defensive.py tests/test_slow_lane_flush.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - flush_to_db ruft `set_current_tenant` vor dem Write + `clear_current_tenant` im finally (grep)
    - Per-Item-TX (kein Sammel-Commit über mehrere Tenants); Per-Item-Fehler-Isolierung + EXPLIZITES Item-Fehler-Log (kein stilles `except: pass`)
    - bestehender test_slow_lane_flush.py grün (kein Regress an Drain/Flush-Mechanik)
  </acceptance_criteria>
  <done>flush_to_db schreibt abstain_log unter GUC (wie Consumer) — kein fail-closed-Verlust beim Shutdown-Flush.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — failed-Backstop + flush-mit-GUC</name>
  <files>tests/test_slowlane_callid_defensive.py</files>
  <read_first>
    - tests/test_slow_lane_tenant_guc.py (TENANT-FOUND, Muster für Slow-Lane-Integration gegen PG)
    - tests/conftest.py (db_session, TEST_TENANT_UUID, cleanup_rows)
  </read_first>
  <action>
    Gegen real PG (db_session, skip ohne DSN):
    (a) Backstop: ein intent_event OHNE call_id (call_id=NULL), das abstainen würde → `_persist_event_ref` setzt `handling_status='failed'` (Query-Assertion), schreibt KEIN abstain_log (Absenz-Query), loggt den Alarm. Kein 'pending' übrig (H-3 würde nicht re-queuen).
    (b) flush-mit-GUC: ein abstain-fähiges Item MIT gültiger call_id (seeded calls.tenant_id=TEST_TENANT_UUID) → flush_to_db schreibt die abstain_log-Row (GUC gesetzt), nicht fail-closed. Reverse-FK-clean via cleanup_rows.
    Echte DB-Row-Assertionen (kein Source-Presence). create_call_for_sid/grade_handling ggf. monkeypatchen wie test_slow_lane_tenant_guc.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_slowlane_callid_defensive.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - (a) No-call_id-Abstain → handling_status='failed', 0 abstain_log, Alarm (V-CI-4)
    - (b) flush_to_db schreibt abstain_log mit GUC bei gültiger call_id (V-CI-5)
    - grün server-seitig (oder skip ohne DSN, im SUMMARY vermerkt)
  </acceptance_criteria>
  <done>Backstop (failed+Alarm) und flush-GUC integrationsbelegt.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `intent_event` (SCHREIBEN: handling_status='failed' im Backstop) — Schema unverändert.
- `abstain_log` (SCHREIBEN: nur mit GUC + gültiger call_id; FORCE RLS aus TENANT-FOUND/0022) — Schema unverändert.
- `calls` (LESEN: _tenant_id_for; keine RLS) — keine Änderung.
### Cross-Layer-Konsistenz
| Datum | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| `handling_status='failed'` | _persist_event_ref Backstop | DB `intent_event.handling_status` (NOT NULL, default 'pending') | ✓ models.py:771 |
| `app.tenant_id`-GUC | flush_to_db (NEU) + Consumer (vorhanden) | TX-local set_config (RAM/Session) | ✓ db.py:73-89 |
| `abstain_log`-INSERT | nur unter GUC + nicht-None tenant | DB `abstain_log` (FORCE RLS, tenant_id NOT NULL) | ✓ 0022 (TENANT-FOUND) |

<verification>
- Backstop: tenant None im Abstain → 'failed' + Alarm + kein abstain_log + kein 'pending' (V-CI-4).
- flush_to_db: set/clear_current_tenant (grep), per-Item-TX; abstain_log mit GUC geschrieben (V-CI-5).
- bestehende Slow-Lane-Mechanik (test_slow_lane_flush) regress-frei.
</verification>

<success_criteria>
- Defensiver Backstop terminal 'failed' + lauter Alarm (kein Endlos-Loop, kein stiller Verlust) — primäres Netz.
- flush_to_db symmetrisch zum Consumer (A1-GUC) — kein Shutdown-Flush-Verlust.
- VALIDATION V-CI-4 + V-CI-5 nach grünem Gate auf green.
</success_criteria>

<output>
`.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-03-SUMMARY.md`.
</output>
