---
phase: 08.23.2.CALLID
plan: 04
type: execute
wave: 3
complexity: "🔴"
depends_on: ["08.23.2.CALLID-01", "08.23.2.CALLID-02", "08.23.2.CALLID-03"]
deploy: 2
gated_by: soak-gate (V-CI-6 — 0 neue NULL-call_id-Zeilen + kein Backstop-Alarm nach Deploy 1)
files_modified:
  - alembic/versions/00XX_intent_event_call_id_not_null.py
  - database/models.py
autonomous: false
requirements: [CI-3]
user_setup: []

must_haves:
  truths:
    - "Die Deploy-2-Migration prüft VOR jeder Änderung, dass alle verbleibenden call_id-NULL-Zeilen handling_status='failed' sind — frische NULLs (aus einem Rest-Race) => STOP, NICHT dran scheitern, nichts ändern"
    - "Die 54 (bzw. verbleibenden) 'failed'+call_id-NULL-Zeilen werden gelöscht (idempotent, scoped WHERE call_id IS NULL)"
    - "intent_event.call_id wird auf NOT NULL gesetzt (DB-Wächter); models.py nullable=False nachgezogen"
    - "Diese Welle läuft NUR nach grünem SOAK-Gate (V-CI-6); autonomous:false (beaufsichtigter Deploy 2)"
  artifacts:
    - path: "alembic/versions/00XX_intent_event_call_id_not_null.py"
      provides: "Pre-Check (alle NULL == failed) + DELETE NULL + SET NOT NULL; down_revision = aktueller head (beim Execute verifizieren)"
      contains: "SET NOT NULL"
  key_links:
    - from: "alembic 00XX upgrade"
      to: "intent_event.call_id Constraint"
      via: "Pre-Check → DELETE WHERE call_id IS NULL → ALTER COLUMN SET NOT NULL"
      pattern: "alter_column.*call_id.*nullable=False"
---

<objective>
DEPLOY 2 (CI-3, phased; NUR nach grünem SOAK). Härtet die call_id-Integrität als DB-Wächter, NACHDEM Deploy 1 (Plan 01-03) bewiesen hat, dass keine neuen NULLs mehr entstehen (V-CI-6: `count(*) WHERE call_id IS NULL AND timestamp > <deploy1>` == 0, kein Backstop-Alarm). Schritte (Locked-Reihenfolge):
1. **Pre-Check (BLOCKIEREND):** alle verbleibenden `call_id IS NULL`-Zeilen MÜSSEN `handling_status='failed'` sein. Eine FRISCHE NULL (nach Deploy 1) = Race NICHT geschlossen → **STOP, nichts ändern** (kein DELETE, kein SET NOT NULL), zurück zu Plan 02.
2. **DELETE** der 54 (bzw. verbleibenden) 'failed'+NULL-Zeilen (Pre-Launch-Testdaten, nicht rekonstruierbar, André-Freigabe). Idempotent: `WHERE call_id IS NULL`.
3. **SET NOT NULL** auf `intent_event.call_id` + models.py `nullable=False`.

Defensiver Backstop (Plan 03) bleibt PRIMÄRES Netz auch nach NOT NULL — NOT NULL ist Belt-and-Suspenders.

Output: Migration (Pre-Check + DELETE + SET NOT NULL) + models.py. create_all-Falle: Migration als postgres VOR Restart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
PRE-EXECUTE (BLOCKIEREND, André/Claudian):
- SOAK-Gate GRÜN (V-CI-6): nach Deploy 1 über echte Test-Anrufe alle 4 emit-Lanes → `inspect.sh`: `SELECT count(*) FROM intent_event WHERE call_id IS NULL AND timestamp > '<deploy1-zeit>'` == 0; `inspect.sh logs` ohne `[CALLID-ALARM]`. NICHT grün → Deploy 2 NICHT fahren.
- Alembic-head beim Execute neu prüfen (`inspect.sh migrations`) — `down_revision` = der TATSÄCHLICHE head zum Deploy-2-Zeitpunkt (Annahme heute 0024 aus TENANT-FOUND; kann sich verschieben, wenn andere Phasen zwischenzeitlich Migrationen landen → NICHT blind hardcoden, am Prod-head ausrichten). Revision-Nummer entsprechend (z.B. 00XX = head+1).
- autonomous:false — beaufsichtigter Deploy. Multi-Segment-Gotcha: Pfade hardcoden, gsd-verifier umgehen.
</execution_context>

<context>
@.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-RESEARCH.md
<interfaces>
intent_event.call_id (models.py:765): UUID, FK calls.id CASCADE, nullable=True, definiert 0016 (RESEARCH §6). 54 prod-Zeilen call_id=NULL, alle 'failed' (RESEARCH §6, Pre-Check beim Execute neu belegen).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — Pre-Check + DELETE NULL + SET NOT NULL</name>
  <files>alembic/versions/00XX_intent_event_call_id_not_null.py</files>
  <read_first>
    - alembic/versions/0016_intent_event_table.py (call_id-Definition, Revisions-Muster)
    - alembic/versions/0024_rubric_score_tenant_not_null.py (alter_column SET NOT NULL-Muster, postgresql.UUID existing_type, down_revision-Kette aus TENANT-FOUND)
    - RESEARCH §6 (Schema, 54-NULL-Belege) + §0 (Pre-Check-STOP-Regel)
  </read_first>
  <action>
    Neue Migration `00XX_intent_event_call_id_not_null.py`, `down_revision` = aktueller Prod-head (beim Execute via `inspect.sh migrations` verifizieren; Annahme 0024). upgrade() in DIESER Reihenfolge:
    ```python
    def upgrade() -> None:
        conn = op.get_bind()
        # (1) Pre-Check (BLOCKIEREND): frische NULLs (nicht 'failed') = Race offen -> STOP.
        bad = conn.execute(sa.text(
            "SELECT count(*) FROM intent_event "
            "WHERE call_id IS NULL AND handling_status <> 'failed'"
        )).scalar()
        if bad and bad > 0:
            raise RuntimeError(
                f"CALLID Deploy 2 ABGEBROCHEN: {bad} call_id-NULL-Zeilen sind NICHT 'failed' "
                "(frische NULLs => Race nicht geschlossen). KEIN DELETE, KEIN SET NOT NULL. "
                "Zurueck zu Plan 02 + Soak.")
        # (2) DELETE der 'failed'+NULL-Zeilen (Pre-Launch-Testdaten, Andre-Freigabe). Idempotent.
        conn.execute(sa.text("DELETE FROM intent_event WHERE call_id IS NULL"))
        # (3) SET NOT NULL.
        op.alter_column('intent_event', 'call_id', nullable=False,
                        existing_type=postgresql.UUID(as_uuid=True))

    def downgrade() -> None:
        op.alter_column('intent_event', 'call_id', nullable=True,
                        existing_type=postgresql.UUID(as_uuid=True))
        # DELETE ist NICHT reversibel (Testdaten weg) — bewusst, AUDIT-NOTE.
    ```
    `import sqlalchemy as sa` + `from sqlalchemy.dialects import postgresql` (Hausmuster). KEIN COMMENT-Change nötig (Spalte ist FK/trivial-nah; falls models.py-Schild eh existiert, konsistent halten). Reine Daten+Constraint-Migration.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/ -k "intent_event or callid" -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - Pre-Check raised bei NULL-nicht-'failed' (grep: `handling_status <> 'failed'` + raise)
    - DELETE `WHERE call_id IS NULL` (idempotent) VOR SET NOT NULL
    - `alter_column('intent_event','call_id', nullable=False` (grep) + downgrade nullable=True
    - down_revision == aktueller Prod-head (Execute-Beleg im SUMMARY)
  </acceptance_criteria>
  <done>Migration fail-closed gegen Rest-Race (Pre-Check-STOP), löscht NULLs, erzwingt NOT NULL.</done>
</task>

<task type="auto">
  <name>Task 2: models.py intent_event.call_id nullable=False</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py:765 (call_id-Column)
  </read_first>
  <action>
    `IntentEvent.call_id`: `nullable=True` → `nullable=False`. FK/CASCADE/index/comment unverändert. Den COMMENT-Text (falls „Events ohne call_id moeglich" enthält) auf den neuen Stand ziehen (call_id ist jetzt Pflicht; Single-Source models.py == DB; falls eine COMMENT-Migration nötig wird, in dieselbe 00XX-Migration ein `op.execute("COMMENT ON COLUMN ...")` ergänzen — Schild-Pflicht Punkt 23 nur falls der Text sich ändert).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_schild_guard.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `IntentEvent.call_id` nullable=False (grep); FK/CASCADE unverändert
    - falls Schild-Text geändert: COMMENT-Migration == models.py (Single-Source, Punkt 23)
  </acceptance_criteria>
  <done>ORM konsistent zur NOT-NULL-DB; Schild aktuell falls berührt.</done>
</task>

<task type="auto">
  <name>Task 3: intent_event-TABELLEN-Schild korrigieren (veraltet — Schild-Aktualitäts-Pflicht, André 2026-06-25)</name>
  <files>database/models.py, alembic/versions/00XX_intent_event_call_id_not_null.py</files>
  <read_first>
    - database/models.py:776-778 (IntentEvent `__table_args__` `comment=`)
    - CLAUDE.md Punkt 23 → „Schild-AKTUALITÄTS-Pflicht" (Schild muss aktuell sein + ALLE Schreiber nennen)
  </read_first>
  <action>
    Das intent_event-TABELLEN-Schild (`__table_args__` `comment=`) ist VERALTET + UNVOLLSTÄNDIG (klassischer Aktualitäts-Verstoß, Punkt 23): (a) es nennt „Slow Lane reichert via separatem Score-Objekt an" — das war die VERWORFENE Fallback-Variante; real benotet die Slow Lane IN-PLACE (handling_score_numeric/handling_status, TAXO2-Plan-03, services/slow_lane.py:_persist_event_ref). (b) die Schreiber-Liste nennt nur einwand_keyword_matcher.py + claude_service.py — es FEHLEN services/deepgram_service.py (EWB-Knopf-Emit, ui_asserted) UND services/slow_lane.py (In-Place-Benotung). Korrigieren — **NUR die COMMENTs, kein Verhaltens-Change:**

    1. **models.py** `__table_args__` `comment=` neu:
       - „separatem Score-Objekt an (TAXO2)" → „**in-place an (handling_score_numeric/handling_status, TAXO2)**".
       - Schreiber-Liste: „Schreibt services/einwand_keyword_matcher.py (Keyword-Fast-Lane), services/claude_service.py (Medium+QA-Lane), **services/deepgram_service.py (EWB-Knopf-Emit)**, **services/slow_lane.py (In-Place-Benotung)**; liest TAXO2/TAXO3-Auswertung."
    2. **COMMENT-Migration** (Single-Source models.py == DB, Punkt 23): in DERSELBEN 00XX-Migration ein `op.execute("COMMENT ON TABLE intent_event IS '<neuer Text>'")` (Hausmuster, KEIN autogenerate). Da Task 1 ohnehin eine 00XX-Migration baut, hier nur das COMMENT-Statement ergänzen — kein zweites File.

    Reiner Schild/COMMENT-Fix, keine Spalten-/Constraint-/Daten-Änderung über Task 1+2 hinaus.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_schild_guard.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - models.py intent_event-Tabellen-Schild: „in-place" statt „separatem Score-Objekt"; Schreiber-Liste enthält deepgram_service.py + slow_lane.py (grep)
    - COMMENT-Migration == models.py-Text (Single-Source, Punkt 23)
    - `inspect.sh schilder intent_event` zeigt nach Deploy 2 den korrigierten Text (Beleg im SUMMARY)
  </acceptance_criteria>
  <done>intent_event-Schild aktuell + vollständig (alle 4 Emit-Schreiber + Slow-Lane-In-Place), kein irreführender „separates Score-Objekt"-Text mehr.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `intent_event` (SCHEMA: call_id NOT NULL + DELETE der NULL-Zeilen) — `inspect.sh schema intent_event` zeigt nach Deploy 2 `not null` (V-CI-7). Pre-Check-Beleg verbatim im SUMMARY.
### Cross-Layer-Konsistenz
| Datum | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| `intent_event.call_id` Constraint | 00XX SET NOT NULL | DB-Constraint | ✓ nach Deploy 2 |
| 54 NULL-Rows | DELETE WHERE call_id IS NULL | DB-Rows (gelöscht) | ✓ Pre-Check + count==0 |

<verification>
- SOAK V-CI-6 grün VOR dieser Welle (manual, prod).
- Pre-Check STOP bei frischer NULL (kein blindes NOT NULL).
- DELETE + SET NOT NULL; models.py nullable=False; Schild-Guard grün.
- `inspect.sh schema intent_event` → `not null` (V-CI-7).
- Task 3: intent_event-TABELLEN-Schild korrigiert (in-place statt „separates Score-Objekt"; Schreiber-Liste +deepgram_service.py +slow_lane.py); COMMENT-Migration == models.py (Punkt 23 Aktualitäts-Pflicht). `inspect.sh schilder intent_event` zeigt den neuen Text.
</verification>

<success_criteria>
- call_id NOT NULL erzwungen NACH bewiesener Race-Schließung; 54 NULL-Testzeilen gelöscht.
- Pre-Check verhindert NOT NULL bei offenem Race (kein Live-INSERT-Bruch).
- Defensiver Backstop bleibt primär; NOT NULL = Belt-and-Suspenders.
- VALIDATION V-CI-7 nach grünem Deploy-2-Gate auf green.
</success_criteria>

<output>
`.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-04-SUMMARY.md` (Pre-Check-Beleg + head-Beleg + count==0).
</output>
