---
phase: 08.23.2.CALLID
plan: 04
status: complete
result: built-pending-deploy2-gate
requirements: [CI-3]
deploy: 2
migration: "0025"
down_revision: "0024"
---

# Plan 04 (Deploy 2 — call_id NOT NULL + 54 löschen + Schild-Fix) — BUILT (pending supervised Deploy 2)

> Acceptance = der `deploy.sh production`-Pytest-Gate (kein Local-Dev). Code committet + gepusht;
> **Claudian fährt den beaufsichtigten Deploy 2** (Migration als postgres VOR Restart + Gate).
> Multi-Segment-Gotcha: Pfade hardcoded, gsd-verifier/gsd-sdk/gsd-code-review umgangen, SUMMARY/STATE/
> ROADMAP/VALIDATION hand-editiert.

## Voraussetzung erfüllt (Soak GRÜN)

Deploy 1 (Plan 01-03) live seit 2026-06-25. Soak 2026-06-26 (Claudian, 3 echte Test-Anrufe über alle
feuernden Pfade — Knopf + Medium-Lane + Stichwort-Erkenner): **alle Events MIT call_id, 0 neue NULL,
0 `[CALLID-ALARM]`**. V-CI-6 grün → NOT NULL ist abgesichert.

## Was gebaut wurde (CI-3)

**`alembic/versions/0025_intent_event_call_id_not_null.py`** (revision=`0025`, down_revision=`0024`).
`upgrade()` in GELOCKTER Reihenfolge:
1. **PRE-CHECK (BLOCKIEREND):** `SELECT count(*) FROM intent_event WHERE call_id IS NULL AND handling_status <> 'failed'`. > 0 → `raise RuntimeError` (frische NULL = Race nicht geschlossen → STOP, NICHTS ändern, zurück zu Plan 02 + Soak).
2. **DELETE** `WHERE call_id IS NULL` (die ~54 'failed'+NULL Pre-Launch-Testzeilen; idempotent, scoped).
3. **`op.alter_column('intent_event','call_id', nullable=False, ...)`** (SET NOT NULL — DB-Wächter, Belt-and-Suspenders; Backstop Plan 03 bleibt primär).
4. **Schild-Fold** (4): `COMMENT ON COLUMN intent_event.call_id` (NOT-NULL-Stand) + `COMMENT ON TABLE intent_event` (in-place-Korrektur, s.u.).

`downgrade()`: `call_id` zurück auf nullable; DELETE bewusst NICHT reversibel (AUDIT-NOTE).

**`database/models.py`**: `IntentEvent.call_id` `nullable=True` → `nullable=False` + Spalten-Comment auf NOT-NULL-Stand; `__table_args__`-Tabellen-Comment korrigiert (Single-Source == COMMENT-Migration).

## Schild-Fix (gefaltet, CLAUDE.md Punkt 23 Aktualitäts-Pflicht)

Das intent_event-Tabellen-Schild war veraltet + unvollständig. Korrigiert (NUR COMMENT, kein Verhalten):
- „Slow Lane reichert via **separatem Score-Objekt** an" → „Slow Lane reichert **IN-PLACE** an (handling_score_numeric/handling_status, TAXO2)" (separates Score-Objekt war die verworfene Variante).
- Schreiber-Liste ergänzt: + `services/deepgram_service.py` (EWB-Knopf-Emit) + `services/slow_lane.py` (In-Place-Benotung); vorher nur einwand_keyword_matcher + claude_service.

## Stale-Test-Anpassung (Pflicht — Contract-Change NOT NULL)

NOT NULL auf `intent_event.call_id` ist ein Contract-Change: jeder Test, der eine intent_event-Zeile
mit NULL/fehlender call_id INSERTet, bräche am Post-`0025`-Gate. Triage (grep `call_id=None` / `IntentEvent(` /
`emit_intent_event(` über tests/) → **6 Tests in 5 Dateien** an die NOT-NULL-Realität gezogen, OHNE Assertion
aufzuweichen:
- `tests/test_intent_event_writer.py` — 5 inserting Tests: `_seed_call`-Helfer + echte `call_id=cid` statt `None` (+ call_id-Assertion in Test 1); cleanup um `public.calls` ergänzt. Test 3 (reject) bleibt `call_id=None` (raised VOR Insert).
- `tests/test_callid_threading.py::test_emit_alarm_on_none_call_id_fails_closed` (umbenannt) — Premise invertiert: unter NOT NULL → Alarm bleibt sichtbar (vor INSERT), INSERT fail-closed → `emit` gibt **-1** (Edge 3, kein raise), KEINE Row.
- `tests/test_slowlane_callid_defensive.py::test_backstop_unresolvable_tenant_sets_failed_no_abstain_log` (umbenannt) — Backstop jetzt über eine **gültige call_id mit `calls.tenant_id=NULL`** getriggert (statt call_id=NULL) → `_tenant_id_for`→None → identischer fail-closed-Schutz ('failed' + Alarm, kein abstain_log).
- `tests/test_abstain_log_rls.py::_seed_intent_event` — seedet jetzt eine calls-Row + setzt `call_id`; cleanup um `public.calls` ergänzt.
- `tests/test_medium_lane_intent_event_live.py` — `_seed_call` + `state['call_id']=cid` → der Dispatch reicht eine gültige call_id durch; Assertion `row.call_id == cid`; cleanup um `public.calls`.

py_compile aller geänderten Dateien (Migration + models.py + 5 Test-Dateien) lokal OK (reiner Parse, kein Acceptance).

## Pre-Execute (BLOCKIEREND, Claudian am Deploy)

- **SCHRITT 0:** `inspect.sh migrations` → head == `0024` unmittelbar vor dem Lauf. Abweichung → STOP, `down_revision` neu ausrichten.
- Soak V-CI-6 grün (erfüllt 2026-06-26).
- Migration als postgres VOR Restart (create_all-Falle).

## Persistenz-Schicht-Verifikation (Punkt 21)

- `intent_event` (SCHEMA: call_id NOT NULL + DELETE NULL-Zeilen) — `inspect.sh schema intent_event` zeigt nach Deploy 2 `not null` (V-CI-7); `inspect.sh schilder intent_event` zeigt den korrigierten Schild-Text. Pre-Check-Beleg + head-Beleg + count==0 verbatim ins Deploy-Log.

## Status

🔨 built — pending `deploy.sh production`-Gate (Deploy 2). V-CI-7 geht nach grünem Gate auf green.
