---
phase: 08.23.2.CALLID
plan: 01
type: execute
wave: 1
complexity: "🔴"
depends_on: []
deploy: 1
files_modified:
  - services/live_session.py
  - services/intent_event_writer.py
  - services/claude_service.py
  - services/deepgram_service.py
  - services/einwand_keyword_matcher.py
  - tests/test_callid_threading.py
autonomous: true
requirements: [CI-1]
user_setup: []

must_haves:
  truths:
    - "Ein neuer Helfer resolve_call_id_for_sid(sid) liefert die durable call_id-UUID aus dem per-SID-Zustand; bei None ODER '__call_pending__' gibt er None zurück (NIEMALS den Sentinel-String)"
    - "emit_intent_event hat KEINEN call_id=None-Default mehr — call_id ist Pflicht-Param (keyword-only)"
    - "Alle 4 Produktiv-Aufrufer lesen call_id DIREKT aus dem bereits gehaltenen state (state['call_id'] durch den reinen Sentinel-Guard _durable_call_id) und übergeben es explizit an emit — KEIN re-sperrender Getter-Aufruf in der gehaltenen Sperre (Deadlock-Falle, plain Lock). resolve_call_id_for_sid ist NUR für Kontexte OHNE gehaltene Sperre"
    - "Erreicht call_id ausnahmsweise None (Race vor Plan 02), schreibt emit weiterhin (call_id NULL) UND loggt einen LAUTEN Alarm — kein raise im Live-Emit-Pfad (Latenz/Verfügbarkeit), Sichtbarkeit statt stiller Verlust"
  artifacts:
    - path: "services/live_session.py"
      provides: "_durable_call_id(raw) — reiner Sentinel-/None-Guard (Single-Source der 'nie Sentinel'-Regel) + resolve_call_id_for_sid(sid) — Locking-Getter NUR für unlocked-Kontexte"
      contains: "def _durable_call_id"
    - path: "services/intent_event_writer.py"
      provides: "emit_intent_event ohne call_id-Default (Pflicht-Param) + lauter Alarm bei None"
      contains: "call_id"
  key_links:
    - from: "services/claude_service.py:1065 + :1599 / deepgram_service.py:877 / einwand_keyword_matcher.py:315"
      to: "state['call_id'] (bereits im gehaltenen _session_state_lock-Block gelesen) → emit_intent_event(call_id=...)"
      via: "call_id direkt aus dem schon-gehaltenen state lesen via reinem Guard _durable_call_id(state.get('call_id')) — KEIN re-sperrender Getter in der Sperre"
      pattern: "_durable_call_id\\("
---

<objective>
Reicht `call_id` durch den Live-Emit-Pfad (CI-1). Heute schreibt `emit_intent_event` `call_id` verbatim (intent_event_writer.py:122), aber alle 4 Aufrufer übergeben ihn nicht → jede Prod-Zeile NULL (RESEARCH §1). Diese Welle: (1) Single-Source-Helfer `resolve_call_id_for_sid(sid)` in live_session.py (liest `_session_state[sid]['state']['call_id']`, behandelt `None`/`'__call_pending__'` als nicht-durable → None); (2) `emit_intent_event` macht `call_id` zum Pflicht-Param (Default `=None` entfernt); (3) die 4 Aufrufer lesen `call_id` DIREKT aus dem bereits gehaltenen `state` (`state['call_id']` durch den reinen Sentinel-Guard `_durable_call_id`) und übergeben es an `emit_intent_event` — sie rufen NICHT den re-sperrenden Getter `resolve_call_id_for_sid` innerhalb der gehaltenen Sperre auf (Deadlock, plain Lock).

Locked: NIEMALS den Sentinel-String `'__call_pending__'` als call_id durchreichen (landete sonst in der UUID-Spalte). Race-Schließung selbst = Plan 02 (Ordering-Gate); diese Welle stellt nur die Naht her + macht eine Rest-None SICHTBAR (lauter Alarm, kein raise — Live-Pfad bricht nicht, Punkt 25).

Output: Helfer + Pflicht-Param + 4 Call-Sites + Tests. call_id-Spalte bleibt NULLABLE (Deploy 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
PRE-EXECUTE: kein Local-Dev (Verify server-seitig deploy.sh-Gate). Multi-Segment-Gotcha: Pfade hardcoden, gsd-tools/gsd-sdk/gsd-code-review/gsd-verifier umgehen, SUMMARY/STATE/ROADMAP hand-editieren.
</execution_context>

<context>
@.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-RESEARCH.md

<interfaces>
emit_intent_event-Signatur (services/intent_event_writer.py:32-52, keyword-only): Pflicht heute session_id/mode/source/speaker_role (Validierung :67-74); `call_id=None` :44; call_id-Write :122 verbatim. Writer fasst KEINEN per-SID-Zustand/g an (RESEARCH §1).
create_call_for_sid-State (live_session.py): `_session_state[sid]['state']['call_id']` — `None` (init :368), `'__call_pending__'` (Sentinel :557), durable UUID (:586). `_session_state_lock` :211 (plain Lock, NIE mit state_lock nesten — :464). Bestehende Inline-Leser: live_session.py:546, deepgram_service.py:707/:1017.
4 Aufrufer (alle halten `_session_state_lock` + lesen `state` im selben Block — RESEARCH §1): claude_service.py:1065 (sid; locked read :1030-1041), claude_service.py:1599 (sid; :1573-1581), deepgram_service.py:877 (_sid; :853-859), einwand_keyword_matcher.py:315 (sid; :285-293).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _durable_call_id (reiner Guard) + resolve_call_id_for_sid (Locking-Getter, unlocked-Kontexte) + Test</name>
  <files>services/live_session.py, tests/test_callid_threading.py</files>
  <read_first>
    - services/live_session.py:205-215 (_session_state + _session_state_lock) + :529-590 (create_call_for_sid, Sentinel/durable-Write) + :546 (bestehender Inline-Read als Muster)
    - RESEARCH §2 (kein Getter existiert; Sentinel-Falle)
  </read_first>
  <action>
    Zwei Helfer in services/live_session.py (neben den anderen per-SID-Gettern):
    ```python
    def _durable_call_id(raw):
        """Reiner Sentinel-/None-Guard (KEIN Lock): durable UUID-str bleibt, None/'__call_pending__' → None.
        NIEMALS den Sentinel-String zurückgeben (CI-1) — er würde sonst in eine UUID-Spalte geschrieben.
        Single-Source der 'nie Sentinel'-Regel: genutzt von BEIDEN — dem gesperrten Getter UND den 4
        emit-Aufrufern, die call_id direkt aus dem schon-gehaltenen state lesen (kein 4× dupliziertes Guard)."""
        return raw if (raw and raw != '__call_pending__') else None

    def resolve_call_id_for_sid(sid):
        """Locking-Getter NUR für Kontexte OHNE gehaltene _session_state_lock-Sperre (Backfill/unlocked).
        Die 4 Live-emit-Aufrufer rufen DIESEN Getter NICHT (sie halten die Sperre schon → Re-Lock auf den
        plain Lock = Deadlock) — sie lesen state['call_id'] direkt und wenden _durable_call_id an.
        Race-Schließung (call_id durable VOR Detection) ist Plan 02."""
        with _session_state_lock:
            cid = _session_state.get(sid, {}).get('state', {}).get('call_id')
        return _durable_call_id(cid)
    ```
    Test tests/test_callid_threading.py: (a) `_durable_call_id` (reine Funktion, kein Setup): durable UUID → UUID; `'__call_pending__'` → None; None/leer → None. (b) `resolve_call_id_for_sid`: durable UUID im State gesetzt → UUID; Sentinel `'__call_pending__'` → None; leerer/unbekannter sid → None. State-Mutation echt (kein Source-Presence). Skip-frei (reiner RAM-State, kein DSN nötig) ODER mit minimalem _session_state-Setup.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_callid_threading.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `services/live_session.py` enthält `def _durable_call_id(raw):` (reiner Guard) UND `def resolve_call_id_for_sid(sid):` (Locking-Getter, auf _durable_call_id aufgebaut)
    - `_durable_call_id` gibt None für `'__call_pending__'` UND None/leer; durable UUID bleibt (Test, reine Funktion)
    - Getter gibt None für `'__call_pending__'` UND für leer/unbekannt (grep + Test)
    - resolve_call_id_for_sid-Docstring: NUR für unlocked-Kontexte (nicht die 4 gesperrten Live-Sites)
  </acceptance_criteria>
  <done>Reiner Guard + Locking-Getter liefern durable call_id, nie den Sentinel; getestet. Getter nur für unlocked-Kontexte.</done>
</task>

<task type="auto">
  <name>Task 2: emit_intent_event call_id Pflicht-Param + lauter Alarm bei None</name>
  <files>services/intent_event_writer.py</files>
  <read_first>
    - services/intent_event_writer.py:32-74 (Signatur + Validierungs-Block) + :110-146 (Insert + call_id :122 + slow_lane.put :146)
    - RESEARCH §0 (Backstop: Sichtbarkeit statt raise im Live-Pfad) + §8
  </read_first>
  <action>
    1. `call_id=None`-Default (:44) ENTFERNEN → `call_id` wird Pflicht-Keyword-Param (in den `*`-Block ohne Default). 
    2. KEIN harter `raise` bei None (Live-Emit-Pfad — ein raise bräche Detection/Latenz, Punkt 25). Stattdessen: erreicht `call_id` None (Rest-Race vor Plan 02 ODER Regression danach), einen LAUTEN Alarm loggen (z.B. `print("[CALLID-ALARM] emit_intent_event mit call_id=None — Race/Regression, intent_event wird NULL geschrieben, Slow-Lane markiert 'failed'")`) und den Emit weiterlaufen lassen (call_id NULL). So bleibt der Vorfall SICHTBAR + der defensive Backstop (Plan 03) fängt ihn downstream, ohne den Live-Pfad zu brechen.
    Kein weiterer Verhaltens-Change am Writer.
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_callid_threading.py tests/test_intent_event_writer.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - `emit_intent_event`-Signatur hat KEIN `call_id=None` mehr (grep: kein `call_id=None` in der def)
    - None-call_id → lauter `[CALLID-ALARM]`-Log + Emit läuft (kein raise) — Test belegt Log + geschriebene Row
    - bestehende test_intent_event_writer.py angepasst (Aufrufe übergeben call_id) und grün
  </acceptance_criteria>
  <done>call_id ist Pflicht-Param; None ist sichtbar (Alarm) statt still; Live-Pfad bricht nicht.</done>
</task>

<task type="auto">
  <name>Task 3: 4 Aufrufer lesen call_id direkt aus dem gehaltenen state (_durable_call_id), kein Getter in der Sperre</name>
  <files>services/claude_service.py, services/deepgram_service.py, services/einwand_keyword_matcher.py, tests/test_callid_threading.py</files>
  <read_first>
    - services/claude_service.py:1030-1074 (Caller A) + :1573-1608 (Caller B)
    - services/deepgram_service.py:853-883 (Caller C, _sid)
    - services/einwand_keyword_matcher.py:285-322 (Caller D)
    - RESEARCH §1 (alle 4 halten _session_state_lock + lesen state im selben Block)
  </read_first>
  <action>
    In JEDEM der 4 Aufrufer im bereits gehaltenen `_session_state_lock`-Block (wo `state` schon gelesen wird) die durable call_id in eine lokale Variable lesen — `_cid = _durable_call_id(state.get('call_id'))` — und `call_id=_cid` an den `emit_intent_event(...)`-Aufruf übergeben. `_durable_call_id` ist REIN (kein Lock) → im gehaltenen Block sicher; das Lesen aus demselben schon-gehaltenen `state` ist atomar (kein TOCTOU, kein zweiter Lock). Den re-sperrenden Getter `resolve_call_id_for_sid` an diesen 4 Sites NICHT aufrufen (Re-Lock auf den plain `_session_state_lock` → Deadlock). Caller C liest den `_sid`-state, A/B/D den `sid`-state; alle 4 haben `state` im Lock-Block in scope (RESEARCH §1 / interfaces). import `_durable_call_id` aus live_session (claude_service/einwand_keyword_matcher haben `ls`/`_ls`-Alias; deepgram_service `ls`).
    Test ergänzen: ein emit-Pfad mit gesetztem durable call_id → die geschriebene intent_event-Row trägt die call_id (server-seitig gegen PG, db_session). Mind. 1 echter Caller-Pfad integrationsgetestet (kein reiner Mock).
  </action>
  <verify>
    <automated>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && source venv/bin/activate && python -m pytest tests/test_callid_threading.py -x -v'</automated>
  </verify>
  <acceptance_criteria>
    - 4 Call-Sites lesen call_id via `_durable_call_id(state.get('call_id'))` aus dem gehaltenen state (grep `_durable_call_id(` == 4 in den 3 Dateien)
    - KEIN `resolve_call_id_for_sid(`-Aufruf an den 4 Live-Sites (grep == 0 dort) → kein Re-Lock auf den plain `_session_state_lock`, kein Deadlock
    - Integration: emit mit durable call_id → intent_event.call_id gesetzt (echte Row)
  </acceptance_criteria>
  <done>Alle 4 Live-Emit-Pfade lesen die durable call_id direkt aus dem gehaltenen state (kein Getter-Re-Lock); integrationsbelegt.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (Punkt 21)
### Angefasste Tabellen
- `intent_event` (SCHREIBEN: call_id beim Emit) — Spalte nullable bleibt (Deploy 1), Schema-unverändert. inspect.sh schema intent_event beim Execute ziehen (RESEARCH §6).
- `_session_state` (RAM, kein DB) — call_id-Quelle.
### Cross-Layer-Konsistenz
| Datum | Pfad | Schicht | Verifiziert? |
|---|---|---|---|
| `call_id` (= _durable_call_id(state['call_id'])) | 4 emit-Aufrufer (gehaltener state-Read) → emit_intent_event:122 | DB `intent_event.call_id` (UUID, nullable, FK calls.id CASCADE) | ✓ models.py:765 / 0016 |
| `state['call_id']` | 4 Aufrufer direkt im gehaltenen Lock-Block (+ resolve_call_id_for_sid für unlocked) | RAM `_session_state[sid]['state']['call_id']` (NICHT DB) | ✓ live_session.py:557/586 — Sentinel-Falle via _durable_call_id |

<verification>
- _durable_call_id: durable→UUID, Sentinel/None/leer→None (reine Funktion). resolve_call_id_for_sid (unlocked-Getter): dito gegen State (V-CI-2).
- emit_intent_event: kein call_id-Default; None→Alarm+Emit (kein raise) (V-CI-1).
- 4 Call-Sites lesen via _durable_call_id aus gehaltenem state (grep _durable_call_id==4); KEIN resolve_call_id_for_sid an den Live-Sites (kein Re-Lock/Deadlock); Integration emit→call_id gesetzt.
</verification>

<success_criteria>
- call_id-Naht hergestellt (Helfer + Pflicht-Param + 4 Aufrufer). Sentinel nie durchgereicht.
- Rest-None sichtbar (Alarm), Live-Pfad bricht nicht (Punkt 25).
- Plan 02 (Race-Close) baut darauf auf; call_id-Spalte bleibt nullable (Deploy 1).
- VALIDATION V-CI-1 + V-CI-2 nach grünem Gate auf green.
</success_criteria>

<output>
`.planning/phases/08.23.2.CALLID-intent-event-call-id-durchreichen-integritaet/08.23.2.CALLID-01-SUMMARY.md`.
</output>
