---
phase: 08.23.2.CALLID
slug: intent-event-call-id-durchreichen-integritaet
status: draft
nyquist_compliant: false
created: 2026-06-25
---

# Phase 08.23.2.CALLID — Validation Strategy

> HART: Tests laufen server-seitig gegen real Postgres `nerve_test` via `deploy.sh production`-Gate — KEIN lokales pytest als Acceptance. Skip ohne `TEST_DATABASE_URL` (kein SQLite-False-Green). 2-Deploy-Phasing (CI-3): Deploy 1 = Code-Fix, SOAK-Gate, Deploy 2 = NOT NULL + 54-Löschung.

## Test Infrastructure

| Property | Value |
|----------|-------|
| Framework | pytest (server-seitig nerve_test, PGTEST-Gate) |
| Config | `tests/conftest.py` (TEST_TENANT_UUID, `cleanup_rows`, `db_session` mit GUC) |
| Quick run | `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && <pytest target>'` |
| Full gate | `bash deploy.sh production` (Pytest-Gate VOR Restart; rot → kein Restart) |

## Per-Claim Verification Map (falsifiable)

| ID | Claim | Plan | Test / Proof | Status |
|----|-------|------|--------------|--------|
| V-CI-1 | emit_intent_event verlangt call_id (Default entfernt); alle 4 Aufrufer lesen call_id DIREKT aus dem gehaltenen state via `_durable_call_id(state['call_id'])` (kein Getter-Re-Lock in der Sperre) | 01 | grep: 4 Call-Sites mit `_durable_call_id(`, 0× `resolve_call_id_for_sid(` an den Live-Sites; `emit_intent_event`-Signatur ohne `call_id=None`-Default; server pytest emit-roundtrip trägt call_id | 🔨 built (grep ✓, pending deploy-gate) |
| V-CI-2 | `_durable_call_id(raw)` (reiner Guard) + `resolve_call_id_for_sid(sid)` (Locking-Getter, unlocked-Kontexte) → durable UUID; `None`/`'__call_pending__'` → None (nie Sentinel-String) | 01 | unit: _durable_call_id reine Funktion (durable/Sentinel/None); integration: gesetzter State → UUID; pending-Sentinel → None; leer → None | 🔨 built (pending deploy-gate) |
| V-CI-3 | Race geschlossen — call_id durable BEVOR Detection für die sid feuern kann (Ordering-Gate, Single-Owner) | 02 | integration: Race nachgestellt → kein NULL-call_id-Emit; grep: create_call_for_sid NICHT im Detection-Thread; Reorder/Buffer-Beleg | 🔨 built (Mechanik=REORDER, pending deploy-gate) |
| V-CI-4 | Defensiver Backstop: call_id/Tenant nicht ermittelbar → `handling_status='failed'`, KEIN Endlos-Re-Queue, KEIN abstain_log, LAUTER Alarm | 03 | integration: Event ohne call_id → 'failed' + Alarm-Log; H-3 re-queued es NICHT erneut | 🔨 built (pending deploy-gate) |
| V-CI-5 | `flush_to_db` setzt Tenant-GUC (A1-Klammer) — abstain_log mit gültiger call_id wird beim Flush geschrieben, nicht fail-closed | 03 | integration: SIGTERM/atexit-Flush-Pfad schreibt abstain_log mit GUC; grep: set_current_tenant in flush_to_db | 🔨 built (pending deploy-gate) |
| V-CI-6 | SOAK (Deploy-1-Gate): 0 NEUE intent_event-Zeilen mit call_id=NULL über ALLE 4 emit-Pfade; kein Backstop-Alarm | — (manual) | Prod: `SELECT count(*) FROM intent_event WHERE call_id IS NULL AND timestamp > <deploy1>` == 0; `inspect.sh logs` ohne Backstop-Alarm | ✅ green (Claudian, 3 Test-Anrufe 2026-06-26: 0 NULL, 0 [CALLID-ALARM]) |
| V-CI-7 | Deploy 2: 54 'failed'-NULL-Zeilen gelöscht + `intent_event.call_id` NOT NULL; Migration pre-checkt 0 NULL übrig | 04 | Migration: Pre-Check `count(NULL)` & alle 'failed' → DELETE → SET NOT NULL; `inspect.sh schema intent_event` zeigt `not null` | 🔨 built (Migration 0025, pending Deploy-2-gate) |

*Status: ⬜ pending · 🔨 built (pending deploy-gate) · ✅ green · ❌ red*

## Manual-Only / Supervised

| Behavior | Why Manual | Instructions |
|----------|-----------|--------------|
| SOAK-Gate (V-CI-6) | Real-Daten, prod-only, mehrere Test-Anrufe über 4 Lanes | Nach Deploy 1: Test-Anrufe (Keyword/Medium/Button/abstain) → `inspect.sh` count NULL-seit-Deploy == 0 + keine Backstop-Alarme. Grün → Deploy 2 freigeben. |
| Deploy-2-Migration (V-CI-7) | prod Schema-Change (NOT NULL), hard-to-reverse, create_all-Falle | Pre-Check 0 NULL + alle 'failed' (sonst STOP, Race offen) → DELETE → SET NOT NULL als postgres VOR Restart. |

## Wave 0 Requirements
- [ ] `_durable_call_id` (reine Funktion) + `resolve_call_id_for_sid`-Test (durable/pending/leer) — Plan 01
- [ ] emit-roundtrip-Test mit call_id — Plan 01
- [ ] Race-Close-Integration (kein NULL-Emit im Start-Fenster) — Plan 02
- [ ] Defensiver-Backstop-Test (failed + Alarm, kein Re-Queue, kein abstain_log) — Plan 03
- [ ] flush_to_db-GUC-Test (abstain_log beim Flush) — Plan 03

## Sign-Off
- [ ] Alle Tasks haben server-seitigen `<automated>` verify oder Wave-0-Dependency
- [ ] Race-Close beweist KEIN NULL-Emit (nicht nur „Helper existiert")
- [ ] Backstop-Test beweist failed-statt-Endlos-Loop
- [ ] SOAK-Gate-Kriterium falsifizierbar (count == 0) vor Deploy 2
- [ ] `nyquist_compliant: true` nach Plan-Checker

**Approval:** pending (🔴 Cross-AI Gemini-3-Sichten + Claudian Pre-Execute-Audit vor Execute).
