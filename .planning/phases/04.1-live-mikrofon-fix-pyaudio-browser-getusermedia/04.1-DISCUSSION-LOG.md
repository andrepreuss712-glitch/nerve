# Phase 4.1: Live-Mikrofon Fix — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-02
**Phase:** 04.1-live-mikrofon-fix-pyaudio-browser-getusermedia
**Areas discussed:** Audio Pipeline, Session Scoping, Diarization Strategy, Cold-Call vs Meeting Mode

---

## Audio Pipeline

| Option | Description | Selected |
|--------|-------------|----------|
| AudioWorkletNode → PCM16 | getUserMedia → AudioContext → AudioWorklet → Int16Array → socket.emit | ✓ (Claude's recommendation, user accepted) |
| MediaRecorder → WebM chunks | Like training mode but streaming — variable chunk size, Deepgram streaming compatibility concerns | |
| ScriptProcessorNode → PCM16 | Deprecated browser API | |

**User's choice:** "Nimm deine Empfehlung" (take your recommendation) → AudioWorkletNode → PCM16
**Notes:** Keeps existing Deepgram config (linear16, 16000Hz). New file: static/audio-processor.js (~15 lines).

---

## Session Scoping

| Option | Description | Selected |
|--------|-------------|----------|
| Per-Socket.IO session | Each sid gets own Deepgram connection, opens on start_live_session, closes on disconnect | ✓ |
| Global connection (current) | One server-wide Deepgram connection opened at startup — broken for multi-user | |

**User's choice:** Per-user Deepgram connection
**Notes:** Remove global deepgram_starten() call from app.py:683-686. Server maintains dict keyed by sid.

---

## Diarization Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Keep diarization | Works for phone/in-person calls where both voices are audible to mic | ✓ |
| Disable diarization | Simpler, single speaker only — for online call scenarios | |

**User's choice:** Keep diarization
**Notes:** v1 user scenario: classic phone call, headset on ear, phone on speakerphone next to computer. Computer mic captures both Berater (Speaker 0) and Kunde (Speaker 1). Existing speaker mapping stays unchanged.

---

## Cold-Call vs Meeting Mode

| Option | Description | Selected |
|--------|-------------|----------|
| Technical fix only | Just get getUserMedia + streaming working on VPS. Mode UX stays in Phase 4. | ✓ |
| Include mode selection UI | Implement Cold-Call vs Meeting Mode UX as part of this phase | |

**User's choice:** Technical fix only — scope klein halten (keep scope minimal)
**Notes:** DSGVO banner (Phase 4 LEGAL-01), Cold-Call vs Meeting Mode selection (Phase 4 D-22) stay in Phase 4. Existing DSGVO banner from Phase 2 remains as-is.

---

## Claude's Discretion

- Socket event names (audio_chunk, start_live_session, stop_live_session)
- Exact chunk size / emit interval
- Thread safety approach for per-session dict
- Whether to handle pause by stopping mic stream or ignoring server-side

## Deferred Ideas

- Cold-Call/Meeting Mode UX → Phase 4
- Single-speaker mode for Teams/Zoom → future toggle
