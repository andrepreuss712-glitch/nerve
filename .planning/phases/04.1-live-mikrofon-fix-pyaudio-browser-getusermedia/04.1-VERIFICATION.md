---
phase: 04.1-live-mikrofon-fix-pyaudio-browser-getusermedia
verified: 2026-04-02T19:30:00Z
status: human_needed
score: 3/4 must-haves verified
re_verification: false
human_verification:
  - test: "Confirm live transcription works end-to-end on getnerve.app after Plan 03 AudioContext fix"
    expected: "Speak into browser mic → transcripts appear in UI within 1-2 seconds. Browser DevTools console shows '[Mic] AudioContext resumed, state: running' and '[Mic] Erster audio_chunk gesendet, bytes: 3200'. Server logs show '[DG] start_live_session received', '[DG] Verbunden', '[DG] audio_chunk received'."
    why_human: "The previous UAT (2026-04-02) confirmed live mic was broken — no transcript appeared. Plan 03 code-fixed the root cause (AudioContext suspension via audioCtx.resume()) and committed it (commit 723d746), but the human-verify checkpoint in Plan 03 Task 2 was auto-approved (auto_advance=true) — no real human test was done on the AudioContext fix. MIC-04 requires observed end-to-end behavior on the live VPS."
  - test: "Confirm training page does NOT show mic permission prompt on page load after Plan 03 fix"
    expected: "Navigate to getnerve.app/training — no browser mic permission dialog appears on page load. Mic is only requested when pressing the PTT button inside a training session."
    why_human: "Gap 2 from the original UAT (training mic side-effect) was fixed by adding a window.location.pathname === '/live' guard. This guard is present in code but requires visual confirmation on the live VPS because the training page has its own getUserMedia call triggered by user gesture — a page load test cannot be automated here."
---

# Phase 4.1: Live-Mikrofon Fix — Verification Report

**Phase Goal:** Replace server-side PyAudio mic capture with browser-side getUserMedia + Socket.IO streaming so live transcription works on the VPS (no audio hardware)
**Verified:** 2026-04-02T19:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Server starts without PyAudio — no `import pyaudio` in production code | VERIFIED | No `pyaudio` import found in `services/deepgram_service.py` or `app.py`. `deepgram_starten` fully removed. `register_audio_handlers(socketio)` call confirmed in `app.py` line 686. |
| 2 | Each Socket.IO session gets its own Deepgram WebSocket connection (per-session, not global) | VERIFIED | `_deepgram_sessions = {}` dict present in `deepgram_service.py` line 9. `_open_deepgram_connection(sid)` creates per-sid connection. `_close_deepgram_connection(sid)` cleans up. All 4 Socket.IO handlers registered: `start_live_session`, `stop_live_session`, `audio_chunk`, `disconnect`. |
| 3 | Browser captures mic audio via getUserMedia + AudioWorklet at 16kHz, streams Int16 PCM via Socket.IO | VERIFIED | `startMicStream()` confirmed in `static/app.js` lines 17-53. AudioContext at 16kHz, `addModule('/static/audio-processor.js')`, `AudioWorkletNode`, `socket.emit('audio_chunk', e.data)`, `socket.emit('start_live_session')` all present. AudioContext resume fix (commit 723d746) present at lines 24-27. |
| 4 | Live transcription works end-to-end on getnerve.app: speak into browser mic, see transcripts in UI | ? NEEDS HUMAN | Code path is complete and the AudioContext fix is committed. However the human-verify checkpoint in Plan 03 Task 2 was auto-approved — no real human test was run after the root-cause fix. Previous UAT (2026-04-02) confirmed transcription was broken before the fix. End-to-end behavior on VPS requires human verification. |

**Score: 3/4 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `services/deepgram_service.py` | Per-session Deepgram connection management | VERIFIED | 193 lines. Contains `_deepgram_sessions`, `register_audio_handlers`, `_open_deepgram_connection`, `_close_deepgram_connection`, `_make_on_message`, all 4 Socket.IO event handlers, `room=sid` on all transcript emits (3 occurrences). No `pyaudio` import. No `deepgram_starten`. |
| `static/audio-processor.js` | AudioWorklet processor module — Float32 to Int16 conversion | VERIFIED | 27 lines. Contains `AudioWorkletProcessor`, `registerProcessor('audio-processor', AudioProcessor)`, `Int16Array`, `this.port.postMessage`, flush threshold `1600`, conversion constants `0x8000` and `0x7FFF`. |
| `static/app.js` | getUserMedia + AudioWorklet setup + Socket.IO audio streaming | VERIFIED | 872 lines. Contains `startMicStream()`, `stopMicStream()`, `getUserMedia`, `AudioContext({ sampleRate: 16000 })`, `audioCtx.resume()`, `addModule('/static/audio-processor.js')`, `AudioWorkletNode`, `socket.emit('audio_chunk')`, `socket.emit('start_live_session')`, `socket.emit('stop_live_session')`, `window.location.pathname === '/live'` guard. Existing `socket.on('transcript')`, `socket.on('coaching')`, `beenden()`, `togglePause()` all preserved. |
| `app.py` | Server startup without `deepgram_starten()` | VERIFIED | Line 683: `from services.deepgram_service import register_audio_handlers`. Line 686: `register_audio_handlers(socketio)`. No `deepgram_starten` reference anywhere. `analyse_loop` and `coaching_loop` threads preserved at lines 687-688. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `services/deepgram_service.py` | `extensions.socketio` | Socket.IO event handlers `start_live_session`, `stop_live_session`, `audio_chunk`, `disconnect` | WIRED | `register_audio_handlers(sio)` registers all 4 handlers on the passed socketio instance. Called in `app.py` line 686 with the live `socketio` object. |
| `services/deepgram_service.py` | `services/live_session.py` | `ls.pause_lock`, `ls.is_paused`, `ls.stabilize_speaker`, `ls._flush_segment`, `ls._merge_lock`, `ls._sp2_lock`, `ls.conversation_log` | WIRED | All live_session state references confirmed present at lines 35-36, 40, 45, 80, 94. |
| `static/app.js` | `static/audio-processor.js` | `audioCtx.audioWorklet.addModule('/static/audio-processor.js')` | WIRED | Line 28 in `app.js`. File exists at `static/audio-processor.js`. |
| `static/app.js` | Socket.IO server | `socket.emit('audio_chunk', e.data)` + `socket.emit('start_live_session')` + `socket.emit('stop_live_session')` | WIRED | `audio_chunk` at line 34, `start_live_session` at line 44, `stop_live_session` at line 58. |
| `static/app.js` | `stopMicStream()` in `beenden()` | `stopMicStream()` called before `/api/beenden` fetch | WIRED | Line 356: `stopMicStream();` confirmed before `fetch('/api/beenden')` at line 360. |
| `static/app.js` | `stopMicStream()` on disconnect | `socket.on('disconnect')` handler | WIRED | Lines 281-283: `socket.on('disconnect', () => { stopMicStream(); });` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `services/deepgram_service.py` `_make_on_message` | `text` from Deepgram `result.channel.alternatives[0].transcript` | Deepgram WebSocket live transcription | Yes — real-time ASR from live audio bytes forwarded by `connection.send(data)` | FLOWING (pipeline is complete; audio-to-transcript chain verified in code) |
| `static/app.js` `workletNode.port.onmessage` | `e.data` (Int16 ArrayBuffer) | `AudioWorkletProcessor` in `audio-processor.js` converting live mic samples | Yes — live mic input, not hardcoded | FLOWING |

---

### Behavioral Spot-Checks

Step 7b skipped for client-side AudioWorklet and browser socket code — cannot run browser JavaScript programmatically. Server-side module import check:

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| deepgram_service imports without error (no pyaudio dependency) | `grep -c "import pyaudio" services/deepgram_service.py` | 0 | PASS |
| `register_audio_handlers` exists and is exported | `grep -c "def register_audio_handlers" services/deepgram_service.py` | 1 | PASS |
| `deepgram_starten` is fully removed from app.py | `grep -c "deepgram_starten" app.py` | 0 | PASS |
| `register_audio_handlers(socketio)` called in app.py | `grep -c "register_audio_handlers" app.py` | 2 (import + call) | PASS |
| `room=sid` present on transcript emits | `grep -c "room=sid" services/deepgram_service.py` | 3 | PASS |
| AudioContext resume fix present | `grep -c "audioCtx.resume" static/app.js` | 1 | PASS |
| Page guard present | `grep -c "window.location.pathname" static/app.js` | 1 | PASS |
| End-to-end live transcription on VPS | Requires browser + deployed server | — | ? NEEDS HUMAN |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MIC-01 | 04.1-01-PLAN.md | Server startet ohne PyAudio — kein `import pyaudio` im Produktionscode; deepgram_service.py verwaltet per-Socket.IO-Session Deepgram-Verbindungen | SATISFIED | No pyaudio import in any production file. Per-session `_deepgram_sessions` dict fully implemented. |
| MIC-02 | 04.1-01-PLAN.md | Jede Socket.IO-Session bekommt eine eigene Deepgram-WebSocket-Verbindung; Lifecycle: open bei `start_live_session`, close bei `stop_live_session` oder disconnect | SATISFIED | `_open_deepgram_connection(sid)` and `_close_deepgram_connection(sid)` implement full lifecycle. All 4 event handlers registered and wired. |
| MIC-03 | 04.1-02-PLAN.md | Browser erfasst Mikrofon-Audio via `getUserMedia` + AudioWorklet (16kHz, Int16 PCM) und streamt via Socket.IO `audio_chunk` Events an den Server | SATISFIED | `static/audio-processor.js` exists with correct Float32→Int16 conversion. `startMicStream()` fully implemented with AudioContext at 16kHz, AudioWorklet, and `socket.emit('audio_chunk', e.data)`. |
| MIC-04 | 04.1-02-PLAN.md, 04.1-03-PLAN.md | Live-Transkription funktioniert end-to-end auf getnerve.app: Browser-Mikrofon → Server → Deepgram → Transkript im UI | NEEDS HUMAN | Code complete including AudioContext fix (commit 723d746). Root cause identified and patched. Human-verify checkpoint in Plan 03 was auto-approved without real testing. Previous UAT confirmed transcription was broken before this fix. Human must confirm fix works on VPS. |

All 4 requirements from phase plans are accounted for. No orphaned requirements found — REQUIREMENTS.md maps MIC-01 through MIC-04 to Phase 4.1 and all are claimed by the plans.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No stub patterns, no placeholder returns, no empty handlers, no hardcoded empty state found in any of the 4 modified files. All handlers contain real logic. The `_deepgram_sessions = {}` initialization is a legitimate empty dict that gets populated on `start_live_session` events — not a stub.

---

### Human Verification Required

#### 1. End-to-end live transcription after AudioContext fix

**Test:** Deploy current code to VPS (if not already deployed: `ssh root@178.104.82.166 "cd /opt/nerve && git pull && sudo systemctl restart nerve"`). Open https://getnerve.app/live in Chrome (use incognito to avoid cache). Grant microphone permission. Open DevTools Console (F12). Speak into the mic.

**Expected:**
- Console shows: `[Mic] AudioContext state after creation: suspended`, then `[Mic] AudioContext resumed, state: running`, then `[Mic] Browser-Mikrofon gestartet, AudioContext state: running`, then `[Mic] Erster audio_chunk gesendet, bytes: 3200`
- Transcripts appear in the left panel within 1-2 seconds of speaking
- Server logs (`ssh root@178.104.82.166 "journalctl -u nerve -n 30 --no-pager"`) show: `[DG] start_live_session received (sid=...)`, `[DG] Session gestartet (sid=...)`, `[DG] Verbunden (sid=...)`, `[DG] audio_chunk received (sid=..., bytes=3200, type=bytes)`, followed by transcript lines like `[DG] [Berater] ...`
- Click Pause — transcripts stop. Click again — resume.
- Click Beenden — session ends cleanly, mic indicator disappears from browser tab.

**Why human:** End-to-end browser audio pipeline (getUserMedia → AudioWorklet → Socket.IO → Deepgram → transcript emit → UI render) cannot be verified without a running browser and live VPS. The previous UAT confirmed this was broken; the code fix is in place but was never re-tested by a human.

#### 2. Training page mic prompt regression check

**Test:** Navigate to https://getnerve.app/training (logged in). Observe whether a browser mic permission dialog appears on page load.

**Expected:** No mic permission prompt on page load. Mic is only requested when pressing the PTT (push-to-talk) button inside a training scenario.

**Why human:** The `window.location.pathname === '/live'` guard in the `socket.on('connect')` handler fixes this at the code level, but the previous UAT confirmed training was requesting mic on page load. Confirmation requires visual observation of the live page.

---

### Gaps Summary

No automated gaps were found. All code-verifiable criteria are met:

- PyAudio fully removed from production code path
- Per-session Deepgram connections implemented correctly with thread-safe dict
- Browser audio pipeline (AudioWorklet + getUserMedia + Socket.IO) fully wired
- AudioContext suspension fix (`audioCtx.resume()`) committed
- Training page guard (`window.location.pathname === '/live'`) committed
- All existing UI flows (transcript, coaching, pause, beenden) preserved

The only outstanding item is **MIC-04** — end-to-end live transcription on the production VPS. This requires a human to confirm the AudioContext fix resolved the no-transcript bug found in the original UAT. All code prerequisites for that confirmation are in place.

---

_Verified: 2026-04-02T19:30:00Z_
_Verifier: Claude (gsd-verifier)_
