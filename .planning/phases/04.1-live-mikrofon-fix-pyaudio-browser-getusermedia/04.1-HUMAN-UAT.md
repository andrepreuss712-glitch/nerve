---
status: partial
phase: 04.1-live-mikrofon-fix-pyaudio-browser-getusermedia
source: [04.1-VERIFICATION.md]
started: 2026-04-02T19:35:00Z
updated: 2026-04-02T19:35:00Z
---

# Phase 4.1: Human UAT

## Tests Required

### Test 1: End-to-end live transcription after AudioContext fix (MIC-04)

**Deploy:**
```bash
ssh root@178.104.82.166 "cd /opt/nerve && git pull && sudo systemctl restart nerve"
```

**Steps:**
1. Open https://getnerve.app/live in Chrome **incognito** (to avoid cache)
2. Grant microphone permission when prompted
3. Open DevTools Console (F12)
4. Speak into the mic

**Expected — Console (DevTools):**
- `[Mic] AudioContext state after creation: suspended`
- `[Mic] AudioContext resumed, state: running`
- `[Mic] Browser-Mikrofon gestartet, AudioContext state: running`
- `[Mic] Erster audio_chunk gesendet, bytes: 3200`
- Transcripts appear in left panel within 1-2 seconds of speaking

**Expected — Server logs:**
```bash
ssh root@178.104.82.166 "journalctl -u nerve -n 30 --no-pager"
```
- `[DG] start_live_session received (sid=...)`
- `[DG] Session gestartet (sid=...)`
- `[DG] Verbunden (sid=...)`
- `[DG] audio_chunk received (sid=..., bytes=3200, type=bytes)`
- `[DG] [Berater] ...` (transcript lines)

**Also verify:** Pause stops transcripts, resume resumes. Beenden ends session cleanly.

---

### Test 2: Training page — no mic prompt on page load

**Steps:**
1. Navigate to https://getnerve.app/training (logged in)
2. Observe page load — no mic permission dialog should appear

**Expected:** No browser mic permission prompt on page load. Mic is only requested when pressing the PTT button inside a training scenario.

---

## Result

<!-- Fill in after testing -->
- [ ] Test 1 PASSED — transcripts appear
- [ ] Test 2 PASSED — no training mic prompt
- [ ] Issues found: (describe below)
