---
phase: "08.19.5.6.2"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - static/pip-launcher.js
  - tests/test_08_20_3.py
autonomous: true
requirements:
  - R-01
  - R-02
  - R-03
  - R-04
  - R-05

must_haves:
  truths:
    - "Step 4 zeigt genau 1 primaeren Button 'Briefing uebernehmen' (kein Modus-A/B/C)"
    - "PiP-Briefing-Tab erscheint automatisch wenn precallBriefing.firmenname vorhanden (kein briefingModus-Check)"
    - "state.briefingModus existiert nicht mehr im Code — 0 Treffer in pip-launcher.js"
    - "Step-5-Personalisieren-Button navigiert weiterhin zu Step 4b"
    - "pytest tests/test_08_20_3.py laeuft gruen durch"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "Konsolidierter 1-Button-Step4, bereinigtes briefingModus-freies PiP-Gate"
      contains: "lnr-step4-confirm"
    - path: "tests/test_08_20_3.py"
      provides: "Bereinigte Testdatei ohne toten briefingModus-Kommentar"
  key_links:
    - from: "renderStep4() lnr-step4-confirm click handler"
      to: "state.step = 5 + renderStep()"
      via: "direkter onclick-Handler"
      pattern: "lnr-step4-confirm.*onclick"
    - from: "_initPipLive() briefingTab gate"
      to: "state.precallBriefing.firmenname"
      via: "if-Bedingung in _initPipLive"
      pattern: "precallBriefing.*firmenname"
---

<objective>
Konsolidierung der 3-Button-Modus-Wahl (Modus A/B/C) in Step 4 zu einem einzigen "Briefing uebernehmen"-Button.
EWB-Integration und PiP-Tab werden automatisches Default-Verhalten (kein Modus-Flag mehr noetig).
state.briefingModus wird vollstaendig aus pip-launcher.js entfernt.
Personalisierung bleibt als optionaler Step-5-Pfad erhalten.

Purpose: Anti-UX-3-Modi-Wahl beseitigen. Alle 3 Funktionen (EWB, PiP-Tab, Personalisierung)
sollen immer aktiv sein — kein User-Decision-Point in Step 4 mehr.

Output: pip-launcher.js mit 1-Button-Step4, bereinigtem PiP-Gate, 0 briefingModus-Vorkommen.
tests/test_08_20_3.py bereinigt (kein toter Modus-Kommentar).
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\PROJECT.md
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\STATE.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.2-briefing-buttons-konsolidierung-3-zu-1\08.19.5.6.2-SPEC.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.2-briefing-buttons-konsolidierung-3-zu-1\08.19.5.6.2-CONTEXT.md

<interfaces>
<!-- Exakte Code-Stellen aus pip-launcher.js — verifiziert mit grep. -->
<!-- Alle Zeilennummern sind exact (Stand 2026-05-06). -->

State-Init (line 70):
```js
briefingModus: null,                     // 'A' | 'B' | 'C' | null (Phase 08.20.3)
```

renderStep4() Button-Block (lines 448-464):
```js
      // Modus A — Primary (SPEC Req 1)
      '<button class="launcher-btn-primary" id="lnr-step4-modus-a">',
      '  ▶ Briefing übernehmen',
      '  <span style="display:block;font-size:11px;font-weight:normal;opacity:0.8">Briefing fließt automatisch in die EWB-Hilfe ein</span>',
      '</button>',
      // Modus B — Ghost
      '<button class="launcher-btn-ghost" id="lnr-step4-modus-b">',
      '  📋 Briefing während Call sichtbar lassen',
      '  <span style="display:block;font-size:11px;opacity:0.7">Ausklappbarer Tab im PiP zum Nachschauen</span>',
      '</button>',
      // Modus C — disabled if no openers (SPEC Req 2, D-01 Modus-C-Disabled-Regel)
      (state.openerItems && state.openerItems.length > 0)
        ? '<button class="launcher-btn-ghost" id="lnr-step4-modus-c">...</button>'
        : '<button class="launcher-btn-ghost" id="lnr-step4-modus-c" disabled ...>...</button>',
```

renderStep4() Handler-Block (lines 488-510):
```js
    // Phase 08.20.3: 3-Button Modus-Selector — A/B/C
    document.getElementById('lnr-step4-modus-a').onclick = function () {
      var ta = document.getElementById('lnr-briefing-edit');
      if (ta && state.precallBriefing) state.precallBriefing.text = ta.value || state.precallBriefing.text;
      state.briefingModus = 'A';     // line 492
      state.step = 5;
      renderStep();
    };
    document.getElementById('lnr-step4-modus-b').onclick = function () {   // lines 496-501
      ...
      state.briefingModus = 'B';     // line 499
      ...
    };
    document.getElementById('lnr-step4-modus-c').onclick = function () {   // lines 503-510
      ...
      state.briefingModus = 'C';     // line 507
      ...
    };
```

_savePersonalizedAndStartCall() / renderStep4c() handler (line 638):
```js
    document.getElementById('lnr-step4c-original').onclick = function () {
      state.briefingModus = 'A';     // line 638 — DELETE
      state._personalizedSkriptText = null;
      state.step = 5;
      renderStep();
    };
```

Step-5-Personalisieren-Button handler (line 1191):
```js
        state.briefingModus = 'C';   // line 1191 — DELETE, rest of handler stays
        state.step = '4b';
        renderStep();
```

PiP-Tab-Gate in _initPipLive() (lines 1949-1951):
```js
      if (state.briefingModus === 'B' &&
          state.precallBriefing &&
          state.precallBriefing.firmenname) {
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: renderStep4() umbauen — 3 Buttons zu 1 Button (R-01, D-01, D-02)</name>
  <files>static/pip-launcher.js</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\static\pip-launcher.js (vollstaendig — lines 388–511 sind der Kern, aber Datei komplett lesen fuer Kontext)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.2-briefing-buttons-konsolidierung-3-zu-1\08.19.5.6.2-SPEC.md
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.2-briefing-buttons-konsolidierung-3-zu-1\08.19.5.6.2-CONTEXT.md
  </read_first>

  <action>
In static/pip-launcher.js die Funktion renderStep4() anpassen.

**SCHRITT A — Button-HTML ersetzen (im c.innerHTML Array-Join-Block, lines 448–462):**

Ersetze den gesamten Abschnitt von `// Modus A — Primary` bis zum Ende des Modus-C-Buttons
(inklusive aller 3 Button-Strings und des Kommentars) durch einen einzigen Button:

ENTFERNEN (lines 448–462, gesamter Block mit 3 Buttons inkl. Kommentare):
```
      // Modus A — Primary (SPEC Req 1)
      '<button class="launcher-btn-primary" id="lnr-step4-modus-a">',
      '  ▶ Briefing übernehmen',
      '  <span style="display:block;font-size:11px;font-weight:normal;opacity:0.8">Briefing fließt automatisch in die EWB-Hilfe ein</span>',
      '</button>',
      // Modus B — Ghost
      '<button class="launcher-btn-ghost" id="lnr-step4-modus-b">',
      '  📋 Briefing während Call sichtbar lassen',
      '  <span style="display:block;font-size:11px;opacity:0.7">Ausklappbarer Tab im PiP zum Nachschauen</span>',
      '</button>',
      // Modus C — disabled if no openers (SPEC Req 2, D-01 Modus-C-Disabled-Regel)
      (state.openerItems && state.openerItems.length > 0)
        ? '<button class="launcher-btn-ghost" id="lnr-step4-modus-c">✨ Skript auf diesen Lead personalisieren<span style="display:block;font-size:11px;opacity:0.7">KI passt einen Opener/Skript an die Lead-Daten an</span></button>'
        : '<button class="launcher-btn-ghost" id="lnr-step4-modus-c" disabled title="Erst einen Opener im Profil anlegen" style="opacity:0.4;cursor:not-allowed">✨ Skript auf diesen Lead personalisieren<span style="display:block;font-size:11px;opacity:0.7">KI passt einen Opener/Skript an die Lead-Daten an</span></button>',
```

ERSETZEN DURCH (1 Button, ID lnr-step4-confirm per D-01):
```
      '<button class="launcher-btn-primary" id="lnr-step4-confirm">Briefing übernehmen</button>',
```

**SCHRITT B — 3 onclick-Handler entfernen + 1 neuen Handler hinzufuegen (lines 488–510):**

Ersetze den gesamten Kommentar + 3 Handler-Bloecke:
```
    // Phase 08.20.3: 3-Button Modus-Selector — A/B/C
    document.getElementById('lnr-step4-modus-a').onclick = function () {
      var ta = document.getElementById('lnr-briefing-edit');
      if (ta && state.precallBriefing) state.precallBriefing.text = ta.value || state.precallBriefing.text;
      state.briefingModus = 'A';
      state.step = 5;
      renderStep();
    };
    document.getElementById('lnr-step4-modus-b').onclick = function () {
      var ta = document.getElementById('lnr-briefing-edit');
      if (ta && state.precallBriefing) state.precallBriefing.text = ta.value || state.precallBriefing.text;
      state.briefingModus = 'B';
      state.step = 5;
      renderStep();
    };
    document.getElementById('lnr-step4-modus-c').onclick = function () {
      if (!state.openerItems || state.openerItems.length === 0) return;  // guard
      var ta = document.getElementById('lnr-briefing-edit');
      if (ta && state.precallBriefing) state.precallBriefing.text = ta.value || state.precallBriefing.text;
      state.briefingModus = 'C';
      state.step = 5;
      renderStep();
    };
```

ERSETZEN DURCH (1 Handler, per D-02 Textarea-Sync beibehalten):
```
    // Phase 08.19.5.6.2: 1-Button "Briefing übernehmen" — EWB + PiP-Tab automatisch (kein briefingModus)
    document.getElementById('lnr-step4-confirm').onclick = function () {
      var ta = document.getElementById('lnr-briefing-edit');
      if (ta && state.precallBriefing) state.precallBriefing.text = ta.value || state.precallBriefing.text;
      state.step = 5;
      renderStep();
    };
```

Sicherstellen: Die Sekundaer-Buttons (lnr-step4-back, lnr-step4-edit, lnr-step4-new) und deren Handler bleiben unveraendert.
  </action>

  <verify>
    <automated>
      cd C:\Users\andre\dev\salesnerve && node -e "const fs=require('fs');const c=fs.readFileSync('static/pip-launcher.js','utf8');const modusCount=(c.match(/lnr-step4-modus-[abc]/g)||[]).length;const confirmCount=(c.match(/lnr-step4-confirm/g)||[]).length;const primaryCount=(c.match(/renderStep4[\s\S]*?(?=function renderStep4b)/)[0].match(/launcher-btn-primary/g)||[]).length;console.log('modus-ids:',modusCount,'confirm:',confirmCount,'primary-in-step4:',primaryCount);if(modusCount>0||confirmCount<1||primaryCount!==1)process.exit(1);"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "lnr-step4-modus-a\|lnr-step4-modus-b\|lnr-step4-modus-c" static/pip-launcher.js` liefert 0
    - `grep -c "lnr-step4-confirm" static/pip-launcher.js` liefert mindestens 2 (1x im HTML-String, 1x im onclick-Selektor)
    - Der Abschnitt zwischen `function renderStep4()` und `function renderStep4b()` enthaelt genau 1 Vorkommen von `launcher-btn-primary`
    - Der Abschnitt enthaelt keinen String `briefingModus`
    - lnr-step4-back, lnr-step4-edit, lnr-step4-new und ihre Handler sind unveraendert vorhanden
  </acceptance_criteria>

  <done>renderStep4() erzeugt genau 1 primaeren Button mit ID lnr-step4-confirm. Alle 3 Modus-IDs und deren Handler sind entfernt. Textarea-Sync-Logik ist in den neuen Handler uebertragen.</done>
</task>

<task type="auto">
  <name>Task 2: state.briefingModus vollstaendig entfernen — Init + verbleibende Setter (R-04)</name>
  <files>static/pip-launcher.js</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\static\pip-launcher.js (vollstaendig lesen — insbesondere lines 68–74, 635–642, 1189–1194)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.2-briefing-buttons-konsolidierung-3-zu-1\08.19.5.6.2-SPEC.md
  </read_first>

  <action>
Nach Task 1 sind die 3 Modus-Handler in renderStep4() bereits entfernt (lines 492, 499, 507 weg).
In diesem Task die 3 verbleibenden briefingModus-Vorkommen entfernen:

**SCHRITT A — State-Init (line 70):**

Entfernen:
```
    briefingModus: null,                     // 'A' | 'B' | 'C' | null (Phase 08.20.3)
```
Die Zeile davor (`micMuted: false,`) und die Zeile danach (`briefingTabExpandedAtStreamStart: false,`) bleiben unveraendert.

**SCHRITT B — renderStep4c() / lnr-step4c-original Handler (line 638):**

Aktueller Code:
```js
    document.getElementById('lnr-step4c-original').onclick = function () {
      state.briefingModus = 'A';
      state._personalizedSkriptText = null;
      state.step = 5;
      renderStep();
    };
```

Ersetzen durch (briefingModus-Setter entfernen, Rest unveraendert):
```js
    document.getElementById('lnr-step4c-original').onclick = function () {
      state._personalizedSkriptText = null;
      state.step = 5;
      renderStep();
    };
```

**SCHRITT C — Step-5-Personalisieren-Button Handler (line 1191):**

Aktueller Code (nur diese eine Zeile im Handler):
```js
        state.briefingModus = 'C';
        state.step = '4b';
        renderStep();
```

Entfernen nur die briefingModus-Zeile, Rest bleibt:
```js
        state.step = '4b';
        renderStep();
```

Sicherstellen: Es gibt keine weiteren briefingModus-Vorkommen (grep bestaetigt 0 nach diesen 3 Schritten plus den bereits in Task 1 entfernten 3 Handlern).
  </action>

  <verify>
    <automated>
      cd C:\Users\andre\dev\salesnerve && node -e "const fs=require('fs');const c=fs.readFileSync('static/pip-launcher.js','utf8');const count=(c.match(/briefingModus/g)||[]).length;console.log('briefingModus occurrences:',count);if(count>0)process.exit(1);"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "briefingModus" static/pip-launcher.js` liefert 0 (haertes Acceptance-Kriterium aus SPEC.md)
    - `grep -c "briefingTabExpandedAtStreamStart" static/pip-launcher.js` liefert mindestens 1 (Nachbar-Key unveraendert)
    - `grep -c "_personalizedSkriptText" static/pip-launcher.js` liefert mindestens 1 (lnr-step4c-original Handler funktional)
    - `grep -c "state.step = '4b'" static/pip-launcher.js` liefert mindestens 1 (Step-5-Personalisieren-Navigation unveraendert)
  </acceptance_criteria>

  <done>state.briefingModus existiert nicht mehr in pip-launcher.js. State-Init bereinigt. Alle 3 verbleibenden Setter (lines 638, 1191, 70) entfernt. grep briefingModus liefert 0.</done>
</task>

<task type="auto">
  <name>Task 3: PiP-Tab-Gate ersetzen — briefingModus === 'B' zu precallBriefing.firmenname (R-02)</name>
  <files>static/pip-launcher.js</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\static\pip-launcher.js lines 1940–1975 (Funktion _initPipLive, PiP-Tab-Block)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.2-briefing-buttons-konsolidierung-3-zu-1\08.19.5.6.2-SPEC.md
  </read_first>

  <action>
In static/pip-launcher.js in der Funktion _initPipLive() das PiP-Briefing-Tab-Gate ersetzen.

Aktueller Code (lines 1949–1951):
```js
      if (state.briefingModus === 'B' &&
          state.precallBriefing &&
          state.precallBriefing.firmenname) {
```

Ersetzen durch (briefingModus-Check entfernen, nur noch Briefing-Existenz pruefen):
```js
      if (state.precallBriefing &&
          state.precallBriefing.firmenname) {
```

Alles nach der if-Bedingung (Tab anzeigen, Titel setzen, Content befuellen, Education-Hint) bleibt unveraendert.

Sicherstellen: Der Kommentar `// ── Phase 08.20.3: Modus-B Briefing Tab` (line 1946) kann
auf `// ── Phase 08.19.5.6.2: PiP Briefing Tab (automatisch wenn Briefing vorhanden)` aktualisiert werden
(optional, aber sauber).
  </action>

  <verify>
    <automated>
      cd C:\Users\andre\dev\salesnerve && node -e "const fs=require('fs');const c=fs.readFileSync('static/pip-launcher.js','utf8');const badGate=(c.match(/briefingModus.*===.*'B'/g)||[]).length;const goodGate=(c.match(/precallBriefing.*firmenname|firmenname.*precallBriefing/g)||[]).length;console.log('briefingModus===B gate:',badGate,'precallBriefing.firmenname gates:',goodGate);if(badGate>0||goodGate<1)process.exit(1);"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep "briefingModus.*B\|briefingModus === 'B'" static/pip-launcher.js` liefert 0 Treffer
    - `grep "precallBriefing && state.precallBriefing.firmenname\|state.precallBriefing.*firmenname" static/pip-launcher.js` liefert mindestens 1 Treffer im Kontext von _initPipLive
    - `grep -c "briefingModus" static/pip-launcher.js` liefert 0 (kumulativ — alle 3 Tasks zusammen)
    - Der Block nach dem if (briefingTab.style.display = 'block', titleEl, contentEl) ist unveraendert vorhanden
  </acceptance_criteria>

  <done>PiP-Briefing-Tab erscheint automatisch wenn state.precallBriefing.firmenname vorhanden ist. briefingModus === 'B'-Check ist entfernt. Kein briefingModus mehr in der gesamten Datei.</done>
</task>

<task type="auto">
  <name>Task 4: tests/test_08_20_3.py bereinigen — toten briefingModus-Kommentar entfernen (R-04, D-03)</name>
  <files>tests/test_08_20_3.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\tests\test_08_20_3.py (vollstaendig)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Test-Qualitaets-Regel Abschnitt)
  </read_first>

  <action>
In tests/test_08_20_3.py line 35 den toten Kommentar entfernen (per D-03):

Aktueller Code (lines 33–37):
```python
    def test_modus_a_sets_state_contract(self, client):
        """Modus A flow: after selecting A, no personalization endpoint is hit."""
        # Modus A requires no backend call — only state.briefingModus = 'A'
        # Verified by: /api/precall/personalize is NOT called for Modus A
        # This test verifies the route exists and rejects calls without opener_id
```

Ersetzen durch (Kommentar-Zeile line 35 entfernen):
```python
    def test_modus_a_sets_state_contract(self, client):
        """Modus A flow: after selecting A, no personalization endpoint is hit."""
        # Verified by: /api/precall/personalize is NOT called without briefing context
        # This test verifies the route exists and rejects calls without opener_id
```

Pruefe den Rest der Datei auf weitere briefingModus-Vorkommen:
- CLAUDE.md Test-Qualitaets-Regel: `inspect.getsource` mit briefingModus-String-Check waere Source-Presence → LOESCHEN falls vorhanden
- Runtime-Tests (Route-Response-Codes, State-Mutations) → BEHALTEN

Nach grep des Gesamtbefunds aus Task-Planung: Die Testdatei hat keine weiteren briefingModus-Referenzen
ausserhalb von line 35. Nur diese eine Kommentar-Zeile entfernen.

Sicherstellen: Test-Klassen, Methoden und Assertions bleiben unveraendert.
  </action>

  <verify>
    <automated>
      cd C:\Users\andre\dev\salesnerve && python -m pytest tests/test_08_20_3.py -v 2>&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "briefingModus" tests/test_08_20_3.py` liefert 0
    - `python -m pytest tests/test_08_20_3.py -v` exitiert mit Code 0 (alle Tests gruen oder expected-skip)
    - Die Methode `test_modus_a_sets_state_contract` existiert weiterhin (nur der Kommentar geaendert, kein Loeschen der Methode)
    - Keine `inspect.getsource`-Calls in der Datei vorhanden (kein Source-Presence-Test eingefuehrt)
  </acceptance_criteria>

  <done>tests/test_08_20_3.py enthaelt keinen briefingModus-Kommentar mehr. pytest laeuft gruen durch. Kein toter Code, kein Source-Presence-Test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JS → Flask API | Unveraendert — kein neuer User-Input-Vektor in dieser Phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.5.6.2-01 | Tampering | pip-launcher.js state-Objekt | accept | Reine Frontend-Variable; State lebt nur im Browser. Entfernung von briefingModus reduziert Code-Surface. Kein sicherheitsrelevanter Guard entfernt — PiP-Tab war nie Sicherheits-Gate. |
| T-08.19.5.6.2-02 | Information Disclosure | mdToHtml() im PiP-Briefing-Tab | accept | mdToHtml() eskapt &, &lt;, &gt; vor der MD-Konversion (XSS-sicher). Unveraendert durch diese Phase. |

Fazit: Diese Phase entfernt toten State-Code und vereinfacht eine if-Bedingung. Kein neuer
Attack-Surface. Keine Backend-Aenderung. Keine Auth-Aenderung. Bedrohungsniveau: minimal.
</threat_model>

<verification>
Gesamtpruefung nach allen 4 Tasks:

```bash
# Haertes Acceptance-Kriterium (muss 0 liefern):
grep -c "briefingModus" C:\Users\andre\dev\salesnerve\static\pip-launcher.js

# Neuer Button vorhanden:
grep -c "lnr-step4-confirm" C:\Users\andre\dev\salesnerve\static\pip-launcher.js

# Alte Modus-IDs weg:
grep "lnr-step4-modus-a\|lnr-step4-modus-b\|lnr-step4-modus-c" C:\Users\andre\dev\salesnerve\static\pip-launcher.js

# PiP-Gate korrekt:
grep "precallBriefing.*firmenname" C:\Users\andre\dev\salesnerve\static\pip-launcher.js

# Test-Suite gruen:
cd C:\Users\andre\dev\salesnerve && python -m pytest tests/test_08_20_3.py -v
```
</verification>

<success_criteria>
- `grep -c "briefingModus" static/pip-launcher.js` = 0
- `grep -c "lnr-step4-confirm" static/pip-launcher.js` >= 2
- `grep "lnr-step4-modus-[abc]" static/pip-launcher.js` liefert leere Ausgabe
- `grep "state.precallBriefing && state.precallBriefing.firmenname" static/pip-launcher.js` hat Treffer
- `python -m pytest tests/test_08_20_3.py -v` exitiert 0
- `grep -c "briefingModus" tests/test_08_20_3.py` = 0
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/08.19.5.6.2-briefing-buttons-konsolidierung-3-zu-1/08.19.5.6.2-01-SUMMARY.md`

Pflichtinhalt:
- tasks_completed: [Task 1, Task 2, Task 3, Task 4]
- files_modified: [static/pip-launcher.js, tests/test_08_20_3.py]
- key_decisions: [D-01 lnr-step4-confirm ID gewaehlt, D-02 Textarea-Sync beibehalten, D-03 Kommentar entfernt]
- acceptance_verified: grep briefingModus = 0, pytest gruen
- bugs_fixed: []
- next_phase_notes: "Visual-Polish Step-4-UI (Button-Styling, Spacing) ist out of scope — kommt in Block O Teil 2"
</output>
