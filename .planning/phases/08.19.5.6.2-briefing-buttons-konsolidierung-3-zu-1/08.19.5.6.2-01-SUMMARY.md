---
phase: "08.19.5.6.2"
plan: "01"
subsystem: "pip-launcher"
tags: ["briefing", "step4", "pip-tab", "refactor", "state-cleanup"]
dependency_graph:
  requires: []
  provides: ["1-Button-Step4-lnr-step4-confirm", "briefingModus-free-pip-launcher"]
  affects: ["static/pip-launcher.js", "tests/test_08_20_3.py"]
tech_stack:
  added: []
  patterns: ["State-Cleanup", "Direct-Assignment-statt-Fallback"]
key_files:
  created: []
  modified:
    - static/pip-launcher.js
    - tests/test_08_20_3.py
decisions:
  - "D-01: ID lnr-step4-confirm gewaehlt (statt lnr-step4-modus-a behalten) — saubere semantische Trennung"
  - "D-02: Textarea-Sync als direkte Zuweisung (ta.value) statt fallback-Operator — verhindert dass User leeren Text nicht speichern kann"
  - "D-03: Toter Kommentar in test_08_20_3.py Zeile 35 entfernt, Kommentar ohne briefingModus-Referenz beibehalten"
metrics:
  duration: "~15 Minuten"
  completed_date: "2026-05-06"
  tasks_completed: 4
  files_modified: 2
---

# Phase 08.19.5.6.2 Plan 01: Briefing-Buttons-Konsolidierung 3-zu-1 Summary

**One-liner:** 3-Button-Modus-Wahl (A/B/C) in Step 4 zu 1-Button lnr-step4-confirm konsolidiert; state.briefingModus vollstaendig aus pip-launcher.js entfernt (0 Treffer); PiP-Tab-Gate auf precallBriefing.firmenname umgestellt.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | renderStep4() 3-Button zu 1-Button | d118a4f | static/pip-launcher.js |
| 2 | state.briefingModus vollstaendig entfernt | df11b67 | static/pip-launcher.js |
| 3 | PiP-Tab-Gate ersetzt | c3122be | static/pip-launcher.js |
| 4 | tests/test_08_20_3.py bereinigt | fc9fe08 | tests/test_08_20_3.py |

## Acceptance Verified

- `grep -c "briefingModus" static/pip-launcher.js` = **0** (haertes Kriterium erfuellt)
- `grep -c "lnr-step4-confirm" static/pip-launcher.js` = **2** (1x HTML-String, 1x onclick-Selektor)
- `grep "lnr-step4-modus-[abc]" static/pip-launcher.js` = **leer** (alle 3 Modus-IDs entfernt)
- `grep "precallBriefing.firmenname" static/pip-launcher.js` = **4 Treffer** (PiP-Gate korrekt)
- `python -m pytest tests/test_08_20_3.py -v` = **28 passed, 0 failed** (exit 0)
- `grep -c "briefingModus" tests/test_08_20_3.py` = **0**

## Key Changes

### static/pip-launcher.js

**renderStep4() — Button-HTML:**
- Entfernt: 3 Buttons (`lnr-step4-modus-a` primary, `lnr-step4-modus-b` ghost, `lnr-step4-modus-c` ghost/disabled mit ternary)
- Ersetzt durch: `<button class="launcher-btn-primary" id="lnr-step4-confirm">Briefing uebernehmen</button>`

**renderStep4() — Handler-Block:**
- Entfernt: 3 onclick-Handler (modus-a/b/c) mit briefingModus-Settern
- Ersetzt durch: 1 onclick-Handler mit direkter Textarea-Sync (`ta.value` statt `ta.value || fallback`) + `state.step = 5`

**State-Init (ex-line 70):**
- Entfernt: `briefingModus: null,` Property

**renderStep4c() lnr-step4c-original Handler:**
- Entfernt: `state.briefingModus = 'A';` Setter

**Step-5-Personalisieren Handler:**
- Entfernt: `state.briefingModus = 'C';` Setter

**_initPipLive() PiP-Tab-Gate:**
- Alt: `if (state.briefingModus === 'B' && state.precallBriefing && state.precallBriefing.firmenname)`
- Neu: `if (state.precallBriefing && state.precallBriefing.firmenname)`
- Kommentar aktualisiert auf Phase 08.19.5.6.2

### tests/test_08_20_3.py

- Zeile 35 entfernt: `# Modus A requires no backend call — only state.briefingModus = 'A'`
- Ersetzt durch: `# Verified by: /api/precall/personalize is NOT called without briefing context`

## Decisions Made

**D-01: ID lnr-step4-confirm**
Button-ID wurde neu vergeben statt lnr-step4-modus-a behalten — saubere semantische Trennung zwischen altem Modus-A-Konzept und neuer modusfreier Aktion.

**D-02: Direkte Textarea-Zuweisung**
`state.precallBriefing.text = ta.value` statt `ta.value || state.precallBriefing.text` — Cross-AI-Review hatte MEDIUM-Finding: Fallback-Operator wuerde leere Textarea ignorieren und alten Text beibehalten, was es dem User unmoeoglich macht, das Briefing zu leeren.

**D-03: Kommentar-Anpassung statt Loeschung**
Toter Zeilen-Kommentar (briefingModus-Setter-Referenz) entfernt; beschreibender Kommentar ohne Modus-Referenz beibehalten — Test-Intent bleibt klar.

## Deviations from Plan

**1. [Rule 1 - Bug] Kommentar in Task-1-Handler enthielt briefingModus-Verweis**
- Gefunden bei: Task 2 Verifikation (count lieferte 2 statt 0)
- Problem: Der neu geschriebene Kommentar `// Phase 08.19.5.6.2: ... (kein briefingModus)` enthielt das Wort `briefingModus` — damit schlug die grep-Pruefung fehl
- Fix: Kommentar auf `// Phase 08.19.5.6.2: 1-Button "Briefing uebernehmen" — EWB + PiP-Tab automatisch` gekuerzt
- Dateien: static/pip-launcher.js (im Task-2-Commit df11b67 enthalten)

## Known Stubs

Keine. Alle Aenderungen sind vollstaendig verdrahtet.

## Threat Flags

Keine neuen Sicherheits-Surfaces eingefuehrt. Bestaetigt per STRIDE-Register im Plan:
- T-08.19.5.6.2-01: State-Code entfernt (reduziert Surface)
- T-08.19.5.6.2-02: mdToHtml() unveraendert (XSS-sicher)

## Next Phase Notes

- Visual-Polish Step-4-UI (Button-Styling, Spacing fuer neuen 1-Button-Step-4) ist out of scope — kommt in Block O Teil 2
- `briefingTabExpandedAtStreamStart` und alle anderen briefingTab-State-Keys bleiben erhalten (gehoeren zum PiP-Tab-Mechanismus, nicht zu briefingModus)

## Self-Check: PASSED

- [x] static/pip-launcher.js existiert und enthaelt lnr-step4-confirm
- [x] tests/test_08_20_3.py existiert und enthaelt keine briefingModus-Referenz
- [x] Commits d118a4f, df11b67, c3122be, fc9fe08 existieren in git log
- [x] pytest 28 passed, exit 0
