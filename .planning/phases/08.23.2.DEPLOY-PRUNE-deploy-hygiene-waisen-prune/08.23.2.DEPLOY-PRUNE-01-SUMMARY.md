---
phase: 08.23.2.DEPLOY-PRUNE-deploy-hygiene-waisen-prune
plan: 01
status: code-complete-pending-deploy-gate
executed: 2026-07-07
files_modified:
  - scripts/prune_orphans.sh   # neu
  - deploy.sh                  # additiv
  - tests/test_deploy_prune_guard.py  # neu
commits:
  - 660715b   # Task 1 — scripts/prune_orphans.sh
  - 59ef6ae   # Task 2 — deploy.sh Manifest + Prune-Aufruf
  - 6b16c88   # Task 3 — test_deploy_prune_guard.py
key-files:
  created:
    - scripts/prune_orphans.sh
    - tests/test_deploy_prune_guard.py
---

# SUMMARY — 08.23.2.DEPLOY-PRUNE Plan 01 (Deploy-Hygiene Waisen-Prune)

**Status:** Code-complete, 3 atomische Commits auf `main`, `git diff` zeigt AUSSCHLIESSLICH die 3 geplanten
Dateien (kein NERVE-App-Code). **Deploy-Gate-Acceptance NOCH OFFEN** — supervised (`bash deploy.sh production`),
André+Claudian fahren; **Erst-Lauf erzwungen Dry-Run** (D-03): Kandidatenliste wird verifiziert, DANN erst
`PRUNE_APPLY=1` scharf. Lokal nur `bash -n` + `py_compile` (Syntax) + Plan-Acceptance-Greps — **kein Acceptance**.

## Task 1 — scripts/prune_orphans.sh (neu)
Isolierte Prune-Funktion. Manifest-Diff (Weg 1), NUL-safe (`git ls-files -z`/`find -print0`/`read -r -d ''`).
- **Funktions-Struktur + BASH_SOURCE-Main-Guard (Fable MUST-3):** `prune_guard_customer_data`, `prune_guard_cap`,
  `prune_compute_candidates`, `prune_apply`. Gesourct läuft NICHTS → Test trifft den Guard direkt.
- **Whitelist NICHT env-überschreibbar** (`grep 'PRUNE_DIRS="${'` == 0). Nur Output-Pfade (Report/Trash) via
  `PRUNE_REPORT_DIR`/`PRUNE_TRASH_BASE` für Tests übersteuerbar — kein Prod-Scope-Override.
- **Exclude VOR Cap** (`case`, set-e-safe): `__pycache__/`, `*.pyc`, `*.db*`, `tests/tts_samples/` (MUST-1a/SHOULD-b).
- 3 Guards fail-closed: a leeres Manifest → b logs/database-Hard-Abort (D-05) → c Cap N=30 (`PRUNE_MAX`).
- Dry-Run default (`[PRUNE][DRY-RUN]`, Report + Retention); Apply nur bei `PRUNE_APPLY=1` (`mv` → `/opt/nerve/trash/<ts>/`,
  Retention 2). Kein `((COUNT++))` (grep == 0), keine `rm` auf App-Pfade.

## Task 2 — deploy.sh (additiv)
- `git ls-files -z <Code-Whitelist>`-Manifest lokal → scp `/root/nerve_prune_manifest` (SHOULD-c).
- **MUST-1b:** genau 1 Zeile `--exclude='./tests/tts_samples'` in TAR_EXCLUDES.
- **MUST-4:** Prune als SEPARATER ssh NACH `ENDHEREDOC` (Zeilenprüfung: Prune 267 > ENDHEREDOC 250), `|| echo WARN`
  → nie deploy-blockierend (Heredoc `set -e` unberührt).
- **MUST-2:** `grep 'PRUNE_APPLY:+'` == 0; expliziter `[ "${PRUNE_APPLY:-0}" = "1" ]`. Default Dry-Run.
- tar-Upload/Gate/Restart unverändert.

## Task 3 — tests/test_deploy_prune_guard.py (neu, 9 Tests)
Subprocess gegen tmp-$APP_DIR: orphan_listed_dryrun, empty_manifest_aborts, cap_exceeded_aborts,
excluded_pycache_not_candidate, space_path_orphan (NUL), apply_moves_to_trash.
Source-Stil (MUST-3): logs_candidate_hard_abort + database_candidate_hard_abort (source + direkter Guard-Aufruf,
Assert `returncode != 0` UND `[PRUNE][ABORT]` in stderr).
**Anti-Vakuum-Grün (Fable-Recheck):** `test_guard_passes_clean_candidate` — `prune_guard_customer_data templates/x.html`
→ exit 0 (beweist, dass das !=0 der Hard-Abort-Tests vom Guard kommt, nicht von defektem Sourcen).

## Lokale Checks (kein Acceptance)
`bash -n scripts/prune_orphans.sh` ✓ · `bash -n deploy.sh` ✓ · `py_compile` ✓ · alle Plan-Greps grün.

## ⚠ FLAG für André (Scope-Entscheidung) — CRLF/Zeilenenden
`core.autocrlf=true`, **kein `.gitattributes`**. Aktuell ist die Working-Copy von `scripts/prune_orphans.sh`
**LF** (verifiziert) → der nächste supervised Deploy tar't LF → läuft. **Latentes Risiko:** ein künftiger
`git checkout`/`reset --hard` könnte `*.sh` als CRLF re-materialisieren → `\r`-Bruch auf dem Linux-Server.
Bereits `deploy.sh`/`inspect.sh` teilen dieses Risiko. **Mitigiert durch Design:** der Prune ist isoliert +
`|| WARN` → selbst ein CRLF-Bruch kann den Deploy NICHT umwerfen (nur der Prune-Schritt warnt).
**Empfehlung (André-Entscheidung, 4. Datei außerhalb des 3-Dateien-Rahmens):** `.gitattributes` mit
`*.sh text eol=lf` ergänzen (härtet AUCH deploy.sh/inspect.sh). Nicht blockierend für diesen Deploy.

## OFFEN (supervised, André+Claudian) — Deploy-Gate = Acceptance
1. `bash deploy.sh production` — Gate grün inkl. `test_deploy_prune_guard.py` (9 Tests, PASSED nicht SKIPPED).
2. **Erst-Lauf Dry-Run:** `[PRUNE][DRY-RUN]`-Report NACH dem Restart; André verifiziert Kandidaten (keine
   logs/database, plausible Ex-Repo-Dateien); Deploy bleibt grün.
3. Nach OK: `PRUNE_APPLY=1 bash deploy.sh production` → Kandidaten in `/opt/nerve/trash/<ts>/`, Server == Repo
   für Code; `find $APP_DIR/logs` + `database` intakt.
4. Entscheidung `.gitattributes`-Hardening (siehe FLAG).

**Bekannte Restlücke (dokumentiert):** Manifest = git-Index vs tar = Working-Tree → künftige untracked
Whitelist-Dateien erscheinen im Dry-Run-Report (nicht auto-gelöscht; Report = Wächter; tar-Wurzelfix vertagt).
tts_samples ist der bisher einzige bekannte Fall, via TAR-Exclude entschärft.
