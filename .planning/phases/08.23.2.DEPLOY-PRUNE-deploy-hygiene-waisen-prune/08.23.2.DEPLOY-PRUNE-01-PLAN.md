---
phase: 08.23.2.DEPLOY-PRUNE-deploy-hygiene-waisen-prune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prune_orphans.sh
  - deploy.sh
  - tests/test_deploy_prune_guard.py
autonomous: false   # Prod-kritisch (deploy.sh = Deploy-Mechanismus), supervised, Erst-Lauf erzwungen Dry-Run
requirements: NONE   # Herkunft: AUTH-1-Live-Befund (Ghost-Cruft), keine REQ-IDs im ROADMAP gemappt
complexity: "🟡 (deploy.sh-nah → 🔴-nah) — Cross-AI/Fable Pflicht VOR Execute (Punkt 24)"

must_haves:
  truths:
    - "deploy.sh räumt im Repo gelöschte Code-Dateien server-seitig ab: nach einem Deploy, in dem eine getrackte Datei aus einem Whitelist-Dir entfernt wurde, ist ihre Server-Kopie nicht mehr unter $APP_DIR/<dir> (sondern in /opt/nerve/trash/<ts>/). Verifiziert am Prune-Report + find."
    - "Kein Kundendaten-Verlust: kein Pfad unter logs/ oder database/ wird je Kandidat. scripts/prune_orphans.sh bricht HART ab (exit, keine Bewegung), falls ein Kandidat mit logs/ oder database/ beginnt (D-05), UND diese Dirs sind gar nicht erst im Whitelist-Suchbereich (D-01)."
    - "Selbst-Aussperr-Schutz: der Prune läuft als isolierte Funktion NACH systemctl restart (deploy.sh) — ein Prune-Fehler (exit != 0) blockiert weder Deploy noch Restart (Deploy ist zum Prune-Zeitpunkt bereits live). Cap N=30 (PRUNE_MAX-übersteuerbar) + leeres-Manifest-Abbruch verhindern Massen-Löschung."
    - "Erst-Rollout sicher: ohne PRUNE_APPLY=1 macht das Skript NUR Dry-Run (listet Kandidaten in einen Report, bewegt NICHTS). Scharf erst mit PRUNE_APPLY=1. Quarantäne statt rm: Kandidaten wandern per mv nach /opt/nerve/trash/<ts>/, reversibel."
    - "Wächter (Test-Netz-Ratsche): tests/test_deploy_prune_guard.py (>=8 Tests) läuft im deploy.sh-pytest-Gate (nicht live/perf) mit und ist GRÜN — beweist die Prune-Logik deterministisch (Orphan-Erkennung, Exclude-vor-Cap, Cap-Abbruch, Leerzeichen-Pfad/NUL) gegen tmp-Fixtures UND den logs/database-Hard-Abort via SOURCE+direktem prune_guard_customer_data-Aufruf (MUST-3, kein Scope-Override)."
    - "Sisyphus vermieden (MUST-1): tests/tts_samples/*.mp3 (16 untracked, .gitignore) werden aus TAR_EXCLUDES (deploy.sh, genau 1 additive Zeile) UND aus dem Prune-Exclude entfernt → sie fressen nicht den Cap, der Prune bricht nicht dauerhaft ab. Bekannte Restlücke dokumentiert: Manifest=git-Index vs tar=Working-Tree → künftige untracked Whitelist-Dateien erscheinen im Dry-Run-Report (der Report ist der Wächter)."
    - "Rahmen gewahrt (Punkt 17/27): NUR scripts/prune_orphans.sh (neu), deploy.sh (Prune-Aufruf + Manifest-Transport + genau 1 tts_samples-TAR-Exclude-Zeile), tests/test_deploy_prune_guard.py (neu). KEIN NERVE-App-Code-Change. git diff zeigt nur diese 3 Dateien."
  artifacts:
    - path: "scripts/prune_orphans.sh"
      provides: >
        Isolierte, separat testbare Prune-Funktion. Liest NUL-delimitiertes git-Manifest,
        vergleicht gegen find in den Whitelist-Code-Dirs, ermittelt Waisen, wendet Cap +
        Exclude + logs/database-Hard-Abort an, verschiebt (Dry-Run default) nach /opt/nerve/trash/<ts>/.
      contains: "PRUNE_APPLY"
    - path: "deploy.sh"
      provides: >
        Erzeugt lokal git ls-files -z Manifest der Whitelist, scp't es zum Server, ruft
        scripts/prune_orphans.sh NACH systemctl restart auf (isoliert, nicht deploy-blockierend).
      contains: "prune_orphans.sh"
    - path: "tests/test_deploy_prune_guard.py"
      provides: "Deterministischer Deploy-Gate-Guard der die Prune-Logik gegen tmp-Fixtures prüft."
      contains: "def test_"
  key_links:
    - from: "deploy.sh (post-restart Prune-Aufruf)"
      to: "scripts/prune_orphans.sh auf $APP_DIR"
      via: "ssh … bash $APP_DIR/scripts/prune_orphans.sh (nach systemctl restart)"
      pattern: "prune_orphans\\.sh"
    - from: "deploy.sh (Manifest-Erzeugung lokal)"
      to: "git ls-files -z Whitelist → scp → Server"
      via: "git ls-files -z <whitelist> app.py config.py extensions.py > manifest; scp"
      pattern: "git ls-files -z"
    - from: "tests/test_deploy_prune_guard.py"
      to: "scripts/prune_orphans.sh (Dry-Run gegen tmp-Fixture)"
      via: "subprocess-Aufruf des Skripts in Dry-Run + Assertion auf Kandidatenliste"
      pattern: "prune_orphans"
---

<objective>
`deploy.sh` so härten, dass im Repo gelöschte **Code**-Dateien beim nächsten Deploy AUCH server-seitig
verschwinden (Server == Repo für Code), OHNE dass ein kaputter Prune künftige Prod-Deploys lahmlegt und
OHNE Kundendaten (logs/, database/) zu gefährden. Mechanik = **Manifest-Diff (Weg 1)** mit fünf
Sicherheits-Schichten (André-Entscheidung 2026-07-07, siehe CONTEXT D-01..D-07).

**Kern-Fakt (live an deploy.sh:78-79 belegt):** tar-extract mergt — es löscht nie server-only Dateien.
Der Prune schließt genau diese Lücke, additiv, NACH dem Restart, isoliert.

**Rahmen:** NUR deploy.sh + neues scripts/prune_orphans.sh + neuer Guard-Test. KEIN App-Code.
autonomous:false — Erst-Prod-Lauf erzwungen Dry-Run, André verifiziert die Kandidaten, dann scharf.

**Fünf Sicherheits-Schichten (alle Pflicht):**
1. Scope-Whitelist (nur 11 Code-Dirs + top-level *.py) → logs/database strukturell draußen.
2. Cap N=30 (PRUNE_MAX-übersteuerbar) + leeres-Manifest-Abbruch → keine Massen-Löschung.
3. Dry-Run default; scharf nur mit PRUNE_APPLY=1; Erst-Prod-Lauf erzwungen Dry-Run.
4. Quarantäne statt rm (mv nach /opt/nerve/trash/<ts>/, Retention 2) → reversibel.
5. logs/database-Hard-Abort (D-05) → belt-and-suspenders.

**Fable-Final-Review eingearbeitet (--reviews, 2026-07-07):** 4 MUST — (1) tts_samples-Sisyphus (16 untracked
mp3 füllen den Cap → Prune bricht immer ab): Prune-Exclude + TAR-Exclude; (2) PRUNE_APPLY-Bug `${:+}` → expliziter
`==1`-Check; (3) logs/database-Hard-Abort per Konstruktion unerreichbar → Skript in Funktionen, Test sourct + ruft
Guard direkt (kein Scope-Override); (4) Prune NUR separater ssh NACH dem Heredoc (Heredoc hat set -e). Plus 4 SHOULD
(Leerzeichen-/Exclude-Test, Exclude-vor-Cap + set-e-safe Zähler, Manifest→/root, Report-Retention). Siehe REVIEWS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.DEPLOY-PRUNE-deploy-hygiene-waisen-prune/08.23.2.DEPLOY-PRUNE-CONTEXT.md
@.planning/phases/08.23.2.DEPLOY-PRUNE-deploy-hygiene-waisen-prune/08.23.2.DEPLOY-PRUNE-RESEARCH.md
@deploy.sh
@services/live_session.py

<interfaces>
<!-- Live-verifiziert 2026-07-07. Der Executor baut gegen diese Verträge. -->

deploy.sh-Anker (VERBATIM-Bezug):
- `:51-70` TAR_EXCLUDES (logs, database/*.db, .git, .env, __pycache__, *.pyc, …).
- `:78-79` tar-over-ssh (das mergende Deploy).
- `:85-96` scp-Muster für Zusatzdateien (nginx/service) — Manifest analog transportieren.
- `:99-240` Remote-Heredoc; `:186-194` pytest-Gate; `:237` `sudo systemctl restart $SERVICE_NAME`; `:242` `echo "[deploy] Done."` (lokal, nach Heredoc).

Whitelist (live git-verifiziert, alle getrackt): templates static routes services scripts tests alembic
config tools nerve_rt docs + top-level app.py config.py extensions.py.

APP_DIR=/opt/nerve/app · Quarantäne=/opt/nerve/trash/<ts>/ · Cap default 30 (PRUNE_MAX).
Manifest lokal via `git ls-files -z`; Server hat kein .git (tar-excluded).
</interfaces>
</context>

## 5. Persistenz-/Sicherheits-Schicht-Verifikation (Punkt 21/22)

Diese Phase LÖSCHT (via Quarantäne) Dateien auf dem Prod-Server. Kein DB-Zugriff, kein App-Code.
Alle Pfad-/Scope-Fakten sind in RESEARCH.md §2/§4 live-verifiziert (git ls-files + deploy.sh-Zeilen).
**Kundendaten-Pfade (logs/, database/) werden NIE Kandidat** — doppelt geschützt (Scope-Whitelist D-01 +
Hard-Abort D-05). Kein Objekt außerhalb `$APP_DIR` (venv, .deploy_meta, .env) ist je im Suchbereich.

### Bei Diskrepanz: STOP + Replan
Wenn der Executor findet, dass (a) deploy.sh NICHT tar-basiert deployt, ODER (b) ein Whitelist-Dir fehlt,
ODER (c) `logs/`/`database/` doch im Suchbereich landen könnten → STOP, im SUMMARY dokumentieren, nicht raten.

<tasks>

<task type="auto">
  <name>Task 1: scripts/prune_orphans.sh — isolierte, separat testbare Prune-Funktion (D-01..D-06)</name>
  <files>scripts/prune_orphans.sh</files>

  <read_first>
    - deploy.sh:51-70 (TAR_EXCLUDES — die Exclude-Semantik, die der Prune spiegeln muss).
    - deploy.sh:78-79 + :237 (wo/wann der Prune andockt: nach Restart, gegen $APP_DIR).
    - services/live_session.py:37 (LOG_DIR — der Kundendaten-Pfad, der NIE angefasst wird).
    - .planning/.../08.23.2.DEPLOY-PRUNE-CONTEXT.md (D-01..D-06, LOCKED).
  </read_first>

  <behavior>
    - Eingaben (Env/Args): APP_DIR (default /opt/nerve/app), MANIFEST-Pfad (NUL-delimitierte git-Liste),
      PRUNE_APPLY (nur wörtlich "1" = scharf, sonst Dry-Run), PRUNE_MAX (default 30).
    - **FUNKTIONS-STRUKTUR (Fable MUST-3):** die Kern-Logik liegt in aufrufbaren Funktionen, sodass der
      Guard-Test (Task 3) das Skript SOURCEN und die Guard-Funktion mit synthetischer Kandidatenliste direkt
      aufrufen kann — der logs/database-Hard-Abort ist per Konstruktion aus dem Whitelist-Scan NICHT erreichbar,
      also muss der Test die Funktion direkt treffen können, OHNE den Prod-Scope aufzuweichen. Mindestens:
        * `prune_compute_candidates` — sammelt Waisen aus den Whitelist-Dirs (find -print0 minus Manifest minus Exclude).
        * `prune_guard_customer_data <candidates…>` — HARTER exit != 0, wenn ein Kandidat mit `logs/` oder `database/` beginnt (D-05).
        * `prune_guard_cap <count>` — exit != 0, wenn count > PRUNE_MAX (D-02).
        * `prune_apply` / Dry-Run-Report-Funktion.
      Main-Guard `if [ "${BASH_SOURCE[0]:-$0}" = "$0" ]; then … fi` am Ende: gesourct läuft NICHTS (Test sourct nur).
    - Ermittelt Kandidaten: für jedes Whitelist-Dir `find "$APP_DIR/<dir>" -type f -print0` → Pfade relativ
      zu $APP_DIR normalisieren → Menge MINUS Manifest-Einträge MINUS Exclude-Muster = Waisen. NUL-delimitiert
      durchgehend (`git ls-files -z`/`-print0`, `while IFS= read -r -d ''`), damit Leerzeichen-Pfade tragen.
    - **EXCLUDE VOR CAP (Fable SHOULD-b + MUST-1a):** der Exclude-Filter läuft ZWINGEND vor dem Cap-Count — der
      Server trägt >60 `__pycache__`-Einträge + 16 `tests/tts_samples/*.mp3` (untracked, .gitignore:13); ungefiltert
      sprengen sie den Cap sofort und brechen den Prune DAUERHAFT ab (Sisyphus → Phase liefert nie). Exclude-Muster
      (mind.): `__pycache__/`, `*.pyc`, `*.db*`, **`tests/tts_samples/` bzw. `*.mp3`**.
    - Top-Level *.py (app.py config.py extensions.py) analog (nicht-rekursiv im App-Root).
    - GUARD-Reihenfolge (fail-closed, VOR jeder Bewegung): (a) Manifest leer/fehlt → exit != 0; (b) `prune_guard_customer_data`:
      Kandidat beginnt mit `logs/` oder `database/` → HARTER exit != 0 (D-05); (c) `prune_guard_cap`: count > PRUNE_MAX → exit != 0 (D-02).
    - Dry-Run (default): Kandidaten in Report schreiben (stdout + Datei /opt/nerve/prune_report_<ts>.txt),
      NICHTS bewegen, Marker `[PRUNE][DRY-RUN]`. **Report-Retention (SHOULD-d):** nur die letzten N Reports behalten.
    - Scharf (PRUNE_APPLY=1): jeden Kandidaten per `mv` nach `/opt/nerve/trash/<ts>/<relpfad>` (Zielordner
      `mkdir -p`), relative Struktur erhalten. Danach alte trash-Dirs bis auf die letzten 2 entfernen. Marker `[PRUNE][APPLIED]`.
    - **set -e-Sicherheit (SHOULD-b):** Zähler als `COUNT=$((COUNT+1))` (NIE `((COUNT++))` — liefert exit 1 bei 0→1
      unter set -e), grep-Exclude mit `|| true` gegen No-Match-exit-1.
    - Exit 0 bei sauberem Lauf (auch wenn 0 Kandidaten). Idempotent + KEINE Nebenwirkung auf App-Code.
  </behavior>

  <action>
Erzeuge `scripts/prune_orphans.sh` (bash, `set -euo pipefail`). Konkrete Pflicht-Elemente (grep-geprüft):
- Whitelist-Array EXAKT: `PRUNE_DIRS=(templates static routes services scripts tests alembic config tools nerve_rt docs)`.
  **NICHT env-überschreibbar** (kein `PRUNE_DIRS="${…:-`-Muster — Prod-Scope bleibt fix, MUST-3).
- Top-Level-Datei-Liste: `PRUNE_TOP_FILES=(app.py config.py extensions.py)`.
- Konstanten: `APP_DIR="${APP_DIR:-/opt/nerve/app}"`, `PRUNE_MAX="${PRUNE_MAX:-30}"`, `TS="$(date -u +%Y%m%dT%H%M%SZ)"`,
  `TRASH_DIR="/opt/nerve/trash/$TS"`.
- **Funktionen (MUST-3):** `prune_compute_candidates`, `prune_guard_customer_data`, `prune_guard_cap`, `prune_apply`
  + Dry-Run-Report-Funktion. Main-Guard `if [ "${BASH_SOURCE[0]:-$0}" = "$0" ]; then … fi` am Ende — Skript ist
  sourc-bar OHNE zu laufen (Task 3 sourct + ruft `prune_guard_customer_data` direkt).
- Manifest einlesen NUL-delimitiert in ein assoziatives Set (`declare -A MANIFEST`), Key = relativer Pfad.
- **Exclude-Filter VOR Cap-Count (MUST-1a/SHOULD-b), Muster:** `__pycache__/`, `*.pyc`, `*.db*`, `tests/tts_samples/` (bzw. `*.mp3`).
- Guards in Reihenfolge a (leeres Manifest) → b (`prune_guard_customer_data`, logs/database Hard-Abort) → c (`prune_guard_cap`),
  jeder mit `exit 1` + `[PRUNE][ABORT] …`.
- Dry-Run-Zweig: Report + Retention (letzte N, SHOULD-d). Apply-Zweig (`[ "${PRUNE_APPLY:-0}" = "1" ]`): `mkdir -p` + `mv` + trash-Retention (letzte 2).
- **set -e-safe (SHOULD-b):** `COUNT=$((COUNT+1))` (NIE `((COUNT++))`); grep-Exclude mit `|| true`.
- KEIN `rm -rf` auf App-Pfade (nur `mv`; `rm` NUR auf alte /opt/nerve/trash bzw. Reports jenseits der Retention).
WICHTIG: KEIN App-Code-Import, KEIN DB-Zugriff. Whitelist NICHT env-überschreibbar. Kommentare referenzieren D-01..D-06 + die Fable-Fixes.
  </action>

  <verify>
    <automated>
    Lokal (Syntax, KEIN Acceptance — HART Kein-Local-Dev): `bash -n scripts/prune_orphans.sh`.
    Acceptance = Task 3 Guard-Test im server-side deploy.sh-Gate (siehe dort).
    grep-Belege:
    grep -c 'PRUNE_DIRS=(templates static routes services scripts tests alembic config tools nerve_rt docs)' scripts/prune_orphans.sh   # 1
    grep -c 'PRUNE_APPLY' scripts/prune_orphans.sh   # >=1
    grep -c 'prune_guard_customer_data' scripts/prune_orphans.sh   # >=1 (sourc-bare Guard-Funktion, MUST-3)
    grep -cE 'tts_samples|\*\.mp3' scripts/prune_orphans.sh   # >=1 (MUST-1a Exclude)
    grep -c 'BASH_SOURCE' scripts/prune_orphans.sh   # >=1 (Main-Guard, sourc-bar)
    grep -c '((COUNT++))' scripts/prune_orphans.sh   # 0 (set -e-safe, SHOULD-b — kein Post-Increment)
    grep -c 'mv ' scripts/prune_orphans.sh   # >=1 (Quarantäne, kein rm auf App)
    grep -c 'PRUNE_MAX' scripts/prune_orphans.sh   # >=1
    </automated>
  </verify>

  <acceptance_criteria>
    - `bash -n scripts/prune_orphans.sh` exit 0 (gültige Syntax).
    - Whitelist-Array exakt vorhanden (grep == 1) UND NICHT env-überschreibbar (kein `PRUNE_DIRS="${`-Muster, MUST-3).
    - **MUST-3:** Kern-Logik in Funktionen (`prune_guard_customer_data` etc.) + Main-Guard `BASH_SOURCE` → sourc-bar ohne zu laufen.
    - `PRUNE_APPLY`-Gate vorhanden; ohne `=1` KEINE `mv`-Bewegung (Dry-Run default).
    - **MUST-1a/SHOULD-b:** Exclude-Filter läuft VOR dem Cap-Count und schließt `tests/tts_samples/`/`*.mp3` + `__pycache__` aus.
    - logs/database-Hard-Abort in `prune_guard_customer_data`, VOR jeder Bewegung.
    - Cap-Guard `PRUNE_MAX` default 30; > Cap → exit 1 ohne Bewegung.
    - Bewegung ist `mv` nach `/opt/nerve/trash/`; KEIN `rm` auf `$APP_DIR`-Pfade.
    - NUL-delimitiert: `-print0` und `read -r -d ''` vorhanden. **set -e-safe:** `grep -c '((COUNT++))'` == 0 (SHOULD-b).
  </acceptance_criteria>

  <done>
scripts/prune_orphans.sh existiert als isolierte, syntaktisch gültige, NUL-sichere Prune-Funktion mit
Whitelist-Scope, drei fail-closed Guards (leeres Manifest / logs-database-Hard-Abort / Cap), Dry-Run-Default
und Quarantäne-statt-rm. Kein App-Code, kein DB-Zugriff.
  </done>
</task>

<task type="auto">
  <name>Task 2: deploy.sh — Manifest lokal erzeugen + scp + Prune-Aufruf NACH Restart (D-02, isoliert)</name>
  <files>deploy.sh</files>

  <read_first>
    - deploy.sh:85-96 (scp-Muster für nginx/service — Manifest analog transportieren).
    - deploy.sh:99-240 (Remote-Heredoc-Struktur) + :237 (systemctl restart) + :242 (Done, lokal).
    - deploy.sh:51-70 (TAR_EXCLUDES — hier kommt genau `--exclude='./tests/tts_samples'` dazu, MUST-1b; Manifest via scp nach /root, nicht ins App-tar).
    - deploy.sh:99-100 (`ssh … bash -s << ENDHEREDOC` + `set -e` — WARUM der Prune NICHT hier rein darf, MUST-4).
    - .gitignore (tts_samples/*.mp3 sind untracked — Grund für MUST-1b, sonst Sisyphus).
    - scripts/prune_orphans.sh (Task 1 — die aufzurufende Funktion).
  </read_first>

  <behavior>
    - VOR dem tar-Upload, lokal: Manifest erzeugen —
      `git ls-files -z templates static routes services scripts tests alembic config tools nerve_rt docs app.py config.py extensions.py > "$MANIFEST_LOCAL"` (NUL-delimitiert).
    - Manifest per scp zum Server nach **/root/nerve_prune_manifest** (Muster wie :85; /root statt /tmp = Symlink-Härtung, SHOULD-c). Das App-tar bleibt unberührt.
    - **TAR_EXCLUDES (Fable MUST-1b, bewusste Rahmen-Ausnahme):** genau EINE additive Zeile `--exclude='./tests/tts_samples'`
      ergänzen, damit tar die 16 untracked mp3 (1.7MB) nicht mehr hochlädt → Prune räumt die liegenden einmal weg,
      danach stabiler Endzustand. Sonst TAR_EXCLUDES unverändert.
    - **Prune-Aufruf NUR als separater ssh NACH dem Heredoc (Fable MUST-4, PFLICHT):** NICHT im Heredoc — dort gilt
      `set -e` (deploy.sh:100), ein Prune-exit≠0 würde den Deploy-Exit NACH erfolgreichem Restart kippen. Der Aufruf
      steht nach `ENDHEREDOC` (deploy.sh:240), abgesichert mit `|| echo "[deploy][WARN] …"` (Deploy ist da bereits live).
    - PRUNE_APPLY server-side NUR durchgereicht, wenn lokal wörtlich "1" (Fable MUST-2) → Default = Dry-Run (D-03).
  </behavior>

  <action>
In deploy.sh:
1. Lokal, vor dem tar-Upload: `MANIFEST_LOCAL="$(mktemp)"; git ls-files -z templates static routes services scripts tests alembic config tools nerve_rt docs app.py config.py extensions.py > "$MANIFEST_LOCAL"`.
2. **MUST-1b:** in TAR_EXCLUDES (deploy.sh:51-70) genau eine Zeile ergänzen: `--exclude='./tests/tts_samples'`.
3. **SHOULD-c:** `scp -i "$SSH_KEY" "$MANIFEST_LOCAL" "$VPS_HOST":/root/nerve_prune_manifest` (Muster :85).
4. **MUST-4 + MUST-2:** NACH `ENDHEREDOC` (deploy.sh:240, nach dem Restart-Block, AUSSERHALB des Heredocs) als SEPARATER ssh:
   ```bash
   PRUNE_ENV="MANIFEST=/root/nerve_prune_manifest APP_DIR='$APP_DIR'"
   if [ "${PRUNE_APPLY:-0}" = "1" ]; then PRUNE_ENV="$PRUNE_ENV PRUNE_APPLY=1"; fi
   ssh -i "$SSH_KEY" "$VPS_HOST" "$PRUNE_ENV bash '$APP_DIR/scripts/prune_orphans.sh'" \
     || echo "[deploy][WARN] Prune-Schritt fehlgeschlagen — Deploy ist bereits live, Cruft manuell prüfen"
   ```
   PRUNE_APPLY nur bei wörtlich "1" (MUST-2 — NICHT `${PRUNE_APPLY:+…}`, das triggert bei jedem nicht-leeren Wert).
   NUR separater ssh, NIE im Heredoc (MUST-4). Default Dry-Run (D-03).
5. Kommentar-Block D-02/D-03 + die 4 Fable-Fixes benennen.
WICHTIG: tar-Upload (:78-79), pytest-Gate (:186-194), Restart (:237) UNVERÄNDERT. TAR_EXCLUDES bekommt GENAU die eine tts_samples-Zeile (bewusste Ausnahme MUST-1b). Manifest → /root (nicht ins App-tar).
  </action>

  <verify>
    <automated>
    Lokal (Syntax): `bash -n deploy.sh` exit 0.
    grep-Belege:
    grep -c 'git ls-files -z' deploy.sh   # >=1
    grep -c 'prune_orphans.sh' deploy.sh   # >=1
    grep -c 'nerve_prune_manifest' deploy.sh   # >=1 (/root/…, SHOULD-c)
    grep -c "exclude='./tests/tts_samples'" deploy.sh   # 1 (MUST-1b)
    grep -c 'PRUNE_APPLY:+' deploy.sh   # 0 (MUST-2 — der buggy Trigger ist WEG)
    grep -c 'PRUNE_APPLY:-0' deploy.sh   # >=1 (expliziter ==1-Check, MUST-2)
    Der Prune-ssh steht NACH 'ENDHEREDOC' (manuelle Zeilenprüfung: Prune-Zeile > ENDHEREDOC-Zeile, MUST-4).
    Server-side Acceptance = beim nächsten `bash deploy.sh production` (supervised, André): Dry-Run-Report erscheint im Deploy-Log NACH dem Restart, Deploy bleibt grün.
    </automated>
  </verify>

  <acceptance_criteria>
    - `bash -n deploy.sh` exit 0.
    - `grep -c 'git ls-files -z' deploy.sh` >= 1 (Manifest lokal, NUL-delimitiert).
    - `grep -c 'prune_orphans.sh' deploy.sh` >= 1; der Aufruf steht NACH `ENDHEREDOC` als SEPARATER ssh (MUST-4), mit `|| echo …WARN`.
    - **MUST-2:** `grep -c 'PRUNE_APPLY:+' deploy.sh` == 0; stattdessen expliziter `[ "${PRUNE_APPLY:-0}" = "1" ]`-Check (`grep -c 'PRUNE_APPLY:-0' deploy.sh` >= 1).
    - **MUST-1b:** `grep -c "exclude='./tests/tts_samples'" deploy.sh` == 1 (genau eine additive TAR_EXCLUDES-Zeile).
    - **SHOULD-c:** Manifest → `/root/nerve_prune_manifest` (nicht /tmp, nicht ins App-tar).
    - tar-Upload (:78-79), pytest-Gate (:186-194), Restart (:237) UNVERÄNDERT; TAR_EXCLUDES nur die eine tts_samples-Zeile mehr.
  </acceptance_criteria>

  <done>
deploy.sh erzeugt lokal das NUL-Manifest, scp't es nach /root, ergänzt genau die tts_samples-TAR-Exclude-Zeile
und ruft scripts/prune_orphans.sh als SEPARATER ssh NACH dem Heredoc/Restart auf (PRUNE_APPLY nur bei wörtlich "1",
Default Dry-Run, `|| WARN` — nie deploy-blockierend). Kein Eingriff in tar-Upload/Gate/Restart.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_deploy_prune_guard.py — Deploy-Gate-Guard der Prune-Logik (D-07, Wächter C)</name>
  <files>tests/test_deploy_prune_guard.py</files>

  <read_first>
    - scripts/prune_orphans.sh (Task 1 — das zu testende Verhalten).
    - deploy.sh:186-194 (der pytest-Gate, in dem dieser Test server-side mitläuft; Marker `not live and not perf`).
    - tests/conftest.py (Test-Konventionen; der Test braucht KEINE DB — reines tmp-Fixture + subprocess).
  </read_first>

  <behavior>
    - Deterministisch, DB-frei, kein Netz. ZWEI Test-Stile (Fable MUST-3):
      (A) **subprocess gegen tmp-$APP_DIR** für die aus dem Scan erreichbaren End-zu-End-Fälle
          (Orphan gelistet, leeres Manifest, Cap, Exclude-Filter, Leerzeichen-Pfad, Apply→trash).
      (B) **SOURCE-und-Funktion-direkt** für den logs/database-Hard-Abort: der ist per Konstruktion aus dem
          Whitelist-Scan NICHT erreichbar (logs/database werden nie gescannt) → der Test `source`t
          scripts/prune_orphans.sh und ruft `prune_guard_customer_data logs/x.txt` direkt auf, erwartet exit != 0.
          KEIN Prod-Scope-Override, KEIN Test-Fudging.
    - Nutzt `tmp_path` als fake $APP_DIR mit Whitelist-Dateien + Waisen + Fake-Manifest.
    - Marker: nicht mit `live`/`perf` markiert → läuft im deploy.sh-Gate.
  </behavior>

  <action>
Erzeuge `tests/test_deploy_prune_guard.py` (pytest, jede `def test_…`).
**Subprocess-Stil** (`tmp_path` als $APP_DIR, `subprocess.run([...], env=..., capture_output=True)`):
- `test_orphan_listed_dryrun` — Waise in `templates/` gelistet, Manifest-Datei nicht, nichts bewegt.
- `test_empty_manifest_aborts` — leeres Manifest → returncode != 0, App-Dateien unberührt.
- `test_cap_exceeded_aborts` — PRUNE_MAX=1, 2 Waisen → returncode != 0 (D-02).
- `test_excluded_pycache_not_candidate` — `__pycache__/x.pyc` erscheint NICHT im Report (Exclude VOR Cap, SHOULD-a).
- `test_space_path_orphan` — Waise mit Leerzeichen (z.B. `templates/a b.html`) korrekt gelistet (NUL-Schutz, T-PRUNE-06, SHOULD-a).
- `test_apply_moves_to_trash` — PRUNE_APPLY=1 → Waise weg aus Whitelist-Dir, in trash vorhanden (mv).
**Source-Stil (MUST-3, Guard direkt):**
- `test_logs_candidate_hard_abort` — `bash -c 'source scripts/prune_orphans.sh; prune_guard_customer_data logs/x.txt'` → returncode != 0.
- `test_database_candidate_hard_abort` — analog `database/y.db`.
Skip-Guard: fehlt scripts/prune_orphans.sh oder `bash` → `pytest.skip` (kein False-Green; server-Gate läuft scharf).
WICHTIG: nur `tmp_path`, kein echtes /opt/nerve, keine DB. Der Source-Stil beweist den Hard-Abort OHNE den Prod-Scan zu schwächen.
  </action>

  <verify>
    <automated>
    Server-side via deploy.sh-Gate (HART Kein-Local-Dev): `bash deploy.sh production` → der pytest-Gate
    (`pytest tests/ -m "not live and not perf"`) enthält test_deploy_prune_guard.py und ist GRÜN.
    grep-Belege lokal:
    grep -c 'def test_' tests/test_deploy_prune_guard.py   # >=8
    grep -c 'prune_guard_customer_data' tests/test_deploy_prune_guard.py   # >=1 (Source-Stil, MUST-3)
    grep -cE 'source |\. .*prune_orphans' tests/test_deploy_prune_guard.py   # >=1 (sourct das Skript)
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'def test_' tests/test_deploy_prune_guard.py` >= 8 (die oben genannten Fälle).
    - **MUST-3:** logs/database-Hard-Abort via SOURCE + direktem `prune_guard_customer_data`-Aufruf (nicht via unerreichbarem Scan); grep `prune_guard_customer_data` >= 1.
    - **SHOULD-a:** `test_space_path_orphan` (NUL/Leerzeichen) + `test_excluded_pycache_not_candidate` (Exclude vor Cap) vorhanden.
    - Test nutzt `tmp_path`; KEIN Zugriff auf echtes /opt/nerve, KEINE DB.
    - Die erreichbaren Guard-Fälle (leeres Manifest, Cap) prüfen `returncode != 0` UND dass nichts bewegt wurde.
    - Nicht mit `live`/`perf` markiert. Im server-side deploy.sh-Gate GRÜN (supervised-Beleg im SUMMARY).
  </acceptance_criteria>

  <done>
tests/test_deploy_prune_guard.py beweist die Prune-Logik deterministisch (Orphan-Erkennung, Manifest-Respekt,
alle drei Guards, Apply-nach-trash) gegen tmp-Fixtures und läuft im deploy.sh-Gate mit — Test-Netz-Ratsche steht.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prune-Skript → Kundendaten (logs/, database/) | Der Prune darf NIEMALS Call-Logs oder DBs bewegen. Zwei Schichten: Scope-Whitelist (nie im Suchbereich) + Hard-Abort (D-05). |
| Prune-Fehler → Deploy-Verfügbarkeit | Ein Prune-Bug darf künftige Deploys/Restarts NICHT blockieren. Prune läuft NACH Restart, isoliert, `|| WARN`. |
| Dry-Run → scharfer Lauf | Ohne PRUNE_APPLY=1 keine Bewegung; Erst-Prod-Lauf erzwungen Dry-Run (André verifiziert). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-PRUNE-01 | Data Loss / Tampering | Kundendaten logs//database/ | mitigate | Scope-Whitelist (D-01) + logs/database-Hard-Abort (D-05) + tar-exclude schon aktiv. Guard-Test beweist beide. |
| T-PRUNE-02 | Denial of Service | kaputter Prune blockiert Deploy | mitigate | Prune NACH Restart, isolierte Funktion, `|| WARN` (kein set -e-Abbruch). Deploy ist zum Prune-Zeitpunkt live. |
| T-PRUNE-03 | Data Loss / Over-Delete | Massen-Löschung bei fehlerhaftem Manifest | mitigate | leeres-Manifest-Abbruch + Cap N=30 (PRUNE_MAX) → fail-closed, keine Bewegung. |
| T-PRUNE-04 | Recoverability | versehentlich gepruntes Code-File | mitigate | Quarantäne statt rm (mv → /opt/nerve/trash/<ts>/, Retention 2) → reversibel binnen 2 Deploys. |
| T-PRUNE-05 | False-Green | Guard-Test skippt still auf Server | accept | Server-Gate ist Linux+bash → Test läuft scharf; Skip nur lokal/Windows. SUMMARY belegt PASSED (nicht SKIPPED) im Gate. |
| T-PRUNE-06 | Elevation / Path-Injection | Leerzeichen-/Sonderzeichen-Pfade umgehen Filter | mitigate | NUL-delimitiert durchgehend (git ls-files -z / -print0 / read -d '') → keine Wort-Split-Lücke. |
</threat_model>

<verification>
Gesamt-Phasen-Checks (server-side via deploy.sh-Gate — HART Kein-Local-Dev; supervised, André fährt):
1. **Guard grün:** `bash deploy.sh production` → pytest-Gate enthält test_deploy_prune_guard.py, PASSED (nicht SKIPPED).
2. **Erst-Lauf Dry-Run:** im Deploy-Log erscheint NACH dem Restart ein `[PRUNE][DRY-RUN]`-Report mit Kandidatenliste; NICHTS bewegt; Deploy bleibt grün. André verifiziert die Liste (keine logs/database, plausible Ex-Repo-Dateien).
3. **Scharf-Lauf (nach André-OK):** mit PRUNE_APPLY=1 → Kandidaten in /opt/nerve/trash/<ts>/, Whitelist-Dirs waisenfrei; `find $APP_DIR/<dir>` == `git ls-files <dir>` (Server == Repo für Code).
4. **Kundendaten intakt:** `/opt/nerve/app/logs` + `/opt/nerve/app/database` unverändert (kein Kandidat, kein Move).
5. **Isolations-Beleg:** ein absichtlich fehlerhafter Prune (z.B. leeres Manifest) bricht NUR den Prune ab, nicht den Deploy (Service läuft).
6. **git diff:** AUSSCHLIESSLICH scripts/prune_orphans.sh (neu), deploy.sh, tests/test_deploy_prune_guard.py (neu). Kein App-Code.
</verification>

<success_criteria>
- 3 Dateien: scripts/prune_orphans.sh (neu), deploy.sh (additiv), tests/test_deploy_prune_guard.py (neu). Kein NERVE-App-Code.
- Manifest-Diff (Weg 1) mit allen 5 Sicherheits-Schichten (Scope-Whitelist, Cap, Dry-Run/Erst-Dry-Run, Quarantäne, logs/database-Hard-Abort).
- deploy.sh-Gate grün inkl. neuem Guard-Test (PASSED, nicht SKIPPED).
- Erst-Prod-Lauf Dry-Run von André verifiziert, DANN scharf; Server == Repo für Code, Kundendaten intakt.
- Prune blockiert nie Deploy/Restart (isoliert, nach Restart, `|| WARN`).
- Punkt 17/27 gewahrt: additiver, einfachster tragfähiger Weg; kein deploy.sh-Rewrite, kein App-Change.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.DEPLOY-PRUNE-deploy-hygiene-waisen-prune/08.23.2.DEPLOY-PRUNE-01-SUMMARY.md`.
Dokumentiere: Guard-Test-Ergebnis im Gate (PASSED/SKIPPED, >=8 Tests), Dry-Run-Report-Auszug (Kandidaten),
André-Verifikation, ob scharfgeschaltet (PRUNE_APPLY) + trash-Beleg, Kundendaten-intakt-Beleg, git-diff-Scope.
**Bekannte Restlücke (Fable MUST-1, explizit dokumentieren):** Manifest = git-Index (`git ls-files`), tar packt
aber den Working-Tree → künftige UNTRACKED Dateien in Whitelist-Dirs werden von tar hochgeladen und erscheinen als
Kandidat im Dry-Run-Report (nicht auto-gelöscht, da Dry-Run default). Der Dry-Run-Report ist der Wächter dieser
Lücke; der Wurzelfix (tar aus git ls-files statt Working-Tree) ist vertagt. tts_samples ist der bisher einzige
bekannte Fall und via TAR-Exclude entschärft.
</output>
