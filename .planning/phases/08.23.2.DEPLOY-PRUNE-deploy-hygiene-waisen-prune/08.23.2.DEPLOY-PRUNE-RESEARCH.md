# Phase 08.23.2.DEPLOY-PRUNE — Research / Verbindungs-Karte

**Erstellt:** 2026-07-07 · Alle Fakten am **live deploy.sh** verifiziert (lokal, git-synchron) + `git ls-files`-Belege.

## 1. Root-Cause (bewiesen, nicht angenommen)

| Fakt | Beleg |
|------|-------|
| Deploy mergt statt zu ersetzen | `deploy.sh:78-79` `tar -cf - ./ \| ssh … "tar -xf - -C $APP_DIR --no-same-owner"` — tar-extract überschreibt/ergänzt, **löscht nie** server-only Dateien |
| rsync ist keine Option | `deploy.sh:5` „Works on Windows Git-Bash (no rsync required)" |
| Kundendaten IM App-Dir | `deploy.sh:104` `mkdir -p /opt/nerve/app/logs`; `services/live_session.py:37` `LOG_DIR=…/logs`; chown `:105` |
| logs/database bereits tar-excluded | `deploy.sh:58` `logs`, `:68-69` `database/*.db` |
| pytest-Gate-Andockstelle (Wächter) | `deploy.sh:186-194` `pytest tests/ -m "not live and not perf"` (server-side, im Deploy-Heredoc) |
| Restart-Punkt (Prune läuft danach) | `deploy.sh:237` `systemctl restart` |
| scp-Muster für Zusatzdateien | `deploy.sh:85-96` (nginx/service-Units) → Manifest analog transportieren |
| Server hat kein `.git` | `deploy.sh:52` `--exclude='./.git'` → Manifest MUSS lokal erzeugt + transportiert werden |

## 2. Prune-Scope-Katalog (live git-verifiziert 2026-07-07)

**Whitelist-Code-Dirs (alle existieren, git-tracked):** templates(38) static(33) routes(24) services(44) scripts(23) tests(124) alembic(36) config(2) tools(1) nerve_rt(15) docs(4). **Top-Level `*.py`:** app.py, config.py, extensions.py.

**Getrackte Top-Level-Dirs die NICHT in der Whitelist sind — und warum sicher:**
- `logs/`, `.env`(n/a im Dir) → Kundendaten/Secrets, strukturell + explizit tabu (D-01/D-05/D-06).
- `database/` → Kundendaten-`.db`; bewusster Scope-Gap (database/*.py-Waisen nicht auto-gepruned, akzeptiert).
- `.planning/`, `.claude/`, `deploy/`, `03 Planung/` → tar-excluded ODER nicht sicherheitsrelevant; nicht im Scope (harmlos, kein Prune nötig).
- venv `/opt/nerve/venv`, `.deploy_meta` `/opt/nerve/`, `.env` `/etc/nerve/` → **außerhalb** `$APP_DIR` → nie Kandidat.

## 3. Drei-Wege-Vergleich (Leitsatz 3 — André entschied Weg 1)

| Kriterium | Weg 1 Manifest-Diff | Weg 2 Whitelist-Muster | Weg 3 Staged Dry-Run |
|---|---|---|---|
| Vollständigkeit (fängt neue Waisen) | ✅ alle inkl. tote .py | ❌ nur kuratierte | ✅ (Diff-basiert) |
| Fehl-Lösch-Risiko | mittel (Scope+Cap+Quarantäne mildern) | niedrig | sehr niedrig |
| Automatisierungsgrad | hoch | hoch | niedrig (Extra-Handgriff) |
| Pflege-Aufwand | niedrig | hoch (Liste pflegen) | niedrig |
| Komplexität | mittel | niedrig | mittel |

**André-Entscheidung:** **Weg 1** (vollständig, fängt gefährliche tote .py), **entschärft** durch: Scope-Whitelist (D-01), Cap N=30 (D-02), erzwungener Erst-Dry-Run (D-03), Quarantäne statt rm (D-04), logs/database-Hard-Abort (D-05). Rollout = isolierte Funktion NACH Restart (D-02, kein Deploy-Blocker).

## 4. Persistenz-/Sicherheits-Schicht (Punkt 21)

| Objekt | Pfad | Prune-Berührung | Schutz |
|---|---|---|---|
| Call-Logs | `/opt/nerve/app/logs/*` | NIE | nicht in Whitelist + D-05 Hard-Abort + tar-exclude |
| SQLite-DBs | `/opt/nerve/app/database/*.db` | NIE | nicht in Whitelist + D-05 + tar-exclude `:68-69` |
| Code (11 Dirs + top .py) | `$APP_DIR/<whitelist>` | Manifest-Diff | Cap + Quarantäne + Erst-Dry-Run |
| venv / .deploy_meta / .env | außerhalb `$APP_DIR` | NIE | strukturell außerhalb Scope |

**Punkt 26 (Schreib-Zeitpunkt):** Prune läuft NACH Restart, einmalig, synchron — kein nebenläufiger Schreiber; keine Race gegen live Call-Logs (die sind ohnehin out-of-scope).
