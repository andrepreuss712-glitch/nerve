---
phase: 08.23.2.DEPLOY-PRUNE-deploy-hygiene-waisen-prune
status: passed
verified: 2026-07-08
method: server-side deploy.sh production gate (HART Kein-Local-Dev) + supervised Dry-Run→scharf + ssh-Server-Verifikation
requirements: NONE (AUTH-1-Live-Befund Ghost-Cruft, keine REQ-IDs gemappt)
---

# VERIFICATION — 08.23.2.DEPLOY-PRUNE (Deploy-Hygiene Waisen-Prune)

**Verdikt: PASSED.** Phasen-Ziel erreicht — deploy.sh räumt im Repo gelöschte Code-Dateien server-seitig
ab (Server == Repo für Code), ohne Kundendaten zu gefährden und ohne den Deploy blockieren zu können.
Verifiziert am realen `bash deploy.sh production`-Gate + supervised Dry-Run→scharf + ssh-Server-Check.
Dienst neu gestartet 2026-07-08 06:20:32 UTC.

## Goal-backward: Phasen-Ziel
> „deploy.sh räumt im Repo gelöschte Dateien AUCH server-seitig ab (toter Cruft in /opt/nerve/app)."

Erreicht: 5 tote Waisen erkannt (Dry-Run), verifiziert, per Quarantäne entfernt; Server-`find` bestätigt
sie weg; Kundendaten intakt.

## Acceptance-Kriterien (alle server-side bestätigt)

| # | Kriterium | Ergebnis |
|---|-----------|----------|
| 1 | Deploy-Gate grün inkl. Guard-Test | **906 passed**, `test_deploy_prune_guard.py` (9) **PASSED nicht SKIPPED** |
| 2 | Erst-Lauf erzwungen Dry-Run, nichts bewegt | `[PRUNE][DRY-RUN]` 5 Kandidaten, keine Bewegung, Deploy grün |
| 3 | Kandidaten von André+Claudian verifiziert | alle 5 untracked + unreferenziert = echt tot, keine Kundendaten |
| 4 | Scharf = Quarantäne statt rm | `[PRUNE][APPLIED] 5` → `/opt/nerve/trash/20260708T062033Z/` (mv) |
| 5 | Server == Repo für Code | ssh: alle 5 aus `$APP_DIR` weg + im Papierkorb |
| 6 | Kundendaten intakt | ssh: `logs/` 249 Dateien, `database/` 9 — unberührt |

## must_haves (Plan) — alle erfüllt
- ✅ Prune räumt server-seitig ab (5 Waisen → trash).
- ✅ Kein Kundendaten-Verlust (Scope-Whitelist + logs/database-Hard-Abort + intakt-Beleg).
- ✅ Selbst-Aussperr-Schutz (Prune isoliert nach Restart, `|| WARN`, Cap N=30 — Deploy nie blockiert).
- ✅ Erst-Rollout sicher (erzwungener Dry-Run, dann `PRUNE_APPLY=1`; Quarantäne reversibel).
- ✅ Wächter grün (9 Tests PASSED, inkl. Source-Stil-Hard-Abort + Positiv-Kontrolle, kein Vakuum-Grün).
- ✅ Rahmen gewahrt: nur deploy.sh + scripts/prune_orphans.sh + test + .gitattributes (bewusste 4. Datei,
  André-Entscheidung, CRLF-Härtung) — kein NERVE-App-Code.

## Entfernte Waisen (Quarantäne 20260708T062033Z)
`templates/login.html` · `templates/admin/feedback_notification.html` · `static/app.js` ·
`routes/dashboard.py.bak` · `scripts/export_ft_jsonl.py`

## Restlücke / Folgefund
- **Bekannte Restlücke:** Manifest = git-Index vs tar = Working-Tree → künftige untracked Whitelist-Dateien
  erscheinen im Dry-Run-Report (Report = Wächter; tar-Wurzelfix vertagt).
- **DSGVO-Nebenfund → Backlog 999.7:** git-getrackter Call-Log `logs/salesnerve_log_2026-03-2X…txt` prüfen/entfernen.

## Übersprungen (Multi-Segment-Gotcha)
gsd-verifier/gsd-code-review umgangen; hand-authored. Acceptance = realer server-side Deploy-Gate + supervised
Verifikation, nicht ein Agent-Report.
