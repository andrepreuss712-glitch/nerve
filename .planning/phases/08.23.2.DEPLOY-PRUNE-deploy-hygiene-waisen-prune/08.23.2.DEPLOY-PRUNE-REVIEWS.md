# Phase 08.23.2.DEPLOY-PRUNE — Cross-AI Review (Fable, Final) + Einarbeitung

**Reviewer:** Fable (3. Sicht), am echten Code (deploy.sh + Plan) read-only. **Datum:** 2026-07-07.
**Ergebnis:** 4 MUST-Fixes + 4 SHOULD, alle Plan-Text. Eingearbeitet via `/gsd-plan-phase --reviews`. Rahmen unverändert (3 Dateien, kein App-Code).

## MUST (4) — alle eingearbeitet

| # | Finding | Fix im Plan | Beleg |
|---|---------|-------------|-------|
| MUST-1 | **Sisyphus (tts_samples):** `tests/tts_samples/` = 16 untracked mp3 (.gitignore), tar lädt sie jeden Deploy hoch, liegen in Whitelist-Dir `tests/` → Prune quarantänisiert sie jeden Lauf; 16 > ½ von Cap N=30 → mit echten Ghost-Waisen Cap-Überschreitung → Prune bricht IMMER ab, Phase liefert nie. | (a) Prune-Exclude um `tests/tts_samples/`/`*.mp3` (Task 1); (b) genau 1 additive `--exclude='./tests/tts_samples'`-Zeile in TAR_EXCLUDES (Task 2). Restlücke Manifest=git-Index vs tar=Working-Tree dokumentiert (Dry-Run-Report = Wächter). | CONTEXT D-08, Truth „Sisyphus vermieden", Task 1/2 |
| MUST-2 | **PRUNE_APPLY-Bug:** `${PRUNE_APPLY:+PRUNE_APPLY=1}` triggert bei JEDEM nicht-leeren Wert → `PRUNE_APPLY=0` würde scharf schalten. | Expliziter Check `[ "${PRUNE_APPLY:-0}" = "1" ]`, nur bei wörtlich „1" durchreichen. grep-Guard: `PRUNE_APPLY:+`==0. | CONTEXT D-10, Task 2 |
| MUST-3 | **Hard-Abort-Test-Naht:** logs/database-Hard-Abort ist aus dem Whitelist-Scan unerreichbar → Test kann Guard nicht auslösen → Executor improvisiert (Scope-Override = Prod-Schwächung ODER Test-Fudge = False-Green). | Skript in Funktionen (`prune_guard_customer_data` etc.) + `BASH_SOURCE`-Main-Guard; Test SOURCT + ruft Guard direkt mit synthetischem Kandidat. Whitelist NICHT env-überschreibbar. | CONTEXT D-09, Task 1/3 |
| MUST-4 | **Heredoc-Zweideutigkeit:** „eigener ssh ODER am Ende des Heredocs" — im Heredoc gilt `set -e` (deploy.sh:100), ein Prune-exit≠0 kippt den Deploy-Exit NACH erfolgreichem Restart. | NUR separater ssh NACH `ENDHEREDOC`, mit `|| echo WARN`. Als Pflicht festgelegt. | CONTEXT D-10, Task 2 |

## SHOULD (4) — eingearbeitet

| # | Finding | Fix |
|---|---------|-----|
| SHOULD-a | Test-Abdeckung dünn | +`test_space_path_orphan` (NUL/Leerzeichen, T-PRUNE-06) +`test_excluded_pycache_not_candidate` (Exclude vor Cap). Tests jetzt >=8. |
| SHOULD-b | Exclude-vor-Cap + set-e-Fallen | Exclude-Filter ZWINGEND vor Cap-Count (Server >60 `__pycache__`); `COUNT=$((COUNT+1))` statt `((COUNT++))`; grep-Exclude `|| true`. |
| SHOULD-c | /tmp Symlink-Risiko | Manifest → `/root/nerve_prune_manifest`. |
| SHOULD-d | Report-Ansammlung | Dry-Run-Report-Retention (letzte N). |

## Nächster Schritt
Kurzer Fable-Recheck der 4 Fixes → Claudian Pre-Execute-Audit → `/gsd-execute-phase 08.23.2.DEPLOY-PRUNE` (supervised, Erst-Lauf erzwungen Dry-Run).
