---
phase: 08.23.2.D.UX
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - alembic/versions/0007_add_score_fields_to_calls.py
  - database/models.py
autonomous: true
requirements:
  - REQ-D.UX-11

must_haves:
  truths:
    - "calls.score_breakdown JSONB-Spalte existiert (nullable)"
    - "calls.score_schema_version SMALLINT NOT NULL DEFAULT 1 existiert"
    - "coaching_score wird NICHT erneut hinzugefügt (bereits vorhanden)"
    - "Alembic zeigt Migration 0007 als applied"
    - "downgrade() entfernt beide Spalten vollständig"
  artifacts:
    - path: "alembic/versions/0007_add_score_fields_to_calls.py"
      provides: "Alembic migration: score_breakdown JSONB + score_schema_version SMALLINT"
      exports: ["upgrade", "downgrade"]
    - path: "database/models.py"
      provides: "Call model updated with score_breakdown + score_schema_version columns"
      contains: "score_breakdown.*score_schema_version"
  key_links:
    - from: "alembic/versions/0007_add_score_fields_to_calls.py"
      to: "calls table on production"
      via: "alembic upgrade head"
      pattern: "batch_alter_table.*score_breakdown.*score_schema_version"
---

<objective>
Alembic Migration 0007: fügt `calls.score_breakdown JSONB NULL` und `calls.score_schema_version SMALLINT NOT NULL DEFAULT 1` zur calls-Tabelle hinzu. coaching_score ist BEREITS vorhanden und wird NICHT neu hinzugefügt. models.py synchron halten.

Purpose: Wave 4 (correct_outcome-Endpoint) schreibt nach Outcome-Confirm `score_breakdown` (9-Schlüssel-JSONB) + setzt `score_schema_version=1`. Ohne diese Spalten schlägt der DB-Write fehl.
Output: Migration 0007, aktualisiertes Call-Modell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-SPEC.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-CONTEXT.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-RESEARCH.md

<interfaces>
<!-- Aus database/models.py Z.1-15: JSON_TYPE Pattern für JSONB -->
```python
from sqlalchemy import JSON as _JSON
from sqlalchemy.dialects.postgresql import JSONB as _JSONB
JSON_TYPE = _JSON().with_variant(_JSONB(), 'postgresql')
# Dieses Pattern für score_breakdown verwenden (analog zu meddpicc_extracted)
```

<!-- Aus database/models.py Z.664: coaching_score BEREITS vorhanden -->
```python
coaching_score = Column(Float, nullable=True)  # BEREITS DA — NICHT in Migration 0007 aufnehmen
```

<!-- score_breakdown JSON-Schema (9 Schlüssel, alle required nach erstem Write) -->
```json
{
  "schema_version": 1,
  "kb_end_norm": <float 0-100>,
  "behandelt_rate": <float 0-1>,
  "redeanteil_score": <float 0-100>,
  "skript_norm": <float 0-100>,
  "frage_qualitaet": 0,
  "outcome_modifier": <float>,
  "process_score": <float 0-100>,
  "final_score": <int 0-100>,
  "computed_at_iso": "<ISO-8601 string>"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-Execute Audit + Migration 0007 erstellen</name>
  <files>alembic/versions/0007_add_score_fields_to_calls.py</files>
  <read_first>
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
      → Bestätigen: coaching_score BEREITS vorhanden (NICHT nochmal hinzufügen)
      → Bestätigen: score_breakdown NICHT vorhanden
      → Bestätigen: score_schema_version NICHT vorhanden
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
      → Bestätigen: 0006 ist applied (0007 depends_on 0006)
    - database/models.py Z.1-15 (JSON_TYPE Definition — verwenden für score_breakdown)
    - database/models.py Z.644-684 (vollständiges Call-Modell)
    - alembic/versions/0005_add_outcome_fields_to_calls.py Z.1-35 (revision/down_revision Kopfzeilen-Muster)
    - HINWEIS: Plans 01 + 02 laufen in Wave 0 parallel. 0007 depends_on 0006 in Alembic-Revision-Chain.
      In der Migration-Datei: `down_revision = '0006'`. Wenn Plan 01 noch nicht applied:
      erst 0006 deployen (Plan 01), dann 0007 deployen (dieser Plan).
  </read_first>

  <action>
**KRITISCH VOR BEGINN:** Falls inspect.sh zeigt dass 0006 noch nicht applied ist (Plan 01 lief parallel), warte bis Plan 01 completed ist. Dann erst 0007 erstellen + deployen.

**Migration 0007 erstellen:**

Datei: `alembic/versions/0007_add_score_fields_to_calls.py`

```python
"""Add score_breakdown and score_schema_version to calls.

Revision ID: 0007
Revises: 0006
Create Date: 2026-05-28

Note: coaching_score (FLOAT) already existed before this migration
(present in initial schema, confirmed via inspect.sh 2026-05-28).
This migration ONLY adds score_breakdown + score_schema_version.
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import JSONB as _JSONB

revision = '0007'
down_revision = '0006'
branch_labels = None
depends_on = None

# JSON type with PostgreSQL JSONB variant (matches models.py JSON_TYPE pattern)
_JSON_TYPE = sa.JSON().with_variant(_JSONB(), 'postgresql')


def upgrade() -> None:
    with op.batch_alter_table('calls') as batch_op:
        # coaching_score already exists — DO NOT add it
        batch_op.add_column(
            sa.Column('score_breakdown', _JSON_TYPE, nullable=True)
        )
        batch_op.add_column(
            sa.Column('score_schema_version', sa.SmallInteger(), nullable=False,
                      server_default='1')
        )


def downgrade() -> None:
    with op.batch_alter_table('calls') as batch_op:
        batch_op.drop_column('score_schema_version')
        batch_op.drop_column('score_breakdown')
```

**Deploy:**
```bash
git add alembic/versions/0007_add_score_fields_to_calls.py
git commit -m "feat(08.23.2.D.UX-02): migration 0007 - add score_breakdown + score_schema_version"
git push
bash deploy.sh production
```
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
# Erwartete Ausgabe: score_breakdown vorhanden (nullable JSONB), score_schema_version vorhanden (NOT NULL, DEFAULT 1)
# coaching_score ebenfalls vorhanden (war vorher schon da — nicht gedoppelt)
    </automated>
  </verify>

  <acceptance_criteria>
- ssh inspect.sh schema calls → score_breakdown Spalte vorhanden, nullable
- ssh inspect.sh schema calls → score_schema_version Spalte vorhanden, NOT NULL, DEFAULT 1
- ssh inspect.sh sample calls 10 → score_schema_version=1 in allen Rows (server_default greift)
- ssh inspect.sh migrations → 0007 applied
- coaching_score weiterhin vorhanden (nur einmal, nicht gedoppelt)
  </acceptance_criteria>

  <done>Migration 0007 auf Production deployed. score_breakdown + score_schema_version Spalten vorhanden. coaching_score nicht dupliziert.</done>
</task>

<task type="auto">
  <name>Task 2: database/models.py um score_breakdown + score_schema_version erweitern</name>
  <files>database/models.py</files>
  <read_first>
    - database/models.py Z.644-685 (vollständiges Call-Modell inkl. __table_args__)
    - database/models.py Z.1-15 (JSON_TYPE Definition — für score_breakdown Column verwenden)
    - database/models.py Z.685+ (CallEvent Model — nicht überschreiben)
    - Prüfe ob Plan 01 (0006-Migration) models.py bereits angepasst hat: followup_intent Column + neues ck_calls_outcome. Wenn ja: nur score_breakdown + score_schema_version hinzufügen, bestehende Änderungen erhalten.
  </read_first>

  <action>
In `database/models.py`, in der `Call`-Klasse NACH der `coaching_score`-Zeile (Z.664), folgende zwei Columns einfügen:

```python
    score_breakdown = Column(JSON_TYPE, nullable=True)
    score_schema_version = Column(SmallInteger, nullable=False, server_default='1')
```

ACHTUNG: `SmallInteger` muss aus SQLAlchemy importiert sein. Prüfe Imports in models.py Z.1-30. Falls `SmallInteger` noch nicht importiert: Import-Zeile ergänzen:
```python
from sqlalchemy import ..., SmallInteger
```

Die `__table_args__` benötigen KEINE Änderung für diese zwei Spalten (kein CHECK-Constraint nötig).

**SYNC-CHECK:** Nach dem Edit — models.py `__table_args__` muss IDENTISCH zur Migration 0007 sein (für batch_alter_table rebuild auf SQLite). score_breakdown + score_schema_version haben keine CHECK-Constraints, daher kein __table_args__-Eintrag nötig.

**Commit zusammen mit oder nach Task 1:**
```bash
git add database/models.py
git commit -m "feat(08.23.2.D.UX-02): update Call model with score_breakdown + score_schema_version"
git push
```
(Kann in denselben Commit wie Task 1 gebündelt werden)
  </action>

  <verify>
    <automated>
ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "score_breakdown"'
# Erwartet: Treffer in database/models.py (deployed Stand)
    </automated>
  </verify>

  <acceptance_criteria>
- database/models.py enthält score_breakdown = Column(JSON_TYPE, nullable=True)
- database/models.py enthält score_schema_version = Column(SmallInteger, nullable=False, server_default='1')
- SmallInteger ist importiert in models.py
- models.py und Migration 0007 sind konsistent (identische Constraint-Definitionen)
  </acceptance_criteria>

  <done>models.py um score_breakdown + score_schema_version erweitert. Synchron mit Migration 0007.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration-Script → PostgreSQL | DDL-Statements für neue Spalten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D.UX-02-01 | Tampering | score_breakdown JSONB | mitigate | Schema-Version-Feld (score_schema_version) ermöglicht spätere Migration ohne Datenverlust |
| T-D.UX-02-02 | Denial | Doppeltes Hinzufügen von coaching_score | mitigate | Expliziter Kommentar in Migration-Datei + pre-execute inspect.sh audit verhindert Duplikat |
| T-D.UX-02-03 | Information Disclosure | score_breakdown enthält Gesprächs-Metriken | accept | Nur intern zugänglich (authenticated routes); keine PII-Daten in score_breakdown |
</threat_model>

<verification>
Nach Deploy:
1. `ssh inspect.sh schema calls` — score_breakdown + score_schema_version vorhanden
2. `ssh inspect.sh sample calls 5` — score_schema_version=1 in allen Rows
3. `ssh inspect.sh migrations` — 0007 listed
</verification>

<success_criteria>
- Migration 0007 deployed
- score_breakdown (JSONB nullable) + score_schema_version (SMALLINT DEFAULT 1) vorhanden
- coaching_score nicht dupliziert
- models.py synchron mit beiden Migrationen (0006 + 0007)
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-02-SUMMARY.md` erstellen.
</output>
