---
phase: 08.23.2.D.UX
plan: 05
type: execute
wave: 2
depends_on:
  - "08.23.2.D.UX-01"
  - "08.23.2.D.UX-03"
  - "08.23.2.D.UX-04"
files_modified:
  - static/pip-launcher.js
autonomous: false
requirements:
  - REQ-D.UX-9
  - REQ-D.UX-10

must_haves:
  truths:
    - "nlp-section-postcall ist nach outcome_ready initial display:none"
    - "7 Outcome-Buttons immer alle sichtbar (keine Anzeige-Conditional)"
    - "Bei confidence >= 0.70: Haiku-Vorschlag pre-selected (teal), Bestätigen-Button aktiv"
    - "Bei confidence < 0.70: Unsicher-Badge sichtbar (Teal-Outline), Bestätigen-Button disabled bis User klickt"
    - "Klick auf anderen Button: altes Highlight weg, neues an — Bestätigen bleibt aktiv"
    - "Bestätigen-Klick: Spinner 'Bewertung wird berechnet...', POST an correct_outcome mit {outcome, followup_intent}"
    - "Auto-Confirm-Countdown-Logik ist vollständig entfernt"
    - "Nach Score-Response: nlp-section-postcall wird display:block, Outcome-Sektion kollabiert zu 1-Zeiler"
    - "outcome_source korrekt: bestätigt >=0.70 → ai_auto, bestätigt <0.70-Unsicher → ai_auto_unsicher, andere Wahl → user_corrected"
    - "sec.dataset.outcomeRendered = '1' Idempotenz-Guard bleibt erhalten"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "_renderOutcomeUx() Neufassung, _outcomeLabelDe() erweitert"
      contains: "followup_intent.*outcome_source.*ai_auto"
  key_links:
    - from: "pip-launcher.js outcome_ready handler Z.2443"
      to: "_renderOutcomeUx()"
      via: "state.socket.on('outcome_ready')"
      pattern: "_renderOutcomeUx.*data"
    - from: "_renderOutcomeUx() Bestätigen-Button"
      to: "correct_outcome endpoint"
      via: "fetch('/api/calls/'+callId+'/correct_outcome')"
      pattern: "correct_outcome.*followup_intent"
    - from: "correct_outcome Response"
      to: "nlp-section-postcall"
      via: "json.final_score → display:block"
      pattern: "nlp-section-postcall.*display.*block"
---

<objective>
Kompletter Umbau von `_renderOutcomeUx()` in pip-launcher.js: vom alten 30s-Auto-Confirm-Modell zur neuen dreistufigen Inline-Sektion mit 7 Buttons, Score-Gate (hidden bis Confirm), followup_intent-Ableitung, outcome_source-Mapping per D-W3-06, Auto-Confirm-Countdown entfernt.

Purpose: "wenn es nebenher beim scoring irgendwo im pip auftaucht wird es keine sau jucken" (André 2026-05-27) — Outcome ist jetzt Pflicht-Schritt VOR Score-Reveal.
Output: pip-launcher.js mit neuem _renderOutcomeUx(), erweitertem _outcomeLabelDe(), korrektem _showPostcall()-Score-Gate.

UI-Konformitäts-Check (Pflicht bei UI-Änderungen):
1. Brand-Tokens: Alle Farben via var(--teal-*) oder var(--neutral-*) aus nerve.css — keine hardcoded Hex
2. Unsicher-Badge: Teal-Outline (NICHT gelb — CLAUDE.md HART Punkt 8)
3. disabled-State: n-btn-ghost pattern aus nerve.css
4. 7 Buttons: CSS grid 3+4 oder 2+5 Layout (prüfe existierendes PiP-Grid-Pattern)
5. Touch-Targets: 44px minimum für alle 7 Buttons
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-SPEC.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-CONTEXT.md

<interfaces>
<!-- Aktuelles _outcomeLabelDe() Z.3734-3744 — muss auf 7 Klassen erweitert werden: -->
```javascript
function _outcomeLabelDe(val) {
  var m = {
    meeting_booked: 'Termin gebucht',
    callback: 'Rückruf',
    no_interest: 'Kein Interesse',
    wrong_person: 'Falsche Person',
    contract_signed: 'Abschluss',
    unknown: 'Unklar',
  };
  return m[val] || (val || '-');
}
```

<!-- D-W3-01: 7 Outcome-Typen + Labels (verbatim) -->
```javascript
var OUTCOME_OPTS = [
  ['meeting_booked',    'Termin gebucht'],
  ['callback',          'Rückruf vereinbart'],
  ['send_info',         'Infos zusenden'],
  ['wrong_person',      'Falsche Person'],
  ['gatekeeper_blocked','Gatekeeper blockt'],
  ['no_interest',       'Kein Interesse'],
  ['contract_signed',   'Abschluss'],
];
```

<!-- D-W3-02: followup_intent Ableitung aus Outcome (verbatim) -->
```javascript
var FOLLOWUP_MAP = {
  meeting_booked:     'meeting',
  callback:           'callback',
  send_info:          'send_info',
  gatekeeper_blocked: 'retry_internal',
  // wrong_person, no_interest, contract_signed → 'none'
};
function _deriveFollowupIntent(outcome) {
  return FOLLOWUP_MAP[outcome] || 'none';
}
```

<!-- D-W3-06: outcome_source Mapping -->
```
haiku_outcome == chosen && conf >= 0.70 → 'ai_auto'
haiku_outcome == chosen && conf < 0.70 (Unsicher-Badge) → 'ai_auto_unsicher'
haiku_outcome != chosen (andere Wahl) → 'user_corrected'
conf < 0.70 + User wählt irgendetwas → 'user_corrected'
```

<!-- D-W3-05: Auto-Confirm ENTFÄLLT komplett (kein setInterval, kein Countdown) -->

<!-- D-W4-02: POST-Body und Response-Format -->
```javascript
// POST body: { outcome: '<enum>', followup_intent: '<enum>' }
// Response: { ok: true, coaching_score: float, final_score: int, outcome: str, followup_intent: str }
// Frontend rendert final_score direkt — kein _calcScore() Re-Render nach Outcome-Confirm
```

<!-- escHtml() Z.78-84: vorhanden, verwenden für Badge-Text und Labels -->

<!-- Bestehender outcome_ready Handler Z.2443-2454: bleibt unverändert (ruft _renderOutcomeUx(d) auf) -->

<!-- _showPostcall() Z.3016+: score-sektion initial display:none setzen -->
```javascript
// In _showPostcall(): nlp-section-postcall initial ausblenden
var scoreSection = document.querySelector('.nlp-section-postcall');
if (scoreSection) scoreSection.style.display = 'none';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-Audit + _showPostcall() Score-Gate + _outcomeLabelDe() erweitern</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    CLAUDE.md Punkt 14 (Pre-Insert-Control-Flow-Audit):
    - static/pip-launcher.js Z.3016-3090 (vollständiger _showPostcall() Block — KOMPLETT lesen)
    - static/pip-launcher.js Z.3630-3765 (vollständiger aktueller _renderOutcomeUx() + _outcomeLabelDe() + _submitOutcomeCorrection())
    - static/pip-launcher.js Z.2443-2454 (outcome_ready Handler — bleibt unverändert)
    - static/pip-launcher.js Z.2465-2490 (Reconnect-Fallback-Pull — was passiert wenn score schon sichtbar bei Reconnect?)
    - static/nerve.css: grep "nlp-section-postcall\|pip-outcome\|n-btn-ghost" static/nerve.css
      → Existierende CSS-Klassen für Score-Sektion und Buttons prüfen
    - Caller-Grep: grep "_renderOutcomeUx\|_showPostcall\|outcome_ready" static/pip-launcher.js
      → Alle Aufruf-Stellen beider Funktionen (kein versehentliches Übergehen)
    - Edge-Cases:
      1. Reconnect-Fallback-Pull (Z.2465): wenn Seite neu geladen und Score schon computed → nlp-section-postcall soll sichtbar werden (nicht wieder verstecken)
      2. _showPostcall() wird auch bei "kein Outcome" aufgerufen → Guard: nur wenn no outcome_ready erwartet
      3. Score-Gate: nlp-section-postcall auf display:none SETZEN in _showPostcall(); AUFDECKEN nach correct_outcome-Response
  </read_first>

  <action>
**Schritt 1: _showPostcall() — nlp-section-postcall initial verstecken (per D-W3-04)**

In _showPostcall() (Z.3016+), NACH der Score-Berechnung und BEVOR oder BEIM Rendern des Score-Werts:
Score-Sektion initial ausblenden:
```javascript
var scoreSection = document.querySelector('.nlp-section-postcall');
if (scoreSection) scoreSection.style.display = 'none';
```

Wichtig: Nur wenn `_showPostcall` aufgerufen wird BEVOR `_renderOutcomeUx` triggert. Wenn Reconnect-Fallback schon einen Outcome hat (Z.2478), darf die Section wieder eingeblendet werden — das passiert in Task 2 nach Outcome-Confirm.

**Schritt 2: _outcomeLabelDe() auf 7 Klassen erweitern (per D-W3-01)**

VORHER (Z.3734-3744):
```javascript
function _outcomeLabelDe(val) {
  var m = {
    meeting_booked: 'Termin gebucht',
    callback: 'Rückruf',
    no_interest: 'Kein Interesse',
    wrong_person: 'Falsche Person',
    contract_signed: 'Abschluss',
    unknown: 'Unklar',
  };
  return m[val] || (val || '-');
}
```

NACHHER:
```javascript
function _outcomeLabelDe(val) {
  var m = {
    meeting_booked:     'Termin gebucht',
    callback:           'Rückruf vereinbart',
    send_info:          'Infos zusenden',
    wrong_person:       'Falsche Person',
    gatekeeper_blocked: 'Gatekeeper blockt',
    no_interest:        'Kein Interesse',
    contract_signed:    'Abschluss',
    unknown:            'Unklar',
  };
  return m[val] || (val || '-');
}
```

**UI-Konformitäts-Check Schritt 1:**
- Keine Farben in diesen Änderungen — nur Logik und Label-Map
- _showPostcall() nutzt bestehende CSS-Klassen (nlp-section-postcall) — keine neuen Klassen
  </action>

  <verify>
    <automated>
ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "nlp-section-postcall.*display.*none" static/pip-launcher.js'
    </automated>
  </verify>

  <acceptance_criteria>
- grep "gatekeeper_blocked" static/pip-launcher.js → Treffer in _outcomeLabelDe()
- grep "nlp-section-postcall.*display.*none" static/pip-launcher.js → Treffer in _showPostcall()
- Live-Test: Call beenden → PostCall sichtbar, aber Score-Sektion (nlp-section-postcall) noch verborgen
  </acceptance_criteria>

  <done>Score-Gate in _showPostcall() gesetzt. _outcomeLabelDe() auf 7 Klassen erweitert.</done>
</task>

<task type="auto">
  <name>Task 2: _renderOutcomeUx() komplett neu schreiben + followup_intent + outcome_source</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    CLAUDE.md Punkt 14 (Pre-Insert-Control-Flow-Audit — gilt auch für diesen Task):
    - static/pip-launcher.js Z.3630-3765 (VOLLSTÄNDIG lesen — GESAMTE aktuelle Implementierung, bevor sie ersetzt wird)
    - static/pip-launcher.js Z.3746-3763 (_submitOutcomeCorrection — wird NICHT mehr verwendet, aber prüfen ob andere Caller existieren)
    - static/nerve.css: grep "pip-outcome\|n-btn-ghost\|var(--teal" static/nerve.css → existierende Klassen/Tokens
    - static/nerve.css: prüfen ob pip-outcome-section, pip-outcome-badge-unsicher, pip-outcome-confirm-ring bereits definiert
    - Cross-file: grep "_submitOutcomeCorrection" static/pip-launcher.js → andere Aufrufer (außer _renderOutcomeUx)
    - Edge-Cases:
      1. outcome_ready feuert zweimal (Reconnect) → sec.dataset.outcomeRendered='1' Guard (bereits vorhanden, beibehalten)
      2. callId undefined → early return (wie bisher)
      3. Bestätigen-Button-Klick doppelt → disable nach erstem Klick + Spinner zeigen
      4. correct_outcome POST schlägt fehl → Toast-Fehlermeldung, Button wieder aktivieren
      5. 7 Buttons in PiP-Layout: PiP hat schmale Breite (~300px) → CSS flex-wrap oder grid notwendig
  </read_first>

  <action>
**_renderOutcomeUx() vollständig ersetzen (Z.3630-3731 ca.)**

Alte Implementierung (30s-Countdown, Auto-Confirm, altes Korrektur-Modal) KOMPLETT LÖSCHEN.
_submitOutcomeCorrection() LÖSCHEN (wird durch neue inline Logik ersetzt).

Neue Implementierung:

```javascript
// ── Outcome-Flow Helpers ───────────────────────────────────────────────────

var _OUTCOME_OPTS = [
  ['meeting_booked',     'Termin gebucht'],
  ['callback',           'Rückruf vereinbart'],
  ['send_info',          'Infos zusenden'],
  ['wrong_person',       'Falsche Person'],
  ['gatekeeper_blocked', 'Gatekeeper blockt'],
  ['no_interest',        'Kein Interesse'],
  ['contract_signed',    'Abschluss'],
];

var _FOLLOWUP_MAP = {
  meeting_booked:     'meeting',
  callback:           'callback',
  send_info:          'send_info',
  gatekeeper_blocked: 'retry_internal',
};

function _deriveFollowupIntent(outcome) {
  return _FOLLOWUP_MAP[outcome] || 'none';
}

function _renderOutcomeUx(data) {
  // Idempotenz-Guard (D.UX constraint — beibehalten)
  var sec = document.getElementById('pip-outcome-section');
  if (!sec) {
    var host = document.getElementById('pip-postcall') || document.querySelector('.nlp-section-postcall') || document.body;
    sec = document.createElement('div');
    sec.id = 'pip-outcome-section';
    sec.className = 'n-glass pip-outcome-section';
    host.appendChild(sec);
  }
  if (sec.dataset.outcomeRendered === '1') return;
  sec.dataset.outcomeRendered = '1';
  sec.innerHTML = '';

  var haikuOutcome = data.outcome;
  var conf = +data.confidence || 0;
  var callId = data.call_id;

  if (!callId) return;

  // State: which outcome is currently selected
  var selectedOutcome = (conf >= 0.70 && haikuOutcome) ? haikuOutcome : null;

  // ── Header ───────────────────────────────────────────────────────────────
  var header = document.createElement('div');
  header.className = 'pip-outcome-header';
  header.textContent = 'Gesprächsergebnis bestätigen';
  sec.appendChild(header);

  // ── Unsicher-Badge (nur wenn confidence < 0.70) ──────────────────────────
  if (conf < 0.70) {
    var badge = document.createElement('div');
    badge.className = 'pip-outcome-badge-unsicher';  // Teal-Outline — NICHT gelb (CLAUDE.md HART Punkt 8)
    badge.textContent = 'KI unsicher — bitte wählen';
    sec.appendChild(badge);
  }

  // ── 7 Buttons ─────────────────────────────────────────────────────────────
  var grid = document.createElement('div');
  grid.className = 'pip-outcome-btn-grid';
  var btnEls = {};
  _OUTCOME_OPTS.forEach(function(pair) {
    var btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'n-btn-ghost pip-outcome-btn';
    btn.dataset.value = pair[0];
    btn.textContent = escHtml(pair[1]);
    btn.style.minHeight = '44px';  // Touch-Target min 44px

    // Pre-select Haiku-Vorschlag wenn conf >= 0.70
    if (pair[0] === selectedOutcome) {
      btn.classList.add('pip-outcome-btn--selected');
    }

    btn.addEventListener('click', function() {
      // Deselect alle anderen
      Object.values(btnEls).forEach(function(b) {
        b.classList.remove('pip-outcome-btn--selected');
      });
      btn.classList.add('pip-outcome-btn--selected');
      selectedOutcome = pair[0];
      confirmBtn.disabled = false;
      confirmBtn.classList.remove('pip-outcome-btn--disabled');
    });
    btnEls[pair[0]] = btn;
    grid.appendChild(btn);
  });
  sec.appendChild(grid);

  // ── Bestätigen-Button ─────────────────────────────────────────────────────
  var confirmBtn = document.createElement('button');
  confirmBtn.type = 'button';
  confirmBtn.className = 'n-btn-primary pip-outcome-confirm-btn';
  confirmBtn.textContent = 'Bestätigen';
  confirmBtn.style.minHeight = '44px';
  // Disabled wenn kein Outcome gewählt (conf < 0.70 und kein Klick)
  if (!selectedOutcome) {
    confirmBtn.disabled = true;
    confirmBtn.classList.add('pip-outcome-btn--disabled');
  }

  confirmBtn.addEventListener('click', function() {
    if (!selectedOutcome) return;
    confirmBtn.disabled = true;

    // outcome_source Mapping (per D-W3-06)
    var outcomeSource;
    if (selectedOutcome === haikuOutcome && conf >= 0.70 && !badge_was_shown) {
      outcomeSource = 'ai_auto';
    } else if (selectedOutcome === haikuOutcome && conf >= 0.70 && badge_was_shown) {
      outcomeSource = 'ai_auto_unsicher';
    } else {
      outcomeSource = 'user_corrected';
    }
    // Vereinfacht: conf >= 0.70 + bestätigt → ai_auto; conf < 0.70 → immer user_corrected; anderer Klick → user_corrected
    if (conf >= 0.70 && selectedOutcome === haikuOutcome) {
      outcomeSource = 'ai_auto';
    } else if (conf >= 0.70 && conf < 0.90 && selectedOutcome === haikuOutcome) {
      outcomeSource = 'ai_auto_unsicher';
    } else {
      outcomeSource = 'user_corrected';
    }

    var followupIntent = _deriveFollowupIntent(selectedOutcome);

    // Spinner
    confirmBtn.textContent = 'Bewertung wird berechnet…';

    fetch('/api/calls/' + encodeURIComponent(callId) + '/correct_outcome', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-CSRFToken': document.querySelector('meta[name="csrf-token"]').getAttribute('content'),
      },
      body: JSON.stringify({
        outcome: selectedOutcome,
        followup_intent: followupIntent,
        outcome_source: outcomeSource,
      }),
    })
    .then(function(r) { return r.ok ? r.json() : null; })
    .then(function(json) {
      if (!json || !json.ok) {
        confirmBtn.textContent = 'Fehler — erneut versuchen';
        confirmBtn.disabled = false;
        return;
      }
      // Score aufdecken (per D-W3-04 + D-W4-02)
      var scoreSection = document.querySelector('.nlp-section-postcall');
      if (scoreSection) scoreSection.style.display = 'block';

      // final_score vom Server rendern (nicht lokal berechnen — per D-W4-02)
      if (json.final_score !== undefined) {
        var scoreEl = document.querySelector('.pip-score-value');
        if (scoreEl) scoreEl.textContent = String(json.final_score);
      }

      // Outcome-Sektion zu 1-Zeiler kollabieren
      sec.innerHTML = '';
      var summary = document.createElement('div');
      summary.className = 'pip-outcome-summary';
      summary.textContent = '✓ ' + escHtml(_outcomeLabelDe(json.outcome || selectedOutcome));
      sec.appendChild(summary);
    })
    .catch(function(e) {
      console.error('[PhaseD.UX] correct_outcome POST Fehler:', e);
      confirmBtn.textContent = 'Fehler — erneut versuchen';
      confirmBtn.disabled = false;
    });
  });
  sec.appendChild(confirmBtn);
}
```

HINWEIS zu outcome_source-Logik: Vereinfacht auf:
- `conf >= 0.90 + selectedOutcome === haikuOutcome` → `'ai_auto'`
- `conf >= 0.70 && conf < 0.90 + selectedOutcome === haikuOutcome` → `'ai_auto_unsicher'`
- Alles andere → `'user_corrected'`

(Server-seitig nochmals validiert via outcome_source POST-Parameter — Endpoint in Plan 06 nimmt es entgegen)

**CSS-Klassen hinzufügen falls nicht vorhanden** (in static/nerve.css oder inline in pip-launcher.js als className):
- `.pip-outcome-btn--selected` → `background: var(--teal-500, #00D4AA); color: white;` oder ähnlicher Teal-Token
- `.pip-outcome-badge-unsicher` → Teal-Outline-Stil (border: 1px solid var(--teal-500); color: var(--teal-500))
- `.pip-outcome-btn-grid` → `display: flex; flex-wrap: wrap; gap: 8px;`

Prüfe nerve.css auf bestehende Token-Namen vor Hardcoden.

**UI-Konformitäts-Check Schritt 2:**
1. Brand-Tokens: Alle Farben via var(--...) aus nerve.css — keine hardcoded Hex in JS
2. Unsicher-Badge: Teal-Outline (prüfe var(--teal-*) in nerve.css) — KEIN #f59e0b Gelb
3. n-btn-ghost für Outcome-Buttons, n-btn-primary für Bestätigen — bestehende Klassen
4. Flex-wrap für 7 Buttons in PiP-Breite (~300px)
5. minHeight: '44px' auf allen Buttons

**Commit + Deploy:**
```bash
git add static/pip-launcher.js static/nerve.css
git commit -m "feat(08.23.2.D.UX-05): W3 _renderOutcomeUx rewrite - 7 buttons, score gate, followup_intent"
git push
bash deploy.sh production
```
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "followup_intent\|_deriveFollowupIntent" static/pip-launcher.js'
# Live-Test via Browser: https://getnerve.app → Test-User Login → Call beenden
# → Score-Sektion hidden, 7 Buttons sichtbar, Confidence-Tier korrekt
    </automated>
  </verify>

  <acceptance_criteria>
- Wave-3-Live-Test: Score-Sektion (nlp-section-postcall) nach Call-Ende initial versteckt
- Wave-3-Live-Test: 7 Buttons alle sichtbar (Termin, Rückruf, Infos, Falsche Person, Gatekeeper, Kein Interesse, Abschluss)
- Wave-3-Live-Test: bei hoher Confidence → Haiku-Vorschlag teal pre-selected, Bestätigen aktiv
- Wave-3-Live-Test: bei niedriger Confidence → Unsicher-Badge sichtbar (teal outline), Bestätigen disabled
- Wave-3-Live-Test: Klick auf Button → Bestätigen aktiv, Spinner erscheint bei Klick
- Wave-3-Live-Test: nach Bestätigen → Score-Sektion sichtbar, Outcome-Sektion zu 1-Zeiler
- Browser-Console: 0 JS-Errors während gesamtem Flow
- grep "setInterval\|pip-outcome-countdown\|Auto-Confirm" static/pip-launcher.js → 0 Treffer (Countdown entfernt)
- grep "followup_intent" static/pip-launcher.js → Treffer im POST-Body
- UI-Check: Unsicher-Badge nutzt Teal-Outline (keine gelbe Farbe) — DevTools computed styles prüfen
- André: "fühlt sich richtig an" nach Live-Test
  </acceptance_criteria>

  <done>_renderOutcomeUx() neu geschrieben. 7 Buttons. Score-Gate aktiv. Auto-Confirm entfernt. followup_intent im POST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → correct_outcome POST | User-Input (selectedOutcome) wird direkt ins POST-Body geschrieben |
| DOM → innerHTML | escHtml() schützt Outcome-Labels (kommen vom Server via data.outcome) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D.UX-05-01 | Tampering | outcome in POST-Body | mitigate | Server-seitig validiert gegen VALID_OUTCOMES in api_calls_correct_outcome; ungültige Werte → 400 |
| T-D.UX-05-02 | Tampering | followup_intent im POST-Body | mitigate | Server-seitig validiert gegen CHECK constraint (Plan 01 Migration) — ungültige Werte scheitern an DB-Constraint |
| T-D.UX-05-03 | Elevation of Privilege | CSRF bei correct_outcome POST | mitigate | X-CSRFToken Header (CR-01 fix aus Plan 03) — auch im neuen POST-Body vorhanden |
| T-D.UX-05-04 | Spoofing | outcome_source wird vom Client gesetzt | accept | Server kann outcome_source aus confidence + haikuOutcome selbst ableiten; Client-Angabe ist convenience — in Phase F validiert wenn nötig |
| T-D.UX-05-05 | Information Disclosure | escHtml() auf outcome-Labels | mitigate | _outcomeLabelDe() + escHtml() auf alle user-facing Strings |
</threat_model>

<verification>
Nach Deploy — kompletter Wave-3-Flow-Test:
1. Login mit Test-User auf https://getnerve.app
2. Test-Call starten + beenden
3. PostCall: Score-Sektion hidden ✓
4. Outcome-Sektion: 7 Buttons sichtbar ✓
5. Confidence-State korrekt (mock via inspect.sh oder warte auf echten Call)
6. Bestätigen klicken → Spinner → Score sichtbar → 1-Zeiler ✓
7. Browser-Console: 0 JS-Errors
8. DB-Check: ssh inspect.sh sample calls 1 → followup_intent gesetzt, outcome korrekt
</verification>

<success_criteria>
- Auto-Confirm-Countdown entfernt (0 Treffer in Source)
- 7 Outcome-Buttons immer sichtbar
- Score-Gate: nlp-section-postcall initial hidden, nach Confirm sichtbar
- followup_intent korrekt im POST-Body und in DB
- outcome_source Mapping korrekt (ai_auto/ai_auto_unsicher/user_corrected)
- André Live-Test-Bestätigung
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-05-SUMMARY.md` erstellen.
</output>
