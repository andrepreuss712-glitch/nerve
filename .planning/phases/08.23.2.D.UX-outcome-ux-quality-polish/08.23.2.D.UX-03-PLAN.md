---
phase: 08.23.2.D.UX
plan: 03
type: execute
wave: 1
depends_on:
  - "08.23.2.D.UX-01"
files_modified:
  - templates/dashboard.html
  - routes/app_routes.py
  - routes/performance.py
autonomous: true
requirements:
  - REQ-D.UX-1
  - REQ-D.UX-2
  - REQ-D.UX-3
  - REQ-D.UX-4
  - REQ-D.UX-5

must_haves:
  truths:
    - "dashboard.html correct_outcome POST enthält X-CSRFToken-Header"
    - "_audio_health_bg Thread-Signatur hat user_id Parameter; Query filtert Call.user_id == user_id"
    - "performance.py uses or_() not Pipe-Operator for Dashboard-Reminder-Query"
    - "renderRecommendations() escaped r.icon via escHtml() before innerHTML insert"
    - "Debug-Print Block app_routes.py Z.488-494 ist entfernt"
  artifacts:
    - path: "templates/dashboard.html"
      provides: "CSRF-Fix + XSS-Fix recommendations renderer"
      contains: "X-CSRFToken.*csrf"
    - path: "routes/app_routes.py"
      provides: "CR-02 ownership filter + IN-03 debug print removed"
      contains: "_audio_health_bg.*user_id"
    - path: "routes/performance.py"
      provides: "WR-01 or_() fix"
      contains: "or_(Call.outcome_source"
  key_links:
    - from: "dashboard.html correct_outcome fetch()"
      to: "app_routes.py api_calls_correct_outcome"
      via: "X-CSRFToken header"
      pattern: "X-CSRFToken.*content"
    - from: "_audio_health_bg"
      to: "calls DB row"
      via: "filter(Call.id == call_id_val, Call.user_id == user_id_val)"
      pattern: "_CallAH.user_id.*user_id_val"
---

<objective>
5 Security/Quality Fixes aus dem Code-Review (Wave 1):
- CR-01: CSRF-Token-Header in dashboard.html correct_outcome POST
- CR-02: Ownership-Filter in _audio_health_bg Background-Thread
- WR-01: SQLAlchemy or_() statt Pipe-Operator in performance.py Dashboard-Reminder
- WR-02: XSS-escaping für r.icon in renderRecommendations()
- IN-03: Debug-Print-Block in app_routes.py entfernen

Purpose: Alle 5 sind Security/Quality-Pflicht laut Code-Review 08.23.2.D. CR-01 und CR-02 sind CRITICAL-Findings.
Output: 3 Dateien gepatcht, 0 neues Verhalten für User sichtbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-SPEC.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-CONTEXT.md

<interfaces>
<!-- pip-launcher.js Z.78-84: escHtml() — gleiche Funktion in dashboard.html als esc() vorhanden -->
```javascript
function escHtml(str) {
    return String(str || '')
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;');
}
```

<!-- dashboard.html: existierende esc()-Funktion prüfen (Z.~760-780) — verwenden falls vorhanden -->

<!-- app_routes.py Z.543-587: _audio_health_bg Funktion (aktuell ohne user_id) -->
```python
def _audio_health_bg(call_id_val, wc_buffer, flask_app):
    with flask_app.app_context():
        ...
        _row = _db_ah.query(_CallAH).filter(_CallAH.id == call_id_val).first()
        # FEHLT: .filter(_CallAH.user_id == user_id_val) — CR-02 Fix
```

<!-- app_routes.py Z.582-587: Thread-Start — user_id muss als Argument übergeben werden -->
```python
threading.Thread(
    target=_audio_health_bg,
    args=(_phase_d_call_id, _phase_d_word_confidences, _flask_app),
    daemon=True,
).start()
```

<!-- performance.py Z.450-457: Pipe-Operator Bug -->
```python
(Call.outcome_source == 'ai_auto_unsicher') | (Call.outcome.is_(None))
# MUSS WERDEN:
or_(Call.outcome_source == 'ai_auto_unsicher', Call.outcome.is_(None))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 CSRF-Fix + WR-02 XSS-Fix in dashboard.html</name>
  <files>templates/dashboard.html</files>
  <read_first>
    CLAUDE.md Punkt 14 (Pre-Insert-Control-Flow-Audit):
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "correct_outcome\|renderRecommendations\|getCsrfToken\|csrf-token\|X-CSRFToken\|function esc" templates/dashboard.html'
      → PFLICHT ZUERST: Liefert aktuelle Zeilennummern für alle Edit-Anker (Line-Drift-Schutz per BLOCKER 5 Claudian-Review 2026-05-28)
    - templates/dashboard.html — vollständiger correct_outcome JS-Block (grep-Ergebnis zeigt aktuelle Zeilen — 30 Zeilen um fetch('/api/calls/'+callId+'/correct_outcome' lesen)
    - templates/dashboard.html — renderRecommendations()-Funktion vollständig (grep-Ergebnis zeigt aktuelle Zeile — function renderRecommendations lesen)
    - templates/dashboard.html — esc()-Funktion prüfen (grep-Ergebnis zeigt ob 'function esc(' vorhanden; falls nicht: escHtml-Variante hinzufügen)
    - templates/dashboard.html — meta[name=csrf-token] prüfen (grep-Ergebnis aus obigem Befehl zeigt ob vorhanden; falls nicht: im <head> hinzufügen)
    - pip-launcher.js — escHtml-Funktion (grep "function escHtml" static/pip-launcher.js → korrekte Implementierung referenzieren)
  </read_first>

  <action>
**CR-01 Fix: X-CSRFToken in dashboard.html correct_outcome fetch()**

ANKER: Suche die fetch()-Aufruf-Zeile des correct_outcome-Blocks via Funktionsname `fetch('/api/calls/'` innerhalb des outcome-Korrektions-Handlers (KEINE feste Zeilennummer verwenden — aktuellen Stand via inspect.sh grep aus read_first ermitteln).

Vorher prüfen (aus inspect.sh-Ergebnis): Existiert `<meta name="csrf-token" ...>` in dashboard.html Head?
Falls NICHT: CSRF-Meta-Tag im `<head>` des Templates hinzufügen:
```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```

CSRF-Token defensiv lesen (HIGH 6 Claudian-Review 2026-05-28 — null-check PFLICHT):
```javascript
// Vor dem fetch()-Aufruf:
var csrfMeta = document.querySelector('meta[name="csrf-token"]');
var csrfToken = csrfMeta ? csrfMeta.getAttribute('content') : '';
```

NACHHER (fetch mit csrfToken Variable — nicht direktes .getAttribute() auf möglicherweise null):
```javascript
fetch('/api/calls/' + encodeURIComponent(callId) + '/correct_outcome', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-CSRFToken': csrfToken
  },
  body: JSON.stringify({ outcome: pair[0] }),
})
```

VORHER-Muster (ohne CSRF-Header — wird ersetzt):
```javascript
fetch('/api/calls/' + encodeURIComponent(callId) + '/correct_outcome', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ outcome: pair[0] }),
})
```

**WR-02 Fix: XSS in renderRecommendations() r.icon**

In `renderRecommendations()` (Z.896-905 ca.):
VORHER:
```javascript
html += '<div class="db2-reco-icon">' + r.icon + '</div>';
```

NACHHER:
```javascript
html += '<div class="db2-reco-icon">' + esc(r.icon) + '</div>';
```

Prüfen ob `esc()` in dashboard.html vorhanden ist (grep: `function esc(`). Falls ja: direkt verwenden.
Falls nein: `escHtml`-Variante als `esc()` am Anfang des Script-Blocks definieren:
```javascript
function esc(str) {
  return String(str || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}
```

**UI-Konformitäts-Check (Pflicht für UI-Änderungen):**
Diese Änderungen sind rein JavaScript-seitig (keine CSS-Änderungen). Keine neuen Farben eingeführt.
Der CSRF-Fix ändert keine sichtbaren UI-Elemente. WR-02 escaped Text — Icons sind Server-seitig kontrolliert (kein UX-Impact).
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "X-CSRFToken" templates/dashboard.html'
# Erwartete Ausgabe: Treffer in der fetch()-Zeile
# Manuelle Verifikation: DevTools Network-Tab nach Klick auf Unsicher-Dot → POST zeigt X-CSRFToken Header
    </automated>
  </verify>

  <acceptance_criteria>
- DevTools Network-Tab: POST /api/calls/.../correct_outcome enthält X-CSRFToken-Header nach Deploy
- grep "X-CSRFToken" templates/dashboard.html → Treffer in fetch()-Aufruf
- grep "r.icon" templates/dashboard.html → kein ungescapter r.icon mehr (esc(r.icon) stattdessen)
- XSS-Test: `<img src=x onerror=alert(1)>` als recommendation.icon → als Text gerendert, kein Alert
  </acceptance_criteria>

  <done>CR-01 und WR-02 in dashboard.html gefixt. CSRF-Header vorhanden. Icon XSS-escaped.</done>
</task>

<task type="auto">
  <name>Task 2: CR-02 Ownership-Filter + IN-03 Debug-Print-Cleanup in app_routes.py</name>
  <files>routes/app_routes.py</files>
  <read_first>
    CLAUDE.md Punkt 14 (Pre-Insert-Control-Flow-Audit für _audio_health_bg):
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "_audio_health_bg\|api_calls_correct_outcome\|Phase08.23.2.D.*DEBUG\|api_beenden" routes/app_routes.py'
      → PFLICHT ZUERST: Liefert aktuelle Zeilennummern (BLOCKER 5 — keine festen Z.XXX-Anker verwenden)
    - routes/app_routes.py — vollständige _audio_health_bg Funktion (grep-Ergebnis zeigt aktuelle Zeile der def-Zeile; 60 Zeilen ab `def _audio_health_bg(` lesen)
    - routes/app_routes.py — 30 Zeilen VOR `def _audio_health_bg(` (Kontext: g.user.id verfügbar?)
    - routes/app_routes.py — Debug-Print Block (grep zeigt exakte Zeile des `[Phase08.23.2.D] DEBUG Lookup` Prints)
    - Cross-file grep: "api_beenden\|_audio_health_bg" routes/ — alle Aufruforte der Funktion
    - Flask-Context-Lifecycle PFLICHT: `g.user` gilt nur im aktiven Request-Context. read_first muss 30 Zeilen VOR `threading.Thread(` lesen und bestätigen: (a) Thread-Start-Code liegt innerhalb einer @login_required-Route (g.user gültig), (b) `g.user.id` wird als Python-Int-Wert in args-Tuple kopiert BEVOR `.start()` aufgerufen wird. Im Thread selbst: g-Object nicht verfügbar (kein Flask-Request-Context) — nur der kopierte Int-Wert ist thread-safe.
    - Edge-Cases:
      1. Race: Wenn user löscht Account zwischen thread_start und DB-query → filter gibt None → sicher (return)
      2. Session-Kontext: g.user ist im Flask-Thread nicht verfügbar → user_id MUSS als primitiver Int-Wert übergeben werden (nicht g.user-Objekt)
      3. call_id gehört nicht user_id → filter gibt None → sicher, kein DB-Write
  </read_first>

  <action>
**CR-02 Fix: user_id zu _audio_health_bg hinzufügen**

ANKER: Suche `def _audio_health_bg(` — aktuelle Zeile aus inspect.sh-Ergebnis (read_first) entnehmen.

Schritt 1: Funktion-Signatur ändern (semantischer Anker: `def _audio_health_bg(`):
VORHER: `def _audio_health_bg(call_id_val, wc_buffer, flask_app):`
NACHHER: `def _audio_health_bg(call_id_val, wc_buffer, flask_app, user_id_val):`

Schritt 2: In der Funktion, Query mit user_id filtern (Z.557 ca.):
VORHER:
```python
_row = _db_ah.query(_CallAH).filter(_CallAH.id == call_id_val).first()
```
NACHHER:
```python
_row = _db_ah.query(_CallAH).filter(
    _CallAH.id == call_id_val,
    _CallAH.user_id == user_id_val
).first()
```

Schritt 3: Thread-Start user_id übergeben (Z.582-587):
VORHER:
```python
threading.Thread(
    target=_audio_health_bg,
    args=(_phase_d_call_id, _phase_d_word_confidences, _flask_app),
    daemon=True,
).start()
```
NACHHER:
```python
threading.Thread(
    target=_audio_health_bg,
    args=(_phase_d_call_id, _phase_d_word_confidences, _flask_app, g.user.id),
    daemon=True,
).start()
```

WICHTIG: `g.user.id` wird zum Thread-Start-Zeitpunkt als Python-Int-Wert in args-Tuple kopiert. Das ist thread-safe: args-Tuple ist immutable, g.user.id ist ein primitiver Wert (int). KEIN g.user-Objekt übergeben (SQLAlchemy-Object in anderem Thread → DetachedInstanceError).

**IN-03 Fix: Debug-Print Block entfernen**

Block mit `[Phase08.23.2.D] DEBUG Lookup`-Print vollständig löschen (semantischer Anker: `print(f'[Phase08.23.2.D] DEBUG Lookup'` — aktuellen Block aus inspect.sh grep-Ergebnis in read_first entnehmen, KEINE feste Zeilennummer verwenden):
```python
    # Phase 08.23.2.D Debug-Print 2026-05-27 — Diagnose warum Lookup leer auf Production.
    # [... Kommentar ...]
    try:
        _sess_count = len(ls._session_state)
        _sess_keys = list(ls._session_state.keys())[:3]
        _sess_user_match = [k for k, v in ls._session_state.items() if v.get('user_id') == g.user.id]
        print(f'[Phase08.23.2.D] DEBUG Lookup: sessions={_sess_count} keys={_sess_keys} user_match={_sess_user_match} found_call_id={_phase_d_call_id} posted={_posted_call_id if "_posted_call_id" in dir() else None}')
    except Exception as _e_dbg:
        print(f'[Phase08.23.2.D] DEBUG-print Fehler: {_e_dbg}')
```

Die Fallback-Prints DARUNTER (Z.511, Z.513) und der Fehler-Print Z.483 bleiben erhalten (nützliche Runtime-Logs).

**Commit:**
```bash
git add routes/app_routes.py
git commit -m "fix(08.23.2.D.UX-03): CR-02 ownership filter audio_health_bg + IN-03 remove debug print"
git push
bash deploy.sh production
```
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "Phase08.23.2.D.*DEBUG Lookup" routes/app_routes.py'
# Erwartete Ausgabe: 0 Treffer (Debug-Print entfernt)
# Danach: ssh inspect.sh logs 50 → 0 '[Phase08.23.2.D] DEBUG Lookup' Zeilen nach Deploy
    </automated>
  </verify>

  <acceptance_criteria>
- grep "[Phase08.23.2.D] DEBUG Lookup" routes/app_routes.py → 0 Treffer
- ssh inspect.sh logs 20 → keine DEBUG Lookup Zeilen nach Deploy
- ssh inspect.sh grep "user_id_val" routes/app_routes.py → Treffer in _audio_health_bg
- CRUD-Test (optional): POST /api/calls/<fremde-uuid>/correct_outcome mit anderer Session → 404
  (Dieser Test gilt für api_calls_correct_outcome. _audio_health_bg ist intern-only, kein direkter HTTP-Test möglich.)
  </acceptance_criteria>

  <done>CR-02: _audio_health_bg filtert jetzt nach user_id. IN-03: Debug-Print-Block entfernt.</done>
</task>

<task type="auto">
  <name>Task 3: WR-01 SQLAlchemy or_() Fix in performance.py</name>
  <files>routes/performance.py</files>
  <read_first>
    CLAUDE.md Punkt 14 (Pre-Insert-Control-Flow-Audit):
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "outcome_source.*ai_auto_unsicher\|outcome.is_\|from sqlalchemy\|import or_" routes/performance.py'
      → PFLICHT ZUERST: Liefert aktuelle Zeilennummern (BLOCKER 5 — keine festen Z.XXX-Anker verwenden)
    - routes/performance.py — vollständiger Reminder-Query-Block (grep zeigt aktuelle Zeile von `outcome_source.*ai_auto_unsicher`; 30 Zeilen davor + danach lesen)
    - routes/performance.py — Import-Sektion (erste 20 Zeilen — ist `or_` bereits importiert?)
    - Cross-file: grep "from sqlalchemy" routes/performance.py → welche sqlalchemy-Symbols bereits importiert
    - Edge-Case: Pipe-Operator `|` auf SQLAlchemy Column-Expressions produziert in PostgreSQL korrekt OR-SQL (da SQLAlchemy die `__or__`-Methode überschreibt). WR-01 ist eine Code-Qualitäts-Korrektur (explizit statt implizit), nicht unbedingt ein Runtime-Bug. ABER: Verhalten mit IS NULL kann auf SQLite vs. Postgres unterschiedlich sein. or_() ist der kanonische Pfad.
  </read_first>

  <action>
**Schritt 1: Import prüfen / hinzufügen**

In routes/performance.py, Import-Sektion oben prüfen ob `or_` bereits importiert. Falls nicht:
```python
from sqlalchemy import or_
```
(Kann inline im try-Block erfolgen falls bestehende Imports strukturell getrennt sind.)

**Schritt 2: Pipe-Operator durch or_() ersetzen (Z.455)**

VORHER:
```python
(Call.outcome_source == 'ai_auto_unsicher') | (Call.outcome.is_(None)),
```

NACHHER:
```python
or_(Call.outcome_source == 'ai_auto_unsicher', Call.outcome.is_(None)),
```

Sicherstellen dass `or_` verfügbar ist (Import im selben scope oder top-level).

**Commit:**
```bash
git add routes/performance.py
git commit -m "fix(08.23.2.D.UX-03): WR-01 SQLAlchemy or_() for reminder query"
git push
bash deploy.sh production
```
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "or_(Call.outcome_source" routes/performance.py'
# Erwartete Ausgabe: Treffer
    </automated>
  </verify>

  <acceptance_criteria>
- grep "or_(Call.outcome_source" routes/performance.py → Treffer
- grep "outcome_source == 'ai_auto_unsicher') |" routes/performance.py → 0 Treffer (Pipe-Operator weg)
- Dashboard-Reminder-Stripe erscheint bei User mit unsicheren Outcomes (Live-Test via Browser)
  </acceptance_criteria>

  <done>WR-01 gefixt. performance.py nutzt or_() für Dashboard-Reminder-Query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → correct_outcome POST | Unauthenticated requests möglich ohne CSRF-Fix |
| Thread → DB | Background-Thread könnte fremde Call-Rows schreiben ohne Ownership-Check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D.UX-03-01 | Spoofing | dashboard.html correct_outcome POST (CSRF) | mitigate | X-CSRFToken Header aus meta[name=csrf-token] — identisch zum pip-launcher.js Pattern |
| T-D.UX-03-02 | Tampering | _audio_health_bg schreibt audio_health_score ohne Ownership | mitigate | Filter(Call.user_id == user_id_val) — user_id als primitiver Int im Thread-Arg übergeben |
| T-D.UX-03-03 | Information Disclosure | renderRecommendations r.icon ungescaped XSS | mitigate | esc(r.icon) wrapper — verhindert script-injection via server-seitige recommendation data |
| T-D.UX-03-04 | Information Disclosure | Debug-Print leakt session-state auf Production | mitigate | Block entfernt — IN-03 |
| T-D.UX-03-05 | Elevation of Privilege | or_() vs Pipe-Operator in Reminder Query | accept | Pipe-Operator funktioniert auf Postgres (SQLAlchemy __or__ override), or_() ist expliziter — kein Security-Risk, aber Code-Quality-Fix |
</threat_model>

<verification>
Nach Deploy:
1. DevTools Network-Tab: Klick auf Unsicher-Dot → POST zeigt X-CSRFToken Header
2. `ssh inspect.sh logs 20` → keine DEBUG Lookup Prints mehr
3. `ssh inspect.sh grep "or_(Call" routes/performance.py` → Treffer
4. Manuelle XSS-Probe: `<img src=x onerror=alert(1)>` als icon — kein alert
</verification>

<success_criteria>
- CR-01: CSRF-Header in dashboard.html correct_outcome POST
- CR-02: _audio_health_bg filtert nach user_id
- WR-01: or_() in performance.py Reminder-Query
- WR-02: r.icon escaped in renderRecommendations
- IN-03: Debug-Print-Block entfernt, 0 Treffer in Logs nach Deploy
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-03-SUMMARY.md` erstellen.
</output>
