---
phase: 08.23.2.D.UX
plan: 04
type: execute
wave: 1
depends_on:
  - "08.23.2.D.UX-01"
files_modified:
  - services/outcome_service.py
autonomous: true
requirements:
  - REQ-D.UX-6
  - REQ-D.UX-7
  - REQ-D.UX-8

must_haves:
  truths:
    - "classify('') gibt {outcome: 'unknown', confidence: 0.0} ohne HTTP-Call zurück"
    - "classify(text < 20 Wörter) gibt confidence <= 0.65 zurück"
    - "Haiku-Prompt enthält 5 Few-Shot-Beispiele in XML-Tags und Rangfolge-Anweisung"
    - "VALID_OUTCOMES frozenset enthält send_info + gatekeeper_blocked"
    - "_select_snippets nutzt word-count Heuristik (nicht mehr 'längste Aussagen')"
    - "WR-03 Kommentar zeigt >= 0.70 statt > 0.70"
  artifacts:
    - path: "services/outcome_service.py"
      provides: "Klassifikations-Tuning: Snippet-Heuristik + Few-Shot-Prompt + VALID_OUTCOMES"
      exports: ["classify", "calculate_audio_health", "VALID_OUTCOMES", "SYSTEM_PROMPT"]
  key_links:
    - from: "services/outcome_service.py VALID_OUTCOMES"
      to: "routes/app_routes.py api_calls_correct_outcome"
      via: "from services.outcome_service import VALID_OUTCOMES"
      pattern: "VALID_OUTCOMES.*send_info.*gatekeeper_blocked"
    - from: "_select_snippets"
      to: "claude_client.messages.create"
      via: "classify() → _select_snippets() → _build_prompt()"
      pattern: "len.*split.*1.4.*2000"
---

<objective>
Klassifikations-Tuning in outcome_service.py — 3 Änderungen:
1. D-W2-03: Snippet-Auswahl-Bug fixen (word-count Heuristik ersetzt "längste Aussagen"-Logik die Termin-Live-Test 2026-05-27 zerstörte)
2. D-W2-01/02/04: Haiku-Prompt auf 7 Klassen + 5 Few-Shot-Beispiele + Rangfolge-Anweisung erweitern
3. REQ-D.UX-8: WR-03 Boundary-Kommentar korrigieren

Purpose: Live-Bug 2026-05-27: Termin-Schlusssatz war kurz → fiel aus Top-3-Snippets → Haiku klassifizierte als "Kein Interesse". Word-count Heuristik + Few-Shot lösen das.
Output: outcome_service.py komplett überarbeitet, alle classify()-Edge-Cases abgedeckt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-SPEC.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-CONTEXT.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-RESEARCH.md

<interfaces>
<!-- Aktuelles outcome_service.py (vollständig aus Codebase gelesen): -->

```python
# VORHER (komplett ersetzen):
VALID_OUTCOMES = frozenset({
    'meeting_booked', 'callback', 'no_interest', 'wrong_person',
    'contract_signed', 'unknown'
})

def _select_snippets(log_entries, dauer_sek, max_count=3):
    # "Top-3 längste Aussagen" — ENTFERNEN

def _build_prompt(conv_data, snippets):
    # Generischer Prompt ohne Few-Shot — ERSETZEN

def classify(conv_data):
    # system= Parameter fehlt, max_tokens=200 — ANPASSEN
```

<!-- RESEARCH.md Topic 2: Exakter SYSTEM_PROMPT (verbatim verwenden) -->
<!-- RESEARCH.md Topic 1: Exakte _select_snippets Implementierung (verbatim verwenden) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ts_ms-Verfügbarkeit prüfen + _select_snippets + VALID_OUTCOMES + WR-03 updaten</name>
  <files>services/outcome_service.py</files>
  <read_first>
    CLAUDE.md Punkt 14 (Pre-Insert-Control-Flow-Audit):
    - services/outcome_service.py Z.1-109 (vollständige Datei — alle Funktionen lesen)
    - JSON-Parse-Guard PFLICHT: prüfen ob classify() bereits try/except für json.loads(response.content[0].text) hat. Falls nicht: Snippet in Schritt 3 (classify() updaten) muss try/except ergänzen mit `except Exception: return {'outcome': 'unknown', 'confidence': 0.0}` — malformed JSON von Haiku crasht sonst die Funktion. Befund in Threat-Model T-D.UX-04-02 dokumentieren.
    - Cross-file: grep "log_entries\|ts_ms\|classify(" routes/learning.py — wo classify() aufgerufen wird und welche Felder in log_entries vorhanden sind
    - Cross-file: grep "ts_ms\|log_entries" services/live_session.py — prüfen ob ts_ms pro Entry gesetzt wird
    - Cross-file: grep "log_entries\|ts_ms" routes/app_routes.py — alternate call paths
    - Edge-Cases:
      1. ts_ms fehlt in log_entries → Fallback auf index-basiert (erste N + letzte M Entries)
      2. dauer_sek=0 → (0-60)*1000 = -60000 → alle Entries mit ts_ms>=0 → vollständiges Transkript faktisch (OK)
      3. leeres log_entries → early return [] (bleibt wie gehabt)
      4. Kurzer Text <20 Wörter → confidence ceiling 0.65 NACH Haiku-Response (nicht vor API-Call)
  </read_first>

  <action>
**Schritt 1: ts_ms-Verfügbarkeit feststellen**

Führe aus: `ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ts_ms" services/live_session.py'`
und: `ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "ts_ms" routes/learning.py'`

Wenn ts_ms-Feld in log_entries vorhanden → time-based Fallback nutzen (per RESEARCH.md Implementierung).
Wenn ts_ms NICHT vorhanden → index-basierter Fallback: erste 15 + letzte 15 Entries.

**Schritt 2: _select_snippets komplett ersetzen**

Alte Funktion (Z.35-45 "Top-3 längste Aussagen") VOLLSTÄNDIG ENTFERNEN und ersetzen durch:

```python
def _estimate_tokens(text: str) -> int:
    """Conservative token estimate for German transcript text.
    Factor 1.4 is a conservative best-guess (WARNING Claudian-Review 2026-05-28).
    German compound nouns tokenize heavier than English (~1.4–1.6 tokens/word).
    Calibrate factor with real NERVE call corpus after EA launch.
    Current 1.4 may undercount tokens for long compound-heavy transcripts —
    true boundary may be 1400 words rather than 2000/1.4 ≈ 1429 words."""
    return int(len(text.split()) * 1.4)


def _select_snippets(log_entries: List[dict], dauer_sek: int) -> List[str]:
    """D.UX-W2-03: Full transcript if < 2000 estimated tokens, else first-30s + last-60s.
    Replaces the old 'top-3 longest statements' heuristic which dropped short
    closing sentences (e.g. 'Montag um 10' = meeting_booked → classified as unknown).

    Token estimate: len(words) * 1.4 (conservative, German compound nouns tokenize heavier).
    Fallback boundary: if ts_ms available → time-based (first 30s + last 60s).
                       if ts_ms absent → index-based (first 15 + last 15 entries).
    """
    if not log_entries:
        return []
    texts = [e.get('text', '') for e in log_entries if e.get('text')]
    if not texts:
        return []

    full_text = ' '.join(texts)

    # Full transcript if below token threshold
    if _estimate_tokens(full_text) < 2000:
        return texts

    # Fallback: first 30s + last 60s
    # Check if ts_ms is available in entries
    has_ts = any('ts_ms' in e for e in log_entries)
    if has_ts:
        early = [e.get('text', '') for e in log_entries
                 if e.get('ts_ms', 0) <= 30_000 and e.get('text')]
        late  = [e.get('text', '') for e in log_entries
                 if e.get('ts_ms', 0) >= (dauer_sek - 60) * 1000 and e.get('text')]
    else:
        # Index-based fallback: first 15 + last 15 entries
        early = texts[:15]
        late  = texts[-15:]
    return early + late
```

**Schritt 3: VALID_OUTCOMES aktualisieren**

VORHER:
```python
VALID_OUTCOMES = frozenset({
    'meeting_booked', 'callback', 'no_interest', 'wrong_person',
    'contract_signed', 'unknown'
})
```

NACHHER (per D-W2-01):
```python
VALID_OUTCOMES = frozenset({
    'meeting_booked', 'callback', 'send_info', 'wrong_person',
    'gatekeeper_blocked', 'no_interest', 'contract_signed', 'unknown'
})
```

**Schritt 4: WR-03 Boundary-Kommentar (in routes/learning.py oder outcome_service.py)**

grep nach Kommentar `> 0.70` oder `>= 0.70` im Kontext von ai_auto_unsicher:
`ssh inspect.sh grep "> 0.70" routes/learning.py services/outcome_service.py`

Kommentar-Zeile(n) die `> 0.70` für ai_auto_unsicher zeigen:
VORHER: `# confidence > 0.70 → ai_auto_unsicher` (oder ähnlich)
NACHHER: `# confidence >= 0.70 and < 0.90 → ai_auto_unsicher`
  </action>

  <verify>
    <automated>
ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "_estimate_tokens\|_select_snippets\|ts_ms" services/outcome_service.py'
    </automated>
  </verify>

  <acceptance_criteria>
- grep "_estimate_tokens" services/outcome_service.py → Treffer (neue Funktion vorhanden)
- grep "Top-3\|sorted_by_len\|max_count" services/outcome_service.py → 0 Treffer (alte Logik weg)
- grep ">= 0.70 and < 0.90" services/outcome_service.py (oder learning.py) → Treffer (WR-03)
- VALID_OUTCOMES enthält send_info + gatekeeper_blocked
  </acceptance_criteria>

  <done>_select_snippets mit word-count Heuristik. VALID_OUTCOMES auf 8 Werte. WR-03 Kommentar korrigiert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Few-Shot SYSTEM_PROMPT + Snippet-Heuristik testen + classify() updaten</name>
  <files>services/outcome_service.py, tests/test_outcome_service.py</files>
  <read_first>
    - services/outcome_service.py Z.48-108 (classify() + _build_prompt() — vollständig lesen nach Task 1 Änderungen)
    - tests/ Verzeichnis: prüfen ob test_outcome_service.py bereits existiert
    - Cross-file: grep "from services.outcome_service import\|import outcome_service" tests/
    - Vorhandene Test-Patterns in tests/ (pytest-Konventionen, Fixtures, monkeypatch-Pattern)
    - routes/learning.py Z.1-30 + grep "classify(" — welche Imports aus outcome_service verwendet werden
  </read_first>

  <behavior>
    - Test: classify({}) ohne HTTP-Call → {'outcome': 'unknown', 'confidence': 0.0} (dauer=0 → early return)
    - Test: classify({'dauer_sekunden': 0}) → {'outcome': 'unknown', 'confidence': 0.0}
    - Test: classify({'dauer_sekunden': 45, 'log_entries': [{'text': 'ja tschüss'}]}) → confidence <= 0.65 (< 20 Wörter → ceiling)
    - Test: _estimate_tokens('') → 0
    - Test: _estimate_tokens('ein zwei drei') → int(3 * 1.4) = 4
    - Test: _select_snippets([], 60) → []
    - Test: _select_snippets([{'text': 'Hallo'}], 30) → ['Hallo'] (short → full transcript)
  </behavior>

  <action>
**Schritt 1: SYSTEM_PROMPT als Modul-Konstante hinzufügen (verbatim aus RESEARCH.md Topic 2)**

Nach den bestehenden Konstanten (_HAIKU_MODEL etc.), SYSTEM_PROMPT definieren:

```python
SYSTEM_PROMPT = """\
Du bist ein Klassifikations-Assistent für B2B-Verkaufsgespräche im DACH-Markt.

Klassifiziere den Gesprächsausgang in genau eine der 7 Kategorien:
- meeting_booked: Ein konkreter Termin mit Datum/Zeit wurde vereinbart
- callback: Rückruf vereinbart, aber kein fester Termin (z.B. "melde mich nächste Woche")
- send_info: Interessent möchte Unterlagen/Infos zugeschickt bekommen
- wrong_person: Gesprächspartner ist nicht der richtige Ansprechpartner
- gatekeeper_blocked: Sekretariat/Empfang hat Weiterleitung verweigert, kein Durchkommen
- no_interest: Klare Ablehnung, kein Interesse
- contract_signed: Vertrag oder Abschluss wurde im Gespräch bestätigt

Rangfolge bei Überlappung: Termin (meeting_booked) > Rückruf (callback) > Kein Interesse (no_interest).

<examples>
<example>
<input>Ja, das klingt interessant. Montag um 10 hätte ich Zeit. Tragen Sie mich ein.</input>
<output>{"outcome": "meeting_booked", "confidence": 0.95}</output>
</example>
<example>
<input>Nein danke, das ist nichts für uns. Bitte rufen Sie nicht wieder an.</input>
<output>{"outcome": "no_interest", "confidence": 0.97}</output>
</example>
<example>
<input>Schicken Sie mir die Unterlagen, ich melde mich nächste Woche. Einen festen Termin kann ich jetzt nicht zusagen.</input>
<output>{"outcome": "callback", "confidence": 0.88}</output>
</example>
<example>
<input>Das macht bei uns die Frau Müller in der Geschäftsführung. Ich gebe Ihnen mal die Durchwahl.</input>
<output>{"outcome": "wrong_person", "confidence": 0.92}</output>
</example>
<example>
<input>Frau Schmidt ist heute nicht zu sprechen. Versuchen Sie es nächste Woche. Durchstellen kann ich Sie nicht.</input>
<output>{"outcome": "gatekeeper_blocked", "confidence": 0.91}</output>
</example>
</examples>

Antworte AUSSCHLIESSLICH mit JSON in genau diesem Format, ohne Markdown-Code-Fences:
{"outcome": "<enum-wert>", "confidence": <float 0.0-1.0>}\
"""
```

**Schritt 2: _build_prompt() updaten (verbatim aus RESEARCH.md)**

VORHER: Enthält Anweisung zur Klassifikation IM Prompt-String
NACHHER: Nur Kontext-Daten + Transkript (System-Anweisung ist in SYSTEM_PROMPT)

```python
def _build_prompt(conv_data: dict, snippets: List[str]) -> str:
    """Baut den User-Message-Teil für Haiku (System-Prompt ist in SYSTEM_PROMPT)."""
    return (
        f"Gesprächsdauer (s): {conv_data.get('dauer_sekunden', 0)}\n"
        f"Erreichte Phase: {conv_data.get('erreichte_phase', 'unbekannt')}\n"
        f"Einwände: {json.dumps(conv_data.get('einwaende_liste', [])[:10], ensure_ascii=False)}\n"
        f"Kaufbereitschaft Ende (0-100): {conv_data.get('kb_endwert', 0)}\n\n"
        "<transkript>\n"
        + "\n".join(f"- {s}" for s in snippets)
        + "\n</transkript>\n\n"
        "Klassifiziere den Gesprächsausgang."
    )
```

**Schritt 3: classify() updaten**

Änderungen in classify():
1. Early-exit für leere/zu-kurze Inputs gibt `{'outcome': 'unknown', 'confidence': 0.0}` zurück (per REQ-D.UX-6):
```python
# Early exit for empty or very short calls
if not conv_data or conv_data.get('dauer_sekunden', 0) < 30:
    return {'outcome': 'unknown', 'confidence': 0.0}
```
2. `system=SYSTEM_PROMPT` zum claude_client.messages.create() hinzufügen
3. `max_tokens=200` → `max_tokens=80` (JSON output ist klein)
4. Confidence-Ceiling für kurze Inputs (nach JSON-Parse, vor return):

```python
# Post-process: enforce confidence ceiling for short inputs
word_count = sum(len(s.split()) for s in snippets)
if word_count < 20:
    confidence = min(confidence, 0.65)
```

Vollständiger classify() API-Call Block:
```python
response = claude_client.messages.create(
    model=_HAIKU_MODEL,
    max_tokens=80,
    system=SYSTEM_PROMPT,
    messages=[{'role': 'user', 'content': prompt}],
)
```

**Schritt 4: Tests in tests/test_outcome_service.py (RED → GREEN)**

```python
import pytest
from unittest.mock import patch, MagicMock
from services.outcome_service import classify, _estimate_tokens, _select_snippets, VALID_OUTCOMES


def test_classify_empty_text_no_http():
    """Empty conv_data with dauer=0 returns unknown/0.0 without HTTP call."""
    with patch('services.outcome_service.claude_client') as mock_client:
        result = classify({})
        mock_client.messages.create.assert_not_called()
    assert result == {'outcome': 'unknown', 'confidence': 0.0}


def test_classify_short_call_no_http():
    """Call shorter than 30s returns unknown/0.0 without HTTP call."""
    with patch('services.outcome_service.claude_client') as mock_client:
        result = classify({'dauer_sekunden': 29})
        mock_client.messages.create.assert_not_called()
    assert result == {'outcome': 'unknown', 'confidence': 0.0}


def test_confidence_ceiling_short_text():
    """Text < 20 words gets confidence ceiling of 0.65."""
    mock_response = MagicMock()
    mock_response.content = [MagicMock(text='{"outcome": "meeting_booked", "confidence": 0.92}')]
    with patch('services.outcome_service.claude_client') as mock_client:
        mock_client.messages.create.return_value = mock_response
        result = classify({
            'dauer_sekunden': 60,
            'log_entries': [{'text': 'ja tschüss'}]
        })
    assert result['confidence'] <= 0.65


def test_estimate_tokens_empty():
    assert _estimate_tokens('') == 0


def test_estimate_tokens_three_words():
    assert _estimate_tokens('ein zwei drei') == int(3 * 1.4)


def test_select_snippets_empty():
    assert _select_snippets([], 60) == []


def test_select_snippets_short_returns_full():
    entries = [{'text': 'Hallo wie geht es'}]
    result = _select_snippets(entries, 30)
    assert result == ['Hallo wie geht es']


def test_valid_outcomes_contains_new_values():
    assert 'send_info' in VALID_OUTCOMES
    assert 'gatekeeper_blocked' in VALID_OUTCOMES


def test_system_prompt_contains_examples():
    from services.outcome_service import SYSTEM_PROMPT
    assert '<examples>' in SYSTEM_PROMPT
    assert 'meeting_booked' in SYSTEM_PROMPT
```

**Commit + Deploy:**
```bash
git add services/outcome_service.py tests/test_outcome_service.py
git commit -m "feat(08.23.2.D.UX-04): W2 few-shot prompt + snippet heuristic + valid outcomes"
git push
bash deploy.sh production
```

**Tests auf Production:**
```bash
ssh deploy@getnerve.app 'cd /opt/nerve/app && source venv/bin/activate && pytest tests/test_outcome_service.py -x -v'
```
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && source venv/bin/activate && pytest tests/test_outcome_service.py -x -v'
    </automated>
  </verify>

  <acceptance_criteria>
- Alle pytest tests in test_outcome_service.py grün (auf Production via SSH)
- grep "SYSTEM_PROMPT" services/outcome_service.py → Treffer (Modul-Konstante vorhanden)
- grep "max_tokens=80" services/outcome_service.py → Treffer (nicht mehr 200)
- grep "system=SYSTEM_PROMPT" services/outcome_service.py → Treffer im messages.create() call
- classify({}) → {'outcome': 'unknown', 'confidence': 0.0} (kein HTTP-Call, outcome='unknown' nicht None)
- Live-Test 1: Manueller Call mit klarem Termin-Satz ("Montag 10 Uhr passt") → meeting_booked confidence >= 0.80
- Live-Test 2: Manueller Call mit klarer Ablehnung ("kein Interesse, bitte nicht mehr anrufen") → no_interest confidence >= 0.80
  </acceptance_criteria>

  <done>Few-Shot-Prompt implementiert. Snippet-Heuristik aktiv. Tests grün auf Production. VALID_OUTCOMES enthält 8 Werte. classify() gibt 'unknown' (nicht None) für leere Inputs zurück.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outcome_service → Anthropic API | Transkript-Daten gehen an Haiku |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D.UX-04-01 | Information Disclosure | Transkript zu Haiku (B2B-Gesprächsinhalt) | accept | Defense-in-depth Anonymisierung bereits in Phase B (log_entries sind anonymisiert); DSGVO-Rechtsgrundlage für KI-Analyse bereits dokumentiert |
| T-D.UX-04-02 | Denial | Haiku API Timeout → classify() Exception | mitigate | try/except in classify() gibt {'outcome': 'unknown', 'confidence': 0.0} zurück — graceful degradation |
| T-D.UX-04-03 | Tampering | VALID_OUTCOMES erweitert ohne Migration | accept | Migration 0006 (Plan 01) läuft in Wave 0 vor Plan 04 — beide Werte sind in DB gültig bevor classify() sie zurückgeben kann |
</threat_model>

<verification>
Nach Deploy:
1. pytest tests/test_outcome_service.py auf Production
2. Live-Test: Ein Test-Call mit "Termin" im Abschluss → inspect.sh sample calls 1 → outcome=meeting_booked
3. grep "sorted_by_len" services/outcome_service.py → 0 Treffer (alte Logik entfernt)
</verification>

<success_criteria>
- Snippet-Heuristik: word-count statt längste Aussagen
- SYSTEM_PROMPT mit 5 Few-Shot-Beispielen + Rangfolge-Anweisung
- VALID_OUTCOMES: 8 Werte inkl. send_info + gatekeeper_blocked
- WR-03 Kommentar: >= 0.70 statt > 0.70
- classify() early-exit gibt outcome='unknown' (nicht None) zurück
- Tests grün auf Production
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-04-SUMMARY.md` erstellen.
</output>
