---
phase: 08.23.2.D.UX
plan: 06
type: execute
wave: 3
depends_on:
  - "08.23.2.D.UX-02"
  - "08.23.2.D.UX-05"
files_modified:
  - routes/app_routes.py
autonomous: true
requirements:
  - REQ-D.UX-11

must_haves:
  truths:
    - "correct_outcome Endpoint nimmt {outcome, followup_intent} im POST-Body entgegen"
    - "Score-Formel: process_score = kb_end*0.30 + behandelt_rate*0.30 + redeanteil_score*0.20 + skript*0.10 + 0 (frage_qualitaet=0)"
    - "Outcome-Multiplikatoren korrekt (contract_signed 1.15 ... no_interest 0.85)"
    - "final_score = clamp(round(process_score * modifier), 0, 100)"
    - "coaching_score + score_breakdown + score_schema_version atomar in einer Transaction gespeichert"
    - "Response-Format: {ok: true, coaching_score: float, final_score: int, outcome: str, followup_intent: str}"
    - "score_breakdown enthält alle 9 Schlüssel inkl. schema_version: 1"
  artifacts:
    - path: "routes/app_routes.py"
      provides: "api_calls_correct_outcome erweitert mit score-calculation + followup_intent"
      contains: "followup_intent.*score_breakdown.*schema_version"
  key_links:
    - from: "api_calls_correct_outcome"
      to: "calls.coaching_score + calls.score_breakdown"
      via: "atomar db_co.commit() nach allen Updates"
      pattern: "coaching_score.*score_breakdown.*score_schema_version"
---

<objective>
`api_calls_correct_outcome` Endpoint erweitern: POST-Body nimmt `followup_intent` entgegen, berechnet coaching_score mit neuem Outcome-Multiplikator, speichert score_breakdown (9-Schlüssel-JSONB) + score_schema_version=1 atomar, gibt neues Response-Format zurück.

Purpose: Plan 05 sendet `{outcome, followup_intent}` aber der Endpoint ignoriert followup_intent noch und gibt kein `final_score` zurück. Plan 06 schließt diese Lücke.
Output: Vollständiger Score-Persistierungs-Endpoint, atomar, deterministische Formel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-SPEC.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-CONTEXT.md

<interfaces>
<!-- Aktuelles api_calls_correct_outcome Z.1689-1751 — wird erweitert: -->
```python
@app_routes_bp.route('/api/calls/<call_id>/correct_outcome', methods=['POST'])
@login_required
def api_calls_correct_outcome(call_id):
    # Aktuell: nimmt nur {outcome, note}; setzt outcome_source='user_corrected'
    # ERWEITERN: followup_intent, outcome_source vom Client, Score-Berechnung
    data_co = request.get_json(silent=True) or {}
    new_outcome = data_co.get('outcome')
    new_note = data_co.get('note')
    # ...
    return jsonify({'ok': True, 'call_id': str(row.id), 'outcome': row.outcome, 'source': row.outcome_source, 'outcome_note': row.outcome_note})
    # MUSS WERDEN: {'ok': True, 'coaching_score': float, 'final_score': int, 'outcome': str, 'followup_intent': str}
```

<!-- D-W4-04: Score-Formel (verbatim) -->
```python
# process_score = kb_end*0.30 + behandelt_rate*0.30 + redeanteil_score*0.20 + skript*0.10 + frage_qualitaet*0.10
# frage_qualitaet = 0 (v1)
# final_score = clamp(round(process_score * outcome_modifier), 0, 100)
```

<!-- D-W4-01: Outcome-Multiplikatoren (verbatim) -->
```python
OUTCOME_MODIFIERS = {
    'contract_signed':    1.15,
    'meeting_booked':     1.10,
    'wrong_person':       1.00,
    'callback':           0.95,
    'send_info':          0.95,
    'gatekeeper_blocked': 0.95,
    'no_interest':        0.85,
}
# Default für unbekannte Outcomes: 1.00
```

<!-- D-W4-02: Response-Format (verbatim) -->
```python
return jsonify({'ok': True, 'coaching_score': coaching_score_val, 'final_score': final_score_val, 'outcome': row.outcome, 'followup_intent': row.followup_intent})
```

<!-- score_breakdown Schema (9 Schlüssel, alle required — Cross-AI-Empfehlung) -->
```python
score_breakdown_data = {
    'schema_version': 1,
    'kb_end_norm': kb_end_norm,
    'behandelt_rate': behandelt_rate,
    'redeanteil_score': redeanteil_score,
    'skript_norm': skript_norm,
    'frage_qualitaet': 0,  # v1 placeholder
    'outcome_modifier': outcome_modifier,
    'process_score': round(process_score, 4),
    'final_score': final_score_val,
    'computed_at_iso': datetime.now(timezone.utc).isoformat(),
}
```

<!-- Verbundene ConversationLog (kb_end, einwaende, redeanteil holen) -->
```python
# row.conversation_log_id → ConversationLog.id
# ConversationLog.kb_end, .einwaende_gesamt, .einwaende_behandelt, .redeanteil_avg, .skript_abdeckung
from database.models import ConversationLog
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score-Berechnung + followup_intent im correct_outcome Endpoint</name>
  <files>routes/app_routes.py, tests/test_correct_outcome.py</files>
  <read_first>
    CLAUDE.md Punkt 14 (Pre-Insert-Control-Flow-Audit):
    - routes/app_routes.py Z.1689-1752 (VOLLSTÄNDIG lesen — aktueller api_calls_correct_outcome)
    - routes/app_routes.py Z.606-617 (_calc_call_score — alte Formel, wird NICHT mehr als primärer Score verwendet aber nicht löschen — bleibt als Fallback-Utility)
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh grep "coaching_score" templates/dashboard.html services/performance.py routes/founder_cost_dashboard.py'
      → BLOCKER Claudian-Review 2026-05-28: coaching_score Skala MUSS 0-100 sein (konsistent mit final_score in derselben Funktion). _calc_coaching_score gibt `round(process_score * modifier, 4)` zurück — KEIN /100.0. Falls ein Konsumer (dashboard.html, performance.py, founder_cost_dashboard.py) Skala 0-1 erwartet: Konsumer-Anpassung im selben Plan — NICHT als Post-Execute-Cleanup delegieren.
    - database/models.py Z.644-684 (Call-Modell — coaching_score, score_breakdown, score_schema_version, followup_intent)
    - database/models.py: grep "ConversationLog" — ConversationLog-Felder kb_end, einwaende_gesamt etc.
    - routes/app_routes.py Z.1 (Imports — datetime, timezone fehlen möglicherweise → hinzufügen wenn nötig)
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
      → Bestätigen dass coaching_score, score_breakdown, score_schema_version, followup_intent alle vorhanden (Migrations 0006 + 0007 aus Wave 0)
    - Cross-caller grep: grep "correct_outcome" templates/dashboard.html static/pip-launcher.js
      → Alle Stellen die auf Response-Format angewiesen sind
    - Edge-Cases:
      1. conversation_log_id ist None → process_score aus Fallback-Defaults berechnen (kb=30, behandelt=0.5, redeanteil=50, skript=0)
      2. new_outcome nicht in OUTCOME_MODIFIERS → modifier=1.00
      3. followup_intent vom Client ungültig → ignorieren, null speichern (kein 400 für followup_intent)
      4. DB-Commit schlägt fehl → rollback, 500 zurückgeben
      5. coaching_score = None nach Formel (NaN) → 0.0 als Fallback
  </read_first>

  <behavior>
    - Test: Bekannte Inputs → berechenbarer final_score (deterministischer pytest)
      kb=70, behandelt_rate=1.0, redeanteil=40, skript=50, outcome=meeting_booked →
      process_score = 70*0.30 + 100*0.30 + 100*0.20 + 50*0.10 + 0 = 21+30+20+5+0 = 76
      final_score = round(76 * 1.10) = round(83.6) = 84 → clamp(84, 0, 100) = 84
    - Test: outcome=no_interest, gleiche Inputs → final_score = round(76 * 0.85) = round(64.6) = 65
    - Test: outcome=contract_signed → final_score = round(76 * 1.15) = round(87.4) = 87
    - Test: score_breakdown enthält alle 9 Schlüssel (schema_version, kb_end_norm, ...)
    - Test: followup_intent='meeting' für meeting_booked wird gespeichert
  </behavior>

  <action>
**Schritt 1: Score-Berechnung als Helper-Funktion**

NACH dem bestehenden `_calc_call_score()` (Z.606-617), neue Funktion hinzufügen:

```python
# D.UX Phase — neue Formel mit Outcome-Multiplikator
_OUTCOME_MODIFIERS = {
    'contract_signed':    1.15,
    'meeting_booked':     1.10,
    'wrong_person':       1.00,
    'callback':           0.95,
    'send_info':          0.95,
    'gatekeeper_blocked': 0.95,
    'no_interest':        0.85,
}

def _calc_coaching_score(conv, outcome):
    """D.UX REQ-D.UX-11: coaching_score mit Outcome-Multiplikator.
    Formel: process_score = kb*0.30 + behandelt*0.30 + redeanteil*0.20 + skript*0.10 + frage*0.10
    frage_qualitaet = 0 in v1.
    final_score = clamp(round(process_score * modifier), 0, 100).
    Returns: (coaching_score_float, final_score_int, breakdown_dict)
    """
    from datetime import datetime, timezone as _tz_score
    kb = (conv.kb_end if conv is not None and conv.kb_end is not None else 30)
    kb_norm = float(kb)  # already 0-100
    einw_total = (conv.einwaende_gesamt or 0) if conv else 0
    einw_ok = (conv.einwaende_behandelt or 0) if conv else 0
    behandelt_rate = (einw_ok / einw_total) if einw_total > 0 else 0.5
    redeanteil = (conv.redeanteil_avg if conv is not None and conv.redeanteil_avg is not None else 50)
    redeanteil_score = float(max(0, 100 - abs(redeanteil - 40) * 2))
    skript = float((conv.skript_abdeckung or 0) if conv else 0)
    frage_qualitaet = 0.0  # v1 placeholder

    process_score = (
        kb_norm * 0.30
        + behandelt_rate * 100 * 0.30
        + redeanteil_score * 0.20
        + skript * 0.10
        + frage_qualitaet * 0.10
    )
    modifier = _OUTCOME_MODIFIERS.get(outcome, 1.00)
    final_score = int(min(100, max(0, round(process_score * modifier))))

    breakdown = {
        'schema_version': 1,
        'kb_end_norm': round(kb_norm, 4),
        'behandelt_rate': round(behandelt_rate, 4),
        'redeanteil_score': round(redeanteil_score, 4),
        'skript_norm': round(skript, 4),
        'frage_qualitaet': frage_qualitaet,
        'outcome_modifier': modifier,
        'process_score': round(process_score, 4),
        'final_score': final_score,
        'computed_at_iso': datetime.now(_tz_score.utc).isoformat(),
    }
    return (round(process_score * modifier, 4), final_score, breakdown)  # 0-100 scale, consistent with final_score
```

**Schritt 2: api_calls_correct_outcome erweitern**

In der Funktion, NACH der Outcome-Validierung + UUID-Prüfung + Row-Fetch:

1. `followup_intent` aus Request-Body lesen:
```python
new_followup_intent = data_co.get('followup_intent')
_VALID_FOLLOWUP = frozenset({'none', 'callback', 'meeting', 'send_info', 'retry_internal'})
if new_followup_intent not in _VALID_FOLLOWUP:
    new_followup_intent = None  # ignore invalid values
```

2. `outcome_source` vom Client akzeptieren (optional override):
```python
client_outcome_source = data_co.get('outcome_source')
_VALID_SOURCES = frozenset({'ai_auto', 'ai_auto_unsicher', 'user_corrected'})
if client_outcome_source in _VALID_SOURCES:
    row.outcome_source = client_outcome_source
else:
    row.outcome_source = 'user_corrected'
```

3. followup_intent in row schreiben:
```python
if new_followup_intent is not None:
    row.followup_intent = new_followup_intent
```

4. Score berechnen + speichern:
```python
from database.models import ConversationLog as _CLScore
conv_for_score = None
if row.conversation_log_id:
    conv_for_score = db_co.query(_CLScore).filter(_CLScore.id == row.conversation_log_id).first()
coaching_score_val, final_score_val, breakdown_data = _calc_coaching_score(conv_for_score, new_outcome)
row.coaching_score = coaching_score_val
row.score_breakdown = breakdown_data
row.score_schema_version = 1
```

5. Commit bleibt atomar (alle Updates in einem Commit)

6. Response-Format updaten:
```python
return jsonify({
    'ok': True,
    'coaching_score': coaching_score_val,
    'final_score': final_score_val,
    'outcome': row.outcome,
    'followup_intent': row.followup_intent or 'none',
})
```

**Schritt 3: Tests in tests/test_correct_outcome.py**

```python
import pytest
from routes.app_routes import _calc_coaching_score

class _MockConv:
    def __init__(self, kb=70, einw_total=2, einw_ok=2, redeanteil=40, skript=50):
        self.kb_end = kb
        self.einwaende_gesamt = einw_total
        self.einwaende_behandelt = einw_ok
        self.redeanteil_avg = redeanteil
        self.skript_abdeckung = skript

def test_score_meeting_booked():
    conv = _MockConv(kb=70, einw_total=2, einw_ok=2, redeanteil=40, skript=50)
    _, final, bd = _calc_coaching_score(conv, 'meeting_booked')
    assert final == 84

def test_score_no_interest():
    conv = _MockConv(kb=70, einw_total=2, einw_ok=2, redeanteil=40, skript=50)
    _, final, bd = _calc_coaching_score(conv, 'no_interest')
    assert final == 65

def test_score_contract_signed():
    conv = _MockConv(kb=70, einw_total=2, einw_ok=2, redeanteil=40, skript=50)
    _, final, bd = _calc_coaching_score(conv, 'contract_signed')
    assert final == 87

def test_score_breakdown_has_9_keys():
    _, _, bd = _calc_coaching_score(None, 'meeting_booked')
    expected_keys = {'schema_version', 'kb_end_norm', 'behandelt_rate', 'redeanteil_score',
                     'skript_norm', 'frage_qualitaet', 'outcome_modifier', 'process_score',
                     'final_score', 'computed_at_iso'}
    assert expected_keys.issubset(set(bd.keys()))

def test_score_breakdown_schema_version():
    _, _, bd = _calc_coaching_score(None, 'callback')
    assert bd['schema_version'] == 1

def test_score_clamp_max():
    conv = _MockConv(kb=100, einw_total=1, einw_ok=1, redeanteil=40, skript=100)
    _, final, _ = _calc_coaching_score(conv, 'contract_signed')
    assert final <= 100

def test_score_clamp_min():
    conv = _MockConv(kb=0, einw_total=1, einw_ok=0, redeanteil=0, skript=0)
    _, final, _ = _calc_coaching_score(conv, 'no_interest')
    assert final >= 0

def test_score_no_conv_uses_defaults():
    """conv=None uses fallback defaults."""
    coaching, final, bd = _calc_coaching_score(None, 'meeting_booked')
    assert isinstance(final, int)
    assert 0 <= final <= 100
```

**Commit + Deploy:**
```bash
git add routes/app_routes.py tests/test_correct_outcome.py
git commit -m "feat(08.23.2.D.UX-06): W4 coaching_score + score_breakdown + followup_intent in correct_outcome"
git push
bash deploy.sh production
```
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && source venv/bin/activate && pytest tests/test_correct_outcome.py -x -v'
    </automated>
  </verify>

  <acceptance_criteria>
- Alle pytest tests in test_correct_outcome.py grün (auf Production via SSH)
- Wave-4-Live-Test: Outcome-Confirm in PiP → inspect.sh sample calls 1 → coaching_score NOT NULL, score_breakdown NOT NULL mit 9 Schlüsseln, score_schema_version=1
- Wave-4-Live-Test: final_score in PiP nach Confirm rendert Server-Wert (nicht lokale Berechnung)
- ssh inspect.sh grep "followup_intent" routes/app_routes.py → Treffer in api_calls_correct_outcome
- ssh inspect.sh grep "score_breakdown" routes/app_routes.py → Treffer (Schreibzugriff auf Spalte)
  </acceptance_criteria>

  <done>correct_outcome Endpoint berechnet und persistiert coaching_score + score_breakdown + score_schema_version + followup_intent atomar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → correct_outcome POST | followup_intent + outcome_source kommen vom Client |
| correct_outcome → ConversationLog | Score-Berechnung liest Conv-Daten (nur eigene Calls via user_id filter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D.UX-06-01 | Tampering | outcome_source vom Client manipuliert | accept | Client gibt outcome_source an; für Phase-E-DPO-Gate ist Source relevant aber kein direkter Sicherheitsschaden — Phase F: serverseitige Validierung gegen confidence-Schwellen |
| T-D.UX-06-02 | Tampering | followup_intent vom Client manipuliert | mitigate | Validiert gegen _VALID_FOLLOWUP frozenset; DB hat CHECK constraint (Migration 0006); ungültige Werte werden ignoriert (None) |
| T-D.UX-06-03 | Denial | _calc_coaching_score Division-by-Zero | mitigate | einwaende_gesamt=0 → behandelt_rate=0.5 Fallback |
| T-D.UX-06-04 | Information Disclosure | score_breakdown enthält Gesprächs-Metriken | accept | Nur für Call-Owner zugänglich (user_id filter in query) |
</threat_model>

<verification>
Nach Deploy:
1. pytest tests/test_correct_outcome.py grün
2. inspect.sh sample calls 1 nach Outcome-Confirm → score_breakdown JSON mit 9 Schlüsseln sichtbar
3. inspect.sh sample calls 1 → score_schema_version=1
4. PiP zeigt finalen Score nach Confirm
</verification>

<success_criteria>
- Score-Formel: process_score = kb*0.30 + behandelt*0.30 + redeanteil*0.20 + skript*0.10
- Outcome-Multiplikatoren korrekt gesetzt
- score_breakdown mit 9 Schlüsseln + schema_version=1 nach Confirm
- followup_intent in DB nach Confirm
- Response: {ok, coaching_score, final_score, outcome, followup_intent}
- Deterministischer pytest grün
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-06-SUMMARY.md` erstellen.
</output>
