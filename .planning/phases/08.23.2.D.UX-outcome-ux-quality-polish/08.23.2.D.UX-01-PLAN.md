---
phase: 08.23.2.D.UX
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - alembic/versions/0006_extend_outcome_and_followup_intent.py
  - database/models.py
autonomous: true
requirements:
  - REQ-D.UX-1
  - REQ-D.UX-3

must_haves:
  truths:
    - "calls.outcome CHECK constraint akzeptiert 8 Werte: meeting_booked, callback, send_info, wrong_person, gatekeeper_blocked, no_interest, contract_signed, unknown"
    - "calls.followup_intent Spalte existiert mit DEFAULT 'none' und CHECK constraint (none/callback/meeting/send_info/retry_internal)"
    - "Bestehende Calls haben followup_intent='none' (NOT NULL DEFAULT greift)"
    - "Alembic zeigt Migration 0006 als applied"
    - "downgrade() kehrt beide Änderungen vollständig um"
  artifacts:
    - path: "alembic/versions/0006_extend_outcome_and_followup_intent.py"
      provides: "Alembic migration: extend outcome CHECK + add followup_intent"
      exports: ["upgrade", "downgrade"]
    - path: "database/models.py"
      provides: "Call model updated with new constraints and followup_intent column"
      contains: "send_info.*gatekeeper_blocked.*ck_calls_outcome"
  key_links:
    - from: "alembic/versions/0006_extend_outcome_and_followup_intent.py"
      to: "calls table on production"
      via: "alembic upgrade head in deploy.sh"
      pattern: "batch_alter_table.*calls"
    - from: "database/models.py __table_args__"
      to: "alembic batch_alter_table temp table"
      via: "render_as_batch=True in env.py"
      pattern: "ck_calls_outcome.*send_info.*gatekeeper_blocked"
---

<objective>
Alembic Migration 0006: erweitert den `calls.outcome` CHECK-Constraint um 2 neue Werte (`send_info`, `gatekeeper_blocked`) und fügt die neue Spalte `calls.followup_intent TEXT NOT NULL DEFAULT 'none'` mit eigenem CHECK-Constraint hinzu. Außerdem: models.py synchron halten.

Purpose: Wave 3 + 4 setzen voraus dass `send_info` + `gatekeeper_blocked` in DB und ORM gültig sind. Ohne diese Migration schlägt jeder INSERT/UPDATE mit neuen Outcome-Werten fehl.
Output: Migration-Datei 0006, aktualisiertes Call-Modell in models.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-SPEC.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-CONTEXT.md
@.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-RESEARCH.md

<interfaces>
<!-- Aus database/models.py Z.644-683: Aktuelles Call-Modell -->
```python
class Call(Base):
    __tablename__ = 'calls'
    id = Column(UUID_TYPE, primary_key=True, default=uuid.uuid4)
    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)
    outcome = Column(Text, nullable=True)
    audio_health_score = Column(Float, nullable=True)
    coaching_score = Column(Float, nullable=True)   # BEREITS VORHANDEN — NICHT neu hinzufügen
    outcome_confidence = Column(Float, nullable=True)
    outcome_note = Column(Text, nullable=True)
    outcome_source = Column(Text, nullable=True)
    __table_args__ = (
        CheckConstraint("outcome IN ('meeting_booked', 'callback', 'no_interest', 'wrong_person', 'contract_signed', 'unknown') OR outcome IS NULL", name='ck_calls_outcome'),
        CheckConstraint("outcome_source IN ('ai_auto', 'ai_auto_unsicher', 'user_corrected') OR outcome_source IS NULL", name='ck_calls_outcome_source'),
        ...
    )
```

<!-- Aus alembic/versions/0004_add_mode_events_to_call_events.py: Kanonisches Muster -->
```python
_NEW_CONSTRAINT = "event_type IN ('transcript_chunk', ...)"
_OLD_CONSTRAINT = "event_type IN ('transcript_chunk', ...)"
_CONSTRAINT_NAME = 'ck_call_events_event_type'

def upgrade() -> None:
    with op.batch_alter_table('call_events') as batch_op:
        batch_op.drop_constraint(_CONSTRAINT_NAME, type_='check')
        batch_op.create_check_constraint(_CONSTRAINT_NAME, _NEW_CONSTRAINT)

def downgrade() -> None:
    with op.batch_alter_table('call_events') as batch_op:
        batch_op.drop_constraint(_CONSTRAINT_NAME, type_='check')
        batch_op.create_check_constraint(_CONSTRAINT_NAME, _OLD_CONSTRAINT)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-Execute Schema Audit (Pflicht-Vorab-Check)</name>
  <files>— (read-only audit, keine Datei-Änderung)</files>
  <read_first>
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh constraints calls'
      → Bestätigen: ck_calls_outcome hat GENAU 6 Werte (kein send_info, kein gatekeeper_blocked)
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
      → Bestätigen: followup_intent Spalte NICHT vorhanden (Column existiert nicht)
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
      → Bestätigen: coaching_score Spalte BEREITS vorhanden (NICHT neu hinzufügen in irgendwelchen Migrationen)
    - ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
      → Bestätigen: letzte Migration ist 0005, noch kein 0006
    - database/models.py Z.644-683 (aktuelles Call-Modell — Code-Lesen lokal OK)
    - alembic/versions/0004_add_mode_events_to_call_events.py (kanonisches Muster — lesen und exakt replizieren)
    - alembic/versions/0005_add_outcome_fields_to_calls.py Z.1-60 (Kommentare zu batch_alter_table + Idempotenz)
  </read_first>

  <action>
AUDIT-STEP — keine Code-Änderung. Führe die inspect.sh-Befehle aus und dokumentiere:
1. Welche exakten 6 Werte hat ck_calls_outcome aktuell? (müssen sein: meeting_booked, callback, no_interest, wrong_person, contract_signed, unknown)
2. Existiert followup_intent bereits? (NEIN erwartet — falls JA: Migration 0006 muss DIESE Spalte aus dem upgrade() überspringen)
3. Existiert coaching_score bereits? (JA erwartet — Migration 0007 darf es NICHT nochmal hinzufügen)
4. Welchen Revision-Hash hat die letzte Migration (0005)?

Wenn Abweichungen gefunden: STOPP — Planner-Note melden, nicht blindlings fortfahren.
  </action>

  <verify>
    <automated>ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh constraints calls'</automated>
  </verify>
  <done>Schema-Stand dokumentiert. Pre-Conditions bestätigt. Keine Abweichungen von Erwartungen. Weiter zu Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0006 erstellen + models.py synchronisieren</name>
  <files>alembic/versions/0006_extend_outcome_and_followup_intent.py, database/models.py</files>
  <read_first>
    - alembic/versions/0004_add_mode_events_to_call_events.py (exaktes Muster für batch_alter_table drop_constraint + create_check_constraint)
    - alembic/versions/0005_add_outcome_fields_to_calls.py (revision/down_revision Pattern, branch_labels, depends_on)
    - database/models.py Z.644-683 (Call.__table_args__ — muss synchron mit Migration sein)
    - alembic/env.py Z.1-40 (render_as_batch=True bestätigen)
    - 30 Zeilen NACH models.py Z.683 (was folgt nach Call model — kein versehentliches Überschreiben von CallEvent)
  </read_first>

  <action>
**Schritt 1: Migration 0006 erstellen**

Datei: `alembic/versions/0006_extend_outcome_and_followup_intent.py`

```python
"""Extend outcome CHECK + add followup_intent to calls.

Revision ID: 0006
Revises: 0005
Create Date: 2026-05-28
"""
from alembic import op
import sqlalchemy as sa

revision = '0006'
down_revision = '0005'
branch_labels = None
depends_on = None

# ── Outcome CHECK constraint ─────────────────────────────────────────────────

_CK_OUTCOME = 'ck_calls_outcome'

_NEW_OUTCOME_CONSTRAINT = (
    "outcome IN ('meeting_booked', 'callback', 'send_info', 'wrong_person', "
    "'gatekeeper_blocked', 'no_interest', 'contract_signed', 'unknown') OR outcome IS NULL"
)
_OLD_OUTCOME_CONSTRAINT = (
    "outcome IN ('meeting_booked', 'callback', 'no_interest', 'wrong_person', "
    "'contract_signed', 'unknown') OR outcome IS NULL"
)

# ── followup_intent column + CHECK ──────────────────────────────────────────

_CK_FOLLOWUP = 'ck_calls_followup_intent'
_FOLLOWUP_CONSTRAINT = (
    "followup_intent IN ('none', 'callback', 'meeting', 'send_info', 'retry_internal')"
)


def upgrade() -> None:
    with op.batch_alter_table('calls') as batch_op:
        # 1. Extend outcome CHECK constraint (drop old 6-value, add new 8-value)
        batch_op.drop_constraint(_CK_OUTCOME, type_='check')
        batch_op.create_check_constraint(_CK_OUTCOME, _NEW_OUTCOME_CONSTRAINT)
        # 2. Add followup_intent column (NOT NULL with server_default='none')
        batch_op.add_column(
            sa.Column('followup_intent', sa.Text(), nullable=False,
                      server_default='none')
        )
        # 3. Add followup_intent CHECK constraint
        batch_op.create_check_constraint(_CK_FOLLOWUP, _FOLLOWUP_CONSTRAINT)


def downgrade() -> None:
    with op.batch_alter_table('calls') as batch_op:
        # Revert followup_intent (CHECK first, then column)
        batch_op.drop_constraint(_CK_FOLLOWUP, type_='check')
        batch_op.drop_column('followup_intent')
        # Revert outcome CHECK to 6 values
        batch_op.drop_constraint(_CK_OUTCOME, type_='check')
        batch_op.create_check_constraint(_CK_OUTCOME, _OLD_OUTCOME_CONSTRAINT)
```

**Schritt 2: database/models.py synchronisieren**

In der `Call`-Klasse, `__table_args__`-Tuple (Z.672-683):
- CheckConstraint `ck_calls_outcome`: Werte-Liste um `'send_info'` + `'gatekeeper_blocked'` erweitern (vor `'no_interest'`)
  VORHER: `"outcome IN ('meeting_booked', 'callback', 'no_interest', 'wrong_person', 'contract_signed', 'unknown') OR outcome IS NULL"`
  NACHHER: `"outcome IN ('meeting_booked', 'callback', 'send_info', 'wrong_person', 'gatekeeper_blocked', 'no_interest', 'contract_signed', 'unknown') OR outcome IS NULL"`
- Neue Column VOR `__table_args__` einfügen: `followup_intent = Column(Text, nullable=False, server_default='none')`
- In `__table_args__` neue CheckConstraint hinzufügen:
  `CheckConstraint("followup_intent IN ('none', 'callback', 'meeting', 'send_info', 'retry_internal')", name='ck_calls_followup_intent'),`

**KRITISCH — models.py + Migration MÜSSEN identische Constraint-Definitionen haben.** Alembic SQLite batch_alter_table rebuildet die Tabelle anhand von models.py — Mismatch führt zu Downgrade-Fehler (dokumentiert in Migration 0005 Kommentaren).

**Schritt 3: Deploy + Migration ausführen**
```bash
git add alembic/versions/0006_extend_outcome_and_followup_intent.py database/models.py
git commit -m "feat(08.23.2.D.UX-01): migration 0006 - extend outcome CHECK + add followup_intent"
git push
bash deploy.sh production
```
Deploy.sh führt `alembic upgrade head` automatisch aus.
  </action>

  <verify>
    <automated>
bash deploy.sh production && ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh constraints calls'
# Erwartete Ausgabe: ck_calls_outcome zeigt 8 Werte inkl. send_info + gatekeeper_blocked
# Erwartete Ausgabe: ck_calls_followup_intent zeigt none/callback/meeting/send_info/retry_internal
# Danach: ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh schema calls'
# Erwartete Ausgabe: followup_intent Spalte vorhanden, NOT NULL, DEFAULT 'none'
# Danach: ssh deploy@getnerve.app 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
# Erwartete Ausgabe: 0006 als current head
    </automated>
  </verify>

  <acceptance_criteria>
- ssh inspect.sh constraints calls → ck_calls_outcome enthält genau 8 Werte (meeting_booked, callback, send_info, wrong_person, gatekeeper_blocked, no_interest, contract_signed, unknown)
- ssh inspect.sh constraints calls → ck_calls_followup_intent existiert mit 5 Werten
- ssh inspect.sh schema calls → followup_intent Spalte vorhanden, NOT NULL, DEFAULT 'none'
- ssh inspect.sh sample calls 10 → alle Rows haben followup_intent='none' (kein NULL)
- ssh inspect.sh migrations → 0006 ist applied
- Downgrade-Test (optional auf Production): alembic downgrade -1 → alembic upgrade head → Schema identisch
  </acceptance_criteria>

  <done>Migration 0006 auf Production deployed. followup_intent Spalte vorhanden. ck_calls_outcome hat 8 Werte. Alle bestehenden Rows haben followup_intent='none'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration-Script → PostgreSQL | DDL-Statements werden als DB-User ausgeführt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-D.UX-01-01 | Tampering | alembic/versions/0006 | mitigate | Migration via Git versioniert; deploy.sh zieht nur committed Code |
| T-D.UX-01-02 | Denial | batch_alter_table auf großer calls-Tabelle | accept | SQLite: rebuild nötig, aber calls-Tabelle ist klein (Pre-Launch); Postgres: ALTER direkt, kein rebuild |
| T-D.UX-01-03 | Information Disclosure | followup_intent in DB | mitigate | Art. 6 Abs. 1f DSGVO-Rechtsgrundlage wird in Plan 08 dokumentiert; Spalte enthält nur Enum-Wert, keine PII direkt |
</threat_model>

<verification>
Manuelle Verifikation nach Deploy:
1. `ssh inspect.sh constraints calls` — 8 outcome-Werte, ck_calls_followup_intent vorhanden
2. `ssh inspect.sh sample calls 10` — followup_intent-Spalte sichtbar, alle 'none'
3. `ssh inspect.sh migrations` — 0006 listed as current
</verification>

<success_criteria>
- Migration 0006 deployed und applied
- calls.outcome CHECK akzeptiert send_info + gatekeeper_blocked
- calls.followup_intent existiert NOT NULL DEFAULT 'none'
- database/models.py synchron mit Migration
- Downgrade funktioniert vollständig
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.23.2.D.UX-outcome-ux-quality-polish/08.23.2.D.UX-01-SUMMARY.md` erstellen.
</output>
