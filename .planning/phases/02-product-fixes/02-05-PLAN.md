---
phase: 02-product-fixes
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-04]
files_modified:
  - templates/onboarding.html
  - templates/profile_editor.html
  - database/models.py
  - app.py
  - routes/onboarding.py
  - routes/dashboard.py
  - templates/dashboard.html
autonomous: true
requirements: [PROD-08, PROD-10]
must_haves:
  truths:
    - "Onboarding placeholder says 'Vorname' not 'Max'"
    - "Onboarding step 1 shows 3 Beispiel-Boxen (Gegenargument, Kaufbereitschaft, Coaching-Tipp)"
    - "Onboarding has Dashboard-Style card selector (Fokus/Vollstaendig)"
    - "User model has dashboard_style column (String(20), default vollstaendig)"
    - "Dashboard route reads dashboard_style and conditionally renders widgets"
    - "Profile editor shows generic placeholders (Ihr Unternehmen, Ihr Produkt)"
  artifacts:
    - path: "templates/onboarding.html"
      provides: "Fixed placeholders, Beispiel-Boxen, Dashboard-Style selector"
    - path: "templates/profile_editor.html"
      provides: "Generic placeholder text"
    - path: "database/models.py"
      provides: "User.dashboard_style column"
      contains: "dashboard_style"
    - path: "routes/dashboard.py"
      provides: "Conditional widget rendering based on dashboard_style"
  key_links:
    - from: "templates/onboarding.html"
      to: "routes/onboarding.py"
      via: "POST form submission with dashboard_style field"
      pattern: "dashboard_style"
    - from: "routes/dashboard.py"
      to: "User.dashboard_style"
      via: "attribute read for conditional rendering"
      pattern: "dashboard_style.*fokus\|vollstaendig"
---

<objective>
Fix onboarding placeholders, add Beispiel-Boxen and Dashboard-Style selector, fix profile editor placeholders (PROD-08, PROD-10).

Purpose: New users must see generic, professional placeholders instead of demo content. Beispiel-Boxen explain the product value. Dashboard-Style lets users choose widget density.
Output: Polished onboarding flow with 3 preview cards and style selector; clean profile editor placeholders.

Note: This plan depends on 02-04 because both modify database/models.py (_migrate). Plan 02-04 adds Organisation fair-use columns; this plan adds User.dashboard_style. Running sequentially avoids migration conflicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-product-fixes/02-CONTEXT.md
@.planning/phases/02-product-fixes/02-RESEARCH.md
@.planning/phases/02-product-fixes/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix onboarding placeholders, add Beispiel-Boxen and Dashboard-Style selector</name>
  <files>templates/onboarding.html, database/models.py, app.py, routes/onboarding.py</files>
  <read_first>templates/onboarding.html, database/models.py, app.py, routes/onboarding.py</read_first>
  <action>
Per D-09, D-10, D-11:

**1. Fix placeholders in onboarding.html** (D-09):
- Find `placeholder="Max"` (~line 226) and change to `placeholder="Vorname"`
- Scan all other input placeholders in the template for NERVE-specific demo content — replace with generic DACH B2B equivalents
- Any hardcoded person/company/product names must become generic

**2. Add Beispiel-Boxen to onboarding step 1** (D-11):
Per UI-SPEC, add 3 static preview cards in step 1 (the welcome/intro step). Insert after the step heading, before any form fields. HTML structure:

```html
<div class="beispiel-boxen" style="display:grid; gap:12px; margin:var(--space-md) 0;">
  <div class="beispiel-box">
    <div class="beispiel-label">Gegenargument</div>
    <div class="beispiel-body">Das verstehe ich — viele Kunden sagen das am Anfang. Was waere fuer Sie konkret der Unterschied zu Ihrer aktuellen Loesung?</div>
  </div>
  <div class="beispiel-box">
    <div class="beispiel-label">Kaufbereitschaft</div>
    <div class="beispiel-body">72% — Starkes Interesse erkannt. Jetzt auf Abschluss steuern.</div>
  </div>
  <div class="beispiel-box">
    <div class="beispiel-label">Coaching-Tipp</div>
    <div class="beispiel-body">Lass den Kunden ausreden. Stille nach einer Frage ist gut — sie zeigt Nachdenken.</div>
  </div>
</div>
```

CSS for `.beispiel-box` (add in the template's `<style>` block). IMPORTANT: Do not use hardcoded color values — use CSS custom properties exclusively. If a needed opacity variant does not exist as a custom property, define a local custom property at the top of the style block (e.g., `--color-gold-subtle: color-mix(in srgb, var(--color-amber) 5%, transparent)`) and reference it:
```css
.beispiel-box {
  background: var(--color-gold-subtle, color-mix(in srgb, var(--color-amber) 5%, transparent));
  border: 1px solid var(--color-border-gold);
  border-radius: var(--radius-lg);
  padding: var(--space-sm) var(--space-md);
  font-size: 0.75rem;
  font-family: var(--font-body);
  color: var(--color-text-secondary);
  line-height: 1.65;
}
.beispiel-label {
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: .07em;
  text-transform: uppercase;
  color: var(--color-amber);
  margin-bottom: var(--space-xs);
}
@media (min-width: 580px) {
  .beispiel-boxen { grid-template-columns: repeat(3, 1fr); }
}
```

**3. Add Dashboard-Style selector** (D-10):
IMPORTANT: `dashboard_stil` (Text) already exists on User for gamification text style. The new field is `dashboard_style` (String(20)) — these are DIFFERENT fields.

a. Add column to User model in `database/models.py`:
```python
# Block 8: Dashboard Layout Preference
dashboard_style = Column(String(20), default='vollstaendig')
```

b. Add migration in `app.py _migrate()` users table block:
```python
('dashboard_style', "VARCHAR(20) DEFAULT 'vollstaendig'"),
```

c. In `templates/onboarding.html`, add a Dashboard-Style selection step. Best placement: as a new section in an existing step (e.g., step 2) or between steps 2 and 3. Add 2 cards using the `.t-modus-card` / `.t-modus-active` pattern from UI-SPEC:

```html
<div class="dashboard-style-section">
  <h3 style="font-family:var(--font-headline);font-size:1.25rem;font-weight:600;text-align:center;margin-bottom:var(--space-md);">Dashboard-Ansicht</h3>
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:var(--space-sm);">
    <div class="style-card selected" data-style="vollstaendig" onclick="selectDashboardStyle('vollstaendig')">
      <strong>Vollstaendig</strong>
      <p style="font-size:0.75rem;color:var(--color-text-secondary);margin-top:var(--space-xs);">Alle Widgets: ROI, Performance, Verlauf, Coaching</p>
    </div>
    <div class="style-card" data-style="fokus" onclick="selectDashboardStyle('fokus')">
      <strong>Fokus</strong>
      <p style="font-size:0.75rem;color:var(--color-text-secondary);margin-top:var(--space-xs);">Nur ROI, Kaufbereitschaft und naechstes Training</p>
    </div>
  </div>
  <input type="hidden" name="dashboard_style" id="dashboardStyleInput" value="vollstaendig">
</div>
```

CSS for `.style-card` (reuse card selection pattern). IMPORTANT: No hardcoded hex or rgba — use CSS custom properties:
```css
.style-card {
  background: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-md);
  cursor: pointer;
  text-align: center;
  transition: border-color 0.2s, background 0.2s;
}
.style-card.selected {
  border-color: var(--color-amber);
  background: var(--color-amber-subtle, color-mix(in srgb, var(--color-amber) 8%, transparent));
}
```

JS for selection:
```javascript
function selectDashboardStyle(style) {
  document.querySelectorAll('.style-card').forEach(c => c.classList.remove('selected'));
  document.querySelector('[data-style="'+style+'"]').classList.add('selected');
  document.getElementById('dashboardStyleInput').value = style;
}
```

d. In `routes/onboarding.py`, update the POST handler to read `dashboard_style` from form data and save to user:
```python
user.dashboard_style = request.form.get('dashboard_style', 'vollstaendig')
```
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && grep -c "beispiel-box" templates/onboarding.html && grep -c "dashboard_style" templates/onboarding.html && grep -n "dashboard_style" database/models.py | head -3 && grep "placeholder.*Vorname" templates/onboarding.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - templates/onboarding.html does NOT contain `placeholder="Max"` — contains `placeholder="Vorname"` instead
    - templates/onboarding.html contains 3 `.beispiel-box` elements with labels: Gegenargument, Kaufbereitschaft, Coaching-Tipp
    - templates/onboarding.html contains `.style-card` elements for vollstaendig and fokus
    - templates/onboarding.html contains hidden input `name="dashboard_style"`
    - database/models.py User class contains `dashboard_style = Column(String(20), default='vollstaendig')`
    - database/models.py User class still contains `dashboard_stil = Column(Text)` (NOT overwritten)
    - app.py `_migrate()` contains `ALTER TABLE` for `dashboard_style`
    - routes/onboarding.py POST handler saves `dashboard_style` to user
    - All new CSS uses CSS custom properties (no hardcoded hex/rgba values)
  </acceptance_criteria>
  <done>Onboarding has generic placeholders, 3 Beispiel-Boxen, and Dashboard-Style selector that persists to User model</done>
</task>

<task type="auto">
  <name>Task 2: Fix profile editor placeholders and wire dashboard_style rendering</name>
  <files>templates/profile_editor.html, routes/dashboard.py, templates/dashboard.html</files>
  <read_first>templates/profile_editor.html, routes/dashboard.py, templates/dashboard.html</read_first>
  <action>
Per D-14 and D-10:

**1. Profile editor placeholders** (D-14, PROD-10):
In `templates/profile_editor.html`, scan ALL input placeholders and replace any demo/specific content:
- Company names -> `placeholder="Ihr Unternehmen"`
- Product names -> `placeholder="Ihr Produkt oder Service"`
- Person names -> `placeholder="Ihr Name"`
- Contact person -> `placeholder="Ansprechpartner"`
- Any other NERVE-specific demo text -> generic DACH B2B equivalents

This is a pure template edit. Read the entire file first to find all placeholder attributes.

**2. Wire dashboard_style to dashboard rendering** (D-10):
In `routes/dashboard.py` index/main route:
- After loading user from DB, read `dashboard_style`:
```python
dashboard_style = getattr(user, 'dashboard_style', 'vollstaendig')
```
- Pass to template context:
```python
return render_template('dashboard.html', ..., dashboard_style=dashboard_style)
```

In `templates/dashboard.html`:
- Wrap optional widget sections in a Jinja2 conditional:
```html
{% if dashboard_style != 'fokus' %}
  <!-- Full widgets: performance history, coaching tips, detailed stats -->
  ...
{% endif %}

<!-- Always visible: ROI widget, Kaufbereitschaft, next-session CTA -->
```

Identify which widgets should always show (ROI, Kaufbereitschaft, next training CTA) vs which are hidden in fokus mode (session history table, detailed stats, coaching summary, streak/gamification widgets). Keep ROI tracker, Kaufbereitschaft gauge, and a "Starte Training" CTA always visible.
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && grep -c "Ihr Unternehmen\|Ihr Produkt\|Ihr Name\|Ansprechpartner" templates/profile_editor.html && grep -n "dashboard_style" routes/dashboard.py | head -5 && grep -n "dashboard_style\|fokus" templates/dashboard.html | head -5</automated>
  </verify>
  <acceptance_criteria>
    - templates/profile_editor.html contains `placeholder="Ihr Unternehmen"` (at least 1 match)
    - templates/profile_editor.html contains `placeholder="Ihr Produkt oder Service"` (at least 1 match)
    - templates/profile_editor.html does NOT contain hardcoded demo company/product names in placeholders
    - routes/dashboard.py reads `dashboard_style` from user and passes to template
    - templates/dashboard.html contains `{% if dashboard_style != 'fokus' %}` conditional (or equivalent)
    - In fokus mode, ROI widget and Kaufbereitschaft are still visible
  </acceptance_criteria>
  <done>Profile editor shows generic placeholders; dashboard conditionally renders widgets based on user's dashboard_style preference</done>
</task>

</tasks>

<verification>
1. `grep "Max" templates/onboarding.html` returns 0 matches for placeholder usage
2. `grep "beispiel-box" templates/onboarding.html` returns 3+ matches
3. `grep "dashboard_style" database/models.py` returns column definition
4. `grep "Ihr Unternehmen" templates/profile_editor.html` returns placeholder match
5. `grep "fokus" templates/dashboard.html` returns conditional rendering
</verification>

<success_criteria>
- Onboarding uses generic German placeholders throughout
- 3 Beispiel-Boxen explain NERVE's value with sample Gegenargument, Kaufbereitschaft, Coaching-Tipp
- Dashboard-Style selector works with 2 options (Vollstaendig default, Fokus)
- User.dashboard_style persists correctly (coexists with existing dashboard_stil)
- Profile editor has zero demo-specific placeholder content
- Dashboard renders correct widget set based on user preference
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-fixes/02-05-SUMMARY.md`
</output>
