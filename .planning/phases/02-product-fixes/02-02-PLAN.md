---
phase: 02-product-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - static/app.js
  - templates/app.html
autonomous: true
requirements: [PROD-07]
must_haves:
  truths:
    - "DSGVO banner appears BEFORE getUserMedia is called"
    - "Script button opens the script panel when a profile with phasen is active"
    - "Compact mode circles render correctly in the kompakt-panel"
    - "Toggle button is positioned correctly"
  artifacts:
    - path: "static/app.js"
      provides: "Fixed DSGVO banner timing and script button logic"
    - path: "templates/app.html"
      provides: "Fixed compact mode CSS and toggle position"
  key_links:
    - from: "static/app.js"
      to: "DSGVO banner DOM element"
      via: "Banner show before getUserMedia"
      pattern: "dsgvoBanner.*visible.*getUserMedia"
    - from: "templates/app.html"
      to: ".kompakt-panel .metric-circle"
      via: "CSS rendering fix"
      pattern: "kompakt.*metric-circle"
---

<objective>
Fix 4 live-mode bugs in the coaching interface (PROD-07). The DSGVO banner timing fix is legally critical.

Purpose: The live coaching interface must work correctly for first-impression quality. The DSGVO banner must appear before microphone access per German data protection law.
Output: All 4 live-mode bugs resolved in app.html and app.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-product-fixes/02-CONTEXT.md
@.planning/phases/02-product-fixes/02-RESEARCH.md
@.planning/phases/02-product-fixes/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DSGVO banner timing and script button state (app.js)</name>
  <files>static/app.js, routes/app_routes.py</files>
  <read_first>static/app.js, routes/app_routes.py, templates/app.html</read_first>
  <action>
Per D-08, two JS fixes in `static/app.js`:

**Bug 1 — DSGVO Banner (CRITICAL):**
The DSGVO consent banner currently fires when the first transcript arrives (app.js ~line 134, inside the Socket.IO `transcript` event handler). This means `getUserMedia` has ALREADY been called.

Fix:
1. Find the `getUserMedia` call site in app.js (search for `navigator.mediaDevices.getUserMedia`)
2. BEFORE that call, show the DSGVO banner:
```javascript
const dsgvoBanner = document.getElementById('dsgvoBanner');
if (dsgvoBanner && !dsgvoBanner._shown) {
    dsgvoBanner._shown = true;
    dsgvoBanner.classList.add('visible');
    setTimeout(() => dsgvoBanner.classList.remove('visible'), 6000);
}
```
3. Remove or disable the old banner display code from the `transcript` event handler (line ~134) so it does not fire a second time
4. The banner must be visible for at least a brief moment before `getUserMedia` is called. If the current flow calls getUserMedia immediately on session start, add the banner display BEFORE the getUserMedia call in the session start flow.

**Bug 2 — Script Button:**
The `#skriptToggleBtn` starts with class `disabled` in app.html (~line 444). `initSkript()` in app.js (~line 854) is called with `active_phasen` injected server-side (~line 957).

Fix:
1. Read `routes/app_routes.py` live() route to verify what `active_phasen` is set to — ensure it correctly reads from the active profile's `daten` JSON → `phasen` key
2. In app.js `initSkript()`, verify the button is enabled (remove `disabled` class) when `skriptData` has at least one phase with items: `skriptData.some(p => p.items && p.items.length > 0)`
3. If `active_phasen` is arriving as `null` or empty string from server when a profile with phasen IS active, fix the server-side injection in `routes/app_routes.py` live() route
4. If the JS logic is correct but the button still stays disabled, check if `initSkript()` is called at the right time (after DOM ready) and if `skriptData` is correctly parsed from the server-injected value
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && grep -n "getUserMedia" static/app.js | head -5 && echo "---" && grep -n "dsgvoBanner" static/app.js | head -10 && echo "---" && grep -n "skriptToggleBtn\|initSkript\|disabled" static/app.js | head -10</automated>
  </verify>
  <acceptance_criteria>
    - static/app.js contains `dsgvoBanner` display logic BEFORE or at the same location as the `getUserMedia` call (not inside `transcript` event handler)
    - The old banner display in the `transcript` event handler is removed or guarded to not fire
    - `initSkript` function enables the button (removes `disabled` class) when skriptData has phases with items
    - routes/app_routes.py live() route correctly injects `active_phasen` from the active profile
  </acceptance_criteria>
  <done>DSGVO banner fires before microphone access; script button enables when profile has phasen data</done>
</task>

<task type="auto">
  <name>Task 2: Fix compact mode circles CSS and toggle button position (app.html)</name>
  <files>templates/app.html</files>
  <read_first>templates/app.html, static/app.js</read_first>
  <action>
Per D-08, two CSS/DOM fixes in `templates/app.html`:

**Bug 3 — Compact Mode Circles:**
`.metric-circle` elements are defined globally (~line 196) but may not render correctly inside `#kompaktPanel` due to different flex/grid constraints.

Fix:
1. Search for `.metric-circle` CSS rules in app.html
2. Search for `#kompaktPanel` or `.kompakt-panel` to find the compact mode container
3. If `.metric-circle` inside the compact panel is clipped or not visible, add a scoped CSS override:
```css
.kompakt-panel .metric-circle {
    /* Adjust width, height, flex-shrink as needed based on inspection */
    width: 60px;
    height: 60px;
    flex-shrink: 0;
}
```
4. The exact fix depends on what the rendering bug looks like — inspect the existing CSS for `.metric-circle` and `.kompakt-panel` to determine if it is a sizing, overflow, or display issue. The circles must be fully visible and properly sized in compact mode.

**Bug 4 — Toggle Button Position:**
`.btn-view-toggle` is currently `position:fixed;top:8px;right:16px` (~line 96) and rendered inside the profile-bar div (~line 467).

Fix:
1. Per Research open question 3: the toggle is likely overlapping or positioned incorrectly relative to the header (52px height)
2. Move the toggle to a semantically correct position — either:
   a. Keep `position:fixed` but adjust `top` to account for header height (e.g., `top: 60px` or `top: calc(52px + 8px)`) so it sits below the header
   b. Or remove `position:fixed` and place it inside the header bar element as a child, styled with `position:absolute` within the header
3. Choose the approach that keeps the toggle visible and accessible in both normal and compact modes without overlapping header content
4. Use CSS custom properties from UI-SPEC for spacing: `var(--space-sm)` for gaps
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && grep -n "kompakt.*metric-circle\|metric-circle.*kompakt\|btn-view-toggle" templates/app.html | head -10</automated>
  </verify>
  <acceptance_criteria>
    - templates/app.html contains a CSS rule scoped to `.kompakt-panel .metric-circle` (or equivalent fix ensuring circles render in compact mode)
    - `.btn-view-toggle` position CSS is updated (no longer `top:8px` if that caused overlap with 52px header)
    - Both fixes use CSS custom properties where applicable (not hardcoded hex values)
  </acceptance_criteria>
  <done>Compact mode circles display correctly; toggle button is positioned without overlapping the header</done>
</task>

</tasks>

<verification>
1. `grep -n "dsgvoBanner" static/app.js` shows banner logic near getUserMedia, not in transcript handler
2. `grep -n "kompakt.*metric" templates/app.html` shows scoped CSS fix
3. `grep -n "btn-view-toggle" templates/app.html` shows updated position
</verification>

<success_criteria>
- DSGVO banner appears before any microphone access (legally compliant)
- Script button activates when profile has phase data
- Compact mode circles render fully visible
- Toggle button positioned correctly relative to header
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-fixes/02-02-SUMMARY.md`
</output>
