---
phase: 02-product-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/training.html
  - routes/training.py
autonomous: true
requirements: [PROD-03, PROD-04, PROD-05, PROD-06]
must_haves:
  truths:
    - "User selecting Frei mode cannot use hint system during training"
    - "User selecting Gefuehrt mode can request hints with visible point deduction"
    - "User sees a preview section after a training session ends showing what NERVE would have displayed in a real call"
    - "All 11 standard scenarios are visible in the training scenario selector"
    - "User can filter scenarios by Schwierigkeit level"
  artifacts:
    - path: "templates/training.html"
      provides: "Training UI with mode cards, help button visibility, live preview, scenario selector"
    - path: "routes/training.py"
      provides: "Training route with modus handling, scoring, penalty, live_preview call"
  key_links:
    - from: "templates/training.html JS selectModus()"
      to: "#t-helpRow visibility"
      via: "style.display toggle"
      pattern: "helpRow.*display.*free.*none"
    - from: "routes/training.py training_end"
      to: "services/training_service.py _generate_live_preview()"
      via: "function call in POST handler"
      pattern: "_generate_live_preview"
---

<objective>
Verify and polish training modes (Frei/Gefuehrt), post-training preview, and scenario selector (PROD-03, PROD-04, PROD-05, PROD-06).

Purpose: These features are already implemented. This plan confirms they work correctly and fixes any issues found during verification.
Output: Verified training flow with confirmed mode switching, scoring, preview rendering, and scenario filtering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-product-fixes/02-CONTEXT.md
@.planning/phases/02-product-fixes/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify training modes (Frei/Gefuehrt) and post-training preview</name>
  <files>templates/training.html, routes/training.py, services/training_service.py</files>
  <read_first>templates/training.html, routes/training.py, services/training_service.py</read_first>
  <action>
Per D-05 and D-06, these features are already implemented. Verify and fix if broken:

**Training Modes (PROD-03 + PROD-04):**

1. In `templates/training.html`, find the `selectModus()` JS function and verify:
   - When `selectModus('free')` is called, `trainingModus` is set to `'free'`
   - The `.t-modus-card` for free gets `.t-modus-active` class; guided card loses it
   - In `initChat()` (~line 539): `#t-helpRow` has `display: 'none'` when `trainingModus === 'free'`
   - Default mode on page load is Gefuehrt (guided card has `t-modus-active` class in HTML)

2. In `routes/training.py`, verify the scoring logic in the training_end handler (~line 314-317):
   - Penalty formula: `penalty = min(hilfe_count * 5, 30)`
   - Points floor: `points = max(base_points - penalty, base_points // 2)`
   - Frei mode bonus: verify if there is a `+50%` bonus when modus is 'free' — if missing, add:
     ```python
     if modus == 'free':
         points = int(points * 1.5)
     ```

3. If `selectModus('free')` does not hide the help button, add the display toggle. If it already works, document in summary that it was verified correct.

**Post-Training Preview (PROD-05):**

4. In `routes/training.py` training_end handler, verify `_generate_live_preview()` is called from `services/training_service.py`
5. In `templates/training.html`, find `.t-live-preview` section (~line 908) and verify it renders the preview data returned from the route
6. If the preview section is hidden or not populated, check the JS that handles the training_end response and populates the preview DOM elements

If everything is already correct, no code changes needed — just document verification results.
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && grep -n "selectModus\|t-helpRow\|t-helpBtn\|trainingModus" templates/training.html | head -15 && echo "---" && grep -n "_generate_live_preview\|hilfe_count.*5\|modus.*free" routes/training.py | head -10</automated>
  </verify>
  <acceptance_criteria>
    - templates/training.html JS `selectModus('free')` sets `trainingModus = 'free'`
    - templates/training.html `initChat()` hides `#t-helpRow` when `trainingModus === 'free'`
    - routes/training.py training_end contains `min(hilfe_count * 5, 30)` penalty calculation
    - routes/training.py training_end calls `_generate_live_preview()`
    - templates/training.html contains `.t-live-preview` section with rendering logic
  </acceptance_criteria>
  <done>Training modes switch correctly: Frei hides help, Gefuehrt shows help with penalty. Post-training preview renders after session end.</done>
</task>

<task type="auto">
  <name>Task 2: Verify training scenarios and schwierigkeit filter</name>
  <files>app.py, routes/training.py, templates/training.html</files>
  <read_first>app.py, routes/training.py, templates/training.html</read_first>
  <action>
Per D-07, all 11 standard DACH Mittelstand scenarios are already seeded. Verify and fix if broken:

**Scenario Seeding (PROD-06):**

1. In `app.py`, read `_seed_training_scenarios()` (~lines 449-571) and verify all 11 scenarios exist:
   - 3x Leicht: Warmer Lead, Empfehlung, Follow-up nach Demo
   - 3x Mittel: Kaltakquise, Wettbewerber-Wechsel, Preisverhandlung
   - 3x Schwer: Abwimmler, Technischer Entscheider, Einkaufer
   - 2x Sekretaerin: Blockt + Entscheiderin
   Count the scenarios. If fewer than 11, identify and add the missing ones.

2. Verify each scenario has `schwierigkeit` set to the correct level ('leicht', 'mittel', 'schwer', 'sekretaerin') and `spezial_einwaende` populated as a JSON array of strings.

**Scenario Selector UI:**

3. In `routes/training.py`, find the `/training/scenarios` or equivalent API endpoint that returns scenarios. Verify it returns all scenarios for the user's org.

4. In `templates/training.html`, find the scenario selector UI and verify:
   - Scenarios are listed (rendered from the route response)
   - A `schwierigkeit` filter exists (buttons or tabs for leicht/mittel/schwer/sekretaerin)
   - Clicking a filter shows only scenarios of that difficulty
   - `spezial_einwaende` from each scenario are passed to the training engine (sent to the training start POST request)

5. If the schwierigkeit filter UI does not exist or is broken, add/fix it:
   - Add filter buttons: "Alle", "Leicht", "Mittel", "Schwer", "Sekretaerin"
   - JS filters the scenario list by matching `schwierigkeit` attribute on each scenario card
   - Default: show all

If everything works correctly, no changes needed — document in summary.
  </action>
  <verify>
    <automated>cd /c/Users/andre/OneDrive/Desktop/salesnerve && grep -c "TrainingScenario(" app.py && echo "scenarios seeded" && grep -n "schwierigkeit" templates/training.html | head -10 && echo "---" && grep -n "scenarios\|schwierigkeit" routes/training.py | head -10</automated>
  </verify>
  <acceptance_criteria>
    - app.py `_seed_training_scenarios()` creates exactly 11 TrainingScenario objects
    - Each scenario has `schwierigkeit` in ('leicht', 'mittel', 'schwer', 'sekretaerin')
    - Each scenario has `spezial_einwaende` as a non-empty JSON string
    - templates/training.html has a schwierigkeit filter UI (buttons or equivalent)
    - routes/training.py has an endpoint returning scenarios (GET /training/scenarios or embedded in page context)
  </acceptance_criteria>
  <done>All 11 standard DACH scenarios are seeded, visible in selector, filterable by difficulty, and spezial_einwaende are used by the training engine</done>
</task>

</tasks>

<verification>
1. `grep -c "TrainingScenario(" app.py` returns 11
2. `grep "selectModus\|t-helpRow" templates/training.html` confirms mode switching exists
3. `grep "_generate_live_preview" routes/training.py` confirms preview is called
</verification>

<success_criteria>
- Training Frei mode hides help, awards bonus points
- Training Gefuehrt mode shows help, deducts penalty per hint
- Post-training preview renders "Was NERVE im echten Call gezeigt haette"
- All 11 scenarios visible and filterable by difficulty
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-fixes/02-03-SUMMARY.md`
</output>
