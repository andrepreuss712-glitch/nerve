---
phase: 04.11-coach-modul-inserted
plan: 03
type: execute
wave: 2
depends_on: ["04.11-01"]
files_modified:
  - services/claude_service.py
  - services/live_session.py
  - routes/app_routes.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Active learning cards are loaded into live_session state at session start"
    - "Learning card context is injected into the live Claude Haiku prompt"
    - "Haiku system prompt instructs Claude to return lernkarte_match fields when a card matches"
    - "Cards are cleared on session reset"
    - "load_learning_cards(user_id) is called after set_active_profile() in routes/app_routes.py"
  artifacts:
    - path: "services/live_session.py"
      provides: "Learning card loading at session start, clearing at reset"
      contains: "active_learning_cards"
    - path: "services/claude_service.py"
      provides: "Learning card context injection into kontext + lernkarte_match in system prompt JSON schema"
      contains: "lernkarte_match"
    - path: "routes/app_routes.py"
      provides: "load_learning_cards call wired after set_active_profile"
      contains: "load_learning_cards"
  key_links:
    - from: "services/claude_service.py (_build_system_prompt)"
      to: "static/app.js (pollErgebnis)"
      via: "lernkarte_match field in JSON response schema flows through state['ergebnis'] to frontend"
      pattern: "lernkarte_match"
    - from: "routes/app_routes.py"
      to: "services/live_session.py"
      via: "load_learning_cards(g.user.id) call after set_active_profile()"
      pattern: "load_learning_cards"
    - from: "services/claude_service.py (analyse_loop)"
      to: "services/live_session.py"
      via: "state['active_learning_cards'] read in analyse_loop"
      pattern: "active_learning_cards"
---

<objective>
Inject active learning cards into the live Claude Haiku prompt so the KI can recognize situations matching user's learning goals, return a lernkarte_match field in the JSON response, and wire the load_learning_cards call at session start.

Purpose: D-09 — "Vor jedem Call werden aktive Lernkarten des Users geladen und als Kontext an den Claude-Prompt im Live-Assistenten uebergeben." Plus the system prompt schema extension for lernkarte_match that completes the detection/rendering loop (Plan 02 wires the frontend side).
Output: Learning cards loaded at session start, injected into kontext string in analyse_loop, system prompt extended with lernkarte_match JSON field, cards cleared at reset.
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.11-coach-modul-inserted/04.11-CONTEXT.md
@.planning/phases/04.11-coach-modul-inserted/04.11-RESEARCH.md
@.planning/phases/04.11-coach-modul-inserted/04.11-01-SUMMARY.md

@services/live_session.py
@services/claude_service.py
@routes/app_routes.py

<interfaces>
<!-- From live_session.py — set_active_profile pattern (line 163) -->
def set_active_profile(name: str, daten: dict):
    global active_profile_data, active_profile_name
    with active_profile_lock:
        active_profile_name = name or ''
        active_profile_data = daten if isinstance(daten, dict) else {}

<!-- From live_session.py — reset_session clears state dict keys (line 264) -->
with state_lock:
    state['version'] = 0
    state['aktiv'] = False
    state['ergebnis'] = None
    state['ewb_clicks'] = []
    # ... many more resets

<!-- From claude_service.py — analyse_loop kontext assembly (line 692) -->
kontext = " ".join(ls.analysiert_bisher[-20:])
# Then: ergebnis = analysiere_mit_claude(neuer_text, kontext)

<!-- From claude_service.py — _build_system_prompt (line 252) -->
# Builds the Haiku system prompt with profile data
# Returns string used as system= parameter in messages.create()

<!-- From claude_service.py — SYSTEM_PROMPT_BASE JSON schema (line 46-53) -->
# Falls KEIN Einwand:
# {"einwand": false, "notiz": "..."}
# Falls Einwand erkannt:
# {"einwand": true, "typ": "...", ...plus Phase 04.8 boolean flags}

<!-- From routes/app_routes.py — set_active_profile call sites -->
# Line 114: ls.set_active_profile(active_profile.name if active_profile else '', ad)
#   — in the /live route, g.user.id is available
# Line 530: ls.set_active_profile(p.name, daten)
#   — in api_switch_profile, g.user.id is available

<!-- From Plan 01 — get_active_cards function -->
from services.coaching_service import get_active_cards
# Returns: [{'id', 'category', 'final_text', 'lernziel', 'applied_count', 'created_at'}]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load learning cards at session start, inject into live prompt, clear at reset</name>
  <files>services/live_session.py, routes/app_routes.py</files>
  <read_first>
    - services/live_session.py lines 95-115 (state dict definition — add active_learning_cards key)
    - services/live_session.py lines 157-173 (set_active_profile — where load_learning_cards is defined after)
    - services/live_session.py lines 264-360 (reset_session — where to clear cards)
    - routes/app_routes.py lines 100-120 (the /live route where set_active_profile is called at line 114)
    - routes/app_routes.py lines 520-540 (api_switch_profile where set_active_profile is called at line 530)
  </read_first>
  <action>
**1. services/live_session.py** — Add learning cards to session state.

Find the `state` dict initialization (around line 95, search for `state = {`). Add a new key:
```python
state['active_learning_cards'] = []
```

**2. services/live_session.py** — Add load_learning_cards function after `set_active_profile()`:

```python
def load_learning_cards(user_id):
    """D-09: Load active learning cards for the current user at session start."""
    try:
        from services.coaching_service import get_active_cards
        cards = get_active_cards(user_id)
        with state_lock:
            state['active_learning_cards'] = cards[:5]  # max 5 per D-07
        print(f"[Coach] {len(cards)} aktive Lernkarten geladen")
    except Exception as e:
        print(f"[Coach] Lernkarten laden fehlgeschlagen: {e}")
        with state_lock:
            state['active_learning_cards'] = []
```

**3. services/live_session.py** — Clear cards in `reset_session()`. Find the `state_lock` block in reset_session (around line 283) where state keys are reset. Add inside the FIRST `with state_lock:` block (alongside the other state resets):
```python
state['active_learning_cards'] = []
```

**4. routes/app_routes.py** — Wire load_learning_cards call after set_active_profile in the /live route.

At line 114, AFTER `ls.set_active_profile(active_profile.name if active_profile else '', ad)`, add:
```python
        ls.load_learning_cards(g.user.id)
```

At line 530 (inside api_switch_profile), AFTER `ls.set_active_profile(p.name, daten)`, add:
```python
        ls.load_learning_cards(g.user.id)
```

These are the only two call sites for set_active_profile in routes/app_routes.py. Both have g.user.id available via the @login_required decorator.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && grep -c "active_learning_cards" services/live_session.py && grep -c "load_learning_cards" services/live_session.py && grep -c "load_learning_cards" routes/app_routes.py</automated>
  </verify>
  <done>active_learning_cards key in state dict, load_learning_cards() function in live_session.py, cards cleared in reset_session(), load_learning_cards(g.user.id) called after both set_active_profile() call sites in routes/app_routes.py (line 114 and line 530).</done>
</task>

<task type="auto">
  <name>Task 2: Inject learning card context into analyse_loop + extend system prompt with lernkarte_match field</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py lines 9-63 (SYSTEM_PROMPT_BASE — full JSON response schema to extend)
    - services/claude_service.py lines 252-300 (_build_system_prompt — where to add learning card context)
    - services/claude_service.py lines 680-700 (analyse_loop — where kontext is assembled before analysiere_mit_claude call)
  </read_first>
  <action>
**1. services/claude_service.py** — Extend SYSTEM_PROMPT_BASE JSON schema with lernkarte_match fields.

Find the end of SYSTEM_PROMPT_BASE (line ~63, the closing `"""`). BEFORE the closing triple-quote, add after the existing Phase 04.8 boolean flags section (after the `"monosyllabisch"` flag description):

```
Zusaetzlich (Phase 04.11 — Lernkarten-Match): Falls aktive Lernkarten im Kontext vorhanden sind (markiert als [Aktive Lernkarten des Beraters]), pruefe ob die aktuelle Gespraechssituation zu einer Lernkarte passt. Falls ja, ergaenze diese optionalen Felder:
- "lernkarte_match": bool — true wenn die aktuelle Situation zu einer aktiven Lernkarte passt (z.B. Kunde nennt Einwand der zur Lernkarte passt)
- "lernkarte_match_text": string — der final_text der passenden Lernkarte (woertlich aus dem Kontext uebernehmen)
- "lernkarte_match_category": string — die Kategorie der passenden Lernkarte
Falls keine Lernkarten im Kontext oder keine Situation passt: diese Felder weglassen.
```

**2. services/claude_service.py** — Inject learning cards into kontext in analyse_loop.

Find the line (around 692):
```python
kontext = " ".join(ls.analysiert_bisher[-20:])
```

After this line, add:
```python
# D-09: Inject active learning cards into kontext
with ls.state_lock:
    _lk_cards = ls.state.get('active_learning_cards', [])
if _lk_cards:
    _lk_ctx = "\n\n[Aktive Lernkarten des Beraters - bei passender Situation mit lernkarte_match=true markieren]:\n"
    for _c in _lk_cards[:5]:
        _lk_ctx += f"- [{_c['category']}] {_c['final_text'][:80]}\n"
    kontext = kontext + _lk_ctx
```

This keeps the injection compact (category + first 80 chars of text, max 5 cards) per Pitfall 3 to avoid live prompt bloat (~200 tokens max). The system prompt schema tells Haiku to check these cards and return lernkarte_match/lernkarte_match_text/lernkarte_match_category fields when a match is detected. The frontend (Plan 02) reads these fields from data.ergebnis in pollErgebnis and calls renderLernkarteHint().
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && grep -c "lernkarte_match" services/claude_service.py && grep -c "active_learning_cards" services/claude_service.py && grep -c "Aktive Lernkarten" services/claude_service.py</automated>
  </verify>
  <done>SYSTEM_PROMPT_BASE extended with lernkarte_match/lernkarte_match_text/lernkarte_match_category optional JSON fields. Learning card context injected into kontext in analyse_loop (compact, max ~200 tokens). Full detection loop wired: cards in kontext -> Haiku returns lernkarte_match -> state['ergebnis'] includes it -> pollErgebnis (Plan 02) renders golden hint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live_session state -> Claude prompt | Learning card text injected into Haiku prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.11-08 | Info Disclosure | Learning card injection | accept | Cards are user's own data, injected only into their own session's prompt — no cross-user exposure |
| T-04.11-09 | Denial of Service | Prompt bloat | mitigate | Card text truncated to 80 chars each, max 5 cards, total ~200 tokens per Pitfall 3 |
</threat_model>

<verification>
1. grep confirms active_learning_cards in state dict, load function, reset, and analyse_loop injection
2. grep confirms load_learning_cards called in routes/app_routes.py (at least 2 call sites)
3. grep confirms lernkarte_match in SYSTEM_PROMPT_BASE (system prompt schema)
4. grep confirms lernkarte_match_text in SYSTEM_PROMPT_BASE
</verification>

<success_criteria>
- Learning cards loaded into live_session.state['active_learning_cards'] at session start via load_learning_cards(g.user.id)
- load_learning_cards called after set_active_profile at line 114 and line 530 in routes/app_routes.py
- Compact card context injected into kontext string before analysiere_mit_claude() call
- SYSTEM_PROMPT_BASE extended with lernkarte_match, lernkarte_match_text, lernkarte_match_category optional fields
- Cards cleared to [] in reset_session()
- Prompt injection capped at ~200 tokens (80 char/card, max 5 cards)
</success_criteria>

<output>
After completion, create `.planning/phases/04.11-coach-modul-inserted/04.11-03-SUMMARY.md`
</output>
