---
phase: 04.11-coach-modul-inserted
plan: 03
subsystem: api
tags: [claude, haiku, live-session, learning-cards, prompt-injection]

# Dependency graph
requires:
  - phase: 04.11-01
    provides: LearningCard model, coaching_service.get_active_cards(), DB schema
provides:
  - active_learning_cards loaded into live_session state at session start
  - Learning card context injected into Haiku live prompt via kontext parameter
  - SYSTEM_PROMPT_BASE extended with lernkarte_match JSON response fields
  - Cards cleared on session reset
affects: [04.11-02, 04.11-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [learning card injection into live prompt context, optional JSON fields in system prompt schema]

key-files:
  created: []
  modified:
    - services/live_session.py
    - services/claude_service.py
    - routes/app_routes.py

key-decisions:
  - "Learning card context capped at ~200 tokens (80 chars/card, max 5 cards) per Pitfall 3"
  - "Cards injected into kontext string (not system prompt) to keep system prompt stable"
  - "lernkarte_match fields are optional in JSON schema — Haiku only returns them when cards are in context and a match is found"

patterns-established:
  - "Pattern: Optional JSON fields in SYSTEM_PROMPT_BASE gated by context presence"
  - "Pattern: State dict key for cross-module data flow (learning cards loaded in routes, consumed in claude_service)"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-04-12
---

# Phase 04.11 Plan 03: Live Prompt Learning Card Injection Summary

**Active learning cards loaded at session start, injected into Haiku kontext (~200 tokens), system prompt extended with lernkarte_match/lernkarte_match_text/lernkarte_match_category optional JSON fields**

## Performance

- **Duration:** 2 min
- **Started:** 2026-04-12T11:13:36Z
- **Completed:** 2026-04-12T11:15:20Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Learning cards loaded into live_session.state['active_learning_cards'] at session start via load_learning_cards(user_id)
- Compact card context injected into kontext string before analysiere_mit_claude() call (max 5 cards, 80 chars each)
- SYSTEM_PROMPT_BASE extended with lernkarte_match, lernkarte_match_text, lernkarte_match_category optional fields
- Cards cleared to [] in reset_session()

## Task Commits

Each task was committed atomically:

1. **Task 1: Load learning cards at session start, inject into live prompt, clear at reset** - `cda637b` (feat)
2. **Task 2: Inject learning card context into analyse_loop + extend system prompt with lernkarte_match field** - `ddd251b` (feat)

## Files Created/Modified
- `services/live_session.py` - active_learning_cards in state dict, load_learning_cards() function, reset in reset_session()
- `services/claude_service.py` - SYSTEM_PROMPT_BASE lernkarte_match schema extension, kontext injection in analyse_loop
- `routes/app_routes.py` - load_learning_cards(g.user.id) wired after both set_active_profile() call sites

## Decisions Made
- Learning card context capped at ~200 tokens (80 chars/card, max 5 cards) per Pitfall 3 to avoid live prompt bloat
- Cards injected into kontext string (not system prompt body) to keep system prompt stable across sessions
- lernkarte_match fields are optional in JSON schema — Haiku only returns them when cards are in context and a match is found

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Full detection loop wired: cards in kontext -> Haiku returns lernkarte_match -> state['ergebnis'] includes it -> pollErgebnis (Plan 02) renders golden hint
- Plan 04 (weekly coach report + long-term charts) can proceed independently

---
*Phase: 04.11-coach-modul-inserted*
*Completed: 2026-04-12*
