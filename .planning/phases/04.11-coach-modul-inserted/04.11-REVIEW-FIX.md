---
phase: 04.11-coach-modul-inserted
fixed_at: 2026-04-12T15:00:00Z
review_path: .planning/phases/04.11-coach-modul-inserted/04.11-REVIEW.md
iteration: 1
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 04.11: Code Review Fix Report

**Fixed at:** 2026-04-12T15:00:00Z
**Source review:** .planning/phases/04.11-coach-modul-inserted/04.11-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5
- Fixed: 5
- Skipped: 0

## Fixed Issues

### CR-01: XSS via Learning Card Hint in Live View

**Files modified:** `static/app.js`
**Commit:** 46d65d0
**Applied fix:** Wrapped `text` parameter in existing `escHtml()` function inside `renderLernkarteHint()` innerHTML template literal to prevent injection of arbitrary HTML/script tags from user-controlled `final_text` field.

### WR-01: LearningCard Model Default Contradicts Actual Usage

**Files modified:** `database/models.py`
**Commit:** 18d9ea3
**Applied fix:** Changed `LearningCard.status` column default from `'aktiv'` to `'vorschlag'` and updated the status comment to list all four valid values: `'vorschlag' | 'aktiv' | 'gelernt' | 'archiviert'`. This aligns the default with actual card creation flow in coaching_service.py.

### WR-02: DB Commit Failure in coaching_service Silently Loses Persisted Cards

**Files modified:** `services/coaching_service.py`
**Commit:** d00e127
**Applied fix:** Added `db.rollback()` call and `return []` in the DB persist exception handler so the frontend receives an empty list (indicating failure) instead of unpersisted card suggestions that cannot be saved later.

### WR-03: Race Condition Between Duplicate Guard and Card Persistence

**Files modified:** `services/coaching_service.py`
**Commit:** a91017c
**Applied fix:** Added `import threading` and a module-level `_analysis_lock = threading.Lock()`. Wrapped the entire `generate_postcall_analysis` function body (duplicate guard check + Sonnet call + DB persist) in `with _analysis_lock:` to prevent concurrent requests for the same conv_id from bypassing the duplicate guard. Fixed: requires human verification (logic change).

### WR-04: validate_user_text Returns Permissive Default on Error

**Files modified:** `services/coaching_service.py`
**Commit:** bde307b
**Applied fix:** Changed the error fallback return from `{"covers_goal": True, "feedback": ""}` to `{"covers_goal": False, "feedback": "Validierung fehlgeschlagen, bitte erneut versuchen."}` so users are prompted to retry rather than silently accepting unvalidated text.

---

_Fixed: 2026-04-12T15:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
