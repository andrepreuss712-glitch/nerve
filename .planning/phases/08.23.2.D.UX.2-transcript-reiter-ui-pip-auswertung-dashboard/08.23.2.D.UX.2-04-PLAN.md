---
phase: 08.23.2.D.UX.2
plan: 04
type: execute
complexity: 🟡 mittel
wave: 2
depends_on: ["08.23.2.D.UX.2-01"]
files_modified:
  - static/pip-launcher.js
autonomous: true
requirements: [PT-01, PT-02, PT-03, DQ-01, DQ-03]
must_haves:
  truths:
    - "Im PiP waehrend des Calls gibt es einen 'Transkript ▶' Toggle-Knopf, der das PiP-Fenster verbreitert und ein Transkript-Panel side-by-side links neben dem Coaching einblendet"
    - "Das Live-Transkript-Panel rendert eingehende Segmente aus dem SocketIO 'transcript'-Event (roher RAM-Text, DQ-01) als kompakte Sprecher-Zeilen"
    - "Auto-Scroll zum neuesten Segment; sobald der User hochscrollt erscheint ein '↓ Neueste'-Knopf, der zurueckspringt"
    - "Bei gemessener Breite <900px wird statt side-by-side ein Overlay-Fallback gezeigt"
    - "In der PiP-Post-Call-Score-Ansicht gibt es einen 'Transkript ansehen' Knopf, der dasselbe RAM-Transkript inline ein-/ausklappt (DQ-03)"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "PiP Transkript-Toggle + resize + Live-Segment-Render + Auto-Scroll + Post-Call-Panel"
      contains: "resizeTo(960"
  key_links:
    - from: "transcript-Toggle-Klick"
      to: "state.pipWindow.resizeTo + measure-350ms + side-by-side|fallback"
      via: "Spike-Blueprint resizeTo(960,900) -> setTimeout 350 -> innerWidth-Branch"
      pattern: "resizeTo\\(960"
    - from: "SocketIO 'transcript'-Event"
      to: "Live-Segment-Liste (Neubau)"
      via: "state.socket.on('transcript') akkumuliert + rendert Segmente"
      pattern: "on\\(['\\\"]transcript['\\\"]"
    - from: "_revealScoreAndActions Post-Call-Screen"
      to: "Post-Call-Transkript-Panel (RAM, DQ-03)"
      via: "Transkript-ansehen-Knopf in .pip-postcall-actions"
      pattern: "Transkript ansehen"
---

<objective>
Baue die PiP-Transkript-Anzeige an zwei Kontexten: (1) Live waehrend des Calls als side-by-side
Panel im automatisch verbreiterten Document-PiP (PT-01/02/03, DQ-01), (2) Post-Call inline
ein-/ausklappbar in der Score-Ansicht (DQ-03). Beide nutzen den rohen RAM-Text (NICHT die DB) —
bewusst anders als das Dashboard.

Purpose: Tasks 1+2+5 der ROADMAP. Der Cold-Caller sieht das Transkript live ohne mitzuschreiben.
Output: `static/pip-launcher.js` erweitert.

⚠ Zwei verifizierte Landmines sind in die Tasks eingearbeitet:
- Der SocketIO `transcript`-Event traegt KEINE gerenderte Liste — die Live-Liste ist ein NEUBAU.
- Der Event-Payload ist `{type, text, speaker, line_id}` — **speaker ist ein INT (0=Berater, 1=Kunde,
  sonst/None=unbekannt), es gibt KEIN ts_ms.** (UI-SPEC behauptete faelschlich `{speaker,text,ts_ms}`.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-UI-SPEC.md
@.planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-RESEARCH.md
@.claude/skills/spike-findings-salesnerve/references/pip-window-resize.md
@CLAUDE.md

<interfaces>
<!-- VERIFIZIERTE Server-Emit-Shape (services/deepgram_service.py:86-88) — Landmine 2 / A2 aufgeloest: -->
sio.emit('transcript', {'type': 'final', 'text': text, 'speaker': emit_speaker, 'line_id': line_id}, room=sid)
<!--   type: 'final' | 'interim'   (NUR 'final' rendern — 'interim' ist Zwischenstand) -->
<!--   text: roher (NICHT anonymisierter) Transcript-Text -->
<!--   speaker: emit_speaker = INT 0 (Berater) | 1 (Kunde) | None/sonst (unbekannt -> 'system'-aequivalent) -->
<!--   KEIN ts_ms im Payload. -->

<!-- VERIFIZIERTER bestehender Handler (static/pip-launcher.js:2274-2278) — setzt NUR state.lastTranscript: -->
state.socket.on('transcript', function (d) {
  if (d && d.type === 'final' && d.text) { state.lastTranscript = d.text; }
});
<!--   -> KEINE gerenderte Segment-Liste. Live-Liste ist NEUBAU. Diesen Handler erweitern (Segmente akkumulieren). -->

<!-- VERIFIZIERTE PiP-Infra: -->
<!--   requestWindow({width:480, height:900}) bei pip-launcher.js:1540 (narrow oeffnen — NICHT aendern) -->
<!--   _setupPipWindow() :1697 ; nerve.css wird in den PiP injiziert :1700-1715 (Tokens aus Plan 01 dort verfuegbar) -->
<!--   pipEl(id) Helper = PiP-aware Lookup (state.pipWindow.document) ; state.pipWindow = Document-PiP window -->

<!-- VERIFIZIERTER Post-Call-Hook (D.UX.4 + MEETSTEP, Landmine 5 / A3 aufgeloest): -->
<!--   _revealScoreAndActions(json) (pip-launcher.js:3855) blendet Score + .pip-postcall-actions in -->
<!--   #nlp-section-postcall ein. Das ist der "Platz gelassen"-Hook -> Post-Call-Transkript-Knopf hier anhaengen. -->
<!--   .pip-postcall-actions ist die Aktions-Button-Reihe im Post-Call-Screen. -->

<!-- Spike-Blueprint (PT-GATE=JA, Chrome 148, 915px): resizeTo(960,900) -> setTimeout 350ms -> -->
<!--   pip.innerWidth >= 900 -> side-by-side, sonst Fallback-Overlay. Klick IST die transient activation. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-PiP Transkript-Toggle + resize + Side-by-Side/Fallback + Segment-Render + Auto-Scroll (PT-01/02/03, DQ-01)</name>
  <read_first>
    - static/pip-launcher.js :1533-1560 (Feature-Check + requestWindow narrow), :1697-1743 (_setupPipWindow + nerve.css-Injektion), :2268-2360 (Socket-Events, insb. transcript-Handler :2274)
    - .claude/skills/spike-findings-salesnerve/references/pip-window-resize.md GANZ (resize-Blueprint, harte Regeln, measure-and-fallback)
    - .planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-UI-SPEC.md §"Component Inventory 3" (Toggle, side-by-side/fallback Layout, compact speaker line, Auto-Scroll PT-02), §"Copywriting Contract" ("Transkript ▶"/"◀", "↓ Neueste", "Schließen ×", "Transkript wird aufgezeichnet…"), §"Interaction States > PiP transcript toggle / Auto-scroll"
    - .planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-RESEARCH.md §"Landmine 2" (Event-Payload-Shape, Neubau) + §"Architecture Patterns > PiP Resize-on-Toggle" + §"Anti-Patterns" (kein Timer-Resize ohne Klick, kein moveTo, requestWindow narrow lassen)
  </read_first>
  <action>
    Erweitere `static/pip-launcher.js`. Das PiP oeffnet weiterhin narrow (480px, :1540 NICHT aendern).

    1. **Toggle-Knopf:** In die bestehende PiP-Header-Controls-Reihe (Live-Ansicht) einen Knopf
       `Transkript ▶` (Lucide `data-lucide="file-text"`, `n-btn n-btn-ghost` small) einfuegen — PiP-aware via pipEl.
       Default-State: Transkript hidden. Aria-Label "Transkript einblenden".

    2. **Resize-on-Toggle (Spike-Blueprint, HART):** Klick-Handler:
       ```js
       var pip = state.pipWindow;
       pip.resizeTo(960, 900);              // Klick IST die transient activation — kein Timer davor
       setTimeout(function () {
         if (pip.innerWidth >= 900) renderSideBySide();   // Primaerpfad
         else                       renderFallbackOverlay();
         // log {requested:960, after:pip.innerWidth, layout}
       }, 350);
       ```
       KEIN Timer-Resize ohne Klick (Chrome blockiert). KEIN moveTo. Manuelles Ziehen ist kein Fallback.

    3. **Side-by-Side-Layout (≥900px):** Transkript-Panel links (~480px), bestehendes Coaching rechts (~480px).
       DOM/CSS in das PiP-`document` injizieren (pipEl/state.pipWindow.document). Klassen aus Plan 01 nerve.css
       (.transcript-segment-row etc.) sind im PiP verfuegbar (nerve.css wird injiziert, :1700). Toggle-Label → "Transkript ◀".

    4. **Fallback-Overlay (<900px):** absolut positioniertes Overlay ueber dem Coaching, volle PiP-Breite,
       "Schließen ×"-Knopf oben rechts zum Zuruecksetzen auf Coaching.

    5. **Live-Segment-Render (NEUBAU — Landmine 2):** Erweitere den bestehenden `state.socket.on('transcript', ...)`-
       Handler (:2274). Bisher setzt er nur `state.lastTranscript`. NEU zusaetzlich: bei `d.type==='final' && d.text`
       das Segment in einen Akkumulator `state.transcriptSegments` pushen `{speaker:d.speaker, text:d.text}` und —
       wenn das Live-Panel offen ist — eine `.transcript-segment-row` anhaengen:
       - **speaker-Mapping (verifizierte Shape, KEIN ts_ms):** `d.speaker===0` → label "BERATER" data-speaker="berater";
         `d.speaker===1` → "KUNDE" data-speaker="kunde"; sonst (None/undefined/andere) → "SYSTEM"
         data-speaker="system" (neutral-muted, Plan-01-FLAG). KEIN Timestamp rendern (Payload hat keinen).
       - Text via textContent (XSS). KEIN 'interim' rendern.
       - Empty-State solange keine Segmente: "Transkript wird aufgezeichnet…".

    6. **Auto-Scroll (PT-02):** Panel scrollt automatisch ans Ende wenn neue Segmente kommen — ABER nur wenn
       der User nicht hochgescrollt hat. Pause-Bedingung: `scrollTop + clientHeight < scrollHeight - 10`.
       Wenn pausiert: `↓ Neueste`-Knopf (fixed unten im Panel, `--btn-primary-*` Teal — PRIMARY-Accent fuer PiP)
       einblenden. Klick → ans Ende scrollen, Knopf verstecken, Auto-Scroll wieder aktiv.

    Harte Regeln: ASCII-Identifier (state.transcriptSegments, data-speaker="berater"); User-Strings mit echten
    Umlauten exakt aus UI-SPEC ("Transkript ▶"/"◀", "↓ Neueste", "Schließen ×", "Transkript wird aufgezeichnet…").
    Toggle-Button neutral-ghost (kein Teal-Glow). Kein Gelb. requestWindow bleibt narrow (480).
  </action>
  <verify>
    Production-only (CLAUDE.md HART):
    1. git commit + git push ; 2. `bash deploy.sh production` (static-asset landet trotz pre-existing test_ft_seed-Gate)
    3. Browser-Live-Call auf getnerve.app als Test-User (Chrome): PiP oeffnet narrow → "Transkript ▶" klicken →
       PiP verbreitert ~960px → Transkript-Panel links side-by-side, Coaching rechts. Live-Segmente erscheinen
       mit Sprecher-Farbe (Berater teal, Kunde violet), roher Text. Hochscrollen → "↓ Neueste" erscheint →
       Klick springt ans Ende. (Auf kleinem Screen <900px: Overlay-Fallback + "Schließen ×".)
    KEIN lokales pytest/Server, KEIN git pull.
  </verify>
  <acceptance_criteria>
    - static/pip-launcher.js contains 'resizeTo(960'
    - static/pip-launcher.js contains 'innerWidth' (measure-and-branch)
    - static/pip-launcher.js contains 'Transkript ▶' und 'Transkript ◀'
    - static/pip-launcher.js contains '↓ Neueste'
    - static/pip-launcher.js contains 'Transkript wird aufgezeichnet…'
    - static/pip-launcher.js contains 'transcriptSegments'
    - static/pip-launcher.js contains 'data-speaker' (Sprecher-Mapping)
    - static/pip-launcher.js enthaelt KEIN 'ts_ms' im neuen Live-PiP-Render-Pfad (Payload hat keinen)
    - Production-Live-Call: side-by-side ≥900px, Live-Segmente farbcodiert, Auto-Scroll + ↓ Neueste, Fallback-Overlay <900px
  </acceptance_criteria>
  <done>Toggle verbreitert PiP via Spike-Blueprint (measure+fallback), Live-Segmente (Neubau aus transcript-Event, speaker-INT-Mapping, kein ts_ms) farbcodiert, Auto-Scroll mit ↓ Neueste. Roher RAM-Text (DQ-01).</done>
</task>

<task type="auto">
  <name>Task 2: Post-Call PiP Transkript-Panel (inline collapsible, DQ-03)</name>
  <read_first>
    - static/pip-launcher.js :3848-3911 (_revealScoreAndActions — der D.UX.4/MEETSTEP "Platz gelassen"-Hook, Landmine 5 aufgeloest) + :2986-2992 (.pip-postcall-actions Referenz)
    - static/pip-launcher.js (Task-1-Stand) — state.transcriptSegments-Akkumulator + die Segment-Render-Hilfsfunktion aus Task 1 wiederverwenden
    - .planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-UI-SPEC.md §"Component Inventory 4" (Post-Call: collapsible, Auto-Scroll OFF, full-width, Toggle "Transkript ansehen"/"Transkript schließen"), §"Copywriting Contract"
    - .planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-RESEARCH.md §"Landmine 5" + §"Landmine 3" (PiP roh vs Dashboard anonymisiert — gewollt)
  </read_first>
  <action>
    Erweitere `_revealScoreAndActions(json)` (pip-launcher.js:3855), den verifizierten D.UX.4/MEETSTEP-Hook:

    1. **Knopf anhaengen:** Nach dem Einblenden von `.pip-postcall-actions` einen sekundaeren Ghost-Knopf
       `Transkript ansehen` (Lucide chevron-down) in die Actions-Reihe haengen (PiP-aware, pipEl). Idempotent:
       wenn schon vorhanden, nicht doppelt anhaengen (_revealScoreAndActions kann mehrfach laufen — Kommentar im Code sagt das).
    2. **Inline-Panel:** Klick toggelt ein collapsibles `.transcript-segments-container`-Panel inline unter dem
       Score (full-width 480px, KEIN side-by-side — Call ist vorbei). Default: collapsed. Label wechselt
       "Transkript ansehen" ↔ "Transkript schließen".
    3. **Datenquelle (DQ-03):** der **RAM-Akkumulator `state.transcriptSegments`** aus Task 1 (roh, sofort da —
       kein DB-Roundtrip, konsistent mit Live). Dieselbe Segment-Render-Hilfsfunktion wie Task 1 wiederverwenden
       (speaker-INT-Mapping, kein ts_ms). **Auto-Scroll OFF** (Call vorbei — User liest von oben, kein ↓-Neueste-Knopf).
       Empty-State falls leer: "Kein Transkript verfügbar für diesen Call."
    4. **Layer-Hinweis im Code-Kommentar:** "DQ-03: roher RAM-Text (state.transcriptSegments), bewusst anders
       als das anonymisierte Dashboard-Transkript (DQ-02). Kein Bug — RESEARCH Landmine 3."

    Harte Regeln: ASCII-Identifier; "Transkript ansehen"/"Transkript schließen" mit echten Umlauten exakt aus
    UI-SPEC. Ghost-Stil (kein Teal). textContent fuer Text. Idempotenter Knopf-Anhang.
  </action>
  <verify>
    Production-only:
    1. git commit + git push ; 2. `bash deploy.sh production`
    3. Browser-Live-Call auf getnerve.app als Test-User: Call auflegen → Outcome bestaetigen → Score-Screen erscheint
       → "Transkript ansehen"-Knopf sichtbar → Klick blendet das Transkript inline ein (roher RAM-Text, Sprecher-
       farbcodiert, kein Auto-Scroll) → erneuter Klick klappt zu (Label "Transkript schließen" ↔ "ansehen").
       Bei meeting_booked-Pfad: Knopf erscheint NACH Skip/Weiter (wenn _revealScoreAndActions laeuft).
    KEIN lokales pytest/Server, KEIN git pull.
  </verify>
  <acceptance_criteria>
    - static/pip-launcher.js contains 'Transkript ansehen'
    - static/pip-launcher.js contains 'Transkript schließen'
    - static/pip-launcher.js contains 'state.transcriptSegments' im Post-Call-Pfad (RAM-Quelle, DQ-03)
    - Der neue Knopf-Anhang sitzt in _revealScoreAndActions (Hook verifiziert)
    - Production-Live-Call: Post-Call "Transkript ansehen" klappt rohes RAM-Transkript ein/aus, kein Auto-Scroll
  </acceptance_criteria>
  <done>Post-Call-Score-Screen hat "Transkript ansehen"-Knopf (im verifizierten _revealScoreAndActions-Hook), klappt das RAM-Transkript (DQ-03, roh) inline ein/aus, Auto-Scroll OFF, idempotenter Knopf-Anhang.</done>
</task>

</tasks>

<verification>
- Live: Toggle verbreitert PiP (resizeTo(960,900)+measure+fallback), side-by-side ≥900px, Live-Segmente
  (Neubau, speaker-INT-Mapping, kein ts_ms), Auto-Scroll + ↓ Neueste.
- Post-Call: "Transkript ansehen" im _revealScoreAndActions-Hook klappt RAM-Transkript ein/aus.
- Beide Kontexte zeigen rohen RAM-Text (DQ-01/DQ-03) — bewusst anders als das anonymisierte Dashboard (RESEARCH Landmine 3, kein Bug).
- Alles Production-verifiziert mit Test-User (Chrome).
</verification>

<success_criteria>
- PT-01 (side-by-side via Spike-Blueprint + Fallback), PT-02 (Auto-Scroll + ↓ Neueste), PT-03 (kompakte
  Sprecher-Zeilen), DQ-01 (roher Live-RAM), DQ-03 (Post-Call RAM inline) erfuellt.
- Verifizierte Server-Emit-Shape eingehalten (speaker INT, kein ts_ms). Umlaut-Copy exakt, Identifier ASCII.
</success_criteria>

<output>
Erstelle .planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-04-SUMMARY.md
</output>
