---
phase: 08.23.2.D.UX.2
plan: 02
status: complete
completed: 2026-06-03
---

# Plan 02 SUMMARY — GET /api/transcript/<int:id> (DQ-02)

## Was gebaut wurde

`routes/learning.py`:
1. **Import-Fix (HART):** `abort` zur flask-Import-Zeile hinzugefuegt
   (`from flask import Blueprint, request, jsonify, g, abort`) — sonst NameError zur Laufzeit bei `abort(404)`.
2. **Neuer Endpoint** `GET /api/transcript/<int:conversation_log_id>` (`@login_required`) ans Dateiende:
   - Owner-Scope: `ConversationLog.id == id AND ConversationLog.user_id == g.user.id` → `abort(404)`
     bei fremdem/fehlendem Call (IDOR-Schutz, kein Org-Leak — RESEARCH §Security V4).
   - Liest `transcript_segments` (anonymisierte DB-Tabelle, NICHT der RAM-Puffer) mit dem bestehenden
     Stabil-Sort `order_by(ts_ms, id)` (1:1 vom learning.py:85-Pfad uebernommen).
   - JSON-Shape `{conversation_log_id, segments:[{ts_ms, speaker, text}], count}`.
   - Leerer Call → 200 mit `segments:[]`, `count:0`. try/finally + db.close().
   - `<int:...>`-Converter erzwingt Integer (conversation_logs.id ist INTEGER, nicht UUID — der
     G/MEET-Merge nach crm.*/UUID blieb DEFERRED; transcript_segments lebt in public.*).

## Layer-Distinktion (Punkt 21, bewusst, kein Bug)
Dieser Endpoint = **DB-Pfad** (anonymisierter, persistierter Text, Dashboard/Auswertung).
PiP (Plan 04) = **RAM-Pfad** (roher Live-/Post-Call-Text). DQ-01/02/03 — bewusst getrennt.
`segment.text` ist eine echte DB-Spalte; der rohe PiP-Text ist KEINE DB-Spalte sondern RAM.

## Verifikation
- Lokal (grep, Code-Lesen): Import enthaelt `abort`; Route-Decorator, `user_id == g.user.id`,
  `abort(404)`, `order_by(TranscriptSegment.ts_ms, TranscriptSegment.id)` alle present;
  10× `@login_required` (inkl. neuer Route).
- **Production-Verify offen (André-Deploy):** `bash deploy.sh production` →
  `inspect.sh routes | grep transcript` muss `/api/transcript/<int:conversation_log_id>` zeigen;
  Browser als Test-User: eigener Call → 200 + segments/count; fremder Call → **404** (Beweis dass
  abort importiert ist — NICHT 500/NameError). KEIN lokales pytest (CLAUDE.md HART).

## Forward-Notiz (NICHT diese Phase, aus REVIEWS.md)
`/api/transcript` ist user_id-scoped → bei TEAM/COACH (Org-Ownership) spaeter mit Org+Rollen-Check
aufweiten.

## Key-Files
- modified: `routes/learning.py`
