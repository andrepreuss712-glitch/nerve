---
phase: 08.23.2.D.UX.2
plan: 04
status: complete
completed: 2026-06-03
---

# Plan 04 SUMMARY — PiP Live-Transkript (side-by-side) + Post-Call inline (DQ-01/03)

## Was gebaut wurde

`static/pip-launcher.js` (nur diese Datei; alles JS-injiziert, da base.html nicht im Scope):

**Task 1 — Live-PiP Transkript (PT-01/02/03, DQ-01):**
- **Segment-Akku (DUX2-01):** Der bestehende `state.socket.on('transcript')`-Handler (:2274) erweitert:
  `if (!state.transcriptSegments) state.transcriptSegments = []` VOR dem ersten push (kein TypeError),
  dann `{speaker:d.speaker, text:d.text}` pushen + `_appendLiveSegment`. Neubau — der Event traegt KEINE
  gerenderte Liste. **speaker = INT** 0→BERATER/berater, 1→KUNDE/kunde, None/sonst→SYSTEM/system.
  **KEIN ts_ms** (Payload hat keinen — nur als Negativ-Kommentar dokumentiert, nirgendwo gerendert).
- **Toggle-Knopf:** `_wireTranscriptToggle` injiziert "Transkript ▶" (`n-btn n-btn-ghost`, lucide file-text)
  in `.pip-header-right` (PiP-aware, idempotent), wird in `_setupPipWindow` nach `_initPipLive()` gerufen.
- **Resize-on-Toggle (DUX2-02):** `_openTranscriptPanel`: `pip.resizeTo(960,900)` (Klick = transient
  activation) + **committed-Guard** + **(A) ResizeObserver** auf `pip.document.body` (zuverlaessiger Flip,
  auch wenn Animation >350ms) + **(B) 350ms-Notnagel-Timer** (>=900→commit, sonst Fallback-Overlay).
  `commitSideBySide` idempotent, disconnected Observer nach Commit (kein Doppel-Render/Leak).
- **Side-by-Side (>=900):** Transkript-Panel links (480px) + `#pip-live-window` rechts (flex:1), body→row.
- **Fallback-Overlay (<900):** position:fixed full-width Overlay mit "Schließen ×"; vom Observer durch
  side-by-side ersetzt falls das Fenster doch auf 960 aufgeht.
- **Render:** `.transcript-segment-row` (textContent — XSS), farbcodiert (berater teal/kunde violet/
  system neutral-muted). Empty: "Transkript wird aufgezeichnet…".
- **Auto-Scroll (PT-02):** ans Ende bei neuen Segmenten, ABER pausiert wenn `scrollTop+clientHeight <
  scrollHeight-10`; dann "↓ Neueste"-Knopf (PRIMARY-Teal) → Klick springt ans Ende.

**Task 2 — Post-Call inline (DQ-03):**
- `_appendPostcallTranscriptButton()` am Ende von `_revealScoreAndActions` (verifizierter Hook):
  haengt idempotent "Transkript ansehen" (Ghost) an `.pip-postcall-actions`, toggelt ein inline
  collapsibles Panel (full-width, **Auto-Scroll OFF**) aus **`state.transcriptSegments`** (roher RAM,
  DQ-03 — bewusst anders als anonymisiertes Dashboard, RESEARCH Landmine 3). Label "Transkript ansehen"
  ↔ "Transkript schließen". Empty: "Kein Transkript verfügbar für diesen Call."

## Verifikation
- Lokal: alle Acceptance-Criteria gegrept (resizeTo(960, innerWidth, ResizeObserver, Akku-Init-Guard,
  Transkript ▶/◀, ↓ Neueste, "Transkript wird aufgezeichnet…", data-speaker, Transkript ansehen/schließen).
  `ts_ms` nur in 2 Negativ-Kommentaren (nirgendwo gerendert). **`node --check` (reine Syntax-Parse, kein
  App-Start) auf n-tabs.js + pip-launcher.js: OK.**
- **Production-Verify offen (André-Live-Call, CLAUDE.md HART):** `bash deploy.sh production` → Chrome
  Live-Call: "Transkript ▶" → PiP ~960 → side-by-side, Live-Segmente farbcodiert, erstes Segment kein
  TypeError, Hochscrollen→"↓ Neueste". Langsamer Rechner: ResizeObserver-Flip holt side-by-side nach.
  <900px: Overlay-Fallback. Post-Call: "Transkript ansehen" klappt RAM-Transkript ein/aus.

## ⚠ FLAG für André (UAT — NICHT blockierend)
Bis Sprecher-Rollen serverseitig bestätigt sind (`roles_confirmed`, deepgram_service.py:78-87), emittiert
der Server `speaker:None` AUCH für echte Äußerungen → diese **frühen Segmente erscheinen als SYSTEM**
(neutral-muted, Plan-01-FLAG). Korrekt gemappt, aber UX-Surprise. Bei UAT bestätigen lassen.

## Key-Files
- modified: `static/pip-launcher.js`
