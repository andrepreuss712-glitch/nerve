---
phase: 08.23.2.D.UX.2
plan: 02
type: execute
complexity: 🟡 mittel
wave: 1
depends_on: []
files_modified:
  - routes/learning.py
autonomous: true
requirements: [DQ-02]
must_haves:
  truths:
    - "GET /api/transcript/<int:conversation_log_id> liefert die sortierten, anonymisierten Segmente eines eigenen Calls als JSON"
    - "Ein Call, der nicht dem eingeloggten User gehoert, liefert 404 (kein Org-Leak, kein IDOR)"
    - "Ein eigener Call ohne Segmente liefert 200 mit segments:[] und count:0"
  artifacts:
    - path: "routes/learning.py"
      provides: "Read-Endpoint /api/transcript/<int:id> in learning_bp"
      contains: "/api/transcript/<int:conversation_log_id>"
  key_links:
    - from: "/api/transcript/<int:id>"
      to: "conversation_logs (owner-scope)"
      via: "filter CL.id==id AND CL.user_id==g.user.id -> sonst abort(404)"
      pattern: "user_id == g.user.id"
    - from: "/api/transcript/<int:id>"
      to: "transcript_segments (read)"
      via: "TranscriptSegment.conversation_log_id == conv.id order_by(ts_ms, id)"
      pattern: "order_by\\(TranscriptSegment.ts_ms"
---

<objective>
Baue den neuen Read-Endpoint `GET /api/transcript/<int:conversation_log_id>` in `learning_bp`,
der die anonymisierten, sortierten Transcript-Segmente eines Calls liefert — owner-scoped
(IDOR-Schutz). Plan 03 (session_detail Transkript-Reiter) fetcht hieraus.

Purpose: DQ-02. Die grosse Auswertung liest den persistierten, anonymisierten Text aus der DB
(bewusst anders als PiP, das den rohen RAM-Text zeigt — DQ-01/DQ-03, kein Bug).
Output: ein neuer Endpoint in `routes/learning.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-RESEARCH.md
@CLAUDE.md
@routes/CLAUDE.md

<interfaces>
<!-- Bereits in routes/learning.py importiert (Z.2-7): -->
<!--   from routes.auth import login_required        (Decorator: @login_required, NICHT @learning_bp.*) -->
<!--   from database.db import get_session -->
<!--   from database.models import ConversationLog, Call, TranscriptSegment -->
<!--   from flask import Blueprint, request, jsonify, g   (Z.2 — VERIFIZIERT: KEIN abort dabei!) -->
<!--   learning_bp = Blueprint('learning', __name__)   (Blueprint-Name 'learning') -->

<!-- Bestehende Read-Query (learning.py:85-92) — 1:1 wiederverwenden inkl. (ts_ms, id)-Stabil-Sort: -->
segments = (db.query(TranscriptSegment)
            .filter(TranscriptSegment.conversation_log_id == conv.id)
            .order_by(TranscriptSegment.ts_ms, TranscriptSegment.id)
            .all())
# -> [{'ts_ms': s.ts_ms, 'speaker': s.speaker, 'text': s.text} for s in segments]

<!-- Scoping-Vorlage (dashboard.py:729-734, session_detail) — spiegeln: -->
conv = db.query(ConversationLog).filter(
    ConversationLog.id == conversation_log_id,
    ConversationLog.user_id == g.user.id,
).first()
if not conv:
    abort(404)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint GET /api/transcript/<int:conversation_log_id> in learning_bp</name>
  <read_first>
    - routes/learning.py Z.1-99 (Imports Z.2 + bestehender TranscriptSegment-Read-Pfad Z.85-92 + conv-Scope Z.66-69 — Query + Stabil-Sort 1:1 uebernehmen)
    - routes/dashboard.py Z.720-734 (session_detail Owner-Scope-Pattern: filter id+user_id, abort(404) bei Fehlen)
    - routes/CLAUDE.md (Blueprint-Name 'learning' verifiziert; url_for-Regeln falls noetig)
    - .planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-RESEARCH.md §"Endpoint-Empfehlung (DQ-02)" + §"Persistenz-Schicht-Verifikation" + §"Security Domain"
    - Produktions-Schema bei Bedarf re-verifizieren:
      ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
      ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema conversation_logs'
  </read_first>
  <action>
    Ergaenze in `routes/learning.py` einen neuen Endpoint (ans Ende der Datei oder bei den anderen
    api-Routen). `login_required` ist bereits importiert.

    **Import-Fix (HART — NICHT optional):** Die bestehende Import-Zeile (routes/learning.py:2) ist
    `from flask import Blueprint, request, jsonify, g` — `abort` fehlt dort. `abort` zu eben dieser Zeile
    hinzufuegen, sodass sie lautet:
    `from flask import Blueprint, request, jsonify, g, abort`
    Ohne diesen Import wirft `abort(404)` zur Laufzeit einen NameError. Das ist Pflicht, keine Pruefung "falls
    nicht vorhanden".

    ```python
    @learning_bp.route('/api/transcript/<int:conversation_log_id>', methods=['GET'])
    @login_required
    def api_transcript(conversation_log_id):
        """DQ-02: anonymisierte, sortierte Transcript-Segmente eines eigenen Calls.
        Owner-scoped (user_id == g.user.id) -> IDOR-Schutz; abort(404) bei fremdem/fehlendem Call.
        Quelle: DB-Tabelle transcript_segments (anonymisiert) — NICHT der RAM-Puffer (das ist der PiP-Pfad)."""
        db = get_session()
        try:
            conv = db.query(ConversationLog).filter(
                ConversationLog.id == conversation_log_id,
                ConversationLog.user_id == g.user.id,
            ).first()
            if not conv:
                abort(404)
            segments = (db.query(TranscriptSegment)
                        .filter(TranscriptSegment.conversation_log_id == conv.id)
                        .order_by(TranscriptSegment.ts_ms, TranscriptSegment.id)
                        .all())
            return jsonify({
                'conversation_log_id': conv.id,
                'segments': [
                    {'ts_ms': s.ts_ms, 'speaker': s.speaker, 'text': s.text}
                    for s in segments
                ],
                'count': len(segments),
            })
        finally:
            db.close()
    ```

    Harte Regeln:
    - Scope MUSS `user_id == g.user.id` sein (NICHT org_id — sonst Org-Leak, RESEARCH §Security V4).
    - `<int:...>`-Converter erzwingt Integer (conversation_logs.id ist INTEGER, kein UUID — Verbindungs-Karte Punkt 22).
    - KEINE Pagination (76 Rows total in Prod, pro Call ein Bruchteil).
    - try/finally + db.close() (Projekt-Pattern).
    - Identifier ASCII; KEINE User-facing-Strings in dieser Route (reines JSON).
  </action>
  <verify>
    Production-only (CLAUDE.md HART):
    1. git commit + git push
    2. `bash deploy.sh production` (eingebauter Pytest-Gate laeuft server-side vor Restart; bei Test-Fail kein Restart)
    3. Route registriert? `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh routes' | grep transcript`
       → muss `/api/transcript/<int:conversation_log_id>` zeigen.
    4. Browser-Fetch als Test-User auf getnerve.app: eigener Call-id → 200 + {segments:[...], count:N};
       fremde/nicht-existierende id → 404 (NICHT 500/NameError → Beweis dass abort importiert ist).
    KEIN lokales pytest, KEIN lokaler Server, KEIN git pull im Verify.
  </verify>
  <acceptance_criteria>
    - routes/learning.py line 'from flask import' contains 'abort' (Import-Fix — sonst NameError zur Laufzeit)
    - routes/learning.py contains "@learning_bp.route('/api/transcript/<int:conversation_log_id>'"
    - routes/learning.py contains '@login_required' in der Naehe der neuen Route
    - routes/learning.py contains 'user_id == g.user.id' (im neuen Endpoint, Owner-Scope)
    - routes/learning.py contains 'abort(404)'
    - routes/learning.py contains 'order_by(TranscriptSegment.ts_ms, TranscriptSegment.id)'
    - Production: `inspect.sh routes` listet /api/transcript/<int:conversation_log_id>
    - Production-Browser: eigener Call → 200 + JSON segments/count; fremder Call → 404 (kein 500/NameError)
  </acceptance_criteria>
  <done>abort ist zur flask-Import-Zeile hinzugefuegt; Endpoint registriert auf Production; eigener Call liefert sortierte anonymisierte Segmente, fremder Call 404, leerer Call 200/count:0.</done>
</task>

</tasks>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21)

### Angefasste Tabellen
- `transcript_segments` (**lesen**) — der neue Endpoint + bestehender learning.py-Read-Pfad (Z.85).
- `conversation_logs` (**lesen**, nur Scoping) — `user_id`-Filter, kein Schreiben.
- **Kein Schreiben, kein Schema-Change in dieser Phase.**

### inspect.sh-Beleg (Production, read-only, Stand 2026-06-03 — bei Bedarf re-run)
```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
id bigint PK; conversation_log_id integer NOT NULL FK->conversation_logs(id) ON DELETE CASCADE;
ts_ms integer NOT NULL; speaker text NOT NULL CHECK IN ('berater','kunde','system');
text text NOT NULL; created_at timestamptz default now();
Index idx_transcript_segments_conv_ts (conversation_log_id, ts_ms). count=76.

$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema conversation_logs'
id INTEGER PK; user_id INTEGER NOT NULL; org_id INTEGER NOT NULL; ...
```
**ORM ⇄ Production: DECKUNGSGLEICH** (database/models.py:718 TranscriptSegment, :268 ConversationLog).
`transcript_segments` lebt in `public.*` mit INTEGER `conversation_log_id` — NICHT durch G/MEET nach
`crm.*`/UUID verschoben (der Merge blieb DEFERRED). Verbindungs-Karte Punkt 22: siehe RESEARCH.md §Verbindungs-Karte.

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `TranscriptSegment.ts_ms / speaker / text` | gelesen im neuen Endpoint | DB-Spalten `transcript_segments.*` (anonymisiert) | ✓ inspect.sh + models.py:718 |
| `conv.id` (= conversation_log_id) | URL-Param `<int:id>` | DB-Spalte `conversation_logs.id` (INTEGER) | ✓ models.py:268 |
| `g.user.id` (Owner-Scope) | Endpoint-Filter | Flask-`g` (Request-Local aus Session) | ✓ dashboard.py:731-Pattern |
| **WICHTIG — Layer-Distinktion:** PiP-Live/Post-Call-Text | NICHT in diesem Endpoint | **RAM-Puffer / SocketIO**, nicht DB | ✓ — PiP liest RAM (Plan 04), Dashboard liest DB (dieser Plan). Bewusst getrennt (DQ-01/02/03). |

**Bei Diskrepanz: STOP + Replan.** Keine Diskrepanz (ORM ⇄ Production deckungsgleich). Die Layer-Distinktion
DB (dieser Endpoint, anonymisiert) vs. RAM (PiP, roh) ist genau der Punkt-21-Variablen-Namens-Fallen-Schutz:
`segment.text` aus der DB ist eine echte DB-Spalte; der rohe PiP-Text ist KEINE DB-Spalte sondern RAM.

<verification>
- Route auf Production registriert (`inspect.sh routes`).
- Owner-Scope greift: eigener Call 200, fremder Call 404 (Browser-Test mit Test-User).
- JSON-Shape {conversation_log_id, segments:[{ts_ms,speaker,text}], count}.
</verification>

<success_criteria>
- Endpoint live auf Production, owner-scoped, kein IDOR.
- Liefert sortierte anonymisierte Segmente; leerer Call 200/count:0; fremder Call 404.
- Persistenz-Schicht-Verifikation vollstaendig (Punkt 21 erfuellt).
</success_criteria>

<output>
Erstelle .planning/phases/08.23.2.D.UX.2-transcript-reiter-ui-pip-auswertung-dashboard/08.23.2.D.UX.2-02-SUMMARY.md
</output>
</output>
