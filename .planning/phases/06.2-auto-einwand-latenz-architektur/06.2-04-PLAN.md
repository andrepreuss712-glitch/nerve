---
phase: 06.2-auto-einwand-latenz-architektur
plan: 04
type: execute
wave: 4
depends_on: [06.2-03]
files_modified:
  - services/claude_service.py
autonomous: true
requirements: [BUG-10-LAT]
requirements_addressed: [D-08]

must_haves:
  truths:
    - "analyse_loop spawnt KEINE streame_auto_variante mehr fuer Slot 1 wenn bereits ein KeywordMatch dafuer einen Stream aktiv gemacht hat (Doppel-Variante vermeiden)"
    - "busy_until-Guard in analyse_loop pruefft jetzt BEIDE Quellen (Keyword + analyse_loop) — gemeinsamer Lock"
    - "Keyword-Pipeline ist die Primary — analyse_loop's Variant-Spawn wird zum Fallback falls Keyword nicht matcht"
    - "Backend-Log zeigt klar welcher Pfad den Stream spawnt: [PiP-AutoVar] ENTRY trigger=keyword vs trigger=analyse_loop"
  artifacts:
    - path: "services/claude_service.py"
      provides: "analyse_loop mit gemeinsamem busy_until + Trigger-Source-Log"
      contains: "trigger="

tasks:
  - id: task-1
    title: "Gemeinsamer busy_until-Lock in ls.state"
    description: >
      busy_until wandert in services/live_session.py:

        state.setdefault('slot1_variant_busy_until', 0.0)

      In analyse_loop und in deepgram_service.py Keyword-Pipe:

        with ls.state_lock:
            _busy = ls.state.get('slot1_variant_busy_until', 0)
        if time.monotonic() >= _busy:
            with ls.state_lock:
                ls.state['slot1_variant_busy_until'] = time.monotonic() + 6
            sio.start_background_task(streame_auto_variante, ...)

      Beide Call-Sites nutzen denselben Lock -> kein Doppel-Spawn mehr moeglich
      egal ob Keyword oder analyse_loop zuerst feuert.
    autonomous: true

  - id: task-2
    title: "streame_auto_variante mit trigger-Parameter"
    description: >
      Optional-Arg trigger: str hinzufuegen, default "analyse_loop". Log-Line
      am Anfang: print(f"[PiP-AutoVar] ENTRY trigger={trigger} sid={sid} ...").

      Keyword-Pipe uebergibt trigger="keyword", analyse_loop uebergibt
      trigger="analyse_loop". Reine Observability, keine Verhaltensaenderung.
    autonomous: true

  - id: task-3
    title: "Regression-Check Doku"
    description: >
      In 06.2-VERIFICATION.md (wird spaeter vom verifier erstellt) soll dokumentiert
      werden:
      - KeywordMatch + analyse_loop feuern NIE beide fuer denselben Satz
      - Wenn KeywordMatch zuerst: analyse_loop spawnt Slot-1-Variant NICHT (busy_until hit)
      - Wenn KeywordMatch nicht matcht (Satz ohne Keyword): analyse_loop-Fallback aktiv
      - Button-Klick bleibt unabhaengig — streame_manual_ewb_variante nutzt DIESEN Lock NICHT

      Dieser Task ist Dokumentation zur Klarstellung, keine Code-Aenderung ausser
      ggf. Kommentaren.
    autonomous: true

verification:
  - "Satz mit Keyword ('keine zeit') -> nur 1 streame_auto_variante-Aufruf mit trigger=keyword"
  - "Satz ohne Keyword aber mit Einwand-Gehalt -> nur 1 streame_auto_variante-Aufruf mit trigger=analyse_loop"
  - "Zwei schnell aufeinander folgende Einwaende (je <6s) -> nur 1 Variant-Stream (busy_until blockt zweiten)"
  - "streame_manual_ewb_variante (Button) bleibt unabhaengig, kein shared busy_until, kein Regression"
  - "Log zeigt trigger=keyword|analyse_loop|manual fuer jeden Variant-Aufruf -> Auswertbarkeit im Postcall"
