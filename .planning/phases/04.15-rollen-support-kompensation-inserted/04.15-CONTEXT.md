# Phase 04.15: Rollen, Support & Kompensation - Context

**Gathered:** 2026-04-13
**Status:** Deferred — not needed for EA Launch

<domain>
## Phase Boundary

Granulares Plattform-Rollen-System (superadmin/support/billing/analyst), DSGVO-konformer Support-Zugriff auf Kundenprofile mit Audit-Trail, Kompensation (Gratis-Tage bei Ausfällen), Mitarbeiter-Verwaltung.

</domain>

<decisions>
## Implementation Decisions

### Scope & Timing
- **D-01:** Phase wird komplett deferred. Kein Code wird geschrieben.
- **D-02:** Begründung: André ist Solo-Founder mit 50 EA-Usern. Es gibt kein Support-Team, keine Mitarbeiter, keine Kompensations-Workflows. Das bestehende System (`is_superadmin` + `users.rolle` owner/admin/member) reicht für den EA-Launch.
- **D-03:** Trigger für Reaktivierung: Wenn der erste Mitarbeiter kommt (~50-150 Kunden). Dann separate `admin_users` Tabelle, Support-Zugriff, Kompensation bauen.
- **D-04:** Kein YAGNI — nichts auf Vorrat bauen was noch nicht gebraucht wird.

### Was bereits existiert (kein Handlungsbedarf)
- `users.rolle` = owner/admin/member (Kunden-Rollen innerhalb Organisationen)
- `users.is_superadmin` Flag für André (ENV-Seed via SUPERADMIN_EMAIL)
- `@login_required` Decorator setzt `g.user` + `g.org`
- `before_request` Hook populates `g.user` für Flask-Admin Routes (Phase 04.14 Fix)
- Audit-Log mit `log_action()` (Phase 4.7)
- CRM mit Kunden-Übersicht + Notizen (Phase 4.14)

</decisions>

<specifics>
## Specific Ideas

Detailliertes Briefing für die spätere Umsetzung existiert:
- Berechtigungs-Matrix (superadmin/support/billing/analyst) ist vordefiniert
- `admin_users` Tabelle, `customer_compensation` Tabelle, Audit-Log Upgrade spezifiziert
- Support-Zugriff UX (Banner, Vorher/Nachher-Diff) dokumentiert
- Kompensations-Email Template vorhanden

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these when this phase is eventually activated.**

### Feature-Briefing
- `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\02 Projekte\NERVE Rollen Support & Kompensation.md` — Vollständiges Briefing: Rollen-Definition, Berechtigungs-Matrix, DB-Schema, Support-UX, Kompensations-Workflow, DSGVO-Anforderungen

### Bestehende Infrastruktur
- `database/models.py` — User.rolle, User.is_superadmin (bestehende Rollen)
- `services/audit.py` — log_action() (Audit-Trail Basis)
- `routes/admin_views.py` — _is_superadmin(), SecureModelView (bestehende Admin-Auth)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `_is_superadmin()` in admin_views.py — Basis für spätere `role_required()` Decorator
- `log_action()` in services/audit.py — Audit-Trail Infrastruktur vorhanden
- CrmView in admin_views.py — Pattern für weitere Admin-Views

### Established Patterns
- Flask-Admin BaseView + is_accessible() für Admin-Auth
- `g.user` via `before_request` Hook (seit Phase 04.14)
- SQLAlchemy models mit `utcnow`, ForeignKey Pattern

### Integration Points
- `admin_users` Tabelle würde neben `users` existieren (zwei getrennte Auth-Systeme)
- `/admin/login` separater Login für Mitarbeiter
- Audit-Log bekommt `admin_id` Column zusätzlich zu `user_id`

</code_context>

<deferred>
## Deferred Ideas

None — Phase selbst ist deferred. Alle Features bleiben im Briefing-Dokument bis Reaktivierung.

</deferred>

---

*Phase: 04.15-rollen-support-kompensation-inserted*
*Context gathered: 2026-04-13*
