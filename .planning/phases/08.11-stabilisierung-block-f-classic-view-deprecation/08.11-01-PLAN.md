---
phase: 08.11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - salesnerve/routes/app_routes.py
  - salesnerve/app.py
autonomous: true
requirements: [BLOCK-F-01]

must_haves:
  truths:
    - "Alle 11 Classic-only-Routen in app_routes.py sind entfernt"
    - "/live Route gibt redirect(url_for('dashboard_bp.dashboard')) zurueck statt app.html zu rendern"
    - "app.py importiert API_FRAGE_PROMPT_BASE nicht mehr"
    - "_seed_prompt_versions() seeded nur noch 4 Module (api_frage entfernt)"
    - "_SuppressPolling-Klasse und ihr addFilter-Call sind aus app.py entfernt"
    - "pytest laeuft durch: 266 passing (test_ft_seed.py schlaegt fehl bis Plan 03)"
  artifacts:
    - path: salesnerve/routes/app_routes.py
      provides: "Bereinigtes Route-File ohne Classic-only Handler"
    - path: salesnerve/app.py
      provides: "Bereinigtes app.py ohne API_FRAGE_PROMPT_BASE-Import und _SuppressPolling"
  key_links:
    - from: salesnerve/routes/app_routes.py
      to: /dashboard
      via: "redirect(url_for('dashboard_bp.dashboard')) in live()-Funktion"
      pattern: "redirect.*dashboard"
---

<objective>
Backend-Cleanup Wave 1: Alle Classic-only HTTP-Routen aus app_routes.py loeschen, /live-Route durch Redirect zu /dashboard ersetzen, app.py von toten Importen und _SuppressPolling befreien.

Purpose: Das Backend hat nach app.html-Deletion 0 Caller fuer 11 Routen. Diese werden jetzt entfernt, bevor die Frontend-Files in Wave 2 geloescht werden.
Output: app_routes.py ohne ~400 Zeilen Classic-only-Code, /live leitet zu /dashboard um, app.py sauber.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@/c/Users/andre/dev/salesnerve/.planning/STATE.md
@/c/Users/andre/dev/salesnerve/.planning/phases/08.11-stabilisierung-block-f-classic-view-deprecation/08.11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classic-Routen aus app_routes.py loeschen + /live durch Redirect ersetzen</name>
  <files>salesnerve/routes/app_routes.py</files>

  <read_first>
    salesnerve/routes/app_routes.py — komplett lesen (1312 Zeilen). Alle unten genannten Loeschbereiche verifizieren bevor etwas geaendert wird.
  </read_first>

  <action>
**Schritt 1 — Modul-globale Konstanten loeschen (app_routes.py):**

Zeilen 13-14: Loeschen:
```python
_letzte_gemeldete_version: dict[int, int] = {}
_MAX_VERSION_CACHE = 200
```
(nur von api_ergebnis() benutzt — wird mit geloescht)

Zeilen 16-23: Loeschen:
```python
API_FRAGE_PROMPT_BASE = """..."""
```
(gesamter Multiline-String, genau die Zeilen 16-23 aus dem Research)

Zeilen 25-35: BEHALTEN — `OBJECTION_TRIGGER_PROMPT_BASE` bleibt stehen (wird von app.py fuer den 'objection_trigger' Seed-Eintrag importiert, per User Decision D-05).

**Schritt 2 — /live Route durch Redirect ersetzen (Zeilen 38-131):**

Den gesamten Body der `live()`-Funktion (Zeilen 38-131) loeschen und ersetzen durch:
```python
@app_routes_bp.route('/live')
@login_required
def live():
    # Classic-View entfernt (Block F). Redirect auf Dashboard.
    return redirect(url_for('dashboard_bp.dashboard'))
```

Das sind ~93 Zeilen die raus kommen, ersetzt durch 4 Zeilen.

**Schritt 3 — Folgende Route-Funktionen komplett loeschen:**

| Funktion | Zeilen (aus RESEARCH.md) | Route |
|----------|--------------------------|-------|
| `api_ergebnis()` | 134-162 | /api/ergebnis |
| `api_analyse_line()` | 165-187 | /api/analyse_line |
| `api_log_correction()` | 190-205 | /api/log_correction |
| `api_pause()` | 208-215 | /api/pause |
| `api_log()` | 218-238 | /api/log |
| `api_set_phase()` | 1063-1085 | /api/set_phase |
| `log_gegenargument_wahl()` | 1087-1098 | /api/log_gegenargument_wahl |
| `api_frage()` | 1100-1142 | /api/frage |
| `api_ewb_trigger()` | 1145-1254 | /api/ewb_trigger |
| `api_postcall_insights()` | 1304-1338 | /api/postcall_insights |

Jede Funktion inkl. ihres `@app_routes_bp.route(...)` und `@login_required` Dekorators vollstaendig entfernen.

**NICHT loeschen (PiP-Used Routes — aus RESEARCH.md Section 2):**
- `api_keepalive()` (Zeile ~666) — aufgerufen von base.html:565
- `api_beenden()` (Zeile ~240) — aufgerufen von pip-launcher.js:1885
- `api_set_profile()` (Zeile ~961) — aufgerufen von pip-launcher.js:869
- `api_launcher_init()` (Zeile ~988) — aufgerufen von pip-launcher.js:103
- `api_launcher_profile()` (Zeile ~1034) — aufgerufen von pip-launcher.js:512
- `api_postcall_trend()` (Zeile ~926) — aufgerufen von pip-launcher.js:2138
- `api_feedback()` (Zeile ~1257) — aufgerufen von pip-launcher.js
- `api_precall_research()` (Zeile ~1339) — aufgerufen von pip-launcher.js
- `api_ewb_rate()` (Zeile ~1379) — aufgerufen von pip-launcher.js

**Schritt 4 — Unused Imports pruefen:**
Nach den Loeschungen: Imports am Dateianfang pruefen. Falls `render_template` nur von `live()` benutzt wurde, entfernen. Falls `request` nur Classic-Routen dienten, entfernen. Nur tatsaechlich unused Imports entfernen — nicht raten.

**Wichtig:** OBJECTION_TRIGGER_PROMPT_BASE bleibt als Modul-Konstante in app_routes.py stehen. Die Funktion api_ewb_trigger() die sie als HTTP-Handler nutzte wird geloescht — aber die Konstante selbst bleibt fuer den Import aus app.py (_seed_prompt_versions).
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve/salesnerve && python -c "from routes.app_routes import app_routes_bp; print('Import OK')"</automated>
    Zusaetzlich pruefen:
    - `grep -n "api_ergebnis\|api_analyse_line\|api_log_correction\|api_pause\|api_set_phase\|api_postcall_insights\|api_frage\|api_ewb_trigger\|log_gegenargument_wahl" salesnerve/routes/app_routes.py` → 0 Treffer fuer Funktionsdefinitionen (def ...)
    - `grep -n "def live" salesnerve/routes/app_routes.py` → genau 1 Treffer, die neue redirect-Funktion
    - `grep -n "OBJECTION_TRIGGER_PROMPT_BASE" salesnerve/routes/app_routes.py` → mindestens 1 Treffer (Konstante bleibt)
    - `grep -n "API_FRAGE_PROMPT_BASE" salesnerve/routes/app_routes.py` → 0 Treffer
    - `grep -n "_letzte_gemeldete_version\|_MAX_VERSION_CACHE" salesnerve/routes/app_routes.py` → 0 Treffer
  </verify>

  <acceptance_criteria>
    - `grep -c "def api_ergebnis\|def api_analyse_line\|def api_log_correction\|def api_pause\b\|def api_log\b\|def api_set_phase\|def log_gegenargument_wahl\|def api_frage\b\|def api_ewb_trigger\|def api_postcall_insights" salesnerve/routes/app_routes.py` gibt `0` aus
    - `grep -n "redirect.*dashboard_bp.dashboard" salesnerve/routes/app_routes.py` gibt mindestens 1 Treffer
    - `grep -n "API_FRAGE_PROMPT_BASE" salesnerve/routes/app_routes.py` gibt 0 Treffer
    - `grep -n "OBJECTION_TRIGGER_PROMPT_BASE" salesnerve/routes/app_routes.py` gibt mindestens 1 Treffer
    - `python -c "from routes.app_routes import app_routes_bp"` (aus salesnerve/) laeuft ohne Import-Fehler
  </acceptance_criteria>

  <done>app_routes.py hat keine Classic-only Route-Handler mehr. /live gibt redirect zurueck. OBJECTION_TRIGGER_PROMPT_BASE ist erhalten. API_FRAGE_PROMPT_BASE ist weg.</done>
</task>

<task type="auto">
  <name>Task 2: app.py — API_FRAGE_PROMPT_BASE Import entfernen + _seed entfernen + _SuppressPolling loeschen</name>
  <files>salesnerve/app.py</files>

  <read_first>
    salesnerve/app.py — Zeilen 1-30 (Imports, _SuppressPolling-Klasse) und Zeilen 845-870 (_seed_prompt_versions-Funktion) lesen.
  </read_first>

  <action>
**Schritt 1 — _SuppressPolling-Klasse entfernen (app.py Zeilen 14-19):**

Die gesamte Klasse entfernen:
```python
class _SuppressPolling(logging.Filter):
    def filter(self, record):
        msg = record.getMessage()
        return '/api/ergebnis' not in msg and '/api/status' not in msg
logging.getLogger('werkzeug').addFilter(_SuppressPolling())
```
Beide gefilterten Routen sind weg: `/api/ergebnis` (diese Phase) und `/api/status` (Block I bereits geloescht). Die Klasse ist vollstaendig toter Code.

**Schritt 2 — Import in app.py bereinigen (Zeile ~848-850):**

Aktuelle Import-Zeile (laut RESEARCH.md Zeile 848-850) lautet ungefaehr:
```python
from routes.app_routes import (OBJECTION_TRIGGER_PROMPT_BASE, API_FRAGE_PROMPT_BASE)
```

Aendern zu:
```python
from routes.app_routes import OBJECTION_TRIGGER_PROMPT_BASE
```

(Nur API_FRAGE_PROMPT_BASE entfernen. OBJECTION_TRIGGER_PROMPT_BASE bleibt — wird fuer 'objection_trigger' Seed-Eintrag benoetigt, per D-05.)

**Schritt 3 — api_frage Seed-Eintrag aus _seed_prompt_versions() entfernen (app.py Zeile ~860):**

In der Funktion `_seed_prompt_versions()` gibt es eine `modules`-Liste. Der Eintrag:
```python
('api_frage', API_FRAGE_PROMPT_BASE),
```
komplett entfernen. Die restlichen 4 Eintraege bleiben unveraendert.

Nach Loeschung hat die modules-Liste 4 Eintraege statt 5.

**Kein weiterer Schritt:** Keine anderen Aenderungen an app.py. `OBJECTION_TRIGGER_PROMPT_BASE` und dessen zugehoerigen 'objection_trigger' Seed-Eintrag NICHT anfassen.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve/salesnerve && python -c "import app; print('app.py Import OK')"</automated>
    Zusaetzlich:
    - `grep -n "_SuppressPolling\|api/ergebnis.*filter\|api/status.*filter" salesnerve/app.py` → 0 Treffer
    - `grep -n "API_FRAGE_PROMPT_BASE" salesnerve/app.py` → 0 Treffer
    - `grep -n "OBJECTION_TRIGGER_PROMPT_BASE" salesnerve/app.py` → mindestens 1 Treffer (Import bleibt)
    - `grep -n "api_frage" salesnerve/app.py` → 0 Treffer
  </verify>

  <acceptance_criteria>
    - `grep -c "_SuppressPolling" salesnerve/app.py` gibt `0` aus
    - `grep -c "API_FRAGE_PROMPT_BASE" salesnerve/app.py` gibt `0` aus
    - `grep -c "api_frage" salesnerve/app.py` gibt `0` aus
    - `grep -c "OBJECTION_TRIGGER_PROMPT_BASE" salesnerve/app.py` gibt mindestens `1` aus
    - `python -c "import app"` (aus salesnerve/) laeuft ohne Fehler
  </acceptance_criteria>

  <done>app.py hat kein _SuppressPolling mehr, importiert kein API_FRAGE_PROMPT_BASE mehr, seeded nur noch 4 PromptVersion-Module.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-1-Commit + pytest</name>
  <files></files>

  <read_first>
    Kein File lesen — nur commit + test ausfuehren.
  </read_first>

  <action>
Aus dem Repo-Root `/c/Users/andre/dev/salesnerve/`:

```bash
cd /c/Users/andre/dev/salesnerve
pytest salesnerve/tests/ -x -q 2>&1 | tail -5
```

**Erwartetes Ergebnis:** `test_ft_seed.py` schlaegt FEHL (erwartet noch 5 Module, findet 4). Alle anderen 265 Tests GRUEN. Dieser Zustand ist korrekt — test_ft_seed.py wird in Plan 03 gefixt.

Wenn nur test_ft_seed.py rot ist → commit:
```bash
git add salesnerve/routes/app_routes.py salesnerve/app.py
git commit -m "feat(08.11): remove classic-only routes + /live redirect + cleanup app.py [Wave 1]"
```

Wenn andere Tests rot sind → STOPP. Fehler melden bevor commit.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest salesnerve/tests/ -q 2>&1 | grep -E "passed|failed|error"</automated>
  </verify>

  <acceptance_criteria>
    - pytest Output enthaelt `265 passed` und maximal `1 failed` (nur test_ft_seed.py)
    - `git log --oneline -1` zeigt den neuen commit mit "[Wave 1]" im Message
    - Kein anderer Test als test_ft_seed.py ist rot
  </acceptance_criteria>

  <done>Wave-1-Commit auf main. Pytest: 265 passing, 1 expected failing (test_ft_seed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /live URL → Dashboard | Alte Bookmark-URLs werden redirected — kein freier Zugang zu geloeschten Funktionen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.11-01 | Tampering | /live redirect | accept | Redirect ist 302 zu login_required Route — kein Auth-Bypass moeglich |
| T-08.11-02 | Denial of Service | Geloeschte Routes | accept | 404 bei direktem Aufruf ist korrektes Verhalten, kein Angriffspotenzial |
</threat_model>

<verification>
Nach Wave 1:
- `python -c "from routes.app_routes import app_routes_bp"` → kein Error
- `python -c "import app"` → kein Error
- `grep -c "def api_ergebnis\|def api_frage\|def api_ewb_trigger\|def api_postcall_insights\|def api_pause\b\|def api_log\b\|def api_set_phase\|def api_analyse_line\|def api_log_correction\|def log_gegenargument_wahl" salesnerve/routes/app_routes.py` → `0`
- `grep -n "redirect.*dashboard" salesnerve/routes/app_routes.py` → Treffer in live()-Funktion
- pytest: 265 passing, nur test_ft_seed.py rot
</verification>

<success_criteria>
- 11 Classic-only Route-Handler geloescht aus app_routes.py
- /live Route gibt redirect(url_for('dashboard_bp.dashboard')) zurueck
- API_FRAGE_PROMPT_BASE Konstante + Import aus allen Files entfernt
- OBJECTION_TRIGGER_PROMPT_BASE unveraendert erhalten
- _SuppressPolling-Klasse aus app.py entfernt
- _seed_prompt_versions() seeded 4 statt 5 Module
- Wave-1-Commit auf git
- pytest: alle Tests ausser test_ft_seed.py gruen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.11-stabilisierung-block-f-classic-view-deprecation/08.11-01-SUMMARY.md` erstellen.
</output>
