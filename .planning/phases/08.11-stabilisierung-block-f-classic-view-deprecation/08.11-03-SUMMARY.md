---
phase: 08.11
plan: 03
subsystem: frontend-js, tests
tags: [wave-3, legacy-cleanup, pip-launcher, test-fix, block-f]
dependency_graph:
  requires: [08.11-01, 08.11-02]
  provides: [clean-pip-launcher-no-legacy-opener, green-test_ft_seed]
  affects: [static/pip-launcher.js, tests/test_ft_seed.py]
tech_stack:
  added: []
  patterns: [dead-code-removal, test-count-sync]
key_files:
  created: []
  modified:
    - static/pip-launcher.js
    - tests/test_ft_seed.py
decisions:
  - "[08.11-03]: legacyOpener / profileDaten.opener vollstaendig entfernt — alle EA-Profile nutzen openerItems direkt seit Phase 04.17. Kein Fallback mehr noetig."
  - "[08.11-03]: test_ft_seed.py auf 4 Module reduziert — api_frage Route in Wave 1 geloescht, Seed-Funktion entfernt"
metrics:
  duration: ~8min
  completed: 2026-04-25
  tasks_completed: 3
  files_modified: 2
---

# Phase 08.11 Plan 03: Wave 3 Legacy-Opener-Cleanup + test_ft_seed Fix Summary

**One-liner:** Legacy `profileDaten.opener` Fallback aus pip-launcher.js entfernt (6 Stellen) + test_ft_seed.py auf 4-Modul-Seed-Count korrigiert.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | pip-launcher.js Legacy-Opener entfernen | 42a7c29 | static/pip-launcher.js |
| 2 | test_ft_seed.py 5→4 Module | 57605d9 | tests/test_ft_seed.py |
| 3 | Wave-3-Commit + pytest-Verifikation | (siehe Notiz) | — |

## What Was Built

### Task 1: pip-launcher.js Cleanup

Entfernte alle 6 Legacy-Branches in pip-launcher.js:

1. `var legacyOpener = (state.profileDaten && state.profileDaten.opener) ? state.profileDaten.opener : ''` — Definition entfernt
2. Opener-Preview-Fallback: `legacyOpener ? escHtml(legacyOpener) : 'Kein Opener hinterlegt'` → `'Kein Opener hinterlegt'`
3. Opener-Edit-Button-Conditional: `(state.openerItems.length > 0 || legacyOpener)` → `state.openerItems.length > 0`
4. `else if (state.profileDaten && state.profileDaten.opener) { openerFuerKi = ... }` in pipStartCall — entfernt
5. `else if (state.profileDaten && state.profileDaten.opener) { openerText = ... }` in renderTeleprompter — entfernt

Verifizierung:
- `grep -c "legacyOpener\|profileDaten\.opener" static/pip-launcher.js` → `0`
- `grep -c "openerItems" static/pip-launcher.js` → `14`
- `node --check static/pip-launcher.js` → Syntax OK

### Task 2: test_ft_seed.py Fix

- `api_frage` aus `EXPECTED_MODULES` entfernt (Route in Wave 1 geloescht)
- Beide count-Assertions von `== 5` auf `== 4` geaendert
- `pytest tests/test_ft_seed.py -v` → 2 passed (test_prompt_seed, test_seed_idempotent)

### Task 3: pytest-Baseline

- `pytest tests/test_ft_seed.py -v` → **2 passed** (Ziel erreicht)
- `pytest tests/ --ignore=tests/test_ft_seed.py -q` → **266 passed, 11 failed, 28 errors** (pre-existing Baseline unveraendert)
- test_ft_seed.py erscheint im Gesamtlauf als Collection-ERROR (nicht als FAILED) — pre-existing Umgebungsproblem: kaputtes `requests`-Package verhindert `authlib`-Import bei `from app import _seed_prompt_versions` im Gesamtlauf. Direkt aufgerufen laeuft der Test einwandfrei durch.

## Deviations from Plan

**1. [Rule 3 - Environment] test_ft_seed.py Collection-Error im Gesamtlauf**

- **Found during:** Task 3
- **Issue:** `pytest tests/ -q` zeigt `ERROR tests/test_ft_seed.py` statt `PASSED`. Ursache: kaputtes `requests`-Paket in der lokalen Python-Umgebung verhindert `authlib`-Import wenn `app.py` als Teil des Gesamt-Laufs importiert wird. Direkter Aufruf `pytest tests/test_ft_seed.py` funktioniert einwandfrei (2 passed).
- **Fix:** Nicht behoben — pre-existing Umgebungsproblem, ausserhalb des Plan-Scope. test_ft_seed.py selbst ist korrekt und gruen.
- **Impact:** 266 passing baseline unveraendert. test_ft_seed.py ist nicht mehr in der FAILED-Liste (war vorher FAILED wegen `== 5` vs tatsaechlicher 4 Module).

## Known Stubs

Keine. Beide Aenderungen sind vollstaendige Loeschungen / Korrekturen ohne Platzhalter.

## Threat Flags

Keine neuen Security-relevanten Surfaces eingefuehrt. Laut Threat-Register T-08.11-05: profileDaten.opener war Client-Side-Only, kein Server-Side-Secret. Deletion risikolos.

## Self-Check: PASSED

- `static/pip-launcher.js` existiert und hat 0 legacyOpener-Treffer
- `tests/test_ft_seed.py` existiert und hat 0 api_frage-Treffer, 2 "== 4"-Assertions
- Commits 42a7c29 und 57605d9 existieren auf main
- `pytest tests/test_ft_seed.py -v` → 2 passed
