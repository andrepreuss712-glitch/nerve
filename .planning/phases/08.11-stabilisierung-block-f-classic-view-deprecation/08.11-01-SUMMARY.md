---
phase: 08.11
plan: "01"
subsystem: backend-routes
tags: [cleanup, classic-view-deprecation, block-f, wave-1]
dependency_graph:
  requires: []
  provides: [classic-routes-deleted, live-redirect, app-py-clean]
  affects: [routes/app_routes.py, app.py]
tech_stack:
  added: []
  patterns: [route-deletion, redirect-pattern]
key_files:
  modified:
    - routes/app_routes.py
    - app.py
decisions:
  - "OBJECTION_TRIGGER_PROMPT_BASE bleibt als Modul-Konstante in app_routes.py — wird noch fuer objection_trigger Seed-Eintrag benoetigt (D-05)"
  - "/live leitet per 302 auf dashboard_bp.dashboard um — keine app.html-Renderung mehr"
  - "logging-Import in app.py nach _SuppressPolling-Loeschung unused — pre-existing, out-of-scope fuer diesen Plan"
  - "11 pre-existing Test-Fehler (11 failed, 28 errors) bleiben unveraendert — durch diese Aenderungen nicht verursacht"
metrics:
  duration: "~15min"
  completed: "2026-04-25T13:57:00Z"
  tasks_completed: 3
  files_modified: 2
---

# Phase 08.11 Plan 01: Backend-Cleanup Wave 1 Summary

Backend-Cleanup Wave 1: 10 Classic-only HTTP-Routen aus app_routes.py geloescht, /live durch Redirect zu /dashboard ersetzt, app.py von totem _SuppressPolling-Code und API_FRAGE_PROMPT_BASE-Import befreit — ~453 Zeilen entfernt.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Classic-Routen aus app_routes.py loeschen + /live Redirect | c05e548 | routes/app_routes.py |
| 2 | app.py — API_FRAGE_PROMPT_BASE Import + _SuppressPolling loeschen | c05e548 | app.py |
| 3 | Wave-1-Commit + pytest | c05e548 | — |

## What Was Built

### routes/app_routes.py

Folgende Classic-only Route-Handler wurden vollstaendig entfernt:

| Route | Funktion | Grund |
|-------|----------|-------|
| /api/ergebnis | api_ergebnis() | Classic-Polling, PiP nutzt Socket.IO push |
| /api/analyse_line | api_analyse_line() | Classic flipRowRole, 0 PiP-Caller |
| /api/log_correction | api_log_correction() | Classic-only |
| /api/pause | api_pause() | Classic-only (PiP nutzt mute_mic Socket-Event) |
| /api/log | api_log() | Classic downloadLog, 0 PiP-Caller |
| /api/set_phase | api_set_phase() | Classic-only (app.html:1289) |
| /api/log_gegenargument_wahl | log_gegenargument_wahl() | 0 PiP-Caller |
| /api/frage | api_frage() | Classic "Frage stellen"-Modal |
| /api/ewb_trigger | api_ewb_trigger() | Classic HTTP-EWB, PiP nutzt manual_ewb Socket |
| /api/postcall_insights | api_postcall_insights() | Classic PostCall-Overlay |

Zusaetzlich entfernt:
- `API_FRAGE_PROMPT_BASE` Multiline-String-Konstante
- `_letzte_gemeldete_version` dict + `_MAX_VERSION_CACHE` Konstante
- `render_template` und `Response` aus Flask-Import (wurden nur von geloeschten Routen genutzt)

`/live`-Route durch Redirect ersetzt:
```python
@app_routes_bp.route('/live')
@login_required
def live():
    # Classic-View entfernt (Block F). Redirect auf Dashboard.
    from flask import redirect, url_for
    return redirect(url_for('dashboard_bp.dashboard'))
```

`OBJECTION_TRIGGER_PROMPT_BASE` bleibt erhalten — wird noch fuer den `objection_trigger` PromptVersion-Seed-Eintrag benoetigt.

### app.py

- `_SuppressPolling`-Klasse entfernt (beide gefilterten Routen sind weg: `/api/ergebnis` in diesem Plan, `/api/status` in Block I)
- `from routes.app_routes import (OBJECTION_TRIGGER_PROMPT_BASE, API_FRAGE_PROMPT_BASE)` geaendert zu `from routes.app_routes import OBJECTION_TRIGGER_PROMPT_BASE`
- `('api_frage', API_FRAGE_PROMPT_BASE)` Tupel aus `modules`-Liste in `_seed_prompt_versions()` entfernt — jetzt 4 statt 5 Module

## Deviations from Plan

### Auto-cleaned Unused Imports

**[Rule 2 - Cleanup] render_template und Response aus Flask-Import entfernt**
- **Found during:** Task 1, Schritt 4 (Unused Imports pruefen)
- **Issue:** Nach Loeschung aller Classic-Routen waren `render_template` und `Response` unused
- **Fix:** Beide aus dem Flask-Import in app_routes.py entfernt
- **Files modified:** routes/app_routes.py Zeile 3
- **Commit:** c05e548

### Pre-existing Test-Fehler (Out-of-Scope)

Vor den Aenderungen existierten bereits `11 failed, 28 errors` in pytest. Diese stammen aus frueherem Code (test ordering issues, exchange_rates, qa_pipeline). Unveraendert nach diesem Plan — kein neuer Fehler eingefuehrt.

Dokumentiert in deferred-items.md (out-of-scope).

## Test Results

```
pytest tests/ --ignore=tests/test_ft_seed.py
11 failed, 266 passed, 1 skipped, 28 errors  ← identical to pre-change baseline
```

```
pytest tests/test_ft_seed.py
2 failed  ← expected (api_frage seed entfernt, test erwartet noch 5 Module)
```

test_ft_seed.py wird in Plan 03 korrigiert.

## Known Stubs

Keine — alle Aenderungen sind Loeschungen, kein neuer Code mit Stub-Pattern.

## Threat Flags

Keine neuen Threat-relevanten Surfaces eingefuehrt (nur Code entfernt).

## Self-Check: PASSED

- `routes/app_routes.py` existiert: FOUND
- `app.py` existiert: FOUND
- Commit c05e548 existiert: FOUND
- `grep -c "def api_ergebnis|..."` = 0: VERIFIED
- `grep -n "redirect.*dashboard_bp.dashboard"` = 1 Treffer: VERIFIED
- `grep -c "API_FRAGE_PROMPT_BASE" app.py` = 0: VERIFIED
- `grep -c "_SuppressPolling" app.py` = 0: VERIFIED
- `python -c "from routes.app_routes import app_routes_bp"`: OK
- `python -c "import app"`: OK
