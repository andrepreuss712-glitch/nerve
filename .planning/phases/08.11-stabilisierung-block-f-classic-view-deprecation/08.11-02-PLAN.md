---
phase: 08.11
plan: 02
type: execute
wave: 2
depends_on: [08.11-01]
files_modified:
  - salesnerve/static/app.js
  - salesnerve/templates/app.html
  - salesnerve/templates/base.html
  - salesnerve/templates/dashboard.html
  - salesnerve/templates/logs_page.html
  - salesnerve/templates/onboarding.html
autonomous: true
requirements: [BLOCK-F-02]

must_haves:
  truths:
    - "static/app.js existiert nicht mehr im Dateisystem"
    - "templates/app.html existiert nicht mehr im Dateisystem"
    - "base.html hat kein request.path == '/live' Conditional mehr"
    - "dashboard.html oeffnet PiP-Launcher statt zu /live zu navigieren"
    - "logs_page.html oeffnet PiP-Launcher statt zu /live zu navigieren"
    - "onboarding.html oeffnet PiP-Launcher statt window.location.href auf /live zu setzen"
    - "pytest 265 passing nach Wave-2-Commit"
  artifacts:
    - path: salesnerve/templates/base.html
      provides: "Bereinigtes Nav ohne /live active-class-Condition"
    - path: salesnerve/templates/dashboard.html
      provides: "Call-starten Button oeffnet NerveLauncher statt /live"
    - path: salesnerve/templates/logs_page.html
      provides: "Kein /live Link mehr"
    - path: salesnerve/templates/onboarding.html
      provides: "Kein window.location.href='/live' mehr"
  key_links:
    - from: salesnerve/templates/dashboard.html
      to: window.NerveLauncher.open()
      via: "onclick Attribut auf dem Call-starten Button"
      pattern: "NerveLauncher.*open"
---

<objective>
Frontend-Cleanup Wave 2: app.js und app.html loeschen, alle /live-Referenzen in Templates auf PiP-Launcher umstellen.

Purpose: Mit den Backend-Routen aus Wave 1 geloescht, koennen jetzt app.js (2121 Zeilen) und app.html (1436 Zeilen) sicher entfernt werden. Template-Links auf /live werden auf window.NerveLauncher.open() umgebogen.
Output: Keine Classic-View-Files mehr im Repo. Templates zeigen auf PiP-Launcher.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/c/Users/andre/dev/salesnerve/.planning/phases/08.11-stabilisierung-block-f-classic-view-deprecation/08.11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.js und app.html loeschen</name>
  <files>salesnerve/static/app.js, salesnerve/templates/app.html</files>

  <read_first>
    Kein Lesen noetig — beide Files werden komplett geloescht. Research bestaetigt: 0 Cross-Calls von pip-launcher.js in app.js. app.html ist standalone (kein {% extends 'base.html' %}).
  </read_first>

  <action>
Beide Files loeschen:

```bash
rm /c/Users/andre/dev/salesnerve/salesnerve/static/app.js
rm /c/Users/andre/dev/salesnerve/salesnerve/templates/app.html
```

**Begruendung (aus RESEARCH.md):**
- `app.js` (2121 Zeilen) wird ausschliesslich von `app.html:1228` via `<script src>` geladen. pip-launcher.js hat 0 Cross-Calls in app.js. Alle PiP-Helper-Funktionen in app.js (getPipElement, openPipWindow, etc.) werden nur von app.html aufgerufen — nicht von pip-launcher.js.
- `app.html` (1436 Zeilen) ist ein standalone HTML-File ohne `{% extends %}`. Kein Template-Inheritance-Impact.

**Nach Loeschung:** Flask gibt 404 bei direktem Aufruf von /live (bis der Redirect aus Wave 1 greift, was bereits committed ist). Korrekt.
  </action>

  <verify>
    <automated>ls /c/Users/andre/dev/salesnerve/salesnerve/static/app.js 2>&1; ls /c/Users/andre/dev/salesnerve/salesnerve/templates/app.html 2>&1</automated>
    Erwartetes Ergebnis: beide Befehle geben "No such file or directory" aus.
  </verify>

  <acceptance_criteria>
    - `test -f salesnerve/static/app.js && echo EXISTS || echo DELETED` gibt `DELETED` aus
    - `test -f salesnerve/templates/app.html && echo EXISTS || echo DELETED` gibt `DELETED` aus
    - `grep -r "src.*app\.js" salesnerve/templates/` gibt 0 Treffer (keine Template referenziert app.js mehr)
  </acceptance_criteria>

  <done>app.js und app.html sind aus dem Dateisystem entfernt.</done>
</task>

<task type="auto">
  <name>Task 2: Template-Links von /live auf PiP-Launcher umstellen</name>
  <files>salesnerve/templates/base.html, salesnerve/templates/dashboard.html, salesnerve/templates/logs_page.html, salesnerve/templates/onboarding.html</files>

  <read_first>
    - salesnerve/templates/base.html: Zeile 41 lesen (n-nav-item mit /live conditional)
    - salesnerve/templates/dashboard.html: Zeile 45 lesen (href="/live" Button)
    - salesnerve/templates/logs_page.html: Zeile 84 lesen (href="/live" Link)
    - salesnerve/templates/onboarding.html: Zeile 632 lesen (window.location.href = '/live')
  </read_first>

  <action>
**base.html — Zeile 41 (aus RESEARCH.md Section 6):**

Vor-Zustand:
```html
<a class="n-nav-item {% if request.path == '/live' %}n-nav-item-active{% endif %}" 
   href="javascript:void(0)" onclick="window.NerveLauncher && window.NerveLauncher.open()" 
   title="Live-Assistent">
```

Nach-Zustand (Jinja2-Conditional komplett entfernen):
```html
<a class="n-nav-item" 
   href="javascript:void(0)" onclick="window.NerveLauncher && window.NerveLauncher.open()" 
   title="Live-Assistent">
```

**dashboard.html — Zeile 45 (aus RESEARCH.md Section 2):**

Vor-Zustand:
```html
href="/live"
```

Nach-Zustand: Den `href="/live"`-Button so umbauen, dass er den PiP-Launcher oeffnet:
```html
href="javascript:void(0)" onclick="window.NerveLauncher && window.NerveLauncher.open()"
```

Falls der Button ein `<a>`-Tag ist: href und onclick setzen wie oben.
Falls ein `<button>`-Tag: onclick setzen, href entfernen.
Den genauen Tag-Typ erst aus der Zeile 45 lesen, dann entsprechend aendern.

**logs_page.html — Zeile 84 (aus RESEARCH.md Section 2):**

Vor-Zustand:
```html
href="/live"
```

Nach-Zustand:
```html
href="javascript:void(0)" onclick="window.NerveLauncher && window.NerveLauncher.open()"
```

**onboarding.html — Zeile 632 (aus RESEARCH.md Section 2):**

Vor-Zustand:
```javascript
window.location.href = '/live'
```

Nach-Zustand:
```javascript
window.NerveLauncher && window.NerveLauncher.open()
```

(Falls dort auch ein Fallback sinnvoll ist: `window.NerveLauncher ? window.NerveLauncher.open() : window.location.href = '/dashboard'` — aber nur wenn der Kontext das erfordert. Primaer: direkter open()-Call.)

**Keine anderen Aenderungen** an diesen Templates vornehmen.
  </action>

  <verify>
    <automated>grep -rn "'/live'\|href=\"/live\"\|href='/live'\|request\.path.*live" salesnerve/salesnerve/templates/ 2>/dev/null || grep -rn "'/live'\|href=\"/live\"\|href='/live'\|request\.path.*live" /c/Users/andre/dev/salesnerve/salesnerve/templates/</automated>
    Erwartetes Ergebnis: 0 Treffer (alle /live Referenzen in Templates entfernt).
  </verify>

  <acceptance_criteria>
    - `grep -rn "href=\"/live\"\|href='/live'" salesnerve/templates/` gibt 0 Treffer
    - `grep -rn "request\.path.*live\|live.*request\.path" salesnerve/templates/` gibt 0 Treffer
    - `grep -rn "window\.location.*href.*live\|location\.href.*live" salesnerve/templates/` gibt 0 Treffer
    - `grep -c "NerveLauncher.*open" salesnerve/templates/base.html` gibt mindestens `1` aus
    - `grep -c "NerveLauncher.*open" salesnerve/templates/dashboard.html` gibt mindestens `1` aus
    - `grep -c "NerveLauncher.*open" salesnerve/templates/logs_page.html` gibt mindestens `1` aus
  </acceptance_criteria>

  <done>Alle 4 Templates haben keine /live-Referenzen mehr. PiP-Launcher-Open-Calls sind eingebaut.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-2-Commit + pytest</name>
  <files></files>

  <read_first>
    Kein File lesen.
  </read_first>

  <action>
```bash
cd /c/Users/andre/dev/salesnerve
pytest salesnerve/tests/ -x -q 2>&1 | tail -5
```

**Erwartetes Ergebnis:** test_ft_seed.py weiterhin rot (wird in Plan 03 gefixt). Alle anderen 265 Tests gruen.

Wenn nur test_ft_seed.py rot ist:
```bash
git add salesnerve/static/app.js salesnerve/templates/app.html salesnerve/templates/base.html salesnerve/templates/dashboard.html salesnerve/templates/logs_page.html salesnerve/templates/onboarding.html
git commit -m "feat(08.11): delete app.js + app.html + fix /live template refs [Wave 2]"
```

Hinweis: `git add` auf geloeschte Files funktioniert — git staged die Deletion. Alternativ: `git add -u salesnerve/static/app.js salesnerve/templates/app.html` um nur deletions zu stagen.

Wenn andere Tests rot sind → STOPP. Fehler melden.
  </action>

  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest salesnerve/tests/ -q 2>&1 | grep -E "passed|failed|error"</automated>
  </verify>

  <acceptance_criteria>
    - pytest Output: `265 passed` und maximal `1 failed` (nur test_ft_seed.py)
    - `git log --oneline -1` zeigt "[Wave 2]" im Commit-Message
    - `git show --stat HEAD` enthaelt `app.js` und `app.html` als deleted files
  </acceptance_criteria>

  <done>Wave-2-Commit auf main. app.js und app.html aus Git-History entfernt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template → NerveLauncher.open() | JavaScript-Call statt HTTP-Navigation — kein neues Angriffsvektor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.11-03 | Information Disclosure | app.js Deletion | accept | Kein sensitiver Code in app.js — Classic-View-UI-Logik. Git-History bleibt erhalten fuer Audit. |
| T-08.11-04 | Tampering | Template-Links | accept | NerveLauncher.open() ist read-only Client-Call — kein State-Mutation-Risiko |
</threat_model>

<verification>
Nach Wave 2:
- `test -f salesnerve/static/app.js` → exit code 1 (nicht existent)
- `test -f salesnerve/templates/app.html` → exit code 1 (nicht existent)
- `grep -rn "href=\"/live\"" salesnerve/templates/` → 0 Treffer
- `grep -rn "request.path.*live" salesnerve/templates/` → 0 Treffer
- Pytest: 265 passing, 1 expected failing (test_ft_seed)
</verification>

<success_criteria>
- app.js geloescht (2121 Zeilen Classic-JS entfernt)
- app.html geloescht (1436 Zeilen Classic-Template entfernt)
- base.html: kein /live conditional mehr im Nav
- dashboard.html, logs_page.html, onboarding.html: alle /live-Referenzen durch NerveLauncher.open() ersetzt
- Wave-2-Commit auf git mit korrekten deleted-file Eintraegen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.11-stabilisierung-block-f-classic-view-deprecation/08.11-02-SUMMARY.md` erstellen.
</output>
