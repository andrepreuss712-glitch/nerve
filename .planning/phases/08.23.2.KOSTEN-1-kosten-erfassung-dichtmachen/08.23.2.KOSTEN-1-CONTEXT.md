# 08.23.2.KOSTEN-1 — CONTEXT (Ist-Anker, Prod-verifiziert 2026-07-20)

> **★ DESIGN-HOHEIT:** Der Bauplan ist FERTIG und kommt von **Fable** (2026-07-19), Gemini-cross-gecheckt +
> Claudian-auditiert. GSD setzt ihn UM, entwirft ihn NICHT neu.
> **Pflicht-Pre-Read (= die Spezifikation):**
> `C:\Users\andre\OneDrive\Desktop\Nerve-Vault\03 Planung\KOSTEN-1 Kosten-Erfassung dichtmachen + Dashboard — Bauplan (Fable 2026-07-19).md`
>
> **★ DISZIPLIN (Gemini-Stolperdraht, verbindlich):** FIX-Block, **KEIN neues Kosten-System**.
> Kein Wrapper-/Decorator-Framework um den Anthropic-Client, kein AST-Parsing, kein Event-Bus für
> `nerve_rt`, keine Rate-Sync-Engine, kein Backfill-Job. Wenn ein Teil aufbläht → **STOP, melden, deferren.**

---

## 0. Was diese Datei ist

Die am **echten HEAD (88d2571) + an der echten Prod-DB** nachgeprüfte Anker-Karte zum Fable-Plan.
Fables Plan ist die Spezifikation; diese Datei korrigiert die **Zeilen-/Ist-Anker**, wo sie gedriftet sind,
und hält die **fünf neuen Funde** fest, die beim Nachprüfen aufgefallen sind. Kein Scope-Zuwachs.

---

## 1. Persistenz-Schicht-Verifikation (Punkt 21) — verbatim inspect.sh-Output

Gezogen am **2026-07-20 gegen Production** (`178.104.82.166`, `scripts/inspect.sh`, read-only):

```
$ bash scripts/inspect.sh schema api_rates
                                           Table "public.api_rates"
     Column      |            Type             | Nullable |                Default
-----------------+-----------------------------+----------+---------------------------------------
 id              | integer                     | not null | nextval('api_rates_id_seq'::regclass)
 provider        | character varying(32)       | not null |
 model           | character varying(64)       | not null |
 unit_type       | character varying(32)       | not null |
 price_per_unit  | numeric(12,8)               | not null |
 currency        | character varying(3)        | not null |
 active          | boolean                     | not null |
 last_checked_at | timestamp without time zone | not null |
 source_url      | character varying(512)      |          |
 created_at      | timestamp without time zone | not null |
Indexes:
    "api_rates_pkey" PRIMARY KEY, btree (id)
    "ix_api_rates_provider" btree (provider)
    "uix_api_rate_active" UNIQUE CONSTRAINT, btree (provider, model, unit_type, active)
Referenced by:
    TABLE "price_change_log" CONSTRAINT "price_change_log_api_rate_id_fkey" FOREIGN KEY (api_rate_id) REFERENCES api_rates(id)
```

```
$ bash scripts/inspect.sh schema api_cost_log
                                           Table "public.api_cost_log"
     Column      |            Type             | Nullable
-----------------+-----------------------------+----------
 id              | integer                     | not null
 created_at      | timestamp without time zone | not null   (idx)
 provider        | character varying(32)       | not null   (idx)
 model           | character varying(64)       | not null
 user_id         | integer                     |            (FK users, idx)
 org_id          | integer                     |            (FK organisations, idx)
 units           | numeric(14,4)               | not null
 unit_type       | character varying(32)       | not null
 rate_applied    | numeric(12,8)               | not null
 rate_currency   | character varying(3)        | not null
 fx_rate_applied | numeric(10,6)               | not null
 cost_eur        | numeric(12,6)               | not null
 session_id      | character varying(64)       |            (idx)
 context_tag     | character varying(32)       |
 latency_ms      | integer                     |
 call_site       | character varying(50)       |
```

```
$ bash scripts/inspect.sh schema price_change_log
 id | api_rate_id (FK api_rates, not null) | changed_at (idx) | old_rate numeric(12,8)
 | new_rate numeric(12,8) | currency varchar(3) | impact_eur_per_month numeric(12,2) | note text
```

**Schreib-Zeitpunkt-Spalte (Punkt 26):** `api_rates` wird **beim App-Start** (Seed) und **on-demand über die
Admin-UI** geschrieben; `api_cost_log` wird **synchron im Moment des API-Calls** geschrieben (kein Batch,
kein Hintergrund-Thread) → **kein Leser-läuft-dem-Schreiber-voraus-Risiko** in diesem Block.
Einzige Ausnahme: der Deepgram-STT-Log läuft erst **bei Session-Ende** (`_close_deepgram_connection`) —
der Dashboard-Leser liest immer im Nachhinein, also unkritisch.

## 1b. Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `ApiRate.active` | gelesen `cost_tracker.py:105-108`, geschrieben Seed + `admin_dashboard.py:411` | DB-Spalte `api_rates.active` (bool, Teil des UNIQUE) | ✓ inspect.sh |
| `ApiCostLog.cost_eur` | geschrieben `cost_tracker.py`, gelesen `admin_dashboard.py:541 ff.` | DB-Spalte | ✓ inspect.sh |
| `_stt_seconds_accumulated[sid]` | geschrieben `deepgram_service.py:160`, gelesen/gepoppt `:491` | **RAM (Modul-Dict, per-sid gekeyt)** — KEINE DB-Spalte | ✓ Code |
| nerve_rt STT-Sekunden | existiert heute **gar nicht** | — | ✓ (R3 baut sie) |
| `COST_DATA_COMPLETE_SINCE` | nur gelesen (Dashboard) | **Python-Konstante**, keine DB | ✓ (R5) |

---

## 2. Ist-Anker — korrigiert am echten Code (HEAD 88d2571)

### 2.1 `services/cost_tracker.py` — Fables Anker **alle korrekt**
- `log_api_cost()` Signatur **:68-80** — positional `provider, model, user_id, units, unit_type`;
  kw-only `org_id, session_id, context_tag, latency_ms, call_site`. Gibt `None` zurück, **raist nie**
  (äußeres try/except :91 / :142-143).
- **Der stille Skip — :105-112** (verbatim):
  ```python
              rate = (db.query(ApiRate)
                        .filter_by(provider=provider, model=model,
                                   unit_type=unit_type, active=True)
                        .first())
              if not rate:
                  print(f"[CostTracker] no active ApiRate for "
                        f"{provider}/{model}/{unit_type} — skipping log")
                  return
  ```
- Eigene `SessionLocal` **:103** (`import database.db as _db_mod` :92) → aus jedem Prozess nutzbar.
- Resolver `_resolve_user_id_from_live_session` **:31-49**, `_resolve_org_id_from_live_session` **:52-65**
  (beide `None` ohne `sid`). `_get_current_fx_rate` :14-28 (Fallback `Decimal('0.92')`).

### 2.2 `services/deepgram_service.py`
- **:497-499** loggt tatsächlich `'nova-3'` (Fable korrekt):
  ```python
          log_api_cost('deepgram', 'nova-3', user_id=None,
                       units=minutes, unit_type='per_minute',
                       session_id=str(sid), context_tag='stt')
  ```
- ⚠ **Anker-Korrektur:** `:491` ist der **Pop** (`_stt_seconds_accumulated.pop(sid, 0.0)`) in
  `_close_deepgram_connection` (:486). Die **Akkumulation** sitzt bei **:160**
  (`_stt_seconds_accumulated[sid] = ... + _dur`), Dict-Deklaration :21. Das ist das Muster, das R3 spiegelt.
- ⚠ **Anker-Korrektur:** `diarize` (:457) ist **konditional** (`diarize=is_meeting`, `is_meeting` :441),
  nicht hart `True`. Modell `model="nova-3"` :451.

### 2.3 `app.py` — die zwei Seeds
- **Seed 08.14** (per-Tripel SELECT-dann-INSERT, das GUTE Muster): **:955-984**, Liste **:958-967**
  (nur die 8 anthropic-Voll-ID-Zeilen), `source_url='seed:2026-04-27'`.
- **Seed A** (`count()==0`, der Bug): in `_seed_founder_dashboard_defaults()` (**:1092**),
  Guard **:1116**, Liste **:1117-1126**, Insert-Schleife :1127-1131.
  ⚠ **Anker-Korrektur:** Fables `:963-966` zeigt auf Seed-08.14-Zeilen, nicht auf Seed A.
  ⚠ **Wurzel bestätigt:** sobald Seed 08.14 einmal gelaufen ist, feuert Seed A **nie wieder** →
  neue Modelle/Preise kommen nie nach.

### 2.4 `services/claude_service.py`
- Bestehender Kosten-Hook **:542-568** (Muster für alle neuen Hooks), Cache-Calls **:559-561** / **:563-565**.
- `_model_autovar` **:626** = `config.MODEL_PIP_AUTOVAR`, Fallback :628, Hooks :693/696/703/707.
- Insgesamt **22 bestehende `log_api_cost`-Sites** in `services/` (claude_service, coaching_service,
  crm_service, deepgram_service, precall_service, qa_pipeline, training_service).

### 2.5 Die 8 ungehookten Call-Sites — **alle bestätigt**

| # | Site | Anker (verifiziert) | Anmerkung |
|---|---|---|---|
| 1 | Judge-Runner | `services/judge_runner.py:382` (`model=MODEL_JUDGE` :383) | `MODEL_JUDGE` :38 = `os.getenv('MODEL_JUDGE', config.MODEL_POSTCALL_ANALYSIS)` |
| 2 | Adoption-Runner | `services/adoption_runner.py:281` (:282) | `MODEL_ADOPTION` :39, dito |
| 3 | Outcome-Classifier | `services/outcome_service.py:164` | `_HAIKU_MODEL` :28-31 |
| 4 | Training-Preview | `routes/training.py:961` | `config.MODEL_TRAINING_PREVIEW` :962, `g.user`/`g.org` ✓ |
| 5 | validate_user_text | `services/coaching_service.py:147` | in `validate_user_text` :144 |
| 6 | Training-Transkription | `routes/training.py:842-848` | `model='nova-2'` :846, `transcribe_file` :848 |
| 7 | Brave Search | `services/precall_service.py:315` | in `_brave_search` :298 |
| 8 | Stripe-Fees | `routes/payments.py:248` = **Definition** `_record_revenue`; **Aufrufer :98** (Webhook) | ⚠ Anker-Präzisierung |

### 2.6 `nerve_rt/` — Prozess-Grenze (R3)
- `nerve_rt/services/llm/claude_adapter.py`: ⚠ **`MODEL = "claude-haiku-4-5-20251001"` steht bei `:24`**
  (nicht :49). Der Call ist `:49-58` (`await self._client.messages.create(...)`), Latenz `:60`,
  Funktion `async def analyse` :35. **`msg.usage` wird heute nirgends gelesen.**
- `nerve_rt/services/stt/deepgram_adapter.py`: `async def close(self, conn)` **:140**, `conn.finish()` :146.
  **Es gibt heute KEINE Sekunden-Akkumulation.** Session-Ende liegt in
  `nerve_rt/services/session_manager.py:169` (`await self.stt.close(stt_conn)`, in `handle_session` :115).
- **Wichtig:** `nerve_rt` hat **null** DB-Kopplung (0 Treffer für `SessionLocal`, `database.models`,
  `cost_tracker`, `psycopg`), eigener FastAPI-Prozess auf Port 8001, Brücke via `nerve_rt/redis_bridge.py`.
  Import wäre auflösbar (gleiches Repo-Root, absolute Paket-Importe), ist aber **neu** → R3 ist die
  riskanteste Naht des Blocks. Fables STOP-Signal gilt: bei Sync/Async-Schmerz →
  `run_in_executor`; wenn auch das eskaliert → nur Rohdaten strukturiert nach stdout + deferren.

### 2.7 Sonstiges
- `routes/admin_dashboard.py:541` `compute_org_profitability(db, org_id, start, end)` ✓ — liest
  `RevenueLog.netto_cents` + `SUM(ApiCostLog.cost_eur)`, korrigiert sich nach R1/R2 automatisch.
  Konsument: JSON-Route `kunden_page()` **:563**, gerendert in `templates/admin/_tab_kunden.html:27-28`;
  Drilldown `kunden_drilldown(org_id)` :596. Shell-Route `index()` :60-73 → `templates/admin/dashboard.html`.
- ⚠ `config.py`-MODEL-Block ist **:74-97** (nicht :74-93). `MODEL_JUDGE`/`MODEL_ADOPTION` stehen
  **nicht** in `config.py` — sie sind `os.getenv`-Locals in ihren eigenen Modulen.

---

## 3. ★ FÜNF NEUE FUNDE beim Nachprüfen (nicht in Fables Plan — Cross-AI-Pflichtpunkte)

**F-1 — Der Sonnet-Kurzname `claude-sonnet-4-5` hat KEINE Rate (zweites stilles Loch).**
Prod hat `sonnet-4-5` (id 9-12) und `claude-sonnet-4-5-20251022` (id 15-18), aber **nicht**
`claude-sonnet-4-5`. Genau diesen String loggt `_model_autovar` (`config.MODEL_PIP_AUTOVAR` =
`"claude-sonnet-4-5"`, `config.py:93`) an vier Stellen (`claude_service.py:693/696/703/707`).
→ **Die PiP-Autovarianten-Kosten werden seit jeher still verworfen.** Gehört in R1 (Fable hat die
Namensvarianten-Pflicht benannt, den konkreten Prod-Beleg aber nicht gehabt).

**F-2 — Fables Aussage „für Kurzname `haiku-4-5` fehlen die Cache-Rates ganz" ist an Prod FALSCH.**
Sie existieren (id 13/14), sind nur **preislich zu niedrig**: `cache_read` 0.00008 (soll 0.0001),
`cache_write` 0.001 (soll 0.00125). → R1 wird zur **Preis-Korrektur**, nicht zur Neuanlage. Ändert nichts
am Ergebnis, aber am Bau-Schritt (UPDATE-Pfad statt INSERT-Pfad).

**F-3 — `uix_api_rate_active` = UNIQUE(provider, model, unit_type, **active**) begrenzt das
„alte Rate deaktivieren + neue einfügen"-Muster auf GENAU EINE Korrektur pro Tripel.**
Heute gibt es 0 inaktive Zeilen → die KOSTEN-1-Korrektur geht sauber durch. Die **zweite** Preisänderung
desselben Tripels würde am UNIQUE scheitern. → **Kein Umbau in dieser Phase** (Discipline), aber
Backlog-Eintrag `APIRATE-HISTORY-UNIQUE` + Kommentar am Constraint. Betrifft auch die bestehende
Admin-Route `api_rate_new_price` (`admin_dashboard.py:393-442`) — die hat denselben Deckel.

**F-4 — Das „alte Rate deaktivieren + PriceChangeLog schreiben"-Muster EXISTIERT bereits im Haus.**
`routes/admin_dashboard.py:393-442` (`api_rate_new_price`) macht exakt das: `old.active = False` →
neue `ApiRate` → `PriceChangeLog(api_rate_id, old_rate, new_rate, currency, impact_eur_per_month, note)`.
→ R1 **spiegelt dieses Muster**, erfindet nichts. Und: die Haiku-Preis-Korrektur bekommt einen
`PriceChangeLog`-Eintrag (Tabelle existiert, FK existiert, 1 INSERT) — das ist die **hauseigene**
Historie-Spur zusätzlich zum Dashboard-Marker aus R5. **Zur Cross-AI-Freigabe markiert**, weil in Fables
Plan nicht erwähnt; Default = JA (Null-Risiko, existierende Tabelle, Finanzamt-Linie D-02 bleibt gewahrt:
alte `api_cost_log`-Zeilen werden NICHT angefasst).

**F-5 — Seed A ist heute schon tot (bestätigt), aber seine 8 Zeilen LEBEN auf Prod** (id 1-8, aus dem
Ur-Seed 2026-04-09). Das heißt: die Konsolidierung „Seed A ins per-Tripel-Muster mergen" ist reine
Code-Hygiene mit **Null Daten-Effekt** auf Prod — sie schützt nur die Zukunft. Wichtig fürs Risiko-Bild.

---

## 4. Gelockte Entscheidungen (nicht neu aufmachen)

1. **Stripe-Fees (R2.8): JETZT mit bauen.**
2. **Alte `api_cost_log`-Zeilen NICHT rückwirkend korrigieren** — nur Historie-Marker (D-02).
3. **W3 (Laufzeit-Skip-Zähler + Founder-Alert) = PFLICHT-Kernwächter** (Gemini: grep sieht
   ENV-basierte Modellnamen nicht).
4. **Wellen-Reihenfolge:** W1 → W2 → W3 (W1 zuerst = reine Daten-Änderung, stoppt sofort das größte Leck).

## 5. Offene André-Entscheidungen (blockieren Plan 01 Task 1)

| # | Frage | Empfehlung (Claudian) |
|---|---|---|
| a | Deepgram-Plan: PAYG **$0.0077**/min oder Growth **$0.0065**? | **0.0077** (PAYG) — wir sind auf keinem Growth-Vertrag; lieber zu vorsichtig als zu optimistisch |
| b | ElevenLabs: API-Tarif **$0.10**/1k Zeichen oder effektiver Abo-Preis? | **0.10** setzen, bis die Rechnung was anderes sagt (heute 0.30 = 3× zu HOCH) |
| c | Brave: Rate jetzt anlegen oder nur W2-Allowlist bis Paid-Plan? | **Rate mit 0.0 anlegen** + Kommentar — dann ist der Hook aus R2.7 nicht tot und W1 bleibt grün |
| d | Nova-2 (Training-Prerecorded): auf **0.0058** heben? | Ja — Seed-Wert 0.0036 ist veraltet |

**Rest-Prüfpunkte (Gemini, klein, VOR Launch):** Deepgram-Diarization im Minuten-Preis enthalten?
Stripe-Fee-Typen vollständig (Transaktion + ggf. Radar/Payout)? Hosting (Hetzner/PG/Storage) ist ein
**fixer** Block → NICHT Teil dieses API-Trackings (gehört in die Gesamt-Unit-Economics).

## 6. Abgrenzung

- **nerve_rt-Verdrahtung (R3) wird HIER EINMAL gebaut.** METER R6 setzt später auf **denselben**
  akkumulierten Sekunden auf — die Akkumulations-Variable gehört diesem Block, der Zähler-Sync gehört METER.
- **Stripe-Fee-Hook berührt `routes/payments.py`** → METER/AUTH-3 fassen dieselbe Datei später an.
  Naht bekannt; hier **nur** `_record_revenue` (:248) + sein Webhook-Aufrufer (:98).
