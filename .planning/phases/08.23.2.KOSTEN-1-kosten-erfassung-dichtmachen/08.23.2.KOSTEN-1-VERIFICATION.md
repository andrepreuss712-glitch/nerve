# 08.23.2.KOSTEN-1 — VERIFICATION

**Status: LIVE, ein Live-Beleg offen. NICHT complete.**
Deploy: 2026-07-20, Claudian, Zwei-Tore-Netz, `git_head 50bf8af`. `nerve` + `nerve-rt` neu gestartet.

---

## 1. Belegt (Sicht, keine Zusicherung)

| # | Behauptung | Beleg | Quelle |
|---|---|---|---|
| 1 | Pytest-Gate vollständig grün gegen echtes Postgres (`nerve_test`) | Tor 2 bestanden, sonst kein Restart | Claudian, Deploy-Gate |
| 2 | W1-Wächter war gegen den ungefixten Stand ROT | ERST-ROT-Lauf vor R1, Commit `4e55b06` | Plan 01 Task 1 |
| 3 | W2-Wächter beide Stufen ERST-ROT | Stufe 1: judge/adoption/outcome/training + 2 nerve_rt-Adapter · Stufe 2: `validate_user_text`, `_brave_search`, `_record_revenue`, `training_transcribe` | Commit `285b948` |
| 4 | Preise live auf Prod | Raten-Tabelle nach Restart geprüft | Claudian |
| 5 | **Kosten buchen tatsächlich** | `deepgram/nova-3` = **0,006734 €/Min**, direkt am Prod-Prozess belegt | Claudian |
| 6 | Skip-Zähler leer | W3-Zähler = 0 nach Restart | Claudian |
| 7 | Keine Fehlerzeilen | keine `[CostTracker] no active ApiRate`, kein nerve_rt-Import-Fehler | Claudian |
| 8 | Statische Wächter grün | W2 (4 Tests), STT-Parity, Skip-Zähler-Logik, `py_compile` aller geänderten Dateien | lokal, vor Deploy |

**Zu Nr. 5:** 0,0077 USD × FX ≈ 0,006734 € — die Rate greift, der FX-Pfad greift, die Zeile wird
geschrieben. Das ist der Beweis, dass das Hauptloch zu ist.

## 2. Offen — Live-Beleg durch André (Test-Anruf heute Abend)

Drei Punkte, die nur ein echter Anruf zeigen kann. **Bis dahin bleibt die Phase offen.**

1. **`inspect.sh sample api_cost_log 30`** nach dem Anruf →
   - eine `deepgram/nova-3`-Zeile mit `cost_eur > 0` (bei Meeting-Modus: `nova-3-diarize`)
   - Zeilen für Judge (`context_tag='judge'`), Adoption (`'adoption'`), Outcome (`'outcome'`)
     — das sind die Welle-2-Hooks im Nachlauf
2. **Founder-Dashboard → Kachel „Kosten-Log-Skips" = 0.**
   Steht dort etwas anderes, ist die Phase **nicht** fertig — dann zeigt der Wächter das nächste
   Loch, und das ist sein Erfolg, kein Grund zum Abhaken. Die Kachel nennt das verletzende Tripel.
3. **Historie-Badge** erscheint im Kunden-Tab bei Zeiträumen **vor** dem 20.07. und **nicht** danach
   (Bedingung ist `start <` Stichtag — der Übergangs-Monat Juli zeigt das Badge also noch).

## 3. Nicht belegbar in dieser Phase (bewusst)

- **nerve_rt-Live-Beleg** (`stt_rt`-Zeile + `anthropic/claude-haiku-4-5-20251001` aus dem
  FastAPI-Prozess): der Pfad ist noch nicht produktiv. Code ist deployed und der Prozess läuft mit
  neuem Stand; der Beleg kommt, wenn nerve_rt live geht. **Bis dahin ist R3 gebaut, aber ungeprüft
  im Echtbetrieb** — ehrlich als solches zu führen.
- **Stripe-Fee-Zeile** (`provider='stripe'`): braucht eine echte Zahlung, also Stripe-Testmodus aus
  AUTH-3/W0. Der Hook ist gebaut, der Beleg gehört zu AUTH-3.

## 4. Requirements-Abdeckung

| Req | Status | Anmerkung |
|---|---|---|
| K1-W1 Raten-Wächter | ✅ live | ERST-ROT belegt, grün |
| K1-R1 / R1b / R1c Raten + Seed | ✅ live | Nr. 4 + 5 oben |
| K1-W2 Hook-Wächter | ✅ live | inkl. nachgezogener Stufe 2 |
| K1-R2.1–R2.7 sieben Hooks | ⚠ live, Anruf-Beleg offen | Judge/Adoption/Outcome via §2 Nr. 1 |
| K1-R2.8 Stripe-Fees | ⚠ gebaut, Beleg in AUTH-3 | braucht echte Zahlung |
| K1-R3.1 / R3.2 nerve_rt | ⚠ gebaut, Echtbetrieb ausstehend | §3 |
| K1-W3 Laufzeit-Skip-Zähler | ✅ live | Nr. 6, Dashboard-Kachel §2 Nr. 2 |
| K1-R5 Historie-Marker | ⚠ live, Sicht-Prüfung offen | §2 Nr. 3 |
| K1-P3 Prozess-Regel | ✅ | CLAUDE.md Punkt 30 |

## 5. Folgearbeit (angelegt, nicht offen gelassen)

- **Backlog 999.9 `APIRATE-HISTORY-UNIQUE`** — das Preis-Historie-Kontingent ist nach dieser
  Korrektur aufgebraucht; die nächste Preisrunde desselben Tripels läuft in den UNIQUE.
  **Vor der zweiten Preisrunde fällig.**
- **AUTH-3:** echte Stripe-Gebühr via `BalanceTransaction` statt Modell (André-Vorgabe 2026-07-20)
  · Migration `skip_billing` wird **0035** (0034 ist vergeben).
- **Launch-Liste (Claudian):** `language="de"` hart verdrahtet — Konflikt mit US-first.
- **METER R6** setzt auf `conn._nerve_stt_seconds` auf. Die Variable gehört KOSTEN-1, der
  Zähler-Sync gehört METER. **Punkt-26-Hinweis für METER:** der Wert ist erst bei *Session-Ende*
  vollständig, nicht währenddessen.

## 6. Nach dem Test-Anruf

Wenn §2 Punkte 1-3 belegt sind: ROADMAP + STATE auf **COMPLETE**, Log-Eintrag im Vault.
Schlägt einer fehl, ist der Befund die nächste Arbeit — nicht das Abhaken.
