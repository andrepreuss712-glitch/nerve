---
status: complete
phase: 08.19.5-per-user-daten-trennung-websocket-auth
source: 08.19.5-01-SUMMARY.md, 08.19.5-02-SUMMARY.md, 08.19.5-03-SUMMARY.md, 08.19.5-04-SUMMARY.md
started: 2026-05-02T18:00:00Z
updated: 2026-05-03T09:20:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running server. Start the app fresh (python app.py). In the startup output you should see two new migration lines: "[DB] Migration 08.19.5: ft_objection_events + ft_qa_events dropped" and "[DB] Migration: added conversation_logs.anrede". App boots without errors, login page loads.
result: pass

### 2. Profile Switch During Live Session — No Crash
expected: Start a live session (go to /live, start recording). While the session is running, call /api/set_profile (either via the profile selector in the UI or curl). The request should return a 200 response and not produce a TypeError/500 in the server logs. No "load_learning_cards() missing 1 required positional argument" error in console.
result: pass
note: "UI path not directly testable (unrelated UX bug: PiP closes on tab switch, to be tracked in Block J). Crash path verified via commit cbb3842 — load_learning_cards call removed with explanatory comment."

### 3. Session Pause Isolation (Two Tabs)
expected: Open /live in two browser tabs (or two browser windows logged in as two users). Start a session in Tab 1 and Tab 2. Pause Tab 1. Tab 2 should continue receiving analysis results — it should NOT be paused. Unpausing Tab 1 should only affect Tab 1.
result: pass
note: "Verified with workaround: two different browsers needed due to browser API limit (one PiP window per browser profile). Chrome Tab 1 paused, second browser Tab 2 continued. Per-SID isolation confirmed."

### 4. Session-Rating Endpoint Works
expected: After ending a call (api_beenden), the session rating save flow completes without error. Check server logs — no "column anrede does not exist" or OperationalError. The ConversationLog record is saved correctly. (The /api/session-rating endpoint, formerly /api/feedback in app_routes, handles session star ratings.)
result: pass
note: "4x POST /api/beenden logged, all 200, no OperationalError. 'session_anrede=Sie set from PreCall' visible — anrede column correctly written. WR-03 fix confirmed. Session-rating UI path blocked by unrelated pre-launch bug (dashboard sessions not clickable, tracked in Block J Mini-Adds). Route rename /api/feedback→/api/session-rating verified via code review."

### 5. Unauthenticated WebSocket Rejected
expected: An unauthenticated WebSocket connection attempt (no valid Flask session cookie) is rejected. In server logs: "[WS-Auth] Rejected unauthenticated connection". Authenticated connections connect normally with "[WS-Auth] Authenticated: user_id=X".
result: pass
note: "8 authenticated connect logs in last 30min: [WS-Auth] Authenticated connect: sid=... user_id=2. Reject path verified via code review: deepgram_service.py:312-325 handle_connect checks flask_session.get('user_id'), logs Unauthorized rejected + return False on None. REQ-03 confirmed."

### 6. EWB Manual Variant — Error Surfaces Properly
expected: If a manual EWB (Einwand-Behandlung) variant is triggered and profile context fails to build, the error is returned in the response as {"error": "profile_context failed: ...", "gegenargument_1": null} rather than an empty/silent fallback. This is visible in server logs with [EWB] tag. (Requires a misconfigured profile or deliberate test — skip if not easily reproducible.)
result: pass
note: "Verified via code review: claude_service.py:743-745 has try/except with [EWB] profile_context error log + return {'error': 'profile_context failed: ...', 'gegenargument_1': None}. No silent fallback. REQ-08 confirmed."

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
