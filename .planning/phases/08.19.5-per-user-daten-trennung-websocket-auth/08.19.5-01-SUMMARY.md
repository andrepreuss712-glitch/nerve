---
phase: "08.19.5"
plan: "01"
subsystem: "dead-code-cleanup"
tags: ["cleanup", "route-rename", "error-propagation", "db-migration"]
dependency_graph:
  requires: []
  provides: ["clean-route-table", "ft-tables-dropped", "ewb-error-propagation"]
  affects: ["routes/app_routes.py", "routes/admin_ewb.py", "database/models.py", "app.py", "services/claude_service.py"]
tech_stack:
  added: []
  patterns: ["idempotent DROP TABLE IF EXISTS migration", "error-propagating return dict"]
key_files:
  created: []
  modified:
    - routes/admin_ewb.py
    - database/models.py
    - app.py
    - routes/app_routes.py
    - routes/CLAUDE.md
    - services/claude_service.py
    - tests/test_ab_stats.py
decisions:
  - "CASE A: No FE caller found for /api/feedback session-rating at execution time (HIGH-3 pre-check confirmed)"
  - "FtObjectionEvent + FtQaEvent model classes removed — no writer ever existed, tables always empty"
  - "streame_manual_ewb_variante() now propagates build_profile_context errors as {'error': ..., 'gegenargument_1': None}"
metrics:
  duration: "~20 minutes"
  completed: "2026-05-02"
  tasks_completed: 3
  files_modified: 7
---

# Phase 08.19.5 Plan 01: Dead Code Cleanup + Route Conflict Resolution + Error Propagation Summary

Wave 1 dead code cleanup — ft_objection_events/ft_qa_events removed, /api/feedback renamed to /api/session-rating, streame_manual_ewb_variante() error propagation fixed.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Remove ft_objection_events reader and drop both dead FT tables | b630cd7 | routes/admin_ewb.py, database/models.py, app.py |
| 2 | Rename /api/feedback to /api/session-rating (D-02, REQ-04) | 7f78f41 | routes/app_routes.py, routes/CLAUDE.md |
| 3 | Fix streame_manual_ewb_variante() error propagation (REQ-08) | 31be646 | services/claude_service.py |

## What Was Built

- **Task 1:** Replaced raw SQL `ft_objection_events` query in `admin_ewb.py` with `ab_rows = []`. Removed `FtObjectionEvent` and `FtQaEvent` model classes from `database/models.py`. Added idempotent `DROP TABLE IF EXISTS ft_objection_events` + `DROP TABLE IF EXISTS ft_qa_events` migration block to `app.py` (confirmed running: `[DB] Migration 08.19.5: ft_objection_events + ft_qa_events dropped`).
- **Task 2:** Ran mandatory HIGH-3 pre-check grep — only hit was `static/feedback.js` (bug-report widget with `context_url` payload, not `stars/comment/session_log_id`). CASE A confirmed. Route decorator changed from `/api/feedback` to `/api/session-rating`, function renamed `api_session_rating()`. `routes/CLAUDE.md` updated with route change note. `feedback.py /api/feedback` remains untouched.
- **Task 3:** Replaced silent fallback in `streame_manual_ewb_variante()` except clause with `return {'error': f'profile_context failed: {_bpc_e}', 'gegenargument_1': None}`. Log tag changed `[Manual-EWB]` → `[EWB]`. Empty-string fallback path unchanged.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed test_ab_stats.py importing deleted FtObjectionEvent model**
- **Found during:** Post-task test run after Task 3
- **Issue:** `tests/test_ab_stats.py` imported `FtObjectionEvent` (now deleted) causing `ImportError` on collection, blocking all tests
- **Fix:** Removed `FtObjectionEvent` from imports; removed `test_ab_stats_join_success_rate` and `test_ab_stats_filters_null_success` (both tested dead SQL on the dropped `ft_objection_events` table — no writer ever existed)
- **Files modified:** `tests/test_ab_stats.py`
- **Commit:** 73d053f

## Verification Results

```
# HIGH-3 pre-check
grep -n "HIGH-3 pre-check" routes/app_routes.py → 1 hit (CASE A documented)

# REQ-04: Only one POST /api/feedback active (feedback.py)
grep -rn "route.*api/feedback" routes/ → only routes/feedback.py (1 hit)

# REQ-04: New route resolvable
url_for('app_routes.api_session_rating') → /api/session-rating ✓
url_for('feedback.api_feedback') → /api/feedback ✓

# REQ-05: No dead table references in services/ routes/ templates/
grep -rn "FtObjectionEvent|FtQaEvent" services/ routes/ templates/ → 0 hits ✓

# REQ-08: Error propagation
grep -n "EWB.*profile_context error" services/claude_service.py → 1 hit ✓
grep -n "gegenargument_1.*None" services/claude_service.py → 1 hit ✓

# Tests
test_ab_stats.py: 7 passed ✓
test_08_20_3.py: 28 passed ✓
```

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced beyond those already in the plan's threat model.

## Self-Check: PASSED

- routes/admin_ewb.py: ab_rows = [] present ✓
- database/models.py: FtObjectionEvent + FtQaEvent removed ✓
- app.py: DROP TABLE IF EXISTS ft_objection_events + ft_qa_events present ✓
- routes/app_routes.py: /api/session-rating + api_session_rating() present ✓
- services/claude_service.py: error propagation return dict present ✓
- Commits: b630cd7, 7f78f41, 31be646, 73d053f all exist ✓
