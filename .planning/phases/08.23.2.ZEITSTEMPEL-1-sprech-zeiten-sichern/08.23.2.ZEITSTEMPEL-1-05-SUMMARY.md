---
phase: 08.23.2.ZEITSTEMPEL-1
plan: 05
subsystem: call-finalisierung
tags: [transcript-segments, sprech-zeiten, reine-transform, insert, d-02, d-04, d-08]
requires:
  - "08.23.2.ZEITSTEMPEL-1-03 (drei nullable Spalten start_ms/end_ms/word_count + Migration 0039, NICHT gefahren)"
  - "08.23.2.ZEITSTEMPEL-1-04 (RAM-Log-Vertrag: _entry['start_ms'/'end_ms'/'word_count'], inkl. Weg-C-Platzhalter-Zeile)"
provides:
  - "routes/app_routes.py::_transcript_entries_to_segments — gibt jetzt SECHS Schluessel zurueck (ts_ms, speaker, text, start_ms, end_ms, word_count)"
  - "routes/app_routes.py::api_beenden — INSERT setzt start_ms/end_ms/word_count je Zeile"
  - "Logzeile [ZEITSTEMPEL-1] ... added=N mit_sprechzeiten=M als Wirkungs-Anker fuer Plan 06"
affects:
  - "08.23.2.ZEITSTEMPEL-1-06 (GRUEN-Tor: 3 FAILED -> 0; danach Prod-Migration 0039 VOR Deploy, echter Test-Anruf)"
  - "public.transcript_segments (neue Zeilen tragen drei zusaetzliche Werte; Bestandszeilen unangetastet)"
tech-stack:
  added: []
  patterns:
    - "Reines Durchreichen statt Rechnen — die einzige Umwandlung bleibt die eine reine Transform (D-08)"
    - "Fehlender Schluessel -> None, nie 0 (D-04): _entry.get(k) ohne Default-Argument"
    - "Zaehlwert statt Zeichenketten-Anker im Log (mit_sprechzeiten=N)"
key-files:
  created: []
  modified:
    - "routes/app_routes.py (+19/-2, einzige geaenderte Datei)"
decisions:
  - "Drei Schluessel, nicht vier: seam_before ist mit dem Versatz zusammen gestrichen (D-05/D-06a) und hat in dieser Datei 0 Treffer"
  - "Kein zweiter Umwandlungs-Pfad: die Werte kommen fertig aus dem RAM-Log (Plan 04) und werden hier nur weitergereicht — keine Arithmetik in routes/"
  - "ts_ms-Achse unangetastet: base/running/Klemmung und beide continue-Pfade unveraendert (D-02)"
  - "Der Reentrance-Guard, das if _segs: commit und das except+rollback bleiben Zeile fuer Zeile stehen — ein Fehler im Schreiber darf die Call-Finalisierung weiterhin NIE abbrechen (T-ZS1-15/16)"
metrics:
  duration: "~20 min"
  completed: "2026-08-10"
  tasks: 2
  commits: 2
  files_changed: 1
---

# Phase 08.23.2.ZEITSTEMPEL-1 Plan 05: Transform + INSERT — Summary

Die letzte Etappe der Naht: `start_ms`/`end_ms`/`word_count` wandern aus dem RAM-Log durch die
reine Transform in den `transcript_segments`-INSERT — reines Durchreichen, kein Rechnen, kein
Default.

## Was gebaut wurde

| Commit | Task | Inhalt |
|---|---|---|
| `891b291` | Task 1 | `out.append({...})` um drei Schluessel erweitert (`_entry.get(...)` ohne Default) + Docstring auf sechs Schluessel nachgezogen |
| `403be5a` | Task 2 | drei Kwargs im `db_conv.add(TranscriptSegment(...))` + Logzeile `[ZEITSTEMPEL-1] ... mit_sprechzeiten=N` |

**Genau eine Datei geaendert, additiv:**

```
$ git diff --stat HEAD~2..HEAD
 routes/app_routes.py | 21 +++++++++++++++++++--
 1 file changed, 19 insertions(+), 2 deletions(-)
```

Die zwei geloeschten Zeilen sind die in-place ersetzte Docstring-Kopfzeile und die alte
`[D.UX.1] ... added=`-Logzeile. Keine Datei geloescht, keine umbenannt
(`git diff --diff-filter=DR --name-only HEAD~2..HEAD` = leer).

## Kein Test wurde angefasst (geforderter Beleg)

```
$ git diff --name-only HEAD~2..HEAD -- tests/
(leer)
```

Die Implementierung wurde an den Vertrag angepasst, nie umgekehrt.
`git diff --stat HEAD~2..HEAD` nennt `routes/app_routes.py` als **einzige** Datei —
`tests/`, `database/models.py`, `alembic/`, `services/deepgram_service.py` und
`services/live_session.py` sind unberuehrt.

> **Eine ehrliche Randnotiz zum Anker „`git diff --stat` nennt genau eine Datei":** im
> Arbeitsverzeichnis lag bei Plan-Beginn eine **fremde, unkommittierte** Aenderung an
> `.claude/settings.local.json` (Agenten-Werkzeug-Konfiguration, nicht von diesem Plan
> verursacht — sie stand schon vor dem ersten Edit in `git status`). Sie wurde **nicht**
> gestaged und ist in **keinem** der beiden Commits enthalten. Der Anker ist damit auf der
> Commit-Ebene erfuellt (`git diff --stat HEAD~2..HEAD` = 1 Datei); auf der
> Arbeitsverzeichnis-Ebene waere er ohne diese Erklaerung falsch-rot gewesen.

## ROT-Zuordnung — die drei verbliebenen Assertions und die Aenderung, die sie erfuellt

Nach Plan 04 waren aus dem gueltigen ROT-Lauf (16 FAILED) noch **genau drei** offen, alle in
`tests/test_transcript_segments_write.py`:

| Test (ROT-LAUF.md Zeile) | Assertion | Fehlergrund im ROT-Lauf | Aenderung, die sie erfuellt |
|---|---|---|---|
| `test_transform_reicht_sprechzeiten_durch` (`:66`) | `segs[0]['start_ms'] == 1200`, `['end_ms'] == 4300`, `['word_count'] == 7` | `KeyError: 'start_ms'` | **Task 1**: `'start_ms': _entry.get('start_ms')` (+ `end_ms`, `word_count`) im `out.append`-Dict — die Werte kommen 1:1 aus dem Entry, ohne Umrechnung |
| `test_knopfzeile_ohne_wortzeiten_wird_null` (`:80`) | dreimal `is None` | `KeyError: 'start_ms'` | **Task 1**: `_entry.get(k)` **ohne** Default-Argument → fehlender Schluessel liefert `None`. Der Abwesenheits-Anker `_entry.get('start_ms', 0)` = **0** belegt, dass kein `0`-Default eingeschmuggelt wurde (D-04) |
| `test_platzhalterzeile_behaelt_ihre_sprechzeiten` (`:97`) | `segs[0]['end_ms'] - segs[0]['start_ms'] == 30000`, `text == '[nicht gespeichert]'`, `word_count == 61` | `KeyError: 'end_ms'` | **Task 1**: die Weg-C-Zeile ist **nicht leer** und faellt darum nicht durch `if not _txt: continue` (`:58`, unveraendert, Anker = **1**); ihre Zeiten werden wie bei jeder anderen Zeile durchgereicht — es gibt **keinen** Sonderfall-Zweig fuer den Platzhalter |

**Der vierte Test der Datei** (`test_ts_ms_bleibt_unberuehrt_von_den_neuen_spalten`) war im
ROT-Lauf bereits gruen und ist der **Gegenpol**: er beweist, dass die Deepgram-Zeiten die
`ts_ms`-Arithmetik nicht anfassen. Er bleibt gruen, weil `base`, `running`, die Klemmung
`:65-67` und `'ts_ms': _rel` unveraendert stehen (Anker `running = _rel` = 1, `'ts_ms': _rel,`
= 1, `'ts_ms': _entry` = **0** — die beiden Achsen wurden nicht vermischt).

**Soll fuer das GRUEN-Tor in Plan 06: 3 FAILED → 0 FAILED.** Der Wirkungs-Beweis ist der
serverseitige Lauf, nicht diese Tabelle — es wurde **kein** lokaler pytest gefahren
(HART-Regel „Kein Local-Dev"; auch kein `python -c`-Rauchtest).

**Task 2 hat keine der drei Assertions bedient** — die Unit-Tests rufen die reine Funktion
ohne DB auf. Task 2 ist die Halbstrecke, die erst am echten Anruf sichtbar wird
(`inspect.sh sample transcript_segments 40` + `mit_sprechzeiten=N` in `inspect.sh logs`,
beides Plan 06).

## Ausgefuehrte Abnahme-Anker (alle, mit gemessener Zahl)

**Vor dem Edit** (Verbindungs-Karte §3 + Abwesenheits-Pruefung mit Existenz-Anker, Bau-Regel 20):

| Anker | Soll | Ist |
|---|---|---|
| `grep -rn "TranscriptSegment" nerve_rt/ \| wc -l` | 0 | **0** |
| gepaarter Existenz-Anker `TranscriptSegment` in `routes/ services/ database/` | 37 | **37** |
| Code-Form-Muster `add_column(.*seam_before\|seam_before *=\|\.seam_before\|_dg_ms_offset\|mark_dg_seam` | 0 | **0** |
| Existenz-Anker `grep -c "WARUM DREI UND NICHT VIER" alembic/versions/0039_...py` | 1 | **1** |
| `_transcript_entries_to_segments` — Definition / Produktions-Aufrufer | `:45` / `:548` | **`:45` / `:548`** (dazu Kommentar `:19` und der Test-Import) |
| `grep -c "_entry.get(" routes/app_routes.py` (Vorstand) | 4 | **4** |
| `grep -c "_existing_seg" routes/app_routes.py` (Vorstand) | 2 | **2** |

Task 1:

| Anker | Soll | Ist |
|---|---|---|
| `_entry.get('start_ms')\|_entry.get('end_ms')\|_entry.get('word_count')` | 3 | **3** |
| Abwesenheit Default (`_entry.get('start_ms', 0)\|...`) | 0 | **0** |
| gepaarter Existenz-Anker `_entry.get(` | 7 | **7** |
| `running = _rel` | 1 | **1** |
| `'ts_ms': _rel,` | 1 | **1** |
| gepaarter Abwesenheits-Anker `'ts_ms': _entry` | 0 | **0** |
| `seam_before` | 0 | **0** |
| gepaarter Existenz-Anker `word_count` | >= 1 | **2** |
| `if not _txt:` (Leer-Text-Weiche steht noch) | 1 | **1** |

Task 2:

| Anker | Soll | Ist |
|---|---|---|
| `start_ms=_s['start_ms']\|end_ms=_s['end_ms']\|word_count=_s['word_count']` | 3 | **3** |
| `mit_sprechzeiten` | 1 | **1** |
| `git diff \| grep -c "^-.*_existing_seg\|^-.*db_conv.rollback\|^-.*db_conv.commit"` | 0 | **0** |
| gepaarter Existenz-Anker `_existing_seg` | 2 | **2** |
| Abwesenheit Default (`_s['start_ms'] or 0\|_s['word_count'] or 0`) | 0 | **0** |
| gepaarter Existenz-Anker `db_conv.add(TranscriptSegment(` | 1 | **1** |
| `git diff --stat` auf Commit-Ebene | 1 Datei | **1 Datei** (Randnotiz oben) |
| `git diff --name-only -- tests/` | leer | **leer** |
| `git diff HEAD~1 HEAD --diff-filter=D` | leer | **leer** |

**Alle Anker beim ersten Lauf getroffen — keine Abweichung, keine Ersatz-Formulierung.**

## Was ausdruecklich unveraendert geblieben ist (nachgelesen, nicht angenommen)

Der Schreiber-Block nach dem Edit (`routes/app_routes.py:551-583`), Zeile fuer Zeile geprueft:

- `:553-556` Reentrance-Guard `_existing_seg is None` — unveraendert (T-ZS1-16: ein zweiter
  `api_beenden`-Durchlauf schreibt weiterhin gar nicht)
- `:571-572` `if _segs: db_conv.commit()` — unveraendert, kein leerer Commit
- `:576-577` der `else`-Zweig mit der `[D.UX.1] ... skipped (idempotent)`-Zeile — unveraendert
- `:578-583` `except Exception as _seg_err:` → `print` → `try: db_conv.rollback() except: pass`
  — unveraendert (T-ZS1-15 **und** die DB-Regel „nie stiller except ohne rollback"). **Die
  Invariante „ein Fehler im Schreiber bricht die Call-Finalisierung NIE ab" ueberlebt diese
  Aenderung**, weil die drei neuen Kwargs vollstaendig **innerhalb** des bestehenden `try`
  liegen und der Zaehl-Ausdruck `_mit_zeiten` ebenfalls dort steht.
- der DA-06-Kommentarblock darunter — unveraendert

`ts_ms` wurde von dieser Aenderung nicht beruehrt: kein neuer Wert, keine neue Quelle, keine
Arithmetik daran.

## Punkt 26 — kein Anker auf `created_at`

Diese Aenderung fuegt **keine** Logik hinzu, die auf `created_at` ankert. Der Batch-Write am
Call-Ende bleibt wie er ist (alle Zeilen einer `conversation_log_id` tragen identisches
`created_at`, im Plan §5 an Prod-Daten belegt). Der einzige Bestandsleser, der auf `created_at`
ankert (`services/slow_lane.py:205-209`), ist von dieser Phase **nicht** angefasst; die drei
neuen Spalten sind fuer seinen Filter unsichtbar.

## Punkt 23 — Schild-Gegenpruefung (Ergebnis: keine Nachziehung noetig)

Punkt 23 verlangt die Schild-Nachziehung im **selben** Commit, wenn sich der Lese-/Schreib-Pfad
einer Tabelle aendert. Hier aendert sich der Schreib-Pfad von `transcript_segments` insofern,
als derselbe INSERT drei Werte mehr setzt. Das Schild aus Plan 03 (`database/models.py:967-970`)
benennt bereits:

| Aussage im Schild | von diesem Plan gebaut? |
|---|---|
| Quelle „RAM-Log aus `services/deepgram_service.py`" ueber die Transform in `routes/app_routes.py` | ✅ genau dieser Weg, ein einziger Aufrufer |
| „NULL = unbekannt, nie 0" | ✅ `_entry.get(k)` ohne Default → `None` → nullable Spalte |
| Weg-C-Platzhalter `[nicht gespeichert]` mit echten Zeiten | ✅ kommt ohne Sonderfall durch die Transform |

**Ergebnis: das Schild deckt den Pfad korrekt und vollstaendig ab; `database/models.py` wurde
nicht angefasst** (waere auch ausserhalb des Plan-Scopes gewesen — der Plan besitzt genau eine
Datei). Es gibt keine Schild-Schuld aus diesem Plan.

## Punkt 31 — kein neuer Waechter, aber die Grenzen dieser Etappe

Dieser Plan baut und erweitert **keinen** Waechter, es entsteht also keine `RESTLUECKEN`-Pflicht
im Code. Was diese Durchreichung strukturell **nicht** leistet, gehoert trotzdem hierhin:

1. **Sie prueft nichts.** Kommt aus dem RAM-Log Unsinn (negative Zeiten, `end_ms < start_ms`,
   ueberlappende Endergebnisse — T-ZS1-31, in Plan 04 als **UNVERIFIED** benannt), wandert der
   Unsinn unveraendert in die DB. Das ist gewollt: geklemmt wird erst, wenn jemand das Ausmass
   an echten Daten kennt (Plan 06 Task 2, Klemm-Regel gehoert nach METRIK-1).
2. **Sie sieht die Migration nicht.** Steht Schema `0038` und laeuft schon neuer Code, faengt
   `:578 except` den `UndefinedColumn` **still** und die Segmente **eines** Anrufs gehen
   verloren — ohne dass irgendwo etwas rot wird. Genau deshalb ist die Reihenfolge in Plan 06
   umgedreht und verbindlich: **erst `alembic upgrade head` auf Production, dann
   `bash deploy.sh production`** (D-16/T-ZS1-18). Der Alt-Code ist vorwaerts-kompatibel, damit
   gibt es **gar kein** Fenster statt eines kurzen.
3. **Die Logzeile beweist nur die Zaehlung, nicht die Werte.** `mit_sprechzeiten=N` zaehlt
   Zeilen mit `start_ms is not None`. Ob die Zahlen **plausibel** sind (`end_ms > start_ms`,
   `word_count > 0`), zeigt erst `inspect.sh sample transcript_segments 40` am echten Anruf.
4. **Cold-Call-Grenze (D-16) unveraendert:** `log_sp` ist im Cold-Call hart `0`, der Redeanteil
   dort IMMER exakt 100 %. Diese Phase aendert daran nichts; steht bereits im Tabellen-Schild.

**Zweite Schicht darunter:** das serverseitige Deploy-Tor in Plan 06 (3 FAILED → 0) und danach
der echte Test-Anruf mit `inspect.sh sample` + `inspect.sh logs`.

## DSGVO-Bestaetigung (Pflicht-Satz, woertlich)

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**

Fuer diesen Plan konkret: der Schreiber legt ausschliesslich **neue** Zeilen an — es gibt
weiterhin **kein** `DELETE` und **kein** `UPDATE` auf `transcript_segments` oder
`conversation_logs`. Es wandern drei **Zahlen** zusaetzlich in die DB; der einzige Text bleibt
das unveraenderte `text`-Feld (anonymisiert wie bisher, oder der inhaltslose Weg-C-Platzhalter
ohne Kategorie). Kein neuer Endpunkt, keine neue Rolle, keine Aenderung an Auth oder RLS
(T-ZS1-17/19).

## Latenz (Punkt 25)

Der Pfad laeuft **einmal am Call-Ende** in `api_beenden`, nicht im Live-Pfad. Hinzu kommen drei
`dict.get`-Aufrufe je Segment in der Transform und ein `sum(...)` ueber die Segmentliste (bei
einem 15-Minuten-Anruf Groessenordnung 100-300 Elemente). Kein zusaetzlicher Riegel, kein
zusaetzlicher DB-Roundtrip — es ist derselbe INSERT mit drei Spalten mehr. **Auf der spuerbaren
Antwort-Latenz im Gespraech: null.**

## Context7 (Pflicht bei fremden Bibliotheken)

**Nicht gezogen — und das ist die richtige Entscheidung, nicht eine ausgelassene Pflicht.**
Dieser Plan beruehrt keine fremde Bibliotheks-Schnittstelle: die Deepgram-Ergebnis-Form wurde
in **Plan 04** angefasst (dort wurde `/websites/developers_deepgram` gezogen), und der INSERT
nutzt ausschliesslich das hauseigene SQLAlchemy-Muster `db_conv.add(TranscriptSegment(...))`,
das an dieser Stelle seit D.UX.1 unveraendert steht — es kommen drei Kwargs auf ein bestehendes
ORM-Modell dazu, keine neue SQLAlchemy-API.

## Abweichungen vom Plan

**Keine.** Der Plan wurde exakt wie geschrieben ausgefuehrt, beide Code-Bloecke wortgleich zur
Plan-Vorlage. Keine Deviation-Regel 1-4 kam zum Einsatz, kein Auto-Fix, kein Checkpoint. Die
einzige Ergaenzung gegenueber dem Plan-Text ist die **Randnotiz** oben zum
`git diff --stat`-Anker (fremde unkommittierte Datei im Arbeitsverzeichnis) — sie ist benannt
statt stillschweigend weggelassen (Punkt 31).

## Pruning-Notiz (Pflichtteil)

| Stelle | Status |
|---|---|
| Die alte Logzeile `[D.UX.1] transcript_segments INSERT conv=... added=...` | **geloescht** — durch `[ZEITSTEMPEL-1] ... added=N mit_sprechzeiten=M` ersetzt, weil die alte Zeile ab jetzt nur die halbe Wahrheit sagt (sie zaehlt Zeilen, nicht Zeilen **mit** Sprech-Zeiten) |
| Die `[D.UX.1] ... skipped (idempotent)`- und `[D.UX.1] ... Fehler:`-Zeilen | **bleiben unveraendert** — sie gehoeren zum Reentrance-/Fehler-Pfad, den diese Phase ausdruecklich nicht anfasst; ein Praefix-Wechsel dort waere Reparatur-Modus-Verstoss (Bau-Regel 5) |
| Die Docstring-Kopfzeile mit der Drei-Schluessel-Aufzaehlung | **geloescht/ersetzt** — sie haette sonst ueber den Rueckgabewert geluegen |
| Der `if not _txt: continue`-Filter (`:58`) | **bleibt** — er ist der Grund, warum Weg C einen **Platzhalter** statt Leere schreibt; eine leere Zeile fiele hier still heraus |
| EWB-Knopf-Zeilen (ohne die drei Schluessel) | **bleiben ohne Sonderfall-Zweig** — sie fallen ueber `_entry.get(k) -> None` als NULL heraus, genau wie in Plan 04 begruendet |

Kein Code ist durch diesen Plan obsolet geworden, der stehen geblieben waere.

## Naechster Schritt

**Plan 06 — HALT-PUNKT MENSCH**, in dieser Reihenfolge (D-16):
1. `alembic upgrade head` auf Production (als OS-User `postgres`, `DATABASE_URL` gesetzt —
   sonst faellt alembic auf SQLite)
2. Gegenprobe `inspect.sh schema transcript_segments` / `inspect.sh schilder transcript_segments`
3. **erst dann** `bash deploy.sh production` (agenten-gesperrt, Andre faehrt ihn) — das Tor muss
   **0 FAILED** zeigen; `[BASELINE-AUTO-FIX]`-Warnungen duerfen sich nicht vermehren
4. echter Test-Anruf, dann `inspect.sh sample transcript_segments 40` (`end_ms > start_ms`,
   `word_count > 0`) und `inspect.sh logs 300` (`mit_sprechzeiten=N` mit **N > 0**)

## Self-Check: PASSED

- `routes/app_routes.py` — FOUND (geaendert, +19/-2)
- `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-05-SUMMARY.md` — FOUND
- Commit `891b291` — FOUND
- Commit `403be5a` — FOUND
- `tests/` in diesem Plan angefasst — NEIN (belegt, `git diff --name-only HEAD~2..HEAD -- tests/` leer)
- Geloeschte/umbenannte Dateien — KEINE
