---
phase: 08.23.2.ZEITSTEMPEL-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_transcript_segments_write.py
  - tests/test_speech_timing_extraction.py
  - tests/test_art9_zeitzeile.py
autonomous: true
requirements: [D-04, D-07, D-08, D-11, D-13, D-14]
must_haves:
  truths:
    - "Ein Test schlaegt gegen den HEUTIGEN Code fehl, weil die Transform start_ms/end_ms/word_count nicht durchreicht"
    - "Ein Test schlaegt gegen den HEUTIGEN Code fehl, weil es keine Wortzeiten-Extraktion gibt"
    - "Ein Test schlaegt gegen den HEUTIGEN Code fehl, weil ein Art-9-Abschnitt heute GAR KEINE Zeile erzeugt"
    - "Die Tests pruefen Rueckgabewerte bzw. Zustands-Wirkung, keine Quelltext-Zeichenketten"
    - "Kein Test committet Zeilen in nerve_test (kein Zeilen-Aufraeumen noetig)"
    - "Kein Test nennt seam_before, _dg_ms_offset oder offset_ms - diese Mechanik ist gestrichen"
  artifacts:
    - path: "tests/test_speech_timing_extraction.py"
      provides: "ROT-Netz fuer _extract_word_times (D-07)"
      contains: "_extract_word_times"
    - path: "tests/test_transcript_segments_write.py"
      provides: "ROT-Netz fuer die Durchreichung der drei neuen Schluessel (D-08/D-04) und der Weg-C-Platzhalter-Zeile"
      contains: "word_count"
    - path: "tests/test_art9_zeitzeile.py"
      provides: "ROT-Netz fuer Weg C: Art-9-/Fehler-Abschnitte erzeugen eine Zeile mit echten Zeiten und dem Platzhalter-Text"
      contains: "[nicht gespeichert]"
  key_links:
    - from: "tests/test_speech_timing_extraction.py"
      to: "services/deepgram_service.py::_extract_word_times"
      via: "top-level import"
      pattern: "from services.deepgram_service import"
    - from: "tests/test_transcript_segments_write.py"
      to: "routes/app_routes.py::_transcript_entries_to_segments"
      via: "bestehender top-level import"
      pattern: "from routes.app_routes import _transcript_entries_to_segments"
    - from: "tests/test_art9_zeitzeile.py"
      to: "services/deepgram_service.py::_make_on_message (Anonymisierungs-Naht)"
      via: "Verhaltens-Test mit gemocktem anonymize() gegen den echten Handler"
      pattern: "_make_on_message"
---

<objective>
Das Regressions-Netz bauen, das gegen den **heutigen** Stand ROT ist (Bau-Regel 1, D-11).
Diese Welle aendert **keinen** Produktionscode und **kein** Schema — sie erzeugt ausschliesslich
den Beweis, dass die Luecke existiert.

Purpose: Ohne ROT-Lauf ist ein spaeteres GRUEN wertlos (Punkt 31, Bau-Regel 1). Der rote Lauf
aus Plan 02 ist ein Abnahme-Artefakt, kein Nebenprodukt.
Output: drei Test-Dateien (zwei neu, eine erweitert), die im naechsten Deploy-Tor als
FAILED/ERROR erscheinen.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
</objective>

<replan_notiz>
## Was dieser Replan gegenueber der ersten Fassung geaendert hat (Cross-AI 2026-08-10)

| Aenderung | Ausloesender Review-Befund |
|---|---|
| **`seam_before` restlos aus Task 1 entfernt** (Test C ersetzt) | Beide Sichten: DEFER auf D-06a — die zwei Uhren sind bereits das Naht-Signal |
| **`offset_ms`-Tests aus Task 2 entfernt**, Anker neu gemessen | Beide Sichten: der Versatz faellt MIT dem Marker; „nur den Marker streichen waere die schlechteste Variante" |
| **Anker `_extract_word_times(` == 12 korrigiert** | Sicht 2 HIGH-3: `grep -c` zaehlt Zeilen, nicht Vorkommen (sechster Fehl-Anker der Klasse). **Alle Anker dieses Plans wurden gegen die materialisierte Vorlage ausgefuehrt, nicht gelesen.** |
| **Alter Task 3 (Pause-Zweig-Waechter) GESTRICHEN** | Folge der D-06a-Streichung, siehe Begruendung unten |
| **NEUER Task 3: Art-9-Verhaltens-Test (Weg C)** | Gemini HIGH + Sicht 2 MEDIUM-6, entschieden von Andre als **Weg C** (D-07) |

### Warum der alte Task 3 gestrichen ist — bewusst entschieden, nicht vergessen

Der alte Task 3 baute einen Verhaltens-Waechter fuer den **Pause-Zweig** von
`handle_audio_chunk`. Beide Sichten haben seine **Form** ausdruecklich als richtig bestaetigt
(kein Riegel im 10-Hz-Ton-Weg, Verhaltens-Test statt Quelltext-Check, Gegenpol als Messgeraet).
**Trotzdem faellt er** — aus zwei Gruenden, die beide aus der Streichung von D-05/D-06a folgen:

1. **Diese Phase stellt nichts mehr in den Pause-Zweig.** Ohne `mark_dg_seam` bleibt der Zweig
   ein nacktes `return`, wortgleich zum heutigen Stand. Ein Waechter dafuer waere reine
   Bestandssicherung an einer Stelle, die diese Phase nicht anfasst — **Reparatur-Modus-Regel:
   nur der Fehler wird angefasst.**
2. **Er koennte nicht mehr rot sein.** Seine Wirkungs-Zusicherung war
   `_dg_seam_pending is True`; die faellt mit dem Marker weg. Uebrig blieben zwei Zusicherungen,
   die **heute schon** durchgehen — ein Test, der nie rot war, beweist nichts (Bau-Regel 1), und
   der Gegenpol waere ein Test, der ohne zu schuetzenden Fix von Tag eins gruen ist.

**Konsequenz, benannt statt verschwiegen (Punkt 31):** der Pause-Zweig bleibt damit so unbewacht
wie vor dieser Phase — der Bestandstest ruft weiterhin nur mit `_SID_UNBEKANNT` auf und betritt
ihn nie. **Das ist keine Verschlechterung, aber auch keine Verbesserung.** Der Waechter gehoert
in **die** Phase, die den Pause-Zweig real erreichbar macht (echter Schalter, der
`state['is_paused'] = True` setzt) — dort zusammen mit Marker und Versatz. Gehoert in die
SUMMARY-Restluecken (Plan 06).
`tests/test_audio_path_lock_free_guard.py` wird von dieser Phase **nicht angefasst**.
</replan_notiz>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-CONTEXT.md
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-RESEARCH.md
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-REVIEWS.md
@CLAUDE.md
@tests/test_transcript_segments_write.py
@routes/app_routes.py

<interfaces>
<!-- Vertraege, gegen die die Tests geschrieben werden. Der Executor braucht KEINE Codebase-Suche. -->

Heutiger Stand — routes/app_routes.py:45-73 (reine Transform, gibt HEUTE nur 3 Schluessel zurueck):

```python
def _transcript_entries_to_segments(log_entries):
    out = []
    ...
    out.append({
        'ts_ms': _rel,
        'speaker': _sp_map.get(_entry.get('speaker'), 'system'),
        'text': _txt,
    })
    return out
```

ZIEL-Vertrag nach Plan 05 (gegen diesen schreiben die Tests):

```python
out.append({
    'ts_ms': _rel,
    'speaker': ...,
    'text': _txt,
    'start_ms':   _entry.get('start_ms'),     # int | None
    'end_ms':     _entry.get('end_ms'),       # int | None
    'word_count': _entry.get('word_count'),   # int | None
})
```

ZIEL-Vertrag nach Plan 04 — services/deepgram_service.py, neue Modul-Funktion neben `_get_speaker`:

```python
def _extract_word_times(result):
    """Deepgram-Wortzeiten -> (start_ms, end_ms, word_count). (None, None, None) wenn
    keine Wortobjekte vorliegen."""
```
⛔ **Kein `offset_ms`-Parameter.** Der Versatz (D-05) ist nach dem Cross-AI-Review gestrichen.

ZIEL-Vertrag nach Plan 04 Task 2 (Weg C, D-07) — der Art-9-/Fehler-Pfad haengt eine Zeile an
statt sie zu verwerfen:

```python
_PLATZHALTER_NICHT_GESPEICHERT = '[nicht gespeichert]'

def _append_transcript_entry(sid, ts, speaker, text, start_ms, end_ms, word_count):
    """Einziger Anhaenger fuer type='transcript' im on_message-Weg."""
```

Deepgram-Vertrag (Context7, developers.deepgram.com/docs/diarization):
`result.channel.alternatives[0].words` ist eine Liste von Objekten mit `.start` / `.end`
als **Float-SEKUNDEN** ab Verbindungs-Oeffnung. Umrechnung ist `* 1000`.
</interfaces>
</context>

## 5. Persistenz-Schicht-Verifikation

**Dieser Plan liest und schreibt KEINE Datenbank.** Die Tests aus Task 1 und 2 rufen reine
Funktionen mit Python-Dicts/Fake-Objekten auf. Die Tests aus Task 3 rufen den echten
`on_message`-Handler, beruehren aber ausschliesslich das per-SID-**RAM**-Dict
`ls._session_state` (in der Fixture geseedet und im Teardown wieder entfernt) — kein DB-Zugriff,
keine Deepgram-Verbindung, keine SocketIO-Verbindung (`extensions.socketio` wird durch eine
stumme Attrappe ersetzt). **Keiner** der Tests committet nach `nerve_test`, alle brauchen daher
**kein** `cleanup_rows` (tests/conftest.py-Konvention). Der `_baseline_cleanup_guard` bleibt
gruen, weil keine Zeile entsteht.

**Schreib-Zeitpunkt-Spalte (Punkt 26):** entfaellt fuer diesen Plan — es wird nichts persistiert
und nichts gelesen, was ein anderer Pfad schreibt. Der vollstaendige
`inspect.sh schema/constraints`-Beleg fuer `public.transcript_segments` steht verbatim in Plan 03
(Schema-Aenderung) und Plan 05 (Schreiber).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ROT-Netz fuer die Durchreichung der drei Schluessel (D-08/D-04) und die Weg-C-Zeile</name>

  <read_first>
    - `tests/test_transcript_segments_write.py` (52 Zeilen, vollstaendig — Stil, Kopf-Kommentar, bestehende 4 Tests)
    - `routes/app_routes.py:45-73` (`_transcript_entries_to_segments`, die reine Transform — heutiger Stand)
    - `routes/app_routes.py:20-33` (`WARN-4`/`WARN-5`-Kommentarblock: warum `ts_ms` Wall-Clock ist)
    - `services/deepgram_service.py:1094-1105` (EWB-Knopf-Schreiber — die Zeile, die NULL bekommen muss)
    - `CLAUDE.md`, Abschnitt "Test-Qualitaets-Regel" (kein Source-Presence-Test)
  </read_first>

  <behavior>
    - Test A `test_transform_reicht_sprechzeiten_durch`: Entry mit `start_ms=1200`, `end_ms=4300`,
      `word_count=7` -> Segment traegt exakt diese drei Werte.
    - Test B `test_knopfzeile_ohne_wortzeiten_wird_null`: Entry OHNE diese drei Schluessel
      (= EWB-Knopf-Zeile) -> alle drei Werte sind `None`, **nicht** `0` (D-04).
    - Test C `test_platzhalterzeile_behaelt_ihre_sprechzeiten`: Entry mit
      `text='[nicht gespeichert]'` und echten Zeiten -> Segment traegt den Platzhalter-Text und
      die Zeiten; die Dauer ist rechenbar (Weg C, D-07).
    - Test D `test_ts_ms_bleibt_unberuehrt_von_den_neuen_spalten`: die bestehende `ts_ms`-Arithmetik
      (`[0, 4000, 8000]`) aendert sich nicht, wenn die neuen Schluessel gesetzt sind (D-02:
      zwei Achsen, die sich nicht beeinflussen).
  </behavior>

  <action>
Haenge an `tests/test_transcript_segments_write.py` (bestehender Import bleibt, keine Zeile
loeschen) vier neue Testfunktionen an. Konkrete Werte, kein Platzhalter:

```python
# ── Phase 08.23.2.ZEITSTEMPEL-1 — Sprech-Zeiten durch die reine Transform ──────────────
# ROT gegen den Stand vor dieser Phase: die Transform gibt heute nur ts_ms/speaker/text
# zurueck, der Zugriff auf 'start_ms' wirft KeyError. Function-Call-Return-Test, kein
# Source-Presence-Check (CLAUDE.md Test-Qualitaets-Regel).

def test_transform_reicht_sprechzeiten_durch():
    segs = _transcript_entries_to_segments([
        {'type': 'transcript', 'ts': '00:00:01', 'speaker': 0, 'text': 'Guten Tag',
         'start_ms': 1200, 'end_ms': 4300, 'word_count': 7},
    ])
    assert len(segs) == 1
    assert segs[0]['start_ms'] == 1200
    assert segs[0]['end_ms'] == 4300
    assert segs[0]['word_count'] == 7


def test_knopfzeile_ohne_wortzeiten_wird_null():
    # Die EWB-Knopf-Zeile (services/deepgram_service.py:1094-1105) setzt die drei
    # Schluessel schlicht nicht. D-04: dann NULL, ausdruecklich nicht 0 —
    # word_count=0 hiesse "hat nichts gesagt", None heisst "unbekannt".
    segs = _transcript_entries_to_segments([
        {'type': 'transcript', 'ts': '00:00:01', 'speaker': 1,
         'text': 'preis *ewb button*', 'data': {'ewb_button': True}},
    ])
    assert len(segs) == 1
    assert segs[0]['start_ms'] is None
    assert segs[0]['end_ms'] is None
    assert segs[0]['word_count'] is None


def test_platzhalterzeile_behaelt_ihre_sprechzeiten():
    # Weg C (Andre 2026-08-10): ein Abschnitt mit Art-9-Treffer oder Anonymisierungs-
    # Fehler wird NICHT mehr verworfen, sondern mit neutralem Platzhalter-Text und
    # ECHTEN Zeiten geschrieben. Sonst fehlte seine Sprech-Zeit in Zaehler UND Nenner
    # des Redeanteils und die Luecke wuerde als Pause fehlgelesen.
    segs = _transcript_entries_to_segments([
        {'type': 'transcript', 'ts': '00:00:01', 'speaker': 1,
         'text': '[nicht gespeichert]', 'start_ms': 2000, 'end_ms': 32000,
         'word_count': 61},
    ])
    assert len(segs) == 1
    assert segs[0]['text'] == '[nicht gespeichert]'
    assert segs[0]['end_ms'] - segs[0]['start_ms'] == 30000
    assert segs[0]['word_count'] == 61


def test_ts_ms_bleibt_unberuehrt_von_den_neuen_spalten():
    # D-02: zwei getrennte Achsen. Die Deepgram-Zeiten duerfen die ts_ms-Arithmetik
    # (Wall-Clock, relativ zum ersten Entry, monoton geklemmt) NICHT veraendern.
    segs = _transcript_entries_to_segments([
        {'type': 'transcript', 'ts': '00:00:01', 'speaker': 0, 'text': 'a',
         'start_ms': 500,  'end_ms': 900,  'word_count': 1},
        {'type': 'transcript', 'ts': '00:00:05', 'speaker': 1, 'text': 'b',
         'start_ms': 4100, 'end_ms': 4800, 'word_count': 2},
        {'type': 'transcript', 'ts': '00:00:09', 'speaker': 9, 'text': 'c',
         'start_ms': 8200, 'end_ms': 9000, 'word_count': 2},
    ])
    assert [s['ts_ms'] for s in segs] == [0, 4000, 8000]
```

**Nichts anderes an der Datei aendern** (Reparatur-Modus: keine Umbenennungen, keine
Import-Sortierung, die vier Bestandstests bleiben unveraendert).
  </action>

  <acceptance_criteria>
    > **Alle Zahlen unten wurden am 2026-08-10 gegen die materialisierte Vorlage AUSGEFUEHRT**
    > (Bestandsdatei + obiger Block zusammengefuegt, dann gegrept), nicht abgezaehlt.
    > Grund: `grep -c` zaehlt **Zeilen**, nicht Vorkommen — genau dieser Denkfehler war der
    > sechste Fehl-Anker dieser Phase. `grep -ciF` wird nirgends verwendet.

    - `grep -c "^def test_" tests/test_transcript_segments_write.py` liefert **8**
      (4 Bestand + 4 neu; **ausgefuehrt**). Existenz-Anker daneben (Bau-Regel 20):
      `grep -c "_transcript_entries_to_segments" tests/test_transcript_segments_write.py`
      liefert **9** (5 im Bestand **gemessen**, plus 4 neue Aufrufe) — der grep hat also
      wirklich gelesen.
    - Die Datei enthaelt **keine** der verbotenen Muster (Test-Qualitaets-Regel). Abwesenheits-Anker:
      `grep -c "inspect.getsource\|hasattr(" tests/test_transcript_segments_write.py` liefert **0**;
      gepaarter Existenz-Anker: `grep -c "assert " tests/test_transcript_segments_write.py`
      liefert **24** (11 im Bestand **gemessen**, plus 13 aus der Vorlage oben).
    - Kein `cleanup_rows`-Bedarf: `grep -c "db\.\|session\.\|commit(" tests/test_transcript_segments_write.py`
      liefert **0** (**ausgefuehrt**).
    - **Die gestrichene Mechanik ist restlos raus:**
      `grep -c "seam_before\|_dg_ms_offset\|_dg_last_end_ms\|_dg_seam_pending\|mark_dg_seam\|offset_ms" tests/test_transcript_segments_write.py`
      liefert **0**. Gepaarter Existenz-Anker: `grep -c "word_count" ...` liefert **9**
      (**ausgefuehrt**: 5 Vorkommen in den Dict-Literalen + 4 in den Zusicherungen).
    - **Wirkungs-Beweis ist ausdruecklich VERTAGT auf Plan 02:** der ROT-Lauf auf dem
      Production-Server muss genau diese vier Namen als FAILED melden.
  </acceptance_criteria>

  <verify>
    <automated>VERTAGT — der Beweis ist der ROT-Lauf in Plan 02 (`bash deploy.sh production`, von Andre gefahren). KEIN lokaler pytest (HART-Regel "Kein Local-Dev").</automated>
  </verify>

  <done>Vier neue Testfunktionen in `tests/test_transcript_segments_write.py`, committet, keine Bestandszeile veraendert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ROT-Netz fuer die Wortzeiten-Extraktion (D-07)</name>

  <read_first>
    - `services/deepgram_service.py:58-70` (`_get_speaker` — das Vorbild: liest `result.channel.alternatives[0].words`, faengt Leer-Liste ab, kapselt in `try/except Exception`)
    - `services/deepgram_service.py:188-191` (zweiter bestehender `words`-Zugriff, mit `try/except (AttributeError, IndexError)`)
    - `tests/test_deepgram_keepalive.py` (Stil eines bestehenden deepgram_service-Tests)
    - `CLAUDE.md`, Abschnitt "Test-Qualitaets-Regel"
  </read_first>

  <behavior>
    - Normalfall: `words` mit `start=1.2 / end=4.3` und 3 Eintraegen -> `(1200, 4300, 3)`.
    - Rundung: `start=0.0004` -> `0`; `end=4.2996` -> `4300` (`int(round(x * 1000))`).
    - Leere Wortliste -> `(None, None, None)`, **kein** `IndexError`.
    - Fehlendes/kaputtes `result`-Objekt -> `(None, None, None)`, **kein** Crash im Live-Thread.
    - Die Wortanzahl ist `len(words)`, nicht die Wortzahl eines Textes.
    - Die Funktion haelt **keinen** Zustand: zwei Aufrufe mit demselben Ergebnis sind identisch.
    - ⛔ **Kein `offset_ms`.** Der Versatz ist gestrichen (D-05).
  </behavior>

  <action>
Neue Datei `tests/test_speech_timing_extraction.py`:

```python
"""Phase 08.23.2.ZEITSTEMPEL-1 — ROT-Netz fuer die Wortzeiten-Naht.

Function-Call-Return-Tests gegen die reine Extraktions-Funktion. KEIN Source-Presence-Check
(CLAUDE.md Test-Qualitaets-Regel): die Tests rufen die Funktion auf und assertieren auf den
Rueckgabewert. Kein DB-Zugriff, kein commit -> kein Zeilen-Aufraeumen noetig (tests/conftest.py-Konvention).

ROT gegen den Stand vor dieser Phase: services/deepgram_service._extract_word_times
existiert nicht -> ImportError beim Sammeln.

KEIN Versatz-Parameter: der Reconnect-Versatz (D-05) ist nach dem Cross-AI-Review vom
2026-08-10 gestrichen. Ohne ihn wird die Pause ueber eine Naht negativ — physikalisch
unmoeglich und damit ein selbsterklaerendes "unbekannt".
"""
from services.deepgram_service import _extract_word_times


class _FakeWord:
    def __init__(self, start, end):
        self.start = start
        self.end = end


class _FakeResult:
    """Minimaler Nachbau von result.channel.alternatives[0].words (deepgram-sdk 3.10.0)."""
    def __init__(self, words):
        alt = type('Alt', (), {'words': words})()
        self.channel = type('Ch', (), {'alternatives': [alt]})()


def test_extrahiert_start_ende_und_wortanzahl():
    r = _FakeResult([_FakeWord(1.2, 1.9), _FakeWord(2.0, 2.6), _FakeWord(3.9, 4.3)])
    assert _extract_word_times(r) == (1200, 4300, 3)


def test_rundet_auf_ganze_millisekunden():
    r = _FakeResult([_FakeWord(0.0004, 0.5), _FakeWord(4.0, 4.2996)])
    assert _extract_word_times(r) == (0, 4300, 2)


def test_leere_wortliste_gibt_none_statt_indexerror():
    # is_final=True kann laut Deepgram-Verhalten mit leerer words-Liste kommen.
    assert _extract_word_times(_FakeResult([])) == (None, None, None)


def test_kaputtes_result_gibt_none_statt_crash():
    # Der Live-Loop-Thread darf an dieser Stelle NIE sterben (Muster _get_speaker).
    assert _extract_word_times(object()) == (None, None, None)
    assert _extract_word_times(None) == (None, None, None)


def test_wortanzahl_ist_die_zahl_der_wortobjekte():
    # D-07: gezaehlt werden die ROHEN Wortobjekte, nicht die Woerter des spaeter
    # anonymisierten Textes ([PERSON_A] ersetzt zwei gesprochene Woerter durch eines).
    r = _FakeResult([_FakeWord(0.5, 0.8), _FakeWord(0.9, 1.4), _FakeWord(1.5, 2.5)])
    assert _extract_word_times(r)[2] == 3


def test_zweiter_aufruf_liefert_dasselbe():
    # Die Funktion haelt keinen Zustand — kein akkumulierender Versatz, kein Zaehler.
    r = _FakeResult([_FakeWord(0.5, 2.5)])
    assert _extract_word_times(r) == _extract_word_times(r) == (500, 2500, 1)
```
  </action>

  <acceptance_criteria>
    > **Alle Zahlen unten wurden am 2026-08-10 gegen die materialisierte Vorlage AUSGEFUEHRT.**
    > Die frueher hier stehende `12` war der sechste Fehl-Anker dieser Klasse (Sicht 2, HIGH-3):
    > `grep -c` zaehlt **Zeilen**, der Plan hatte **Vorkommen** gezaehlt. Wo Vorkommen gemeint
    > sind, steht unten ausdruecklich `grep -o … | wc -l`.

    - Datei `tests/test_speech_timing_extraction.py` existiert; `grep -c "^def test_"` liefert **6**.
    - Abwesenheits-Anker (Test-Qualitaets-Regel):
      `grep -c "inspect.getsource\|open(" tests/test_speech_timing_extraction.py` liefert **0**;
      gepaarter Existenz-Anker **auf Zeilen**:
      `grep -c "_extract_word_times(" tests/test_speech_timing_extraction.py` liefert **7**,
      und **auf Vorkommen**:
      `grep -o "_extract_word_times(" tests/test_speech_timing_extraction.py | wc -l` liefert **8**.
      Beide Zahlen sind ausgefuehrt; wer nur eine prueft, prueft die richtige — sie sind
      absichtlich beide genannt, damit die Verwechslung nicht ein siebtes Mal passiert.
    - Abwesenheits-Anker DB:
      `grep -c "commit(\|SessionLocal\|cleanup_rows" tests/test_speech_timing_extraction.py`
      liefert **0**; gepaarter Existenz-Anker `grep -c "assert " ...` liefert **7**.
    - **Die gestrichene Mechanik ist restlos raus:**
      `grep -c "offset_ms\|seam_before\|_dg_" tests/test_speech_timing_extraction.py` liefert **0**;
      gepaarter Existenz-Anker: `grep -c "_FakeResult(" ...` liefert **5**.
    - **Wirkungs-Beweis VERTAGT auf Plan 02:** der ROT-Lauf meldet dieses Modul als
      Collection-Error (`ImportError: cannot import name '_extract_word_times'`) — **einen**
      Error fuer das ganze Modul, nicht sechs.
  </acceptance_criteria>

  <verify>
    <automated>VERTAGT — der Beweis ist der ROT-Lauf in Plan 02 (`bash deploy.sh production`, von Andre gefahren). KEIN lokaler pytest.</automated>
  </verify>

  <done>`tests/test_speech_timing_extraction.py` existiert mit 6 Tests, committet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ROT-Netz fuer Weg C — Art-9-Abschnitte behalten ihre Sprech-Zeit (D-07)</name>

  <read_first>
    - `services/deepgram_service.py:72-105` (`_make_on_message`, Kopf: die beiden fruehen Ausgaenge, `is_final`, `stabilize_speaker`, `next_line_id`, `ts`)
    - `services/deepgram_service.py:113-118` (Cold-Call-Zweig: `sp_label='Berater'`, `log_sp = 0`, `roles_confirmed = True` — der Modus, in dem der Test faehrt, weil er `close_moment` umgeht)
    - `services/deepgram_service.py:142-179` **vollstaendig** (die Anonymisierungs-Naht: Art-9, `[ANON_FEHLER]`, `else`-Append, die zwei `except`-Zweige — das ist der Pruefling)
    - `services/deepgram_service.py:188-215` (Word-Confidence-Block, der nach der Naht laeuft und `word_confidences` im per-SID-Dict erwartet)
    - `services/anonymization.py:78` (`class AnonymizationPipelineUnavailable`) und `:539` (`def anonymize(text, cache)` — die Signatur, gegen die gemockt wird)
    - `services/live_session.py:95-104` (`next_line_id`) und `:1236-1260` (`stabilize_speaker`) — beide Ghost-SID-sicher mit `.get()`-Defaults, deshalb genuegt ein handgeseedetes dict
    - `tests/test_audio_path_lock_free_guard.py:73-85` (die Fixture `geseedeter_state` — das hauseigene Muster, `ls._session_state[sid]` direkt zu seeden statt `init_session_state` zu rufen)
    - `CLAUDE.md`, Abschnitt "Test-Qualitaets-Regel" und die zwei unverhandelbaren Daten-Schranken
  </read_first>

  <behavior>
    - Art-9-Treffer -> **eine** Zeile im RAM-Log, `text == '[nicht gespeichert]'`,
      `start_ms=2000`, `end_ms=5000`, `word_count=5`.
    - Diese Zeile enthaelt **kein** Fragment des Originaltexts und **keine** Kategorie
      (weder `krankgeschrieben` noch `Kollege` noch `ART9`).
    - `[ANON_FEHLER]` -> dieselbe Platzhalter-Zeile.
    - `AnonymizationPipelineUnavailable` -> dieselbe Platzhalter-Zeile.
    - Unerwartete Ausnahme in `anonymize()` -> dieselbe Platzhalter-Zeile.
    - Normalfall -> weiterhin der **anonymisierte** Text, plus die drei Zeiten.
    - Art-9-Treffer **ohne** Wortobjekte -> Zeile mit dreimal `None` (D-04: unbekannt, nicht 0).
    - **Heute ist all das rot:** in den vier Fehlerpfaden entsteht gar keine Zeile
      (`len(log) == 0`), im Normalfall fehlen die drei Schluessel (`KeyError`).
  </behavior>

  <action>
Neue Datei `tests/test_art9_zeitzeile.py`:

```python
"""Phase 08.23.2.ZEITSTEMPEL-1 — Weg C: Art-9-Abschnitte verlieren ihre Sprech-Zeit nicht.

Verhaltens-Test gegen den ECHTEN on_message-Handler mit gemockter Anonymisierungs-Naht.
Kein Source-Presence-Check, kein DB-Zugriff, kein commit -> kein Zeilen-Aufraeumen noetig.

ROT gegen den Stand vor dieser Phase: bei einem Art-9-Treffer entsteht heute GAR KEINE
Zeile im conversation_log (services/deepgram_service.py:154-157 setzen nur
_text_for_analysis = None und ueberspringen den Append) -> len(log) == 0 statt 1.

WARUM WEG C (Andre-Entscheidung 2026-08-10, DIALOG-GSD-CLAUDIAN.md):
Ein verworfener Abschnitt fehlt in Zaehler UND Nenner des Redeanteils, und seine Luecke
wird spaeter als Pause fehlgelesen. Eine still verzerrte Kennzahl ist genau die
Fehlerklasse, die diese Phase beseitigen soll. Gespeichert wird trotzdem GENAUSO WENIG
INHALT WIE HEUTE: nur Zeiten, eine Zahl und ein neutraler Platzhalter OHNE Kategorie
(der Marker soll nicht verraten, worum es ging).

Die Anonymisierungs-Pipeline wird dabei nur ERGAENZT, nicht umgebaut: anonymize() bleibt
fail-closed, es wird KEIN roher und KEIN geschwaerzter Text zusaetzlich gespeichert.
"""
import pytest

import services.deepgram_service as dg
import services.live_session as ls


_SID = 'zs1-art9-001'
_PLATZHALTER = '[nicht gespeichert]'
_ROHTEXT = 'Mein Kollege ist seit Montag krankgeschrieben'


class _FakeWord:
    def __init__(self, start, end):
        self.start = start
        self.end = end
        self.confidence = 0.99


class _FakeResult:
    """Minimaler Nachbau eines finalisierten Deepgram-Ergebnisses."""
    def __init__(self, transcript, words):
        self.is_final = True
        alt = type('Alt', (), {'transcript': transcript, 'words': words})()
        self.channel = type('Ch', (), {'alternatives': [alt]})()
        self.metadata = type('Meta', (), {'duration': 0.0})()


class _StummesSio:
    def emit(self, *a, **k):
        return None


@pytest.fixture
def geseedete_sid(monkeypatch):
    """Seedet den per-SID-Zustand direkt (Muster tests/test_audio_path_lock_free_guard.py)
    und ersetzt extensions.socketio durch eine stumme Attrappe — on_message holt sie
    erst zur Aufrufzeit (`from extensions import socketio as sio`)."""
    import extensions
    monkeypatch.setattr(extensions, 'socketio', _StummesSio(), raising=False)
    with ls._session_state_lock:
        ls._session_state[_SID] = {
            'state': {'is_paused': False},
            'conversation_log': [],
            'word_confidences': [],
            '_line_id_counter': 0,
            'anonymisierer': None,
        }
    yield
    with ls._session_state_lock:
        ls._session_state.pop(_SID, None)


def _lauf(monkeypatch, anon_rueckgabe, words=None):
    """Ruft den echten on_message-Handler einmal auf und gibt den RAM-Log zurueck."""
    import services.anonymization as anon
    monkeypatch.setattr(anon, 'anonymize', lambda text, cache: anon_rueckgabe)
    handler = dg._make_on_message(_SID, mode='cold_call')
    handler(None, _FakeResult(_ROHTEXT, words if words is not None else [
        _FakeWord(2.0, 2.4), _FakeWord(2.5, 3.0), _FakeWord(3.1, 3.6),
        _FakeWord(3.7, 4.0), _FakeWord(4.1, 5.0),
    ]))
    with ls._session_state_lock:
        return list(ls._session_state[_SID]['conversation_log'])


def test_art9_treffer_erzeugt_zeile_mit_zeiten_ohne_inhalt(geseedete_sid, monkeypatch):
    log = _lauf(monkeypatch, ('[ART9_REDACTED]', 'art9'))
    assert len(log) == 1
    assert log[0]['text'] == _PLATZHALTER
    assert log[0]['start_ms'] == 2000
    assert log[0]['end_ms'] == 5000
    assert log[0]['word_count'] == 5


def test_art9_zeile_traegt_kein_fragment_des_originaltexts(geseedete_sid, monkeypatch):
    log = _lauf(monkeypatch, ('[ART9_REDACTED]', 'art9'))
    _ganze_zeile = repr(log[0])
    assert 'krankgeschrieben' not in _ganze_zeile
    assert 'Kollege' not in _ganze_zeile
    assert 'ART9' not in _ganze_zeile


def test_anonymisierungs_fehler_erzeugt_dieselbe_platzhalter_zeile(geseedete_sid, monkeypatch):
    log = _lauf(monkeypatch, ('[ANON_FEHLER]', 'fehler'))
    assert len(log) == 1
    assert log[0]['text'] == _PLATZHALTER
    assert log[0]['word_count'] == 5


def test_pipeline_ausfall_erzeugt_platzhalter_zeile_statt_gar_keiner(geseedete_sid, monkeypatch):
    import services.anonymization as anon

    def _wirft(text, cache):
        raise anon.AnonymizationPipelineUnavailable('Test')

    monkeypatch.setattr(anon, 'anonymize', _wirft)
    handler = dg._make_on_message(_SID, mode='cold_call')
    handler(None, _FakeResult(_ROHTEXT, [_FakeWord(0.0, 1.0), _FakeWord(1.2, 2.0)]))
    with ls._session_state_lock:
        log = list(ls._session_state[_SID]['conversation_log'])
    assert len(log) == 1
    assert log[0]['text'] == _PLATZHALTER
    assert log[0]['start_ms'] == 0
    assert log[0]['end_ms'] == 2000


def test_unerwarteter_fehler_erzeugt_platzhalter_zeile(geseedete_sid, monkeypatch):
    import services.anonymization as anon

    def _wirft(text, cache):
        raise ValueError('unerwartet')

    monkeypatch.setattr(anon, 'anonymize', _wirft)
    handler = dg._make_on_message(_SID, mode='cold_call')
    handler(None, _FakeResult(_ROHTEXT, [_FakeWord(0.0, 1.0), _FakeWord(1.2, 2.0)]))
    with ls._session_state_lock:
        log = list(ls._session_state[_SID]['conversation_log'])
    assert len(log) == 1
    assert log[0]['text'] == _PLATZHALTER


def test_normaler_abschnitt_traegt_weiterhin_den_anonymisierten_text(geseedete_sid, monkeypatch):
    log = _lauf(monkeypatch, ('Mein Kollege ist seit [DATUM] krank', 'tier_1'))
    assert len(log) == 1
    assert log[0]['text'] == 'Mein Kollege ist seit [DATUM] krank'
    assert log[0]['start_ms'] == 2000
    assert log[0]['end_ms'] == 5000
    assert log[0]['word_count'] == 5


def test_abschnitt_ohne_wortobjekte_bekommt_none_statt_null_werte(geseedete_sid, monkeypatch):
    # Bekannte Daten-Kante (Gemini LOW): ein Endergebnis mit Text, aber ohne
    # Wortobjekte. NULL heisst "unbekannt"; eine 0 wuerde "hat nichts gesagt"
    # behaupten und jeden Mittelwert verfaelschen (D-04).
    log = _lauf(monkeypatch, ('[ART9_REDACTED]', 'art9'), words=[])
    assert len(log) == 1
    assert log[0]['start_ms'] is None
    assert log[0]['end_ms'] is None
    assert log[0]['word_count'] is None
```

**Kein pytest-Marker** — `deploy.sh:221` faehrt `-m "not live and not perf"`; ein Marker wuerde
den Test aus dem Abnahme-Tor werfen.
  </action>

  <acceptance_criteria>
    > **Alle Zahlen unten wurden am 2026-08-10 gegen die materialisierte Vorlage AUSGEFUEHRT.**

    - Datei `tests/test_art9_zeitzeile.py` existiert; `grep -c "^def test_"` liefert **7**.
    - `grep -c "assert " tests/test_art9_zeitzeile.py` liefert **26**.
    - `grep -c "_PLATZHALTER" tests/test_art9_zeitzeile.py` liefert **5**
      (1 Definition + 4 Verwendungen).
    - Abwesenheits-Anker (Test-Qualitaets-Regel — dies ist ein Verhaltens-, kein Quelltext-Test):
      `grep -c "inspect.getsource\|hasattr(" tests/test_art9_zeitzeile.py` liefert **0**;
      gepaarter Existenz-Anker: `grep -c "@pytest.fixture" ...` liefert **1**.
    - Abwesenheits-Anker DB: `grep -c "commit(\|SessionLocal\|cleanup_rows" tests/test_art9_zeitzeile.py`
      liefert **0**; gepaarter Existenz-Anker: `grep -c "ls._session_state" ...` liefert **10** (5 Riegel-Nahmen + 5 Zugriffe auf das per-SID-Dict).
    - Abwesenheits-Anker Marker: `grep -c "pytest.mark" tests/test_art9_zeitzeile.py` liefert **0**.
    - **Die gestrichene Mechanik ist restlos raus:**
      `grep -c "seam_before\|offset_ms\|mark_dg_seam\|_dg_" tests/test_art9_zeitzeile.py` liefert **0**.
    - **DSGVO-Selbstpruefung (Bau-Regel 3):** der Test beweist ausdruecklich, dass **kein**
      Fragment des Originaltexts in der Zeile landet
      (`test_art9_zeile_traegt_kein_fragment_des_originaltexts`) — das ist die Zusicherung, an
      der Weg C haengt. Faellt dieser eine Test spaeter aus, ist die Datenschutz-Linie unbelegt.
    - **Wirkungs-Beweis VERTAGT auf Plan 02:** der ROT-Lauf meldet **alle sieben** Tests dieses
      Moduls als FAILED (das Modul importiert heute sauber — es zieht kein neues Symbol —, die
      Tests fallen an der Zusicherung, nicht am Import).
  </acceptance_criteria>

  <verify>
    <automated>VERTAGT — der Beweis ist der ROT-Lauf in Plan 02 (`bash deploy.sh production`, von Andre gefahren). KEIN lokaler pytest (HART-Regel "Kein Local-Dev").</automated>
  </verify>

  <done>`tests/test_art9_zeitzeile.py` existiert mit 7 Verhaltens-Tests, committet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keine neue | Dieser Plan fuegt ausschliesslich Test-Code hinzu, kein Laufzeit-Pfad, kein Endpunkt, kein Eingabe-Kanal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ZS1-01 | Tampering | Test-Suite (False-Green) | mitigate | Der Beweis ist der ROT-Lauf in Plan 02, nicht die Existenz der Datei. Ein Test, der nie rot war, gilt in dieser Phase ausdruecklich nicht als Beleg (Punkt 31). Genau darum ist der alte Task 3 gestrichen: sein Rot war mit der Streichung von D-06a nicht mehr herstellbar. |
| T-ZS1-02 | Information Disclosure | Test-Fixtures | accept | Die Fixtures enthalten nur erfundene Zahlen und erfundene Saetze — keine echten Transkript-Daten, kein PII. Der Satz in Task 3 ist bewusst ein *erfundener* Art-9-naher Satz. |
| T-ZS1-26 | Information Disclosure | Weg-C-Platzhalter | mitigate | `test_art9_zeile_traegt_kein_fragment_des_originaltexts` prueft die Zeile als Ganzes (`repr`) gegen drei verbotene Zeichenketten — Originalwoerter UND Kategorie. Ohne diesen Test waere Weg C eine unbelegte Behauptung. |
| T-ZS1-27 | Denial of Service | Test-Suite (Task 3, echter Handler) | accept | Der Handler laeuft synchron im Test-Faden, ohne Netz, ohne DB, ohne Deepgram-Verbindung; `extensions.socketio` ist eine stumme Attrappe. Der Word-Confidence-Block nimmt kurz `_session_state_lock` und gibt ihn wieder frei — kein Halter-Faden, keine Haenge-Gefahr fuer `deploy.sh` (das keinen pytest-Timeout hat). |
</threat_model>

<verification>
Kein Local-Dev. Diese Welle hat **absichtlich keine** eigene Ausfuehrungs-Abnahme: der einzige
gueltige Beweis ist der rote Lauf des serverseitigen Tors in Plan 02.
</verification>

<success_criteria>
- [ ] 4 neue Testfunktionen in `tests/test_transcript_segments_write.py` (Gesamt 8)
- [ ] `tests/test_speech_timing_extraction.py` mit 6 Testfunktionen, **ohne** `offset_ms`
- [ ] `tests/test_art9_zeitzeile.py` mit 7 Verhaltens-Tests fuer Weg C
- [ ] `tests/test_audio_path_lock_free_guard.py` **unangetastet** (alter Task 3 gestrichen)
- [ ] Kein Produktionscode und kein Schema angefasst (`git diff --stat` nennt nur die drei Test-Dateien)
- [ ] Kein Source-Presence-Muster, kein DB-Commit, kein `seam_before`/`offset_ms` in allen drei Dateien
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-01-SUMMARY.md`
</output>
