---
phase: 08.23.2.ZEITSTEMPEL-1
plan: 03
type: execute
wave: 3
depends_on: ["08.23.2.ZEITSTEMPEL-1-02"]
files_modified:
  - database/models.py
  - alembic/versions/0039_zeitstempel1_sprechzeiten.py
autonomous: true
requirements: [D-02, D-03, D-06a, D-09, D-10]
must_haves:
  truths:
    - "transcript_segments hat vier neue nullable Spalten: start_ms, end_ms, word_count, seam_before"
    - "Jede der vier Spalten traegt ein Schild in der DB (pg_description), nicht nur im Code"
    - "Das Tabellen-Schild nennt ALLE fuenf heutigen Leser und den geaenderten Schreib-Pfad"
    - "Die 627 Bestandszeilen ueberleben unveraendert mit NULL in allen vier Spalten"
    - "ts_ms bleibt unveraendert - Typ, Nullability, Bedeutung, Index"
  artifacts:
    - path: "database/models.py"
      provides: "ORM-Definition der vier Spalten mit comment="
      contains: "seam_before"
    - path: "alembic/versions/0039_zeitstempel1_sprechzeiten.py"
      provides: "DDL + COMMENT ON COLUMN + Tabellen-Schild-Nachzug, Revision auf Kopf 0038"
      contains: "COMMENT ON COLUMN"
  key_links:
    - from: "alembic/versions/0039_zeitstempel1_sprechzeiten.py"
      to: "public.transcript_segments"
      via: "op.add_column + op.execute COMMENT"
      pattern: "add_column\\('transcript_segments'"
    - from: "database/models.py::TranscriptSegment"
      to: "alembic/versions/0039_zeitstempel1_sprechzeiten.py"
      via: "zeichengleiche Schild-Texte (Punkt 23)"
      pattern: "comment="
---

<objective>
Die vier neuen Spalten anlegen — als **Alembic-Revision auf den Kopf 0038**, mit Schild im
selben Commit (Punkt 23), ORM und DDL synchron (Punkt 21).

> ⚠ **`seam_before` (D-06a) ist NICHT von Andre freigegeben.** Die Spalte ist die technische
> Ableitung des Orchestrators aus der F-4-Korrektur („Naht-Pause = NULL statt 0"); CONTEXT.md
> nennt sie ausdruecklich als **den ersten Punkt, den der Cross-AI-Review angreifen soll**.
> Dazu kommt der Befund dieser Planung: **beide** Naht-Quellen sind im heutigen Code
> unerreichbar (Reconnect: `deepgram_service.py:764-766` loescht vorher den Log; Pause:
> `state['is_paused']` hat 0 Setzer bei 7 Fundstellen). Die Spalte ist damit **bewusste
> Vorsorge, keine gesicherte Entscheidung** — sie wird vor Plan 06 durch
> `/gsd-review` (zwei Sichten, Pflicht bei 🟡) erneut aufgerufen. Faellt sie dort, entfaellt
> die vierte Spalte hier und der Marker in Plan 04/05; die drei Zahlen-Spalten bleiben davon
> unberuehrt.

Purpose: Ohne diese Spalten hat die Naht aus Plan 04/05 kein Ziel. Die Migration ist der
**kleine** Teil dieser Phase — sie ist bewusst als eigener, kleiner Plan gefasst, damit sie den
Blick nicht auf sich zieht.
Output: `0039_zeitstempel1_sprechzeiten.py` + vier `comment=` in `database/models.py`.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-CONTEXT.md
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-RESEARCH.md
@CLAUDE.md
@CLAUDE-BAUREGELN-13-14-20.md
@database/models.py
@alembic/versions/0036_messgeraete1_ttft_ms.py
@alembic/versions/0038_sofort2_meetings_schild.py

<interfaces>
Heutiger Stand — `database/models.py:966-982`:

```python
class TranscriptSegment(Base):
    __tablename__ = 'transcript_segments'
    id                  = Column(BigInteger, primary_key=True, autoincrement=True)
    conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id', ondelete='CASCADE'), nullable=False)
    ts_ms               = Column(Integer, nullable=False, comment='ms ab Call-Start, fuer Reihenfolge')
    speaker             = Column(Text, nullable=False, comment="Sprecher: 'berater'|'kunde'|'system' (CHECK)")
    text                = Column(Text, nullable=False, comment='Anonymisierter Segment-Text (Pipeline B)')
    created_at          = Column(DateTime(timezone=True), server_default=func.now())
    __table_args__ = (
        CheckConstraint("speaker IN ('berater', 'kunde', 'system')", name='ck_transcript_segments_speaker'),
        Index('idx_transcript_segments_conv_ts', 'conversation_log_id', 'ts_ms'),
        {'comment': 'Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B). Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt Anonymisierungs-Pipeline; liest Analyse-/Anzeige-Pfad.'},
    )
```

Hausmuster fuer Schild-Migrationen (`0036`/`0038`, verifiziert):
`op.execute("COMMENT ON {} IS '{}'".format(objekt, text.replace("'", "''")))`
</interfaces>
</context>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen

- `public.transcript_segments` — **Schema-Aenderung (ADD COLUMN) + COMMENT**. Keine Datenzeile
  wird geschrieben, geaendert oder geloescht.

### inspect.sh-Beleg (verbatim gegen Production, 178.104.82.166, 2026-08-10)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
                                           Table "public.transcript_segments"
       Column        |           Type           | Collation | Nullable |                     Default
---------------------+--------------------------+-----------+----------+-------------------------------------------------
 id                  | bigint                   |           | not null | nextval('transcript_segments_id_seq'::regclass)
 conversation_log_id | integer                  |           | not null |
 ts_ms               | integer                  |           | not null |
 speaker             | text                     |           | not null |
 text                | text                     |           | not null |
 created_at          | timestamp with time zone |           |          | now()
Indexes:
    "transcript_segments_pkey" PRIMARY KEY, btree (id)
    "idx_transcript_segments_conv_ts" btree (conversation_log_id, ts_ms)
Check constraints:
    "transcript_segments_speaker_check" CHECK (speaker = ANY (ARRAY['berater'::text, 'kunde'::text, 'system'::text]))
Foreign-key constraints:
    "transcript_segments_conversation_log_id_fkey" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id) ON DELETE CASCADE
```

```
$ ssh ... 'bash scripts/inspect.sh constraints transcript_segments'
                   conname                    |                                 pg_get_constraintdef
----------------------------------------------+----------------------------------------------------------------------------------------
 transcript_segments_speaker_check            | CHECK ((speaker = ANY (ARRAY['berater'::text, 'kunde'::text, 'system'::text])))
 transcript_segments_pkey                     | PRIMARY KEY (id)
 transcript_segments_conversation_log_id_fkey | FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id) ON DELETE CASCADE
(3 rows)
```

```
$ ssh ... 'bash scripts/inspect.sh count transcript_segments'
 count
-------
   627
(1 row)

$ ssh ... 'bash scripts/inspect.sh count conversation_logs'
 count
-------
    77
(1 row)
```

```
$ ssh ... 'bash scripts/inspect.sh migrations'
 version_num
-------------
 0038
(1 row)
```

```
$ ssh ... 'bash scripts/inspect.sh schilder transcript_segments'
── Schild: transcript_segments (Tabelle) ──
 public | Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B).
          Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt Anonymisierungs-Pipeline;
          liest Analyse-/Anzeige-Pfad.
── Spalten-Schilder ──
 id                  |
 conversation_log_id |
 ts_ms               | ms ab Call-Start, fuer Reihenfolge
 speaker             | Sprecher: 'berater'|'kunde'|'system' (CHECK)
 text                | Anonymisierter Segment-Text (Pipeline B)
 created_at          |
── Migrations-Historie ──
  0015_table_column_comments_schilder.py:
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Kann ein Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|---|
| `TranscriptSegment.ts_ms` | geschrieben `routes/app_routes.py:552`; gelesen von 5 Modulen | DB-Spalte `transcript_segments.ts_ms` | Batch am Call-Ende | Nein (Gate `transcript_resolved`) | ✓ inspect.sh oben |
| `TranscriptSegment.start_ms` **(NEU)** | geschrieben `routes/app_routes.py` (Plan 05) | DB-Spalte — **existiert heute NICHT**, wird von 0039 angelegt | Batch am Call-Ende, identisch mit `ts_ms` | Nein — gleicher INSERT, gleiche Transaktion | ⏳ inspect.sh oben belegt den IST-Zustand (Spalte fehlt) |
| `TranscriptSegment.end_ms` **(NEU)** | dito | dito | dito | Nein | ⏳ dito |
| `TranscriptSegment.word_count` **(NEU)** | dito | dito | dito | Nein | ⏳ dito |
| `TranscriptSegment.seam_before` **(NEU)** | dito | dito | dito | Nein | ⏳ dito |

**Punkt 26 (Bereitschafts-Naht):** Die vier neuen Spalten entstehen im **selben** INSERT wie
`ts_ms`/`speaker`/`text` — es gibt keinen zweiten Schreiber und keinen zeitlichen Versatz. Der
Zeit-Anker der Phase ist `start_ms`/`end_ms` (Sprech-Zeit), **nie** `created_at`
(Batch-Schreibzeit; alle Zeilen einer Gruppe tragen denselben Wert, im Sample oben belegt).

### Bau-Regel 13 — Real-Daten-Validation

1. **Sample gezogen:** `inspect.sh sample transcript_segments 20` (Output in RESEARCH.md, Zeilen
   653-664) — 627 Zeilen ueber 77 `conversation_logs`.
2. **Gegen das neue Schema gehalten:** die Aenderung ist rein additiv, alle vier Spalten sind
   `nullable=True` ohne `server_default`. Jede Bestandszeile bleibt gueltig und bekommt `NULL`.
   Es gibt **keinen** `extra='forbid'`-artigen Strict-Mode, der eine Bestandszeile ablehnen
   koennte, und **kein** Backfill (D-03: die Rohzeiten existieren nicht mehr).
3. **Findings-Tabelle:** keine Type-Konvertierung noetig, keine Zeile braucht Migrations-Logik.
4. **Approval-Bedingung (b) erfuellt:** es werden keine Felder geloescht, `ts_ms` bleibt
   unangetastet.
5. **Test-Pflicht:** der bestehende `tests/test_schild_guard.py` laeuft im Deploy-Tor gegen
   `nerve_test` (per `pg_dump`-Restore + `alembic upgrade head` aufgebaut) und wird rot, wenn eine
   der vier neuen Spalten kein Schild >= 10 Zeichen traegt. Das ist der automatische Real-Schema-Test.

## Verbindungs-Karte (Punkt 22) — grep-belegt, nichts angenommen

### 1. Namen / Identifier — Kandidaten und Treffer im aktiven Produktionspfad

```
$ grep -rn "start_ms\|end_ms\|word_count\|seam_before" services/ routes/ database/ alembic/ --include=*.py | wc -l
0        (Stand vor dieser Phase — die vier Namen sind FREI, keine Kollision)
```
**Existenz-Anker daneben (Bau-Regel 20), damit "0" nicht "nichts gelesen" heisst:**
```
$ grep -rn "ts_ms" services/ routes/ database/ --include=*.py | wc -l
> 0      (bekanntes Muster, der grep liest also wirklich)
```
Der Executor fuehrt beide Zaehlungen VOR dem Edit aus und schreibt die Zahlen ins SUMMARY.

### 2. Tabellen / Spalten — gegen Production verifiziert

Siehe `inspect.sh schema/constraints/count/migrations/schilder` oben, verbatim. Ergebnis:
Tabelle existiert, 627 Zeilen, Kopf-Revision `0038`, die vier Zielspalten existieren **nicht**.

### 3. Wer schreibt / wer liest — vollstaendig, grep-belegt 2026-08-10

```
$ grep -rln "TranscriptSegment" routes/ services/ database/ tests/ --include=*.py
routes/app_routes.py
routes/learning.py
routes/settings.py
services/adoption_runner.py
services/judge_runner.py
services/slow_lane.py
database/models.py
tests/test_adoption_runner.py
```

| Rolle | Datei:Zeile | Was |
|---|---|---|
| **Schreiber (einziger)** | `routes/app_routes.py:543-556` | `db_conv.add(TranscriptSegment(...))`, gebuendelt am Call-Ende, idempotent per Reentrance-Guard `:544-547` |
| Leser | `services/adoption_runner.py:267-271` | ganzes Transkript, `ts_ms ASC` |
| Leser | `services/judge_runner.py:371-373` | `ts_ms ASC, id ASC` |
| Leser | `services/slow_lane.py:205-209` | filtert `speaker == 'berater'` **und `created_at >= ev.timestamp`** — die in Punkt 26 dokumentierte Batch-Write-Falle |
| Leser | `routes/learning.py:85-87, 257-259, 628-630` | drei Query-Stellen, `ts_ms, id` sortiert |
| Leser | `routes/settings.py:208-209` | `.count()` ueber `conversation_log_id.in_(...)` |
| Definition | `database/models.py:966-982` | ORM |

> ⚠ **Fund dieser Planung, der in CONTEXT.md und RESEARCH.md fehlt:** dort sind **drei** Leser
> genannt (`adoption_runner`, `judge_runner`, `slow_lane`). Tatsaechlich sind es **fuenf** —
> `routes/learning.py` und `routes/settings.py` fehlten. Fuer Punkt 23 ist das nicht kosmetisch:
> ein Schild, das nicht **alle** Leser nennt, gilt als nutzlos. Das neue Tabellen-Schild nennt
> deshalb alle fuenf.

**Vertraeglichkeit mit NULL:** alle fuenf Leser selektieren ueber die ORM-Klasse
(`db.query(TranscriptSegment)`), keiner nennt eine explizite Spaltenliste, keiner filtert oder
sortiert auf eine der vier neuen Spalten. Vier zusaetzliche `NULL`-Spalten aendern fuer sie
nichts. `routes/settings.py` zaehlt nur.

**Nicht betroffen (Abwesenheits-Pruefung + Existenz-Anker):**
```
$ grep -rn "TranscriptSegment\|transcript_segments" nerve_rt/ | wc -l
0
$ grep -rn "TranscriptSegment" routes/ services/ database/ --include=*.py | wc -l
37       (Existenz-Anker: der grep liest)
```

### 4. Reichweite

- Keine RLS-Policy auf `transcript_segments` (der `constraints`-Output nennt keine; die Tabelle
  liefert `inspect.sh sample`-Daten ohne Tenant-GUC — waere bei FORCE-RLS nicht moeglich).
- Kein Trigger, kein View, keine Fremd-FK **auf** diese Tabelle (nur die FK **von** ihr auf
  `conversation_logs`).
- `alembic upgrade head` laeuft im Deploy-Tor bereits gegen `nerve_test` (`deploy.sh:187`) —
  die Revision wird dort automatisch mitgeprueft, bevor sie Production erreicht.

<tasks>

<task type="auto">
  <name>Task 1: Vier Spalten + Schild in database/models.py</name>

  <read_first>
    - `database/models.py:966-982` (`class TranscriptSegment`, vollstaendig — inkl. des Kommentars zur `text`-Shadowing-Falle)
    - `database/models.py` Kopf: welche Typen sind importiert (`Boolean` vorhanden? sonst ergaenzen)
    - `alembic/versions/0036_messgeraete1_ttft_ms.py` (Muster: Schild-Konstanten zeichengleich zu models.py)
    - `CLAUDE.md` Punkt 23 (Schild-Pflicht + Aktualitaets-Pflicht)
  </read_first>

  <action>
**Schritt 0 (Pflicht-grep, Bau-Regel 20) — Ergebnis ins SUMMARY:**
```
grep -rn "start_ms\|end_ms\|word_count\|seam_before" services/ routes/ database/ alembic/ --include=*.py | wc -l    # erwartet: 0
grep -rn "ts_ms" services/ routes/ database/ --include=*.py | wc -l                                                 # Existenz-Anker, erwartet: > 0
```

**Schritt 1 — Import:** sicherstellen, dass `Boolean` aus `sqlalchemy` importiert ist. Falls
nicht, in die bestehende Import-Zeile aufnehmen (**keine** Import-Umsortierung, Reparatur-Modus).

**Schritt 2 — vier Spalten** nach `text` und **vor** `created_at` einfuegen. Typwahl begruendet:
`Integer` (nicht `BigInteger`) — ein 15-Minuten-Anruf sind 900.000 ms, der Integer-Bereich reicht
fuer rund 24 Tage Audio; `BigInteger` waere doppelter Speicher ohne Nutzen. `Boolean` fuer
`seam_before`, weil die Aussage genau zwei bekannte Zustaende plus "unbekannt" hat.

```python
    start_ms            = Column(Integer, nullable=True, comment=_SCHILD_START_MS)
    end_ms              = Column(Integer, nullable=True, comment=_SCHILD_END_MS)
    word_count          = Column(Integer, nullable=True, comment=_SCHILD_WORD_COUNT)
    seam_before         = Column(Boolean, nullable=True, comment=_SCHILD_SEAM_BEFORE)
```

**Schritt 3 — die vier Schild-Texte als Modul-Konstanten** direkt oberhalb der Klasse anlegen
(Muster `0036`). Diese vier Zeichenketten muessen in Task 2 **zeichengleich** wiederverwendet
werden:

```python
# ── Phase 08.23.2.ZEITSTEMPEL-1 — Schild-Texte, zeichengleich zu alembic 0039 (Punkt 23) ──
_SCHILD_START_MS = 'Beginn des Abschnitts in ms auf der DEEPGRAM-AUDIO-Achse (erstes Wort, Reconnect-Versatz addiert). NICHT dieselbe Achse wie ts_ms (Wall-Clock, auf ganze Sekunden gerundet) - nie gegeneinander rechnen. NULL = unbekannt (Zeile ohne Deepgram-Wortzeiten, z.B. EWB-Knopf-Zeile, oder Anruf vor ZEITSTEMPEL-1). Verworfen wurde die dritte Variante (ts_ms einfach genau machen): dann stuenden Alt-Anrufe sekundengenau und Neu-Anrufe millisekundengenau in DERSELBEN Spalte, ein Vergleich ueber die Zeit ergaebe still Unsinn.'
_SCHILD_END_MS = 'Ende des Abschnitts in ms auf der DEEPGRAM-AUDIO-Achse (letztes Wort, Reconnect-Versatz addiert). end_ms minus start_ms ist die Sprech-Dauer des Abschnitts - Grundlage fuer Redeanteil, Sprechtempo, Redeblock-Laenge und Pausenlaenge (gerechnet in METRIK-1, nicht hier). NULL = unbekannt, nie 0.'
_SCHILD_WORD_COUNT = 'Anzahl gesprochener Woerter aus den ROHEN Deepgram-Wortobjekten, gezaehlt VOR der Anonymisierung. Nicht aus dem anonymisierten Text zaehlen: der Platzhalter [PERSON_A] steht fuer zwei gesprochene Woerter. NULL = unbekannt; 0 hiesse hat nichts gesagt und wuerde jeden Mittelwert verfaelschen.'
_SCHILD_SEAM_BEFORE = 'TRUE = vor diesem Abschnitt liegt eine NAHT (neue Deepgram-Verbindung oder zurueckgehaltenes Audio); die Luecke davor ist damit UNBEKANNT und darf in keine Pausen-Statistik eingehen - eine 0 waere ein Wert und wuerde jeden Mittelwert nach unten ziehen. FALSE = keine Naht, die Luecke ist rechenbar. NULL = unbekannt (Zeile ohne Deepgram-Wortzeiten oder Anruf vor ZEITSTEMPEL-1). Der Abschnitt SELBST bleibt gueltig - nur die Luecke DAVOR ist unbekannt.'
_SCHILD_TABELLE = 'Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B). Status: lebt (neue Architektur Phase 08.23.2.A+). ZWEI Zeitachsen, nie mischen: ts_ms = Wall-Clock ab Call-Start auf ganze Sekunden gerundet, nur fuer die Reihenfolge; start_ms/end_ms = Deepgram-Audio-Zeit in ms, nur fuer Messgroessen. Gebuendelt am Call-Ende geschrieben - alle created_at einer Gruppe sind identisch, created_at ist KEIN Zeit-Anker (Punkt 26). Schreibt routes/app_routes.py api_beenden (Quelle: RAM-Log aus services/deepgram_service.py on_message und EWB-Knopf); liest services/adoption_runner.py, services/judge_runner.py, services/slow_lane.py (ankert auf created_at), routes/learning.py, routes/settings.py.'
```

**Schritt 4 — Tabellen-Schild ersetzen:** in `__table_args__` den bestehenden
`{'comment': 'Anonymisierte Transkript-Segmente ... liest Analyse-/Anzeige-Pfad.'}`-Eintrag durch
`{'comment': _SCHILD_TABELLE}` ersetzen. Begruendung: der **Schreib-Pfad aendert sich** (vier
neue Spalten, neue Quelle), und die alte `Schreibt/liest`-Zeile nannte weder den echten Schreiber
noch die fuenf Leser — nach Punkt 23 Aktualitaets-Pflicht ist so ein Schild nutzlos.

**Nichts anderes an `TranscriptSegment` anfassen:** `ts_ms` behaelt Typ, `nullable=False`,
Kommentar und Position. Der `Index('idx_transcript_segments_conv_ts', ...)` bleibt unveraendert —
**kein neuer Index auf `start_ms`**: alle fuenf Leser holen die Zeilen eines Calls ueber
`conversation_log_id` und sortieren im Speicher; ein zweiter Index kostete Schreibzeit bei jedem
Batch-INSERT ohne einen einzigen Leser zu beschleunigen (Punkt 27).
  </action>

  <acceptance_criteria>
    - `grep -c "nullable=True, comment=_SCHILD_" database/models.py` liefert **4** — die vier
      Spalten-Zeilen aus der Vorlage oben, unabhaengig von der Zeilen-Umbruch-Form der
      Schild-Texte. Zweiter, breiterer Anker: `grep -c "_SCHILD_" database/models.py` liefert
      **10** (5 Definitionen + 4 Spalten-Verweise + 1 Tabellen-Verweis).
      ⚠ Ein `grep -c "start_ms\|end_ms\|word_count\|seam_before"` waere hier ein schlechter
      Anker: die Konstanten-NAMEN sind gross geschrieben (`_SCHILD_START_MS`) und treffen das
      Muster nicht — er zaehlte 6, nicht 9.
    - `grep -c "nullable=True" database/models.py` steigt gegenueber dem Vorstand um **4**
      (Vorher-Wert vor dem Edit messen und ins SUMMARY schreiben).
    - Abwesenheits-Anker: `grep -c "ts_ms *= *Column(Integer, nullable=False" database/models.py`
      liefert weiterhin **1** — `ts_ms` ist unangetastet. Gepaarter Existenz-Anker:
      `grep -c "class TranscriptSegment" database/models.py` liefert **1**.
    - Der Diff beruehrt **nur** die Klasse `TranscriptSegment` und ggf. die `sqlalchemy`-Importzeile
      (`git diff --stat` nennt genau eine Datei; `git diff` zeigt keine fremde Klasse).
  </acceptance_criteria>

  <verify>
    <automated>VERTAGT auf Plan 06 (`bash deploy.sh production`, von Andre gefahren) — dort laeuft `alembic upgrade head` gegen nerve_test und `tests/test_schild_guard.py` prueft die vier neuen Schilder scharf gegen pg_description. KEIN lokaler pytest, KEIN lokales python.</automated>
  </verify>

  <done>Vier Spalten mit `comment=` und das neue Tabellen-Schild stehen in `database/models.py`.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic-Revision 0039 auf Kopf 0038 — DDL + COMMENT</name>

  <read_first>
    - `alembic/versions/0036_messgeraete1_ttft_ms.py` (vollstaendig — `add_column` + `_comment()`-Helfer + Docstring-Stil)
    - `alembic/versions/0038_sofort2_meetings_schild.py` (vollstaendig — `_q()`-Helfer, `_ALT`-Konstanten fuer `downgrade()`)
    - `database/models.py` nach Task 1 (die fuenf Schild-Konstanten, zum zeichengleichen Kopieren)
    - `inspect.sh migrations`-Beleg oben (Kopf ist `0038`)
  </read_first>

  <action>
Neue Datei `alembic/versions/0039_zeitstempel1_sprechzeiten.py`.

**Vor dem Schreiben verifizieren** (nicht aus dem Plan uebernehmen, D-09):
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
Steht dort etwas anderes als `0038`, ist `down_revision` entsprechend anzupassen und die
Abweichung im SUMMARY zu vermerken.

```python
"""transcript_segments: Sprech-Zeiten (start_ms/end_ms/word_count/seam_before) + Schild-Nachzug

Revision ID: 0039
Revises: 0038
Create Date: 2026-08-10

Phase 08.23.2.ZEITSTEMPEL-1. Vier nullable Spalten, kein Backfill, keine Bestandszeile
angefasst (D-03: die Deepgram-Rohzeiten alter Anrufe existieren nicht mehr, es gibt kein
Nachtragen — genau deshalb ist diese Phase nicht nachholbar).

WARUM VIER STATT DREI SPALTEN: seam_before traegt die Aussage, dass die Luecke VOR einem
Abschnitt unbekannt ist (Naht durch neue Verbindung oder zurueckgehaltenes Audio). Die
Pausenlaenge wird spaeter als naechster.start_ms - voriger.end_ms gerechnet; eine ganze Zahl
kann nicht unbekannt sagen, und eine 0 waere ein Wert, der jeden Mittelwert nach unten zieht.
Die billigere Alternative (start_ms des Abschnitts nach der Naht auf NULL lassen) wurde
verworfen, weil sie die DAUER dieses Abschnitts mit vernichtet (end_ms - start_ms waere NULL)
und damit den Redeanteil verfaelscht.

WARUM ALEMBIC UND NICHT _migrate(): app._migrate() returnt auf Postgres frueh (app.py:140) —
das Muster ist auf dem Live-Server wirkungslos und ausserdem das Alt-Muster (Punkt 29).

Prod-Kopf bei Planung (inspect.sh migrations, 2026-08-10): version_num = 0038.

Die Texte unten spiegeln database/models.py (comment=) ZEICHENGLEICH (Punkt 23).
"""
from alembic import op
import sqlalchemy as sa

revision = '0039'
down_revision = '0038'
branch_labels = None
depends_on = None

_SCHILD_START_MS = '<zeichengleich aus database/models.py>'
_SCHILD_END_MS = '<zeichengleich aus database/models.py>'
_SCHILD_WORD_COUNT = '<zeichengleich aus database/models.py>'
_SCHILD_SEAM_BEFORE = '<zeichengleich aus database/models.py>'
_SCHILD_TABELLE_NEU = '<zeichengleich aus database/models.py>'

# Die Fassung aus 0015 — fuer downgrade() (verbatim aus inspect.sh schilder, 2026-08-10).
_SCHILD_TABELLE_ALT = 'Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B). Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt Anonymisierungs-Pipeline; liest Analyse-/Anzeige-Pfad.'


def _comment(objekt: str, text: str) -> None:
    op.execute("COMMENT ON {} IS '{}'".format(objekt, text.replace("'", "''")))


def upgrade():
    op.add_column('transcript_segments', sa.Column('start_ms', sa.Integer(), nullable=True))
    op.add_column('transcript_segments', sa.Column('end_ms', sa.Integer(), nullable=True))
    op.add_column('transcript_segments', sa.Column('word_count', sa.Integer(), nullable=True))
    op.add_column('transcript_segments', sa.Column('seam_before', sa.Boolean(), nullable=True))
    _comment('COLUMN transcript_segments.start_ms', _SCHILD_START_MS)
    _comment('COLUMN transcript_segments.end_ms', _SCHILD_END_MS)
    _comment('COLUMN transcript_segments.word_count', _SCHILD_WORD_COUNT)
    _comment('COLUMN transcript_segments.seam_before', _SCHILD_SEAM_BEFORE)
    _comment('TABLE transcript_segments', _SCHILD_TABELLE_NEU)


def downgrade():
    _comment('TABLE transcript_segments', _SCHILD_TABELLE_ALT)
    op.drop_column('transcript_segments', 'seam_before')
    op.drop_column('transcript_segments', 'word_count')
    op.drop_column('transcript_segments', 'end_ms')
    op.drop_column('transcript_segments', 'start_ms')
```

**Kein `server_default`** auf einer der vier Spalten — ein Default wuerde Bestandszeilen (und
kuenftige Knopf-Zeilen) mit einem **Wert** befuellen und damit genau die 0-statt-NULL-Falle
erzeugen, die D-04/D-06 verbieten.
  </action>

  <acceptance_criteria>
    - Datei `alembic/versions/0039_zeitstempel1_sprechzeiten.py` existiert.
    - `grep -c "add_column('transcript_segments'" alembic/versions/0039_zeitstempel1_sprechzeiten.py` liefert **4**.
    - `grep -c "COMMENT ON" alembic/versions/0039_zeitstempel1_sprechzeiten.py` liefert **1**
      (der Helfer) — plus `grep -c "_comment(" ...` liefert **7**: 1 Definitions-Zeile
      (`def _comment(...)`) + 5 Aufrufe in `upgrade()` + 1 Aufruf in `downgrade()`.
      Gegen die Vorlage oben ausgezaehlt, nicht geschaetzt.
    - Abwesenheits-Anker: `grep -c "server_default" alembic/versions/0039_zeitstempel1_sprechzeiten.py`
      liefert **0**. Gepaarter Existenz-Anker: `grep -c "nullable=True" ...` liefert **4**.
    - Die fuenf Schild-Konstanten sind zeichengleich zu `database/models.py`. Pruefung ohne
      Selbstbezug: fuer jede Konstante liefert
      `grep -c "<die ersten 40 Zeichen des Textes>" database/models.py alembic/versions/0039_zeitstempel1_sprechzeiten.py`
      in **beiden** Dateien **1**.
    - `down_revision = '0038'` stimmt mit dem frisch gezogenen `inspect.sh migrations` ueberein.
  </acceptance_criteria>

  <verify>
    <automated>VERTAGT auf Plan 06: `bash deploy.sh production` (Andre) fuehrt `alembic upgrade head` gegen nerve_test aus (deploy.sh:187) — eine kaputte Revision bricht dort ab. Danach read-only: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'`</automated>
  </verify>

  <done>`0039_zeitstempel1_sprechzeiten.py` existiert, `down_revision='0038'`, vier `add_column`, fuenf Schilder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anwendung -> Postgres (DDL) | Die Migration fuehrt `ALTER TABLE` auf einer Produktions-Tabelle mit 627 Zeilen aus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ZS1-06 | Tampering | `public.transcript_segments` Bestandsdaten | mitigate | Nur `ADD COLUMN ... NULL` ohne `server_default` und ohne `UPDATE`. Keine Bestandszeile wird angefasst; `downgrade()` ist `DROP COLUMN` der vier neuen Spalten und beruehrt `ts_ms`/`text` nicht. |
| T-ZS1-07 | Denial of Service | Produktions-DB waehrend der Migration | accept | `ADD COLUMN` ohne Default ist in Postgres 11+ eine reine Katalog-Aenderung (kein Table-Rewrite); bei 627 Zeilen ohnehin unmessbar. |
| T-ZS1-08 | Information Disclosure | Schild-Texte in `pg_description` | accept | Die Schild-Texte enthalten ausschliesslich Struktur-Erklaerungen, keine Nutzer- oder Gespraechsdaten. `pg_description` ist ohnehin nur fuer DB-Rollen lesbar. |
| T-ZS1-09 | Tampering | ORM/DDL-Drift | mitigate | Die Schild-Texte sind Modul-Konstanten und werden zeichengleich in beiden Dateien gefuehrt; `tests/test_schild_guard.py` prueft im Deploy-Tor gegen `pg_description` von `nerve_test`. |
</threat_model>

<verification>
Kein Local-Dev. Alle Pruefungen laufen im Deploy-Tor auf dem Server (Plan 06) bzw. read-only:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schilder transcript_segments'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
</verification>

<success_criteria>
- [ ] Vier nullable Spalten in `database/models.py` mit `comment=`
- [ ] Tabellen-Schild nennt den echten Schreiber und **alle fuenf** Leser
- [ ] `0039_zeitstempel1_sprechzeiten.py` auf `down_revision='0038'`
- [ ] Kein `server_default`, kein Backfill, kein neuer Index
- [ ] `ts_ms` unveraendert (Typ, `nullable=False`, Kommentar, Index)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-03-SUMMARY.md`
</output>
