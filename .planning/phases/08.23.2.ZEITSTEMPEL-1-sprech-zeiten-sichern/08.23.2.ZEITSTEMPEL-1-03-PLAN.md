---
phase: 08.23.2.ZEITSTEMPEL-1
plan: 03
type: execute
wave: 3
depends_on: ["08.23.2.ZEITSTEMPEL-1-02"]
files_modified:
  - database/models.py
  - alembic/versions/0039_zeitstempel1_sprechzeiten.py
autonomous: true
requirements: [D-02, D-03, D-09, D-10]
must_haves:
  truths:
    - "transcript_segments hat DREI neue nullable Spalten: start_ms, end_ms, word_count"
    - "Jede der drei Spalten traegt ein Schild in der DB (pg_description), nicht nur im Code"
    - "Das Tabellen-Schild nennt ALLE fuenf heutigen Leser, den geaenderten Schreib-Pfad, die Cold-Call-Grenze und den Weg-C-Platzhalter"
    - "Die 627 Bestandszeilen ueberleben unveraendert mit NULL in allen drei Spalten"
    - "ts_ms bleibt unveraendert - Typ, Nullability, Bedeutung, Index"
    - "Es gibt KEINE vierte Spalte seam_before und keinen Boolean-Import"
  artifacts:
    - path: "database/models.py"
      provides: "ORM-Definition der drei Spalten mit comment= plus neues Tabellen-Schild"
      contains: "_SCHILD_WORD_COUNT"
    - path: "alembic/versions/0039_zeitstempel1_sprechzeiten.py"
      provides: "DDL + COMMENT ON COLUMN + Tabellen-Schild-Nachzug, Revision auf Kopf 0038"
      contains: "COMMENT ON"
  key_links:
    - from: "alembic/versions/0039_zeitstempel1_sprechzeiten.py"
      to: "public.transcript_segments"
      via: "op.add_column + op.execute COMMENT"
      pattern: "add_column\\('transcript_segments'"
    - from: "database/models.py::TranscriptSegment"
      to: "alembic/versions/0039_zeitstempel1_sprechzeiten.py"
      via: "zeichengleiche Schild-Texte (Punkt 23)"
      pattern: "comment="
---

<objective>
Die **drei** neuen Spalten anlegen — als **Alembic-Revision auf den Kopf 0038**, mit Schild im
selben Commit (Punkt 23), ORM und DDL synchron (Punkt 21).

Purpose: Ohne diese Spalten hat die Naht aus Plan 04/05 kein Ziel. Die Migration ist der
**kleine** Teil dieser Phase — sie ist bewusst als eigener, kleiner Plan gefasst, damit sie den
Blick nicht auf sich zieht.
Output: `0039_zeitstempel1_sprechzeiten.py` + drei `comment=` + ein neues Tabellen-Schild in
`database/models.py`.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
</objective>

<replan_notiz>
## Was dieser Replan geaendert hat (Cross-AI 2026-08-10)

| Aenderung | Ausloesender Review-Befund |
|---|---|
| **VIER Spalten -> DREI.** `seam_before` faellt vollstaendig weg | Beide Sichten DEFER auf D-06a; die zwei Uhren sind bereits das Naht-Signal |
| **Der konditionale `Boolean`-Import-Schritt faellt ersatzlos weg** | Sicht 2: `Boolean` ist in `database/models.py:2` ohnehin schon importiert — der Schritt war ein No-op, und ohne die vierte Spalte braucht es ihn gar nicht |
| **Der Warn-Kasten „seam_before ist nicht freigegeben" entfernt** | Die Frage ist entschieden: gestrichen |
| **Spalten-Schild `start_ms`** um die Ueberlappungs-Kante und das Zwei-Uhren-Naht-Signal ergaenzt | Sicht 2 MEDIUM-5: ueberlappende Endergebnisse sind aus dem Code allein nicht auszuschliessen (UNVERIFIED); ein Leser muss negative Differenzen abfangen |
| **Spalten-Schild `word_count`** um die Leer-Wortliste-Kante ergaenzt | Gemini LOW: Text ohne Wortobjekte -> NULL trotz Rede |
| **Tabellen-Schild** um die **Cold-Call-Grenze** und den **Weg-C-Platzhalter** ergaenzt | Sicht 2 HIGH-1 (Redeanteil im Cold-Call strukturell 100 %) + Andres Weg-C-Entscheidung (D-07) |
| Alle `grep -c`-Anker gegen die materialisierte Vorlage **ausgefuehrt** | Sicht 2 HIGH-3: diese Anker wurden gelesen statt ausgefuehrt |
</replan_notiz>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-CONTEXT.md
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-RESEARCH.md
@CLAUDE.md
@CLAUDE-BAUREGELN-13-14-20.md
@database/models.py
@alembic/versions/0036_messgeraete1_ttft_ms.py
@alembic/versions/0038_sofort2_meetings_schild.py

<interfaces>
Heutiger Stand — `database/models.py:966-982`:

```python
class TranscriptSegment(Base):
    __tablename__ = 'transcript_segments'
    id                  = Column(BigInteger, primary_key=True, autoincrement=True)
    conversation_log_id = Column(Integer, ForeignKey('conversation_logs.id', ondelete='CASCADE'), nullable=False)
    ts_ms               = Column(Integer, nullable=False, comment='ms ab Call-Start, fuer Reihenfolge')
    speaker             = Column(Text, nullable=False, comment="Sprecher: 'berater'|'kunde'|'system' (CHECK)")
    text                = Column(Text, nullable=False, comment='Anonymisierter Segment-Text (Pipeline B)')
    created_at          = Column(DateTime(timezone=True), server_default=func.now())
    __table_args__ = (
        CheckConstraint("speaker IN ('berater', 'kunde', 'system')", name='ck_transcript_segments_speaker'),
        Index('idx_transcript_segments_conv_ts', 'conversation_log_id', 'ts_ms'),
        {'comment': 'Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B). Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt Anonymisierungs-Pipeline; liest Analyse-/Anzeige-Pfad.'},
    )
```

Hausmuster fuer Schild-Migrationen (`0036`/`0038`, verifiziert):
`op.execute("COMMENT ON {} IS '{}'".format(objekt, text.replace("'", "''")))`
</interfaces>
</context>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen

- `public.transcript_segments` — **Schema-Aenderung (ADD COLUMN) + COMMENT**. Keine Datenzeile
  wird geschrieben, geaendert oder geloescht.

### inspect.sh-Beleg (verbatim gegen Production, 178.104.82.166, 2026-08-10)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
                                           Table "public.transcript_segments"
       Column        |           Type           | Collation | Nullable |                     Default
---------------------+--------------------------+-----------+----------+-------------------------------------------------
 id                  | bigint                   |           | not null | nextval('transcript_segments_id_seq'::regclass)
 conversation_log_id | integer                  |           | not null |
 ts_ms               | integer                  |           | not null |
 speaker             | text                     |           | not null |
 text                | text                     |           | not null |
 created_at          | timestamp with time zone |           |          | now()
Indexes:
    "transcript_segments_pkey" PRIMARY KEY, btree (id)
    "idx_transcript_segments_conv_ts" btree (conversation_log_id, ts_ms)
Check constraints:
    "transcript_segments_speaker_check" CHECK (speaker = ANY (ARRAY['berater'::text, 'kunde'::text, 'system'::text]))
Foreign-key constraints:
    "transcript_segments_conversation_log_id_fkey" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id) ON DELETE CASCADE
```

```
$ ssh ... 'bash scripts/inspect.sh constraints transcript_segments'
                   conname                    |                                 pg_get_constraintdef
----------------------------------------------+----------------------------------------------------------------------------------------
 transcript_segments_speaker_check            | CHECK ((speaker = ANY (ARRAY['berater'::text, 'kunde'::text, 'system'::text])))
 transcript_segments_pkey                     | PRIMARY KEY (id)
 transcript_segments_conversation_log_id_fkey | FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id) ON DELETE CASCADE
(3 rows)
```

```
$ ssh ... 'bash scripts/inspect.sh count transcript_segments'
 count
-------
   627
(1 row)

$ ssh ... 'bash scripts/inspect.sh count conversation_logs'
 count
-------
    77
(1 row)
```

```
$ ssh ... 'bash scripts/inspect.sh migrations'
 version_num
-------------
 0038
(1 row)
```

```
$ ssh ... 'bash scripts/inspect.sh schilder transcript_segments'
── Schild: transcript_segments (Tabelle) ──
 public | Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B).
          Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt Anonymisierungs-Pipeline;
          liest Analyse-/Anzeige-Pfad.
── Spalten-Schilder ──
 id                  |
 conversation_log_id |
 ts_ms               | ms ab Call-Start, fuer Reihenfolge
 speaker             | Sprecher: 'berater'|'kunde'|'system' (CHECK)
 text                | Anonymisierter Segment-Text (Pipeline B)
 created_at          |
── Migrations-Historie ──
  0015_table_column_comments_schilder.py:
```

### Cross-Layer-Konsistenz-Tabelle (Punkt 21 + Punkt 26)

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | **Schreib-Zeitpunkt** | Kann ein Leser vorauslaufen? | Verifiziert? |
|---|---|---|---|---|---|
| `TranscriptSegment.ts_ms` | geschrieben `routes/app_routes.py:552`; gelesen von 5 Modulen | DB-Spalte `transcript_segments.ts_ms` | **batch-am-Call-Ende** | Nein (Gate `transcript_resolved`) | ✓ inspect.sh oben |
| `TranscriptSegment.start_ms` **(NEU)** | geschrieben `routes/app_routes.py` (Plan 05) | DB-Spalte — **existiert heute NICHT**, wird von 0039 angelegt | **batch-am-Call-Ende**, identischer INSERT wie `ts_ms` | Nein — gleicher INSERT, gleiche Transaktion | ⏳ inspect.sh oben belegt den IST-Zustand (Spalte fehlt) |
| `TranscriptSegment.end_ms` **(NEU)** | dito | dito | dito | Nein | ⏳ dito |
| `TranscriptSegment.word_count` **(NEU)** | dito | dito | dito | Nein | ⏳ dito |

**Punkt 26 (Bereitschafts-Naht):** Die drei neuen Spalten entstehen im **selben** INSERT wie
`ts_ms`/`speaker`/`text` — es gibt keinen zweiten Schreiber und keinen zeitlichen Versatz. Der
Zeit-Anker der Phase ist `start_ms`/`end_ms` (Sprech-Zeit), **nie** `created_at`
(Batch-Schreibzeit; alle Zeilen einer Gruppe tragen denselben Wert, im Sample in Plan 05 belegt).

⚠ **Der EINE Fall, in dem ein Leser dem Schreiber vorauslaufen kann, ist kein Anwendungs-Fall,
sondern das MIGRATIONS-FENSTER** — und der ist der Grund fuer die umgedrehte Reihenfolge in
D-16/Plan 06: laeuft der neue ORM-Code gegen Schema 0038, wirft **jeder** der fuenf
Entity-Leser `UndefinedColumn`, nicht nur der Schreiber. Deshalb: **Migration zuerst, Deploy
danach.** Der Alt-Code ist vorwaerts-kompatibel und schreibt die neuen Spalten schlicht nicht.

### Bau-Regel 13 — Real-Daten-Validation

1. **Sample gezogen:** `inspect.sh sample transcript_segments 20` (Output verbatim in Plan 05) —
   627 Zeilen ueber 77 `conversation_logs`.
2. **Gegen das neue Schema gehalten:** die Aenderung ist rein additiv, alle drei Spalten sind
   `nullable=True` ohne `server_default`. Jede Bestandszeile bleibt gueltig und bekommt `NULL`.
   Es gibt **keinen** `extra='forbid'`-artigen Strict-Mode, der eine Bestandszeile ablehnen
   koennte, und **kein** Backfill (D-03: die Rohzeiten existieren nicht mehr).
3. **Findings-Tabelle:** keine Type-Konvertierung noetig, keine Zeile braucht Migrations-Logik.
4. **Approval-Bedingung (b) erfuellt:** es werden keine Felder geloescht, `ts_ms` bleibt
   unangetastet.
5. **Test-Pflicht:** der bestehende `tests/test_schild_guard.py` laeuft im Deploy-Tor gegen
   `nerve_test` (per `pg_dump`-Restore + `alembic upgrade head` aufgebaut) und wird rot, wenn eine
   der drei neuen Spalten kein Schild >= 10 Zeichen traegt. Das ist der automatische
   Real-Schema-Test.

## Verbindungs-Karte (Punkt 22) — grep-belegt, nichts angenommen

### 1. Namen / Identifier — Kandidaten und Treffer im aktiven Produktionspfad

Der Executor fuehrt **beide** Zaehlungen VOR dem Edit aus und schreibt die Ausgaben ins SUMMARY:
```
grep -rn "start_ms\|end_ms\|word_count" services/ routes/ database/ alembic/ --include=*.py | wc -l
        # Soll: 12 — gemessen 2026-08-10 gegen den Bestand (NICHT 0).
        # KEIN Treffer ist eine DB-Spalte oder ein Modell-Attribut. Die 12 sind:
        #   services/live_session.py:1304,1310,1317          lokale Variable word_count
        #   services/outcome_service.py:216,217              lokale Variable word_count
        #   services/outcome_service.py:260,263,264,266,268  lokale Variable block_start_ms
        #   services/deepgram_service.py:480,495             Deepgram-Options-Schluessel utterance_end_ms
        # Es gibt also keine Spalten-Kollision. Abweichung von 12 nach oben ODER unten = STOP,
        # Ursache klaeren und die Zahl mit Begruendung nachziehen — nicht stillschweigend anpassen.
grep -rn "ts_ms" services/ routes/ database/ --include=*.py | wc -l
        # Existenz-Anker (Bau-Regel 20) — gemessen 2026-08-10: 59 (Soll: > 0).
        # Der grep liest also wirklich; eine 0 hier hiesse "nichts gelesen", nicht "sauber".
```

⚠ **Nebenbefund fuer das SUMMARY (Restluecke, Adressat METRIK-1):** `services/live_session.py:1304`
zaehlt Woerter aus dem **gemergten Text** (`len(merged_text.split())`) — genau der von **D-07
verworfene Weg** (zaehlt Platzhalter statt Woerter). Diese Phase aendert das **nicht**. Nach dieser
Phase gibt es also **zwei** Wortzaehlungen mit **unterschiedlicher Bedeutung**: die fluechtige
RAM-Zaehlung in `live_session.py` (nach der Anonymisierung, Platzhalter mitgezaehlt) und die neue
Spalte `word_count` (vor der Anonymisierung, aus den Deepgram-Wortobjekten). Wer spaeter
Sprechtempo rechnet, nimmt **`word_count`**.

⚠ **Zusaetzlicher Abwesenheits-Anker fuer die gestrichene Mechanik — auf die CODE-FORM, nicht
auf den nackten Namen** (D-14: der Anker folgt dem Artefakt, nie umgekehrt). Der Name
`seam_before` MUSS in der **Streichungs-Begruendung** vorkommen duerfen (Punkt 31 verlangt sie);
ein Anker auf das blosse Vorkommen der Zeichenkette waere gegen die von diesem Plan selbst
vorgeschriebene Migrations-Vorlage **unerfuellbar** — der einzige Weg zu Gruen waere, die
Begruendung zu loeschen. Gezaehlt wird deshalb nur **aktive Mechanik**: Spalten-Anlage,
Variablen-/Kwarg-Zuweisung, Dict-Schluessel, Attribut-Zugriff, Funktionsaufruf.

Dies ist das **Code-Form-Muster** dieser Phase — Plan 03 und Plan 05 verwenden es **wortgleich**,
damit die Wellen-Reihenfolge egal ist:
```
grep -rnE 'add_column\(.*seam_before|seam_before *=|seam_before.? *:|\.seam_before|_dg_ms_offset|mark_dg_seam' services/ routes/ database/ alembic/ --include=*.py | wc -l
        # MUSS 0 sein — am 2026-08-10 gegen den Bestand AUSGEFUEHRT: 0
        # (das alte nackte Muster "seam_before" liefert nach Task 2 eine 1 und traf
        #  ausschliesslich die Streichungs-Begruendung in der Migrations-Vorlage)
```

### 2. Tabellen / Spalten — gegen Production verifiziert

Siehe `inspect.sh schema/constraints/count/migrations/schilder` oben, verbatim. Ergebnis:
Tabelle existiert, 627 Zeilen, Kopf-Revision `0038`, die drei Zielspalten existieren **nicht**.

### 3. Wer schreibt / wer liest — vollstaendig, grep-belegt 2026-08-10

```
$ grep -rln "TranscriptSegment" routes/ services/ database/ tests/ --include=*.py
routes/app_routes.py
routes/learning.py
routes/settings.py
services/adoption_runner.py
services/judge_runner.py
services/slow_lane.py
database/models.py
tests/test_adoption_runner.py
```

| Rolle | Datei:Zeile | Was |
|---|---|---|
| **Schreiber (einziger)** | `routes/app_routes.py:543-556` | `db_conv.add(TranscriptSegment(...))`, gebuendelt am Call-Ende, idempotent per Reentrance-Guard `:544-547` |
| Leser | `services/adoption_runner.py:267-271` | ganzes Transkript, `ts_ms ASC` |
| Leser | `services/judge_runner.py:370-373` | `ts_ms ASC, id ASC` |
| Leser | `services/slow_lane.py:205-209` | filtert `speaker == 'berater'` **und `created_at >= ev.timestamp`** — die in Punkt 26 dokumentierte Batch-Write-Falle |
| Leser | `routes/learning.py:85, 257, 628` | drei Query-Stellen, `ts_ms, id` sortiert |
| Leser | `routes/settings.py:208` | `.count()` ueber `conversation_log_id.in_(...)` |
| Definition | `database/models.py:966-982` | ORM |

> ⚠ **Fund dieser Planung, der in CONTEXT.md und RESEARCH.md ursprünglich fehlte:** dort waren
> **drei** Leser genannt. Tatsaechlich sind es **fuenf**. Fuer Punkt 23 ist das nicht kosmetisch:
> ein Schild, das nicht **alle** Leser nennt, gilt als nutzlos. Das neue Tabellen-Schild nennt
> deshalb alle fuenf. (CONTEXT.md ist inzwischen nachgezogen.)

**Vertraeglichkeit mit NULL:** alle fuenf Leser selektieren ueber die ORM-Klasse
(`db.query(TranscriptSegment)`), keiner nennt eine explizite Spaltenliste, keiner filtert oder
sortiert auf eine der drei neuen Spalten. Drei zusaetzliche `NULL`-Spalten aendern fuer sie
nichts — **ausserhalb des Migrationsfensters**. Innerhalb bricht genau diese Eigenschaft
(SQLAlchemy schreibt **jede** gemappte Spalte in das SELECT); siehe D-16.

**Nicht betroffen (Abwesenheits-Pruefung + Existenz-Anker):**
```
$ grep -rn "TranscriptSegment\|transcript_segments" nerve_rt/ | wc -l
0
$ grep -rn "TranscriptSegment" routes/ services/ database/ --include=*.py | wc -l
37       (Existenz-Anker, 2026-08-10 gemessen: der grep liest)
```

### 4. Reichweite

- Keine RLS-Policy auf `transcript_segments` (der `constraints`-Output nennt keine; die Tabelle
  liefert `inspect.sh sample`-Daten ohne Tenant-GUC — waere bei FORCE-RLS nicht moeglich).
- Kein Trigger, kein View, keine Fremd-FK **auf** diese Tabelle (nur die FK **von** ihr auf
  `conversation_logs`).
- `alembic upgrade head` laeuft im Deploy-Tor bereits gegen `nerve_test` (`deploy.sh:187`) —
  die Revision wird dort automatisch mitgeprueft. **Das ersetzt die Produktions-Migration NICHT**
  (D-16, Plan 06 Task 1 Schritt 2).

<tasks>

<task type="auto">
  <name>Task 1: Drei Spalten + vier Schild-Konstanten in database/models.py</name>

  <read_first>
    - `database/models.py:966-982` (`class TranscriptSegment`, vollstaendig — inkl. des Kommentars zur `text`-Shadowing-Falle)
    - `database/models.py:1-10` (Import-Zeile: `Integer` ist vorhanden; ein `Boolean`-Import wird **nicht** gebraucht, weil keine Boolean-Spalte mehr entsteht)
    - `alembic/versions/0036_messgeraete1_ttft_ms.py` (Muster: Schild-Konstanten zeichengleich zu models.py)
    - `CLAUDE.md` Punkt 23 (Schild-Pflicht + Aktualitaets-Pflicht)
    - `services/deepgram_service.py:113-118` und `:490` (die zwei Code-Anker der Cold-Call-Grenze, die im Tabellen-Schild woertlich zitiert werden)
  </read_first>

  <action>
**Schritt 0 (Pflicht-grep, Bau-Regel 20) — Ergebnisse ins SUMMARY:** die drei Zaehlungen aus
Abschnitt „Verbindungs-Karte / 1." ausfuehren. Ist die dritte (`seam_before` etc.) nicht `0`,
ist irgendwo Restmechanik liegengeblieben -> STOP.

**Schritt 1 — vier Schild-Konstanten** direkt oberhalb der Klasse `TranscriptSegment` anlegen
(Muster `0036`). Diese vier Zeichenketten muessen in Task 2 **zeichengleich** wiederverwendet
werden. Sie enthalten bewusst **keine** Apostrophe, damit die SQL-Escape-Zeile trivial bleibt:

```python
# ── Phase 08.23.2.ZEITSTEMPEL-1 — Schild-Texte, zeichengleich zu alembic 0039 (Punkt 23) ──
_SCHILD_START_MS = 'Beginn des Abschnitts in ms auf der DEEPGRAM-AUDIO-Achse (Startzeit des ersten Wortobjekts). NICHT dieselbe Achse wie ts_ms (Wall-Clock, auf ganze Sekunden gerundet) - nie gegeneinander rechnen. NULL = unbekannt (Zeile ohne Deepgram-Wortzeiten, z.B. EWB-Knopf-Zeile, oder Anruf vor ZEITSTEMPEL-1). Verworfen wurde die dritte Variante (ts_ms einfach genau machen): dann stuenden Alt-Anrufe sekundengenau und Neu-Anrufe millisekundengenau in DERSELBEN Spalte, ein Vergleich ueber die Zeit ergaebe still Unsinn. UNVERIFIED: ueberlappende Deepgram-Endergebnisse sind nicht ausgeschlossen (endpointing=900 plus smart_format); ein Leser muss end_ms minus start_ms kleiner 0 und negative Luecken abfangen. Eine negative Luecke zum Vorgaenger ist zugleich das gewollte Naht-Signal - es gibt bewusst KEINEN Naht-Marker und keinen Versatz.'
_SCHILD_END_MS = 'Ende des Abschnitts in ms auf der DEEPGRAM-AUDIO-Achse (Endzeit des letzten Wortobjekts). end_ms minus start_ms ist die Sprech-Dauer des Abschnitts - Grundlage fuer Redeanteil, Sprechtempo, Redeblock-Laenge und Pausenlaenge (gerechnet in METRIK-1, nicht hier). NULL = unbekannt, nie 0.'
_SCHILD_WORD_COUNT = 'Anzahl gesprochener Woerter aus den ROHEN Deepgram-Wortobjekten, gezaehlt VOR der Anonymisierung. Nicht aus dem anonymisierten Text zaehlen: der Platzhalter [PERSON_A] steht fuer zwei gesprochene Woerter. NULL = unbekannt; 0 hiesse hat nichts gesagt und wuerde jeden Mittelwert verfaelschen. Bekannte Kante: ein Endergebnis mit Text, aber ohne Wortobjekte (nur Satzzeichen) liefert NULL, obwohl gesprochen wurde.'
_SCHILD_TABELLE = 'Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B). Status: lebt (neue Architektur Phase 08.23.2.A+). ZWEI Zeitachsen, nie mischen: ts_ms = Wall-Clock ab Call-Start auf ganze Sekunden gerundet, nur fuer die Reihenfolge; start_ms/end_ms = Deepgram-Audio-Zeit in ms, nur fuer Messgroessen. Naht-Luecken (neue Verbindung, zurueckgehaltenes Audio) erkennt ein Leser an der Divergenz beider Uhren bzw. an einer negativen Differenz - es gibt bewusst KEINEN Naht-Marker und keinen Versatz. GEPRUEFT UND OFFEN: im Cold-Call-Modus sind Redeanteil und Redeblock-Laenge strukturell NICHT berechenbar (diarize=is_meeting in services/deepgram_service.py:490, log_sp hart 0 in :113-118) - jedes Segment landet als berater, der Redeanteil ist dort IMMER exakt 100 Prozent, eine Konstante die wie eine Messung aussieht. Sprechtempo und Pausenlaenge bleiben im Cold-Call gueltig; im Meeting-Modus kommen alle vier heraus. Abschnitte mit Art-9-Treffer oder Anonymisierungs-Fehler werden seit ZEITSTEMPEL-1 MIT echten Zeiten und dem neutralen Platzhalter-Text [nicht gespeichert] geschrieben (Weg C) - vorher entstand gar keine Zeile und ihre Sprech-Zeit fehlte still in jeder Summe. Gebuendelt am Call-Ende geschrieben - alle created_at einer Gruppe sind identisch, created_at ist KEIN Zeit-Anker (Punkt 26). Schreibt routes/app_routes.py api_beenden (Quelle: RAM-Log aus services/deepgram_service.py on_message und EWB-Knopf); liest services/adoption_runner.py, services/judge_runner.py, services/slow_lane.py (ankert auf created_at), routes/learning.py, routes/settings.py.'
```

**Schritt 2 — drei Spalten** nach `text` und **vor** `created_at` einfuegen. Typwahl begruendet:
`Integer` (nicht `BigInteger`) — ein 15-Minuten-Anruf sind 900.000 ms, der Integer-Bereich reicht
fuer rund 24 Tage Audio; `BigInteger` waere doppelter Speicher ohne Nutzen (beide Cross-AI-Sichten
bestaetigt).

```python
    start_ms            = Column(Integer, nullable=True, comment=_SCHILD_START_MS)
    end_ms              = Column(Integer, nullable=True, comment=_SCHILD_END_MS)
    word_count          = Column(Integer, nullable=True, comment=_SCHILD_WORD_COUNT)
```

⛔ **Keine vierte Spalte.** `seam_before` ist nach dem Cross-AI-Review gestrichen (D-06a) — und
mit ihr der Versatz (D-05), weil sie ein Paar sind: nur den Marker zu streichen und den Versatz
zu behalten waere die schlechteste Variante gewesen (die Naht fiele still auf ~0 zusammen).
**Kein `Boolean`-Import noetig** (er stuende ohnehin schon in `database/models.py:2` — der
frueher hier geplante konditionale Import-Schritt war ein No-op).

**Schritt 3 — Tabellen-Schild ersetzen:** in `__table_args__` den bestehenden
`{'comment': 'Anonymisierte Transkript-Segmente ... liest Analyse-/Anzeige-Pfad.'}`-Eintrag durch
`{'comment': _SCHILD_TABELLE}` ersetzen. Begruendung: der **Schreib-Pfad aendert sich** (drei
neue Spalten **und** ein zusaetzlicher Append-Pfad aus Weg C), und die alte `Schreibt/liest`-Zeile
nannte weder den echten Schreiber noch die fuenf Leser — nach Punkt 23 Aktualitaets-Pflicht ist
so ein Schild nutzlos.

**Nichts anderes an `TranscriptSegment` anfassen:** `ts_ms` behaelt Typ, `nullable=False`,
Kommentar und Position. Der `Index('idx_transcript_segments_conv_ts', ...)` bleibt unveraendert —
**kein neuer Index auf `start_ms`**: alle fuenf Leser holen die Zeilen eines Calls ueber
`conversation_log_id` und sortieren im Speicher; ein zweiter Index kostete Schreibzeit bei jedem
Batch-INSERT ohne einen einzigen Leser zu beschleunigen (Punkt 27, beide Sichten bestaetigt).
  </action>

  <acceptance_criteria>
    > **Alle Zahlen unten wurden am 2026-08-10 gegen die materialisierte Vorlage AUSGEFUEHRT**
    > (Vorlagen-Block in eine Datei geschrieben, dann gegrept), nicht abgezaehlt.

    - `grep -c "nullable=True, comment=_SCHILD_" database/models.py` liefert **3** — die drei
      Spalten-Zeilen aus der Vorlage oben. Zweiter, breiterer Anker:
      `grep -c "_SCHILD_" database/models.py` liefert **8**
      (**ausgefuehrt**: 4 Definitionen + 3 Spalten-Verweise + 1 Tabellen-Verweis).
      ⚠ Ein `grep -c "start_ms\|end_ms\|word_count"` waere hier ein schlechter Anker: die
      Konstanten-NAMEN sind gross geschrieben (`_SCHILD_START_MS`) und treffen das Muster nicht.
    - `grep -c "nullable=True" database/models.py` liefert **152** (Vorstand **149**, am
      2026-08-10 gemessen, plus 3). Der Executor misst den Vorstand vor dem Edit erneut und
      schreibt beide Zahlen ins SUMMARY — weicht der Vorstand ab, hat jemand anders die Datei
      angefasst, und der Anker wird mit Begruendung nachgezogen (nie stillschweigend gesenkt).
    - Abwesenheits-Anker: `grep -c "ts_ms *= *Column(Integer, nullable=False" database/models.py`
      liefert weiterhin **1** — `ts_ms` ist unangetastet. Gepaarter Existenz-Anker:
      `grep -c "class TranscriptSegment" database/models.py` liefert **1**.
    - **Die gestrichene Mechanik ist restlos raus:**
      `grep -c "seam_before" database/models.py` liefert **0**. Gepaarter Existenz-Anker:
      `grep -c "word_count" database/models.py` liefert **1** — die Spalten-Zeile. (Am
      Vorstand gemessen: **0**. Die Konstante heisst `_SCHILD_WORD_COUNT` und trifft das
      kleingeschriebene Muster bewusst NICHT; wer hier 2 erwartet, hat die Schreibweise
      nicht ausgefuehrt.)
    - Das Tabellen-Schild traegt die drei Pflicht-Aussagen. Ohne Selbstbezug pruefbar an
      Zeichenketten, die **nur** aus dem Schild stammen koennen:
      `grep -c "IMMER exakt 100 Prozent" database/models.py` liefert **1** (Cold-Call-Grenze),
      `grep -c "KEINEN Naht-Marker" database/models.py` liefert **2** (Tabellen- und
      `start_ms`-Schild), `grep -c "nicht gespeichert" database/models.py` liefert **1** (Weg C).
    - Der Diff beruehrt **nur** die Klasse `TranscriptSegment` und die vier Konstanten darueber
      (`git diff --stat` nennt genau eine Datei; `git diff` zeigt keine fremde Klasse und
      **keine** geaenderte Import-Zeile).
  </acceptance_criteria>

  <verify>
    <automated>VERTAGT auf Plan 06 (`bash deploy.sh production`, von Andre gefahren) — dort laeuft `alembic upgrade head` gegen nerve_test und `tests/test_schild_guard.py` prueft die drei neuen Schilder scharf gegen pg_description. KEIN lokaler pytest, KEIN lokales python.</automated>
  </verify>

  <done>Drei Spalten mit `comment=` und das neue Tabellen-Schild stehen in `database/models.py`.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic-Revision 0039 auf Kopf 0038 — DDL + COMMENT</name>

  <read_first>
    - `alembic/versions/0036_messgeraete1_ttft_ms.py` (vollstaendig — `add_column` + `_comment()`-Helfer + Docstring-Stil)
    - `alembic/versions/0038_sofort2_meetings_schild.py` (vollstaendig — `_q()`-Helfer, `_ALT`-Konstanten fuer `downgrade()`)
    - `database/models.py` nach Task 1 (die vier Schild-Konstanten, zum zeichengleichen Kopieren)
    - `inspect.sh migrations`-Beleg oben (Kopf ist `0038`)
  </read_first>

  <action>
Neue Datei `alembic/versions/0039_zeitstempel1_sprechzeiten.py`.

**Vor dem Schreiben verifizieren** (nicht aus dem Plan uebernehmen, D-09):
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
Steht dort etwas anderes als `0038`, ist `down_revision` entsprechend anzupassen und die
Abweichung im SUMMARY zu vermerken.

```python
"""transcript_segments: Sprech-Zeiten (start_ms/end_ms/word_count) + Schild-Nachzug

Revision ID: 0039
Revises: 0038
Create Date: 2026-08-10

Phase 08.23.2.ZEITSTEMPEL-1. DREI nullable Spalten, kein Backfill, keine Bestandszeile
angefasst (D-03: die Deepgram-Rohzeiten alter Anrufe existieren nicht mehr, es gibt kein
Nachtragen — genau deshalb ist diese Phase nicht nachholbar).

WARUM DREI UND NICHT VIER: eine vierte Spalte seam_before war geplant und wurde nach dem
Cross-AI-Review vom 2026-08-10 (zwei Sichten, beide unabhaengig DEFER) gestrichen — zusammen
mit dem Reconnect-Versatz, weil beide ein Paar sind. Es gibt bereits ZWEI Uhren: bei einer
Naht laeuft ts_ms (Wall-Clock) weiter, waehrend die Deepgram-Uhr steht; die Divergenz IST das
Naht-Signal. Und ohne Versatz wird naechster.start_ms minus voriger.end_ms nach einer neuen
Verbindung NEGATIV - physikalisch unmoeglich und damit ein selbsterklaerendes unbekannt, das
auch ein Leser fasst, der nie von Naehten gehoert hat. Nur den Marker zu streichen und den
Versatz zu behalten waere die schlechteste der drei Varianten gewesen: dann faellt die Naht
still auf 0 zusammen und nichts zeigt es an.

WARUM ALEMBIC UND NICHT _migrate(): app._migrate() returnt auf Postgres frueh (app.py:140) —
das Muster ist auf dem Live-Server wirkungslos und ausserdem das Alt-Muster (Punkt 29).

REIHENFOLGE BEIM AUSROLLEN (D-16, umgedreht nach Cross-AI): diese Migration laeuft auf
Production VOR dem Deploy des neuen Codes. Der Alt-Code ist vorwaerts-kompatibel (er schreibt
nur die vier Bestandsspalten), neue nullable Spalten bleiben schlicht NULL - damit gibt es
GAR KEIN Fenster. Umgekehrt braeche gegen Schema 0038 mit neuem ORM JEDER der fuenf
Entity-Leser mit UndefinedColumn, nicht nur der Schreiber.

Prod-Kopf bei Planung (inspect.sh migrations, 2026-08-10): version_num = 0038.

Die Texte unten spiegeln database/models.py (comment=) ZEICHENGLEICH (Punkt 23).
"""
from alembic import op
import sqlalchemy as sa

revision = '0039'
down_revision = '0038'
branch_labels = None
depends_on = None

_SCHILD_START_MS = '<zeichengleich aus database/models.py>'
_SCHILD_END_MS = '<zeichengleich aus database/models.py>'
_SCHILD_WORD_COUNT = '<zeichengleich aus database/models.py>'
_SCHILD_TABELLE_NEU = '<zeichengleich aus database/models.py _SCHILD_TABELLE>'

# Die Fassung aus 0015 — fuer downgrade() (verbatim aus inspect.sh schilder, 2026-08-10).
_SCHILD_TABELLE_ALT = 'Anonymisierte Transkript-Segmente pro Call (Kind von conversation_logs, Pipeline B). Status: lebt (neue Architektur Phase 08.23.2.A+). Schreibt Anonymisierungs-Pipeline; liest Analyse-/Anzeige-Pfad.'


def _comment(objekt: str, text: str) -> None:
    op.execute("COMMENT ON {} IS '{}'".format(objekt, text.replace("'", "''")))


def upgrade():
    op.add_column('transcript_segments', sa.Column('start_ms', sa.Integer(), nullable=True))
    op.add_column('transcript_segments', sa.Column('end_ms', sa.Integer(), nullable=True))
    op.add_column('transcript_segments', sa.Column('word_count', sa.Integer(), nullable=True))
    _comment('COLUMN transcript_segments.start_ms', _SCHILD_START_MS)
    _comment('COLUMN transcript_segments.end_ms', _SCHILD_END_MS)
    _comment('COLUMN transcript_segments.word_count', _SCHILD_WORD_COUNT)
    _comment('TABLE transcript_segments', _SCHILD_TABELLE_NEU)


def downgrade():
    _comment('TABLE transcript_segments', _SCHILD_TABELLE_ALT)
    op.drop_column('transcript_segments', 'word_count')
    op.drop_column('transcript_segments', 'end_ms')
    op.drop_column('transcript_segments', 'start_ms')
```

**Kein `server_default`** auf einer der drei Spalten — ein Default wuerde Bestandszeilen (und
kuenftige Knopf-Zeilen) mit einem **Wert** befuellen und damit genau die 0-statt-NULL-Falle
erzeugen, die D-04 verbietet.
  </action>

  <acceptance_criteria>
    > **Alle Zahlen unten wurden am 2026-08-10 gegen die materialisierte Vorlage AUSGEFUEHRT.**

    - Datei `alembic/versions/0039_zeitstempel1_sprechzeiten.py` existiert.
    - `grep -c "add_column('transcript_segments'" alembic/versions/0039_zeitstempel1_sprechzeiten.py`
      liefert **3**.
    - `grep -c "COMMENT ON" alembic/versions/0039_zeitstempel1_sprechzeiten.py` liefert **1**
      (der Helfer) — plus `grep -c "_comment(" ...` liefert **6**: 1 Definitions-Zeile
      (`def _comment(...)`) + 4 Aufrufe in `upgrade()` + 1 Aufruf in `downgrade()`.
    - `grep -c "drop_column('transcript_segments'" ...` liefert **3**.
    - Abwesenheits-Anker: `grep -c "server_default" alembic/versions/0039_zeitstempel1_sprechzeiten.py`
      liefert **0**. Gepaarter Existenz-Anker: `grep -c "nullable=True" ...` liefert **3**.
    - **Die gestrichene Mechanik ist restlos raus — Anker auf die CODE-FORM (D-14):**
      `grep -cE "add_column\(.*seam_before|sa\.Boolean|Column\(Boolean|seam_before *=" alembic/versions/0039_zeitstempel1_sprechzeiten.py`
      liefert **0** (**ausgefuehrt** gegen die materialisierte Vorlage, 2026-08-10).
      ⚠ Das frueher hier stehende nackte Muster `"seam_before\|Boolean"` lieferte **1** und traf
      die von diesem Plan selbst vorgeschriebene Docstring-Zeile „WARUM DREI UND NICHT VIER: eine
      vierte Spalte seam_before war geplant …". Der einzige Weg zu Gruen waere gewesen, die
      Streichungs-Begruendung zu loeschen — dann haette der Anker das Artefakt geformt.
      Gepaarte Existenz-Anker: `grep -c "sa.Integer()" ...` liefert **3** UND
      `grep -c "WARUM DREI UND NICHT VIER" alembic/versions/0039_zeitstempel1_sprechzeiten.py`
      liefert **1** — die Begruendung muss stehen bleiben (Punkt 31); wer sie beim naechsten
      Aufraeumen entfernt, macht **diesen** Anker rot, statt den Text still zu verlieren.
    - Die vier Schild-Konstanten sind zeichengleich zu `database/models.py`. Pruefung ohne
      Selbstbezug: fuer jede Konstante liefert
      `grep -c "<die ersten 40 Zeichen des Textes>" database/models.py alembic/versions/0039_zeitstempel1_sprechzeiten.py`
      in **beiden** Dateien **1**.
    - `down_revision = '0038'` stimmt mit dem frisch gezogenen `inspect.sh migrations` ueberein.
  </acceptance_criteria>

  <verify>
    <automated>VERTAGT auf Plan 06: die Migration wird von Andre auf Production gefahren (Schritt 2 vor dem Deploy, D-16); anschliessend read-only `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'` und `... inspect.sh schema transcript_segments`. Zusaetzlich faehrt `bash deploy.sh production` (Andre) `alembic upgrade head` gegen nerve_test — eine kaputte Revision bricht dort ab.</automated>
  </verify>

  <done>`0039_zeitstempel1_sprechzeiten.py` existiert, `down_revision='0038'`, drei `add_column`, vier Schilder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anwendung -> Postgres (DDL) | Die Migration fuehrt `ALTER TABLE` auf einer Produktions-Tabelle mit 627 Zeilen aus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ZS1-06 | Tampering | `public.transcript_segments` Bestandsdaten | mitigate | Nur `ADD COLUMN ... NULL` ohne `server_default` und ohne `UPDATE`. Keine Bestandszeile wird angefasst; `downgrade()` ist `DROP COLUMN` der drei neuen Spalten und beruehrt `ts_ms`/`text` nicht. |
| T-ZS1-07 | Denial of Service | Produktions-DB waehrend der Migration | accept | `ADD COLUMN` ohne Default ist in Postgres 11+ eine reine Katalog-Aenderung (kein Table-Rewrite); bei 627 Zeilen ohnehin unmessbar. |
| T-ZS1-08 | Information Disclosure | Schild-Texte in `pg_description` | accept | Die Schild-Texte enthalten ausschliesslich Struktur-Erklaerungen, keine Nutzer- oder Gespraechsdaten. `pg_description` ist ohnehin nur fuer DB-Rollen lesbar. |
| T-ZS1-09 | Tampering | ORM/DDL-Drift | mitigate | Die Schild-Texte sind Modul-Konstanten und werden zeichengleich in beiden Dateien gefuehrt; `tests/test_schild_guard.py` prueft im Deploy-Tor gegen `pg_description` von `nerve_test`. |
| T-ZS1-28 | Denial of Service | die fuenf Entity-Leser waehrend des Ausrollens | mitigate | **Migration VOR Deploy** (D-16). Der Alt-Code ist vorwaerts-kompatibel; damit existiert kein Fenster, in dem neuer ORM-Code gegen altes Schema laeuft. Die frueher geplante Reihenfolge (Deploy -> Migration) haette **alle fuenf** Leser gebrochen, nicht nur den Schreiber — inklusive `slow_lane`, das beim Neustart per `_requeue_pending()` von allein hineinlaeuft und Fehler ohne `rollback` verschluckt. |
</threat_model>

<verification>
Kein Local-Dev. Alle Pruefungen laufen auf dem Server (Plan 06) bzw. read-only:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schilder transcript_segments'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
</verification>

<success_criteria>
- [ ] **Drei** nullable Spalten in `database/models.py` mit `comment=` — keine vierte
- [ ] Tabellen-Schild nennt den echten Schreiber, **alle fuenf** Leser, die Cold-Call-Grenze und Weg C
- [ ] `0039_zeitstempel1_sprechzeiten.py` auf `down_revision='0038'`, drei `add_column`
- [ ] Kein `server_default`, kein Backfill, kein neuer Index, kein `Boolean`
- [ ] `ts_ms` unveraendert (Typ, `nullable=False`, Kommentar, Index)
- [ ] `seam_before`/`_dg_ms_offset`/`mark_dg_seam` haben **0 Treffer als aktive Mechanik** im
      gesamten Repo (Spalten-Anlage, Variablen-/Kwarg-Zuweisung, Dict-Schluessel, Attribut-Zugriff,
      Funktionsaufruf) — gemessen mit dem **Code-Form-Muster** aus §Verbindungs-Karte/1, am
      2026-08-10 ausgefuehrt: **0**. Vorkommen in **Streichungs-Begruendungen** (Docstring der
      Migration `0039`) sind ausdruecklich **erwuenscht** und zaehlen NICHT als Treffer
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-03-SUMMARY.md`
</output>
