---
phase: 08.23.2.ZEITSTEMPEL-1
plan: 02
type: execute
wave: 2
depends_on: ["08.23.2.ZEITSTEMPEL-1-01"]
files_modified:
  - .planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/ROT-LAUF.md
autonomous: false
requirements: [D-11, D-16]
must_haves:
  truths:
    - "Das serverseitige Test-Tor meldet die neuen Tests als FAILED/ERROR"
    - "Der Deploy bricht ab, es gibt KEINEN Neustart, Production laeuft unveraendert weiter"
    - "Die rote Tor-Ausgabe liegt verbatim als Datei im Phasen-Ordner"
    - "Die Art-9-Tests fallen aus dem RICHTIGEN Grund durch (gar keine Zeile), nicht aus einem beliebigen"
  artifacts:
    - path: ".planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/ROT-LAUF.md"
      provides: "Der verbatim rote Lauf als Abnahme-Artefakt (Bau-Regel 1, Punkt 31)"
      contains: "failed"
  key_links:
    - from: "tests/test_speech_timing_extraction.py"
      to: "deploy.sh Postgres-Test-Gate"
      via: "pytest tests/ gegen nerve_test (deploy.sh:221)"
      pattern: "pytest tests/"
---

<objective>
Den ROT-Lauf ziehen. Das Tor auf dem Production-Server muss wegen der neuen Tests aus Plan 01
abbrechen — **vor** jeder Zeile Produktionscode.

Purpose: Bau-Regel 1 und Punkt 31 verlangen den Rot-Beleg als eigenes Abnahme-Artefakt, gezogen
VOR dem Fix. Ein Test, der nie rot war, beweist nichts. Genau dieses Muster hat in
MEHRNUTZER-REST-1 funktioniert (`2 failed, 1140 passed` -> Tor blockte den Restart).
Output: `ROT-LAUF.md` mit der unveraenderten Tor-Ausgabe.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
</objective>

<replan_notiz>
## Was dieser Replan geaendert hat (Cross-AI 2026-08-10)

| Aenderung | Ausloesender Review-Befund |
|---|---|
| Erwartetes Rot **ohne** den Pause-Zweig-Waechter und **ohne** den Gegenpol | Folge der D-06a-Streichung (Plan 01, alter Task 3 gestrichen) |
| Erwartetes Rot **mit** den sieben Art-9-Tests | Weg C (D-07), Andre-Entscheidung |
| **Anker `grep -c "passed" ROT-LAUF.md >= 1` ersetzt** durch `grep -cE "[0-9]+ (failed\|error)"` | Sicht 2 LOG-8: die Vorlage enthielt das Wort `passed` selbst im Prosa-Abschnitt — der Anker waere gruen gewesen, auch wenn der verbatim-Block leer bleibt (D-14, im Dokument, dessen einziger Zweck Verbatimheit ist) |
| Prosa-Abschnitt der Vorlage so umformuliert, dass er **keine** Anker-Zeichenkette mehr enthaelt | D-14: kein Anker auf Prosa, die derselbe Plan vorschreibt |
| Deploy-Reihenfolge-Hinweis: dieser Lauf ist **vor** jeder Migration | D-16 (umgedreht) — hier noch ohne Wirkung, weil kein Schema-Bezug, aber der Plan sagt es, damit niemand vorgreift |
</replan_notiz>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-CONTEXT.md
@CLAUDE.md
@deploy.sh
</context>

## 5. Persistenz-Schicht-Verifikation

**Dieser Plan aendert kein Schema und schreibt keine Anwendungsdaten.** Er loest ausschliesslich
das serverseitige Test-Tor aus. Das Tor baut sich seine eigene Wegwerf-DB `nerve_test`
(`deploy.sh:160-187`: `DROP` -> `CREATE` -> `pg_dump --schema-only nerve` -> `alembic upgrade head`)
und droppt sie per `trap cleanup EXIT` wieder. **Die Produktions-DB `nerve` wird dabei nur
gelesen** (`pg_dump --schema-only`), nie geschrieben.

**Schreib-Zeitpunkt-Spalte (Punkt 26):** entfaellt — dieser Plan liest keine fremd-geschriebenen
Daten. Die einzige gelesene Quelle ist das Prod-Schema, und das wird in **dieser** Welle
ausdruecklich noch **nicht** veraendert (die Migration kommt erst in Plan 06 Task 1, Schritt 2 —
D-16, Migration VOR Deploy).

Da der Lauf **rot** endet, gilt zusaetzlich `deploy.sh:222`:
`|| { echo "[deploy] FEHLER: pytest gegen nerve_test ROT — kein Restart, kein Deploy"; exit 1; }`
— kein Restart, der alte Code bleibt live. Das ist der Zweck des Tors, kein Fehlschlag.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Andre faehrt den Deploy — das Tor muss ROT werden</name>

  <read_first>
    - `CLAUDE.md`, Abschnitt "HART: Kein Local-Dev" (Workflow pro Plan-Task, Schritte 1-6)
    - `deploy.sh:153-223` (Postgres-Test-Gate: nerve_test-Aufbau, pytest-Kommandozeile, Abbruch-Pfad)
    - `~/.claude/settings.json:34-37` (Deny-Regel: `deploy.sh` ist fuer den Agenten gesperrt)
  </read_first>

  <what-built>
    Plan 01 hat drei Test-Dateien angelegt bzw. erweitert, die gegen den heutigen Produktionscode
    fehlschlagen muessen. Es wurde **kein** Produktionscode und **kein** Schema geaendert.
  </what-built>

  <how-to-verify>
    1. Der Agent hat committet und gepusht. Wenn nicht: `git push origin main`.
    2. **Andre fuehrt selbst aus** (der Agent darf das nicht — Deny-Regel):
       ```
       ! bash deploy.sh production
       ```
    3. Erwartetes Ergebnis: das Tor bricht ab mit
       `[deploy] FEHLER: pytest gegen nerve_test ROT — kein Restart, kein Deploy`.
       **Der Dienst wird NICHT neu gestartet. `app.getnerve.app` laeuft unveraendert weiter.**
    4. Die vollstaendige pytest-Zusammenfassung (die `FAILED`/`ERROR`-Zeilen plus die Schlusszeile)
       in die Antwort kopieren oder in eine Datei umleiten.
    5. **Es wird in dieser Welle KEINE Migration auf Production gefahren.** Die Migration ist der
       erste Schritt in Plan 06 (D-16: Migration vor Deploy) — hier waere sie verfrueht.

    **Wenn das Tor GRUEN ist, ist das ein Blocker, kein Erfolg** — dann prueft ein Test nicht, was
    er zu pruefen behauptet. In dem Fall: STOP, zurueck zu Plan 01, kein Weiterbauen.
  </how-to-verify>

  <acceptance_criteria>
    **Erwartetes Rot, vollstaendig aufgezaehlt — 11 FAILED + 1 ERROR:**

    - **4x FAILED** aus `tests/test_transcript_segments_write.py`:
      `test_transform_reicht_sprechzeiten_durch`, `test_knopfzeile_ohne_wortzeiten_wird_null`,
      `test_platzhalterzeile_behaelt_ihre_sprechzeiten`,
      `test_ts_ms_bleibt_unberuehrt_von_den_neuen_spalten`.
      **So sieht ihr Rot aus:** `KeyError: 'start_ms'` — die Transform gibt heute nur drei
      Schluessel zurueck.
    - **1x ERROR** (Collection) fuer `tests/test_speech_timing_extraction.py`:
      `ImportError: cannot import name '_extract_word_times'`.
      ⚠ **Es ist EIN Error fuer das ganze Modul, nicht sechs.** Die sechs Tests dieses Moduls
      tauchen in der passed/failed-Bilanz gar nicht auf, weil schon der Import scheitert. Wer
      sechs Fehlerzeilen sucht, findet eine und haelt das faelschlich fuer eine Abweichung.
    - **7x FAILED** aus `tests/test_art9_zeitzeile.py`. **So sieht ihr Rot aus, und nur so:**
      in den vier Fehlerpfaden entsteht heute **gar keine** Zeile -> `assert len(log) == 1`
      schlaegt mit `0 == 1` fehl; im Normalfall-Test und im
      `ohne_wortobjekte`-Test schlaegt der Zugriff auf `log[0]['start_ms']` als `KeyError` fehl.
      Ein Rot mit einer **anderen** Begruendung (Import-Fehler, `AttributeError` auf der
      Attrappe, Zeitueberschreitung) ist **kein** erwartetes Rot, sondern ein Befund — dann STOP.
    - **`tests/test_audio_path_lock_free_guard.py` ist unveraendert GRUEN** (beide Bestandstests).
      Diese Phase fasst die Datei nicht an; ein Rot dort waere ein Befund.
    - Die Schlusszeile nennt eine Zahl **> 0** vor `failed` **und** eine Zahl **> 0** vor `error`.
    - Die `passed`-Zahl liegt in der Groessenordnung des letzten gruenen Standes (**1142**, Phase
      MEHRNUTZER-REST-1 vom 2026-08-07) — faellt sie deutlich darunter, ist mehr kaputt als geplant
      und der Lauf ist zu klaeren, bevor es weitergeht (Mindest-Soll gegen den stillen Ausfall).
    - Es gibt **keinen** Neustart-Eintrag:
      `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh service-status'`
      zeigt dieselbe PID/Startzeit wie vor dem Lauf.
    - `ssh ... 'bash scripts/inspect.sh migrations'` liefert weiterhin **0038** — die Migration
      wurde in dieser Welle bewusst NICHT gefahren.
  </acceptance_criteria>

  <resume-signal>Die vollstaendige rote Tor-Ausgabe hier einfuegen (oder "Tor war gruen" melden — dann STOP).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: ROT-Lauf verbatim als Artefakt sichern</name>

  <read_first>
    - Die von Andre in Task 1 gelieferte Tor-Ausgabe
    - `CLAUDE.md`, Punkt 31 ("ROT-Beleg vor GRUEN … Ausgabe verbatim ins SUMMARY")
    - `CLAUDE.md`, "Acht weitere Vault-Regeln" Punkt 8 (`grep -ciF` nie als Anker)
  </read_first>

  <action>
Lege `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/ROT-LAUF.md` an mit genau
dieser Struktur:

```markdown
# ROT-Lauf — Phase 08.23.2.ZEITSTEMPEL-1

**Gezogen:** <Datum> · **Gefahren von:** Andre (`bash deploy.sh production`, Deny-Regel)
**Ergebnis:** Tor ROT -> kein Restart, Production unveraendert (PID <pid>, Startzeit <zeit>)
**Commit-Stand beim Lauf:** <git rev-parse --short HEAD>
**Prod-Migrations-Kopf beim Lauf:** 0038 (bewusst noch nicht migriert — D-16)

## Tor-Ausgabe (verbatim, nicht gekuerzt, nicht umformuliert)

```
<hier die Ausgabe 1:1 einfuegen>
```

## Aussage des Laufs

- `_transcript_entries_to_segments` reicht die drei Sprech-Zeit-Schluessel heute NICHT durch
  (vier Fehlschlaege, jeweils `KeyError`).
- `services/deepgram_service._extract_word_times` existiert heute nicht (ein Sammel-Fehler
  fuer das ganze Modul, sechs Tests laufen gar nicht erst an).
- Ein Art-9-Treffer erzeugt heute ueberhaupt keine Zeile im RAM-Log — die Sprech-Zeit des
  Abschnitts geht damit still verloren (sieben Fehlschlaege).
- Der Ton-Weg-Waechter ist unveraendert in Ordnung; diese Phase fasst ihn nicht an.
- Die Zahl in der Schlusszeile vor dem Wort fuer bestandene Tests belegt, dass die Suite
  wirklich gelaufen und nicht abgebrochen ist — Existenz-Anker gegen "rot, weil nichts lief".
```

Danach committen. Die Ausgabe **nicht** kuerzen, **nicht** neu formatieren, **nicht**
zusammenfassen — das ist der ganze Zweck des Artefakts.

⚠ **Warum der Prosa-Abschnitt oben das Wort im letzten Punkt umschreibt statt es zu nennen:**
der frueher hier stehende Anker `grep -c "passed" ROT-LAUF.md >= 1` wurde bereits von der
Vorlagen-Prosa erfuellt und waere auch bei leerem verbatim-Block gruen gewesen (D-14). Der
Anker unten zaehlt jetzt ein **ziffern-tragendes** Muster, das nur aus echter pytest-Ausgabe
stammen kann — und die Prosa darf es deshalb nicht selbst enthalten.
  </action>

  <acceptance_criteria>
    - Datei existiert.
    - **Ziffern-tragender Anker (ersetzt den fehlerhaften `passed`-Anker):**
      `grep -cE "[0-9]+ (failed|error)" ROT-LAUF.md` liefert **>= 1**. Diese Form kann die
      Vorlagen-Prosa nicht erfuellen — sie enthaelt keine Zahl vor einem dieser beiden Woerter.
      ⛔ `grep -ciF` wird nicht verwendet (stuerzt auf Andres Git-Bash ab und liefert gar nichts
      statt `0`).
    - `grep -c "test_transform_reicht_sprechzeiten_durch" ROT-LAUF.md` liefert **>= 1**.
    - `grep -c "_extract_word_times" ROT-LAUF.md` liefert **>= 1**.
    - `grep -c "test_art9_treffer_erzeugt_zeile_mit_zeiten_ohne_inhalt" ROT-LAUF.md` liefert **>= 1**.
    - **Abwesenheits-Anker mit Existenz-Anker (D-13):**
      `grep -c "seam_before\|mark_dg_seam\|_dg_ms_offset" ROT-LAUF.md` liefert **0** — die
      gestrichene Mechanik taucht auch im Beleg nicht auf. Gepaarter Existenz-Anker:
      `grep -c "tests/" ROT-LAUF.md` liefert **>= 3** (die drei betroffenen Test-Dateien).
    - Der Commit dieses Artefakts enthaelt **nur** diese Datei (`git show --stat`).
  </acceptance_criteria>

  <verify>
    <automated>Kein Test-Lauf. Das Artefakt IST die Abnahme; sein Inhalt stammt aus dem serverseitigen Tor in Task 1.</automated>
  </verify>

  <done>`ROT-LAUF.md` liegt im Phasen-Ordner und ist committet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entwickler-Rechner -> Production-Server | `deploy.sh` schiebt Code auf den Live-Server. Diese Grenze wird bewusst NUR von Andre ueberschritten (Deny-Regel), nie vom Agenten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ZS1-03 | Denial of Service | Production-Dienst | mitigate | Der rote Lauf loest per Konstruktion **keinen** Restart aus (`deploy.sh:222` bricht vor dem Restart ab). Abnahme-Kriterium prueft die unveraenderte PID. |
| T-ZS1-04 | Tampering | Produktions-DB `nerve` | mitigate | Das Tor liest `nerve` nur per `pg_dump --schema-only` und schreibt ausschliesslich in die Wegwerf-DB `nerve_test` (Whitelist-Guard `deploy.sh:162-166`). In dieser Welle wird **keine** Migration auf Production gefahren (D-16). |
| T-ZS1-05 | Repudiation | Abnahme-Beleg | mitigate | Die Tor-Ausgabe wird verbatim als Datei committet, nicht paraphrasiert — nachtraeglich nicht schoenfaerbbar. Der Anker prueft ein ziffern-tragendes Muster, das die Vorlage selbst nicht erzeugen kann. |
</threat_model>

<verification>
Ausschliesslich auf dem Production-Server. Kein lokaler pytest, kein lokales `python`.
Read-only-Gegenprobe:
```
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh service-status'
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
```
</verification>

<success_criteria>
- [ ] Tor lief ROT (11 FAILED + 1 Collection-ERROR) und blockte den Restart
- [ ] Production-PID/Startzeit unveraendert, Migrations-Kopf weiterhin 0038
- [ ] `ROT-LAUF.md` enthaelt die Ausgabe verbatim
- [ ] Der Anker ist ziffern-tragend und nicht von der eigenen Prosa erfuellbar
- [ ] `passed`-Zahl plausibel gegen den letzten gruenen Stand (1142)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-02-SUMMARY.md`
</output>
