---
phase: 08.23.2.ZEITSTEMPEL-1
plan: 06
type: execute
wave: 5
depends_on: ["08.23.2.ZEITSTEMPEL-1-03", "08.23.2.ZEITSTEMPEL-1-04", "08.23.2.ZEITSTEMPEL-1-05"]
files_modified:
  - .planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-06-SUMMARY.md
  - .planning/DIALOG-GSD-CLAUDIAN.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [D-01, D-02, D-05, D-06, D-11, D-12, D-13, D-15, D-16]
must_haves:
  truths:
    - "Das Deploy-Tor ist GRUEN und der Dienst wurde neu gestartet"
    - "Production traegt Alembic-Revision 0039 und die vier Spalten mit Schild"
    - "Nach einem echten Test-Anruf stehen echte Zahlen in den neuen Spalten: end_ms > start_ms, word_count > 0"
    - "Die Bestandszeilen tragen weiterhin NULL und sind unveraendert"
    - "Die Pflicht-Notizen aus D-01, D-02 und D-06 stehen im SUMMARY"
    - "Die Restluecken und die Pruning-Notiz sind benannt, nicht weggeschrieben"
  artifacts:
    - path: ".planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-06-SUMMARY.md"
      provides: "Phasen-Abschluss mit ROT/GRUEN-Beleg, Pflicht-Notizen, Restluecken, Pruning-Notiz"
      contains: "Pruning-Notiz"
    - path: ".planning/DIALOG-GSD-CLAUDIAN.md"
      provides: "Fragen-Kanal: Foundation-Eintrag + zwei Befunde ausserhalb des Scopes"
      contains: "ZEITSTEMPEL-1"
  key_links:
    - from: "bash deploy.sh production"
      to: "public.transcript_segments"
      via: "alembic upgrade head als postgres + Neustart"
      pattern: "0039"
---

<objective>
Die Phase auf Production bringen und **an echten Zahlen** abnehmen — nicht an Schreibweisen.

Purpose: D-12 verlangt Wirkungs-Anker: echte Zeilen aus der DB nach einem echten Test-Anruf.
Ein `grep` auf Code-Text ist hier ausdruecklich **kein** Beweis. Und D-11/Punkt 31 verlangen,
dass der ROT-Lauf aus Plan 02 und der GRUEN-Lauf hier nebeneinander im SUMMARY stehen.
Output: gruenes Tor, migrierte Production, echte Zahlen, SUMMARY, Fragen-Kanal-Eintraege.

**Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-CONTEXT.md
@.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/ROT-LAUF.md
@CLAUDE.md
@deploy.sh
</context>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen

- `public.transcript_segments` — **Schema-Migration wird angewandt** (`alembic upgrade head`),
  danach werden durch den Test-Anruf **neue Zeilen geschrieben**. Keine Bestandszeile wird
  geaendert oder geloescht.
- `public.alembic_version` — von Alembic auf `0039` gesetzt.

### inspect.sh-Beleg (verbatim, Stand VOR dieser Welle, 2026-08-10)

```
$ ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
                                           Table "public.transcript_segments"
       Column        |           Type           | Collation | Nullable |                     Default
---------------------+--------------------------+-----------+----------+-------------------------------------------------
 id                  | bigint                   |           | not null | nextval('transcript_segments_id_seq'::regclass)
 conversation_log_id | integer                  |           | not null |
 ts_ms               | integer                  |           | not null |
 speaker             | text                     |           | not null |
 text                | text                     |           | not null |
 created_at          | timestamp with time zone |           |          | now()
Indexes:
    "transcript_segments_pkey" PRIMARY KEY, btree (id)
    "idx_transcript_segments_conv_ts" btree (conversation_log_id, ts_ms)
Check constraints:
    "transcript_segments_speaker_check" CHECK (speaker = ANY (ARRAY['berater'::text, 'kunde'::text, 'system'::text]))
Foreign-key constraints:
    "transcript_segments_conversation_log_id_fkey" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id) ON DELETE CASCADE

$ ssh ... 'bash scripts/inspect.sh count transcript_segments'
 count
-------
   627

$ ssh ... 'bash scripts/inspect.sh migrations'
 version_num
-------------
 0038
```

Genau diese drei Ausgaben werden in Task 1/2 **erneut** gezogen — der Vergleich vorher/nachher
ist die Abnahme.

### ⚠ Reihenfolge — die Expand/Contract-Falle (AUTH-2-Lehre)

`deploy.sh` faehrt `alembic upgrade head` **nur gegen die Wegwerf-DB `nerve_test`**
(`deploy.sh:187`) — **nicht** gegen Production. Das Tor kann also gruen sein, waehrend
Production noch auf `0038` steht. In diesem Fenster wuerde ein Anruf-Ende beim INSERT scheitern
(abgefangen von `routes/app_routes.py:561`, der Call bleibt heil, aber **die Segmente dieses
Anrufs waeren verloren**).

**Verbindliche Reihenfolge in Task 1: Deploy -> sofort Migration -> erst DANN telefonieren.**
Zwischen Neustart und Migration wird **kein** Anruf gefuehrt.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Andre faehrt Deploy und Migration — Tor muss GRUEN sein</name>

  <read_first>
    - `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/ROT-LAUF.md` (die Zahlen aus dem ROT-Lauf, gegen die der GRUEN-Lauf gehalten wird)
    - `deploy.sh:153-254` (Postgres-Test-Gate, Schild-Guard-Hinweis)
    - `CLAUDE.md`, Abschnitt "HART: Kein Local-Dev"
  </read_first>

  <what-built>
    Plan 03: vier nullable Spalten + Schild in `database/models.py` und
    `alembic/versions/0039_zeitstempel1_sprechzeiten.py`.
    Plan 04: `_extract_word_times`, die Wortzeiten-Naht in `on_message`, Versatz und Naht-Marker
    per-SID.
    Plan 05: Durchreichung in der reinen Transform und im INSERT.
  </what-built>

  <how-to-verify>
    1. **Deploy** (Andre selbst — Deny-Regel sperrt `deploy.sh` fuer den Agenten):
       ```
       bash deploy.sh production
       ```
       Erwartet: `[deploy] pytest gegen nerve_test bestanden` und ein Neustart.
       Wird das Tor ROT: **STOP**, kein Weiterbauen, Ausgabe zurueck an den Agenten.

    2. **Migration auf Production, unmittelbar danach** (als `postgres`, mit gesetzter
       `DATABASE_URL` — ohne sie faellt Alembic auf SQLite zurueck, siehe MESSGERAETE-1):
       ```
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
         "sudo -u postgres bash -c 'cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve /opt/nerve/venv/bin/alembic upgrade head'"
       ```

    3. **Zwischen Schritt 1 und Schritt 2 KEINEN Anruf fuehren** (siehe Reihenfolge-Notiz oben).

    4. **Gegenlesen (read-only):**
       ```
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh migrations'
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema transcript_segments'
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schilder transcript_segments'
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh count transcript_segments'
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh service-status'
       ```
  </how-to-verify>

  <acceptance_criteria>
    - Die pytest-Schlusszeile nennt **0 failed** und **0 errors**; die `passed`-Zahl ist
      **groesser** als im ROT-Lauf (die **14** neuen Tests aus Plan 01 sind hinzugekommen,
      davon waren **13** rot — der Gegenpol-Test war von Anfang an gruen und steckt damit
      schon in der ROT-Zahl; Herleitung in Task 3, die beiden Zahlen muessen uebereinstimmen).
      Sinkt sie stattdessen, ist ein Bestandstest umgefallen -> STOP.
    - `inspect.sh migrations` liefert `0039` (vorher `0038`).
    - `inspect.sh schema transcript_segments` listet `start_ms | integer | | |`,
      `end_ms | integer | | |`, `word_count | integer | | |`, `seam_before | boolean | | |` —
      alle vier **ohne** `not null` und **ohne** Default.
    - `inspect.sh schilder transcript_segments` zeigt fuer **alle vier** neuen Spalten einen
      nicht-leeren Text und ein Tabellen-Schild, das `learning` und `settings` nennt
      (die zwei bisher fehlenden Leser).
    - `inspect.sh count transcript_segments` liefert weiterhin **627** — die Migration hat keine
      Zeile angefasst.
    - `inspect.sh service-status` zeigt eine **neue** PID/Startzeit (der Neustart hat
      stattgefunden — Gegenstueck zum unveraenderten Stand aus Plan 02).
  </acceptance_criteria>

  <resume-signal>Die gruene Tor-Schlusszeile, den Alembic-Output und die fuenf inspect.sh-Ausgaben hier einfuegen.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Echter Test-Anruf — die Zahlen muessen stimmen (D-12)</name>

  <read_first>
    - Die Ausgaben aus Task 1 (Schema und Revision muessen stimmen, bevor telefoniert wird)
    - `CLAUDE.md`, Abschnitt "Pre-Launch Sandbox-Pattern: Test-User-Account"
  </read_first>

  <what-built>
    Der komplette Pfad steht: Deepgram-Wortzeiten -> RAM-Log -> reine Transform -> INSERT.
  </what-built>

  <how-to-verify>
    1. Mit dem Test-Konto auf `https://app.getnerve.app` einen **echten** Anruf fuehren.
       Mindestens **eine Minute**, mit mehreren Sprech-Abschnitten und mindestens einer
       hoerbaren **Pause von ~3 Sekunden** zwischen zwei Saetzen (damit eine echte Pausenlaenge
       in den Zahlen sichtbar wird). Wenn moeglich zusaetzlich **einmal den EWB-Knopf druecken**
       (das erzeugt die Zeile, die NULL bekommen muss).
    2. Anruf sauber beenden (Knopf, nicht Tab schliessen) — der INSERT haengt am Call-Ende.
    3. Zahlen ziehen:
       ```
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh count transcript_segments'
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample transcript_segments 40'
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs 300'
       ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs-errors 100'
       ```
       (`sample` zeigt die zuletzt geschriebenen Zeilen mit; falls die Ausgabe nur Altzeilen
       zeigt, die Zeilen des neuen `conversation_log_id` gezielt anschauen.)
  </how-to-verify>

  <acceptance_criteria>
    **Wirkungs-Anker (D-12) — echte Zahlen, keine Schreibweisen:**
    - Der neue Anruf hat mindestens **3** Zeilen in `transcript_segments`; `count` ist um genau
      diese Zahl gestiegen (Mindest-Soll gegen den stillen Ausfall: **0 neue Zeilen = rot**).
    - Fuer **jede** gesprochene Zeile des neuen Anrufs gilt: `start_ms` und `end_ms` sind
      **nicht NULL** und `end_ms > start_ms`.
    - `word_count` ist fuer jede gesprochene Zeile **> 0**.
    - Die Werte sind **plausibel**: `end_ms - start_ms` liegt je Abschnitt zwischen ~500 ms und
      ~30.000 ms (kein Wert im Bereich von Stunden — das waere die Wall-Clock-Achse, die hier
      nichts zu suchen hat), und `start_ms` der ersten Zeile liegt nahe **0**.
    - `end_ms` der Zeilen steigt ueber den Anruf hinweg monoton — kein Ruecksprung.
    - Die eingebaute **3-Sekunden-Pause** ist rechnerisch sichtbar: fuer ein Zeilenpaar gilt
      `naechster.start_ms - voriger.end_ms` etwa **2.000-4.000 ms**. (Das ist der eigentliche
      Zweck der Phase — genau diese Zahl war vorher nicht berechenbar.)
    - `seam_before` ist fuer die gesprochenen Zeilen **`f`** (false), nicht NULL — der Marker
      wird also gesetzt und nicht vergessen.
      *(Dieses eine Kriterium entfaellt ersatzlos, falls der Cross-AI-Review D-06a verwirft —
      `seam_before` ist die einzige nicht von Andre freigegebene Entscheidung der Phase. Die
      drei uebrigen Zahlen-Kriterien darueber sind davon unberuehrt.)*
    - **Falls der EWB-Knopf gedrueckt wurde:** die Zeile mit dem Textende `*ewb button*` traegt
      in **allen vier** neuen Spalten NULL — nicht `0`, nicht `f` (D-04).
    - Die Bestandszeilen (`id <= 627`) tragen in allen vier Spalten NULL.
    - `inspect.sh logs` enthaelt `[ZEITSTEMPEL-1] transcript_segments INSERT conv=<id> added=<N> mit_sprechzeiten=<M>`
      mit **M > 0**. Existenz-Anker daneben: dieselbe Log-Ausgabe enthaelt bekannte Bestands-Marker
      (z.B. `[DG] audio_chunk`) — sonst waere "keine Fehler" nur "nichts gelesen".
    - `inspect.sh logs-errors` enthaelt **keinen** neuen Eintrag mit `transcript_segments`,
      `_extract_word_times` oder `mark_dg_seam`.
  </acceptance_criteria>

  <resume-signal>Die Zahlen aus `sample` und die relevanten Logzeilen hier einfuegen — oder beschreiben, was nicht stimmt.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: SUMMARY mit Pflicht-Notizen, Restluecken und Pruning-Notiz + Fragen-Kanal</name>

  <read_first>
    - `ROT-LAUF.md` (Plan 02) und die gruene Schlusszeile aus Task 1
    - Die Zahlen aus Task 2
    - `CLAUDE.md`, "Acht weitere Vault-Regeln" Punkte 4 (Fragen-Kanal) und 7 (Pruning-Notiz)
    - `CLAUDE.md`, Punkt 24 Teil B / Abschnitt B "Roadmap-Sync ist HART"
    - `.planning/DIALOG-GSD-CLAUDIAN.md` (Ende der Datei — Stil der Eintraege)
  </read_first>

  <action>
**A) SUMMARY** `08.23.2.ZEITSTEMPEL-1-06-SUMMARY.md` schreiben. Pflicht-Bestandteile,
keiner davon optional:

1. **ROT und GRUEN nebeneinander, verbatim** — die Schlusszeile aus `ROT-LAUF.md` und die aus
   Task 1, mit der Rechnung **`N_rot_passed + 13 = N_gruen_passed`**. Die Herleitung gehoert
   dazu, sonst ist die Zahl nicht pruefbar: Plan 01 legt **14** neue Tests an
   (4 in `test_transcript_segments_write.py`, 8 in `test_speech_timing_extraction.py`,
   2 in `test_audio_path_lock_free_guard.py`). Davon waren im ROT-Lauf **13** rot
   (4 FAILED + **EIN** Collection-Error fuer das ganze Modul `test_speech_timing_extraction.py`
   — dessen 8 Tests tauchen in der passed/failed-Bilanz gar nicht auf, weil schon der Import
   scheitert; wer im `ROT-LAUF.md` acht Fehlerzeilen sucht, findet eine und haelt das
   faelschlich fuer eine Abweichung — plus 1 FAILED im Pause-Zweig-Waechter);
   der **Gegenpol-Test war
   schon dort gruen** und steckt damit bereits in `N_rot_passed`. Weicht die Rechnung ab, wird
   die Abweichung erklaert, nicht geglaettet — eine zu HOHE Zahl heisst, dass anderswo etwas
   uebersprungen wurde, eine zu NIEDRIGE, dass ein Bestandstest umgefallen ist.

2. **Pflicht-Notiz D-01 (b)** — woertlich, nicht nur die Speicherzahl:
   *Eine Wort-Tabelle waere faktisch eine zweite Kopie des Transkripts und braeuchte einen
   eigenen Schwaerzungs-Pfad — Verstoss gegen "genau EINE Schwaerzungs-Pipeline". Das wiegt
   schwerer als der Speicherplatz. Die drei Features, die Pro-Wort gebraucht haetten
   (Fuellwort-/Stocken-Erkennung, wortgenaue Unterbrechungs-Erkennung, Sprechtempo-Kurve),
   sind alle gestrichen.*
   Dazu der Speicher-Vorbehalt: die Prod-Stichprobe (627 Zeilen / 77 `conversation_logs` ~ 8
   Segmente je Anruf) ist von kurzen Test-Anrufen dominiert und **nicht** repraesentativ fuer
   einen 15-Minuten-Realanruf; das Verhaeltnis A:B bleibt bei ~100x, die absoluten MB-Zahlen
   brauchen diesen Vorbehalt.

3. **Pflicht-Notiz D-02 — die verworfene DRITTE Variante:**
   *"`ts_ms` einfach genau machen" ist naheliegend und falsch. Dann stuenden in derselben Spalte
   Alt-Anrufe auf ganze Sekunden gerundet und Neu-Anrufe millisekundengenau — ein Vergleich
   ueber die Zeit ergaebe still Unsinn, ohne dass irgendetwas rot wird. Zwei getrennte Achsen
   sind ehrlicher als eine Spalte mit zwei Bedeutungen.*

4. **Pflicht-Notiz D-06 — die Grenze:** Luecken ueber eine Naht sind **unbekannt**, nicht null.
   `seam_before = TRUE` markiert genau die Luecke DAVOR; der Abschnitt selbst bleibt gueltig.

5. **RESTLUECKEN — was diese Phase NICHT abdeckt** (Punkt 31, ehrlich, mit grep-Belegen):
   - **GEPRUEFT UND OFFEN — der Pause-Pfad ist heute nicht erreichbar:** `state['is_paused']`
     wird im gesamten Produktionscode nirgends auf `True` gesetzt (grep 2026-08-10: **0** Setzer
     bei **7** Fundstellen insgesamt — Existenz-Anker). Der Mikrofon-Knopf schaltet stattdessen
     `track.enabled` um (`static/pip-launcher.js:1679`); dabei fliesst Stille weiter, die
     Deepgram-Uhr laeuft mit, es entsteht **keine** Naht. **Damit widerspricht dieser Befund
     ausdruecklich CONTEXT.md D-05**, wo steht, der Pause-Pfad sei "unstrittig erreichbar".
   - **GEPRUEFT UND OFFEN — der Reconnect-Pfad ist heute nicht erreichbar:** der einzige
     Aufrufer, der fuer eine bestehende `sid` erneut oeffnet, ruft vorher `pop_session_state` +
     `init_session_state` (`services/deepgram_service.py:764-766`) und legt danach immer einen
     neuen Anruf an. Der Versatz ist deshalb **Vorsorge**, kein heute wirksamer Mechanismus —
     und er wird bewusst **nicht** ueber `pop`/`init` hinweggetragen (das waere falsch: die
     erste Zeile eines NEUEN Anrufs stuende sonst bei z.B. 45.000 ms statt bei 0).
   - **GESCHLOSSEN — der Waechter sieht den Pause-Zweig jetzt:** vorher rief
     `tests/test_audio_path_lock_free_guard.py` nur mit einer **unpausierten** sid auf und
     betrat den Pause-Zweig nie; ein Riegel darin waere gruen durchgelaufen. Plan 01 Task 3
     legt einen Verhaltens-Test durch den Zweig (`_SID_PAUSED` unter gehaltenem Riegel, mit
     Wirkungs-Zusicherung auf den Marker) plus einen Gegenpol, der belegt, dass der Halter-Faden
     den Riegel wirklich haelt. Der Test lag im ROT-Lauf rot und ist hier gruen — Bau-Regel 1
     erfuellt, nicht ersetzt. `mark_dg_seam` ist riegel-frei gebaut, mit der fuer
     `get_sid_paused` dokumentierten Begruendung (`services/live_session.py:105-138`).
   - **WAS DIESES GRUEN NICHT SAGT:** dass der Pause-Zweig in Produktion je durchlaufen wird —
     `is_paused` hat 0 Setzer. Gruen heisst „wenn der Zweig laeuft, klemmt er nicht und der
     Marker wird gesetzt". Nicht gedeckt bleibt der `on_message`-Weg. Die Zahl der dortigen Riegel-Nahmen ist am 2026-08-10 nachgemessen: **7 heute, 9 nach Plan 04** (dieser Plan legt zwei dazu). Der Modul-Docstring des Waechters nennt an dieser Stelle noch **13** — diese Zahl stammt aus der Zeit vor dieser Phase, ist nicht nachvollzogen und darf nicht als belegt gelesen werden
     Riegel-Nahmen (dort wird der Marker verbraucht, bewusst unter dem Riegel).
   - **DURCHRUTSCHER:** ein gedrosselter Hintergrund-Tab, der voruebergehend gar kein Audio
     schickt — dann feuert `handle_audio_chunk` nicht, der Marker wird nicht gesetzt, und die
     Luecke erscheint spaeter als kurze Pause.
   - **DURCHRUTSCHER:** ein Endergebnis mit Text, aber ohne Wortobjekte — die Zeile bekommt
     NULL in allen vier Spalten und faellt aus jeder Statistik heraus. Ehrlich, aber ein
     Datenverlust gegenueber dem Idealfall.
   - **GEPRUEFT UND GESCHLOSSEN:** die EWB-Knopf-Zeile bekommt NULL statt 0 (D-04) — die
     direkteste Form der Fehlerklasse, gegen die diese Phase gebaut wurde, ist abgedeckt.
   - **ZWEITE SCHICHT DARUNTER:** keine. Es wurde bewusst **kein** zusaetzlicher
     Deploy-Tor-Waechter (`end_ms >= start_ms`) gebaut — Begruendung in Plan 04, Abschnitt
     "Punkt 27". Ob die Zahlen im Betrieb stimmen, zeigt `inspect.sh sample`, kein Tor.

6. **Verbindungs-Karten-Korrektur:** `transcript_segments` hat **fuenf** Leser, nicht drei —
   `routes/learning.py` und `routes/settings.py` fehlten in CONTEXT.md und RESEARCH.md. Das
   Tabellen-Schild nennt jetzt alle fuenf (Punkt 23 Aktualitaets-Pflicht).

7. **Pruning-Notiz (Bau-Regel 7):** pro Stelle **geloescht** ODER **bleibt als Foundation fuer
   Phase Y, weil ...**
   - `services/live_session.py::mark_dg_seam` + der Aufruf in `handle_audio_chunk` —
     **bleibt als Foundation**, weil der Pause-Pfad heute keinen Schreiber hat. Aktivierungs-
     Trigger: sobald ein echter Pause-Schalter `state['is_paused'] = True` setzt.
     -> Eintrag in `Nerve-Vault/04 Entscheidungen/Foundation-Code-Register.md` erbeten
     (siehe B).
   - Die Versatz-Regel in `_open_deepgram_connection` — **bleibt als Foundation**, weil der
     Reconnect-Pfad heute keinen erreichbaren Trigger hat. Aktivierungs-Trigger: sobald ein
     Wiederverbindungs-Versuch den `conversation_log` erhaelt.
   - **Nichts obsolet geworden, nichts geloescht** — die Phase ist rein additiv. Insbesondere
     bleiben `get_speech_stats()` (`services/live_session.py:1439-1460`) und der
     `created_at`-Anker in `services/slow_lane.py:205-209` **unveraendert**; beide gehoeren zu
     METRIK-1 bzw. einer eigenen Phase.

8. **Tueroeffner-Validierung (Bau-Regel 2):** kurz belegen, dass die vier Spalten das spaetere
   Feature tragen — Redeanteil = Summe(`end_ms`-`start_ms`) je `speaker` durch Gesamt;
   Sprechtempo = `word_count` durch (`end_ms`-`start_ms`); Redeblock-Laenge = zusammenhaengende
   Abschnitte desselben `speaker`; Pausenlaenge = `naechster.start_ms` - `voriger.end_ms`,
   ausser wenn `seam_before` TRUE ist. Alle vier kommen aus.

9. **Vier Saeulen (Regel D):** Was wird automatisiert (Erfassung der Sprech-Zeiten, bisher
   Handarbeit unmoeglich)? Wo gespeichert (`transcript_segments`, vier Spalten)? Welcher
   Schalter (keiner noetig — reine Erfassung, kein Entscheidungs-Automatismus; das ist die
   ehrliche Antwort, kein "bauen wir spaeter")? Welche Handarbeit gespart (die vier Kennzahlen
   waren bisher gar nicht ermittelbar)?

10. **Der Bestaetigungs-Satz woertlich:**
    *"Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs."*

**B) Fragen-Kanal** — ans **Ende** von `.planning/DIALOG-GSD-CLAUDIAN.md` anhaengen und sofort
committen (Bau-Regel 4, zusaetzlich als normaler Fliesstext im Terminal ausgeben, **kein**
interaktives Menue):

```markdown
## BEFUND — 08.23.2.ZEITSTEMPEL-1 — <Datum>

**1. Bitte um zwei Eintraege im Foundation-Code-Register.** Diese Phase hat zwei Haken gebaut,
die heute keinen erreichbaren Ausloeser haben (Bau-Regel 20 verlangt dafuer einen Register-
Eintrag mit Aktivierungs-Trigger): (a) `services/live_session.py::mark_dg_seam` samt Aufruf in
`handle_audio_chunk` — Trigger: ein echter Pause-Schalter, der `state['is_paused'] = True`
setzt; (b) die Versatz-Regel in `_open_deepgram_connection` — Trigger: ein Wiederverbindungs-
Pfad, der den `conversation_log` erhaelt.

**2. CONTEXT.md D-05 stimmt in einem Punkt nicht.** Dort steht, der Pause-Pfad sei "unstrittig
erreichbar". Am Code ist er es nicht: `state['is_paused']` wird nirgends auf `True` gesetzt
(0 Setzer bei 7 Fundstellen), und der Mikrofon-Knopf schaltet `track.enabled` um — dabei
fliesst Stille weiter und die Deepgram-Uhr laeuft mit. Beide Naht-Quellen sind damit heute
Vorsorge. Das aendert nichts an der Richtigkeit von D-06/D-06a, aber es aendert ihre Rolle.

**3. Zwei Befunde ausserhalb des Scopes, wie beim Discuss besprochen (Reparatur-Modus-Regel):**
(a) Ein erneutes `start_live_session` auf derselben `sid` loescht ueber `pop_session_state` das
**gesamte bisherige Transkript** des Anrufs und legt einen neuen Anruf an
(`services/deepgram_service.py:764-766` vor dem Idempotenz-Check `:835-841`) — bei einem
Verbindungsabriss geht damit nicht nur die Zeit, sondern das ganze Gespraech verloren, und das
Frontend versucht gar keine Wiederverbindung (`static/pip-launcher.js:2474`). (b) Der Mikrofon-
"Pause"-Knopf ist faktisch kein Pause: Stille laeuft weiter zu Deepgram, das kostet
STT-Minuten. Beides braucht einen eigenen Ort.
```

**C) Roadmap-Sync (Regel B, HART).** In `.planning/ROADMAP.md` den Eintrag
`### Phase 08.23.2.ZEITSTEMPEL-1` auf **COMPLETE + LIVE** setzen (Datum, Commit, die 6 Plans
mit je einem Satz Ergebnis) und im selben Zug einen Abschnitt
`## ROADMAP-SYNC — 08.23.2.ZEITSTEMPEL-1 — <Datum>` in `.planning/DIALOG-GSD-CLAUDIAN.md`
anhaengen, damit Claudian die Vault-Roadmap nachzieht.
  </action>

  <acceptance_criteria>
    - `grep -c "Pruning-Notiz" <SUMMARY>` liefert **>= 1**.
    - `grep -c "RESTLUECKEN" <SUMMARY>` liefert **>= 1**.
    - `grep -c "Geprueft: Diese Phase persistiert kein Audio und loescht keine Call-Logs." <SUMMARY>`
      liefert **1** (D-15, Bau-Regel 3 — der Satz woertlich, genau einmal).
    - Das SUMMARY enthaelt **beide** Schlusszeilen (ROT und GRUEN) als woertliches Zitat aus den
      jeweiligen Laeufen — pruefbar daran, dass `grep -c "passed" <SUMMARY>` **>= 2** liefert.
    - `.planning/DIALOG-GSD-CLAUDIAN.md` enthaelt am Ende einen `## BEFUND — 08.23.2.ZEITSTEMPEL-1`
      und einen `## ROADMAP-SYNC — 08.23.2.ZEITSTEMPEL-1`-Abschnitt.
    - `.planning/ROADMAP.md` markiert die Phase als abgeschlossen und listet 6 Plans.
    - `git push origin main` ist gelaufen (Git-Regel "Immer pushen").
  </acceptance_criteria>

  <verify>
    <automated>Kein Test-Lauf noetig — die Abnahme dieser Phase sind die gruene Tor-Schlusszeile aus Task 1 und die echten DB-Zahlen aus Task 2. Read-only-Gegenprobe: `ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample transcript_segments 40'`</automated>
  </verify>

  <done>SUMMARY, Fragen-Kanal-Eintraege und Roadmap-Sync sind geschrieben, committet und gepusht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entwickler-Rechner -> Production | Deploy und Migration; ausschliesslich von Andre gefahren. |
| Test-Anruf -> Produktions-Datenbestand | Ein echter Anruf erzeugt echte Zeilen in `calls`/`conversation_logs`/`transcript_segments`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ZS1-20 | Tampering | Production-Schema | mitigate | `alembic upgrade head` ist versioniert und idempotent; `downgrade()` in 0039 setzt den Zustand zurueck. `inspect.sh count` vor/nach belegt, dass keine Zeile angefasst wurde. |
| T-ZS1-21 | Denial of Service | Fenster zwischen Neustart und Migration | mitigate | Verbindliche Reihenfolge + ausdrueckliches Verbot, in diesem Fenster einen Anruf zu fuehren. Schlimmster Fall waere ohnehin nur der Verlust der Segmente **eines** Anrufs (`app_routes.py:561` faengt ab). |
| T-ZS1-22 | Information Disclosure | Test-Anruf-Daten | mitigate | Der Anruf laeuft ueber das Test-Konto; der Text ist wie immer anonymisiert (Pipeline unveraendert). Die inspect.sh-Ausgaben, die ins SUMMARY wandern, werden auf die **Zahlen**spalten gekuerzt — kein Transkript-Text ins Planungs-Dokument. |
| T-ZS1-23 | Repudiation | Abnahme | mitigate | ROT- und GRUEN-Lauf stehen verbatim nebeneinander im SUMMARY, plus echte DB-Zahlen. |
</threat_model>

<verification>
Ausschliesslich Production. Kein lokaler pytest, kein lokales `python`.
```
bash deploy.sh production                                  # Andre
ssh ... 'bash scripts/inspect.sh migrations'
ssh ... 'bash scripts/inspect.sh schema transcript_segments'
ssh ... 'bash scripts/inspect.sh schilder transcript_segments'
ssh ... 'bash scripts/inspect.sh count transcript_segments'
ssh ... 'bash scripts/inspect.sh sample transcript_segments 40'
ssh ... 'bash scripts/inspect.sh logs 300'
ssh ... 'bash scripts/inspect.sh logs-errors 100'
ssh ... 'bash scripts/inspect.sh service-status'
```
</verification>

<success_criteria>
- [ ] Tor GRUEN, `passed` groesser als im ROT-Lauf, 0 failed
- [ ] Production auf Revision `0039`, vier Spalten nullable, vier Schilder gesetzt, `count` weiterhin 627
- [ ] Echter Test-Anruf: `end_ms > start_ms`, `word_count > 0`, Pause rechnerisch sichtbar, `seam_before = f`
- [ ] EWB-Knopf-Zeile (falls erzeugt): viermal NULL
- [ ] SUMMARY mit ROT/GRUEN, den drei Pflicht-Notizen, RESTLUECKEN und Pruning-Notiz
- [ ] Fragen-Kanal-Eintraege + Roadmap-Sync geschrieben, gepusht
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.ZEITSTEMPEL-1-sprech-zeiten-sichern/08.23.2.ZEITSTEMPEL-1-06-SUMMARY.md`
</output>
