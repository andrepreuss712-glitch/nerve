---
phase: 08.23.2.H1
plan: 01
subsystem: live-qa-dispatch
tags: [qakill, live-loop, sonnet-cut, cost, taxo]
requires: []
provides: ["_qa_pipeline_dispatch ohne generate_qa_response-Aufrufe"]
affects: [services/claude_service.py, tests/test_h1_qakill.py, tests/test_08_5_03_integration.py]
tech-stack:
  added: []
  patterns: ["minimaler Schnitt (Punkt 27): nur der tote Antwort-Zweig faellt, alles Lebende byte-gleich"]
key-files:
  created: [tests/test_h1_qakill.py]
  modified: [services/claude_service.py, tests/test_08_5_03_integration.py]
decisions:
  - "Verworfenen generate_qa_response-Call (Sonnet, MODEL_QA) aus dem Live-Dispatch entfernt — 0 Konsumenten seit PIP-01"
metrics:
  duration: "~25min"
  completed: "2026-07-22"
---

# Phase 08.23.2.H1 Plan 01: generate_qa_response streichen (QAKILL) Summary

Der verworfene, konsumenten-freie `generate_qa_response`-Antwort-Call (Sonnet via `config.MODEL_QA`) ist aus `_qa_pipeline_dispatch` entfernt; `classify_utterance` + Abstain-intent_events + FAQ `used_count` bleiben byte-fuer-byte lebendig, per Runtime-Waechter abgesichert.

## Pflicht-Vorabcheck (Punkt 20) — Konsumenten-Freiheit belegt

**Grep-Beleg (`grep -rn "generate_qa_response" services/`, HEAD 1aa2445):**

```
services/claude_service.py:1528:   classify_utterance, generate_qa_response,          # Import
services/claude_service.py:1630:   # Pfad (generate_qa_response / FAQ) bleibt ...      # Kommentar
services/claude_service.py:1641:   _antwort = generate_qa_response(                    # AUFRUF 1 (einwand_unknown else)
services/claude_service.py:1686:   _antwort = generate_qa_response(                    # AUFRUF 2 (frage/kein-FAQ else)
services/qa_pipeline.py:12:        - generate_qa_response(utterance, category, ...      # Docstring
services/qa_pipeline.py:82:        # ... (siehe generate_qa_response).                  # Kommentar
services/qa_pipeline.py:377:       # -- Public: generate_qa_response --                 # Section-Kommentar
services/qa_pipeline.py:378:       def generate_qa_response(utterance: str, ...        # DEFINITION
services/qa_pipeline.py:500:       print(f"[QA] generate_qa_response failed: {e}")      # Log-String
```

=> Genau **ZWEI Produktiv-Aufrufe** (`:1641` + `:1686`), beide in `_qa_pipeline_dispatch`. Alles Uebrige = Import/Kommentar/Docstring/def/Log.

**Konsumenten-Freiheit von `_antwort` (beide else-Zweige, identisches Muster):**
`_antwort` floss ausschliesslich in `if not _antwort`, `apply_tabu_filter(_antwort,...)` (reine Funktion — entscheidet nur WELCHER No-Op), `len(_antwort)` in einem `print`, und die No-Op-Emitter `_emit_soft_hint`/`_emit_qa_slot1` (seit PIP-01 nur `print`, kein Emit — claude_service.py:1548 + :1618). **KEIN `return`, KEIN `ls._session_state[...]=`, KEIN `.commit()`, KEIN `sio.emit`, KEIN intent_event** haengt an `_antwort`. => Verworfener Bezahl-Call, sicher kappbar. **Kein lebender Konsument gefunden.**

**MODEL_QA=Sonnet zur Laufzeit bestaetigt:** `config.MODEL_QA` Default = `claude-sonnet-4-5` (config.py:75), genutzt bei `model=config.MODEL_QA` (qa_pipeline.py:444), Cost-Model-Ableitung `'sonnet-4-5' if 'sonnet' in config.MODEL_QA` (:473). Runtime-Assert `test_model_qa_default_is_sonnet` GRUEN. Der gekappte Call war also ein **verworfener Sonnet-Call im Live-Loop** — der Kill schliesst das „kein Sonnet live"-Constraint-Leck.

## Was entfernt wurde (Diff)

1. **Import** (claude_service.py:1527-1530): `generate_qa_response` aus dem lokalen Import entfernt — bleibt `classify_utterance, match_faq, apply_tabu_filter`.
2. **einwand_unknown high-conf `else`-Zweig** (frueher :1640-1651): kompletter `_antwort = generate_qa_response(... 'einwand_unknown' ...)` + 3 Folge-Emit-Zweige geloescht → durch Erklaer-Kommentar ersetzt. Der `if _conf < THRESHOLD:`-Abstain-Zweig **unveraendert**.
3. **frage/kein-FAQ high-conf `else`-Zweig** (frueher :1685-1695): analog geloescht. Der low-conf-Abstain-Zweig **unveraendert**.

**Verifikation:** `grep -rn "generate_qa_response(" services/` → nur noch die `def` (qa_pipeline.py:378) + Docstring, **0 Aufrufe** in claude_service.py. `ast.parse` GRUEN.

## Lebende Konsumenten intakt (belegt)

- `classify_utterance` (Haiku) — BLEIBT, unveraendert aufgerufen.
- Low-conf **Abstain-intent_event** (`_emit_abstain_event` → `emit_intent_event`, `abstained=True`) — Runtime-Test `test_low_conf_einwand_unknown_still_fires_abstain_intent_event` GRUEN.
- FAQ-Treffer **used_count += 1** (nicht-tabu-Gate) — Runtime-Test `test_faq_match_increments_used_count` GRUEN (3→4, `commit` aufgerufen).
- Guards (SID / `kw_fired_for_line` / Slot-1-Mutex) unangetastet.

## Deviations from Plan

None - Plan wurde exakt wie geschrieben ausgefuehrt. Zwei stale-Contract-Tests in `tests/test_08_5_03_integration.py` (`test_einwand_unknown_high_conf_no_readzone_emit`, frueher `test_einwand_unknown_passes_profile_data_not_empty`) wurden **retargetet** auf den neuen Vertrag (assert `generate_qa_response` NICHT aufgerufen) — Code nicht zurueckgedreht (plan-vorgesehen).

## Lokales Vorab-Signal (KEINE Abnahme — Verbindlich = deploy.sh production, Plan 03)

```
python -m pytest tests/test_h1_qakill.py tests/test_08_5_03_integration.py tests/test_qa_pipeline.py -q
=> 37 passed
```

- Task-1-RED (vor Kill): `2 failed, 3 passed` (Kill-Asserts ROT, lebende Konsumenten schon GRUEN).
- Task-2-GRUEN (nach Kill): `37 passed`.

## Commits

- `d5a7f15` — test(08.23.2.H1-01): RED-Waechter + Konsumenten-Freiheit-Beleg (Task 1)
- `94be9e2` — feat(08.23.2.H1-01): generate_qa_response im Live-Dispatch kappen (Task 2)

## Self-Check: PASSED

- `tests/test_h1_qakill.py` — FOUND
- Commit `d5a7f15` — FOUND
- Commit `94be9e2` — FOUND
- `grep "generate_qa_response(" services/claude_service.py` → 0 Aufrufe (nur Kommentare) — VERIFIED
