---
phase: 08.23.2.H1
plan: 03
type: execute
wave: 3
depends_on: ["08.23.2.H1-02"]
files_modified:
  - tests/test_h1_latte.py
autonomous: false
requirements: [H1-LATTE, H1-CAL]
complexity: "🔴"

must_haves:
  truths:
    - "Ein Runtime-Waechter beweist: aus dem gemergten Ergebnis bekommen ALLE lebenden Call-1-Konsumenten weiter valide, gleichwertige Werte (D2-Latte)."
    - "Der Kalibrierungs-Anruf (André/Claudian) misst Einwand-Erkennungs-Qualitaet Merge-vs-Zwei-Calls, TTFT UND Time-to-Last-Token — belegt gegen den Ist-Stand."
    - "Deploy + Test-Anruf laufen ueber deploy.sh production (Pytest-Gate) + Browser-Live-Test, nicht lokal."
  artifacts:
    - path: "tests/test_h1_latte.py"
      provides: "D2-Latte-Waechter: jeder lebende Konsument erhaelt aus dem Merged-Ergebnis gueltige Werte (Runtime, kein Source-Presence)"
      contains: "def test_"
  key_links:
    - from: "analysiere_und_klassifiziere (Merged-Ergebnis)"
      to: "alle lebenden Konsumenten (intent_event, Moment, Kaufbereitschaft, gegenargument_log, Readiness, EWB-Buttons, Phase-Kadenz, Abstain, FAQ used_count)"
      via: "Runtime-Assert gegen die Latte-Liste"
      pattern: "ergebnis\\.get\\(|qa_section\\.get\\("
---

<objective>
**Akzeptanz-Latte absichern (Bau-Vorgabe 5 / D2) + Kalibrierungs-Anruf (Cross-AI-Zusatzfunde).**

Zwei Teile:
1. **Latte-Waechter (autonom baubar):** ein Runtime-Test, der beweist, dass der Merged-Call jeden lebenden Konsumenten von `ergebnis` gleichwertig speist — Runtime-Verhalten, kein Source-Presence (CLAUDE.md Test-Qualitaets-Regel).
2. **Deploy + Kalibrierungs-Anruf (Checkpoint — André/Claudian, NICHT GSD):** verbindliches Tor = Pytest-Gate in `deploy.sh production`; danach echter Test-Anruf, der die Erkennungs-Qualitaet + Latenz gegen den Ist-Stand belegt. Das ist der `autonomous: false`-Teil.

Purpose: Beweisen, dass „Erkennung sinkt nicht + Latenz steigt nicht" gegen die KONKRETE Konsumenten-Liste gilt (nicht pauschal), und den Merge live schalten.
Output: Latte-Waechter; dokumentierter Kalibrierungs-Anruf mit Mess-Zahlen + Go/Rollback-Entscheidung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.H1-live-schleifen-zusammenlegen/08.23.2.H1-CONTEXT.md
@CLAUDE.md
@services/claude_service.py

<latte_liste>
<!-- Die exakte D2-Liste (aus CONTEXT.md <acceptance_bar> + ROADMAP-Bau-Vorgabe 5). Jeder Punkt = eine Assertion. -->
LEBENDE Konsumenten (muessen gleichwertig bleiben):
1. Einwand-Erkennung + Medium-Lane `intent_event` (Typ, confidence, Zitat) — feuert bei `ergebnis.get('einwand')`.
2. Moment-Open (`get_or_open_moment` bei einwand=true) + Moment-CLOSE im Nicht-Einwand-Fall (cold_call substanzieller Turn → `close_moment('advisor_answered')`).
3. Kaufbereitschaft-Update (`intensitaet`=='hoch' → -5, sonst -3) + `intensitaet`-Read.
4. gegenargument_log (einwand_typ=intent_type, einwand_zitat, gegenargument_1/2, ist_vorwand abgeleitet).
5. 8 Readiness-Flags (kaufsignal/einwand_geloest/detailfrage/budget_erwaehnt/naechster_schritt/zustimmung/konkurrenz/zeitdruck_kunde/monosyllabisch) → compute_readiness_score.
6. Dynamische EWB-Buttons via `ergebnis['typ']` (Freitext, ≠ intent_type).
7. Phase-Classifier-Kadenz (jeder 5. Cycle) — weiter valide Werte.
8. Abstain-intent_events (low-conf, aus QA-Sektion) + FAQ used_count (aus FAQ-Match).
9. Latenz: TTFT UND Time-to-Last-Token steigen nicht (Kalibrierungs-Anruf).

NICHT in die Latte (tot — keine Regression): FT-Events, `lernkarte_match`.
</latte_liste>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D2-Latte-Waechter (Runtime, kein Source-Presence)</name>
  <files>tests/test_h1_latte.py</files>
  <read_first>
    - <latte_liste> oben
    - services/claude_service.py analyse_loop (die Konsumenten-Bloecke) + `_qa_pipeline_dispatch`
    - Muster: tests/test_h1_merge_wiring.py (Plan 02) + test_medium_lane_intent_event_live.py (`_OneShotTrigger` + gemockter Claude)
  </read_first>
  <action>
    Schreibe tests/test_h1_latte.py: EIN deterministischer Durchlauf des analyse_loop-Ticks mit `MERGE_ANALYSE_QA='1'` und einem GEMOCKTEN `analysiere_und_klassifiziere`, das ein realistisches Merged-Ergebnis liefert (einwand=true, intent_type, confidence, intensitaet='hoch', einwand_zitat, gegenargument_1/2, `typ`='Preis-Einwand' Freitext, mehrere Readiness-Flags gesetzt, plus `qa`={kategorie:'frage', confidence:0.6}). Gemockte Seams: `emit_intent_event`, DB (used_count), sio. Dann pro Latte-Punkt EINE Runtime-Assertion (Function-Call-/State-Mutation-Assert, NIE `getsource`/`in src`):
    - (1) `emit_intent_event` mit dem intent_type + confidence + triggering_text aufgerufen.
    - (2) `get_or_open_moment` aufgerufen (einwand=true); separater Fall einwand=false + cold_call + langer Turn → `close_moment('advisor_answered')`.
    - (3) `update_kaufbereitschaft(sid, -5)` bei intensitaet='hoch'.
    - (4) `_session_state[sid]['gegenargument_log']` hat einen neuen Eintrag mit `einwand_typ`==intent_type, `ist_vorwand`==(intent_type=='vorwand'), Freitext anonymisiert.
    - (5) `readiness_score`/`readiness_bucket` per-SID gesetzt; score reflektiert die gesetzten Flags (score > Baseline ohne Flags).
    - (6) dynamische EWB-Buttons: `last_einwand_typ` per-SID = `ergebnis['typ']` (Freitext), NICHT intent_type.
    - (7) Phase-Kadenz: nach 5 Ticks wird `classify_phase` einmal getriggert (Zaehler per-SID).
    - (8) Guard-frei + QA-Sektion low-conf → `emit_intent_event(abstained=True)`; FAQ-Match-Pfad → used_count-Inkrement erreicht.
    Ziel: der Test wird ROT, wenn eine kuenftige Merge-Aenderung einem Konsumenten den Wert entzieht (Ratsche). KEIN echter LLM-/Netz-Call (nicht `-m live`, laeuft im Standard-Gate `not live and not perf`).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -m pytest tests/test_h1_latte.py -q</automated>
    <!-- Vorab-Signal. VERBINDLICH = deploy.sh production Pytest-Gate (Task 2). -->
  </verify>
  <done>Alle 8 Latte-Konsumenten haben je eine Runtime-Assertion; Test GRUEN gegen den Merge-Code; kein Source-Presence-Muster.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy auf Production + Kalibrierungs-Anruf (André/Claudian)</name>
  <what-built>
    Merge Call 1 + Call 3 → EIN Haiku-Call (`analysiere_und_klassifiziere`), truncation-fest, hinter `MERGE_ANALYSE_QA` (Default an); generate_qa_response (verworfener Sonnet-Call) gestrichen; Latte-Waechter GRUEN.
  </what-built>
  <how-to-verify>
    Dieser Schritt laeuft NICHT autonom (HART-Regel: Verify=Production; Deploy + Test-Anruf fahren André/Claudian, nicht GSD).
    1. **Deploy:** `bash deploy.sh production` — das eingebaute Pytest-Gate laeuft server-seitig vor dem Restart; bei rotem Test KEIN Restart (alter Code bleibt live). Erwartung: test_h1_qakill / test_h1_merge_parse / test_h1_merge_wiring / test_h1_latte + bestehende QA-Tests GRUEN im Prod-Gate.
    2. **Kalibrierungs-Anruf (Pflicht, gegen Ist-Stand):**
       - **Einwand-Erkennungs-Qualitaet (Attention-Loss, Gemini):** denselben/aequivalenten Transkript-Abschnitt einmal mit `MERGE_ANALYSE_QA=1` (Merge) und einmal `=0` (zwei getrennte Calls) fahren; Einwand-Typ/confidence/Zitat vergleichen. Merge darf NICHT schlechter erkennen.
       - **Frage-Einstufung (QA):** die 4-Wege-Klassifikation bleibt sinnvoll (Abstain-intent_events + FAQ used_count fuellen sich wie im Ist).
       - **Latenz (W-1 — kein neues Instrument bauen, bestehendes nutzen):**
         - **Time-to-Last-Token = der Mess-Hebel des Merge.** Der Merged-Call ist ein blockierendes `messages.create` OHNE `stream=True` → er hat KEINEN Time-to-First-Token. Seine Gesamt-Latenz steht bereits im bestehenden Log `[Claude-1] SID=… Ergebnis (Latenz {latency_e}s)` (`claude_service.py`, `latency_e = time.monotonic() - t_start`). Merge-vs-2-Call ueber `MERGE_ANALYSE_QA=1/0` am selben/aequivalenten Transkript vergleichen: die `latency_e`-Werte duerfen NICHT steigen und der Wert darf den 4s-Tick (`ANALYSE_INTERVALL`) nicht ueberlappen (sonst Tick-Stau).
         - **TTFT des user-sichtbaren Antwort-Pfads ist by construction unveraendert:** TTFT ist im Code nur fuer `streame_auto_variante` instrumentiert (der EWB-Antwort-Stream) — den der Merge GAR NICHT anfasst. Also NICHT neu instrumentieren; nur festhalten/verifizieren, dass `streame_auto_variante` (und damit die spuerbare Antwort-Latenz) unberuehrt bleibt.
         - KEIN neuer 4-Zeitstempel-Instrument-Task noetig.
    3. **Ehrliche Erwartung (im Anruf-Log festhalten, nicht als Nutzen einplanen):** Spar-Effekt ≈ 20-30 % der Tick-Kosten (NICHT 35-45 %). Cache-Bonus UNSICHER — Haiku 4.5 braucht 4.096-Token-Prefix; der gemergte Prompt bleibt evtl. drunter → messen, nicht annehmen.
    4. **Entscheidung:** Erkennung ok + Latenz ok → Merge bleibt an (`MERGE_ANALYSE_QA=1`). Regression → `MERGE_ANALYSE_QA=0` setzen (<30s, ohne Deploy) + Befund dokumentieren.
    Falls Deploy Auth/CLI-Fehler wirft: Auth-Gate dynamisch, André authentifiziert + retry.
  </how-to-verify>
  <resume-signal>André/Claudian: „approved — Merge live, Latenz+Erkennung ok" ODER „rollback: MERGE_ANALYSE_QA=0, Befund: …". Mess-Zahlen (TTFT, Time-to-Last-Token, Erkennungs-Vergleich, Spar-%) im SUMMARY + Vault 05 Log festhalten.</resume-signal>
</task>

</tasks>

<persistenz_schicht>
## 5. Persistenz-Schicht-Verifikation (Punkt 21)
Dieser Plan schreibt/aendert KEINE persistierten Daten selbst (Test + Deploy + manueller Anruf). Der Latte-Waechter mockt alle DB-/Emit-Seams. Kein Schema-Bezug.
</persistenz_schicht>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-H1CA-01 | Tampering | stille Latte-Regression (ein Konsument bekommt keinen Wert mehr) | mitigate | Runtime-Latte-Waechter (Ratsche) + Kalibrierungs-Anruf gegen Ist-Stand |
| T-H1CA-02 | Denial of Service (Latenz) | Time-to-Last-Token-Ueberlappung mit naechstem Tick | mitigate | Kalibrierungs-Anruf misst beide Latenzen; Env-Rollback bereit |
</threat_model>

<verification>
- tests/test_h1_latte.py GRUEN im Prod-Pytest-Gate (`deploy.sh production`).
- Kalibrierungs-Anruf dokumentiert: Erkennungs-Vergleich Merge-vs-Zwei-Calls + TTFT + Time-to-Last-Token + Spar-% + Go/Rollback.
- Browser-Live-Test mit Test-User (is_test_user) auf getnerve.app.
</verification>

<success_criteria>
- Alle 8 Latte-Konsumenten laufzeit-abgesichert.
- Merge live geschaltet ODER bewusst per Env zurueckgerollt mit dokumentiertem Befund.
- Erkennung nicht schlechter, TTFT + Time-to-Last-Token nicht hoeher — belegt am echten Anruf.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.H1-live-schleifen-zusammenlegen/08.23.2.H1-03-SUMMARY.md` — mit Kalibrierungs-Zahlen + Go/Rollback-Entscheidung.
</output>
