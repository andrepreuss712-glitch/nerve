---
phase: 08.23.2.H1
plan: 02
subsystem: api
tags: [claude, haiku, live-loop, prompt-merge, truncation-parser, cost]

# Dependency graph
requires:
  - phase: 08.23.2.H1-01
    provides: "QAKILL — generate_qa_response im Live-Dispatch gekappt (_antwort konsumenten-frei)"
provides:
  - "analysiere_und_klassifiziere — EIN Haiku-Call liefert Einwand-Sektion (top-level) + QA-Klassifikation (nested unter 'qa')"
  - "_parse_merged_sections — truncation-fester sektionsweiser Parser (Einwand ueberlebt QA-Abschnitt, B-1 kollisionssicherer Anker)"
  - "config.MERGE_ANALYSE_QA — Env-Rollback-Ventil (Default '1'), Zwei-Call-Fallback bei '0' (+ Restart)"
  - "analyse_loop + _qa_pipeline_dispatch(qa_section=) verdrahtet — EIN Call/Tick, Latte unveraendert"
affects: [08.23.2.H1-03, Kalibrierungs-Anruf, TAXO3]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sektionsweiser truncation-fester JSON-Parser (STRICT ORDER + Doppelpunkt-Anker + string-sichere Top-Level-Komma-Grenzen)"
    - "Env-Schalter als Rollback-Ventil ohne Deploy (import-Zeit getenv, Restart-Semantik)"

key-files:
  created:
    - tests/test_h1_merge_parse.py
    - tests/test_h1_merge_wiring.py
  modified:
    - services/claude_service.py
    - config.py

key-decisions:
  - "K1: Merged-Prompt = SYSTEM_PROMPT_BASE VERBATIM + qa-Nachsatz — KEIN Feld nachgeruestet (kaufsignal/tipp/kritischer_fehler/kb_delta bleiben un-emittiert -> tote Active-Hints feuern nicht)"
  - "K2: Classifier-Semantik wortgleich aus _FALLBACK_CLASSIFIER_PROMPT in den qa-Nachsatz (Merge- und Fallback-Pfad klassifizieren identisch); PROMPT_CLASSIFIER_VERSION_OVERRIDE fuer Merge-Pfad wirkungslos (nur EIN Seed classifier/v1)"
  - "K3: MERGE_ANALYSE_QA wirkt erst nach systemctl restart nerve (getenv import-Zeit) — ehrliche Rollback-Semantik, kein hot-reload"
  - "K4/B-1: Happy-Path = json.loads-Dict (kein Rescue fuer haeufigsten kein-Einwand-Fall); Rescue-Anker rfind('\"qa\":') MIT Doppelpunkt (kollisionssicher gegen Einwand-Werte '\"qa\"')"
  - "D1: MODEL_ANALYSE bleibt Haiku (kein Modellwechsel); max_tokens=600 (400+150+Headroom)"

patterns-established:
  - "Merged-Live-Call: Einwand-Sektion flach top-level (Latte by construction) + Sekundaer-Sektion nested"
  - "Truncation-Rescue via string-sichere _top_level_commas (Kommas in Werten zaehlen nicht)"

requirements-completed: [H1-MERGE, H1-TRUNC]

# Metrics
duration: ~45min
completed: 2026-07-22
---

# Phase 08.23.2.H1 Plan 02: MERGE + TRUNC (WEG 1 Kern) Summary

**`analysiere_mit_claude` (Call 1) + `classify_utterance` (Call 3) zu EINEM Haiku-Call (`analysiere_und_klassifiziere`) zusammengelegt — Einwand-Sektion top-level (Latte unveraendert), QA nested unter `qa`, truncation-fest geparst, per `MERGE_ANALYSE_QA` ohne Deploy rollbackbar.**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-07-22
- **Completed:** 2026-07-22
- **Tasks:** 3/3
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- Der grosse Kosten-Hebel (2 Live-Haiku-Calls -> 1) ohne Latte-Regression: der Einwand-Fan-out (`:975-1394`) liest weiter `ergebnis.get(...)` — KEIN Fan-out-Code angefasst.
- Truncation-fester sektionsweiser Parser: ein abgeschnittenes Merged-JSON killt die Einwand-Sektion NICHT mehr (fail-open `qa={}`), inkl. adversarialer Anker-Kollisionssicherheit (B-1).
- Instant-Rollback-Ventil `MERGE_ANALYSE_QA=0` (Zwei-Call-Fallback intakt) mit ehrlicher Restart-Semantik.

## Task Commits

1. **Task 1: RED — 8 Parser-Tests** - `fd9d909` (test)
2. **Task 2: GREEN — _parse_merged_sections + analysiere_und_klassifiziere + config** - `b10ddda` (feat)
3. **Task 3: Wire — analyse_loop + _qa_pipeline_dispatch(qa_section=)** - `a785f9b` (feat)

_TDD-Gate: Task 1 (RED, `test(...)`) vor Task 2 (GREEN, `feat(...)`) — Gate-Sequenz eingehalten._

## Files Created/Modified
- `services/claude_service.py` — `_parse_merged_sections` + `_top_level_commas` + `analysiere_und_klassifiziere` + `_MERGED_SYSTEM`/`_MERGED_QA_NACHSATZ`; analyse_loop-Weiche; `_qa_pipeline_dispatch(qa_section=None)`.
- `config.py` — `MERGE_ANALYSE_QA` (Default `'1'`, Restart-Semantik-Kommentar).
- `tests/test_h1_merge_parse.py` — 8 Parser-Tests (Truncation/Nesting/Adversarial/Order/Happy).
- `tests/test_h1_merge_wiring.py` — 5 Wiring-Tests (EIN Call/Tick, Guard, Truncation, IL-2-Ordering, Rollback).

## Belege (Orchestrator-Ausgabe)

### K1 — Merged-Prompt = SYSTEM_PROMPT_BASE VERBATIM + qa-Nachsatz
`_MERGED_SYSTEM = SYSTEM_PROMPT_BASE + _MERGED_QA_NACHSATZ` (reine Konkatenation, BASE unangetastet). Der `_MERGED_QA_NACHSATZ` fuegt NUR den kompakten qa-Block hinzu — kein Einwand-Feld erfunden, keins weggelassen. Die im `<interfaces>`-Block irrefuehrend gelisteten `kaufsignal`/`tipp`/`kritischer_fehler`/`kb_delta` sind bewusst NICHT im Prompt (BASE emittiert sie heute nicht -> die toten Active-Hints `:1319/1335` feuern weiter nicht = keine stille Verhaltensaenderung).

### B-1 — kollisionssicherer Anker (verbatim Rot-vor-Fix)
Gegenprobe mit dem naiven Anker `rfind('"qa"')` (ohne Doppelpunkt) gegen Test-6-Input:
```
NAIVER ANKER rfind('"qa"') Ergebnis-Keys: ['confidence', 'einwand', 'intensitaet', 'intent_type', 'qa']
typ: None | gegenargument_1: None
VERLOREN (naiv): ['gegenargument_1', 'gegenargument_2', 'typ', 'detailfrage', 'monosyllabisch']
```
Der naive Anker schneidet am Einwand-WERT `"qa"` (einwand_zitat) und verliert alle Felder danach -> Test 6 waere ROT. Der reale `rfind('"qa":')` (mit Doppelpunkt) trifft nur den echten Schluessel -> Test 6 GRUEN (alle Felder ueberleben, `qa=={}`).

### Parser-Tests (8) — Erst-Rot belegt
Task-1-Lauf ROT (ImportError `cannot import name '_parse_merged_sections'`), nach Task 2 alle 8 GRUEN. Inklusive Test 6 (Adversarial), Test 7 (Order-Violation qa-zuerst -> fail-open Dict, kein Crash), Test 8 (kein-Einwand-Happy-Path faellt NICHT in Rescue).

### IL-2-Reihenfolge-Assert (Wiring d)
Echter Laufzeit-`side_effect` am `_qa_pipeline_dispatch`-Mock liest `ls._session_state[sid]['state']['primary_intent']` IM MOMENT des Aufrufs -> `captured == 'echter_einwand'` beweist, dass der IL-2-Write (`:1022`) VOR dem Dispatch passierte. Der Guard-Test (b) nutzt den lebenden D-02-Guard `kw_fired_for == line_id` (`:1514`), NICHT den toten `slot1_variant_busy_until`-Guard.

### MERGE_ANALYSE_QA — Restart-Semantik (K3)
config.py-Kommentar praezisiert: `os.getenv` zur Import-Zeit -> Rollback = `.env` setzen + `systemctl restart nerve` (kein Deploy, aber Restart Pflicht). Kein "hot/sofort".

### grep-Kontrakt (verification-Sektion)
```
analysiere_und_klassifiziere(  -> 2 (1 def + 1 Live-Caller)
= analysiere_mit_claude(       -> 1 (nur Fallback-Zweig)
classify_utterance(            -> 1 (nur Dispatch-Fallback, qa_section is None)
```

## Persistenz-Schicht (Punkt 21)
Kein Schema-Change, keine neue Spalte, keine neue DB-Tabelle. Der Merge aendert nur die QUELLE der Werte (ein Haiku-Call statt zwei), nicht wo/wann sie liegen (`intent_event`, `profile_faqs.used_count`, per-SID RAM-State unveraendert). Das prod-`inspect.sh schema intent_event`/`profile_faqs` ist Claudians Welle-3-Lauf zugeordnet (dieser Executor faehrt kein SSH/deploy.sh; Multi-Segment-Phase-ID `08.23.2.H1` -> Pfade hartkodiert). Reiner Beruhigungs-Check — dieser Plan beruehrt kein Schema.

## Lokales Vorab-Signal (NICHT Acceptance — HART-Regel: Verify=Production/deploy.sh-Gate ist Plan 03/Claudian)
```
tests/test_h1_merge_parse.py     -> 8 passed
tests/test_h1_merge_wiring.py    -> 5 passed
tests/test_h1_qakill.py          -> 5 passed (Welle-1-Regression gruen)
Trio zusammen                    -> 18 passed
Breiter (-k "qa or claude or classif or dispatch or h1", ohne PG-only ft_seed) -> 73 passed, 4 skipped, 1 failed
ast.parse services/claude_service.py + config.py -> OK
```

## Deviations from Plan

### Auto-fixed / Out-of-scope
Keine Auto-Fixes noetig — Plan wurde wie geschrieben (inkl. K1-K4 + B-1) umgesetzt.

**Out-of-scope (deferred, NICHT gefixt — Scope-Boundary):**
- `tests/test_ft_seed.py` — Collection-Error `unknown database crm` (SQLite kennt kein `crm`-Schema). Bekannter Pre-existing PG-only-Fehler (CLAUDE.md Staging-Promotion-Notiz). Nicht von diesem Plan verursacht.
- `tests/test_phase_classifier.py::test_phase_classifier_integration_real_haiku` — 1 failed: `@pytest.mark.integration`-Test braucht echten Haiku-API-Call (`_FakeAnthropic` hat kein `messages`). Phase-Classifier-Datei unberuehrt; umgebungsabhaengig/pre-existing. Nicht mein Change.

## Bestaetigungen
- KEIN `deploy.sh` gefahren (lokal nur `pytest` als Vorab-Signal).
- Coaching unberuehrt (`analysiere_coaching`/`coaching_loop` nicht angefasst).
- Kein Modellwechsel — `MODEL_ANALYSE` bleibt Haiku (D1), kein Sonnet, kein Caching-Bau.
- `ast.parse` gruen; Commit-Hashes: `fd9d909`, `b10ddda`, `a785f9b`.
- Nicht gepusht (Orchestrator pusht; Claudian faehrt Welle 3 = Tor + Kalibrierungs-Anruf).

## Known Stubs
Keine. Alle neuen Pfade sind produktiv verdrahtet (Merge-Call live im Default-Zweig, qa_section durchgereicht, Fallback lebend).

## Self-Check: PASSED

Alle erstellten Dateien vorhanden, alle 3 Task-Commits im git log, Kern-Symbole (`analysiere_und_klassifiziere`, `_parse_merged_sections`, `MERGE_ANALYSE_QA`) verifiziert.
