---
phase: 08.23.2.H1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/claude_service.py
  - tests/test_h1_qakill.py
autonomous: true
requirements: [H1-QAKILL]
complexity: "🔴"

must_haves:
  truths:
    - "Der verworfene generate_qa_response-Antwort-Call feuert im Live-Tick NICHT mehr (0 Produktiv-Aufrufe)."
    - "classify_utterance laeuft weiter — seine LEBENDEN Konsumenten (Abstain-intent_events low-conf + FAQ used_count) bleiben unveraendert."
    - "Die Guards (SID / kw_fired_for_line / Slot-1-Mutex) und die Abstain-Emit-Logik sind unangetastet."
  artifacts:
    - path: "services/claude_service.py"
      provides: "_qa_pipeline_dispatch ohne generate_qa_response-Aufrufe; Import bereinigt"
      contains: "_qa_pipeline_dispatch"
    - path: "tests/test_h1_qakill.py"
      provides: "Runtime-Waechter: generate_qa_response wird nie aufgerufen, Abstain+used_count feuern weiter"
      contains: "def test_"
  key_links:
    - from: "analyse_loop"
      to: "_qa_pipeline_dispatch"
      via: "Aufruf pro Tick — classify_utterance BLEIBT, generate_qa_response WEG"
      pattern: "_qa_pipeline_dispatch\\("
---

<objective>
**Bau-Vorgabe 4 (isoliert vorgezogen): `generate_qa_response` streichen — mit Pflicht-Vorabcheck (Punkt 20).**

Der QA-Slot ist seit PIP-01 unterdrueckt (`_emit_qa_slot1`/`_emit_soft_hint` sind No-Ops). `classify_utterance` laeuft berechtigt weiter (Abstain-intent_events H-4 + FAQ used_count sind lebende Konsumenten) — aber der `generate_qa_response`-Aufruf erzeugt eine Antwort, die **verworfen** wird = bezahlter Aufruf ohne Konsument.

**★ Zusatzfund dieser Planung (Executor MUSS im Vorabcheck bestaetigen):** `generate_qa_response` laeuft auf `config.MODEL_QA` — Default `claude-sonnet-4-5` (`config.py:75`; belegt via `qa_pipeline.py:444` + Cost-Model-Ableitung `:473`). Der verworfene Aufruf ist also ein **verworfener Sonnet-Call im Live-Loop** — er verletzt zusaetzlich die CLAUDE.md-Constraint „Sonnet MUSS raus aus dem Live-Loop". Streichen spart mehr als ein Haiku-Tick und schliesst ein Constraint-Leck.

Diese Vorgabe gilt **unabhaengig vom Merge** und wird bewusst als Welle 1 vorgezogen: sie raeumt die toten Antwort-Zweige im Dispatch weg, BEVOR Plan 02 den Dispatch neu verdrahtet. Kleinster tragfaehiger Schnitt (Punkt 27): nur die verworfenen Aufrufe + ihr toter High-Conf-Zweig fallen; alles Lebende bleibt Byte-fuer-Byte gleich.

Purpose: Bezahlten Nutzlos-Call (Sonnet, live) entfernen + Dispatch fuer den Merge aufraeumen.
Output: Bereinigter `_qa_pipeline_dispatch`; Runtime-Waechter, der den Kill absichert UND die lebenden Konsumenten schuetzt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.H1-live-schleifen-zusammenlegen/08.23.2.H1-CONTEXT.md
@CLAUDE.md
@services/claude_service.py
@services/qa_pipeline.py

<interfaces>
<!-- Aktueller Ist-Zustand der beiden Ziel-Call-Sites (Executor arbeitet gegen diese Vertraege, KEINE Codebase-Suche noetig). -->

Dispatch-Struktur in services/claude_service.py, Funktion `_qa_pipeline_dispatch(neuer_text, line_id, kontext, ls, sio, sid=None)`:

- Import (aktuell): `from services.qa_pipeline import (classify_utterance, generate_qa_response, match_faq, apply_tabu_filter)`
- `_qa_result = classify_utterance(neuer_text, kontext, _user_id, sid=sid)` → `_kat`, `_conf` — **BLEIBT.**
- Zweig `if _kat == 'einwand_unknown':`
    - `if _conf < CLASSIFIER_CONFIDENCE_THRESHOLD:` → `_emit_abstain_event('echter_einwand')` **[LEBT]** + `_emit_soft_hint(...)` [No-Op].
    - `else:` → `_antwort = generate_qa_response(neuer_text, 'einwand_unknown', ...)` → `_emit_qa_slot1(_antwort)` [No-Op]. **← DIESER else-Zweig faellt komplett.**
- Zweig `elif _kat == 'frage':`
    - FAQ-Match: bei Treffer + NICHT tabu → `_emit_qa_slot1(_faq_antwort)` [No-Op] + **`used_count += 1` [LEBT]**.
    - Kein FAQ + `_conf < THRESHOLD` → `_emit_abstain_event('info_frage')` **[LEBT]** + `_emit_soft_hint(...)` [No-Op].
    - Kein FAQ + High-Conf → `_antwort = generate_qa_response(neuer_text, 'frage', ...)` → `_emit_qa_slot1(_antwort)` [No-Op]. **← DIESER else-Zweig faellt komplett.**

generate_qa_response Signatur (qa_pipeline.py): `generate_qa_response(utterance, category, profile_data, anrede, confidence=1.0, version='', user_id=0, sid=None) -> str` — laeuft auf `config.MODEL_QA` (Sonnet-Default), `max_tokens=500`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pflicht-Vorabcheck (Punkt 20) — Konsumenten-Freiheit von generate_qa_response beweisen</name>
  <files>tests/test_h1_qakill.py</files>
  <read_first>
    - services/claude_service.py — `_qa_pipeline_dispatch` (Import + beide `_antwort = generate_qa_response(...)`-Zweige + `_emit_qa_slot1`/`_emit_soft_hint`-Defs)
    - services/qa_pipeline.py:444/:473 (MODEL_QA-Nutzung + Cost-Model-Ableitung) + config.py:75 (`MODEL_QA` Default)
    - Vault-Regel R2 (Output-Konsum) + CONTEXT.md `<specifics>` Bonus-Kosten-Fund
  </read_first>
  <action>
    Belege AM CODE (grep, nicht Prosa), dass das Ergebnis `_antwort` beider `generate_qa_response`-Aufrufe konsumenten-frei ist — KEIN Emit, KEIN State-Write, KEIN DB-Write, KEIN Log-Konsum, KEIN Return-Weg:
    1. `grep -n "generate_qa_response" services/ -r` → beweise: genau ZWEI Produktiv-Aufrufe, beide in `_qa_pipeline_dispatch`, plus die `def` in qa_pipeline.py. Alle uebrigen Treffer sind Tests/Docs/Planung.
    2. Fuer jeden der beiden Aufrufe: `_antwort` fliesst ausschliesslich in `_emit_qa_slot1(_antwort)` (No-Op seit PIP-01) bzw. `_emit_soft_hint(...)` (No-Op), via `apply_tabu_filter` (reine Funktion, entscheidet nur WELCHER No-Op). Kein `return`, kein `ls._session_state[...]=`, kein `.commit()`, kein `sio.emit`, kein `intent_event`-Emit haengt an `_antwort`.
    3. **Falls ein Aufruf doch etwas Lebendes speist:** NICHT kappen — als Befund im SUMMARY melden und STOP.
    Schreibe das Beweis-Ergebnis als Modul-Docstring/Kommentar in tests/test_h1_qakill.py (Grep-Belege verbatim), und lege einen Runtime-Test an (kein Source-Presence, Test-Qualitaets-Regel): mocke `services.qa_pipeline.generate_qa_response` und `classify_utterance`; treibe `_qa_pipeline_dispatch` mit einem High-Conf `einwand_unknown`-Ergebnis; **assert** `generate_qa_response.called is False` NACH dem Kill (der Test ist zunaechst ROT gegen den Ist-Code — Erst-Rot-Disziplin — und wird von Task 2 GRUEN gemacht).
    Zusaetzlich: assert, dass der Test-Aufbau `config.MODEL_QA` als Sonnet-Default sieht (dokumentiert den „verworfener Sonnet-Call live"-Fund; inspect.getsource NICHT noetig — lies `config.MODEL_QA` zur Laufzeit).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -m pytest tests/test_h1_qakill.py -x -q</automated>
    <!-- Vorab-Signal (lokal). VERBINDLICHES Tor = Pytest-Gate in `deploy.sh production` (Plan 03 Checkpoint). -->
  </verify>
  <done>Grep-Beleg zeigt genau 2 Produktiv-Aufrufe, beide konsumenten-frei; Test existiert und ist ROT gegen den Ist-Code (assert not called schlaegt fehl, weil noch aufgerufen). Kein lebender Konsument gefunden.</done>
</task>

<task type="auto">
  <name>Task 2: generate_qa_response im Dispatch kappen (minimaler Schnitt), Waechter GRUEN</name>
  <files>services/claude_service.py, tests/test_h1_qakill.py</files>
  <read_first>
    - Ergebnis von Task 1 (Konsumenten-Freiheit bestaetigt)
    - services/claude_service.py — beide High-Conf-`else`-Zweige in `_qa_pipeline_dispatch`
  </read_first>
  <action>
    Minimaler Schnitt (Punkt 27 — nur das Tote faellt, alles Lebende Byte-gleich):
    1. Aus dem Import in `_qa_pipeline_dispatch` `generate_qa_response` ENTFERNEN (bleibt: `classify_utterance, match_faq, apply_tabu_filter`).
    2. Im `einwand_unknown`-Zweig: den kompletten `else:`-Block (`_antwort = generate_qa_response(... 'einwand_unknown' ...)` + die drei Folge-Emits) LOESCHEN. Der `if _conf < THRESHOLD:`-Zweig (Abstain + soft_hint) bleibt unveraendert. High-Conf `einwand_unknown` fuehrt danach zu KEINER Aktion (identisch zum heutigen Netto-Effekt: Antwort wurde verworfen).
    3. Im `frage`/kein-FAQ-Zweig: den `else:`-Block (`_antwort = generate_qa_response(... 'frage' ...)` + Folge-Emits) LOESCHEN. Der `if _conf < THRESHOLD:`-Zweig (Abstain + soft_hint) bleibt.
    4. **FAQ-used_count-Paritaet WAHREN:** im FAQ-Treffer-Pfad bleibt die `apply_tabu_filter`-Gate um `used_count += 1` EXAKT erhalten (used_count inkrementiert nur bei NICHT-tabu — heutige Semantik, lebender Konsument). Der `_emit_qa_slot1(_faq_antwort)`-Aufruf im Nicht-tabu-Zweig ist No-Op; er darf bleiben (kein Toter-Code-Zwang) ODER entfernt werden, solange used_count-Gate + -Inkrement unangetastet bleiben.
    5. `_emit_qa_slot1`/`_emit_soft_hint`-Helfer NICHT umbauen (sie bleiben Caller haben oder werden nur No-Op — geringste Blast-Radius-Wahl). `apply_tabu_filter`-Import BLEIBT (FAQ nutzt ihn).
    Danach Task-1-Test GRUEN machen (assert generate_qa_response nicht aufgerufen) + zwei Positiv-Asserts ergaenzen: (a) low-conf `einwand_unknown` feuert weiter `emit_intent_event` mit `abstained=True` (mocke `intent_event_writer.emit_intent_event`, assert called); (b) FAQ-Treffer (nicht-tabu) inkrementiert `used_count` (mocke DB-Pfad ODER assert der Code-Pfad erreicht wird via Zaehler-Stub).
    KEIN Modell-String geaendert, KEIN Kosten-Hook angefasst (Punkt 30 nicht tangiert — es faellt nur ein Call weg).
  </action>
  <verify>
    <automated>cd C:\Users\andre\dev\salesnerve && python -m pytest tests/test_h1_qakill.py tests/test_08_5_03_integration.py -q</automated>
    <!-- Vorab-Signal. Bestehende test_08_5_03_integration.py mitlaufen lassen (Regression: mockt generate_qa_response — Tests, die den Kill erwarten, ggf. anpassen; Tests, die den ALTEN Aufruf pruefen, sind stale-Contract und werden retargetet, nicht der Code zurueckgedreht). VERBINDLICH = deploy.sh production. -->
  </verify>
  <done>generate_qa_response hat 0 Produktiv-Aufrufe (grep); Waechter GRUEN; Abstain-intent_events + FAQ used_count nachweislich weiter aktiv; keine sonstige Verhaltensaenderung.</done>
</task>

</tasks>

<persistenz_schicht>
## 5. Persistenz-Schicht-Verifikation (Punkt 21) + Async-Naht (Punkt 26)

**Angefasste Persistenz — dieser Plan aendert KEIN Schema, KEINE Spalte, KEINEN Schreib-Pfad.** Er entfernt nur einen verworfenen LLM-Aufruf.

| Daten | Lese/Schreib | Schicht | Schreib-Zeitpunkt | Von H1-01 beruehrt? |
|---|---|---|---|---|
| `ProfileFaq.used_count` | Schreib (`+= 1`) | DB-Spalte (public.profile_faqs) | live in-tick | NEIN — Inkrement + tabu-Gate bleiben identisch |
| `intent_event` (Abstain-Row) | Schreib | DB-Tabelle (public.intent_event) | live in-tick, synchron | NEIN — `_emit_abstain_event` unveraendert |
| `_antwort` (generate_qa_response-Return) | — | RAM, sofort verworfen | — | JA — der Aufruf faellt weg (0 Leser) |

**Async-Bereitschafts-Naht:** entfaellt — keine der beheiltenen Schreib-Quellen wird von einem anderen Pfad vorausgelesen. Der gekappte Wert wurde nie persistiert.

**Executor/Auditor-Pflicht (ich kann von hier kein inspect.sh gegen Production):** vor Execute `inspect.sh schema profile_faqs` + `inspect.sh count intent_event` gegenpruefen, dass used_count + intent_event unveraendert existieren (reiner Beruhigungs-Check, kein Schema-Bump in diesem Plan).
</persistenz_schicht>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Kunde-Transkript → Haiku/Sonnet-Call | Untrusted Gespraechstext floss in den (jetzt gekappten) generate_qa_response-Prompt |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-H1QK-01 | Information Disclosure | verworfener Sonnet-Call im Live-Loop (MODEL_QA) | mitigate | Aufruf entfernt → kein untrusted Text mehr an Sonnet live; schliesst „kein Sonnet live"-Constraint-Leck |
| T-H1QK-02 | Denial of Service (Kosten) | bezahlter Nutzlos-Call pro einwand_unknown/frage-High-Conf-Tick | mitigate | Aufruf entfernt → Tick-Kosten sinken, keine verworfene Bezahl-Antwort mehr |
| T-H1QK-03 | Tampering | versehentliches Kappen eines lebenden Konsumenten (Abstain/used_count) | mitigate | Runtime-Waechter Task 2 asserts Abstain + used_count feuern weiter; Pflicht-Vorabcheck Task 1 |
</threat_model>

<verification>
- `grep -rn "generate_qa_response(" services/` → nur die `def` in qa_pipeline.py, 0 Aufrufe in claude_service.py.
- Runtime-Waechter tests/test_h1_qakill.py GRUEN: generate_qa_response nie aufgerufen; Abstain-intent_event + FAQ used_count feuern weiter.
- Bestehende QA-Integration-Tests laufen (stale-Contract-Tests, die den alten Aufruf erwarten, werden retargetet — nie der Code zurueckgedreht).
- **Verbindliches Tor:** Pytest-Gate in `deploy.sh production` (Plan 03) — lokale Laeufe sind nur Vorab-Signal (HART-Regel).
</verification>

<success_criteria>
- 0 Produktiv-Aufrufe von generate_qa_response; classify_utterance + Abstain-intent_events + FAQ used_count unveraendert lebendig.
- Kein Schema-/Modell-/Kosten-Hook-Change.
- Waechter sichert Kill + lebende Konsumenten als Runtime-Verhalten ab.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.H1-live-schleifen-zusammenlegen/08.23.2.H1-01-SUMMARY.md` — inkl. Grep-Beleg der Konsumenten-Freiheit + Bestaetigung MODEL_QA=Sonnet-Fund.
</output>
