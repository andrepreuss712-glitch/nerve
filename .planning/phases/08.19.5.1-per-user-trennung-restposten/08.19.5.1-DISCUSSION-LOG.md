# Phase 08.19.5.1: Per-User-Trennung Restposten — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-03
**Phase:** 08.19.5.1 — Per-User-Trennung Restposten
**Areas discussed:** Fallback sid=None, Plan-Aufteilung, Test-Strategie FT-Event

---

## Fallback sid=None

| Option | Description | Selected |
|--------|-------------|----------|
| Return-Early | Kein DB-Write, kein Global-Read. `if sid is None: print(...); return`. DSGVO-sicher. | ✓ |
| Legacy-Global-Fallback | Bei sid=None: ls.state_lock + ls.state lesen (alter Pfad). Backward-Compat. | |

**User's choice:** Return-Early
**Notes:** Beide aktuellen Caller übergeben nach Migration immer sid explizit. Legacy-Global-Fallback widerspricht dem Ziel der Phase. DSGVO-Sicherheit hat Vorrang.

---

## Plan-Aufteilung

| Option | Description | Selected |
|--------|-------------|----------|
| 1 Plan (atomar) | WR-01 + WR-02 + Tests in einem Plan. Passt zu Komplexität 🟡 mittel. | ✓ |
| 2 Plans (Wave) | Wave 1: Code-Changes. Wave 2: Tests. Mehr Overhead. | |

**User's choice:** 1 Plan (atomar)
**Notes:** Phase klein genug (~35 Zeilen Code + ~40 Zeilen Tests). Kein Wave-Overhead gerechtfertigt.

---

## Test-Strategie FT-Event

| Option | Description | Selected |
|--------|-------------|----------|
| monkeypatch SessionLocal | Identisch zu REQ-06-Pattern. In-Memory-DB. Prüft user_id der geschriebenen Row. | ✓ |
| monkeypatch db.add() | Leichterer Ansatz. Kein DB-Schema nötig. Testet nicht DB-Commit-Pfad. | |

**User's choice:** monkeypatch SessionLocal
**Notes:** Konsistenz zu bestehenden Tests in test_per_sid_migration.py. Testet echten DB-Write-Pfad (add + commit), nicht nur Row-Konstruktion.

---

## Claude's Discretion

- **Lock-Granularität**: Einmaliger `_session_state_lock`-Block für alle Felder (kein Split-Read) — technische Entscheidung, André hat nicht interveniert.
- **Test-Datei-Platzierung**: Neue Tests in bestehender `tests/test_per_sid_migration.py` (nicht neue Datei) — passt thematisch.

## Deferred Ideas

Keine — Diskussion blieb vollständig im Phase-Scope.
