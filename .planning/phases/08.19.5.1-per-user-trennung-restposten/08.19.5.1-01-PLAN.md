---
phase: 08.19.5.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/claude_service.py
  - tests/test_per_sid_migration.py
  - tests/test_ft_write_hooks.py
autonomous: true
requirements:
  - REQ-1
  - REQ-2
  - REQ-3
  - REQ-4

must_haves:
  truths:
    - "_write_ft_assistant_event liest user_id, market, language, mode, ft_session_id, kaufbereitschaft aus _session_state[sid] wenn sid gesetzt ist"
    - "analyse_loop liest active_learning_cards pro SID aus _session_state[sid]['state'] — nicht aus ls.state"
    - "FT-Event fuer SID-A traegt user_id von User-A, nicht User-B (selbst bei gleichzeitiger Session)"
    - "Alle bestehenden Tests bleiben gruen — auch test_ft_write_hooks.py nach Update"
    - "Zwei neue per-SID-Isolation-Tests in test_per_sid_migration.py beweisen Runtime-Verhalten"
  artifacts:
    - path: "services/claude_service.py"
      provides: "_write_ft_assistant_event mit sid-Parameter + Return-Early bei sid=None"
      contains: "def _write_ft_assistant_event(module, hint_type, hint_text, model_used, context=None, sid=None)"
    - path: "services/claude_service.py"
      provides: "WR-02: per-SID Learning-Cards-Read in analyse_loop"
      contains: "_session_state.get(sid, {}).get('state', {}).get('active_learning_cards', [])"
    - path: "tests/test_per_sid_migration.py"
      provides: "2 neue Isolation-Tests (WR-01 + WR-02)"
      contains: "test_write_ft_event_isolation_per_sid"
    - path: "tests/test_ft_write_hooks.py"
      provides: "Bestehende Tests auf neues sid-API aktualisiert"
      contains: "sid=sid_a"
  key_links:
    - from: "services/claude_service.py:1000 (analyse_loop FT-call)"
      to: "_write_ft_assistant_event"
      via: "sid=sid keyword argument"
      pattern: "_write_ft_assistant_event\\(.*sid=sid"
    - from: "services/claude_service.py:1548 (coaching_loop FT-call)"
      to: "_write_ft_assistant_event"
      via: "sid=sid keyword argument"
      pattern: "_write_ft_assistant_event\\(.*sid=sid"
    - from: "analyse_loop:916 (Learning-Cards-Read)"
      to: "ls._session_state[sid]['state']['active_learning_cards']"
      via: "_session_state_lock"
      pattern: "_session_state\\.get\\(sid.*active_learning_cards"
---

<objective>
WR-01 und WR-02 aus dem Phase-08.19.5-Code-Review nachmigieren: `_write_ft_assistant_event`
liest Session-Kontext per-SID statt aus dem Modul-Globalen `ls.state`; `analyse_loop:916`
liest `active_learning_cards` per-SID statt global. Danach ist Multi-User-Daten-Trennung
100% abgeschlossen.

Purpose: DSGVO-Daten-Qualitaets-Risiko eliminieren — bei zwei gleichzeitigen Nutzern
koennte FT-Event von Session A aktuell user_id aus Session B loggen.

Output:
- services/claude_service.py — WR-01 Funktion + Caller-Updates + WR-02 Read-Pfad
- tests/test_per_sid_migration.py — 2 neue per-SID-Isolation-Tests angehaengt
- tests/test_ft_write_hooks.py — bestehende Tests auf neues sid-API aktualisiert
</objective>

<execution_context>
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19.5.1-per-user-trennung-restposten/08.19.5.1-CONTEXT.md
@C:/Users/andre/dev/salesnerve/.planning/phases/08.19.5.1-per-user-trennung-restposten/08.19.5.1-SPEC.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<pruning_note>
## REQ-1: Pflicht-Grep-Ergebnis (vor Migration verifiziert)

Grep-Muster: `_write_ft_assistant_event` ueber gesamte Codebase (*.py)

Gefundene Call-Sites:
- services/claude_service.py:142 — DEFINITION (kein Caller)
- services/claude_service.py:1000 — analyse_loop FT-Hook (KEIN sid-Argument aktuell) -> MIGRIEREN
- services/claude_service.py:1548 — coaching_loop FT-Hook (KEIN sid-Argument aktuell) -> MIGRIEREN
- services/cost_tracker.py:5 — NUR KOMMENTAR, kein Funktionsaufruf -> IGNORE
- tests/test_ft_write_hooks.py:86,136,164,191 — Test-Aufrufe ohne sid (Legacy-Pfad) -> UPDATE NOETIG

BEFUND: Genau 2 Produktions-Caller (beides bekannte) + 4 Test-Caller.

WICHTIG fuer test_ft_write_hooks.py: D-01 (Return-Early bei sid=None) bricht
test_cold_call_forces_transcript_null und test_meeting_mode_preserves_transcript —
diese erwarten DB-Rows, werden aber bei sid=None early-return. Task 3 updated
diese Tests um sid korrekt zu setzen.
test_skips_when_ft_session_id_missing und test_write_hook_swallows_exceptions
bleiben valide (kein DB-Write erwartet — bleibt korrekt nach Return-Early).
</pruning_note>

<interfaces>
<!-- Aus services/claude_service.py:142-220 extrahiert — aktuelle Implementierung -->

Aktuelle Signatur (wird in Task 1 geaendert):
```python
def _write_ft_assistant_event(
    module: str,
    hint_type: str,
    hint_text: str,
    model_used: str,
    context: dict | None = None,
) -> None:
```

Aktueller Global-Read (wird in Task 1 ersetzt):
```python
with ls.state_lock:
    ft_session_id = ls.state.get('ft_session_id')
    mode          = ls.state.get('mode') or 'cold_call'
    user_id       = ls.state.get('user_id')
    market        = ls.state.get('market') or 'dach'
    language      = ls.state.get('language') or 'de'
    readiness     = ls.state.get('kaufbereitschaft')
```

<!-- Aus services/live_session.py:268-324 — _session_state[sid] Layout -->

_session_state[sid] Top-Level-Keys:
- user_id, org_id, market, language, mode, kaufbereitschaft

_session_state[sid]['state'] Sub-Keys:
- ft_session_id, active_learning_cards, is_paused, kaufbereitschaft,
  readiness_score, readiness_bucket, aktiv, version, ergebnis, line_id, ...

<!-- analyse_loop Learning-Cards-Read — aktuelle Stelle (WR-02 Target) -->
<!-- services/claude_service.py:914-921 -->

```python
# D-09: Inject active learning cards into kontext
with ls.state_lock:
    _lk_cards = ls.state.get('active_learning_cards', [])
if _lk_cards:
    ...
```

<!-- analyse_loop Caller bei Zeile 1000 — aktueller Stand (kein sid) -->
```python
_write_ft_assistant_event(
    module='assistant_live',
    hint_type=_hint_type,
    hint_text=_hint_text,
    model_used=config.MODEL_ANALYSE,
    context={
        'transcript_segment': neuer_text,
        'speaker': 'rep',
        'conversation_phase': None,
        'hint_category': ergebnis.get('typ'),
    },
)
```

<!-- coaching_loop Caller bei Zeile 1548 — aktueller Stand (kein sid) -->
```python
_write_ft_assistant_event(
    module='coaching_live',
    hint_type='coaching',
    hint_text=_coach_text,
    model_used=config.MODEL_COACHING,
    context={
        'hint_category': 'coaching',
    },
)
```

<!-- Aus tests/test_per_sid_migration.py — Pattern fuer neue Tests -->

Setup-Pattern (D-03):
```python
sid_a = 'test-ft-iso-sid-a'
ls.init_session_state(sid_a, user_id=1, org_id=1)
monkeypatch.setattr(_db_mod, 'SessionLocal', lambda: db_session)
```

Teardown-Pattern:
```python
finally:
    _clean_sids(sid_a, sid_b)
```

<!-- Aus tests/test_ft_write_hooks.py — bestehende Helper die wiederverwendbar sind -->

_seed_fixtures(db_session, mode) — erstellt Org, User, FtCallSession, PromptVersion
_FakeSession(db_session) — Wrapper um db_session fuer SessionLocal
_setup_ls_state(**overrides) — patcht ls.state (Legacy — nach Migration nicht mehr noetig)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 — _write_ft_assistant_event per-SID Migration + Caller-Updates</name>
  <read_first>
    - services/claude_service.py (Zeilen 142-220 — aktuelle Implementierung, VOLLSTAENDIG lesen)
    - services/claude_service.py (Zeilen 990-1015 — analyse_loop FT-Call-Stelle)
    - services/claude_service.py (Zeilen 1539-1560 — coaching_loop FT-Call-Stelle)
    - services/live_session.py (Zeilen 268-355 — _session_state[sid] Layout fuer Key-Bestaetigung)
    - .planning/phases/08.19.5.1-per-user-trennung-restposten/08.19.5.1-CONTEXT.md (D-01 Return-Early, D-03 Lock-Strategie)
  </read_first>
  <files>services/claude_service.py</files>
  <behavior>
    - Test WR-01-A: _write_ft_assistant_event mit sid=None gibt sofort zurueck ohne DB-Write
    - Test WR-01-B: _write_ft_assistant_event mit gesetztem sid liest user_id, market, language aus top-level _session_state[sid]; ft_session_id und kaufbereitschaft aus _session_state[sid]['state']
    - Test WR-01-C: Cold-Call-DSGVO-Enforcement bleibt aktiv — mode aus _session_state[sid] (nicht ls.state)
  </behavior>
  <action>
**Schritt 1: Signatur erweitern (Zeilen 142-147)**

Neue Signatur:
```python
def _write_ft_assistant_event(
    module: str,
    hint_type: str,
    hint_text: str,
    model_used: str,
    context: dict | None = None,
    sid: str | None = None,
) -> None:
```

**Schritt 2: Return-Early bei sid=None (D-01 — KEIN Legacy-Fallback)**

Den bestehenden Block `with ls.state_lock: ft_session_id = ls.state.get(...)` (Zeilen 164-170)
ersetzen durch:

```python
# D-01 (Phase 08.19.5.1): sid=None → skip immediately (no DB write, no global read)
if sid is None:
    print(f'[FT] skip — no sid for module={module}')
    return

with ls._session_state_lock:
    _sid_entry = ls._session_state.get(sid, {})
    _sid_state = _sid_entry.get('state', {})
    ft_session_id = _sid_state.get('ft_session_id')
    mode          = _sid_entry.get('mode') or 'cold_call'
    user_id       = _sid_entry.get('user_id')
    market        = _sid_entry.get('market') or 'dach'
    language      = _sid_entry.get('language') or 'de'
    readiness     = _sid_state.get('kaufbereitschaft')
```

Der nachfolgende Check `if ft_session_id is None or user_id is None: return` (Zeile 172-174)
bleibt UNVERAENDERT — er greift jetzt auf die per-SID Werte, nicht mehr auf ls.state.

Der restliche Funktionskörper (db = SessionLocal(), FtAssistantEvent(...), db.add/commit,
except Exception) bleibt UNVERAENDERT.

**Schritt 3: analyse_loop Caller (~Zeile 1000) — sid=sid hinzufuegen**

Den bestehenden `_write_ft_assistant_event(...)`-Aufruf in analyse_loop (nach
`# ── FT logging hook (Phase 04.7.1) ───`) erweitern:

```python
_write_ft_assistant_event(
    module='assistant_live',
    hint_type=_hint_type,
    hint_text=_hint_text,
    model_used=config.MODEL_ANALYSE,
    context={
        'transcript_segment': neuer_text,
        'speaker': 'rep',
        'conversation_phase': None,
        'hint_category': ergebnis.get('typ'),
    },
    sid=sid,
)
```

**Schritt 4: coaching_loop Caller (~Zeile 1548) — sid=sid hinzufuegen**

Den bestehenden `_write_ft_assistant_event(...)`-Aufruf in coaching_loop (nach
`# ── FT logging hook (Phase 04.7.1) ───`) erweitern:

```python
_write_ft_assistant_event(
    module='coaching_live',
    hint_type='coaching',
    hint_text=_coach_text,
    model_used=config.MODEL_COACHING,
    context={
        'hint_category': 'coaching',
    },
    sid=sid,
)
```

**WICHTIG — Lock-Konvention (D-03):**
- Einmaliger `with ls._session_state_lock:` Block fuer ALLE _session_state-Reads
- KEIN `ls.state_lock` mehr in dieser Funktion — alter Global-Lock entfernen
- Kein Split-Read (erst top-level lesen, dann state-sub-key separat)

**WICHTIG — MUST NOT raise:**
Der aeussere `except Exception as e: print(f"[FT] ...")` Wrapper bleibt unveraendert.
Return-Early bei sid=None ist KEIN Exception-Pfad — einfaches `return`.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "import ast, sys; ast.parse(open('services/claude_service.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - `_write_ft_assistant_event` Signatur enthaelt `sid: str | None = None` als letzten Parameter
    - Bei sid=None: sofortiger return mit print `[FT] skip — no sid for module=...`
    - Bei sid gesetzt: liest user_id/market/language/mode aus `ls._session_state[sid]` top-level; ft_session_id/kaufbereitschaft aus `ls._session_state[sid]['state']`
    - Lock ist `ls._session_state_lock` — kein `ls.state_lock` mehr in dieser Funktion
    - analyse_loop Caller uebergibt `sid=sid`
    - coaching_loop Caller uebergibt `sid=sid`
    - Syntax-Check: `python -c "import ast; ast.parse(open('services/claude_service.py').read())"` fehlerfrei
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 — analyse_loop Learning-Cards-Read per-SID</name>
  <read_first>
    - services/claude_service.py (Zeilen 913-922 — aktueller Learning-Cards-Read-Block)
    - services/live_session.py (Zeilen 297-325 — state-Sub-Key active_learning_cards Zeile 315)
    - .planning/phases/08.19.5.1-per-user-trennung-restposten/08.19.5.1-CONTEXT.md (WR-02 Abschnitt)
  </read_first>
  <files>services/claude_service.py</files>
  <behavior>
    - Test WR-02-A: Nach load_learning_cards(sid_A, user_A) liest analyse_loop fuer sid_A die korrekten Cards
    - Test WR-02-B: analyse_loop fuer sid_B (andere Session, keine load_learning_cards) bekommt leere Liste
  </behavior>
  <action>
**Eine Zeile ersetzen: Zeile ~915-916 (D-09-Block in analyse_loop)**

Aktueller Code:
```python
# D-09: Inject active learning cards into kontext
with ls.state_lock:
    _lk_cards = ls.state.get('active_learning_cards', [])
```

Ersetzen durch:
```python
# D-09: Inject active learning cards into kontext (per-SID — WR-02 Phase 08.19.5.1)
with ls._session_state_lock:
    _lk_cards = ls._session_state.get(sid, {}).get('state', {}).get('active_learning_cards', [])
```

Kontext: Dieser Block befindet sich INNERHALB des `for sid in active_sids:` Loops,
direkt nach dem `ls._per_sid_transcript`-Lock-Block. `sid` ist lokal verfuegbar.

Der nachfolgende `if _lk_cards:` Block (Zeilen 917-921) bleibt UNVERAENDERT.

Lock-Wahl: `ls._session_state_lock` — konsistent mit WR-01 und allen anderen
per-SID-Reads im Loop. Kein `ls.state_lock` mehr.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "import ast, sys; ast.parse(open('services/claude_service.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - Zeile ~916 lautet: `_lk_cards = ls._session_state.get(sid, {}).get('state', {}).get('active_learning_cards', [])`
    - Lock ist `ls._session_state_lock` (nicht `ls.state_lock`)
    - Kein `ls.state.get('active_learning_cards', [])` mehr in analyse_loop
    - Syntax-Check fehlerfrei
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — 2 neue Isolation-Tests + test_ft_write_hooks.py auf neues API aktualisieren</name>
  <read_first>
    - tests/test_per_sid_migration.py (VOLLSTAENDIG — Teardown-Helper, monkeypatch-Pattern, Test-Struktur)
    - tests/test_ft_write_hooks.py (VOLLSTAENDIG — _seed_fixtures, _FakeSession, alle 4 bestehenden Tests)
    - services/claude_service.py (Zeilen 142-220 — neue _write_ft_assistant_event Signatur nach Task 1)
    - services/live_session.py (Zeilen 268-355 — init_session_state Aufruf-Signatur)
  </read_first>
  <files>tests/test_per_sid_migration.py, tests/test_ft_write_hooks.py</files>
  <behavior>
    - Test NEU-A: FT-Event fuer SID-A traegt row.user_id == 1 (User-A), nicht 2 (User-B) bei zwei gleichzeitigen Sessions
    - Test NEU-B: analyse_loop-Learning-Cards-Isolation: SID-A sieht eigene Cards, SID-B sieht leere Liste
    - Test EXISTING-A: test_cold_call_forces_transcript_null schreibt weiterhin eine Row (nach Update auf sid-basiertes Setup)
    - Test EXISTING-B: test_meeting_mode_preserves_transcript schreibt weiterhin eine Row (nach Update auf sid-basiertes Setup)
  </behavior>
  <action>
**Teil A: 2 neue Tests an tests/test_per_sid_migration.py ANHAENGEN**

Ans Ende der Datei anhaengen (NICHT neue Datei erstellen):

```python
# ── REQ-2/REQ-3 (Phase 08.19.5.1): WR-01 + WR-02 per-SID Isolation ──────────

def test_write_ft_event_isolation_per_sid(db_session, monkeypatch):
    """FT-Event fuer SID-A traegt user_id von User-A, nicht User-B (WR-01 REQ-2)."""
    import services.claude_service as cs
    import database.db as _db_mod
    from database.models import FtAssistantEvent, Organisation, User, FtCallSession, PromptVersion

    # Fixtures: Org, zwei User, zwei FtCallSessions
    org = Organisation(name='IsoTest', plan='starter')
    db_session.add(org); db_session.flush()

    u_a = User(org_id=org.id, email='iso-a@nerve.de', passwort_hash='x',
               market='dach', language='de')
    u_b = User(org_id=org.id, email='iso-b@nerve.de', passwort_hash='x',
               market='dach', language='de')
    db_session.add_all([u_a, u_b]); db_session.flush()

    sess_a = FtCallSession(user_id=u_a.id, mode='meeting', market='dach', language='de')
    db_session.add(sess_a); db_session.flush()
    db_session.add(PromptVersion(
        module='assistant_live', version='v1.0.0',
        prompt_text='x' * 40, is_active=True,
    ))
    db_session.commit()

    sid_a = 'test-ft-iso-sid-a'
    sid_b = 'test-ft-iso-sid-b'
    try:
        ls.init_session_state(sid_a, user_id=u_a.id, org_id=org.id, mode='meeting',
                              market='dach', language='de')
        ls.init_session_state(sid_b, user_id=u_b.id, org_id=org.id, mode='meeting',
                              market='dach', language='de')
        # Set ft_session_id in state sub-key for sid_a
        with ls._session_state_lock:
            ls._session_state[sid_a]['state']['ft_session_id'] = sess_a.id

        monkeypatch.setattr(cs, '_ACTIVE_PROMPT_CACHE', {})
        from tests.test_ft_write_hooks import _FakeSession
        monkeypatch.setattr(_db_mod, 'SessionLocal', lambda: _FakeSession(db_session))

        def _fake_gapv(module):
            from database.models import PromptVersion as PV
            pv = db_session.query(PV).filter_by(module=module, is_active=True).first()
            return pv.version if pv else 'unknown'
        monkeypatch.setattr(cs, 'get_active_prompt_version', _fake_gapv)

        # Rufe mit SID-A auf — nur User-A darf in die Row
        cs._write_ft_assistant_event(
            module='assistant_live',
            hint_type='einwand',
            hint_text='Test Einwand',
            model_used='claude-haiku-test',
            context={'transcript_segment': 'Test', 'speaker': 'rep',
                     'conversation_phase': 'discovery'},
            sid=sid_a,
        )

        rows = db_session.query(FtAssistantEvent).all()
        assert len(rows) == 1, "Genau eine Row erwartet"
        assert rows[0].user_id == u_a.id, \
            f"Row muss user_id von User-A ({u_a.id}) haben, nicht {rows[0].user_id}"
        assert rows[0].user_id != u_b.id, \
            "Row darf NICHT user_id von User-B haben (WR-01 Isolation)"
    finally:
        _clean_sids(sid_a, sid_b)


def test_learning_cards_isolation_per_sid(monkeypatch):
    """WR-02: SID-A sieht eigene active_learning_cards; SID-B sieht leere Liste (REQ-3)."""
    sid_a = 'test-lk-iso-sid-a'
    sid_b = 'test-lk-iso-sid-b'
    try:
        ls.init_session_state(sid_a, user_id=1, org_id=1)
        ls.init_session_state(sid_b, user_id=2, org_id=1)

        # Schreibe Cards fuer SID-A (so wie load_learning_cards es tut)
        cards_a = [{'category': 'einwand', 'final_text': 'Karte A1'}]
        with ls._session_state_lock:
            ls._session_state[sid_a]['state']['active_learning_cards'] = cards_a

        # Lese per-SID wie analyse_loop es nach WR-02-Fix tut
        with ls._session_state_lock:
            lk_a = ls._session_state.get(sid_a, {}).get('state', {}).get('active_learning_cards', [])
            lk_b = ls._session_state.get(sid_b, {}).get('state', {}).get('active_learning_cards', [])

        assert lk_a == cards_a, \
            f"SID-A muss eigene Cards sehen, got: {lk_a}"
        assert lk_b == [], \
            f"SID-B muss leere Liste sehen (kein Overlap), got: {lk_b}"
    finally:
        _clean_sids(sid_a, sid_b)
```

**Teil B: tests/test_ft_write_hooks.py — 2 betroffene Tests auf sid-basiertes Setup updaten**

D-01 Return-Early bei sid=None bricht `test_cold_call_forces_transcript_null` und
`test_meeting_mode_preserves_transcript`, weil sie ohne `sid` aufrufen und DB-Rows
erwarten. Diese muessen `sid` uebergeben und die SID in `_session_state` initialisieren.

UPDATE `test_cold_call_forces_transcript_null`:
- Vor dem `_write_ft_assistant_event`-Aufruf eine SID initialisieren und ft_session_id setzen
- `_write_ft_assistant_event(..., sid=test_sid)` hinzufuegen
- `_clean_sids(test_sid)` im finally-Block

Konkreter Umbau (ersetzt den try-Block ab `ls = _setup_ls_state(...)`):

```python
def test_cold_call_forces_transcript_null(db_session, monkeypatch):
    """D-04/D-05: Cold-Call mode MUST hard-NULL transcript_segment and speaker."""
    import services.live_session as ls_mod
    from database.models import FtAssistantEvent

    _org, u, sess = _seed_fixtures(db_session, mode='cold_call')

    import database.db as dbmod
    import services.claude_service as cs
    monkeypatch.setattr(cs, '_ACTIVE_PROMPT_CACHE', {})
    fake_factory = lambda: _FakeSession(db_session)
    monkeypatch.setattr(dbmod, 'SessionLocal', fake_factory)

    def _fake_gapv(module):
        from database.models import PromptVersion
        pv = db_session.query(PromptVersion).filter_by(module=module, is_active=True).first()
        return pv.version if pv else 'unknown'
    monkeypatch.setattr(cs, 'get_active_prompt_version', _fake_gapv)

    test_sid = 'test-cold-call-sid'
    ls_mod.init_session_state(test_sid, user_id=u.id, org_id=_org.id,
                               mode='cold_call', market='dach', language='de')
    with ls_mod._session_state_lock:
        ls_mod._session_state[test_sid]['state']['ft_session_id'] = sess.id
        ls_mod._session_state[test_sid]['state']['kaufbereitschaft'] = 50

    try:
        cs._write_ft_assistant_event(
            module='assistant_live',
            hint_type='objection',
            hint_text='Test hint',
            model_used='claude-haiku-test',
            context={
                'transcript_segment': 'SHOULD BE DROPPED BY DSGVO',
                'speaker': 'customer',
                'conversation_phase': 'discovery',
                'hint_category': 'kosten',
            },
            sid=test_sid,
        )

        rows = db_session.query(FtAssistantEvent).all()
        assert len(rows) == 1, "exactly one row should be written"
        row = rows[0]
        assert row.transcript_segment is None, "D-04: Cold-Call must NULL transcript_segment"
        assert row.speaker is None, "D-05: Cold-Call must NULL speaker"
        assert row.hint_text == 'Test hint'
        assert row.hint_type == 'objection'
        assert row.hint_category == 'kosten'
        assert row.model_used == 'claude-haiku-test'
        assert row.prompt_version == 'v1.0.0'
        assert row.market == 'dach'
        assert row.language == 'de'
        assert row.readiness_score == 50
        assert row.conversation_phase == 'discovery'
    finally:
        ls_mod.pop_session_state(test_sid)
```

UPDATE `test_meeting_mode_preserves_transcript` analog:

```python
def test_meeting_mode_preserves_transcript(db_session, monkeypatch):
    """Meeting mode must preserve transcript_segment and speaker from context."""
    import services.live_session as ls_mod
    from database.models import FtAssistantEvent

    _org, u, sess = _seed_fixtures(db_session, mode='meeting')

    import database.db as dbmod
    import services.claude_service as cs
    monkeypatch.setattr(cs, '_ACTIVE_PROMPT_CACHE', {})
    monkeypatch.setattr(dbmod, 'SessionLocal', lambda: _FakeSession(db_session))

    def _fake_gapv(module):
        from database.models import PromptVersion
        pv = db_session.query(PromptVersion).filter_by(module=module, is_active=True).first()
        return pv.version if pv else 'unknown'
    monkeypatch.setattr(cs, 'get_active_prompt_version', _fake_gapv)

    test_sid = 'test-meeting-sid'
    ls_mod.init_session_state(test_sid, user_id=u.id, org_id=_org.id,
                               mode='meeting', market='dach', language='de')
    with ls_mod._session_state_lock:
        ls_mod._session_state[test_sid]['state']['ft_session_id'] = sess.id

    try:
        cs._write_ft_assistant_event(
            module='assistant_live',
            hint_type='hint',
            hint_text='Meeting hint',
            model_used='claude-haiku-test',
            context={'transcript_segment': 'kept', 'speaker': 'rep'},
            sid=test_sid,
        )

        rows = db_session.query(FtAssistantEvent).all()
        assert len(rows) == 1
        assert rows[0].transcript_segment == 'kept'
        assert rows[0].speaker == 'rep'
    finally:
        ls_mod.pop_session_state(test_sid)
```

`test_skips_when_ft_session_id_missing` und `test_write_hook_swallows_exceptions`
bleiben UNVERAENDERT — sie erwarten keinen DB-Write und sind mit Return-Early kompatibel:
- test_skips_when_ft_session_id_missing: kein sid → Return-Early → 0 Rows (Assertion bleibt korrekt)
- test_write_hook_swallows_exceptions: kein sid → Return-Early, kein DB-Call → kein Crash (Assertion bleibt korrekt)
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -m pytest tests/test_per_sid_migration.py tests/test_ft_write_hooks.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - tests/test_per_sid_migration.py enthaelt `test_write_ft_event_isolation_per_sid` und `test_learning_cards_isolation_per_sid`
    - Beide neuen Tests pruefen Runtime-Verhalten (DB-Write, State-Read) — keine Source-Presence-Checks
    - test_cold_call_forces_transcript_null und test_meeting_mode_preserves_transcript uebergeben `sid=test_sid`
    - `pytest tests/test_per_sid_migration.py tests/test_ft_write_hooks.py -v` — alle Tests GRUEN
    - Neue Tests schlagen fehl wenn WR-01/WR-02 Fixes rueckgaengig gemacht werden
  </done>
</task>

<task type="auto">
  <name>Task 4: Gesamtverifikation — alle 08.19.5-Tests bleiben gruen</name>
  <read_first>
    - Keine — reiner Test-Run
  </read_first>
  <files></files>
  <action>
Kompletten Test-Run ueber alle per-SID-Migrations-Tests ausfuehren:

```bash
cd C:/Users/andre/dev/salesnerve
python -m pytest tests/test_per_sid_migration.py tests/test_ft_write_hooks.py -v
```

Bei Fehlern: Fehler analysieren und fixen. Haeufige Stolperstellen:
- `_session_state[sid]` nicht initialisiert vor FT-Call → `init_session_state` aufrufen
- `ft_session_id` im `state`-Sub-Key nicht gesetzt → `_session_state[sid]['state']['ft_session_id'] = sess.id`
- import-Pfad fuer `_FakeSession` aus test_ft_write_hooks — direkt in Hilfsklasse kopieren statt importieren falls circular

Nach gruenen Tests: Syntax-Pruefung der Service-Datei:
```bash
python -c "import ast; ast.parse(open('services/claude_service.py').read()); print('claude_service.py syntax OK')"
```
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -m pytest tests/test_per_sid_migration.py tests/test_ft_write_hooks.py -v 2>&1 | grep -E "(PASSED|FAILED|ERROR|passed|failed|error)"</automated>
  </verify>
  <done>
    - Alle Tests in test_per_sid_migration.py PASSED (inklusive 2 neue)
    - Alle Tests in test_ft_write_hooks.py PASSED (alle 4)
    - Kein FAILED, kein ERROR
    - Acceptance-Checklist aus SPEC.md Zeile 82-90 vollstaendig erfuellt
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background-Thread → DB | analyse_loop/coaching_loop schreiben FT-Events — kein User-Input im Pfad |
| _session_state[sid] → FtAssistantEvent | user_id aus Session-State bestimmt wessen Daten geloggt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08191-01 | Information Disclosure | _write_ft_assistant_event: user_id-Leak | mitigate | Per-SID-Read aus _session_state[sid] — kein Modul-Global mehr; Return-Early bei sid=None verhindert jeden globalen Zugriff |
| T-08191-02 | Information Disclosure | analyse_loop: Learning-Cards shared across sessions | mitigate | WR-02: Read aus _session_state[sid]['state']['active_learning_cards'] — per-SID isoliert |
| T-08191-03 | Denial of Service | _write_ft_assistant_event crasht background thread | accept | MUST-NOT-raise Invariante bleibt — except Exception Block unveraendert; Return-Early ist kein Exception-Pfad |
</threat_model>

<verification>
Nach Abschluss aller Tasks:

1. Grep-Pruefung WR-01:
```bash
grep -n "sid=sid" C:/Users/andre/dev/salesnerve/services/claude_service.py | grep "_write_ft_assistant_event"
```
Erwartung: 2 Treffer (analyse_loop ~1000, coaching_loop ~1548)

2. Grep-Pruefung WR-02:
```bash
grep -n "active_learning_cards" C:/Users/andre/dev/salesnerve/services/claude_service.py
```
Erwartung: `_session_state.get(sid, {}).get('state', {}).get('active_learning_cards', [])` — kein `ls.state.get('active_learning_cards')`

3. Return-Early verifizieren:
```bash
grep -n "no sid for module" C:/Users/andre/dev/salesnerve/services/claude_service.py
```
Erwartung: 1 Treffer bei der Return-Early-Zeile

4. Kein Legacy-Global-Read mehr:
```bash
grep -n "ls\.state\.get\('ft_session_id'\)\|ls\.state\.get\('user_id'\)\|ls\.state\.get\('mode'\)" C:/Users/andre/dev/salesnerve/services/claude_service.py
```
Erwartung: 0 Treffer in `_write_ft_assistant_event` (die Funktion endet bei Zeile 220)

5. Test-Suite:
```bash
cd C:/Users/andre/dev/salesnerve && python -m pytest tests/test_per_sid_migration.py tests/test_ft_write_hooks.py -v
```
Erwartung: Alle Tests PASSED
</verification>

<success_criteria>
- [ ] `_write_ft_assistant_event` Signatur enthaelt `sid: str | None = None`
- [ ] Bei sid=None: sofortiger return mit `[FT] skip — no sid for module=...`
- [ ] Bei sid gesetzt: liest aus `ls._session_state[sid]` (top-level + `['state']` sub-key)
- [ ] Lock ist `ls._session_state_lock` — kein `ls.state_lock` mehr in der Funktion
- [ ] analyse_loop Caller bei ~Zeile 1000 uebergibt `sid=sid`
- [ ] coaching_loop Caller bei ~Zeile 1548 uebergibt `sid=sid`
- [ ] analyse_loop:916 liest `active_learning_cards` aus `_session_state[sid]['state']`
- [ ] 2 neue Tests in test_per_sid_migration.py: test_write_ft_event_isolation_per_sid + test_learning_cards_isolation_per_sid
- [ ] test_cold_call_forces_transcript_null und test_meeting_mode_preserves_transcript uebergeben `sid` und bleiben GRUEN
- [ ] `pytest tests/test_per_sid_migration.py tests/test_ft_write_hooks.py -v` — 0 Failures
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen:
`.planning/phases/08.19.5.1-per-user-trennung-restposten/08.19.5.1-01-SUMMARY.md`

Inhalt: Welche Zeilen geaendert wurden, Grep-Ergebnis-Bestaetigung, Test-Ergebnis.
</output>
