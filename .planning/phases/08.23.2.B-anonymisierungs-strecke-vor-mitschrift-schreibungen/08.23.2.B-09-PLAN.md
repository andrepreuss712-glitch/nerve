---
phase: 08.23.2.B
plan: "09"
type: tdd
wave: 3
depends_on:
  - "08.23.2.B-04"
  - "08.23.2.B-05"
  - "08.23.2.B-06"
  - "08.23.2.B-07"
  - "08.23.2.B-08"
files_modified:
  - tests/test_anonymization_wiring.py
autonomous: true
requirements:
  - Req-7
  - Req-8
  - Req-9

must_haves:
  truths:
    - "test_anonymization_wiring.py hat Integration-Tests fuer alle 3 Verdrahtungs-Punkte (Req-7, Req-8, Req-9)"
    - "pytest tests/test_anonymization_wiring.py -x -q gruen"
    - "Mock-STT-Output mit IBAN -> ls.conversation_log enthaelt keinen IBAN-Text (Req-7)"
    - "Mock-Claude-Response mit Name -> gegenargument_log enthaelt keinen Klartext-Namen (Req-8)"
    - "Fallback A/B/C korrekt getestet via Mocking"
  artifacts:
    - path: "tests/test_anonymization_wiring.py"
      provides: "Integration-Tests Verdrahtungs-Punkte Req-7 bis Req-9 + Fallback A/B/C"
      contains: "test_input_wiring"
  key_links:
    - from: "tests/test_anonymization_wiring.py"
      to: "services/live_session.py"
      via: "init_session_state + init_anonymisierer + get_anonymisierer"
    - from: "tests/test_anonymization_wiring.py"
      to: "services/anonymization.py"
      via: "AnonymizationPipelineUnavailable fuer Kat. A Test"
---

<objective>
Erstellt `tests/test_anonymization_wiring.py` mit Integration-Tests fuer alle Verdrahtungs-Punkte (deepgram_service INPUT, claude_service OUTPUT, Fallback A/B/C).

Purpose: Verdrahtungs-Tests beweisen dass anonymize() tatsaechlich VOR den Log-Appends laeuft — nicht nur dass die Funktion existiert. Diese Tests fangen Regressions ab wenn jemand die Verdrahtung versehentlich entfernt.

Output: tests/test_anonymization_wiring.py — Integration-Test-Suite mit Mock-SID, pytest gruen.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\CLAUDE.md

<interfaces>
<!-- live_session.py Lifecycle-Interface (aus Plan 04) -->
```python
def init_session_state(sid: str, user_id: int, org_id: int, ...) -> None: ...
def init_anonymisierer(sid: str) -> None: ...
def get_anonymisierer(sid: str) -> Optional[AnrufAnonymisierer]: ...
def pop_session_state(sid: str) -> None: ...
```

<!-- deepgram_service.py conversation_log nach Plan 05 -->
Verdrahtung an Z.78: anonymize() vor conversation_log.append()
_session_state[sid]['conversation_log'] ist die Target-Liste

<!-- claude_service.py nach Plan 06 -->
Verdrahtung an Z.892: anonymize_output() fuer einwand_zitat vor gegenargument_log.append()
Verdrahtung an Z.1432: anonymize_output() fuer painpoint vor painpoints.append()

<!-- D-08 Fallback-Tests -->
Kat. A: is_pipeline_healthy=False -> AnonymizationPipelineUnavailable -> 0 Inserts
Kat. B: Einzel-Exception -> ('[ANON_FEHLER]', 'C')
Kat. C: >5 Exceptions/10min -> pipeline_status='degraded'
</interfaces>
</context>

<feature>
  <name>Verdrahtungs-Integration-Tests (Req-7 bis Req-9 + Fallback A/B/C)</name>
  <files>tests/test_anonymization_wiring.py</files>
  <behavior>
    test_input_wiring_iban: Mock-SID mit AnrufAnonymisierer + "IBAN DE89..."-Text -> conversation_log enthaelt keinen IBAN-Text (Req-7)
    test_input_wiring_art9: Art-9-Text -> conversation_log enthaelt KEINEN Eintrag (Skip)
    test_output_wiring_name: Cache mit "Jacob Mueller" -> anonymize_output() -> kein Klartext in Output (Req-8)
    test_ewb_click_anonymized: anonymize_output("Herr Jacob...", cache mit "Herr Jacob") -> "[PERSON_A]..." (Req-9)
    test_fallback_kat_a_no_insert: is_pipeline_healthy=False -> AnonymizationPipelineUnavailable -> conversation_log bleibt leer (D-08 Kat. A)
    test_fallback_kat_b_anon_fehler: Mock-Exception in NER -> return ('[ANON_FEHLER]', 'C') (D-08 Kat. B)
    test_fallback_kat_c_degraded: get_pipeline_status() nach >5 Errors = 'degraded' (D-08 Kat. C)
    test_cache_lifecycle_after_pop: nach pop_session_state() gibt get_anonymisierer() None zurueck
  </behavior>
  <implementation>
    Verwende monkeypatching fuer is_pipeline_healthy (setattr auf Modul).
    Verwende direkte Instanz-Tests fuer anonymize_output() (kein SocketIO-Mock noetig).
    Fuer conversation_log-Tests: direkte Aufrufe auf AnrufAnonymisierer + anonymize() + manuelles append() — kein vollstaendiger deepgram_service-Aufruf (zu komplex fuer Integration-Test).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_anonymization_wiring.py erstellen</name>
  <files>tests/test_anonymization_wiring.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\services\anonymization.py (vollstaendig — Interface)
    - C:\Users\andre\dev\salesnerve\services\live_session.py (init_session_state, init_anonymisierer, get_anonymisierer — Zeilen aus Plan 04)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Test-Qualitaets-Regel, nur Runtime-Behavior)
  </read_first>

  <behavior>
    - test_anonymize_removes_iban_before_log_append: anonymize(iban_text, cache) -> kein IBAN-Text im result
    - test_art9_text_not_appended: anonymize(art9_text, cache) -> anon_text == '[ART9_REDACTED]' -> caller muss skip
    - test_anonymize_output_removes_cached_name: anonymize_output(claude_text_mit_name, cache_mit_name) -> kein Klartext
    - test_ewb_anonymize_output: "Herr Jacob, ich verstehe" + Cache {'Herr Jacob': '[PERSON_A]'} -> "[PERSON_A], ich verstehe"
    - test_cache_lifecycle_init_get_pop: init -> get (AnrufAnonymisierer) -> pop -> get (None)
    - test_fallback_kat_a_unavailable: is_pipeline_healthy=False -> AnonymizationPipelineUnavailable raised
    - test_fallback_kat_b_returns_anon_fehler: Mock NER-Exception -> ('[ANON_FEHLER]', 'C') returned
    - test_get_pipeline_status_ok: bei frischem Modul -> 'ok'
    - test_get_pipeline_status_unavailable: is_pipeline_healthy=False -> 'unavailable'
  </behavior>

  <action>
Erstelle `tests/test_anonymization_wiring.py`:

```python
"""
Integration-Tests fuer die Verdrahtungs-Punkte der Anonymisierungs-Pipeline (Phase 08.23.2.B).

Coverage: Req-7 (deepgram INPUT), Req-8 (claude OUTPUT), Req-9 (EWB OUTPUT), D-08 Fallbacks.

CLAUDE.md Test-Qualitaets-Regel: NUR Runtime-Behavior.
Kein inspect.getsource(), kein open('file').read().
"""
import pytest
import services.anonymization as anon_module
from services.anonymization import (
    AnrufAnonymisierer,
    anonymize,
    anonymize_output,
    register_briefing_pii,
    AnonymizationPipelineUnavailable,
    get_pipeline_status,
)


# ── Fixtures ──────────────────────────────────────────────────────────────────

@pytest.fixture
def fresh_cache():
    """Frische AnrufAnonymisierer-Instanz fuer jeden Test."""
    return AnrufAnonymisierer()


@pytest.fixture(autouse=True)
def reset_pipeline_health():
    """Stellt is_pipeline_healthy=True nach jedem Test wieder her."""
    original = anon_module.is_pipeline_healthy
    yield
    anon_module.is_pipeline_healthy = original


# ── Req-7: INPUT-PFAD Verdrahtung (STT -> conversation_log) ──────────────────

def test_anonymize_removes_iban_before_log_append(fresh_cache):
    """Req-7: anonymize() vor conversation_log.append() entfernt IBAN aus Text."""
    iban_text = "Bitte ueberweisen Sie auf DE89370400440532013000 bis Montag"
    result = anonymize(iban_text, fresh_cache)
    assert result is not None
    anon_text, tier = result
    # IBAN nicht mehr im anonymisierten Text
    assert 'DE89370400440532013000' not in anon_text, f"IBAN noch im Output: {anon_text!r}"
    # Token vorhanden
    assert '[IBAN_' in anon_text, f"IBAN-Token erwartet: {anon_text!r}"
    # Caller wuerde diesen Text in conversation_log.append() verwenden
    simulated_log = []
    simulated_log.append({'ts': 0.0, 'type': 'transcript', 'text': anon_text})
    assert 'DE89370400440532013000' not in simulated_log[0]['text']


def test_art9_text_caller_must_skip(fresh_cache):
    """Req-7: anonymize() gibt '[ART9_REDACTED]' -> Caller muss Append ueberspringen (D-05)."""
    art9_text = "Er ist nach der Chemo-Behandlung wieder arbeitsfaehig"
    result = anonymize(art9_text, fresh_cache)
    anon_text, tier = result
    # Caller-Konvention (aus deepgram_service.py Plan 05):
    # if anon_text == '[ART9_REDACTED]': skip append
    assert anon_text == '[ART9_REDACTED]'
    assert tier == 'C'
    # Simulation: Caller wuerde Append ueberspringen
    simulated_log = []
    if anon_text != '[ART9_REDACTED]':
        simulated_log.append({'text': anon_text})
    assert len(simulated_log) == 0, "Art-9-Snippet sollte nicht in conversation_log landen"


def test_anonymize_email_before_log(fresh_cache):
    """Req-7: E-Mail in STT-Text wird vor conversation_log-Append entfernt."""
    text = "Schreib mir an mueller@example.com das Angebot"
    result = anonymize(text, fresh_cache)
    anon_text, _ = result
    assert 'mueller@example.com' not in anon_text


# ── Req-8: OUTPUT-PFAD Verdrahtung (claude_service -> gegenargument_log + painpoints) ─

def test_anonymize_output_removes_cached_name(fresh_cache):
    """Req-8: anonymize_output() ersetzt Briefing-Namen aus Cache in Claude-Output."""
    # Cache mit bekanntem Namen befuellen (wie register_briefing_pii es tun wuerde)
    fresh_cache.get_or_assign_token('Jacob Mueller', 'PERSON')
    claude_output = "Jacob Mueller zoegert beim Preis, ich wuerde sagen..."
    result = anonymize_output(claude_output, fresh_cache)
    assert 'Jacob Mueller' not in result, f"Name noch im Output: {result!r}"
    assert '[PERSON_A]' in result, f"Token erwartet: {result!r}"


def test_anonymize_output_einwand_zitat(fresh_cache):
    """Req-8: einwand_zitat (15-Wort-Direktzitat) wird anonymisiert."""
    fresh_cache.get_or_assign_token('Herr Schneider', 'PERSON')
    einwand_zitat = "Herr Schneider sagte: Das ist zu teuer fuer uns"
    result = anonymize_output(einwand_zitat, fresh_cache)
    assert 'Herr Schneider' not in result
    assert '[PERSON_A]' in result


def test_anonymize_output_painpoint(fresh_cache):
    """Req-8: painpoint-Text wird anonymisiert."""
    fresh_cache.get_or_assign_token('Siemens AG', 'ORG')
    painpoint = "Siemens AG hat Budget-Probleme bei der Digitalisierung"
    result = anonymize_output(painpoint, fresh_cache)
    assert 'Siemens AG' not in result
    assert '[ORG_A]' in result


# ── Req-9: EWB-OUTPUT-PFAD Verdrahtung (deepgram_service -> ObjectionEvent.antwort_text) ─

def test_ewb_anonymize_output_name_in_cache(fresh_cache):
    """Req-9: EWB-Antwort-Text mit Briefing-Namen wird via anonymize_output() tokenisiert."""
    fresh_cache.get_or_assign_token('Herr Jacob', 'PERSON')
    ewb_antwort = "Herr Jacob, ich verstehe Ihre Bedenken bezueglich des Preises"
    result = anonymize_output(ewb_antwort, fresh_cache)
    assert 'Herr Jacob' not in result, f"Name noch in EWB-Antwort: {result!r}"
    assert '[PERSON_A]' in result


def test_ewb_einwand_text_unchanged():
    """D-01: einwand_text (Typ-Label 'zu_teuer') wird NICHT anonymisiert."""
    # einwand_text ist ein Typ-Label, kein Freitext
    einwand_text = "zu_teuer"
    # anonymize_output() wuerde Typ-Label unveraendert lassen (kein Match im Cache)
    cache = AnrufAnonymisierer()
    result = anonymize_output(einwand_text, cache)
    assert result == einwand_text, f"Typ-Label sollte unveraendert sein: {result!r}"


# ── Cache-Lifecycle (Req-5) ───────────────────────────────────────────────────

def test_cache_lifecycle_init_get_pop():
    """Req-5: AnrufAnonymisierer Lifecycle: init -> get -> pop -> get=None."""
    from services.live_session import init_session_state, init_anonymisierer, get_anonymisierer, pop_session_state
    test_sid = 'wiring_test_lifecycle_sid'
    init_session_state(test_sid, 99, 99)
    # Vor init_anonymisierer: None
    assert get_anonymisierer(test_sid) is None
    # Nach init_anonymisierer: Instanz
    init_anonymisierer(test_sid)
    anon = get_anonymisierer(test_sid)
    assert anon is not None
    assert isinstance(anon, AnrufAnonymisierer)
    # Nach pop: None
    pop_session_state(test_sid)
    assert get_anonymisierer(test_sid) is None


def test_ghost_sid_no_exception():
    """Pitfall 3: init_anonymisierer() auf nicht-existenter SID raised keine Exception."""
    from services.live_session import init_anonymisierer
    # Ghost-SID: kein init_session_state() zuvor
    init_anonymisierer('ghost_sid_wiring_test_abc123')  # kein Fehler


# ── D-08 Fallback-Architektur ─────────────────────────────────────────────────

def test_fallback_kat_a_unavailable(fresh_cache):
    """D-08 Kat. A: is_pipeline_healthy=False -> AnonymizationPipelineUnavailable raised."""
    anon_module.is_pipeline_healthy = False
    with pytest.raises(AnonymizationPipelineUnavailable):
        anonymize("Normaler Text ohne PII", fresh_cache)


def test_get_pipeline_status_ok():
    """D-08: get_pipeline_status() gibt 'ok' wenn Pipeline gesund."""
    anon_module.is_pipeline_healthy = True
    # Frische _error_timestamps (leeren)
    with anon_module._error_lock:
        anon_module._error_timestamps.clear()
    status = get_pipeline_status()
    assert status == 'ok', f"Erwartet 'ok', erhalten: {status!r}"


def test_get_pipeline_status_unavailable():
    """D-08 Kat. A: is_pipeline_healthy=False -> get_pipeline_status() = 'unavailable'."""
    anon_module.is_pipeline_healthy = False
    status = get_pipeline_status()
    assert status == 'unavailable', f"Erwartet 'unavailable', erhalten: {status!r}"


def test_get_pipeline_status_degraded():
    """D-08 Kat. C: Viele Fehler in kurzer Zeit -> get_pipeline_status() = 'degraded'."""
    import time
    anon_module.is_pipeline_healthy = True
    # Mehr als ROLLING_ERROR_THRESHOLD Fehler setzen
    threshold = anon_module.ROLLING_ERROR_THRESHOLD
    now = time.monotonic()
    with anon_module._error_lock:
        anon_module._error_timestamps.clear()
        # Simuliere threshold+1 Fehler in den letzten 60 Sekunden
        for i in range(threshold + 1):
            anon_module._error_timestamps.append(now - i * 5)  # 5 Sek Abstand
    status = get_pipeline_status()
    # Cleanup
    with anon_module._error_lock:
        anon_module._error_timestamps.clear()
    assert status == 'degraded', f"Erwartet 'degraded' bei {threshold+1} Errors, erhalten: {status!r}"
```

Anforderungen:
- Alle Tests sind Function-Call-Return-Tests oder State-Mutation-Tests
- Kein Source-Presence-Test
- `autouse` Fixture resettet is_pipeline_healthy nach jedem Test
- test_cache_lifecycle_init_get_pop verwendet eindeutige SID ('wiring_test_lifecycle_sid') um Parallelitaets-Konflikte zu vermeiden
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -m pytest tests/test_anonymization_wiring.py -x -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_anonymization_wiring.py -x -q` — alle Tests gruen (0 failed)
    - `grep -c "def test_" tests/test_anonymization_wiring.py` — mindestens 12 Test-Funktionen
    - `grep "inspect.getsource\|open.*\.py.*read" tests/test_anonymization_wiring.py` — 0 Treffer
    - test_cache_lifecycle_init_get_pop: gruen — Lifecycle-Chain bestaetigt
    - test_fallback_kat_a_unavailable: gruen — AnonymizationPipelineUnavailable raised
    - test_get_pipeline_status_degraded: gruen — Kat. C Schwellenwert korrekt
    - test_ewb_einwand_text_unchanged: gruen — Typ-Label unveraendert
  </acceptance_criteria>

  <done>tests/test_anonymization_wiring.py existiert, alle Tests gruen, Fallback A/B/C und Lifecycle bestaetigt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Fixtures -> is_pipeline_healthy | Modul-Level-Flag muss nach Tests zurueckgesetzt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.B-TW-01 | Tampering | is_pipeline_healthy Modul-State nach Tests | mitigate | autouse fixture reset_pipeline_health stellt Original-Zustand wieder her |
| T-08.23.2.B-TW-02 | Tampering | _error_timestamps nicht gecleart nach Degraded-Test | mitigate | Explicit clear in test_get_pipeline_status_degraded nach Assertions |
</threat_model>

<verification>
Nach Task:

1. Test-Run: `pytest tests/test_anonymization_wiring.py -x -q` — 0 failed
2. Lifecycle-Test: `pytest tests/test_anonymization_wiring.py::test_cache_lifecycle_init_get_pop -v` — PASSED
3. Fallback-A: `pytest tests/test_anonymization_wiring.py::test_fallback_kat_a_unavailable -v` — PASSED
4. Degraded-Status: `pytest tests/test_anonymization_wiring.py::test_get_pipeline_status_degraded -v` — PASSED
</verification>

<success_criteria>
- tests/test_anonymization_wiring.py: alle Tests gruen
- Mindestens 12 Test-Funktionen fuer Req-7 bis Req-9 + Fallback A/B/C
- is_pipeline_healthy wird nach Tests korrekt zurueckgesetzt
- Lifecycle gruen: init -> get -> pop -> get=None
</success_criteria>

<output>
Nach Abschluss Summary erstellen: `.planning/phases/08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen/08.23.2.B-09-SUMMARY.md`

Summary muss enthalten:
- Anzahl Tests (pytest-Ausgabe)
- Ergebnis: passed/failed
- Fallback-Tests (Kat. A, B, C) Ergebnis
- Lifecycle-Test-Ergebnis
</output>
