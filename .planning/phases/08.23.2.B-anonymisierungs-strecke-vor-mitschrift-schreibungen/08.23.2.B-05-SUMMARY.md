---
phase: "08.23.2.B"
plan: "05"
subsystem: anonymization-wiring
tags:
  - deepgram
  - anonymization
  - dsgvo
  - input-pfad
  - output-pfad
dependency_graph:
  requires:
    - "08.23.2.B-01"  # anonymization.py Modul (anonymize, anonymize_output, AnonymizationPipelineUnavailable)
    - "08.23.2.B-03"  # art9_keywords.py
    - "08.23.2.B-04"  # get_anonymisierer() in live_session.py
  provides:
    - "deepgram_service.py INPUT-PFAD verdrahtet (Z.77-106)"
    - "deepgram_service.py OUTPUT-PFAD verdrahtet (Z.591-602)"
  affects:
    - "services/deepgram_service.py"
tech_stack:
  added: []
  patterns:
    - "Pre-Insert-Skip-Check via Sentinel-Wert-Vergleich (Finding 4)"
    - "Fail-open fuer Output-Pfad (EWB-Antwort unveraendert bei Fehler)"
    - "Fail-closed fuer Input-Pfad (kein DB-Write bei Fehler)"
key_files:
  created: []
  modified:
    - "services/deepgram_service.py"
decisions:
  - "INPUT-PFAD fail-closed: bei Fehler kein conversation_log.append() — lieber kein Eintrag als roher PII-Text"
  - "OUTPUT-PFAD fail-open: bei anonymize_output()-Fehler bleibt _antwort unveraendert — EWB-Flow darf nicht reissen"
  - "einwand_text=typ (Typ-Label) wird nicht anonymisiert — kein Freitext, kein PII-Risiko (D-01)"
  - "Finding 4: Beide Sentinel-Werte ('[ART9_REDACTED]', '[ANON_FEHLER]') explizit vor append() abgefangen"
metrics:
  duration: "~15 min"
  completed_date: "2026-05-13"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 1
---

# Phase 08.23.2.B Plan 05: Deepgram-Anonymisierungs-Verdrahtung Summary

**One-liner:** anonymize() und anonymize_output() in deepgram_service.py verdrahtet — STT-Transcript und EWB-Antworten laufen jetzt durch DSGVO-Filter vor DB-Write.

## Tasks

| Task | Name | Commit | Zeilennummern |
|------|------|--------|---------------|
| 1 | INPUT-PFAD anonymize() vor conversation_log.append() | d52673b | Z.77-106 |
| 2 | OUTPUT-PFAD anonymize_output() vor record_ewb_click() | f392c94 | Z.591-602 |

## Exakte Zeilennummern der Aenderungen

**INPUT-PFAD (Task 1):**
- Block beginnt Z.77: `# Phase 08.23.2.B: INPUT-PFAD Anonymisierung`
- `anonymize(text, _anon_cache)` Aufruf: Z.84
- Art-9-Skip-Check: Z.86 (`if _anon_text == '[ART9_REDACTED]'`)
- ANON_FEHLER-Skip-Check: Z.89 (`elif _anon_text == '[ANON_FEHLER]'`)
- `conversation_log.append()` nur im else-Branch: Z.94-98
- `AnonymizationPipelineUnavailable`-Handler: Z.99-101

**OUTPUT-PFAD (Task 2):**
- Block beginnt Z.591: `# Phase 08.23.2.B: OUTPUT-PFAD Anonymisierung`
- `anonymize_output(_antwort, _anon_cache)` Aufruf: Z.599
- `record_ewb_click()` nach Anonymisierung: Z.604

## Pre-Insert-Control-Flow-Audit Ergebnis

**INPUT-PFAD Audit (Z.47-114):**
- Block laeuft innerhalb `if result.is_final:` Branch — korrekt, nur finale Transkripte werden persistiert
- `ls.log_lock` Lock war bereits vorhanden — der neue Block verschiebt den Lock in den `else`-Branch (nur bei erfolgreich anonymisiertem Text)
- Race-Condition (Ghost-SID): `get_anonymisierer(sid)` gibt `None` zurueck wenn SID nicht mehr im State ist. `anonymize(text, None)` laeuft dann in Regex-Only-Modus — kein Crash, kein unbewachter PII-Write
- Keine weiteren Race-Conditions gefunden

**OUTPUT-PFAD Audit (Z.538-594):**
- `_run()` ist eine Closure — `_sid` aus outer scope, stabil
- `_antwort` wird nach `streame_manual_ewb_variante()` gesetzt — genau der richtige Punkt vor `record_ewb_click()`
- `einwand_text=typ` ist ein Typ-Label (z.B. 'zu_teuer') — kein Freitext, kein Anonymisierungs-Bedarf bestätigt

## Syntax-Check Ergebnis

```
python -c "import ast; ast.parse(open('services/deepgram_service.py').read()); print('Syntax OK')"
→ Syntax OK
```

Beide Tasks ergaben syntaktisch korrekten Python-Code.

## Finding 4 Fix dokumentiert

**Finding 4:** `anonymize()` gibt `('[ART9_REDACTED]', 'C')` oder `('[ANON_FEHLER]', 'C')` zurueck statt `None`. Ohne expliziten Skip-Check wuerden diese Sentinel-Werte als Klartext in `conversation_log` persistiert werden.

**Fix in Task 1 (INPUT-PFAD):**
- `if _anon_text == '[ART9_REDACTED]':` — kein append(), Log-Print
- `elif _anon_text == '[ANON_FEHLER]':` — kein append(), Log-Print
- `else:` — erst hier wird `ls.conversation_log.append()` aufgerufen

**Finding 4 gilt NICHT fuer OUTPUT-PFAD:** `anonymize_output()` implementiert einen Cache-Reverse-Lookup und gibt nie Sentinel-Werte zurueck. Kein Skip-Check noetig.

## Threat-Model Abdeckung

| Threat ID | Status |
|-----------|--------|
| T-08.23.2.B-DG-01 | Mitigiert — anonymize() vor append(), Art-9-Skip |
| T-08.23.2.B-DG-02 | Mitigiert — Ghost-SID via None-Cache abgefangen |
| T-08.23.2.B-DG-03 | Mitigiert — anonymize_output() vor record_ewb_click() |
| T-08.23.2.B-DG-04 | Mitigiert — einwand_text=typ unveraendert |
| T-08.23.2.B-DG-05 | Mitigiert — Beide Sentinel-Werte explizit abgefangen (Finding 4) |

## Deviations from Plan

None — Plan exakt wie beschrieben ausgefuehrt. Beide Eingriffsstellen entsprachen dem Plan (exakter Match von VORHER-Bloecken). Keine strukturellen Abweichungen.

## Known Stubs

None.

## Threat Flags

None — keine neuen Trust-Boundaries eingeführt. Beide Eingriffsstellen lagen bereits auf bestehenden Boundaries (STT-Callback -> conversation_log, Claude-EWB -> record_ewb_click).

## Self-Check: PASSED

- [x] `services/deepgram_service.py` existiert und ist syntaktisch korrekt
- [x] Commit d52673b existiert (INPUT-PFAD)
- [x] Commit f392c94 existiert (OUTPUT-PFAD)
- [x] `grep -n "ART9_REDACTED" services/deepgram_service.py` gibt Treffer vor conversation_log.append()
- [x] `grep -n "anonymize_output" services/deepgram_service.py` gibt Treffer vor record_ewb_click()
