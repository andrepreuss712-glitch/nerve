---
phase: 08.23.2.B
plan: "04"
type: execute
wave: 2
depends_on:
  - "08.23.2.B-01"
files_modified:
  - services/live_session.py
autonomous: true
requirements:
  - Req-5

must_haves:
  truths:
    - "init_session_state() initialisiert _session_state[sid]['anonymisierer'] = None"
    - "init_anonymisierer(sid) erstellt AnrufAnonymisierer-Instanz fuer SID nach init_session_state"
    - "get_anonymisierer(sid) gibt AnrufAnonymisierer-Instanz oder None zurueck"
    - "pop_session_state() loescht AnrufAnonymisierer automatisch (via Dict-Pop)"
    - "AnrufAnonymisierer.mapping landet NIEMALS in DB"
  artifacts:
    - path: "services/live_session.py"
      provides: "Cache-Lifecycle: init_anonymisierer(), get_anonymisierer() als neue Funktionen"
      contains: "init_anonymisierer"
  key_links:
    - from: "init_session_state()"
      to: "_session_state[sid]['anonymisierer']"
      via: "Neu initialisiert als None; init_anonymisierer() setzt echte Instanz"
    - from: "pop_session_state()"
      to: "AnrufAnonymisierer"
      via: "_session_state.pop(sid, None) loescht 'anonymisierer'-Key automatisch"
---

<objective>
Verdrahtet den AnrufAnonymisierer-Lifecycle in `services/live_session.py`: init/get/cleanup-Funktionen als neue Module-Level-Functions.

Purpose: Bevor deepgram_service.py und claude_service.py anonymize() aufrufen koennen, brauchen sie eine Moeglichkeit die AnrufAnonymisierer-Instanz per SID zu holen (D-06).

Output: Zwei neue Funktionen in live_session.py — `init_anonymisierer(sid)` und `get_anonymisierer(sid)` — plus `'anonymisierer': None` als neuer Key in init_session_state().
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\CLAUDE.md

<interfaces>
<!-- Aus services/live_session.py — bestehende Struktur (verifiziert) -->

init_session_state() Signatur (Z.268-356):
```python
def init_session_state(sid: str, user_id: int, org_id: int, profile_id=None,
                       market: str = 'dach', language: str = 'de',
                       mode: str = 'meeting') -> None:
    """Initialize _session_state[sid] for a new WebSocket connection (D-02)."""
    with _session_state_lock:
        _session_state[sid] = {
            'user_id': user_id,
            # ... viele Keys ...
            '_line_id_counter': 0,
            # NEU: 'anonymisierer': None wird hier hinzugefuegt
        }
    # WR-03: init per-SID coaching buffer...
    with _per_sid_coaching_lock:
        _per_sid_coaching_buffer[sid] = []
```

pop_session_state() (Z.362-374):
```python
def pop_session_state(sid: str) -> None:
    with _session_state_lock:
        _session_state.pop(sid, None)   # loescht 'anonymisierer'-Key automatisch
    # ... weitere pops ...
```

set_briefing_for_sid() Pattern (Z.249-259):
```python
def set_briefing_for_sid(sid: str, briefing_text: str | None) -> None:
    with _session_state_lock:
        if sid not in _session_state:
            _logger.debug(f"[SID] set_briefing_for_sid: Ghost SID {sid!r} — dropped")
            return
        _session_state[sid]['_briefing'] = briefing_text or ''
```

CLAUDE.md: Dict-Key 'anonymisierer' — ASCII (kein Umlaut im Identifier).
ASCII-Key-Konvention korrekt: 'anonymisierer' (7 Zeichen, kein Umlaut trotz Aehlichkeit zu 'Anonymisierer')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnrufAnonymisierer-Lifecycle in live_session.py verdrahten</name>
  <files>services/live_session.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\services\live_session.py (vollstaendig lesen — insbesondere Z.268-374 fuer init_session_state + pop_session_state, Z.249-259 fuer set_briefing_for_sid Pattern)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Pre-Insert-Control-Flow-Audit: 30 Zeilen Kontext vor+nach, kein Refactor ausserhalb Scope)
  </read_first>

  <action>
**Pre-Insert-Control-Flow-Audit (PFLICHT laut CLAUDE.md):**
Vor jeder Aenderung: 30 Zeilen Kontext vor+nach dem Insert-Punkt lesen, Funktions-Skelett verstehen, Edge-Cases + Race-Conditions identifizieren.

**Drei Aenderungen in live_session.py:**

**Aenderung 1: 'anonymisierer' als neuer Key in init_session_state()**

In der `_session_state[sid] = { ... }`-Dict-Zuweisung innerhalb von `init_session_state()`, fuege am Ende der Key-Value-Liste (nach `'_line_id_counter': 0`) hinzu:

```python
            'anonymisierer':         None,    # D-06: AnrufAnonymisierer, erstellt via init_anonymisierer()
```

Exakte Position: nach dem letzten bestehenden Key `'_line_id_counter': 0`, vor der schliessenden `}`.

**Aenderung 2: init_anonymisierer() als neue Funktion**

Fuege NACH der `get_briefing_for_sid()`-Funktion (ca. Z.265) eine neue Funktion ein. Pattern analog zu `set_briefing_for_sid()`:

```python
def init_anonymisierer(sid: str) -> None:
    """Erstellt AnrufAnonymisierer fuer SID und legt ihn in _session_state[sid]['anonymisierer'].
    Muss NACH init_session_state() aufgerufen werden.
    Ghost-SID Guard: kein Fehler wenn SID nicht mehr existiert (Race-Condition Pitfall 3).
    """
    from services.anonymization import AnrufAnonymisierer
    with _session_state_lock:
        if sid not in _session_state:
            print(f'[ANON] init_anonymisierer: Ghost SID {sid!r} — skipped')
            return
        _session_state[sid]['anonymisierer'] = AnrufAnonymisierer()
        print(f'[ANON] AnrufAnonymisierer erstellt fuer sid={sid!r}')


def get_anonymisierer(sid: str):
    """Gibt AnrufAnonymisierer fuer SID zurueck, oder None wenn nicht initialisiert/SID unbekannt.
    Thread-safe. Gibt None bei Ghost-SID (Race-Condition Pitfall 3 — Caller handelt None defensiv).
    """
    with _session_state_lock:
        return _session_state.get(sid, {}).get('anonymisierer')
```

**Aenderung 3: KEINE weiteren Aenderungen**

pop_session_state() loescht `_session_state[sid]` bereits vollstaendig via `.pop(sid, None)` — der `'anonymisierer'`-Key wird automatisch mitgeloescht. Kein separater Cleanup-Step noetig (Pattern aus RESEARCH.md Pattern 5 verifiziert).

CLAUDE.md-Konformitaet:
- ASCII-Key: 'anonymisierer' (kein Umlaut)
- Kein Refactor ausserhalb Scope (CLAUDE.md Punkt 17)
- Lazy-Import `from services.anonymization import AnrufAnonymisierer` um Circular-Import zu vermeiden
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "from services.live_session import init_session_state, pop_session_state, init_anonymisierer, get_anonymisierer; print('Import OK'); init_session_state('test_sid_01', 1, 1); init_anonymisierer('test_sid_01'); anon = get_anonymisierer('test_sid_01'); assert anon is not None, 'get_anonymisierer returned None'; from services.anonymization import AnrufAnonymisierer; assert isinstance(anon, AnrufAnonymisierer), f'Typ: {type(anon)}'; pop_session_state('test_sid_01'); assert get_anonymisierer('test_sid_01') is None, 'Nach pop noch vorhanden'; print('Lifecycle OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `from services.live_session import init_anonymisierer, get_anonymisierer` schlaegt nicht fehl
    - `init_session_state('sid', 1, 1)` + `get_anonymisierer('sid')` gibt None zurueck (noch nicht init_anonymisierer aufgerufen)
    - `init_anonymisierer('sid')` + `get_anonymisierer('sid')` gibt AnrufAnonymisierer-Instanz zurueck
    - `pop_session_state('sid')` + `get_anonymisierer('sid')` gibt None zurueck (automatisch geloescht)
    - `init_anonymisierer('ghost_sid_nicht_existent')` raised KEINE Exception (Ghost-SID Guard)
    - `grep -rn "AnrufAnonymisierer\|anonymisierer" C:/Users/andre/dev/salesnerve/database/` gibt 0 Treffer (Cache nie in DB)
    - Kein anderer Code in live_session.py wurde geaendert (nur 3 definierte Aenderungen)
  </acceptance_criteria>

  <done>init_anonymisierer() und get_anonymisierer() in live_session.py exportierbar; Lifecycle-Test (init -> get -> pop -> get=None) gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SocketIO-Thread -> _session_state_lock | Multiple Threads greifen auf _session_state zu — Lock bereits vorhanden |
| init_anonymisierer() Ghost-SID | Race-Condition: SID kann zwischen get_anonymisierer() und init_anonymisierer() geloescht werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.B-L-01 | Tampering | anonymisierer-Key in _session_state nach pop | mitigate | pop_session_state() loescht gesamtes Dict — 'anonymisierer' automatisch mitgeloescht |
| T-08.23.2.B-L-02 | Information Disclosure | Ghost-SID in init_anonymisierer() | mitigate | if sid not in _session_state: return (Pitfall 3 Guard) |
| T-08.23.2.B-L-03 | Information Disclosure | Circular-Import services/anonymization | mitigate | Lazy-Import innerhalb init_anonymisierer() verhindert Import-Loop |
</threat_model>

<verification>
Nach Task:

1. Import-Test: `python -c "from services.live_session import init_anonymisierer, get_anonymisierer"` — kein Fehler
2. Lifecycle-Test: init_session_state -> init_anonymisierer -> get_anonymisierer (AnrufAnonymisierer) -> pop_session_state -> get_anonymisierer (None)
3. Ghost-SID-Test: init_anonymisierer('nonexistent_sid') raised keine Exception
4. DB-Grep: `grep -rn "AnrufAnonymisierer" database/` — 0 Treffer
</verification>

<success_criteria>
- live_session.py exportiert init_anonymisierer() und get_anonymisierer()
- Lifecycle gruen: init -> get -> pop -> get=None
- Ghost-SID-Guard aktiv
- Kein DB-Bezug fuer AnrufAnonymisierer
</success_criteria>

<output>
Nach Abschluss Summary erstellen: `.planning/phases/08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen/08.23.2.B-04-SUMMARY.md`

Summary muss enthalten:
- Exakte Zeilennummern der 3 Aenderungen in live_session.py
- Lifecycle-Test-Ergebnis
- Ghost-SID-Verhalten dokumentiert
</output>
