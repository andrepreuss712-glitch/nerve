---
phase: 08.23.2.B
plan: "05"
type: execute
wave: 2
depends_on:
  - "08.23.2.B-01"
  - "08.23.2.B-04"
files_modified:
  - services/deepgram_service.py
autonomous: true
requirements:
  - Req-7
  - Req-9

must_haves:
  truths:
    - "conversation_log.append() bei STT-Final laeuft durch anonymize() (INPUT-PFAD)"
    - "Art-9-Snippet wird bei conversation_log.append() uebersprungen ([ART9_REDACTED] nicht persistiert)"
    - "EWB-Antwort-Text (_antwort) laeuft durch anonymize_output() vor record_ewb_click() (OUTPUT-PFAD)"
    - "AnonymizationPipelineUnavailable-Exception verhindert Snippet-Persistenz (Kat. A)"
  artifacts:
    - path: "services/deepgram_service.py"
      provides: "Verdrahtung an Z.78 (INPUT) und Z.568 (OUTPUT)"
      contains: "anonymize"
  key_links:
    - from: "deepgram_service.py Z.77-82"
      to: "anonymize(text, cache)"
      via: "ls.get_anonymisierer(sid) holt Cache; anonymize() vor conversation_log.append()"
    - from: "deepgram_service.py Z.568 _run()-Funktion"
      to: "anonymize_output(_antwort, cache)"
      via: "anonymize_output() vor record_ewb_click(antwort_text=anon)"
---

<objective>
Verdrahtet `anonymize()` am STT-Final-Callback (Z.78) und `anonymize_output()` am EWB-Click-Handler (Z.568) in `services/deepgram_service.py`.

Purpose: Das sind die zwei INPUT- und OUTPUT-Verdrahtungs-Punkte in deepgram_service.py. Ohne diese Verdrahtung landet roher Deepgram-Transkript-Text und Claude-EWB-Antworten ungefiltert in conversation_log und ObjectionEvent.

Output: deepgram_service.py mit zwei minimalen Eingriffsstellen — nur Anonymisierungs-Verdrahtung, kein sonstiger Refactor (CLAUDE.md Punkt 17).
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\CLAUDE.md

<interfaces>
<!-- Bestehender Code deepgram_service.py Z.77-83 (INPUT-PFAD — verifiziert) -->
```python
                with ls.log_lock:
                    ls.conversation_log.append({
                        'ts': ts, 'type': 'transcript',
                        'speaker': log_sp if roles_confirmed else None,
                        'text': text, 'data': None,
                    })
```

<!-- Bestehender Code deepgram_service.py Z.565-579 (OUTPUT-PFAD — verifiziert) -->
```python
        def _run():
            try:
                result = streame_manual_ewb_variante(typ, profile_einwand or {}, kontext, _sid, slot=1)
                _antwort = (result.get('gegenargument_1') or '').strip() or None
                try:
                    ls.record_ewb_click(typ, success=True,
                                        antwort_text=_antwort, einwand_text=typ)
                except Exception as e:
                    print(f"[PiP] record_ewb_click error (sid={_sid}): {e}")
            except Exception as ex:
                # ...
```

<!-- live_session.py neue Funktionen (aus Plan 04) -->
```python
def get_anonymisierer(sid: str):
    """Gibt AnrufAnonymisierer fuer SID zurueck, oder None wenn nicht initialisiert."""
    with _session_state_lock:
        return _session_state.get(sid, {}).get('anonymisierer')
```

<!-- anonymization.py Interface (aus Plan 01) -->
```python
def anonymize(text: str, cache: Optional[AnrufAnonymisierer]) -> Tuple[str, str]:
    # Returns ('[ART9_REDACTED]', 'C') bei Art-9-Treffer
    # Returns ('[ANON_FEHLER]', 'C') bei Exception
    # Raises AnonymizationPipelineUnavailable bei is_pipeline_healthy=False
    ...

def anonymize_output(text: str, cache: Optional[AnrufAnonymisierer]) -> str:
    # Cache-Reverse-Lookup: ersetzt bekannte Namen aus cache.mapping
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: INPUT-PFAD — anonymize() vor conversation_log.append() in deepgram_service.py (Z.78)</name>
  <files>services/deepgram_service.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\services\deepgram_service.py (Z.47-110: 30 Zeilen Kontext vor+nach Z.78 lesen — Pre-Insert-Control-Flow-Audit per CLAUDE.md)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Pre-Insert-Control-Flow-Audit, kein Refactor ausserhalb Scope)
  </read_first>

  <action>
**Pre-Insert-Control-Flow-Audit:**
1. Lese Z.47-110 vollstaendig (30 Zeilen Kontext um Z.78)
2. Verstehe: Der Block mit `ls.conversation_log.append()` laeuft innerhalb eines `if result.is_final:` Branch, hat bereits `with ls.log_lock:` Lock
3. Race-Condition-Audit: `get_anonymisierer(sid)` kann None zurueckgeben wenn SID zwischen STT-Callback und dem Append-Aufruf disconnectet ist (Pitfall 3 aus RESEARCH.md) — anonymize() handelt None-Cache defensiv (Regex-Only-Modus)

**Ersetze den bestehenden Block (exakter Match):**

VORHER (Z.77-82):
```python
                with ls.log_lock:
                    ls.conversation_log.append({
                        'ts': ts, 'type': 'transcript',
                        'speaker': log_sp if roles_confirmed else None,
                        'text': text, 'data': None,
                    })
```

NACHHER:
```python
                # Phase 08.23.2.B: INPUT-PFAD Anonymisierung (D-01, Req-7)
                # Pre-Insert-Audit: laeuft unter ls.log_lock; Race-Condition (Ghost-SID) via
                # get_anonymisierer()-None-Return abgefangen; anonymize() handelt None-Cache defensiv.
                try:
                    from services.anonymization import anonymize, AnonymizationPipelineUnavailable
                    _anon_cache = ls.get_anonymisierer(sid)
                    _anon_result = anonymize(text, _anon_cache)
                    _anon_text, _anon_tier = _anon_result
                    if _anon_text == '[ART9_REDACTED]':
                        # Art-9-Treffer: Snippet nicht persistieren (D-05)
                        print(f'[ANON] Art-9 erkannt, Transcript-Snippet verworfen (sid={sid!r}, len={len(text)})')
                    else:
                        with ls.log_lock:
                            ls.conversation_log.append({
                                'ts': ts, 'type': 'transcript',
                                'speaker': log_sp if roles_confirmed else None,
                                'text': _anon_text, 'data': None,
                            })
                except AnonymizationPipelineUnavailable:
                    # D-08 Kat. A: Pipeline unavailable — kein Insert, Live-Call laeuft weiter
                    print(f'[ANON] Pipeline unavailable, Transcript-Snippet verworfen (sid={sid!r})')
                except Exception as _anon_err:
                    # Unerwarteter Fehler — Safety: lieber nicht persistieren
                    print(f'[ANON] Unerwarteter Fehler im INPUT-PFAD (sid={sid!r}): {type(_anon_err).__name__}')
```

WICHTIG:
- Einrueckung exakt anpassen an den umgebenden Code (4 Spaces pro Level)
- Kein anderer Code in dieser Funktion veraendern
- Nur diesen einen Block ersetzen
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('services/deepgram_service.py').read()); print('Syntax OK')" && grep -n "AnonymizationPipelineUnavailable" services/deepgram_service.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('services/deepgram_service.py').read())"` — kein SyntaxError
    - `grep -n "anonymize(" services/deepgram_service.py` — gibt mindestens einen Treffer (INPUT-PFAD)
    - `grep -n "ART9_REDACTED" services/deepgram_service.py` — gibt mindestens einen Treffer
    - `grep -n "AnonymizationPipelineUnavailable" services/deepgram_service.py` — gibt Treffer
    - Kein anderer Code in deepgram_service.py ausserhalb der definierten Stellen wurde geaendert
    - `grep -n "conversation_log.append" services/deepgram_service.py` — Treffer nur noch im NACHHER-Block (nicht mehr im VORHER-Block)
  </acceptance_criteria>

  <done>INPUT-PFAD Z.78 verdrahtet: anonymize() vor conversation_log.append(); Art-9-Skip und Pipeline-Unavailable-Handler vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: OUTPUT-PFAD — anonymize_output() vor record_ewb_click() in deepgram_service.py (Z.568-570)</name>
  <files>services/deepgram_service.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\services\deepgram_service.py (Z.538-594: 30 Zeilen Kontext um Z.568-579 lesen — Pre-Insert-Audit)
  </read_first>

  <action>
**Pre-Insert-Control-Flow-Audit:**
1. Lese Z.538-594 vollstaendig (30 Zeilen Kontext um Z.568)
2. Verstehe: `_run()` ist eine Closure innerhalb von `handle_manual_ewb()`. `_sid` ist der SID aus dem outer scope. `_antwort` ist der Claude-EWB-Antwort-Text.
3. Einwand-Text (`einwand_text=typ`) ist ein Typ-Label ('zu_teuer', 'keine_zeit') — KEIN Freitext, KEIN Anonymisierungs-Bedarf (D-01 "NICHT durch Pipeline").

**Ersetze innerhalb der `_run()` Funktion:**

VORHER (Z.568-572):
```python
                _antwort = (result.get('gegenargument_1') or '').strip() or None
                try:
                    ls.record_ewb_click(typ, success=True,
                                        antwort_text=_antwort, einwand_text=typ)
```

NACHHER:
```python
                _antwort = (result.get('gegenargument_1') or '').strip() or None
                # Phase 08.23.2.B: OUTPUT-PFAD Anonymisierung (D-01, Req-9)
                # anonymize_output() nutzt Cache-Reverse-Lookup (bekannte Namen aus Briefing echoen)
                # einwand_text=typ ist ein Typ-Label ('zu_teuer') — kein Anonymisierungs-Bedarf (D-01)
                if _antwort:
                    try:
                        from services.anonymization import anonymize_output
                        _anon_cache = ls.get_anonymisierer(_sid)
                        _antwort = anonymize_output(_antwort, _anon_cache)
                    except Exception as _out_err:
                        print(f'[ANON] anonymize_output Fehler (EWB, sid={_sid!r}): {type(_out_err).__name__}')
                        # Fallback: _antwort bleibt unveraendert (fail-open fuer Output-Pfad)
                try:
                    ls.record_ewb_click(typ, success=True,
                                        antwort_text=_antwort, einwand_text=typ)
```

WICHTIG:
- Nur diese spezifische Stelle ersetzen
- Einrueckung exakt beibehalten (innerhalb `_run()` Closure)
- `einwand_text=typ` bleibt unveraendert (Typ-Label, kein Freitext)
- Kein anderer Code in _run() oder handle_manual_ewb() aendern
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('services/deepgram_service.py').read()); print('Syntax OK')" && grep -n "anonymize_output" services/deepgram_service.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('services/deepgram_service.py').read())"` — kein SyntaxError
    - `grep -n "anonymize_output" services/deepgram_service.py` — gibt mindestens einen Treffer (OUTPUT-PFAD EWB)
    - `grep -n "record_ewb_click" services/deepgram_service.py` — `record_ewb_click` kommt nach `anonymize_output` (nicht davor)
    - `grep -n "einwand_text=typ" services/deepgram_service.py` — typ-Label unveraendert (kein Anonymisierungs-Wrap)
    - Kein anderer Code in deepgram_service.py geaendert
  </acceptance_criteria>

  <done>OUTPUT-PFAD Z.568 verdrahtet: anonymize_output(_antwort, cache) vor record_ewb_click(); einwand_text bleibt unveraendert (Typ-Label).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deepgram-STT-Callback -> conversation_log | Ungefilterter Transkript-Text (PERSON, IBAN, etc.) |
| Claude-EWB-Output -> record_ewb_click | KI-Text kann Briefing-Namen echoen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.B-DG-01 | Information Disclosure | STT-Text ohne Anonymisierung in conversation_log | mitigate | anonymize() vor append(); Art-9-Check gibt '[ART9_REDACTED]' -> skip |
| T-08.23.2.B-DG-02 | Information Disclosure | Ghost-SID Race-Condition in STT-Callback | mitigate | get_anonymisierer() gibt None zurueck; anonymize(text, None) laeuft in Regex-Only-Modus |
| T-08.23.2.B-DG-03 | Information Disclosure | EWB-Antwort mit Briefing-Namen in ObjectionEvent | mitigate | anonymize_output(_antwort, cache) ersetzt bekannte Namen via Cache-Reverse-Lookup |
| T-08.23.2.B-DG-04 | Tampering | einwand_text (Typ-Label) faelschlicherweise anonymisiert | mitigate | einwand_text=typ bleibt unveraendert; nur _antwort durch anonymize_output() |
</threat_model>

<verification>
Nach beiden Tasks:

1. Syntax-Check: `python -c "import ast; ast.parse(open('services/deepgram_service.py').read())"` — kein SyntaxError
2. INPUT-Pfad: `grep -n "anonymize(" services/deepgram_service.py` — Treffer um Z.78
3. OUTPUT-Pfad: `grep -n "anonymize_output" services/deepgram_service.py` — Treffer in _run()
4. Art-9-Skip: `grep -n "ART9_REDACTED" services/deepgram_service.py` — Treffer
5. Pipeline-Unavailable: `grep -n "AnonymizationPipelineUnavailable" services/deepgram_service.py` — Treffer
</verification>

<success_criteria>
- deepgram_service.py syntaktisch korrekt nach beiden Aenderungen
- INPUT-PFAD Z.78: anonymize() vor conversation_log.append()
- OUTPUT-PFAD Z.568: anonymize_output() vor record_ewb_click()
- Art-9-Snippet wird uebersprungen (nicht persistiert)
- einwand_text=typ unveraendert (Typ-Label, kein Freitext)
</success_criteria>

<output>
Nach Abschluss Summary erstellen: `.planning/phases/08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen/08.23.2.B-05-SUMMARY.md`

Summary muss enthalten:
- Exakte Zeilennummern der beiden Aenderungen (nach Edit)
- Pre-Insert-Control-Flow-Audit Ergebnis (Race-Conditions gefunden?)
- Syntax-Check-Ergebnis
</output>
