---
phase: 08.23.2.B
plan: "06"
type: execute
wave: 2
depends_on:
  - "08.23.2.B-01"
  - "08.23.2.B-03"
  - "08.23.2.B-04"
files_modified:
  - services/claude_service.py
autonomous: true
requirements:
  - Req-8

must_haves:
  truths:
    - "gegenargument_log.append() laeuft durch anonymize_output() fuer einwand_zitat (OUTPUT-PFAD)"
    - "painpoints.append() laeuft durch anonymize_output() fuer painpoint-Text (OUTPUT-PFAD)"
    - "Klartext-Namen aus Token-Cache landen nicht in gegenargument_log oder painpoints"
    - "conversation_log.append(type=painpoint) laeuft durch anonymize_output() (OUTPUT-PFAD — painpoint IS Claude-Paraphrase von STT-Content)"
  artifacts:
    - path: "services/claude_service.py"
      provides: "OUTPUT-PFAD Verdrahtung an Z.892 (gegenargument_log) und Z.1432 (painpoints)"
      contains: "anonymize_output"
  key_links:
    - from: "claude_service.py Z.892"
      to: "anonymize_output(einwand_zitat, cache)"
      via: "ls.gegenargument_log.append() erhaelt anonymisierten einwand_zitat"
    - from: "claude_service.py Z.1432"
      to: "anonymize_output(painpoint, cache)"
      via: "ls.painpoints.append() erhaelt anonymisierten painpoint-Text"
---

<objective>
Verdrahtet `anonymize_output()` an beiden Claude-Output-Stellen in `services/claude_service.py`: gegenargument_log.append() (Z.892) und painpoints.append() (Z.1432).

Purpose: Claude-generierte Texte koennen Briefing-Namen echoen (z.B. "Herr Jacob, ich verstehe Ihre Bedenken"). anonymize_output() ersetzt bekannte Namen via Cache-Reverse-Lookup bevor sie in die In-Memory-Logs gehen (die spaeter in DB persistiert werden).

Output: claude_service.py mit zwei minimalen Eingriffsstellen — kein sonstiger Refactor (CLAUDE.md Punkt 17).

**Finding 4 Hinweis fuer diesen Plan:**
Beide Verdrahtungs-Punkte in Plan 06 verwenden ausschliesslich `anonymize_output()` (OUTPUT-PFAD).
`anonymize_output()` gibt den Text zurueck (nie Sentinel-Werte wie '[ART9_REDACTED]' oder '[ANON_FEHLER]') —
es ist ein Cache-Reverse-Lookup ohne Art-9-Filter. Daher ist der Skip-Check (Finding 4) hier nicht
erforderlich. Der Finding-4-Skip-Check gilt nur fuer `anonymize()` Aufrufe (INPUT-PFAD, Plans 05 und 07).
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\CLAUDE.md

<interfaces>
<!-- Bestehender Code claude_service.py Z.882-904 (gegenargument_log — verifiziert) -->
```python
                if ergebnis.get('einwand'):
                    with ls.gegenargument_log_lock:
                        # Vorherigen Eintrag mit kb_nachher aktualisieren
                        if ls.gegenargument_log:
                            last = ls.gegenargument_log[-1]
                            if last['kb_nachher'] is None:
                                last['kb_nachher'] = kb_vor_einwand
                                last['kb_delta']   = kb_vor_einwand - last['kb_vorher']
                                last['erfolgreich'] = last['kb_delta'] > 0
                        # Neuen Eintrag anlegen
                        ls.gegenargument_log.append({
                            'ts':               ts,
                            'einwand_typ':      ergebnis.get('typ', ''),
                            'einwand_zitat':    ergebnis.get('einwand_zitat', ''),
                            'ist_vorwand':      ergebnis.get('ist_vorwand', False),
                            'gegenargument_1':  ergebnis.get('gegenargument_1', ''),
                            'gegenargument_2':  ergebnis.get('gegenargument_2', ''),
                            'gewaehlte_option': None,
                            'kb_vorher':        kb_aktuell,
                            'kb_nachher':       None,
                            'kb_delta':         None,
                            'erfolgreich':      None,
                        })
```

<!-- Bestehender Code claude_service.py Z.1426-1432 (painpoints — verifiziert) -->
```python
                if painpoint:
                    with ls.painpoints_lock:
                        if ls.ist_painpoint_duplikat(painpoint, ls.painpoints):
                            print(f"[Claude-2] Painpoint Duplikat: {painpoint!r}")
                            painpoint = None
                        else:
                            ls.painpoints.append({'ts': ts, 'text': painpoint})
```

<!-- anonymize_output() Interface (aus Plan 01) -->
```python
def anonymize_output(text: str, cache: Optional[AnrufAnonymisierer]) -> str:
    """Cache-Reverse-Lookup. Gibt unveraenderten text zurueck wenn cache=None.
    Gibt NIE Sentinel-Werte wie '[ART9_REDACTED]' oder '[ANON_FEHLER]' zurueck.
    Finding 4: Kein Skip-Check noetig bei anonymize_output()-Aufrufen."""
    ...

def should_persist(anon_text: str) -> bool:
    """Gibt False fuer '[ART9_REDACTED]' und '[ANON_FEHLER]'.
    Nicht benoetigt in Plan 06 — anonymize_output() gibt keine Sentinel-Werte zurueck."""
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OUTPUT-PFAD — anonymize_output() fuer gegenargument_log in claude_service.py (Z.892)</name>
  <files>services/claude_service.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Z.862-920: 30 Zeilen Kontext um Z.892 lesen — Pre-Insert-Control-Flow-Audit per CLAUDE.md)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Pre-Insert-Control-Flow-Audit, kein Refactor ausserhalb Scope)
  </read_first>

  <action>
**Pre-Insert-Control-Flow-Audit:**
1. Lese Z.862-920 vollstaendig (30 Zeilen Kontext um Z.892)
2. Verstehe: Der Block laeuft innerhalb `with ls.gegenargument_log_lock:` Lock
3. Mehrere Felder im append-Dict: nur Freitext-Felder durch anonymize_output() — einwand_typ ist ein Typ-Label (kein Freitext)
4. Race-Condition: `get_anonymisierer(sid)` kann None sein — anonymize_output() handelt None-Cache defensiv (gibt Text unveraendert zurueck)

**Finding 4 Begruendung warum kein Skip-Check noetig:**
`anonymize_output()` ist ein Cache-Reverse-Lookup — ersetzt bekannte Klartext-Namen durch Tokens.
Es gibt niemals Sentinel-Werte wie '[ART9_REDACTED]' zurueck. Der Skip-Check (Finding 4) gilt
NUR fuer `anonymize()` (INPUT-PFAD). Hier ist kein Skip-Check noetig.

**Aenderung:** Im gegenargument_log.append()-Block, ersetze die Werte fuer `einwand_zitat`, `gegenargument_1`, `gegenargument_2` durch anonymize_output()-Aufrufe.

Fuege VOR dem `with ls.gegenargument_log_lock:` Block (aber nach der `if ergebnis.get('einwand'):` Zeile) ein:

```python
                if ergebnis.get('einwand'):
                    # Phase 08.23.2.B: OUTPUT-PFAD Anonymisierung (D-01, Req-8)
                    # anonymize_output() ersetzt bekannte Namen (Briefing) via Cache-Reverse-Lookup
                    # Finding 4: anonymize_output() gibt keine Sentinel-Werte zurueck — kein Skip-Check noetig.
                    try:
                        from services.anonymization import anonymize_output
                        _anon_cache = ls.get_anonymisierer(sid)
                        _einwand_zitat_anon = anonymize_output(
                            ergebnis.get('einwand_zitat', ''), _anon_cache
                        )
                        _gegenarg_1_anon = anonymize_output(
                            ergebnis.get('gegenargument_1', ''), _anon_cache
                        )
                        _gegenarg_2_anon = anonymize_output(
                            ergebnis.get('gegenargument_2', ''), _anon_cache
                        )
                    except Exception as _anon_err:
                        print(f'[ANON] anonymize_output Fehler (gegenargument_log, sid={sid!r}): {type(_anon_err).__name__}')
                        # Fallback: originale Werte verwenden (fail-open fuer Output-Pfad)
                        _einwand_zitat_anon = ergebnis.get('einwand_zitat', '')
                        _gegenarg_1_anon = ergebnis.get('gegenargument_1', '')
                        _gegenarg_2_anon = ergebnis.get('gegenargument_2', '')
                    with ls.gegenargument_log_lock:
                        # Vorherigen Eintrag mit kb_nachher aktualisieren
                        if ls.gegenargument_log:
                            last = ls.gegenargument_log[-1]
                            if last['kb_nachher'] is None:
                                last['kb_nachher'] = kb_vor_einwand
                                last['kb_delta']   = kb_vor_einwand - last['kb_vorher']
                                last['erfolgreich'] = last['kb_delta'] > 0
                        # Neuen Eintrag anlegen (Freitext-Felder anonymisiert)
                        ls.gegenargument_log.append({
                            'ts':               ts,
                            'einwand_typ':      ergebnis.get('typ', ''),      # Typ-Label, kein Freitext
                            'einwand_zitat':    _einwand_zitat_anon,          # anonymisiert
                            'ist_vorwand':      ergebnis.get('ist_vorwand', False),
                            'gegenargument_1':  _gegenarg_1_anon,             # anonymisiert
                            'gegenargument_2':  _gegenarg_2_anon,             # anonymisiert
                            'gewaehlte_option': None,
                            'kb_vorher':        kb_aktuell,
                            'kb_nachher':       None,
                            'kb_delta':         None,
                            'erfolgreich':      None,
                        })
```

WICHTIG:
- `einwand_typ` bleibt unveraendert (Typ-Label 'zu_teuer', 'keine_zeit' — kein Freitext)
- Einrueckung exakt beibehalten
- Der `with ls.gegenargument_log_lock:` Lock bleibt — die anonymize_output()-Aufrufe laufen AUSSERHALB des Locks (kein Lock-Nesting-Problem)
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('services/claude_service.py').read()); print('Syntax OK')" && grep -n "anonymize_output" services/claude_service.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('services/claude_service.py').read())"` — kein SyntaxError
    - `grep -n "anonymize_output" services/claude_service.py` — gibt mindestens einen Treffer fuer gegenargument_log
    - `grep -n "_einwand_zitat_anon" services/claude_service.py` — Treffer (anonymisierte Variable)
    - `grep -n "einwand_typ.*ergebnis.get" services/claude_service.py` — unveraendertes Typ-Label (kein Anonymisierungs-Wrap)
    - **Finding 4:** `grep -n "ART9_REDACTED\|ANON_FEHLER" services/claude_service.py` — 0 Treffer ist akzeptabel (anonymize_output() gibt keine Sentinel-Werte zurueck, kein Skip-Check noetig); falls Treffer: nur in Kommentaren, nicht als Bedingung
    - Kein anderer Code in claude_service.py geaendert
  </acceptance_criteria>

  <done>gegenargument_log.append() Z.892 erhaelt anonymisierte einwand_zitat + gegenargument_1/2 via anonymize_output().</done>
</task>

<task type="auto">
  <name>Task 2: OUTPUT-PFAD — anonymize_output() fuer painpoints in claude_service.py (Z.1432)</name>
  <files>services/claude_service.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Z.1400-1445: 30 Zeilen Kontext um Z.1432 lesen — Pre-Insert-Audit)
  </read_first>

  <action>
**Pre-Insert-Control-Flow-Audit:**
1. Lese Z.1400-1445 vollstaendig
2. Verstehe: `ls.ist_painpoint_duplikat()` prueft vor dem Append — diese Pruefung laeuft auf dem originalen `painpoint`-Text (vor Anonymisierung)
3. Wichtig: Duplikat-Pruefung auf Original-Text ist korrekt (verhindert dass Duplikate durch verschiedene Token-Zuweisung unterschiedlich erscheinen). Anonymisierung nur fuer gespeicherten Text.

**Finding 4 Begruendung warum kein Skip-Check noetig:**
`anonymize_output()` gibt den Text unveraendert zurueck wenn kein Mapping-Treffer — nie
'[ART9_REDACTED]'. Der painpoint-Text wurde bereits als INPUT durch anonymize() gefiltert
bevor er in conversation_log landete. Der OUTPUT-PFAD braucht hier nur Cache-Reverse-Lookup.

**Ersetze innerhalb des `if painpoint:` Blocks:**

VORHER (Z.1426-1437):
```python
                if painpoint:
                    with ls.painpoints_lock:
                        if ls.ist_painpoint_duplikat(painpoint, ls.painpoints):
                            print(f"[Claude-2] Painpoint Duplikat: {painpoint!r}")
                            painpoint = None
                        else:
                            ls.painpoints.append({'ts': ts, 'text': painpoint})
                    if painpoint:
                        with ls.log_lock:
                            ls.conversation_log.append({
                                'ts': ts, 'type': 'painpoint', 'text': painpoint,
                            })
```

NACHHER:
```python
                if painpoint:
                    with ls.painpoints_lock:
                        if ls.ist_painpoint_duplikat(painpoint, ls.painpoints):
                            print(f"[Claude-2] Painpoint Duplikat: {painpoint!r}")
                            painpoint = None
                        else:
                            # Phase 08.23.2.B: OUTPUT-PFAD Anonymisierung (D-01, Req-8)
                            # Duplikat-Check auf Original-Text (korrekt — vor Anonymisierung)
                            # painpoint IS Claude-Paraphrase von STT-Content und kann Namen enthalten
                            # (D-01 OUTPUT-PFAD: anonymize_output() Pflicht, DSGVO-Blocker-Fix)
                            # Finding 4: anonymize_output() gibt keine Sentinel-Werte zurueck — kein Skip-Check noetig.
                            try:
                                from services.anonymization import anonymize_output
                                _anon_cache = ls.get_anonymisierer(sid)
                                _painpoint_anon = anonymize_output(painpoint, _anon_cache)
                            except Exception as _anon_err:
                                print(f'[ANON] anonymize_output Fehler (painpoint, sid={sid!r}): {type(_anon_err).__name__}')
                                _painpoint_anon = painpoint  # Fallback: Original-Text
                            ls.painpoints.append({'ts': ts, 'text': _painpoint_anon})
                    if painpoint:
                        # Phase 08.23.2.B: conversation_log[type=painpoint] ebenfalls anonymisieren
                        # painpoint-Text ist Claude-Paraphrase von STT-Content (OUTPUT-PFAD per D-01)
                        # und kann Briefing-Namen enthalten — anonymize_output() Pflicht (DSGVO)
                        try:
                            from services.anonymization import anonymize_output
                            _anon_cache = ls.get_anonymisierer(sid)
                            _painpoint_log_anon = anonymize_output(painpoint, _anon_cache)
                        except Exception as _anon_err:
                            print(f'[ANON] anonymize_output Fehler (conv_log painpoint, sid={sid!r}): {type(_anon_err).__name__}')
                            _painpoint_log_anon = painpoint  # Fallback: Original-Text
                        with ls.log_lock:
                            ls.conversation_log.append({
                                'ts': ts, 'type': 'painpoint', 'text': _painpoint_log_anon,
                            })
```

WICHTIG:
- `conversation_log.append(type=painpoint)` erhaelt nun ebenfalls anonymisierten Text — painpoint-Text ist Claude-Paraphrase von STT-Content (OUTPUT-PFAD per D-01) und kann Briefing-Namen enthalten (DSGVO-Pflicht, Blocker-Fix)
- Duplikat-Check laeuft weiterhin auf Original-Text (vor Anonymisierung) — korrekt um Token-Variationen keine Duplikate zu verpassen
- Einrueckung exakt beibehalten
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('services/claude_service.py').read()); print('Syntax OK')" && grep -c "anonymize_output" services/claude_service.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('services/claude_service.py').read())"` — kein SyntaxError
    - `grep -c "anonymize_output" services/claude_service.py` — gibt 3 oder mehr (gegenargument_log + painpoints + conv_log painpoint)
    - `grep -n "_painpoint_anon" services/claude_service.py` — Treffer (anonymisierte Variable fuer ls.painpoints)
    - `grep -n "_painpoint_log_anon" services/claude_service.py` — Treffer (anonymisierte Variable fuer conversation_log)
    - `grep -n "ist_painpoint_duplikat" services/claude_service.py` — Duplikat-Check noch vorhanden (auf Original-Text)
    - `grep -n "conv_log.*painpoint\|painpoint.*conv_log\|_painpoint_log_anon" services/claude_service.py` — Treffer (conversation_log anonymisiert)
    - **Finding 4:** `grep -n "ART9_REDACTED\|ANON_FEHLER" services/claude_service.py` — 0 Treffer ist akzeptabel (anonymize_output() gibt keine Sentinel-Werte zurueck); falls Treffer nur in Kommentaren
    - Kein anderer Code in claude_service.py geaendert
  </acceptance_criteria>

  <done>painpoints.append() Z.1432 erhaelt anonymisierten Painpoint-Text via anonymize_output(); conversation_log.append(type=painpoint) ebenfalls anonymisiert (DSGVO-Pflicht: painpoint ist Claude-Paraphrase von STT-Content); Duplikat-Check laeuft weiterhin auf Original-Text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude-Response -> gegenargument_log | einwand_zitat (15-Wort-Direktzitat), gegenargument_1/2 koennen Briefing-Namen enthalten |
| Claude-Response -> painpoints | Painpoint-Paraphrasen aus Transkript-Kontext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.B-CS-01 | Information Disclosure | einwand_zitat mit Klartext-Namen in gegenargument_log | mitigate | anonymize_output(_einwand_zitat_anon, cache) vor append() |
| T-08.23.2.B-CS-02 | Information Disclosure | painpoint-Text mit Klartext-Namen in painpoints | mitigate | anonymize_output(_painpoint_anon, cache) vor append() |
| T-08.23.2.B-CS-03 | Tampering | einwand_typ faelschlicherweise anonymisiert | mitigate | einwand_typ bleibt unveraendert (Typ-Label, kein Freitext) |
| T-08.23.2.B-CS-04 | Tampering | Lock-Nesting in gegenargument_log_lock | mitigate | anonymize_output() laeuft AUSSERHALB gegenargument_log_lock (kein Lock-Nesting) |
| T-08.23.2.B-CS-05 | Information Disclosure | Sentinel-Wert in DB (Finding 4) | accept | anonymize_output() gibt keine Sentinel-Werte zurueck — kein Skip-Check noetig; Finding 4 gilt nur fuer anonymize() (INPUT-PFAD) |
</threat_model>

<verification>
Nach beiden Tasks:

1. Syntax-Check: `python -c "import ast; ast.parse(open('services/claude_service.py').read())"` — kein SyntaxError
2. Anzahl anonymize_output-Aufrufe: `grep -c "anonymize_output" services/claude_service.py` — mindestens 3
3. Duplikat-Check noch vorhanden: `grep -n "ist_painpoint_duplikat" services/claude_service.py` — Treffer
4. Typ-Label unveraendert: `grep -n "einwand_typ" services/claude_service.py` — kein anonymize_output() drum herum
</verification>

<success_criteria>
- claude_service.py syntaktisch korrekt nach beiden Aenderungen
- gegenargument_log: einwand_zitat + gegenargument_1/2 durch anonymize_output()
- painpoints: text durch anonymize_output()
- conversation_log[type=painpoint]: text durch anonymize_output() (painpoint ist OUTPUT-PFAD — Claude-Paraphrase von STT-Content)
- einwand_typ-Label unveraendert
- Duplikat-Check auf Original-Text (nicht auf anonymisierten Text)
- Finding 4 dokumentiert: anonymize_output() gibt keine Sentinel-Werte zurueck, kein Skip-Check noetig in diesem Plan
</success_criteria>

<output>
Nach Abschluss Summary erstellen: `.planning/phases/08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen/08.23.2.B-06-SUMMARY.md`

Summary muss enthalten:
- Exakte Zeilennummern der beiden Aenderungen (nach Edit)
- Pre-Insert-Audit Ergebnis (Lock-Nesting-Check, Race-Conditions)
- Syntax-Check-Ergebnis
- Finding 4 Begruendung dokumentiert: anonymize_output() gibt keine Sentinel-Werte — kein Skip-Check in Plan 06 noetig
</output>
