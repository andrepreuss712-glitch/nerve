---
phase: 08.23.2.B
plan: "07.1"
type: execute
wave: 2
depends_on:
  - "08.23.2.B-07"
files_modified:
  - services/anonymization.py
  - routes/app_routes.py
  - static/nerve.css
  - templates/dashboard.html
autonomous: true
requirements:
  - Req-1
  - Req-5

must_haves:
  truths:
    - "Dashboard zeigt roten Streifen wenn pipeline_status='unavailable' (D-08 Kat. A)"
    - "Dashboard zeigt gelben Streifen wenn pipeline_status='degraded' (D-08 Kat. C, >5 Snippet-Fehler in 10 Min)"
    - "Snippet-Fehler ([ANON_FEHLER]) werden als Timestamps gespeichert und in get_pipeline_status() ausgewertet"
    - "GET /api/health gibt pipeline_error_count_10min zurueck (erweiterter Response)"
    - "Bei /api/health-Ausfall: kein Banner gezeigt (silent fail)"
  artifacts:
    - path: "services/anonymization.py"
      provides: "_record_snippet_error() und get_pipeline_status() mit error_count_10min"
      contains: "_record_snippet_error"
    - path: "routes/app_routes.py"
      provides: "/api/health mit pipeline_error_count_10min Feld"
      contains: "pipeline_error_count_10min"
    - path: "static/nerve.css"
      provides: ".n-pipeline-error und .n-pipeline-warning Klassen via CSS-Vars"
      contains: "n-pipeline-error"
    - path: "templates/dashboard.html"
      provides: "Pipeline-Status-Streifen HTML + JS-Erweiterung des health-fetch"
      contains: "pipeline-error-strip"
  key_links:
    - from: "anonymization.py anonymize() Fehler-Pfad"
      to: "_record_snippet_error()"
      via: "Aufruf wenn return ('[ANON_FEHLER]', 'C')"
      pattern: "_record_snippet_error"
    - from: "routes/app_routes.py api_health()"
      to: "get_pipeline_status() dict['error_count_10min']"
      via: "pipeline_error_count_10min im jsonify-Response"
      pattern: "pipeline_error_count_10min"
    - from: "dashboard.html fetch('/api/health') Callback"
      to: "pipeline-error-strip / pipeline-warning-strip"
      via: "data.pipeline_status Auswertung im JS"
      pattern: "pipeline_status"
---

<objective>
Implementiert den Dashboard-Hinweis-Streifen fuer Anonymisierungs-Pipeline-Fehler (D-08 Kat. A + C).

Purpose:
- Wenn die NER-Pipeline down ist (Kat. A): Roter Streifen "Anruf-Aufzeichnung temporaer ausgesetzt"
- Wenn Snippet-Fehler-Rate > Schwellenwert (Kat. C): Gelber Streifen mit Fehleranzahl
- Betreiber (André) sieht sofort im Dashboard ob die Pipeline Probleme hat

Output:
1. `services/anonymization.py` — _record_snippet_error() + get_pipeline_status() gibt dict mit error_count_10min zurueck
2. `routes/app_routes.py` — /api/health erhaelt pipeline_error_count_10min Feld
3. `static/nerve.css` — .n-pipeline-error und .n-pipeline-warning CSS-Klassen via CSS-Vars
4. `templates/dashboard.html` — zwei neue Streifen-Divs + JS-Erweiterung im bestehenden health-fetch

Abhaengigkeit: Plan 07 hat get_pipeline_status() als String-Return definiert und _error_timestamps/_error_lock/ROLLING_ERROR_WINDOW_S/ROLLING_ERROR_THRESHOLD bereits als Modul-Level-Namen angelegt. Plan 07.1 erweitert get_pipeline_status() auf dict-Return und fuegt _record_snippet_error() hinzu.

**Finding 4 Hinweis fuer diesen Plan:**
Plan 07.1 enthaelt keine direkten `anonymize()` Aufrufe — nur `get_pipeline_status()` und
`_record_snippet_error()` Operationen. Der Finding-4-Skip-Check (Sentinel-Wert-Pruefung vor
DB-Write) gilt fuer `anonymize()` Aufrufe in Plans 05 und 07. Plan 07.1 ist nicht betroffen.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\CLAUDE.md

<interfaces>
<!-- Plan 07 Task 2 hat get_pipeline_status() als STRING-Return definiert (MUSS erweitert werden auf dict): -->
```python
# AKTUELLER Stand nach Plan 07 (string return):
def get_pipeline_status() -> str:
    global is_pipeline_healthy, _error_timestamps
    if not is_pipeline_healthy:
        return 'unavailable'
    import time
    now = time.monotonic()
    with _error_lock:
        cutoff = now - ROLLING_ERROR_WINDOW_S
        recent_errors = sum(1 for ts in _error_timestamps if ts >= cutoff)
    if recent_errors > ROLLING_ERROR_THRESHOLD:
        return 'degraded'
    return 'ok'
```

<!-- Plan 07 hat folgende Modul-Level-Namen angelegt (bereits vorhanden nach Plan 07): -->
```python
is_pipeline_healthy: bool          # Modul-Level-Flag
_error_timestamps: list            # Fuer Rolling-Error-Count (Kat. C)
_error_lock: threading.Lock        # Thread-Safety fuer _error_timestamps
ROLLING_ERROR_WINDOW_S: int/float  # Default: 600 (10 Minuten)
ROLLING_ERROR_THRESHOLD: int       # Default: 5
```

<!-- Plan 07 app_routes.py /api/health nach Plan 07 Task 2: -->
```python
    # Phase 08.23.2.B: Anonymisierungs-Pipeline-Status (D-08)
    try:
        from services.anonymization import get_pipeline_status
        pipeline_status = get_pipeline_status()
    except Exception:
        pipeline_status = 'unknown'

    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
        'pipeline_status': pipeline_status,   # D-08: 'ok' | 'degraded' | 'unavailable'
    })
```

<!-- Bestehende CSS-Tokens in nerve.css (relevant fuer Wiederverwendung): -->
```css
/* :root bereits definiert: */
--warning-bg:      #f59e0b;   /* Amber — fuer gelben Streifen (Kat. C) */
--warning-text:    #1a1a1a;
--warning-padding: 8px 16px;
--warning-size:    14px;
--warning-weight:  500;
--danger-red:      #EF4444;   /* Rot — fuer roten Streifen (Kat. A) */

/* .n-backup-warning (Pattern zum Spiegeln): */
.n-backup-warning{display:none;background:var(--warning-bg);color:var(--warning-text);padding:var(--warning-padding);text-align:center;font-size:var(--warning-size);font-weight:var(--warning-weight);position:relative;z-index:100;}
```

<!-- Bestehender health-fetch in dashboard.html (IIFE, einmaliger fetch, kein Polling): -->
```html
<div id="backup-warning-strip" class="n-backup-warning">
  &#9888; Postgres-Backup veraltet oder fehlgeschlagen &mdash;
  <span id="backup-warning-detail">bitte prüfen</span>
</div>
<script>
(function() {
  fetch('/api/health')
    .then(r => r.json())
    .then(data => {
      if (data.backup_status && data.backup_status !== 'ok') {
        var strip = document.getElementById('backup-warning-strip');
        var detail = document.getElementById('backup-warning-detail');
        if (strip) {
          if (data.backup_status === 'stale') {
            detail.textContent = 'Letztes Backup vor ' + (data.backup_age_hours || '?') + ' Stunden.';
          } else {
            detail.textContent = 'Kein Backup gefunden.';
          }
          strip.style.display = 'block';
        }
      }
    })
    .catch(function() {}); // silent fail — backup warning is non-critical
})();
</script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: anonymization.py — _record_snippet_error() hinzufuegen + get_pipeline_status() auf dict erweitern</name>
  <files>services/anonymization.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\services\anonymization.py (komplett lesen — Plan 07 hat die Datei erstellt; verstehe den aktuellen Stand von get_pipeline_status(), _error_timestamps, _error_lock, ROLLING_ERROR_WINDOW_S, ROLLING_ERROR_THRESHOLD, anonymize())
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-07-PLAN.md (Task 2 lesen — exakter Wortlaut der get_pipeline_status()-Implementierung nach Plan 07, um sicherzustellen dass Plan 07.1 nur erweitert, nicht dupliziert)
  </read_first>

  <action>
**Schritt 1 — Pre-Edit-Audit:**
1. Lese services/anonymization.py komplett
2. Stelle sicher dass get_pipeline_status() noch einen STRING zurueckgibt (Stand nach Plan 07)
3. Pruefe ob _record_snippet_error() bereits existiert — wenn ja, diesen Schritt ueberspringen und nur get_pipeline_status() anpassen

**Schritt 2 — _record_snippet_error() hinzufuegen:**

Fuege diese Funktion DIREKT VOR get_pipeline_status() ein:

```python
def _record_snippet_error() -> None:
    """Registriert einen Snippet-Fehler ([ANON_FEHLER]) fuer Rolling-Error-Rate (D-08 Kat. C).
    Thread-safe via _error_lock. Entfernt Eintraege ausserhalb des Zeitfensters.
    """
    import time
    now = time.monotonic()
    with _error_lock:
        _error_timestamps.append(now)
        # Prune: Eintraege aelter als ROLLING_ERROR_WINDOW_S entfernen
        cutoff = now - ROLLING_ERROR_WINDOW_S
        while _error_timestamps and _error_timestamps[0] < cutoff:
            _error_timestamps.pop(0)
```

**Schritt 3 — get_pipeline_status() auf dict-Return erweitern:**

VORHER (Stand nach Plan 07 — string return):
```python
def get_pipeline_status() -> str:
    """Gibt pipeline_status zurueck fuer /api/health Endpoint.
    ...
    """
    global is_pipeline_healthy, _error_timestamps
    if not is_pipeline_healthy:
        return 'unavailable'
    import time
    now = time.monotonic()
    with _error_lock:
        cutoff = now - ROLLING_ERROR_WINDOW_S
        recent_errors = sum(1 for ts in _error_timestamps if ts >= cutoff)
    if recent_errors > ROLLING_ERROR_THRESHOLD:
        return 'degraded'
    return 'ok'
```

NACHHER (dict return, abwaertskompatibel erweitert):
```python
def get_pipeline_status() -> dict:
    """Gibt pipeline_status und error_count_10min zurueck fuer /api/health Endpoint.
    'ok': Pipeline verfuegbar, keine erhoehlte Fehlerrate
    'degraded': Rolling-Error-Rate ueber Schwellenwert (D-08 Kat. C)
    'unavailable': spaCy nicht ladbar (D-08 Kat. A)
    """
    import time
    now = time.monotonic()
    with _error_lock:
        cutoff = now - ROLLING_ERROR_WINDOW_S
        recent_errors = sum(1 for ts in _error_timestamps if ts >= cutoff)
    if not is_pipeline_healthy:
        return {'status': 'unavailable', 'error_count_10min': recent_errors}
    if recent_errors > ROLLING_ERROR_THRESHOLD:
        return {'status': 'degraded', 'error_count_10min': recent_errors}
    return {'status': 'ok', 'error_count_10min': recent_errors}
```

**Schritt 4 — _record_snippet_error() in anonymize() aufrufen:**

Suche in anonymize() den Pfad der `('[ANON_FEHLER]', 'C')` zurueckgibt (Exception-Handler). Fuege dort den Aufruf ein:

```python
        except Exception as e:
            print(f'[ANON] anonymize() Fehler: {type(e).__name__}: {e}')
            _record_snippet_error()   # D-08 Kat. C: Rolling-Error-Counter
            return ('[ANON_FEHLER]', 'C')
```

WICHTIG:
- `_error_timestamps` ist eine einfache list (kein deque) — `pop(0)` ist korrekt fuer pruning
- Thread-safety: IMMER `_error_lock` verwenden beim Zugriff auf `_error_timestamps`
- `time.monotonic()` statt `time.time()` — monotone Uhr, kein DST-Sprung-Risiko
- Keine anderen Stellen in anonymize() aendern
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('services/anonymization.py').read()); print('Syntax OK')" && python -c "from services.anonymization import get_pipeline_status, _record_snippet_error; r = get_pipeline_status(); assert isinstance(r, dict), f'Expected dict, got {type(r)}'; assert 'status' in r; assert 'error_count_10min' in r; print('get_pipeline_status dict OK:', r)"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('services/anonymization.py').read())"` — kein SyntaxError
    - `grep -n "_record_snippet_error" services/anonymization.py` — mind. 2 Treffer (Definition + Aufruf in anonymize())
    - `grep -n "error_count_10min" services/anonymization.py` — Treffer in get_pipeline_status()
    - `python -c "from services.anonymization import get_pipeline_status; r = get_pipeline_status(); assert isinstance(r, dict) and 'status' in r and 'error_count_10min' in r"` — kein AssertionError
    - `grep -n "_error_lock" services/anonymization.py` — Treffer in BEIDEN neuen Funktionen (_record_snippet_error und get_pipeline_status)
    - **Finding 4 Hinweis:** Plan 07.1 hat keine direkten anonymize()-Aufrufe — kein Skip-Check noetig. `grep -n "ART9_REDACTED\|ANON_FEHLER" services/anonymization.py` — Treffer nur in return-Statements und Kommentaren (kein Skip-Check in diesem Plan erforderlich)
  </acceptance_criteria>

  <done>_record_snippet_error() existiert und wird in anonymize() Exception-Pfad aufgerufen. get_pipeline_status() gibt dict mit 'status' und 'error_count_10min' zurueck. Thread-safe via _error_lock.</done>
</task>

<task type="auto">
  <name>Task 2: app_routes.py — /api/health um pipeline_error_count_10min erweitern</name>
  <files>routes/app_routes.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\routes\app_routes.py (api_health()-Funktion lesen — suche den Block der nach Plan 07 Task 2 hinzugefuegt wurde: get_pipeline_status()-Import und pipeline_status-Feld in jsonify)
  </read_first>

  <action>
**Pre-Edit-Audit:**
1. Suche in app_routes.py api_health() nach dem Block aus Plan 07 Task 2:
   ```python
   from services.anonymization import get_pipeline_status
   pipeline_status = get_pipeline_status()
   ```
2. Verstehe: Nach Plan 07 gibt get_pipeline_status() noch einen STRING zurueck und pipeline_status wird direkt ins jsonify geschrieben. Nach Task 1 dieses Plans gibt get_pipeline_status() ein DICT zurueck. Der api_health()-Block muss angepasst werden.

**Ersetze den Pipeline-Status-Block in api_health():**

VORHER (Stand nach Plan 07 Task 2 — string return):
```python
    # Phase 08.23.2.B: Anonymisierungs-Pipeline-Status (D-08)
    try:
        from services.anonymization import get_pipeline_status
        pipeline_status = get_pipeline_status()
    except Exception:
        pipeline_status = 'unknown'

    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
        'pipeline_status': pipeline_status,
    })
```

NACHHER (dict-Return auspacken, error_count_10min hinzufuegen):
```python
    # Phase 08.23.2.B: Anonymisierungs-Pipeline-Status (D-08)
    try:
        from services.anonymization import get_pipeline_status
        _pipeline = get_pipeline_status()
        pipeline_status = _pipeline.get('status', 'unknown')
        pipeline_error_count = _pipeline.get('error_count_10min', 0)
    except Exception:
        pipeline_status = 'unknown'
        pipeline_error_count = 0

    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
        'pipeline_status': pipeline_status,              # D-08: 'ok' | 'degraded' | 'unavailable'
        'pipeline_error_count_10min': pipeline_error_count,  # D-08 Kat. C: Fehler in 10-Min-Fenster
    })
```

WICHTIG:
- `.get()` statt direktem Zugriff — defensiv gegen unerwartete dict-Struktur
- Kein anderer Code in api_health() aendern
- Einrueckung (4 Spaces) beibehalten
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('routes/app_routes.py').read()); print('Syntax OK')" && grep -n "pipeline_error_count_10min" routes/app_routes.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('routes/app_routes.py').read())"` — kein SyntaxError
    - `grep -n "pipeline_error_count_10min" routes/app_routes.py` — Treffer im jsonify()-Block
    - `grep -n "pipeline_status" routes/app_routes.py` — Treffer erhalten
    - `grep -n "_pipeline.get" routes/app_routes.py` — Treffer (dict-Auspacken mit .get())
    - `grep -n "backup_status\|backup_age_hours" routes/app_routes.py` — noch vorhanden (nicht entfernt)
  </acceptance_criteria>

  <done>/api/health gibt pipeline_error_count_10min im JSON zurueck. Pipeline-Status-Block liest dict-Return von get_pipeline_status() korrekt aus.</done>
</task>

<task type="auto">
  <name>Task 3: nerve.css + dashboard.html — Pipeline-Streifen CSS-Klassen und HTML-Divs</name>
  <files>static/nerve.css, templates/dashboard.html</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\static\nerve.css (Z.18-30: --warning-* Token-Block lesen; Z.370-380: --danger-red Token lesen; Z.598-605: .n-backup-warning Klassen-Body lesen — exaktes Pattern zum Spiegeln)
    - C:\Users\andre\dev\salesnerve\templates\dashboard.html (Z.1-35: backup-warning-strip div und script-Block lesen — Einfuegestelle fuer neue Divs und JS-Erweiterung)
  </read_first>

  <action>
**Teil A — nerve.css:**

**Schritt 1 — CSS-Token-Analyse:**
Pruefe ob `--danger` (ohne `-red`) als :root-Variable existiert.
- `grep -n "^\s*--danger:" static/nerve.css` — wenn kein Treffer: `--danger` ist KEIN definierter Token, nur `--danger-red: #EF4444`

**Schritt 2 — Neue CSS-Tokens in :root-Block einfuegen:**

Fuege NACH dem bestehenden `--warning-weight: 500;` Block (ca. Z.28) DIREKT den neuen Kommentar-Block ein:

```css
  /* ═══════════════════════════════════════
     PIPELINE STATUS STRIPS (.n-pipeline-error, .n-pipeline-warning)
     ═══════════════════════════════════════ */
  --pipeline-error-bg:      var(--danger-red, #EF4444);
  --pipeline-error-text:    #fff;
  --pipeline-warning-bg:    var(--warning-bg);
  --pipeline-warning-text:  var(--warning-text);
```

WICHTIG: `--pipeline-error-bg` referenziert `var(--danger-red, #EF4444)` — kein direkter Hex in Klassen-Body. `--pipeline-warning-bg` und `--pipeline-warning-text` WIEDERVERWENDEN die bestehenden `--warning-*` Tokens (kein Hex, keine Duplizierung).

**Schritt 3 — CSS-Klassen nach .n-backup-warning einfuegen:**

Suche `.n-backup-warning{...}` (ca. Z.601). Fuege DIREKT DANACH ein:

```css
.n-pipeline-error{display:none;background:var(--pipeline-error-bg);color:var(--pipeline-error-text);padding:var(--warning-padding);text-align:center;font-size:var(--warning-size);font-weight:var(--warning-weight);position:relative;z-index:100;}
.n-pipeline-warning{display:none;background:var(--pipeline-warning-bg);color:var(--pipeline-warning-text);padding:var(--warning-padding);text-align:center;font-size:var(--warning-size);font-weight:var(--warning-weight);position:relative;z-index:100;}
```

WICHTIG:
- `display:none` initial in CSS — das ist die initiale Sichtbarkeit, KEIN inline style auf dem HTML-Element ausser fuer JS-gesteuerte Show/Hide (die JS-Zeilen `style.display = 'block'` sind erlaubt)
- Die HTML-Divs erhalten `style="display:none"` als initial-state (CLAUDE.md-Ausnahme: display:none fuer Initial-Zustand ist erlaubt)
- Alle anderen Farben nur via CSS-Vars

**Teil B — dashboard.html:**

**Schritt 4 — Neue Streifen-Divs einfuegen:**

Suche `<div id="backup-warning-strip" ...>` (Z.6). Fuege die zwei neuen Divs DIREKT DARUNTER (nach der schliessenden `</div>` des backup-warning-strip) ein:

```html
<!-- Pipeline-Status-Strips (Phase 08.23.2.B D-08) — Kat. A (rot) und Kat. C (gelb) -->
<div id="pipeline-error-strip" class="n-pipeline-error" style="display:none">
  &#9888; Anonymisierungs-Pipeline gest&ouml;rt &mdash; Anruf-Aufzeichnung tempor&auml;r ausgesetzt.
</div>
<div id="pipeline-warning-strip" class="n-pipeline-warning" style="display:none">
  &#9888; Anonymisierungs-Pipeline produziert Fehler (<span id="pipeline-error-count">0</span> Snippets in 10 Min) &mdash; Logs pr&uuml;fen.
</div>
```

WICHTIG:
- HTML-Entities fuer Umlaute (ö→&ouml;, ä→&auml;, ü→&uuml;) — User-facing Text, echte Umlaute sind OK aber Entities sind sicherer in HTML-Attribut-Kontext
- `style="display:none"` ist die erlaubte Ausnahme (Initial-Zustand, CLAUDE.md)
- IDs `pipeline-error-strip` und `pipeline-warning-strip` — ASCII, kein Umlaut (CLAUDE.md Regel: HTML-IDs sind Code-Identifier)
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && grep -c "n-pipeline-error\|n-pipeline-warning" static/nerve.css && grep -c "pipeline-error-strip\|pipeline-warning-strip" templates/dashboard.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "n-pipeline-error" static/nerve.css` — mindestens 2 (Token-Def in :root + Klassen-Body)
    - `grep -c "n-pipeline-warning" static/nerve.css` — mindestens 2 (Token-Def in :root + Klassen-Body)
    - `grep -n "pipeline-error-strip\|pipeline-warning-strip" templates/dashboard.html` — mind. 2 Treffer (2 div-IDs)
    - `grep -n "pipeline-error-count" templates/dashboard.html` — Treffer (span-ID fuer Fehleranzahl)
    - KEINE hardcoded Hex-Farbe direkt in .n-pipeline-error oder .n-pipeline-warning Klassen-Body: `grep "n-pipeline-error{" static/nerve.css` zeigt KEINEN Hex-Wert (#xxx) im Klassen-Body — nur `var(...)` Referenzen
    - `grep -n "backup-warning-strip" templates/dashboard.html` — noch vorhanden (nicht geloescht)
  </acceptance_criteria>

  <done>.n-pipeline-error und .n-pipeline-warning CSS-Klassen existieren via CSS-Vars. dashboard.html hat beide Streifen-Divs mit korrekten IDs. Kein hardcoded Hex in CSS-Klassen-Body.</done>
</task>

<task type="auto">
  <name>Task 4: dashboard.html — JS-Polling-Erweiterung fuer pipeline_status</name>
  <files>templates/dashboard.html</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\templates\dashboard.html (Z.10-31: den kompletten fetch('/api/health') IIFE-Block lesen — verstehe die exakte Struktur des bestehenden .then(data => ...) Callbacks um die Pipeline-Erweiterung korrekt einzufuegen)
  </read_first>

  <action>
**Pre-Edit-Audit:**
1. Lese Z.10-31 des dashboard.html
2. Verstehe: Es ist ein einmaliger fetch (kein setInterval) — IIFE die beim Load einmal /api/health abruft
3. Verstehe: Der .catch(function() {}) am Ende ist der silent-fail Handler — dieser bleibt unveraendert

**Ersetze den kompletten fetch-IIFE-Block:**

VORHER:
```html
<script>
(function() {
  fetch('/api/health')
    .then(r => r.json())
    .then(data => {
      if (data.backup_status && data.backup_status !== 'ok') {
        var strip = document.getElementById('backup-warning-strip');
        var detail = document.getElementById('backup-warning-detail');
        if (strip) {
          if (data.backup_status === 'stale') {
            detail.textContent = 'Letztes Backup vor ' + (data.backup_age_hours || '?') + ' Stunden.';
          } else {
            detail.textContent = 'Kein Backup gefunden.';
          }
          strip.style.display = 'block';
        }
      }
    })
    .catch(function() {}); // silent fail — backup warning is non-critical
})();
</script>
```

NACHHER (Backup-Logik unveraendert; Pipeline-Block angehaengt):
```html
<script>
(function() {
  fetch('/api/health')
    .then(r => r.json())
    .then(data => {
      // Backup-Status-Streifen (Phase 08.23.2.A D-18) — unveraendert
      if (data.backup_status && data.backup_status !== 'ok') {
        var strip = document.getElementById('backup-warning-strip');
        var detail = document.getElementById('backup-warning-detail');
        if (strip) {
          if (data.backup_status === 'stale') {
            detail.textContent = 'Letztes Backup vor ' + (data.backup_age_hours || '?') + ' Stunden.';
          } else {
            detail.textContent = 'Kein Backup gefunden.';
          }
          strip.style.display = 'block';
        }
      }
      // Pipeline-Status-Streifen (Phase 08.23.2.B D-08)
      var pipelineStatus = data.pipeline_status;
      var errorStrip = document.getElementById('pipeline-error-strip');
      var warnStrip = document.getElementById('pipeline-warning-strip');
      if (errorStrip && warnStrip) {
        if (pipelineStatus === 'unavailable') {
          errorStrip.style.display = 'block';
          warnStrip.style.display = 'none';
        } else if (pipelineStatus === 'degraded') {
          errorStrip.style.display = 'none';
          warnStrip.style.display = 'block';
          var countEl = document.getElementById('pipeline-error-count');
          if (countEl) countEl.textContent = data.pipeline_error_count_10min || 0;
        } else {
          errorStrip.style.display = 'none';
          warnStrip.style.display = 'none';
        }
      }
    })
    .catch(function() {}); // silent fail — warnings sind non-critical
})();
</script>
```

WICHTIG:
- Backup-Logik ist EXAKT unveraendert (nur Kommentar hinzugefuegt)
- Pipeline-Block kommt NACH dem Backup-Block im selben .then()-Callback
- `null`-Guard `if (errorStrip && warnStrip)` — beide Elemente muessen existieren
- Bei pipeline_status = 'ok' oder fehlendem Wert: beide Streifen hidden (kein Banner)
- .catch() bleibt leer — silent fail wenn /api/health nicht erreichbar
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && grep -c "pipeline_status\|pipeline-error-strip\|pipeline-warning-strip" templates/dashboard.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "pipeline_status" templates/dashboard.html` — mind. 1 Treffer (JS-Auswertung)
    - `grep -n "pipeline-error-strip" templates/dashboard.html` — mind. 2 Treffer (HTML-div-ID + JS-getElementById)
    - `grep -n "pipeline-warning-strip" templates/dashboard.html` — mind. 2 Treffer (HTML-div-ID + JS-getElementById)
    - `grep -n "pipeline_error_count_10min" templates/dashboard.html` — Treffer (data.pipeline_error_count_10min Zugriff)
    - `grep -n "pipeline-error-count" templates/dashboard.html` — Treffer (span-ID + JS-getElementById)
    - `grep -n "backup-warning-strip" templates/dashboard.html` — noch vorhanden, unversehrt
    - `grep -n "backup_status" templates/dashboard.html` — noch vorhanden (Backup-Logik nicht entfernt)
    - `grep -n "catch" templates/dashboard.html` — Treffer (.catch bleibt empty/silent-fail)
    - **Finding 4 Hinweis:** Plan 07.1 enthaelt keine anonymize()-Aufrufe — kein Skip-Check noetig; `grep -n "ART9_REDACTED\|ANON_FEHLER" templates/dashboard.html` gibt 0 Treffer (korrekt)
  </acceptance_criteria>

  <done>dashboard.html fetch-IIFE-Block erhaelt Pipeline-Status-Auswertung. Bei 'unavailable' → roter Streifen. Bei 'degraded' → gelber Streifen mit Fehleranzahl. Bei 'ok' oder fehlenden Daten → kein Streifen. Backup-Logik unveraendert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/health -> Dashboard JS | Response enthaelt pipeline_status + pipeline_error_count_10min |
| anonymize() Exception-Handler -> _error_timestamps | Schreibzugriff aus beliebigen Threads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.B-071-01 | Information Disclosure | /api/health pipeline_error_count_10min | accept | Nur Integer-Zaehler — kein PII, kein Rueckschluss auf Gespraechtsinhalte |
| T-08.23.2.B-071-02 | Denial of Service | _error_timestamps unbegrenztes Wachstum | mitigate | _record_snippet_error() pruned Eintraege ausserhalb ROLLING_ERROR_WINDOW_S sofort beim Schreiben |
| T-08.23.2.B-071-03 | Tampering | Race-Condition auf _error_timestamps aus parallelen Threads | mitigate | _error_lock (threading.Lock) umschliesst alle Lese- und Schreib-Zugriffe |
</threat_model>

<verification>
Abschluss-Checkliste nach allen 4 Tasks:

1. Syntax-Check anonymization: `python -c "import ast; ast.parse(open('services/anonymization.py').read())"` — kein SyntaxError
2. Syntax-Check app_routes: `python -c "import ast; ast.parse(open('routes/app_routes.py').read())"` — kein SyntaxError
3. get_pipeline_status dict: `python -c "from services.anonymization import get_pipeline_status; r = get_pipeline_status(); assert isinstance(r, dict); assert 'error_count_10min' in r; print('OK')"` — kein AssertionError
4. _record_snippet_error vorhanden: `grep -c "_record_snippet_error" services/anonymization.py` — mind. 2 (Definition + Aufruf)
5. /api/health Erweiterung: `grep -n "pipeline_error_count_10min" routes/app_routes.py` — Treffer
6. CSS-Klassen vorhanden: `grep -c "n-pipeline-error\|n-pipeline-warning" static/nerve.css` — mind. 4 (2 Token-Defs + 2 Klassen)
7. Kein Hex in CSS-Klassen-Body: `grep "n-pipeline-error{" static/nerve.css` — kein #-Wert im Klassen-Body
8. HTML-Divs: `grep -c "pipeline-error-strip\|pipeline-warning-strip" templates/dashboard.html` — mind. 4 Treffer
9. JS-Erweiterung: `grep -n "pipeline_status" templates/dashboard.html` — Treffer
10. Backup-Logik intakt: `grep -n "backup-warning-strip" templates/dashboard.html` — noch vorhanden
</verification>

<success_criteria>
- _record_snippet_error() wird in anonymize() Exception-Pfad aufgerufen und fuegt time.monotonic()-Timestamp zu _error_timestamps hinzu (thread-safe via _error_lock)
- get_pipeline_status() gibt dict {'status': str, 'error_count_10min': int} zurueck
- GET /api/health Response enthaelt pipeline_status und pipeline_error_count_10min
- .n-pipeline-error und .n-pipeline-warning CSS-Klassen existieren ohne hardcoded Hex im Klassen-Body
- Dashboard zeigt roten Streifen bei pipeline_status='unavailable', gelben bei 'degraded', keinen bei 'ok'
- pipeline-error-count span zeigt Fehleranzahl im gelben Streifen
- Kein Banner bei /api/health-Netzwerkfehler (silent fail)
- Bestehende Backup-Warning-Logik unveraendert
- Finding 4 Begruendung dokumentiert: Plan 07.1 hat keine anonymize()-Aufrufe — kein Skip-Check noetig
</success_criteria>

<output>
Nach Abschluss Summary erstellen: `.planning/phases/08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen/08.23.2.B-07.1-SUMMARY.md`

Summary muss enthalten:
- Zeile(n) in services/anonymization.py wo _record_snippet_error() und get_pipeline_status() (dict-Version) definiert sind
- Zeile(n) in routes/app_routes.py wo pipeline_error_count_10min im jsonify steht
- Zeile(n) in static/nerve.css wo .n-pipeline-error und .n-pipeline-warning definiert sind
- Zeile(n) in templates/dashboard.html wo pipeline-error-strip und pipeline-warning-strip divs stehen
- Bestaetigung dass Backup-Logik unveraendert bleibt
- Finding 4 Begruendung: Plan 07.1 verwendet nur anonymize_output() indirekt via get_pipeline_status() — kein Sentinel-Skip-Check noetig
</output>
