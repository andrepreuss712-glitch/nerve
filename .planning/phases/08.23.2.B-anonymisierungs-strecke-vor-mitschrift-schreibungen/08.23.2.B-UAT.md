---
status: complete
phase: 08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen
source:
  - 08.23.2.B-01-SUMMARY.md
  - 08.23.2.B-02-SUMMARY.md
  - 08.23.2.B-03-SUMMARY.md
  - 08.23.2.B-04-SUMMARY.md
  - 08.23.2.B-05-SUMMARY.md
  - 08.23.2.B-06-SUMMARY.md
  - 08.23.2.B-07-SUMMARY.md
  - 08.23.2.B-07.1-SUMMARY.md
  - 08.23.2.B-08-SUMMARY.md
  - 08.23.2.B-09-SUMMARY.md
  - 08.23.2.B-10-SUMMARY.md
started: 2026-05-13T00:00:00Z
updated: 2026-05-13T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill any running Flask process. Start the app fresh: `python app.py`
  The server should boot without errors — no NameError, no ImportError, no crash.
  You should see: [ANON] Pipeline pre-warmed (or a silent pass if spaCy is not installed locally).
  The app should be reachable at http://localhost:5000
result: pass

### 2. pytest Suite Green
expected: |
  Run: `pytest tests/test_anonymization.py tests/test_anonymization_wiring.py tests/test_anonymization_perf.py -v`
  All tests should pass: 22 unit tests + 14 integration tests + 3 perf tests = 39 total.
  (spaCy NER tests auto-skip locally if de_core_news_lg is not installed — that is expected.)
  No failures, no errors.
result: pass

### 3. /api/health Returns pipeline_status
expected: |
  Log in and call: GET /api/health
  The JSON response should include a "pipeline_status" field with value "ok", "degraded", or "unavailable".
  Example: {"status": "ok", "pipeline_status": "ok", "backup_status": "...", ...}
  The field must be present (was added in Plan 07).
result: pass

### 4. Dashboard Pipeline-Status-Banner HTML Present
expected: |
  Open the dashboard page. In the HTML source (Ctrl+U or DevTools), search for "pipeline-error-strip".
  Two hidden divs should exist: id="pipeline-error-strip" and id="pipeline-warning-strip".
  Both should have style="display:none" since pipeline is healthy.
  The pipeline_status from /api/health should flow into these banners via the JS fetch callback.
result: pass

### 5. Live Session Start — No NameError Crash
expected: |
  Start a live session (click Start on the /live page, allow mic access).
  The session should initialize without crashing.
  In the Flask console, you should NOT see: NameError: name 'market' is not defined
  You SHOULD see: [ANON] AnrufAnonymisierer erstellt fuer sid='...' (from init_anonymisierer fix).
  Deepgram connection should establish normally.
result: skipped
reason: Local env has no login (no SQLite user, OAuth disabled). Code verified via CR-01 fix at deepgram_service.py:424-425 and WR-03 fix at deepgram_service.py:460. test_cache_lifecycle_init_get_pop + test_ghost_sid_no_exception cover the path. Deferred to production smoke test after ./deploy.sh.

### 6. Art-9 Keyword Block — Sensitive Content Not Stored
expected: |
  During a live session (or via a direct test call), speak or inject text containing a clear
  Art-9 keyword such as "Chemotherapie" or "Gewerkschaft Tarifvertrag".
  In the Flask console you should see: [ANON] Art-9-Treffer — Snippet nicht persistiert
  The transcript snippet should NOT appear in conversation_logs in the DB.
  (You can verify via: `SELECT text FROM conversation_logs ORDER BY id DESC LIMIT 5;`)
result: pass
reason: Direct Python test confirmed: anonymize('Chemo-Therapie läuft gut', None) returned ('[ART9_REDACTED]', 'C'). Live DB verification deferred to production deploy.

### 7. Phrase.quality_tier Column Exists in DB
expected: |
  After running `alembic upgrade head` (or checking the current schema), the phrases table
  should have a quality_tier column (VARCHAR(1), NOT NULL, DEFAULT 'A').
  Quick check: `SELECT quality_tier FROM phrases LIMIT 1;` should return 'A' for existing rows.
  Or check schema: the column should be visible in any DB browser.
result: pass

## Summary

total: 7
passed: 6
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps

[none yet]
