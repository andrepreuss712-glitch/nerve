---
phase: 08.23.2.B
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/art9_keywords.py
  - services/anonymization.py
  - app.py
autonomous: true
requirements:
  - Req-1
  - Req-2
  - Req-3
  - Req-4
  - Req-6

must_haves:
  truths:
    - "services/anonymization.py ist importierbar und exportiert AnrufAnonymisierer, anonymize, anonymize_output, register_briefing_pii, should_persist"
    - "services/art9_keywords.py existiert mit ART9_KEYWORDS Dict, 6 Kategorien, ~175 Keywords"
    - "anonymize() gibt Tuple[str, str] zurueck — bei Art-9-Treffer ('[ART9_REDACTED]', 'C')"
    - "anonymize() gibt ('[ANON_FEHLER]', 'C') bei Einzel-Pipeline-Exception (Kat. B)"
    - "is_pipeline_healthy=False wenn spaCy nicht ladbar; anonymize() raised AnonymizationPipelineUnavailable (Kat. A)"
    - "get_or_assign_token() vergibt [PERSON_A], [PERSON_B] ... mit threading.Lock()"
    - "ART9_KEYWORDS ist Modul-Level-Import in anonymization.py (nicht per-call-Import in _check_art9())"
    - "should_persist() gibt False zurueck fuer '[ART9_REDACTED]' und '[ANON_FEHLER]'"
    - "app.py enthaelt anonymize('Warmup', None) Pre-Warm-Aufruf nach Blueprint-Registrierung"
  artifacts:
    - path: "services/art9_keywords.py"
      provides: "ART9_KEYWORDS Dict[str, List[str]] fuer 6 DSGVO-Art.-9-Kategorien"
      contains: "ART9_KEYWORDS"
    - path: "services/anonymization.py"
      provides: "Anonymisierungs-Pipeline — drei Funktionen + AnrufAnonymisierer-Klasse + should_persist()"
      exports:
        - AnrufAnonymisierer
        - anonymize
        - anonymize_output
        - register_briefing_pii
        - is_pipeline_healthy
        - AnonymizationPipelineUnavailable
        - should_persist
    - path: "app.py"
      provides: "Pre-Warm-Aufruf beim App-Start (erster Request ohne Latenz-Spike)"
      contains: "anonymize"
  key_links:
    - from: "anonymize()"
      to: "art9_keywords.ART9_KEYWORDS"
      via: "Modul-Level-Import (Zeile ~5 in anonymization.py) — Art-9-Check Schritt 0"
    - from: "anonymize()"
      to: "_get_nlp() / spacy.load('de_core_news_lg')"
      via: "Thread-safe lazy load mit Double-Checked-Locking"
    - from: "AnrufAnonymisierer.get_or_assign_token()"
      to: "threading.Lock()"
      via: "self._lock in Konstruktor"
    - from: "should_persist()"
      to: "Caller-Code in Plans 05/06/07"
      via: "Gibt False fuer '[ART9_REDACTED]' und '[ANON_FEHLER]' — verhindert DB-Spam"
---

<objective>
Erstellt das DSGVO-Foundation-Modul `services/anonymization.py` und die Art-9-Keyword-Liste `services/art9_keywords.py` von Grund auf (Greenfield). Erweitert app.py um einen Pre-Warm-Aufruf.

Purpose: Kein Klartext-PII aus Live-Gespraechen oder Claude-Outputs darf in die DB landen. Dieses Modul ist die Basis fuer alle Verdrahtungs-Plaene (Plans 04-07).

Output:
- `services/art9_keywords.py` — ART9_KEYWORDS Dict mit ~175 Keywords in 6 Kategorien
- `services/anonymization.py` — AnrufAnonymisierer-Klasse + anonymize() + anonymize_output() + register_briefing_pii() + should_persist() + Fallback-Architektur (D-08)
- `app.py` — Pre-Warm anonymize('Warmup', None) nach Blueprint-Registrierung (Finding 5)
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\CLAUDE.md

<interfaces>
<!-- Bestehende Patterns aus codebase (services/-Struktur) -->

Aus services/live_session.py (Pattern fuer Thread-Safety):
```python
import threading
_session_state_lock = threading.Lock()
# Double-checked locking Pattern fuer lazy init
```

Aus database/models.py — Phrase-Modell (fuer kuenftige quality_tier-Verwendung):
```python
class Phrase(Base):
    __tablename__ = 'phrases'
    id             = Column(Integer, primary_key=True)
    user_id        = Column(Integer, ForeignKey('users.id'), nullable=False)
    session_id     = Column(Integer, ForeignKey('conversation_logs.id'), nullable=True)
    text           = Column(Text, nullable=False)
    objection_type = Column(String(100), nullable=False)
    created_at     = Column(DateTime, default=utcnow)
    # quality_tier kommt via Plan 03 Alembic-Migration
```

CLAUDE.md ASCII-Pflicht fuer Code-Identifier: Dict-Key 'anonymisierer' (kein Umlaut),
Attribute mapping, counters, is_pipeline_healthy — alle ASCII.
Klassen-Name AnrufAnonymisierer ist OK (User-facing, nicht DB-Spalte).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/art9_keywords.py erstellen</name>
  <files>services/art9_keywords.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (ASCII-Pflicht, Code-Style)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md (D-04: 6 Kategorien, ~175 Keywords, 0% False-Negative Pflicht)
  </read_first>

  <action>
Erstelle `services/art9_keywords.py` als neue Datei. Die Datei exportiert genau ein Symbol: `ART9_KEYWORDS: Dict[str, List[str]]`.

Dateistruktur:
```python
"""
Art-9-DSGVO-Keyword-Liste fuer den Anonymisierungs-Filter.
Quelle: DSGVO Art. 9 Abs. 1, BayLDA-Auditierbarkeit.
Letzte Aktualisierung: 2026-05-12
Kategorien: Gesundheit, Religion, Gewerkschaft, Sexuelle_Orientierung,
            Politische_Ueberzeugung, Ethnische_Herkunft
"""
from typing import Dict, List

ART9_KEYWORDS: Dict[str, List[str]] = {
    'Gesundheit': [
        # Diagnosen und Krankheiten (~50 Keywords)
        'krebs', 'tumor', 'diabetes', 'hiv', 'aids', 'depression', 'burnout',
        'angststoerung', 'schizophrenie', 'epilepsie', 'demenz', 'alzheimer',
        'herzinfarkt', 'schlaganfall', 'parkinson', 'multiple sklerose',
        'rheuma', 'arthritis', 'asthma', 'allergie', 'chemo', 'chemotherapie',
        'bestrahlung', 'dialyse', 'transplantation', 'herzschrittmacher',
        # Medizinische Einrichtungen und Behandlung
        'krankenhaus', 'klinik', 'psychiatrie', 'rehabilitation', 'reha',
        'therapie', 'psychotherapie', 'arzt', 'aerztin', 'chirurg', 'onkologe',
        'facharzt', 'hausarzt', 'krankenversicherung', 'pflegestufe', 'pflegegrad',
        # Symptome und Zustande
        'schwangerschaft', 'fehlgeburt', 'behinderung', 'beeintraechtigung',
        'krankschreibung', 'arbeitsunfaehigkeit', 'sucht', 'abhaengigkeit',
        'medikament', 'tabletten', 'antidepressivum', 'schmerzmittel',
        'insulin', 'blutdruck', 'herzrhythmus', 'blutungen', 'infektion',
        'covid', 'long covid', 'corona', 'impfung', 'impfschaden',
        'psychose', 'manie', 'bipolaar', 'bipolar', 'zwangsstoerung',
    ],
    'Religion': [
        # Konfessionen und Religionen (~30 Keywords)
        'katholisch', 'evangelisch', 'muslimisch', 'islamisch', 'juedisch',
        'buddhistisch', 'hinduistisch', 'konfession', 'taufe', 'kommunion',
        'kirche', 'moschee', 'synagoge', 'tempel', 'gottesdienst',
        'glaube', 'beten', 'gebet', 'bibel', 'koran', 'torah',
        'ramadan', 'fastenzeit', 'freikirche', 'sekunde', 'orden',
        'missionierung', 'atheist', 'agnostiker', 'religionsgemeinschaft',
        'kirchenmitglied', 'kirchensteuer',
    ],
    'Gewerkschaft': [
        # Gewerkschaftliche Aktivitaeten (~20 Keywords)
        'gewerkschaft', 'ig metall', 'verdi', 'dgb', 'dbb', 'betriebsrat',
        'streik', 'tarifvertrag', 'tarif', 'lohnverhandlung', 'tarifverhandlung',
        'arbeitsniederlegung', 'solidaritaet', 'betriebsraetswahl',
        'mitbestimmung', 'gesamtbetriebsrat', 'arbeitnehmervertreter',
        'gewerkschaftsmitglied', 'streikposten', 'aussperrung',
    ],
    'Sexuelle_Orientierung': [
        # Sexuelle Orientierung und Identitaet (~15 Keywords)
        'schwul', 'lesbisch', 'homosexuell', 'bisexuell', 'queer', 'lgbtq',
        'transgender', 'transsexuell', 'intersexuell', 'nicht-binaer',
        'gleichgeschlechtlich', 'gleichgeschlechtliche partnerschaft',
        'lebenspartnerschaft', 'pride', 'coming out',
    ],
    'Politische_Ueberzeugung': [
        # Parteien und politische Ueberzeugungen (~30 Keywords)
        'cdu', 'csu', 'spd', 'gruene', 'fdp', 'afd', 'linke', 'bsw',
        'npd', 'rechtsextrem', 'linksextrem', 'konservativ', 'sozialist',
        'kommunist', 'anarchist', 'liberaler', 'neonazi', 'nazi',
        'antifa', 'pegida', 'reichsbuerger', 'querdenker',
        'partei', 'parteimitglied', 'politische gesinnung', 'weltanschauung',
        'politische ueberzeugung', 'wahlverhalten', 'kandidatur',
        'abgeordneter', 'mdb', 'mep', 'verfassungsschutz',
    ],
    'Ethnische_Herkunft': [
        # Ethnische und nationale Herkunft mit Kontext-Markern (~30 Keywords)
        'migrationshintergrund', 'migrant', 'fluechtling', 'asylbewerber',
        'asylsuchender', 'abschiebung', 'aufenthaltstitel', 'visum',
        'herkunftsland', 'ethnie', 'volksgruppe', 'minderheit',
        'sinti', 'roma', 'kurde', 'kurden', 'tuerke', 'tuerken',
        'arabisch', 'afrikanisch', 'rassismus', 'diskriminierung',
        'antisemitismus', 'islamophobie', 'fremdenfeindlichkeit',
        'einbuergerung', 'staatsangehoerig', 'doppelte staatsangehoerig',
        'volkszugehoerigkeit', 'stammeszugehoerigkeit',
    ],
}
```

Anforderungen:
- Alle Keywords in Kleinbuchstaben (keyword-Matching erfolgt auf `text.lower()`)
- ASCII-Identifier fuer Dict-Keys (Sexuelle_Orientierung, nicht Sexuelle_Orientierung — Unterstrich OK als ASCII)
- Kommentare mit Quellenangabe und Update-Datum fuer BayLDA-Auditierbarkeit
- Keine Logik in dieser Datei — pure Datendatei
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "from services.art9_keywords import ART9_KEYWORDS; assert len(ART9_KEYWORDS) == 6; total = sum(len(v) for v in ART9_KEYWORDS.values()); print(f'OK: {len(ART9_KEYWORDS)} Kategorien, {total} Keywords gesamt'); assert total >= 150"</automated>
  </verify>

  <acceptance_criteria>
    - `from services.art9_keywords import ART9_KEYWORDS` schlaegt NICHT fehl
    - `len(ART9_KEYWORDS) == 6` (genau 6 Kategorien)
    - `sum(len(v) for v in ART9_KEYWORDS.values()) >= 150` (mind. 150 Keywords)
    - 'chemo' in ART9_KEYWORDS['Gesundheit'] (Art-9-Treffer aus SPEC Req-4 Acceptance)
    - Alle Keys sind ASCII (kein Umlaut in Dict-Key-Strings)
    - Alle Values sind Listen von lowercase strings
  </acceptance_criteria>

  <done>services/art9_keywords.py existiert, ist importierbar, hat 6 Kategorien mit mind. 150 Keywords gesamt, 'chemo' ist in Gesundheit-Liste.</done>
</task>

<task type="auto">
  <name>Task 2: services/anonymization.py erstellen (AnrufAnonymisierer-Klasse + alle drei Funktionen + should_persist() + Fallback-Architektur)</name>
  <files>services/anonymization.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md (D-01 bis D-08 vollstaendig)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md (Pattern 1-7, Pitfalls 1-5)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (ASCII-Pflicht, Test-Qualitaets-Regel)
    - C:\Users\andre\dev\salesnerve\services\art9_keywords.py (soeben erstellt — Schritt 0 konsumiert diese Datei)
  </read_first>

  <action>
Erstelle `services/anonymization.py` als neue Datei. Dies ist das komplette Foundation-Modul mit allen Exports.

**KRITISCHE INTERFACE-ENTSCHEIDUNG (D-05 vs. Req-4 — planning_context Abschnitt key_research_findings #3):**
D-05 (CONTEXT.md) gewinnt: `anonymize()` gibt **immer** `Tuple[str, str]` zurueck.
Bei Art-9-Treffer: `return ('[ART9_REDACTED]', 'C')` — KEIN None.
Caller-Code prueft `if anon_text == '[ART9_REDACTED]': skip_insert()`.
Alternativ: Caller kann `should_persist(anon_text)` verwenden (neue Hilfsfunktion, s.u.).

**FINDING 1 — KRITISCH: ART9_KEYWORDS Modul-Level-Import (NICHT in _check_art9()):**
`from services.art9_keywords import ART9_KEYWORDS` MUSS auf Modul-Ebene stehen (Zeile ~5,
nach den anderen Imports). NIEMALS innerhalb der `_check_art9()` Funktion — das waere
ein per-call-Import-Anti-Pattern das den Import-Cache bei jedem Aufruf konsultiert.
`_check_art9()` verwendet `ART9_KEYWORDS` direkt ohne eigenen Import-Statement.

**Modul-Struktur (exakte Reihenfolge):**

```python
"""
DSGVO-Anonymisierungs-Foundation-Modul fuer NERVE.
Pipeline-Reihenfolge: Art-9-Filter (Schritt 0) -> Regex (Schritt 1) -> spaCy NER (Schritt 2)
Phase: 08.23.2.B
"""
import os
import re
import time
import threading
from typing import Optional, Tuple, Dict, List

# ── Modul-Level-Import: ART9_KEYWORDS (PFLICHT hier — NICHT in _check_art9()) ──────────────
# Per-Call-Import in _check_art9() waere Anti-Pattern (Cross-AI-Review Finding 1).
from services.art9_keywords import ART9_KEYWORDS

# ── Modul-Level-Zustand ──────────────────────────────────────────────────────

is_pipeline_healthy: bool = True   # D-08 Kat. A: False wenn spaCy nicht ladbar
_error_timestamps: list = []       # Monotonic-Timestamps fuer Rolling-Error-Rate
_error_lock = threading.Lock()

ROLLING_ERROR_THRESHOLD = int(os.environ.get('ANON_ERROR_THRESHOLD', '5'))
ROLLING_ERROR_WINDOW_S  = int(os.environ.get('ANON_ERROR_WINDOW_S', '600'))  # 10 min

# ── spaCy Lazy-Load (Thread-safe Double-Checked Locking) ────────────────────

_nlp = None
_nlp_lock = threading.Lock()

def _get_nlp():
    """Thread-safe lazy load von de_core_news_lg. Einmal pro Prozess geladen."""
    global _nlp, is_pipeline_healthy
    if _nlp is None:
        with _nlp_lock:
            if _nlp is None:
                try:
                    import spacy
                    _nlp = spacy.load('de_core_news_lg')
                    print('[ANON] de_core_news_lg geladen')
                except Exception as e:
                    print(f'[ANON] KRITISCH: de_core_news_lg nicht ladbar: {type(e).__name__}')
                    is_pipeline_healthy = False
    return _nlp

# ── Custom Exception ─────────────────────────────────────────────────────────

class AnonymizationPipelineUnavailable(Exception):
    """Raised wenn is_pipeline_healthy=False (spaCy-Lade-Fehler, D-08 Kat. A)."""
    pass

# ── Regex-PII-Patterns (Schritt 1) ───────────────────────────────────────────

_RE_IBAN = re.compile(
    r'\bDE\d{2}[\s]?[A-Z0-9]{4}[\s]?[A-Z0-9]{4}[\s]?[A-Z0-9]{4}[\s]?[A-Z0-9]{4}[\s]?[A-Z0-9]{2}\b',
    re.IGNORECASE
)
_RE_EMAIL = re.compile(r'\b[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}\b')
_RE_USTID = re.compile(r'\bDE\s*\d{9}\b', re.IGNORECASE)
_RE_STEUERNR = re.compile(r'\b\d{2,3}[\s/]\d{3}[\s/]\d{4,5}\b')
_RE_DATUM_KONTEXT = re.compile(
    r'(?:geboren|geb\.|geburtstag|geburtsdatum)\s*:?\s*(\d{1,2}[./]\d{1,2}[./]\d{2,4})',
    re.IGNORECASE
)
_RE_KREDITKARTE = re.compile(r'(?<!\d)(?:\d[\s\-]?){13,19}(?!\d)')
```

**AnrufAnonymisierer-Klasse (exakt nach D-03):**
```python
class AnrufAnonymisierer:
    """Thread-safe Token-Cache fuer einen Anruf.
    Lifecycle: init_session_state -> pop_session_state (D-06).
    Cache NIEMALS in DB persistieren (BfDI-Konsultation).
    """

    def __init__(self):
        self._lock = threading.Lock()
        self.mapping: dict = {}            # 'Mueller' -> '[PERSON_A]'
        self.counters: dict = {'PERSON': 0, 'ORG': 0, 'LOC': 0}

    def get_or_assign_token(self, entity_text: str, entity_type: str) -> str:
        """Gibt stabilen Token fuer entity_text zurueck (erstellt neuen bei erstem Auftritt).
        >26 Treffer: [PERSON_AA], [PERSON_AB], ...
        Thread-safe via self._lock.
        """
        with self._lock:
            if entity_text in self.mapping:
                return self.mapping[entity_text]
            idx = self.counters[entity_type]
            if idx < 26:
                suffix = chr(ord('A') + idx)
            else:
                suffix = 'A' + chr(ord('A') + (idx - 26))
            token = f'[{entity_type}_{suffix}]'
            self.mapping[entity_text] = token
            self.counters[entity_type] += 1
            return token
```

**Hilfsfunktionen:**

```python
def _luhn_check(number_str: str) -> bool:
    digits = [int(d) for d in number_str if d.isdigit()]
    if len(digits) < 13:
        return False
    odd_digits = digits[-1::-2]
    even_digits = digits[-2::-2]
    total = sum(odd_digits)
    for d in even_digits:
        total += sum(divmod(d * 2, 10))
    return total % 10 == 0

def _parse_phone_de(text: str) -> list:
    """DE-Telefonnummern via phonenumbers mit Regex-Fallback."""
    found = []
    try:
        import phonenumbers
        for match in phonenumbers.PhoneNumberMatcher(text, 'DE'):
            found.append(text[match.start:match.end])
    except Exception:
        for m in re.finditer(r'(?:\+49|0049|0)\s*\d{2,5}[\s/\-]?\d{3,}[\s\d]{0,10}', text):
            found.append(m.group())
    return found

def _apply_regex_filter(text: str, cache: Optional[AnrufAnonymisierer]) -> Tuple[str, str]:
    """Regex-Vorfilter: ersetzt strukturierte PII durch [TYP_X]-Tokens.
    Gibt (bearbeiteter_text, quality_tier) zurueck. quality_tier='A' wenn kein Edge-Case.
    """
    tier = 'A'

    # IBAN
    for m in _RE_IBAN.finditer(text):
        iban_val = m.group()
        if cache:
            token = cache.get_or_assign_token(iban_val, 'IBAN')
        else:
            token = '[IBAN_X]'
        text = text.replace(iban_val, token)

    # E-Mail
    for m in _RE_EMAIL.finditer(text):
        email_val = m.group()
        if cache:
            token = cache.get_or_assign_token(email_val, 'EMAIL')
        else:
            token = '[EMAIL_X]'
        text = text.replace(email_val, token)

    # Telefon (phonenumbers-Library)
    for phone_val in _parse_phone_de(text):
        if cache:
            token = cache.get_or_assign_token(phone_val, 'TEL')
        else:
            token = '[TEL_X]'
        text = text.replace(phone_val, token)

    # USt-ID
    for m in _RE_USTID.finditer(text):
        ustid_val = m.group()
        if cache:
            token = cache.get_or_assign_token(ustid_val, 'USTID')
        else:
            token = '[USTID_X]'
        text = text.replace(ustid_val, token)

    # Steuernummer
    for m in _RE_STEUERNR.finditer(text):
        nr_val = m.group()
        if cache:
            token = cache.get_or_assign_token(nr_val, 'STEUERNR')
        else:
            token = '[STEUERNR_X]'
        text = text.replace(nr_val, token)

    # Kreditkarte (nur mit Luhn-Check)
    for m in _RE_KREDITKARTE.finditer(text):
        cc_val = m.group()
        if _luhn_check(cc_val):
            if cache:
                token = cache.get_or_assign_token(cc_val, 'KREDITKARTE')
            else:
                token = '[KREDITKARTE_X]'
            text = text.replace(cc_val, token)

    # Geburtsdatum mit Kontext-Marker
    text = _RE_DATUM_KONTEXT.sub('[DATUM]', text)

    return (text, tier)

def _apply_ner(text: str, cache: Optional[AnrufAnonymisierer], nlp) -> Tuple[str, str]:
    """spaCy NER: ersetzt PER/LOC/ORG durch stabile [TYP_X]-Tokens.
    quality_tier='B' wenn unsichere NER-Treffer (strukturelle Heuristik).
    HINWEIS: spaCy de_core_news_lg liefert KEINE Confidence-Scores fuer NER (Pitfall 1).
    quality_tier='B' via: Token-Laenge < 3 Zeichen, keine Grossbuchstaben, Sonderzeichen.
    """
    tier = 'A'
    doc = nlp(text)
    # Offset-basiertes Replacement (Entities von hinten nach vorne um Offset-Shift zu vermeiden)
    spans = [(ent.start_char, ent.end_char, ent.label_, ent.text) for ent in doc.ents
             if ent.label_ in ('PER', 'LOC', 'ORG')]
    # Sortiere von hinten nach vorne
    spans.sort(key=lambda x: x[0], reverse=True)
    for start, end, label, ent_text in spans:
        # Typ-Mapping: PER->PERSON, LOC->LOC, ORG->ORG
        token_type = 'PERSON' if label == 'PER' else label
        # quality_tier='B' Heuristik (kein spaCy-Confidence-Score verfuegbar)
        is_uncertain = (
            len(ent_text) < 3
            or not ent_text[0].isupper()
            or any(c in ent_text for c in ['/', '\\', '@', '#'])
        )
        if is_uncertain:
            tier = 'B'
        if cache:
            token = cache.get_or_assign_token(ent_text, token_type)
        else:
            # Kein Cache (Ghost-SID, Pitfall 3): anonymer Token ohne Mapping-Persistenz
            suffix_map = {'PERSON': 'X', 'LOC': 'X', 'ORG': 'X'}
            token = f'[{token_type}_{suffix_map[token_type]}]'
        text = text[:start] + token + text[end:]
    return (text, tier)

def _check_art9(text: str) -> bool:
    """Prueft ob text Art-9-DSGVO-Keywords enthaelt. 0% False-Negative Pflicht.
    WICHTIG: ART9_KEYWORDS ist Modul-Level-Import (NICHT hier importieren —
    Cross-AI-Review Finding 1: per-call Import ist Anti-Pattern).
    """
    text_lower = text.lower()
    for keywords in ART9_KEYWORDS.values():
        for kw in keywords:
            if kw in text_lower:
                return True
    return False

def _register_error() -> None:
    """Prueft Rolling-Error-Rate und setzt is_pipeline_healthy bei Schwellenwert (D-08 Kat. C)."""
    global is_pipeline_healthy
    now = time.monotonic()
    with _error_lock:
        _error_timestamps.append(now)
        cutoff = now - ROLLING_ERROR_WINDOW_S
        while _error_timestamps and _error_timestamps[0] < cutoff:
            _error_timestamps.pop(0)
        if len(_error_timestamps) > ROLLING_ERROR_THRESHOLD:
            is_pipeline_healthy = False   # Kat. C: degraded
```

**Drei Public-Funktionen + should_persist():**

```python
def anonymize(text: str, cache: Optional[AnrufAnonymisierer]) -> Tuple[str, str]:
    """
    DSGVO-Anonymisierungs-Pipeline (INPUT-PFAD: rohes STT-Material).

    Pipeline-Reihenfolge:
      Schritt 0: Art-9-Filter (ZUERST — 0% False-Negative Pflicht)
      Schritt 1: Regex-Vorfilter (IBAN, Email, Tel, USt-ID, Steuernr, Kreditkarte, Datum)
      Schritt 2: spaCy NER (PER/LOC/ORG -> Buchstaben-Tokens)

    Returns: Tuple[str, str] — (anonymized_text, quality_tier)
      quality_tier='A': sauber anonymisiert, keine Art-9-Treffer
      quality_tier='B': Edge-Cases (unsichere NER-Treffer via Heuristik)
      quality_tier='C': Art-9-Treffer -> '[ART9_REDACTED]', oder Exception -> '[ANON_FEHLER]'

    WICHTIG (D-05 vs. Req-4 Aufloesung): Bei Art-9-Treffer return ('[ART9_REDACTED]', 'C').
    Caller-Code prueft: if anon_text == '[ART9_REDACTED]': skip_insert()
    Alternativ: if not should_persist(anon_text): skip_insert()
    Cache=None: Regex-Only-Modus + NER ohne Mapping-Persistenz (Ghost-SID-Fallback, Pitfall 3).
    """
    global is_pipeline_healthy

    # D-08 Kat. A: Pipeline-Lade-Fehler
    if not is_pipeline_healthy:
        raise AnonymizationPipelineUnavailable('[ANON] Pipeline nicht verfuegbar (is_pipeline_healthy=False)')

    if not text or not text.strip():
        return ('', 'A')

    # Schritt 0: Art-9-Filter (MUSS VOR Regex+NER laufen — D-04)
    try:
        if _check_art9(text):
            # Ganzer Snippet verworfen — kein Partial-Replace (DSGVO-Pflicht)
            return ('[ART9_REDACTED]', 'C')
    except Exception as e:
        _register_error()
        print(f'[ANON] Art9-Check-Fehler (len={len(text)}): {type(e).__name__}')
        return ('[ANON_FEHLER]', 'C')

    # Schritt 1: Regex-Vorfilter
    try:
        text, tier = _apply_regex_filter(text, cache)
    except Exception as e:
        _register_error()
        print(f'[ANON] Regex-Fehler (len={len(text)}): {type(e).__name__}')
        return ('[ANON_FEHLER]', 'C')

    # Schritt 2: spaCy NER
    try:
        nlp = _get_nlp()
        if nlp and is_pipeline_healthy:
            text, ner_tier = _apply_ner(text, cache, nlp)
            if ner_tier == 'B':
                tier = 'B'
    except AnonymizationPipelineUnavailable:
        raise
    except Exception as e:
        _register_error()
        print(f'[ANON] NER-Fehler (len={len(text)}): {type(e).__name__}')
        return ('[ANON_FEHLER]', 'C')

    return (text, tier)


def anonymize_output(text: str, cache: Optional[AnrufAnonymisierer]) -> str:
    """
    OUTPUT-PFAD: Cache-Reverse-Lookup fuer Claude-generierten Text.
    Ersetzt alle bekannten Klartext-Entitaeten aus cache.mapping durch ihre Tokens.
    Laengere Keys zuerst (verhindert Partial-Matches: 'Dr. Mueller' vor 'Mueller').
    Kein NER-Aufruf (Output-Pfad ist Cache-Lookup-Only, A6 aus RESEARCH.md).
    """
    if not text or not cache:
        return text if text else ''
    with cache._lock:
        items = sorted(cache.mapping.items(), key=lambda x: len(x[0]), reverse=True)
    for original, token in items:
        if original in text:
            text = text.replace(original, token)
    return text


def register_briefing_pii(briefing_data: dict, cache: AnrufAnonymisierer) -> None:
    """
    Vorbefuellung des Token-Caches mit PreCall-Briefing-PII.
    Briefing-PII bekommt zuerst Buchstaben (A, B, ...) — Live-Mitschrift danach (D-03).
    Verdrahtung in live_session.py kommt in Phase 08.23.2.G (Account-Memory-Tabelle).
    """
    if not briefing_data or not cache:
        return
    # Personennamen aus Briefing registrieren
    for feld in ('personen', 'ansprechpartner', 'kontakte'):
        personen = briefing_data.get(feld, [])
        if isinstance(personen, list):
            for name in personen:
                if isinstance(name, str) and name.strip():
                    cache.get_or_assign_token(name.strip(), 'PERSON')
    # Firma aus Briefing registrieren
    firmenname = briefing_data.get('firmenname') or briefing_data.get('firma', '')
    if isinstance(firmenname, str) and firmenname.strip():
        cache.get_or_assign_token(firmenname.strip(), 'ORG')


def should_persist(anon_text: str) -> bool:
    """Gibt False zurueck wenn anon_text nicht in die DB persistiert werden soll.
    Verhindert DB-Spam mit literalem '[ART9_REDACTED]' oder '[ANON_FEHLER]' (D-05).
    Caller-Verwendung: if not should_persist(anon_text): continue  # skip DB insert
    """
    return anon_text not in ('[ART9_REDACTED]', '[ANON_FEHLER]')
```

**Wichtige CLAUDE.md-Konformitaets-Checks:**
- Alle Code-Identifier sind ASCII (is_pipeline_healthy, _error_timestamps, _apply_ner, _check_art9, etc.)
- Klassen-Name AnrufAnonymisierer: OK (User-facing, nicht DB-Spalte)
- Keine Source-Presence-Tests — nur Runtime-Behavior-Tests (Tests kommen in Plan 08)
- Kein INSERT/DB-Write in dieser Datei
- `AnrufAnonymisierer.mapping` ist dict (RAM-only, nie persistiert)
- ART9_KEYWORDS Import auf Modul-Ebene (Finding 1 Fix)
- `_check_art9()` enthaelt KEINEN eigenen Import-Statement (Finding 1 Fix)
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "from services.anonymization import AnrufAnonymisierer, anonymize, anonymize_output, register_briefing_pii, is_pipeline_healthy, AnonymizationPipelineUnavailable, should_persist; print('Import OK'); c = AnrufAnonymisierer(); t1 = c.get_or_assign_token('Mueller', 'PERSON'); t2 = c.get_or_assign_token('Mueller', 'PERSON'); assert t1 == t2 == '[PERSON_A]', f'Stable token failed: {t1} vs {t2}'; assert should_persist('[ART9_REDACTED]') == False; assert should_persist('[ANON_FEHLER]') == False; assert should_persist('Hallo') == True; print('should_persist OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `from services.anonymization import AnrufAnonymisierer, anonymize, anonymize_output, register_briefing_pii, should_persist` schlaegt NICHT fehl
    - `AnrufAnonymisierer().get_or_assign_token('Mueller', 'PERSON')` gibt '[PERSON_A]' zurueck (Buchstaben-suffixed, nicht [PERSON_1])
    - Zweiter Aufruf mit gleichem Namen gibt denselben Token (stable token)
    - `anonymize` hat Signatur mit Parametern `(text, cache)` — pruefbar via inspect.signature
    - `anonymize_output` hat Signatur mit Parametern `(text, cache)`
    - `register_briefing_pii` hat Signatur mit Parametern `(briefing_data, cache)`
    - `is_pipeline_healthy` ist True beim Import (kein Fail-Zustand)
    - `AnonymizationPipelineUnavailable` ist eine Exception-Klasse
    - `grep -rn "AnrufAnonymisierer\|mapping" C:/Users/andre/dev/salesnerve/database/` gibt 0 Treffer (Cache niemals in DB)
    - **Finding 1 Fix:** `grep -n "from services.art9_keywords import" services/anonymization.py` — Treffer NUR in den ersten ~10 Zeilen (Modul-Level), KEIN Treffer innerhalb einer Funktion
    - **Finding 1 Fix:** `grep -n "def _check_art9" services/anonymization.py` gefolgt von Lesen der Funktionskoerpers — der Koerper enthaelt KEIN `import` Statement
    - `should_persist('[ART9_REDACTED]')` gibt False zurueck
    - `should_persist('[ANON_FEHLER]')` gibt False zurueck
    - `should_persist('normaler Text')` gibt True zurueck
  </acceptance_criteria>

  <done>services/anonymization.py existiert, alle Symbols importierbar inkl. should_persist(), AnrufAnonymisierer gibt [PERSON_A] zurueck (Buchstaben-Suffix), ART9_KEYWORDS als Modul-Level-Import (kein per-call-Import in _check_art9()), kein DB-Bezug in database/.</done>
</task>

<task type="auto">
  <name>Task 3: app.py — spaCy Pipeline Pre-Warm nach Blueprint-Registrierung</name>
  <files>app.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\app.py (vollstaendig lesen — Blueprint-Registrierungsbereich identifizieren, ca. Z.20-80; App-Startup-Sequenz verstehen)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Logging-Konvention: [ANON]-Prefix, try-except mit pass fuer non-critical)
  </read_first>

  <action>
**Pre-Edit-Audit:**
1. Lese app.py vollstaendig
2. Identifiziere die letzte Blueprint-app.register_blueprint()-Zeile
3. Identifiziere wo `if __name__ == '__main__':` steht (Ende der Datei)
4. Pre-Warm-Aufruf muss NACH allen register_blueprint()-Aufrufen stehen und
   VOR dem `if __name__ == '__main__':`-Block

**Fuege NACH der letzten Blueprint-Registrierung und VOR if __name__ == '__main__': ein:**

```python
# ── Phase 08.23.2.B: spaCy Pipeline Pre-Warm (Finding 5 Fix) ─────────────────
# Ladet de_core_news_lg beim App-Start damit der erste echte Snippet keine
# 700MB-Lade-Latenz ausloest. Non-critical: App startet auch wenn Pre-Warm fehlschlaegt.
try:
    from services.anonymization import anonymize
    anonymize('Warmup', None)
    print('[ANON] Pipeline pre-warmed')
except Exception:
    pass  # Non-critical — main startup continues
# ────────────────────────────────────────────────────────────────────────────────
```

WICHTIG:
- `except Exception: pass` ist korrekt hier (CLAUDE.md Pattern fuer non-critical startup)
- anonymize('Warmup', None) loest `_get_nlp()` aus, der wiederum `spacy.load('de_core_news_lg')` aufruft
- Bei spaCy-Nicht-Verfuegbarkeit bleibt `is_pipeline_healthy = False` — korrekt (D-08 Kat. A)
- Kein anderer Code in app.py aendern
- Pre-Warm darf die App-Startup-Zeit verlaengern (~3-5s auf CX22 beim ersten Deploy) — akzeptabel
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('app.py').read()); print('Syntax OK')" && grep -n "anonymize" app.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('app.py').read())"` — kein SyntaxError
    - `grep -n "anonymize" app.py` — mindestens ein Treffer mit `anonymize('Warmup', None)` oder `anonymize("Warmup")`
    - `grep -n "pre-warmed\|pre_warmed\|Pre-Warm\|prewarm" app.py` — Treffer (Kommentar oder Print-Statement vorhanden)
    - `grep -n "from services.anonymization import" app.py` — Treffer im Pre-Warm-Block
    - Der Pre-Warm-Block steht NACH mindestens einer `register_blueprint`-Zeile (grep Zeilennummern pruefen)
    - Kein anderer Code in app.py wurde geaendert (nur der Pre-Warm-Block hinzugefuegt)
  </acceptance_criteria>

  <done>app.py enthaelt Pre-Warm-Aufruf anonymize('Warmup', None) nach Blueprint-Registrierung; erster echter Snippet hat keine spaCy-Lade-Latenz; App startet weiterhin bei Pre-Warm-Fehler (non-critical).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STT-Output -> anonymize() | Ungefilterter Deepgram-Transkript-Text (kann beliebige PII enthalten) |
| Claude-Output -> anonymize_output() | KI-generierter Text (kann Briefing-Namen echoen) |
| AnrufAnonymisierer.mapping -> RAM | Cache darf NIEMALS Richtung DB fliessen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.B-01 | Information Disclosure | AnrufAnonymisierer.mapping | mitigate | Cache RAM-only; grep database/ auf 'AnrufAnonymisierer' muss 0 Treffer geben |
| T-08.23.2.B-02 | Information Disclosure | _apply_ner() Log-Output | mitigate | Logs enthalten nur len(text) + Exception-Typ, nie den Snippet-Text selbst |
| T-08.23.2.B-03 | Information Disclosure | Art-9-False-Negative | mitigate | Art-9-Scan lauft VOR Regex+NER; ganzer Snippet wird '[ART9_REDACTED]' (kein Partial-Replace) |
| T-08.23.2.B-04 | Spoofing | Cross-Session Token-Leak | mitigate | Pro SID isolierte AnrufAnonymisierer-Instanz — kein globaler Singleton |
| T-08.23.2.B-05 | Tampering | Pipeline-Fail-Open | mitigate | AnonymizationPipelineUnavailable zwingt alle Caller zum Skip (0 Inserts bei Kat. A) |
| T-08.23.2.B-06 | Denial of Service | spaCy Modell-Load | mitigate | Pre-Warm bei App-Start (Finding 5) verhindert Latenz-Spike beim ersten User-Request; Gunicorn gthread-Config (Finding 2, Plan 02) verhindert OOM durch mehrfaches Laden |
| T-08.23.2.B-07 | Information Disclosure | DB-Spam '[ART9_REDACTED]' literal | mitigate | should_persist() Hilfsfunktion; alle Caller-Plaene (05/06/07) pruefen explizit auf '[ART9_REDACTED]' vor DB-Write |
</threat_model>

<verification>
Nach allen drei Tasks:

1. Import-Check: `python -c "from services.anonymization import AnrufAnonymisierer, anonymize, anonymize_output, register_briefing_pii, should_persist"` — kein Fehler
2. Art9-Keywords: `python -c "from services.art9_keywords import ART9_KEYWORDS; print(len(ART9_KEYWORDS))"` gibt 6
3. Token-Format: `python -c "from services.anonymization import AnrufAnonymisierer; c = AnrufAnonymisierer(); print(c.get_or_assign_token('Mueller', 'PERSON'))"` gibt '[PERSON_A]' (Buchstabe, nicht Zahl)
4. Cache-nicht-in-DB: `grep -rn "AnrufAnonymisierer\|\.mapping" C:/Users/andre/dev/salesnerve/database/` gibt 0 Treffer
5. Zweite Instanz gibt unabhaengige Tokens: neues AnrufAnonymisierer() startet wieder bei 'A'
6. **Finding 1 Verifikation:** `grep -n "from services.art9_keywords import" services/anonymization.py` — Treffer nur in den ersten Zeilen (Modul-Level), nicht innerhalb von _check_art9()
7. **should_persist() Verifikation:** `python -c "from services.anonymization import should_persist; assert not should_persist('[ART9_REDACTED]'); assert not should_persist('[ANON_FEHLER]'); assert should_persist('Text'); print('OK')"` — kein Fehler
8. **Pre-Warm Verifikation:** `grep -n "Warmup" app.py` — Treffer
</verification>

<success_criteria>
- services/art9_keywords.py: importierbar, 6 Kategorien, >= 150 Keywords, 'chemo' in Gesundheit
- services/anonymization.py: alle Symbols + is_pipeline_healthy + AnonymizationPipelineUnavailable + should_persist() exportiert
- Token-Format: [PERSON_A] (Buchstaben-Suffix per D-03) — NICHT [PERSON_1]
- Art-9-Return: anonymize() gibt ('[ART9_REDACTED]', 'C') zurueck (D-05 Tuple-Kontrakt)
- Pipeline-Exception-Return: ('[ANON_FEHLER]', 'C')
- AnonymizationPipelineUnavailable raised wenn is_pipeline_healthy=False
- Kein AnrufAnonymisierer-Bezug in database/
- **Finding 1 Fix:** ART9_KEYWORDS als Modul-Level-Import, kein per-call Import in _check_art9()
- **Finding 4 Hilfsfunktion:** should_persist('[ART9_REDACTED]') == False, should_persist('[ANON_FEHLER]') == False
- **Finding 5 Fix:** app.py enthaelt anonymize('Warmup', None) Pre-Warm-Block nach Blueprint-Registrierung
</success_criteria>

<output>
Nach Abschluss Summary erstellen: `.planning/phases/08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen/08.23.2.B-01-SUMMARY.md`

Summary muss enthalten:
- Exakte Datei-Groesse beider Service-Dateien (wc -l)
- Anzahl der Art-9-Keywords pro Kategorie (aus ART9_KEYWORDS)
- Import-Verifikation Ergebnis
- Token-Format-Verifikation ('[PERSON_A]' Buchstaben-Suffix bestaetigt)
- Konflikt-Aufloesung dokumentiert: D-05 Tuple-Interface implementiert (nicht None bei Art-9)
- Finding 1 Fix dokumentiert: ART9_KEYWORDS auf Modul-Ebene (nicht in _check_art9())
- Finding 4 Fix dokumentiert: should_persist() Hilfsfunktion exportiert
- Finding 5 Fix dokumentiert: Pre-Warm-Zeile in app.py (Zeilennummer)
</output>
</output>
