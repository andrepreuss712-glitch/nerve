---
phase: 08.23.2.B
plan: "07"
type: execute
wave: 2
depends_on:
  - "08.23.2.B-01"
  - "08.23.2.B-04"
files_modified:
  - routes/app_routes.py
autonomous: true
requirements:
  - Req-1
  - Req-5

must_haves:
  truths:
    - "POST /api/session-rating anonymisiert comment via anonymize() mit Regex+NER ohne Token-Cache (ephemerer Modus)"
    - "GET /api/health gibt pipeline_status='ok' oder 'unavailable' zurueck"
    - "Dashboard zeigt roten Streifen wenn pipeline_status='unavailable' (D-08 Kat. A)"
    - "Dashboard zeigt gelben Streifen wenn pipeline_status='degraded' (D-08 Kat. C)"
  artifacts:
    - path: "routes/app_routes.py"
      provides: "Anonymisierungs-Verdrahtung in /api/session-rating (Z.903) und /api/health (Z.1350)"
      contains: "pipeline_status"
  key_links:
    - from: "app_routes.py Z.903 latest.kommentar = comment"
      to: "anonymize(comment, None)"
      via: "cache=None (kein SID-Kontext nach Call) -> Regex+NER ohne Mapping (quality_tier='B')"
    - from: "app_routes.py Z.1389 return jsonify()"
      to: "is_pipeline_healthy aus services.anonymization"
      via: "pipeline_status='ok' | 'unavailable' | 'degraded'"
---

<objective>
Verdrahtet Anonymisierung in `routes/app_routes.py` an zwei Stellen: User-Kommentar in POST /api/session-rating (Z.903) und pipeline_status-Feld in GET /api/health (Z.1389).

Purpose:
1. ConversationLog.kommentar ist User-Freitext nach Call — muss anonymisiert werden. Da der Token-Cache nach Session-Ende geloescht ist, wird cache=None verwendet (Regex+NER ohne Mapping-Persistenz, quality_tier='B').
2. /api/health muss pipeline_status exponieren damit das Dashboard Kat-A- und Kat-C-Fallbacks visualisieren kann (D-08).

Output: app_routes.py mit zwei minimalen Aenderungen — kein sonstiger Refactor.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-RESEARCH.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\CLAUDE.md

<interfaces>
<!-- app_routes.py Z.893-904 (/api/session-rating — verifiziert) -->
```python
        if latest and latest.sterne is None:
            latest.sterne    = int(stars)
            latest.kommentar = comment    # <-- Anonymisierungs-Stelle
        db.commit()
```

<!-- app_routes.py Z.1389-1393 (/api/health Rueckgabe — verifiziert) -->
```python
    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
    })
```

<!-- anonymization.py Interface -->
```python
is_pipeline_healthy: bool  # Modul-Level-Flag
_error_timestamps: list    # Fuer Rolling-Error-Count (Kat. C)
ROLLING_ERROR_THRESHOLD: int  # Default 5

def anonymize(text: str, cache: Optional[AnrufAnonymisierer]) -> Tuple[str, str]:
    # cache=None: Regex-Only + NER ohne Mapping-Persistenz (Pitfall 4 Loesung)
    # Returns ('[ART9_REDACTED]', 'C') bei Art-9; ('[ANON_FEHLER]', 'C') bei Exception
```

<!-- Pitfall 4 aus RESEARCH.md: /api/session-rating kein SID-Kontext -->
Loesung: anonymize(comment, None) — cache=None aktiviert Regex-Only+NER-Modus ohne Mapping-Persistenz
quality_tier='B' fuer alle Kommentare ohne Cache-Kontext
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/session-rating — anonymize() fuer User-Kommentar (Z.903)</name>
  <files>routes/app_routes.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\routes\app_routes.py (Z.873-920: 30 Zeilen Kontext um Z.903 lesen — Pre-Insert-Audit)
    - C:\Users\andre\dev\salesnerve\CLAUDE.md (Pre-Insert-Control-Flow-Audit, Postgres-Drift)
  </read_first>

  <action>
**Pre-Insert-Control-Flow-Audit:**
1. Lese Z.873-920 vollstaendig
2. Verstehe: `/api/session-rating` empfaengt `comment` (User-Freitext). Der Token-Cache ist nach Session-Ende bereits via pop_session_state() geloescht (Pitfall 4 aus RESEARCH.md).
3. Loesung (Pitfall 4 Option a): `anonymize(comment, None)` — cache=None aktiviert Regex+NER-Modus ohne Mapping-Persistenz. quality_tier='B' fuer alle Kommentare in diesem Pfad.
4. Race-Condition: Kein SID-Kontext im HTTP-Request — korrekt, cache=None ist die einzige Loesung.

**Ersetze den Kommentar-Write-Block:**

VORHER (die Zeile `latest.kommentar = comment`):
```python
        if latest and latest.sterne is None:
            latest.sterne    = int(stars)
            latest.kommentar = comment
```

NACHHER:
```python
        if latest and latest.sterne is None:
            latest.sterne = int(stars)
            # Phase 08.23.2.B: INPUT-PFAD Anonymisierung (D-01, Req-7)
            # cache=None: Token-Cache nach Session-Ende geloescht (Pitfall 4).
            # Regex+NER laeuft ohne Mapping-Persistenz (quality_tier='B').
            _comment_anon = comment
            if comment:
                try:
                    from services.anonymization import anonymize, AnonymizationPipelineUnavailable
                    _anon_result = anonymize(comment, None)  # cache=None: ephemerer Modus
                    _comment_text, _comment_tier = _anon_result
                    if _comment_text != '[ART9_REDACTED]':
                        _comment_anon = _comment_text
                    else:
                        _comment_anon = ''  # Art-9-Treffer: leerer Kommentar (nicht persistieren)
                        print(f'[ANON] Art-9 in session-rating Kommentar erkannt, Kommentar geleert')
                except AnonymizationPipelineUnavailable:
                    print('[ANON] Pipeline unavailable, Kommentar nicht anonymisiert — nicht persistiert')
                    _comment_anon = ''  # Fail-safe: kein Kommentar wenn Pipeline down
                except Exception as _anon_err:
                    print(f'[ANON] anonymize() Fehler in session-rating: {type(_anon_err).__name__}')
                    _comment_anon = comment  # Fail-open Fallback fuer Output-Pfad
            latest.kommentar = _comment_anon
```

WICHTIG:
- `latest.sterne` bleibt unveraendert (numerisch, kein PII)
- Art-9-Treffer: Kommentar wird leer gesetzt (Inhalt nicht persistieren)
- Pipeline-Unavailable (Kat. A): Kommentar nicht persistieren (fail-safe)
- Einrueckung exakt beibehalten
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('routes/app_routes.py').read()); print('Syntax OK')" && grep -n "anonymize" routes/app_routes.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('routes/app_routes.py').read())"` — kein SyntaxError
    - `grep -n "anonymize" routes/app_routes.py` — Treffer fuer session-rating Block
    - `grep -n "_comment_anon" routes/app_routes.py` — Treffer
    - `grep -n "latest.kommentar" routes/app_routes.py` — zeigt `latest.kommentar = _comment_anon` (nicht `= comment`)
    - Kein anderer Code in app_routes.py geaendert
  </acceptance_criteria>

  <done>/api/session-rating: latest.kommentar erhaelt anonymisierten Kommentar; Art-9-Treffer und Pipeline-Unavailable werden behandelt.</done>
</task>

<task type="auto">
  <name>Task 2: /api/health — pipeline_status Feld hinzufuegen (Z.1389)</name>
  <files>routes/app_routes.py</files>

  <read_first>
    - C:\Users\andre\dev\salesnerve\routes\app_routes.py (Z.1348-1394: 30 Zeilen Kontext um Z.1389 lesen — api_health Funktion vollstaendig)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen\08.23.2.B-CONTEXT.md (D-08: Kat. A = 'unavailable', Kat. C = 'degraded')
  </read_first>

  <action>
**Pre-Insert-Audit:**
1. Lese Z.1348-1394 vollstaendig (api_health() Funktion)
2. Verstehe: Bestehende Rueckgabe hat `status`, `backup_status`, `backup_age_hours`. Neues Feld `pipeline_status` wird hinzugefuegt.
3. Kat. A (is_pipeline_healthy=False): 'unavailable'
4. Kat. C (Rolling-Error > Threshold): Kein separates Flag in is_pipeline_healthy notwendig — Kat. C setzt is_pipeline_healthy=False via _register_error(). Fuer 'degraded' Status brauchen wir Zugriff auf `_error_timestamps` Laenge. Einfachere Loesung: eigenstaendige `get_pipeline_status()` Funktion in anonymization.py.

**Aenderung in anonymization.py (Hilfsfunktion) — WARNUNG: Diese Hilfsfunktion muss in Plan 01 NICHT definiert sein. Definiere sie jetzt als zusaetzliche Export-Funktion:**

Erweitere `services/anonymization.py` um eine Funktion ganz am Ende (nach `register_briefing_pii()`):

```python
def get_pipeline_status() -> str:
    """Gibt pipeline_status zurueck fuer /api/health Endpoint.
    'ok': Pipeline verfuegbar und keine erhoehlte Fehlerrate
    'degraded': Rolling-Error-Rate ueber Schwellenwert (D-08 Kat. C)
    'unavailable': spaCy nicht ladbar (D-08 Kat. A)
    """
    global is_pipeline_healthy, _error_timestamps
    if not is_pipeline_healthy:
        return 'unavailable'
    import time
    now = time.monotonic()
    with _error_lock:
        cutoff = now - ROLLING_ERROR_WINDOW_S
        recent_errors = sum(1 for ts in _error_timestamps if ts >= cutoff)
    if recent_errors > ROLLING_ERROR_THRESHOLD:
        return 'degraded'
    return 'ok'
```

**Dann in app_routes.py api_health():**

Fuege VOR dem `return jsonify({...})` Block ein:

```python
    # Phase 08.23.2.B: Anonymisierungs-Pipeline-Status (D-08)
    try:
        from services.anonymization import get_pipeline_status
        pipeline_status = get_pipeline_status()
    except Exception:
        pipeline_status = 'unknown'
```

Erweitere den bestehenden `return jsonify({})` Block um `pipeline_status`:

VORHER:
```python
    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
    })
```

NACHHER:
```python
    return jsonify({
        'status': 'ok',
        'backup_status': backup_status,
        'backup_age_hours': age_hours,
        'pipeline_status': pipeline_status,   # D-08: 'ok' | 'degraded' | 'unavailable'
    })
```

WICHTIG: Nur die zwei definierten Aenderungen. Kein anderer Code in api_health() aendern.
  </action>

  <verify>
    <automated>cd "C:/Users/andre/dev/salesnerve" && python -c "import ast; ast.parse(open('routes/app_routes.py').read()); import ast; ast.parse(open('services/anonymization.py').read()); print('Syntax OK')" && grep -n "pipeline_status" routes/app_routes.py</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('routes/app_routes.py').read())"` — kein SyntaxError
    - `python -c "import ast; ast.parse(open('services/anonymization.py').read())"` — kein SyntaxError
    - `grep -n "pipeline_status" routes/app_routes.py` — Treffer in api_health()
    - `grep -n "get_pipeline_status" services/anonymization.py` — Treffer (neue Funktion)
    - `grep -n "pipeline_status" routes/app_routes.py` gibt Zeile mit `'pipeline_status': pipeline_status` zurueck
    - Bestehende Felder (backup_status, backup_age_hours) noch vorhanden
  </acceptance_criteria>

  <done>/api/health gibt pipeline_status='ok'|'degraded'|'unavailable' zurueck; get_pipeline_status() in anonymization.py exportiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP POST /api/session-rating -> DB | User-Freitext-Kommentar ohne SID-Kontext (Token-Cache geloescht) |
| GET /api/health -> Dashboard | Pipeline-Status darf keine PII enthalten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.B-AR-01 | Information Disclosure | User-Kommentar ohne SID-Cache in DB | mitigate | anonymize(comment, None): Regex+NER-Modus ohne Mapping-Persistenz; quality_tier='B' |
| T-08.23.2.B-AR-02 | Information Disclosure | Art-9 im User-Kommentar | mitigate | '[ART9_REDACTED]' -> leerer Kommentar (nicht persistieren) |
| T-08.23.2.B-AR-03 | Information Disclosure | /api/health exponiert sensible Infos | accept | pipeline_status ist nur 'ok'/'degraded'/'unavailable' — kein PII |
</threat_model>

<verification>
Nach beiden Tasks:

1. Syntax-Check routes: `python -c "import ast; ast.parse(open('routes/app_routes.py').read())"` — kein SyntaxError
2. Syntax-Check anonymization: `python -c "import ast; ast.parse(open('services/anonymization.py').read())"` — kein SyntaxError
3. pipeline_status in Health: `grep -n "pipeline_status" routes/app_routes.py` — Treffer
4. get_pipeline_status Export: `grep -n "get_pipeline_status" services/anonymization.py` — Treffer
5. Kommentar-Anonymisierung: `grep -n "_comment_anon" routes/app_routes.py` — Treffer
</verification>

<success_criteria>
- /api/session-rating: latest.kommentar = anonymize(comment, None) Ergebnis (cache=None ephemerer Modus)
- /api/health: pipeline_status-Feld in JSON-Response
- get_pipeline_status() in anonymization.py als neue Exportfunktion
- Art-9-Treffer in Kommentar -> leerer Kommentar (nicht persistiert)
- Pipeline-Unavailable in Kommentar -> leerer Kommentar (fail-safe)
</success_criteria>

<output>
Nach Abschluss Summary erstellen: `.planning/phases/08.23.2.B-anonymisierungs-strecke-vor-mitschrift-schreibungen/08.23.2.B-07-SUMMARY.md`

Summary muss enthalten:
- Exakte Zeilennummern der Aenderungen in app_routes.py
- Ergebnis der Pre-Insert-Audit (Pitfall-4-Loesung dokumentiert)
- get_pipeline_status() Rueckgabewerte dokumentiert
</output>
