---
phase: 08.9
plan: "04"
type: execute
wave: 3
depends_on:
  - "08.9-02"
files_modified:
  - routes/profiles.py
autonomous: true
requirements:
  - H-25

must_haves:
  truths:
    - "wizard_create() gibt 403 zurueck wenn _rolle() nicht 'owner' oder 'admin' ist"
    - "aktivieren() gibt 403 zurueck wenn _rolle() nicht 'owner' oder 'admin' ist"
    - "api_faqs_create() gibt 403 zurueck wenn _rolle() nicht 'owner' oder 'admin' ist"
    - "api_tabu_update() gibt 403 zurueck wenn _rolle() nicht 'owner' oder 'admin' ist"
    - "api_faqs_update() und api_faqs_delete() haben bereits Org-Isolation (p.org_id == g.org.id) und kein neuer Rollen-Check noetig (sie arbeiten auf FAQ-Ebene, nicht Profil-Ebene)"
  artifacts:
    - path: "routes/profiles.py"
      provides: "_rolle()-Checks in wizard_create, aktivieren, api_faqs_create, api_tabu_update"
      contains: "if _rolle() not in ('owner', 'admin')"
  key_links:
    - from: "routes/profiles.py:wizard_create"
      to: "_rolle()"
      via: "if _rolle() not in ('owner', 'admin'): return jsonify({'error': 'Keine Berechtigung'}), 403"
      pattern: "_rolle\\(\\).*owner.*admin.*wizard_create"
    - from: "routes/profiles.py:aktivieren"
      to: "_rolle()"
      via: "if _rolle() not in ('owner', 'admin'): return jsonify({'error': 'Keine Berechtigung'}), 403"
      pattern: "_rolle\\(\\).*owner.*admin.*aktivieren"
---

<objective>
H-25 Rollen-Checks: _rolle() in wizard_create, aktivieren, api_faqs_create und api_tabu_update einbauen.

Purpose: Vier Mutations-Endpunkte hatten keinen Rollen-Check. Jeder eingeloggte User (auch 'member') konnte Profile anlegen, aktivieren, FAQs erstellen und Tabu-Woerter setzen. Das widerspricht der org-weiten Permission-Strategie (owner/admin = Mutations-Recht). Andere Mutations (neu, bearbeiten, loeschen, skript_erstellen, opener_erstellen) haben diesen Check bereits.

Output: 4 Endpunkte mit konsistenten _rolle()-Guards, Muster analog zu bearbeiten() und loeschen().
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-02-SUMMARY.md
</context>

<interfaces>
<!-- Bestehendes Muster fuer POST-Mutations in profiles.py (z.B. bearbeiten, Zeile 56-58): -->
```python
    if _rolle() not in ('owner', 'admin'):
        flash('Keine Berechtigung.', 'error')
        return redirect(url_for('profiles.liste'))
```

<!-- Bestehendes Muster fuer JSON-API-Mutations (z.B. skript_erstellen, Zeile 251-252): -->
```python
    if _rolle() not in ('owner', 'admin'):
        return jsonify({'error': 'Keine Berechtigung'}), 403
```

<!-- _rolle() Implementierung (Zeile 33-34): -->
```python
def _rolle():
    return flask_session.get('rolle', 'member')
```

<!-- wizard_create gibt redirect zurueck (kein JSON) → flash+redirect Pattern -->
<!-- aktivieren gibt jsonify zurueck → jsonify({'error': ...}), 403 Pattern -->
<!-- api_faqs_create gibt jsonify zurueck → jsonify({'error': ...}), 403 Pattern -->
<!-- api_tabu_update gibt jsonify zurueck → jsonify({'error': ...}), 403 Pattern -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: H-25 — _rolle()-Checks in wizard_create, aktivieren, api_faqs_create, api_tabu_update</name>
  <files>routes/profiles.py</files>

  <read_first>
    routes/profiles.py (Zeilen 91-143 — wizard_create; Zeilen 187-210 — aktivieren; Zeilen 443-472 — api_faqs_create; Zeilen 530-578 — api_tabu_update)
  </read_first>

  <action>
In routes/profiles.py vier Funktionen um _rolle()-Check erweitern:

**1. wizard_create() — am Anfang der Funktion (nach Zeile 93 `def wizard_create():`):**

AKTUELL (Zeile 94): direkt `firma = request.form.get(...)` ohne Rollen-Check.

EINFUEGEN nach `def wizard_create():` und dem Docstring, VOR den request.form.get-Aufrufen:
```python
    if _rolle() not in ('owner', 'admin'):
        flash('Keine Berechtigung.', 'error')
        return redirect(url_for('profiles.liste'))
```

Begruendung flash+redirect statt jsonify: wizard_create ist ein HTML-Form-Endpunkt (non-AJAX), return redirect ist konsistent mit neu() und bearbeiten().

**2. aktivieren() — am Anfang der Funktion (nach `def aktivieren(pid):`):**

AKTUELL (Zeile 190): direkt `db = get_session()` ohne Rollen-Check.

EINFUEGEN nach `def aktivieren(pid):`, VOR `db = get_session()`:
```python
    if _rolle() not in ('owner', 'admin'):
        return jsonify({'error': 'Keine Berechtigung'}), 403
```

Begruendung jsonify statt redirect: aktivieren() wird per POST aufgerufen und gibt JSON zurueck (siehe return jsonify({'error': 'not found'}), 404 weiter unten).

**3. api_faqs_create() — am Anfang der Funktion nach den Validierungschecks, VOR _require_own_profile:**

AKTUELL (Zeile 457): `p, db = _require_own_profile(profile_id)` ohne Rollen-Check.

EINFUEGEN nach dem letzten Validierungscheck (`if kategorie not in _FAQ_KATEGORIEN: return ...`), VOR `p, db = _require_own_profile(profile_id)`:
```python
    if _rolle() not in ('owner', 'admin'):
        return jsonify({'error': 'Keine Berechtigung'}), 403
```

**4. api_tabu_update() — am Anfang der Funktion, VOR _require_own_profile:**

AKTUELL (ca. Zeile 560): `p, db = _require_own_profile(profile_id)` ohne Rollen-Check.

EINFUEGEN nach der Validierungsschleife (nach `if len(valid) >= _MAX_TABU_COUNT: break`), VOR `p, db = _require_own_profile(profile_id)`:
```python
    if _rolle() not in ('owner', 'admin'):
        return jsonify({'error': 'Keine Berechtigung'}), 403
```

**Was NICHT geaendert wird:**
- api_faqs_update() und api_faqs_delete(): Diese nutzen bereits eine implizite Org-Isolation via `p.org_id != g.org.id` Check. Da FAQs pro FAQ-ID abgerufen werden (nicht pro profile_id wie create/tabu), gibt es kein Profil-Ebene-Rollen-Problem — das Org-Check genuegt. KEINE Aenderung noetig.
- api_faqs_list() (GET): Read-Only, kein Rollen-Check noetig.
  </action>

  <verify>
    <automated>python -c "
import ast
src = open('routes/profiles.py').read()
ast.parse(src)
# Alle 4 Funktionen muessen den _rolle()-Check enthalten
checks = [
    ('wizard_create', \"_rolle() not in ('owner', 'admin')\"),
    ('aktivieren', \"_rolle() not in ('owner', 'admin')\"),
    ('api_faqs_create', \"_rolle() not in ('owner', 'admin')\"),
    ('api_tabu_update', \"_rolle() not in ('owner', 'admin')\"),
]
# Simplifiziert: pruefe nur dass Gesamtanzahl der _rolle()-Checks gestiegen ist
count = src.count(\"_rolle() not in ('owner', 'admin')\")
print(f'_rolle()-Checks gefunden: {count}')
assert count >= 6, f'Erwartet mind. 6 (2 bestehend + 4 neu), gefunden {count}'
print('OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "_rolle() not in" routes/profiles.py | wc -l → mindestens 6 Treffer (2 bestehende in neu/loeschen + 4 neue)
    - grep -n "_rolle() not in" routes/profiles.py → Treffer in wizard_create, aktivieren, api_faqs_create, api_tabu_update
    - python -c "import ast; ast.parse(open('routes/profiles.py').read()); print('OK')" → "OK"
    - pytest tests/ -q exits 0 (266 passing — unveraendert)
  </acceptance_criteria>

  <done>4 Endpunkte haben _rolle()-Check. member-User erhalten 403 bei Mutations-Versuchen auf wizard_create, aktivieren, api_faqs_create, api_tabu_update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Authenticated User → Mutation-Endpoints | Eingeloggter User beliebiger Rolle → wizard_create/aktivieren/FAQ-Create/Tabu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.9-08 | Elevation of Privilege | wizard_create ohne Rollen-Check | mitigate | _rolle() not in ('owner', 'admin') → 403. Member kann keine neuen Profile anlegen. |
| T-08.9-09 | Elevation of Privilege | aktivieren ohne Rollen-Check | mitigate | _rolle() not in ('owner', 'admin') → 403. Member kann kein Profil als aktiv setzen (beeinflusst den Live-Loop). |
| T-08.9-10 | Elevation of Privilege | api_faqs_create ohne Rollen-Check | mitigate | _rolle() not in ('owner', 'admin') → 403. Member kann keine FAQ-Eintraege anlegen. |
| T-08.9-11 | Elevation of Privilege | api_tabu_update ohne Rollen-Check | mitigate | _rolle() not in ('owner', 'admin') → 403. Member kann keine Tabu-Woerter setzen (wuerde Live-QA-Filter beeinflussen). |
| T-08.9-12 | Spoofing | flask_session['rolle'] Manipulation | accept | Session ist server-seitig (Flask secret_key geschuetzt). Kein JWT mit Client-Side Rolle. |
</threat_model>

<verification>
Nach Task 1:

```bash
pytest tests/ -q
```

Erwartetes Ergebnis: 266 passing (oder aktueller Stand + kein Rueckgang).

```bash
grep -n "_rolle() not in" routes/profiles.py
```
Erwartetes Ergebnis: Mindestens 6 Zeilen, darunter wizard_create, aktivieren, api_faqs_create, api_tabu_update.

git add routes/profiles.py
git commit -m "fix(08.9-04): H-25 _rolle()-Checks fuer wizard_create + aktivieren + api_faqs_create + api_tabu_update"
git push origin main
</verification>

<success_criteria>
- routes/profiles.py: 4 neue _rolle()-Checks eingefuegt
- Konsistentes Muster mit bestehenden Mutations (neu, bearbeiten, loeschen)
- pytest unveraendert gruen (kein Rueckgang)
- git commit + push auf main
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-04-SUMMARY.md` erstellen.
</output>
