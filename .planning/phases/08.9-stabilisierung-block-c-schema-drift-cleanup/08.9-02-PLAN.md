---
phase: 08.9
plan: "02"
type: execute
wave: 1
depends_on:
  - "08.9-01"
files_modified:
  - routes/profiles.py
autonomous: true
requirements:
  - HSR-2

must_haves:
  truths:
    - "wizard_create() speichert daten als {'basis': {'produktbeschreibung': ..., 'zielkunden': ..., 'einwaende': [...], 'phasen': []}, 'ki': {'anrede': 'Sie'}}"
    - "qa_pipeline.py kann profile_data['basis']['tabu_begriffe'] lesen ohne KeyError"
    - "wizard_create() legt kein Top-Level 'firma'/'produkt'/'rolle'/'zielkunden' mehr an"
  artifacts:
    - path: "routes/profiles.py"
      provides: "wizard_create mit basis.*-Schema"
      contains: "'basis':"
  key_links:
    - from: "routes/profiles.py:wizard_create"
      to: "services/qa_pipeline.py:generate_qa_response"
      via: "profile_data['basis']['produktbeschreibung']"
      pattern: "basis.*produktbeschreibung"
---

<objective>
wizard_create() in profiles.py auf basis.*-Schema angleichen — konsistent mit BRANCHE_TEMPLATES (Plan 01) und qa_pipeline.py Read-Pfad.

Purpose: Nach Plan 01 haben Onboarding-Templates das basis.*-Schema. Wizard-Create erzeugte bisher Flat-Profile mit Top-Level firma/produkt/zielkunden/rolle/einwaende. Das ist inkompatibel mit qa_pipeline.py:375 (`basis = (daten.get('basis') or {})`). Nach diesem Plan schreiben alle drei Profile-Erzeugungswege (neu/bearbeiten, Template, Wizard) Schema-konform.

Output: wizard_create() in profiles.py schreibt {'basis': {...}, 'ki': {...}} statt Flat-Dict.
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-01-SUMMARY.md
</context>

<interfaces>
<!-- qa_pipeline.py liest profile_data so (Zeile 371-379): -->
```python
daten = profile_data.get('daten') or profile_data or {}
if not isinstance(daten, dict):
    daten = {}
basis = (daten.get('basis') or {}) if isinstance(daten, dict) else {}
tabu_raw = basis.get('tabu_begriffe') or []
```

<!-- api_tabu_update schreibt so (profiles.py:571-572): -->
```python
if not isinstance(pdata.get('basis'), dict):
    pdata['basis'] = {}
pdata['basis']['tabu_begriffe'] = valid
```

<!-- aktivieren() liest daten so (profiles.py:198): -->
```python
daten = json.loads(p.daten) if p.daten else {}
ls_mod.set_active_profile(p.name, daten, profile_id=p.id)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: wizard_create() auf basis.*-Schema umstellen</name>
  <files>routes/profiles.py</files>

  <read_first>
    routes/profiles.py (Zeilen 91-142 — wizard_create Funktion vollstaendig lesen)
    services/qa_pipeline.py (Zeilen 365-380 — profile_data Read-Pfad bestaetigen)
  </read_first>

  <action>
In routes/profiles.py, Funktion wizard_create() (Zeilen 91-142): Das daten-Dict von Flat-Schema auf basis.*-Schema umstellen.

AKTUELLER CODE (Zeilen 115-121):
```python
    daten = json.dumps({
        'firma': firma,
        'produkt': produkt,
        'zielkunden': zielkunden,
        'rolle': rolle,
        'einwaende': einwaende_list,
    }, ensure_ascii=False)
```

ZIELCODE:
```python
    daten = json.dumps({
        'basis': {
            'produktbeschreibung': produkt,
            'zielkunden': zielkunden,
            'einwaende': einwaende_list,
            'phasen': [],
        },
        'ki': {
            'anrede': 'Sie',
        },
        'meta': {
            'firma': firma,
            'rolle': rolle,
        },
    }, ensure_ascii=False)
```

Begruendung der Strukturentscheidungen:
- 'produktbeschreibung' statt 'produkt': konsistent mit BRANCHE_TEMPLATES-Migration (Plan 01) und qa_pipeline-Lesepfad
- 'phasen': [] leer: Wizard hat kein Phasen-Schritt, leere Liste ist korrekter Default
- 'ki': {'anrede': 'Sie'}: Default-Anrede fuer QA-Pipeline (konservativ Sie)
- 'meta': {'firma', 'rolle'}: firma und rolle sind Metadaten, kein qa_pipeline-Input — separater Namespace verhindert Key-Kollisionen

Der Profilname (Zeile 127: `name=firma if firma else 'Mein Profil'`) bleibt unveraendert.
Alles andere in wizard_create() (flush, active_profile_id, commit, flash, redirect) bleibt unveraendert.
  </action>

  <verify>
    <automated>python -c "
import json, sys
# Simulate wizard_create logic with test data
firma = 'Testfirma GmbH'
produkt = 'CRM-Software'
zielkunden = 'Mittelstand'
rolle = 'Vertriebsleiter'
einwaende_list = ['Zu teuer', 'Kein Bedarf']
daten = json.dumps({
    'basis': {
        'produktbeschreibung': produkt,
        'zielkunden': zielkunden,
        'einwaende': einwaende_list,
        'phasen': [],
    },
    'ki': {'anrede': 'Sie'},
    'meta': {'firma': firma, 'rolle': rolle},
}, ensure_ascii=False)
d = json.loads(daten)
assert 'basis' in d, 'basis fehlt'
assert 'produktbeschreibung' in d['basis'], 'produktbeschreibung fehlt'
assert d['basis']['einwaende'] == einwaende_list, 'einwaende falsch'
assert 'produkt' not in d, 'altes Top-Level produkt vorhanden'
assert 'firma' not in d, 'altes Top-Level firma vorhanden'
print('OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "'basis':" routes/profiles.py → Treffer in wizard_create (Zeile ca. 115-125)
    - grep -n "'produktbeschreibung'" routes/profiles.py → Treffer in wizard_create
    - grep -n "'phasen': \[\]" routes/profiles.py → Treffer in wizard_create
    - grep -n "daten = json.dumps({" routes/profiles.py | head -1 → der zugehoerige Block hat 'basis' als ersten Key
    - python -c "import ast; ast.parse(open('routes/profiles.py').read()); print('OK')" → "OK"
    - pytest tests/ -q exits 0 (265 passing)
  </acceptance_criteria>

  <done>wizard_create() schreibt {'basis': {'produktbeschreibung', 'zielkunden', 'einwaende', 'phasen'}, 'ki': {'anrede'}, 'meta': {'firma', 'rolle'}}. Kein Flat-Schema mehr.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Wizard-Form → wizard_create() | Form-Daten: firma, produkt, zielkunden, rolle, einwaende (JSON-Liste) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.9-04 | Tampering | wizard_create daten-JSON | accept | Bestehende Input-Validierung (strip(), json.loads mit Fallback []) unveraendert erhalten. Neues Schema aendert nur Struktur, nicht Validierungslogik. |
| T-08.9-05 | Information Disclosure | meta.firma / meta.rolle | accept | firma und rolle sind User-eigene Eingaben, kein Fremdkontext. Gespeichert in profiles.daten (org-isoliert). |
</threat_model>

<verification>
Nach Task 1:

```bash
pytest tests/ -q
```

Erwartetes Ergebnis: 265 passed.

Manuelle Stichprobe:
```bash
python -c "
import ast, sys
src = open('routes/profiles.py').read()
# Sicherstellen dass 'basis' im wizard_create-Bereich vorkommt
tree = ast.parse(src)
print('Syntax OK')
"
```

git add routes/profiles.py
git commit -m "feat(08.9-02): wizard_create auf basis.*-Schema umgestellt (HSR-2)"
git push origin main
</verification>

<success_criteria>
- routes/profiles.py: wizard_create() schreibt daten mit basis-Schluesselebene
- Kein Top-Level firma/produkt/zielkunden/rolle mehr in wizard_create-Output
- pytest 265 passing
- git commit + push auf main
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-02-SUMMARY.md` erstellen.
</output>
