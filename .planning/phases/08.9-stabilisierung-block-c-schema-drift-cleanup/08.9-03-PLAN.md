---
phase: 08.9
plan: "03"
type: execute
wave: 2
depends_on:
  - "08.9-01"
  - "08.9-02"
files_modified:
  - services/claude_service.py
  - tests/test_08_5_03_integration.py
autonomous: true
requirements:
  - LB-3
  - WR-01
  - WR-03

must_haves:
  truths:
    - "_qa_pipeline_dispatch uebergibt profile_data aus live_session.get_active_profile() an generate_qa_response — nicht {}"
    - "confidence wird als float(float-Wert) uebergeben — kein leerer String, kein TypeError"
    - "user_id wird als keyword-Argument user_id=_user_id uebergeben — nicht als positionelles version-Argument"
    - "Alle 265 Tests bleiben gruen — kein neuer pytest-Fehler durch den Fix"
    - "test_08_5_03_integration.py Test 6 (einwand_unknown_high_conf_emits_slot1) patcht generate_qa_response mit korrekten Argument-Erwartungen"
  artifacts:
    - path: "services/claude_service.py"
      provides: "_qa_pipeline_dispatch mit korrektem generate_qa_response-Aufruf"
      contains: "generate_qa_response(neuer_text, 'einwand_unknown', _profile_daten"
    - path: "tests/test_08_5_03_integration.py"
      provides: "WR-01 Test-Fix fuer generate_qa_response Argument-Reihenfolge"
      contains: "profile_data=_profile_daten"
  key_links:
    - from: "services/claude_service.py:_qa_pipeline_dispatch"
      to: "services/qa_pipeline.py:generate_qa_response"
      via: "generate_qa_response(neuer_text, kategorie, _profile_daten, _anrede, confidence=float(_conf), user_id=_user_id)"
      pattern: "generate_qa_response.*_profile_daten.*confidence=float"
    - from: "services/claude_service.py:_qa_pipeline_dispatch"
      to: "services/live_session.py:get_active_profile"
      via: "_ls_ref.get_active_profile() → _profile_daten"
      pattern: "_profile_daten"
---

<objective>
LB-3 Komplett-Fix: _qa_pipeline_dispatch() in claude_service.py uebergibt profile_data korrekt an generate_qa_response, confidence als float, user_id als keyword-Argument. WR-01 Test-Argumente in test_08_5_03_integration.py korrigieren.

Purpose: Drei Bugs in einem Aufruf — profile_data={} (leer statt echte Profildaten), confidence='' (ValueError), user_id wird als version uebergeben. generate_qa_response kann ohne profile_data keine Tabu-Regeln anwenden und liefert inhaltlich schlechtere Antworten. Nach dem Fix fliesst der vollstaendige Profilkontext durch die QA-Pipeline.

Output: Zwei generate_qa_response-Aufrufe in _qa_pipeline_dispatch gefixt (einwand_unknown + frage-Branch), Test WR-01 korrigiert.
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-01-SUMMARY.md
@.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-02-SUMMARY.md
</context>

<interfaces>
<!-- generate_qa_response Signatur (qa_pipeline.py:353-355): -->
```python
def generate_qa_response(utterance: str, category: str, profile_data: dict,
                         anrede: str, confidence: float = 1.0,
                         version: str = '', user_id: int = 0) -> str:
```

<!-- _qa_pipeline_dispatch laedt bereits _profile_daten (claude_service.py:1142-1145): -->
```python
import services.live_session as _ls_ref
try:
    _profile_name, _profile_daten = _ls_ref.get_active_profile()
except Exception:
    _profile_daten = {}
```

<!-- BUGGY Aufruf 1 (claude_service.py:1179-1181): -->
```python
_antwort = generate_qa_response(
    neuer_text, 'einwand_unknown', {}, _anrede, '', _user_id
)
```
# Problem: profile_data={}, confidence='' (5. Arg), _user_id als version (6. Arg)

<!-- BUGGY Aufruf 2 (claude_service.py:1219-1221): -->
```python
_antwort = generate_qa_response(
    neuer_text, 'frage', {}, _anrede, '', _user_id
)
```
# Identische Bugs

<!-- get_active_profile Rueckgabe (live_session.py:198-200): -->
```python
def get_active_profile():
    with active_profile_lock:
        return active_profile_name, dict(active_profile_data)
```
# Gibt (str, dict) zurueck — _profile_daten ist also ein dict
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WR-03 + profile_data-Fix — generate_qa_response Aufrufe in _qa_pipeline_dispatch korrigieren</name>
  <files>services/claude_service.py</files>

  <read_first>
    services/claude_service.py (Zeilen 1096-1232 — vollstaendige _qa_pipeline_dispatch Funktion lesen)
    services/qa_pipeline.py (Zeilen 353-360 — generate_qa_response Signatur bestaetigen)
  </read_first>

  <action>
In services/claude_service.py zwei generate_qa_response-Aufrufe reparieren:

**AUFRUF 1 (einwand_unknown-Branch, ca. Zeile 1179-1181):**

AKTUELL (fehlerhaft):
```python
                _antwort = generate_qa_response(
                    neuer_text, 'einwand_unknown', {}, _anrede, '', _user_id
                )
```

ZIEL:
```python
                _antwort = generate_qa_response(
                    neuer_text, 'einwand_unknown', _profile_daten, _anrede,
                    confidence=float(_conf), user_id=_user_id
                )
```

**AUFRUF 2 (frage-Branch, ca. Zeile 1219-1221):**

AKTUELL (fehlerhaft):
```python
                    _antwort = generate_qa_response(
                        neuer_text, 'frage', {}, _anrede, '', _user_id
                    )
```

ZIEL:
```python
                    _antwort = generate_qa_response(
                        neuer_text, 'frage', _profile_daten, _anrede,
                        confidence=float(_conf), user_id=_user_id
                    )
```

**Warum float(_conf) und nicht direkt _conf:**
_conf kommt aus `_qa_result.get('confidence', 0.0)` — theoretisch float, aber defensiv float() wrappen fuer den Fall dass classify_utterance einen String zurueckgibt.

**Warum keyword-Argumente:**
generate_qa_response-Signatur hat confidence als 5. Positional-Arg und user_id als 7. Positional-Arg mit version (str) dazwischen. Keyword-Notation macht den Aufruf robust gegen zukuenftige Signatur-Aenderungen.

Kein anderer Code in dieser Funktion wird veraendert. _profile_daten ist bereits definiert (Zeilen 1142-1145, wird aus _ls_ref.get_active_profile() geladen).
  </action>

  <verify>
    <automated>python -c "
import ast
src = open('services/claude_service.py').read()
ast.parse(src)
# Sicherstellen dass beide fehlerhaften Aufrufe nicht mehr vorhanden
assert \"generate_qa_response(\n                    neuer_text, 'einwand_unknown', {}, _anrede, '', _user_id\" not in src, 'Bug 1 noch vorhanden'
assert \"generate_qa_response(\n                        neuer_text, 'frage', {}, _anrede, '', _user_id\" not in src, 'Bug 2 noch vorhanden'
# Sicherstellen dass _profile_daten in den Aufrufen vorkommt
assert '_profile_daten' in src, '_profile_daten nicht im Aufruf'
print('OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "generate_qa_response" services/claude_service.py → beide Aufrufe haben _profile_daten als 3. Argument (nicht {})
    - grep -n "confidence=float(_conf)" services/claude_service.py → mindestens 2 Treffer (einwand_unknown + frage Branch)
    - grep -n "user_id=_user_id" services/claude_service.py → mindestens 2 Treffer
    - grep -n "'einwand_unknown', {}" services/claude_service.py → kein Treffer (Bug 1 entfernt)
    - grep -n "'frage', {}" services/claude_service.py → kein Treffer (Bug 2 entfernt)
    - python -c "import ast; ast.parse(open('services/claude_service.py').read()); print('OK')" → "OK"
    - pytest tests/test_08_5_03_integration.py -q → Tests laufen durch (auch wenn einzelne mocken muessen angepasst werden)
  </acceptance_criteria>

  <done>Beide generate_qa_response-Aufrufe uebergeben _profile_daten, confidence als float, user_id als keyword-Argument.</done>
</task>

<task type="auto">
  <name>Task 2: WR-01 — Test-Argumente in test_08_5_03_integration.py korrigieren</name>
  <files>tests/test_08_5_03_integration.py</files>

  <read_first>
    tests/test_08_5_03_integration.py (vollstaendig lesen — alle generate_qa_response patch-Aufrufe finden)
  </read_first>

  <action>
In tests/test_08_5_03_integration.py: generate_qa_response wird in mehreren Tests via patch() gemockt. Die Mocks prufen keine Argument-Reihenfolge explizit (sie verwenden return_value=..., nicht side_effect mit Arg-Checks), daher sind keine Argument-Assertions zu korrigieren.

**Was jedoch zu pruefen ist:** Ob irgendein Test `assert_called_with(...)` oder `assert_called_once_with(...)` auf generate_qa_response verwendet, das die alten (falschen) Argumente {} und '' prueft.

Datei lesen und alle Stellen mit `assert_called` auf generate_qa_response suchen.

Falls gefunden: Die expected-Argumente auf die korrekten Werte anpassen:
- 3. Argument: beliebiges dict (nicht {}) — mit `unittest.mock.ANY` oder dem konkreten Mock-Profil-Dict
- 5. Argument: float (confidence=...) statt ''
- 6/7. Argument: user_id=... statt positionell

Falls KEINE assert_called-Checks auf generate_qa_response vorhanden sind: Kommentar in der Datei einfuegen dass WR-01 geprueft und kein expliziter Argument-Check gefunden wurde.

**Verpflichtend einzufuegen** (unabhaengig von assert_called-Checks): In der Klasse TestAnalyseLoopDispatcher einen neuen Test hinzufuegen der sicherstellt dass generate_qa_response NICHT mit leerem Dict aufgerufen wird:

```python
    def test_einwand_unknown_passes_profile_data_not_empty(self):
        """WR-01 Regression: generate_qa_response muss _profile_daten erhalten, nicht {}."""
        dispatch = self._get_dispatch_fn()
        ls, sio = _build_qa_dispatch_context(line_id='line-20', kw_fired_for_line=None)
        captured_calls = []
        def _capture_gen(*args, **kwargs):
            captured_calls.append({'args': args, 'kwargs': kwargs})
            return 'Testantwort'
        
        with patch('services.qa_pipeline.classify_utterance',
                   return_value={'kategorie': 'einwand_unknown', 'confidence': 0.95}), \
             patch('services.qa_pipeline.generate_qa_response',
                   side_effect=_capture_gen), \
             patch('services.qa_pipeline.apply_tabu_filter', return_value=False), \
             patch('services.claude_service._qa_load_tabu', return_value=[]), \
             patch('services.live_session.get_active_profile',
                   return_value=('TestProfil', {'basis': {'produktbeschreibung': 'CRM'}})):
            dispatch('Das ist zu teuer', 'line-20', '', ls, sio)
        
        self.assertTrue(len(captured_calls) > 0, "generate_qa_response wurde nicht aufgerufen")
        call_args = captured_calls[0]['args']
        # 3. Argument muss ein nicht-leeres Dict sein (nicht {})
        profile_data_arg = call_args[2]
        self.assertIsInstance(profile_data_arg, dict, "profile_data muss dict sein")
        self.assertNotEqual(profile_data_arg, {}, "profile_data darf nicht leer sein (WR-01)")
```

Diesen Test nach test_einwand_unknown_high_conf_emits_slot1 (Test 6) einsetzen.
  </action>

  <verify>
    <automated>pytest tests/test_08_5_03_integration.py -v -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - pytest tests/test_08_5_03_integration.py -v → test_einwand_unknown_passes_profile_data_not_empty PASSED
    - pytest tests/test_08_5_03_integration.py -v → alle bestehenden 9 Tests PASSED (kein neuer FAILED)
    - pytest tests/ -q exits 0 (265 passing + neuer WR-01 Regressionstest = 266 passing oder mehr)
    - grep -n "WR-01" tests/test_08_5_03_integration.py → mindestens 1 Treffer (Kommentar oder Testname)
  </acceptance_criteria>

  <done>WR-01 Regressionstest eingefuegt, alle bestehenden Tests weiterhin gruen. pytest Gesamtzahl um 1 gestiegen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| live_session → _qa_pipeline_dispatch | Profildaten aus In-Memory-State (trusted, eigene App) |
| _qa_pipeline_dispatch → generate_qa_response | Interne Funktion, alle Args aus kontrolliertem State |

## STRIDE Threat Register

| Threat ID | Category | Componente | Disposition | Mitigation Plan |
|-----------|----------|------------|-------------|-----------------|
| T-08.9-06 | Information Disclosure | _profile_daten in generate_qa_response | accept | Profildaten sind user-eigene Daten (org-isoliert). Der Fix verbessert die Antwortqualitaet, veroeffentlicht keine neuen Daten. |
| T-08.9-07 | Denial of Service | float(_conf) bei unerwartetem Typ | mitigate | float() wirft ValueError bei nicht-konvertierbarem String. Bereits in aeusserem try/except von _qa_pipeline_dispatch abgefangen (Zeile 1230). |
</threat_model>

<verification>
Nach beiden Tasks:

```bash
pytest tests/ -q
```

Erwartetes Ergebnis: 266 passed (265 alt + 1 neuer WR-01 Test).

Stichprobe auf korrekten Aufruf:
```bash
grep -n "generate_qa_response" services/claude_service.py
```
Erwartetes Pattern in den zwei relevanten Zeilen: `_profile_daten, _anrede,` und `confidence=float(_conf), user_id=_user_id`.

git add services/claude_service.py tests/test_08_5_03_integration.py
git commit -m "fix(08.9-03): LB-3 _qa_pipeline_dispatch profile_data+confidence+user_id Fix, WR-01 Regressionstest"
git push origin main
</verification>

<success_criteria>
- services/claude_service.py: beide generate_qa_response-Aufrufe haben _profile_daten (nicht {}), confidence=float(_conf), user_id=_user_id
- tests/test_08_5_03_integration.py: WR-01 Regressionstest eingefuegt, prueft dass profile_data nicht leer ist
- pytest 266 passing (oder mehr, falls pre-existing count hoeher)
- git commit + push auf main
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-03-SUMMARY.md` erstellen.
</output>
