---
phase: 08.9-stabilisierung-block-c-schema-drift-cleanup
fixed_at: 2026-04-25T00:00:00Z
review_path: .planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-REVIEW.md
iteration: 1
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 08.9: Code Review Fix Report

**Fixed at:** 2026-04-25
**Source review:** `.planning/phases/08.9-stabilisierung-block-c-schema-drift-cleanup/08.9-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 3
- Fixed: 3
- Skipped: 0

## Fixed Issues

### WR-01: Demo-Profile Migration Guard — Dual OR-Condition Can Miss Partially-Migrated Rows

**Files modified:** `app.py`
**Commit:** 0c82dc6
**Applied fix:** Added a `print(f"[DB] Migration 08.9: Profil ID {_pid} hat keine Flat-Schema-Keys — uebersprungen")` log line inside the `'produkt' not in _daten and 'einwaende' not in _daten` skip branch (line 753-755), and added a clarifying comment above it documenting the AND semantics. Empty-daten rows are now logged instead of silently skipped.

---

### WR-02: `db.query(User).get()` — Deprecated SQLAlchemy 2.x API in onboarding.py

**Files modified:** `routes/onboarding.py`
**Commit:** 761681f
**Applied fix:** Replaced `db.query(User).get(g.user.id)` with `db.get(User, g.user.id)` at line 192 in `complete()`. This matches the SQLAlchemy 2.x pattern used in `routes/auth.py` and `routes/profiles.py` and eliminates the `AttributeError` on the `/onboarding/complete` endpoint.

---

### WR-03: `create_profile_from_template` — Silently Creates Incomplete Profile on Missing `basis` Key

**Files modified:** `routes/onboarding.py`
**Commit:** da703e8
**Applied fix:** Moved `_basis = template.get('basis', {})` extraction to before the DB write (immediately after the template lookup). Added a defensive `if not _basis:` guard with a `print(f"[Onboarding] create_profile_from_template: no basis in template for branche={branche}")` log line. The `return jsonify(...)` response continues to use `_basis`, which is now consistent with what was stored regardless of DB write order.

---

_Fixed: 2026-04-25_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
