---
phase: "08.9"
plan: "03"
subsystem: qa-pipeline/claude-service
tags: [qa-pipeline, bug-fix, profile-data, regression-test, LB-3, WR-01, WR-03]
dependency_graph:
  requires: [08.9-01, 08.9-02]
  provides: [LB-3-qa-pipeline-profile-data-fix, WR-01-regression-test, WR-03-confidence-float]
  affects: [services/claude_service.py, tests/test_08_5_03_integration.py]
tech_stack:
  added: []
  patterns: [keyword-arg-robustness, regression-test-side-effect-capture]
key_files:
  created: []
  modified:
    - services/claude_service.py
    - tests/test_08_5_03_integration.py
decisions:
  - "LB-3: _qa_pipeline_dispatch beide generate_qa_response-Aufrufe korrigiert — {} → _profile_daten, '' → confidence=float(_conf), positional user_id → user_id=_user_id keyword"
  - "WR-03: float(_conf) wrap defensiv — _conf kommt theoretisch als float aus classify_utterance, aber float() schuetzt gegen String-Rueckgabe bei kuenftigem Refactoring"
  - "WR-01: side_effect=_capture_gen statt return_value — prueft 3. positionales Argument (profile_data) auf Nicht-Leere, kein assert_called_with noetig"
metrics:
  duration: "4 min"
  completed: "2026-04-25"
  tasks_completed: 2
  files_modified: 2
---

# Phase 08.9 Plan 03: LB-3 QA-Pipeline Komplett-Fix + WR-01 Regressionstest Summary

**One-liner:** Beide generate_qa_response-Aufrufe in `_qa_pipeline_dispatch` auf `_profile_daten` + `confidence=float(_conf)` + `user_id=_user_id` korrigiert; WR-01 Regressionstest sichert den Fix gegen Rueckfall ab.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | WR-03 + profile_data-Fix — generate_qa_response Aufrufe korrigieren | 692bf66 | services/claude_service.py |
| 2 | WR-01 — Regressionstest eingefuegt | 6de0916 | tests/test_08_5_03_integration.py |

## What Was Built

### Task 1 — LB-3: generate_qa_response Aufrufe in _qa_pipeline_dispatch

Zwei Aufrufe in `services/claude_service.py` (Funktion `_qa_pipeline_dispatch`) korrigiert:

**Aufruf 1 (einwand_unknown-Branch, Zeile 1179):**
```python
# Vorher (buggy):
_antwort = generate_qa_response(
    neuer_text, 'einwand_unknown', {}, _anrede, '', _user_id
)

# Nachher (korrekt):
_antwort = generate_qa_response(
    neuer_text, 'einwand_unknown', _profile_daten, _anrede,
    confidence=float(_conf), user_id=_user_id
)
```

**Aufruf 2 (frage-Branch, Zeile 1220):**
```python
# Vorher (buggy):
_antwort = generate_qa_response(
    neuer_text, 'frage', {}, _anrede, '', _user_id
)

# Nachher (korrekt):
_antwort = generate_qa_response(
    neuer_text, 'frage', _profile_daten, _anrede,
    confidence=float(_conf), user_id=_user_id
)
```

**Drei gleichzeitig behobene Bugs:**
1. `profile_data={}` → `_profile_daten` aus `_ls_ref.get_active_profile()` (bereits ab Zeile 1142 geladen)
2. `confidence=''` (positional, leerer String → ValueError) → `confidence=float(_conf)` (keyword)
3. `_user_id` als 6. positionales Argument (landet auf `version: str`) → `user_id=_user_id` (keyword, korrekte Stelle)

`_profile_daten` war bereits vor diesem Fix korrekt geladen — nur der Uebergabe-Schritt fehlte.

### Task 2 — WR-01: Regressionstest test_einwand_unknown_passes_profile_data_not_empty

In `tests/test_08_5_03_integration.py` neuen Test nach Test 6 (`test_einwand_unknown_high_conf_emits_slot1`) eingefuegt:

```python
def test_einwand_unknown_passes_profile_data_not_empty(self):
    """WR-01 Regression: generate_qa_response muss _profile_daten erhalten, nicht {}."""
    ...
    patch('services.live_session.get_active_profile',
          return_value=('TestProfil', {'basis': {'produktbeschreibung': 'CRM'}})):
    ...
    profile_data_arg = call_args[2]
    self.assertIsInstance(profile_data_arg, dict, "profile_data muss dict sein")
    self.assertNotEqual(profile_data_arg, {}, "profile_data darf nicht leer sein (WR-01)")
```

`side_effect=_capture_gen` statt `return_value` — ermoeglicht Argument-Inspektion ohne `assert_called_with`, prueft gezielt das 3. positionale Argument (profile_data).

Kein bestehender Test verwendete `assert_called_with` auf `generate_qa_response` mit den alten falschen Argumenten — kein Aufraeumen noetig.

## Verification

```
pytest tests/test_08_5_03_integration.py -v → 10 passed (9 existing + 1 new WR-01)
pytest tests/ -q --ignore=tests/test_ft_seed.py → 266 passed (265 alt + 1 neuer WR-01 Test)
```

Acceptance-Check:
```
grep -n "confidence=float(_conf)" services/claude_service.py → 2 Treffer (Zeile 1181 + 1222)
grep -n "user_id=_user_id" services/claude_service.py → 2 Treffer
grep -n "'einwand_unknown', {}" services/claude_service.py → kein Treffer
grep -n "'frage', {}" services/claude_service.py → kein Treffer
```

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — T-08.9-06 (Information Disclosure) disposition accept: Profildaten sind user-eigene Daten (org-isoliert). T-08.9-07 (DoS via float()) disposition mitigate: float() ValueError wird von aeusserem try/except in _qa_pipeline_dispatch (Zeile 1230) abgefangen.

## Self-Check

- [x] services/claude_service.py enthaelt `_profile_daten, _anrede,\n                    confidence=float(_conf), user_id=_user_id` — FOUND (2x)
- [x] tests/test_08_5_03_integration.py enthaelt `WR-01` — FOUND
- [x] Commit 692bf66 existiert — FOUND
- [x] Commit 6de0916 existiert — FOUND

## Self-Check: PASSED
