---
phase: 08.6-stabilisierung-block-a-quick-wins
fixed_at: 2026-04-24T10:30:00Z
review_path: .planning/phases/08.6-stabilisierung-block-a-quick-wins/08.6-REVIEW.md
iteration: 1
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
---

# Phase 08.6: Code Review Fix Report

**Fixed at:** 2026-04-24T10:30:00Z
**Source review:** .planning/phases/08.6-stabilisierung-block-a-quick-wins/08.6-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 4 (WR-01 through WR-04; Info findings excluded per fix_scope)
- Fixed: 4
- Skipped: 0

## Fixed Issues

### WR-01: ValueError crash on non-numeric form input in PlanningListView

**Files modified:** `routes/admin_views.py`
**Commit:** 002d4b3
**Applied fix:** Wrapped `int(request.form.get('id') or 0)` in a `try/except (ValueError, TypeError)` block that falls back to `link_id = 0`, preventing an unhandled 500 on non-numeric POST input.

### WR-02: Jinja2 HTML entity double-escaping in analytics.html

**Files modified:** `templates/analytics.html`
**Commit:** 5696864
**Applied fix:** Replaced all three `'&mdash;'` string literals inside `{{ ... }}` expressions (lines 21, 22, 26) with the unicode em dash character `'—'` directly, preventing Jinja2 autoescape from encoding the `&` to `&amp;`.

### WR-03: Conflicting display declarations on perf-empty-chart element

**Files modified:** `templates/dashboard.html`
**Commit:** e092889
**Applied fix:** Two-part fix:
1. Removed the duplicate `display:flex` from the `perf-empty-chart` inline style attribute (line 143), leaving only `display:none` so the element starts hidden.
2. Added `if (emptyEl) emptyEl.style.display = 'none';` in `renderPerformance()` before the `if (!p.hat_daten)` check, ensuring the element is always reset to hidden before conditionally showing it.

### WR-04: settings_language silently coerces invalid input instead of returning 400

**Files modified:** `routes/settings.py`
**Commit:** 6d039bc
**Applied fix:** Replaced the silent `lang = 'de'` fallback with `return jsonify({'error': 'Ungültige Sprache'}), 400`, making the language endpoint consistent with `settings_theme` which already returns 400 for invalid values. Error string uses real Umlaute per project conventions.

---

_Fixed: 2026-04-24T10:30:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
