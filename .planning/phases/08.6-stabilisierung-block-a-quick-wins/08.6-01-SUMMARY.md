---
phase: "08.6"
plan: "08.6-01"
title: "Block A Quick-Wins — 8 atomic fixes"
subsystem: "deepgram_service, admin_views, analytics, dashboard, config, settings, organisations"
tags: [bugfix, chore, cors, imports, state, admin, settings]
dependency_graph:
  requires: []
  provides:
    - "ls.state org_id + mode fields for live session tracking"
    - "Correct DB column names in admin ConvLog view and analytics template"
    - "ROI KPI card hidden until implementation complete"
    - "CORS default domain pointing to getnerve.app"
    - "Clean imports in settings.py and organisations.py"
    - "HTTP 400 for invalid theme values in settings API"
    - "Language selection restricted to de/en only"
  affects:
    - "services/deepgram_service.py"
    - "routes/admin_views.py"
    - "templates/analytics.html"
    - "templates/dashboard.html"
    - "config.py"
    - "routes/settings.py"
    - "routes/organisations.py"
tech_stack:
  added: []
  patterns:
    - "HTTP 400 JSON error response for invalid enum values (settings API)"
key_files:
  created: []
  modified:
    - services/deepgram_service.py
    - routes/admin_views.py
    - templates/analytics.html
    - templates/dashboard.html
    - config.py
    - routes/settings.py
    - routes/organisations.py
decisions:
  - "[08.6-01] LB-5+LB-6: org_id and mode written inside existing state_lock block — no new lock needed, both u and mode already in scope"
  - "[08.6-01] LB-12: einwaende_total/einwaende_ok were ghost names; real DB columns are einwaende_gesamt/einwaende_behandelt — no migration needed"
  - "[08.6-01] LB-13: ROI card hidden via inline style (not deleted) — preserves JS reference to id=perf-roi for future implementation"
  - "[08.6-01] CORS: nerve.app -> getnerve.app; FLASK_DEBUG wildcard path unchanged"
  - "[08.6-01] settings.py: 'redirect' on line 203 is a JSON dict key string, not the Flask function — import removal is safe"
  - "[08.6-01] settings_theme: silent coercion replaced with explicit 400 + Umlaut error message per CLAUDE.md user-facing text rule"
  - "[08.6-01] settings_language: allowed list shrunk to ['de', 'en'] — fr/es/it/pt/nl/pl/cs/tr have no app content"
metrics:
  duration: "165s (~3 min)"
  completed_date: "2026-04-24"
  tasks_completed: 8
  files_modified: 7
  commits: 8
---

# Phase 08.6 Plan 01: Block A Quick-Wins Summary

**One-liner:** 8 surgical line-level fixes eliminating launch blockers LB-5/6/12/13, CORS drift, 2 unused imports, invalid-theme 400, and language overreach.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| T-01 | LB-5+LB-6: Write org_id and mode into ls.state | a2de63c | services/deepgram_service.py |
| T-02 | LB-12: Fix ghost column names in admin_views.py + analytics.html | c0ea37a | routes/admin_views.py, templates/analytics.html |
| T-03 | LB-13: Hide ROI KPI card in dashboard.html | 972b060 | templates/dashboard.html |
| T-04 | CORS drift: Fix production domain in config.py | 499509c | config.py |
| T-05 | settings.py: Remove unused redirect and url_for imports | 9cade60 | routes/settings.py |
| T-06 | organisations.py: Remove unused BillingEvent import | 61aee01 | routes/organisations.py |
| T-07 | settings_theme: Return 400 for invalid theme values | f4b90e7 | routes/settings.py |
| T-08 | settings_language: Restrict allowed list to de and en | 5a38721 | routes/settings.py |

## Acceptance Criteria — All Green

| Check | Result |
|-------|--------|
| `grep "einwaende_total\|einwaende_ok" routes/admin_views.py templates/analytics.html` | Empty |
| `grep "ls.state['org_id']" services/deepgram_service.py` | 1 match (line 354) |
| `grep "ls.state['mode']" services/deepgram_service.py` | 1 match (line 355) |
| `grep "db2-perf-kpi.*display:none" templates/dashboard.html` | 1 match (line 130) |
| `grep "perf-roi" templates/dashboard.html` | Element still present |
| `grep "getnerve.app" config.py` | 1 match (line 13) |
| `grep "redirect\|url_for" routes/settings.py` (import line) | Only dict-key string on line 203 |
| `grep "BillingEvent" routes/organisations.py` | Empty |
| `grep "400" routes/settings.py` | Line 142 (settings_theme) |
| `grep "allowed = \[" routes/settings.py` | `['de', 'en']` only |

## Bugs Fixed

- **LB-5**: `ls.state['org_id']` missing — org context unavailable in analyse_loop
- **LB-6**: `ls.state['mode']` missing — session mode unavailable in analyse_loop
- **LB-12**: Admin ConvLog view crashed with `AttributeError` on ghost columns `einwaende_total`/`einwaende_ok`
- **LB-13**: ROI KPI card showed `—` permanently (ROI calc not implemented)
- **CORS drift**: Default CORS origin pointed to `nerve.app` instead of `getnerve.app`
- **Unused imports (x2)**: `redirect, url_for` in settings.py; `BillingEvent` in organisations.py
- **Theme-400**: Invalid theme silently coerced to `'dark'` instead of returning 400
- **Language-Restrict**: 10 language codes accepted, only 2 (de/en) have actual content

## Deviations from Plan

None — plan executed exactly as written.

## Notes

`routes/app_routes.py` contains `einwaende_total` and `einwaende_ok` as JSON dict-key strings
in an API response (aliasing DB columns) and as a local variable name. These are out-of-scope
pre-existing code, unrelated to the DB column name fix in LB-12. Not modified.

## Self-Check

Files modified confirmed present:
- services/deepgram_service.py — contains `ls.state['org_id']` at line 354
- routes/admin_views.py — contains `einwaende_gesamt` at line 96
- templates/analytics.html — contains `s.einwaende_gesamt` at line 24
- templates/dashboard.html — contains `style="display:none"` at line 130
- config.py — contains `getnerve.app` at line 13
- routes/settings.py — clean import line 1, 400 at line 142, `['de', 'en']` at line 158
- routes/organisations.py — no BillingEvent

All 8 commits verified in git log.

## Self-Check: PASSED
