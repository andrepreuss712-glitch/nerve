---
phase: 08.10-stabilisierung-block-b-auth-haertung
plan: 02
subsystem: auth
tags: [flask, session-security, cookie-hardening, proxy-fix, werkzeug]

requires:
  - phase: 08.10-01
    provides: Traceback-Leak-Fix abgeschlossen, app.py bereinigt

provides:
  - ProxyFix ohne x_host (nur x_for+x_proto) mit Security-Kommentar
  - SESSION_COOKIE_SECURE=not _debug (FLASK_DEBUG-aware, env-var-basiert)
  - SESSION_COOKIE_HTTPONLY=True
  - SESSION_COOKIE_SAMESITE=Lax
  - PERMANENT_SESSION_LIFETIME=timedelta(days=14)
  - timedelta importiert in app.py

affects:
  - 08.10-03
  - 08.10-04
  - 08.10-05
  - 08.10-06

tech-stack:
  added: []
  patterns:
    - "FLASK_DEBUG-aware security config via os.environ.get (nicht app.debug — ist unter gunicorn immer False)"
    - "ProxyFix nur x_for+x_proto, kein x_host (fixed-domain, Host-Header-Injection vermieden)"

key-files:
  created: []
  modified:
    - app.py

key-decisions:
  - "08.10-02: x_host=1 aus ProxyFix entfernt — Host-Header-Injection-Vektor bei Fixed-Domain getnerve.app unnoetig und riskant"
  - "08.10-02: _debug via os.environ.get('FLASK_DEBUG') — app.debug ist unter gunicorn immer False (State.md Entscheidung Phase 03)"
  - "08.10-02: SESSION_COOKIE_SECURE=not _debug — False lokal (kein HTTPS), True Prod"
  - "08.10-02: PERMANENT_SESSION_LIFETIME=timedelta(days=14) — SESSION_PERMANENT=True bereits vorhanden, greift korrekt"

requirements-completed: [LB-10]

duration: 5min
completed: 2026-04-25
---

# Phase 08.10 Plan 02: ProxyFix-Hardening + SESSION_COOKIE_* Summary

**ProxyFix auf x_for+x_proto reduziert (x_host entfernt), SESSION_COOKIE_SECURE/HTTPONLY/SAMESITE/LIFETIME mit FLASK_DEBUG-aware Conditional in app.py eingebaut (LB-10)**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-25T19:00:00Z
- **Completed:** 2026-04-25T19:05:00Z
- **Tasks:** 1 (Task C)
- **Files modified:** 1

## Accomplishments

- ProxyFix Import in Top-Level-Imports-Block verschoben (war inline nach app = Flask(__name__))
- x_host=1 aus ProxyFix entfernt — Host-Header-Injection-Vektor bei Fixed-Domain unnoetig
- timedelta zum datetime-Import hinzugefuegt
- SESSION_COOKIE_SECURE=not _debug (FLASK_DEBUG env-var, nicht app.debug)
- SESSION_COOKIE_HTTPONLY=True, SESSION_COOKIE_SAMESITE='Lax', PERMANENT_SESSION_LIFETIME=timedelta(days=14)
- Sicherheits-Kommentare dokumentieren ProxyFix-Reihenfolge-Anforderung

## Task Commits

1. **Task C: ProxyFix hardening + SESSION_COOKIE_* config** - `c7c80f8` (feat)

## Files Created/Modified

- `app.py` - ProxyFix umgebaut (kein x_host), timedelta importiert, 4 SESSION_COOKIE_* Config-Lines hinzugefuegt

## Decisions Made

- x_host=1 entfernt: getnerve.app ist Fixed-Domain hinter Nginx — X-Forwarded-Host-Trust ist Host-Header-Injection-Vektor, kein Benefit bei Fixed-Domain
- _debug ueber os.environ.get('FLASK_DEBUG') statt app.debug: app.debug ist unter Gunicorn immer False (Established pattern seit Phase 03)
- ProxyFix-Import in Top-Level-Block verschoben: sauberer Import-Block, keine Inline-Imports im App-Init

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] x_host=1 aus ProxyFix entfernt**
- **Found during:** Task C (ProxyFix-Prerequisite-Check)
- **Issue:** Bestehende ProxyFix-Zeile hatte x_host=1 — Plan dokumentiert ausdruecklich dass x_host WEGGELASSEN werden soll (Host-Header-Injection-Vektor, Fixed-Domain)
- **Fix:** ProxyFix-Aufruf auf ProxyFix(app.wsgi_app, x_for=1, x_proto=1) reduziert; ProxyFix-Import in Top-Level-Imports-Block verschoben
- **Files modified:** app.py
- **Verification:** grep "ProxyFix" app.py — kein x_host=1 mehr
- **Committed in:** c7c80f8 (Task C commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug: falsches ProxyFix-Argument)
**Impact on plan:** Sicherheitsverbesserung, kein Scope-Creep. x_host entfernen war ausdrueckliches Plan-Ziel.

## Issues Encountered

- test_ft_seed.py hat pre-existing ImportError (requests.Session Namenskonflikt) — vorhanden vor diesen Aenderungen, nicht durch diesen Plan verursacht. pytest 266 passing (--ignore=tests/test_ft_seed.py).

## Known Stubs

None — alle Config-Werte sind echte Produktions-Werte, kein Placeholder.

## Threat Flags

Keine neuen Threat-Surfaces eingefuehrt. Session-Cookie-Haertung schliesst T-08.10-02-A/B/C/D aus dem Threat-Register.

## Next Phase Readiness

- SESSION_COOKIE_SECURE=True in Prod aktiv nach naechstem Deploy
- Wave 3 (CSRFProtect) kann auf bestehendem SameSite=Lax aufbauen
- 08.10-03 kann starten

---
*Phase: 08.10-stabilisierung-block-b-auth-haertung*
*Completed: 2026-04-25*
