---
phase: 08.10-stabilisierung-block-b-auth-haertung
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - app.py
autonomous: true
requirements:
  - LB-10

must_haves:
  truths:
    - "SESSION_COOKIE_SECURE ist False in Dev (FLASK_DEBUG=true) und True in Prod"
    - "SESSION_COOKIE_HTTPONLY ist True"
    - "SESSION_COOKIE_SAMESITE ist 'Lax'"
    - "PERMANENT_SESSION_LIFETIME ist 14 Tage"
    - "timedelta ist in app.py importiert"
    - "pytest 266 passing nach Änderung"
  artifacts:
    - path: "app.py"
      provides: "FLASK_DEBUG-aware Session-Cookie-Konfiguration"
      contains: "SESSION_COOKIE_HTTPONLY"
  key_links:
    - from: "app.py FLASK_DEBUG-check"
      to: "SESSION_COOKIE_SECURE config"
      via: "_debug boolean"
      pattern: "_debug.*SESSION_COOKIE_SECURE"
---

<objective>
Wave 2: SESSION_COOKIE_SECURE/HTTPONLY/SAMESITE/LIFETIME in app.py setzen (FLASK_DEBUG-aware).

Purpose: Session-Cookies ohne Secure-Flag können über unverschlüsselte HTTP-Verbindungen abgegriffen werden (Session-Hijacking). HTTPONLY verhindert JS-Zugriff. SameSite=Lax schützt gegen CSRF als Defense-in-Depth. LIFETIME=14 Tage setzt Ablauf konsistent.

Output: 4 Cookie-Config-Lines in app.py, FLASK_DEBUG-aware Conditional für SESSION_COOKIE_SECURE.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md

Bestehende app.py Config (Zeile 26-29, bereits gelesen):
```python
app.config['SECRET_KEY']           = SECRET_KEY
app.config['SESSION_PERMANENT']    = True
app.config['CSS_VERSION']          = '20260421-1'
app.config['MAX_CONTENT_LENGTH']   = 5 * 1024 * 1024
```

Import Zeile 5: `from datetime import datetime` — timedelta noch NICHT importiert.

Edge-Cases dokumentiert für Cross-AI-Review:
- SESSION_COOKIE_SECURE=True bricht localhost (kein HTTPS). FLASK_DEBUG-aware Conditional verhindert Dev-Breakage.
- FLASK_DEBUG-Check MUSS `os.environ.get('FLASK_DEBUG')` nutzen (nicht `app.debug`) — app.debug ist unter gunicorn immer False (State.md Entscheidung Phase 03).
- SESSION_PERMANENT=True bereits gesetzt — PERMANENT_SESSION_LIFETIME greift nur wenn SESSION_PERMANENT=True (bereits korrekt).
</context>

<tasks>

<task type="auto">
  <name>Task C: SESSION_COOKIE_SECURE/HTTPONLY/SAMESITE/LIFETIME in app.py</name>
  <files>app.py</files>
  <read_first>
    - app.py Zeile 1-12 (Imports-Block) — für timedelta-Import
    - app.py Zeile 22-35 (Flask-App-Config-Block) — für Einfügestelle
  </read_first>
  <action>
**1. timedelta zum datetime-Import hinzufügen (Zeile 5):**

Vor: `from datetime import datetime`
Nach: `from datetime import datetime, timedelta`

(Prüfen ob timedelta bereits irgendwo in app.py importiert ist — falls schon `from datetime import datetime, timedelta` vorhanden, kein Duplicate.)

**2. FLASK_DEBUG-aware Session-Cookie-Config nach dem bestehenden Config-Block einfügen (nach Zeile 29, vor der SECRET_KEY-Check-Zeile 31):**

```python
# ── Session-Cookie-Hardening (LB-10) ──────────────────────────────────────────
# SESSION_COOKIE_SECURE=True nur in Prod (HTTPS). FLASK_DEBUG=true → False (Dev/localhost).
# HINWEIS: app.debug ist unter gunicorn immer False → env-var statt app.debug prüfen.
_debug = os.environ.get('FLASK_DEBUG', '').strip() not in ('', '0', 'false', 'False')
app.config['SESSION_COOKIE_SECURE']         = not _debug  # False lokal, True Prod
app.config['SESSION_COOKIE_HTTPONLY']       = True
app.config['SESSION_COOKIE_SAMESITE']       = 'Lax'
app.config['PERMANENT_SESSION_LIFETIME']    = timedelta(days=14)
```

Einfügestelle: NACH `app.config['MAX_CONTENT_LENGTH']` (Zeile 29), VOR dem `if SECRET_KEY == 'dev-secret-change-me'` Check (Zeile 31).

**Edge-Case-Dokumentation im Kommentar** ist bereits im Code-Block oben enthalten.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "SESSION_COOKIE_HTTPONLY" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "SESSION_COOKIE_SECURE" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "SESSION_COOKIE_SAMESITE" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "PERMANENT_SESSION_LIFETIME" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "from datetime import" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SESSION_COOKIE_HTTPONLY" app.py` gibt Treffer mit `True`
    - `grep -n "SESSION_COOKIE_SECURE" app.py` gibt Treffer mit `not _debug`
    - `grep -n "SESSION_COOKIE_SAMESITE" app.py` gibt Treffer mit `'Lax'`
    - `grep -n "PERMANENT_SESSION_LIFETIME" app.py` gibt Treffer mit `timedelta(days=14)`
    - `grep -n "timedelta" app.py` gibt mindestens 1 Treffer im Import
    - `grep -n "_debug.*FLASK_DEBUG" app.py` gibt 1 Treffer
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>Session-Cookies sind in Prod Secure+HttpOnly+SameSite=Lax. Dev bleibt funktionsfähig ohne HTTPS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server (Cookie) | Session-Cookie wird bei jedem Request übertragen |
| JS → Cookie | HttpOnly verhindert JavaScript-Zugriff auf Session-Cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.10-02-A | Spoofing / Session Hijacking | Flask Session-Cookie | mitigate | SESSION_COOKIE_SECURE=True in Prod verhindert Cookie-Übertragung über HTTP |
| T-08.10-02-B | Tampering | Flask Session-Cookie | mitigate | SESSION_COOKIE_HTTPONLY=True verhindert XSS-basiertes Cookie-Stehlen via JS |
| T-08.10-02-C | Cross-Site Request Forgery | Flask Session-Cookie | mitigate (partial) | SESSION_COOKIE_SAMESITE=Lax reduziert CSRF-Angriffsfläche als Defense-in-Depth (vollständiges CSRF-Schutz kommt in Wave 3) |

Residual-Risk: SameSite=Lax schützt nicht gegen alle CSRF-Angriffe (nur GET safe-method). Wave 3 (CSRFProtect) schließt diese Lücke vollständig. Backwards-Compatibility: Bestehende User-Sessions bekommen neue Cookie-Flags beim nächsten Login ausgestellt — kein erzwungener Logout.
</threat_model>

<verification>
```bash
cd /c/Users/andre/dev/salesnerve
grep -n "SESSION_COOKIE_SECURE\|SESSION_COOKIE_HTTPONLY\|SESSION_COOKIE_SAMESITE\|PERMANENT_SESSION_LIFETIME" app.py
pytest tests/ -x -q
```
</verification>

<success_criteria>
- Alle 4 Cookie-Config-Zeilen in app.py vorhanden
- FLASK_DEBUG-aware Conditional korrekt (nicht app.debug)
- timedelta importiert
- pytest 266 passing
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.10-stabilisierung-block-b-auth-haertung/08.10-02-SUMMARY.md` erstellen.
</output>
