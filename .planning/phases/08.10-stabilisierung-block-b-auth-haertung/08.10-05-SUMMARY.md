---
phase: 08.10-stabilisierung-block-b-auth-haertung
plan: "05"
subsystem: auth-security
tags: [rate-limiting, brute-force-protection, flask-limiter, H-20]
dependency_graph:
  requires:
    - "08.10-02 (ProxyFix — Voraussetzung fuer korrektes per-IP Rate-Limiting)"
    - "08.10-03 (CSRFProtect — Reihenfolge ProxyFix → CSRFProtect → init_limiter)"
  provides:
    - "H-20: Brute-Force-Schutz auf /api/login (10/min) und /api/register (5/min)"
    - "429-JSON-Response {'ok': False, 'error': 'rate limit exceeded'}"
  affects:
    - "routes/auth.py (api_login, api_register)"
    - "app.py (Initialisierungsblock)"
tech_stack:
  added:
    - "flask-limiter>=3.5.0 (In-Memory Rate-Limiting, per-IP via get_remote_address)"
  patterns:
    - "Singleton-Limiter in services/rate_limiter.py, init_limiter(app) in app.py"
    - "Decorator-Pattern @limiter.limit() auf Route-Funktionen"
key_files:
  created:
    - services/rate_limiter.py
  modified:
    - requirements.txt
    - app.py
    - routes/auth.py
    - .gitignore
decisions:
  - "In-Memory-Storage fuer Single-Worker (Gunicorn gthread 1+4) — Counter-Reset bei App-Restart akzeptiert; Redis-Hinweis fuer Block-M-Skalierung dokumentiert in services/rate_limiter.py"
  - "init_limiter(app) nach CSRFProtect — Reihenfolge ProxyFix→CSRFProtect→init_limiter kritisch fuer korrektes per-IP-Bucketing hinter Nginx"
  - "Kein globales Limit (default_limits=[]) — nur spezifische Decorators auf Login/Register, keine ungewollten Kollateral-Limits auf anderen Routen"
metrics:
  duration: "5min"
  completed: "2026-04-25"
  tasks_completed: 1
  files_modified: 5
---

# Phase 08.10 Plan 05: Brute-Force-Schutz via flask-limiter Summary

flask-limiter Singleton mit per-IP Rate-Limiting auf /api/login (10/min) und /api/register (5/min), Initialisierungsreihenfolge ProxyFix→CSRFProtect→init_limiter, JSON 429-Handler, Redis-Hinweis fuer Block-M-Skalierung.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| H | flask-limiter + services/rate_limiter.py + Limiter in app.py + Rate-Limits auf Login/Register | e9bb631 | requirements.txt, app.py, routes/auth.py, services/rate_limiter.py, .gitignore |

## Deviations from Plan

None — plan executed exactly as written.

Note: `.gitignore` was also updated to exclude pre-existing pip install artifact files (`=1.2.0`, `=3.5.0`) that appeared as untracked. This is a Rule 3 (cleanup) deviation — not a code change, prevents accidental commit of junk files.

## Threat Surface Scan

No new network endpoints introduced. The plan's threat model (T-08.10-05-A/B/C/D) is fully mitigated:
- T-05-A: /api/login — @limiter.limit("10 per minute") per IP
- T-05-B: /api/register — @limiter.limit("5 per minute") per IP
- T-05-C: 429 response — {'ok': False, 'error': 'rate limit exceeded'}, no internal detail
- T-05-D: ProxyFix(x_for=1) from Wave 2 confirmed present before init_limiter — real client IP used

## Known Stubs

None.

## Self-Check: PASSED

- `services/rate_limiter.py` — FOUND
- `requirements.txt` contains `flask-limiter>=3.5.0` — FOUND
- `app.py` contains `init_limiter(app)` at line 62 — FOUND
- `app.py` contains `@app.errorhandler(429)` at line 64 — FOUND
- `routes/auth.py` contains `@limiter.limit("10 per minute")` at line 183 — FOUND
- `routes/auth.py` contains `@limiter.limit("5 per minute")` at line 201 — FOUND
- Commit `e9bb631` — FOUND
- pytest 266 passing — CONFIRMED
