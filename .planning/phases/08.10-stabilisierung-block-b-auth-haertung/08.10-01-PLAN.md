---
phase: 08.10-stabilisierung-block-b-auth-haertung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - routes/training.py
  - routes/app_routes.py
  - routes/admin_views.py
  - static/pip-launcher.js
  - static/app.js
autonomous: true
requirements:
  - LB-7
  - H-15
  - M-NEW-5

must_haves:
  truths:
    - "500-Fehler geben keine Tracebacks an den Client aus (weder JSON noch plaintext)"
    - "Route-Exception-Catch-Blöcke geben 'internal' statt str(e) zurück"
    - "Server-seitiges Logging bleibt vollständig erhalten (print/app.logger)"
    - "Frontend-Fehlermeldungen enthalten keine Python-Tracebacks"
    - "pytest 266 passing nach allen Änderungen"
  artifacts:
    - path: "app.py"
      provides: "Zentraler Error-Handler ohne Traceback-Leak im Response-Body"
      contains: "traceback': 'internal server error'"
    - path: "routes/training.py"
      provides: "training_start Exception-Handler ohne str(e) im JSON-Response"
    - path: "routes/app_routes.py"
      provides: "Zwei Route-Handler ohne str(e) im JSON-Response"
    - path: "routes/admin_views.py"
      provides: "CRM-Note-Route ohne str(e) im JSON-Response"
    - path: "static/pip-launcher.js"
      provides: "sanitizeErrorMsg() filtert Traceback-Strings aus Fehlermeldungen"
    - path: "static/app.js"
      provides: "Traceback-Filter bei errEl.textContent-Assignments"
  key_links:
    - from: "app.py _handle_500/_handle_exception"
      to: "Client-Browser"
      via: "jsonify response"
      pattern: "'traceback': 'internal server error'"
    - from: "static/pip-launcher.js sanitizeErrorMsg"
      to: "errEl2.textContent"
      via: "function call"
      pattern: "sanitizeErrorMsg"
---

<objective>
Wave 1: Zentraler Error-Handler in app.py Traceback-Leak beheben + Route-Exception-Leaks in 4 Dateien stopfen + Frontend-Traceback-Filter in pip-launcher.js und app.js.

Purpose: Verhindert Python-Tracebacks im Client-Response — Server-Code bleibt intern, Angreifer sehen keine Datei-Pfade, Stack-Frames oder Variablen-Werte. Vorarbeit für alle folgenden Waves: neue Fehler durch CSRF/Session/Limiter werden korrekt maskiert.

Output: app.py Error-Handler gibt 'internal server error' statt Traceback zurück, 4 Route-Files geben 'internal' statt str(e), Frontend-Filter filtert Traceback-Strings.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

Relevante Code-Stellen (bereits gelesen, nicht erneut lesen):

app.py Zeile 1740-1769 (bestehende Error-Handler):
```python
@app.errorhandler(500)
def _handle_500(e):
    tb_str = _tb.format_exc()
    print(tb_str)                        # Server-Log: BLEIBT
    if (...json... or ...XHR...):
        return jsonify({
            'ok': False,
            'error': str(e),
            'traceback': tb_str[-1000:],  # <-- LEAK: raus
        }), 500
    return tb_str, 500, ...              # <-- LEAK: raus

@app.errorhandler(Exception)
def _handle_exception(e):
    if isinstance(e, _HTTPException): return e
    tb_str = _tb.format_exc()
    print(tb_str)                        # Server-Log: BLEIBT
    if (...json... or ...XHR...):
        return jsonify({
            'ok': False,
            'error': f'{type(e).__name__}: {str(e)}',
            'traceback': tb_str[-1000:],  # <-- LEAK: raus
        }), 500
    return tb_str, 500, ...              # <-- LEAK: raus
```

training.py Zeile 395-403 (exception leak):
```python
except Exception as _start_err:
    import traceback
    tb = traceback.format_exc()
    print(tb)
    lines = [l for l in tb.strip().split('\n') if l.strip()]
    last_frame = ' | '.join(lines[-3:]) if len(lines) >= 3 else tb[-300:]
    return jsonify({'ok': False, 'error': f'{last_frame[:500]}'}), 500  # <-- LEAK
```

app_routes.py Zeile ~1296 (exception leak Sterne-Rating):
```python
except Exception as _re:
    print(f"[Engine] Rating-Event Fehler: {_re}")
# (kein leak hier — aber bei Zeile 1160 und 1272 context fehlt — prüfe bei read)
```

admin_views.py Zeile 217-219:
```python
except Exception as e:
    db.rollback()
    return jsonify({'ok': False, 'error': str(e)}), 500  # <-- LEAK
```

pip-launcher.js Zeile 323 (Traceback aus API-Response in errMsg):
```javascript
throw new Error(j.error || 'Server-Fehler (' + r.status + ')');
```
(Der Fehler selbst kommt jetzt aus Backend — aber errEl2.textContent = errMsg könnte Traceback-Text aus j.error enthalten, falls Backend noch leaked. Vorsorge: sanitizeErrorMsg() einfügen.)

app.js Zeile 154-156 (errEl.textContent Assignments):
```javascript
errEl.textContent = result.data.error || 'Recherche fehlgeschlagen';
```
Zeile 890:
```javascript
antwortDiv.innerHTML='<div ...>Fehler: '+escHtml(data.error||'?')+'</div>';
```
</context>

<tasks>

<task type="auto">
  <name>Task A: app.py Error-Handler Traceback-Leak beheben + Route-Exception-Leaks stopfen</name>
  <files>app.py, routes/training.py, routes/app_routes.py, routes/admin_views.py</files>
  <read_first>
    - app.py Zeile 1734-1769 (bestehende Error-Handler)
    - routes/training.py Zeile 390-410
    - routes/app_routes.py Zeile 1155-1175 und 1265-1305 (um die zwei Leak-Stellen zu finden)
    - routes/admin_views.py Zeile 205-222
  </read_first>
  <action>
**1. app.py — _handle_500 und _handle_exception bereinigen (Zeile 1740-1769):**

_handle_500: `print(tb_str)` bleibt. JSON-Response: `'traceback'` Key entfernen, `'error'` auf `'internal server error'` setzen. Plaintext-Fallback: `return '', 500` (kein Traceback an Client).

_handle_exception: `print(tb_str)` bleibt. JSON-Response: `'traceback'` Key entfernen, `'error'` auf `'Internal Server Error'` setzen. Plaintext-Fallback: `return '', 500`.

Ergebnis:
```python
@app.errorhandler(500)
def _handle_500(e):
    tb_str = _tb.format_exc()
    print(tb_str)  # Server-side logging BLEIBT
    if (_request.content_type and 'json' in _request.content_type) or _request.headers.get('X-Requested-With') == 'XMLHttpRequest':
        return jsonify({'ok': False, 'error': 'internal server error'}), 500
    return '', 500

@app.errorhandler(Exception)
def _handle_exception(e):
    if isinstance(e, _HTTPException):
        return e
    tb_str = _tb.format_exc()
    print(tb_str)  # Server-side logging BLEIBT
    if (_request.content_type and 'json' in _request.content_type) or _request.headers.get('X-Requested-With') == 'XMLHttpRequest':
        return jsonify({'ok': False, 'error': 'Internal Server Error'}), 500
    return '', 500
```

**2. routes/training.py — training_start Exception-Block (Zeile ~396-403):**

Vor-Fix: `return jsonify({'ok': False, 'error': f'{last_frame[:500]}'}), 500`
Nach-Fix: `return jsonify({'ok': False, 'error': 'internal'}), 500`
`print(tb)` bleibt für Server-Logging.
Entferne: `lines = [...]`, `last_frame = [...]` (toten Code entfernen da nicht mehr genutzt).
Behalte: `import traceback`, `tb = traceback.format_exc()`, `print(tb)`.

**3. routes/app_routes.py — Exception-Leaks:**

Beim Lesen Zeile 1155-1175 und 1265-1305 nach `str(e)` oder `f'{...e...}'` in jsonify-Returns suchen. Alle gefundenen Exception-Messages an Client: auf `'internal'` setzen. Server-prints (`print(...)`) bleiben unverändert.

**4. routes/admin_views.py — CRM-Note Exception (Zeile ~217-219):**

Vor: `return jsonify({'ok': False, 'error': str(e)}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
`db.rollback()` bleibt.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "'traceback'" app.py | grep -v "# Server-side\|internal server error" || echo "CLEAN"</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "str(e)" routes/admin_views.py | head -5</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "last_frame\|tb\[-300" routes/training.py | head -5</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'traceback': tb_str" app.py` gibt 0 Treffer
    - `grep -n "return tb_str" app.py` gibt 0 Treffer
    - `grep -n "'error': str(e)" routes/admin_views.py` gibt 0 Treffer (war Zeile ~219)
    - `grep -n "last_frame" routes/training.py` gibt 0 Treffer
    - `grep -n "'error': 'internal'" routes/training.py` gibt mindestens 1 Treffer
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>Alle 5 Leak-Stellen in 4 Dateien bereinigt. Server-Logging bleibt erhalten. pytest grün.</done>
</task>

<task type="auto">
  <name>Task B: Frontend-Traceback-Filter in pip-launcher.js und app.js</name>
  <files>static/pip-launcher.js, static/app.js</files>
  <read_first>
    - static/pip-launcher.js Zeile 310-345 (PreCall-Analyse Fetch-Block)
    - static/app.js Zeile 148-175 (PreCall errEl-Block) und Zeile 885-895 (EWB antwortDiv-Block)
  </read_first>
  <action>
**1. pip-launcher.js — sanitizeErrorMsg() hinzufügen und bei errEl2 einsetzen:**

Nach der letzten globalen Funktion VOR dem ersten Event-Listener oder direkt am Anfang des Files (nach `'use strict'` oder ganz oben im anonymen Wrapper), diese Hilfsfunktion einfügen:

```javascript
function sanitizeErrorMsg(msg) {
  if (!msg) return 'Fehler aufgetreten.';
  if (msg.includes('Traceback') || msg.includes('File "') || msg.includes('  File ')) {
    return 'Interner Fehler. Bitte Seite neu laden.';
  }
  return msg;
}
```

Dann bei Zeile ~334 (die Stelle wo `errEl2.textContent = errMsg`):
Vor: `if (errEl2) { errEl2.textContent = errMsg; errEl2.style.display = 'block'; }`
Nach: `if (errEl2) { errEl2.textContent = sanitizeErrorMsg(errMsg); errEl2.style.display = 'block'; }`

**2. app.js — errEl.textContent bei PreCall-Block (Zeile ~155) und EWB-Block (Zeile ~890) filtern:**

Füge sanitizeErrorMsg() analog in app.js ein (gleiche Implementierung wie in pip-launcher.js).

Zeile ~155 (PreCall errEl):
Vor: `errEl.textContent = result.data.error || 'Recherche fehlgeschlagen';`
Nach: `errEl.textContent = sanitizeErrorMsg(result.data.error) || 'Recherche fehlgeschlagen';`

Zeile ~890 (EWB antwortDiv — verwendet escHtml, kein textContent):
Der Wert `data.error` könnte Traceback enthalten. Vor dem escHtml-Call filtern:
Vor: `antwortDiv.innerHTML='<div ...>Fehler: '+escHtml(data.error||'?')+'</div>';`
Nach: `antwortDiv.innerHTML='<div ...>Fehler: '+escHtml(sanitizeErrorMsg(data.error)||'?')+'</div>';`

Platzierung in app.js: `sanitizeErrorMsg` als top-level Funktion, nicht in einem lokalen Scope der die anderen Call-Sites nicht erreicht.

Hinweis: Diese Maßnahme ist Defense-in-Depth. Nach Task A gibt das Backend keine Tracebacks mehr aus. Frontend-Filter fängt trotzdem edge-cases (zwischengespeicherte Responses, Dritt-Fehler).
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "sanitizeErrorMsg" static/pip-launcher.js</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "sanitizeErrorMsg" static/app.js</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sanitizeErrorMsg" static/pip-launcher.js` gibt mindestens 2 Treffer (Definition + Aufruf)
    - `grep -n "sanitizeErrorMsg" static/app.js` gibt mindestens 3 Treffer (Definition + 2 Aufrufe)
    - `grep -n "function sanitizeErrorMsg" static/pip-launcher.js` gibt 1 Treffer
    - `grep -n "function sanitizeErrorMsg" static/app.js` gibt 1 Treffer
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>Frontend filtert Traceback-Strings aus Fehlermeldungen bevor sie im DOM landen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client (JSON Response) | 500-Error-Responses trugen bislang Python-Tracebacks im Response-Body |
| Frontend JS → DOM | errEl.textContent-Assignments konnten Traceback-Strings anzeigen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.10-01-A | Information Disclosure | app.py _handle_500/_handle_exception | mitigate | Traceback aus JSON-Response entfernen; nur server-side print() bleibt |
| T-08.10-01-B | Information Disclosure | routes/training.py training_start | mitigate | last_frame-Leak entfernen; 'internal' als client-seitige Fehlermeldung |
| T-08.10-01-C | Information Disclosure | routes/app_routes.py Exception-Handler | mitigate | str(e) in jsonify durch 'internal' ersetzen |
| T-08.10-01-D | Information Disclosure | routes/admin_views.py CRM-Note | mitigate | str(e) durch 'internal' ersetzen |
| T-08.10-01-E | Information Disclosure | pip-launcher.js / app.js DOM | mitigate | sanitizeErrorMsg() filtert Traceback-Strings bevor DOM-Write |

Residual-Risk: Server-seitiges Logging vollständig erhalten. Kein Informationsverlust für Debugging. Residual: Log-Files auf VPS müssen access-controlled sein (außerhalb dieses Plans).
</threat_model>

<verification>
Nach beiden Tasks:
```bash
cd /c/Users/andre/dev/salesnerve
# Kein Traceback-Key mehr im Error-Handler
grep -n "'traceback'" app.py
# Kein str(e) in Route-Responses
grep -rn "jsonify.*str(e)" routes/
# Frontend-Filter vorhanden
grep -n "sanitizeErrorMsg" static/pip-launcher.js static/app.js
# Test-Baseline stabil
pytest tests/ -x -q
```
</verification>

<success_criteria>
- Kein `'traceback': tb_str` im Response-Body von Error-Handlern
- Kein `str(e)` in jsonify-Responses der 4 Route-Files
- sanitizeErrorMsg() in pip-launcher.js und app.js vorhanden und an allen errEl-Stellen genutzt
- pytest 266 passing
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.10-stabilisierung-block-b-auth-haertung/08.10-01-SUMMARY.md` erstellen.
</output>
