---
phase: 08.10-stabilisierung-block-b-auth-haertung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - routes/training.py
  - routes/app_routes.py
  - routes/admin_views.py
  - static/pip-launcher.js
autonomous: true
requirements:
  - LB-7
  - H-15
  - M-NEW-5

must_haves:
  truths:
    - "500-Fehler geben keine Tracebacks an den Client aus (weder JSON noch plaintext)"
    - "Route-Exception-Catch-Blöcke geben 'internal' statt str(e) zurück"
    - "Server-seitiges Logging bleibt vollständig erhalten (print/app.logger)"
    - "Frontend-Fehlermeldungen in pip-launcher.js enthalten keine Python-Tracebacks"
    - "pytest 266 passing nach allen Änderungen"
  artifacts:
    - path: "app.py"
      provides: "Zentraler Error-Handler ohne Traceback-Leak im Response-Body"
      contains: "traceback': 'internal server error'"
    - path: "routes/training.py"
      provides: "Alle 4 Exception-Handler ohne str(e)/type+str-Leaks im JSON-Response"
    - path: "routes/app_routes.py"
      provides: "str(e)-Leak in api_analyse_line bereinigt (api_frage + api_ewb_trigger wurden in Phase 08.11 gelöscht — nicht mehr in Scope)"
    - path: "routes/admin_views.py"
      provides: "CRM-Note-Route ohne str(e) im JSON-Response"
    - path: "static/pip-launcher.js"
      provides: "sanitizeErrorMsg() filtert Traceback-Strings aus Fehlermeldungen"
  key_links:
    - from: "app.py _handle_500/_handle_exception"
      to: "Client-Browser"
      via: "jsonify response"
      pattern: "'traceback': 'internal server error'"
    - from: "static/pip-launcher.js sanitizeErrorMsg"
      to: "errEl2.textContent"
      via: "function call"
      pattern: "sanitizeErrorMsg"
---

<!-- Fix vs Rebuild: PATCH — Security logic is correct; only dead-code references to deleted app.js
     need removal. app.js wurde in Phase 08.11 gelöscht — kein Frontend-Filter für app.js nötig. -->

<objective>
Wave 1: Zentraler Error-Handler in app.py Traceback-Leak beheben + Route-Exception-Leaks in 4 Dateien stopfen + Frontend-Traceback-Filter in pip-launcher.js.

Purpose: Verhindert Python-Tracebacks im Client-Response — Server-Code bleibt intern, Angreifer sehen keine Datei-Pfade, Stack-Frames oder Variablen-Werte. Vorarbeit für alle folgenden Waves: neue Fehler durch CSRF/Session/Limiter werden korrekt maskiert.

Output: app.py Error-Handler gibt 'internal server error' statt Traceback zurück, Route-Files geben 'internal' statt str(e), Frontend-Filter filtert Traceback-Strings in pip-launcher.js.

Hinweis: app.js wurde in Phase 08.11 gelöscht. Das einzige Frontend-Ziel für sanitizeErrorMsg() ist pip-launcher.js.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

Relevante Code-Stellen (bereits gelesen, nicht erneut lesen):

app.py Zeile 1740-1769 (bestehende Error-Handler):
```python
@app.errorhandler(500)
def _handle_500(e):
    tb_str = _tb.format_exc()
    print(tb_str)                        # Server-Log: BLEIBT
    if (...json... or ...XHR...):
        return jsonify({
            'ok': False,
            'error': str(e),
            'traceback': tb_str[-1000:],  # <-- LEAK: raus
        }), 500
    return tb_str, 500, ...              # <-- LEAK: raus

@app.errorhandler(Exception)
def _handle_exception(e):
    if isinstance(e, _HTTPException): return e
    tb_str = _tb.format_exc()
    print(tb_str)                        # Server-Log: BLEIBT
    if (...json... or ...XHR...):
        return jsonify({
            'ok': False,
            'error': f'{type(e).__name__}: {str(e)}',
            'traceback': tb_str[-1000:],  # <-- LEAK: raus
        }), 500
    return tb_str, 500, ...              # <-- LEAK: raus
```

training.py Zeile 396-403 (training_start outer except — LEAK):
```python
except Exception as _start_err:
    import traceback
    tb = traceback.format_exc()
    print(tb)
    lines = [l for l in tb.strip().split('\n') if l.strip()]
    last_frame = ' | '.join(lines[-3:]) if len(lines) >= 3 else tb[-300:]
    return jsonify({'ok': False, 'error': f'{last_frame[:500]}'}), 500  # <-- LEAK
```

training.py Zeile 291-294 (training_start inner except — KI-Antwort, LEAK):
```python
except Exception as _gen_err:
    import traceback
    traceback.print_exc()
    return jsonify({'error': f'KI-Antwort fehlgeschlagen: {type(_gen_err).__name__}: {str(_gen_err)[:200]}'}), 500
```

training.py Zeile 598-601 (training_help except — LEAK):
```python
except Exception as e:
    import traceback
    traceback.print_exc()
    return jsonify({'error': f'{type(e).__name__}: {str(e)[:200]}'}), 500
```

training.py Zeile 852-853 (training_transcribe except — LEAK):
```python
except Exception as e:
    return jsonify({'error': str(e)}), 500
```

training.py Zeile 973-976 (api_training_personality_generate except — LEAK):
```python
except Exception as e:
    import traceback
    traceback.print_exc()
    return jsonify({'error': f'{type(e).__name__}: {str(e)[:200]}'}), 500
```

app_routes.py Zeile 186-187 (api_analyse_line except — LEAK):
```python
except Exception as e:
    return jsonify({'error': str(e)}), 500
```

admin_views.py Zeile 217-219:
```python
except Exception as e:
    db.rollback()
    return jsonify({'ok': False, 'error': str(e)}), 500  # <-- LEAK
```

pip-launcher.js Zeile 323 (Traceback aus API-Response in errMsg):
```javascript
throw new Error(j.error || 'Server-Fehler (' + r.status + ')');
```
(Der Fehler selbst kommt jetzt aus Backend — aber errEl2.textContent = errMsg könnte Traceback-Text aus j.error enthalten, falls Backend noch leaked. Vorsorge: sanitizeErrorMsg() einfügen.)
</context>

<tasks>

<task type="auto">
  <name>Task A: app.py Error-Handler Traceback-Leak beheben + Route-Exception-Leaks stopfen</name>
  <files>app.py, routes/training.py, routes/app_routes.py, routes/admin_views.py</files>
  <read_first>
    - app.py Zeile 1734-1769 (bestehende Error-Handler)
    - routes/training.py VOLLSTÄNDIG (alle Exception-Handler — es gibt 5 Leak-Stellen verteilt über die gesamte Datei)
    - routes/app_routes.py VOLLSTÄNDIG (alle Exception-Handler — es gibt 3 Leak-Stellen bei Zeile 187, 1142, 1254)
    - routes/admin_views.py Zeile 205-222
  </read_first>
  <action>
**1. app.py — _handle_500 und _handle_exception bereinigen (Zeile 1740-1769):**

_handle_500: `print(tb_str)` bleibt. JSON-Response: `'traceback'` Key entfernen, `'error'` auf `'internal server error'` setzen. Plaintext-Fallback: `return '', 500` (kein Traceback an Client).

_handle_exception: `print(tb_str)` bleibt. JSON-Response: `'traceback'` Key entfernen, `'error'` auf `'Internal Server Error'` setzen. Plaintext-Fallback: `return '', 500`.

Ergebnis:
```python
@app.errorhandler(500)
def _handle_500(e):
    tb_str = _tb.format_exc()
    print(tb_str)  # Server-side logging BLEIBT
    if (_request.content_type and 'json' in _request.content_type) or _request.headers.get('X-Requested-With') == 'XMLHttpRequest':
        return jsonify({'ok': False, 'error': 'internal server error'}), 500
    return '', 500

@app.errorhandler(Exception)
def _handle_exception(e):
    if isinstance(e, _HTTPException):
        return e
    tb_str = _tb.format_exc()
    print(tb_str)  # Server-side logging BLEIBT
    if (_request.content_type and 'json' in _request.content_type) or _request.headers.get('X-Requested-With') == 'XMLHttpRequest':
        return jsonify({'ok': False, 'error': 'Internal Server Error'}), 500
    return '', 500
```

**2. routes/training.py — ALLE 5 Exception-Leaks bereinigen:**

Zeile ~396-403 (training_start outer except — last_frame-Leak):
Vor: `return jsonify({'ok': False, 'error': f'{last_frame[:500]}'}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
`print(tb)` bleibt für Server-Logging.
Entferne: `lines = [...]`, `last_frame = [...]` (toten Code entfernen da nicht mehr genutzt).
Behalte: `import traceback`, `tb = traceback.format_exc()`, `print(tb)`.

Zeile ~291-294 (training_start inner except — KI-Antwort type+str-Leak):
Vor: `return jsonify({'error': f'KI-Antwort fehlgeschlagen: {type(_gen_err).__name__}: {str(_gen_err)[:200]}'}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
`traceback.print_exc()` bleibt für Server-Logging.

Zeile ~598-601 (training_help type+str-Leak):
Vor: `return jsonify({'error': f'{type(e).__name__}: {str(e)[:200]}'}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
`traceback.print_exc()` bleibt für Server-Logging.

Zeile ~853 (training_transcribe str-Leak):
Vor: `return jsonify({'error': str(e)}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
Kein print vorhanden — `print(f'[Training] Transcribe-Fehler: {e}')` hinzufügen DAVOR.

Zeile ~973-976 (api_training_personality_generate type+str-Leak):
Vor: `return jsonify({'error': f'{type(e).__name__}: {str(e)[:200]}'}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
`traceback.print_exc()` bleibt für Server-Logging.

**3. routes/app_routes.py — str(e)-Leak in api_analyse_line bereinigen:**

Hinweis (v3): api_frage (war Zeile ~1142) und api_ewb_trigger (war Zeile ~1254) wurden in Phase 08.11 (Block F) vollständig gelöscht. Diese Routen existieren nicht mehr — kein Fix nötig und kein Fix möglich.

Zeile ~187 (api_analyse_line — EINZIGER verbleibender Leak in dieser Datei):
Vor: `return jsonify({'error': str(e)}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
Logging hinzufügen: `print(f'[API] analyse_line Fehler: {e}')` DAVOR.

Beim Lesen der vollständigen Datei: weitere `str(e)` in jsonify-Returns suchen. Falls weitere gefunden: ebenfalls auf `'internal'` setzen.

**4. routes/admin_views.py — CRM-Note Exception (Zeile ~217-219):**

Vor: `return jsonify({'ok': False, 'error': str(e)}), 500`
Nach: `return jsonify({'ok': False, 'error': 'internal'}), 500`
`db.rollback()` bleibt.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "'traceback'" app.py | grep -v "# Server-side\|internal server error" || echo "CLEAN"</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "str(e)" routes/admin_views.py | head -5</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "last_frame\|tb\[-300" routes/training.py | head -5</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "jsonify.*str(e)\|str(e).*jsonify" routes/training.py | head -10</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "jsonify.*str(e)\|str(e).*jsonify" routes/app_routes.py | head -10</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -r "traceback" routes/ app.py | grep -v "print\|format_exc\|print_exc\|import traceback\|# " | head -10 || echo "CLEAN — keine Traceback-Strings in JSON-Responses"</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'traceback': tb_str" app.py` gibt 0 Treffer
    - `grep -n "return tb_str" app.py` gibt 0 Treffer
    - `grep -n "'error': str(e)" routes/admin_views.py` gibt 0 Treffer (war Zeile ~219)
    - `grep -n "last_frame" routes/training.py` gibt 0 Treffer
    - `grep -n "jsonify({'error': str(e)" routes/training.py` gibt 0 Treffer
    - `grep -n "jsonify({'error': f'{type" routes/training.py` gibt 0 Treffer
    - `grep -n "jsonify({'error': str(e)" routes/app_routes.py` gibt 0 Treffer (war Zeile 187, 1142, 1254)
    - `grep -n "'error': 'internal'" routes/training.py` gibt mindestens 4 Treffer (Zeile ~294, ~403, ~601, ~853, ~976)
    - `grep -n "'error': 'internal'" routes/app_routes.py` gibt mindestens 1 Treffer (nur api_analyse_line Zeile ~187 — api_frage + api_ewb_trigger in Phase 08.11 gelöscht)
    - `grep -r "traceback" routes/ app.py | grep -v "print\|format_exc\|print_exc\|import traceback\|# "` gibt 0 Treffer (kein Traceback-String in JSON-Responses)
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>Alle Leak-Stellen in training.py (5 Handler), app_routes.py (3 Handler), admin_views.py (1 Handler) und app.py Error-Handler bereinigt. Server-Logging bleibt erhalten. pytest grün.</done>
</task>

<task type="auto">
  <name>Task B: Frontend-Traceback-Filter in pip-launcher.js</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js Zeile 310-345 (PreCall-Analyse Fetch-Block)
  </read_first>
  <action>
**pip-launcher.js — sanitizeErrorMsg() hinzufügen und bei errEl2 einsetzen:**

Hinweis: app.js wurde in Phase 08.11 gelöscht — kein Frontend-Filter für app.js nötig. Das einzige Frontend-Ziel ist pip-launcher.js.

Nach der letzten globalen Funktion VOR dem ersten Event-Listener oder direkt am Anfang des Files (nach `'use strict'` oder ganz oben im anonymen Wrapper), diese Hilfsfunktion einfügen:

```javascript
function sanitizeErrorMsg(msg) {
  if (!msg) return 'Fehler aufgetreten.';
  if (msg.includes('Traceback') || msg.includes('File "') || msg.includes('  File ')) {
    return 'Interner Fehler. Bitte Seite neu laden.';
  }
  return msg;
}
```

Dann bei Zeile ~334 (die Stelle wo `errEl2.textContent = errMsg`):
Vor: `if (errEl2) { errEl2.textContent = errMsg; errEl2.style.display = 'block'; }`
Nach: `if (errEl2) { errEl2.textContent = sanitizeErrorMsg(errMsg); errEl2.style.display = 'block'; }`

Hinweis: Diese Maßnahme ist Defense-in-Depth. Nach Task A gibt das Backend keine Tracebacks mehr aus. Frontend-Filter fängt trotzdem edge-cases (zwischengespeicherte Responses, Dritt-Fehler).
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "sanitizeErrorMsg" static/pip-launcher.js</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -c "sanitizeErrorMsg" static/pip-launcher.js</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sanitizeErrorMsg" static/pip-launcher.js` gibt mindestens 2 Treffer (Definition + Aufruf)
    - `grep -n "function sanitizeErrorMsg" static/pip-launcher.js` gibt 1 Treffer
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>pip-launcher.js filtert Traceback-Strings aus Fehlermeldungen bevor sie im DOM landen. app.js existiert nicht mehr (gelöscht in Phase 08.11) — kein Filter nötig.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client (JSON Response) | 500-Error-Responses trugen bislang Python-Tracebacks im Response-Body |
| Frontend JS → DOM | errEl2.textContent-Assignment in pip-launcher.js könnte Traceback-Strings anzeigen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.10-01-A | Information Disclosure | app.py _handle_500/_handle_exception | mitigate | Traceback aus JSON-Response entfernen; nur server-side print() bleibt |
| T-08.10-01-B | Information Disclosure | routes/training.py training_start (outer + inner except) | mitigate | last_frame-Leak + type+str-Leak entfernen; 'internal' als client-seitige Fehlermeldung |
| T-08.10-01-C | Information Disclosure | routes/training.py training_help + training_transcribe + personality_generate | mitigate | type+str-Leaks und str(e)-Leaks durch 'internal' ersetzen |
| T-08.10-01-D | Information Disclosure | routes/app_routes.py api_analyse_line | mitigate | str(e) in jsonify durch 'internal' ersetzen (api_frage + api_ewb_trigger in Phase 08.11 gelöscht — nicht mehr in Scope) |
| T-08.10-01-E | Information Disclosure | routes/admin_views.py CRM-Note | mitigate | str(e) durch 'internal' ersetzen |
| T-08.10-01-F | Information Disclosure | pip-launcher.js DOM | mitigate | sanitizeErrorMsg() filtert Traceback-Strings bevor DOM-Write |

Residual-Risk: Server-seitiges Logging vollständig erhalten. Kein Informationsverlust für Debugging. Residual: Log-Files auf VPS müssen access-controlled sein (außerhalb dieses Plans).
</threat_model>

<verification>
Nach beiden Tasks:
```bash
cd /c/Users/andre/dev/salesnerve
# Kein Traceback-Key mehr im Error-Handler
grep -n "'traceback'" app.py
# Kein str(e) in Route-Responses (training + app_routes)
grep -rn "jsonify.*str(e)\|str(e).*500" routes/training.py routes/app_routes.py routes/admin_views.py
# Bestätigen: keine Traceback-Strings in JSON-Responses
grep -r "traceback" routes/ app.py | grep -v "print\|format_exc\|print_exc\|import traceback\|# "
# Frontend-Filter vorhanden
grep -c "sanitizeErrorMsg" static/pip-launcher.js
# Test-Baseline stabil
pytest tests/ -x -q
```
</verification>

<success_criteria>
- Kein `'traceback': tb_str` im Response-Body von Error-Handlern
- Kein `str(e)` oder `type(e).__name__: str(e)` in jsonify-Responses aller Route-Files
- `grep -r "traceback" routes/ app.py | grep -v "print\|format_exc\|print_exc\|import traceback\|# "` gibt 0 Treffer
- sanitizeErrorMsg() in pip-launcher.js vorhanden (mindestens 2 Treffer: 1 Def + 1 Call)
- pytest 266 passing
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.10-stabilisierung-block-b-auth-haertung/08.10-01-SUMMARY.md` erstellen.
</output>
