---
phase: 08.10-stabilisierung-block-b-auth-haertung
plan: 05
type: execute
wave: 5
depends_on:
  - "08.10-02"
  - "08.10-03"
files_modified:
  - requirements.txt
  - app.py
  - routes/auth.py
  - services/rate_limiter.py
autonomous: true
requirements:
  - H-20

must_haves:
  truths:
    - "flask-limiter ist in requirements.txt"
    - "ProxyFix ist in app.py VOR init_limiter(app) registriert"
    - "Limiter ist in app.py initialisiert mit In-Memory-Storage"
    - "/api/login ist auf 10 per minute per IP limitiert"
    - "/api/register ist auf 5 per minute per IP limitiert"
    - "429-Response ist JSON mit {'ok': False, 'error': 'rate limit exceeded'}"
    - "Multi-Worker-Hinweis (Redis-Backend) ist im Code dokumentiert"
    - "pytest 266 passing nach allen Änderungen"
  artifacts:
    - path: "requirements.txt"
      provides: "flask-limiter Dependency"
      contains: "flask-limiter"
    - path: "app.py"
      provides: "Limiter-Initialisierung + @app.errorhandler(429)"
      contains: "Limiter"
    - path: "routes/auth.py"
      provides: "@limiter.limit() Decorator auf /api/login und /api/register"
      contains: "limiter.limit"
    - path: "services/rate_limiter.py"
      provides: "Singleton-Limiter-Instanz mit init_limiter(app)"
      contains: "init_limiter"
  key_links:
    - from: "app.py ProxyFix (Wave 2)"
      to: "app.py init_limiter(app)"
      via: "Initialisierungsreihenfolge: ProxyFix → CSRFProtect → init_limiter"
      pattern: "ProxyFix.*init_limiter"
    - from: "app.py limiter"
      to: "routes/auth.py"
      via: "from services.rate_limiter import limiter"
      pattern: "from services.rate_limiter import limiter"
---

<!-- Fix vs Rebuild: PATCH — Add ProxyFix ordering constraint + @app.errorhandler(429).
     Pflicht-Hinweis #3: ProxyFix-Konfiguration muss VOR Rate-Limiter-Init in app.py stehen.
     Ohne ProxyFix gibt get_remote_address 127.0.0.1 zurück für alle Requests hinter Nginx —
     Rate-Limit wird zum globalen Bucket für alle User. -->

<objective>
Wave 5: Brute-Force-Schutz via flask-limiter — Rate-Limiting auf /api/login und /api/register.

Purpose: Ohne Rate-Limiting kann ein Angreifer unbegrenzt Login-Versuche senden und Passwörter per Brute-Force erraten. 10 Versuche/Minute auf /api/login sind ausreichend für legitime User (auch bei "Passwort vergessen"-Loops) und blockieren Automatisierung effektiv.

Initialisierungsreihenfolge in app.py (KRITISCH):
(1) ProxyFix(app.wsgi_app) — bereits in Wave 2 gesetzt
(2) CSRFProtect(app) — bereits in Wave 3 gesetzt
(3) init_limiter(app) — dieser Plan

Ohne ProxyFix gibt get_remote_address 127.0.0.1 zurück für alle Requests hinter Nginx — Rate-Limit wird zum globalen Bucket für alle User statt per-IP.

Output: flask-limiter in requirements.txt, services/rate_limiter.py, init_limiter(app) in app.py NACH ProxyFix, @app.errorhandler(429) in app.py, @limiter.limit() auf api_login und api_register.

Multi-Worker-Hinweis: In-Memory-Storage reicht für Single-Worker (aktueller Stand auf VPS mit Gunicorn gthread 1+4). Bei Skalierung auf Multi-Worker (Block M) muss auf Redis-Backend umgestellt werden. Dieser Hinweis ist im Code dokumentiert.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md

Relevante Codebase-Fakten (bereits gelesen):

requirements.txt (aktuell, fehlt flask-limiter):
```
flask>=3.0.0
flask-socketio>=5.3.6
...
pytest>=8.0.0
```

routes/auth.py:
- api_login() ist auf Zeile 172 als `def api_login()` (keine Zeile für Route-Decorator gesehen — beim Lesen finden)
- api_register() ist auf Zeile 188 als `def api_register()`
- Import-Section Zeile 1-10

app.py Struktur:
- Import-Block oben (Zeile 1-12)
- `app.wsgi_app = ProxyFix(...)` — in Wave 2 hinzugefügt (MUSS vor init_limiter stehen)
- `socketio = SocketIO(app, ...)` Zeile 34
- `csrf = CSRFProtect(app)` — in Wave 3 hinzugefügt (Zeile ~35)
- init_limiter(app) kommt NACH csrf = CSRFProtect(app)

flask-limiter Import-Pattern:
```python
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address
```
</context>

<tasks>

<task type="auto">
  <name>Task H: flask-limiter in requirements.txt + services/rate_limiter.py + Limiter in app.py + Rate-Limits auf Login/Register</name>
  <files>requirements.txt, app.py, routes/auth.py, services/rate_limiter.py</files>
  <read_first>
    - requirements.txt (vollständig — kurz)
    - app.py Zeile 1-12 (Import-Block)
    - app.py Zeile 33-42 (ProxyFix + socketio/csrf-Initialisierung — ProxyFix-Platz bestätigen)
    - routes/auth.py Zeile 1-12 (Import-Block)
    - routes/auth.py Zeile 168-175 (api_login Route-Decorator und Definition)
    - routes/auth.py Zeile 185-195 (api_register Route-Decorator und Definition)
  </read_first>
  <action>
**SCHRITT 0 — ProxyFix-Prerequisite-Verifikation (VOR allem anderen):**

Prüfen ob ProxyFix in app.py bereits aus Wave 2 vorhanden ist:
```bash
grep -n "ProxyFix" app.py
```
Falls 0 Treffer: STOP — Wave 2 (Plan 02) muss zuerst ausgeführt werden. ProxyFix ist Voraussetzung für korrektes Rate-Limiting.
Falls Treffer vorhanden: weiter.

init_limiter(app) MUSS nach ProxyFix-Setup in app.py stehen. Reihenfolge:
(1) ProxyFix(app.wsgi_app) → (2) CSRFProtect(app) → (3) init_limiter(app)
Ohne ProxyFix gibt get_remote_address 127.0.0.1 zurück für alle Requests hinter Nginx — Rate-Limit wird zum globalen Bucket für alle User.

**1. requirements.txt — flask-limiter hinzufügen:**

```
flask-limiter>=3.5.0
```

Nach `flask-wtf>=1.2.0` (wurde in Wave 3 hinzugefügt) einfügen.

**2. services/rate_limiter.py — Singleton-Limiter-Instanz erstellen (neue Datei):**

```python
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address

# H-20: Brute-Force-Schutz — Singleton-Instanz, wird in app.py mit init_limiter(app) gebunden.
# ACHTUNG: ProxyFix muss in app.py VOR init_limiter(app) gesetzt sein.
# Ohne ProxyFix gibt get_remote_address 127.0.0.1 fuer alle Requests hinter Nginx —
# Rate-Limit wird zum globalen Bucket statt per-IP.
limiter = Limiter(
    key_func=get_remote_address,
    default_limits=[],       # Kein globales Limit — nur spezifische Decorators
    storage_uri="memory://",
    # Future Multi-Worker (Block M): storage_uri="redis://localhost:6379" setzen.
)

def init_limiter(app):
    """H-20: Bindet limiter an Flask-App. Aufzurufen in app.py nach ProxyFix-Setup."""
    limiter.init_app(app)
```

**3. app.py — Import + init_limiter(app) + @app.errorhandler(429):**

Im Import-Block (oben, nach den Flask-Imports):
```python
from services.rate_limiter import limiter, init_limiter
```

NACH `csrf = CSRFProtect(app)` (Wave 3), mit explizitem Kommentar zur Reihenfolge:
```python
# ── Brute-Force-Schutz (H-20) ─────────────────────────────────────────────────
# Reihenfolge KRITISCH: (1) ProxyFix(app.wsgi_app) [Wave 2] →
#                       (2) CSRFProtect(app) [Wave 3] →
#                       (3) init_limiter(app) [dieser Block]
# Ohne ProxyFix gibt get_remote_address 127.0.0.1 fuer alle Requests hinter Nginx —
# Rate-Limit wird zum globalen Bucket statt per-IP.
# Future Multi-Worker (Block M): in services/rate_limiter.py storage_uri auf
# "redis://localhost:6379" umstellen.
init_limiter(app)

@app.errorhandler(429)
def _handle_rate_limit(e):
    """H-20: Konsistentes JSON-Format für 429-Responses (analog Plan 01 Error-Normalisierung)."""
    return jsonify({'ok': False, 'error': 'rate limit exceeded'}), 429
```

**4. routes/auth.py — limiter Import + Decorator auf api_login und api_register:**

Import hinzufügen (im Import-Block, nach bestehenden Imports):
```python
from services.rate_limiter import limiter
```

Auf api_login (beim Lesen exakte Zeile für Route-Decorator finden):
```python
@auth_bp.route('/api/login', methods=['POST'])
@limiter.limit("10 per minute")  # H-20: Brute-Force-Schutz
def api_login():
    ...
```

Auf api_register:
```python
@auth_bp.route('/api/register', methods=['POST'])
@limiter.limit("5 per minute")  # H-20: Brute-Force-Schutz (weniger Registrierungen als Logins)
def api_register():
    ...
```
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "ProxyFix" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "flask-limiter" requirements.txt</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "limiter\|Limiter\|init_limiter" app.py | head -10</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "@app.errorhandler(429)" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "limiter.limit\|@limiter" routes/auth.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && test -f services/rate_limiter.py && echo "EXISTS" || echo "MISSING"</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ProxyFix" app.py` gibt mindestens 1 Treffer (Voraussetzung aus Wave 2)
    - `grep -n "flask-limiter" requirements.txt` gibt 1 Treffer
    - `grep -n "init_limiter\|from services.rate_limiter" app.py` gibt Treffer
    - `grep -n "@app.errorhandler(429)" app.py` gibt 1 Treffer
    - `grep -n "rate limit exceeded" app.py` gibt 1 Treffer (im errorhandler)
    - `grep -n "@limiter.limit" routes/auth.py` gibt 2 Treffer (api_login + api_register)
    - `grep -n '"10 per minute"' routes/auth.py` gibt 1 Treffer
    - `grep -n '"5 per minute"' routes/auth.py` gibt 1 Treffer
    - `test -f services/rate_limiter.py` ist true
    - `grep -n "Multi-Worker\|redis://" services/rate_limiter.py` gibt Treffer (Kommentar)
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>Brute-Force-Schutz aktiv. ProxyFix VOR init_limiter bestätigt. /api/login 10/min, /api/register 5/min. 429-Response ist JSON {'ok': False, 'error': 'rate limit exceeded'}. Redis-Hinweis für Multi-Worker dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/login | Unauthentifizierte POST-Requests — Brute-Force-Angriffspunkt |
| Internet → /api/register | Spam-Registrierungen oder Account-Enumeration |
| Nginx → Gunicorn (get_remote_address) | Ohne ProxyFix liefert get_remote_address immer 127.0.0.1 — Rate-Limit wäre global statt per-IP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.10-05-A | Denial of Service (Brute-Force) | routes/auth.py /api/login | mitigate | @limiter.limit("10 per minute") per IP via flask-limiter |
| T-08.10-05-B | Denial of Service (Spam) | routes/auth.py /api/register | mitigate | @limiter.limit("5 per minute") per IP |
| T-08.10-05-C | Information Disclosure | flask-limiter 429 Response | mitigate | @app.errorhandler(429) gibt {'ok': False, 'error': 'rate limit exceeded'} — kein internes Detail |
| T-08.10-05-D | Bypass (IP-Spoofing via Proxy) | get_remote_address hinter Nginx | mitigate | ProxyFix(x_for=1) aus Wave 2 stellt echte Client-IP sicher — init_limiter NACH ProxyFix |

Residual-Risk: In-Memory-Storage bedeutet Rate-Limit-Counter werden bei App-Restart zurückgesetzt. Akzeptabel für Single-Worker. Multi-Worker (Block M) muss Redis-Backend verwenden.

Multi-Worker-Dokumentation: `services/rate_limiter.py` enthält Kommentar mit konkreter Anleitung für Redis-Umstellung.
</threat_model>

<verification>
```bash
cd /c/Users/andre/dev/salesnerve
# Reihenfolge prüfen: ProxyFix muss VOR init_limiter in app.py stehen
grep -n "ProxyFix\|init_limiter\|CSRFProtect" app.py
# 429-Handler
grep -n "errorhandler(429)\|rate limit exceeded" app.py
# Rate-Limits
grep -n "flask-limiter" requirements.txt
grep -n "@limiter.limit\|per minute" routes/auth.py
pytest tests/ -x -q
```
</verification>

<success_criteria>
- ProxyFix in app.py VOR init_limiter(app) (Zeilen-Reihenfolge: ProxyFix < CSRFProtect < init_limiter)
- flask-limiter in requirements.txt
- services/rate_limiter.py existiert mit Multi-Worker-Hinweis
- @app.errorhandler(429) in app.py gibt {'ok': False, 'error': 'rate limit exceeded'}
- @limiter.limit("10 per minute") auf api_login
- @limiter.limit("5 per minute") auf api_register
- pytest 266 passing
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.10-stabilisierung-block-b-auth-haertung/08.10-05-SUMMARY.md` erstellen.
</output>
