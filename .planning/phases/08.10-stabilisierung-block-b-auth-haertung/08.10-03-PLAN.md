---
phase: 08.10-stabilisierung-block-b-auth-haertung
plan: 03
type: execute
wave: 3
depends_on:
  - "08.10-02"
files_modified:
  - app.py
  - routes/payments.py
  - routes/oauth.py
  - templates/base.html
  - static/pip-launcher.js
  - static/profile_editor.js
  - static/feedback.js
  - static/admin_dashboard.js
autonomous: true
requirements:
  - LB-9

must_haves:
  truths:
    - "CSRFProtect(app) ist in app.py initialisiert"
    - "stripe_webhook ist CSRF-exempt (externe Stripe-Server, kein Browser-Origin)"
    - "google_callback und microsoft_callback sind CSRF-exempt (OAuth-Flow, kein Browser-POST)"
    - "Alle JS-Files mit POST-Calls senden X-CSRFToken Header"
    - "base.html enthält <meta name='csrf-token'> mit csrf_token()"
    - "pytest 266 passing nach allen Änderungen"
  artifacts:
    - path: "app.py"
      provides: "CSRFProtect(app) Initialisierung + 3 csrf.exempt() Aufrufe"
      contains: "CSRFProtect"
    - path: "templates/base.html"
      provides: "CSRF-Token Meta-Tag für Frontend-JS"
      contains: "csrf-token"
    - path: "static/pip-launcher.js"
      provides: "X-CSRFToken Header in POST-Requests"
    - path: "static/profile_editor.js"
      provides: "X-CSRFToken Header in POST-Requests"
    - path: "static/feedback.js"
      provides: "X-CSRFToken Header in POST-Requests"
    - path: "static/admin_dashboard.js"
      provides: "getCsrfToken() helper definiert für zukünftige POST-Calls"
  key_links:
    - from: "templates/base.html <meta name='csrf-token'>"
      to: "JS getCsrfToken()"
      via: "document.querySelector"
      pattern: "csrf-token"
    - from: "CSRFProtect(app)"
      to: "stripe_webhook"
      via: "csrf.exempt()"
      pattern: "csrf.exempt"
---

<!-- Fix vs Rebuild: PATCH — Remove dead app.js references; 4 files instead of 5.
     app.js wurde in Phase 08.11 gelöscht — nur 4 verbleibende JS-Files erhalten CSRF-Token.
     VARIANTE B (v3): SocketIO CSRF-Exempt explizit dokumentiert und verifiziert (André-Entscheid
     2026-04-25). Live-Coaching ist Kern-Feature — Drift nach Deploy muss ausgeschlossen werden. -->

<objective>
Wave 3: CSRF-Schutz vollständig implementieren — Backend CSRFProtect(app) + Webhook-Exempts + Meta-Tag in base.html + CSRF-Token in allen JS POST-Calls.

Purpose: Ohne CSRF-Schutz kann ein Angreifer einen eingeloggten User dazu bringen, ungewollte Aktionen auszuführen (z.B. Profil-Änderungen, Session-Start). Flask-WTF CSRFProtect schützt alle State-ändernden Endpunkte. Stripe-Webhook und OAuth-Callbacks müssen explizit exempt sein (keine Browser-Sessions).

Output: CSRFProtect(app) in app.py, 3 csrf.exempt(), Meta-Tag in base.html, getCsrfToken()-Helper in allen 4 verbleibenden JS-Files.

Hinweis: app.js wurde in Phase 08.11 gelöscht — nur 4 JS-Files erhalten CSRF-Token: pip-launcher.js, profile_editor.js, feedback.js, admin_dashboard.js.

CSRF-Exempt-Begründung (für Cross-AI-Review):
- payments.stripe_webhook: Externe Stripe-Server senden POST, kein Browser-Cookie vorhanden. Stripe signiert via STRIPE_WEBHOOK_SECRET — das ist der korrekte Auth-Mechanismus.
- oauth.google_callback: OAuth-Flow = GET-Callback von Google. Kein Browser-POST, kein CSRF-Risiko.
- oauth.microsoft_callback: OAuth-Flow = GET-Callback von Microsoft. Identisch zu Google.

Backwards-Compatibility: Bestehende Browser-Sessions verlieren ihre CSRF-Token-Referenz erst beim nächsten Page-Load. Beim nächsten Page-Load wird der neue Meta-Tag gerendert, alle folgenden POST-Requests funktionieren. Kein User-Logout erforderlich.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md

Relevante Informationen (bereits gelesen):

app.py Blueprint-Registrierungen (Zeile 1697-1703):
```python
app.register_blueprint(changelog_bp)
app.register_blueprint(payments_bp)
app.register_blueprint(legal_bp)
app.register_blueprint(performance_bp)
app.register_blueprint(oauth_bp)
app.register_blueprint(learning_bp)
init_oauth(app)
```

routes/payments.py:
- Blueprint: `payments_bp = Blueprint('payments', __name__, url_prefix='/payments')`
- Stripe-Webhook-Function: `def stripe_webhook():` (Zeile 65)
- Route: `/payments/webhook` (POST, externes Stripe)

routes/oauth.py:
- Blueprint: `oauth_bp = Blueprint('oauth', __name__)`
- `def google_callback()` auf `/auth/google/callback` (GET)
- `def microsoft_callback()` auf `/auth/microsoft/callback` (GET)

feedback.js:
- Zeile 11: `var res = await fetch('/api/feedback', {method:'POST', body:fd, credentials:'same-origin'})`
- FormData-Body (nicht JSON) — X-CSRFToken als Header, nicht im Body

admin_dashboard.js:
- Zeile 29: `const r = await fetch('/admin/dashboard/api/overview?period=...')` — GET-Request, kein CSRF nötig
- Falls weitere POST-Calls existieren: beim Lesen suchen und ebenfalls absichern

profile_editor.js:
- Zeile 99: `method: 'POST'` — hat POST-Calls (FAQ + Tabu)
- Zeile 378: `method: 'POST'`
</context>

<tasks>

<task type="auto">
  <name>Task D: CSRF Backend — CSRFProtect(app) + Webhook-Exempts + Meta-Tag in base.html</name>
  <files>app.py, templates/base.html</files>
  <read_first>
    - app.py Zeile 1-15 (Imports — für flask_wtf Import-Stelle)
    - app.py Zeile 1695-1710 (Blueprint-Registrierungen — für csrf.exempt() Platzierung)
    - templates/base.html Zeile 1-30 (Head-Bereich — für Meta-Tag Einfügestelle)
  </read_first>
  <action>
**1. flask-wtf installieren (requirements.txt hinzufügen):**

`flask-wtf>=1.2.0` zu requirements.txt hinzufügen.

**2. app.py — CSRFProtect Import und Initialisierung:**

Import hinzufügen (nach den bestehenden Flask-Imports, im Import-Block ganz oben):
```python
from flask_wtf.csrf import CSRFProtect
```

NACH `socketio = SocketIO(...)` (Zeile 34), VOR dem `@app.template_filter('fromjson')`:
```python
csrf = CSRFProtect(app)
```

**3. app.py — csrf.exempt() NACH allen Blueprint-Registrierungen:**

NACH `init_oauth(app)` (Zeile 1703), vor dem `@app.before_request`:
```python
# ── CSRF-Exempts für externe Endpoints ────────────────────────────────────────
# stripe_webhook: Stripe-Server POSTen direkt — kein Browser-Cookie, Stripe signiert via
# STRIPE_WEBHOOK_SECRET. CSRF-Schutz hier wäre falsch-positiv und würde Webhooks brechen.
# google_callback / microsoft_callback: OAuth GET-Callbacks — kein Browser-POST, kein CSRF-Risiko.
from routes.payments import stripe_webhook
from routes.oauth import google_callback, microsoft_callback
csrf.exempt(stripe_webhook)
csrf.exempt(google_callback)
csrf.exempt(microsoft_callback)
```

**4. templates/base.html — CSRF-Token Meta-Tag im <head>:**

Im `<head>`-Bereich, nach dem `<meta charset="UTF-8">` Tag, einfügen:
```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```

Beim Lesen von base.html: Die exakte Stelle nach `<meta charset="UTF-8">` oder `<meta name="viewport">` finden und dort einfügen.

**5. SocketIO CSRF-Kompatibilität verifizieren (VARIANTE B — PFLICHT vor Abschluss):**

Flask-SocketIO (async_mode='threading') registriert seinen Handler auf WSGI-Ebene VOR Flask-Routing.
Socket.io-Requests (/socket.io/*) erreichen Flask's before_request-Hooks NICHT → CSRFProtect's
before_request-Hook enthält `if not request.endpoint: return` (Flask-WTF 1.x) → Socket.io-Requests
sind automatisch exempt weil sie keinen Flask-Endpoint haben.

BEDINGUNG für diese Sicherheit: `socketio = SocketIO(app, ...)` MUSS in app.py VOR `csrf = CSRFProtect(app)` stehen.
Beim Lesen von app.py bestätigen:
```bash
grep -n "socketio = SocketIO\|csrf = CSRFProtect" app.py
# Erwartung: socketio-Zeile KLEINER als csrf-Zeile
```

Falls Reihenfolge falsch (csrf vor socketio): `csrf = CSRFProtect(app)` NACH `socketio = SocketIO(...)` verschieben.

NOTFALL-WORKAROUND (nur ausführen falls Live-Coaching nach Deploy bricht mit CSRF-400):
```python
# In app.py, NACH csrf = CSRFProtect(app):
# Flask-SocketIO Polling-Transport: /socket.io/ POST hat kein Flask-Endpoint →
# CSRFProtect überspringt (endpoint=None check in Flask-WTF 1.x).
# Falls das nicht greift — expliziter Hook:
_csrf_hook = app.before_request_funcs[None][-1]  # CSRFProtect's zuletzt registrierter Hook
app.before_request_funcs[None].remove(_csrf_hook)

@app.before_request
def _csrf_protect_socketio_aware():
    from flask import request as _r
    if _r.path.startswith('/socket.io'):
        return  # SocketIO: kein Browser-CSRF-Risiko (WSGI-Handler)
    return _csrf_hook()
```
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "CSRFProtect" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "csrf.exempt" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "socketio = SocketIO\|csrf = CSRFProtect" app.py</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "csrf-token" templates/base.html</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "flask-wtf" requirements.txt</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CSRFProtect(app)" app.py` gibt 1 Treffer
    - `grep -n "csrf.exempt(stripe_webhook)" app.py` gibt 1 Treffer
    - `grep -n "csrf.exempt(google_callback)" app.py` gibt 1 Treffer
    - `grep -n "csrf.exempt(microsoft_callback)" app.py` gibt 1 Treffer
    - `grep -n 'name="csrf-token"' templates/base.html` gibt 1 Treffer mit `{{ csrf_token() }}`
    - `grep -n "flask-wtf" requirements.txt` gibt 1 Treffer
    - `grep -n "socketio = SocketIO" app.py | head -1` hat kleinere Zeilennummer als `grep -n "csrf = CSRFProtect" app.py | head -1` (SocketIO VOR CSRF — garantiert WSGI-Level-Interception)
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>CSRF-Backend aktiv. Stripe + OAuth-Callbacks sind korrekt exempt. Meta-Tag in base.html verfügbar. SocketIO-Initialisierungsreihenfolge bestätigt (VARIANTE B).</done>
</task>

<task type="auto">
  <name>Task E: CSRF Frontend — X-CSRFToken Header in allen 4 JS POST-Calls</name>
  <files>static/pip-launcher.js, static/profile_editor.js, static/feedback.js, static/admin_dashboard.js</files>
  <read_first>
    - static/pip-launcher.js Zeile 1-30 (für getCsrfToken-Platzierung) + POST-Calls
    - static/profile_editor.js Zeile 1-30 + POST-Calls bei Zeile 95-105 und 375-385
    - static/feedback.js Zeile 1-20 + POST-Call bei Zeile 11
    - static/admin_dashboard.js Zeile 1-40 (Überprüfen ob POST-Calls existieren)
  </read_first>
  <action>
**Hinweis: app.js wurde in Phase 08.11 gelöscht — nur 4 verbleibende JS-Files erhalten CSRF-Token.**

**Pattern für alle 4 Files:**

Hilfsfunktion oben in jedes File einfügen (als globale Funktion oder im jeweiligen Scope):
```javascript
function getCsrfToken() {
  var m = document.querySelector('meta[name="csrf-token"]');
  return m ? m.getAttribute('content') : '';
}
```

Dann bei JEDEM `fetch(url, { method: 'POST', ... })` Call den Header hinzufügen:

**Für JSON-Calls (Content-Type: application/json):**
```javascript
headers: {
  'Content-Type': 'application/json',
  'X-CSRFToken': getCsrfToken()
}
```

**Für FormData-Calls (feedback.js — kein Content-Type Header, da FormData auto-setzt):**
```javascript
// Bestehend: fetch('/api/feedback', {method:'POST', body:fd, credentials:'same-origin'})
// Nach Fix:
fetch('/api/feedback', {
  method: 'POST',
  body: fd,
  credentials: 'same-origin',
  headers: { 'X-CSRFToken': getCsrfToken() }
})
```

**Spezifische Stellen:**

pip-launcher.js: POST-Calls bei PreCall-Analyse-Fetch (Zeile ~305-320) und anderen POST-Calls. getCsrfToken() zu headers hinzufügen.

profile_editor.js: Zeile ~99 und ~378 haben `method: 'POST'`. Bestehende headers (falls vorhanden) ergänzen oder neu anlegen.

feedback.js: Zeile ~11 FormData-POST. Headers-Object hinzufügen mit nur `'X-CSRFToken': getCsrfToken()`.

admin_dashboard.js: Beim Lesen prüfen ob POST-Calls existieren. Falls nur GET (Zeile 29), keine Header-Änderung nötig — nur getCsrfToken() Definition einfügen (für Zukunftssicherheit). Falls POST-Calls gefunden: Header hinzufügen.

**Wichtig:** getCsrfToken() muss in jedem File separat definiert sein (kein shared module). Platzierung: ganz oben im File oder im obersten Scope, erreichbar von allen POST-Call-Stellen.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "getCsrfToken\|X-CSRFToken" static/pip-launcher.js | head -10</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "getCsrfToken\|X-CSRFToken" static/profile_editor.js | head -10</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "getCsrfToken\|X-CSRFToken" static/feedback.js</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && grep -n "getCsrfToken" static/admin_dashboard.js</automated>
    <automated>cd /c/Users/andre/dev/salesnerve && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function getCsrfToken" static/pip-launcher.js` gibt 1 Treffer
    - `grep -n "X-CSRFToken" static/pip-launcher.js` gibt mindestens 1 Treffer
    - `grep -n "function getCsrfToken" static/profile_editor.js` gibt 1 Treffer
    - `grep -n "X-CSRFToken" static/profile_editor.js` gibt mindestens 1 Treffer
    - `grep -n "X-CSRFToken" static/feedback.js` gibt 1 Treffer
    - `grep -n "function getCsrfToken" static/admin_dashboard.js` gibt 1 Treffer
    - `pytest tests/ -x -q` endet mit "266 passed"
  </acceptance_criteria>
  <done>Alle 4 verbleibenden JS-Files haben getCsrfToken() definiert; Files mit POST-Calls nutzen X-CSRFToken Header. CSRF-Schutz ist end-to-end aktiv. (app.js wurde in Phase 08.11 gelöscht — kein CSRF-Token nötig.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (POST) | Ohne CSRF-Schutz können cross-origin Seiten POST-Requests im Namen eingeloggter User auslösen |
| Stripe-Server → /payments/webhook | Externe Server, kein Browser-Origin, kein Cookie — CSRF-Exempt korrekt |
| Google/Microsoft-Server → /auth/*/callback | OAuth GET-Callback, kein Browser-POST — CSRF-Exempt korrekt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.10-03-A | Spoofing + Tampering | Alle POST-Endpunkte | mitigate | CSRFProtect(app) + X-CSRFToken Header in JS (4 Files) |
| T-08.10-03-B | Spoofing | payments.stripe_webhook | accept (by design) | csrf.exempt() korrekt — Stripe-Auth via HMAC-Signature (STRIPE_WEBHOOK_SECRET) |
| T-08.10-03-C | Spoofing | oauth.google_callback | accept (by design) | csrf.exempt() korrekt — OAuth state-Parameter ist CSRF-Mitigation für OAuth-Flow |
| T-08.10-03-D | Spoofing | oauth.microsoft_callback | accept (by design) | csrf.exempt() korrekt — OAuth state-Parameter ist CSRF-Mitigation für OAuth-Flow |
| T-08.10-03-E | Information Disclosure | CSRF-Token im Meta-Tag | accept | Token ist session-scoped, kein Geheimnis per se — nur für eingeloggte User sichtbar |

Residual-Risk: CSRF-Token im Meta-Tag ist für XSS-Angreifer lesbar. XSS-Prävention (bleach in markdown_filter bereits vorhanden) ist primäre Gegenmassnahme. HttpOnly auf Session-Cookie (Wave 2) verhindert direkten Cookie-Diebstahl.

Backwards-Compatibility: Bestehende Sessions: Beim nächsten Page-Load erhält Browser den neuen Meta-Tag. Alle folgenden POST-Requests haben Token. Sessions mid-flight verlieren nichts (CSRF-Token ist stateless per Session).
</threat_model>

<verification>
```bash
cd /c/Users/andre/dev/salesnerve
# Backend
grep -n "CSRFProtect\|csrf.exempt" app.py
grep -n 'csrf-token' templates/base.html
# Frontend (4 Files — app.js wurde in Phase 08.11 gelöscht)
grep -rn "X-CSRFToken\|getCsrfToken" static/pip-launcher.js static/profile_editor.js static/feedback.js static/admin_dashboard.js
# Tests
pytest tests/ -x -q
```
</verification>

<success_criteria>
- CSRFProtect(app) in app.py aktiv
- Genau 3 csrf.exempt() Aufrufe: stripe_webhook, google_callback, microsoft_callback
- base.html hat `<meta name="csrf-token" content="{{ csrf_token() }}">`
- Alle 4 verbleibenden JS-Files haben getCsrfToken() definiert; Files mit POST-Calls nutzen X-CSRFToken Header
- pytest 266 passing
</success_criteria>

<output>
Nach Completion: `.planning/phases/08.10-stabilisierung-block-b-auth-haertung/08.10-03-SUMMARY.md` erstellen.
</output>
