---
phase: 08.19.5.6.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/pip-launcher.js
  - static/nerve.css
autonomous: true
requirements:
  - R-02
  - R-03
  - R-05

must_haves:
  truths:
    - "Jeder der 4 Reiter-Dropdowns hat '— kein/e [Type] —' als erste Option"
    - "Auswahl von '— kein/e ... —' setzt die entsprechende state-Variable auf null"
    - "Tab-Wechsel → Textfeld zeigt sofort den Text des selektierten oder ersten Items"
    - "Hint-Box ist sichtbar über den 4 Tab-Buttons und erklärt Reihenfolge + Skript-Priorität"
    - "Kein hardcodierter Hex-/RGB-Farbwert in neuem HTML/CSS"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "renderStep5() mit Null-Optionen in allen 4 Dropdowns + switchTab5() mit Preview-Trigger + Hint-Box-HTML"
      contains: "kein Opener|keine Erlaubnisfrage|keinen Pitch|kein Skript"
    - path: "static/nerve.css"
      provides: "CSS-Klasse launcher-hint-box (nur falls nötig)"
  key_links:
    - from: "renderStep5() opOpts"
      to: "state.selectedOpenerId"
      via: "onchange handler setzt id = parseInt(value) || null"
      pattern: "kein Opener"
    - from: "switchTab5()"
      to: "lnr-opener-preview / lnr-skript-preview"
      via: "getElementById + textContent setzen nach renderStep5()"
      pattern: "switchTab5"
---

<objective>
R-02 (Null-Default-Optionen), R-03 (Render-Trigger nach Tab-Switch), R-05 (Hint-Box) in renderStep5() und switchTab5() implementieren.

Purpose: Behebt Bug "kein Abwählen möglich" (R-02), Bug "leeres Textfeld beim Tab-Wechsel" (R-03), und fügt Erklär-Hint-Box für User-Education ein (R-05). Alle Änderungen sind in renderStep5() / switchTab5() konzentriert — keine anderen Funktionen berührt.

Output: Modifiziertes pip-launcher.js mit Null-Optionen, sofortigem Preview-Trigger und Hint-Box. Ggf. eine neue CSS-Klasse in nerve.css.
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish\08.19.5.6.1-SPEC.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish\08.19.5.6.1-CONTEXT.md

<interfaces>
<!-- Aus static/pip-launcher.js — gelesen vor Bearbeitung -->

state-Variablen (relevant für diesen Plan):
  state.selectedOpenerId    — null oder Integer
  state.selectedErlaubnisId — null oder Integer
  state.selectedPitchId     — null oder Integer
  state.selectedSkriptId    — null oder Integer
  state.activeTab           — 'opener' | 'erlaubnis' | 'pitch' | 'skript'
  state.openerItems         — Array mit {id, name, inhalt, type: 'opener'|'erlaubnis'|'pitch'}
  state.skripte             — Array mit {id, name, inhalt}

switchTab5() — Zeile ~804:
  function switchTab5(tab) { state.activeTab = tab; renderStep5(); }

renderStep5() — Zeile ~808:
  - Baut innerHTML via JS-String-Array (monolithisch, D-06)
  - Tab-Nav: fcd-tabs + fcd-tab CSS-Klassen (D-09)
  - Opener-Tab (Zeile ~849): opOpts = openerItems.map(o => '<option value="'+o.id+'" ...>')
  - Erlaubnis-Tab (Zeile ~868): erlOpts analog
  - Pitch-Tab (Zeile ~886): pitchOpts analog
  - Skript-Tab (Zeile ~904): skOpts aus state.skripte
  - Event-Wiring nach innerHTML: openerSel5.onchange (Zeile ~998), skriptSel5.onchange (Zeile ~1020)

openerSel5.onchange (aktuell, Zeile ~998-1016):
  var id = parseInt(openerSel5.value) || null;
  // setzt selectedOpenerId / selectedErlaubnisId / selectedPitchId je nach activeTab
  // aktualisiert lnr-opener-preview via textContent

CSS-Tokens (nerve.css / base.html inline):
  var(--glass-bg)            — Hintergrund weiß/hell
  var(--glass-border)        — Border grau
  var(--glass-shadow)        — leichter Schatten
  var(--page-text-muted)     — #9CA3AF gedämpfter Text
  var(--page-text-secondary) — #6B7280 sekundärer Text
  var(--btn-primary-bg-from) — #00D4AA Akzentfarbe
  var(--btn-primary-text)    — #0D1117 dunkler Text auf Akzent-Bg

Bestehende CSS-Klassen (base.html inline styles, Zeile ~371-375):
  .fcd-tabs — Tab-Container
  .fcd-tab  — einzelner Tab-Button
  .fcd-tab.active — aktiver Tab
  .launcher-select — Dropdown
  .launcher-opener-preview — Vorschau-Div
  .launcher-btn-ghost — Ghost-Button
  .launcher-btn-primary — Primary-Button
  .launcher-anrede-btn — Toggle-Button
  Kein .launcher-hint-box vorhanden → neue Klasse nötig
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Null-Optionen in allen 4 Reiter-Dropdowns (R-02)</name>
  <read_first>
    - static/pip-launcher.js (Zeile 808–1089, renderStep5() komplett lesen vor Edit)
    - static/nerve.css (nur Token-Referenz, kein Edit hier)
  </read_first>
  <files>static/pip-launcher.js</files>
  <action>
In renderStep5() — in jedem der 4 Tab-Blöcke — als ersten Eintrag in der Options-Liste eine Null-Option prependen.

**Opener-Tab** (aktuell ab Zeile ~855):
```js
var opOpts = '<option value="">' + '— kein Opener —' + '</option>'
  + openerItems.map(function (o) {
      return '<option value="' + o.id + '"' + (o.id === state.selectedOpenerId ? ' selected' : '') + '>' + escHtml(o.name) + '</option>';
    }).join('');
```
Null-Option wird selected wenn state.selectedOpenerId === null: `(state.selectedOpenerId === null ? ' selected' : '')` auf der Null-Option.

**Erlaubnisfrage-Tab** (aktuell ab Zeile ~874):
```js
var erlOpts = '<option value=""' + (state.selectedErlaubnisId === null ? ' selected' : '') + '>' + '— keine Erlaubnisfrage —' + '</option>'
  + erlaubnisItems.map(function (o) {
      return '<option value="' + o.id + '"' + (o.id === state.selectedErlaubnisId ? ' selected' : '') + '>' + escHtml(o.name) + '</option>';
    }).join('');
```

**Pitch-Tab** (aktuell ab Zeile ~892):
```js
var pitchOpts = '<option value=""' + (state.selectedPitchId === null ? ' selected' : '') + '>' + '— keinen Pitch —' + '</option>'
  + pitchItems.map(function (o) {
      return '<option value="' + o.id + '"' + (o.id === state.selectedPitchId ? ' selected' : '') + '>' + escHtml(o.name) + '</option>';
    }).join('');
```

**Skript-Tab** (aktuell ab Zeile ~909):
```js
var skOpts = '<option value=""' + (state.selectedSkriptId === null ? ' selected' : '') + '>' + '— kein Skript —' + '</option>'
  + state.skripte.map(function (s) {
      return '<option value="' + s.id + '"' + (s.id === state.selectedSkriptId ? ' selected' : '') + '>' + escHtml(s.name) + '</option>';
    }).join('');
```

**Preview-Variablen anpassen:**
Alle vier `*Prev`-Variablen (opPrev, erlPrev, pitchPrev, skPrev) werden aktuell aus `*Sel5`-Lookup berechnet. Wenn state.selectedXId === null → `*Sel5` ist undefined → `*Prev = ''` (bereits korrekt durch `? escHtml(...) : ''`). Kein weiterer Eingriff nötig.

**Event-Handler openerSel5.onchange (Zeile ~998-1016) — keine Änderung nötig:**
`parseInt(openerSel5.value) || null` konvertiert bereits `value=""` auf `null`. Handler bleibt unverändert.

**Event-Handler skriptSel5.onchange (Zeile ~1020-1031) — keine Änderung nötig:**
`parseInt(skriptSel5.value) || null` konvertiert bereits `value=""` auf `null`.

ACHTUNG: Nur die Options-HTML-Strings ändern, nicht die Event-Handler. D-07 (inline onclick) und D-06 (monolithisch) bleiben erhalten. Kein neuer Code außerhalb renderStep5().
  </action>
  <verify>
    <automated>
      grep -n "kein Opener\|keine Erlaubnisfrage\|keinen Pitch\|kein Skript" C:\Users\andre\dev\salesnerve\static\pip-launcher.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "kein Opener" static/pip-launcher.js` → findet mindestens 1 Treffer in renderStep5()
    - `grep "keine Erlaubnisfrage" static/pip-launcher.js` → findet mindestens 1 Treffer
    - `grep "keinen Pitch" static/pip-launcher.js` → findet mindestens 1 Treffer
    - `grep "kein Skript" static/pip-launcher.js` → findet mindestens 1 Treffer
    - Alle 4 Null-Optionen haben `value=""` (nicht value="0" oder value="null")
    - `grep 'value=""' static/pip-launcher.js` → findet ≥4 Treffer im renderStep5()-Bereich
    - Die State-Variablen werden durch `parseInt(value) || null` bei value="" auf null gesetzt (kein weiterer Handler-Change nötig — bestehende Handler bereits korrekt)
  </acceptance_criteria>
  <done>Alle 4 Dropdowns haben "— kein/e [Type] —" als erste Option mit value="". User kann aktive Auswahl auf null zurücksetzen.</done>
</task>

<task type="auto">
  <name>Task 2: Tab-Switch Preview-Trigger (R-03) + Hint-Box (R-05)</name>
  <read_first>
    - static/pip-launcher.js (Zeile 803–810 switchTab5, Zeile 808–1089 renderStep5, nach Task 1 bereits aktuell)
    - static/nerve.css (prüfen ob launcher-hint-box bereits existiert — falls nicht: neue Klasse hinzufügen)
    - templates/base.html (Zeile 331–375 für bestehende launcher-* CSS-Klassen)
  </read_first>
  <files>static/pip-launcher.js, static/nerve.css</files>
  <action>
**R-03: Preview-Trigger nach Tab-Switch**

In `renderStep5()` — nach dem `c.innerHTML = [...].join('')` Aufruf (aktuell Zeile ~962) und VOR dem Event-Wiring-Block — Preview-Trigger einfügen:

```js
// R-03: Tab-Switch Preview-Trigger — sofort den ersten/selektierten Item-Text anzeigen
(function () {
  var tab = state.activeTab;
  var previewId, items, selId;
  if (tab === 'skript') {
    previewId = 'lnr-skript-preview';
    items = state.skripte || [];
    selId = state.selectedSkriptId;
  } else {
    previewId = 'lnr-opener-preview';
    items = (state.openerItems || []).filter(function (o) { return o.type === tab; });
    selId = tab === 'erlaubnis' ? state.selectedErlaubnisId
          : tab === 'pitch'    ? state.selectedPitchId
          :                      state.selectedOpenerId;
  }
  var previewEl = document.getElementById(previewId);
  if (!previewEl || items.length === 0) return;
  // Selektiertes Item anzeigen; wenn null → ersten Item als Preview (aber state bleibt null per D-03)
  var item = selId ? items.find(function (i) { return i.id === selId; }) : null;
  var displayItem = item || items[0];
  if (displayItem && displayItem.inhalt) {
    previewEl.textContent = displayItem.inhalt;
    previewEl.style.fontStyle = item ? 'normal' : 'italic';
    previewEl.style.color = item ? '' : 'var(--page-text-muted)';
  }
})();
```

WICHTIG per D-03: Wenn `selId === null` (kein Item selektiert), wird `items[0].inhalt` als Preview-Text angezeigt (fontStyle italic, color muted), aber `state.selectedXId` bleibt `null`. Die state-Variable wird hier NICHT gesetzt.

**R-05: Hint-Box über Tab-Nav**

In `renderStep5()` — im innerHTML-Array — vor `tabNavHtml` eine permanente Hint-Box prependen (per D-01: permanent sichtbar, kein Toggle):

```js
var hintBoxHtml = '<div class="launcher-hint-box">'
  + '<span class="launcher-hint-icon">ⓘ</span>'
  + '<span>Reihenfolge im Teleprompter: <strong>Opener → Erlaubnisfrage → Pitch/Skript</strong>. '
  + 'Skript hat Vorrang vor Pitch. '
  + 'Mit „— kein/e …“ diesen Teil überspringen.</span>'
  + '</div>';
```

Dann im c.innerHTML-Array vor `tabNavHtml` einfügen:
```js
c.innerHTML = [
  '<div class="launcher-step">',
  '<div class="nav-live-title">Gesprächsvorbereitung</div>',
  // Profil-Picker (unverändert)
  state.profiles.length > 0 ? '...' : '...',
  // NEU: Hint-Box vor Tab-Nav
  hintBoxHtml,
  // Tab-Nav
  tabNavHtml,
  // Tab-Content (unverändert)
  tabContentHtml,
  // ... Rest unverändert
].join('');
```

**CSS-Klasse launcher-hint-box in nerve.css (D-08: nur nerve.css-Tokens):**

Am Ende des PiP-Launcher-CSS-Blocks in `nerve.css` (nach .launcher-anrede-btn.active, Zeile ~2718+) folgende Klasse hinzufügen:

```css
/* R-05: Step-5 Hint-Box — Gesprächsvorbereitung Reihenfolge-Erklärung (Phase 08.19.5.6.1) */
.launcher-hint-box {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  padding: 8px 12px;
  background: var(--glass-bg);
  border: 1.5px solid var(--glass-border);
  border-radius: 8px;
  font-size: 12px;
  color: var(--page-text-secondary);
  line-height: 1.4;
}
.launcher-hint-icon {
  flex-shrink: 0;
  color: var(--btn-primary-bg-from);
  font-size: 14px;
  margin-top: 1px;
}
```

KEIN hardcoded Hex/RGB in diesen CSS-Regeln — ausschließlich var(--...) Tokens verwenden.

**Umlaute:** Hint-Box-Text ist user-facing → echte Umlaute verwenden. Aber: CSS-Klassen-Namen und JS-Variablen bleiben ASCII (launcher-hint-box, hintBoxHtml). Unicode-Escapes (ⓘ = ⓘ, → = →, „ = „, “ = ", ü = ü) in JS-Strings sind korrekt um Encoding-Fragen zu umgehen.
  </action>
  <verify>
    <automated>
      grep -n "launcher-hint-box\|launcher-hint-icon\|R-03\|Tab-Switch Preview" C:\Users\andre\dev\salesnerve\static\pip-launcher.js
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "launcher-hint-box" static/pip-launcher.js` → findet ≥1 Treffer in renderStep5()-Bereich (hintBoxHtml-Variable oder direktes String-Literal)
    - `grep "launcher-hint-box" static/nerve.css` → findet die CSS-Regel
    - `grep "launcher-hint-icon" static/nerve.css` → findet die CSS-Regel
    - `grep "var(--" static/nerve.css | grep "launcher-hint"` → alle Farb-/Hintergrund-Deklarationen in .launcher-hint-box verwenden ausschließlich var(--...) Tokens
    - `grep "color: #\|background: #\|background-color: #" static/nerve.css | grep "hint"` → kein Treffer (kein hardcoded Hex in Hint-Box-Klassen)
    - Preview-Trigger-Code steht nach dem `c.innerHTML = [...].join('')` Aufruf und vor dem ersten `document.getElementById`-Aufruf im Event-Wiring-Block
    - Preview-Trigger enthält: `items[0]` als Fallback wenn selId null ist
    - Preview-Trigger setzt NICHT `state.selectedXId` (state-Variable bleibt null per D-03)
    - `grep "state\.selected.*= items\[0\]\|state\.selected.*= displayItem\|state\.selected.*= item" static/pip-launcher.js` → kein Treffer im Preview-Trigger-Block (state bleibt unverändert)
    - Hint-Box-HTML steht im c.innerHTML-Array VOR tabNavHtml (d.h. hintBoxHtml wird vor tabNavHtml im Join platziert)
  </acceptance_criteria>
  <done>Tab-Wechsel zeigt sofort Item-Text im Preview-Div. Hint-Box ist permanent über Tab-Buttons sichtbar. Keine hardcoded Farben. state.selectedXId bleibt null wenn nicht aktiv selektiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API-Response → state.openerItems | JSON von /api/launcher/profile/{id} wird in state geschrieben und via escHtml() in innerHTML injiziert |
| user input → innerHTML | Dropdown-Wert (value="") und preview-textContent — kein direktes innerHTML mit User-Input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.5.6.1-01 | Tampering | renderStep5() Null-Option value="" | accept | parseInt(value) || null konvertiert "" sicher zu null; kein server-side impact da frontend-only |
| T-08.19.5.6.1-02 | Information Disclosure | Preview-Trigger zeigt items[0].inhalt | accept | inhalt ist eigenes Profil-Material des Users — kein fremder Content; bereits in renderStep5 sichtbar |
| T-08.19.5.6.1-03 | XSS | hintBoxHtml enthält keine User-Daten | accept | Hint-Box-Text ist statischer JS-String, kein User-Input injiziert; escHtml() bereits für alle dynamischen Werte in renderStep5 verwendet |
| T-08.19.5.6.1-04 | XSS | preview.textContent = displayItem.inhalt | mitigate | textContent statt innerHTML verwenden — kein HTML-Injection möglich; inhalt-Wert kommt aus API-Response die server-seitig aus DB gelesen wird |
</threat_model>

<verification>
Nach Ausführung beider Tasks prüfen:

1. `grep -c "kein Opener\|keine Erlaubnisfrage\|keinen Pitch\|kein Skript" static/pip-launcher.js` → Ausgabe 4
2. `grep -c 'value=""' static/pip-launcher.js` → mind. 4 Treffer im renderStep5()-Bereich
3. `grep "launcher-hint-box" static/nerve.css` → Treffer vorhanden
4. `grep "color: #\|background-color: #\|background: #" static/nerve.css | grep -i "hint"` → kein Treffer
5. Preview-Trigger steht nach innerHTML-Assign, liest items[0] als Fallback, setzt state NICHT
6. Manuelle Browser-Prüfung: Tab-Wechsel → Preview sofort gefüllt; Null-Option wählbar; Hint-Box sichtbar
</verification>

<success_criteria>
- Alle 4 Reiter-Dropdowns zeigen "— kein/e [Type] —" als erste auswählbare Option
- User kann aktive Auswahl rückgängig machen (zurück auf null)
- Tab-Wechsel → Textfeld sofort gefüllt (kein zweiter Klick nötig)
- Hint-Box sichtbar über Tab-Nav mit korrekter Reihenfolge (Opener → Erlaubnisfrage → Pitch/Skript) + Skript-Priorität-Hinweis
- Kein hardcoded Hex/RGB in neuem CSS-Code
- state.selectedXId bleibt null wenn Null-Option selektiert oder bei Tab-Wechsel ohne aktive Selektion
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish/08.19.5.6.1-01-SUMMARY.md` erstellen.
</output>
