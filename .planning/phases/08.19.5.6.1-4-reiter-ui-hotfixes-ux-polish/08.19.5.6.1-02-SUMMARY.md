---
phase: 08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish
plan: 02
subsystem: ui
tags: [pip-launcher, teleprompter, personalisierung, renderStep5, vanilla-js]

# Dependency graph
requires:
  - phase: 08.19.5.6.1
    plan: 01
    provides: Null-Optionen in Dropdowns, Tab-Switch Preview, Hint-Box

provides:
  - _initTeleprompter() korrekte Block-Sequenz [openerText?, erlaubnisText?, skriptOrPitchBlocks?]
  - selectedErlaubnisId wird in Teleprompter-Blöcken ausgewertet
  - hasOpener/hasErlaubnis Guard: Teleprompter zeigt Opener+Erlaubnis auch ohne Skript/Pitch
  - _savePersonalizedAndStartCall() ruft renderStep5() statt startCall(true)
  - Profil-Reload nach Personalisierung: neuestes personalisiertes Item selektiert

affects:
  - pip-launcher.js _initTeleprompter, _savePersonalizedAndStartCall

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "hasOpener/hasErlaubnis Guard vor frühem Return in _initTeleprompter()"
    - "Block-Concat via .concat().concat() statt ternärer [x].concat(y)"
    - "Defensives skriptBlocks = [] wenn inhalt leer (statt direktes split)"
    - "Profil-Reload fetch in .then() nach Save, renderStep5() als Return-Flow"
    - "Null-Guard für leere is_personalized-Ergebnisse mit _showToast Fallback"

key-files:
  created: []
  modified:
    - static/pip-launcher.js

key-decisions:
  - "Rule-1-Fix: neue var saveBtn mit falscher ID lnr-personalize-save-btn entfernt — vorhandener saveBtn (lnr-step4c-save) aus Funktionskopf weiterverwenden"
  - "OD-01 Pitch/Skript-Priorität via inhalt-Variable unverändert erhalten — nur Block-Builder neu"
  - "Kein startCall(true) auch im Fallback (.catch) — renderStep5() ist immer der Return-Flow"

requirements-completed: [R-01, R-04]

# Metrics
duration: 25min
completed: 2026-05-06
---

# Phase 08.19.5.6.1 Plan 02: Teleprompter Block-Builder + Personalisierungs-Return-Flow Summary

**Teleprompter zeigt korrekte Block-Sequenz [Opener, Erlaubnisfrage, Skript/Pitch]; nach Personalisierung kehrt App zu 4-Reiter-Ansicht zurück statt Call automatisch zu starten**

## Performance

- **Duration:** 25 min
- **Started:** 2026-05-06
- **Completed:** 2026-05-06
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- R-01: `_initTeleprompter()` baut korrekte Block-Sequenz `[openerText?, erlaubnisText?, skriptOrPitchBlocks?]`. `erlaubnisText` wird aus `selectedErlaubnisId` via `openerItems` (type='erlaubnis') gelesen. `hasOpener`/`hasErlaubnis` Guard erweitert frühen Return: Teleprompter zeigt Opener+Erlaubnisfrage auch ohne Skript/Pitch. OD-01 Skript > Pitch Priorität via `inhalt`-Variable unverändert erhalten.
- R-04: `_savePersonalizedAndStartCall()` ruft nach erfolgreichem Save `fetch('/api/launcher/profile/{id}')` auf, selektiert neuestes `is_personalized`-Item und ruft `renderStep5()` auf. Kein `startCall(true)` — auch nicht im `.catch()`-Fallback.

## Task Commits

1. **Task 1: Teleprompter Block-Builder korrekte Sequenz (R-01)** - `895b5ee` (feat)
2. **Task 2: Personalisierungs-Flow kehrt zu renderStep5() zurück (R-04)** - `e1e80b0` (feat)

## Files Created/Modified

- `static/pip-launcher.js` — `_initTeleprompter()`: hasOpener/hasErlaubnis Guard, erlaubnisText Block, defensive skriptBlocks, openerBlocks.concat(erlaubnisBlocks).concat(skriptBlocks). `_savePersonalizedAndStartCall()`: startCall(true) durch fetch+renderStep5()-Flow ersetzt, Null-Guard in Meeting+Cold-Call Pfad, _showToast bei leerem Reload.

## Decisions Made

- `var saveBtn` doppelt deklariert (mit falscher ID `lnr-personalize-save-btn`) — Rule 1 Bug: entfernt, vorhandener `saveBtn` aus Funktionskopf (`lnr-step4c-save`) wird weitergenutzt
- `startCall(true)` in Kommentaren (Zeile 681/732) als Referenz behalten — kein echter Aufruf mehr im `_savePersonalizedAndStartCall()`-Block
- `.catch()` in fetch ruft `renderStep5()` auf — kein Fallback auf `startCall(true)`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Falsche saveBtn-ID in neu eingefügtem Code**
- **Found during:** Task 2 (Post-Edit Verifikation)
- **Issue:** Plan spezifizierte `lnr-personalize-save-btn` als ID für den Save-Button innerhalb des neuen `.then()`-Blocks — aber die tatsächliche Button-ID im Template ist `lnr-step4c-save` (bereits am Funktionskopf Zeile 648 korrekt deklariert). Doppelte `var saveBtn`-Deklaration wäre ein JS-Fehler im strict mode.
- **Fix:** Redundante `var saveBtn = document.getElementById('lnr-personalize-save-btn')` Zeile entfernt. Bestehender `saveBtn` aus dem Funktionskopf wird im gesamten `.then()`-Block genutzt.
- **Files modified:** `static/pip-launcher.js`
- **Commit:** `e1e80b0` (enthält den Fix)

## Issues Encountered

Keine weiteren.

## User Setup Required

Keine — reine Frontend-Änderung, kein Backend, keine Konfiguration erforderlich.

## Next Phase Readiness

- Plan 02 vollständig: R-01 (Teleprompter Block-Sequenz) + R-04 (Return-Flow) implementiert
- Alle 5 Requirements der Phase 08.19.5.6.1 abgeschlossen: R-01 ✓, R-02 ✓, R-03 ✓, R-04 ✓, R-05 ✓
- Manuelle Browser-Prüfung empfohlen: 4 Reiter selektieren → Teleprompter-Blöcke prüfen; Personalisieren → Button zeigt "Lädt…" → zurück zu Step-5-Ansicht

## Known Stubs

Keine — alle Änderungen wired gegen echte state-Variablen und echte API-Calls.

## Self-Check

- [x] `static/pip-launcher.js` vorhanden und modifiziert
- [x] Commit `895b5ee` existiert (Task 1: R-01)
- [x] Commit `e1e80b0` existiert (Task 2: R-04)
- [x] `[openerText].concat(skriptBlocks)` → kein Treffer (alter Code entfernt)
- [x] `erlaubnisText` → Treffer in `_initTeleprompter()` (Zeilen 2493, 2498, 2513)
- [x] `startCall(true)` → nur Kommentare (Zeile 681/732) + andere Funktionen (Zeile 837/1157), kein echter Aufruf in `_savePersonalizedAndStartCall()`
- [x] `persSkripte.length === 0` + `persOpener.length === 0` → Treffer (Null-Guard vorhanden)
- [x] `saveBtnOrigText` → 4 Treffer (Loading-State + alle Re-Enable-Stellen)
- [x] `lnr-personalize-save-btn` → kein Treffer (falsche ID entfernt)

## Self-Check: PASSED

---
*Phase: 08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish*
*Completed: 2026-05-06*
