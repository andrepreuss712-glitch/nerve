---
phase: 08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish
plan: 01
subsystem: ui
tags: [pip-launcher, renderStep5, vanilla-js, css-tokens, dropdown, preview]

# Dependency graph
requires:
  - phase: 08.19.5.6-4-reiter-ui-skript-opener-auswahl-briefing-skript-merge
    provides: renderStep5() 4-Tab-UI, state.selectedXId, state.openerItems mit type-Feld

provides:
  - Null-Optionen ("— kein/e [Type] —") in allen 4 Reiter-Dropdowns mit value=""
  - Tab-Switch Preview-Trigger: Textfeld sofort befüllt nach Tab-Wechsel
  - Hint-Box permanent über Tab-Nav (Reihenfolge + Skript-Priorität)
  - CSS-Klassen launcher-hint-box und launcher-hint-icon in nerve.css

affects:
  - 08.19.5.6.1-02 (R-01, R-04 — weitere Hotfixes in renderTeleprompter + _savePersonalizedAndStartCall)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Null-Option pattern: value=\"\" als erste Option mit state.selectedXId === null als selected-Bedingung"
    - "IIFE Preview-Trigger nach innerHTML-Assign, vor Event-Wiring-Block"
    - "nerve.css-Token-Pflicht: ausschließlich var(--...) Tokens in neuen CSS-Klassen"

key-files:
  created: []
  modified:
    - static/pip-launcher.js
    - static/nerve.css

key-decisions:
  - "D-03: Bei Tab-Wechsel ohne Selektion zeigt Preview items[0].inhalt kursiv — state.selectedXId bleibt null (kein Auto-Select)"
  - "Hint-Box als statischer JS-String in renderStep5() — kein Toggle, permanent sichtbar (D-01)"
  - "Preview-Trigger als IIFE nach innerHTML, nicht in switchTab5() — konsistenter mit monolithischem renderStep5()-Pattern (D-06)"

patterns-established:
  - "Null-Option: value=\"\" + (state.selectedXId === null ? ' selected' : '') — gleiche Struktur für alle 4 Dropdowns"
  - "Preview-IIFE: steht nach c.innerHTML = [...].join('') und vor dem Event-Wiring-Block"

requirements-completed: [R-02, R-03, R-05]

# Metrics
duration: 20min
completed: 2026-05-06
---

# Phase 08.19.5.6.1 Plan 01: 4-Reiter-UI Hotfixes + UX-Polish (R-02/R-03/R-05) Summary

**Null-Abwahl in allen 4 Reiter-Dropdowns, sofortiger Tab-Switch-Preview und permanente Hint-Box mit Reihenfolge-Erklärung in renderStep5()**

## Performance

- **Duration:** 20 min
- **Started:** 2026-05-06T00:00:00Z
- **Completed:** 2026-05-06
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- R-02: Alle 4 Dropdowns (Opener, Erlaubnisfrage, Pitch, Skript) haben "— kein/e [Type] —" als erste Option mit `value=""` — User kann aktive Auswahl auf null zurücksetzen
- R-03: IIFE nach innerHTML-Assign befüllt Preview-Div sofort beim Tab-Wechsel; bei null-State wird `items[0].inhalt` kursiv angezeigt ohne state zu mutieren
- R-05: Permanente Hint-Box mit ⓘ-Icon direkt über Tab-Nav erklärt Reihenfolge (Opener → Erlaubnisfrage → Pitch/Skript), Skript-Priorität und Null-Option-Bedeutung; CSS ausschließlich mit var(--...) Tokens

## Task Commits

1. **Task 1: Null-Optionen in allen 4 Reiter-Dropdowns (R-02)** - `234843d` (feat)
2. **Task 2: Tab-Switch Preview-Trigger (R-03) + Hint-Box (R-05)** - `eaa42c3` (feat)

## Files Created/Modified

- `static/pip-launcher.js` - renderStep5(): Null-Optionen in opOpts/erlOpts/pitchOpts/skOpts, hintBoxHtml vor tabNavHtml, IIFE Preview-Trigger nach innerHTML
- `static/nerve.css` - neue CSS-Klassen .launcher-hint-box + .launcher-hint-icon mit var(--...) Tokens

## Decisions Made

- Preview-Trigger als IIFE direkt nach `c.innerHTML = [...].join('')` platziert (nicht in switchTab5()) — bleibt konsistent mit monolithischem D-06-Pattern: renderStep5() ist self-contained
- items[0] als Preview-Fallback (kursiv + muted) wenn selId null — state.selectedXId wird nicht gesetzt (D-03 eingehalten)
- hintBoxHtml als Variable vor dem innerHTML-Block gebaut, dann im Array vor tabNavHtml eingefügt — lesbar und debuggbar

## Deviations from Plan

Keine — Plan wurde exakt wie spezifiziert ausgeführt.

## Issues Encountered

Keine.

## User Setup Required

Keine — reine Frontend-Änderung, kein Backend, keine Konfiguration erforderlich.

## Next Phase Readiness

- Plan 01 vollständig: R-02, R-03, R-05 implementiert
- Plan 02 kann starten: R-01 (Teleprompter Block-Builder Erlaubnisfrage/Pitch) + R-04 (Return-Flow nach Personalisierung) noch offen
- Keine Blocker

## Self-Check

- [x] `static/pip-launcher.js` vorhanden und modifiziert
- [x] `static/nerve.css` vorhanden und modifiziert
- [x] Commit `234843d` existiert (Task 1)
- [x] Commit `eaa42c3` existiert (Task 2)
- [x] Keine hardcoded Hex-Farben in .launcher-hint-box / .launcher-hint-icon
- [x] state.selectedXId wird im Preview-Trigger nicht mutiert

## Self-Check: PASSED

---
*Phase: 08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish*
*Completed: 2026-05-06*
