---
phase: 08.19.5.6.1
plan: 02
type: execute
wave: 2
depends_on:
  - 08.19.5.6.1-01
files_modified:
  - static/pip-launcher.js
autonomous: true
requirements:
  - R-01
  - R-04

must_haves:
  truths:
    - "Alle 4 Reiter selektiert → Teleprompter zeigt Opener (Block 0), Erlaubnisfrage (Block 1), Skript-Blöcke (Block 2+)"
    - "Nur Erlaubnisfrage null → Erlaubnisfrage-Block fehlt im Teleprompter (kein leerer Block)"
    - "Nach Personalisierung → Step-5-4-Reiter-Ansicht wird gezeigt, Call startet nicht automatisch"
    - "OD-01 Skript-Priorität bleibt erhalten: selectedSkriptId schlägt selectedPitchId"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "_initTeleprompter() mit erlaubnisText + pitchText Block-Logik + _savePersonalizedAndStartCall() mit renderStep5()-Return-Flow"
      contains: "erlaubnisText|selectedErlaubnisId"
  key_links:
    - from: "_initTeleprompter()"
      to: "state.teleprompterBlocks"
      via: "blocks = [openerText?, erlaubnisText?, ...skriptOrPitchBlocks]"
      pattern: "erlaubnisText"
    - from: "_savePersonalizedAndStartCall() fetch success"
      to: "renderStep5()"
      via: "fetch openerItems reload → filter is_personalized → sort id desc → state setzen → renderStep5()"
      pattern: "renderStep5"
---

<objective>
R-01 (Teleprompter Block-Builder korrekte Sequenz) und R-04 (Personalisierungs-Flow kehrt zu 4-Reiter-Ansicht zurück) implementieren.

Purpose: Behebt Bug "Teleprompter zeigt falsche Block-Sequenz" (R-01, Root-Cause: selectedErlaubnisId + selectedPitchId fehlen im Block-Builder) und UX-Lücke "nach Personalisierung springt App direkt zum Live-Assistenten" (R-04).

Output: _initTeleprompter() baut korrekte Block-Sequenz [openerText?, erlaubnisText?, skriptOrPitchBlocks?]. _savePersonalizedAndStartCall() ruft renderStep5() auf statt startCall(true).
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish\08.19.5.6.1-SPEC.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish\08.19.5.6.1-01-SUMMARY.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish\08.19.5.6.1-CONTEXT.md

<interfaces>
<!-- Aus static/pip-launcher.js — gelesen vor Bearbeitung -->

_initTeleprompter() — Zeile ~2366 (aktuelle fehlerhafte Implementierung):
  // Liest inhalt aus selectedSkriptId (oder _editedSkriptText)
  // Liest openerText aus selectedOpenerId (oder _editedOpenerText)
  // FEHLER: erlaubnisText und pitchText fehlen komplett
  var blocks = openerText ? [openerText].concat(skriptBlocks) : skriptBlocks;
  // ↑ muss ersetzt werden durch korrekte 4-Block-Sequenz

Relevante state-Variablen für R-01:
  state.selectedOpenerId    — null oder Integer
  state.selectedErlaubnisId — null oder Integer (FEHLT aktuell in _initTeleprompter)
  state.selectedPitchId     — null oder Integer (FEHLT aktuell in _initTeleprompter)
  state.selectedSkriptId    — null oder Integer (OD-01: schlägt selectedPitchId)
  state._editedOpenerText   — String oder null (manuell bearbeiteter Opener-Text)
  state._editedSkriptText   — String oder null (manuell bearbeiteter Skript-Text)
  state.openerItems         — Array mit {id, name, inhalt, type: 'opener'|'erlaubnis'|'pitch'}
  state.skripte             — Array mit {id, name, inhalt}

_savePersonalizedAndStartCall() — Zeile ~647 (aktuelle fehlerhafte Implementierung):
  // Nach erfolgreichem Save endet mit:
  _collectEditedTexts();
  startCall(true);   // ← MUSS ersetzt werden durch R-04-Flow

R-04 Reload-Sequenz (per D-04):
  1. fetch('/api/launcher/profile/' + state.activeProfileId)
  2. filter response.opener (oder data.opener) nach is_personalized === true
  3. sort by id desc → [0] = neuestes personalisiertes Item
  4. Modus-abhängige State-Zuweisung:
     - Cold-Call (state.mode !== 'meeting'): state.selectedOpenerId = newest.id; state.activeTab = 'opener';
     - Meeting (state.mode === 'meeting'): state.selectedSkriptId = newest.id; state.activeTab = 'skript';
  5. renderStep5() aufrufen — kein startCall(true)

API-Response von /api/launcher/profile/{id} (bekanntes Pattern aus renderStep5 profileSel5.onchange):
  data.opener — Array mit opener-Items (enthält is_personalized-Feld seit Phase 08.19.5.6/08.20.3)
  data.skripte — Array mit Skript-Items
  data.id, data.daten, data.skripte
  Achtung: data.opener enthält Items aller types (opener/erlaubnis/pitch) — filter nach is_personalized=true
  Für Cold-Call personalisiert → Item ist type='opener'; für Meeting → Item ist in data.skripte

R-04 Fallback (aus SPEC.md Constraint):
  Falls API-Response kein is_personalized-Feld liefert oder Filter leer → renderStep5() trotzdem aufrufen
  (kein startCall(true) auch im Fallback-Fall)

OD-01 bleibt erhalten (unverändert):
  var _resolvedTeleprompterSkript = ... // bereits vorhanden — NICHT anfassen
  Teleprompter liest inhalt via _resolvedTeleprompterSkript (selectedSkriptId > selectedPitchId)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teleprompter Block-Builder korrekte Sequenz (R-01)</name>
  <read_first>
    - static/pip-launcher.js (Zeile 2366–2425, _initTeleprompter() komplett lesen)
    - static/pip-launcher.js (Zeile 1–60, state-Initialisierung für selectedErlaubnisId-Initialwert prüfen)
  </read_first>
  <files>static/pip-launcher.js</files>
  <action>
In `_initTeleprompter()` — den Block-Builder-Abschnitt (Zeile ~2385 bis ~2397) ersetzen.

**Aktuell (fehlerhaft):**
```js
// D-03: Opener als erster Teleprompter-Block
var openerText = '';
if (state._editedOpenerText) {
  openerText = state._editedOpenerText;
} else if (state.selectedOpenerId && state.openerItems && state.openerItems.length > 0) {
  var selOp = state.openerItems.find(function (o) { return o.id === state.selectedOpenerId; });
  if (selOp) openerText = selOp.inhalt;
}

var skriptBlocks = inhalt.split(/\n\n+/).filter(function (b) { return b.trim(); });
var blocks = openerText ? [openerText].concat(skriptBlocks) : skriptBlocks;
```

**Neu (korrekt):**
```js
// R-01: Block-Sequenz [openerText?, erlaubnisText?, skriptOrPitchBlocks?]
// Opener (Block 0)
var openerText = '';
if (state._editedOpenerText) {
  openerText = state._editedOpenerText;
} else if (state.selectedOpenerId && state.openerItems && state.openerItems.length > 0) {
  var selOp = state.openerItems.find(function (o) { return o.id === state.selectedOpenerId; });
  if (selOp) openerText = selOp.inhalt || '';
}

// Erlaubnisfrage (Block 1) — NEU: selectedErlaubnisId aus openerItems (type='erlaubnis')
var erlaubnisText = '';
if (state.selectedErlaubnisId && state.openerItems && state.openerItems.length > 0) {
  var selErl = state.openerItems.find(function (o) {
    return o.id === state.selectedErlaubnisId && o.type === 'erlaubnis';
  });
  if (selErl) erlaubnisText = selErl.inhalt || '';
}

// Skript-oder-Pitch-Blöcke (OD-01 bleibt erhalten via inhalt-Variable oben)
// inhalt ist bereits durch den OD-01-Block am Anfang von _initTeleprompter() aufgelöst:
// _editedSkriptText > selectedSkriptId > (Pitch wenn selectedSkriptId null: selectedPitchId)
// HINWEIS: Wenn inhalt leer und weder Skript noch Pitch selektiert → früher return bereits aktiv
var skriptBlocks = inhalt.split(/\n\n+/).filter(function (b) { return b.trim(); });

// Blöcke zusammensetzen — nur nicht-leere Strings werden eingefügt
var blocks = [];
if (openerText && openerText.trim()) blocks.push(openerText.trim());
if (erlaubnisText && erlaubnisText.trim()) blocks.push(erlaubnisText.trim());
blocks = blocks.concat(skriptBlocks);
```

WICHTIG: Der "früher return"-Block am Anfang von _initTeleprompter() (Zeile ~2379) prüft `if (!inhalt || !inhalt.trim())` — dieser Block bleibt unverändert. Der frühe Return gilt nur wenn kein Skript/Pitch selektiert. Falls nur Opener + Erlaubnis selektiert aber kein Skript/Pitch → inhalt ist leer → früher Return → Teleprompter zeigt "Kein Skript hinterlegt". Das ist FALSCH für reine Opener+Erlaubnis-Situationen.

ANPASSUNG des frühen Returns (Zeile ~2379):
Den frühen Return-Guard erweitern: Teleprompter soll auch angezeigt werden wenn Opener ODER Erlaubnisfrage selektiert (auch ohne Skript/Pitch):

```js
// Opener oder Erlaubnisfrage vorhanden auch ohne Skript/Pitch?
var hasOpener = !!(state._editedOpenerText || state.selectedOpenerId);
var hasErlaubnis = !!state.selectedErlaubnisId;

if (!inhalt || !inhalt.trim()) {
  // Kein Skript/Pitch — aber Opener oder Erlaubnisfrage können trotzdem gezeigt werden
  if (!hasOpener && !hasErlaubnis) {
    container.innerHTML = '<div class="tp-empty">Kein Skript hinterlegt — Profil bearbeiten</div>';
    state.teleprompterBlocks = [];
    return;
  }
  // Nur Opener/Erlaubnis ohne Skript — weiter mit leerem inhalt, skriptBlocks = []
}
```

Dann weiter mit dem Block-Builder wie oben (skriptBlocks = [] wenn inhalt leer, blocks enthält nur Opener+Erlaubnis).

ACHTUNG OD-01: Das Pitch-als-eigenständiger-Block-Szenario ("Nur Pitch selektiert, kein Skript") ist durch den bestehenden OD-01-Block bereits abgedeckt: `inhalt` wird am Anfang von _initTeleprompter() auf den Pitch-Text gesetzt wenn selectedSkriptId null aber selectedPitchId gesetzt. In diesem Fall erscheint Pitch als `skriptBlocks` (in inhalt aufgelöst) — OD-01 bleibt erhalten, kein weiterer Pitch-Block nötig.
  </action>
  <verify>
    <automated>
      grep -n "erlaubnisText\|selectedErlaubnisId\|erlaubnis" C:\Users\andre\dev\salesnerve\static\pip-launcher.js | grep -v "renderStep5\|switchTab5\|state\.selectedErlaubnisId =\|lnr-opener"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "erlaubnisText" static/pip-launcher.js` → findet Treffer in _initTeleprompter() (nicht nur in renderStep5)
    - `grep "selectedErlaubnisId" static/pip-launcher.js | grep "_initTeleprompter\|erlaubnisText\|selErl"` → mindestens 1 Treffer im _initTeleprompter()-Bereich
    - Der Block-Builder verwendet `blocks = []; if (openerText) blocks.push(...); if (erlaubnisText) blocks.push(...); blocks = blocks.concat(skriptBlocks)` — keine direkte Concatenation mit [openerText].concat(skriptBlocks) mehr
    - `grep "\[openerText\]\.concat(skriptBlocks)" static/pip-launcher.js` → kein Treffer (alter fehlerhafter Code entfernt)
    - `grep "hasOpener\|hasErlaubnis" static/pip-launcher.js` → Treffer im frühen-Return-Guard-Block von _initTeleprompter()
    - OD-01 erhalten: `grep "_resolvedTeleprompterSkript\|selectedSkriptId.*selectedPitchId\|selectedPitchId.*selectedSkriptId" static/pip-launcher.js` → bestehende OD-01-Logik unverändert vorhanden
  </acceptance_criteria>
  <done>Teleprompter zeigt korrekte Block-Sequenz: Opener (Block 0), Erlaubnisfrage (Block 1), Skript-Blöcke (Block 2+). Null-Selektionen erzeugen keine Blöcke. OD-01 (Skript > Pitch) unverändert.</done>
</task>

<task type="auto">
  <name>Task 2: Personalisierungs-Flow kehrt zu renderStep5() zurück (R-04)</name>
  <read_first>
    - static/pip-launcher.js (Zeile 646–700, _savePersonalizedAndStartCall() komplett lesen)
    - static/pip-launcher.js (Zeile 964–993, profileSel5.onchange als Referenz-Pattern für /api/launcher/profile/ fetch)
  </read_first>
  <files>static/pip-launcher.js</files>
  <action>
In `_savePersonalizedAndStartCall()` — den `.then(function(data) { ... })` Block nach erfolgreichem Save (Zeile ~670-690) ersetzen.

**Aktuell (fehlerhaft, Zeile ~682-690):**
```js
// Modus-abhängig: Meeting → _editedSkriptText; Cold-Call → _editedOpenerText
if (state.mode === 'meeting') {
  state._editedSkriptText  = state._personalizedSkriptText || "";
} else {
  state._editedOpenerText  = state._personalizedSkriptText || "";
}
state._personalizedSkriptText = null;
state._personalizeAbortController = null;
_collectEditedTexts();
startCall(true);    // ← ERSETZEN durch R-04-Flow
```

**Neu (korrekt, per D-04):**
```js
// R-04: Zurück zu Step-5-4-Reiter-Ansicht statt startCall(true)
// _editedText bereinigen (nicht mehr benötigt — personalisierter Text ist jetzt in DB)
state._personalizedSkriptText = null;
state._personalizeAbortController = null;

// Neuestes personalisiertes Item laden und selektieren
fetch('/api/launcher/profile/' + state.activeProfileId)
  .then(function (r) {
    if (!r.ok) throw new Error('profile load failed: ' + r.status);
    return r.json();
  })
  .then(function (profileData) {
    // openerItems und skripte aktualisieren
    state.openerItems = profileData.opener || [];
    state.skripte     = profileData.skripte || [];

    if (state.mode === 'meeting') {
      // Meeting: neuestes personalisiertes Skript selektieren
      var persSkripte = (profileData.skripte || []).filter(function (s) { return s.is_personalized; });
      persSkripte.sort(function (a, b) { return b.id - a.id; });
      if (persSkripte.length > 0) {
        state.selectedSkriptId = persSkripte[0].id;
      }
      state.activeTab = 'skript';
    } else {
      // Cold-Call: neuestes personalisiertes Opener selektieren
      var persOpener = (profileData.opener || []).filter(function (o) {
        return o.type === 'opener' && o.is_personalized;
      });
      persOpener.sort(function (a, b) { return b.id - a.id; });
      if (persOpener.length > 0) {
        state.selectedOpenerId = persOpener[0].id;
      }
      state.activeTab = 'opener';
    }
    state.step = 5;
    renderStep5();
  })
  .catch(function () {
    // Fallback: renderStep5() auch ohne Reload aufrufen — kein startCall(true)
    state.activeTab = state.mode === 'meeting' ? 'skript' : 'opener';
    state.step = 5;
    renderStep5();
  });
```

WICHTIG: Die Zeilen `state._editedSkriptText = ...` und `state._editedOpenerText = ...` (bisherige Seiteneffekte) werden entfernt. Der personalisierte Text ist jetzt in der DB gespeichert und wird via openerItems/skripte neu geladen — `_editedText`-State ist nach dem Save nicht mehr nötig.

`_collectEditedTexts()` wird NICHT aufgerufen (kein startCall(true) mehr).

Der `saveBtn` disabled-State wird am Anfang der Funktion bereits gesetzt — nach dem fetch-Abschluss (egal ob Erfolg oder Catch) ist der Button nicht mehr sichtbar (wir sind in renderStep5). Kein explizites re-enable des Buttons nötig.

`state.step = 5` setzen damit `renderStep()` (falls aufgerufen) zu Step 5 springt — redundant bei direktem renderStep5()-Aufruf aber defensiv korrekt.
  </action>
  <verify>
    <automated>
      grep -n "renderStep5\(\)\|startCall(true)\|R-04\|is_personalized" C:\Users\andre\dev\salesnerve\static\pip-launcher.js | grep -A2 -B2 "savePersonalized\|renderStep5"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "startCall(true)" static/pip-launcher.js` → im _savePersonalizedAndStartCall()-Block kein Treffer mehr (startCall(true) wurde durch renderStep5()-Aufruf ersetzt)
    - `grep "is_personalized" static/pip-launcher.js` → mindestens 1 Treffer im _savePersonalizedAndStartCall()-Bereich (Filter auf is_personalized === true)
    - `grep "renderStep5()" static/pip-launcher.js` → Treffer im _savePersonalizedAndStartCall()-Bereich (sowohl im .then() als auch im .catch())
    - `grep "persOpener\|persSkripte" static/pip-launcher.js` → Treffer im _savePersonalizedAndStartCall()-Block (Reload-Variablen vorhanden)
    - `grep "activeTab.*skript\|activeTab.*opener" static/pip-launcher.js | grep "savePersonalized\|persOp\|persSkript\|mode.*meeting\|catch"` → Treffer (state.activeTab wird gesetzt)
    - Im .catch()-Block steht `renderStep5()` — kein `startCall(true)` als Fallback
    - `grep "_editedOpenerText.*_personalizedSkriptText\|_editedSkriptText.*_personalizedSkriptText" static/pip-launcher.js` → kein Treffer mehr im _savePersonalizedAndStartCall()-Block (bisherige Seiteneffekte entfernt)
  </acceptance_criteria>
  <done>Nach Personalisierungs-Save: Profil-Reload, neuestes personalisiertes Item selektiert, renderStep5() aufgerufen. Call startet nicht automatisch. User sieht 4-Reiter-Ansicht mit personalisierten Item selektiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/launcher/profile/{id} → state | API-Response-Daten werden direkt in state.openerItems/skripte geschrieben; IDs werden als integers in selectedXId gesetzt |
| state.selectedXId → _initTeleprompter() | Client-seitige state-Variable; keine server-side Validation des Block-Builders nötig (rein UI-Logik) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.19.5.6.1-05 | Tampering | state.selectedErlaubnisId in _initTeleprompter() | accept | State ist client-seitig; Teleprompter-Inhalt hat keine server-side Auswirkung; inhalt kommt aus eigenem Profil-Material |
| T-08.19.5.6.1-06 | Elevation of Privilege | /api/launcher/profile/{id} in R-04 Reload | mitigate | Endpoint ist bereits authenticated (login_required); profile_id = state.activeProfileId (user-owned); server-side validiert org_id-Ownership — kein neues Risiko durch Client-Aufruf |
| T-08.19.5.6.1-07 | Spoofing | is_personalized-Filter im Client | accept | Filter ist UI-Convenience (welches Item vorselektiert wird); kein security-kritischer Pfad; Server validiert Ownership bei jedem API-Call |
| T-08.19.5.6.1-08 | Denial of Service | fetch in _savePersonalizedAndStartCall() catch-Branch | accept | Catch ruft renderStep5() auf (kein unendlicher Loop); fetch-Fehler führen zu graceful fallback auf Step-5-Ansicht |
</threat_model>

<verification>
Nach Ausführung beider Tasks prüfen:

1. `grep -c "startCall(true)" static/pip-launcher.js` → Anzahl der Treffer überprüfen — im _savePersonalizedAndStartCall()-Block sollte kein Treffer mehr sein (andere startCall-Aufrufe im File bleiben)
2. `grep "erlaubnisText" static/pip-launcher.js | grep -v "renderStep5"` → Treffer in _initTeleprompter()-Bereich
3. `grep "\[openerText\]\.concat" static/pip-launcher.js` → kein Treffer (alter Code entfernt)
4. `grep "is_personalized" static/pip-launcher.js` → Treffer im _savePersonalizedAndStartCall()-Block
5. Manuelle Browser-Prüfung R-01: Alle 4 Reiter selektieren → Teleprompter zeigt Opener (Block 0), Erlaubnisfrage (Block 1), Skript (Block 2+)
6. Manuelle Browser-Prüfung R-04: Personalisieren + "Personalisiert nutzen" klicken → zurück zu Step-5-Ansicht (nicht zum Live-Assistenten)
</verification>

<success_criteria>
- Teleprompter Block-Sequenz korrekt: [openerText?, erlaubnisText?, skriptOrPitchBlocks?]
- Erlaubnisfrage null → kein Erlaubnisfrage-Block im Teleprompter
- OD-01 unverändert: Skript-Priorität über Pitch erhalten
- Nach Personalisierung → 4-Reiter-Ansicht mit neuem Item selektiert; kein automatischer Call-Start
- R-04 Fallback: auch bei Reload-Fehler → renderStep5() aufgerufen (nicht startCall)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.19.5.6.1-4-reiter-ui-hotfixes-ux-polish/08.19.5.6.1-02-SUMMARY.md` erstellen.
</output>
