---
phase: 08.23.2.C
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - deploy.sh
  - scripts/verify_corpus_gate.py
  - scripts/gliner_smoke.py
  - tests/fixtures/phase_classifier_corpus.schema.json
  - tests/fixtures/gatekeeper_classifier_corpus.schema.json
  - tests/fixtures/gatekeeper_phrases_seed.md
autonomous: false
requirements: [Req-1]
tags: [nerve, gliner, ner, anonymization, gatekeeper, corpus-gate]
must_haves:
  truths:
    - "gliner Python-Package ist als Dependency in requirements.txt"
    - "deploy.sh enthält GLiNER-Modell-Vorab-Download"
    - "Pre-Execute-Gate-Skript scripts/verify_corpus_gate.py existiert und prüft beide Korpus-JSON-Dateien"
    - "Korpus-Schema-Dateien definieren das erwartete JSON-Format pro Eintrag"
    - "tests/fixtures/gatekeeper_phrases_seed.md enthält Phrase-Entwurf für 4 Mr.-Miyagi-Buttons mit Quellen-Attribut"
    - "GLiNER-Smoke-Skript verifiziert Return-Format von predict_entities() experimentell"
  artifacts:
    - path: "requirements.txt"
      provides: "gliner Package-Pinning"
      contains: "gliner"
    - path: "deploy.sh"
      provides: "GLiNER-Modell-Vorab-Download für deterministische App-Start-Zeit"
      contains: "gliner"
    - path: "scripts/verify_corpus_gate.py"
      provides: "Pre-Execute-Gate für Korpus-Dateien"
    - path: "scripts/gliner_smoke.py"
      provides: "Latenz + Return-Format-Smoke für GLiNER (Open Question 1)"
    - path: "tests/fixtures/phase_classifier_corpus.schema.json"
      provides: "JSON-Schema für 20 Phasen-Sequenzen"
    - path: "tests/fixtures/gatekeeper_classifier_corpus.schema.json"
      provides: "JSON-Schema für 10 Gatekeeper-Snippets"
    - path: "tests/fixtures/gatekeeper_phrases_seed.md"
      provides: "Phrase-Entwurf vor Andre-Gate"
  key_links:
    - from: "deploy.sh"
      to: "gliner package + Modell-Cache"
      via: "pip install + from_pretrained()-Warmup"
      pattern: "gliner"
    - from: "scripts/verify_corpus_gate.py"
      to: "tests/fixtures/*_corpus.json"
      via: "File-Existence + JSON-Schema-Validation + Min-Anzahl"
      pattern: "phase_classifier_corpus|gatekeeper_classifier_corpus"
---

<objective>
Foundation für Phase 08.23.2.C: GLiNER als Dependency installieren, Deploy-Pipeline auf Modell-Download erweitern, Pre-Execute-Gate für die zwei async-erstellten Test-Korpora bauen, GLiNER-Smoke-Skript zur Klärung der offenen Frage (Return-Format mit/ohne Offsets, Latenz auf CX32) erstellen, und Mr.-Miyagi-Phrase-Entwurf für André's Andre-Gate vorbereiten.

Purpose: Wave 1 deblockiert alle nachfolgenden Waves: Wave 2 (anonymization.py + classify_phase()) braucht GLiNER lauffähig; Wave 5 (Tests) braucht die Korpus-Gate-Logik; P02 (Migration) braucht den bestätigten Phrase-Seed.

Output: GLiNER installierbar + vorab-downloadbar, Pre-Execute-Gate-Skript, Korpus-Schema-Dateien, GLiNER-Smoke-Skript, Phrase-Entwurf-Markdown für Andre-Gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md
@Nerve-Vault/03 Planung/gatekeeper detection.md
@requirements.txt
@deploy.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: GLiNER-Dependency + Deploy-Modell-Warmup</name>
  <files>requirements.txt, deploy.sh</files>
  <read_first>
    - requirements.txt (aktueller Stand — spacy>=3.7.0, phonenumbers>=8.13.0 bereits aus Phase B vorhanden)
    - deploy.sh (Phase B hat de_core_news_lg-Download in Z.82 eingebaut — analoges Pattern)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Sektion "Environment Availability" + "Standard Stack")
  </read_first>
  <action>
    Konkreter Schritt 1 — requirements.txt erweitern:
    - Neue Zeile hinzufügen: `gliner>=0.2.24`  (per RESEARCH.md Standard Stack — einzige offizielle GLiNER-Library)
    - Pin als Range-Pin (≥0.2.24), nicht hartes ==-Pin — Patch-Updates akzeptabel
    - Reihenfolge: alphabetisch in bestehende requirements.txt einsortieren

    Konkreter Schritt 2 — deploy.sh erweitern (analog Phase B de_core_news_lg-Download):
    - Nach `python -m spacy download de_core_news_lg`-Zeile (vorhanden seit Phase B Plan 02): neuen Block einfügen:
    ```bash
    # GLiNER Modell vorab cachen (Phase 08.23.2.C — Req-1)
    python -c "from gliner import GLiNER; GLiNER.from_pretrained('urchade/gliner_multi-v2.1')" || \
        echo "[deploy] GLiNER-Pre-Download fehlgeschlagen — App startet trotzdem, lädt beim ersten Request"
    ```
    - Nicht-fatal: `|| echo ...` per RESEARCH.md "Missing dependencies ohne Fallback" — App soll auch ohne Pre-Cache starten, GLiNER lädt sonst beim ersten Anonymisierungs-Aufruf (~450MB Download).

    Konkreter Schritt 3 — Verifikation in der Plan-Phase NICHT lokal ausführen:
    - GLiNER lokal installieren ist nicht Plan-Pflicht. Plan-Task installiert NUR auf Server beim nächsten Deploy.
  </action>
  <verify>
    <automated>grep -q "^gliner" requirements.txt && grep -q "GLiNER.from_pretrained" deploy.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt enthält genau eine Zeile beginnend mit `gliner` (case-sensitive)
    - requirements.txt hat keine doppelte `gliner`-Zeile (`grep -c "^gliner" requirements.txt` == 1)
    - deploy.sh enthält String `urchade/gliner_multi-v2.1` (vollständige Modell-ID)
    - deploy.sh enthält `from gliner import GLiNER` (Python-Pre-Cache-Snippet)
    - deploy.sh GLiNER-Download nicht-fatal: enthält `||` nach python -c Aufruf
  </acceptance_criteria>
  <done>Dependency und Deploy-Hook für GLiNER sind verdrahtet.</done>
</task>

<task type="auto">
  <name>Task 2: GLiNER-Smoke-Skript + Korpus-Gate + Korpus-Schemas</name>
  <files>scripts/gliner_smoke.py, scripts/verify_corpus_gate.py, tests/fixtures/phase_classifier_corpus.schema.json, tests/fixtures/gatekeeper_classifier_corpus.schema.json</files>
  <read_first>
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Open Questions 1-2 + Common Pitfalls 1-2 + "Code Examples" mit JSON-Schemas)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md (D-04 Pre-Execute-Gate)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-12, Req-13 Acceptance)
  </read_first>
  <action>
    Konkreter Schritt 1 — scripts/gliner_smoke.py:
    - Standalone-Script (kein pytest), per `python scripts/gliner_smoke.py` aufrufbar.
    - Lädt `GLiNER.from_pretrained('urchade/gliner_multi-v2.1')` (timed, gibt Lade-Sekunden aus).
    - Führt 100 Inferenzen mit Test-Text `"Hallo Herr Müller, ich rufe wegen Siemens AG an."` durch labels `['person', 'organisation', 'location']`.
    - Gibt aus:
      - Lade-Zeit in Sekunden
      - Min/Avg/P95-Latenz über 100 Runs in ms
      - Return-Format-Dump für ersten Run: `print(repr(entities))` — KLÄRT Open Question 1 (hat Return optional `start`/`end` Felder?)
    - Exit-Code 0 immer (Diagnose-Skript, kein Test-Gate).
    - Kein Import in app.py — pure Standalone-Diagnostik.

    Konkreter Schritt 2 — scripts/verify_corpus_gate.py:
    - Standalone-Script (Exit-Code 0 wenn alle Checks grün, sonst 1 mit Fehlermeldung pro fehlender Bedingung).
    - Prüft Existenz:
      - `tests/fixtures/phase_classifier_corpus.json`
      - `tests/fixtures/gatekeeper_classifier_corpus.json`
    - JSON-Schema-Validation pro Eintrag (manuell, kein jsonschema-Package nötig):
      - phase_classifier: jeder Eintrag muss Keys `transcript_window` (List[str]), `mode` (in {'cold_call','meeting','gatekeeper'}), `expected_phase` (int 1..6) haben
      - gatekeeper_classifier: jeder Eintrag muss Keys `transcript_window` (List[str]), `briefing_ceo_name` (str), `expected_category` (in {'target','gatekeeper','unknown'}) haben
    - Mindest-Anzahlen: ≥20 phase-Einträge, ≥10 gatekeeper-Einträge (per Req-12, Req-13).
    - Print-Format pro Check: `[CORPUS-GATE] OK: <check>` oder `[CORPUS-GATE] FEHLER: <reason>`.
    - main() druckt Summary am Ende: `[CORPUS-GATE] ALLE CHECKS GRÜN` oder `[CORPUS-GATE] BLOCKIERT: <count> Fehler`.

    Konkreter Schritt 3 — Korpus-Schema-JSON-Dateien:
    - `tests/fixtures/phase_classifier_corpus.schema.json` — JSON-Schema-Draft (informell, zum Lesen für André):
    ```json
    {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "array",
      "minItems": 20,
      "items": {
        "type": "object",
        "required": ["transcript_window", "mode", "expected_phase"],
        "properties": {
          "transcript_window": {"type": "array", "items": {"type": "string"}, "minItems": 1},
          "mode": {"enum": ["cold_call", "meeting", "gatekeeper"]},
          "expected_phase": {"type": "integer", "minimum": 1, "maximum": 6}
        }
      }
    }
    ```
    - `tests/fixtures/gatekeeper_classifier_corpus.schema.json`:
    ```json
    {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "array",
      "minItems": 10,
      "items": {
        "type": "object",
        "required": ["transcript_window", "briefing_ceo_name", "expected_category"],
        "properties": {
          "transcript_window": {"type": "array", "items": {"type": "string"}, "minItems": 1},
          "briefing_ceo_name": {"type": "string"},
          "expected_category": {"enum": ["target", "gatekeeper", "unknown"]}
        }
      }
    }
    ```
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/verify_corpus_gate.py').read()); ast.parse(open('scripts/gliner_smoke.py').read())" && python -c "import json; json.load(open('tests/fixtures/phase_classifier_corpus.schema.json')); json.load(open('tests/fixtures/gatekeeper_classifier_corpus.schema.json'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/verify_corpus_gate.py` exit-code 1 (Korpora existieren noch nicht — erwartet)
    - `python scripts/verify_corpus_gate.py` druckt mindestens einmal `[CORPUS-GATE] FEHLER`
    - scripts/gliner_smoke.py enthält String `predict_entities` und `from_pretrained`
    - scripts/gliner_smoke.py enthält String `time.perf_counter` oder `timeit` (Latenz-Messung)
    - Beide Schema-JSONs sind syntaktisch valides JSON (json.load() exit 0)
    - Beide Schemas haben `"minItems": 20` (phase) und `"minItems": 10` (gatekeeper)
  </acceptance_criteria>
  <done>Korpus-Gate-Skript blockiert wenn Korpora fehlen; GLiNER-Smoke-Skript ist lauffähig; Schemas sind dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 3: Mr.-Miyagi-Phrase-Entwurf für Andre-Gate</name>
  <files>tests/fixtures/gatekeeper_phrases_seed.md</files>
  <read_first>
    - Nerve-Vault/03 Planung/gatekeeper detection.md (Abschnitte B.6, B.7, Bonus-Block — per CONTEXT.md D-05 sind das die Quellen)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md (D-05: 2-3 Varianten pro Button, Template-Variablen, Source-Attribution)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-9: 4 Buttons — Verbündeten-Bitte, Insider-Antwort, Voss-Label, Vornamen-Pause)
  </read_first>
  <action>
    Tests/fixtures/gatekeeper_phrases_seed.md anlegen als gut lesbares Markdown-Dokument für André's Review.

    Struktur des Dokuments:
    ```markdown
    # Gatekeeper-Phrases Seed-Entwurf (Phase 08.23.2.C)

    **Andre-Gate:** Bitte vor Execute-Start prüfen und ggf. editieren.
    Plan-Author hat aus `Nerve-Vault/03 Planung/gatekeeper detection.md` (Abschnitte B.6, B.7, Bonus-Block) extrahiert.

    **Template-Variablen:** {branche}, {detail}, {vorname}, {nachname} — werden im PiP aus Briefing-Daten + classify_contact()-Output gefüllt.

    ---

    ## Button 1: "Verbündeten-Bitte"
    *Source: Limbeck — "Sekretärin als Verbündete"*

    Varianten (≥2-3):
    1. "{vorname}, dürfte ich Sie um Ihre Einschätzung bitten — wer wäre bei Ihnen für {branche} der richtige Ansprechpartner?"
    2. "Frau {nachname}, Sie kennen das Haus am besten — bei wem soll ich anfragen wegen {detail}?"
    3. "Sie wissen sicher am besten, wer bei Ihnen für {branche} verantwortlich ist — können Sie mir kurz helfen?"

    ## Button 2: "Insider-Antwort"
    *Source: Heinrich-Hypothese — "Klingt wie Insider, nicht wie Sales"*

    Varianten:
    1. "Es geht um {detail} — könnten Sie mich bitte mit der zuständigen Person verbinden?"
    2. "Wir hatten letzte Woche bereits Kontakt zu {branche}-Themen — wer hat das damals begleitet?"
    3. "Konkret: {detail}. Wer ist bei Ihnen dafür der richtige Ansprechpartner?"

    ## Button 3: "Voss-Label"
    *Source: Voss-Label — "Es klingt so, als ob..."*

    Varianten:
    1. "Es klingt so, als ob Sie das tagtäglich filtern müssen — was wäre der einfachste Weg, mich kurz durchzustellen?"
    2. "Es scheint, als hätten Sie viele solcher Anfragen — was bräuchten Sie, damit Sie sich sicher fühlen mich weiterzuleiten?"

    ## Button 4: "Vornamen-Pause"
    *Source: Limbeck — Vornamenspause, Pattern-Interrupt*

    Varianten:
    1. "{vorname} ... (Pause) ... ich brauche genau zwei Minuten."
    2. "{vorname} — kurz: {detail}. Reicht das für Ihre Einschätzung?"

    ---

    **Insert-Format für Alembic 0003 (zur Information, nicht für André):**
    Pro Variante eine Row in `phrases` mit: text, mode='gatekeeper', user_id=1 (Admin-MVP-Lösung per RESEARCH.md A7), objection_type='gatekeeper_button_<N>', quality_tier='A'.
    ```

    Konkrete Inhalte aus Vault extrahieren — nicht erfinden. Wenn Vault-Datei eine Variante nicht hergibt, MERKE als "TODO André prüfen" in der Variante.
  </action>
  <verify>
    <automated>test -s tests/fixtures/gatekeeper_phrases_seed.md && grep -c "## Button" tests/fixtures/gatekeeper_phrases_seed.md | grep -qx 4 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Datei tests/fixtures/gatekeeper_phrases_seed.md existiert und ist nicht leer
    - Genau 4 Buttons (`## Button 1`, `## Button 2`, `## Button 3`, `## Button 4`)
    - Pro Button ≥2 Varianten (nummerierte Liste-Einträge nach "Varianten:")
    - Pro Button eine `*Source:`-Zeile (Quellen-Attribution per D-05)
    - Mindestens eine Template-Variable je `{branche}`, `{vorname}`, `{nachname}`, `{detail}` ist im Gesamt-Dokument enthalten
  </acceptance_criteria>
  <done>Phrase-Entwurf liegt für Andre-Gate bereit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Andre-Gate — Phrase-Entwurf bestätigen + Korpus-Erstellung anstoßen</name>
  <what-built>
    Plan-Author hat tests/fixtures/gatekeeper_phrases_seed.md mit 4 Mr.-Miyagi-Buttons (≥2 Varianten je) aus Vault-Recherche extrahiert.
    Korpus-Schemas (tests/fixtures/*_corpus.schema.json) sind erstellt und definieren das JSON-Format für die zwei Test-Korpora.
    Pre-Execute-Gate-Skript scripts/verify_corpus_gate.py ist lauffähig und blockiert solange Korpora fehlen.
  </what-built>
  <how-to-verify>
    1. Öffne `tests/fixtures/gatekeeper_phrases_seed.md` und review die Phrase-Varianten:
       - Sind die Varianten realistisch für DACH B2B Cold-Call Gatekeeper-Szenarien?
       - Sind die Source-Attributionen korrekt (Limbeck, Voss, Heinrich)?
       - Edit Markdown direkt, wenn Änderungen nötig — Migration P02 nimmt die bestätigte Version.
    2. Beginne Korpus-Erstellung asynchron via claude.ai Chat-Sparring (separate Session):
       - NERVE-ICP-Kontext: KMU-Solo-Sales / Mittelstand-Vertriebsleiter, B2B-Cold-Calls, Deutsche Sprache
       - Erstelle `tests/fixtures/phase_classifier_corpus.json` (20 Sequenzen, Schema: siehe phase_classifier_corpus.schema.json)
       - Erstelle `tests/fixtures/gatekeeper_classifier_corpus.json` (10 Snippets, Schema: siehe gatekeeper_classifier_corpus.schema.json)
       - Pro Eintrag: realistische Berater-Sätze, manuelle Korrektur für Realismus.
       - Commit beide JSONs in `salesnerve/tests/fixtures/` bevor Execute der Tests-Plans (Wave 5) startet.
    3. Optional: Auf Server `python scripts/gliner_smoke.py` ausführen → Lade-Zeit + P95-Latenz + Return-Format-Dump notieren (entscheidet Implementierung in P04 Task 1).
  </how-to-verify>
  <resume-signal>
    Antworte:
    - "phrase-seed approved" wenn die Phrase-Varianten OK sind (Migration P02 nutzt die aktuelle Datei).
    - "phrase-seed edits done" wenn du in der Datei editiert hast.
    - Korpus-Erstellung läuft parallel — verify_corpus_gate.py blockiert Wave 5 (Tests) automatisch.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy.sh → externe `pip install gliner` | Untrusted package download from PyPI |
| GLiNER-Modell-Download → huggingface.co | Untrusted model weights |
| tests/fixtures/*.json → pytest | Untrusted fixture data (André schreibt, andere lesen) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.C-01 | Tampering | requirements.txt gliner-Pin | accept | Standard pip-Trust-Modell; gliner ist offizielles Package des Autors; Range-Pin ≥0.2.24 erlaubt Security-Updates |
| T-08.23.2.C-02 | Availability | GLiNER from_pretrained() blockiert App-Start bei Netzwerk-Ausfall | mitigate | deploy.sh-Hook ist nicht-fatal (`|| echo ...`); App-Start lädt sonst beim ersten Request (is_pipeline_healthy=False als Fallback) |
| T-08.23.2.C-03 | Information Disclosure | gliner_smoke.py mit Test-Strings auf Server | accept | Test-String `Hallo Herr Müller, Siemens AG` ist generisch, keine reale PII |
| T-08.23.2.C-04 | Tampering | tests/fixtures/*_corpus.json — manipulierte Test-Daten | accept | Korpora sind Test-Fixtures, nicht in Production-Pfad; Gate-Skript validiert nur Schema, nicht Inhalt |
</threat_model>

<verification>
- requirements.txt enthält gliner-Pin
- deploy.sh hat GLiNER-Pre-Cache mit nicht-fatalem Fallback
- scripts/verify_corpus_gate.py blockiert solange Korpora fehlen
- scripts/gliner_smoke.py ist lauffähig auf Server (Open Question 1 klärbar)
- tests/fixtures/gatekeeper_phrases_seed.md wurde von André approved oder editiert
</verification>

<success_criteria>
- `grep -c "^gliner" requirements.txt` == 1
- `grep -q "urchade/gliner_multi-v2.1" deploy.sh` exit 0
- `python -m py_compile scripts/verify_corpus_gate.py scripts/gliner_smoke.py` exit 0
- `python scripts/verify_corpus_gate.py` exit 1 (erwartet — Korpora fehlen noch)
- 4 Buttons + ≥2 Varianten in gatekeeper_phrases_seed.md
- Andre-Gate-Bestätigung im Resume-Signal
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-01-SUMMARY.md`
</output>
