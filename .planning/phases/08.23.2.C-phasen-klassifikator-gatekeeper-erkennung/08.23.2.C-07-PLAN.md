---
phase: 08.23.2.C
plan: 07
type: execute
wave: 4
depends_on: [06, 06b]
files_modified:
  - static/pip-launcher.js
  - static/nerve.css
  - templates/base.html
  - routes/app_routes.py
autonomous: false
requirements: [Req-6, Req-8, Req-9]
tags: [nerve, pip, ui, gatekeeper, keyboard, uwg-banner, css-tokens]
must_haves:
  truths:
    - "Ctrl+G im PiP wechselt UI auf Gatekeeper-Modus und sendet Socket-Event"
    - "Ctrl+E im PiP wechselt UI auf Target-Modus und sendet Socket-Event"
    - "PiP rendert 4 Gatekeeper-Buttons (statt Standard-EWB-Buttons) bei contact_category='gatekeeper'"
    - "UWG-Hard-Block triggert rotes PiP-Banner via Socket-Event 'uwg_hard_block'"
    - "Alle neuen CSS-Regeln nutzen var(--...) Tokens — keine hardcoded Hex-Farben"
    - "API-Endpoint /api/gatekeeper/phrases gibt die 4 Mr.-Miyagi-Phrases zurück"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "Keyboard-Handler + Gatekeeper-Buttons + UWG-Banner + Socket-Event-Subscriptions"
      contains: "Ctrl+G"
    - path: "static/nerve.css"
      provides: "Neue CSS-Klassen für Gatekeeper-Modus + UWG-Banner — alle var(--...)"
      contains: ".pip-uwg-banner"
    - path: "templates/base.html"
      provides: "PiP-UWG-Banner-DOM"
      contains: "pip-uwg-banner"
    - path: "routes/app_routes.py"
      provides: "GET /api/gatekeeper/phrases — gibt seeded Gatekeeper-Phrases zurück mit Variable-Ersetzung"
      contains: "/api/gatekeeper/phrases"
  key_links:
    - from: "static/pip-launcher.js Keydown-Listener"
      to: "Socket.IO emit('manual_mode_toggle', {category}) → routes/app_routes.py"
      via: "socket.emit + Server-Handler-Update setzt contact_category in _session_state[sid]['state']"
      pattern: "manual_mode_toggle"
    - from: "Server Socket emits 'contact_category_update', 'phase_change', 'uwg_hard_block'"
      to: "static/pip-launcher.js socket.on(...) handlers"
      via: "Socket.IO Room-basierter Emit"
      pattern: "socket\\.on\\('contact_category_update'"
    - from: "static/pip-launcher.js _renderGatekeeperButtons()"
      to: "GET /api/gatekeeper/phrases"
      via: "fetch + Variable-Replacement aus Session-Briefing"
      pattern: "/api/gatekeeper/phrases"
---

<objective>
PiP-Frontend-Erweiterungen für Phase 08.23.2.C: Ctrl+G/Ctrl+E Keyboard-Shortcuts, conditional Gatekeeper-Buttons-Rendering, UWG-Hard-Block-Banner, plus API-Endpoint der die Mr.-Miyagi-Phrases mit Template-Variablen-Ersetzung an das Frontend liefert. Alle CSS-Regeln nutzen var(--...)-Tokens (CLAUDE.md).

Purpose: Backend (Plan 06) emittet bereits die Socket-Events (contact_category_update, phase_change, uwg_hard_block). Frontend muss diese konsumieren und visualisieren. Manual-Toggle (Ctrl+G/E) ist Backup für False-Negatives in classify_contact.

Output: PiP reagiert auf Gatekeeper-Modus mit Buttons-Swap + Banner + Keyboard-Toggle. Andre verifiziert manuell im Live-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-06-SUMMARY.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-06b-SUMMARY.md
@static/pip-launcher.js
@static/nerve.css
@templates/base.html
@routes/app_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: API-Endpoint /api/gatekeeper/phrases + Server-Handler für manual_mode_toggle</name>
  <files>routes/app_routes.py</files>
  <read_first>
    - routes/app_routes.py (Endpoint-Pattern, jsonify, login_required)
    - routes/CLAUDE.md (Routes-spezifische Regeln, url_for-Verifikation)
    - database/models.py Phrase-Klasse (mode-Spalte aus Plan 02)
    - services/live_session.py (Plan 06 — _session_state, _session_state_lock)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-9 — Phrases-Load)
  </read_first>
  <action>
    Schritt 1 — GET /api/gatekeeper/phrases Endpoint hinzufügen:

    ```python
    @app_routes_bp.route('/api/gatekeeper/phrases', methods=['GET'])
    @login_required
    def api_gatekeeper_phrases():
        """Gibt die 4 Mr.-Miyagi-Phrases (mode='gatekeeper') mit Template-Variablen-Ersetzung.

        Phase 08.23.2.C Req-9. Variablen kommen aus dem aktuellen Session-Briefing +
        contact_category-Detected-Names (falls vorhanden).
        """
        from database.db import get_session
        from database.models import Phrase
        db = get_session()
        try:
            # Phrases laden — user_id=1 (Admin-Seed) oder ggf. user-eigene Gatekeeper-Phrases
            rows = (
                db.query(Phrase)
                  .filter(Phrase.mode == 'gatekeeper')
                  .filter((Phrase.user_id == 1) | (Phrase.user_id == g.user.id))
                  .order_by(Phrase.objection_type, Phrase.id)
                  .all()
            )
            # Gruppiere nach objection_type (= Button-ID)
            grouped = {}
            for p in rows:
                grouped.setdefault(p.objection_type, []).append({
                    'id': p.id,
                    'text': p.text,
                })
            # Template-Variablen aus Session-State (best effort — fehlende Werte bleiben als Platzhalter)
            sid = request.args.get('sid') or session.get('sid')
            variables = {'branche': '', 'detail': '', 'vorname': '', 'nachname': ''}
            if sid:
                from services.live_session import _session_state, _session_state_lock
                with _session_state_lock:
                    st = _session_state.get(sid, {}).get('state', {})
                    briefing = st.get('active_profile_data') or st.get('_briefing') or {}
                variables['branche'] = briefing.get('branche', '') or ''
                variables['detail'] = briefing.get('detail', '') or briefing.get('produkt', '') or ''
                # vorname/nachname kommen aus erkanntem Gatekeeper-Namen (Plan 06 classify_contact)
                # Fallback: leer (Berater spricht Platzhalter aus oder editiert vor Click)
            # Apply variable replacement
            def _fill(text):
                out = text
                for k, v in variables.items():
                    out = out.replace('{' + k + '}', v or ('{' + k + '}'))
                return out
            for btn, phrases in grouped.items():
                for ph in phrases:
                    ph['filled_text'] = _fill(ph['text'])
            return jsonify({'buttons': grouped, 'variables': variables})
        finally:
            db.close()
    ```

    Schritt 2 — Socket-Handler für manual_mode_toggle (Req-6):

    Falls die Socket-Handler in services/deepgram_service.py oder app.py liegen — Plan-Author identifiziert per grep `@socketio.on(`:

    ```python
    @socketio.on('manual_mode_toggle')
    def on_manual_mode_toggle(data):
        """Phase 08.23.2.C Req-6 — Berater toggled manuell zwischen target/gatekeeper.

        Setzt contact_category + current_mode im per-SID-State. Schickt Bestätigung zurueck.
        """
        from services.live_session import _session_state, _session_state_lock
        sid = request.sid
        category = data.get('category')
        if category not in ('target', 'gatekeeper'):
            emit('manual_mode_toggle_ack', {'ok': False, 'error': 'invalid category'})
            return
        new_mode = 'gatekeeper' if category == 'gatekeeper' else 'cold_call'
        with _session_state_lock:
            if sid in _session_state:
                st = _session_state[sid].setdefault('state', {})
                st['contact_category'] = category
                st['current_mode'] = new_mode
                # Reset Hysterese auf neuer Modus
                st['current_phase'] = 'opener' if new_mode == 'cold_call' else 'greeting'
                st['phase_hint_count'] = 0
                st['pending_phase'] = None
                import time as _t
                st['phase_entered_at'] = _t.monotonic()
        emit('contact_category_update', {'category': category, 'mode': new_mode, 'source': 'manual'}, room=sid)
        emit('manual_mode_toggle_ack', {'ok': True, 'category': category, 'mode': new_mode})
    ```

    Routes-Regeln pruefen (routes/CLAUDE.md):
    - Blueprint-Name korrekt (app_routes_bp.route nicht generisches Flask)
    - login_required-Decorator aktiv
    - kein url_for-String der broken sein koennte (nur direkter Route-Path)
  </action>
  <verify>
    <!--
      Runtime-Check via Flask Test-Client: ruft den Endpoint auf und prueft Response-Schema.
      CLAUDE.md Source-Presence-Test-Verbot eingehalten (keine open().read() + assert 'string' in src).
      Wenn login_required eine echte Session erzwingt, gibt der Endpoint 401/302 zurueck — beides ist
      ein gueltiges Runtime-Signal dass die Route registriert ist (vs. 404 = Route fehlt).
    -->
    <automated>python -c "
import app as flask_app
client = flask_app.app.test_client()
resp = client.get('/api/gatekeeper/phrases')
assert resp.status_code != 404, f'Route fehlt (404). Got: {resp.status_code}'
print(f'/api/gatekeeper/phrases registered, status={resp.status_code}')
# manual_mode_toggle: Socket-Handler-Registration via socketio.handlers pruefen
from app import socketio
handlers = getattr(socketio, 'handlers', {}) or {}
# socketio.handlers ist {namespace: {event: callback}} — durchsuche alle namespaces
found = False
for ns, evts in handlers.items():
    if 'manual_mode_toggle' in evts:
        found = True
        break
assert found, 'manual_mode_toggle Socket-Handler nicht registriert'
print('manual_mode_toggle handler registered')
"</automated>
  </verify>
  <acceptance_criteria>
    - routes/app_routes.py enthält Route `/api/gatekeeper/phrases` mit `methods=['GET']`
    - Endpoint hat `@login_required`-Decorator
    - Endpoint filtert `Phrase.mode == 'gatekeeper'`
    - Endpoint gibt JSON mit Key `buttons` zurück (dict gruppiert nach objection_type)
    - Template-Variablen `{branche}, {detail}, {vorname}, {nachname}` werden im Output ersetzt (oder als Platzhalter belassen)
    - Socket-Handler `manual_mode_toggle` ist via `socketio.handlers` registriert (Runtime-Check, nicht Source-Grep)
    - Socket-Handler setzt sowohl contact_category als auch current_mode + resetet phase-Hysterese-State
    - Socket-Handler emittet contact_category_update Event zurück an die Session (room=sid)
    - `python -m py_compile routes/app_routes.py` exit 0
    - Flask-Test-Client GET /api/gatekeeper/phrases liefert non-404 Status (auch 401/302 ist OK = Route existiert)
  </acceptance_criteria>
  <done>Backend bietet Phrases-Endpoint + Manual-Toggle-Socket-Handler an.</done>
</task>

<task type="auto">
  <name>Task 2: CSS-Tokens + nerve.css-Erweiterung + base.html UWG-Banner-DOM</name>
  <files>static/nerve.css, templates/base.html</files>
  <read_first>
    - static/nerve.css (existing CSS-Tokens — :root-Block mit --pipeline-error-bg, --danger-red, --glass-bg etc. aus Phase B und 04.7)
    - templates/base.html (PiP-Modal-DOM, nerveNavModal-Pattern aus Phase 08.19.5.4)
    - CLAUDE.md (CSS-Constraint — var(--...))
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-6 Acceptance: keine hardcoded Farben; Req-8 PiP-Banner)
  </read_first>
  <action>
    Schritt 1 — nerve.css erweitern. Pruefe ob folgende Tokens schon existieren (Phase B Plan 07.1 hat `--pipeline-error-bg`, `--pipeline-error-text`, `--pipeline-warning-bg`):

    Falls --danger-red noch nicht da ist: pruefen ob `--accent-error` oder vergleichbar existiert. Plan-Author nutzt was im :root-Block vorhanden ist; ergaenzt nur wenn echt fehlt.

    Neue CSS-Regeln am Ende von nerve.css:

    ```css
    /* Phase 08.23.2.C — Gatekeeper-Modus + UWG-Banner */

    .pip-mode-indicator {
        display: inline-block;
        padding: 2px 8px;
        border-radius: 4px;
        font-size: 11px;
        font-weight: 600;
        background-color: var(--glass-bg);
        color: var(--teal-accent);
        margin-left: 6px;
    }

    .pip-mode-indicator[data-mode="gatekeeper"] {
        background-color: var(--pipeline-warning-bg);
        color: var(--pipeline-warning-text);
    }

    .pip-uwg-banner {
        display: none;
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        padding: 8px 12px;
        background-color: var(--pipeline-error-bg);
        color: var(--pipeline-error-text);
        font-weight: 600;
        font-size: 13px;
        z-index: 100;
        border-radius: 8px 8px 0 0;
    }

    .pip-uwg-banner.is-visible {
        display: block;
    }

    .pip-uwg-banner__close {
        float: right;
        background: transparent;
        color: var(--pipeline-error-text);
        border: 0;
        cursor: pointer;
        font-size: 14px;
    }

    /* Gatekeeper-Button-Set (gleiche Optik wie .pip-ewb-btn aber mit Mode-Indikator) */
    .pip-ewb-btn[data-mode-button="gatekeeper"] {
        border-color: var(--pipeline-warning-text);
    }
    ```

    Anti-Pattern check: KEINE Hex-Farben (`#fff`, `#000`, `rgb(...)`). Nur `var(--...)`.

    Schritt 2 — templates/base.html: PiP-Modal-DOM um Banner + Mode-Indicator erweitern.

    Suche im base.html den PiP-DOM-Block (vermutlich um pip-briefing-tab und pip-ewb-row herum). Direkt am Anfang des PiP-Modal-Containers (vor pip-ewb-row):

    ```html
    <div id="pip-uwg-banner" class="pip-uwg-banner" role="alert">
        <span id="pip-uwg-banner-text">UWG §7: Anrufer hat Opt-Out erklaert.</span>
        <button type="button" id="pip-uwg-banner-close" class="pip-uwg-banner__close" aria-label="Schliessen">×</button>
    </div>
    <span id="pip-mode-indicator" class="pip-mode-indicator" data-mode="cold_call">Cold-Call</span>
    ```
  </action>
  <verify>
    <!--
      CSS/HTML sind keine Runtime-evaluierbaren Module — die Verifikation prueft
      Existenz der CSS-Klassen und DOM-Elemente. Das ist KEIN Source-Presence-False-Green
      im Sinne von CLAUDE.md (das Verbot zielt auf Python-Tests, die Funktions-EXISTENZ
      pruefen statt -VERHALTEN). CSS-Klassen und HTML-Markup-Existenz sind die einzig
      sinnvolle pre-deployment Verifikation; das Live-Verhalten wird im Checkpoint (Task 4)
      visuell verifiziert.
    -->
    <automated>python -c "
import pathlib, re
css = pathlib.Path('static/nerve.css').read_text(encoding='utf-8')
html = pathlib.Path('templates/base.html').read_text(encoding='utf-8')
assert '.pip-uwg-banner' in css, 'pip-uwg-banner CSS-Klasse fehlt'
assert '.pip-mode-indicator' in css, 'pip-mode-indicator CSS-Klasse fehlt'
assert 'pip-uwg-banner' in html, 'pip-uwg-banner DOM-Element fehlt in base.html'
# Hex-Farben in den neuen Bloecken: extrahiere nur die Phase-C-Regeln und pruefe
phase_c = css.split('Phase 08.23.2.C', 1)
if len(phase_c) > 1:
    block = phase_c[1]
    hex_matches = re.findall(r'#[0-9a-fA-F]{3,8}\b', block)
    assert not hex_matches, f'Hex-Farben in Phase-C CSS-Block: {hex_matches}'
print('CSS+DOM OK, no hex in new classes')
"</automated>
  </verify>
  <acceptance_criteria>
    - static/nerve.css enthält `.pip-uwg-banner` Klasse
    - static/nerve.css enthält `.pip-mode-indicator` Klasse
    - Keine hardcoded Hex-Farben (`#RRGGBB`, `#RGB`) oder `rgb()`/`rgba()` in den neuen CSS-Regeln — nur `var(--...)`
    - templates/base.html enthält `<div id="pip-uwg-banner"` Element
    - templates/base.html enthält `<span id="pip-mode-indicator"` Element
    - `pip-uwg-banner` ist standardmäßig hidden (`display: none`) und wird via `.is-visible` Klasse aktiviert
  </acceptance_criteria>
  <done>UI-Foundation (CSS + DOM) ist verdrahtet, alle Farben tokenisiert.</done>
</task>

<task type="auto">
  <name>Task 3: pip-launcher.js — Keyboard-Handler + Gatekeeper-Buttons + UWG-Banner + Socket-Subscriptions</name>
  <files>static/pip-launcher.js</files>
  <read_first>
    - static/pip-launcher.js (existing Socket-Subscription-Pattern, _renderEwbButtons, pipEl-Helper aus Phase 08.20.3-04, EwB-Button-Click-Handler aus Phase 04.7)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pattern 8 Gatekeeper-Buttons)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-6, Req-8, Req-9 Acceptance)
  </read_first>
  <action>
    Schritt 1 — Keyboard-Shortcuts Ctrl+G / Ctrl+E (Req-6):

    ```javascript
    document.addEventListener('keydown', function(ev) {
        if (!(ev.ctrlKey || ev.metaKey)) return;
        var category = null;
        if (ev.key === 'g' || ev.key === 'G') {
            category = 'gatekeeper';
        } else if (ev.key === 'e' || ev.key === 'E') {
            category = 'target';
        } else {
            return;
        }
        ev.preventDefault();
        // Skip wenn User in einem Input-Feld tippt (Briefing-Editor)
        var tag = (ev.target && ev.target.tagName || '').toLowerCase();
        if (tag === 'input' || tag === 'textarea' || (ev.target && ev.target.isContentEditable)) {
            return;
        }
        if (window.socket && window.socket.emit) {
            window.socket.emit('manual_mode_toggle', {category: category});
        }
    });
    ```

    Schritt 2 — Socket-Subscriptions fuer Phase-C-Events:

    ```javascript
    if (window.socket && window.socket.on) {
        window.socket.on('contact_category_update', function(data) {
            _updateContactCategory(data.category, data.mode);
        });
        window.socket.on('phase_change', function(data) {
            console.log('[pip] phase_change', data);
        });
        window.socket.on('uwg_hard_block', function(data) {
            _showUwgBanner(data.phrase);
        });
        window.socket.on('trigger_phrase_hint', function(data) {
            console.log('[pip] trigger_phrase_hint', data);
            // Phase 08.23.2.C Req-7 — optional UI-Hinweis. MVP: nur Console-Log.
        });
    }
    ```

    Schritt 3 — _updateContactCategory + Buttons-Rendering:

    ```javascript
    function _updateContactCategory(category, mode) {
        state.contactCategory = category;
        state.currentMode = mode;
        var indicator = pipEl('pip-mode-indicator');
        if (indicator) {
            indicator.dataset.mode = mode;
            var label = (mode === 'gatekeeper') ? 'Vorzimmer' : (mode === 'meeting') ? 'Meeting' : 'Cold-Call';
            indicator.textContent = label;
        }
        if (category === 'gatekeeper') {
            _renderGatekeeperButtons();
        } else {
            _renderStandardEwbButtons();
        }
    }

    function _renderGatekeeperButtons() {
        var row = pipEl('pip-ewb-row');
        if (!row) return;
        fetch('/api/gatekeeper/phrases?sid=' + encodeURIComponent(state.sid || ''))
          .then(function(r) { return r.json(); })
          .then(function(data) {
              if (!data || !data.buttons) return;
              var ORDER = [
                  'gatekeeper_verbuendeten_bitte',
                  'gatekeeper_insider_antwort',
                  'gatekeeper_voss_label',
                  'gatekeeper_vornamen_pause',
              ];
              var LABELS = {
                  'gatekeeper_verbuendeten_bitte': 'Verbündeten-Bitte',
                  'gatekeeper_insider_antwort': 'Insider-Antwort',
                  'gatekeeper_voss_label': 'Voss-Label',
                  'gatekeeper_vornamen_pause': 'Vornamen-Pause',
              };
              var html = ORDER.map(function(key) {
                  var variants = data.buttons[key] || [];
                  if (variants.length === 0) return '';
                  var firstText = variants[0].filled_text || variants[0].text;
                  return '<button type="button" class="pip-ewb-btn" data-mode-button="gatekeeper" data-typ="'
                       + escHtml(key) + '" data-phrase="' + escHtml(firstText) + '">' + escHtml(LABELS[key]) + '</button>';
              }).join('');
              row.innerHTML = html;
          })
          .catch(function(e) { console.error('[pip] Fehler beim Laden Gatekeeper-Phrases', e); });
    }
    ```

    Schritt 4 — UWG-Banner anzeigen + auto-hide:

    ```javascript
    function _showUwgBanner(phrase) {
        var banner = document.getElementById('pip-uwg-banner');
        var textEl = document.getElementById('pip-uwg-banner-text');
        if (!banner) return;
        if (textEl) {
            textEl.textContent = 'UWG §7: "' + (phrase || '').slice(0, 100) + '" erkannt – DSGVO/UWG-Risiko, Anruf zeitnah hoeflich beenden.';
        }
        banner.classList.add('is-visible');
    }

    var bannerClose = document.getElementById('pip-uwg-banner-close');
    if (bannerClose) {
        bannerClose.addEventListener('click', function() {
            var banner = document.getElementById('pip-uwg-banner');
            if (banner) banner.classList.remove('is-visible');
        });
    }
    ```

    Schritt 5 — _renderStandardEwbButtons() referenziert ggf. bestehende Funktion in pip-launcher.js. Plan-Author identifiziert die existierende EWB-Rendering-Funktion und nutzt sie hier wieder.
  </action>
  <verify>
    <!--
      JavaScript-Verifikation per `node --check` (Syntax-Validation). Pruefe per `node -v`:
      Wenn Node.js verfuegbar (typisch in Dev-Umgebung), liefert node --check echte
      Parse-Errors. Falls Node.js auf dem Deploy-Server nicht installiert ist: dieser
      Check ist pre-deployment-only (laeuft lokal/CI) und ist explizit als Build-Schritt
      akzeptabel — kein Source-Presence-Test im pytest-Sinne (Plan 08 hat die Runtime-Tests).
      Fallback: wenn node fehlt, py_compile-aehnlicher Smoke ueber regex-Klammern-Balance.
    -->
    <automated>bash -c "
if command -v node >/dev/null 2>&1; then
  node --check static/pip-launcher.js && echo 'JS syntax OK'
else
  python -c \"
import re, pathlib
src = pathlib.Path('static/pip-launcher.js').read_text(encoding='utf-8')
# Smoke: Klammern-Balance + erwartete Token vorhanden
opens = src.count('{')
closes = src.count('}')
assert opens == closes, f'Curly-brace imbalance: open={opens} close={closes}'
print(f'JS smoke OK (node nicht verfuegbar, brace-balance ok: {opens})')
\"
fi
"</automated>
  </verify>
  <acceptance_criteria>
    - static/pip-launcher.js parsiert ohne Syntax-Errors (`node --check` exit 0 wo Node verfuegbar; sonst Curly-Brace-Balance ok)
    - Keydown-Listener mit Ctrl+G/Ctrl+E + Skip-fuer-Input-Felder funktional (visuell im Live-Test verifiziert, Task 4)
    - Socket-Subscriptions fuer contact_category_update, phase_change, uwg_hard_block, trigger_phrase_hint
    - _renderGatekeeperButtons() fetcht /api/gatekeeper/phrases und rendert 4 Buttons in ORDER-Reihenfolge
    - 4 Button-Labels: Verbündeten-Bitte, Insider-Antwort, Voss-Label, Vornamen-Pause (User-Text mit echten Umlauten per CLAUDE.md User-Facing-Regel)
    - _showUwgBanner() setzt `.is-visible` Klasse auf #pip-uwg-banner
    - Close-Button entfernt `.is-visible` Klasse
    - KEINE hardcoded Hex-Farben in den neuen JS-Strings (alle Farben via CSS)
    - Code-Identifier (state.contactCategory, dataset.mode) sind ASCII
  </acceptance_criteria>
  <done>Frontend reagiert auf Keyboard, Socket-Events und rendert Gatekeeper-UI dynamisch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-PiP-Test (manuell, da kein Browser-Headless-Setup)</name>
  <what-built>
    PiP-Frontend reagiert auf 4 neue Socket-Events (contact_category_update, phase_change, uwg_hard_block, trigger_phrase_hint).
    Ctrl+G / Ctrl+E senden manual_mode_toggle Event an Server, der contact_category + current_mode setzt.
    Bei Gatekeeper-Modus werden 4 Mr.-Miyagi-Buttons via /api/gatekeeper/phrases geladen (≥2 Varianten pro Button aus Plan 06b).
    UWG-Hard-Block-Banner erscheint rot wenn das Backend die Phrase erkennt.
    Alle Farben via var(--...) Tokens — keine Hex-Farben in den neuen CSS-Regeln.
  </what-built>
  <how-to-verify>
    1. Backend-Smoke ist ausreichend (kein echtes Mikrofon noetig):
       a) Server starten: `python app.py`
       b) Browser oeffnen: `https://localhost:5000/live`
       c) Cold-Call-Modus starten + Mikrofon erlauben
       d) Browser-Dev-Tools Console oeffnen: `window.socket` muss vorhanden sein
       e) Manuell Events triggern:
          - `window.socket.emit('manual_mode_toggle', {category: 'gatekeeper'})` → PiP soll auf Gatekeeper-Modus wechseln, 4 Buttons erscheinen, mode-indicator = "Vorzimmer"
          - `window.socket.emit('manual_mode_toggle', {category: 'target'})` → Zurueck auf Standard-Buttons
          - Ctrl+G drueckt: gleiches Verhalten wie emit
          - Ctrl+E drueckt: zurueck zu target
       f) UWG-Banner-Simulation: server-side emit triggern (oder client-seitig _showUwgBanner('test phrase') aufrufen)

    2. Optional: echter end-to-end Test mit briefing_ceo_name = "Schmidt" → "Hallo Frau Mueller, ..." → classify_contact → Buttons.

    3. Sichtkontrolle:
       - Keine hardcoded Hex-Farben in pip-uwg-banner und pip-mode-indicator (Dev-Tools Inspector)
       - Banner ist responsive und blockiert nicht den Click auf KI-Antwort-Bereich
       - Mode-Indikator ist sichtbar in PiP-Header
  </how-to-verify>
  <resume-signal>
    Antworte:
    - "live test passed" wenn alle Verifikationen oben gruen sind
    - "fix: <beschreibung>" wenn UI-Bug auftaucht (Plan-Phase generiert Fix-Plan)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → manual_mode_toggle Socket Event | User-controllable input |
| /api/gatekeeper/phrases → DB → Frontend | Server stellt seeded data bereit, kein User-Input |
| socket.on(...) handlers → DOM manipulation | Server-emit-events steuern UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.C-24 | Tampering | manual_mode_toggle akzeptiert beliebige category-Strings | mitigate | Server-Handler validiert `category in ('target', 'gatekeeper')` — invalid emit ack mit ok:False |
| T-08.23.2.C-25 | Spoofing | Fremde Sessions koennten manual_mode_toggle senden | accept | Socket.IO bindet emit an request.sid (server-side); kein Cross-Session-Write moeglich |
| T-08.23.2.C-26 | Information Disclosure | UWG-Phrase im Banner zeigt 100 Zeichen Kunden-Text | mitigate | Banner laeuft nur im PiP des Beraters, kein Browser-LocalStorage; auto-hide nicht implementiert (Berater haendelt manuell) |
| T-08.23.2.C-27 | Tampering | Hex-Farbe schleicht sich in neue CSS-Regeln | mitigate | Verify-Block grept auf Hex-Patterns im Phase-C CSS-Block |
| T-08.23.2.C-28 | XSS | Phrase-Text wird unsanitized ins DOM gesetzt | mitigate | escHtml() wird genutzt fuer textContent (nicht innerHTML); button-Renderer nutzt escHtml fuer alle Variablen |
</threat_model>

<verification>
- API-Endpoint /api/gatekeeper/phrases registriert (Flask-Test-Client non-404)
- Socket-Handler manual_mode_toggle registriert (socketio.handlers introspection)
- CSS nutzt nur var(--...) Tokens (Hex-Regex auf Phase-C-Block)
- JS parsiert ohne Syntax-Errors (node --check oder Klammern-Balance)
- Live-Test im Browser bestaetigt sichtbares Verhalten (Checkpoint)
</verification>

<success_criteria>
- Flask test-client GET /api/gatekeeper/phrases liefert non-404
- socketio.handlers enthaelt 'manual_mode_toggle'
- node --check static/pip-launcher.js exit 0 (oder Brace-Balance ok)
- Andre's Live-Test Checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-07-SUMMARY.md`
</output>
