---
phase: 08.23.2.C
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04, 05]
files_modified:
  - services/gatekeeper.py
  - services/live_session.py
  - services/claude_service.py
  - services/deepgram_service.py
autonomous: true
requirements: [Req-3, Req-4, Req-5, Req-7, Req-8, Req-11]
tags: [nerve, gatekeeper, classify-contact, hysteresis, call-events, session-state, foundation-stub]
must_haves:
  truths:
    - "services/gatekeeper.py existiert mit classify_contact(), detect_trigger_phrases(), detect_uwg_hard_block(), get_gatekeeper_phase_for_window(), populate_context_notes()-Stub"
    - "classify_contact(window, ceo_name, mode) gibt 'target' | 'gatekeeper' | 'unknown' zurück (UNION fuer target, CONSENSUS fuer gatekeeper per Review Finding 4)"
    - "init_session_state() enthält contact_category, current_mode, context_notes, phase_hint_count, pending_phase, phase_entered_at, uwg_blocked"
    - "Bei start_live_session wird ein Call-Record angelegt und call_id im _session_state gespeichert"
    - "Analyse-Loop ruft Hysterese-Check auf und persistiert phase_change in call_events"
    - "UWG-Hard-Block wird in call_events als event_type='reaction' mit payload.type='uwg_hard_block' geloggt UND setzt uwg_blocked=True im Session-State"
    - "Deepgram-Loop und Analyse-Loop pruefen uwg_blocked-Flag und brechen ab wenn True (UWG §7 Guard)"
    - "Deepgram-Loop ruft detect_trigger_phrases() pro Berater-Zeile auf und emittet trigger_phrase_hint Socket-Event"
  artifacts:
    - path: "services/gatekeeper.py"
      provides: "UNION/CONSENSUS-NER + classify_contact + RapidFuzz _names_match + Trigger-Phrasen-Detector + Hysterese-State-Helpers + Foundation-Stub"
      contains: "def classify_contact"
    - path: "services/live_session.py"
      provides: "Erweiterte init_session_state mit Phase-C State-Keys inkl. uwg_blocked + Call-Record-Lifecycle"
      contains: "contact_category"
    - path: "services/claude_service.py"
      provides: "Analyse-Loop ruft Hysterese-Helper + persistiert phase_change in call_events + uwg_blocked Guard"
      contains: "phase_change"
    - path: "services/deepgram_service.py"
      provides: "UWG-Hard-Block + Trigger-Phrasen-Detection + uwg_blocked Guard in der Transcript-Loop"
      contains: "detect_trigger_phrases"
  key_links:
    - from: "services/live_session.py init_session_state"
      to: "_session_state[sid]['state'] (per-SID, NICHT ls.state global)"
      via: "Direct dict-Init in init_session_state()"
      pattern: "context_notes.*\\[\\]"
    - from: "services/claude_service.py analyse_loop"
      to: "services/gatekeeper.apply_hysteresis() + call_events INSERT"
      via: "Function call + SQLAlchemy CallEvent.add"
      pattern: "phase_change"
    - from: "services/deepgram_service.py (line received)"
      to: "services/gatekeeper.detect_uwg_hard_block() + detect_trigger_phrases()"
      via: "Function call pro Berater-Zeile, Trigger -> emit Socket-Events (uwg_hard_block, trigger_phrase_hint)"
      pattern: "trigger_phrase_hint"
---

<objective>
Kernmodul services/gatekeeper.py anlegen (D-02) mit RapidFuzz-basiertem _names_match() und UNION/CONSENSUS-Voting in classify_contact() (Review Findings 3+4). Session-State um Phase-C-Felder inkl. uwg_blocked erweitern (per-SID, NICHT global per RESEARCH.md Pitfall 3+6). Call-Record-Lifecycle bei Session-Start anlegen (Pitfall 4 — call_id-Provenienz für CallEvent-Insert). Analyse-Loop mit Hysterese-Check + phase_change-Persistierung + UWG-Guard verdrahten. Deepgram-Loop mit UWG-Hard-Block-Detection (setzt uwg_blocked=True, Review Finding 5) + Trigger-Phrasen-Detection + uwg_blocked-Guard erweitern.

**Hinweis:** Migration 0003 Seed-Insert (Mr.-Miyagi-Phrases) ist in Plan 06b ausgelagert (Wave 3, depends_on: 06). Reduziert Plan-06-Scope auf reine Wiring-Logik.

Purpose: Wave 4 (PiP-UI) konsumiert contact_category + current_mode aus dem Session-State und reagiert auf Socket-Events (UWG-Banner, Trigger-Phrasen-Hinweis, Gatekeeper-Mode-Toggle). Wave 5 (Tests) testet classify_contact, Hysterese, UWG-Hard-Block, Trigger-Phrasen direkt.

Output: Komplettes Backend-Verhalten für Phasen-Klassifikator-Umbau + Gatekeeper-Erkennung verdrahtet. UWG-Block haltet weitere Verarbeitung an (kein Deepgram/Claude nach Hard-Block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-02-SUMMARY.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-03-SUMMARY.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-04-SUMMARY.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-05-SUMMARY.md
@.planning/04 Entscheidungen/Foundation-Code-Register.md
@services/live_session.py
@services/claude_service.py
@services/deepgram_service.py
@database/models.py
</context>

<interfaces>
<!-- ALLE aus den vorherigen Plans bereits verfuegbar -->

From services/anonymization.py (Plan 04):
```python
def extract_entities(text: str, cache=None) -> list:
    # Returns: [{'text': str, 'type': 'PERSON'|'ORG'|'LOC', 'source': 'spacy'|'gliner'}, ...]
```

From services/claude_service.py (Plan 05):
```python
_PHASE_NAMES_COLD_CALL, _PHASE_NAMES_MEETING, _PHASE_NAMES_GATEKEEPER, _PHASE_NAMES_BY_MODE
def classify_phase(transcript_window, current_phase, elapsed_s, mode)
```

From services/ki_logik.py (Plan 05):
```python
TRIGGER_PHRASES  # list of {'pattern', 'category', 'hard_block'}
UWG_HARD_BLOCK_PATTERNS  # list[str]
def _phrase_matches(line, pattern) -> bool
```

From config/phase_transitions.py (Plan 03):
```python
MIN_PHASE_DURATIONS  # {mode: {phase: seconds}}
ALLOWED_TRANSITIONS  # {mode: {from: [to1, to2]}}
FORBIDDEN_TRANSITIONS
MODE_TRANSITION_AUTO  # {(from_mode, to_mode): bool}
HYSTERESIS_REQUIRED_HINTS  # = 2
```

From database/models.py (Phase A):
```python
class Call(Base): __tablename__='calls'; id, user_id, started_at, call_mode, ...
class CallEvent(Base): __tablename__='call_events'; id, call_id (NOT NULL FK), event_type, event_ts_ms, payload (JSONB)
# CHECK: event_type IN (..., 'phase_change', 'reaction', ...)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: services/gatekeeper.py — Modul-Skelett mit allen Funktionen + RapidFuzz + UNION/CONSENSUS-Voting</name>
  <files>services/gatekeeper.py</files>
  <read_first>
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md (D-01 Konsens-Voting + D-02 Modul-Struktur)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pitfall 2 Case-Sensitivity, Pitfall 3 State-Scope)
    - services/anonymization.py extract_entities() (Plan 04 — NER-Quelle)
    - services/ki_logik.py TRIGGER_PHRASES + UWG_HARD_BLOCK_PATTERNS (Plan 05)
    - config/phase_transitions.py (Plan 03 — alle 4 Konstanten)
    - .planning/04 Entscheidungen/Foundation-Code-Register.md (populate_context_notes-Spec)
    - requirements.txt (pruefen ob rapidfuzz bereits eingetragen — falls nicht: eintragen)
  </read_first>
  <action>
    Neues Modul services/gatekeeper.py mit allen 6 Funktionen aus D-02.

    HINWEIS zu requirements.txt: Vor dem Schreiben pruefen ob `rapidfuzz` in requirements.txt steht. Falls nicht: Zeile `rapidfuzz>=3.0.0` einfuegen (Review Finding 3). RapidFuzz hat keine weiteren Abhaengigkeiten und ist rein in Python/C geschrieben.

    ```python
    """Gatekeeper-Klassifikation und Trigger-Phrasen-Erkennung (Phase 08.23.2.C, D-02).

    NER-Schnittstelle: ruft services.anonymization.extract_entities() auf —
    KEINE eigenen GLiNER/spaCy-Aufrufe (DRY-Pflicht per D-02).

    Voting-Strategie (Review Finding 4):
    - Target-Erkennung: UNION (ein Modell reicht) — Recall maximieren
    - Gatekeeper-Erkennung: CONSENSUS (beide Modelle) — False-Positives minimieren
    """
    import re
    import time
    from typing import Optional

    from services.anonymization import extract_entities
    from services.ki_logik import (
        TRIGGER_PHRASES,
        UWG_HARD_BLOCK_PATTERNS,
        _phrase_matches,
    )
    from config.phase_transitions import (
        MIN_PHASE_DURATIONS,
        ALLOWED_TRANSITIONS,
        FORBIDDEN_TRANSITIONS,
        MODE_TRANSITION_AUTO,
        HYSTERESIS_REQUIRED_HINTS,
    )


    # ── Helpers ────────────────────────────────────────────────────────────

    def _names_match(extracted: str, briefing: str) -> bool:
        """Fuzzy-Match zwischen extrahierter Name und Briefing-CEO/GF.

        Strategie (Review Finding 3):
        1. Exact match (case-insensitive)
        2. Substring/Wort-Overlap (Vorname oder Nachname reicht)
        3. RapidFuzz fuzzy (threshold=0.85) — fuer STT-Phonetik-Varianten
           (Meier/Meyer/Maier, Schroeder/Schröder etc.)

        RapidFuzz-Import ist optional — bei ImportError degradiert auf Substring-only.
        """
        if not extracted or not briefing:
            return False
        e = extracted.strip().lower()
        b = briefing.strip().lower()

        # Exact match
        if e == b:
            return True

        # Substring: ein Vorname / Nachname reicht
        e_parts = set(e.split())
        b_parts = set(b.split())
        if e_parts & b_parts:
            return True

        # RapidFuzz fuzzy: handles STT phonetic variants (Meier/Meyer/Maier) — Review Finding 3
        try:
            from rapidfuzz import fuzz
            for e_part in e_parts:
                for b_part in b_parts:
                    # Kurze Woerter (< 3 Zeichen) ueberspringen — zu viele Falsch-Treffer
                    if len(e_part) >= 3 and len(b_part) >= 3:
                        if fuzz.ratio(e_part, b_part) >= 85:
                            return True
        except ImportError:
            pass  # rapidfuzz nicht installiert -> Substring-only (degraded mode)

        return False


    # ── classify_contact (Req-5) ───────────────────────────────────────────

    def classify_contact(
        transcript_window: list,
        briefing_ceo_name: Optional[str],
        current_mode: str,
    ) -> str:
        """UNION/CONSENSUS-Voting fuer Kontakt-Klassifikation (Review Finding 4).

        Voting-Asymmetrie (warum unterschiedlich):
        - Target (CEO erkannt): UNION — ein Modell reicht wenn Name CEO matcht.
          Recall maximieren: spaCy hat ~77% Recall auf deutschen Namen;
          wenn spaCy den CEO verpasst aber GLiNER ihn erkennt, verlieren wir den
          Target-Treffer bei reinem Consensus.
        - Gatekeeper (Unbekannte Person erkannt): CONSENSUS — beide Modelle muessen
          einig sein. FP-Rate minimieren: falscher Gatekeeper-Modus blockiert den
          Sales-Flow unnoetig.

        Returns:
            'target'     wenn UNION-Set einen Namen enthaelt der CEO fuzzy-matched (D-01)
            'gatekeeper' wenn CONSENSUS-Set nicht leer und kein CEO-Match
            'unknown'    wenn kein Konsens fuer gatekeeper und kein Union-Hit fuer target
        """
        if not transcript_window:
            return 'unknown'

        combined_text = ' '.join(transcript_window)
        entities = extract_entities(combined_text, cache=None)

        spacy_persons = {e['text'].lower() for e in entities if e['source'] == 'spacy' and e['type'] == 'PERSON'}
        gliner_persons = {e['text'].lower() for e in entities if e['source'] == 'gliner' and e['type'] == 'PERSON'}

        all_persons = spacy_persons | gliner_persons  # UNION fuer Target-Check
        consensus = spacy_persons & gliner_persons     # CONSENSUS fuer Gatekeeper-Check

        # UNION: wenn EIN Modell einen Namen erkennt der CEO matcht -> target
        if briefing_ceo_name and all_persons:
            for name in all_persons:
                if _names_match(name, briefing_ceo_name):
                    return 'target'

        # CONSENSUS: nur wenn BEIDE Modelle einig sind -> gatekeeper
        if consensus:
            return 'gatekeeper'

        return 'unknown'


    # ── detect_trigger_phrases (Req-7) ─────────────────────────────────────

    def detect_trigger_phrases(transcript_line: str) -> dict:
        """Scant einzelne Zeile auf bekannte Gatekeeper-Trigger.

        Returns:
            dict {'matches': [list of category strings], 'hard_block': bool}
        """
        if not transcript_line:
            return {'matches': [], 'hard_block': False}
        matches = []
        hard_block = False
        for p in TRIGGER_PHRASES:
            if _phrase_matches(transcript_line, p['pattern']):
                matches.append(p['category'])
                if p.get('hard_block'):
                    hard_block = True
        return {'matches': matches, 'hard_block': hard_block}


    # ── detect_uwg_hard_block (Req-8) ──────────────────────────────────────

    def detect_uwg_hard_block(transcript_line: str) -> bool:
        """Schneller Single-Purpose-Check fuer UWG §7 Opt-Out.
        Identisch mit detect_trigger_phrases()['hard_block'] aber ohne andere Kategorien zu scannen.
        """
        if not transcript_line:
            return False
        return any(
            _phrase_matches(transcript_line, p)
            for p in UWG_HARD_BLOCK_PATTERNS
        )


    # ── get_gatekeeper_phase_for_window (Helper fuer Wave 5 Tests) ─────────

    def get_gatekeeper_phase_for_window(transcript_window: list) -> str:
        """Heuristische Phasen-Abschaetzung fuer Gatekeeper-Modus (4 Phasen).

        Sehr einfacher String-basierter Mapper — fuer Tests und Quick-Estimation.
        Echte Klassifikation passiert weiterhin via claude_service.classify_phase().
        """
        if not transcript_window:
            return 'greeting'
        joined = ' '.join(transcript_window).lower()
        if any(w in joined for w in ('mit wem', 'wer sind sie', 'von welcher firma')):
            return 'identify'
        if any(w in joined for w in ('verbinden', 'durchstellen', 'weiterleiten')):
            return 'handoff'
        if any(w in joined for w in ('worum geht es', 'um was geht es', 'wegen welcher angelegenheit')):
            return 'bypass'
        return 'greeting'


    # ── Hysterese (Req-3) ──────────────────────────────────────────────────

    def apply_hysteresis(state: dict, proposed_phase: str, mode: str) -> Optional[str]:
        """Prueft drei Bedingungen vor Phasen-Wechsel-Akzeptanz (Req-3).

        Args:
            state: per-SID Session-State dict (mutiert IN-PLACE!).
                   Erwartet Keys: 'current_phase', 'phase_hint_count', 'pending_phase',
                                  'phase_entered_at' (monotonic seconds).
            proposed_phase: Vom Klassifikator vorgeschlagene neue Phase (Token wie 'pitch').
            mode: 'cold_call' | 'gatekeeper' | 'meeting'.

        Returns:
            new_phase wenn Wechsel akzeptiert (Caller persistiert phase_change), sonst None.
        """
        current = state.get('current_phase')

        # Same phase -> reset hint count (stability signal)
        if proposed_phase == current:
            state['phase_hint_count'] = 0
            state['pending_phase'] = None
            return None

        # Build hint count
        if state.get('pending_phase') == proposed_phase:
            state['phase_hint_count'] = state.get('phase_hint_count', 0) + 1
        else:
            state['pending_phase'] = proposed_phase
            state['phase_hint_count'] = 1

        # Bedingung 1: >=HYSTERESIS_REQUIRED_HINTS aufeinanderfolgende Hinweise
        if state['phase_hint_count'] < HYSTERESIS_REQUIRED_HINTS:
            return None

        # Bedingung 2: Mindest-Verweildauer in aktueller Phase
        durations = MIN_PHASE_DURATIONS.get(mode, {})
        min_dwell = durations.get(current, 0)
        entered_at = state.get('phase_entered_at') or time.monotonic()
        elapsed = time.monotonic() - entered_at
        if elapsed < min_dwell:
            return None  # noch zu frueh

        # Bedingung 3: Uebergang erlaubt?
        allowed = ALLOWED_TRANSITIONS.get(mode, {}).get(current, [])
        forbidden = FORBIDDEN_TRANSITIONS.get(mode, {}).get(current, [])
        if proposed_phase in forbidden:
            return None
        if current is not None and proposed_phase not in allowed:
            # Keine erlaubte Kante -> ablehnen (Req-3 Acceptance)
            return None

        # ALLE Bedingungen ok -> Wechsel
        state['current_phase'] = proposed_phase
        state['phase_entered_at'] = time.monotonic()
        state['phase_hint_count'] = 0
        state['pending_phase'] = None
        return proposed_phase


    # ── Foundation-Stub (Req-11, D-02) ─────────────────────────────────────

    def populate_context_notes(state: dict, entity: dict) -> None:
        """Foundation-Stub fuer Phase 08.23.2.I (Sekretaer-Uebergabe-Feature).

        Aktiviert von: Phase 08.23.2.I
        Aktueller Stand: Body bleibt leer. Phase 08.23.2.I schreibt die Implementierung
                         und ruft die Funktion aus dem Gatekeeper-Loop heraus auf.

        Siehe .planning/04 Entscheidungen/Foundation-Code-Register.md Eintrag 1.
        """
        pass


    __all__ = [
        'classify_contact',
        'detect_trigger_phrases',
        'detect_uwg_hard_block',
        'get_gatekeeper_phase_for_window',
        'apply_hysteresis',
        'populate_context_notes',
    ]
    ```

    Anti-Patterns vermeiden (RESEARCH.md):
    - KEINE direkten GLiNER/spaCy-Imports in gatekeeper.py (D-02 explizit verboten).
    - Hysterese-State NICHT in ls.state schreiben — Caller (Plan 06 Task 3 in claude_service.py) uebergibt per-SID-State-Dict.
    - RapidFuzz-Import ist optional (try/except ImportError) — kein Hard-Fail bei fehlendem Package.
  </action>
  <verify>
    <automated>python -c "
from services.gatekeeper import classify_contact, detect_trigger_phrases, detect_uwg_hard_block, get_gatekeeper_phase_for_window, apply_hysteresis, populate_context_notes
assert callable(classify_contact)
assert detect_uwg_hard_block('Bitte rufen Sie nicht mehr an') is True
assert detect_uwg_hard_block('Guten Tag') is False
r = detect_trigger_phrases('Worum geht es?')
assert 'inquiry' in r['matches']
assert r['hard_block'] is False
from services.gatekeeper import _names_match
assert _names_match('meyer', 'Meier') is True, 'RapidFuzz fuzzy match Meier/Meyer fehlgeschlagen'
assert _names_match('xyz', 'Jacob') is False, 'false positive in _names_match'
assert _names_match('jacob', 'Jakob Jacob Heinrich') is True, 'Substring-Match fehlgeschlagen'
print('gatekeeper module OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - services/gatekeeper.py existiert
    - Exportierte Symbole: classify_contact, detect_trigger_phrases, detect_uwg_hard_block, get_gatekeeper_phase_for_window, apply_hysteresis, populate_context_notes
    - `detect_uwg_hard_block('Bitte rufen Sie nicht mehr an')` returnt True
    - `detect_uwg_hard_block('Guten Tag')` returnt False
    - `detect_trigger_phrases('Worum geht es?')['matches']` enthält 'inquiry'
    - `populate_context_notes({}, {})` returnt None ohne Exception (Stub mit pass-Body)
    - services/gatekeeper.py importiert NICHT direkt `from gliner` oder `import spacy` (grep: 0 Treffer)
    - services/gatekeeper.py importiert `extract_entities` aus `services.anonymization`
    - apply_hysteresis({...}, 'pitch', 'cold_call') mit nur 1 Hinweis returnt None (Bedingung 1 nicht erfuellt)
    - apply_hysteresis akzeptiert nach 2 Hinweisen + Dwell + erlaubter Kante
    - apply_hysteresis lehnt verbotenen Uebergang 'handoff' -> 'greeting' ab (mode='gatekeeper')
    - `_names_match('meyer', 'Meier')` returnt True (RapidFuzz fuzzy — Review Finding 3)
    - `_names_match('xyz', 'Jacob')` returnt False (kein false positive)
    - `classify_contact(['Hallo Herr Meyer'], 'Meier', 'cold_call')` returnt 'target' wenn NER 'Meyer' erkennt (UNION + fuzzy — Review Finding 4)
    - classify_contact nutzt UNION fuer target-Check: `all_persons = spacy_persons | gliner_persons` (grep-pruefbar)
    - classify_contact nutzt CONSENSUS fuer gatekeeper-Check: `consensus = spacy_persons & gliner_persons` (grep-pruefbar)
    - requirements.txt enthält `rapidfuzz>=3.0.0` (Review Finding 3)
  </acceptance_criteria>
  <done>Komplettes Gatekeeper-Modul exportiert, alle Funktionen runtime-testbar. RapidFuzz-Fuzzy-Match + UNION/CONSENSUS-Voting implementiert.</done>
</task>

<task type="auto">
  <name>Task 2: Session-State erweitern + Call-Record-Lifecycle + uwg_blocked-Key in live_session.py</name>
  <files>services/live_session.py</files>
  <read_first>
    - services/live_session.py vollstaendig (init_session_state Z.~298+, pop_session_state, set_active_profile, _session_state-Lock-Pattern). Insbesondere: wo wird active_profile_data oder active_profile in den State geschrieben? (grep `set_active_profile`, `active_profile_data`)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pitfall 3, 4, 6 — kritisch)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-11)
    - database/models.py (Call-Klasse — Felder fuer Insert)
  </read_first>
  <action>
    Schritt 1 — init_session_state() erweitern um Phase-C Felder (Pitfall 3+6: ALLE neuen Keys in _session_state[sid]['state'], NICHT in globalem ls.state):

    Suche in services/live_session.py die Funktion `init_session_state(sid, ...)`. Im `state`-Dict (innerhalb _session_state[sid]) folgende Keys initialisieren:

    ```python
    # Phase 08.23.2.C — Gatekeeper-State (Req-11, Foundation-Code-Register Eintrag 2)
    'contact_category': 'unknown',       # 'target' | 'gatekeeper' | 'unknown'
    'current_mode': 'cold_call',         # initial cold_call; wird ggf. via set_active_profile/call_mode ueberschrieben
    'context_notes': [],                 # Phase 08.23.2.I aktiviert Befuellung
    # Hysterese-interne Keys (Req-3)
    'current_phase': None,               # initial unbekannt; wird beim ersten classify_phase()-Hit gesetzt
    'phase_hint_count': 0,
    'pending_phase': None,
    'phase_entered_at': None,
    # Call-Record-Referenz (Pitfall 4 — call_id-Provenienz)
    'call_id': None,                     # UUID nach Call-Insert in start_live_session
    # UWG §7 Hard-Block Guard (Review Finding 5)
    'uwg_blocked': False,                # True nach detect_uwg_hard_block() — stoppt Deepgram+Claude
    ```

    Schritt 2 — Call-Record bei Session-Start anlegen (Pitfall 4 — Req-4 braucht non-null call_id):

    Suche die Funktion, die im normalen Lifecycle direkt nach init_session_state() laeuft (vermutlich Socket.IO `start_live_session`-Handler oder Aequivalent in routes/app_routes.py oder direkt in deepgram_service.py handle_connect). Falls Call-Insert besser zentral in live_session.py: neue Helper-Funktion `create_call_for_sid(sid, user_id, call_mode)` einfuegen.

    Neuer Helper in live_session.py:
    ```python
    def create_call_for_sid(sid: str, user_id: int, call_mode: str = 'cold_call') -> Optional[str]:
        """Legt Call-Record an und speichert call_id im per-SID-State.

        Phase 08.23.2.C Pitfall 4 — call_events.call_id ist NOT NULL.
        Erstellt bei Session-Start einen Call-Eintrag, sodass CallEvent-Inserts
        (z.B. phase_change in Plan 06 Task 3) eine gueltige FK-Referenz haben.

        Returns: call_id (UUID-string) oder None bei Fehler.
        """
        from database.db import get_session
        from database.models import Call
        import uuid
        db = get_session()
        try:
            call = Call(
                id=str(uuid.uuid4()),
                user_id=user_id,
                call_mode=call_mode,
                # weitere benoetigte Felder aus Call-Schema einfuegen (started_at etc.)
            )
            db.add(call)
            db.commit()
            cid = call.id
            with _session_state_lock:
                if sid in _session_state:
                    _session_state[sid].setdefault('state', {})['call_id'] = cid
            return cid
        except Exception as e:
            print(f'[live_session] create_call_for_sid Fehler: {type(e).__name__}: {e}')
            db.rollback()
            return None
        finally:
            db.close()
    ```

    WICHTIG — Plan-Author muss zuerst grep auf `start_live_session` durchfuehren und identifizieren wo init_session_state() heute aufgerufen wird. Im selben Block direkt danach `create_call_for_sid(sid, user_id, call_mode)` aufrufen. Falls user_id zu dem Zeitpunkt nicht verfuegbar: Plan-Author entscheidet entweder
    (a) Aufruf-Site nach unten verschieben wo user_id verfuegbar, oder
    (b) call_id lazy beim ersten phase_change anlegen (defensive in claude_service.py Task 3).

    Schritt 3 — pop_session_state() pruefen: `dict.pop()` mit `None`-Default fuer alle neuen Keys ist bereits aus Phase B abgedeckt; falls jedoch explizite Cleanups (z.B. Call-Record close/UPDATE bei Session-Ende) noetig: Aufruf-Site in api_beenden() oder Session-Stop-Handler. Plan-Author dokumentiert in SUMMARY ob Cleanup-Pfad noetig oder optional (kein Call-Update in dieser Phase — Phase D wuerde das uebernehmen).
  </action>
  <verify>
    <automated>python -c "
from services.live_session import init_session_state, _session_state, _session_state_lock
init_session_state('test-sid-c1')
st = _session_state['test-sid-c1']['state']
assert st.get('contact_category') == 'unknown'
assert st.get('current_mode') == 'cold_call'
assert st.get('context_notes') == []
assert 'phase_hint_count' in st
assert st.get('call_id') is None
assert st.get('uwg_blocked') is False, 'uwg_blocked Key fehlt oder nicht False'
del _session_state['test-sid-c1']
print('init_session_state Phase-C keys OK inkl. uwg_blocked')
"</automated>
  </verify>
  <acceptance_criteria>
    - `init_session_state('test')` legt `_session_state['test']['state']` mit allen 9 neuen Keys an
    - `_session_state['test']['state']['contact_category'] == 'unknown'`
    - `_session_state['test']['state']['current_mode'] == 'cold_call'`
    - `_session_state['test']['state']['context_notes'] == []`
    - `_session_state['test']['state']['phase_hint_count'] == 0`
    - `_session_state['test']['state']['current_phase'] is None`
    - `_session_state['test']['state']['pending_phase'] is None`
    - `_session_state['test']['state']['phase_entered_at'] is None`
    - `_session_state['test']['state']['call_id'] is None`
    - `_session_state['test']['state']['uwg_blocked'] is False` (Review Finding 5 — UWG §7 Guard-Key)
    - KEIN neuer Key wird in `ls.state` (globalem Dict) gesetzt — grep `ls.state\[.*contact_category` 0 Treffer
    - `create_call_for_sid` ist als Helper exportiert und verfuegbar (callable)
    - pop_session_state() loescht state-Dict via dict.pop (kein Refactor noetig)
  </acceptance_criteria>
  <done>Session-State ist per-SID erweitert inkl. uwg_blocked-Key, Call-Record-Lifecycle-Helper verfuegbar.</done>
</task>

<task type="auto">
  <name>Task 3: Analyse-Loop verdrahten — Hysterese + phase_change + classify_contact + UWG-Guard + Trigger-Phrasen</name>
  <files>services/claude_service.py, services/deepgram_service.py</files>
  <read_first>
    - services/claude_service.py Z.~954 (RESEARCH.md "Integration Points" — classify_phase() Aufruf im Analyse-Loop)
    - services/deepgram_service.py (Z.~77-106 / Z.~591-602 aus Phase-B Pattern — Stelle wo conversation_log.append() passiert nach anonymize())
    - services/gatekeeper.py (gerade erstellt — apply_hysteresis, classify_contact, detect_uwg_hard_block, detect_trigger_phrases)
    - services/live_session.py — Briefing-Storage-Pfad identifizieren: grep `set_active_profile`, `active_profile_data`, `_briefing` in services/live_session.py. Briefing-Daten werden vermutlich unter `_session_state[sid]['state']['active_profile_data']` oder `_session_state[sid]['active_profile']` abgelegt. Plan-Author bestaetigt den exakten Pfad VOR Wiring (kein Blind-Code).
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-4 Acceptance + Req-7 + Req-8 Acceptance)
    - database/models.py CallEvent-Klasse
  </read_first>
  <action>
    Schritt 1 — UWG-Guard in services/claude_service.py (Review Finding 5):

    Am ANFANG der Analyse-Loop-Iteration (vor jeglicher Claude-API-Nutzung) pruefen:

    ```python
    # UWG §7 Guard — Review Finding 5: kein Claude-API-Call nach Hard-Block
    with _session_state_lock:
        if _session_state.get(sid, {}).get('state', {}).get('uwg_blocked'):
            return  # oder: continue (je nach Loop-Struktur) — kein API-Call nach Hard-Block
    ```

    Schritt 2 — Analyse-Loop in services/claude_service.py erweitern (Req-3 + Req-4 + Req-5 Wiring):

    Briefing-Lookup-Konvention (basierend auf grep aus read_first — Plan-Author verifiziert die exakten Keys vor Implementation):
    ```python
    # Briefing-Daten werden bei set_active_profile() in den Session-State geschrieben.
    # Erwartete Keys (Plan-Author prueft per grep VOR Wiring):
    #   primaer:   sid_state.get('active_profile_data', {}).get('ansprechpartner')
    #   fallback:  sid_state.get('active_profile_data', {}).get('ceo_name')
    #   weitere:   sid_state.get('active_profile', {}).get('ansprechpartner_name')
    # Falls Schluessel-Pfad anders heisst: Plan-Author dokumentiert die echte Quelle in der SUMMARY.
    def _resolve_briefing_ceo_name(sid_state: dict) -> Optional[str]:
        apd = sid_state.get('active_profile_data') or {}
        ap = sid_state.get('active_profile') or {}
        return (
            apd.get('ansprechpartner')
            or apd.get('ceo_name')
            or apd.get('ansprechpartner_name')
            or ap.get('ansprechpartner')
            or ap.get('ceo_name')
            or None
        )
    ```

    Suche die Stelle wo `classify_phase(...)` aufgerufen wird (RESEARCH.md verweist auf Z.~954). Aktueller Pseudocode:

    ```python
    result = classify_phase(transcript_window, current_phase, elapsed_s, mode)
    if result:
        ls.state['current_phase'] = result['phase']  # ALT: global state
    ```

    Neuer Code (per-SID State, Hysterese, classify_contact, persist phase_change):

    ```python
    from services.gatekeeper import apply_hysteresis, classify_contact
    from services.live_session import _session_state, _session_state_lock, get_anonymisierer
    from database.db import get_session
    from database.models import CallEvent
    import time

    with _session_state_lock:
        sid_state = _session_state.get(sid, {}).get('state', {})

    current_mode = sid_state.get('current_mode', 'cold_call')
    current_phase_token = sid_state.get('current_phase')  # token like 'opener' or None

    # 1) Classify-Contact (Req-5) — laeuft auf jedem Analyse-Tick wenn category=unknown oder periodisch
    if sid_state.get('contact_category') == 'unknown':
        ceo_name = _resolve_briefing_ceo_name(sid_state)
        category = classify_contact(transcript_window, ceo_name, current_mode)
        if category != 'unknown':
            with _session_state_lock:
                _session_state[sid]['state']['contact_category'] = category
                if category == 'gatekeeper' and current_mode != 'gatekeeper':
                    _session_state[sid]['state']['current_mode'] = 'gatekeeper'
                    _session_state[sid]['state']['current_phase'] = None  # reset hysterese
                    _session_state[sid]['state']['phase_entered_at'] = time.monotonic()
                elif category == 'target' and current_mode == 'gatekeeper':
                    _session_state[sid]['state']['current_mode'] = 'cold_call'
                    _session_state[sid]['state']['current_phase'] = 'opener'  # MODE_TRANSITION_AUTO reset
                    _session_state[sid]['state']['phase_entered_at'] = time.monotonic()
            # Emit Socket-Event fuer PiP-Mode-Update (Plan 07 hoert darauf)
            socketio.emit('contact_category_update', {'category': category, 'mode': _session_state[sid]['state']['current_mode']}, room=sid)

    # 2) Classify-Phase + Hysterese (Req-2, Req-3)
    elapsed_s = time.monotonic() - (sid_state.get('phase_entered_at') or time.monotonic())
    raw_result = classify_phase(transcript_window, current_phase_token, elapsed_s, current_mode)
    if raw_result:
        proposed_phase_token = _PHASE_NAMES_BY_MODE[current_mode][0].get(raw_result['phase'])
        if proposed_phase_token:
            # Hysterese — mutates sid_state in-place
            with _session_state_lock:
                live_state = _session_state.setdefault(sid, {}).setdefault('state', {})
                old_phase = live_state.get('current_phase')
                accepted = apply_hysteresis(live_state, proposed_phase_token, current_mode)
            if accepted:
                # Req-4 — phase_change in call_events persistieren
                call_id = live_state.get('call_id')
                if call_id:
                    db = get_session()
                    try:
                        ev = CallEvent(
                            call_id=call_id,
                            event_type='phase_change',
                            event_ts_ms=int(time.time() * 1000),
                            payload={
                                'old_phase': old_phase,
                                'new_phase': accepted,
                                'confidence': raw_result.get('confidence', 0.0),
                                'mode': current_mode,
                            }
                        )
                        db.add(ev)
                        db.commit()
                    except Exception as e:
                        print(f'[claude_service] phase_change persist Fehler: {type(e).__name__}: {e}')
                        db.rollback()
                    finally:
                        db.close()
                else:
                    print(f'[claude_service] phase_change skipped — no call_id for sid={sid} (Pitfall 4)')
                # Socket emit fuer UI-Update
                socketio.emit('phase_change', {'old': old_phase, 'new': accepted, 'mode': current_mode}, room=sid)
    ```

    Schritt 3 — Deepgram-Loop in services/deepgram_service.py erweitern (Req-7 Trigger-Phrasen + Req-8 UWG-Detection + Review Finding 5 UWG-Guard):

    Suche die Stelle wo nach anonymize() eine Berater-Zeile in conversation_log.append() geschrieben wird (Phase B Pattern in Z.~77-106).

    ZUERST: UWG-Guard am Anfang der Transcript-Loop-Iteration einfuegen (Review Finding 5):

    ```python
    # UWG §7 Guard — Review Finding 5: keine weitere Verarbeitung nach Hard-Block
    # Verhindert weitere Deepgram-Verarbeitung und Claude-API-Calls.
    # Billing-Konsequenz: kein Deepgram/Claude nach uwg_blocked=True.
    if _session_state.get(sid, {}).get('state', {}).get('uwg_blocked'):
        return  # oder: continue — je nach Loop-Struktur (Deepgram-Loop-Callback oder while-Loop)
    ```

    DANN: Trigger-Phrasen-Detection direkt vor oder nach dem conversation_log.append() PRO TRANSCRIPT-ZEILE:

    ```python
    from services.gatekeeper import detect_uwg_hard_block, detect_trigger_phrases

    # ── Req-7: Trigger-Phrasen-Detection ────────────────────────────────
    # Laeuft pro Transcript-Zeile (kein Window — Phrasen sind lokale Pattern).
    # Routing: Socket-Event 'trigger_phrase_hint' fuer PiP-Hinweis + optional
    #          call_events-Log als event_type='reaction' payload.type='trigger_phrase'.
    # Empfehlung: Match auf rohen Text VOR anonymize() (Pattern triggern auch bei teilanonymisierten Inputs).
    try:
        tp_result = detect_trigger_phrases(text)
        if tp_result['matches']:
            sid_state = _session_state.get(sid, {}).get('state', {})
            for category in tp_result['matches']:
                # Socket-Event pro Match (Frontend kann mehrere Hints stacken)
                socketio.emit('trigger_phrase_hint', {
                    'category': category,
                    'phrase': text[:200],
                }, room=sid)
            # call_events als reaction (best-effort, schweigt bei DB-Fehler)
            cid = sid_state.get('call_id')
            if cid:
                try:
                    from database.db import get_session
                    from database.models import CallEvent
                    db = get_session()
                    try:
                        ev = CallEvent(
                            call_id=cid,
                            event_type='reaction',
                            event_ts_ms=int(time.time() * 1000),
                            payload={
                                'type': 'trigger_phrase',
                                'categories': tp_result['matches'],
                                'phrase': text[:200],
                            },
                        )
                        db.add(ev)
                        db.commit()
                    finally:
                        db.close()
                except Exception as e:
                    print(f'[deepgram] trigger_phrase persist Fehler: {type(e).__name__}')
    except Exception as e:
        print(f'[deepgram] trigger_phrase detect Fehler: {type(e).__name__}')

    # ── Req-8: UWG §7 Hard-Block-Detection + uwg_blocked Guard ─────────
    # Hard-Block ist semantisch staerker als trigger_phrase und bekommt einen
    # eigenen Socket-Event + Banner-Pfad (Plan 07) + setzt uwg_blocked=True (Review Finding 5).
    if detect_uwg_hard_block(text):
        # uwg_blocked=True setzen — verhindert weitere Verarbeitung ab naechstem Tick
        with _session_state_lock:
            if sid in _session_state:
                _session_state[sid].setdefault('state', {})['uwg_blocked'] = True
        # In call_events persistieren
        try:
            from database.db import get_session
            from database.models import CallEvent
            db = get_session()
            try:
                cid = _session_state.get(sid, {}).get('state', {}).get('call_id')
                if cid:
                    ev = CallEvent(
                        call_id=cid,
                        event_type='reaction',
                        event_ts_ms=int(time.time() * 1000),
                        payload={'type': 'uwg_hard_block', 'phrase': text[:200]},
                    )
                    db.add(ev)
                    db.commit()
            finally:
                db.close()
        except Exception as e:
            print(f'[deepgram] uwg_hard_block persist Fehler: {type(e).__name__}')
        # Emit Socket-Event fuer PiP-Banner (Plan 07)
        socketio.emit('uwg_hard_block', {'phrase': text[:200]}, room=sid)
        # Nach Emit: kein weiterer Deepgram-Callback (Guard oben faengt naechste Iteration ab)
    ```

    Reihenfolge: Trigger-Phrasen-Detection VOR UWG-Hard-Block-Check (hard_block-Phrasen sind idR auch im TRIGGER_PHRASES-Pattern enthalten). Doppel-Emit ist akzeptabel — Frontend dedupliziert nicht, das ist fachlich gewollt.

    Plan-Author entscheidet: Matching auf rohen Text (empfohlen) VOR anonymize() — Pattern triggern auch wenn anonymize() Woerter verfremdet.
  </action>
  <verify>
    <automated>python -c "import services.claude_service, services.deepgram_service; print('imports OK')" && grep -q "apply_hysteresis" services/claude_service.py && grep -q "phase_change" services/claude_service.py && grep -q "uwg_blocked" services/claude_service.py && grep -q "detect_uwg_hard_block" services/deepgram_service.py && grep -q "detect_trigger_phrases" services/deepgram_service.py && grep -q "trigger_phrase_hint" services/deepgram_service.py && grep -q "uwg_hard_block" services/deepgram_service.py && grep -q "uwg_blocked.*True" services/deepgram_service.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - services/claude_service.py importiert `apply_hysteresis` und `classify_contact` aus services.gatekeeper
    - services/claude_service.py enthält Code-Block der CallEvent mit `event_type='phase_change'` inserted
    - services/claude_service.py liest `_session_state[sid]['state']` (per-SID) statt `ls.state` (global) für contact_category/current_mode/current_phase
    - services/claude_service.py ruft `_resolve_briefing_ceo_name(sid_state)` (oder inline-Aequivalent) auf — keine hardcoded `briefing.get('ceo_name')` ohne Fallback-Kette
    - services/claude_service.py enthaelt UWG-Guard: prueft `uwg_blocked` und returnt/bricht ab wenn True (Review Finding 5)
    - services/deepgram_service.py importiert `detect_uwg_hard_block` UND `detect_trigger_phrases`
    - services/deepgram_service.py ruft `detect_trigger_phrases(text)` PRO TRANSCRIPT-ZEILE (nicht nur in Test-Pfad)
    - services/deepgram_service.py emittet Socket-Event `trigger_phrase_hint` mit `{category: str, phrase: str}` (Req-7)
    - services/deepgram_service.py inserted CallEvent mit `event_type='reaction'` und `payload['type']='uwg_hard_block'`
    - services/deepgram_service.py setzt `_session_state[sid]['state']['uwg_blocked'] = True` bei Hard-Block (Review Finding 5)
    - services/deepgram_service.py enthaelt UWG-Guard am Anfang der Transcript-Loop-Iteration (prueft uwg_blocked, Review Finding 5)
    - Beide Module emiten Socket-Events (`phase_change`, `contact_category_update`, `uwg_hard_block`, `trigger_phrase_hint`) per `socketio.emit(..., room=sid)`
    - `python -m py_compile services/claude_service.py services/deepgram_service.py` exit 0
    - call_id wird vor Insert geprueft (Fail-Safe wenn None — kein IntegrityError)
  </acceptance_criteria>
  <done>Analyse- und Deepgram-Loops sind verdrahtet. UWG-Hard-Block setzt uwg_blocked=True und haelt weitere Verarbeitung an (kein Deepgram/Claude nach Hard-Block). Alle Phase-C-State-Reads sind per-SID, call_events bekommt phase_change + uwg_hard_block + trigger_phrase, Socket-Events trigger_phrase_hint sind live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript_window → classify_contact() | Untrusted user-input (Browser microphone → Deepgram → analyse_loop) |
| Briefing-CEO/GF-Name → _names_match() | Trusted (vom User selbst im Briefing eingegeben) |
| UWG-Phrase → call_events.payload | Untrusted; gespeichert als generischer String, erste 200 Zeichen |
| trigger_phrase text → call_events.payload | Untrusted; analog UWG, 200 Zeichen Cap |
| call_id → CallEvent FK | Trusted (intern erzeugte UUID) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.C-18 | Spoofing | False-Positive in classify_contact -> falscher Modus + falsche Buttons | mitigate | UNION fuer target (Recall), CONSENSUS fuer gatekeeper (FP-Minimierung); manueller Strg+E-Toggle als Backup (Plan 07) |
| T-08.23.2.C-18b | Spoofing | RapidFuzz-Fuzzy-Match zu aggressiv (threshold 85 zu niedrig) | accept | Threshold 0.85 auf Wort-Ebene (nicht Satz-Ebene); kurze Woerter (<3 Zeichen) ausgeschlossen; Degradation bei ImportError auf Substring-only ist sicherer |
| T-08.23.2.C-19 | Elevation of Privilege | Cross-Session-Contamination via globalen ls.state-Write | mitigate | ALLE neuen Keys in _session_state[sid]['state'] (per-SID); kein Write in ls.state; gegrept wird in Verify |
| T-08.23.2.C-20 | Information Disclosure | UWG-Phrase mit PII landet in payload | mitigate | payload['phrase'] auf 200 Zeichen begrenzt; Phrasen-Patterns sind generisch (kein PII-Pattern) — UWG-Detect feuert nur bei festen Mustern |
| T-08.23.2.C-21 | Repudiation | phase_change ohne call_id nicht persistierbar | mitigate | call_id beim Session-Start via create_call_for_sid; Fail-Safe: skip mit Log-Warning wenn None (kein IntegrityError) |
| T-08.23.2.C-23 | Denial of Service | apply_hysteresis hold lock too long | mitigate | Lock nur fuer State-Read; apply_hysteresis selbst lockfrei (state ist Dict-Referenz); CallEvent-Insert ist async-safe |
| T-08.23.2.C-23b | Denial of Service | detect_trigger_phrases laeuft pro Transcript-Zeile -> O(n*m) Pattern-Loop | mitigate | TRIGGER_PHRASES ist ~12 Pattern, Transcript-Zeilen sind kurz (<500 Zeichen); P95-Impact in Perf-Test (Plan 08 Task 5) abgedeckt |
| T-08.23.2.C-24 | Denial of Service | uwg_blocked haelt alle weiteren Calls an — fehlerhafter False-Positive blockt Session dauerhaft | accept | UWG-Block ist UWG §7-Pflicht (legal compliance > Convenience); User kann neue Session starten; False-Positive-Rate der UWG-Pattern-Matches ist via Pitfall-Tests abgedeckt |
</threat_model>

<verification>
- gatekeeper.py-Funktionen alle exportiert und runtime-testbar
- _names_match() nutzt RapidFuzz fuzzy-Matching als Fallback (threshold=0.85)
- classify_contact() nutzt UNION fuer target, CONSENSUS fuer gatekeeper
- init_session_state hat alle neuen Keys (per-SID, NICHT global) inkl. uwg_blocked=False
- create_call_for_sid legt Call-Record + speichert call_id in State
- Analyse-Loop: classify_contact, Hysterese, phase_change-Persist + uwg_blocked Guard
- Deepgram-Loop: Trigger-Phrasen-Detection (Socket-Event trigger_phrase_hint) + UWG-Hard-Block Persist + Emit + uwg_blocked=True + uwg_blocked Guard
- KEIN Write auf ls.state für Phase-C-Keys (grep-Check)
- Socket.IO-Events: contact_category_update, phase_change, uwg_hard_block, trigger_phrase_hint werden emittiert
- requirements.txt: rapidfuzz>=3.0.0 eingetragen
</verification>

<success_criteria>
- `from services.gatekeeper import classify_contact, apply_hysteresis, detect_uwg_hard_block, detect_trigger_phrases, populate_context_notes` exit 0
- `_names_match('meyer', 'Meier')` returnt True (RapidFuzz)
- `init_session_state('test')` legt `_session_state['test']['state']` mit allen Phase-C-Keys an inkl. `uwg_blocked=False`
- `grep "ls\.state\['contact_category'\]" services/*.py` returnt 0 Treffer
- `grep "uwg_blocked.*True" services/deepgram_service.py` returnt >= 1 Treffer (uwg_blocked wird gesetzt)
- `grep "uwg_blocked" services/claude_service.py` returnt >= 1 Treffer (Guard vorhanden)
- Live-Simulation (manuell, mit gemockten Inputs): phase_change-Event landet in call_events nach Hysterese-Akzeptanz
- detect_trigger_phrases('Worum geht es?') in der Deepgram-Loop -> Socket-Event trigger_phrase_hint mit category='inquiry' wird emittiert
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-06-SUMMARY.md`
</output>
