---
phase: 08.23.2.C
plan: 06b
type: execute
wave: 3
depends_on: [02, 06]
files_modified:
  - alembic/versions/0003_add_phrases_mode.py
autonomous: true
requirements: [Req-9]
tags: [nerve, gatekeeper, seed-data, alembic, mr-miyagi-phrases]
must_haves:
  truths:
    - "Migration 0003 enthaelt am Ende von upgrade() einen op.bulk_insert mit ≥8 Gatekeeper-Phrasen (≥2 Varianten pro 4 Buttons per D-05)"
    - "downgrade() loescht die Seed-Phrasen vor dem drop_column"
    - "Nach `alembic upgrade head`: SELECT count(*) FROM phrases WHERE mode='gatekeeper' >= 8"
  artifacts:
    - path: "alembic/versions/0003_add_phrases_mode.py"
      provides: "Plan-02-Migration um Seed-Insert fuer Gatekeeper-Phrases (≥2 Varianten pro Button) erweitert"
      contains: "op.bulk_insert"
  key_links:
    - from: "tests/fixtures/gatekeeper_phrases_seed.md (Andre-Gate)"
      to: "alembic/versions/0003_add_phrases_mode.py bulk_insert payload"
      via: "Plan-Author kopiert die Texte 1:1 in den bulk_insert-Block"
      pattern: "gatekeeper_verbuendeten_bitte"
---

<objective>
Migration 0003 (aus Plan 02 — `phrases.mode`-Spalte) um Seed-Insert fuer Mr.-Miyagi-Gatekeeper-Phrasen erweitern. D-05 verlangt ≥2-3 Varianten pro Button — dieser Plan implementiert ≥2 pro Button (4 Buttons × ≥2 = ≥8 Rows).

Purpose: Plan 07 (PiP-Frontend) ruft `/api/gatekeeper/phrases` auf und erwartet 4 Button-Gruppen mit jeweils mehreren Varianten. Ohne Seed-Daten ist der Endpoint leer und das Frontend kann keine Buttons rendern.

Output: Migration 0003 enthaelt 8+ Gatekeeper-Phrases mit `mode='gatekeeper'`, `user_id=1`, 4 verschiedenen `objection_type`-Werten.

**Scope-Trennung:** Dieser Plan war urspruenglich Plan 06 Task 4. Wurde zur Scope-Reduktion (Cross-AI-Review: Plan 06 touched 5 Files) als eigener Wave-3-Plan ausgelagert. Plan 06 enthaelt jetzt nur noch Wiring-Logik (gatekeeper.py + live_session.py + analyse-loop).

**Cross-Plan-Konsistenz (Review Finding 5):** Plan 06 Task 3 setzt `uwg_blocked=True` im Session-State wenn detect_uwg_hard_block() feuert. Dieser Plan (06b) beruehrt nur die Migration / Seed-Daten — der uwg_blocked-Guard ist in Plan 06 Task 2 (init_session_state) und Task 3 (deepgram_service.py) implementiert. Hier kein weiteres Zutun noetig. Seed-Phrasen sind die *Inhalte* der Mr.-Miyagi-Buttons; UWG-Hard-Block-Erkennung laeuft separat via services/ki_logik.py UWG_HARD_BLOCK_PATTERNS (Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-02-SUMMARY.md
@tests/fixtures/gatekeeper_phrases_seed.md
@alembic/versions/0003_add_phrases_mode.py
@database/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0003 — Seed-Insert fuer ≥2 Varianten pro Gatekeeper-Button</name>
  <files>alembic/versions/0003_add_phrases_mode.py</files>
  <read_first>
    - alembic/versions/0003_add_phrases_mode.py (von Plan 02 — Schema-Migration upgrade()+downgrade() bereits da)
    - tests/fixtures/gatekeeper_phrases_seed.md (Andre-bestaetigte Phrases aus Plan 01 — Plan-Author nimmt EXAKT diese Texte. Falls Andre Aenderungen committet hat, die geaenderten Texte verwenden, NICHT den Original-Entwurf)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md (D-05 — user_id=1 MVP, ≥2-3 Varianten pro Button, source-Kommentar pro Phrase)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pitfall 7 — phrases.user_id NOT NULL, MVP-Loesung user_id=1)
    - database/models.py Phrase-Klasse (Felder: id, user_id, session_id, text, objection_type, created_at, quality_tier, mode)
  </read_first>
  <action>
    Migration 0003 hat heute (aus Plan 02) nur den Schema-Schritt (ADD COLUMN + CHECK). Erweitere die `upgrade()`-Funktion am ENDE um data-only-Insert der Gatekeeper-Seed-Phrases.

    **D-05-Pflicht:** Mindestens 2 Varianten pro Button. Plan-Author kann gerne 3 pro Button nehmen wenn `tests/fixtures/gatekeeper_phrases_seed.md` das hergibt.

    Insert-Schritt am Ende von upgrade() — nach CHECK-Constraint:

    ```python
    # Gatekeeper-Seed-Phrases (Phase 08.23.2.C D-05, Req-9).
    # Eingelesen aus tests/fixtures/gatekeeper_phrases_seed.md (Andre-Gate approved in Plan 01).
    # user_id=1 (Admin-MVP — Pitfall 7, RESEARCH.md A7).
    # objection_type entspricht der Mr.-Miyagi-Button-ID im PiP.
    # D-05: mindestens 2 Varianten pro Button (4 Buttons × ≥2 = ≥8 Rows).
    # uwg_blocked-Kontext: Diese Seed-Phrasen sind Vertriebs-Reaktions-Vorschlaege (Mr. Miyagi),
    # NICHT die UWG-Hard-Block-Erkennungs-Pattern (die liegen in services/ki_logik.py).
    # Plan 06 Task 3 setzt uwg_blocked=True wenn ein UWG-Pattern erkannt wird —
    # ab diesem Zeitpunkt sind die Mr.-Miyagi-Buttons nicht mehr relevant (Guard blockiert Loop).
    phrases_table = sa.table(
        'phrases',
        sa.column('user_id', sa.Integer),
        sa.column('text', sa.Text),
        sa.column('objection_type', sa.String),
        sa.column('mode', sa.String),
        sa.column('quality_tier', sa.String),
    )

    op.bulk_insert(phrases_table, [
        # ── Button 1: Verbuendeten-Bitte (Source: Limbeck) — 2 Varianten
        {'user_id': 1, 'text': '{vorname}, duerfte ich Sie um Ihre Einschaetzung bitten — wer waere bei Ihnen fuer {branche} der richtige Ansprechpartner?',
         'objection_type': 'gatekeeper_verbuendeten_bitte', 'mode': 'gatekeeper', 'quality_tier': 'A'},
        {'user_id': 1, 'text': 'Frau {nachname}, Sie kennen das Haus am besten — bei wem soll ich anfragen wegen {detail}?',
         'objection_type': 'gatekeeper_verbuendeten_bitte', 'mode': 'gatekeeper', 'quality_tier': 'A'},
        # ── Button 2: Insider-Antwort (Source: Heinrich) — 2 Varianten
        {'user_id': 1, 'text': 'Es geht um {detail} — koennten Sie mich bitte mit der zustaendigen Person verbinden?',
         'objection_type': 'gatekeeper_insider_antwort', 'mode': 'gatekeeper', 'quality_tier': 'A'},
        {'user_id': 1, 'text': 'Konkret: {detail}. Wer ist bei Ihnen dafuer der richtige Ansprechpartner?',
         'objection_type': 'gatekeeper_insider_antwort', 'mode': 'gatekeeper', 'quality_tier': 'A'},
        # ── Button 3: Voss-Label (Source: Chris Voss) — 2 Varianten
        {'user_id': 1, 'text': 'Es klingt so, als ob Sie das tagtaeglich filtern muessen — was waere der einfachste Weg, mich kurz durchzustellen?',
         'objection_type': 'gatekeeper_voss_label', 'mode': 'gatekeeper', 'quality_tier': 'A'},
        {'user_id': 1, 'text': 'Ich verstehe, {vorname}. Es klingt so, als haetten Sie bereits Erfahrungen mit aehnlichen Loesungen gemacht — stimmt das?',
         'objection_type': 'gatekeeper_voss_label', 'mode': 'gatekeeper', 'quality_tier': 'A'},
        # ── Button 4: Vornamen-Pause (Source: Limbeck) — 2 Varianten
        {'user_id': 1, 'text': '{vorname} ... ich brauche genau zwei Minuten.',
         'objection_type': 'gatekeeper_vornamen_pause', 'mode': 'gatekeeper', 'quality_tier': 'A'},
        {'user_id': 1, 'text': '{vorname} ... [Pause] ... Ich schaetze Ihre Direktheit.',
         'objection_type': 'gatekeeper_vornamen_pause', 'mode': 'gatekeeper', 'quality_tier': 'A'},
    ])
    ```

    WICHTIG — Plan-Author muss tests/fixtures/gatekeeper_phrases_seed.md GENAU lesen (post Andre-Gate) und die Varianten 1:1 in die bulk_insert-Liste uebernehmen. Die obigen Texte sind der derzeit bekannte Entwurf — wenn Andre den Markdown editiert hat, die geaenderten Texte nehmen (NICHT den Original-Entwurf).

    downgrade() entsprechend erweitern — vor `drop_column` die Seed-Daten loeschen:
    ```python
    def downgrade() -> None:
        # Seed-Daten loeschen vor Schema-Aenderung
        op.execute("DELETE FROM phrases WHERE mode = 'gatekeeper' AND objection_type LIKE 'gatekeeper_%'")
        op.execute("ALTER TABLE phrases DROP CONSTRAINT IF EXISTS ck_phrases_mode")
        op.drop_column('phrases', 'mode')
    ```

    Verifikation nach Migration-Run: `SELECT COUNT(*) FROM phrases WHERE mode='gatekeeper'` >= 8 (D-05 ≥2 Varianten pro 4 Buttons).
  </action>
  <verify>
    <automated>python -c "
import re, pathlib
src = pathlib.Path('alembic/versions/0003_add_phrases_mode.py').read_text(encoding='utf-8')
assert 'op.bulk_insert' in src, 'bulk_insert fehlt'
for t in ('gatekeeper_verbuendeten_bitte','gatekeeper_insider_antwort','gatekeeper_voss_label','gatekeeper_vornamen_pause'):
    cnt = src.count(\"'\" + t + \"'\")
    assert cnt >= 2, f'objection_type {t} hat nur {cnt} Varianten (D-05 verlangt >=2)'
print('Seed-Insert OK: ≥2 Varianten pro Button')
"</automated>
  </verify>
  <acceptance_criteria>
    - alembic/versions/0003_add_phrases_mode.py enthaelt `op.bulk_insert(phrases_table, ...)` Block am Ende von upgrade()
    - Mindestens 8 Insert-Rows (D-05: ≥2 Varianten pro 4 Buttons)
    - Pro objection_type-Wert mindestens 2 Rows: gatekeeper_verbuendeten_bitte, gatekeeper_insider_antwort, gatekeeper_voss_label, gatekeeper_vornamen_pause
    - Alle Rows haben `'mode': 'gatekeeper'`
    - Alle Rows haben `'user_id': 1` (Admin-MVP)
    - downgrade() loescht die Seed-Phrases vor dem drop_column
    - Texte stimmen mit tests/fixtures/gatekeeper_phrases_seed.md ueberein (Plan-Author verifiziert manuell vor Commit)
    - `python -m py_compile alembic/versions/0003_add_phrases_mode.py` exit 0
  </acceptance_criteria>
  <done>Migration 0003 inserted ≥8 Gatekeeper-Seed-Phrases (≥2 Varianten pro 4 Buttons) mit user_id=1, mode='gatekeeper'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/fixtures/gatekeeper_phrases_seed.md → bulk_insert payload | Trusted (Andre-Gate gefiltert) |
| Seed-Phrases → phrases-Tabelle | Trusted; user_id=1 (Admin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.C-22 | Tampering | Gatekeeper-Phrase-Inserts (user_id=1) — Admin-Phrases ungeschuetzt | accept | MVP — Admin-Account ist Solo-Founder-only; spaeter (Phase 2+) ggf. system-level Phrases mit user_id=NULL |
| T-08.23.2.C-22b | Tampering | Migration laeuft 2x -> duplikate Seeds | accept | Alembic-Versioning verhindert Re-Run; downgrade() loescht definierte Range vor drop_column |
</threat_model>

<verification>
- Migration 0003 enthaelt bulk_insert fuer ≥8 Phrases (≥2 pro Button)
- 4 verschiedene objection_type-Werte
- Alle mode='gatekeeper', user_id=1
- downgrade() saubert die Seeds bevor drop_column laeuft
- Migration laeuft idempotent (Alembic-Versioning + sauberer downgrade)
- Cross-Plan: uwg_blocked-State-Key ist in Plan 06 Task 2 (init_session_state) implementiert; Plan 06 Task 3 (deepgram_service.py) setzt uwg_blocked=True und blockiert weitere Verarbeitung — dieser Plan beruehrt nur Seed-Daten
</verification>

<success_criteria>
- `python -m py_compile alembic/versions/0003_add_phrases_mode.py` exit 0
- Nach `alembic upgrade head`: SELECT COUNT(*) FROM phrases WHERE mode='gatekeeper' >= 8
- Nach `alembic downgrade -1`: SELECT COUNT(*) FROM phrases WHERE mode='gatekeeper' == 0 (Seeds geloescht)
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-06b-SUMMARY.md`
</output>
