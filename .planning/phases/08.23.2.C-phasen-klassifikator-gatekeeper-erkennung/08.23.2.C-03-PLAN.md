---
phase: 08.23.2.C
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - config/__init__.py
  - config/phase_transitions.py
  - scripts/calibrate_phase_durations.py
  - .planning/04 Entscheidungen/Foundation-Code-Register.md
autonomous: true
requirements: [Req-3, Req-11]
tags: [nerve, config, hysteresis, foundation-register]
must_haves:
  truths:
    - "config/phase_transitions.py existiert mit MIN_PHASE_DURATIONS, ALLOWED_TRANSITIONS, FORBIDDEN_TRANSITIONS pro Modus"
    - "Hysterese-Mindest-Zeiten dokumentieren D-03-Werte 1:1"
    - "Foundation-Code-Register existiert und dokumentiert populate_context_notes-Stub für Phase 08.23.2.I"
    - "Kalibrierungs-Skript scripts/calibrate_phase_durations.py ist lauffähig"
  artifacts:
    - path: "config/__init__.py"
      provides: "config-Package-Marker (Directory existiert noch nicht — Forschungsbefund)"
    - path: "config/phase_transitions.py"
      provides: "Hysterese-Konfig + Übergangs-Matrix (Req-3)"
      contains: "MIN_PHASE_DURATIONS"
    - path: "scripts/calibrate_phase_durations.py"
      provides: "Stufe-1-Kalibrierung gegen Korpus (D-03)"
    - path: ".planning/04 Entscheidungen/Foundation-Code-Register.md"
      provides: "Register für Foundation-Stubs (Req-11)"
      contains: "populate_context_notes"
  key_links:
    - from: "config/phase_transitions.py"
      to: "services/gatekeeper.py + services/claude_service.py (Wave 2/3 consumers)"
      via: "Import-Statement: from config.phase_transitions import MIN_PHASE_DURATIONS, ALLOWED_TRANSITIONS, FORBIDDEN_TRANSITIONS"
      pattern: "from config.phase_transitions import"
    - from: ".planning/04 Entscheidungen/Foundation-Code-Register.md"
      to: "services/gatekeeper.py populate_context_notes-Stub + Phase 08.23.2.I-Aktivierung"
      via: "Register-Eintrag mit Stub-Name + Aktivierungs-Phase"
      pattern: "populate_context_notes"
---

<objective>
Konfigurations-Foundation für Hysterese-Logik (Req-3) und Foundation-Code-Register (Req-11) erstellen. config/-Directory existiert laut RESEARCH.md noch nicht und muss angelegt werden.

Purpose: Wave 2 (P05 classify_phase) und Wave 3 (P06 gatekeeper.py) konsumieren die Hysterese-Konfig direkt per Import. Foundation-Code-Register dokumentiert den `populate_context_notes`-Stub, den P06 in services/gatekeeper.py anlegt — der Eintrag erklärt warum der Stub existiert und welche Phase ihn aktiviert (08.23.2.I).

Output: config/phase_transitions.py mit drei Konfig-Dicts (MIN_PHASE_DURATIONS, ALLOWED_TRANSITIONS, FORBIDDEN_TRANSITIONS) pro Modus, Kalibrierungs-Skript-Stub, Foundation-Code-Register als neue Datei.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md
@services/ki_logik.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: config/-Directory + phase_transitions.py</name>
  <files>config/__init__.py, config/phase_transitions.py</files>
  <read_first>
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md (D-03 — vollständige Werte und Übergangs-Matrix)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pattern 6 + Don't Hand-Roll Section)
    - services/ki_logik.py (SCORE_FACTORS-Pattern als Vorlage für Python-Dict mit Kommentaren)
  </read_first>
  <action>
    Schritt 1 — config/__init__.py:
    Leere Datei (Package-Marker). Inhalt:
    ```python
    # config-Package — Hysterese-Konfig, Phase-Transition-Matrix.
    # Neu in Phase 08.23.2.C (per RESEARCH.md: config/ existierte noch nicht).
    ```

    Schritt 2 — config/phase_transitions.py:
    Inhalt 1:1 aus D-03 (CONTEXT.md). Python-Dict mit Kommentar pro Wert. Code-Identifier in ASCII (mode-Keys, Phase-Namen sind bereits englische Tokens).

    ```python
    """Hysterese-Konfig für Phasen-Klassifikator (Phase 08.23.2.C, D-03).

    Werte sind initiale Schätzungen — Stufe-1-Kalibrierung über
    scripts/calibrate_phase_durations.py vor erstem Live-Test.
    Stufe-2-Kalibrierung monatlich gegen call_events.event_type='phase_change'
    (siehe CLAUDE.md Punkt 3c).
    """

    # Mindest-Verweildauer pro Phase in SEKUNDEN.
    # Wechsel akzeptiert nur wenn: (1) >=2 aufeinanderfolgende Classifier-Hinweise
    # UND (2) elapsed_in_current_phase >= MIN_PHASE_DURATIONS[mode][phase_name]
    # UND (3) Uebergang in ALLOWED_TRANSITIONS[mode] erlaubt.
    MIN_PHASE_DURATIONS = {
        'cold_call': {
            'opener': 5,       # Begruessungs-Phase ist sehr kurz
            'permission': 8,   # Erlaubnis-Frage typisch 5-10s
            'reason': 12,      # Grund des Anrufs braucht Substanz
            'pitch': 20,       # Pitch braucht Zeit zum Aufbau
            'discovery': 30,   # Bedarfsanalyse: laengste Phase
            'closing': 15,     # Abschluss + Termin-Vereinbarung
        },
        'gatekeeper': {
            'greeting': 3,     # "Guten Tag, Mueller-AG"
            'identify': 5,     # "Wen darf ich melden"
            'bypass': 10,      # Mr.-Miyagi-Phase, braucht 2-3 Saetze
            'handoff': 5,      # Uebergabe oder Ablehnung
        },
        'meeting': {
            'intro': 10,       # Vorstellung beider Seiten
            'agenda': 15,      # Agenda-Klaerung
            'discovery': 45,   # Meeting-Bedarfsanalyse laenger als Cold-Call
            'pitch': 30,       # Praesentation
            'objection': 20,   # Einwand-Block
            'closing': 20,     # Termin / Naechste Schritte
        },
    }

    # Erlaubte Vorwaerts- + Rueckwaerts-Uebergaenge pro Modus.
    # Format: {mode: {from_phase: [to_phase_1, to_phase_2, ...]}}
    ALLOWED_TRANSITIONS = {
        'cold_call': {
            'opener': ['permission'],
            'permission': ['reason'],
            'reason': ['pitch'],
            'pitch': ['discovery', 'closing'],          # closing direkt erlaubt (Express-Pitch)
            'discovery': ['pitch', 'closing'],           # Einwand-Recovery rueckwaerts erlaubt
            'closing': ['discovery', 'pitch'],           # Einwand-Recovery rueckwaerts erlaubt
        },
        'gatekeeper': {
            'greeting': ['identify'],
            'identify': ['bypass'],
            'bypass': ['identify', 'handoff'],           # rueck zu identify wenn neuer Name fallt
            'handoff': ['bypass'],                       # rueck zu bypass wenn Uebergabe-Versuch scheitert
        },
        'meeting': {
            'intro': ['agenda'],
            'agenda': ['discovery'],
            'discovery': ['pitch', 'objection'],
            'pitch': ['discovery', 'objection'],
            'objection': ['discovery', 'pitch', 'closing'],
            'closing': ['objection'],                    # rueck zu objection wenn neuer Einwand kommt
        },
    }

    # Explizit verbotene Uebergaenge (Sanity-Check / dokumentiert auch wenn nicht in ALLOWED).
    # Hauptzweck: Klarstellung dass kein Sprung zurueck zu Begruessungs-Phasen erlaubt ist.
    FORBIDDEN_TRANSITIONS = {
        'cold_call': {
            'permission': ['opener'],
            'reason': ['opener', 'permission'],
            'pitch': ['opener', 'permission'],
            'discovery': ['opener', 'permission'],
            'closing': ['opener', 'permission', 'reason'],
        },
        'gatekeeper': {
            'identify': ['greeting'],
            'bypass': ['greeting'],
            'handoff': ['greeting', 'identify'],
        },
        'meeting': {
            'agenda': ['intro'],
            'discovery': ['intro', 'agenda'],
            'pitch': ['intro', 'agenda'],
            'objection': ['intro', 'agenda'],
            'closing': ['intro', 'agenda', 'discovery'],
        },
    }

    # Modus-Wechsel-Regel (D-03):
    # gatekeeper -> cold_call: immer erlaubt wenn classify_contact() == 'target'
    #   -> Hysterese-Reset: aktive Phase wird zu 'opener' (cold_call), Timer neu.
    # cold_call -> gatekeeper: NUR via manueller Strg+G-Toggle (kein Auto).
    MODE_TRANSITION_AUTO = {
        # Auto-allowed: (from_mode, to_mode) Tupel
        ('gatekeeper', 'cold_call'): True,
        ('gatekeeper', 'meeting'): False,
        ('cold_call', 'gatekeeper'): False,    # nur manuell
        ('cold_call', 'meeting'): False,        # call_mode-bound, kein mid-call swap
        ('meeting', 'cold_call'): False,
        ('meeting', 'gatekeeper'): False,       # nur manuell
    }

    # Anzahl aufeinanderfolgender Classifier-Hinweise bevor Wechsel akzeptiert wird (D-03 Bedingung 1).
    HYSTERESIS_REQUIRED_HINTS = 2
    ```

    Code-Identifier ASCII: alle Dict-Keys (mode-Namen + Phase-Namen) sind englisch — keine Umlaute. Kommentare nutzen ASCII (ue/ae) — Plan-Konvention.
  </action>
  <verify>
    <automated>python -c "from config.phase_transitions import MIN_PHASE_DURATIONS, ALLOWED_TRANSITIONS, FORBIDDEN_TRANSITIONS, MODE_TRANSITION_AUTO, HYSTERESIS_REQUIRED_HINTS; assert set(MIN_PHASE_DURATIONS.keys()) == {'cold_call', 'gatekeeper', 'meeting'}; assert len(MIN_PHASE_DURATIONS['cold_call']) == 6; assert len(MIN_PHASE_DURATIONS['gatekeeper']) == 4; assert len(MIN_PHASE_DURATIONS['meeting']) == 6; assert HYSTERESIS_REQUIRED_HINTS == 2; print('config/phase_transitions OK')"</automated>
  </verify>
  <acceptance_criteria>
    - config/__init__.py existiert (kann minimal sein)
    - config/phase_transitions.py importierbar: `python -c "import config.phase_transitions"` exit 0
    - MIN_PHASE_DURATIONS hat genau 3 Top-Level-Keys: 'cold_call' (6 Phasen), 'gatekeeper' (4 Phasen), 'meeting' (6 Phasen)
    - cold_call-Phasen sind exakt: opener, permission, reason, pitch, discovery, closing
    - gatekeeper-Phasen sind exakt: greeting, identify, bypass, handoff
    - meeting-Phasen sind exakt: intro, agenda, discovery, pitch, objection, closing
    - Werte stimmen 1:1 mit D-03 überein (opener=5, permission=8, reason=12, pitch=20, discovery=30, closing=15; greeting=3, identify=5, bypass=10, handoff=5; intro=10, agenda=15, discovery=45, pitch=30, objection=20, closing=20)
    - ALLOWED_TRANSITIONS['gatekeeper'] enthält KEINEN Key 'handoff' der zu 'greeting' geht (verbotener Sprung)
    - FORBIDDEN_TRANSITIONS['gatekeeper']['handoff'] enthält 'greeting' (explizit verboten)
    - HYSTERESIS_REQUIRED_HINTS == 2
    - MODE_TRANSITION_AUTO[('gatekeeper','cold_call')] is True
    - MODE_TRANSITION_AUTO[('cold_call','gatekeeper')] is False
  </acceptance_criteria>
  <done>Hysterese-Konfig importierbar, alle D-03-Werte verdrahtet.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/calibrate_phase_durations.py — Stufe-1-Kalibrierung</name>
  <files>scripts/calibrate_phase_durations.py</files>
  <read_first>
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md (D-03 Stufe-1-Kalibrierung)
    - tests/fixtures/phase_classifier_corpus.schema.json (von Plan 01 erstellt — Schema kennen)
    - config/phase_transitions.py (von Task 1 — MIN_PHASE_DURATIONS als Vergleichsbasis)
  </read_first>
  <action>
    Standalone-Skript (kein pytest-Aufruf). Aufruf: `python scripts/calibrate_phase_durations.py`.

    Funktionalität:
    1. Lädt `tests/fixtures/phase_classifier_corpus.json` (kann fehlen — dann Print "Korpus fehlt, calibration übersprungen" + exit 0).
    2. Für jeden Eintrag: Schätzt Phase-Dauer aus `len(transcript_window) * 4` Sekunden (heuristisch: pro Berater-Satz ~4s, weil Pause + Antwort + nächster Satz).
       Alternative wenn Plan-Author einen besseren Heuristik-Wert hat: dokumentieren als Kommentar oben im Skript.
    3. Aggregiert pro `mode` und `expected_phase` → Average + P50 + P95 in Sekunden.
    4. Vorschlag-Werte: `recommended_min = max(3, round(avg * 0.6))` (60% des Avg, mindestens 3s).
    5. Vergleicht mit aktuellen `MIN_PHASE_DURATIONS` aus `config/phase_transitions.py`.
    6. Ausgabe als Tabelle pro mode:
       ```
       [CALIBRATE] cold_call
         opener        current=5   corpus_avg=4.2   recommended=3   delta=-2
         permission    current=8   corpus_avg=6.5   recommended=4   delta=-4
         ...
       ```
    7. Schreibt KEINE Änderung an config/phase_transitions.py — gibt nur Vorschlag aus. André bestätigt manuell und editiert die Konfig.

    Exit-Code: 0 (Diagnose, kein Gate).
  </action>
  <verify>
    <automated>python -m py_compile scripts/calibrate_phase_durations.py && python scripts/calibrate_phase_durations.py</automated>
  </verify>
  <acceptance_criteria>
    - Datei scripts/calibrate_phase_durations.py existiert und ist syntaktisch korrekt
    - `python scripts/calibrate_phase_durations.py` exit 0, auch wenn Korpus-Datei fehlt
    - Skript importiert `MIN_PHASE_DURATIONS` aus `config.phase_transitions`
    - Skript liest `tests/fixtures/phase_classifier_corpus.json` (mit try/except FileNotFoundError)
    - Skript schreibt keine Datei (read-only Diagnose)
    - Output enthält Strings `[CALIBRATE]` und `recommended`
  </acceptance_criteria>
  <done>Kalibrierungs-Skript ist lauffähig und unabhängig von Korpus-Datei.</done>
</task>

<task type="auto">
  <name>Task 3: Foundation-Code-Register.md anlegen</name>
  <files>.planning/04 Entscheidungen/Foundation-Code-Register.md</files>
  <read_first>
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-11)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md (D-02 — populate_context_notes-Stub-Definition)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Assumption A6 — Datei existiert noch nicht, muss neu angelegt werden)
  </read_first>
  <action>
    Datei `.planning/04 Entscheidungen/Foundation-Code-Register.md` neu anlegen (Directory ".planning/04 Entscheidungen/" existiert laut Glob-Check noch nicht — anlegen lassen durch Write).

    Inhalt:
    ```markdown
    # Foundation-Code-Register

    **Zweck:** Stubs/leere Strukturen die heute angelegt werden um spaetere Phasen ohne Refactor zu aktivieren. Jeder Eintrag dokumentiert: Was es ist, warum es leer ist, welche Phase es aktiviert.

    **Regel:** Stubs duerfen nur leere Bodies (`pass`) oder leere Container (`[]`, `{}`) sein. Keine "fake" Implementierung. Aktivierung erfolgt durch die in "Aktiviert von" genannte Phase.

    ---

    ## Eintrag 1: `populate_context_notes(state, entity) -> None`

    | Feld | Wert |
    |------|------|
    | Stub-Funktion | `populate_context_notes(state, entity) -> None: pass` |
    | Modul | `services/gatekeeper.py` (Phase 08.23.2.C P06) |
    | Angelegt von | Phase 08.23.2.C (D-02) |
    | Aktiviert von | Phase 08.23.2.I (Sekretaer-Uebergabe-Feature / Live-Notizblock) |
    | State-Key | `_session_state[sid]['state']['context_notes']` (initialisiert als `[]` in `init_session_state()`) |
    | Aktueller Stand | Stub mit pass-Body, kein Aufruf im Live-Loop. context_notes-Liste bleibt leer. |
    | Aktivierungs-Trigger | Phase 08.23.2.I implementiert NER-Extraktion auf Gatekeeper-Aussagen (z.B. "Herr Schmidt ist im Termin bis 14 Uhr") und ruft `populate_context_notes(state, entity)` auf — Funktion fuellt `context_notes` mit strukturierten Hinweisen. |

    **Rationale (Phase 08.23.2.C):** State-Feld + Stub-Funktion werden jetzt angelegt, damit Phase 08.23.2.I nur die Body-Implementierung schreibt — ohne Session-State-Migration oder neue Import-Pfade.

    ---

    ## Eintrag 2: `context_notes`-State-Feld

    | Feld | Wert |
    |------|------|
    | State-Pfad | `_session_state[sid]['state']['context_notes']` |
    | Initial-Wert | `[]` (leere Liste) |
    | Modul | `services/live_session.py` `init_session_state()` (Phase 08.23.2.C P06) |
    | Angelegt von | Phase 08.23.2.C (Req-11) |
    | Aktiviert von | Phase 08.23.2.I |
    | Reset-Verhalten | `pop_session_state(sid)` loescht die Liste automatisch via `dict.pop`. |
    | Aktueller Stand | Liste wird angelegt, aber nie befuellt. Kein UI-Konsument. |

    ---

    *Register erstellt: 2026-05-14 (Phase 08.23.2.C Plan 03)*
    ```

    Konvention: ASCII-Kommentare im Markdown OK (CLAUDE.md erlaubt Plan-Docs in ASCII), Bezeichner sind ohnehin englisch.
  </action>
  <verify>
    <automated>test -s ".planning/04 Entscheidungen/Foundation-Code-Register.md" && grep -q "populate_context_notes" ".planning/04 Entscheidungen/Foundation-Code-Register.md" && grep -q "context_notes" ".planning/04 Entscheidungen/Foundation-Code-Register.md" && grep -q "08.23.2.I" ".planning/04 Entscheidungen/Foundation-Code-Register.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Datei `.planning/04 Entscheidungen/Foundation-Code-Register.md` existiert und ist nicht leer
    - Enthält Eintrag für `populate_context_notes` mit Modul `services/gatekeeper.py`
    - Enthält Eintrag für State-Feld `context_notes` mit Pfad `_session_state[sid]['state']['context_notes']`
    - Enthält Phase-Verweis auf `08.23.2.I` (Aktivierungs-Phase)
    - Enthält Phase-Verweis auf `08.23.2.C` (Anlage-Phase)
  </acceptance_criteria>
  <done>Foundation-Code-Register dokumentiert beide Stubs (Funktion + State-Feld) für Phase 08.23.2.I.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/phase_transitions.py → consumed by services/* | Config-Modul mit Dict-Werten; nur Read-Pfad in Production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.C-08 | Tampering | config/phase_transitions.py wird zur Laufzeit veraendert | accept | Modul-Reload nicht moeglich ohne Service-Restart; Konfig ist Code, kein User-Input |
| T-08.23.2.C-09 | Repudiation | Kalibrierungs-Skript schreibt nicht zurueck — Aenderungen erfordern manuelle Andre-Bestaetigung | mitigate | calibrate-Skript ist read-only-Diagnose; manuelle Andre-Bestaetigung pro Wert per D-03 |
</threat_model>

<verification>
- config/phase_transitions.py importierbar und alle Konstanten verfügbar
- Kalibrierungs-Skript lauffähig auch ohne Korpus-Datei
- Foundation-Code-Register dokumentiert Stub + State-Feld
</verification>

<success_criteria>
- `python -c "import config.phase_transitions"` exit 0
- `python scripts/calibrate_phase_durations.py` exit 0
- Foundation-Code-Register existiert und referenziert beide Foundation-Items
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-03-SUMMARY.md`
</output>
