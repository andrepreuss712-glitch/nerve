---
phase: "08.23.2.C"
plan: "06b"
subsystem: "gatekeeper-seed-data"
tags: [nerve, gatekeeper, seed-data, alembic, mr-miyagi-phrases, migration]
dependency_graph:
  requires: ["08.23.2.C-02", "08.23.2.C-06"]
  provides: ["gatekeeper-seed-phrases-in-db"]
  affects: ["08.23.2.C-07"]
tech_stack:
  added: []
  patterns: ["op.bulk_insert fuer Data-Migration in Alembic upgrade()"]
key_files:
  modified:
    - alembic/versions/0003_add_phrases_mode.py
decisions:
  - "Alle 10 Andre-Gate-genehmigten Phrasen aus gatekeeper_phrases_seed.md 1:1 uebernommen (3+3+2+2 pro Button)"
  - "User-facing Phrase-Texte verwenden echte Umlaute (CLAUDE.md-Regel: User-Text mit Umlauten)"
  - "Code-Identifier (objection_type-Werte wie gatekeeper_verbuendeten_bitte) ohne Umlaute (ASCII)"
  - "downgrade() loescht Seeds via DELETE WHERE mode='gatekeeper' AND objection_type LIKE 'gatekeeper_%'"
metrics:
  duration: "10 Minuten"
  completed: "2026-05-15"
  tasks_completed: 1
  files_modified: 1
---

# Phase 08.23.2.C Plan 06b: Gatekeeper Seed-Insert (Migration 0003) Summary

## One-Liner

Migration 0003 um op.bulk_insert erweitert: 10 Gatekeeper-Phrasen (3+3+2+2 Varianten fuer 4 Mr.-Miyagi-Buttons) mit user_id=1, mode='gatekeeper'.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Migration 0003 — Seed-Insert fuer >=2 Varianten pro Gatekeeper-Button | 4fb6267 | alembic/versions/0003_add_phrases_mode.py |

## What Was Built

Migration 0003 (`alembic/versions/0003_add_phrases_mode.py`) wurde um den Data-Insert-Schritt erweitert. Am Ende der `upgrade()`-Funktion — nach dem CHECK-Constraint — wird ein `op.bulk_insert()` Block ausgefuehrt:

**Button 1: gatekeeper_verbuendeten_bitte (3 Varianten)**
- Quelle: Stephan Heinrich + Ulrike Knauer
- Template-Variablen: {vorname}, {branche}, {detail}

**Button 2: gatekeeper_insider_antwort (3 Varianten)**
- Quelle: Tim Taxis + Eduard Klein
- Template-Variablen: {detail}, {nachname}

**Button 3: gatekeeper_voss_label (2 Varianten)**
- Quelle: Chris Voss (Never Split the Difference, Labeling-Technik)

**Button 4: gatekeeper_vornamen_pause (2 Varianten)**
- Quelle: Martin Limbeck (Vornamenspause, Pattern-Interrupt)
- Template-Variablen: {vorname}, {nachname}

Alle 10 Rows: `mode='gatekeeper'`, `user_id=1` (Admin-MVP per RESEARCH.md A7), `quality_tier='A'`.

`downgrade()` wurde erweitert: loescht Seeds (`DELETE FROM phrases WHERE mode = 'gatekeeper' AND objection_type LIKE 'gatekeeper_%'`) bevor `drop_column` laeuft.

## Verification Passed

```
python -c "assert 'op.bulk_insert' in src; assert cnt >= 2 per objection_type"
# => Seed-Insert OK: >=2 Varianten pro Button

python -m py_compile alembic/versions/0003_add_phrases_mode.py
# => exit 0
```

## Deviations from Plan

### Scope-Erweiterung (kein Problem)

**Button 1 und 2 jeweils 3 Varianten statt Minimum 2**

Die Seed-Datei `tests/fixtures/gatekeeper_phrases_seed.md` enthielt 3 genehmigte Varianten fuer Button 1 (Verbuendeten-Bitte) und Button 2 (Insider-Antwort). Alle Andre-Gate-genehmigten Phrasen wurden 1:1 uebernommen — 10 Rows statt Minimum 8. D-05 verlangt >=2 pro Button, tatsaechlich: 3+3+2+2 = 10. Kein Deviance, nur bessere Abdeckung.

**Formulierung Button 4 Variante 1**

Die Seed-Datei enthielt `{vorname} ... (Pause) ... ich brauche genau zwei Minuten.` statt des Plan-Entwurfs `{vorname} ... ich brauche genau zwei Minuten.` — Plan-Datei hatte bereits die richtige genehmigt Fassung mit `(Pause)`, der Plan-Entwurf-Text in der PLAN.md war veraltet. Andre-Gate-Markdown ist massgeblich.

## Known Stubs

Keine. Die Phrasen sind echte Inhalte — keine Platzhalter.

## Threat Surface Scan

Keine neuen Trust-Boundaries. Die beiden in der PLAN.md dokumentierten Threats (T-08.23.2.C-22 und T-08.23.2.C-22b) wurden bewertet und akzeptiert (Admin-MVP + Alembic-Versioning verhindert Re-Run).

## Self-Check: PASSED

| Check | Status |
|-------|--------|
| alembic/versions/0003_add_phrases_mode.py exists | FOUND |
| Commit 4fb6267 exists | FOUND |
