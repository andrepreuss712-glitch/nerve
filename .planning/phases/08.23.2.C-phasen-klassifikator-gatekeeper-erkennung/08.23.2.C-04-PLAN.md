---
phase: 08.23.2.C
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - services/anonymization.py
  - app.py
autonomous: true
requirements: [Req-1]
tags: [nerve, gliner, ner, anonymization, voting]
must_haves:
  truths:
    - "GLiNER-Modell wird beim App-Start vorgewärmt (analog spaCy)"
    - "anonymize() nutzt GLiNER ODER spaCy als Namen-NER (Union-Voting per D-01)"
    - "extract_entities(text, cache=None) ist öffentliche Funktion in services/anonymization.py"
    - "Latenz-Gate tests/test_anonymization_perf.py bleibt grün (<200ms P95)"
    - "is_pipeline_healthy bleibt False bei GLiNER-Load-Fehler (Fallback-A)"
  artifacts:
    - path: "services/anonymization.py"
      provides: "GLiNER als zweite NER-Stufe + extract_entities()-Export"
      contains: "_get_gliner"
    - path: "app.py"
      provides: "GLiNER Warmup beim App-Start (analog spaCy)"
      contains: "_get_gliner"
  key_links:
    - from: "app.py Pre-Warm-Block"
      to: "services/anonymization._get_gliner()"
      via: "Import + Aufruf direkt nach spaCy-Warmup"
      pattern: "_get_gliner"
    - from: "services/gatekeeper.py classify_contact() (Plan 06)"
      to: "services/anonymization.extract_entities()"
      via: "from services.anonymization import extract_entities"
      pattern: "extract_entities"
---

<objective>
GLiNER `urchade/gliner_multi-v2.1` als zweite NER-Stufe in services/anonymization.py einbauen. Union-Voting per D-01 für DSGVO-Anonymisierungs-Pfad (Recall maximieren, einer der beiden NER-Treffer reicht). Neue öffentliche Funktion `extract_entities(text, cache=None)` exportieren, die rohe Entities mit `source`-Tag (spaCy oder gliner) zurückgibt — für Konsens-Voting in services/gatekeeper.py (Plan 06).

Purpose: Wave 3 (services/gatekeeper.py) konsumiert `extract_entities()`. Wave 5 (Re-ID-Test) braucht aktive GLiNER-Pipeline. Phase-B-Latenz-Test (200ms P95) MUSS grün bleiben — Wave 0 Smoke (Plan 01 Task 2) lieferte das experimentelle Daten-Profil.

Output: anonymization.py hat _get_gliner() (thread-safe lazy load), erweitertes _apply_ner() mit Union-Voting + nativen Offsets / re.finditer-Fallback, exportiertes extract_entities(). app.py warmt GLiNER beim Start vor. Bei P95 > 100ms: _apply_ner_parallel() via ThreadPoolExecutor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-01-SUMMARY.md
@services/anonymization.py
@app.py
</context>

<interfaces>
<!-- Aus RESEARCH.md Pattern 1, 2, 3 — VERIFIED. Aus Plan 01 Smoke-Ergebnis (08.23.2.C-01-SUMMARY.md) -->

GLiNER API (verified, github.com/urchade/GLiNER + huggingface):
```python
from gliner import GLiNER
model = GLiNER.from_pretrained("urchade/gliner_multi-v2.1")
entities = model.predict_entities(text, ['person', 'organisation', 'location'], threshold=0.5)
# Return: [{'text': 'Müller', 'label': 'person', 'start': 12, 'end': 18, 'score': 0.91}, ...]
# HINWEIS: GLiNER liefert laut Standard-Lib 'start' und 'end' als Char-Offsets.
# Plan-Author MUSS 08.23.2.C-01-SUMMARY.md lesen um zu prüfen ob der installierte
# gliner-Build diese Keys tatsächlich liefert (repr(entities) aus Smoke-Test).
# Falls ja -> native Offsets verwenden (bevorzugt).
# Falls nein (ältere Wrapper-Version) -> re.finditer() Fallback.
```

spaCy aktuell in anonymization.py (Phase B):
```python
_nlp = None
_nlp_lock = threading.Lock()
def _get_nlp() -> Language | None:
    # Thread-safe lazy load, setzt is_pipeline_healthy=False bei Fehler
```

Erwartetes neues extract_entities()-Return-Format:
```python
List[dict]: [
  {'text': str, 'type': 'PERSON'|'ORG'|'LOC', 'source': 'spacy'|'gliner'},
  ...
]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: _get_gliner() Lazy-Loader + GLiNER in _apply_ner() Union-Voting mit nativen Offsets / re.finditer-Fallback</name>
  <files>services/anonymization.py</files>
  <read_first>
    - services/anonymization.py vollständig (Z.1-400+ — _get_nlp, _apply_ner, _anonymize_input, AnrufAnonymisierer-Klasse)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pattern 1+2+3, Common Pitfalls 1-2-5)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-01-SUMMARY.md (GLiNER-Smoke-Ergebnis — KRITISCH: `repr(entities)` Ausgabe prüfen um festzustellen ob 'start'/'end'-Keys vorhanden sind)
  </read_first>
  <action>
    Schritt 0 — SMOKE-OUTPUT LESEN: Öffne 08.23.2.C-01-SUMMARY.md und suche nach der `repr(entities)` Ausgabe des Smoke-Tests. Prüfe ob die Dicts 'start' und 'end' Keys enthalten. Diese Entscheidung bestimmt den Offset-Pfad in _apply_ner().

    Schritt 1 — _get_gliner() einfügen (direkt nach _get_nlp() in services/anonymization.py):

    ```python
    _gliner_model = None
    _gliner_lock = threading.Lock()

    def _get_gliner():
        """Thread-safe lazy load von GLiNER gliner_multi-v2.1.
        Analog _get_nlp(). Bei Load-Fehler: is_pipeline_healthy=False, return None.
        """
        global _gliner_model, is_pipeline_healthy
        if _gliner_model is not None:
            return _gliner_model
        with _gliner_lock:
            if _gliner_model is not None:
                return _gliner_model
            try:
                from gliner import GLiNER
                _gliner_model = GLiNER.from_pretrained('urchade/gliner_multi-v2.1')
                print('[ANON] GLiNER gliner_multi-v2.1 geladen')
            except Exception as e:
                print(f'[ANON] KRITISCH: GLiNER nicht ladbar: {type(e).__name__}: {e}')
                is_pipeline_healthy = False
                _gliner_model = None
        return _gliner_model
    ```

    Schritt 2 — _apply_ner() erweitern (D-01 Union-Voting fuer Anonymisierungs-Pfad).

    WICHTIG — Offset-Strategie (Review Finding 1):
    - Standard-GLiNER liefert 'start' und 'end' als Char-Offsets im Ergebnis-Dict.
    - Falls Smoke-Output (01-SUMMARY.md) bestätigt: keys vorhanden → `ent['start']` / `ent['end']` direkt verwenden (genaueste Methode).
    - Falls Smoke-Output zeigt KEINE 'start'/'end' Keys (älterer Wrapper): Fallback via `re.finditer(re.escape(ent_text), text)` um ALLE Vorkommen zu finden (robuster als text.find()-while-Loop).
    - KEIN reiner `text.find()`-while-Loop — dieser wird durch `re.finditer` als Fallback ersetzt.

    Bestehende _apply_ner() nimmt `(text, cache, nlp)`. Erweitern:
    - Signatur unveraendert lassen (kein Breaking-Change), aber intern _get_gliner() aufrufen.
    - Beide NER-Quellen auswerten, Treffer in `all_spans` aggregieren.
    - Dedupliziert: wenn spaCy und GLiNER denselben Span finden → einmal in all_spans.
    - Token-Zuweisung wie heute via AnrufAnonymisierer-Cache.

    Code-Skelett:

    ```python
    import re  # Sicherstellen dass re importiert ist (am Modul-Anfang wenn noch nicht vorhanden)

    def _apply_ner(text: str, cache, nlp) -> Tuple[str, str]:
        """Union-Voting: spaCy OR GLiNER fuer Personen-/Organisations-/Ortsnamen.
        Recall maximieren — DSGVO-Pflicht (D-01 Anonymisierungs-Pipeline).
        Review Finding 1: native GLiNER-Offsets bevorzugt, re.finditer() als Fallback.
        """
        tier = 'A'
        all_spans = []  # List[Tuple[start, end, type, original_text]]

        # spaCy-Treffer (Char-Offsets verfuegbar via ent.start_char / ent.end_char)
        if nlp:
            doc = nlp(text)
            for ent in doc.ents:
                if ent.label_ in ('PER', 'LOC', 'ORG'):
                    token_type = 'PERSON' if ent.label_ == 'PER' else ent.label_
                    all_spans.append((ent.start_char, ent.end_char, token_type, ent.text))

        # GLiNER-Treffer — native Offsets preferred, re.finditer() als Fallback
        gliner = _get_gliner()
        if gliner is not None:
            try:
                results = gliner.predict_entities(
                    text,
                    ['person', 'organisation', 'location'],
                    threshold=0.5,
                )
                for ent in results:
                    ent_text = ent.get('text', '')
                    if not ent_text:
                        continue
                    ent_label = ent.get('label', '').lower()
                    if ent_label == 'person':
                        token_type = 'PERSON'
                    elif ent_label == 'organisation':
                        token_type = 'ORG'
                    elif ent_label == 'location':
                        token_type = 'LOC'
                    else:
                        continue

                    # Native Offsets wenn GLiNER sie liefert (Standard-Lib gliner_multi-v2.1)
                    if 'start' in ent and 'end' in ent:
                        all_spans.append((ent['start'], ent['end'], token_type, ent_text))
                    else:
                        # Fallback: re.finditer findet ALLE Vorkommen (robuster als text.find-Loop)
                        for m in re.finditer(re.escape(ent_text), text):
                            all_spans.append((m.start(), m.end(), token_type, ent_text))
            except Exception as e:
                print(f'[ANON] GLiNER predict_entities Fehler: {type(e).__name__}')
                # Kein Fail-Hard — spaCy-only-Treffer reichen fuer Fallback

        # Dedup: gleiche (start, end, type) nur einmal
        unique_spans = list(set((s, e, t, txt) for (s, e, t, txt) in all_spans))
        # Sort by start ascending fuer deterministisches Replacement
        unique_spans.sort(key=lambda x: x[0])

        # Replacement (reverse so spaeter spans nicht offset-shiften)
        result = text
        for start, end, token_type, original in reversed(unique_spans):
            token = cache.get_or_assign_token(token_type, original) if cache else f'[{token_type}_X]'
            result = result[:start] + token + result[end:]

        return result, tier
    ```

    Schritt 3 — `extract_entities()` als neue oeffentliche Funktion (D-02 — fuer gatekeeper.py-Konsens-Voting):

    ```python
    def extract_entities(text: str, cache=None) -> list:
        """Gibt rohe NER-Treffer mit Source-Tag zurueck.

        Fuer Konsens-Voting in services/gatekeeper.classify_contact() (D-01 Gatekeeper-Pfad).

        Args:
            text: Roher Transkript-Text.
            cache: Optional, nicht verwendet — Signatur konsistent mit anonymize().

        Returns:
            List[dict]: [{'text': str, 'type': 'PERSON'|'ORG'|'LOC', 'source': 'spacy'|'gliner'}, ...]
            Leere Liste wenn NER nicht verfuegbar.
        """
        entities = []
        nlp = _get_nlp()
        if nlp is not None:
            try:
                doc = nlp(text)
                for ent in doc.ents:
                    if ent.label_ in ('PER', 'LOC', 'ORG'):
                        entities.append({
                            'text': ent.text,
                            'type': 'PERSON' if ent.label_ == 'PER' else ent.label_,
                            'source': 'spacy',
                        })
            except Exception as e:
                print(f'[ANON] extract_entities spaCy Fehler: {type(e).__name__}')

        gliner = _get_gliner()
        if gliner is not None:
            try:
                results = gliner.predict_entities(
                    text,
                    ['person', 'organisation', 'location'],
                    threshold=0.5,
                )
                for ent in results:
                    ent_label = ent.get('label', '').lower()
                    if ent_label == 'person':
                        token_type = 'PERSON'
                    elif ent_label == 'organisation':
                        token_type = 'ORG'
                    elif ent_label == 'location':
                        token_type = 'LOC'
                    else:
                        continue
                    entities.append({
                        'text': ent.get('text', ''),
                        'type': token_type,
                        'source': 'gliner',
                    })
            except Exception as e:
                print(f'[ANON] extract_entities GLiNER Fehler: {type(e).__name__}')

        return entities
    ```

    Schritt 4 — _apply_ner_parallel() als optionale Parallel-Dispatch-Funktion einfügen (Review Finding 2 — Latenz-Mitigation):

    ```python
    import concurrent.futures

    def _apply_ner_parallel(text: str, cache, nlp) -> Tuple[str, str]:
        """Concurrent spaCy + GLiNER dispatch fuer Latenz-Reduktion (Review Finding 2).

        Wird verwendet wenn Server-GLiNER-P95 > 100ms bei sequentiellem _apply_ner().
        GIL-Release erlaubt echte Parallelitaet bei I/O-bound-Inferenz.
        Auswahl sequentiell vs. parallel: Plan-Author prueft 08.23.2.C-01-SUMMARY.md.
        """

        def run_spacy():
            if nlp is None:
                return []
            doc = nlp(text)
            return [
                (e.start_char, e.end_char, 'PERSON' if e.label_ == 'PER' else e.label_, e.text)
                for e in doc.ents if e.label_ in ('PER', 'LOC', 'ORG')
            ]

        def run_gliner():
            gliner = _get_gliner()
            if gliner is None:
                return []
            spans = []
            try:
                results = gliner.predict_entities(
                    text,
                    ['person', 'organisation', 'location'],
                    threshold=0.5,
                )
                for ent in results:
                    ent_text = ent.get('text', '')
                    if not ent_text:
                        continue
                    ent_label = ent.get('label', '').lower()
                    if ent_label == 'person':
                        token_type = 'PERSON'
                    elif ent_label == 'organisation':
                        token_type = 'ORG'
                    elif ent_label == 'location':
                        token_type = 'LOC'
                    else:
                        continue
                    if 'start' in ent and 'end' in ent:
                        spans.append((ent['start'], ent['end'], token_type, ent_text))
                    else:
                        for m in re.finditer(re.escape(ent_text), text):
                            spans.append((m.start(), m.end(), token_type, ent_text))
            except Exception as e:
                print(f'[ANON] parallel GLiNER Fehler: {type(e).__name__}')
            return spans

        with concurrent.futures.ThreadPoolExecutor(max_workers=2) as pool:
            f_spacy = pool.submit(run_spacy)
            f_gliner = pool.submit(run_gliner)
            spacy_spans = f_spacy.result()
            gliner_spans = f_gliner.result()

        all_spans = list(set(spacy_spans + gliner_spans))
        all_spans.sort(key=lambda x: x[0])

        result = text
        for start, end, token_type, original in reversed(all_spans):
            token = cache.get_or_assign_token(token_type, original) if cache else f'[{token_type}_X]'
            result = result[:start] + token + result[end:]

        return result, 'A'
    ```

    Wichtige Anti-Patterns vermeiden (RESEARCH.md):
    - KEIN `from gliner import GLiNER` per-Request — Import bleibt in _get_gliner() lokal nach Modul-Level-Pattern aus Phase B (Cross-AI-Finding 1).
    - KEINE hardcoded Confidence-Schwelle ausser 0.5 (per RESEARCH.md Pattern 3).
    - Thread-Safety: _gliner_model wird einmal geladen, dann nur read-only — kein Lock fuer predict_entities() (siehe Pitfall 5 — falls Race-Issues auftreten in Tests, dann Inference unter _gliner_lock serialisieren — initial OHNE Lock starten).
    - KEIN reiner text.find()-while-Loop fuer GLiNER-Span-Positionen — re.finditer ist der Fallback (Review Finding 1).
  </action>
  <verify>
    <automated>python -c "from services.anonymization import extract_entities, anonymize, _get_gliner; assert callable(extract_entities); assert callable(_get_gliner); import inspect; sig = inspect.signature(extract_entities); assert 'text' in sig.parameters; assert 'cache' in sig.parameters; print('extract_entities OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from services.anonymization import extract_entities, _get_gliner` exit 0
    - `inspect.signature(extract_entities).parameters` enthält 'text' und 'cache'
    - services/anonymization.py enthält Zeile `def _get_gliner():`
    - services/anonymization.py enthält String `'urchade/gliner_multi-v2.1'`
    - services/anonymization.py enthält String `threshold=0.5`
    - services/anonymization.py enthält `re.finditer` als Fallback fuer GLiNER-Offset-Resolution (Review Finding 1)
    - services/anonymization.py enthaelt KEIN `text.find(ent_text, start)` while-Loop — dieser wurde durch re.finditer oder native Offsets ersetzt (Review Finding 1)
    - services/anonymization.py enthält bedingten Offset-Block: `if 'start' in ent and 'end' in ent:` (native Offsets bevorzugt) plus `else:` + `re.finditer`-Fallback
    - services/anonymization.py enthält `def _apply_ner_parallel(` mit `concurrent.futures.ThreadPoolExecutor` (Review Finding 2)
    - is_pipeline_healthy wird auf False gesetzt bei GLiNER-Load-Exception
    - GLiNER-Import-Statement steht innerhalb _get_gliner() (NICHT auf Modul-Ebene — Cross-AI-Pattern aus Phase B)
    - Modul ohne installiertes GLiNER: `_get_gliner()` returnt `None`, kein Hard-Crash (try/except umschließt Import)
  </acceptance_criteria>
  <done>GLiNER ist in der Anonymisierungs-Pipeline integriert + extract_entities() exportiert + _apply_ner_parallel() fuer Latenz-Mitigation bereit.</done>
</task>

<task type="auto">
  <name>Task 2: GLiNER-Warmup in app.py einbauen</name>
  <files>app.py</files>
  <read_first>
    - app.py (Pre-Warm-Block fuer Anonymisierung — Phase B Plan 01 Finding 5: `anonymize('Warmup', None)` in Z.2273)
    - services/anonymization.py _get_nlp() + _get_gliner() (gerade angelegt)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Architecture Patterns: App-Start-Loading)
  </read_first>
  <action>
    In app.py finde den bestehenden Pre-Warm-Block fuer Anonymisierung (`anonymize('Warmup', None)` aus Phase B Plan 01). Erweitere den Block:

    ```python
    # Pre-Warm Anonymisierungs-Pipeline (Phase B + C)
    try:
        from services.anonymization import anonymize, _get_gliner
        # spaCy-Warmup via anonymize() (Phase B)
        anonymize('Warmup', None)
        # GLiNER-Warmup explizit (Phase C — Modell ~800MB, App-Start besser als First-Request)
        _gliner = _get_gliner()
        if _gliner is not None:
            print('[INIT] GLiNER pre-warmed')
        else:
            print('[INIT] GLiNER nicht verfuegbar — Pipeline laeuft im Fallback-Modus')
    except Exception as e:
        print(f'[INIT] Anonymisierungs-Pre-Warm Fehler: {type(e).__name__}: {e}')
    ```

    Position: exakt dort wo Phase B Plan 01 den `anonymize('Warmup', None)`-Aufruf eingefuegt hat (Z.~2273). Ersetze den bestehenden Single-Aufruf mit erweitertem Block (oder fuege GLiNER-Warmup unmittelbar danach ein wenn der bestehende Aufruf nicht ueberschrieben werden soll).

    Warum App-Start statt First-Request:
    - GLiNER `gliner_multi-v2.1` ist ~800MB im Memory + ~2-5min Download bei Cold-Start.
    - First-Request waere fuer User unzumutbar.
    - is_pipeline_healthy=False bei Fehler — anonymize() faellt nach Phase-B-Pattern auf spaCy-only zurueck (D-08 Kat. A oder C).
  </action>
  <verify>
    <automated>grep -q "_get_gliner" app.py && grep -q "GLiNER pre-warmed\|GLiNER nicht verfuegbar" app.py && python -m py_compile app.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - app.py importiert `_get_gliner` aus services.anonymization
    - app.py ruft `_get_gliner()` waehrend Pre-Warm-Block auf (nach `anonymize('Warmup', None)`)
    - app.py druckt `[INIT] GLiNER pre-warmed` (oder `nicht verfuegbar`) abhaengig vom Ergebnis
    - `python -m py_compile app.py` exit 0
    - Bestehender `anonymize('Warmup', None)`-Aufruf bleibt im Code (Phase-B-Verhalten erhalten)
  </acceptance_criteria>
  <done>GLiNER wird beim App-Start vorgeladen; Fallback bei Fehler dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-B Performance-Test re-run als Regression-Gate + Parallel-Dispatch-Entscheidung</name>
  <files>(kein File-Write — Test-Run zur Verifikation)</files>
  <read_first>
    - tests/test_anonymization_perf.py (Phase B Plan 10 — P95-Latenz-Test auf 1000-Zeichen-Text, 100 Runs)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-1 Acceptance: <200ms P95 bleibt)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pitfall 1 — Latenz-Risiko)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-01-SUMMARY.md (GLiNER-P95-Wert aus Smoke-Test)
  </read_first>
  <action>
    Aufruf: `pytest tests/test_anonymization_perf.py -x -v`

    Erwartetes Verhalten:
    - Test laeuft mit aktiver GLiNER-Pipeline (nach Task 1+2 Eingriffen).
    - Bestehender P95-Test soll grün bleiben.

    Mitigation-Reihenfolge wenn Server-GLiNER-P95 > 100ms (Review Finding 2):

    1. PRÜFEN: 08.23.2.C-01-SUMMARY.md nach GLiNER-P95-Solo-Latenz (Smoke-Test-Ergebnis).
       - Falls P95 <= 100ms: sequentielles _apply_ner() reicht, kein Parallel-Dispatch noetig.
       - Falls P95 > 100ms → Schritt 2.

    2. PRIMÄR — Parallel-Dispatch via ThreadPoolExecutor (Review Finding 2):
       In _apply_ner() den Aufruf auf _apply_ner_parallel() umstellen (bereits implementiert in Task 1).
       spaCy und GLiNER laufen concurrent — GIL-Release erlaubt echte Parallelitaet bei Inferenz.
       Test erneut ausfuehren.

    3. FALLBACK — nur wenn Parallel-Dispatch P95 immer noch > 200ms:
       GLiNER-Labels auf ['person'] only reduzieren (kein 'organisation', 'location').
       Das ist die kostenlose Latenz-Reduktion ohne Recall-Verlust fuer Namen (Pitfall 1).

    4. ESCALATION — falls auch das nicht reicht:
       Latenz-Wert + Mitigation-Status in 08.23.2.C-04-SUMMARY.md dokumentieren und an André eskalieren.
       Plan-Author gibt NICHT still auf — SUMMARY muss messbaren Wert + naechsten Schritt enthalten.

    Falls Test grün: Commit mit Message `feat(08.23.2.C): integrate GLiNER as 2nd NER stage (Req-1)`.

    Wichtig: GLiNER ist nicht zwingend lokal installiert (RESEARCH.md "Environment Availability"). Auf Dev-Maschine ohne GLiNER: Test prueft de-facto nur spaCy-Latenz (GLiNER-Pfad wird via _get_gliner() returnt None geblockt). Das ist akzeptabel — Server-Verifikation ist die Wahrheit.
  </action>
  <verify>
    <automated>pytest tests/test_anonymization_perf.py -x -v 2>&1 | tee /tmp/perf_run.log; grep -q "passed" /tmp/perf_run.log || (echo "PERF-GATE FAILED" && grep -i "p95\|latency\|fail" /tmp/perf_run.log)</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_anonymization_perf.py -x` exit 0 (grün)
    - Test-Output enthält "passed" für mindestens den P95-Test
    - Falls Server-GLiNER-P95 > 100ms (aus 08.23.2.C-01-SUMMARY.md): `_apply_ner_parallel()` mit ThreadPoolExecutor als primäre Mitigation implementiert und getestet (Review Finding 2)
    - Falls Parallel-Dispatch immer noch > 200ms: PERSON-only-Labels als sekundäre Mitigation dokumentiert
    - Falls fehlschlägt: SUMMARY.md dokumentiert Latenz-Wert + angewendete Mitigation + naechsten Schritt — Plan gilt als NICHT erfolgreich
  </acceptance_criteria>
  <done>Latenz-Gate aus Phase B bleibt grün mit aktiver GLiNER-Pipeline; Mitigation-Reihenfolge (ThreadPoolExecutor → Label-Reduktion → Eskalation) dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted text → GLiNER predict_entities() | Modell-Inferenz auf User-Input |
| GLiNER from_pretrained() → huggingface.co | Modell-Download zur App-Start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.C-10 | Denial of Service | predict_entities() blockiert > 200ms | mitigate | Latenz-Gate via Phase-B-Perf-Test; Primär-Mitigation: ThreadPoolExecutor-Parallel (Review Finding 2); Sekundär: PERSON-only Labels falls noetig |
| T-08.23.2.C-11 | Spoofing | Falsch-Positiv-NER fuehrt zu Falsch-Anonymisierung | accept | Union-Voting in anonymize() — False-Positives sind im DSGVO-Kontext akzeptabel (uebermaessige Anonymisierung); False-Negatives waeren DSGVO-Verstoss |
| T-08.23.2.C-12 | Information Disclosure | GLiNER laed Modell aus Huggingface (Erst-Run) | accept | Modell-Quelle ist offizielles urchade/-Repo; SHA-Pinning nicht moeglich (nicht von GLiNER unterstuetzt); deploy.sh-Pre-Cache mitigiert Cold-Start-Risk |
| T-08.23.2.C-13 | Information Disclosure | extract_entities() gibt rohe Entities zurueck | mitigate | Nur intern aufgerufen (gatekeeper.py); keine API-Exposition; rohe Names verlassen den Prozess nicht (werden zu target/gatekeeper/unknown reduziert) |
| T-08.23.2.C-14 | Tampering | GLiNER predict_entities() bekommt manipulierte Inputs | accept | Inputs sind transcript-Strings aus Deepgram (vertrauter Upstream); kein SQL-Pfad |
</threat_model>

<verification>
- extract_entities() importierbar mit korrekter Signatur
- _get_gliner() ist thread-safe (Lock-Pattern wie spaCy)
- app.py warmt GLiNER beim Start vor (oder dokumentiert Fallback)
- Phase-B-Performance-Test bleibt grün
- is_pipeline_healthy=False bei GLiNER-Load-Fehler (Phase-B-Fallback-Pattern aktiv)
- _apply_ner() nutzt native GLiNER-Offsets wenn vorhanden, re.finditer() als Fallback (KEIN text.find-while-Loop)
- _apply_ner_parallel() mit ThreadPoolExecutor als Latenz-Mitigation bereit
</verification>

<success_criteria>
- `from services.anonymization import extract_entities` exit 0
- `pytest tests/test_anonymization_perf.py -x` grün
- App-Start druckt `[INIT] GLiNER pre-warmed` (auf Server-Run)
- services/anonymization.py enthält `re.finditer` als GLiNER-Offset-Fallback
- services/anonymization.py enthält `_apply_ner_parallel` mit `ThreadPoolExecutor`
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-04-SUMMARY.md`
</output>
