---
phase: 08.23.2.C
plan: 05
type: execute
wave: 2
depends_on: [03]
files_modified:
  - services/claude_service.py
autonomous: true
requirements: [Req-2, Req-7]
tags: [nerve, phase-classifier, claude, ki-logik, trigger-phrases]
must_haves:
  truths:
    - "_PHASE_NAMES_COLD_CALL, _PHASE_NAMES_MEETING, _PHASE_NAMES_GATEKEEPER existieren als Modul-Konstanten"
    - "classify_phase(window, current_phase, elapsed_s, mode) wählt phase_names basierend auf mode"
    - "classify_phase('gatekeeper') gibt niemals Phasen-Nummer > 4 zurück"
    - "classify_phase('cold_call') / classify_phase('meeting') gibt niemals Phasen-Nummer > 6 zurück"
    - "PHASE_CLASSIFIER_PROMPT enthält modus-spezifische Phasen-Labels"
    - "TRIGGER_PHRASES Konstante (>=12 deutsche Phrasen) in services/ki_logik.py"
  artifacts:
    - path: "services/claude_service.py"
      provides: "Modus-spezifische Phasen-Klassifikation (Req-2)"
      contains: "_PHASE_NAMES_GATEKEEPER"
    - path: "services/ki_logik.py"
      provides: "TRIGGER_PHRASES Konstante (Req-7)"
      contains: "TRIGGER_PHRASES"
  key_links:
    - from: "services/claude_service.py classify_phase()"
      to: "config/phase_transitions.py MIN_PHASE_DURATIONS"
      via: "Import nicht direkt — Plan 06 verbindet im Analyse-Loop"
      pattern: "_PHASE_NAMES_"
    - from: "services/gatekeeper.py detect_trigger_phrases() (Plan 06)"
      to: "services/ki_logik.py TRIGGER_PHRASES"
      via: "from services.ki_logik import TRIGGER_PHRASES"
      pattern: "TRIGGER_PHRASES"
---

<objective>
Modus-blinden Phasen-Klassifikator umbauen: drei separate Phasen-Listen als Konstanten, classify_phase() wählt Liste basierend auf mode-Parameter, Prompt-Template wird modus-spezifisch konstruiert. Validation der Phase-Range pro Modus (Cold-Call/Meeting 1-6, Gatekeeper 1-4). Zusätzlich: TRIGGER_PHRASES-Konstante in ki_logik.py mit ≥12 deutschen Gatekeeper-Trigger-Phrasen + UWG-Hard-Block-Pattern (Req-7+Req-8 Data-Layer).

Purpose: Wave 3 (services/gatekeeper.py + live_session-Integration) konsumiert beide neuen Strukturen. Hysterese-Logik (Plan 06) braucht ein modus-bewusstes classify_phase() um zwischen Cold-Call/Meeting/Gatekeeper-Phase-Sets zu unterscheiden.

Output: claude_service.py mit 3 Phasen-Listen + Modus-Dispatch in classify_phase(), ki_logik.py mit TRIGGER_PHRASES + Hard-Block-Pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-CONTEXT.md
@.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md
@Nerve-Vault/03 Planung/gatekeeper detection.md
@services/claude_service.py
@services/ki_logik.py
</context>

<interfaces>
<!-- VERIFIED: services/claude_service.py Z.219-313 — aktuelle Signatur -->

Aktuell:
```python
# Z.219 (verified)
_PHASE_NAMES = {1: 'Opener', 2: 'Qualifizierung', 3: 'Bedarfsanalyse',
                4: 'Pitch', 5: 'Einwandbehandlung', 6: 'Abschluss'}

# Z.~253 (verified)
def classify_phase(transcript_window, current_phase, elapsed_s, mode):
    # nutzt _PHASE_NAMES unabhaengig von mode-Parameter
    # Returns: {'phase': int, 'confidence': float, 'grund': str} or None
```

Ziel (Phase C):
```python
_PHASE_NAMES_COLD_CALL = {1: 'opener', 2: 'permission', 3: 'reason',
                          4: 'pitch', 5: 'discovery', 6: 'closing'}
_PHASE_NAMES_MEETING = {1: 'intro', 2: 'agenda', 3: 'discovery',
                        4: 'pitch', 5: 'objection', 6: 'closing'}
_PHASE_NAMES_GATEKEEPER = {1: 'greeting', 2: 'identify', 3: 'bypass', 4: 'handoff'}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Modus-spezifische Phasen-Listen + classify_phase()-Dispatch</name>
  <files>services/claude_service.py</files>
  <read_first>
    - services/claude_service.py Z.219-330 (vollständig: _PHASE_NAMES, PHASE_CLASSIFIER_PROMPT, classify_phase, Aufruf-Site)
    - services/claude_service.py Z.~954 (RESEARCH.md "Integration Points": classify_phase()-Aufruf im Analyse-Loop — mode-Parameter wird bereits übergeben)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-RESEARCH.md (Pattern 4)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-2 Acceptance)
  </read_first>
  <action>
    Schritt 1 — Drei neue Konstanten (ersetzen das alte `_PHASE_NAMES` oder ergaenzen):

    ```python
    # Phase 08.23.2.C — Modus-spezifische Phasen-Listen (Req-2).
    # Phase-Namen sind englische Lowercase-Tokens (Code-Identifier, ASCII per CLAUDE.md).
    _PHASE_NAMES_COLD_CALL = {
        1: 'opener',
        2: 'permission',
        3: 'reason',
        4: 'pitch',
        5: 'discovery',
        6: 'closing',
    }
    _PHASE_NAMES_MEETING = {
        1: 'intro',
        2: 'agenda',
        3: 'discovery',
        4: 'pitch',
        5: 'objection',
        6: 'closing',
    }
    _PHASE_NAMES_GATEKEEPER = {
        1: 'greeting',
        2: 'identify',
        3: 'bypass',
        4: 'handoff',
    }

    # Hilfs-Mapping fuer Modus -> Phasen-Liste + max_phase.
    _PHASE_NAMES_BY_MODE = {
        'cold_call': (_PHASE_NAMES_COLD_CALL, 6),
        'meeting': (_PHASE_NAMES_MEETING, 6),
        'gatekeeper': (_PHASE_NAMES_GATEKEEPER, 4),
    }
    ```

    Das alte `_PHASE_NAMES` kann entweder
    (a) komplett ersetzt werden — alle Reads umstellen, oder
    (b) als Alias `_PHASE_NAMES = _PHASE_NAMES_COLD_CALL` (Lowercase!) erhalten bleiben fuer Backward-Compat.

    Plan-Author wählt Option (a) wenn kein anderer Code `_PHASE_NAMES` direkt liest, sonst (b). Konkret: grep auf `_PHASE_NAMES` durchführen — wenn nur classify_phase()-internal: Option (a). Wenn andere Module/Routes lesen: Option (b) + Migration-Hinweis im Commit.

    Schritt 2 — classify_phase() umbauen:

    ```python
    def classify_phase(transcript_window, current_phase, elapsed_s, mode):
        """Modus-bewusster Phasen-Klassifikator (Phase 08.23.2.C Req-2).

        Args:
            transcript_window: List[str] der letzten Berater-Saetze.
            current_phase: int — aktuell aktive Phasen-Nummer.
            elapsed_s: float — Sekunden seit Phase-Eintritt (heute nicht im Prompt
                       verwendet, aber Caller-Logik kann es nutzen).
            mode: 'cold_call' | 'meeting' | 'gatekeeper'.

        Returns:
            dict {'phase': int (1..max_phase), 'confidence': float (0..1), 'grund': str}
            oder None bei API-Fehler / ungueltigem Output.
        """
        phase_names, max_phase = _PHASE_NAMES_BY_MODE.get(
            mode, (_PHASE_NAMES_COLD_CALL, 6)  # Fallback: cold_call
        )

        # Prompt-Konstruktion modus-spezifisch (siehe Task 1 Pattern 4 aus RESEARCH.md)
        labels_str = ', '.join(f'{i}={name}' for i, name in phase_names.items())
        prompt = PHASE_CLASSIFIER_PROMPT.format(
            labels=labels_str,
            window='\n'.join(transcript_window),
            current_phase=current_phase,
            elapsed_s=int(elapsed_s),
            mode=mode,
        )

        # Haiku-Call (CLAUDE.md Constraint — Live MUSS Haiku sein)
        # Bestehende Anthropic-Aufruf-Logik beibehalten — nur prompt-String ist modus-spezifisch
        # ... bestehender Code ...

        # Validation: phase 1..max_phase, confidence 0..1
        try:
            phase = int(data.get('phase'))
            conf = float(data.get('confidence'))
        except (TypeError, ValueError):
            return None
        if 1 <= phase <= max_phase and 0.0 <= conf <= 1.0:
            return {'phase': phase, 'confidence': conf, 'grund': data.get('grund', '')}
        return None  # Out-of-range -> reject (Req-2 Acceptance)
    ```

    Schritt 3 — PHASE_CLASSIFIER_PROMPT erweitern:

    Bestehender Prompt nimmt `{labels}` Platzhalter? Falls ja, alles paletti — neue `labels_str` wird modus-spezifisch eingefuellt.
    Falls nein: Prompt-Template anpassen, sodass `{labels}` als Platzhalter im Format-String existiert. Zusaetzlich `{mode}` als Kontext einfuegen, damit das Modell weiss in welchem Anrufs-Modus es klassifiziert.

    Beispiel-Erweiterung (Plan-Author passt an realen aktuellen Prompt an):
    ```python
    PHASE_CLASSIFIER_PROMPT = """Du bist Phasen-Klassifikator fuer einen Sales-Anruf im Modus '{mode}'.
    Phasen: {labels}
    Aktuelle Phase: {current_phase}, seit {elapsed_s}s.
    Letzte Berater-Saetze:
    {window}

    Antwort als JSON: {{"phase": <1..N>, "confidence": <0.0..1.0>, "grund": "<kurz>"}}
    Wahle die wahrscheinlichste Phase basierend auf den Saetzen.
    """
    ```

    WICHTIG: aktuellen Prompt-Wortlaut beibehalten — nur Phasen-Liste dynamisch + mode-Hinweis ergaenzen. Keine drastische Umformulierung (regressive Risk auf Phase-Erkennung).
  </action>
  <verify>
    <automated>python -c "from services.claude_service import _PHASE_NAMES_COLD_CALL, _PHASE_NAMES_MEETING, _PHASE_NAMES_GATEKEEPER, _PHASE_NAMES_BY_MODE; assert _PHASE_NAMES_COLD_CALL == {1:'opener',2:'permission',3:'reason',4:'pitch',5:'discovery',6:'closing'}; assert _PHASE_NAMES_GATEKEEPER == {1:'greeting',2:'identify',3:'bypass',4:'handoff'}; assert _PHASE_NAMES_MEETING == {1:'intro',2:'agenda',3:'discovery',4:'pitch',5:'objection',6:'closing'}; assert _PHASE_NAMES_BY_MODE['gatekeeper'][1] == 4; print('Phase-Names OK')"</automated>
  </verify>
  <acceptance_criteria>
    - services/claude_service.py exportiert `_PHASE_NAMES_COLD_CALL`, `_PHASE_NAMES_MEETING`, `_PHASE_NAMES_GATEKEEPER`, `_PHASE_NAMES_BY_MODE`
    - `_PHASE_NAMES_COLD_CALL[1] == 'opener'`, `_PHASE_NAMES_COLD_CALL[6] == 'closing'`
    - `_PHASE_NAMES_GATEKEEPER[1] == 'greeting'`, `_PHASE_NAMES_GATEKEEPER[4] == 'handoff'`
    - `_PHASE_NAMES_MEETING[1] == 'intro'`, `_PHASE_NAMES_MEETING[6] == 'closing'`
    - `_PHASE_NAMES_BY_MODE['gatekeeper'][1]` (max_phase) == 4
    - `_PHASE_NAMES_BY_MODE['cold_call'][1]` == 6
    - classify_phase()-Signatur unveraendert: `(transcript_window, current_phase, elapsed_s, mode)`
    - classify_phase() enthaelt Logik `if 1 <= phase <= max_phase` oder Aequivalent (Range-Validation)
    - Test (manuell mit gemockter Anthropic-Response): `classify_phase(['Hallo'], 1, 10, 'gatekeeper')` retourniert dict mit `phase <= 4` oder None
    - `python -m py_compile services/claude_service.py` exit 0
    - grep `^_PHASE_NAMES\s*=` in claude_service.py: 0 Treffer ODER es ist klar dokumentierter Alias (`_PHASE_NAMES = _PHASE_NAMES_COLD_CALL`)
  </acceptance_criteria>
  <done>classify_phase() ist modus-bewusst, Phasen-Listen sind separat, Range-Validation aktiv.</done>
</task>

<task type="auto">
  <name>Task 2: TRIGGER_PHRASES + UWG_HARD_BLOCK_PATTERNS in ki_logik.py</name>
  <files>services/ki_logik.py</files>
  <read_first>
    - services/ki_logik.py (SCORE_FACTORS-Pattern als Vorlage)
    - Nerve-Vault/03 Planung/gatekeeper detection.md (Block B.6 — typische Sekretaer-Phrasen)
    - .planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-SPEC.md (Req-7 Acceptance: 12 Muster; Req-8 Hard-Block-Pattern)
  </read_first>
  <action>
    In services/ki_logik.py zwei neue Konstanten und ein Hilfs-Function einfuegen.

    Schritt 1 — TRIGGER_PHRASES Konstante:

    Mindestens 12 Phrasen, jeweils mit Kategorie + Muster + Hard-Block-Flag. Strukturierte Liste damit detect_trigger_phrases() darueber iterieren kann.

    ```python
    # Phase 08.23.2.C Req-7 — Gatekeeper-Trigger-Phrasen (≥12 Muster aus Block B.6).
    # Strings sind User-Text -> echte Umlaute in den Phrasen. Code-Identifier (Keys) ASCII.
    TRIGGER_PHRASES = [
        # Kategorie: Brush-Off / Vorzimmer-Filter (B.6)
        {'pattern': r'worum geht es', 'category': 'inquiry', 'hard_block': False},
        {'pattern': r'um was geht es', 'category': 'inquiry', 'hard_block': False},
        {'pattern': r'nicht im Haus', 'category': 'absence', 'hard_block': False},
        {'pattern': r'(ist|sind) (in einem|im) Termin', 'category': 'absence', 'hard_block': False},
        {'pattern': r'probieren Sie (es )?spaeter|versuchen Sie spaeter', 'category': 'callback', 'hard_block': False},
        {'pattern': r'rufen Sie spaeter (nochmal )?an', 'category': 'callback', 'hard_block': False},
        {'pattern': r'schicken Sie (uns )?(bitte )?(eine )?(E-?Mail|Mail)', 'category': 'redirect', 'hard_block': False},
        {'pattern': r'per Mail|per E-?Mail', 'category': 'redirect', 'hard_block': False},
        {'pattern': r'kein Interesse', 'category': 'rejection', 'hard_block': False},
        {'pattern': r'(einen Moment|einen Augenblick) bitte', 'category': 'hold', 'hard_block': False},
        {'pattern': r'mit wem spreche ich', 'category': 'identify_caller', 'hard_block': False},
        {'pattern': r'(wer|von welcher Firma) sind Sie', 'category': 'identify_caller', 'hard_block': False},
        # Kategorie: UWG §7 Hard-Block (Req-8) — separat aufgelistet damit detect_uwg_hard_block() schnell scannen kann
        {'pattern': r'bitte rufen Sie (mich )?nicht (mehr )?an', 'category': 'uwg_optout', 'hard_block': True},
        {'pattern': r'rufen Sie hier nicht (mehr )?an', 'category': 'uwg_optout', 'hard_block': True},
        {'pattern': r'wir wollen (das|hier) nicht', 'category': 'uwg_optout', 'hard_block': True},
        {'pattern': r'(loeschen|streichen) Sie (uns )?(aus der|von der) Liste', 'category': 'uwg_optout', 'hard_block': True},
    ]

    # Subset fuer schnelle Hard-Block-Detection (Req-8).
    UWG_HARD_BLOCK_PATTERNS = [p['pattern'] for p in TRIGGER_PHRASES if p['hard_block']]
    ```

    Notes:
    - Patterns sind Regex (re.IGNORECASE wird in gatekeeper.py beim Match-Aufruf gesetzt).
    - User-Text (die Phrasen selbst) MIT Umlauten — sie sind Inhalt, kein Identifier.
    - ASCII-Ersatz `ue` in Identifiern (`uwg_optout`, `identify_caller`) — Code-Identifier.
    - Min-Anzahl: 16 Eintraege (12 Brush-Off + 4 UWG) — uebererfuellt Req-7 (12) und liefert Req-8-Sub-Set.

    Schritt 2 — Hilfs-Function `_phrase_matches(line: str, pattern: str) -> bool`:

    ```python
    import re

    def _phrase_matches(line: str, pattern: str) -> bool:
        """Case-insensitive Regex-Match auf einzelne Zeile.
        Wrapper fuer wiederholte Nutzung in gatekeeper.py.
        """
        return bool(re.search(pattern, line or '', re.IGNORECASE))
    ```

    Schritt 3 — KEINE detect_trigger_phrases()-Logik hier (D-02: keine Gatekeeper-Logik in ki_logik.py). Das ist Plan 06 in gatekeeper.py.
  </action>
  <verify>
    <automated>python -c "from services.ki_logik import TRIGGER_PHRASES, UWG_HARD_BLOCK_PATTERNS, _phrase_matches; assert len(TRIGGER_PHRASES) >= 12; assert all('pattern' in p and 'category' in p and 'hard_block' in p for p in TRIGGER_PHRASES); assert len(UWG_HARD_BLOCK_PATTERNS) >= 1; assert _phrase_matches('Bitte rufen Sie nicht mehr an', UWG_HARD_BLOCK_PATTERNS[0]); print('TRIGGER_PHRASES OK count:', len(TRIGGER_PHRASES))"</automated>
  </verify>
  <acceptance_criteria>
    - services/ki_logik.py importierbar nach Edit: `python -c "import services.ki_logik"` exit 0
    - `from services.ki_logik import TRIGGER_PHRASES, UWG_HARD_BLOCK_PATTERNS, _phrase_matches` exit 0
    - `len(TRIGGER_PHRASES) >= 12` (Req-7 Mindest-Anzahl)
    - Jeder Eintrag hat Keys: 'pattern', 'category', 'hard_block'
    - Mindestens 1 Eintrag mit `hard_block=True` (Req-8)
    - `_phrase_matches('Bitte rufen Sie nicht mehr an', ...)` matched mindestens ein Hard-Block-Pattern
    - `_phrase_matches('Worum geht es', ...)` matched ein Inquiry-Pattern
    - Keine hardcoded `inspect.getsource()` oder Source-Presence-Reads (CLAUDE.md Verbot)
  </acceptance_criteria>
  <done>TRIGGER_PHRASES und UWG_HARD_BLOCK_PATTERNS sind als pure Datenstruktur in ki_logik.py exportiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classify_phase() → Anthropic API | Untrusted transcript_window text gesendet an Haiku |
| Trigger-Phrasen-Match → call_events.payload | Regex-Match-Ergebnis wird geloggt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.23.2.C-15 | Spoofing | classify_phase() gibt out-of-range Phase zurueck | mitigate | Range-Validation `1 <= phase <= max_phase` per Modus — Out-of-Range returnt None |
| T-08.23.2.C-16 | Tampering | Regex-Pattern als ReDoS-Risk | accept | Patterns sind kurz, ohne katastrophale Backtracking-Struktur; Inputs sind Transkript-Zeilen mit beschraenkter Laenge |
| T-08.23.2.C-17 | Information Disclosure | PHASE_CLASSIFIER_PROMPT enthaelt Transcript-Window an Haiku | accept | Transcript ist bereits anonymisiert per Phase-B-Pipeline; Modus-Hinweis ist nicht-sensitiv |
</threat_model>

<verification>
- Drei Phasen-Listen exportiert + Range-Validation aktiv
- classify_phase('gatekeeper') retourniert max phase 4
- TRIGGER_PHRASES hat ≥12 Eintraege, UWG_HARD_BLOCK_PATTERNS hat ≥1
</verification>

<success_criteria>
- `python -c "from services.claude_service import _PHASE_NAMES_BY_MODE; assert _PHASE_NAMES_BY_MODE['gatekeeper'][1] == 4"` exit 0
- `python -c "from services.ki_logik import TRIGGER_PHRASES; assert len(TRIGGER_PHRASES) >= 12"` exit 0
- Bestehende classify_phase()-Aufruf-Sites (claude_service.py Z.~954) brechen nicht
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.C-phasen-klassifikator-gatekeeper-erkennung/08.23.2.C-05-SUMMARY.md`
</output>
