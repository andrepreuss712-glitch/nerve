---
phase: 08.23.2.C
plan: "01"
subsystem: gliner-dependency-corpus-gate
status: complete
tags: [nerve, gliner, ner, anonymization, gatekeeper, corpus-gate, dependency]
dependency_graph:
  requires: []
  provides:
    - gliner-package-pinned-in-requirements
    - deploy-gliner-model-warmup
    - corpus-gate-script
    - gliner-smoke-script
    - corpus-schemas
    - gatekeeper-phrases-seed-approved
  affects:
    - deploy.sh (GLiNER pre-cache)
    - requirements.txt (gliner>=0.2.24)
    - Wave 5 Tests (blocked by corpus gate)
    - Plan 02 Migration (bestätigte Phrase-Seed)
tech_stack:
  added:
    - gliner>=0.2.24 (neue Dependency, wird bei nächstem Deploy auf CX32 installiert)
  patterns:
    - Non-fataler Deploy-Hook analog Phase B (de_core_news_lg-Pattern)
    - Pre-Execute-Gate-Skript mit Exit-Code 1 als Blocking-Mechanismus
key_files:
  created:
    - requirements.txt (gliner>=0.2.24 ergänzt)
    - deploy.sh (GLiNER-Pre-Cache-Block)
    - scripts/gliner_smoke.py
    - scripts/verify_corpus_gate.py
    - tests/fixtures/phase_classifier_corpus.schema.json
    - tests/fixtures/gatekeeper_classifier_corpus.schema.json
    - tests/fixtures/gatekeeper_phrases_seed.md
  modified: []
decisions:
  - "GLiNER als Range-Pin >=0.2.24 (nicht ==) — Patch-Updates akzeptabel per RESEARCH.md Standard Stack"
  - "Deploy-Hook nicht-fatal via || echo — App startet auch ohne Pre-Cache (T-08.23.2.C-02 mitigate)"
  - "verify_corpus_gate.py ohne jsonschema-Package — manuelle Validation ausreichend, keine neue Dependency"
  - "gatekeeper_phrases_seed.md: 3+3+2+2 Varianten (Buttons 1+2: je 3, Buttons 3+4: je 2) aus Vault B.6/B.7/Bonus-Block extrahiert — André-editiert und bestätigt"
metrics:
  duration_minutes: 15
  completed_date: "2026-05-15"
  tasks_completed: 4
  tasks_total: 4
  files_created: 7
  files_modified: 2
---

# Phase 08.23.2.C Plan 01: GLiNER Foundation + Corpus-Gate + Phrase-Seed Summary

**Status: COMPLETE**

**One-liner:** GLiNER als Dependency gepinnt, Deploy-Warmup + Pre-Execute-Gate + Smoke-Skript + Corpus-Schemas + Mr.-Miyagi-Phrase-Entwurf aus Vault-Recherche erstellt — Wave-1-Foundation für alle nachfolgenden Phase-08.23.2.C-Waves bereit.

---

## Abgeschlossene Tasks

| Task | Name | Commit | Dateien |
|------|------|--------|---------|
| 1 | GLiNER-Dependency + Deploy-Modell-Warmup | 93a5921 | requirements.txt, deploy.sh |
| 2 | GLiNER-Smoke + Korpus-Gate + Korpus-Schemas | 979c814 | scripts/gliner_smoke.py, scripts/verify_corpus_gate.py, tests/fixtures/*.schema.json |
| 3 | Mr.-Miyagi-Phrase-Entwurf | 865853a | tests/fixtures/gatekeeper_phrases_seed.md |

---

## Was gebaut wurde

### Task 1: GLiNER-Dependency + Deploy-Warmup

`requirements.txt`: `gliner>=0.2.24` als Range-Pin ergänzt (alphabetisch einsortiert). Kein hartes ==-Pin — Patch-Updates akzeptabel.

`deploy.sh`: Nach dem `python -m spacy download de_core_news_lg`-Block (Phase B, Z.82) ein neuer nicht-fataler Block:
```bash
python -c "from gliner import GLiNER; GLiNER.from_pretrained('urchade/gliner_multi-v2.1')" || \
    echo "[deploy] GLiNER-Pre-Download fehlgeschlagen — ..."
```
Nicht-fatal via `||` — App startet auch wenn Netzwerk beim Deploy nicht verfügbar ist (T-08.23.2.C-02).

### Task 2: Diagnose- und Gate-Skripte

**`scripts/gliner_smoke.py`**: Standalone-Diagnoseskript (Exit-Code immer 0). Lädt `gliner_multi-v2.1`, misst Ladezeit, führt 100 Inferenzen durch, gibt Min/Avg/P95-Latenz aus und dumpt das Return-Format des ersten Runs (klärt Open Question 1 — hat `predict_entities()` `start`/`end`-Felder?). Prüft ob P95 > 200ms und warnt dann.

**`scripts/verify_corpus_gate.py`**: Pre-Execute-Gate (Exit-Code 0=OK, 1=blockiert). Prüft Existenz, JSON-Validität, Schema-Konformität und Mindest-Anzahl beider Korpora. Aktuell Exit-Code 1 (erwartet — Korpora fehlen noch). Ausgabe mit `[CORPUS-GATE] OK:` und `[CORPUS-GATE] FEHLER:` Präfixen.

**`tests/fixtures/phase_classifier_corpus.schema.json`**: JSON-Schema-Draft mit `minItems=20`, required-Felder `transcript_window` (List[str]), `mode` (enum cold_call/meeting/gatekeeper), `expected_phase` (int 1-6).

**`tests/fixtures/gatekeeper_classifier_corpus.schema.json`**: JSON-Schema-Draft mit `minItems=10`, required-Felder `transcript_window` (List[str]), `briefing_ceo_name` (str), `expected_category` (enum target/gatekeeper/unknown).

### Task 3: Mr.-Miyagi-Phrase-Entwurf

`tests/fixtures/gatekeeper_phrases_seed.md` mit 4 Buttons, extrahiert aus Vault B.6, B.7, Bonus-Block:
- **Button 1 Verbündeten-Bitte** (Heinrich, Knauer, Voss): 3 Varianten mit {vorname}/{branche}/{detail}
- **Button 2 Insider-Antwort** (Taxis, Klein): 3 Varianten mit {nachname}/{detail}
- **Button 3 Voss-Label** (Voss Labeling): 2 Varianten
- **Button 4 Vornamen-Pause** (Limbeck): 2 Varianten mit {vorname}/{detail}

Alle Template-Variablen (`{branche}`, `{detail}`, `{vorname}`, `{nachname}`) im Dokument vorhanden. Source-Attribution pro Button. TODO-Marker für offene Prüfpunkte.

---

## Task 4 — Andre-Gate: Abgeschlossen

**Resume-Signal erhalten:** "phrase-seed edits done" (André hat die Datei direkt editiert)

**Acceptance-Criteria-Ergebnis:**
- Datei existiert und nicht leer: OK
- Genau 4 Buttons: OK
- Varianten pro Button (3/3/2/2): OK (alle ≥2)
- Template-Variablen {branche}/{vorname}/{nachname}/{detail}: alle OK
- Source-Attributionen (4x `*Source:`): OK

**Commit:** 2f07f2a — `docs(08.23.2.C-01): user-approved phrase seed edits (Andre-Gate)`

**Offen (asynchron, kein Blocker für Plan 02):**
- Korpora erstellen: `tests/fixtures/phase_classifier_corpus.json` (≥20) + `tests/fixtures/gatekeeper_classifier_corpus.json` (≥10) — `verify_corpus_gate.py` blockiert Wave 5 automatisch bis vorhanden.
- Optional: `python scripts/gliner_smoke.py` auf Server — klärt Open Question 1 (Return-Format + P95-Latenz).

---

## Deviations from Plan

Keine — Plan exakt wie geschrieben ausgeführt.

---

## Threat Flags

Keine neuen Security-relevanten Oberflächen über den Plan-Threat-Register hinaus eingeführt.

---

## Known Stubs

Keine produktions-relevanten Stubs. Die Phrase-Seed-Datei ist bewusst ein Entwurf-Dokument (Andre-Gate vor Execute).

---

## Self-Check: PASSED

```
FOUND: requirements.txt (gliner>=0.2.24)
FOUND: deploy.sh (urchade/gliner_multi-v2.1 + from gliner import GLiNER)
FOUND: scripts/gliner_smoke.py
FOUND: scripts/verify_corpus_gate.py
FOUND: tests/fixtures/phase_classifier_corpus.schema.json
FOUND: tests/fixtures/gatekeeper_classifier_corpus.schema.json
FOUND: tests/fixtures/gatekeeper_phrases_seed.md

Commits verified:
  93a5921 feat(08.23.2.C-01): GLiNER-Dependency + Deploy-Modell-Warmup
  979c814 feat(08.23.2.C-01): Korpus-Gate + GLiNER-Smoke + Schema-Dateien
  865853a feat(08.23.2.C-01): Mr.-Miyagi-Phrase-Entwurf fuer Andre-Gate
  2f07f2a docs(08.23.2.C-01): user-approved phrase seed edits (Andre-Gate)

verify_corpus_gate.py Exit-Code 1 (erwartet): CONFIRMED
gliner_smoke.py Syntax: OK
JSON-Schemas valide: OK
4 Buttons in gatekeeper_phrases_seed.md: CONFIRMED (3+3+2+2 Varianten, alle Acceptance-Criteria grün)
Andre-Gate Task 4: ABGESCHLOSSEN (Resume-Signal "phrase-seed edits done" erhalten)
```
