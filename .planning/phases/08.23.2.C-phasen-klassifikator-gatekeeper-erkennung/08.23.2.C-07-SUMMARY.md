---
phase: 08.23.2.C
plan: 07
status: checkpoint-paused
subsystem: pip-frontend, gatekeeper-api, socket-handlers
tags: [nerve, pip, ui, gatekeeper, keyboard, uwg-banner, css-tokens]
requirements: [Req-6, Req-8, Req-9]
dependency_graph:
  requires: [08.23.2.C-06, 08.23.2.C-06b]
  provides: [gatekeeper-pip-ui, uwg-banner, phrases-api, manual-mode-toggle]
  affects: [static/pip-launcher.js, static/nerve.css, templates/base.html, routes/app_routes.py, services/deepgram_service.py]
tech_stack:
  added: []
  patterns: [flask-route-jsonify, socketio-handler, vanilla-js-socket-subscription, css-custom-properties]
key_files:
  created: []
  modified:
    - routes/app_routes.py
    - services/deepgram_service.py
    - static/nerve.css
    - templates/base.html
    - static/pip-launcher.js
decisions:
  - "Kein --teal-accent Token vorhanden — var(--btn-primary-bg-from) als Teal-Farbe genutzt (semantisch identisch mit #00D4AA)"
  - "manual_mode_toggle Handler in register_audio_handlers() in deepgram_service.py platziert (dort leben alle sio.on-Handler)"
  - "_renderStandardEwbButtons() als Wrapper um bestehende _renderEwbButtons() implementiert (DRY)"
  - "pip-uwg-banner DOM vor pip-section-live (nicht innerhalb) platziert — Banner deckt gesamte PiP-Breite ab"
  - "_wireUwgBannerClose() sofort beim IIFE-Start aufgerufen (DOM-ready im PiP-Fenster)"
metrics:
  duration: "~25 min"
  completed_date: "2026-05-15"
  tasks_total: 4
  tasks_completed: 3
  tasks_pending: 1 (checkpoint Task 4)
---

# Phase 08.23.2.C Plan 07: PiP-UI Gatekeeper-Modus Summary

**One-liner:** PiP-Frontend mit Ctrl+G/Ctrl+E Keyboard-Toggle, 4 Mr.-Miyagi-Gatekeeper-Buttons via /api/gatekeeper/phrases, UWG-Hard-Block-Banner und CSS-Token-basiertem Gatekeeper-Modus — wartet auf manuellen Live-Test (Checkpoint Task 4).

## Status: CHECKPOINT-PAUSED

Plan ist bei Task 4 (Live-PiP-Test) gestoppt. Tasks 1–3 sind committed. Plan wird nach "live test passed"-Signal abgeschlossen.

## Tasks Abgeschlossen

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | API-Endpoint /api/gatekeeper/phrases + manual_mode_toggle Socket-Handler | 51f83ab | routes/app_routes.py, services/deepgram_service.py |
| 2 | CSS-Tokens + nerve.css-Erweiterung + base.html UWG-Banner-DOM | 54dcc7d | static/nerve.css, templates/base.html |
| 3 | pip-launcher.js Keyboard-Handler + Gatekeeper-Buttons + UWG-Banner + Socket-Subscriptions | 05ba9ef | static/pip-launcher.js |

## Was gebaut wurde

### Task 1 — Backend
- `GET /api/gatekeeper/phrases`: filtert `Phrase.mode='gatekeeper'`, gruppiert nach `objection_type`, Template-Variablen-Ersetzung ({branche}, {detail}), `@login_required`, non-404 verifiziert (302 = auth redirect)
- `manual_mode_toggle` Socket-Handler in `register_audio_handlers()`: Whitelist-Validierung (`target` | `gatekeeper`), setzt `contact_category` + `current_mode` + Hysterese-Reset, emittet `contact_category_update` + `manual_mode_toggle_ack` zurueck; in `socketio.handlers['/']` registriert verifiziert

### Task 2 — CSS + DOM
- `nerve.css`: `.pip-uwg-banner` (hidden/`.is-visible`), `.pip-mode-indicator`, `.pip-uwg-banner__close`, `.pip-ewb-btn[data-mode-button="gatekeeper"]` — alle `var(--...)` Tokens, keine Hex-Farben (Hex-Regex auf Phase-C-Block = 0 Treffer)
- `base.html`: `<div id="pip-uwg-banner">` + `<span id="pip-mode-indicator">` vor `pip-section-live` eingefuegt

### Task 3 — Frontend JS
- Ctrl+G / Ctrl+E Keydown-Listener: emittet `manual_mode_toggle` an Socket, Skip fuer Input/Textarea/contentEditable
- Socket-Subscriptions in `_registerSocketEvents()`: `contact_category_update`, `phase_change`, `uwg_hard_block`, `trigger_phrase_hint`, `manual_mode_toggle_ack`
- `_updateContactCategory()`: aktualisiert `pip-mode-indicator`, ruft `_renderGatekeeperButtons()` oder `_renderStandardEwbButtons()`
- `_renderGatekeeperButtons()`: fetcht `/api/gatekeeper/phrases`, rendert 4 Buttons in fester ORDER mit `escHtml` (XSS-Mitigierung T-08.23.2.C-28)
- `_showUwgBanner()`: setzt `.is-visible` auf `#pip-uwg-banner` mit geslictem Phrase-Text (max 100 Zeichen)
- `_wireUwgBannerClose()`: Close-Button entfernt `.is-visible`
- node --check: exit 0, Brace-Balance: 736/736

## Deviations from Plan

### Auto-adjustments (keine Deviation-Regeln ausgeloest)

**1. [Anpassung] --teal-accent Token nicht vorhanden**
- **Found during:** Task 2
- **Issue:** SPEC nutzt `var(--teal-accent)` aber Token existiert nicht in nerve.css :root
- **Fix:** `var(--btn-primary-bg-from)` genutzt (= #00D4AA, semantisch identisch — Token aus Phase B Plan 07.1)
- **Files modified:** static/nerve.css

**2. [Anpassung] manual_mode_toggle Handler-Platzierung**
- **Found during:** Task 1
- **Issue:** Plan zeigte `@socketio.on()` als direkten Decorator, aber Codebase nutzt `register_audio_handlers(sio)` Pattern in deepgram_service.py
- **Fix:** Handler als `@sio.on('manual_mode_toggle')` innerhalb `register_audio_handlers()` implementiert
- **Files modified:** services/deepgram_service.py

**3. [Anpassung] pip-ewb-row ID**
- **Found during:** Task 3
- **Issue:** Plan spricht von `pip-ewb-row`, tatsaechliches DOM-ID ist `nlp-ewb-row` (aus Phase 04.7)
- **Fix:** `_renderGatekeeperButtons()` und `_renderStandardEwbButtons()` nutzen `pipEl('nlp-ewb-row')` (korrekt)
- **Files modified:** static/pip-launcher.js

## Known Stubs

Keine — alle 4 Gatekeeper-Buttons werden mit echten DB-Phrases aus Migration 0003 (Plan 06b) befullt. Template-Variablen {branche}/{detail} bleiben als Platzhalter wenn kein aktiver Session-State vorhanden (intentional, Berater editiert vor Click).

## Threat Surface Scan

Alle in Plan-STRIDE registrierten Threats mitigiert:
- T-08.23.2.C-24: `category in ('target', 'gatekeeper')` Whitelist im Socket-Handler ✓
- T-08.23.2.C-26: Phrase-Text auf 100 Zeichen gesliced, laeuft nur im PiP des Beraters ✓
- T-08.23.2.C-27: Hex-Regex auf Phase-C-Block = 0 Treffer ✓
- T-08.23.2.C-28: `escHtml()` auf alle Phrase-Strings in Button-Renderer ✓

Keine neue Threat-Surface ausserhalb des Plans.

## Self-Check

Commits:
- 51f83ab: routes/app_routes.py + services/deepgram_service.py ✓
- 54dcc7d: static/nerve.css + templates/base.html ✓
- 05ba9ef: static/pip-launcher.js ✓
