---
phase: 08.23.2.G-MEET
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0011_tenant_orgs_dualwrite_backfill.py
  - database/models.py
  - tests/test_tenant_orgs.py
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-08, D-09, D-20]
must_haves:
  truths:
    - "Every existing organisations row has exactly one matching public.tenant_orgs row (legacy_org_id bridge)."
    - "Registering a new company (any of the 3 creation paths) auto-creates a tenant_orgs row in the same transaction."
    - "calls.tenant_id is backfilled for all calls whose user belongs to a known org."
    - "Production app behaves exactly as before — org_id path untouched, nothing reads tenant_orgs in the request path yet."
    - "Residual role/schema facts are RECORDED AS PROVEN CONSTANTS (pre-execute audit, psql as postgres 2026-06-01), not assumed: PG=16.14 (gen_random_uuid is core/built-in, NO pgcrypto/uuid-ossp needed); nerve_app rolbypassrls=f + rolconfig EMPTY; schema training exists / crm absent / transcript_archive exists / preference_pairs missing / nerve_anon_worker exists; no PgBouncer/pooler. A cheap one-line re-confirm at execute is a sanity check, NOT a blocking unknown."
  artifacts:
    - path: "alembic/versions/0011_tenant_orgs_dualwrite_backfill.py"
      provides: "tenant_orgs table + dual-write trigger + calls.tenant_id backfill + seed"
      contains: "CREATE TABLE IF NOT EXISTS public.tenant_orgs"
    - path: "database/models.py"
      provides: "TenantOrg ORM model"
      contains: "class TenantOrg"
    - path: "tests/test_tenant_orgs.py"
      provides: "Integration-Assertion test for seed + dual-write + backfill"
      contains: "def test_"
  key_links:
    - from: "public.organisations AFTER INSERT"
      to: "public.tenant_orgs"
      via: "trigger trg_mk_tenant_org -> mk_tenant_org()"
      pattern: "CREATE TRIGGER trg_mk_tenant_org"
    - from: "public.calls.tenant_id"
      to: "public.tenant_orgs.id"
      via: "backfill UPDATE join calls->users->organisations->tenant_orgs"
      pattern: "UPDATE public.calls"
---

<objective>
Wave 1 — Multi-Tenancy-Unterbau (UUID-Achse). Lay the `tenant_orgs` UUID tenancy root as a parallel register beside the live Integer `org_id` model, bridged by `legacy_org_id`. Seed one row per existing organisation, auto-create future rows via a DB trigger (Dual-Write, D-09), and backfill `calls.tenant_id`.

Purpose: This is the foundation every later wave hangs on (crm.* tables FK to `tenant_orgs`; RLS filters on `tenant_id`). It must ship as a CLEAN, independently deployable production state (D-02): it ONLY ADDS a parallel register + a trigger; nothing in the request path reads `tenant_orgs` yet, so the app behaves exactly as before.

Output: `public.tenant_orgs` table (seeded + bridged), idempotent dual-write trigger, `calls.tenant_id` backfilled, `TenantOrg` ORM model, isolation/seed test.

Komplexitaet: 🔴 (Foundation + DSGVO). Pre-execute audit (Claudian + Cross-AI Gemini, 2026-06-01) is COMPLETE — all must-verify gaps resolved against Production with PROVEN psql facts (see Task 0). Cross-AI is SATISFIED; next step after this revision is EXECUTE, not another review round.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-CONTEXT.md
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-RESEARCH.md
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-PATTERNS.md
@database/models.py
@alembic/versions/0008_training_schema_foundation.py
@alembic/versions/0010_add_transcript_segments.py
@CLAUDE.md

<interfaces>
<!-- Reuse these from the codebase — no exploration needed. -->

From database/models.py (lines 1-16):
```python
UUID_TYPE = String(36).with_variant(UUID(as_uuid=True), "postgresql")  # SQLite-test-compatible
JSON_TYPE = JSON().with_variant(JSONB(), "postgresql")
def utcnow():
    return datetime.now(timezone.utc).replace(tzinfo=None)
```

From database/models.py — Organisation (Integer PK, the dual-write SOURCE):
```python
class Organisation(Base):
    __tablename__ = 'organisations'
    id   = Column(Integer, primary_key=True)
    name = Column(String(200), nullable=False)
    # ... 33 more cols (see schema ref in RESEARCH Production Schema Reference)
```

From database/models.py — Call (UUID-PK new-architecture template; calls.tenant_id is the backfill target, currently 0 writers):
```python
class Call(Base):
    __tablename__ = 'calls'
    id        = Column(UUID_TYPE, primary_key=True, default=uuid.uuid4)
    tenant_id = Column(UUID_TYPE, nullable=True)   # backfill target, NO FK (D-05)
    user_id   = Column(Integer, ForeignKey('users.id'), nullable=False)
```

Migration idiom (0008/0010): `revision`/`down_revision` header; `op.execute("""...""")` raw DDL with `IF NOT EXISTS`; `ALTER TABLE x OWNER TO nerve_app` for new public tables; symmetric `downgrade()` REVOKE/DROP in reverse.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Record proven residual facts as constants (pre-execute audit RESOLVED them — sanity re-confirm only, NO blocking gate)</name>
  <read_first>
    - RESEARCH §"Residual verification need" (lines 92-99) + Assumptions Log A1/A2/A3/A4 (lines 516-521)
    - RESEARCH §"CRITICAL TOOL LIMITATION" (lines 68-99) — why inspect.sh as nerve_app cannot see training.* or role attributes
    - CLAUDE.md "HART: Kein Local-Dev" override block — inspect.sh runs against Production 178.104.82.166
  </read_first>
  <action>
    The pre-execute audit (Claudian + Cross-AI Gemini, psql read-only as postgres, 2026-06-01) ALREADY resolved A1/A2/A3/A4 against Production. These are now FACTS — record them verbatim in the SUMMARY as the binding constants the downstream waves inherit. There is NO "verify-later / fail-the-wave-if-unknown" gate anymore; A3 in particular is RESOLVED (PG=16.14), so there is no PG>=13 hard-fail branch.

    **PROVEN PRODUCTION FACTS (record verbatim in SUMMARY):**
    - **A3 (PostgreSQL version): PG = 16.14.** Therefore `gen_random_uuid()` is CORE/built-in (since PG 13). NO `CREATE EXTENSION pgcrypto`, NO uuid-ossp, NO version branch. All CREATE TABLE defaults and the dual-write trigger use `gen_random_uuid()` UNCONDITIONALLY. (No PG<13 contingency exists — do not write one.)
    - **A2 (nerve_app role attributes):** rolsuper=f, rolbypassrls=f, rolcreatedb=f, rolconfig=EMPTY. => NO `ALTER ROLE nerve_app NOBYPASSRLS` is needed (already false). => rolconfig EMPTY means there is NO role-level search_path; downstream crm tables are schema-qualified in the ORM (`__table_args__ = {'schema': 'crm'}`), NOT reliant on search_path.
    - **A2 (nerve_anon_worker):** rolsuper=f, rolbypassrls=f. Exists.
    - **A1 (schema/table reality):** schema `training` EXISTS; schema `crm` does NOT exist. `training.transcript_archive` EXISTS (owner=postgres); `training.preference_pairs` MISSING; role `nerve_anon_worker` EXISTS. => Wave 3 EXTENDS (no CREATE SCHEMA training, no CREATE ROLE, no recreate of transcript_archive); only `CREATE TABLE training.preference_pairs` is new.
    - **A4 (pooler):** NO PgBouncer/pooler on Production (no pgbouncer process; Postgres only on localhost:5432; app via unix-socket/peer-auth). No acute connection-leak today. (The transaction-local set_config approach in Wave 2 is pooler-agnostic regardless, immune if a pooler is added later.)

    **Optional cheap re-confirm at execute (sanity check ONLY — not a gate; if any value differs from the constants above, STOP and surface a discrepancy, but the expected result is a match):**
    ```
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SHOW server_version"'                       # expect 16.14
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT rolname, rolbypassrls, rolsuper, rolconfig FROM pg_roles WHERE rolname IN (\$\$nerve_app\$\$, \$\$nerve_anon_worker\$\$)"'  # expect bypassrls=f, rolconfig NULL/empty
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT nspname FROM pg_namespace WHERE nspname IN (\$\$training\$\$, \$\$crm\$\$)"'   # expect training only
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "\dt training.*"'                            # expect transcript_archive present, preference_pairs absent
    ```
    Copy whatever output you capture into the SUMMARY beside the recorded constants. No PASS/FAIL gate — the facts are proven; this is belt-and-suspenders only.
  </action>
  <verify>
    <automated>MISSING — no automated test for a fact-recording step. The SUMMARY (proven constants + optional re-confirm output) is the artifact. Read-only; no deploy needed. (This task does not block: PG=16.14 is proven, gen_random_uuid is core.)</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records the proven constants verbatim: PG=16.14 (gen_random_uuid core), nerve_app rolbypassrls=f + rolconfig EMPTY, training exists / crm absent / transcript_archive exists / preference_pairs missing / nerve_anon_worker exists, no pooler.
    - SUMMARY notes that these came from the 2026-06-01 pre-execute audit (psql as postgres) and are FACTS, not assumptions — no blocking gate, no PG<13 branch.
    - If the optional re-confirm was run, its output is pasted beside the constants. If any value DIFFERS from the recorded constant, the discrepancy is surfaced (STOP) — but a match is the expected outcome.
  </acceptance_criteria>
  <done>The proven residual facts (PG 16.14, nerve_app rolbypassrls=f/rolconfig empty, training/role/transcript_archive exist, crm/preference_pairs absent, no pooler) are recorded as binding constants in the SUMMARY; no value is left as an unknown and there is no wave-failing gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0011 — tenant_orgs table + seed + dual-write trigger + calls.tenant_id backfill</name>
  <read_first>
    - alembic/versions/0008_training_schema_foundation.py (full — revision header, op.execute raw DDL, batch_alter_table, symmetric downgrade)
    - alembic/versions/0010_add_transcript_segments.py (full — CREATE TABLE IF NOT EXISTS + OWNER TO nerve_app + index pattern)
    - PATTERNS.md §"alembic/versions/0011_*.py" (lines 34-63 — exact DDL for tenant_orgs + trigger + downgrade)
    - RESEARCH §3 "Dual-Write" (lines 283-300 — trigger shape, ON CONFLICT, SECURITY DEFINER caveat)
    - RESEARCH §"Connection B: calls.tenant_id backfill" (lines 208-211 — UPDATE-join path)
    - RESEARCH Production Schema Reference §"columns organisations" (lines 470-482 — Integer PK source) + §"schema calls" (lines 413-456 — tenant_id uuid nullable target)
    - Task 0 SUMMARY constants (PG=16.14 proven — gen_random_uuid is core/built-in; no version branch)
  </read_first>
  <behavior>
    - test_seed_one_row_per_org: after migration, COUNT(tenant_orgs) == COUNT(organisations), and every organisations.id appears exactly once as legacy_org_id.
    - test_dualwrite_trigger_fires: INSERT a new organisations row -> a tenant_orgs row with legacy_org_id = new org id appears in the same transaction.
    - test_dualwrite_idempotent: the trigger uses ON CONFLICT (legacy_org_id) DO NOTHING — re-running seed or a duplicate insert does not error or duplicate.
    - test_calls_tenant_id_backfilled: a call whose user belongs to org X has calls.tenant_id == the tenant_orgs.id bridged from org X.
    - test_calls_tenant_id_stays_nullable: column is NOT made NOT NULL (old pre-backfill rows tolerated). NOTE: this test asserts the COLUMN CONSTRAINT (still nullable), NOT row values — it does NOT contradict the post-backfill NULL-guard added below, which asserts the post-backfill ROW STATE within the migration transaction (see test_no_orphan_calls_after_backfill).
    - test_no_orphan_calls_after_backfill: after a successful 0011 upgrade against real data, `SELECT count(*) FROM calls WHERE tenant_id IS NULL` for calls of known-org users is 0 — the migration's post-backfill RAISE guard would have aborted the deploy otherwise. (Asserts the guard's effect on ROW state; column stays nullable per test_calls_tenant_id_stays_nullable.)
  </behavior>
  <action>
    Create `alembic/versions/0011_tenant_orgs_dualwrite_backfill.py` chaining from head `0010` (`revision='0011'`, `down_revision='0010'`). All identifiers ASCII (CLAUDE.md). The migration runs as postgres on Production.

    **Placement decision (RESEARCH A5 / Punkt-2 note — RESOLVED by audit, tenant_orgs placement stands):** `tenant_orgs` goes in `public`, NOT `crm`. Rationale: it is tenancy INFRASTRUCTURE (the thing `tenant_id` points at), the `organisations` table it bridges is in public, and `calls.tenant_id` (public) FKs to it intra-public. The crm/training wall (D-17) is unaffected — that wall only forbids crm<->training FKs. (Audit confirmed; no further review needed.)

    **UUID generation — gen_random_uuid() is CORE/built-in (PG=16.14 proven, Task 0/A3):** this migration uses `gen_random_uuid()` UNCONDITIONALLY as the table DEFAULT and in the seed. NO pgcrypto, NO uuid-ossp, NO conditional/version-branch logic — Production is PG 16.14 where `gen_random_uuid()` has been core since PG 13. Put this in the migration header docstring verbatim: `# PG 16.14 verified (pre-execute audit 2026-06-01) -- gen_random_uuid() is core/built-in. No pgcrypto/uuid-ossp extension and no version branch needed.` Do NOT add any CREATE EXTENSION statement and do NOT add a PG<13 fallback.

    **upgrade() steps (in order):**

    1. CREATE TABLE:
    ```python
    op.execute("""
        CREATE TABLE IF NOT EXISTS public.tenant_orgs (
            id            UUID        PRIMARY KEY DEFAULT gen_random_uuid(),   -- gen_random_uuid: core on PG 16.14 (verified Task 0/A3)
            legacy_org_id INTEGER     NOT NULL UNIQUE REFERENCES public.organisations(id),
            name          TEXT        NOT NULL,
            created_at    TIMESTAMPTZ DEFAULT now()
        )
    """)
    op.execute("ALTER TABLE public.tenant_orgs OWNER TO nerve_app")  # owner-consistency (0010:40 pattern)
    ```
    Note the UNIQUE on legacy_org_id is REQUIRED for the trigger's ON CONFLICT. The `OWNER TO nerve_app` is also LOAD-BEARING for the trigger function below: because nerve_app OWNS this table, the SECURITY INVOKER trigger (running as nerve_app on an org INSERT) already has INSERT on it — no SECURITY DEFINER needed.

    `tenant_orgs.name` is a convenience snapshot seeded at INSERT; the canonical org name remains `public.organisations.name`. No AFTER UPDATE sync trigger is added (Gemini review Q5, ⚪ LOW — accepted deliberate decision: a later `UPDATE organisations SET name=...` will drift the snapshot, which is tolerated because the snapshot is non-canonical).

    2. SEED one row per existing organisation (idempotent):
    ```python
    op.execute("""
        INSERT INTO public.tenant_orgs (id, legacy_org_id, name, created_at)
        SELECT gen_random_uuid(), o.id, o.name, now()
        FROM public.organisations o
        ON CONFLICT (legacy_org_id) DO NOTHING
    """)
    ```

    3. DUAL-WRITE trigger function + trigger (RESEARCH §3 lines 288-297). The function defaults to `SECURITY INVOKER` — do NOT use `SECURITY DEFINER` and do NOT set an explicit search_path (Gemini SQL-layer review Q1, 🔴 HIGH). Rationale: `public.tenant_orgs` is `OWNER TO nerve_app` (set in step 1). The trigger fires on an INSERT performed by nerve_app, and a `SECURITY INVOKER` function runs AS nerve_app — which, as the OWNER of `tenant_orgs`, already has INSERT. So the insert succeeds without any privilege escalation. `SECURITY DEFINER` would instead make the trigger body run as the postgres SUPERUSER (the migration runner that created the function) on EVERY org INSERT — a gratuitous privilege escalation and attack surface, NOT needed. The earlier justification ("audit-RESOLVED — needed so nerve_app can fire the tenant_orgs insert") was factually WRONG: nerve_app OWNS the table, so no definer rights are required. Dropping the explicit `SET search_path = public` is safe because the function body is a single FIXED INSERT with no dynamic SQL — there is no search_path-hijack surface for an INVOKER function here.
    ```python
    op.execute("""
        CREATE OR REPLACE FUNCTION public.mk_tenant_org() RETURNS trigger
        LANGUAGE plpgsql SECURITY INVOKER AS $$
        BEGIN
          INSERT INTO public.tenant_orgs (id, legacy_org_id, name, created_at)
          VALUES (gen_random_uuid(), NEW.id, NEW.name, now())
          ON CONFLICT (legacy_org_id) DO NOTHING;
          RETURN NEW;
        END; $$
    """)
    op.execute("""
        CREATE TRIGGER trg_mk_tenant_org AFTER INSERT ON public.organisations
        FOR EACH ROW EXECUTE FUNCTION public.mk_tenant_org()
    """)
    ```
    This covers ALL THREE org-creation sites (auth `_create_org_and_user`, waitlist `invite_from_waitlist`, coach) atomically — RESEARCH Punkt 3/4 found all three; a DB trigger is the anti-Abrieb choice over editing 3 code paths. (Audit RESOLVED the trigger gen_random_uuid built-in question — it is core on PG 16.14.)

    4. BACKFILL calls.tenant_id (one-time UPDATE join; nullable stays nullable):
    ```python
    op.execute("""
        UPDATE public.calls c
        SET tenant_id = t.id
        FROM public.users u
        JOIN public.tenant_orgs t ON t.legacy_org_id = u.org_id
        WHERE c.user_id = u.id
          AND c.tenant_id IS NULL
    """)
    ```
    Do NOT add a NOT NULL constraint and do NOT add a FK yet (D-08 — FK from calls deferred; old rows tolerated). Do NOT touch `call_events.tenant_id` (D-08 defers it).

    5. POST-BACKFILL NULL-TENANT GUARD — HARD RAISE (Gemini SQL-layer review Q3, 🟡 MEDIUM; André decision: hard RAISE). IMMEDIATELY AFTER the backfill UPDATE, append this assertion. The join is mathematically TOTAL: `users.org_id` is NOT NULL, and the seed (step 2) runs over ALL organisations with ON CONFLICT DO NOTHING immediately before this backfill in the SAME `upgrade()` transaction — so the INNER JOIN calls->users->organisations->tenant_orgs maps every call to a tenant. Any remaining NULL therefore = a corrupted/orphan row, and the deploy MUST fail loudly rather than ship silent orphans:
    ```python
    op.execute("""
        DO $$
        BEGIN
          IF EXISTS (SELECT 1 FROM public.calls WHERE tenant_id IS NULL) THEN
            RAISE EXCEPTION 'Backfill failed: orphaned calls with NULL tenant_id';
          END IF;
        END $$;
    """)
    ```
    The COLUMN stays NULLABLE (we do NOT add NOT NULL) — pre-backfill historical rows are tolerated at the schema level, and `test_calls_tenant_id_stays_nullable` stays valid (it asserts the constraint, not row values). The guard asserts only the POST-BACKFILL ROW STATE within this migration's transaction: if it RAISEs, the whole `upgrade()` transaction rolls back and the deploy aborts (deploy.sh's pre-restart gate leaves old code live). RAISE-on-any-NULL is correct because there are no legitimately-NULL-tenant calls for known-org users (the join is total).

    **downgrade()** (reverse order, symmetric to 0008:54-63): DROP TRIGGER trg_mk_tenant_org; DROP FUNCTION mk_tenant_org(); set calls.tenant_id back to NULL where it was backfilled is NOT required (leave data); DROP TABLE public.tenant_orgs. (The DO-block guard is a one-shot assertion inside upgrade(); nothing to reverse for it.)
  </action>
  <verify>
    <automated>bash deploy.sh production && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/python -m pytest tests/test_tenant_orgs.py -x'</automated>
    <live>ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema tenant_orgs' shows id uuid PK, legacy_org_id integer UNIQUE NOT NULL; bash scripts/inspect.sh count tenant_orgs == bash scripts/inspect.sh count organisations</live>
    <migrate>sudo -u postgres bash -c 'cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve /opt/nerve/venv/bin/alembic upgrade head' (head shows 0011; the post-backfill DO-block guard does NOT raise => backfill is complete)</migrate>
  </verify>
  <acceptance_criteria>
    - `inspect.sh migrations` returns `0011`.
    - Migration docstring states "PG 16.14 verified -- gen_random_uuid() is core/built-in" (no extension, no version branch).
    - The trigger function `public.mk_tenant_org()` is created with `SECURITY INVOKER` (the default) and has NO `SECURITY DEFINER` and NO `SET search_path` — verify: `sudo -u postgres psql -d nerve -c "SELECT prosecdef, proconfig FROM pg_proc WHERE proname='mk_tenant_org'"` returns prosecdef=f (NOT definer) and proconfig NULL; and a grep of the migration shows the function body does NOT contain `SECURITY DEFINER`.
    - The `upgrade()` migration contains the post-backfill guard `RAISE EXCEPTION 'Backfill failed: orphaned calls with NULL tenant_id'` AFTER the backfill UPDATE (Gemini Q3 hard-RAISE).
    - `inspect.sh schema tenant_orgs` shows: `id uuid not null` (PK), `legacy_org_id integer not null` with UNIQUE, `name text not null`, `created_at timestamptz`. Owner = nerve_app.
    - `inspect.sh count tenant_orgs` equals `inspect.sh count organisations` (seed complete).
    - The trigger exists: `sudo -u postgres psql -d nerve -c "\d public.organisations"` lists `trg_mk_tenant_org ... AFTER INSERT`.
    - `tests/test_tenant_orgs.py` passes all 6 behaviors above on the Production server (in-memory `client` fixture, no DATABASE_URL on SSH — CLAUDE.md test rule).
    - calls backfill: `inspect.sh sample calls 20` shows tenant_id populated for calls of seeded-org users (spot-check non-null); the migration's RAISE guard did NOT abort the deploy (head=0011 proves the guard passed => 0 orphans).
  </acceptance_criteria>
  <done>Migration 0011 is head on Production; tenant_orgs is seeded 1:1 with organisations; the dual-write trigger fires + is idempotent and is SECURITY INVOKER (no superuser escalation); calls.tenant_id is backfilled and the post-backfill RAISE guard confirmed 0 orphans; all 6 pytest behaviors green server-side.</done>
</task>

<task type="auto">
  <name>Task 2: TenantOrg ORM model (DDL/ORM parity, CLAUDE.md Punkt 21)</name>
  <read_first>
    - database/models.py lines 1-16 (UUID_TYPE, JSON_TYPE, utcnow) + lines 19-60 (Organisation) + lines 645-697 (Call — UUID PK template)
    - PATTERNS.md §"database/models.py — TenantOrg" (lines 127-155)
    - Migration 0011 (Task 1) — the ORM must match the DDL exactly (column names/types/nullable)
  </read_first>
  <action>
    Add a `TenantOrg` model to `database/models.py` (after the `Call`/`CallEvent`/`TranscriptSegment` block, in the "Neue Architektur-Tabellen" section). It must be ORM/DDL-consistent with migration 0011 (CLAUDE.md Punkt 21 — the ORM is the test-schema source for SQLite in-memory tests):
    ```python
    class TenantOrg(Base):
        __tablename__ = 'tenant_orgs'
        # public schema (tenancy infra, not crm) — no schema= table_arg
        id            = Column(UUID_TYPE, primary_key=True, default=uuid.uuid4)
        legacy_org_id = Column(Integer, ForeignKey('organisations.id'), nullable=False, unique=True)
        name          = Column(Text, nullable=False)
        created_at    = Column(DateTime(timezone=True), server_default=func.now())
    ```
    Use `func.now()` for server_default (NOT `text('now()')` — `text` is shadow-safe here but follow the established TranscriptSegment convention at models.py:729). ASCII identifiers only. Do NOT add a relationship to Call (calls.tenant_id has no FK by design, D-05/D-08).
  </action>
  <verify>
    <automated>bash deploy.sh production && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/python -m pytest tests/test_tenant_orgs.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class TenantOrg" database/models.py` returns the class.
    - The SQLite in-memory test schema (built from models.py via `init_db`) creates tenant_orgs so `tests/test_tenant_orgs.py` can run with the `client`/in-memory fixture (no DATABASE_URL on SSH).
    - Column names/types in the model exactly match migration 0011's DDL (id uuid PK, legacy_org_id integer unique nullable=False, name text, created_at).
  </acceptance_criteria>
  <done>TenantOrg model exists, matches the migration DDL 1:1, and the in-memory test suite builds the table from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App (nerve_app role) -> Postgres | nerve_app issues INSERTs into organisations; trigger runs inside that transaction AS nerve_app (SECURITY INVOKER) |
| Migration (postgres superuser) -> Postgres | one-time DDL + seed + backfill, full privilege |
| Future request path -> tenant_orgs | NOT crossed in Wave 1 — nothing reads tenant_orgs in the request path yet (clean stop-point) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-G1-01 | Elevation of Privilege | `mk_tenant_org()` trigger function | mitigate (by design) | RESOLVED by using `SECURITY INVOKER` (Gemini review Q1, 🔴 HIGH): the function runs as the INVOKING `nerve_app`, which OWNS `public.tenant_orgs`, so the INSERT succeeds with NO privilege escalation. The earlier `SECURITY DEFINER` would have run the trigger body as the postgres SUPERUSER on every org INSERT — that escalation is now REMOVED. No search_path-hijack surface remains: the function is INVOKER with a single STATIC INSERT and no dynamic SQL, so no explicit `SET search_path` is needed. Verified: `pg_proc.prosecdef=f` and `proconfig` NULL for mk_tenant_org. |
| T-G1-02 | Tampering | Dual-write integrity (org without tenant row) | mitigate | Trigger fires AFTER INSERT FOR EACH ROW on ALL org-creation paths atomically; `ON CONFLICT (legacy_org_id) DO NOTHING` + UNIQUE constraint guarantee idempotency and no duplicate/orphan. Seed covers pre-existing orgs. |
| T-G1-03 | Information Disclosure | calls.tenant_id backfill mis-join leaking tenant linkage | mitigate | Backfill joins calls->users->organisations->tenant_orgs on real FKs; the post-backfill hard-RAISE guard (Gemini Q3) aborts the deploy if ANY call retains a NULL tenant_id, so no silent orphan/mis-mapping ships. tenant_id is not yet read by any RLS/request path in Wave 1. Verified by test_calls_tenant_id_backfilled + test_no_orphan_calls_after_backfill. |
| T-G1-04 | Denial of Service | Trigger on hot org-insert path | accept | Org creation is rare (registration), single-row INSERT with ON CONFLICT; negligible overhead. |
| T-G1-05 | Repudiation | Unproven role/version assumptions (BYPASSRLS, PG ver) | mitigate | RESOLVED by pre-execute audit (2026-06-01, psql as postgres): PG=16.14 (gen_random_uuid core), nerve_app rolbypassrls=f + rolconfig empty, training/role/transcript_archive existence proven. Task 0 records these as constants; no inference carried into Wave 2. |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `public.tenant_orgs` (CREATE + seed + trigger-write) — NEW
- `public.organisations` (read for seed; trigger source AFTER INSERT) — siehe inspect.sh
- `public.calls` (UPDATE tenant_id backfill + post-backfill NULL guard) — siehe inspect.sh
- `public.users` (read for backfill join: users.org_id) — siehe inspect.sh

### inspect.sh-Beleg (verbatim aus RESEARCH Production Schema Reference, lines 413-482; re-confirmed by pre-execute audit Task 0)

```
$ bash scripts/inspect.sh schema calls
 id                    | uuid                        | not null
 tenant_id             | uuid                        |          <- backfill target, NULLABLE, NO FK
 account_id            | uuid                        |
 contact_id            | uuid                        |
 user_id               | integer                     | not null    (FK users.id)
 ... (24 cols total; calls.tenant_id has NO FK — deferred D-05)
```
```
$ bash scripts/inspect.sh columns organisations
 id integer NOT NULL (PK); name varchar NOT NULL; ... (35 cols)   <- Integer PK, dual-write source
```
```
$ bash scripts/inspect.sh columns tenant_orgs
 (0 rows)   <- genuinely absent pre-migration; created in 0011
```

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `TenantOrg.legacy_org_id` | written by seed + trigger; read by backfill join | DB column public.tenant_orgs.legacy_org_id (Integer, UNIQUE, FK organisations.id) | created in 0011; matches ORM (Task 2) |
| `organisations.id` | read for seed + trigger NEW.id | DB column public.organisations.id (Integer PK) | ✓ inspect.sh columns organisations |
| `organisations.name` | read for seed + trigger NEW.name | DB column public.organisations.name | ✓ inspect.sh columns organisations |
| `calls.tenant_id` | written by backfill UPDATE; asserted non-NULL by post-backfill guard | DB column public.calls.tenant_id (uuid, NULLABLE, no FK) | ✓ inspect.sh schema calls — confirmed real column, not RAM |
| `users.org_id` | read for backfill join | DB column public.users.org_id (Integer FK) | ✓ 189 production hits (RESEARCH Punkt 1); real DB column |

### Bei Diskrepanz: STOP + Replan

<verification>
- Migration head = 0011 (`inspect.sh migrations`).
- tenant_orgs seeded 1:1 with organisations; trigger present on organisations and is SECURITY INVOKER (pg_proc.prosecdef=f).
- calls.tenant_id backfilled (non-null for known-org users); post-backfill hard-RAISE guard confirmed 0 orphans (deploy did not abort).
- App request path unchanged (no new reads of tenant_orgs) — Production behaves exactly as before.
- Task 0 residual facts recorded as proven constants (PG 16.14, nerve_app rolbypassrls=f/rolconfig empty, training/role/transcript_archive exist, crm/preference_pairs absent, no pooler) — no blocking gate.
</verification>

<success_criteria>
- Wave 1 is independently deployable: `bash deploy.sh production` green, migration 0011 applied as postgres, app behaves as before.
- `tests/test_tenant_orgs.py` green server-side (6 Integration-Assertion behaviors).
- tenant_orgs is the FK target ready for Wave 2; nothing in the request path depends on it yet (clean stop-point, D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-01-SUMMARY.md` — MUST include the Task 0 proven constants (PG 16.14; nerve_app rolbypassrls=f/rolconfig empty; training exists / crm absent / transcript_archive exists / preference_pairs missing / nerve_anon_worker exists; no pooler) so Wave 2 inherits proven facts. ALSO record that mk_tenant_org() is SECURITY INVOKER (Gemini Q1) and that the post-backfill hard-RAISE guard (Gemini Q3) confirmed 0 orphan calls.
</output>
</content>
</invoke>
