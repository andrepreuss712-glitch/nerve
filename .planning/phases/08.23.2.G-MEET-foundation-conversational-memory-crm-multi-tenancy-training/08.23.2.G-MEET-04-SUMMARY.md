---
phase: 08.23.2.G-MEET
plan: 04
wave: 1
status: complete   # 2026-06-02: Migration 0014 applied as postgres on prod (head=0014), crm.user_preferences live with FORCED RLS + NULLIF policy + nerve_app=arwd, MM-05 accounts UNIQUE live, 7/7 tests green server-side, /api/health ok. Wave 1 done; Wave 2 (Plan 05 Frontend) still open.
subsystem: crm / meeting-write-path / user-preferences / rls / dsgvo
requirements: [MEET-REQ-1, MEET-REQ-2, MEET-REQ-4]
deployed: YES   # manual-direct-prod 2026-06-02 (André drove it), alembic head=0014
migration_head_prod: 0014
git_head: 7a127c7
tags: [crm, meetings, user-preferences, rls, multi-tenant, dsgvo, postgres, migration, tz-aware]
key-files:
  created:
    - alembic/versions/0014_crm_user_preferences_rls.py
    - tests/test_meeting_save_rls.py
  modified:
    - database/models.py        # + UserPreference (crm schema); Account += uq_accounts_tenant_name (MM-05)
    - routes/crm_export.py       # + save_meeting / get_meeting_pref / set_meeting_pref + _resolve_account/_resolve_contact
commits:
  - e30e8ff  # UserPreference ORM + Migration 0014 (crm.user_preferences FORCED RLS + MM-05 accounts UNIQUE)
  - 7a127c7  # /crm/meetings + /crm/preferences routes + real-PG RLS tests
---

# Phase 08.23.2.G-MEET Plan 04: Wave 1 — Backend: crm.user_preferences + Meeting-Write-Route + RLS-Tests

**Status: COMPLETE + LIVE ON PROD.** Migration 0014 applied as postgres (head=0014), `crm.user_preferences`
live with FORCED RLS + the proven NULLIF `tenant_isolation` policy + `nerve_app=arwd` via default-ACL,
`crm.accounts` now carries `uq_accounts_tenant_name` (MM-05), 7/7 real-PG tests green server-side,
`/api/health` ok. This is the **self-contained, independently-deployable backend unit** the Plan 05
PiP frontend (Wave 2) will consume. Wave 2 is still open — phase-level verification intentionally
deferred (wave filter `--wave 1`).

This increment incorporates the Cross-AI `--reviews` findings already locked into the plan:
**MM-01** (tz-aware scheduled_at), **MM-04** (sanitized error logging), **MM-05** (atomic accounts UNIQUE),
**MM-07** (prefs keyed server-side on `g.user.id`, rejected as a non-issue).

---

## (a) What was built per task (+ commit SHAs)

### Task 1 — UserPreference ORM + Migration 0014 (`e30e8ff`)
- **`database/models.py`:** `UserPreference` inserted after `Meeting`, before the Wave-3 training block.
  `{'schema': 'crm'}` is the LAST `__table_args__` element (explicit schema-qualification, no search_path
  reliance). Columns: `id` UUID PK, `tenant_id` UUID NOT NULL, `user_id` Integer NOT NULL (soft-link to
  `public.users.id`, NO mauer-FK — D-08 analogy; set server-side from `g.user.id`, never a client value — MM-07),
  `auto_save_meeting` Boolean NOT NULL `server_default=text('false')` (DSGVO opt-in OFF, Art. 25 Abs. 2),
  `schema_version` SmallInteger, `created_at`/`updated_at`. UNIQUE(tenant_id, user_id) + tenant index.
  Imports (Boolean/UniqueConstraint/text/Integer/SmallInteger/Index/DateTime/func) already present at models.py:2.
- **MM-05 ORM parity:** `Account.__table_args__` gained `UniqueConstraint('tenant_id', 'name', name='uq_accounts_tenant_name')`
  (before the `{'schema':'crm'}` element, which stays last).
- **`alembic/versions/0014_crm_user_preferences_rls.py`:** `revision='0014'`, `down_revision='0013'`, all-ASCII,
  runs as postgres. `upgrade()`: `CREATE TABLE IF NOT EXISTS crm.user_preferences` (UUID PK `gen_random_uuid()`,
  tenant_id FK -> public.tenant_orgs, user_id INTEGER, `auto_save_meeting BOOLEAN NOT NULL DEFAULT false`,
  schema_version, created/updated, UNIQUE(tenant_id, user_id)) -> ENABLE + FORCE RLS -> `tenant_isolation`
  policy (USING + WITH CHECK, the proven `nullif(current_setting('app.tenant_id', true), '')::uuid` fail-closed
  form, identical to 0012) -> tenant index. **MM-05:** DO-block-guarded `ALTER TABLE crm.accounts ADD CONSTRAINT
  uq_accounts_tenant_name UNIQUE (tenant_id, name)` (idempotent re-run guard). Symmetric `downgrade()`.
  **NO `GRANT`, NO `ALTER ... OWNER TO nerve_app`** (default-ACL from 0012:57-58 grants nerve_app=arwd on the
  new postgres-owned table automatically; ownership would bypass RLS — Research §0.3). The only literal "GRANT"
  strings in the file are docstring prose explaining what it deliberately does NOT do.

### Task 2 — Write-route + Preferences + real-PG tests (`7a127c7`)
- **`routes/crm_export.py`** (extended the only crm blueprint, `crm_export_bp` url_prefix `/crm`):
  - `_resolve_account(db, tenant_id, firma)` — RLS-scoped find by name; blank -> None; **MM-05:** create via
    `INSERT ... ON CONFLICT (tenant_id, name) DO NOTHING` + flush + re-select (atomic vs double-submit; the DB
    UNIQUE wins the race, no IntegrityError to the client). `_resolve_contact` analogous (contacts-uniqueness
    deferred, documented).
  - `POST /crm/meetings` `save_meeting` — fail-closed 403 if no `g.tenant_id`; **MM-01:** parses an offset-bearing
    ISO via `datetime.fromisoformat`, **rejects offset-less/naive input with 400 'Datum braucht Zeitzone'** so
    Postgres can't silently assume the session TZ (10:00+02:00 -> stored 08:00Z instant); stamps
    `tenant_id=g.tenant_id` (== GUC -> WITH CHECK passes), resolve-or-creates accounts/contacts, maps `call_id`
    soft-link, blank Firma -> account_id NULL. **MM-04:** except block logs `traceback.print_exc()` +
    `print('[CRM-Meeting] ...')` before a sanitized 500 (no traceback in the body).
  - `GET/POST /crm/preferences` — UPSERT `auto_save_meeting`, keyed on `g.user.id` server-side (MM-07, never a
    client user_id); GET returns False when no row. Both have MM-04 logging.
- **`tests/test_meeting_save_rls.py`** (cloned from `test_rls_isolation.py`; real-PG via `nerve_app_pg_conn`,
  NO SQLite fallback by design): `test_user_preferences_rls_isolation` (A can't see/insert B's prefs),
  `test_auto_save_meeting_default_false` (D-2/D-4 DSGVO off-by-default), `test_meeting_resolve_or_create_account`,
  `test_meeting_account_no_duplicate_on_conflict` (MM-05 — exactly one row after double ON CONFLICT insert),
  `test_meeting_scheduled_at_tz_instant` (MM-01 — 10:00+02:00 reads back 08:00Z), `test_meeting_scheduled_at_naive_rejected`
  (MM-01 — function-level assertion on the parse branch, runtime not source-grep), `test_meeting_tenant_stamp`
  (stamp + cross-tenant INSERT raises). All `[MEET-TEST]`-tagged rows cleaned up reverse-FK in teardown.

---

## (b) STEP-0 Hard-Gate + Deployment (manual-direct-prod, André-driven)

Per CLAUDE.md (HART: no local dev; Production is the test environment) and André's "I drive it" pattern, the
prod migration was a `[BLOCKING]` human-action checkpoint. André ran the gated sequence and confirmed `applied`:

1. **STEP-0 hard-gate (read-only, as postgres, BEFORE migration):** head == `0013`, `to_regclass('crm.user_preferences')`
   == NULL (absent), `uq_accounts_tenant_name` absent. → PASS (matched the expected pre-state; no drift across the
   `/clear` boundary). Any mismatch would have been a HARD STOP.
2. **Code tar-deploy** (`deploy.sh production`; the built-in full-pytest gate trips on the pre-existing `test_ft_seed`
   — known manual path, code tar lands regardless).
3. **Migration 0014 as postgres** (`alembic upgrade head`) → head == **0014**.
4. **Post-migration verify (as postgres):** `\d crm.user_preferences` shows FORCED RLS + `tenant_isolation`
   (NULLIF form) + `uq_user_preferences_tenant_user` + `idx_user_preferences_tenant`; `\dp` shows owner postgres +
   `nerve_app=arwd/postgres` (default-ACL auto-grant); `\d crm.accounts` shows `uq_accounts_tenant_name` UNIQUE
   (tenant_id, name) (MM-05).
5. **`.deploy_meta` 0014 + `systemctl restart nerve` + `/api/health`** → ok.
6. **`pytest tests/test_meeting_save_rls.py`** server-side (real PG, nerve_app) → **7/7 green** (RLS isolation,
   DSGVO default-false, resolve-or-create, MM-05 no-duplicate, MM-01 tz-instant + naive-reject, tenant-stamp +
   cross-tenant reject).

> Confirmation basis: the operator (André) replied `applied`, which per the checkpoint resume-signal asserts all of
> the above expected results were met. The orchestrator did not itself run the prod migration (confirmation-pflichtig
> trotz config `auto_advance:true`).

---

## (c) Cross-Layer / Persistenz-Schicht (CLAUDE.md Punkt 21/22)
Every touched layer matches: `g.tenant_id` (app.py:2201 publish -> transaction-local GUC via db.py after_begin) ==
the stamped `crm.meetings.tenant_id`/`crm.user_preferences.tenant_id`; `crm.user_preferences` was verified ABSENT
pre-migration and CREATED in 0014 (no code-variable-masquerading-as-DB-column); `auto_save_meeting` is a real new
BOOLEAN DEFAULT false column (read back False in the test); `public.users.id` is integer-PK (soft-link, no FK).

---

## (d) Deviations
| # | Rule | What |
|---|------|------|
| 1 | Process | `gsd-code-review` SKIPPED (multi-segment phase-ID gotcha — gsd-tools misresolves `08.23.2.G-MEET` to the wrong empty dir). Cross-AI-informed plan findings (MM-01..MM-07) + manual review stand in (consistent with Waves 1/2/3). |
| 2 | Process | STATE.md / ROADMAP.md hand-edited (gsd-tools `roadmap update-plan-progress` misresolves the multi-segment ID). |
| 3 | Verify ordering | Task 2's `<verify>` SSH-pytest needs Migration 0014 (the table) -> it was run as part of the Task 3 checkpoint AFTER the migration, not before. Local static greps (route defs, MM-01 tzinfo guard, MM-05 ON CONFLICT, 3× MM-04 logging, Blueprint name) all passed at Task 2 time. |
| — | Scope (deferred, not silent) | MM-02 auto_save "honor" behavior (actually auto-creating meetings on call-end) is NOT in this plan — deferred to **Backlog 999.3** per André. This plan only persists the preference. Contacts-uniqueness DB constraint also deferred (accounts is the mandatory resolve target; MM-05 "at least accounts" satisfied). |

## Known Stubs
None. The preference is fully persisted + RLS-scoped; the write-route is wired to real crm tables.

## Self-Check
- Files: alembic/versions/0014_crm_user_preferences_rls.py, tests/test_meeting_save_rls.py — FOUND on disk;
  deployed to prod (André confirmed `applied`). database/models.py + routes/crm_export.py modified.
- Migration head on prod = 0014 (confirmed). crm.user_preferences FORCED RLS + NULLIF policy + nerve_app=arwd;
  crm.accounts has uq_accounts_tenant_name. 7/7 tests green server-side.
- Commits e30e8ff, 7a127c7 — FOUND in git log + pushed to origin (3d97b5b..7a127c7).

## Self-Check: PASSED
