---
phase: 08.23.2.G-MEET
plan: 03
wave: 3
status: complete   # 2026-06-01: migration 0013 live on prod (head=0013), 14/14 runnable tests green, D-16 worker runtime VERIFIED via Claudian SET-ROLE gate, service restarted. Wave 3 + whole phase complete.
subsystem: training / dpo-foundation / anonymizer-worker / crm-rls
requirements: [D-13, D-15, D-16, D-17, D-18, D-19]
deployed: YES   # manual-direct-prod 2026-06-01, alembic head=0013
migration_head_prod: 0013
git_head: a5a2b60
tags: [training, dpo, anonymizer, rls, multi-tenant, dsgvo, postgres, migration, cron]
key-files:
  created:
    - alembic/versions/0013_preference_pairs_anonymizer_grants.py
    - scripts/anonymizer_worker.py
    - tests/test_anonymizer_worker.py
  modified:
    - database/models.py            # + PreferencePair (training schema)
    - tests/conftest.py             # + anon_worker_pg_engine fixture
    - tests/test_account_memory_briefing.py   # ATTACH training schema (PreferencePair now in metadata)
    - .env.example                  # + ANON_WORKER_DATABASE_URL (name only)
commits:
  - 46060a5  # migration 0013 (preference_pairs + worker grants + worker crm RLS policies)
  - 31386ff  # PreferencePair ORM model
  - 7aa6fae  # anonymizer worker + Integration-Assertion test
  - d084edf  # fix: seed call_mode='cold_call' (ck_calls_call_mode)
  - e91adbe  # fix: explicit transcript_segments.id (SQLite BIGINT no autoincrement)
  - 665acee  # fix: dry-run archives nothing + fixture text
  - a5a2b60  # fix: briefing test ATTACHes training schema
---

# Phase 08.23.2.G-MEET Plan 03: Wave 3 — Training DPO-Foundation + Anonymizer-Worker

**Status: COMPLETE + LIVE ON PROD.** Migration 0013 applied as postgres (head=0013), all DDL/policy/grant
structure live-verified, 14/14 runnable tests green server-side, the D-16 worker runtime behavior VERIFIED
via a Claudian SET-ROLE gate, service restarted, `/api/health` ok. This is the **last wave** — Phase
08.23.2.G/MEET (all 3 waves) is complete.

One-liner: `training.preference_pairs` (DPO foundation, created-not-populated — Phase E owns population)
+ extended `nerve_anon_worker` grants (training write + crm column-level stamp) + two worker-targeted
PERMISSIVE RLS policies on `crm.account_memory` (`anon_worker_read` + `anon_worker_stamp`, TO
nerve_anon_worker) so the worker reads crm CROSS-TENANT and its `anonymized_at` stamp PERSISTS while
`nerve_app` stays tenant-scoped + a standalone Variante-A anonymizer cron writing `training.transcript_archive`.

---

## (a) STEP-0 HARD GATE — Wave-2 Production Re-Verification (read-only, as postgres) → PASS

Ran the read-only `ssh ... sudo -u postgres psql -d nerve` inspection against prod `178.104.82.166`
BEFORE any code/migration action (drift guard across the `/clear` boundary; SUMMARY = intention,
Production = truth). **Every check matched. Verbatim key results:**

**1. `\d crm.account_memory`** — columns `anonymized_at TIMESTAMPTZ`, `meddpicc jsonb`, `context_hooks jsonb`
all present; footer "Policies (forced row security enabled)"; and:
```
POLICY "tenant_isolation"
  USING ((tenant_id = (NULLIF(current_setting('app.tenant_id'::text, true), ''::text))::uuid))
  WITH CHECK ((tenant_id = (NULLIF(current_setting('app.tenant_id'::text, true), ''::text))::uuid))
```
✓ MATCH — policy EXISTS, PERMISSIVE, NO `TO`/`FOR` clause (applies to PUBLIC), USING + WITH CHECK both
present. **The predicate is the `nullif(...)` form — this is the LEGITIMATE Wave-2 fail-closed amendment
(02-SUMMARY §(e)), NOT a mismatch.** Per the gate's own semantics (and André's execute-time directive),
the gate verifies the policy SHAPE (exists / permissive / no TO-FOR / USING + WITH CHECK), NOT the exact
old expression string — so the nullif form is accepted. This is exactly the shape 0013 stacks anon_worker_* on.

**2. all crm tables relrowsecurity + relforcerowsecurity:** accounts / contacts / account_memory / meetings
ALL `t / t`. ✓ MATCH (FORCE RLS on everywhere).

**3. `SELECT version_num FROM alembic_version` → `0012`.** ✓ MATCH (Wave 2 is head; Wave 3 not run yet).

**4. `\dn training` → `training | postgres`.** ✓ present.
**5. `\dt training.*` → only `transcript_archive` (table, postgres).** ✓ transcript_archive PRESENT,
preference_pairs ABSENT.
**6. `\dg nerve_anon_worker` → role present.** ✓
**7. policy baseline on crm.account_memory:** ONLY `tenant_isolation` (polcmd `*`, polpermissive `t`,
polroles `{-}` = PUBLIC). ✓ clean base — anon_worker_* not yet present.

**GATE VERDICT: PASS** — proceeded to write code. (Any structural deviation → HARD STOP, no code change.)

---

## (b) What was built per task (+ commit SHAs)

### Task 1 — Migration 0013 (`46060a5`)
`alembic/versions/0013_preference_pairs_anonymizer_grants.py`, `revision='0013'`, `down_revision='0012'`,
all-ASCII, runs as postgres. EXTEND-only:
- `CREATE TABLE IF NOT EXISTS training.preference_pairs` — UUID PK (`gen_random_uuid()`, core on PG 16.14),
  prompt/chosen/rejected `JSONB NOT NULL`, batch_id UUID NOT NULL, anonymizer_version, source_call_hash
  TEXT (hash, NOT a call_id FK — D-17), labeller, rating_chosen/rejected, rationale, split CHECK(train/val/test),
  schema_version SMALLINT DEFAULT 1 (D-19) + `idx_preference_pairs_batch`. **CREATED here, POPULATED by Phase E.**
- `GRANT INSERT, SELECT ON training.preference_pairs TO nerve_anon_worker` (provisioned now so Phase E needs
  no further grant migration) + `GRANT UPDATE (anonymized_at) ON crm.account_memory TO nerve_anon_worker`
  (column-level — worker can stamp ONLY, cannot mutate cleartext, T-G3-02). (crm SELECT was already granted in 0012.)
- Two worker-targeted PERMISSIVE policies on crm.account_memory: `anon_worker_read` (FOR SELECT TO
  nerve_anon_worker USING true) + `anon_worker_stamp` (FOR UPDATE TO nerve_anon_worker USING true WITH CHECK true).
  They stack OR on the unchanged 0012 `tenant_isolation` policy → worker row set = all rows cross-tenant
  (D-16); nerve_app NOT in TO list → stays tenant-scoped. `tenant_isolation` is NOT modified/dropped/recreated.
- Symmetric `downgrade()`: DROP both policies → REVOKE UPDATE(anonymized_at) → REVOKE preference_pairs grants
  → DROP INDEX → DROP TABLE preference_pairs. Does NOT touch tenant_isolation / schema / role / transcript_archive.
- **NO `CREATE SCHEMA training`, NO `CREATE ROLE` op.execute statements** (precise grep on the deployed file
  confirmed NONE — the only such strings are docstring prose explaining what it does NOT do).

### Task 2 — PreferencePair ORM model (`31386ff`)
`database/models.py`: `PreferencePair` with `{'schema':'training'}` as the LAST `__table_args__` element
(explicit schema-qualification, no search_path reliance), column-for-column parity with 0013, ck split CHECK,
batch index. Explicitly NO ForeignKey to any crm.* table (D-17).

### Task 3 — Anonymizer worker + Integration-Assertion test (`7aa6fae` + fixes)
`scripts/anonymizer_worker.py` — standalone cron, connects as `nerve_anon_worker` via a dedicated
`ANON_WORKER_DATABASE_URL` (NOT the nerve_app engine), `--dry-run`, `[ANON]` logging. Variante A loop:
SELECT crm.account_memory WHERE anonymized_at IS NULL (cross-tenant via anon_worker_read; sets NO
app.tenant_id) → resolve source calls (`calls.account_id`/`contact_id` → `users.is_test_user`) → SKIP
is_test_user calls → anonymize `transcript_segments` via `services.anonymization.anonymize()` with
`should_persist()` gating → INSERT into `training.transcript_archive` with `source_call_hash = sha256(call.id)`
(one-way, no raw id, D-17) → `UPDATE crm.account_memory SET anonymized_at` (column-level grant +
anon_worker_stamp policy). Per-row SAVEPOINT isolates failures. Docstring PINS the RLS resolution + the W-6
boundary (writes transcript_archive now; preference_pairs is Phase E). `limit_ids` is a TEST-ONLY scoping seam
(zero prod blast radius). `tests/conftest.py` gained `anon_worker_pg_engine` (DSN `ANON_WORKER_TEST_DSN`,
skip-if-absent, no SQLite fallback). `.env.example` documents `ANON_WORKER_DATABASE_URL` (name only).

---

## (c) Deployment (manual-direct-prod, executor-driven with André's authorization)

Gated sequence, output captured at each gate:
1. **Pre-flight:** `main` pushed (`5ef75f4..7aa6fae`, later `..a5a2b60`). Prod git frozen/stale at `014fcef`
   (in-history); "M" files are the known tar-landing artifacts, NOT a real SCP hotfix → safe to tar-deploy.
2. **Code tar-deploy** (Wave-1 manual path — `deploy.sh`'s full-suite gate trips on pre-existing `test_ft_seed`;
   deps unchanged → pip/spaCy/GLiNER re-cache skipped). 0013 + models.py + worker + tests landed; box `py_compile` OK.
3. **Migration 0013 as postgres:** `0012 → 0013`, `SELECT version_num` = **0013**.
4. **Live verification (as postgres):**
   - `\d training.preference_pairs`: prompt/chosen/rejected `jsonb NOT NULL`, batch_id uuid NOT NULL,
     schema_version default 1, split CHECK(train/val/test), UUID PK gen_random_uuid, batch index. ✓
   - FK count on preference_pairs = **0** (D-17, information_schema). ✓
   - `pg_policy` on crm.account_memory = **exactly 3**: `anon_worker_read` (r, permissive, {nerve_anon_worker},
     USING true), `anon_worker_stamp` (w, permissive, {nerve_anon_worker}, USING true + WITH CHECK true),
     `tenant_isolation` (*, permissive, {-} PUBLIC, **unchanged** nullif form). ✓
   - grants: nerve_anon_worker INSERT,SELECT on preference_pairs; UPDATE on crm.account_memory ONLY on
     `anonymized_at` column. ✓
5. **Restart + health:** `.deploy_meta` GIT_HEAD=7aa6fae written, `systemctl restart nerve` → `active`,
   `/api/health` → `status ok, pipeline ok, git_head 7aa6fae` (later code: a5a2b60 carries only test-infra fixes).

---

## (d) Test results (server-side, real PG / in-memory)

**14 passed, 1 skipped** across the Wave 2 + Wave 3 suites (`test_rls_isolation.py` 4 + `test_account_memory_briefing.py`
4 + `test_anonymizer_worker.py` 6 passed / 1 skipped):

**Logic group (in-memory SQLite, ATTACH crm+training, injected anonymize stub) — 5/5 PASS:**
- `test_worker_processes_unstamped` (2 of 3 segments persisted, Mueller→[PERSON_A]; 'GEHEIM' dropped by should_persist; stamped)
- `test_worker_skips_stamped` (idempotent — stamped row not reselected)
- `test_worker_filters_test_user` (is_test_user call's segments NOT written; row still stamped)
- `test_no_crm_id_in_training` (source_call_hash == sha256(call_id), 64 hex, never a raw id — D-17)
- `test_dry_run_writes_nothing` (candidates reported, 0 archived, 0 stamped)

**RLS group (real PG, nerve_app via peer) — nerve_app no-leak PASS, worker test credential-gated:**
- `test_nerve_app_still_tenant_scoped` ✓ PASS — after the 0013 worker policies, nerve_app (GUC=tenant_A) sees
  ONLY tenant_A's account_memory row, NOT tenant_B's → the worker policies (TO nerve_anon_worker) do NOT leak
  to the app role (no Wave-2 isolation regression).
- `test_worker_cross_tenant_read_and_stamp_persists` — SKIPPED in the persistent run (nerve_anon_worker has
  no OS user / its TCP password is out of VCS; ANON_WORKER_TEST_DSN not set). **Its behavior is VERIFIED by the
  Claudian SET-ROLE gate below — NOT verification debt.**

**Wave-2 regression:** `test_rls_isolation.py` 4/4 still green + `test_account_memory_briefing.py` 4/4 (after the
ATTACH-training fixture fix) → 0013 introduced no Wave-2 regression.

### D-16 worker runtime — VERIFIED via Claudian SET-ROLE gate
The worker's cross-tenant read + stamp-persist + column-bound write + no-leak were proven on real PG by
connecting as postgres and `SET ROLE nerve_anon_worker` inside a rollback transaction (faithful: PostgreSQL
evaluates RLS bypass against the CURRENT role after SET ROLE, and nerve_anon_worker is non-superuser /
non-BYPASSRLS / non-owner → RLS truly engages):
- (a) cross-tenant read: SELECT crm.account_memory saw **both** tenants' rows (anon_worker_read).
- (b) stamp: `UPDATE ... SET anonymized_at` succeeded on **2** rows cross-tenant (anon_worker_stamp through FORCE RLS).
- (c) column bound: `UPDATE ... SET meddpicc = ...` → **permission denied** (column-level grant confines the worker
  to anonymized_at even though anon_worker_stamp WITH CHECK is `true`, T-G3-02).
- (d) no leak: nerve_app with app.tenant_id = tenant_A saw only **1** row (its own) — worker policies do not leak to nerve_app.

The persistent real-login regression test (`test_worker_cross_tenant_read_and_stamp_persists`) becomes runnable
as soon as `ANON_WORKER_TEST_DSN` is provided server-side (see §(f)).

---

## (e) W-6 SCOPE BOUNDARY (explicit, NOT a silent reduction)
`training.preference_pairs` is CREATED in Wave 3 (schema + grants ready) but left **EMPTY**. The Wave-3 worker
writes `training.transcript_archive` and stamps `crm.account_memory.anonymized_at` (Variante A). It does NOT
populate `preference_pairs`: the prompt/chosen/rejected DPO-triple mapping is owned by **Phase 08.23.2.E** (the
DPO consumer) and is not yet specified; emitting triples now would produce structurally-valid-but-empty pairs
(a silent D-15 reduction). Phase E can populate it without a new migration (INSERT grant already in place).

## (f) Operational open item (NOT verification debt)
`ANON_WORKER_DATABASE_URL` (a nerve_anon_worker password DSN) is NOT yet set in `/etc/nerve/.env`, and the
anonymizer cron is NOT yet scheduled. This is **only needed when the anonymizer actually runs (Phase E timeframe)**,
not now — Wave 3 is independently deployable and the app behaves identically whether or not the cron runs (D-02).
Setting it also flips the skipped `test_worker_cross_tenant_read_and_stamp_persists` to a runnable persistent
regression test (Option 1).

## (g) Deviations
| # | Rule | What |
|---|------|------|
| 1 | Rule 3 (correctness — caught by running the suite on prod) | Logic-group seed used `call_mode='live'` which violates `ck_calls_call_mode` ('cold_call','meeting_consented'). Fixed to 'cold_call' + dropped deprecated `utcnow()`. Commit `d084edf`. |
| 2 | Rule 3 (correctness) | `TranscriptSegment.id` is BigInteger; SQLite only auto-increments INTEGER PRIMARY KEY, so the in-memory seed must supply explicit ids (process-global counter). Commit `e91adbe`. |
| 3 | Rule 3 (correctness) | `--dry-run` counted a segment as archived (counter was outside the dry_run guard) + a logic-test fixture text contained the ART9 'GEHEIM' trigger → 0 rows. Fixed both. Commit `665acee`. |
| 4 | Rule 2 (cross-phase regression) | Adding `PreferencePair` ({'schema':'training'}) to Base.metadata broke the Wave-2 briefing test's in-memory `create_all` (its fixture ATTACHed only `crm`). Fixed by also ATTACHing `training`. Caught by the regression run. Commit `a5a2b60`. |
| — | Test harness | The two-role worker RLS test can't use peer auth for both roles in one process; nerve_anon_worker needs TCP+password (out of VCS). Per André's decision, the worker runtime was proven via the Claudian SET-ROLE gate (§(d)) instead, and the persistent real-login test is deferred to when the cron DSN is configured. |
| — | Process | `gsd-code-review` SKIPPED (multi-segment phase-ID gotcha — gsd-tools misresolves `08.23.2.G-MEET` to the wrong empty dir). Manual + Cross-AI-informed review stands in (consistent with Wave 1/2). |

## Known Stubs
None. preference_pairs is intentionally created-not-populated (Phase E owns it — documented W-6 boundary, not a stub).
The worker is wired to real anonymization + real crm/training reads/writes.

## Self-Check
- Files: alembic/versions/0013_preference_pairs_anonymizer_grants.py, scripts/anonymizer_worker.py,
  tests/test_anonymizer_worker.py — all FOUND on disk and on prod (`/opt/nerve/app`).
- Migration head on prod = 0013 (verified). preference_pairs FK count = 0. account_memory has exactly 3 policies.
- Commits 46060a5, 31386ff, 7aa6fae, d084edf, e91adbe, 665acee, a5a2b60 — all FOUND in git log + pushed to origin.

## Self-Check: PASSED
