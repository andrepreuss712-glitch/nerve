---
phase: 08.23.2.G-MEET
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - alembic/versions/0013_preference_pairs_anonymizer_grants.py
  - database/models.py
  - scripts/anonymizer_worker.py
  - tests/test_anonymizer_worker.py
autonomous: true
requirements: [D-13, D-15, D-16, D-17, D-18, D-19]
must_haves:
  truths:
    - "training.preference_pairs exists (TRL-compatible: prompt/chosen/rejected JSONB, schema_version, no FK to crm) — CREATED in Wave 3, POPULATED by Phase 08.23.2.E (the DPO consumer)."
    - "The existing training schema / nerve_anon_worker role / transcript_archive are reused, NOT recreated (PROVEN by pre-execute audit 2026-06-01: training schema EXISTS, transcript_archive EXISTS, nerve_anon_worker EXISTS, preference_pairs MISSING)."
    - "The anonymizer worker reads crm rows WHERE anonymized_at IS NULL, writes anonymized transcript material to training.transcript_archive, stamps anonymized_at — no crm IDs cross the wall."
    - "The worker filters is_test_user calls (no test data into the training foundation)."
    - "The worker connects as nerve_anon_worker (the only role seeing both walls); no crm<->training FK exists."
    - "nerve_anon_worker can SEE unstamped crm.account_memory rows CROSS-TENANT and its anonymized_at stamp PERSISTS (role-targeted PERMISSIVE policies in 0013); a second worker run no longer selects those rows (idempotent — stamp took). nerve_app stays tenant-scoped after these policies (no leak regression)."
  artifacts:
    - path: "alembic/versions/0013_preference_pairs_anonymizer_grants.py"
      provides: "training.preference_pairs (DPO foundation, populated in Phase E) + worker grants (crm read + column UPDATE) + worker-targeted PERMISSIVE RLS policies on crm.account_memory (read + stamp cross-tenant)"
      contains: "CREATE TABLE IF NOT EXISTS training.preference_pairs"
    - path: "scripts/anonymizer_worker.py"
      provides: "standalone cron worker (Variante A state-tracking) — populates transcript_archive, stamps anonymized_at"
      contains: "anonymized_at"
    - path: "tests/test_anonymizer_worker.py"
      provides: "Integration-Assertion test (archive write + stamp + is_test_user filter + cross-tenant-read/stamp-persist/idempotency on real PG)"
      contains: "def test_"
  key_links:
    - from: "scripts/anonymizer_worker.py"
      to: "training.transcript_archive"
      via: "INSERT anonymized transcript material as nerve_anon_worker"
      pattern: "transcript_archive"
    - from: "scripts/anonymizer_worker.py"
      to: "crm.account_memory.anonymized_at"
      via: "UPDATE stamp after processing (Variante A)"
      pattern: "anonymized_at"
    - from: "migration 0013 worker policies"
      to: "crm.account_memory (cross-tenant read + stamp for nerve_anon_worker)"
      via: "CREATE POLICY anon_worker_read/anon_worker_stamp TO nerve_anon_worker USING(true)/WITH CHECK(true)"
      pattern: "anon_worker_(read|stamp)"
---

<objective>
Wave 3 — Training-Schema completion + Anonymizer-Worker. ADD `training.preference_pairs` (the DPO foundation table for Phase 08.23.2.E) to the ALREADY-EXISTING `training` schema, extend the EXISTING `nerve_anon_worker` grants for crm-read + column-level UPDATE, add the worker-targeted PERMISSIVE RLS policies that let nerve_anon_worker read crm cross-tenant AND persist its anonymized_at stamp (D-15/D-16), and build the standalone anonymizer cron worker that moves cleartext crm data across the wall into anonymized training material (Variante A state-tracking).

Purpose: Completes the DSGVO crm/training separation — the wall that legally carries the "echte Anonymisierung" claim. The worker is the only component seeing both walls (D-16). It is a separate cron: if not yet scheduled, nothing in the app breaks — making Wave 3 independently deployable (D-02).

⚠ SCOPE BOUNDARY — preference_pairs is CREATED here, POPULATED in Phase E (W-6, explicit, NOT a silent reduction): `training.preference_pairs` is the DPO-triple sink (prompt/chosen/rejected). The mapping from a `crm.account_memory` row to a meaningful DPO triple is a DPO-pipeline concern owned by Phase 08.23.2.E (the actual DPO consumer), and that mapping is NOT yet specified — populating it now would risk structurally-valid-but-empty pairs (a silent D-15 reduction). Therefore Wave 3 adopts **Variante A scoped to transcript material**: the worker establishes the pipeline by populating `training.transcript_archive` (the existing raw anonymized-segment sink) and stamping `crm.account_memory.anonymized_at`. `training.preference_pairs` is created (schema + grants ready) but left EMPTY for Phase E to populate once the DPO triple mapping is defined. This boundary is explicit and intentional; it is recorded in the SUMMARY so it is not mistaken for a missed feature.

⚠ PROVEN (pre-execute audit, psql as postgres 2026-06-01 — RESOLVED, no longer an assumption): the `training` schema, `nerve_anon_worker` role, and `training.transcript_archive` ALREADY EXIST; `training.preference_pairs` is MISSING. Wave 3 EXTENDS — it MUST NOT `CREATE SCHEMA training`, MUST NOT `CREATE ROLE nerve_anon_worker`, MUST NOT recreate transcript_archive. `CREATE SCHEMA IF NOT EXISTS` is used ONLY for `crm` (in Plan 02), NEVER for training. Only `CREATE TABLE training.preference_pairs` (with IF NOT EXISTS) is new. Task 1 opens with a HARD GATE that re-verifies these facts (plus the Wave 2 crm state) against Production before ANY action — SUMMARY = intention, Production = truth, mismatch → HARD STOP.

Output: migration 0013 (preference_pairs + worker grants + worker-targeted crm RLS policies), PreferencePair ORM model, anonymizer worker script (populates transcript_archive + stamps anonymized_at), worker Integration-Assertion test (incl. cross-tenant read + stamp-persist + idempotency + nerve_app no-leak on real PG).

Komplexitaet: 🔴 (DSGVO + DPO foundation). Pre-execute audit (Claudian + Cross-AI Gemini, 2026-06-01) COMPLETE — training/role/transcript_archive existence proven, no crm<->training FK confirmed. Cross-AI is SATISFIED; next step is EXECUTE, not another review round.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-CONTEXT.md
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-RESEARCH.md
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-PATTERNS.md
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-01-SUMMARY.md
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-02-SUMMARY.md
@database/models.py
@alembic/versions/0008_training_schema_foundation.py
@alembic/versions/0009_grant_anon_worker_training_sequences.py
@CLAUDE.md

<interfaces>
<!-- PROVEN (pre-execute audit, 01-SUMMARY): training schema + transcript_archive + nerve_anon_worker EXIST; preference_pairs MISSING. Re-verified by the Task 1 STEP 0 HARD GATE before any action (mismatch → HARD STOP). -->

Existing training schema (migration 0008) — DO NOT recreate:
```
training.transcript_archive (BIGSERIAL PK, source_call_hash TEXT, segment_index INT,
    speaker CHECK, text, ts_offset_ms, schema_version SMALLINT, archived_at)
nerve_anon_worker: USAGE on training; INSERT,SELECT on transcript_archive;
    SELECT on public.calls/users/conversation_logs/transcript_segments;
    USAGE,SELECT on training sequences (+ ALTER DEFAULT PRIVILEGES covers future training seqs).
    (rolsuper=f, rolbypassrls=f — proven 2026-06-01.)
```

crm side (Wave 2 / 0012): crm.account_memory has `anonymized_at TIMESTAMPTZ` NULL (Variante A);
nerve_anon_worker already has GRANT SELECT on crm.* TABLES (from 0012 ALTER DEFAULT PRIVILEGES).
crm RLS (Wave 2 / 0012): each crm table has ENABLE + FORCE ROW LEVEL SECURITY and a PERMISSIVE
`tenant_isolation` policy with NO `TO` clause and NO `FOR` clause (=> applies to PUBLIC incl. the
worker, governs SELECT and UPDATE/WITH CHECK), predicate
`tenant_id = current_setting('app.tenant_id', true)::uuid`.
KEY CONSEQUENCE for the worker (non-owner, rolbypassrls=f proven, sets NO app.tenant_id):
predicate -> NULL -> FALSE for every row => SELECT sees 0 rows AND the anonymized_at stamp UPDATE
is blocked (USING/WITH CHECK). The crm SELECT grant alone is NOT sufficient against FORCE RLS.
=> 0013 MUST add worker-targeted PERMISSIVE policies (`TO nerve_anon_worker USING(true)`) so the
worker row set becomes `tenant_isolation(FALSE) OR anon_worker_*(TRUE)` = all rows cross-tenant
(by design, D-16). PERMISSIVE policies combine with OR. nerve_app is NOT in the `TO` list, so its
access stays tenant-scoped (no leak).

users.is_test_user (migration 0008, BOOLEAN NOT NULL DEFAULT false) — anonymizer MUST filter these out.

Worker structure analog: scripts/delete_pretest_data.py (standalone, load_dotenv, DATABASE_URL, --dry-run, [TAG] logging).
Anonymization logic: services/anonymization.py — anonymize(text, cache) (line 403), should_persist() (512), AnrufAnonymisierer cache (101).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0013 — training.preference_pairs + worker grant extension + worker-targeted crm RLS policies (EXTEND, never recreate)</name>
  <read_first>
    - 01-SUMMARY proven constants (training schema/role/transcript_archive EXIST, preference_pairs MISSING; PG=16.14 gen_random_uuid core) — re-verified by this task's STEP 0 HARD GATE before any action (mismatch → HARD STOP)
    - 02-SUMMARY + Plan 02 Task 1 (0012): the crm `tenant_isolation` policy shape (PERMISSIVE, no TO, no FOR, USING + WITH CHECK on current_setting('app.tenant_id',true)::uuid) on crm.account_memory — the worker policies in 0013 stack OR on top of it
    - alembic/versions/0008_training_schema_foundation.py (CREATE TABLE training.* + GRANT pattern)
    - alembic/versions/0009_grant_anon_worker_training_sequences.py (ALTER DEFAULT PRIVILEGES SEQUENCES — already covers future training seqs)
    - PATTERNS.md §"alembic/versions/0013_*.py" (lines 106-123 — verbatim preference_pairs DDL + grant + state-tracking)
    - RESEARCH §7 preference_pairs sketch (lines 351-369 verbatim) + §4 Variante A (lines 302-306) + Discrepancy Reconcile (lines 232-244)
  </read_first>
  <behavior>
    - test_preference_pairs_created: table exists with prompt/chosen/rejected JSONB NOT NULL, schema_version SMALLINT, split CHECK, NO FK to crm.
    - test_no_crm_fk: preference_pairs has no foreign key referencing any crm.* table (D-17) — uses source_call_hash TEXT instead.
    - test_worker_has_pref_grants: nerve_anon_worker can INSERT+SELECT preference_pairs and UPDATE(anonymized_at) on crm.account_memory but cannot UPDATE other crm columns.
    - test_worker_policies_present: crm.account_memory has TWO additional PERMISSIVE policies `anon_worker_read` (FOR SELECT TO nerve_anon_worker) and `anon_worker_stamp` (FOR UPDATE TO nerve_anon_worker), both with USING true (+ WITH CHECK true on the UPDATE policy), alongside the unchanged `tenant_isolation` policy.
  </behavior>
  <action>
    **🔴 STEP 0 — WAVE 2 PRODUCTION RE-VERIFICATION (HARD GATE — runs BEFORE any code/migration action).**
    This task starts across a `/clear` boundary from Wave 2. **SUMMARY = the expected intention. Production = the only truth. On ANY mismatch: HARD STOP — make NO code change, do NOT trust the markdown, report the discrepancy.** Verify the ACTUAL deployed Wave 2 state as postgres on Production (inspect.sh connects as nerve_app and would give privilege false-negatives — use direct `sudo -u postgres psql`):
    ```
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "\d crm.account_memory"'
    #   expect: columns anonymized_at TIMESTAMPTZ, meddpicc jsonb, context_hooks jsonb present;
    #           footer shows "Row security: enabled, forced"; and POLICY "tenant_isolation" with USING + WITH CHECK,
    #           and NO "FOR" / NO "TO" clause (PERMISSIVE, applies to PUBLIC) — this is EXACTLY the shape 0013 stacks anon_worker_* on.
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT relname,relrowsecurity,relforcerowsecurity FROM pg_class WHERE relnamespace='\''crm'\''::regnamespace ORDER BY relname"'
    #   expect: accounts, contacts, account_memory, meetings ALL show relrowsecurity=t AND relforcerowsecurity=t
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "SELECT version_num FROM alembic_version"'
    #   expect: 0012  (Wave 2 is head; Wave 3 has NOT run yet)
    ```
    Then re-verify the training facts that 0013 EXTENDS (must NOT recreate) — same hard-gate semantics:
    ```
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "\dn training"'        # expect training schema present
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "\dt training.*"'       # expect transcript_archive PRESENT, preference_pairs ABSENT
    ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'sudo -u postgres psql -d nerve -c "\dg nerve_anon_worker"' # expect role present
    ```
    Record the verbatim output in 03-SUMMARY. Proceed to create 0013 ONLY if EVERY check matches. Any deviation — especially preference_pairs ALREADY present, the 0012 `tenant_isolation` policy shape differs from what 0013 stacks on, FORCE not set on any crm table, or alembic head != 0012 — → HARD STOP, report the discrepancy, do NOT create the migration.

    Create `alembic/versions/0013_preference_pairs_anonymizer_grants.py`, `revision='0013'`, `down_revision='0012'`. **MIGRATION ORDERING (explicit):** 0013 chains AFTER 0012 (`down_revision='0012'`), so by the time 0013 runs, `crm.account_memory` AND its base `tenant_isolation` policy (created in 0012) already exist — the worker policies added here stack on top of that existing table+policy. The worker policies live in 0013 (not 0012) because they are a WORKER concern and pair with the worker grant in this same migration; keeping them next to `GRANT UPDATE (anonymized_at)` makes the worker's full crm-access surface auditable in one place. Runs as postgres. All identifiers ASCII. Do NOT `CREATE SCHEMA training` / `CREATE ROLE` / recreate transcript_archive — they EXIST (proven). The ONLY `CREATE SCHEMA IF NOT EXISTS` in this whole phase is for `crm` in Plan 02; this migration touches training purely by EXTENDING it, and touches crm only by ADDING worker policies + the column UPDATE grant.

    **preference_pairs table** (RESEARCH §7 verbatim, lines 351-367 — UUID PK so NO sequence; NO FK to crm per D-17; schema_version per D-19). NOTE: this table is CREATED here but POPULATED by Phase 08.23.2.E (W-6 boundary — see objective). `gen_random_uuid()` is core/built-in on PG 16.14 (proven Plan 01 Task 0/A3 — no extension, no version branch):
    ```python
    op.execute("""
        CREATE TABLE IF NOT EXISTS training.preference_pairs (
            pair_id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),   -- gen_random_uuid: core on PG 16.14 (proven Plan 01 Task 0/A3)
            prompt             JSONB NOT NULL,
            chosen             JSONB NOT NULL,
            rejected           JSONB NOT NULL,
            batch_id           UUID NOT NULL,
            anonymizer_version TEXT NOT NULL,
            source_call_hash   TEXT,                 -- HASH not call_id (no FK across the wall, D-17)
            labeller           TEXT,
            rating_chosen      SMALLINT,
            rating_rejected    SMALLINT,
            rationale          TEXT,
            split              TEXT CHECK (split IN ('train','val','test')),
            schema_version     SMALLINT NOT NULL DEFAULT 1,
            created_at         TIMESTAMPTZ DEFAULT now()
        )
    """)
    op.execute("CREATE INDEX IF NOT EXISTS idx_preference_pairs_batch ON training.preference_pairs(batch_id)")
    ```
    (UUID PK => no sequence grant needed, but 0009's ALTER DEFAULT PRIVILEGES already covers any future training sequence.)

    **Grant extension** (EXTEND existing nerve_anon_worker — write training, read+stamp crm; Variante A, RESEARCH §4). Mirror the existing 0008/0009 grant idiom (`ALTER DEFAULT PRIVILEGES IN SCHEMA training ... TO nerve_anon_worker` for future-table coverage where applicable):
    ```python
    op.execute("GRANT INSERT, SELECT ON training.preference_pairs TO nerve_anon_worker")
    # crm read already granted in 0012 (ALTER DEFAULT PRIVILEGES SELECT). Add the column-level UPDATE for the stamp:
    op.execute("GRANT UPDATE (anonymized_at) ON crm.account_memory TO nerve_anon_worker")
    ```
    The preference_pairs INSERT grant is provisioned NOW so Phase E needs no further grant migration; Wave 3's worker does not itself INSERT preference_pairs (it writes transcript_archive — see Task 3). Column-level UPDATE keeps the wall minimal — the worker can ONLY stamp anonymized_at, not mutate cleartext. NO FK crm<->training (D-17). NO crm IDs stored in training (source_call_hash is a hash, not an FK).

    **Worker-targeted crm RLS policies (PIN the standard-Postgres resolution of D-15/D-16 — NOT deferred to the executor).** The 0012 `tenant_isolation` policy is PERMISSIVE with NO `TO`/`FOR` clause, so it applies to nerve_anon_worker too; the worker sets no app.tenant_id, so its predicate is NULL -> FALSE -> it would SELECT 0 unstamped rows AND its anonymized_at-stamp UPDATE would be blocked by USING/WITH CHECK (it would stamp 0 rows — re-processing the same rows every cron run forever). D-16 makes nerve_anon_worker the ONE role that reads crm CROSS-TENANT to anonymize and stamps anonymized_at. Add TWO role-targeted PERMISSIVE policies on crm.account_memory so the worker's effective row set becomes `tenant_isolation(FALSE) OR anon_worker_*(TRUE)` = all rows cross-tenant (PERMISSIVE policies combine with OR):
    ```python
    op.execute("""
        CREATE POLICY anon_worker_read ON crm.account_memory
          FOR SELECT TO nerve_anon_worker USING (true)
    """)
    op.execute("""
        CREATE POLICY anon_worker_stamp ON crm.account_memory
          FOR UPDATE TO nerve_anon_worker USING (true) WITH CHECK (true)
    """)
    ```
    Scope of these policies (intentional, bounded):
      - They are `TO nerve_anon_worker` ONLY. `nerve_app` is NOT in the `TO` list, so nerve_app still sees only its own tenant's rows via the unchanged `tenant_isolation` policy — NO leak to the app role. FORCE RLS stays on for everyone.
      - `anon_worker_stamp`'s `WITH CHECK (true)` only lifts the RLS ROW-predicate; it does NOT broaden the column surface. The worker's column-level `GRANT UPDATE (anonymized_at)` (above) still confines every worker UPDATE to the single `anonymized_at` column — the worker physically cannot mutate meddpicc/context_hooks/cleartext. (Two independent controls: the column grant bounds WHICH columns; the policy bounds WHICH rows.)
      - The base `tenant_isolation` policy is NOT modified, dropped, or re-created — it stays exactly as 0012 shipped it.
    Worker crm-table scope (CONFIRMED): the Wave-3 worker's ONLY crm access is `crm.account_memory` — it SELECTs unstamped rows there and UPDATEs `anonymized_at` there (Task 3 loop). It does NOT SELECT crm.accounts/crm.contacts: the anonymized payload is built from PUBLIC-schema transcript material (public.calls / public.transcript_segments / public.conversation_logs, the existing 0008 worker grants), NOT from crm account/contact rows. Therefore the worker policies are pinned ONLY on `crm.account_memory`; no `FOR SELECT TO nerve_anon_worker` policy is needed on crm.accounts/contacts (the worker never reads them). If a future wave makes the worker join crm.accounts/contacts to build the payload, ADD a `CREATE POLICY ... FOR SELECT TO nerve_anon_worker USING (true)` on each such table then — out of scope now.

    **downgrade()** (symmetric, reverse): DROP POLICY anon_worker_stamp ON crm.account_memory; DROP POLICY anon_worker_read ON crm.account_memory; REVOKE UPDATE(anonymized_at) ON crm.account_memory; REVOKE INSERT,SELECT ON training.preference_pairs; DROP INDEX; DROP TABLE IF EXISTS training.preference_pairs. Do NOT drop the schema/role/transcript_archive (they predate this migration) and do NOT touch the 0012 `tenant_isolation` policy.
  </action>
  <verify>
    <automated>bash deploy.sh production && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/python -m pytest tests/test_anonymizer_worker.py -x'</automated>
    <migrate>sudo -u postgres bash -c 'cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve /opt/nerve/venv/bin/alembic upgrade head' (head shows 0013)</migrate>
    <live>sudo -u postgres psql -d nerve -c "\d training.preference_pairs" (shows prompt/chosen/rejected jsonb NOT NULL, no FK to crm); sudo -u postgres psql -d nerve -c "SELECT count(*) FROM information_schema.table_constraints WHERE table_schema='training' AND table_name='preference_pairs' AND constraint_type='FOREIGN KEY'" (= 0); sudo -u postgres psql -d nerve -c "SELECT polname, polcmd, polroles::regrole[], pg_get_expr(polqual, polrelid) AS using_expr FROM pg_policy JOIN pg_class c ON c.oid=polrelid WHERE c.relname='account_memory' AND c.relnamespace='crm'::regnamespace" (shows tenant_isolation + anon_worker_read + anon_worker_stamp; the two anon_worker_* rows are TO nerve_anon_worker with using_expr = true)</live>
  </verify>
  <acceptance_criteria>
    - `inspect.sh migrations` returns 0013.
    - Migration docstring states the W-6 boundary (preference_pairs created here, populated by Phase E) AND that PG 16.14 makes gen_random_uuid core (no extension/branch) AND that training/role/transcript_archive are PROVEN-existing (EXTEND-only) AND the migration-ordering fact (0013 after 0012 so the crm table + tenant_isolation policy exist before the worker policies stack on them).
    - `\d training.preference_pairs` shows prompt/chosen/rejected JSONB NOT NULL, batch_id UUID NOT NULL, schema_version SMALLINT, split CHECK(train/val/test), UUID PK.
    - ZERO foreign keys on training.preference_pairs (D-17) — verified via information_schema query = 0.
    - nerve_anon_worker has INSERT,SELECT on training.preference_pairs + UPDATE(anonymized_at) ONLY on crm.account_memory (not other columns).
    - crm.account_memory has exactly THREE policies: the unchanged `tenant_isolation` (PERMISSIVE, applies to PUBLIC) PLUS `anon_worker_read` (FOR SELECT, TO nerve_anon_worker, USING true) PLUS `anon_worker_stamp` (FOR UPDATE, TO nerve_anon_worker, USING true + WITH CHECK true) — verified via the pg_policy query above. The two worker policies are TO nerve_anon_worker (NOT to PUBLIC, NOT to nerve_app).
    - Migration did NOT CREATE SCHEMA training / CREATE ROLE / recreate transcript_archive (grep the migration: no `CREATE SCHEMA training`, no `CREATE ROLE`) and did NOT alter/drop the 0012 `tenant_isolation` policy.
    - STEP 0 hard gate ran and PASSED before any migration code: the verbatim Wave 2 Production re-verification output (crm.account_memory columns + "Row security: enabled, forced" + tenant_isolation policy shape, all 4 crm tables relrowsecurity=t AND relforcerowsecurity=t, alembic head==0012, training schema + transcript_archive present + preference_pairs ABSENT + nerve_anon_worker present) is recorded in 03-SUMMARY. On any mismatch the task HARD-STOPPED instead of proceeding.
  </acceptance_criteria>
  <done>training.preference_pairs added (DPO foundation, created-not-populated, Phase E owns population); worker grants extended (training write + crm column-stamp); worker-targeted PERMISSIVE policies (anon_worker_read FOR SELECT + anon_worker_stamp FOR UPDATE, both TO nerve_anon_worker USING/CHECK true) added on crm.account_memory so the worker reads cross-tenant AND its stamp persists, while nerve_app stays tenant-scoped; no crm/training FK; existing (proven) objects + the 0012 tenant_isolation policy untouched.</done>
</task>

<task type="auto">
  <name>Task 2: PreferencePair ORM model (DDL/ORM parity, training schema)</name>
  <read_first>
    - database/models.py lines 1-16 + 700-733 (CallEvent/TranscriptSegment — JSONB, schema_version, BIGSERIAL note, text-shadow gotcha at 725-729)
    - Migration 0013 (Task 1) — ORM must match DDL exactly
    - PATTERNS.md §"database/models.py" pattern notes (lines 127-155)
  </read_first>
  <action>
    Add a `PreferencePair` model to `database/models.py`, schema-qualified to training (mirror the crm models' explicit `{'schema': 'training'}` table_arg — no search_path reliance):
    ```python
    class PreferencePair(Base):
        __tablename__ = 'preference_pairs'
        pair_id            = Column(UUID_TYPE, primary_key=True, default=uuid.uuid4)
        prompt             = Column(JSON_TYPE, nullable=False)
        chosen             = Column(JSON_TYPE, nullable=False)
        rejected           = Column(JSON_TYPE, nullable=False)
        batch_id           = Column(UUID_TYPE, nullable=False)
        anonymizer_version = Column(Text, nullable=False)
        source_call_hash   = Column(Text, nullable=True)   # hash, NOT call_id (D-17, no FK)
        labeller           = Column(Text, nullable=True)
        rating_chosen      = Column(SmallInteger, nullable=True)
        rating_rejected    = Column(SmallInteger, nullable=True)
        rationale          = Column(Text, nullable=True)
        split              = Column(Text, nullable=True)
        schema_version     = Column(SmallInteger, nullable=False, server_default='1')
        created_at         = Column(DateTime(timezone=True), server_default=func.now())
        __table_args__ = (
            CheckConstraint("split IN ('train','val','test') OR split IS NULL", name='ck_preference_pairs_split'),
            Index('idx_preference_pairs_batch', 'batch_id'),
            {'schema': 'training'},   # MUST be last element
        )
    ```
    Explicitly NO ForeignKey to any crm table (D-17). ASCII identifiers. DDL-parity with 0013.
  </action>
  <verify>
    <automated>bash deploy.sh production && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/python -m pytest tests/test_anonymizer_worker.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class PreferencePair" database/models.py` returns the class.
    - `{'schema': 'training'}` is the last __table_args__ element.
    - No `ForeignKey(` referencing crm.* anywhere in the model (D-17).
    - Column names/types match 0013 DDL 1:1; in-memory test schema builds preference_pairs from it.
  </acceptance_criteria>
  <done>PreferencePair model exists, training-schema-qualified, DDL-parity with 0013, no crm FK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Anonymizer worker (standalone cron, Variante A) + Integration-Assertion test</name>
  <read_first>
    - scripts/delete_pretest_data.py:1-44 (standalone script structure: docstring, load_dotenv, DATABASE_URL, --dry-run, backup awareness, [TAG] logging)
    - services/anonymization.py — anonymize(text, cache) (line 403), should_persist() (512), AnrufAnonymisierer (101)
    - CLAUDE.md "Pre-Launch Sandbox-Pattern" (is_test_user filter mandate) + "Test-Qualitaets-Regel" (Integration-Assertion)
    - PATTERNS.md §"Anonymizer worker" (lines 188-197 — role, Variante A flow, is_test_user filter, [ANON] logging, own connection as nerve_anon_worker)
    - RESEARCH §4 Variante A (lines 302-306) + Discrepancy Reconcile is_test_user row (line 242)
    - Migration 0013 grants + worker policies (Task 1) + crm.account_memory.anonymized_at (Wave 2)
  </read_first>
  <behavior>
    - test_worker_processes_unstamped: given a crm.account_memory row with anonymized_at IS NULL whose source call has transcript material, the worker writes anonymized rows to training.transcript_archive and sets anonymized_at to a timestamp.
    - test_worker_skips_stamped: a row with anonymized_at already set is NOT reprocessed (idempotent).
    - test_worker_filters_test_user: a row whose source call belongs to a user with is_test_user=true is NOT written to training (CLAUDE.md mandate).
    - test_no_crm_id_in_training: the written transcript_archive rows store source_call_hash (a hash), never a raw crm id (no re-identification, D-17).
    - test_worker_sees_cross_tenant_and_stamp_persists (REAL PG, as nerve_anon_worker): seed unstamped crm.account_memory rows under TWO different tenant_ids; connect as nerve_anon_worker (sets NO app.tenant_id); assert the worker SELECTs BOTH tenants' unstamped rows (cross-tenant, anon_worker_read), runs, and the anonymized_at stamp PERSISTS on both (anon_worker_stamp). Then run the worker a SECOND time and assert those rows are NO LONGER selected (idempotency — proves the stamp took; a SELECT-only fix that never persisted would re-select them).
    - test_nerve_app_still_tenant_scoped (REAL PG, as nerve_app): after the 0013 worker policies exist, connect as nerve_app inside a transaction with app.tenant_id = tenant_A and assert it sees ONLY tenant_A's account_memory rows (the worker policies, being TO nerve_anon_worker, do NOT leak cross-tenant rows to nerve_app — no regression of Wave 2's isolation).
  </behavior>
  <action>
    Create `scripts/anonymizer_worker.py` as a standalone cron (analog: scripts/delete_pretest_data.py). It is the ONLY component connecting as DB-role `nerve_anon_worker` (D-16 — the single role seeing both walls).

    ⚠ V1 SCOPE BOUNDARY (W-6 — explicit, NOT a silent reduction): Wave 3's worker implements **Variante A scoped to transcript material** — it populates `training.transcript_archive` (the existing raw anonymized-segment sink) and stamps `crm.account_memory.anonymized_at`. It does NOT populate `training.preference_pairs`. Reason: the prompt/chosen/rejected DPO-triple mapping is owned by Phase 08.23.2.E (the DPO consumer) and is not yet specified; emitting triples now would produce structurally-valid-but-empty pairs (a silent D-15 reduction). preference_pairs exists (created in Task 1) with the worker's INSERT grant already provisioned, so Phase E can populate it without a new migration. Record this boundary in the SUMMARY.

    Structure:
    - Docstring header (purpose, run-as nerve_anon_worker, Variante A state-tracking, the W-6 scope boundary — transcript_archive now, preference_pairs in Phase E — and the DSGVO note).
    - `load_dotenv()`; read a dedicated `ANON_WORKER_DATABASE_URL` (connects as nerve_anon_worker, NOT the app's nerve_app engine — distinct role). Do NOT reuse database/db.py's engine. Document the env var in `.env.example` (name only, no secret — CLAUDE.md secrets rule).
    - `--dry-run` flag (mirror delete_pretest_data.py).
    - Main loop (Variante A, RESEARCH §4):
      1. SELECT crm.account_memory (+ join to source call/user to evaluate is_test_user) WHERE `anonymized_at IS NULL`.
      2. FILTER OUT rows whose source call belongs to a user with `is_test_user = true` (CLAUDE.md — no test data into the training foundation).
      3. For each remaining row: anonymize the associated transcript/free text using `services/anonymization.py` (`anonymize()` for the text; `should_persist()` gating). Compute `source_call_hash` = a one-way hash of the call id (NOT the raw id — no re-identification surface, D-17).
      4. INSERT the anonymized transcript segments into `training.transcript_archive` (source_call_hash, segment_index, speaker, text, ts_offset_ms, schema_version) — reuse the existing 0008 grants. (preference_pairs is intentionally NOT written in Wave 3 — Phase E owns that, see boundary above.)
      5. `UPDATE crm.account_memory SET anonymized_at = now()` for processed rows (the column-level grant from 0013 is the only crm write the worker can do; the anon_worker_stamp policy from 0013 is what lets that UPDATE pass FORCE RLS).
    - Per-record error handling: log `[ANON]` + continue batch (CLAUDE.md error-swallow for non-critical; never abort the whole run on one bad record).
    - `[ANON]`-prefixed logging throughout.
    - DOCSTRING on RLS (PINNED — no longer "executor to resolve empirically"): nerve_anon_worker reads/stamps crm under FORCE RLS as a non-owner without BYPASSRLS (rolbypassrls=f, proven) and sets NO app.tenant_id. The crm `tenant_isolation` policy (0012) would therefore evaluate to FALSE for the worker (NULL predicate) and hide every row AND block the anonymized_at stamp. This is RESOLVED in migration 0013 by two role-targeted PERMISSIVE policies on crm.account_memory: `anon_worker_read` (FOR SELECT TO nerve_anon_worker USING true) and `anon_worker_stamp` (FOR UPDATE TO nerve_anon_worker USING/CHECK true). PERMISSIVE policies combine with OR, so the worker's row set becomes all rows cross-tenant (D-16, by design — it is the sole role seeing both walls). nerve_app is NOT in the policies' `TO` list, so app reads stay tenant-scoped. The worker therefore does NOT set app.tenant_id and relies on these policies. State in the docstring that the column-level GRANT UPDATE (anonymized_at) still confines the worker's write to one column even though anon_worker_stamp's WITH CHECK is `true`.

    Then `tests/test_anonymizer_worker.py` — Integration-Assertion (CLAUDE.md). Two test groups:
    1. **Logic group (may use in-memory schema since it tests merge/filter/hash logic, not RLS):** seed crm.account_memory rows (one normal with source transcript material, one whose user is_test_user, one already-stamped), invoke the worker's processing function (monkeypatch the anonymize() I/O if it calls external APIs), and ASSERT on actual training.transcript_archive rows written + anonymized_at stamps set + the test-user row absent + source_call_hash is a hash. NO source-presence assertions. (Note in the test docstring: RLS does not apply on SQLite — the RLS-dependent behaviors MUST be proven on real PG, group 2.)
    2. **RLS group (REAL PG, NO SQLite fallback — same harness style as Plan 02 Task 4):** connect as `nerve_anon_worker` against the real Production nerve DB (NOT postgres, NOT SQLite). Seed unstamped crm.account_memory rows under two different tenant_ids (use test-tenant rows tagged is_test_user/tag='test' lineage so anonymizer/analytics already exclude them — clean up in teardown). Assert: (a) the worker (no app.tenant_id) SELECTs BOTH tenants' unstamped rows (cross-tenant read via anon_worker_read); (b) after the worker stamps, `anonymized_at` PERSISTS on both rows (anon_worker_stamp let the UPDATE through FORCE RLS); (c) a SECOND worker run selects NONE of them (idempotency — the stamp took). Then connect as `nerve_app` inside a transaction with set_config('app.tenant_id', tenant_A, true) and assert nerve_app sees ONLY tenant_A's rows (the worker policies did not leak cross-tenant rows to the app role — no Wave-2 isolation regression). If conftest cannot provide a real-PG nerve_anon_worker / nerve_app connection server-side, FIRST make conftest expose those fixtures (a dedicated worker DSN + the existing nerve_app DSN); do NOT degrade group 2 into a SQLite branch — if it cannot be arranged autonomously, flip this task to a checkpoint (autonomous:false) and surface what conftest needs. Document the harness (real PG, two roles, no SQLite for group 2, why) in the test-module docstring.
  </action>
  <verify>
    <automated>bash deploy.sh production && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && /opt/nerve/venv/bin/python -m pytest tests/test_anonymizer_worker.py -x -v'</automated>
    <live>ssh ... 'cd /opt/nerve/app && /opt/nerve/venv/bin/python scripts/anonymizer_worker.py --dry-run' prints [ANON] candidate count without writing; a real run then shows training.transcript_archive row count increase via sudo -u postgres psql -d nerve -c "SELECT count(*) FROM training.transcript_archive"; a second real run shows NO further increase (idempotent — stamp persisted)</live>
  </verify>
  <acceptance_criteria>
    - `scripts/anonymizer_worker.py` exists; connects via a dedicated nerve_anon_worker DATABASE_URL (not database/db.py engine); has --dry-run; uses [ANON] logging.
    - `grep -n "is_test_user" scripts/anonymizer_worker.py` shows the test-user filter.
    - `grep -n "anonymized_at" scripts/anonymizer_worker.py` shows the Variante A stamp UPDATE.
    - `grep -n "transcript_archive" scripts/anonymizer_worker.py` shows the worker writes transcript_archive (NOT preference_pairs — the docstring states preference_pairs is Phase E's job).
    - source_call_hash is a HASH of the call id, never the raw id (grep shows a hashing call, e.g. hashlib).
    - The worker docstring PINS the RLS resolution (the two 0013 anon_worker_* policies + the reason nerve_app stays tenant-scoped) — it does NOT defer the resolution to the executor or describe it as a SELECT-only bypass.
    - `tests/test_anonymizer_worker.py`: logic group (4 behaviors green server-side, runtime assertions) PLUS the RLS group on real PG proving (a) cross-tenant worker SELECT, (b) anonymized_at stamp PERSISTS, (c) second run selects nothing (idempotent), (d) nerve_app still tenant-scoped (no leak). No SQLite fallback for the RLS group.
    - --dry-run does not write (count unchanged); a second real run after a first does not increase the archive count (idempotency proven live).
  </acceptance_criteria>
  <done>Anonymizer worker reads unstamped crm rows CROSS-TENANT (via the 0013 anon_worker_read policy, proven on real PG), filters test-users, writes anonymized training.transcript_archive, stamps anonymized_at AND the stamp PERSISTS (via anon_worker_stamp, proven idempotent on a second run); nerve_app stays tenant-scoped (no leak); no crm IDs cross the wall; preference_pairs left for Phase E (explicit boundary); proven by Integration-Assertion test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymizer worker (nerve_anon_worker role) -> crm (read + anonymized_at stamp only) | the only crossing of the crm side; column-level write; cross-tenant read+stamp via role-targeted 0013 policies (D-16) |
| Anonymizer worker -> training (write) | anonymized output sink |
| crm <-> training | the DSGVO wall — NO FK, NO raw IDs cross (D-17) |
| Worker runtime (separate cron) -> app | none — worker is out-of-process; app does not depend on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-G3-01 | Information Disclosure | Re-identification: raw crm id stored in training | mitigate | preference_pairs + transcript_archive have NO FK to crm (D-17); store `source_call_hash` (one-way hash), never raw id; verified test_no_crm_id_in_training + information_schema FK-count=0. HIGH severity. |
| T-G3-02 | Tampering | Worker mutates crm cleartext beyond the stamp | mitigate | Column-level `GRANT UPDATE (anonymized_at)` only — worker physically cannot UPDATE other crm columns; anon_worker_stamp's WITH CHECK(true) lifts only the RLS row-predicate, NOT the column grant; verified by grant inspection. |
| T-G3-03 | Information Disclosure | Test-user data poisons the training foundation | mitigate | Worker filters `is_test_user` source calls (CLAUDE.md mandate); verified test_worker_filters_test_user. |
| T-G3-04 | Elevation of Privilege | Worker runs as nerve_app (owner-bypass) instead of nerve_anon_worker | mitigate | Worker uses a dedicated nerve_anon_worker DATABASE_URL, NOT the app engine; the role (rolsuper=f, rolbypassrls=f, proven) has only its explicit grants (no ownership of crm/training tables). |
| T-G3-05 | Tampering | Double-processing / re-anonymizing the same row | accept | Variante A `anonymized_at IS NULL` filter + idempotent skip; the anon_worker_stamp policy makes the stamp PERSIST so a second run selects nothing; verified test_worker_skips_stamped + the idempotency assertion in test_worker_sees_cross_tenant_and_stamp_persists. Low risk (cron re-run safe). |
| T-G3-06 | Repudiation | Unverified assumption that training schema exists | mitigate | RESOLVED: pre-execute audit (psql as postgres 2026-06-01) proved training/role/transcript_archive exist; Task 1 STEP 0 HARD GATE re-verifies against Production before extending (mismatch → HARD STOP). |
| T-G3-07 | Information Disclosure / Defect | Worker cannot read+stamp crm under FORCE RLS (the 0012 PERMISSIVE tenant_isolation policy, no TO/FOR, makes the worker's NULL-tenant predicate FALSE -> 0 rows selected AND 0 rows stamped) OR is over-exempted (leaks crm to other roles) | mitigate | PINNED in migration 0013 (NOT deferred to the executor): two role-targeted PERMISSIVE policies on crm.account_memory — `anon_worker_read` (FOR SELECT TO nerve_anon_worker USING true) and `anon_worker_stamp` (FOR UPDATE TO nerve_anon_worker USING/CHECK true). PERMISSIVE policies combine with OR, so the worker's row set = `tenant_isolation(FALSE) OR anon_worker_*(TRUE)` = all rows cross-tenant (D-16, the sole role seeing both walls). The stamp now PERSISTS (no infinite re-processing). Over-exemption is CONTAINED to the single role D-16 already designates: the policies are `TO nerve_anon_worker` only, so `nerve_app` keeps its tenant-scoped access via the unchanged tenant_isolation policy (re-identification surface unchanged from Wave 2); the column-level GRANT UPDATE (anonymized_at) further bounds the worker write to one column. FORCE RLS stays on. Proven on real PG by test_worker_sees_cross_tenant_and_stamp_persists (cross-tenant read + stamp-persist + idempotency) and test_nerve_app_still_tenant_scoped (no leak). HIGH severity. |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `training.preference_pairs` (CREATE only; POPULATED in Phase E) — NEW (Task 1)
- `training.transcript_archive` (worker INSERT) — EXISTS (0008, proven), reuse — the Wave 3 worker sink
- `crm.account_memory` (worker SELECT + UPDATE anonymized_at; + 2 new worker-targeted RLS policies in 0013) — from Wave 2
- `public.calls` / `public.users` (worker read: source call + is_test_user) — EXISTS

### inspect.sh-Beleg

```
$ bash scripts/inspect.sh columns preference_pairs     (as nerve_app, BEFORE)
 (0 rows)   <- privilege-masked (training is worker-side) — proven absent via postgres (audit 2026-06-01)
$ bash scripts/inspect.sh columns transcript_archive   -> (0 rows)  [FALSE-NEGATIVE: exists per 0008/audit, nerve_app has no grant]
```
(Verify as postgres post-deploy — training.* is NOT visible to nerve_app:
`sudo -u postgres psql -d nerve -c "\d training.preference_pairs"`,
`sudo -u postgres psql -d nerve -c "\d training.transcript_archive"`.
Verify the worker policies as postgres:
`sudo -u postgres psql -d nerve -c "SELECT polname, polcmd, polroles::regrole[] FROM pg_policy JOIN pg_class c ON c.oid=polrelid WHERE c.relname='account_memory' AND c.relnamespace='crm'::regnamespace"` shows tenant_isolation + anon_worker_read + anon_worker_stamp.)

```
$ bash scripts/inspect.sh schema calls
 user_id | integer | not null    <- worker joins calls->users to read is_test_user
```
users.is_test_user confirmed added in migration 0008 (RESEARCH Discrepancy Reconcile line 242).

### Cross-Layer-Konsistenz-Tabelle

| Code-Variable / Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `preference_pairs.prompt/chosen/rejected` | NOT written in Wave 3 (Phase E populates) | DB columns training.preference_pairs (JSONB NOT NULL) | created 0013; ORM-parity Task 2; postgres `\d` confirms table exists empty |
| `transcript_archive.source_call_hash` | worker INSERT (hash) | DB column training.transcript_archive.source_call_hash (TEXT) — NOT an FK | exists 0008 (proven); worker writes; hash prevents re-identification |
| `account_memory.anonymized_at` | worker SELECT (filter) + UPDATE (Variante A stamp, cross-tenant) | DB column crm.account_memory.anonymized_at (TIMESTAMPTZ) | created Wave 2 (0012); worker has column-level UPDATE grant + anon_worker_stamp policy (0013); stamp-persist proven on real PG (Task 3 RLS group) |
| crm worker RLS (anon_worker_read / anon_worker_stamp) | governs worker SELECT/UPDATE row visibility | pg_policy on crm.account_memory (PERMISSIVE, TO nerve_anon_worker) | created 0013; pg_policy query confirms; cross-tenant read + stamp-persist + nerve_app-no-leak proven on real PG (Task 3) |
| `users.is_test_user` | worker read (filter) | DB column public.users.is_test_user (BOOLEAN NOT NULL DEFAULT false) | ✓ migration 0008 (RESEARCH); real DB column, not RAM |
| `calls.id` -> source_call_hash | worker read + hash | DB column public.calls.id (uuid PK); hashed before storing | ✓ inspect.sh schema calls; hash prevents re-identification |

### Bei Diskrepanz: STOP + Replan

<verification>
- Migration head = 0013; preference_pairs added (no crm FK, created-not-populated); worker grants extended; two worker-targeted PERMISSIVE policies (anon_worker_read + anon_worker_stamp) added on crm.account_memory; existing (proven) training objects + the 0012 tenant_isolation policy untouched.
- Anonymizer worker green (logic group + RLS group); --dry-run safe; writes transcript_archive, stamps anonymized_at, leaves preference_pairs for Phase E.
- Worker connects as nerve_anon_worker; filters is_test_user; Variante A stamp; no raw crm IDs in training; cross-tenant crm read + stamp-persist proven on real PG (idempotent second run); nerve_app stays tenant-scoped (no leak).
- Worker is a separate cron — app unaffected if unscheduled (clean stop-point, D-02).
</verification>

<success_criteria>
- Wave 3 independently deployable: training.preference_pairs created as the DPO foundation for Phase 08.23.2.E (populated there); anonymizer worker operational as a separate cron writing transcript_archive; app behaves identically whether or not the cron runs.
- DSGVO wall complete: cleartext in crm, anonymized in training, no FK / no raw-ID crossing, worker is the sole bridge — and it can actually DO its job (reads cross-tenant, stamps persistently) because the 0013 worker policies pin the standard-Postgres resolution of D-15/D-16 in DDL, not "to be proven empirically".
- nerve_app remains tenant-scoped after the worker policies (no Wave-2 isolation regression — proven on real PG).
- W-6 boundary explicit: preference_pairs population is Phase E's responsibility, documented in the SUMMARY, not a silent reduction.
- Training schema/role/transcript_archive existence is PROVEN (audit), not assumed — Cross-AI satisfied; next step is execute.
</success_criteria>

<output>
After completion, create `.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-03-SUMMARY.md`. Record the postgres runbook output re-confirming training schema/role existence, the final preference_pairs `\d` output, the pg_policy output showing the two worker policies (anon_worker_read + anon_worker_stamp) alongside the unchanged tenant_isolation, the proof that the worker reads cross-tenant + the anonymized_at stamp persists (idempotent second run) + nerve_app stays tenant-scoped, and the explicit W-6 boundary (preference_pairs created here, populated by Phase E; Wave 3 worker writes transcript_archive).
</output>
