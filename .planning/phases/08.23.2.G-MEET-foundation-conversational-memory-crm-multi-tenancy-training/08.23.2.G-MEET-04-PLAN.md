---
phase: 08.23.2.G-MEET
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0014_crm_user_preferences_rls.py
  - database/models.py
  - routes/crm_export.py
  - tests/test_meeting_save_rls.py
autonomous: false   # contains the [BLOCKING] manual prod-migration-as-postgres task
requirements:
  - MEET-REQ-1   # crm.user_preferences table (NEW) + forced RLS, NULLIF policy, owner postgres
  - MEET-REQ-2   # POST write-route -> crm.meetings (tenant_id stamp, resolve-or-create) + prefs GET/SET
  - MEET-REQ-4   # DSGVO server-side: auto_save_meeting DEFAULT false, opt-in honored off-by-default
user_setup: []

revised: 2026-06-02   # --reviews pass, Cross-AI MM-01/MM-04/MM-05/MM-07 (Plan 04 scope)
changelog:
  - "MM-01 (HIGH): tz-aware scheduled_at — backend parst offset-tragende ISO als aware datetime, lehnt offset-lose Eingabe ab (Andre-Decision Option a: Frontend sendet Offset). Real-PG-Instant-Test 10:00+02:00 -> 08:00Z."
  - "MM-04 (MED): except-Bloecke in save_meeting/set_meeting_pref/get_meeting_pref loggen traceback.print_exc()+[CRM-Meeting]-print VOR sanitized 500 (CLAUDE.md Punkt 15 / learning.py:184-185-Pattern)."
  - "MM-05 (MED): UNIQUE(tenant_id,name) auf crm.accounts in SELBER Migration 0014 + _resolve_account ON CONFLICT DO NOTHING -> re-select (atomar gegen Doppel-Submit). Contacts-Uniqueness dokumentiert deferred."
  - "MM-07 (REJECTED, kein Code-Change): One-Line-Note dass prefs-Routen serverseitig auf g.user.id keyen, nie auf Client-Wert."

must_haves:
  truths:
    - "crm.user_preferences exists on prod with FORCED RLS + tenant_isolation NULLIF policy, owner postgres, nerve_app=arwd via default-ACL"
    - "POST /crm/meetings creates a crm.meetings row with tenant_id == g.tenant_id"
    - "POST with a new Firma resolves-or-creates a tenant-stamped crm.accounts row; blank Firma -> account_id NULL, no row"
    - "A freshly-created user_preferences row has auto_save_meeting = False when not specified (DSGVO default-off, server-honored)"
    - "tenant A cannot read or insert tenant B's crm.user_preferences rows (RLS isolation)"
    - "MM-01: An offset-bearing scheduled_at (e.g. 2026-06-03T10:00:00+02:00) persists the correct instant in timestamptz (= 08:00Z); an offset-less value is rejected (400) — no silent TZ-shift"
    - "MM-05: crm.accounts has UNIQUE(tenant_id, name); a double-submit of the same Firma yields exactly one accounts row (no duplicate)"
  artifacts:
    - path: "alembic/versions/0014_crm_user_preferences_rls.py"
      provides: "crm.user_preferences DDL + FORCED RLS + tenant_isolation NULLIF policy + unique + index; PLUS MM-05 UNIQUE(tenant_id,name) on crm.accounts"
      contains: "DEFAULT false"
    - path: "database/models.py"
      provides: "UserPreference ORM model (SQLite test-schema source, Punkt 21)"
      contains: "class UserPreference"
    - path: "routes/crm_export.py"
      provides: "POST /crm/meetings save_meeting (tz-aware parse, MM-04 logging) + GET/POST /crm/preferences auto_save_meeting; _resolve_account ON CONFLICT (MM-05)"
      exports: ["save_meeting", "get_meeting_pref", "set_meeting_pref"]
    - path: "tests/test_meeting_save_rls.py"
      provides: "real-PG RLS isolation + tenant-stamp + resolve-or-create + DSGVO default-off + MM-01 tz-instant + MM-05 no-duplicate tests"
      contains: "nerve_app_pg_conn"
  key_links:
    - from: "routes/crm_export.py save_meeting"
      to: "crm.meetings"
      via: "ORM INSERT with tenant_id=g.tenant_id (WITH CHECK == GUC)"
      pattern: "g\\.tenant_id"
    - from: "routes/crm_export.py save_meeting"
      to: "crm.accounts / crm.contacts"
      via: "resolve-or-create by (tenant_id, name), tenant-stamped, ON CONFLICT (MM-05)"
      pattern: "Account\\(tenant_id="
    - from: "crm.user_preferences policy"
      to: "app.tenant_id GUC"
      via: "nullif(current_setting('app.tenant_id', true), '')::uuid"
      pattern: "nullif\\(current_setting"
---

# 🔴 Plan 04 — Backend: crm.user_preferences + Meeting-Write-Route + RLS-Tests

🔴 **komplex** — DSGVO-Rechtspflicht (Art. 25 Abs. 2 + Art. 6 Abs. 1 f), multi-tenant DATEN-SCHREIBPFAD, neue RLS-Tabelle, neue Migration 0014. Cross-AI-Plan-Review ist PFLICHT vor Execute (CLAUDE.md GSD-Workflow, 🔴). **Cross-AI-Findings MM-01/MM-04/MM-05/MM-07 sind in diesem Plan eingearbeitet (--reviews 2026-06-02).**

<objective>
Liefert die self-contained, eigenständig auf Production deploybare Backend-Einheit für das Meeting-Merk-Feature:
1. Neue Tabelle `crm.user_preferences` (Migration 0014) mit FORCED RLS, exakter live-NULLIF-Policy, owner=postgres. PLUS MM-05: UNIQUE(tenant_id,name) auf `crm.accounts` in derselben Migration.
2. ORM-Modell `UserPreference` (DDL-parity, SQLite-Test-Schema-Quelle, Punkt 21).
3. POST-Write-Route `/crm/meetings` (tenant_id-Stamp aus `g.tenant_id`, **MM-01 tz-aware scheduled_at**, resolve-or-create accounts/contacts mit **MM-05 ON CONFLICT**, call_id-Soft-Link, Fehler-Kontrakt mit **MM-04 Logging**) + GET/SET-Route für `auto_save_meeting`.
4. Real-PG RLS-Isolation- + Tenant-Stamp- + Resolve-or-Create- + DSGVO-default-off- + **MM-01 tz-Instant- + MM-05 no-duplicate**-Tests.

Purpose: Die Frontend-PiP-Form (Plan 05) konsumiert diese live Route. Backend muss zuerst stehen und unabhängig deploybar sein.
Output: Migration 0014, UserPreference-Modell, 3 neue Routen in crm_export.py, tests/test_meeting_save_rls.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-MEETING-MODAL-RESEARCH.md
@.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-UI-SPEC.md

<interfaces>
<!-- Proven contracts the executor uses directly. Do NOT re-explore the codebase. -->

ORM crm-Schema-Pattern (database/models.py:755-825) — KLON-VORLAGE für UserPreference.
`{'schema': 'crm'}` MUSS das LETZTE __table_args__-Element sein. Imports bereits vorhanden
(models.py:2): Boolean, UniqueConstraint, text, func, SmallInteger, Integer, Index, DateTime.
UUID_TYPE / JSON_TYPE sind die projektweiten Alias (vgl. existierende crm-Modelle).

Migration-Klon-Vorlage (alembic/versions/0012_crm_schema_tables_rls.py:138-146) — exakte RLS-Form:
```sql
ALTER TABLE crm.<t> ENABLE ROW LEVEL SECURITY;
ALTER TABLE crm.<t> FORCE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON crm.<t>
  USING (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
  WITH CHECK (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid);
CREATE INDEX IF NOT EXISTS idx_<t>_tenant ON crm.<t>(tenant_id);
```
0012 BEWEIST (Header + pg_default_acl, Research §0.3): KEIN GRANT, KEIN ALTER OWNER nötig —
`ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA crm GRANT ... TO nerve_app` (0012:57-58)
vergibt nerve_app=arwd automatisch an jede neue postgres-erstellte crm-Tabelle.

Tenant-Quelle (app.py:2200-2202): `g.tenant_id = session.get('tenant_id')`, dann
`set_current_tenant(...)` -> database/db.py after_begin -> `set_config('app.tenant_id', tid, true)`
transaction-local. Die Route stampt `tenant_id=g.tenant_id` -> identisch zur GUC -> WITH CHECK passt.

Blueprint (routes/crm_export.py:15): `crm_export_bp = Blueprint('crm_export', __name__, url_prefix='/crm')`.
Bestehende Routen sind read-only CSV (kein POST). @login_required-Import: `from routes.auth import login_required`.
get_session/Account/Contact bereits importiert (crm_export.py:11-13); Meeting/UserPreference ZU ERGÄNZEN.

MM-04 Logging-Pattern (PROJEKT-IST, kein neuer Logger): routes/ nutzt KEINE logging.getLogger —
crm_export.py hat aktuell KEINEN Logger. Das projektweite Route-Error-Pattern ist
`traceback.print_exc()` + `print(f"[Tag] ... Fehler: {e}")` (verbatim wie routes/learning.py:184-185
in api_postcall_analysis). Tag für dieses Modul: `[CRM-Meeting]`. `import traceback` am Modulkopf
ergänzen. Das IST hier das CLAUDE.md-Punkt-15-Äquivalent zu `exc_info=True` (gleiche Diagnose-Information,
projekt-konsistent). KEIN neuer logging.getLogger einführen.

CSRF: Flask-WTF global; PiP-fetch sendet X-CSRFToken via getCsrfToken() (pip-launcher.js:8-11).
Route braucht KEINE csrf.exempt.

Real-PG-Test-Harness (tests/test_rls_isolation.py — KLON-VORLAGE): `nerve_app_pg_conn`-Fixture
(conftest.py:112, DSN aus NERVE_APP_TEST_DSN, SKIP wenn absent — NIE SQLite-Fallback).
`_new_tenant(cur, suffix)` legt org an, liest die trigger-erzeugte tenant_orgs.id zurück.
Pattern: `cur.execute("SELECT set_config('app.tenant_id', %s, true)", (tid,))` vor jeder crm-Query.
</interfaces>
</context>

## 5. Persistenz-Schicht-Verifikation (CLAUDE.md Punkt 21 — PFLICHT)

### Angefasste Tabellen
- `crm.user_preferences` (CREATE + write) — neu, via Migration 0014
- `crm.meetings` (write) — INSERT-Ziel (scheduled_at = timestamptz, MM-01 tz-aware)
- `crm.accounts` (read + write + MM-05 UNIQUE) — resolve-or-create
- `crm.contacts` (read + write) — resolve-or-create
- `public.users` (read) — `g.user.id` (Integer) als soft-link user_id
- `public.tenant_orgs` (read) — FK-Ziel von tenant_id

### Verbindungs-Karte (Punkt 22) — Referenz, NICHT neu hergeleitet
Die bewiesene Verbindungs-Karte IST die Datei
`08.23.2.G-MEET-MEETING-MODAL-RESEARCH.md` §0 (NAMEN/IDENTIFIER §0.1, TABELLEN/SPALTEN §0.2 mit
verbatim `\d`-Output, GRANTS/OWNER §0.3 mit `pg_default_acl`, WER-SCHREIBT/LIEST §0.4, REICHWEITE
§0.5) — jeder Eintrag mit `grep file:line` + live psql-Beleg gegen Production 2026-06-02.
Hier NICHT re-derivieren: die Karte zitieren.

### inspect.sh / psql-Beleg (verbatim aus Research §0.2 — NICHT zusammengefasst)
```
# crm.meetings (\d crm.meetings, prod 2026-06-02):
 id            | uuid        | not null | gen_random_uuid()
 tenant_id     | uuid        | not null |              <- MUSS auf INSERT gestampt werden
 account_id    | uuid        |          |              <- NULLABLE -> blank Firma = NULL
 contact_id    | uuid        |          |              <- NULLABLE -> blank Ansprechpartner = NULL
 call_id       | uuid        |          |              <- soft link public.calls.id, KEIN FK
 scheduled_at  | timestamptz |          |              <- MM-01: timestamptz speichert INSTANT, nicht wall-clock -> Eingabe MUSS tz-aware sein
 notes         | text        |          |
 schema_version| smallint    | not null | 1
 created_at    | timestamptz |          | now()
 Policies (FORCED RLS): tenant_isolation USING/WITH CHECK
   ((tenant_id = (NULLIF(current_setting('app.tenant_id'::text, true), ''::text))::uuid))

# crm.accounts (\d crm.accounts, prod 2026-06-02) — MM-05:
 id        | uuid | not null | gen_random_uuid()
 tenant_id | uuid | not null |
 name      | text |          |              <- MM-05: aktuell KEIN UNIQUE -> Doppel-Submit erzeugt Duplikat. 0014 ergänzt UNIQUE(tenant_id, name).
 Tabelle ist neu / near-empty (Research §0.5) -> UNIQUE sicher nachrüstbar ohne Backfill-Konflikt.

# crm.user_preferences:
$ sudo -u postgres psql -d nerve -c "SELECT to_regclass('crm.user_preferences');"
 to_regclass
-------------
              <- NULL = existiert NICHT. Safe zu CREATE in 0014.

# public.users: integer PK id (nextval users_id_seq), org_id int NOT NULL. INTEGER-keyed, NICHT uuid.
# public.calls: id uuid NOT NULL (app-supplied), -> meetings.call_id uuid ist typ-kompatibel.

$ sudo -u postgres psql -d nerve -tc "SELECT version_num FROM alembic_version;"  -> 0013
```

### Cross-Layer-Konsistenz-Tabelle
| Code-Feld / Wert | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `g.tenant_id` | app.py:2201 publish -> GUC | session-cookie -> contextvar -> transaction-local PG-GUC | ✓ grep+source |
| `meetings.tenant_id` (INSERT) | save_meeting stampt = g.tenant_id | DB-Spalte crm.meetings.tenant_id (uuid NOT NULL) | ✓ psql |
| `meetings.scheduled_at` (INSERT) | save_meeting parst offset-ISO -> aware datetime (MM-01) | DB-Spalte crm.meetings.scheduled_at (timestamptz — speichert Instant) | ✓ psql + MM-01-Test |
| `meetings.call_id` | POST-Body call_id (frontend state.lastCallId) | DB-Spalte uuid, KEIN FK | ✓ psql |
| `meetings.account_id/contact_id` | resolve-or-create (ON CONFLICT, MM-05) | DB-Spalten (nullable) | ✓ psql |
| `accounts` UNIQUE(tenant_id,name) | 0014 fügt Constraint hinzu (MM-05) | DB-Constraint crm.accounts | ✓ psql (aktuell absent -> add) |
| `auto_save_meeting` | set_meeting_pref schreibt | NEUE DB-Spalte crm.user_preferences.auto_save_meeting (bool DEFAULT false) | ✓ verified absent -> create |
| `user_id` | route schreibt g.user.id (Integer, serverseitig — MM-07) | DB-Spalte user_preferences.user_id (Integer, KEIN FK, soft-link D-08-Analogie) | ✓ Research §1 |

**Bei Diskrepanz: STOP + Replan.** (Keine Code-Variable maskiert als DB-Spalte — `auto_save_meeting` ist korrekt als CREATE modelliert, nicht als angenommene Spalte.)

<tasks>

<task type="auto">
  <name>Task 1: UserPreference ORM-Modell + Migration 0014 (Tabelle + FORCED RLS + MM-05 accounts UNIQUE)</name>
  <read_first>
    - database/models.py:755-825 (Account/Contact/Meeting crm-Pattern — Klon-Vorlage; {'schema':'crm'} als LETZTES __table_args__-Element; Imports an :2 bereits vorhanden)
    - alembic/versions/0012_crm_schema_tables_rls.py:52-146 (DDL + RLS + default-ACL-Beweis; Header :20-37 owner=postgres-Rationale)
    - Research §0.3 (KEIN GRANT/KEIN ALTER OWNER), §1 (DDL + ORM verbatim), §0.2 (head=0013 -> revision 0014; crm.accounts hat KEIN UNIQUE(tenant_id,name))
  </read_first>
  <action>
  **1a. ORM-Modell** in `database/models.py` NACH der `Meeting`-Klasse (nach Z.825), VOR dem
  training-Block (Z.828), einfügen — verbatim aus Research §1:
  ```python
  class UserPreference(Base):
      __tablename__ = 'user_preferences'
      id                = Column(UUID_TYPE, primary_key=True, default=uuid.uuid4)
      tenant_id         = Column(UUID_TYPE, nullable=False)
      user_id           = Column(Integer, nullable=False)          # soft link zu public.users.id, KEIN FK (D-08-Analogie); serverseitig aus g.user.id, nie Client-Wert (MM-07)
      auto_save_meeting = Column(Boolean, nullable=False, server_default=text('false'))  # DSGVO default OFF
      schema_version    = Column(SmallInteger, nullable=False, server_default='1')
      created_at        = Column(DateTime(timezone=True), server_default=func.now())
      updated_at        = Column(DateTime(timezone=True), server_default=func.now())
      __table_args__ = (
          UniqueConstraint('tenant_id', 'user_id', name='uq_user_preferences_tenant_user'),
          Index('idx_user_preferences_tenant', 'tenant_id'),
          {'schema': 'crm'},
      )
  ```
  (Imports Boolean/UniqueConstraint/text/func/SmallInteger/Integer/Index/DateTime sind an models.py:2 bereits vorhanden — NICHT neu importieren.)

  **MM-05 ORM-Parität:** Dem bestehenden `Account`-Modell (database/models.py:755-Bereich) in seinem
  `__table_args__` einen `UniqueConstraint('tenant_id', 'name', name='uq_accounts_tenant_name')` ergänzen
  (VOR dem `{'schema': 'crm'}`-Element — das bleibt letztes Element). DDL-parity mit der Migration unten.

  **1b. Migration** `alembic/versions/0014_crm_user_preferences_rls.py` mit `revision = '0014'`,
  `down_revision = '0013'`, `branch_labels = None`, `depends_on = None`. Header-Kommentar wie
  0012/0013: "Runs as postgres on Production", "owner=postgres -> RLS engagiert für nerve_app",
  "KEIN GRANT/KEIN ALTER OWNER — default-ACL aus 0012 vergibt nerve_app=arwd automatisch".
  `upgrade()`:
  ```python
  op.execute("""
      CREATE TABLE IF NOT EXISTS crm.user_preferences (
          id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
          tenant_id         UUID NOT NULL REFERENCES public.tenant_orgs(id),
          user_id           INTEGER NOT NULL,
          auto_save_meeting BOOLEAN NOT NULL DEFAULT false,
          schema_version    SMALLINT NOT NULL DEFAULT 1,
          created_at        TIMESTAMPTZ DEFAULT now(),
          updated_at        TIMESTAMPTZ DEFAULT now(),
          CONSTRAINT uq_user_preferences_tenant_user UNIQUE (tenant_id, user_id)
      )
  """)
  op.execute("ALTER TABLE crm.user_preferences ENABLE ROW LEVEL SECURITY")
  op.execute("ALTER TABLE crm.user_preferences FORCE ROW LEVEL SECURITY")
  op.execute("""
      CREATE POLICY tenant_isolation ON crm.user_preferences
        USING (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
        WITH CHECK (tenant_id = nullif(current_setting('app.tenant_id', true), '')::uuid)
  """)
  op.execute("CREATE INDEX IF NOT EXISTS idx_user_preferences_tenant ON crm.user_preferences(tenant_id)")

  # MM-05: atomare Doppel-Submit-Sicherung auf accounts. Tabelle ist neu/near-empty (Research §0.5)
  # -> UNIQUE sicher nachrüstbar. IF NOT EXISTS-Guard via DO-Block, falls Re-Run.
  op.execute("""
      DO $$ BEGIN
        IF NOT EXISTS (
          SELECT 1 FROM pg_constraint WHERE conname = 'uq_accounts_tenant_name'
        ) THEN
          ALTER TABLE crm.accounts ADD CONSTRAINT uq_accounts_tenant_name UNIQUE (tenant_id, name);
        END IF;
      END $$;
  """)
  ```
  `downgrade()`: symmetrisch — `ALTER TABLE crm.accounts DROP CONSTRAINT IF EXISTS uq_accounts_tenant_name`,
  DROP INDEX idx_user_preferences_tenant, DROP POLICY tenant_isolation, DROP TABLE crm.user_preferences.
  KEIN GRANT/OWNER zu reverten (keiner gesetzt).
  KEIN `GRANT`, KEIN `ALTER TABLE ... OWNER TO nerve_app` (Research §0.3 — würde RLS für nerve_app aushebeln).

  **MM-05 contacts-Entscheidung (dokumentiert):** Für `crm.contacts` wird in diesem Increment KEINE
  Uniqueness ergänzt. Begründung: ein Kontaktname ist account-skopiert sinnvoll-eindeutig
  (tenant_id, account_id, name), aber account_id ist hier nullable (blank-Firma-Fall -> account_id NULL),
  was eine partielle/zusammengesetzte Uniqueness verkompliziert; das Doppel-Submit-Risiko adressiert
  primär accounts (Firma ist das Pflicht-Resolve-Ziel). Contacts-Uniqueness ist als deferred Follow-up
  vermerkt — nicht in 0014. (Cross-AI MM-05 verlangte "mindestens accounts" — erfüllt.)
  </action>
  <verify>
    <automated>grep -n "class UserPreference" database/models.py && grep -n "uq_accounts_tenant_name" database/models.py && grep -n "auto_save_meeting BOOLEAN NOT NULL DEFAULT false" alembic/versions/0014_crm_user_preferences_rls.py && grep -n "FORCE ROW LEVEL SECURITY" alembic/versions/0014_crm_user_preferences_rls.py && grep -n "nullif(current_setting('app.tenant_id', true), '')::uuid" alembic/versions/0014_crm_user_preferences_rls.py && grep -n "uq_accounts_tenant_name" alembic/versions/0014_crm_user_preferences_rls.py && grep -c "GRANT\|ALTER TABLE crm.user_preferences OWNER\|ALTER TABLE crm.accounts OWNER" alembic/versions/0014_crm_user_preferences_rls.py</automated>
  </verify>
  <done>UserPreference-Modell existiert mit {'schema':'crm'} als letztem __table_args__-Element; Account-Modell trägt uq_accounts_tenant_name; 0014 enthält CREATE TABLE + FORCE RLS + NULLIF-Policy + UNIQUE + Index + MM-05 accounts-UNIQUE (DO-Block-guarded); grep für GRANT/ALTER OWNER gibt 0 (außer dem REFERENCES-FK zu tenant_orgs); revision=0014, down_revision=0013.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write-Route /crm/meetings (MM-01 tz-aware, MM-04 logging) + Preferences-Route + Real-PG-Tests</name>
  <read_first>
    - routes/crm_export.py (komplette Datei — Blueprint-Name 'crm_export', url_prefix '/crm', @login_required-Import, get_session-Pattern, Account/Contact bereits importiert, Meeting/UserPreference ergänzen)
    - routes/learning.py:180-186 (MM-04 Logging-Pattern verbatim: try/except mit traceback.print_exc() + print(f"[Tag] ... Fehler: {_e}") — DAS ist das CLAUDE.md-Punkt-15-Äquivalent in routes/)
    - routes/CLAUDE.md (url_for-Verifikation: vor url_for('crm_export.save_meeting') -> grep "def save_meeting" routes/crm_export.py + Blueprint-Name bestätigen)
    - app.py:2193-2217 (g.tenant_id-Quelle), database/db.py after_begin (GUC bereits global)
    - tests/test_rls_isolation.py (komplette Datei — Klon-Vorlage: nerve_app_pg_conn, _new_tenant, set_config-Pattern, reverse-FK-Teardown)
    - Research §2 (Route-Architektur, tenant-stamp, resolve-or-create verbatim), §3 (DSGVO D-2/D-4), §5 (Test-Strategie)
  </read_first>
  <behavior>
    - POST /crm/meetings (logged-in, tenant gesetzt) mit {firma, ansprechpartner, scheduled_at, notes, call_id} -> erzeugt crm.meetings-Row mit tenant_id == g.tenant_id; Response {ok:true, firma, thema, scheduled_at}.
    - MM-01: scheduled_at "2026-06-03T10:00:00+02:00" -> gespeicherter Instant in timestamptz == 08:00Z (selber Augenblick, korrekte Wandzeit). Offset-lose Eingabe ("2026-06-03T10:00" oder "...T10:00:00" ohne Offset) -> 400 'Datum braucht Zeitzone' (kein silent TZ-Shift).
    - POST mit neuer Firma -> erzeugt tenant-gestampte crm.accounts-Row; POST mit existierender Firma -> reuse (kein Duplikat); blank Firma -> account_id IS NULL, keine accounts-Row.
    - MM-05: zweimaliges POST derselben Firma (Doppel-Submit) -> genau EINE accounts-Row (ON CONFLICT DO NOTHING -> re-select); keine IntegrityError-500 nach außen.
    - Cross-Tenant: unter Tenant-A-GUC kann KEINE Row für Tenant B geschrieben/gelesen werden (RLS WITH CHECK / USING).
    - POST /crm/meetings ohne tenant -> 403 'Kein Mandant' (fail-closed).
    - GET /crm/preferences -> {auto_save_meeting: bool} (default false wenn keine Row). MM-07: scopt auf g.user.id (serverseitig), nie Client-user_id.
    - POST /crm/preferences {auto_save_meeting: true/false} -> UPSERT (ON CONFLICT (tenant_id,user_id) DO UPDATE), liest zurück.
    - DSGVO D-2/D-4: frisch erzeugte user_preferences-Row OHNE auto_save_meeting -> read-back False; POST /crm/meetings setzt NIE auto_save_meeting serverseitig auf true.
    - MM-04: ein erzwungener DB-Fehler im save_meeting/set_meeting_pref-except-Block schreibt traceback.print_exc()+[CRM-Meeting]-Logzeile, gibt aber sanitized 500 (kein Traceback im Response-Body).
  </behavior>
  <action>
  **2a. Route-Code** in `routes/crm_export.py`. Imports erweitern: `from flask import g, request, jsonify`,
  `from database.models import Account, Contact, Meeting, UserPreference` (Account/Contact stehen schon —
  Meeting/UserPreference ergänzen), `from datetime import datetime`, `import traceback` (MM-04).

  Helper `_resolve_account(db, tenant_id, firma)` und `_resolve_contact(db, tenant_id, account_id, name)`
  aus Research §2 (match by name — RLS scopt auf Tenant; blank -> None; create stampt tenant_id;
  `db.flush()` vor meeting-INSERT um id zu erhalten). **MM-05: `_resolve_account` muss den UNIQUE-Konflikt
  atomar behandeln** — find-then-create ist nicht atomar; bei Doppel-Submit gewinnt die DB-Constraint.
  Pattern (ON CONFLICT DO NOTHING -> re-select):
  ```python
  def _resolve_account(db, tenant_id, firma):
      if not firma:
          return None
      acc = db.query(Account).filter(Account.name == firma).first()  # RLS scopt Tenant
      if acc:
          return acc.id
      # MM-05: atomar gegen Doppel-Submit. INSERT ... ON CONFLICT (tenant_id, name) DO NOTHING,
      # dann re-select (der Gewinner ODER der parallele Erzeuger). uq_accounts_tenant_name (0014).
      db.execute(
          text("INSERT INTO crm.accounts (id, tenant_id, name) "
               "VALUES (gen_random_uuid(), :tid, :name) "
               "ON CONFLICT (tenant_id, name) DO NOTHING"),
          {"tid": tenant_id, "name": firma}
      )
      db.flush()
      acc = db.query(Account).filter(Account.name == firma).first()
      return acc.id if acc else None
  ```
  (`text` aus sqlalchemy ist in models/db bereits projektweit genutzt — in crm_export.py
  `from sqlalchemy import text` ergänzen. tenant_id in den VALUES stampt explizit; RLS-WITH-CHECK
  auf accounts greift identisch.)

  **MM-01 tz-aware scheduled_at parse:**
  ```python
  @crm_export_bp.route('/meetings', methods=['POST'])
  @login_required
  def save_meeting():
      tenant_id = g.tenant_id
      if not tenant_id:
          return jsonify(ok=False, error='Kein Mandant'), 403
      data = request.get_json(silent=True) or {}
      firma   = (data.get('firma') or '').strip()
      person  = (data.get('ansprechpartner') or '').strip()
      notes   = (data.get('notes') or '').strip() or None
      call_id = data.get('call_id') or None
      sched_raw = data.get('scheduled_at')
      scheduled_at = None
      if sched_raw:
          # MM-01 (Andre-Decision Option a): Frontend sendet offset-tragende ISO-8601
          # (z.B. "2026-06-03T10:00:00+02:00"). datetime.fromisoformat erzeugt dann ein
          # tz-AWARE datetime -> Insert in timestamptz speichert den korrekten Instant
          # (10:00+02:00 == 08:00Z). Eine offset-LOSE Eingabe (naive) wird ABGELEHNT,
          # damit Postgres NICHT still die Session-TZ unterstellt (sonst 10:00 -> 12:00-Drift).
          # Hinweis Py 3.8: fromisoformat akzeptiert "+02:00"-Offsets; "Z"-Suffix erst ab 3.11.
          # Frontend (Plan 05) sendet numerischen Offset, kein "Z" -> 3.8-kompatibel.
          try:
              parsed = datetime.fromisoformat(sched_raw)
          except (ValueError, TypeError):
              return jsonify(ok=False, error='Datum ungueltig'), 400
          if parsed.tzinfo is None or parsed.utcoffset() is None:
              return jsonify(ok=False, error='Datum braucht Zeitzone'), 400
          scheduled_at = parsed
      db = get_session()
      try:
          account_id = _resolve_account(db, tenant_id, firma)
          contact_id = _resolve_contact(db, tenant_id, account_id, person)
          m = Meeting(tenant_id=tenant_id, account_id=account_id, contact_id=contact_id,
                      call_id=call_id, scheduled_at=scheduled_at, notes=notes)
          db.add(m); db.commit()
          return jsonify(ok=True, firma=firma, thema=notes or '',
                         scheduled_at=scheduled_at.isoformat() if scheduled_at else None)
      except Exception as e:
          db.rollback()
          # MM-04 (CLAUDE.md Punkt 15 / learning.py:184-185-Pattern): Diagnose loggen VOR
          # sanitized Antwort. User-Response bleibt ohne Traceback.
          traceback.print_exc()
          print(f"[CRM-Meeting] save_meeting Fehler: {e}")
          return jsonify(ok=False, error='Konnte nicht gespeichert werden'), 500
      finally:
          db.close()
  ```
  (Fehler-Kontrakt matcht UI-SPEC: NIE 200-mit-silent-failure; Response sanitized — kein Traceback.)

  Preferences-Routen (MM-07: keyed auf g.user.id serverseitig — NIE Client-user_id; MM-04 Logging):
  ```python
  @crm_export_bp.route('/preferences', methods=['GET'])
  @login_required
  def get_meeting_pref():
      db = get_session()
      try:
          # MM-07: per-User-Authz via Session-Identität g.user.id (kein Client-Wert).
          # RLS scopt zusätzlich auf Tenant -> Nutzer liest nur die EIGENE Zeile.
          pref = db.query(UserPreference).filter(UserPreference.user_id == g.user.id).first()
          return jsonify(auto_save_meeting=bool(pref.auto_save_meeting) if pref else False)
      except Exception as e:
          traceback.print_exc()
          print(f"[CRM-Meeting] get_meeting_pref Fehler: {e}")
          return jsonify(auto_save_meeting=False), 500
      finally:
          db.close()

  @crm_export_bp.route('/preferences', methods=['POST'])
  @login_required
  def set_meeting_pref():
      tenant_id = g.tenant_id
      if not tenant_id:
          return jsonify(ok=False, error='Kein Mandant'), 403
      val = bool((request.get_json(silent=True) or {}).get('auto_save_meeting'))
      db = get_session()
      try:
          # MM-07: g.user.id serverseitig, kein Client-user_id.
          pref = db.query(UserPreference).filter(UserPreference.user_id == g.user.id).first()
          if pref is None:
              pref = UserPreference(tenant_id=tenant_id, user_id=g.user.id, auto_save_meeting=val)
              db.add(pref)
          else:
              pref.auto_save_meeting = val
          db.commit()
          return jsonify(ok=True, auto_save_meeting=val)
      except Exception as e:
          db.rollback()
          traceback.print_exc()
          print(f"[CRM-Meeting] set_meeting_pref Fehler: {e}")
          return jsonify(ok=False, error='Speichern fehlgeschlagen'), 500
      finally:
          db.close()
  ```
  (url_for-Namen: `crm_export.save_meeting`, `crm_export.get_meeting_pref`, `crm_export.set_meeting_pref` —
  vor jedem url_for grep "def save_meeting" routes/crm_export.py ausführen, routes/CLAUDE.md.)

  **2b. Tests** in `tests/test_meeting_save_rls.py` — KLON von tests/test_rls_isolation.py
  (nerve_app_pg_conn, _new_tenant, set_config-Pattern, reverse-FK-Teardown). Tests:
  - `test_user_preferences_rls_isolation`: zwei Tenants, je eine user_preferences-Row unter eigener
    GUC INSERT; unter Tenant-A-GUC SELECT sieht nur A's Row, B's nicht; INSERT als Tenant-B unter
    A-GUC raises psycopg2.Error (WITH CHECK).
  - `test_auto_save_meeting_default_false` (D-2/D-4): INSERT user_preferences OHNE auto_save_meeting
    -> read-back `auto_save_meeting IS false`.
  - `test_meeting_resolve_or_create_account`: POST-Logik via real-PG — neue Firma erzeugt accounts-Row
    (tenant-gestampt); zweiter Aufruf gleiche Firma -> kein Duplikat (COUNT==1); blank Firma -> 0 accounts-Rows.
  - `test_meeting_account_no_duplicate_on_conflict` (MM-05): zweimal INSERT ... ON CONFLICT (tenant_id,name)
    DO NOTHING mit identischer Firma unter selber Tenant-GUC -> `SELECT count(*) FROM crm.accounts WHERE name=...`
    == 1 (DB-Constraint uq_accounts_tenant_name greift, keine zweite Row).
  - `test_meeting_scheduled_at_tz_instant` (MM-01): INSERT crm.meetings mit
    scheduled_at = '2026-06-03T10:00:00+02:00' (als tz-aware Python-datetime via fromisoformat);
    read-back `scheduled_at AT TIME ZONE 'UTC'` == 2026-06-03 08:00:00 (korrekter Instant, kein Drift).
    Plus: ein naive datetime-Insert-Versuch über die Route-Validierungs-Logik (parsed.tzinfo is None)
    wird abgelehnt — als Funktions-Call-Assertion auf den Parse-Branch (nicht source-grep): rufe die
    Parse-Logik mit "2026-06-03T10:00" -> erwarte 400-Pfad / None-tzinfo-Branch.
  - `test_meeting_tenant_stamp`: meetings-Row hat tenant_id == GUC; cross-tenant INSERT raises.
  Real-PG-Pattern (nicht client-Fixture) für RLS/Stamp/Resolve/tz/no-duplicate; alle Rows in Teardown reverse-FK gelöscht.
  </action>
  <verify>
    <automated>grep -n "def save_meeting\|def get_meeting_pref\|def set_meeting_pref" routes/crm_export.py && grep -n "tenant_id=tenant_id" routes/crm_export.py && grep -n "parsed.tzinfo is None" routes/crm_export.py && grep -n "ON CONFLICT (tenant_id, name) DO NOTHING" routes/crm_export.py && grep -c "traceback.print_exc()" routes/crm_export.py && ssh -i ~/.ssh/nerve_vps -o ConnectTimeout=15 root@178.104.82.166 'cd /opt/nerve/app && NERVE_APP_TEST_DSN="$NERVE_APP_TEST_DSN" /opt/nerve/venv/bin/pytest tests/test_meeting_save_rls.py -x -v'</automated>
  </verify>
  <done>Drei Routen existieren mit @login_required; save_meeting stampt tenant_id=g.tenant_id; MM-01: lehnt offset-lose scheduled_at ab (parsed.tzinfo is None -> 400) und speichert offset-tragende ISO als korrekten Instant; MM-05: _resolve_account nutzt ON CONFLICT (tenant_id,name) DO NOTHING -> re-select; MM-04: alle drei except-Bloecke loggen traceback.print_exc()+[CRM-Meeting]; tests/test_meeting_save_rls.py grün auf Prod via SSH (real-PG, nerve_app) inkl. tz-Instant + no-duplicate; RLS-Isolation + DSGVO-default-false bewiesen.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Task 3: Migration 0014 MANUELL als postgres auf Production anwenden</name>
  <what-built>
  Code (UserPreference-Modell + Account-UNIQUE + 3 Routen + Tests) ist gepusht und via
  `deploy.sh production` (tar-Push, eingebauter Pytest-Gate vor Restart) deployed. `deploy.sh` führt
  KEIN alembic aus — Migration 0014 muss manuell als postgres angewendet werden (mirror 0011/0012/0013,
  Research §6). 0014 enthält JETZT auch die MM-05 accounts-UNIQUE-Constraint (selbe Migration).
  </what-built>
  <how-to-verify>
  **STEP-0 Hard-Gate ZUERST (trust code not markdown — Prod-Ist-Zustand vor Aktion verifizieren):**
  ```bash
  ssh -i ~/.ssh/nerve_vps -o ConnectTimeout=15 root@178.104.82.166 \
    "sudo -u postgres psql -d nerve -tc \"SELECT version_num FROM alembic_version;\" && \
     sudo -u postgres psql -d nerve -c \"SELECT to_regclass('crm.user_preferences');\" && \
     sudo -u postgres psql -d nerve -tc \"SELECT 1 FROM pg_constraint WHERE conname='uq_accounts_tenant_name';\""
  ```
  ERWARTET: head == `0013` UND `to_regclass('crm.user_preferences')` == NULL (leer) UND
  uq_accounts_tenant_name noch NICHT vorhanden (leer).
  Bei head != 0013 ODER user_preferences existiert bereits ODER Constraint schon da -> **HARD STOP**, NICHT migrieren, Plan-Author informieren.

  **Dann Migration anwenden (als postgres — owner=postgres engagiert RLS für nerve_app + default-ACL greift):**
  ```bash
  ssh -i ~/.ssh/nerve_vps -o ConnectTimeout=15 root@178.104.82.166 \
    "sudo -u postgres bash -c 'cd /opt/nerve/app && DATABASE_URL=postgresql://postgres@/nerve /opt/nerve/venv/bin/alembic upgrade head'"
  ```

  **Post-Migration verifizieren (read-only):**
  ```bash
  ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
    "sudo -u postgres psql -d nerve -c '\d crm.user_preferences' && \
     sudo -u postgres psql -d nerve -c '\dp crm.user_preferences' && \
     sudo -u postgres psql -d nerve -c '\d crm.accounts'"
  ```
  ERWARTET: FORCED RLS + policy `tenant_isolation` (NULLIF-Form) + uq_user_preferences_tenant_user +
  idx_user_preferences_tenant; `\dp` zeigt `nerve_app=arwd/postgres` (default-ACL auto-grant), owner postgres;
  crm.accounts zeigt `uq_accounts_tenant_name` UNIQUE (tenant_id, name) (MM-05).

  **Service-Meta + Restart + Health (Research §6):**
  ```bash
  ssh -i ~/.ssh/nerve_vps root@178.104.82.166 \
    "echo '0014' > /opt/nerve/.deploy_meta && systemctl restart nerve && sleep 3 && curl -s https://getnerve.app/api/health"
  ```
  ERWARTET: /api/health == ok/200.
  </how-to-verify>
  <resume-signal>Tippe "applied" wenn head==0014, \d zeigt FORCED RLS + NULLIF-Policy, crm.accounts hat uq_accounts_tenant_name (MM-05), \dp zeigt nerve_app=arwd, /api/health ok. Bei STEP-0-Mismatch oder Migrationsfehler: beschreibe das Problem (kein Resume).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (PiP) -> POST /crm/meetings | Untrusted JSON-Input (firma/person/notes/scheduled_at/call_id) quert hier die Wall |
| nerve_app -> crm.* | RLS-Mandanten-Wall: nerve_app ist non-owner + rolbypassrls=f -> tenant_isolation engagiert |
| Tenant A request -> Tenant B data | Cross-Tenant-Isolation via app.tenant_id-GUC == row.tenant_id |
| Client-user_id -> prefs (MM-07) | KEINE Wall nötig: Routen lesen NIE einen Client-user_id; sie keyen auf g.user.id (Session) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-G04-01 | Tampering | POST /crm/meetings tenant_id | mitigate | tenant_id NIE aus Body — immer aus g.tenant_id; RLS WITH CHECK == GUC lehnt Fremd-tenant-INSERT ab (test_meeting_tenant_stamp beweist) |
| T-G04-02 | Information Disclosure | crm cross-tenant read | mitigate | FORCED RLS + NULLIF tenant_isolation-Policy; unset GUC -> 0 rows (fail-closed); real-PG-Isolation-Test |
| T-G04-03 | Elevation of Privilege | nerve_app als Tabellen-Owner würde RLS umgehen | mitigate | Migration läuft als postgres -> owner=postgres; KEIN ALTER OWNER; default-ACL gibt nur DML (arwd), nicht Ownership (Research §0.3) |
| T-G04-04 | Spoofing/CSRF | POST-Routen | mitigate | Flask-WTF global; X-CSRFToken via getCsrfToken() im PiP-fetch; KEINE csrf.exempt |
| T-G04-05 | Repudiation/Injection | firma/person als SQL | mitigate | ORM-parametrisiert (db.query(...).filter(Account.name==firma)); _resolve_account ON CONFLICT nutzt parametrisiertes text() mit :name-Bind (kein String-Interpolation) — niedriges Risiko |
| T-G04-06 | DoS | unbounded account/contact create | accept | Solo-/Early-Access-Scope, B2B-Kontaktdaten, kein öffentlicher Endpoint; per-Request 1 INSERT-Pfad; MM-05 UNIQUE verhindert Doppel-Submit-Spam-Rows |
| T-G04-07 | DSGVO Info Disclosure | auto_save serverseitig default-on | mitigate | Spalte DEFAULT false; Route setzt NIE true ohne expliziten POST; test_auto_save_meeting_default_false |
| T-G04-08 | Tampering | TZ-Drift bei scheduled_at (MM-01) | mitigate | Backend lehnt offset-lose Eingabe ab (parsed.tzinfo is None -> 400); aware ISO -> korrekter Instant in timestamptz; test_meeting_scheduled_at_tz_instant |
| T-G04-09 | EoP | cross-user pref tampering (MM-07) | mitigate | Routen keyen auf g.user.id (Session-Identität), NIE Client-user_id; per-User-Authz im App-Code + Tenant-RLS — kein Exploit-Pfad (Cross-AI MM-07 als REJECTED bestätigt) |
</threat_model>

<verification>
- STEP-0-Gate erfüllt: head==0013 + user_preferences absent + uq_accounts_tenant_name absent VOR Migration (Task 3).
- 0014 angewendet: head==0014, \d zeigt FORCED RLS + tenant_isolation NULLIF-Policy + unique + index, crm.accounts hat uq_accounts_tenant_name (MM-05), \dp zeigt nerve_app=arwd owner postgres.
- tests/test_meeting_save_rls.py grün auf Prod via SSH (real-PG als nerve_app): RLS-Isolation, tenant-stamp, resolve-or-create, DSGVO-default-false, MM-01 tz-Instant, MM-05 no-duplicate.
- /api/health == ok nach Restart.
- KEIN lokaler pytest, KEIN git pull auf Prod (tar-deployed, .git excluded).
</verification>

<success_criteria>
- crm.user_preferences live mit FORCED RLS, NULLIF-Policy, owner postgres, nerve_app=arwd via default-ACL.
- crm.accounts trägt UNIQUE(tenant_id,name) (MM-05) — Doppel-Submit erzeugt keine Duplikate.
- POST /crm/meetings stampt tenant_id=g.tenant_id, parst scheduled_at tz-aware (MM-01), resolved-or-creates accounts/contacts (ON CONFLICT), mappt call_id, loggt Fehler (MM-04), hält UI-SPEC-Fehler-Kontrakt.
- GET/POST /crm/preferences liest/schreibt auto_save_meeting (UPSERT), keyed auf g.user.id (MM-07).
- DSGVO-Default-off serverseitig bewiesen (real-PG-Test).
- Backend eigenständig auf Production deployed und grün — Plan 05 (Frontend) kann konsumieren.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.23.2.G-MEET-foundation-conversational-memory-crm-multi-tenancy-training/08.23.2.G-MEET-04-SUMMARY.md` erstellen.
</output>
</content>
</invoke>
