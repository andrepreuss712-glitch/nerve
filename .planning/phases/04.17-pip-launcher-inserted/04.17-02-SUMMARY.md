---
phase: 04.17-pip-launcher-inserted
plan: 02
subsystem: ui
tags: [pip, vanilla-js, localstorage, postcall-score, flask-api]

# Dependency graph
requires:
  - phase: 04.17-01
    provides: setPipState, showPipSetupStep, pipStartSetup, pipPopulateProfiles, pipPopulateKundendatenHistory, HTML scaffolding
  - phase: 04.13
    provides: /api/precall/research endpoint
provides:
  - Complete PiP call lifecycle behavior: Setup -> Live -> Postcall -> Next Call
  - Kundendaten localStorage history with try/catch validation
  - PreCall research button integration from PiP
  - Client-side postcall score calculation (40% KB + 30% einwand + 20% redeanteil + 10% skript)
  - Colored highlight tags (teal/yellow/red) from postcall metrics
  - pipNextCall resets state for next call without leaving PiP
affects:
  - 04.17-03 (Plan 03 will do final polish/integration testing)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Client-side score formula: calcPipScore() computes weighted composite from postcall metrics without LLM call"
    - "Dynamic CSS class via string concat: 'pip-tag-' + color — avoids enumerating classes in JS"
    - "pip-btn-beenden guard: sessionSeconds < 10 shows fallback postcall instead of calling /api/beenden"
    - "saveKundendaten deduplicates by (firma, name) before prepending to history array"

key-files:
  created: []
  modified:
    - static/app.js

key-decisions:
  - "calcPipScore uses ga_details.filter(x.erfolgreich) for behandelt rate — no backend change needed (D-14)"
  - "pipStartCall calls startMicStream() synchronously in direct click handler to preserve user gesture for getUserMedia"
  - "pipConsentReject silently falls back to cold_call mode — same UX as rejecting meeting consent (MODE-03)"
  - "pip-tag colors generated dynamically via string concatenation — literal class names in HTML template CSS cover all cases"

patterns-established:
  - "calcPipScore(postcall): single function owns score math, testable independently"
  - "buildHighlightTags(postcall): returns [{text, color}] capped at 3 items (prefer 2 positive + 1 negative)"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-04-13
---

# Phase 04.17 Plan 02: PiP Launcher Behavior Summary

**Full PiP call lifecycle wired: mode selection with meeting consent, Kundendaten localStorage history, PreCall research, call start via startMicStream(), postcall score (weighted composite) with 3 colored highlight tags, and next-call loop**

## Performance

- **Duration:** 12 min
- **Started:** 2026-04-13T13:37:00Z
- **Completed:** 2026-04-13T13:49:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- PiP call lifecycle fully functional: Setup flow (mode -> kundendaten -> skript) -> Live coaching tabs -> Postcall score/tags -> "Naechster Call" loop
- Kundendaten history in localStorage with deduplication by (firma, name), max 5 entries, input validation (try/catch + Array.isArray)
- Client-side score: 40% Kaufbereitschaft + 30% Einwand-Behandlung + 20% Redeanteil (target 40%) + 10% Skript — zero LLM calls
- Colored tags system: up to 3 tags (teal=positive, yellow=warning, red=negative) from postcall metrics including ga_details.erfolgreich

## Task Commits

1. **Task 1 + Task 2: Setup flow and postcall display** - `54fffc2` (feat) — both tasks implemented in single atomic edit and committed together

## Files Created/Modified
- `static/app.js` - 316 lines added: loadKundendatenHistory, saveKundendaten, pipSelectMode, pipConsentAccept/Reject, pipSubmitKundendaten, pipStartPrecall, pipStartCall, pipBeendenCall, calcPipScore, buildHighlightTags, showPipPostcall, pipNextCall, pipShowDetails

## Decisions Made
- Used `ga_details.filter(x => x.erfolgreich === true).length` for einwaende_behandelt rate (no backend change, per D-14)
- `startMicStream()` called synchronously inside `pipStartCall()` to preserve user gesture context for `getUserMedia`
- Tag colors constructed dynamically as `'pip-tag-' + color` — literal class names exist in app.html CSS so no tree-shaking issue
- Meeting consent rejection falls back to `cold_call` silently — same behavior as selecting cold call (cleaner UX for 30-40 calls/day flow)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

The verification script checked for literal strings `pip-tag-teal` and `pip-tag-red` in JS source. These are generated dynamically via `'pip-tag-' + t.color` — the colors `'teal'` and `'red'` are present as string values in the tag objects and the CSS classes exist in app.html. False negative in the verification check; implementation is correct.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All PiP call lifecycle functions implemented and committed
- HTML scaffolding (Plan 01) + JS behavior (Plan 02) complete
- Plan 03 (if exists) can proceed with integration/polish
- No blockers

---
*Phase: 04.17-pip-launcher-inserted*
*Completed: 2026-04-13*
