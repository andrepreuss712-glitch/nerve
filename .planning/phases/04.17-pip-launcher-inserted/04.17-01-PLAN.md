---
phase: 04.17-pip-launcher-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/app.html
  - routes/app_routes.py
  - static/app.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "PiP window shows setup panels (modus, kundendaten, skript) instead of jumping straight to live tabs"
    - "PiP window shows postcall panel with score area and action buttons"
    - "PiP state machine controls which section is visible (setup, live, postcall)"
    - "Profile list is available in JS as window._allProfiles for skript selection"
  artifacts:
    - path: "templates/app.html"
      provides: "Setup panels (3 steps) + postcall panel inside #pip-window"
      contains: "pip-section-setup"
    - path: "templates/app.html"
      provides: "CSS for setup flow panels and postcall"
      contains: "pip-setup-step"
    - path: "routes/app_routes.py"
      provides: "profiles_for_pip template variable"
      contains: "profiles_for_pip"
    - path: "static/app.js"
      provides: "PiP state machine (setPipState, showPipSetupStep)"
      contains: "setPipState"
  key_links:
    - from: "templates/app.html"
      to: "static/app.js"
      via: "onclick handlers on setup buttons call JS functions"
      pattern: "pipSelectMode|pipSubmitKundendaten|pipStartCall"
    - from: "routes/app_routes.py"
      to: "templates/app.html"
      via: "profiles_for_pip template variable"
      pattern: "profiles_for_pip"
---

<objective>
Build the HTML/CSS structure and JS state machine skeleton for the PiP Launcher upgrade.

Purpose: Transform the existing PiP from a live-coaching-only viewer into a multi-panel launcher with pre-call setup, live coaching, and post-call summary states. This plan creates the scaffolding; Plan 02 wires behavior.

Output: New HTML panels inside #pip-window, CSS styles, profile list embedding, and the PiP state machine functions in app.js.
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.17-pip-launcher-inserted/04.17-CONTEXT.md
@.planning/phases/04.17-pip-launcher-inserted/04.17-RESEARCH.md

@templates/app.html (lines 909-956 — existing PiP HTML)
@static/app.js (lines 1414-1692 — existing PiP JS)
@routes/app_routes.py (lines 37-126 — live() route)

<interfaces>
<!-- Existing PiP patterns the executor must follow -->

From static/app.js:
```javascript
// PiP element helper (use this for ALL PiP DOM access)
function getPipElement(id) {
  if (window._pipWindow && !window._pipWindow.closed) {
    return window._pipWindow.document.getElementById(id) || document.getElementById(id);
  }
  return document.getElementById(id);
}

// Existing state variables
window._pipWindow = null;
window._pipTabLocked = null;

// Existing functions that remain unchanged
function openPipWindow() { ... }      // Opens Document PiP, moves #pip-window into it
function initPipContent() { ... }     // Initializes live coaching tabs
function activatePipTab(tabName) { ... } // Shows one of the 4 live tabs
```

From templates/app.html (line 910):
```html
<div id="pip-window" style="display:none">
  <!-- Existing: pip-header, pip-tabs, pip-content (4 panels), pip-ewb-row -->
</div>
```

From routes/app_routes.py (line 120-124):
```python
return render_template('app.html', user=g.user, org=g.org,
                       active_profile=active_profile, profiles=profiles,
                       active_phasen=active_phasen,
                       active_profile_daten=active_profile_daten,
                       precall_verfuegbar=precall_verfuegbar)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PiP Launcher HTML panels and CSS to app.html + embed profile list</name>
  <files>templates/app.html, routes/app_routes.py</files>

  <read_first>
    - templates/app.html (lines 909-956 — existing #pip-window structure)
    - templates/app.html (lines 1-50 — check existing CSS/style blocks for PiP styles)
    - routes/app_routes.py (lines 37-126 — live() route, see profiles variable)
    - static/nerve.css (first 50 lines — CSS variables: --page-bg, --glass-border, --color-teal)
  </read_first>

  <action>
**A) Restructure #pip-window in app.html (line 910-956):**

Wrap the existing PiP content into sections. The new structure inside `#pip-window`:

```html
<div id="pip-window" style="display:none">

  <!-- ── Section: SETUP (pre-call) ── -->
  <div id="pip-section-setup" class="pip-section" style="display:flex;flex-direction:column;height:100%">

    <!-- Step 1: Modus (per D-01, D-02, D-04) -->
    <div id="pip-setup-step-1" class="pip-setup-step">
      <div class="pip-setup-title">Modus waehlen</div>
      <div class="pip-setup-buttons">
        <button class="pip-mode-btn" onclick="pipSelectMode('cold_call')">
          <span class="pip-mode-icon">&#x1F512;</span>
          <span>Cold Call</span>
        </button>
        <button class="pip-mode-btn" onclick="pipSelectMode('meeting')">
          <span class="pip-mode-icon">&#x1F91D;</span>
          <span>Meeting</span>
        </button>
      </div>
    </div>

    <!-- Step 2: Kundendaten (per D-05) -->
    <div id="pip-setup-step-2" class="pip-setup-step" style="display:none">
      <div class="pip-setup-title">Kundendaten</div>
      <div class="pip-setup-form">
        <input type="text" id="pip-kd-firma" class="pip-input" placeholder="Firma" list="pip-kd-firma-list" autocomplete="off">
        <datalist id="pip-kd-firma-list"></datalist>
        <input type="text" id="pip-kd-name" class="pip-input" placeholder="Name" list="pip-kd-name-list" autocomplete="off">
        <datalist id="pip-kd-name-list"></datalist>
        <input type="text" id="pip-kd-position" class="pip-input" placeholder="Position" list="pip-kd-position-list" autocomplete="off">
        <datalist id="pip-kd-position-list"></datalist>
      </div>
      <div class="pip-setup-actions">
        <button class="pip-btn-precall" id="pip-precall-btn" onclick="pipStartPrecall()" style="display:none">PreCall-Recherche</button>
        <button class="pip-btn-next" onclick="pipSubmitKundendaten()">Weiter</button>
      </div>
      <div id="pip-precall-result" class="pip-precall-result" style="display:none"></div>
    </div>

    <!-- Step 3: Skript/Profil (per D-06) -->
    <div id="pip-setup-step-3" class="pip-setup-step" style="display:none">
      <div class="pip-setup-title">Skript waehlen</div>
      <select id="pip-profil-select" class="pip-select"></select>
      <button class="pip-btn-start" onclick="pipStartCall()">Call starten</button>
    </div>

    <!-- Inline Meeting Consent (shown only if Meeting mode selected) -->
    <div id="pip-consent-inline" class="pip-consent" style="display:none">
      <p class="pip-consent-text">Alle Teilnehmer muessen dem Mitschnitt zustimmen.</p>
      <div class="pip-consent-actions">
        <button class="pip-btn-consent-yes" onclick="pipConsentAccept()">Akzeptieren</button>
        <button class="pip-btn-consent-no" onclick="pipConsentReject()">Ablehnen</button>
      </div>
    </div>
  </div>

  <!-- ── Section: LIVE (existing coaching tabs — unchanged per D-12/D-13) ── -->
  <div id="pip-section-live" class="pip-section" style="display:none;flex-direction:column;height:100%">
    <!-- Move ALL existing PiP content here: pip-header, pip-tabs, pip-content, pip-active-hint, pip-ewb-row, pip-fallback-warning -->
    <!-- (The existing HTML from lines 912-955 goes inside this div) -->
  </div>

  <!-- ── Section: POSTCALL (per D-14, D-15, D-16) ── -->
  <div id="pip-section-postcall" class="pip-section" style="display:none;flex-direction:column;align-items:center;justify-content:center;height:100%;padding:16px">
    <div class="pip-postcall-score" id="pip-postcall-score">--</div>
    <div class="pip-postcall-label">Call Score</div>
    <div class="pip-postcall-tags" id="pip-postcall-tags"></div>
    <div class="pip-postcall-kundendaten" id="pip-postcall-kundendaten"></div>
    <div class="pip-postcall-actions">
      <button class="pip-btn-next-call" onclick="pipNextCall()">Naechster Call</button>
      <button class="pip-btn-details" onclick="pipShowDetails()">Details</button>
    </div>
  </div>

</div>
```

IMPORTANT: The existing PiP elements (pip-header with mode badge + timer + Vollbild link, pip-tabs with 4 tab buttons, pip-content with 4 panels, pip-active-hint, pip-ewb-row, pip-fallback-warning) must be moved INSIDE `#pip-section-live`. Do NOT delete or modify them. Just wrap them.

Also add a "Beenden" button inside pip-section-live, after pip-ewb-row:
```html
<button class="pip-btn-beenden" id="pip-btn-beenden" onclick="pipBeendenCall()">Beenden</button>
```

**B) Add CSS for new PiP panels (in the existing `<style>` block where .pip-header etc. are defined):**

```css
/* PiP Launcher Sections */
.pip-section { flex-direction: column; }
.pip-setup-step { padding: 16px; display: flex; flex-direction: column; gap: 10px; flex: 1; }
.pip-setup-title { font-size: 14px; font-weight: 600; color: #00D4AA; text-align: center; margin-bottom: 4px; }
.pip-setup-buttons { display: flex; gap: 10px; justify-content: center; flex: 1; align-items: center; }
.pip-mode-btn {
  background: #1C2333; border: 2px solid #2a3548; border-radius: 12px;
  color: #e8ecf4; padding: 16px 20px; font-size: 14px; cursor: pointer;
  display: flex; flex-direction: column; align-items: center; gap: 6px;
  transition: border-color 0.2s, background 0.2s; flex: 1; max-width: 140px;
}
.pip-mode-btn:hover { border-color: #00D4AA; background: #1a2a3a; }
.pip-mode-icon { font-size: 24px; }
.pip-input {
  background: #1C2333; border: 1px solid #2a3548; border-radius: 8px;
  color: #e8ecf4; padding: 8px 12px; font-size: 13px; width: 100%;
  box-sizing: border-box; outline: none;
}
.pip-input:focus { border-color: #00D4AA; }
.pip-input::placeholder { color: #555e70; }
.pip-setup-form { display: flex; flex-direction: column; gap: 8px; }
.pip-setup-actions { display: flex; gap: 8px; margin-top: 8px; }
.pip-select {
  background: #1C2333; border: 1px solid #2a3548; border-radius: 8px;
  color: #e8ecf4; padding: 8px 12px; font-size: 13px; width: 100%;
  box-sizing: border-box; outline: none; margin-bottom: 12px;
}
.pip-btn-next, .pip-btn-start, .pip-btn-next-call {
  background: #00D4AA; color: #0D1117; border: none; border-radius: 8px;
  padding: 10px 16px; font-size: 13px; font-weight: 600; cursor: pointer;
  flex: 1; transition: opacity 0.2s;
}
.pip-btn-next:hover, .pip-btn-start:hover, .pip-btn-next-call:hover { opacity: 0.85; }
.pip-btn-precall {
  background: transparent; border: 1px solid #2a3548; border-radius: 8px;
  color: #00D4AA; padding: 10px 12px; font-size: 12px; cursor: pointer;
  flex: 0 0 auto; transition: border-color 0.2s;
}
.pip-btn-precall:hover { border-color: #00D4AA; }
.pip-btn-details {
  background: #1C2333; border: 1px solid #2a3548; border-radius: 8px;
  color: #e8ecf4; padding: 10px 16px; font-size: 13px; cursor: pointer;
  flex: 1; transition: border-color 0.2s;
}
.pip-btn-details:hover { border-color: #00D4AA; }
.pip-btn-beenden {
  background: transparent; border: 1px solid rgba(248,113,113,0.4); border-radius: 8px;
  color: #f87171; padding: 6px 12px; font-size: 12px; cursor: pointer;
  margin: 6px 8px; transition: background 0.2s;
}
.pip-btn-beenden:hover { background: rgba(248,113,113,0.1); }
.pip-consent { padding: 12px 16px; background: rgba(248,113,113,0.05); border-top: 1px solid rgba(248,113,113,0.2); }
.pip-consent-text { font-size: 12px; color: #e8ecf4; margin: 0 0 8px 0; }
.pip-consent-actions { display: flex; gap: 8px; }
.pip-btn-consent-yes { background: #00D4AA; color: #0D1117; border: none; border-radius: 6px; padding: 6px 14px; font-size: 12px; cursor: pointer; }
.pip-btn-consent-no { background: transparent; border: 1px solid #2a3548; border-radius: 6px; color: #e8ecf4; padding: 6px 14px; font-size: 12px; cursor: pointer; }
.pip-precall-result {
  background: #1C2333; border: 1px solid #2a3548; border-radius: 8px;
  padding: 8px 12px; font-size: 11px; color: #a0aec0; max-height: 120px;
  overflow-y: auto; margin-top: 8px; white-space: pre-wrap; line-height: 1.4;
}
/* PostCall */
.pip-postcall-score { font-size: 48px; font-weight: 700; color: #00D4AA; line-height: 1; }
.pip-postcall-label { font-size: 12px; color: #a0aec0; margin-top: 4px; }
.pip-postcall-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 12px; justify-content: center; }
.pip-tag { padding: 4px 10px; border-radius: 12px; font-size: 11px; font-weight: 500; }
.pip-tag-teal { background: rgba(0,212,170,0.15); color: #00D4AA; }
.pip-tag-yellow { background: rgba(245,158,11,0.15); color: #F59E0B; }
.pip-tag-red { background: rgba(248,113,113,0.15); color: #F87171; }
.pip-postcall-kundendaten { font-size: 11px; color: #555e70; margin-top: 8px; }
.pip-postcall-actions { display: flex; gap: 10px; margin-top: 16px; width: 100%; max-width: 280px; }
```

**C) Embed profile list for PiP in app.html:**

Find the existing `<script>` block in app.html that sets `window._profileData` and `window._profileEinwaende`. Near that block, add:

```html
<script>
  window._allProfiles = {{ profiles_for_pip | tojson | safe }};
</script>
```

**D) Add `profiles_for_pip` to live() route in app_routes.py:**

In `routes/app_routes.py`, inside the `live()` function, before the `return render_template(...)` call (around line 120), add:

```python
import json as _json_pip
profiles_for_pip = [{'id': p.id, 'name': p.name} for p in profiles]
```

Then add `profiles_for_pip=profiles_for_pip` to the `render_template()` call.

**E) Show PreCall button conditionally:**

In the template `<script>` block where `window._allProfiles` is set, also add:
```html
<script>
  window._precallVerfuegbar = {{ 'true' if precall_verfuegbar else 'false' }};
</script>
```
  </action>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import sys
# Check app.html has new panels
with open('templates/app.html','r',encoding='utf-8') as f: html=f.read()
checks = [
  ('pip-section-setup' in html, 'pip-section-setup exists'),
  ('pip-section-live' in html, 'pip-section-live exists'),
  ('pip-section-postcall' in html, 'pip-section-postcall exists'),
  ('pip-setup-step-1' in html, 'setup step 1 exists'),
  ('pip-setup-step-2' in html, 'setup step 2 exists'),
  ('pip-setup-step-3' in html, 'setup step 3 exists'),
  ('pip-kd-firma' in html, 'kundendaten firma input'),
  ('pip-postcall-score' in html, 'postcall score element'),
  ('pip-btn-beenden' in html, 'beenden button in live section'),
  ('profiles_for_pip' in html, 'profiles_for_pip embedded'),
  ('pip-postcall-tags' in html, 'postcall tags container'),
  ('pipSelectMode' in html, 'pipSelectMode onclick'),
  ('pipStartCall' in html, 'pipStartCall onclick'),
  ('pipNextCall' in html, 'pipNextCall onclick'),
  ('pip-consent-inline' in html, 'consent inline panel'),
]
# Check app_routes.py
with open('routes/app_routes.py','r',encoding='utf-8') as f: py=f.read()
checks.append(('profiles_for_pip' in py, 'profiles_for_pip in route'))
ok = all(c[0] for c in checks)
for c in checks:
  print(('PASS' if c[0] else 'FAIL') + ': ' + c[1])
sys.exit(0 if ok else 1)
"</automated>
  </verify>

  <acceptance_criteria>
    - templates/app.html contains `id="pip-section-setup"`, `id="pip-section-live"`, `id="pip-section-postcall"` divs inside `#pip-window`
    - templates/app.html contains `id="pip-setup-step-1"`, `id="pip-setup-step-2"`, `id="pip-setup-step-3"` divs
    - templates/app.html contains `id="pip-kd-firma"`, `id="pip-kd-name"`, `id="pip-kd-position"` input fields with datalist elements
    - templates/app.html contains `id="pip-profil-select"` select element
    - templates/app.html contains `id="pip-postcall-score"` and `id="pip-postcall-tags"` elements
    - templates/app.html contains `id="pip-btn-beenden"` button inside pip-section-live
    - templates/app.html contains CSS classes `.pip-setup-step`, `.pip-mode-btn`, `.pip-input`, `.pip-postcall-score`, `.pip-tag-teal`, `.pip-tag-yellow`, `.pip-tag-red`
    - templates/app.html contains `window._allProfiles = {{ profiles_for_pip` script block
    - routes/app_routes.py contains `profiles_for_pip` variable creation and inclusion in render_template call
    - Existing PiP elements (pip-header, pip-tabs, pip-content, pip-ewb-row) are INSIDE `pip-section-live`, not deleted
  </acceptance_criteria>

  <done>All HTML panels, CSS styles, and profile embedding are in place. The PiP window has 3 sections (setup, live, postcall) with setup split into 3 steps.</done>
</task>

<task type="auto">
  <name>Task 2: Add PiP state machine and openPipWindow modifications to app.js</name>
  <files>static/app.js</files>

  <read_first>
    - static/app.js (lines 1414-1510 — openPipWindow, closePipToVollbild)
    - static/app.js (lines 1565-1593 — initPipContent)
    - static/app.js (lines 1-17 — module-level vars: socket, sessionMode, precallBriefingText)
  </read_first>

  <action>
**A) Add PiP state machine variables and functions BEFORE the existing `openPipWindow` function (insert around line 1414):**

```javascript
// ── PiP Launcher State Machine ────────────────────────────────────────────────
window._pipState = 'setup';  // 'setup' | 'live' | 'postcall'
var _pipSetupStep = 1;       // 1: modus, 2: kundendaten, 3: skript
var _pipSelectedMode = null; // 'cold_call' | 'meeting'
window._pipKundendaten = null;  // { firma, name, position }

function setPipState(newState) {
  window._pipState = newState;
  var sections = {
    'setup':    getPipElement('pip-section-setup'),
    'live':     getPipElement('pip-section-live'),
    'postcall': getPipElement('pip-section-postcall'),
  };
  Object.keys(sections).forEach(function(k) {
    if (sections[k]) sections[k].style.display = (k === newState) ? 'flex' : 'none';
  });
}

function showPipSetupStep(step) {
  [1, 2, 3].forEach(function(s) {
    var el = getPipElement('pip-setup-step-' + s);
    if (el) el.style.display = (s === step) ? 'flex' : 'none';
  });
  // Hide consent panel when switching steps
  var consent = getPipElement('pip-consent-inline');
  if (consent) consent.style.display = 'none';
  _pipSetupStep = step;
}

function pipStartSetup() {
  _pipSetupStep = 1;
  _pipSelectedMode = null;
  window._pipKundendaten = null;
  setPipState('setup');
  showPipSetupStep(1);
  // Populate profile dropdown
  pipPopulateProfiles();
  // Populate kundendaten datalists from history
  pipPopulateKundendatenHistory();
  // Show/hide precall button
  var pcBtn = getPipElement('pip-precall-btn');
  if (pcBtn) pcBtn.style.display = window._precallVerfuegbar ? 'block' : 'none';
  // Clear precall result
  var pcResult = getPipElement('pip-precall-result');
  if (pcResult) { pcResult.style.display = 'none'; pcResult.textContent = ''; }
}

function pipPopulateProfiles() {
  var sel = getPipElement('pip-profil-select');
  if (!sel) return;
  sel.innerHTML = '';
  var profiles = window._allProfiles || [];
  var activeId = null;
  try { activeId = parseInt(document.querySelector('[data-active-profile-id]').getAttribute('data-active-profile-id')); } catch(e) {}
  profiles.forEach(function(p) {
    var opt = sel.ownerDocument.createElement('option');
    opt.value = p.id;
    opt.textContent = p.name;
    if (p.id === activeId) opt.selected = true;
    sel.appendChild(opt);
  });
}
```

**B) Modify `openPipWindow()` to request larger window size and start in setup state:**

Change the `requestWindow` call from `{ width: 380, height: 280 }` to `{ width: 380, height: 400 }`.

After the line `initPipContent();` (line ~1507), add:
```javascript
  // Start PiP in setup state (launcher mode)
  pipStartSetup();
```

**C) Modify the `pagehide` handler inside `openPipWindow()` to reset PiP state:**

After `window._pipTabLocked = null;` (around line 1499), add:
```javascript
    window._pipState = 'setup';
```

**D) Add a `data-active-profile-id` attribute for the profile select default:**

In `initPipContent()`, at the end of the function (after `activatePipTab('opener');`), add:
```javascript
  // Note: active profile ID is embedded via flask_session in app.html
```
This is already available via `flask_session['active_profile_id']`. The actual attribute should be added in app.html. Add to the `<script>` block where `window._allProfiles` is set:
```javascript
window._activeProfileId = {{ active_profile.id if active_profile else 'null' }};
```

Then update `pipPopulateProfiles()` to use `window._activeProfileId` instead of the DOM query:
```javascript
var activeId = window._activeProfileId || null;
```
  </action>

  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -c "
import sys
with open('static/app.js','r',encoding='utf-8') as f: js=f.read()
checks = [
  ('setPipState' in js, 'setPipState function exists'),
  ('showPipSetupStep' in js, 'showPipSetupStep function exists'),
  ('pipStartSetup' in js, 'pipStartSetup function exists'),
  ('pipPopulateProfiles' in js, 'pipPopulateProfiles function exists'),
  ('window._pipState' in js, '_pipState variable'),
  ('_pipSelectedMode' in js, '_pipSelectedMode variable'),
  ('_pipKundendaten' in js, '_pipKundendaten variable'),
  ('width: 380, height: 400' in js or 'width: 380,\\n' in js, 'PiP window size updated'),
  ('pipStartSetup()' in js, 'pipStartSetup called after openPipWindow'),
]
ok = all(c[0] for c in checks)
for c in checks:
  print(('PASS' if c[0] else 'FAIL') + ': ' + c[1])
sys.exit(0 if ok else 1)
"</automated>
  </verify>

  <acceptance_criteria>
    - static/app.js contains `function setPipState(newState)` that toggles display of pip-section-setup, pip-section-live, pip-section-postcall
    - static/app.js contains `function showPipSetupStep(step)` that toggles display of pip-setup-step-1, pip-setup-step-2, pip-setup-step-3
    - static/app.js contains `function pipStartSetup()` that resets state, populates profiles and kundendaten history
    - static/app.js contains `function pipPopulateProfiles()` that reads window._allProfiles
    - static/app.js contains `window._pipState = 'setup'` initialization
    - openPipWindow requestWindow uses height: 400 (not 280)
    - pipStartSetup() is called inside openPipWindow after initPipContent
  </acceptance_criteria>

  <done>PiP state machine skeleton is functional: opening PiP shows setup step 1 (mode selection), state transitions between setup/live/postcall are wired via setPipState().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PiP JS -> Backend API | All API calls go through existing authenticated endpoints (session cookie) |
| localStorage -> PiP | Kundendaten history read from localStorage (user-controlled data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.17-01 | Spoofing | Profile list in window._allProfiles | accept | Data is server-rendered from authenticated session; no user input involved |
| T-04.17-02 | Information Disclosure | Kundendaten in localStorage | accept | localStorage is per-origin, same security as existing session data; no PII beyond what user types; max 5 entries |
| T-04.17-03 | Tampering | localStorage kundendaten history | mitigate | Parse with try/catch, validate structure before use, treat as untrusted input (already in code pattern) |
</threat_model>

<verification>
- Open /live page in Chrome, click PiP button
- PiP window opens at 380x400px showing Mode selection (Cold Call / Meeting)
- Setup panels have dark theme styling consistent with existing PiP
- Profile dropdown is populated with user's profiles
</verification>

<success_criteria>
- PiP opens in setup state (not live state)
- Three setup steps exist in HTML (modus, kundendaten, skript)
- Postcall section exists with score, tags, and action buttons
- Profile list is available as window._allProfiles
- State machine functions (setPipState, showPipSetupStep) toggle visibility correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04.17-pip-launcher-inserted/04.17-01-SUMMARY.md`
</output>
