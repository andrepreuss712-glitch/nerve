---
phase: 04.17-pip-launcher-inserted
plan: 01
subsystem: ui
tags: [pip, vanilla-js, state-machine, flask-template, document-pip-api]

# Dependency graph
requires:
  - phase: 04.13
    provides: precall_service.ist_verfuegbar() and precall_verfuegbar template flag
provides:
  - PiP window restructured into 3 sections: setup (3-step wizard), live (existing tabs), postcall
  - PiP state machine (setPipState, showPipSetupStep, pipStartSetup) in app.js
  - Profile list embedded as window._allProfiles via profiles_for_pip template variable
  - Postcall panel scaffolding with score, tags, and action buttons
affects:
  - 04.17-02 (Plan 02 wires behavior: pipSelectMode, pipSubmitKundendaten, pipStartCall, pipBeendenCall)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PiP section state machine: setPipState() toggles display of pip-section-setup/live/postcall"
    - "Setup wizard steps: showPipSetupStep(N) shows one of 3 pip-setup-step-N divs"
    - "localStorage kundendaten history fed into HTML datalist elements for native browser autocomplete"
    - "Profile list embedded at page load via Flask template variable (window._allProfiles) — avoids extra AJAX request"

key-files:
  created: []
  modified:
    - templates/app.html
    - static/app.js
    - routes/app_routes.py

key-decisions:
  - "Wrap existing PiP live-coaching content inside pip-section-live (not deleted/modified) per D-12/D-13"
  - "Embed profiles as window._allProfiles at page load via Flask template variable instead of new AJAX endpoint"
  - "Use window._activeProfileId (server-rendered) for profile dropdown default selection"
  - "T-04.17-03 mitigation: localStorage kundendaten history parsed with try/catch + Array.isArray validation"

patterns-established:
  - "setPipState(state): single function controls all section visibility, follows existing _pipWindow/_pipTabLocked pattern"
  - "pipPopulateKundendatenHistory(): always try/catch + validate before use (treats localStorage as untrusted)"

requirements-completed: []

# Metrics
duration: 18min
completed: 2026-04-13
---

# Phase 04.17 Plan 01: PiP Launcher HTML/CSS/JS Scaffolding Summary

**PiP window restructured into setup/live/postcall state machine with 3-step pre-call wizard, profile dropdown, and postcall score panel — all existing live-coaching tabs preserved unchanged**

## Performance

- **Duration:** 18 min
- **Started:** 2026-04-13T13:18:00Z
- **Completed:** 2026-04-13T13:36:26Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `#pip-window` now has three distinct sections: `pip-section-setup` (3-step wizard), `pip-section-live` (existing tabs, untouched), `pip-section-postcall` (score + tags + buttons)
- PiP state machine in app.js: `setPipState()`, `showPipSetupStep()`, `pipStartSetup()`, `pipPopulateProfiles()`, `pipPopulateKundendatenHistory()`
- `openPipWindow()` now requests 380x400px (was 280) and calls `pipStartSetup()` on open — PiP launches directly into mode selection
- Profile list available as `window._allProfiles` (server-rendered), active profile pre-selected via `window._activeProfileId`

## Task Commits

1. **Task 1: Add PiP Launcher HTML panels, CSS, and profile embedding** - `0524576` (feat)
2. **Task 2: Add PiP state machine and launcher functions to app.js** - `bedfa48` (feat)

## Files Created/Modified
- `templates/app.html` - Restructured #pip-window with 3 sections, CSS for all new components, window._allProfiles/_activeProfileId/_precallVerfuegbar script block
- `static/app.js` - PiP state machine functions before getPipElement, openPipWindow height + pipStartSetup call, pagehide state reset
- `routes/app_routes.py` - profiles_for_pip variable + added to render_template call

## Decisions Made
- Wrapped existing live-coaching HTML inside `pip-section-live` rather than touching individual elements — zero risk of breaking existing coaching UI per D-12/D-13
- Used `window._activeProfileId` (server-rendered integer) instead of DOM attribute query for profile dropdown default — cleaner and reliable across PiP document move
- `pipPopulateKundendatenHistory()` uses HTML `<datalist>` elements (native browser autocomplete) per research recommendation — no custom dropdown CSS needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## Known Stubs
- `pipSelectMode()`, `pipSubmitKundendaten()`, `pipStartCall()`, `pipBeendenCall()`, `pipNextCall()`, `pipShowDetails()`, `pipStartPrecall()`, `pipConsentAccept()`, `pipConsentReject()` — onclick handlers referenced in HTML but not yet defined. These are intentional stubs; Plan 02 wires all behavior.
- `pip-postcall-score` shows "--" (placeholder) — score calculation implemented in Plan 02.

## Next Phase Readiness
- All scaffolding in place for Plan 02 to wire behavior
- Plan 02 needs to implement: pipSelectMode, pipSubmitKundendaten, pipStartCall, pipBeendenCall, pipNextCall, pipShowDetails, pipConsentAccept/Reject, pipStartPrecall
- No blockers

---
*Phase: 04.17-pip-launcher-inserted*
*Completed: 2026-04-13*
