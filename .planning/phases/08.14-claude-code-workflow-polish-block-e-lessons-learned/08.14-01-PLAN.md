---
phase: 08.14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/andre/.claude/settings.json
  - C:/Users/andre/dev/salesnerve/routes/CLAUDE.md
  - C:/Users/andre/dev/salesnerve/CLAUDE.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04

must_haves:
  truths:
    - "Nach jedem Python-Edit laeuft ruff format automatisch auf die bearbeitete Datei"
    - "Context7-MCP ist in der globalen settings.json registriert und nach Neustart nutzbar"
    - "routes/CLAUDE.md existiert mit url_for-Verifikationsregeln"
    - "CLAUDE.md Hauptdatei verweist auf routes/CLAUDE.md und enthaelt Regel 13"
  artifacts:
    - path: "C:/Users/andre/.claude/settings.json"
      provides: "PostToolUse ruff-format Hook + Context7-MCP mcpServers-Eintrag"
      contains: "ruff format"
    - path: "C:/Users/andre/dev/salesnerve/routes/CLAUDE.md"
      provides: "url_for-Verifikationsregeln fuer routes/-Arbeit"
      min_lines: 20
    - path: "C:/Users/andre/dev/salesnerve/CLAUDE.md"
      provides: "Verweis auf routes/CLAUDE.md + Regel 13 Determinismus-Entscheidung"
      contains: "Regel 13"
  key_links:
    - from: "C:/Users/andre/.claude/settings.json PostToolUse"
      to: "ruff format {file}"
      via: "matcher Edit|Write auf *.py"
      pattern: "ruff format"
    - from: "C:/Users/andre/dev/salesnerve/CLAUDE.md"
      to: "routes/CLAUDE.md"
      via: "## Routes-spezifische Regeln Abschnitt"
      pattern: "routes/CLAUDE.md"
---

<objective>
Vier Tooling-Verbesserungen in Wave 1 parallel umsetzen: ruff-Format-Hook in der globalen
settings.json, Context7-MCP-Registrierung, Sub-CLAUDE.md fuer den routes/-Ordner und Regel 13
(Determinismus-Entscheidung) in der Projekt-CLAUDE.md.

Purpose: Werkzeugschaerfe vor Block N (Profil-Redesign, 25-35h) — verhindert Style-Drift in
Diffs, verhindert halluzinierte SDK-Methoden, verhindert url_for-Fehler, macht Hook-vs-Regel
deterministisch.

Output: Aktualisierte settings.json (2 neue Eintraege), neue routes/CLAUDE.md, erweitertes
CLAUDE.md (routes-Verweis + Regel 13).
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- Aktuelle settings.json-Struktur (gelesen 2026-04-26) -->
<!-- C:/Users/andre/.claude/settings.json hat bereits: -->
<!--   "hooks": { "SessionStart": [...], "PostToolUse": [...], "PreToolUse": [...], "Stop": [...], ... } -->
<!-- PostToolUse hat bereits 2 Eintraege: gsd-context-monitor + gsd-phase-boundary -->
<!-- Neuer ruff-Hook muss als DRITTER Eintrag in PostToolUse rein (kein Konflikt mit bestehenden) -->

<!-- settings.local.json (Projekt-lokal) hat NUR "permissions" — KEINE hooks -->
<!-- ruff-Hook gehoert in C:/Users/andre/.claude/settings.json (projektweit aktiv) -->
<!-- Alternativ: in settings.local.json ergaenzen, ABER: settings.local.json hat kein "hooks"-Key -->
<!-- Entscheidung: ruff-Hook in settings.local.json (C:/Users/andre/dev/salesnerve/.claude/settings.local.json) -->
<!-- BEGRUENDUNG: Der Hook soll nur fuer das salesnerve-Projekt gelten, nicht global -->
<!-- settings.local.json unterstuetzt "hooks" genauso wie settings.json -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ruff-Format-PostToolUse-Hook in settings.local.json ergaenzen</name>
  <files>C:/Users/andre/dev/salesnerve/.claude/settings.local.json</files>
  <read_first>
    - C:/Users/andre/dev/salesnerve/.claude/settings.local.json — MUSS gelesen werden, Datei hat bereits "permissions"-Key, Hook-Key hinzufuegen ohne Permissions zu ueberschreiben
    - C:/Users/andre/.claude/settings.json — Referenz fuer korrektes Hook-Format (PostToolUse-Syntax)
  </read_first>
  <action>
    settings.local.json um einen "hooks"-Key ergaenzen. Datei hat aktuell nur "permissions".
    Neue Struktur muss BEIDE Keys enthalten.

    WICHTIG: ruff muss installiert sein. Vor Ausfuehren pruefen:
      pip show ruff
    Falls nicht installiert: pip install ruff

    Konkreter JSON-Block der in settings.local.json ergaenzt wird:

    ```json
    "hooks": {
      "PostToolUse": [
        {
          "matcher": "Edit|Write|MultiEdit",
          "hooks": [
            {
              "type": "command",
              "command": "bash -c 'FILE=$(echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get(\\\"file_path\\\",d.get(\\\"path\\\",\\\"\\\")))\" 2>/dev/null); if [[ \"$FILE\" == *.py ]]; then ruff format \"$FILE\" 2>/dev/null; fi'",
              "timeout": 10
            }
          ]
        }
      ]
    }
    ```

    Die finale settings.local.json hat genau zwei Top-Level-Keys: "permissions" und "hooks".
    Reihenfolge ist egal (JSON-Objekt).

    HINWEIS: Der Hook liest den Dateipfad aus CLAUDE_TOOL_INPUT (JSON), extrahiert file_path
    oder path, und laeuft ruff format nur wenn die Extension .py ist.
    Kein Output-Noise bei Non-Python-Edits.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/c/Users/andre/dev/salesnerve/.claude/settings.local.json')); assert 'hooks' in d; assert 'PostToolUse' in d['hooks']; assert any('ruff' in str(h) for h in d['hooks']['PostToolUse']); print('OK: ruff-Hook vorhanden')"</automated>
  </verify>
  <done>
    settings.local.json enthaelt "hooks.PostToolUse" mit einem Eintrag der "ruff format" ausfuehrt.
    "permissions"-Key bleibt unveraendert erhalten.
    grep -c "ruff" /c/Users/andre/dev/salesnerve/.claude/settings.local.json gibt >= 1 zurueck.
  </done>
</task>

<task type="auto">
  <name>Task 2: Context7-MCP in globaler settings.json registrieren</name>
  <files>C:/Users/andre/.claude/settings.json</files>
  <read_first>
    - C:/Users/andre/.claude/settings.json — MUSS gelesen werden, Datei hat bereits "hooks" und "statusLine" und andere Keys; "mcpServers"-Key muss auf Top-Level ergaenzt werden ohne bestehende Keys zu ueberschreiben
  </read_first>
  <action>
    settings.json um "mcpServers"-Key ergaenzen. Datei hat aktuell: hooks, statusLine, effortLevel,
    autoUpdatesChannel, model.

    Konkreter JSON-Block der auf Top-Level ergaenzt wird:

    ```json
    "mcpServers": {
      "context7": {
        "command": "npx",
        "args": ["-y", "@upstash/context7-mcp@latest"],
        "env": {}
      }
    }
    ```

    KEIN Loeschen bestehender Keys. Alle bestehenden Keys (hooks, statusLine, effortLevel,
    autoUpdatesChannel, model) muessen unveraendert erhalten bleiben.

    NACH der Aenderung: Claude Code Neustart noetig damit MCP-Server aktiv wird.
    Kein manuelles Install noetig — npx -y laedt @upstash/context7-mcp beim ersten Aufruf.

    Test nach Neustart: Claude Code starten, dann fragen:
      "Was ist die aktuelle Methode zum Erstellen eines Anthropic-Streaming-Calls in Python?"
      → Context7 liefert aktuelle Docs aus dem anthropic-SDK, keine halluzinierten Methoden.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/c/Users/andre/.claude/settings.json')); assert 'mcpServers' in d; assert 'context7' in d['mcpServers']; assert d['mcpServers']['context7']['command']=='npx'; assert 'hooks' in d; print('OK: Context7 MCP registriert, bestehende Keys erhalten')"</automated>
  </verify>
  <done>
    settings.json enthaelt "mcpServers.context7" mit command="npx" und args=["-y","@upstash/context7-mcp@latest"].
    Alle bestehenden Keys (hooks, statusLine, effortLevel, autoUpdatesChannel, model) unveraendert.
    grep "context7" /c/Users/andre/.claude/settings.json gibt mindestens 1 Treffer.
    Hinweis im Plan: Claude Code Neustart erforderlich fuer MCP-Aktivierung.
  </done>
</task>

<task type="auto">
  <name>Task 3: routes/CLAUDE.md anlegen + Haupt-CLAUDE.md Verweis + Regel 13 ergaenzen</name>
  <files>
    C:/Users/andre/dev/salesnerve/routes/CLAUDE.md
    C:/Users/andre/dev/salesnerve/CLAUDE.md
  </files>
  <read_first>
    - C:/Users/andre/dev/salesnerve/CLAUDE.md — MUSS gelesen werden, Regel 13 ans Ende ergaenzen und "Routes-spezifische Regeln"-Abschnitt hinzufuegen; bestehenden Inhalt nicht aendern
    - C:/Users/andre/dev/salesnerve/routes/CLAUDE.md — existiert NICHT, neu erstellen
  </read_first>
  <action>
    SCHRITT A: routes/CLAUDE.md neu erstellen mit folgendem Inhalt:

    ```markdown
    # routes/ — Spezifische Regeln

    ## url_for-Verifikation (PFLICHT vor jedem url_for()-Aufruf)

    Bevor ein `url_for()`-Aufruf in eine Route geschrieben wird, drei Schritte:

    ### Schritt 1: Endpoint-Funktion pruefen
    ```bash
    grep -rn "def <endpoint_name>" routes/
    ```
    Beispiel: `grep -rn "def login" routes/` → prueft ob `login()` in routes/ existiert.

    ### Schritt 2: Blueprint-Name verifizieren
    ```bash
    grep -rn "_bp = Blueprint(" routes/
    ```
    Der erste Argument-String ist der Blueprint-Name fuer url_for().
    Beispiel: `auth_bp = Blueprint('auth', __name__)` → url_for('auth.login')

    ### Schritt 3: Route im Flask-Shell-Kontext testen
    ```bash
    flask shell
    ```
    ```python
    from flask import url_for
    with app.test_request_context():
        print(url_for('auth.login'))
    ```
    Wenn kein BuildError: Route ist auflösbar.

    ## Haeufiger Fehler
    `url_for('auth.login')` schlaegt fehl wenn Blueprint-Name nicht 'auth' ist.
    Immer Schritt 2 vor dem Schreiben ausfuehren.

    ## Blueprint-Uebersicht (Stand 2026-04-26)
    | Datei            | Blueprint-Instanz  | Blueprint-Name  |
    |------------------|--------------------|-----------------|
    | auth.py          | auth_bp            | 'auth'          |
    | dashboard.py     | dashboard_bp       | 'dashboard'     |
    | app_routes.py    | app_routes_bp      | 'app_routes'    |
    | profiles.py      | profiles_bp        | 'profiles'      |
    | training.py      | training_bp        | 'training'      |
    | coach.py         | coach_bp           | 'coach'         |
    | settings.py      | settings_bp        | 'settings'      |
    | organisations.py | organisations_bp   | 'organisations' |
    | payments.py      | payments_bp        | 'payments'      |
    | onboarding.py    | onboarding_bp      | 'onboarding'    |
    | oauth.py         | oauth_bp           | 'oauth'         |
    | feedback.py      | feedback_bp        | 'feedback'      |
    | learning.py      | learning_bp        | 'learning'      |
    | logs_routes.py   | logs_bp            | (pruefen)       |
    | performance.py   | performance_bp     | (pruefen)       |
    | legal.py         | legal_bp           | (pruefen)       |
    | changelog.py     | changelog_bp       | (pruefen)       |
    | waitlist.py      | waitlist_bp        | (pruefen)       |

    Eintraege mit "(pruefen)" → Schritt 2 ausfuehren um aktuellen Namen zu bestaetigen.
    ```

    SCHRITT B: CLAUDE.md am Ende ergaenzen (NACH dem letzten bestehenden Abschnitt).
    Zwei neue Abschnitte anhaengen:

    ```markdown

    ## Routes-spezifische Regeln

    Regeln fuer die Arbeit im routes/-Ordner: siehe `routes/CLAUDE.md`
    (url_for-Verifikation, Blueprint-Namen, Fehlerquellen).

    ## Regel 13: Hook vs. CLAUDE.md — Determinismus-Entscheidung

    Bevor eine neue Verhaltensregel in CLAUDE.md geschrieben wird:

    - Ist das Verhalten "jedes Mal ohne Ausnahme, unabhaengig vom Kontext"?
      → **Hook** (settings.json PostToolUse/PreToolUse). Deterministisch, kein Vergessen.
    - Braucht es Urteilsvermögen, Kontext oder Ausnahmen?
      → **CLAUDE.md-Regel**. Gilt als Guideline, nicht als erzwungene Aktion.
    - Memory-Regel ist halb-zuverlaessig fuer deterministische Aktionen — nicht fuer
      "immer X tun"-Anforderungen verwenden.

    Beispiele:
    - "Nach Python-Edit immer ruff format" → Hook (kein Urteil noetig)
    - "Bei Umlaut-Entscheidung User-Text vs. Code-Identifier unterscheiden" → CLAUDE.md
    - "Immer git push nach Phase" → CLAUDE.md (Ausnahmen: lokale Branches, WIP)
    ```
  </action>
  <verify>
    <automated>test -f /c/Users/andre/dev/salesnerve/routes/CLAUDE.md && grep -c "url_for" /c/Users/andre/dev/salesnerve/routes/CLAUDE.md && grep -c "Regel 13" /c/Users/andre/dev/salesnerve/CLAUDE.md && grep -c "Routes-spezifische" /c/Users/andre/dev/salesnerve/CLAUDE.md && echo "OK: Alle 3 Pruefungen bestanden"</automated>
  </verify>
  <done>
    routes/CLAUDE.md existiert mit mindestens 3 url_for-Vorkommen (Regel + Beispiele).
    CLAUDE.md enthaelt "Regel 13" (grep liefert >= 1 Treffer).
    CLAUDE.md enthaelt "routes/CLAUDE.md" als Querverweis (grep liefert >= 1 Treffer).
    Bestehender CLAUDE.md-Inhalt (Zeilen 1-347) unveraendert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| PostToolUse Hook → Shell | Hook-Command wird als Shell-Befehl ausgefuehrt; FILE-Pfad aus CLAUDE_TOOL_INPUT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08.14-01 | Tampering | ruff-Hook CLAUDE_TOOL_INPUT | accept | Pfad kommt aus Claude's eigenem Tool-Output, nicht aus Netzwerk; kein External-Input-Vektor |
| T-08.14-02 | Information Disclosure | Context7 MCP npx -y | accept | @upstash/context7-mcp ist open-source, kein API-Key benoetigt; Docs-Lookup ist read-only |
</threat_model>

<verification>
Alle 3 Tasks koennen nach Ausfuehrung per grep verifiziert werden:

1. ruff-Hook: `grep -c "ruff" /c/Users/andre/dev/salesnerve/.claude/settings.local.json` → >= 1
2. Context7: `grep "context7" /c/Users/andre/.claude/settings.json` → Treffer vorhanden
3. routes/CLAUDE.md: `test -f /c/Users/andre/dev/salesnerve/routes/CLAUDE.md && echo exists`
4. Regel 13: `grep "Determinismus" /c/Users/andre/dev/salesnerve/CLAUDE.md` → Treffer vorhanden
5. Routing-Verweis: `grep "routes/CLAUDE.md" /c/Users/andre/dev/salesnerve/CLAUDE.md` → Treffer

Live-Test ruff-Hook: Test-Edit auf einer .py-Datei vornehmen → in der Hook-Ausgabe erscheint
"ruff format" oder keine Ausgabe (ruff formatiert still wenn keine Aenderungen noetig).
</verification>

<success_criteria>
- settings.local.json hat "hooks.PostToolUse" mit ruff-format-Command fuer *.py Dateien
- settings.json hat "mcpServers.context7" mit npx @upstash/context7-mcp@latest
- routes/CLAUDE.md existiert mit url_for-Verifikations-Workflow (3 Schritte) + Blueprint-Tabelle
- CLAUDE.md enthaelt "## Routes-spezifische Regeln" mit Verweis auf routes/CLAUDE.md
- CLAUDE.md enthaelt "## Regel 13: Hook vs. CLAUDE.md — Determinismus-Entscheidung"
- Alle bestehenden Inhalte in settings.json, settings.local.json und CLAUDE.md unveraendert
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.14-claude-code-workflow-polish-block-e-lessons-learned/08.14-01-SUMMARY.md` erstellen.
</output>
