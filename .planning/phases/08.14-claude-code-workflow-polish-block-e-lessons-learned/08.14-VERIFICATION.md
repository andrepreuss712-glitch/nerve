---
phase: 08.14-claude-code-workflow-polish-block-e-lessons-learned
verified: 2026-04-27T12:00:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "Context7-MCP ist in der globalen settings.json registriert und nach Neustart nutzbar"
    reason: "Claude Code Schema-Validation lehnt mcpServers als Top-Level-Key in settings.json ab. Korrekte Registrierung erfolgte via 'claude mcp add' nach ~/.claude.json — gleiches Ziel (Context7 verfuegbar nach Neustart), anderer Speicherort. Intentionale Abweichung dokumentiert in 08.14-01-SUMMARY.md Decision #2."
    accepted_by: "gsd-verifier"
    accepted_at: "2026-04-27T12:00:00Z"
re_verification: null
gaps: []
deferred: []
human_verification:
  - test: "Context7 MCP aktiv pruefen"
    expected: "'claude mcp list' zeigt context7 mit Status 'Connected'"
    why_human: "MCP-Status ist nur im laufenden Claude-Code-Prozess verifizierbar — nicht per grep/file-check"
---

# Phase 08.14: Claude-Code-Workflow-Polish + Block-E-Lessons-Learned Verification Report

**Phase Goal:** Werkzeugschaerfung vor Block N: 4 konkrete GSD-Setup-Luecken schliessen (ruff-Hook, Context7-MCP, Sub-CLAUDE.md fuer routes/, Determinismus-Regel 13) + 2 Lessons-Learned aus Block-E-Live-Deploy integrieren (ApiRate-Seeding in _migrate() + Sonnet-Date-Suffix in config.py).
**Verified:** 2026-04-27T12:00:00Z
**Status:** passed (1 human verification item for Context7 live-status)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | ruff format laeuft automatisch nach jedem Python-Edit (PostToolUse Hook) | VERIFIED | `settings.local.json` lines 57-70: `hooks.PostToolUse` with `matcher: "Edit\|Write\|MultiEdit"`, command extracts FILE from CLAUDE_TOOL_INPUT and runs `ruff format "$FILE"` if `*.py` |
| 2 | Context7-MCP ist registriert und nach Neustart nutzbar | PASSED (override) | Registriert via `claude mcp add context7` — schreibt in `~/.claude.json`. Settings.json-Schema erlaubt kein `mcpServers`-Top-Level-Key. SUMMARY dokumentiert `claude mcp list` zeigt `context7: Connected`. |
| 3 | routes/CLAUDE.md existiert mit url_for-Verifikationsregeln | VERIFIED | Datei existiert, 8+ `url_for`-Vorkommen, 3-Schritte-Workflow dokumentiert, Blueprint-Tabelle mit 20 Eintraegen (inkl. Korrektur: `orgs_bp='orgs'` statt `organisations`) |
| 4 | CLAUDE.md enthaelt Regel 13 + Verweis auf routes/CLAUDE.md | VERIFIED | `## Regel 13: Hook vs. CLAUDE.md — Determinismus-Entscheidung` vorhanden; `## Routes-spezifische Regeln` mit `routes/CLAUDE.md` Verweis vorhanden |
| 5 | app.py _migrate() seeded 8 ApiRate-Rows idempotent fuer sonnet-4-5-20251022 + haiku-4-5-20251001 | VERIFIED | app.py lines 865-892: Phase-08.14-Block mit 8 Rows (4 Sonnet + 4 Haiku je input/output/cache_read/cache_write), SELECT-vor-INSERT Idempotenz, `except Exception as _e: print(...)` non-fatal. Tabellenname `api_rates` korrekt (entspricht `ApiRate.__tablename__ = 'api_rates'`) |
| 6 | config.py alle 9 Sonnet-MODEL_*-Konstanten zeigen auf claude-sonnet-4-5-20251022 | VERIFIED | config.py lines 48-56: alle 9 MODEL_*-Konstanten haben `"claude-sonnet-4-5-20251022"` als Default. Kein nakter `"claude-sonnet-4-5"` Alias ohne Date-Suffix |

**Score:** 6/6 truths verified (1 override fuer Context7 Speicherort-Abweichung)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `/c/Users/andre/dev/salesnerve/.claude/settings.local.json` | ruff-Hook in PostToolUse | VERIFIED | `hooks.PostToolUse` mit Matcher `Edit\|Write\|MultiEdit` und ruff-command vorhanden. `permissions`-Key unveraendert erhalten. |
| `~/.claude.json` | Context7 MCP registriert (via CLI, nicht settings.json) | PASSED (override) | Via `claude mcp add` in ~/.claude.json geschrieben. Settings.json hat KEIN mcpServers-Key (korrekt — Schema wuerde es ablehnen). |
| `/c/Users/andre/dev/salesnerve/routes/CLAUDE.md` | url_for-Verifikations-Workflow + Blueprint-Tabelle | VERIFIED | 61 Zeilen, 3-Schritte-Workflow, Blueprint-Tabelle mit 20 verifizierten Eintraegen (nicht aus Plan — nach grep-Verifikation korrigiert) |
| `/c/Users/andre/dev/salesnerve/CLAUDE.md` | Regel 13 + routes/CLAUDE.md Verweis | VERIFIED | Beide Abschnitte am Dateiende angehaengt, bestehender Inhalt unveraendert |
| `/c/Users/andre/dev/salesnerve/config.py` | 9 Sonnet-Konstanten mit Date-Suffix 20251022 | VERIFIED | lines 48-56: alle 9 MODEL_* constants = `"claude-sonnet-4-5-20251022"` |
| `/c/Users/andre/dev/salesnerve/app.py` | _migrate() ApiRate-Seed-Block 8 Rows idempotent | VERIFIED | lines 865-892: 8-Row-Seed, SELECT-vor-INSERT, non-fatal exception, `_conn.commit()` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| settings.local.json PostToolUse | ruff format {FILE} | matcher Edit\|Write\|MultiEdit auf *.py | WIRED | Kommando liest FILE aus CLAUDE_TOOL_INPUT JSON, prueft `*.py` Extension, ruft `ruff format` auf |
| CLAUDE.md | routes/CLAUDE.md | `## Routes-spezifische Regeln` Abschnitt | WIRED | Expliziter Verweis: "siehe `routes/CLAUDE.md`" im Abschnitt vorhanden |
| app.py _migrate() | api_rates Tabelle | INSERT mit SELECT-Idempotenz-Check provider+model+unit_type | WIRED | `SELECT 1 FROM api_rates WHERE provider=:p AND model=:m AND unit_type=:u AND active=1` vor jedem INSERT; `api_rates` entspricht korrektem `ApiRate.__tablename__` |
| config.py MODEL_EWB | alle anthropic-SDK-Aufrufe | os.getenv() mit Default claude-sonnet-4-5-20251022 | WIRED | 9 Konstanten verfuegbar, ENV-overridable via os.getenv() |

### Data-Flow Trace (Level 4)

Nicht anwendbar — diese Phase produziert Konfigurationsdateien, Hooks und DB-Migration-Code, keine Rendering-Komponenten mit Dynamic-Data-Flow.

### Behavioral Spot-Checks

| Behavior | Evidence | Status |
|----------|----------|--------|
| ruff-Hook konfiguriert | `settings.local.json` JSON-Parse: `hooks.PostToolUse[0].hooks[0].command` enthaelt `ruff format` | PASS |
| Sonnet-Date-Suffix vollstaendig | 9 grep-Matches auf `claude-sonnet-4-5-20251022` in config.py, 0 nackte `claude-sonnet-4-5"` Aliase | PASS |
| ApiRate-Seed im _migrate()-Body | Block zwischen letztem `except: pass` (Zeile 863) und `_migrate()` Aufruf (Zeile 894) | PASS |
| Idempotenz-Pattern korrekt | `SELECT 1 FROM api_rates WHERE ... AND active=1` vor jedem INSERT | PASS |
| Non-fatal Exception | `except Exception as _e: print(...)` ohne `raise` | PASS |
| Tabellenname korrekt | `ApiRate.__tablename__ = 'api_rates'` (models.py line 581), Seed verwendet `api_rates` (plural) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| TOOL-01 | 08.14-01 | ruff-Format-Hook | SATISFIED | `settings.local.json` hooks.PostToolUse mit ruff-command |
| TOOL-02 | 08.14-01 | Context7-MCP registriert | SATISFIED (override) | Via `claude mcp add` in ~/.claude.json, nicht settings.json |
| TOOL-03 | 08.14-01 | routes/CLAUDE.md mit url_for-Regeln | SATISFIED | Datei vorhanden, Blueprint-Tabelle verifiziert und korrigiert |
| TOOL-04 | 08.14-01 | Regel 13 in CLAUDE.md | SATISFIED | `## Regel 13: Hook vs. CLAUDE.md — Determinismus-Entscheidung` vorhanden |
| CODE-01 | 08.14-02 | Sonnet-Date-Suffix in config.py | SATISFIED | 9 MODEL_*-Konstanten auf `claude-sonnet-4-5-20251022` |
| CODE-02 | 08.14-02 | ApiRate-Seed in _migrate() | SATISFIED | 8-Row idempotenter Seed vorhanden |

Hinweis: TOOL-01 bis CODE-02 sind phaseinterne IDs — sie erscheinen nicht in der globalen REQUIREMENTS.md (die PROD/INFRA/PAY/LEGAL/MIC/MODE-Requirements fuer Milestone 1 trackt). Keine REQUIREMENTS.md-Abdeckungspruefung noetig.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| app.py | 881 | `SELECT 1 FROM api_rates ... AND active=1` | Info | Die Idempotenz-Query filtert zusaetzlich auf `active=1`. Falls eine Row mit `active=0` existiert, wird sie erneut inserted. Da der Seed neue Rows mit `active=1` schreibt und kein Deaktivierungs-Use-Case im _migrate()-Kontext existiert, ist das kein Blocker. |

Keine Blocker-Anti-Patterns gefunden. Keine TODO/FIXME/Placeholder-Kommentare in den modifizierten Dateien.

### Planabweichung: Tabellenname `api_rate` vs `api_rates`

Der Plan (08.14-02-PLAN.md) sprach von `api_rate` (singular) als erwartetem Tabellennamen. Die Implementierung verwendet korrekt `api_rates` (plural), was `ApiRate.__tablename__ = 'api_rates'` in `database/models.py` line 581 entspricht. Dies ist KEIN Fehler — der Plan hatte einen falschen Tabellennamen dokumentiert; die Implementierung korrigierte das beim Schreiben.

### Planabweichung: Context7-MCP Speicherort

Der Plan sah `mcpServers` als Top-Level-Key in `C:/Users/andre/.claude/settings.json` vor. Claude Code lehnt dieses Feld als unbekanntes Schema-Feld ab. Context7 wurde stattdessen via `claude mcp add context7 -- npx -y @upstash/context7-mcp@latest` in `~/.claude.json` registriert. Das ist der korrekte Weg. Die Funktionalitaet (Context7 verfuegbar) ist identisch. Override dokumentiert oben.

### Human Verification Required

#### 1. Context7 MCP Live-Status

**Test:** In Claude Code terminal ausfuehren: `claude mcp list`
**Expected:** Output enthaelt `context7` mit Status `Connected` (oder `running`)
**Why human:** MCP-Server-Status ist nur im laufenden Claude-Code-Prozess pruefbar — nicht per file-check oder grep. Der SUMMARY-Bericht behauptet `Connected`, aber das kann sich nach Neustart aendern wenn npx-Paket nicht gecacht ist.

### Gaps Summary

Keine Gaps. Alle 6 must-have Truths sind verified oder durch Override akzeptiert. Die Phase hat ihr Ziel erreicht: Vier Tooling-Verbesserungen + ApiRate-Seed + Sonnet-Date-Suffix sind implementiert und wired.

---

_Verified: 2026-04-27T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
