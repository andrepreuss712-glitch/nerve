---
phase: "08.14"
plan: "02"
subsystem: "database-seeding, config"
tags: ["api-rate", "config", "model-constants", "migration", "seed", "sonnet", "haiku"]
dependency_graph:
  requires: []
  provides: ["ApiRate rows for sonnet-4-5-20251022 cost tracking", "Sonnet Model-IDs with date-suffix in config.py"]
  affects: ["app.py _migrate()", "config.py MODEL_* constants", "api_rate table"]
tech_stack:
  added: []
  patterns: ["idempotent SELECT-before-INSERT seed in _migrate()", "ENV-overridable model constants with date-suffix"]
key_files:
  created: []
  modified:
    - "config.py"
    - "app.py"
decisions:
  - "Alle 9 Sonnet-MODEL_*-Konstanten auf claude-sonnet-4-5-20251022 (Date-Suffix) — vermeidet Anthropic-Alias-Drift auf aeltere Modelle"
  - "ApiRate-Seed in _migrate() statt _seed_founder_dashboard_defaults() — deploy.sh ruft _migrate() auf, damit sind Preise nach jedem Deploy verfuegbar"
  - "8 Rows: 4x Sonnet + 4x Haiku (input/output/cache_read/cache_write) — vollstaendige Cache-Token-Typen fuer Cost-Tracking"
metrics:
  duration: "5min"
  completed: "2026-04-27"
  tasks_completed: 2
  files_modified: 2
---

# Phase 08.14 Plan 02: ApiRate Seed + Sonnet Date-Suffix Summary

**One-liner:** ApiRate 8-Row-Seed (sonnet-4-5-20251022 + haiku-4-5-20251001 inkl. cache_read/write) in `_migrate()` integriert; alle 9 Sonnet-MODEL_*-Konstanten in `config.py` auf vollstaendigen Date-Suffix aktualisiert.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 4 | config.py — 9 Sonnet-Konstanten auf Date-Suffix | 3a43457 | config.py |
| 5 | app.py _migrate() — ApiRate-Seed-Block einfuegen | 5495b9d | app.py |

## Verification Results

- `grep "claude-sonnet-4-5\"" config.py` → 0 Treffer (keine nackten Aliase)
- `grep -c "claude-sonnet-4-5-20251022" config.py` → 9
- `grep -c "claude-sonnet-4-5-20251022" app.py` → 4 (vier Sonnet-Rows im Seed)
- `grep -c "08.14" app.py` → 2 (Kommentar + non-fatal print)
- Seed-Block idempotent: SELECT 1 vor INSERT per provider+model+unit_type
- Exception non-fatal: `except Exception as _e: print(...)` ohne raise

## Decisions Made

1. **Date-Suffix fuer Sonnet:** `claude-sonnet-4-5` ohne Datum kann auf aeltere Modell-Versionen zeigen. Der vollstaendige Bezeichner `claude-sonnet-4-5-20251022` ist konsistent mit den Haiku-Konstanten die bereits `20251001` hatten.

2. **_migrate() statt _seed_founder_dashboard_defaults():** `deploy.sh` ruft `_migrate()` auf, nicht `_seed_founder_dashboard_defaults()`. Der Seed muss in `_migrate()` sein um nach jedem Auto-Deploy verfuegbar zu sein.

3. **8 Rows vollstaendig:** 4 Token-Typen (input/output/cache_read/cache_write) fuer beide Modelle — deckt alle Cost-Tracking-Szenarien ab incl. Prompt-Caching.

## Deviations from Plan

Keine — Plan exakt wie beschrieben ausgefuehrt.

## Known Stubs

Keine — beide Aenderungen sind vollstaendig verdrahtet.

## Threat Flags

Keine neuen Threat-Surfaces. Alle INSERT-Werte sind Literal-Konstanten im Code (kein User-Input). Idempotenz-Check per SELECT 1 verhindert Duplikate. Exception ist non-fatal per T-08.14-04.

## Self-Check: PASSED

- config.py modifiziert: FOUND
- app.py modifiziert: FOUND
- Commit 3a43457: FOUND (config.py)
- Commit 5495b9d: FOUND (app.py)
- 9 Sonnet-Konstanten mit 20251022: VERIFIED (grep -c = 9)
- 4 Sonnet-Rows im Seed: VERIFIED (grep -c = 4)
- Keine nackten Aliase: VERIFIED (grep "claude-sonnet-4-5\"" = 0 Treffer)
