---
phase: 08.14
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - C:/Users/andre/dev/salesnerve/app.py
  - C:/Users/andre/dev/salesnerve/config.py
autonomous: true
requirements:
  - CODE-01
  - CODE-02

must_haves:
  truths:
    - "app.py _migrate() seeded 8 ApiRate-Rows fuer sonnet-4-5-20251022 + haiku-4-5-20251001 mit cache_read/write idempotent"
    - "config.py alle 9 Sonnet-Konstanten referenzieren claude-sonnet-4-5-20251022 (mit Date-Suffix)"
    - "Wiederholtes Ausfuehren von _migrate() erzeugt keine Duplikate"
  artifacts:
    - path: "C:/Users/andre/dev/salesnerve/app.py"
      provides: "ApiRate-Seed-Block direkt vor der abschliessenden Zeile '_migrate()'"
      contains: "claude-sonnet-4-5-20251022"
    - path: "C:/Users/andre/dev/salesnerve/config.py"
      provides: "Alle MODEL_*-Sonnet-Konstanten mit Date-Suffix"
      contains: "claude-sonnet-4-5-20251022"
  key_links:
    - from: "app.py _migrate()"
      to: "api_rate Tabelle"
      via: "INSERT mit Idempotenz-Check SELECT 1 WHERE provider+model+unit_type"
      pattern: "claude-sonnet-4-5-20251022"
    - from: "config.py MODEL_EWB"
      to: "alle anthropic-SDK-Aufrufe im Projekt"
      via: "os.getenv() mit Default claude-sonnet-4-5-20251022"
      pattern: "MODEL_EWB.*claude-sonnet-4-5-20251022"
---

<objective>
Zwei Code-Aenderungen aus Block-E-Lessons-Learned: ApiRate-Seed fuer die neuen Modell-IDs
(sonnet-4-5-20251022 + haiku-4-5-20251001 inkl. cache-Token-Typen) in _migrate() integrieren
damit Auto-Deploy vollstaendig ist. Und alle 9 Sonnet-Konstanten in config.py auf den
vollstaendigen Date-Suffix claude-sonnet-4-5-20251022 aktualisieren (analog zu Haiku).

Purpose: deploy.sh ruft _migrate() aber NICHT _seed_founder_dashboard_defaults() — die
neuen Modell-Preise fuer Cost-Tracking fehlen nach jedem Deploy. Der Date-Suffix in config.py
ist fuer Anthropic-API-Konsistenz wichtig (Alias ohne Date kann auf aeltere Modelle zeigen).

Output: app.py mit 8-Row-ApiRate-Seed-Block am Ende von _migrate(); config.py mit
claude-sonnet-4-5-20251022 in allen 9 Sonnet-Konstanten.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/andre/dev/salesnerve/.planning/PROJECT.md
@C:/Users/andre/dev/salesnerve/.planning/ROADMAP.md
@C:/Users/andre/dev/salesnerve/.planning/STATE.md

<interfaces>
<!-- app.py _migrate() Abschluss (gelesen 2026-04-26, Zeilen 851-865): -->
<!--   # Phase 08.13: ApiCostLog Latenz + Call-Site               -->
<!--       try: ALTER TABLE api_cost_log ADD COLUMN latency_ms ... -->
<!--       except: pass                                            -->
<!--       try: ALTER TABLE api_cost_log ADD COLUMN call_site ...  -->
<!--       except: pass                                            -->
<!--   <-- HIER neuer ApiRate-Seed-Block einfuegen -->            -->
<!--   _migrate() Zeile 865 (Aufruf, ausserhalb Funktion)         -->
<!--   _seed_founder_dashboard_defaults() Zeile 868 (Definition)  -->

<!-- config.py Sonnet-Zeilen (gelesen 2026-04-26, Zeilen 48-56): -->
<!--   MODEL_EWB               = os.getenv("MODEL_EWB",               "claude-sonnet-4-5") -->
<!--   MODEL_QA                = os.getenv("MODEL_QA",                "claude-sonnet-4-5") -->
<!--   MODEL_POSTCALL_INSIGHTS = os.getenv("MODEL_POSTCALL_INSIGHTS",  "claude-sonnet-4-5") -->
<!--   MODEL_POSTCALL_ANALYSIS = os.getenv("MODEL_POSTCALL_ANALYSIS",  "claude-sonnet-4-5") -->
<!--   MODEL_WEEKLY_SUMMARY    = os.getenv("MODEL_WEEKLY_SUMMARY",     "claude-sonnet-4-5") -->
<!--   MODEL_PRECALL           = os.getenv("MODEL_PRECALL",            "claude-sonnet-4-5") -->
<!--   MODEL_CRM               = os.getenv("MODEL_CRM",               "claude-sonnet-4-5") -->
<!--   MODEL_TRAINING_HELP     = os.getenv("MODEL_TRAINING_HELP",      "claude-sonnet-4-5") -->
<!--   MODEL_TRAINING_SCORING  = os.getenv("MODEL_TRAINING_SCORING",   "claude-sonnet-4-5") -->
<!-- Haiku-Zeilen (Zeilen 58-68): bereits "claude-haiku-4-5-20251001" — unveraendert -->

<!-- ApiRate.__tablename__ verifizieren: -->
<!--   from database.models import ApiRate; print(ApiRate.__tablename__) -->
<!--   Erwarteter Output: "api_rate" (SQLAlchemy-Default lowercase) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4: config.py — 9 Sonnet-Konstanten auf Date-Suffix aktualisieren</name>
  <files>C:/Users/andre/dev/salesnerve/config.py</files>
  <read_first>
    - C:/Users/andre/dev/salesnerve/config.py Zeilen 45-70 — MUSS gelesen werden um aktuelle Werte zu sehen; nur die 9 Sonnet-Zeilen aendern, Haiku-Zeilen unveraendert lassen
  </read_first>
  <action>
    In config.py genau 9 Zeilen aendern: alle Default-Werte von "claude-sonnet-4-5" auf
    "claude-sonnet-4-5-20251022".

    Betroffene Konstanten (Zeilen 48-56):
      MODEL_EWB               → "claude-sonnet-4-5-20251022"
      MODEL_QA                → "claude-sonnet-4-5-20251022"
      MODEL_POSTCALL_INSIGHTS → "claude-sonnet-4-5-20251022"
      MODEL_POSTCALL_ANALYSIS → "claude-sonnet-4-5-20251022"
      MODEL_WEEKLY_SUMMARY    → "claude-sonnet-4-5-20251022"
      MODEL_PRECALL           → "claude-sonnet-4-5-20251022"
      MODEL_CRM               → "claude-sonnet-4-5-20251022"
      MODEL_TRAINING_HELP     → "claude-sonnet-4-5-20251022"
      MODEL_TRAINING_SCORING  → "claude-sonnet-4-5-20251022"

    UNVERAENDERT lassen:
    - Alle Haiku-Zeilen (Zeilen 58-68, bereits "claude-haiku-4-5-20251001")
    - Alle anderen config.py-Inhalte

    Kommentar in Zeile 47 aktualisieren:
      Alt: "# Sonnet 4.5 fuer User-sichtbare Outputs"
      Neu: "# Sonnet 4.5 fuer User-sichtbare Outputs (20251022)"

    Nach der Aenderung: kein "claude-sonnet-4-5" ohne Date-Suffix in config.py mehr vorhanden.
  </action>
  <verify>
    <automated>grep -c "claude-sonnet-4-5\"" /c/Users/andre/dev/salesnerve/config.py || echo "0 nackte Aliase — OK"</automated>
  </verify>
  <done>
    grep -n "claude-sonnet-4-5" /c/Users/andre/dev/salesnerve/config.py zeigt ausschliesslich
    Eintraege mit "20251022"-Suffix. Kein Eintrag endet auf claude-sonnet-4-5" (ohne Date-Suffix).
    grep -c "claude-sonnet-4-5-20251022" config.py gibt >= 9 zurueck.
  </done>
</task>

<task type="auto">
  <name>Task 5: app.py _migrate() — ApiRate-Seed-Block am Ende der Funktion einfuegen</name>
  <files>C:/Users/andre/dev/salesnerve/app.py</files>
  <read_first>
    - C:/Users/andre/dev/salesnerve/app.py Zeilen 840-870 — MUSS gelesen werden um genaue Einfuegestelle zu finden; Block kommt NACH dem letzten "except: pass" der call_site-Migration und VOR der Zeile "_migrate()" (Aufruf)
    - C:/Users/andre/dev/salesnerve/config.py Zeilen 48-56 — gelesen um Model-IDs zu bestaetigen (claude-sonnet-4-5-20251022 nach Task 4)
  </read_first>
  <action>
    In app.py den folgenden Block am Ende der _migrate()-Funktion einfuegen — NACH dem letzten
    "except: pass" (call_site-Migration) und DIREKT VOR der Leerzeile + "_migrate()"-Aufruf (Zeile 865).

    Der Block ERSETZT NICHT den bestehenden Code — er wird eingefuegt:

    ```python
        # ── Phase 08.14: ApiRate Seed fuer sonnet-4-5-20251022 + haiku-4-5-20251001 (cache_read/write) ────
        try:
            from database.models import ApiRate
            _needed = [
                ('anthropic', 'claude-sonnet-4-5-20251022', 'per_1k_input_tokens',       0.003,   'USD'),
                ('anthropic', 'claude-sonnet-4-5-20251022', 'per_1k_output_tokens',      0.015,   'USD'),
                ('anthropic', 'claude-sonnet-4-5-20251022', 'per_1k_cache_read_tokens',  0.0003,  'USD'),
                ('anthropic', 'claude-sonnet-4-5-20251022', 'per_1k_cache_write_tokens', 0.00375, 'USD'),
                ('anthropic', 'claude-haiku-4-5-20251001',  'per_1k_input_tokens',       0.00025, 'USD'),
                ('anthropic', 'claude-haiku-4-5-20251001',  'per_1k_output_tokens',      0.00125, 'USD'),
                ('anthropic', 'claude-haiku-4-5-20251001',  'per_1k_cache_read_tokens',  0.000025,'USD'),
                ('anthropic', 'claude-haiku-4-5-20251001',  'per_1k_cache_write_tokens', 0.0003,  'USD'),
            ]
            with engine.connect() as _conn:
                for _provider, _model, _unit, _price, _currency in _needed:
                    _exists = _conn.execute(
                        text("SELECT 1 FROM api_rate WHERE provider=:p AND model=:m AND unit_type=:u"),
                        {'p': _provider, 'm': _model, 'u': _unit}
                    ).fetchone()
                    if not _exists:
                        _conn.execute(
                            text("INSERT INTO api_rate (provider, model, unit_type, price_per_unit, currency, active, source_url) VALUES (:p,:m,:u,:price,:cur,1,'seed:2026-04-27')"),
                            {'p': _provider, 'm': _model, 'u': _unit, 'price': _price, 'cur': _currency}
                        )
                        print(f"[DB] ApiRate seeded: {_model} {_unit}")
                _conn.commit()
        except Exception as _e:
            print(f"[DB] ApiRate seed (08.14) failed (non-fatal): {_e}")
    ```

    WICHTIGER HINWEIS ZUR EINRUECKUNG:
    Der Block ist Teil der _migrate()-Funktion — er muss mit 4 Leerzeichen eingerueckt sein
    (gleiche Ebene wie die anderen try-Bloecke in _migrate()).

    TABELLENNAME-VERIFIKATION vor dem Schreiben:
    ApiRate.__tablename__ = "api_rate" (SQLAlchemy-Default, lowercase, verifiziert in STATE.md
    Phase-08.13-01: "ApiRate-Seeding idempotent — 6 neue Rows").

    IDEMPOTENZ-LOGIK:
    - SELECT 1 FROM api_rate WHERE provider+model+unit_type → falls Row existiert: skip
    - Falls nicht existiert: INSERT
    - commit() am Ende des with-Blocks
    - Bei Exception: non-fatal print, kein raise → verhindert App-Start-Fehler bei DB-Problemen
  </action>
  <verify>
    <automated>grep -c "claude-sonnet-4-5-20251022" /c/Users/andre/dev/salesnerve/app.py</automated>
  </verify>
  <done>
    grep "ApiRate seed (08.14)" /c/Users/andre/dev/salesnerve/app.py gibt einen Treffer.
    grep -c "claude-sonnet-4-5-20251022" app.py gibt >= 4 zurueck (4 sonnet-Zeilen im Seed).
    Idempotenz-Test (nach Ausfuehren): python3 -c "from app import _migrate; _migrate()" — kein
    "[DB] ApiRate seeded"-Output wenn Rows bereits existieren (zweiter Aufruf muss leer bleiben).
    Kein Syntax-Fehler: python3 -c "import py_compile; py_compile.compile('app.py'); print('OK')"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| _migrate() → api_rate Tabelle | SQL-INSERT mit Literal-Werten aus dem Code, kein User-Input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08.14-03 | Tampering | ApiRate-Seed SQL | accept | Alle Werte sind Literal-Konstanten im Code, kein externer Input; Idempotenz-Check per SELECT 1 verhindert Duplikate |
| T-08.14-04 | Denial of Service | _migrate() Exception | accept | except Exception mit non-fatal print — App-Start wird nicht blockiert bei seed-Fehler |
</threat_model>

<verification>
1. config.py: `grep "claude-sonnet-4-5\"" /c/Users/andre/dev/salesnerve/config.py` → 0 Treffer (keine nackten Aliase mehr)
2. config.py: `grep -c "claude-sonnet-4-5-20251022" /c/Users/andre/dev/salesnerve/config.py` → >= 9
3. app.py Syntax: `python3 -c "import py_compile; py_compile.compile('/c/Users/andre/dev/salesnerve/app.py'); print('Syntax OK')"`
4. Seed-Block vorhanden: `grep -c "08.14" /c/Users/andre/dev/salesnerve/app.py` → >= 1
5. Idempotenz lokal (optional): `python3 -c "from app import _migrate; _migrate()"` — zweiter Aufruf erzeugt keine "[DB] ApiRate seeded:"-Zeilen
</verification>

<success_criteria>
- config.py: alle 9 MODEL_*-Sonnet-Konstanten zeigen auf "claude-sonnet-4-5-20251022"
- config.py: kein "claude-sonnet-4-5" ohne Date-Suffix (grep gibt 0 Treffer fuer nackten Alias)
- app.py: _migrate() enthaelt ApiRate-Seed-Block mit 8 Rows (4 Sonnet + 4 Haiku je input/output/cache_read/cache_write)
- app.py: Seed ist idempotent (SELECT-vor-INSERT Pattern, commit() pro Batch)
- app.py: Exception non-fatal (kein raise, nur print)
- Keine Syntax-Fehler in app.py oder config.py
</success_criteria>

<output>
Nach Abschluss `.planning/phases/08.14-claude-code-workflow-polish-block-e-lessons-learned/08.14-02-SUMMARY.md` erstellen.
</output>
