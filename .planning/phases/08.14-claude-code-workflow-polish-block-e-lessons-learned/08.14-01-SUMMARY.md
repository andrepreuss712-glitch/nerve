---
phase: 08.14
plan: 01
subsystem: tooling
tags: [ruff, mcp, context7, claude-md, hooks, url_for, blueprint]
dependency_graph:
  requires: []
  provides:
    - ruff-format-hook-salesnerve
    - context7-mcp-registered
    - routes-claude-md
    - regel-13-determinismus
  affects:
    - all-python-edits-in-salesnerve
    - all-routes-work
    - future-claude-md-rules
tech_stack:
  added:
    - ruff 0.15.12 (Python formatter, installed via pip)
    - Context7 MCP (@upstash/context7-mcp@latest, registered via claude mcp add)
  patterns:
    - PostToolUse Hook fuer automatisches Formatting
    - Sub-CLAUDE.md fuer routes/-spezifische Regeln
    - Hook-vs-CLAUDE.md Determinismus-Entscheidungsbaum
key_files:
  created:
    - C:/Users/andre/dev/salesnerve/routes/CLAUDE.md
  modified:
    - C:/Users/andre/dev/salesnerve/.claude/settings.local.json
    - C:/Users/andre/dev/salesnerve/CLAUDE.md
    - ~/.claude.json (ausserhalb Repo, via claude mcp add)
decisions:
  - "[08.14-01-T1] ruff-Hook in settings.local.json (projektlokal) statt settings.json (global) — Hook soll nur fuer salesnerve gelten"
  - "[08.14-01-T2] Context7-MCP via claude mcp add statt settings.json — mcpServers ist kein gueltiges settings.json-Schema-Feld; korrekter Weg ist ~/.claude.json via CLI"
  - "[08.14-01-T3] Blueprint-Tabelle in routes/CLAUDE.md nach grep-Verifikation korrigiert — organisations.py nutzt orgs_bp='orgs', nicht 'organisations'"
metrics:
  duration: "~15 min"
  completed: "2026-04-27"
  tasks: 3
  files: 3
---

# Phase 08.14 Plan 01: Tooling-Schaerfung — ruff Hook, Context7 MCP, routes/CLAUDE.md, Regel 13 Summary

ruff-Format-Hook (projektlokal), Context7-MCP-Registrierung, routes/CLAUDE.md mit verifizierten Blueprint-Namen und Regel 13 (Hook-vs-CLAUDE.md-Determinismus) in der Haupt-CLAUDE.md.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | ruff-Format-PostToolUse-Hook in settings.local.json | 320d435 | `.claude/settings.local.json` |
| 2 | Context7-MCP registrieren (claude mcp add) | 60bf905 | `~/.claude.json` (ausserhalb Repo) |
| 3 | routes/CLAUDE.md + Regel 13 in CLAUDE.md | 3dde8ee | `routes/CLAUDE.md`, `CLAUDE.md` |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] mcpServers kein gueltiges settings.json-Schema-Feld**
- **Found during:** Task 2
- **Issue:** Claude Code Schema-Validation wuerde `mcpServers` als Top-Level-Key in `settings.json` ablehnen (Unrecognized field). Plan sah direktes Hinzufuegen in settings.json vor.
- **Fix:** Context7 via `claude mcp add context7 -- npx -y @upstash/context7-mcp@latest` registriert. Eintrag landet in `~/.claude.json` unter dem salesnerve-Projekt-Key. `claude mcp list` zeigt `context7: Connected`.
- **Files modified:** `~/.claude.json` (ausserhalb Git-Repo, nicht versioniert)
- **Commit:** 60bf905 (empty commit als Dokumentation)

**2. [Rule 1 - Bug] Blueprint-Namen in Tabelle nicht verifiziert**
- **Found during:** Task 3, beim grep auf routes/
- **Issue:** Plan-Tabelle hatte `organisations_bp = Blueprint('organisations', ...)` — tatsaechlich ist es `orgs_bp = Blueprint('orgs', ...)`. Auch `admin_dashboard.py` und `admin_ewb.py` waren nicht im Plan.
- **Fix:** Tabelle in routes/CLAUDE.md nach `grep -rn "_bp = Blueprint(" routes/` verifiziert und korrigiert. `orgs`-Eintrag korrekt, Admin-Blueprints als `(pruefen)` hinzugefuegt.
- **Files modified:** `routes/CLAUDE.md`
- **Commit:** 3dde8ee

## Decisions Made

1. **ruff-Hook projektlokal (settings.local.json):** Der Hook soll nur im salesnerve-Kontext laufen, nicht bei jedem Python-Edit in anderen Projekten. settings.local.json ueberschreibt settings.json projektspezifisch.

2. **Context7-MCP via CLI statt settings.json:** Das Claude-Code-Schema erlaubt `mcpServers` nicht als Top-Level-Key in settings.json. Der korrekte Weg ist `claude mcp add` — das schreibt in `~/.claude.json`. Kein Neustart noetig, MCP ist sofort aktiv.

3. **Blueprint-Tabelle nach Verifikation:** Alle 20 route-Dateien per grep geprueft. Kritische Korrektur: `organisations.py` hat Blueprint-Name `'orgs'`, nicht `'organisations'`. Fehler-Quelle fuer url_for()-Bugs dokumentiert.

## Known Stubs

Keine — alle 3 Artifacts sind vollstaendig und einsatzbereit.

## Threat Flags

Keine neuen sicherheitsrelevanten Surfaces eingefuehrt. Der ruff-Hook liest nur aus CLAUDE_TOOL_INPUT (Claude-internem Output) — kein externer Input-Vektor (T-08.14-01 accept bestaetigt).

## Self-Check: PASSED

- `routes/CLAUDE.md` existiert: ja
- `grep -c "url_for" routes/CLAUDE.md`: 8 Treffer
- `grep -c "Regel 13" CLAUDE.md`: 1 Treffer
- `grep -c "Routes-spezifische" CLAUDE.md`: 1 Treffer
- `grep -c "ruff" .claude/settings.local.json`: >= 1 Treffer
- Context7 MCP: `claude mcp list` zeigt `Connected`
- Commits: 320d435, 60bf905, 3dde8ee vorhanden
