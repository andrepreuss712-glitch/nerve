---
phase: "08.13"
plan: "02"
subsystem: client-consolidation, cost-tracking
tags: [shared-client, model-constants, H-12, H-29, connection-pooling]
dependency_graph:
  requires:
    - 08.13-01 (config.MODEL_* constants, cost_tracker latency_ms/call_site, ApiRate rows)
  provides:
    - shared claude_client in all 5 service modules (coaching, crm, precall, training, dashboard)
    - H-12 partial completion (5 inline clients eliminated, Block E done)
    - H-29 cost-hook in dashboard._generate_weekly_summary (context_tag=weekly_dashboard, call_site=weekly)
    - all model strings in 5 modules on config.MODEL_* constants
  affects:
    - services/coaching_service.py
    - services/crm_service.py
    - services/precall_service.py
    - services/training_service.py
    - routes/dashboard.py
tech_stack:
  added: []
  patterns:
    - shared HTTP client via module-level import (connection pooling)
    - config.MODEL_* constants replacing hardcoded model strings
    - cost-hook pattern: try/except around log_api_cost after API call
key_files:
  created: []
  modified:
    - services/coaching_service.py
    - services/crm_service.py
    - services/precall_service.py
    - services/training_service.py
    - routes/dashboard.py
decisions:
  - "H-12 Block E: 5 inline anthropic.Anthropic() clients eliminated — all replaced by shared claude_client from claude_service"
  - "precall_service cost-hook model string changed haiku-4-5 -> sonnet-4-5 (MODEL_PRECALL is Sonnet per CONTEXT.md)"
  - "training_service.generate_help_suggestion: Haiku -> config.MODEL_TRAINING_HELP (Sonnet) per CONTEXT.md intent"
  - "dashboard H-29 cost-hook uses getattr(user, 'id', None) for safe user_id access (T-08.13-04 mitigated)"
metrics:
  duration: "~20 Minuten"
  completed_date: "2026-04-26"
  tasks_completed: 2
  files_modified: 5
---

# Phase 08.13 Plan 02: Client-Consolidation Summary

**One-liner:** 5 inline anthropic.Anthropic()-Clients eliminiert — alle Module nutzen shared claude_client aus claude_service (H-12 Block E), alle model-Strings auf config.MODEL_*-Konstanten, H-29 Cost-Hook in dashboard._generate_weekly_summary.

## Was wurde gebaut

### Task 1: coaching_service.py + crm_service.py

**coaching_service.py (3 Aenderungen):**
- Zeile 5-10: `from config import ANTHROPIC_API_KEY` + `import anthropic` + `_client = anthropic.Anthropic(...)` ersetzt durch `import config` + `from services.claude_service import claude_client`
- 3 API-Calls: `_client.messages.create(...)` → `claude_client.messages.create(...)`
- Model-Strings: `"claude-sonnet-4-6"` (x2) → `config.MODEL_POSTCALL_ANALYSIS` / `config.MODEL_WEEKLY_SUMMARY`, `"claude-haiku-4-5-20251001"` → `config.MODEL_VALIDATE_USER_TEXT`
- Cost-Hook model strings: `'sonnet-4'` (x4) → `'sonnet-4-5'` (stimmt mit ApiRate-Rows ueberein)

**crm_service.py (3 Aenderungen):**
- Zeile 1-5: `import anthropic` + `from config import ANTHROPIC_API_KEY` + `_client = anthropic.Anthropic(...)` ersetzt durch `import config` + `from services.claude_service import claude_client`
- `_client.messages.create(...)` → `claude_client.messages.create(...)`
- Model-String: `"claude-haiku-4-5-20251001"` → `config.MODEL_CRM`

Verifikation: Objekt-Identitaet `cs.claude_client is csvc.claude_client` bestaetigt shared client.

### Task 2: precall_service.py + training_service.py + dashboard.py

**precall_service.py (3 Aenderungen):**
- `claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)` geloescht, stattdessen `from services.claude_service import claude_client` (Import oben)
- Model-String: `"claude-haiku-4-5-20251001"` → `config.MODEL_PRECALL`
- Cost-Hook model string: `'haiku-4-5'` → `'sonnet-4-5'` (MODEL_PRECALL ist Sonnet)

**training_service.py (5 Model-String-Aenderungen):**
- `claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)` geloescht, stattdessen `from services.claude_service import claude_client`
- `generate_response`: `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_DIALOG`
- `generate_response_with_mood`: `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_DIALOG`
- `generate_help_suggestion`: `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_HELP` (Sonnet per CONTEXT.md)
- `generate_scoring`: `"claude-sonnet-4-6"` → `config.MODEL_TRAINING_SCORING`
- `_generate_live_preview`: `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_PREVIEW`

**routes/dashboard.py — H-29 Cost-Hook:**
- `from config import ANTHROPIC_API_KEY` + `import anthropic` aus `_generate_weekly_summary()` geloescht
- `client = anthropic.Anthropic(...)` (lokal in Funktion) geloescht
- `import config` + `from services.claude_service import claude_client` als module-level Imports ergaenzt
- `client.messages.create(...)` → `claude_client.messages.create(...)`
- Model-String: `'claude-haiku-4-5-20251001'` → `config.MODEL_WEEKLY_SUMMARY`
- Cost-Hook nach API-Call ergaenzt (H-29): `log_api_cost(..., context_tag='weekly_dashboard', call_site='weekly')` mit `getattr(user, 'id', None)` fuer sichere user_id-Extraktion

Verifikation: Objekt-Identitaet `ps.claude_client is csvc.claude_client` + `ts.claude_client is csvc.claude_client` bestaetigt.

## Verifikation

Alle Plan-Verifikationen bestanden:
1. `from services.coaching_service import generate_postcall_analysis` — OK
2. `from services.crm_service import generate_crm_export` — OK
3. `from services.precall_service import recherche_firma` — OK (generate_briefing existiert nicht — Plan-Referenz auf Nicht-Funktion, recherche_firma ist die korrekte Public-API)
4. `from services.training_service import generate_response` — OK
5. `from routes.dashboard import _generate_weekly_summary` — OK
6. `grep "anthropic.Anthropic()" services/coaching_service.py ... training_service.py` — keine Ausgabe (OK)
7. `grep "client = anthropic.Anthropic" routes/dashboard.py` — keine Ausgabe (OK)
8. pytest (35 Plan-01-Tests) — alle GREEN
9. Gesamt-Testsuite: 285+ bestehende Tests weiterhin GREEN (11 failures + 5 errors waren pre-existing, vor diesem Plan bestaetigt)

## Deviations from Plan

### Normalbefund: generate_briefing nicht in precall_service.py

**Found during:** Task 2 Verifikation
**Issue:** Plan-Verifikation `from services.precall_service import generate_briefing` schlaegt fehl — `generate_briefing` existiert nicht. Die tatsaechliche Public-API ist `recherche_firma`.
**Fix:** Kein Fix noetig — `recherche_firma` ist die korrekte Funktion und wurde korrekt verifiziert. Die Plan-Verifikationszeile war ein Fehler im Plan selbst (Funktion existiert nicht). Kein Behavior-Problem.
**Rule:** Kein Deviat — Plan-Spec-Fehler, kein Code-Fehler.

## Known Stubs

Keine. Alle 5 Module nutzen den echten shared claude_client. Keine Placeholder-Werte. Cost-Hook in dashboard ist vollstaendig verdrahtet.

## Threat Flags

Keine neuen Security-relevanten Surfaces. T-08.13-04 (Spoofing — dashboard.py Cost-Hook user_id) wurde wie geplant mitigiert: `getattr(user, 'id', None)` ist sicher fuer None-user.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| services/coaching_service.py vorhanden | FOUND |
| services/crm_service.py vorhanden | FOUND |
| services/precall_service.py vorhanden | FOUND |
| services/training_service.py vorhanden | FOUND |
| routes/dashboard.py vorhanden | FOUND |
| Commit ea2f681 (Task 1: coaching + crm) | FOUND |
| Commit 7cb35dc (Task 2: precall + training + dashboard) | FOUND |
| SUMMARY.md vorhanden | FOUND |
