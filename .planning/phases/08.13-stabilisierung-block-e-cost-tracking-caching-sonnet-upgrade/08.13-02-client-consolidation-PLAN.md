---
phase: 08.13
plan: 02
type: execute
wave: 2
depends_on:
  - 08.13-01
files_modified:
  - services/coaching_service.py
  - services/crm_service.py
  - services/precall_service.py
  - services/training_service.py
  - routes/dashboard.py
autonomous: true
requirements:
  - H-12
  - H-29
  - LB-4

must_haves:
  truths:
    - "coaching_service.py nutzt claude_client aus claude_service statt eigenen _client"
    - "crm_service.py nutzt claude_client aus claude_service statt eigenen _client"
    - "precall_service.py nutzt claude_client aus claude_service statt eigenen _client"
    - "training_service.py nutzt claude_client aus claude_service statt eigenen _client"
    - "routes/dashboard.py _generate_weekly_summary() nutzt claude_client aus claude_service statt lokalem client"
    - "Alle 5 Module importieren: from services.claude_service import claude_client"
  artifacts:
    - path: "services/coaching_service.py"
      provides: "Kein eigener anthropic.Anthropic()-Client mehr"
      contains: "from services.claude_service import claude_client"
    - path: "services/crm_service.py"
      provides: "Kein eigener anthropic.Anthropic()-Client mehr"
      contains: "from services.claude_service import claude_client"
    - path: "services/precall_service.py"
      provides: "Kein eigener anthropic.Anthropic()-Client mehr"
      contains: "from services.claude_service import claude_client"
    - path: "services/training_service.py"
      provides: "Kein eigener anthropic.Anthropic()-Client mehr"
      contains: "from services.claude_service import claude_client"
    - path: "routes/dashboard.py"
      provides: "Kein lokaler anthropic.Anthropic()-Client in _generate_weekly_summary mehr"
      contains: "from services.claude_service import claude_client"
  key_links:
    - from: "services/coaching_service.py"
      to: "services/claude_service.claude_client"
      via: "from services.claude_service import claude_client"
      pattern: "from services.claude_service import claude_client"
---

<objective>
Alle 5 inline-Anthropic-Clients auf den geteilten claude_client aus claude_service konsolidieren
(H-12-Completion). Gleichzeitig: dashboard.py _generate_weekly_summary() erhaelt Cost-Hook
(H-29-Teil 1) und alle model-Strings auf config.MODEL_*-Konstanten umstellen.

Purpose: Ein geteilter HTTP-Client bedeutet Connection-Pooling (weniger TLS-Handshakes),
konsistente API-Key-Konfiguration und ein einziger Ort fuer Client-Konfiguration.

Output:
- 5 Module entfernen ihre `anthropic.Anthropic()`-Instanziierung
- 5 Module importieren `from services.claude_service import claude_client`
- dashboard.py _generate_weekly_summary(): Cost-Hook ergaenzt, user_id als Parameter,
  model auf config.MODEL_WEEKLY_SUMMARY umgestellt
- Alle model-Strings in den 5 Modulen auf config.MODEL_*-Konstanten umgestellt
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-RESEARCH.md

<interfaces>
<!-- Aus Phase 08.13-01 SUMMARY oder direkt aus Codebase — kein Scouting noetig -->

Aus services/claude_service.py (Zeile 14 — Ziel-Client):
```python
claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)
```

Bestehende Inline-Clients die GELOESCHT werden:
- services/coaching_service.py:10: `_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)`
- services/crm_service.py:5: `_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)`
- services/precall_service.py:12: `claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)`
- services/training_service.py:10: `claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)`
- routes/dashboard.py:339 (lokal in _generate_weekly_summary): `client = anthropic.Anthropic(...)`

Aus config.py (nach Plan 08.13-01):
```python
import config
config.MODEL_WEEKLY_SUMMARY    # "claude-sonnet-4-5"
config.MODEL_POSTCALL_ANALYSIS # "claude-sonnet-4-5"
config.MODEL_VALIDATE_USER_TEXT# "claude-haiku-4-5-20251001"
config.MODEL_CRM               # "claude-sonnet-4-5"
config.MODEL_PRECALL           # "claude-sonnet-4-5"
config.MODEL_TRAINING_DIALOG   # "claude-haiku-4-5-20251001"
config.MODEL_TRAINING_HELP     # "claude-sonnet-4-5"
config.MODEL_TRAINING_SCORING  # "claude-sonnet-4-5"
config.MODEL_TRAINING_PREVIEW  # "claude-haiku-4-5-20251001"
```

Bestehende log_api_cost()-Signatur (nach Plan 08.13-01 erweitert):
```python
def log_api_cost(
    provider: str, model: str, user_id: int | None, units: float, unit_type: str,
    *, org_id=None, session_id=None, context_tag=None,
    latency_ms: int | None = None, call_site: str | None = None
) -> None
```

CIRCULAR-IMPORT CHECK (aus RESEARCH.md):
claude_service.py importiert bei Modul-Load NUR services.prompt_pipeline und
services.ewb_pipeline. coaching_service.py, crm_service.py, precall_service.py,
training_service.py importieren NICHTS aus claude_service.py.
Circular Import ist AUSGESCHLOSSEN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: coaching_service.py + crm_service.py — Client-Konsolidierung + Model-Strings</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\coaching_service.py (vollstaendig — ca. 340 Zeilen)
    - C:\Users\andre\dev\salesnerve\services\crm_service.py (vollstaendig — pruefe Zeile 5, 59-67)
  </read_first>
  <action>
**coaching_service.py — 3 Aenderungen:**

1. Zeile 10: `_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)` ERSETZEN durch:
   `from services.claude_service import claude_client`
   (Den anthropic-Import oben beibehalten NUR wenn noch anderswo in der Datei benoetigt,
   sonst `import anthropic` Zeile loeschen. `ANTHROPIC_API_KEY` Import ebenfalls
   entfernen wenn nur fuer _client genutzt.)

2. Alle `_client.messages.create(...)` durch `claude_client.messages.create(...)` ersetzen:
   - Zeile ~86 in generate_postcall_analysis()
   - Zeile ~148 in validate_user_text()
   - Zeile ~319 in get_or_generate_weekly_report()

3. Alle hardcoded model-Strings ersetzen (per RESEARCH.md verifiziert):
   - Zeile 87: `"claude-sonnet-4-6"` → `config.MODEL_POSTCALL_ANALYSIS`
   - Zeile 149: `"claude-haiku-4-5-20251001"` → `config.MODEL_VALIDATE_USER_TEXT`
   - Zeile 320: `"claude-sonnet-4-6"` → `config.MODEL_WEEKLY_SUMMARY`
   - ZUSAETZLICH: Cost-Hook model-Strings (Zeilen 96, 100, 329, 333):
     `log_api_cost('anthropic', 'sonnet-4', ...)` → `log_api_cost('anthropic', 'sonnet-4-5', ...)`
     (Model-String im Cost-Hook muss mit ApiRate.model uebereinstimmen — nach Upgrade auf 4-5)
   - `import config` am Anfang der Datei ergaenzen (nach bestehenden Imports)

**crm_service.py — 3 Aenderungen:**

1. Zeile 5: `_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)` ERSETZEN durch:
   `from services.claude_service import claude_client`

2. Zeile ~59: `_client.messages.create(...)` → `claude_client.messages.create(...)`

3. Zeile ~60: `"claude-haiku-4-5-20251001"` → `config.MODEL_CRM`
   - `import config` ergaenzen falls nicht vorhanden
   - BEACHTE: generate_crm_export() hat noch keinen Cost-Hook — dieser wird in Wave 3
     Plan 03 ergaenzt (zusammen mit user_id-Parameter). Hier nur Client + Model.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "
import services.coaching_service as cs
import services.crm_service as crm
import services.claude_service as cls
# Pruefe dass coaching_service den shared client nutzt
# (nicht pruefen via inspect.getsource — Runtime-Check via Objekt-Identitaet)
import inspect
src_cs = inspect.getsource(cs)
assert 'from services.claude_service import claude_client' in src_cs, 'coaching_service: shared client fehlt'
assert '_client = anthropic.Anthropic' not in src_cs, 'coaching_service: alter _client noch vorhanden'
src_crm = inspect.getsource(crm)
assert 'from services.claude_service import claude_client' in src_crm, 'crm_service: shared client fehlt'
assert 'claude-sonnet-4-6' not in src_cs, 'coaching_service: alter sonnet-4-6 String noch vorhanden'
print('OK: coaching_service + crm_service konsolidiert')
"</automated>
  </verify>
  <done>
    coaching_service.py: kein `_client = anthropic.Anthropic(...)` mehr. Nutzt `claude_client` aus claude_service.
    Model-Strings: "claude-sonnet-4-6" ersetzt durch config.MODEL_POSTCALL_ANALYSIS / config.MODEL_WEEKLY_SUMMARY.
    crm_service.py: kein `_client = anthropic.Anthropic(...)` mehr. Nutzt `claude_client`.
    Model-String: "claude-haiku-4-5-20251001" (CRM) ersetzt durch config.MODEL_CRM.
  </done>
</task>

<task type="auto">
  <name>Task 2: precall_service.py + training_service.py + dashboard.py — Konsolidierung + H-29 Cost-Hook</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\precall_service.py (vollstaendig — Zeilen 12, 161-180)
    - C:\Users\andre\dev\salesnerve\services\training_service.py (vollstaendig — Zeilen 10, 798-870, 985-990, 1169-1175, 1208-1245)
    - C:\Users\andre\dev\salesnerve\routes\dashboard.py (vollstaendig — Zeilen 286-360, _generate_weekly_summary)
  </read_first>
  <action>
**precall_service.py — 3 Aenderungen:**

1. Zeile 12: `claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)` LOESCHEN.
   Stattdessen: `from services.claude_service import claude_client` (als Import oben, nach
   bestehenden Imports).

2. Zeile ~161: `claude_client.messages.create(...)` bleibt (Variablenname gleich) — KEIN
   Rename noetig da precall_service.py bereits `claude_client` als Variablenname nutzt.

3. Zeile ~162: `"claude-haiku-4-5-20251001"` → `config.MODEL_PRECALL`
   Cost-Hook (Zeilen 175-180): `'haiku-4-5'` → `'sonnet-4-5'` in log_api_cost-Aufrufen
   (da MODEL_PRECALL nun Sonnet ist). `import config` ergaenzen falls nicht vorhanden.
   user_id in den log_api_cost-Aufrufen: aktuell `user_id=None` — BEACHTE: user_id-Fix
   kommt in Wave 3 Plan 03 (generate_briefing() Signatur-Erweiterung). Hier nur
   Client-Konsolidierung + Model-String.

**training_service.py — 3 Aenderungen:**

1. Zeile 10: `claude_client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)` LOESCHEN.
   Stattdessen: `from services.claude_service import claude_client` (Import ergaenzen).
   Variablenname `claude_client` bleibt gleich — alle `claude_client.messages.create(...)`
   in generate_response, generate_response_with_mood, generate_help_response, generate_scoring,
   _generate_live_preview bleiben UNVERAENDERT.

2. Alle hardcoded model-Strings ersetzen:
   - Zeile ~817 (generate_response): `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_DIALOG`
   - Zeile ~860 (generate_response_with_mood): `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_DIALOG`
   - Zeile ~986 (generate_help_response): aktuell Haiku — pruefen: laut CONTEXT.md ist
     MODEL_TRAINING_HELP Sonnet. `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_HELP`
   - Zeile ~1170 (generate_scoring): `"claude-sonnet-4-6"` → `config.MODEL_TRAINING_SCORING`
   - Zeile ~1239 (_generate_live_preview): `"claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_PREVIEW`
   - `import config` ergaenzen falls nicht vorhanden.

**routes/dashboard.py — _generate_weekly_summary() H-29 Cost-Hook (LOCKED H-29):**

1. Lokal importiertes `import anthropic` (Zeile ~301) und `client = anthropic.Anthropic(...)` (Zeile ~339)
   LOESCHEN. Stattdessen: `from services.claude_service import claude_client` als Import oben im File
   (nicht lokal in der Funktion).

2. `client.messages.create(...)` → `claude_client.messages.create(...)`

3. Zeile ~341: `"claude-haiku-4-5-20251001"` → `config.MODEL_WEEKLY_SUMMARY`

4. Cost-Hook ERGAENZEN nach dem API-Call (H-29-Fix):
   ```python
   try:
       from services.cost_tracker import log_api_cost
       _u = getattr(msg, 'usage', None)
       if _u:
           _in = getattr(_u, 'input_tokens', 0) or 0
           _out = getattr(_u, 'output_tokens', 0) or 0
           log_api_cost('anthropic', 'sonnet-4-5', user_id=getattr(user, 'id', None),
                        units=_in/1000.0, unit_type='per_1k_input_tokens',
                        context_tag='weekly_dashboard', call_site='weekly')
           log_api_cost('anthropic', 'sonnet-4-5', user_id=getattr(user, 'id', None),
                        units=_out/1000.0, unit_type='per_1k_output_tokens',
                        context_tag='weekly_dashboard', call_site='weekly')
   except Exception as _e:
       print(f"[CostHook] weekly_summary skipped: {_e}")
   ```
   `user` ist der erste Parameter von `_generate_weekly_summary(user, stats, logs)` —
   `getattr(user, 'id', None)` ist sicher (user kann None sein falls Fehler im Aufrufer).
   `import config` am Anfang des Files ergaenzen falls nicht vorhanden.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "
import inspect
import services.precall_service as ps
import services.training_service as ts
import routes.dashboard as db

src_ps = inspect.getsource(ps)
assert 'from services.claude_service import claude_client' in src_ps, 'precall_service: shared client fehlt'
assert 'claude_client = anthropic.Anthropic' not in src_ps, 'precall_service: alter client noch da'

src_ts = inspect.getsource(ts)
assert 'from services.claude_service import claude_client' in src_ts, 'training_service: shared client fehlt'
assert 'claude-sonnet-4-6' not in src_ts, 'training_service: sonnet-4-6 String noch vorhanden'

src_db = inspect.getsource(db)
assert 'from services.claude_service import claude_client' in src_db, 'dashboard: shared client fehlt'
assert 'client = anthropic.Anthropic' not in src_db, 'dashboard: lokaler client noch da'
assert 'weekly_dashboard' in src_db, 'dashboard: Cost-Hook context_tag fehlt'
print('OK: precall/training/dashboard konsolidiert')
"</automated>
  </verify>
  <done>
    precall_service.py: kein eigener Anthropic-Client mehr, nutzt shared claude_client,
    model auf config.MODEL_PRECALL.
    training_service.py: kein eigener Anthropic-Client mehr, nutzt shared claude_client,
    alle 5 model-Strings auf config.MODEL_TRAINING_* Konstanten umgestellt.
    routes/dashboard.py _generate_weekly_summary(): kein lokaler client mehr, shared
    claude_client, model auf config.MODEL_WEEKLY_SUMMARY, Cost-Hook mit context_tag='weekly_dashboard'
    und call_site='weekly' ergaenzt (H-29).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → claude_client | Alle 5 Module teilen einen HTTP-Client — ein Fehler im Client betrifft alle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.13-03 | Denial of Service | Shared claude_client | accept | Anthropic SDK hat Connection-Pooling mit Retry-Logik. Ein shared Client ist robuster als 5 separate. Kein neues Risk. |
| T-08.13-04 | Spoofing | dashboard.py Cost-Hook user_id | mitigate | `getattr(user, 'id', None)` — gibt None wenn user kein id-Attribut hat. None ist sicher (Cost-Hook schreibt dann ohne user_id). |
</threat_model>

<verification>
1. `python -c "from services.coaching_service import generate_postcall_analysis"` exits 0 (kein Import-Error)
2. `python -c "from services.crm_service import generate_crm_export"` exits 0
3. `python -c "from services.precall_service import generate_briefing"` exits 0
4. `python -c "from services.training_service import generate_response"` exits 0
5. `python -c "from routes.dashboard import _generate_weekly_summary"` exits 0
6. `grep -r "anthropic.Anthropic()" services/coaching_service.py services/crm_service.py services/precall_service.py services/training_service.py` gibt KEINE Ausgabe
7. `grep -n "client = anthropic.Anthropic" routes/dashboard.py` gibt KEINE Ausgabe
</verification>

<success_criteria>
- 5 inline-Anthropic-Clients eliminiert (H-12 vollstaendig abgeschlossen nach Block F)
- Alle 5 Module: `from services.claude_service import claude_client`
- coaching_service.py: "claude-sonnet-4-6" nicht mehr vorhanden, config.MODEL_*-Konstanten genutzt
- training_service.py: "claude-sonnet-4-6" nicht mehr vorhanden, alle 5 model-Strings auf config.*
- routes/dashboard.py: lokaler `client = anthropic.Anthropic(...)` nicht mehr vorhanden
- routes/dashboard.py _generate_weekly_summary(): Cost-Hook vorhanden mit call_site='weekly' (H-29)
- pytest noch gruen (kein Regression durch Client-Umstellung)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade/08.13-02-SUMMARY.md` erstellen.
</output>
