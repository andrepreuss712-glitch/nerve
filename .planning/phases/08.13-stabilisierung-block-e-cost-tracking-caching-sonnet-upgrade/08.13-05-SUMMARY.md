---
phase: "08.13"
plan: "05"
subsystem: deepgram-cost-tracking, model-migration
tags: [H-9, stt-seconds, deepgram, model-strings, pytest-gate, smoke-check]
dependency_graph:
  requires:
    - 08.13-01 (config.MODEL_* Konstanten, cost_tracker)
    - 08.13-02 (shared claude_client)
    - 08.13-03 (cost_tracker Signaturen)
    - 08.13-04 (POLISH-58 Prompt-Caching)
  provides:
    - deepgram_service._stt_seconds_accumulated: echte STT-Sekunden statt Socket-Lifetime
    - routes/training.py: vollstaendig auf config.MODEL_* migriert
    - Phase 08.13 abgeschlossen + git push origin main
  affects:
    - services/deepgram_service.py
    - routes/training.py
tech_stack:
  added: []
  patterns:
    - "H-9: result.metadata.duration akkumulieren bei is_final=True, stt_sek/60.0 als Cost-Hook-Input"
    - "Smoke-Check-Driven Fix: fehlende config.MODEL_*-Migration in routes/training.py nachgeholt"
key_files:
  created: []
  modified:
    - services/deepgram_service.py
    - routes/training.py
decisions:
  - "T-08.13-11 accept: Race-Condition in _stt_seconds_accumulated (Deepgram-Thread write vs. close-Thread pop) — Worst-Case < 1 Chunk verloren, Billing-Fehler minimal, Lock kann spaeter ergaenzt werden"
  - "routes/training.py Kundentyp-Generator: model=config.MODEL_TRAINING_PREVIEW (Haiku) — semantisch korrekt, da es sich um eine schnelle Generierung handelt (kein Scoring/Dialog)"
  - "pytest pre-existing failures unveraendert: test_ft_seed.py + test_ab_stats.py + test_ewb_rate_api.py + test_profile_editor_validation.py scheitern an authlib/requests ImportError — nicht durch Phase 08.13 verursacht, 295 Tests passing wie vor der Phase"
metrics:
  duration: "~15 Minuten"
  completed_date: "2026-04-26"
  tasks_completed: 2
  files_modified: 2
---

# Phase 08.13 Plan 05: Deepgram-Verifikation + Phase-Abschluss Summary

**One-liner:** H-9 behoben (Deepgram STT nutzt akkumulierte result.metadata.duration statt Socket-Lifetime), Smoke-Check-Fix in routes/training.py, pytest-Gate bestaetigt, git push origin main — Phase 08.13 abgeschlossen.

## Was wurde gebaut

### Task 1: deepgram_service.py — H-9 STT-Sekunden Fix

**Neuer Modul-Level-Dict:**
```python
_stt_seconds_accumulated = {}  # {sid: float} — H-9: echte STT-Sekunden, nicht Socket-Lifetime
```

**on_message bei is_final=True (Duration-Akkumulierung):**
- `_dur = getattr(getattr(result, 'metadata', None), 'duration', 0.0) or 0.0`
- Wenn `_dur > 0`: `_stt_seconds_accumulated[sid] += _dur`
- None-sicherer Fallback per T-08.13-12 (mitigiert)

**_close_deepgram_connection (Cost-Hook-Fix):**
- `_cost_opened_at.pop(sid, None)` beibehalten fuer sauberes Dict
- NEU: `stt_sek = _stt_seconds_accumulated.pop(sid, 0.0)`
- `minutes = stt_sek / 60.0` — kein `time.time() - opened` mehr
- Kein Cost-Log wenn keine Final-Results (minutes <= 0.01)

**Vorher (Bug):** `seconds = max(0.0, time.time() - opened)` — loggte Socket-Lifetime (Pausen + Latenz + Verbindungsaufbau)

**Nachher (Fix):** Nur tatsaechlich transkribierte Audio-Dauer wird als STT-Kosten geloggt.

### Task 2: Phase-Abschluss-Verifikation

**pytest-Gate:**
- 295 Tests passing (identisch mit Stand vor Phase 08.13)
- Pre-existing failures unveraendert: authlib/requests ImportError in test_ft_seed.py, test_ab_stats.py, test_ewb_rate_api.py, test_profile_editor_validation.py
- Keine neuen Failures durch Phase 08.13

**Smoke-Check Model-Strings:**
- `grep -rn "claude-haiku-4-5-20251001|claude-sonnet-4-6|claude-sonnet-4-5" services/ routes/` — 1 Treffer gefunden: `routes/training.py:961`
- Fix: `import config` ergaenzt, `model="claude-haiku-4-5-20251001"` → `config.MODEL_TRAINING_PREVIEW`
- Nach Fix: 0 Literal-Strings in services/ und routes/ (nur config.py als erlaubte Quelle)

**time.time()-opened Check:**
- `grep -n "time.time() - opened" services/deepgram_service.py` → kein Treffer (alter Bug entfernt)

**ApiRate-Status:** OK — in Plan 01 Task 0 verifiziert und geseedet (sonnet-4-5/haiku-4-5 inkl. cache_read/cache_write)

**git push origin main:** Ausgefuehrt — c1d3afe..047cb3f

## Phase 08.13 Gesamt-Abschluss-Nachweis

| Requirement | Status | Plan |
|-------------|--------|------|
| 21 MODEL_* + 3 CACHE_* Konstanten in config.py | OK | Plan 01 |
| Alle model-Strings auf config.MODEL_* migriert | OK | Plan 01-03 + Plan 05 Fix |
| 5 inline anthropic.Anthropic()-Clients eliminiert (H-12) | OK | Plan 02 |
| Cost-Hooks mit latency_ms + call_site (LB-4, H-29) | OK | Plan 02-03 |
| DB latency_ms + call_site Spalten | OK | Plan 01 |
| ApiRate Rows sonnet-4-5/haiku-4-5 + cache_read/write | OK | Plan 01 |
| Prompt-Caching ENV-Toggle + 16000-Threshold (POLISH-58) | OK | Plan 04 |
| Cache-Token-DB-Logging per_1k_cache_read/write_tokens | OK | Plan 04 |
| H-22 learning.py Exception-Handling | OK | Plan 03 |
| H-9 deepgram echte STT-Sekunden statt Socket-Lifetime | OK | Plan 05 |
| pytest kein neuer Failure | OK | Plan 05 |
| git push origin main | OK | Plan 05 |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Smoke-Check: routes/training.py Literal-String nicht migriert**
- **Found during:** Task 2 Smoke-Check
- **Issue:** `routes/training.py:961` hatte noch `model="claude-haiku-4-5-20251001"` als Literal; `import config` fehlte in der Datei (war in Phase 08.13 Waves 1-4 nicht bearbeitet worden)
- **Fix:** `import config` hinzugefuegt, Literal durch `config.MODEL_TRAINING_PREVIEW` ersetzt
- **Files modified:** routes/training.py
- **Commit:** 047cb3f

## Known Stubs

Keine. Alle Implementierungen sind vollstaendig verdrahtet.

## Threat Flags

Keine neuen Security-relevanten Surfaces. T-08.13-11 (Race-Condition _stt_seconds_accumulated) und T-08.13-12 (None-Fallback bei result.metadata) per Plan-Spec behandelt.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| services/deepgram_service.py vorhanden | FOUND |
| routes/training.py vorhanden | FOUND |
| _stt_seconds_accumulated in deepgram_service (3 Zeilen) | FOUND (Zeilen 13, 81, 251) |
| time.time() - opened in deepgram_service | NOT FOUND (korrekt) |
| Commit 09bd6a9 (Task 1: H-9 deepgram fix) | FOUND |
| Commit 047cb3f (Task 2: smoke-check fix + push) | FOUND |
| git push origin main | DONE (c1d3afe..047cb3f) |
| pytest 295 passing | CONFIRMED |
