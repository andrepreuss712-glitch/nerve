---
phase: 08.13
plan: 03
type: execute
wave: 3
depends_on:
  - 08.13-01
  - 08.13-02
files_modified:
  - services/claude_service.py
  - services/qa_pipeline.py
  - services/crm_service.py
  - services/precall_service.py
  - services/training_service.py
  - services/coaching_service.py
autonomous: true
requirements:
  - LB-4
  - H-22
  - H-29

must_haves:
  truths:
    - "Alle 21 hardcoded Model-Strings in claude_service.py auf config.MODEL_*-Konstanten umgestellt"
    - "qa_pipeline.py model-Strings auf config.MODEL_ANALYSE / config.MODEL_QA umgestellt"
    - "training_service.py: alle 4 Calls ohne Cost-Hooks (generate_response, generate_response_with_mood, generate_help_response, generate_scoring, _generate_live_preview) erhalten latency_ms-messende Cost-Hooks"
    - "crm_service.py: generate_crm_export() erhaelt Cost-Hook mit user_id-Parameter"
    - "precall_service.py: generate_briefing() erhaelt user_id-Parameter, Cost-Hook nutzt user_id statt None"
    - "H-22: routes/learning.py Exception-Handling geprueft und bei Bedarf korrigiert"
  artifacts:
    - path: "services/claude_service.py"
      provides: "Alle model-Strings auf config.MODEL_*"
      contains: "model=config.MODEL_ANALYSE"
    - path: "services/training_service.py"
      provides: "Cost-Hooks fuer alle 5 Training-Calls"
      contains: "log_api_cost('anthropic', 'haiku-4-5', user_id=user_id"
    - path: "services/crm_service.py"
      provides: "Cost-Hook + user_id in generate_crm_export"
      contains: "log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id"
  key_links:
    - from: "services/training_service.py:generate_response"
      to: "services/cost_tracker.py:log_api_cost"
      via: "latency_ms-messender Cost-Hook"
      pattern: "_latency_ms.*log_api_cost"
    - from: "services/crm_service.py:generate_crm_export"
      to: "services/cost_tracker.py:log_api_cost"
      via: "neuer Cost-Hook mit user_id"
      pattern: "log_api_cost.*user_id=user_id"
---

<objective>
Alle verbleibenden hardcoded Model-Strings auf config.MODEL_*-Konstanten umstellen
(claude_service.py + qa_pipeline.py) und Cost-Hook-Gaps schliessen (training_service.py,
crm_service.py, precall_service.py). H-22 Exception-Handling in learning.py pruefen.

Purpose: Nach Wave 2 haben alle Module den shared client. Wave 3 vervollstaendigt die
Model-String-Migration und schliesst die Cost-Hook-Luecken. Ohne diese Wave fehlen Billing-
Eintraege fuer Training und CRM komplett — LB-4 bleibt relevant.

Output:
- claude_service.py: alle 8 model-Strings + 2 FT-Logging-Strings auf config.MODEL_*
- qa_pipeline.py: 2 model-Strings auf config.MODEL_ANALYSE + config.MODEL_QA
- training_service.py: 5 Cost-Hooks mit latency_ms-Messung, user_id als optionaler Parameter
- crm_service.py: Cost-Hook mit user_id-Parameter in generate_crm_export()
- precall_service.py: user_id-Parameter in generate_briefing(), Cost-Hook auf user_id umgestellt
- routes/learning.py: Exception-Handling geprueft (H-22)
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-RESEARCH.md

<interfaces>
<!-- Model-String-Tabelle aus RESEARCH.md (verifiziert per grep) -->

claude_service.py model-Strings die umgestellt werden (8 API-Calls + 2 FT-Logging):
| Zeile | Aktuell | → Konstante |
|-------|---------|-------------|
| 330 | "claude-haiku-4-5-20251001" | config.MODEL_PHASE_CLASSIFY (classify_phase) |
| 402 | "claude-haiku-4-5-20251001" | config.MODEL_COLDCALL_INFER (infer_coldcall_state) |
| 481 | "claude-haiku-4-5-20251001" | config.MODEL_ANALYSE (analysiere_mit_claude) |
| 554 | "claude-haiku-4-5-20251001" | config.MODEL_PIP_AUTOVAR (streame_auto_variante) |
| 642 | "claude-haiku-4-5-20251001" | config.MODEL_PIP_VARIANTE (streame_manual_ewb_variante) |
| 711 | "claude-haiku-4-5-20251001" | config.MODEL_COACHING (analysiere_coaching) |
| 834 | "claude-haiku-4-5-20251001" | config.MODEL_ANALYSE (FT-Logging model_used-String) |
| 1351 | "claude-haiku-4-5-20251001" | config.MODEL_COACHING (FT-Logging model_used-String) |

qa_pipeline.py model-Strings:
| Zeile | Aktuell | → Konstante |
|-------|---------|-------------|
| 306 | "claude-haiku-4-5-20251001" | config.MODEL_ANALYSE (classify_utterance) |
| 410 | "claude-haiku-4-5-20251001" | config.MODEL_QA (generate_qa_response) |

Cost-Hook-Pattern (bestehend in claude_service.py, replizieren):
```python
_t0 = time.time()
response = claude_client.messages.create(...)
_latency_ms = int((time.time() - _t0) * 1000)
try:
    from services.cost_tracker import log_api_cost
    _u = getattr(response, 'usage', None)
    if _u:
        _in = getattr(_u, 'input_tokens', 0) or 0
        _out = getattr(_u, 'output_tokens', 0) or 0
        log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                     units=_in/1000.0, unit_type='per_1k_input_tokens',
                     context_tag='ewb', latency_ms=_latency_ms, call_site='ewb')
        log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                     units=_out/1000.0, unit_type='per_1k_output_tokens',
                     context_tag='ewb', call_site='ewb')
except Exception as _e:
    print(f"[CostHook] ewb skipped: {_e}")
```

call_site-Werte (normalisiert, ASCII, max 50 Zeichen):
- 'training_dialog' — generate_response/generate_response_with_mood (Haiku)
- 'training_help' — generate_help_response (Sonnet)
- 'training_scoring' — generate_scoring (Sonnet)
- 'training_preview' — _generate_live_preview (Haiku)
- 'crm' — generate_crm_export (Sonnet)
- 'precall' — generate_briefing (Sonnet, bereits context_tag='precall' vorhanden)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: claude_service.py + qa_pipeline.py model-String-Migration</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Zeilen 316-360, 395-420, 449-500, 505-590, 593-690, 702-730, 820-840, 1340-1360)
    - C:\Users\andre\dev\salesnerve\services\qa_pipeline.py (Zeilen 285-350, 353-450)
  </read_first>
  <action>
**claude_service.py — `import config` ergaenzen (falls noch nicht vorhanden nach Wave 2):**
Pruefe ob `import config` bereits am Anfang steht. Falls nicht: nach `from config import ANTHROPIC_API_KEY`
einen `import config`-Eintrag ergaenzen.

**claude_service.py — 8 model= Parameter ersetzen:**

1. Zeile ~330 (classify_phase): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_PHASE_CLASSIFY`
2. Zeile ~402 (infer_coldcall_state): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_COLDCALL_INFER`
3. Zeile ~481 (analysiere_mit_claude): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_ANALYSE`
4. Zeile ~554 (streame_auto_variante): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_PIP_AUTOVAR`
5. Zeile ~642 (streame_manual_ewb_variante): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_PIP_VARIANTE`
6. Zeile ~711 (analysiere_coaching): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_COACHING`

**claude_service.py — 2 FT-Logging model_used Strings (Zeilennummern ~834 und ~1351):**
Diese sind KEINE `model=` API-Parameter, sondern String-Argumente an `_write_ft_assistant_event()`.
Suche nach dem Pattern `model_used=` oder `'model_used': ` in den Zeilen 830-840 und 1345-1355.
Ersetze den hardcoded `"claude-haiku-4-5-20251001"` durch die jeweilige config-Konstante:
- Zeile ~834 (analyse_loop FT): → `config.MODEL_ANALYSE`
- Zeile ~1351 (coaching FT): → `config.MODEL_COACHING`

**qa_pipeline.py — `import config` ergaenzen + 2 model-Strings:**

1. `import config` am Anfang ergaenzen (pruefe ob vorhanden)
2. Zeile ~306 (classify_utterance): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_ANALYSE`
3. Zeile ~410 (generate_qa_response): `model="claude-haiku-4-5-20251001"` → `model=config.MODEL_QA`
   BEACHTE: generate_qa_response() soll Sonnet bekommen (config.MODEL_QA = "claude-sonnet-4-5").
   Das ist korrekt per CONTEXT.md D-01: QA-Response ist User-sichtbar.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "
import subprocess, sys
# Pruefe dass keine hardcoded claude-haiku/sonnet-4-6 Strings mehr in claude_service/qa_pipeline
result = subprocess.run(['grep', '-n', 'claude-haiku-4-5-20251001\|claude-sonnet-4-6',
    'services/claude_service.py', 'services/qa_pipeline.py'],
    capture_output=True, text=True, cwd='C:/Users/andre/dev/salesnerve')
lines = [l for l in result.stdout.splitlines() if not l.strip().startswith('#')]
if lines:
    print('FEHLER: hardcoded model-Strings noch vorhanden:')
    print('\n'.join(lines))
    sys.exit(1)
print('OK: keine hardcoded model-Strings in claude_service + qa_pipeline')
"</automated>
  </verify>
  <done>
    claude_service.py: alle 8 model= Parameter und 2 FT-Logging-Strings nutzen config.MODEL_*-Konstanten.
    qa_pipeline.py: classify_utterance nutzt config.MODEL_ANALYSE, generate_qa_response nutzt config.MODEL_QA.
    Kein "claude-haiku-4-5-20251001" oder "claude-sonnet-4-6" Literal mehr in diesen Dateien
    (ausser in Kommentaren).
  </done>
</task>

<task type="auto">
  <name>Task 2: training_service.py + crm_service.py + precall_service.py Cost-Hooks + H-22 Check</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\training_service.py (Zeilen 798-835, 830-880, 980-1010, 1079-1180, 1208-1260)
    - C:\Users\andre\dev\salesnerve\services\crm_service.py (vollstaendig — Zeilen 8-70)
    - C:\Users\andre\dev\salesnerve\services\precall_service.py (Zeilen 155-185)
    - C:\Users\andre\dev\salesnerve\routes\learning.py (vollstaendig — H-22 Exception-Handling)
  </read_first>
  <action>
**training_service.py — 5 Cost-Hooks ergaenzen:**

Fuer jede der 5 Funktionen: latency_ms messen, Cost-Hook nach API-Call ergaenzen.

WICHTIG — LB-4 user_id fuer Training-Service (RESOLVED in RESEARCH.md):
generate_response() und generate_response_with_mood() nehmen keinen user_id-Parameter
entgegen, da sie von Background-Threads aufgerufen werden die keinen Flask-Request-Kontext
haben. g.user und flask.session sind in diesen Threads nicht verfuegbar. Cost-Hook mit
user_id=None ist akzeptiertes Verhalten fuer diese 2 Stellen — Billing-Fehler minimal
(Training-Dialog ist Haiku, guenstig). KEIN user_id-Parameter in diese 2 Funktionen
ergaenzen — das wuerde die Signatur brechen ohne echten Nutzen.

generate_help_response(), generate_scoring(), _generate_live_preview() ebenfalls
user_id=None da diese Funktionen keinen Flask-Request-Kontext haben wenn sie aufgerufen
werden. Konsistent mit generate_response(). Alle 5 Training-Calls: user_id=None.

Muster fuer generate_response() (Haiku, Training-Dialog):
```python
import time as _time
# VOR response = claude_client.messages.create(...)
_t0 = _time.time()
response = claude_client.messages.create(
    model=config.MODEL_TRAINING_DIALOG,
    ...
)
_latency_ms = int((_time.time() - _t0) * 1000)
# NACH response:
try:
    from services.cost_tracker import log_api_cost
    _u = getattr(response, 'usage', None)
    if _u:
        _in = getattr(_u, 'input_tokens', 0) or 0
        _out = getattr(_u, 'output_tokens', 0) or 0
        log_api_cost('anthropic', 'haiku-4-5', user_id=None,
                     units=_in/1000.0, unit_type='per_1k_input_tokens',
                     context_tag='training_dialog', latency_ms=_latency_ms,
                     call_site='training_dialog')
        log_api_cost('anthropic', 'haiku-4-5', user_id=None,
                     units=_out/1000.0, unit_type='per_1k_output_tokens',
                     context_tag='training_dialog', call_site='training_dialog')
except Exception as _e:
    print(f"[CostHook] training_dialog skipped: {_e}")
```

Fuer generate_help_response() (Sonnet): `'sonnet-4-5'`, context_tag/call_site='training_help'
Fuer generate_scoring() (Sonnet): `'sonnet-4-5'`, context_tag/call_site='training_scoring'
Fuer _generate_live_preview() (Haiku): `'haiku-4-5'`, context_tag/call_site='training_preview'

`time` stdlib ist in training_service.py bereits importiert oder ergaenzen:
`import time as _time` am Anfang oder direkt `import time` wenn nicht vorhanden.

**crm_service.py — Cost-Hook + user_id:**

generate_crm_export() Signatur erweitern: `def generate_crm_export(log_entries, painpoints, einwaende, ..., user_id=None):`
(user_id am Ende als optionaler Parameter, damit Rueckwaertskompatibilitaet erhalten bleibt)

Cost-Hook nach dem API-Call ergaenzen:
```python
try:
    from services.cost_tracker import log_api_cost
    _u = getattr(msg, 'usage', None)
    if _u:
        _in = getattr(_u, 'input_tokens', 0) or 0
        _out = getattr(_u, 'output_tokens', 0) or 0
        log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                     units=_in/1000.0, unit_type='per_1k_input_tokens',
                     context_tag='crm', latency_ms=_latency_ms, call_site='crm')
        log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                     units=_out/1000.0, unit_type='per_1k_output_tokens',
                     context_tag='crm', call_site='crm')
except Exception as _e:
    print(f"[CostHook] crm skipped: {_e}")
```
Latenz-Messung: `_t0 = time.time()` vor `msg = claude_client.messages.create(...)`,
`_latency_ms = int((time.time() - _t0) * 1000)` danach.
`import time` ergaenzen falls nicht vorhanden.

**precall_service.py — user_id-Fix (LB-4):**

generate_briefing() Signatur erweitern um `user_id=None`:
`def generate_briefing(firmenname, branche, ..., user_id=None):`
Bestehende log_api_cost-Aufrufe (Zeilen ~175-180): `user_id=None` → `user_id=user_id`.
Model-String im Cost-Hook anpassen: `'haiku-4-5'` → `'sonnet-4-5'` (da MODEL_PRECALL
jetzt Sonnet ist, muss der model-String im Cost-Hook matchen).
latency_ms-Messung ergaenzen (gleiche Pattern wie oben).
call_site='precall' ergaenzen (Zeilen ~175-180 haben context_tag='precall', call_site fehlt).

**H-22 — routes/learning.py Exception-Handling pruefen:**

routes/learning.py lesen (Endpoint `/api/postcall_analysis`, POST).
Pruefen ob Exceptions korrekt als JSON-Error zurueckgegeben werden oder silent geswapt werden.
CONTEXT.md D-01 (H-22): "Silent-Swallow entfernen: bei Exception strukturierter Error
zurueck, kein leeres {'ok': True}".
Falls Exception-Handling fehlt oder silent ist: `except Exception as e: return jsonify({'ok': False, 'error': str(e)}), 500`
ergaenzen. Falls schon korrekt: nur verifizieren und in SUMMARY notieren.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && py -c "
import inspect
import services.training_service as ts
import services.crm_service as crm

# Runtime Signatur-Check (inspect.signature erlaubt per CLAUDE.md)
sig_crm = inspect.signature(crm.generate_crm_export)
assert 'user_id' in sig_crm.parameters, 'crm_service: user_id Parameter fehlt'

sig_tr = inspect.signature(ts.generate_response)
# generate_response hat keinen user_id param (per LB-4 Entscheidung: user_id=None akzeptiert fuer BG-Thread)
# Pruefe stattdessen ob die Funktion aufrufbar ist und das Modul sauber importiert
sig_help = inspect.signature(ts.generate_help_response)
assert sig_help is not None, 'training_service: generate_help_response nicht importierbar'

# Modul-Level Attribut-Check: _stt_seconds_accumulated ist in deepgram_service
# fuer training: pruefe dass cost_tracker importierbar bleibt
import services.cost_tracker as ct
sig_log = inspect.signature(ct.log_api_cost)
assert 'call_site' in sig_log.parameters, 'cost_tracker.log_api_cost: call_site Parameter fehlt (Plan 01 muss zuerst laufen)'
assert 'latency_ms' in sig_log.parameters, 'cost_tracker.log_api_cost: latency_ms Parameter fehlt (Plan 01 muss zuerst laufen)'

print('OK: crm user_id-Parameter + cost_tracker call_site/latency_ms Signatur verifiziert')
"</automated>
  </verify>
  <done>
    training_service.py: alle 5 Claude-Calls haben Cost-Hooks mit latency_ms-Messung und call_site.
    crm_service.py: generate_crm_export() hat user_id-Parameter + Cost-Hook mit call_site='crm'.
    precall_service.py: generate_briefing() hat user_id-Parameter, Cost-Hook nutzt user_id statt None,
    latency_ms + call_site='precall' ergaenzt.
    routes/learning.py: Exception-Handling geprueft, kein silent-swallow mehr (H-22).
    pytest noch gruen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route → Service (user_id) | user_id kommt aus g.user.id im Route-Handler, darf nicht aus Session in Background-Thread gelesen werden |
| training_service → cost_tracker | Training-Dialog hat keinen Flask-Session-Kontext, user_id=None ist akzeptiert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.13-05 | Information Disclosure | generate_crm_export user_id=None default | accept | user_id=None ist rueckwaertskompatibel. Aufrufer muss user_id uebergeben fuer korrekte Zuordnung — das ist Aufrufer-Verantwortung. |
| T-08.13-06 | Elevation of Privilege | H-22 learning.py Exception-Handling | mitigate | Exception-Details nicht an Frontend leaken: `'error': 'internal error'` statt `str(e)` bei produktiven Exceptions. |
</threat_model>

<verification>
1. `grep -rn "claude-haiku-4-5-20251001\|claude-sonnet-4-6" services/ routes/` — nur noch in Kommentaren, KEINE Literals in model= oder model_used= Parametern
2. `python -c "from services.training_service import generate_response"` exits 0
3. `python -c "import inspect; from services.crm_service import generate_crm_export; assert 'user_id' in str(inspect.signature(generate_crm_export))"` exits 0
4. `pytest tests/ -x -q 2>&1 | tail -5` — kein neuer Failure
5. `python -c "from routes.learning import *"` exits 0 (H-22 kein Import-Error)
</verification>

<success_criteria>
- Alle 21 hardcoded model-Strings aus RESEARCH.md-Tabelle auf config.MODEL_*-Konstanten umgestellt
- training_service.py: 5 Cost-Hooks vorhanden mit latency_ms und call_site
- crm_service.py: user_id-Parameter + Cost-Hook (LB-4 fuer CRM)
- precall_service.py: user_id-Parameter + Cost-Hook nutzt user_id (LB-4 fuer PreCall)
- H-22: routes/learning.py gibt strukturierten Error bei Exception statt silent {'ok': True}
- pytest kein neuer Failure
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade/08.13-03-SUMMARY.md` erstellen.
</output>
