---
phase: 08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade
fixed_at: 2026-04-26T13:00:00Z
review_path: .planning/phases/08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade/08.13-REVIEW.md
iteration: 1
findings_in_scope: 12
fixed: 9
skipped: 3
status: partial
---

# Phase 08.13: Code Review Fix Report

**Fixed at:** 2026-04-26T13:00:00Z
**Source review:** `.planning/phases/08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade/08.13-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 12
- Fixed: 9 (CR-01, WR-01, WR-02, WR-03, WR-04, WR-05, WR-07, IN-03, IN-04)
- Skipped: 3 (WR-06, IN-01, IN-02)

Note: IN-04 (Cache-Check prints behind flag) was resolved as part of the CR-01 edit — all `[Cache-Check]` prints were simultaneously guarded and the threshold lowered in the same code blocks. No separate commit was created for IN-04.

---

## Fixed Issues

### CR-01: Cache threshold 16000 chars never triggers — prompt caching permanently disabled

**Files modified:** `services/claude_service.py`, `services/qa_pipeline.py`
**Commit:** f962c56
**Applied fix:** Added module-level constant `_CACHE_MIN_CHARS = 4096` (with comment `# Anthropic minimum: 1024 tokens ≈ 4096 chars`) at the top of each file. Replaced all four hardcoded `>= 16000` threshold comparisons with `>= _CACHE_MIN_CHARS`. Also guarded all `[Cache-Check]` prints behind their respective cache config flags (see IN-04 below).

### WR-01: Cache token cost logged against wrong model string (sonnet-4-5 instead of haiku-4-5)

**Files modified:** `services/claude_service.py`
**Commit:** 7749eed
**Applied fix:** In `streame_auto_variante` (cache-read and cache-write rows ~lines 617-622) and `streame_manual_ewb_variante` (cache-read and cache-write rows ~lines 742-748), changed `'sonnet-4-5'` to `'haiku-4-5'` to match the actual models used (`MODEL_PIP_AUTOVAR` and `MODEL_PIP_VARIANTE`, both Haiku).

### WR-02: Same wrong-model bug in qa_pipeline.py cache token logging

**Files modified:** `services/qa_pipeline.py`
**Commit:** d4eabae
**Applied fix:** Added `_cost_model = 'sonnet-4-5' if 'sonnet' in config.MODEL_QA else 'haiku-4-5'` before the cost logging block in `generate_qa_response`. Replaced all four hardcoded model strings (regular input/output rows used `'haiku-4-5'`; cache rows used `'sonnet-4-5'`) with `_cost_model`, which correctly resolves to `'sonnet-4-5'` since `MODEL_QA = "claude-sonnet-4-5"`.

### WR-03: precall_service logs cost against hardcoded 'sonnet-4-5' — model migration miss

**Files modified:** `services/precall_service.py`
**Commit:** 75fdffb
**Applied fix:** (1) Updated module header comment from the contradictory "Kein Sonnet (CLAUDE.md Constraint: nur Haiku fuer alles Live)" to "PreCall-Briefing: einmaliger User-Trigger vor dem Anruf — Sonnet korrekt (kein Live-Loop)" to reflect the intentional architectural decision. (2) Added `_cost_model = 'sonnet-4-5' if 'sonnet' in config.MODEL_PRECALL else 'haiku-4-5'` before the `log_api_cost` calls and replaced the two hardcoded `'sonnet-4-5'` strings with `_cost_model`.

### WR-04: coaching_service cost hooks use hardcoded 'sonnet-4-5'

**Files modified:** `services/coaching_service.py`
**Commit:** 1148e3a
**Applied fix:** In `generate_postcall_analysis` (~line 94): added `_cost_model = 'sonnet-4-5' if 'sonnet' in config.MODEL_POSTCALL_ANALYSIS else 'haiku-4-5'` and replaced both `'sonnet-4-5'` literals with `_cost_model`. In `get_or_generate_weekly_report` (~line 327): added `_cost_model = 'sonnet-4-5' if 'sonnet' in config.MODEL_WEEKLY_SUMMARY else 'haiku-4-5'` and replaced both `'sonnet-4-5'` literals with `_cost_model`.

### WR-05: training_service.generate_help_suggestion cost hook hardcoded 'sonnet-4-5'

**Files modified:** `services/training_service.py`
**Commit:** 3c407c9
**Applied fix:** In `generate_help_suggestion` (~line 1032): added `_cost_model = 'sonnet-4-5' if 'sonnet' in config.MODEL_TRAINING_HELP else 'haiku-4-5'` and replaced both hardcoded strings. In `generate_scoring` (~line 1234): added `_cost_model = 'sonnet-4-5' if 'sonnet' in config.MODEL_TRAINING_SCORING else 'haiku-4-5'` and replaced both hardcoded strings.

### WR-07: deepgram_service STT accumulation race condition

**Files modified:** `services/deepgram_service.py`
**Commit:** cedfb2a
**Applied fix:** Wrapped the `_stt_seconds_accumulated[sid]` increment in `on_message` with `with _sessions_lock:`, matching the lock already used in `_close_deepgram_connection` for the `pop` operation. Eliminates the race between concurrent accumulate-in-message and pop-in-close operations.

### IN-03: learning.py exception handler swallows stacktrace

**Files modified:** `routes/learning.py`
**Commit:** 201afd9
**Applied fix:** Added `import traceback; traceback.print_exc()` inside the `except Exception` block in `api_postcall_analysis`, before the existing `print(f"[Learning] ...")` line.

### IN-04: Cache-Check prints fire regardless of cache flag

**Files modified:** `services/claude_service.py`, `services/qa_pipeline.py`
**Commit:** f962c56 (folded into CR-01)
**Applied fix:** All three `[Cache-Check]` print statements in `claude_service.py` (analyse, ewb autovar, ewb manual) and the one in `qa_pipeline.py` (qa) are now inside `if config.CACHE_ANALYSE:` / `if config.CACHE_EWB:` / `if config.CACHE_QA:` guards respectively. This was applied simultaneously with the threshold change in CR-01, so no separate commit was created.

---

## Skipped Issues

### WR-06: crm_service latency_ms only on input row

**File:** `services/crm_service.py`
**Reason:** No code change needed. The reviewer noted this as a minor inconsistency but confirmed the existing pattern in other services (e.g. `precall_service.py`) also omits `latency_ms` on the output row. The CRM input-row passing of `latency_ms` is the outlier, not a bug. The fix instructions confirmed: document as skipped/acknowledged. Latency data is captured once per API call on the input row, which is sufficient.

### IN-01: config.MODEL_* constants use non-canonical model strings (missing date suffix on Sonnet)

**File:** `config.py:48-54`
**Reason:** Cannot verify the exact Anthropic date suffix for `claude-sonnet-4-5` without a live API call or official documentation lookup. The haiku models use `claude-haiku-4-5-20251001` as the versioned form. The equivalent versioned Sonnet string would likely be `claude-sonnet-4-5-20251001`, but this requires verification against Anthropic's model list before committing. **Action required:** Verify `claude-sonnet-4-5-20251001` is a valid Anthropic model ID and update all Sonnet MODEL_* constants in `config.py` to use the date-versioned form.

### IN-02: _resolve_user_id_from_live_session duplication + user_id=None in background threads

**File:** `services/cost_tracker.py:31-48`
**Reason:** Fix instructions explicitly mark this as "Skip — no action." The reviewer noted no immediate production action is required. The fallback via `ls.state['user_id']` works correctly in all current call sites. Documenting here per instructions.

---

## pytest Results

**Command:** `python -m pytest tests/ -x -q --ignore=tests/test_ft_seed.py`
**Result:** 301 passed, 11 failed, 1 skipped, 28 errors (pre-existing)

All failures and errors are pre-existing and unrelated to this fix set:
- Import errors: `ImportError: cannot import name 'Session' from 'requests'` — authlib/requests version incompatibility affecting tests that import `app.py` directly. Pre-existing.
- `test_fail_closed_on_exception`: Expects `''` but function returns fallback Rückfrage string — pre-existing semantic mismatch in test expectation.
- `test_build_tabu_instruction_populated`: Expects `'WICHTIG:'` in output but instruction text changed in a prior phase — pre-existing content drift.
- `test_exchange_rates.*`: Unrelated to cost tracking/caching changes.

No new test failures introduced by this fix set.

---

_Fixed: 2026-04-26T13:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
