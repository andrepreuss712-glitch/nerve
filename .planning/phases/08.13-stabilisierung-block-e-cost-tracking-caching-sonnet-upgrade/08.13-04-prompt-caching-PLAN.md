---
phase: 08.13
plan: 04
type: execute
wave: 4
depends_on:
  - 08.13-03
files_modified:
  - services/claude_service.py
  - services/qa_pipeline.py
autonomous: true
requirements:
  - POLISH-58

must_haves:
  truths:
    - "EWB-Generation (streame_auto_variante + streame_manual_ewb_variante) aktiviert Caching nur wenn config.CACHE_EWB==True UND len(system_prompt) >= 16000"
    - "Analyse-Loop (analysiere_mit_claude) aktiviert Caching nur wenn config.CACHE_ANALYSE==True UND len(system_prompt) >= 16000 (Default: AUS)"
    - "QA-Response (generate_qa_response) aktiviert Caching nur wenn config.CACHE_QA==True UND len(system_prompt) >= 16000"
    - "cache_read_input_tokens und cache_creation_input_tokens werden als eigene ApiCostLog-Rows geloggt (nicht nur als Debug-Print)"
    - "Diagnostischer Print bei jedem Call: Prompt-Laenge, Threshold, Cache-Status (an/aus)"
    - "Kein anthropic-beta extra_headers noetig (SDK >= 0.40.0, GA)"
  artifacts:
    - path: "services/claude_service.py"
      provides: "Prompt-Caching mit ENV-Toggle + Threshold fuer EWB + Analyse-Loop"
      contains: "CACHE_EWB"
    - path: "services/qa_pipeline.py"
      provides: "Prompt-Caching mit ENV-Toggle + Threshold fuer QA-Response"
      contains: "CACHE_QA"
  key_links:
    - from: "services/claude_service.py:streame_auto_variante"
      to: "Anthropic API"
      via: "system=[{type:text, text:..., cache_control:{type:ephemeral}}] wenn Threshold erfuellt"
      pattern: "cache_control.*ephemeral"
    - from: "services/claude_service.py:analysiere_mit_claude"
      to: "services/cost_tracker.py:log_api_cost"
      via: "cache_read_input_tokens als per_1k_cache_read_tokens Row"
      pattern: "per_1k_cache_read_tokens"
    - from: "services/qa_pipeline.py:generate_qa_response"
      to: "services/cost_tracker.py:log_api_cost"
      via: "cache_read_input_tokens als per_1k_cache_read_tokens Row"
      pattern: "per_1k_cache_read_tokens"
---

<objective>
Prompt-Caching (POLISH-58) mit ENV-Toggle + Threshold-Check implementieren.
Cache-Token-Metriken in ApiCostLog persistieren (B1 aus Reviews).

Purpose: System-Prompt mit cache_control=ephemeral markieren wenn Threshold erfuellt →
Anthropic cached fuer 5 Minuten. Cache-Hit: 0.1x Input-Preis. Fuer 4000-Token Sonnet-Prompt:
gecacht $0.30/MT vs. ungecacht $3.00/MT. Ohne DB-Logging der Cache-Tokens: Founder hat
keinen ROI-Nachweis (Blindflug-Risiko aus REVIEWS.md).

Output:
- claude_service.py: analysiere_mit_claude(), streame_auto_variante(), streame_manual_ewb_variante()
  mit dynamischem Threshold-Check + ENV-Toggle + Cache-Token-DB-Logging
- qa_pipeline.py: generate_qa_response() mit dynamischem Threshold-Check + ENV-Toggle +
  Cache-Token-DB-Logging
- Pitfall-Dokumentation: generate_qa_response cacht nur session-intern (profile_data pro User
  unterschiedlich — Cross-Session-Cache-Hit ist NICHT erwartet, KEIN Bug)
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-RESEARCH.md

<interfaces>
<!-- SDK-Version verifiziert: anthropic>=0.40.0 in requirements.txt — kein extra_headers noetig -->

Aus config.py (nach Plan 08.13-01):
```python
import config
config.CACHE_EWB     # True  (Default)
config.CACHE_QA      # True  (Default)
config.CACHE_ANALYSE # False (Default — Analyse-Prompt kuerzer)
```

Caching-Threshold (Decision 1+2+3 aus Aufgabenbeschreibung):
- Threshold: len(system_prompt) >= 16000 Zeichen (~4000 Token)
- Cache-Write-Preis: 1.25x normal → amortisiert erst ab ~2 Calls/Session
- Falls Prompt kuerzer: system_prompt als String uebergeben (kein cache_control)

Dynamischer Cache-Check-Pattern (LOCKED per Decision 1):
```python
# In analysiere_mit_claude():
if config.CACHE_ANALYSE and len(system_prompt) >= 16000:
    system = [{"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}}]
else:
    system = system_prompt  # String, kein Cache
print(f"[Cache-Check] analyse system_prompt: {len(system_prompt)} chars, "
      f"threshold 16000, cache={'on' if config.CACHE_ANALYSE and len(system_prompt) >= 16000 else 'off'}")
```

Fuer QA (Decision 2): Gleicher Check mit `config.CACHE_QA` und `'qa'` statt `'analyse'`.
Fuer EWB (streame_auto_variante + streame_manual_ewb_variante): `config.CACHE_EWB` und `'ewb'`.

Cache-Token-DB-Logging-Pattern (B1 aus Reviews — BLOCKING):
```python
# Nach dem API-Call (im bestehenden Cost-Hook-Block):
_cache_hits = getattr(getattr(response, 'usage', None), 'cache_read_input_tokens', 0) or 0
_cache_writes = getattr(getattr(response, 'usage', None), 'cache_creation_input_tokens', 0) or 0
if _cache_hits > 0:
    log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                 units=_cache_hits/1000.0, unit_type='per_1k_cache_read_tokens',
                 context_tag='ewb', call_site='ewb')
if _cache_writes > 0:
    log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                 units=_cache_writes/1000.0, unit_type='per_1k_cache_write_tokens',
                 context_tag='ewb', call_site='ewb')
```
Model-String im Cache-Hook entspricht dem Call-Site-Modell (sonnet-4-5 fuer EWB/QA,
haiku-4-5 fuer Analyse-Loop).

Pitfall 4 (M3 aus Reviews — dokumentieren):
generate_qa_response() baut System-Prompt aus profile_data → pro User unterschiedlich.
Cache-Hit nur session-intern (gleicher User, gleiche Session, <= 5 Min TTL).
Cross-Session-Cache-Hit ist NICHT erwartet. Das ist normales Design, KEIN Bug.
Im Kommentar im Code dokumentieren.

Bestehende log_api_cost()-Signatur (nach Plan 08.13-01):
```python
def log_api_cost(
    provider, model, user_id, units, unit_type,
    *, org_id=None, session_id=None, context_tag=None,
    latency_ms=None, call_site=None
) -> None
```

ApiRate-Rows fuer cache_read/cache_write existieren (Plan 01 Task 0 hat diese angelegt).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: claude_service.py — Caching fuer EWB + Analyse-Loop (ENV-Toggle + Threshold + DB-Logging)</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Zeilen 449-510, analysiere_mit_claude — komplette Funktion)
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Zeilen 505-595, streame_auto_variante)
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Zeilen 593-695, streame_manual_ewb_variante)
    - C:\Users\andre\dev\salesnerve\requirements.txt (SDK-Version Bestaetigung)
  </read_first>
  <action>
**Vorbedingung:** `import config` muss am Anfang von claude_service.py vorhanden sein
(wurde in Wave 3 ergaenzt). Falls nicht: jetzt ergaenzen.

**1. analysiere_mit_claude() — Analyse-Loop:**

Das `claude_client.messages.create(...)` mit `system=system_prompt` finden.
VOR dem API-Call den dynamischen Cache-Check einfuegen:

```python
# ── Phase 08.13: Prompt-Caching Analyse-Loop (CACHE_ANALYSE=False default) ──
if config.CACHE_ANALYSE and len(system_prompt) >= 16000:
    _system = [{"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}}]
else:
    _system = system_prompt  # String, kein Cache
print(f"[Cache-Check] analyse system_prompt: {len(system_prompt)} chars, "
      f"threshold 16000, cache={'on' if config.CACHE_ANALYSE and len(system_prompt) >= 16000 else 'off'}")
```

`system=system_prompt` im messages.create()-Aufruf durch `system=_system` ersetzen.

Im bestehenden Cost-Hook-Block (nach dem API-Call) Cache-Token-Logging ERGAENZEN
(nach den normalen input/output Rows):
```python
        # Cache-Token-Logging (B1 Review-Finding)
        _cache_hits = getattr(getattr(response, 'usage', None), 'cache_read_input_tokens', 0) or 0
        _cache_writes = getattr(getattr(response, 'usage', None), 'cache_creation_input_tokens', 0) or 0
        if _cache_hits > 0:
            log_api_cost('anthropic', 'haiku-4-5', user_id=user_id,
                         units=_cache_hits/1000.0, unit_type='per_1k_cache_read_tokens',
                         context_tag='analyse', call_site='analyse')
        if _cache_writes > 0:
            log_api_cost('anthropic', 'haiku-4-5', user_id=user_id,
                         units=_cache_writes/1000.0, unit_type='per_1k_cache_write_tokens',
                         context_tag='analyse', call_site='analyse')
```
Model: 'haiku-4-5' (Analyse-Loop bleibt Haiku per CONTEXT.md).
Alles im bestehenden try-except-Block — kein neuer try-except noetig.

**2. streame_auto_variante() — EWB AutoVar:**

Gleicher dynamischer Cache-Check:
```python
# ── Phase 08.13: Prompt-Caching EWB AutoVar (CACHE_EWB=True default) ─────────
if config.CACHE_EWB and len(system_prompt) >= 16000:
    _system = [{"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}}]
else:
    _system = system_prompt
print(f"[Cache-Check] ewb system_prompt: {len(system_prompt)} chars, "
      f"threshold 16000, cache={'on' if config.CACHE_EWB and len(system_prompt) >= 16000 else 'off'}")
```

`system=system_prompt` → `system=_system`.

BEACHTE: Wenn streame_auto_variante() `system=` NICHT als Parameter nutzt sondern den
System-Prompt anderswo einbaut (z.B. als messages[0]):
Die Datei lesen und das tatsaechliche Format ermitteln. `system_prompt`-Variable
identifizieren. Cache-Check auf diese Variable anwenden. NICHT raten.

Cache-Token-Logging im Cost-Hook (wie analysiere_mit_claude, aber model='sonnet-4-5'
und context_tag/call_site='ewb'):
```python
        _cache_hits = getattr(getattr(response, 'usage', None), 'cache_read_input_tokens', 0) or 0
        _cache_writes = getattr(getattr(response, 'usage', None), 'cache_creation_input_tokens', 0) or 0
        if _cache_hits > 0:
            log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                         units=_cache_hits/1000.0, unit_type='per_1k_cache_read_tokens',
                         context_tag='ewb', call_site='ewb')
        if _cache_writes > 0:
            log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                         units=_cache_writes/1000.0, unit_type='per_1k_cache_write_tokens',
                         context_tag='ewb', call_site='ewb')
```

**3. streame_manual_ewb_variante() — EWB Manual:**

Gleiche Aenderungen wie streame_auto_variante(). Gleicher Cache-Check-Print mit 'ewb'.
Gleicher Cache-Token-DB-Log-Block.

**SDK-Version-Check:**
`grep anthropic requirements.txt` — `anthropic>=0.40.0` bestaetigt kein extra_headers noetig.
Falls requirements.txt eine aeltere Version zeigt: extra_headers ergaenzen. Bei >= 0.40.0:
kein extra_headers.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "
import subprocess, sys

# cache_control ist Anthropic-API-Argument das nicht ohne echten API-Call runtime-pruefbar ist.
# grep als Verifikation des strukturellen Vorhandenseins (CLAUDE.md Grenzfall: kein Runtime-Equivalent).
r = subprocess.run(['grep', '-c', 'cache_control', 'services/claude_service.py'],
    capture_output=True, text=True, cwd='C:/Users/andre/dev/salesnerve')
count = int(r.stdout.strip()) if r.returncode == 0 else 0
assert count >= 3, f'claude_service.py: cache_control nur {count}x gefunden, erwartet >= 3 (analyse + 2x ewb)'

# Cache-Token-DB-Logging: per_1k_cache_read_tokens muss in claude_service.py vorhanden sein
r2 = subprocess.run(['grep', '-c', 'per_1k_cache_read_tokens', 'services/claude_service.py'],
    capture_output=True, text=True, cwd='C:/Users/andre/dev/salesnerve')
count2 = int(r2.stdout.strip()) if r2.returncode == 0 else 0
assert count2 >= 2, f'claude_service.py: per_1k_cache_read_tokens nur {count2}x gefunden, erwartet >= 2'

# Modul importiert sauber (kein Syntax-Fehler)
import services.claude_service as cs
import inspect
assert callable(cs.analysiere_mit_claude)
assert callable(cs.streame_auto_variante)
assert callable(cs.streame_manual_ewb_variante)
print(f'OK: cache_control an {count} Stellen, per_1k_cache_read_tokens an {count2} Stellen, Modul importiert sauber')
"</automated>
  </verify>
  <done>
    claude_service.py analysiere_mit_claude(): Dynamischer Cache-Check (CACHE_ANALYSE + Threshold),
    Diagnostic-Print, system=_system, Cache-Token-DB-Logging im Cost-Hook.
    claude_service.py streame_auto_variante(): Gleiche Aenderungen mit CACHE_EWB.
    claude_service.py streame_manual_ewb_variante(): Gleiche Aenderungen mit CACHE_EWB.
    Kein extra_headers (SDK >= 0.40.0).
    Cache-Token-Rows werden in ApiCostLog als per_1k_cache_read_tokens / per_1k_cache_write_tokens geloggt.
  </done>
</task>

<task type="auto">
  <name>Task 2: qa_pipeline.py — Caching fuer QA-Response (ENV-Toggle + Threshold + DB-Logging + Pitfall-Doku)</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\qa_pipeline.py (Zeilen 353-455, generate_qa_response vollstaendig)
  </read_first>
  <action>
generate_qa_response() in qa_pipeline.py vollstaendig lesen (ca. Zeilen 353-455).

**Schritt 1 — System-Prompt-Variable identifizieren:**
Den Ort finden wo der System-Prompt gebaut wird (ggf. `build_system_prompt(profile_data)`
oder direkter String). Die Variable die an `messages.create()` uebergeben wird benennen
(im Folgenden `system_prompt` als Platzhalter — echter Variablenname aus der Datei verwenden).

**Schritt 2 — Dynamischer Cache-Check:**
```python
# ── Phase 08.13: Prompt-Caching QA (CACHE_QA=True default) ───────────────────
# PITFALL: generate_qa_response() baut System-Prompt aus profile_data — pro User
# unterschiedlich. Cache-Hit nur session-intern (gleicher User, <= 5 Min TTL).
# Cross-Session-Cache-Hit ist NICHT erwartet. Normales Design, KEIN Bug.
if config.CACHE_QA and len(system_prompt) >= 16000:
    _system = [{"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}}]
else:
    _system = system_prompt
print(f"[Cache-Check] qa system_prompt: {len(system_prompt)} chars, "
      f"threshold 16000, cache={'on' if config.CACHE_QA and len(system_prompt) >= 16000 else 'off'}")
```

`system=system_prompt` im messages.create()-Aufruf durch `system=_system` ersetzen.

Falls System-Prompt als messages[0] eingebaut (kein `system=`-Parameter):
Das erste messages-Element umstrukturieren:
```python
messages=[
    {"role": "user", "content": [
        {"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}},
        {"type": "text", "text": user_utterance}
    ]}
]
```
Nur wenn tatsaechlich kein `system=`-Format verwendet wird — Datei prueft welches Format
vorliegt. NICHT raten.

**Schritt 3 — Cache-Token-DB-Logging (B1 aus Reviews):**

Im bestehenden Cost-Hook-Block (nach den normalen input/output Rows) ergaenzen:
```python
        # Cache-Token-Logging (B1 Review-Finding)
        _cache_hits = getattr(getattr(response, 'usage', None), 'cache_read_input_tokens', 0) or 0
        _cache_writes = getattr(getattr(response, 'usage', None), 'cache_creation_input_tokens', 0) or 0
        if _cache_hits > 0:
            log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                         units=_cache_hits/1000.0, unit_type='per_1k_cache_read_tokens',
                         context_tag='qa', call_site='qa')
        if _cache_writes > 0:
            log_api_cost('anthropic', 'sonnet-4-5', user_id=user_id,
                         units=_cache_writes/1000.0, unit_type='per_1k_cache_write_tokens',
                         context_tag='qa', call_site='qa')
```
Model: 'sonnet-4-5' (QA-Response ist Sonnet per CONTEXT.md D-01).

Falls qa_pipeline.py noch keinen Cost-Hook hat: jetzt anlegen. Pattern aus dem
`<interfaces>`-Block oben verwenden. user_id-Parameter muss in generate_qa_response()
verfuegbar sein (entweder als Parameter oder aus dem Aufruf-Kontext ermitteln).

Falls kein user_id-Parameter in generate_qa_response() vorhanden: `user_id=None` als
optionalen Parameter ergaenzen und Aufrufer in routes/ entsprechend anpassen.

**Schritt 4 — `import config` sicherstellen:**
Pruefe ob `import config` am Anfang von qa_pipeline.py vorhanden ist. Falls nicht: ergaenzen.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "
import subprocess, sys

# cache_control in qa_pipeline.py
r = subprocess.run(['grep', '-c', 'cache_control', 'services/qa_pipeline.py'],
    capture_output=True, text=True, cwd='C:/Users/andre/dev/salesnerve')
count = int(r.stdout.strip()) if r.returncode == 0 else 0
assert count >= 1, f'qa_pipeline.py: cache_control nicht gefunden'

# Cache-Token-DB-Logging
r2 = subprocess.run(['grep', '-c', 'per_1k_cache_read_tokens', 'services/qa_pipeline.py'],
    capture_output=True, text=True, cwd='C:/Users/andre/dev/salesnerve')
count2 = int(r2.stdout.strip()) if r2.returncode == 0 else 0
assert count2 >= 1, f'qa_pipeline.py: per_1k_cache_read_tokens nicht gefunden (B1-Fix fehlt)'

# Modul importiert sauber
import services.qa_pipeline as qp
import inspect
sig = inspect.signature(qp.generate_qa_response)
assert sig is not None
print(f'OK: qa_pipeline.py hat Prompt-Caching (cache_control an {count} Stelle/n) + Cache-Token-DB-Logging')
"</automated>
  </verify>
  <done>
    qa_pipeline.py generate_qa_response(): Dynamischer Cache-Check (CACHE_QA + Threshold),
    Diagnostic-Print, system=_system (oder messages-Format entsprechend).
    Cache-Token-DB-Logging als per_1k_cache_read_tokens / per_1k_cache_write_tokens.
    Pitfall-Kommentar dokumentiert: Cross-Session-Cache-Hit nicht erwartet (profile_data pro User).
    Alle 3 CONTEXT.md-bestimmten Caching-Call-Sites (EWB, QA, Analyse-Loop) aktiv mit Toggle + Threshold.
    pytest gruen (kein Regression).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude_service → Anthropic API | cache_control sendet System-Prompt-Inhalt zur Anthropic-Seite fuer Caching |
| ENV → config.CACHE_* | CACHE_*-Booleans steuern ob Caching aktiviert wird |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.13-08 | Information Disclosure | Prompt-Caching System-Prompt | accept | System-Prompt wird ohnehin bei jedem API-Call gesendet. Caching aendert nichts am Datenschutz-Scope — Anthropic hat die Daten bereits beim normalen Call. |
| T-08.13-09 | Tampering | Dynamische System-Prompts + Caching | accept | 5-Minuten-TTL + byte-identisch-Requirement des Anthropic SDK. Wenn System-Prompt sich aendert: neuer Cache-Write, kein falscher Hit moeglich. |
| T-08.13-10 | Denial of Service | Cache-Write-Kosten bei zu kurzem Prompt | mitigate | Threshold-Check len(system_prompt) >= 16000 verhindert Cache-Writes fuer kurze Prompts. CACHE_ANALYSE=False Default schaltet Analyse-Loop-Caching aus bis Token-Groesse verifiziert. |
</threat_model>

<verification>
1. `grep -n "cache_control" services/claude_service.py` — mindestens 3 Treffer (analyse + 2x ewb)
2. `grep -n "cache_control" services/qa_pipeline.py` — mindestens 1 Treffer
3. `grep -n "per_1k_cache_read_tokens" services/claude_service.py` — mindestens 2 Treffer
4. `grep -n "per_1k_cache_read_tokens" services/qa_pipeline.py` — mindestens 1 Treffer
5. `grep -n "Cache-Check" services/claude_service.py` — mindestens 3 Treffer (je 1 pro Funktion)
6. `grep -n "Cache-Check" services/qa_pipeline.py` — mindestens 1 Treffer
7. `python -c "from services.claude_service import analysiere_mit_claude"` exits 0
8. `python -c "from services.qa_pipeline import generate_qa_response"` exits 0
9. `pytest tests/ -x -q 2>&1 | tail -5` — kein neuer Failure
</verification>

<success_criteria>
- POLISH-58 implementiert: EWB + QA + Analyse-Loop nutzen cache_control=ephemeral (wenn Threshold erfuellt)
- ENV-Toggles: CACHE_EWB/CACHE_QA wirken (True=an), CACHE_ANALYSE wirkt (False=aus per Default)
- Threshold 16000 Zeichen ist hartcodiert als Grenze (kein Cache bei kuerzeren Prompts)
- Diagnostic-Print bei jedem Call: Prompt-Laenge + Cache-Status (on/off)
- Cache-Token-DB-Logging: cache_read_input_tokens + cache_creation_input_tokens als
  per_1k_cache_read_tokens / per_1k_cache_write_tokens Rows in ApiCostLog (B1-Fix)
- Pitfall-Kommentar in generate_qa_response: Cross-Session-Cache-Hit nicht erwartet
- Kein anthropic-beta extra_headers (SDK >= 0.40.0)
- pytest kein neuer Failure
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade/08.13-04-SUMMARY.md` erstellen.
</output>
