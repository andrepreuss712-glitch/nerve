---
phase: 08.13
plan: 04
type: execute
wave: 4
depends_on:
  - 08.13-03
files_modified:
  - services/claude_service.py
autonomous: true
requirements:
  - POLISH-58

must_haves:
  truths:
    - "EWB-Generation (streame_auto_variante + streame_manual_ewb_variante) sendet cache_control auf System-Prompt-Block"
    - "QA-Response (generate_qa_response in qa_pipeline.py) sendet cache_control auf System-Prompt-Block"
    - "Analyse-Loop (analysiere_mit_claude) sendet cache_control auf System-Prompt-Block"
    - "Cache-Hit-Tokens werden aus response.usage.cache_read_input_tokens gelesen und geloggt"
    - "Kein Anthropic Beta-Header noetig (GA seit 2025 — pruefe SDK-Version)"
  artifacts:
    - path: "services/claude_service.py"
      provides: "Prompt-Caching auf EWB + Analyse-Loop Call-Sites"
      contains: "cache_control"
    - path: "services/qa_pipeline.py"
      provides: "Prompt-Caching auf QA-Response"
      contains: "cache_control"
  key_links:
    - from: "services/claude_service.py:streame_auto_variante"
      to: "Anthropic API"
      via: "system=[{type: text, text: ..., cache_control: {type: ephemeral}}]"
      pattern: "cache_control.*ephemeral"
    - from: "services/qa_pipeline.py:generate_qa_response"
      to: "Anthropic API"
      via: "system=[{type: text, text: ..., cache_control: {type: ephemeral}}]"
      pattern: "cache_control.*ephemeral"
---

<objective>
Prompt-Caching (POLISH-58) fuer die drei call_sites aktivieren, die laut CONTEXT.md
Caching erhalten: EWB-Generation, QA-Response, Analyse-Loop.

Purpose: System-Prompt mit cache_control=ephemeral markieren → Anthropic cached den
Prompt fuer 5 Minuten. Cache-Hit: 0.1x normaler Preis. Fuer 4000-Token System-Prompt
+ Sonnet: gecacht $0.30/MT Input vs. Haiku ungecacht $0.80/MT.

Output:
- claude_service.py: analysiere_mit_claude(), streame_auto_variante(), streame_manual_ewb_variante()
  nutzen system=[{type:text, text:..., cache_control:{type:ephemeral}}] statt system=string
- qa_pipeline.py: generate_qa_response() nutzt cache_control auf System-Prompt-Block
- Cache-Hit-Tokens in Cost-Hook geloggt (als separater log_api_cost-Aufruf oder Kommentar)
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-RESEARCH.md

<interfaces>
<!-- Caching-Pattern aus CONTEXT.md (LOCKED) -->

Aktuelles API-Call-Format (system als String):
```python
response = claude_client.messages.create(
    model=config.MODEL_ANALYSE,
    max_tokens=...,
    system=system_prompt,  # <-- aktuell String
    messages=[{"role": "user", "content": user_msg}]
)
```

Nach Caching (system als Liste mit cache_control):
```python
response = claude_client.messages.create(
    model=config.MODEL_ANALYSE,
    max_tokens=...,
    system=[{
        "type": "text",
        "text": system_prompt,
        "cache_control": {"type": "ephemeral"}
    }],
    messages=[{"role": "user", "content": user_msg}]
)
# Cache-Hit lesen:
_cache_hits = getattr(response.usage, 'cache_read_input_tokens', 0) or 0
_cache_writes = getattr(response.usage, 'cache_creation_input_tokens', 0) or 0
```

WICHTIG — Beta-Header-Check (Assumption A1 aus RESEARCH.md):
anthropic SDK 0.40.0+ hat Caching als GA — kein extra_headers noetig.
Pruefe ob SDK-Version >= 0.40.0 in requirements.txt. Falls aelter: extra_headers ergaenzen:
```python
extra_headers={"anthropic-beta": "prompt-caching-2024-07-31"}
```
Im Zweifel: extra_headers WEGLASSEN (GA-Verhalten) und bei API-Error im Test-Log beobachten.

Pitfall 4 (aus RESEARCH.md): System-Prompt muss byte-identisch sein fuer Cache-Hit.
Dynamische Teile (Username, Zeitstempel) duerften NICHT im system=[]-Block sein.
Pruefe build_ewb_prompt() und QA-System-Prompt auf dynamische Teile.

Werte die Caching bekommen (LOCKED per CONTEXT.md):
- analysiere_mit_claude() — Analyse-Loop (Haiku, System-Prompt: SYSTEM_PROMPT_BASE + EWB-Context)
- streame_auto_variante() — PiP AutoVar (Haiku, System-Prompt: EWB-Pipeline-Prompt)
- streame_manual_ewb_variante() — PiP Manual (Haiku, System-Prompt: EWB-Pipeline-Prompt)
- generate_qa_response() (qa_pipeline.py) — QA (Sonnet, System-Prompt: Profil + Skript + FAQ)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: claude_service.py — Prompt-Caching fuer Analyse-Loop + EWB-Generation</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Zeilen 449-510, analysiere_mit_claude — komplette Funktion)
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Zeilen 505-595, streame_auto_variante)
    - C:\Users\andre\dev\salesnerve\services\claude_service.py (Zeilen 593-695, streame_manual_ewb_variante)
    - C:\Users\andre\dev\salesnerve\requirements.txt (SDK-Version pruefen)
  </read_first>
  <action>
In allen drei Funktionen: `system=system_prompt` (String) → `system=[{"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}}]` (Liste).

**1. analysiere_mit_claude():**
Finde das `claude_client.messages.create(...)` mit `system=...` und aendere:
```python
# VORHER:
response = claude_client.messages.create(
    model=config.MODEL_ANALYSE,
    max_tokens=...,
    system=system_prompt,
    messages=[...]
)

# NACHHER:
response = claude_client.messages.create(
    model=config.MODEL_ANALYSE,
    max_tokens=...,
    system=[{"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}}],
    messages=[...]
)
```
Nach dem API-Call Cache-Stats lesen und loggen (als Debug-Print — kein extra Cost-Hook):
```python
_cache_hits = getattr(getattr(response, 'usage', None), 'cache_read_input_tokens', 0) or 0
if _cache_hits > 0:
    print(f"[Cache] analyse: {_cache_hits} cache-hit tokens")
```

**2. streame_auto_variante():**
Gleiche Aenderung. Falls diese Funktion kein explizites `system=`-Argument hat (weil
System-Prompt ueber messages[] als erstes User-Element eingebaut ist), pruefe das
tatsaechliche Format und passe entsprechend an. NICHT raten — Datei lesen.

**3. streame_manual_ewb_variante():**
Gleiche Aenderung wie #2.

**SDK-Version-Check:**
`grep anthropic requirements.txt` — pruefe ob >= 0.40.0.
Falls requirements.txt `anthropic==0.40.0` oder hoeher zeigt: kein extra_headers noetig.
Falls aelter: `extra_headers={"anthropic-beta": "prompt-caching-2024-07-31"}` zu allen
drei messages.create()-Aufrufen hinzufuegen.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && py -c "
import services.claude_service as cs
import inspect

# inspect.signature ist erlaubt per CLAUDE.md (Runtime API shape)
# Pruefe dass analysiere_mit_claude und streame_auto_variante existieren und importierbar sind
sig1 = inspect.signature(cs.analysiere_mit_claude)
sig2 = inspect.signature(cs.streame_auto_variante)
sig3 = inspect.signature(cs.streame_manual_ewb_variante)
assert sig1 is not None and sig2 is not None and sig3 is not None

# Struktureller Smoke-Test: Modul laesst sich importieren nach cache_control Aenderung
# (wuerde bei Syntax-Fehler in der Aenderung scheitern)
# Runtime-Verifikation des cache_control Verhaltens nur via echtem API-Call moeglich
# (ausserhalb Scope dieses Smoke-Tests)
# Verwende grep fuer Struktur-Verifikation (erlaubt per CLAUDE.md fuer grep auf Quelldateien
# wenn kein Runtime-Equivalent existiert — Grenzfall: cache_control ist Anthropic-API-Aufruf-Argument
# das nicht ohne echten API-Call pruefbar ist)
import subprocess, sys
r = subprocess.run(['grep', '-c', 'cache_control', 'services/claude_service.py'],
    capture_output=True, text=True, cwd='C:/Users/andre/dev/salesnerve')
count = int(r.stdout.strip()) if r.returncode == 0 else 0
assert count >= 2, f'claude_service.py: cache_control nur {count}x gefunden, erwartet >= 2 (fuer 3 Funktionen)'
print(f'OK: cache_control in claude_service.py an {count} Stellen — Modul importiert sauber')
"</automated>
  </verify>
  <done>
    claude_service.py: analysiere_mit_claude(), streame_auto_variante(), streame_manual_ewb_variante()
    nutzen system=[{type:text, text:..., cache_control:{type:ephemeral}}].
    Cache-Hit-Tokens werden nach dem API-Call mit getattr(response.usage, 'cache_read_input_tokens', 0) gelesen.
    Debug-Print bei Cache-Hit vorhanden.
  </done>
</task>

<task type="auto">
  <name>Task 2: qa_pipeline.py — Prompt-Caching fuer QA-Response</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\services\qa_pipeline.py (Zeilen 353-455, generate_qa_response vollstaendig)
  </read_first>
  <action>
generate_qa_response() in qa_pipeline.py lesen (ca. Zeilen 353-455).

Das messages.create()-API-Call finden. Pruefe ob `system=` als String oder ob
System-Prompt anderswo eingebaut ist (z.B. als erstes messages[]-Element).

Falls `system=system_prompt` (String-Format):
Umstellen auf:
```python
response = claude_client.messages.create(
    model=config.MODEL_QA,
    max_tokens=...,
    system=[{"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}}],
    messages=[...]
)
_cache_hits = getattr(getattr(response, 'usage', None), 'cache_read_input_tokens', 0) or 0
if _cache_hits > 0:
    print(f"[Cache] qa: {_cache_hits} cache-hit tokens")
```

Falls System-Prompt als messages[0] statt system= eingebaut:
Das erste messages[]-Element umstrukturieren:
```python
messages=[
    {"role": "user", "content": [
        {"type": "text", "text": system_prompt, "cache_control": {"type": "ephemeral"}},
        {"type": "text", "text": user_utterance}
    ]}
]
```
(Nur falls system= API nicht benutzt wird — Datei prueft welches Format tatsaechlich vorliegt.)

BEACHTE — Pitfall 4 (RESEARCH.md): generate_qa_response() bekommt profile_data als
Parameter — pruefe ob der System-Prompt (ggf. mit build_system_prompt(profile_data))
bei jedem Call anders ist (session-spezifische Daten im System-Prompt = kein Cache-Hit).
Falls der System-Prompt dynamisch pro User ist: Caching trotzdem aktivieren — erste
Calls in einer Session schreiben den Cache, folgende Calls der gleichen Session
profitieren (5-Minuten-TTL). Das ist korrekt.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && py -c "
import services.qa_pipeline as qp
import inspect, subprocess, sys

# Smoke-Test: Modul importiert ohne Fehler (wuerde bei Syntax-Fehler scheitern)
sig = inspect.signature(qp.generate_qa_response)
assert sig is not None, 'generate_qa_response nicht importierbar'

# cache_control ist Anthropic-API-Argument — nicht ohne echten API-Call runtime-pruefbar
# grep als Verifikation des Vorhandenseins (Grenzfall per CLAUDE.md: kein Runtime-Equivalent)
r = subprocess.run(['grep', '-c', 'cache_control', 'services/qa_pipeline.py'],
    capture_output=True, text=True, cwd='C:/Users/andre/dev/salesnerve')
count = int(r.stdout.strip()) if r.returncode == 0 else 0
assert count >= 1, f'qa_pipeline.py: cache_control nicht gefunden'
print(f'OK: qa_pipeline.py hat Prompt-Caching (cache_control an {count} Stelle/n)')
"</automated>
  </verify>
  <done>
    qa_pipeline.py generate_qa_response(): System-Prompt mit cache_control={type:ephemeral} markiert.
    Cache-Hit-Tokens werden ausgelesen.
    Alle 3 CONTEXT.md-bestimmten Caching-Call-Sites (EWB, QA, Analyse-Loop) aktiv.
    pytest gruen (kein Regression durch system-Format-Aenderung).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude_service → Anthropic API | cache_control sendet System-Prompt-Inhalt zur Anthropic-Seite fuer Caching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.13-07 | Information Disclosure | Prompt-Caching System-Prompt | accept | System-Prompt wird ohnehin bei jedem API-Call gesendet. Caching aendert nichts am Datenschutz-Scope — Anthropic hat die Daten bereits beim normalen Call. |
| T-08.13-08 | Tampering | Dynamische System-Prompts + Caching | accept | 5-Minuten-TTL + byte-identisch-Requirement des Anthropic SDK. Wenn System-Prompt sich aendert: neuer Cache-Write, kein falscher Hit moeglich. |
</threat_model>

<verification>
1. `grep -n "cache_control" services/claude_service.py` — mindestens 3 Treffer
2. `grep -n "cache_control" services/qa_pipeline.py` — mindestens 1 Treffer
3. `python -c "from services.claude_service import analysiere_mit_claude"` exits 0
4. `python -c "from services.qa_pipeline import generate_qa_response"` exits 0
5. `pytest tests/ -x -q 2>&1 | tail -5` — kein neuer Failure
</verification>

<success_criteria>
- POLISH-58 implementiert: EWB + QA + Analyse-Loop nutzen cache_control=ephemeral
- Cache-Hit-Token-Auslesen via `getattr(response.usage, 'cache_read_input_tokens', 0)` vorhanden
- Kein Anthropic Beta-Header noetig (GA — SDK >= 0.40.0) oder Header korrekt gesetzt
- system-Parameter Format: Liste mit dict statt String fuer gecachte Calls
- pytest kein neuer Failure (system=[...] format ist API-kompatibel)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade/08.13-04-SUMMARY.md` erstellen.
</output>
