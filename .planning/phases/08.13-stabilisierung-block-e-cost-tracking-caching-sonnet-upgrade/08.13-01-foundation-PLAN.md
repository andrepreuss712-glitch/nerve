---
phase: 08.13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - app.py
  - database/models.py
  - services/cost_tracker.py
autonomous: true
requirements:
  - LB-4
  - H-12
  - H-22
  - H-29
  - POLISH-58
  - H-9

must_haves:
  truths:
    - "config.py exportiert alle 21 MODEL_*-Konstanten als ENV-overridable Defaults"
    - "api_cost_logs Tabelle hat Spalten latency_ms und call_site nach app-Start"
    - "log_api_cost() akzeptiert latency_ms und call_site als optionale kwargs und schreibt beide Spalten"
    - "DB-Migration ist idempotent — doppelter app-Start wirft keine Exception"
  artifacts:
    - path: "config.py"
      provides: "Alle MODEL_*-Konstanten mit ENV-Defaults"
      contains: "MODEL_EWB = os.getenv"
    - path: "app.py"
      provides: "Idempotente DB-Migration fuer latency_ms + call_site"
      contains: "ALTER TABLE api_cost_log ADD COLUMN latency_ms"
    - path: "services/cost_tracker.py"
      provides: "Erweiterte log_api_cost()-Signatur mit latency_ms/call_site"
      exports: ["log_api_cost"]
  key_links:
    - from: "services/cost_tracker.py:log_api_cost"
      to: "database/models.py:ApiCostLog"
      via: "db.add(ApiCostLog(latency_ms=..., call_site=...))"
      pattern: "latency_ms=latency_ms"
    - from: "app.py:_migrate"
      to: "api_cost_log table"
      via: "ALTER TABLE api_cost_log ADD COLUMN"
      pattern: "ADD COLUMN latency_ms"
---

<objective>
Fundament fuer alle nachfolgenden Waves legen: MODEL_*-Konstanten in config.py und
DB-Schema-Erweiterung + cost_tracker-Erweiterung.

Purpose: Ohne config.py-Konstanten koennen Call-Sites nicht auf config.MODEL_XYZ umgestellt
werden. Ohne latency_ms/call_site in DB und cost_tracker koennen die erweiterten
Cost-Hooks in Waves 2-4 nicht schreiben.

Output:
- config.py: 21 MODEL_*-Konstanten (12 ENV-Vars nach CONTEXT.md, 9 zusaetzliche fuer
  alle in RESEARCH.md gefundenen Stellen)
- app.py: 2 idempotente ALTER TABLE statements fuer api_cost_log
- database/models.py: ApiCostLog-Klasse um latency_ms + call_site erweitert
- services/cost_tracker.py: log_api_cost() um latency_ms + call_site kwargs erweitert
</objective>

<execution_context>
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\dev\salesnerve\.planning\..\..\..\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\andre\dev\salesnerve\.planning\ROADMAP.md
@C:\Users\andre\dev\salesnerve\.planning\STATE.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-CONTEXT.md
@C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-RESEARCH.md

<interfaces>
<!-- Bestehende Signaturen — Executor liest diese, kein Codebase-Scouting noetig -->

Aus services/cost_tracker.py (aktuell):
```python
def log_api_cost(
    provider: str,       # 'anthropic' | 'deepgram' | 'elevenlabs'
    model: str,          # z.B. 'haiku-4-5', 'sonnet-4', 'nova-2'
    user_id: int | None,
    units: float,
    unit_type: str,
    *,
    org_id: int | None = None,
    session_id: str | None = None,
    context_tag: str | None = None,
) -> None:
```

Aus database/models.py — ApiCostLog (aktuelles Schema, OHNE latency_ms/call_site):
```python
class ApiCostLog(Base):
    __tablename__ = 'api_cost_log'
    id           = Column(Integer, primary_key=True)
    created_at   = Column(DateTime, ...)
    provider     = Column(String(32), ...)
    model        = Column(String(64), ...)
    user_id      = Column(Integer, ForeignKey('users.id'), nullable=True)
    org_id       = Column(Integer, ForeignKey('organisations.id'), nullable=True)
    units        = Column(Numeric(14, 4), ...)
    unit_type    = Column(String(32), ...)
    rate_applied = Column(Numeric(12, 8), ...)
    rate_currency= Column(String(3), ...)
    fx_rate_applied = Column(Numeric(10, 6), ...)
    cost_eur     = Column(Numeric(12, 6), ...)
    session_id   = Column(String(64), nullable=True)
    context_tag  = Column(String(32), nullable=True)
    # FEHLT: latency_ms, call_site
```

Aus app.py — _migrate()-Muster (idempotent):
```python
def _migrate():
    from sqlalchemy import text
    with engine.connect() as conn:
        try:
            conn.execute(text('ALTER TABLE users ADD COLUMN {col} {typedef}'))
            conn.commit()
        except Exception:
            pass
```

Aus config.py (aktuell — KEINE MODEL_*-Eintraege vorhanden):
```python
import os
from dotenv import load_dotenv
load_dotenv()
ANTHROPIC_API_KEY = os.environ.get('ANTHROPIC_API_KEY', '')
# ... andere Keys/Constants
# MODEL_* Eintraege: KEINER vorhanden
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.py MODEL_*-Konstanten (21 Stellen)</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\config.py (vollstaendig lesen — 65 Zeilen, KEIN MODEL_* vorhanden)
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-CONTEXT.md (Sektion "Model-Auswahl" und "ENV-basierte Model-Switchbarkeit")
    - C:\Users\andre\dev\salesnerve\.planning\phases\08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade\08.13-RESEARCH.md (Tabelle "Alle hardcoded Model-Strings" und Sektion "config.py aktueller Stand")
  </read_first>
  <behavior>
    - python -c "import config; assert config.MODEL_EWB == 'claude-sonnet-4-5'" exits 0
    - python -c "import config; assert config.MODEL_ANALYSE == 'claude-haiku-4-5-20251001'" exits 0
    - python -c "import config; assert config.MODEL_TRAINING_DIALOG == 'claude-haiku-4-5-20251001'" exits 0
    - ENV-Override: MODEL_EWB=claude-opus-4-5 python -c "import config; assert config.MODEL_EWB == 'claude-opus-4-5'" exits 0
  </behavior>
  <action>
Nach dem bestehenden CLASSIFIER_CONFIDENCE_THRESHOLD-Block in config.py (nach Zeile 44)
einen neuen Abschnitt einfuegen:

```python
# ── Phase 08.13: MODEL-Konstanten (ENV-overridable, D-01) ─────────────────────
# Sonnet 4.5 fuer User-sichtbare Outputs — default "claude-sonnet-4-5"
MODEL_EWB               = os.getenv("MODEL_EWB",               "claude-sonnet-4-5")
MODEL_QA                = os.getenv("MODEL_QA",                "claude-sonnet-4-5")
MODEL_POSTCALL_INSIGHTS = os.getenv("MODEL_POSTCALL_INSIGHTS",  "claude-sonnet-4-5")
MODEL_POSTCALL_ANALYSIS = os.getenv("MODEL_POSTCALL_ANALYSIS",  "claude-sonnet-4-5")
MODEL_WEEKLY_SUMMARY    = os.getenv("MODEL_WEEKLY_SUMMARY",     "claude-sonnet-4-5")
MODEL_PRECALL           = os.getenv("MODEL_PRECALL",            "claude-sonnet-4-5")
MODEL_CRM               = os.getenv("MODEL_CRM",               "claude-sonnet-4-5")
MODEL_TRAINING_HELP     = os.getenv("MODEL_TRAINING_HELP",      "claude-sonnet-4-5")
MODEL_TRAINING_SCORING  = os.getenv("MODEL_TRAINING_SCORING",   "claude-sonnet-4-5")
# Haiku 4.5 fuer Latenz/Cost-kritisch — UNVERAENDERLICH per CONTEXT.md D-01
MODEL_ANALYSE           = os.getenv("MODEL_ANALYSE",           "claude-haiku-4-5-20251001")
MODEL_TRAINING_DIALOG   = os.getenv("MODEL_TRAINING_DIALOG",   "claude-haiku-4-5-20251001")
MODEL_PERSONALITY_GEN   = os.getenv("MODEL_PERSONALITY_GEN",   "claude-haiku-4-5-20251001")
# Weitere Haiku-Stellen aus RESEARCH.md (grep-verifiziert)
MODEL_PHASE_CLASSIFY    = os.getenv("MODEL_PHASE_CLASSIFY",    "claude-haiku-4-5-20251001")
MODEL_COLDCALL_INFER    = os.getenv("MODEL_COLDCALL_INFER",    "claude-haiku-4-5-20251001")
MODEL_PIP_AUTOVAR       = os.getenv("MODEL_PIP_AUTOVAR",       "claude-haiku-4-5-20251001")
MODEL_PIP_VARIANTE      = os.getenv("MODEL_PIP_VARIANTE",      "claude-haiku-4-5-20251001")
MODEL_COACHING          = os.getenv("MODEL_COACHING",          "claude-haiku-4-5-20251001")
MODEL_VALIDATE_USER_TEXT= os.getenv("MODEL_VALIDATE_USER_TEXT","claude-haiku-4-5-20251001")
MODEL_TRAINING_PREVIEW  = os.getenv("MODEL_TRAINING_PREVIEW",  "claude-haiku-4-5-20251001")
```

Einfuegestelle: zwischen `CLASSIFIER_CONFIDENCE_THRESHOLD = ...`-Block und `MERGE_WINDOW_S = 0.3`.
os.getenv (nicht os.environ.get) ist korrekt und aequivalent — beide Varianten sind im Projekt
vorhanden, getenv ist kuerzer.

CLAUDE.md-Regel: Identifier-Namen MODEL_VALIDATE_USER_TEXT ist ASCII (kein Umlaut) — korrekt.
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "import config; assert config.MODEL_EWB == 'claude-sonnet-4-5', f'MODEL_EWB wrong: {config.MODEL_EWB}'; assert config.MODEL_ANALYSE == 'claude-haiku-4-5-20251001', f'MODEL_ANALYSE wrong'; assert config.MODEL_TRAINING_DIALOG == 'claude-haiku-4-5-20251001', f'MODEL_TRAINING_DIALOG wrong'; assert config.MODEL_WEEKLY_SUMMARY == 'claude-sonnet-4-5'; assert config.MODEL_CRM == 'claude-sonnet-4-5'; assert config.MODEL_TRAINING_SCORING == 'claude-sonnet-4-5'; print('OK: alle MODEL_*-Konstanten korrekt')"</automated>
  </verify>
  <done>
    config.py enthaelt 21 MODEL_*-Konstanten. Alle Sonnet-Konstanten defaulten auf
    "claude-sonnet-4-5". Alle Haiku-Konstanten defaulten auf "claude-haiku-4-5-20251001".
    python -c "import config; assert config.MODEL_EWB == 'claude-sonnet-4-5'" exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB-Migration (latency_ms + call_site) + ApiCostLog-Model + cost_tracker-Erweiterung</name>
  <read_first>
    - C:\Users\andre\dev\salesnerve\database\models.py (ApiCostLog-Klasse, Zeilen 556-574)
    - C:\Users\andre\dev\salesnerve\services\cost_tracker.py (vollstaendig — 120 Zeilen)
    - C:\Users\andre\dev\salesnerve\app.py (Zeilen 123-200, _migrate()-Funktion Pattern)
  </read_first>
  <behavior>
    - app.py _migrate() aufgerufen: kein Exception, print "[DB] Migration: added api_cost_log.latency_ms" erscheint beim ersten Start
    - app.py _migrate() zweimal aufgerufen: kein Exception beim zweiten Mal (idempotent)
    - log_api_cost(..., latency_ms=250, call_site='ewb') schreibt Row ohne Exception
    - log_api_cost(...) ohne latency_ms/call_site schreibt Row mit None fuer beide Felder (rueckwaertskompatibel)
  </behavior>
  <action>
**Schritt A — database/models.py:**
In der ApiCostLog-Klasse nach der letzten Spalte `context_tag = Column(String(32), ...)` zwei
neue Spalten ergaenzen:
```python
    latency_ms  = Column(Integer, nullable=True)
    call_site   = Column(String(50), nullable=True)
```
Identifier-Namen latency_ms und call_site sind ASCII (CLAUDE.md-Regel erfuellt).

**Schritt B — app.py:**
In der `_migrate()`-Funktion KURZ VOR dem letzten `conn.commit()` bzw. am Ende des
Migrationsfunktion-Koerpers (nach dem letzten bestehenden try-Block, z.B. nach dem
email_confirmed-Block nahe Zeile 846) zwei neue idempotente ALTER TABLE-Bloecke einfuegen:
```python
        # ── Phase 08.13: ApiCostLog Latenz + Call-Site ────────────────────────
        try:
            conn.execute(text("ALTER TABLE api_cost_log ADD COLUMN latency_ms INTEGER"))
            conn.commit()
            print("[DB] Migration: added api_cost_log.latency_ms")
        except Exception:
            pass
        try:
            conn.execute(text("ALTER TABLE api_cost_log ADD COLUMN call_site VARCHAR(50)"))
            conn.commit()
            print("[DB] Migration: added api_cost_log.call_site")
        except Exception:
            pass
```
Tabellen-Name ist `api_cost_log` (aus models.py __tablename__ = 'api_cost_log' verifiziert).

**Schritt C — services/cost_tracker.py:**
1. Signatur von log_api_cost() um zwei neue kwargs erweitern:
```python
def log_api_cost(
    provider: str,
    model: str,
    user_id: int | None,
    units: float,
    unit_type: str,
    *,
    org_id: int | None = None,
    session_id: str | None = None,
    context_tag: str | None = None,
    latency_ms: int | None = None,    # NEU: Phase 08.13
    call_site: str | None = None,     # NEU: Phase 08.13
) -> None:
```

2. Im db.add(ApiCostLog(...)) Block die zwei neuen Felder ergaenzen:
```python
            db.add(ApiCostLog(
                provider=provider,
                model=model,
                user_id=user_id,
                org_id=org_id,
                units=units_d,
                unit_type=unit_type,
                rate_applied=rate_d,
                rate_currency=rate_currency,
                fx_rate_applied=fx_rate,
                cost_eur=cost_eur,
                session_id=session_id,
                context_tag=context_tag,
                latency_ms=latency_ms,    # NEU
                call_site=call_site,      # NEU
            ))
```
  </action>
  <verify>
    <automated>cd /c/Users/andre/dev/salesnerve && python -c "
import inspect
from services import cost_tracker
sig = inspect.signature(cost_tracker.log_api_cost)
assert 'latency_ms' in sig.parameters, 'latency_ms fehlt in Signatur'
assert 'call_site' in sig.parameters, 'call_site fehlt in Signatur'
from database.models import ApiCostLog
assert hasattr(ApiCostLog, 'latency_ms'), 'latency_ms fehlt in ApiCostLog'
assert hasattr(ApiCostLog, 'call_site'), 'call_site fehlt in ApiCostLog'
print('OK: Signatur + Model korrekt')
"</automated>
  </verify>
  <done>
    database/models.py: ApiCostLog hat latency_ms (Integer, nullable) und call_site (String(50), nullable).
    app.py: _migrate() enthaelt zwei idempotente ALTER TABLE api_cost_log ADD COLUMN Bloecke.
    services/cost_tracker.py: log_api_cost() akzeptiert latency_ms und call_site als optionale kwargs.
    inspect.signature(log_api_cost).parameters enthaelt 'latency_ms' und 'call_site'.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV → config.py | MODEL_*-Werte aus .env koennen auf beliebige Model-Strings gesetzt werden |
| cost_tracker → DB | latency_ms/call_site kommen von internen Call-Sites (kein User-Input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.13-01 | Tampering | config.py MODEL_* via ENV | accept | MODEL_*-Strings landen nur in `model=`-Parameter des Anthropic-Calls — kein Code-Execution-Pfad. Falscher Model-String -> API-Error, kein Security-Issue. |
| T-08.13-02 | Information Disclosure | cost_tracker.py log_api_cost | accept | latency_ms/call_site sind interne Metriken, kein User-PII. DB ist server-intern. |
</threat_model>

<verification>
1. `python -c "import config; assert config.MODEL_EWB == 'claude-sonnet-4-5'"` exits 0
2. `python -c "import config; assert config.MODEL_ANALYSE == 'claude-haiku-4-5-20251001'"` exits 0
3. `python -c "from database.models import ApiCostLog; assert hasattr(ApiCostLog, 'latency_ms')"` exits 0
4. `python -c "import inspect; from services.cost_tracker import log_api_cost; sig = inspect.signature(log_api_cost); assert 'latency_ms' in sig.parameters"` exits 0
5. app.py Import ohne Exception: `python -c "import app"` (prueft _migrate Pattern vorhanden)
</verification>

<success_criteria>
- config.py: 21 MODEL_*-Konstanten vorhanden, alle mit os.getenv() und korrekten Defaults
- Sonnet-Konstanten (MODEL_EWB, MODEL_QA, MODEL_POSTCALL_INSIGHTS, MODEL_POSTCALL_ANALYSIS,
  MODEL_WEEKLY_SUMMARY, MODEL_PRECALL, MODEL_CRM, MODEL_TRAINING_HELP, MODEL_TRAINING_SCORING)
  default auf "claude-sonnet-4-5"
- Haiku-Konstanten (MODEL_ANALYSE, MODEL_TRAINING_DIALOG, MODEL_PERSONALITY_GEN und 6 weitere)
  default auf "claude-haiku-4-5-20251001"
- database/models.py: ApiCostLog.latency_ms (Integer nullable) + ApiCostLog.call_site (String(50) nullable)
- app.py: zwei idempotente ALTER TABLE api_cost_log ADD COLUMN Bloecke in _migrate()
- services/cost_tracker.py: log_api_cost() Signatur hat latency_ms und call_site kwargs
- Rueckwaertskompatibel: alle bestehenden log_api_cost()-Aufrufe ohne neue kwargs funktionieren unveraendert
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.13-stabilisierung-block-e-cost-tracking-caching-sonnet-upgrade/08.13-01-SUMMARY.md` erstellen.
</output>
