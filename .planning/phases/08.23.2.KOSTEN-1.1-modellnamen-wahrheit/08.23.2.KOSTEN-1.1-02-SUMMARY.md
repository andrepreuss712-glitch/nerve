---
phase: 08.23.2.KOSTEN-1.1
plan: 02
subsystem: kosten-erfassung
tags: [cost-tracking, guard, ast, margin, ewb]
requires: [08.23.2.KOSTEN-1.1-01-INVENTUR]
provides: [W4-Modellnamen-Wahrheits-Waechter, streame_manual_ewb_variante-Sonnet-Buchung]
affects: [services/claude_service.py, api_cost_log.model]
key-files:
  created: [tests/test_cost_model_truth.py]
  modified: [services/claude_service.py]
decisions:
  - "W4 ist statischer AST-Abgleich, kein Laufzeit-Harness (Punkt 27, CLAUDE.md-Grenzfall)"
  - "Nur die 4 INVENTUR-belegten stimmt=nein-Zeilen gefixt; keine rueckwirkende Korrektur alter Zeilen (D-02)"
metrics:
  completed: 2026-07-22
  tasks: 2
  files: 2
---

# Phase 08.23.2.KOSTEN-1.1 Plan 02: WÄCHTER + FIX Summary

W4-Wächter (statischer AST-Abgleich gebuchter Modellname vs. Kosten-Klasse des aufgerufenen
`config.MODEL_*`) gebaut, gegen den ungefixten Stand rot belegt, dann die 4 belegten
`streame_manual_ewb_variante`-Buchungen von `'haiku-4-5'` auf `config.MODEL_PIP_VARIANTE` (Sonnet)
korrigiert — der Knopf-Pfad bucht Sonnet jetzt als Sonnet.

## ★ ERST-ROT (verbatim, gegen den UNGEFIXTEN Stand, HEAD d943226)

```
.F                                                                       [100%]
================================== FAILURES ===================================
_______________ test_no_booked_literal_contradicts_called_model _______________
...
E       AssertionError: Gebuchter Modellname widerspricht dem aufgerufenen Modell (Kosten-Klasse) - Sonnet-Kosten werden als Haiku verbucht (o. umgekehrt), die Marge ist still falsch:
E           services/claude_service.py::streame_manual_ewb_variante:843: bucht 'haiku-4-5' (Klasse haiku-4-5), ruft aber config.MODEL_PIP_VARIANTE (Klasse sonnet-4-5) auf
E           services/claude_service.py::streame_manual_ewb_variante:846: bucht 'haiku-4-5' (Klasse haiku-4-5), ruft aber config.MODEL_PIP_VARIANTE (Klasse sonnet-4-5) auf
E           services/claude_service.py::streame_manual_ewb_variante:853: bucht 'haiku-4-5' (Klasse haiku-4-5), ruft aber config.MODEL_PIP_VARIANTE (Klasse sonnet-4-5) auf
E           services/claude_service.py::streame_manual_ewb_variante:857: bucht 'haiku-4-5' (Klasse haiku-4-5), ruft aber config.MODEL_PIP_VARIANTE (Klasse sonnet-4-5) auf
E         
E         Fix: das Literal durch die echte config.MODEL_*-Variable des model=-Aufrufs ersetzen (Muster _model_autovar, claude_service.py:620).

tests\test_cost_model_truth.py:218: AssertionError
=========================== short test summary info ===========================
FAILED tests/test_cost_model_truth.py::test_no_booked_literal_contradicts_called_model
1 failed, 1 passed in 0.49s
```

Rot aus dem RICHTIGEN Grund: die Klassen-Assertion (haiku vs. sonnet), nicht Import/Collection.
`test_scanner_finds_something` grün (führender `.`) — Blind-Grün-Schutz greift. Alle 4 belegten
Fundstellen exakt genannt.

## Fix — Diff der 4 Zeilen (vorher → nachher)

Einmalig gesetzt (neu, :841): `_model_variante = config.MODEL_PIP_VARIANTE`

| Zeile | vorher | nachher |
|---|---|---|
| :843 | `log_api_cost('anthropic', 'haiku-4-5', ...` | `log_api_cost('anthropic', _model_variante, ...` |
| :846 | `log_api_cost('anthropic', 'haiku-4-5', ...` | `log_api_cost('anthropic', _model_variante, ...` |
| :853 | `log_api_cost('anthropic', 'haiku-4-5', ...` | `log_api_cost('anthropic', _model_variante, ...` |
| :857 | `log_api_cost('anthropic', 'haiku-4-5', ...` | `log_api_cost('anthropic', _model_variante, ...` |

Alle übrigen Argumente (`units`, `unit_type`, `context_tag`, `call_site`, `session_id`) unverändert.

**`model=`-Zeile (:803) belegt unverändert:** `model=config.MODEL_PIP_VARIANTE,` — kein
Verhaltens-Change am Streaming-Pfad, nur der gebuchte Name wird wahr.

## GRÜN-Beleg nach Fix (Vorab-Signal, verbatim)

```
$ python -m pytest tests/test_cost_model_truth.py -q
..                                                                       [100%]
2 passed in 0.44s
$ python -m pytest tests/test_cost_hook_coverage.py -q        # W2, keine Regression
....                                                                     [100%]
4 passed in 0.04s
```

W1/W3 skippen lokal (real-PG / `TEST_DATABASE_URL`) — erwartet, kein Fehler; ihre bindende Abnahme
ist der Deploy-Gate (Plan 03).

## W1-Regressions-Gegenrechnung (Punkt 30, manuell)

Der neu gebuchte Rohname ist `config.MODEL_PIP_VARIANTE = 'claude-sonnet-4-5'`.
`normalize_model_name('claude-sonnet-4-5') = 'sonnet-4-5'`. In `app._API_RATE_SOLL` (app.py:1120-1123)
hat `('anthropic', 'claude-sonnet-4-5', <unit>)` **alle 4** unit_types (input/output/cache_read/cache_write)
mit aktiver Rate — es ist derselbe String, den `MODEL_PIP_AUTOVAR` bereits loggt (KOSTEN-1 R1 Fund F-1).
⇒ W1 kann durch diesen Fix nicht neu rot werden.

## Grep-Anker gegen den Endzustand (Anti-TEMPO-1)

- `grep -nF "_model_variante = config.MODEL_PIP_VARIANTE" services/claude_service.py` → 1 Treffer (:841)
- `grep -cF "log_api_cost('anthropic', _model_variante" services/claude_service.py` → 4
- `'haiku-4-5'`-Literal-Buchungen innerhalb `streame_manual_ewb_variante` → 0
- repo-weit bleiben 10 korrekte `'haiku-4-5'`-Literal-Buchungen bestehen (echte Haiku-Pfade, unangetastet)
- `ast.parse` beider Dateien → OK

## Fable-Grenzen im Docstring (Doku)

Im W4-Modul-Docstring festgehalten: (1) W3/Skip-Zähler füllt nur FEHLENDE Raten, nicht falsche Namen
mit gültiger Rate — genau die Lücke, die W4 schließt (der Defekt hatte eine gültige Haiku-Rate, floss
nie in den Skip-Zähler). (2) W4 hängt an der config-Modell-Einstellung der Testumgebung (löst
`config.MODEL_*` zur Analysezeit auf; ändert sich Default/ENV, ändert sich die Soll-Klasse mit).

## Deviations from Plan

None — Plan exakt wie geschrieben ausgeführt. Genau der Wächter + die 4 Fix-Zeilen.

## Persistenz-Schicht (Punkt 21)

Keine Schema-Änderung, kein neuer Schreiber/Leser — nur der WERT der bestehenden Spalte
`api_cost_log.model` wird künftig wahr. inspect.sh-Beleg (SSH gegen Prod) NICHT gezogen: Executor
fährt kein SSH/Deploy (Grenze); Schema-Verifikation gehört zum Deploy-Gate (Plan 03, Claudian).
Als offene Annahme markiert: `api_cost_log.model` existiert als VARCHAR (unverändert, kein Fix-Bezug).

## Harte Grenzen — Bestätigung

- Kein `deploy.sh` gefahren (lokaler pytest nur Vorab-Signal, keine Abnahme).
- Nur die 4 INVENTUR-belegten Zeilen geändert; keine andere Buchungsstelle, kein Modellwechsel.
- Keine rückwirkende Korrektur alter `api_cost_log`-Zeilen (D-02).
- Nicht gepusht (Orchestrator pusht).

## Commits

- `b76a907` — test(...): W4-Wächter (ERST-ROT)
- `6c91198` — fix(...): streame_manual_ewb_variante bucht Sonnet als Sonnet

## Self-Check: PASSED

- `tests/test_cost_model_truth.py` — FOUND
- `services/claude_service.py` (Fix) — FOUND
- Commit `b76a907` — FOUND
- Commit `6c91198` — FOUND
