---
phase: 08.23.2.KOSTEN-1.1
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tests/test_cost_model_truth.py
  - services/claude_service.py
autonomous: true
requirements: [K11-GUARD, K11-FIX]

must_haves:
  truths:
    - "Ein Waechter zeigt ROT, wenn eine Funktion einen Modellnamen als String-Literal bucht, der nicht zur Kosten-Klasse des dort tatsaechlich aufgerufenen config.MODEL_*-Modells passt"
    - "Der Waechter ist gegen den UNGEFIXTEN Stand rot (streame_manual_ewb_variante), verbatim belegt"
    - "Nach dem Fix bucht streame_manual_ewb_variante das Modell, das es aufruft (config.MODEL_PIP_VARIANTE)"
    - "W1/W2/W3 regressieren nicht: fuer den umgestellten Namen existiert eine aktive Rate"
  artifacts:
    - path: "tests/test_cost_model_truth.py"
      provides: "W4 — Modellnamen-Wahrheits-Waechter (statischer AST-Abgleich)"
      contains: "streame_manual_ewb_variante"
    - path: "services/claude_service.py"
      provides: "Fix: streame_manual_ewb_variante bucht die echte Modell-Variable"
      contains: "_model_variante = config.MODEL_PIP_VARIANTE"
  key_links:
    - from: "tests/test_cost_model_truth.py"
      to: "config.MODEL_* + normalize_model_name"
      via: "AST: model=-Arg der messages.create/stream gegen Arg2 des log_api_cost-Literals, beide normalisiert"
      pattern: "ast\\.parse"
    - from: "services/claude_service.py streame_manual_ewb_variante"
      to: "config.MODEL_PIP_VARIANTE"
      via: "log_api_cost('anthropic', _model_variante, ...)"
      pattern: "log_api_cost\\('anthropic', _model_variante"
---

<objective>
W4-Waechter bauen (fangt die Fehlerklasse "gebuchter Modellname != an derselben Stelle aufgerufenes
Modell" dauerhaft), gegen den ungefixten Stand ROT belegen, DANN die stimmt=nein-Stellen aus der
INVENTUR fixen und GRUEN belegen. Strikte Reihenfolge — ohne rot-vorher prueft der Waechter nichts.

Purpose: Marge-Schutz. Der belegte Pfad streame_manual_ewb_variante verbucht Sonnet-Kosten seit jeher
als Haiku (~3x zu niedrig). W2 fing das nicht (prueft nur OB ein Hook da ist, nicht OB der Name stimmt).
Output: tests/test_cost_model_truth.py (W4) + korrigierte Buchung in claude_service.py.

★ Punkt 27 (einfachster tragfaehiger Weg): W4 ist ein statischer AST-Abgleich, KEIN Laufzeit-Harness.
Begruendung: der Defekt-Pfad (streame_manual_ewb_variante) haengt an messages.stream + SocketIO +
prompt_pipeline — ein Laufzeit-Mock waere ein schweres Harness fuer eine rein statische Wahrheit
("bucht die Funktion einen Literal-Namen, der zur Kosten-Klasse ihres config.MODEL_*-Aufrufs passt?").
Das ist der CLAUDE.md-Grenzfall "inspect/AST fuer Runtime-Constraints OK, wenn kein Function-Mock die
Constraint direkt testbar macht" — Grund im Test dokumentieren. Es ist KEIN Source-Presence-False-Green:
der Test parst die Struktur (model=-Arg vs Buchungs-Arg) und vergleicht ueber config-Aufloesung +
normalize_model_name — er wird rot, wenn die Werte auseinanderlaufen, nicht nur wenn ein String fehlt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-INVENTUR.md
@.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-CONTEXT.md
@CLAUDE.md

<read_first>
- .planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-INVENTUR.md
  — die "STIMMT=NEIN"-Liste ist die EINZIG erlaubte Fix-Menge. Keine anderen Stellen anfassen.
- tests/test_api_rate_coverage.py — Muster fuer den statischen Sweep + config-Aufloesung (W1)
- tests/test_cost_hook_coverage.py — Muster fuer Allowlist mit Begruendungs-Pflicht (W2)
- services/claude_service.py:620-706 — das KORREKTE Muster (`_model_autovar` + Buchung ueber die Variable)
- services/claude_service.py:740-861 — streame_manual_ewb_variante: model=config.MODEL_PIP_VARIANTE
  (:803), Buchung hart 'haiku-4-5' (:843/846/853/857) — DER Fix.
- services/cost_tracker.py:61-82 — normalize_model_name (import + verwenden, existiert seit KOSTEN-1)
- config.py:74-97 — MODEL_*-Defaults
- app.py:1108-1130 — Raten: 'sonnet-4-5' UND 'claude-sonnet-4-5' haben je alle 4 unit_types (Fix bleibt W1-gruen)
</read_first>

<interfaces>
normalize_model_name (services/cost_tracker.py) — bereits vorhanden, kein Neubau:
```python
def normalize_model_name(model: str | None) -> str:
    return 'sonnet-4-5' if 'sonnet' in (model or '') else 'haiku-4-5'
```
log_api_cost-Aufruf-Form (Arg 1 = provider-Literal, Arg 2 = model):
```python
log_api_cost('anthropic', <model>, user_id=..., units=..., unit_type='...', ...)
```
Modell-Aufruf-Form im selben Funktionskoerper:
```python
claude_client.messages.stream(model=config.MODEL_PIP_VARIANTE, ...)  # oder .messages.create
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: W4-Waechter bauen und gegen den UNGEFIXTEN Stand ROT belegen (ERST-ROT, PFLICHT)</name>
  <files>tests/test_cost_model_truth.py</files>
  <action>
Schreibe tests/test_cost_model_truth.py — ein statischer AST-Sweep ueber services/, routes/, nerve_rt/.

Algorithmus (deterministisch, KEIN DB, kein db_session, kein cleanup noetig — reiner Struktur-Check):
1. import config; from services.cost_tracker import normalize_model_name (existiert bereits).
2. Fuer jede .py-Datei: ast.parse. Fuer jede FunctionDef (auch nested):
   a. Sammle Buchungen: Calls `log_api_cost(...)`, deren Arg 1 == String-Literal 'anthropic' UND
      deren Arg 2 ein ast.Constant/Str-LITERAL ist. Merke dessen Wert (booked_literal). Nicht-Literal-
      Arg2 (Name/Attribute wie _model_autovar, _m, config.MODEL_X) => diese Buchung UEBERSPRINGEN
      (nicht pruefbar per Literal-Regel; W3 deckt die Laufzeit-Faelle).
   b. Sammle Modell-Aufrufe: Calls `*.messages.create(...)` / `*.messages.stream(...)` in DERSELBEN
      Funktion mit keyword `model=`. Loese den model=-Wert auf:
         - ast.Attribute `config.MODEL_X` => getattr(config, 'MODEL_X', None)
         - ast.Name, der in derselben Funktion via `<name> = config.MODEL_X` (einfache Zuweisung)
           gesetzt wird => getattr(config, 'MODEL_X', None). (Ein-Schritt-Aufloesung reicht;
           Fallback-Reassignments wie `_model_autovar = 'claude-haiku-...'` konservativ als
           "mehrdeutig" behandeln, s.u.)
         - alles andere (Konstante ohne config, unaufloesbar) => diese Funktion als "unresolved"
           markieren und NICHT als Verstoss werten (konservativ; Doku als bekannte Grenze).
   c. Verstoss NUR wenn: die Funktion hat >=1 Literal-Buchung UND genau EINE eindeutig aufgeloeste
      Anthropic-Modell-Klasse aus (b), UND normalize_model_name(booked_literal) !=
      normalize_model_name(aufgeloestes_modell). Bei mehreren verschiedenen aufgeloesten Modell-Klassen
      in einer Funktion => "mehrdeutig", nicht werten (Doku als Grenze; kommt real nicht vor).
3. test_no_booked_literal_contradicts_called_model(): assert not verstoesse, mit einer Liste
   "Datei::Funktion: bucht '<literal>' (Klasse X), ruft aber <config.MODEL_Y> (Klasse Z) auf".
4. test_scanner_finds_something(): assert dass der Sweep ueberhaupt log_api_cost-Literal-Buchungen
   findet (>0) — sonst ist der Scanner kaputt und wuerde blind-gruen sein (W1-Lehre).

Dokumentiere im Modul-Docstring VERBINDLICH (analog W1/W2):
   - WOFUER (Fehlerklasse), WARUM AST statt Laufzeit (Punkt 27 + CLAUDE.md-Grenzfall), warum KEIN
     Source-Presence-False-Green (Struktur-Abgleich, nicht String-Presence).
   - BEWUSSTE GRENZEN: Nicht-Literal-Buchungen (_model_autovar, _m, config.MODEL_X direkt gebucht)
     sieht W4 NICHT — die deckt W3 zur Laufzeit ab. Ein Literal, das per config auf die falsche
     Variable zeigt, faengt W4 nur ueber die Kosten-Klasse (Haiku vs Sonnet), nicht ueber die
     exakte Modell-Version.

★ ERST-ROT (PFLICHT, VERBATIM-BELEG): Fuehre den Waechter JETZT — VOR jedem Fix, gegen den
ungefixten claude_service.py — als Vorab-Signal aus:
   cd C:/Users/andre/dev/salesnerve && python -m pytest tests/test_cost_model_truth.py::test_no_booked_literal_contradicts_called_model -x -q
Er MUSS ROT sein und in der Fehlermeldung mindestens
   "services/claude_service.py::streame_manual_ewb_variante: bucht 'haiku-4-5' ... ruft ... MODEL_PIP_VARIANTE"
nennen (Klasse haiku-4-5 vs sonnet-4-5). Kopiere die rote Ausgabe VERBATIM in die SUMMARY. Zeigt er
NICHT rot am Button-Pfad, ist der Waechter defekt (prueft nichts) — reparieren, bevor Task 2 laeuft.
NB: lokaler pytest ist hier nur Vorab-Signal/ERST-ROT-Beleg, KEINE Abnahme (HART-Regel). Die bindende
Abnahme ist der Deploy-Gate in Plan 03.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -m pytest tests/test_cost_model_truth.py::test_no_booked_literal_contradicts_called_model -q 2>&1 | grep -E "streame_manual_ewb_variante|FAILED|failed"</automated>
  </verify>
  <done>
tests/test_cost_model_truth.py existiert, laeuft ohne DB, und ist gegen den UNGEFIXTEN Stand ROT mit
verbatim-Nennung von streame_manual_ewb_variante (haiku vs sonnet). test_scanner_finds_something ist
gruen. Roter Output verbatim in SUMMARY. (Dieser Task bleibt bewusst rot bis Task 2 fixt.)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix der stimmt=nein-Stellen + W4 GRUEN + keine W1/W2/W3-Regression</name>
  <files>services/claude_service.py</files>
  <behavior>
    - Nach dem Fix ruft W4 (test_no_booked_literal_contradicts_called_model) GRUEN.
    - streame_manual_ewb_variante bucht an allen 4 Stellen (input/output/cache_read/cache_write) die
      Variable, die auch der Aufruf nutzt — nicht mehr das Literal 'haiku-4-5'.
    - Kein Verhaltens-Change am Antwort-/Streaming-Pfad: model=config.MODEL_PIP_VARIANTE bleibt
      unveraendert, nur der GEBUCHTE Name wird wahr.
    - W1 (Rate-Abdeckung) bleibt gruen: der neue gebuchte Name hat eine aktive Rate.
  </behavior>
  <action>
Fixe AUSSCHLIESSLICH die in INVENTUR.md als stimmt=nein belegten Stellen. Belegter Mindest-Fix
(einziger bekannter Defekt): services/claude_service.py, streame_manual_ewb_variante.

Muster exakt wie der korrekte Nachbar _model_autovar (:620): definiere im Funktionskoerper EINMAL
   _model_variante = config.MODEL_PIP_VARIANTE
(sinnvoll direkt vor dem Cost-Hook-Block, oder oben bei den Locals). Ersetze in allen VIER
log_api_cost-Aufrufen (aktuell :843/846/853/857) das Literal `'haiku-4-5'` durch `_model_variante`:
   log_api_cost('anthropic', _model_variante, user_id=None, units=..., unit_type='...', ...)
Die uebrigen Argumente (units, unit_type, context_tag, session_id, call_site) bleiben UNVERAENDERT.

Kein Modellwechsel, kein Umbau des Aufruf-Pfads, kein neuer Logik-Zweig (kein DACH-Fallback noetig —
diese Funktion hat keinen, model= ist immer config.MODEL_PIP_VARIANTE). normalize_model_name bleibt
die Normalisierungsschicht in cost_tracker; wir buchen hier bewusst den ROH-Namen wie _model_autovar
(config.MODEL_PIP_VARIANTE = 'claude-sonnet-4-5'), der laut app.py:1119-1122 eine aktive Rate hat.

Falls die INVENTUR weitere stimmt=nein-Stellen belegt: jede analog auf ihre eigene echte
config.MODEL_*-Variable umstellen (die Variable des model=-Aufrufs DERSELBEN Funktion). KEINE Stelle
mit stimmt=ja anfassen (die Haiku-Pfade sind korrekt).

Danach W4 gruen belegen (Vorab-Signal) + Nicht-Regression der drei Bestands-Waechter pruefen:
   python -m pytest tests/test_cost_model_truth.py tests/test_cost_hook_coverage.py -q   (kein DB noetig)
   # W1/W3 brauchen real-PG (TEST_DATABASE_URL) und skippen sonst — das ist erwartet, kein Fehler.
W1-Gegenrechnung MANUELL bestaetigen (Punkt 30): der neue gebuchte Name
`normalize_model_name('claude-sonnet-4-5')` = 'sonnet-4-5' UND der Rohname 'claude-sonnet-4-5' haben
in app._API_RATE_SOLL je alle 4 unit_types (app.py:1119-1126) => W1 kann nicht neu rot werden. Diese
Gegenrechnung in die SUMMARY schreiben.

★ GREP-KRITERIEN GEGEN DEN ENDZUSTAND (Anti-TEMPO-1) — gegen CODE-ANKER, mit -F, END-Zustand:
   - grep -nF "_model_variante = config.MODEL_PIP_VARIANTE" services/claude_service.py   => >=1 Treffer
   - Innerhalb streame_manual_ewb_variante: `log_api_cost('anthropic', _model_variante` == 4 Treffer,
     `log_api_cost('anthropic', 'haiku-4-5'` == 0 Treffer (NUR in dieser Funktion — repo-weit bleiben
     die korrekten Haiku-Pfade bestehen; KEINE repo-weite 'haiku-4-5'-Zaehlung als Kriterium, die
     aendert sich durch den Fix und wuerde falsch rechnen).
   - Die Wahrheit prueft ohnehin W4 (test_no_booked_literal_contradicts_called_model gruen) — die
     grep-Anker sind nur Doppel-Absicherung, nicht die Abnahme.
  </action>
  <verify>
    <automated>cd C:/Users/andre/dev/salesnerve && python -m pytest tests/test_cost_model_truth.py -q && python -m pytest tests/test_cost_hook_coverage.py -q</automated>
  </verify>
  <done>
W4 gruen (kein Verstoss). streame_manual_ewb_variante bucht _model_variante an allen 4 Stellen, 0x
'haiku-4-5' in dieser Funktion. W2 gruen, W1/W3 skippen lokal (real-PG) — Regressions-Gegenrechnung
fuer W1 (Rate existiert) in der SUMMARY belegt. Nur stimmt=nein-Stellen angefasst.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| keine neue | Reine interne Kosten-Buchung; keine untrusted Eingabe, kein neuer Endpoint, kein neues Recht |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-K11-01 | Repudiation | api_cost_log-Buchung (Marge-Wahrheit) | mitigate | W4-Waechter erzwingt gebuchter Name == aufgerufenes Modell dauerhaft; ERST-ROT belegt Wirksamkeit |
| T-K11-02 | Tampering | historische api_cost_log-Zeilen | accept | D-02/Finanzamt: alte Zeilen werden NIE rueckwirkend geaendert; Fix wirkt nur ab jetzt |
| T-K11-03 | Information Disclosure | W4-Waechter | accept | statischer AST-Check, liest nur Quellcode + config-Attribute; keine Secrets, keine Nutzerdaten |
</threat_model>

<persistenz_schicht_verifikation>
## 5. Persistenz-Schicht-Verifikation (Punkt 21)

### Angefasste Tabellen
- `api_cost_log` (schreiben, ueber bestehendes log_api_cost) — NUR der Wert der bestehenden Spalte
  `model` aendert sich. KEINE Schema-Aenderung, KEINE neue Spalte, KEIN neuer Schreiber/Leser.

### inspect.sh-Beleg (vom Executor zu ziehen, verbatim in die SUMMARY)
  ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh schema api_cost_log'
Erwartung: Spalte `model` existiert (VARCHAR). Falls SSH nicht moeglich: als offene Annahme markieren.

### Cross-Layer-Konsistenz-Tabelle
| Code-Wert | Schreib-Pfad | Persistenz-Schicht | Schreib-Zeitpunkt | Verifiziert? |
|---|---|---|---|---|
| gebuchter `model` (kuenftig _model_variante) | log_api_cost -> ApiCostLog.model | DB-Spalte api_cost_log.model | synchron beim Buchen (kein Batch/bg-Race, Punkt 26 n/a) | inspect.sh schema |

### Schild (Punkt 23)
Kein neuer Schreiber, keine neue Spalte, kein geaenderter Lese-/Schreib-Mechanismus — nur der
SpaltenWERT wird wahr. Das api_cost_log-Schild (Schreibt/liest-Liste) bleibt inhaltlich korrekt.
Executor bestaetigt via `inspect.sh schilder api_cost_log`, dass das Schild noch stimmt (Aktualitaets-
Pflicht) — falls es cost_tracker/log_api_cost nennt, ist es weiterhin vollstaendig. Kein Nachziehen erwartet.
</persistenz_schicht_verifikation>

<verification>
- W4 rot VOR Fix (verbatim), gruen NACH Fix.
- W2 gruen; W1/W3 lokal geskippt (real-PG), Regression per Gegenrechnung ausgeschlossen.
- Nur stimmt=nein-Stellen der INVENTUR angefasst; kein model=-Aufruf veraendert.
</verification>

<success_criteria>
Der Button-Pfad bucht kuenftig Sonnet als Sonnet. W4 haelt die Fehlerklasse dauerhaft rot-faehig.
Kein Bestands-Waechter regressiert. Alle harten Zahlen gegen den Endzustand gerechnet.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-02-SUMMARY.md`
mit: verbatim ERST-ROT-Ausgabe, W4-gruen-Beleg, W1-Regressions-Gegenrechnung, Liste der gefixten Stellen.
</output>
