---
phase: 08.23.2.KOSTEN-1.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-INVENTUR.md
autonomous: true
requirements: [K11-INVENTUR]

must_haves:
  truths:
    - "Jede log_api_cost-Buchungsstelle in services/, routes/, nerve_rt/ ist einzeln erfasst"
    - "Fuer jede Stelle steht belegt, welches Modell dieselbe Funktion tatsaechlich per model= aufruft"
    - "Jede Stelle ist als stimmt=ja oder stimmt=nein markiert, grep-belegt pro Zeile"
    - "Der belegte Defekt streame_manual_ewb_variante taucht als stimmt=nein auf"
  artifacts:
    - path: ".planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-INVENTUR.md"
      provides: "Inventur-Tabelle + Verbindungs-Karte (Punkt 22)"
      contains: "streame_manual_ewb_variante"
  key_links:
    - from: "jede log_api_cost-Stelle"
      to: "das model=-Argument der zugehoerigen messages.create/stream in DERSELBEN Funktion"
      via: "grep file:line + config.MODEL_*-Aufloesung"
      pattern: "log_api_cost\\('anthropic'"
---

<objective>
INVENTUR — das Beweis-Fundament (Punkt 22). Ermittle grep-belegt JEDE `log_api_cost`-Buchungsstelle
und halte den gebuchten Modellnamen gegen das Modell, das dieselbe Funktion tatsaechlich an die API
gibt. Ohne diese Tabelle raet der Fix.

Purpose: Der Fix (Plan 02) darf NUR Stellen anfassen, die hier als stimmt=nein belegt sind. Eine
hart gebuchte `'haiku-4-5'`-Stelle ist NUR dann ein Defekt, wenn die Funktion wirklich auf einem
anderen Modell laeuft — die meisten Haiku-Buchungen sind korrekt (der Pfad laeuft wirklich auf Haiku).
Output: `08.23.2.KOSTEN-1.1-INVENTUR.md` mit der vollstaendigen Tabelle. Diese Tabelle ist zugleich
die Verbindungs-Karte (Punkt 22) fuer den Fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-CONTEXT.md
@CLAUDE.md

<read_first>
- services/cost_tracker.py — `log_api_cost` (Signatur: provider, model, ...), `normalize_model_name`
  ('sonnet' im Namen -> 'sonnet-4-5', sonst 'haiku-4-5'), `_count_skip` (W3)
- services/claude_service.py:620 (`_model_autovar = config.MODEL_PIP_AUTOVAR`, KORREKTES Muster),
  :803 (`model=config.MODEL_PIP_VARIANTE`) + :843-857 (der belegte Defekt: bucht hart 'haiku-4-5')
- config.py:74-97 — die `MODEL_*`-Defaults. WICHTIG: MODEL_PIP_VARIANTE + MODEL_PIP_AUTOVAR +
  MODEL_EWB + MODEL_QA + MODEL_POSTCALL_ANALYSIS + MODEL_PRECALL + MODEL_CRM + MODEL_TRAINING_HELP/
  SCORING defaulten auf `claude-sonnet-4-5` (Sonnet). MODEL_ANALYSE, MODEL_TRAINING_DIALOG,
  MODEL_COACHING, MODEL_PHASE_CLASSIFY, MODEL_COLDCALL_INFER, MODEL_VALIDATE_USER_TEXT,
  MODEL_TRAINING_PREVIEW defaulten auf `claude-haiku-4-5-20251001` (Haiku).
- app.py:1090-1130 (`_API_RATE_SOLL`) — welche Modellnamen ueberhaupt eine Rate haben
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vollstaendige Buchungsstellen-Inventur als Verbindungs-Karte</name>
  <files>.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-INVENTUR.md</files>
  <action>
Ermittle per grep ALLE `log_api_cost`-Buchungsstellen in `services/`, `routes/`, `nerve_rt/`
(NICHT tests/). Pflicht-grep (Endzustand ist der VORHER-Zustand — Plan 01 aendert keinen Code):

  grep -rn "log_api_cost(" services/ routes/ nerve_rt/

Zaehle die Zahl der **Buchungsstellen** (jeder `log_api_cost(...)`-Aufruf) — NICHT die Zahl der
String-Vorkommen. Die Roadmap nennt "22x haiku + 4x sonnet"; eine Roh-Zaehlung ergibt ~35x/15x.
Die echte Zahl der Aufrufe ist per grep zu belegen; die Abweichung zur Roadmap-Zahl gehoert
explizit in die SUMMARY (Roadmap-Zahl war nachweislich zu eng).

Fuer JEDE Buchungsstelle bestimme, welches Modell die zugehoerige `claude_client.messages.create`
bzw. `.messages.stream` in DERSELBEN Funktion per `model=` an die API gibt:
  - Literal? config.MODEL_*-Variable? lokale Fallback-Konstante (z.B. _model_autovar)?
  - Bei config.MODEL_*: den (ENV-ueberschriebenen) Default aus config.py nennen.
  - Nicht-Anthropic-Stellen (deepgram nova-*, stripe card, elevenlabs multilingual-v2, brave
    web_search) sind KEINE Modellnamen-Wahrheits-Frage in diesem Sinn — als "n/a (kein Anthropic-
    Modell)" markieren, nicht als Defekt.

ENV-Gegenbeleg (Prod ueberschreibt die MODEL_*-Defaults nicht) via inspect.sh — Namen, nie Werte:
  ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh env-keys'
Erwartung: KEIN MODEL_PIP_VARIANTE / MODEL_EWB / MODEL_QA in der Ausgabe (== 0 Overrides). Den
verbatim-Ausschnitt (grep auf die MODEL_-Zeilen) in die INVENTUR aufnehmen. Falls SSH nicht moeglich
ist, das explizit als offene Annahme markieren — NICHT stillschweigend "kein Override" behaupten.

Schreibe `08.23.2.KOSTEN-1.1-INVENTUR.md` mit einer Markdown-Tabelle, EINE Zeile pro Buchungsstelle:

| Datei:Zeile | Funktion | gebuchter Name (Arg 2) | tatsaechlich aufgerufenes Modell (model=) | normalize gleich? | stimmt | Fix noetig |

"normalize gleich?" = `normalize_model_name(gebucht)` vs `normalize_model_name(aufgerufen)`:
'sonnet' im echten Modell aber 'haiku' gebucht (oder umgekehrt) => stimmt=nein => Fix noetig.
Gleiche Kosten-Klasse (beide Haiku oder beide Sonnet) => stimmt=ja, auch wenn die exakten Strings
abweichen (z.B. gebucht 'haiku-4-5' vs aufgerufen 'claude-haiku-4-5-20251001' — beide Haiku, ok).

Jede Zeile mit grep file:line belegt (Beweis pro Behauptung, keine Prosa — Punkt 22).

Bekannter Anker (muss in der Tabelle als stimmt=nein erscheinen): services/claude_service.py:843/846/
853/857 · streame_manual_ewb_variante · gebucht 'haiku-4-5' · aufgerufen config.MODEL_PIP_VARIANTE
(= claude-sonnet-4-5) · normalize: haiku-4-5 vs sonnet-4-5 · stimmt=nein.

Bekannte stimmt=ja-Faelle zur Kalibrierung (nicht anfassen): qa_pipeline.py:333/336 (model=MODEL_ANALYSE,
Haiku), training_service.py:821/825/881/885 (model=MODEL_TRAINING_DIALOG, Haiku), claude_service.py:
393/396/470/473/543-557/900-903 (Haiku-Pfade), dashboard.py:366/369 (Weekly-Summary, Sonnet, bucht
'sonnet-4-5' -> korrekt), crm_service.py:71/74 (MODEL_CRM Sonnet, bucht 'sonnet-4-5' -> korrekt).
Diese trotzdem einzeln verifizieren und begruenden — nicht blind uebernehmen.

Am Ende der INVENTUR: eine kurze Liste "STIMMT=NEIN — zu fixen in Plan 02" mit exakten Datei:Zeile.
  </action>
  <verify>
    <automated>test -f .planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-INVENTUR.md && grep -q "streame_manual_ewb_variante" .planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-INVENTUR.md</automated>
  </verify>
  <done>
INVENTUR.md existiert, enthaelt eine Zeile pro log_api_cost-Buchungsstelle (Zahl grep-belegt +
Abweichung zur Roadmap-Zahl notiert), jede Zeile mit stimmt ja/nein, der Anker
streame_manual_ewb_variante steht als stimmt=nein, die "zu fixen"-Liste ist explizit.
  </done>
</task>

</tasks>

<verification>
- Jede log_api_cost-Stelle aus services/routes/nerve_rt in der Tabelle vertreten (grep-Gegenzaehlung).
- Kein Anthropic-Modell-Pfad ohne aufgeloestes echtes Modell.
- ENV-Gegenbeleg (kein MODEL_*-Override auf Prod) belegt ODER als offene Annahme markiert.
</verification>

<success_criteria>
INVENTUR.md liefert die belegte Grundlage: welche Stellen stimmen (nicht anfassen) und welche
nicht (Plan 02 fixt exakt diese, keine anderen).
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-01-SUMMARY.md`
mit der Inventur-Kernzahl (Buchungsstellen gesamt, davon stimmt=nein) und der Roadmap-Abweichung.
</output>
