---
phase: 08.23.2.KOSTEN-1.1
plan: 03
type: execute
wave: 3
depends_on: [2]
files_modified: []
autonomous: false
requirements: [K11-VERIFY]

must_haves:
  truths:
    - "Der Pytest-Gate in deploy.sh production ist gruen gelaufen (bindende Abnahme, inkl. W4)"
    - "Der neue Waechter W4 laeuft im Deploy-Gate mit und ist dort gruen"
    - "Ein Live-Test-Klick am Button-Pfad bucht kuenftig Sonnet als Sonnet (Kosten-Log-Skips bleiben 0)"
  artifacts: []
  key_links:
    - from: "deploy.sh production (Pytest-Gate auf Prod)"
      to: "tests/test_cost_model_truth.py + Bestands-Waechter W1/W2/W3"
      via: "Pre-Restart-Gate; bei rot kein Restart"
      pattern: "deploy.sh production"
---

<objective>
Verify=Production (HART-Regel): die bindende Abnahme. Claudian faehrt `deploy.sh production`; dessen
Pytest-Gate laeuft auf dem Prod-Server VOR dem Restart und enthaelt jetzt W4. Gruen => Fix ist live,
der Button-Pfad verbucht Sonnet wahr. Danach ein Post-hoc-Live-Signal.

Purpose: Lokale Laeufe aus Plan 02 sind nur Vorab-Signal. Erst der Deploy-Gate ist Abnahme.
Output: gruener Deploy-Gate-Lauf + Bestaetigung, dass Kosten-Log-Skips im Founder-Dashboard 0 bleiben.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
Plan 02 hat den W4-Waechter (tests/test_cost_model_truth.py) gebaut (ERST-ROT belegt) und
streame_manual_ewb_variante gefixt (bucht kuenftig config.MODEL_PIP_VARIANTE statt hart 'haiku-4-5').
Lokal: W4 gruen, W2 gruen, W1/W3 real-PG. Fehlt nur noch die bindende Prod-Abnahme.
  </what-built>
  <how-to-verify>
1. Deploy fahren (Claudian, NICHT GSD — HART-Regel "Deploy fährt Claudian"):
     bash deploy.sh production
   Das eingebaute Pytest-Gate laeuft auf dem Prod-Server vor dem Restart. Es MUSS gruen sein,
   inkl. tests/test_cost_model_truth.py (W4) und der Bestands-Waechter W1/W2/W3
   (test_api_rate_coverage / test_cost_hook_coverage / test_cost_skip_counter). Bei rot: KEIN
   Restart, alter Code bleibt live — Ursache fixen (zurueck zu Plan 02), nicht am Gate vorbei deployen.
2. Post-hoc-Live-Signal (kein Gate, nur Bestaetigung): mit dem Test-User einen Call starten und im
   PiP-Live-Fenster einen EWB-Button klicken (loest streame_manual_ewb_variante aus). Danach im
   Founder-Dashboard "Kosten-Log-Skips" pruefen — Soll weiterhin 0 (der Fix darf keinen neuen Skip
   erzeugen; die Sonnet-Rate existiert). Optional Beleg:
     ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh logs-errors 50'
   sollte keinen "[CostTracker] no active ApiRate ... sonnet"-Skip fuer den Button-Pfad zeigen.
3. Git: nach gruenem Deploy `git push origin main` (Git-Regel: immer pushen).
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn der Deploy-Gate gruen war (W4 inklusive) und die Kosten-Log-Skips 0 bleiben — oder beschreibe das Problem.</resume-signal>
</task>

</tasks>

<verification>
- Deploy-Gate gruen inkl. W4 (bindende Abnahme).
- Kosten-Log-Skips im Founder-Dashboard == 0 nach einem Button-Klick.
- git push erfolgt.
</verification>

<success_criteria>
Der Fix ist live und durch den Prod-Pytest-Gate abgenommen; W4 schuetzt die Fehlerklasse dauerhaft
im Deploy-Gate. Keine neuen stillen Skips.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.KOSTEN-1.1-modellnamen-wahrheit/08.23.2.KOSTEN-1.1-03-SUMMARY.md`
mit: Deploy-Gate-Ergebnis (gruen, W4 gelaufen), Kosten-Log-Skip-Stand, Push-Bestaetigung.
</output>
