---
phase: 04.7.2
plan: 04
type: execute
wave: 3
depends_on: ["04.7.2-02", "04.7.2-03"]
files_modified:
  - routes/admin_dashboard.py
  - app.py
  - templates/admin/dashboard.html
  - templates/admin/_tab_uebersicht.html
  - templates/admin/_tab_einnahmen.html
  - static/admin_dashboard.js
  - static/admin_dashboard.css
  - tests/test_admin_dashboard_auth.py
autonomous: true
requirements:
  - BE-CD-08
  - BE-CD-09
  - BE-CD-11
must_haves:
  truths:
    - "Normaler User (is_superadmin=False) bekommt 403 auf /admin/dashboard"
    - "Superadmin sieht 6 Tab-Header (Übersicht/Einnahmen/Ausgaben/Kunden/EÜR/Export)"
    - "Tab-Switching ist client-side (Vanilla JS), URL-Hash bleibt erhalten"
    - "Tab Übersicht zeigt 5 KPI-Karten (MRR, Kosten/Mo, Marge, Aktive User, Gewinn/Mo)"
    - "Tab Übersicht zeigt 2 Charts: MRR+Kosten 12-Monate Combo + Marge-Trend 12-Monate Line"
    - "Tab Einnahmen zeigt Netto/USt/Brutto Zusammenfassung nach tax_treatment + Plan-Split + Länder-Split + paginierte Transaktionstabelle"
    - "Period-Parameter (?period=2026-03) wird mit Regex ^\\d{4}-\\d{2}$ validiert"
  artifacts:
    - path: "routes/admin_dashboard.py"
      provides: "Blueprint admin_dashboard_bp mit /admin/dashboard, Tab-Endpoints, Data-APIs"
      exports: ["admin_dashboard_bp"]
    - path: "templates/admin/dashboard.html"
      provides: "Shell mit 6-Tab-Navigation + Chart.js + Tab-Include-Loader"
    - path: "templates/admin/_tab_uebersicht.html"
      provides: "5 KPI-Karten + 2 Charts + Breakeven-Bar"
    - path: "templates/admin/_tab_einnahmen.html"
      provides: "Zeitraum-Auswahl + 3 Aggregat-Tabellen + Transaktionsliste"
  key_links:
    - from: "routes/admin_dashboard.py@every_route"
      to: "services.auth_decorators.superadmin_required"
      via: "Decorator @superadmin_required nach @login_required"
      pattern: "@superadmin_required"
    - from: "templates/admin/dashboard.html"
      to: "static/admin_dashboard.js"
      via: "Vanilla-JS Tab-Switcher + Chart.js Renderer"
      pattern: "admin_dashboard\\.js"
    - from: "routes/admin_dashboard.py::api_overview_kpis"
      to: "MRR aus Organisation + Einnahmen+Kosten aus revenue_log/api_cost_log"
      via: "JSON-Endpoint für Übersichts-KPIs"
      pattern: "/api/overview"
---

<objective>
Wave 3a: Admin-Dashboard-Blueprint aufsetzen (Auth-gated), Shell mit 6-Tab-Navigation rendern, Tab 1 (Übersicht) und Tab 2 (Einnahmen) voll funktional. Tabs 3-6 bleiben als Stub-Templates + "Wird in Plan 05/06/07 implementiert" Hinweis.

Purpose: Blueprint + Auth + Shell + die 2 "einfachen" Tabs (nur Aggregationen aus bestehenden Daten, kein CRUD, keine Berechnungen).

Output: `/admin/dashboard` live, André (is_superadmin=True) sieht funktionale Übersicht + Einnahmen. Tests für Auth-Gate grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-CONTEXT.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-RESEARCH.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-01-SUMMARY.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-02-SUMMARY.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-03-SUMMARY.md
@C:\Users\andre\OneDrive\Desktop\Nerve-Vault\02 Projekte\NERVE Admin Dashboard Final.md
@services/auth_decorators.py
@routes/admin_views.py
@routes/dashboard.py
@templates/dashboard.html
@database/models.py
@static/nerve.css

<interfaces>
Aus Plan 01:
```python
from database.models import (
    ApiCostLog, ApiRate, FixedCost,
    RevenueLog, ExchangeRate, PriceChangeLog,
    Organisation, User
)
```

Bestehender Decorator (services/auth_decorators.py:5):
```python
def superadmin_required(f):
    @wraps(f)
    def wrapper(*args, **kwargs):
        if not getattr(g, 'user', None) or not getattr(g.user, 'is_superadmin', False):
            abort(403)
        return f(*args, **kwargs)
    return wrapper
```

Bestehendes Chart.js-Pattern (templates/dashboard.html:281):
```html
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
```

Organisation-Feld (aus Phase 04):
- stripe_customer_id: str|None
- subscription_status: str ('active'|'canceled'|'past_due'|None)
- plan_key: str ('solo'|'team'|'business'|'coach')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Blueprint + Shell + Tab-Navigation + Auth-Gate + Auth-Tests</name>
  <read_first>
    - services/auth_decorators.py (vollständig — superadmin_required)
    - routes/admin_views.py (Pattern-Referenz für Blueprint-Registrierung)
    - routes/auth.py (login_required decorator)
    - app.py (Blueprint-Registrierungs-Block — wo neue Blueprints eingehängt werden)
    - static/nerve.css (für KPI-Card und Tab-Styling-Tokens)
    - templates/dashboard.html (Chart.js CDN-Link + n-card Pattern)
  </read_first>
  <files>
    routes/admin_dashboard.py
    templates/admin/dashboard.html
    templates/admin/_tab_ausgaben.html
    templates/admin/_tab_kunden.html
    templates/admin/_tab_eur.html
    templates/admin/_tab_export.html
    static/admin_dashboard.js
    static/admin_dashboard.css
    app.py
    tests/test_admin_dashboard_auth.py
  </files>
  <behavior>
    - Test 1: Unauthenticated request to /admin/dashboard → 302 Redirect zu Login
    - Test 2: Normal user (is_superadmin=False) → 403
    - Test 3: Superadmin user → 200 mit HTML enthält alle 6 Tab-Buttons
    - Test 4: /admin/dashboard?tab=einnahmen → 200
    - Test 5: /admin/dashboard?period=BADFORMAT → 400 (invalid period)
    - Test 6: /admin/dashboard?period=2026-03 → 200
  </behavior>
  <action>
**1a) `routes/admin_dashboard.py`:**

```python
"""Phase 04.7.2 — Founder Cost Dashboard Blueprint.

Alle Routes gated mit @login_required + @superadmin_required.
6 Tabs: uebersicht, einnahmen, ausgaben, kunden, eur, export.
"""
from __future__ import annotations
import re
from datetime import date, datetime, timedelta
from flask import Blueprint, render_template, request, jsonify, abort
from routes.auth import login_required
from services.auth_decorators import superadmin_required
from database.db import get_session

admin_dashboard_bp = Blueprint(
    'admin_dashboard', __name__,
    url_prefix='/admin/dashboard',
    template_folder='../templates/admin',
)

VALID_TABS = {'uebersicht', 'einnahmen', 'ausgaben', 'kunden', 'eur', 'export'}
PERIOD_RE = re.compile(r'^\d{4}-\d{2}$')


def _parse_period(period_str: str | None) -> tuple[date, date]:
    """Parst 'YYYY-MM' in (start_date, end_date_exclusive).
    Fallback: aktueller Monat."""
    if period_str and not PERIOD_RE.match(period_str):
        abort(400, description="Invalid period format, expected YYYY-MM")
    if not period_str:
        today = date.today()
        period_str = f"{today.year}-{today.month:02d}"
    year, month = map(int, period_str.split('-'))
    start = date(year, month, 1)
    if month == 12:
        end = date(year + 1, 1, 1)
    else:
        end = date(year, month + 1, 1)
    return start, end


@admin_dashboard_bp.route('/')
@login_required
@superadmin_required
def index():
    tab = request.args.get('tab', 'uebersicht')
    if tab not in VALID_TABS:
        tab = 'uebersicht'
    period = request.args.get('period')
    _parse_period(period)  # validates format, raises 400 on bad
    return render_template('admin/dashboard.html',
                           active_tab=tab,
                           period=period or date.today().strftime('%Y-%m'))
```

**1b) `templates/admin/dashboard.html` — Shell mit Tab-Nav + Chart.js:**

```html
{% extends "base.html" %}
{% block title %}NERVE Admin — Founder Cost Dashboard{% endblock %}
{% block head %}
<link rel="stylesheet" href="{{ url_for('static', filename='admin_dashboard.css') }}">
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
{% endblock %}
{% block content %}
<div class="fcd-wrap" data-period="{{ period }}">
  <header class="fcd-header">
    <h1>NERVE ADMIN <span class="fcd-sub">André Preuß — Founder Cost Dashboard</span></h1>
    <div class="fcd-period-picker">
      <label>Zeitraum: <input type="month" id="fcd-period" value="{{ period }}"></label>
    </div>
  </header>

  <nav class="fcd-tabs" role="tablist">
    <button class="fcd-tab" data-tab="uebersicht">Übersicht</button>
    <button class="fcd-tab" data-tab="einnahmen">Einnahmen</button>
    <button class="fcd-tab" data-tab="ausgaben">Ausgaben</button>
    <button class="fcd-tab" data-tab="kunden">Kunden</button>
    <button class="fcd-tab" data-tab="eur">EÜR</button>
    <button class="fcd-tab" data-tab="export">Export</button>
  </nav>

  <main class="fcd-body">
    <section class="fcd-panel" id="panel-uebersicht">
      {% include 'admin/_tab_uebersicht.html' %}
    </section>
    <section class="fcd-panel" id="panel-einnahmen" hidden>
      {% include 'admin/_tab_einnahmen.html' %}
    </section>
    <section class="fcd-panel" id="panel-ausgaben" hidden>
      {% include 'admin/_tab_ausgaben.html' %}
    </section>
    <section class="fcd-panel" id="panel-kunden" hidden>
      {% include 'admin/_tab_kunden.html' %}
    </section>
    <section class="fcd-panel" id="panel-eur" hidden>
      {% include 'admin/_tab_eur.html' %}
    </section>
    <section class="fcd-panel" id="panel-export" hidden>
      {% include 'admin/_tab_export.html' %}
    </section>
  </main>
</div>
<script>window.FCD_INITIAL_TAB = "{{ active_tab }}";</script>
<script src="{{ url_for('static', filename='admin_dashboard.js') }}"></script>
{% endblock %}
```

**1c) Stub-Templates für Tabs 3-6 (werden in Plans 05/06/07 ersetzt):**

`templates/admin/_tab_ausgaben.html`:
```html
<div class="fcd-stub">Tab "Ausgaben" — wird in Plan 04.7.2-05 implementiert.</div>
```
`templates/admin/_tab_kunden.html`:
```html
<div class="fcd-stub">Tab "Kunden" — wird in Plan 04.7.2-05 implementiert.</div>
```
`templates/admin/_tab_eur.html`:
```html
<div class="fcd-stub">Tab "EÜR" — wird in Plan 04.7.2-06 implementiert.</div>
```
`templates/admin/_tab_export.html`:
```html
<div class="fcd-stub">Tab "Export" — wird in Plan 04.7.2-07 implementiert.</div>
```

**1d) `static/admin_dashboard.js` — Tab-Switcher (Vanilla):**

```javascript
// Phase 04.7.2 — Founder Cost Dashboard client-side tab switcher + charts.
(function() {
  const tabs = document.querySelectorAll('.fcd-tab');
  const panels = document.querySelectorAll('.fcd-panel');

  function activate(name) {
    tabs.forEach(t => t.classList.toggle('active', t.dataset.tab === name));
    panels.forEach(p => { p.hidden = (p.id !== 'panel-' + name); });
    if (location.hash !== '#' + name) history.replaceState(null, '', '#' + name);
  }

  tabs.forEach(t => t.addEventListener('click', () => activate(t.dataset.tab)));

  const fromHash = (location.hash || '').replace('#', '');
  const initial = fromHash || window.FCD_INITIAL_TAB || 'uebersicht';
  activate(initial);

  // Period picker — reload on change
  const p = document.getElementById('fcd-period');
  if (p) p.addEventListener('change', () => {
    const url = new URL(location.href);
    url.searchParams.set('period', p.value);
    location.href = url.toString();
  });

  // Overview KPIs + Charts (nur wenn Tab sichtbar UND data-endpoint vorhanden)
  window.FCD = window.FCD || {};
  window.FCD.loadOverview = async function(period) {
    const r = await fetch('/admin/dashboard/api/overview?period=' + encodeURIComponent(period));
    if (!r.ok) return;
    const data = await r.json();
    // Fill KPI cards
    document.querySelectorAll('[data-kpi]').forEach(el => {
      const key = el.dataset.kpi;
      if (data.kpis && data.kpis[key] !== undefined) el.textContent = data.kpis[key];
    });
    // Render charts
    if (data.mrr_costs_12m) FCD.renderMrrCosts(data.mrr_costs_12m);
    if (data.margin_12m) FCD.renderMarginChart(data.margin_12m);
  };

  window.FCD.renderMrrCosts = function(series) {
    const el = document.getElementById('fcd-chart-mrr-costs');
    if (!el || !window.Chart) return;
    new Chart(el, {
      type: 'bar',
      data: {
        labels: series.labels,
        datasets: [
          { label: 'MRR (€)', data: series.mrr, backgroundColor: '#00D4AA' },
          { label: 'Kosten (€)', data: series.costs, type: 'line', borderColor: '#ef4444' },
        ]
      },
      options: { responsive: true, maintainAspectRatio: false }
    });
  };

  window.FCD.renderMarginChart = function(series) {
    const el = document.getElementById('fcd-chart-margin');
    if (!el || !window.Chart) return;
    new Chart(el, {
      type: 'line',
      data: { labels: series.labels, datasets: [{ label: 'Marge %', data: series.values, borderColor: '#00D4AA', tension: 0.2 }] },
      options: { responsive: true, maintainAspectRatio: false, scales: { y: { min: 0, max: 100 } } }
    });
  };

  const period = document.querySelector('.fcd-wrap').dataset.period;
  if (window.FCD.loadOverview) window.FCD.loadOverview(period);
})();
```

**1e) `static/admin_dashboard.css` — Basis-Styles:**

```css
/* Phase 04.7.2 — Founder Cost Dashboard */
.fcd-wrap { max-width: 1400px; margin: 0 auto; padding: 24px; color: var(--n-text, #e6edf3); }
.fcd-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
.fcd-header h1 { font-size: 20px; font-weight: 700; margin: 0; }
.fcd-sub { color: #9ca3af; font-weight: 400; margin-left: 12px; font-size: 14px; }
.fcd-period-picker input { background: #1C2333; color: #e6edf3; border: 1px solid #2D3748; padding: 6px 10px; border-radius: 6px; }
.fcd-tabs { display: flex; gap: 4px; border-bottom: 2px solid #1C2333; margin-bottom: 20px; }
.fcd-tab { background: transparent; color: #9ca3af; border: none; padding: 10px 16px; cursor: pointer; font-weight: 500; border-bottom: 2px solid transparent; margin-bottom: -2px; }
.fcd-tab:hover { color: #e6edf3; }
.fcd-tab.active { color: #00D4AA; border-bottom-color: #00D4AA; }
.fcd-panel { min-height: 400px; }
.fcd-kpi-row { display: grid; grid-template-columns: repeat(5, 1fr); gap: 16px; margin-bottom: 24px; }
.fcd-kpi { background: #1C2333; border-radius: 12px; padding: 16px; box-shadow: 0 1px 3px rgba(0,0,0,.2); }
.fcd-kpi h3 { margin: 0 0 8px; font-size: 12px; color: #9ca3af; text-transform: uppercase; letter-spacing: 0.05em; }
.fcd-kpi .value { font-size: 24px; font-weight: 700; color: #e6edf3; }
.fcd-kpi .delta { font-size: 12px; margin-top: 4px; }
.fcd-kpi .delta.up { color: #00D4AA; }
.fcd-kpi .delta.down { color: #ef4444; }
.fcd-chart-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 24px; }
.fcd-chart-card { background: #1C2333; border-radius: 12px; padding: 16px; }
.fcd-chart-card h3 { margin: 0 0 12px; font-size: 14px; color: #e6edf3; }
.fcd-chart-card canvas { height: 280px !important; }
.fcd-breakeven { background: #1C2333; border-radius: 12px; padding: 16px; }
.fcd-stub { padding: 40px; text-align: center; color: #9ca3af; background: #1C2333; border-radius: 12px; }
.fcd-table { width: 100%; border-collapse: collapse; }
.fcd-table th, .fcd-table td { padding: 8px 12px; text-align: left; border-bottom: 1px solid #2D3748; font-size: 13px; }
.fcd-table th { color: #9ca3af; font-weight: 600; text-transform: uppercase; font-size: 11px; letter-spacing: 0.05em; }
@media (max-width: 900px) {
  .fcd-kpi-row { grid-template-columns: repeat(2, 1fr); }
  .fcd-chart-row { grid-template-columns: 1fr; }
}
```

**1f) Blueprint registrieren in `app.py`:**

Im bestehenden Block wo andere Blueprints registriert werden:
```python
from routes.admin_dashboard import admin_dashboard_bp
app.register_blueprint(admin_dashboard_bp)
```

**1g) `tests/test_admin_dashboard_auth.py`:**

```python
"""Phase 04.7.2 — admin_dashboard auth gate integration tests."""
import pytest
from flask import Flask

def _make_normal_user(db_session):
    from database.models import User, Organisation
    from werkzeug.security import generate_password_hash
    org = Organisation(name='TestCo', plan_key='solo', subscription_status='active')
    db_session.add(org); db_session.flush()
    u = User(email='normal@example.com', passwort_hash=generate_password_hash('pw'),
             rolle='owner', org_id=org.id, is_superadmin=False)
    db_session.add(u); db_session.commit()
    return u


def _make_superadmin(db_session):
    from database.models import User, Organisation
    from werkzeug.security import generate_password_hash
    org = Organisation(name='NerveOrg', plan_key='coach', subscription_status='active')
    db_session.add(org); db_session.flush()
    u = User(email='andre@nerve.app', passwort_hash=generate_password_hash('pw'),
             rolle='owner', org_id=org.id, is_superadmin=True)
    db_session.add(u); db_session.commit()
    return u


def test_unauthenticated_redirects_to_login(client):
    r = client.get('/admin/dashboard/', follow_redirects=False)
    assert r.status_code in (302, 401)


def test_normal_user_gets_403(client, db_session):
    u = _make_normal_user(db_session)
    with client.session_transaction() as s:
        s['user_id'] = u.id
    r = client.get('/admin/dashboard/')
    assert r.status_code == 403


def test_superadmin_gets_200_with_6_tabs(client, db_session):
    u = _make_superadmin(db_session)
    with client.session_transaction() as s:
        s['user_id'] = u.id
    r = client.get('/admin/dashboard/')
    assert r.status_code == 200
    html = r.data.decode('utf-8')
    for tab in ['uebersicht', 'einnahmen', 'ausgaben', 'kunden', 'eur', 'export']:
        assert f'data-tab="{tab}"' in html, f"tab {tab} missing from shell"


def test_invalid_period_format_400(client, db_session):
    u = _make_superadmin(db_session)
    with client.session_transaction() as s:
        s['user_id'] = u.id
    r = client.get('/admin/dashboard/?period=BADFORMAT')
    assert r.status_code == 400


def test_valid_period_200(client, db_session):
    u = _make_superadmin(db_session)
    with client.session_transaction() as s:
        s['user_id'] = u.id
    r = client.get('/admin/dashboard/?period=2026-03')
    assert r.status_code == 200
```

**Hinweis:** `client` und `db_session` fixture müssen aus tests/conftest.py verfügbar sein. Falls `client` fehlt, eine minimale Flask test_client-Fixture in conftest.py ergänzen (nach dem bestehenden db_session Muster). Nicht dieser Plan soll die Fixture hand-rollen, aber falls noch nicht vorhanden dokumentieren und in conftest.py eintragen.
  </action>
  <verify>
    <automated>pytest tests/test_admin_dashboard_auth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `routes/admin_dashboard.py` existiert mit `admin_dashboard_bp`
    - `grep -c "@superadmin_required" routes/admin_dashboard.py` >= 1
    - `grep -c "PERIOD_RE" routes/admin_dashboard.py` >= 1
    - `templates/admin/dashboard.html` existiert und enthält `data-tab="uebersicht"` ... `data-tab="export"` (alle 6)
    - 4 Stub-Tab-Templates existieren
    - `static/admin_dashboard.js` und `static/admin_dashboard.css` existieren
    - `grep -c "admin_dashboard_bp" app.py` >= 1
    - `pytest tests/test_admin_dashboard_auth.py -x -q` grün, 5 tests passed
  </acceptance_criteria>
  <done>Shell live, Auth-Gate verifiziert, Tab-Switching funktional.</done>
</task>

<task type="auto">
  <name>Task 2: Tab Übersicht — KPI-Endpoint + Template + 2 Charts</name>
  <read_first>
    - routes/admin_dashboard.py (Blueprint + _parse_period aus Task 1)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\02 Projekte\NERVE Admin Dashboard Final.md (Abschnitt "Tab 1: ÜBERSICHT")
    - database/models.py (RevenueLog, ApiCostLog, FixedCost, Organisation, User)
  </read_first>
  <files>
    routes/admin_dashboard.py
    templates/admin/_tab_uebersicht.html
  </files>
  <action>
**2a) In `routes/admin_dashboard.py` neue Endpoints + Helpers:**

```python
# ── Tab Übersicht: KPI Endpoint ─────────────────────────────────────

@admin_dashboard_bp.route('/api/overview')
@login_required
@superadmin_required
def api_overview():
    from database.models import (
        RevenueLog, ApiCostLog, FixedCost, Organisation, User
    )
    from sqlalchemy import func

    period = request.args.get('period')
    start, end = _parse_period(period)

    db = get_session()
    try:
        # MRR: Summe der aktiven Subscriptions (forward-looking)
        mrr_query = (db.query(func.sum(Organisation.plan_price_eur))
                       .filter(Organisation.subscription_status == 'active'))
        mrr_eur = float(mrr_query.scalar() or 0)

        # Einnahmen in Periode (backward-looking)
        revenue_q = (db.query(func.sum(RevenueLog.netto_cents))
                       .filter(RevenueLog.paid_at >= start,
                               RevenueLog.paid_at < end))
        revenue_eur = float((revenue_q.scalar() or 0) / 100.0)

        # API-Kosten in Periode
        api_costs_q = (db.query(func.sum(ApiCostLog.cost_eur))
                         .filter(ApiCostLog.created_at >= start,
                                 ApiCostLog.created_at < end))
        api_costs_eur = float(api_costs_q.scalar() or 0)

        # Fixkosten (cycle='monthly')
        fc_monthly = (db.query(func.sum(FixedCost.amount_eur))
                        .filter(FixedCost.cycle == 'monthly', FixedCost.active.is_(True))
                        .scalar() or 0)
        total_costs = api_costs_eur + float(fc_monthly)

        # Aktive User
        active_users = (db.query(func.count(User.id))
                          .join(Organisation, Organisation.id == User.org_id)
                          .filter(Organisation.subscription_status == 'active')
                          .scalar() or 0)

        gewinn = revenue_eur - total_costs
        marge_pct = (gewinn / revenue_eur * 100.0) if revenue_eur > 0 else 0

        # 12-Monats-Serie
        labels_12m = []
        mrr_12m = []
        costs_12m = []
        margin_12m = []
        from datetime import date as _d
        import calendar
        cursor_year, cursor_month = start.year, start.month
        for _ in range(12):
            cursor_month -= 1
            if cursor_month == 0:
                cursor_month = 12
                cursor_year -= 1
        for _ in range(12):
            cursor_month += 1
            if cursor_month > 12:
                cursor_month = 1
                cursor_year += 1
            m_start = _d(cursor_year, cursor_month, 1)
            _last = calendar.monthrange(cursor_year, cursor_month)[1]
            m_end = _d(cursor_year, cursor_month, _last) + timedelta(days=1)
            rev = float((db.query(func.sum(RevenueLog.netto_cents))
                           .filter(RevenueLog.paid_at >= m_start,
                                   RevenueLog.paid_at < m_end).scalar() or 0) / 100.0)
            api_c = float(db.query(func.sum(ApiCostLog.cost_eur))
                            .filter(ApiCostLog.created_at >= m_start,
                                    ApiCostLog.created_at < m_end).scalar() or 0)
            total_c = api_c + float(fc_monthly)
            labels_12m.append(f"{cursor_year}-{cursor_month:02d}")
            mrr_12m.append(round(rev, 2))
            costs_12m.append(round(total_c, 2))
            margin_12m.append(round((rev - total_c) / rev * 100, 1) if rev > 0 else 0)

        return jsonify({
            'period': start.strftime('%Y-%m'),
            'kpis': {
                'mrr': f"{mrr_eur:,.2f} €".replace(',', '.'),
                'costs_mo': f"{total_costs:,.2f} €".replace(',', '.'),
                'marge': f"{marge_pct:,.1f} %".replace(',', '.'),
                'active_users': str(int(active_users)),
                'gewinn_mo': f"{gewinn:,.2f} €".replace(',', '.'),
            },
            'mrr_costs_12m': {
                'labels': labels_12m,
                'mrr': mrr_12m,
                'costs': costs_12m,
            },
            'margin_12m': {
                'labels': labels_12m,
                'values': margin_12m,
            },
        })
    finally:
        db.close()
```

**WICHTIG:** `Organisation.plan_price_eur` muss existieren — falls nicht, ersetze durch Mapping via PLANS Dict aus config.py: `sum(PLANS[o.plan_key]['price'] for o in active_orgs)`. Prüfen bei Execution.

**2b) `templates/admin/_tab_uebersicht.html`:**

```html
<div class="fcd-kpi-row">
  <div class="fcd-kpi">
    <h3>MRR</h3>
    <div class="value" data-kpi="mrr">—</div>
    <div class="delta">aktive Abos</div>
  </div>
  <div class="fcd-kpi">
    <h3>Kosten / Monat</h3>
    <div class="value" data-kpi="costs_mo">—</div>
    <div class="delta">API + Fixkosten</div>
  </div>
  <div class="fcd-kpi">
    <h3>Marge</h3>
    <div class="value" data-kpi="marge">—</div>
    <div class="delta">(Gewinn / Einnahmen)</div>
  </div>
  <div class="fcd-kpi">
    <h3>User</h3>
    <div class="value" data-kpi="active_users">—</div>
    <div class="delta">aktive Subscriptions</div>
  </div>
  <div class="fcd-kpi">
    <h3>Gewinn / Monat</h3>
    <div class="value" data-kpi="gewinn_mo">—</div>
    <div class="delta">vor Steuern</div>
  </div>
</div>

<div class="fcd-chart-row">
  <div class="fcd-chart-card">
    <h3>MRR + Kosten (12 Monate)</h3>
    <canvas id="fcd-chart-mrr-costs"></canvas>
  </div>
  <div class="fcd-chart-card">
    <h3>Marge-Entwicklung (12 Monate)</h3>
    <canvas id="fcd-chart-margin"></canvas>
  </div>
</div>

<div class="fcd-breakeven">
  <strong>BREAKEVEN:</strong> Live-Berechnung aus aktuellen KPIs. Ziel: Gewinn > 0.
</div>
```
  </action>
  <verify>
    <automated>python -c "import routes.admin_dashboard; assert hasattr(routes.admin_dashboard, 'api_overview')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api_overview" routes/admin_dashboard.py` >= 1
    - `grep -c "data-kpi=\"mrr\"" templates/admin/_tab_uebersicht.html` == 1
    - `grep -c "data-kpi=\"costs_mo\"" templates/admin/_tab_uebersicht.html` == 1
    - `grep -c "data-kpi=\"marge\"" templates/admin/_tab_uebersicht.html` == 1
    - `grep -c "data-kpi=\"active_users\"" templates/admin/_tab_uebersicht.html` == 1
    - `grep -c "data-kpi=\"gewinn_mo\"" templates/admin/_tab_uebersicht.html` == 1
    - `grep -c "fcd-chart-mrr-costs" templates/admin/_tab_uebersicht.html` == 1
    - `grep -c "fcd-chart-margin" templates/admin/_tab_uebersicht.html` == 1
    - `pytest tests/test_admin_dashboard_auth.py -x -q` bleibt grün
  </acceptance_criteria>
  <done>Tab Übersicht mit JSON-Endpoint + KPI-Template + 2 Charts wired.</done>
</task>

<task type="auto">
  <name>Task 3: Tab Einnahmen — Aggregat-Endpoint + Template mit 3 Tabellen + Transaktionsliste</name>
  <read_first>
    - routes/admin_dashboard.py (nach Task 2)
    - database/models.py (RevenueLog Felder)
    - C:\Users\andre\OneDrive\Desktop\Nerve-Vault\02 Projekte\NERVE Admin Dashboard Final.md (Abschnitt "Tab 2: EINNAHMEN" — Zusammenfassung, Plan-Split, Länder-Split, Tabelle)
  </read_first>
  <files>
    routes/admin_dashboard.py
    templates/admin/_tab_einnahmen.html
  </files>
  <action>
**3a) Neue Endpoints in `routes/admin_dashboard.py`:**

```python
# ── Tab Einnahmen ────────────────────────────────────────────────────

@admin_dashboard_bp.route('/einnahmen')
@login_required
@superadmin_required
def einnahmen_page():
    period = request.args.get('period')
    start, end = _parse_period(period)
    from database.models import RevenueLog, Organisation
    from sqlalchemy import func
    db = get_session()
    try:
        # Summary by tax_treatment
        rows = (db.query(RevenueLog.tax_treatment,
                         func.sum(RevenueLog.netto_cents),
                         func.sum(RevenueLog.ust_cents),
                         func.sum(RevenueLog.brutto_cents),
                         func.count(RevenueLog.id))
                  .filter(RevenueLog.paid_at >= start, RevenueLog.paid_at < end)
                  .group_by(RevenueLog.tax_treatment).all())
        summary = [{
            'treatment': r[0],
            'netto': round((r[1] or 0) / 100.0, 2),
            'ust': round((r[2] or 0) / 100.0, 2),
            'brutto': round((r[3] or 0) / 100.0, 2),
            'count': r[4],
        } for r in rows]

        # By plan
        plan_rows = (db.query(RevenueLog.plan_key,
                              func.count(RevenueLog.id),
                              func.sum(RevenueLog.netto_cents))
                       .filter(RevenueLog.paid_at >= start, RevenueLog.paid_at < end)
                       .group_by(RevenueLog.plan_key).all())
        by_plan = [{'plan': (r[0] or '—'), 'count': r[1], 'netto': round((r[2] or 0)/100.0, 2)} for r in plan_rows]

        # By country (für USt-VA)
        country_rows = (db.query(RevenueLog.country, RevenueLog.tax_treatment,
                                 func.count(RevenueLog.id),
                                 func.sum(RevenueLog.netto_cents))
                          .filter(RevenueLog.paid_at >= start, RevenueLog.paid_at < end)
                          .group_by(RevenueLog.country, RevenueLog.tax_treatment).all())
        by_country = [{'country': r[0] or '?', 'treatment': r[1], 'count': r[2],
                       'netto': round((r[3] or 0)/100.0, 2)} for r in country_rows]

        # Transactions (paginated)
        page = max(1, int(request.args.get('page', 1)))
        per_page = 50
        txs = (db.query(RevenueLog)
                 .filter(RevenueLog.paid_at >= start, RevenueLog.paid_at < end)
                 .order_by(RevenueLog.paid_at.desc())
                 .offset((page - 1) * per_page).limit(per_page).all())
        transactions = [{
            'date': t.paid_at.strftime('%Y-%m-%d') if t.paid_at else '',
            'invoice_id': t.stripe_invoice_id,
            'country': t.country or '',
            'plan': t.plan_key or '',
            'netto': round(t.netto_cents / 100.0, 2),
            'ust': round(t.ust_cents / 100.0, 2),
            'brutto': round(t.brutto_cents / 100.0, 2),
            'treatment': t.tax_treatment,
        } for t in txs]

        return jsonify({
            'period': start.strftime('%Y-%m'),
            'summary': summary,
            'by_plan': by_plan,
            'by_country': by_country,
            'transactions': transactions,
            'page': page,
        })
    finally:
        db.close()
```

**3b) `templates/admin/_tab_einnahmen.html`:**

```html
<div class="fcd-einnahmen" data-endpoint="/admin/dashboard/einnahmen">
  <h3>EINNAHMEN — <span class="fcd-period-label"></span></h3>

  <h4>Zusammenfassung</h4>
  <table class="fcd-table">
    <thead><tr><th>Kategorie</th><th>Netto</th><th>USt</th><th>Brutto</th><th>Anzahl</th></tr></thead>
    <tbody id="fcd-einn-summary"></tbody>
  </table>

  <h4 style="margin-top:24px;">Nach Abo-Plan</h4>
  <table class="fcd-table">
    <thead><tr><th>Plan</th><th>Anzahl</th><th>Netto</th></tr></thead>
    <tbody id="fcd-einn-plans"></tbody>
  </table>

  <h4 style="margin-top:24px;">Nach Kundenland (USt-VA-relevant)</h4>
  <table class="fcd-table">
    <thead><tr><th>Land</th><th>Behandlung</th><th>Anzahl</th><th>Netto</th></tr></thead>
    <tbody id="fcd-einn-country"></tbody>
  </table>

  <h4 style="margin-top:24px;">Einzeltransaktionen</h4>
  <table class="fcd-table">
    <thead><tr><th>Datum</th><th>Rechnung</th><th>Land</th><th>Plan</th><th>Netto</th><th>USt</th><th>Brutto</th></tr></thead>
    <tbody id="fcd-einn-tx"></tbody>
  </table>
</div>
<script>
(function(){
  async function load() {
    const period = document.querySelector('.fcd-wrap').dataset.period;
    const r = await fetch('/admin/dashboard/einnahmen?period=' + encodeURIComponent(period));
    if (!r.ok) return;
    const d = await r.json();
    document.querySelectorAll('.fcd-period-label').forEach(e => e.textContent = d.period);
    const fmt = n => n.toLocaleString('de-DE',{minimumFractionDigits:2,maximumFractionDigits:2}) + ' €';
    document.getElementById('fcd-einn-summary').innerHTML =
      (d.summary||[]).map(s=>`<tr><td>${s.treatment}</td><td>${fmt(s.netto)}</td><td>${fmt(s.ust)}</td><td>${fmt(s.brutto)}</td><td>${s.count}</td></tr>`).join('');
    document.getElementById('fcd-einn-plans').innerHTML =
      (d.by_plan||[]).map(p=>`<tr><td>${p.plan}</td><td>${p.count}</td><td>${fmt(p.netto)}</td></tr>`).join('');
    document.getElementById('fcd-einn-country').innerHTML =
      (d.by_country||[]).map(c=>`<tr><td>${c.country}</td><td>${c.treatment}</td><td>${c.count}</td><td>${fmt(c.netto)}</td></tr>`).join('');
    document.getElementById('fcd-einn-tx').innerHTML =
      (d.transactions||[]).map(t=>`<tr><td>${t.date}</td><td>${t.invoice_id}</td><td>${t.country}</td><td>${t.plan}</td><td>${fmt(t.netto)}</td><td>${fmt(t.ust)}</td><td>${fmt(t.brutto)}</td></tr>`).join('');
  }
  // Load when tab becomes visible
  const obs = new MutationObserver(()=>{
    const p = document.getElementById('panel-einnahmen');
    if (p && !p.hidden && !p.dataset.loaded) { p.dataset.loaded = '1'; load(); }
  });
  obs.observe(document.getElementById('panel-einnahmen'), {attributes:true});
  // Initial load if active
  const panel = document.getElementById('panel-einnahmen');
  if (panel && !panel.hidden) { panel.dataset.loaded = '1'; load(); }
})();
</script>
```
  </action>
  <verify>
    <automated>pytest tests/test_admin_dashboard_auth.py -x -q && python -c "import routes.admin_dashboard; assert hasattr(routes.admin_dashboard, 'einnahmen_page')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def einnahmen_page" routes/admin_dashboard.py` == 1
    - `grep -c "@admin_dashboard_bp.route('/einnahmen')" routes/admin_dashboard.py` == 1
    - `grep -c "tax_treatment" routes/admin_dashboard.py` >= 2
    - `grep -c "fcd-einn-summary" templates/admin/_tab_einnahmen.html` == 1
    - `grep -c "fcd-einn-plans" templates/admin/_tab_einnahmen.html` == 1
    - `grep -c "fcd-einn-country" templates/admin/_tab_einnahmen.html` == 1
    - `grep -c "fcd-einn-tx" templates/admin/_tab_einnahmen.html` == 1
    - `pytest tests/test_admin_dashboard_auth.py -x -q` grün
  </acceptance_criteria>
  <done>Tab Einnahmen lädt Aggregat-JSON + rendert 4 Tabellen client-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normal User / Org-Owner → /admin/dashboard | **KRITISCH** — niemand außer is_superadmin darf diese Routes sehen. D-08 Rationale: Andrés Privatzahlen. |
| Query-Param `period` | User-Input, in SQL-Filter gebraucht → Regex-Validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.7.2-04-01 | E (Privilege Escalation) | `/admin/dashboard/*` | mitigate | `@login_required` + `@superadmin_required` auf JEDER Route (nicht nur Index). Per D-08 auf `is_superadmin` nicht `rolle`. |
| T-04.7.2-04-02 | T (Tampering) | period-Param → SQL | mitigate | PERIOD_RE `^\d{4}-\d{2}$`, abort(400) bei Format-Mismatch. SQLAlchemy ORM, keine rohen Queries. |
| T-04.7.2-04-03 | T (Tampering) | page-Param | mitigate | `max(1, int(...))` mit default 1 |
| T-04.7.2-04-04 | I (Info Disclosure) | JSON-Endpoints leaken volle Liste | accept | Ziel-Audience ist 1 User (André), keine Paginierungs-Pflicht für Privacy |
| T-04.7.2-04-05 | T (XSS) | JS innerHTML mit Server-Daten | mitigate | stripe_invoice_id + country + plan_key sind kontrollierte Werte aus Stripe/Seed. Falls später User-Eingabe rein: escapeHTML-Helper ergänzen. |
</threat_model>

<verification>
- `pytest tests/test_admin_dashboard_auth.py -x -q` grün (5 tests)
- Smoke: `/admin/dashboard/` als Superadmin → 200, HTML enthält alle 6 Tabs
- `/admin/dashboard/api/overview?period=2026-03` → JSON mit `kpis`, `mrr_costs_12m`, `margin_12m`
- `/admin/dashboard/einnahmen?period=2026-03` → JSON mit `summary`, `by_plan`, `by_country`, `transactions`
</verification>

<success_criteria>
- Blueprint registriert, Auth-Gate getestet
- Tab Übersicht voll funktional mit Charts
- Tab Einnahmen voll funktional mit 4 Tabellen
- Stubs für Ausgaben/Kunden/EÜR/Export vorhanden
</success_criteria>

<output>
`.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-04-SUMMARY.md`
</output>
