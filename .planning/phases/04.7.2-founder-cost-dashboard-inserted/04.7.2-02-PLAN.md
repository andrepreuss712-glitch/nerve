---
phase: 04.7.2
plan: 02
type: execute
wave: 2
depends_on: ["04.7.2-01"]
files_modified:
  - services/cost_tracker.py
  - services/claude_service.py
  - services/deepgram_service.py
  - services/training_service.py
  - tests/test_cost_tracker.py
autonomous: true
requirements:
  - BE-CD-02
must_haves:
  truths:
    - "Jeder Claude messages.create Call erzeugt 2 api_cost_log Rows (input + output tokens)"
    - "Jeder Deepgram Session-Close erzeugt 1 api_cost_log Row mit korrekten Minuten"
    - "Jede ElevenLabs text_to_speech Call erzeugt 1 api_cost_log Row mit Zeichenanzahl"
    - "Wechselkurs und rate_applied werden beim Schreiben eingefroren und NIE später neu berechnet"
    - "log_api_cost Exception crashed nie den API-Call (silent skip wenn ApiRate fehlt)"
  artifacts:
    - path: "services/cost_tracker.py"
      provides: "log_api_cost(provider, model, user_id, units, unit_type, ...) Helper"
      exports: ["log_api_cost"]
    - path: "services/claude_service.py"
      provides: "Hook nach jedem messages.create"
      contains: "log_api_cost"
    - path: "services/deepgram_service.py"
      provides: "Hook beim Close der Session mit Minuten-Delta"
      contains: "log_api_cost"
    - path: "services/training_service.py"
      provides: "Hook nach text_to_speech mit Zeichenanzahl"
      contains: "log_api_cost"
  key_links:
    - from: "services/claude_service.py"
      to: "api_cost_log"
      via: "cost_tracker.log_api_cost() Helper pro messages.create Aufruf"
      pattern: "log_api_cost\\('anthropic'"
    - from: "services/deepgram_service.py"
      to: "api_cost_log"
      via: "log_api_cost bei Session-Close, Minuten aus open/close Delta"
      pattern: "log_api_cost\\('deepgram'"
    - from: "services/training_service.py"
      to: "api_cost_log"
      via: "log_api_cost nach TTS-Request mit len(text)"
      pattern: "log_api_cost\\('elevenlabs'"
---

<objective>
Per-D-02: API-Cost-Tracking-Hooks in die 3 Service-Dateien einbauen. Neuer Helper `services/cost_tracker.py` kapselt das Schreiben + Fx-Freeze + ApiRate-Lookup. Hooks sind nicht-blockierend (try/except, silent skip).

Purpose: Ohne diese Hooks bleibt api_cost_log leer → Tab Ausgaben + Tab Kunden (Profitabilität) + EÜR-Calculator sind nutzlos.

Output: Nach jeder API-Interaktion erscheint automatisch eine api_cost_log Row mit gefrorenem Kurs und Rate. Tests grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-CONTEXT.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-RESEARCH.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-01-SUMMARY.md
@database/models.py
@services/claude_service.py
@services/deepgram_service.py
@services/training_service.py
@services/live_session.py

<interfaces>
Aus Plan 01 verfügbar:
```python
# database/models.py
class ApiCostLog(Base):
    provider: str; model: str; user_id: int|None; org_id: int|None
    units: Numeric; unit_type: str
    rate_applied: Numeric; rate_currency: str; fx_rate_applied: Numeric
    cost_eur: Numeric; session_id: str|None; context_tag: str|None

class ApiRate(Base):
    provider: str; model: str; unit_type: str
    price_per_unit: Numeric; currency: str; active: bool

class ExchangeRate(Base):
    date: Date; currency_pair: str; rate: Numeric
```

Referenz-Pattern aus Phase 04.7.1 (services/claude_service.py:97):
```python
# _write_ft_assistant_event — try/except kapselnd, liest live_session.state.user_id,
# nicht-blockierend, silent failure mit print("[FT] write failed: ...")
```

Anthropic SDK Response (VERIFIED):
```python
msg = claude_client.messages.create(...)
msg.usage.input_tokens   # int
msg.usage.output_tokens  # int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/cost_tracker.py — Helper mit Fx-Freeze + nicht-blockierendem Hook</name>
  <read_first>
    - database/models.py (ApiCostLog, ApiRate, ExchangeRate Signaturen)
    - services/claude_service.py Zeile 97 (_write_ft_assistant_event als Referenz-Pattern für nicht-blockierenden DB-Write)
    - services/live_session.py (state_lock, state Dict mit 'user_id', 'org_id', 'session_id')
    - tests/conftest.py (db_session Fixture)
  </read_first>
  <files>
    services/cost_tracker.py
    tests/test_cost_tracker.py
  </files>
  <behavior>
    - Test 1 (test_freeze_fx_on_write): log_api_cost schreibt cost_eur = units * rate_applied * fx_rate_applied mit exakt eingefrorenen Werten; nachträgliche Änderung von ExchangeRate ändert den persistierten cost_eur NICHT
    - Test 2 (test_missing_rate_no_raise): log_api_cost mit unbekanntem (provider, model, unit_type) raised nicht, printet Warning, keine Row geschrieben
    - Test 3 (test_eur_rate_currency_no_fx): Wenn rate_currency='EUR', fx_rate_applied=1.0, cost_eur = units * rate_applied
    - Test 4 (test_reads_user_id_from_live_session): Wenn user_id=None übergeben, wird live_session.state['user_id'] gelesen
    - Test 5 (test_silent_on_db_error): Simulierter DB-Error (mock SessionLocal) → kein Raise, print Warning
  </behavior>
  <action>
**1a) `services/cost_tracker.py` neu erstellen:**

```python
"""Phase 04.7.2 — API Cost Tracker.

Pattern: nicht-blockierend. Schreibt api_cost_log mit eingefrorenem Wechselkurs
und gefrorener ApiRate. Darf NIEMALS raisen — API-Calls duerfen nie wegen
Logging-Fehler crashen (Referenz: _write_ft_assistant_event aus Phase 04.7.1).

D-02: Wechselkurs + rate_applied werden beim Schreiben eingefroren.
Nachtraegliche Kursaenderungen veraendern keine bestehenden Buchungen.
"""
from __future__ import annotations
from decimal import Decimal


def _get_current_fx_rate(db, rate_currency: str) -> Decimal:
    """Liest den neuesten USD_EUR Kurs aus exchange_rates. Fallback 0.92."""
    if rate_currency == 'EUR':
        return Decimal('1.0')
    from database.models import ExchangeRate
    row = (db.query(ExchangeRate)
             .filter_by(currency_pair=f'{rate_currency}_EUR')
             .order_by(ExchangeRate.date.desc())
             .first())
    return Decimal(str(row.rate)) if row else Decimal('0.92')


def _resolve_user_id_from_live_session() -> int | None:
    """Liest user_id aus live_session.state fuer Background-Thread-Kontexte
    (analyse_loop, Deepgram-Close) wo kein Flask g.user verfuegbar ist."""
    try:
        import services.live_session as ls
        with ls.state_lock:
            return ls.state.get('user_id')
    except Exception:
        return None


def _resolve_org_id_from_live_session() -> int | None:
    try:
        import services.live_session as ls
        with ls.state_lock:
            return ls.state.get('org_id')
    except Exception:
        return None


def log_api_cost(
    provider: str,
    model: str,
    user_id: int | None,
    units: float,
    unit_type: str,
    *,
    org_id: int | None = None,
    session_id: str | None = None,
    context_tag: str | None = None,
) -> None:
    """Schreibt api_cost_log Eintrag. Darf NIEMALS raisen.

    Args:
        provider: 'anthropic' | 'deepgram' | 'elevenlabs' | 'stripe'
        model: z.B. 'haiku-4-5', 'nova-2', 'multilingual-v2'
        user_id: User-ID oder None (wird dann aus live_session.state gelesen)
        units: Anzahl Einheiten (tokens/1000, Minuten, chars/1000)
        unit_type: muss zu ApiRate.unit_type matchen
        org_id, session_id, context_tag: optional
    """
    try:
        from database.db import SessionLocal
        from database.models import ApiCostLog, ApiRate
        if user_id is None:
            user_id = _resolve_user_id_from_live_session()
        if org_id is None:
            org_id = _resolve_org_id_from_live_session()

        db = SessionLocal()
        try:
            rate = (db.query(ApiRate)
                      .filter_by(provider=provider, model=model,
                                 unit_type=unit_type, active=True)
                      .first())
            if not rate:
                print(f"[CostTracker] no active ApiRate for "
                      f"{provider}/{model}/{unit_type} — skipping log")
                return

            fx_rate = _get_current_fx_rate(db, rate.currency or 'USD')
            units_d = Decimal(str(units))
            rate_d = Decimal(str(rate.price_per_unit))
            cost_eur = (units_d * rate_d * fx_rate).quantize(Decimal('0.000001'))

            db.add(ApiCostLog(
                provider=provider,
                model=model,
                user_id=user_id,
                org_id=org_id,
                units=units_d,
                unit_type=unit_type,
                rate_applied=rate_d,
                rate_currency=rate.currency or 'USD',
                fx_rate_applied=fx_rate,
                cost_eur=cost_eur,
                session_id=session_id,
                context_tag=context_tag,
            ))
            db.commit()
        finally:
            db.close()
    except Exception as e:
        print(f"[CostTracker] log_api_cost failed ({provider}/{model}): {e}")
```

**1b) `tests/test_cost_tracker.py` — Skeleton aus Plan 01 mit echten Tests ersetzen:**

```python
"""Phase 04.7.2 — cost_tracker unit tests."""
from decimal import Decimal
import pytest
from database.models import ApiCostLog, ApiRate, ExchangeRate
from services.cost_tracker import log_api_cost


@pytest.fixture
def seeded_rate(db_session):
    """Legt eine ApiRate + ExchangeRate für Tests an."""
    rate = ApiRate(provider='anthropic', model='haiku-test',
                   unit_type='per_1k_input_tokens',
                   price_per_unit=Decimal('0.00025'),
                   currency='USD', active=True)
    db_session.add(rate)
    from datetime import date
    fx = ExchangeRate(date=date(2026, 4, 1), currency_pair='USD_EUR',
                      rate=Decimal('0.92'), source='test')
    db_session.add(fx)
    db_session.commit()
    return rate


def test_freeze_fx_on_write(db_session, seeded_rate):
    log_api_cost('anthropic', 'haiku-test', user_id=1, units=10.0,
                 unit_type='per_1k_input_tokens')
    row = db_session.query(ApiCostLog).filter_by(provider='anthropic').first()
    assert row is not None
    assert float(row.fx_rate_applied) == 0.92
    assert float(row.rate_applied) == 0.00025
    # 10 * 0.00025 * 0.92 = 0.0023
    assert abs(float(row.cost_eur) - 0.0023) < 1e-6


def test_missing_rate_no_raise(db_session):
    # KEIN seeded_rate — log should silently skip
    log_api_cost('unknown', 'noop', user_id=1, units=5, unit_type='per_minute')
    assert db_session.query(ApiCostLog).count() == 0  # nothing written


def test_eur_rate_currency_no_fx(db_session):
    rate = ApiRate(provider='stripe', model='card', unit_type='fixed_per_tx',
                   price_per_unit=Decimal('0.25'), currency='EUR', active=True)
    db_session.add(rate)
    db_session.commit()
    log_api_cost('stripe', 'card', user_id=1, units=1.0, unit_type='fixed_per_tx')
    row = db_session.query(ApiCostLog).filter_by(provider='stripe').first()
    assert row is not None
    assert float(row.fx_rate_applied) == 1.0
    assert float(row.cost_eur) == 0.25


def test_silent_on_db_error(monkeypatch, capsys):
    def broken_session():
        raise RuntimeError("db down")
    monkeypatch.setattr('database.db.SessionLocal', broken_session)
    # Should NOT raise
    log_api_cost('anthropic', 'haiku-test', user_id=1, units=1.0,
                 unit_type='per_1k_input_tokens')
    captured = capsys.readouterr()
    assert 'failed' in captured.out.lower() or 'CostTracker' in captured.out
```
  </action>
  <verify>
    <automated>pytest tests/test_cost_tracker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Datei `services/cost_tracker.py` existiert
    - `grep -c "def log_api_cost" services/cost_tracker.py` == 1
    - `grep -c "def _get_current_fx_rate" services/cost_tracker.py` == 1
    - `grep -c "except Exception" services/cost_tracker.py` >= 2 (einmal um DB-Block, einmal im äußeren try)
    - `pytest tests/test_cost_tracker.py -x -q` exit 0, 4 passed
    - Keine Test-ist-skipped mehr in test_cost_tracker.py
  </acceptance_criteria>
  <done>cost_tracker importierbar, 4 Tests grün, Hook-Funktion verfügbar für Task 2+3.</done>
</task>

<task type="auto">
  <name>Task 2: Hook in services/claude_service.py nach jedem messages.create</name>
  <read_first>
    - services/claude_service.py (alle Vorkommen von `messages.create` lokalisieren — laut RESEARCH.md Zeilen ~449, ~476, ~492)
    - services/cost_tracker.py (von Task 1)
  </read_first>
  <files>services/claude_service.py</files>
  <action>
**Schritt 1:** Alle Stellen mit `claude_client.messages.create(...)` finden (in der Regel 3: Live-Haiku in `analysiere_mit_claude`, Post-Sonnet in `generate_scoring`, EWB-Ranking in `rank_ewb`). Für jede Stelle nach der Zuweisung `msg = ...` einen Hook-Block einfügen.

**Schritt 2:** Pro Stelle folgendes Muster einsetzen (Beispiel Live-Haiku):

```python
msg = claude_client.messages.create(
    model="claude-haiku-4-5-20251001",
    ...
)

# ── Phase 04.7.2 Cost-Hook ─────────────────────────────────────────
try:
    from services.cost_tracker import log_api_cost
    model_key = 'haiku-4-5'  # MUSS mit ApiRate.model matchen
    input_units = (msg.usage.input_tokens or 0) / 1000.0
    output_units = (msg.usage.output_tokens or 0) / 1000.0
    log_api_cost('anthropic', model_key, user_id=None,
                 units=input_units, unit_type='per_1k_input_tokens',
                 context_tag='live_haiku')
    log_api_cost('anthropic', model_key, user_id=None,
                 units=output_units, unit_type='per_1k_output_tokens',
                 context_tag='live_haiku')
except Exception as _e:
    print(f"[CostHook] claude haiku skipped: {_e}")
# ────────────────────────────────────────────────────────────────────
```

**Mapping der context_tag:**
- Live-Haiku (analysiere_mit_claude, analyse_loop) → `context_tag='live_haiku'`, `model_key='haiku-4-5'`
- Post-Sonnet (generate_scoring oder äquivalent) → `context_tag='post_sonnet'`, `model_key='sonnet-4'`
- EWB-Ranking (rank_ewb) → `context_tag='ewb_rank'`, `model_key='haiku-4-5'`

**Schritt 3:** Wenn eine Stelle `user_id` explizit verfügbar hat (z.B. über Function-Argument), übergeben statt None. In Background-Threads (analyse_loop) bleibt None → cost_tracker liest es aus live_session.state.

**WICHTIG:** 
- Nicht den bestehenden Code-Flow verändern.
- Der ganze Hook-Block muss in try/except — Cost-Logging darf Claude-Calls NIEMALS blockieren.
- Die exakten Zeilennummern aus RESEARCH.md (449/476/492) zur Orientierung verwenden, aber lokal verifizieren — Datei könnte sich leicht verschoben haben.
- `msg.usage` kann je nach SDK-Version None sein → defensiv mit `getattr(msg, 'usage', None)` + `(getattr(u, 'input_tokens', 0) or 0)`.

Defensive Variante:
```python
try:
    from services.cost_tracker import log_api_cost
    u = getattr(msg, 'usage', None)
    if u is not None:
        in_tok = getattr(u, 'input_tokens', 0) or 0
        out_tok = getattr(u, 'output_tokens', 0) or 0
        log_api_cost('anthropic', 'haiku-4-5', user_id=None,
                     units=in_tok/1000.0, unit_type='per_1k_input_tokens',
                     context_tag='live_haiku')
        log_api_cost('anthropic', 'haiku-4-5', user_id=None,
                     units=out_tok/1000.0, unit_type='per_1k_output_tokens',
                     context_tag='live_haiku')
except Exception as _e:
    print(f"[CostHook] claude haiku skipped: {_e}")
```
  </action>
  <verify>
    <automated>python -c "import ast, sys; t=ast.parse(open('services/claude_service.py').read()); calls=[n for n in ast.walk(t) if isinstance(n, ast.Call) and getattr(getattr(n.func,'attr',None),'__str__',lambda:None)()=='log_api_cost' or (isinstance(n.func, ast.Name) and n.func.id=='log_api_cost')]; print(f'log_api_cost calls found: {len(calls)}'); sys.exit(0 if len(calls)>=4 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "log_api_cost('anthropic'" services/claude_service.py` >= 4 (mindestens 2 Call-Sites × 2 Calls input/output; wenn alle 3 Stellen hooked: 6)
    - `grep -c "context_tag='live_haiku'" services/claude_service.py` >= 1
    - `grep -c "context_tag='post_sonnet'" services/claude_service.py` >= 1 (falls Sonnet-Aufruf in dieser Datei existiert; sonst in der Datei die es enthält hooken)
    - `grep -c "except Exception" services/claude_service.py` ist mindestens um 3 gestiegen (jeder Hook in try/except)
    - `python -c "from services.claude_service import *"` lädt ohne Fehler
    - Kein bestehender Test regrediert: `pytest -x -q` bleibt grün
  </acceptance_criteria>
  <done>Jeder messages.create-Call schreibt automatisch 2 api_cost_log-Rows (input+output). Hook ist exception-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Hooks in deepgram_service.py (STT-Minuten) + training_service.py (TTS-Zeichen)</name>
  <read_first>
    - services/deepgram_service.py (speziell `_open_deepgram_connection` und `_close_deepgram_connection`, Zeile ~150 laut Research)
    - services/training_service.py (`text_to_speech`-Funktion, Zeile ~610 laut Research)
    - services/cost_tracker.py (log_api_cost Signatur)
  </read_first>
  <files>
    services/deepgram_service.py
    services/training_service.py
  </files>
  <action>
**3a) Deepgram-Hook in `services/deepgram_service.py`:**

Ziel: Pro Session die genutzte STT-Dauer in Minuten loggen. Dafür beim Open einen Timestamp speichern, beim Close das Delta berechnen.

Schritt 1: In `_open_deepgram_connection` (oder wo immer eine neue Session-Dict pro sid erstellt wird) einen Key hinzufügen:
```python
import time
session_dict['_cost_opened_at'] = time.time()
```

Schritt 2: In `_close_deepgram_connection` (oder `stop_live_session`-Handler) vor dem tatsächlichen Close:
```python
# ── Phase 04.7.2 Cost-Hook: STT-Minuten ────────────────────────────
try:
    from services.cost_tracker import log_api_cost
    opened = session_dict.get('_cost_opened_at')
    if opened:
        seconds = max(0.0, time.time() - opened)
        minutes = seconds / 60.0
        if minutes > 0.01:  # keine Artefakt-Rows für Sub-Sekunden
            log_api_cost('deepgram', 'nova-2', user_id=None,
                         units=minutes, unit_type='per_minute',
                         context_tag='stt')
except Exception as _e:
    print(f"[CostHook] deepgram stt skipped: {_e}")
# ────────────────────────────────────────────────────────────────────
```

**WICHTIG:**
- `user_id=None` → cost_tracker liest aus live_session.state.
- Falls mehrere sids parallel (z.B. Cold-Call + Meeting-Mode), jedes session_dict hat seinen eigenen `_cost_opened_at`.
- Kein Import von `time` oben fehlen lassen.

**3b) ElevenLabs-TTS-Hook in `services/training_service.py`:**

Ziel: Nach jedem erfolgreichen TTS-Call die Zeichenanzahl loggen.

Schritt 1: In `text_to_speech(text, ...)` nach dem erfolgreichen `requests.post(...)`-Aufruf (oder SDK-Call) und vor dem Return:
```python
# ── Phase 04.7.2 Cost-Hook: TTS-Zeichen ────────────────────────────
try:
    from services.cost_tracker import log_api_cost
    char_units = len(text or '') / 1000.0
    if char_units > 0:
        from flask import g
        uid = getattr(getattr(g, 'user', None), 'id', None)
        log_api_cost('elevenlabs', 'multilingual-v2', user_id=uid,
                     units=char_units, unit_type='per_1k_chars',
                     context_tag='training_tts')
except Exception as _e:
    print(f"[CostHook] elevenlabs tts skipped: {_e}")
# ────────────────────────────────────────────────────────────────────
```

**WICHTIG:**
- `text_to_speech` läuft im HTTP-Request-Kontext (nicht Background-Thread) — daher `g.user.id` verfügbar.
- Falls `g` nicht importiert: `from flask import g` oben in der Datei ergänzen.
- Falls `text_to_speech` auch im Background-Thread verwendet wird (z.B. live assistent): der getattr-Fallback greift dann auf None und cost_tracker liest aus live_session.state.
  </action>
  <verify>
    <automated>grep -c "log_api_cost('deepgram'" services/deepgram_service.py && grep -c "log_api_cost('elevenlabs'" services/training_service.py && python -c "import services.deepgram_service, services.training_service; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "log_api_cost('deepgram'" services/deepgram_service.py` >= 1
    - `grep -c "_cost_opened_at" services/deepgram_service.py` >= 2 (set + read)
    - `grep -c "log_api_cost('elevenlabs'" services/training_service.py` >= 1
    - `grep -c "context_tag='stt'" services/deepgram_service.py` >= 1
    - `grep -c "context_tag='training_tts'" services/training_service.py` >= 1
    - `python -c "import services.deepgram_service, services.training_service"` exit 0
    - Kein bestehender Test regrediert: `pytest -x -q`
  </acceptance_criteria>
  <done>Beide Services haben Cost-Hooks, alle try/except-umschlossen, keine Regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background-Thread → DB | analyse_loop schreibt api_cost_log aus Background-Thread ohne Flask-Context. State-Resolution über live_session.state_lock. |
| Untrusted SDK Response | `msg.usage` aus Anthropic SDK wird verwendet. Defensiv mit getattr, defaults auf 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.7.2-02-01 | D (DoS) | log_api_cost im Hot-Path | mitigate | try/except äußerstes Level, silent failure — API-Calls nie blockiert selbst bei DB-down |
| T-04.7.2-02-02 | T (Tampering) | Wechselkurs nachträglich änderbar | mitigate | D-02: fx_rate_applied + rate_applied werden beim Schreiben eingefroren und in cost_eur persistiert. Dashboard-Aggregate lesen cost_eur direkt, nie neu berechnen. |
| T-04.7.2-02-03 | I (Info Disclosure) | cost_tracker Exception-Print | mitigate | Print enthält nur provider/model/Fehlerklasse, NIE Token-Inhalte oder API-Keys |
| T-04.7.2-02-04 | R (Repudiation) | user_id aus live_session.state könnte falsch sein | accept | Phase 04.7.2 ist internes Dashboard; Attribution ist für Profitabilitäts-Schätzung, kein steuerlicher Beleg pro User |
</threat_model>

<verification>
- `pytest tests/test_cost_tracker.py -x -q` grün
- `pytest -x -q` bleibt gesamt grün (keine Regression in claude/deepgram/training tests)
- Manueller Smoke: eine Claude-Anfrage auslösen → `sqlite3 database/salesnerve.db "SELECT provider, model, units, cost_eur FROM api_cost_log ORDER BY id DESC LIMIT 2"` zeigt 2 Rows (input + output)
</verification>

<success_criteria>
- cost_tracker.py existiert + Tests grün
- 3 Service-Dateien haben Hooks mit try/except
- Keine Regression in bestehenden Tests
</success_criteria>

<output>
`.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-02-SUMMARY.md` mit Fokus auf: genaue Hook-Positionen (Line-Numbers), welche context_tags verwendet werden, welche model-Keys mit ApiRate matchen müssen.
</output>
