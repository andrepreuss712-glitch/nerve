---
phase: 04.7.2
plan: 07
type: execute
wave: 6
depends_on: ["04.7.2-06"]
files_modified:
  - routes/admin_dashboard.py
  - templates/admin/eur_pdf.html
  - templates/admin/_tab_export.html
  - app.py
  - tests/test_eur_pdf.py
autonomous: true
requirements:
  - BE-CD-04
  - BE-CD-07
must_haves:
  truths:
    - "Tab Export bietet 5 Downloads: EÜR-PDF, Einnahmen-CSV, Ausgaben-CSV, USt-VA-CSV, DATEV-Stub-CSV"
    - "EÜR-PDF wird via WeasyPrint aus templates/admin/eur_pdf.html gerendert (nur wenn WeasyPrint installiert)"
    - "HTML-Preview-Endpoint /admin/dashboard/eur.html funktioniert IMMER (auch auf Windows-Dev ohne WeasyPrint)"
    - "de_currency Jinja-Filter ist global registriert und formatiert 1234.56 → '1.234,56 €'"
    - "DATEV-Stub-CSV hat Spalten: Datum,Konto,Gegenkonto,Betrag,Buchungstext (nicht DATEV-format-konform)"
    - "Einnahmen-CSV enthält pro RevenueLog: Datum, Rechnung, Kunde-Country, Plan, Netto, USt, Brutto, Treatment"
    - "Ausgaben-CSV enthält pro Posten: Datum, Anbieter, Kategorie, Netto, EÜR-Zeile, SKR03"
    - "USt-VA-CSV enthält alle KZ-Werte aus compute_eur Output"
  artifacts:
    - path: "routes/admin_dashboard.py"
      provides: "Export-Endpoints für EÜR-PDF + 4 CSVs + HTML-Preview"
    - path: "templates/admin/eur_pdf.html"
      provides: "WeasyPrint-optimiertes PDF-Template mit DIN A4 CSS + deutsches Zahlenformat"
    - path: "templates/admin/_tab_export.html"
      provides: "UI mit 5 Download-Buttons"
    - path: "app.py"
      provides: "Registrierung des de_currency Jinja-Filters"
  key_links:
    - from: "routes/admin_dashboard.py::export_eur_pdf"
      to: "WeasyPrint HTML.write_pdf"
      via: "HTML-Template + weasyprint Library (importorskip wenn nicht installiert)"
      pattern: "weasyprint"
    - from: "templates/admin/eur_pdf.html"
      to: "compute_eur(...) dict"
      via: "Jinja Template-Variable + de_currency Filter"
      pattern: "de_currency"
---

<objective>
Letzte Welle: Tab 6 Export + alle Datei-Downloads. EÜR-PDF via WeasyPrint (nur Linux) + immer verfügbarer HTML-Preview-Fallback. 4 CSV-Exporte. DATEV-Stub (D-07). Jinja-Filter für deutsches Zahlenformat.

Purpose: Phase-Abschluss. Am Monatsende ein Klick → count.tax bekommt alles fertig aufbereitet. Auf Dev-Maschine (Windows) immer noch testbar via HTML-Preview.

Output: 5 Download-Endpoints live, WeasyPrint-Smoke-Test, Phase-Verification bereit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-CONTEXT.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-RESEARCH.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-06-SUMMARY.md
@.planning/phases/04.7.2-founder-cost-dashboard-inserted/HUMAN-TASKS.md
@services/eur_calculator.py
@routes/admin_dashboard.py
@database/models.py

<interfaces>
```python
from services.eur_calculator import compute_eur
# Returns dict mit Keys: period, einnahmen, ausgaben, ergebnis, ust_voranmeldung

import csv  # stdlib
from io import StringIO

# WeasyPrint (optional, nur auf Linux-VPS):
try:
    from weasyprint import HTML
    HTML(string=html).write_pdf()
except ImportError:
    # Fallback: HTML-Preview
```

Jinja Filter Pattern (app.py):
```python
app.jinja_env.filters['de_currency'] = de_currency_filter
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: de_currency Jinja-Filter + EÜR-PDF-Template + PDF-Endpoint mit Fallback</name>
  <read_first>
    - app.py (bestehende Jinja-Filter-Registrierungen — Pattern wiederverwenden)
    - services/eur_calculator.py (compute_eur Output-Struktur)
    - .planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-RESEARCH.md (Code-Beispiel "Jinja de_currency Filter")
    - Briefing Tab 5 für gewünschtes PDF-Layout
  </read_first>
  <files>
    app.py
    templates/admin/eur_pdf.html
    routes/admin_dashboard.py
    tests/test_eur_pdf.py
  </files>
  <action>
**1a) de_currency Filter in `app.py`:**

```python
def de_currency_filter(value):
    """Formatiert Zahl im deutschen Format mit Euro-Symbol: 1234.56 → '1.234,56 €'."""
    if value is None:
        return '—'
    try:
        n = float(value)
    except (TypeError, ValueError):
        return str(value)
    formatted = f"{n:,.2f}"  # '1,234.56' (US)
    # Swap . and , via sentinel
    return formatted.replace(',', '§').replace('.', ',').replace('§', '.') + ' €'

app.jinja_env.filters['de_currency'] = de_currency_filter
```

**1b) `templates/admin/eur_pdf.html` (WeasyPrint + HTML-Preview-Template):**

```html
<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="utf-8">
<title>EÜR — André Preuß — {{ data.period.start }} bis {{ data.period.end }}</title>
<style>
  @page { size: A4; margin: 20mm; }
  body { font-family: sans-serif; font-size: 10pt; color: #000; }
  h1 { font-size: 16pt; margin: 0 0 4pt; }
  h2 { font-size: 12pt; margin: 12pt 0 6pt; border-bottom: 1pt solid #000; padding-bottom: 2pt; }
  table { width: 100%; border-collapse: collapse; margin-bottom: 8pt; }
  td, th { padding: 3pt 4pt; text-align: left; vertical-align: top; }
  td.num, th.num { text-align: right; font-variant-numeric: tabular-nums; }
  .sum { border-top: 0.5pt solid #000; font-weight: bold; }
  .grand { border-top: 1.5pt double #000; font-weight: bold; font-size: 11pt; }
  .header { margin-bottom: 12pt; }
  .line-col { width: 60pt; color: #666; }
  .footer-note { margin-top: 16pt; font-size: 8pt; color: #666; }
</style>
</head>
<body>
  <div class="header">
    <h1>EINNAHME-ÜBERSCHUSS-RECHNUNG</h1>
    <div>André Preuß — Einzelunternehmer — NERVE (SaaS-Software)</div>
    <div>Zeitraum: {{ data.period.start }} bis {{ data.period.end }}</div>
  </div>

  <h2>A. Betriebseinnahmen</h2>
  <table>
    <tr><td>Umsatzerlöse 19% USt (DE-Kunden)</td><td class="num">{{ data.einnahmen.Z14_de_19.netto|de_currency }}</td><td class="line-col">Z.14</td></tr>
    <tr><td>Umsatzerlöse 0% Reverse Charge (EU B2B)</td><td class="num">{{ data.einnahmen.Z16_eu_rc.netto|de_currency }}</td><td class="line-col">Z.16</td></tr>
    <tr><td>Umsatzerlöse 0% Drittland</td><td class="num">{{ data.einnahmen.Z17_drittland.netto|de_currency }}</td><td class="line-col">Z.17</td></tr>
    <tr><td>Vereinnahmte USt (19%)</td><td class="num">{{ data.einnahmen.Z19_vereinnahmte_ust|de_currency }}</td><td class="line-col">Z.19</td></tr>
    <tr class="sum"><td>SUMME EINNAHMEN</td><td class="num">{{ data.einnahmen.summe_einnahmen_netto|de_currency }}</td><td></td></tr>
  </table>

  <h2>B. Betriebsausgaben</h2>
  <table>
    <tr><td>Bezogene Fremdleistungen</td><td class="num">{{ data.ausgaben.Z26_fremdleistungen.total_netto|de_currency }}</td><td class="line-col">Z.26</td></tr>
    {% for item in data.ausgaben.Z26_fremdleistungen.items %}
      <tr><td style="padding-left:16pt;">— {{ item.provider }}</td><td class="num">{{ item.netto|de_currency }}</td><td></td></tr>
    {% endfor %}
    <tr><td>Miete/Leasing EDV</td><td class="num">{{ data.ausgaben.Z52_miete_edv.total_netto|de_currency }}</td><td class="line-col">Z.52</td></tr>
    <tr><td>Übrige Betriebsausgaben</td><td class="num">{{ data.ausgaben.Z57_uebrige.total_netto|de_currency }}</td><td class="line-col">Z.57</td></tr>
    <tr><td>Häusliches Arbeitszimmer</td><td class="num">{{ data.ausgaben.Z65_homeoffice.total_netto|de_currency }}</td><td class="line-col">Z.65</td></tr>
    <tr class="sum"><td>SUMME AUSGABEN (netto)</td><td class="num">{{ data.ausgaben.Z75_summe_ausgaben|de_currency }}</td><td class="line-col">Z.75</td></tr>
    <tr><td>Vorsteuer Inland</td><td class="num">{{ data.ausgaben.Z72_vorsteuer.inland_vst|de_currency }}</td><td class="line-col">Z.72</td></tr>
    <tr><td>Vorsteuer §13b Reverse Charge</td><td class="num">{{ data.ausgaben.Z72_vorsteuer.rc_13b_vst|de_currency }}</td><td class="line-col">Z.72</td></tr>
    <tr class="sum"><td>VORSTEUER GESAMT</td><td class="num">{{ data.ausgaben.Z72_vorsteuer.total|de_currency }}</td><td></td></tr>
  </table>

  <h2>C. Ergebnis</h2>
  <table>
    <tr><td>Betriebseinnahmen</td><td class="num">{{ data.einnahmen.summe_einnahmen_netto|de_currency }}</td></tr>
    <tr><td>./. Betriebsausgaben</td><td class="num">{{ data.ausgaben.Z75_summe_ausgaben|de_currency }}</td></tr>
    <tr class="grand"><td>GEWINN VOR STEUERN</td><td class="num">{{ data.ergebnis.Z77_gewinn_vor_steuern|de_currency }}</td><td class="line-col">Z.77</td></tr>
    <tr><td>./. USt-Zahllast</td><td class="num">{{ data.ergebnis.ust_zahllast|de_currency }}</td></tr>
    <tr class="grand"><td>GEWINN NACH USt</td><td class="num">{{ data.ergebnis.gewinn_nach_ust|de_currency }}</td></tr>
  </table>

  <h2>USt-Voranmeldung ({{ data.period.start }} bis {{ data.period.end }})</h2>
  <table>
    <tr><td>KZ 81 — Steuerpfl. Umsätze 19% (Basis)</td><td class="num">{{ data.ust_voranmeldung.KZ81_steuerpfl_19.basis|de_currency }}</td></tr>
    <tr><td>KZ 81 — davon USt</td><td class="num">{{ data.ust_voranmeldung.KZ81_steuerpfl_19.ust|de_currency }}</td></tr>
    <tr><td>KZ 21 — Steuerfreie igL (EU B2B RC)</td><td class="num">{{ data.ust_voranmeldung.KZ21_igL|de_currency }}</td></tr>
    <tr><td>KZ 45 — Nicht steuerbare Drittland</td><td class="num">{{ data.ust_voranmeldung.KZ45_drittland|de_currency }}</td></tr>
    <tr><td>KZ 84 — §13b RC Bemessungsgrundlage</td><td class="num">{{ data.ust_voranmeldung.KZ84_rc_bemessung|de_currency }}</td></tr>
    <tr><td>KZ 85 — §13b RC USt</td><td class="num">{{ data.ust_voranmeldung.KZ85_rc_ust|de_currency }}</td></tr>
    <tr><td>KZ 66 — Vorsteuer Inland</td><td class="num">{{ data.ust_voranmeldung.KZ66_vst_inland|de_currency }}</td></tr>
    <tr><td>KZ 67 — Vorsteuer §13b RC</td><td class="num">{{ data.ust_voranmeldung.KZ67_vst_rc|de_currency }}</td></tr>
    <tr class="sum"><td>USt-Zahllast</td><td class="num">{{ data.ust_voranmeldung.zahllast|de_currency }}</td></tr>
  </table>

  <div class="footer-note">
    Diese EÜR ist maschinell generiert aus dem NERVE Founder Cost Dashboard.
    Einkommensteuer und Gewerbesteuer werden durch count.tax auf Basis dieser Daten berechnet.
    §13b Reverse-Charge-Behandlung der US-API-Anbieter (Anthropic, Deepgram, ElevenLabs)
    basiert auf Briefing-Interpretation und wurde vor erstem produktiven USt-VA-Export
    durch count.tax validiert (HT-04).
  </div>
</body>
</html>
```

**1c) Export-Endpoints in `routes/admin_dashboard.py`:**

```python
# ── Tab Export: PDF + CSV Downloads ─────────────────────────────────

@admin_dashboard_bp.route('/export/eur.html')
@login_required
@superadmin_required
def export_eur_html_preview():
    """HTML-Preview des EÜR-Reports. Funktioniert IMMER (auch ohne WeasyPrint).
    Dev-Fallback wenn WeasyPrint nicht installiert ist."""
    period = request.args.get('period')
    start, end = _parse_period(period)
    home_days = int(request.args.get('home_days', 0))
    from services.eur_calculator import compute_eur
    db = get_session()
    try:
        data = compute_eur(start, end, db, home_days=home_days)
    finally:
        db.close()
    return render_template('admin/eur_pdf.html', data=data)


@admin_dashboard_bp.route('/export/eur.pdf')
@login_required
@superadmin_required
def export_eur_pdf():
    """EÜR als PDF via WeasyPrint. 500 wenn WeasyPrint nicht installiert (Dev-Maschine)."""
    try:
        from weasyprint import HTML
    except ImportError:
        return ("WeasyPrint nicht installiert. Verwende /export/eur.html für Preview.", 501)
    period = request.args.get('period')
    start, end = _parse_period(period)
    home_days = int(request.args.get('home_days', 0))
    from services.eur_calculator import compute_eur
    db = get_session()
    try:
        data = compute_eur(start, end, db, home_days=home_days)
    finally:
        db.close()
    html = render_template('admin/eur_pdf.html', data=data)
    pdf_bytes = HTML(string=html).write_pdf()
    from flask import Response
    return Response(pdf_bytes, mimetype='application/pdf', headers={
        'Content-Disposition': f'attachment; filename=eur_{period or "current"}.pdf'
    })
```

**1d) `tests/test_eur_pdf.py` (Skeleton ersetzen):**

```python
"""Phase 04.7.2 — EÜR PDF Smoke (optional, importorskip)."""
import pytest
weasyprint = pytest.importorskip("weasyprint")

def test_weasyprint_can_render_minimal():
    """Smoke: WeasyPrint kann eine minimale HTML → PDF rendern."""
    html = "<html><body><h1>Test</h1></body></html>"
    pdf = weasyprint.HTML(string=html).write_pdf()
    assert pdf is not None
    assert len(pdf) > 100  # valides PDF >100 Bytes


def test_eur_pdf_template_smoke(db_session, app):
    """Template rendert mit minimalen Daten."""
    from services.eur_calculator import compute_eur
    from datetime import date
    data = compute_eur(date(2026, 3, 1), date(2026, 4, 1), db_session, home_days=0)
    with app.app_context():
        from flask import render_template
        html = render_template('admin/eur_pdf.html', data=data)
        assert 'EINNAHME-ÜBERSCHUSS-RECHNUNG' in html or 'UeBERSCHUSS' in html
        assert 'KZ 84' in html
        assert 'KZ 85' in html
        assert 'KZ 67' in html
```
  </action>
  <verify>
    <automated>pytest tests/test_eur_pdf.py -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "de_currency" app.py` >= 2 (def + register)
    - `templates/admin/eur_pdf.html` existiert mit `{{ data.einnahmen.Z14_de_19.netto|de_currency }}`
    - `grep -c "KZ 84" templates/admin/eur_pdf.html` >= 1
    - `grep -c "def export_eur_pdf" routes/admin_dashboard.py` == 1
    - `grep -c "def export_eur_html_preview" routes/admin_dashboard.py` == 1
    - `grep -c "from weasyprint import HTML" routes/admin_dashboard.py` >= 1
    - Wenn weasyprint installiert: `pytest tests/test_eur_pdf.py -q` grün
    - Wenn nicht installiert: Tests skippen via importorskip (keine Errors)
    - /admin/dashboard/export/eur.html smoke → 200 mit HTML
  </acceptance_criteria>
  <done>EÜR-PDF-Endpoint + HTML-Preview-Fallback + Jinja-Filter live.</done>
</task>

<task type="auto">
  <name>Task 2: 4 CSV-Export-Endpoints (Einnahmen, Ausgaben, USt-VA, DATEV-Stub)</name>
  <read_first>
    - routes/admin_dashboard.py (nach Task 1)
    - database/models.py (RevenueLog, ApiCostLog, FixedCost)
    - Briefing Tab 6 für gewünschte CSV-Struktur
    - CONTEXT.md D-07 für DATEV-Stub-Umfang
  </read_first>
  <files>routes/admin_dashboard.py</files>
  <action>
```python
# ── CSV Exports ──────────────────────────────────────────────────────

def _csv_response(rows: list, headers: list, filename: str):
    """Hilfs-Helper für CSV-Download mit UTF-8-BOM (Excel-kompatibel)."""
    import csv
    from io import StringIO
    from flask import Response
    buf = StringIO()
    writer = csv.writer(buf, delimiter=';', quotechar='"', quoting=csv.QUOTE_MINIMAL)
    writer.writerow(headers)
    for row in rows:
        writer.writerow(row)
    data = '\ufeff' + buf.getvalue()  # BOM for Excel
    return Response(data, mimetype='text/csv; charset=utf-8', headers={
        'Content-Disposition': f'attachment; filename={filename}'
    })


@admin_dashboard_bp.route('/export/einnahmen.csv')
@login_required
@superadmin_required
def export_einnahmen_csv():
    from database.models import RevenueLog
    period = request.args.get('period')
    start, end = _parse_period(period)
    db = get_session()
    try:
        rows = (db.query(RevenueLog)
                  .filter(RevenueLog.paid_at >= start, RevenueLog.paid_at < end)
                  .order_by(RevenueLog.paid_at).all())
        csv_rows = [[
            r.paid_at.strftime('%Y-%m-%d') if r.paid_at else '',
            r.stripe_invoice_id or '',
            r.country or '',
            r.plan_key or '',
            f"{r.netto_cents/100:.2f}".replace('.', ','),
            f"{r.ust_cents/100:.2f}".replace('.', ','),
            f"{r.brutto_cents/100:.2f}".replace('.', ','),
            r.tax_treatment,
        ] for r in rows]
        return _csv_response(csv_rows,
            ['Datum','Rechnung','Land','Plan','Netto EUR','USt EUR','Brutto EUR','Behandlung'],
            f'einnahmen_{period or "current"}.csv')
    finally:
        db.close()


@admin_dashboard_bp.route('/export/ausgaben.csv')
@login_required
@superadmin_required
def export_ausgaben_csv():
    from database.models import ApiCostLog, FixedCost
    period = request.args.get('period')
    start, end = _parse_period(period)
    home_days = int(request.args.get('home_days', 0))
    db = get_session()
    try:
        csv_rows = []
        # API cost log (per day aggregiert pro provider)
        api_rows = (db.query(ApiCostLog)
                      .filter(ApiCostLog.created_at >= start, ApiCostLog.created_at < end)
                      .order_by(ApiCostLog.created_at).all())
        for a in api_rows:
            category = EUR_CATEGORY_BY_PROVIDER.get(a.provider, {})
            csv_rows.append([
                a.created_at.strftime('%Y-%m-%d') if a.created_at else '',
                a.provider,
                category.get('label', a.provider),
                f"{float(a.cost_eur):.6f}".replace('.', ','),
                str(category.get('eur_line', '')),
                category.get('skr03', ''),
                a.context_tag or '',
            ])
        # FixedCost rows (als aggregiertes Period-Row)
        for fc in db.query(FixedCost).filter(FixedCost.active.is_(True)).all():
            amt = float(fc.amount_eur)
            if fc.cycle == 'monthly':  period_cost = amt
            elif fc.cycle == 'yearly': period_cost = amt / 12.0
            elif fc.cycle == 'per_day': period_cost = amt * home_days
            else: period_cost = 0
            if period_cost > 0:
                csv_rows.append([
                    start.strftime('%Y-%m-%d'),
                    'fixed_cost',
                    fc.name,
                    f"{period_cost:.2f}".replace('.', ','),
                    str(fc.eur_line or ''),
                    fc.skr03 or '',
                    fc.cycle,
                ])
        return _csv_response(csv_rows,
            ['Datum','Quelle','Bezeichnung','Netto EUR','EÜR-Zeile','SKR03','Kontext'],
            f'ausgaben_{period or "current"}.csv')
    finally:
        db.close()


@admin_dashboard_bp.route('/export/ustva.csv')
@login_required
@superadmin_required
def export_ustva_csv():
    from services.eur_calculator import compute_eur
    period = request.args.get('period')
    start, end = _parse_period(period)
    home_days = int(request.args.get('home_days', 0))
    db = get_session()
    try:
        data = compute_eur(start, end, db, home_days=home_days)
    finally:
        db.close()
    u = data['ust_voranmeldung']
    rows = [
        ['KZ 81', 'Steuerpflichtige Umsätze 19% (Basis)', f"{u['KZ81_steuerpfl_19']['basis']:.2f}".replace('.', ',')],
        ['KZ 81', 'Darauf entfallende USt',              f"{u['KZ81_steuerpfl_19']['ust']:.2f}".replace('.', ',')],
        ['KZ 21', 'Steuerfreie igL (EU B2B RC)',         f"{u['KZ21_igL']:.2f}".replace('.', ',')],
        ['KZ 45', 'Nicht steuerbare Drittland',          f"{u['KZ45_drittland']:.2f}".replace('.', ',')],
        ['KZ 84', 'Bemessungsgrundlage §13b RC',         f"{u['KZ84_rc_bemessung']:.2f}".replace('.', ',')],
        ['KZ 85', 'USt §13b RC',                         f"{u['KZ85_rc_ust']:.2f}".replace('.', ',')],
        ['KZ 66', 'Vorsteuer Inland',                    f"{u['KZ66_vst_inland']:.2f}".replace('.', ',')],
        ['KZ 67', 'Vorsteuer §13b RC',                   f"{u['KZ67_vst_rc']:.2f}".replace('.', ',')],
        ['—',     'USt-Zahllast',                         f"{u['zahllast']:.2f}".replace('.', ',')],
    ]
    return _csv_response(rows, ['KZ','Bezeichnung','Betrag EUR'],
                         f'ustva_{period or "current"}.csv')


@admin_dashboard_bp.route('/export/datev_stub.csv')
@login_required
@superadmin_required
def export_datev_stub():
    """D-07: STUB. NICHT DATEV-format-konform. Volle Implementierung wartet auf count.tax."""
    from database.models import RevenueLog, ApiCostLog, FixedCost
    period = request.args.get('period')
    start, end = _parse_period(period)
    db = get_session()
    try:
        csv_rows = []
        # Einnahmen → Konto 8400 (Erlöse 19%) / Gegenkonto 1200 (Bank)
        for r in db.query(RevenueLog).filter(
                RevenueLog.paid_at >= start, RevenueLog.paid_at < end).all():
            betrag = f"{r.brutto_cents/100:.2f}".replace('.', ',')
            konto = '8400' if r.tax_treatment == 'DE_19' else '8338'  # Platzhalter
            csv_rows.append([
                r.paid_at.strftime('%d.%m.%Y') if r.paid_at else '',
                konto, '1200', betrag,
                f"NERVE Abo {r.plan_key or ''} {r.country or ''} #{r.stripe_invoice_id}"[:200],
            ])
        # API-Kosten → Konto 3100 Fremdleistungen / Gegenkonto 1200
        for a in db.query(ApiCostLog).filter(
                ApiCostLog.created_at >= start, ApiCostLog.created_at < end).all():
            category = EUR_CATEGORY_BY_PROVIDER.get(a.provider, {})
            konto_haben = category.get('skr03', '3100')
            betrag = f"{float(a.cost_eur):.2f}".replace('.', ',')
            csv_rows.append([
                a.created_at.strftime('%d.%m.%Y') if a.created_at else '',
                konto_haben, '1200', betrag,
                f"{a.provider} {a.model} {a.context_tag or ''}"[:200],
            ])
        return _csv_response(csv_rows,
            ['Datum','Konto','Gegenkonto','Betrag','Buchungstext'],
            f'datev_stub_{period or "current"}.csv')
    finally:
        db.close()
```
  </action>
  <verify>
    <automated>python -c "import routes.admin_dashboard as m; [getattr(m, f) for f in ['export_einnahmen_csv','export_ausgaben_csv','export_ustva_csv','export_datev_stub','export_eur_pdf','export_eur_html_preview']]; print('all export endpoints present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def export_einnahmen_csv" routes/admin_dashboard.py` == 1
    - `grep -c "def export_ausgaben_csv" routes/admin_dashboard.py` == 1
    - `grep -c "def export_ustva_csv" routes/admin_dashboard.py` == 1
    - `grep -c "def export_datev_stub" routes/admin_dashboard.py` == 1
    - `grep -c "_csv_response" routes/admin_dashboard.py` >= 5
    - `grep -c "ufeff" routes/admin_dashboard.py` >= 1 (BOM für Excel)
    - `pytest tests/test_admin_dashboard_auth.py tests/test_eur_calculator.py -x -q` grün
  </acceptance_criteria>
  <done>4 CSV-Endpoints live, DATEV-Stub gekennzeichnet als nicht-konform.</done>
</task>

<task type="auto">
  <name>Task 3: templates/admin/_tab_export.html mit 5 Download-Buttons + Phase-Abschluss-Check</name>
  <read_first>
    - templates/admin/_tab_einnahmen.html (JS-Pattern)
    - routes/admin_dashboard.py (Endpoints aus Task 1+2)
    - Briefing Tab 6 Layout
  </read_first>
  <files>templates/admin/_tab_export.html</files>
  <action>
```html
<div class="fcd-export">
  <h3>DATEN-EXPORT FÜR STEUERBERATER — <span class="fcd-period-label"></span></h3>
  <p>Zeitraum aus Header-Dropdown. Homeoffice-Tage: <input type="number" id="fcd-export-home" value="0" min="0" max="366"></p>

  <h4>Berichte</h4>
  <ul class="fcd-export-list">
    <li>
      <strong>EÜR als PDF</strong> — DIN A4, deutsches Zahlenformat, Anlage-EÜR-Format
      <a id="fcd-dl-pdf" href="#" class="n-btn">Herunterladen (PDF)</a>
      <a id="fcd-dl-html" href="#" class="n-btn-ghost">HTML-Preview (Dev-Fallback)</a>
    </li>
    <li>
      <strong>Einnahmen-Detail (CSV)</strong> — alle Stripe-Transaktionen mit Netto/USt/Brutto/Land/Plan/Treatment
      <a id="fcd-dl-einn" href="#" class="n-btn">Herunterladen (CSV)</a>
    </li>
    <li>
      <strong>Ausgaben-Detail (CSV)</strong> — API-Kosten + Fixkosten mit SKR03/EÜR-Zeile
      <a id="fcd-dl-ausg" href="#" class="n-btn">Herunterladen (CSV)</a>
    </li>
    <li>
      <strong>USt-Voranmeldung Daten (CSV)</strong> — KZ 81/21/45/84/85/66/67 + Zahllast
      <a id="fcd-dl-ustva" href="#" class="n-btn">Herunterladen (CSV)</a>
    </li>
    <li>
      <strong>DATEV-Buchungsstapel (CSV-Stub)</strong> — <em>NICHT DATEV-format-konform.</em>
      Einfache Spalten Datum/Konto/Gegenkonto/Betrag/Text. Voll-Implementierung wartet auf count.tax-Format-Anfrage.
      <a id="fcd-dl-datev" href="#" class="n-btn">Herunterladen (CSV)</a>
    </li>
  </ul>

  <h4>Automatisierung</h4>
  <p class="fcd-stub">Monatliche automatische Datenlieferung an count.tax: <strong>Deferred auf Milestone 3</strong> (Phase 04.7.3 oder später per Briefing).</p>
</div>
<script>
(function(){
  function updateLinks() {
    const period = document.querySelector('.fcd-wrap').dataset.period;
    const homeDays = parseInt(document.getElementById('fcd-export-home').value || 0);
    const q = `period=${encodeURIComponent(period)}&home_days=${homeDays}`;
    document.getElementById('fcd-dl-pdf').href  = '/admin/dashboard/export/eur.pdf?' + q;
    document.getElementById('fcd-dl-html').href = '/admin/dashboard/export/eur.html?' + q;
    document.getElementById('fcd-dl-einn').href = '/admin/dashboard/export/einnahmen.csv?' + q;
    document.getElementById('fcd-dl-ausg').href = '/admin/dashboard/export/ausgaben.csv?' + q;
    document.getElementById('fcd-dl-ustva').href= '/admin/dashboard/export/ustva.csv?' + q;
    document.getElementById('fcd-dl-datev').href= '/admin/dashboard/export/datev_stub.csv?' + q;
    document.querySelectorAll('.fcd-period-label').forEach(e => e.textContent = period);
  }
  document.getElementById('fcd-export-home').addEventListener('change', updateLinks);
  const panel = document.getElementById('panel-export');
  const obs = new MutationObserver(()=>{
    if (panel && !panel.hidden && !panel.dataset.loaded) { panel.dataset.loaded='1'; updateLinks(); }
  });
  obs.observe(panel, {attributes:true});
  if (panel && !panel.hidden) { panel.dataset.loaded='1'; updateLinks(); }
})();
</script>
```

**Phase-Abschluss-Verifikation (manuell/smoke nach Task 3):**

1. `pytest -x -q` Gesamt → grün
2. App starten, als Superadmin einloggen, `/admin/dashboard/` öffnen
3. Alle 6 Tabs durchklicken — jeder lädt Daten ohne Error
4. Einen API-Call auslösen (z.B. Live-Session oder Training) → api_cost_log hat Row (SQL-Check)
5. Export-Tab → HTML-Preview des EÜR → sichtbar
6. Export-Tab → alle 4 CSVs downloaden, Excel-kompatibel öffnen
7. Falls WeasyPrint lokal installiert: PDF-Download testen
  </action>
  <verify>
    <automated>grep -c "fcd-dl-pdf" templates/admin/_tab_export.html && grep -c "fcd-dl-einn" templates/admin/_tab_export.html && grep -c "fcd-dl-ausg" templates/admin/_tab_export.html && grep -c "fcd-dl-ustva" templates/admin/_tab_export.html && grep -c "fcd-dl-datev" templates/admin/_tab_export.html && pytest tests/test_admin_dashboard_auth.py tests/test_eur_calculator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Template existiert mit 6 Download-Links (pdf, html-preview, einn, ausg, ustva, datev)
    - `grep -c "updateLinks" templates/admin/_tab_export.html` >= 2
    - `grep -c "home_days" templates/admin/_tab_export.html` >= 1
    - `pytest -x -q` Gesamt bleibt grün
    - Smoke: `/admin/dashboard/export/einnahmen.csv?period=2026-03` → 200 text/csv
    - Smoke: `/admin/dashboard/export/eur.html?period=2026-03` → 200 text/html
  </acceptance_criteria>
  <done>Export-Tab komplett, Phase 04.7.2 fertig.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Export-Downloads enthalten sensitive Finanzdaten | Gated @superadmin_required. CSV enthält User-Emails/Country-Infos. |
| WeasyPrint Template-Rendering | Jinja auto-escape on (default). Keine User-Input-Injection in PDF. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-04.7.2-07-01 | I (Info Disclosure) | CSV-Downloads mit Finanzdaten | mitigate | @superadmin_required auf allen Endpoints |
| T-04.7.2-07-02 | T (XSS in PDF) | Jinja-Rendering von compute_eur Output | mitigate | Jinja auto-escape default on; Werte aus controlled DB-Quellen (Numerik) |
| T-04.7.2-07-03 | T (CSV-Injection) | Buchungstext in DATEV-CSV | mitigate | Strings via csv.writer mit QUOTE_MINIMAL; keine =/+/- Prefix-Escape (Empfehlung: falls später Excel öffnet, Feld-Prefix prüfen) |
| T-04.7.2-07-04 | D (DoS) | WeasyPrint Render-Time | accept | Einzelner User, Monats-Export, <5s erwartet |
| T-04.7.2-07-05 | E | Endpoint ohne Auth | mitigate | Jeder Export-Route hat @login_required + @superadmin_required |
| T-04.7.2-07-06 | I | Datei-Inhalte mit PII | accept | Nur André sieht sie, Weitergabe an count.tax ist beabsichtigt |
</threat_model>

<verification>
- `pytest -x -q` Gesamt grün
- Smoke: alle 6 Export-Endpoints geben 200 (ausser eur.pdf → 501 wenn WeasyPrint fehlt)
- Tab Export UI zeigt 5 Buttons + HTML-Preview-Link
- HT-03 VPS-Install dokumentiert (Pre-Deploy Blocker für PDF)
</verification>

<success_criteria>
- de_currency Filter global registriert
- EÜR-PDF-Template + Endpoint + HTML-Preview-Fallback
- 4 CSV-Exports funktional (Einnahmen, Ausgaben, USt-VA, DATEV-Stub)
- Tab Export UI mit allen Downloads
- Phase-Verification-Smoke-Plan dokumentiert
</success_criteria>

<output>
`.planning/phases/04.7.2-founder-cost-dashboard-inserted/04.7.2-07-SUMMARY.md` inkl. Phase-Closeout-Checklist: welche HT-Tasks müssen für Production erfüllt sein (HT-01..HT-04).
</output>
