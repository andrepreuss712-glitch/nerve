---
phase: 08.23.2.D.UX.4
plan: 03
subsystem: frontend-pip
tags: [pip-launcher, outcome-screen, hold-to-end, ladebalken, score, pipEl, blocker-fix]
requires:
  - "Plan 01: correct_outcome-Response (final_score, outcome, source) unveraendert"
  - "Plan 02: /api/postcall_outcome (Haiku, schnell) + /api/postcall_cards (Sonnet, Hintergrund)"
provides:
  - "PiP-Reihenfolge-Umbau: Hold -> Ladebalken 1 -> Outcome-Screen (3 States) -> Score+Analytics"
  - "BLOCKER-Fix NEW-1/F5: alle Outcome/Score/Analytics-Lookups via pipEl() (PiP-Fenster)"
affects:
  - static/pip-launcher.js
  - static/nerve.css
  - static/outcome-modal-state.js (unveraendert — bereits U-01-konform)
  - tests/test_decide_modal_state.test.js
tech-stack:
  added: []
  patterns:
    - "PiP-Document-Pointer-Delegation fuer rebuild-sichere Hold-Bindung (F10)"
    - "Generation-Guard (endCallGen) + haikuTimedOut-Flag fuer Race-/Late-Haiku-Schutz"
    - "pipEl() statt bare document fuer alle Zwei-Fenster-DOM-Lookups"
key-files:
  created:
    - tests/test_decide_modal_state.test.js (bereits vorhanden — um conf-zero-beats-source-Case erweitert)
  modified:
    - static/pip-launcher.js
    - static/nerve.css
decisions:
  - "Option 3 (Andre 2026-05-31): nach Confirm zeigt PiP NUR Score + Basis-Analytics — keine Sonnet-Karten, kein Ladebalken 2"
  - "S-03-Analytics via _renderQuickStats (PiP-aware, null-safe) wiederverwendet + Kaufbereitschaft-Tile ergaenzt"
  - "Score+Analytics-Erfolgspfad bereits in Task-2-Edit eingebaut (zusammen mit Submit-Lock); Task 3 = _showPostcall L-01 + CSS"
metrics:
  duration: ~40min
  completed: 2026-05-31
  tasks: 3
  files: 4
---

# Phase 08.23.2.D.UX.4 Plan 03: Frontend Reihenfolge-Umbau Summary

PiP-Call-Ende komplett umgebaut: 600ms-Hold-to-end (rebuild-sicher), ehrlicher Ladebalken 1 mit 9s-Timeout, Outcome-Screen 'Call wirklich beenden?' mit 3 U-01-States (teal/rot/neutral), Score+Basis-Analytics sofort nach Confirm — und alle DOM-Lookups via `pipEl()` statt bare `document`, womit der BLOCKER (Score landete unsichtbar im Haupt-Fenster) behoben ist.

## Was gebaut wurde

### Task 1 — Hold-to-end + Reorder + Ladebalken 1 + 9s-Timeout + bg cards + cleanup (commit cbd0d35)
- **B-02/F10:** Single-Click-Beenden entfernt; 600ms Press-and-Hold via `pointerdown/up/leave/cancel`-Delegation auf `pipWindow.document` (`closest('#nlp-btn-beenden')`) — rebuild-sicher, kein one-time Element-Listener. Plus Keyboard-2nd-Press ('Nochmal drücken zum Beenden', 2s-Fenster). `holdFired`-Flag verhindert Doppel-Trigger.
- **LB-01:** Neue `_showLadebalken1()`-View (unbestimmter Spinner + 'KI wertet das Gespräch aus…'), ersetzt das Score-Skeleton. PiP-aware. Postcall-Section bleibt versteckt (F9).
- **LB-02/HIGH-2/F5:** 9s-Timeout vor dem Outcome-Fetch armiert; Stale-Guard (`endCallGen`) + `haikuTimedOut`-Flag; Timeout-Callback setzt `pipEl('pip-outcome-section').dataset.outcomeRendered='1'` (korrekte ID, PiP-aware) als Defence-in-depth gegen Late-Haiku.
- **Plan 02 Endpoints:** `/api/postcall_outcome` (schnell) statt `postcall_analysis`; `/api/postcall_cards` als fire-and-forget Hintergrund-Trigger am Beenden (Response verworfen, NEW-2/Option 3).
- **S-03-Vorbereitung:** `state.pendingPostcall = data.postcall` (nur Erfolgs-Pfad).
- **NEW-5:** `_resetLiveState` nullt jetzt `state.pendingPostcall` + `state.lastCallId`.
- Socket connect-Handler bare-document -> `pipEl` (F5-Konsistenz).

### Task 2 — Outcome-Screen 3 States + Submit-Lock + modal-Test (commit 97e46e8)
- **NEW-1/F5 (BLOCKER):** `_renderOutcomeUx` voll PiP-aware — `sec` via `pipEl`, Section-Erzeugung im PiP-Document (`_doc.createElement`), Host via `pipEl('pip-postcall')`.
- **B-01:** Heading 'Call wirklich beenden?' (alt: 'Gesprächsergebnis bestätigen').
- **HIGH-2:** `sec.dataset.outcomeRendered='1'` branch-unabhaengig (vor der state-Branch-Logik) gesetzt — greift auch im kein_versuch-Branch.
- **U-01:** sicher=teal Vorauswahl/Confirm sofort; unsicher=ROTE Vorauswahl (`presetUnsicher`, `pip-outcome-btn--selected-unsicher`) + Disclaimer 'KI unsicher, bitte prüfen' (alert-triangle SVG) + Confirm disabled bis aktiver Tap; kein_versuch=neutral/disabled bis Tap. Aktiver Tap loescht beide Vorauswahl-Klassen + Disclaimer und schaltet teal.
- **MEDIUM-3:** confirm-handler setzt `confirmBtn.disabled=true` + disabled-Klasse SOFORT vor dem POST; Re-Enable nur im `.catch`/Hard-Error mit Copy 'Konnte nicht speichern – bitte erneut auf Bestätigen tippen.'. `state.lastOutcome` gesetzt.
- **Test:** `test_decide_modal_state.test.js` um `conf-zero-beats-source`-Case erweitert (6 Cases).
- Hinweis: Der Score+Analytics-Erfolgspfad (Task 3 B) wurde in diesem Edit miteingebaut, da er sauberer in einem Schreibvorgang mit dem Submit-Lock-Erfolgspfad lebt.

### Task 3 — Score+Analytics S-03 + L-01 + CSS (commit 4328e97)
- **L-01:** `_showPostcall` rendert keinen `_calcScore`-Score mehr (`_showPostcallRaw('', tags)`); `_calcScore`-Funktion bleibt als Datei-Funktion, fliesst aber nicht mehr in den angezeigten Score. Bare-document-Hide -> `pipEl` (F5).
- **S-01/S-03/F9 (im confirm-handler):** Score aus `json.final_score` (PiP-aware, Band-Farbe via `--session-score-*`-Tokens), Label 'Coaching-Score'; `_fetchAndRenderTrend(json.final_score)`; Basis-Analytics null-safe via `if (_pp) _renderQuickStats(_pp)` (KB/Redeanteil/Einwaende-Tiles, Kaufbereitschaft-Tile ergaenzt), Fallback leert KPI ohne Throw. Postcall-Section-Reveal ist der EINZIGE Reveal-Ort (Score-Hide-Owner).
- **Option 3:** keine Karten, kein Ladebalken 2, kein 'Detail-Auswertung…' im PiP.
- **CSS (nerve.css, Token-only):** `.pip-outcome-btn--selected-unsicher`, `.pip-outcome-disclaimer`, `.pip-beenden-holding`, `.pip-ladebalken1`/`.pip-ladebalken1-text`/`.pip-loading-spinner` (eine Spinner-Klasse, kein -2). Kein roher Hex ausser var()-Fallback (bestehendes Projekt-Pattern).

## Deviations from Plan

**1. [Rule 3 - Blocking/Reality] Test-Datei existierte bereits**
- Plan sagt "tests/test_decide_modal_state.test.js NEU anlegen (Datei existiert NICHT)". Sie existiert seit D.UX.1 Plan 04 mit 5 aequivalenten Cases. Statt Neuanlage den fehlenden `conf-zero-beats-source`-Case (ai_auto + conf 0 -> kein_versuch) ergaenzt. Alle 6 Cases gruen.
- Datei: tests/test_decide_modal_state.test.js — Commit 97e46e8

**2. [Strukturell] Score/Analytics-Erfolgspfad in Task-2-Edit gebaut**
- Der confirm-handler-Erfolgspfad (Task 3 B: Score-Reveal, Trend, null-safe KPI) wurde zusammen mit dem Submit-Lock (Task 2 C) in einem Schreibvorgang umgesetzt, weil beide denselben `.then(json)`-Block teilen. Task 3 enthielt dann nur noch `_showPostcall`/L-01 + CSS. Funktional vollstaendig wie geplant, nur Commit-Grenze verschoben.

## Verifikation (lokal, Code-only)
- `node --check static/pip-launcher.js` -> PARSE OK (alle 3 Commits)
- BLOCKER-Greps: `document.getElementById('pip-outcome-section')` = 0, `document.querySelector('#nlp-section-postcall')` = 0, `pipEl('pip-outcome-section')` = 3
- Copy-Greps (echte Umlaute): 'Halten zum Beenden', 'Nochmal drücken zum Beenden', 'KI wertet das Gespräch aus', 'Call wirklich beenden?', 'KI unsicher, bitte prüfen', 'Konnte nicht speichern', 'Coaching-Score' je vorhanden
- Option 3: 'Detail-Auswertung' = 0, `pip-loading-spinner-2`-Klasse existiert nicht
- Null-Guard: `var _pp = state.pendingPostcall; if (_pp) {...}` vorhanden
- `node --test tests/test_decide_modal_state.test.js` lokal 6/6 gruen (Server-Acceptance laeuft via Orchestrator-Deploy, HART)

## Bekannte Gaps / Hinweise
- Server-seitiger node-Test + Live-UAT (Hold/Timeout/PiP-Render DOM-Verhalten) laufen ueber den Orchestrator-Deploy (manual-direct-prod) mit Andre-Approval — NICHT in diesem Executor (HART: kein Local-Dev, kein Prod-Contact).
- `_showPostcall` hat nach diesem Plan keinen Caller mehr (endCall ruft es nicht; Confirm-Pfad rendert direkt). Bewusst nicht entfernt (Plan: Funktion darf bleiben).
- KNOWN-GAP (Plan): per-Call Sonnet-Lernkarten persistieren im Hintergrund, werden aber nirgends per-Call angezeigt (Phase 4.11 / Backlog 999.2). Kein Regress.

## Self-Check: PASSED
- static/pip-launcher.js: FOUND (3 Commits)
- static/nerve.css: FOUND
- tests/test_decide_modal_state.test.js: FOUND
- Commits cbd0d35, 97e46e8, 4328e97: alle gepusht auf main
