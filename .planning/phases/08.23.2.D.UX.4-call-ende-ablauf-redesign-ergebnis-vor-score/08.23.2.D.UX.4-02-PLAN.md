---
phase: 08.23.2.D.UX.4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/learning.py
  - tests/test_postcall_split.py
autonomous: true
complexity: medium
requirements: [L-04, LB-04, B-01]
must_haves:
  truths:
    - "Haiku-Outcome-Klassifikation ist ueber einen eigenen, schnellen Endpoint (/api/postcall_outcome) abrufbar, der NICHT auf Sonnet wartet"
    - "Sonnet-Lernkarten (generate_postcall_analysis) laufen NUR ueber einen separaten Endpoint (/api/postcall_cards), der erst nach dem Confirm getriggert wird"
    - "Der schnelle Outcome-Endpoint behaelt den conf=0-Degraded-Pfad (outcome null, source null) fuer LB-04/LB-02"
    - "Der outcome_ready Socket-Emit feuert weiterhin auch bei conf=0 (room-targeted, kein Broadcast)"
    - "calls.outcome/outcome_confidence/outcome_source-UPDATE + Schwellenlogik bleiben im Outcome-Endpoint unveraendert"
  artifacts:
    - path: "routes/learning.py"
      provides: "Zwei getrennte Endpoints: /api/postcall_outcome (Haiku, schnell) + /api/postcall_cards (Sonnet, post-confirm)"
      contains: "def api_postcall_outcome"
    - path: "tests/test_postcall_split.py"
      provides: "Runtime-Tests: Outcome-Endpoint liefert outcome ohne Sonnet, conf=0-Pfad, Cards-Endpoint liefert vorschlaege"
      contains: "def test_"
  key_links:
    - from: "routes/learning.py:/api/postcall_outcome"
      to: "services.outcome_service.classify + calls-UPDATE + outcome_ready-emit"
      via: "Haiku-Klassifikation ohne Sonnet"
      pattern: "outcome_service.classify"
    - from: "routes/learning.py:/api/postcall_cards"
      to: "services.coaching_service.generate_postcall_analysis"
      via: "Sonnet nur hier"
      pattern: "generate_postcall_analysis"
---

<objective>
Backend Postcall-Split (L-04 / B-01). Heute ruft `/api/postcall_analysis` (learning.py:18-186) den langsamen
Sonnet (`generate_postcall_analysis`, Z.42) ZUERST und die schnelle Haiku-Outcome-Klassifikation
(`outcome_service.classify`, Z.96) DANACH — der User wartet auf Sonnet, bevor er das Outcome bestaetigen kann.

Dieser Plan teilt den Endpoint in zwei:
- `/api/postcall_outcome` — NUR Haiku-Klassifikation + calls-UPDATE + outcome_ready-Emit. Schnell, treibt
  Ladebalken 1 (Outcome-Screen). KEIN Sonnet-Aufruf.
- `/api/postcall_cards` — NUR Sonnet `generate_postcall_analysis`. Wird vom Frontend erst NACH dem
  Outcome-Confirm getriggert (Ladebalken 2, Detail-Auswertung). Blockiert den Score nicht.

Purpose: Eliminiert die heutige Kopplung "langsamer Sonnet verzoegert den Outcome-Screen". Der
Outcome-Screen erscheint sofort nach Haiku (oder 9s-Timeout, Plan 03), die Lernkarten laufen separat danach.
Output: Zwei Endpoints in learning.py, neue Test-Datei. KEINE Schema-Aenderung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-CONTEXT.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-SCHEMA-EVIDENCE.md

<interfaces>
<!-- Aus learning.py:18-186 extrahiert. Executor nutzt direkt. -->

Heutiger Monolith `/api/postcall_analysis` (learning.py:18) Struktur:
- Body: {conv_id (req), call_id?, einwaende, painpoints, kb_start, kb_end, redeanteil_berater,
  redeanteil_kunde, dauer_sek, skript_abdeckung, ga_details, kaufsignale}
- Ownership-Check: ConversationLog.id==conv_id, user_id==g.user.id -> 404 sonst
- Block A (Sonnet, Z.40-55): `from services.coaching_service import generate_postcall_analysis`
  `suggestions = generate_postcall_analysis(conv_id=..., user_id=..., einwaende=..., painpoints=...,
  kb_start=..., kb_end=..., redeanteil_berater=..., redeanteil_kunde=..., dauer_sek=..., skript_abdeckung=...,
  ga_details=..., kaufsignale=...)`
- Block B (Haiku-Outcome, Z.57-170): liest transcript_segments via
  `from database.models import TranscriptSegment` order_by(ts_ms, id), baut _conv_data mit log_entries
  (aus DB-Segmenten), ruft `outcome_service.classify(_conv_data)`. Schwellenlogik:
  conf==0 -> outcome None/source None; conf<0.90 -> 'ai_auto_unsicher'; conf>=0.90 -> 'ai_auto'.
  calls-UPDATE (separate Session): outcome/outcome_confidence/outcome_source. Guard: nur wenn
  `_outcome_val is not None or _outcome_conf > 0.0`.
- Block C (Emit, Z.145-170): room-targeted `_sio_phase_d.emit('outcome_ready', {outcome, confidence,
  source, call_id}, room=_sid_for_emit)`. Feuert AUCH bei conf=0 (BLOCKER-1 — kein Guard). SID-Lookup
  ueber ls._session_state.
- Response (Z.175-181): {vorschlaege, outcome, confidence, source, call_id}

Modul-Imports oben in learning.py vorhanden: `outcome_service`, `ls` (live_session), `_sio_phase_d`
(SocketIO), `Call`, `TranscriptSegment`, `ConversationLog`, `get_session`. (Pflicht: vor Edit die
tatsaechlichen Top-Level-Imports von learning.py lesen — read_first.)

Blueprint: learning_bp = Blueprint('learning', __name__)  (routes/CLAUDE.md verifiziert)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /api/postcall_outcome — Haiku-only Endpoint extrahieren (kein Sonnet)</name>
  <read_first>
    - routes/learning.py:1-186 (Modul-Imports oben + kompletter `api_postcall_analysis` — Block A/B/C)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md
      "Wichtig fuer den Split" (Empfehlung zwei Endpoints) + "Backend degraded path (LB-04)"
    - routes/CLAUDE.md (Blueprint 'learning', url_for-Regel)
  </read_first>
  <action>
    Neuen Endpoint `@learning_bp.route('/api/postcall_outcome', methods=['POST'])` `def api_postcall_outcome():`
    in routes/learning.py anlegen. Inhalt = Block B + Block C des heutigen `api_postcall_analysis`, OHNE
    Block A (Sonnet). Konkret:

    - Body lesen + conv_id-Pflicht-Check + Ownership-Check (ConversationLog.id==conv_id, user_id==g.user.id,
      404 sonst) — identisch zu heute (Z.22-38).
    - KEIN `generate_postcall_analysis`-Aufruf.
    - Block B unveraendert uebernehmen: transcript_segments laden (order_by ts_ms, id), _conv_data bauen,
      `outcome_service.classify(_conv_data)`, Schwellenlogik (conf==0 -> None/None;
      conf<0.90 -> 'ai_auto_unsicher'; conf>=0.90 -> 'ai_auto'), calls-UPDATE (separate Session) mit dem
      bestehenden Guard `if _outcome_val is not None or _outcome_conf > 0.0`. WICHTIG: die conf=0-Telemetrie
      (`_fail_type` empty_transcript/classify_zero, print) beibehalten.
    - Block C unveraendert uebernehmen: room-targeted `outcome_ready`-Emit, der AUCH bei conf=0 feuert
      (BLOCKER-1 — KEIN Guard, NICHT in den Write-Guard verschieben). SID-Lookup ueber ls._session_state.
    - Response: `{'outcome': _outcome_val, 'confidence': round(_outcome_conf,3) if _outcome_conf else 0.0,
      'source': _outcome_source_val, 'call_id': str(_posted_call_id) if _posted_call_id else None}`
      (KEIN 'vorschlaege'-Key — der gehoert jetzt zu /api/postcall_cards).

    LANDMINE (RESEARCH / Punkt 21): NICHT auf ein `conv.log_entries`-getattr zurueckfallen — log_entries
    kommt aus der DB-Tabelle transcript_segments (Bug-A-Wurzel). Bestehenden Segment-Lesepfad 1:1 uebernehmen.
    Punkt 14 Control-Flow-Audit: neuer Endpoint, kein Insert in bestehende Funktion — aber Block B/C werden
    aus dem Monolithen herausgeloest; sicherstellen dass alle in Block B/C genutzten Modul-Imports
    (`outcome_service`, `ls`, `_sio_phase_d`, `Call`, `TranscriptSegment`, `ConversationLog`) auf Modul-Ebene
    verfuegbar sind (sie sind es heute).
    UTF-8: Code-Identifier ASCII; print-/Log-Strings duerfen Umlaute haben.
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-02): add /api/postcall_outcome (haiku-only)" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull && source ../venv/bin/activate && pytest tests/test_postcall_split.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - grep `def api_postcall_outcome` routes/learning.py -> 1 Treffer
    - grep `'/api/postcall_outcome'` routes/learning.py -> 1 Treffer
    - Im api_postcall_outcome-Body: grep `generate_postcall_analysis` -> 0 Treffer (kein Sonnet)
    - grep `outcome_service.classify` im neuen Endpoint -> 1 Treffer
    - grep `emit('outcome_ready'` im neuen Endpoint -> 1 Treffer (Emit feuert auch bei conf=0)
    - test_postcall_split.py::test_outcome_endpoint_returns_outcome_without_sonnet GRUEN (mockt classify,
      assertiert outcome/source in Response, generate_postcall_analysis NICHT aufgerufen)
    - test_postcall_split.py::test_outcome_endpoint_conf_zero_path GRUEN (classify -> conf 0 -> outcome None)
  </acceptance_criteria>
  <done>Schneller Haiku-only Outcome-Endpoint existiert, kein Sonnet, conf=0-Pfad + Emit erhalten. Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: /api/postcall_cards — Sonnet-only Endpoint + alten Monolith retten</name>
  <read_first>
    - routes/learning.py (Block A Sonnet-Aufruf Z.40-55 + Response-Struktur)
    - routes/learning.py (neuer api_postcall_outcome aus Task 1)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md "Ladebalken-2"
  </read_first>
  <action>
    Neuen Endpoint `@learning_bp.route('/api/postcall_cards', methods=['POST'])` `def api_postcall_cards():`
    in routes/learning.py anlegen. Inhalt = NUR Block A (Sonnet) des heutigen Monolithen:

    - Body lesen + conv_id-Pflicht-Check + Ownership-Check (identisch).
    - `from services.coaching_service import generate_postcall_analysis`
    - `suggestions = generate_postcall_analysis(conv_id=conv_id, user_id=g.user.id, einwaende=data.get('einwaende',[]),
      painpoints=data.get('painpoints',[]), kb_start=data.get('kb_start',30), kb_end=data.get('kb_end',30),
      redeanteil_berater=data.get('redeanteil_berater',50), redeanteil_kunde=data.get('redeanteil_kunde',50),
      dauer_sek=data.get('dauer_sek',0), skript_abdeckung=data.get('skript_abdeckung',0),
      ga_details=data.get('ga_details',[]), kaufsignale=data.get('kaufsignale',[]))`
    - Response: `{'vorschlaege': suggestions}`
    - try/except mit traceback wie heute (Z.182-186) -> 500 bei Fehler.

    Den alten `/api/postcall_analysis`-Endpoint (Monolith) BEHALTEN als Backward-Compat-Shim, damit
    kein anderer Caller bricht (Pflicht-grep zeigt: nur das Frontend ruft ihn, das in Plan 03 auf die
    neuen Endpoints umgestellt wird; ein Reconnect/alter Tab koennte ihn aber noch treffen). Den Monolith
    NICHT loeschen — unveraendert lassen.

    SHIM-DEBT-NOTIZ (INFO 2, bewusste Entscheidung): Da Plan 03 (gleiche Phase, Wave 2) das Frontend
    vollstaendig auf /api/postcall_outcome + /api/postcall_cards umstellt, ist der alte
    /api/postcall_analysis-Monolith nach Deploy von Plan 03 auf Production faktisch SOFORT Dead-Code
    (kein aktiver Caller mehr ausser theoretischem Reconnect/alten Tab). Wir behalten ihn diese Phase
    bewusst als Sicherheitsnetz (vermeidet Wave-1/Wave-2-Kopplung), aber er ist ein bekannter Cleanup-
    Kandidat. FOLLOW-UP (nicht in dieser Phase): nach 1-2 Wochen Production-Stabilitaet einen Mini-
    Cleanup-Task anlegen, der /api/postcall_analysis entfernt — vorher per Live-Logs (`inspect.sh logs`)
    bestaetigen, dass die Route 0 Hits hat. So geht der Debt nicht vergessen.

    Punkt 14: neuer Endpoint, kein Insert. UTF-8: 'vorschlaege' ist ein JSON-Key -> ASCII (korrekt).
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-02): add /api/postcall_cards (sonnet-only)" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull && source ../venv/bin/activate && pytest tests/test_postcall_split.py -x && bash scripts/inspect.sh routes | grep postcall'</automated>
  </verify>
  <acceptance_criteria>
    - grep `def api_postcall_cards` routes/learning.py -> 1 Treffer
    - grep `'/api/postcall_cards'` routes/learning.py -> 1 Treffer
    - grep `generate_postcall_analysis` im api_postcall_cards-Body -> 1 Treffer
    - grep `outcome_service.classify` im api_postcall_cards-Body -> 0 Treffer (kein Haiku hier)
    - grep `def api_postcall_analysis` routes/learning.py -> 1 Treffer (Monolith bleibt als Shim)
    - inspect.sh routes auf Production listet /api/postcall_outcome UND /api/postcall_cards
    - test_postcall_split.py::test_cards_endpoint_returns_vorschlaege GRUEN (mockt generate_postcall_analysis)
  </acceptance_criteria>
  <done>Sonnet-only Cards-Endpoint existiert, Monolith als Shim erhalten (mit dokumentierter Cleanup-Debt-Notiz INFO 2), beide Routes live auf Production. Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/postcall_outcome | conv_id + call_id (untrusted) — Ownership-Check ConversationLog.user_id==g.user.id, Call.user_id==g.user.id |
| client -> /api/postcall_cards | conv_id (untrusted) — Ownership-Check identisch |
| /api/postcall_outcome -> outcome_ready emit | room-targeted via SID-Lookup, KEIN Broadcast (Multi-User-Privacy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UX4-04 | Information Disclosure | outcome_ready-Emit | mitigate | Room-targeted Emit (SID-Lookup ueber ls._session_state), KEIN Broadcast — bestehendes BLOCKER-1-Pattern unveraendert uebernehmen |
| T-UX4-05 | Spoofing | conv_id/call_id eines anderen Users | mitigate | Ownership-Filter user_id==g.user.id in beiden Endpoints (1:1 aus Monolith) |
| T-UX4-06 | Information Disclosure | TranscriptSegment-Wortlaut | accept | Nur eigene Segmente (conversation_log_id-gebunden, user-owned conv); kein Cross-User-Read |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `calls` (schreiben: outcome, outcome_confidence, outcome_source — nur in /api/postcall_outcome)
- `conversation_logs` (lesen: Ownership-Check + erreichte_phase)
- `transcript_segments` (lesen: ts_ms, speaker, text — fuer Haiku-Klassifikation)

### inspect.sh-Beleg (verbatim, Production 178.104.82.166, 2026-05-31)

```
Check constraints (calls):
    "ck_calls_outcome" CHECK ((outcome = ANY (ARRAY['meeting_booked','callback','send_info','wrong_person','gatekeeper_blocked','no_interest','contract_signed','unknown'])) OR outcome IS NULL)
    "ck_calls_outcome_source" CHECK ((outcome_source = ANY (ARRAY['ai_auto','ai_auto_unsicher','user_corrected'])) OR outcome_source IS NULL)
```
calls.outcome (text), calls.outcome_confidence (double precision), calls.outcome_source (text) — alle
in SCHEMA-EVIDENCE.md verbatim bestaetigt. transcript_segments existiert seit Migration 0010 (D.UX.1).

### Cross-Layer-Konsistenz-Tabelle

| Code-Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `_conv_data['log_entries']` | gelesen in classify() | DB-Tabelle `transcript_segments` (ts_ms/speaker/text) — NICHT `conversation_logs.log_entries` | ✓ Bug-A-Wurzel vermieden (D.UX.1-Fix uebernommen) |
| `_call_row.outcome` | geschrieben in /api/postcall_outcome | DB-Spalte `calls.outcome` text, CHECK 7+unknown/NULL | ✓ inspect.sh bestaetigt |
| `_call_row.outcome_source` | geschrieben (ai_auto/ai_auto_unsicher) | DB-Spalte `calls.outcome_source`, CHECK -> kein Constraint-Change | ✓ inspect.sh bestaetigt |
| `_call_row.outcome_confidence` | geschrieben | DB-Spalte `calls.outcome_confidence` double | ✓ inspect.sh bestaetigt |

### Cross-Variable-Naming-Check
KRITISCH: `conv.log_entries` ist KEINE DB-Spalte (RAM/TXT — D.UX-Falle). Der Outcome-Endpoint liest
Transkript-Wortlaut aus der DB-Tabelle `transcript_segments` (bestehender D.UX.1-Lesepfad). Beim
Herausloesen von Block B NICHT versehentlich auf ein `getattr(conv, 'log_entries')` zurueckfallen.

### Bei Diskrepanz: STOP + Replan
Keine Diskrepanz — keine neue Spalte, keine Migration. Split ist rein Route-intern.

<verification>
- pytest tests/test_postcall_split.py -x (Production-Server, client-Fixture) — gruen
- inspect.sh routes auf Production: /api/postcall_outcome + /api/postcall_cards vorhanden
- conf=0-Pfad liefert outcome None + Emit feuert trotzdem (LB-04)
</verification>

<success_criteria>
- Haiku-Outcome ueber schnellen eigenen Endpoint ohne Sonnet-Wartezeit (L-04, B-01)
- Sonnet-Lernkarten ueber separaten Endpoint (Ladebalken 2, post-confirm)
- conf=0-Degraded-Pfad + room-targeted Emit erhalten (LB-04)
- KEINE Schema-Aenderung, transcript_segments-Lesepfad (kein log_entries-getattr)
- Monolith-Shim bewusst als Debt dokumentiert (INFO 2) + Follow-up-Cleanup vorgemerkt
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-02-SUMMARY.md`
</output>
</output>
