---
phase: 08.23.2.D.UX.4
plan: 01
subsystem: backend-score-split
tags: [score, coaching_score, process_score, outcome_modifier, app_routes]
status: BLOCKED — Task 0 PASSED, Task 1 code-complete (committed+pushed, UNVERIFIED), Task 2+3 NOT executed
requires:
  - calls.score_breakdown (jsonb), calls.coaching_score (double) — existieren bereits
  - ConversationLog-Aggregate (kb_end, einwaende_*, redeanteil_avg, skript_abdeckung)
provides:
  - _calc_process_score(conv) -> (process_score, base_breakdown)  [committed, unverified]
  - _apply_outcome_modifier(process_score, base_breakdown, outcome) -> (cs, final, full)  [committed, unverified]
  - _calc_coaching_score(conv, outcome) Thin-Wrapper  [committed, unverified]
affects:
  - routes/app_routes.py
  - tests/test_score_split.py
tech-stack:
  added: []
  patterns: [function-split, thin-wrapper-backcompat]
key-files:
  created:
    - tests/test_score_split.py
  modified:
    - routes/app_routes.py
decisions:
  - "Task 0 HIGH-1-Gate GRUEN auf Production-Real-Daten (Aggregate non-default)"
  - "Split exakt nach Plan: base_breakdown ohne Modifier-Keys, _apply_outcome_modifier ergaenzt sie"
metrics:
  duration: "~25min (bis Blocker)"
  completed_date: 2026-05-31
---

# Phase 08.23.2.D.UX.4 Plan 01: Backend Score-Split Summary

Score-Split (`_calc_coaching_score` -> `_calc_process_score` + `_apply_outcome_modifier` + Thin-Wrapper)
lokal implementiert, committet und gepusht. **BLOCKIERT bei der Production-Verifikation** (Task 1
`<verify>`): die Production-Git-Historie ist gegenueber `origin/main` divergiert, `git pull` ist
nicht ohne Zerstoerung des laufenden Live-Working-Trees moeglich. Task 0 (HIGH-1-Gate) PASSED.
Task 2 + Task 3 NICHT ausgefuehrt (warten auf Verifikation von Task 1).

## Task 0 — HIGH-1-Gate (BLOCKING) — PASSED

Befehl (Production, HART-konform):
`ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && bash scripts/inspect.sh sample conversation_logs 20'`

Sample lieferte 10 Rows (id 191-200), alle mit `ended_at != NULL` und `dauer_sekunden > 0`
(echte beendete Calls). Positionsverifizierte Aggregat-Spalten (Spaltenreihenfolge gegen den
verbatim Header der `sample`-Ausgabe gemappt):

```
 id  | ended_at                    | dauer | segmente | einw_gesamt | einw_behandelt | kb_end | redeanteil_avg | skript_abdeckung
-----+-----------------------------+-------+----------+-------------+----------------+--------+----------------+-----------------
 191 | 2026-05-24 12:52:28.432768  |   52  |    0     |     4       |       4        |   30   |       0        |       17
 192 | 2026-05-27 09:58:01.542813  |   37  |    0     |     1       |       1        |   30   |       0        |       17
 193 | 2026-05-27 10:03:25.444898  |   18  |    0     |     0       |       0        |   30   |       0        |       17
 194 | 2026-05-27 10:06:19.347618  |   54  |    0     |     0       |       0        |   30   |       0        |       17
 195 | 2026-05-27 10:10:58.397736  |   30  |    0     |     0       |       0        |   30   |       0        |       17
 196 | 2026-05-27 10:35:38.254914  |   62  |    0     |     0       |       0        |   30   |       0        |       17
 197 | 2026-05-27 10:39:56.880461  |   63  |   12     |     0       |       0        |   30   |       0        |       33
 198 | 2026-05-27 10:46:22.527395  |   62  |   12     |     0       |       0        |   30   |       0        |       33
 199 | 2026-05-28 16:21:54.122243  |   56  |   11     |     0       |       0        |   35   |       0        |       17
 200 | 2026-05-30 11:51:32.727812  |   50  |   11     |     0       |       0        |   33   |       0        |       33
```

### Non-Default-Belege (Gate-Entscheidung)

| Aggregat | Default | Beleg non-default |
|---|---|---|
| `kb_end` | 30 | **id 199 = 35**, **id 200 = 33** (>= 1 Row weicht ab -> echt geschrieben) |
| `skript_abdeckung` | 0 | **ALLE Rows non-zero** (17 oder 33) |
| `einwaende_gesamt` | 0 | **id 191 = 4**, **id 192 = 1** |
| `einwaende_behandelt` | 0 | **id 191 = 4**, **id 192 = 1** |
| `redeanteil_avg` | 0/50 | im Sample durchgehend 0 (siehe Hinweis) |

**Gate-Verdikt: GRUEN.** Acceptance-Kriterium (`kb_end != 30 ODER redeanteil != 50/0 ODER
skript != 0 ODER einwaende_gesamt > 0` in mind. einer Row) ist mehrfach erfuellt. Der HIGH-1-
Failure-Fall (ALLE Rows fuer ALLE 5 Aggregate exakt Default) liegt eindeutig NICHT vor — die
Aggregate werden real persistiert, der Stash (Task 2) wird nicht systematisch mit Defaults rechnen.
Der Code-Trace (Plan Sektion 5: Aggregate committed Z.322 + Z.403 VOR Stash-Punkt Z.603) ist
empirisch bestaetigt.

**Hinweis `redeanteil_avg`:** im Sample durchgehend 0. Das Gate ist OR-basiert und ist erfuellt;
die HIGH-1-Sorge (systematisch ALLE Aggregate Default) ist widerlegt. `redeanteil_avg=0` deutet
darauf hin, dass die `stats.get('redeanteil', 0)`-Quelle (app_routes.py Z.305) bei diesen Test-
Calls 0 lieferte (kurze Test-Calls ohne Redeanteil-Messung). Das ist KEIN Stash-Defekt — der
Prozess-Score-Fallback in `_calc_process_score` setzt redeanteil-Default nur bei `conv is None`
oder `redeanteil_avg is None`, nicht bei echtem 0. Fuer 0 wird `redeanteil_score = 100-abs(0-40)*2
= 20` gerechnet (echter Wert, kein Default). Kein Handlungsbedarf.

## Task 1 — Score-Split (committet + gepusht, UNVERIFIZIERT)

Code-Aenderung in `routes/app_routes.py` (Z.720-764 ersetzt durch drei Funktionen):
- `_calc_process_score(conv)` — outcome-unabhaengig, Returns `(process_score, base_breakdown)`.
  base_breakdown enthaelt NUR: schema_version, kb_end_norm, behandelt_rate, redeanteil_score,
  skript_norm, frage_qualitaet, process_score (KEIN outcome_modifier/final_score/computed_at_iso).
- `_apply_outcome_modifier(process_score, base_breakdown, outcome)` — Returns
  `(coaching_score_float, final_score_int, full_breakdown)`. full = base + outcome_modifier +
  final_score + computed_at_iso (identische Keys wie alter Monolith).
- `_calc_coaching_score(conv, outcome)` — Thin-Wrapper, delegiert an beide. Signatur + 3-Tupel
  unveraendert (Rueckwaerts-Kompatibilitaet fuer correct_outcome + tests/test_correct_outcome.py).
- `_calc_call_score` (Z.695) + `_OUTCOME_MODIFIERS` (Z.709) UNVERAENDERT.

Neue Test-Datei `tests/test_score_split.py` (5 Runtime-Tests, kein Source-Presence):
test_process_score_outcome_independent, test_modifier_application, test_modifier_unknown_outcome,
test_thin_wrapper_compat, test_none_conv_defaults.

**Commit:** `8505a49` — `feat(08.23.2.D.UX.4-01): split score into process+modifier` (gepusht
nach origin/main: `dbb9537..8505a49`).

**Verifikations-Status: NICHT durchgefuehrt.** Die Plan-`<verify>`-Sequenz (`git pull` auf
Production -> pytest) ist am Production-Git-State gescheitert (siehe Blocker). HART verbietet
lokales pytest, daher gibt es aktuell KEINEN gruenen Verifikations-Beleg fuer Task 1.

## BLOCKER — Production-Git-Divergenz (Deployment-State, Rule 4 / Constraint 5)

Die Task-1-`<verify>` setzt `cd /opt/nerve/app && git pull` voraus. Das schlaegt fehl:

**Production-Zustand (verifiziert via SSH 2026-05-31):**
- `git HEAD` = `014fcef` ("chore: remove debug logging from EWB buttons") — ein EWB-Debug-Branch.
- `.deploy_meta` = `GIT_HEAD=a2d7d3c` (DEPLOYED_AT 2026-05-30) — der letzte echte D.UX.1-Commit.
- Working-Tree-`app_routes.py` ist **identisch zu `a2d7d3c`** (`git diff --stat a2d7d3c --
  routes/app_routes.py` = leer). Der laufende Live-Code = `a2d7d3c`-Inhalt, manuell auf HEAD
  `014fcef` aufgepatcht -> `git status` zeigt ~70 Dateien als "modified" + viele untracked.
- Mein gepushter Commit `8505a49` unterscheidet sich vom Live-Working-Tree exakt um den
  Score-Split (`git diff --stat 8505a49 -- routes/app_routes.py` = 16 insertions / 43 deletions)
  — d.h. der Split ist die einzige noetige Delta auf den Live-Code, Implementierung korrekt.

**Warum BLOCK statt Auto-Fix (Rule 4):**
- `origin/main` (Basis `dbb9537`, mit meinem `8505a49`) und Production-HEAD `014fcef` haben
  DIVERGIERTE Historien. `a2d7d3c` (= Live-Code) ist auf dem Server erreichbar, aber nicht HEAD.
- `git pull` ist unmoeglich ohne (a) Verwerfen des kompletten Live-Working-Trees (zerstoert
  laufenden Production-Code) ODER (b) manuelle Reconciliation zweier divergenter Branches.
- Beides ist eine Production-Deployment-State-Entscheidung ausserhalb meiner Befugnis und
  explizit Teil der `manual-direct-prod`-Prozedur, die Andre besitzt (Memory + HART-Regel).
- Constraint 5 (Realitaet widerspricht Plan-Annahme) + destructive_git_prohibition: STOP + Report,
  KEIN `git reset --hard`/`git clean`/`git checkout -- .` auf dem Production-Server.

**Folge:** Task 1 ist code-fertig + gepusht, aber UNVERIFIZIERT. Task 2 (Beenden-Stash) und
Task 3 (correct_outcome-Rewire) wurden NICHT ausgefuehrt, weil sie auf einer gruenen Task-1-
Verifikation aufbauen und ohne deploybare Production-Basis kein Verify moeglich ist.

## Self-Check

- `tests/test_score_split.py` — FOUND (lokal erstellt)
- `routes/app_routes.py` Edit — FOUND (lokal, committet)
- Commit `8505a49` — FOUND auf origin/main (Push bestaetigt: `dbb9537..8505a49`)

## Empfohlene naechste Schritte (Andre-Entscheidung noetig)

1. **Production-Git-State sanieren** (manual-direct-prod, Andre-owned): entscheiden, wie der
   divergente Live-Working-Tree (`a2d7d3c`-Inhalt auf HEAD `014fcef`) mit `origin/main`
   (`8505a49`) zusammengefuehrt wird — z.B. Live-Aenderungen committen/stashen, dann
   `8505a49` deployen; ODER `deploy.sh production` falls dieser den Zustand sauber handhabt.
2. Danach Task 1 `<verify>` nachholen: `pytest tests/test_score_split.py tests/test_correct_outcome.py -x`.
3. Erst bei gruenem Task-1-Verify: Task 2 + Task 3 ausfuehren (Code unveraendert wie geplant).

## Deviations from Plan

- **[Rule 3 -> Rule 4 Eskalation] Production-Git-Divergenz blockiert Deploy/Verify.** Plan-`<verify>`
  nahm sauberen `git pull` an; Production-Working-Tree ist divergiert. Eskaliert zu Rule 4
  (Deployment-State / Infra-Entscheidung), STOP statt destruktiver Auto-Fix.
- **[Abweichung Commit-Staging] `git add <files>` statt `git add -A`.** Plan-`<verify>` nutzt
  `git add -A`; lokal liegen `.planning/STATE.md` (Constraint 1: nicht anfassen) + unrelated
  untracked logs vor. Nur `routes/app_routes.py` + `tests/test_score_split.py` gestaged.

## Self-Check: PASSED (lokale Artefakte + Push), Verifikation OFFEN (Blocker)
