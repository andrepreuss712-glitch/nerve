---
phase: 08.23.2.D.UX.4
plan: 03
type: execute
wave: 2
depends_on: ["08.23.2.D.UX.4-01", "08.23.2.D.UX.4-02"]
files_modified:
  - static/pip-launcher.js
  - static/nerve.css
  - static/outcome-modal-state.js
  - tests/test_decide_modal_state.test.js
autonomous: true
complexity: medium
# LB-03 (skip-link) ist DEFERRED, B-04 (terminal/no-resume) ist SATISFIED-BY-DESIGN — beide werden
# NICHT in diesem Plan gebaut, nur zur Coverage-Transparenz unten annotiert (WARNING 3).
# SCOPE-DOWN (Andre 2026-05-31, Claudian-Audit NEW-3 Option 3): "Ladebalken 2 in PiP" + Sonnet-
# Lernkarten-Render im PiP werden NICHT gebaut. S-03 ist auf "Score + Basis-Analytics erscheinen
# sofort zusammen, null-safe" reduziert; LB-04 (zwei Ladebalken im PiP) auf "ein Ladebalken (LB-1)"
# reduziert. Die Sonnet-Analyse selbst (LearningCard-Persistenz) bleibt — laeuft im Hintergrund ab
# Beenden, unabhaengig vom Confirm (siehe Plan 02). Diese Reduktion supersedet die "Ladebalken 2"-
# Teile von S-03/LB-04; die "Score + Basis-Analytics zusammen sofort"-Teile von S-03 BLEIBEN.
requirements: [L-01, L-02, L-03, L-04, U-01, U-02, U-03, U-04, B-01, B-02, B-03, LB-01, LB-02, "LB-04 (scoped-down: 1 Ladebalken im PiP, Andre 2026-05-31)", S-01, "S-03 (scoped-down: kein Ladebalken-2/Cards-Render im PiP, Andre 2026-05-31)", "B-04 (satisfied-by-design)", "LB-03 (deferred)"]
must_haves:
  truths:
    - "Beenden-Knopf endet den Call NUR nach 600ms Press-and-Hold; einzelner Tap/Klick beendet nicht (B-02)"
    - "Die 600ms-Hold-Bindung ueberlebt PiP-Neuaufbauten — gebunden ueber PiP-Document-Pointer-Delegation (closest('#nlp-btn-beenden')) ODER bei jedem PiP-Rebuild neu angehaengt; NICHT ein einmaliges addEventListener auf das Button-Element, das beim Rebuild neu erzeugt wird (F10)"
    - "Nach Hold-Complete: Mic stoppt sofort, dann Ladebalken 1 'KI wertet das Gespraech aus…' (unbestimmter Spinner, kein Prozentbalken) (LB-01)"
    - "Ladebalken 1 hat 9s-Timeout -> Outcome-Screen ohne Vorauswahl (kein_versuch); der Timeout-Pfad setzt outcomeRendered='1', sodass ein Late-Haiku-Socket-Emit den Timeout-Screen NICHT ueberschreibt (LB-02, HIGH-2)"
    - "Outcome-Screen-Heading ist 'Call wirklich beenden?'; der Screen IST die Bestaetigung, kein separater Ja/Nein-Dialog (B-01); terminal, kein Back-to-Call (B-03)"
    - "Sichere KI (ai_auto): teal Vorauswahl + Bestaetigen sofort klickbar; Unsichere KI (ai_auto_unsicher): ROTE Vorauswahl + 'KI unsicher, bitte pruefen' + Bestaetigen disabled bis aktiver Tap (U-01)"
    - "Bestaetigen-Button ist waehrend des laufenden correct_outcome-POST disabled (Submit-Lock), wird im .catch wieder freigegeben — kein Double-Submit (MEDIUM-3)"
    - "Score erscheint sofort nach Confirm ohne Animation, EINMAL, aus correct_outcome-Response (S-01); kein vorlaeufiger _calcScore mehr im Render-Pfad (L-01)"
    - "Score + Analytics werden ueber pipEl() im PiP-Fenster gerendert (NICHT bare document) — nach Confirm im PiP sichtbar; jeder Element-Lookup im Reorder-/Outcome-/Score-Pfad geht ueber pipEl() / state.pipWindow.document (NEW-1/F5 BLOCKER-Fix)"
    - "Score-Karte + Basis-Analytics (Kaufbereitschaft, Redeanteil, Einwaende) erscheinen ZUSAMMEN sofort nach Confirm; Basis-Analytics sind null-safe wenn pendingPostcall fehlt (Score allein, KPI-Tiles leer/uebersprungen, kein Throw) (S-03 scoped-down)"
    - "Die postcall-Section (#nlp-section-postcall) bleibt im Ladebalken-1-/Outcome-Screen-Zustand versteckt (display:none via pipEl); NUR der Confirm-Handler deckt sie auf — _showPostcall wird am Beenden NICHT mehr aufgerufen, daher uebernimmt der Confirm-Handler explizit das Verstecken-bis-Confirm (F9)"
    - "Der Trend wird im Confirm-Handler mit dem bestaetigten final_score gefuettert: _fetchAndRenderTrend(json.final_score) — NICHT mit dem alten _calcScore-Wert (F9)"
    - "Sonnet-Lernkarten-Generierung (LearningCard-Persistenz) laeuft im Hintergrund ab Beenden, unabhaengig vom Confirm — keine Daten-Regression bei Abbruch; im PiP werden KEINE Karten gerendert und KEIN zweiter Ladebalken gezeigt (Option 3, Andre 2026-05-31)"
    - "state.pendingPostcall (und state.lastCallId) werden im Reset-/Teardown-Pfad genullt, damit kein Postcall-/Call-Zustand in den naechsten Call leckt (NEW-5)"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "endCall-Reorder, Hold-to-end (rebuild-sicher), Ladebalken 1, 9s-Timeout mit Generation-Guard + outcomeRendered-Set (HIGH-2), Confirm-Submit-Lock (MEDIUM-3), _renderOutcomeUx 3 States PiP-aware via pipEl (NEW-1/F5), _calcScore aus Render entfernt, Score+Analytics sofort null-safe via pipEl (S-03 scoped-down), Confirm-Handler haelt postcall-Section bis Confirm versteckt + Trend mit final_score (F9), background postcall_cards-Trigger am Beenden (NEW-2/Option 3), Cleanup nullt pendingPostcall (NEW-5)"
      contains: "Halten zum Beenden"
    - path: "static/outcome-modal-state.js"
      provides: "_decideModalState mit U-01-konformem 'unsicher' (Vorauswahl gesetzt aber Confirm-disabled-Semantik im Render)"
      contains: "_decideModalState"
    - path: "static/nerve.css"
      provides: "pip-outcome-btn--selected-unsicher (rote Vorauswahl, Token-only) + Ladebalken-1-Spinner-Klasse (KEINE -spinner-2-Klasse, Option 3)"
      contains: "pip-outcome-btn--selected-unsicher"
    - path: "tests/test_decide_modal_state.test.js"
      provides: "node:test fuer _decideModalState inkl. U-01-Faelle (sicher/unsicher/kein_versuch/final)"
      contains: "_decideModalState"
  key_links:
    - from: "static/pip-launcher.js:_wirePipButtons (PiP-Document-Pointer-Delegation)"
      to: "endCall via 600ms pointer-hold (rebuild-sicher)"
      via: "pointerdown/up/leave/cancel mit closest('#nlp-btn-beenden') auf dem PiP-Document"
      pattern: "Halten zum Beenden"
    - from: "static/pip-launcher.js:endCall"
      to: "/api/postcall_outcome (Haiku) + 9s timeout + background /api/postcall_cards (Sonnet, fire-and-forget)"
      via: "fetch + endCallGen stale-guard; cards-Trigger unabhaengig vom Confirm"
      pattern: "postcall_outcome"
    - from: "static/pip-launcher.js:confirm-handler"
      to: "/api/calls/<id>/correct_outcome -> Score+Analytics via pipEl + _fetchAndRenderTrend(final_score)"
      via: "pipEl('nlp-section-postcall') reveal nach correct_outcome-Erfolg"
      pattern: "_fetchAndRenderTrend(json.final_score)"
---

<objective>
Frontend-Reihenfolge-Umbau (B-01/L-04/U-01/S-03 + B-02 Hold). Heute zeigt `endCall()` einen vorlaeufigen
Frontend-Score (`_calcScore`, L-01-Bug), wartet auf den langsamen Sonnet (im alten Monolith), und der
Outcome-Screen erscheint erst danach. Dieser Plan baut die Sequenz im PiP um:

Beenden (600ms Hold, B-02) -> Mic-Stop + Ladebalken 1 "KI wertet das Gespraech aus…" (LB-01, 9s-Timeout LB-02)
-> Outcome-Screen "Call wirklich beenden?" mit 3 Vorauswahl-States (U-01) -> Confirm -> Score + Basis-Analytics
SOFORT zusammen (S-01/S-03). KEIN zweiter Ladebalken, KEINE Lernkarten im PiP (Option 3, Andre 2026-05-31).

SCOPE-DOWN (Andre 2026-05-31, Claudian-Audit NEW-3 Option 3): Nach dem Confirm zeigt der PiP NUR Score +
Basis-Analytics (Kaufbereitschaft / Redeanteil / Einwaende). KEINE Sonnet-Lernkarten-Liste, KEIN zweiter
Ladebalken, KEIN "-> Dashboard"-CTA im PiP. Die Sonnet-Analyse selbst (LearningCard-Persistenz) bleibt —
sie wird im Hintergrund direkt am Beenden getriggert (unabhaengig vom Confirm), die Response wird im
Frontend VERWORFEN (kein Render). Damit gibt es keine Daten-Regression wenn der User abbricht, aber auch
keine neu erfundene Karten-UI im PiP (gegen "nur umsortieren"-Regel). Siehe KNOWN-GAP unten.

BLOCKER-Fix (NEW-1/F5): Alle Element-Lookups im Reorder-/Outcome-/Score-Pfad gehen ueber `pipEl()` (bzw.
`state.pipWindow.document`), NICHT ueber bare `document`. Bare `document` trifft im PiP-Modus das HAUPT-
Fenster -> der Score landet unsichtbar im falschen Dokument. Das ist die Wurzel des BLOCKERs.

Konsumiert die zwei Backend-Endpoints aus Plan 02 (`/api/postcall_outcome` schnell, `/api/postcall_cards`
Hintergrund) und die unveraenderte `correct_outcome`-Response aus Plan 01 (S-01). Entfernt `_calcScore`
aus dem Score-Render-Pfad (L-01). Alle async-Pfade tragen den `endCallGen`-Generation-Guard (Race-Schutz).

Purpose: Outcome-vor-Score, Score genau einmal, ehrliches Loading, Score im RICHTIGEN PiP-Fenster sichtbar.
Output: umgebaute pip-launcher.js, token-gebundene CSS-Ergaenzungen, U-01-erweiterte modal-state-Logik + node-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-CONTEXT.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md

<interfaces>
<!-- Aus pip-launcher.js + outcome-modal-state.js + nerve.css extrahiert. Direkt verwenden. -->

PIP-DOM-HELFER (KRITISCH, NEW-1/F5) — `pipEl(id)` (pip-launcher.js:99-104):
```javascript
function pipEl(id) {
  if (state.pipWindow && state.pipWindow.document) {
    var e = state.pipWindow.document.getElementById(id);
    if (e) return e;
  }
  return document.getElementById(id);
}
```
`pipEl(id)` loest ein Element ZUERST im PiP-Fenster auf, faellt nur dann auf das Haupt-`document` zurueck,
wenn kein PiP aktiv ist. Bares `document.getElementById(...)` / `document.querySelector(...)` trifft im
PiP-Modus IMMER das Haupt-Fenster -> alles dort gerenderte/getoggelte ist fuer den User im PiP UNSICHTBAR.
DAS ist die Wurzel des BLOCKERs. REGEL fuer diesen Plan: jeder Lookup im Reorder-/Outcome-/Score-Pfad
geht ueber `pipEl(...)`; neu erzeugte Sektionen werden in `state.pipWindow.document` erzeugt (Fallback
Haupt-Doc nur ohne PiP).

BARE-`document`-STELLEN DIE UMGEBAUT WERDEN MUESSEN (verifiziert, vom Orchestrator gegrept):
- `_showPostcall` Z.3045: `var scoreSection = document.querySelector('#nlp-section-postcall');` (Score-Hide, D-W3-04)
- confirm-handler Z.3823: `var scoreSection = document.querySelector('#nlp-section-postcall');` (Score-Reveal)
- `_renderOutcomeUx` Z.3671: `var sec = document.getElementById('pip-outcome-section');`
- `_renderOutcomeUx` Z.3673: Host-Lookup `document.getElementById('pip-postcall') || document.querySelector('#nlp-section-postcall') || document.body`
- socket-Handler Z.2469 + latest_outcome Z.2955: `document.getElementById('pip-outcome-section')`
KORREKTE Outcome-Section-ID = `pip-outcome-section` (NICHT `nlp-outcome-section`).

Beenden-Delegation (pip-launcher.js:1784-1795) — heute Single-Click, ueber PiP-Document-Delegation:
```javascript
pipWindow.document.addEventListener('click', function (ev) {
  var beenden = t.closest('#nlp-btn-beenden');
  if (beenden) { ev.preventDefault(); ev.stopPropagation(); endCall(); return; }
  ...
```
Das ist die rebuild-SICHERE Bindungsart (Document-Delegation, ueberlebt Button-Neuaufbau). Die Hold-Logik
MUSS denselben Mechanismus nutzen (F10): pointerdown/up/leave/cancel-Delegation auf `pipWindow.document`
mit `t.closest('#nlp-btn-beenden')`, NICHT ein addEventListener direkt auf dem Button (Z.2070 wird der
Button bei jedem PiP-Rebuild via `pipEl('nlp-btn-beenden')` neu aufgeloest -> ein Element-Listener ginge verloren).

endCall (2876) heute: _stopTimer/_stopMic/_resetLiveState/_showPostcallLoading,
endCallGen = (state.callGen = (state.callGen||0)+1), fetch /api/beenden ->
.then(data) { stale-guard `if (state.callGen !== endCallGen || state.micStarted) return;`
state.lastConvId=data.conv_id; state.lastCallId=data.call_id; _showPostcall(data.postcall);
dann fetch /api/postcall_analysis -> .then(paResult) _renderOutcomeUx(...) }.

_calcScore (2982): vorlaeufiger FE-Score. EINZIGER Caller = _showPostcall:3047. -> aus Render entfernen.

_showPostcall (3020): hasConversation-Guard, versteckt #nlp-section-postcall (Z.3045 bare document!),
ruft _calcScore (Z.3047) + _buildTags + _showPostcallRaw + _renderQuickStats + _renderSparkline +
_fetchAndRenderTrend(score) (Z.3053 — score aus _calcScore, das ist der F9-Fehler-Feed).

outcome_ready Socket-Handler (2444/2469): if d.call_id mismatch -> ignore; sonst _renderOutcomeUx(d).

_decideModalState (outcome-modal-state.js): 5 Zustaende. Aktuell:
  source==='user_corrected' -> 'final'; conf===0 -> 'kein_versuch'; source==='ai_auto' -> 'sicher';
  source==='ai_auto_unsicher' -> 'unsicher'; sonst 'unsicher'.

_renderOutcomeUx (3669): Idempotenz-Guard sec.dataset.outcomeRendered==='1' (Z.3679); sec.dataset
.outcomeRendered='1' wird heute auf Z.3680 SOFORT nach der sec-Aufloesung gesetzt (PRUEFEN beim Lesen:
ob auf ALLEN Branches erreicht — HIGH-2). Header-Text heute 'Gesprächsergebnis bestätigen'; selectedOutcome
nur bei 'sicher' gesetzt; Badge 'KI unsicher — bitte wählen' (Teal-Outline) bei 'unsicher'; Hint
'Automatische Erkennung nicht verfügbar' bei 'kein_versuch'; 7 Buttons aus _OUTCOME_OPTS; confirmBtn disabled
wenn !selectedOutcome; confirm-handler POSTet /api/calls/<callId>/correct_outcome, rendert json.final_score+'%'
in #nlp-postcall-score, deckt #nlp-section-postcall auf (Z.3823 bare document!), kollabiert sec zu '✓ Label'.

WICHTIG (HIGH-2): pruefe beim Lesen von _renderOutcomeUx, in WELCHEN Branches heute
`sec.dataset.outcomeRendered = '1'` gesetzt wird. Falls es NUR auf dem 'sicher'/preselect-Pfad gesetzt
wird (und NICHT im 'kein_versuch'-Branch), ist genau das der HIGH-2-Bug. Der Fix unten verlangt, dass es
auf ALLEN Branches gesetzt wird — inkl. kein_versuch (Timeout/No-Attempt).

_OUTCOME_OPTS (3639): [['meeting_booked','Termin gebucht'],['callback','Rückruf vereinbart'],
['send_info','Infos zusenden'],['wrong_person','Falsche Person'],['gatekeeper_blocked','Gatekeeper blockt'],
['no_interest','Kein Interesse'],['contract_signed','Abschluss']].

correct_outcome-Response (Plan 01, unveraendert): {ok, coaching_score, final_score(int), outcome,
followup_intent, note_dropped?}. Frontend rendert json.final_score+'%'.

CLEANUP-PFAD (NEW-5): state.lastCallId wird heute NUR am Call-START genullt (Z.1449). state.pendingPostcall
(neu in dieser Phase) wird NIRGENDS genullt. Reset-/Teardown-Pfad: _resetLiveState / _showPipLive (~2064+,
re-resolved Elemente via pipEl) + teardown-forEach Z.3269 ueber ['nlp-btn-beenden','nlp-ewb-row','pip-section-live'].

NEUE Endpoints (Plan 02):
- POST /api/postcall_outcome  Body wie heute /api/postcall_analysis (conv_id, call_id, einwaende...);
  Response {outcome, confidence, source, call_id}. SCHNELL (Haiku, kein Sonnet).
- POST /api/postcall_cards    Body identisch; Response {vorschlaege}. Im Hintergrund AM BEENDEN triggern
  (fire-and-forget), Response im Frontend VERWERFEN (kein PiP-Render, Option 3). NICHT nach Confirm.

nerve.css bestehende Tokens (verifiziert): --btn-danger-bg rgba(248,113,113,0.1), --btn-danger-text #f87171,
--btn-danger-border rgba(248,113,113,0.2); .pip-outcome-btn--selected (teal), .pip-outcome-btn--disabled
(opacity .45, not-allowed); --session-score-high #00D4AA / --session-score-mid #6B7280 /
--session-score-low #f87171; .n-kpi-value (28px JetBrains Mono), .n-kpi-label (11px). @keyframes n-pulse.

UI-SPEC Copy-Contract (echte Umlaute, user-facing):
  rest 'Beenden' | holding 'Halten zum Beenden…' | keyboard 'Nochmal drücken zum Beenden' |
  Ladebalken1 'KI wertet das Gespräch aus…' | Heading 'Call wirklich beenden?' |
  Disclaimer 'KI unsicher, bitte prüfen' | CTA 'Bestätigen' | in-flight 'Bewertung wird berechnet…' |
  Score-Label 'Coaching-Score' | Analytics-Heading 'Auswertung' |
  Tiles 'Kaufbereitschaft' · 'Redeanteil' · 'Einwände' |
  Hard-Error 'Konnte nicht speichern – bitte erneut auf Bestätigen tippen.'
  (HINWEIS Option 3: KEIN 'Detail-Auswertung läuft…'-String mehr — Ladebalken 2 entfaellt im PiP.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Beenden Hold-to-end rebuild-sicher (B-02/F10) + endCall-Reorder + Ladebalken 1 + 9s-Timeout (PiP-aware, HIGH-2/F5) + background postcall_cards (NEW-2) + Cleanup-Null (NEW-5)</name>
  <read_first>
    - static/pip-launcher.js:99-104 (pipEl-Helfer — VERWENDEN statt bare document)
    - static/pip-launcher.js:1780-1810 (Beenden-Click-Delegation auf pipWindow.document, B-02/F10-Umbaupunkt)
    - static/pip-launcher.js:2060-2075 (_showPipLive — Button-Re-Resolve via pipEl('nlp-btn-beenden') bei Rebuild)
    - static/pip-launcher.js:2876-2979 (endCall — Reorder-Kern), 3225-3263 (_showPostcallLoading-Basis)
    - static/pip-launcher.js:2440-2490 (outcome_ready-Socket-Handler, pip-outcome-section-Lookup) + 2950-2960 (latest_outcome)
    - static/pip-launcher.js:3260-3275 (Reset-/Teardown-forEach) + :1445-1455 (Call-START lastCallId-Reset) + _resetLiveState
    - static/pip-launcher.js:3669-3760 (_renderOutcomeUx — PRUEFEN wo heute `outcomeRendered = '1'` gesetzt wird; HIGH-2-Wurzel)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md Sektionen "B-02" + "Step 1/Step 2"
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md "Ladebalken-1 Timeout" + Landmines 1,2,7
  </read_first>
  <action>
    A) HOLD-TO-END REBUILD-SICHER (B-02 + F10, UI-SPEC: Placement-Isolation + 600ms Hold, kein Dialog).
    F10 (Audit): die Hold-Logik DARF NICHT als einmaliges `addEventListener` auf dem Button-Element haengen —
    der Button wird bei jedem PiP-Rebuild in _showPipLive (~Z.2070) via `pipEl('nlp-btn-beenden')` neu
    aufgeloest, ein Element-Listener ginge verloren. Stattdessen GENAU EINER der folgenden Wege (beide
    rebuild-sicher), konsistent zur bestehenden Click-Delegation in _wirePipButtons:1788:
      WEG A (bevorzugt): Pointer-Delegation auf dem PiP-Document. In _wirePipButtons den bestehenden
      `pipWindow.document.addEventListener('click', ...)`-Block fuer #nlp-btn-beenden ersetzen durch
      pointerdown/pointerup/pointerleave/pointercancel-Delegations-Listener auf demselben
      `pipWindow.document` (bzw. `state.pipWindow.document`), jeweils mit `ev.target.closest('#nlp-btn-beenden')`-
      Treffer-Check. Hold-State (holdTimer/holdFired) in einer Closure ueber _wirePipButtons halten.
      WEG B: in _showPipLive nach jedem `pipEl('nlp-btn-beenden')`-Re-Resolve die Hold-Handler frisch
      anhaengen (mit vorherigem removeEventListener, damit kein Doppel-Bind). Dann ist es kein "one-time".
    Verhalten (egal welcher Weg):
    - pointerdown auf #nlp-btn-beenden: Hold-Timer `var holdTimer = setTimeout(function(){ ... endCall() ... }, 600)`
      starten; Label auf 'Halten zum Beenden…' setzen; rote Progress-Wipe-Klasse `pip-beenden-holding`
      hinzufuegen (CSS aus Task 3). Bei 600ms: kurz solid-hover-Flash (~120ms), dann `try { endCall(); } catch...`.
      Doppel-Trigger-Schutz (Landmine 7): ein `holdFired`-Flag, damit pointerup nach Complete nicht erneut feuert.
    - pointerup / pointerleave / pointercancel VOR 600ms: clearTimeout(holdTimer), Label zurueck auf 'Beenden',
      Wipe-Klasse entfernen, KEIN endCall.
    - Keyboard-Aequivalent (UI-SPEC, B-02): Enter/Space auf fokussiertem Beenden zeigt inline den exakten
      String 'Nochmal drücken zum Beenden' (echte Umlaute, user-facing) und verlangt zweites Enter/Space
      binnen 2s -> dann endCall. Kein Modal. (Ein `keyArmedAt`-Timestamp + 2000ms-Fenster + Reset-Timeout.)
      Auch das ueber die Document-Delegation bzw. rebuild-sicher binden.
    - Den bestehenden single-`click`-Zweig fuer #nlp-btn-beenden ENTFERNEN (sonst feuern Click + Hold ->
      Doppel-endCall, Landmine 7). EWB-Click-Delegation unangetastet lassen.
    WICHTIG: im richtigen Fenster binden — `state.pipWindow.document` (Fallback document ohne PiP).

    B) endCall-REORDER (B-01/L-04 + Ladebalken 1, PiP-aware). In endCall (2876):
    - _stopTimer()/_stopMic()/_resetLiveState() SOFORT beibehalten (B-01: Mic stoppt sofort).
    - Statt `_showPostcallLoading()` (Score-Spinner) eine neue Loading-View `_showLadebalken1()` zeigen:
      zentrierter unbestimmter Spinner (Klasse `pip-loading-spinner` aus Task 3, nutzt @keyframes n-pulse)
      + Text 'KI wertet das Gespräch aus…' (14px/600). KEIN Prozentbalken (LB-01). View im PiP-Fenster
      rendern (ueber pipEl-aufgeloeste Container, NICHT bare document).
    - `var endCallGen = (state.callGen = (state.callGen||0)+1);` (bestehender Guard beibehalten).
    - fetch /api/beenden wie heute. Im .then(data): bestehenden stale-guard
      `if (state.callGen !== endCallGen || state.micStarted) return;` behalten. state.lastConvId/lastCallId setzen.
      `_showPostcall(data.postcall)` NICHT mehr hier aufrufen — der Score-Render kommt erst nach Confirm
      (Task 2/3). Stattdessen `data.postcall` in `state.pendingPostcall = data.postcall` zwischenspeichern
      (S-03: Basis-Analytics werden zusammen mit Score nach Confirm gerendert).
      HINWEIS (BLOCKER-Bezug): `state.pendingPostcall` wird NUR auf dem Erfolgs-/Non-Stale-Pfad gesetzt.
      Auf dem 9s-Timeout-Pfad, bei einem stale-guard-`return` (callGen-Mismatch) oder einem
      outcome_ready-Socket-Race bleibt es undefined. Task 3 MUSS den KPI-Render deshalb null-safe halten.
    - BACKGROUND POSTCALL_CARDS (NEW-2 / Option 3): NOCH im selben /api/beenden-Erfolgs-.then (nach dem
      stale-guard, unabhaengig vom spaeteren Confirm) einen FIRE-AND-FORGET `fetch('/api/postcall_cards',
      {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({conv_id:data.conv_id,
      call_id:data.call_id, einwaende:..., painpoints:..., kb_start:..., kb_end:..., redeanteil_berater:...,
      redeanteil_kunde:..., dauer_sek:..., skript_abdeckung:..., ga_details:..., kaufsignale:...})})` absetzen
      (dieselben postcall-Felder wie der Outcome-Call). Die Response wird VERWORFEN (`.then(function(){}).catch
      (function(){})` — kein PiP-Render, Option 3). Zweck: Sonnet-LearningCard-Persistenz laeuft auch wenn der
      User den Outcome-Screen abbricht (keine Daten-Regression vs. heutigem Monolith, der die Karten immer am
      Beenden persistiert hat). endCallGen-Guard fuer diesen Hintergrund-Call OPTIONAL (Ergebnis wird eh verworfen).
    - 9s-TIMEOUT (LB-02 + HIGH-2 + F5) armen, BEVOR der Outcome-Fetch startet:
      ```javascript
      var haikuTimedOut = false;
      var lb1Timeout = setTimeout(function () {
        if (state.callGen !== endCallGen) return;            // Stale-Guard (Landmine 2)
        haikuTimedOut = true;
        _renderOutcomeUx({ outcome: null, confidence: 0, source: null, call_id: state.lastCallId });
        // HIGH-2: Timeout-Screen MUSS sofort gegen Late-Haiku-Socket idempotent sein.
        // F5-FIX: KORREKTE ID 'pip-outcome-section' UND PiP-aware via pipEl (NICHT bare/pipWindow-direct,
        // NICHT 'nlp-outcome-section'). Defence-in-depth zum Branch-Set in _renderOutcomeUx (Task 2 C):
        try {
          var _sec = pipEl('pip-outcome-section');
          if (_sec) _sec.dataset.outcomeRendered = '1';
        } catch (_e) {}
      }, 9000);
      ```
    - Statt /api/postcall_analysis jetzt `/api/postcall_outcome` (Plan 02) fetchen (Body unveraendert:
      conv_id, call_id, einwaende, painpoints, kb_start, kb_end, redeanteil_berater, redeanteil_kunde,
      dauer_sek, skript_abdeckung, ga_details, kaufsignale). Im .then(paResult):
      `clearTimeout(lb1Timeout); if (state.callGen !== endCallGen) return; if (haikuTimedOut) return;`
      (Late-Haiku darf den Timeout-Screen NICHT ueberschreiben — Landmine 1). Dann
      `if (paResult && paResult.call_id) _renderOutcomeUx({outcome,confidence,source,call_id});`
    - Den outcome_ready-Socket-Handler (2444/2469) ebenfalls absichern: der Idempotenz-Guard
      (`sec.dataset.outcomeRendered==='1'`) ist vorhanden; da der Timeout-Render JETZT outcomeRendered='1'
      auf der PiP-aufgeloesten Section setzt (HIGH-2-Fix in Task 2 C + Defence-in-depth oben), prallt ein
      spaeter Socket-Emit korrekt ab. Wenn der Socket-Handler selbst `document.getElementById('pip-outcome-section')`
      (Z.2469) nutzt, im Zuge dieses Plans auf `pipEl('pip-outcome-section')` umstellen (F5-Konsistenz);
      sonst keine Logikaenderung am Handler ausser dass er weiterhin _renderOutcomeUx aufruft.

    C) CLEANUP-NULL (NEW-5). Im Reset-/Teardown-Pfad (_resetLiveState bzw. _showPipLive ~Z.2064 /
    teardown-forEach Z.3269): `state.pendingPostcall = null;` setzen. Zusaetzlich sicherstellen, dass
    `state.lastCallId` im selben Reset-/Teardown-Pfad genullt wird (heute nur am Call-START Z.1449) — damit
    weder Postcall- noch Call-Zustand in den naechsten Call leckt. Konkret: am Anfang von _resetLiveState
    (oder der zentralen Teardown-Funktion, die beim Start einer neuen Live-Session laeuft) beide nullen.

    Generation-Guard (Landmine 2): lb1Timeout-Callback UND postcall_outcome-.then pruefen beide endCallGen.
    UTF-8: user-facing Strings mit echten Umlauten ('Halten zum Beenden…', 'Nochmal drücken zum Beenden',
    'KI wertet das Gespräch aus…'); Variablen/Klassen ASCII (holdTimer, holdFired, keyArmedAt,
    haikuTimedOut, pip-loading-spinner, pip-beenden-holding).

    HINWEIS: Die DEFINITIVE Quelle des outcomeRendered='1'-Setzens auf dem kein_versuch-Branch ist der
    Fix in Task 2 C (innerhalb _renderOutcomeUx, PiP-aware sec). Der direkte Set im Timeout-Callback oben ist
    Defence-in-depth fuer den Fall, dass der Executor den Branch in _renderOutcomeUx uebersieht.
  </action>
  <verify>
    <!-- Deploy + Server-Node-Test laufen NICHT pro Task. Sie laufen EINMAL pro Plan via manual-direct-prod (siehe <deploy_verify> unten), orchestrator-gefahren mit Andre-Approval vor jedem tar-Push. KEIN git pull auf Prod. -->
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-03): hold-to-end rebuild-safe + ladebalken1 + 9s timeout pipEl + reorder + bg cards + cleanup null" && git push && node --check static/pip-launcher.js</automated>
    <!-- VERIFY-GAP (bewusste Entscheidung, WARNING 2): Die Laufzeit-Logik dieses Tasks (600ms Hold,
    9s-Timeout, endCallGen-Generation-Guard, Late-Haiku-Suppression, Keyboard-2nd-Press, PiP-Fenster-Render)
    ist Vanilla-JS DOM/Pointer-Verhalten und headless nicht sinnvoll testbar. `node --check` deckt nur
    Parse-Fehler ab. Das Verhalten wird durch den PFLICHT-Post-Execute-Schritt validiert: Cross-AI Peer-
    Review + GSD Code-Review + Live-UAT auf Production (CLAUDE.md Punkt 7, 🔴-Phase). Die rein-funktionale,
    testbare Outcome-Entscheidung (sicher/unsicher/kein_versuch) liegt im _decideModalState-Pfad
    (outcome-modal-state.js), der in Task 2 via `node --test` abgedeckt ist. Dokumentierte Entscheidung. -->
  </verify>
  <acceptance_criteria>
    - grep `Halten zum Beenden` static/pip-launcher.js -> 1 Treffer (echte Umlaute, user-facing)
    - grep `Nochmal drücken zum Beenden` static/pip-launcher.js -> 1 Treffer (B-02 Keyboard-2nd-Press; echte Umlaute)
    - grep `KI wertet das Gespräch aus` static/pip-launcher.js -> 1 Treffer
    - grep `postcall_outcome` static/pip-launcher.js -> 1 Treffer (Outcome-Endpoint im endCall-Pfad)
    - grep `postcall_cards` static/pip-launcher.js erscheint im /api/beenden-/endCall-Hintergrund-Pfad
      (NEW-2 background-Trigger), NICHT im confirm-handler — grep zeigt den postcall_cards-fetch innerhalb
      des endCall-/beenden-.then, und 0 Treffer von postcall_cards im _renderOutcomeUx/confirm-handler-Block
    - grep `haikuTimedOut` static/pip-launcher.js -> mind. 2 Treffer (Arm + Guard)
    - grep `9000` static/pip-launcher.js -> 1 Treffer (9s-Timeout)
    - F5/HIGH-2: im lb1Timeout-Callback `pipEl('pip-outcome-section')` (KORREKTE ID, PiP-aware) + direkt
      danach `.dataset.outcomeRendered = '1'` — grep `nlp-outcome-section` im Timeout-Callback -> 0 Treffer;
      grep `pipWindow.document.getElementById('nlp-outcome-section')` -> 0 Treffer (alter F5-Fehler weg)
    - grep `state.pendingPostcall` static/pip-launcher.js -> Treffer (Postcall fuer S-03 zwischengespeichert)
    - NEW-5: grep `state.pendingPostcall = null` static/pip-launcher.js -> 1 Treffer im Reset-/Teardown-Pfad
      (_resetLiveState / _showPipLive / teardown-forEach); grep zeigt state.lastCallId-Reset ebenfalls im Reset-Pfad
    - F10: die Hold-Logik haengt NICHT als one-time addEventListener am Button-Element — sie ist entweder
      Pointer-Delegation auf state.pipWindow.document (closest('#nlp-btn-beenden')) ODER wird in _showPipLive
      bei jedem Rebuild neu angehaengt (Code-Pfad-Beleg in beiden Faellen pruefbar)
    - Im endCall-/beenden-.then: KEIN `_showPostcall(data.postcall)` mehr (Score erst nach Confirm) — grep zeigt
      _showPostcall nicht mehr im beenden-.then-Block
    - Beenden-Single-`click`-Zweig in _wirePipButtons ruft NICHT mehr direkt endCall (Hold-Logik ersetzt ihn)
    - node --check static/pip-launcher.js ok (kein Parse-Fehler)
  </acceptance_criteria>
  <done>Hold-to-end (600ms, rebuild-sicher ueber PiP-Document-Delegation bzw. Re-Attach, F10) + Keyboard-2nd-Press, Mic-sofort-Stop, Ladebalken 1 im PiP mit 9s-Timeout (pipEl, korrekte ID, F5/HIGH-2) + Generation-Guard, neuer Outcome-Endpoint, background postcall_cards am Beenden unabhaengig vom Confirm (NEW-2/Option 3, Response verworfen), Postcall fuer S-03 zwischengespeichert (nur Erfolgs-Pfad), Cleanup nullt pendingPostcall + lastCallId (NEW-5). Timeout-Pfad setzt outcomeRendered='1' auf der PiP-Section (HIGH-2) -> Late-Haiku ueberschreibt nicht.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _renderOutcomeUx 3 States PiP-aware (U-01 ROT + NEW-1/F5 pipEl) + outcomeRendered ALLE Branches (HIGH-2) + Confirm-Submit-Lock (MEDIUM-3) + Heading + modal-state node-Test</name>
  <read_first>
    - static/pip-launcher.js:99-104 (pipEl — VERWENDEN statt bare document)
    - static/pip-launcher.js:3660-3846 (_renderOutcomeUx + _decideModalState-Anbindung + confirm-handler —
      PRUEFEN: wo wird outcomeRendered='1' heute gesetzt? Z.3671/3673 bare document! Z.3823 bare document!
      confirm-handler hat KEINEN disabled-Lock?)
    - static/outcome-modal-state.js (komplett — 5-Zustand-Decider)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md "U-01" + "Step 3" (3-State-Tabelle) + Beenden-Misclick-Protection
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md Landmine 5 (correct_outcome ohne Stale-Guard)
  </read_first>
  <behavior>
    test_decide_modal_state.test.js (node:test, require('../static/outcome-modal-state.js')):
    - test_sicher: {source:'ai_auto', confidence:0.95} -> 'sicher'
    - test_unsicher: {source:'ai_auto_unsicher', confidence:0.8} -> 'unsicher'
    - test_kein_versuch: {confidence:0} -> 'kein_versuch'
    - test_final: {source:'user_corrected'} -> 'final'
    - test_conf_zero_beats_source: {source:'ai_auto', confidence:0} -> 'kein_versuch' (Reihenfolge load-bearing)
  </behavior>
  <action>
    A) _decideModalState (outcome-modal-state.js): Die Zustands-Logik bleibt funktional korrekt fuer U-01
    (sie liefert bereits 'sicher'/'unsicher'/'kein_versuch'/'final'). KEINE neue Zustands-Klasse noetig —
    die U-01-Aenderung liegt im RENDER (unten), nicht im Decider. Falls die bestehende Reihenfolge schon
    'final' vor conf===0 prueft (tut sie), unveraendert lassen. Nur sicherstellen, dass der defensive
    Fallback 'unsicher' bleibt.

    B) tests/test_decide_modal_state.test.js NEU anlegen (Datei existiert NICHT — Wave-0-Gap). node:test mit
    `const { _decideModalState } = require('../static/outcome-modal-state.js');` und den 5 behavior-Cases oben.

    C) _renderOutcomeUx (pip-launcher.js:3669) umbauen — PIP-AWARE (NEW-1/F5 BLOCKER-Fix):
    - sec-Lookup PiP-aware: `var sec = pipEl('pip-outcome-section');` (Z.3671 — bare document RAUS).
    - Falls sec fehlt, die Section IM PiP-FENSTER erzeugen: Host und createElement ueber das PiP-Document.
      Konkret (Z.3672-3677-Aequivalent):
      ```javascript
      var _doc = (state.pipWindow && state.pipWindow.document) ? state.pipWindow.document : document;
      if (!sec) {
        var host = pipEl('pip-postcall') || (_doc.querySelector('#nlp-section-postcall')) || _doc.body;
        sec = _doc.createElement('div');
        sec.id = 'pip-outcome-section';
        sec.className = 'n-glass pip-outcome-section';
        host.appendChild(sec);
      }
      ```
      So landet die Outcome-Section im PiP, nicht im Haupt-Fenster (BLOCKER-Wurzel). Alle weiteren Lookups
      innerhalb dieser Funktion (confirmBtn, Buttons-Container, Disclaimer etc.) ueber `sec.querySelector(...)`
      bzw. `_doc`/pipEl — NICHT bare document.
    - Idempotenz-Guard `sec.dataset.outcomeRendered==='1'` am Funktionsanfang BEIBEHALTEN (Landmine 1) — jetzt
      auf der PiP-aufgeloesten sec.
    - HIGH-2 — outcomeRendered='1' auf ALLEN Branches (auf der PiP-sec):
      Setze `sec.dataset.outcomeRendered = '1'` EINMAL zentral, NACHDEM die Section sichtbar/gerendert ist und
      VOR der Branch-spezifischen Vorauswahl — sodass es fuer ALLE drei Branches gilt, INKL. 'kein_versuch'
      (Timeout/No-Attempt). Konkret: direkt nachdem Buttons + Header gerendert sind (vor der
      `_decideModalState`-Branch-Logik) `sec.dataset.outcomeRendered = '1';`. Damit kann ein spaet
      eintreffender outcome_ready-Socket-Emit den bereits gerenderten kein_versuch-Timeout-Screen NICHT mehr
      ueberschreiben (HIGH-2). Den Set NICHT in einen `if (modalState === 'sicher')`-Zweig packen — branch-unabhaengig.
    - Header-Text von 'Gesprächsergebnis bestätigen' auf **'Call wirklich beenden?'** aendern (B-01).
      Klasse pip-outcome-header behalten.
    - 'final'-Zweig (read-only Summary) unveraendert lassen (B-03 Reconnect-Fall) — setzt outcomeRendered
      ebenfalls (faellt unter den zentralen Set oben).
    - State-Variable: `var modalState = _decideModalState(data);`
    - selectedOutcome-Initialwert:
      * 'sicher': selectedOutcome = haikuOutcome (teal Vorauswahl, Confirm sofort aktiv) — wie heute.
      * 'unsicher': selectedOutcome bleibt NULL fuer Confirm-Zwecke (U-01: Bestätigen disabled bis aktiver
        Tap), ABER eine separate Variable `var presetUnsicher = haikuOutcome;` haelt den rot-vorauszuwaehlenden
        Button. NICHT als bestaetigbar behandeln (U-01/U-02).
      * 'kein_versuch': selectedOutcome NULL, kein preset.
    - Disclaimer (nur 'unsicher'): Zeile ueber dem Grid mit Text **'KI unsicher, bitte prüfen'**, Klasse
      `pip-outcome-disclaimer` (Token --btn-danger-text, Task 3), mit Lucide alert-triangle inline-SVG 16px
      stroke #f87171. Den ALTEN Badge 'KI unsicher — bitte wählen' (pip-outcome-badge-unsicher, Teal-Outline)
      fuer den 'unsicher'-Fall ENTFERNEN/ersetzen (U-04 supersedes D.UX.1).
    - Hint (nur 'kein_versuch'): bestehenden 'Automatische Erkennung nicht verfügbar'-Hinweis behalten.
    - 7 Buttons aus _OUTCOME_OPTS (Reihenfolge unveraendert). Pre-Select-Styling:
      * 'sicher' + pair[0]===selectedOutcome -> classList.add('pip-outcome-btn--selected') (teal, wie heute).
      * 'unsicher' + pair[0]===presetUnsicher -> classList.add('pip-outcome-btn--selected-unsicher')
        (rote Vorauswahl, neue Token-Klasse aus Task 3) + alert-triangle-Icon links im Button.
    - Button-Click-Handler (aktiver Tap): deselektiert alle (entfernt BEIDE Klassen
      'pip-outcome-btn--selected' UND 'pip-outcome-btn--selected-unsicher'), setzt geklickten Button auf TEAL
      'pip-outcome-btn--selected', setzt selectedOutcome=pair[0], entfernt die Disclaimer-Zeile (falls
      vorhanden), aktiviert confirmBtn (disabled=false, entfernt 'pip-outcome-btn--disabled'). Das gilt fuer
      ALLE drei States — im 'unsicher'/'kein_versuch'-Fall ist genau das der "bewusste Klick" (U-02).
    - confirmBtn: disabled wenn !selectedOutcome (also disabled bei 'unsicher' UND 'kein_versuch' bis Tap,
      sofort aktiv bei 'sicher'). Label 'Bestätigen'; disabled-Style pip-outcome-btn--disabled.
    - confirm-handler — MEDIUM-3 Submit-Lock (RESEARCH Landmine 5: correct_outcome hat heute KEINEN
      Stale-Guard, Double-Tap -> Double-POST/Double-Commit):
      * SOFORT beim Click, VOR dem fetch-Start: `confirmBtn.disabled = true;` setzen UND visuell sperren
        (`confirmBtn.classList.add('pip-outcome-btn--disabled')`). Optional in-flight-Label
        'Bewertung wird berechnet…' (UI-SPEC). Damit kein zweiter Submit waehrend des laufenden POST.
      * outcome_source-Mapping (conf>=0.90 && sel===haiku -> 'ai_auto'; conf>=0.70 && sel===haiku ->
        'ai_auto_unsicher'; sonst 'user_corrected') BEIBEHALTEN. POST an
        /api/calls/<callId>/correct_outcome wie heute.
      * Im `.catch` (Hard-Error-Pfad): den Button WIEDER FREIGEBEN — `confirmBtn.disabled = false;
        confirmBtn.classList.remove('pip-outcome-btn--disabled');` — und die Hard-Error-Copy auf
        **'Konnte nicht speichern – bitte erneut auf Bestätigen tippen.'** setzen. So kann der User nach
        einem Fehler erneut tippen, aber NICHT waehrend des laufenden POST (kein Double-Submit, MEDIUM-3).
        Bei Erfolg bleibt der Button gesperrt (terminaler Screen, B-03).
      * Nach Erfolg `state.lastOutcome = json.outcome` o.ae. fuer Task 3 verfuegbar machen.
      * HINWEIS (F9/NEW-1): den eigentlichen Score-Reveal + Trend-Feed + Analytics-Render baut Task 3 in
        DIESEN .then-Erfolgspfad ein (alle Lookups PiP-aware via pipEl). Hier in Task 2 nur Submit-Lock +
        Mapping + Fehler-Recovery + state.lastOutcome.

    B-03: KEIN Back-to-Call-Button hinzufuegen (terminal/Forced-Choice).
    UTF-8: alle sichtbaren Strings echte Umlaute ('Call wirklich beenden?', 'KI unsicher, bitte prüfen',
    'Bestätigen', 'Bewertung wird berechnet…', 'Konnte nicht speichern – bitte erneut auf Bestätigen tippen.');
    Klassen/Variablen ASCII (outcomeRendered, presetUnsicher, confirmBtn, pipEl).
  </action>
  <verify>
    <!-- Deploy + Server-Node-Test laufen NICHT pro Task. Sie laufen EINMAL pro Plan via manual-direct-prod (siehe <deploy_verify> unten), orchestrator-gefahren mit Andre-Approval vor jedem tar-Push. KEIN git pull auf Prod. -->
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-03): outcome screen 3 states pipEl (new1/f5) + U-01 red + outcomeRendered all branches (high2) + confirm submit-lock (medium3) + heading + modal test" && git push</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_decide_modal_state.test.js existiert; `node --test` -> alle 5 Cases GRUEN (auf Server)
    - grep `Call wirklich beenden?` static/pip-launcher.js -> 1 Treffer (Heading geaendert, B-01)
    - grep `KI unsicher, bitte prüfen` static/pip-launcher.js -> 1 Treffer (Disclaimer-Copy, U-01)
    - grep `pip-outcome-btn--selected-unsicher` static/pip-launcher.js -> mind. 1 Treffer (rote Vorauswahl)
    - grep `presetUnsicher` static/pip-launcher.js -> Treffer (Vorauswahl gesetzt aber nicht confirmbar)
    - NEW-1/F5 (BLOCKER): in _renderOutcomeUx wird `pip-outcome-section` ueber `pipEl(` aufgeloest und (falls
      neu erzeugt) im PiP-Document erstellt — grep zeigt im _renderOutcomeUx-Block KEIN
      `document.getElementById('pip-outcome-section')` und KEIN `document.querySelector('#nlp-section-postcall')`
      mehr (beide muessen pipEl(...) / state.pipWindow.document sein); grep `pipEl('pip-outcome-section')` -> Treffer
    - HIGH-2: in _renderOutcomeUx wird `sec.dataset.outcomeRendered = '1'` BRANCH-UNABHAENGIG gesetzt (Set-Zeile
      VOR der `if (modalState ===`-Branch-Logik bzw. ausserhalb aller drei state-Zweige; im kein_versuch-Pfad erreicht)
    - MEDIUM-3: confirm-handler setzt `confirmBtn.disabled = true` VOR dem correct_outcome-fetch
      (grep `confirmBtn.disabled = true` -> Treffer im confirm-handler) UND gibt im `.catch` wieder frei
      (grep `confirmBtn.disabled = false` -> Treffer im catch-Block)
    - grep `Konnte nicht speichern` static/pip-launcher.js -> 1 Treffer (Hard-Error-Copy, im catch mit Re-Enable)
    - grep `Gesprächsergebnis bestätigen` static/pip-launcher.js -> 0 Treffer (alter Heading ersetzt)
    - 'sicher'-Fall: confirmBtn NICHT disabled initial; 'unsicher'/'kein_versuch': confirmBtn disabled bis Tap (Code-Pfad)
    - node --check static/pip-launcher.js ok
  </acceptance_criteria>
  <done>Outcome-Screen PiP-aware (pipEl, BLOCKER-Fix NEW-1/F5) mit Heading 'Call wirklich beenden?', 3 States (teal/rot/neutral), U-01 rote Vorauswahl + Disclaimer + Confirm-disabled-bis-Tap, outcomeRendered='1' auf ALLEN Branches inkl. kein_versuch (HIGH-2), confirm-handler Submit-Lock (disabled vor POST, re-enable im catch, MEDIUM-3), modal-state node-Test gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Score+Analytics S-03 sofort PiP-aware + null-safe (NEW-1) + Score-Hide-Owner + Trend mit final_score (F9) + _calcScore aus Render entfernt (L-01) + CSS (KEINE Ladebalken-2-Klasse, Option 3)</name>
  <read_first>
    - static/pip-launcher.js:99-104 (pipEl — VERWENDEN statt bare document)
    - static/pip-launcher.js:2982-3054 (_calcScore + _showPostcall — L-01-Entfernung + Z.3045 bare-document-Hide + Z.3053 _fetchAndRenderTrend(score) F9-Feed), confirm-handler 3776-3845 (Z.3823 bare-document-Reveal)
    - static/nerve.css:85-95 (--btn-danger-*), 130-139 (--kpi-*), 210-214 (--session-score-*), 3319-3330 (pip-outcome-btn--selected/--disabled), @keyframes n-pulse
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md "Step 4/Step 5" (Panel-Order) + "Color" (Token-Bindung)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md "Basis-Analytics fuer S-03" (Feldnamen)
  </read_first>
  <action>
    A) L-01 — _calcScore aus dem Score-Render entfernen. In _showPostcall (pip-launcher.js:3047-3049):
    den `var score = _calcScore(postcall); ... _showPostcallRaw(score + '%', tags);` so umbauen, dass beim
    initialen Render KEIN vorlaeufiger Score gesetzt wird (der Score kommt ausschliesslich aus der
    correct_outcome-Response nach Confirm). `_calcScore`-Funktion selbst darf als Datei-Funktion bleiben
    (NICHT zwingend loeschen), aber sie DARF NICHT mehr in den angezeigten Score fliessen. Wenn _showPostcall
    weiterhin fuer den finalen Render nach Confirm genutzt wird, den score-Parameter aus der
    correct_outcome-Response (json.final_score) durchreichen statt _calcScore. _buildTags/_renderQuickStats/
    _renderSparkline duerfen bleiben. WICHTIG (F9): der bisherige `_fetchAndRenderTrend(score)`-Aufruf
    (Z.3053) mit dem _calcScore-Wert DARF im Render-Pfad NICHT mehr mit dem _calcScore-Score gefuettert
    werden (siehe B — Feed mit final_score im Confirm-Handler).

    A2) F9 — SCORE-HIDE-OWNER. Da `_showPostcall` am Beenden NICHT mehr aufgerufen wird (Task 1), feuert der
    bisherige "Score-Sektion bis Confirm verstecken"-Hide aus _showPostcall:3045-3046 nicht mehr automatisch.
    Stelle explizit sicher, wer die postcall-Section bis zum Confirm versteckt haelt:
    - Der Ladebalken-1-View (Task 1 `_showLadebalken1`) und der Outcome-Screen-Zustand MUESSEN
      `pipEl('nlp-section-postcall')` auf `display:'none'` lassen/setzen (PiP-aware, NICHT bare document).
      Konkret: beim Betreten von Ladebalken 1 (Task 1) bzw. spaetestens beim Render des Outcome-Screens
      `var _sps = pipEl('nlp-section-postcall'); if (_sps) _sps.style.display = 'none';`.
    - NUR der confirm-handler (B unten) deckt die Section wieder auf. Das ist der EINZIGE Ort, der die
      postcall-Section sichtbar macht.
    - Falls _showPostcall den Z.3045-Hide weiterhin enthaelt, diesen Lookup von bare
      `document.querySelector('#nlp-section-postcall')` auf `pipEl('nlp-section-postcall')` umstellen (F5/NEW-1).

    B) S-03 — Score-Karte + Basis-Analytics ZUSAMMEN nach Confirm, PiP-AWARE + NULL-SAFE (Option 3: KEINE
    Karten, KEIN Ladebalken 2). Im confirm-handler von _renderOutcomeUx (nach erfolgreicher
    correct_outcome-Response, Task 2 hat state.lastOutcome gesetzt):
    - Score-Section aufdecken PiP-aware: `var _scoreSec = pipEl('nlp-section-postcall'); if (_scoreSec)
      _scoreSec.style.display = '';` (Z.3823 — bare document RAUS). Dies ist der EINZIGE Reveal (F9).
    - Score-Wert aus `json.final_score` rendern (S-01, kein _calcScore), Ziel-Element via pipEl
      (`pipEl('nlp-postcall-score')` o.ae.). Farbe nach Band: >=70 var(--session-score-high), >=40
      var(--session-score-mid), sonst var(--session-score-low). Label 'Coaching-Score'. Der Score-Render
      haengt NICHT von pendingPostcall ab — er kommt allein aus json.final_score und MUSS immer erscheinen.
    - TREND mit final_score fuettern (F9): `_fetchAndRenderTrend(json.final_score);` — NICHT mit dem alten
      _calcScore-Wert. Es darf KEIN `_fetchAndRenderTrend(score)` mit dem _calcScore-Score im Render-Pfad
      verbleiben.
    - Basis-Analytics aus `state.pendingPostcall` (in Task 1 gespeichert) SOFORT zusammen rendern, PiP-aware.
      KRITISCH (BLOCKER, null-safe): `state.pendingPostcall` kann am Confirm-Zeitpunkt undefined/null sein
      (9s-Timeout-Pfad, outcome_ready-Socket-Race, oder stale-guard-`return` im /api/beenden-.then). Der
      KPI-Block MUSS explizit null-guarded sein und darf NIE werfen. Guard-Pattern (genau so oder aequivalent):
      ```javascript
      var _pp = state.pendingPostcall;
      if (_pp) {
        _renderKpiTiles({
          kaufbereitschaft: _pp.kb_end,
          redeanteil: _pp.redeanteil_berater,        // %
          einwaende: (_pp.einwaende || []).length
        });
      } else {
        // Fallback: Score-Karte allein, KPI-Block leer/uebersprungen — KEIN Throw, KEIN undefined-Access.
        _clearKpiTiles && _clearKpiTiles();
      }
      ```
      Drei KPI-Tiles (.n-kpi/.n-kpi-value/.n-kpi-label) im PiP-Document gerendert (pipEl-aufgeloeste Container):
      Kaufbereitschaft = pendingPostcall.kb_end; Redeanteil = pendingPostcall.redeanteil_berater (%);
      Einwände = (pendingPostcall.einwaende||[]).length. Panel-Heading 'Auswertung'. (Bestehende
      _renderQuickStats kann wiederverwendet/angepasst werden, solange KB/Redeanteil/Einwände-Tiles zusammen
      mit dem Score erscheinen UND der Aufruf null-safe gewrappt ist + PiP-aware rendert.)
    - KEINE Animation (S-01).

    C) OPTION 3 (Andre 2026-05-31) — KEIN Ladebalken 2, KEINE Lernkarten im PiP. ENTFERNEN/NICHT BAUEN:
    - KEIN post-confirm `/api/postcall_cards`-Trigger im confirm-handler (der Cards-Trigger lebt jetzt im
      Hintergrund am Beenden, Task 1 / Plan 02; Response wird verworfen).
    - KEIN Spinner-Block 'Detail-Auswertung läuft…' / `pip-loading-spinner-2` im PiP.
    - KEINE in-PiP Lernkarten-Render-Logik, KEIN '-> Dashboard'-CTA.
    Nach dem Confirm zeigt der PiP NUR: Score + Basis-Analytics (Kaufbereitschaft / Redeanteil / Einwaende).
    Begruendung: Option 3 superseded den "Ladebalken 2 in PiP"-Teil von S-03/LB-04 (Andre 2026-05-31). Die
    Sonnet-Analyse selbst bleibt — sie laeuft im Hintergrund (Task 1 / Plan 02) und persistiert LearningCards.

    D) CSS (static/nerve.css) — NUR Token-gebundene Ergaenzungen, KEINE -spinner-2-Klasse (Option 3):
    - `.pip-outcome-btn--selected-unsicher` (neben .pip-outcome-btn--selected): border:1.5px solid
      var(--btn-danger-text); background:var(--btn-danger-bg); color:var(--btn-danger-text); font-weight:600.
      KEIN ad-hoc Hex (Punkt 8).
    - `.pip-outcome-disclaimer`: color:var(--btn-danger-text); font-size:12px; font-weight:600; display:flex;
      align-items:center; gap:4px. Kein Background.
    - `.pip-beenden-holding`: rote Progress-Wipe-Optik fuer den 600ms-Hold (z.B. background:linear-gradient
      mit var(--btn-danger-text), KEIN Teal, KEIN ad-hoc Hex; nutze die danger-Tokens).
    - `.pip-loading-spinner` (NUR Ladebalken 1): unbestimmter Spinner/Pulse via bestehender @keyframes n-pulse
      (oder vorhandener pip-score-spinner-Animation). KEIN Prozentbalken. KEINE `.pip-loading-spinner-2`-Klasse
      anlegen (Option 3 — kein zweiter Ladebalken).
    Alle neuen Klassen ASCII; nur var(--...)-Tokens, keine neuen Hex-Werte.

    UTF-8: 'Coaching-Score', 'Auswertung', 'Kaufbereitschaft', 'Redeanteil', 'Einwände' user-facing mit
    Umlauten; Klassen/Variablen ASCII. (KEIN 'Detail-Auswertung läuft…'-String mehr — Option 3.)
  </action>
  <verify>
    <!-- Deploy + Server-Node-Test laufen NICHT pro Task. Sie laufen EINMAL pro Plan via manual-direct-prod (siehe <deploy_verify> unten), orchestrator-gefahren mit Andre-Approval vor jedem tar-Push. KEIN git pull auf Prod. -->
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-03): score+analytics S-03 instant pipEl null-safe + score-hide-owner + trend final_score (f9) + remove _calcScore from render + css (no ladebalken2)" && git push && node --check static/pip-launcher.js</automated>
    <!-- VERIFY-GAP (bewusste Entscheidung, WARNING 2): Die confirm-handler-Laufzeit (Score-Reveal im PiP,
    null-safe KPI-Render, PiP-Fenster-Bindung) ist DOM-Verhalten und headless nicht sinnvoll testbar.
    `node --check` deckt Parse-Fehler + die statische Praesenz des null-Guards + pipEl-Nutzung (grep unten)
    ab. Das tatsaechliche Render-Verhalten (inkl. pendingPostcall-Miss-Fall + Score-im-PiP-sichtbar) wird im
    PFLICHT-Post-Execute-Schritt validiert: Cross-AI Peer-Review + GSD Code-Review + Live-UAT auf Production
    (CLAUDE.md Punkt 7, 🔴-Phase). Dokumentierte Entscheidung, keine Auslassung. -->
  </verify>
  <acceptance_criteria>
    - grep `_calcScore(postcall)` static/pip-launcher.js innerhalb _showPostcall-Render-Pfad -> 0 Treffer (L-01: nicht mehr im angezeigten Score)
    - F9: grep `_fetchAndRenderTrend(json.final_score)` static/pip-launcher.js -> 1 Treffer (Trend mit bestaetigtem Score)
    - F9: grep `_fetchAndRenderTrend(score)` (mit dem _calcScore-`score`) im Render-Pfad -> 0 Treffer
    - NEW-1/F5 (BLOCKER): im confirm-handler + _showPostcall wird die postcall-Section ueber `pipEl(` aufgeloest —
      grep `document.querySelector('#nlp-section-postcall')` static/pip-launcher.js -> 0 Treffer (alle pipEl);
      grep `document.getElementById('pip-outcome-section')` static/pip-launcher.js -> 0 Treffer in den von
      diesem Plan editierten Funktionen (alle pipEl)
    - F9 Score-Hide-Owner: der confirm-handler ist der EINZIGE Ort, der die postcall-Section auf display sichtbar
      setzt — grep zeigt `_scoreSec.style.display = ''` (o.ae. Reveal) nur im confirm-handler; Ladebalken-1/
      Outcome-Screen setzen sie auf 'none' (Code-Pfad)
    - OPTION 3: grep `postcall_cards` im confirm-handler/_renderOutcomeUx-Block -> 0 Treffer (Cards nur im
      Hintergrund am Beenden, Task 1)
    - OPTION 3: grep `Detail-Auswertung läuft` static/pip-launcher.js -> 0 Treffer (kein Ladebalken 2 im PiP)
    - OPTION 3: grep `pip-loading-spinner-2` static/nerve.css -> 0 Treffer (Klasse existiert nicht)
    - grep `Coaching-Score` static/pip-launcher.js -> 1 Treffer
    - grep `Auswertung` + `Kaufbereitschaft` + `Einwände` static/pip-launcher.js -> je Treffer (S-03 Tiles)
    - grep `state.pendingPostcall` static/pip-launcher.js -> Read im confirm-handler (Analytics-Quelle)
    - NULL-SAFETY (BLOCKER): grep zeigt im confirm-handler einen expliziten Guard um den KPI-Render —
      ENTWEDER `if (state.pendingPostcall)` / `if (_pp)` (truthy-Wrap) ODER `if (!state.pendingPostcall)`
      (early-skip). Der KPI-Render darf NICHT ungeguarded `state.pendingPostcall.kb_end` o.ae. dereferenzieren
      -> grep `state.pendingPostcall.kb_end` ohne vorausgehenden Guard = FAIL.
    - Score-Render haengt NICHT an pendingPostcall: json.final_score wird auch ohne pendingPostcall gerendert (Code-Pfad)
    - grep `pip-outcome-btn--selected-unsicher` static/nerve.css -> 1 Treffer (Token-only, var(--btn-danger-text))
    - grep `pip-outcome-disclaimer` static/nerve.css -> 1 Treffer
    - grep `pip-loading-spinner` static/nerve.css -> Treffer (Ladebalken 1, ohne -2-Variante)
    - In den neuen CSS-Klassen KEIN roher Hex ausser ueber var() — grep `#f8` / `#00d` in neuen Bloecken -> 0 (nur Tokens)
  </acceptance_criteria>
  <done>Score (aus final_score) erscheint immer aus correct_outcome im PiP (pipEl, BLOCKER-Fix); Basis-Analytics erscheinen SOFORT zusammen wenn pendingPostcall vorhanden, sonst Score allein ohne Throw (null-safe); Score-Hide-Owner ist der Ladebalken-1/Outcome-Screen-Zustand, Reveal NUR im confirm-handler (F9); Trend mit json.final_score (F9); KEINE Lernkarten + KEIN Ladebalken 2 im PiP (Option 3); _calcScore raus aus Render-Pfad (L-01); CSS token-gebunden, kein Gelb/ad-hoc-Hex, keine -spinner-2-Klasse.</done>
</task>

</tasks>

<deploy_verify>
## Manual-Direct-Prod Deploy (orchestrator-run, Andre-Approval VOR jedem tar-Push)

Staging-Gate bewusst uebersprungen (Staging-Boot-Bug, wie D.UX.1). KEINE Migration in dieser Phase — reiner Code-Push + systemctl restart. Production-git wird NICHT angefasst (tar excludet .git). Plan 03 hat KEIN pytest-Target; der server-seitige Check ist der node-Test (test_decide_modal_state.test.js). Die DOM/Pointer-Laufzeit (Hold, 9s-Timeout, PiP-Render) ist headless nicht testbar — sie wird durch Andres Live-UAT validiert (VERIFY-GAP-Notizen pro Task).

**Schritt 1 — tar-over-ssh (excludes wie deploy.sh Z.85-103):**
```bash
tar --exclude='./.git' --exclude='./.gitignore' --exclude='./.env' --exclude='./.planning' --exclude='./.claude' --exclude='./node_modules' --exclude='./logs' --exclude='./deploy' --exclude='./deploy.sh' --exclude='*.pyc' --exclude='__pycache__' --exclude='salesnerve_log_*.txt' --exclude='*.db-journal' --exclude='*.db-wal' --exclude='*.db-shm' --exclude='./database/*.db' --exclude='./database/salesnerve.db' -cf - ./ \
  | ssh -i ~/.ssh/nerve_vps root@178.104.82.166 "tar -xf - -C /opt/nerve/app --no-same-owner"
```

**Schritt 2 — Ownership + Server-Node-Test + Restart:**
```bash
ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'chown -R nerve_app:nerve_app /opt/nerve/app/logs /opt/nerve/app/database 2>/dev/null; cd /opt/nerve/app && node --test tests/test_decide_modal_state.test.js && sudo systemctl restart nerve && sudo systemctl status nerve --no-pager -l | head -5'
```

KEIN pytest in dieser Phase (reine FE/CSS-Plan-03). KEIN `git pull`, KEINE Prod-git-Operation, KEINE Migration. Die testbare Outcome-Entscheidung (sicher/unsicher/kein_versuch) deckt der node-Test ab; das restliche Runtime-Verhalten validiert Andres Live-UAT nach Deploy.
</deploy_verify>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PiP-Client -> /api/postcall_outcome, /api/postcall_cards, /api/correct_outcome | Frontend sendet conv_id/call_id/outcome; Server validiert Ownership + VALID_OUTCOMES (Plan 01/02) |
| Late async callback -> bereits neue Session | endCallGen-Generation-Guard schuetzt gegen stale Render in neuem Call |
| PiP-Fenster vs. Haupt-Fenster (zwei DOM-Dokumente) | pipEl() loest im PiP-Document auf; bare document trifft das falsche Fenster (BLOCKER NEW-1/F5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UX4-07 | Tampering | DOM-injizierte Outcome-Labels | mitigate | Labels aus statischem _OUTCOME_OPTS via escHtml() rendern (bestehendes Pattern beibehalten) |
| T-UX4-08 | Denial of Service | Slow/haengender Haiku | mitigate | 9s-Timeout (LB-02) -> Outcome-Screen ohne Vorauswahl; kein ewiges Warten (LB-03 abgelehnt) |
| T-UX4-09 | Tampering | Stale async Render in neuer Session | mitigate | endCallGen-Guard auf lb1Timeout + postcall_outcome-.then + haikuTimedOut-Flag (Landmine 1/2) |
| T-UX4-10 | Repudiation | Doppel-endCall durch Click+Hold | mitigate | Single-Click-Zweig entfernt, holdFired-Flag, rebuild-sichere Pointer-Delegation (Landmine 7, F10) |
| T-UX4-11 | Denial of Service | KPI-Render wirft bei pendingPostcall-Miss (Timeout/Race) -> Score-Screen broken | mitigate | Null-Guard um KPI-Block (Task 3 B); Score rendert unabhaengig aus final_score; kein undefined-Deref |
| T-UX4-13 | Tampering | Late-Haiku-Socket ueberschreibt Timeout-kein_versuch-Screen (HIGH-2) | mitigate | Timeout-Pfad + _renderOutcomeUx setzen outcomeRendered='1' branch-unabhaengig auf der PiP-Section -> Idempotenz-Guard blockt Late-Emit (Task 1 + Task 2 C) |
| T-UX4-14 | Repudiation | Double-Submit auf correct_outcome durch ungeduldigen Confirm-Doppel-Tap (MEDIUM-3) | mitigate | confirmBtn.disabled=true vor POST, re-enable nur im .catch (Task 2 C); bei Erfolg terminal gesperrt |
| T-UX4-15 | Information Disclosure / DoS | Score+Analytics landen via bare document im FALSCHEN Fenster (Haupt statt PiP) -> nach Confirm unsichtbar (BLOCKER NEW-1/F5) | mitigate | Alle Element-Lookups + Section-Erzeugung im Reorder-/Outcome-/Score-Pfad ueber pipEl() / state.pipWindow.document (Task 1+2+3); grep-Acceptance: kein bare document.querySelector('#nlp-section-postcall') / document.getElementById('pip-outcome-section') in editierten Funktionen |
| T-UX4-16 | Information Disclosure (Daten-Regression) | User bricht Outcome-Screen ab -> Sonnet-Lernkarten wuerden bei Confirm-gekoppelter Generierung nie persistieren (0 LearningCards) | mitigate | postcall_cards laeuft im Hintergrund AM BEENDEN unabhaengig vom Confirm (Task 1 / Plan 02); idempotente Persistenz in coaching_service (duplicate-guard) (NEW-2) |
| T-UX4-17 | Tampering | pendingPostcall/lastCallId leckt in den naechsten Call (NEW-5) | mitigate | Reset-/Teardown-Pfad nullt state.pendingPostcall + state.lastCallId (Task 1 C) |

> Reine Frontend-/CSS-Phase ohne neuen Daten-Persistenz-Pfad — keine `## 5. Persistenz-Schicht-Verifikation`
> noetig (alle DB-Schreibwege liegen in Plan 01/02; dieser Plan ruft nur deren Endpoints auf). Die
> konsumierte correct_outcome-Response + die zwei neuen Endpoints sind in Plan 01/02 schema-verifiziert.
> Der background postcall_cards-Trigger (NEW-2) ruft nur den Plan-02-Endpoint auf — die LearningCard-
> Persistenz liegt unveraendert in coaching_service (services/coaching_service.py:115-130, idempotenter
> duplicate-guard 60-68), kein neuer Schreibpfad in diesem Plan.

## Bekanntes Rest-Risiko (kein Stopp)

**VER-HIGH1-Caveat (Backlog-Kandidat, kein Regress durch diese Phase):** Bei Kaltakquise ohne
Sprecher-Trennung wird Redeanteil als 0 gespeichert -> Redeanteil-Score 20 statt 80. **Identisch zu heute**
(diese Phase aendert die Score-Berechnung nicht, nur die Reihenfolge der Anzeige). Das Score-Gate erkennt
die 0 nicht als Problem. Reine Notiz fuer den Backlog, kein Blocker dieser Phase.
</threat_model>

## KNOWN-GAP (Andre 2026-05-31 — bewusst dokumentiert, kein Regress dieser Phase)

**Per-Call Sonnet-Lernkarten haben aktuell KEINE per-Call user-facing Anzeige.**
- `coaching_service.generate_postcall_analysis` (services/coaching_service.py:49-141) PERSISTIERT die
  Sonnet-Vorschlaege als `LearningCard`-Rows (Z.115-130, `status='vorschlag'`, keyed `call_id=conv_id`),
  mit idempotentem duplicate-guard (Z.60-68). Das laeuft (nach diesem Plan) im Hintergrund ab Beenden.
- Diese per-Call-Karten werden NIRGENDS user-facing per Call angezeigt:
  - pip-launcher.js rendert `vorschlaege` NICHT (Option 3 — bewusst auch nach dieser Phase nicht).
  - `session_detail.html` "Lernkarten"-Sektion ist ein **"Kommt mit Phase 4.11"-Platzhalter**.
  - `dashboard.html` lernkarten-section ist das SEPARATE activated-cards-Feature (`get_active_cards`,
    status='aktiv') — NICHT die per-Call-Vorschlaege.
- **Folge:** Die Karten werden generiert + persistiert (gut fuer Phase-4.11/Backlog-999.2-Display +
  das spaetere Aktivieren), aber ein User sieht sie pro Call heute nirgends. Das ist **kein Regress dieser
  Phase** (der PiP hat sie nie gezeigt; session_detail war schon Platzhalter), sondern ein bestehender Gap.
- **Flag fuer Andre / Future-Design:** per-Call-Lernkarten-Anzeige ist offenes Design (Phase 4.11 /
  Backlog 999.2). Dieser Plan macht den Gap explizit, damit er nicht still verschwindet.

<verification>
- node --test tests/test_decide_modal_state.test.js (Production-Server via <deploy_verify>, kein git pull) — 5 Cases gruen
- node --check static/pip-launcher.js (kein Parse-Fehler)
- Grep-Akzeptanz: user-facing Copy-Strings (echte Umlaute) + neue Endpoint-Calls + Token-only-CSS
  + KPI-Null-Guard (BLOCKER) + outcomeRendered branch-unabhaengig (HIGH-2) + confirmBtn-Submit-Lock (MEDIUM-3)
  + pipEl statt bare document im Reorder-/Outcome-/Score-Pfad (NEW-1/F5 BLOCKER) + _fetchAndRenderTrend(final_score)
  (F9) + state.pendingPostcall=null im Reset (NEW-5) + postcall_cards im Beenden-Hintergrund nicht im Confirm
  (NEW-2/Option 3) + keine Detail-Auswertung/-spinner-2 (Option 3)
- Manuelle Live-Verifikation des kompletten Ablaufs (inkl. Score-im-PiP-sichtbar nach Confirm) ist Sache des
  UAT nach Deploy (Cross-AI + Code-Review laut CLAUDE.md Punkt 7 PFLICHT vor Execute dieser Phase). DOM/Pointer-
  Laufzeit von Task 1 + Task 3 ist bewusst nur durch diesen Post-Execute-Schritt validiert (VERIFY-GAP-Notizen).
</verification>

<success_criteria>
- B-02 600ms-Hold beendet Call; Single-Tap nicht (kein Doppel-endCall); Keyboard-2nd-Press-Aequivalent;
  Hold-Bindung ueberlebt PiP-Rebuild (F10)
- Ablauf: Hold -> Mic-Stop + Ladebalken 1 (9s-Timeout) -> Outcome-Screen 'Call wirklich beenden?' (3 States)
  -> Confirm -> Score+Analytics sofort im PiP (KEIN Ladebalken 2, KEINE Karten — Option 3)
- NEW-1/F5 (BLOCKER): Score + Analytics + Outcome-Section werden via pipEl im PiP-Fenster gerendert -> nach
  Confirm im PiP sichtbar (kein bare document mehr in den editierten Funktionen)
- U-01: sicher=teal/sofort, unsicher=rot+Disclaimer+Confirm-disabled-bis-Tap, kein_versuch=neutral+disabled-bis-Tap
- HIGH-2: Timeout/kein_versuch-Screen setzt outcomeRendered='1' auf der PiP-Section -> Late-Haiku ueberschreibt nicht
- MEDIUM-3: confirmBtn waehrend POST disabled, re-enable im catch -> kein Double-Submit
- F9: postcall-Section bleibt bis Confirm versteckt (Owner = Ladebalken-1/Outcome-Screen, Reveal nur im Confirm-Handler);
  Trend wird mit json.final_score gefuettert
- L-01: kein vorlaeufiger _calcScore im Render; Score genau einmal aus final_score
- S-03 (scoped-down): Score + KB/Redeanteil/Einwände zusammen sofort; Score rendert auch wenn pendingPostcall fehlt (null-safe)
- NEW-2/Option 3: Sonnet-Lernkarten-Generierung laeuft im Hintergrund ab Beenden unabhaengig vom Confirm (keine
  Daten-Regression bei Abbruch); im PiP KEINE Karten + KEIN Ladebalken 2
- NEW-5: pendingPostcall + lastCallId im Reset genullt
- Generation-Guard auf allen async-Pfaden; kein Gelb, kein ad-hoc-Hex (Punkt 8)
</success_criteria>

## Review Findings Addressed (Gemini 2026-05-31)

> Deploy/Verify auf manual-direct-prod umgestellt (tar-over-ssh, kein git pull) — Andre 2026-05-31, Staging-Gate uebersprungen, keine Migration.

Dieser Plan ist der FE-Anker fuer die Cross-AI-Findings. Mapping jedes adressierten Findings -> Aenderung -> Stelle:

| Finding | Severity | Was geaendert | Datei:Stelle |
|---|---|---|---|
| HIGH-1 — /api/beenden Aggregations-Timing | HIGH | Backend-seitig in Plan 01 geloest (Code-Trace Aggregate committed Z.322/403 vor Stash Z.603 + [BLOCKING] Task 0 Staging-Real-Daten). Kein FE-Change noetig. | Plan 01 Sektion 5 + Task 0 (siehe dort) |
| HIGH-2 — Timeout-Pfad muss outcomeRendered='1' setzen | HIGH | (a) Timeout-Callback in endCall setzt outcomeRendered='1' direkt auf der PiP-Section (Defence-in-depth). (b) _renderOutcomeUx setzt outcomeRendered='1' BRANCH-UNABHAENGIG vor der state-Branch-Logik, auf der pipEl-aufgeloesten sec, sodass auch der kein_versuch-Branch ihn setzt. Late-Haiku-Socket prallt am Idempotenz-Guard ab. | Plan 03 Task 1 action B (lb1Timeout-Callback) + Task 2 action C (zentraler Set); must_haves.truths #4; T-UX4-13; acceptance_criteria HIGH-2 in Task 1+2 |
| MEDIUM-3 — Submit-Lock auf Confirm-POST | MEDIUM | confirm-handler setzt `confirmBtn.disabled=true` + disabled-Klasse SOFORT beim Click vor dem correct_outcome-fetch; re-enable nur im `.catch` (verdrahtet in den Hard-Error-Pfad mit Copy 'Konnte nicht speichern…'). Kein Double-Submit. | Plan 03 Task 2 action C "confirm-handler — MEDIUM-3 Submit-Lock"; must_haves.truths #7; T-UX4-14; acceptance_criteria MEDIUM-3 in Task 2 |
| MEDIUM-4 — partielle score_breakdown-Reader | MEDIUM | Backend-seitig in Plan 01 verifiziert (einziger Reader = correct_outcome selbst, kein FE-Reader von score_breakdown-Keys). Kein FE-Change. | Plan 01 Task 3 (siehe dort) |
| Headless verify-gap (hold/timeout/DOM) | INFO (WARNING-2, akzeptiert) | Unveraendert akzeptiert; holdFired-Guard bleibt; Timing-Code defensiv; VERIFY-GAP-Notizen in Task 1+3 dokumentieren die bewusste Entscheidung (UAT + Cross-AI deckt ab). | Plan 03 Task 1+3 VERIFY-GAP-Kommentare |
| Shim old-tab outcome_ready cross-talk | INFO-2 (akzeptiert, Cleanup-Debt) | Unveraendert akzeptiert; Monolith-Shim-Debt + Follow-up-Cleanup in Plan 02 Task 2 dokumentiert. | Plan 02 Task 2 "SHIM-DEBT-NOTIZ" |

## Claudian Audit Findings Addressed (2026-05-31)

Pre-Execute-Audit (Punkt 19) — Triangulation zur Gemini-Review. Mapping jedes Audit-Items -> Aenderung -> Stelle:

| Audit-Item | Severity | Was geaendert | Datei:Stelle |
|---|---|---|---|
| NEW-1 — Score/Analytics landen im falschen Fenster (bare document statt pipEl) | BLOCKER | Alle Element-Lookups + Section-Erzeugung im Reorder-/Outcome-/Score-Pfad auf pipEl() / state.pipWindow.document umgestellt: _renderOutcomeUx sec + Host (Task 2 C), _showPostcall-Hide + confirm-handler-Reveal + KPI-Tiles + Score-Element (Task 3 A2/B), Timeout-Callback + Socket-Handler (Task 1 B). Grep-Acceptance: kein bare document.querySelector('#nlp-section-postcall') / document.getElementById('pip-outcome-section') in editierten Funktionen. | Task 1 action B; Task 2 action C; Task 3 action A2/B; T-UX4-15; must_haves.truths #9 |
| F5 — HIGH-2 Schicht-2 falsche ID + falsches Dokument | MEDIUM | Timeout-Defence-in-depth nutzt jetzt `pipEl('pip-outcome-section')` (korrekte ID, PiP-aware) statt `pipWindow.document.getElementById('nlp-outcome-section')`. | Task 1 action B (lb1Timeout-Callback); acceptance_criteria F5/HIGH-2 in Task 1 |
| F9 — Score-Hide-Owner + Trend-Feed | MEDIUM | (a) Score-Hide-Owner explizit: Ladebalken-1/Outcome-Screen halten pipEl('nlp-section-postcall') auf display:none; NUR der confirm-handler deckt auf (einziger Reveal). (b) Trend mit json.final_score gefuettert (_fetchAndRenderTrend(json.final_score)), nicht mehr mit _calcScore-Wert. | Task 3 action A2 + B; acceptance_criteria F9 in Task 3; must_haves.truths #11+#12 |
| F10 — Hold-to-end ueberlebt PiP-Rebuild | MEDIUM | Hold-Logik rebuild-sicher: Pointer-Delegation auf state.pipWindow.document (closest('#nlp-btn-beenden')) ODER Re-Attach bei jedem _showPipLive-Rebuild — kein one-time addEventListener auf dem neu erzeugten Button. Im PiP-Fenster gebunden. | Task 1 action A; acceptance_criteria F10 in Task 1; must_haves.truths #2 |
| NEW-2 — Lernkarten fehlen bei Abbruch (Daten-Regression) | MEDIUM | postcall_cards laeuft im Hintergrund AM BEENDEN (fire-and-forget, Response verworfen), unabhaengig vom Confirm -> LearningCards persistieren auch bei Abbruch. Idempotente Persistenz in coaching_service. | Task 1 action B (background-Trigger); Plan 02 must_haves + Cross-Layer-Tabelle; T-UX4-16; must_haves.truths #13 |
| NEW-3 — Plan beschreibt Lernkarten-Anzeige im PiP die es nicht gibt | MEDIUM | Option 3 (Andre 2026-05-31): KEINE Karten + KEIN Ladebalken 2 im PiP. Cards-Trigger, Spinner-2-Block, in-PiP-Render + .pip-loading-spinner-2-CSS entfernt/nicht gebaut. S-03/LB-04 entsprechend scoped-down (Frontmatter-Kommentar + requirements). | Task 3 action C + D; Frontmatter scope-down; must_haves.truths #13; KNOWN-GAP-Sektion |
| NEW-5 — lastCallId/pendingPostcall lecken in naechsten Call | LOW | Reset-/Teardown-Pfad nullt state.pendingPostcall + state.lastCallId. | Task 1 action C; acceptance_criteria NEW-5 in Task 1; T-UX4-17; must_haves.truths #14 |
| Backlog — Kaltakquise ohne Sprecher-Trennung -> Redeanteil 0 -> Score 20 | INFO (Backlog) | Notiert als bekanntes Rest-Risiko (kein Regress dieser Phase, Score-Berechnung unveraendert). Backlog-Kandidat. | threat_model "Bekanntes Rest-Risiko" |
| KNOWN-GAP — per-Call Sonnet-Karten ohne user-facing Anzeige | INFO (Flag) | Dokumentiert: Karten persistieren (LearningCard status='vorschlag') aber session_detail=Phase-4.11-Platzhalter, kein per-Call-Display. Kein Regress; Flag fuer Andre/Future-Design (Backlog 999.2). | KNOWN-GAP-Sektion |

<output>
Nach Abschluss: `.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-03-SUMMARY.md`
</output>
