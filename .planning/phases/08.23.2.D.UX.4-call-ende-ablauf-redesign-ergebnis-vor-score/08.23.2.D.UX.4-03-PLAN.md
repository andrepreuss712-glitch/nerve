---
phase: 08.23.2.D.UX.4
plan: 03
type: execute
wave: 2
depends_on: ["08.23.2.D.UX.4-01", "08.23.2.D.UX.4-02"]
files_modified:
  - static/pip-launcher.js
  - static/nerve.css
  - static/outcome-modal-state.js
  - tests/test_decide_modal_state.test.js
autonomous: true
complexity: medium
requirements: [L-01, L-02, L-03, L-04, U-01, U-02, U-03, U-04, B-01, B-02, B-03, B-04, LB-01, LB-02, LB-03, LB-04, S-01, S-03]
must_haves:
  truths:
    - "Beenden-Knopf endet den Call NUR nach 600ms Press-and-Hold; einzelner Tap/Klick beendet nicht (B-02)"
    - "Nach Hold-Complete: Mic stoppt sofort, dann Ladebalken 1 'KI wertet das Gespraech aus…' (unbestimmter Spinner, kein Prozentbalken) (LB-01)"
    - "Ladebalken 1 hat 9s-Timeout -> Outcome-Screen ohne Vorauswahl (kein_versuch); Late-Haiku ueberschreibt den Timeout-Screen NICHT (LB-02, Generation-Guard)"
    - "Outcome-Screen-Heading ist 'Call wirklich beenden?'; der Screen IST die Bestaetigung, kein separater Ja/Nein-Dialog (B-01); terminal, kein Back-to-Call (B-03)"
    - "Sichere KI (ai_auto): teal Vorauswahl + Bestaetigen sofort klickbar; Unsichere KI (ai_auto_unsicher): ROTE Vorauswahl + 'KI unsicher, bitte pruefen' + Bestaetigen disabled bis aktiver Tap (U-01)"
    - "Score erscheint sofort nach Confirm ohne Animation, EINMAL, aus correct_outcome-Response (S-01); kein vorlaeufiger _calcScore mehr im Render-Pfad (L-01)"
    - "Score-Karte + Basis-Analytics (Kaufbereitschaft, Redeanteil, Einwaende) erscheinen ZUSAMMEN sofort; nur die Sonnet-Lernkarten bekommen Ladebalken 2 'Detail-Auswertung laeuft…' (S-03, L-04)"
  artifacts:
    - path: "static/pip-launcher.js"
      provides: "endCall-Reorder, Hold-to-end, Ladebalken 1+2, 9s-Timeout mit Generation-Guard, _renderOutcomeUx 3 States, _calcScore aus Render entfernt, S-03 Panel-Order, neue Endpoint-Calls"
      contains: "Halten zum Beenden"
    - path: "static/outcome-modal-state.js"
      provides: "_decideModalState mit U-01-konformem 'unsicher' (Vorauswahl gesetzt aber Confirm-disabled-Semantik im Render)"
      contains: "_decideModalState"
    - path: "static/nerve.css"
      provides: "pip-outcome-btn--selected-unsicher (rote Vorauswahl, Token-only) + Ladebalken-Spinner-Klassen"
      contains: "pip-outcome-btn--selected-unsicher"
    - path: "tests/test_decide_modal_state.test.js"
      provides: "node:test fuer _decideModalState inkl. U-01-Faelle (sicher/unsicher/kein_versuch/final)"
      contains: "_decideModalState"
  key_links:
    - from: "static/pip-launcher.js:_wirePipButtons"
      to: "endCall via 600ms pointer-hold"
      via: "pointerdown/up/leave/cancel + setTimeout(600)"
      pattern: "Halten zum Beenden"
    - from: "static/pip-launcher.js:endCall"
      to: "/api/postcall_outcome (Haiku) + 9s timeout"
      via: "fetch + endCallGen stale-guard"
      pattern: "postcall_outcome"
    - from: "static/pip-launcher.js:confirm-handler"
      to: "/api/postcall_cards (Sonnet, Ladebalken 2)"
      via: "fetch nach correct_outcome-Erfolg"
      pattern: "postcall_cards"
---

<objective>
Frontend-Reihenfolge-Umbau (B-01/L-04/U-01/S-03 + B-02 Hold). Heute zeigt `endCall()` einen vorlaeufigen
Frontend-Score (`_calcScore`, L-01-Bug), wartet auf den langsamen Sonnet (im alten Monolith), und der
Outcome-Screen erscheint erst danach. Dieser Plan baut die Sequenz im PiP um:

Beenden (600ms Hold, B-02) -> Mic-Stop + Ladebalken 1 "KI wertet das Gespraech aus…" (LB-01, 9s-Timeout LB-02)
-> Outcome-Screen "Call wirklich beenden?" mit 3 Vorauswahl-States (U-01) -> Confirm -> Score + Basis-Analytics
SOFORT zusammen (S-01/S-03) -> Ladebalken 2 "Detail-Auswertung laeuft…" nur fuer Sonnet-Lernkarten (L-04).

Konsumiert die zwei Backend-Endpoints aus Plan 02 (`/api/postcall_outcome` schnell, `/api/postcall_cards`
post-confirm) und die unveraenderte `correct_outcome`-Response aus Plan 01 (S-01). Entfernt `_calcScore`
aus dem Score-Render-Pfad (L-01). Alle async-Pfade tragen den `endCallGen`-Generation-Guard (Race-Schutz).

Purpose: Outcome-vor-Score, Score genau einmal, ehrliches Loading. Output: umgebaute pip-launcher.js,
token-gebundene CSS-Ergaenzungen, U-01-erweiterte modal-state-Logik + node-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-CONTEXT.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md

<interfaces>
<!-- Aus pip-launcher.js + outcome-modal-state.js + nerve.css extrahiert. Direkt verwenden. -->

Beenden-Delegation (pip-launcher.js:1784-1795) — heute Single-Click:
```javascript
pipWindow.document.addEventListener('click', function (ev) {
  var beenden = t.closest('#nlp-btn-beenden');
  if (beenden) { ev.preventDefault(); ev.stopPropagation(); endCall(); return; }
  ...
```

endCall (2876) heute: _stopTimer/_stopMic/_resetLiveState/_showPostcallLoading,
endCallGen = (state.callGen = (state.callGen||0)+1), fetch /api/beenden ->
.then(data) { stale-guard `if (state.callGen !== endCallGen || state.micStarted) return;`
state.lastConvId=data.conv_id; state.lastCallId=data.call_id; _showPostcall(data.postcall);
dann fetch /api/postcall_analysis -> .then(paResult) _renderOutcomeUx(...) }.

_calcScore (2982): vorlaeufiger FE-Score. EINZIGER Caller = _showPostcall:3047. -> aus Render entfernen.

_showPostcall (3020): hasConversation-Guard, versteckt #nlp-section-postcall, ruft _calcScore + _buildTags
+ _showPostcallRaw + _renderQuickStats + _renderSparkline + _fetchAndRenderTrend.

outcome_ready Socket-Handler (2444): if d.call_id mismatch -> ignore; sonst _renderOutcomeUx(d).

_decideModalState (outcome-modal-state.js): 5 Zustaende. Aktuell:
  source==='user_corrected' -> 'final'; conf===0 -> 'kein_versuch'; source==='ai_auto' -> 'sicher';
  source==='ai_auto_unsicher' -> 'unsicher'; sonst 'unsicher'.

_renderOutcomeUx (3669): Idempotenz-Guard sec.dataset.outcomeRendered==='1'; Header-Text heute
'Gesprächsergebnis bestätigen'; selectedOutcome nur bei 'sicher' gesetzt; Badge 'KI unsicher — bitte wählen'
(Teal-Outline) bei 'unsicher'; Hint 'Automatische Erkennung nicht verfügbar' bei 'kein_versuch';
7 Buttons aus _OUTCOME_OPTS; confirmBtn disabled wenn !selectedOutcome; confirm-handler POSTet
/api/calls/<callId>/correct_outcome, rendert json.final_score+'%' in #nlp-postcall-score, deckt
#nlp-section-postcall auf, kollabiert sec zu '✓ Label'.

_OUTCOME_OPTS (3639): [['meeting_booked','Termin gebucht'],['callback','Rückruf vereinbart'],
['send_info','Infos zusenden'],['wrong_person','Falsche Person'],['gatekeeper_blocked','Gatekeeper blockt'],
['no_interest','Kein Interesse'],['contract_signed','Abschluss']].

correct_outcome-Response (Plan 01, unveraendert): {ok, coaching_score, final_score(int), outcome,
followup_intent, note_dropped?}. Frontend rendert json.final_score+'%'.

NEUE Endpoints (Plan 02):
- POST /api/postcall_outcome  Body wie heute /api/postcall_analysis (conv_id, call_id, einwaende...);
  Response {outcome, confidence, source, call_id}. SCHNELL (Haiku, kein Sonnet).
- POST /api/postcall_cards    Body identisch; Response {vorschlaege}. NUR nach Confirm triggern.

nerve.css bestehende Tokens (verifiziert): --btn-danger-bg rgba(248,113,113,0.1), --btn-danger-text #f87171,
--btn-danger-border rgba(248,113,113,0.2); .pip-outcome-btn--selected (teal), .pip-outcome-btn--disabled
(opacity .45, not-allowed); --session-score-high #00D4AA / --session-score-mid #6B7280 /
--session-score-low #f87171; .n-kpi-value (28px JetBrains Mono), .n-kpi-label (11px). @keyframes n-pulse.

UI-SPEC Copy-Contract (echte Umlaute, user-facing):
  rest 'Beenden' | holding 'Halten zum Beenden…' | keyboard 'Nochmal drücken zum Beenden' |
  Ladebalken1 'KI wertet das Gespräch aus…' | Heading 'Call wirklich beenden?' |
  Disclaimer 'KI unsicher, bitte prüfen' | CTA 'Bestätigen' | in-flight 'Bewertung wird berechnet…' |
  Score-Label 'Coaching-Score' | Analytics-Heading 'Auswertung' |
  Tiles 'Kaufbereitschaft' · 'Redeanteil' · 'Einwände' | Ladebalken2 'Detail-Auswertung läuft…' |
  Hard-Error 'Konnte nicht speichern – bitte erneut auf Bestätigen tippen.'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Beenden Hold-to-end (B-02) + endCall-Reorder + Ladebalken 1 + 9s-Timeout mit Generation-Guard</name>
  <read_first>
    - static/pip-launcher.js:1780-1810 (Beenden-Click-Delegation, B-02-Umbaupunkt)
    - static/pip-launcher.js:2876-2979 (endCall — Reorder-Kern), 3225-3263 (_showPostcallLoading-Basis)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md Sektionen "B-02" + "Step 1/Step 2"
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md "Ladebalken-1 Timeout" + Landmines 1,2,7
  </read_first>
  <action>
    A) HOLD-TO-END (B-02, UI-SPEC: Placement-Isolation + 600ms Hold, kein Dialog). In _wirePipButtons
    (pip-launcher.js:1784): den `t.closest('#nlp-btn-beenden')`-Click-Zweig so umbauen, dass ein einzelner
    Click NICHT mehr endCall() ausloest. Stattdessen pointer-basierte Hold-Logik auf #nlp-btn-beenden:
    - pointerdown: Hold-Timer `var holdTimer = setTimeout(function(){ ... endCall() ... }, 600)` starten;
      Label auf 'Halten zum Beenden…' setzen; rote Progress-Wipe-Klasse hinzufuegen (CSS aus Task 3:
      `pip-beenden-holding`). Bei 600ms: kurz solid-hover-Flash (~120ms), dann `try { endCall(); } catch...`.
      Doppel-Trigger-Schutz (Landmine 7): ein `holdFired`-Flag, damit pointerup nach Complete nicht erneut feuert.
    - pointerup / pointerleave / pointercancel VOR 600ms: clearTimeout(holdTimer), Label zurueck auf 'Beenden',
      Wipe-Klasse entfernen, KEIN endCall.
    - Keyboard-Aequivalent (UI-SPEC): Enter/Space auf fokussiertem Beenden zeigt inline 'Nochmal drücken zum
      Beenden' und verlangt zweites Enter/Space binnen 2s -> dann endCall. Kein Modal.
    - Den bestehenden `click`-Zweig fuer #nlp-btn-beenden ENTFERNEN (sonst feuern beide -> Doppel-endCall,
      Landmine 7). EWB-Click-Delegation unangetastet lassen.

    B) endCall-REORDER (B-01/L-04 + Ladebalken 1). In endCall (2876):
    - _stopTimer()/_stopMic()/_resetLiveState() SOFORT beibehalten (B-01: Mic stoppt sofort).
    - Statt `_showPostcallLoading()` (Score-Spinner) eine neue Loading-View `_showLadebalken1()` zeigen:
      zentrierter unbestimmter Spinner (Klasse `pip-loading-spinner` aus Task 3, nutzt @keyframes n-pulse)
      + Text 'KI wertet das Gespräch aus…' (14px/600). KEIN Prozentbalken (LB-01). Baue diese kleine
      View auf der _showPostcallLoading-Struktur auf oder als eigene Funktion.
    - `var endCallGen = (state.callGen = (state.callGen||0)+1);` (bestehender Guard beibehalten).
    - fetch /api/beenden wie heute. Im .then(data): bestehenden stale-guard
      `if (state.callGen !== endCallGen || state.micStarted) return;` behalten. state.lastConvId/lastCallId setzen.
      `_showPostcall(data.postcall)` NICHT mehr hier aufrufen — der Score-Render kommt erst nach Confirm
      (Task 2/3). Stattdessen `data.postcall` in `state.pendingPostcall = data.postcall` zwischenspeichern
      (S-03: Basis-Analytics werden zusammen mit Score nach Confirm gerendert).
    - 9s-TIMEOUT (LB-02) armen, BEVOR der Outcome-Fetch startet:
      ```javascript
      var haikuTimedOut = false;
      var lb1Timeout = setTimeout(function () {
        if (state.callGen !== endCallGen) return;            // Stale-Guard (Landmine 2)
        haikuTimedOut = true;
        _renderOutcomeUx({ outcome: null, confidence: 0, source: null, call_id: state.lastCallId });
      }, 9000);
      ```
    - Statt /api/postcall_analysis jetzt `/api/postcall_outcome` (Plan 02) fetchen (Body unveraendert:
      conv_id, call_id, einwaende, painpoints, kb_start, kb_end, redeanteil_berater, redeanteil_kunde,
      dauer_sek, skript_abdeckung, ga_details, kaufsignale). Im .then(paResult):
      `clearTimeout(lb1Timeout); if (state.callGen !== endCallGen) return; if (haikuTimedOut) return;`
      (Late-Haiku darf den Timeout-Screen NICHT ueberschreiben — Landmine 1). Dann
      `if (paResult && paResult.call_id) _renderOutcomeUx({outcome,confidence,source,call_id});`
    - Den outcome_ready-Socket-Handler (2444) ebenfalls absichern: `clearTimeout`-Wirkung via Idempotenz-Guard
      (`sec.dataset.outcomeRendered==='1'`) ist vorhanden; ZUSAETZLICH `if (haikuTimedOut) return;` ist nicht
      moeglich (Closure-Scope) — daher reicht hier der bestehende outcomeRendered-Guard; KEINE Aenderung am
      Socket-Handler ausser dass er weiterhin _renderOutcomeUx aufruft. (Der Timeout-Render setzt
      outcomeRendered='1', also wird ein spaeter Socket-Emit korrekt ignoriert.)

    Generation-Guard (Landmine 2): lb1Timeout-Callback UND postcall_outcome-.then pruefen beide endCallGen.
    UTF-8: user-facing Strings mit echten Umlauten ('Halten zum Beenden…', 'KI wertet das Gespräch aus…');
    Variablen/Klassen ASCII (holdTimer, haikuTimedOut, pip-loading-spinner).
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-03): hold-to-end + ladebalken1 + 9s timeout + reorder" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull'</automated>
  </verify>
  <acceptance_criteria>
    - grep `Halten zum Beenden` static/pip-launcher.js -> 1 Treffer (echte Umlaute, user-facing)
    - grep `KI wertet das Gespräch aus` static/pip-launcher.js -> 1 Treffer
    - grep `postcall_outcome` static/pip-launcher.js -> 1 Treffer (neuer Endpoint statt postcall_analysis im endCall-Pfad)
    - grep `haikuTimedOut` static/pip-launcher.js -> mind. 2 Treffer (Arm + Guard)
    - grep `9000` static/pip-launcher.js -> 1 Treffer (9s-Timeout)
    - grep `state.pendingPostcall` static/pip-launcher.js -> Treffer (Postcall fuer S-03 zwischengespeichert)
    - Im endCall-/beenden-.then: KEIN `_showPostcall(data.postcall)` mehr (Score erst nach Confirm) — grep zeigt _showPostcall nicht mehr im beenden-.then-Block
    - Beenden-Click-Zweig in _wirePipButtons ruft NICHT mehr direkt endCall im click-Handler (Hold-Logik)
    - node -c static/pip-launcher.js bzw. Syntax-Check ok (kein Parse-Fehler)
  </acceptance_criteria>
  <done>Hold-to-end (600ms), Mic-sofort-Stop, Ladebalken 1 mit 9s-Timeout + Generation-Guard, neuer Outcome-Endpoint, Postcall fuer S-03 zwischengespeichert. Late-Haiku ueberschreibt Timeout nicht.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _renderOutcomeUx 3 States (U-01 ROT) + Heading 'Call wirklich beenden?' + modal-state node-Test</name>
  <read_first>
    - static/pip-launcher.js:3660-3846 (_renderOutcomeUx + _decideModalState-Anbindung + confirm-handler)
    - static/outcome-modal-state.js (komplett — 5-Zustand-Decider)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md "U-01" + "Step 3" (3-State-Tabelle)
    - .planning/phases/08.23.2.D.UX.1-...-CONTEXT.md (Zustand-Definitionen, die U-04 ersetzt) falls vorhanden
  </read_first>
  <behavior>
    test_decide_modal_state.test.js (node:test, require('../static/outcome-modal-state.js')):
    - test_sicher: {source:'ai_auto', confidence:0.95} -> 'sicher'
    - test_unsicher: {source:'ai_auto_unsicher', confidence:0.8} -> 'unsicher'
    - test_kein_versuch: {confidence:0} -> 'kein_versuch'
    - test_final: {source:'user_corrected'} -> 'final'
    - test_conf_zero_beats_source: {source:'ai_auto', confidence:0} -> 'kein_versuch' (Reihenfolge load-bearing)
  </behavior>
  <action>
    A) _decideModalState (outcome-modal-state.js): Die Zustands-Logik bleibt funktional korrekt fuer U-01
    (sie liefert bereits 'sicher'/'unsicher'/'kein_versuch'/'final'). KEINE neue Zustands-Klasse noetig —
    die U-01-Aenderung liegt im RENDER (unten), nicht im Decider. Falls die bestehende Reihenfolge schon
    'final' vor conf===0 prueft (tut sie), unveraendert lassen. Nur sicherstellen, dass der defensive
    Fallback 'unsicher' bleibt.

    B) tests/test_decide_modal_state.test.js NEU anlegen (Datei existiert NICHT — Wave-0-Gap). node:test mit
    `const { _decideModalState } = require('../static/outcome-modal-state.js');` und den 5 behavior-Cases oben.

    C) _renderOutcomeUx (pip-launcher.js:3669) umbauen:
    - Idempotenz-Guard `sec.dataset.outcomeRendered==='1'` BEIBEHALTEN (Landmine 1).
    - Header-Text von 'Gesprächsergebnis bestätigen' auf **'Call wirklich beenden?'** aendern (B-01).
      Klasse pip-outcome-header behalten.
    - 'final'-Zweig (read-only Summary) unveraendert lassen (B-03 Reconnect-Fall).
    - State-Variable: `var modalState = _decideModalState(data);`
    - selectedOutcome-Initialwert:
      * 'sicher': selectedOutcome = haikuOutcome (teal Vorauswahl, Confirm sofort aktiv) — wie heute.
      * 'unsicher': selectedOutcome bleibt NULL fuer Confirm-Zwecke (U-01: Bestätigen disabled bis aktiver
        Tap), ABER eine separate Variable `var presetUnsicher = haikuOutcome;` haelt den rot-vorauszuwaehlenden
        Button. NICHT als bestaetigbar behandeln (U-01/U-02).
      * 'kein_versuch': selectedOutcome NULL, kein preset.
    - Disclaimer (nur 'unsicher'): Zeile ueber dem Grid mit Text **'KI unsicher, bitte prüfen'**, Klasse
      `pip-outcome-disclaimer` (Token --btn-danger-text, Task 3), mit Lucide alert-triangle inline-SVG 16px
      stroke #f87171. Den ALTEN Badge 'KI unsicher — bitte wählen' (pip-outcome-badge-unsicher, Teal-Outline)
      fuer den 'unsicher'-Fall ENTFERNEN/ersetzen (U-04 supersedes D.UX.1).
    - Hint (nur 'kein_versuch'): bestehenden 'Automatische Erkennung nicht verfügbar'-Hinweis behalten.
    - 7 Buttons aus _OUTCOME_OPTS (Reihenfolge unveraendert). Pre-Select-Styling:
      * 'sicher' + pair[0]===selectedOutcome -> classList.add('pip-outcome-btn--selected') (teal, wie heute).
      * 'unsicher' + pair[0]===presetUnsicher -> classList.add('pip-outcome-btn--selected-unsicher')
        (rote Vorauswahl, neue Token-Klasse aus Task 3) + alert-triangle-Icon links im Button.
    - Button-Click-Handler (aktiver Tap): deselektiert alle (entfernt BEIDE Klassen
      'pip-outcome-btn--selected' UND 'pip-outcome-btn--selected-unsicher'), setzt geklickten Button auf TEAL
      'pip-outcome-btn--selected', setzt selectedOutcome=pair[0], entfernt die Disclaimer-Zeile (falls
      vorhanden), aktiviert confirmBtn (disabled=false, entfernt 'pip-outcome-btn--disabled'). Das gilt fuer
      ALLE drei States — im 'unsicher'/'kein_versuch'-Fall ist genau das der "bewusste Klick" (U-02).
    - confirmBtn: disabled wenn !selectedOutcome (also disabled bei 'unsicher' UND 'kein_versuch' bis Tap,
      sofort aktiv bei 'sicher'). Label 'Bestätigen'; disabled-Style pip-outcome-btn--disabled.
    - confirm-handler: outcome_source-Mapping (conf>=0.90 && sel===haiku -> 'ai_auto';
      conf>=0.70 && sel===haiku -> 'ai_auto_unsicher'; sonst 'user_corrected') BEIBEHALTEN. POST an
      /api/calls/<callId>/correct_outcome wie heute. Score-Reveal + Ladebalken 2 sind Task 3 — hier nur
      sicherstellen, dass nach Erfolg `state.lastOutcome = json.outcome` o.ae. fuer Task 3 verfuegbar ist,
      und Hard-Error-Copy auf **'Konnte nicht speichern – bitte erneut auf Bestätigen tippen.'** setzen
      (statt 'Fehler — erneut versuchen').

    B-03: KEIN Back-to-Call-Button hinzufuegen (terminal/Forced-Choice).
    UTF-8: alle sichtbaren Strings echte Umlaute ('Call wirklich beenden?', 'KI unsicher, bitte prüfen',
    'Bestätigen', 'Konnte nicht speichern – bitte erneut auf Bestätigen tippen.'); Klassen/Variablen ASCII.
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-03): outcome screen 3 states (U-01 red) + heading + modal test" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull && node --test tests/test_decide_modal_state.test.js'</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_decide_modal_state.test.js existiert; `node --test` -> alle 5 Cases GRUEN (auf Server)
    - grep `Call wirklich beenden?` static/pip-launcher.js -> 1 Treffer (Heading geaendert, B-01)
    - grep `KI unsicher, bitte prüfen` static/pip-launcher.js -> 1 Treffer (Disclaimer-Copy, U-01)
    - grep `pip-outcome-btn--selected-unsicher` static/pip-launcher.js -> mind. 1 Treffer (rote Vorauswahl)
    - grep `presetUnsicher` static/pip-launcher.js -> Treffer (Vorauswahl gesetzt aber nicht confirmbar)
    - grep `Konnte nicht speichern` static/pip-launcher.js -> 1 Treffer (Hard-Error-Copy)
    - grep `Gesprächsergebnis bestätigen` static/pip-launcher.js -> 0 Treffer (alter Heading ersetzt)
    - 'sicher'-Fall: confirmBtn NICHT disabled; 'unsicher'/'kein_versuch': confirmBtn disabled bis Tap (Code-Pfad)
  </acceptance_criteria>
  <done>Outcome-Screen mit Heading 'Call wirklich beenden?', 3 States (teal/rot/neutral), U-01 rote Vorauswahl + Disclaimer + Confirm-disabled-bis-Tap, modal-state node-Test gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Score+Analytics S-03 sofort + Ladebalken 2 (Sonnet) + _calcScore aus Render entfernen (L-01) + CSS</name>
  <read_first>
    - static/pip-launcher.js:2982-3054 (_calcScore + _showPostcall — L-01-Entfernung), confirm-handler 3776-3845
    - static/nerve.css:85-95 (--btn-danger-*), 130-139 (--kpi-*), 210-214 (--session-score-*), 3319-3330 (pip-outcome-btn--selected/--disabled), @keyframes n-pulse
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-UI-SPEC.md "Step 4/Step 5" (Panel-Order) + "Color" (Token-Bindung)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md "Basis-Analytics fuer S-03" (Feldnamen)
  </read_first>
  <action>
    A) L-01 — _calcScore aus dem Score-Render entfernen. In _showPostcall (pip-launcher.js:3047-3049):
    den `var score = _calcScore(postcall); ... _showPostcallRaw(score + '%', tags);` so umbauen, dass beim
    initialen Render KEIN vorlaeufiger Score gesetzt wird (der Score kommt ausschliesslich aus der
    correct_outcome-Response nach Confirm). `_calcScore`-Funktion selbst darf als Datei-Funktion bleiben
    (NICHT zwingend loeschen), aber sie DARF NICHT mehr in den angezeigten Score fliessen. Wenn _showPostcall
    weiterhin fuer den finalen Render nach Confirm genutzt wird, den score-Parameter aus der
    correct_outcome-Response (json.final_score) durchreichen statt _calcScore. _buildTags/_renderQuickStats/
    _renderSparkline/_fetchAndRenderTrend duerfen bleiben.

    B) S-03 — Score-Karte + Basis-Analytics ZUSAMMEN nach Confirm. Im confirm-handler von _renderOutcomeUx
    (nach erfolgreicher correct_outcome-Response, Task 2 hat state.lastOutcome gesetzt):
    - Score-Section aufdecken (#nlp-section-postcall display:'') wie heute.
    - Score-Wert aus `json.final_score` rendern (S-01, kein _calcScore). Farbe nach Band:
      >=70 var(--session-score-high), >=40 var(--session-score-mid), sonst var(--session-score-low)
      (oder bestehende Score-Color-Logik wiederverwenden). Label 'Coaching-Score'.
    - Basis-Analytics aus `state.pendingPostcall` (in Task 1 gespeichert) SOFORT zusammen rendern — drei
      KPI-Tiles (.n-kpi/.n-kpi-value/.n-kpi-label): Kaufbereitschaft = pendingPostcall.kb_end;
      Redeanteil = pendingPostcall.redeanteil_berater (%); Einwände = (pendingPostcall.einwaende||[]).length.
      Panel-Heading 'Auswertung'. (Bestehende _renderQuickStats kann wiederverwendet/angepasst werden, solange
      KB/Redeanteil/Einwände-Tiles zusammen mit dem Score erscheinen.)
    - KEINE Animation (S-01).

    C) Ladebalken 2 (L-04/S-03) — NUR fuer Sonnet-Lernkarten, UNTER Score+Analytics. Nach dem Score-Reveal
    im confirm-handler:
    - Einen Spinner-Block 'Detail-Auswertung läuft…' (Klasse pip-loading-spinner-2) UNTER der Score/Analytics-
      Sektion einfuegen — er darf Score/Analytics NICHT ueberdecken (S-03).
    - `fetch('/api/postcall_cards', {POST, Body: conv_id/call_id + dieselben postcall-Felder wie der
      Outcome-Call})` (Plan 02). Im .then: Spinner durch die gerenderten Lernkarten ersetzen (bestehende
      Lernkarten-Render-Logik nutzen, sofern vorhanden; sonst vorschlaege in die bestehende
      Lernkarten-Sektion rendern). Score/Analytics bleiben sichtbar (Landmine 6: Sonnet darf Score NICHT
      ueberschreiben).
    - Generation-Guard: dieser Fetch laeuft nach Confirm (terminal); ein endCallGen-Check ist optional, aber
      falls state.callGen erreichbar, `if (state.callGen !== endCallGen) return;` analog (Konsistenz).

    D) CSS (static/nerve.css) — NUR Token-gebundene Ergaenzungen neben den bestehenden Klassen:
    - `.pip-outcome-btn--selected-unsicher` (neben .pip-outcome-btn--selected): border:1.5px solid
      var(--btn-danger-text); background:var(--btn-danger-bg); color:var(--btn-danger-text); font-weight:600.
      KEIN ad-hoc Hex (Punkt 8).
    - `.pip-outcome-disclaimer`: color:var(--btn-danger-text); font-size:12px; font-weight:600; display:flex;
      align-items:center; gap:4px. Kein Background.
    - `.pip-beenden-holding`: rote Progress-Wipe-Optik fuer den 600ms-Hold (z.B. background:linear-gradient
      mit var(--btn-danger-text), KEIN Teal, KEIN ad-hoc Hex; nutze die danger-Tokens).
    - `.pip-loading-spinner` + `.pip-loading-spinner-2`: unbestimmter Spinner/Pulse via bestehender
      @keyframes n-pulse (oder vorhandener pip-score-spinner-Animation). KEIN Prozentbalken.
    Alle neuen Klassen ASCII; nur var(--...)-Tokens, keine neuen Hex-Werte.

    UTF-8: 'Detail-Auswertung läuft…', 'Coaching-Score', 'Auswertung', 'Kaufbereitschaft', 'Redeanteil',
    'Einwände' user-facing mit Umlauten; Klassen/Variablen ASCII.
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-03): score+analytics S-03 instant + ladebalken2 + remove _calcScore from render + css" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull'</automated>
  </verify>
  <acceptance_criteria>
    - grep `_calcScore(postcall)` static/pip-launcher.js innerhalb _showPostcall-Render-Pfad -> 0 Treffer (L-01: nicht mehr im angezeigten Score)
    - grep `postcall_cards` static/pip-launcher.js -> 1 Treffer (Ladebalken 2 / Sonnet nach Confirm)
    - grep `Detail-Auswertung läuft` static/pip-launcher.js -> 1 Treffer
    - grep `Coaching-Score` static/pip-launcher.js -> 1 Treffer
    - grep `Auswertung` + `Kaufbereitschaft` + `Einwände` static/pip-launcher.js -> je Treffer (S-03 Tiles)
    - grep `state.pendingPostcall` static/pip-launcher.js -> Read im confirm-handler (Analytics-Quelle)
    - grep `pip-outcome-btn--selected-unsicher` static/nerve.css -> 1 Treffer (Token-only, var(--btn-danger-text))
    - grep `pip-outcome-disclaimer` static/nerve.css -> 1 Treffer
    - grep `pip-loading-spinner` static/nerve.css -> Treffer
    - In den neuen CSS-Klassen KEIN roher Hex ausser ueber var() — grep `#f8` / `#00d` in neuen Bloecken -> 0 (nur Tokens)
    - json.final_score wird als Score gerendert (nicht _calcScore) — Code-Pfad
  </acceptance_criteria>
  <done>Score (aus final_score) + Basis-Analytics erscheinen sofort zusammen nach Confirm ohne Animation; Ladebalken 2 nur fuer Sonnet-Lernkarten darunter; _calcScore raus aus Render-Pfad (L-01); CSS token-gebunden, kein Gelb/ad-hoc-Hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PiP-Client -> /api/postcall_outcome, /api/postcall_cards, /api/correct_outcome | Frontend sendet conv_id/call_id/outcome; Server validiert Ownership + VALID_OUTCOMES (Plan 01/02) |
| Late async callback -> bereits neue Session | endCallGen-Generation-Guard schuetzt gegen stale Render in neuem Call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UX4-07 | Tampering | DOM-injizierte Outcome-Labels | mitigate | Labels aus statischem _OUTCOME_OPTS via escHtml() rendern (bestehendes Pattern beibehalten) |
| T-UX4-08 | Denial of Service | Slow/haengender Haiku | mitigate | 9s-Timeout (LB-02) -> Outcome-Screen ohne Vorauswahl; kein ewiges Warten (LB-03 abgelehnt) |
| T-UX4-09 | Tampering | Stale async Render in neuer Session | mitigate | endCallGen-Guard auf lb1Timeout + postcall_outcome-.then + haikuTimedOut-Flag (Landmine 1/2) |
| T-UX4-10 | Repudiation | Doppel-endCall durch Click+Hold | mitigate | Click-Zweig entfernt, holdFired-Flag, single pointer-hold-Pfad (Landmine 7) |

> Reine Frontend-/CSS-Phase ohne neuen Daten-Persistenz-Pfad — keine `## 5. Persistenz-Schicht-Verifikation`
> noetig (alle DB-Schreibwege liegen in Plan 01/02; dieser Plan ruft nur deren Endpoints auf). Die
> konsumierte correct_outcome-Response + die zwei neuen Endpoints sind in Plan 01/02 schema-verifiziert.
</threat_model>

<verification>
- node --test tests/test_decide_modal_state.test.js (Production-Server) — 5 Cases gruen
- Syntax/Parse-Check static/pip-launcher.js (kein Fehler)
- Grep-Akzeptanz aller user-facing Copy-Strings (echte Umlaute) + neue Endpoint-Calls + Token-only-CSS
- Manuelle Live-Verifikation des kompletten Ablaufs ist Sache des UAT nach Deploy (Cross-AI + Code-Review
  laut CLAUDE.md Punkt 7 PFLICHT vor Execute dieser Phase)
</verification>

<success_criteria>
- B-02 600ms-Hold beendet Call; Single-Tap nicht (kein Doppel-endCall)
- Ablauf: Hold -> Mic-Stop + Ladebalken 1 (9s-Timeout) -> Outcome-Screen 'Call wirklich beenden?' (3 States)
  -> Confirm -> Score+Analytics sofort -> Ladebalken 2 (Sonnet)
- U-01: sicher=teal/sofort, unsicher=rot+Disclaimer+Confirm-disabled-bis-Tap, kein_versuch=neutral+disabled-bis-Tap
- L-01: kein vorlaeufiger _calcScore im Render; Score genau einmal aus final_score
- S-03: Score + KB/Redeanteil/Einwände zusammen; Ladebalken 2 nur fuer Sonnet
- Generation-Guard auf allen async-Pfaden; kein Gelb, kein ad-hoc-Hex (Punkt 8)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-03-SUMMARY.md`
</output>
