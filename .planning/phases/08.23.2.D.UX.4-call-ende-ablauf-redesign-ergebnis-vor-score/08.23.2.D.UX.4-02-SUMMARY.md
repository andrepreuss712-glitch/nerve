---
phase: 08.23.2.D.UX.4
plan: 02
subsystem: backend-postcall
tags: [postcall-split, haiku-outcome, sonnet-cards, f11-soft-error, learning-cards]
requires:
  - "routes/learning.py monolith /api/postcall_analysis (Block A Sonnet + Block B/C Haiku)"
  - "services/coaching_service.generate_postcall_analysis (persists LearningCards)"
  - "services/outcome_service.classify"
provides:
  - "/api/postcall_outcome — Haiku-only Outcome-Endpoint (no Sonnet, soft-error F11)"
  - "/api/postcall_cards — Sonnet-only Cards-Endpoint (persists LearningCards, confirm-independent)"
affects:
  - "Plan 03 (Frontend): wires both new endpoints, triggers /api/postcall_cards in background at call-end"
tech-stack:
  added: []
  patterns: ["endpoint split with retained backward-compat shim", "outer try/except soft-500 (F11)"]
key-files:
  created:
    - "tests/test_postcall_split.py"
  modified:
    - "routes/learning.py"
decisions:
  - "Monolith /api/postcall_analysis kept as backward-compat shim (not deleted) — Cleanup-Debt INFO-2, follow-up after 1-2 weeks Production stability"
  - "/api/postcall_cards is confirm-independent (NEW-2): persists LearningCards at call-end regardless of Outcome-Confirm to avoid data regression on abort"
metrics:
  duration: "~15min"
  completed: "2026-05-31"
  tasks: 2
  files: 2
---

# Phase 08.23.2.D.UX.4 Plan 02: Backend Postcall-Split Summary

Split the monolithic `/api/postcall_analysis` into a fast Haiku-only `/api/postcall_outcome` (drives the Outcome-Screen without waiting on Sonnet) and a Sonnet-only `/api/postcall_cards` (persists LearningCards in the background, confirm-independent), keeping the monolith as a backward-compat shim.

## What Was Built

### Task 1 — `/api/postcall_outcome` (Haiku-only, F11 soft-error)
- New endpoint `routes/learning.py:196` `api_postcall_outcome()`.
- Content = Block B (Haiku `outcome_service.classify` + threshold logic + `calls`-UPDATE) + Block C (room-targeted `outcome_ready` emit) of the old monolith — **without** Block A (Sonnet).
- conf=0 degraded path preserved (outcome `None`, source `None`, telemetry print) for LB-04.
- `outcome_ready` emit fires even at conf=0 (BLOCKER-1, not moved into the write-guard).
- `transcript_segments` read path kept verbatim (ts_ms, id ordering); **no** `log_entries` getattr (Punkt 21 / D.UX.1 Bug-A root avoided).
- Outer `try/except` (F11, lines 347-353): Haiku/DB exception → soft 500 + traceback log, no hard crash. Ownership-404 stays outside the try (not swallowed).
- Response: `{outcome, confidence, source, call_id}` — no `vorschlaege` key.
- Commit: `5efa559`

### Task 2 — `/api/postcall_cards` (Sonnet-only, confirm-independent) + monolith shim
- New endpoint `routes/learning.py:356` `api_postcall_cards()`.
- Content = Block A only: `generate_postcall_analysis(...)` (Sonnet) which persists LearningCard rows (`status='vorschlag'`, keyed `call_id=conv_id`) via `coaching_service`, guarded by its idempotent duplicate-guard.
- Outer `try/except` (F11, lines 412-417) → soft 500.
- Response: `{vorschlaege: suggestions}`.
- **NEW-2 confirm-independence** documented in the docstring: the frontend (Plan 03) triggers this in the background at call-end (fire-and-forget), independent of the Outcome-Confirm, so cards persist even if the user aborts the Outcome-Screen (parity with the old monolith → no data regression). Endpoint itself is confirm-agnostic.
- Old `/api/postcall_analysis` monolith **kept unchanged as a backward-compat shim** (`routes/learning.py:20`).
- Test file `tests/test_postcall_split.py`: runtime tests via real Flask `test_client` (mocked `classify` + `generate_postcall_analysis`): outcome without Sonnet, conf=0 degraded path, cards return `vorschlaege`, ownership 404 on both. No source-presence tests.
- Commit: `c1ca118`

## Acceptance Criteria (code-side, grep-verified)
- `def api_postcall_outcome` → 1 hit; `'/api/postcall_outcome'` → 1 hit.
- `api_postcall_outcome` body: `outcome_service.classify` 1×, `emit('outcome_ready'` 1×, F11 `except Exception`/`traceback`/`'internal error'` present, `generate_postcall_analysis` 0× in its body.
- `def api_postcall_cards` → 1 hit; `'/api/postcall_cards'` → 1 hit; `generate_postcall_analysis` 1× in body; `outcome_service.classify` 0× in body; F11 present.
- `def api_postcall_analysis` → still 1 hit (monolith shim retained).
- Pending (orchestrator-run via manual-direct-prod, NOT executor): server pytest `tests/test_postcall_split.py`, `inspect.sh routes | grep postcall` after restart.

## Deviations from Plan
None of substance. One mechanical note: both new endpoints landed in a single `routes/learning.py` edit, so the Task-1 commit (`5efa559`) physically contains both endpoint functions; the Task-2 commit (`c1ca118`) adds the test file. The shim and per-endpoint contracts match the plan exactly. No Rule 1-4 fixes were needed.

## Known Stubs
None.

## Cleanup Debt (INFO-2)
`/api/postcall_analysis` is a deliberately retained backward-compat shim. After Plan 03 deploys, it becomes effective dead-code (only theoretical reconnect/old-tab callers). Follow-up (not this phase): after 1-2 weeks of Production stability, confirm 0 hits via `inspect.sh logs` then remove it in a mini cleanup task.

## Constraints Honored
- No Production contact, no deploy, no `git pull`, no STATE.md / ROADMAP.md edits.
- Specific-file `git add` (`routes/learning.py`, `tests/test_postcall_split.py`) — never `-A` (avoided staging STATE.md, logs/, _design_export/).
- No local pytest / local app / local DB run (HART rule).
- UTF-8 rule: code identifiers + dict/JSON keys ASCII (`vorschlaege`, `einwaende`); user-facing/comment Umlaute fine.

## Self-Check: PASSED
- routes/learning.py — FOUND
- tests/test_postcall_split.py — FOUND
- SUMMARY.md — FOUND
- Commit 5efa559 (Task 1) — FOUND
- Commit c1ca118 (Task 2) — FOUND
