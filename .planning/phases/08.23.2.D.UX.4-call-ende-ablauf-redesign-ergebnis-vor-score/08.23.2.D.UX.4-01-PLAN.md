---
phase: 08.23.2.D.UX.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/app_routes.py
  - tests/test_score_split.py
autonomous: true
complexity: medium
requirements: [S-02, S-01, U-03, L-01]
must_haves:
  truths:
    - "Prozess-Score wird outcome-unabhaengig in /api/beenden berechnet und in calls.score_breakdown persistiert (coaching_score bleibt dort NULL)"
    - "correct_outcome liest den gestashten Prozess-Score, wendet NUR den Outcome-Modifier an, persistiert final coaching_score atomar"
    - "Bei fehlendem Stash (alter Call / kein Beenden-Stash) berechnet correct_outcome den Prozess-Score per Fallback neu, kein Hard-Fail"
    - "correct_outcome-Response liefert weiterhin synchron coaching_score + final_score (S-01, Shape unveraendert)"
    - "Prozess-Teil wird genau einmal gerechnet, Modifier genau einmal — keine Doppelrechnung"
  artifacts:
    - path: "routes/app_routes.py"
      provides: "_calc_process_score(conv) + _apply_outcome_modifier(process_score, base_breakdown, outcome) + Thin-Wrapper _calc_coaching_score + Beenden-Stash + correct_outcome-Rewire"
      contains: "_calc_process_score"
    - path: "tests/test_score_split.py"
      provides: "Runtime-Tests: Prozess-Score outcome-unabhaengig, Modifier-Anwendung, Stash-Read, Response-Shape"
      contains: "def test_"
  key_links:
    - from: "routes/app_routes.py:/api/beenden calls-UPDATE-Block"
      to: "calls.score_breakdown (jsonb)"
      via: "_calc_process_score(conv) -> _call_row.score_breakdown = base_breakdown"
      pattern: "_calc_process_score"
    - from: "routes/app_routes.py:correct_outcome"
      to: "_apply_outcome_modifier"
      via: "row.score_breakdown stash read + modifier apply"
      pattern: "_apply_outcome_modifier"
---

<objective>
Backend Score-Split (S-02). Die Logik "Coaching-Score = Prozess-Score (outcome-UNABHAENGIG) x Outcome-Modifier"
steht heute monolithisch in `_calc_coaching_score(conv, outcome)` (app_routes.py:720-764). Die EINZIGE
outcome-abhaengige Zeile ist Z.747 (`modifier = _OUTCOME_MODIFIERS.get(outcome, 1.00)`). Dieser Plan trennt
die Funktion in zwei (`_calc_process_score` + `_apply_outcome_modifier`), behaelt `_calc_coaching_score` als
Thin-Wrapper fuer bestehende Caller/Tests, stasht den Prozess-Score persistent in `calls.score_breakdown`
am Ende von `/api/beenden` (laeuft immer, deterministisch, kein neuer Thread) und verdrahtet `correct_outcome`
so um, dass beim Confirm nur noch der Modifier auf den gestashten Prozess-Score angewandt wird.

Purpose: Score erscheint beim Confirm gefuehlt instant (Prozess-Teil lief schon im Hintergrund) UND wird
mathematisch genau einmal gerechnet (S-02 ist Performance- UND Korrektheits-Fix). Behebt die L-01-Doppelrechnung
auf der Backend-Seite.
Output: Gesplittete Score-Funktionen, Beenden-Stash, umverdrahtetes correct_outcome, neue Test-Datei.
KEINE Schema-Aenderung — `score_breakdown` (jsonb) + `coaching_score` (double) existieren bereits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-CONTEXT.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md
@.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-SCHEMA-EVIDENCE.md

<interfaces>
<!-- Vom Executor direkt zu verwenden — keine Codebase-Exploration noetig. -->

Bestehend in routes/app_routes.py:709-764 (Quelle des Splits):
```python
_OUTCOME_MODIFIERS = {
    'contract_signed':    1.15,
    'meeting_booked':     1.10,
    'wrong_person':       1.00,
    'callback':           0.95,
    'send_info':          0.95,
    'gatekeeper_blocked': 0.95,
    'no_interest':        0.85,
}

def _calc_coaching_score(conv, outcome):
    # Formel: process_score = kb*0.30 + behandelt_rate*100*0.30 + redeanteil_score*0.20
    #                         + skript*0.10 + frage_qualitaet*0.10
    # frage_qualitaet = 0.0 (v1). modifier = _OUTCOME_MODIFIERS.get(outcome, 1.00).
    # final_score = int(clamp(round(process_score*modifier), 0, 100)).
    # coaching_score_val = round(process_score*modifier, 4).
    # Returns (coaching_score_val, final_score, breakdown_dict)
    # breakdown-Keys: schema_version, kb_end_norm, behandelt_rate, redeanteil_score,
    #                 skript_norm, frage_qualitaet, outcome_modifier, process_score,
    #                 final_score, computed_at_iso
```

WICHTIG — `_calc_call_score(conv)` (app_routes.py:695) ist NICHT betroffen und NICHT Dead-Code:
es wird aktiv in routes/dashboard.py:774 genutzt. NICHT anfassen, NICHT entfernen.

Bestehende Caller von `_calc_coaching_score` (Pflicht-grep bestaetigt):
- routes/app_routes.py:1923 (correct_outcome) — wird in diesem Plan umverdrahtet
- tests/test_correct_outcome.py (8 Tests, rufen `_calc_coaching_score(conv, outcome)` mit
  3-Tupel-Return-Erwartung auf) — Thin-Wrapper MUSS dieselbe Signatur + denselben 3-Tupel-Return
  behalten, sonst brechen diese Tests.

Beenden calls-UPDATE-Block (app_routes.py:603-621), wo `_call_row` schon geladen + committed wird,
`saved_conv_id` verfuegbar ist:
```python
if _phase_d_call_id:
    from database.models import Call as _CallModel
    _db_calls = get_session()
    try:
        _call_row = _db_calls.query(_CallModel).filter(_CallModel.id == _phase_d_call_id).first()
        if _call_row is not None:
            _call_row.ended_at = _dt.now(_tz.utc)
            _call_row.conversation_log_id = saved_conv_id
            _call_row.call_mode = 'cold_call' if _req_mode == 'cold_call' else 'meeting_consented'
            _db_calls.commit()
    ...
```

correct_outcome Score-Block heute (app_routes.py:1917-1929):
```python
from database.models import ConversationLog as _CLScore
conv_for_score = None
if row.conversation_log_id:
    conv_for_score = db_co.query(_CLScore).filter(_CLScore.id == row.conversation_log_id).first()
try:
    coaching_score_val, final_score_val, breakdown_data = _calc_coaching_score(conv_for_score, new_outcome)
except Exception as _e_score:
    print(...)
    coaching_score_val, final_score_val, breakdown_data = 0.0, 0, {}
row.coaching_score = coaching_score_val
row.score_breakdown = breakdown_data
row.score_schema_version = 1
db_co.commit()
```
Response-Shape (UNVERAENDERT lassen): {ok, coaching_score, final_score, outcome, followup_intent, note_dropped?}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _calc_coaching_score in _calc_process_score + _apply_outcome_modifier splitten (Thin-Wrapper behalten)</name>
  <read_first>
    - routes/app_routes.py:695-765 (`_calc_call_score`, `_OUTCOME_MODIFIERS`, `_calc_coaching_score` — exakte Formel + breakdown-Keys)
    - tests/test_correct_outcome.py (alle 8 Tests — sie erwarten `_calc_coaching_score(conv, outcome)` -> 3-Tupel)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md Sektion "S-02 Backend Score-Split" (Referenz-Refactor)
  </read_first>
  <behavior>
    - test_process_score_outcome_independent: `_calc_process_score(conv)` liefert fuer denselben conv IMMER
      denselben process_score-Float, egal welches Outcome (Funktion nimmt gar kein outcome-Argument).
    - test_modifier_application: `_apply_outcome_modifier(100.0, base_bd, 'contract_signed')` -> final_score
      basiert auf 100*1.15 -> clamp 100; `_apply_outcome_modifier(80.0, base_bd, 'no_interest')` -> 80*0.85=68.
    - test_modifier_unknown_outcome: unbekanntes Outcome -> modifier 1.00 (process_score unveraendert).
    - test_thin_wrapper_compat: `_calc_coaching_score(conv, 'meeting_booked')` liefert weiterhin
      ein 3-Tupel (coaching_score_float, final_score_int, breakdown_dict) mit denselben breakdown-Keys
      wie heute (schema_version, kb_end_norm, behandelt_rate, redeanteil_score, skript_norm,
      frage_qualitaet, outcome_modifier, process_score, final_score, computed_at_iso).
    - test_none_conv_defaults: `_calc_process_score(None)` nutzt Defaults (kb 30, behandelt_rate 0.5,
      redeanteil 50, skript 0) ohne Exception.
  </behavior>
  <action>
    In routes/app_routes.py, ERSETZE die bestehende `_calc_coaching_score`-Funktion (Z.720-764) durch drei
    Funktionen. `_OUTCOME_MODIFIERS` (Z.709-717) und `_calc_call_score` (Z.695-705) bleiben UNVERAENDERT.

    1. `_calc_process_score(conv)` — outcome-UNABHAENGIG, Returns `(process_score_float, base_breakdown_dict)`:
       - kb_norm = float(conv.kb_end if conv is not None and conv.kb_end is not None else 30)
       - einw_total = (conv.einwaende_gesamt or 0) if conv is not None else 0
       - einw_ok = (conv.einwaende_behandelt or 0) if conv is not None else 0
       - behandelt_rate = (einw_ok / einw_total) if einw_total > 0 else 0.5
       - redeanteil = float(conv.redeanteil_avg if conv is not None and conv.redeanteil_avg is not None else 50)
       - redeanteil_score = float(max(0, 100 - abs(redeanteil - 40) * 2))
       - skript = float((conv.skript_abdeckung or 0) if conv is not None else 0)
       - frage_qualitaet = 0.0
       - process_score = kb_norm*0.30 + behandelt_rate*100*0.30 + redeanteil_score*0.20 + skript*0.10 + frage_qualitaet*0.10
       - base_breakdown = {'schema_version': 1, 'kb_end_norm': round(kb_norm,4), 'behandelt_rate': round(behandelt_rate,4),
         'redeanteil_score': round(redeanteil_score,4), 'skript_norm': round(skript,4),
         'frage_qualitaet': frage_qualitaet, 'process_score': round(process_score,4)}
       - return process_score, base_breakdown

    2. `_apply_outcome_modifier(process_score, base_breakdown, outcome)` — Returns
       `(coaching_score_float, final_score_int, full_breakdown_dict)`:
       - from datetime import datetime, timezone as _tz
       - modifier = _OUTCOME_MODIFIERS.get(outcome, 1.00)
       - raw = process_score * modifier
       - final_score = int(min(100, max(0, round(raw))))
       - full = dict(base_breakdown); full.update({'outcome_modifier': modifier, 'final_score': final_score,
         'computed_at_iso': datetime.now(_tz.utc).isoformat()})
       - return round(raw, 4), final_score, full

    3. `_calc_coaching_score(conv, outcome)` — Thin-Wrapper fuer Rueckwaerts-Kompatibilitaet (3-Tupel-Return,
       identische breakdown-Keys wie heute):
       - process_score, base_bd = _calc_process_score(conv)
       - return _apply_outcome_modifier(process_score, base_bd, outcome)

    UTF-8: rein Code-Identifier, ASCII (process_score, behandelt_rate etc.) — keine Umlaute in dieser Task.
    Punkt 14 Control-Flow-Audit: Die alte Funktion hat genau einen In-App-Caller (correct_outcome:1923,
    in Task 3 umverdrahtet) + Test-Caller. Wrapper haelt beide gruen.
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-01): split score into process+modifier" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull && source ../venv/bin/activate && pytest tests/test_score_split.py tests/test_correct_outcome.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - grep `def _calc_process_score` routes/app_routes.py -> 1 Treffer
    - grep `def _apply_outcome_modifier` routes/app_routes.py -> 1 Treffer
    - grep `def _calc_coaching_score` routes/app_routes.py -> 1 Treffer (Wrapper bleibt)
    - grep `def _calc_call_score` routes/app_routes.py -> 1 Treffer (UNVERAENDERT, NICHT entfernt)
    - pytest tests/test_correct_outcome.py -> alle 8 bestehenden Tests GRUEN (Wrapper-Kompatibilitaet)
    - pytest tests/test_score_split.py -> alle neuen Tests GRUEN (auf Production-Server, client-Fixture)
  </acceptance_criteria>
  <done>Drei Funktionen existieren, alte Caller via Wrapper gruen, neue Split-Tests gruen. `_calc_call_score` unangetastet.</done>
</task>

<task type="auto">
  <name>Task 2: Prozess-Score in /api/beenden persistent in calls.score_breakdown stashen</name>
  <read_first>
    - routes/app_routes.py:603-686 (Beenden calls-UPDATE-Block + Response — wo `_call_row` + `saved_conv_id` leben)
    - routes/app_routes.py:1917-1921 (correct_outcome conv_for_score-Lade-Pattern als Vorlage)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md "Wo den Prozess-Score stashen?"
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-SCHEMA-EVIDENCE.md (score_breakdown jsonb existiert)
  </read_first>
  <action>
    Im bestehenden Beenden calls-UPDATE-Block (app_routes.py:603-621), INNERHALB des
    `if _call_row is not None:`-Zweigs, NACH `_call_row.conversation_log_id = saved_conv_id` und VOR
    `_db_calls.commit()`, den Prozess-Score-Stash einfuegen:

    - conv_for_score laden: `from database.models import ConversationLog as _CLBeenden`
      `_conv_bd = _db_calls.query(_CLBeenden).filter(_CLBeenden.id == saved_conv_id).first() if saved_conv_id else None`
    - `_proc_score, _base_bd = _calc_process_score(_conv_bd)`
    - `_call_row.score_breakdown = _base_bd`   (enthaelt 'process_score' + schema_version 1)
    - `_call_row.score_schema_version = 1`
    - `coaching_score` NICHT setzen — bleibt NULL bis zum Confirm (noch kein Outcome).

    Das Ganze in einem try/except wrappen (analog der bestehenden Beenden-Blocks): bei Exception
    `print('[08.23.2.D.UX.4] process-score stash Fehler: ...')` + weiter (non-fatal — der
    correct_outcome-Fallback in Task 3 faengt einen fehlenden Stash ab). Der bestehende
    `_db_calls.commit()` persistiert den Stash mit.

    Control-Flow-Audit (Punkt 14): Stash sitzt im IMMER laufenden Beenden-Pfad (nicht im conf-gated
    postcall_analysis), daher robust gegen Haiku-Ausfall (LANDMINE 4 der RESEARCH). Keine neue DB-Session.
    UTF-8: Code-Identifier ASCII; Log-String darf Umlaute haben.
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-01): stash process score in /api/beenden" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull && source ../venv/bin/activate && pytest tests/test_score_split.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - grep `_calc_process_score(_conv_bd)` routes/app_routes.py -> 1 Treffer im Beenden-Block
    - grep `score_breakdown = _base_bd` routes/app_routes.py -> 1 Treffer
    - test_score_split.py::test_process_score_stashed: nach simuliertem /api/beenden (oder direkter
      Funktionsverwendung mit client-Fixture + DB-read) hat die calls-Row `score_breakdown['process_score']`
      gesetzt und `coaching_score` ist NULL/None
    - Kein zweiter commit/Session im Beenden-Block (Stash nutzt `_db_calls`)
  </acceptance_criteria>
  <done>Prozess-Score wird am Ende von /api/beenden in calls.score_breakdown persistiert, coaching_score bleibt NULL. Test gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: correct_outcome auf Stash-Read + Modifier-Anwendung umverdrahten (mit Fallback)</name>
  <read_first>
    - routes/app_routes.py:1853-1947 (kompletter correct_outcome-Handler — Score-Block Z.1917-1929)
    - routes/app_routes.py (neue `_calc_process_score` / `_apply_outcome_modifier` aus Task 1)
    - .planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-RESEARCH.md "Beim Confirm (correct_outcome)" + Response-Shape-Block
  </read_first>
  <behavior>
    - test_correct_outcome_uses_stash: wenn die calls-Row ein `score_breakdown` mit 'process_score' hat,
      nutzt correct_outcome diesen Wert (KEIN Neu-Aufruf von _calc_process_score) und wendet nur den
      Modifier an -> final_score = clamp(round(process_score * modifier)).
    - test_correct_outcome_fallback_no_stash: fehlt 'process_score' im score_breakdown (alter Call / leeres
      jsonb), berechnet correct_outcome process_score per `_calc_process_score(conv_for_score)`-Fallback
      neu -> kein Hard-Fail, valider final_score.
    - test_correct_outcome_response_shape: Response enthaelt ok, coaching_score (float), final_score (int),
      outcome, followup_intent — Shape unveraendert (S-01).
  </behavior>
  <action>
    In routes/app_routes.py correct_outcome (Score-Block Z.1917-1929) ERSETZE den
    `_calc_coaching_score(conv_for_score, new_outcome)`-Aufruf durch Stash-Read + Modifier:

    ```python
    from database.models import ConversationLog as _CLScore
    conv_for_score = None
    if row.conversation_log_id:
        conv_for_score = db_co.query(_CLScore).filter(_CLScore.id == row.conversation_log_id).first()
    try:
        base_bd = dict(row.score_breakdown or {})
        if 'process_score' in base_bd:
            process_score = base_bd['process_score']
        else:
            process_score, base_bd = _calc_process_score(conv_for_score)  # Fallback: Stash-Miss
        coaching_score_val, final_score_val, breakdown_data = _apply_outcome_modifier(
            process_score, base_bd, new_outcome)
    except Exception as _e_score:
        print(f'[08.23.2.D.UX.4] correct_outcome score Fehler: {_e_score}')
        coaching_score_val, final_score_val, breakdown_data = 0.0, 0, {}
    row.coaching_score = coaching_score_val
    row.score_breakdown = breakdown_data
    row.score_schema_version = 1
    ```

    Response-Block (Z.1932-1941) UNVERAENDERT lassen (S-01: liefert coaching_score + final_score synchron).
    Optionaler Konsistenz-Schritt (RESEARCH Landmine 5): falls trivial, `endCallGen`-aequivalenter
    Stale-Guard ist hier NICHT noetig — Backend-Route hat keinen callGen; Race-Schutz liegt im Frontend
    (Plan 03). KEINEN Frontend-Guard ins Backend bauen.

    Control-Flow-Audit (Punkt 14): Der Modifier wird genau einmal angewandt (S-02, keine Doppelrechnung).
    Prozess-Teil kommt entweder aus dem Beenden-Stash (Normalfall) oder dem Fallback (genau einmal).
    UTF-8: Code-Identifier ASCII.
  </action>
  <verify>
    <automated>git add -A && git commit -m "feat(08.23.2.D.UX.4-01): correct_outcome reads stash + applies modifier" && git push && ssh -i ~/.ssh/nerve_vps root@178.104.82.166 'cd /opt/nerve/app && git pull && source ../venv/bin/activate && pytest tests/test_score_split.py tests/test_correct_outcome.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - grep `_apply_outcome_modifier(` routes/app_routes.py -> Treffer in correct_outcome
    - grep `'process_score' in base_bd` routes/app_routes.py -> 1 Treffer (Stash-Read mit Fallback)
    - grep `_calc_coaching_score(conv_for_score, new_outcome)` routes/app_routes.py -> 0 Treffer (alter Aufruf ersetzt)
    - test_correct_outcome_uses_stash + test_correct_outcome_fallback_no_stash + test_correct_outcome_response_shape GRUEN
    - tests/test_correct_outcome.py weiterhin alle GRUEN (Wrapper unberuehrt)
  </acceptance_criteria>
  <done>correct_outcome liest gestashten Prozess-Score, wendet nur Modifier an, Fallback bei Stash-Miss, Response-Shape unveraendert, alle Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/correct_outcome | User-gewaehltes outcome (untrusted) — bereits gegen VALID_OUTCOMES validiert (Z.1871), Ownership Call.user_id==g.user.id (Z.1883) |
| /api/beenden -> calls.score_breakdown | Server-interner Stash-Write, kein User-Input im Prozess-Score (liest nur aggregierte ConversationLog-Spalten) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-UX4-01 | Tampering | correct_outcome outcome-Wert | mitigate | Bestehende VALID_OUTCOMES-Whitelist (Z.1871) + Ownership-Filter (Z.1883) unveraendert beibehalten |
| T-UX4-02 | Information Disclosure | score_breakdown jsonb | accept | Enthaelt nur aggregierte Score-Komponenten (kb_norm, rates), keine PII; Ownership-gebunden |
| T-UX4-03 | Denial of Service | Stash-Exception in /api/beenden | mitigate | Stash in try/except non-fatal — Beenden-Pfad faellt nie wegen Score-Stash; correct_outcome-Fallback faengt fehlenden Stash |
</threat_model>

## 5. Persistenz-Schicht-Verifikation

### Angefasste Tabellen
- `calls` (lesen + schreiben: score_breakdown, coaching_score, score_schema_version, outcome)
- `conversation_logs` (lesen: kb_end, einwaende_gesamt, einwaende_behandelt, redeanteil_avg, skript_abdeckung)

### inspect.sh-Beleg (verbatim, Production 178.104.82.166, 2026-05-31)

```
                                    Table "public.calls"
        Column         |            Type             | Collation | Nullable |    Default
-----------------------+-----------------------------+-----------+----------+---------------
 id                    | uuid                        |           | not null |
 user_id               | integer                     |           | not null |
 outcome               | text                        |           |          |
 coaching_score        | double precision            |           |          |
 created_at            | timestamp without time zone |           |          |
 conversation_log_id   | integer                     |           |          |
 outcome_confidence    | double precision            |           |          |
 outcome_source        | text                        |           |          |
 followup_intent       | text                        |           | not null | 'none'::text
 score_breakdown       | jsonb                       |           |          |
 score_schema_version  | smallint                    |           | not null | '1'::smallint
Check constraints:
    "ck_calls_outcome" CHECK ((outcome = ANY (ARRAY['meeting_booked'::text, 'callback'::text, 'send_info'::text, 'wrong_person'::text, 'gatekeeper_blocked'::text, 'no_interest'::text, 'contract_signed'::text, 'unknown'::text])) OR outcome IS NULL)
    "ck_calls_outcome_source" CHECK ((outcome_source = ANY (ARRAY['ai_auto'::text, 'ai_auto_unsicher'::text, 'user_corrected'::text])) OR outcome_source IS NULL)
Foreign-key constraints:
    "fk_calls_conversation_log_id" FOREIGN KEY (conversation_log_id) REFERENCES conversation_logs(id)
```
(vollstaendiger Output in SCHEMA-EVIDENCE.md)

### Cross-Layer-Konsistenz-Tabelle

| Code-Feld | Lese-/Schreib-Pfad | Persistenz-Schicht | Verifiziert? |
|---|---|---|---|
| `_call_row.score_breakdown` (Prozess-Stash) | geschrieben in /api/beenden | DB-Spalte `calls.score_breakdown` jsonb | ✓ inspect.sh bestaetigt — existiert, keine Migration |
| `row.score_breakdown` (Stash-Read) | gelesen in correct_outcome | DB-Spalte `calls.score_breakdown` jsonb | ✓ inspect.sh bestaetigt |
| `row.coaching_score` | geschrieben in correct_outcome (NULL nach Beenden) | DB-Spalte `calls.coaching_score` double | ✓ inspect.sh bestaetigt |
| `row.score_schema_version` | geschrieben | DB-Spalte `calls.score_schema_version` smallint | ✓ inspect.sh bestaetigt |
| `conv.kb_end / einwaende_* / redeanteil_avg / skript_abdeckung` | gelesen in `_calc_process_score` | DB-Spalten in `conversation_logs` (aggregierte Werte, real persistiert) | ✓ — KEINE `log_entries`-Spalte beruehrt (D.UX-Falle vermieden) |

### Cross-Variable-Naming-Check
`_calc_process_score` liest NUR aggregierte ConversationLog-Spalten. Es beruehrt `conv.log_entries`
NICHT (das ist KEINE DB-Spalte — bekannte D.UX-Falle, nur RAM/TXT). Kein Risiko.

### Bei Diskrepanz: STOP + Replan
Keine Diskrepanz — alle Felder existieren als echte Spalten, keine Migration noetig.

<verification>
- pytest tests/test_score_split.py -x (Production-Server, client-Fixture) — alle gruen
- pytest tests/test_correct_outcome.py -x — alle 8 bestehenden gruen (Wrapper-Kompatibilitaet)
- inspect.sh schema calls auf Production unveraendert (keine neue Spalte)
</verification>

<success_criteria>
- Prozess-Score outcome-unabhaengig berechnet + in /api/beenden persistent gestasht
- correct_outcome wendet nur Modifier an, mit Fallback bei Stash-Miss
- coaching_score genau einmal final berechnet (keine Doppelrechnung — S-02/L-01 backend-seitig erfuellt)
- correct_outcome-Response-Shape unveraendert (S-01)
- KEINE Schema-Aenderung
- `_calc_call_score` (dashboard.py-Caller) unberuehrt
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.23.2.D.UX.4-call-ende-ablauf-redesign-ergebnis-vor-score/08.23.2.D.UX.4-01-SUMMARY.md`
</output>
