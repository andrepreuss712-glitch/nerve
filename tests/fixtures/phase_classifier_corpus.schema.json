{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Schema fuer tests/fixtures/phase_classifier_corpus.json (Phase 08.23.2.C, Req-12). Wird von scripts/verify_corpus_gate.py manuell validiert (kein jsonschema-Package noetig).",
  "type": "array",
  "minItems": 20,
  "items": {
    "type": "object",
    "required": ["transcript_window", "mode", "expected_phase"],
    "additionalProperties": false,
    "properties": {
      "transcript_window": {
        "type": "array",
        "items": {"type": "string"},
        "minItems": 1,
        "description": "Liste von Berater-Saetzen aus dem Gespraechsfenster (1-5 Saetze)"
      },
      "mode": {
        "enum": ["cold_call", "meeting", "gatekeeper"],
        "description": "Gespraeches-Modus: cold_call (B2B Kaltakquise), meeting (Termin), gatekeeper (Vorzimmer)"
      },
      "expected_phase": {
        "type": "integer",
        "minimum": 1,
        "maximum": 6,
        "description": "Erwartete Gespraechsphase (1-6 fuer cold_call/meeting, 1-4 fuer gatekeeper)"
      }
    }
  }
}
